---
phase: 337-audit-protocol-author-the-model-agnostic-multi-round-externa
plan: 04
type: execute
wave: 4
depends_on: ["337-01", "337-02", "337-03"]
files_modified:
  - audit/rng-audit-kit/verify-kit.sh
  - audit/rng-audit-kit/337-KIT-VALIDATION.md
autonomous: true
requirements: [RNGAUDIT-01, RNGAUDIT-02, RNGAUDIT-03, RNGAUDIT-04]
must_haves:
  truths:
    - "Every contracts/ file:line cited anywhere in the kit resolves in-range at HEAD (anchor-resolution lint passes)"
    - "The canonical freeze-invariant string is present verbatim in the '+' form and the 'and' form is absent (freeze-invariant lint passes)"
    - "The kit contains zero references to audit/FINDINGS-*.md or RNGLOCK-CATALOG.md (self-containment lint passes)"
    - "The kit contains no freeze verdict / reassurance phrasing (no-answer-key lint passes, hand-reviewed for the allowed exempt/methodology phrasings)"
    - "All 4 exempt entries present + R1->R4 all present + the 5 context-pack sections present + model-agnostic & PACKAGE-ONLY framing present (structural-presence lints pass)"
    - "All lint output is captured in 337-KIT-VALIDATION.md with the exact commands + expected vs actual, so the gate is auditable without an external model"
  artifacts:
    - path: "audit/rng-audit-kit/verify-kit.sh"
      provides: "An executable lint script running the RESEARCH §8 checks (anchor-resolution + freeze-invariant verbatim + self-containment + no-answer-key + structural presence) with a non-zero exit on any failure"
      min_lines: 60
    - path: "audit/rng-audit-kit/337-KIT-VALIDATION.md"
      provides: "The validation ledger — each §8 check as a concrete command with expected output and the captured actual result, mirroring how 335 recorded 335-LOCAL-VERIFICATION.md"
      min_lines: 50
  key_links:
    - from: "audit/rng-audit-kit/verify-kit.sh"
      to: "the authored kit files + HEAD source"
      via: "the script greps the kit + sed-resolves every cited contracts anchor against HEAD"
      pattern: "sed -n"
    - from: "audit/rng-audit-kit/337-KIT-VALIDATION.md"
      to: "verify-kit.sh output"
      via: "the ledger records the script's exit code + per-check actual output"
      pattern: "PASS"
---

<objective>
Author the TERMINAL VALIDATION GATE for the kit (RESEARCH §8): a `verify-kit.sh` lint script that runs the full §8 check set as concrete commands, and a `337-KIT-VALIDATION.md` ledger recording each check (command + expected + actual). This gate proves the kit's correctness WITHOUT ever running it through an external model — exactly the grep/test-only validation the phase requires. It re-attests all four RNGAUDIT requirements structurally (the anchors resolve, the invariant matches, the kit is self-contained + answer-key-free, the rounds + context pack + packaging are all present).

Purpose: make the documentation deliverable's correctness CHECKABLE and auditable; catch any stale anchor, leaked verdict, FINDINGS reference, or missing section before the phase closes.
Output: `audit/rng-audit-kit/verify-kit.sh` (executable) + `audit/rng-audit-kit/337-KIT-VALIDATION.md` (the ledger).

This is the LAST wave; it gates the whole kit. If any lint fails, the gate FAILS — fix the offending kit file (which lives in the earlier plans' scope) and re-run. The script is a validation artifact, NOT a contract; it touches no `contracts/*.sol`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/337-audit-protocol-author-the-model-agnostic-multi-round-externa/337-RESEARCH.md
@audit/rng-audit-kit/RNG-AUDIT-KIT.md
@audit/rng-audit-kit/CHUNK-MANIFEST.md
@audit/rng-audit-kit/337-ANCHOR-ATTESTATION.md

<interfaces>
<!-- RESEARCH §8 lint set (the concrete checks; expected output in parens). -->
<!-- 1. Anchor-resolution: extract every `contracts/...:NNN` token from the kit; for each, assert NNN is in-range for that file at HEAD (sed -n 'NNNp' returns a non-empty line). Stale pre-v50 lines (:716, 1250-1260) must be absent. -->
<!-- 2. Freeze-invariant verbatim: grep -F the canonical '+' string == 1 hit; the 'and' form == 0 hits. -->
<!-- 3. Self-containment: grep -riE 'FINDINGS-v[0-9]|audit/FINDINGS|RNGLOCK-CATALOG' over the kit dir == 0 hits. -->
<!-- 4. No-answer-key: grep -riE for verdict/reassurance phrasings == 0 hits, EXCLUDING the allowed exempt-entry structural framing + the methodology conditional ("frozen if EVERY writer"). Hand-review the handful of conditional/methodology hits. -->
<!-- 5. Exempt set: advanceGame + rawFulfillRandomWords + retryLootboxRng + rngGate each present with HEAD file:line (>=4). -->
<!-- 6. R1->R4: grep -cE '^### R[1-4] ' == 4. -->
<!-- 7. Context pack: grep -cE '^## Context Pack 4[a-e]' == 5. -->
<!-- 8. Chunk-manifest sums: per-file wc -l/-c match HEAD; group token sums add up. -->
<!-- 9. Model-agnostic + package-only: Gemini + ChatGPT recipes present; literal "PACKAGE-ONLY" + "future cycle" present. -->

<!-- Canonical freeze-invariant string ('+' form, the 337-02 target): -->
<!-- while `rngLockedFlag = true`, every storage slot that participates in any VRF-influenced output is frozen until `rngLockedFlag = false`; only the incoming VRF word + its deterministic derivations may be unknown -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author verify-kit.sh running the RESEARCH §8 lint set with non-zero exit on failure</name>
  <files>audit/rng-audit-kit/verify-kit.sh</files>
  <read_first>
    - .planning/phases/337-audit-protocol-author-the-model-agnostic-multi-round-externa/337-RESEARCH.md (§8 the full lint table — each check's concrete command + the canonical freeze-invariant string + the no-answer-key allow-list for exempt/methodology phrasings)
    - audit/rng-audit-kit/RNG-AUDIT-KIT.md (the kit body the lints check — confirm the round headings, context-pack headings, invariant string, exempt anchors, recipe, package-only phrasing are all in their final form)
    - audit/rng-audit-kit/CHUNK-MANIFEST.md (for the manifest-sum check)
    - audit/rng-audit-kit/337-ANCHOR-ATTESTATION.md (the anchor table the anchor-resolution lint cross-checks)
  </read_first>
  <action>
Create `audit/rng-audit-kit/verify-kit.sh` (a bash validation script, NOT a contract; make it executable). It must run from the repo root, target `audit/rng-audit-kit/`, print one PASS/FAIL line per check, and `exit 1` if ANY check fails (so the gate is machine-decidable). Implement these checks (RESEARCH §8):

1. ANCHOR-RESOLUTION: extract every `contracts/[^ :]*\.sol:[0-9]+` token from RNG-AUDIT-KIT.md + 337-ANCHOR-ATTESTATION.md; for each `file:line`, assert the file exists and `sed -n 'LINEp' file` returns a NON-EMPTY line (in-range at HEAD). FAIL listing any token that does not resolve. Additionally assert the stale pre-v50 markers are absent: `grep -REc ':716|1250-1260' audit/rng-audit-kit/RNG-AUDIT-KIT.md audit/rng-audit-kit/337-ANCHOR-ATTESTATION.md` is 0. When counting tokens in prose, strip markdown so a comment/header `#` cannot self-invalidate the count (use `grep -oE` to pull the tokens, not `grep -c` on raw lines).
2. FREEZE-INVARIANT VERBATIM: `grep -cF` the canonical '+' string in RNG-AUDIT-KIT.md == 1; `grep -c 'VRF word and its deterministic derivations'` == 0.
3. SELF-CONTAINMENT: `grep -riE 'FINDINGS-v[0-9]|audit/FINDINGS|RNGLOCK-CATALOG' audit/rng-audit-kit/` == 0.
4. NO-ANSWER-KEY: `grep -riE 'is frozen because|we (found|verified|confirmed)|no (writer )?escape|safe by construction|the invariant holds|proven[- ]non-participating'` over the kit dir == 0. (The allowed methodology conditional "a slot is only frozen if EVERY writer …" and the exempt-entry framing do NOT match these patterns — confirm the patterns are written so they do not false-positive on the conditional; if any hit appears, the script prints it for hand-review and FAILS so a human looks.)
5. EXEMPT SET: `grep -cE 'advanceGame|rawFulfillRandomWords|retryLootboxRng|rngGate'` in RNG-AUDIT-KIT.md >= 4.
6. R1->R4: `grep -cE '^### R[1-4] '` in RNG-AUDIT-KIT.md == 4.
7. CONTEXT PACK: `grep -cE '^## Context Pack 4[a-e]'` in RNG-AUDIT-KIT.md == 5.
8. MANIFEST SUMS: for each contract file named in CHUNK-MANIFEST.md, assert the recorded line count matches `wc -l` at HEAD (at minimum spot-check the facade DegenerusGame.sol live count appears in the manifest).
9. MODEL-AGNOSTIC + PACKAGE-ONLY: RNG-AUDIT-KIT.md contains (case-insensitive) "Gemini", "ChatGPT", "PACKAGE-ONLY", "future cycle".

Use `grep -v '^#'`-style filtering where a raw `grep -c` over a file with prose could miscount (grep gate hygiene). End with a summary line and the aggregate exit code.
  </action>
  <verify>
    <automated>test -x audit/rng-audit-kit/verify-kit.sh && bash audit/rng-audit-kit/verify-kit.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `test -x audit/rng-audit-kit/verify-kit.sh` exits 0 (executable)
    - `bash audit/rng-audit-kit/verify-kit.sh` exits 0 (ALL §8 checks pass against the finished kit)
    - the script prints a PASS line for each of the 9 checks: `bash audit/rng-audit-kit/verify-kit.sh | grep -c PASS` returns >= 9
    - the script genuinely fails on a planted defect (sanity): temporarily appending a line `see audit/FINDINGS-v49.0.md` to a copy makes self-containment FAIL — verify the script's exit is 1 in that case, then discard the copy (do NOT mutate the real kit)
    - touches no `contracts/*.sol`: `git status --porcelain contracts/` is empty
  </acceptance_criteria>
  <done>verify-kit.sh is executable, implements all 9 §8 checks with grep-gate hygiene + an anchor-resolution loop against HEAD, exits 0 on the finished kit and 1 on a planted defect.</done>
</task>

<task type="auto">
  <name>Task 2: Run the lints and record 337-KIT-VALIDATION.md (the auditable ledger)</name>
  <files>audit/rng-audit-kit/337-KIT-VALIDATION.md</files>
  <read_first>
    - audit/rng-audit-kit/verify-kit.sh (the script authored in Task 1 — its output is the ledger source)
    - .planning/phases/337-audit-protocol-author-the-model-agnostic-multi-round-externa/337-RESEARCH.md (§8 the expected output per check; the "record in a 337-KIT-VALIDATION.md ledger mirroring 335-LOCAL-VERIFICATION.md" recommendation)
    - audit/rng-audit-kit/RNG-AUDIT-KIT.md + CHUNK-MANIFEST.md + 337-ANCHOR-ATTESTATION.md (the validated artifacts)
  </read_first>
  <action>
Run `bash audit/rng-audit-kit/verify-kit.sh` and create `audit/rng-audit-kit/337-KIT-VALIDATION.md` capturing the result, mirroring the style of `335-LOCAL-VERIFICATION.md`. Author: a header stating the HEAD short SHA at validation time + the frozen-subject fact (`git diff e756a6f3 HEAD -- contracts/` empty); a table with columns Check (the §8 name) · Command · Expected · Actual · Status, one row per check (anchor-resolution, freeze-invariant verbatim, self-containment, no-answer-key, exempt-set, R1→R4, context-pack, manifest-sums, model-agnostic+package-only); the aggregate script exit code; and a one-line attestation that the kit is grep-validated without any external-model run (RNGAUDIT-01..04 structurally re-attested). Paste the actual command output (or its PASS/FAIL lines) into the Actual column so the ledger is self-evidencing. If any check is FAIL, do NOT paper over it — record the failure, fix the offending kit file (in 337-01/02/03 scope), re-run, and update the ledger to the passing state.

Note for the executor: this ledger is the phase's correctness evidence; the 337 orchestrator/checker reads it. Keep every Actual value a literal captured result, not a paraphrase.
  </action>
  <verify>
    <automated>test -f audit/rng-audit-kit/337-KIT-VALIDATION.md && grep -qiE 'anchor[- ]resolution' audit/rng-audit-kit/337-KIT-VALIDATION.md && grep -qiE 'self-containment' audit/rng-audit-kit/337-KIT-VALIDATION.md && grep -qiE 'no-answer-key' audit/rng-audit-kit/337-KIT-VALIDATION.md && grep -c PASS audit/rng-audit-kit/337-KIT-VALIDATION.md | awk '$1>=9{print "PASS"}'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f audit/rng-audit-kit/337-KIT-VALIDATION.md` exits 0
    - all 9 §8 checks are recorded as rows: `grep -ciE 'anchor[- ]resolution|freeze-invariant|self-containment|no-answer-key|exempt|R1|context pack|manifest|package-only' audit/rng-audit-kit/337-KIT-VALIDATION.md` returns >= 9
    - the ledger records PASS for every check: `grep -c PASS audit/rng-audit-kit/337-KIT-VALIDATION.md` returns >= 9
    - the frozen-subject fact is recorded: `grep -c 'e756a6f3' audit/rng-audit-kit/337-KIT-VALIDATION.md` returns >= 1
    - re-running the gate still passes: `bash audit/rng-audit-kit/verify-kit.sh` exits 0
  </acceptance_criteria>
  <done>337-KIT-VALIDATION.md records all 9 §8 checks (command + expected + actual + PASS) with the frozen-subject fact and the no-external-model attestation; the gate re-runs green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored kit → validation gate | The lint script is the boundary that proves no answer key / stale anchor / FINDINGS reference / missing section slipped through any earlier plan. A weak lint = a false-clean kit. |
| validation script ↔ contracts | The script reads contracts at HEAD (sed/wc) but must MUTATE nothing under `contracts/`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-337-10 | Tampering (false-clean lint) | verify-kit.sh | mitigate | The planted-defect sanity check (Task 1 acceptance) proves the lint actually fails on a FINDINGS reference; grep-gate hygiene (`grep -v '^#'`, `grep -oE` for tokens) prevents comment self-invalidation; the script exits 1 on any failure. |
| T-337-11 | Information Disclosure (leaked verdict survives) | no-answer-key lint | mitigate | The no-answer-key check greps the WHOLE kit dir for verdict phrasings == 0; hand-review fallback for the allowed exempt/methodology conditionals; recorded in the ledger. |
| T-337-12 | Tampering (stale anchor survives) | anchor-resolution lint | mitigate | The lint sed-resolves EVERY cited `contracts/...:NNN` against HEAD + asserts `:716`/`1250-1260` absent; fails listing any unresolved token. |
| T-337-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs; the script uses only git/grep/sed/wc already present. No Package Legitimacy Audit applies. |
</threat_model>

<verification>
- `bash audit/rng-audit-kit/verify-kit.sh` exits 0 with >= 9 PASS lines.
- The planted-defect sanity check makes the script exit 1.
- `337-KIT-VALIDATION.md` records all 9 checks as PASS + the frozen-subject SHA.
- `git status --porcelain contracts/` is empty (no contract mutation).
</verification>

<success_criteria>
- RNGAUDIT-01..04 structurally re-attested by the §8 lints: anchors resolve at HEAD, canonical invariant verbatim ('+' form), self-contained, no answer key, all 4 exempt + R1->R4 + 5 context-pack sections + model-agnostic + PACKAGE-ONLY present.
- verify-kit.sh is a reusable, machine-decidable gate; 337-KIT-VALIDATION.md is the auditable ledger; no external model was run; zero contract edits.
</success_criteria>

<output>
Create `.planning/phases/337-audit-protocol-author-the-model-agnostic-multi-round-externa/337-04-SUMMARY.md` when done.
</output>
