---
phase: 337-audit-protocol-author-the-model-agnostic-multi-round-externa
plan: 02
type: execute
wave: 2
depends_on: ["337-01"]
files_modified:
  - audit/rng-audit-kit/RNG-AUDIT-KIT.md
autonomous: true
requirements: [RNGAUDIT-01, RNGAUDIT-02]
must_haves:
  truths:
    - "The kit states ONE canonical freeze-invariant target verbatim using the REQUIREMENTS RNGAUDIT-01 wording (the '+' form), so the 337-04 lint has an exact grep target"
    - "All 4 exempt entry points are named with their re-attested HEAD anchors and a one-line structural reason (the one allowed 'given')"
    - "The protocol is a 4-round adversarial sequence R1 (catalog) -> R2 (re-derive) -> R3 (adversarial challenge) -> R4 (reconcile), each with its own heading, driving the external model's OWN discovery"
    - "R3 instructs the external model to mirror the zero-day-hunter methodology (cross-module composition, edge-of-lifecycle) as a method to emulate, NOT as a finding source"
    - "No answer key: the protocol embeds no freeze verdict, no 'we found', no FINDINGS reference"
  artifacts:
    - path: "audit/rng-audit-kit/RNG-AUDIT-KIT.md"
      provides: "The freeze-invariant target + exempt-entry set (RNGAUDIT-01) and the R1->R4 multi-round protocol (RNGAUDIT-02), prepended above the 337-01 context pack in the same file"
      min_lines: 200
      contains: "while `rngLockedFlag = true`"
  key_links:
    - from: "RNG-AUDIT-KIT.md R1 round"
      to: "the 337-01 context pack 4a-4e"
      via: "R1 instructs the model to build its catalog FROM the context pack (not handed a pre-built answer)"
      pattern: "Context Pack"
    - from: "RNG-AUDIT-KIT.md exempt set"
      to: "337-ANCHOR-ATTESTATION.md HEAD anchors"
      via: "advanceGame:154 / rawFulfillRandomWords:1735 / retryLootboxRng:1105 / rngGate:1152"
      pattern: "AdvanceModule.sol:154"
---

<objective>
Author the PROTOCOL layer of the kit into `RNG-AUDIT-KIT.md` (the same file 337-01 created): (1) the freeze-invariant TARGET stated precisely as the external auditor's single goal + the 4 exempt entry points (RNGAUDIT-01), and (2) the multi-round R1→R4 adversarial sequence designed to force the external model's OWN discovery with NO answer key (RNGAUDIT-02). These sections sit ABOVE the 337-01 context pack so the assembled file reads: invariant target → exempt set → R1→R4 → context pack.

Purpose: state the question (the invariant) and the method (R1→R4) so precisely that a cold external model produces its own freeze verdicts — the "different perspective" RNGAUDIT-02 demands — without ever seeing ours.
Output: the RNGAUDIT-01 + RNGAUDIT-02 sections prepended into `audit/rng-audit-kit/RNG-AUDIT-KIT.md`.

CANONICAL FREEZE-INVARIANT STRING — RESOLVED (RESEARCH §8 flagged the open `+`-vs-`and` wording mismatch). This plan uses the REQUIREMENTS.md / ROADMAP SC1 wording VERBATIM (the formal acceptance bar), the '+' form:
"while `rngLockedFlag = true`, every storage slot that participates in any VRF-influenced output is frozen until `rngLockedFlag = false`; only the incoming VRF word + its deterministic derivations may be unknown"
This exact string is the kit's stated target AND the 337-04 freeze-invariant lint target. The 334 sketch used "and"; we deliberately converge on the REQUIREMENTS '+' form.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/337-audit-protocol-author-the-model-agnostic-multi-round-externa/337-RESEARCH.md
@.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-RNGAUDIT-STRUCTURE-SKETCH.md
@audit/rng-audit-kit/RNG-AUDIT-KIT.md
@audit/rng-audit-kit/337-ANCHOR-ATTESTATION.md
@audit/EXTERNAL-AUDIT-PROMPT.md

<interfaces>
<!-- The 4 EXEMPT entry points (re-attested HEAD anchors from 337-01 Task 1; UNCHANGED files). -->
<!-- These are the ONE allowed near-conclusion: structural orientation telling the model WHERE the legitimate resolution writes are. -->
- advanceGame() + reachable resolution flow — AdvanceModule.sol:154 — the consume driver; it IS the daily resolution that writes VRF-derived outputs
- rawFulfillRandomWords (VRF callback) — AdvanceModule.sol:1735 (facade DegenerusGame.sol:2226) — delivers the VRF word (the entropy source)
- retryLootboxRng() failsafe — AdvanceModule.sol:1105 (facade DegenerusGame.sol:2177) — the lootbox-RNG retry, part of the resolution machinery
- rngGate(...) returns the rngWord — AdvanceModule.sol:1152 — hands the consumed word to the resolution flow

<!-- zero-day-hunter methodology (R3 tone to mirror — emulate the METHOD, not a finding source): -->
<!-- cross-contract state composition (state assumptions that hold individually but break in composition; module interaction through shared Game storage; ordering/interleaving); edge-of-lifecycle states (pre-first-purchase, exact-boundary level transitions, post-gameover residual, empty-level processing, partial multi-step gameOver between VRF steps). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the freeze-invariant target + the 4 exempt entry points (RNGAUDIT-01)</name>
  <files>audit/rng-audit-kit/RNG-AUDIT-KIT.md</files>
  <read_first>
    - audit/rng-audit-kit/RNG-AUDIT-KIT.md (the 337-01 file with the reserved protocol placeholder + the context pack the invariant references)
    - audit/rng-audit-kit/337-ANCHOR-ATTESTATION.md (the HEAD anchors for the 4 exempt entries — cite these EXACT lines)
    - .planning/REQUIREMENTS.md (RNGAUDIT-01 verbatim — the canonical '+' wording is the acceptance bar)
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-RNGAUDIT-STRUCTURE-SKETCH.md (§1 invariant + §2 exempt-set table — fill it, do not redesign)
    - .planning/phases/337-audit-protocol-author-the-model-agnostic-multi-round-externa/337-RESEARCH.md (§4 edge case: "exempt entry point labeling is SHIPPABLE", "stating the invariant target is SHIPPABLE")
  </read_first>
  <action>
Into `audit/rng-audit-kit/RNG-AUDIT-KIT.md`, in the reserved space ABOVE the context pack, author two sections.

`## 1 — The Freeze Invariant (Your Target)`: a short framing (this kit drives YOU, an external model, to independently verify ONE property of the Degenerus RNG machinery from source) followed by the canonical invariant string VERBATIM as a blockquote: `> while \`rngLockedFlag = true\`, every storage slot that participates in any VRF-influenced output is frozen until \`rngLockedFlag = false\`; only the incoming VRF word + its deterministic derivations may be unknown`. Add the equivalent restatement from the sketch §1 (no player-reachable path may write a slot that feeds a VRF-derived output after the word that resolves it becomes requestable/known, until the lock clears; the terminal-jackpot variant is enforced separately by the `_livenessTriggered` freeze). Add a one-line pointer that the mechanics + locators to verify against are in the Context Pack below. Do NOT state whether the invariant holds — that is the model's job.

`## 2 — Exempt Entry Points (the legitimate VRF-window writers)`: a table with columns Exempt entry · Anchor (HEAD file:line from the attestation) · Why exempt (one structural line). The 4 rows: advanceGame() (AdvanceModule.sol:154); rawFulfillRandomWords (AdvanceModule.sol:1735 / facade DegenerusGame.sol:2226); retryLootboxRng() (AdvanceModule.sol:1105 / facade :2177); rngGate(...) (AdvanceModule.sol:1152). Frame this verbatim-in-spirit: "These four legitimately write VRF-window slots because they ARE the resolution. You are told they are exempt; you must independently confirm nothing ELSE writes those slots. Everything outside this set must be frozen during the lock." This exempt framing is the ONLY near-conclusion the kit ships (RESEARCH §4 edge case) — it is structural orientation so the model does not false-flag advanceGame, NOT a freeze verdict.
  </action>
  <verify>
    <automated>grep -cF 'while `rngLockedFlag = true`, every storage slot that participates in any VRF-influenced output is frozen until `rngLockedFlag = false`; only the incoming VRF word + its deterministic derivations may be unknown' audit/rng-audit-kit/RNG-AUDIT-KIT.md | grep -q '^1$' && grep -cE 'AdvanceModule.sol:154|AdvanceModule.sol:1735|AdvanceModule.sol:1105|AdvanceModule.sol:1152' audit/rng-audit-kit/RNG-AUDIT-KIT.md | awk '$1>=4{print "PASS"}'</automated>
  </verify>
  <acceptance_criteria>
    - the canonical invariant string is present VERBATIM exactly once: `grep -cF 'while \`rngLockedFlag = true\`, every storage slot that participates in any VRF-influenced output is frozen until \`rngLockedFlag = false\`; only the incoming VRF word + its deterministic derivations may be unknown' audit/rng-audit-kit/RNG-AUDIT-KIT.md` returns 1
    - the string uses the '+' form, NOT 'and': `grep -c 'VRF word and its deterministic derivations' audit/rng-audit-kit/RNG-AUDIT-KIT.md` returns 0
    - all 4 exempt anchors present: `grep -cE 'AdvanceModule.sol:154|AdvanceModule.sol:1735|AdvanceModule.sol:1105|AdvanceModule.sol:1152' audit/rng-audit-kit/RNG-AUDIT-KIT.md` returns >= 4
    - no freeze verdict on the invariant: the section does NOT contain "holds" / "we verified" / "no escape" (`grep -riE 'the invariant holds|we verified|no (writer )?escape' audit/rng-audit-kit/RNG-AUDIT-KIT.md` returns 0)
  </acceptance_criteria>
  <done>Sections 1 (invariant target, canonical '+' string verbatim) and 2 (the 4 exempt entries with HEAD anchors) authored above the context pack; no freeze verdict stated.</done>
</task>

<task type="auto">
  <name>Task 2: Author the R1->R4 multi-round adversarial protocol (RNGAUDIT-02), no answer key</name>
  <files>audit/rng-audit-kit/RNG-AUDIT-KIT.md</files>
  <read_first>
    - audit/rng-audit-kit/RNG-AUDIT-KIT.md (sections 1-2 from Task 1 + the context pack — R1 references the context pack)
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-RNGAUDIT-STRUCTURE-SKETCH.md (§3 R1->R4 — the LOCKED round semantics to fill; §5 the no-answer-key / "different perspective" constraint verbatim)
    - .planning/REQUIREMENTS.md (RNGAUDIT-02 verbatim — the round definitions + "the external model performs its OWN discovery — no answer key")
    - .planning/phases/337-audit-protocol-author-the-model-agnostic-multi-round-externa/337-RESEARCH.md (§6 R3 mirrors zero-day-hunter cross-composition + edge-of-lifecycle; §4 the WITHHOLD list)
    - ~/.claude/skills/zero-day-hunter/SKILL.md (the methodology R3 emulates — cross-contract state composition + edge-of-lifecycle hunting; reference as a METHOD, not a finding source)
    - audit/EXTERNAL-AUDIT-PROMPT.md (reuse the TONE: blind review, verify-from-source, state "no issue" explicitly, do not anchor on prior audits — but be STRICTER, no pre-stated conclusions)
  </read_first>
  <action>
Into `audit/rng-audit-kit/RNG-AUDIT-KIT.md`, below sections 1-2 and above the context pack, author the four-round protocol. Lead with a short `## 3 — The Protocol (Multi-Round, Self-Driven)` framing: blind review, verify every claim from the source you are given, this is a MULTI-TURN session (run the rounds in order in one session; persist your R1 catalog and paste it back at the top of each later round so context eviction does not lose it), and the explicit no-answer-key statement verbatim-in-spirit from the sketch §5: "You are given the codebase, the methodology, and the invariant target — and nothing about what the answer should be. Re-derive everything from source. A different perspective is the entire point." Then four sub-sections with these EXACT headings:

`### R1 — Catalog the VRF Read-Graph`: instruct the model to enumerate EVERY storage slot that participates in any VRF-influenced output, with its writers AND readers across all 11 modules + Storage + the facade + peripherals, building the catalog FROM the Context Pack below (explicitly: "you build this catalog — you are not handed a pre-built answer").

`### R2 — Independently Re-Derive Each Slot's Freeze Status`: for each catalogued slot, classify as one of {frozen / reverts-if-written-during-lock / proven-non-participating}, re-derived from source (not copied). State the classification options as the model's OUTPUT categories — the kit does NOT pre-fill any slot's classification.

`### R3 — Adversarially Challenge the Catalog`: the zero-day-hunter leg. Instruct the model to attack its own R1/R2: hunt for any writer that ESCAPES the freeze (mutates a participating slot during the lock without reverting) and any CROSS-MODULE COMPOSITION that does (a sequence of calls across modules that together perturb a frozen input). Tell it to emulate the zero-day-hunter METHOD — cross-contract state composition (assumptions that hold individually but break in composition; module interaction through shared Game storage; ordering/interleaving) and edge-of-lifecycle states (pre-first-purchase, exact level-boundary transitions, post-gameover residual, empty-level processing, partial multi-step gameOver between VRF steps) — as a methodology to apply against THIS subject, explicitly NOT as a source of pre-existing findings.

`### R4 — Reconcile and Report`: reconcile R2 vs R3, resolve every discrepancy, report the verified per-slot freeze status + any escape found, in the model's OWN words. Instruct it to state "no issue found" explicitly where it finds none (do not inflate), and to cite file:line for every claim.

ANSWER-KEY DISCIPLINE: embed NO freeze verdict, NO "we found/verified/confirmed", NO "no escape", NO reference to audit/FINDINGS-*.md or RNGLOCK-CATALOG.md, NO WHALE-04/MINTDIV safety reasoning. The rounds describe WHAT THE MODEL DOES, never WHAT IT WILL CONCLUDE.
  </action>
  <verify>
    <automated>[ "$(grep -cE '^### R[1-4] ' audit/rng-audit-kit/RNG-AUDIT-KIT.md)" -eq 4 ] && grep -qi 'zero-day' audit/rng-audit-kit/RNG-AUDIT-KIT.md && ! grep -riE 'FINDINGS-v[0-9]|audit/FINDINGS|RNGLOCK-CATALOG|safe by construction|no (writer )?escape|the invariant holds' audit/rng-audit-kit/RNG-AUDIT-KIT.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - all 4 rounds present as headings: `grep -cE '^### R[1-4] ' audit/rng-audit-kit/RNG-AUDIT-KIT.md` returns exactly 4
    - R1 references the context pack as the catalog source: `grep -c 'Context Pack' audit/rng-audit-kit/RNG-AUDIT-KIT.md` returns >= 1
    - R3 names the zero-day-hunter method to emulate: `grep -ci 'zero-day' audit/rng-audit-kit/RNG-AUDIT-KIT.md` returns >= 1
    - no-answer-key (whole file): `grep -riE 'FINDINGS-v[0-9]|audit/FINDINGS|RNGLOCK-CATALOG|safe by construction|no (writer )?escape|the invariant holds|is frozen because' audit/rng-audit-kit/RNG-AUDIT-KIT.md` returns 0
    - the round semantics use the model-output framing (classification options present): `grep -cE 'reverts-if-written-during-lock' audit/rng-audit-kit/RNG-AUDIT-KIT.md` returns >= 1 (as an R2 output CATEGORY, not a per-slot verdict — hand-confirm it is presented as a choice the model makes)
  </acceptance_criteria>
  <done>R1->R4 authored with exact headings, R1 sourced from the context pack, R3 emulating the zero-day-hunter method (not as findings), R4 reporting the model's own verdicts; no-answer-key lint clean across the whole file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal verdicts → protocol prose | The R2/R4 rounds ask the model to PRODUCE freeze classifications; the kit must not pre-fill any. The boundary: ship the classification CATEGORIES, withhold every per-slot answer. |
| exempt-set framing → freeze verdict | The exempt-entry table is the one allowed near-conclusion (structural orientation). The boundary: name WHERE legitimate writes are; do NOT say the rest "is frozen" (that is the R2 output). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-337-04 | Information Disclosure | R1->R4 protocol prose | mitigate | No-answer-key lint (Task 2 acceptance, whole file): grep for verdict phrasings + FINDINGS/CATALOG refs returns 0; rounds describe model ACTIONS, never conclusions. |
| T-337-05 | Information Disclosure | invariant + exempt set | mitigate | The exempt-set is the single sanctioned near-conclusion (RESEARCH §4 edge case); the invariant is the QUESTION not the answer; Task 1 acceptance greps confirm no "holds/verified/no-escape" leaked. |
| T-337-06 | Tampering (canonical-string drift) | freeze-invariant target | mitigate | One canonical '+' string resolved (REQUIREMENTS wording); Task 1 grep asserts exactly-one verbatim match and zero 'and'-form matches; 337-04 lint re-checks. |
| T-337-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs — documentation-only; no Package Legitimacy Audit applies. |
</threat_model>

<verification>
- `grep -cE '^### R[1-4] ' audit/rng-audit-kit/RNG-AUDIT-KIT.md` == 4 (multi-round, SC2).
- The canonical invariant string present verbatim exactly once ('+' form); 'and' form absent.
- All 4 exempt anchors present at HEAD lines.
- No-answer-key + self-containment lints clean across the whole file.
</verification>

<success_criteria>
- RNGAUDIT-01: invariant target stated verbatim (canonical '+' form) + 4 exempt entries with HEAD anchors and structural reasons.
- RNGAUDIT-02: R1->R4 self-driven adversarial sequence, R3 emulating zero-day-hunter, no answer key, "different perspective" framing present.
- Assembled file reads invariant -> exempt -> R1->R4 -> context pack, coherent and lint-clean.
</success_criteria>

<output>
Create `.planning/phases/337-audit-protocol-author-the-model-agnostic-multi-round-externa/337-02-SUMMARY.md` when done.
</output>
