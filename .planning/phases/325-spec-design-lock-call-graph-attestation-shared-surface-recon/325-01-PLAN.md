---
phase: 325-spec-design-lock-call-graph-attestation-shared-surface-recon
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-PFIX-RFALL.md
  - .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-KEEP-POOL.md
  - .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-BTOMB-HERO.md
autonomous: true
requirements: [BATCH-01]
must_haves:
  truths:
    - "Every file:line cited in items 1/2/3/4/5/6's plan docs has a grep-verified verdict row resolved at HEAD da5c9d50"
    - "All anchor drift is recorded as line-number deltas; no plan-doc 'by construction' claim survives un-grepped"
    - "The IAfKing/IAfKingSubscribe withdraw/poolOf interface adds are confirmed to match AfKing.sol signatures verbatim, and AfKing.sol is confirmed otherwise needing no change"
    - "KEEP-04 VAULT registered-affiliate-code fact (owner==VAULT distinct from address-derived default) is grep-resolved to a yes/no + conditional setup step"
    - "KEEP-05 autoOpen existing-vs-new keeper capability is grep-resolved"
  artifacts:
    - path: ".planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-PFIX-RFALL.md"
      provides: "Per-anchor grep tables for items 1 (PFIX, LootboxModule) + 2 (RFALL, StakedDegenerusStonk + DegenerusGame pullRedemptionReserve)"
      contains: "Verdict legend"
    - path: ".planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-KEEP-POOL.md"
      provides: "Per-anchor grep tables for items 3 (KEEP) + 4 (POOL) incl. KEEP-04/KEEP-05/POOL-05 resolution"
      contains: "Verdict legend"
    - path: ".planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-BTOMB-HERO.md"
      provides: "Per-anchor grep tables for items 5 (BTOMB, BurnieCoin vaultAllowance + GameOverModule) + 6 (HERO, DegeneretteModule scoring/payout/award anchors)"
      contains: "Verdict legend"
  key_links:
    - from: "the 7 plan docs' cited file:line anchors"
      to: "contracts/ source at HEAD da5c9d50"
      via: "git show da5c9d50:contracts/... | grep -n + live-tree grep (tree == baseline, zero drift)"
      pattern: "MATCH|SHIFTED|ABSENT"
---

<objective>
Grep-attest every cited `file:line` anchor across SIX of the seven v48.0 plan docs (items 1 PFIX,
2 RFALL, 3 KEEP, 4 POOL, 5 BTOMB, 6 HERO) against the v47.0-closure HEAD
`da5c9d50989707c8964a9411e68c51ca1b1a25f2`, recording per-anchor MATCH / SHIFTED(±N) / ABSENT
verdicts in three grouped `325-ATTEST-*.md` files. Resolve the three pure-attestation
Claude's-Discretion items that are grep facts (KEEP-04 VAULT registered code, KEEP-05 autoOpen
capability, POOL-05 `AfKing.sol` interface-signature-match-verbatim + otherwise-unchanged). Item 7
(SWAP) attestation is owned by Plan 02; the reconciliation that consumes these verdicts is owned by
Plan 03.

Purpose: produce the load-bearing call-graph evidence so Plan 03's shared-signature reconciliation
and Phase 326's batched diff carry zero un-grepped "by construction" assumptions (the DegenerusGame
mint/jackpot inline-duplication precedent). This is the BATCH-01 attestation half.

Output: three `325-ATTEST-*.md` grep-table files under the phase directory. ZERO contracts/*.sol mutation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-CONTEXT.md

# The precedent ATTEST format to mirror (verdict legend + anchor reconciliation table + exact-body excerpt):
@.planning/phases/321-spec-design-lock-call-graph-attestation-reconciliation/321-ATTEST-TOMB.md

# The 6 plan docs whose anchors this plan attests (item 7 SWAP is Plan 02):
@.planning/PLAN-V48-PRESALE-BOX-DRAIN-FIX.md
@.planning/PLAN-V48-REDEMPTION-ETH-STETH-FALLBACK.md
@.planning/PLAN-V48-KEEPER-RENAME-AND-VAULT-CODE.md
@.planning/PLAN-V48-AFKING-POOL-RECOVERY.md
@.planning/PLAN-V48-GAMEOVER-BURNIE-TOMBSTONE.md
@.planning/PLAN-V48-DEGENERETTE-HERO-2PT-RESCALE.md

<attestation_method>
The current working tree's `contracts/` is byte-identical to the baseline HEAD
`da5c9d50989707c8964a9411e68c51ca1b1a25f2` (verified: `git diff --name-only da5c9d50 HEAD -- contracts/`
returns ZERO files). So grep the LIVE `contracts/` tree directly; each verdict is implicitly resolved
at the baseline. For belt-and-suspenders on any anchor, `git show da5c9d50:contracts/<path> | grep -n`.
Read source from `contracts/` ONLY — stale copies elsewhere must be ignored. Each ATTEST doc must
state this byte-identical-to-da5c9d50 fact in its header so the verdicts are baseline-anchored.

Verdict legend (mirror 321-ATTEST): `MATCH` (anchor lands on the claimed line) · `SHIFTED(±N)`
(content present, N lines off) · `ABSENT` (content not found / materially diverged).
</attestation_method>

<interfaces>
<!-- Grep-confirmed anchors at the baseline; executor extends, does not re-discover from scratch. -->
PFIX (item 1) — contracts/modules/DegenerusGameLootboxModule.sol:
  :671 `dgnrsOut = _presaleBoxDgnrsReward(player, amount, soldBefore);`
  :686 closing-box `swept = dgnrs.transferFromPool(...)` (the windfall sweep)
  :705 `function _presaleBoxDgnrsReward(`
  :717 `uint256 tierTenths = _presaleBoxDgnrsTierTenths(soldBefore);`
  :719-720 the `base = poolStart/100` derivation comment + `/(1_000 * 1 ether)` divisor (the F-47-01 1-line fix target)

BTOMB (item 5) — contracts/BurnieCoin.sol:
  :172-180 `struct Supply { ... uint128 vaultAllowance; }` packed with totalSupply; ctor seeds vaultAllowance 2_000_000 ether
  :256 `totalSupply()` EXCLUDES vaultAllowance · :263-264 `supplyIncUncirculated()` INCLUDES it · :270-271 `vaultMintAllowance()`
  :370 `_supply.vaultAllowance += amount128;` (the GAME-gated allowance-increase path BTOMB reuses)
  GameOverModule: burnAtGameOver invoked DegenerusGameGameOverModule.sol:142; handleFinalSweep :192 (+30d)

HERO (item 6) — contracts/modules/DegenerusGameDegeneretteModule.sol:
  :256-260 `QUICK_PLAY_PAYOUTS_N0..N4_PACKED` (9-bucket M=0..7 packed + M=8 separate)
  :331/:339-342 `HERO_SCALE` / `HERO_BOOST_N0..N4_PACKED` (the standalone multiplier HERO-02 deletes)
  (executor greps: `_countMatches`, `_applyHeroMultiplier`, `HERO_PENALTY`, `_fullTicketPayout`, `_awardDegeneretteDgnrs`/`DEGEN_DGNRS_6/7/8_BPS`, `_wwxrpBonusBucket`/`WWXRP_FACTORS_N*_PACKED`, `FT_HERO_SHIFT`, `FullTicketResult.matches`)

KEEP/POOL (items 3+4) — inline IAfKingSubscribe at DegenerusVault.sol:76 and StakedDegenerusStonk.sol:57
  (there is NO standalone contracts/interfaces/IAfKing*.sol — the interface is inline in each consumer)
  AfKing.sol: `_poolOf` slot 0 (~:195), `withdraw(uint256)` (~:318, msg.sender.call ~:327), `depositFor(address)` (~:304), `subscribe(...)` 6-arg, sweep `rngLocked`-gated (~:568)
  StakedDegenerusStonk.sol: `receive()` onlyGame (~:433), `burnAtGameOver()` onlyGame (~:525) + `balanceOf(this)==0` early-return (~:526-527), ctor subscribe (~:384)
  DegenerusVault.sol: `receive()` open (~:497), ctor subscribe (~:471)
  KEEP-03/04: AfKing affiliate-code pass site (currently `0`) + DegenerusAffiliate.sol `_setReferralCode` :436/:449/:463-476
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attest PFIX (item 1) + RFALL (item 2) anchors → 325-ATTEST-PFIX-RFALL.md</name>
  <read_first>
    - .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-01-PLAN.md (this plan, the `<interfaces>` block)
    - .planning/PLAN-V48-PRESALE-BOX-DRAIN-FIX.md (every cited anchor for item 1)
    - .planning/PLAN-V48-REDEMPTION-ETH-STETH-FALLBACK.md (every cited anchor for item 2)
    - .planning/phases/321-spec-design-lock-call-graph-attestation-reconciliation/321-ATTEST-TOMB.md (the table + exact-body-excerpt format to mirror)
    - contracts/modules/DegenerusGameLootboxModule.sol (PFIX: :671/:686/:705/:717/:719-720 + _presaleBoxDgnrsTierTenths + transferFromPool clamp)
    - contracts/StakedDegenerusStonk.sol (RFALL: _submitGamblingClaimFrom ~:844-848 totalMoney basis, ~:880-887 maxIncrement pull, :622/:932 stETH-transfer paths, ctor subscribe :384)
    - contracts/DegenerusGame.sol (RFALL: pullRedemptionReserve ~:1888-1899 CHECKED claimable[SDGNRS] debit)
  </read_first>
  <action>Create 325-ATTEST-PFIX-RFALL.md mirroring 321-ATTEST-TOMB.md: a `## Scope` + `## Sources of truth` header naming the exact contracts/ files + line counts + the byte-identical-to-da5c9d50 attestation-method note, a `## Verdict legend`, then one `## Anchor reconciliation table` per item with columns `# | Anchor (claimed) | ACTUAL (contracts/<file>) | Verdict`. For PFIX grep-verify the divisor line `/(1_000 * 1 ether)` and the `base = poolStart/100` derivation comment (:719-720) — the exact 1-line fix target — plus the closing-box sweep at :686 and `transferFromPool`'s clamp-to-live-balance. For RFALL grep-verify the `totalMoney` 4-term basis in `_submitGamblingClaimFrom`, the `maxIncrement` 175% pull site, `pullRedemptionReserve`'s CHECKED single-asset (`claimableWinnings[SDGNRS]`-only) debit with NO stETH/ETH fallback today (the F-47-02 gap to confirm present), the two existing stETH-transfer claim paths, and the v47 OPEN-E 6-arg ctor subscribe + the SUB-09 self-sub that drains claimable[SDGNRS]. Record every divergence as SHIFTED(±N) with the actual line; flag any ABSENT anchor as an IMPL blocker. End with a `## Roll-up` line stating the blocker count for these two items. NO contracts/*.sol edit.</action>
  <verify>
    <automated>bash -c 'F=.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-PFIX-RFALL.md; test -f "$F" && grep -q "Verdict legend" "$F" && grep -Eqi "MATCH|SHIFTED|ABSENT" "$F" && grep -q "1_000" "$F" && grep -q "pullRedemptionReserve" "$F" && grep -q "da5c9d50" "$F"'</automated>
  </verify>
  <acceptance_criteria>
    - 325-ATTEST-PFIX-RFALL.md exists with a Verdict legend (MATCH/SHIFTED/ABSENT) and a per-item anchor reconciliation table.
    - The PFIX table has a row attesting the `/(1_000 * 1 ether)` divisor + the `poolStart/100` derivation comment at the claimed lines (the F-47-01 fix target), and a row for the closing-box `transferFromPool` sweep + its clamp.
    - The RFALL table has a row attesting `pullRedemptionReserve` is a CHECKED single-asset claimable[SDGNRS] debit with NO ETH/stETH fallback today, a row for `_submitGamblingClaimFrom`'s totalMoney 4-term basis + maxIncrement pull, and a row for the two existing stETH-transfer claim paths.
    - Header states contracts/ is byte-identical to da5c9d50 so verdicts are baseline-anchored.
    - A Roll-up line states the IMPL-blocker count for items 1+2. No contracts/*.sol modified.
  </acceptance_criteria>
  <done>325-ATTEST-PFIX-RFALL.md committed with grep-verified verdict rows for every item-1 and item-2 anchor, blocker roll-up present, zero .sol mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Attest KEEP (item 3) + POOL (item 4) anchors + resolve KEEP-04/KEEP-05/POOL-05 → 325-ATTEST-KEEP-POOL.md</name>
  <read_first>
    - .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-01-PLAN.md (this plan, the `<interfaces>` block)
    - .planning/PLAN-V48-KEEPER-RENAME-AND-VAULT-CODE.md (item 3 anchors + KEEP-04/KEEP-05)
    - .planning/PLAN-V48-AFKING-POOL-RECOVERY.md (item 4 anchors + POOL-05/POOL-06)
    - .planning/phases/321-spec-design-lock-call-graph-attestation-reconciliation/321-ATTEST-TOMB.md (format, esp. the AfKing withdraw/poolOf rows #20)
    - contracts/AfKing.sol (sweep + mass-resolve/open entrypoints to be renamed; `withdraw(uint256)` ~:318 + `msg.sender.call` ~:327; `poolOf(address)` getter; `_poolOf` slot 0 ~:195; `depositFor` ~:304; subscribe 6-arg; rngLocked sweep gate ~:568; the affiliate-code arg currently passed as 0 into game.purchase)
    - contracts/DegenerusVault.sol (inline IAfKingSubscribe :76-86, ctor subscribe :471, open receive() :497, the gameXxx onlyVaultOwner wrapper pattern; whether VAULT holds a registered affiliate code)
    - contracts/StakedDegenerusStonk.sol (inline IAfKingSubscribe :57-67, ctor subscribe :384, receive() onlyGame ~:433, burnAtGameOver onlyGame ~:525 + balanceOf(this)==0 early-return ~:526-527)
    - contracts/DegenerusAffiliate.sol (`_setReferralCode` :436/:449/:463-476 — KEEP-03 foreclosure semantics; how a code is registered / owner==code-owner) ; if absent, grep the affiliate/referral registration path wherever it lives
    - contracts/DegenerusGame.sol + modules (the in-game mass-resolve/open crank entrypoints KEEP-01 renames; whether `autoOpen` lootbox-open is an existing keeper capability or new — KEEP-05)
  </read_first>
  <action>Create 325-ATTEST-KEEP-POOL.md with the same header + legend + per-item tables. For KEEP (item 3): grep-attest `AfKing.sol`'s `sweep` + the in-game mass-resolve/open crank entrypoint names (the rename targets), the call site where AfKing's auto-buy passes the affiliate code into `game.purchase(...)` (confirm it is currently `0`), and the `creditFlip` minted-bounty + `BOUNTY_ETH_TARGET` peg (KEEP-02, KEPT). Resolve KEEP-04 with a definitive yes/no row: does a registered affiliate code with `owner == VAULT` exist (distinct from VAULT's address-derived default)? — grep the affiliate registration storage + `_setReferralCode` semantics; if NO, record the conditional setup step (register one) as an explicit ATTEST output. Resolve KEEP-05 with a row: is opening subscribers' lootboxes (`autoOpen`) an existing keeper capability or a new one — grep the keeper's current open-path. For POOL (item 4): grep-attest `AfKing.withdraw(uint256)` (~:318) + `msg.sender.call` (~:327) + `poolOf(address)` getter + `_poolOf` slot, both inline `IAfKingSubscribe` decls (Vault :76, sStonk :57) confirming they declare ONLY `subscribe` today, sStonk `receive()` onlyGame (~:433) + `burnAtGameOver` onlyGame (~:525) + the `balanceOf(this)==0` early-return ordering (~:526-527), VAULT open `receive()` (~:497). Resolve POOL-05 with an explicit verdict: the planned interface adds `withdraw(uint256)` and `poolOf(address) returns (uint256)` MATCH `AfKing.sol`'s actual signatures verbatim, and `AfKing.sol` itself needs no other change. End with a `## Roll-up` (blocker count + the KEEP-04 yes/no + KEEP-05 existing/new + POOL-05 verbatim-match yes/no, stated explicitly). NO contracts/*.sol edit.</action>
  <verify>
    <automated>bash -c 'F=.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-KEEP-POOL.md; test -f "$F" && grep -q "Verdict legend" "$F" && grep -Eqi "MATCH|SHIFTED|ABSENT" "$F" && grep -q "KEEP-04" "$F" && grep -q "KEEP-05" "$F" && grep -q "POOL-05" "$F" && grep -Eq "withdraw\(uint256\)" "$F" && grep -Eq "poolOf\(address\)" "$F" && grep -q "da5c9d50" "$F"'</automated>
  </verify>
  <acceptance_criteria>
    - 325-ATTEST-KEEP-POOL.md exists with legend + per-item anchor tables for items 3 and 4.
    - KEEP-04 resolved with an explicit yes/no on a registered code `owner == VAULT` distinct from the address-derived default, plus the conditional register-one setup step if absent.
    - KEEP-05 resolved with an explicit existing-vs-new verdict for `autoOpen` (open subscribers' lootboxes).
    - The KEEP table confirms AfKing's auto-buy passes affiliate code `0` today (the KEEP-03 wiring target) and `creditFlip`/`BOUNTY_ETH_TARGET` exist (KEEP-02 KEPT).
    - POOL-05 resolved: `withdraw(uint256)` + `poolOf(address) returns (uint256)` match AfKing.sol verbatim, AfKing.sol otherwise unchanged; both inline IAfKingSubscribe decls (Vault :76 / sStonk :57) confirmed to declare only `subscribe` today.
    - sStonk `receive()` onlyGame + `burnAtGameOver` onlyGame + `balanceOf(this)==0` early-return ordering attested; VAULT open `receive()` attested.
    - Roll-up states blocker count + the three resolution verdicts. No contracts/*.sol modified.
  </acceptance_criteria>
  <done>325-ATTEST-KEEP-POOL.md committed with grep-verified verdict rows for every item-3 and item-4 anchor, KEEP-04/KEEP-05/POOL-05 explicitly resolved, blocker roll-up present, zero .sol mutation.</done>
</task>

<task type="auto">
  <name>Task 3: Attest BTOMB (item 5) + HERO (item 6) anchors → 325-ATTEST-BTOMB-HERO.md</name>
  <read_first>
    - .planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-01-PLAN.md (this plan, the `<interfaces>` block)
    - .planning/PLAN-V48-GAMEOVER-BURNIE-TOMBSTONE.md (item 5 anchors)
    - .planning/PLAN-V48-DEGENERETTE-HERO-2PT-RESCALE.md (item 6 anchors)
    - .planning/phases/321-spec-design-lock-call-graph-attestation-reconciliation/321-ATTEST-TOMB.md (format)
    - contracts/BurnieCoin.sol (Supply struct :172-180 packed totalSupply+vaultAllowance uint128; totalSupply() :256 excludes; supplyIncUncirculated() :263-264 includes; vaultMintAllowance() :270-271; vaultAllowance += amount128 GAME-gated path :370; ctor seed 2_000_000 ether :180; DGVB pro-rata claim math)
    - contracts/modules/DegenerusGameGameOverModule.sol (burnAtGameOver invocation :142; handleFinalSweep :192 +30d; the gameover-drain one-shot site for the BTOMB call)
    - contracts/modules/DegenerusGameDegeneretteModule.sol (`_countMatches`; `_resolveFullTicketBet`; `_fullTicketPayout` + hero-multiplier branch + M<2/M=8 carve-out; `_applyHeroMultiplier`; HERO_BOOST_N0..N4_PACKED :339-342; HERO_PENALTY; HERO_SCALE :331; FT_HERO_SHIFT decode; heroQuadrant>=4 revert; QUICK_PLAY_PAYOUTS_N0..N4_PACKED :256-260 + the M=8 separate slot; `_getBasePayoutBps`; `_awardDegeneretteDgnrs` + DEGEN_DGNRS_6/7/8_BPS; `_wwxrpBonusBucket`/`_wwxrpFactor`/WWXRP_FACTORS_N*_PACKED; FullTicketResult.matches (0-8) doc/range; the v47 resolveBets write-batch shape DGAS)
    - contracts/modules/DegenerusGameJackpotModule.sol + contracts/DegenerusGame.sol (`dailyHeroWagers`/`_rollHeroSymbol` daily-hero-symbol jackpot at DegenerusGame:2693 — confirm it reads wagers not per-bet scores, the HERO-06 no-leak attestation)
  </read_first>
  <action>Create 325-ATTEST-BTOMB-HERO.md with header + legend + per-item tables. For BTOMB (item 5): grep-attest the `Supply` struct packing (`uint128 vaultAllowance` co-packed with `totalSupply`), the constant-seed `2_000_000 ether` at ctor, that `totalSupply()` EXCLUDES vaultAllowance (:256) while `supplyIncUncirculated()`/`vaultMintAllowance()` INCLUDE it (:263-271), the existing GAME-gated `_supply.vaultAllowance += amount128` increase path (:370) BTOMB reuses, and the gameover-drain one-shot call site (GameOverModule :142 burnAtGameOver / the drain). Note the `uint128` headroom (1e36 wei flood vs ~3.4e38 max) as a CONFIRMED-FEASIBLE row (the packing/cap design itself is Plan 03's open-item; this attests the field width + the existing increase path exist). For HERO (item 6): grep-attest every named construct the rescale touches/deletes — `_countMatches`, `_resolveFullTicketBet`, `_fullTicketPayout` (+ its hero-multiplier branch + the M<2/M=8 carve-out), `_applyHeroMultiplier`, `HERO_BOOST_N0..N4_PACKED`, `HERO_PENALTY`, `HERO_SCALE`, `FT_HERO_SHIFT` (kept), `heroQuadrant >= 4` revert (kept), `QUICK_PLAY_PAYOUTS_N0..N4_PACKED` + the separate M=8 slot (the 9-bucket layout the rescale widens to 10), `_getBasePayoutBps`, `_awardDegeneretteDgnrs` + `DEGEN_DGNRS_6/7/8_BPS`, `_wwxrpBonusBucket`/`WWXRP_FACTORS_N*_PACKED`, `FullTicketResult.matches` (0-8) doc/range, and the v47 `resolveBets` write-batch shape (DGAS, the byte-identical constraint). Add an explicit HERO-06 no-leak row: `dailyHeroWagers`/`_rollHeroSymbol` (DegenerusGame:2693 + JackpotModule) reads wagered hero symbols, NOT per-bet scores — confirm the `matches`-range change cannot leak in. End with a `## Roll-up` (blocker count + the BTOMB field/path-exists confirmation + the HERO-06 no-leak verdict). NO contracts/*.sol edit.</action>
  <verify>
    <automated>bash -c 'F=.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-BTOMB-HERO.md; test -f "$F" && grep -q "Verdict legend" "$F" && grep -Eqi "MATCH|SHIFTED|ABSENT" "$F" && grep -q "vaultAllowance" "$F" && grep -q "_applyHeroMultiplier" "$F" && grep -q "QUICK_PLAY_PAYOUTS" "$F" && grep -q "dailyHeroWagers" "$F" && grep -q "da5c9d50" "$F"'</automated>
  </verify>
  <acceptance_criteria>
    - 325-ATTEST-BTOMB-HERO.md exists with legend + per-item anchor tables for items 5 and 6.
    - BTOMB table attests the `uint128 vaultAllowance` field packing, totalSupply()-excludes / supplyIncUncirculated()-includes split, the existing GAME-gated `vaultAllowance += amount128` increase path, and the gameover-drain one-shot call site; a feasibility row confirms 1e36 wei « uint128 max.
    - HERO table has a verdict row for every deleted construct (`_applyHeroMultiplier`, HERO_BOOST_N0..N4_PACKED, HERO_PENALTY, HERO_SCALE, M<2/M=8 carve-out) AND every kept/edited one (`FT_HERO_SHIFT`, heroQuadrant>=4 revert, `_countMatches`→`_score`, QUICK_PLAY_PAYOUTS_N*_PACKED, `_getBasePayoutBps`, `_awardDegeneretteDgnrs`/DEGEN_DGNRS_6/7/8_BPS, WWXRP factors, FullTicketResult.matches 0-8).
    - An explicit HERO-06 row confirms `dailyHeroWagers`/`_rollHeroSymbol` reads wagers not per-bet scores (no `matches`-range leak).
    - Header states contracts/ byte-identical to da5c9d50. Roll-up states blocker count + the two verdicts. No contracts/*.sol modified.
  </acceptance_criteria>
  <done>325-ATTEST-BTOMB-HERO.md committed with grep-verified verdict rows for every item-5 and item-6 anchor, BTOMB feasibility + HERO-06 no-leak attested, blocker roll-up present, zero .sol mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a paper-only attestation plan. The "trust boundary" is between the plan docs' *claimed*
`file:line` anchors and the *actual* baseline source — an under-attested anchor lets a stale or
absent reference reach Phase 326's batched diff un-flagged.

| Boundary | Description |
|----------|-------------|
| plan-doc anchor → contracts/ source @ da5c9d50 | Untrusted (drafted against an earlier draft HEAD); must be grep-reconciled before the IMPL diff trusts it |
| ATTEST verdict → Plan 03 reconciliation input | A rubber-stamped MATCH on drifted/absent code would propagate a wrong shared signature |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-325-01 | Tampering | A cited anchor drifted/was deleted post-v47 and the attestation rubber-stamps it | mitigate | Every anchor verdict is grep-derived from the live tree (== da5c9d50, verified zero-drift); ABSENT anchors flagged as IMPL blockers in the Roll-up, never silently passed |
| T-325-02 | Information disclosure (false-negative) | A "by construction"/"single fn reaches all paths" claim survives un-grepped (the DegenerusGame mint/jackpot inline-duplication precedent) | mitigate | Each plan-doc claim that asserts a single reachable path is decomposed into its concrete anchors and grepped; the RFALL `pullRedemptionReserve`-is-only-claimable-debit and the HERO-06 no-leak claims are explicit verdict rows |
| T-325-03 | Tampering | POOL-05 interface add does NOT match AfKing.sol verbatim → Phase 326 adds a mismatched selector → AfKing reverts | mitigate | Task 2 emits an explicit verbatim-match verdict for `withdraw(uint256)` + `poolOf(address) returns (uint256)` against AfKing.sol's actual signatures |
| T-325-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this paper-only plan; no install task exists. RESEARCH Package Legitimacy Gate N/A (no dependency adds) |
</threat_model>

<verification>
- All three ATTEST docs exist, each with a Verdict legend and the byte-identical-to-da5c9d50 header note.
- Every anchor cited in plan docs 1–6 has a verdict row (MATCH/SHIFTED(±N)/ABSENT); ABSENT rows are surfaced as blockers in each Roll-up.
- KEEP-04, KEEP-05, POOL-05 are each resolved with an explicit yes/no or existing/new verdict.
- Zero contracts/*.sol files modified: `git diff --name-only -- 'contracts/*.sol'` is empty.
</verification>

<success_criteria>
- Items 1–6 anchor attestation complete across the three ATTEST docs with grep-derived verdicts.
- The three pure-attestation Discretion items (KEEP-04, KEEP-05, POOL-05) resolved in writing.
- No "by construction" claim left un-grepped for items 1–6.
- ZERO contracts/*.sol mutation (paper-only phase).
</success_criteria>

<output>
Create `.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-01-SUMMARY.md` when done, listing the three ATTEST docs, the aggregate blocker count, and the KEEP-04/KEEP-05/POOL-05 verdicts for Plan 03 to consume.
</output>
