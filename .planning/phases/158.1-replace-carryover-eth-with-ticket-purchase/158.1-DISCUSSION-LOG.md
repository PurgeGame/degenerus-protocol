# Phase 158.1: Carryover Redesign + Final BurnieCoin Quest Cleanup - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-01
**Phase:** 158.1-replace-carryover-eth-with-ticket-purchase
**Areas discussed:** Carryover ticket mechanism, DailyQuestRolled event fate, rollDailyQuest cleanup, recordMintQuestStreak access
**Mode:** Auto (all decisions auto-selected)

---

## Carryover Ticket Mechanism

| Option | Description | Selected |
|--------|-------------|----------|
| Reuse existing _mintTicketsForTraitWinner pattern | Fold carryover tickets into payDailyJackpotCoinAndTickets flow | ✓ |
| New dedicated carryover ticket function | Separate function for carryover-specific ticket minting | |
| Direct purchaseTickets call | Use the public ticket purchase path | |

**User's choice:** [auto] Reuse existing pattern — already proven in payDailyJackpotCoinAndTickets
**Notes:** The existing _budgetToTicketUnits + _mintTicketsForTraitWinner pipeline handles trait-based ticket distribution. No reason to build a parallel path.

---

## DailyQuestRolled Event Fate

| Option | Description | Selected |
|--------|-------------|----------|
| Remove entirely | BurnieCoin-only event, DegenerusQuests has its own events | ✓ |
| Migrate to DegenerusQuests | Move event emission to quest contract | |
| Keep in BurnieCoin | Leave as-is even after removing rollDailyQuest | |

**User's choice:** [auto] Remove entirely — DegenerusQuests already has comprehensive quest events
**Notes:** JackpotModule calls quests.rollDailyQuest() directly and doesn't emit DailyQuestRolled.

---

## rollDailyQuest Cleanup

| Option | Description | Selected |
|--------|-------------|----------|
| Remove function + interface | Dead code with 0 callers | ✓ |
| Keep as convenience wrapper | Maintain backward compatibility | |

**User's choice:** [auto] Remove — confirmed 0 callers via grep
**Notes:** JackpotModule lines 644 and 743 call quests.rollDailyQuest() directly.

---

## recordMintQuestStreak Access

| Option | Description | Selected |
|--------|-------------|----------|
| GAME only (tighten) | Remove COIN access, MintModule is sole caller via GAME | ✓ |
| Keep COIN + GAME | Maintain expanded access from Phase 158 | |

**User's choice:** [auto] Tighten to GAME only — no COIN callers remain after Phase 158 cleanup
**Notes:** MintModule._questMint calls recordMintQuestStreak via IDegenerusGame(address(this)), which is GAME address.

---

## Claude's Discretion

- Order of changes (all three work areas are independent)
- Task grouping strategy
- NatSpec wording updates
- Constant renaming decisions
