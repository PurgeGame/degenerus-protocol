---
phase: 158.1-replace-carryover-eth-with-ticket-purchase
plan: 02
subsystem: contracts
tags: [solidity, dead-code-removal, access-control, BurnieCoin, DegenerusGame]

# Dependency graph
requires:
  - phase: 158-handler-integration
    provides: "Direct quest calls from game modules to DegenerusQuests (notify* wrappers removed)"
provides:
  - "BurnieCoin without rollDailyQuest, DailyQuestRolled, onlyDegenerusGameContract"
  - "DegenerusGame recordMintQuestStreak with GAME-only access"
affects: [delta-audit, known-issues]

# Tech tracking
tech-stack:
  added: []
  patterns: ["dead-code removal after call-graph pruning"]

key-files:
  created: []
  modified:
    - contracts/BurnieCoin.sol
    - contracts/interfaces/IDegenerusCoin.sol
    - contracts/DegenerusGame.sol
    - contracts/interfaces/IDegenerusGame.sol

key-decisions:
  - "OnlyGame error retained -- still used by burnForCoinflip and mintForGame"
  - "questModule/IDegenerusQuests retained -- still used by decimatorBurn.handleDecimator"
  - "onlyDegenerusGameContract in BurnieCoinflip is a separate modifier, not touched"

patterns-established:
  - "Verify callers before removing access modifiers -- OnlyGame had 2 remaining callers"

requirements-completed: [CLEANUP-02, CLEANUP-03]

# Metrics
duration: 4min
completed: 2026-04-01
---

# Phase 158.1 Plan 02: BurnieCoin Quest Passthrough Removal Summary

**Removed BurnieCoin.rollDailyQuest dead code (function, event, modifier) and tightened recordMintQuestStreak to GAME-only access**

## Performance

- **Duration:** 4 min
- **Started:** 2026-04-01T18:58:22Z
- **Completed:** 2026-04-01T19:02:30Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Removed `rollDailyQuest` function, `DailyQuestRolled` event, and `onlyDegenerusGameContract` modifier from BurnieCoin.sol (dead code with zero callers after JackpotModule direct-call migration)
- Tightened `recordMintQuestStreak` access from COIN-or-GAME to GAME-only in both DegenerusGame.sol and IDegenerusGame.sol
- Verified OnlyGame error and questModule retained (still have callers in burnForCoinflip/mintForGame and decimatorBurn respectively)
- Updated contract-level NatSpec and modifier hierarchy table in BurnieCoin.sol

## Task Details

### Task 1: Remove BurnieCoin rollDailyQuest, DailyQuestRolled event, and onlyDegenerusGameContract modifier

**Contract changes (unstaged for user review):**

1. Deleted `DailyQuestRolled` event declaration and NatSpec
2. Deleted `onlyDegenerusGameContract` modifier and NatSpec
3. Removed stale `onlyDegenerusGame` row from modifier hierarchy table
4. Deleted entire `QUEST INTEGRATION` section header and `rollDailyQuest` function
5. Updated contract-level NatSpec: access control list now reads `onlyTrustedContracts, onlyVault`
6. Verified `OnlyGame` error retained (3 uses: declaration + burnForCoinflip + mintForGame)
7. Verified `questModule` retained (2 uses: declaration + decimatorBurn.handleDecimator)
8. Confirmed `IDegenerusCoin.sol` already clean (rollDailyQuest removed in Phase 158)

### Task 2: Tighten recordMintQuestStreak access to GAME-only

**Contract changes (unstaged for user review):**

1. Simplified access check from `msg.sender != ContractAddresses.COIN && msg.sender != ContractAddresses.GAME` to `msg.sender != ContractAddresses.GAME`
2. Updated NatSpec in DegenerusGame.sol: `Access: GAME contract only (via MintModule delegatecall)`
3. Updated NatSpec in IDegenerusGame.sol: `Called by GAME contract (via MintModule delegatecall)`

## Files Modified
- `contracts/BurnieCoin.sol` - Removed rollDailyQuest, DailyQuestRolled, onlyDegenerusGameContract; updated NatSpec
- `contracts/interfaces/IDegenerusCoin.sol` - No changes needed (already clean)
- `contracts/DegenerusGame.sol` - Tightened recordMintQuestStreak to GAME-only
- `contracts/interfaces/IDegenerusGame.sol` - Updated NatSpec for recordMintQuestStreak

## Decisions Made
- OnlyGame error retained -- still used by burnForCoinflip and mintForGame (verified by grep)
- questModule/IDegenerusQuests retained -- still used by decimatorBurn.handleDecimator (verified by grep)
- onlyDegenerusGameContract in BurnieCoinflip.sol is a separate modifier defined in that contract, not in scope

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
- Compilation error in DegenerusGameAdvanceModule.sol (undeclared `dailyEthPhase` identifier) -- pre-existing from parallel Plan 01 agent's in-progress changes, not caused by this plan's edits. All 4 files modified by this plan are error-free.

## Verification Results
- `grep -c "rollDailyQuest" BurnieCoin.sol` = 0
- `grep -c "DailyQuestRolled" contracts/` = 0
- `grep -c "onlyDegenerusGameContract" BurnieCoin.sol` = 0
- `grep -c "QUEST INTEGRATION" BurnieCoin.sol` = 0
- `grep "Access control:" BurnieCoin.sol` shows `onlyTrustedContracts, onlyVault` only
- `grep -c "OnlyGame" BurnieCoin.sol` = 3 (retained)
- `grep -c "questModule" BurnieCoin.sol` = 2 (retained)
- `recordMintQuestStreak` shows GAME-only access check
- JackpotModule still calls `quests.rollDailyQuest()` directly (lines 619, 718)

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All BurnieCoin quest passthroughs removed
- Contract changes ready for user review as unstaged modifications
- Delta audit should verify the removals have no ripple effects

## Self-Check: PASSED

- SUMMARY.md: FOUND
- Dead code (rollDailyQuest, DailyQuestRolled, onlyDegenerusGameContract, QUEST INTEGRATION): 0 matches in BurnieCoin.sol
- recordMintQuestStreak: GAME-only access check confirmed, no ContractAddresses.COIN in that function
- OnlyGame error: 3 occurrences retained (declaration + 2 callers)
- questModule: 2 occurrences retained (declaration + decimatorBurn)
- Contract changes: all unstaged working-tree modifications (not committed)

---
*Phase: 158.1-replace-carryover-eth-with-ticket-purchase*
*Completed: 2026-04-01*
