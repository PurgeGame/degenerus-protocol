---
phase: 446-impl-batched-contract-diff-contract-commit-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/storage/DegenerusGameStorage.sol
  - contracts/ContractAddresses.sol
autonomous: true
requirements: [FOIL-01, MATCH-01, MATCH-02, MATCH-05, RARE-03, SEC-03]

must_haves:
  truths:
    - "foilRecord (one packed slot per level=>player, now stamping buyDay at [168-191]) and foilMatchClaimed (sparse) exist, appended at the storage tail after boxPlayers — no existing slot moved."
    - "The §U eligibility-window + foil-queue storage exists: levelStartDay (uint24=>uint24), foilTicketQueue (uint24=>address[]), foilOwedPacked, foilCursor (uint32) — all appended at the tail."
    - "A buy at level L writes foilRecord[L][player]; a re-buy at L+1 writes a DIFFERENT outer key, so L's unclaimed signatures survive (loss edge eliminated)."
    - "GAME_FOILPACK_MODULE address constant exists alongside the existing 12 GAME_*_MODULE constants."
  artifacts:
    - path: "contracts/storage/DegenerusGameStorage.sol"
      provides: "foilRecord (+ buyDay [168-191]) + foilMatchClaimed + levelStartDay + foilTicketQueue + foilOwedPacked + foilCursor; FOIL_TO_FUTURE_BPS + FOIL_SEED_TAG/FOIL_MAG_TAG/FOIL_CCY_TAG; _FOIL_* masks/shifts (incl _FOIL_BUYDAY_*); _foilRecordFor + _foilBoughtThisLevel accessors"
      contains: "foilRecord"
    - path: "contracts/ContractAddresses.sol"
      provides: "GAME_FOILPACK_MODULE address constant"
      contains: "GAME_FOILPACK_MODULE"
  key_links:
    - from: "contracts/storage/DegenerusGameStorage.sol::_foilBoughtThisLevel"
      to: "foilRecord[lvl][player]"
      via: "presence (!= 0) predicate"
      pattern: "foilRecord\\["
    - from: "contracts/storage/DegenerusGameStorage.sol::_foilRecordFor"
      to: "the packed 192-bit superset (sigs + multBps + stamp + buyDay)"
      via: "single SLOAD + mask/shift unpack"
      pattern: "_FOIL_MULT_SHIFT|_FOIL_SIG_MASK|_FOIL_BUYDAY_SHIFT"
---

<objective>
Author the STORAGE foundation of the v71 Foil Pack diff in `DegenerusGameStorage.sol` (the delegatecall-shared
base; SEC-03): the tail-appended foil state, the new constants, the `_FOIL_*` masks/shifts, and the
`_foilRecordFor`/`_foilBoughtThisLevel` accessors. Plus the new `GAME_FOILPACK_MODULE` address constant in
`ContractAddresses.sol`. Authoring storage first gives the module + the spine wiring (Plan 03) the exact slot
layout, accessor signatures, queue/cursor types, and address constant with no re-derivation.

§U DELTA (authoritative, supersedes the older §D body this plan was first built from):
- §U.2 — stamp `buyDay` `uint24` into `foilRecord` at bits `[168-191]` (reserved shrinks to `[192-255]`); add
  the `_FOIL_BUYDAY_*` mask/shift; add `mapping(uint24 => uint24) levelStartDay` (append-only, the AdvanceModule
  spine writes it in Plan 03). `claimFoilMatch` becomes 4-arg with an explicit `recLevel` — the storage side
  exposes the record load + window inputs (the window check itself lives in the Plan-03 module body).
- §U.3 — the 4 foil tickets enter the regular jackpot via a SEPARATE append-only queue, NOT `ticketQueue`:
  add `foilTicketQueue` (mirrors `ticketQueue` = `mapping(uint24 => address[])`, the SAME `wk` key from
  `_tqReadKey`/`_tqWriteKey`), a `foilOwedPacked` per-(level,player) owed counter (or fold the owed count into
  `foilRecord` reserved bits — IMPL choice), and a `foilCursor` (mirrors `ticketCursor` = `uint32`).

Purpose: deliver the per-level cap (FOIL-01 = record presence), the frozen per-(player,level) signature store
(MATCH-01), the whole-level eligibility window inputs (MATCH-02 = `buyDay` + `levelStartDay`), the
persist-per-level grief-resistance (MATCH-05), the frozen `multBps` field (RARE-03), and the no-slot-move
append (SEC-03), exactly per SPEC §D + §U. Both layout pins are RESOLVED (USER sign-off 2026-06-19):
PIN 1 = level=>player surviving-record; PIN 2 = sigs `[0-127]`/multBps `[128-143]`/stamp `[144-167]`, now
EXTENDED by §U.2 with `buyDay [168-191]` / reserved `[192-255]`.

Output: `DegenerusGameStorage.sol` gains the foil mappings + queue + cursor + constants + masks + accessors;
`ContractAddresses.sol` gains one address constant. No existing slot moved/retyped/reordered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/446-impl-batched-contract-diff-contract-commit-gate/446-CONTEXT.md
@.planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC.md

<interfaces>
<!-- Storage anchors verified at ffbd7796. -->

contracts/storage/DegenerusGameStorage.sol (the append target + reused fields/idioms):
  :157  uint256 internal constant TICKET_SCALE = 100;            // 4 whole foil tickets = quantityScaled 400
  :236  uint24 public level = 0;                                  // the raw level key (outer key of foilRecord + levelStartDay)
  :462  mapping(uint24 => uint256) internal rngWordByDay;         // retained daily VRF — claim re-derives from this
  :488  mapping(uint24 => address[]) internal ticketQueue;        // foilTicketQueue MIRRORS THIS TYPE (uint24 => address[])
  :499  uint32 internal ticketCursor;                             // foilCursor MIRRORS THIS TYPE (uint32)
  :772  function _tqWriteKey(uint24 lvl) internal view returns (uint24)  // the "wk" write key — foilTicketQueue uses the SAME key
  :777  function _tqReadKey(uint24 lvl) internal view returns (uint24)   // the "wk" read key — foilTicketQueue uses the SAME key
  :1122 mapping(address => uint256) internal whalePassClaims;     // ALREADY EXISTS — do NOT re-declare (4-of-4 does +=1)
  :1857-1876 centuryBonusUsed / _CENTURY_USED_MASK(:1864) / _centuryUsedFor  // the (level<<224)|payload stamp idiom template
  :2393 mapping(uint48 => address[]) internal boxPlayers;         // the LAST state var; closing brace at :2394
  -- APPEND all new foil state AFTER :2393, BEFORE the :2394 closing brace --

contracts/ContractAddresses.sol (the 12 existing GAME_*_MODULE constants to append alongside):
  :13-35 GAME_MINT_MODULE … GAME_AFKING_MODULE (12 address internal constant entries; add the 13th here)
</interfaces>

<the_two_resolved_pins_plus_U2_extension>
PIN 1 (RESOLVED): `mapping(uint24 => mapping(address => uint256)) internal foilRecord` — level=>player
surviving-record. The single-slot self-overwrite loss edge is ELIMINATED (distinct levels = independent records).
PIN 2 (RESOLVED) + §U.2 EXTENSION — packed bit layout of the `uint256` per (level, player):
  `sig0..sig3` at `[0-127]`, `multBps` at `[128-143]`, `rawLevel` stamp at `[144-167]`,
  **`buyDay` `uint24` at `[168-191]` (§U.2 — NEW)**, `[192-255]` reserved 0.
  `_FOIL_MULT_SHIFT = 128`, `_FOIL_STAMP_SHIFT = 144`, **`_FOIL_BUYDAY_SHIFT = 168` (NEW)**.
The 446 author reads both pins + the §U.2 extension as FINAL (SPEC §D.6 / §T / §U.2).
</the_two_resolved_pins_plus_U2_extension>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append foilRecord (+ buyDay) + foilMatchClaimed + levelStartDay + foilTicketQueue + foilOwedPacked + foilCursor + constants + masks at the storage tail</name>
  <files>contracts/storage/DegenerusGameStorage.sol</files>
  <read_first>
    - 445-SPEC.md §U.2 (the `buyDay` stamp + `levelStartDay`), §U.3 (the `foilTicketQueue` + `foilOwedPacked` + `foilCursor`), §U.4 (the storage append list — all append-only, no retype)
    - contracts/storage/DegenerusGameStorage.sol :2391-2394 (the append point: `boxPlayers` is the LAST state var at :2393, closing brace at :2394 — append BETWEEN), :157 (TICKET_SCALE), :236 (the `uint24 public level` outer key), :462 (rngWordByDay), :488 (`ticketQueue` = `mapping(uint24 => address[])` — the type foilTicketQueue mirrors), :499 (`ticketCursor` = `uint32` — the type foilCursor mirrors), :772/:777 (`_tqWriteKey`/`_tqReadKey` — the SAME wk key foilTicketQueue uses), :1122 (whalePassClaims — DO NOT re-declare), :1857-1876 (the centuryBonusUsed stamp idiom + `_CENTURY_USED_MASK` :1864 — the `private constant` mask template)
    - 445-SPEC.md §D.1 (foilRecord + the 168-bit superset D.1.1 — EXTENDED to 192-bit by §U.2), §D.1.3 (the five `_FOIL_*` masks/shifts — ADD the sixth `_FOIL_BUYDAY_*`), §D.3 (foilMatchClaimed + the keccak key D.3.1), §D.5 (no-slot-move attestation), §E.1 step 4 (`FOIL_TO_FUTURE_BPS = 2500`), §E.1 step 6 (`FOIL_SEED_TAG`), §E.5 (B) (`FOIL_MAG_TAG`/`FOIL_CCY_TAG`), §E.5 (C) (the `ETH_WIN_CAP_BPS = 1000` is reused from Degenerette, not redeclared here)
  </read_first>
  <action>
    APPEND after `boxPlayers` (:2393), before the :2394 closing brace — NO existing slot moved/retyped/reordered:
    (1) `mapping(uint24 => mapping(address => uint256)) internal foilRecord;` — the level=>player surviving-record
    (PIN 1). The packed `uint256` per (level, player) holds the §U.2-EXTENDED superset: `sig0..sig3` at `[0-127]`,
    `multBps` at `[128-143]`, `rawLevel` stamp at `[144-167]`, **`buyDay` `uint24` at `[168-191]` (§U.2)**,
    `[192-255]` reserved 0.
    (2) `mapping(bytes32 => bool) internal foilMatchClaimed;` — the sparse double-claim marker (UNIFIED name
    `foilMatchClaimed`, NOT `foilClaimed` — §1 corrected anchor #4 / V3 DEFECT E-γ).
    (3) `mapping(uint24 => uint24) internal levelStartDay;` (§U.2) — append-only; the day each raw level began.
    The AdvanceModule spine writes it ONCE at the `level = lvl` transition (Plan 03).
    (4) `mapping(uint24 => address[]) internal foilTicketQueue;` (§U.3) — mirrors `ticketQueue` (:488) EXACTLY
    in type, keyed by the SAME `wk` from `_tqWriteKey`/`_tqReadKey` (:772/:777). The 4 foil tickets queue HERE,
    NOT in `ticketQueue`.
    (5) `foilOwedPacked` — the per-(level,player) foil-owed counter: declare
    `mapping(uint24 => mapping(address => uint256)) internal foilOwedPacked;` (a small counter; the `400`
    = `4 × TICKET_SCALE` quantity governs it), OR fold the owed count into the `foilRecord` reserved bits
    `[192-255]` if the wk/level keying lines up cleanly (D-446-01/D-446-03 IMPL choice — name the chosen form in
    the SUMMARY).
    (6) `uint32 internal foilCursor;` (§U.3) — mirrors `ticketCursor` (:499); the resumable foil-drain cursor.
    (7) Six `private constant` masks/shifts (inlined like `_CENTURY_USED_MASK`:1864, zero storage footprint),
    per §D.1.3 + §U.2: `_FOIL_SIG_MASK = (uint256(1) << 32) - 1`, `_FOIL_MULT_SHIFT = 128`,
    `_FOIL_MULT_MASK = (uint256(1) << 16) - 1`, `_FOIL_STAMP_SHIFT = 144`, `_FOIL_STAMP_MASK = (uint256(1) << 24) - 1`,
    **`_FOIL_BUYDAY_SHIFT = 168`** and **`_FOIL_BUYDAY_MASK = (uint256(1) << 24) - 1`** (§U.2 — NEW).
    (8) New constants: `FOIL_TO_FUTURE_BPS = 2500` (§E.1 step 4 — the 75/25 fork; NOT the shared
    `PURCHASE_TO_FUTURE_BPS = 1000`); and the keccak-domain tags `FOIL_SEED_TAG`, `FOIL_MAG_TAG`, `FOIL_CCY_TAG`
    as `keccak256(...)` constants with DISTINCT domains (each ≠ the others ≠ `BONUS_TRAITS_TAG`). Constant
    NAMING/PLACEMENT is D-446-02/D-446-03 discretion matching house convention; literal-vs-mask per D-446-03.
    Do NOT redeclare `whalePassClaims` (:1122) or `ETH_WIN_CAP_BPS` (lives in DegeneretteModule:221).
    `level` = the raw `uint24 public level` (:236), the outer key — NEVER `_activeTicketLevel()`.
  </action>
  <verify>
    <automated>grep -q "mapping(uint24 => mapping(address => uint256)) internal foilRecord" contracts/storage/DegenerusGameStorage.sol && grep -q "foilMatchClaimed" contracts/storage/DegenerusGameStorage.sol && grep -q "mapping(uint24 => uint24) internal levelStartDay" contracts/storage/DegenerusGameStorage.sol && grep -q "foilTicketQueue" contracts/storage/DegenerusGameStorage.sol && grep -q "uint32 internal foilCursor" contracts/storage/DegenerusGameStorage.sol && grep -q "FOIL_TO_FUTURE_BPS = 2500" contracts/storage/DegenerusGameStorage.sol && grep -q "_FOIL_STAMP_SHIFT = 144" contracts/storage/DegenerusGameStorage.sol && grep -q "_FOIL_BUYDAY_SHIFT = 168" contracts/storage/DegenerusGameStorage.sol && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `mapping(uint24 => mapping(address => uint256)) internal foilRecord` declared (PIN 1, level=>player).
    - `mapping(bytes32 => bool) internal foilMatchClaimed` declared (UNIFIED name, not `foilClaimed`).
    - `mapping(uint24 => uint24) internal levelStartDay` declared (§U.2).
    - `mapping(uint24 => address[]) internal foilTicketQueue` declared (§U.3 — SAME type as `ticketQueue` :488).
    - `foilOwedPacked` declared as a per-(level,player) counter OR folded into `foilRecord` reserved bits (the chosen form named in the SUMMARY).
    - `uint32 internal foilCursor` declared (§U.3 — SAME type as `ticketCursor` :499).
    - All new state appears AFTER `boxPlayers` (:2393) — `grep -n boxPlayers` line < the `grep -n foilRecord` line; the `grep -n foilClaimed` count is 0.
    - `FOIL_TO_FUTURE_BPS = 2500` present (NOT 1000); the six `_FOIL_*` masks/shifts present with the exact values (`_FOIL_MULT_SHIFT = 128`, `_FOIL_STAMP_SHIFT = 144`, `_FOIL_BUYDAY_SHIFT = 168`).
    - `FOIL_SEED_TAG`/`FOIL_MAG_TAG`/`FOIL_CCY_TAG` declared with distinct keccak domains.
    - `git diff` shows NO change to any line at or before :2393 except additions inside the existing brace; `whalePassClaims` and `ETH_WIN_CAP_BPS` NOT re-declared.
  </acceptance_criteria>
  <done>All foil state + constants + masks appended at the tail (foilRecord with buyDay, foilMatchClaimed, levelStartDay, foilTicketQueue, foilOwedPacked, foilCursor); no existing slot moved; the no-slot-move layout golden re-pass is owned by 448.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add _foilRecordFor (now returning buyDay) + _foilBoughtThisLevel accessors</name>
  <files>contracts/storage/DegenerusGameStorage.sol</files>
  <read_first>
    - 445-SPEC.md §U.2 (the `buyDay` field the accessor now unpacks for the window), §D.1.4 (`_foilRecordFor` shape), §D.2 (`_foilBoughtThisLevel`)
    - contracts/storage/DegenerusGameStorage.sol :922 (`_claimableOf` — an existing internal view accessor shape), :1857-1876 (`_centuryUsedFor` — the stamp-unpack accessor idiom)
  </read_first>
  <action>
    Add two `internal view` accessors (no storage footprint), per §D.1.4 / §D.2 / §U.2:
    (a) `_foilRecordFor(address player, uint256 lvl)` returns `(bool present, uint16 multBps, uint24 buyDay, uint32[4] sigs)`
    — one SLOAD `packed = foilRecord[uint24(lvl)][player]`; `present = (packed != 0)`;
    `sigs[i] = uint32((packed >> (32*i)) & _FOIL_SIG_MASK)`;
    `multBps = uint16((packed >> _FOIL_MULT_SHIFT) & _FOIL_MULT_MASK)`;
    `buyDay = uint24((packed >> _FOIL_BUYDAY_SHIFT) & _FOIL_BUYDAY_MASK)` (§U.2 — the new return field the
    Plan-03 eligibility-window check consumes). The nested level key replaces the century-style stamp-compare
    auto-reset (a prior level's record lives at a DIFFERENT outer key). A retained stamp-equality assertion
    `(packed >> _FOIL_STAMP_SHIFT) & _FOIL_STAMP_MASK == lvl` is a redundant defensive check (always true) —
    include it or omit it per D-446-03 house convention; it must never change the result.
    (b) `_foilBoughtThisLevel(address player, uint256 lvl)` returns `bool`: true iff
    `foilRecord[uint24(lvl)][player] != 0` — the per-RAW-level one-pack cap (FOIL-01). Keyed on raw `level` (:236),
    NEVER `_activeTicketLevel()`. The record's presence in the level sub-map IS the cap (no separate slot).
  </action>
  <verify>
    <automated>grep -q "function _foilRecordFor" contracts/storage/DegenerusGameStorage.sol && grep -q "function _foilBoughtThisLevel" contracts/storage/DegenerusGameStorage.sol && grep -q "_FOIL_BUYDAY_SHIFT" contracts/storage/DegenerusGameStorage.sol && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `function _foilRecordFor(address player, uint256 lvl) internal view returns (bool, uint16, uint24, uint32[4] ...)` exists; one SLOAD of `foilRecord[uint24(lvl)][player]`; it unpacks `buyDay` via `_FOIL_BUYDAY_SHIFT`/`_FOIL_BUYDAY_MASK` (§U.2).
    - `function _foilBoughtThisLevel(address player, uint256 lvl) internal view returns (bool)` exists; returns `foilRecord[uint24(lvl)][player] != 0`.
    - Unpack uses `_FOIL_SIG_MASK`, `_FOIL_MULT_SHIFT`, `_FOIL_MULT_MASK`, `_FOIL_BUYDAY_SHIFT`, `_FOIL_BUYDAY_MASK` (the Task-1 constants).
    - Neither accessor reads `_activeTicketLevel()`; both key on the raw `lvl`/`level`.
  </acceptance_criteria>
  <done>Both accessors added; `_foilRecordFor` returns `buyDay` for the Plan-03 window check; the module (Plan 03) consumes them for the cap (buy step 2) and the claim record-load + eligibility window.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add GAME_FOILPACK_MODULE address constant to ContractAddresses</name>
  <files>contracts/ContractAddresses.sol</files>
  <read_first>
    - contracts/ContractAddresses.sol :13-35 (the 12 existing `address internal constant GAME_*_MODULE` entries — the exact declaration shape + placeholder/deploy-address convention to mirror)
    - 445-SPEC.md §F.2 (the new constant) ; 446-CONTEXT D-446-04 (modifiable without the per-file gate; rides the one batched commit)
  </read_first>
  <action>
    Add `address internal constant GAME_FOILPACK_MODULE = …;` alongside the existing 12 `GAME_*_MODULE`
    constants at :13-35, following the identical declaration shape and the house placeholder/deploy-address
    convention used by the neighbors (D-446-04). The literal address value follows the same convention the
    other 12 constants use at this stage of the lifecycle (placeholder vs real deploy address — match the
    neighbors exactly; this is the only acceptable value source).
  </action>
  <verify>
    <automated>grep -q "GAME_FOILPACK_MODULE" contracts/ContractAddresses.sol && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `address internal constant GAME_FOILPACK_MODULE = …;` declared in the :13-35 neighborhood.
    - The declaration shape and value convention match the existing 12 `GAME_*_MODULE` neighbors.
    - `git diff` shows NO change to the existing 12 constants.
  </acceptance_criteria>
  <done>The 13th GAME_*_MODULE constant added; the facade (Plan 03) delegatecalls `ContractAddresses.GAME_FOILPACK_MODULE`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TB-1 client → foilRecord[level] slot | the player's frozen sigs + multBps + stamp + buyDay at that level (only the player's own re-buy AT THE SAME LEVEL mutates it; distinct levels are independent records) |
| TB-4 advance → levelStartDay / foilTicketQueue / foilCursor | the spine writes (Plan 03) cross into the new appended slots; reads gate the jackpot draw |
| TB-5 claimant → foilMatchClaimed | the per-tuple double-claim marker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-445-D1 | Tampering | a new foil slot collides with an existing slot | mitigate | append-only AFTER boxPlayers (:2393); the §U.4 storage list (`foilRecord`, `foilMatchClaimed`, `levelStartDay`, `foilTicketQueue`, `foilOwedPacked`, `foilCursor`) is all append-only, no existing slot moved/retyped (supersedes the older "two new base mapping slots" count; §D.5 / §U.4; SEC-04 layout-golden re-pass at 448) |
| T-445-D3 | Denial of Service | a fast level++ strands an unclaimed match (grief) | mitigate | record persists per-level under the level=>player keying — `foilRecord[L][player]` lives at its own outer key, never touched by another level's buy / `advanceGame` / another player; `levelStartDay` bounds the window without aging the record; single-slot self-overwrite loss edge ELIMINATED (PIN 1, §D.4) |
| T-446-02-BUYDAY | Information disclosure | a malformed `buyDay`/`levelStartDay` widening the eligible window | accept | `buyDay` is stamped at buy from the raw day; `levelStartDay[L]` written once at the advance; the Plan-03 window check `[buyDay+1, levelStartDay[recLevel+1])` is closed-form; storage only EXPOSES the fields (no policy here) |
| T-445-SC | Tampering (supply chain) | npm/pip/cargo install | accept | NO new package installs — storage reuses in-repo audited rails (the centuryBonusUsed stamp idiom, the existing `whalePassClaims` slot, the `ticketQueue`/`ticketCursor` shapes); §S T-445-SC |
</threat_model>

<verification>
- `grep` confirms all foil state (foilRecord, foilMatchClaimed, levelStartDay, foilTicketQueue, foilOwedPacked, foilCursor), the constants, the six masks (incl `_FOIL_BUYDAY_*`), the two accessors, and the address constant exist with exact values.
- Append-only assertion: `boxPlayers` line index < `foilRecord` line index; no `foilClaimed` (mis-named) occurrence.
- `forge build` clean + the no-slot-move storage-layout sanity is asserted in Plan 04 (the empirical pre-gate); the full layout golden re-pass lands at 448.
</verification>

<success_criteria>
- FOIL-01: per-RAW-level one-pack cap = record presence (`_foilBoughtThisLevel`).
- MATCH-01: 4 sigs + multBps frozen per (player, level) in `foilRecord`.
- MATCH-02: the whole-level eligibility window inputs exist — `buyDay` (in `foilRecord`) + `levelStartDay` (§U.2).
- MATCH-05: records persist per-level; distinct levels are independent (loss edge eliminated).
- RARE-03: the frozen 16-bit `multBps` field lives in the slot (consumed at resolve, never live-read).
- SEC-03: storage appended in the shared base only; no existing slot moved (golden re-pass at 448).
</success_criteria>

<output>
Create `.planning/phases/446-impl-batched-contract-diff-contract-commit-gate/446-02-SUMMARY.md` when done —
NAME the chosen `foilOwedPacked` form (standalone counter vs folded into `foilRecord` reserved bits).
Do NOT commit `contracts/*.sol` — the single consolidated contract commit happens ONCE in Plan 04 (the sole
approval gate). Applying/editing `.sol` is fine; committing it is not.
</output>
