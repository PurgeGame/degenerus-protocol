---
phase: 446-impl-batched-contract-diff-contract-commit-gate
plan: 04
type: execute
wave: 3
depends_on: ["446-01", "446-02", "446-03"]
files_modified: []
autonomous: false
requirements: [SEC-03]

must_haves:
  truths:
    - "forge build is clean on the full post-IMPL subject (foil surface + the three additive spine touches)."
    - "The new GAME_FOILPACK_MODULE body, the facade-after-stubs DegenerusGame, and the touched MintModule all fit the 24,576 B EIP-170 ceiling — no duplicated batch body — with actual measured sizes recorded vs the §F estimate."
    - "The storage layout shows ALL new appended slots (foilRecord + buyDay field, foilMatchClaimed, levelStartDay, foilTicketQueue, foilOwedPacked, foilCursor) at the tail with NO existing slot moved/retyped."
    - "A both-queues readiness behaviour smoke confirms a level's jackpot cannot draw on a partially-drained foil queue."
    - "The full diff is presented to the USER grouped by track for ONE hand-review, and committed as ONE atomic contracts/*.sol commit ONLY on approval."
  artifacts:
    - path: ".planning/phases/446-impl-batched-contract-diff-contract-commit-gate/446-04-SUMMARY.md"
      provides: "the empirical-gate evidence (build + EIP-170 sizes + layout sanity over all new slots + both-queues readiness smoke + parity smoke) and the commit record"
  key_links:
    - from: "the empirical pre-gate"
      to: "the single consolidated contract commit"
      via: "USER hand-review approval"
      pattern: "CONTRACTS_COMMIT_APPROVED"
---

<objective>
Close the v71 Foil Pack IMPL with the SOLE approval gate of the milestone: run the pre-approval empirical
gate on the full post-IMPL subject (the .sol from Plans 01–03, now spanning the foil surface PLUS the three
additive §U.4 spine touches), present ONE consolidated diff grouped by track for the USER's hand-review, and —
ONLY on explicit USER approval — land ONE atomic `contracts/*.sol` commit that byte-freezes the v71 subject.

§U.4 DELTA: the edit surface widened additively to `DegenerusGameMintModule.sol` + `DegenerusGameAdvanceModule.sol`
(intrinsic to FOIL-05 + MATCH-02). So the EIP-170 re-measure now ALSO covers the touched MintModule (assert no
duplicated batch body — the single shared `_raritySymbolBatch` is reused, not cloned — and MintModule + Game
still fit); the layout sanity now covers ALL the §U.4 appended slots + the `foilRecord` `buyDay` field; and a
both-queues readiness behaviour smoke is added.

Purpose: SEC-03 EIP-170 re-measure (§F.4 HARD-REQ §6.7); a storage no-slot-move sanity over all new appended
slots; a both-queues readiness smoke; a baseline-parity smoke; and the single batch-approval commit (the only
contract-commit gate this milestone holds). This plan edits ZERO `.sol` content — it MEASURES and COMMITS.

Output: `446-04-SUMMARY.md` with the gate evidence + the commit hash; the v71 subject byte-frozen at the
approved commit. `git push` is left to the USER.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/446-impl-batched-contract-diff-contract-commit-gate/446-CONTEXT.md
@.planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC.md
@.planning/phases/446-impl-batched-contract-diff-contract-commit-gate/446-01-SUMMARY.md
@.planning/phases/446-impl-batched-contract-diff-contract-commit-gate/446-02-SUMMARY.md
@.planning/phases/446-impl-batched-contract-diff-contract-commit-gate/446-03-SUMMARY.md

<gate_facts>
- Baseline (clean) subject: contracts/ tree `99f2e53f` @ `ffbd7796`; origin/main `0bc8cf72`. `contracts/*.sol` was CLEAN at milestone start.
- EIP-170 ceiling: 24,576 B deployed. §F estimate: new module body ~8–11 KB / headroom ~13.5–16.5 KB; facade-after-stubs ~3.3–3.7 KB free. The §U.4 spine touches are SMALL additive deltas to MintModule (one param + one branch + the leftover-budget drain + foilCursor) / AdvanceModule (one SSTORE + the readiness condition) / Game — re-measure ALL touched contracts.
- The §U.4 footprint is 9 files: the 7 original foil-surface files + `DegenerusGameMintModule.sol` + `DegenerusGameAdvanceModule.sol`.
- Size/identity tool: `scripts/bytecode-manifest.sh` (`save`/`check`) — the existing bytecode-hash manifest; `forge build` + `forge inspect <Contract> bytecode` for per-contract deployed size; `forge build --sizes` for the table.
- Commit-guard hook: a PreToolUse hook BLOCKS all commits while `contracts/*.sol` is dirty; bypass for the approved contract commit ONLY via `CONTRACTS_COMMIT_APPROVED=1`. Commit ALL planning docs (the four SUMMARYs, ROADMAP, STATE) BEFORE the contract commit. `.planning/` is gitignored → force-add planning docs with `git add -f`.
- The ONLY action needing approval is the ONE `contracts/*.sol` commit. The empirical gate, the planning-doc commits, and the diff presentation run hands-off.
- Storage layout golden re-capture + the RNG-freeze re-attest are 448 deliverables — Plan 04 does a SANITY check only (append-at-tail, no slot move), not the full golden re-pass.
</gate_facts>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pre-approval empirical gate — forge build + EIP-170 re-measure (incl. MintModule, no duplicated batch body) + layout sanity over all new slots + readiness + parity smoke</name>
  <files>(measurement only — no .sol content edits)</files>
  <read_first>
    - scripts/bytecode-manifest.sh (the `save`/`check` size/identity tool + the CONTRACTS array)
    - 445-SPEC.md §F (placement + EIP-170 + the §F.4 re-measure caveat), §U.4 (the widened surface + the "no duplicated batch body" assertion + the layout append-only list), §D.5 (the no-slot-move attestation), §R (the SEC-03 row)
    - the three prior SUMMARYs (the files actually touched — now 9 — for the diff-surface sanity; the named `foilOwedPacked` form + module name)
  </read_first>
  <action>
    Run the pre-approval empirical gate on the full post-IMPL subject (the .sol from Plans 01–03):
    1. `forge build` — MUST exit 0 (clean compile). Capture any warnings.
    2. EIP-170 re-measure (SEC-03 / §F.4 / §U.4): measure the DEPLOYED bytecode size of the new
       `DegenerusGameFoilPackModule`, the post-stubs `DegenerusGame` facade, AND the §U.4-touched
       `DegenerusGameMintModule` (e.g. `forge build --sizes`, or per-contract `forge inspect <C> bytecode`
       byte-length). Record ACTUAL sizes vs the §F estimate. ASSERT no-duplicated-batch-body: the §U.3 foil-drain
       reuses the SINGLE shared `_raritySymbolBatch` (one definition) — confirm `grep -c "function _raritySymbolBatch"
       contracts/modules/DegenerusGameMintModule.sol` == 1 (the body was NOT cloned, which would have spiked
       MintModule's size). ALL touched contracts must fit 24,576 B. FAIL the gate if any exceeds the ceiling.
    3. Storage no-slot-move SANITY over ALL §U.4 appended slots: confirm `foilRecord`, `foilMatchClaimed`,
       `levelStartDay`, `foilTicketQueue`, `foilOwedPacked` (if standalone), and `foilCursor` all appear AFTER
       `boxPlayers` (`grep -n` line ordering), and that `git diff ffbd7796 -- contracts/storage/DegenerusGameStorage.sol`
       shows only ADDITIONS inside the existing brace (no moved/retyped/reordered existing declaration). Confirm the
       `foilRecord` `buyDay` field is the only payload change vs the §D pin (`_FOIL_BUYDAY_SHIFT = 168` present).
       The full forge-inspect layout golden re-capture is 448's job; this is a fast regression sanity.
    4. Both-queues readiness behaviour smoke (§U.3): a quick targeted forge check that the jackpot/advance
       readiness gate now requires BOTH `ticketCursor`-finish AND `foilCursor == foilTicketQueue.length` — i.e. a
       partially-drained foil queue blocks the draw. If a targeted test is not yet available, record the static
       confirmation (the gate reads `foilCursor` in AdvanceModule) and defer the behavioural assertion to 447 TST.
    5. Baseline-parity smoke: run a quick targeted forge test (or the existing regression-baseline smoke set) to
       confirm the foil additions + the `multBps == 0` normal-path preservation did not break existing behaviour —
       full behavioural proof is 447 TST.
    Record all five results (exit codes, actual byte sizes vs estimate incl. MintModule, the single-batch-body
    assertion, the layout-ordering check over all new slots, the readiness smoke, the parity smoke) into the
    SUMMARY. The §E.7 calibration is CONFIRM-and-REPORT only (closed-form ≈1.94; no recompute here — 447 confirms
    empirically).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` exits 0 (clean).
    - The new module, the facade-after-stubs `DegenerusGame`, AND the §U.4-touched `DegenerusGameMintModule` deployed sizes are RECORDED (actual bytes) and ALL < 24,576 B (SEC-03 / §F.4 / §U.4).
    - No duplicated batch body: `grep -c "function _raritySymbolBatch" contracts/modules/DegenerusGameMintModule.sol` == 1 (the foil-drain reuses the single shared batch).
    - ALL §U.4 appended slots (`foilRecord`, `foilMatchClaimed`, `levelStartDay`, `foilTicketQueue`, `foilOwedPacked` if standalone, `foilCursor`) appear AFTER `boxPlayers`; `git diff ffbd7796 -- contracts/storage/DegenerusGameStorage.sol` shows additions only (no existing slot moved); `_FOIL_BUYDAY_SHIFT = 168` present.
    - The both-queues readiness gate is confirmed (static at minimum: `foilCursor` read in the AdvanceModule gate; behavioural assertion deferred to 447 if no targeted test yet).
    - The baseline-parity smoke passes (or any delta is explained as an expected foil-surface addition).
    - All five gate results recorded in `446-04-SUMMARY.md`.
  </acceptance_criteria>
  <done>The empirical gate passes; the actual EIP-170 sizes (incl. MintModule) + the no-duplicated-batch-body assertion + the no-slot-move sanity over all new slots + the readiness smoke + the parity smoke are recorded; the diff is ready for hand-review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Consolidated diff presentation + the single atomic contract commit (the SOLE approval gate)</name>
  <files>contracts/*.sol (all 9 foil-surface files — the one batched commit)</files>
  <action>
    This is the SOLE approval gate of the milestone — a blocking human-verify checkpoint. It is NEVER
    auto-approvable (workflow.auto_advance is ignored here). Do NOT commit any `contracts/*.sol` without the
    USER's explicit approval of the single batched diff.

    BEFORE presenting: commit ALL planning docs (the four 446 SUMMARYs, ROADMAP, STATE) with `git add -f`
    (`.planning/` is gitignored) so the working tree carries ONLY `contracts/*.sol` for the one batched commit.

    Present the batched diff GROUPED BY TRACK for ONE auditable hand-review (D-446 diff-presentation discretion):
    (1) RARE (producers + curve): `DegenerusTraitUtils.sol` + `ActivityCurveLib.sol`;
    (2) STORAGE + WIRING: `DegenerusGameStorage.sol` + `ContractAddresses.sol`;
    (3) FOIL economics + entrypoint + MATCH claim + payout: `DegenerusGameFoilPackModule.sol` + `DegenerusGame.sol` + `IDegenerusGame.sol`;
    (4) SPINE TOUCHES (§U.4 additive): `DegenerusGameMintModule.sol` (the `_raritySymbolBatch` multBps branch + the leftover-budget foil-drain + foilCursor) + `DegenerusGameAdvanceModule.sol` (the `levelStartDay` SSTORE + the both-queues readiness gate).
    For each file: a per-file change map (what changed, which REQ-IDs / SPEC §U sections it satisfies, the §1
    corrected anchors carried — the `400` queue scale, `whalePassClaims` reuse at :1122, the `:877-915` clone
    source, `foilMatchClaimed`, `dailyHeroWagers[day-1]`). Surface the chosen `foilOwedPacked` form + the D-446-02
    module name + the §U.2 4-arg `claimFoilMatch` + the Task-1 EIP-170 actual sizes (incl. MintModule) + the
    no-duplicated-batch-body confirmation.

    On EXPLICIT USER approval ONLY: land ONE atomic commit with `CONTRACTS_COMMIT_APPROVED=1` (the only
    sanctioned bypass of the commit-guard, for this one approved contract commit). Commit all `contracts/*.sol`
    with a message like `feat(446): v71 Foil Pack — batched IMPL diff [byte-freezes the v71 subject]` ending with
    the Co-Authored-By trailer. This byte-freezes the v71 subject. `git push` is left to the USER.

    If the USER requests changes: do NOT commit; loop back, apply the requested .sol edits, re-run the Task-1
    gate, and re-present.
  </action>
  <what-built>
    The full v71 Foil Pack feature applied as ONE batched `contracts/*.sol` diff across 9 files:
    - contracts/DegenerusTraitUtils.sol (the two siblings `traitFromWordFoil` standalone + `packedTraitsFoil`)
    - contracts/libraries/ActivityCurveLib.sol (`foilBoostBps` + 5 constants)
    - contracts/storage/DegenerusGameStorage.sol (`foilRecord` + `buyDay` + `foilMatchClaimed` + `levelStartDay` + `foilTicketQueue` + `foilOwedPacked` + `foilCursor` + constants + masks + accessors)
    - contracts/ContractAddresses.sol (`GAME_FOILPACK_MODULE`)
    - contracts/modules/DegenerusGameFoilPackModule.sol (NEW — `_buyFoilPack` + the 4-arg `_claimFoilMatch` + the isolated payout)
    - contracts/DegenerusGame.sol (the two facade stubs — the 4-arg `claimFoilMatch`)
    - contracts/interfaces/IDegenerusGame.sol (the new selectors, if required)
    - contracts/modules/DegenerusGameMintModule.sol (§U.4 — the `_raritySymbolBatch` multBps branch + the leftover-budget foil-drain + foilCursor)
    - contracts/modules/DegenerusGameAdvanceModule.sol (§U.4 — the `levelStartDay` SSTORE + the both-queues readiness gate)
    The pre-approval empirical gate (Task 1) PASSED: forge build clean, EIP-170 re-measured (actual sizes incl. MintModule recorded, no duplicated batch body), storage no-slot-move sanity over all new slots, both-queues readiness smoke, baseline-parity smoke.
  </what-built>
  <how-to-verify>
    Review the batched diff track-by-track (RARE / STORAGE+WIRING / FOIL+MATCH+payout / SPINE TOUCHES) using the
    per-file change map; confirm the §1 corrected anchors, the chosen `foilOwedPacked` form, the D-446-02 module
    name, the §U.2 4-arg `claimFoilMatch`, the §U.3 separate foil queue + both-queues gate, and the Task-1
    EIP-170 actual sizes (incl. MintModule, no duplicated batch body). Approve to land the single commit, or
    describe required changes.
  </how-to-verify>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && git diff --stat ffbd7796 -- contracts/ | tail -14</automated>
  </verify>
  <resume-signal>Type "approved" to land the single batched contract commit, or describe the changes to make (which loops back to a re-gate + re-present — no commit).</resume-signal>
  <done>The single batched `contracts/*.sol` commit lands ONLY after explicit USER approval (with planning docs committed first); the v71 subject is byte-frozen at the approved commit; `git push` left to the USER.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → the committed subject | the one batched `contracts/*.sol` commit byte-freezes the v71 subject; only the USER hand-review authorizes it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-446-04-SIZE | Denial of Service | the new module / facade / the §U.4-touched MintModule exceeding EIP-170 (undeployable) | mitigate | re-measure DEPLOYED sizes on the post-IMPL build for ALL touched contracts incl. MintModule (SEC-03 / §F.4 / §U.4); assert the shared `_raritySymbolBatch` is NOT duplicated (one definition); FAIL the gate if any > 24,576 B before presenting |
| T-446-04-SLOT | Tampering | an unintended existing-slot move slips into the diff (now 6 new slots) | mitigate | the no-slot-move sanity (`git diff ffbd7796 --` additions-only) over ALL §U.4 appended slots before commit; the full layout golden re-pass at 448 |
| T-446-04-GATE | Tampering | the both-queues readiness gate regressed (foil under-resolution) | mitigate | the readiness smoke confirms the `foilCursor == foilTicketQueue.length` condition gates the draw (static min; behavioural at 447) before the commit |
| T-446-04-COMMIT | Elevation of Privilege | an unapproved `contracts/*.sol` commit | mitigate | the commit-guard hook BLOCKS commits while contracts are dirty; the single batched commit requires explicit USER approval + `CONTRACTS_COMMIT_APPROVED=1`; this checkpoint is NEVER auto-approvable (workflow.auto_advance ignored) |
| T-445-SC | Tampering (supply chain) | npm/pip/cargo install | accept | NO new package installs in the entire 446 diff — all reuse is in-repo audited rails; §S T-445-SC |
</threat_model>

<verification>
- `forge build` exit 0; the touched deployed sizes recorded (incl. MintModule) and all < 24,576 B; `_raritySymbolBatch` not duplicated.
- The storage no-slot-move sanity passes over all §U.4 appended slots (additions-only diff vs `ffbd7796`).
- The both-queues readiness smoke passes (static min).
- The baseline-parity smoke passes.
- The single batched `contracts/*.sol` commit lands ONLY after explicit USER approval; planning docs committed first.
</verification>

<success_criteria>
- SEC-03: EIP-170 re-measured on the post-IMPL build for the module + facade + the §U.4-touched MintModule; all fit (actual sizes recorded); no duplicated batch body.
- The full v71 IMPL (9-file footprint) is committed as ONE atomic, USER-approved `contracts/*.sol` commit — the sole approval gate — byte-freezing the v71 subject.
- `git push` left to the USER; 447 TST / 448 REAUDIT / 449 TERMINAL follow.
</success_criteria>

<output>
Create `.planning/phases/446-impl-batched-contract-diff-contract-commit-gate/446-04-SUMMARY.md` when done —
record the five gate results (build, EIP-170 actual sizes vs estimate incl. MintModule + no-duplicated-batch-body,
layout sanity over all new slots, readiness smoke, parity smoke) and the final commit hash. The single contract
commit is the only gated action; everything else runs hands-off.
</output>
