---
phase: 446-impl-batched-contract-diff-contract-commit-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/DegenerusTraitUtils.sol
  - contracts/libraries/ActivityCurveLib.sol
autonomous: true
requirements: [RARE-01, RARE-02, RARE-04]

must_haves:
  truths:
    - "A new sibling producer rolls foil traits with a tapered, multiplier-scaled color ladder; the v70-frozen producers are byte-identical after the diff."
    - "A new foilBoostBps(score) curve returns the locked bps table (20000 @ 0, 50000 @ 300, 55000 @ 500, 60000 @ 30000+)."
    - "Gold (color 7) probability per quadrant = 0.78125% × M exactly; commons (0/1/2) are the sole shrink sink."
  artifacts:
    - path: "contracts/DegenerusTraitUtils.sol"
      provides: "traitFromWordFoil + packedTraitsFoil sibling producers (tapered /15360 color ladder; symbol uniform 1/8)"
      contains: "function packedTraitsFoil"
    - path: "contracts/libraries/ActivityCurveLib.sol"
      provides: "foilBoostBps(score) 4-anchor/3-segment curve + the 5 FOIL_* constants"
      contains: "function foilBoostBps"
  key_links:
    - from: "contracts/DegenerusTraitUtils.sol::packedTraitsFoil"
      to: "contracts/DegenerusTraitUtils.sol::traitFromWordFoil"
      via: "per-lane call"
      pattern: "traitFromWordFoil"
    - from: "contracts/DegenerusTraitUtils.sol::traitFromWordFoil"
      to: "the /15360 cutoff ladder"
      via: "multBps-scaled color stage"
      pattern: "15360"
---

<objective>
Author the RARE foundation of the v71 Foil Pack diff: the two NEW sibling trait producers
(`traitFromWordFoil` / `packedTraitsFoil`) in `DegenerusTraitUtils.sol` and the NEW activity curve
`foilBoostBps(score)` in `ActivityCurveLib.sol`. These are pure-library primitives consumed by the foil
module (Plan 03); authoring them first gives the executor the exact producer/curve contracts with no
codebase scavenger hunt.

Purpose: deliver the activity-scaled rarity boost mechanism — RARE-01 (new sibling, frozen producers
untouched), RARE-02 (×2→×6 curve), RARE-04 (tapered tail, gold ≈4.7%/quadrant @ M=6) — exactly per
SPEC §A.1 and §A.2. Both files are `internal pure`/`internal`, no storage footprint, additive only.

Output: `DegenerusTraitUtils.sol` gains the two siblings; `ActivityCurveLib.sol` gains the curve + 5
constants. No existing function touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/446-impl-batched-contract-diff-contract-commit-gate/446-CONTEXT.md
@.planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC.md

<critical_path_note>
The SPEC and 446-CONTEXT cite `contracts/libraries/DegenerusTraitUtils.sol` — the ACTUAL path is
`contracts/DegenerusTraitUtils.sol` (root, NOT under libraries/). `ActivityCurveLib.sol` IS correctly under
`contracts/libraries/`. Edit the real paths in `files_modified`.
</critical_path_note>

<interfaces>
<!-- Producer/curve contracts and clone analogs the executor authors against. Verified at ffbd7796. -->

Clone analog — contracts/DegenerusTraitUtils.sol (do NOT edit these; clone their shape):
  :91   library DegenerusTraitUtils {
  :115  function weightedColorBucket(uint32 rnd) internal pure returns (uint8)   // FROZEN (RARE-01) — color cutoff-ladder template; /256 chain
  :143  function traitFromWord(uint64 rnd) internal pure returns (uint8)          // FROZEN
  :169  function packedTraitsFromSeed(uint256 rand) internal pure returns (uint32)// FROZEN — the [QQ][CCC][SSS]-per-byte pack layout to MATCH
  :201  function packedTraitsDegenerette(uint256 rand) internal pure returns (uint32) // the structural clone source for packedTraitsFoil
  :218  function _degTrait(uint64 rnd) private pure returns (uint8)               // the structural clone source for traitFromWordFoil (symbol stage copied verbatim)

Existing curve analogs — contracts/libraries/ActivityCurveLib.sol (do NOT edit; clone their shape):
  :26   ACTIVITY_SEG_B_KNEE_POINTS = 500       // REUSE this shared knee
  :29   ACTIVITY_EFFECTIVE_CAP_POINTS = 30_000 // REUSE this shared knee
  :35-39 MULT_MIN_BPS / MULT_K_POINTS / MULT_VA_BPS / MULT_VB_BPS / MULT_MAX_BPS  // constant-block shape to mirror
  :42-61 function decMultBps(uint256 score) internal pure returns (uint256)       // the segment-interp shape to mirror
  :74-90 function centuryBps(uint256 score) internal pure returns (uint256)       // second analog
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add foilBoostBps(score) curve + 5 FOIL_* constants to ActivityCurveLib</name>
  <files>contracts/libraries/ActivityCurveLib.sol</files>
  <read_first>
    - contracts/libraries/ActivityCurveLib.sol :26, :29 (the two shared knees to REUSE), :35-61 (`decMultBps` + its MULT_* constant block — the shape to mirror), :74-90 (`centuryBps` second analog)
    - 445-SPEC.md §A.2 (the curve: A.2.1 the 5 constants, A.2.2 the 4 segment closed forms, A.2.3 the value table, A.2.5 invariants)
  </read_first>
  <action>
    Add a new `function foilBoostBps(uint256 score) internal pure returns (uint256)` to `ActivityCurveLib`,
    modeled byte-for-byte on the `decMultBps` segment-interp shape (§A.2.2). Add the 5 constants per §A.2.1
    (mirror the `MULT_*` block placement/naming):
    `FOIL_MIN_BPS = 20_000`, `FOIL_K_POINTS = 300`, `FOIL_VA_BPS = 50_000`, `FOIL_VB_BPS = 55_000`,
    `FOIL_MAX_BPS = 60_000`. REUSE the two existing shared knees `ACTIVITY_SEG_B_KNEE_POINTS = 500` (:26) and
    `ACTIVITY_EFFECTIVE_CAP_POINTS = 30_000` (:29) — do NOT redeclare them.
    Segment closed forms (§A.2.2, integer floor division, explicit endpoint guards so endpoints are exact):
    `score == 0 ⇒ return 20000`; seg A `0 < score ≤ 300`: `20000 + 100*score`; seg B `300 < score ≤ 500`:
    `50000 + 25*(score - 300)`; seg C `500 < score < 30000`: `55000 + (score - 500)*5000/29500`;
    `score >= 30000 ⇒ return 60000`. Use the `decMultBps`-identical interpolation form
    `V_lo + (score - knee_lo)*(V_hi - V_lo)/(knee_hi - knee_lo)` with the two guards short-circuiting first.
    `350` is NOT a breakpoint (D-02) — do not add a knee there. Do NOT edit `decMultBps`/`centuryBps`/`decBucket`.
  </action>
  <verify>
    <automated>grep -q "function foilBoostBps" contracts/libraries/ActivityCurveLib.sol && grep -q "FOIL_MIN_BPS = 20_000" contracts/libraries/ActivityCurveLib.sol && grep -q "FOIL_MAX_BPS = 60_000" contracts/libraries/ActivityCurveLib.sol && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `function foilBoostBps(uint256 score) internal pure returns (uint256)` exists.
    - Five constants exist with EXACT values: `FOIL_MIN_BPS = 20_000`, `FOIL_K_POINTS = 300`, `FOIL_VA_BPS = 50_000`, `FOIL_VB_BPS = 55_000`, `FOIL_MAX_BPS = 60_000`.
    - The two shared knees (`ACTIVITY_SEG_B_KNEE_POINTS`, `ACTIVITY_EFFECTIVE_CAP_POINTS`) are REUSED, not redeclared.
    - Guards: `score == 0` returns exactly `20000`; `score >= 30000` returns exactly `60000`.
    - `git diff` shows ZERO edits to `decMultBps`, `centuryBps`, `decBucket`, `minScoreForBucket`, or the existing `MULT_*`/`CENTURY_*` constant blocks.
  </acceptance_criteria>
  <done>foilBoostBps + 5 constants added; existing curves byte-untouched; the value table of §A.2.3 reproduces by the closed forms.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add traitFromWordFoil + packedTraitsFoil sibling producers to DegenerusTraitUtils</name>
  <files>contracts/DegenerusTraitUtils.sol</files>
  <read_first>
    - contracts/DegenerusTraitUtils.sol :115-126 (`weightedColorBucket` — the /256 color cutoff-ladder CONTROL FLOW to mirror in /15360), :169-200 (`packedTraitsFromSeed` — the [QQ][CCC][SSS]-per-byte pack layout to MATCH), :201-223 (`packedTraitsDegenerette` + `_degTrait` — the structural clone source; symbol stage copied verbatim)
    - 445-SPEC.md §A.1.1 (the two siblings), §A.1.3 (the taper rule + rare-rank weights), §A.1.4 (the /15360 integer cutoff ladder — build-ready exact), §A.1.6 (per-tier probability table for self-check)
  </read_first>
  <action>
    Add to `library DegenerusTraitUtils` two NEW siblings, cloned STRUCTURALLY from
    `packedTraitsDegenerette`/`_degTrait` (:201-223): ONLY the color stage changes; the symbol stage and the
    pack layout are copied verbatim.
    (a) `function traitFromWordFoil(uint64 rnd, uint256 multBps) internal pure returns (uint8)` — per-quadrant
    6-bit `[CCC][SSS]`: COLOR from the §A.1.4 `/15360` tapered cutoff ladder (`multBps` enters here, `10000 = 1×`);
    SYMBOL uniform 1/8 from `rnd >> 32 & 7` (byte-identical to `_degTrait`'s symbol stage). Quadrant bits added
    by the caller. The /15360 ladder (§A.1.4, exact): per rare tier `c ∈ {3,4,5,6,7}` with `w5 = {3:1,4:2,5:3,6:4,7:5}`,
    `width15360[c] = base[c]*60 * (50000 + (multBps - 10000)*w5[c]) / 50000`; baseline color widths
    `base[] = {0:64,1:64,2:64,3:32,4:16,5:8,6:6,7:2}` (/256, verified at `weightedColorBucket` :115-126);
    `rare = Σ width15360[c]`; `rem = 15360 - rare`; the three commons each get `rem/3` and **color 0 additionally
    absorbs `rem mod 3`**; walk the running-sum cutoff chain in fixed order exactly like `weightedColorBucket`'s
    `if (scaled < …)` chain, scaling the draw into `[0, 15360)`. Gold lands exactly on `width15360[7] = 120*M`.
    (b) `function packedTraitsFoil(uint256 rand, uint256 multBps) internal pure returns (uint32)` — calls
    `traitFromWordFoil` on each 64-bit lane (`uint64(rand)`, `>>64`, `>>128`, `>>192`), OR's in the quadrant
    identifiers (`| 64`, `| 128`, `| 192`), and packs four bytes in the IDENTICAL `[QQ][CCC][SSS]`-per-byte
    layout as `packedTraitsFromSeed`/`packedTraitsDegenerette` (direct byte compare).
    RARE-01 HARD CONSTRAINT: do NOT edit, retype, or move `weightedColorBucket` (:115), `traitFromWord` (:143),
    or `packedTraitsFromSeed` (:169) — the foil path is purely additive.
  </action>
  <verify>
    <automated>grep -q "function traitFromWordFoil" contracts/DegenerusTraitUtils.sol && grep -q "function packedTraitsFoil" contracts/DegenerusTraitUtils.sol && grep -q "15360" contracts/DegenerusTraitUtils.sol && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `function traitFromWordFoil(uint64 rnd, uint256 multBps) internal pure returns (uint8)` exists.
    - `function packedTraitsFoil(uint256 rand, uint256 multBps) internal pure returns (uint32)` exists.
    - The /15360 super-ladder is present (literal `15360` appears); gold width is `120 * M`-derived (`base[7]*60`).
    - The symbol stage matches `_degTrait` (`rnd >> 32 & 7`, uniform 1/8).
    - The pack OR's quadrant identifiers `| 64`/`| 128`/`| 192` in the same byte order as `packedTraitsFromSeed`.
    - `git diff` shows ZERO edits to `weightedColorBucket` (:115), `traitFromWord` (:143), `packedTraitsFromSeed` (:169) (RARE-01).
  </acceptance_criteria>
  <done>Both siblings added; frozen producers byte-untouched; the §A.1.6 per-tier table reproduces from the ladder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → traitFromWordFoil/foilBoostBps | the frozen `multBps`/`score` inputs cross into the rarity ladder (pure functions; no state) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-446-01-RARE | Tampering | the v70-frozen shared producers (`weightedColorBucket`/`traitFromWord`/`packedTraitsFromSeed`) | mitigate | foil path is PURELY ADDITIVE — new siblings only; `git diff` asserts zero edits to :115/:143/:169 (RARE-01); layout/byte goldens re-attested at 448 |
| T-446-01-CURVE | Information disclosure | `multBps` out-of-range producing invalid PMF widths | accept | curve range locked `[20000, 60000]`; §A.1.5 proves all 8 ladder widths ≥ 0 over the full grid (valid to M ≈ 8.87, far beyond the locked [2,6]) |
| T-445-SC | Tampering (supply chain) | npm/pip/cargo install | accept | NO new package installs — both edits reuse in-repo audited library rails (`packedTraitsDegenerette`, `ActivityCurveLib`); paper-trail per SPEC §S T-445-SC |
</threat_model>

<verification>
- `grep` confirms both siblings + the curve + the 5 constants exist with exact values.
- `git diff` confirms the three frozen producers and the existing two curves are byte-untouched.
- `forge build` clean is asserted in Plan 04 (the empirical pre-gate) after all foil code lands.
</verification>

<success_criteria>
- RARE-01: new sibling producers added; frozen producers untouched (git-diff verified).
- RARE-02: `foilBoostBps` delivers ×2→×6 over [20000, 60000] via the locked 4-anchor/3-segment curve.
- RARE-04: the tapered /15360 ladder lifts all rare tiers; gold = 120·M (≈4.7%/quadrant @ M=6).
</success_criteria>

<output>
Create `.planning/phases/446-impl-batched-contract-diff-contract-commit-gate/446-01-SUMMARY.md` when done.
Do NOT commit `contracts/*.sol` — the single consolidated contract commit happens ONCE in Plan 04 (the sole
approval gate). Applying/editing `.sol` is fine; committing it is not.
</output>
