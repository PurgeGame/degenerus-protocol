---
phase: 446-impl-batched-contract-diff-contract-commit-gate
plan: 03
type: execute
wave: 2
depends_on: ["446-01", "446-02"]
files_modified:
  - contracts/modules/DegenerusGameFoilPackModule.sol
  - contracts/DegenerusGame.sol
  - contracts/interfaces/IDegenerusGame.sol
  - contracts/modules/DegenerusGameMintModule.sol
  - contracts/modules/DegenerusGameAdvanceModule.sol
autonomous: true
requirements: [FOIL-02, FOIL-03, FOIL-04, FOIL-05, RARE-03, MATCH-01, MATCH-02, MATCH-03, MATCH-04, MATCH-05, MATCH-06, MATCH-07, MATCH-08, MATCH-09, SEC-03]

must_haves:
  truths:
    - "buyFoilPack() charges 10×priceForLevel, rejects the afking leg, routes 75/25, freezes 4 boosted sigs + multBps + buyDay, queues 4 foil tickets into foilTicketQueue + foilOwedPacked (NOT ticketQueue), one-per-raw-level."
    - "claimFoilMatch(day, ticketIndex, drawKind, recLevel) loads foilRecord[recLevel][player], enforces the [buyDay+1, levelStartDay[recLevel+1]) window, re-derives the LIVE + HERO-FREE winning sets, counts exact 6-bit positional matches, gates 4-of-4 on heroFreeCount, pays the isolated 40/40/20 spin, marks-before-payout."
    - "Foil-owed jackpot entries resolve via the shared _raritySymbolBatch (multBps param) whose :759 line branches traitFromWord ↔ traitFromWordFoil — real color==7 gold — without editing the frozen producers; the both-queues readiness gate blocks the draw until foilCursor == foilTicketQueue.length."
  artifacts:
    - path: "contracts/modules/DegenerusGameFoilPackModule.sol"
      provides: "_buyFoilPack + _claimFoilMatch + the isolated 40/40/20 spin payout + the LIVE/HERO-FREE re-derivation + the eligibility window"
      min_lines: 200
      contains: "_buyFoilPack"
    - path: "contracts/DegenerusGame.sol"
      provides: "buyFoilPack() + claimFoilMatch(day, ticketIndex, drawKind, recLevel) thin facade stubs (delegatecall GAME_FOILPACK_MODULE)"
      contains: "buyFoilPack"
    - path: "contracts/modules/DegenerusGameMintModule.sol"
      provides: "_raritySymbolBatch multBps param + :759 branch; processTicketBatch leftover-budget foil-drain + foilCursor + both-queues readiness gate"
      contains: "traitFromWordFoil"
    - path: "contracts/modules/DegenerusGameAdvanceModule.sol"
      provides: "levelStartDay[lvl] SSTORE at the level=lvl transition; foilCursor read in the queue-drained readiness gate"
      contains: "levelStartDay"
    - path: "contracts/interfaces/IDegenerusGame.sol"
      provides: "the two new external selectors if the delegatecall encoding requires them"
  key_links:
    - from: "contracts/DegenerusGame.sol::buyFoilPack"
      to: "ContractAddresses.GAME_FOILPACK_MODULE"
      via: "delegatecall + _revertDelegate"
      pattern: "GAME_FOILPACK_MODULE.*delegatecall|delegatecall.*FOILPACK"
    - from: "contracts/modules/DegenerusGameFoilPackModule.sol::_claimFoilMatch"
      to: "foilMatchClaimed[mk]"
      via: "mark-before-payout (CEI)"
      pattern: "foilMatchClaimed\\[.*\\] = true"
    - from: "contracts/modules/DegenerusGameMintModule.sol::processTicketBatch"
      to: "foilTicketQueue[wk] via foilCursor"
      via: "leftover-budget foil-drain + both-queues readiness gate"
      pattern: "foilTicketQueue|foilCursor"
    - from: "contracts/modules/DegenerusGameMintModule.sol::_raritySymbolBatch"
      to: "traitFromWordFoil(s, multBps)"
      via: ":759 multBps==0 ? traitFromWord : traitFromWordFoil branch"
      pattern: "traitFromWordFoil"
---

<v_supersede priority="authoritative">
⚠ **SPEC §V SUPERSEDES the §U.2 claim + §E.3 HERO-FREE design below (USER sign-off 2026-06-19).** Read 445-SPEC.md §V FIRST. The module/spine deltas:
1. **BUY body `_buyFoilPack` (V.1 Option A):** key the one-per-level cap AND the `foilRecord` write on **`_activeTicketLevel()`**, not raw `level`: `uint24 lvl = _activeTicketLevel();` then `if (_foilBoughtThisLevel(buyer, lvl)) revert E();`, `foilRecord[lvl][buyer] = ...`. The price `10*priceForLevel(lvl)`, the `FOIL_SEED_TAG` seed `keccak(buyer, lvl, FOIL_SEED_TAG)`, and the step-7 foil-queue write all use this same `lvl == _activeTicketLevel()`. **DROP** writing the rawLevel stamp (storage removed it).
2. **CLAIM body — REPLACE the §U.2 4-arg + §E.3 re-derivation entirely.** New sig `claimFoilMatch(uint256 day, uint256 ticketIndex, uint8 drawKind)` (DROP `recLevel`). Body in order: (1) `require(ticketIndex<4); require(drawKind<2);` (2) `(bool drawPresent, uint32 mainSet, uint32 bonusSet, uint24 L) = _foilDrawFor(day); require(drawPresent);` (3) `(bool present, uint16 multBps, uint24 buyDay, uint32[4] memory sigs) = _foilRecordFor(player, L); require(present);` (4) `require(day > buyDay);` (D2) (5) `bytes32 mk = keccak256(abi.encode(player, uint256(L), day, uint256(drawKind), ticketIndex)); require(!foilMatchClaimed[mk]); foilMatchClaimed[mk] = true;` (CEI, level KEPT) (6) `uint32 winSet = drawKind==1 ? bonusSet : mainSet;` (7) count over 4 quadrants: `for q in 0..3 { if (uint8(sigs[q] & 0x3F) == uint8((winSet>>(8*q)) & 0x3F)) liveCount++; }` (8) tier: `liveCount==2`→2-of-4, `==3`→3-of-4, `==4`→4-of-4, else `revert E()`. **FULLY REMOVE** the HERO-FREE branch, `heroFreeCount`, the dual derivation, the `4-of-4==heroFreeCount==4` gate, the `_rollHeroSymbol`/`dailyHeroWagers[day-1]` reconstruction, and EVERY read of `rngWordByDay[day]` for **set derivation**. Payout lanes (E.5) UNCHANGED: 4-of-4 still `whalePassClaims[player] += 1` + bonus spin; FOIL_MAG/CCY lanes still read `rw = rngWordByDay[day]` ONLY for the payout magnitude/currency entropy, never for set derivation.
3. **FACADE (DegenerusGame.sol):** `claimFoilMatch(uint256 day, uint256 ticketIndex, uint8 drawKind) external` thin stub (3-arg, was 4-arg). `buyFoilPack()` stub unchanged.
4. **ADVANCE MODULE:** DROP the planned `levelStartDay[lvl]=day` SSTORE (it never lands — V.1 removed levelStartDay). No other AdvanceModule change for D1-D4.
5. **JACKPOT MODULE (NEW surface — D1 write + D4 two heroes):**
   - **D1 write:** in `_emitDailyWinningTraits` (`:1799-1816`) add `dailyFoilDraw[questDay] = _packFoilDraw(mainTraitsPacked, bonusTraitsPacked, lvl);`. **Day-1:** in the external `emitDailyWinningTraits` (`:1568-1574`) add `dailyFoilDraw[questDay] = _packFoilDraw(mainTraitsPacked, bonusTraitsPacked, 1);` after both sets compute. Exactly one write per logical day.
   - **D4 two heroes:** add `view` helper `_rollHeroSymbolExcluding(uint24 day, uint256 entropy, uint8 excludeQuadrant, uint8 excludeSymbol)` after `_rollHeroSymbol` (~:1413) that zeroes the excluded slot's weight, recomputes leader/total/leaderBonus over the remainder, single modulo pick; `total==0`→`(false,0,0)` (pure-VRF fallback). Edit ONLY the bonus leg of `_rollWinningTraitsPair` (:1791-1794) and the `isBonus==true` branch of `_rollWinningTraits` (:1764-1769): roll MAIN hero off the unsalted word (`_rollHeroSymbol(dailyIdx, randWord)`), then if `hasWinner` roll BONUS via `_rollHeroSymbolExcluding(dailyIdx, rBonus, mainQ, mainSym)` else `_rollHeroSymbol(dailyIdx, rBonus)`, apply via `_applyHeroResult(traits, rBonus, ...)`. Keep `_rollHeroSymbol`/`_applyHeroResult`/`_applyHeroOverride`/`getRandomTraits`/`packWinningTraits`/the MAIN legs/the `:239` terminal `_rollWinningTraits(_,false)` **BYTE-UNTOUCHED**. Do NOT refactor `_rollHeroSymbol`. (Pseudocode pinned in §V.4 / the synthesis `jackpot_mechanic_final`.)
6. **SCOPE GUARD:** make NO change to `foilTicketQueue`/`foilOwedPacked`/`foilCursor`/the `processTicketBatch` foil-drain/the `_raritySymbolBatch` multBps branch/the readiness gate (§U.3/§U.4). **MintModule is NOT touched by D1-D4** (its §U.4 foil-queue-drain edits are independent — they are NOT part of THIS plan's D1-D4 work; if previously planned here, they stay as the separate §U.3/§U.4 concern).
Where the task bodies below conflict with §V, §V governs.
</v_supersede>

<objective>
Author the FOIL + MATCH module body AND the additive spine wiring — the heart of the v71 diff: a NEW
`contracts/modules/DegenerusGameFoilPackModule.sol` holding `_buyFoilPack` (E.1) and `_claimFoilMatch` (E.2 +
the §U.2 eligibility window) with the LIVE-vs-HERO-FREE re-derivation (E.3) and the isolated 40/40/20 spin
payout (E.5); the two thin facade stubs on `DegenerusGame.sol` (the §U.2 4-arg `claimFoilMatch`); the
interface selectors; and the §U.3/§U.1 spine touches to `MintModule` (the `_raritySymbolBatch` `multBps`
param + the `:759` producer branch + the `processTicketBatch` leftover-budget foil-drain + `foilCursor` +
the both-queues readiness gate) and `AdvanceModule` (the `levelStartDay` SSTORE + the `foilCursor` readiness
read). Depends on Plan 01 (the `traitFromWordFoil`/`packedTraitsFoil` producers + `foilBoostBps` curve) and
Plan 02 (the storage layout, accessors, queue/cursor types, constants, and address constant).

§U DELTA (authoritative — supersedes the older §E body this plan was first built from):
- §U.1 — the RESOLUTION producer is single-trait `traitFromWordFoil(s, multBps)` at `MintModule:759` (the
  live producer is `traitFromWord`, NOT `packedTraitsFromSeed`). `packedTraitsFoil` is BUY-side only (the 4
  frozen sigs). So the foil-owed resolution wires through the SHARED `_raritySymbolBatch`, not a separate path.
- §U.2 — `claimFoilMatch` is 4-arg `(day, ticketIndex, drawKind, recLevel)` (facade + interface + module body +
  the double-claim key). The eligibility window is `[buyDay+1, levelStartDay[recLevel+1])`, using the `buyDay`
  stamped in `foilRecord` + the `levelStartDay` map. `buyFoilPack` stamps `buyDay`.
- §U.3 — the 4 foil tickets enter the regular jackpot via a SEPARATE `foilTicketQueue[wk]` + `foilOwedPacked`
  (NOT `ticketQueue`), drained inside `processTicketBatch` on LEFTOVER budget after the normal queue, with a
  `foilCursor` resume and a both-queues-drained readiness gate before any jackpot draw.

Purpose: deliver FOIL-02..05, RARE-03 (apply-at-buy), MATCH-01..09, and the SEC-03 module placement, exactly
per SPEC §E + §U. The 4-of-4 steer-proof gate (SEC-01 basis) and the ETH ≤10%-cap solvency bound (SEC-02
basis) are the load-bearing mitigations; full attestation is downstream at 448.

Output: the new module (~8–11 KB est. body); two facade stubs; interface selectors; the MintModule +
AdvanceModule additive spine wiring. No edits to the v70-frozen producers; no Degenerette `quickPlay` routing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/446-impl-batched-contract-diff-contract-commit-gate/446-CONTEXT.md
@.planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC.md
@.planning/phases/446-impl-batched-contract-diff-contract-commit-gate/446-01-SUMMARY.md
@.planning/phases/446-impl-batched-contract-diff-contract-commit-gate/446-02-SUMMARY.md

<interfaces>
<!-- All anchors verified at ffbd7796. The two prior plans provide the producers/curve (01) and storage/accessors/queue/cursor/address (02). -->

FROM Plan 01 (contracts/DegenerusTraitUtils.sol + contracts/libraries/ActivityCurveLib.sol):
  traitFromWordFoil(uint64 s, uint16 multBps) internal pure returns (uint8)        // §U.1 — THE single-trait resolution producer (wired at MintModule:759)
  packedTraitsFoil(uint256 rand, uint16 multBps) internal pure returns (uint32)    // buy-side 4-quadrant packer (the 4 frozen sigs)
  ActivityCurveLib.foilBoostBps(uint256 score) internal pure returns (uint256)     // 20000..60000

FROM Plan 02 (contracts/storage/DegenerusGameStorage.sol):
  mapping(uint24 => mapping(address => uint256)) internal foilRecord;              // packed: sigs[0-127], multBps[128-143], stamp[144-167], buyDay[168-191]
  mapping(bytes32 => bool) internal foilMatchClaimed;
  mapping(uint24 => uint24) internal levelStartDay;                                // AdvanceModule writes; claim reads for the window
  mapping(uint24 => address[]) internal foilTicketQueue;                           // SAME type/key as ticketQueue (:488), wk from _tqReadKey/_tqWriteKey
  foilOwedPacked (per-(level,player) owed counter OR folded — see Plan-02 SUMMARY)
  uint32 internal foilCursor;                                                      // SAME type as ticketCursor (:499)
  _foilRecordFor(address player, uint256 lvl) internal view returns (bool present, uint16 multBps, uint24 buyDay, uint32[4] sigs)
  _foilBoughtThisLevel(address player, uint256 lvl) internal view returns (bool)
  FOIL_TO_FUTURE_BPS = 2500 ; FOIL_SEED_TAG ; FOIL_MAG_TAG ; FOIL_CCY_TAG ; _FOIL_BUYDAY_SHIFT = 168
  ContractAddresses.GAME_FOILPACK_MODULE   // from contracts/ContractAddresses.sol

SPINE-TOUCH anchors — contracts/modules/DegenerusGameMintModule.sol (§U.1/§U.3 — EDIT these):
  :720  function _raritySymbolBatch(... uint256 baseKey, ... uint32 take, ...)  // ADD a `uint16 multBps` param
  :745-747  s = keccak256(abi.encode(baseKey, entropyWord, groupIdx))           // the LCG seed; FOIL_SEED_TAG domain-separates the foil seed
  :759  uint8 traitId = DegenerusTraitUtils.traitFromWord(s) + ((i & 3) << 6);  // BRANCH → multBps==0 ? traitFromWord(s) : traitFromWordFoil(s, multBps); retain ((i&3)<<6)
  :846  function processTicketBatch(uint24 lvl) external returns (bool finished) // ADD the leftover-budget foil-drain after the normal ticketQueue
  :849-915  the normal-queue drain (ticketQueue[rk], ticketCursor, _processOneTicketEntry budget)  // the foil-drain continues HERE on leftover budget
  :592-595 / :962  baseKey = (lvl<<224)|(idx<<192)|(player<<32)|owed            // the foil baseKey mirrors this shape (with FOIL_SEED_TAG separation)
  :777 (Storage) _tqReadKey(lvl) → the wk key foilTicketQueue[wk] uses

SPINE-TOUCH anchors — contracts/modules/DegenerusGameAdvanceModule.sol (§U.2/§U.3 — EDIT these):
  :1732  level = lvl;                                                            // ADD `levelStartDay[lvl] = day;` ADJACENT (the additive spine touch)
  :233 / :276 / :689 / :1135  the `ticketQueue[..].length`/`ticketsFullyProcessed` queue-drained checks  // ADD the `foilCursor == foilTicketQueue.length` (both-queues) condition

REUSED in-repo rails (verified — clone shape / call, do NOT edit unless listed as a spine touch):
  contracts/DegenerusGame.sol :614-629  buyPresaleBox(...) — the thin-facade template (resolve player → MODULE.delegatecall(abi.encodeWithSelector) → _revertDelegate)
  contracts/DegenerusGame.sol :944      _revertDelegate(bytes) ; :505 _resolvePlayer(...)
  contracts/storage/DegenerusGameStorage.sol :617/:649 _livenessTriggered() guard ; :641 _queueTicketsScaled(buyer, level, quantityScaled, bool) — SHAPE reference; foil queues to foilTicketQueue instead ; :663-664 TICKET_SCALE math
  contracts/storage/DegenerusGameStorage.sol :738 _setPrizePools(uint128,uint128) ; :752 _setPendingPools(...) ; :922 _claimableOf(address) ; :933 _creditClaimable(addr,wei) ; :941 _debitClaimable(addr,wei) ; :956 _debitAfking(addr,wei) — NEVER call for foil ; prizePoolFrozen flag
  contracts/modules/DegenerusGameMintModule.sol :117 PURCHASE_TO_FUTURE_BPS=1000 (the bps to FORK, not reuse) ; :189-217 _recordMintPayment frozen/unfrozen branch (REUSE verbatim, only the bps differs) ; :236-299 _processMintPayment accounting SHAPE (afking leg at the silent-tap branch — REJECT for foil) ; :1709 cachedScore = _playerActivityScore(buyer, questStreak) — the SAME score source to freeze
  contracts/modules/DegenerusGameMintStreakUtils.sol :139 _activeTicketLevel() ; :267 _playerActivityScore(...)
  contracts/libraries/PriceLookupLib.sol :21 priceForLevel(uint24) internal pure returns (uint256)
  contracts/libraries/JackpotBucketLib.sol :267 packWinningTraits(uint8[4]) ; :272 unpackWinningTraits(uint32) ; :281 getRandomTraits(uint256 rw)
  contracts/libraries/EntropyLib.sol :23 hash2(uint256,uint256)
  contracts/modules/DegenerusGameJackpotModule.sol :172 BONUS_TRAITS_TAG=keccak256("BONUS_TRAITS") ; :1316-1341 _applyHeroResult(w, randomWord, hasHero, hQ, hSym) ; :1352 _rollHeroSymbol(uint24 day, uint256 entropy) → (hasWinner, winQuadrant, winSymbol) ; :1760 _rollWinningTraits(randWord, isBonus) ; :1778 _rollWinningTraitsPair(randWord) — the E.3 mirror
  contracts/modules/DegenerusGameDegeneretteModule.sol :221 ETH_WIN_CAP_BPS=1_000 ; :877-915 the ETH cap-spin clone source (maxEth :889 = pool*ETH_WIN_CAP_BPS/10_000 ; lootbox-resolve :915)
  IDegenerusCoin.sol :20 mintForGame(address,uint256) ; WrappedWrappedXRP.sol :229 mintPrize(address,uint256)
  contracts/storage/DegenerusGameStorage.sol :1122 whalePassClaims (EXISTING — 4-of-4 does += 1, do NOT re-declare)
</interfaces>

<carry_verbatim_corrected_anchors>
SPEC §1 — a stale value steers 446 to wrong code. Carry EXACTLY:
- Foil-ticket queue scale = `400` (= 4 × TICKET_SCALE=100), NOT `4`. §U.3: the foil tickets queue into
  `foilTicketQueue[wk]` + record `400`-worth of owed in `foilOwedPacked` (NOT `ticketQueue` via `_queueTicketsScaled`).
- `whalePassClaims` ALREADY exists at :1122 — 4-of-4 tier does `whalePassClaims[player] += 1`; do NOT re-declare.
- ETH cap-spin clone source = `DegenerusGameDegeneretteModule.sol:877-915` (maxEth :889, lootbox-resolve :915) — NOT :402-446.
- Double-claim marker name = `foilMatchClaimed` (unified) — NOT `foilClaimed`.
- `claimFoilMatch(D)` reads the hero pool via `_rollHeroSymbol(dailyIdxFor(day), rw)` where `dailyIdxFor(day) == day - 1` (the prior-day slot; verified at JackpotModule :1290-1291).
</carry_verbatim_corrected_anchors>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author the new module + _buyFoilPack body (E.1 + §U.2 buyDay stamp + §U.3 foil-queue write)</name>
  <files>contracts/modules/DegenerusGameFoilPackModule.sol</files>
  <read_first>
    - 445-SPEC.md §E.1 (the `_buyFoilPack(address buyer, uint256 ethSent)` body, in order), §U.2 (the `buyDay` stamp at `[168-191]`), §U.3 (the foil-queue write — `foilTicketQueue` + `foilOwedPacked`, NOT `ticketQueue`), §A.2.4 (RARE-03 freeze-at-buy), §D.2 (the cap)
    - contracts/modules/DegenerusGameMintModule.sol :117, :189-217, :236-299, :1709 (the payment/routing/cachedScore shapes to fork; the afking silent-tap branch to REJECT)
    - contracts/storage/DegenerusGameStorage.sol :617/:649 (`_livenessTriggered`), :641 (`_queueTicketsScaled` SHAPE — foil queues to `foilTicketQueue` instead), :772/:777 (`_tqWriteKey`/`_tqReadKey` — the wk key), :738/:752/:922/:933/:941 (pool + claimable helpers)
    - contracts/libraries/PriceLookupLib.sol :21 ; the Plan-01/Plan-02 SUMMARYs for the producer + accessor + constant + queue/cursor signatures
    - an existing module header (e.g. contracts/modules/DegenerusGameDegeneretteModule.sol :1-30) for the module file/license/pragma/import skeleton convention
  </read_first>
  <action>
    Create `contracts/modules/DegenerusGameFoilPackModule.sol` (contract name per D-446-02, e.g.
    `DegenerusGameFoilPackModule`), inheriting the shared storage base like the other modules, with the house
    file/license/pragma/import skeleton. Author `_buyFoilPack(address buyer, uint256 ethSent)` IN ORDER per §E.1:
    1. Liveness gate: `if (_livenessTriggered()) revert E();`
    2. One-per-RAW-level cap: `uint24 lvl = level;` then revert when `_foilBoughtThisLevel(buyer, lvl)` (the
       record-presence read from Plan 02). The stamp write is in step 6 (after price settles) so a reverting buy
       leaves no flag. Keyed on raw `level`, NEVER `_activeTicketLevel()`.
    3. Price + payment, AFKING LEG REJECTED (FOIL-02/FOIL-03): `uint256 cost = 10 * PriceLookupLib.priceForLevel(lvl);`
       `uint256 ethUsed = ethSent < cost ? ethSent : cost;` (overpay ignored); `uint256 remaining = cost - ethUsed;`
       if `remaining != 0`: `uint256 claimable = _claimableOf(buyer); uint256 avail = claimable > 1 ? claimable - 1 : 0;`
       `if (remaining > avail) revert E();` — THIS revert IS the afking-rejection guard (the normal mint silently
       taps afking principal; the foil path must NOT). Then `_debitClaimable(buyer, remaining);` and a separate
       `claimablePool -= uint128(remaining);`. NEVER call `_debitAfking` (:956).
    4. Pool split 75/25 (FOIL-04): `uint256 futureShare = (cost * FOIL_TO_FUTURE_BPS) / 10_000;`
       `uint256 nextShare = cost - futureShare;` (FOIL_TO_FUTURE_BPS=2500, NOT PURCHASE_TO_FUTURE_BPS). Write
       through the `_recordMintPayment` frozen/unfrozen branch VERBATIM: `prizePoolFrozen ⇒ _setPendingPools(...)`
       else `_setPrizePools(...)`. Only the bps constant is forked; the routing branch is reused unchanged.
    5. Boost freeze (RARE-03): `score = _playerActivityScore(buyer, …)` — the SAME source the mint path freezes as
       cachedScore (MintModule :1709). `uint16 multBps = uint16(ActivityCurveLib.foilBoostBps(score));` (20000..60000).
    6. Roll 4 sigs + write the record (MATCH-01, §U.2): `uint256 seed = uint256(keccak256(abi.encode(buyer, lvl, FOIL_SEED_TAG)));`
       (deterministic, frozen — NO live RNG at buy). For `i in 0..3`:
       `uint32 sig_i = DegenerusTraitUtils.packedTraitsFoil(uint256(keccak256(abi.encode(seed, i))), multBps);`.
       Pack `sig0..3` `[0-127]`, `multBps` `[128-143]`, stamp `lvl` `[144-167]`, **`buyDay = uint24(day)` at
       `[168-191]` via `_FOIL_BUYDAY_SHIFT` (§U.2)** — `day` = the raw current day. Write
       `foilRecord[lvl][buyer] = packed;` — a SINGLE SSTORE.
    7. Queue 4 foil tickets into the SEPARATE foil queue (FOIL-05, §U.3): append `buyer` to
       `foilTicketQueue[_tqWriteKey(_activeTicketLevel())]` (the SAME wk key `ticketQueue` uses — `_tqWriteKey`,
       :772) and record `400`-worth of owed in `foilOwedPacked[lvl][buyer]` (or the folded form per the Plan-02
       SUMMARY). `400 = 4 × TICKET_SCALE=100` governs the foil owed-count (§1 #1). Do NOT call `_queueTicketsScaled`
       (that writes the normal `ticketQueue`); use a foil-aware writer of the same shape.
    8. No FLIP/WWXRP/whale-pass mint at buy — pure cost-in.
    Mirror the module-class conventions (events/errors/imports) of the neighboring modules.
  </action>
  <verify>
    <automated>grep -q "_buyFoilPack" contracts/modules/DegenerusGameFoilPackModule.sol && grep -q "FOIL_TO_FUTURE_BPS" contracts/modules/DegenerusGameFoilPackModule.sol && grep -q "foilTicketQueue\[" contracts/modules/DegenerusGameFoilPackModule.sol && grep -q "foilRecord\[" contracts/modules/DegenerusGameFoilPackModule.sol && grep -q "_FOIL_BUYDAY_SHIFT" contracts/modules/DegenerusGameFoilPackModule.sol && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Module file exists; `_buyFoilPack(address buyer, uint256 ethSent)` authored in the §E.1 step order.
    - `cost = 10 * PriceLookupLib.priceForLevel(lvl)` (FOIL-02); the `remaining > avail` `revert E()` afking-rejection guard present (FOIL-03); `_debitAfking` is NEVER called (`grep -c _debitAfking` == 0).
    - `FOIL_TO_FUTURE_BPS` used for the split (FOIL-04); routes through the `_setPendingPools`/`_setPrizePools` branch.
    - `multBps = uint16(ActivityCurveLib.foilBoostBps(score))` frozen from the cachedScore source (RARE-03); written into `foilRecord[lvl][buyer]` with one SSTORE, with `buyDay` stamped at `_FOIL_BUYDAY_SHIFT` (§U.2; MATCH-01/MATCH-02).
    - Foil tickets queue into `foilTicketQueue[...]` + `foilOwedPacked` (NOT `ticketQueue`); `_queueTicketsScaled` is NOT called from the foil module (`grep -c _queueTicketsScaled` in the module == 0); the `400` owed-count governs (§1 corrected anchor #1).
    - The 4 sigs roll via `packedTraitsFoil(…, multBps)` (the Plan-01 buy-side packer); no live RNG at buy.
  </acceptance_criteria>
  <done>The module + the full 8-step buy body land (with the §U.2 buyDay stamp + the §U.3 foil-queue write); FOIL-02/03/04/05 + RARE-03 + MATCH-01/02 satisfied at the buy side.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author _claimFoilMatch (4-arg, §U.2 window) + the LIVE/HERO-FREE re-derivation + the isolated 40/40/20 payout (E.2/E.3/E.5)</name>
  <files>contracts/modules/DegenerusGameFoilPackModule.sol</files>
  <read_first>
    - 445-SPEC.md §E.2 (the ordered `_claimFoilMatch` body), §U.2 (the 4-arg signature + the `[buyDay+1, levelStartDay[recLevel+1])` window), §E.3 (the crux — LIVE vs HERO-FREE re-derivation + the steer-proof tier gate), §E.5 (the isolated tier→faces schedule, the disjoint entropy lanes, the 40/40/20 split + the ETH cap-spin), §D.3 (the `foilMatchClaimed` keccak key + CEI mark-before-payout)
    - contracts/modules/DegenerusGameJackpotModule.sol :172 (BONUS_TRAITS_TAG), :1316-1341 (`_applyHeroResult`), :1352-… (`_rollHeroSymbol` — note `dailyIdx == day-1`), :1760-1795 (`_rollWinningTraits`/`_rollWinningTraitsPair` — the exact re-derivation mirror)
    - contracts/libraries/JackpotBucketLib.sol :267/:272/:281 (`packWinningTraits`/`unpackWinningTraits`/`getRandomTraits`)
    - contracts/modules/DegenerusGameDegeneretteModule.sol :877-915 (the ETH cap-spin clone source — maxEth :889, lootbox-resolve :915) ; :221 (ETH_WIN_CAP_BPS=1000)
    - contracts/storage/DegenerusGameStorage.sol :462 (rngWordByDay), :933 (`_creditClaimable`), :1122 (`whalePassClaims`) ; the Plan-02 `_foilRecordFor` (now returns `buyDay`) + `levelStartDay` ; IDegenerusCoin.sol :20 (mintForGame), WrappedWrappedXRP.sol :229 (mintPrize)
  </read_first>
  <action>
    Author `_claimFoilMatch(address player, uint256 day, uint256 ticketIndex, uint8 drawKind, uint256 recLevel)`
    (§U.2 — 4 user-facing args + the resolved `player`) IN ORDER per §E.2, with E.3 and E.5 inlined or in private
    helpers:
    1. Bounds + record load: `require(ticketIndex < 4); require(drawKind < 2);` load
       `(bool present, uint16 multBps, uint24 buyDay, uint32[4] sigs) = _foilRecordFor(player, recLevel);` then
       `require(present);`. `recLevel` is the EXPLICIT param (§U.2) — the outer `foilRecord`/marker key; never a
       live `level` compare.
    2. Eligibility window (MATCH-02, §U.2): `require(day >= uint256(buyDay) + 1);` AND
       `(levelStartDay[uint24(recLevel) + 1] == 0 || day < levelStartDay[uint24(recLevel) + 1]);` — eligible across
       `[buyDay+1, levelStartDay[recLevel+1])` (no look-back to pre-buy draws; open while the level is live).
    3. RNG availability: `uint256 rw = rngWordByDay[uint24(day)]; require(rw != 0);` (:462, the retained daily VRF).
    4. Double-claim guard (CEI, MATCH-05): `bytes32 mk = keccak256(abi.encode(player, recLevel, day, drawKind, ticketIndex));`
       `require(!foilMatchClaimed[mk]); foilMatchClaimed[mk] = true;` — UNIFIED name `foilMatchClaimed`, set BEFORE
       any payout (CEI). The key carries `recLevel` (§U.2).
    5. Re-derive the day's winning sets (E.3): `r = (drawKind == 1) ? EntropyLib.hash2(rw, uint256(BONUS_TRAITS_TAG)) : rw;`
       HERO-FREE pure-VRF: `uint8[4] heroFreeW = JackpotBucketLib.getRandomTraits(r); uint32 heroFreeSet = packWinningTraits(heroFreeW);`
       LIVE: `(bool hasHero, uint8 hQ, uint8 hSym) = _rollHeroSymbol(dailyIdxFor(day), rw);` (hero entropy = the
       unsalted day word `rw`; `dailyIdxFor(day) == day - 1` — the prior-day slot, §1 corrected anchor #5);
       copy `uint8[4] liveW = heroFreeW;` then `_applyHeroResult(liveW, r, hasHero, hQ, hSym);` (rewrites ONLY
       `liveW[hQ]`); `uint32 liveSet = packWinningTraits(liveW);`.
    6. Count exact positional matches (MATCH-03): per quadrant `q`, match iff `foilQuad_q == winQuad_q` as the FULL
       6-bit `[CCC][SSS]` (color AND symbol; both carry `[QQ]`). Color-only or wrong-quadrant does NOT count.
       Compute `liveCount` (vs LIVE) and `heroFreeCount` (vs HERO-FREE) over the chosen ticket's 4 quadrant sigs.
    7. Tier gate (MATCH-09 — steer-proof): 2-of-4 = `liveCount == 2`; 3-of-4 = `liveCount == 3`;
       4-of-4 = `heroFreeCount == 4` ONLY (never `liveCount`); else `tier == 0 ⇒ revert E();`.
    8. Pay the tier via the ISOLATED schedule (E.5; MATCH-04 — MUST NOT route through Degenerette quickPlay):
       1 face = 1000 FLIP = `priceForLevel(recLevel)` ETH. Tier faces: 2-of-4 → 5; 3-of-4 → 65; 4-of-4 → bonus
       spin (~1000 faces) PLUS `whalePassClaims[player] += 1;` (:1122, EXISTING slot — pool-neutral, no ETH leaves
       for the pass). Disjoint entropy lanes (MATCH-08):
       `magnitudeLane = uint256(keccak256(abi.encode(rw, day, drawKind, ticketIndex, FOIL_MAG_TAG)));`
       `currencyLane = uint256(keccak256(abi.encode(rw, day, drawKind, ticketIndex, FOIL_CCY_TAG)));`. Currency split
       40/40/20 (MATCH-06): `uint256 c = currencyLane % 100;` `c < 40` FLIP `coin.mintForGame(player, faces*1000e18)`;
       `c < 80` ETH = the EXISTING capped-spin clone (clamp to `ETH_WIN_CAP_BPS=1000` = 10% futurePrizePool, over-cap
       spills to lootbox, credit via `_creditClaimable`, decrement runningFuture/pendingFuture exactly as :884-903 —
       CLONE `DegenerusGameDegeneretteModule.sol:877-915`); else WWXRP `wwxrp.mintPrize(player, faces*1000e18)`. ALL
       payout effects come AFTER `foilMatchClaimed[mk] = true` (CEI).
  </action>
  <verify>
    <automated>grep -q "_claimFoilMatch" contracts/modules/DegenerusGameFoilPackModule.sol && grep -q "foilMatchClaimed\[" contracts/modules/DegenerusGameFoilPackModule.sol && grep -q "heroFreeCount" contracts/modules/DegenerusGameFoilPackModule.sol && grep -q "levelStartDay\[" contracts/modules/DegenerusGameFoilPackModule.sol && grep -q "ETH_WIN_CAP_BPS" contracts/modules/DegenerusGameFoilPackModule.sol && grep -q "whalePassClaims\[" contracts/modules/DegenerusGameFoilPackModule.sol && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `_claimFoilMatch(address player, uint256 day, uint256 ticketIndex, uint8 drawKind, uint256 recLevel)` authored (§U.2 4-arg + player) in the §E.2 step order; `mk = keccak256(abi.encode(player, recLevel, day, drawKind, ticketIndex))`; `foilMatchClaimed[mk] = true` written BEFORE any mint/transfer/credit (CEI; grep-verify the marker-set line precedes the payout lane).
    - Eligibility window enforced (§U.2): `day >= buyDay + 1` AND (`levelStartDay[recLevel+1] == 0` OR `day < levelStartDay[recLevel+1]`) — `levelStartDay[` is read in the claim.
    - Both winning sets derived: `getRandomTraits(r)` for HERO-FREE; `_applyHeroResult` on a copy for LIVE; `r = hash2(rw, BONUS_TRAITS_TAG)` for `drawKind==1`.
    - Hero pool read uses `dailyIdxFor(day) == day - 1` (§1 corrected anchor #5).
    - Tier gate: 4-of-4 gated on `heroFreeCount == 4` ONLY (never `liveCount`); 2/3-of-4 off `liveCount` (MATCH-09).
    - Match count is the FULL 6-bit positional compare (color AND symbol; color-only excluded) — MATCH-03.
    - Isolated faces schedule (5 / 65 / bonus) — NO call into Degenerette `quickPlay` (`grep -c quickPlay` == 0) — MATCH-04.
    - 40/40/20 split via `currencyLane % 100`; FLIP `mintForGame`, WWXRP `mintPrize`, ETH clamped to `ETH_WIN_CAP_BPS=1000` cloned from `:877-915` (MATCH-06); disjoint `FOIL_MAG_TAG`/`FOIL_CCY_TAG` lanes (MATCH-08).
    - 4-of-4 grants `whalePassClaims[player] += 1` against the EXISTING :1122 slot (MATCH-07), pool-neutral.
  </acceptance_criteria>
  <done>The full 4-arg claim body + the §U.2 window + LIVE/HERO-FREE re-derivation + isolated 40/40/20 payout land; MATCH-02..09 satisfied at the claim side.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Facade stubs (4-arg claimFoilMatch) + interface selectors</name>
  <files>contracts/DegenerusGame.sol, contracts/interfaces/IDegenerusGame.sol</files>
  <read_first>
    - contracts/DegenerusGame.sol :614-629 (`buyPresaleBox` — the thin-facade template to copy verbatim), :505 (`_resolvePlayer`), :944 (`_revertDelegate`)
    - 445-SPEC.md §E.1 (the `buyFoilPack()` facade signature — no params, payable), §U.2 (the 4-arg `claimFoilMatch(uint256 day, uint256 ticketIndex, uint8 drawKind, uint256 recLevel)` signature — supersedes the §E.2 3-arg pin), §F.2 (the constant + facade)
    - contracts/interfaces/IDegenerusGame.sol (the existing external-selector declaration shape, if the delegatecall encoding requires the two new selectors)
  </read_first>
  <action>
    (a) Add two thin facade stubs to `DegenerusGame.sol`, copying the `buyPresaleBox` :614-629 template verbatim
    (resolve player → `ContractAddresses.GAME_FOILPACK_MODULE.delegatecall(abi.encodeWithSelector(...))` →
    `_revertDelegate(data)` on failure):
    `function buyFoilPack() external payable` — NO parameters (the pack is fully determined by
    `(msg.sender, level, msg.value)`); encodes `_buyFoilPack(buyer, msg.value)`.
    `function claimFoilMatch(uint256 day, uint256 ticketIndex, uint8 drawKind, uint256 recLevel) external` — the
    §U.2 4-arg signature; encodes `_claimFoilMatch(player, day, ticketIndex, drawKind, recLevel)`. The facade
    carries no state, no branching.
    (b) If the delegatecall encoding / facade signature requires it, add the two external selectors to
    `IDegenerusGame.sol` (the `claimFoilMatch` selector MUST be the 4-arg form) matching the house
    selector-declaration shape (D-446-02). If the existing encoding pattern (e.g. a module-specific interface)
    is used instead, follow that pattern — do NOT add unused selectors.
  </action>
  <verify>
    <automated>grep -q "function buyFoilPack() external payable" contracts/DegenerusGame.sol && grep -Eq "function claimFoilMatch\(uint256 day, uint256 ticketIndex, uint8 drawKind, uint256 recLevel\)" contracts/DegenerusGame.sol && grep -Eq "GAME_FOILPACK_MODULE" contracts/DegenerusGame.sol && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `function buyFoilPack() external payable` (no params) + `function claimFoilMatch(uint256 day, uint256 ticketIndex, uint8 drawKind, uint256 recLevel) external` (the §U.2 4-arg form) exist on `DegenerusGame`.
    - Both stubs delegatecall `ContractAddresses.GAME_FOILPACK_MODULE` + `_revertDelegate` on failure, matching the `buyPresaleBox` template; no business logic in the facade.
    - Any added interface selectors match the house declaration shape and use the 4-arg `claimFoilMatch`; no unused selectors added.
  </acceptance_criteria>
  <done>The facade (with the §U.2 4-arg claimFoilMatch) + interface selectors land; the external surface is reachable.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Spine wiring — MintModule foil-resolution drain (§U.1/§U.3) + AdvanceModule levelStartDay + readiness gate (§U.2/§U.3)</name>
  <files>contracts/modules/DegenerusGameMintModule.sol, contracts/modules/DegenerusGameAdvanceModule.sol</files>
  <read_first>
    - 445-SPEC.md §U.1 (the `:759` producer branch — single-trait `traitFromWordFoil`), §U.3 (the leftover-budget foil-drain + `foilCursor` + the both-queues readiness gate + the `processTicketBatch`-only routing), §U.4 (the three additive spine touches, behaviorally normal-preserving)
    - contracts/modules/DegenerusGameMintModule.sol :720-770 (`_raritySymbolBatch` — ADD the `uint16 multBps` param; :745-747 the seed; :759 the producer line to branch), :846-915 (`processTicketBatch` — the normal `ticketQueue` drain over `ticketCursor` + `_processOneTicketEntry` budget `:959-981` — the foil-drain continues HERE on leftover budget), :592-595/:962 (the `baseKey` shape to mirror for the foil baseKey, with `FOIL_SEED_TAG`)
    - contracts/modules/DegenerusGameAdvanceModule.sol :1729-1735 (the `level = lvl;` transition at :1732 — ADD `levelStartDay[lvl] = day;` adjacent), :233/:276/:689/:1135 (the `ticketQueue[..].length`/`ticketsFullyProcessed` queue-drained checks — ADD the `foilCursor == foilTicketQueue.length` condition so the jackpot cannot draw until BOTH queues are drained)
    - contracts/storage/DegenerusGameStorage.sol :488 (`ticketQueue`), :499 (`ticketCursor`), :772/:777 (`_tqWriteKey`/`_tqReadKey`) — the foil queue/cursor mirror; the Plan-02 `foilTicketQueue`/`foilCursor`/`foilOwedPacked` signatures
  </read_first>
  <action>
    Three SURGICAL, ADDITIVE, behaviorally-normal-preserving spine touches (§U.4). The existing normal-ticket
    path must be byte-behaviorally identical for `multBps == 0`.
    (A) MintModule `_raritySymbolBatch` (:720) — ADD a trailing `uint16 multBps` parameter; at the producer line
    `:759`, BRANCH `multBps == 0 ? DegenerusTraitUtils.traitFromWord(s) : DegenerusTraitUtils.traitFromWordFoil(s, multBps)`
    and RETAIN the `+ ((i & 3) << 6)` quadrant offset. Every EXISTING caller of `_raritySymbolBatch` passes
    `multBps = 0` (the normal path is unchanged). §U.1: the resolution producer is single-trait `traitFromWordFoil`.
    (B) MintModule `processTicketBatch` (:846) — AFTER the normal `ticketQueue[rk]` drain is exhausted
    (`ticketCursor == queue.length` / the existing finish branch) AND write-budget remains (the `take`/`maxT`/`baseOv`
    accounting in `_processOneTicketEntry` :959-981), CONTINUE the SAME loop into `foilTicketQueue[wk]`
    (`wk = _tqReadKey(lvl)`), resolving each foil entry via the shared `_raritySymbolBatch` with the FROZEN `multBps`
    read from `foilRecord` (RARE-03) and a `FOIL_SEED_TAG`-separated foil baseKey/seed (mirror the `:592-595` baseKey
    shape with the foil domain tag so a player holding both normal + foil entries at one level cannot collide).
    Track resume with `foilCursor` (paralleling `ticketCursor`). ONE shared budget envelope ⇒ the combined advance
    stays gas-bounded (the v62 gas-brick lesson). This is the CURRENT drain (`processTicketBatch`) ONLY — NOT
    `processFutureTicketBatch` (foil entries are current-level; confirm against the `false` routing arg of the buy
    step-7 pin). `foilOwedPacked` supplies the per-(level,player) owed count consumed here.
    (C) AdvanceModule — (c1) at the `level = lvl;` transition (:1732), ADD `levelStartDay[lvl] = day;` adjacent
    (§U.2; `day` = the day the level began) — written ONCE per level, append-only, inert to existing logic.
    (c2) In the jackpot/advance queue-drained readiness gate (the `ticketQueue[..].length`/`ticketsFullyProcessed`
    checks at :233/:276/:689/:1135), ADD the `foilCursor == foilTicketQueue.length` (both-queues-drained) condition
    so a level's jackpot CANNOT draw until BOTH the normal AND the foil queue are drained (§U.3 — else foil's
    boosted `traitBurnTicket` entries silently under-resolve, a non-reverting FOIL-05 violation). Foil is
    best-effort on budget but GATED before the draw.
  </action>
  <verify>
    <automated>grep -q "traitFromWordFoil" contracts/modules/DegenerusGameMintModule.sol && grep -q "foilTicketQueue" contracts/modules/DegenerusGameMintModule.sol && grep -q "foilCursor" contracts/modules/DegenerusGameMintModule.sol && grep -q "FOIL_SEED_TAG" contracts/modules/DegenerusGameMintModule.sol && grep -q "levelStartDay\[" contracts/modules/DegenerusGameAdvanceModule.sol && grep -q "foilCursor" contracts/modules/DegenerusGameAdvanceModule.sol && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `_raritySymbolBatch` gains a `uint16 multBps` param; `:759` branches `multBps == 0 ? traitFromWord(s) : traitFromWordFoil(s, multBps)` retaining `((i & 3) << 6)` (§U.1); every existing caller passes `multBps = 0` (normal path behaviorally unchanged).
    - `processTicketBatch` drains `foilTicketQueue[wk]` on LEFTOVER budget after the normal queue, via the shared `_raritySymbolBatch` with the frozen `multBps` from `foilRecord` and a `FOIL_SEED_TAG`-separated foil seed; `foilCursor` makes it resumable; the foil-drain is in `processTicketBatch` only (NOT `processFutureTicketBatch`).
    - AdvanceModule writes `levelStartDay[lvl] = day` at the `level = lvl` transition (:1732), append-only.
    - The queue-drained readiness gate now requires BOTH `ticketCursor`-finish AND `foilCursor == foilTicketQueue.length` before a jackpot draws (`grep` shows `foilCursor` read in the AdvanceModule gate).
    - `git diff` shows the v70-frozen producers (`weightedColorBucket`/`traitFromWord`/`packedTraitsFromSeed`) are byte-untouched (RARE-01); the existing normal-ticket drain behaviour is preserved for `multBps == 0`.
  </acceptance_criteria>
  <done>The three §U.4 spine touches land (MintModule resolution branch + foil-drain, AdvanceModule levelStartDay + both-queues readiness gate); FOIL-05 boosted traits resolve to real gold; the draw is gated until both queues drain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TB-1 client → buyFoilPack | fresh ETH / claimable half / the per-level cap (the payable buy entrypoint) |
| TB-2 top-wagerer → the LIVE hero symbol | one quadrant's symbol of the LIVE winning set (the dailyHeroWagers board → _applyHeroResult) |
| TB-3 claimant → the ETH prize pool | up to 10% of futurePrizePool per ETH spin (the capped ETH lane) |
| TB-4 advance/drain → foilTicketQueue + foilCursor + levelStartDay | the shared-budget foil-drain + the readiness gate + the window bound (the spine touches) |
| TB-5 claimant → foilMatchClaimed | the per-tuple double-claim marker (CEI mark-before-payout) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-445-E1 | Elevation of Privilege | 4-of-4 whale-pass moonshot via hero steering | mitigate | 4-of-4 gated ONLY on `heroFreeCount == 4` (pure VRF); a steered hero shifts at most ONE quadrant's symbol on LIVE → reaches at most 3-of-4, NEVER heroFreeCount==4 (SEC-01 basis, §E.3); attested at 448 |
| T-445-E2 | Denial of Service / solvency | ETH lane drains futurePrizePool | mitigate | `ETH_WIN_CAP_BPS = 1000` (10%) clamp + lootbox spill, cloned from DegeneretteModule:877-915; FLIP/WWXRP are MINTS (no pool draw); whale pass pool-neutral (SEC-02 basis, §E.5 C); attested at 448 |
| T-445-E3 | Tampering | afking principal spent on a foil buy | mitigate | the foil payment path REJECTS the afking leg (`remaining > avail ⇒ revert E()`); only fresh-ETH/claimable accepted; `_debitAfking` never called (FOIL-03, §E.1 step 3) |
| T-445-E4 | Information disclosure / grind | magnitude/currency steered via the match lane | mitigate | disjoint keccak domains (`FOIL_MAG_TAG`/`FOIL_CCY_TAG` ≠ the match lane ≠ BONUS_TRAITS_TAG); derived from retained `rw` at claim, never live-read (MATCH-08, §E.5 B) |
| T-445-D2 | Repudiation | double-claim of the same (player, recLevel, day, drawKind, ticketIndex) | mitigate | sparse `foilMatchClaimed[mk]` set BEFORE payout (CEI); five distinct positional `abi.encode` fields (incl `recLevel`, §U.2) → collision-free (§D.3) |
| T-446-03-DRAIN | Denial of Service (gas) | the foil-drain desyncs/inflates the advance gas envelope (a brick) | mitigate | ONE SHARED write-budget envelope across both queues (foil runs ONLY on leftover budget after the normal drain); `foilCursor` makes a partial foil drain resumable across txs (the v62 gas-brick lesson); a dedicated brick test is a 447 deliverable |
| T-446-03-GATE | Tampering | the jackpot draws before the foil queue is drained (foil entries silently under-resolve) | mitigate | the readiness gate requires BOTH `ticketCursor`-finish AND `foilCursor == foilTicketQueue.length`; the draw cannot fire on a partially-drained foil queue (§U.3); attested at 447/448 |
| T-446-03-SPINE | Tampering | the `levelStartDay`/`:759`-branch spine edits alter existing normal behaviour | mitigate | additive-only: `_raritySymbolBatch` `multBps == 0` is the byte-behavior-identical normal path; `levelStartDay[lvl] = day` is a write-once append inert to existing reads; frozen producers untouched; re-attested at 448 RNG-freeze + layout golden |
| T-445-SC | Tampering (supply chain) | npm/pip/cargo install | accept | NO new package installs — module reuses in-repo audited rails (the facade template, the capped-spin path, `whalePassClaims`, `mintForGame`/`mintPrize`, the `_raritySymbolBatch` drain); §S T-445-SC |
</threat_model>

<verification>
- `grep` confirms the module body, the 4-arg facade stubs, the §U.2 window read (`levelStartDay[`), the CEI marker-before-payout ordering, the steer-proof gate, the isolated faces schedule (no `quickPlay`), the 40/40/20 split, and the §U.1/§U.3 spine wiring (`traitFromWordFoil` branch, `foilTicketQueue`/`foilCursor` drain, `levelStartDay` SSTORE, both-queues gate).
- `git diff` confirms the v70-frozen producers are byte-untouched (RARE-01) and the normal-ticket drain is preserved for `multBps == 0`.
- `forge build` clean + EIP-170 re-measure (the module + the facade + the touched MintModule/Game/AdvanceModule all fit 24,576 B; no duplicated batch body) is asserted in Plan 04 (the empirical pre-gate).
</verification>

<success_criteria>
- FOIL-02/03/04: 10× price, afking-rejection guard, 75/25 routing.
- FOIL-05: foil tickets enter the regular jackpot via the separate foil queue; boosted traits resolve via the shared `_raritySymbolBatch` `traitFromWordFoil` branch (real gold); the both-queues gate prevents under-resolution.
- RARE-03: boost frozen at buy, applied at resolve, never live-read.
- MATCH-01/02: 4 sigs + multBps + buyDay frozen per (player, level); the `[buyDay+1, levelStartDay[recLevel+1])` whole-level window enforced.
- MATCH-03..09: exact positional count, LIVE/HERO-FREE split, steer-proof 4-of-4 gate, isolated 40/40/20 payout, disjoint lanes, half-whale-pass grant, persist-per-level.
- SEC-03: the module + the additive spine touches fit EIP-170 (re-measured at Plan 04); storage in the base only.
</success_criteria>

<output>
Create `.planning/phases/446-impl-batched-contract-diff-contract-commit-gate/446-03-SUMMARY.md` when done —
NAME the D-446-02 module name + the chosen `foilOwedPacked` form. Do NOT commit `contracts/*.sol` — the single
consolidated contract commit happens ONCE in Plan 04 (the sole approval gate). Applying/editing `.sol` is fine;
committing it is not.
</output>
