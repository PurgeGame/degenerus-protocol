---
phase: 322-impl-the-one-batched-contract-diff-all-7-items
plan: 07
type: execute
wave: 7
depends_on: ["322-06"]
files_modified:
  - contracts/AfKing.sol
autonomous: true
requirements: [TOMB-01, TOMB-02, TOMB-03]
must_haves:
  truths:
    - "setDailyQuantity(0) is a true in-place tombstone — it does NOT call _removeFromSet (relocates no one); sets s.dailyQuantity = 0 in place; defers the _subOf delete-vs-preserve to the in-sweep reclaim; preserves the SubscriptionUpdated(...,0,...) event; fixes the :449 NatSpec mislabel."
    - "The sweep loop has an in-loop tombstone-reclaim branch on sub.dailyQuantity == 0 that applies the deferred delete-vs-preserve, _removeFromSet(player), emits, and continues WITHOUT advancing the cursor (mirroring the :644/:739 no-++cursor swap-pop pattern)."
    - "The tombstone check is ordered vs the lastSweptDay >= today skip so a dead tombstone is ALWAYS reclaimed, never left as a permanent dead slot."
    - "Reactivation via setDailyQuantity(q>0) has no set churn / no double-add; subscribe() does not double-add an in-set tombstoned address."
    - "External cancel never relocates an entry -> no subscriber is skipped for the day because of someone else's cancel -> mint streaks not collaterally broken (H-CANCEL-SWAP-MISS fixed; SUB-07 restored)."
  artifacts:
    - path: "contracts/AfKing.sol"
      provides: "in-place tombstone setDailyQuantity(0) + in-sweep loop-top tombstone-reclaim branch (no-cursor-advance); NatSpec :449 corrected"
      contains: "dailyQuantity == 0"
  key_links:
    - from: "setDailyQuantity(0) in-place tombstone"
      to: "sweep-loop reclaim branch"
      via: "dailyQuantity == 0 sentinel reclaimed in-loop without ++cursor"
      pattern: "dailyQuantity == 0"
---

<objective>
Restore the locked SUB-07 in-place cancel-tombstone in AfKing.sol (SPEC R7, fixes the v46.0 H-CANCEL-SWAP-MISS finding). Two coordinated edits: (1) `setDailyQuantity(0)` stops swap-popping on cancel and becomes a true in-place tombstone; (2) the sweep loop gains a loop-top `dailyQuantity == 0` reclaim branch that swap-pops + reclaims the dead tombstone WITHOUT advancing the cursor. This is an ISOLATED surface (AfKing.sol only) that joins the same batched diff.

Purpose: ROADMAP SC-5 (the AfKing leg). Restores the design the user locked at v46.0 SPEC — revert-to-spec, not new design.
Output: An in-place tombstone + an in-sweep reclaim that never relocates an entry behind the cursor, so streaks are not collaterally broken.

NON-NEGOTIABLE: EDIT .sol autonomously; do NOT commit. Security/correctness over gas. AfKing.sol shares NO file/function with plans 01-06 — no cross-plan reconciliation. The reclaim branch MUST mirror the existing no-++cursor pattern (:644 / :739) so the swap-pop occupant (a pending mover from ahead) is processed at the same index this sweep. Comments describe what IS (rewrite the :449 NatSpec to describe the true in-place tombstone, no history).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-CONTEXT.md
@.planning/phases/321-spec-design-lock-call-graph-attestation-reconciliation/321-SPEC.md
@.planning/phases/321-spec-design-lock-call-graph-attestation-reconciliation/321-ATTEST-TOMB.md
@.planning/PLAN-V47-AFKING-CANCEL-TOMBSTONE.md

<grep_protocol>
Re-grep every construct; AfKing.sol was NOT edited by plans 01-06 (isolated), so the ATTEST-TOMB anchors are exact against current source — but re-grep anyway per project rule. Source-of-truth: ATTEST-TOMB §B (full current setDailyQuantity body :447-472), §C (full sweep-loop structure + the two existing no-++cursor swap-pop sites :642-649 / :737-744 + the lastSweptDay skip :614), §D (Sub struct :79-86), §F (the gap: no loop-top reclaim today — CONFIRMED ABSENT).
</grep_protocol>

<interfaces>
ATTEST-TOMB §B — current setDailyQuantity(uint8 q) (:455-472): the q==0 branch calls _removeFromSet(msg.sender) (:459) THEN does the _subOf delete-vs-preserve INLINE (:460-467: preservePaidWindow = (s.flags & FLAG_WINDOW_PAID) != 0 && s.paidThroughDay > _currentDay(); preserve -> s.dailyQuantity=0 + emit; else delete _subOf + emit). The q>0 tail (:470-471) sets s.dailyQuantity = q + emits.
ATTEST-TOMB §C — sweep loop top :609; lastSweptDay >= today skip :614 (ADVANCES cursor); auto-pause swap-pop :642-644 + continue no-++cursor :646-649; funding-kill swap-pop :737-739 + continue no-++cursor :741-744; cursor read :579, persist :777. _removeFromSet swap-and-pop body :825-837.
ATTEST-TOMB §D — Sub struct (6 fields, 31 used bytes): dailyQuantity/lastSweptDay/paidThroughDay/reinvestPct/flags/fundingSource. FLAG_WINDOW_PAID=1, FLAG_DRAIN_FIRST=2, FLAG_USE_TICKETS=4.
ATTEST-TOMB §A #14 — subscribe() 6-arg (:375-382, incl. address fundingSource).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit 1 — setDailyQuantity(0) becomes a true in-place tombstone (no relocation, deferred delete-vs-preserve)</name>
  <read_first>
    - contracts/AfKing.sol (setDailyQuantity body :455-472; the q==0 branch :458-468 incl. _removeFromSet :459 + the inline _subOf preserve/delete :460-467; the q>0 tail :470-471; NatSpec :447-454 incl. the mislabel :449; FLAG_WINDOW_PAID + _currentDay() usage; SubscriptionUpdated event shape :160-167)
    - 321-ATTEST-TOMB §B (full current body), §A #1-#4 (anchors), PLAN-TOMB §2 Edit 1
  </read_first>
  <action>
    TOMB-01 (in-place tombstone): rewrite the q==0 branch of setDailyQuantity so it does NOT call _removeFromSet(msg.sender) (relocate no one — leave the entry in _subscribers). Set `s.dailyQuantity = 0;` in place (the tombstone sentinel; dailyQuantity == 0 already means "paused", :70). DEFER the _subOf delete-vs-preserve decision (currently :460-467) to the in-sweep reclaim (Task 2) — the Sub record must stay readable so the reclaim can apply the preserve-paid-window-vs-delete branch. The cancel tx just sets the sentinel + emits SubscriptionUpdated(msg.sender, 0, (s.flags & FLAG_DRAIN_FIRST) != 0, (s.flags & FLAG_USE_TICKETS) != 0, s.reinvestPct, s.fundingSource) — preserve the existing event shape (the same 6-arg emit the in-place pause used). Stranded _poolOf ETH stays withdrawable (unchanged). The q>0 tail (reactivation, :470-471) is UNCHANGED — it sets s.dailyQuantity = q with no set churn (the sub never left the set). Rewrite the :449 NatSpec to describe the TRUE in-place tombstone (no _removeFromSet on cancel; delete-vs-preserve deferred to the sweep reclaim) — describe what IS, no history (the mislabel "the swap-pop removes the set membership" must go).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -n "function setDailyQuantity" contracts/AfKing.sol && echo "--- verify q==0 branch no longer calls _removeFromSet ---"</automated>
  </verify>
  <acceptance_criteria>
    - The q==0 branch does NOT call _removeFromSet (manual inspection + grep: _removeFromSet appears only in the sweep loop + the reclaim branch, NOT in setDailyQuantity).
    - q==0 sets s.dailyQuantity = 0 in place and emits SubscriptionUpdated(...,0,...) with the existing 6-arg shape; the _subOf delete-vs-preserve is NOT done here (deferred).
    - The q>0 reactivation tail is unchanged (no set churn).
    - The :449 NatSpec describes the in-place tombstone (no swap-pop-on-cancel language).
  </acceptance_criteria>
  <done>setDailyQuantity(0) is a true in-place tombstone; relocates no one; delete-vs-preserve deferred; event shape + reactivation preserved; NatSpec corrected.</done>
</task>

<task type="auto">
  <name>Task 2: Edit 2 — in-sweep tombstone-reclaim branch (no-++cursor), ordered vs the lastSweptDay skip</name>
  <read_first>
    - contracts/AfKing.sol (sweep loop top :609; lastSweptDay >= today skip :614; auto-pause swap-pop + no-++cursor continue :642-649; funding-kill swap-pop + no-++cursor continue :737-744; _removeFromSet :825-837; cursor read :579, persist :777; the deferred delete-vs-preserve logic to apply: preservePaidWindow = (s.flags & FLAG_WINDOW_PAID) != 0 && s.paidThroughDay > _currentDay())
    - contracts/AfKing.sol (subscribe :375-382 — confirm it does not double-add an in-set tombstoned address)
    - 321-ATTEST-TOMB §C (no-++cursor pattern), §F (the gap), PLAN-TOMB §2 Edit 2 + the invariant
  </read_first>
  <action>
    TOMB-02 (in-sweep reclaim): add a loop-top reclaim branch in the sweep loop. On reaching an entry with sub.dailyQuantity == 0 (an externally-cancelled tombstone): apply the DEFERRED _subOf delete-vs-preserve (preserve iff (sub.flags & FLAG_WINDOW_PAID) != 0 && sub.paidThroughDay > _currentDay() — keep _subOf with the sentinel; else delete _subOf), then _removeFromSet(player), emit the appropriate event (mirror the existing SubscriptionExpired/SubscriptionUpdated shape used by the in-loop swap-pops), and `continue` WITHOUT advancing the cursor (only ++processed, mirroring :646-649 / :741-744) so the swap-pop occupant (a pending mover from ahead) is processed at this same index this sweep. ORDERING vs the lastSweptDay >= today skip (:614): place/branch the tombstone check so a dead tombstone is ALWAYS reclaimed regardless of lastSweptDay (a tombstone must NOT be skipped as "already swept" and left as a permanent dead slot) — i.e. the dailyQuantity == 0 reclaim must take precedence over (or be evaluated before) the lastSweptDay skip for tombstoned entries. Behind-cursor tombstones (cancelled after the cursor passed this day) are simply never reached this day and reclaim on the next-day cursor reset — that's fine, nothing was relocated.
    TOMB-03 (invariant): confirm subscribe() (:375-382) does NOT double-add an in-set tombstoned address (it is still in _subscriberIndex) — reactivation routes through setDailyQuantity(q>0) (:470) with no set churn. If subscribe could re-add an in-set address, guard it (idempotent set membership). The resulting invariant: external cancel never relocates an entry -> the only swap-pops are in-loop (auto-pause, funding-kill, tombstone-reclaim), all no-cursor-advance -> no subscriber is skipped for the day because of someone else's cancel -> streaks not collaterally broken.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -q "dailyQuantity == 0" contracts/AfKing.sol && timeout 300 forge build 2>&1 | grep -ciE "^error|Error \(" | tr -d "\n" ; echo " errcount (AfKing isolated; should be 0 for this file)"</automated>
  </verify>
  <acceptance_criteria>
    - A loop-top branch reclaims sub.dailyQuantity == 0 tombstones: applies the deferred delete-vs-preserve, _removeFromSet, emits, and continues WITHOUT ++cursor (mirrors :644/:739).
    - The tombstone check is ordered so it is NEVER skipped by lastSweptDay >= today (no permanent dead slot).
    - subscribe() does not double-add an in-set tombstoned address (idempotent membership).
    - AfKing.sol compiles clean (isolated file).
  </acceptance_criteria>
  <done>In-sweep tombstone-reclaim added with the no-++cursor guarantee; ordered ahead of the lastSweptDay skip; reactivation/subscribe idempotent; H-CANCEL-SWAP-MISS structurally fixed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| external cancel -> sweep iteration | a cancel must never relocate a pending entry behind the persisted chunked-sweep cursor (the H-CANCEL-SWAP-MISS regression) |
| tombstone -> dead slot | a tombstone must always be reclaimable, never a permanent dead slot |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-322-07-01 | Tampering / DoS | cancel relocates a pending tail behind the cursor | mitigate | setDailyQuantity(0) relocates no one (in-place tombstone); the only swap-pops are in-loop with no-++cursor — no pending entry is ever pushed behind the cursor -> no missed day -> mint streak not reset (SUB-07 restored). |
| T-322-07-02 | DoS | permanent dead-slot buildup | mitigate | The reclaim branch is ordered ahead of the lastSweptDay skip so a tombstone is always reclaimed (this day if ahead of cursor, else next-day reset). |
| T-322-07-03 | Tampering | double-add on reactivate/subscribe | mitigate | Reactivation routes through setDailyQuantity(q>0) with no set churn; subscribe() confirmed/guarded against re-adding an in-set tombstoned address. |
| T-322-07-04 | Spoofing | targeted grief | accept | A deliberate grief is weak (the attacker cannot target a victim — swap moves whoever is the tail — and forfeits its own sub); the real exposure was the normal-operation correctness break, now fixed. |
| T-322-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
- AfKing.sol compiles clean (isolated file; errcount 0 for this file).
- setDailyQuantity(0) does not call _removeFromSet; the sweep has a no-++cursor dailyQuantity==0 reclaim branch ordered ahead of the lastSweptDay skip.
- subscribe()/reactivation are idempotent on set membership.
- No git commit performed.
</verification>

<success_criteria>
- ROADMAP SC-5 (AfKing leg) satisfied: in-place cancel-tombstone + in-sweep reclaim restore SUB-07; no external cancel relocates an entry -> no mid-day miss -> mint streaks not collaterally broken (H-CANCEL-SWAP-MISS fixed).
</success_criteria>

<output>
Create `.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-07-SUMMARY.md` when done. Record the reclaim-branch ordering decision (relative to the lastSweptDay skip) and the subscribe() double-add check outcome. Do NOT git commit. (TOMB-04/05 tests are Phase 323.)
</output>
