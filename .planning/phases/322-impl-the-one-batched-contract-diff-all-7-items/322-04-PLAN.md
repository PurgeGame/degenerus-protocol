---
phase: 322-impl-the-one-batched-contract-diff-all-7-items
plan: 04
type: execute
wave: 4
depends_on: ["322-03"]
files_modified:
  - contracts/StakedDegenerusStonk.sol
  - contracts/DegenerusGame.sol
  - contracts/interfaces/IDegenerusGame.sol
  - contracts/modules/DegenerusGameGameOverModule.sol
  - contracts/BurnieCoinflip.sol
  - contracts/BurnieCoin.sol
autonomous: true
requirements: [REDEEM-01, REDEEM-02, REDEEM-03, REDEEM-04, REDEEM-05, REDEEM-06, REDEEM-07]
must_haves:
  truths:
    - "Owed redemption ETH is physically segregated: at submit, MAX 175% of base is pulled from claimableWinnings[SDGNRS] into sDGNRS balance via a new SDGNRS-gated checked pullRedemptionReserve; the burn reverts if the full 175% cannot segregate (fail-closed)."
    - "resolveRedemptionPeriod lowers pendingRedemptionEthValue from MAX to the rolled amount (accounting only); claimRedemption pays ethDirect first from segregated balance."
    - "resolveRedemptionLootbox (DegenerusGame.sol) is external payable, credits futurePrizePool from the arriving msg.value, and the unchecked claimableWinnings[SDGNRS] -= amount debit is DELETED (Defect A fixed)."
    - "GameOver drops the double-counted + pendingRedemptionEthValue from reserved (both sites)."
    - "BURNIE is settled at submit via one atomic redeemBurnieShare (creditFlip offset by burn+consume; net new BURNIE = 0); the whole BURNIE reserve apparatus is deleted; claimRedemption is ETH-only."
    - "SDGNRS is added to onlyFlipCreditors AND can consume its own stake / burn its own held BURNIE (a new SDGNRS-gated burn on BurnieCoin); both gates touched per C4."
    - "No unchecked claimable subtraction survives anywhere in the redemption path; claimablePool == sum(claimableWinnings) stays balanced."
  artifacts:
    - path: "contracts/DegenerusGame.sol"
      provides: "new SDGNRS-gated pullRedemptionReserve(uint256); resolveRedemptionLootbox payable + unchecked debit removed + futurePrizePool credited from msg.value"
      contains: "pullRedemptionReserve"
    - path: "contracts/StakedDegenerusStonk.sol"
      provides: "submit MAX-175% pull + BURNIE flip-credit; resolve ETH release-to-rolled; claim ETH-first + payable lootbox; _payEth no game.claimWinnings; _payBurnie + flipDay + BURNIE reserve deleted"
    - path: "contracts/modules/DegenerusGameGameOverModule.sol"
      provides: "reserved drops + pendingRedemptionEthValue (both sites)"
    - path: "contracts/BurnieCoinflip.sol"
      provides: "SDGNRS in onlyFlipCreditors + atomic redeemBurnieShare(redeemer, base)"
      contains: "redeemBurnieShare"
    - path: "contracts/BurnieCoin.sol"
      provides: "new SDGNRS-gated burn for sDGNRS's own held BURNIE"
  key_links:
    - from: "StakedDegenerusStonk submit"
      to: "DegenerusGame pullRedemptionReserve"
      via: "SDGNRS-gated checked claimable[SDGNRS] -= amount + claimablePool -= amount + ETH transfer to sDGNRS"
      pattern: "pullRedemptionReserve"
    - from: "StakedDegenerusStonk submit BURNIE leg"
      to: "BurnieCoinflip redeemBurnieShare"
      via: "burn min(base,held) + consume stake remainder + creditFlip(redeemer, base)"
      pattern: "redeemBurnieShare"
---

<objective>
Make sDGNRS redemption airtight (ROADMAP SC-5). Physically segregate owed ETH (kills the reachable claimableWinnings[SDGNRS] underflow, Defect A), settle BURNIE entirely at submit as a conserved flip-credit (kills BURNIE-can't-block-ETH, Defect B), make resolveRedemptionLootbox payable with the unchecked debit removed (R1), and drop the gameOver double-count (REDEEM-04). Delete the whole BURNIE reserve apparatus and extend BURNIE authority to SDGNRS (C4).

Purpose: Closes the two redemption-accounting defects the v47 audit targets; the most security-sensitive plan in the diff.
Output: Segregated-ETH redemption; conserved-BURNIE settle-at-submit; a checked-only redemption claimable path.

NON-NEGOTIABLE: EDIT .sol autonomously; do NOT commit. R1 apply-order: this plan applies the REDEEM-03 changes to the Game-side resolveRedemptionLootbox (:1788) — make it payable + DELETE the unchecked debit FIRST; the boon-always-roll behavior is already provided by 322-03's R2 (no call-site flag), so no boon edit is needed here. R3: pullRedemptionReserve is the ONLY surviving claimableWinnings[SDGNRS] debit and it is CHECKED; no unchecked claimable subtraction may survive the redemption path. Security over gas: fail-closed on the MAX-175% pull shortfall (LOCKED = revert). BURNIE conservation: net new mint across a redemption == 0 (creditFlip offset by burn+consume).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-CONTEXT.md
@.planning/phases/321-spec-design-lock-call-graph-attestation-reconciliation/321-SPEC.md
@.planning/phases/321-spec-design-lock-call-graph-attestation-reconciliation/321-ATTEST-REDEEM-CPAY.md
@.planning/PLAN-SDGNRS-REDEMPTION-ACCOUNTING.md
@.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-03-SUMMARY.md

<grep_protocol>
Re-grep every construct; sDGNRS lines are uniformly +1/+2 below the plan citations (ATTEST-REDEEM §I.5), Game/GameOver/Coinflip/Coin anchors are exact. Source-of-truth: ATTEST-REDEEM §B (full current bodies), §C (sDGNRS ctor 6-arg subscribe — SUB-09 self-sub still drains claimable[SDGNRS], C5), §D (full resolveRedemptionLootbox body + joint-edit reconciliation), §E (onlyFlipCreditors — SDGNRS absent), §F (no SDGNRS-gated burn — additive new fn), §H (canonical CPAY shortfall pattern).
</grep_protocol>

<interfaces>
ATTEST-REDEEM §D — current Game-side resolveRedemptionLootbox (DegenerusGame.sol:1788-1838): SDGNRS-gated; unchecked claimableWinnings[SDGNRS] -= amount + claimablePool -= uint128(amount) debit at :1802-1806 (Defect A); credits futurePrizePool by reclassifying claimable; resolves in 5-ETH chunks via GAME_LOOTBOX_MODULE delegatecall.
ATTEST-REDEEM §E — onlyFlipCreditors (BurnieCoinflip:191-201) = {GAME, QUESTS, AFFILIATE, ADMIN, AF_KING}; SDGNRS ABSENT. consumeCoinflipsForBurn (:356-361) gated by SEPARATE onlyBurnieCoin (C4 — REDEEM-07 touches both).
ATTEST-REDEEM §B — submit ethValueOwed base; previewBurn/burnieReserve reserve terms; structs PendingRedemption (ethValueOwed/burnieOwed/activityScore) + RedemptionPeriod (roll/flipDay).
Canonical CPAY shortfall pattern (ATTEST §H): msg.value > cost reverts; shortfall = cost - msg.value; STRICT claimable <= shortfall reverts (1-wei sentinel); claimableWinnings[x] -= shortfall; claimablePool -= uint128(shortfall).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ETH segregation primitive (Game side) + payable lootbox + gameOver double-count drop</name>
  <read_first>
    - contracts/DegenerusGame.sol (resolveRedemptionLootbox :1788-1838 incl. unchecked debit :1802-1806, SDGNRS gate :1794, 5-ETH chunk loop :1818-1837; existing _addClaimableEth / claimablePool accounting; freeze-aware futurePrizePool credit)
    - contracts/interfaces/IDegenerusGame.sol (resolveRedemptionLootbox decl; add pullRedemptionReserve)
    - contracts/modules/DegenerusGameGameOverModule.sol (reserved = claimablePool + pendingRedemptionEthValue at :91-92 and :154-155)
    - 321-ATTEST-REDEEM §D, §A.1 R21/R24/R25, SPEC R1/R3
  </read_first>
  <action>
    REDEEM-01 (new pull): add `function pullRedemptionReserve(uint256 amount) external` to DegenerusGame.sol — gate `if (msg.sender != ContractAddresses.SDGNRS) revert E();`; CHECKED debit `claimableWinnings[SDGNRS] -= amount` (NOT unchecked) + `claimablePool -= uint128(amount)`; then a real ETH transfer of amount to sDGNRS (call{value: amount} to ContractAddresses.SDGNRS, revert on failure). This is the ONLY surviving claimable[SDGNRS] debit and it is checked. Declare it in IDegenerusGame.sol.
    REDEEM-03 + R1 (resolveRedemptionLootbox): make the Game-side resolveRedemptionLootbox (:1788) external payable. DELETE the entire unchecked debit block (:1802-1806: uint256 claimable = ...; unchecked { claimableWinnings[SDGNRS] = claimable - amount; } claimablePool -= uint128(amount);). Credit futurePrizePool from the arriving msg.value instead (freeze-aware: if prizePoolFrozen -> _setPendingPools(pNext, pFuture + uint128(msg.value)); else _setPrizePools(next, future + uint128(msg.value))). Keep the SDGNRS gate (:1794) and the 5-ETH chunk delegatecall loop (:1818-1837) UNCHANGED — the boon-always-roll comes from 322-03's R2 inside _resolveLootboxCommon, no flag edit here.
    REDEEM-04 (gameOver): in GameOverModule.sol drop the + pendingRedemptionEthValue term from reserved at BOTH sites (:91-92 and :154-155). Deterministic ETH->stETH fallback unchanged. Re-check the BurnsBlockedDuringLiveness window rationale and note any concern in the SUMMARY.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -q "function pullRedemptionReserve" contracts/DegenerusGame.sol && grep -q "pullRedemptionReserve" contracts/interfaces/IDegenerusGame.sol && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - pullRedemptionReserve exists, SDGNRS-gated, CHECKED debit + claimablePool decrement + real ETH transfer; declared in the interface.
    - The Game-side resolveRedemptionLootbox is external payable; the unchecked claimableWinnings[SDGNRS] -= amount block is GONE; futurePrizePool is credited from msg.value (freeze-aware).
    - No unchecked claimable subtraction remains in resolveRedemptionLootbox.
    - reserved drops + pendingRedemptionEthValue at both GameOverModule sites.
  </acceptance_criteria>
  <done>Game-side ETH segregation primitive + payable lootbox + gameOver double-count drop done; Defect A debit removed.</done>
</task>

<task type="auto">
  <name>Task 2: sDGNRS ETH side — submit MAX-175% pull, resolve release-to-rolled, claim ETH-first + payable lootbox call</name>
  <read_first>
    - contracts/StakedDegenerusStonk.sol (_submitGamblingClaimFrom :824-910, ethValueOwed base :855-860; resolveRedemptionPeriod :648-681, ETH release :660; claimRedemption :690-755, ethDirect/lootboxEth split + resolveRedemptionLootbox call :743 + _payEth :754; _payEth :913-935, the game.claimWinnings(address(0)) pull :919; pendingRedemptionEthValue :265; ctor subscribe :380)
    - 321-ATTEST-REDEEM §B.1-§B.4, §C; PLAN-REDEEM §3 (ETH segregation steps 1-3)
  </read_first>
  <action>
    REDEEM-01 (submit pull): in _submitGamblingClaimFrom, after computing base ethValueOwed, compute MAX payout = 175% of base (roll caps at 175). Call game.pullRedemptionReserve(maxPayout) to pull that ETH from claimableWinnings[SDGNRS] into sDGNRS's balance. Track pendingRedemptionEthValue += maxPayout (the segregated 175%). If pullRedemptionReserve reverts (full 175% can't segregate — e.g. AfKing SUB-09 drained claimable[SDGNRS], C5), the whole burn reverts (fail-closed, LOCKED). The proportional base math stays; the SEGREGATED amount is the 175% MAX.
    REDEEM-02 (resolve): in resolveRedemptionPeriod, lower pendingRedemptionEthValue from MAX to rolled — accounting only (pendingRedemptionEthValue = pendingRedemptionEthValue - segregatedMax + rolledEth; rolledEth = base*roll/100). The excess ETH already in sDGNRS balance becomes free backing (no transfer back). Mirror the :660 release shape against the MAX-segregated value.
    REDEEM-03 (claim ETH-first + payable lootbox): in claimRedemption pay ethDirect FIRST from sDGNRS's segregated balance. DROP the game.claimWinnings(address(0)) pull in _payEth (:919) for this path. Forward lootboxEth as real msg.value: game.resolveRedemptionLootbox{value: lootboxEth}(player, lootboxEth, entropy, actScore). Keep the gameOver -> all-direct / else half-half split unchanged.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -q "pullRedemptionReserve" contracts/StakedDegenerusStonk.sol && grep -q "value: lootboxEth" contracts/StakedDegenerusStonk.sol && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - submit pulls MAX 175% via game.pullRedemptionReserve and reverts the burn fail-closed if it cannot fully segregate.
    - resolve lowers pendingRedemptionEthValue MAX->rolled (accounting only, no transfer back).
    - claim pays ethDirect first from segregated balance; the game.claimWinnings(address(0)) pull is removed from the redemption path.
    - resolveRedemptionLootbox is called with {value: lootboxEth}.
  </acceptance_criteria>
  <done>sDGNRS ETH segregation (submit/resolve/claim) wired against the Game-side primitive; no unchecked claimable debit in this path.</done>
</task>

<task type="auto">
  <name>Task 3: BURNIE settle-at-submit (redeemBurnieShare) + delete the BURNIE reserve apparatus + extend authority to SDGNRS</name>
  <read_first>
    - contracts/StakedDegenerusStonk.sol (_submitGamblingClaimFrom BURNIE base :862-866; resolveRedemptionPeriod BURNIE release :663/:666 + burnieToCredit; claimRedemption BURNIE branch :730-736 + day+1 flip lookup :719-725 + _payBurnie call :748; _payBurnie :938-949; previewBurn :769-799 + burnieReserve :804-808 reserve terms; pendingRedemptionBurnie :266; RedemptionPeriod.flipDay :233)
    - contracts/BurnieCoinflip.sol (onlyFlipCreditors :191-201; creditFlip :849-855; consumeCoinflipsForBurn :356-361 gated by onlyBurnieCoin :203-206; _claimCoinflipsAmount clamp :374-377)
    - contracts/BurnieCoin.sol (burn surface: _burn :401, burnForCoinflip :430, mintForGame :439, burnForKeeper :456, burnCoin :586; no SDGNRS-gated burn exists — ATTEST §F)
    - 321-ATTEST-REDEEM §B.5/§B.6/§B.7, §E, §F; PLAN-REDEEM §4 (BURNIE flip-credit at submit), §6 (touch-points), §9 (LOCKED redeemBurnieShare orchestration)
  </read_first>
  <action>
    REDEEM-07 (authority, C4 — touch BOTH gates): add `ContractAddresses.SDGNRS` to onlyFlipCreditors (BurnieCoinflip:191-201). Allow sDGNRS to consume its own coinflip stake (extend the consumeCoinflipsForBurn gate OR fold it into redeemBurnieShare). Add a NEW SDGNRS-gated burn fn on BurnieCoin.sol for sDGNRS's own held BURNIE (additive — no such fn exists).
    REDEEM-05 (redeemBurnieShare, LOCKED single atomic call): add `function redeemBurnieShare(address redeemer, uint256 base) external` on BurnieCoinflip (SDGNRS-gated): burn min(base, BurnieCoin.balanceOf(SDGNRS)) of sDGNRS's HELD BURNIE via the new SDGNRS-gated burn; consume the remainder (base - burned) from sDGNRS's coinflip stake (consumeCoinflipsForBurn-style); then creditFlip(redeemer, base). Net new BURNIE = 0 (creditFlip's deferred mint is offset by the burn+consume). Airtight: amount <= supply => base <= held + stake, so the burn->consume waterfall always covers base.
    Wire the submit BURNIE leg (StakedDegenerusStonk._submitGamblingClaimFrom): compute base = (BurnieCoin.balanceOf(SDGNRS) + coinflip.previewClaimCoinflips(SDGNRS)) * amount / supply; call coinflip.redeemBurnieShare(redeemer, base). No roll on BURNIE (ETH-only 25-175 roll).
    REDEEM-06 (delete reserve apparatus): delete pendingRedemptionBurnie (:266) + every - pendingRedemptionBurnie term (previewBurn :796, submit :865, burnieReserve :807) + the resolve-time release (:663-666) + _payBurnie (:938-949) + RedemptionPeriod.flipDay (:233) + the day+1 coinflip lookup + the partial-claim BURNIE branch in claimRedemption (:719-736). claimRedemption becomes ETH-only. Update previewBurn/burnieReserve to drop the now-deleted reserve terms (and the whole BURNIE-preview branch if it no longer applies).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -q "function redeemBurnieShare" contracts/BurnieCoinflip.sol && grep -q "SDGNRS" contracts/BurnieCoinflip.sol && test -z "$(grep -rl pendingRedemptionBurnie contracts/)" && test -z "$(grep -rl _payBurnie contracts/StakedDegenerusStonk.sol)" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - redeemBurnieShare exists (SDGNRS-gated, atomic burn->consume->creditFlip); submit calls it; no claim-time BURNIE path remains.
    - SDGNRS is in onlyFlipCreditors AND can consume its own stake / burn its own held BURNIE (both gates touched, C4).
    - pendingRedemptionBurnie, _payBurnie, RedemptionPeriod.flipDay, the day+1 lookup, and the partial-claim BURNIE branch are all deleted; claimRedemption is ETH-only.
    - BURNIE net mint across a redemption == 0 (creditFlip offset by burn+consume).
  </acceptance_criteria>
  <done>BURNIE settled at submit (conserved); reserve apparatus deleted; SDGNRS authority extended; claim is ETH-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| sDGNRS -> Game.pullRedemptionReserve | new SDGNRS-gated checked ETH pull; access control is the boundary |
| sDGNRS -> BurnieCoinflip.redeemBurnieShare / BurnieCoin SDGNRS-burn | widened BURNIE mint/burn authority |
| 2+ same-day claimants | the original underflow trigger (Defect A) |
| claim ETH leg vs BURNIE leg | a BURNIE revert must not unwind ETH (Defect B) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-322-04-01 | Tampering | claimableWinnings[SDGNRS] underflow | mitigate | Delete the unchecked debit in resolveRedemptionLootbox (Defect A); the only surviving claimable[SDGNRS] debit (pullRedemptionReserve) is CHECKED; ETH is physically segregated so a 2nd same-day claimant cannot wrap claimable near 2^256 (R3, REDEEM-08 invariant). |
| T-322-04-02 | Denial of Service | BURNIE blocks ETH | mitigate | BURNIE settled entirely at submit (redeemBurnieShare); no BURNIE on the claim/ETH path; the ETH leg can never be unwound by a BURNIE revert (Defect B). |
| T-322-04-03 | Elevation of Privilege | widened BURNIE authority | accept | sDGNRS is a fixed trusted contract; the credited amount is bounded strictly by the redemption math (no caller-supplied value); creditFlip is offset by an equal burn+consume (net mint 0). |
| T-322-04-04 | Tampering | gameOver double-count | mitigate | Drop + pendingRedemptionEthValue from reserved at both sites — the ETH already left the game (pre-pulled into sDGNRS); re-check BurnsBlockedDuringLiveness rationale. |
| T-322-04-05 | Reentrancy | new payable resolveRedemptionLootbox + ETH transfers | mitigate | pullRedemptionReserve does checked debit BEFORE the ETH transfer (CEI); resolveRedemptionLootbox receives ETH then credits pools (no external call between receive and state update beyond the existing delegatecall loop, which is to a trusted module). Verify no reentrant claimable mutation. |
| T-322-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
- forge build clean for this plan's scope.
- No `unchecked` claimable subtraction in the redemption path (grep resolveRedemptionLootbox + sDGNRS submit/resolve/claim).
- pullRedemptionReserve is SDGNRS-gated + checked; resolveRedemptionLootbox is payable; both gameOver sites drop pendingRedemptionEthValue.
- No pendingRedemptionBurnie / _payBurnie / RedemptionPeriod.flipDay token survives; claimRedemption is ETH-only.
- redeemBurnieShare exists; SDGNRS in onlyFlipCreditors; new SDGNRS-gated burn on BurnieCoin.
- No git commit performed.
</verification>

<success_criteria>
- ROADMAP SC-5 satisfied: ETH hard-segregated (fail-closed), resolveRedemptionLootbox payable + debit removed, gameOver double-count dropped, BURNIE settled-at-submit (net mint 0, reserve apparatus deleted), SDGNRS authority extended.
- claimablePool == sum(claimableWinnings) stays balanced; no unchecked claimable subtraction in the redemption path.
</success_criteria>

<output>
Create `.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-04-SUMMARY.md` when done. Record the BurnsBlockedDuringLiveness re-check outcome and confirm the burn->consume->creditFlip conservation. Do NOT git commit.
</output>
