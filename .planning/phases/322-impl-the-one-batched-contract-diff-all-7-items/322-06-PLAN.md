---
phase: 322-impl-the-one-batched-contract-diff-all-7-items
plan: 06
type: execute
wave: 6
depends_on: ["322-05"]
files_modified:
  - contracts/modules/DegenerusGameWhaleModule.sol
  - contracts/DegenerusGame.sol
  - contracts/storage/DegenerusGameStorage.sol
autonomous: true
requirements: [CPAY-01, CPAY-03]
must_haves:
  truths:
    - "purchaseWhaleBundle / purchaseLazyPass / purchaseDeityPass accept msg.value + claimableWinnings shortfall via the canonical pattern (overpay reverts; shortfall = cost - msg.value; STRICT 1-wei sentinel; debit claimable + claimablePool)."
    - "The 3 WhaleModule earlybird credit-accrual sites are swapped to presaleBoxCredit accrual (the PRESALE-03 remainder); the _awardEarlybirdDgnrs body is then deleted if this is the last call site."
    - "All external payable entries in DegenerusGame.sol are swept for uniform claimable-pay application; _batchPurchaseUnit (onlySelf) is excluded; no path legitimately requiring fresh-ETH-only is changed; claimablePool == sum(claimableWinnings) stays balanced."
  artifacts:
    - path: "contracts/modules/DegenerusGameWhaleModule.sol"
      provides: "claimable-pay on the 3 whale purchases (CPAY-01); presaleBoxCredit accrual replacing the 3 _awardEarlybirdDgnrs sites (PRESALE-03)"
      contains: "claimablePool"
    - path: "contracts/DegenerusGame.sol"
      provides: "external payable entry sweep audited for uniform claimable-pay (CPAY-03)"
  key_links:
    - from: "whale purchase price check"
      to: "canonical CPAY shortfall pattern"
      via: "msg.value > price reverts; shortfall claimable debit + claimablePool decrement"
      pattern: "claimablePool"
---

<objective>
Apply universal claimable-pay (ROADMAP SC-4). The 3 WhaleModule purchases (whale bundle / lazy pass / deity pass) accept msg.value + claimableWinnings shortfall; the full external-payable entry sweep on DegenerusGame.sol confirms uniform application. Also swap the 3 WhaleModule earlybird credit-accrual sites to presaleBoxCredit (the PRESALE-03 remainder — these are the last 3 of the 4 former _awardEarlybirdDgnrs sites) and delete the _awardEarlybirdDgnrs body if this is the last call site.

Purpose: ROADMAP SC-4; completes PRESALE-03 credit accrual across all ETH buys except Degenerette.
Output: Claimable-pay on whale purchases; the swept payable surface; the earlybird body fully removed.

NON-NEGOTIABLE: EDIT .sol autonomously; do NOT commit. R3: every new claimable debit uses the canonical pattern (overpay reverts, STRICT 1-wei sentinel, claimablePool decremented by exactly the shortfall) so claimablePool == sum(claimableWinnings) stays balanced. CPAY-02 (presale box) was already landed in 322-02 — do NOT re-implement it. _batchPurchaseUnit is onlySelf — EXCLUDE it from the sweep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-CONTEXT.md
@.planning/phases/321-spec-design-lock-call-graph-attestation-reconciliation/321-SPEC.md
@.planning/phases/321-spec-design-lock-call-graph-attestation-reconciliation/321-ATTEST-REDEEM-CPAY.md
@.planning/PLAN-UNIVERSAL-CLAIMABLE-PAY.md
@.planning/PLAN-PRESALE-COIN-BOXES-RAKE-FREE.md
@.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-01-SUMMARY.md
@.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-02-SUMMARY.md

<grep_protocol>
Re-grep every construct; WhaleModule + Game lines drifted after 322-02/03/04. Source-of-truth: ATTEST-CPAY §G (the 3 whale price-check blocks + their _awardEarlybirdDgnrs sites), §H (canonical shortfall pattern, 3 styles agree), §A.2 C7-C15 (the external payable entry list). 322-01/02-SUMMARY: confirm who owns the final _awardEarlybirdDgnrs body deletion (this plan does it if 322-02 deferred it).
</grep_protocol>

<interfaces>
ATTEST-CPAY §G — the 3 edit targets in DegenerusGameWhaleModule.sol:
- purchaseWhaleBundle (decl :187): if (msg.value != totalPrice) revert E(); :262 ; _awardEarlybirdDgnrs(buyer, totalPrice); :263
- purchaseLazyPass (decl :380): if (msg.value != totalPrice) revert E(); :474 ; (earlybird site is :476 _awardEarlybirdDgnrs(buyer, benefitValue))
- purchaseDeityPass (decl :538): if (msg.value != totalPrice) revert E(); :581 ; _awardEarlybirdDgnrs(buyer, totalPrice); :587
Canonical CPAY pattern (ATTEST §H): (1) msg.value > cost reverts (no overpay); (2) shortfall = cost - msg.value; (3) STRICT sentinel claimableWinnings[buyer] <= shortfall reverts (preserve 1-wei); (4) claimableWinnings[buyer] -= shortfall; (5) claimablePool -= uint128(shortfall).
ATTEST-CPAY §A.2 — DegenerusGame.sol external payable entries: :347 recordMint, :498 purchase, :593 purchaseWhaleBundle, :615 purchaseLazyPass, :635 purchaseDeityPass, :712 placeDegeneretteBet, :1691 batchPurchase, :1732 _batchPurchaseUnit (onlySelf — EXCLUDE), :1875 adminSwapEthForStEth. Plus the new buyPresaleBox/buyLootboxAndPresaleBox from 322-02.
From 322-01: presaleBoxCredit mapping + presaleOver latch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CPAY-01 — claimable-pay on the 3 whale purchases + swap the 3 WhaleModule earlybird sites to presaleBoxCredit</name>
  <read_first>
    - contracts/modules/DegenerusGameWhaleModule.sol (purchaseWhaleBundle price compute :240-260 + check :262 + earlybird :263; purchaseLazyPass price :438-468 + check :474 + earlybird :476; purchaseDeityPass price :555-579 + check :581 + earlybird :587)
    - 321-ATTEST-CPAY §G.1/§G.2/§G.3, §H (canonical pattern); PLAN-CPAY (the established pattern); 322-02-SUMMARY (earlybird body ownership)
  </read_first>
  <action>
    CPAY-01: in EACH of purchaseWhaleBundle / purchaseLazyPass / purchaseDeityPass, replace `if (msg.value != totalPrice) revert E();` with the canonical shortfall pattern (ATTEST §H): if (msg.value > totalPrice) revert E(); shortfall = totalPrice - msg.value; if (shortfall != 0) { uint256 claimable = claimableWinnings[buyer]; if (claimable <= shortfall) revert E(); /* STRICT, preserve 1-wei sentinel */ unchecked { claimableWinnings[buyer] = claimable - shortfall; } claimablePool -= uint128(shortfall); } — i.e. msg.value first, claimable covers the rest, overpay reverts. Use the exact buyer address each fn already resolves. The deity pass also tracks deityPassPaidTotal[buyer] += totalPrice (:586) — keep that.
    PRESALE-03 (remainder): swap the 3 earlybird accrual sites to presaleBoxCredit: purchaseWhaleBundle :263 `_awardEarlybirdDgnrs(buyer, totalPrice)` -> `if (!presaleOver) { presaleBoxCredit[buyer] += totalPrice / 4; }`; purchaseLazyPass :476 `_awardEarlybirdDgnrs(buyer, benefitValue)` -> `if (!presaleOver) { presaleBoxCredit[buyer] += benefitValue / 4; }`; purchaseDeityPass :587 `_awardEarlybirdDgnrs(buyer, totalPrice)` -> `if (!presaleOver) { presaleBoxCredit[buyer] += totalPrice / 4; }`. Accrue on the TOTAL price actually committed (msg.value + claimable shortfall), matching the former earlybird semantics (it accrued on totalPrice). Then, with the MintModule site (322-02) and these 3 now swapped, the `_awardEarlybirdDgnrs` BODY in DegenerusGameStorage.sol has NO remaining caller — DELETE the body now (this plan owns the final deletion if 322-02 deferred it; grep-confirm zero callers first, then delete). Also confirm earlybirdDgnrsPoolStart/earlybirdEthIn/EARLYBIRD_TARGET_ETH are gone (deleted in 322-01 or here once unreferenced).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && test -z "$(grep -rl _awardEarlybirdDgnrs contracts/)" && grep -q "presaleBoxCredit" contracts/modules/DegenerusGameWhaleModule.sol && grep -q "claimablePool" contracts/modules/DegenerusGameWhaleModule.sol && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 3 whale purchases use the canonical shortfall pattern (overpay reverts; STRICT 1-wei sentinel; claimable + claimablePool debited by exactly the shortfall).
    - The 3 WhaleModule earlybird sites accrue presaleBoxCredit += committedPrice/4 gated by !presaleOver.
    - No _awardEarlybirdDgnrs token survives anywhere in contracts/ (body deleted; zero callers).
  </acceptance_criteria>
  <done>Whale purchases claimable-pay-capable; PRESALE-03 credit accrual complete across all ETH buys except Degenerette; earlybird body fully removed.</done>
</task>

<task type="auto">
  <name>Task 2: CPAY-03 — sweep all DegenerusGame.sol external payable entries for uniform claimable-pay</name>
  <read_first>
    - contracts/DegenerusGame.sol (external payable entries :347, :498, :593, :615, :635, :712, :1691, :1732 onlySelf, :1875; plus the new buyPresaleBox/buyLootboxAndPresaleBox from 322-02)
    - 321-ATTEST-CPAY §A.2 C7-C15 (entry list + the note that C9/C10/C11 are facades forwarding to WhaleModule where the actual check lives)
    - 322-02-SUMMARY (which entries already claimable-pay-capable)
  </read_first>
  <action>
    CPAY-03: audit EVERY external payable entry in DegenerusGame.sol and confirm uniform claimable-pay where appropriate. Most are FACADES that delegatecall the module where the actual msg.value check lives (e.g. :593/:615/:635 forward to WhaleModule — handled by Task 1; :347 recordMint + :498 purchase + :712 placeDegeneretteBet + :1691 batchPurchase already implement the claimable/Combined pattern per ATTEST §H.3/§H.2 — confirm, do not double-apply). For each entry: (a) if it already accepts claimable shortfall (mint/lootbox/batchPurchase/Degenerette bet — ATTEST confirms), leave it; (b) if it forwards to a module edited in Task 1 / 322-02, it is covered; (c) EXCLUDE :1732 _batchPurchaseUnit (onlySelf, internal self-call — not a user entry); (d) :1875 adminSwapEthForStEth is an admin ETH->stETH swap that legitimately requires fresh ETH — confirm it should NOT take claimable and document WHY (it is not a player purchase). For ANY player-facing purchase entry that does NOT yet accept claimable and has no legitimate fresh-ETH-only reason, apply the canonical pattern. The new buyPresaleBox/buyLootboxAndPresaleBox already accept claimable-pay (322-02 CPAY-02) — confirm. Produce a per-entry disposition table in the SUMMARY (entry -> covered-by / excluded-with-reason / edited-here). Confirm claimablePool == sum(claimableWinnings) stays balanced across the swept set (no entry debits claimable without an equal claimablePool decrement, or moves real ETH).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && timeout 300 forge build 2>&1 | grep -ciE "^error|Error \(" | tr -d "\n" ; echo " errcount (investigate any not owned by a downstream plan)"</automated>
  </verify>
  <acceptance_criteria>
    - Every external payable entry in DegenerusGame.sol has a documented disposition (covered / excluded-with-reason / edited-here) in the SUMMARY.
    - _batchPurchaseUnit (onlySelf) is excluded; adminSwapEthForStEth's fresh-ETH-only rationale is recorded.
    - No player-facing purchase entry silently rejects claimable-pay without a documented reason.
    - claimablePool == sum(claimableWinnings) stays balanced across the swept set.
  </acceptance_criteria>
  <done>Payable entry sweep complete with a per-entry disposition table; claimable-pay applied uniformly where appropriate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| buyer -> whale purchase (external payable) | untrusted msg.value + claimable shortfall cross here |
| claimable ledger | claimablePool == sum(claimableWinnings) must hold across every new debit |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-322-06-01 | Tampering | claimable-invariant breakage | mitigate | Canonical pattern: claimablePool -= exactly the shortfall that claimableWinnings[buyer] is debited; STRICT 1-wei sentinel prevents draining to 0; no overpay. |
| T-322-06-02 | Spoofing | overpay / underpay | mitigate | msg.value > cost reverts; shortfall must be fully covered by claimable (else revert) — no free passes. |
| T-322-06-03 | Information Disclosure | a payable entry silently fresh-ETH-only | mitigate | Per-entry disposition table forces an explicit covered/excluded/edited verdict; adminSwapEthForStEth fresh-ETH rationale documented. |
| T-322-06-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
- forge build clean for this plan's scope.
- All 3 whale purchases use the canonical shortfall pattern; the 3 earlybird sites swapped to presaleBoxCredit; _awardEarlybirdDgnrs body gone.
- Per-entry payable disposition table in the SUMMARY; claimablePool invariant balanced.
- No git commit performed.
</verification>

<success_criteria>
- ROADMAP SC-4 satisfied: every ETH-in path accepts msg.value + claimable shortfall (whale purchases + presale box from 322-02) with claimablePool == sum(claimableWinnings) balanced; the full payable entry set is swept and dispositioned.
- PRESALE-03 credit accrual complete (mint + 3 whale sites); earlybird emission subsystem fully removed.
</success_criteria>

<output>
Create `.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-06-SUMMARY.md` when done. Include the per-entry payable disposition table and confirm the _awardEarlybirdDgnrs body is fully deleted. Do NOT git commit.
</output>
