---
phase: 322-impl-the-one-batched-contract-diff-all-7-items
plan: 03
type: execute
wave: 3
depends_on: ["322-02"]
files_modified:
  - contracts/modules/DegenerusGameLootboxModule.sol
  - contracts/modules/DegenerusGameMintModule.sol
  - contracts/DegenerusGame.sol
  - contracts/DegenerusVault.sol
autonomous: true
requirements: [LOOT-01, LOOT-02, LOOT-03, LOOT-04, LOOT-05, LOOT-06]
must_haves:
  truths:
    - "The entire BURNIE lootbox surface is removed (openBurnieLootBox, purchaseBurnieLootbox, _purchaseBurnieLootboxFor, the purchaseCoin lootbox branch, BurnieLootOpen event, the vault wrapper) — closing the terminal-paradox."
    - "BURNIE->tickets is KEPT (purchaseCoin ticketQuantity branch, vault gamePurchaseTicketsBurnie); purchaseCoin becomes tickets-only."
    - "The 3 surviving ETH lootbox callers (openLootBox, resolveLootboxDirect, resolveRedemptionLootbox in LootboxModule) all roll full boons+passes via _resolveLootboxCommon always rolling boons + always allowing passes."
    - "The 10% boon-budget haircut is fixed on the two formerly-allowBoons=false paths (the budget is now actually spent)."
    - "_resolveLootboxCommon loses the presale/allowPasses/allowBoons bool params (5->2 bools, KEEPING emitLootboxEvent + payColdBustConsolation); all 3 callers updated to the 2-bool tail."
  artifacts:
    - path: "contracts/modules/DegenerusGameLootboxModule.sol"
      provides: "openBurnieLootBox + BurnieLootOpen deleted; _resolveLootboxCommon 2-bool signature; 3 callers updated; boon/pass conditionals collapsed; dead BURNIE-conversion helper dropped"
    - path: "contracts/modules/DegenerusGameMintModule.sol"
      provides: "purchaseBurnieLootbox + _purchaseBurnieLootboxFor deleted; purchaseCoin tickets-only"
    - path: "contracts/DegenerusGame.sol"
      provides: "purchaseBurnieLootbox + openBurnieLootBox/_openBurnieLootBoxFor deleted; purchaseCoin signature tickets-only"
    - path: "contracts/DegenerusVault.sol"
      provides: "gamePurchaseBurnieLootbox + interface decl deleted; purchaseCoin interface updated; gamePurchaseTicketsBurnie KEPT"
  key_links:
    - from: "the 3 surviving callers"
      to: "_resolveLootboxCommon 2-bool tail"
      via: "openLootBox(emit=true,coldBust=true) / resolveLootboxDirect(emit=false,coldBust=false) / resolveRedemptionLootbox(emit=false,coldBust=false)"
      pattern: "_resolveLootboxCommon"
---

<objective>
Remove the BURNIE lootbox surface entirely (terminal-paradox closure, LOOT-01) and unify the 3 remaining ETH lootbox callers to full boons+passes (LOOT-03/04/06), collapsing _resolveLootboxCommon from 5 bools to 2 (LOOT-05, SPEC R2). This fixes the 10% haircut on the two formerly-boon-less paths and removes the now-dead presale/allowPasses/allowBoons params while KEEPING emitLootboxEvent + payColdBustConsolation (C1). BURNIE->tickets is kept (LOOT-02).

Purpose: ROADMAP SC-2. Closes the unguardable BURNIE->future-ticket hole; makes all ETH boxes identical.
Output: BURNIE-box-free lootbox surface; a 2-bool common resolver; 3 unified ETH callers.

NON-NEGOTIABLE: EDIT .sol autonomously; do NOT commit. This plan owns the LootboxModule-side _resolveLootboxCommon body + the LootboxModule-side resolveRedemptionLootbox (:664) caller-arg update. The Game-side resolveRedemptionLootbox (:1788, the payable wrapper) payable + debit-removal is owned by 322-04 (REDEEM, R1) and runs AFTER this plan — per R1 the boon-flag behavior is achieved by R2's always-roll here (no call-site flag), so there is no conflict. R2 KEEPS emitLootboxEvent + payColdBustConsolation — do NOT over-delete to 0 bools.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-CONTEXT.md
@.planning/phases/321-spec-design-lock-call-graph-attestation-reconciliation/321-SPEC.md
@.planning/phases/321-spec-design-lock-call-graph-attestation-reconciliation/321-ATTEST-LOOT-DGAS-DSPIN.md
@.planning/PLAN-LOOTBOX-BOON-UNIFICATION.md
@.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-02-SUMMARY.md

<grep_protocol>
Re-grep every construct; lines drifted after 322-02 (+62% removal). Source-of-truth: ATTEST-LOOT §B.1 (actual 5-bool signature + positional order), §B.2 (each caller's bool-arg shape), §B.3 (cross-file BURNIE-box removal surface), §B.4 (Decimator also routes through resolveLootboxDirect), §F (record-corrections: BURNIE box passed allowBoons=true today; _applyEvMultiplierWithCap is cross-module; private _resolveLootboxDirect wrapper).
</grep_protocol>

<interfaces>
ATTEST-LOOT §B.1 — current _resolveLootboxCommon bools at positional args 8-12:
  (..., bool presale, bool allowPasses, bool emitLootboxEvent, bool payColdBustConsolation, bool allowBoons, ...)
Remove presale(8), allowPasses(9), allowBoons(12). KEEP emitLootboxEvent(10) + payColdBustConsolation(11).
Surviving callers (ATTEST §B.2): openLootBox -> (emit=true, coldBust=true); resolveLootboxDirect -> (emit=false, coldBust=false); resolveRedemptionLootbox -> (emit=false, coldBust=false). openBurnieLootBox is DELETED.
Conditionals to collapse: if(allowBoons) :982 -> always run; allowPasses pool-weight gates :1268-1282 + :1344-1352 -> always eligible (still real-state-gated by lazyPassValue != 0 / deity eligibility); +62% block already removed by 322-02.
Haircut (LOOT-04): mainAmount = amount - _lootboxBoonBudget(amount) (:949) now always pairs with a spent budget.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the BURNIE lootbox surface (LootboxModule + MintModule + Game + Vault)</name>
  <read_first>
    - contracts/modules/DegenerusGameLootboxModule.sol (openBurnieLootBox :561 + common call :592-607; BurnieLootOpen event decl :88, emit :609; BURNIE conversion amountEth=...*80/(...*100) :574)
    - contracts/modules/DegenerusGameMintModule.sol (purchaseCoin/_purchaseCoinFor :852-897; _purchaseBurnieLootboxFor :1425; lootBoxBurnieAmount branch :894-896; ENF-01 if(gameOverPossible) revert :881 KEEP)
    - contracts/DegenerusGame.sol (purchaseCoin wrapper :537; purchaseBurnieLootbox :559; openBurnieLootBox :664; _openBurnieLootBoxFor :682)
    - contracts/DegenerusVault.sol (gamePurchaseBurnieLootbox :554-557; purchaseBurnieLootbox interface decl :32; purchaseCoin interface :51-55; gamePurchaseTicketsBurnie :545-548 KEEP)
    - 321-ATTEST-LOOT §B.3 (removal table + current purchaseCoin/_purchaseCoinFor shape), PLAN-LOOTBOX §3.0/§5
  </read_first>
  <action>
    LOOT-01 (first): delete the BURNIE lootbox surface across files. (1) LootboxModule: delete openBurnieLootBox (:561) + BurnieLootOpen (decl :88, emit :609) + the now-unused BURNIE-conversion helper path (amountEth = burnieAmount*priceWei*80/(PRICE_COIN_UNIT*100), :574) once it has no caller. (2) MintModule: delete purchaseBurnieLootbox (:864) + _purchaseBurnieLootboxFor (:1425); remove the if(lootBoxBurnieAmount != 0){ _purchaseBurnieLootboxFor(...) } branch (:894-896); make purchaseCoin tickets-only — drop the lootBoxBurnieAmount parameter from purchaseCoin + _purchaseCoinFor. KEEP the ENF-01 if(gameOverPossible) revert GameOverPossible(); ticket guard (:881). (3) DegenerusGame.sol: delete purchaseBurnieLootbox (:559) + openBurnieLootBox/_openBurnieLootBoxFor (:664/:682); update the purchaseCoin wrapper (:537) to the new tickets-only arity. (4) DegenerusVault.sol: delete gamePurchaseBurnieLootbox (:554-557) + its purchaseBurnieLootbox interface decl (:32); update the purchaseCoin interface (:51) to tickets-only arity. KEEP gamePurchaseTicketsBurnie (:545-548) — it already calls purchaseCoin(this, ticketQuantity, 0), so a tickets-only purchaseCoin just drops the trailing 0 (LOOT-02).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && test -z "$(grep -rl openBurnieLootBox contracts/)" && test -z "$(grep -rl BurnieLootOpen contracts/)" && grep -q gamePurchaseTicketsBurnie contracts/DegenerusVault.sol && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - No openBurnieLootBox / purchaseBurnieLootbox / _purchaseBurnieLootboxFor / BurnieLootOpen / gamePurchaseBurnieLootbox token in contracts/.
    - purchaseCoin is tickets-only (no lootBoxBurnieAmount param) across MintModule + Game + Vault interface, in lock-step arity.
    - The ENF-01 ticket guard + gamePurchaseTicketsBurnie are PRESERVED (BURNIE->tickets kept).
  </acceptance_criteria>
  <done>BURNIE lootbox surface fully removed; BURNIE->tickets kept; purchaseCoin arity updated everywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce _resolveLootboxCommon 5->2 bools + unify the 3 ETH callers (R2, fix 10% haircut)</name>
  <read_first>
    - contracts/modules/DegenerusGameLootboxModule.sol (_resolveLootboxCommon sig :917 + param docs :880-916; haircut :949; if(allowBoons) :982; allowPasses gates :1268-1282 + :1344-1352; surviving caller blocks openLootBox :537-552, resolveLootboxDirect :639-654, resolveRedemptionLootbox :675-690)
    - contracts/modules/DegenerusGameDecimatorModule.sol (:597-608 — Decimator also delegatecalls resolveLootboxDirect)
    - 321-ATTEST-LOOT §B.1/§B.2/§B.4/§F-4; PLAN-LOOTBOX §3.1/§3.3/§4 (D1 uniform; haircut fix)
  </read_first>
  <action>
    LOOT-05 + R2: from _resolveLootboxCommon (:917) REMOVE 3 of 5 bools — presale (pos 8; +62% body removed by 322-02, grep-confirm no other reader), allowPasses (pos 9), allowBoons (pos 12). KEEP emitLootboxEvent (pos 10) + payColdBustConsolation (pos 11). In the body: delete the if(allowBoons) gate (:982) so the boon roll ALWAYS runs; delete the if(allowPasses) pool-weight gates (:1268-1282 + :1344-1352) so passes are always eligible (KEEP the real-state checks lazyPassValue != 0 / deity eligibility). Update param docs (:880-916) to the 2-bool tail.
    LOOT-04 (haircut): with allowBoons always-true, the mainAmount = amount - _lootboxBoonBudget(amount) carve (:949) always pairs with a spent boon budget on the two formerly-false paths — confirm the budget is actually consumed (no silent 10% drop).
    LOOT-03/LOOT-06 (unify): update each surviving caller to the 2-bool tail: openLootBox -> (..., emit=true, coldBust=true, distressEth, totalPackedEth); resolveLootboxDirect -> (..., emit=false, coldBust=false, 0, 0); resolveRedemptionLootbox (LootboxModule:664) -> (..., emit=false, coldBust=false, 0, 0). resolveLootboxDirect is shared by Degenerette (DegeneretteModule:772) AND Decimator (DecimatorModule:601) — both now roll full boons+passes (D1 uniform). Do NOT touch the Game-side resolveRedemptionLootbox (:1788) payable wrapper (322-04 owns it, R1). Verify the Decimator delegatecall args still line up after the signature change.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && test -z "$(grep -c 'bool allowBoons' contracts/modules/DegenerusGameLootboxModule.sol | grep -v '^0')" ; grep -q emitLootboxEvent contracts/modules/DegenerusGameLootboxModule.sol && grep -q payColdBustConsolation contracts/modules/DegenerusGameLootboxModule.sol && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - _resolveLootboxCommon has exactly 2 bool params (emitLootboxEvent + payColdBustConsolation); presale/allowPasses/allowBoons removed.
    - The boon roll runs unconditionally; passes are gated only by real game-state (lazyPassValue/deity), not allowPasses.
    - All 3 surviving callers pass the correct (emit, coldBust) pair; openBurnieLootBox is gone.
    - The 10% haircut is fixed on resolveLootboxDirect + resolveRedemptionLootbox (budget actually spent).
    - Decimator's delegatecall to resolveLootboxDirect still compiles/lines-up.
  </acceptance_criteria>
  <done>5->2 bool reduction + 3-caller unification done; haircut fixed; Decimator + Degenerette win paths now boon/pass-eligible.</done>
</task>

<task type="auto">
  <name>Task 3: Build the LootboxModule-side surface and confirm no dangling BURNIE-box references</name>
  <read_first>
    - The four files this plan edited; any cross-module callers of the removed selectors (grep)
  </read_first>
  <action>
    Run forge build over the touched files. Resolve any compile error introduced by this plan (e.g. a missed caller of a removed selector, a stale interface decl). Residual errors that belong to a not-yet-run downstream plan (e.g. the Game-side resolveRedemptionLootbox payable edit owned by 322-04, or earlybird body deletion owned elsewhere) are acceptable IF documented in the SUMMARY — but anything caused by THIS plan's BURNIE-box removal or bool reduction must compile clean. Grep-confirm zero remaining references to any removed BURNIE-box selector across contracts/.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && timeout 300 forge build 2>&1 | grep -ciE "^error|Error \(" | tr -d '\n' ; echo " (error count; investigate any not owned by a downstream plan)"</automated>
  </verify>
  <acceptance_criteria>
    - No dangling reference to any removed BURNIE-box selector (grep clean).
    - forge build errors, if any, are confined to constructs explicitly owned by a downstream plan and are documented in the SUMMARY.
  </acceptance_criteria>
  <done>LootboxModule + MintModule + Game + Vault compile clean for this plan's scope; no BURNIE-box reference survives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| player -> openLootBox/resolveLootboxDirect/resolveRedemptionLootbox | box resolution can now mint passes (whale/lazy) on all 3 ETH paths |
| terminal-paradox | future-level tickets must only exist against pool-funding ETH |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-322-03-01 | Elevation of Privilege | BURNIE->future-ticket | mitigate | Remove the BURNIE lootbox surface entirely (LOOT-01) — the unguardable BURNIE->future-ticket path is deleted, closing the terminal-paradox (ENF-01 is current-level-only, cannot guard future-dated lootbox tickets). |
| T-322-03-02 | Tampering | boon draw RNG | accept | Boon draw uses the committed lootbox rngWord (freeze invariant intact, v45); no new RNG surface — D1 win paths are player-claim, off advanceGame. |
| T-322-03-03 | Information Disclosure | over-deletion of bools | mitigate | KEEP emitLootboxEvent + payColdBustConsolation (C1); they legitimately differ per caller. Verify both survive. |
| T-322-03-04 | Repudiation | passes via non-ETH boxes | mitigate | Presale box stays boon-less (322-02) and Degenerette/Decimator win boxes are ETH-value-funded — every pass-minting box is ETH-funded (PLAN-LOOTBOX §4). |
| T-322-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
- forge build clean for this plan's scope (downstream-owned residual errors documented).
- No BURNIE-box selector survives anywhere in contracts/.
- _resolveLootboxCommon is 2-bool; the 3 callers pass the right (emit, coldBust) pair.
- BURNIE->tickets + gamePurchaseTicketsBurnie preserved.
- No git commit performed.
</verification>

<success_criteria>
- ROADMAP SC-2 satisfied: BURNIE lootbox surface removed (terminal-paradox closed); 3 ETH callers unified with full boons+passes + 10% haircut fixed + dead params removed; BURNIE->tickets kept.
</success_criteria>

<output>
Create `.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-03-SUMMARY.md` when done. Record any forge build error deferred to a downstream plan (with the owning plan id). Do NOT git commit.
</output>
