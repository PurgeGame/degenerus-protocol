---
phase: 322-impl-the-one-batched-contract-diff-all-7-items
plan: 08
type: execute
wave: 8
depends_on: ["322-01", "322-02", "322-03", "322-04", "322-05", "322-06", "322-07"]
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "The full batched diff compiles (forge build succeeds) with the reconciled signatures intact (single resolveRedemptionLootbox; 2-bool _resolveLootboxCommon; single DegeneretteModule DGAS+DSPIN edit; presale-param removal landed with the presale-bonus removal)."
    - "The existing test suite shows NO NEW breakage vs the v46 baseline (the documented ~44 pre-existing failures are not regressions; do not chase them)."
    - "The reconciliation joint-checks pass: no unchecked claimable subtraction in the redemption path; claimablePool == sum(claimableWinnings) balanced across PRESALE-06/CPAY/REDEEM; no BURNIE-box selector survives; the box RNG uses the salted committed word; AfKing cancel relocates no one."
    - "The batched contract diff is HELD at the contract-commit boundary and presented for ONE explicit USER hand-review; nothing is committed without that approval."
  artifacts: []
  key_links: []
---

<objective>
Verify the complete batched diff (all 7 items, plans 322-01..07) and HOLD it at the contract-commit boundary for the single USER hand-review. This plan compiles the whole tree, runs the existing test suite to confirm no NEW breakage vs the v46 baseline, runs the cross-plan reconciliation joint-checks (BATCH-01 surfaces from the SPEC), and then presents the ONE batched `contracts/*.sol` diff for explicit user approval. It does NOT commit — the user reviews the diff first.

Purpose: ROADMAP SC-6 (reconciled per manifest §2, HELD at the commit boundary). The single approval gate for the whole milestone's contract surface.
Output: A verified, compiling, non-regressing batched diff + the user-approval gate.

NON-NEGOTIABLE: This is the ONLY place the batched diff is presented for approval, and it is `autonomous: false`. NO task may state or imply the contracts are "pre-approved." The executor APPLIES nothing new here (all edits landed in 322-01..07); it VERIFIES + PRESENTS. Commit only AFTER the user explicitly approves the diff (the commit-guard hook blocks commits while .sol is dirty; the approved-commit uses the sanctioned bypass). Scope is IMPL-only: confirm it compiles + no NEW test breakage — do NOT author new behavioral/gas/repro tests (Phase 323) and do NOT run the formal RNG-freeze sweep (Phase 324; may NOTE the R4 re-verification).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-CONTEXT.md
@.planning/phases/321-spec-design-lock-call-graph-attestation-reconciliation/321-SPEC.md
@.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-01-SUMMARY.md
@.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-02-SUMMARY.md
@.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-03-SUMMARY.md
@.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-04-SUMMARY.md
@.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-05-SUMMARY.md
@.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-06-SUMMARY.md
@.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-07-SUMMARY.md

<baseline_note>
The v46 closure HEAD documented suite baseline is 559 pass / 44 fail (plus the testGas04 stale 45th, which is a TEST-only fix in Phase 323 — it will still fail here). So at this IMPL phase, expect ~45 pre-existing failures UNCHANGED. A NEW failure = a count above the baseline OR a previously-passing test now failing. Do NOT chase the pre-existing baseline failures (they are unrelated, v45/v46-documented). The goal is ZERO NEW failures attributable to the v47 diff.
</baseline_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compile the full batched diff + run the reconciliation joint-checks (BATCH-01 surfaces)</name>
  <read_first>
    - All 7 SUMMARYs (322-01..07) — collect the deferred-error notes, the per-entry payable disposition, the earlybird-body ownership, the dead-flag dispositions
    - 321-SPEC.md §1 R1-R7 + §0 C1-C9 (the reconciliation surfaces to joint-check)
  </read_first>
  <action>
    Compile + reconcile. (1) `forge build` over the whole tree — it MUST succeed (zero errors). Any error surfaced means a cross-plan reconciliation gap (e.g. a stale selector, an arity mismatch, a dangling earlybird/BURNIE-box reference) — fix it (this is the plan that owns final cross-plan reconciliation per BATCH-01). (2) Run the joint-checks via grep:
      - R1: exactly ONE Game-side resolveRedemptionLootbox, declared `external payable`, with NO unchecked claimableWinnings[SDGNRS] debit; the LootboxModule-side caller updated to the 2-bool tail.
      - R2: _resolveLootboxCommon has exactly 2 bools (emitLootboxEvent + payColdBustConsolation); presale/allowPasses/allowBoons gone; 3 callers only (openBurnieLootBox gone).
      - R3: no `unchecked` claimable subtraction in the redemption path; every claimablePool mutation has a matching claimableWinnings mutation of equal magnitude OR a real ETH in/out (spot-check _creditBoxProceeds, pullRedemptionReserve, the whale shortfall debits, the box buy).
      - R5: the DegeneretteModule edit landed DGAS + DSPIN together; MAX_SPINS_PER_BET gone.
      - R6: no Earlybird token in the Pool enums; _awardEarlybirdDgnrs / _finalizeEarlybird / EARLYBIRD_* gone (or kept-with-documented-reason).
      - R7: setDailyQuantity(0) does not call _removeFromSet; the sweep has the dailyQuantity==0 reclaim branch.
      - C-checks: emitLootboxEvent + payColdBustConsolation preserved (C1); CURRENCY_WWXRP used (C6); presaleOver in slot-0 padding (C7); no BURNIE-box selector (LOOT-01).
    Record each joint-check result in the SUMMARY (PASS/FAIL + the grep evidence).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && timeout 400 forge build 2>&1 | tail -3 && timeout 400 forge build 2>&1 | grep -ciE "^error|Error \(" | tr -d "\n" ; echo " total build errors (MUST be 0)"</automated>
  </verify>
  <acceptance_criteria>
    - forge build succeeds with ZERO errors over the whole tree.
    - All BATCH-01 joint-checks (R1/R2/R3/R5/R6/R7 + the C-checks) PASS with grep evidence recorded in the SUMMARY.
    - No dangling BURNIE-box / earlybird reference; no MAX_SPINS_PER_BET; single payable resolveRedemptionLootbox; 2-bool _resolveLootboxCommon.
  </acceptance_criteria>
  <done>The full batched diff compiles; all cross-plan reconciliation joint-checks pass with evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Run the existing test suite — confirm NO NEW breakage vs the v46 baseline</name>
  <read_first>
    - <baseline_note> above (the ~44/45 documented pre-existing failures)
    - The v46 closure suite count (559 pass / 44 fail + testGas04 stale = the documented baseline)
  </read_first>
  <action>
    Run the existing test suite (`forge test`, or the project's standard invocation — check foundry.toml for the profile). Capture the pass/fail count. Compare against the documented v46 baseline (~559 pass / ~44-45 fail). Classify EVERY failure: (a) a documented pre-existing baseline failure (unrelated to v47 — do NOT chase) OR (b) a NEW failure attributable to the v47 diff. For any NEW failure, root-cause it and fix the CONTRACT (not the test — tests are Phase 323; only fix a contract bug the test legitimately catches). The testGas04 stale failure (the 45th) is a TEST-only fix scheduled for Phase 323 — it WILL still fail here; record it as the known stale failure, NOT a v47 regression. Goal: ZERO NEW failures attributable to the v47 diff. Note (do NOT execute) that the presale-box RNG-freeze formal re-verification (R4) is flagged for Phase 324; you MAY note the freeze argument holds (committed word + salt, no new mutable SLOAD) but the formal sweep is out of scope here.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && timeout 600 forge test 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - The suite runs to completion; the pass/fail count is captured and compared to the v46 baseline.
    - Every failure is classified pre-existing-baseline vs NEW-v47; ZERO NEW v47-attributable failures (any new one is contract-root-caused + fixed).
    - The testGas04 stale failure is recorded as the known Phase-323 test-only fix, not a regression.
  </acceptance_criteria>
  <done>Suite shows no NEW breakage vs the v46 baseline; all failures classified; the diff is correctness-clean at the IMPL level.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: USER hand-review of the ONE batched contract diff (the single approval gate)</name>
  <what-built>
    The complete v47.0 batched `contracts/*.sol` diff — all 7 work items reconciled per manifest §2 / SPEC R1-R7:
    1. PRESALE — rake removed (20% vault skim + 62% BURNIE bonus gone), credit-gated boon-less presale boxes (80/20, 50-ETH clamp-close + sweep + presaleOver latch), Pool.Earlybird->PresaleBox, earlybird subsystem removed.
    2. LOOT — BURNIE lootbox surface removed (terminal-paradox closed), 3 ETH callers unified (full boons+passes, 10% haircut fixed), _resolveLootboxCommon 5->2 bools.
    3+6. DGAS+DSPIN — Degenerette write-batched same-results at per-currency caps (ETH 25 / BURNIE 15 / WWXRP 5).
    4. CPAY — universal claimable-pay (whale purchases + presale box + payable entry sweep), claimablePool invariant balanced.
    5. REDEEM — sDGNRS ETH hard-segregated (underflow fixed, fail-closed), BURNIE flip-credit-at-submit (BURNIE-can't-block-ETH, net mint 0), resolveRedemptionLootbox payable + unchecked debit removed, gameOver double-count dropped, SDGNRS authority extended.
    7. TOMB — AfKing in-place cancel-tombstone + in-sweep reclaim (H-CANCEL-SWAP-MISS fixed; SUB-07 restored).
    Verified: forge build succeeds; all BATCH-01 reconciliation joint-checks pass; the existing suite shows no NEW breakage vs the v46 baseline.
  </what-built>
  <how-to-verify>
    1. Review the full batched diff: `git diff -- contracts/` (read every changed file; this is the single hand-review for the whole milestone's contract surface).
    2. Confirm the reconciled surfaces match the SPEC: single payable resolveRedemptionLootbox with no unchecked claimable debit (R1); 2-bool _resolveLootboxCommon keeping emitLootboxEvent+payColdBustConsolation (R2/C1); _creditBoxProceeds 80/20 + pullRedemptionReserve checked (R3); presale box RNG = salted committed word (R4); single DegeneretteModule DGAS+DSPIN edit (R5); Pool.PresaleBox + earlybird removed (R6); AfKing in-place tombstone (R7).
    3. Confirm the build/test evidence in the 322-08 SUMMARY (forge build clean; no NEW failures vs baseline).
    4. If approved, the diff may be committed (a single sanctioned contract commit using the approved bypass — `CONTRACTS_COMMIT_APPROVED=1`); the .planning/ docs (already authored) are force-added as needed. If issues are found, describe them for a gap-closure pass.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the single batched contract commit, or describe the issues to fix (gap-closure).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| applied .sol diff -> committed source | the contract-commit boundary; ONLY crossed after explicit user hand-review |
| cross-plan reconciliation | the 7 plans' edits to shared files must compose into one coherent diff (BATCH-01) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-322-08-01 | Tampering | a cross-plan reconciliation gap ships silently | mitigate | forge build MUST be zero-error + the R1-R7/C joint-checks run with grep evidence before the gate. |
| T-322-08-02 | Repudiation | contract committed without review | mitigate | Task 3 is autonomous:false blocking-human; NO pre-approval; commit only after explicit "approved"; commit-guard hook blocks commits while .sol is dirty (bypass only post-approval). |
| T-322-08-03 | Tampering | a v47 regression masquerades as a baseline failure | mitigate | Every test failure is classified pre-existing vs NEW; only the documented ~44/45 baseline failures are tolerated; any NEW failure is contract-root-caused + fixed. |
| T-322-08-04 | Tampering | RNG-freeze breakage slips past IMPL | accept | R4 freeze argument noted (committed word + salt, no new mutable SLOAD); the FORMAL re-verification is Phase 324 (out of IMPL scope). |
| T-322-08-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this milestone (audit-remediation, no new deps). No `## Package Legitimacy Audit` required. |
</threat_model>

<verification>
- forge build: zero errors over the whole tree.
- All BATCH-01 joint-checks (R1/R2/R3/R5/R6/R7 + C-checks) PASS with recorded grep evidence.
- forge test: no NEW failures vs the v46 baseline; all failures classified.
- Task 3 reached as a blocking-human gate; nothing committed until explicit user approval.
</verification>

<success_criteria>
- ROADMAP SC-6 satisfied: the diff is reconciled per manifest §2, compiles + passes the existing suite with no NEW breakage, and is HELD at the contract-commit boundary for the single explicit USER hand-review (no pre-approval).
</success_criteria>

<output>
Create `.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-08-SUMMARY.md` when done. Record: the forge build result, the full BATCH-01 joint-check table (with grep evidence), the test pass/fail count vs baseline + the failure classification, and the user-approval outcome. The single batched contract commit happens ONLY after explicit user approval (this plan does not pre-commit).
</output>
