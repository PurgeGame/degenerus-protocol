---
phase: 322-impl-the-one-batched-contract-diff-all-7-items
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/storage/DegenerusGameStorage.sol
  - contracts/StakedDegenerusStonk.sol
  - contracts/interfaces/IStakedDegenerusStonk.sol
  - contracts/modules/DegenerusGameAdvanceModule.sol
  - contracts/modules/DegenerusGamePayoutUtils.sol
autonomous: true
requirements: [PRESALE-10, PRESALE-11, PRESALE-13, PRESALE-06]
must_haves:
  truths:
    - "Pool.Earlybird is renamed to Pool.PresaleBox at the same ordinal (4) in both the concrete and interface enums — ABI ordinal preserved."
    - "The entire earlybird emission subsystem (_awardEarlybirdDgnrs body, _finalizeEarlybird, EARLYBIRD_END_LEVEL trigger, earlybird state + constants) is deleted with no surviving consumer."
    - "New presale storage exists: presaleOver (slot-0 padding), presaleBoxEthSold counter, presaleBoxCredit mapping, presale-box index→RNG-word + per-player queued-box-ETH mirrors."
    - "A _creditBoxProceeds(uint256 boxEth) helper exists in PayoutUtils that splits box ETH 80% VAULT / 20% SDGNRS while bumping claimablePool by the full boxEth (invariant-preserving)."
  artifacts:
    - path: "contracts/storage/DegenerusGameStorage.sol"
      provides: "presaleOver bool (slot 0), presaleBoxEthSold counter, presaleBoxCredit mapping, presale-box index/RNG-word + queued-ETH mirrors; earlybird subsystem deleted"
      contains: "presaleOver"
    - path: "contracts/StakedDegenerusStonk.sol"
      provides: "Pool.PresaleBox enum (ordinal 4) replacing Pool.Earlybird; pool-list assignment renamed"
      contains: "PresaleBox"
    - path: "contracts/interfaces/IStakedDegenerusStonk.sol"
      provides: "Pool.PresaleBox enum (ordinal 4) renamed in lockstep"
      contains: "PresaleBox"
    - path: "contracts/modules/DegenerusGamePayoutUtils.sol"
      provides: "_creditBoxProceeds(uint256 boxEth) 80/20 split helper"
      contains: "_creditBoxProceeds"
  key_links:
    - from: "contracts/storage/DegenerusGameStorage.sol presaleBoxCredit"
      to: "credit-accrual sites (322-02)"
      via: "presaleBoxCredit[x] += eth/4 replacing _awardEarlybirdDgnrs"
      pattern: "presaleBoxCredit"
    - from: "contracts/modules/DegenerusGamePayoutUtils.sol _creditBoxProceeds"
      to: "box-buy routing (322-02)"
      via: "claimablePool += boxEth; VAULT += 80%; SDGNRS += 20%"
      pattern: "_creditBoxProceeds"
---

<objective>
Lay the PRESALE storage + token-pool + helper foundation that items PRESALE, CPAY (box leg), and the box-buy/resolution plan (322-02) build on. This plan establishes new state and the 80/20 box-proceeds helper, renames the sDGNRS DGNRS pool slot Earlybird→PresaleBox, and removes the entire earlybird emission subsystem (which the presale boxes replace) — all in files that 322-02 then wires into.

Purpose: Interface-first ordering per the SPEC §2 edit-order map (storage/enums first → helpers). Downstream plans must not scavenger-hunt for the new state shape or the box-proceeds helper.
Output: New presale storage fields, the renamed `Pool.PresaleBox` enum (both concrete + interface), the deleted earlybird subsystem, and the `_creditBoxProceeds` helper.

NON-NEGOTIABLE: This plan EDITS `contracts/*.sol` but MUST NOT commit anything. The whole milestone is ONE batched diff committed only after the final USER-APPROVAL gate (plan 322-08). The plan docs are already committed by the planner; do NOT git commit during execution (the commit-guard hook blocks commits while `.sol` is dirty anyway).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-CONTEXT.md
@.planning/phases/321-spec-design-lock-call-graph-attestation-reconciliation/321-SPEC.md
@.planning/phases/321-spec-design-lock-call-graph-attestation-reconciliation/321-ATTEST-PRESALE.md
@.planning/PLAN-PRESALE-COIN-BOXES-RAKE-FREE.md

<grep_protocol>
Every cited file:line is from contract HEAD 2a18d622 (byte-identical to current HEAD — confirmed). Lines will drift as the batched diff lands across plans 01→08. Before each edit, re-grep the named construct (function name, constant name, enum member) to find its CURRENT line — never trust a raw line number from this plan. The ATTEST-PRESALE table is the source-of-truth for what each construct looks like.
</grep_protocol>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Pool.Earlybird → Pool.PresaleBox (concrete + interface, ABI-safe)</name>
  <read_first>
    - contracts/StakedDegenerusStonk.sol (enum at :210-216; pre-mint pool-list assignment at :366-370; EARLYBIRD_POOL_BPS decl :297, ctor use :348, assign :370; burnAtGameOver :521)
    - contracts/interfaces/IStakedDegenerusStonk.sol (enum at :10-16)
    - 321-ATTEST-PRESALE.md §5 (enum + pool-list anchors) and §6 (interface enum)
  </read_first>
  <action>
    Per PRESALE-10 + SPEC R6: rename the enum member `Earlybird` → `PresaleBox` in BOTH `StakedDegenerusStonk.sol` (enum `Pool { Whale, Affiliate, Lootbox, Reward, Earlybird }`) and `interfaces/IStakedDegenerusStonk.sol` (identical enum). Keep the member at ordinal position 4 (last) — do NOT reorder; `uint8(Pool.X)` ordinals must stay stable across the ABI boundary. Rename the pool-list assignment `poolBalances[uint8(Pool.Earlybird)] = earlybirdAmount;` to `poolBalances[uint8(Pool.PresaleBox)] = ...` (the local that computes the 10%-of-INITIAL_SUPPLY amount may stay named or be renamed — your discretion). Rename the `EARLYBIRD_POOL_BPS = 1000` constant to a `PRESALE_BOX_POOL_BPS` (or equivalent) — value UNCHANGED at 1000 (the 10% allocation is preserved, PRESALE-10). Update comments to describe what IS (no history per project rule). PRESALE-13: confirm `burnAtGameOver` (StakedDegenerusStonk.sol:521) `delete poolBalances` still zeroes the renamed slot — no edit needed, just verify the undrained-pool burn covers PresaleBox (it does — `delete` clears all ordinals).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && ! grep -rn "Earlybird" contracts/StakedDegenerusStonk.sol contracts/interfaces/IStakedDegenerusStonk.sol && grep -c "PresaleBox" contracts/StakedDegenerusStonk.sol contracts/interfaces/IStakedDegenerusStonk.sol</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PresaleBox" contracts/interfaces/IStakedDegenerusStonk.sol` shows it at the SAME ordinal position (5th/last enum member) as `Earlybird` was.
    - No `Earlybird` token remains in either file.
    - The `1000` bps allocation constant is preserved (renamed, value unchanged).
    - `delete poolBalances` in `burnAtGameOver` is unchanged (still burns the renamed pool at game-over).
  </acceptance_criteria>
  <done>Both enums carry `PresaleBox` at ordinal 4; pool-list + bps constant renamed; no `Earlybird` token survives in these two files.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the entire earlybird emission subsystem (grep-confirm no surviving consumer)</name>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol (_awardEarlybirdDgnrs fn body :971-1014; earlybirdDgnrsPoolStart :957; earlybirdEthIn :960; EARLYBIRD_TARGET_ETH :178; EARLYBIRD_END_LEVEL :175)
    - contracts/modules/DegenerusGameAdvanceModule.sol (_finalizeEarlybird fn :1744-1757; EARLYBIRD_END_LEVEL trigger :1672-1673)
    - 321-ATTEST-PRESALE.md §3d/§3e (_finalizeEarlybird + trigger), §4c (_awardEarlybirdDgnrs body), §4d (state + constants), §8 (the 4 call sites + sole _finalizeEarlybird caller)
  </read_first>
  <action>
    Per PRESALE-10 + SPEC R6: delete the earlybird emission machinery. (1) DELETE `_awardEarlybirdDgnrs(address,uint256)` body in `DegenerusGameStorage.sol` (the quadratic-curve fn). (2) DELETE `_finalizeEarlybird()` in `AdvanceModule.sol` and its sole call — the `if (lvl == EARLYBIRD_END_LEVEL) { _finalizeEarlybird(); }` trigger. (3) DELETE the storage `earlybirdDgnrsPoolStart`, `earlybirdEthIn` and the constants `EARLYBIRD_TARGET_ETH`, `EARLYBIRD_END_LEVEL`. The 4 `_awardEarlybirdDgnrs` CALL SITES (MintModule:1210, WhaleModule:263/476/587) are NOT touched here — they are swapped to `presaleBoxCredit` accrual in plan 322-02 / 322-06 (this plan only removes the fn definition + finalize; do NOT leave a dangling reference — if removing the fn body before the call sites are swapped breaks the build, leave a temporary stub ONLY if necessary and note it for 322-02, but prefer to keep the call sites compiling by having 322-02 own the swap; the cleanest sequence is: keep `_awardEarlybirdDgnrs` deletable only once call sites are gone). PRACTICAL ORDER: because the 4 call sites live in MintModule + WhaleModule (owned by 322-02 / 322-06), do NOT delete the `_awardEarlybirdDgnrs` BODY in this plan — instead, in this plan delete only `_finalizeEarlybird` + its trigger + the `EARLYBIRD_END_LEVEL`/`EARLYBIRD_TARGET_ETH`/`earlybirdDgnrsPoolStart`/`earlybirdEthIn` that `_finalizeEarlybird`/`_awardEarlybirdDgnrs` reference, and convert `_awardEarlybirdDgnrs` into the new accrual primitive OR leave its body intact-but-unused until 322-02 removes the call sites and then deletes the body. CHOSEN APPROACH (deterministic): leave `_awardEarlybirdDgnrs` body in place in THIS plan; delete it in 322-02 immediately after the last call site is swapped (322-02 owns MintModule, 322-06 owns the 3 WhaleModule sites). In THIS plan: delete `_finalizeEarlybird` + its trigger only if doing so leaves no dangling reference (it has a sole caller at :1672-1673 — delete both together). Keep `EARLYBIRD_END_LEVEL`/`EARLYBIRD_TARGET_ETH`/`earlybirdDgnrsPoolStart`/`earlybirdEthIn` until their last reader is gone — grep before deleting each. Document in the SUMMARY exactly which earlybird symbols this plan removed vs deferred to 322-02/322-06.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && ! grep -rn "_finalizeEarlybird" contracts/ && ! grep -rn "EARLYBIRD_END_LEVEL" contracts/</automated>
  </verify>
  <acceptance_criteria>
    - `_finalizeEarlybird` and its `EARLYBIRD_END_LEVEL` trigger are deleted (grep returns nothing).
    - Any earlybird symbol deleted in this plan has zero surviving readers (grep each before deletion).
    - The SUMMARY records the precise split: which earlybird symbols this plan removed vs which are deferred to 322-02 (MintModule call site + `_awardEarlybirdDgnrs` body) / 322-06 (3 WhaleModule call sites).
  </acceptance_criteria>
  <done>`_finalizeEarlybird` + trigger removed; remaining earlybird state/constants either removed (if no reader) or explicitly deferred-with-reason; no dangling reference left that breaks the build for this plan's files.</done>
</task>

<task type="auto">
  <name>Task 3: Add new presale storage + the _creditBoxProceeds 80/20 helper</name>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol (slot-0 layout: header table :43-66, inline comment :216-220, last slot-0 field prizePoolFrozen :332, slot-1 opener currentPrizePool :342; existing lootbox mirrors `lootboxEth`/`lootboxRngWordByIndex` to mirror; LOOTBOX_PRESALE_ETH_CAP :852)
    - contracts/modules/DegenerusGamePayoutUtils.sol (whole file, 47 lines: `_creditClaimable` :21-27 credits balance NOT pool; `_queueWhalePassClaimCore` :30-46 bumps pool only on remainder)
    - 321-ATTEST-PRESALE.md §4a (slot-0 free-byte tally: 2 bytes free, presaleOver fits with 1 to spare), §7a/§7b (_creditClaimable + _queueWhalePassClaimCore — why a NEW helper is needed not a reuse)
    - PLAN-PRESALE-COIN-BOXES-RAKE-FREE.md §4 (storage block) + §3.3 (80/20 routing)
  </read_first>
  <action>
    Per PRESALE-11, PRESALE-06, CPAY-02 + SPEC R3/C7. STORAGE (DegenerusGameStorage.sol):
    (1) Add `bool internal presaleOver;` IMMEDIATELY after `prizePoolFrozen` (the last slot-0 field at :332) so it lands in slot-0's 2-byte padding (byte [30:31], 1 byte still free per C7/ATTEST §4a). LATCHING monotonic terminal.
    (2) Add `uint96 internal presaleBoxEthSold;` in its own slot (cumulative box ETH, cap = `LOOTBOX_PRESALE_ETH_CAP`'s presale-box analog of 50 ether — note the 50-ETH box cap is a NEW value; the existing 200-ETH `LOOTBOX_PRESALE_ETH_CAP` is a DIFFERENT cap. Define a new `PRESALE_BOX_ETH_CAP = 50 ether` constant). The counter does NOT fit slot-0's 1 remaining byte (needs >uint64), so it takes its own slot; co-pack with another partially-filled slot only if you find one at edit time (your discretion).
    (3) Add `mapping(address => uint256) internal presaleBoxCredit;` (accrued spendable credit, PRESALE-03 consumer added in 322-02).
    (4) Add the presale-box queue mirrors: `mapping(uint48 => uint256) internal presaleBoxRngWordByIndex;` (or reuse the existing combined lootbox index — see R4: combined lootbox+box share ONE index/word; for a STANDALONE box you still need a per-index queued-ETH + word) and `mapping(address => mapping(uint48 => uint256)) internal presaleBoxEth;` (per-player queued box ETH at index), MIRRORING the existing `lootboxEth` / `lootboxRngWordByIndex` shapes. Match the existing index width used by the lootbox queue (grep `lootboxRngWordByIndex` for the key type). Per R4, the COMBINED lootbox+box case shares the lootbox index and its committed `lootboxRngWordByIndex[index]` word — the standalone box still needs its own queued-ETH record; the box payout entropy is derived (in 322-02) via the salted `keccak256(abi.encodePacked(rngWord, "PRESALE_BOX"))`.
    Comments describe what IS (no changelog).
    HELPER (DegenerusGamePayoutUtils.sol): add `function _creditBoxProceeds(uint256 boxEth) internal` per R3: `uint256 sdgnrsShare = boxEth * 20 / 100; claimablePool += uint128(boxEth); claimableWinnings[ContractAddresses.VAULT] += boxEth - sdgnrsShare; claimableWinnings[ContractAddresses.SDGNRS] += sdgnrsShare;` — remainder of the integer division lands in the VAULT (80%) side (`boxEth - sdgnrsShare`). Invariant: `claimablePool += boxEth` exactly matches the sum of the two `claimableWinnings` credits. Emit `PlayerCredited` for each credited beneficiary if that matches the existing `_creditClaimable` convention (your discretion — match the module's existing event pattern). This single helper serves PRESALE-06 (purchase routing) AND CPAY-02 (when the box is paid from claimable, the player-claimable debit is done at the call site in 322-02 and this helper does the VAULT/SDGNRS re-credit — net pool delta 0 for the claimable-funded portion, +msg.value for the fresh-ETH portion).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -n "presaleOver" contracts/storage/DegenerusGameStorage.sol && grep -n "presaleBoxCredit" contracts/storage/DegenerusGameStorage.sol && grep -n "_creditBoxProceeds" contracts/modules/DegenerusGamePayoutUtils.sol && timeout 300 forge build 2>&1 | grep -iE "error" | grep -v "warning" || echo "BUILD_OK_OR_PARTIAL"</automated>
  </verify>
  <acceptance_criteria>
    - `presaleOver` is declared immediately after `prizePoolFrozen` (slot-0 padding, not its own slot).
    - `presaleBoxEthSold` (uint96), `presaleBoxCredit` mapping, and the presale-box index→RNG-word + per-player queued-box-ETH mirrors all exist.
    - A `PRESALE_BOX_ETH_CAP = 50 ether` constant exists (distinct from the existing 200-ether `LOOTBOX_PRESALE_ETH_CAP`).
    - `_creditBoxProceeds(uint256 boxEth)` exists in PayoutUtils and bumps `claimablePool` by the full `boxEth` while crediting VAULT (80%, incl. rounding remainder) + SDGNRS (20%) so the credited amounts sum to `boxEth`.
    - The files this plan touches compile (build errors confined to dangling references owned by downstream plans are acceptable IF documented; aim for a clean build of these files).
  </acceptance_criteria>
  <done>New presale storage + cap constant + box-proceeds helper exist; the 80/20 split sums to boxEth and bumps the pool by boxEth; slot-0 packing honored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| sStonk ABI ↔ Game | `uint8(Pool.X)` ordinals cross the delegatecall/external boundary; a reorder would silently misroute pool balances. |
| storage layout | redeploy-fresh pre-launch; slot packing changes are intentional (no migration). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-322-01-01 | Tampering | Pool enum rename | mitigate | Keep `PresaleBox` at ordinal 4 (last); verify `uint8(Pool.PresaleBox)` == old `uint8(Pool.Earlybird)` == 4. Rename concrete + interface in lockstep. |
| T-322-01-02 | Information Disclosure / Integrity | `_creditBoxProceeds` | mitigate | Assert credited amounts sum to `boxEth` and `claimablePool += boxEth` exactly (R3 invariant) — no silent rounding leak; remainder to VAULT side. |
| T-322-01-03 | Denial of Service | dangling earlybird symbol | accept | Build-breaking dangling references are caught by `forge build`; this plan defers the `_awardEarlybirdDgnrs` body + call-site removal to 322-02/06 to avoid a half-removed subsystem. No runtime exposure (pre-launch, uncommitted). |
| T-322-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. (No `## Package Legitimacy Audit` needed.) |
</threat_model>

<verification>
- `forge build` compiles the touched files (any residual error must be a dangling earlybird reference explicitly owned by a downstream plan, documented in the SUMMARY).
- `grep -rn "Earlybird" contracts/StakedDegenerusStonk.sol contracts/interfaces/IStakedDegenerusStonk.sol` returns nothing.
- The slot-0 byte tally still holds (presaleOver in padding; 1 free byte remains).
- No git commit performed (batched-diff discipline).
</verification>

<success_criteria>
- `Pool.PresaleBox` replaces `Pool.Earlybird` at ordinal 4 in both enums; 10% bps allocation preserved.
- Earlybird `_finalizeEarlybird` + trigger removed; remaining earlybird symbols removed-or-deferred-with-reason.
- New presale storage (presaleOver, presaleBoxEthSold, presaleBoxCredit, box index/word + queued-ETH mirrors) + `PRESALE_BOX_ETH_CAP = 50 ether`.
- `_creditBoxProceeds` 80/20 helper present and invariant-preserving.
</success_criteria>

<output>
Create `.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-01-SUMMARY.md` when done. Record exactly which earlybird symbols were removed here vs deferred to 322-02/322-06, and the final slot-0 byte tally. Do NOT git commit.
</output>
