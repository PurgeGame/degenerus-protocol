---
phase: 269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning
phase_number: 269
plan: 269-01
plan_id: 269-01
plan_number: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-01-PLAN.md
  - contracts/modules/DegenerusGameLootboxModule.sol
  - test/gas/LootboxOpenGas.test.js
  - test/gas/AdvanceGameGas.test.js
  - test/gas/Phase264GasRegression.test.js
  - test/stat/SurfaceRegression.test.js
  - package.json
  - .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-01-SUMMARY.md
  - .planning/STATE.md
autonomous: false
milestone: v37.0
milestone_name: Degenerette Recalibration + Maintenance Bundle
phase_status: open

write_policy: "Mixed-maintenance phase per CONTEXT.md. 1 batched USER-APPROVED contract commit (Task 3) + 1 batched USER-APPROVED test commit (Task 5) + AGENT-COMMITTED RCA/SUMMARY commits (Task 1 + Task 6). Both USER-APPROVED gate tasks are `checkpoint:human-verify` non-autonomous per `feedback_no_contract_commits.md` (test-tree treated identically to contract-tree per Phase 268 D-268-APPROVAL-02 carry) + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_wait_for_approval.md`. Per `feedback_never_preapprove_contracts.md`, the orchestrator/agent does NOT pre-approve any commit. Per `feedback_manual_review_before_push.md`, the agent does NOT `git push`. Tasks 1, 2, 4, 6 are AGENT-COMMITTED autonomous (planning artifact + edits-that-precede-approval-gate). Tasks 2 and 4 produce the diff that gates 3 and 5 — they MUST be implemented (file edits) before the approval gate is presented; the executor MUST NOT skip the approval gates."

requirements:
  - LBX-01
  - LBX-02
  - LBX-03
  - GASPIN-01
  - GASPIN-02
  - GASPIN-03

# Audit anchors (per CONTEXT.md <domain> + REQUIREMENTS.md baseline)
baseline: 1c0f09132d7439af9881c56fe197f81757f8164a
baseline_short: 1c0f0913
baseline_signal: MILESTONE_V36_AT_HEAD_1c0f09132d7439af9881c56fe197f81757f8164a
phase_267_close_sha: e1136071
phase_268_close_sha: 4b277aaf

# Wave rationale (per CONTEXT.md <plan_shape_locked_by_context>)
wave_rationale: "Single wave (wave: 1). All ~6 tasks are dependency-serialized through user-approval gates. Two reasons no parallel wave structure: (a) the test commit at Task 5 depends on BOTH the GASPIN-01 RCA outcome from Task 1 (for GASPIN-02 stabilization diff shape per D-269-STAB-01) AND the post-LBX-01 deletion HEAD sha from Task 3 (for SURF-03 re-baseline anchor per D-269-SURF03-01); (b) the user-approval gates at Task 3 and Task 5 serialize execution by their semantics (the user must approve before the next gate's diff can be authored). Task 4 (test edits) can begin in parallel with Task 2 (contract edit) but cannot commit until Task 3 lands; the executor's wall-clock benefit is negligible and not worth the dependency-tracking complexity. Single-wave assignment matches Phase 267 / Phase 268 single-multi-task precedent."

# Locked decisions consumed by this plan (per 269-CONTEXT.md <decisions>)
locked_decisions:
  - id: D-269-LBX-SHAPE-01
    summary: "Pure deletion of L1574-1577 dead inner branch. Replace L1573-1580 surviving guard body with `ticketsOut = ticketsScaled;` only. NO `require(targetLevel >= currentLevel)` invariant assert (per `feedback_no_dead_guards.md`); NO NatSpec contract on caller's responsibility; NO trace comment at the deletion site (per `feedback_no_history_in_comments.md`; caller-clamp invariant self-evident from local `_resolveLootboxCommon` L860-884 reading). Maximum gas savings (~50g/open + bytecode shrink). Net diff -5 / +1 lines. Single hunk in `contracts/modules/DegenerusGameLootboxModule.sol` `_resolveLootboxRoll`."
  - id: D-269-LBX-CALLERCLAMP-CARRY-01
    summary: "Triple-defense caller-clamp invariant proves byte-equivalence at runtime. Layer 1: `openLootBox` L557-559 outer clamp. Layer 2: `_resolveLootboxCommon` L882-884 inner clamp. Layer 3: deleted L1574-1577 dead branch. Layers 1+2 unconditionally execute before layer 3 is reachable; both came in at the initial commit `aafb7e0d` (git pickaxe traced during 269-CONTEXT discussion). LBX-01 deletion removes layer 3 only; layers 1+2 untouched → byte-equivalent player-facing behavior at any timing for any open path (ETH lootbox, BURNIE lootbox, direct resolve, redemption resolve)."
  - id: D-269-LBX-GAMETHEORY-CARRY-01
    summary: "Delay confers no advantage; only loses 10% far-future bucket-spread variety. `seed = keccak256(rngWord, player, day, amount)` is fixed for ETH lootboxes since `day = lootboxDay[index][player]` is snapshotted at buy by `MintModule.sol` L1409-1410. Player who holds past grace-period gets `baseLevel = purchaseLevel`, most rolls land below currentLevel → triple-defense clamp pulls `targetLevel` up to currentLevel → tickets queue at currentLevel (immediate next cycle). NO tickets lost; NO past-bucket queueing; NO timing-pick advantage. Validates the pure-deletion choice."
  - id: D-269-PLAN-01
    summary: "Single `269-01-PLAN.md` (this file) with multi-task structure: GASPIN-01 RCA inline (Task 1) → LBX-01 contract edit (Task 2) → LBX-01 USER-APPROVED contract commit gate (Task 3) → LBX-02 + GASPIN-02 + SURF-03 + optional package.json test edits (Task 4) → test USER-APPROVED batched commit gate (Task 5) → phase-close SUMMARY + STATE.md flip (Task 6). 4 atomic-commit boundaries: (a) AGENT-COMMITTED RCA-inline + plan revision commit (Task 1 may or may not produce a standalone commit — see Task 1 acceptance); (b) USER-APPROVED contract commit at Task 3; (c) USER-APPROVED test commit at Task 5; (d) AGENT-COMMITTED phase-close docs commit at Task 6. Mirrors Phase 267/268 single-multi-task atomic-commit-per-task precedent."
  - id: D-269-COMMITS-01
    summary: "Single batched USER-APPROVED test commit (Task 5) covering: (a) LBX-02 `test/gas/LootboxOpenGas.test.js` v37.0 describe extension with worst-case derivation NatSpec header per D-269-WORSTGAS-01; (b) GASPIN-02 stabilization diff (re-pin / ordering-fix / split-files, chosen post-Task-1 RCA per D-269-STAB-01); (c) SURF-03 re-baseline in `test/stat/SurfaceRegression.test.js` from `1c0f09132d7439af9881c56fe197f81757f8164a` to Phase-269-close HEAD per D-269-SURF03-01; (d) `package.json` test:stat wiring delta ONLY if Task 1 RCA chose split-files (D-269-STAB-01 option c). One diff, one approval, one commit. Mirrors Phase 268 `4b277aaf` batching pattern."
  - id: D-269-RCA-01
    summary: "Bisect-by-removal first, escalate to instrumentation if inconclusive. Task 1 binary-partition the 14-file `test:stat` ordering (~log₂(14) ≈ 4 rounds) to isolate the polluting predecessor of the SURF-05 gas pins. If single-file pollution: confirm cause (state pollution / fixture cache / snapshot ordering / hardhat node restart timing); pick GASPIN-02 stabilization path per D-269-STAB-01. If multi-file cumulative pollution: escalate to instrumented hardhat-snapshot diff or per-test gas-meter trace inspection. Deliverable: short root-cause section IN `269-01-PLAN.md` (this file, appended by Task 1) + inline NatSpec at the affected test fixture (Task 4). NO separate `269-NN-GASPIN-RCA.md` working file (Phase 270 owns the only working-file appendix in v37.0; Phase 269 stays plan-internal)."
  - id: D-269-STAB-01
    summary: "GASPIN-02 stabilization preference NOT pre-locked. Task 1 RCA outcome dictates; Task 4 implements least-invasive among: (a) re-pin gas snapshots to combined-suite-stable values + document offset rationale inline (least-invasive when pollution mechanism is structural — known fixture-loader cache behavior, cannot be eliminated cheaply); (b) ordering-fix via `before(() => vm.reset())` injection or test-file isolation (least-invasive when RCA reveals a one-line surgical fix — specific fixture re-deployment between describes); (c) split affected describes across separate test files (chosen only if (a) and (b) prove infeasible — pollution is hardhat-node-restart-timing; isolation requires file-boundary, and forces `package.json` test:stat wiring delta). Decision documented inline at the test fixture per `feedback_no_history_in_comments.md` (describe what IS, not what changed). Test commit lands per D-269-COMMITS-01."
  - id: D-269-SURF03-01
    summary: "Re-baseline SURF-03 anchor in `test/stat/SurfaceRegression.test.js` from `1c0f09132d7439af9881c56fe197f81757f8164a` (current v37.0 SURF-01..04 describe at L603 const V36_BASELINE + L752 SURF-03 it block) to Phase-269-close HEAD. Pure deletion of L1574-1577 lines + hoist of `ticketsOut = ticketsScaled;` is a small coherent hunk (~5-line diff in one function). Cleaner than allowed-hunk exception (which would require maintaining a hunk-allowlist that future phases would grow). Update lands in the same batched test commit per D-269-COMMITS-01. Phase 268 D-268-SURF03-01 carried this update to Phase 269."
  - id: D-269-WORSTGAS-01
    summary: "LBX-02 theoretical worst-case 55%-tickets-path lootbox-open derived FIRST in `test/gas/LootboxOpenGas.test.js` v37.0 describe NatSpec header BEFORE measurement (per `feedback_gas_worst_case.md`). Worst-case dimensions: (a) `roll < 11` branch (55%-tickets-path, the path containing the deleted dead inner check); (b) `ticketsScaled != 0` inner guard passes; ticketsOut assignment executes (always-taken pre-deletion AND unconditional post-deletion); (c) maximally cold storage state — fresh fixture; no pre-warmed slots; (d) tickets-path consumption full — `_lootboxTicketCount` returns max ticketsScaled (within bounds) to exercise the full BURNIE-arithmetic-removal benefit on the cold-storage branch. Test construction: deterministic seed engineered so `uint16(seed >> 40) % 20 < 11` (tickets-path) AND `_lootboxTicketCount(...) > 0`. Assertion: per-open gas delta vs v36.0 baseline ∈ [30, 80] gas (ROADMAP-locked envelope); bytecode shrink confirmed via deployment-byte-count delta. NO trace comment at the deletion site per `feedback_no_history_in_comments.md`."
  - id: D-269-APPROVAL-02
    summary: "Contract + test commits USER-APPROVED batched per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md`. Phase 269 has 1 batched contract commit (Task 3 gate) AND 1 batched test commit (Task 5 gate); agent presents diff and waits for explicit user `approved` before each. Per `feedback_manual_review_before_push.md`, agent does NOT `git push`."
  - id: D-269-CLOSURE-02
    summary: "Phase 269 SUMMARY (Task 6) contributes both §9.NN.i USER-APPROVED contracts row (LBX-01 from Task 3) AND §9.NN.ii USER-APPROVED tests row (LBX-02 + GASPIN-02 + SURF-03 + optional package.json from Task 5); §9.NN.iii AGENT-COMMITTED planning artifacts row (PLAN authoring + Task 1 RCA-inline revision + Task 6 SUMMARY/STATE). NO §9.NN.iv awaiting-approval subsection at phase close."
  - id: D-269-DISCRETION-CHOICES
    summary: "Planner-locked Claude's-Discretion picks per CONTEXT.md <decisions> Claude's-Discretion section: (a) bisect ordering = binary partition first (fewer rounds: 4 vs ~7 for reverse-order traversal); (b) GASPIN-02 stabilization choice = locked only post-RCA per D-269-STAB-01 — Task 1 deliverable records chosen path with rationale appended inline at the bottom of this PLAN.md; (c) package.json test:stat wiring = only changes if Task 1 RCA chooses split-files (D-269-STAB-01 option c); (d) LBX-02 test-file shape = add a new top-level `describe(\"Phase 269 LBX-02 — ...\")` block at end-of-file in `test/gas/LootboxOpenGas.test.js` (after the existing Phase 266 describes at L180/L227/L290; matches per-phase describe organization precedent over modifying an existing describe); (e) worst-case seed engineering = inline-precomputed deterministic seed within the v37.0 describe (no helper extraction — file does not yet have a deterministic-seed helper, and inline is shorter than introducing one); (f) atomic-commit count = 4 (Task 1 RCA-inline plan revision = AGENT-COMMITTED docs commit; Task 3 contract = 1 USER-APPROVED commit; Task 5 test = 1 USER-APPROVED commit; Task 6 SUMMARY = 1 AGENT-COMMITTED docs commit). Plus a 5th implicit commit if the gsd-plan-checker revises this plan post-author."

# Goal-backward must-haves
must_haves:
  truths:
    - "Phase 269 goal: 1 batched USER-APPROVED contract-tree commit (LBX-01 dead-branch deletion) + 1 batched USER-APPROVED test-tree commit (LBX-02 LootboxOpenGas extension + GASPIN-02 stabilization + SURF-03 re-baseline + optional package.json wiring), with the GASPIN-01 root-cause section appended inline to this PLAN.md before either commit lands. 6 of 6 Phase 269 requirements (LBX-01..03 + GASPIN-01..03) flip to PASS at Phase 269 close."
    - "LBX-01 contract commit is USER-APPROVED (per `feedback_no_contract_commits.md` + D-269-APPROVAL-02): Task 2 implements the single-hunk deletion at `contracts/modules/DegenerusGameLootboxModule.sol` `_resolveLootboxRoll` L1573-1580 (replaces the 8-line `if (ticketsScaled != 0) { if (targetLevel < currentLevel) { burnieOut = ...; } else { ticketsOut = ticketsScaled; } }` body with `if (ticketsScaled != 0) { ticketsOut = ticketsScaled; }` per D-269-LBX-SHAPE-01 paste-ready diff). Task 3 BLOCKING approval gate presents the diff via `git diff contracts/`, waits for the user's explicit `approved` token, THEN commits with subject `feat(269): delete unreachable BURNIE-conversion branch in _resolveLootboxRoll [LBX-01]`. No `git push` per `feedback_manual_review_before_push.md`."
    - "LBX-02 + GASPIN-02 + SURF-03 test commit is USER-APPROVED single batched commit (per `feedback_batch_contract_approval.md` + D-269-COMMITS-01): Task 4 implements all four test-tree deltas together (LBX-02 v37.0 describe in `test/gas/LootboxOpenGas.test.js` with worst-case NatSpec per D-269-WORSTGAS-01; GASPIN-02 stabilization diff per D-269-STAB-01 Task-1-RCA-chosen path; SURF-03 baseline-sha re-anchor in `test/stat/SurfaceRegression.test.js`; optional `package.json` test:stat wiring if GASPIN-02 chose split-files). Task 5 BLOCKING approval gate presents the diff, waits for `approved`, THEN commits with subject `test(269): lootbox-open gas savings + SURF-05 gas-pin stabilization + SURF-03 re-baseline [LBX-02, GASPIN-01..03, SURF-03]`. No `git push`."
    - "ZERO behavioral change at LBX (caller-clamp invariant L557 + L882 proves byte-equivalence; D-269-LBX-CALLERCLAMP-CARRY-01): the triple-defense pattern (Layer 1 `openLootBox` L557-559 + Layer 2 `_resolveLootboxCommon` L882-884 + Layer 3 deleted L1574-1577) unconditionally clamps `targetLevel = max(targetLevel, currentLevel)` before `_resolveLootboxRoll` is reached. Player-facing behavior at any timing on any open path (ETH lootbox, BURNIE lootbox, direct resolve, redemption resolve) is byte-equivalent; only loses 10% far-future bucket-spread variety (D-269-LBX-GAMETHEORY-CARRY-01) — no tickets and no advantage."
    - "NO `git push` by agent — user reviews diff manually before push (per `feedback_manual_review_before_push.md`). Both USER-APPROVED commits (Task 3 contract + Task 5 test) and both AGENT-COMMITTED commits (Task 1 plan revision + Task 6 SUMMARY) land locally only; pre-push human review reserved for the user."
    - "GASPIN-01 RCA documented inline in this `269-01-PLAN.md` (per D-269-RCA-01; no separate working file): Task 1 appends a `## Root Cause (GASPIN-01)` section at the bottom of this plan naming (a) which file holds the actually-drifting pin (`test/gas/Phase264GasRegression.test.js` SURF-05 pins at L376/L428 — currently in `test:stat` ordering — OR `test/gas/AdvanceGameGas.test.js` Phase 264 SURF-05 describe at L1464+ — currently OUTSIDE `test:stat` ordering; first confirm-via-bisect that the drift is actually triggered by the stat-suite ordering); (b) the polluting predecessor file identified via binary-partition bisect across the 14-file `test:stat` list per D-269-DISCRETION-CHOICES; (c) the mechanism from {state pollution, gas-meter snapshot ordering, fixture-loader caching, hardhat node restart timing}; (d) the chosen GASPIN-02 stabilization path per D-269-STAB-01 with rationale on least-invasive choice; (e) escalation note if bisect was inconclusive."
    - "GASPIN-02 stabilization is the least-invasive option (per D-269-STAB-01): Task 4 implements (a) re-pin gas snapshots to combined-suite-stable values + inline NatSpec at the affected test fixture documenting current stabilization shape (per `feedback_no_history_in_comments.md` — describe what IS, not what changed), OR (b) ordering-fix via `before(() => vm.reset())` injection or test-file isolation, OR (c) split affected describes across separate test files (forces `package.json` test:stat wiring delta). Task 1 RCA-inline section names the chosen path."
    - "GASPIN-03 acceptance evidence: `npm run test:stat` exits 0 across 3 consecutive fresh-checkout runs; both standalone (`npx hardhat test test/gas/AdvanceGameGas.test.js -g \"Phase 264 SURF-05\"` AND `npx hardhat test test/gas/Phase264GasRegression.test.js`) and combined-suite (`npm run test:stat`) gas pins agree at the new pinned values; zero flaky failures. Task 5 acceptance-criteria block enforces this before the approval gate is presented."
    - "SURF-03 re-baseline (per D-269-SURF03-01): Task 4 updates `test/stat/SurfaceRegression.test.js` L603 `const V36_BASELINE = \"1c0f09132d7439af9881c56fe197f81757f8164a\"` -- the SURF-03 it block at L752 retains file-level zero-diff semantics but the assertion baseline-sha string is updated to the Phase-269-close HEAD (post-LBX-01 commit). The change is mechanical (single-string replacement), but it depends on the Task-3 LBX-01 commit having landed (so the post-deletion HEAD sha is known) — that is captured in Task 4's read_first / acceptance_criteria. SURF-01/02/04 anchors are unaffected (they remain at v36.0 `1c0f0913`)."
    - "LBX-03 cross-phase anchor recording (Phase 271 §3.A delta-surface inputs): Task 6 SUMMARY records post-deletion line numbers for the 4 hash2/bit-slice callsites in `_resolveLootboxRoll` (v36.0 line anchors: L1548 / L1569 / L1585 / L1599 per CONTEXT.md `<deferred>` Phase 271 §3.A LBX-03 audit-trail row). The author of the Phase 271 LBX-03 row does NOT need to re-read the contract — the post-deletion line numbers are captured in `269-01-SUMMARY.md` under a `## Phase 271 §3.A delta-surface inputs` section. This is how LBX-03 is materially advanced in Phase 269 per CONTEXT.md cross-phase coordination."
    - "STATE.md flip at Task 6 (AGENT-COMMITTED): Current Position advances to `Phase 270 next`, last_activity bullet appended, progress.completed_phases 2 → 3, progress.completed_plans 2 → 3, progress.percent 40 → 60. NO milestone-level closure flips (those land in Phase 271 terminal phase)."
    - "Test infrastructure preservation: Phase 261/264/266/268 chi² primitives + SURF-01/02/04 baselines + EntropyLib/JackpotModule/TraitUtils file-level zero-diffs at the v36.0 `1c0f0913` baseline ALL UNTOUCHED in Phase 269. Only SURF-03 baseline-sha string moves (per D-269-SURF03-01). The test:stat ordering is preserved unless GASPIN-02 chose split-files (D-269-STAB-01 option c)."

  artifacts:
    - path: contracts/modules/DegenerusGameLootboxModule.sol
      provides: "MODIFIED at Task 2 (Task 3 USER-APPROVED batched contract commit). Single-hunk deletion in `_resolveLootboxRoll` at L1573-1580 (current HEAD). Pre-deletion 8-line body: `if (ticketsScaled != 0) { if (targetLevel < currentLevel) { burnieOut = (uint256(ticketsScaled) * PRICE_COIN_UNIT) / TICKET_SCALE; } else { ticketsOut = ticketsScaled; } }`. Post-deletion 3-line body: `if (ticketsScaled != 0) { ticketsOut = ticketsScaled; }`. Net diff: -5 lines, +1 line. NO `require(targetLevel >= currentLevel)` invariant assert; NO NatSpec contract on caller's responsibility; NO trace comment at the deletion site (per `feedback_no_history_in_comments.md` + `feedback_no_dead_guards.md` + D-269-LBX-SHAPE-01). `hardhat compile` exits 0 with no new warnings."
      contains: "ticketsOut = ticketsScaled;"
      not_contains: "if (targetLevel < currentLevel)"
      not_contains_2: "// Convert to BURNIE if target level already passed"
    - path: test/gas/LootboxOpenGas.test.js
      provides: "EXTENDED at Task 4 (Task 5 USER-APPROVED batched test commit). Add a new top-level `describe(\"Phase 269 LBX-02 — 55%-tickets-path lootbox-open gas savings + bytecode shrink at v37.0 HEAD\")` block at end-of-file (after the existing Phase 266 `resolveLootboxDirect` describe closing at L312). NatSpec header derives theoretical worst-case 55%-tickets-path open FIRST per D-269-WORSTGAS-01 + `feedback_gas_worst_case.md`. Worst-case dimensions enumerated: roll < 11 (tickets-path branch); ticketsScaled != 0 (inner guard passes); maximally cold storage (fresh fixture; no pre-warmed slots); max ticketsScaled (full BURNIE-arithmetic-removal benefit). Test construction: deterministic seed engineered so `uint16(seed >> 40) % 20 < 11` AND `_lootboxTicketCount(...) > 0`. Two `it` blocks: (1) `it(\"per-open gas delta vs v36.0 baseline ∈ [30, 80] gas at 55%-tickets-path worst case\")` — pin pre-deletion baseline gas as `PRE_DELETION_GAS_REF` constant (REF-CAPTURE protocol mirroring Phase 266 L207 + Phase 268 SURF-06 L348), measure post-deletion via REF-CAPTURE first-run print + executor-pin pattern, assert `(REF_PRE - measured_POST) ∈ [30, 80]` gas (ROADMAP-locked envelope); (2) `it(\"bytecode shrink confirmed via DegenerusGameLootboxModule deployment-byte-count delta vs v36.0 baseline\")` — measure `await artifacts.readArtifact('DegenerusGameLootboxModule').deployedBytecode.length / 2` vs pinned `V36_DEPLOYED_BYTECODE_BYTES_REF` constant; assert post-deletion bytecode strictly smaller. NO trace comment at the deletion site per `feedback_no_history_in_comments.md`."
      contains: "Phase 269 LBX-02"
      contains_alt: "55%-tickets-path"
      contains_alt_2: "Worst-case dimensions"
    - path: test/gas/AdvanceGameGas.test.js
      provides: "POTENTIALLY MODIFIED at Task 4 (Task 5 USER-APPROVED batched test commit) — only if Task 1 RCA identifies the drifting pin as the Phase 264 SURF-05 describe at L1464+ AND chooses option (a) re-pin (mutate the gas-pin literal values inline) or option (b) ordering-fix (mutate the fixture loader / before hook). If Task 1 RCA chooses option (c) split-files OR identifies a different drift site (e.g. Phase264GasRegression.test.js), this file is UNTOUCHED. Task 1 must explicitly name the file mutated (or 'none') in its RCA-inline section. Any inline NatSpec at the affected fixture documents the CURRENT stabilization shape per `feedback_no_history_in_comments.md` — NO 'was 120K, now X' style annotations."
    - path: test/gas/Phase264GasRegression.test.js
      provides: "POTENTIALLY MODIFIED at Task 4 (Task 5 USER-APPROVED batched test commit) — only if Task 1 RCA identifies the drifting pin as in this file (Phase 264 SURF-05 per-call gas deltas at L376/L428) AND chooses option (a) re-pin or option (b) ordering-fix. If Task 1 RCA chooses option (c) split-files OR identifies the drift in AdvanceGameGas.test.js instead, this file's mutation is correspondingly different (split-files spawns a new sibling file; AdvanceGameGas drift leaves this file untouched). Task 1 must explicitly name the file mutated (or 'none') in its RCA-inline section. Inline NatSpec at the affected fixture follows `feedback_no_history_in_comments.md`."
    - path: test/stat/SurfaceRegression.test.js
      provides: "MODIFIED at Task 4 (Task 5 USER-APPROVED batched test commit). SURF-03 baseline-sha string re-anchored per D-269-SURF03-01. Currently the v37.0 SURF-01..04 describe block at L609 uses `V36_BASELINE = \"1c0f09132d7439af9881c56fe197f81757f8164a\"` shared across all 4 SURF-01..04 it blocks (L742-766). The re-baseline cannot simply change the shared `V36_BASELINE` constant (that would also move SURF-01/02/04 anchors which must remain at v36.0). Instead, introduce a separate `const PHASE_269_CLOSE_BASELINE = \"<post-LBX-01-commit-sha>\"` constant immediately after `V36_BASELINE` (or use the Task-3 post-commit `HEAD~1` if SURF-03 baseline is to be the LBX-01 commit itself), and update ONLY the SURF-03 it block at L752 to pass `PHASE_269_CLOSE_BASELINE` to `expectFileLevelZeroDiffV37(...)` instead of `V36_BASELINE`. Update the SURF-03 `it` block title from `vs v36.0 baseline 1c0f0913 (D-268-SURF03-01: Phase 269 owns the post-cleanup re-baseline)` to `vs Phase-269-close baseline <sha-short> (D-269-SURF03-01: post-LBX-01-cleanup file-level zero-diff)` per `feedback_no_history_in_comments.md`. SURF-01 (L742), SURF-02 (L747), SURF-04 (L763) UNTOUCHED."
      contains: "PHASE_269_CLOSE_BASELINE"
      contains_alt: "D-269-SURF03-01"
    - path: package.json
      provides: "POTENTIALLY MODIFIED at Task 4 (Task 5 USER-APPROVED batched test commit) — only if Task 1 RCA chose split-files (D-269-STAB-01 option c). Append the new sibling-split file-path(s) to the existing space-separated `test:stat` script value per Phase 268 precedent. Current Phase 268 close ordering: `hardhat test test/gas/Phase261GasRegression.test.js test/gas/Phase264GasRegression.test.js test/gas/Phase268GasRegression.test.js test/stat/TraitDistribution.test.js test/stat/GoldSoloCoverage.test.js test/stat/SoloEvUplift.test.js test/stat/PackFeel.test.js test/stat/SurfaceRegression.test.js test/stat/PerPullLevelDistribution.test.js test/stat/PerPullEmptyBucketSkip.test.js test/stat/LootboxEntropyDistribution.test.js test/stat/DegenerettePerNEvExactness.test.js test/stat/DegeneretteProducerChi2.test.js test/stat/DegeneretteBonusEv.test.js`. If split-files not chosen, package.json UNTOUCHED. Task 1 RCA-inline section names whether package.json mutates."
    - path: .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-01-PLAN.md
      provides: "MODIFIED at Task 1 (AGENT-COMMITTED docs commit). Appended `## Root Cause (GASPIN-01)` section at end-of-file documenting (a) which file holds the actually-drifting pin (after bisect-verified-or-rejected the `test:stat` ordering hypothesis); (b) the polluting predecessor file (or 'multi-file cumulative' if escalation triggered); (c) the mechanism from {state pollution, gas-meter snapshot ordering, fixture-loader caching, hardhat node restart timing}; (d) the chosen GASPIN-02 stabilization path per D-269-STAB-01 with rationale on least-invasive choice; (e) explicit names of test files Task 4 will mutate (`test/gas/AdvanceGameGas.test.js` and/or `test/gas/Phase264GasRegression.test.js` and/or `package.json` and/or a new sibling-split file); (f) escalation note if bisect was inconclusive — what instrumented tooling was used and what it showed. Per D-269-RCA-01: NO separate `269-NN-GASPIN-RCA.md` working file; plan-internal only."
      min_lines: 30  # appended RCA section minimum
      contains: "Root Cause (GASPIN-01)"
    - path: .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-01-SUMMARY.md
      provides: "NEW at Task 6 (AGENT-COMMITTED docs commit). Phase-closure summary mirroring `.planning/phases/268-degenerette-statistical-validation-cross-surface-preservation/268-01-SUMMARY.md` format. Frontmatter: `requirements-completed: [LBX-01, LBX-02, LBX-03, GASPIN-01, GASPIN-02, GASPIN-03]` + closure_signal pointing to Phase 271. Per-task atomic-commit log (Task 1 docs(269) RCA-inline; Task 3 feat(269) LBX-01 contract; Task 5 test(269) batched test; Task 6 docs(269) summary). Per-REQ tally for 6 Phase 269 requirements (each REQ ID → file+line evidence + verifying grep recipe + test-pass evidence). Cross-phase cross-cite density section (forward to Phase 271 §4 surface (f) byte-equivalence audit + §3.A LBX-03 audit-trail row + §5 REG-01 v36.0 non-widening lootbox-bodies-byte-identical-except-for-LBX-01). Project-feedback-rules-honored table (`feedback_no_contract_commits` + `feedback_batch_contract_approval` + `feedback_never_preapprove_contracts` + `feedback_wait_for_approval` + `feedback_manual_review_before_push` + `feedback_no_history_in_comments` + `feedback_no_dead_guards` + `feedback_gas_worst_case` + `feedback_design_intent_before_deletion` carry + `feedback_rng_backward_trace` N/A explicit). THREE-subsection commit-readiness register: §i USER-APPROVED contracts (1 commit at Task 3); §ii USER-APPROVED tests (1 commit at Task 5); §iii AGENT-COMMITTED planning artifacts (3 commits: PLAN authoring + Task 1 RCA-inline revision + Task 6 SUMMARY/STATE). NO §iv awaiting-approval. Plus a new `## Phase 271 §3.A delta-surface inputs` section recording post-deletion line numbers for the 4 hash2/bit-slice callsites in `_resolveLootboxRoll` (v36.0 anchors L1548 / L1569 / L1585 / L1599; post-deletion anchors derived via `grep -n 'EntropyLib\\.hash2\\|seed >> ' contracts/modules/DegenerusGameLootboxModule.sol` at Phase-269-close HEAD)."
      min_lines: 100
      contains: "LBX-01"
      contains_alt: "GASPIN-01"
      contains_alt_2: "Phase 271 §3.A delta-surface inputs"
    - path: .planning/STATE.md
      provides: "MODIFIED at Task 6 (AGENT-COMMITTED docs commit, same commit as 269-01-SUMMARY.md per single-task atomic-commit boundary). Update Current Position (Phase 269 SHIPPED; Phase 270 next), last_activity bullet, progress.completed_phases (2 → 3), progress.completed_plans (2 → 3), progress.percent (40 → 60). NO milestone-level closure flips (those land in Phase 271 — terminal phase)."
      contains: "Phase 269 SHIPPED"

  key_links:
    - from: "contracts/modules/DegenerusGameLootboxModule.sol _resolveLootboxRoll L1573-1580 (Task 2 deletion site)"
      to: "contracts/modules/DegenerusGameLootboxModule.sol _resolveLootboxCommon L882-884 (layer-2 caller clamp) + openLootBox L557-559 (layer-1 caller clamp)"
      via: "Triple-defense caller-clamp invariant (D-269-LBX-CALLERCLAMP-CARRY-01); _resolveLootboxRoll is `private` (grep recipe verifies); only 2 call sites at _resolveLootboxCommon L910 + L938; both reach via clamped targetLevel"
      pattern: "if \\(targetLevel < currentLevel\\)"
    - from: "test/gas/LootboxOpenGas.test.js Phase 269 LBX-02 describe (Task 4)"
      to: "contracts/modules/DegenerusGameLootboxModule.sol _resolveLootboxRoll post-deletion body (Task 2/3 commit) + _lootboxTicketCount L1639 (returns ticketsScaled) + LOOTBOX_TICKET_ROLL_BPS L250 (16_100 = 161%) + TICKET_SCALE / PRICE_COIN_UNIT (BURNIE-arithmetic-removal-benefit consumers)"
      via: "Deterministic seed engineered so `uint16(seed >> 40) % 20 < 11` (tickets-path roll) AND `_lootboxTicketCount(...)` returns nonzero; REF-CAPTURE pre/post pair (pre-deletion baseline pinned as PRE_DELETION_GAS_REF; post-deletion measured via REF-CAPTURE first-run print + executor-pin)"
      pattern: "PRE_DELETION_GAS_REF|seed >> 40|55%-tickets-path"
    - from: "test/stat/SurfaceRegression.test.js v37.0 SURF-03 it block L752 (Task 4)"
      to: "contracts/modules/DegenerusGameLootboxModule.sol post-Task-3 HEAD (Phase-269-close baseline anchor)"
      via: "Re-baseline anchor: introduce PHASE_269_CLOSE_BASELINE const post-V36_BASELINE; update SURF-03 expectFileLevelZeroDiffV37(PHASE_269_CLOSE_BASELINE, LOOTBOX_MODULE_PATH); leave SURF-01/02/04 anchored at V36_BASELINE per D-269-SURF03-01"
      pattern: "PHASE_269_CLOSE_BASELINE.*=.*\"[0-9a-f]+\""
    - from: "Task 1 RCA-inline (this PLAN.md `## Root Cause (GASPIN-01)` section)"
      to: "test/gas/{AdvanceGameGas|Phase264GasRegression}.test.js SURF-05 fixture(s) AND/OR package.json test:stat ordering"
      via: "Binary-partition bisect across 14-file `test:stat` ordering (~log₂(14) ≈ 4 rounds per D-269-RCA-01 + D-269-DISCRETION-CHOICES bisect-ordering choice); chosen stabilization path per D-269-STAB-01 documented inline at the affected test fixture (per `feedback_no_history_in_comments.md`)"
      pattern: "Root Cause \\(GASPIN-01\\)"
    - from: "Task 5 USER-APPROVED test commit (per D-269-COMMITS-01 single batched)"
      to: "All 4 test-tree deltas: LBX-02 + GASPIN-02 + SURF-03 + (optional) package.json wiring"
      via: "One diff via `git diff` over `test/` + `package.json`; one explicit `approved` token from user; one commit with subject `test(269): lootbox-open gas savings + SURF-05 gas-pin stabilization + SURF-03 re-baseline [LBX-02, GASPIN-01..03, SURF-03]`"
      pattern: "test\\(269\\):.*\\[LBX-02.*GASPIN.*SURF-03\\]"
    - from: ".planning/phases/269-…/269-01-SUMMARY.md `## Phase 271 §3.A delta-surface inputs` section (Task 6)"
      to: "audit/FINDINGS-v37.0.md §3.A delta-surface row authored at Phase 271 (LBX-03 cross-phase coordination)"
      via: "Post-deletion line numbers for 4 hash2/bit-slice callsites in `_resolveLootboxRoll` captured at Phase 269 close so Phase 271 author does NOT re-read contracts/; v36.0 anchors L1548/L1569/L1585/L1599 are the input; post-deletion anchors are derived via `grep -n 'EntropyLib.hash2\\|seed >> ' contracts/modules/DegenerusGameLootboxModule.sol`"
      pattern: "Phase 271 §3\\.A delta-surface inputs"

# Canonical grep recipes (executors copy verbatim — do NOT re-derive)
canonical_grep_recipes:
  task1_rca_inline:
    - 'grep -c "## Root Cause (GASPIN-01)" .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-01-PLAN.md  # Expected: 1 (RCA section header present).'
    - 'grep -cE "state pollution|gas-meter snapshot|fixture-loader cach|hardhat node restart" .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-01-PLAN.md  # Expected: >= 1 (mechanism named from the 4-option set).'
    - 'grep -cE "stabilization path|stabilization choice|chosen.*(re-pin|ordering-fix|split-files)" .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-01-PLAN.md  # Expected: >= 1 (D-269-STAB-01 choice documented).'
  task2_contract_edit:
    - 'grep -c "if (targetLevel < currentLevel)" contracts/modules/DegenerusGameLootboxModule.sol  # Expected: 2 (down from 3 — layer-1 at L557 + layer-2 at L882 remain; layer-3 deleted).'
    - 'grep -c "Convert to BURNIE if target level already passed" contracts/modules/DegenerusGameLootboxModule.sol  # Expected: 0 (dead-branch comment removed).'
    - 'grep -c "burnieOut = (uint256(ticketsScaled) \\* PRICE_COIN_UNIT) / TICKET_SCALE" contracts/modules/DegenerusGameLootboxModule.sol  # Expected: 0 (dead-branch BURNIE-conversion assignment removed).'
    - 'grep -cE "^[[:space:]]*ticketsOut = ticketsScaled;[[:space:]]*$" contracts/modules/DegenerusGameLootboxModule.sol  # Expected: 1 (single unconditional assignment inside the surviving ticketsScaled != 0 guard).'
    - 'grep -rn "_resolveLootboxRoll" contracts/ | grep -v "^Binary file"  # Expected: 3 lines (definition at L1548 ± shift + 2 callsites at _resolveLootboxCommon L910 + L938 ± shift).'
    - 'npx hardhat compile  # Expected: exit 0; no new warnings (compare against pre-edit baseline if hardhat is chatty on this file).'
  task3_contract_approval_gate:
    - 'git log -1 --format="%s"  # Expected: starts with "feat(269): delete unreachable BURNIE-conversion branch in _resolveLootboxRoll [LBX-01]".'
    - 'git diff HEAD~1 HEAD -- contracts/modules/DegenerusGameLootboxModule.sol | grep -cE "^-.*if \\(targetLevel < currentLevel\\) \\{$"  # Expected: 1 (the deletion is at this hunk).'
    - 'git diff HEAD~1 HEAD --stat | grep -c "DegenerusGameLootboxModule.sol"  # Expected: 1 (only contract file changed).'
    - 'git log -1 --format="%H"  # captures the Phase-269-close baseline sha for Task 4 SURF-03 re-baseline anchor.'
  task4_test_edits:
    # LBX-02
    - 'test -f test/gas/LootboxOpenGas.test.js  # Expected: file present (Phase 266 introduced it).'
    - 'grep -c "Phase 269 LBX-02" test/gas/LootboxOpenGas.test.js  # Expected: >= 1 (new describe block present).'
    - 'grep -cE "55%-tickets-path|Worst-case dimensions" test/gas/LootboxOpenGas.test.js  # Expected: >= 1 (worst-case derivation NatSpec header per D-269-WORSTGAS-01).'
    - 'grep -cE "\\[30, *80\\]|\\bbetween 30 and 80\\b|>= *30.*<= *80" test/gas/LootboxOpenGas.test.js  # Expected: >= 1 (ROADMAP-locked envelope assertion).'
    - 'grep -cE "PRE_DELETION_GAS_REF|REF-CAPTURE" test/gas/LootboxOpenGas.test.js  # Expected: >= 1 (REF-CAPTURE pin protocol present).'
    - 'grep -cE "deployedBytecode|bytecode shrink|V36_DEPLOYED_BYTECODE_BYTES_REF" test/gas/LootboxOpenGas.test.js  # Expected: >= 1 (bytecode shrink assertion).'
    # GASPIN-02 — exact form depends on Task 1 RCA path
    - 'grep -rcE "GASPIN-02|D-269-STAB-01" test/ package.json  # Expected: >= 1 (stabilization decision documented at the affected fixture or at the test:stat config).'
    # SURF-03 re-baseline
    - 'grep -c "PHASE_269_CLOSE_BASELINE" test/stat/SurfaceRegression.test.js  # Expected: >= 1 (re-baseline constant introduced).'
    - 'grep -c "D-269-SURF03-01" test/stat/SurfaceRegression.test.js  # Expected: >= 1 (decision-ID cross-cite present at the SURF-03 it block).'
    - 'grep -cE "v36\\.0 baseline 1c0f0913.*\\(D-268-SURF03-01" test/stat/SurfaceRegression.test.js  # Expected: 0 (old SURF-03 title with v36.0 anchor removed).'
    # SURF-01/02/04 still anchored at V36_BASELINE
    - 'grep -cE "V36_BASELINE.*TRAIT_UTILS_PATH|V36_BASELINE.*JACKPOT_MODULE_PATH|V36_BASELINE.*ENTROPY_LIB_PATH" test/stat/SurfaceRegression.test.js  # Expected: >= 3 (SURF-01, SURF-02, SURF-04 untouched).'
    # Test executes cleanly
    - 'npx hardhat test test/gas/LootboxOpenGas.test.js  # Expected: exit 0; LBX-02 describe passes (REF-CAPTURE first-run prints if PRE_DELETION_GAS_REF still 0; otherwise asserts envelope).'
    - 'npx hardhat test test/stat/SurfaceRegression.test.js  # Expected: exit 0; SURF-01..04 v37.0 describe passes at the new mixed baselines.'
    - 'for i in 1 2 3; do echo "Run $i"; npm run test:stat; done  # Expected: all 3 runs exit 0 with no flaky failures (GASPIN-03 acceptance).'
  task5_test_approval_gate:
    - 'git log -1 --format="%s"  # Expected: starts with "test(269): lootbox-open gas savings + SURF-05 gas-pin stabilization + SURF-03 re-baseline [LBX-02, GASPIN-01..03, SURF-03]".'
    - 'git diff HEAD~1 HEAD --stat | grep -cE "test/gas/LootboxOpenGas\\.test\\.js|test/stat/SurfaceRegression\\.test\\.js|test/gas/(AdvanceGameGas|Phase264GasRegression)\\.test\\.js|package\\.json"  # Expected: >= 2 (at minimum LBX-02 + SURF-03; plus GASPIN-02 file + optional package.json depending on D-269-STAB-01 choice).'
    - 'git diff HEAD~1 HEAD --stat | grep -c "contracts/"  # Expected: 0 (test commit must NOT touch contracts/).'
  task6_phase_close:
    - 'test -f .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-01-SUMMARY.md  # Expected: file present.'
    - 'grep -cE "LBX-0[1-3]" .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-01-SUMMARY.md  # Expected: >= 3 (all LBX IDs cited in per-REQ tally).'
    - 'grep -cE "GASPIN-0[1-3]" .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-01-SUMMARY.md  # Expected: >= 3 (all GASPIN IDs cited).'
    - 'grep -c "6 of 6" .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-01-SUMMARY.md  # Expected: >= 1 (PASS count documented).'
    - 'grep -c "Phase 271 §3.A delta-surface inputs" .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-01-SUMMARY.md  # Expected: 1 (LBX-03 cross-phase anchor recorded).'
    - 'grep -cE "L1548|L1569|L1585|L1599" .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-01-SUMMARY.md  # Expected: >= 4 (v36.0 hash2/bit-slice callsite anchors enumerated).'
    - 'grep -c "Phase 269 SHIPPED" .planning/STATE.md  # Expected: 1.'
    - 'grep -c "completed_phases: 3" .planning/STATE.md  # Expected: 1.'
    - 'grep -c "completed_plans: 3"  .planning/STATE.md  # Expected: 1.'
    - 'grep -c "percent: 60"         .planning/STATE.md  # Expected: 1.'
---

<objective>
Phase 269 ships two orthogonal maintenance workstreams under one phase boundary:

1. **LBX-01..03 — Dead-branch cleanup.** Delete the structurally-dead inner branch in
   `contracts/modules/DegenerusGameLootboxModule.sol` `_resolveLootboxRoll` at L1573-1580
   (replacing 8 lines with 3 per the D-269-LBX-SHAPE-01 paste-ready diff in
   CONTEXT.md `<specifics>`). The deletion is byte-equivalent at runtime because the
   triple-defense caller-clamp invariant (Layer 1 `openLootBox` L557-559 + Layer 2
   `_resolveLootboxCommon` L882-884 + deleted Layer 3) unconditionally clamps
   `targetLevel = max(targetLevel, currentLevel)` before `_resolveLootboxRoll` is
   reachable. ~50g/open savings + bytecode shrink. Single batched USER-APPROVED
   contract commit.

2. **GASPIN-01..03 — SURF-05 gas-pin drift.** Root-cause + stabilize the Phase
   261/264 SURF-05 ~120K gas-pin drift observed at v36.0 Phase 266 close under
   `npm run test:stat` ordering vs standalone-stable. Binary-partition bisect across
   the 14-file `test:stat` ordering identifies the polluting predecessor; chosen
   stabilization path (re-pin / ordering-fix / split-files) is the least-invasive
   option per D-269-STAB-01 RCA-driven choice.

Both workstreams ship under one single batched USER-APPROVED test commit per
D-269-COMMITS-01 (covering LBX-02 LootboxOpenGas extension + GASPIN-02 stabilization
diff + SURF-03 re-baseline + optional `package.json` wiring). Plus the Task 1 GASPIN
RCA inline-documented at the bottom of this plan (AGENT-COMMITTED docs revision).
Plus the Task 6 phase-close SUMMARY + STATE.md flip (AGENT-COMMITTED docs commit).

Total atomic-commit boundaries:
- 1 AGENT-COMMITTED docs(269) Task 1 RCA-inline plan revision
- 1 USER-APPROVED feat(269) Task 3 contract commit (LBX-01)
- 1 USER-APPROVED test(269) Task 5 batched test commit (LBX-02 + GASPIN-02 + SURF-03)
- 1 AGENT-COMMITTED docs(269) Task 6 phase-close SUMMARY + STATE.md flip

Plus optional gsd-plan-checker revision iterations on this plan and a possible
follow-up tracking-tree mutation commit from post-close SDK calls (mirrors Phase
268 precedent).

Purpose: clear two long-deferred maintenance items (lootbox cleanup carry from
Phase 266 + SURF-05 drift carry from Phase 264) before the Phase 271 terminal
audit, so the v37.0 audit-trail-row authoring at Phase 271 §3.A LBX-03 has clean
post-deletion line-number anchors and the SURF-05 invariant doesn't carry a "flaky
under combined-suite" footnote into the FINDINGS-v37.0.md deliverable.

Output:
- 1 modified contract file (`contracts/modules/DegenerusGameLootboxModule.sol`,
  -5 / +1 LOC).
- 1-4 modified test files depending on Task-1 RCA path:
  - `test/gas/LootboxOpenGas.test.js` (LBX-02 v37.0 describe) — ALWAYS modified.
  - `test/stat/SurfaceRegression.test.js` (SURF-03 re-baseline) — ALWAYS modified.
  - `test/gas/AdvanceGameGas.test.js` and/or `test/gas/Phase264GasRegression.test.js`
    (GASPIN-02 stabilization) — POTENTIALLY modified depending on RCA outcome.
  - `package.json` (test:stat wiring) — POTENTIALLY modified ONLY if RCA chose
    split-files.
- 1 modified plan artifact (this file, GASPIN-01 RCA-inline section appended at
  bottom).
- 1 new phase-summary artifact (`269-01-SUMMARY.md`).
- 1 modified state artifact (`.planning/STATE.md`).

ZERO new storage slots; zero new public/external mutation entry points; zero new
admin functions; zero new modifiers (per CONTEXT.md `<domain>` Phase 269 boundary
state at close).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-CONTEXT.md
@.planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-DISCUSSION-LOG.md
@.planning/phases/268-degenerette-statistical-validation-cross-surface-preservation/268-01-PLAN.md
@.planning/phases/268-degenerette-statistical-validation-cross-surface-preservation/268-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Key contract surfaces the executor needs. Extracted from contracts/ verbatim. -->
<!-- Executor uses these directly — no codebase exploration needed for the LBX-01 -->
<!-- deletion or for the LBX-02 worst-case construction. -->

From contracts/modules/DegenerusGameLootboxModule.sol L548-559 (Layer-1 caller clamp at openLootBox):
```solidity
        uint24 currentLevel = level + 1;
        bool withinGracePeriod = currentDay <= day + 7;
        uint24 baseLevelPacked = lootboxBaseLevelPacked[index][player];
        uint24 graceLevel = baseLevelPacked == 0 ? currentLevel : baseLevelPacked - 1;
        uint24 baseLevel = withinGracePeriod ? graceLevel : purchaseLevel;

        uint256 seed = uint256(keccak256(abi.encode(rngWord, player, day, amount)));
        uint24 targetLevel = _rollTargetLevel(baseLevel, seed);

        if (targetLevel < currentLevel) {
            targetLevel = currentLevel;
        }
```

From contracts/modules/DegenerusGameLootboxModule.sol L882-884 (Layer-2 caller clamp in _resolveLootboxCommon):
```solidity
        if (targetLevel < currentLevel) {
            targetLevel = currentLevel;
        }
```

From contracts/modules/DegenerusGameLootboxModule.sol L1568-1581 (Layer-3 dead branch — LBX-01 deletion target):
```solidity
        uint256 roll = uint16(seed >> 40) % 20;
        if (roll < 11) {
            // 55% chance: tickets (returned as scaled × TICKET_SCALE)
            uint256 ticketBudget = (amount * LOOTBOX_TICKET_ROLL_BPS) / 10_000;
            uint32 ticketsScaled =
                _lootboxTicketCount(ticketBudget, targetPrice, seed);
            if (ticketsScaled != 0) {
                if (targetLevel < currentLevel) {
                    // Convert to BURNIE if target level already passed
                    burnieOut = (uint256(ticketsScaled) * PRICE_COIN_UNIT) / TICKET_SCALE;
                } else {
                    ticketsOut = ticketsScaled;
                }
            }
            applyPresaleMultiplier = false;
        }
```

Paste-ready diff from CONTEXT.md `<specifics>` (LBX-01):
```diff
             uint32 ticketsScaled =
                 _lootboxTicketCount(ticketBudget, targetPrice, seed);
             if (ticketsScaled != 0) {
-                if (targetLevel < currentLevel) {
-                    // Convert to BURNIE if target level already passed
-                    burnieOut = (uint256(ticketsScaled) * PRICE_COIN_UNIT) / TICKET_SCALE;
-                } else {
-                    ticketsOut = ticketsScaled;
-                }
+                ticketsOut = ticketsScaled;
             }
             applyPresaleMultiplier = false;
```

From contracts/modules/DegenerusGameLootboxModule.sol L250 (LOOTBOX_TICKET_ROLL_BPS):
```solidity
    uint16 private constant LOOTBOX_TICKET_ROLL_BPS = 16_100;  // 161%
```

From contracts/modules/DegenerusGameLootboxModule.sol L1639 (_lootboxTicketCount signature, returns countScaled — uint32 ticketsScaled the deletion-site receives):
```solidity
    function _lootboxTicketCount(
        uint256 budget,
        uint256 priceWei,
        uint256 seed
    ) private pure returns (uint32 countScaled) {
```

From test/stat/SurfaceRegression.test.js L603 + L609 + L752 (current SURF-03 anchor at v36.0 — Task 4 re-baselines just the SURF-03 it block; SURF-01/02/04 stay at v36.0):
```javascript
const V36_BASELINE = "1c0f09132d7439af9881c56fe197f81757f8164a";
// ...
describe("v37.0 SURF-01..04 — protected surfaces vs v36.0 baseline 1c0f0913", function () {
  // ...
  it("SURF-03 — DegenerusGameLootboxModule.sol file-level zero-diff vs v36.0 baseline 1c0f0913 (D-268-SURF03-01: Phase 269 owns the post-cleanup re-baseline)", function () {
    const result = expectFileLevelZeroDiffV37(V36_BASELINE, LOOTBOX_MODULE_PATH);
    if (result.skipped) this.skip();
  });
```

From test/gas/LootboxOpenGas.test.js L66-70 (Phase 266 REF-CAPTURE constants — Task 4's LBX-02 v37.0 describe reuses the same PRE_DELETION_GAS_REF / REF-CAPTURE pattern, NOT factored — declares its own constants at file scope or within the new describe per existing precedent):
```javascript
const PER_OPEN_GAS_DELTA_BOUND       = 300;       // GAS-01 ±300 gas per-open
const ENTRY_POINT_DELTA_TOLERANCE    = 2000;      // ±2000 gas per-site tolerance vs pinned REF (codegen variance)
const OPEN_LOOTBOX_GAS_REF             = 0;       // executor-pinned post REF-CAPTURE first run
const OPEN_BURNIE_LOOTBOX_GAS_REF      = 0;       // executor-pinned post REF-CAPTURE first run
const RESOLVE_LOOTBOX_DIRECT_GAS_REF   = 0;       // executor-pinned post REF-CAPTURE first run
```

From test/gas/AdvanceGameGas.test.js L1464-1468 (Phase 264 SURF-05 describe — GASPIN-01 primary drift candidate site):
```javascript
describe("Phase 264 SURF-05 — advanceGame 1.99× margin preserved at v35.0 HEAD", function () {
  this.timeout(180_000);

  const MAX_BLOCK_GAS = 30_000_000n;
  const REQUIRED_MARGIN = 1.99;
```

Current package.json L9 `test:stat` ordering (Phase 268 close state — Task 1 bisect input; Task 4 mutates ONLY if D-269-STAB-01 chose split-files):
```
"test:stat": "hardhat test test/gas/Phase261GasRegression.test.js test/gas/Phase264GasRegression.test.js test/gas/Phase268GasRegression.test.js test/stat/TraitDistribution.test.js test/stat/GoldSoloCoverage.test.js test/stat/SoloEvUplift.test.js test/stat/PackFeel.test.js test/stat/SurfaceRegression.test.js test/stat/PerPullLevelDistribution.test.js test/stat/PerPullEmptyBucketSkip.test.js test/stat/LootboxEntropyDistribution.test.js test/stat/DegenerettePerNEvExactness.test.js test/stat/DegeneretteProducerChi2.test.js test/stat/DegeneretteBonusEv.test.js",
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GASPIN-01 root-cause investigation + RCA-inline plan revision</name>
  <files>.planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-01-PLAN.md</files>
  <read_first>
    - .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-CONTEXT.md `<decisions>` D-269-RCA-01 + D-269-STAB-01 + D-269-DISCRETION-CHOICES + `<specifics>` GASPIN-01 bisect plan sketch (binary partition; 14-file `test:stat` ordering enumerated at L243-256).
    - .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-CONTEXT.md `<code_context>` Reusable Assets (existing test files identified as drift candidates: `test/gas/AdvanceGameGas.test.js` Phase 264 SURF-05 describe at L1464+; `test/gas/Phase264GasRegression.test.js`).
    - .planning/REQUIREMENTS.md GASPIN-01..03 (acceptance criteria: identify which hypothesis from {state pollution, gas-meter snapshot ordering, fixture-loader caching, hardhat node restart timing}; least-invasive stabilization path; combined-suite stable across 3 consecutive runs).
    - test/gas/AdvanceGameGas.test.js L1464-1468 (Phase 264 SURF-05 describe block — confirm whether it's in `test:stat` ordering or only invoked via separate `npm test` command; CONTEXT.md `<specifics>` L259 flags this: "SURF-05 `Phase 264 SURF-05` describe also lives in `test/gas/AdvanceGameGas.test.js` L1464+, which is NOT in `test:stat` ordering — confirm during RCA whether the drift hits the stat-suite-included `Phase264GasRegression.test.js` SURF-05 pin OR the `AdvanceGameGas.test.js` SURF-05 describe under a different test command.").
    - test/gas/Phase264GasRegression.test.js L372 (Phase 264 SURF-05 per-pull-level resample entry-point gas regression describe — secondary drift candidate; this file IS in `test:stat` ordering).
    - package.json L9 `test:stat` script (current 14-file ordering).
    - `feedback_no_history_in_comments.md` — RCA-inline must describe what IS, not what changed; inline NatSpec at any affected fixture follows the same rule in Task 4.
    - `feedback_design_intent_before_deletion.md` — Trace original design intent + actor game-theory across timing/state combos BEFORE the deletion (already done during the 269-CONTEXT discussion; the triple-defense + game-theory traces are recorded in D-269-LBX-CALLERCLAMP-CARRY-01 + D-269-LBX-GAMETHEORY-CARRY-01 carry decisions).
    - This `269-01-PLAN.md` file (entirety) — Task 1 appends a new `## Root Cause (GASPIN-01)` section at end-of-file; must NOT modify any frontmatter or existing `<tasks>` blocks.
  </read_first>
  <action>
    Investigate the GASPIN-01 root cause via binary-partition bisect across the 14-file `test:stat` ordering per D-269-RCA-01 + D-269-DISCRETION-CHOICES (binary partition first chosen for fewer rounds: ~log₂(14) ≈ 4 vs ~7 for reverse-order traversal).

    **Step 1 — Confirm drift surface and isolate the actual drifting pin file.** Run two baselines: (a) `npx hardhat test test/gas/Phase264GasRegression.test.js` standalone; (b) `npx hardhat test test/gas/AdvanceGameGas.test.js -g "Phase 264 SURF-05"` standalone (if reachable; `test/gas/AdvanceGameGas.test.js` L1464+ describe). Record the standalone gas measurements. Then run `npm run test:stat` and compare the SURF-05 measurements in the combined-suite output against the standalone baselines. The CONTEXT.md `<specifics>` note at L259 flags that the v36.0-observed ~120K drift may actually be in `test/gas/AdvanceGameGas.test.js` (which is NOT in `test:stat`) — confirm or reject this by checking whether the `Phase264GasRegression.test.js` SURF-05 entry-point delta assertions at L376/L428 (`payDailyCoinJackpot` stage-6 and `payDailyJackpotCoinAndTickets` stage-9) drift under combined-suite, OR whether the AdvanceGameGas describe at L1464 is the actual drift site. If `AdvanceGameGas.test.js` is the actual drift site AND it is NOT in `test:stat` ordering, that contradicts the "drifts under `test:stat` ordering" claim in CONTEXT.md — clarify by running `npm test` if there's a separate combined-suite invocation that pulls AdvanceGameGas in, or by checking historical CI logs / `.planning/STATE.md` Phase 266 close notes for the exact invocation context. Record the actual drift-bearing file (or 'drift not reproducible at Phase 268 close HEAD' if it isn't).

    **Step 2 — If drift IS reproducible, binary-partition bisect.** Configure 4 bisect rounds across the 14-file `test:stat` list (per CONTEXT.md `<specifics>` ordering enumeration):
      Round 1: Run with files [1..7] only (Phase261GasRegression through SurfaceRegression). Observe drift?
      Round 2: Halve the failing half. (If [1..7] drifts → run [1..3]; if [8..14] drifts → run [8..10].)
      Round 3+: Iterate until a single polluting predecessor is isolated.
    For each round invoke a one-off hardhat invocation: `npx hardhat test test/gas/Phase261GasRegression.test.js test/gas/Phase264GasRegression.test.js <subset> test/gas/Phase264GasRegression.test.js` (always include `Phase264GasRegression.test.js` last as the SURF-05 measurement target; vary the prefix subset). Use `--grep "Phase 264 SURF-05"` to limit output. Record the gas measurement for each subset.

    **Step 3 — Identify mechanism.** Once a polluting predecessor (or "no isolated polluter — drift not reproducible" or "multi-file cumulative") is identified, classify the cause from D-269-RCA-01's 4-option list: (a) state pollution from prior test files; (b) gas-meter snapshot ordering; (c) fixture-loader caching; (d) hardhat node restart timing. Mechanism identification may require reading the suspected polluter's `beforeEach` / `before` hooks for fixture-deployment patterns, or instrumenting the hardhat config for gas-meter snapshot-ordering verification per D-269-RCA-01 escalation clause. If bisect inconclusive → escalate to instrumented hardhat-snapshot diff or per-test gas-meter trace inspection; record what tooling was used and what it showed.

    **Step 4 — Pick stabilization path per D-269-STAB-01 (least-invasive principle).** Pick from:
      (a) **re-pin** gas snapshots to combined-suite-stable values + inline NatSpec at the affected test fixture documenting current stabilization shape — least-invasive when pollution mechanism is structural (e.g., known fixture-loader cache behavior, cannot be eliminated cheaply).
      (b) **ordering-fix** via `before(() => vm.reset())` injection or test-file isolation — least-invasive when RCA reveals a one-line surgical fix (e.g., specific fixture re-deployment between describes).
      (c) **split-files** — chosen only if (a) and (b) prove infeasible (e.g., pollution is hardhat-node-restart-timing; isolation requires file-boundary). Forces a `package.json` test:stat wiring delta.
    If drift is "not reproducible at Phase 268 close HEAD", chosen path is (d) "no stabilization needed — drift cleared by Phase 267/268 commits, GASPIN-02 acceptance is the 3-consecutive-run cleanliness check at Task 4/5"; document this explicitly.

    **Step 5 — Append `## Root Cause (GASPIN-01)` section to this PLAN.md.** Use the Edit tool (NOT Write — the file already exists and only the bottom appends). Append at the end-of-file (after the last task closing block — pick the natural Markdown end-of-document). The new section MUST contain (per D-269-RCA-01 deliverable spec):
      - A subsection `### Drift surface confirmation` reporting Step 1 outcomes (which file's pin drifts; or "not reproducible at Phase 268 close HEAD").
      - A subsection `### Bisect rounds` enumerating the 4 (or fewer) rounds run + the gas measurements + the polluting predecessor (or "multi-file cumulative" or "no isolated polluter — drift not reproducible").
      - A subsection `### Mechanism` naming one of {state pollution, gas-meter snapshot ordering, fixture-loader caching, hardhat node restart timing} (or "drift not reproducible"; or "escalated to instrumented {hardhat-snapshot diff | per-test gas-meter trace} which showed <finding>").
      - A subsection `### Chosen stabilization path (D-269-STAB-01)` naming option (a) / (b) / (c) / (d) with the rationale on least-invasive choice.
      - A subsection `### Task 4 mutation map` naming the explicit list of files Task 4 will mutate: `test/gas/AdvanceGameGas.test.js` and/or `test/gas/Phase264GasRegression.test.js` and/or `package.json` and/or a new sibling file (with proposed filename if split-files). For each file named, state which existing line range(s) Task 4 will edit (e.g., "Phase264GasRegression.test.js L376 PER_CALL_GAS_REF re-pin from 75000 to <new pinned value>").

    **Step 6 — Per `feedback_no_history_in_comments.md`, the RCA section describes what IS (current state of the drift + chosen fix) — NOT what was tried and failed (unless it informs the chosen path). The section is plan-internal; per D-269-RCA-01 it does NOT spawn a separate `269-NN-GASPIN-RCA.md` working file.**

    **Step 7 — Commit the plan revision.** `git add .planning/phases/269-…/269-01-PLAN.md` then commit with subject `docs(269): GASPIN-01 root-cause inline — <2-3 word mechanism summary>` and a short body (≤5 lines) restating the chosen stabilization path. NO `git push` per `feedback_manual_review_before_push.md`. This is an AGENT-COMMITTED docs commit per D-269-APPROVAL-01.
  </action>
  <verify>
    <automated>bash -c 'grep -c "## Root Cause (GASPIN-01)" .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-01-PLAN.md  # Expected: 1' && bash -c 'grep -cE "state pollution|gas-meter snapshot|fixture-loader cach|hardhat node restart|drift not reproducible" .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-01-PLAN.md  # Expected: >= 1' && bash -c 'grep -cE "stabilization path|Chosen stabilization|(re-pin|ordering-fix|split-files|no stabilization needed)" .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-01-PLAN.md  # Expected: >= 1' && bash -c 'grep -cE "Task 4 mutation map|files Task 4 will mutate" .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-01-PLAN.md  # Expected: >= 1' && bash -c 'git log -1 --format=%s | grep -c "^docs(269): GASPIN-01 root-cause inline"  # Expected: 1'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Root Cause (GASPIN-01)" .planning/phases/269-…/269-01-PLAN.md` returns `1` (header present).
    - `grep -cE "state pollution|gas-meter snapshot|fixture-loader cach|hardhat node restart|drift not reproducible" .planning/phases/269-…/269-01-PLAN.md` returns `>= 1` (mechanism named from the 4-option set, or explicit "not reproducible" path documented).
    - `grep -cE "stabilization path|Chosen stabilization|(re-pin|ordering-fix|split-files|no stabilization needed)" .planning/phases/269-…/269-01-PLAN.md` returns `>= 1` (D-269-STAB-01 choice documented; explicit option a/b/c/d named).
    - `grep -cE "Task 4 mutation map|files Task 4 will mutate" .planning/phases/269-…/269-01-PLAN.md` returns `>= 1` (mutation map present so Task 4 knows exactly which files to touch).
    - `git log -1 --format=%s` returns a line starting with `docs(269): GASPIN-01 root-cause inline`.
    - NO new file created at `.planning/phases/269-…/269-NN-GASPIN-RCA.md` (verify via `ls .planning/phases/269-…/269-*-GASPIN-RCA.md 2>/dev/null | wc -l` returns `0`) — per D-269-RCA-01 plan-internal-only.
    - NO push performed (`git log origin/main..HEAD` shows at least 1 unpushed commit OR the agent has not run `git push` in this session; per `feedback_manual_review_before_push.md`).
  </acceptance_criteria>
  <done>RCA section appended inline to 269-01-PLAN.md; mechanism named from {state pollution, gas-meter snapshot ordering, fixture-loader caching, hardhat node restart timing, drift not reproducible}; D-269-STAB-01 stabilization path chosen + named; Task 4 mutation map enumerates the test files Task 4 will edit; agent-committed `docs(269): GASPIN-01 root-cause inline — <mechanism>` commit landed locally with no push.</done>
</task>

<task type="auto">
  <name>Task 2: LBX-01 contract edit — delete dead BURNIE-conversion branch</name>
  <files>contracts/modules/DegenerusGameLootboxModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameLootboxModule.sol L1548-1581 (Layer-3 dead-branch deletion site; `_resolveLootboxRoll` signature + tickets-path branch with the dead inner check). The exact 8-line body to be replaced is at L1573-1580 (`if (ticketsScaled != 0) { if (targetLevel < currentLevel) { burnieOut = ...; } else { ticketsOut = ticketsScaled; } }`).
    - contracts/modules/DegenerusGameLootboxModule.sol L860-988 (`_resolveLootboxCommon` — sole caller of `_resolveLootboxRoll` at L910 + L938; Layer-2 caller clamp at L882-884 — source of byte-equivalence proof).
    - contracts/modules/DegenerusGameLootboxModule.sol L526-598 (`openLootBox` — outer ETH-lootbox open path with Layer-1 caller clamp at L557-559; grace-period rule at L549-552; `seed = keccak256(rngWord, player, day, amount)` at L554).
    - .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-CONTEXT.md `<specifics>` LBX-01 deletion diff paste-ready block (L192-209) — verbatim diff to apply.
    - .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-CONTEXT.md `<decisions>` D-269-LBX-SHAPE-01 (pure deletion; NO `require` invariant; NO NatSpec contract; NO trace comment per `feedback_no_history_in_comments.md` + `feedback_no_dead_guards.md`).
    - .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-CONTEXT.md `<decisions>` D-269-LBX-CALLERCLAMP-CARRY-01 + D-269-LBX-GAMETHEORY-CARRY-01 (byte-equivalence + zero-advantage carry from discussion; the executor does NOT need to re-derive these — they are locked).
    - `feedback_no_history_in_comments.md` — NO trace comment at the deletion site; comments describe what IS, not what was there.
    - `feedback_no_dead_guards.md` — NO `require(targetLevel >= currentLevel)` invariant assert; pure deletion is the maximum-savings shape.
    - `feedback_never_preapprove_contracts.md` — Task 2 produces the diff but does NOT commit; commit lands in Task 3 after explicit user `approved`.
  </read_first>
  <action>
    Apply the paste-ready diff from CONTEXT.md `<specifics>` L192-209 to `contracts/modules/DegenerusGameLootboxModule.sol` `_resolveLootboxRoll` L1573-1580. Use the Edit tool with the following exact old_string / new_string mapping:

    **old_string** (8 lines, current state at HEAD; verify these line numbers by reading L1573-1580 directly; the existing block at HEAD is):
    ```
                if (ticketsScaled != 0) {
                    if (targetLevel < currentLevel) {
                        // Convert to BURNIE if target level already passed
                        burnieOut = (uint256(ticketsScaled) * PRICE_COIN_UNIT) / TICKET_SCALE;
                    } else {
                        ticketsOut = ticketsScaled;
                    }
                }
    ```

    **new_string** (3 lines per D-269-LBX-SHAPE-01):
    ```
                if (ticketsScaled != 0) {
                    ticketsOut = ticketsScaled;
                }
    ```

    Net diff: -5 lines, +1 line. Single hunk. NO other changes anywhere in the file.

    **Discretion choices already locked per D-269-LBX-SHAPE-01 — do NOT deviate:**
    - NO `require(targetLevel >= currentLevel)` invariant assert added (per `feedback_no_dead_guards.md`).
    - NO NatSpec contract on caller's responsibility added (per D-269-LBX-SHAPE-01 + `feedback_no_history_in_comments.md`).
    - NO trace comment at the deletion site (caller-clamp invariant self-evident from local `_resolveLootboxCommon` L860-884 reading; per `feedback_no_history_in_comments.md`).
    - NO mutation of the surrounding `roll < 11` if-clause structure (only the inner body of the `if (ticketsScaled != 0)` guard changes).
    - NO mutation of `applyPresaleMultiplier = false;` at the trailing line (it stays).

    **After the edit, run `npx hardhat compile` to verify the deletion compiles cleanly.** If new warnings appear vs the pre-edit baseline (`git stash` + compile + `git stash pop` to compare), surface them — but expected outcome is zero new warnings.

    **Do NOT commit at this task.** Per `feedback_never_preapprove_contracts.md` + `feedback_wait_for_approval.md` + D-269-APPROVAL-02, the diff awaits explicit user approval at Task 3 BLOCKING gate. Leave the working tree dirty (modified file unstaged or staged but uncommitted) so Task 3 can present `git diff`.
  </action>
  <verify>
    <automated>bash -c 'grep -c "if (targetLevel < currentLevel)" contracts/modules/DegenerusGameLootboxModule.sol  # Expected: 2 (down from 3 — layer-1 + layer-2 remain; layer-3 deleted)' && bash -c 'grep -c "Convert to BURNIE if target level already passed" contracts/modules/DegenerusGameLootboxModule.sol  # Expected: 0' && bash -c 'grep -cE "^[[:space:]]+burnieOut = \\(uint256\\(ticketsScaled\\)" contracts/modules/DegenerusGameLootboxModule.sol  # Expected: 0 (the dead-branch BURNIE-conversion assignment is removed; other burnieOut assignments elsewhere in the file stay)' && bash -c 'grep -cE "^[[:space:]]+ticketsOut = ticketsScaled;[[:space:]]*$" contracts/modules/DegenerusGameLootboxModule.sol  # Expected: 1 (the single unconditional assignment inside the surviving ticketsScaled != 0 guard)' && bash -c 'grep -rn "_resolveLootboxRoll" contracts/ | wc -l  # Expected: 3 (definition + 2 callsites at _resolveLootboxCommon L910 + L938 — no widening of the call graph)' && bash -c 'cd /home/zak/Dev/PurgeGame/degenerus-audit && npx hardhat compile 2>&1 | tail -10  # Expected: ends with "Compiled X Solidity files successfully" or equivalent success line; no new warnings'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "if (targetLevel < currentLevel)" contracts/modules/DegenerusGameLootboxModule.sol` returns exactly `2` (Layer-1 at L557 + Layer-2 at L882 remain; deleted Layer-3 was the 3rd).
    - `grep -c "Convert to BURNIE if target level already passed" contracts/modules/DegenerusGameLootboxModule.sol` returns `0` (dead-branch comment removed).
    - `grep -cE "^[[:space:]]+burnieOut = \(uint256\(ticketsScaled\)" contracts/modules/DegenerusGameLootboxModule.sol` returns `0` (the specific BURNIE-conversion-of-ticketsScaled assignment is removed; other `burnieOut = ...` assignments in `_resolveLootboxRoll` other branches stay untouched).
    - `grep -cE "^[[:space:]]+ticketsOut = ticketsScaled;[[:space:]]*$" contracts/modules/DegenerusGameLootboxModule.sol` returns `1` (single unconditional assignment inside the surviving guard).
    - `grep -rn "_resolveLootboxRoll" contracts/` returns 3 lines (1 `function _resolveLootboxRoll(` definition at L1548 ± shift, 2 callsites at L910 + L938 ± shift inside `_resolveLootboxCommon`). No widening of the call graph; `_resolveLootboxRoll` remains `private`.
    - `npx hardhat compile` exits 0 with no new warnings vs pre-edit baseline.
    - `git status --porcelain contracts/modules/DegenerusGameLootboxModule.sol` shows `M ` (file modified, working-tree dirty — uncommitted; Task 3 will commit after approval).
    - NO commit landed at this task (verify via `git log -1 --format=%s` does NOT start with `feat(269):` — last commit is the Task 1 `docs(269): GASPIN-01 root-cause inline` commit).
  </acceptance_criteria>
  <done>Dead branch at L1574-1577 (current HEAD line range; may shift) deleted via Edit tool; single-hunk diff applied per D-269-LBX-SHAPE-01 paste-ready spec; `hardhat compile` exits 0 with no new warnings; working tree dirty (modified file unstaged or staged but uncommitted) ready for Task 3 BLOCKING approval gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] LBX-01 USER-APPROVED contract commit gate</name>
  <files>contracts/modules/DegenerusGameLootboxModule.sol</files>
  <read_first>
    - `feedback_no_contract_commits.md` — NEVER commit `contracts/` changes without explicit user approval.
    - `feedback_batch_contract_approval.md` — present ONE diff for the phase contract changes, get ONE approval.
    - `feedback_wait_for_approval.md` — Present fix and WAIT for explicit approval before committing. Do NOT pre-emptively commit.
    - `feedback_never_preapprove_contracts.md` — Orchestrator/agent must NEVER tell the executor that contract changes are "pre-approved".
    - `feedback_manual_review_before_push.md` — NO `git push` by the agent. User reviews diff manually before any push.
    - .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-CONTEXT.md `<decisions>` D-269-APPROVAL-02 (contract + test commits USER-APPROVED batched).
  </read_first>
  <what-built>
    Task 2 implemented the LBX-01 dead-branch deletion at `contracts/modules/DegenerusGameLootboxModule.sol` `_resolveLootboxRoll` L1573-1580. Single-hunk diff: -5 / +1 LOC per D-269-LBX-SHAPE-01 paste-ready spec. The 8-line `if (ticketsScaled != 0) { if (targetLevel < currentLevel) { burnieOut = ...; } else { ticketsOut = ticketsScaled; } }` body was replaced with the 3-line `if (ticketsScaled != 0) { ticketsOut = ticketsScaled; }`. Working tree is dirty; uncommitted.
  </what-built>
  <how-to-verify>
    The agent presents the diff and STOPS for explicit user approval. The agent body for this checkpoint executes the following sequence:

    1. Run `git status --porcelain contracts/` to confirm only `contracts/modules/DegenerusGameLootboxModule.sol` is dirty (no other contract files touched).
    2. Run `git diff contracts/modules/DegenerusGameLootboxModule.sol` and present the full diff verbatim to the user.
    3. Run `grep -c "if (targetLevel < currentLevel)" contracts/modules/DegenerusGameLootboxModule.sol` (expect `2`) and cite to user as triple-defense → double-defense witness.
    4. Run `grep -rn "_resolveLootboxRoll" contracts/` and cite the 3-line output (definition + 2 callsites at `_resolveLootboxCommon` L910 + L938) as the no-widening-of-call-graph proof.
    5. Run `npx hardhat compile` and cite the success-line output.
    6. Display the byte-equivalence proof restated: D-269-LBX-CALLERCLAMP-CARRY-01 (Layer-1 at L557 + Layer-2 at L882 unconditionally clamp `targetLevel = max(targetLevel, currentLevel)` before `_resolveLootboxRoll` is reached) + D-269-LBX-GAMETHEORY-CARRY-01 (delay confers no advantage; only loses 10% far-future bucket-spread variety).
    7. **STOP. Wait for the user reply containing the literal token `approved`.** Per `feedback_wait_for_approval.md` + `feedback_never_preapprove_contracts.md`, the agent does NOT proceed without the explicit token.
    8. ONLY after the user reply contains `approved`: run `git add contracts/modules/DegenerusGameLootboxModule.sol && git commit -m "feat(269): delete unreachable BURNIE-conversion branch in _resolveLootboxRoll [LBX-01]"` with an optional 3-5 line body restating the triple-defense byte-equivalence and the -5/+1 LOC.
    9. Run `git log -1 --format="%H %s"` and report the new HEAD sha (this sha becomes the Phase-269-close SURF-03 re-baseline anchor used by Task 4's SURF-03 update).
    10. DO NOT push. Per `feedback_manual_review_before_push.md`, user reviews the diff manually before any push.

    If the user reply contains anything other than the explicit `approved` token (e.g., requested changes, "looks good but…", "approve" without `d`, "approve the deletion"), the agent must NOT commit. The agent must surface the deviation, ask for clarification, and re-present the gate. Per `feedback_never_preapprove_contracts.md`, NO partial-credit interpretations.

    If the user explicitly DISAPPROVES, the agent reverts the working tree (`git checkout contracts/modules/DegenerusGameLootboxModule.sol`) and surfaces the next steps (e.g., re-discuss D-269-LBX-SHAPE-01 if a different deletion shape is requested).
  </how-to-verify>
  <resume-signal>Type `approved` to commit, or describe the requested deviation. Per `feedback_never_preapprove_contracts.md`, partial-credit tokens (e.g., "approve" without `d`) do NOT count; the agent will surface and re-present.</resume-signal>
  <action>
    Execute the LBX-01 USER-APPROVED contract commit gate per the how-to-verify sequence above (steps 1-10). The gate is BLOCKING: present the diff via the diff command on contracts/; cite the grep proofs (grep count "if (targetLevel < currentLevel)" returns 2 + recursive grep "_resolveLootboxRoll" returns 3 lines + hardhat compile exits 0); restate the byte-equivalence proof (D-269-LBX-CALLERCLAMP-CARRY-01 triple-defense caller-clamp invariant + D-269-LBX-GAMETHEORY-CARRY-01 delay-confers-no-advantage); STOP and wait for the literal user token `approved`. ONLY after the token is captured: stage the contract file then commit with subject `feat(269): delete unreachable BURNIE-conversion branch in _resolveLootboxRoll [LBX-01]`. Record the new HEAD sha for Task 4 SURF-03 re-baseline anchor. Do NOT push (per `feedback_manual_review_before_push.md`). Per `feedback_never_preapprove_contracts.md`, do NOT proceed on partial-credit tokens; re-present the gate.
  </action>
  <verify>
    <automated>bash -c 'git log -1 --format="%s" | grep -c "^feat(269): delete unreachable BURNIE-conversion branch in _resolveLootboxRoll \\[LBX-01\\]"  # Expected: 1 (commit landed with exact subject)' && bash -c 'git diff HEAD~1 HEAD --stat | grep -c "DegenerusGameLootboxModule.sol"  # Expected: 1' && bash -c 'git diff HEAD~1 HEAD --stat | grep -cE "test/|package\\.json|\\.planning/"  # Expected: 0 (contract-only commit; no test/planning files batched in)' && bash -c 'git diff HEAD~1 HEAD -- contracts/modules/DegenerusGameLootboxModule.sol | grep -cE "^-.*if \\(targetLevel < currentLevel\\) \\{"  # Expected: 1 (the dead-branch if removed; -1 OLD-side line of the if)' && bash -c 'git log origin/main..HEAD 2>/dev/null | wc -l  # informational — should show at least 1 unpushed commit; if origin/main is not configured this is 0 but agent must NOT have run git push'</automated>
  </verify>
  <acceptance_criteria>
    - User reply containing the literal token `approved` captured at the gate (recorded in conversation log).
    - `git log -1 --format="%s"` returns exactly `feat(269): delete unreachable BURNIE-conversion branch in _resolveLootboxRoll [LBX-01]`.
    - `git diff HEAD~1 HEAD --stat` shows exactly 1 file changed: `contracts/modules/DegenerusGameLootboxModule.sol`. NO test/planning/package.json files in this commit (those are batched into the Task 5 test commit per D-269-COMMITS-01).
    - `git diff HEAD~1 HEAD -- contracts/modules/DegenerusGameLootboxModule.sol` shows a single hunk with -5 / +1 LOC net (the 5 OLD-side lines of the dead branch removed; 1 NEW-side `ticketsOut = ticketsScaled;` line added).
    - The new HEAD sha is recorded in the conversation log and is used by Task 4's SURF-03 re-baseline anchor.
    - NO `git push` performed (per `feedback_manual_review_before_push.md`); the commit is local-only at this point.
  </acceptance_criteria>
  <done>User approved with the literal `approved` token; `feat(269): delete unreachable BURNIE-conversion branch in _resolveLootboxRoll [LBX-01]` commit landed locally with a single-file contract diff (-5/+1 LOC); no push performed; the new HEAD sha is captured for Task 4's SURF-03 anchor.</done>
</task>

<task type="auto">
  <name>Task 4: Apply LBX-02 + GASPIN-02 + SURF-03 + (optional) package.json test edits</name>
  <files>test/gas/LootboxOpenGas.test.js, test/stat/SurfaceRegression.test.js, test/gas/AdvanceGameGas.test.js, test/gas/Phase264GasRegression.test.js, package.json</files>
  <read_first>
    - .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-01-PLAN.md `## Root Cause (GASPIN-01)` section appended by Task 1 — names which files Task 4 mutates for GASPIN-02 stabilization (Task 4 mutation map) AND which stabilization path was chosen per D-269-STAB-01.
    - test/gas/LootboxOpenGas.test.js (entirety, 312 LOC at Phase 268 close) — Task 4 appends a new top-level describe block at end-of-file (after L312); existing Phase 266 describes at L180 / L227 / L290 are UNTOUCHED.
    - test/stat/SurfaceRegression.test.js L580-787 (v37.0 SURF-01..04 describe block) — Task 4 updates ONLY the SURF-03 it block at L752; SURF-01 (L742), SURF-02 (L747), SURF-04 (L763) UNTOUCHED.
    - test/gas/AdvanceGameGas.test.js L1464-1690 (Phase 264 SURF-05 describe + v36.0 1.99× margin invariant carries-forward describe) — Task 4 mutates ONLY if Task 1 RCA's mutation map names this file.
    - test/gas/Phase264GasRegression.test.js L372-460 (Phase 264 SURF-05 entry-point delta describes) — Task 4 mutates ONLY if Task 1 RCA's mutation map names this file.
    - package.json L9 `test:stat` script — Task 4 mutates ONLY if Task 1 RCA chose split-files (D-269-STAB-01 option c).
    - .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-CONTEXT.md `<decisions>` D-269-WORSTGAS-01 (LBX-02 worst-case NatSpec dimensions) + `<specifics>` L211-236 (worst-case NatSpec header sketch verbatim).
    - .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-CONTEXT.md `<decisions>` D-269-SURF03-01 (SURF-03 re-baseline to Phase-269-close HEAD).
    - .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-CONTEXT.md `<decisions>` D-269-COMMITS-01 (single batched test commit — all four deltas together).
    - contracts/modules/DegenerusGameLootboxModule.sol post-Task-3 HEAD `_resolveLootboxRoll` (with deletion applied) — Task 4's LBX-02 worst-case test exercises the post-deletion code path.
    - contracts/modules/DegenerusGameLootboxModule.sol L1639 `_lootboxTicketCount` signature — Task 4's worst-case test engineers the seed so this returns nonzero.
    - contracts/modules/DegenerusGameLootboxModule.sol L1568 `uint256 roll = uint16(seed >> 40) % 20;` — Task 4's seed engineering ensures `uint16(seed >> 40) % 20 < 11` for the 55%-tickets-path branch.
    - `feedback_no_history_in_comments.md` — Task 4's inline NatSpec at any affected fixture describes what IS (current stabilization shape), NOT what changed. No "was 120K, now X" annotations.
    - `feedback_gas_worst_case.md` — LBX-02 worst-case derivation in NatSpec header BEFORE measurement.
    - `feedback_never_preapprove_contracts.md` — Task 4 applies edits but does NOT commit; commit lands in Task 5 after explicit user `approved`.
    - The Phase-269-close HEAD sha from Task 3 commit (captured by Task 3 final step) — needed for the SURF-03 re-baseline `PHASE_269_CLOSE_BASELINE` constant.
  </read_first>
  <action>
    Apply 3 always-required test-tree edits + 1 conditional edit + 1 conditional package.json edit in this exact order. Per `feedback_never_preapprove_contracts.md` + `feedback_wait_for_approval.md` + D-269-APPROVAL-02, do NOT commit — diff awaits explicit user approval at Task 5 BLOCKING gate.

    **Sub-step 4a — LBX-02 v37.0 describe in `test/gas/LootboxOpenGas.test.js` (ALWAYS required).** Use the Edit tool to append a new top-level describe block at end-of-file (after the existing Phase 266 closing brace at L312). The new block contains:

      - NatSpec header (block comment above the `describe`) verbatim per D-269-WORSTGAS-01 + CONTEXT.md `<specifics>` L211-236 sketch (do NOT re-derive; paste the dimensions block from CONTEXT.md). Headers MUST contain the strings: `Phase 269 LBX-02`, `Worst-case dimensions:`, `55%-tickets-path`, `roll < 11`, `ticketsScaled != 0`, `cold storage`, `max ticketsScaled`, `[30, 80] gas`, `bytecode shrink`.
      - File-scope constants:
        ```javascript
        const LBX_02_PER_OPEN_DELTA_MIN = 30;   // ROADMAP-locked envelope low bound
        const LBX_02_PER_OPEN_DELTA_MAX = 80;   // ROADMAP-locked envelope high bound
        const PRE_DELETION_GAS_REF       = 0;   // executor-pinned post REF-CAPTURE pre-deletion run (see protocol below)
        const POST_DELETION_GAS_REF      = 0;   // executor-pinned post REF-CAPTURE post-deletion run
        const V36_DEPLOYED_BYTECODE_BYTES_REF = 0;  // executor-pinned post REF-CAPTURE first run
        ```
      - `describe("Phase 269 LBX-02 — 55%-tickets-path lootbox-open gas savings + bytecode shrink at v37.0 HEAD", function () { ... });` with two `it` blocks:

        Block 1: `it("per-open gas delta vs v36.0 baseline ∈ [30, 80] gas at 55%-tickets-path worst case", async function () { ... })`. Body:
        - `await loadFixture(deployFullProtocol)` to get fresh cold-storage state.
        - Construct a deterministic seed satisfying both worst-case conditions: `uint16(seed >> 40) % 20 < 11` (tickets-path roll) AND `_lootboxTicketCount(...)` returns nonzero. Choose a seed value via inline precomputation: try seeds in sequence (e.g., starting from `0xC038_0000n` per Phase 269 seed convention extending Phase 268's `0xC037_NNNN` family — pick `0xC038_NNNN`). For each candidate seed, check `(BigInt.asUintN(16, candidate >> 40n) % 20n) < 11n`; pick the first satisfier. Construction MUST NOT brute-force >2^16 candidates (if needed, fall back to bit-engineering bits 40-55 of the seed directly).
        - Inject the engineered seed into `lootboxRngWordByIndex[index]` via `await hre.network.provider.send('hardhat_setStorageAt', [...])` — mirror the Phase 268 `Phase268GasRegression.test.js:hardhat_setStorageAt` pattern (see Phase 268 chore inventory §3 for the storage-slot calculation if not already inline-documented in the existing file).
        - Buy lootboxes for `alice` via the existing `buyLootboxes(game, alice, 20, 0.02)` helper at L105.
        - Reach an openable lootbox via `reachOpenableLootbox(fixture)` at L122 (or its analog); confirm `_lootboxTicketCount(...)` returns nonzero at the engineered seed by inspecting the returned bytecode-level state OR via a static-call probe.
        - Run `tx = await game.connect(alice).openLootBox(alice.address, index); receipt = await tx.wait(); measured = Number(receipt.gasUsed);`.
        - REF-CAPTURE protocol mirroring Phase 266 LootboxOpenGas L207 + Phase 268 Phase268GasRegression L348:
          - First run: print `[REF-CAPTURE] LBX_02_POST_DELETION_GAS_REF = ${measured}` and skip the assertion if `POST_DELETION_GAS_REF == 0`.
          - Subsequent runs: assert `(PRE_DELETION_GAS_REF - measured) >= LBX_02_PER_OPEN_DELTA_MIN && (PRE_DELETION_GAS_REF - measured) <= LBX_02_PER_OPEN_DELTA_MAX`.
        - Soft-skip on fixture-coverage gap per Phase 266 LootboxOpenGas L191 precedent if `reachOpenableLootbox` returns a non-null `reason`.
        - The PRE_DELETION_GAS_REF baseline is pinned by running the test at the pre-Task-3 HEAD; the executor pins this constant by checking out `git rev-parse HEAD~1` temporarily IF the executor is willing to run the test against the pre-deletion contract, OR — preferred per `feedback_gas_worst_case.md` audit-trail discipline — the executor REF-CAPTURE-prints the pre-deletion baseline once (running with the working tree reverted to pre-Task-2 state via `git stash` + run + `git stash pop`) and pins both PRE and POST values into the file at the same Task 4 edit boundary.

        Block 2: `it("bytecode shrink confirmed via DegenerusGameLootboxModule deployment-byte-count delta vs v36.0 baseline", async function () { ... })`. Body:
        - `const artifact = await hre.artifacts.readArtifact('DegenerusGameLootboxModule');`
        - `const deployedBytes = (artifact.deployedBytecode.length - 2) / 2;` (subtract `0x` prefix; divide hex by 2 for byte count).
        - REF-CAPTURE: print `[REF-CAPTURE] V36_DEPLOYED_BYTECODE_BYTES_REF = ${deployedBytes}` on first run; on subsequent runs assert `deployedBytes < V36_DEPLOYED_BYTECODE_BYTES_REF` (post-Task-3 deployedBytes MUST be strictly smaller than pre-Task-3 baseline). The V36 baseline pin is similarly captured via `git stash` + run-against-pre-deletion + `git stash pop` flow.
        - NO trace comment in the contract; NO trace comment in the test about the historical state (per `feedback_no_history_in_comments.md`). The test asserts the CURRENT shrink; the NatSpec header explains the worst-case derivation.

    **Sub-step 4b — GASPIN-02 stabilization per D-269-STAB-01 chosen path in Task 1 RCA (CONDITIONAL on mutation map).** Read the `## Root Cause (GASPIN-01)` `### Task 4 mutation map` subsection at the bottom of this PLAN.md. Implement the chosen path:

      - Option (a) **re-pin**: Find the affected pin literal(s) in the mutation-map-named file(s) (`test/gas/AdvanceGameGas.test.js` and/or `test/gas/Phase264GasRegression.test.js`). For each pin: update the literal value to the combined-suite-stable value (the value observed under `npm run test:stat` at Phase 268 close, per Task 1 Step-1 measurements). Add an inline NatSpec block above each updated pin describing the CURRENT stabilization shape per `feedback_no_history_in_comments.md` (e.g., `// GASPIN-02: This pin is the combined-suite-stable value under `npm run test:stat` ordering. Polluting predecessor: <file from Task 1 mutation map>. Mechanism: <mechanism from Task 1 RCA>. D-269-STAB-01 chosen path: re-pin.`). NO "was X, now Y" prose.
      - Option (b) **ordering-fix**: Implement the surgical fix per Task 1 RCA — typically a `before(() => vm.reset())` injection or test-file isolation. Add inline NatSpec describing the fix's shape (e.g., `// GASPIN-02: Pre-describe vm.reset() ensures fixture-cache isolation under combined-suite ordering. D-269-STAB-01 chosen path: ordering-fix.`).
      - Option (c) **split-files**: Create a new sibling test file at the path proposed in Task 1's mutation map (e.g., `test/gas/AdvanceGameSurf05Isolated.test.js`). Copy the affected describe block into the new file verbatim; remove the describe from the original file. The new file's header NatSpec documents the isolation requirement per `feedback_no_history_in_comments.md` (e.g., `// GASPIN-02: SURF-05 advanceGame margin assertion lives in this sibling file because under combined-suite hardhat-node-restart-timing, in-suite ordering inflates the gas-meter pin by ~120K. D-269-STAB-01 chosen path: split-files.`). Also requires sub-step 4d (package.json).
      - Option (d) **no stabilization needed**: If Task 1 RCA concluded "drift not reproducible at Phase 268 close HEAD", make NO changes for GASPIN-02 in this sub-step; the acceptance comes from the 3-consecutive-run cleanliness check in Task 5 / verify block.

    **Sub-step 4c — SURF-03 re-baseline in `test/stat/SurfaceRegression.test.js` (ALWAYS required).** Use the Edit tool. Two changes within the v37.0 SURF-01..04 describe block at L580-787:

      Change 1 — introduce a new const immediately after `V36_BASELINE` at L603. Insert:
      ```javascript
      const PHASE_269_CLOSE_BASELINE = "<post-Task-3-HEAD-sha-from-Task-3-step-9>";  // D-269-SURF03-01: post-LBX-01 dead-branch cleanup baseline
      ```
      The sha placeholder MUST be replaced with the actual 40-character sha captured in Task 3's final step. The const must be defined at module scope (same scope as `V36_BASELINE`).

      Change 2 — update ONLY the SURF-03 it block at L752. Replace:
      ```javascript
        it("SURF-03 — DegenerusGameLootboxModule.sol file-level zero-diff vs v36.0 baseline 1c0f0913 (D-268-SURF03-01: Phase 269 owns the post-cleanup re-baseline)", function () {
          // D-268-SURF03-01 cite: SURF-03 file-level zero-diff is honest at Phase 268
          // close because Phase 267 doesn't touch LootboxModule. Phase 269 lootbox
          // dead-branch cleanup will require either (a) re-baselining this assertion
          // to Phase 268 close HEAD, or (b) adding allowed-hunk exception for the
          // ~L1568-1581 deletion. Each version is honest at its respective HEAD; no
          // lying about state that hasn't shipped.
          const result = expectFileLevelZeroDiffV37(V36_BASELINE, LOOTBOX_MODULE_PATH);
          if (result.skipped) this.skip();
        });
      ```
      with:
      ```javascript
        it("SURF-03 — DegenerusGameLootboxModule.sol file-level zero-diff vs Phase-269-close baseline <sha-short> (D-269-SURF03-01: post-LBX-01-cleanup file-level zero-diff)", function () {
          // D-269-SURF03-01: SURF-03 re-baselined to Phase-269-close HEAD after the
          // LBX-01 dead-branch deletion. SURF-01/02/04 remain anchored at the v36.0
          // baseline `1c0f0913`; only SURF-03 moves to the Phase-269-close HEAD
          // because LBX-01 is the only Phase 269 contract change. The post-LBX-01
          // file-level zero-diff at this baseline proves that subsequent Phase 270
          // (audit-only) and Phase 271 (audit-deliverable-only) phases leave the
          // file untouched at the byte level.
          const result = expectFileLevelZeroDiffV37(PHASE_269_CLOSE_BASELINE, LOOTBOX_MODULE_PATH);
          if (result.skipped) this.skip();
        });
      ```
      where `<sha-short>` is the 8-character short form of the Task-3 commit sha. Per `feedback_no_history_in_comments.md`, the comment describes what IS (current re-baseline at Phase-269-close HEAD; SURF-01/02/04 stay at v36.0; rationale for the per-it-block split baseline anchor), NOT what changed.

      SURF-01 (L742), SURF-02 (L747), SURF-04 (L763) are UNTOUCHED — they continue to pass `V36_BASELINE` to their respective `walkAndAssertV37` / `expectFileLevelZeroDiffV37` calls.

    **Sub-step 4d — package.json `test:stat` wiring delta (CONDITIONAL on Task 1 RCA choosing split-files D-269-STAB-01 option c).** If GASPIN-02 chose option (c), append the new sibling-split file path to the existing space-separated `test:stat` script value per Phase 268 precedent. Place the new file in the natural position in the ordering (e.g., next to the file it was split from). Format: single-string-edit in `package.json` L9. If GASPIN-02 chose option (a), (b), or (d), this sub-step is a NO-OP and `package.json` is NOT touched.

    **Sub-step 4e — Verify all 3 / 4 / 5 edited files compile + tests pass.** After all edits:
    - `npx hardhat compile` — exit 0 with no new warnings (no contract changes, but Hardhat may regenerate ABI).
    - `npx hardhat test test/gas/LootboxOpenGas.test.js` — exit 0; LBX-02 describe passes (REF-CAPTURE first-run prints if PRE/POST constants still 0; otherwise asserts envelope).
    - `npx hardhat test test/stat/SurfaceRegression.test.js` — exit 0; SURF-01..04 v37.0 describe passes at the new mixed baselines (3 anchors at V36_BASELINE, 1 anchor at PHASE_269_CLOSE_BASELINE).
    - `for i in 1 2 3; do echo "Run $i"; npm run test:stat; done` — all 3 runs exit 0 with no flaky failures (GASPIN-03 acceptance).

    **Do NOT commit at this task.** Per `feedback_never_preapprove_contracts.md` + `feedback_wait_for_approval.md` + D-269-APPROVAL-02, the diff awaits explicit user approval at Task 5 BLOCKING gate. Leave the working tree dirty with all 3 / 4 / 5 edited files modified (uncommitted) so Task 5 can present the combined `git diff`.
  </action>
  <verify>
    <automated>bash -c 'grep -c "Phase 269 LBX-02" test/gas/LootboxOpenGas.test.js  # Expected: >= 1' && bash -c 'grep -cE "Worst-case dimensions|55%-tickets-path" test/gas/LootboxOpenGas.test.js  # Expected: >= 1' && bash -c 'grep -cE "LBX_02_PER_OPEN_DELTA_MIN.*30|LBX_02_PER_OPEN_DELTA_MAX.*80" test/gas/LootboxOpenGas.test.js  # Expected: >= 1' && bash -c 'grep -cE "PRE_DELETION_GAS_REF|REF-CAPTURE" test/gas/LootboxOpenGas.test.js  # Expected: >= 1' && bash -c 'grep -cE "deployedBytecode|V36_DEPLOYED_BYTECODE_BYTES_REF" test/gas/LootboxOpenGas.test.js  # Expected: >= 1' && bash -c 'grep -c "PHASE_269_CLOSE_BASELINE" test/stat/SurfaceRegression.test.js  # Expected: >= 1' && bash -c 'grep -c "D-269-SURF03-01" test/stat/SurfaceRegression.test.js  # Expected: >= 1' && bash -c 'grep -c "post-LBX-01-cleanup" test/stat/SurfaceRegression.test.js  # Expected: >= 1' && bash -c 'cd /home/zak/Dev/PurgeGame/degenerus-audit && npx hardhat compile 2>&1 | tail -3 && npx hardhat test test/gas/LootboxOpenGas.test.js 2>&1 | tail -10 && npx hardhat test test/stat/SurfaceRegression.test.js 2>&1 | tail -10' && bash -c 'cd /home/zak/Dev/PurgeGame/degenerus-audit && for i in 1 2 3; do echo "=== Run $i ==="; npm run test:stat 2>&1 | tail -5; done  # Expected: all 3 runs report passing'</automated>
  </verify>
  <acceptance_criteria>
    - LBX-02 NatSpec header in `test/gas/LootboxOpenGas.test.js` contains all required tokens: `Phase 269 LBX-02`, `Worst-case dimensions:`, `55%-tickets-path`, `roll < 11`, `ticketsScaled != 0`, `cold storage`, `max ticketsScaled`, `[30, 80] gas`, `bytecode shrink` (each greppable).
    - LBX-02 envelope constants `LBX_02_PER_OPEN_DELTA_MIN = 30` and `LBX_02_PER_OPEN_DELTA_MAX = 80` declared at file scope.
    - REF-CAPTURE protocol present (`PRE_DELETION_GAS_REF` + `POST_DELETION_GAS_REF` + `V36_DEPLOYED_BYTECODE_BYTES_REF` constants + REF-CAPTURE print lines for each).
    - SURF-03 re-baseline: `PHASE_269_CLOSE_BASELINE` const introduced in `test/stat/SurfaceRegression.test.js`; SURF-03 it block at L752 uses `PHASE_269_CLOSE_BASELINE` (not `V36_BASELINE`); SURF-03 it block title contains `Phase-269-close baseline` (not `v36.0 baseline 1c0f0913`).
    - SURF-01/02/04 it blocks UNTOUCHED — still pass `V36_BASELINE` to their assertions.
    - GASPIN-02 mutation matches Task 1 RCA's chosen path: if option (a) re-pin → mutation-map-named file(s) contain updated literal value(s) + inline NatSpec describing current stabilization shape; if option (b) ordering-fix → surgical fix applied at the named line range + NatSpec; if option (c) split-files → new sibling file created + original file's affected describe removed; if option (d) no stabilization needed → no GASPIN-02-specific changes in this sub-step. Inline NatSpec at any affected fixture follows `feedback_no_history_in_comments.md` (no "was X, now Y" prose).
    - `package.json` test:stat script updated ONLY if Task 1 RCA chose split-files (option c); otherwise UNTOUCHED.
    - `npx hardhat compile` exits 0 with no new warnings.
    - `npx hardhat test test/gas/LootboxOpenGas.test.js` exits 0 (REF-CAPTURE first-run prints expected; assertions pass after executor pins PRE/POST constants).
    - `npx hardhat test test/stat/SurfaceRegression.test.js` exits 0; v37.0 SURF-01..04 describe passes (SURF-03 against PHASE_269_CLOSE_BASELINE; SURF-01/02/04 against V36_BASELINE).
    - `npm run test:stat` exits 0 across 3 consecutive runs with no flaky failures (GASPIN-03 acceptance).
    - `git status --porcelain` shows the expected modified files (3 always + 1-2 conditionally) and NO uncommitted contract changes (the contract was committed at Task 3).
    - NO commit landed at this task (verify via `git log -1 --format=%s` returns the Task 3 contract commit subject — last commit is the Task 3 contract commit; Task 5 will commit the test diff).
  </acceptance_criteria>
  <done>LBX-02 v37.0 describe appended to `test/gas/LootboxOpenGas.test.js`; SURF-03 re-baseline applied to `test/stat/SurfaceRegression.test.js`; GASPIN-02 stabilization implemented per Task 1 RCA's chosen path; `package.json` updated only if split-files chosen; all three (`hardhat test test/gas/LootboxOpenGas.test.js` + `hardhat test test/stat/SurfaceRegression.test.js` + 3× `npm run test:stat`) pass; working tree dirty (3-5 files modified, uncommitted) ready for Task 5 BLOCKING approval gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: [BLOCKING] LBX-02 + GASPIN-02 + SURF-03 USER-APPROVED batched test commit gate</name>
  <files>test/gas/LootboxOpenGas.test.js, test/stat/SurfaceRegression.test.js, test/gas/AdvanceGameGas.test.js, test/gas/Phase264GasRegression.test.js, package.json</files>
  <read_first>
    - `feedback_no_contract_commits.md` — NEVER commit `test/` changes without explicit user approval (test-tree treated identically to contract-tree per Phase 268 D-268-APPROVAL-02 carry).
    - `feedback_batch_contract_approval.md` — present ONE diff for all phase test changes, get ONE approval.
    - `feedback_wait_for_approval.md` — Present fix and WAIT for explicit approval before committing.
    - `feedback_never_preapprove_contracts.md` — Orchestrator/agent must NEVER tell the executor that test changes are "pre-approved".
    - `feedback_manual_review_before_push.md` — NO `git push` by the agent.
    - .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-CONTEXT.md `<decisions>` D-269-COMMITS-01 (single batched test commit) + D-269-APPROVAL-02.
  </read_first>
  <what-built>
    Task 4 implemented the batched test-tree changes per D-269-COMMITS-01:
    - LBX-02 v37.0 describe in `test/gas/LootboxOpenGas.test.js` (worst-case 55%-tickets-path gas savings ∈ [30, 80] + bytecode shrink, both via REF-CAPTURE pinning).
    - GASPIN-02 stabilization per Task 1 RCA's chosen path in `test/gas/AdvanceGameGas.test.js` and/or `test/gas/Phase264GasRegression.test.js` (re-pin / ordering-fix / split-files / no-op).
    - SURF-03 re-baseline in `test/stat/SurfaceRegression.test.js` (PHASE_269_CLOSE_BASELINE const introduced; SURF-03 it block at L752 anchored to Phase-269-close HEAD; SURF-01/02/04 unchanged at V36_BASELINE).
    - Optional `package.json` test:stat wiring delta (only if split-files chosen).
    Working tree dirty; uncommitted.
  </what-built>
  <how-to-verify>
    The agent presents the diff and STOPS for explicit user approval. The agent body for this checkpoint executes the following sequence:

    1. Run `git status --porcelain test/ package.json` to confirm only the expected files are dirty (3 always: `test/gas/LootboxOpenGas.test.js` + `test/stat/SurfaceRegression.test.js` + the file named in Task 1 mutation map; OR more if split-files).
    2. Run `git status --porcelain contracts/` to confirm NO contract changes are pending (the LBX-01 contract change was committed at Task 3; the test commit MUST NOT include contract changes per D-269-COMMITS-01).
    3. Run `git diff test/ package.json` and present the full diff verbatim to the user. The diff covers:
       - LBX-02 describe addition in `test/gas/LootboxOpenGas.test.js`.
       - SURF-03 re-baseline in `test/stat/SurfaceRegression.test.js`.
       - GASPIN-02 stabilization (varies per Task 1 RCA path).
       - Optional package.json delta.
    4. Run the 3 LBX-02 evidence greps:
       - `grep -c "Phase 269 LBX-02" test/gas/LootboxOpenGas.test.js` (expect >= 1).
       - `grep -cE "Worst-case dimensions|55%-tickets-path" test/gas/LootboxOpenGas.test.js` (expect >= 1).
       - `grep -cE "PRE_DELETION_GAS_REF|REF-CAPTURE" test/gas/LootboxOpenGas.test.js` (expect >= 1).
    5. Run the SURF-03 re-baseline evidence greps:
       - `grep -c "PHASE_269_CLOSE_BASELINE" test/stat/SurfaceRegression.test.js` (expect >= 1).
       - `grep -c "D-269-SURF03-01" test/stat/SurfaceRegression.test.js` (expect >= 1).
       - `grep -c "post-LBX-01-cleanup" test/stat/SurfaceRegression.test.js` (expect >= 1).
       - `grep -cE "v36\\.0 baseline 1c0f0913.*\\(D-268-SURF03-01" test/stat/SurfaceRegression.test.js` (expect `0` — old SURF-03 title with v36.0 anchor removed).
    6. Cite Task 1 RCA's chosen stabilization path (option a/b/c/d) + the files named in the Task 4 mutation map; cite each grep proof showing the mutation landed.
    7. Run `npx hardhat test test/gas/LootboxOpenGas.test.js` and cite the success output.
    8. Run `npx hardhat test test/stat/SurfaceRegression.test.js` and cite the success output.
    9. Run `for i in 1 2 3; do npm run test:stat 2>&1 | tail -5; done` and cite all 3 success outputs (GASPIN-03 evidence).
    10. **STOP. Wait for the user reply containing the literal token `approved`.** Per `feedback_wait_for_approval.md` + `feedback_never_preapprove_contracts.md`, the agent does NOT proceed without the explicit token.
    11. ONLY after the user reply contains `approved`: run `git add test/ package.json` (or the specific files named in the mutation map) and `git commit -m "test(269): lootbox-open gas savings + SURF-05 gas-pin stabilization + SURF-03 re-baseline [LBX-02, GASPIN-01..03, SURF-03]"` with an optional 3-5 line body restating the RCA mechanism + chosen stabilization path.
    12. Run `git log -1 --format="%H %s"` and report the new HEAD sha (this sha becomes the Phase-269-close-test-commit anchor cited in Task 6 SUMMARY).
    13. DO NOT push. Per `feedback_manual_review_before_push.md`, user reviews the diff manually before any push.

    If the user reply contains anything other than the explicit `approved` token, the agent must NOT commit. Per `feedback_never_preapprove_contracts.md`, NO partial-credit interpretations.

    If the user explicitly DISAPPROVES, the agent identifies which sub-step is contested and re-presents the gate after addressing the feedback (without committing intermediate states).
  </how-to-verify>
  <resume-signal>Type `approved` to commit the batched test diff, or describe the requested deviation. Per `feedback_never_preapprove_contracts.md`, partial-credit tokens (e.g., "approve" without `d`) do NOT count.</resume-signal>
  <action>
    Execute the LBX-02 + GASPIN-02 + SURF-03 USER-APPROVED batched test commit gate per the how-to-verify sequence above (steps 1-13). The gate is BLOCKING: confirm no contracts/ dirty (status porcelain check); present the batched test diff via diff on test/ + package.json; cite each evidence grep (LBX-02 Phase 269 + Worst-case + REF-CAPTURE; SURF-03 PHASE_269_CLOSE_BASELINE + D-269-SURF03-01 + post-LBX-01-cleanup); cite Task 1 RCA chosen stabilization path + mutation-map files; run hardhat test on LootboxOpenGas + SurfaceRegression + 3 times npm run test:stat for GASPIN-03 cleanliness evidence; STOP and wait for the literal user token `approved`. ONLY after the token is captured: stage the test/package files actually mutated per Task 1 mutation map, then commit with subject `test(269): lootbox-open gas savings + SURF-05 gas-pin stabilization + SURF-03 re-baseline [LBX-02, GASPIN-01..03, SURF-03]`. Record the new HEAD sha for Task 6 SUMMARY anchor. Do NOT push (per `feedback_manual_review_before_push.md`). Per `feedback_never_preapprove_contracts.md`, do NOT proceed on partial-credit tokens; re-present the gate.
  </action>
  <verify>
    <automated>bash -c 'git log -1 --format="%s" | grep -c "^test(269): lootbox-open gas savings + SURF-05 gas-pin stabilization + SURF-03 re-baseline \\[LBX-02, GASPIN-01..03, SURF-03\\]"  # Expected: 1' && bash -c 'git diff HEAD~1 HEAD --stat | grep -cE "test/gas/LootboxOpenGas\\.test\\.js|test/stat/SurfaceRegression\\.test\\.js"  # Expected: >= 2 (LBX-02 + SURF-03 always present)' && bash -c 'git diff HEAD~1 HEAD --stat | grep -c "contracts/"  # Expected: 0 (test commit must NOT touch contracts/)' && bash -c 'git diff HEAD~1 HEAD --stat | grep -c ".planning/"  # Expected: 0 (test commit must NOT touch .planning/)' && bash -c 'git log origin/main..HEAD 2>/dev/null | wc -l  # informational — should show at least 2 unpushed commits (Task 3 + Task 5); agent must NOT have run git push'</automated>
  </verify>
  <acceptance_criteria>
    - User reply containing the literal token `approved` captured at the gate (recorded in conversation log).
    - `git log -1 --format="%s"` returns exactly `test(269): lootbox-open gas savings + SURF-05 gas-pin stabilization + SURF-03 re-baseline [LBX-02, GASPIN-01..03, SURF-03]`.
    - `git diff HEAD~1 HEAD --stat` shows at minimum 2 files changed (`test/gas/LootboxOpenGas.test.js` + `test/stat/SurfaceRegression.test.js`); plus 1-2 more files if GASPIN-02 chose options (a)/(b)/(c).
    - `git diff HEAD~1 HEAD --stat` shows ZERO files in `contracts/` (test commit must NOT touch contracts; LBX-01 committed at Task 3).
    - `git diff HEAD~1 HEAD --stat` shows ZERO files in `.planning/` (Task 6 commits the SUMMARY/STATE separately).
    - The new HEAD sha is recorded in the conversation log.
    - NO `git push` performed (per `feedback_manual_review_before_push.md`); the commit is local-only.
  </acceptance_criteria>
  <done>User approved with the literal `approved` token; `test(269): lootbox-open gas savings + SURF-05 gas-pin stabilization + SURF-03 re-baseline [LBX-02, GASPIN-01..03, SURF-03]` commit landed locally with the batched test diff (no contract, no planning); no push performed.</done>
</task>

<task type="auto">
  <name>Task 6: Phase-close SUMMARY + STATE.md flip + LBX-03 Phase 271 anchor recording</name>
  <files>.planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-01-SUMMARY.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/268-degenerette-statistical-validation-cross-surface-preservation/268-01-SUMMARY.md (entirety) — Phase 269 SUMMARY mirrors this structure exactly: frontmatter + Overview + Per-Task Atomic-Commit Log + Per-REQ Tally + Cross-Phase Cross-Cite Density + Project-Feedback-Rules Honored + Commit-Readiness Register (3 subsections) + Open Items / Deferrals + Closure Signal + Notes + Self-Check.
    - .planning/STATE.md — current state at Phase 268 SHIPPED; Task 6 flips Current Position to "Phase 270 next", increments progress.completed_phases 2 → 3, progress.completed_plans 2 → 3, progress.percent 40 → 60.
    - This `269-01-PLAN.md` `## Root Cause (GASPIN-01)` section (appended by Task 1) — Task 6 SUMMARY's Per-REQ Tally rows for GASPIN-01..03 cite the inline RCA mechanism + chosen stabilization path.
    - contracts/modules/DegenerusGameLootboxModule.sol post-Task-3 HEAD — Task 6 SUMMARY's `## Phase 271 §3.A delta-surface inputs` section records the post-deletion line numbers for the 4 hash2/bit-slice callsites in `_resolveLootboxRoll`. v36.0 anchors (per CONTEXT.md `<deferred>` Phase 271 §3.A LBX-03 audit-trail row + ROADMAP success criterion 3): L1548 / L1569 / L1585 / L1599. Post-deletion anchors are computed via `grep -n 'EntropyLib.hash2\|seed >> ' contracts/modules/DegenerusGameLootboxModule.sol` at Phase-269-close HEAD, filtered to the 4 specific callsites inside `_resolveLootboxRoll` (line range L1548 ± shift to L1646 ± shift).
    - .planning/REQUIREMENTS.md LBX-01..03 + GASPIN-01..03 (6 requirements; Task 6 SUMMARY Per-REQ Tally enumerates each with file+line evidence + verifying grep recipe).
    - .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-CONTEXT.md `<decisions>` D-269-CLOSURE-02 (commit-readiness register §9.NN three-subsection format; NO §iv awaiting-approval).
  </read_first>
  <action>
    Author `269-01-SUMMARY.md` mirroring `268-01-SUMMARY.md` structure exactly, and apply STATE.md flips. Single atomic commit covering both.

    **Sub-step 6a — Compute post-deletion line numbers for the 4 hash2/bit-slice callsites in `_resolveLootboxRoll`.** Run `grep -n "EntropyLib.hash2\|seed >> " contracts/modules/DegenerusGameLootboxModule.sol` at Phase-269-close HEAD. Filter to lines inside `_resolveLootboxRoll` (definition at L1548 ± shift; closing brace at L1646 ± shift). The 4 v36.0 anchors per CONTEXT.md / ROADMAP are L1548 (function definition; could shift down to L1548 if nothing above moved, OR stay at L1548 since the deletion is below the function definition), L1569 (the `roll = uint16(seed >> 40) % 20` line — this is the bit-slice that DOES NOT move because it's above the deletion at L1573-1580), L1585 (the DGNRS-tier `_lootboxDgnrsReward` callsite, which IS below the deletion → line number shifts down by 4 due to the -5/+1 net diff), L1599 (the WWXRP `LOOTBOX_WWXRP_PRIZE` callsite, which IS below the deletion → shifts down by 4). Record actual computed line numbers (e.g., L1548 / L1569 / L1581 / L1595 at post-deletion HEAD vs L1548 / L1569 / L1585 / L1599 at v36.0). Cross-cite v36.0 → Phase-269-close mapping for each anchor.

    **Sub-step 6b — Author `.planning/phases/269-…/269-01-SUMMARY.md`** mirroring `268-01-SUMMARY.md`:

    Frontmatter (mirror 268-01-SUMMARY format with appropriate Phase 269 values):
    ```yaml
    ---
    phase: 269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning
    phase_number: 269
    plan: 269-01
    plan_id: 269-01
    plan_number: 01
    type: summary
    status: complete
    milestone: v37.0
    milestone_name: Degenerette Recalibration + Maintenance Bundle
    completed: 2026-05-10  # or actual date when Task 6 runs
    duration: ~Xh
    deliverable: "LBX-01 dead-branch deletion (single-hunk contract commit) + LBX-02 v37.0 LootboxOpenGas describe (55%-tickets-path worst-case gas savings ∈ [30, 80] + bytecode shrink) + GASPIN-01..03 RCA-inline + stabilization + SURF-03 re-baseline anchor"
    requirements-completed: [LBX-01, LBX-02, LBX-03, GASPIN-01, GASPIN-02, GASPIN-03]
    baseline: 1c0f09132d7439af9881c56fe197f81757f8164a
    baseline_signal: MILESTONE_V36_AT_HEAD_1c0f09132d7439af9881c56fe197f81757f8164a
    phase_267_close_sha: e1136071
    phase_268_close_sha: 4b277aaf
    contract_commit_sha: <Task 3 sha>
    test_commit_sha: <Task 5 sha>
    phase_close_sha: pending-task-6-commit
    milestone_closure_signal: pending-phase-271
    milestone_closure_target: MILESTONE_V37_AT_HEAD_<sha>
    ---
    ```

    Then sections:
    - `# Phase 269 — Lootbox Dead-Branch Cleanup + SURF-05 Gas-Pin Re-Pinning (SUMMARY)`
    - `## Overview` — Restate the 2-workstream phase boundary (LBX-01..03 dead-branch cleanup + GASPIN-01..03 SURF-05 stabilization); cite the byte-equivalence proof (triple-defense caller-clamp invariant D-269-LBX-CALLERCLAMP-CARRY-01); state 6/6 requirements PASS; reference the 4 atomic-commit boundaries (Task 1 RCA-inline + Task 3 contract + Task 5 test + Task 6 close).
    - `## Per-Task Atomic-Commit Log` — Table mirroring 268-01-SUMMARY's format with columns: # | Subject | SHA short | AGENT/USER | Files (counts). Rows for each commit (PLAN.md authoring + any gsd-plan-checker revisions + Task 1 + Task 3 + Task 5 + Task 6).
    - `## Per-REQ Tally (6 of 6 PASS)` — Table with columns: REQ ID | File evidence | Verifying grep recipe | ✓. One row per requirement:
      - LBX-01: contract file L1573-1580 deletion; `grep -c "if (targetLevel < currentLevel)" contracts/modules/DegenerusGameLootboxModule.sol` returns 2; `npx hardhat compile` exits 0.
      - LBX-02: `test/gas/LootboxOpenGas.test.js` Phase 269 LBX-02 describe; `grep -c "Phase 269 LBX-02"` returns >= 1; `[30, 80] gas` assertion present; `npx hardhat test test/gas/LootboxOpenGas.test.js` exits 0.
      - LBX-03: Phase 271 §3.A delta-surface inputs section in this SUMMARY (see sub-step 6c below).
      - GASPIN-01: `## Root Cause (GASPIN-01)` section in `269-01-PLAN.md`; mechanism named; chosen stabilization path documented.
      - GASPIN-02: Task 4 stabilization-path implementation per Task 1 RCA's mutation map; inline NatSpec at the affected fixture per `feedback_no_history_in_comments.md`.
      - GASPIN-03: `npm run test:stat` exits 0 across 3 consecutive runs at Task 4/5; both standalone and combined-suite gas pins agree.
    - `## Cross-Phase Cross-Cite Density` — Forward consumer table:
      - Phase 271 §3.A surface (f): lootbox dead-branch removal byte-equivalence audit input (caller-clamp invariant at L860-884).
      - Phase 271 §3.A delta-surface row (LBX-03 audit-trail authoring): post-deletion line numbers for 4 hash2/bit-slice callsites (from sub-step 6a).
      - Phase 271 §5 REG-01: v36.0 closure-signal non-widening; lootbox v36.0-refactored bodies byte-identical EXCEPT for LBX-01 dead-branch deletion (no-behavior-change cleanup; audit-trail row at AUDIT-01 §3.A).
      - Phase 271 §4 surface (f) verdict (expected SAFE_BY_STRUCTURAL_CLOSURE per CONTEXT.md `<deferred>`): caller-clamp invariant proven during 269-CONTEXT discussion.
    - `## Project-Feedback-Rules Honored` — Table mirroring 268-01-SUMMARY format. Rows for: `feedback_no_contract_commits.md` (Task 3 contract + Task 5 test commits both USER-APPROVED at `checkpoint:human-verify` gates); `feedback_batch_contract_approval.md` (single contract commit at Task 3 + single batched test commit at Task 5); `feedback_never_preapprove_contracts.md` (no pre-approval claimed at any gate); `feedback_wait_for_approval.md` (explicit `approved` tokens captured); `feedback_manual_review_before_push.md` (NO git push performed); `feedback_no_history_in_comments.md` (NO trace comment at LBX-01 deletion site; inline NatSpec at GASPIN-02 affected fixture describes current state); `feedback_no_dead_guards.md` (NO `require(targetLevel >= currentLevel)` invariant; pure deletion); `feedback_gas_worst_case.md` (LBX-02 worst-case 55%-tickets-path derived FIRST in NatSpec header); `feedback_design_intent_before_deletion.md` (triple-defense + game-theory analysis recorded in 269-CONTEXT D-269-LBX-CALLERCLAMP-CARRY-01 + D-269-LBX-GAMETHEORY-CARRY-01 before deletion); `feedback_rng_backward_trace.md` N/A explicit (no rngLocked / commitment-window changes; BURNIE-lootbox `lootboxDay = 0` v38+ deferred per CONTEXT.md `<deferred>`).
    - `## Commit-Readiness Register` — THREE subsections per D-269-CLOSURE-02:
      - `### §i USER-APPROVED contracts (1 commit at Task 3)`: row for Task 3 commit subject + files + approval evidence.
      - `### §ii USER-APPROVED tests (1 commit at Task 5)`: row for Task 5 commit subject + files + approval evidence.
      - `### §iii AGENT-COMMITTED planning artifacts (3+ commits)`: rows for PLAN.md authoring + any gsd-plan-checker revisions + Task 1 RCA-inline revision + Task 6 SUMMARY/STATE (this commit).
      - NO §iv awaiting-approval subsection.
    - `## Open Items / Deferrals` — Forward to Phase 270 + Phase 271. Reference BURNIE-lootbox `lootboxDay = 0` v38+ deferral.
    - `## Closure Signal` — `pending-phase-271` (carry).
    - `## Notes` — Mirror 268-01-SUMMARY format. Bullets for: (a) RCA-inline discipline (D-269-RCA-01); (b) byte-equivalence proof (triple-defense + game-theory); (c) GASPIN-02 chosen stabilization path with rationale; (d) any user-approved deviations at Task 3 or Task 5 gates documented in commit bodies + §ii row.
    - `## Self-Check: PASSED` — Mirror 268-01-SUMMARY format with verifications enumerated.

    **Sub-step 6c — Add `## Phase 271 §3.A delta-surface inputs` section to the SUMMARY (LBX-03 cross-phase coordination).** New section appended at end-of-document (after Self-Check). Content:
    - Table with columns: Callsite | v36.0 anchor | Phase-269-close anchor | Bit-slice budget unchanged.
    - 4 rows (one per `_resolveLootboxRoll` hash2/bit-slice callsite per CONTEXT.md `<deferred>` Phase 271 §3.A row): L1548 (function definition) | L1548 (or post-shift) | L1548-ish (function def doesn't shift — deletion is below it) | UNAFFECTED; L1569 (`roll = uint16(seed >> 40) % 20`) | L1569 | L1569 (unaffected by deletion below) | UNAFFECTED; L1585 (`_lootboxDgnrsReward` callsite or related hash2/bit-slice site in DGNRS branch) | L1585 | L1581 (post-shift: -4 due to deletion) | UNAFFECTED; L1599 (WWXRP `LOOTBOX_WWXRP_PRIZE` / bit-slice site) | L1599 | L1595 (post-shift: -4 due to deletion) | UNAFFECTED.
    - NOTE the post-shift offsets above are illustrative; Task 6 computes the ACTUAL post-deletion anchors at HEAD via the `grep -n` command in sub-step 6a — the executor records the actual measured line numbers, NOT the illustrative ones.
    - State the cross-phase contract: "Phase 271 §3.A row author (per Phase 271 plan + the LBX-03 audit-trail-row requirement) consumes this section directly without re-reading `contracts/modules/DegenerusGameLootboxModule.sol` — the v36.0 → Phase-269-close line-number mapping is the LBX-03 audit-trail-row content." Per CONTEXT.md `<plan_shape_locked_by_context>` LBX-03 cross-phase coordination: "This is how LBX-03 is materially advanced in Phase 269."

    **Sub-step 6d — Apply `.planning/STATE.md` flips (single Edit invocation):**
    - Update `last_updated:` to the current ISO timestamp.
    - Update `last_activity:` with a new bullet: "2026-05-10 -- Phase 269 SHIPPED. <Task 3 SHA short> LBX-01 contract commit (`feat(269): delete unreachable BURNIE-conversion branch in _resolveLootboxRoll [LBX-01]`; -5/+1 LOC). <Task 5 SHA short> batched test commit (`test(269): lootbox-open gas savings + SURF-05 gas-pin stabilization + SURF-03 re-baseline [LBX-02, GASPIN-01..03, SURF-03]`; <X> files / <+Y/-Z> LOC). GASPIN-01 RCA inline in `269-01-PLAN.md`; <chosen stabilization path>. 6 of 6 LBX-01..03 + GASPIN-01..03 requirements PASS. Next: `/gsd-discuss-phase 270` for the Post-v32.0 Deferred-Commit Adversarial Sub-Audit."
    - Update `progress.completed_phases`: 2 → 3.
    - Update `progress.completed_plans`: 2 → 3.
    - Update `progress.percent`: 40 → 60.
    - Update `## Current Position` block: Phase advances to 270 (or remains 269 but marked SHIPPED depending on STATE.md convention); resume_file points to Phase 270 CONTEXT once it exists.
    - Update `**Current focus:**` banner in the `## Project Reference` block to point at Phase 270 next.
    - Update `**v37.0 Degenerette Recalibration + Maintenance Bundle**` block under `## Active Milestone` to add a Phase 269 SHIPPED bullet (mirroring the existing Phase 267 SHIPPED and Phase 268 SHIPPED bullets): single-commit subjects + LOC + 6/6 requirements PASS.

    **Sub-step 6e — Commit the SUMMARY + STATE flip as a single AGENT-COMMITTED docs commit:**
    `git add .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-01-SUMMARY.md .planning/STATE.md` then `git commit -m "docs(269): phase 269 summary + commit-readiness register"` with an optional 3-5 line body restating the 6/6 PASS + the closure-signal-pending-phase-271 carry. NO `git push`.

    **Sub-step 6f — Optional follow-up tracking-tree commit (mirrors Phase 268 precedent).** Run `gsd-sdk query roadmap update-plan-progress 269` + `gsd-sdk query requirements mark-complete LBX-01 LBX-02 LBX-03 GASPIN-01 GASPIN-02 GASPIN-03`. If any tracking-tree mutation results from those SDK calls (ROADMAP.md Progress row flip from `0/0` to `1/1`; REQUIREMENTS.md status flips Pending → Complete), commit as `docs(phase-269): update tracking after phase 269 close`. If no mutations result (SDK invocation idempotent), skip this commit.
  </action>
  <verify>
    <automated>bash -c 'test -f .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-01-SUMMARY.md  # Expected: file present' && bash -c 'grep -cE "LBX-0[1-3]" .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-01-SUMMARY.md  # Expected: >= 3' && bash -c 'grep -cE "GASPIN-0[1-3]" .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-01-SUMMARY.md  # Expected: >= 3' && bash -c 'grep -c "6 of 6" .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-01-SUMMARY.md  # Expected: >= 1' && bash -c 'grep -c "Phase 271 §3.A delta-surface inputs" .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-01-SUMMARY.md  # Expected: 1' && bash -c 'grep -cE "L1548|L1569|L1585|L1599" .planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/269-01-SUMMARY.md  # Expected: >= 4' && bash -c 'grep -c "Phase 269 SHIPPED" .planning/STATE.md  # Expected: 1' && bash -c 'grep -c "completed_phases: 3" .planning/STATE.md  # Expected: 1' && bash -c 'grep -c "completed_plans: 3" .planning/STATE.md  # Expected: 1' && bash -c 'grep -c "percent: 60" .planning/STATE.md  # Expected: 1' && bash -c 'git log -1 --format=%s | grep -c "^docs(269): phase 269 summary + commit-readiness register"  # Expected: 1'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/269-…/269-01-SUMMARY.md` exists.
    - SUMMARY has frontmatter with `requirements-completed: [LBX-01, LBX-02, LBX-03, GASPIN-01, GASPIN-02, GASPIN-03]` (all 6 IDs present).
    - SUMMARY contains all 6 REQ IDs in per-REQ tally (greppable as `LBX-01`, `LBX-02`, `LBX-03`, `GASPIN-01`, `GASPIN-02`, `GASPIN-03`).
    - SUMMARY contains `6 of 6` PASS count.
    - SUMMARY contains `## Phase 271 §3.A delta-surface inputs` section (greppable as exact string).
    - SUMMARY's Phase 271 §3.A section enumerates ≥ 4 line-number anchors (greppable as `L1548|L1569|L1585|L1599` or their post-shift equivalents — at least 4 distinct line-number anchors documented).
    - SUMMARY contains 3-subsection commit-readiness register (`### §i USER-APPROVED contracts` + `### §ii USER-APPROVED tests` + `### §iii AGENT-COMMITTED planning artifacts`); NO `### §iv awaiting-approval`.
    - SUMMARY's Project-Feedback-Rules table cites ≥ 8 feedback rules (the 10 listed in sub-step 6b).
    - STATE.md has `Phase 269 SHIPPED` token.
    - STATE.md `progress.completed_phases: 3` (was 2).
    - STATE.md `progress.completed_plans: 3` (was 2).
    - STATE.md `progress.percent: 60` (was 40).
    - `git log -1 --format=%s` returns a line starting with `docs(269): phase 269 summary + commit-readiness register`.
    - Optional follow-up tracking-tree commit landed IF SDK calls produced mutations (subject: `docs(phase-269): update tracking after phase 269 close`).
    - NO `git push` performed.
  </acceptance_criteria>
  <done>SUMMARY authored at `269-01-SUMMARY.md` with 6/6 PASS tally + 3-subsection commit-readiness register + `## Phase 271 §3.A delta-surface inputs` section (LBX-03 cross-phase anchor recording); STATE.md flipped (Phase 269 SHIPPED; progress 40 → 60); `docs(269): phase 269 summary + commit-readiness register` commit landed locally with no push; optional tracking-tree follow-up commit lands only if SDK mutations result.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → `_resolveLootboxRoll` (private) | Internal-only callsite; only reachable via 2 callsites in `_resolveLootboxCommon` (L910 + L938). Layer-1 (`openLootBox` L557-559) + Layer-2 (`_resolveLootboxCommon` L882-884) caller-clamp invariants unconditionally normalize `targetLevel` before `_resolveLootboxRoll` is reached. No external entry point; no upgrade hook; no admin function. |
| player → `openLootBox` / `openBurnieLootBox` / `resolveLootboxDirect` / `resolveRedemptionLootbox` | External entry points unaffected by LBX-01. ETH-lootbox `day` snapshotted at buy by `MintModule.sol` L1409-1410 → seed `keccak256(rngWord, player, day, amount)` is fixed; player cannot grind `targetLevel` via timing. BURNIE-lootbox `day` fallback at L623-626 → flagged for v38+ deferral per CONTEXT.md `<deferred>` (NOT a Phase 269 concern). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-269-01 | Tampering | LBX-01 dead-branch deletion changes player-facing behavior under some timing/state combo | accept (no behavior change) | **Byte-equivalence proven structurally** via triple-defense caller-clamp invariant (D-269-LBX-CALLERCLAMP-CARRY-01): Layer-1 `openLootBox` L557-559 + Layer-2 `_resolveLootboxCommon` L882-884 unconditionally execute `if (targetLevel < currentLevel) targetLevel = currentLevel;` before `_resolveLootboxRoll` (Layer-3 deleted) is reachable. Therefore the deleted inner `if (targetLevel < currentLevel)` check at L1574 was structurally dead — `targetLevel < currentLevel` cannot hold at the deletion site for ANY caller. Player-facing behavior at any timing on any open path (ETH lootbox, BURNIE lootbox, direct resolve, redemption resolve) is byte-equivalent. Verified by Task 2 acceptance criteria (`grep -c "if (targetLevel < currentLevel)"` returns 2: layers 1 + 2 remain). Phase 271 §4 surface (f) adversarial pass (sequenced) re-verifies via `/contract-auditor` + `/zero-day-hunter`; expected verdict SAFE_BY_STRUCTURAL_CLOSURE per CONTEXT.md `<deferred>`. |
| T-269-02 | Tampering | Player exploits timing to game `targetLevel` distribution after LBX-01 cleanup | accept (no advantage) | **Game-theory neutrality proven** (D-269-LBX-GAMETHEORY-CARRY-01): `seed = keccak256(rngWord, player, day, amount)` is fixed for ETH lootboxes since `day = lootboxDay[index][player]` is snapshotted at buy by `MintModule.sol` L1409-1410. Player who holds past 7-day grace period (`openLootBox` L549-552) gets `baseLevel = purchaseLevel` instead of `graceLevel`; most rolls land below currentLevel → triple-defense clamp pulls `targetLevel` up to currentLevel → tickets queue at currentLevel (immediate next cycle). NO tickets lost; NO past-bucket queueing; NO timing-pick advantage. Player loses only the 10% far-future bucket-spread variety (`_rollTargetLevel` 90% near-future / 10% far-future split at L812-826) — far-future spread collapses to "currentLevel only" for holds-past-grace, but no tickets and no advantage. |
| T-269-03 | Information Disclosure | LBX-01 deletion changes deployed-bytecode hash → downstream consumers / off-chain indexers reading deployedBytecode mismatch | accept (intentional change) | **Intentional bytecode shrink** — LBX-02 Task 4 assertion enforces `post-deletion bytecode_bytes < V36_DEPLOYED_BYTECODE_BYTES_REF` (strictly smaller). Off-chain indexers reading deployedBytecode hashes must update to the post-Phase-269-close artifact; this is documented in Phase 271 §3.A delta-surface row (LBX-03 cross-phase coordination). No public ABI change (`_resolveLootboxRoll` is `private`); no event signature change; no storage layout change. |
| T-269-04 | Denial of Service | GASPIN-02 stabilization (re-pin / ordering-fix / split-files) masks a real regression in advanceGame margin under combined-suite ordering | mitigate | Task 1 RCA confirms-or-rejects the drift's reality. If reproducible: mechanism named from {state pollution, gas-meter snapshot ordering, fixture-loader caching, hardhat node restart timing}; chosen stabilization path is least-invasive per D-269-STAB-01. Re-pin path documents combined-suite-stable value WITHOUT relaxing the 1.99× margin invariant (per CONTEXT.md `<canonical_refs>` — `test/gas/AdvanceGameGas.test.js` L1467 `MAX_BLOCK_GAS = 30_000_000n` + L1468 `REQUIRED_MARGIN = 1.99` UNTOUCHED). GASPIN-03 acceptance enforces 3-consecutive-run cleanliness at Task 4/5 — neither standalone nor combined-suite drift across runs. If drift is "not reproducible at Phase 268 close HEAD" (option d), GASPIN-02 is no-op and acceptance is the cleanliness check alone. |
| T-269-05 | Elevation of Privilege | LBX-01 deletion accidentally widens the call graph of `_resolveLootboxRoll` (e.g., changes private → internal/external) | mitigate | Task 2 acceptance criterion `grep -rn "_resolveLootboxRoll" contracts/` returns exactly 3 lines (1 definition at L1548 ± shift + 2 callsites at L910 + L938). The signature line `function _resolveLootboxRoll(...) private returns (...)` is UNTOUCHED in Task 2's diff — verified via Task 2 acceptance `git diff HEAD~1 HEAD -- contracts/modules/DegenerusGameLootboxModule.sol` showing the diff hunk is INSIDE the function body (L1573-1580 region), NOT at the function signature. |
| T-269-06 | Repudiation | LBX-01 deletion commit lands without explicit user approval, contradicting `feedback_no_contract_commits.md` | mitigate | Task 3 BLOCKING `checkpoint:human-verify` gate captures explicit user `approved` token. Per `feedback_never_preapprove_contracts.md`, agent does NOT proceed without the literal token. Per `feedback_wait_for_approval.md`, agent re-presents the gate on partial-credit tokens. Per `feedback_manual_review_before_push.md`, NO `git push` performed — user reviews the commit diff manually before any push. Task 5 BLOCKING gate enforces the same discipline for the batched test commit. |

**Out-of-scope (deferred per CONTEXT.md `<deferred>`):** BURNIE-lootbox `lootboxDay = 0` fallback at `openBurnieLootBox` L623-626 (potential RNG-grindable IF a BURNIE-lootbox buy path leaves `lootboxDay` unset; ETH-lootbox path is safe — `MintModule.sol` L1409-1410 sets `lootboxDay` unconditionally at buy). Confirmation requires tracing all BURNIE-lootbox buy paths; routed to v38+ backlog or a dedicated maintenance phase if confirmed exploitable. NOT a Phase 269 concern.

**Adversarial pass deferral:** Phase 271 §4 surface (f) — lootbox dead-branch removal byte-equivalence audit. `/contract-auditor` + `/zero-day-hunter` SEQUENTIAL after full §4 draft. Expected verdict SAFE_BY_STRUCTURAL_CLOSURE (caller-clamp invariant proven during 269-CONTEXT discussion + verified by Task 2/3 acceptance criteria). `/economic-analyst` + `/degen-skeptic` NOT in scope for Phase 269 (behavior-preserving cleanup + test-infrastructure stabilization has zero economic-mechanism implications); inclusion in Phase 271 §4 deferred to Phase 271 discuss-phase per CONTEXT.md `<deferred>`.
</threat_model>

<verification>
Plan-wide verification gates (applied at Task 6 phase close):

1. **6 of 6 requirements PASS** — LBX-01 (Task 2/3 contract commit + grep proof) + LBX-02 (Task 4/5 test commit + envelope assertion) + LBX-03 (Task 6 SUMMARY `## Phase 271 §3.A delta-surface inputs` section with 4 line-number anchors) + GASPIN-01 (Task 1 RCA-inline + mechanism + path) + GASPIN-02 (Task 4 stabilization + inline NatSpec) + GASPIN-03 (3-consecutive-run cleanliness at Task 4/5).
2. **Zero contract widening** — `grep -rn "_resolveLootboxRoll" contracts/` returns 3 lines (definition + 2 callsites at `_resolveLootboxCommon` L910 + L938); no external visibility change; no new modifier; no new admin function.
3. **Triple-defense caller-clamp invariant preserved** — `grep -c "if (targetLevel < currentLevel)" contracts/modules/DegenerusGameLootboxModule.sol` returns 2 (Layer-1 + Layer-2 remain; Layer-3 deleted).
4. **Single batched USER-APPROVED contract commit (Task 3)** — `git log` shows the Task 3 commit with subject `feat(269): delete unreachable BURNIE-conversion branch in _resolveLootboxRoll [LBX-01]`; commit touches only `contracts/modules/DegenerusGameLootboxModule.sol`.
5. **Single batched USER-APPROVED test commit (Task 5)** — `git log` shows the Task 5 commit with subject `test(269): lootbox-open gas savings + SURF-05 gas-pin stabilization + SURF-03 re-baseline [LBX-02, GASPIN-01..03, SURF-03]`; commit covers all 4 test-tree deltas (LBX-02 + GASPIN-02 + SURF-03 + optional package.json) in one diff.
6. **GASPIN-01 RCA inline** — `269-01-PLAN.md` has `## Root Cause (GASPIN-01)` section appended by Task 1; mechanism named from {state pollution, gas-meter snapshot ordering, fixture-loader caching, hardhat node restart timing, drift not reproducible}; chosen stabilization path option (a)/(b)/(c)/(d) documented per D-269-STAB-01.
7. **GASPIN-03 cleanliness check** — `npm run test:stat` exits 0 across 3 consecutive runs at Task 4/5 with no flaky failures; standalone and combined-suite gas pins agree.
8. **SURF-03 re-baseline** — `test/stat/SurfaceRegression.test.js` has `PHASE_269_CLOSE_BASELINE` const; SURF-03 it block at L752 anchored to it; SURF-01/02/04 retain `V36_BASELINE` anchoring.
9. **LBX-03 Phase 271 anchor recording** — `269-01-SUMMARY.md` has `## Phase 271 §3.A delta-surface inputs` section enumerating post-deletion line numbers for 4 hash2/bit-slice callsites in `_resolveLootboxRoll`; Phase 271 LBX-03 row author consumes this without re-reading contracts/.
10. **No `git push`** — all commits land locally; user reviews diffs manually before any push per `feedback_manual_review_before_push.md`.
</verification>

<success_criteria>
Phase 269 closes successfully when:

- [ ] `269-01-PLAN.md` `## Root Cause (GASPIN-01)` section appended by Task 1 (AGENT-COMMITTED `docs(269): GASPIN-01 root-cause inline — <mechanism>` commit landed).
- [ ] LBX-01 contract commit (`feat(269): delete unreachable BURNIE-conversion branch in _resolveLootboxRoll [LBX-01]`) landed at Task 3 BLOCKING gate via explicit user `approved`; single-hunk -5/+1 LOC diff in `contracts/modules/DegenerusGameLootboxModule.sol`.
- [ ] Test commit (`test(269): lootbox-open gas savings + SURF-05 gas-pin stabilization + SURF-03 re-baseline [LBX-02, GASPIN-01..03, SURF-03]`) landed at Task 5 BLOCKING gate via explicit user `approved`; batched diff covers LBX-02 + GASPIN-02 + SURF-03 (+ optional package.json).
- [ ] `npm run test:stat` exits 0 across 3 consecutive fresh-checkout runs at Task 4/5 (GASPIN-03 acceptance).
- [ ] `269-01-SUMMARY.md` authored at Task 6 with 6/6 PASS tally + 3-subsection commit-readiness register + `## Phase 271 §3.A delta-surface inputs` section.
- [ ] STATE.md flipped (Phase 269 SHIPPED; progress 40 → 60).
- [ ] `docs(269): phase 269 summary + commit-readiness register` commit landed at Task 6.
- [ ] Optional `docs(phase-269): update tracking after phase 269 close` commit IF SDK calls produced ROADMAP/REQUIREMENTS mutations.
- [ ] NO `git push` performed at any task.
- [ ] All 6 v37.0 Phase 269 requirements (LBX-01..03 + GASPIN-01..03) flipped to PASS at phase close.
- [ ] ZERO new storage slots; zero new public/external mutation entry points; zero new admin functions; zero new modifiers (per CONTEXT.md `<domain>` Phase 269 boundary state at close).
- [ ] Triple-defense caller-clamp invariant byte-equivalence proven structurally (T-269-01 STRIDE register row) AND empirically (LBX-02 [30, 80] gas savings envelope + bytecode shrink at Task 4 LootboxOpenGas).
</success_criteria>

<output>
After completion, the artifacts produced under `.planning/phases/269-lootbox-dead-branch-cleanup-surf-05-gas-pin-re-pinning/`:
- `269-01-PLAN.md` (this file, modified by Task 1 to append `## Root Cause (GASPIN-01)` section).
- `269-01-SUMMARY.md` (NEW at Task 6; 6/6 PASS + 3-subsection commit-readiness register + `## Phase 271 §3.A delta-surface inputs` section for LBX-03 cross-phase coordination).

Source-tree mutations:
- `contracts/modules/DegenerusGameLootboxModule.sol` — single-hunk LBX-01 deletion, -5/+1 LOC, committed at Task 3.
- `test/gas/LootboxOpenGas.test.js` — LBX-02 v37.0 describe extension, committed at Task 5.
- `test/stat/SurfaceRegression.test.js` — SURF-03 re-baseline, committed at Task 5.
- `test/gas/AdvanceGameGas.test.js` and/or `test/gas/Phase264GasRegression.test.js` — GASPIN-02 stabilization mutations IF Task 1 RCA chose options (a) or (b); committed at Task 5.
- `package.json` — test:stat wiring delta IF Task 1 RCA chose split-files (option c); committed at Task 5.

Atomic commits (4 minimum + 1 optional follow-up):
1. AGENT-COMMITTED `docs(269): GASPIN-01 root-cause inline — <mechanism>` (Task 1).
2. USER-APPROVED `feat(269): delete unreachable BURNIE-conversion branch in _resolveLootboxRoll [LBX-01]` (Task 3).
3. USER-APPROVED `test(269): lootbox-open gas savings + SURF-05 gas-pin stabilization + SURF-03 re-baseline [LBX-02, GASPIN-01..03, SURF-03]` (Task 5).
4. AGENT-COMMITTED `docs(269): phase 269 summary + commit-readiness register` (Task 6).
5. (Optional) AGENT-COMMITTED `docs(phase-269): update tracking after phase 269 close` (Task 6 follow-up IF SDK calls produced tracking-tree mutations).

Plus any pre-Task-1 plan revision commits (e.g., from gsd-plan-checker iterations on this PLAN.md).

LBX-03 §3.A audit-trail row authoring is DEFERRED to Phase 271 per CONTEXT.md cross-phase coordination — Phase 269's `269-01-SUMMARY.md` `## Phase 271 §3.A delta-surface inputs` section provides the 4 post-deletion line-number anchors so Phase 271 author can populate the row without re-reading contracts/.

Closure signal `MILESTONE_V37_AT_HEAD_<sha>` remains DEFERRED to Phase 271 terminal-phase delivery per D-269-CLOSURE-01 carry. Phase 269 closes only at the plan level.
</output>
</content>
</invoke>