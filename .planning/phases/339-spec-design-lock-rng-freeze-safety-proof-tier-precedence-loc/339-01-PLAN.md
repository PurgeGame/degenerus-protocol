---
phase: 339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-BINGO06-FREEZE-PROOF.md
  - .planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-TRAITBURNTICKET-SOUNDNESS-ATTESTATION.md
autonomous: true
requirements: [BINGO-06]

must_haves:
  truths:
    - "The BINGO-06 freeze proof is recorded as a structured per-slot enumeration (D-04) with the verdict FREEZE-SAFE"
    - "Every storage slot claimBingo touches is classified as (i) own NEW bitfield write, (ii) post-resolution READ, or (iii) external reward call — and NONE is a current-VRF-window output slot during rngLock"
    - "v45-vrf-freeze-invariant is re-attested by name for the traitBurnTicket read"
    - "The traitBurnTicket write-site soundness attestation PROVES an address appears at traitBurnTicket[level][traitId][slot] IFF it owned a post-RNG-resolved entry carrying that exact trait byte [QQ][CCC][SSS]"
    - "The whale-frontrunning disposition is enshrined as a written ACCEPTED-BY-DESIGN non-finding (D-03) with the per-VRF-reveal race-window framing"
  artifacts:
    - path: ".planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-BINGO06-FREEZE-PROOF.md"
      provides: "The BINGO-06 RNG-freeze-safety proof via structured per-slot enumeration (SC2)"
      contains: "FREEZE-SAFE"
    - path: ".planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-TRAITBURNTICKET-SOUNDNESS-ATTESTATION.md"
      provides: "The full traitBurnTicket write-site soundness attestation (D-02) + the whale-race ACCEPTED-BY-DESIGN non-finding (D-03)"
      contains: "iff"
  key_links:
    - from: "339-BINGO06-FREEZE-PROOF.md"
      to: "contracts/storage/DegenerusGameStorage.sol:416 traitBurnTicket + :285 gameOver + StakedDegenerusStonk.sol:464 poolBalance"
      via: "per-slot classification table"
      pattern: "traitBurnTicket|gameOver|poolBalance"
    - from: "339-TRAITBURNTICKET-SOUNDNESS-ATTESTATION.md"
      to: "contracts/DegenerusGame.sol:2701/2730/2813 + DegenerusGameJackpotModule.sol:654 (the write-sites)"
      via: "write-site enumeration"
      pattern: "2701|2730|2813|654"
---

<objective>
Record the two security-bearing PROOFS that gate the entire claimBingo bundle: the BINGO-06 RNG-freeze-safety proof (via the structured per-slot enumeration mandated by D-04) and the full traitBurnTicket write-site soundness attestation (D-02) — plus the whale-race ACCEPTED-BY-DESIGN non-finding (D-03). These are the v51 security floor for this surface (the internal adversarial sweep is DEFERRED → v52, so this paper proof IS the security deliverable).

Purpose: BINGO-06 is the design-gating attestation — it must PROVE (not assume) that claimBingo's read-only-of-post-resolution-traitBurnTicket shape is freeze-safe BEFORE the entrypoint is authored at IMPL 340. The "PROVEN not assumed" directive is governing here: the precedent is the DegenerusGame mint/jackpot inline-duplication caught in prior milestones — no "by construction" / "single fn reaches all paths" claim survives un-checked.
Output: Two markdown proof documents in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-CONTEXT.md
@.planning/PLAN-V51-CLAIMBINGO-COLOR-COMPLETION.md

<interfaces>
<!-- All anchors below confirmed against the live tree == 812abeee (git diff 812abeee HEAD -- contracts/ is EMPTY). -->
<!-- BINGO-06 freeze proof load-bearing facts: -->
- contracts/storage/DegenerusGameStorage.sol:416 — `mapping(uint24 => address[][256]) internal traitBurnTicket;` (the post-resolution READ source; traitId fits 256 fixed slots, inner address[] dynamic)
- contracts/storage/DegenerusGameStorage.sol:285 — `bool public gameOver;` (the hard-cutoff READ)
- contracts/StakedDegenerusStonk.sol:464 — `function poolBalance(Pool pool) external view returns (uint256)` (the pool READ)
- contracts/StakedDegenerusStonk.sol:485 — `function transferFromPool(Pool pool, address to, uint256 amount) external onlyGame returns (uint256 transferred)` (clamped-return external reward call)
- The 3 NEW bitfield WRITES (appended to DegenerusGameStorage.sol, the shared delegatecall layout): bingoClaimed (per-player uint8) / firstQuadrant (systemwide uint8) / firstSymbol (systemwide uint32) — see D-05 plan doc
- coinflip.creditFlip(msg.sender, amount) — the BURNIE external reward call (uncapped emission, same path as autoBuy bounty)
- v45-vrf-freeze-invariant (memory): every var interacting with a VRF word must be frozen [request→unlock] vs players; the read must consume post-resolution, not buffered-for-next

<!-- traitBurnTicket soundness attestation load-bearing facts (D-02): -->
- contracts/DegenerusGame.sol:2701 — `address[] storage arr = traitBurnTicket[lvlSel][traitSel];` (write-site 1)
- contracts/DegenerusGame.sol:2730 — `address[] storage arr = traitBurnTicket[targetLvl][traitSel];` (write-site 2)
- contracts/DegenerusGame.sol:2813 — `address[] storage a = traitBurnTicket[lvl][trait];` (write-site 3)
- contracts/modules/DegenerusGameJackpotModule.sol:654 — `address[][256] storage bucket = traitBurnTicket[lvl];` (the jackpot storage-bucket writer/reader)
- contracts/DegenerusTraitUtils.sol:17-39 — trait byte layout [QQ][CCC][SSS] (Q bits 7-6, C bits 5-3, S bits 2-0); traitId = (quadrant<<6)|(c<<3)|symInQ
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the BINGO-06 RNG-freeze-safety proof via structured per-slot enumeration (SC2, D-04)</name>
  <files>.planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-BINGO06-FREEZE-PROOF.md</files>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol (read the traitBurnTicket decl :416 + the :404-416 surrounding storage region the 3 new mappings append after; gameOver :285; the currentLevel field — quote real line numbers, do not assume)
    - contracts/StakedDegenerusStonk.sol (poolBalance :464 the READ, transferFromPool :485 the clamped-return external call — confirm the onlyGame modifier + the `returns (uint256 transferred)` clamp idiom)
    - contracts/DegenerusTraitUtils.sol:17-39 (trait byte layout [QQ][CCC][SSS] — confirms the read target is the resolved trait byte)
    - .planning/PLAN-V51-CLAIMBINGO-COLOR-COMPLETION.md ("Validation sketch" + "Storage additions" + "Open before SPEC" items 4 and 5 — the traitBurnTicket post-resolution invariant + the RNG-freeze interaction)
    - .planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-CONTEXT.md (D-04 the structured-per-slot-enumeration directive, the v45-vrf-freeze-invariant + threat-model-reentrancy-mev-nonissues memory locks)
  </read_first>
  <action>
    Author 339-BINGO06-FREEZE-PROOF.md proving BINGO-06 with a structured per-slot enumeration (per D-04 — NOT prose alone) and the verdict FREEZE-SAFE. Build a per-slot classification TABLE that enumerates EVERY storage slot claimBingo touches and classifies each into exactly one of three classes: (i) its own NEW bitfield WRITE — bingoClaimed[level][msg.sender] (per-player u8), firstQuadrant[level] (systemwide u8), firstSymbol[level] (systemwide u32), all freshly-appended slots never read by any VRF-influenced path; (ii) a post-resolution READ — traitBurnTicket[level][traitId][slots[c]] (the resolved address[]), currentLevel, gameOver (:285), poolBalance(Pool.Reward) (StakedDegenerusStonk:464); (iii) an external reward CALL — transferFromPool (:485, clamped-return) and coinflip.creditFlip. For each, state WHY it is not a current-VRF-window output slot during rngLock: class (i) slots are brand-new and read by nothing the VRF writes; class (ii) reads consume only POST-RESOLUTION state (traitBurnTicket[level][traitId] is populated only AFTER level-L traits are RNG-resolved — attest the populated-only-after-level-L-resolution invariant and cite the soundness companion doc 339-TRAITBURNTICKET-SOUNDNESS-ATTESTATION.md for the write-site proof); class (iii) calls touch the sDGNRS pool + BURNIE flip-credit, neither a VRF-derived current-window output. Lock the race-start semantics (per D-03): claimable the moment level-N entry traits are RNG-resolved (currentLevel advances). Explicitly re-attest v45-vrf-freeze-invariant BY NAME for the traitBurnTicket read — argue the read consumes post-resolution data, not buffered-for-next, so no var interacting with a current VRF word is mutated within the locked window. State that claimBingo adds NO write to traitBurnTicket (read-only consumer). Cite every file:line against the live tree (== 812abeee). Do NOT inline Solidity implementation; reference identifiers/signatures/line-anchors only.
  </action>
  <acceptance_criteria>
    - 339-BINGO06-FREEZE-PROOF.md exists and contains the literal verdict "FREEZE-SAFE"
    - The doc contains a per-slot classification TABLE enumerating every slot claimBingo touches, each labeled class (i) NEW write / (ii) post-resolution READ / (iii) external CALL
    - Class (i) lists all three new bitfields: bingoClaimed, firstQuadrant, firstSymbol
    - Class (ii) lists traitBurnTicket, currentLevel, gameOver, poolBalance and cites DegenerusGameStorage.sol:416 (traitBurnTicket) + :285 (gameOver) + StakedDegenerusStonk.sol:464 (poolBalance)
    - Class (iii) lists transferFromPool (StakedDegenerusStonk.sol:485) and coinflip.creditFlip
    - The doc attests the "traitBurnTicket[level][traitId] populated-only-after-level-L-resolution" invariant and references the soundness companion doc for the write-site proof
    - The doc states claimBingo adds NO write to traitBurnTicket (read-only consumer)
    - The doc locks the race-start semantics (claimable the moment level-N traits are RNG-resolved as currentLevel advances)
    - v45-vrf-freeze-invariant is re-attested BY NAME for the read
    - The doc concludes NONE of the touched slots is a current-VRF-window output slot during rngLock
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-BINGO06-FREEZE-PROOF.md; test -f "$F" && grep -q "FREEZE-SAFE" "$F" && grep -q "v45-vrf-freeze-invariant" "$F" && grep -q "bingoClaimed" "$F" && grep -q "firstQuadrant" "$F" && grep -q "firstSymbol" "$F" && grep -qE "traitBurnTicket" "$F" && grep -qE "416" "$F" && grep -qE "485" "$F" && grep -qiE "post-resolution|post-RNG" "$F"</automated>
  </verify>
  <done>The BINGO-06 freeze proof is recorded with verdict FREEZE-SAFE, a structured per-slot classification table (3 classes), the post-resolution-read invariant, the read-only-consumer statement, the locked race-start semantics, and the v45 re-attestation — citing real anchors.</done>
</task>

<task type="auto">
  <name>Task 2: Record the traitBurnTicket write-site soundness attestation + whale-race non-finding (D-02, D-03)</name>
  <files>.planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-TRAITBURNTICKET-SOUNDNESS-ATTESTATION.md</files>
  <read_first>
    - contracts/DegenerusGame.sol (the three traitBurnTicket population/write-sites at :2701 `traitBurnTicket[lvlSel][traitSel]`, :2730 `traitBurnTicket[targetLvl][traitSel]`, :2813 `traitBurnTicket[lvl][trait]` — read the surrounding context of each to confirm WHAT is appended and WHEN, what level/trait key each uses, and that the appended address is the resolved-entry owner)
    - contracts/modules/DegenerusGameJackpotModule.sol (the storage-bucket access at :654 `traitBurnTicket[lvl]`; the read-side selection at :936/:1554/:1696 — confirm the jackpot path treats the bucket as post-resolution; confirm no transfer/burn re-population lets a NON-owner land at a slot)
    - contracts/DegenerusTraitUtils.sol:17-39 (the [QQ][CCC][SSS] trait byte; confirm the append is keyed by the resolved trait byte → traitId)
    - .planning/PLAN-V51-CLAIMBINGO-COLOR-COMPLETION.md ("Open before SPEC" item 4 the post-resolution invariant + "Notes" the duplicate-slot-griefing-impossible argument + "MEV" + "Game-theory notes")
    - .planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-CONTEXT.md (D-02 the full-write-site-attestation directive [parts a/b/c], D-03 the whale-race ACCEPTED-BY-DESIGN non-finding)
  </read_first>
  <action>
    Author 339-TRAITBURNTICKET-SOUNDNESS-ATTESTATION.md as the full write-site attestation (per D-02 — NOT a precedent-based hand-wave). State the soundness theorem up front: an address appears at traitBurnTicket[level][traitId][slot] IFF it owned a post-RNG-resolved entry carrying that exact trait byte [QQ][CCC][SSS]. Read the actual population sites (DegenerusGame.sol:2701/2730/2813 + the JackpotModule:654 storage-bucket writer) and PROVE the three D-02 sub-claims explicitly: (a) the append is KEYED BY THE RESOLVED TRAIT BYTE — show each write-site indexes [level][traitId] where traitId is the resolved trait, so there is no cross-trait contamination (an entry of trait X is never appended under trait Y); (b) DUPLICATE-APPEND behavior — a player who resolved N entries of the same trait appears N times in that bucket, which is fine: claimBingo only requires the caller to NAME a slot they occupy (traitBurnTicket[level][traitId][slots[c]] == msg.sender), and duplicate-slot griefing is impossible because each trait byte encodes exactly one (quadrant, color, symbol) so two different color-loop indices c cannot point at the same entry; (c) TRANSFER/BURN RE-POPULATION semantics — examine whether any path lets a NON-owner land at a slot (e.g. resale, burn-and-reappend, bucket compaction) and attest the result: either no such path exists, or if one does, attest it preserves the owner==appended-address invariant. Conclude with the SOUND verdict (the IFF holds → claimBingo cannot be spoofed). Then append the whale-race section enshrining D-03 as a written ACCEPTED-BY-DESIGN NON-FINDING: state verbatim that "whale frontrunning on the per-VRF trait-resolution batch is accepted by design — the race window is the per-VRF reveal, not per-block; two simultaneous first-claimants for the same (level, quadrant) or (level, symbol) require both to land their last needed color in the same VRF resolution, which is rare", and state the purpose (the deferred v52 adversarial sweep treats this as already-dispositioned/known, not a fresh finding). Cite every file:line against the live tree. Do NOT inline Solidity implementation.
  </action>
  <acceptance_criteria>
    - 339-TRAITBURNTICKET-SOUNDNESS-ATTESTATION.md exists and states the soundness theorem with the word "iff" (an address appears at traitBurnTicket[level][traitId][slot] iff it owned a post-RNG-resolved entry carrying that exact trait byte)
    - The doc cites all three DegenerusGame.sol write-sites: :2701, :2730, :2813
    - The doc cites the JackpotModule:654 storage-bucket writer
    - The doc proves sub-claim (a) keyed-by-resolved-trait-byte (no cross-trait contamination)
    - The doc proves sub-claim (b) duplicate-append (N entries → N appearances; duplicate-slot griefing impossible because each trait byte encodes exactly one (quadrant, color, symbol))
    - The doc proves sub-claim (c) transfer/burn re-population (attests no NON-owner can land at a slot, or the invariant is preserved)
    - The doc records the SOUND verdict (claimBingo cannot be spoofed)
    - The doc enshrines the D-03 whale-race ACCEPTED-BY-DESIGN non-finding with the per-VRF-reveal (not per-block) race-window framing and notes the v52-sweep-already-dispositioned purpose
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-TRAITBURNTICKET-SOUNDNESS-ATTESTATION.md; test -f "$F" && grep -qi "iff" "$F" && grep -q "2701" "$F" && grep -q "2730" "$F" && grep -q "2813" "$F" && grep -q "654" "$F" && grep -qi "cross-trait" "$F" && grep -qiE "ACCEPTED-BY-DESIGN|accepted by design" "$F" && grep -qiE "per-VRF|per VRF|trait-resolution batch" "$F"</automated>
  </verify>
  <done>The soundness attestation states the IFF theorem, cites all four write-sites (:2701/:2730/:2813 + :654), proves D-02 sub-claims a/b/c, records the SOUND verdict, and enshrines the D-03 whale-race non-finding — citing real anchors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a paper-only SPEC phase that ships no code. The threat surface is the CORRECTNESS of the security-bearing design proofs this plan records — these proofs ARE the v51 security floor for the claimBingo surface (the internal adversarial sweep is DEFERRED → v52).

| Boundary | Description |
|----------|-------------|
| SPEC proof → IMPL 340 | A wrong freeze proof or an un-attested soundness claim lets a real vulnerability (a freeze-window read of mutable VRF state, or a spoofable bingo claim) ship at IMPL. |
| claimBingo caller → traitBurnTicket read | An untrusted caller names slots[8]; the soundness attestation is what proves a non-owner cannot pass the ownership check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-339-01 | Tampering | BINGO-06 freeze proof | mitigate | The structured per-slot enumeration (D-04) classifies every touched slot into NEW-write / post-resolution-READ / external-CALL and shows none is a current-VRF-window output during rngLock — citing real grep-attested anchors (Storage:416/:285, StakedDegenerusStonk:464/:485); v45-vrf-freeze-invariant re-attested by name so "future-resolved therefore safe" hand-waving cannot ship. |
| T-339-02 | Spoofing | claimBingo ownership check (traitBurnTicket read) | mitigate | The full write-site attestation (D-02) proves the IFF (owner == appended address) across all three DegenerusGame.sol write-sites + JackpotModule:654 — the feedback_verify_call_graph_against_source floor; the transfer/burn re-population sub-claim (c) closes the non-owner-lands-at-a-slot vector. |
| T-339-03 | Information disclosure / front-running | whale-race on the trait-resolution batch | accept | D-03 ACCEPTED-BY-DESIGN: the race window is the per-VRF reveal, not per-block (threat-model-reentrancy-mev-nonissues: MEV LOW/confirmatory); enshrined as a written non-finding so the v52 sweep treats it as known, not fresh. |
| T-339-SC | Tampering | npm/pip/cargo installs | accept | No package installs — paper-only Markdown authoring against the frozen tree. |
</threat_model>

<verification>
- Both proof documents exist in the phase directory.
- BINGO-06 proof: verdict FREEZE-SAFE, the structured per-slot classification table (3 classes), the post-resolution-read invariant, the read-only-consumer statement, the v45 re-attestation.
- Soundness attestation: the IFF theorem, all four write-sites (:2701/:2730/:2813 + :654), D-02 sub-claims a/b/c, the SOUND verdict, the D-03 whale-race non-finding.
- No contracts/*.sol modified (paper-only): `git diff --name-only -- contracts/` is empty.
</verification>

<success_criteria>
- 339-BINGO06-FREEZE-PROOF.md satisfies all Task 1 acceptance criteria (BINGO-06 / SC2).
- 339-TRAITBURNTICKET-SOUNDNESS-ATTESTATION.md satisfies all Task 2 acceptance criteria (D-02 / D-03).
- Zero contracts/*.sol edits.
</success_criteria>

<output>
Create `.planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-01-SUMMARY.md` when done
</output>
