---
phase: 339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-DESIGN-LOCK-BINGO.md
  - .planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-TIER-PRECEDENCE-ACCEPTANCE-CONTRACT.md
autonomous: true
requirements: [BATCH-01]

must_haves:
  truths:
    - "The claimBingo(uint256 level, uint8 symbol, uint32[8] calldata slots) signature is locked (D-01) with the uint32 slot-width cap disposition written out"
    - "The storage shape is locked: bingoClaimed per-player u8 / firstQuadrant systemwide u8 / firstSymbol systemwide u32, all keyed by the existing uint24 level (D-05/D-07/D-10)"
    - "Module placement is locked: new contracts/modules/DegenerusGameBingoModule.sol delegatecalled from DegenerusGame.claimBingo via GAME_BINGO_MODULE (D-10)"
    - "The reward constants are transcribed verbatim (D-05): REGULAR_DGNRS_BPS=5 / FIRST_SYMBOL_BONUS_DGNRS_BPS=5 / FIRST_QUADRANT_DGNRS_BPS=50; REGULAR_BURNIE=1_000e18 / FIRST_SYMBOL_BONUS_BURNIE=1_000e18 / FIRST_QUADRANT_BURNIE=5_000e18"
    - "The tier-precedence rule (D-06) is written as the IMPL acceptance contract: isQuadrantFirst checked BEFORE isSymbolFirst; quadrant-first marks BOTH bits + pays REPLACEMENT + suppresses symbol-first; symbol-first marks firstSymbol + pays ADDITIVE; regular pays baseline"
  artifacts:
    - path: ".planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-DESIGN-LOCK-BINGO.md"
      provides: "The settled signature + storage shape + slot-width disposition + module placement + reward constants (SC1, BATCH-01)"
      contains: "FIRST_QUADRANT_DGNRS_BPS"
    - path: ".planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-TIER-PRECEDENCE-ACCEPTANCE-CONTRACT.md"
      provides: "The tier-precedence + suppression + bit-marking IMPL acceptance contract (SC3, BATCH-01, Open-before-SPEC item 7)"
      contains: "isQuadrantFirst"
  key_links:
    - from: "339-DESIGN-LOCK-BINGO.md"
      to: "contracts/storage/DegenerusGameStorage.sol:416 (traitBurnTicket uint24 key) + DegenerusTraitUtils.sol:17-39 (trait byte) + ContractAddresses.sol (GAME_BINGO_MODULE)"
      via: "storage-shape + traitId-derivation citation"
      pattern: "uint24|traitId|GAME_BINGO_MODULE"
    - from: "339-TIER-PRECEDENCE-ACCEPTANCE-CONTRACT.md"
      to: "the three-tier selection (regular / additive symbol-first / replacement quadrant-first)"
      via: "precedence + suppression rule"
      pattern: "isQuadrantFirst|isSymbolFirst|suppress"
---

<objective>
Settle in writing the full claimBingo bundle DESIGN (BATCH-01, SC1) so IMPL 340 authors a fully-reconciled diff with zero "by construction" assumptions: the function signature + the uint32 slot-width disposition (D-01), the storage shape + the uint24 level key (D-05/D-07/D-10), the module placement + delegatecall wiring (D-10), and the reward constants (D-05). Separately, write out the tier-precedence rule (D-06) as the IMPL acceptance contract (SC3, "Open before SPEC" item 7) — the quadrant-first-before-symbol-first precedence + the both-bits-marking + the symbol-first-bonus suppression.

Purpose: this is the design-lock. The economics are LOCKED in the plan doc — the SPEC TRANSCRIBES them into an acceptance contract, it does NOT re-litigate settled numbers. The tier-precedence rule is the one piece of selection logic that must be written unambiguously so IMPL gets the suppression + bit-marking exactly right (the double-pay trap).
Output: Two markdown design-lock documents in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-CONTEXT.md
@.planning/PLAN-V51-CLAIMBINGO-COLOR-COMPLETION.md

<interfaces>
<!-- All anchors confirmed against the live tree == 812abeee (git diff empty for contracts/). -->
<!-- Signature + storage (D-01/D-05/D-07/D-09/D-10): -->
- Signature: `claimBingo(uint256 level, uint8 symbol, uint32[8] calldata slots)` (D-01 — slots is uint32[8], NOT uint256[8])
- Storage additions (appended to the SHARED contracts/storage/DegenerusGameStorage.sol layout, NOT declared in the module):
    - `mapping(uint24 => mapping(address => uint8)) bingoClaimed;`  (per-player, 4 bits used — quadrant mask)
    - `mapping(uint24 => uint8)  firstQuadrant;`                    (systemwide, 4 bits)
    - `mapping(uint24 => uint32) firstSymbol;`                      (systemwide, 32 bits)
- contracts/storage/DegenerusGameStorage.sol:416 — `mapping(uint24 => address[][256]) internal traitBurnTicket;` (the uint24 level key the 3 new mappings match; slots[c] indexes the inner address[])
- contracts/DegenerusTraitUtils.sol:17-39 — trait byte [QQ][CCC][SSS]; traitId = (quadrant<<6)|(c<<3)|symInQ, quadrant = symbol>>3, symInQ = symbol & 7
- uint32 slot-width cap: ~4.29B entries per (level, traitId) — unreachable (would need 4B RNG-resolved entries of ONE trait byte on ONE level); cheap calldata. D-01 requires the SPEC to STATE the cap explicitly (written disposition, not silence).
- Module placement (D-10): new contracts/modules/DegenerusGameBingoModule.sol; DegenerusGame.claimBingo external entrypoint → ContractAddresses.GAME_BINGO_MODULE.delegatecall(...); new GAME_BINGO_MODULE address in contracts/ContractAddresses.sol (freely modifiable per feedback_contractaddresses_policy); interface (IDegenerusGame or equivalent) gains the claimBingo signature
- contracts/DegenerusGame.sol — the GAME_*_MODULE.delegatecall dispatch pattern (e.g. :280/:309 GAME_ADVANCE_MODULE, :521/:546 GAME_MINT_MODULE) is the copy-paste wiring shape; 8 existing modules

<!-- Reward constants (D-05) — LOCKED, transcribe verbatim: -->
- REGULAR_DGNRS_BPS            = 5    (0.05% of Pool.Reward)
- FIRST_SYMBOL_BONUS_DGNRS_BPS = 5    (+0.05% ADDED to regular)
- FIRST_QUADRANT_DGNRS_BPS     = 50   (0.5% of Pool.Reward, REPLACEMENT)
- REGULAR_BURNIE               = 1_000e18
- FIRST_SYMBOL_BONUS_BURNIE    = 1_000e18  (ADDED to regular)
- FIRST_QUADRANT_BURNIE        = 5_000e18  (REPLACES regular + symbol bonus)

<!-- Tier-precedence (D-06/D-07/D-08): -->
- isQuadrantFirst = (firstQuadrant[level] & qMask) == 0; checked BEFORE isSymbolFirst
- isSymbolFirst   = (firstSymbol[level]   & sMask) == 0
- quadrant-first → mark BOTH firstQuadrant |= qMask AND firstSymbol |= sMask; pay REPLACEMENT (50 bps + 5_000e18); SUPPRESS symbol-first bonus; emit FirstQuadrantBingo
- symbol-first (not quadrant-first) → mark firstSymbol |= sMask; pay ADDITIVE (5+5=10 bps + 1_000+1_000=2_000e18); emit FirstSymbolBingo
- regular (both set) → baseline (5 bps + 1_000e18)
- per-player dedup: bingoClaimed[level][msg.sender] & qMask == 0 then |= qMask → max 4 claims/player/level
- reward paths (D-08): sDGNRS via sdgnrs.transferFromPool(IStakedDegenerusStonk.Pool.Reward, msg.sender, (poolBal*bps)/10_000) [clamped return = dgnrsPaid]; BURNIE via coinflip.creditFlip(msg.sender, amount); empty/0-amount pool = graceful no-op; gameOver = hard cutoff (revert); leaderboard event-only (FirstQuadrantBingo / FirstSymbolBingo / BingoClaimed)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the BINGO design-lock — signature + storage + slot-width + module placement + constants (SC1, BATCH-01)</name>
  <files>.planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-DESIGN-LOCK-BINGO.md</files>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol (the traitBurnTicket :416 uint24 level key the 3 new mappings must match; read the :404-416 region to confirm where the new mappings append in the shared delegatecall layout — quote real line numbers)
    - contracts/DegenerusTraitUtils.sol:17-39 (confirm the [QQ][CCC][SSS] byte → the traitId = (quadrant<<6)|(c<<3)|symInQ derivation, quadrant = symbol>>3, symInQ = symbol & 7)
    - contracts/DegenerusGame.sol (the GAME_*_MODULE.delegatecall dispatch pattern — read one existing entrypoint e.g. GAME_ADVANCE_MODULE :280 or GAME_MINT_MODULE :521 to confirm the copy-paste wiring shape for claimBingo)
    - contracts/ContractAddresses.sol (confirm where a new GAME_BINGO_MODULE address constant would be added — freely modifiable per feedback_contractaddresses_policy)
    - .planning/PLAN-V51-CLAIMBINGO-COLOR-COMPLETION.md ("Function signature" + "Storage additions" + "Constants" + "File touchpoints" + "Open before SPEC" items 1 and 2)
    - .planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-CONTEXT.md (D-01 slot-width, D-05 constants, D-07 dedup keys, D-09 traitId derivation, D-10 module placement)
  </read_first>
  <action>
    Author 339-DESIGN-LOCK-BINGO.md as the settled BINGO design (transcribe the LOCKED design — do NOT re-derive economics). Record: (1) the SIGNATURE `claimBingo(uint256 level, uint8 symbol, uint32[8] calldata slots)` per D-01, and write out the uint32 slot-width DISPOSITION explicitly (the cap is ~4.29B entries per (level, traitId), unreachable — it would require 4 billion RNG-resolved entries of ONE trait byte on ONE level; cheap calldata; the cap is a non-issue but stated so the audit has a written disposition, not silence). (2) the STORAGE SHAPE per D-05/D-07/D-10 — bingoClaimed (mapping uint24 => mapping(address => uint8), per-player, 4 bits = quadrant mask), firstQuadrant (mapping uint24 => uint8, systemwide, 4 bits), firstSymbol (mapping uint24 => uint32, systemwide, 32 bits) — all keyed by the existing uint24 level (cite DegenerusGameStorage.sol:416 as the key precedent) and all appended to the SHARED DegenerusGameStorage.sol layout (delegatecall architecture → storage must be shared, NOT declared in the module; pre-launch redeploy-fresh → appending is safe, no migration). (3) the traitId DERIVATION per D-09: traitId = (quadrant<<6)|(c<<3)|symInQ, quadrant = symbol>>3, symInQ = symbol & 7, trait byte [QQ][CCC][SSS] (cite DegenerusTraitUtils.sol:17-39); note duplicate-slot griefing is impossible (each trait byte encodes exactly one (quadrant, color, symbol)). (4) the MODULE PLACEMENT per D-10: new contracts/modules/DegenerusGameBingoModule.sol delegatecalled from a new DegenerusGame.claimBingo external entrypoint via ContractAddresses.GAME_BINGO_MODULE.delegatecall(...) (the established dispatch pattern — cite the existing GAME_*_MODULE delegatecall site you read), a new GAME_BINGO_MODULE address in ContractAddresses.sol, and the interface (IDegenerusGame or equivalent) gaining the claimBingo signature. (5) the REWARD CONSTANTS transcribed VERBATIM per D-05: REGULAR_DGNRS_BPS=5, FIRST_SYMBOL_BONUS_DGNRS_BPS=5, FIRST_QUADRANT_DGNRS_BPS=50; REGULAR_BURNIE=1_000e18, FIRST_SYMBOL_BONUS_BURNIE=1_000e18, FIRST_QUADRANT_BURNIE=5_000e18 — with the percent equivalents (0.05% / +0.05% / 0.5%) and the additive-vs-replacement semantics noted. (6) the reward PATHS + dedup keys per D-07/D-08: per-player dedup once per (level, quadrant) via bingoClaimed (max 4/player/level); systemwide first keys (level,quadrant) for 4 quadrant-firsts + (level,symbol) for 32 symbol-firsts; sDGNRS via transferFromPool(Pool.Reward,...) clamped-return; BURNIE via coinflip.creditFlip; empty-pool graceful no-op; gameOver hard cutoff; event-only leaderboard. Cross-reference the tier-precedence companion doc (339-TIER-PRECEDENCE-ACCEPTANCE-CONTRACT.md) for the selection logic. Cite every file:line. Do NOT inline a full Solidity function body — name signatures, identifiers, and anchors only.
  </action>
  <acceptance_criteria>
    - 339-DESIGN-LOCK-BINGO.md exists and states the signature verbatim: `claimBingo(uint256 level, uint8 symbol, uint32[8] calldata slots)`
    - The uint32 slot-width cap disposition is written out (the ~4.29B / 4-billion-entries unreachable argument is present, not silent)
    - The storage shape lists all three mappings with their types (bingoClaimed uint8 per-player, firstQuadrant uint8 systemwide, firstSymbol uint32 systemwide) and states they are keyed by the uint24 level and appended to the SHARED DegenerusGameStorage.sol layout (cites :416)
    - The traitId derivation is stated: traitId = (quadrant<<6)|(c<<3)|symInQ, quadrant = symbol>>3, symInQ = symbol & 7, with DegenerusTraitUtils.sol:17-39 cited
    - Module placement is locked: new contracts/modules/DegenerusGameBingoModule.sol + DegenerusGame.claimBingo delegatecall + GAME_BINGO_MODULE in ContractAddresses.sol + interface signature, citing an existing GAME_*_MODULE.delegatecall site
    - All six reward constants are transcribed verbatim: REGULAR_DGNRS_BPS=5, FIRST_SYMBOL_BONUS_DGNRS_BPS=5, FIRST_QUADRANT_DGNRS_BPS=50, REGULAR_BURNIE=1_000e18, FIRST_SYMBOL_BONUS_BURNIE=1_000e18, FIRST_QUADRANT_BURNIE=5_000e18
    - The reward paths (transferFromPool clamped-return + creditFlip), the empty-pool no-op, the gameOver hard cutoff, the per-player (level,quadrant) dedup (max 4/level), and the event-only leaderboard are recorded
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-DESIGN-LOCK-BINGO.md; test -f "$F" && grep -q "uint32\[8\] calldata slots" "$F" && grep -q "REGULAR_DGNRS_BPS" "$F" && grep -q "FIRST_QUADRANT_DGNRS_BPS" "$F" && grep -q "5_000e18" "$F" && grep -q "DegenerusGameBingoModule.sol" "$F" && grep -q "GAME_BINGO_MODULE" "$F" && grep -q "bingoClaimed" "$F" && grep -q "firstQuadrant" "$F" && grep -q "firstSymbol" "$F" && grep -qE "uint24" "$F" && grep -q "416" "$F" && grep -qiE "4.29B|4 billion|4,294|4294" "$F"</automated>
  </verify>
  <done>The BINGO design-lock records the locked signature, the uint32 slot-width disposition, the three new mappings keyed by uint24, the traitId derivation, the module placement + delegatecall wiring, all six reward constants verbatim, and the reward paths/dedup/no-op/cutoff — citing real anchors.</done>
</task>

<task type="auto">
  <name>Task 2: Author the tier-precedence acceptance contract — quadrant-first-before-symbol-first + suppression (SC3, BATCH-01, item 7)</name>
  <files>.planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-TIER-PRECEDENCE-ACCEPTANCE-CONTRACT.md</files>
  <read_first>
    - .planning/PLAN-V51-CLAIMBINGO-COLOR-COMPLETION.md ("Validation sketch" — the isQuadrantFirst-before-isSymbolFirst tier selection block + the "Tier ordering matters" note + "Decisions LOCKED" the additive-vs-replacement semantics + "Open before SPEC" item 7)
    - .planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-CONTEXT.md (D-06 the tier-precedence rule LOCKED, D-07 the dedup keys, D-08 the reward paths + event-only leaderboard, the "Specific Ideas" note that the SPEC is a faithful design-lock not a re-litigation)
    - .planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-CONTEXT.md (D-05 constants — to state the exact bps/BURNIE each tier pays)
  </read_first>
  <action>
    Author 339-TIER-PRECEDENCE-ACCEPTANCE-CONTRACT.md as the IMPL acceptance contract for the 3-tier selection (per D-06 — written so IMPL 340 implements the suppression + bit-marking exactly, avoiding the double-pay trap). Write the precedence rule as an ordered decision: compute isQuadrantFirst = (firstQuadrant[level] & qMask) == 0 and isSymbolFirst = (firstSymbol[level] & sMask) == 0, then CHECK isQuadrantFirst BEFORE isSymbolFirst. Enumerate the three branches as an acceptance table with, for EACH branch: the condition, the bits marked, the reward paid (exact bps + exact BURNIE per D-05), the event emitted, and the suppression behavior — (1) QUADRANT-FIRST (isQuadrantFirst true): mark BOTH firstQuadrant |= qMask AND firstSymbol |= sMask, pay the REPLACEMENT reward (FIRST_QUADRANT_DGNRS_BPS=50 → 0.5% Pool.Reward + FIRST_QUADRANT_BURNIE=5_000e18), SUPPRESS the symbol-first bonus (non-additive), emit FirstQuadrantBingo; (2) SYMBOL-FIRST (isQuadrantFirst false, isSymbolFirst true): mark firstSymbol |= sMask only, pay the ADDITIVE reward (REGULAR_DGNRS_BPS+FIRST_SYMBOL_BONUS_DGNRS_BPS = 5+5 = 10 → 0.1% + REGULAR_BURNIE+FIRST_SYMBOL_BONUS_BURNIE = 1_000+1_000 = 2_000e18), emit FirstSymbolBingo; (3) REGULAR (both set): pay the baseline (REGULAR_DGNRS_BPS=5 → 0.05% + REGULAR_BURNIE=1_000e18), no first-bit marking, emit BingoClaimed. State the KEY INVARIANT explicitly: a quadrant-first claim marking the firstSymbol bit GUARANTEES no later claim of that symbol can re-collect the symbol-first bonus (the suppression-is-also-a-bit-set rule) — this is the precise behavior TST-02 (Phase 341) will prove and the reason the ordering matters (a quadrant-first claim is necessarily also the chronological first claim for its symbol). Note that every branch then emits BingoClaimed(msg.sender, level, symbol, burnieReward, dgnrsPaid) regardless of tier. State this is the binding IMPL acceptance contract for BINGO-03 at Phase 340 and the suppression behavior TST-02 codifies. Do NOT inline a full Solidity function body — describe the decision logic in directive prose + an acceptance table.
  </action>
  <acceptance_criteria>
    - 339-TIER-PRECEDENCE-ACCEPTANCE-CONTRACT.md exists and states isQuadrantFirst is checked BEFORE isSymbolFirst
    - The doc contains a three-branch acceptance table (quadrant-first / symbol-first / regular) with, per branch: condition, bits marked, reward (bps + BURNIE), event, suppression
    - The quadrant-first branch states BOTH bits are marked (firstQuadrant AND firstSymbol), pays REPLACEMENT 50 bps (0.5%) + 5_000e18, SUPPRESSES the symbol-first bonus, emits FirstQuadrantBingo
    - The symbol-first branch states firstSymbol is marked, pays ADDITIVE 10 bps (0.1%) + 2_000e18, emits FirstSymbolBingo
    - The regular branch states baseline 5 bps (0.05%) + 1_000e18, emits BingoClaimed
    - The KEY INVARIANT is stated: a quadrant-first claim marking firstSymbol guarantees no later same-symbol claim can re-collect the symbol-first bonus
    - The doc names this the binding IMPL acceptance contract for BINGO-03 and the behavior TST-02 will prove
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-TIER-PRECEDENCE-ACCEPTANCE-CONTRACT.md; test -f "$F" && grep -q "isQuadrantFirst" "$F" && grep -q "isSymbolFirst" "$F" && grep -qiE "before" "$F" && grep -qiE "suppress" "$F" && grep -q "FirstQuadrantBingo" "$F" && grep -q "FirstSymbolBingo" "$F" && grep -q "BingoClaimed" "$F" && grep -qE "0.5%|50 bps|FIRST_QUADRANT_DGNRS_BPS" "$F" && grep -qE "0.1%|10 bps" "$F" && grep -qE "TST-02" "$F"</automated>
  </verify>
  <done>The tier-precedence acceptance contract records the quadrant-first-before-symbol-first ordering, the three-branch table with exact rewards/bits/events/suppression, the both-bits-marked quadrant-first rule, the suppression-guarantee invariant, and the BINGO-03/TST-02 binding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a paper-only SPEC phase that ships no code. The threat surface is the COMPLETENESS + UNAMBIGUITY of the design-lock — an under-specified tier-selection or storage shape lets IMPL 340 ship a double-pay or a mis-keyed mapping.

| Boundary | Description |
|----------|-------------|
| SPEC design-lock → IMPL 340 | An ambiguous tier-precedence rule lets the symbol-first bonus double-pay alongside a quadrant-first reward; a mis-stated storage key lets the dedup/first bits collide. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-339-04 | Elevation of privilege (economic over-pay) | tier-precedence selection | mitigate | The acceptance contract writes the quadrant-first-before-symbol-first ordering + the both-bits-marking + the suppression rule + the no-later-re-collect invariant as a binding three-branch table — the precise double-pay trap the IMPL must avoid; TST-02 (341) proves it empirically. |
| T-339-05 | Tampering | storage shape / level key | mitigate | The design-lock fixes all three mappings to the uint24 level key (cites traitBurnTicket :416 precedent) appended to the shared delegatecall layout; the uint32 slot-width cap gets a written disposition (D-01) so no silent overflow assumption ships. |
| T-339-SC | Tampering | npm/pip/cargo installs | accept | No package installs — paper-only Markdown authoring against the frozen tree. |
</threat_model>

<verification>
- Both design-lock documents exist in the phase directory.
- BINGO design-lock: signature + uint32 slot-width disposition + three mappings (uint24 key) + traitId derivation + module placement + six constants verbatim + reward paths/dedup/no-op/cutoff.
- Tier-precedence acceptance contract: quadrant-first-before-symbol-first ordering, three-branch table, both-bits quadrant-first rule, suppression-guarantee invariant, BINGO-03/TST-02 binding.
- No contracts/*.sol modified (paper-only): `git diff --name-only -- contracts/` is empty.
</verification>

<success_criteria>
- 339-DESIGN-LOCK-BINGO.md satisfies all Task 1 acceptance criteria (BATCH-01 / SC1).
- 339-TIER-PRECEDENCE-ACCEPTANCE-CONTRACT.md satisfies all Task 2 acceptance criteria (BATCH-01 / SC3 / item 7).
- Zero contracts/*.sol edits.
</success_criteria>

<output>
Create `.planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-02-SUMMARY.md` when done
</output>
