---
phase: 339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-REBAL-JACK-ATTESTATION.md
  - .planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-GREP-ATTESTATION-EDIT-ORDER.md
autonomous: true
requirements: [BATCH-01]

must_haves:
  truths:
    - "The COMPLETE pool-BPS set is enumerated and shown to sum to exactly 10000 (D-11) — including the missing ~2000 bps located at CREATOR_BPS=2000 (StakedDegenerusStonk.sol:291)"
    - "The REBAL swap (AFFILIATE 3500→3000 at :295, REWARD 500→1000 at :297) is shown net-zero with NO change to total sDGNRS supply (Pool.Reward 50B→100B; affiliate ~14% haircut; no other pool/constant perturbed)"
    - "The JACK deletion targets (branch :1339-1352, FINAL_DAY_DGNRS_BPS=100 at :191, JackpotDgnrsWin decl :112 + sole emit :1350) are confirmed cleanly orphaned by the deletion"
    - "The preserved isFinalDay plumbing is attested (the lvl+1 ticket-index gate :617 + the _paySoloBucket callers :1085/1095/1135/1161/1190/1312) — no non-Pool.Reward final-day behavior broken"
    - "Every cited file:line is grep-attested vs 812abeee (== current HEAD, contracts/ diff empty) and the producer-before-consumer IMPL-340 edit-order map is recorded (D-13)"
  artifacts:
    - path: ".planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-REBAL-JACK-ATTESTATION.md"
      provides: "The REBAL BPS-sum invariant + the JACK final-day deletion side-effects attestation (SC4, BATCH-01)"
      contains: "10000"
    - path: ".planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-GREP-ATTESTATION-EDIT-ORDER.md"
      provides: "The grep-attestation table vs 812abeee + the producer-before-consumer edit-order map (SC5, BATCH-01, D-13)"
      contains: "812abeee"
  key_links:
    - from: "339-REBAL-JACK-ATTESTATION.md"
      to: "contracts/StakedDegenerusStonk.sol:291/294-298 (full pool-BPS set) + DegenerusGameJackpotModule.sol:191/1339-1352/1350/617"
      via: "BPS-sum enumeration + deletion-side-effect attestation"
      pattern: "CREATOR_BPS|10000|FINAL_DAY_DGNRS_BPS|JackpotDgnrsWin"
    - from: "339-GREP-ATTESTATION-EDIT-ORDER.md"
      to: "all cited file:line anchors across the milestone scope"
      via: "grep-attestation table + edit-order map"
      pattern: "812abeee|producer-before-consumer|edit-order"
---

<objective>
Discharge the SPEC's verification charge for the two satellite items (REBAL + JACK) and close out the grep-attestation / edit-order map (BATCH-01, SC4 + SC5). The REBAL attestation must ENUMERATE THE COMPLETE pool-BPS set and prove it sums to exactly 10000 (D-11 — the 5 visible constants near :294-298 sum to only 8000; the missing ~2000 must be LOCATED and included). The JACK attestation must confirm the final-day Pool.Reward deletion targets are cleanly orphaned (D-12) and the rest of the isFinalDay plumbing is preserved. The grep-attestation doc re-confirms every cited file:line vs the v50.0-closure HEAD 812abeee and records the producer-before-consumer edit-order map for the 340 diff (D-13).

Purpose: SC4 and SC5 are the "no by-construction assumption survives un-checked" deliverables. The REBAL BPS-sum is the invariant that proves the rebalance doesn't silently break the pool allocation; the JACK orphan-check is what lets the deletion land without dangling references; the edit-order map is what lets IMPL 340 author the diff in a no-intermediate-broken-state order.
Output: Two markdown documents in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-CONTEXT.md
@.planning/PLAN-V51-CLAIMBINGO-COLOR-COMPLETION.md

<interfaces>
<!-- All anchors confirmed against the live tree == 812abeee (git diff 812abeee HEAD -- contracts/ is EMPTY → grep at HEAD == grep at 812abeee for contracts/). -->
<!-- REBAL: the FULL pool-BPS set (D-11) — the missing ~2000 is CREATOR_BPS at :291: -->
- contracts/StakedDegenerusStonk.sol:291 — `uint16 private constant CREATOR_BPS = 2000;`            <-- the missing ~2000 the SPEC must locate + include
- contracts/StakedDegenerusStonk.sol:294 — `uint16 private constant WHALE_POOL_BPS = 1000;`
- contracts/StakedDegenerusStonk.sol:295 — `uint16 private constant AFFILIATE_POOL_BPS = 3500;`     <-- REBAL: 3500 → 3000
- contracts/StakedDegenerusStonk.sol:296 — `uint16 private constant LOOTBOX_POOL_BPS = 2000;`
- contracts/StakedDegenerusStonk.sol:297 — `uint16 private constant REWARD_POOL_BPS = 500;`         <-- REBAL: 500 → 1000
- contracts/StakedDegenerusStonk.sol:298 — `uint16 private constant PRESALE_BOX_POOL_BPS = 1000;`
- FULL SET sum: CREATOR 2000 + WHALE 1000 + AFFILIATE 3500 + LOOTBOX 2000 + REWARD 500 + PRESALE_BOX 1000 = 10000 ✓
- Post-REBAL: CREATOR 2000 + WHALE 1000 + AFFILIATE 3000 + LOOTBOX 2000 + REWARD 1000 + PRESALE_BOX 1000 = 10000 ✓ (net-zero +500/−500)
- contracts/StakedDegenerusStonk.sol:355-359 — the per-pool amount derivations `(INITIAL_SUPPLY * X_POOL_BPS) / BPS_DENOM` (proves the constants ARE the allocation; Pool.Reward 50B→100B; total INITIAL_SUPPLY unchanged)

<!-- JACK deletion + preserved plumbing (D-12): -->
- contracts/modules/DegenerusGameJackpotModule.sol:112 — `event JackpotDgnrsWin(address indexed winner, uint256 amount);` (decl; sole emit :1350)
- contracts/modules/DegenerusGameJackpotModule.sol:191 — `uint16 private constant FINAL_DAY_DGNRS_BPS = 100;` (sole use :1343, inside the branch)
- contracts/modules/DegenerusGameJackpotModule.sol:1339 — `if (isFinalDay) {` (branch open; the :1339-1352 Pool.Reward draw to DELETE; :1343 reward calc, :1350 emit)
- PRESERVED: :617 `isFinalDay ? lvl + 1 : lvl` (the lvl+1 ticket-index gate) + the _paySoloBucket callers passing isFinalDay at :1085/1095/1135/1161/1190/1312 — UNTOUCHED
- contracts/modules/DegenerusGameJackpotModule.sol:654 — `traitBurnTicket[lvl]` (the read-only consumer surface BINGO shares; in the SAME module as the JACK deletion)

<!-- The other milestone-scope anchors to attest (SC5): -->
- contracts/storage/DegenerusGameStorage.sol:416 — traitBurnTicket decl (uint24 key); :285 gameOver
- contracts/DegenerusTraitUtils.sol:17-39 — trait byte [QQ][CCC][SSS]
- contracts/DegenerusGame.sol:2701/2730/2813 — traitBurnTicket write-sites; the GAME_*_MODULE.delegatecall dispatch (:280/:521 etc.)
- contracts/StakedDegenerusStonk.sol:464 poolBalance, :485 transferFromPool (clamped return)
- contracts/modules/DegenerusGameDegeneretteModule.sol:1145-1155 — the transferFromPool(Pool.Reward,...) reference pattern (_awardDegeneretteDgnrs; :1148 the empty-pool `if (poolBalance == 0) return;`)
- contracts/modules/DegenerusGameMintModule.sol:1322 — the coinflip.creditFlip(...) BURNIE reference pattern

<!-- Edit-order map (D-13) producer-before-consumer for IMPL 340: -->
- Step 1 (producers): append the 3 storage mappings to DegenerusGameStorage.sol + new DegenerusGameBingoModule.sol + new GAME_BINGO_MODULE in ContractAddresses.sol
- Step 2 (consumer wiring): DegenerusGame.claimBingo external entrypoint delegatecall + the interface signature
- Step 3 (isolated): StakedDegenerusStonk constructor rebalance (:295/:297)
- Step 4 (isolated): JackpotModule final-day Pool.Reward deletion (:1339-1352 + :191 + :112)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attest the REBAL BPS-sum invariant + the JACK final-day deletion side-effects (SC4, D-11/D-12)</name>
  <files>.planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-REBAL-JACK-ATTESTATION.md</files>
  <read_first>
    - contracts/StakedDegenerusStonk.sol (read the pool-BPS constant block :291-298 in FULL — CREATOR_BPS :291, WHALE_POOL_BPS :294, AFFILIATE_POOL_BPS :295, LOOTBOX_POOL_BPS :296, REWARD_POOL_BPS :297, PRESALE_BOX_POOL_BPS :298 — and the per-pool amount derivations :355-359; grep for any OTHER `_BPS` constant in the file to be certain the set is complete and CREATOR_BPS is the only non-:294-298 member of the pool allocation)
    - contracts/modules/DegenerusGameJackpotModule.sol (read the JackpotDgnrsWin decl :112, the FINAL_DAY_DGNRS_BPS constant :191, the isFinalDay branch :1339-1352 incl the :1343 reward calc + :1350 emit; then grep the WHOLE file for `FINAL_DAY_DGNRS_BPS` and `JackpotDgnrsWin` to CONFIRM each has its sole use/emit inside the deleted branch; read :617 the lvl+1 gate and the caller sites :1085/1095/1135/1161/1190/1312 to confirm they pass isFinalDay for non-Pool.Reward purposes)
    - .planning/PLAN-V51-CLAIMBINGO-COLOR-COMPLETION.md ("Co-requisite rebalance" the pool table + "What this replaces" the JACK deletion list + "Open before SPEC" item 6)
    - .planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-CONTEXT.md (D-11 the COMPLETE pool-BPS set must sum to 10000 [the 5 visible sum to 8000 — find the rest], D-12 the JACK clean-orphan + preserved-plumbing attestation)
  </read_first>
  <action>
    Author 339-REBAL-JACK-ATTESTATION.md in two parts. PART 1 — REBAL (D-11): enumerate the COMPLETE pool-BPS set as a table and show it sums to exactly 10000. The 5 constants near :294-298 (WHALE 1000 / AFFILIATE 3500 / LOOTBOX 2000 / REWARD 500 / PRESALE_BOX 1000) sum to only 8000 — LOCATE the remaining 2000 and include it: it is CREATOR_BPS=2000 at StakedDegenerusStonk.sol:291. Show the full set { CREATOR 2000 (:291), WHALE 1000 (:294), AFFILIATE 3500 (:295), LOOTBOX 2000 (:296), REWARD 500 (:297), PRESALE_BOX 1000 (:298) } = 10000 before, and { CREATOR 2000, WHALE 1000, AFFILIATE 3000, LOOTBOX 2000, REWARD 1000, PRESALE_BOX 1000 } = 10000 after the REBAL swap (AFFILIATE 3500→3000 at :295, REWARD 500→1000 at :297). State the swap is net-zero (+500 reward / −500 affiliate) → BPS-sum invariant trivially preserved; total sDGNRS supply UNCHANGED (only the affiliate↔reward split shifts; cite the :355-359 `(INITIAL_SUPPLY * X_POOL_BPS)/BPS_DENOM` derivations as proof the constants are the allocation); Pool.Reward 50B→100B (×2); affiliate per-share ~14% haircut (350B→300B). Attest NO other pool/constant is perturbed (only :295 and :297 change). PART 2 — JACK (D-12): attest the deletion targets are cleanly orphaned — FINAL_DAY_DGNRS_BPS=100 (:191) has its SOLE use at :1343 inside the :1339-1352 branch, and JackpotDgnrsWin (decl :112) has its SOLE emit at :1350 inside the branch (record the grep result confirming no other use/emit). Then attest the PRESERVED plumbing: the lvl+1 ticket-index gate (:617 `isFinalDay ? lvl + 1 : lvl`) and the _paySoloBucket callers passing isFinalDay (:1085/1095/1135/1161/1190/1312) are UNTOUCHED — only the Pool.Reward draw goes away, no non-Pool.Reward final-day behavior is broken. Note the JACK deletion is in the SAME module file (DegenerusGameJackpotModule.sol) the BINGO module reads traitBurnTicket from (:654) → the read-only consumer + the final-day deletion land coherently in one diff. Cite every file:line against the live tree (== 812abeee).
  </action>
  <acceptance_criteria>
    - 339-REBAL-JACK-ATTESTATION.md exists and PART 1 enumerates the COMPLETE pool-BPS set summing to exactly 10000, locating the missing ~2000 as CREATOR_BPS=2000 at StakedDegenerusStonk.sol:291
    - PART 1 shows BOTH the before set (sum 10000) and the after set (sum 10000) and states the swap is net-zero (+500/−500)
    - PART 1 attests total sDGNRS supply is unchanged (cites the :355-359 INITIAL_SUPPLY*BPS/BPS_DENOM derivations), Pool.Reward 50B→100B, affiliate ~14% haircut, and that only :295 + :297 change (no other pool/constant perturbed)
    - PART 2 confirms FINAL_DAY_DGNRS_BPS (:191) has its sole use at :1343 inside the deleted :1339-1352 branch
    - PART 2 confirms JackpotDgnrsWin (decl :112) has its sole emit at :1350 inside the deleted branch (cleanly orphaned)
    - PART 2 attests the preserved plumbing: the lvl+1 gate :617 and the callers :1085/1095/1135/1161/1190/1312 are untouched; no non-Pool.Reward final-day behavior broken
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-REBAL-JACK-ATTESTATION.md; test -f "$F" && grep -q "CREATOR_BPS" "$F" && grep -q "291" "$F" && grep -q "10000" "$F" && grep -q "3000" "$F" && grep -q "FINAL_DAY_DGNRS_BPS" "$F" && grep -q "191" "$F" && grep -q "JackpotDgnrsWin" "$F" && grep -q "1350" "$F" && grep -q "617" "$F" && grep -qiE "net-zero|net zero" "$F" && grep -qiE "50B|100B|unchanged" "$F"</automated>
  </verify>
  <done>The REBAL+JACK attestation enumerates the full pool-BPS set (incl CREATOR_BPS=2000 at :291) summing to 10000 before and after, proves the net-zero swap with unchanged supply, and attests the JACK deletion targets are cleanly orphaned with the isFinalDay plumbing preserved — citing real anchors.</done>
</task>

<task type="auto">
  <name>Task 2: Grep-attest every cited file:line vs 812abeee + record the producer-before-consumer edit-order map (SC5, D-13)</name>
  <files>.planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-GREP-ATTESTATION-EDIT-ORDER.md</files>
  <read_first>
    - Run `git diff --stat 812abeee HEAD -- contracts/` to confirm the contracts/ diff vs the v50.0-closure HEAD is EMPTY (so grep at current HEAD == grep at 812abeee for contracts/ — record this as the shortcut, but still SHOW each anchor line-by-line per the cross-cutting "no by-construction survives un-checked" rule)
    - contracts/storage/DegenerusGameStorage.sol (traitBurnTicket :416, gameOver :285)
    - contracts/DegenerusTraitUtils.sol (trait byte layout :17-39)
    - contracts/DegenerusGame.sol (traitBurnTicket write-sites :2701/:2730/:2813; the GAME_*_MODULE.delegatecall dispatch pattern)
    - contracts/StakedDegenerusStonk.sol (pool-BPS constants :291/:294-298; poolBalance :464; transferFromPool :485)
    - contracts/modules/DegenerusGameJackpotModule.sol (JackpotDgnrsWin :112; FINAL_DAY_DGNRS_BPS :191; isFinalDay branch :1339-1352 + emit :1350; lvl+1 gate :617; callers :1085/1095/1135/1161/1190/1312; traitBurnTicket :654)
    - contracts/modules/DegenerusGameDegeneretteModule.sol (transferFromPool(Pool.Reward,...) reference :1145-1155, empty-pool guard :1148)
    - contracts/modules/DegenerusGameMintModule.sol (coinflip.creditFlip reference :1322)
    - .planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-CONTEXT.md (D-13 grep-attest EVERY anchor vs 812abeee + the producer-before-consumer edit-order; the scout-confirmed empty-diff shortcut)
  </read_first>
  <action>
    Author 339-GREP-ATTESTATION-EDIT-ORDER.md in two parts. PART 1 — GREP-ATTESTATION TABLE (D-13): first state and verify the shortcut — `git diff --stat 812abeee HEAD -- contracts/` is EMPTY (the only commits since 812abeee are v51 planning docs), so grepping current HEAD == grepping 812abeee for contracts/; but per the cross-cutting "no by-construction claim survives un-checked" rule, SHOW each anchor line-by-line anyway. Build a table with columns: Anchor (file:line) | Cited content | Confirmed at HEAD==812abeee? | Drift correction (if any). Attest EVERY milestone-scope anchor: DegenerusGameStorage.sol:416 (traitBurnTicket decl), :285 (gameOver); DegenerusTraitUtils.sol:17-39 (trait byte [QQ][CCC][SSS]); DegenerusGame.sol:2701/2730/2813 (write-sites) + the GAME_*_MODULE.delegatecall dispatch; StakedDegenerusStonk.sol:291 (CREATOR_BPS), :295 (AFFILIATE_POOL_BPS), :297 (REWARD_POOL_BPS), :464 (poolBalance), :485 (transferFromPool); DegenerusGameJackpotModule.sol:112/191/617/654/1339-1352/1350 + callers :1085/1095/1135/1161/1190/1312; DegenerusGameDegeneretteModule.sol:1145-1155 (transferFromPool ref); DegenerusGameMintModule.sol:1322 (creditFlip ref). RECORD any drift between the CONTEXT/plan-doc-cited line and the actual confirmed line and CORRECT it in the table — specifically the REBAL anchors (the plan doc says ":294-298" but the missing CREATOR_BPS lives at :291), the DegeneretteModule transferFromPool ref (CONTEXT cited :1135-1159, actual region :1145-1155), and the creditFlip ref (CONTEXT cited :1319, actual :1322) — note these are reference-pattern citations, not modification targets, so the small drift is informational. PART 2 — PRODUCER-BEFORE-CONSUMER EDIT-ORDER MAP for IMPL 340: record the ordered steps so the single batched diff has NO intermediate broken state — Step 1 (producers): append bingoClaimed/firstQuadrant/firstSymbol to DegenerusGameStorage.sol + create DegenerusGameBingoModule.sol + add GAME_BINGO_MODULE to ContractAddresses.sol; Step 2 (consumer wiring): add the DegenerusGame.claimBingo external entrypoint delegatecall + the interface signature; Step 3 (isolated): StakedDegenerusStonk constructor rebalance (:295 3500→3000, :297 500→1000); Step 4 (isolated): JackpotModule final-day Pool.Reward deletion (:1339-1352 branch + :191 constant + :112 event). State WHY the order is correct (storage + module exist before the entrypoint references them; the REBAL + JACK edits are isolated and order-independent relative to BINGO but listed last). Note this is the binding edit-order for BATCH-02 at Phase 340. Cite every file:line.
  </action>
  <acceptance_criteria>
    - 339-GREP-ATTESTATION-EDIT-ORDER.md exists and PART 1 states the `git diff 812abeee HEAD -- contracts/` empty-diff shortcut AND still shows a per-anchor attestation table
    - The table attests every milestone-scope anchor (Storage:416/:285, TraitUtils:17-39, Game:2701/2730/2813, StakedStonk:291/295/297/464/485, JackpotModule:112/191/617/654/1339-1352/1350 + the 6 callers, DegeneretteModule transferFromPool ref, MintModule creditFlip ref)
    - The table records the drift corrections: the CREATOR_BPS-at-:291 (vs the plan doc's :294-298 framing), the DegeneretteModule transferFromPool ref region, and the creditFlip ref line — noting which are modification targets vs reference patterns
    - PART 2 records the producer-before-consumer edit-order map with the 4 ordered steps (storage+module+ContractAddresses → entrypoint+interface → REBAL → JACK)
    - PART 2 states why the order yields no intermediate broken state and names it the binding edit-order for BATCH-02 at Phase 340
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-GREP-ATTESTATION-EDIT-ORDER.md; test -f "$F" && grep -q "812abeee" "$F" && grep -q "416" "$F" && grep -q "2701" "$F" && grep -q "291" "$F" && grep -q "1339" "$F" && grep -qiE "producer-before-consumer|producer before consumer" "$F" && grep -qiE "edit.order" "$F" && grep -q "GAME_BINGO_MODULE" "$F" && grep -qiE "drift" "$F" && grep -q "BATCH-02" "$F"</automated>
  </verify>
  <done>The grep-attestation doc records the empty-diff shortcut + a per-anchor table confirming every milestone anchor vs 812abeee with drift corrections, and the producer-before-consumer 4-step edit-order map binding for BATCH-02 at Phase 340 — citing real anchors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a paper-only SPEC phase that ships no code. The threat surface is the COMPLETENESS of the attestations — a missed pool constant breaks the BPS-sum invariant; a missed JackpotDgnrsWin emitter leaves a dangling reference; an un-attested anchor lets a "by construction" claim ship at IMPL.

| Boundary | Description |
|----------|-------------|
| SPEC attestation → IMPL 340 | A wrong BPS-sum lets the rebalance silently mis-allocate; an incomplete orphan-check lets the JACK deletion break compile or leave dead code; un-grepped anchors let drift ship. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-339-06 | Tampering | REBAL pool-BPS allocation | mitigate | The attestation enumerates the COMPLETE pool-BPS set (locates the missing CREATOR_BPS=2000 at :291) and proves sum=10000 before and after the net-zero swap; cites the :355-359 allocation derivations so the supply-unchanged claim is grounded, not assumed. |
| T-339-07 | Tampering | JACK deletion dangling references | mitigate | The orphan-check greps the WHOLE JackpotModule for FINAL_DAY_DGNRS_BPS + JackpotDgnrsWin to confirm sole use/emit inside the deleted branch; the preserved-plumbing attestation (the :617 gate + the 6 callers) ensures no non-Pool.Reward final-day behavior breaks. |
| T-339-08 | Tampering | un-attested anchors / drift | mitigate | The grep-attestation table re-confirms every cited file:line vs 812abeee (feedback_verify_call_graph_against_source) and records the drift corrections (CREATOR_BPS at :291, the reference-pattern line shifts) so no by-construction citation ships uncorrected. |
| T-339-SC | Tampering | npm/pip/cargo installs | accept | No package installs — paper-only Markdown authoring against the frozen tree. |
</threat_model>

<verification>
- Both attestation documents exist in the phase directory.
- REBAL+JACK attestation: the full pool-BPS set summing to 10000 (incl CREATOR_BPS=2000 at :291) before and after, the net-zero swap, unchanged supply, the cleanly-orphaned JACK targets, the preserved isFinalDay plumbing.
- Grep-attestation + edit-order: the empty-diff shortcut + the per-anchor table with drift corrections, the 4-step producer-before-consumer edit-order map.
- No contracts/*.sol modified (paper-only): `git diff --name-only -- contracts/` is empty.
</verification>

<success_criteria>
- 339-REBAL-JACK-ATTESTATION.md satisfies all Task 1 acceptance criteria (BATCH-01 / SC4).
- 339-GREP-ATTESTATION-EDIT-ORDER.md satisfies all Task 2 acceptance criteria (BATCH-01 / SC5 / D-13).
- Zero contracts/*.sol edits.
</success_criteria>

<output>
Create `.planning/phases/339-spec-design-lock-rng-freeze-safety-proof-tier-precedence-loc/339-03-SUMMARY.md` when done
</output>
