---
phase: 304-spec-invariant-model-spec
plan: 03
type: execute
wave: 3
depends_on:
  - 304-01
  - 304-02
files_modified:
  - .planning/phases/304-spec-invariant-model-spec/304-SPEC.md
requirements:
  - EDGE-01
  - EDGE-02
  - EDGE-03
  - EDGE-04
  - EDGE-05
  - EDGE-06
  - EDGE-07
  - EDGE-08
  - EDGE-09
  - EDGE-10
  - EDGE-11
  - EDGE-12
  - EDGE-13
  - EDGE-14
  - EDGE-15
  - EDGE-16
  - EDGE-17
  - EDGE-18
autonomous: true

must_haves:
  truths:
    - "§3 contains exactly 18 EDGE-NN subsections, one per requirement ID, ordered EDGE-01..EDGE-18"
    - "Every EDGE-NN entry has a Positive assertion (correct outcome under the case) + Negative assertion (revert or no-exploit) stated narratively"
    - "EDGE-07 V-184 attack reproduction explicitly states the same-day post-resolve re-burn → next-advance overwrite mechanic from RNGLOCK-FIXREC §103, and asserts the negative outcome: redemptionPeriods[D].roll byte-identical to first resolution; no overwrite"
    - "EDGE-08 gameOver-mid-pending resolves per SPEC-04 (a) lock; EDGE-13 zero-rounded ethValueOwed resolves per SPEC-04 (b) lock"
    - "Every EDGE entry maps to (cites) the INV-NN(s) it tests and the SPEC-NN(s) it depends on"
    - "Every EDGE entry is mechanizable into a single Foundry fuzz function (testFuzz_EDGE_NN_<descriptive_name>) without further interpretation"
  artifacts:
    - path: ".planning/phases/304-spec-invariant-model-spec/304-SPEC.md"
      provides: "§3 EDGE-01..18 exhaustive scenario enumeration"
      contains: "EDGE-01:, EDGE-02:, EDGE-03:, EDGE-04:, EDGE-05:, EDGE-06:, EDGE-07:, EDGE-08:, EDGE-09:, EDGE-10:, EDGE-11:, EDGE-12:, EDGE-13:, EDGE-14:, EDGE-15:, EDGE-16:, EDGE-17:, EDGE-18:"
  key_links:
    - from: "§3 EDGE-NN entries"
      to: "§1 INV-NN entries they exercise"
      via: "explicit Tests INV-NN cite"
      pattern: "Tests INV-0[1-9]|Tests INV-1[0-2]"
    - from: "§3 EDGE-NN entries"
      to: "§2 SPEC-NN entries they depend on (EDGE-08 → SPEC-04 (a); EDGE-13 → SPEC-04 (b); EDGE-14 → SPEC-05; EDGE-15 → SPEC-02 + INV-11)"
      via: "explicit Depends on SPEC-NN cite"
      pattern: "Depends on SPEC-0[1-5]"
    - from: "§3 EDGE-07"
      to: ".planning/RNGLOCK-FIXREC.md §103 V-184 mechanic"
      via: "verbatim attack-sequence enumeration"
      pattern: "V-184|post-resolve.*re-burn|HANDOFF-11[1-7]"
---

<objective>
Fill `## §3 — Edge Scenario Enumeration (EDGE-01..18)` with 18 narrative scenario entries, each with a Positive assertion + Negative assertion + INV/SPEC linkage. Phase 306 mechanizes each EDGE-NN into a Foundry fuzz function in `test/fuzz/RedemptionEdgeCases.t.sol` — this section is that test's contract.

Purpose: §3 is the threat enumeration locus per Phase 304 threat-model note. EDGE-07 is the V-184 attack reproduction — the headline finding from RNGLOCK-FIXREC §103. The other 17 cover the (burn × advance × claim × gameOver × transfer × admin) interleaving surface.

Output: §3 of 304-SPEC.md fully populated with EDGE-01..18 narrative entries; each entry is mechanizable into a Foundry fuzz function without interpretation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/304-spec-invariant-model-spec/304-SPEC.md
@.planning/phases/304-spec-invariant-model-spec/304-02-SUMMARY.md
@/home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md
@/home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_backward_trace.md
@/home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_commitment_window.md
@contracts/StakedDegenerusStonk.sol
</context>

<interfaces>
<!-- EDGE-07 V-184 attack mechanic — full backward-trace lives at .planning/RNGLOCK-FIXREC.md §103 (line 5410+) -->
<!-- The attack: same-day post-resolve re-burn → STALE redemptionPeriodIndex points at the already-resolved period -->
<!-- → next-advance resolveRedemptionPeriod overwrites redemptionPeriods[period].roll → cross-day re-roll exploit -->
<!-- POST-REFACTOR: redemptionPeriodIndex slot REMOVED (SPEC-01); pendingByDay[D] is independently keyed; -->
<!-- the next advance reads pendingByDay[dayToResolve = D] (NOT a stale index) → CANNOT overwrite redemptionPeriods[D-1] -->
<!-- EDGE-07 NEGATIVE assertion: redemptionPeriods[D].roll byte-identical across all post-resolution write attempts -->

<!-- Pre-refactor source locations EDGE-NN entries cite: -->
<!-- contracts/StakedDegenerusStonk.sol :486-495 burn() public entry (EDGE-11/12 RNG/liveness gates) -->
<!-- :487 game.gameOver() branch (EDGE-08 path) -->
<!-- :491 livenessTriggered revert (EDGE-12) -->
<!-- :492 rngLocked revert (EDGE-11) -->
<!-- :585-610 resolveRedemptionPeriod (EDGE-04 same-day multi-player; EDGE-09 multi-player same-day claim) -->
<!-- :632 totalRolledEth = (claim.ethValueOwed * roll) / 100 (EDGE-09 dust accumulation) -->
<!-- :638-643 50/50 split + isGameOver branch (EDGE-08 gameOver-post-resolve claim) -->
<!-- :654 burniePayout coinflip-result dependency (EDGE-18 BURNIE pool insufficient) -->
<!-- :657 pendingRedemptionEthValue -= totalRolledEth (EDGE-09 conservation) -->
<!-- :763 50% supply cap check (EDGE-14) -->
<!-- :801 160 ETH EV cap check (EDGE-15) -->
<!-- :842-852 _payBurnie balance + coinflip fallback (EDGE-18) -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fill §3 EDGE-01..18 narrative scenario enumeration</name>
  <files>.planning/phases/304-spec-invariant-model-spec/304-SPEC.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 70-89 (canonical EDGE-01..18 wording)
    - `.planning/ROADMAP.md` Phase 304 success criterion #3 (line 50 — "positive + negative assertions stated narratively, ready for Phase 306 to mechanically turn into Foundry fuzz functions")
    - `.planning/ROADMAP.md` Phase 306 TST goal (lines 71-82 — names the 3 new test files + the mod to RngLockDeterminism.t.sol; EDGE-NN → testFuzz_EDGE_NN mapping)
    - `.planning/phases/304-spec-invariant-model-spec/304-SPEC.md` (current — §1 INV statements + §2 SPEC locks; EDGE-NN entries cite both)
    - `.planning/RNGLOCK-FIXREC.md` lines 5410-5520 (§103 — V-184 attack mechanic; EDGE-07 reproduction is THE headline test for v44.0)
    - `contracts/StakedDegenerusStonk.sol` lines 486-495 (burn entry guards — EDGE-11/12 RNG/liveness reverts), 585-610 (resolveRedemptionPeriod — EDGE-02/04/09), 618-684 (claimRedemption — EDGE-05/08/09/18), 763 (50% supply cap — EDGE-14), 801 (160 ETH EV cap — EDGE-15), 842-852 (_payBurnie fallback — EDGE-18)
    - `/home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_backward_trace.md` (every RNG-window EDGE must backward-trace from consumer to input commitment time)
    - `/home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_commitment_window.md` (check player-controllable state between VRF request and fulfillment — relevant for EDGE-06/07/11)
  </read_first>
  <action>
    Under `## §3 — Edge Scenario Enumeration (EDGE-01..18)`, remove the placeholder and write 18 subsections. Each subsection uses this exact structure:

    `### EDGE-NN: <short name from REQUIREMENTS.md>`

    Six labeled sub-fields per EDGE-NN entry:
    - `**Scenario:**` — narrative setup; actors, sequence of actions, timing, wall-clock day references; 2-6 sentences.
    - `**Positive assertion:**` — what MUST happen under the scenario; the correct-behavior outcome.
    - `**Negative assertion:**` — what MUST NOT happen — either a specific revert (name the error from `contracts/StakedDegenerusStonk.sol` errors at `:88-117`) OR a "no exploit" attestation (a specific storage value MUST be byte-identical to its earlier observed value).
    - `**Tests INV-NN:**` — which INV-NN(s) the scenario exercises — at minimum one.
    - `**Depends on SPEC-NN:**` — which SPEC-NN locks the scenario depends on for its existence or its expected outcome.
    - `**Foundry function name:**` — suggested name for Phase 306; format `testFuzz_EDGE_NN_<descriptive_name>`.

    Per-EDGE content guidance (each entry must restate the REQUIREMENTS.md wording verbatim then expand):

    - **EDGE-01 (pre-advance-gap burn on day D):** Wall flipped to D; day-D advance not yet fired; player burns. Positive: burn lands in `pendingByDay[D]` (NOT `pendingByDay[D-1]`); cumulative `pendingRedemptionEthValue` correctly reflects both pools. Negative: `pendingByDay[D-1].ethBase` UNCHANGED by this burn (byte-identical to pre-burn value). Tests INV-08. Depends on SPEC-01 (per-day pool keying) + SPEC-03 (dayToResolve = D-1).

    - **EDGE-02 (two pending days simultaneously):** D-1 unresolved + D accumulating. Positive: day-D advance resolves D-1 only (`pendingByDay[D-1]` cleared via `delete`); `pendingByDay[D]` untouched. Negative: `pendingByDay[D].ethBase` value byte-identical before/after the advance call; `redemptionPeriods[D].roll == 0` after the advance. Tests INV-08 + INV-09. Depends on SPEC-03 (dayToResolve).

    - **EDGE-03 (single player burns multiple days, never claims):** Burns on D, D+1, D+2; each `pendingRedemptions[player][D{,+1,+2}]` independently stored. Positive: each claim resolvable independently in any order. Negative: claim on D+2 does not affect claim on D (no cross-day state mutation). Tests INV-04 + INV-07. Depends on SPEC-02 (composite key).

    - **EDGE-04 (multiple players burn same day, different times relative to advance):** A burns pre-advance, B burns post-advance same wall day D. Positive: both in `pendingByDay[D]`; both resolve at day-D+1's advance with same `R_{D+1}`. Negative: A's `ethValueOwed` and B's `ethValueOwed` written into the same `pendingByDay[D]` pool — neither overwrites the other. Tests INV-04 + INV-06. Depends on SPEC-01.

    - **EDGE-05 (player claims before advance fires):** Burns day D, calls `claimRedemption(D)` before day-D+1's advance. Positive: revert `NotResolved` (error at `:114`). Negative: no state mutation on the failed claim attempt. Tests INV-07. Depends on SPEC-02.

    - **EDGE-06 (skipped advance, long stall):** Burns day D; advance for day-D+1 delayed 12h+; eventual advance fires. Positive: resolves day D with day-D+1's eventual VRF word (or retryLootboxRng failsafe word) — claim succeeds with the eventual roll. Negative: no stuck claim path; `redemptionPeriods[D].roll != 0` after the deferred advance. Tests INV-09. Depends on SPEC-03. NOTE: cite `feedback_rng_backward_trace.md` — backward-trace from the player's eventual claim outcome to the VRF input commitment.

    - **EDGE-07 (V-184 ATTACK REPRODUCTION — THE HEADLINE NEGATIVE TEST):** Same-day post-resolve re-burn → next-advance overwrite (the catastrophic mechanic at RNGLOCK-FIXREC §103 lines 5443-5470). Attack sequence: Player A burns day D; day-D+1 advance resolves with `R_{D+1}` and writes `redemptionPeriods[D].roll = R_{D+1}`; attacker B (or attacker A) burns 1 wei sDGNRS post-resolve same wall day D; day-D+2 advance fires. Positive: at day-D+2 advance, `resolveRedemptionPeriod` reads `pendingByDay[dayToResolve = D+1]` (NOT `pendingByDay[D]` which was already deleted by SPEC-04 (c) at day-D+1 resolve) — `pendingByDay[D+1]` either contains only the 1-wei re-burn (post-day-boundary timing) or is empty (pre-day-boundary timing, in which case the 1-wei re-burn landed in the now-recreated `pendingByDay[D]` as a fresh per-day entry per SPEC-01 and resolves at the next-next advance with a fresh roll into `redemptionPeriods[D+2]`, NOT overwriting `redemptionPeriods[D]`). Negative: `redemptionPeriods[D].roll` is byte-identical to `R_{D+1}` (its first-write value); NO overwrite under any attack sequence; INV-01 holds (write-once roll). This is THE load-bearing v44.0 negative test. Tests INV-01 + INV-06 + INV-07. Depends on SPEC-01 + SPEC-03 + SPEC-04 (c). Cross-reference: TST-04 (the standalone V-184 reproduction in Phase 306) + TST-05 (the Phase 301 RngLockDeterminism.t.sol `vm.skip(HANDOFF-111..117)` → strict-assertion flip).

    - **EDGE-08 (burn → gameOver → claim):** Burns day D; gameOver triggers before day-D+1 advance OR after resolve but before claim. Positive (per SPEC-04 (a)): if gameOver fires BEFORE day-D+1 advance, the eventual advance still resolves `pendingByDay[D]` normally (advance loop fires); if gameOver fires AFTER resolve but BEFORE claim, claim succeeds with stored `redemptionPeriods[D].roll` and the `isGameOver` branch at `:638-643` gives 100% direct ETH (no lootboxes). Negative: no partial state; no double-payment; no stuck funds. Tests INV-12. Depends on SPEC-04 (a).

    - **EDGE-09 (concurrent claims from N players same day):** N players all burn day D; all claim after day-D+1 advance. Positive: total payouts sum to `(pendingByDay[D].ethBase * R_{D+1}) / 100` ± dust bounded by (N-1) wei (per-claimant floor-division at `:632`); `pendingRedemptionEthValue` decrements by exact `totalRolledEth` per claim (`:657`). Negative: no over-payment in aggregate (sum of all per-claimant `totalRolledEth` ≤ `(pendingByDay[D].ethBase * R_{D+1}) / 100`). Tests INV-02 + INV-05. Depends on SPEC-02.

    - **EDGE-10 (re-entrancy attempt on _payEth):** Malicious recipient contract calls `claimRedemption(D)` re-entrantly from its receive() / fallback() during `_payEth`. Positive: existing CEI ordering at `:683` (pay-eth-LAST) means re-entry attempt finds `pendingRedemptions[player][D]` already deleted (per SPEC-04 (d)) — re-entrant call reverts `NoClaim`. Negative: no double-payout; `pendingRedemptionEthValue` decrements exactly once per claim. Tests INV-02 + INV-07. Depends on SPEC-04 (d).

    - **EDGE-11 (burn during rngLocked window):** Player calls `burn(amount)` while `game.rngLocked() == true`. Positive: revert `BurnsBlockedDuringRng` (error at `:100`). Negative: no state mutation. Tests INV-06 (no roll-input mutation during RNG window). Depends on SPEC-01 (no change to existing guard at `:492`).

    - **EDGE-12 (burn during livenessTriggered window):** Player calls `burn(amount)` while `game.livenessTriggered() == true` AND `game.gameOver() == false`. Positive: revert `BurnsBlockedDuringLiveness` (error at `:105`). Negative: no state mutation. Tests INV-08 (no pending writes in the pre-gameOver-latch window). Depends on SPEC-01 (no change to existing guard at `:491`).

    - **EDGE-13 (zero-rounded ethValueOwed from tiny burn):** Burn 1 wei sDGNRS at a moment when `totalSupply` is very large; `ethValueOwed = (totalMoney * 1) / supplyBefore` rounds to 0. Positive (per SPEC-04 (b)): burn proceeds; `pendingRedemptions[player][D].ethValueOwed += 0`; `pendingByDay[D].ethBase += 0`; no payout at eventual claim but no corruption either. Negative: no overflow; no revert from zero-claim mishandling; cumulative `pendingRedemptionEthValue` does not change. Tests INV-04. Depends on SPEC-04 (b).

    - **EDGE-14 (50% supply cap edge):** Burn exactly `pendingByDay[D].supplySnapshot / 2` succeeds. Burn one wei over reverts `Insufficient`. Same-day subsequent burn after a partial burn checks against the LAZY-INIT snapshot (NOT current totalSupply). Positive: cap value frozen at first-burn snapshot. Negative: cap does not tighten as same-day burns proceed. Tests INV-10. Depends on SPEC-05.

    - **EDGE-15 (160 ETH EV cap edge):** Player accumulates exactly 160 ETH `ethValueOwed` across same-day burns (succeeds). One wei over reverts `ExceedsDailyRedemptionCap` (error at `:117`). Positive: cap enforced per-(player, day). Negative: no silent overflow. Tests INV-11. Depends on SPEC-02.

    - **EDGE-16 (cross-day cap reset):** Burn 160 ETH on D; burn 160 ETH on D+1. Positive: both allowed; per-(player, day) cap resets at day boundary (composite-key keying makes the reset structural — `pendingRedemptions[player][D+1].ethValueOwed` starts at 0). Negative: day-D accumulated value does not affect day-D+1 cap check. Tests INV-11. Depends on SPEC-02 (composite key).

    - **EDGE-17 (burn after resolve same wall-clock day):** Day-D+1's advance fires at 22:58 wall-clock day D; player burns at 23:30 same wall-clock day D. Positive (under per-day keying): burn lands in `pendingByDay[D]` (the same key the resolve just deleted at 22:58); the 23:30 burn re-creates `pendingByDay[D]` as a fresh entry with `supplySnapshot = totalSupply` AT 23:30, `burned = amount`. Eventual day-D+2 advance fires and resolves the new `pendingByDay[D]` entry with a fresh roll into `redemptionPeriods[D+1]` (via the `dayToResolve = D+1` arg). Negative: `redemptionPeriods[D].roll` is byte-identical to its original `R_{D+1}` value — the day-D+2 advance writes to `redemptionPeriods[D+1]`, NOT `redemptionPeriods[D]`. NOTE: this EDGE distinguishes the legitimate same-day-post-resolve re-burn from the V-184 ATTACK in EDGE-07; under post-refactor BOTH outcomes are safe (no overwrite of previously-written rolls). Tests INV-01 + INV-04 + INV-08. Depends on SPEC-01 + SPEC-03 + SPEC-04 (c).

    - **EDGE-18 (BURNIE pool insufficient at claim):** Coinflip pool drained; player claims BURNIE owed. Positive (per existing `_payBurnie` at `:842-852`): falls back correctly — `coin.transfer(player, payBal)` for available balance, then `coinflip.claimCoinflipsForRedemption(address(this), remaining)` for the shortfall, then second `coin.transfer`. Negative: no revert, no stuck claim. Tests INV-03. Depends on SPEC-02 (no change to existing `_payBurnie`).

    Length target: 250-400 lines for §3 total. Each EDGE-NN ~12-25 lines.

    Cross-section footer (end of §3): a 1-line table summarizing INV-NN ← EDGE-NN coverage so Plan 05 can verify the citation manifest. Example row: `INV-08 ← EDGE-01 + EDGE-02 + EDGE-17`. This makes the §1↔§3 cross-link explicit.
  </action>
  <verify>
    <automated>bash -c 'for n in 01 02 03 04 05 06 07 08 09 10 11 12 13 14 15 16 17 18; do grep -q "^### EDGE-$n:" .planning/phases/304-spec-invariant-model-spec/304-SPEC.md || { echo MISSING EDGE-$n; exit 1; }; done; echo ALL_18_EDGE_PRESENT'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^### EDGE-" 304-SPEC.md` returns exactly 18
    - Every EDGE-NN subsection contains all six labeled sub-fields: `**Scenario:**`, `**Positive assertion:**`, `**Negative assertion:**`, `**Tests INV-NN:**`, `**Depends on SPEC-NN:**`, `**Foundry function name:**` (verify: `grep -c "\*\*Scenario:\*\*" 304-SPEC.md` returns 18; same for the other five labels)
    - EDGE-07 explicitly cites the V-184 mechanic with reference to `RNGLOCK-FIXREC §103` and `HANDOFF-111..117`; verify by `grep -c "V-184\|RNGLOCK-FIXREC.*103\|HANDOFF-11[1-7]" 304-SPEC.md` returns ≥ 2
    - EDGE-07 negative assertion explicitly states `redemptionPeriods[D].roll` byte-identical to first-write value
    - EDGE-08 cites SPEC-04 (a); EDGE-13 cites SPEC-04 (b); EDGE-10 cites SPEC-04 (d); EDGE-14 cites SPEC-05
    - EDGE-NN entries that cite a specific revert name a valid error from `contracts/StakedDegenerusStonk.sol` errors at `:88-117` (cross-check: `NotResolved`, `Insufficient`, `BurnsBlockedDuringRng`, `BurnsBlockedDuringLiveness`, `ExceedsDailyRedemptionCap`, `NoClaim`)
    - The §1↔§3 cross-link table at end of §3 lists every INV-01..12 with at least one EDGE-NN exerciser (verify: `grep -c "^INV-0[1-9] ←\|^INV-1[0-2] ←" 304-SPEC.md` returns ≥ 12, or equivalent table-shape match)
    - The placeholder `_To be filled by Plan 03_` under §3 is removed
  </acceptance_criteria>
  <done>304-SPEC.md §3 contains 18 fully-narrated EDGE-NN scenarios, each with Positive + Negative + INV-cite + SPEC-cite + Foundry function name. §1↔§3 cross-link table at end. §4/§5 placeholders untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| §3 EDGE narratives → Phase 306 fuzz function authors | Each EDGE-NN entry is the spec the test function implements; ambiguity here produces under-specified or wrong tests |
| §3 EDGE-07 → RNGLOCK-FIXREC §103 V-184 mechanic | EDGE-07 is the v44.0 closure-blocker test; its mechanic must reproduce the exact attack chain from RNGLOCK-FIXREC §103 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-304-03-01 | Tampering | EDGE-07 attack sequence diverges from V-184 mechanic in RNGLOCK-FIXREC §103 → test does not reproduce the catastrophic scenario | mitigate | Task 1 read_first explicitly cites RNGLOCK-FIXREC.md lines 5410-5520; EDGE-07 entry must restate the same-day post-resolve re-burn + next-advance overwrite mechanic verbatim. Acceptance criterion requires `V-184` + `HANDOFF-11[1-7]` grep match |
| T-304-03-02 | Information disclosure | Negative assertion uses vague language ("works correctly") instead of byte-identity attestation | mitigate | Task 1 action explicitly states Negative assertions must name either a specific revert OR a byte-identity attestation. Acceptance criterion checks for valid error names |
| T-304-03-03 | Repudiation | EDGE entries miss INV-NN coverage → orphan INV-NN entries with no EDGE exerciser | mitigate | §1↔§3 cross-link table at end of §3 lists every INV-01..12 with at least one EDGE-NN exerciser. Plan 05 grep-verifies completeness |
| T-304-SPEC | Threat-enumeration locus | full attack-surface enumeration for the refactor | own | §3 IS the canonical threat enumeration; §4 is the design-intent backward-trace; EDGE-07 V-184 reproduction is the load-bearing artifact for v44.0 closure |
</threat_model>

<verification>
- `grep -c "^### EDGE-" 304-SPEC.md` returns exactly 18
- EDGE-07 cites V-184 + RNGLOCK-FIXREC §103 + HANDOFF-11[1-7]
- §1↔§3 cross-link table present at end of §3
- No "what changed" / "what it used to be" prose in §3 (pre-refactor narration reserved for §4)
</verification>

<success_criteria>
- §3 of 304-SPEC.md fully populated with EDGE-01..18 narrative entries
- Each EDGE-NN has Scenario + Positive + Negative + Tests INV-NN + Depends on SPEC-NN + Foundry function name
- EDGE-07 V-184 reproduction is the headline negative test; mechanic cross-references RNGLOCK-FIXREC §103
- Every INV-01..12 has at least one EDGE-NN exerciser (cross-link table at end of §3)
- §3 entries are mechanizable into Foundry fuzz functions without further interpretation
</success_criteria>

<output>
Create `.planning/phases/304-spec-invariant-model-spec/304-03-SUMMARY.md` when done. SUMMARY captures: (1) the §1↔§3 cross-link table (so Plan 05 can re-verify completeness); (2) the EDGE-NN entries that triggered the deepest design-intent expansion (Plan 04 may need to revisit those deletions); (3) the exact line range §3 occupies (for Plan 05 citation manifest).
</output>
