---
phase: 304-spec-invariant-model-spec
plan: 04
type: execute
wave: 4
depends_on:
  - 304-01
  - 304-02
  - 304-03
files_modified:
  - .planning/phases/304-spec-invariant-model-spec/304-SPEC.md
requirements: []
autonomous: true

must_haves:
  truths:
    - "§4 contains exactly 7 design-intent subsections, one per deletion enumerated in §2's cross-cutting note (5 storage slots + UnresolvedClaim revert + redemptionPeriodIndex reset block)"
    - "Every §4 subsection has 4 labeled fields: ORIGINAL DESIGN INTENT + ACTOR GAME-THEORY WALK (across timing × state combos) + POST-REFACTOR REPLACEMENT (which SPEC-NN lock subsumes it) + DELETION SAFETY ATTESTATION (proof that no game-theoretic scenario re-introduces the deleted behavior)"
    - "The 5 storage-slot deletions each have an actor game-theory walk that traces the V-184 mechanic (RNGLOCK-FIXREC §103) and other attacker-controlled timing combos"
    - "The UnresolvedClaim revert deletion has its own design-intent walk explaining why composite keying makes the revert structurally unreachable"
    - "The redemptionPeriodIndex reset block deletion has its own walk explaining that per-day keying eliminates the cross-day index drift the reset was guarding against"
    - "§4 honors feedback_design_intent_before_deletion.md — design intent + actor game-theory across timing/state combos BEFORE the deletion is locked"
  artifacts:
    - path: ".planning/phases/304-spec-invariant-model-spec/304-SPEC.md"
      provides: "§4 design-intent backward-trace + actor game-theory walk for all 7 deletions"
      contains: "Deletion 1:, Deletion 2:, Deletion 3:, Deletion 4:, Deletion 5:, Deletion 6:, Deletion 7:"
  key_links:
    - from: "§4 deletion subsections"
      to: "§2 SPEC-NN locks that subsume each deletion"
      via: "POST-REFACTOR REPLACEMENT field cross-reference"
      pattern: "subsumed by SPEC-0[1-5]"
    - from: "§4 Deletion 1 (redemptionPeriodIndex slot) + Deletion 7 (reset block)"
      to: ".planning/RNGLOCK-FIXREC.md §103 V-184 mechanic"
      via: "actor game-theory walk traces the cross-day re-roll attack"
      pattern: "V-184|cross-day re-roll|same-day post-resolve"
---

<objective>
Fill `## §4 — Design-Intent Backward-Trace + Actor Game-Theory Walk` with 7 design-intent subsections, one per deletion. Each subsection captures (a) the ORIGINAL DESIGN INTENT of the deleted artifact at v43.0 baseline, (b) an ACTOR GAME-THEORY WALK across all (timing × state) combinations that the original artifact was either guarding or enabling, (c) the POST-REFACTOR REPLACEMENT (which SPEC-NN lock now covers the same behavior or makes the artifact structurally unnecessary), (d) the DELETION SAFETY ATTESTATION (proof that no game-theoretic scenario re-introduces the deleted behavior).

Purpose: Per `feedback_design_intent_before_deletion.md`, every deletion must trace original design intent + actor game-theory across timing/state combos BEFORE the deletion is locked. §4 is the audit-trail proof that each of the 7 v44.0 deletions has been reasoned through — not just removed because the new code path doesn't need it.

Output: §4 of 304-SPEC.md fully populated with 7 deletion subsections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/304-spec-invariant-model-spec/304-SPEC.md
@.planning/phases/304-spec-invariant-model-spec/304-02-SUMMARY.md
@.planning/phases/304-spec-invariant-model-spec/304-03-SUMMARY.md
@/home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_design_intent_before_deletion.md
@/home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_frozen_contracts_no_future_proofing.md
@/home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md
@contracts/StakedDegenerusStonk.sol
</context>

<interfaces>
<!-- The 7 deletions (enumerated in §2 cross-cutting note from Plan 02): -->
<!-- 1. redemptionPeriodIndex storage slot (StakedDegenerusStonk.sol:230) -->
<!-- 2. redemptionPeriodSupplySnapshot storage slot (:229) -->
<!-- 3. redemptionPeriodBurned storage slot (:231) -->
<!-- 4. pendingRedemptionEthBase storage slot (:226) -->
<!-- 5. pendingRedemptionBurnieBase storage slot (:227) -->
<!-- 6. UnresolvedClaim revert (:796-797 — the `if (claim.periodIndex != 0 && claim.periodIndex != currentPeriod) revert UnresolvedClaim();`) -->
<!-- 7. redemptionPeriodIndex reset block (:757-762 — the `if (redemptionPeriodIndex != currentPeriod) { ... } ` block that lazy-initializes the per-period state on first burn of a new day) -->

<!-- RNGLOCK-FIXREC §103 V-184 mechanic — load-bearing for Deletions 1, 4, 5, 7 game-theory walks: -->
<!-- Attack: same-day post-resolve re-burn → STALE redemptionPeriodIndex stays at the just-resolved day -->
<!-- → next-day advance reads stale index → resolveRedemptionPeriod overwrites redemptionPeriods[stale-index].roll -->
<!-- → cross-day re-roll exploit (~19% positive EV per round) -->

<!-- The two pre-refactor uses of redemptionPeriodIndex (cited in design-intent walks): -->
<!-- (a) :588 `uint32 period = redemptionPeriodIndex;` — resolveRedemptionPeriod reads it as the resolve target -->
<!-- (b) :758 `if (redemptionPeriodIndex != currentPeriod)` — _submitGamblingClaimFrom checks day boundary -->
<!-- (c) :760 `redemptionPeriodIndex = currentPeriod;` — _submitGamblingClaimFrom advances the index -->
<!-- POST-REFACTOR: the slot is gone; pendingByDay[uint32] mapping uses the day directly as a key -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fill §4 deletion design-intent walks (Deletions 1-3: redemption period storage slots)</name>
  <files>.planning/phases/304-spec-invariant-model-spec/304-SPEC.md</files>
  <read_first>
    - `/home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_design_intent_before_deletion.md` (the rule being honored — design intent + game-theory BEFORE deletion)
    - `/home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_frozen_contracts_no_future_proofing.md` (pre-launch posture — do NOT defend deletions on future-extensibility grounds)
    - `/home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md` (§4 is the EXCEPTION zone where pre-refactor narration is allowed, under explicit ORIGINAL DESIGN INTENT label; do not leak this prose into §1/§2/§3/§5)
    - `.planning/RNGLOCK-FIXREC.md` lines 5410-5520 (§103 V-184 mechanic — Deletions 1 + 7 game-theory walks cite this)
    - `contracts/StakedDegenerusStonk.sol` lines 226-231 (the 5 storage slots being deleted — read the surrounding comments to extract original author intent), 585-610 (resolveRedemptionPeriod — the consumer of redemptionPeriodIndex at `:588`), 752-814 (_submitGamblingClaimFrom — the writer of redemptionPeriodIndex at `:760` and the reset block at `:757-762`)
  </read_first>
  <action>
    Under `## §4 — Design-Intent Backward-Trace + Actor Game-Theory Walk`, remove the placeholder and write the introductory paragraph + the FIRST THREE deletion subsections (Deletions 1-3). The remaining 4 deletions are filled in Task 2.

    Introductory paragraph (3-5 sentences): cite `feedback_design_intent_before_deletion.md`; state that §4 captures (for each of the 7 deletions): ORIGINAL DESIGN INTENT at v43.0 baseline, ACTOR GAME-THEORY WALK across (timing × state) combinations, POST-REFACTOR REPLACEMENT, and DELETION SAFETY ATTESTATION. Note that §4 is the ONLY section where pre-refactor prose appears (the rest of the SPEC describes the POST-REFACTOR state per `feedback_no_history_in_comments.md`).

    For each of the 3 deletion subsections (Deletions 1, 2, 3) use this exact structure:

    `### Deletion N: <slot name or artifact name> (file:line at v43.0 baseline)`

    `**ORIGINAL DESIGN INTENT (v43.0 baseline):** <2-5 sentences explaining why the original author introduced this slot/artifact; what behavior it was supporting; what assumption it embedded>`

    `**ACTOR GAME-THEORY WALK:**`

    Enumerate the relevant (timing × state) combinations the original artifact was guarding or enabling. For each combination, name:
    - The actor (player burning, advance-stack caller, claim-time claimant, gameOver latcher, admin path, etc.)
    - The timing (pre-advance vs post-advance same-day; post-resolve same-day; post-day-boundary new day; gameOver-mid-pending; etc.)
    - The state (pendingRedemptionEthBase non-zero or zero; pendingRedemptions[player] populated or empty; redemptionPeriods[D] resolved or unresolved; etc.)
    - The attacker incentive (what game-theoretic gain the attacker had under that combination, if any)
    - For Deletion 1 (redemptionPeriodIndex slot) AND Deletion 7 (reset block), the walk MUST trace the V-184 mechanic verbatim: same-day post-resolve re-burn → stale index → next-day advance overwrites redemptionPeriods[stale-index].roll → cross-day re-roll exploit ~19% EV per round.

    `**POST-REFACTOR REPLACEMENT:** <name the SPEC-NN lock that subsumes the deleted artifact; explain how the per-day keying eliminates the need for it; e.g. "Subsumed by SPEC-01 (DayPending struct shape) — per-day pool keying via `pendingByDay[uint32]` mapping eliminates the single-pool index indirection that the V-184 exploit was abusing">`

    `**DELETION SAFETY ATTESTATION:** <prove that no game-theoretic scenario re-introduces the deleted behavior; cite the SPEC-NN locks + INV-NN invariants + EDGE-NN scenarios that bound the post-refactor surface; e.g. "INV-01 (write-once roll) + EDGE-07 (V-184 negative test) jointly attest that no actor under any (timing × state) combination can re-arm an already-resolved period">`

    Per-deletion content guidance for Task 1 (Deletions 1-3):

    **Deletion 1: `redemptionPeriodIndex` storage slot (StakedDegenerusStonk.sol:230, `uint32 internal redemptionPeriodIndex`).**
    - ORIGINAL DESIGN INTENT: At v43.0 baseline the slot identifies the "current redemption period" — used by `_submitGamblingClaimFrom` as both (a) the period key into `redemptionPeriods[period]` for already-resolved rolls and (b) the storage key into the player's `pendingRedemptions[player].periodIndex` (verbatim from RNGLOCK-FIXREC §103.A line 5414). The original author advanced this slot at burn time ONLY on day-boundary transition (the `:757-762` block) and read it in `resolveRedemptionPeriod` as the resolve target (`:588`).
    - ACTOR GAME-THEORY WALK: trace V-184 explicitly. Setup: Player A burns day D; day-D+1 advance writes `redemptionPeriods[D] = {roll: R_{D+1}}`; redemptionPeriodIndex remains at D (advance does NOT update it per RNGLOCK-FIXREC §103.A line 5455). Decision point: attacker reads `redemptionPeriods[D].roll`; if `roll < 100`, attacker burns 1 wei same-day to re-arm `pendingRedemptionEthBase`. Re-roll fire: day-D+2 advance reads `redemptionPeriodIndex = D` (stale) → `resolveRedemptionPeriod` writes `redemptionPeriods[D] = {roll: R_{D+2}}` → OVERWRITE. EV: ~19% per round (RNGLOCK-FIXREC §103.B line 5521). Collateral damage: any other player C with `claim_C.periodIndex = D` is re-rolled WITHOUT consent (RNGLOCK-FIXREC §103.A line 5478).
    - POST-REFACTOR REPLACEMENT: Subsumed by SPEC-01 (DayPending struct shape) — `pendingByDay[uint32]` mapping uses the day directly as a key. There is no "current period" indirection slot to go stale. `resolveRedemptionPeriod(uint16 roll, uint32 flipDay, uint32 dayToResolve)` takes the day explicitly per SPEC-03.
    - DELETION SAFETY ATTESTATION: INV-01 (write-once roll) + INV-06 (no cross-player roll manipulation) + EDGE-07 (V-184 attack reproduction, negative assertion `redemptionPeriods[D].roll byte-identical to first-write value`) jointly attest that the V-184 mechanic is structurally impossible post-refactor.

    **Deletion 2: `redemptionPeriodSupplySnapshot` storage slot (StakedDegenerusStonk.sol:229, `uint256 internal redemptionPeriodSupplySnapshot`).**
    - ORIGINAL DESIGN INTENT: Snapshots `totalSupply` on the first burn of each day so the 50% supply cap at `:763` evaluates against a frozen baseline, NOT against current `totalSupply` (which decreases as burns proceed). Without this snapshot, the cap would automatically tighten as early-day burners reduce supply, creating disadvantage for late-same-day legitimate burners.
    - ACTOR GAME-THEORY WALK: (i) honest day-D burner: first burn of day D initializes snapshot to `totalSupply`; subsequent same-day burns check `redemptionPeriodBurned + amount > snapshot / 2`. (ii) actor that DOESN'T re-init snapshot: if the snapshot were not lazy-initialized, an attacker could front-run a large day-D first burn to set a high snapshot and inflate the cap for subsequent burns. Mitigation in original design: the `:758-762` block is unconditional on first-burn-of-day, so the snapshot is taken at the moment the day boundary is crossed regardless of which actor causes it. (iii) V-184 interaction: under the V-184 attack, the snapshot stays frozen at day D's value even through same-day post-resolve re-burns — the cap continues to bind based on day-D's totalSupply baseline. This was a defense against runaway intra-period volume (RNGLOCK-FIXREC §103.B line 5511) but did NOT prevent the re-roll EV asymmetry.
    - POST-REFACTOR REPLACEMENT: Subsumed by SPEC-01 (`pendingByDay[D].supplySnapshot` field) + SPEC-05 (lazy-init timing — first burn of day D when `pendingByDay[D].supplySnapshot == 0`). Same semantics; new home.
    - DELETION SAFETY ATTESTATION: INV-10 (per-day supply cap) + EDGE-14 (50% supply cap edge — burn exactly cap succeeds, one wei over reverts) attest that the cap behavior is byte-equivalent under per-day keying.

    **Deletion 3: `redemptionPeriodBurned` storage slot (StakedDegenerusStonk.sol:231, `uint256 internal redemptionPeriodBurned`).**
    - ORIGINAL DESIGN INTENT: Tracks cumulative sDGNRS burned within the current day's redemption period. Used at `:763` (`redemptionPeriodBurned + amount > redemptionPeriodSupplySnapshot / 2`) to enforce the 50% supply cap. Reset to 0 in the `:757-762` block on first burn of a new day.
    - ACTOR GAME-THEORY WALK: (i) honest sequence of same-day burners: each accumulates `redemptionPeriodBurned`; when the cumulative exceeds 50% of snapshot, further burns revert `Insufficient`. (ii) attacker trying to bypass cap: cannot reset `redemptionPeriodBurned` mid-day (the `:758` conditional only fires when `currentDayView()` advances). (iii) cross-day reset: under the V-184 exploit, the attacker's 1-wei same-day re-burns increment `redemptionPeriodBurned` by negligible amounts — the cap binds on VOLUME, not on COUNT of burns, so the cap does NOT prevent the V-184 attack (RNGLOCK-FIXREC §103.B line 5511). (iv) day-D+1 transition: legitimate burns on day D+1 reset `redemptionPeriodBurned` to 0 via the `:760-761` reset block — the cap restarts at day D+1's `redemptionPeriodSupplySnapshot / 2`.
    - POST-REFACTOR REPLACEMENT: Subsumed by SPEC-01 (`pendingByDay[D].burned` field) — each day's burned counter lives in its own per-day mapping entry. No cross-day reset needed; new-day entries default to `burned = 0` via Solidity's default-zero mapping semantics.
    - DELETION SAFETY ATTESTATION: INV-10 (per-day supply cap) + EDGE-14 + EDGE-16 (cross-day cap reset — burn 160 ETH on D + 160 ETH on D+1 both succeed) attest that the cap-counter behavior is byte-equivalent under per-day keying and that the cross-day reset is structural rather than block-conditional.

    Length target: 150-200 lines for Task 1 output (Deletions 1-3 + introductory paragraph).
  </action>
  <verify>
    <automated>bash -c 'grep -q "^### Deletion 1:" .planning/phases/304-spec-invariant-model-spec/304-SPEC.md && grep -q "^### Deletion 2:" .planning/phases/304-spec-invariant-model-spec/304-SPEC.md && grep -q "^### Deletion 3:" .planning/phases/304-spec-invariant-model-spec/304-SPEC.md && echo DELETIONS_1_2_3_PRESENT'</automated>
  </verify>
  <acceptance_criteria>
    - §4 introductory paragraph cites `feedback_design_intent_before_deletion.md`
    - `grep -c "^### Deletion [123]:" 304-SPEC.md` returns 3
    - Each Deletion N subsection contains all four labeled fields: `**ORIGINAL DESIGN INTENT`, `**ACTOR GAME-THEORY WALK:**`, `**POST-REFACTOR REPLACEMENT:**`, `**DELETION SAFETY ATTESTATION:**` (verify: `grep -c "\*\*ORIGINAL DESIGN INTENT" 304-SPEC.md` returns ≥ 3; same for the other three labels)
    - Deletion 1 actor game-theory walk explicitly cites V-184 + the same-day post-resolve re-burn → next-day advance overwrite mechanic + the ~19% EV figure
    - Deletion 1 + Deletion 2 + Deletion 3 each cite their SPEC-NN replacement and at least one INV-NN + at least one EDGE-NN in the DELETION SAFETY ATTESTATION field
  </acceptance_criteria>
  <done>§4 introduction + Deletions 1-3 written. Task 2 fills Deletions 4-7. §5 placeholder untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Fill §4 Deletions 4-7 (eth/burnie base slots + UnresolvedClaim revert + reset block)</name>
  <files>.planning/phases/304-spec-invariant-model-spec/304-SPEC.md</files>
  <read_first>
    - `.planning/phases/304-spec-invariant-model-spec/304-SPEC.md` (current state after Task 1 — §4 introduction + Deletions 1-3 present; Task 2 appends Deletions 4-7 in the same subsection format)
    - `.planning/RNGLOCK-FIXREC.md` lines 5410-5520 (§103 V-184 mechanic — Deletion 4, 5, 7 walks reference)
    - `contracts/StakedDegenerusStonk.sol` lines 226-227 (`pendingRedemptionEthBase`, `pendingRedemptionBurnieBase`), 796-797 (`UnresolvedClaim` revert), 757-762 (reset block)
    - `.planning/phases/304-spec-invariant-model-spec/304-02-SUMMARY.md` (the §2 cross-cutting note that enumerated all 7 deletions; Task 2 honors that order)
    - `/home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_design_intent_before_deletion.md`
    - `/home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_frozen_contracts_no_future_proofing.md`
  </read_first>
  <action>
    Continue under `## §4 — Design-Intent Backward-Trace + Actor Game-Theory Walk`, append Deletions 4-7 using the same subsection structure (4 labeled fields each: ORIGINAL DESIGN INTENT + ACTOR GAME-THEORY WALK + POST-REFACTOR REPLACEMENT + DELETION SAFETY ATTESTATION).

    Per-deletion content guidance for Task 2 (Deletions 4-7):

    **Deletion 4: `pendingRedemptionEthBase` storage slot (StakedDegenerusStonk.sol:226, `uint256 internal pendingRedemptionEthBase`).**
    - ORIGINAL DESIGN INTENT: Holds the segregated ETH base for the CURRENT unresolved period. Cleared on resolve (`:594`); incremented on burn (`:790`). Acts as the per-period "what's at stake on the next roll" register — `resolveRedemptionPeriod` reads it and multiplies by `roll / 100` to compute the rolled outcome.
    - ACTOR GAME-THEORY WALK: (i) honest burner: each same-day burn increments this slot; resolve consumes it. (ii) V-184 attacker: same-day post-resolve re-burn re-arms this slot to non-zero AFTER the day's resolve cleared it; next-day advance sees `pendingRedemptionEthBase != 0` (bypasses the early-return at `:589`) and invokes `resolveRedemptionPeriod` again on the stale `redemptionPeriodIndex`. The slot's single-pool nature is what enables the V-184 cross-day re-roll: there is no day-keyed separation between the just-resolved period and the new pending burns. (iii) honest cross-day burner: day-D+1 burner increments this slot post-D+1 reset block fires; resolve at day-D+2 consumes the day-D+1 contributions; works correctly because at day-D+2 advance time the slot represents day-D+1's pool only (no V-184 attack).
    - POST-REFACTOR REPLACEMENT: Subsumed by SPEC-01 (`pendingByDay[D].ethBase` field) — each day's ETH base lives in its own mapping entry. The cross-day pool conflation that V-184 exploited is structurally impossible.
    - DELETION SAFETY ATTESTATION: INV-04 (per-day base correctness) + INV-08 (pre-advance-gap burn safety) + EDGE-01 + EDGE-02 + EDGE-07 attest that per-day pools never leak into each other.

    **Deletion 5: `pendingRedemptionBurnieBase` storage slot (StakedDegenerusStonk.sol:227, `uint256 internal pendingRedemptionBurnieBase`).**
    - ORIGINAL DESIGN INTENT: BURNIE analog of `pendingRedemptionEthBase` — segregated BURNIE base for the current unresolved period. Cleared on resolve (`:601`); incremented on burn (`:792`). `resolveRedemptionPeriod` reads it and multiplies by `roll / 100` to compute the rolled BURNIE credit (`:597`); paid out on claim via `_payBurnie` if the coinflip resolves favorably.
    - ACTOR GAME-THEORY WALK: (i) honest BURNIE-claimer: burns segregate BURNIE; resolve consumes; claim pays out on coinflip win. (ii) V-184 attacker: same mechanic as Deletion 4 — same-day post-resolve 1-wei re-burn re-arms this slot; next-day advance includes the stale re-arming in its resolve computation; BURNIE side of the exploit (the `R_{D+1}` overwrite scales both ETH and BURNIE rolled amounts). (iii) coinflip-decoupled actor: under the existing semantics at `:651-654`, the BURNIE payout depends on the SEPARATE coinflip-day result — the V-184 ETH-side exploit and the coinflip outcome are statistically independent.
    - POST-REFACTOR REPLACEMENT: Subsumed by SPEC-01 (`pendingByDay[D].burnieBase` field).
    - DELETION SAFETY ATTESTATION: INV-03 (BURNIE conservation) + INV-04 (per-day base correctness) + EDGE-04 + EDGE-18 attest that BURNIE pool semantics are byte-equivalent under per-day keying.

    **Deletion 6: `UnresolvedClaim` revert (StakedDegenerusStonk.sol:796-797, `if (claim.periodIndex != 0 && claim.periodIndex != currentPeriod) revert UnresolvedClaim();`).**
    - ORIGINAL DESIGN INTENT: Under pre-refactor single-key `pendingRedemptions[player]`, a player could only have ONE pending claim at a time. The revert at `:796-797` prevented a player who had an unresolved claim from day D from submitting a NEW claim on day D+1 (since the new claim would overwrite the old one in the single-key mapping). This was a UX guardrail — forcing the player to wait for day-D's advance + claim before submitting day-D+1's burn.
    - ACTOR GAME-THEORY WALK: (i) honest player accumulating across days: hits the revert on day-D+1 if day-D's claim is still unresolved (typically because day-D's advance hasn't fired yet). Friction but no economic harm. (ii) deliberate-stall attacker: if an attacker COULD overwrite their own claim mid-period under the single-key scheme, they could front-run the day-D resolve with a fresh day-D burn to inflate `ethValueOwed`. The revert blocks this. (iii) cross-day stacking under the same-period exception: at `:796` the revert is gated on `claim.periodIndex != 0 && claim.periodIndex != currentPeriod` — so a player CAN add to an existing same-period claim (`claim.periodIndex == currentPeriod`). The revert only fires on cross-period accumulation attempts.
    - POST-REFACTOR REPLACEMENT: Subsumed by SPEC-02 (composite-key `pendingRedemptions[player][day]` mapping). Under composite keying, day-D and day-D+1 claims are in SEPARATE mapping entries — there is no overwrite collision to guard against. The revert becomes structurally unreachable; SPEC-02 removes it. The cross-day stacking that the revert blocked is now safe by construction (each day's claim is independently stored).
    - DELETION SAFETY ATTESTATION: INV-07 (no self-roll manipulation via timing — for any (P, D) the locked `ethValueOwed` is byte-identical from burn to claim) + EDGE-03 (single player burns multiple days, never claims — each claim independently resolvable) + EDGE-16 (cross-day cap reset — burn 160 ETH on D + 160 ETH on D+1 both allowed) attest that cross-day claim accumulation under composite keying does NOT enable the front-run-resolve attack the revert was guarding against, because each day's `pendingRedemptions[player][D]` is sealed at burn time and cannot be retroactively modified once day-D's advance fires.

    **Deletion 7: `redemptionPeriodIndex` reset block (StakedDegenerusStonk.sol:757-762).**
    - The literal deleted block — lazy-initializes per-period state on first burn of a new day. Cite the line range explicitly: line 757 opens `uint32 currentPeriod = game.currentDayView();` then 758 `if (redemptionPeriodIndex != currentPeriod) {`; lines 759-761 reset `redemptionPeriodSupplySnapshot = totalSupply; redemptionPeriodIndex = currentPeriod; redemptionPeriodBurned = 0;` and line 762 closes the brace. Plan 05 grep-verifies this range against source HEAD.
    - ORIGINAL DESIGN INTENT: lazy-initializes the per-day cap state (`redemptionPeriodSupplySnapshot`, `redemptionPeriodBurned`) when the first burn of a new wall-clock day arrives. Also advances `redemptionPeriodIndex` to the new day. Under v43.0 baseline this is the ONLY writer of `redemptionPeriodIndex` (per RNGLOCK-FIXREC §103.A line 5414's "single writer" attestation).
    - ACTOR GAME-THEORY WALK: (i) honest first-of-day burner: trips the reset; new period state initialized. (ii) honest subsequent same-day burner: skips the reset (conditional `redemptionPeriodIndex != currentPeriod` is false); accumulates against the existing snapshot. (iii) V-184 attacker: this is THE structural enabler of the V-184 attack. Because `resolveRedemptionPeriod` does NOT advance `redemptionPeriodIndex` (it only WRITES `redemptionPeriods[period]`), the index lags behind the resolved-period boundary by exactly one day. The same-day post-resolve 1-wei re-burn skips the reset block (because `redemptionPeriodIndex == currentPeriod` is still true post-resolve same-day), re-arming `pendingRedemptionEthBase` without updating any "this period was already resolved" marker. The next-day advance then operates on the stale index. (iv) potential v43-baseline tactic-(c) fix that this refactor REPLACES: RNGLOCK-FIXREC §103.C line 5567 proposed advancing `redemptionPeriodIndex` inside `resolveRedemptionPeriod` itself (`redemptionPeriodIndex = period + 1`). Per-day keying is a stronger structural answer — there is no single index to advance because there is no single-pool indirection in the first place.
    - POST-REFACTOR REPLACEMENT: Subsumed by SPEC-01 (per-day keying eliminates the need for a current-period index slot) + SPEC-05 (lazy-init of `pendingByDay[D].supplySnapshot` when the slot reads zero — this is the per-day analog of the reset block's snapshot init, but keyed by day rather than by index change).
    - DELETION SAFETY ATTESTATION: INV-01 (write-once roll) + INV-10 (per-day supply cap with snapshot timing per SPEC-05) + EDGE-07 (V-184 attack reproduction — NEGATIVE outcome) + EDGE-14 (50% supply cap edge) + EDGE-16 (cross-day cap reset) jointly attest that the lazy-init + cross-day reset semantics are byte-equivalent under per-day keying AND that the V-184 mechanic the reset block was structurally enabling is now structurally impossible.

    Length target: 200-300 lines for Task 2 output (Deletions 4-7). Combined §4 total target: 350-500 lines.

    Closing paragraph for §4 (3-4 sentences): summarize the proof — 7 deletions traced, each with original intent + actor game-theory + post-refactor replacement + safety attestation. Explicit attestation that the V-184 mechanic (the only CATASTROPHE-tier finding in the entire v43.0 catalog) is structurally eliminated by the combination of SPEC-01 (per-day keying) + SPEC-03 (dayToResolve arg) + SPEC-04 (c) (delete at resolve) — no single deletion suffices alone; the elimination requires all three locks together. Reference Phase 305 IMPL as the downstream consumer that materializes the 7 deletions in the actual diff.
  </action>
  <verify>
    <automated>bash -c 'for n in 1 2 3 4 5 6 7; do grep -q "^### Deletion $n:" .planning/phases/304-spec-invariant-model-spec/304-SPEC.md || { echo MISSING Deletion $n; exit 1; }; done; echo ALL_7_DELETIONS_PRESENT'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^### Deletion [1-7]:" 304-SPEC.md` returns exactly 7
    - Each Deletion N (N=1..7) subsection contains all 4 labeled fields: `**ORIGINAL DESIGN INTENT`, `**ACTOR GAME-THEORY WALK:**`, `**POST-REFACTOR REPLACEMENT:**`, `**DELETION SAFETY ATTESTATION:**` (verify: `grep -c "\*\*ORIGINAL DESIGN INTENT" 304-SPEC.md` returns ≥ 7; same for the other three labels)
    - Deletion 4 + Deletion 5 + Deletion 7 each cite the V-184 mechanic in their ACTOR GAME-THEORY WALK
    - Deletion 6 (UnresolvedClaim revert) explicitly states the deletion is structurally safe because composite keying makes the revert unreachable
    - Deletion 7 (reset block) explicitly cites the V-184 mechanic as the structural-enabler attack and notes that per-day keying is a STRONGER fix than the RNGLOCK-FIXREC §103.C tactic-(c) `redemptionPeriodIndex = period + 1` proposal
    - §4 closing paragraph explicitly attests V-184 is structurally eliminated by SPEC-01 + SPEC-03 + SPEC-04 (c) JOINTLY (no single lock suffices alone)
    - The placeholder `_To be filled by Plan 04_` under §4 is removed
  </acceptance_criteria>
  <done>§4 fully populated with 7 deletion subsections, each tracing design intent + actor game-theory + post-refactor replacement + safety attestation. §5 placeholder remains for Plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| §4 design-intent walks → Phase 305 IMPL author | The walks justify each deletion; if a walk is incomplete, the IMPL author may unwittingly re-introduce a deleted artifact under a different name |
| §4 V-184 actor game-theory walk → RNGLOCK-FIXREC §103 verbatim mechanic | The walk must reproduce the §103 mechanic; divergence would suggest a different attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-304-04-01 | Tampering | Deletion N walk omits a (timing × state) combination → false safety attestation | mitigate | Task action enumerates the canonical actor categories (honest burner, V-184 attacker, cross-day burner, coinflip-decoupled actor); acceptance criteria require V-184 walk for Deletions 1/4/5/7 |
| T-304-04-02 | Information disclosure | Pre-refactor narration leaks into §1/§2/§3/§5 violating feedback_no_history_in_comments.md | mitigate | Task 1 introductory paragraph explicitly states §4 is the EXCEPTION zone; remaining sections describe POST-REFACTOR state only. Plan 05 grep-verifies (cross-section "what changed" leakage check) |
| T-304-04-03 | Tampering | Deletion 7 walk overstates safety — claims V-184 elimination from a single lock | mitigate | §4 closing paragraph explicitly requires attestation that V-184 elimination requires SPEC-01 + SPEC-03 + SPEC-04 (c) JOINTLY |
| T-304-SPEC | Threat-enumeration locus | full attack-surface enumeration | own (jointly with §3) | §3 carries the EDGE-NN positive/negative threats; §4 carries the deletion-justification design walks. EDGE-07 (in §3) + Deletion 1 + Deletion 7 (in §4) are the canonical V-184 closure artifacts |
</threat_model>

<verification>
- `grep -c "^### Deletion [1-7]:" 304-SPEC.md` returns exactly 7
- V-184 mechanic cited in Deletions 1 + 4 + 5 + 7 ACTOR GAME-THEORY WALK fields
- §4 closing paragraph attests joint elimination via SPEC-01 + SPEC-03 + SPEC-04 (c)
</verification>

<success_criteria>
- §4 of 304-SPEC.md fully populated with 7 deletion subsections
- Each deletion has ORIGINAL DESIGN INTENT + ACTOR GAME-THEORY WALK + POST-REFACTOR REPLACEMENT + DELETION SAFETY ATTESTATION
- §4 honors `feedback_design_intent_before_deletion.md` (design intent + game-theory BEFORE deletion is locked)
- §4 is the ONLY section with pre-refactor narration (per `feedback_no_history_in_comments.md`)
- V-184 structural elimination attested as joint result of SPEC-01 + SPEC-03 + SPEC-04 (c)
</success_criteria>

<output>
Create `.planning/phases/304-spec-invariant-model-spec/304-04-SUMMARY.md` when done. SUMMARY captures: (1) the 7 deletion → SPEC-NN replacement mapping (Plan 05 cross-checks); (2) any file:line citation made in §4 that needs verification at Plan 05; (3) the exact line range §4 occupies (for Plan 05 citation manifest).
</output>
