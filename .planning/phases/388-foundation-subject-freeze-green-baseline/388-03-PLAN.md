---
phase: 388-foundation-subject-freeze-green-baseline
plan: 03
type: execute
wave: 2
depends_on: [388-01, 388-02]
files_modified:
  - test/REGRESSION-BASELINE-v63.md
  - .planning/phases/388-foundation-subject-freeze-green-baseline/388-03-BASELINE-DIFF.md
autonomous: true
requirements: [FND-01, FND-03]

must_haves:
  truths:
    - "The audit subject is byte-frozen at a8b702a7 — the contracts/ fingerprint is recorded and git diff a8b702a7 -- contracts/ is empty"
    - "The baseline diff (delta vs the last-audited v62 subject 77580320) is recorded — the surface this milestone audits"
    - "A full forge run at a8b702a7 shows 0 deterministic failures and is recorded as the GREEN baseline, superseding the carried-red ledger; the Hardhat deterministic subset is run or its limitation documented"
  artifacts:
    - path: "test/REGRESSION-BASELINE-v63.md"
      provides: "The GREEN full-suite baseline record for subject a8b702a7 (the audit oracle the sweeps reproduce findings against)"
      min_lines: 50
    - path: ".planning/phases/388-foundation-subject-freeze-green-baseline/388-03-BASELINE-DIFF.md"
      provides: "The recorded git diff 77580320 a8b702a7 -- contracts/ surface (the audit delta) + the byte-freeze fingerprint pin"
      min_lines: 20
  key_links:
    - from: "REGRESSION-BASELINE-v63.md green baseline"
      to: "subject a8b702a7 contracts/ fingerprint"
      via: "recorded fingerprint + green forge counts"
      pattern: "a8b702a7"
---

<objective>
Record the byte-freeze + baseline diff (FND-01) and establish + record the GREEN forge + JS
regression baseline (FND-03) at subject `a8b702a7` — the audit's safety floor and the oracle every
sweep (389-394) reproduces its leads against. This is the FINAL serial gate of the foundation: it
depends on Plan 01 (the slot-poke harnesses reconciled against the authoritative layout, so a green
run is trustworthy, not green-because-a-stale-slot-masks-a-bug) and Plan 02 (the oracle-hole audit, so
green means invariants ran, not vacuous passes).

The forge suite already runs green at the subject (853 passed / 0 failed / 110 skipped on the
freeze-validation run — even the VRFPath invariants that v62 carried red now pass), so FND-03 is
RE-RUN-on-a-clean-fixture-and-RECORD, not a repair. The supersession of any carried-red ledger is the
whole point: the signal becomes "0 deterministic failures against this green baseline," not "a large
red count certified by a by-name non-widening diff."

Purpose: without a recorded byte-freeze pin + a recorded green baseline, the sweeps have no oracle and
no proof the subject didn't drift. With them, every later phase asserts `git diff a8b702a7 --
contracts/` empty and reproduces against the recorded green counts.

Output: 388-03-BASELINE-DIFF.md (the freeze fingerprint + the 77580320->a8b702a7 audit-delta surface)
+ test/REGRESSION-BASELINE-v63.md (the GREEN forge baseline + the Hardhat disposition, superseding the
carried-red ledger).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/AUDIT-V63-PLAN.md

# The prior milestone's green baseline — the template + the documented Hardhat limitation to carry:
#   section 0 the "0 failures supersedes by-name non-widening" discipline; section 1 the GREEN forge
#   counts format; section 5 the Hardhat limitation (npm test globs an absent test/adversarial/*.test.js
#   -> MODULE_NOT_FOUND before any spec loads; run the explicit test/unit + test/edge subset instead;
#   test:stat is probabilistic, excluded; the 117 JS fails are documented carried gameover-VRF-drive
#   harness drift, corroborating only — forge is the declared PRIMARY oracle).
@test/REGRESSION-BASELINE-v62.md

# Plan 01 + Plan 02 outputs — the recalibration + oracle-hole confirmations this green baseline rests on.
@.planning/phases/388-foundation-subject-freeze-green-baseline/388-01-LAYOUT-KEY.md
@.planning/phases/388-foundation-subject-freeze-green-baseline/388-02-ORACLE-HOLES.md

# foundry.toml: default profile [fuzz] runs=1000 seed=0xdeadbeef (deterministic); [invariant]
# runs=256 depth=128 fail_on_revert=false; via_ir=true.
</context>

<landmine>
⚠ The Hardhat suite compiles on run and `hardhat compile` REGENERATES `contracts/ContractAddresses.sol`,
breaking the forge fixture. Sequence so the forge baseline is captured on a CLEAN fixture: run forge
FIRST, then any hardhat. After ANY hardhat invocation, restore the fixture
(`git checkout HEAD -- contracts/ContractAddresses.sol`) and re-verify `git status --porcelain
contracts/` empty AND `forge build` still clean BEFORE recording/finishing. The subject is byte-frozen:
`git diff a8b702a7 -- contracts/` must be empty at completion. Comparison is always BY NAME, never raw
count.
</landmine>

<tasks>

<task type="auto">
  <name>Task 1: Record the byte-freeze pin + the 77580320 to a8b702a7 audit-delta surface (FND-01)</name>
  <files>.planning/phases/388-foundation-subject-freeze-green-baseline/388-03-BASELINE-DIFF.md</files>
  <read_first>
    - .planning/AUDIT-V63-PLAN.md section 1 (subject and baseline — the audit delta is `git diff 77580320 a8b702a7 -- contracts/`; the subject must stay byte-identical through every sweep)
    - test/REGRESSION-BASELINE-v62.md section 1 header (the fingerprint-recording format: git tree-hash + deterministic content sha256)
  </read_first>
  <action>
    Record the byte-freeze pin for subject a8b702a7: capture `git rev-parse a8b702a7:contracts` (the
    content-addressed tree hash) and a deterministic content sha256
    (`find contracts -name '*.sol' | sort | xargs sha256sum | sha256sum`), and assert
    `git diff a8b702a7 -- contracts/` is empty (the working-tree contracts are byte-identical to the
    frozen subject — note HEAD may be a later docs-only commit; what matters is the contracts/ tree
    equals a8b702a7:contracts, confirm `git rev-parse HEAD:contracts == git rev-parse a8b702a7:contracts`).
    Then record the AUDIT DELTA: `git diff --stat 77580320 a8b702a7 -- contracts/` (the file list + line
    counts — the surface this milestone audits) and a one-line characterization per changed file family
    (storage packing · BURNIE emission rework · gas-identity refactors · new permissionless/keeper
    entrypoints · reward rebalances · redemption rework) so the sweep planners see their slice. Write
    388-03-BASELINE-DIFF.md with: the freeze fingerprint pin, the empty-diff assertion, and the
    audit-delta stat + per-family characterization. Note the untracked PLAYER-PURCHASE-REWARDS.html is
    OUT of scope (player-facing doc, not a contract). Do NOT run hardhat. Keep contracts/ byte-frozen.
  </action>
  <verify>
    <automated>test -f .planning/phases/388-foundation-subject-freeze-green-baseline/388-03-BASELINE-DIFF.md &amp;&amp; grep -q "a8b702a7" .planning/phases/388-foundation-subject-freeze-green-baseline/388-03-BASELINE-DIFF.md &amp;&amp; grep -q "77580320" .planning/phases/388-foundation-subject-freeze-green-baseline/388-03-BASELINE-DIFF.md &amp;&amp; grep -qiE "sha256|tree.hash|fingerprint" .planning/phases/388-foundation-subject-freeze-green-baseline/388-03-BASELINE-DIFF.md &amp;&amp; [ -z "$(git diff a8b702a7 -- contracts/)" ] &amp;&amp; echo FROZEN_OK</automated>
  </verify>
  <acceptance_criteria>
    - 388-03-BASELINE-DIFF.md exists and records the a8b702a7 contracts/ byte-freeze fingerprint (git
      tree-hash + deterministic content sha256) and asserts `git diff a8b702a7 -- contracts/` empty
      (HEAD:contracts == a8b702a7:contracts).
    - It records `git diff --stat 77580320 a8b702a7 -- contracts/` (the audit-delta surface) with a
      one-line per-family characterization (packing / BURNIE / gas-identity / permissionless / rewards
      / redemption).
    - `git diff a8b702a7 -- contracts/` is empty.
  </acceptance_criteria>
  <done>The subject byte-freeze pin + the 77580320->a8b702a7 audit-delta surface are recorded; contracts/ byte-frozen.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run forge clean + Hardhat subset, record the GREEN v63 baseline superseding the carried-red ledger (FND-03)</name>
  <files>test/REGRESSION-BASELINE-v63.md</files>
  <read_first>
    - test/REGRESSION-BASELINE-v62.md section 0 (the "0 failures supersedes by-name non-widening" discipline to carry forward), section 1 (the GREEN forge counts table format), section 5 (the Hardhat limitation: the absent test/adversarial glob -> MODULE_NOT_FOUND; run the explicit test/unit + test/edge subset; test:stat probabilistic excluded; the 117 JS fails = carried gameover-VRF-drive harness drift, corroborating only; forge is PRIMARY)
    - Task 1's 388-03-BASELINE-DIFF.md (the freeze fingerprint to cite) + the 388-01 / 388-02 SUMMARYs (the recalibration + oracle-hole confirmations the green baseline rests on)
  </read_first>
  <action>
    Run `forge clean && forge build` (must be clean), then the FULL forge suite: `forge test` (default
    deterministic profile). Collect the passed/failed/skipped counts and any failing NAMES (dedupe
    across fuzz seeds). The expected result is 0 deterministic failures (the freeze-validation run
    showed 853/0/110 — confirm it reproduces, including the VRFPath invariants now passing). If ANY
    name fails: if it is a deterministic test* red caused by a slot/semantics miss, it is a Plan-01
    reconciliation gap — fix it in the owning test file using the authoritative 388-01-LAYOUT-KEY slot
    (same technique), do NOT touch contracts. If a residual non-deterministic invariant_* red appears,
    confirm it reproduces at the a8b702a7 subject and record it as a documented carried bucket-A
    exception (named). Then run the deterministic Hardhat subset explicitly (avoid the broken npm test
    adversarial glob): `npx hardhat test test/unit/*.test.js test/edge/*.test.js` — capture
    passing/failing/pending + the failing names; characterize the failures against the v62 section 5
    documented carried gameover-VRF-drive harness-drift families (confirm none is an
    accounting-insolvency or RNG-freeze breach — those would be findings, not carried drift), or, if
    the Hardhat environment cannot run the subset, record that limitation explicitly and rely on the
    forge GREEN as the declared PRIMARY oracle. After ANY hardhat run restore the fixture
    (`git checkout HEAD -- contracts/ContractAddresses.sol`) and re-verify `git status --porcelain
    contracts/` empty + `forge build` clean. WRITE test/REGRESSION-BASELINE-v63.md: subject a8b702a7 +
    the Task-1 contracts/ fingerprint, the GREEN forge counts table (passed / 0-deterministic-failed /
    skipped) with any carried bucket-A exceptions named, the Hardhat subset result (counts + carried-
    drift characterization, or the documented limitation), an explicit statement that this SUPERSEDES
    the carried-red ledger (forge is PRIMARY, Hardhat corroborating; future regressions caught by "0
    failures" against this baseline, BY NAME not by count), and a note that the green baseline rests on
    the Plan-01 slot reconciliation + the Plan-02 oracle-hole audit. Comparison BY NAME.
  </action>
  <verify>
    <automated>FOUNDRY_DISABLE_NIGHTLY_WARNING=1 forge clean 1>/dev/null 2>&amp;1; FOUNDRY_DISABLE_NIGHTLY_WARNING=1 forge build 1>/dev/null 2>&amp;1; FOUNDRY_DISABLE_NIGHTLY_WARNING=1 forge test 2>&amp;1 | grep -E "tests passed|0 failed|Suite result: FAILED" | tail -10; git checkout HEAD -- contracts/ContractAddresses.sol 2>/dev/null; test -f test/REGRESSION-BASELINE-v63.md &amp;&amp; grep -q "a8b702a7" test/REGRESSION-BASELINE-v63.md &amp;&amp; grep -qiE "supersede" test/REGRESSION-BASELINE-v63.md &amp;&amp; grep -qiE "green|0 (deterministic )?fail" test/REGRESSION-BASELINE-v63.md &amp;&amp; [ -z "$(git status --porcelain contracts/)" ] &amp;&amp; echo BASELINE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `forge test` on a clean fixture at a8b702a7 shows 0 failing deterministic test* names (only any
      documented carried bucket-A invariant_* exceptions confirmed to reproduce at the subject, each
      named in the doc).
    - test/REGRESSION-BASELINE-v63.md exists, names subject a8b702a7 + the contracts/ fingerprint,
      records the GREEN forge counts table, the Hardhat subset result (or its documented limitation),
      explicitly states it SUPERSEDES the carried-red ledger (forge PRIMARY; "0 failures" by name is
      the signal), and notes the dependence on the Plan-01 reconciliation + Plan-02 oracle-hole audit.
    - `git status --porcelain contracts/` empty (ContractAddresses.sol restored after hardhat);
      `forge build` clean.
  </acceptance_criteria>
  <done>A GREEN forge baseline (0 deterministic failures) + the Hardhat disposition are recorded in REGRESSION-BASELINE-v63.md superseding the carried-red ledger; contracts/ byte-frozen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recorded green baseline ← clean fixture | a baseline captured on a hardhat-regenerated ContractAddresses.sol pins the WRONG subject |
| subject drift across sweeps ← byte-freeze pin | without a recorded fingerprint, a later silent contract edit is undetectable |
| false-green ← Plan-01/02 dependence | a green run on stale-slot harnesses (Plan 01 gap) or vacuous invariants (Plan 02 gap) hides real findings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-388-03-01 | Tampering | contracts/ContractAddresses.sol regen via hardhat | mitigate | forge run FIRST on clean fixture; restore-and-reverify after any hardhat; baseline recorded only after `git status --porcelain contracts/` empty + `forge build` clean |
| T-388-03-02 | Repudiation (undetectable subject drift) | the byte-freeze pin | mitigate | record git tree-hash + deterministic content sha256; assert HEAD:contracts == a8b702a7:contracts; sweeps assert `git diff a8b702a7 -- contracts/` empty |
| T-388-03-03 | Information Disclosure (false-green) | the recorded baseline | mitigate | depends_on Plan 01 (slot reconciliation) + Plan 02 (oracle-hole audit); the doc records that dependence; comparison BY NAME not raw count |

No new contract behavior ships; threat model narrow by design (audit-only baseline record).
</threat_model>

<verification>
- 388-03-BASELINE-DIFF.md records the a8b702a7 byte-freeze fingerprint + the 77580320->a8b702a7
  audit-delta surface; `git diff a8b702a7 -- contracts/` empty.
- `forge test` at a8b702a7 on a clean fixture shows 0 deterministic failures (carried bucket-A
  exceptions, if any, named + confirmed pre-existing).
- test/REGRESSION-BASELINE-v63.md records the GREEN forge baseline + Hardhat disposition, supersedes
  the carried-red ledger, and notes the Plan-01/02 dependence.
- `git status --porcelain contracts/` empty; `forge build` clean.
</verification>

<success_criteria>
The subject byte-freeze pin + the 77580320->a8b702a7 audit-delta surface are recorded; a GREEN forge
baseline (0 deterministic failures) + the Hardhat deterministic disposition are established and
recorded in test/REGRESSION-BASELINE-v63.md superseding the carried-red ledger so future regressions
are caught by "0 failures" BY NAME; the green baseline rests on the Plan-01 slot reconciliation + the
Plan-02 oracle-hole audit; contracts/ is byte-frozen throughout.
</success_criteria>

<output>
Create `.planning/phases/388-foundation-subject-freeze-green-baseline/388-03-SUMMARY.md` when done.
</output>
