---
phase: 232.1-rng-index-ticket-drain-ordering-enforcement
plan: 232.1-03
type: execute
wave: 3
depends_on: [232.1-01, 232.1-02]
files_modified:
  - .planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-SIM-REPLAY.md
  - .planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-PFTB-AUDIT.md
autonomous: true
requirements: [SPEC-R6, SPEC-R-PFTB-AUDIT-ONLY]
must_haves:
  truths:
    - "Sim re-run executed against post-fix HEAD reproducing the SPEC §Background 25L/100P turbo scenario (or the closest available equivalent if the literal harness is not in this repo)"
    - "Post-fix sim shows ZERO `_raritySymbolBatch` events with `entropyWord == 0` (AC-4)"
    - "Post-fix sim shows ZERO zero-hit trait IDs at L5 trait distribution (AC-5)"
    - "Per-quadrant cat-0 and cat-7 percentages at L5 are within ±1 percentage point of design (cat-0 = 13.3%, cat-7 = 10.7%) per AC-5"
    - "Sim infrastructure source location identified (sibling repo or local), and the choice between (a) running the existing turbo sim, (b) producing an equivalent forge-test sim, or (c) recording-the-evidence-of-non-availability is made explicitly and documented"
    - "232.1-03-SIM-REPLAY.md documents the sim source location, the harness used, the post-fix run output, the SQL/aggregation script for trait distribution, the AC-4 and AC-5 verdict, and any caveats from substituted infrastructure"
    - "`processFutureTicketBatch` audit trace executed: every reachable caller path (`_processFutureTicketBatch` at AdvanceModule:1309-1325, called from L300, L344, L354, L392) traced to confirm the `entropy` argument is non-zero at the call site (AC-7)"
    - "232.1-03-PFTB-AUDIT.md exists in v29.0 audit format (SHA + File:Line + Verdict per caller path) confirming AC-7"
    - "ZERO `contracts/` modifications during this plan (D-04 unchanged guarantee for `processTicketBatch`; AC-7 is audit-only per SPEC §In Scope)"
    - "ZERO `test/` modifications during this plan (Plan 02 owns the test artifacts)"
    - "Plan execution is autonomous: writes only to `.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/` -- no contract or test diffs require user approval at this step"
  artifacts:
    - path: ".planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-SIM-REPLAY.md"
      provides: "AC-4 + AC-5 verification: sim re-run output + trait distribution analysis"
      contains: "## AC-4 Verdict"
    - path: ".planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-PFTB-AUDIT.md"
      provides: "AC-7 verification: processFutureTicketBatch reachable-caller audit trace"
      contains: "## Per-Caller Verdict Table"
  key_links:
    - from: ".planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-SIM-REPLAY.md"
      to: "Sim infrastructure (location TBD; either sibling `simulator/` repo, embedded forge-test sim, or substitute documented inline)"
      via: "Re-run command + post-fix output capture"
      pattern: "entropyWord"
    - from: ".planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-PFTB-AUDIT.md"
      to: "contracts/modules/DegenerusGameAdvanceModule.sol L300, L344, L354, L392 (every `_processFutureTicketBatch(...)` call site) + L1309-1325 (the wrapper) + contracts/modules/DegenerusGameMintModule.sol L385 (the audited function)"
      via: "Per-call-site verdict verifying the `entropy` argument is non-zero (sourced from `rngWord` returned by `rngGate`, which is non-zero by construction at the relevant control-flow point)"
      pattern: "_processFutureTicketBatch\\("
---

<objective>
Two audit-only deliverables that close the remaining SPEC.md acceptance criteria not covered by Plan 02:
1. **Sim-replay regression** (AC-4 + AC-5) -- re-run the 25L/100P turbo sim that produced the original bug evidence (136 batches / 3439 traits with `entropy=0` at L5; Q0 cat-0 inflated to 39.1%, Q0 cat-7 collapsed to 0.3%, 6 zero-hit trait IDs) against the post-fix contract; confirm ZERO `entropy=0` batches and trait distribution within ±1pp of design.
2. **`processFutureTicketBatch` reachable-caller audit** (AC-7) -- per-caller verdict table proving every reachable call site supplies non-zero `entropy`; no code change to `processFutureTicketBatch` (per SPEC §In Scope "audit only -- verify all callers pass non-zero entropy; no `revert RngNotReady()` added").

Purpose: Plans 01 and 02 deliver the structural fix and the forge invariant/binding/path-isolation tests. Plan 03 closes the loop by (a) verifying the bug-producing sim no longer reproduces the bug post-fix (statistical end-to-end proof), and (b) documenting that the audit-only requirement on `processFutureTicketBatch` is satisfied without any code change.

Output: Two `.md` artifacts in the phase directory. ZERO contract or test edits. Fully autonomous -- no user-approval checkpoint required (no contracts/ or test/ diffs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-SPEC.md
@.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-CONTEXT.md
@.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-01-PLAN.md
@.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-01-SUMMARY.md
@.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-02-PLAN.md
@.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-02-SUMMARY.md
@.planning/phases/232-decimator-audit/232-03-AUDIT.md

<interfaces>
SPEC.md §Background bug evidence (the empirical baseline this plan must regression-prove against):

> "25L/100P turbo sim against the real compiled contract (not a re-impl) shows 136 batches / 3439 traits at L5 alone minted with `entropy = 0` -- approximately half of L5's 6884 samples. With `entropy = 0`, the trait seed collapses to `keccak256(abi.encode(baseKey, 0, groupIdx))`, making trait RNG a deterministic function of on-chain-visible inputs (level, queueIdx, playerAddr, groupIdx). Per-quadrant category distribution shows Q0 cat-0 at 39.1% (design 13.3%) and Q0 cat-7 at 0.3% (design 10.7%), with 6 trait IDs (56-61) completely unreachable at L5."

SPEC.md §Acceptance Criteria AC-4 (post-fix sim verdict):

> "Sim re-run: 25L/100P turbo sim post-fix shows zero `_raritySymbolBatch` events with `entropyWord == 0` in the event log."

SPEC.md §Acceptance Criteria AC-5 (post-fix trait distribution verdict):

> "Sim re-run: L5 trait distribution shows zero zero-hit trait IDs; per-quadrant cat-0 and cat-7 percentages within ±1 percentage point of design (13.3% and 10.7% respectively)."

SPEC.md §Acceptance Criteria AC-7 (processFutureTicketBatch audit-only requirement):

> "`processFutureTicketBatch` audit trace documented: every reachable caller path confirmed to pass non-zero `entropy`; no code modification to that function."

SPEC.md §Boundaries In Scope (the audit-only scope for processFutureTicketBatch):

> "Audit trace of `processFutureTicketBatch` (DegenerusGameMintModule.sol:385) documenting that its `entropy` parameter always arrives non-zero on every reachable caller path; no code change to that function."

SPEC.md §Out of Scope (re-confirms no defensive guard at the function body):

> "**processFutureTicketBatch code change** -- audit only; verify all callers pass non-zero entropy; no `revert RngNotReady()` added (per user: 'audit, don't fix')."

CONTEXT.md §Specific Ideas (sim choice latitude granted to planner):

> "Whether the sim-replay regression is a single Bash/Python script under `simulator/` or a Forge test."

Reachable-caller enumeration for `_processFutureTicketBatch` (the only AdvanceModule function that delegatecalls into `processFutureTicketBatch`), grep-confirmed against contracts/modules/DegenerusGameAdvanceModule.sol:

| Call site | File:Line | Branch context | `entropy` source |
|---|---|---|---|
| Phase-transition FF drain | DegenerusGameAdvanceModule.sol:300 | inside `if (phaseTransitionActive)` block (L283-316), after `rngGate` returned non-1 | `rngWord` from `rngGate` (must be non-1 to reach here per L276 break) |
| Resume-FF (jackpot-phase entry sweep) | DegenerusGameAdvanceModule.sol:344 | `_prepareFutureTickets` at AdvanceModule.sol:1340-1349 (resume-level branch) | `entropy` parameter, sourced from `rngWord` returned by `rngGate` and threaded through `_prepareFutureTickets` |
| Probe-FF (jackpot-phase entry sweep) | DegenerusGameAdvanceModule.sol:354 | `_prepareFutureTickets` at AdvanceModule.sol:1352-1364 (probe-remaining branch) | same `entropy` parameter |
| Last-purchase-day next-level FF activation | DegenerusGameAdvanceModule.sol:392 | inside `if (lastPurchaseDay) { ... _processFutureTicketBatch(nextLevel, rngWord); }` (L387-396) | `rngWord` from `rngGate` |

The audit MUST trace each of these four call sites and prove the `rngWord` reaching them is non-zero. Key control-flow proofs:
- `rngGate` returns either (a) the resolved `rngWord` (non-zero by construction; the daily callback at L1624 forces `if (word == 0) word = 1;`), (b) sentinel `1` meaning "request issued, retry next block" (caught by the `break` at L276), or (c) reverts `RngNotReady()` (no return path). Therefore any `rngWord` that flows past the L276 break is non-zero.
- The phase-transition branch is entered only after `rngGate` returned non-1 (L268-280 ordering); thus call site L300's `rngWord` is non-zero.
- `_prepareFutureTickets` at L322 is called with `rngWord` ALSO sourced from the same `rngGate` return; threaded to L344/L354 unchanged.
- L392's `rngWord` is the same local from the `do-while` loop scope.

This deterministic non-zero proof is what AC-7 requires. The audit document captures it formally per-call-site.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sim-replay regression (AC-4 + AC-5) -- write 232.1-03-SIM-REPLAY.md</name>
  <files>.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-SIM-REPLAY.md</files>
  <read_first>
    - .planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-SPEC.md §Background (sim evidence: 136 batches / 3439 traits with `entropy=0`; Q0 cat-0 39.1%, Q0 cat-7 0.3%; 6 zero-hit trait IDs at L5) and §Acceptance Criteria AC-4 + AC-5 (post-fix targets: zero `entropy=0` events; ±1pp on cat-0/cat-7; zero zero-hit trait IDs)
    - .planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-CONTEXT.md §Claude's Discretion bullet "Whether the sim-replay regression is a single Bash/Python script under `simulator/` or a Forge test."
    - claude-memory `feedback_skip_research_test_phases.md` (this is a regression run + audit phase -- skip research, plan and execute directly)
    - The sibling `simulator/` location -- verify whether it is a sibling repo of `degenerus-audit` (e.g. `../degenerus-simulator/` or `../simulator/`) or embedded in this repo. Run `ls /home/zak/Dev/PurgeGame/` and `ls -d /home/zak/Dev/PurgeGame/*/simulator/ 2>/dev/null` to find the actual location. The SPEC §Background phrase "25L/100P turbo sim against the real compiled contract (not a re-impl)" implies a node/python harness exists somewhere -- locate it before deciding the substitute strategy.
    - If the simulator harness is found, read its top-level README or entry-point script to confirm the invocation command for "25L/100P turbo" runs and the trait-distribution aggregation script (likely a SQL or Python aggregation against the captured event log)
  </read_first>
  <action>
**Step 0 -- Pre-flight: confirm Plans 01 + 02 have shipped.**
- Run `git log --oneline -5`. The two most recent commits MUST be (in either order, but expected to be Plan 02 then Plan 01):
  - `test(232.1): forge suite for drain-ordering invariant ...`
  - `fix(232.1): lazy pre-finalize lootbox RNG slot at daily-drain entry (D-01)`
- Run `git status --porcelain`. MUST be empty.

**Step 1 -- Locate sim infrastructure.**
Run the following discovery commands (record output in 232.1-03-SIM-REPLAY.md `## Sim Infrastructure Location` section):
```
ls /home/zak/Dev/PurgeGame/
ls -d /home/zak/Dev/PurgeGame/*/simulator/ 2>/dev/null
ls -d /home/zak/Dev/PurgeGame/*/sim/ 2>/dev/null
find /home/zak/Dev/PurgeGame -maxdepth 4 -type d -name "*sim*" 2>/dev/null | head -20
find /home/zak/Dev/PurgeGame -maxdepth 4 -name "turbo*" 2>/dev/null | head -10
find /home/zak/Dev/PurgeGame -maxdepth 4 -name "*25L*" -o -name "*100P*" 2>/dev/null | head -10
```

Three possible outcomes:
1. **Sim harness found** in a sibling repo or a peer directory (e.g. `/home/zak/Dev/PurgeGame/degenerus-simulator/`): proceed to Step 2A.
2. **Sim harness found embedded in this repo** (unlikely given the directory listing): proceed to Step 2A with the embedded path.
3. **Sim harness NOT found**: proceed to Step 2B (substitute via forge-test).

**Step 2A -- Sim harness found: re-run the turbo sim against post-fix HEAD.**
- Identify the invocation command for the 25L/100P turbo run (from the harness's README or entry-point script).
- Confirm the harness reads from this repo's compiled bytecode (not a re-impl). The SPEC §Background phrase "against the real compiled contract" implies it exec-spawns `forge` or reads `out/*.json` artifacts. Verify the artifact path matches `out/DegenerusGameMintModule.sol/...` or equivalent.
- Run `forge build` (or `make invariant-build`) in `degenerus-audit/` to ensure the sim picks up the post-fix bytecode.
- Execute the turbo sim. Capture stdout, stderr, and any output files (event log JSON/CSV, trait counts).
- Run the trait-distribution aggregation script (likely a SQL query against a SQLite event database or a Python pandas script). Capture the per-quadrant cat-0/cat-7 percentages at L5 and the zero-hit trait ID count at L5.

**Step 2B -- Sim harness NOT found: substitute via forge-test sim.**
If no external sim is available, the substitute IS a forge test that:
- Runs `advanceGame` repeatedly across 25 levels with 100 players to L5 (matching the SPEC §Background scenario approximately)
- Captures every `_raritySymbolBatch` invocation's `entropyWord` argument (Option A or B from Plan 02 Step 1; reuse the established instrumentation)
- Aggregates the L5 trait IDs across all minted tickets and computes per-quadrant cat-0/cat-7 percentages

The substitute lives at `test/edge/SimReplayRegression.t.sol` (consistent with `test/edge/SimAdvanceOverflow.t.sol` precedent already in this repo). HOWEVER, since this Plan is autonomous and `feedback_no_contract_commits.md` extends to test/, the substitute test file would require user approval. Therefore:

If Step 1 finds NO external sim, Plan 03 produces 232.1-03-SIM-REPLAY.md with a `## Sim Substitute Required` section that:
- Documents the absence of the external sim infrastructure
- Cites the forge-test fuzz tests from Plan 02 as the closest available statistical proof (the `RngIndexDrainOrderingInvariants.invariant_noZeroEntropyConsumption` invariant runs across many fuzzed sequences and asserts ZERO zero-entropy consumptions; this is the same property AC-4 asks for, just statistically less than the explicit 25L/100P scenario)
- Recommends a follow-up: either land a `test/edge/SimReplayRegression.t.sol` substitute via a separate user-approved commit, OR locate and re-run the original sim infrastructure
- Explicitly marks AC-4 and AC-5 as "PARTIAL via Plan 02 invariant suite; FULL via [follow-up]" rather than claiming false completeness

**Step 3 -- Per-quadrant trait distribution analysis.**
If Step 2A produced output, write the per-quadrant cat-0/cat-7 percentages and the L5 zero-hit trait ID count into 232.1-03-SIM-REPLAY.md. Compute the deltas:
- Q0 cat-0: post-fix value vs design 13.3% -- delta in percentage points
- Q1 cat-0: post-fix value vs design 13.3% -- delta in percentage points
- Q2 cat-0: post-fix value vs design 13.3% -- delta in percentage points
- Q3 cat-0: post-fix value vs design 13.3% -- delta in percentage points
- Q0 cat-7: post-fix value vs design 10.7% -- delta in percentage points (and same for Q1/Q2/Q3)
- L5 zero-hit trait IDs: post-fix count (target = 0) vs pre-fix baseline 6

For each row, mark `WITHIN ±1pp` (PASS for AC-5) or `OUTSIDE ±1pp` (FAIL for AC-5). If any quadrant exceeds ±1pp, surface to user without claiming AC-5 PASS.

**Step 4 -- Write `232.1-03-SIM-REPLAY.md`** with the following structure (executor inserts the literal headers; fills in actual data):

- Title heading + SPEC requirements addressed (R6 + AC-4 + AC-5)
- `## Sim Infrastructure Location` -- output of Step 1 discovery; chosen path (2A or 2B)
- `## Pre-Fix Baseline (from SPEC §Background)` -- 136 batches / 3439 traits with `entropy=0`; Q0 cat-0 39.1%; Q0 cat-7 0.3%; 6 zero-hit trait IDs at L5
- `## Post-Fix Sim Run` -- invocation command + raw stdout/stderr capture (or 2B substitute documentation)
- `## AC-4 Verdict (zero entropy=0 events)` -- post-fix `_raritySymbolBatch(entropyWord=0)` count; PASS if 0, FAIL otherwise
- `## AC-5 Verdict (trait distribution within ±1pp)` -- per-quadrant cat-0 and cat-7 deltas table; L5 zero-hit trait ID count; PASS if all within tolerance and zero zero-hit IDs, FAIL otherwise
- `## Caveats and Substitute Documentation` -- if Step 2B path was used, document what AC-4 / AC-5 actually got verified vs deferred and why

**Step 5 -- DO NOT modify contracts/ or test/.** This plan is audit-only; the only writes are to `.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/`.
  </action>
  <verify>
    <automated>test -f .planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-SIM-REPLAY.md && grep -q "## AC-4 Verdict" .planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-SIM-REPLAY.md && grep -q "## AC-5 Verdict" .planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-SIM-REPLAY.md && test -z "$(git status --porcelain contracts/ test/)"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-SIM-REPLAY.md` exists
    - File contains the literal headers `## Sim Infrastructure Location`, `## Pre-Fix Baseline (from SPEC §Background)`, `## Post-Fix Sim Run`, `## AC-4 Verdict (zero entropy=0 events)`, `## AC-5 Verdict (trait distribution within ±1pp)`, `## Caveats and Substitute Documentation`
    - `## AC-4 Verdict` section explicitly states either `PASS` (post-fix entropy=0 count is 0) or surfaces a non-zero count to user with explanation -- NO silent FAIL
    - `## AC-5 Verdict` section either (a) presents a per-quadrant table with deltas marked WITHIN/OUTSIDE ±1pp and a PASS verdict if all within tolerance + zero zero-hit IDs, OR (b) explicitly documents the substitute path from Step 2B and marks AC-5 as PARTIAL with a follow-up plan
    - `git status --porcelain contracts/` returns empty (zero contracts/ writes)
    - `git status --porcelain test/` returns empty (zero test/ writes)
    - The strings `// was`, `// previously`, `// changed`, `// FIX-` do NOT appear in the file (consistent with `feedback_no_history_in_comments.md`)
  </acceptance_criteria>
  <done>`232.1-03-SIM-REPLAY.md` exists with the post-fix sim re-run output (or Step 2B substitute documentation), AC-4 verdict (zero `_raritySymbolBatch(entropyWord=0)` events on post-fix), AC-5 verdict (per-quadrant cat-0/cat-7 within ±1pp of design + zero L5 zero-hit trait IDs, OR explicit substitute marking). Zero contracts/ or test/ writes occurred during execution.</done>
</task>

<task type="auto">
  <name>Task 2: processFutureTicketBatch reachable-caller audit (AC-7) -- write 232.1-03-PFTB-AUDIT.md</name>
  <files>.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-PFTB-AUDIT.md</files>
  <read_first>
    - .planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-SPEC.md §In Scope (audit trace of processFutureTicketBatch -- entropy non-zero on every reachable caller path; no code change), §Out of Scope ("processFutureTicketBatch code change -- audit only; verify all callers pass non-zero entropy; no revert RngNotReady() added"), §Acceptance Criteria AC-7
    - .planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-CONTEXT.md §Specifics bullet ("No `processFutureTicketBatch` code change -- audit-only. Planner produces an audit trace document (similar to Phase 232's per-function verdict tables) confirming every reachable caller passes non-zero entropy.")
    - .planning/phases/232-decimator-audit/232-03-AUDIT.md (precedent: Phase 232's per-function verdict-table format with SHA + File:Line + Verdict columns; reuse the structure for AC-7)
    - contracts/modules/DegenerusGameAdvanceModule.sol L283-316 (phase-transition FF drain block; cite call site L300)
    - contracts/modules/DegenerusGameAdvanceModule.sol L320-330 (`_prepareFutureTickets` invocation at L322; threads `rngWord` to internal `_processFutureTicketBatch` calls)
    - contracts/modules/DegenerusGameAdvanceModule.sol L1334-1365 (`_prepareFutureTickets` body; cite call sites L1344-1349 and L1354-1358)
    - contracts/modules/DegenerusGameAdvanceModule.sol L385-396 (last-purchase-day next-level FF activation at L392)
    - contracts/modules/DegenerusGameAdvanceModule.sol L1309-1325 (`_processFutureTicketBatch` wrapper -- delegatecalls into MintModule.processFutureTicketBatch with the supplied `entropy` argument)
    - contracts/modules/DegenerusGameAdvanceModule.sol L1056-1125 (`rngGate` -- the upstream source of every `rngWord` that flows into the four call sites; confirm the daily callback's L1624 `if (word == 0) word = 1;` non-zero guard, and the L276 break that catches `rngWord==1` sentinel)
    - contracts/modules/DegenerusGameAdvanceModule.sol L1616-1637 (`rawFulfillRandomWords` L1624 zero-guard `if (word == 0) word = 1;` -- the structural reason every fulfilled VRF word is non-zero)
    - contracts/modules/DegenerusGameMintModule.sol L385-... (`processFutureTicketBatch` -- the audited function; D-04 + AC-7 forbid any code change)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.1 row for `_processFutureTicketBatch` if present (52242a10 commit per ROADMAP §233 -- relevant background but not in 232.1 scope)
  </read_first>
  <action>
**Step 1 -- Re-confirm `processFutureTicketBatch` source unchanged.**
Run `git diff HEAD -- contracts/modules/DegenerusGameMintModule.sol`. MUST be empty (Plan 01 made no MintModule changes per D-04; Plan 02 made no contract changes; Plan 03 is audit-only). If non-empty, halt -- something else modified MintModule outside this phase's scope.

**Step 2 -- Enumerate every call site of `_processFutureTicketBatch` in AdvanceModule.**
Run `grep -n "_processFutureTicketBatch" contracts/modules/DegenerusGameAdvanceModule.sol`. EXPECTED four call sites (per the `<interfaces>` table above):
- AdvanceModule.sol:300 -- phase-transition FF drain
- AdvanceModule.sol:344 -- `_prepareFutureTickets` resume-FF branch
- AdvanceModule.sol:354 -- `_prepareFutureTickets` probe-FF branch
- AdvanceModule.sol:392 -- last-purchase-day next-level FF activation

Plus the wrapper definition at AdvanceModule.sol:1309. If the grep returns any additional call sites, the audit MUST cover them (do not silently skip).

**Step 3 -- For each call site, build the non-zero-entropy proof.**

For each call site:
1. Identify the lexical `entropy` (or `rngWord`) variable supplied to `_processFutureTicketBatch(...)`.
2. Trace the variable's source within the same function. The four expected sources:
   - L300: `rngWord` from `rngGate` returned at L268 (caught by `break` at L276 if rngGate returned sentinel `1`; otherwise non-zero)
   - L344: `entropy` parameter of `_prepareFutureTickets`, passed in from caller at AdvanceModule.sol:322 as `rngWord`
   - L354: same `entropy` parameter
   - L392: `rngWord` local in the do-while loop scope, same source as L300
3. Confirm `rngGate` returns non-zero by referencing the L1624 callback guard `if (word == 0) word = 1;` AND the L276 break that catches the sentinel-`1` case.
4. Record the verdict `SAFE` per call site with File:Line + the chain of evidence.

If any call site's `entropy` source CANNOT be proven non-zero from on-chain reachable code paths, the verdict is `VULNERABLE` and a finding-candidate row is emitted -- this would invalidate AC-7 and require a SPEC revision.

**Step 4 -- Confirm wrapper and audited function signatures match.**
- AdvanceModule.sol:1309-1325 wrapper: `function _processFutureTicketBatch(uint24 lvl, uint256 entropy) private returns (bool worked, bool finished, uint32 writesUsed)`. Verify the abi.encodeWithSelector at L1316-1320 passes `lvl` and `entropy` in correct positional order matching the audited function's signature.
- MintModule.sol:385 audited function: `function processFutureTicketBatch(uint24 lvl, uint256 entropy) external returns (bool worked, bool finished, uint32 writesUsed)`. Confirm signature is byte-identical to the wrapper's selector.
- Confirm `make check-delegatecall` (run as part of `make check-delegatecall` from Plan 01) reports the IM chain still aligned at 44/44.

**Step 5 -- Write `232.1-03-PFTB-AUDIT.md`** in the v29.0 audit format established by Phase 232 (mirror the column schema from `.planning/phases/232-decimator-audit/232-03-AUDIT.md`):

Required structure (executor inserts the literal headers; fills in actual data):

- Title heading + scope (`processFutureTicketBatch` audit-only per SPEC §In Scope + AC-7)
- Method statement (fresh read from HEAD source; reuse Phase 232 verdict-vocabulary `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`)
- `## Per-Caller Verdict Table` with columns: `Caller (File:Line) | Branch Context | entropy Source | Non-Zero Proof | Verdict`
  - Row 1: AdvanceModule.sol:300 -- phase-transition FF drain -- `rngWord` from L268 `rngGate` -- non-zero per L1624 callback guard + L276 sentinel break -- SAFE
  - Row 2: AdvanceModule.sol:344 -- `_prepareFutureTickets` resume-FF -- `entropy` param threaded from L322 -- non-zero per same chain -- SAFE
  - Row 3: AdvanceModule.sol:354 -- `_prepareFutureTickets` probe-FF -- same `entropy` param -- non-zero per same chain -- SAFE
  - Row 4: AdvanceModule.sol:392 -- last-purchase-day next-level FF -- `rngWord` from same do-while scope -- non-zero per same chain -- SAFE
- `## Wrapper-Function Signature Lockstep` -- AdvanceModule.sol:1309-1325 wrapper signature byte-identical to MintModule.sol:385 audited function signature; abi.encodeWithSelector at L1316-1320 passes args in matching positional order; `make check-delegatecall` 44/44 PASS at HEAD
- `## Code-Change Verdict` -- per AC-7 + D-04 + SPEC §Out of Scope: ZERO modifications to `processFutureTicketBatch` body. `git diff HEAD -- contracts/modules/DegenerusGameMintModule.sol` returns empty. The audit trace closes AC-7 without a code change
- `## Cross-Phase Hand-off` -- the `52242a10` commit ("explicit entropy passthrough to processFutureTicketBatch") is the audit subject of Phase 233 JKP-02 per ROADMAP. Phase 232.1's AC-7 audit is SCOPED to "callers within the daily-drain / mid-day / phase-transition / FF-activation control flow currently reachable in `advanceGame`". Phase 233 JKP-02 owns the deeper "cryptographically equivalent to prior derivation, no commitment-window widening, no re-use across calls in the same transaction" audit per its own success criteria. Document this as a hand-off, NOT a finding

**Step 6 -- DO NOT modify contracts/ or test/.** This plan is audit-only.
  </action>
  <verify>
    <automated>test -f .planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-PFTB-AUDIT.md && grep -q "## Per-Caller Verdict Table" .planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-PFTB-AUDIT.md && grep -cE "SAFE|SAFE-INFO|VULNERABLE|DEFERRED" .planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-PFTB-AUDIT.md | awk '{exit ($1 >= 4 ? 0 : 1)}' && test -z "$(git status --porcelain contracts/ test/)"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-PFTB-AUDIT.md` exists
    - File contains the literal headers `## Per-Caller Verdict Table`, `## Wrapper-Function Signature Lockstep`, `## Code-Change Verdict`, `## Cross-Phase Hand-off`
    - The Per-Caller Verdict Table contains at least four rows, one per call site at AdvanceModule.sol:300, 344, 354, 392
    - Each row carries a verdict from the locked vocabulary `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (per Phase 232 precedent)
    - Each row cites a real File:Line in `contracts/modules/DegenerusGameAdvanceModule.sol`
    - The Code-Change Verdict section explicitly cites `git diff HEAD -- contracts/modules/DegenerusGameMintModule.sol` returns empty (zero modifications -- AC-7 closed without code change)
    - The Cross-Phase Hand-off section explicitly names `Phase 233 JKP-02` as the deeper-scope owner of the `52242a10` commit's RNG commitment-window analysis
    - `git status --porcelain contracts/` returns empty (zero contracts/ writes)
    - `git status --porcelain test/` returns empty (zero test/ writes)
    - The string `revert RngNotReady` does NOT appear as a NEW addition anywhere in the diff (per SPEC §Out of Scope: "no `revert RngNotReady()` added" at the function body)
    - The string `F-29-NN` does NOT appear in the file (per Phase 232 precedent: this plan does not assign canonical finding IDs; Phase 236 FIND-01 owns ID assignment if any candidate emerges)
    - At least one row's Non-Zero Proof column references the `if (word == 0) word = 1;` guard at AdvanceModule.sol L1624 (the structural reason every fulfilled VRF word is non-zero)
  </acceptance_criteria>
  <done>`232.1-03-PFTB-AUDIT.md` exists with a per-caller verdict table covering all four reachable call sites of `_processFutureTicketBatch` in AdvanceModule. Every row has a `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` verdict and a non-zero-entropy proof traceable to the L1624 callback guard. Wrapper-function signature lockstep with MintModule.sol:385 confirmed. Code-change verdict states zero modifications to `processFutureTicketBatch` body. Phase 233 JKP-02 hand-off documented for the deeper-scope audit. Zero contracts/ or test/ writes occurred. AC-7 closed.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the two audit-only artifacts (autonomous -- no user approval needed)</name>
  <files>.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-SIM-REPLAY.md, .planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-PFTB-AUDIT.md</files>
  <read_first>
    - .planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-SIM-REPLAY.md (Task 1 output)
    - .planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-PFTB-AUDIT.md (Task 2 output)
  </read_first>
  <action>
**Step 1 -- Stage exactly two files.**
```
git add .planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-SIM-REPLAY.md
git add .planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-PFTB-AUDIT.md
```

DO NOT use `git add -A` or `git add .`. Stage by explicit path.

**Step 2 -- Verify staging.**
Run `git diff --cached --stat`. Output MUST list exactly these two files. Both under `.planning/` -- per `feedback_no_contract_commits.md`, planning artifacts do NOT require explicit user approval (only `contracts/` and `test/` do).

**Step 3 -- Commit with the standardized message:**

```
docs(232.1): sim-replay regression (AC-4 + AC-5) + processFutureTicketBatch audit (AC-7)

Two audit-only artifacts closing the SPEC.md acceptance criteria not
covered by Plan 02's forge tests:

- 232.1-03-SIM-REPLAY.md -- AC-4 + AC-5: post-fix sim re-run shows
  zero _raritySymbolBatch events with entropyWord=0; L5 trait
  distribution per-quadrant within ±1pp of design and zero zero-hit
  trait IDs (or explicit substitute documentation if external sim
  infrastructure unavailable)

- 232.1-03-PFTB-AUDIT.md -- AC-7: per-caller verdict table for
  processFutureTicketBatch's four reachable call sites in AdvanceModule
  (L300 phase-transition, L344 + L354 _prepareFutureTickets,
  L392 last-purchase-day FF). Every entropy source proven non-zero
  via the rngGate L1624 callback guard + L276 sentinel break. ZERO
  modifications to processFutureTicketBatch body per D-04 + SPEC
  §Out of Scope. Phase 233 JKP-02 hand-off documented for the
  deeper-scope 52242a10 audit.

ZERO contracts/ or test/ writes during plan execution.

Refs: SPEC.md AC-4, AC-5, AC-7; CONTEXT.md D-04 + §Specifics.
```

**Step 4 -- Verify commit landed.**
```
git log -1 --stat
git status --porcelain
```
First command: confirm message and two-file change set. Second command: confirm clean working tree.

**Step 5 -- DO NOT push.** Phase 232.1 is now functionally complete (Plan 01 fix + Plan 02 tests + Plan 03 sim regression + PFTB audit) but the user has explicit authority over the push timing per `feedback_manual_review_before_push.md`. The plan returns control with a recommendation to run `/gsd-verify-phase 232.1` for the goal-backward verification before push.
  </action>
  <verify>
    <automated>git log -1 --pretty=format:"%s" | grep -q "docs(232.1): sim-replay regression (AC-4 + AC-5) + processFutureTicketBatch audit (AC-7)" && git log -1 --stat | grep -q "232.1-03-SIM-REPLAY.md" && git log -1 --stat | grep -q "232.1-03-PFTB-AUDIT.md" && test -z "$(git status --porcelain)"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=format:"%s"` returns exactly `docs(232.1): sim-replay regression (AC-4 + AC-5) + processFutureTicketBatch audit (AC-7)`
    - `git log -1 --stat` lists exactly two files: `.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-SIM-REPLAY.md` and `.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-PFTB-AUDIT.md`
    - Commit message body contains the substrings `AC-4`, `AC-5`, `AC-7`, `D-04`, `Phase 233 JKP-02`, and `ZERO contracts/ or test/ writes`
    - `git status --porcelain` returns empty (clean working tree post-commit)
    - `git diff HEAD~3 HEAD --stat -- contracts/` shows ONLY `contracts/modules/DegenerusGameAdvanceModule.sol` as modified across the three Phase 232.1 commits (Plan 01 fix only; Plans 02 and 03 made zero contracts/ changes)
    - Commit was NOT pushed
  </acceptance_criteria>
  <done>The two audit-only artifacts are committed locally. Phase 232.1 is functionally complete: Plan 01 (fix + diff review + commit), Plan 02 (forge tests + replay verification + commit), Plan 03 (sim regression + processFutureTicketBatch audit + commit). All nine SPEC §Acceptance Criteria are addressed: AC-1, AC-2 (Plan 02 invariants); AC-3 (Plan 02 binding test); AC-4, AC-5 (Plan 03 sim replay); AC-6 (Plan 02 existing-suite green); AC-7 (Plan 03 PFTB audit); AC-8 (Plan 01 `make check-delegatecall` 44/44); AC-9 (Plans 01 + 02 explicit user approval before contract/test commits). Push happens only when user explicitly requests it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sim harness -> compiled contract bytecode | Sim reads `out/*.json` artifacts produced by `forge build`; relies on Plan 01's fix being baked into the bytecode (verified by Step 0 git-log check) |
| Trait-distribution aggregation -> raw event log | Aggregation script consumes the captured event log; if the log is incomplete (e.g. sim crashed mid-run), the trait-distribution stats are inadmissible |
| `_processFutureTicketBatch` wrapper -> MintModule.processFutureTicketBatch (delegatecall) | Cross-module delegatecall; `make check-delegatecall` 44/44 confirms the wire alignment is intact post-fix |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-232.1-14 | T (Tampering) | Sim run executes against stale bytecode (pre-fix artifacts not invalidated) | mitigate | Step 2A explicitly runs `forge build` (or `make invariant-build`) before sim invocation; Step 0 confirms Plan 01's fix commit is at HEAD |
| T-232.1-15 | I (Information Disclosure) | Trait-distribution aggregation leaks deterministic seed (post-fix should be VRF-derived; if leakage detected, the fix is incomplete) | mitigate | The aggregation explicitly checks zero zero-hit trait IDs at L5; per SPEC §Background "with `entropy = 0`, the trait seed collapses to `keccak256(abi.encode(baseKey, 0, groupIdx))`" -- presence of zero-hit IDs would indicate the seed is still partially deterministic |
| T-232.1-16 | T (Tampering) | False AC-7 PASS via missed call site (e.g. `delegatecall` to `processFutureTicketBatch` from a different module not enumerated in Step 2) | mitigate | Step 2 uses `grep -n "_processFutureTicketBatch" contracts/modules/DegenerusGameAdvanceModule.sol` AND requires the executor to widen the grep if any unexpected matches surface; the IM chain through `make check-delegatecall` corroborates that no other module delegates to the same selector |
| T-232.1-17 | D (Denial of Service) | Plan 03 produces a partial AC-4/AC-5 verdict due to missing sim infrastructure but masks the partial as full PASS | mitigate | Step 2B explicitly requires marking AC-4/AC-5 as `PARTIAL via Plan 02 invariant suite; FULL via [follow-up]` rather than claiming false completeness; the substitute documentation cites the forge-test fuzz tests from Plan 02 as the closest available statistical proof |
| T-232.1-18 | E (Elevation of Privilege) | Audit document silently approves a contracts/ change that should have required user approval | accept | Plan 03 is autonomous because it makes ZERO contracts/ or test/ writes -- only `.planning/` writes are touched, and per `feedback_no_contract_commits.md` planning artifacts do not require explicit per-diff approval. Verified by `git status --porcelain contracts/ test/` empty in Task acceptance criteria |

**Mitigation status:** All HIGH-severity threats (T-232.1-14, T-232.1-15, T-232.1-16, T-232.1-17) are mitigated by task-level acceptance criteria. Accepted threat (T-232.1-18) is justified by the audit-only scope; the per-task `git status --porcelain contracts/ test/` check enforces the constraint structurally.
</threat_model>

<verification>
- `232.1-03-SIM-REPLAY.md` exists and contains the AC-4 verdict (zero `_raritySymbolBatch(entropyWord=0)` events post-fix) and AC-5 verdict (per-quadrant trait distribution within ±1pp of design + zero L5 zero-hit trait IDs), OR explicit substitute documentation if external sim infrastructure was not available
- `232.1-03-PFTB-AUDIT.md` exists and contains a per-caller verdict table covering all four reachable call sites of `_processFutureTicketBatch` in AdvanceModule, each with a `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` verdict and a non-zero-entropy proof traceable to the L1624 callback guard
- `git diff HEAD~3 HEAD --stat -- contracts/` shows ONLY `contracts/modules/DegenerusGameAdvanceModule.sol` as modified across all three Phase 232.1 commits (Plan 01 fix only; Plans 02 and 03 made zero contracts/ changes -- D-04 fully honored, AC-7 audit-only honored)
- Both audit artifacts are committed locally; commit is NOT pushed
- Phase 232.1 is functionally complete; user can now run `/gsd-verify-phase 232.1` for goal-backward verification before push
</verification>

<success_criteria>
SPEC §Acceptance Criteria AC-4 (post-fix sim shows zero `_raritySymbolBatch(entropyWord=0)` events), AC-5 (post-fix L5 trait distribution per-quadrant within ±1pp of design + zero zero-hit trait IDs), and AC-7 (`processFutureTicketBatch` audit trace documented; every reachable caller passes non-zero entropy; no code change to that function) are all addressed. CONTEXT.md D-04 (no `processTicketBatch` change) and SPEC §Out of Scope (no `revert RngNotReady()` added at the audit-only function) are honored: `git diff HEAD~3 HEAD --stat -- contracts/` shows ONLY `DegenerusGameAdvanceModule.sol` modified across the three Phase 232.1 commits. Cross-phase hand-off to Phase 233 JKP-02 documented for the deeper `52242a10` commitment-window audit. Plan 03 executed autonomously (zero contracts/ or test/ writes; only `.planning/` artifacts produced). Commit is local-only -- push happens when user explicitly requests it after `/gsd-verify-phase 232.1`.
</success_criteria>

<output>
After completion, create `.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-SUMMARY.md`
</output>
</content>
