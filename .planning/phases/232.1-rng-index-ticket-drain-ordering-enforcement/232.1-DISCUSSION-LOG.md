# Phase 232.1: RNG-Index Ticket Drain Ordering Enforcement - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-17
**Phase:** 232.1-rng-index-ticket-drain-ordering-enforcement
**Areas discussed:** Structural gate location, Entropy sourcing at L690, Game-over path scope, L190-194 mid-day guard handling, rngWordCurrent simplification (out-of-scope exploration)

---

## Gray-Area Selection

| Option | Description | Selected |
|--------|-------------|----------|
| Structural gate location | Where to enforce 'no LR_INDEX advance while prior RNG unfulfilled' | ✓ |
| Entropy sourcing at L690 | Keep storage read vs. parameter passthrough | ✓ |
| Game-over path scope | Audit-only vs. extend structural gate vs. forbid LR_INDEX advance post-game-over | ✓ |
| Test harness strategy | Forge invariant vs. unit vs. sim-replay | (skipped) |

**User's selection:** Structural gate location, Entropy sourcing at L690, Game-over path scope.

---

## Structural Gate Location (Round 1 — Initial Options)

| Option | Description | Selected |
|--------|-------------|----------|
| Pre-swap in `_requestRng` | Gate inside `_requestRng` before `_swapTicketSlot` | |
| Lift L190-194 guard above split | Move existing mid-day guard to cover both branches | |
| Bump LR_INDEX at fulfillment | Move LR_INDEX++ from `_finalizeRngRequest` to `_finalizeLootboxRng` | |
| Drain-sequenced stage re-order | Restructure advanceGame to drain unconditionally before rngGate | |

**User's response:** Asked for full path enumeration before answering.

**Notes:** Claude traced `requestLootboxRng` (mid-day, L956-1036) vs `_requestRng → _finalizeRngRequest` (daily, L1445-1540) and identified an asymmetry: mid-day callback writes lootbox slot atomically (L1632); daily callback only sets `rngWordCurrent` and defers slot-write to inside rngGate at L1108. Path enumeration table revealed the daily-flow drain at L242-262 races against the deferred slot-write — that is the structural mismatch.

---

## Structural Gate Location (Round 2 — Refined Options Post-Path-Enumeration)

| Option | Description | Selected |
|--------|-------------|----------|
| Mirror mid-day in callback | Move `_finalizeLootboxRng(word)` into `rawFulfillRandomWords` for daily branch | |
| Drain only inside do-while | Remove L242-262 entirely; drain happens after rngGate at L335 | |
| Bump LR_INDEX at fulfillment | Restructure to bump LR_INDEX inside `_finalizeLootboxRng` | |
| Pre-finalize at L242 entry | Lazy slot-population at top of L242 block: revert if `rngWordCurrent==0`, else call `_finalizeLootboxRng` | |

**User's response:** Asked Claude to clarify backfill interaction concern and articulated invariant: "we need to skip this if the lootboxrngwordbyindex is not set (and revert if it needs to revert), but then run it before doing anything else if the rng came in and needs to be set."

**Notes:** User's articulation matches Pre-finalize-at-L242 verbatim. Claude flagged backfill semantic shift concern.

---

## Backfill Interaction (Round 3)

| Option | Description | Selected |
|--------|-------------|----------|
| Skip pre-finalize on gap days | Revert in gap-day case; let rngGate handle backfill+slot-write | |
| Adjust backfill loop bound | Change `_backfillOrphanedLootboxIndices` to start at idx-2 | |
| Accept the edge | Document semantic shift; in-flight slot gets raw word, no game impact | |

**User's response:** Pushed back on the framing — "lootbox index shouldn't be getting increased during a stall — why does backfill (which is for day index) matter at all?"

**Notes:** User's question prompted re-trace. Claude verified that LR_INDEX is bumped exactly ONCE per stall (rngLockedFlag prevents further daily requests; LR_MID_DAY blocks further mid-day requests; isRetry skips bump on timeout retry). Backfill's "loop" structure is overkill — only the in-flight slot ever needs filling. The "concern" dissolves because backfill no-op vs. raw-fill is cosmetic, not load-bearing.

---

## Fix Shape (Round 4 — Final)

| Option | Description | Selected |
|--------|-------------|----------|
| Eager: mirror at callback | Write lootbox slot in `rawFulfillRandomWords` daily branch + simple revert at L242 | |
| Lazy: pre-finalize at L242 | Slot-write stays in rngGate; lazy populate at L242 entry if rngWordCurrent has the word | ✓ |

**User's choice:** Lazy pre-finalize at L242.

**Notes:** Selection rationale — matches user's articulated invariant verbatim ("run it before doing anything else if the rng came in"); contained to AdvanceModule.sol drain-side; rawFulfillRandomWords callback untouched; smallest diff that fully resolves both Path 2 (daily-flow primary bug) and Path 4 (mid-day cross-day edge).

---

## rngWordCurrent Simplification (Mid-Discussion Exploration)

User asked: "could we get rid of rngcurrent and have the fulfill always set lootboxrngindex and have the lazy path set the day from that instead?"

| Option | Description | Selected |
|--------|-------------|----------|
| Keep current scope | Stay with locked lazy pre-finalize at L242 | ✓ |
| Expand 232.1 scope | Pursue full RNG state-machine refactor in this phase | |
| Hybrid: split roles, keep one | Eliminate Role 1 only; rename to `lastDailyFinalWord` for Role 2 | |

**User's choice:** Keep current scope.

**Notes:** Trace revealed `rngWordCurrent` has TWO roles — (1) in-flight VRF buffer, (2) post-nudge daily word persistent across multi-tx jackpot processing. Role 2 has cross-module consumers (`DegenerusGame.sol:850`, `DegenerusGame.sol:2175`, `DegenerusGameJackpotModule.sol:575-578`) that would need rerouting if eliminated. Out of SPEC.md §In Scope. Deferred to potential v30+ milestone or follow-up phase. Captured in CONTEXT.md §Deferred.

---

## L190-194 Mid-Day Guard Handling

| Option | Description | Selected |
|--------|-------------|----------|
| Leave it alone | Keep existing guard; minimum-diff; locally correct | ✓ |
| Extract `_ensureLootboxRngReady` helper | Single source of truth; two call sites | |
| Drop LR_MID_DAY pre-check | Apply helper unconditionally at both sites | (presented earlier; not voted on after scope-tight lock) |

**User's choice:** Leave it alone.

**Notes:** Mid-day fulfillment writes the slot atomically at L1632, so the existing `LR_MID_DAY != 0 && slot == 0` guard remains correct. Two slightly different guard shapes coexist (one mid-day, one daily) but each is locally correct for its branch. Matches scope-tight decision.

---

## Entropy Sourcing at L690 (Implicitly Resolved)

The structural fix at L242 makes `lootboxRngWordByIndex[LR_INDEX-1] == 0` unreachable at L690 by construction. No question presented; resolution captured directly in CONTEXT.md D-04.

**Implicit decision:** Storage read at L690 stays unchanged. No `processTicketBatch` signature change. No `IDegenerusGameModules` interface delta. No defensive guard at L690.

---

## Game-Over Path Scope (Implicitly Resolved)

Path-isolation verification (three independent grep + code-trace proofs) confirmed game-over path doesn't reach L242 drain or `processTicketBatch`. No question presented; resolution captured directly in CONTEXT.md D-05.

**Implicit decision:** Audit-only. SPEC acceptance criterion 3 satisfied vacuously. Plan should still produce a forge test that confirms the path-isolation claim by simulation.

---

## Closing Question

| Option | Description | Selected |
|--------|-------------|----------|
| I'm ready for context | Write CONTEXT.md, proceed to plan | ✓ |
| Test harness strategy | The 4th gray area skipped earlier | |
| Explore more gray areas | Surface additional implementation decisions | |

**User's choice:** Ready for context.

---

## Claude's Discretion

- Exact formatting / comment style of the L242 pre-finalize block.
- Forge test file location and naming (test/invariant/ vs test/unit/ vs test/integration/).
- Whether the binding-consistency forge test asserts via direct storage reads or captured event logs.
- Whether the sim-replay regression is a single Bash/Python script under `simulator/` or a Forge test.

## Deferred Ideas

- `rngWordCurrent` elimination (multi-module refactor) — future v30+ or Phase 232.2.
- `_backfillOrphanedLootboxIndices` simplification (loop reducible to single write) — out of SPEC scope; future cleanup.
- Test harness strategy detail — left to planner's discretion within Claude's Discretion bullets.
