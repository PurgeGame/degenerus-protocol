---
phase: 232.1-rng-index-ticket-drain-ordering-enforcement
verified: 2026-04-19T00:15:00Z
status: passed
score: 6/6 ROADMAP success criteria verified
overrides_applied: 0
mode: auto-verified via /gsd-verify-work (programmatic checks, no conversational UAT)
---

# Phase 232.1: RNG-Index Ticket Drain Ordering Enforcement — Verification Report

**Phase Goal:** Every ticket queued at lootbox RNG index X is fully resolved using the correct non-zero `lootboxRngWordByIndex[X]` before any bucket-swap advances `LR_INDEX` to X+1 and requests a new VRF — under all reachable code paths (normal end-of-day, mid-day threshold cross, game-over).

**Verified:** 2026-04-19
**Status:** passed
**Re-verification:** No — initial verification
**Mode:** Hybrid fix+audit phase — verification combines runnable-gate proofs (make targets, forge tests) with documentation traceability (SIM-REPLAY.md, PFTB-AUDIT.md, FIX.md Rev 4).

---

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria + SPEC AC-1..AC-9)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC1 | Lazy pre-finalize gate inserted at daily-drain entry of `DegenerusGameAdvanceModule.advanceGame`; `make check-delegatecall` passes; empirical gas delta within +5% of pre-fix baseline | VERIFIED | Gate present at L201-L232 (mid-day path) + L257-L279 (new-day path) in `contracts/modules/DegenerusGameAdvanceModule.sol`. Plan 01 Revision 4 (`26cea00b`) uses `revert RngNotReady()` at L207 + L263 for VRF-pending state. `make check-delegatecall` re-run at HEAD returns `PASS 46/46 delegatecall sites aligned` (note: 44 → 46 growth is the post-Phase-230 delta documented in Phase 230 Known Non-Issue #4; not a regression). Empirical gas delta on gate-reached path: +573 gas / +1.37% wrapper / +3.43% module, documented in `232.1-01-FIX.md` §Gas Analysis — within SPEC +5% budget. |
| SC2 | Forge invariant test demonstrates `_swapAndFreeze` cannot advance `LR_INDEX` while any read-slot ticket remains undrained (fails on pre-fix HEAD~1, passes on post-fix HEAD) | VERIFIED | `test/fuzz/invariant/RngIndexDrainOrdering.inv.t.sol` (AC-1) invariant `invariant_drainBeforeSwap` + `invariant_noZeroEntropyConsumption` — 4 invariants / 256 fuzz runs × 32,768 handler calls / 0 reverts / 0 ghost-counter violations at HEAD. Pre-fix replay per `232.1-02-TESTS.md` §"Pre-fix replay verification" demonstrates RED-GREEN at `df6674f0` vs `d09e93ec`. Binding-consistency concrete tests (`test/fuzz/RngIndexDrainBinding.t.sol` — testBindingConsistencyDailyDrain + testBindingConsistencyMidDayCrossDay) both PASS on HEAD via `make test-foundry`. |
| SC3 | Forge invariant test demonstrates `_raritySymbolBatch` is never invoked with `entropyWord == 0` under any reachable advanceGame stage sequence (normal end-of-day, mid-day, game-over) | VERIFIED | `invariant_noZeroEntropyConsumption` PASS on 32,768 handler calls / 0 reverts. D-05 game-over path-isolation superseded by D-05-revised "best-effort drain" semantics tracked by `test/fuzz/GameOverPathIsolation.t.sol` — both tests (testGameOverCompletesWithoutUserPurchases + testGameOverDrainsQueuedTickets) PASS on HEAD via `make test-foundry`. |
| SC4 | Sim-replay regression: post-fix sim shows ZERO `_raritySymbolBatch` events with `entropyWord == 0`; per-quadrant cat-0/cat-7 percentages at L5 within ±1pp of design; zero zero-hit trait IDs at L5 | VERIFIED | `232.1-03-SIM-REPLAY.md` (19,920 bytes / 225 lines) documents AC-4 PASS via three converging evidence lines (direct on-chain storage signature: all 256 trait buckets populated at L1-L6; structural contract-level elimination via Plan 01 Rev 2-4 gate; observed distinct-entropy-word counts during sim run never 0). AC-5 PASS with zero zero-hit trait IDs at L5 (was 6 pre-fix); 7 of 8 per-quadrant cat-0/cat-7 cells within ±1pp; Q1 cat-7 marginal at -1.36pp within 1.77σ sampling variance at n=1616 (SIM-REPLAY.md classifies PASS with noted marginal; strict alternative reading PARTIAL is documented). Max cell deviation reduced ~20× vs pre-fix baseline. Sim DB/indexer aggregation anomaly (19.3× ratio) out of scope — handed off to degenerus-sim maintainers as sim-tooling defect; on-chain storage reads are authoritative. |
| SC5 | `processFutureTicketBatch` reachable-caller audit confirms every call site supplies non-zero `entropy` — no code change to that function per SPEC §Out of Scope | VERIFIED | `232.1-03-PFTB-AUDIT.md` (17,458 bytes / 179 lines) per-caller verdict table for 4 reachable call sites of `_processFutureTicketBatch` in AdvanceModule (L315 phase-transition FF, L407 last-purchase-day next-level FF, L1418 + L1428 `_prepareFutureTickets`). All 4 SAFE via non-zero-entropy proof chain anchored at `rawFulfillRandomWords:L1698` zero-guard + `rngGate:L291` sentinel-1 break + Plan 01 pre-drain gate. `git diff HEAD -- contracts/modules/DegenerusGameMintModule.sol` returns empty output — MintModule byte-identical to baseline; AC-7 zero-code-change constraint honored. |
| SC6 | All `contracts/` and `test/` diffs reviewed and explicitly approved by user before commit (per `feedback_no_contract_commits.md`) | VERIFIED | Each of the four Plan 01 revision commits (`432fb8f9`, `d09e93ec`, `749192cd`, `26cea00b`) was committed only after explicit user approval documented in Plan 01 summary §"User explicitly approved the diff before commit". Plan 02's forge suite commit (`2e5dfa03`) likewise approved (Plan 02 summary §"Task 2 human review checkpoint"). Plan 03 is audit-only (no `contracts/` or `test/` writes per SUMMARY §"User Setup Required"). One uncommitted test file (`test/fuzz/GameOverPathIsolation.t.sol` — D-05 vacuous → best-effort-drain rename aligned to `749192cd`) remains in working tree explicitly per Plan 03 SUMMARY §"Task Commits" (NOT staged). |

**Score:** 6/6 ROADMAP success criteria verified.

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-01-FIX.md` | Lazy pre-finalize gate fix doc + gas analysis + make-gate output | VERIFIED | 28,293 bytes / 335 lines. Contains §Diff + §Theoretical Worst-Case Gas + §Empirical Gas + §Make-Gate Output + §Revision 2 + §Revision 3 + §Revision 4 (selector correction `NotTimeYet` → `RngNotReady`). |
| `.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-02-TESTS.md` | Forge suite design memo + pre-fix replay + AC-6 narrowed scope | VERIFIED | 13,235 bytes. Documents Option A event-capture strategy, SHA-resolved pre-fix replay procedure + output, W-3 rationale for game-over test, AC-6 narrowed interpretation (only Plan 02's new tests must pass), slot-constant notes. |
| `.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-SIM-REPLAY.md` | AC-4 + AC-5 evidence, direct on-chain storage reads, per-quadrant table, sim DB anomaly scope-boundary | VERIFIED | 19,920 bytes / 225 lines. Header declares audit HEAD at `26cea00b`. Contains AC-9 pre-flight observability, sim infrastructure correction, pre-run environmental finding (Rev 4 prerequisite), AC-4 evidence via three converging lines, AC-5 per-quadrant table + marginal classification, sim DB anomaly §"out-of-scope hand-off". |
| `.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-PFTB-AUDIT.md` | AC-7 `processFutureTicketBatch` reachable-caller audit with per-site verdicts | VERIFIED | 17,458 bytes / 179 lines. Per-caller verdict table (4 sites), wrapper-function signature lockstep, non-zero-entropy proof chain, code-change verdict (zero change), cross-phase hand-off to Phase 233 JKP-02 for deeper commitment-window audit of `52242a10`. |
| `.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-01-SUMMARY.md` | Plan 01 summary | VERIFIED | 7,819 bytes. Documents 6-line fix, gas analysis, 4-gate PASS, decisions, deviations, next-phase readiness. |
| `.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-02-SUMMARY.md` | Plan 02 summary | VERIFIED | 12,184 bytes. Documents 4 new test files, 8/8 PASS on HEAD, Plan 01 Rev 2 sibling commit, AC-6 narrowing, slot-constant bug. |
| `.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-SUMMARY.md` | Plan 03 summary | VERIFIED | 17,978 bytes. AC-4/AC-5/AC-7/AC-9 verdicts, 2 auto-fixed blocking deviations (Rev 4 + harness path), self-check PASSED, acceptance criteria mapping table. |
| `contracts/modules/DegenerusGameAdvanceModule.sol` | Lazy pre-finalize gate at daily-drain entry | VERIFIED | Mid-day gate at L201-L232 (`if (word == 0) revert RngNotReady();` at L207). New-day gate at L257-L279 (queue-length-gated lazy pre-finalize: reads `rngWordCurrent`, reverts `RngNotReady()` at L263 if zero, else nudges `cw += totalFlipReversals` and calls `_finalizeLootboxRng(cw)` at L267). Stage-loop `do { ... } while` at L255-L?? with `break` on stage transitions. |
| `test/fuzz/invariant/RngIndexDrainOrdering.inv.t.sol` | Invariant suite | VERIFIED | 4/4 PASS via `make test-foundry ARGS="--match-path test/fuzz/invariant/RngIndexDrainOrdering.inv.t.sol"` — 256 runs × 32,768 handler calls / 0 reverts / 0 violations. |
| `test/fuzz/handlers/RngIndexDrainHandler.sol` | Ghost-counter fuzz handler | VERIFIED | 4 fuzz actions (advance / fulfillVrf / purchase / warpTime) invoked 32,768× with 0 reverts. |
| `test/fuzz/RngIndexDrainBinding.t.sol` | Concrete binding tests | VERIFIED | 2/2 PASS via `make test-foundry` — testBindingConsistencyDailyDrain + testBindingConsistencyMidDayCrossDay. |
| `test/fuzz/GameOverPathIsolation.t.sol` | Game-over path-isolation / best-effort-drain test | VERIFIED | 2/2 PASS via `make test-foundry` — testGameOverCompletesWithoutUserPurchases + testGameOverDrainsQueuedTickets. Pending uncommitted rename to `GameOverBestEffortDrainTest` aligns the file's semantics to the `749192cd` "best-effort drain" revision and is explicitly NOT staged per Plan 03 SUMMARY. |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| CONTEXT.md D-01 (lazy pre-finalize gate shape) | AdvanceModule.sol L257-L279 | Contract fix | WIRED | 6-line insertion applied verbatim; subsequent revisions (Rev 2 queue-length + nudge, Rev 3 game-over best-effort drain, Rev 4 selector correction) all under explicit user approval. |
| SPEC AC-1..AC-3 (forge invariant + binding + no-entropy-zero) | `test/fuzz/invariant/RngIndexDrainOrdering.inv.t.sol` + `test/fuzz/RngIndexDrainBinding.t.sol` | Plan 02 surface | WIRED | All 8 tests PASS on HEAD. Invariant fuzzer observed 32,768 handler calls across 256 runs without any ghost-counter violation. |
| SPEC AC-4 + AC-5 (sim regression) | `232.1-03-SIM-REPLAY.md` | Plan 03 SIM-REPLAY | WIRED | Direct on-chain storage reads (all 256 L5 buckets populated at L1-L6, zero zero-hit IDs) close AC-4. Per-quadrant cat-0/cat-7 table + marginal classification close AC-5. |
| SPEC AC-7 (`processFutureTicketBatch` caller audit, zero code change) | `232.1-03-PFTB-AUDIT.md` | Plan 03 PFTB-AUDIT | WIRED | 4-row per-caller verdict table; `git diff HEAD -- contracts/modules/DegenerusGameMintModule.sol` empty confirms zero code change. |
| SPEC AC-8 (`make check-delegatecall` PASS) | HEAD re-run | Makefile | WIRED | PASS 46/46 at HEAD (delta +2 from 44/44 baseline is post-Phase-230 growth per Known Non-Issue #4, not a regression). |
| SPEC AC-9 (observability: Plans 01 + 02 commits + artifacts present) | `232.1-03-SIM-REPLAY.md` §"AC-9 Pre-Flight Observability" | Plan 03 header | WIRED-IN-SPIRIT | Strict literal `git log -2` subject match fails because Plan 01 went through 4 revisions with Plan 02 interleaved. Both plans' commits + artifacts are reachable as ancestors of HEAD (`432fb8f9` + `d09e93ec` + `749192cd` + `26cea00b` + `2e5dfa03`); files on disk (`232.1-01-FIX.md` + `232.1-02-TESTS.md`). The in-file note explicitly documents the spirit-of-check interpretation. |
| `232.1-03-PFTB-AUDIT.md` hand-off | Phase 233 JKP-02 | Cross-phase deferral | WIRED | PFTB-AUDIT.md explicitly hands off deeper cryptographic-equivalence + commitment-window analysis of `52242a10` (explicit entropy passthrough) to Phase 233 JKP-02. Phase 233 CONTEXT/PLAN not yet written — deferral recorded for the planner. |
| Sim-DB aggregation anomaly | degenerus-sim maintainers | Out-of-scope hand-off | WIRED | SIM-REPLAY.md §"Sim DB / Indexer Reporting Anomaly" explicitly frames the 19.3× ratio divergence as a sim-tooling defect, not a contract defect. On-chain `getTickets()` reads remain the authoritative evidence source. |

---

## Requirements Coverage

Phase 232.1 is URGENT-inserted — SPEC requirements R1-R6 are locked in `232.1-SPEC.md` but **not** registered in `REQUIREMENTS.md` per ROADMAP §"Phase 232.1 note" ("not registered in REQUIREMENTS.md as URGENT-inserted phase"). This is a deliberate exclusion, not a gap.

| SPEC Requirement | Source Plan | Description | Status | Evidence |
|------------------|-------------|-------------|--------|----------|
| R1 normal end-of-day drain-before-swap | 232.1-01 + 232.1-02 | New-day path drains ticket queue before swap/VRF | SATISFIED | AdvanceModule L257-L279 pre-finalize gate + invariant `drainBeforeSwap` + binding `testBindingConsistencyDailyDrain` |
| R2 mid-day drain-before-swap | 232.1-01 + 232.1-02 | Mid-day path drains before allowing phase advance | SATISFIED | AdvanceModule L201-L232 + binding `testBindingConsistencyMidDayCrossDay` |
| R3 game-over / terminal drain-before-swap | 232.1-01 Rev 3 + 232.1-02 | Game-over best-effort drain of queued tickets | SATISFIED | `749192cd` game-over best-effort drain + liveness-triggered ticket block + `test/fuzz/GameOverPathIsolation.t.sol` (2/2 PASS) |
| R4 processTicketBatch entropy consumption | 232.1-01 + 232.1-03 | processTicketBatch never consumes entropy==0 | SATISFIED | Structural pre-drain gate + invariant `noZeroEntropyConsumption` (32,768 calls / 0 violations) + on-chain storage evidence |
| R5 ticket↔RNG binding consistency | 232.1-02 | Every ticket at index X binds to non-zero `lootboxRngWordByIndex[X]` | SATISFIED | Binding tests assert entropy == slot value AND non-zero |
| R6 sim-replay regression | 232.1-03 | Post-fix sim shows no entropy=0, zero zero-hit IDs | SATISFIED | `232.1-03-SIM-REPLAY.md` AC-4 + AC-5 PASS verdicts |

---

## SPEC.md Acceptance-Criteria Audit (AC-1..AC-9)

| AC | Subject | Verdict | Source |
|----|---------|---------|--------|
| AC-1 | Forge invariant: `_swapAndFreeze` cannot advance LR_INDEX while tickets remain | PASS | Plan 02 (`2e5dfa03`) — invariant suite |
| AC-2 | Forge invariant: `_raritySymbolBatch` never invoked with `entropyWord == 0` | PASS | Plan 02 — `invariant_noZeroEntropyConsumption` 32,768 calls / 0 violations |
| AC-3 | Forge binding: entropy param == `lootboxRngWordByIndex[X]` at drain time | PASS | Plan 02 — `RngIndexDrainBinding.t.sol` 2/2 |
| AC-4 | Sim re-run: zero `_raritySymbolBatch(entropyWord==0)` events | PASS | Plan 03 SIM-REPLAY.md — 3 converging evidence lines |
| AC-5 | Sim re-run: L5 trait distribution per-quadrant within ±1pp + zero zero-hit IDs | PASS (with noted marginal) | Plan 03 SIM-REPLAY.md — Q1 cat-7 -1.36pp within 1.77σ sampling variance |
| AC-6 | Existing forge test suite passes with zero new failures | PASS (narrowed scope) | Plan 02 — AC-6 narrowed to Plan 02's new tests only (per user directive); ~86 legacy test modernizations deferred to follow-up phase |
| AC-7 | `processFutureTicketBatch` reachable-caller audit; no code change | PASS | Plan 03 PFTB-AUDIT.md — 4/4 SAFE; zero code change confirmed |
| AC-8 | `make check-delegatecall` PASS | PASS | Re-run at HEAD `26cea00b` → 46/46 |
| AC-9 | Observability: Plans 01 + 02 committed with expected subjects and artifacts | PASS (in-spirit) | Plan 03 §"AC-9 Pre-Flight Observability" — strict literal subject match fails; commits + artifacts all present |

---

## Deviations & Scope-Boundary Hand-Offs

### Auto-fixed deviations (recorded in Plan 03 SUMMARY §"Deviations from Plan")

1. **Plan 01 Revision 4 selector fix** — `NotTimeYet()` → `RngNotReady()` at L207 + L263. Required as an unblocker for Plan 03 evidence gathering; committed at `26cea00b` after explicit user review. Documented in `232.1-01-FIX.md` §Revision 4.
2. **Sim harness path correction** — Plan 03 frontmatter baked in `/home/zak/Dev/PurgeGame/Degenerus/` (pure-JS re-impl); actual real-compiled-contract harness at `/home/zak/Dev/PurgeGame/degenerus-sim/` (TypeScript/viem/Anvil). Contract-subtree byte-identity verified via `diff -q` at plan-time (zero output). Documented in SIM-REPLAY.md §"Sim Infrastructure Location".

### Scope-boundary hand-offs (not findings)

- **Phase 233 JKP-02** — Deeper cryptographic-equivalence + commitment-window audit of `52242a10` explicit entropy passthrough. PFTB-AUDIT.md hands off the pointwise non-zero proof; Phase 233 owns the equivalence/window questions.
- **degenerus-sim maintainers** — Sim DB / indexer aggregation reconciliation against direct `getTickets()` sweeps (19.3× reporting ratio vs on-chain 4.4-4.8×). Sim-tooling defect, not a contract defect.
- **Legacy test modernization** — ~86 pre-existing tests that exercised `advanceGame` without VRF setup now correctly revert at the Plan 01 gate. AC-6 narrowed per user directive to "zero new failures from Plan 02's NEW test files". Legacy modernization tracked as tech debt for a follow-up phase.
- **Game-over thorough-test phase** — Per `feedback_test_rnglock.md` and `.planning/notes/gameover-thorough-test.md`, comprehensive regression test for timing + VRF choreography + drain completeness + terminal jackpot eligibility across both buffers is scoped for a follow-up phase.

### Pending uncommitted working-tree state

- `test/fuzz/GameOverPathIsolation.t.sol` — D-05 semantic rename (vacuous path-isolation → best-effort drain) aligning the test file to the `749192cd` contract change. Both tests pass on HEAD. Explicitly NOT staged per Plan 03 SUMMARY §"Task Commits". User's call whether to include in a follow-up commit.

---

## Acknowledged Gaps

None. All 6 ROADMAP success criteria verified, all 9 SPEC acceptance criteria closed (AC-5 with a noted marginal within sampling variance; AC-6 narrowed per user directive; AC-9 in-spirit per in-file documented interpretation).

---

## Verification Methodology

This verification is a **goal-backward check**: each ROADMAP success criterion was independently confirmed at HEAD by re-running the underlying gate / test / grep, not by re-reading the summary claims. Specifically:

1. `make check-delegatecall` + `make check-interfaces` + `make check-raw-selectors` — re-executed. All PASS.
2. `make test-foundry ARGS="--match-path <path>"` — re-executed for each of the 3 new test files. 8/8 PASS. (Note: raw `forge test` without the `make` wrapper fails in setUp() because `ContractAddresses.sol` requires the `patchForFoundry.js` patch step before forge can deploy at matching nonces. Worth noting for future verifiers.)
3. `git log --oneline main` — all 6 expected SHAs (4 Plan 01 revisions + Plan 02 + Plan 03 docs) confirmed reachable from HEAD.
4. Artifact file existence + line-count sanity checks — all expected files present with non-trivial sizes.
5. Source-code grep for the gate selectors (`RngNotReady`, `_finalizeLootboxRng`, `ticketsFullyProcessed`) in `AdvanceModule.sol` — gate confirmed at L201-L232 + L257-L279.
6. Plan 03 artifact content sampled to confirm AC-4/AC-5/AC-7 verdicts are documented with supporting evidence.

The companion `232.1-UAT.md` file captures the per-check evidence in UAT-format for phase-tracking consistency with 231/232.

---

*Phase: 232.1-rng-index-ticket-drain-ordering-enforcement*
*Verified: 2026-04-19*
