---
phase: 15-ticket-creation-midday-rng
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified: [audit/v1.2-ticket-rng-deep-dive.md]
autonomous: true
requirements: [TICKET-02, TICKET-04]

must_haves:
  truths:
    - "Mid-day requestLootboxRng trigger conditions and atomic buffer swap are traced"
    - "VRF callback routing (lootbox path) through lootboxRngWordByIndex is traced"
    - "advanceGame mid-day drain path reading lastLootboxRngWord and running processTicketBatch is traced"
    - "Manipulation resistance of mid-day flow is verified with explicit reasoning"
    - "_coinflipLockedDuringTransition 5-condition guard is fully analyzed"
    - "Gap analysis identifies periods where coinflip is unlocked but RNG is sensitive"
    - "Alignment verdict provided for coinflip lock timing vs RNG-sensitive periods"
  artifacts:
    - path: "audit/v1.2-ticket-rng-deep-dive.md"
      provides: "Sections 2 and 4 of ticket RNG deep-dive audit"
      contains: "Section 2: Mid-Day RNG Flow"
  key_links:
    - from: "audit/v1.2-ticket-rng-deep-dive.md Section 2"
      to: "contracts/modules/DegenerusGameAdvanceModule.sol"
      via: "requestLootboxRng, rawFulfillRandomWords, advanceGame mid-day drain code references"
      pattern: "requestLootboxRng.*AdvanceModule:673"
    - from: "audit/v1.2-ticket-rng-deep-dive.md Section 4"
      to: "contracts/BurnieCoinflip.sol"
      via: "_coinflipLockedDuringTransition and claim guard analysis"
      pattern: "_coinflipLockedDuringTransition.*BurnieCoinflip:1032"
---

<objective>
Trace the mid-day RNG flow from requestLootboxRng through VRF callback to processTicketBatch drain (Section 2) and verify coinflip lock timing alignment with RNG-sensitive periods (Section 4), producing manipulation resistance and alignment verdicts.

Purpose: TICKET-02 requires the mid-day flow verified for manipulation resistance with explicit reasoning. TICKET-04 requires coinflip lock windows verified to align with RNG-sensitive periods, with gap analysis.
Output: Sections 2 and 4 appended to `audit/v1.2-ticket-rng-deep-dive.md`.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-ticket-creation-midday-rng/15-01-SUMMARY.md (Plan 01 output -- Sections 1 and 3 already written)

@audit/v1.2-rng-data-flow.md (Phase 12 -- Section 3 mid-day ticket flow lifecycle)
@audit/v1.2-manipulation-windows.md (Phase 14 -- L4 processTicketBatch window, inter-block gap analysis)
@audit/v1.2-ticket-rng-deep-dive.md (Plan 01 output -- Sections 1 and 3)

Primary contract sources to trace:
@contracts/modules/DegenerusGameAdvanceModule.sol (lines 155-196 for mid-day drain, lines 673-735 for requestLootboxRng, lines 1326-1345 for VRF callback routing, lines 785-789 for _finalizeLootboxRng)
@contracts/BurnieCoinflip.sol (lines 248-258 for _depositCoinflip, lines 336-367 for claim guards with rngLocked, lines 1032-1044 for _coinflipLockedDuringTransition)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Section 2 -- Mid-Day RNG Flow Manipulation Resistance (TICKET-02)</name>
  <files>audit/v1.2-ticket-rng-deep-dive.md</files>
  <action>
Append Section 2 to `audit/v1.2-ticket-rng-deep-dive.md` (after Section 1, before Section 3).

Structure Section 2 with these subsections:
- **2a: requestLootboxRng Trigger and Buffer Swap** -- Trace AdvanceModule:673-735. Document trigger conditions: called by anyone when tickets exist in write buffer AND read buffer is fully processed. The function: (1) checks ticketQueue[writeKey].length > 0 && ticketsFullyProcessed, (2) calls _swapTicketSlot to freeze write->read ATOMICALLY before VRF request, (3) sets midDayTicketRngPending = true, (4) sends VRF request with VRF_MIDDAY_CONFIRMATIONS = 3. Key point: buffer swap happens BEFORE VRF request, so any tickets purchased after this call go to the NEW write buffer and cannot be resolved by the incoming VRF word.

- **2b: VRF Callback Routing (Lootbox Path)** -- Trace rawFulfillRandomWords at AdvanceModule:1326-1345. Document how the callback distinguishes daily vs lootbox path (requestId matching), writes word to `lootboxRngWordByIndex[index]`, and does NOT immediately trigger processTicketBatch. The word sits in storage until advanceGame is called.

- **2c: advanceGame Mid-Day Drain** -- Trace AdvanceModule:155-196. Document the same-day path: checks `!ticketsFullyProcessed && midDayTicketRngPending`, reads `lootboxRngWordByIndex[lootboxRngIndex - 1]`, reverts with NotTimeYet if word == 0 (VRF not arrived), writes to `lastLootboxRngWord`, then calls `_runProcessTicketBatch` which reads lastLootboxRngWord via processTicketBatch. Document that midDayTicketRngPending is cleared after processing completes.

- **2d: Manipulation Resistance Analysis** -- Provide explicit reasoning for why this flow is manipulation-resistant:
  1. Buffer swap is atomic with VRF request -- no window to add/remove tickets after swap but before VRF
  2. VRF word is cryptographically unpredictable at swap time (3 confirmations, proof verified on-chain)
  3. VRF callback only writes to storage -- no trait assignment in callback, so callback block builder cannot influence outcomes
  4. processTicketBatch reads from frozen read buffer -- cannot be modified between VRF arrival and consumption
  5. Cite Phase 14 L4 BLOCKED verdict as supporting evidence
  State explicit verdict: SAFE -- mid-day flow provides equivalent manipulation resistance to daily flow through structural isolation (buffer swap before VRF) and VRF unpredictability.

Include exact Solidity line numbers for every claim.
  </action>
  <verify>
    <automated>grep -c "Section 2" audit/v1.2-ticket-rng-deep-dive.md && grep -c "requestLootboxRng" audit/v1.2-ticket-rng-deep-dive.md && grep -c "midDayTicketRngPending" audit/v1.2-ticket-rng-deep-dive.md && grep -c "manipulation" audit/v1.2-ticket-rng-deep-dive.md</automated>
  </verify>
  <done>Section 2 traces the complete mid-day flow: requestLootboxRng trigger with atomic buffer swap, VRF callback word storage, advanceGame drain reading lastLootboxRngWord and running processTicketBatch. Manipulation resistance explicitly reasoned through 5 structural arguments. All claims cite exact Solidity line numbers.</done>
</task>

<task type="auto">
  <name>Task 2: Section 4 -- Coinflip Lock Timing Gap Analysis (TICKET-04)</name>
  <files>audit/v1.2-ticket-rng-deep-dive.md</files>
  <action>
Append Section 4 to `audit/v1.2-ticket-rng-deep-dive.md` (after Section 3).

Structure Section 4 with these subsections:
- **4a: _coinflipLockedDuringTransition Condition Analysis** -- Enumerate all 5 conditions at BurnieCoinflip:1032-1044: `!inJackpotPhase`, `!gameOver`, `lastPurchaseDay`, `rngLocked`, `purchaseLevel % 10 == 0`. Explain what each condition means and when it is true. Emphasize this is a 5-way AND -- ALL must hold for the lock to fire. This guard only applies to `_depositCoinflip` (BurnieCoinflip:248-258).

- **4b: RNG-Sensitive Period Enumeration** -- List all periods when RNG state is sensitive (VRF in-flight or pending consumption):
  1. Daily VRF in-flight: rngLockedFlag = true, from _requestRng to rawFulfillRandomWords callback
  2. Mid-day VRF in-flight: after requestLootboxRng sends VRF request, until callback writes word
  3. Post-callback pre-consumption: VRF word in storage but processTicketBatch not yet called (mid-day drain window)
  4. Jackpot phase gaps: inter-block periods during 5-day jackpot sequence

- **4c: Gap Analysis** -- Identify periods where coinflip is UNLOCKED but RNG is SENSITIVE:
  Gap 1: Non-x10 levels with rngLocked=true on lastPurchaseDay -- `_coinflipLockedDuringTransition` does not fire because `purchaseLevel % 10 != 0`. However, coinflip CLAIM functions (BurnieCoinflip:336/347/357/367) independently check `rngLocked()` and revert. Coinflip DEPOSITS are allowed. Trace the deposit path: `_depositCoinflip` triggers `_claimCoinflipsInternal` (auto-claim), which reads `coinflipDayResult[epoch]` -- these results are ALREADY DETERMINED from a prior VRF word. The deposit itself writes to player state for FUTURE epochs. Verdict: deposit during this gap cannot influence any RNG outcome.
  Gap 2: During jackpot phase -- `_coinflipLockedDuringTransition` does not fire because `!inJackpotPhase` is false. But coinflip claims still blocked by `rngLocked()` check during jackpot VRF requests. Deposits allowed -- same analysis as Gap 1 (auto-claim reads determined results, deposit writes future state). Cross-reference Phase 13-03 decision: "Coinflip deposits during jackpot phase gap safe -- BURNIE-only, no pool/RNG interaction."
  Gap 3: Mid-day VRF in-flight on non-lastPurchaseDay -- `_coinflipLockedDuringTransition` does not fire (lastPurchaseDay = false). But mid-day VRF only affects lootbox RNG (ticket traits), not coinflip results. Coinflip results are determined by daily VRF, not lootbox VRF. No interaction.

- **4d: Alignment Verdict** -- State explicit verdict. Expected: ALIGNED WITH ACCEPTABLE GAPS. The `_coinflipLockedDuringTransition` guard protects the highest-risk window (BAF-eligible transition day with daily VRF in-flight). The gaps identified in 4c are all safe because: (1) coinflip claims have independent `rngLocked()` guards, (2) coinflip deposits only trigger auto-claims on already-determined results, (3) coinflip RNG (daily VRF) is independent from ticket RNG (lootbox VRF). The 5-condition AND is intentionally narrow to avoid unnecessary UX friction.

Include exact Solidity line numbers for every claim. Distinguish clearly between deposit locks and claim locks (Pitfall 4 from RESEARCH.md).
  </action>
  <verify>
    <automated>grep -c "Section 4" audit/v1.2-ticket-rng-deep-dive.md && grep -c "_coinflipLockedDuringTransition" audit/v1.2-ticket-rng-deep-dive.md && grep -c "Gap" audit/v1.2-ticket-rng-deep-dive.md && grep -c "ALIGNED\|verdict" audit/v1.2-ticket-rng-deep-dive.md</automated>
  </verify>
  <done>Section 4 analyzes all 5 conditions of _coinflipLockedDuringTransition, enumerates 4 RNG-sensitive periods, identifies 3 gaps where coinflip is unlocked during RNG sensitivity, and provides an alignment verdict. Deposit locks vs claim locks are clearly distinguished. All gaps assessed as safe with reasoning.</done>
</task>

</tasks>

<verification>
- Section 2 traces mid-day flow: requestLootboxRng -> buffer swap -> VRF callback -> advanceGame drain -> processTicketBatch
- Section 2 provides explicit manipulation resistance reasoning with 5 structural arguments
- Section 4 enumerates all 5 _coinflipLockedDuringTransition conditions
- Section 4 identifies gaps between coinflip lock and RNG-sensitive periods with per-gap verdicts
- Section 4 clearly distinguishes deposit locks from claim locks
- All claims cite exact contract file and line number
</verification>

<success_criteria>
TICKET-02 satisfied: mid-day requestLootboxRng -> buffer swap -> processTicketBatch flow verified for manipulation resistance with explicit reasoning.
TICKET-04 satisfied: coinflip lock timing verified with gap analysis showing alignment (or safe gaps) with RNG-sensitive periods.
</success_criteria>

<output>
After completion, create `.planning/phases/15-ticket-creation-midday-rng/15-02-SUMMARY.md`
</output>
