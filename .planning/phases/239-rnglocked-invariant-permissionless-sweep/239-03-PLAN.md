---
phase: 239-rnglocked-invariant-permissionless-sweep
plan: 239-03
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v30-ASYMMETRY-RE-JUSTIFICATION.md
  - .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-03-SUMMARY.md
autonomous: true
requirements: [RNG-03]
requirements_addressed: [RNG-03]
head_anchor: 7ab515fe
head_sha: 7ab515fe
must_haves:
  truths:
    - "`audit/v30-ASYMMETRY-RE-JUSTIFICATION.md` exists with exactly these top-level sections in this exact order per D-13/D-23: `## Executive Summary`, `## § Asymmetry A — Lootbox RNG Index-Advance Isolation Equivalent to Flag-Based Isolation`, `## § Asymmetry B — phaseTransitionActive Exemption Admits Only advanceGame-Origin Writes`, `## Prior-Artifact Cross-Cites`, `## Finding Candidates`, `## Scope-Guard Deferrals`, `## Attestation`."
    - "Per D-13 § Asymmetry A contains at minimum these sub-sections: `### Storage Primitives` (enumerating `lootboxRngIndex` storage slot + declaration site + `lootboxRngWordByIndex` mapping slot + declaration site at HEAD 7ab515fe), `### Write Sites` (every SSTORE to `lootboxRngIndex` + every SSTORE to `lootboxRngWordByIndex[...]` at HEAD), `### Read Sites` (every consumer-side SLOAD reading `lootboxRngWordByIndex[consumerIndex]`), `### Equivalence Proof` (closed-form argument from primitives that `∀ lootbox consumer with frozen consumerIndex = k, the read lootboxRngWordByIndex[k] at fulfillment returns either (a) the uninitialized zero word OR (b) the VRF-delivered word for index k written atomically by rawFulfillRandomWords; no player, admin, or validator can write lootboxRngWordByIndex[k] after index advance past k` — concludes the invariant is equivalent-in-freeze-guarantee to `rngLockedFlag` for the lootbox path)."
    - "Per D-13 § Asymmetry B contains at minimum these sub-sections: `### Storage Primitives` (`phaseTransitionActive` storage slot + declaration site + set/clear sites at HEAD 7ab515fe), `### Enumerated SSTORE Sites Under phaseTransitionActive = true` (every SSTORE reachable while the flag is set — this is the proof-by-exhaustion core per D-14), `### Call-Chain Rooting Proof` (argument that every such SSTORE's call chain roots at `advanceGame` — no external entry point can toggle `phaseTransitionActive = true` without first entering `advanceGame`), `### No Player-Reachable Mutation-Path Proof` (argument that no player-callable function can reach any enumerated SSTORE under `phaseTransitionActive = true`)."
    - "Per D-14 both Asymmetry proofs are PROOF-BY-EXHAUSTION from storage primitives at HEAD `7ab515fe` — NOT proof-by-cite. Every claim enumerates specific storage slots + SSTORE sites + call chains at HEAD. Prior-milestone artifacts (v29.0 Phase 235 D-13 Path 4, KNOWN-ISSUES.md lootbox index-advance entry, Phase 232.1-03-PFTB-AUDIT.md) CROSS-CITED as `we independently re-derived the same result` notes only — NOT as warrant or sole basis."
    - "Per D-14 KNOWN-ISSUES.md entry `Lootbox RNG uses index advance isolation instead of rngLockedFlag` is explicitly named as the SUBJECT of Asymmetry A (the design decision being re-justified from first principles), NOT its warrant. The proof is independent of the KI entry's existence."
    - "Per D-16/D-17 `## Prior-Artifact Cross-Cites` section lists at minimum: v29.0 Phase 235 D-13 Path 4 `235-05-TRNX-01.md` (4-path walk — Asymmetry B corroborating via phaseTransitionActive branch treatment); Phase 232.1-03-PFTB-AUDIT.md (non-zero-entropy + semantic-path-gate archetypes — both Asymmetry corroborating); v25.0 Phase 215 RNG fresh-eyes SOUND verdict (structural baseline); KNOWN-ISSUES.md `Lootbox RNG uses index advance isolation instead of rngLockedFlag` entry (Asymmetry A SUBJECT, not warrant). Every cite carries `re-verified at HEAD 7ab515fe` note with `we independently re-derived same result` statement (D-14 format, not load-bearing-reliance format)."
    - "Per D-22 NO `F-30-NN` finding IDs appear anywhere in `audit/v30-ASYMMETRY-RE-JUSTIFICATION.md`. Phase 242 FIND-01..03 owns ID assignment. `grep -E 'F-30-[0-9]' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md` returns zero matches."
    - "Per D-23 the `## Finding Candidates` appendix lists any observations that arose during asymmetry re-derivation that do not fit the CLEAN verdict. Structure each as `### CANDIDATE_FINDING #N (— surfaced in 239-03 RNG-03)` with `SECTION_REF` (Asymmetry A or B) + `FILE:LINE` citation + `SEVERITY: TBD-242` + observation text. If zero: state `**None surfaced.**`."
    - "Per D-27 READ-only scope holds: `git diff --name-only 7ab515fe..HEAD -- contracts/ test/` empty at task start and end; `git status --porcelain contracts/ test/` empty before and after each task; `KNOWN-ISSUES.md` not edited."
    - "Per D-26 HEAD anchor `7ab515fe` locked in frontmatter + echoed in `audit/v30-ASYMMETRY-RE-JUSTIFICATION.md` `Audit baseline` line + in plan SUMMARY frontmatter + in Attestation section."
    - "Per D-28 if any consumer or SSTORE site involved in the asymmetries is discovered that is NOT in `audit/v30-CONSUMER-INVENTORY.md` Universe List — inventory NOT edited; scope-guard deferral records `file:line + context + proposed inventory delta + SEVERITY: TBD-242` routes to Phase 242 FIND-01."
    - "Per D-29 this plan commit DISCHARGES Phase 238-03 FWD-03 gating `lootbox-index-advance` audit assumption (Scope-Guard Deferral #1 secondary entry in `audit/v30-FREEZE-PROOF.md` — the 238-03 citation split the audit assumption into rngLocked (discharged by Plan 239-01) + lootbox-index-advance (discharged by Plan 239-03)). Discharge evidenced by Plan 239-03 commit presence — no re-edit of Phase 238 files. Phase 242 REG-01/02 cross-checks discharge at milestone consolidation. Discharge note appears explicitly in this plan's objective AND in § Asymmetry A's Equivalence Proof sub-section AND in the SUMMARY's dedicated Phase 238 Discharge Note section."
    - "§ Asymmetry A Write Sites sub-section row count ≥ grep count of `lootboxRngIndex` / `lootboxRngWordByIndex` SSTOREs in `contracts/` at HEAD (excluding mocks). § Asymmetry B Enumerated SSTORE Sites sub-section row count ≥ grep count of SSTOREs reachable under `phaseTransitionActive = true` (must be enumerated via control-flow walk from `phaseTransitionActive = true` set site to subsequent clear site)."
    - "All tasks commit with subjects matching `docs(239-03):` prefix regex. No `--no-verify` flag used; no force-push; no push to remote."
  artifacts:
    - path: "audit/v30-ASYMMETRY-RE-JUSTIFICATION.md"
      provides: "RNG-03 asymmetries re-justified from first principles per D-14 proof-by-exhaustion — § Asymmetry A (lootbox index-advance equivalent to flag-based isolation) + § Asymmetry B (phaseTransitionActive exemption admits only advanceGame-origin writes) + Finding Candidates"
      contains: "## § Asymmetry A — Lootbox RNG Index-Advance Isolation Equivalent to Flag-Based Isolation"
    - path: ".planning/phases/239-rnglocked-invariant-permissionless-sweep/239-03-SUMMARY.md"
      provides: "Plan 239-03 summary — HEAD 7ab515fe attestation, Asymmetry A/B proof structures, Phase 238 D-13 lootbox-index-advance gate discharge note"
      contains: "HEAD `7ab515fe`"
  key_links:
    - from: "contracts/modules/DegenerusGameAdvanceModule.sol lootboxRngIndex + lootboxRngWordByIndex SSTORE sites at HEAD 7ab515fe"
      to: "audit/v30-ASYMMETRY-RE-JUSTIFICATION.md § Asymmetry A Write Sites + Equivalence Proof"
      via: "D-14 proof-by-exhaustion from storage primitives at HEAD — no reliance on prior-milestone counts"
      pattern: "lootboxRngIndex|lootboxRngWordByIndex"
    - from: "contracts/modules/DegenerusGameAdvanceModule.sol phaseTransitionActive set/clear sites + SSTOREs reachable while flag is set (control-flow walk at HEAD 7ab515fe)"
      to: "audit/v30-ASYMMETRY-RE-JUSTIFICATION.md § Asymmetry B Enumerated SSTORE Sites + Call-Chain Rooting Proof"
      via: "D-14 proof-by-exhaustion from call-chain analysis at HEAD — independent of v29.0 Phase 235 D-13 Path 4"
      pattern: "phaseTransitionActive"
    - from: "Phase 238-03 FWD-03 gating Scope-Guard Deferral #1 (lootbox-index-advance audit assumption pending Phase 239 RNG-03 re-proof)"
      to: "Plan 239-03 commit (discharge evidenced by presence, no re-edit of Phase 238 output per D-29)"
      via: "D-29 discharge mechanism — Phase 242 REG-01/02 cross-checks at milestone consolidation"
      pattern: "lootbox-index-advance|phase-transition-gate"
    - from: "Plan 239-02 RNG-02 respects-equivalent-isolation rows (forward-cite RNG-03(a) by file+section path per D-15)"
      to: "§ Asymmetry A (the cited-to target)"
      via: "D-15 forward-cite mechanism — Wave 1 parallel; 239-02 may commit first; SUMMARY erratum if structural divergence"
      pattern: "audit/v30-ASYMMETRY-RE-JUSTIFICATION.md.*Asymmetry A"
    - from: "KNOWN-ISSUES.md 'Lootbox RNG uses index advance isolation instead of rngLockedFlag' entry"
      to: "§ Asymmetry A (SUBJECT — being re-justified; NOT warrant)"
      via: "D-14 subject-not-warrant discipline — proof is independent of KI entry existence"
      pattern: "Lootbox RNG uses index advance isolation"
---

<objective>
RNG-03 first-principles re-justification of the two documented asymmetries in the v30.0 VRF-consumer determinism invariant: (a) lootbox RNG index-advance isolation is equivalent in freeze-guarantee to flag-based (`rngLockedFlag`) isolation; (b) `phaseTransitionActive` exemption admits only `advanceGame`-origin writes and creates no player-reachable mutation path to RNG-consumer state.

**Single requirement satisfied (directly):**
- **RNG-03** — Two documented asymmetries re-justified from first principles: (a) lootbox RNG index-advance equivalent to rngLockedFlag isolation; (b) `phaseTransitionActive` exemption admits only `advanceGame`-origin writes. Per D-14 both proofs are proof-by-exhaustion from storage primitives at HEAD `7ab515fe` — NOT proof-by-cite. Prior-milestone artifacts cross-cited as "we independently re-derived same result" only.

**Deliverable (per D-24):**
- `audit/v30-ASYMMETRY-RE-JUSTIFICATION.md` — § Asymmetry A first-principles equivalence proof + § Asymmetry B first-principles origin proof + Prior-Artifact Cross-Cites + Finding Candidates + Scope-Guard Deferrals + Attestation

**Wave 1 parallel topology (per D-02):** This plan has zero cross-plan dependencies at HEAD. Runs concurrently with 239-01 and 239-02. Plan 239-02 RNG-02 forward-cites RNG-03(a) Asymmetry A by file+section path per D-15; either plan may commit first — reconciliation via SUMMARY erratum if structural divergence.

**Fresh-eyes mandate (per D-14/D-16/D-17):** Every argument in both asymmetry proofs is derived from contracts/ storage primitives at HEAD `7ab515fe` via proof-by-exhaustion. Prior-milestone artifacts CROSS-CITED with `re-verified at HEAD 7ab515fe — we independently re-derived the same result` notes — NOT relied upon as warrant.

**Scope containment (per D-18/D-20):**
- IN scope: first-principles re-derivation of two specific asymmetries (lootbox index-advance equivalent to flag-based; phaseTransitionActive admits only advanceGame-origin writes).
- OUT of scope: regression / finding-ID assignment (Phase 242); per-consumer freeze verdicts (Phase 238); gameover jackpot-input determinism (Phase 240); KI-exception acceptance re-verification (Phase 241 — EXC-04 owns EntropyLib XOR-shift acceptance; this plan cites EntropyLib only where it interacts with lootbox-index-advance isolation, NOT its PRNG-primitive acceptance).

**No F-30-NN emission (per D-22):** `CANDIDATE_FINDING` rows produce Finding Candidate blocks; Phase 242 FIND-01..03 owns ID assignment.

**READ-only scope (per D-27):** Only writes: `audit/v30-ASYMMETRY-RE-JUSTIFICATION.md` + `.planning/phases/239-rnglocked-invariant-permissionless-sweep/239-03-SUMMARY.md`. `KNOWN-ISSUES.md` NOT touched. `contracts/` / `test/` NOT touched. Phase 237 inventory + Phase 238 outputs READ-only.

**Phase 238 audit-assumption discharge (per D-29):** Phase 238-03 FWD-03 gating cited `lootbox-index-advance` gate correctness as audit assumption pending Phase 239 RNG-03 re-proof (Scope-Guard Deferral #1 in `audit/v30-FREEZE-PROOF.md`, secondary entry paired with the rngLocked entry discharged by Plan 239-01). Discharge evidenced by Plan 239-03 commit presence — no re-edit of Phase 238 files. Plan 239-01 discharges the rngLocked portion; this plan discharges the lootbox-index-advance portion + the phase-transition-gate portion (Asymmetry B). Phase 242 REG-01/02 cross-checks the discharge at milestone consolidation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/239-rnglocked-invariant-permissionless-sweep/239-CONTEXT.md
@audit/v30-CONSUMER-INVENTORY.md
@audit/v30-FREEZE-PROOF.md
@audit/v30-238-03-GATING.md
@KNOWN-ISSUES.md
</context>

<threat_model>
N/A — READ-only audit milestone per D-27. No runtime code modified, no test modified, no `contracts/` or `test/` writes. Attack surface added: NONE. Only writes: markdown under `audit/v30-ASYMMETRY-RE-JUSTIFICATION.md` + `.planning/` SUMMARY. No STRIDE category applies to documentation-only output; threat register empty by construction.

Residual audit-quality risk: a first-principles proof that relies (silently) on a prior-milestone conclusion rather than re-deriving from HEAD primitives would mask a latent asymmetry violation. Mitigation: (a) D-14 proof-by-exhaustion format forces enumeration of specific storage slots + SSTORE sites + call chains at HEAD — grep-reproducible; (b) prior-milestone cross-cites carry "we independently re-derived same result" notes, NOT "we rely on prior conclusion" — any SUMMARY line that violates this is a drift; (c) KNOWN-ISSUES.md entry is explicitly named SUBJECT not warrant (the design decision being re-justified) — proof is valid even if KI entry were removed; (d) Asymmetry A Write Sites row count must ≥ grep count of `lootboxRngIndex` / `lootboxRngWordByIndex` SSTOREs at HEAD — missing site breaks exhaustiveness claim; (e) Asymmetry B Enumerated SSTORE Sites must derive from control-flow walk between `phaseTransitionActive = true` set and clear sites at HEAD — NOT copied from prior Phase 235 enumeration; (f) the "no player-reachable mutation path" proof enumerates external entry points and shows each cannot reach the SSTORE sites under phaseTransitionActive — exhaustive, not sampled.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build § Asymmetry A — Lootbox RNG Index-Advance Isolation Equivalent to Flag-Based Isolation (first-principles proof-by-exhaustion at HEAD 7ab515fe)</name>
  <files>audit/v30-ASYMMETRY-RE-JUSTIFICATION.md</files>
  <read_first>
    - .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-CONTEXT.md (D-13 two-section structure + sub-section template; D-14 proof-by-exhaustion format — NOT proof-by-cite; KI entry is SUBJECT not warrant; D-15 forward-cite target from 239-02; D-17 re-verified-at-HEAD note format; D-22 no F-30-NN; D-25 tabular/no mermaid; D-26 HEAD anchor; D-27 READ-only; D-29 discharge Phase 238 lootbox-index-advance audit assumption)
    - .planning/REQUIREMENTS.md (RNG-03 exact wording — "(a) lootbox RNG uses index-advance instead of rngLockedFlag, (b) advanceGame-origin writes are exempt from the rngLocked guard via phaseTransitionActive. Neither may be assumed carried forward — both re-proven.")
    - .planning/ROADMAP.md Phase 239 block (Success Criterion 3 — names `audit/v30-ASYMMETRY-RE-JUSTIFICATION.md` as the RNG-03 deliverable; names both asymmetries)
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD 7ab515fe — lootbox VRF request + fulfillment paths; pre-scan per CONTEXT.md `<code_context>`:
      * `lootboxRngIndex` storage slot + advance site (mid-day lootbox VRF request path)
      * `lootboxRngWordByIndex` mapping SSTORE inside `rawFulfillRandomWords` (atomic write-on-fulfillment per index)
      * The L1700 branch (`if (rngLockedFlag) { ... }` in `rawFulfillRandomWords` per plan 239-01) — mid-day-lootbox branch writes `lootboxRngWordByIndex[index] = word;` + clears VRF request state
      * Consumer-side read: frozen `consumerIndex` pinned at request time (Phase 237 mid-day-lootbox family 19 rows per 238 D-12 tally)
      * RE-GREP AT HEAD for exact line numbers — DO NOT rely on CONTEXT.md approximations
    - contracts/modules/DegenerusGameLootboxModule.sol + contracts/modules/DegenerusGameMintModule.sol at HEAD — lootbox consumer read sites + the mid-day-lootbox VRF request origination
    - contracts/storage/DegenerusGameStorage.sol at HEAD — `lootboxRngIndex` + `lootboxRngWordByIndex` storage declarations
    - audit/v30-CONSUMER-INVENTORY.md §"Per-Consumer Call Graphs" (mid-day-lootbox family 19 rows — PREFIX-MIDDAY chain; lootbox consumer read sites) + §"Consumer Index" RNG-03 scope (19-row mid-day-lootbox family per 237-03 Decision 4; EXC-04 8 EntropyLib rows routed via lootbox-index-advance per 238-03)
    - audit/v30-238-03-GATING.md §"Gating Verification Table" (Named Gate `lootbox-index-advance` = 20 rows per 238-03 SUMMARY — 19 PREFIX-MIDDAY + 1 INV-237-124 daily-subset EXC-04 routed via lootbox-index-advance per D-06; this plan DISCHARGES the gate-correctness audit assumption per D-29; READ-only per D-29)
    - audit/v30-FREEZE-PROOF.md §"Scope-Guard Deferrals" entry #1 (cites Phase 239 RNG-03 for lootbox-index-advance portion; this plan discharges)
    - KNOWN-ISSUES.md §"Lootbox RNG uses index advance isolation instead of rngLockedFlag" — Asymmetry A SUBJECT (design decision being re-justified); the proof re-derives EQUIVALENCE from first principles, does NOT rely on KI entry as warrant per D-14
    - .planning/milestones/v29.0-phases/235-conservation-rng-commitment-re-proof-phase-transition/235-05-TRNX-01.md — v29.0 rngLocked 4-path walk (corroborating cross-cite per D-16 only — re-derive independently per D-14)
    - .planning/milestones/v29.0-phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-PFTB-AUDIT.md — non-zero-entropy + semantic-path-gate archetypes (corroborating for freeze-proof equivalence template)
  </read_first>
  <action>
    Create `audit/v30-ASYMMETRY-RE-JUSTIFICATION.md` with exactly these top-level sections in this exact order (seed file for Task 2 to extend with Asymmetry B):

    ```
    # v30.0 Two Asymmetries Re-Justified from First Principles (RNG-03)

    **Audit baseline:** HEAD `7ab515fe`
    **Plan:** 239-03
    **Requirement:** RNG-03

    ## Executive Summary
    ## § Asymmetry A — Lootbox RNG Index-Advance Isolation Equivalent to Flag-Based Isolation
    ## § Asymmetry B — phaseTransitionActive Exemption Admits Only advanceGame-Origin Writes
    ## Prior-Artifact Cross-Cites
    ## Finding Candidates
    ## Scope-Guard Deferrals
    ## Attestation
    ```

    **Step 1 — Seed `## Executive Summary`:**
    Brief 3-5 bullets naming both asymmetries, the proof-by-exhaustion format per D-14, and the discharge of Phase 238-03 lootbox-index-advance (Asymmetry A) + phase-transition-gate (Asymmetry B) audit assumptions per D-29.

    **Step 2 — Populate `## § Asymmetry A — Lootbox RNG Index-Advance Isolation Equivalent to Flag-Based Isolation`:**

    This is the Asymmetry A first-principles equivalence proof. Structure it with these sub-sections IN THIS ORDER per D-13:

    **2.1 `### Asymmetry Statement` —**
    One-paragraph statement of what is being proved:
    ```
    Claim: For the mid-day-lootbox VRF path, advancing `lootboxRngIndex` at VRF request time and reading `lootboxRngWordByIndex[consumerIndex]` at fulfillment time provides a freeze-guarantee structurally equivalent to the freeze-guarantee `rngLockedFlag` provides for the daily VRF path. Specifically: `∀ lootbox consumer with consumerIndex = k frozen at VRF request time, the read lootboxRngWordByIndex[k] at fulfillment returns either (a) the uninitialized zero sentinel (guarded elsewhere per Phase 237 inventory) OR (b) the VRF-delivered word for index k written atomically by rawFulfillRandomWords; no player, admin, or validator can write lootboxRngWordByIndex[k] after the index has advanced past k`.

    KI context (SUBJECT not warrant per D-14): the KNOWN-ISSUES.md entry "Lootbox RNG uses index advance isolation instead of rngLockedFlag" documents this design decision. This proof re-derives the equivalence from first principles at HEAD 7ab515fe; the KI entry's existence is NOT relied upon as warrant.
    ```

    **2.2 `### Storage Primitives` —**
    Enumerate exhaustively at HEAD 7ab515fe via fresh grep:
    - `lootboxRngIndex` storage slot declaration: cite file:line in `contracts/storage/DegenerusGameStorage.sol` (fresh grep).
    - `lootboxRngWordByIndex` mapping declaration: cite file:line in `contracts/storage/DegenerusGameStorage.sol`.
    - Type declarations (uint48, mapping(uint48 => uint256) or whatever the actual types are at HEAD — grep to verify).

    Grep commands preserved as fenced code block:
    ```bash
    grep -rn 'lootboxRngIndex' contracts/ --include='*.sol' | grep -v mocks
    grep -rn 'lootboxRngWordByIndex' contracts/ --include='*.sol' | grep -v mocks
    ```

    **2.3 `### Write Sites` —**
    Table with columns `Site ID | File:Line | Function | Writer Context | Write Semantics`:
    - `Site ID`: `ASYM-239-A-W-NN` (two-digit zero-padded).
    - `Writer Context`: one-line description (e.g., "mid-day lootbox VRF request — advances index by 1 per request", "rawFulfillRandomWords — atomic write for delivered index").
    - `Write Semantics`: describes whether the write is monotonic (index advance) or per-key (mapping SSTORE).

    Enumerate every `lootboxRngIndex = ...` SSTORE + every `lootboxRngWordByIndex[...] = ...` SSTORE at HEAD. Row count must ≥ grep count from Step 2.2.

    **2.4 `### Read Sites` —**
    Table with columns `Site ID | File:Line | Function | Read Context | consumerIndex Freeze Citation`:
    - `Site ID`: `ASYM-239-A-R-NN`.
    - `Read Context`: one-line description of the consumer reading `lootboxRngWordByIndex[consumerIndex]`.
    - `consumerIndex Freeze Citation`: cite the code path where `consumerIndex` is frozen at VRF request time (not at fulfillment time) — this is the load-bearing claim that an attacker cannot later shift the consumerIndex to read a different word.

    Cross-reference INV-237-NNN row IDs for the 19 mid-day-lootbox family rows + the EXC-04 rows that route via lootbox-index-advance (per Phase 238-03 SUMMARY, 20 rows total: 19 PREFIX-MIDDAY + 1 INV-237-124 daily-subset EXC-04).

    **2.5 `### Equivalence Proof` —**
    Closed-form proof-by-exhaustion per D-14:
    ```
    **Invariant (Asymmetry A):** ∀ lootbox consumer C with consumerIndex = k frozen at VRF request time:
      Read lootboxRngWordByIndex[k] at fulfillment returns exactly one of:
        (a) the uninitialized zero sentinel (guarded by consumer-side zero-check per Phase 237 inventory — cite INV-237-NNN rows that carry the zero-check)
        (b) the VRF-delivered word for index k written atomically by rawFulfillRandomWords at the mid-day-lootbox branch (cite Site ID ASYM-239-A-W-NN from Write Sites table)

    **Freeze-Guarantee:** No player, admin, or validator can write lootboxRngWordByIndex[k] after the index has advanced past k because:
      1. The ONLY writer to lootboxRngWordByIndex[k] is rawFulfillRandomWords (enumerated in Write Sites) — verify by grep exhaustiveness.
      2. rawFulfillRandomWords is callable only by the Chainlink VRF coordinator (access-control cite — cite contracts/modules/DegenerusGameAdvanceModule.sol rawFulfillRandomWords + onlyCoordinator or equivalent modifier at HEAD).
      3. The mapping structure enforces per-key atomicity (Solidity 0.8.34 mapping semantics: each keccak256(k || slot) slot is independent).
      4. Once index advances past k (monotonic advance per Write Sites), no subsequent VRF fulfillment targets index k — the next fulfillment writes lootboxRngWordByIndex[k+1], [k+2], etc.

    **Equivalence to rngLockedFlag:** The freeze-guarantee above is structurally equivalent to the rngLockedFlag guarantee for the daily VRF path, in the following sense:
      - rngLockedFlag (daily): ALL consumer-side reads of daily RNG state are gated behind the flag; any attempted mutation between request and consumption is blocked by the `if (rngLockedFlag) revert` guard at DegenerusGameAdvanceModule.sol:1031 (re-verified at HEAD 7ab515fe — Plan 239-01 RNG-01 output).
      - lootbox-index-advance (mid-day-lootbox): the consumerIndex freeze + mapping-slot atomicity + monotonic index advance + single-writer-per-slot (VRF coordinator only) composes to the same "no mutable-after-request" property for the specific storage slot lootboxRngWordByIndex[k] read by consumer C with consumerIndex = k.

    **Conclusion:** Asymmetry A is re-justified from first principles at HEAD 7ab515fe. The lootbox-index-advance mechanism provides freeze-guarantee equivalent to flag-based isolation for the mid-day-lootbox VRF path. ∎
    ```

    **2.6 `### Discharge of Phase 238-03 FWD-03 gating `lootbox-index-advance` audit assumption (D-29)` —**
    Explicit note that this § Asymmetry A proof discharges the Phase 238-03 Scope-Guard Deferral #1 (lootbox-index-advance portion). No re-edit of Phase 238 files per D-29. Phase 242 REG-01/02 cross-checks.

    **Step 3 — Seed remaining sections as placeholders:**
    - `## § Asymmetry B — phaseTransitionActive Exemption Admits Only advanceGame-Origin Writes` (Task 2 populates)
    - `## Prior-Artifact Cross-Cites` (Task 2 populates)
    - `## Finding Candidates` (Task 2 populates)
    - `## Scope-Guard Deferrals` (Task 2 populates)
    - `## Attestation` (Task 2 populates)

    **Step 4 — Frontmatter of audit file:** include `audit_baseline: 7ab515fe` + `plan: 239-03` + `requirement: RNG-03`.

    Do NOT commit yet — commit lands in Task 2 after Asymmetry B + all closing sections populated.
  </action>
  <verify>
    <automated>
      test -f audit/v30-ASYMMETRY-RE-JUSTIFICATION.md
      grep -q '^# v30.0 Two Asymmetries Re-Justified' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md
      grep -qE '^audit_baseline: 7ab515fe' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md

      # Mandatory § Asymmetry A section + all sub-sections per D-13
      grep -q '^## § Asymmetry A — Lootbox RNG Index-Advance Isolation Equivalent to Flag-Based Isolation$' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md
      grep -q '^### Asymmetry Statement' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md
      grep -q '^### Storage Primitives' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md
      grep -q '^### Write Sites' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md
      grep -q '^### Read Sites' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md
      grep -q '^### Equivalence Proof' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md

      # D-14 first-principles discipline: `we independently re-derived` or equivalent language (NOT `rely on prior milestone`)
      # Just check Asymmetry A claims enumeration at HEAD, not reliance
      grep -qiE 'proof-by-exhaustion|first principles|HEAD 7ab515fe' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md

      # D-14 KI-as-SUBJECT discipline
      grep -qiE 'SUBJECT.*warrant|subject.*not.*warrant|not.*rely.*KI|KI.*entry.*not.*warrant' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md

      # Write Sites + Read Sites tables populated with row IDs
      grep -qE '\| ASYM-239-A-W-[0-9]{2} \|' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md
      grep -qE '\| ASYM-239-A-R-[0-9]{2} \|' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md

      # Write Sites row count ≥ grep count for lootboxRngIndex / lootboxRngWordByIndex SSTOREs
      # (the row count ≥ SSTORE count — Write Sites may include derived/contextual rows beyond the raw grep; hence ≥ not =)
      SSTORE_COUNT=$(grep -rnE 'lootboxRng(Index|WordByIndex)\[' contracts/ --include='*.sol' | grep -v mocks | grep -E '=\s*' | wc -l)
      SITE_COUNT=$(grep -cE '\| ASYM-239-A-W-[0-9]{2} \|' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md)
      test "$SITE_COUNT" -ge 1

      # D-29 Phase 238 lootbox-index-advance discharge note
      grep -qiE 'Phase 238|D-29|Scope-Guard Deferral #1|lootbox-index-advance.*discharge|discharge.*lootbox' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md

      # Zero F-30-NN per D-22
      ! grep -qE 'F-30-[0-9]' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md

      # Zero mermaid per D-25
      ! grep -qi '```mermaid' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md

      # HEAD anchor echoed
      grep -q '7ab515fe' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md

      # READ-only scope per D-27
      test -z "$(git status --porcelain contracts/ test/)"
      test -z "$(git diff --name-only 7ab515fe..HEAD -- contracts/ test/)"

      # Phase 237/238 outputs + KNOWN-ISSUES untouched per D-28/D-29
      test -z "$(git status --porcelain audit/v30-238-01-BWD.md audit/v30-238-02-FWD.md audit/v30-238-03-GATING.md audit/v30-FREEZE-PROOF.md audit/v30-CONSUMER-INVENTORY.md KNOWN-ISSUES.md 2>/dev/null || true)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `audit/v30-ASYMMETRY-RE-JUSTIFICATION.md` created with the 7 mandatory top-level section headings in order (Executive Summary / § Asymmetry A / § Asymmetry B placeholder / Prior-Artifact Cross-Cites placeholder / Finding Candidates placeholder / Scope-Guard Deferrals placeholder / Attestation placeholder).
    - `## § Asymmetry A` section is FULLY populated with all 6 sub-sections in order: `### Asymmetry Statement`, `### Storage Primitives`, `### Write Sites`, `### Read Sites`, `### Equivalence Proof`, `### Discharge of Phase 238-03 ...`.
    - Write Sites table row IDs format `ASYM-239-A-W-NN` with ≥ 1 row; Read Sites table row IDs format `ASYM-239-A-R-NN` with ≥ 1 row.
    - D-14 proof-by-exhaustion discipline visible: Storage Primitives enumerates slots at HEAD 7ab515fe, Write/Read Sites cite specific file:line at HEAD, Equivalence Proof enumerates invariant + freeze-guarantee + equivalence-to-rngLockedFlag claims from primitives.
    - D-14 KI-as-SUBJECT discipline visible: `### Asymmetry Statement` sub-section explicitly names KI entry as SUBJECT (the design decision being re-justified), NOT warrant.
    - D-29 discharge sub-section explicit: `### Discharge of Phase 238-03 FWD-03 gating lootbox-index-advance audit assumption (D-29)` states discharge-by-presence + no-re-edit-of-238 + Phase 242 cross-check routing.
    - `audit_baseline: 7ab515fe` in frontmatter; `HEAD 7ab515fe` in body per D-26.
    - `grep -E 'F-30-[0-9]'` returns zero matches per D-22.
    - `grep -i 'mermaid'` returns zero matches per D-25.
    - `git status --porcelain contracts/ test/` returns empty per D-27.
    - `git status --porcelain audit/v30-238-*.md audit/v30-FREEZE-PROOF.md audit/v30-CONSUMER-INVENTORY.md KNOWN-ISSUES.md` returns empty per D-28/D-29.
    - No commit yet (commit in Task 2 after Asymmetry B populated).
  </acceptance_criteria>
  <done>
    File skeleton created; § Asymmetry A fully proven via 6-sub-section structure with proof-by-exhaustion from storage primitives at HEAD 7ab515fe; KI entry named as SUBJECT not warrant; D-29 discharge note explicit; READ-only scope preserved; zero F-30-NN / mermaid / placeholders. Ready for Task 2 (Asymmetry B + closing sections + commit).
  </done>
</task>

<task type="auto">
  <name>Task 2: Build § Asymmetry B — phaseTransitionActive Exemption + Prior-Artifact Cross-Cites + Finding Candidates + Scope-Guard Deferrals + Attestation + commit</name>
  <files>audit/v30-ASYMMETRY-RE-JUSTIFICATION.md</files>
  <read_first>
    - .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-CONTEXT.md (D-13 § Asymmetry B sub-section template; D-14 proof-by-exhaustion + call-chain rooting proof; D-16/D-17 cross-cite format; D-19 gameover-bracket interactions; D-20 KI-acceptance out-of-scope; D-22 no F-30-NN; D-23 Finding Candidates; D-29 Phase 238 phase-transition-gate discharge)
    - audit/v30-ASYMMETRY-RE-JUSTIFICATION.md produced by Task 1 — extend with Asymmetry B
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD 7ab515fe — `phaseTransitionActive` surfaces:
      * Storage declaration + set/clear sites (CONTEXT.md pre-scan: set near L634, clear near L323 — RE-GREP AT HEAD; actual observed line numbers per earlier grep: set at L634 `phaseTransitionActive = true;`, clear at L323 `phaseTransitionActive = false;`, guard branch at L298 `if (phaseTransitionActive) { ... }`)
      * Every SSTORE reachable while `phaseTransitionActive = true` is active — walk the control-flow from set site (L634 `_endPhase()`) forward through advanceGame() body until clear site (L323); enumerate every SSTORE encountered + its call-chain rooting
      * The gate branch at `DegenerusGameAdvanceModule.sol:298` (per observed grep — CONTEXT.md said L283 but HEAD grep shows L298) exempting writes from rngLocked guard
    - contracts/storage/DegenerusGameStorage.sol at HEAD — `phaseTransitionActive` storage declaration
    - audit/v30-CONSUMER-INVENTORY.md §"Universe List" — every consumer whose Write Paths column includes `phaseTransitionActive` slot is in-scope for Asymmetry B enumeration (per 237-03 call-graph data)
    - audit/v30-238-03-GATING.md §"Gating Verification Table" — Named Gate `phase-transition-gate` = 0 rows as PRIMARY gate per 238-03 SUMMARY (appears as COMPANION gate only; this plan's Asymmetry B proof justifies the companion-gate semantics first-principles; READ-only per D-29)
    - audit/v30-FREEZE-PROOF.md §"Scope-Guard Deferrals" entry #1 (phase-transition-gate portion — this plan discharges per D-29)
    - .planning/milestones/v29.0-phases/235-conservation-rng-commitment-re-proof-phase-transition/235-CONTEXT.md + 235-05-TRNX-01.md — v29.0 Phase 235 D-13 Path 4 (read/write buffer invariants around phase transition); CORROBORATING per D-16 — re-derive independently at HEAD per D-14
    - .planning/milestones/v29.0-phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-PFTB-AUDIT.md — non-zero-entropy + semantic-path-gate archetypes around phase transition (corroborating)
    - contracts/DegenerusGame.sol at HEAD — external entry points (advanceGame + others) — Asymmetry B "no player-reachable mutation path" proof enumerates external entry points and shows each either (a) cannot toggle phaseTransitionActive = true, or (b) routes through advanceGame's `_endPhase` which does toggle but is the ONLY origin
    - memory/feedback_rng_backward_trace.md (backward-trace methodology — inform call-chain rooting proof direction)
  </read_first>
  <action>
    Extend `audit/v30-ASYMMETRY-RE-JUSTIFICATION.md` with the following sections, replacing placeholders from Task 1:

    **Step 1 — Populate `## § Asymmetry B — phaseTransitionActive Exemption Admits Only advanceGame-Origin Writes` with these 6 sub-sections IN THIS ORDER per D-13:**

    **B.1 `### Asymmetry Statement` —**
    ```
    Claim: The `phaseTransitionActive` storage flag, when true, exempts writes from the rngLocked guard that would otherwise block mutation during the VRF commitment window. This proof demonstrates that (a) every SSTORE reachable while phaseTransitionActive = true originates inside advanceGame's execution context, and (b) no external entry point can toggle phaseTransitionActive = true without first entering advanceGame — therefore the exemption creates no player-reachable mutation path to RNG-consumer state.

    Context: Phase 238 D-13 `phase-transition-gate` appears as a COMPANION gate (not PRIMARY) in the 238-03 Gating Verification Table — rows carrying phaseTransitionActive in their Forward Mutation Paths are PRIMARY-gated by rngLocked (daily/gameover) or lootbox-index-advance (mid-day), with phaseTransitionActive as a secondary cover. This proof re-justifies the companion-gate semantics from first principles.
    ```

    **B.2 `### Storage Primitives` —**
    Enumerate at HEAD 7ab515fe:
    - `phaseTransitionActive` storage slot declaration: file:line in `contracts/storage/DegenerusGameStorage.sol` (fresh grep).
    - Set site(s): `phaseTransitionActive = true;` — cite file:line at HEAD (observed: `DegenerusGameAdvanceModule.sol:634` inside `_endPhase()`).
    - Clear site(s): `phaseTransitionActive = false;` — cite file:line at HEAD (observed: `DegenerusGameAdvanceModule.sol:323`).
    - Gate branch: `if (phaseTransitionActive)` — cite file:line at HEAD (observed: `DegenerusGameAdvanceModule.sol:298`).

    Grep commands preserved as fenced code block:
    ```bash
    grep -rn 'phaseTransitionActive' contracts/ --include='*.sol' | grep -v mocks
    ```

    **B.3 `### Enumerated SSTORE Sites Under phaseTransitionActive = true` —**
    Table with columns `Site ID | File:Line | Function | SSTORE Target (storage slot / mapping key) | Call-Chain Root`:
    - `Site ID`: `ASYM-239-B-S-NN` (two-digit zero-padded).
    - Walk the control-flow from set site (L634 `_endPhase()`) forward through advanceGame body until clear site (L323); enumerate every SSTORE reachable. Include: far-future ticket drain SSTOREs (per Phase 232.1-03 semantic-path-gate archetype), level-prize-pool SSTOREs (`levelPrizePool[lvl]`), counter/flag SSTOREs (`jackpotCounter`, `compressedJackpotFlag`), any other.
    - `Call-Chain Root`: MUST be `advanceGame` — document the call chain (e.g., `advanceGame → _endPhase (sets flag) → _processPhaseTransition → _processFutureTicketBatch → SSTORE`). If ANY SSTORE's call chain roots somewhere other than advanceGame, verdict becomes `CANDIDATE_FINDING`.

    This is the proof-by-exhaustion core per D-14. Row count must ≥ count of SSTOREs in the control-flow walk.

    **B.4 `### Call-Chain Rooting Proof` —**
    Closed-form argument:
    ```
    **Invariant (Asymmetry B, part 1):** ∀ Site ID S ∈ Enumerated SSTORE Sites (sub-section B.3), S's call chain roots at advanceGame. Specifically:
      - `advanceGame` is the only external entry point that calls `_endPhase` (verify by grep for `_endPhase` callers at HEAD).
      - `_endPhase` is the only function that sets `phaseTransitionActive = true` (verify by Write Sites enumeration in B.2).
      - Therefore, the call chain `_endPhase → subsequent SSTOREs → phaseTransitionActive = false (clear)` is entirely inside advanceGame's execution context.

    **Proof:** grep verification at HEAD 7ab515fe:
      - `grep -rn '_endPhase' contracts/ --include='*.sol'` shows exactly one caller: `advanceGame` (cite file:line).
      - `grep -rn 'phaseTransitionActive\s*=\s*true' contracts/` shows exactly one site: `_endPhase` (cite file:line — B.2 set site).
      Therefore every SSTORE in B.3 table has call-chain root = `advanceGame`. ∎
    ```

    **B.5 `### No Player-Reachable Mutation-Path Proof` —**
    ```
    **Invariant (Asymmetry B, part 2):** No player-callable function can reach any SSTORE in the B.3 Enumerated SSTORE Sites table under the condition `phaseTransitionActive = true`.

    **Proof by exhaustion over external entry points:**
      Enumerate every external/public function in contracts/ (reference Plan 239-02 RNG-02 Pass 1 candidate universe — forward cite by file+section path to `audit/v30-PERMISSIONLESS-SWEEP.md ## Pass 1 — Mechanical Grep Discovery`):
      - For each external entry E:
        * If E is admin-gated: E cannot be invoked by a player (out of scope by actor-class).
        * If E is game-internal (onlyGame / onlyCoinflip / self-call / delegatecall-only): E is not reachable by player's direct EOA.
        * If E is permissionless AND mutates state: verify E's call chain never reaches `_endPhase` (the only toggler of phaseTransitionActive = true). E's write-set must be disjoint from `phaseTransitionActive` SSTORE site.
      For the `advanceGame` entry specifically: advanceGame IS callable by a player (anyone can trigger), and it DOES eventually call `_endPhase` under specific end-of-level conditions. However:
        * The SSTOREs in B.3 that execute while phaseTransitionActive = true execute INSIDE advanceGame's own transaction context — they cannot be interleaved with mutations from OTHER transactions (single-threaded EVM execution).
        * An attacker cannot send a DIFFERENT transaction that mutates RNG-consumer state while advanceGame is running phaseTransitionActive = true, because transactions are serialized.
        * Therefore, the exemption from rngLocked is safe: any mutation executed under phaseTransitionActive = true is by definition inside advanceGame, and advanceGame's call chain is the ONLY chain that could have opened the VRF request that set rngLockedFlag in the first place.

    **Conclusion:** Asymmetry B is re-justified from first principles at HEAD 7ab515fe. phaseTransitionActive exemption admits only advanceGame-origin writes and creates no player-reachable mutation path to RNG-consumer state. ∎
    ```

    **B.6 `### Discharge of Phase 238-03 FWD-03 gating `phase-transition-gate` audit assumption (D-29)` —**
    Explicit note that this § Asymmetry B proof discharges the Phase 238-03 Scope-Guard Deferral #1 (phase-transition-gate portion). No re-edit of Phase 238 files per D-29. Phase 242 REG-01/02 cross-checks.

    **Step 2 — Populate `## Prior-Artifact Cross-Cites` per D-16/D-17:**

    Each cite structured with `re-verified at HEAD 7ab515fe — we independently re-derived the same result` (D-14 format). Minimum cites:
    - `.planning/milestones/v29.0-phases/235-conservation-rng-commitment-re-proof-phase-transition/235-05-TRNX-01.md` — v29.0 Phase 235 D-13 Path 4 (read/write buffer invariants around phase transition); `we independently re-derived the same result — v30.0 Asymmetry B proof structurally equivalent to v29.0 Path 4 enumeration`.
    - `.planning/milestones/v29.0-phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-PFTB-AUDIT.md` — non-zero-entropy + semantic-path-gate archetypes around phase transition (corroborating for Asymmetry B companion-gate semantics).
    - `KNOWN-ISSUES.md` §"Lootbox RNG uses index advance isolation instead of rngLockedFlag" — SUBJECT of Asymmetry A (design decision re-justified); NOT warrant per D-14.
    - `.planning/milestones/v25.0-phases/215-rng-fresh-eyes/` — v25.0 RNG SOUND verdict (structural baseline).
    - `audit/v30-CONSUMER-INVENTORY.md` Consumer Index RNG-03 scope (19-row mid-day-lootbox family per 237-03 Decision 4) — scope anchor for Asymmetry A consumer set.
    - `audit/v30-238-03-GATING.md` §"Gating Verification Table" (`lootbox-index-advance` 20 rows + `phase-transition-gate` 0 rows as PRIMARY per 238-03 SUMMARY) — discharge target for D-29.

    **Step 3 — Populate `## Finding Candidates` per D-23:**

    If any observation during asymmetry re-derivation doesn't fit CLEAN: structured blocks with `SECTION_REF` + `FILE:LINE` + `SEVERITY: TBD-242` + routing to Phase 242 FIND-01. If zero: state `**None surfaced.** Both asymmetries re-justified AIRTIGHT from first principles at HEAD 7ab515fe.`

    **Step 4 — Populate `## Scope-Guard Deferrals` per D-28:**

    If any consumer/SSTORE-site discovered during proof that is NOT in Phase 237 inventory: record deferral block. If zero: state `**None surfaced.** Asymmetry A consumer set (19 mid-day-lootbox rows) and Asymmetry B SSTORE set both map to existing INV-237-NNN Universe List rows.`

    **Step 5 — Populate `## Executive Summary` (replace Task 1 seed with final version):**
    - Asymmetry A verdict: first-principles equivalence proven at HEAD 7ab515fe; consumer set = 20 rows (19 PREFIX-MIDDAY + 1 INV-237-124 daily-subset EXC-04 per 238-03); Write Sites row count = N_A_W; Read Sites row count = N_A_R.
    - Asymmetry B verdict: first-principles origin proof complete at HEAD 7ab515fe; Enumerated SSTORE Sites row count = N_B_S; call-chain rooting proof holds for all N_B_S rows.
    - Discharge: Phase 238-03 lootbox-index-advance (Asymmetry A) + phase-transition-gate (Asymmetry B) audit assumptions discharged per D-29.
    - HEAD anchor 7ab515fe, READ-only, zero F-30-NN.

    **Step 6 — Populate `## Attestation`:**
    ```
    **HEAD anchor:** `7ab515fe` (contract tree identical to v29.0 `1646d5af`; all post-v29 commits are docs-only per PROJECT.md).
    **Scope:** RNG-03 two asymmetries re-justified from first principles — § Asymmetry A (lootbox index-advance equivalent to flag-based) + § Asymmetry B (phaseTransitionActive admits only advanceGame-origin writes).
    **D-14 proof-by-exhaustion:** every claim enumerates specific storage slots + SSTORE sites + call chains at HEAD 7ab515fe. Prior-milestone cites carry "we independently re-derived same result" notes (D-14 format) — NOT load-bearing-reliance notes.
    **D-14 KI-as-SUBJECT:** KNOWN-ISSUES.md `Lootbox RNG uses index advance isolation instead of rngLockedFlag` entry is SUBJECT of Asymmetry A (design decision re-justified), NOT warrant. Proof is independent of KI entry existence.
    **D-22 finding-ID emission:** Zero F-30-NN. Finding Candidates routed to Phase 242 FIND-01.
    **D-27 READ-only:** zero contracts/ or test/ writes. KNOWN-ISSUES.md untouched.
    **D-29 Phase 238 discharge:** Phase 238-03 FWD-03 gating Scope-Guard Deferral #1 (lootbox-index-advance + phase-transition-gate portions) discharged by this plan's commit. Plan 239-01 separately discharges the rngLocked portion. No re-edit of Phase 238 files.
    **D-15 forward-cite target:** Plan 239-02 RNG-02 `respects-equivalent-isolation` rows cite `§ Asymmetry A` by file+section path — Wave 1 parallel; either plan may commit first; 239-02 SUMMARY records reconciliation if structural divergence.
    **Row-set integrity:** Asymmetry A Write Sites / Read Sites row counts cited in § A Executive Summary + A.3/A.4 tables; Asymmetry B Enumerated SSTORE Sites row count cited in § B Executive Summary + B.3 table. All grep-reproducible at HEAD 7ab515fe.
    ```

    **Step 7 — Commit:**
    Stage ONLY `audit/v30-ASYMMETRY-RE-JUSTIFICATION.md`. Commit with message:
    ```
    docs(239-03): RNG-03 two asymmetries re-justified from first principles at HEAD 7ab515fe

    § Asymmetry A (lootbox RNG index-advance isolation equivalent to flag-based
    isolation) proven by proof-by-exhaustion from storage primitives per D-14:
    Storage Primitives + Write Sites + Read Sites + Equivalence Proof (freeze-guarantee
    via monotonic index advance + mapping-slot atomicity + single-writer VRF coordinator).

    § Asymmetry B (phaseTransitionActive exemption admits only advanceGame-origin
    writes) proven via control-flow walk from set site to clear site enumerating
    every reachable SSTORE, plus call-chain rooting proof (every SSTORE's chain
    roots at advanceGame) plus no-player-reachable-mutation-path proof.

    Prior-milestone artifacts cross-cited with "we independently re-derived same
    result" notes (D-14 format) — NOT relied upon as warrant. KNOWN-ISSUES.md
    lootbox-index-advance entry named SUBJECT not warrant.

    Discharges Phase 238-03 FWD-03 gating Scope-Guard Deferral #1
    (lootbox-index-advance + phase-transition-gate portions). Plan 239-01
    separately discharges rngLocked portion.

    READ-only: zero contracts/ or test/ writes per D-27. Zero F-30-NN per D-22.
    HEAD anchor 7ab515fe locked per D-26.
    ```

    Do NOT push. Do NOT amend. Do NOT use --no-verify. Single commit only.
  </action>
  <verify>
    <automated>
      # § Asymmetry B section fully populated with all 6 sub-sections per D-13
      grep -q '^## § Asymmetry B — phaseTransitionActive Exemption Admits Only advanceGame-Origin Writes$' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md
      grep -qE '^### Asymmetry Statement' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md
      grep -qE '^### Storage Primitives' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md
      grep -qE '^### Enumerated SSTORE Sites Under phaseTransitionActive = true' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md
      grep -qE '^### Call-Chain Rooting Proof' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md
      grep -qE '^### No Player-Reachable Mutation-Path Proof' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md

      # Enumerated SSTORE Sites table populated
      grep -qE '\| ASYM-239-B-S-[0-9]{2} \|' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md

      # Prior-Artifact Cross-Cites with ≥ 3 "we independently re-derived" notes per D-14 format
      grep -q '^## Prior-Artifact Cross-Cites$' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md
      grep -cE 're-verified at HEAD 7ab515fe' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md | awk '{ if ($1 < 3) exit 1 }'
      grep -qiE 'independently re-derived|we independently derived' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md

      # Finding Candidates + Scope-Guard Deferrals + Attestation sections populated
      grep -q '^## Finding Candidates$' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md
      grep -q '^## Scope-Guard Deferrals$' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md
      grep -q '^## Attestation$' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md

      # D-14 KI-as-SUBJECT discipline preserved
      grep -qiE 'SUBJECT.*warrant|subject.*not.*warrant|NOT.*warrant' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md

      # D-29 discharge note for BOTH lootbox-index-advance AND phase-transition-gate
      grep -qiE 'lootbox-index-advance.*discharge|discharge.*lootbox-index-advance' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md
      grep -qiE 'phase-transition-gate.*discharge|discharge.*phase-transition-gate' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md

      # D-15 forward-cite handling in Attestation
      grep -qiE 'D-15|Plan 239-02.*Asymmetry A|respects-equivalent-isolation' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md

      # Zero F-30-NN per D-22
      ! grep -qE 'F-30-[0-9]' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md

      # Zero mermaid per D-25
      ! grep -qi '```mermaid' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md

      # Zero placeholder tokens
      ! grep -qE '<line>|<path>|<fn|<slug>|TBD-239' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md

      # HEAD anchor echoed
      grep -q '7ab515fe' audit/v30-ASYMMETRY-RE-JUSTIFICATION.md

      # READ-only scope per D-27
      test -z "$(git status --porcelain contracts/ test/)"
      test -z "$(git diff --name-only 7ab515fe..HEAD -- contracts/ test/)"

      # Phase 237/238 outputs + KNOWN-ISSUES untouched per D-28/D-29
      test -z "$(git status --porcelain audit/v30-238-01-BWD.md audit/v30-238-02-FWD.md audit/v30-238-03-GATING.md audit/v30-FREEZE-PROOF.md audit/v30-CONSUMER-INVENTORY.md KNOWN-ISSUES.md 2>/dev/null || true)"

      # Commit landed — subject prefix docs(239-03):
      git log --oneline -1 | grep -qE '^[0-9a-f]+ docs\(239-03\):'
      # Commit touched ONLY audit/v30-ASYMMETRY-RE-JUSTIFICATION.md
      git show --stat HEAD | grep -E '^\s+audit/v30-ASYMMETRY-RE-JUSTIFICATION.md'
      test "$(git show --stat HEAD --name-only --format='' | grep -v '^$' | wc -l)" -eq 1
    </automated>
  </verify>
  <acceptance_criteria>
    - `## § Asymmetry B` section is FULLY populated with all 6 sub-sections in order: `### Asymmetry Statement`, `### Storage Primitives`, `### Enumerated SSTORE Sites Under phaseTransitionActive = true`, `### Call-Chain Rooting Proof`, `### No Player-Reachable Mutation-Path Proof`, `### Discharge of Phase 238-03 FWD-03 gating phase-transition-gate audit assumption (D-29)`.
    - Enumerated SSTORE Sites table row IDs format `ASYM-239-B-S-NN` with ≥ 1 row; every row has Call-Chain Root column filled with `advanceGame` or routed through advanceGame.
    - Call-Chain Rooting Proof cites grep verification commands for `_endPhase` callers + `phaseTransitionActive = true` set sites at HEAD 7ab515fe.
    - No Player-Reachable Mutation-Path Proof enumerates external entry points with reference to Plan 239-02 Pass 1 candidate universe + single-threaded-EVM execution argument for advanceGame self-contained mutations.
    - `## Prior-Artifact Cross-Cites` section has ≥ 3 cites with `re-verified at HEAD 7ab515fe` notes per D-17; at least one cite uses `we independently re-derived the same result` language per D-14 format.
    - `## Finding Candidates` has either N structured blocks OR `**None surfaced.**` statement per D-23.
    - `## Scope-Guard Deferrals` has either N deferral blocks OR `**None surfaced.**` statement per D-28.
    - `## Attestation` lists: HEAD anchor, D-14 proof-by-exhaustion discipline, D-14 KI-as-SUBJECT discipline, D-22 zero F-30-NN, D-27 READ-only, D-29 discharge of both lootbox-index-advance AND phase-transition-gate portions, D-15 forward-cite handling, row-set integrity.
    - `## Executive Summary` has final (non-seed) summary with both Asymmetry A and B verdicts + discharge attestation.
    - D-29 discharge explicit for BOTH portions: lootbox-index-advance (Asymmetry A) AND phase-transition-gate (Asymmetry B).
    - `grep -E 'F-30-[0-9]'` returns zero matches per D-22.
    - `grep -i 'mermaid'` returns zero matches per D-25.
    - Exactly one new commit on `main` with subject matching regex `^docs\(239-03\):`; the commit touches exactly `audit/v30-ASYMMETRY-RE-JUSTIFICATION.md`.
    - `git status --porcelain contracts/ test/` empty per D-27.
    - `git status --porcelain audit/v30-238-*.md audit/v30-FREEZE-PROOF.md audit/v30-CONSUMER-INVENTORY.md KNOWN-ISSUES.md` empty per D-28/D-29.
    - No `--no-verify`, no force-push, no push-to-remote.
  </acceptance_criteria>
  <done>
    Both Asymmetry proofs complete with proof-by-exhaustion from storage primitives at HEAD 7ab515fe; KI entries as SUBJECT not warrant; D-29 discharge explicit for BOTH lootbox-index-advance + phase-transition-gate portions; prior cross-cites use D-14 "we independently re-derived" format; Attestation locks everything; one clean commit on main; Phase 237 inventory + Phase 238 outputs + KNOWN-ISSUES untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write Plan 239-03 SUMMARY with Phase 238 dual-discharge attestation + RNG-02 forward-cite coordination + commit</name>
  <files>.planning/phases/239-rnglocked-invariant-permissionless-sweep/239-03-SUMMARY.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md (SUMMARY template structure)
    - .planning/phases/238-backward-forward-freeze-proofs/238-03-SUMMARY.md (Phase 238 consolidated output summary precedent — particularly dual-portion audit assumption discharge)
    - .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-CONTEXT.md (D-26 HEAD anchor in SUMMARY frontmatter; D-29 discharge evidence by commit presence for BOTH lootbox-index-advance AND phase-transition-gate; D-15 Plan 239-02 forward-cite reconciliation; D-28 scope-guard deferral format)
    - audit/v30-ASYMMETRY-RE-JUSTIFICATION.md (produced by Task 1 + Task 2 — source for Asymmetry A/B proof counts + cross-cite count)
    - .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-02-SUMMARY.md (IF it has been written — check for D-15 forward-cite reconciliation status; 239-02's SUMMARY may reference 239-03 if 239-03 committed first; record bi-directional coordination)
    - audit/v30-PERMISSIONLESS-SWEEP.md (IF Plan 239-02 has committed — check `§ Asymmetry A` forward-cite format used by 239-02's `respects-equivalent-isolation` rows; record reconciliation if structural divergence)
    - git log --oneline -1 (capture commit SHA for SUMMARY Task Commits section)
  </read_first>
  <action>
    Create `.planning/phases/239-rnglocked-invariant-permissionless-sweep/239-03-SUMMARY.md` with frontmatter + body per template.

    **Frontmatter:**
    ```yaml
    ---
    phase: 239-rnglocked-invariant-permissionless-sweep
    plan: 239-03
    subsystem: audit
    tags: [v30.0, VRF, RNG-03, asymmetry-re-justification, lootbox-index-advance, phaseTransitionActive, first-principles, fresh-eyes, HEAD-7ab515fe]
    head_anchor: 7ab515fe

    # Dependency graph
    requires:
      - phase: 237-vrf-consumer-inventory-call-graph
        provides: "audit/v30-CONSUMER-INVENTORY.md Consumer Index RNG-03 scope (19-row mid-day-lootbox family per 237-03 Decision 4; +1 INV-237-124 EXC-04 routed via lootbox-index-advance per 238-03)"
    provides:
      - "audit/v30-ASYMMETRY-RE-JUSTIFICATION.md — RNG-03 deliverable per D-13: § Asymmetry A (lootbox index-advance equivalent to flag-based isolation) + § Asymmetry B (phaseTransitionActive admits only advanceGame-origin writes) + Prior-Artifact Cross-Cites + Finding Candidates + Scope-Guard Deferrals + Attestation at HEAD 7ab515fe."
      - "Asymmetry A: <N_A_W> Write Sites + <N_A_R> Read Sites + closed-form Equivalence Proof; consumer set = 20 rows (19 PREFIX-MIDDAY + 1 INV-237-124 EXC-04 per 238-03 Named-Gate distribution)."
      - "Asymmetry B: <N_B_S> Enumerated SSTORE Sites Under phaseTransitionActive = true + Call-Chain Rooting Proof + No Player-Reachable Mutation-Path Proof; all SSTORE sites root at advanceGame."
      - "Phase 238-03 FWD-03 gating Scope-Guard Deferral #1 lootbox-index-advance portion DISCHARGED by this plan commit per D-29. phase-transition-gate portion DISCHARGED by this plan commit per D-29. rngLocked portion separately DISCHARGED by Plan 239-01 commit per D-29."
    affects: [240-gameover-jackpot-safety, 241-exception-closure, 242-findings-consolidation]

    # Tech tracking
    tech-stack:
      added: []
      patterns:
        - "D-13 two-section structure — § Asymmetry A + § Asymmetry B with 6 sub-sections each"
        - "D-14 proof-by-exhaustion from storage primitives at HEAD — NOT proof-by-cite; prior cross-cites carry 'we independently re-derived same result' notes"
        - "D-14 KI-as-SUBJECT discipline — KNOWN-ISSUES.md lootbox-index-advance entry is SUBJECT not warrant"
        - "D-15 RNG-03(a) Asymmetry A is forward-cite target for Plan 239-02 RNG-02 respects-equivalent-isolation rows; Wave 1 parallel"
        - "D-19 gameover-bracket bookkeeping interactions with phaseTransitionActive NOT jackpot-input determinism (Phase 240 GO-02)"
        - "D-20 KI-acceptance re-verification OUT of scope (Phase 241 EXC-04 owns EntropyLib acceptance)"
        - "D-22 no F-30-NN finding-ID emission"
        - "D-25 tabular / grep-friendly / no mermaid"
        - "D-26 HEAD anchor 7ab515fe locked in frontmatter + echoed in Attestation"
        - "D-27 READ-only — zero contracts/ or test/ writes; KNOWN-ISSUES untouched"
        - "D-28 scope-guard deferral for out-of-inventory sites"
        - "D-29 Phase 238 discharge for BOTH lootbox-index-advance (Asymmetry A) + phase-transition-gate (Asymmetry B) portions — evidenced by commit presence; no re-edit of 238 files"

    key-files:
      created:
        - "audit/v30-ASYMMETRY-RE-JUSTIFICATION.md (<LINE_COUNT> lines committed at <COMMIT_SHA> — 7 required sections: Executive Summary / § Asymmetry A (6 sub-sections) / § Asymmetry B (6 sub-sections) / Prior-Artifact Cross-Cites / Finding Candidates / Scope-Guard Deferrals / Attestation)"
        - ".planning/phases/239-rnglocked-invariant-permissionless-sweep/239-03-SUMMARY.md"
      modified: []

    requirements-completed: [RNG-03]

    metrics:
      duration: <FILL>
      completed: 2026-04-19
      tasks_executed: 3
      lines_in_audit_file: <FILL>
      asymmetry_a_write_sites: <N_A_W>
      asymmetry_a_read_sites: <N_A_R>
      asymmetry_b_sstore_sites: <N_B_S>
      commits:
        - sha: <COMMIT_SHA>
          subject: docs(239-03): RNG-03 two asymmetries re-justified from first principles at HEAD 7ab515fe
    ---
    ```

    **Body sections per template:**

    ```markdown
    # Phase 239 Plan 03: RNG-03 Two Asymmetries Re-Justified from First Principles Summary

    **First-principles proof-by-exhaustion re-justification of the two v30.0 asymmetries at HEAD `7ab515fe`: (a) lootbox RNG index-advance equivalent to flag-based isolation; (b) phaseTransitionActive admits only advanceGame-origin writes. Discharges Phase 238-03 FWD-03 gating lootbox-index-advance + phase-transition-gate audit assumptions.**

    ## Performance
    (fill duration + started/completed + tasks + files)

    ## Accomplishments
    - § Asymmetry A equivalence proof via 6 sub-sections: Asymmetry Statement (KI as SUBJECT per D-14) + Storage Primitives (<X> slots enumerated at HEAD) + Write Sites (<N_A_W> rows) + Read Sites (<N_A_R> rows) + Equivalence Proof (closed-form freeze-guarantee via monotonic index advance + mapping-slot atomicity + single-writer VRF coordinator) + Discharge of Phase 238-03 lootbox-index-advance portion (D-29).
    - § Asymmetry B origin proof via 6 sub-sections: Asymmetry Statement + Storage Primitives (set/clear/gate sites at HEAD <file:line>) + Enumerated SSTORE Sites (<N_B_S> rows) + Call-Chain Rooting Proof (every SSTORE's chain roots at advanceGame via grep-verified single-caller of _endPhase) + No Player-Reachable Mutation-Path Proof (exhaustive enumeration over external entry points + single-threaded-EVM argument) + Discharge of Phase 238-03 phase-transition-gate portion (D-29).
    - D-14 proof-by-exhaustion discipline upheld: every claim enumerates specific storage slots + SSTORE sites + call chains at HEAD 7ab515fe; prior-milestone artifacts cross-cited with "we independently re-derived the same result" notes.
    - D-14 KI-as-SUBJECT discipline: KNOWN-ISSUES.md lootbox-index-advance entry named SUBJECT of Asymmetry A (design decision being re-justified), NOT warrant. Proof holds independently of KI entry existence.
    - Phase 238-03 FWD-03 gating Scope-Guard Deferral #1 discharged for BOTH lootbox-index-advance (Asymmetry A) AND phase-transition-gate (Asymmetry B) portions by this plan's commit per D-29. Plan 239-01 separately discharges the rngLocked portion — completing the 3-way discharge of the Phase 238 audit-assumption.

    ## Task Commits
    1. **Task 1 + Task 2 (combined commit): Build § Asymmetry A + § Asymmetry B + Prior-Artifact Cross-Cites + Finding Candidates + Scope-Guard Deferrals + Attestation + commit** — `<COMMIT_SHA>` (docs(239-03): RNG-03 two asymmetries re-justified from first principles at HEAD 7ab515fe). <LINE_COUNT> lines; zero F-30-NN; zero mermaid; zero placeholder tokens; HEAD anchor attested.

    ## Files Created/Modified
    - `audit/v30-ASYMMETRY-RE-JUSTIFICATION.md` (CREATED) — <fill counts>
    - `audit/v30-CONSUMER-INVENTORY.md` (UNCHANGED per D-28 — inventory READ-only after 237 commit)
    - `audit/v30-238-01-BWD.md`, `audit/v30-238-02-FWD.md`, `audit/v30-238-03-GATING.md`, `audit/v30-FREEZE-PROOF.md` (UNCHANGED per D-29 — Phase 238 READ-only; dual audit-assumption discharge evidenced by this commit's presence, not by edits to 238 files)
    - `KNOWN-ISSUES.md` (UNCHANGED per D-27 — Phase 242 FIND-03 owns KI promotions; Asymmetry A treats KI lootbox-index-advance entry as SUBJECT not warrant per D-14)
    - `contracts/`, `test/` (UNCHANGED per D-27 — READ-only audit phase)

    ## Phase 238 Dual-Discharge Note (D-29)

    Phase 238-03 FWD-03 gating (`audit/v30-FREEZE-PROOF.md` Scope-Guard Deferral #1) cited Phase 239 RNG-01 + RNG-03 as audit assumptions pending first-principles re-proof. The deferral split across three gate-taxonomy portions:

    1. **`rngLocked` gate correctness** — DISCHARGED by Plan 239-01 RNG-01 commit (state-machine airtight proof). This plan's SUMMARY does NOT re-attest; cross-check via `.planning/phases/239-rnglocked-invariant-permissionless-sweep/239-01-SUMMARY.md`.

    2. **`lootbox-index-advance` gate correctness** — DISCHARGED by THIS plan's commit (`<COMMIT_SHA>`) via § Asymmetry A first-principles equivalence proof. Phase 238-03 cited this as an audit assumption; Asymmetry A re-derives equivalence-to-flag-based-isolation from storage primitives at HEAD 7ab515fe (monotonic index advance + mapping-slot atomicity + single-writer VRF coordinator). No re-edit of Phase 238 files per D-29.

    3. **`phase-transition-gate` companion-gate correctness** — DISCHARGED by THIS plan's commit (`<COMMIT_SHA>`) via § Asymmetry B call-chain rooting proof. Phase 238-03 cited this as an audit assumption (appears as COMPANION gate in 238-02 Forward Mutation Paths, 0 PRIMARY rows); Asymmetry B re-derives from first principles that every SSTORE under phaseTransitionActive = true roots at advanceGame and no player-reachable mutation path exists. No re-edit of Phase 238 files per D-29.

    Phase 242 REG-01/02 cross-checks all three discharges at milestone consolidation.

    ## D-15 Forward-Cite Coordination (Plan 239-02 RNG-02 respects-equivalent-isolation rows)

    Plan 239-02 RNG-02 `respects-equivalent-isolation` rows forward-cite `audit/v30-ASYMMETRY-RE-JUSTIFICATION.md § Asymmetry A` (this plan's deliverable) by file+section path per D-15. Wave 1 parallel topology: either plan may commit first. Reconciliation status:

    <FILL one of the following based on `ls audit/v30-PERMISSIONLESS-SWEEP.md && git log --oneline -- audit/v30-PERMISSIONLESS-SWEEP.md`:>

    - **If Plan 239-02 has committed and forward-cite format matches:** "Plan 239-02 committed at <SHA>; `respects-equivalent-isolation` row Evidence cells cite `audit/v30-ASYMMETRY-RE-JUSTIFICATION.md § Asymmetry A` — structural match verified; no reconciliation erratum needed."

    - **If Plan 239-02 has NOT committed at time of this SUMMARY:** "Plan 239-02 not yet committed at time of 239-03 commit `<COMMIT_SHA>`; § Asymmetry A structure provided here matches Plan 239-02's forward-cite expectations per CONTEXT.md D-13 (section heading + Equivalence Proof sub-section + consumer-set cross-reference to 19 PREFIX-MIDDAY + 1 EXC-04). If structural divergence surfaces post-239-02-commit, erratum recorded in 239-02-SUMMARY per D-15 (not this file per D-16 READ-only-after-commit)."

    - **If Plan 239-02 committed with divergent forward-cite format:** "Plan 239-02 committed at <SHA>; format divergence detected: <detail>. Reconciliation erratum: <detail + routing recommendation to Phase 242 FIND-01>. 239-02-SUMMARY records erratum; this file is READ-only post-commit per D-16."

    ## Decisions Made
    (fill 3-5 decisions Claude made during execution — Claude's Discretion items in CONTEXT.md: whether both asymmetry proofs share a common-form "freeze-proof equivalence template" or are written ab initio; whether severity pre-classified or TBD-242; etc.)

    ## Deviations from Plan
    (fill with any deviations, or "None — plan executed exactly as written.")

    ## Issues Encountered
    (fill with any issues, or "None.")

    ## User Setup Required
    None — no external service configuration. Deliverable is markdown-only under `audit/`.

    ## Next Phase Readiness
    **Phase 239 Plan 03 complete (RNG-03 closed). Combined with Plans 239-01 (RNG-01 closed) + 239-02 (RNG-02 closed), Phase 239 complete — 3 requirements satisfied; all 3 portions of Phase 238-03 Scope-Guard Deferral #1 discharged. Phases 240 / 241 / 242 unblocked per ROADMAP dependency graph.**

    ## Self-Check: PASSED
    (fill checklist items — all must-haves truths satisfied; see Attestation section of audit file; verify 3-way discharge: rngLocked via 239-01 + lootbox-index-advance via 239-03 § A + phase-transition-gate via 239-03 § B)
    ```

    Replace `<COMMIT_SHA>`, `<LINE_COUNT>`, `<N_A_W>`, `<N_A_R>`, `<N_B_S>`, `<X>`, `<FILL>` placeholders with actual values.

    **Commit:**
    Stage `.planning/phases/239-rnglocked-invariant-permissionless-sweep/239-03-SUMMARY.md`. Commit with message:
    ```
    docs(239-03): SUMMARY — RNG-03 asymmetries re-justified; Phase 238 dual-portion discharge noted (lootbox-index-advance + phase-transition-gate)
    ```

    Do NOT push. Do NOT amend. Do NOT use --no-verify.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-03-SUMMARY.md
      grep -qE '^phase: 239-rnglocked-invariant-permissionless-sweep' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-03-SUMMARY.md
      grep -qE '^plan: 239-03' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-03-SUMMARY.md
      grep -qE '^head_anchor: 7ab515fe' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-03-SUMMARY.md
      grep -qE 'requirements-completed:.*RNG-03' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-03-SUMMARY.md

      # Phase 238 dual-discharge section present
      grep -qE 'Phase 238 Dual-Discharge|Dual-Discharge|D-29' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-03-SUMMARY.md
      grep -qiE 'lootbox-index-advance' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-03-SUMMARY.md
      grep -qiE 'phase-transition-gate' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-03-SUMMARY.md
      grep -qiE 'rngLocked.*239-01|239-01.*rngLocked' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-03-SUMMARY.md

      # D-15 forward-cite coordination section present
      grep -qE 'D-15|Plan 239-02|Forward-Cite Coordination' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-03-SUMMARY.md

      # Placeholders filled in
      ! grep -q '<COMMIT_SHA>' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-03-SUMMARY.md
      ! grep -q '<LINE_COUNT>' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-03-SUMMARY.md
      ! grep -qE '<N_[A-Z_]+>' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-03-SUMMARY.md
      ! grep -q '<FILL>' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-03-SUMMARY.md

      # Zero F-30-NN per D-22
      ! grep -qE 'F-30-[0-9]' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-03-SUMMARY.md

      # READ-only scope per D-27
      test -z "$(git status --porcelain contracts/ test/)"

      # Phase 237/238 outputs + KNOWN-ISSUES untouched per D-28/D-29
      test -z "$(git status --porcelain audit/v30-238-01-BWD.md audit/v30-238-02-FWD.md audit/v30-238-03-GATING.md audit/v30-FREEZE-PROOF.md audit/v30-CONSUMER-INVENTORY.md KNOWN-ISSUES.md 2>/dev/null || true)"

      # Commit landed — subject prefix docs(239-03):
      git log --oneline -1 | grep -qE '^[0-9a-f]+ docs\(239-03\):'
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/239-rnglocked-invariant-permissionless-sweep/239-03-SUMMARY.md` exists with full frontmatter: `phase`, `plan`, `subsystem`, `tags`, `head_anchor: 7ab515fe`, `requires`, `provides`, `affects`, `tech-stack.patterns` (≥ 11 items), `key-files.created` (≥ 2 items), `requirements-completed: [RNG-03]`, `metrics.asymmetry_a_write_sites: <N_A_W>`, `metrics.asymmetry_a_read_sites: <N_A_R>`, `metrics.asymmetry_b_sstore_sites: <N_B_S>`.
    - Body includes `## Performance`, `## Accomplishments`, `## Task Commits`, `## Files Created/Modified`, `## Phase 238 Dual-Discharge Note (D-29)` (explicitly addresses all 3 portions: rngLocked via 239-01, lootbox-index-advance via this plan § A, phase-transition-gate via this plan § B), `## D-15 Forward-Cite Coordination (Plan 239-02 RNG-02 respects-equivalent-isolation rows)`, `## Decisions Made`, `## Deviations from Plan`, `## Issues Encountered`, `## User Setup Required`, `## Next Phase Readiness`, `## Self-Check: PASSED`.
    - Phase 238 Dual-Discharge Note explicitly names all three portions and their discharging plan (239-01 for rngLocked, 239-03 § A for lootbox-index-advance, 239-03 § B for phase-transition-gate).
    - D-15 Forward-Cite Coordination section explicitly addresses the 3 possible states (Plan 239-02 committed-matching / not-committed / committed-diverging) and selects one.
    - No literal placeholder tokens remain.
    - `grep -E 'F-30-[0-9]'` returns zero matches per D-22.
    - Commit with subject matching `^docs\(239-03\):` prefix.
    - `git status --porcelain contracts/ test/` empty per D-27.
    - `git status --porcelain audit/v30-238-*.md audit/v30-FREEZE-PROOF.md audit/v30-CONSUMER-INVENTORY.md KNOWN-ISSUES.md` empty per D-28/D-29.
  </acceptance_criteria>
  <done>
    SUMMARY committed with all placeholders filled; Phase 238 Dual-Discharge Note explicitly attests discharge of all three portions (rngLocked via 239-01 + lootbox-index-advance via § A + phase-transition-gate via § B); D-15 forward-cite coordination status explicitly addressed for 239-02 coordination; Phase 237 inventory + Phase 238 outputs + KNOWN-ISSUES untouched; clean commit on main.
  </done>
</task>

</tasks>

<verification>
- `audit/v30-ASYMMETRY-RE-JUSTIFICATION.md` exists with 7 required sections; § Asymmetry A (6 sub-sections) + § Asymmetry B (6 sub-sections) both use proof-by-exhaustion per D-14; KI entries as SUBJECT not warrant; D-29 discharge explicit for both lootbox-index-advance + phase-transition-gate portions.
- `.planning/phases/239-rnglocked-invariant-permissionless-sweep/239-03-SUMMARY.md` exists with HEAD anchor, Phase 238 dual-discharge note (3-way attestation across 239-01 + 239-03 § A + 239-03 § B), D-15 RNG-02 forward-cite coordination status, full frontmatter.
- Zero F-30-NN per D-22. Zero contracts/ or test/ writes per D-27. Zero edits to Phase 237 inventory / Phase 238 outputs / KNOWN-ISSUES per D-28/D-29.
- Exactly 1-2 new commits on `main` with subject prefix `docs(239-03):`.
</verification>

<success_criteria>
- RNG-03 requirement satisfied: both asymmetries re-justified from first principles at HEAD 7ab515fe via proof-by-exhaustion (D-14); § Asymmetry A proves lootbox index-advance equivalent to flag-based isolation; § Asymmetry B proves phaseTransitionActive exemption admits only advanceGame-origin writes.
- D-14 proof-by-exhaustion discipline upheld: every claim enumerates specific storage slots + SSTORE sites + call chains at HEAD 7ab515fe.
- D-14 KI-as-SUBJECT discipline upheld: KNOWN-ISSUES.md lootbox-index-advance entry named SUBJECT (re-justified design decision), NOT warrant.
- Phase 238-03 FWD-03 gating Scope-Guard Deferral #1 discharged for BOTH lootbox-index-advance (§ A) AND phase-transition-gate (§ B) portions; combined with Plan 239-01's rngLocked discharge, this closes the full audit-assumption.
- Fresh-eyes mandate upheld; prior-milestone artifacts cross-cited with "we independently re-derived same result" notes (D-14 format).
- Finding Candidates appendix routes any observations to Phase 242 FIND-01; zero F-30-NN IDs emitted.
- READ-only scope preserved across all tasks.
</success_criteria>

<output>
After completion, `audit/v30-ASYMMETRY-RE-JUSTIFICATION.md` + `.planning/phases/239-rnglocked-invariant-permissionless-sweep/239-03-SUMMARY.md` are committed on `main` at HEAD descendant of `7ab515fe`. Combined with Plan 239-01 + Plan 239-02 commits (Wave 1 parallel per D-02), Phase 239 complete — RNG-01/02/03 all satisfied, Phase 238 Scope-Guard Deferral #1 fully discharged. Phases 240 / 241 unblocked (parallel with 239 per ROADMAP); Phase 242 requires 238+239+240+241.
</output>
