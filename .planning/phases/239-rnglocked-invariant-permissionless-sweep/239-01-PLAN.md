---
phase: 239-rnglocked-invariant-permissionless-sweep
plan: 239-01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v30-RNGLOCK-STATE-MACHINE.md
  - .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-01-SUMMARY.md
autonomous: true
requirements: [RNG-01]
requirements_addressed: [RNG-01]
head_anchor: 7ab515fe
head_sha: 7ab515fe
must_haves:
  truths:
    - "`audit/v30-RNGLOCK-STATE-MACHINE.md` exists with these exact four top-level sections in this exact order per D-04: `## Set-Site Table`, `## Clear-Site Table`, `## Path Enumeration Table`, `## Invariant Proof`, plus mandatory appendix sections `## Prior-Artifact Cross-Cites`, `## Finding Candidates`, `## Scope-Guard Deferrals`, `## Attestation`."
    - "Per D-04 the Set-Site Table has exactly these 6 columns in this exact order: `Site ID | File:Line | Function | Trigger Context | Companion Clear Path(s) | Verdict`. Site ID format `RNGLOCK-239-S-NN` (two-digit zero-padded). One row per `rngLockedFlag = true` SSTORE discovered via fresh grep at HEAD `7ab515fe` over `contracts/` (excluding `contracts/mocks/`)."
    - "Per D-04 the Clear-Site Table has exactly these 6 columns in this exact order: `Site ID | File:Line | Function | Trigger Context | Companion Set Path(s) | Verdict`. Site ID format `RNGLOCK-239-C-NN`. One row per `rngLockedFlag = false` SSTORE (direct assignment) OR every structural clear site whose effect is to return `rngLockedFlag` to the cleared state (e.g., via `_unlockRng` or equivalent helper). The L1700 `if (rngLockedFlag) { ... }` branch in `rawFulfillRandomWords` is enumerated as a Clear-Site Ref per D-06 (its revert-safety role is cross-referenced in the Path Enumeration Table)."
    - "Per D-05 every cell in both Set-Site and Clear-Site Verdict columns is exactly one of `{AIRTIGHT, CANDIDATE_FINDING}`. No hedged, pending, conditional, or narrative-only verdicts permitted. If a site cannot be proven airtight, the verdict is `CANDIDATE_FINDING` and the site produces a Finding Candidate block per D-23."
    - "Per D-04 the Path Enumeration Table has exactly these 7 columns in this exact order: `Path ID | Set-Site Ref | Entry Condition | Early-Return Points | Revert Points | Clear-Site Ref | Verdict`. Path ID format `RNGLOCK-239-P-NNN` (three-digit zero-padded). Every reachable execution path from each Set-Site through every early-return / revert branch to a matching Clear-Site is enumerated exhaustively (not sampled). Verdict cell is exactly one of `{SET_CLEARS_ON_ALL_PATHS, CLEAR_WITHOUT_SET_UNREACHABLE, CANDIDATE_FINDING}` per D-04/D-05."
    - "Per D-06 `rawFulfillRandomWords` revert-safety is enumerated as at least one dedicated Path Enumeration row whose verdict accounts for the L1700 branch behavior: when a stale/post-clear VRF delivery arrives, the flag cannot be left `true` indefinitely. Cross-cites v3.7 Phase 63 `rawFulfillRandomWords` revert-safety proof with `re-verified at HEAD 7ab515fe` note per D-17 (corroborating, not load-bearing)."
    - "Per D-07 the VRF request-retry + 12h timeout path is enumerated as at least one dedicated Path Enumeration row. This is the `clear-without-matching-new-set` semantics case (a re-request clears the prior request context without a new `rngLockedFlag = true` SSTORE because the flag is already set). Verdict cell explicitly handles this semantics and is one of `{SET_CLEARS_ON_ALL_PATHS, CLEAR_WITHOUT_SET_UNREACHABLE, CANDIDATE_FINDING}`. Cross-cites v2.1 VRF retry timeout + v3.7 lifecycle audit + v29.0 Phase 235-05-TRNX-01.md 4-path walk with `re-verified at HEAD 7ab515fe` notes per D-17."
    - "Per D-19 the gameover VRF-request-bracket set/clear bookkeeping IS enumerated in the Path Enumeration Table (rngLockedFlag set around gameover VRF request + clear on fulfillment / timeout-path). Jackpot-input determinism proofs are OUT of scope — those are Phase 240 GO-02. Phase 239 only enumerates state-machine bookkeeping around the gameover VRF request."
    - "Per D-04 the `## Invariant Proof` section contains a closed-form argument with both directions of the biconditional: (1) `∀ reachable path P originating at any Set-Site S, P terminates at a matching Clear-Site C before control returns to any external caller OR via a revert that rolls back the Set-Site SSTORE`; (2) `∀ reachable path Q terminating at any Clear-Site C, Q is downstream of a matched Set-Site S with no Clear-Site-without-matching-Set ambiguity`. The proof cites row IDs from the two tables (Set-Site + Clear-Site) and Path IDs from the Path Enumeration Table — no hand-waving, no `seems correct` language."
    - "Per D-16/D-17 a `## Prior-Artifact Cross-Cites` section lists cross-cites to: v29.0 Phase 235-05 `.planning/milestones/v29.0-phases/235-conservation-rng-commitment-re-proof-phase-transition/235-05-TRNX-01.md` (4-path walk); v3.7 Phase 63 `rawFulfillRandomWords` revert-safety (corroborating D-06); v3.8 Phases 68-72 commitment window 51/51 SAFE (structural baseline); v25.0 Phase 215 RNG fresh-eyes SOUND verdict; v29.0 Phase 232.1-03-PFTB-AUDIT.md non-zero-entropy guarantees. Every cite carries `re-verified at HEAD 7ab515fe` note with a one-line structural-equivalence statement."
    - "Per D-22 NO `F-30-NN` finding IDs appear anywhere in `audit/v30-RNGLOCK-STATE-MACHINE.md`. Phase 242 FIND-01..03 owns ID assignment. `grep -E 'F-30-[0-9]' audit/v30-RNGLOCK-STATE-MACHINE.md` returns zero matches."
    - "Per D-23 the `## Finding Candidates` appendix lists every row whose verdict is `CANDIDATE_FINDING` as a structured Finding Candidate block with `ROW_REF` + `FILE:LINE` citation + `SEVERITY: TBD-242` + observation text. If no `CANDIDATE_FINDING` rows exist, the section states `None surfaced.` explicitly."
    - "Per D-27 READ-only scope holds throughout all tasks: `git diff --name-only 7ab515fe..HEAD -- contracts/ test/` returns empty at task start and end; `git status --porcelain contracts/ test/` returns empty before and after each task; `KNOWN-ISSUES.md` is not edited."
    - "Per D-26 HEAD anchor `7ab515fe` is locked in frontmatter + echoed in `audit/v30-RNGLOCK-STATE-MACHINE.md` `Audit baseline` line + in the plan SUMMARY frontmatter + in the Attestation section."
    - "Per D-28 if any rngLockedFlag SSTORE site or execution path is discovered that is not represented in `audit/v30-CONSUMER-INVENTORY.md` — inventory is NOT edited; instead a scope-guard deferral row in `## Scope-Guard Deferrals` records `file:line + context + proposed inventory delta + SEVERITY: TBD-242` and the gap routes to Phase 242 FIND-01."
    - "Per D-29 no re-edit of Phase 238 output: `git status --porcelain audit/v30-238-01-BWD.md audit/v30-238-02-FWD.md audit/v30-238-03-GATING.md audit/v30-FREEZE-PROOF.md` returns empty at task end. Discharge of Phase 238 Scope-Guard Deferral #1 audit assumption is evidenced by Plan 239-01 commit presence — no re-edit of 238 files."
    - "Set-Site Table row count is ≥ 1 and matches the count of unique `rngLockedFlag = true` SSTORE lines in `contracts/` (excluding mocks) at HEAD `7ab515fe` (verifiable via `grep -rn 'rngLockedFlag = true' contracts/ --include='*.sol' | grep -v mocks`). Clear-Site Table row count similarly matches `rngLockedFlag = false` SSTORE lines."
    - "All tasks commit in a sequence ending with exactly one new line in `git log --oneline` per commit, with commit subjects matching `docs(239-01):` prefix regex. No `--no-verify` flag used; no force-push; no push to remote."
  artifacts:
    - path: "audit/v30-RNGLOCK-STATE-MACHINE.md"
      provides: "RNG-01 state-machine airtight proof — Set-Site Table + Clear-Site Table + Path-Enumeration Table + Invariant Proof + Finding Candidates (D-05 closed verdict taxonomy; D-06 L1700 revert-safety; D-07 12h retry-timeout path; D-19 gameover-bracket bookkeeping)"
      contains: "## Set-Site Table"
    - path: ".planning/phases/239-rnglocked-invariant-permissionless-sweep/239-01-SUMMARY.md"
      provides: "Plan 239-01 summary — HEAD 7ab515fe attestation, Set/Clear site counts, Path count, verdict distribution, discharge note for Phase 238 D-13 rngLocked audit assumption"
      contains: "HEAD `7ab515fe`"
  key_links:
    - from: "contracts/modules/DegenerusGameAdvanceModule.sol rngLockedFlag SSTORE sites (set sites + clear sites discovered via grep at HEAD 7ab515fe)"
      to: "audit/v30-RNGLOCK-STATE-MACHINE.md Set-Site Table + Clear-Site Table"
      via: "D-04 fresh re-enumeration — every SSTORE at HEAD gets a row; no reliance on prior-milestone counts"
      pattern: "rngLockedFlag\\s*=\\s*(true|false)"
    - from: "CONTEXT.md D-05 closed verdict taxonomy {AIRTIGHT / CANDIDATE_FINDING}"
      to: "Set-Site / Clear-Site / Path Enumeration Verdict columns"
      via: "D-05 — any row unable to be proven airtight is CANDIDATE_FINDING, not a hedged narrative"
      pattern: "AIRTIGHT|CANDIDATE_FINDING"
    - from: "Phase 238-03 FWD-03 gating Scope-Guard Deferral #1 (rngLocked audit assumption pending Phase 239 RNG-01 re-proof)"
      to: "Plan 239-01 commit (discharge evidenced by presence, no re-edit of Phase 238 output per D-29)"
      via: "D-29 discharge mechanism — Phase 242 REG-01/02 cross-checks discharge at milestone consolidation"
      pattern: "rngLocked"
    - from: "v29.0 Phase 235-05-TRNX-01.md 4-path rngLocked walk + v3.7 Phase 63 rawFulfillRandomWords revert-safety + v3.8 commitment window"
      to: "## Prior-Artifact Cross-Cites (re-verified at HEAD 7ab515fe)"
      via: "D-16/D-17 corroborating cross-cite pattern — NOT relied upon; every cell in Phase 239-01 is re-derived fresh"
      pattern: "re-verified at HEAD 7ab515fe"
---

<objective>
RNG-01 airtight proof of the `rngLockedFlag` state machine: every set site, every clear site, every reachable execution path from each set to a matching clear (including early-returns and reverts), enumerated exhaustively at HEAD `7ab515fe` with a closed-form invariant proof that `no reachable path produces set-without-clear or clear-without-matching-set`.

**Single requirement satisfied (directly):**
- **RNG-01** — `rngLockedFlag` state machine airtight: every set site + every clear site + every early-return/revert path enumerated; no reachable path produces set-without-clear or clear-without-matching-set. Per D-05 every row verdict is `AIRTIGHT` or `CANDIDATE_FINDING`. Per D-06 `rawFulfillRandomWords` L1700 revert-safety branch IS in scope. Per D-07 VRF request-retry + 12h timeout path IS in scope.

**Deliverable (per D-24):**
- `audit/v30-RNGLOCK-STATE-MACHINE.md` — Set-Site Table + Clear-Site Table + Path-Enumeration Table + Invariant Proof + Prior-Artifact Cross-Cites + Finding Candidates + Scope-Guard Deferrals + Attestation

**Wave 1 parallel topology (per D-02):** This plan has zero cross-plan dependencies at HEAD. Runs concurrently with 239-02 and 239-03. No prerequisite on Phase 238 (Phase 238 cites Phase 239 RNG-01 as audit assumption; this plan's commit discharges that assumption per D-29 — no re-edit of 238 files).

**Fresh-eyes mandate (per D-16/D-17):** Every Set-Site / Clear-Site / Path is re-enumerated from `contracts/` at HEAD `7ab515fe`. Prior-milestone artifacts (v29.0 Phase 235-05 `235-05-TRNX-01.md`, v3.7 Phase 63, v3.8 Phases 68-72, v25.0 Phase 215, Phase 232.1-03-PFTB-AUDIT) CROSS-CITED as corroborating evidence only — NOT relied upon. Every cite carries `re-verified at HEAD 7ab515fe` note with one-line structural-equivalence statement.

**Scope containment (per D-18/D-19/D-20):**
- IN scope: rngLockedFlag state machine (global invariant) — set sites, clear sites, all reachable paths, L1700 revert-safety branch, 12h retry-timeout path, gameover-bracket bookkeeping around gameover VRF request.
- OUT of scope: per-consumer freeze proofs (Phase 238); gameover jackpot-input determinism (Phase 240 GO-02); KI-exception acceptance re-verification (Phase 241); regression / finding-ID assignment (Phase 242).

**No F-30-NN emission (per D-22):** `CANDIDATE_FINDING` rows produce Finding Candidate blocks; Phase 242 FIND-01..03 owns ID assignment.

**READ-only scope (per D-27):** Only writes: `audit/v30-RNGLOCK-STATE-MACHINE.md` + `.planning/phases/239-rnglocked-invariant-permissionless-sweep/239-01-SUMMARY.md`. `KNOWN-ISSUES.md` is NOT touched. `contracts/` / `test/` are NOT touched. Phase 237 inventory + Phase 238 outputs READ-only.

**Phase 238 audit-assumption discharge (per D-29):** Phase 238-03 FWD-03 gating cited `rngLocked` gate correctness as audit assumption pending Phase 239 RNG-01 re-proof (Scope-Guard Deferral #1 in `audit/v30-FREEZE-PROOF.md`). Discharge is evidenced by Plan 239-01 commit presence — no re-edit of Phase 238 files. Phase 242 REG-01/02 cross-checks the discharge at milestone consolidation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/239-rnglocked-invariant-permissionless-sweep/239-CONTEXT.md
@audit/v30-CONSUMER-INVENTORY.md
@audit/v30-FREEZE-PROOF.md
@audit/v30-238-03-GATING.md
@KNOWN-ISSUES.md
</context>

<threat_model>
N/A — READ-only audit milestone per D-27. No runtime code modified, no test modified, no `contracts/` or `test/` writes. Attack surface added: NONE. Only writes: markdown under `audit/v30-RNGLOCK-STATE-MACHINE.md` + `.planning/` SUMMARY. No STRIDE category applies to documentation-only output; threat register empty by construction.

Residual audit-quality risk: an incorrectly airtight-classified path (assigning `SET_CLEARS_ON_ALL_PATHS` when an unenumerated early-return leaves the flag set) masks a real state-machine violation from Phase 242 FIND-01 intake. Mitigation: (a) Set-Site + Clear-Site tables must each match the grep-count of their SSTORE pattern at HEAD before verdicts are assigned; (b) Path Enumeration Table row count must ≥ sum of (every early-return point + every revert point) between each set/clear pair; (c) the closed biconditional in `## Invariant Proof` forces both directions — set→clear AND clear←set matched; (d) D-06 forces L1700 enumeration as a dedicated Path row (historical audit miss: L1700 branch treated as implicit); (e) D-07 forces 12h retry-timeout enumeration as a dedicated Path row (historical audit miss: retry-without-matching-set semantics); (f) D-05 closed verdict taxonomy means any audit-ambiguous row is `CANDIDATE_FINDING`, not a hedged narrative deferral.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate Set-Site Table + Clear-Site Table at HEAD 7ab515fe via fresh grep over contracts/</name>
  <files>audit/v30-RNGLOCK-STATE-MACHINE.md</files>
  <read_first>
    - .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-CONTEXT.md (D-04 table shape; D-05 closed verdict taxonomy; D-06 L1700 branch in scope; D-17 re-verified-at-HEAD note; D-18/D-19/D-20 scope exclusions; D-22 no F-30-NN; D-25 tabular/grep-friendly/no-mermaid; D-26 HEAD anchor; D-27 READ-only)
    - .planning/REQUIREMENTS.md (RNG-01 exact wording — "Prove the `rngLockedFlag` set/clear state machine is airtight across all set sites, all clear sites, and all early-return / revert paths. No set-without-clear or clear-without-matching-set.")
    - .planning/ROADMAP.md Phase 239 block (Success Criterion 1 — names `audit/v30-RNGLOCK-STATE-MACHINE.md` as the RNG-01 deliverable)
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD 7ab515fe — pre-scan per CONTEXT.md <code_context>: set site near L1579 (`rngLockedFlag = true;`), clear sites near L1635 + L1676 (`rngLockedFlag = false;`), revert guard near L1031 (`if (rngLockedFlag) revert RngLocked();`), read-surface near L177 (purchase-level branch `(lastPurchase && rngLockedFlag)`), L1700 branch (`if (rngLockedFlag) { ... }` inside `rawFulfillRandomWords`) — VERIFY ALL LINE NUMBERS VIA FRESH GREP per D-11 methodology (do NOT rely on CONTEXT.md approximations; grep at HEAD and cite exact lines)
    - contracts/DegenerusGame.sol at HEAD — pre-scan: 6 occurrences of `rngLockedFlag` / `rngLocked` (read-surface via `rngLocked()` view)
    - contracts/storage/DegenerusGameStorage.sol at HEAD — pre-scan: 8 occurrences (storage declaration + internal uses)
    - contracts/modules/DegenerusGameMintModule.sol at HEAD — pre-scan: 1 occurrence
    - contracts/modules/DegenerusGameWhaleModule.sol at HEAD — pre-scan: 1 occurrence
    - contracts/StakedDegenerusStonk.sol + contracts/DegenerusStonk.sol + contracts/BurnieCoinflip.sol + contracts/BurnieCoin.sol + contracts/interfaces/IDegenerusGame.sol — read-surface consumers of `rngLocked()` view (NOT set/clear sites; inform read-surface enumeration only)
    - audit/v30-CONSUMER-INVENTORY.md §"Universe List" (146 INV-237-NNN rows) + §"Consumer Index" RNG-01 scope row (94-row daily + infrastructure subset per 237-03 Decision 3) — confirms scope anchor for the state-machine; READ-only per D-28
    - audit/v30-FREEZE-PROOF.md §"Scope-Guard Deferrals" entry #1 (cites Phase 239 RNG-01 as audit assumption — this plan discharges it per D-29)
    - audit/v30-238-03-GATING.md §"Gating Verification Table" (Named Gate `rngLocked` = 106 rows per 238-03 SUMMARY — confirms rngLocked gate distribution this plan re-proves; READ-only per D-29)
    - .planning/milestones/v29.0-phases/235-conservation-rng-commitment-re-proof-phase-transition/235-05-TRNX-01.md — v29.0 4-path rngLocked walk at v29.0 baseline `1646d5af` (corroborating cross-cite per D-16; NOT relied upon; re-enumerate at HEAD independently)
  </read_first>
  <action>
    Create `audit/v30-RNGLOCK-STATE-MACHINE.md` with exactly these top-level sections in this exact order:

    ```
    # v30.0 rngLockedFlag State Machine — Airtight Proof (RNG-01)

    **Audit baseline:** HEAD `7ab515fe`
    **Plan:** 239-01
    **Requirement:** RNG-01

    ## Executive Summary
    ## State-Machine Overview (Prose Diagram)
    ## Set-Site Table
    ## Clear-Site Table
    ## Path Enumeration Table
    ## Invariant Proof
    ## Prior-Artifact Cross-Cites
    ## Finding Candidates
    ## Scope-Guard Deferrals
    ## Attestation
    ```

    Then populate in this order:

    **Step 1 — Fresh grep discovery of Set-Site + Clear-Site sites at HEAD 7ab515fe:**
    Run these mechanical greps and include commit-time output in an inline comment block directly above the Set-Site Table for reviewer sanity-check (Claude's Discretion per CONTEXT.md encouragement):

    ```
    grep -rn 'rngLockedFlag\s*=\s*true' contracts/ --include='*.sol' | grep -v mocks
    grep -rn 'rngLockedFlag\s*=\s*false' contracts/ --include='*.sol' | grep -v mocks
    grep -rn 'if (rngLockedFlag)' contracts/ --include='*.sol' | grep -v mocks
    grep -rn '_unlockRng' contracts/ --include='*.sol' | grep -v mocks
    grep -rn '\brngLocked\s*\(' contracts/ --include='*.sol' | grep -v mocks
    ```

    Results at HEAD should include the CONTEXT.md pre-scan surfaces (verify by exact line numbers — do NOT copy CONTEXT.md line numbers; cite whatever grep returns at HEAD):
    - Set: `DegenerusGameAdvanceModule.sol:1579` (`rngLockedFlag = true;`)
    - Clear: `DegenerusGameAdvanceModule.sol:1635`, `DegenerusGameAdvanceModule.sol:1676` (both `rngLockedFlag = false;`)
    - Revert guard: `DegenerusGameAdvanceModule.sol:1031` (`if (rngLockedFlag) revert RngLocked();`) — READ-side, not a set/clear site; inform Path Enumeration Entry Condition column
    - L1700 branch: `DegenerusGameAdvanceModule.sol:1700` (`if (rngLockedFlag) { ... }` inside `rawFulfillRandomWords`) — per D-06 IS a Clear-Site Ref (the branch-structure ensures flag cannot remain set on stale delivery); enumerate as Clear-Site row with its own Path Enumeration coverage
    - Purchase-level branch: `DegenerusGameAdvanceModule.sol:177` — READ-side, not set/clear; inform state-machine read-surface (not a row in Set-Site / Clear-Site tables)

    **Step 2 — Build `## Executive Summary` (3-5 bullets):**
    - Row counts: Set-Site Table = N_set rows, Clear-Site Table = N_clear rows, Path Enumeration Table = N_path rows.
    - Verdict distribution: `AIRTIGHT` = X, `CANDIDATE_FINDING` = Y (total = N_set + N_clear + N_path).
    - Gameover-VRF-bracket bookkeeping coverage (D-19): enumerated as path rows, jackpot-input determinism out-of-scope (Phase 240).
    - Discharge note (D-29): Phase 238-03 FWD-03 gating audit assumption for `rngLocked` gate correctness is discharged by this plan's commit; no re-edit of 238 files.
    - Attestation summary: HEAD `7ab515fe`, READ-only, zero F-30-NN.

    **Step 3 — Build `## State-Machine Overview (Prose Diagram)`:**
    Per CONTEXT.md Claude's Discretion, include a short prose diagram: narrative description of the set/clear lifecycle (e.g., `Set @ _finalizeRngRequest L1579 → VRF request fires → rawFulfillRandomWords callback → either Clear @ L1635 (daily-fulfillment branch) OR Clear @ L1676 (retry branch) OR L1700 conditional write of rngWordCurrent (daily branch structure)`). No mermaid (D-25). Grep-friendly only.

    **Step 4 — Build `## Set-Site Table` with exactly these 6 columns in this exact order per D-04:**
    `Site ID | File:Line | Function | Trigger Context | Companion Clear Path(s) | Verdict`

    - Site ID format: `RNGLOCK-239-S-NN` (two-digit zero-padded, starting at `S-01`).
    - One row per `rngLockedFlag = true` SSTORE site at HEAD 7ab515fe (grep result from Step 1).
    - `Trigger Context` cell: one-line description of what external/internal condition reaches this site (e.g., "Daily advanceGame path fires `_finalizeRngRequest` after VRF request conditions satisfied" — cite specific function name + call-chain root).
    - `Companion Clear Path(s)` cell: comma-separated Clear-Site IDs (`RNGLOCK-239-C-NN`) reachable from this set via the Path Enumeration Table (forward reference resolved after Clear-Site Table built).
    - `Verdict` cell: exactly `AIRTIGHT` or `CANDIDATE_FINDING` per D-05. No hedged verdicts.

    **Step 5 — Build `## Clear-Site Table` with exactly these 6 columns in this exact order per D-04:**
    `Site ID | File:Line | Function | Trigger Context | Companion Set Path(s) | Verdict`

    - Site ID format: `RNGLOCK-239-C-NN` (two-digit zero-padded, starting at `C-01`).
    - One row per `rngLockedFlag = false` SSTORE site + the L1700 branch Clear-Site-Ref per D-06 (verdict cell describes why L1700 branch constitutes a clear-site-equivalent via control-flow semantics even without a direct SSTORE — because the branch routes to either the daily-clear path or the mid-day-non-clear path, both of which preserve flag correctness).
    - Every revert path that rolls back a Set-Site SSTORE also enumerated in this table (revert = implicit clear via EVM transaction revert).
    - `Companion Set Path(s)` cell: comma-separated Set-Site IDs reachable to this clear.
    - `Verdict` cell: exactly `AIRTIGHT` or `CANDIDATE_FINDING` per D-05.

    **Step 6 — Inline comment block above the Attestation section: grep command reproducibility:**
    Include the Step 1 grep commands as a fenced code block under a `## Grep Commands (reproducibility)` sub-section so reviewers can re-run at HEAD. Per Claude's Discretion encouragement in CONTEXT.md for Plan 02 — applying same encouragement here for RNG-01 reviewer sanity-check.

    **Frontmatter of audit file:** include `audit_baseline: 7ab515fe` + `plan: 239-01` + `requirement: RNG-01`.

    Do NOT yet build the Path Enumeration Table or Invariant Proof — those are Task 2. Do NOT yet produce SUMMARY — that is Task 3. Do NOT commit — commit is part of Task 2 so the file lands with both tables + path enumeration + invariant proof in a single commit.
  </action>
  <verify>
    <automated>
      # File exists with mandatory sections
      test -f audit/v30-RNGLOCK-STATE-MACHINE.md
      grep -q '^# v30.0 rngLockedFlag State Machine' audit/v30-RNGLOCK-STATE-MACHINE.md
      grep -q '^## Executive Summary$' audit/v30-RNGLOCK-STATE-MACHINE.md
      grep -q '^## Set-Site Table$' audit/v30-RNGLOCK-STATE-MACHINE.md
      grep -q '^## Clear-Site Table$' audit/v30-RNGLOCK-STATE-MACHINE.md
      grep -q '^audit_baseline: 7ab515fe' audit/v30-RNGLOCK-STATE-MACHINE.md

      # Set-Site Table has ≥ 1 row with RNGLOCK-239-S-NN Site ID + AIRTIGHT/CANDIDATE_FINDING verdict
      grep -E '\| RNGLOCK-239-S-[0-9]{2} \|' audit/v30-RNGLOCK-STATE-MACHINE.md | grep -E '\b(AIRTIGHT|CANDIDATE_FINDING)\b' | head -1

      # Clear-Site Table has ≥ 1 row with RNGLOCK-239-C-NN Site ID + verdict
      grep -E '\| RNGLOCK-239-C-[0-9]{2} \|' audit/v30-RNGLOCK-STATE-MACHINE.md | grep -E '\b(AIRTIGHT|CANDIDATE_FINDING)\b' | head -1

      # Set-Site row count matches grep count of `rngLockedFlag = true` SSTOREs in contracts/ (excluding mocks)
      SET_SSTORES=$(grep -rn 'rngLockedFlag\s*=\s*true' contracts/ --include='*.sol' | grep -v mocks | wc -l)
      SET_ROWS=$(grep -cE '^\| RNGLOCK-239-S-[0-9]{2} \|' audit/v30-RNGLOCK-STATE-MACHINE.md)
      test "$SET_ROWS" -ge "$SET_SSTORES"

      # Clear-Site row count ≥ grep count of `rngLockedFlag = false` SSTOREs in contracts/ (excluding mocks)
      # (≥ because L1700 branch per D-06 may add one Clear-Site-Ref row that isn't a literal `= false` SSTORE)
      CLEAR_SSTORES=$(grep -rn 'rngLockedFlag\s*=\s*false' contracts/ --include='*.sol' | grep -v mocks | wc -l)
      CLEAR_ROWS=$(grep -cE '^\| RNGLOCK-239-C-[0-9]{2} \|' audit/v30-RNGLOCK-STATE-MACHINE.md)
      test "$CLEAR_ROWS" -ge "$CLEAR_SSTORES"

      # HEAD anchor echoed in file
      grep -q '7ab515fe' audit/v30-RNGLOCK-STATE-MACHINE.md

      # Zero F-30-NN IDs per D-22
      ! grep -qE 'F-30-[0-9]' audit/v30-RNGLOCK-STATE-MACHINE.md

      # Zero mermaid fences per D-25
      ! grep -qi '```mermaid' audit/v30-RNGLOCK-STATE-MACHINE.md

      # Zero placeholder tokens
      ! grep -qE '<line>|<path>|<fn|<slug>|<family>|TBD-239' audit/v30-RNGLOCK-STATE-MACHINE.md

      # READ-only scope per D-27
      test -z "$(git status --porcelain contracts/ test/)"
      test -z "$(git diff --name-only 7ab515fe..HEAD -- contracts/ test/)"

      # Prior-plan outputs untouched per D-29 (Phase 238 files unmodified)
      test -z "$(git status --porcelain audit/v30-238-01-BWD.md audit/v30-238-02-FWD.md audit/v30-238-03-GATING.md audit/v30-FREEZE-PROOF.md 2>/dev/null || true)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `audit/v30-RNGLOCK-STATE-MACHINE.md` contains the 4 mandatory top-level section headings: `## Set-Site Table`, `## Clear-Site Table`, `## Path Enumeration Table` (placeholder for Task 2), `## Invariant Proof` (placeholder for Task 2) plus `## Executive Summary`, `## State-Machine Overview (Prose Diagram)`, `## Prior-Artifact Cross-Cites`, `## Finding Candidates`, `## Scope-Guard Deferrals`, `## Attestation`.
    - `## Set-Site Table` column header row is exactly `Row ID | File:Line | Function | Trigger Context | Companion Clear Path(s) | Verdict` (note: the plan renders `Row ID` as `Site ID` with `RNGLOCK-239-S-NN` IDs per D-04).
    - `## Clear-Site Table` column header row is exactly `Site ID | File:Line | Function | Trigger Context | Companion Set Path(s) | Verdict` per D-04.
    - Row count integrity: Set-Site Table row count ≥ grep count of `rngLockedFlag = true` SSTORE in `contracts/` (excluding mocks) at HEAD `7ab515fe`. Clear-Site Table row count ≥ grep count of `rngLockedFlag = false` SSTORE (≥ because D-06 adds L1700 Clear-Site-Ref row).
    - Every Set-Site / Clear-Site Verdict cell contains exactly one of `{AIRTIGHT, CANDIDATE_FINDING}` per D-05. Zero narrative verdicts (`looks correct`, `probably airtight`, `pending`).
    - `grep -E 'F-30-[0-9]' audit/v30-RNGLOCK-STATE-MACHINE.md` returns zero matches per D-22.
    - `grep -i 'mermaid' audit/v30-RNGLOCK-STATE-MACHINE.md` returns zero matches per D-25.
    - `audit_baseline: 7ab515fe` appears in file frontmatter and `HEAD 7ab515fe` appears at least once in file body per D-26.
    - `git status --porcelain contracts/ test/` returns empty per D-27 (READ-only).
    - `git status --porcelain audit/v30-238-01-BWD.md audit/v30-238-02-FWD.md audit/v30-238-03-GATING.md audit/v30-FREEZE-PROOF.md audit/v30-CONSUMER-INVENTORY.md KNOWN-ISSUES.md` returns empty per D-28/D-29 (prior artifacts untouched).
    - No commit yet (commit happens in Task 2 to land set/clear tables + path enumeration + invariant proof together).
  </acceptance_criteria>
  <done>
    File created with Set-Site + Clear-Site tables populated (row counts match grep pre-scan ± D-06 adjustment), closed verdict taxonomy enforced, HEAD anchor locked, READ-only scope confirmed, zero F-30-NN, zero mermaid, zero placeholder tokens, Phase 238 outputs + Phase 237 inventory + KNOWN-ISSUES untouched. Ready for Task 2 (Path Enumeration + Invariant Proof + commit).
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Path Enumeration Table + Invariant Proof + Prior-Artifact Cross-Cites + Finding Candidates + Scope-Guard Deferrals + Attestation + commit</name>
  <files>audit/v30-RNGLOCK-STATE-MACHINE.md</files>
  <read_first>
    - .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-CONTEXT.md (D-04 Path Enumeration shape + Invariant Proof format; D-06 L1700 revert-safety dedicated row; D-07 12h retry-timeout dedicated row; D-16/D-17 prior-artifact cross-cite re-verified-at-HEAD format; D-19 gameover-bracket bookkeeping IN scope, jackpot-input OUT; D-22 no F-30-NN; D-23 Finding Candidates appendix; D-28 scope-guard deferral; D-29 Phase 238 discharge)
    - audit/v30-RNGLOCK-STATE-MACHINE.md produced by Task 1 — Set-Site + Clear-Site tables are INPUT to Path Enumeration (every Path row cites a Set-Site ID and a Clear-Site ID)
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD 7ab515fe — path-walk source: trace every execution path from each set site through every early-return point and revert point to a matching clear site; grep for `return`, `revert`, `break`, `if (` statements between set and clear
    - contracts/modules/DegenerusGameGameOverModule.sol at HEAD — gameover-path clears (D-19 bookkeeping bracket); confirm gameover-VRF-request set/clear symmetry separate from jackpot-input determinism
    - contracts/DegenerusGame.sol at HEAD — external entry points that reach set/clear sites (advanceGame, purchase, claim paths)
    - .planning/milestones/v29.0-phases/235-conservation-rng-commitment-re-proof-phase-transition/235-05-TRNX-01.md — v29.0 4-path walk (Paths 1-4: read buffer / write buffer invariants); CORROBORATING cross-cite per D-16 only (re-derive at HEAD independently)
    - .planning/milestones/v3.7-phases/ — Phase 63 rawFulfillRandomWords revert-safety proof (CORROBORATING per D-06)
    - .planning/milestones/v3.8-phases/ — Phases 68-72 commitment window 51/51 SAFE (structural baseline)
    - .planning/milestones/v25.0-phases/215-rng-fresh-eyes/ — last milestone-level SOUND verdict (structural baseline)
    - audit/v30-FREEZE-PROOF.md §"Scope-Guard Deferrals" #1 (Phase 239 RNG-01 audit assumption — this plan discharges per D-29; verify path contents match audit assumption scope)
    - KNOWN-ISSUES.md §"Gameover prevrandao fallback" (informational context for gameover-branch path enumeration; D-19 says rngLockedFlag bookkeeping IS in scope, acceptance re-verification is NOT — Phase 241 EXC-02 owns that)
  </read_first>
  <action>
    Extend `audit/v30-RNGLOCK-STATE-MACHINE.md` with the following sections (in this exact order; replace placeholders from Task 1):

    **Step 1 — Populate `## Path Enumeration Table` with exactly these 7 columns in this exact order per D-04:**
    `Path ID | Set-Site Ref | Entry Condition | Early-Return Points | Revert Points | Clear-Site Ref | Verdict`

    - Path ID format: `RNGLOCK-239-P-NNN` (three-digit zero-padded, starting at `P-001`).
    - `Set-Site Ref` cell: the Site ID from Set-Site Table (`RNGLOCK-239-S-NN`) where this execution path's SSTORE of `rngLockedFlag = true` occurs.
    - `Entry Condition` cell: the external-entry call-chain condition that reaches the set site on this path (e.g., `advanceGame() reached by any caller; _finalizeRngRequest() conditions satisfied`).
    - `Early-Return Points` cell: comma-separated file:line references to every `return` statement / `break` statement on the control-flow path from set to clear that does NOT traverse through a clear site. Every such point MUST also be cross-referenced under the `Revert Points` column OR explicitly noted as `none — returns after clear` (meaning the early-return is downstream of the clear, not between set and clear).
    - `Revert Points` cell: comma-separated file:line references to every `revert` statement / `require` failure / external-call-can-revert point on the control-flow path from set to clear. Rollback of the set-site SSTORE via EVM transaction revert is the `CLEAR_WITHOUT_SET_UNREACHABLE`-equivalent semantics (i.e., the flag is never observably set if the transaction reverts).
    - `Clear-Site Ref` cell: the Site ID from Clear-Site Table (`RNGLOCK-239-C-NN`) reached on this path.
    - `Verdict` cell: exactly one of `{SET_CLEARS_ON_ALL_PATHS, CLEAR_WITHOUT_SET_UNREACHABLE, CANDIDATE_FINDING}` per D-04/D-05.

    **Mandatory Path rows per D-06 / D-07 / D-19 (these MUST be explicit, not absorbed):**
    - **D-06 — L1700 revert-safety:** one dedicated Path row covering `rawFulfillRandomWords` L1700 branch: when rngLockedFlag is true and a VRF delivery arrives, control enters the branch and writes `rngWordCurrent = word`; the flag is NOT cleared in this branch (flag is cleared later by `_unlockRng` call inside `advanceGame`). Verdict: `SET_CLEARS_ON_ALL_PATHS` via the post-L1700 path routing through `advanceGame`'s `_unlockRng` call. Explicit cross-cite to v3.7 Phase 63 `rawFulfillRandomWords` revert-safety proof with `re-verified at HEAD 7ab515fe — rawFulfillRandomWords body at L1685+, L1700 branch structure unchanged since v3.7 baseline`.
    - **D-07 — 12h retry-timeout path:** one dedicated Path row covering VRF request-retry semantics: if the VRF request is aged ≥ 12h (`GAMEOVER_RNG_FALLBACK_DELAY` / VRF retry timeout constant), a re-request can clear the prior request context. Verdict: must explicitly classify whether this is `CLEAR_WITHOUT_SET_UNREACHABLE` (re-request preserves flag state correctly) or `SET_CLEARS_ON_ALL_PATHS` (re-request resets flag state cleanly). Cross-cite v2.1 VRF retry timeout + v3.7 lifecycle + v29.0 Phase 235-05-TRNX-01.md 4-path walk with `re-verified at HEAD 7ab515fe` notes.
    - **D-19 — gameover-VRF-request bracket:** at least one Path row covering gameover path's rngLockedFlag set/clear symmetry around the gameover VRF request. Verdict covers only the state-machine bookkeeping; jackpot-input determinism is explicitly marked OUT of scope (Phase 240 GO-02 cross-cite in the `Entry Condition` or `Notes` cell).

    Enumerate all other reachable paths exhaustively (not sampled). Expected ≥ 4 paths (matching v29.0 Phase 235-05 4-path walk as a lower bound; HEAD may have more paths given v30.0 delta; re-derive independently).

    **Step 2 — Populate `## Invariant Proof` section with both directions of the biconditional closed-form argument per D-04:**
    ```
    **Invariant 1 (set→clear):** ∀ Set-Site S ∈ Set-Site Table and ∀ reachable execution path P originating at S:
      - P terminates at a Clear-Site C ∈ Clear-Site Table that is in S's `Companion Clear Path(s)` column, OR
      - P terminates at a revert point that rolls back the S-originated SSTORE via EVM transaction revert (flag is never observably set).

    Proof: Enumerate over Path IDs RNGLOCK-239-P-001..NNN; every row's Verdict ∈ {SET_CLEARS_ON_ALL_PATHS, CLEAR_WITHOUT_SET_UNREACHABLE}. Row count = N; all N rows audited airtight → Invariant 1 holds.

    **Invariant 2 (clear←set):** ∀ Clear-Site C ∈ Clear-Site Table and ∀ reachable execution path Q terminating at C:
      - Q originates downstream of a Set-Site S in C's `Companion Set Path(s)` column.
      - No path reaches C from a control-flow position where rngLockedFlag has not been set in the current transaction (i.e., no spurious clear).

    Proof: Inspect each Clear-Site row's `Companion Set Path(s)` column; confirm every Path in the Path Enumeration Table that reaches this Clear-Site has a matched Set-Site predecessor. Row count = M; all M rows match → Invariant 2 holds.

    **Corollary (D-04):** No reachable path produces set-without-clear (Invariant 1) or clear-without-matching-set (Invariant 2). RNG-01 AIRTIGHT.
    ```

    If any row verdict is `CANDIDATE_FINDING`, the Corollary statement is NOT "RNG-01 AIRTIGHT" — instead it states "RNG-01 AIRTIGHT EXCEPT FOR N CANDIDATE_FINDING ROWS ROUTED TO PHASE 242 FIND-01" and lists the offending Path IDs inline.

    **Step 3 — Populate `## Prior-Artifact Cross-Cites` per D-16/D-17:**
    Each cite carries `re-verified at HEAD 7ab515fe` + one-line structural-equivalence statement. Minimum cites:
    - `.planning/milestones/v29.0-phases/235-conservation-rng-commitment-re-proof-phase-transition/235-05-TRNX-01.md` — v29.0 4-path walk; `re-verified at HEAD 7ab515fe — v30.0 Path Enumeration Table superset of v29.0 4-path walk; structural equivalence confirmed`.
    - v3.7 Phase 63 `rawFulfillRandomWords` revert-safety — D-06 corroborating; `re-verified at HEAD 7ab515fe — rawFulfillRandomWords body at L1685+ unchanged since v3.7 baseline`.
    - v3.8 Phases 68-72 commitment window 51/51 SAFE — structural baseline; `re-verified at HEAD 7ab515fe — commitment-window surface area unchanged`.
    - v25.0 Phase 215 RNG fresh-eyes SOUND verdict — structural baseline.
    - v29.0 Phase 232.1-03-PFTB-AUDIT.md non-zero-entropy invariants — corroborating for semantic-path-gate archetypes that interact with rngLockedFlag.

    Each cite is structured as:
    ```
    ### <Milestone><Phase> <artifact-filename>
    **Artifact:** `<path>`
    **Corroboration:** <one-line what this supports>
    **Re-verification note:** re-verified at HEAD 7ab515fe — <structural-equivalence statement>
    ```

    **Step 4 — Populate `## Finding Candidates` per D-23:**
    If any row (Set-Site / Clear-Site / Path Enumeration) has verdict `CANDIDATE_FINDING`: structure each as:
    ```
    ### CANDIDATE_FINDING #N (— surfaced in 239-01 RNG-01)
    **ROW_REF:** <Site ID or Path ID>
    **FILE:LINE:** <contracts/... path + line>
    **OBSERVATION:** <one-paragraph observation>
    **SEVERITY:** TBD-242
    **ROUTING:** Phase 242 FIND-01 intake
    ```

    If zero `CANDIDATE_FINDING` rows: state `**None surfaced.** All rngLockedFlag state-machine surfaces at HEAD 7ab515fe verified AIRTIGHT.` No hedged language.

    **Step 5 — Populate `## Scope-Guard Deferrals` per D-28:**
    If any rngLockedFlag SSTORE site, clear site, or execution path is discovered that is NOT already represented in `audit/v30-CONSUMER-INVENTORY.md` Universe List or Consumer Index RNG-01 row: record it as a scope-guard deferral block with `file:line + context + proposed inventory delta + SEVERITY: TBD-242 + ROUTING: Phase 242 FIND-01`. Inventory is NOT edited (D-28).

    If zero gaps: state `**None surfaced.** Inventory scope anchor (RNG-01 = 94-row daily + infrastructure subset per 237-03 Consumer Index) matches observed rngLockedFlag surface at HEAD 7ab515fe.`

    **Step 6 — Populate `## Attestation`:**
    ```
    **HEAD anchor:** `7ab515fe` (contract tree identical to v29.0 `1646d5af`; all post-v29 commits are docs-only per PROJECT.md).
    **Scope:** RNG-01 rngLockedFlag state machine — every set site, every clear site, every reachable path from each set through early-returns + reverts to a matching clear.
    **Fresh-eyes mandate (D-16/D-17):** Every row re-derived at HEAD 7ab515fe. Prior-milestone artifacts CROSS-CITED with re-verified-at-HEAD notes — NOT relied upon.
    **Finding-ID emission (D-22):** Zero F-30-NN. Finding Candidates routed to Phase 242 FIND-01.
    **READ-only scope (D-27):** Zero contracts/ or test/ writes. KNOWN-ISSUES.md untouched.
    **Phase 238 discharge (D-29):** Phase 238-03 FWD-03 gating audit assumption for rngLocked gate correctness (Scope-Guard Deferral #1 in audit/v30-FREEZE-PROOF.md) discharged by this plan's commit. No re-edit of Phase 238 files.
    **Row-set integrity:** Set-Site Table N rows + Clear-Site Table M rows + Path Enumeration Table K rows; grep-verified against contracts/ at HEAD 7ab515fe.
    ```

    **Step 7 — Commit:**
    Stage ONLY `audit/v30-RNGLOCK-STATE-MACHINE.md`. Commit with message:
    ```
    docs(239-01): RNG-01 rngLockedFlag state machine airtight proof at HEAD 7ab515fe

    Set-Site Table (N rows) + Clear-Site Table (M rows) + Path Enumeration Table
    (K rows) + Invariant Proof + Prior-Artifact Cross-Cites + Finding Candidates
    + Scope-Guard Deferrals + Attestation. Discharges Phase 238-03 FWD-03 gating
    rngLocked audit assumption (Scope-Guard Deferral #1 in audit/v30-FREEZE-PROOF.md).

    Closed verdict taxonomy per D-05: {AIRTIGHT / CANDIDATE_FINDING}. L1700
    revert-safety enumerated per D-06. 12h retry-timeout path enumerated per D-07.
    Gameover-VRF-request bracket bookkeeping enumerated per D-19 (jackpot-input
    determinism out-of-scope — Phase 240 GO-02).

    READ-only: zero contracts/ or test/ writes per D-27. Zero F-30-NN per D-22.
    HEAD anchor 7ab515fe locked per D-26.
    ```

    Do NOT push. Do NOT amend. Do NOT use --no-verify. Single commit only.
  </action>
  <verify>
    <automated>
      # Path Enumeration Table exists with RNGLOCK-239-P-NNN rows + closed verdict taxonomy
      grep -q '^## Path Enumeration Table$' audit/v30-RNGLOCK-STATE-MACHINE.md
      grep -cE '\| RNGLOCK-239-P-[0-9]{3} \|' audit/v30-RNGLOCK-STATE-MACHINE.md | awk '{ if ($1 < 4) exit 1 }'
      # Every path verdict cell contains one of the 3 closed verdicts
      ! grep -E '\| RNGLOCK-239-P-[0-9]{3} \|' audit/v30-RNGLOCK-STATE-MACHINE.md | grep -vE '\b(SET_CLEARS_ON_ALL_PATHS|CLEAR_WITHOUT_SET_UNREACHABLE|CANDIDATE_FINDING)\b'

      # Invariant Proof section exists with both directions
      grep -q '^## Invariant Proof$' audit/v30-RNGLOCK-STATE-MACHINE.md
      grep -qE 'Invariant 1|set.*clear' audit/v30-RNGLOCK-STATE-MACHINE.md
      grep -qE 'Invariant 2|clear.*set' audit/v30-RNGLOCK-STATE-MACHINE.md

      # Prior-Artifact Cross-Cites section with ≥ 3 re-verified notes
      grep -q '^## Prior-Artifact Cross-Cites$' audit/v30-RNGLOCK-STATE-MACHINE.md
      grep -cE 're-verified at HEAD 7ab515fe' audit/v30-RNGLOCK-STATE-MACHINE.md | awk '{ if ($1 < 3) exit 1 }'

      # Finding Candidates + Scope-Guard Deferrals + Attestation sections present
      grep -q '^## Finding Candidates$' audit/v30-RNGLOCK-STATE-MACHINE.md
      grep -q '^## Scope-Guard Deferrals$' audit/v30-RNGLOCK-STATE-MACHINE.md
      grep -q '^## Attestation$' audit/v30-RNGLOCK-STATE-MACHINE.md

      # D-06 L1700 revert-safety enumerated (explicit mention in Path Enumeration Table or nearby)
      grep -qE '1700|rawFulfillRandomWords' audit/v30-RNGLOCK-STATE-MACHINE.md

      # D-07 12h retry-timeout enumerated
      grep -qiE '12h|12 hour|retry.*timeout|GAMEOVER_RNG_FALLBACK_DELAY' audit/v30-RNGLOCK-STATE-MACHINE.md

      # D-19 gameover bracket bookkeeping enumerated + jackpot-determinism out-of-scope noted
      grep -qE 'gameover' audit/v30-RNGLOCK-STATE-MACHINE.md
      grep -qE 'Phase 240|GO-02|jackpot-input.*out.*scope' audit/v30-RNGLOCK-STATE-MACHINE.md

      # D-29 discharge noted
      grep -qiE 'Phase 238|discharge|Scope-Guard Deferral #1' audit/v30-RNGLOCK-STATE-MACHINE.md

      # Zero F-30-NN per D-22
      ! grep -qE 'F-30-[0-9]' audit/v30-RNGLOCK-STATE-MACHINE.md

      # Zero mermaid per D-25
      ! grep -qi '```mermaid' audit/v30-RNGLOCK-STATE-MACHINE.md

      # Zero placeholder tokens
      ! grep -qE '<line>|<path>|<fn|<slug>|TBD-239' audit/v30-RNGLOCK-STATE-MACHINE.md

      # HEAD anchor echoed
      grep -q '7ab515fe' audit/v30-RNGLOCK-STATE-MACHINE.md

      # READ-only scope per D-27
      test -z "$(git status --porcelain contracts/ test/)"
      test -z "$(git diff --name-only 7ab515fe..HEAD -- contracts/ test/)"

      # Phase 237/238 outputs + KNOWN-ISSUES untouched per D-28/D-29
      test -z "$(git status --porcelain audit/v30-238-01-BWD.md audit/v30-238-02-FWD.md audit/v30-238-03-GATING.md audit/v30-FREEZE-PROOF.md audit/v30-CONSUMER-INVENTORY.md KNOWN-ISSUES.md 2>/dev/null || true)"

      # Commit landed — exactly one new commit by this plan with subject prefix docs(239-01):
      git log --oneline -1 | grep -qE '^[0-9a-f]+ docs\(239-01\):'
      # Commit touched ONLY audit/v30-RNGLOCK-STATE-MACHINE.md
      git show --stat HEAD | grep -E '^\s+audit/v30-RNGLOCK-STATE-MACHINE.md'
      test "$(git show --stat HEAD --name-only --format='' | grep -v '^$' | wc -l)" -eq 1
    </automated>
  </verify>
  <acceptance_criteria>
    - `## Path Enumeration Table` exists with ≥ 4 rows (v29.0 Phase 235-05 4-path walk is the lower bound; v30.0 may have more), each with `RNGLOCK-239-P-NNN` Path ID format, Set-Site Ref + Clear-Site Ref cross-references, and Verdict cell = exactly one of `{SET_CLEARS_ON_ALL_PATHS, CLEAR_WITHOUT_SET_UNREACHABLE, CANDIDATE_FINDING}` per D-04/D-05.
    - `## Invariant Proof` section contains both Invariant 1 (set→clear) and Invariant 2 (clear←set) in closed-form with row-ID references, plus the corollary per D-04.
    - D-06 L1700 revert-safety: at least one Path Enumeration row mentions `L1700` or `rawFulfillRandomWords` and cross-cites v3.7 Phase 63 with `re-verified at HEAD 7ab515fe` note.
    - D-07 12h retry-timeout: at least one Path Enumeration row mentions `12h` / `12 hour` / `GAMEOVER_RNG_FALLBACK_DELAY` / retry-timeout semantics and explicitly handles the `clear-without-matching-new-set` case.
    - D-19 gameover-bracket: at least one Path Enumeration row covers gameover-VRF-request bracket AND explicitly routes jackpot-input determinism to Phase 240 GO-02 (out-of-scope marker).
    - `## Prior-Artifact Cross-Cites` section has ≥ 3 cites with `re-verified at HEAD 7ab515fe` notes per D-17.
    - `## Finding Candidates` has either N structured blocks matching `CANDIDATE_FINDING` rows OR `**None surfaced.**` statement.
    - `## Scope-Guard Deferrals` has either N deferral blocks (out-of-inventory paths) OR `**None surfaced.**` statement per D-28.
    - `## Attestation` section lists: HEAD anchor `7ab515fe`, READ-only scope confirmation, zero F-30-NN attestation, Phase 238 discharge note (D-29), row-set integrity statement.
    - `grep -E 'F-30-[0-9]' audit/v30-RNGLOCK-STATE-MACHINE.md` returns zero matches per D-22.
    - `grep -i 'mermaid' audit/v30-RNGLOCK-STATE-MACHINE.md` returns zero matches per D-25.
    - Exactly one new commit on `main` with subject matching regex `^docs\(239-01\):`; the commit touches exactly `audit/v30-RNGLOCK-STATE-MACHINE.md` (no other file staged).
    - `git status --porcelain contracts/ test/` empty per D-27.
    - `git status --porcelain audit/v30-238-*.md audit/v30-FREEZE-PROOF.md audit/v30-CONSUMER-INVENTORY.md KNOWN-ISSUES.md` empty per D-28/D-29.
    - No `--no-verify`, no force-push, no push-to-remote.
  </acceptance_criteria>
  <done>
    File contains all 10 sections populated; closed verdict taxonomies enforced throughout; L1700 + 12h-retry + gameover-bracket paths explicitly enumerated; closed-form Invariant Proof with both directions; prior cross-cites carry re-verified-at-HEAD notes; Finding Candidates / Scope-Guard Deferrals follow exact format; Attestation locks HEAD anchor + READ-only + discharge note; one clean commit on main; Phase 237 inventory + Phase 238 outputs + KNOWN-ISSUES untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write Plan 239-01 SUMMARY with Phase 238 discharge attestation + commit</name>
  <files>.planning/phases/239-rnglocked-invariant-permissionless-sweep/239-01-SUMMARY.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md (SUMMARY template structure)
    - .planning/phases/237-vrf-consumer-inventory-call-graph/237-03-SUMMARY.md (Phase 237 consolidated output summary precedent)
    - .planning/phases/238-backward-forward-freeze-proofs/238-03-SUMMARY.md (Phase 238 consolidated output summary precedent — particularly Phase 239 audit-assumption handling)
    - .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-CONTEXT.md (D-26 HEAD anchor in SUMMARY frontmatter; D-29 discharge evidence by commit presence; D-28 scope-guard deferral format)
    - audit/v30-RNGLOCK-STATE-MACHINE.md (produced by Task 1 + Task 2 — source for Set/Clear/Path counts + verdict distribution + cross-cite count)
    - git log --oneline -1 (to capture the commit SHA for the SUMMARY Task Commits section)
  </read_first>
  <action>
    Create `.planning/phases/239-rnglocked-invariant-permissionless-sweep/239-01-SUMMARY.md` with frontmatter per the template:

    ```yaml
    ---
    phase: 239-rnglocked-invariant-permissionless-sweep
    plan: 239-01
    subsystem: audit
    tags: [v30.0, VRF, RNG-01, rngLockedFlag, state-machine, invariant-proof, fresh-eyes, HEAD-7ab515fe]
    head_anchor: 7ab515fe

    # Dependency graph
    requires:
      - phase: 237-vrf-consumer-inventory-call-graph
        provides: "audit/v30-CONSUMER-INVENTORY.md Consumer Index RNG-01 scope (94-row daily + infrastructure subset)"
    provides:
      - "audit/v30-RNGLOCK-STATE-MACHINE.md — RNG-01 deliverable per D-04: Set-Site Table (N rows) + Clear-Site Table (M rows) + Path Enumeration Table (K rows) + Invariant Proof + Prior-Artifact Cross-Cites + Finding Candidates + Scope-Guard Deferrals + Attestation at HEAD 7ab515fe."
      - "Set-Site / Clear-Site / Path Enumeration verdict distribution: AIRTIGHT = X / CANDIDATE_FINDING = Y (fill from actual table contents)."
      - "Phase 238-03 FWD-03 gating rngLocked audit assumption (Scope-Guard Deferral #1 in audit/v30-FREEZE-PROOF.md) DISCHARGED by this plan commit per D-29."
    affects: [240-gameover-jackpot-safety, 241-exception-closure, 242-findings-consolidation]

    # Tech tracking
    tech-stack:
      added: []
      patterns:
        - "D-04 Set-Site + Clear-Site + Path Enumeration tabular format (Phase 235 D-07 pattern extended to state-machine analysis)"
        - "D-05 closed verdict taxonomy {AIRTIGHT / CANDIDATE_FINDING} — no hedged verdicts"
        - "D-06 L1700 revert-safety enumerated as dedicated Path row"
        - "D-07 12h retry-timeout path enumerated as dedicated Path row"
        - "D-19 gameover-VRF-request bracket bookkeeping IN scope; jackpot-input determinism OUT (Phase 240 GO-02)"
        - "D-22 no F-30-NN finding-ID emission — candidates route to Phase 242 FIND-01"
        - "D-25 tabular / grep-friendly / no mermaid"
        - "D-26 HEAD anchor 7ab515fe locked in frontmatter + echoed in Attestation"
        - "D-27 READ-only — zero contracts/ or test/ writes; KNOWN-ISSUES untouched"
        - "D-28 scope-guard deferral for out-of-inventory paths; Phase 237 inventory READ-only"
        - "D-29 Phase 238 discharge evidenced by commit presence — no re-edit of 238 output"

    key-files:
      created:
        - "audit/v30-RNGLOCK-STATE-MACHINE.md (<LINE_COUNT> lines committed at <COMMIT_SHA> — 10 required sections: Executive Summary / State-Machine Overview / Set-Site Table (N rows) / Clear-Site Table (M rows) / Path Enumeration Table (K rows) / Invariant Proof / Prior-Artifact Cross-Cites (X cites) / Finding Candidates (Y) / Scope-Guard Deferrals (Z) / Attestation)"
        - ".planning/phases/239-rnglocked-invariant-permissionless-sweep/239-01-SUMMARY.md"
      modified: []

    requirements-completed: [RNG-01]

    metrics:
      duration: <FILL>
      completed: 2026-04-19
      tasks_executed: 3
      lines_in_audit_file: <FILL>
      commits:
        - sha: <COMMIT_SHA from Task 2>
          subject: docs(239-01): RNG-01 rngLockedFlag state machine airtight proof at HEAD 7ab515fe
    ---
    ```

    Then add body sections per template:

    ```markdown
    # Phase 239 Plan 01: RNG-01 rngLockedFlag State Machine Airtight Proof Summary

    **Single-file RNG-01 state-machine proof at HEAD `7ab515fe`: every set site, every clear site, every reachable path from set through early-returns + reverts to matching clear enumerated exhaustively with closed-form invariant proof. Discharges Phase 238-03 FWD-03 gating rngLocked audit assumption.**

    ## Performance
    (fill in duration + started/completed + tasks + files)

    ## Accomplishments
    - (fill with actual counts — Set-Site N rows / Clear-Site M rows / Path Enumeration K rows / Cross-Cites X / Finding Candidates Y / Scope-Guard Deferrals Z)
    - D-06 L1700 revert-safety enumerated as Path row — (cite Path ID + contracts cite)
    - D-07 12h retry-timeout enumerated as Path row — (cite Path ID + verdict)
    - D-19 gameover-VRF-request bracket enumerated; jackpot-input determinism routed to Phase 240 GO-02
    - Closed-form Invariant Proof with both directions (set→clear + clear←set); corollary states RNG-01 AIRTIGHT [or AIRTIGHT EXCEPT FOR N CANDIDATE_FINDING rows routed to Phase 242]
    - Prior-milestone cross-cites re-verified at HEAD 7ab515fe (v29.0 Phase 235-05 4-path walk superset confirmed / v3.7 Phase 63 rawFulfillRandomWords revert-safety body unchanged / v3.8 commitment-window surface unchanged / v25.0 Phase 215 SOUND verdict structural baseline)

    ## Task Commits
    1. **Task 1 + Task 2 (combined commit): Build Set-Site + Clear-Site tables + Path Enumeration + Invariant Proof + cross-cites + Finding Candidates + Scope-Guard Deferrals + Attestation + commit** — `<COMMIT_SHA>` (docs(239-01): RNG-01 rngLockedFlag state machine airtight proof at HEAD 7ab515fe). <LINE_COUNT> lines; zero F-30-NN; zero mermaid; zero placeholder tokens; HEAD anchor attested.

    Note: Plan separates Task 1 (build Set/Clear tables) from Task 2 (Path Enumeration + commit). Because there is no intermediate checkpoint between the tables and the commit, both land as one commit — same pattern as 237-02/03, 238-01/02 precedent.

    ## Files Created/Modified
    - `audit/v30-RNGLOCK-STATE-MACHINE.md` (CREATED) — (fill counts)
    - `audit/v30-CONSUMER-INVENTORY.md` (UNCHANGED per D-28 — inventory READ-only after 237 commit)
    - `audit/v30-238-01-BWD.md`, `audit/v30-238-02-FWD.md`, `audit/v30-238-03-GATING.md`, `audit/v30-FREEZE-PROOF.md` (UNCHANGED per D-29 — Phase 238 READ-only; Phase 238 audit-assumption discharge is evidenced by presence of this commit, not by edits to 238 files)
    - `KNOWN-ISSUES.md` (UNCHANGED per D-27 — Phase 242 FIND-03 owns KI promotions)
    - `contracts/`, `test/` (UNCHANGED per D-27 — READ-only audit phase)

    ## Decisions Made
    - (fill 3-5 decisions Claude made during execution — Claude's Discretion items in CONTEXT.md: subsection ordering, prose-diagram inclusion/exclusion, grep-command preservation, severity pre-classification vs TBD-242)

    ## Phase 238 Discharge Note (D-29)
    Phase 238-03 FWD-03 gating (`audit/v30-FREEZE-PROOF.md` Scope-Guard Deferral #1) cited Phase 239 RNG-01 as audit assumption pending first-principles re-proof of the rngLockedFlag state-machine. This plan's commit (`<COMMIT_SHA>`) DISCHARGES that assumption: every rngLockedFlag surface at HEAD 7ab515fe re-enumerated fresh + every reachable path proven AIRTIGHT (or surfaced as CANDIDATE_FINDING for Phase 242 FIND-01 intake). No re-edit of Phase 238 files — discharge evidenced by presence of RNG-01 verdicts at this commit SHA. Phase 242 REG-01/02 cross-checks the discharge at milestone consolidation.

    ## Deviations from Plan
    (fill with any deviations, or "None — plan executed exactly as written.")

    ## Issues Encountered
    (fill with any issues, or "None.")

    ## User Setup Required
    None — no external service configuration. Deliverable is markdown-only under `audit/`.

    ## Next Phase Readiness
    **Phase 239 Plan 01 complete (RNG-01 closed). Plans 239-02 (RNG-02 permissionless sweep) + 239-03 (RNG-03 asymmetry re-justification) running in parallel (Wave 1 per D-02) — no cross-dependencies.**

    ## Self-Check: PASSED
    (fill checklist items — all must-haves truths satisfied; see Attestation section of audit file)
    ```

    Replace `<COMMIT_SHA>` with the actual SHA from Task 2's commit (captured via `git rev-parse HEAD` at time of SUMMARY write). Replace `<LINE_COUNT>` + `N/M/K/X/Y/Z` with the actual counts from the audit file.

    **Commit:**
    Stage `.planning/phases/239-rnglocked-invariant-permissionless-sweep/239-01-SUMMARY.md`. Commit with message:
    ```
    docs(239-01): SUMMARY — RNG-01 state machine proof complete; Phase 238 discharge noted
    ```

    Do NOT push. Do NOT amend. Do NOT use --no-verify.
  </action>
  <verify>
    <automated>
      # SUMMARY file exists with mandatory frontmatter fields
      test -f .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-01-SUMMARY.md
      grep -qE '^phase: 239-rnglocked-invariant-permissionless-sweep' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-01-SUMMARY.md
      grep -qE '^plan: 239-01' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-01-SUMMARY.md
      grep -qE '^head_anchor: 7ab515fe' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-01-SUMMARY.md
      grep -qE 'requirements-completed:.*RNG-01' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-01-SUMMARY.md

      # Phase 238 discharge section present
      grep -qE 'Phase 238.*Discharge|Discharge.*Phase 238|D-29' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-01-SUMMARY.md

      # Commit SHA filled in (not literal <COMMIT_SHA> placeholder)
      ! grep -q '<COMMIT_SHA>' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-01-SUMMARY.md
      ! grep -q '<LINE_COUNT>' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-01-SUMMARY.md
      ! grep -q '<FILL>' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-01-SUMMARY.md

      # Zero F-30-NN per D-22
      ! grep -qE 'F-30-[0-9]' .planning/phases/239-rnglocked-invariant-permissionless-sweep/239-01-SUMMARY.md

      # READ-only scope per D-27
      test -z "$(git status --porcelain contracts/ test/)"

      # Phase 237/238 outputs + KNOWN-ISSUES untouched per D-28/D-29
      test -z "$(git status --porcelain audit/v30-238-01-BWD.md audit/v30-238-02-FWD.md audit/v30-238-03-GATING.md audit/v30-FREEZE-PROOF.md audit/v30-CONSUMER-INVENTORY.md KNOWN-ISSUES.md 2>/dev/null || true)"

      # Commit landed — subject prefix docs(239-01):
      git log --oneline -1 | grep -qE '^[0-9a-f]+ docs\(239-01\):'
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/239-rnglocked-invariant-permissionless-sweep/239-01-SUMMARY.md` exists with full frontmatter matching the template: `phase`, `plan`, `subsystem`, `tags`, `head_anchor: 7ab515fe`, `requires`, `provides`, `affects`, `tech-stack.patterns` (≥ 11 items per CONTEXT.md D-list), `key-files.created` (≥ 2 items), `requirements-completed: [RNG-01]`.
    - Body sections include `## Performance`, `## Accomplishments`, `## Task Commits` (with actual commit SHA filled in — not `<COMMIT_SHA>`), `## Files Created/Modified`, `## Decisions Made`, `## Phase 238 Discharge Note (D-29)`, `## Deviations from Plan`, `## Issues Encountered`, `## User Setup Required`, `## Next Phase Readiness`, `## Self-Check: PASSED`.
    - Phase 238 discharge section explicitly names `Scope-Guard Deferral #1` + `audit/v30-FREEZE-PROOF.md` + states "No re-edit of Phase 238 files" per D-29.
    - No literal placeholder tokens (`<COMMIT_SHA>`, `<LINE_COUNT>`, `<FILL>`, `<N>`, etc.) remain.
    - `grep -E 'F-30-[0-9]'` returns zero matches per D-22.
    - Exactly one new commit on `main` with subject matching regex `^docs\(239-01\):` (the Task 2 commit; this task's commit may be absorbed into the same or land as a second — both patterns acceptable per precedent).
    - `git status --porcelain contracts/ test/` empty per D-27.
    - `git status --porcelain audit/v30-238-*.md audit/v30-FREEZE-PROOF.md audit/v30-CONSUMER-INVENTORY.md KNOWN-ISSUES.md` empty per D-28/D-29.
  </acceptance_criteria>
  <done>
    SUMMARY file committed; all placeholders filled with actual values; Phase 238 discharge note explicit; requirements-completed lists RNG-01; HEAD anchor 7ab515fe in frontmatter; Phase 237 inventory + Phase 238 outputs + KNOWN-ISSUES untouched; clean commit on main.
  </done>
</task>

</tasks>

<verification>
- `audit/v30-RNGLOCK-STATE-MACHINE.md` exists with 10 required sections; Set-Site + Clear-Site + Path Enumeration tables use closed verdict taxonomies per D-05; Invariant Proof both directions present; L1700 + 12h-retry + gameover-bracket explicitly enumerated.
- `.planning/phases/239-rnglocked-invariant-permissionless-sweep/239-01-SUMMARY.md` exists with HEAD anchor, Phase 238 discharge note, and full frontmatter.
- Zero F-30-NN per D-22. Zero contracts/ or test/ writes per D-27. Zero edits to Phase 237 inventory / Phase 238 outputs / KNOWN-ISSUES per D-28/D-29.
- Exactly 1-2 new commits on `main` with subject prefix `docs(239-01):`.
</verification>

<success_criteria>
- RNG-01 requirement satisfied: rngLockedFlag state machine airtight proof at HEAD 7ab515fe with exhaustive set-site, clear-site, and path enumeration; closed-form Invariant Proof; L1700 revert-safety + 12h retry-timeout + gameover-bracket bookkeeping all explicitly enumerated.
- Phase 238-03 Scope-Guard Deferral #1 rngLocked audit assumption discharged (evidenced by commit presence per D-29; no re-edit of Phase 238 files).
- Fresh-eyes mandate upheld: every row re-derived at HEAD 7ab515fe; prior-milestone artifacts cross-cited with `re-verified at HEAD 7ab515fe` notes, not relied upon.
- Finding Candidates appendix routes any non-AIRTIGHT row to Phase 242 FIND-01 intake; zero F-30-NN IDs emitted.
- READ-only scope preserved across all tasks.
</success_criteria>

<output>
After completion, `audit/v30-RNGLOCK-STATE-MACHINE.md` + `.planning/phases/239-rnglocked-invariant-permissionless-sweep/239-01-SUMMARY.md` are committed on `main` at HEAD descendant of `7ab515fe`. Phase 239 Plan 02 (RNG-02 permissionless sweep) + Plan 03 (RNG-03 asymmetry re-justification) run in parallel Wave 1 per D-02 — no cross-dependencies.
</output>
