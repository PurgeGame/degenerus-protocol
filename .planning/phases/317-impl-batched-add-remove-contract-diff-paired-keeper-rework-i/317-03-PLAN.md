---
phase: 317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i
plan: 03
type: execute
wave: 2
depends_on: ["317-01"]
files_modified:
  - contracts/DegenerusGame.sol
  - contracts/interfaces/IDegenerusGame.sol
autonomous: true
requirements: [PROTO-01, PROTO-04, CRANK-01, CRANK-02, CRANK-03, CRANK-04, REW-01, REW-02, REW-03, REW-04, RM-01, RM-04, SUB-09]
must_haves:
  truths:
    - "hasAnyLazyPass(address) external view is exposed (renamed from the kept private _hasAnyLazyPass, NO body change) and mirror-declared in IDegenerusGame.sol next to hasDeityPass (PROTO-01 / RM-04 KEEP+EXPOSE)."
    - "batchPurchase(players[], amounts[], modes[]) is keeper-gated on AF_KING, does per-player try/catch + slice-refund, one batch value transfer, batch-level rngLocked/game-over pre-checked once; CEI-proof unless a re-entrant path is found in the mint→lootbox→prize-pool→EV-cap→quest chain (PROTO-04 / OPEN-C)."
    - "The permissionless do-work crank settles bets (caller-list with BatchAlreadyTaken short-circuit on item 0) and boxes (parameterless cursor following the v45 a303ae18 detect-preserve-re-issue path), per-item onlySelf + try/catch reward-only-successes, WWXRP currency==3 earns zero, reward = gasUnits·0.5gwei→BURNIE via _ethToBurnieValue paid as ONE creditFlip/cranker/tx with fixed gasUnits constants and no caller restriction (CRANK-01..04 + REW-01..04)."
    - "RM-01 deletes the 13 afKing-mode fns (keeping ONLY _hasAnyLazyPass), 3 events, the AfKingLockActive error, 3 consts, and the 2 coinflip.settleFlipModeChange cross-calls; the IDegenerusGame afKing decls are removed; grep-clean for afKing/AFKING_ in DegenerusGame.sol outside comments."
    - "The constructor Deity grant on SDGNRS/VAULT (:222/:223) is preserved byte-unmodified through the surrounding RM edits — no new Deity-bit setter is authored (SUB-09 permanent-deity free-renew)."
  artifacts:
    - path: "contracts/DegenerusGame.sol"
      provides: "hasAnyLazyPass view + batchPurchase + crank entry; afKing-mode surface deleted; ctor Deity grant preserved"
      contains: "hasAnyLazyPass"
    - path: "contracts/interfaces/IDegenerusGame.sol"
      provides: "hasAnyLazyPass decl added; 4 afKing decls removed; hasDeityPass kept"
      contains: "hasAnyLazyPass"
  key_links:
    - from: "contracts/DegenerusGame.sol batchPurchase"
      to: "ContractAddresses.AF_KING"
      via: "keeper-gate (msg.sender == AF_KING)"
      pattern: "AF_KING"
    - from: "crank box cursor"
      to: "v45 a303ae18 detect-preserve-re-issue path"
      via: "lootbox index keyspace coupling"
      pattern: "lootboxRngWordByIndex|RngNotReady"
    - from: "constructor :222/:223 Deity grant"
      to: "hasAnyLazyPass(VAULT/SDGNRS)"
      via: "permanent Deity bit → permanent pass"
      pattern: "HAS_DEITY_PASS_SHIFT"
---

<objective>
Apply the entire `DegenerusGame.sol` surface in one exclusive-ownership plan: PROTO-01 (`hasAnyLazyPass` view), PROTO-04 (`batchPurchase`), the permissionless do-work crank (CRANK-01..04 + REW-01..04), RM-01 (afKing-mode deletion keeping only `_hasAnyLazyPass`), the SUB-09 constructor-Deity-grant preservation, and the matching `IDegenerusGame.sol` interface edits. NO commit (the commit-guard hook keeps `contracts/` dirty for the single batched approval).

Purpose: `DegenerusGame.sol` is touched by five different requirement clusters; co-locating them in one plan gives exclusive file ownership (no overlap with Plan 02's BURNIE files or Plan 05's modules/storage/Vault/sStonk), so it runs parallel to Plans 02 and 04. The crank's resolve path writes game storage directly, so it lives in-game by construction. The box cursor re-couples to the v45 VRF-rotation orphan-index keyspace — the milestone's single biggest landmine — and MUST follow the `a303ae18` detect-preserve-re-issue path.
Output: `DegenerusGame.sol` + `IDegenerusGame.sol` patched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-LEDGER.md
@.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md
@.planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-PATTERNS.md

<interfaces>
<!-- PROTO-01 rename target: _hasAnyLazyPass private view → hasAnyLazyPass external view, NO body change (DegenerusGame.sol). -->
<!-- Crank reuses: _requireApproved + _resolvePlayer (resolve-relaxed); resolveDegeneretteBets / resolveBets caller-list loop; degeneretteBets delete site (DegeneretteModule). -->
<!-- batchPurchase per-player unit = purchase / _purchaseFor; rngLocked pre-check; CEI ETH-send idiom (.call{value:} then if(!ok) revert); per-item isolation = try this.foo(...) {} catch {}. -->
<!-- Reward conversion = _ethToBurnieValue (MintModule, zero-guarded) → coinflip.creditFlip (AdvanceModule bounty idiom); ONE creditFlip/tx; fixed gasUnits constants (RESERVED names, numbers calibrated at Phase 319); WWXRP currency==3 → 0. -->
<!-- Box cursor enqueue signal = lootboxEthBase == 0 first-deposit; dequeue/zero on open; RngNotReady guard; MUST follow v45 a303ae18 re-issue coupling. -->
<!-- SUB-09 preserve: ctor :222/:223 mintPacked_[SDGNRS]/[VAULT] HAS_DEITY_PASS_SHIFT grant — byte-unmodified, no new setter. -->
<!-- Use the LIVE line numbers from 317-LEDGER.md — authoritative over the SPEC/PATTERNS snapshot. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: RM-01 delete the afKing-mode surface (KEEP+EXPOSE _hasAnyLazyPass as PROTO-01) + SUB-09 ctor-grant preservation</name>
  <read_first>
    - 317-LEDGER.md (live lines for the 13 afKing-mode fns, the KEEP _hasAnyLazyPass, the 3 events, AfKingLockActive, the 3 consts, the 2 coinflip.settleFlipModeChange cross-calls, and the ctor Deity grant :222/:223)
    - 316-SPEC.md `## REMOVE Footprint` RM-01 + RM-04/PROTO-01 reconciliation + `## Protocol-Owned Subs (SUB-09)` (permanent-deity, preserve-not-author)
    - 317-PATTERNS.md `contracts/DegenerusGame.sol` section (the RM-01 footprint list + the SUB-09 preserve note)
    - contracts/DegenerusGame.sol, contracts/interfaces/IDegenerusGame.sol
  </read_first>
  <action>
    DELETE the 13 afKing-mode functions enumerated in the ledger KEEPING ONLY the body of _hasAnyLazyPass. Rename that surviving function private view → external view as hasAnyLazyPass with the body byte-identical (PROTO-01 / RM-04). DELETE the 3 events (AutoRebuyToggled, AutoRebuyTakeProfitSet, AfKingModeToggled), the error AfKingLockActive, the 3 consts (AFKING_KEEP_MIN_ETH, AFKING_KEEP_MIN_COIN, AFKING_LOCK_LEVELS), and the 2 coinflip.settleFlipModeChange(player) cross-calls. In IDegenerusGame.sol REMOVE afKingModeFor/afKingActivatedLevelFor/deactivateAfKingFromCoin/syncAfKingLazyPassFromCoin, KEEP hasDeityPass, and ADD the hasAnyLazyPass(address) external view returns (bool) decl mirroring the existing view-decl style next to hasDeityPass. PRESERVE byte-unmodified the constructor Deity grant at :222/:223 (mintPacked_[SDGNRS]/mintPacked_[VAULT] HAS_DEITY_PASS_SHIFT) — do NOT author a new Deity-bit setter, and ensure the nearby RM edits do not perturb those two lines. Comments describe what IS — no history prose. After the edit, grep the DegenerusGame afKing/AFKING_ symbol set and confirm zero matches outside comments except the kept hasAnyLazyPass.
  </action>
  <verify>
    <automated>grep -q "function hasAnyLazyPass" contracts/DegenerusGame.sol && [ "$(grep -v '^[[:space:]]*//' contracts/DegenerusGame.sol | grep -cE 'setAfKingMode|_setAfKingMode|afKingModeFor|afKingActivatedLevelFor|deactivateAfKingFromCoin|syncAfKingLazyPassFromCoin|_deactivateAfKing|AfKingLockActive|AfKingModeToggled|settleFlipModeChange')" = "0" ] && grep -q "HAS_DEITY_PASS_SHIFT" contracts/DegenerusGame.sol && grep -q "hasAnyLazyPass" contracts/interfaces/IDegenerusGame.sol</automated>
  </verify>
  <acceptance_criteria>
    hasAnyLazyPass exists as external view with the body byte-identical to the old _hasAnyLazyPass; outside comments, zero matches in DegenerusGame.sol for the afKing-mode symbol set; the ctor :222/:223 HAS_DEITY_PASS_SHIFT grant lines are unchanged (verified by diffing only those two lines vs baseline); IDegenerusGame.sol has the 4 afKing decls removed, hasDeityPass kept, and hasAnyLazyPass added.
  </acceptance_criteria>
  <done>The afKing-mode surface is deleted keeping+exposing hasAnyLazyPass, the interface is updated, and the ctor Deity grant is preserved byte-unmodified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PROTO-04 batchPurchase (keeper-gated, per-player try/catch + slice-refund, CEI)</name>
  <behavior>
    - A keeper batch of N players completes even when one player's _purchaseFor reverts: the reverting player's value slice is refunded, that player skipped, the other N-1 purchases succeed (non-brick).
    - batchPurchase reverts whole-batch with a clean abort when rngLocked or game-over is true at entry (pre-checked once before any per-player work).
    - A caller other than AF_KING reverts the keeper gate; the keeper does NO per-player approval check.
    - One batch value transfer covers the whole batch (not one hop per player); a post-loop day-stamp follows the per-player work.
    - No double-buy via a reentrant sweep/cancel (the in-context per-player sub-call rolls back on revert; state-update precedes external call per CEI).
  </behavior>
  <read_first>
    - 317-LEDGER.md (live lines: purchase/_purchaseFor, rngLocked, the CEI ETH-send idiom, the per-item self-external-call try/catch idiom, the mint→lootbox→prize-pool→EV-cap→quest callback chain entry points)
    - 316-SPEC.md `## ADD Design — Do-Work Crank` → batchPurchase subsection + `## SPEC-Open Resolutions` OPEN-C (CEI-proof with mandatory IMPL trace)
    - 317-PATTERNS.md `contracts/DegenerusGame.sol` PROTO-04 + the CEI / per-item-isolation shared-pattern sections
    - contracts/DegenerusGame.sol
  </read_first>
  <action>
    Add batchPurchase(address[] players, uint256[] amounts, uint8[] modes) keeper-gated on msg.sender == ContractAddresses.AF_KING (NO per-player approval — it trusts the keeper, which only acts on its own subscribers). Pre-check rngLocked and game-over ONCE at entry for a clean whole-batch abort. Take ONE batch value transfer (one value-hop for the whole batch). Per player, run the existing _purchaseFor unit inside a per-item self-external-call try/catch (try this.<purchaseEntry>(...) {} catch { refund that player's value slice; skip; continue; }) so any per-player revert deep in the mint→lootbox→prize-pool→EV-cap→quest path skips-and-refunds rather than bricking the batch. Apply CEI: state-update before external call, a post-loop day-stamp. The signature MUST match the locked PROTO-04 shape exactly because the keeper's batchPurchase call site (Plan 04) is authored against it. MANDATORY: trace the full mint→lootbox→prize-pool→EV-cap→quest callback chain for any external call that re-enters before the day-stamp; add an explicit reentrancy guard ONLY IF a re-entrant path is found, and record the trace finding in the SUMMARY (this CEI-vs-guard proof is the highest-scrutiny surface, routed to contract-auditor at 318/320 — do NOT attempt the deep adversarial audit inline here, just the structural trace + the conditional guard). Comments describe what IS.
  </action>
  <verify>
    <automated>grep -q "function batchPurchase" contracts/DegenerusGame.sol && grep -q "AF_KING" contracts/DegenerusGame.sol</automated>
  </verify>
  <acceptance_criteria>
    batchPurchase(address[],uint256[],uint8[]) (matching the PROTO-04 element types/order locked in the SPEC) exists, keeper-gated on AF_KING, with: a once-at-entry rngLocked/game-over pre-check, ONE batch value transfer, per-player try this.<entry>(...) {} catch {} slice-refund-and-continue, and a post-loop day-stamp; the callback-chain reentrancy trace is recorded in the SUMMARY with the explicit-guard decision (added IFF a re-entrant path was found). No per-player approval check exists in batchPurchase.
  </acceptance_criteria>
  <done>batchPurchase is keeper-gated, non-brick via per-player try/catch + slice-refund, single value transfer, CEI-traced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The permissionless do-work crank (bets caller-list + box cursor + reward model)</name>
  <behavior>
    - Resolving a list whose item 0 is already resolved reverts immediately with BatchAlreadyTaken (reusing item 0's SLOAD); items 1..N are each wrapped in per-item try/catch so a benign partial-overlap leaves the live tail.
    - A stale/reverting/not-ready item is skipped (onlySelf self-call + try/catch) and only successful items earn reward.
    - WWXRP work (currency == 3) is resolvable but earns zero reward.
    - Reward per successful item = gasUnits(workType)·0.5gwei → BURNIE via the zero-guarded _ethToBurnieValue (bad/zero price → reward 0, never revert); rewards accumulate in memory and pay as exactly ONE creditFlip(caller, sum) per tx.
    - Reward is credited to whoever calls, including a player resolving their own item (no caller restriction); gasUnits are fixed RESERVED constants, never gasleft()/tx.gasprice.
    - The box cursor enqueue/dequeue keys on the lootbox index and follows the v45 a303ae18 detect-preserve-re-issue path; the RngNotReady resolve guard and the one-reward-per-item box-zeroing are preserved untouched.
  </behavior>
  <read_first>
    - 317-LEDGER.md (live lines: resolveDegeneretteBets/resolveBets, degeneretteBets delete site, _requireApproved/_resolvePlayer, openLootBox/lootboxEth/lootboxEthBase/RngNotReady guard, _ethToBurnieValue, the advanceGame bounty creditFlip idiom, PriceLookupLib.priceForLevel)
    - 316-SPEC.md `## ADD Design — Do-Work Crank` (entries + work-type encoding, reward/charge, BatchAlreadyTaken, parameterless box cursor OPEN-D, WWXRP zero) + the OPEN-D box-cursor↔VRF-rotation coupling + `## VRF-Freeze Obligation Retirement`
    - 317-PATTERNS.md `contracts/DegenerusGame.sol` crank section + the OPEN-D box-cursor landmine note + the gas-pegged-bounty + per-item-isolation shared patterns
    - contracts/DegenerusGame.sol, and the DegeneretteModule + LootboxModule + MintModule + AdvanceModule + PriceLookupLib analog sites referenced by the ledger
  </read_first>
  <action>
    Add the permissionless do-work crank entry(s) on DegenerusGame. BETS: a caller-supplied off-chain-discovered (player, betId[]) grouped-by-player work-list path reusing the resolveDegeneretteBets/resolveBets machinery, with _requireApproved RELAXED for the resolve path only (placement stays gated; mirror _resolvePlayer). Implement the CRANK-02 BatchAlreadyTaken short-circuit: check item 0's resolved-state (degeneretteBets[player0][betId0] == 0 via the SLOAD item 0 needs anyway) → revert immediately; wrap items 1..N in per-item onlySelf self-external-call try/catch (skip stale/reverting, reward only successes). BOXES: route the reward through a parameterless cursor (collision-free, advanceGame-style self-partition) keyed on the lootbox index, with the enqueue signal = existing lootboxEthBase == 0 first-deposit and dequeue/zero on open; the box RngNotReady resolve guard and the lootboxEth[index][player]=0/lootboxEthBase one-reward-per-item zeroing are preserved untouched. The box cursor MUST follow the v45 a303ae18 detect-preserve-re-issue path for the orphan-index keyspace — any index-keyed cursor without that coupling re-introduces the v45 catastrophe; record the coupling explicitly and route the deep freeze-under-rotation audit to 320. WWXRP (currency == 3) is resolvable but earns ZERO reward (CRANK-04). REWARD: per successful item compute gasUnits(workType)·0.5gwei → BURNIE via the zero-guarded _ethToBurnieValue (price-zero → reward 0, never revert — OPEN-B), using FIXED RESERVED per-work-type gasUnits/*_ETH_TARGET named constants (numbers calibrated at Phase 319 — reserve the names with placeholder values; NEVER gasleft()/tx.gasprice — REW-03); accumulate per-item rewards in memory and grant exactly ONE creditFlip(caller, sum) at tx end (REW-02); credit whoever calls including a self-resolver (REW-04, no caller restriction, no self-exclusion). Comments describe what IS.
  </action>
  <verify>
    <automated>grep -q "BatchAlreadyTaken" contracts/DegenerusGame.sol && grep -qE "_ethToBurnieValue|creditFlip" contracts/DegenerusGame.sol</automated>
  </verify>
  <acceptance_criteria>
    The crank entry(s) exist with: the BatchAlreadyTaken item-0 short-circuit, per-item onlySelf+try/catch reward-only-successes, the parameterless box cursor keyed on the lootbox index WITH the a303ae18 re-issue coupling recorded (and the RngNotReady guard + box-zeroing preserved), WWXRP currency==3 → zero reward, reward via the zero-guarded _ethToBurnieValue with fixed RESERVED gasUnits constants paid as ONE creditFlip per tx, and no caller restriction; no gasleft()/tx.gasprice in the reward path. The OPEN-D box-cursor↔VRF coupling and the OPEN-C reentrancy disposition are recorded in the SUMMARY for the 318/320 security routing.
  </acceptance_criteria>
  <done>The crank settles bets + boxes permissionlessly, rewards only successes via one batched creditFlip, WWXRP earns zero, and the box cursor follows the a303ae18 re-issue coupling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous caller → crank entry | permissionless mass do-work over off-chain-discovered lists; bounded only by caller gas + the faucet locks. |
| keeper → batchPurchase | keeper-gated entry on the pinned AF_KING; no per-player approval (trusts the keeper). |
| crank box cursor → lootbox index keyspace | re-couples to the v45 VRF-rotation orphan-index surface — the milestone's biggest landmine. |
| ETH value in → mint→lootbox→prize-pool→EV-cap→quest callback chain | re-entrancy surface across batchPurchase's per-player work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-317-03-01 | Denial of Service | unbounded iteration / griefing on the permissionless crank | mitigate | per-item onlySelf + try/catch isolation, caller-bounded iteration (no contract-bounded loop), fixed gasUnits constants, BatchAlreadyTaken loser-gas-cap; deep gas-grief review routed to 318 TST |
| T-317-03-02 | Tampering / RNG | box-cursor index coupling re-introducing the v45 VRF-rotation orphan-index catastrophe | mitigate | box cursor MUST follow the v45 a303ae18 detect-preserve-re-issue path; RngNotReady guard preserved; HIGH-severity freeze-under-rotation re-attestation routed to 320 AUDIT (zero-day-hunter) — blocking if the coupling is absent |
| T-317-03-03 | Elevation | re-entrancy across the batchPurchase mint→lootbox→prize-pool→quest chain enabling a double-buy | mitigate | CEI (state-update before external call, once-at-entry batch debit, post-loop day-stamp) + per-player try/catch; mandatory IMPL callback-chain trace + explicit guard IFF a re-entrant path is found; CEI-vs-guard proof routed to contract-auditor at 318/320 |
| T-317-03-04 | Spoofing | a faucet round-trip earning net-positive reward (self-crank/Sybil) | mitigate | reward pegged below gas via fixed gasUnits·0.5gwei, paid as illiquid coinflip credit (must survive the flip edge), WWXRP excluded; faucet-resistance proof routed to 318 TST (SAFE-01) |
| T-317-03-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan (Solidity edits only) |
</threat_model>

<verification>
- `git diff --stat -- contracts/` shows DegenerusGame.sol + IDegenerusGame.sol only; the commit-guard hook keeps `contracts/` dirty (no commit here).
- hasAnyLazyPass exposed; afKing-mode symbol set grep-clean outside comments; ctor :222/:223 grant unchanged.
- batchPurchase keeper-gated; crank present with BatchAlreadyTaken + creditFlip; no gasleft()/tx.gasprice in the reward path.
</verification>

<success_criteria>
PROTO-01/PROTO-04 + the full crank (CRANK-01..04/REW-01..04) + RM-01 land on DegenerusGame.sol with the SUB-09 ctor grant preserved, the box cursor following the a303ae18 coupling, and `contracts/` left dirty for the single batched approval.
</success_criteria>

<output>
Create `.planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-03-SUMMARY.md` when done.
</output>
