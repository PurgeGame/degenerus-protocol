---
phase: 317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i
plan: 05
type: execute
wave: 3
depends_on: ["317-01", "317-02", "317-03", "317-04"]
files_modified:
  - contracts/modules/DegenerusGameJackpotModule.sol
  - contracts/modules/DegenerusGameAdvanceModule.sol
  - contracts/modules/DegenerusGamePayoutUtils.sol
  - contracts/storage/DegenerusGameStorage.sol
  - contracts/DegenerusVault.sol
  - contracts/StakedDegenerusStonk.sol
autonomous: true
requirements: [RM-02, RM-05, JGAS-01, JGAS-02, SUB-09]
must_haves:
  truths:
    - "RM-02 removes the free ETH auto-rebuy entirely — the AutoRebuyState struct + autoRebuyState mapping (storage), _processAutoRebuy + _calcAutoRebuy + AutoRebuyCalc, and the entropy param dropped from the 3-arg _addClaimableEth at all consume sites; ETH jackpot winnings always credit to claimable; the jackpot credit path no longer consumes a VRF word."
    - "JGAS-02 deletes the daily-ETH two-call split across BOTH modules — SPLIT_* tags, JACKPOT_MAX_WINNERS, resumeEthPool reads/writes + storage decl, _resumeDailyEth, splitMode param + routing, call1Bucket mask, the split-threshold branch (JackpotModule) AND STAGE_JACKPOT_ETH_RESUME + its assignment + the whole resume-check block (AdvanceModule); the daily ETH jackpot completes in ONE call at the preserved 305-winner ceiling with no winner-count/scaling/EV change."
    - "The JGAS-01 SPEC-locked decision ('REMOVE pending JGAS-04 empirical confirmation', 305 ceiling preserved, freeze-invariant SAFE) is REALIZED here as the JGAS-02 deletion — this plan is the IMPL wiring of the JGAS-01 design lock; the freeze-SAFE J5 verdict (no _unlockRng move, fewer in-window inputs, single-call removes the cross-tx resumeEthPool carry) is preserved for 320 AUDIT re-attestation under emergency rotation."
    - "RM-05 cross-contract cascade pruned — IDegenerusGame/IBurnieCoinflip decls already handled in Plan 03/02; Vault gameSetAutoRebuy/gameSetAutoRebuyTakeProfit/gameSetAfKingMode + local iface decls removed (coinSet* KEPT); sStonk setAfKingMode init + local decl removed."
    - "SUB-09 self-subscribe init wiring lands — sDGNRS self-subscribes (claimable-only, lootbox mode, flat 1 + 2% reinvest + setCoinflipAutoRebuy(self,true,0)) replacing the deleted setAfKingMode init; Vault self-subscribes (claimable-only, flat 1, no reinvest, no BURNIE rebuy); both via player==msg.sender self-consent; the AfKing.subscribe call signatures match Plan 04 exactly."
    - "The 2-arg DegeneretteModule._addClaimableEth overload is UNTOUCHED (distinct from the JackpotModule 3-arg form); _budgetToTicketUnits/AutoRebuyCalc orphan-checked zero-caller before deletion; _unlockRng placement UNCHANGED; the PRESERVE set (DAILY_ETH_MAX_WINNERS=305, scale, 159/95/50/1 buckets) untouched."
  artifacts:
    - path: "contracts/storage/DegenerusGameStorage.sol"
      provides: "AutoRebuyState struct + autoRebuyState mapping + resumeEthPool slot deleted"
    - path: "contracts/modules/DegenerusGameJackpotModule.sol"
      provides: "entropy drop + split mechanism removed at the preserved 305 ceiling"
    - path: "contracts/modules/DegenerusGameAdvanceModule.sol"
      provides: "STAGE_JACKPOT_ETH_RESUME + resume-check block removed"
    - path: "contracts/StakedDegenerusStonk.sol"
      provides: "setAfKingMode init replaced by sDGNRS self-subscribe (SUB-09)"
    - path: "contracts/DegenerusVault.sol"
      provides: "gameSet* wrappers removed; Vault self-subscribe (SUB-09)"
  key_links:
    - from: "StakedDegenerusStonk init"
      to: "AfKing.subscribe (self-consent)"
      via: "SUB-09 sDGNRS self-subscribe replacing setAfKingMode"
      pattern: "subscribe"
    - from: "DegenerusVault init"
      to: "AfKing.subscribe (self-consent)"
      via: "SUB-09 Vault self-subscribe"
      pattern: "subscribe"
    - from: "DegenerusVault RM-05 cascade"
      to: "IBurnieCoinflip.sol (Plan 02 settleFlipModeChange removal)"
      via: "RM-05 cross-contract decl coherence"
      pattern: "settleFlipModeChange"
    - from: "JackpotModule resumeEthPool deletion"
      to: "storage slot 33 → −2 region shift"
      via: "combined slot re-derivation (Plan 06)"
      pattern: "resumeEthPool"
---

<objective>
Apply the removal-and-fold surface that spans the shared modules/storage/Vault/sStonk: RM-02 (free ETH auto-rebuy + entropy drop), JGAS-02 (the daily-ETH two-call split removal across JackpotModule + AdvanceModule), RM-05 (the Vault/sStonk cross-contract cascade), and the SUB-09 self-subscribe init wiring. NO commit (the commit-guard hook keeps `contracts/` dirty for the single batched approval).

Purpose: these six files form a tight cluster — the storage deletions (AutoRebuyState slot 19 + resumeEthPool slot 33) drive the −2 compounded slot shift, the entropy drop threads through the JackpotModule consume sites, and the Vault/sStonk SUB-09 self-subscribe replaces the deleted setAfKingMode init using the AfKing.subscribe signature from Plan 04 and the IDegenerusGame edits from Plan 03 — hence the Wave-3 dependency on Plans 03 + 04. The RM-05 cross-contract cascade also coheres with Plan 02's IBurnieCoinflip settleFlipModeChange removal (depends_on 317-02). Storage-layout BREAK is acceptable (pre-launch redeploy-fresh). The test-side SLOT_* re-derivation it triggers is owned by Plan 06.
Output: the six files patched; the post-deletion storage layout is the input to Plan 06's combined `forge inspect`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-LEDGER.md
@.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md
@.planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-PATTERNS.md

<interfaces>
<!-- Use the LIVE line numbers from 317-LEDGER.md (RM + JGAS Footprint sections) — authoritative over the SPEC/PATTERNS snapshot. -->
<!-- RM-02: AutoRebuyState struct + autoRebuyState mapping (storage); _addClaimableEth 3-arg (beneficiary,weiAmount,entropy) → reduce to direct _creditClaimable, drop entropy at all 8 consume sites; DELETE _processAutoRebuy + _calcAutoRebuy + AutoRebuyCalc. Pitfall 4: the DegeneretteModule 2-arg _addClaimableEth overload is DISTINCT — UNTOUCHED. -->
<!-- JGAS-02: SPLIT_NONE/CALL1/CALL2, JACKPOT_MAX_WINNERS=160, resumeEthPool reads/writes + storage decl, _resumeDailyEth, splitMode param + routing, call1Bucket mask, split-threshold branch (JackpotModule); STAGE_JACKPOT_ETH_RESUME=8 + its assignment + the whole if(resumeEthPool!=0){...} resume-check block (AdvanceModule). Renumbering stages is OPTIONAL/cosmetic. _unlockRng placement UNCHANGED (coin-tickets stage). -->
<!-- PRESERVE: DAILY_ETH_MAX_WINNERS=305, DAILY_JACKPOT_SCALE_MAX_BPS=63_600, the 159/95/50/1 bucket derivation — NOT in the deletion set. -->
<!-- SUB-09 sDGNRS: claimable-only, useTickets=false, dailyQuantity=1, reinvestPct=2%, PLUS setCoinflipAutoRebuy(self,true,0). Vault: claimable-only, dailyQuantity=1, reinvestPct=0, NO BURNIE rebuy. Both self-subscribe player==msg.sender. AfKing.subscribe signature MUST match Plan 04. -->
<!-- RM-05 Vault: remove gameSetAutoRebuy/gameSetAutoRebuyTakeProfit/gameSetAfKingMode + local iface decls setAutoRebuy/setAutoRebuyTakeProfit/setAfKingMode; KEEP coinSetAutoRebuy/coinSetAutoRebuyTakeProfit. sStonk: remove setAfKingMode init call + local decl; KEEP the :404 public re-claim. The RM-05 cascade coheres with Plan 02's IBurnieCoinflip.settleFlipModeChange removal (depends_on 317-02). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: RM-02 storage + jackpot entropy/auto-rebuy removal (with orphan checks)</name>
  <read_first>
    - 317-LEDGER.md (RM + PROTO + Crank Ledger: AutoRebuyState/autoRebuyState/resumeEthPool decls, _addClaimableEth 3-arg + the auto-rebuy block + the 8 consume sites, _processAutoRebuy, _calcAutoRebuy/AutoRebuyCalc, the _budgetToTicketUnits orphan, the 2-arg DegeneretteModule overload, JackpotEthWin event fields)
    - 316-SPEC.md `## REMOVE Footprint` RM-02 + `## VRF-Freeze Obligation Retirement` (entropy cascade + the JackpotEthWin ABI-break note + the IMPL grep-the-threading-chain obligation + Pitfall 4)
    - 317-PATTERNS.md JackpotModule + PayoutUtils + Storage sections
    - contracts/storage/DegenerusGameStorage.sol, contracts/modules/DegenerusGameJackpotModule.sol, contracts/modules/DegenerusGamePayoutUtils.sol
  </read_first>
  <action>
    In DegenerusGameStorage.sol DELETE struct AutoRebuyState + the mapping(address => AutoRebuyState) autoRebuyState (slot 19). In DegenerusGameJackpotModule.sol DELETE the auto-rebuy block in _addClaimableEth (the cold autoRebuyState[beneficiary] SLOAD + branch) and reduce the 3-arg _addClaimableEth(beneficiary, weiAmount, entropy) to credit claimable directly via _creditClaimable, dropping the entropy param at ALL consume sites enumerated in the ledger; DELETE _processAutoRebuy. The JackpotEthWin event loses rebuyLevel/rebuyTickets → a benign ABI break (off-chain indexer OUT OF SCOPE) — drop those fields. In DegenerusGamePayoutUtils.sol DELETE _calcAutoRebuy and (after a zero-caller orphan grep) struct AutoRebuyCalc; verify the file still compiles as a valid module (or is fully removed if zero surviving symbols). BEFORE deleting, grep-confirm zero surviving callers of _budgetToTicketUnits and AutoRebuyCalc and grep the full entropyState threading chain to confirm no OTHER reader of the threaded entropy survives. Do NOT touch the 2-arg DegeneretteModule._addClaimableEth overload (Pitfall 4 — distinct function). Post-removal, ETH winnings always credit to claimable. Comments describe what IS — no history prose.
  </action>
  <verify>
    <automated>[ "$(grep -v '^[[:space:]]*//' contracts/storage/DegenerusGameStorage.sol | grep -cE 'AutoRebuyState|autoRebuyState')" = "0" ] && [ "$(grep -v '^[[:space:]]*//' contracts/modules/DegenerusGameJackpotModule.sol | grep -cE '_processAutoRebuy')" = "0" ] && [ "$(grep -v '^[[:space:]]*//' contracts/modules/DegenerusGamePayoutUtils.sol | grep -cE '_calcAutoRebuy|AutoRebuyCalc')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    Outside comments: zero AutoRebuyState/autoRebuyState in storage, zero _processAutoRebuy in JackpotModule, zero _calcAutoRebuy/AutoRebuyCalc in PayoutUtils; the 3-arg _addClaimableEth no longer takes entropy and credits claimable directly; the 8 consume sites no longer pass entropy; _budgetToTicketUnits/AutoRebuyCalc orphan-grep recorded zero callers before deletion; the 2-arg DegeneretteModule overload is unchanged.
  </acceptance_criteria>
  <done>The free ETH auto-rebuy + entropy threading is removed; ETH winnings credit to claimable; orphans verified zero-caller.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: JGAS-02 daily-ETH two-call split removal across JackpotModule + AdvanceModule</name>
  <read_first>
    - 317-LEDGER.md (JGAS-02 Footprint Ledger: SPLIT_*, JACKPOT_MAX_WINNERS, resumeEthPool reads/writes + storage decl, _resumeDailyEth, splitMode param + routing, call1Bucket mask, split-threshold branch; AdvanceModule STAGE_JACKPOT_ETH_RESUME + assignment + resume-check block; the _unlockRng-not-in-resume-branch confirmation; the PRESERVE set)
    - 316-SPEC.md `## JGAS-01 Decision Gate` (4) deletion footprint + (5) J5 freeze-SAFE verdict + the scope fence (mechanism-only at 305 ceiling)
    - 317-PATTERNS.md AdvanceModule (stage handler) + JackpotModule (split removal) sections
    - contracts/modules/DegenerusGameJackpotModule.sol, contracts/modules/DegenerusGameAdvanceModule.sol, contracts/storage/DegenerusGameStorage.sol
  </read_first>
  <action>
    In DegenerusGameJackpotModule.sol DELETE the three SPLIT_NONE/SPLIT_CALL1/SPLIT_CALL2 constants, JACKPOT_MAX_WINNERS=160 (DEAD on removal — split-routing threshold, NOT a winner cap), all resumeEthPool reads/writes (the resume-check, the _resumeDailyEth body read, the call-2 read+zero, the call-1 gated write), the _resumeDailyEth function, the splitMode param + its routing (the SPLIT_CALL2 check + derivation locals), the call1Bucket mask (decl/build/skip-routing), and the split-threshold branch + the _processDailyEth(... splitMode ...) call — collapse to the UNCONDITIONAL single-call path. In DegenerusGameStorage.sol DELETE uint128 internal resumeEthPool (slot 33). In DegenerusGameAdvanceModule.sol DELETE STAGE_JACKPOT_ETH_RESUME=8, its single stage assignment, and the WHOLE if(resumeEthPool!=0){ payDailyJackpot(true,...); stage=STAGE_JACKPOT_ETH_RESUME; break; } resume-check block. Stage renumbering (9/10/11→8/9/10) is OPTIONAL/cosmetic — do it only if it improves clarity, never required. PRESERVE byte-faithfully: DAILY_ETH_MAX_WINNERS=305, DAILY_JACKPOT_SCALE_MAX_BPS=63_600, and the 159/95/50/1 bucket derivation (NOT in the deletion set — zero winner-count/scaling/EV change). PRESERVE _unlockRng placement UNCHANGED (coin-tickets stage) — the resume branch never called it, so its deletion does not move the unlock. The daily ETH jackpot now completes in ONE call at the preserved 305 ceiling. Comments describe what IS — no history prose.
  </action>
  <verify>
    <automated>[ "$(grep -v '^[[:space:]]*//' contracts/modules/DegenerusGameJackpotModule.sol contracts/modules/DegenerusGameAdvanceModule.sol contracts/storage/DegenerusGameStorage.sol | grep -cE 'resumeEthPool|SPLIT_CALL1|SPLIT_CALL2|SPLIT_NONE|_resumeDailyEth|STAGE_JACKPOT_ETH_RESUME|call1Bucket|splitMode')" = "0" ] && grep -q "DAILY_ETH_MAX_WINNERS" contracts/modules/DegenerusGameJackpotModule.sol</automated>
  </verify>
  <acceptance_criteria>
    Outside comments, zero matches across the three files for resumeEthPool/SPLIT_NONE/SPLIT_CALL1/SPLIT_CALL2/_resumeDailyEth/STAGE_JACKPOT_ETH_RESUME/call1Bucket/splitMode; DAILY_ETH_MAX_WINNERS=305 + the scale + the 159/95/50/1 derivation remain; _unlockRng placement is unchanged; the daily ETH jackpot path is a single unconditional call; JACKPOT_MAX_WINNERS is deleted.
  </acceptance_criteria>
  <done>The two-call ETH split is removed across both modules at the preserved 305 ceiling with no EV change and _unlockRng unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: RM-05 Vault/sStonk cascade prune + SUB-09 self-subscribe init wiring</name>
  <read_first>
    - 317-LEDGER.md (RM-05 live lines: Vault local iface decls + gameSet* wrappers + coinSet* KEEP set; sStonk setAfKingMode decl + init call + the :404 re-claim KEEP; the IBurnieCoinflip.settleFlipModeChange removal handled in Plan 02) + the D-01b reconciliation note
    - 316-SPEC.md `## REMOVE Footprint` RM-05 + `## Protocol-Owned Subs (SUB-09)` (sDGNRS + Vault init configs, self-consent, permanent-deity free-renew)
    - 317-PATTERNS.md DegenerusVault + StakedDegenerusStonk sections
    - contracts/DegenerusVault.sol, contracts/StakedDegenerusStonk.sol, contracts/AfKing.sol (Plan 04 — for the live subscribe + setCoinflipAutoRebuy signatures), contracts/interfaces/IBurnieCoinflip.sol (Plan 02 — for the settleFlipModeChange removal coherence)
  </read_first>
  <action>
    In DegenerusVault.sol REMOVE the local interface decls setAutoRebuy/setAutoRebuyTakeProfit/setAfKingMode and the wrappers gameSetAutoRebuy/gameSetAutoRebuyTakeProfit/gameSetAfKingMode; KEEP coinSetAutoRebuy/coinSetAutoRebuyTakeProfit (BURNIE-side wrappers). In StakedDegenerusStonk.sol REMOVE the local decl setAfKingMode and the init call game.setAfKingMode(...); KEEP the public re-claim gameClaimWhalePass() at the live :404 site. Wire the SUB-09 self-subscribe init REPLACING the deleted setAfKingMode init: sDGNRS self-subscribes claimable-only (no pool deposit → empty _poolOf), lootbox mode (useTickets=false), dailyQuantity=1, reinvestPct=2%, PLUS setCoinflipAutoRebuy(self, true, 0) (full BURNIE-flip recycle at the kept flat 75bps); Vault self-subscribes claimable-only, dailyQuantity=1, reinvestPct=0, NO BURNIE rebuy. Both call AfKing.subscribe with player==msg.sender (SUB-02 self-consent — the contract IS the player) — the subscribe + setCoinflipAutoRebuy call signatures MUST match the LIVE AfKing.sol from Plan 04 (conform to the actual signatures, not the snapshot). The RM-05 IBurnieCoinflip.settleFlipModeChange decl removal is handled by Plan 02 (depends_on 317-02); confirm no surviving Vault/sStonk reference to it. Do NOT add any Deity-bit write — the permanent-deity free-renew relies on the existing DegenerusGame ctor grant (preserved in Plan 03). Comments describe what IS — no history prose.
  </action>
  <verify>
    <automated>[ "$(grep -v '^[[:space:]]*//' contracts/DegenerusVault.sol | grep -cE 'gameSetAutoRebuy|gameSetAutoRebuyTakeProfit|gameSetAfKingMode|setAfKingMode')" = "0" ] && [ "$(grep -v '^[[:space:]]*//' contracts/StakedDegenerusStonk.sol | grep -cE 'setAfKingMode')" = "0" ] && grep -q "coinSetAutoRebuy" contracts/DegenerusVault.sol && grep -q "subscribe" contracts/StakedDegenerusStonk.sol && grep -q "subscribe" contracts/DegenerusVault.sol</automated>
  </verify>
  <acceptance_criteria>
    Outside comments: zero gameSetAutoRebuy/gameSetAutoRebuyTakeProfit/gameSetAfKingMode/setAfKingMode in Vault, zero setAfKingMode in sStonk; coinSetAutoRebuy/coinSetAutoRebuyTakeProfit kept in Vault; sStonk + Vault each call AfKing.subscribe with player==msg.sender at init with the SUB-09 configs (sDGNRS adds setCoinflipAutoRebuy(self,true,0)); the call signatures match the LIVE AfKing.sol; no surviving settleFlipModeChange reference in Vault/sStonk (its decl removal owned by Plan 02); no new Deity-bit write added; the sStonk :404 re-claim is preserved.
  </acceptance_criteria>
  <done>The Vault/sStonk cascade is pruned and both protocol contracts self-subscribe at init with the SUB-09 configs matching the live AfKing signatures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VRF word → daily-ETH jackpot credit path | RM-02 removes a VRF consumer (the entropy-derived rebuy roll); JGAS-02 collapses two same-word consumptions into one. |
| storage slot layout (post-deletion) | two deletions (slot 19 + slot 33) compound a −2 shift for the slot-≥34 vrf/lootboxRng family; mis-derivation corrupts the v45 VRF-freeze surface. |
| sDGNRS/Vault init → AfKing.subscribe | the protocol contracts self-subscribe with un-spoofable self-consent; the SUB-09 configs are init-time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-317-05-01 | Tampering / RNG | dropping the entropy param stranding ANOTHER consumer or perturbing the freeze invariant | mitigate | grep the full entropyState threading chain before the drop; the 3-arg form is JackpotModule-only (2-arg overload untouched); JGAS J5 verdict freeze-SAFE (no _unlockRng move, fewer in-window inputs); re-attestation under emergency rotation routed to 320 AUDIT (zero-day-hunter) |
| T-317-05-02 | Tampering | a wrong slot re-derivation corrupting the v45 vrf/lootboxRng −2 family | mitigate | the storage deletions land HERE; the −2 combined re-derivation is owned by Plan 06 (one forge inspect, never blind −1); Plan 06 depends on this plan's post-deletion tree |
| T-317-05-03 | Information disclosure | the daily ETH jackpot single-call mis-paying a bucket / stranding a payout via the dropped resumeEthPool carry | mitigate | mechanism-only removal at the preserved 305 ceiling (305/scale/buckets PRESERVED); single-call sums the same call1+call2 work; 305-winner single-call correctness proof routed to 318 TST (JGAS-03) |
| T-317-05-04 | Spoofing | the sDGNRS/Vault self-subscribe being callable by a non-self party | mitigate | self-subscribe uses player==msg.sender self-consent (the contract IS the player); the protocol-sub exemption keys on pinned identity (Plan 04) |
| T-317-05-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan (Solidity edits only) |
</threat_model>

<verification>
- `git diff --stat -- contracts/` shows the six files touched; the commit-guard hook keeps `contracts/` dirty (no commit here).
- The SC#4 RM-02 + JGAS-02 grep sets return zero matches outside comments across storage + the two modules.
- The PRESERVE set (305/scale/buckets) survives; the 2-arg overload + _unlockRng placement unchanged; Vault/sStonk self-subscribe at init.
</verification>

<success_criteria>
RM-02 + JGAS-02 + RM-05 + SUB-09 init wiring land across the six shared files at the preserved 305 ceiling with the freeze invariant intact and the post-deletion storage layout ready for Plan 06's combined re-derivation; `contracts/` left dirty for the single batched approval.
</success_criteria>

<output>
Create `.planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-05-SUMMARY.md` when done.
</output>
