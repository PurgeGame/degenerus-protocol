---
phase: 317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i
plan: 04
type: execute
wave: 2
depends_on: ["317-01"]
files_modified:
  - contracts/AfKing.sol
autonomous: true
requirements: [SUB-01, SUB-02, SUB-03, SUB-04, SUB-05, SUB-06, SUB-07, SUB-08, REW-01, REW-02, REW-03, PROTO-04]
must_haves:
  truths:
    - "contracts/AfKing.sol exists as the canonical in-tree keeper (D-01), reworked from StreakKeeperV2.sol against the INTENDED end-state (not the mixed live source): owner-less / no-admin, inline ContractAddresses call sites, the 3 economic immutables only."
    - "The sweep is the parameterless sweep(uint256 maxCount) + internal daily-reset sweepCursor (replacing the OLD caller-supplied sweep(startIdx,count)) with concurrent same-block self-partition via the advancing cursor + lastSweptDay idempotency + stall-escalating bounty (SUB-03 / SAFE-03)."
    - "Quantity = max(dailyQuantity, floor(claimable×reinvestPct/price)) with dailyQuantity uint8 minimum 1 and reinvestPct + windowPaid packed into the Sub struct's free bytes — NO new storage slot (SUB-04 / GAS-04)."
    - "The funding waterfall (claimable→combined→directEth→InsufficientPool-skip) is preserved byte-faithfully; claimable-only = empty _poolOf; the two-tier skip-kill cancels NORMAL subs on a funding skip via swap-pop but EXEMPTS Vault/sDGNRS by pinned ContractAddresses.VAULT/SDGNRS identity, never a settable flag (SUB-05 / SUB-06)."
    - "The per-player purchase switches to the game's PROTO-04 batchPurchase with the signature matching DegenerusGame.batchPurchase EXACTLY; the charge is burnForKeeper all-or-nothing (PROTO-02), the bounty is ONE gas-pegged creditFlip per tx (SUB-08 / REW); tombstone-on-cancel + windowPaid-gated _subOf reclaim (SUB-07)."
  artifacts:
    - path: "contracts/AfKing.sol"
      provides: "Reworked in-tree AfKing keeper (cursor sweep, reinvestPct, two-tier skip-kill, batchPurchase switch, burnForKeeper, full creditFlip)"
      contains: "function sweep"
  key_links:
    - from: "contracts/AfKing.sol sweep purchase call"
      to: "DegenerusGame.batchPurchase(address[],uint256[],uint8[])"
      via: "PROTO-04 cross-repo signature match"
      pattern: "batchPurchase"
    - from: "contracts/AfKing.sol charge"
      to: "BurnieCoin.burnForKeeper"
      via: "all-or-nothing burn (if burned != cost auto-pause)"
      pattern: "burnForKeeper"
    - from: "two-tier skip-kill exemption"
      to: "ContractAddresses.VAULT / SDGNRS"
      via: "pinned-identity branch (never a settable flag)"
      pattern: "ContractAddresses.VAULT|ContractAddresses.SDGNRS"
---

<objective>
Bring `StreakKeeperV2.sol` in-tree as the canonical `contracts/AfKing.sol` (D-01) and rework it to the INTENDED end-state: the parameterless cursor sweep, the `reinvestPct` + `windowPaid` packed fields, the two-tier skip-kill by pinned identity, the `batchPurchase` switch, the `pull/mint→burn` rename, and the full `creditFlip` bounty. NO commit (the commit-guard hook now covers this file per D-01a, so it stays dirty for the single batched approval).

Purpose: AfKing is a self-contained NEW file with no overlap against Plans 02/03/05, so it runs parallel in Wave 2. Its `batchPurchase` call site drives the PROTO-04 signature contract (Plan 03), and its `burnForKeeper` consumer is the all-or-nothing contract (Plan 02). The keeper's live source is a MIXED transitional state (pull/mint still present, cursor/reinvestPct/windowPaid absent) — author against the SPEC end-state, NOT the current source.
Output: `contracts/AfKing.sol` (canonical in-tree keeper). The `../degenerus-utilities` single-source reconciliation (D-01b) is wired in Plan 06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-LEDGER.md
@.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md
@.planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-PATTERNS.md

<interfaces>
<!-- Source to rework: ../degenerus-utilities/contracts/StreakKeeperV2.sol — its OLD sweep(startIdx,count) loop body is the per-player processing analog to KEEP; only the iteration driver changes. -->
<!-- Sub struct (maximal-packing model): dailyQuantity uint8 / drainGameCreditFirst bool / useTickets bool / lastSweptDay uint32 / paidThroughDay uint32 — ADD reinvestPct (uint8) + windowPaid (1 bit) into FREE bytes, NO new slot. Slots 0-3 LOCKED (_poolOf/_subOf/_subscribers/_subscriberIndex). -->
<!-- Funding waterfall (PRESERVE byte-faithfully): claimable>cost→Claimable; cred>1→Combined; else DirectEth; if _poolOf[player]<msgValue → PlayerSkipped(player,3) continue. CEI: debit _poolOf → external purchase{value} → day-stamp. -->
<!-- batchPurchase switch MUST match DegenerusGame.batchPurchase(address[],uint256[],uint8[]) exactly (PROTO-04, Plan 03). -->
<!-- burnForKeeper consumption: uint256 burned = IBurnie(COIN).burnForKeeper(player, extractCost); if (burned != extractCost) auto-pause (dailyQuantity=0; _removeFromSet; emit SubscriptionExpired; continue; NO ++i). -->
<!-- swap-pop _removeFromSet (hand-inlined OZ EnumerableSet, 1-indexed, NOT imported); bounty = successfulPlayers * ((ETH_TARGET*PRICE_COIN_UNIT)/mp) → ONE creditFlip(msg.sender,sum)/tx. -->
<!-- Use the LIVE keeper line numbers from 317-LEDGER.md — authoritative over the SPEC/PATTERNS snapshot. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create contracts/AfKing.sol from StreakKeeperV2 + Sub-struct packing + cursor-sweep rework</name>
  <read_first>
    - 317-LEDGER.md (the live keeper transitional-state table + the OLD sweep loop body lines + the Sub struct + the funding-waterfall + CEI ordering lines)
    - 316-SPEC.md `## ADD Design — Subscription Sweep & Authorization` (cursor sweep SUB-03, lifecycle SUB-07, authorization SUB-02, pass-OR-pay SUB-01/08) + the keeper transitional-state caveat
    - 317-PATTERNS.md `contracts/AfKing.sol` section (owner-less posture, Sub struct, sweep cursor rework, swap-pop, bounty payout) + the swap-pop / gas-pegged-bounty / iterable-set shared patterns
    - ../degenerus-utilities/contracts/StreakKeeperV2.sol (the source to rework)
  </read_first>
  <action>
    Copy StreakKeeperV2.sol's structure into contracts/AfKing.sol (rename the contract + the keeper-rename succession STREAK_KEEPER_V2 to AF_KING, onlyStreakKeeper to onlyAfKing) preserving the owner-less / no-admin posture (inline IGame(ContractAddresses.GAME) / IBurnie(ContractAddresses.COIN) / ICoinflip(ContractAddresses.COINFLIP) call sites; constructor sets ONLY the 3 economic immutables with sanity reverts; no immutable BURNIE/IGAME injection). Extend the Sub struct by adding reinvestPct (uint8) + windowPaid (1 bit in a flags byte) into the struct's FREE bytes — NO new storage slot (slots 0-3 stay LOCKED). REPLACE the OLD caller-supplied sweep(uint256 startIdx, uint256 count) with the parameterless sweep(uint256 maxCount) + an internal daily-reset sweepCursor: each call resumes from the cursor, processes at most maxCount un-swept active entries, advances the cursor, and pays the per-chunk stall-escalating bounty (mirror advanceGame's progress-cursor + 1/2/4/6x bountyMultiplier). KEEP the OLD per-player processing loop body as the per-player model; only the iteration driver changes. Preserve the lastSweptDay idempotency backstop (skip when lastSweptDay >= today) so concurrent same-block callers self-partition with no double-buy. Comments describe what IS — no history prose.
  </action>
  <verify>
    <automated>test -f contracts/AfKing.sol && grep -q "function sweep(uint256 maxCount)" contracts/AfKing.sol && grep -q "sweepCursor" contracts/AfKing.sol && grep -q "reinvestPct" contracts/AfKing.sol && grep -q "windowPaid" contracts/AfKing.sol && [ "$(grep -c 'startIdx' contracts/AfKing.sol)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    contracts/AfKing.sol exists; sweep(uint256 maxCount) + sweepCursor replace the OLD sweep(startIdx,count) (zero startIdx matches); reinvestPct + windowPaid are packed into the Sub struct's free bytes with NO new storage slot declared; the owner-less posture (3 economic immutables, inline ContractAddresses call sites) is preserved; lastSweptDay idempotency backstop present.
  </acceptance_criteria>
  <done>AfKing.sol exists with the parameterless cursor sweep, packed reinvestPct/windowPaid, and the owner-less posture.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Quantity max-semantics + funding waterfall + two-tier skip-kill + lifecycle</name>
  <read_first>
    - 317-LEDGER.md (live funding-waterfall lines, the swap-pop _removeFromSet, the SUB-06 VAULT/SDGNRS pinned-identity sites, TICKET_SCALE/lootbox-cost lines)
    - 316-SPEC.md `## Quantity & Funding Model` (SUB-04 max-semantics + price-unit denomination, SUB-05 waterfall + claimable-only, SUB-06 two-tier skip-kill by pinned identity, transient-lootbox-floor-skip-not-kill distinction) + `## ADD Design` SUB-07 lifecycle
    - 317-PATTERNS.md `contracts/AfKing.sol` funding-waterfall + swap-pop sections
    - contracts/AfKing.sol (this plan's Task 1 output)
  </read_first>
  <action>
    Add the SUB-04 quantity model: effective daily buy = max(dailyQuantity, floor(claimable * reinvestPct / price)) computed ON TOP of the preserved waterfall, with dailyQuantity uint8 MINIMUM 1 (flat=0 disallowed; cancel is the tombstone setDailyQuantity(0) path). Keep it unit-consistent via TICKET_SCALE=400 so one dailyQuantity unit = one 1-price lootbox in both ticket and lootbox mode (no reinvest-rounds-to-0 no-op because max(...,>=1) floors back). PRESERVE the funding waterfall byte-faithfully (claimable>cost→Claimable msgValue=0; 1<cred<=cost→Combined msgValue=cost-(cred-1); cred<=1→DirectEth msgValue=cost; if _poolOf[player]<msgValue → PlayerSkipped(player,3) continue without advancing lastSweptDay). Keep claimable-only as an EMERGENT empty-_poolOf property — add NO claimableOnly flag. Implement the SUB-06 two-tier skip-kill: a NORMAL sub on a FUNDING skip (claimable+pool<cost, reason 3) is CANCELLED in-sweep via the swap-pop _removeFromSet (auto-pause sets dailyQuantity=0 + removes WITHOUT ++i; pool dust stays withdrawable); Vault + sDGNRS are EXEMPT — branch on the un-spoofable pinned ContractAddresses.VAULT / ContractAddresses.SDGNRS identity (a no-op-and-retry, NEVER a settable flag); a settable exemption flag is forbidden (trivial cancellation dodge). The renewal-lapse path still cancels BOTH tiers (the exemption is ONLY from the funding-skip kill). Keep the lootbox-floor transient skip (reason 4) distinct from the funding kill (stays in the set + retries). Implement SUB-07 lifecycle: tombstone-on-cancel moves nothing; in-sweep swap-pop reclaim WITHOUT ++i; _subOf delete on lapse/cancel KEEP only to preserve an unexpired PAID window (the windowPaid 1-bit flag set on burnForKeeper, cleared on the free pass-extend); stranded _poolOf stays withdrawable. Comments describe what IS.
  </action>
  <verify>
    <automated>grep -q "reinvestPct" contracts/AfKing.sol && grep -qE "ContractAddresses.VAULT|ContractAddresses.SDGNRS" contracts/AfKing.sol && grep -q "_removeFromSet" contracts/AfKing.sol</automated>
  </verify>
  <acceptance_criteria>
    The effective-buy max(dailyQuantity, floor(claimable*reinvestPct/price)) is present with dailyQuantity minimum 1; the waterfall branches (Claimable/Combined/DirectEth/InsufficientPool-skip) are preserved with the CEI debit-before-call ordering; the two-tier skip-kill cancels NORMAL subs via swap-pop and EXEMPTS Vault/sDGNRS by pinned-address branch (no settable exemption flag exists in the source); the lootbox-floor transient skip is distinct from the funding kill; SUB-07 tombstone/swap-pop/windowPaid-gated reclaim are implemented.
  </acceptance_criteria>
  <done>The quantity max-semantics, the preserved waterfall, and the pinned-identity two-tier skip-kill + SUB-07 lifecycle are wired.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: batchPurchase switch + burnForKeeper charge + full creditFlip bounty + authorization</name>
  <read_first>
    - 317-LEDGER.md (live CEI debit→call→stamp lines, the OLD per-player purchase{value} call site, the all-or-nothing burn consumption block, the bounty payout, the subscribe gate)
    - 316-SPEC.md `## PROTO Additions` (PROTO-04 batchPurchase shape, PROTO-02 burnForKeeper) + `## ADD Design — Subscription Sweep & Authorization` (SUB-02 authorization, SUB-01/08 pass-OR-pay) + `## Call-Graph Attestation` (4) keeper PROTO-side interface obligation (pull/mint→burn switch)
    - 317-PATTERNS.md `contracts/AfKing.sol` CEI / all-or-nothing-burn / bounty-payout sections
    - contracts/AfKing.sol (this plan's prior tasks), and the DegenerusGame.batchPurchase signature from Plan 03's 317-LEDGER/SPEC lock
  </read_first>
  <action>
    Switch the per-player purchase from the OLD per-player purchase{value} call to the game's PROTO-04 batchPurchase(address[],uint256[],uint8[]) — the call site signature MUST match DegenerusGame.batchPurchase EXACTLY (cross-repo contract; if Plan 03 finalized a different element order/type, conform to the LIVE DegenerusGame signature, not the snapshot). Preserve the CEI ordering (debit _poolOf before the external batched call, day-stamp after). Replace the pull/mint keeper-credit calls with the all-or-nothing burnForKeeper charge: uint256 burned = IBurnie(COIN).burnForKeeper(player, extractCost); if (burned != extractCost) auto-pause (dailyQuantity=0; _removeFromSet(player); emit SubscriptionExpired(player,1); continue WITHOUT ++i — clear windowPaid). Set windowPaid on a successful burnForKeeper. Pay the bounty as ONE gas-pegged creditFlip(msg.sender, successfulPlayers * ((BOUNTY_ETH_TARGET * PRICE_COIN_UNIT) / mp)) at tx end — never per-item (REW-02); revert NoSubscribersSwept if zero successes. Implement SUB-01 pass-OR-pay at the monthly renewal branch ONLY (paidThroughDay<=today): pass = hasAnyLazyPass(player) (the PROTO-01 view) → free pass-extend; no pass → burnForKeeper charge or skip-with-emit (never revert the whole sweep). Implement SUB-02 authorization: subscribe(address player, ...) checks self-consent (player==msg.sender or 0) or operator-approval (isOperatorApproved) ONCE at subscribe, third-party path only — NEVER at sweep. Remove all surviving pullForKeeper/mintForKeeper references. Comments describe what IS.
  </action>
  <verify>
    <automated>grep -q "batchPurchase" contracts/AfKing.sol && grep -q "burnForKeeper" contracts/AfKing.sol && grep -q "creditFlip" contracts/AfKing.sol && grep -q "hasAnyLazyPass" contracts/AfKing.sol && [ "$(grep -cE 'pullForKeeper|mintForKeeper' contracts/AfKing.sol)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    The per-player purchase calls the game's batchPurchase with the signature matching the LIVE DegenerusGame.batchPurchase exactly; the charge is the all-or-nothing burnForKeeper with the burned!=cost auto-pause; the bounty is ONE creditFlip per tx; the pass-OR-pay gate calls hasAnyLazyPass at the renewal branch only; SUB-02 authorization is checked once at subscribe (third-party path); zero pullForKeeper/mintForKeeper references survive; CEI debit-before-call ordering preserved.
  </acceptance_criteria>
  <done>The keeper calls batchPurchase, charges via all-or-nothing burnForKeeper, pays one creditFlip bounty, gates on hasAnyLazyPass, and the pull/mint references are gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous caller → sweep(maxCount) | permissionless sweep bountied per chunk; concurrent callers self-partition via the cursor. |
| keeper → DegenerusGame.batchPurchase | the keeper is the sole AF_KING-gated caller; the game trusts it (no per-player approval). |
| keeper → BurnieCoin.burnForKeeper | the all-or-nothing charge; shortfall burns nothing and auto-pauses the sub. |
| swept player vs ContractAddresses.VAULT/SDGNRS | the two-tier skip-kill exemption keys on un-spoofable pinned identity, never a settable flag. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-317-04-01 | Spoofing | a NORMAL sub spoofing the Vault/sDGNRS funding-skip-kill exemption | mitigate | exemption branches on the pinned ContractAddresses.VAULT/SDGNRS identity, never a settable flag; the spoof-resistance check routed to 320 AUDIT |
| T-317-04-02 | Denial of Service | a reverting/stale/not-ready player bricking the sweep, or a reentrant sweep/cancel double-buying | mitigate | per-player isolation via the game's batchPurchase try/catch; CEI debit-before-call + day-stamp-after; swap-pop WITHOUT ++i; non-brick + concurrency proofs routed to 318 TST (SAFE-02/03) |
| T-317-04-03 | Tampering | an un-refundable partial burn or a double-charge on the subscription | mitigate | burnForKeeper is all-or-nothing (burned!=cost → auto-pause, nothing burned); windowPaid gates the _subOf reclaim |
| T-317-04-04 | Denial of Service | a funding-margin griefer force-cancelling a fragile claimable-only sub | mitigate | a solvent pool-funded sub is never skipped by timing (cursor self-partition + lastSweptDay); claimable-only-NORMAL fragility surfaced (SPEC audit note b); griefing review routed to 320 AUDIT |
| T-317-04-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan (Solidity edits only) |
</threat_model>

<verification>
- `git diff --stat -- contracts/` shows contracts/AfKing.sol added; the commit-guard hook keeps `contracts/` dirty (no commit here).
- sweep(maxCount)+sweepCursor replace the OLD sweep; reinvestPct/windowPaid packed (no new slot); zero startIdx/pullForKeeper/mintForKeeper.
- batchPurchase + burnForKeeper + creditFlip + hasAnyLazyPass all present; pinned-identity exemption present.
</verification>

<success_criteria>
The canonical in-tree AfKing keeper exists with the cursor sweep, max-semantics quantity, preserved waterfall + pinned-identity two-tier skip-kill, the batchPurchase switch matching PROTO-04, the all-or-nothing burnForKeeper charge, and the single gas-pegged creditFlip bounty; `contracts/` left dirty for the single batched approval.
</success_criteria>

<output>
Create `.planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-04-SUMMARY.md` when done.
</output>
