---
phase: 317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-LEDGER.md
autonomous: true
requirements: [RM-04, RM-06, JGAS-02]
must_haves:
  truths:
    - "Every file:line cited by 316-SPEC across the 13 protocol files + the keeper is re-grep-verified against current source HEAD and recorded with a MATCH/DRIFT/MISSING verdict (D-01b single-source reconciliation noted)."
    - "The live keeper transitional-state is re-confirmed: pullForKeeper/mintForKeeper still present, sweepCursor/reinvestPct/windowPaid still absent — downstream edits author the INTENDED end-state, not the mixed live source."
    - "The pre-deletion test baseline-failure ledger is captured FIRST so RM-06/JGAS slot re-derivation deltas are attributable and 318 can prove 'no NEW failures'."
  artifacts:
    - path: ".planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-LEDGER.md"
      provides: "Confirmed file:line ledger + baseline-failure snapshot all downstream edit tasks read_first"
      contains: "MATCH"
  key_links:
    - from: "317-LEDGER.md"
      to: "317-02/03/04/05/06 edit tasks"
      via: "read_first dependency"
      pattern: "317-LEDGER"
---

<objective>
Produce the confirmed pre-patch file:line ledger that every downstream contract-edit plan reads first, and capture the pre-deletion test baseline so the RM-06/JGAS-02 slot re-derivation delta is attributable. ZERO source mutation in this plan — read + grep + forge inspect + write one markdown ledger only.

Purpose: 316-SPEC/PATTERNS were grep-verified at the v45 closure HEAD; per `feedback_verify_call_graph_against_source` every "single fn reaches all paths" / inline-duplicated-logic claim must be re-confirmed against the ACTUAL current source as the FIRST task before any edit. The PATTERNS source-freshness note flags the keeper's live source is partially ahead of the SPEC snapshot. This plan is the single source of truth for downstream file:line.
Output: `317-LEDGER.md` (confirmed ledger + baseline snapshot).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md
@.planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-PATTERNS.md
@.planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-grep-verify the RM + PROTO + crank file:line set against current source</name>
  <read_first>
    - 316-SPEC.md sections `## REMOVE Footprint`, `## PROTO Additions`, `## ADD Design — Do-Work Crank`, `## VRF-Freeze Obligation Retirement`
    - 317-PATTERNS.md (every `file:line` excerpt is the verification target)
    - The 13 protocol files: contracts/{ContractAddresses,BurnieCoin,BurnieCoinflip,DegenerusGame,DegenerusVault,StakedDegenerusStonk}.sol + contracts/modules/{DegenerusGameAdvanceModule,DegenerusGameJackpotModule,DegenerusGamePayoutUtils}.sol + contracts/storage/DegenerusGameStorage.sol + contracts/interfaces/{IBurnieCoinflip,IDegenerusGame}.sol + contracts/modules/DegenerusGameMintModule.sol (for `_ethToBurnieValue`) + contracts/libraries/{PriceLookupLib,BitPackingLib}.sol
  </read_first>
  <action>
    For EACH symbol the SPEC's RM-01..06 + PROTO-01..05 + crank/REW design sections cite, grep the actual current source and record the LIVE line number with a MATCH/DRIFT/MISSING verdict. Cover at minimum: RM-01 DegenerusGame afKing fns/events/error/consts/cross-calls + the KEEP `_hasAnyLazyPass` + ctor Deity grant `:222`/`:223`; RM-02 `AutoRebuyState`/`autoRebuyState`/`resumeEthPool` storage decls + `_addClaimableEth` 3-arg form + the auto-rebuy block + `_processAutoRebuy` + the 8 consume sites + `_calcAutoRebuy`/`AutoRebuyCalc`; RM-03 BurnieCoinflip `settleFlipModeChange` + the afKing/deity branches + the KEEP `RECYCLE_BONUS_BPS=75` vs DELETE `AFKING_RECYCLE_BONUS_BPS=100` value distinction + KEEP `processCoinflipPayouts`/`(rngWord & 1)`; RM-05 IDegenerusGame/IBurnieCoinflip/Vault/sStonk decls+wrappers+init; PROTO target sites (`onlyVault`/`_burn`/`vaultMintTo` in BurnieCoin, `onlyFlipCreditors`/`creditFlip` in BurnieCoinflip, the `VAULT`/`SDGNRS` const block in ContractAddresses, `purchase`/`_purchaseFor`/`_resolvePlayer`/`_requireApproved`/`rngLocked` in DegenerusGame); crank reuse sites (`resolveDegeneretteBets`/`resolveBets`/`degeneretteBets` delete site, `openLootBox`/`lootboxEthBase`/`lootboxEth` zeroing/`RngNotReady`, `_ethToBurnieValue`, `advanceGame` bounty `creditFlip` idiom). Run a zero-orphan grep for `_budgetToTicketUnits` and `AutoRebuyCalc` callers. Record every recorded SPEC DRIFT (the JackpotModule auto-rebuy block +2 → live, the `_distributePayout` `~738` interior offset, the IDegenerusGame MISSING resolution) confirmed-or-corrected against live source. Write the verified ledger to `317-LEDGER.md` under a `## RM + PROTO + Crank Ledger` heading. Grep hygiene: filter comment lines (`grep -v '^[[:space:]]*///' | grep -v '^[[:space:]]*//'`) where a count gate would otherwise be self-invalidated by doc prose.
  </action>
  <verify>
    <automated>test -f .planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-LEDGER.md && grep -q "RM + PROTO + Crank Ledger" .planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-LEDGER.md && grep -cE "MATCH|DRIFT|MISSING" .planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-LEDGER.md</automated>
  </verify>
  <acceptance_criteria>
    `317-LEDGER.md` contains a `## RM + PROTO + Crank Ledger` table with a verdict per cited symbol; every SPEC-recorded DRIFT/MISSING item is re-confirmed or corrected against live source; the `_hasAnyLazyPass :1610` body + the ctor Deity grant `:222`/`:223` are confirmed present byte-unmodified (RM-04 / SUB-09 preserve targets); zero-orphan grep results recorded for `_budgetToTicketUnits` and `AutoRebuyCalc`. No `contracts/` file is modified by this task.
  </acceptance_criteria>
  <done>The RM/PROTO/crank ledger section exists with a MATCH/DRIFT/MISSING verdict per symbol and zero source mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Re-grep the JGAS-02 two-module footprint + re-confirm the live keeper transitional state</name>
  <read_first>
    - 316-SPEC.md sections `## JGAS-01 Decision Gate` (4) deletion footprint + (5) J5 verdict, and `## Call-Graph Attestation` (2) JGAS table
    - contracts/modules/DegenerusGameJackpotModule.sol + contracts/modules/DegenerusGameAdvanceModule.sol + contracts/storage/DegenerusGameStorage.sol
    - ../degenerus-utilities/contracts/StreakKeeperV2.sol (the keeper to be reworked into AfKing) + ../degenerus-utilities/script + ../degenerus-utilities/test (deploy/wiring reconciliation, D-01b)
  </read_first>
  <action>
    Grep-verify each JGAS-02 deletion-footprint symbol against live source and record the live line + verdict in `317-LEDGER.md` under `## JGAS-02 Footprint Ledger`: `SPLIT_NONE`/`SPLIT_CALL1`/`SPLIT_CALL2`, `JACKPOT_MAX_WINNERS=160` (confirm it is split-routing-threshold-only, DEAD on removal, NOT a winner cap), `resumeEthPool` reads/writes (resume-check, `_resumeDailyEth` body read, read+zero call-2, write call-1 + its gate), `_resumeDailyEth` decl + call site, `splitMode` param + routing locals, `call1Bucket` mask decl/build/skip-routing, the split-threshold branch + the `_processDailyEth(... splitMode ...)` call, AdvanceModule `STAGE_JACKPOT_ETH_RESUME=8` + its single assignment + the whole resume-check block, and the `resumeEthPool` storage decl. Confirm the two cosmetic `+1` resume-check doc-vs-`if` drifts. Confirm PRESERVE set: `DAILY_ETH_MAX_WINNERS=305`, `DAILY_JACKPOT_SCALE_MAX_BPS=63_600`, the 159/95/50/1 bucket derivation are NOT in the deletion set. Re-confirm `_unlockRng` is NOT called inside the resume branch (J5 freeze trace). Then re-confirm the LIVE keeper transitional state: record `pullForKeeper`/`mintForKeeper`/`burnForKeeper`/`creditFlip`/`sweepCursor`/`reinvestPct`/`windowPaid` occurrence counts and the live `subscribe`/`sweep` signatures, and record the D-01b single-source reconciliation finding (how `../degenerus-utilities` will consume/deploy the canonical `contracts/AfKing.sol` — import or deploy-script update, plus the `AF_KING` pinned-address alignment with the deploy-predicted keeper address).
  </action>
  <verify>
    <automated>grep -q "JGAS-02 Footprint Ledger" .planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-LEDGER.md && grep -qiE "sweepCursor.*0|reinvestPct.*0|windowPaid.*0" .planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-LEDGER.md && grep -qi "D-01b" .planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-LEDGER.md</automated>
  </verify>
  <acceptance_criteria>
    `317-LEDGER.md` has a `## JGAS-02 Footprint Ledger` with a live line + verdict for every JGAS deletion symbol; the PRESERVE set (305 ceiling, scale, buckets) is recorded as out-of-deletion; `_unlockRng`-not-in-resume-branch is confirmed; the live keeper occurrence-count table (sweepCursor/reinvestPct/windowPaid = 0, pull/mint still present) is recorded; the D-01b single-source/deploy reconciliation path is written. Zero source mutation.
  </acceptance_criteria>
  <done>The JGAS-02 footprint ledger + keeper transitional-state table + D-01b reconciliation are recorded; zero source mutation.</done>
</task>

<task type="auto">
  <name>Task 3: Capture the pre-deletion test baseline-failure ledger (RM-06 attribution)</name>
  <read_first>
    - 316-SPEC.md `## Storage Slot-Shift Plan` (the 26 `SLOT_*` test files + the LootboxBoonCoexistence already-+1-stale + baseline-failure hazard)
    - test/ (the ~26 files containing `SLOT_*` constants)
  </read_first>
  <action>
    Run the existing test suite ONCE against the current unmodified tree and record the pre-deletion pass/fail baseline to `317-LEDGER.md` under `## Pre-Deletion Test Baseline`. Capture the count of failing tests and the named known-baseline failures (at minimum `LootboxBoonCoexistence`'s `test_lootboxBoonAppliedDespiteExistingCoinflipBoon`). Record the current `SLOT_*` value for the slot-≥34 family that will shift −2 (`lootboxRngWordByIndex`, `lootboxRngPacked`, `vrfCoordinator`, `lootboxDay`, `degeneretteBets`, `boonPacked`) and flag `LootboxBoonCoexistence`'s already-+1-stale constants so the post-deletion delta is attributable and the re-derivation is NOT blamed for the pre-existing failure. Use a fast non-deep profile for the baseline run; this is a snapshot, not a coverage run (318 owns coverage).
  </action>
  <verify>
    <automated>grep -q "Pre-Deletion Test Baseline" .planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-LEDGER.md && grep -qi "LootboxBoonCoexistence" .planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-LEDGER.md</automated>
  </verify>
  <acceptance_criteria>
    `317-LEDGER.md` `## Pre-Deletion Test Baseline` records the baseline pass/fail count, the named known-baseline failures, and the current slot-≥34 family `SLOT_*` values flagged as the −2 shift targets with the `LootboxBoonCoexistence` stale-baseline hazard called out. Zero source mutation (`git diff --stat -- contracts/` empty).
  </acceptance_criteria>
  <done>The pre-deletion baseline ledger exists with named known-failures and the −2-shift family flagged; zero source mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPEC-claimed file:line → live source | The SPEC's grep was at v45 HEAD; this plan re-verifies against the actual current tree before any edit relies on a stale anchor. |
| 316 grep snapshot → live keeper source | The keeper's live source is partially ahead of the snapshot; mis-trusting the snapshot would author against a phantom state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-317-01 | Tampering | downstream edits keyed on a stale file:line | mitigate | this plan is the FIRST dependency; every edit task `read_first`s `317-LEDGER.md`, not the SPEC anchors directly |
| T-317-02 | Information disclosure | mis-attributing a pre-existing test failure to the slot re-derivation | mitigate | capture the pre-deletion baseline-failure ledger FIRST; 318 proves "no NEW failures vs this baseline" |
| T-317-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan (read/grep/forge-inspect/write-markdown only) |
</threat_model>

<verification>
- `git diff --stat -- contracts/` returns empty after this plan (zero source mutation).
- `317-LEDGER.md` exists with the three ledger sections (RM+PROTO+Crank, JGAS-02 Footprint, Pre-Deletion Test Baseline) plus the keeper transitional-state table + D-01b reconciliation.
</verification>

<success_criteria>
The confirmed pre-patch file:line ledger + baseline snapshot exist and are the single source of truth downstream edit plans read first; no `contracts/` file is touched.
</success_criteria>

<output>
Create `.planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-01-SUMMARY.md` when done.
</output>
