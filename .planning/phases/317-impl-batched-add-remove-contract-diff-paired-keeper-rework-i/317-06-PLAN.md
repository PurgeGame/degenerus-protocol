---
phase: 317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i
plan: 06
type: execute
wave: 4
depends_on: ["317-02", "317-03", "317-04", "317-05"]
files_modified:
  - test/
  - contracts/test/
  - contracts/mocks/
autonomous: true
requirements: [RM-06, JGAS-02, RM-05]
must_haves:
  truths:
    - "Storage-layout slot constants are re-derived from ONE combined forge inspect on the POST-(RM-02+JGAS) DegenerusGame tree — the slot-≥34 vrf/lootboxRng family shifts −2 (NOT a blind −1), the [20,33) region shifts −1; ~28 SLOT_* consts across ~15 test files rewritten file-by-file from that authoritative output, never patch-by-arithmetic (RM-06)."
    - "forge build PASSES on the patched contract tree — contracts/test + mocks get COMPILE-FIXES ONLY (patch references to deleted symbols just enough to compile), NO new coverage and NO behavioral assertion rework (D-03; that is Phase 318 TST)."
    - "The paired ../degenerus-utilities repo compiles and is reconciled to consume/deploy the canonical contracts/AfKing.sol (D-01b — import or deploy-script update + the AF_KING pinned-address alignment), resolving the single-source-of-truth path; no divergent StreakKeeperV2 is maintained as the canonical."
    - "The post-deletion test delta is attributable against the Plan-01 pre-deletion baseline ledger — the LootboxBoonCoexistence already-+1-stale failure is NOT blamed on the re-derivation; 'no NEW failures' is left for Phase 318 to assert on the green-build tree."
    - "KNOWN_ISSUES and the BURNIE win/loss RNG path (processCoinflipPayouts, rngWord & 1) remain byte-UNMODIFIED across the whole diff (RM-06)."
  artifacts:
    - path: "test/"
      provides: "Re-derived SLOT_* constants across the ~15 affected test files (combined −2/−1 shift)"
    - path: "contracts/test/"
      provides: "Compile-fixes to references of deleted symbols (D-03)"
    - path: "contracts/mocks/"
      provides: "Compile-fixes to references of deleted symbols (D-03)"
  key_links:
    - from: "forge inspect storage-layout (post-deletion tree)"
      to: "test/ SLOT_* constants"
      via: "file-by-file re-derivation (one combined pass)"
      pattern: "SLOT_"
    - from: "../degenerus-utilities"
      to: "contracts/AfKing.sol"
      via: "D-01b single-source consume/deploy reconciliation"
      pattern: "AfKing|AF_KING"
---

<objective>
Make the patched tree green: re-derive the test-side storage slot constants from ONE combined `forge inspect` on the post-(RM-02+JGAS) contract (the compounded −2 / −1 shift), apply COMPILE-FIXES ONLY to `contracts/test` + `mocks` (D-03), and reconcile `../degenerus-utilities` to consume/deploy the canonical `contracts/AfKing.sol` (D-01b). The `test/` + planning/docs are AGENT-COMMITTED but DEFERRED until after the approved batched contract commit (the commit-guard hook blocks ALL commits while `contracts/*.sol` is dirty). SC#1 = `forge build` PASS on the patched tree AND the paired keeper compiles.

Purpose: this plan depends on ALL contract edits (Plans 02-05) being applied so `forge inspect` reflects the final layout — the contract source has ZERO numeric slot literals, so RM-06 is entirely a test-side problem. The slot-≥34 vrf/lootboxRng family the v45 VRF-freeze work depends on lands at −2, NOT −1 — a uniform decrement would mis-derive the entire region.
Output: green `forge build`, re-derived SLOT_* constants, the utilities single-source path resolved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-LEDGER.md
@.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md
@.planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-PATTERNS.md

<interfaces>
<!-- Combined shift (from the SPEC Storage Slot-Shift Plan): slot <19 unchanged; [20,33) −1; ≥34 −2. Key −2 family: vrfCoordinator 34→32, lootboxRngPacked 37→35, lootboxRngWordByIndex 38→36, lootboxDay 39→37, degeneretteBets 45→43, boonPacked 61→59; lootboxEthBase 20→19 (−1). -->
<!-- ~28 SLOT_* consts across ~15 files: BafRebuyReconciliation, BafFarFutureTickets, RngIndexDrainBinding(+handler), DegeneretteFreezeResolution, AdvanceGameRewrite, AffiliateDgnrsClaim, QueueDoubleBuffer, VRFCore, StorageFoundation, LootboxBoonCoexistence (already +1 stale + baseline-failing), LootboxRngLifecycle, VrfRotationOrphanIndex, StakedStonkRedemption, RngLockRotationDeterminism, RedemptionEdgeCases, VrfRotationLiveness, JackpotCombinedPool, TicketLifecycle, RngLockDeterminism, VRFStallEdgeCases, RedemptionInvariants.inv, RedemptionHandler. -->
<!-- D-03 scope: contracts/test + contracts/mocks = compile-fixes only (forge build compiles test/ too). NO new coverage, NO behavioral assertion rework (318 TST). -->
<!-- D-01b: ../degenerus-utilities must import-or-deploy the canonical contracts/AfKing.sol + align the AF_KING pinned address; resolve the single-source path. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: One combined forge inspect → re-derive every test SLOT_* file-by-file (RM-06)</name>
  <read_first>
    - 317-LEDGER.md `## Pre-Deletion Test Baseline` (the captured baseline-failure ledger + the current slot-≥34 family SLOT_* values + the LootboxBoonCoexistence stale-baseline flag)
    - 316-SPEC.md `## Storage Slot-Shift Plan` (the combined −2/−1 shift table + the re-derivation MANDATE + the stale-baseline compounding hazard)
    - 317-PATTERNS.md DegenerusGameStorage section (the −2-region warning + the test-side-only mandate)
    - the ~15 affected test files listed in the interfaces block
  </read_first>
  <action>
    Run forge inspect contracts/DegenerusGame.sol:DegenerusGame storage-layout ONCE on the POST-(RM-02+JGAS) tree (both autoRebuyState slot 19 AND resumeEthPool slot 33 already deleted by Plan 05). From that authoritative output, rewrite each test-side SLOT_* constant file-by-file: vars in [20,33) shift −1, vars ≥34 shift −2 (the vrf/lootboxRng family — vrfCoordinator 34→32, lootboxRngPacked 37→35, lootboxRngWordByIndex 38→36, lootboxDay 39→37, degeneretteBets 45→43, boonPacked 59 etc). NEVER patch-by-arithmetic and NEVER apply a blind −1 — read each new slot from the forge inspect output. Special-case LootboxBoonCoexistence.t.sol whose SLOT_* are ALREADY +1 stale (so its delta is not a uniform decrement): set its constants to the authoritative forge-inspect values, not a relative shift. Record the before/after forge inspect storage-layout (this is the D-04 review artifact #3). Do NOT touch contract source (it has zero slot literals). The "no NEW failures vs baseline" assertion is Phase 318's job — this task only re-derives the constants so the suite recompiles with correct slots.
  </action>
  <verify>
    <automated>forge inspect contracts/DegenerusGame.sol:DegenerusGame storage-layout > /tmp/317-post-layout.json 2>/dev/null && test -s /tmp/317-post-layout.json && grep -rq "SLOT_" test/</automated>
  </verify>
  <acceptance_criteria>
    `forge inspect` on the post-deletion DegenerusGame succeeds and the before/after layout is recorded; every affected test file's SLOT_* constants are rewritten from the authoritative forge-inspect output (the slot-≥34 family at −2, [20,33) at −1); LootboxBoonCoexistence's stale constants are set to the absolute forge-inspect values; zero contract-source slot literals changed.
  </acceptance_criteria>
  <done>The test SLOT_* constants are re-derived from one combined forge inspect with the −2 family correct; before/after layout recorded.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: contracts/test + mocks compile-fixes (D-03) → forge build PASS</name>
  <read_first>
    - 317-LEDGER.md (the full RM/JGAS deleted-symbol set — every reference to a deleted symbol in contracts/test + mocks must be compile-fixed)
    - 317-CONTEXT.md D-03 (compile-fixes only; no coverage, no behavioral rework — 318 owns that)
    - contracts/test/ + contracts/mocks/ files referencing the deleted symbols (afKing-mode, AutoRebuyState, _processAutoRebuy/_calcAutoRebuy, settleFlipModeChange, the JGAS split symbols, the JackpotEthWin event fields, the keeper pull/mint)
  </read_first>
  <action>
    Patch references to deleted symbols in contracts/test + contracts/mocks JUST ENOUGH to make forge build green — the minimal compile-fix: drop/stub calls to deleted afKing/auto-rebuy/split functions, update the JackpotEthWin event field-set, update any mock that implemented a removed interface decl, switch mock keeper calls from pull/mint to burnForKeeper if a mock exercises that path. Do NOT add new coverage, do NOT rework behavioral assertions, do NOT change test expectations — that is Phase 318 TST (D-03 boundary). Then run forge build and confirm it PASSES on the full patched tree (which compiles test/ too). The RM grep acceptance (SC#4) already EXCLUDES contracts/test+mocks, so these compile-fixes do not violate the grep-clean criterion. If forge build still fails, the failure is a missed compile-fix or a real contract-edit defect — diagnose and fix the compile-fix (escalate a suspected contract-edit defect to the relevant Wave-2/3 plan's surface rather than papering over it in test/).
  </action>
  <verify>
    <automated>forge build 2>&1 | tail -5; forge build >/dev/null 2>&1 && echo "FORGE_BUILD_PASS" || echo "FORGE_BUILD_FAIL"</automated>
  </verify>
  <acceptance_criteria>
    `forge build` exits 0 (FORGE_BUILD_PASS) on the full patched tree; the only contracts/test + mocks edits are compile-fixes to deleted-symbol references (no new test functions, no changed assertions); contract source is unchanged by this task.
  </acceptance_criteria>
  <done>forge build PASSES with compile-fixes-only on contracts/test + mocks.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Reconcile ../degenerus-utilities to the canonical AfKing (D-01b) + keeper compiles</name>
  <read_first>
    - 317-LEDGER.md (the D-01b single-source/deploy reconciliation finding from Plan 01)
    - 317-CONTEXT.md D-01/D-01a/D-01b (canonical in-tree AfKing; utilities consumes/deploys it)
    - ../degenerus-utilities/ (contracts/, script/, test/, ContractAddresses.sol, deploy broadcasts)
    - contracts/AfKing.sol (Plan 04 — the canonical source)
  </read_first>
  <action>
    Reconcile ../degenerus-utilities to consume/deploy the canonical contracts/AfKing.sol per the D-01b path recorded in the ledger (import the canonical source OR update the deploy script to deploy the in-tree AfKing) so a divergent StreakKeeperV2 is NOT maintained as the canonical. Align the ../degenerus-utilities ContractAddresses AF_KING pinned address with the deploy-predicted keeper address used in contracts/ContractAddresses.sol (PROTO-05). Update the keeper's unit-test selectors / deploy-script pinned-address constants to the reworked AfKing surface (the cursor sweep, reinvestPct, batchPurchase switch). Confirm the paired keeper compiles in its repo (forge build in ../degenerus-utilities, or the repo's build command). This is the keeper-side D-02 surface that the final wave presents for explicit USER review (the commit-guard hook does NOT watch this repo, so its commit gate is the executor pausing for approval at Plan 07).
  </action>
  <verify>
    <automated>cd ../degenerus-utilities 2>/dev/null && (forge build >/dev/null 2>&1 && echo "KEEPER_BUILD_PASS" || echo "KEEPER_BUILD_FAIL"); cd - >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    ../degenerus-utilities is reconciled to consume/deploy the canonical contracts/AfKing.sol (import or deploy-script update recorded), its AF_KING pinned address aligns with contracts/ContractAddresses.sol, and the paired keeper compiles (KEEPER_BUILD_PASS); no divergent StreakKeeperV2 remains the canonical source.
  </acceptance_criteria>
  <done>The utilities repo consumes the canonical AfKing, the pinned address aligns, and the keeper compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| post-deletion storage layout → test SLOT_* constants | a wrong re-derivation corrupts the v45 vrf/lootboxRng −2 family that the freeze-invariant tests depend on. |
| contracts/test+mocks → forge build green | D-03 boundary: compile-fixes only, never behavioral rework that would mask a real contract defect. |
| ../degenerus-utilities deploy → AF_KING pinned address | the cross-repo single-source path must align the pinned address the protocol gates resolve to. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-317-06-01 | Tampering | a blind −1 (instead of −2) mis-deriving the vrf/lootboxRng slot family | mitigate | re-derive from ONE authoritative forge inspect, never patch-by-arithmetic; special-case the already-+1-stale LootboxBoonCoexistence to absolute values |
| T-317-06-02 | Repudiation | a compile-fix masking a real contract-edit defect as a "test problem" | mitigate | D-03 compile-fixes-only; a suspected contract defect is escalated to the owning Wave-2/3 surface, not papered over; 318 asserts no NEW failures vs the Plan-01 baseline |
| T-317-06-03 | Tampering | the utilities deploy pinning a different AF_KING than contracts/ | mitigate | align the ../degenerus-utilities AF_KING with contracts/ContractAddresses.sol per the D-01b reconciliation; the keeper diff is USER-reviewed at Plan 07 (D-02) |
| T-317-06-SC | Tampering | npm/pip/cargo installs | accept | no package installs; `forge build` uses the existing toolchain (verified available) |
</threat_model>

<verification>
- `forge build` exits 0 on the patched tree; the paired keeper compiles.
- `forge inspect` before/after storage layout recorded (D-04 artifact #3).
- The −2 family slots are correct per the authoritative output; the utilities single-source path is resolved.
- No commit happens in this plan (commit-guard blocks while contracts/*.sol is dirty; test/docs commits DEFERRED to Plan 07).
</verification>

<success_criteria>
The patched tree builds green with re-derived slot constants (correct −2 family), compile-fixes-only on contracts/test+mocks, and the utilities repo reconciled to the canonical AfKing — SC#1 satisfied; all commits deferred to the Plan 07 approval gate.
</success_criteria>

<output>
Create `.planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-06-SUMMARY.md` when done.
</output>
