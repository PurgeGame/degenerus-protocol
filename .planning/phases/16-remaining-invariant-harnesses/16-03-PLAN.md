# 16-03: Vault Share Invariant (FUZZ-04)

## Goal
Assert vault share math is consistent: share tokens maintain correct supply and reserves are non-negative.

## Invariants
1. DGVE totalSupply > 0 (refill mechanism prevents zero)
2. DGVB totalSupply > 0 (refill mechanism prevents zero)
3. Vault ETH balance + stETH balance >= 0 (non-negative reserves)

## Implementation
- New file: `test/fuzz/invariant/VaultShare.inv.t.sol`
- Reuses existing handlers (game purchases trigger vault.deposit)
- Pure view-level assertions

## Acceptance
- `make invariant-test` passes with no violations
