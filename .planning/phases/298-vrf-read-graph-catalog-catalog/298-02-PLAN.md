---
phase: 298-vrf-read-graph-catalog-catalog
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/298-vrf-read-graph-catalog-catalog/298-02-CATALOG-section.md
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-04, CAT-06]
must_haves:
  truths:
    - "Consumer §2 (JackpotModule.payDailyJackpotCoinAndTickets at JackpotModule.sol:596) has a CAT-01 backward-trace entry"
    - "Every reachable SLOAD inside the resolution path is enumerated with file:line citation (no 'by construction' shortcuts)"
    - "Every participating slot has its full external/public writer set enumerated"
    - "Every (slot × writer × callsite) tuple carries a classification ∈ {EXEMPT-ADVANCEGAME, EXEMPT-VRFCALLBACK, EXEMPT-RETRYLOOTBOXRNG, VIOLATION}"
    - "Every VIOLATION row carries a remediation tactic ∈ {gated-revert, snapshot-anchor, pre-lock-reorder, immutable} + 1-line rationale"
  artifacts:
    - path: ".planning/phases/298-vrf-read-graph-catalog-catalog/298-02-CATALOG-section.md"
      provides: "Per-consumer §2 catalog content (CAT-01+CAT-02+CAT-03+CAT-04+CAT-06 rows for payDailyJackpotCoinAndTickets)"
      contains: "## CAT-01"
  key_links:
    - from: "298-02-CATALOG-section.md §B SLOAD table"
      to: "298-02-CATALOG-section.md §C writer enumeration"
      via: "Participating? = YES slots forward into §C"
      pattern: "Participating\\?.*YES"
---

<objective>
Backward-trace VRF-derived entropy from `JackpotModule.payDailyJackpotCoinAndTickets` (`contracts/modules/DegenerusGameJackpotModule.sol:596`) per D-298-CONSUMER-LIST-01 entry §2. Produce the §2 catalog section.

Purpose: Phase 298 CAT-01..06 coverage for VRF-consumer entry §2 (daily coin/tickets distribution). One of 13 per-consumer section files that Wave-2 aggregates into `.planning/RNGLOCK-CATALOG.md`.

Output: `.planning/phases/298-vrf-read-graph-catalog-catalog/298-02-CATALOG-section.md` with §A + §B + §C + §D + §E.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/298-vrf-read-graph-catalog-catalog/298-CONTEXT.md
@contracts/modules/DegenerusGameJackpotModule.sol
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Sub-agent backward-trace from payDailyJackpotCoinAndTickets</name>
  <files>.planning/phases/298-vrf-read-graph-catalog-catalog/298-02-CATALOG-section.md</files>
  <read_first>
    - .planning/phases/298-vrf-read-graph-catalog-catalog/298-CONTEXT.md (D-298-CONSUMER-LIST-01 §2; all 8 D-298-* decisions)
    - contracts/modules/DegenerusGameJackpotModule.sol (consumer entry at :596)
    - .planning/milestones/v41.0-phases/287-jackpot-influence-surface-closure-jpsurf/287-01-JPSURF-AUDIT.md (Phase 287 JPSURF SLOAD-table format precedent)
    - feedback_rng_backward_trace.md
    - feedback_rng_window_storage_read_freshness.md
    - feedback_rng_commitment_window.md (player-controllable state mutability between VRF request and fulfillment — load-bearing for Participating? classification)
    - feedback_verify_call_graph_against_source.md
  </read_first>
  <action>
    Dispatch a single general-purpose sub-agent. The sub-agent's job:

    (1) Read `contracts/modules/DegenerusGameJackpotModule.sol` and locate `payDailyJackpotCoinAndTickets` at line 596. Walk transitively into every internal/external function reached from that entry, walking across module/contract boundaries into anything under `contracts/` per D-298-TRACE-DEPTH-01. Includes the `_jackpotTicketRoll` cluster + Phase 278 ENT-05 `EntropyLib.hash2` keccak self-mix chain reached from this consumer. Stop only at external interfaces with no source available.

    (2) §A traced function set with file:line per entry.

    (3) §B SLOAD table — every SLOAD reached, `Slot | Read-site (file:line) | Read context | Participating? (YES/NO) | Attestation if NO`. Enumerate ALL SLOADs per `feedback_rng_window_storage_read_freshness.md` (F-41-02/03 precedent).

    (4) §C per-participating-slot writer enumeration. Include OZ-inherited writers (transfer / transferFrom / approve / _mint / _burn from any of the relevant ERC20/ERC721 contracts reached: BurnieCoin, GNRUS, DegenerusGame NFT) wherever applicable; admin/owner; affiliate; anything reachable from a non-internal entry point.

    (5) §D verdict matrix — per-(slot × writer × callsite). Classifications strict per D-298-EXEMPT-REACH-01 + D-298-EXEMPT-CROSSCONTRACT-01. NO SAFE_BY_DESIGN.

    (6) §E per-VIOLATION tactic from menu (a|b|c|d) + ≤80-char rationale per D-298-RECOMMEND-DEPTH-01.

    Write to `.planning/phases/298-vrf-read-graph-catalog-catalog/298-02-CATALOG-section.md`. Headings: `# §2 — JackpotModule.payDailyJackpotCoinAndTickets (file:line 596)` + `## CAT-01 (§A)` + `## CAT-02 (§B)` + `## CAT-03 (§C)` + `## CAT-04 (§D)` + `## CAT-06 (§E)`.

    Sub-agent prompt template per D-298-SUB-AGENT-PROMPT-01 — adapt anchors for §2 consumer entry. Same explicit-enumeration discipline, same 6 output sections.
  </action>
  <verify>
    <automated>test -f .planning/phases/298-vrf-read-graph-catalog-catalog/298-02-CATALOG-section.md && grep -q "## CAT-01" .planning/phases/298-vrf-read-graph-catalog-catalog/298-02-CATALOG-section.md && grep -q "## CAT-02" .planning/phases/298-vrf-read-graph-catalog-catalog/298-02-CATALOG-section.md && grep -q "## CAT-03" .planning/phases/298-vrf-read-graph-catalog-catalog/298-02-CATALOG-section.md && grep -q "## CAT-04" .planning/phases/298-vrf-read-graph-catalog-catalog/298-02-CATALOG-section.md && grep -q "## CAT-06" .planning/phases/298-vrf-read-graph-catalog-catalog/298-02-CATALOG-section.md && ! grep -q "SAFE_BY_DESIGN" .planning/phases/298-vrf-read-graph-catalog-catalog/298-02-CATALOG-section.md && git diff --name-only HEAD | grep -E '^(contracts|test)/' | wc -l | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `298-02-CATALOG-section.md` exists
    - All 5 CAT sub-headings present
    - §B `Participating?` column populated on every row; NO rows carry attestation
    - §C covers every YES slot
    - Every §D row classified ∈ {EXEMPT-ADVANCEGAME, EXEMPT-VRFCALLBACK, EXEMPT-RETRYLOOTBOXRNG, VIOLATION} (no SAFE_BY_DESIGN, no blank)
    - Every VIOLATION has §E tactic + rationale
    - No 'by construction' / 'covered by single fn' shortcuts
    - Zero `contracts/` + zero `test/` files modified
  </acceptance_criteria>
  <done>§2 section file complete with required structure; zero source-tree mutations.</done>
</task>

</tasks>

<verification>
File exists; 5 CAT sub-headings present; every verdict row classified; no SAFE_BY_DESIGN; zero source-tree mutations.
</verification>

<success_criteria>
1 per-consumer §2 catalog section file authored + ready for Wave-2 aggregation.
</success_criteria>

<output>
Create `.planning/phases/298-vrf-read-graph-catalog-catalog/298-02-SUMMARY.md` when done.
</output>
