---
phase: 298-vrf-read-graph-catalog-catalog
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/298-vrf-read-graph-catalog-catalog/298-09-CATALOG-section.md
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-04, CAT-06]
must_haves:
  truths:
    - "Consumer §9 (AdvanceModule.retryLootboxRng at AdvanceModule.sol:1132) has a CAT-01 backward-trace entry"
    - "Every reachable SLOAD enumerated"
    - "Every participating slot has writer enumeration"
    - "Every tuple classified — §9 callsites classified EXEMPT-RETRYLOOTBOXRNG when reached from retryLootboxRng stack"
    - "Every VIOLATION (callsites NOT reached from any of 3 exempt stacks) has tactic + rationale"
  artifacts:
    - path: ".planning/phases/298-vrf-read-graph-catalog-catalog/298-09-CATALOG-section.md"
      provides: "§9 catalog content for retryLootboxRng (failsafe path)"
      contains: "## CAT-01"
  key_links:
    - from: "§B SLOAD"
      to: "§C writer enumeration"
      via: "Participating? = YES forward"
      pattern: "Participating\\?.*YES"
---

<objective>
Backward-trace VRF-derived entropy from `AdvanceModule.retryLootboxRng` (`contracts/modules/DegenerusGameAdvanceModule.sol:1132`) per D-298-CONSUMER-LIST-01 entry §9. Produce the §9 catalog section.

Purpose: CAT-01..06 coverage for the retryLootboxRng failsafe consumer. Per D-42N-RETRY-RNG-DOMAIN-SEP-01 Option A, this consumer's callsites reached from the retryLootboxRng stack are classified `EXEMPT-RETRYLOOTBOXRNG`. The trace must verify:
1. No participating slot is mutated by retryLootboxRng outside the locked failsafe envelope (≥6h cooldown + ≤1 VRF-replacement per stall event + no pre-lock-state manipulation per Option A).
2. Cross-callsite per D-298-EXEMPT-REACH-01: any callsite of a writer of a participating slot reached from retryLootboxRng = `EXEMPT-RETRYLOOTBOXRNG`; the SAME writer reached from a non-EXEMPT stack at a separate callsite = `VIOLATION`.

Output: `298-09-CATALOG-section.md` with §A + §B + §C + §D + §E.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/298-vrf-read-graph-catalog-catalog/298-CONTEXT.md
@contracts/modules/DegenerusGameAdvanceModule.sol
@.planning/milestones/v42.0-phases/296-cross-surface-adversarial-sweep-sweep/296-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 9.1: Sub-agent backward-trace from retryLootboxRng (failsafe path)</name>
  <files>.planning/phases/298-vrf-read-graph-catalog-catalog/298-09-CATALOG-section.md</files>
  <read_first>
    - .planning/phases/298-vrf-read-graph-catalog-catalog/298-CONTEXT.md (D-298-CONSUMER-LIST-01 §9 + D-42N-RETRY-RNG-DOMAIN-SEP-01 Option A reference; all D-298-* decisions)
    - contracts/modules/DegenerusGameAdvanceModule.sol (consumer entry at :1132)
    - .planning/milestones/v42.0-phases/296-cross-surface-adversarial-sweep-sweep/296-CONTEXT.md (RETRY_LOOTBOX_RNG 4th audit-subject surface context; Option A scope; ≥6h cooldown + ≤1 replacement + no pre-lock-state manipulation)
    - feedback_rng_backward_trace.md
    - feedback_rng_window_storage_read_freshness.md
    - feedback_rng_commitment_window.md (player-controllable state mutability between VRF request and fulfillment — load-bearing for Participating? classification)
    - feedback_verify_call_graph_against_source.md
  </read_first>
  <action>
    Dispatch one general-purpose sub-agent rooted at `AdvanceModule.retryLootboxRng` at `:1132`. Sub-agent walks transitively into every reachable function across `contracts/` per D-298-TRACE-DEPTH-01.

    Per D-42N-RETRY-RNG-DOMAIN-SEP-01 Option A locked scope: the retryLootboxRng failsafe must NOT manipulate any pre-lock state; it only replaces the VRF word when stalled. The trace MUST surface:
    - Every participating-slot SLOAD reached inside the retryLootboxRng resolution
    - Every participating-slot SSTORE reached — these are the slots that the trace must verify are NOT mutated outside the locked failsafe envelope
    - Cooldown-state SLOADs (`retryLastTimestamp` or equivalent) — typically participating because they gate the replacement
    - Cross-call SLOADs (`decClaimRounds[lvl].rngWord` re-read pattern from §13; any analogous cross-call re-reads)

    Per D-298-EXEMPT-REACH-01 + D-298-EXEMPT-CROSSCONTRACT-01: every (slot × writer × callsite) tuple's classification depends on which EXEMPT stack reaches it. Callsites reached from `retryLootboxRng` get `EXEMPT-RETRYLOOTBOXRNG`. The same writer reached from a non-EXEMPT entry point at a separate callsite gets `VIOLATION`. The verdict matrix is per-callsite, NOT per-(slot × writer).

    Write to `.planning/phases/298-vrf-read-graph-catalog-catalog/298-09-CATALOG-section.md`. Heading `# §9 — AdvanceModule.retryLootboxRng (file:line 1132)` + 5 CAT sub-headings.

    Explicit-enumeration + ALL-SLOADs + NO SAFE_BY_DESIGN.
  </action>
  <verify>
    <automated>test -f .planning/phases/298-vrf-read-graph-catalog-catalog/298-09-CATALOG-section.md && grep -q "## CAT-01" .planning/phases/298-vrf-read-graph-catalog-catalog/298-09-CATALOG-section.md && grep -q "## CAT-02" .planning/phases/298-vrf-read-graph-catalog-catalog/298-09-CATALOG-section.md && grep -q "## CAT-03" .planning/phases/298-vrf-read-graph-catalog-catalog/298-09-CATALOG-section.md && grep -q "## CAT-04" .planning/phases/298-vrf-read-graph-catalog-catalog/298-09-CATALOG-section.md && grep -q "## CAT-06" .planning/phases/298-vrf-read-graph-catalog-catalog/298-09-CATALOG-section.md && ! grep -q "SAFE_BY_DESIGN" .planning/phases/298-vrf-read-graph-catalog-catalog/298-09-CATALOG-section.md && git diff --name-only HEAD | grep -E '^(contracts|test)/' | wc -l | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `298-09-CATALOG-section.md` exists with all 5 CAT sub-headings
    - §B Participating? column populated; NO rows attested
    - §C covers every YES slot
    - Every §D row classified ∈ {EXEMPT-ADVANCEGAME, EXEMPT-VRFCALLBACK, EXEMPT-RETRYLOOTBOXRNG, VIOLATION} (no SAFE_BY_DESIGN)
    - At least one §D row classified `EXEMPT-RETRYLOOTBOXRNG` (the failsafe stack itself touches slots)
    - Every VIOLATION has §E tactic + rationale
    - Zero `contracts/` + zero `test/` modifications
  </acceptance_criteria>
  <done>§9 section file complete; zero source-tree mutations; EXEMPT-RETRYLOOTBOXRNG classifications applied.</done>
</task>

</tasks>

<verification>
File exists; 5 CAT sub-headings; every verdict row classified; no SAFE_BY_DESIGN; ≥1 EXEMPT-RETRYLOOTBOXRNG row; zero source-tree mutations.
</verification>

<success_criteria>
1 per-consumer §9 catalog section authored with retryLootboxRng failsafe-path coverage.
</success_criteria>

<output>
Create `.planning/phases/298-vrf-read-graph-catalog-catalog/298-09-SUMMARY.md` when done.
</output>
