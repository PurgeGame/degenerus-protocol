---
phase: 298-vrf-read-graph-catalog-catalog
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/298-vrf-read-graph-catalog-catalog/298-03-CATALOG-section.md
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-04, CAT-06]
must_haves:
  truths:
    - "Consumer §3 (JackpotModule.runTerminalJackpot at JackpotModule.sol:278) has a CAT-01 backward-trace entry"
    - "Every reachable SLOAD enumerated with file:line"
    - "Every participating slot has writer enumeration"
    - "Every (slot × writer × callsite) classified ∈ {EXEMPT-ADVANCEGAME, EXEMPT-VRFCALLBACK, EXEMPT-RETRYLOOTBOXRNG, VIOLATION}"
    - "Every VIOLATION has remediation tactic + 1-line rationale"
  artifacts:
    - path: ".planning/phases/298-vrf-read-graph-catalog-catalog/298-03-CATALOG-section.md"
      provides: "§3 catalog content (CAT-01+CAT-02+CAT-03+CAT-04+CAT-06 for runTerminalJackpot)"
      contains: "## CAT-01"
  key_links:
    - from: "§B SLOAD table"
      to: "§C writer enumeration"
      via: "Participating? = YES slots forward"
      pattern: "Participating\\?.*YES"
---

<objective>
Backward-trace VRF-derived entropy from `JackpotModule.runTerminalJackpot` (`contracts/modules/DegenerusGameJackpotModule.sol:278`) per D-298-CONSUMER-LIST-01 entry §3. Produce the §3 catalog section.

Purpose: Phase 298 CAT-01..06 coverage for VRF-consumer entry §3 (game-over ETH terminal jackpot). IN SCOPE despite EXC-01..03 KI-envelope NEGATIVE-scope carry per D-298-CONSUMER-LIST-01 prose — the freeze invariant applies regardless of the prior milestone's catastrophy-class disposition.

Output: `298-03-CATALOG-section.md` with §A + §B + §C + §D + §E.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/298-vrf-read-graph-catalog-catalog/298-CONTEXT.md
@contracts/modules/DegenerusGameJackpotModule.sol
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Sub-agent backward-trace from runTerminalJackpot</name>
  <files>.planning/phases/298-vrf-read-graph-catalog-catalog/298-03-CATALOG-section.md</files>
  <read_first>
    - .planning/phases/298-vrf-read-graph-catalog-catalog/298-CONTEXT.md (D-298-CONSUMER-LIST-01 §3 prose; D-298-TRACE-DEPTH-01; D-298-SLOT-CLASSIFICATION-01; D-298-EXEMPT-REACH-01; D-298-EXEMPT-CROSSCONTRACT-01; D-298-RECOMMEND-DEPTH-01)
    - contracts/modules/DegenerusGameJackpotModule.sol (consumer entry at :278)
    - .planning/milestones/v41.0-phases/287-jackpot-influence-surface-closure-jpsurf/287-01-JPSURF-AUDIT.md (SLOAD-table format precedent)
    - feedback_rng_backward_trace.md
    - feedback_rng_window_storage_read_freshness.md
    - feedback_rng_commitment_window.md (player-controllable state mutability between VRF request and fulfillment — load-bearing for Participating? classification)
    - feedback_verify_call_graph_against_source.md
  </read_first>
  <action>
    Dispatch one general-purpose sub-agent. Job:

    (1) Read `contracts/modules/DegenerusGameJackpotModule.sol` and locate `runTerminalJackpot` at line 278. Walk transitively into every internal/external function reached from that entry across module/contract boundaries per D-298-TRACE-DEPTH-01. Stop only at no-source external interfaces.

    (2) §A traced function set with file:line.

    (3) §B SLOAD table — every SLOAD reached, full columns per D-298-SLOT-CLASSIFICATION-01 two-tier (Participating? + Attestation if NO). Enumerate ALL SLOADs per F-41-02/03 precedent.

    (4) §C per-participating-slot writer enumeration with OZ-inherited writers + admin/owner + affiliate + every non-internal-entry-reachable writer.

    (5) §D verdict matrix per (slot × writer × callsite). Strict EXEMPT-stack-rooted classification per D-298-EXEMPT-REACH-01 + cross-contract propagation per D-298-EXEMPT-CROSSCONTRACT-01. NO SAFE_BY_DESIGN.

    (6) §E per-VIOLATION tactic (a|b|c|d) + ≤80-char rationale.

    Write to `.planning/phases/298-vrf-read-graph-catalog-catalog/298-03-CATALOG-section.md`. Heading `# §3 — JackpotModule.runTerminalJackpot (file:line 278)` + `## CAT-01 (§A)` + `## CAT-02 (§B)` + `## CAT-03 (§C)` + `## CAT-04 (§D)` + `## CAT-06 (§E)`.
  </action>
  <verify>
    <automated>test -f .planning/phases/298-vrf-read-graph-catalog-catalog/298-03-CATALOG-section.md && grep -q "## CAT-01" .planning/phases/298-vrf-read-graph-catalog-catalog/298-03-CATALOG-section.md && grep -q "## CAT-02" .planning/phases/298-vrf-read-graph-catalog-catalog/298-03-CATALOG-section.md && grep -q "## CAT-03" .planning/phases/298-vrf-read-graph-catalog-catalog/298-03-CATALOG-section.md && grep -q "## CAT-04" .planning/phases/298-vrf-read-graph-catalog-catalog/298-03-CATALOG-section.md && grep -q "## CAT-06" .planning/phases/298-vrf-read-graph-catalog-catalog/298-03-CATALOG-section.md && ! grep -q "SAFE_BY_DESIGN" .planning/phases/298-vrf-read-graph-catalog-catalog/298-03-CATALOG-section.md && git diff --name-only HEAD | grep -E '^(contracts|test)/' | wc -l | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `298-03-CATALOG-section.md` exists
    - All 5 CAT sub-headings present
    - §B `Participating?` column populated; NO rows carry attestation
    - §C covers every YES slot
    - Every §D row classified (no SAFE_BY_DESIGN, no blank)
    - Every VIOLATION has §E tactic + rationale
    - Zero `contracts/` + zero `test/` modifications
  </acceptance_criteria>
  <done>§3 section file complete; zero source-tree mutations.</done>
</task>

</tasks>

<verification>
File exists; 5 CAT sub-headings; every verdict row classified; no SAFE_BY_DESIGN; zero source-tree mutations.
</verification>

<success_criteria>
1 per-consumer §3 catalog section authored.
</success_criteria>

<output>
Create `.planning/phases/298-vrf-read-graph-catalog-catalog/298-03-SUMMARY.md` when done.
</output>
