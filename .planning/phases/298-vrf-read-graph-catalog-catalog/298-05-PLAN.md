---
phase: 298-vrf-read-graph-catalog-catalog
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/298-vrf-read-graph-catalog-catalog/298-05-CATALOG-section.md
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-04, CAT-06]
must_haves:
  truths:
    - "Consumer §5 (GameOverModule rngWordByDay substitution at GameOverModule.sol:100) has a CAT-01 backward-trace entry"
    - "Every reachable SLOAD enumerated"
    - "Every participating slot has writer enumeration"
    - "Every tuple classified"
    - "Every VIOLATION has tactic + rationale"
  artifacts:
    - path: ".planning/phases/298-vrf-read-graph-catalog-catalog/298-05-CATALOG-section.md"
      provides: "§5 catalog content for GameOverModule rngWordByDay substitution"
      contains: "## CAT-01"
  key_links:
    - from: "§B SLOAD"
      to: "§C writer enumeration"
      via: "Participating? = YES forward"
      pattern: "Participating\\?.*YES"
---

<objective>
Backward-trace VRF-derived entropy from the `GameOverModule` rngWordByDay substitution path (`contracts/modules/DegenerusGameGameOverModule.sol:100`) per D-298-CONSUMER-LIST-01 entry §5. Produce the §5 catalog section.

Purpose: CAT-01..06 coverage for game-over RNG-substitution consumer. IN SCOPE despite EXC-01..03 KI-envelope NEGATIVE-scope carry — the freeze invariant applies regardless of prior milestone's catastrophy-class disposition.

Output: `298-05-CATALOG-section.md` with §A + §B + §C + §D + §E.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/298-vrf-read-graph-catalog-catalog/298-CONTEXT.md
@contracts/modules/DegenerusGameGameOverModule.sol
</context>

<tasks>

<task type="auto">
  <name>Task 5.1: Sub-agent backward-trace from GameOverModule rngWordByDay substitution</name>
  <files>.planning/phases/298-vrf-read-graph-catalog-catalog/298-05-CATALOG-section.md</files>
  <read_first>
    - .planning/phases/298-vrf-read-graph-catalog-catalog/298-CONTEXT.md (D-298-CONSUMER-LIST-01 §5; D-298-TRACE-DEPTH-01; D-298-SLOT-CLASSIFICATION-01; D-298-EXEMPT-REACH-01; D-298-EXEMPT-CROSSCONTRACT-01; D-298-RECOMMEND-DEPTH-01)
    - contracts/modules/DegenerusGameGameOverModule.sol (consumer entry at :98-:100)
    - feedback_rng_backward_trace.md
    - feedback_rng_window_storage_read_freshness.md
    - feedback_rng_commitment_window.md (player-controllable state mutability between VRF request and fulfillment — load-bearing for Participating? classification)
    - feedback_verify_call_graph_against_source.md
  </read_first>
  <action>
    Dispatch one general-purpose sub-agent rooted at the GameOverModule rngWordByDay substitution path at `:100`. Sub-agent walks transitively into every reachable function across `contracts/` per D-298-TRACE-DEPTH-01; produces §A + §B + §C + §D + §E.

    Special note: `rngWordByDay[day]` SLOAD at `:100` is itself a substituted RNG consumer (not the direct VRF callback). The trace must capture both the upstream write of `rngWordByDay[day]` (typically advanceGame stack) and every downstream slot read alongside the substituted word.

    Write to `.planning/phases/298-vrf-read-graph-catalog-catalog/298-05-CATALOG-section.md`. Heading `# §5 — GameOverModule rngWordByDay substitution (file:line 100)` + `## CAT-01 (§A)` + `## CAT-02 (§B)` + `## CAT-03 (§C)` + `## CAT-04 (§D)` + `## CAT-06 (§E)`.

    Explicit-enumeration discipline per `feedback_verify_call_graph_against_source.md`; ALL-SLOADs discipline per `feedback_rng_window_storage_read_freshness.md`; NO SAFE_BY_DESIGN classifications.
  </action>
  <verify>
    <automated>test -f .planning/phases/298-vrf-read-graph-catalog-catalog/298-05-CATALOG-section.md && grep -q "## CAT-01" .planning/phases/298-vrf-read-graph-catalog-catalog/298-05-CATALOG-section.md && grep -q "## CAT-02" .planning/phases/298-vrf-read-graph-catalog-catalog/298-05-CATALOG-section.md && grep -q "## CAT-03" .planning/phases/298-vrf-read-graph-catalog-catalog/298-05-CATALOG-section.md && grep -q "## CAT-04" .planning/phases/298-vrf-read-graph-catalog-catalog/298-05-CATALOG-section.md && grep -q "## CAT-06" .planning/phases/298-vrf-read-graph-catalog-catalog/298-05-CATALOG-section.md && ! grep -q "SAFE_BY_DESIGN" .planning/phases/298-vrf-read-graph-catalog-catalog/298-05-CATALOG-section.md && git diff --name-only HEAD | grep -E '^(contracts|test)/' | wc -l | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `298-05-CATALOG-section.md` exists
    - All 5 CAT sub-headings present
    - §B Participating? column populated; NO rows attested
    - §C covers every YES slot
    - Every §D row classified (no SAFE_BY_DESIGN)
    - Every VIOLATION has §E tactic + rationale
    - Zero `contracts/` + zero `test/` modifications
  </acceptance_criteria>
  <done>§5 section file complete; zero source-tree mutations.</done>
</task>

</tasks>

<verification>
File exists; 5 CAT sub-headings; every verdict row classified; no SAFE_BY_DESIGN; zero source-tree mutations.
</verification>

<success_criteria>
1 per-consumer §5 catalog section authored.
</success_criteria>

<output>
Create `.planning/phases/298-vrf-read-graph-catalog-catalog/298-05-SUMMARY.md` when done.
</output>
