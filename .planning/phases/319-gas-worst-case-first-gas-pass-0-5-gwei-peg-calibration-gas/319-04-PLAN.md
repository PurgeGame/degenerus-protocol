---
phase: 319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas
plan: 04
type: execute
wave: 2
depends_on: ["319-01"]
files_modified:
  - test/gas/CrankLeversAndPacking.t.sol
  - .planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-GAS-05-GUARDRAILS.md
autonomous: true
requirements: [GAS-02, GAS-03, GAS-04, GAS-05]
must_haves:
  truths:
    - "The batched-reward levers hold: one creditFlip/cranker/tx, one batch value transfer, level/mintPrice read once per batch (GAS-02)"
    - "Calldata is grouped by player and the per-work-type fns are homogeneous (GAS-03)"
    - "Sub packs to one slot and the placement hot path gains zero new per-bet/box storage (GAS-04)"
    - "Every G1-G13 security-floor guard survives the Scavenger+Skeptic+contract-auditor pass; any removal/packing candidate that touches a guard is rejected (GAS-05)"
  artifacts:
    - path: "test/gas/CrankLeversAndPacking.t.sol"
      provides: "GAS-02/03/04 assertion suite (read-once, one creditFlip, homogeneity, Sub 1-slot via forge inspect, no new hot-path storage) + G1-G13 grep-presence assertions"
      contains: "creditFlip"
    - path: ".planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-GAS-05-GUARDRAILS.md"
      provides: "The GAS-05 audit deliverable: G1-G13 reject-set + every Scavenger candidate's Skeptic/contract-auditor disposition vs the security floor"
      contains: "RngNotReady"
  key_links:
    - from: "319-GAS-05-GUARDRAILS.md"
      to: "the optional GAS-02 priceForLevel/_ethToBurnieValue hoist candidate"
      via: "Skeptic disposition (ship-if-real-saving / no-op-if-already-hoisted) feeding Plan 05's contract-diff decision"
      pattern: "priceForLevel|_ethToBurnieValue"
    - from: "test/gas/CrankLeversAndPacking.t.sol"
      to: "the G1-G13 guards in DegenerusGame.sol + AfKing.sol + the modules"
      via: "grep-presence assertions (comment-stripped) that each guard is byte-present"
      pattern: "RngNotReady|BatchAlreadyTaken|onlySelf|burnForKeeper"
---

<objective>
Verify the GAS-02/03/04 batched-reward + packing levers hold via assertions, and run the GAS-05 Scavenger → Skeptic → contract-auditor security-floor pass producing the G1-G13 guardrail audit deliverable. Surface (but do NOT ship) the optional GAS-02 loop-invariant hoist candidate with a Skeptic disposition — Plan 05 decides whether it ships in the approved diff.

Purpose: GAS-02/03/04 are verification requirements (the levers already HOLD per RESEARCH — this plan proves it with checkable assertions). GAS-05 is the security floor (`feedback_security_over_gas`, HARD): every removal/packing candidate must survive the Skeptic + contract-auditor with the G1-G13 guards as the hard-reject set. This plan produces the audit artifact and the assertion suite; it makes NO contract edit (Plan 05 owns the gate).

Output: one assertion suite `test/gas/CrankLeversAndPacking.t.sol` + the `319-GAS-05-GUARDRAILS.md` audit deliverable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-RESEARCH.md
@.planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-PATTERNS.md

<interfaces>
<!-- GAS-02/03/04 verification targets + G1-G13 guard locations, all source-verified at HEAD 0d9d321f. -->

GAS-02 read-once / one-creditFlip / one-transfer:
  crankBets one creditFlip after loop      DegenerusGame.sol:1578
  crankBoxes one creditFlip after loop     DegenerusGame.sol:1632
  crankBets level read once (lvl)          DegenerusGame.sol:1556
  crankBoxes level read once               DegenerusGame.sol:1610
  AfKing one creditFlip / one batchPurchase value transfer (sweep epilogue)  AfKing.sol:522+
  AfKing mintPrice read once               AfKing.sol:527
  batchPurchase one unspent-value refund   DegenerusGame.sol:1717-1721
  GAS-02 HOIST CANDIDATE (optional): per-item reward _ethToBurnieValue(CONST*CONST, priceForLevel(lvl))
    recomputed per successful item in crankBets :1567-1570 / crankBoxes :1621-1623 — all inputs loop-invariant.
    viaIR runs=200 MAY already hoist (pure calls of a constant). Measure before/after. If it ships -> Plan 05 diff.

GAS-03 grouping / homogeneity:
  crankBets(address[] players, uint64[] betIds)  DegenerusGame.sol:1543  (item i = players[i], betIds[i])
  batchPurchase(players[], amounts[], modes[])    DegenerusGame.sol:1687  (parallel arrays)
  homogeneous fns: crankBets (bets only), crankBoxes (boxes only, parameterless cursor), batchPurchase (purchase only)

GAS-04 packing / no-new-hot-path-storage:
  Sub struct ONE slot (13 bytes used, 19 free)   AfKing.sol:80-88
  boxCursor / boxCursorIndex uint48               DegenerusGame.sol:1507 / :1510
  enqueueBoxForCrank: 1 SSTORE per (index,player) at first deposit (lootboxEthBase==0 signal), NOT per box-open, NOT on bet-placement path  DegenerusGame.sol:1526
  storage-layout verification: forge inspect DegenerusGame storage-layout ; forge inspect AfKing storage-layout

G1-G13 security-floor reject set (RESEARCH §GAS-05, every guard mapped + its proving 318 test):
  G1 RngNotReady resolve (bet)  DegeneretteModule:578 ; placement mirror :452          (318-05)
  G2 RngNotReady open-box       crankBoxes:1603 + LootboxModule open guard               (318-05)
  G3 bet delete one-reward      DegeneretteModule:580                                    (318-02)
  G4 box zeroing one-reward     LootboxModule:530 + crankBoxes:1618                       (318-02)
  G5 double-crank short-circuit BatchAlreadyTaken crankBets:1552                          (318-02)
  G6 batchPurchase try/catch+refund  DegenerusGame.sol:1704-1711 + refund :1717-1721      (318-03)
  G7 crank per-item try/catch + onlySelf  crankBets:1562 / crankBoxes:1620 ; onlySelf :1641 / :1662  (318-03)
  G8 burnForKeeper all-or-nothing  AfKing.sol:587-600                                     (318-03)
  G9 keeper/address gating  batchPurchase:1692 (AF_KING) / onlySelf guards / isOperatorApproved AfKing:610  (318-03 + OPEN-C)
  G10 swap-pop / no-++i cursor integrity  AfKing.sol:594-599                              (318-04)
  G11 bounded tombstone / cursor self-partition  AfKing.sol:532 + lastSweptDay :567       (318-04)
  G12 WWXRP zero reward  crankBets:1564 (currency==3)                                     (318-02)
  G13 rngLocked/gameOver batch pre-check  batchPurchase:1693-1694 ; AfKing sweep :523     (318-03)

WARNING (RESEARCH §Standard Stack): gas-scavenger SKILL.md says "runs=2" — production is optimizer_runs=200 (foundry) / 50 (hardhat). Reason from RUNTIME SLOAD/SSTORE weight, NOT bytecode size. Flag this to the skill when invoking.

GAS-04 note (RESEARCH): Sub has 19 free padding bytes at minimum field widths — NO tighter packing available; the Scavenger MUST NOT propose widening. feedback_maximal_variable_packing already satisfied.

Comment-stripping grep precedent: JackpotSingleCallCorrectness.t.sol testSplitSymbolsGrepClean (vm.readFile + Solidity comment stripper so NatSpec prose can't self-invalidate the gate). Use grep -v '^#'-style filtering for any -c gate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GAS-02/03/04 lever + packing assertion suite</name>
  <read_first>
    - test/fuzz/CrankFaucetResistance.t.sol (the existing creditFlip-count helper `_countCoinflipStakeUpdated()` and the crank fixture — reuse for the one-creditFlip assertion)
    - test/fuzz/JackpotSingleCallCorrectness.t.sol:79-110 (the comment-stripping vm.readFile grep idiom for source-presence assertions)
    - contracts/DegenerusGame.sol:1543-1640,1687-1735 (crankBets/crankBoxes read-once + one-creditFlip, batchPurchase parallel arrays + one refund, boxCursor/boxCursorIndex uint48, enqueueBoxForCrank)
    - contracts/AfKing.sol:80-88 (the Sub struct — confirm 1 slot)
    - .planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-RESEARCH.md (§GAS-02/03/04 Verification Targets — every assertion + its file:line)
  </read_first>
  <action>
    Create `test/gas/CrankLeversAndPacking.t.sol`. Assert the GAS-02/03/04 levers HOLD (RESEARCH confirms they do — this proves it with checkable assertions):
    - GAS-02 one creditFlip per batch: drive a multi-item `crankBets` (reuse the `_countCoinflipStakeUpdated()` helper from `CrankFaucetResistance`) and assert exactly ONE creditFlip event for N>1 successful items (mirrors CrankFaucetResistance's REW-02 test at :414+). Same for `crankBoxes`.
    - GAS-02 read-once / one-transfer: source-presence assert (comment-stripped vm.readFile grep, idiom JackpotSingleCallCorrectness:79-110) that `crankBets`/`crankBoxes` read `_activeTicketLevel()` once before the loop and AfKing reads `mintPrice()` once; that `batchPurchase` does ONE value refund.
    - GAS-03 grouping/homogeneity: source-presence assert the parallel-array signatures (`crankBets(address[],uint64[])`, `batchPurchase(address[],uint256[],uint8[])`) and that the three crank/purchase fns are homogeneous per-work-type (no mixed-work dispatcher).
    - GAS-04 Sub 1-slot: run `forge inspect AfKing storage-layout` (via vm.ffi if enabled, OR a source-presence assert that the `Sub` struct fields sum to <= 32 bytes per the :80-88 widths) and assert `Sub` is one slot. Assert `boxCursor`/`boxCursorIndex` are `uint48`.
    - GAS-04 no-new-hot-path-storage: source-presence assert that `enqueueBoxForCrank` is the only crank-added storage write and it fires from the first-deposit signal (one SSTORE per (index,player)), NOT on the bet-placement path.
    Prefer real behavioral assertions (creditFlip count) where possible; use comment-stripped source-presence greps for the structural claims (read-once, homogeneity, packing) — strip Solidity comments so NatSpec can't self-invalidate (precedent JackpotSingleCallCorrectness testSplitSymbolsGrepClean). Also add G1-G13 grep-presence assertions (each guard byte-present at its RESEARCH file:line, comment-stripped) so a future regression that deletes a guard flips this suite RED — this is the test-side companion to the Plan-04 GAS-05 audit doc. Zero `contracts/*.sol` mutation; restore ContractAddresses.sol via `git checkout` if the build patched it.
  </action>
  <verify>
    <automated>forge test --match-contract CrankLeversAndPacking -vv 2>&1 | grep -qE "[0-9]+ passed" && git diff --name-only -- contracts/ | grep -q . && echo "CONTRACT DIRTY FAIL" || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-contract CrankLeversAndPacking -vv` passes.
    - GAS-02 one-creditFlip is proven behaviorally (exactly one creditFlip for N>1 items, both cranks).
    - GAS-03 grouping/homogeneity + GAS-04 Sub-1-slot + boxCursor uint48 + no-new-hot-path-storage are asserted (behavioral or comment-stripped source-presence).
    - G1-G13 grep-presence assertions are present and green (comment-stripped, no `== 0` gate on an unfiltered file).
    - `git diff --name-only -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>GAS-02/03/04 levers proven HELD via assertions; the G1-G13 guards are pinned by grep-presence so a future regression flips the suite RED; contracts/ clean.</done>
</task>

<task type="auto">
  <name>Task 2: GAS-05 Scavenger → Skeptic → contract-auditor security-floor pass</name>
  <read_first>
    - .planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-RESEARCH.md (§GAS-05 Security-Floor Guardrail Inventory — the full G1-G13 table; the §Standard Stack WARNING about runs=2 vs runs=200; the §GAS-02 optimization candidate)
    - .planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-PATTERNS.md (§Shared Patterns → "GAS-05 security-floor guardrail set" — the Scavenger→Skeptic→contract-auditor process + the runs=200 reasoning correction)
    - .claude/skills/gas-scavenger/SKILL.md, .claude/skills/gas-skeptic/SKILL.md, .claude/skills/gas-audit/SKILL.md, .claude/skills/contract-auditor/SKILL.md (the skill roles)
    - contracts/DegenerusGame.sol:1490-1735 (the crank region — the Scavenger's primary target)
    - contracts/AfKing.sol:516-651 (the sweep region — swap-pop / cursor / burnForKeeper)
    - contracts/modules/DegenerusGameDegeneretteModule.sol:553-801 + contracts/modules/DegenerusGameLootboxModule.sol:477-655,917-1031 (the touched module resolve/open paths)
  </read_first>
  <action>
    Run the GAS-05 security-floor pass and produce `.planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-GAS-05-GUARDRAILS.md`. Process (gas-audit coordinator chain):
    1. Run gas-scavenger over `DegenerusGame.sol:1490-1735` (crank region), `AfKing.sol:516-651` (sweep region), and the touched module resolve/open paths. CRITICAL: tell the skill the optimizer is `runs=200` (foundry) / `runs=50` (hardhat), NOT the SKILL.md's stale `runs=2` — reason from RUNTIME SLOAD/SSTORE weight, NOT bytecode size (RESEARCH §Standard Stack WARNING). A removal justified purely by "saves deployment bytecode" carries less weight at runs=200.
    2. Feed EVERY Scavenger candidate to gas-skeptic with the G1-G13 list (RESEARCH §GAS-05) as the HARD-REJECT set. Any candidate that touches a G-row is escalated to contract-auditor for an invariant proof BEFORE it may be approved.
    3. Document each candidate with: the candidate, the Skeptic disposition (approve / reject / flag-for-human), the G-row it touches (if any), and the contract-auditor verdict (if escalated). The G1-G13 table is the deliverable's spine — restate it with each guard's `file:line`, why it's load-bearing, and its proving 318 test; mark each VERIFIED-PRESENT at HEAD.
    4. The GAS-02 loop-invariant hoist candidate (`priceForLevel(lvl)`/`_ethToBurnieValue` recomputed per item, `crankBets:1567-1570` / `crankBoxes:1621-1623`): the Skeptic should APPROVE it as behavior-identical (pure recomputation of loop-invariants) — but it MAY be a no-op if viaIR at runs=200 already hoists it. Record its disposition as: "approve IF Plan 02's before/after measurement shows a real saving; no-op if already hoisted by the optimizer; if it ships it is a `DegenerusGame.sol` edit → batches into Plan 05's USER-APPROVED diff." Do NOT make the edit here.
    5. GAS-04 packing: record that `Sub` (19 free bytes at minimum field widths) has NO tighter packing — the Scavenger MUST NOT propose widening; `feedback_maximal_variable_packing` is already satisfied. Confirm swap-pop/no-`++i` integrity (G10) + bounded tombstone overhead (G11) survive any candidate.
    Verdict: the doc must conclude with a clear statement of whether ANY removal/packing was approved (and if so, which, batched into Plan 05) or whether the pass is removal-clean (security floor intact, only the optional hoist pending a measurement decision). This is a planning/audit artifact — zero `contracts/*.sol` mutation. No fenced code in the action; the doc is prose + the G1-G13 table.
    Per `feedback_security_over_gas` (HARD floor): NO candidate that weakens a security/RNG invariant may be approved — when in doubt, reject and document.
  </action>
  <verify>
    <automated>test -f .planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-GAS-05-GUARDRAILS.md && grep -q "RngNotReady" .planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-GAS-05-GUARDRAILS.md && grep -qE "G1[0-3]|G[1-9]" .planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-GAS-05-GUARDRAILS.md && grep -qiE "runs=200|runs = 200|200" .planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-GAS-05-GUARDRAILS.md</automated>
  </verify>
  <acceptance_criteria>
    - `319-GAS-05-GUARDRAILS.md` exists with the full G1-G13 reject-set table (each guard: `file:line`, why load-bearing, proving 318 test, VERIFIED-PRESENT at HEAD).
    - Every Scavenger candidate has a Skeptic disposition; any touching a G-row is escalated to contract-auditor with a recorded verdict.
    - The runs=200 (not runs=2) reasoning correction is documented and applied.
    - The GAS-02 hoist candidate is recorded as approve-if-real-saving / no-op-if-hoisted, batched into Plan 05 if it ships — NOT edited here.
    - The doc concludes with an explicit removal-clean-or-approved-set verdict; no candidate weakening a G-row is approved.
    - Zero `contracts/*.sol` mutation.
  </acceptance_criteria>
  <done>The GAS-05 Scavenger→Skeptic→contract-auditor pass is complete; the G1-G13 security floor is proven intact; any approved candidate (or the optional hoist) is handed to Plan 05's gate; contracts/ clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scavenger candidate → security floor | The Scavenger is intentionally reckless; an approved candidate that touches a G1-G13 guard would open a faucet / RNG-manipulation / brick vector |
| stale SKILL.md runs=2 → wrong weighting | Reasoning from bytecode size (runs=2) instead of runtime SLOAD/SSTORE (runs=200) would approve a removal that is actually a runtime regression or a wrongly-justified guard cut |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-319-11 | Tampering (guard removal) | GAS-05 Scavenger candidates | mitigate | G1-G13 is the HARD-REJECT set; any candidate touching a G-row is escalated to contract-auditor for an invariant proof before approval; `feedback_security_over_gas` — when in doubt, reject. The Task-1 grep-presence suite pins the guards so a future regression flips RED |
| T-319-12 | Tampering (RNG-freeze weakening) | G1/G2 RngNotReady guards | mitigate | G1/G2 are in the reject set; the resolve guard (DegeneretteModule:578) + placement mirror (:452) + box gate (crankBoxes:1603) must stay byte-present (318-05 proved them); any candidate touching them → contract-auditor |
| T-319-13 | EoP (faucet via mis-weighting) | runs=2 vs runs=200 | mitigate | The doc documents + applies the runs=200 runtime-weight correction; bytecode-size-only justifications are down-weighted |
| T-319-14 | DoS (brick-guard removal) | G6/G7 try/catch | mitigate | G6/G7 (batchPurchase + crank per-item try/catch + onlySelf) are in the reject set; proven by 318-03 |
| T-319-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH §Package Legitimacy Audit = N/A); the skill agents are local read-only |
</threat_model>

<verification>
- `forge test --match-contract CrankLeversAndPacking -vv` green; GAS-02/03/04 levers + G1-G13 grep-presence asserted.
- `319-GAS-05-GUARDRAILS.md` exists with the full G1-G13 reject-set + every candidate's disposition + the runs=200 correction.
- No candidate weakening a security-floor guard is approved (`feedback_security_over_gas`).
- The optional GAS-02 hoist is surfaced with a Skeptic disposition for Plan 05 — NOT edited here.
- `git diff --name-only -- contracts/` empty; no NEW failures vs the 44-failure 318 baseline.
</verification>

<success_criteria>
- GAS-02/03/04 verified (levers hold); GAS-05 security-floor pass complete with the G1-G13 floor intact.
- Plan 05 receives the GAS-05 verdict + any approved candidate / the optional hoist disposition.
- Zero `contracts/*.sol` mutation.
</success_criteria>

<output>
Create `.planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-04-SUMMARY.md` when done.
</output>
