---
phase: 319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas
plan: 05
type: execute
wave: 3
depends_on: ["319-02", "319-03", "319-04"]
files_modified:
  - contracts/DegenerusGame.sol
  - test/fuzz/CrankFaucetResistance.t.sol
  - test/fuzz/helpers/DeployProtocol.sol
  - .gas-snapshot
autonomous: false
requirements: [GAS-06]
must_haves:
  truths:
    - "The placement hot path is +0% vs the Plan-01 baseline (the crank relaxes RESOLVE only, never placement)"
    - "The two CRANK_*_GAS_UNITS peg constants are EITHER validated as-is (no edit) OR recalibrated to the measured per-item marginal — never above it (the SAFE-01 round-trip ≤ 0 faucet floor holds)"
    - "If the contract constants change, the CrankFaucetResistance test mirror is updated in the SAME change so the peg-equality assertions stay green"
    - "Any contract edit is presented as ONE batched diff for explicit USER approval — nothing pre-approved"
  artifacts:
    - path: "contracts/DegenerusGame.sol"
      provides: "CONDITIONAL: the calibrated CRANK_RESOLVE_BET_GAS_UNITS / CRANK_OPEN_BOX_GAS_UNITS values (only if the measured marginal differs materially from 120_000) — behind the USER-APPROVED gate"
      contains: "CRANK_GAS_PRICE_REF = 0.5 gwei"
    - path: ".gas-snapshot"
      provides: "GAS-06 placement +0% verification (forge snapshot --check vs the Plan-01 baseline)"
  key_links:
    - from: "the Plan-02 per-1-spin-item resolve marginal + Plan-03 per-player sweep marginal"
      to: "the calibrated CRANK_RESOLVE_BET_GAS_UNITS + the BOUNTY_ETH_TARGET deploy-param"
      via: "the measured marginal is the calibration source; the calibrated value never exceeds it"
      pattern: "CRANK_RESOLVE_BET_GAS_UNITS|CRANK_OPEN_BOX_GAS_UNITS|BOUNTY_ETH_TARGET"
    - from: "contracts/DegenerusGame.sol constants"
      to: "test/fuzz/CrankFaucetResistance.t.sol:74-75 mirror"
      via: "the test mirror must match the contract constants or :177-182 peg-equality assertions break"
      pattern: "120_000|CRANK_RESOLVE_BET_GAS_UNITS"
---

<objective>
Close GAS-06: verify the placement hot path is +0%, then calibrate the reward-peg constants from the measured per-item marginals. The two `DegenerusGame.sol` `*_GAS_UNITS` constants are a USER-APPROVED contract gate; the edit is CONDITIONAL — if the measured marginal ≈ the 120_000 placeholder within the chosen safety margin, attest "placeholders validated, no change"; if it differs materially, propose the exact calibrated integer behind the gate. The AfKing `BOUNTY_ETH_TARGET` is a deploy-script param (AGENT-editable). The CrankFaucetResistance test mirror must sync with any contract-constant change in the SAME change.

Purpose: GAS-06 is the calibration deliverable that closes OPEN-A. The reward must reimburse the cranker's MARGINAL per-item gas at 0.5 gwei with NO base-amortization margin (REW-03 + SAFE-01), and MUST NOT exceed the cranker's actual marginal gas cost (else a self-crank Sybil faucet opens — SAFE-01 round-trip ≤ 0, HARD).

Output: the placement +0% verification + the CONDITIONAL contract-constant calibration (one batched USER-APPROVED diff if any edit is needed) + the test-mirror sync + the deploy-param decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-RESEARCH.md
@.planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-PATTERNS.md
@.planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-GAS-DERIVATION.md
@.planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-GAS-05-GUARDRAILS.md

<interfaces>
<!-- The exact calibration targets + the test mirror + the deploy param, all source-verified at HEAD 0d9d321f. -->

Contract constants (DegenerusGame.sol):
  CRANK_GAS_PRICE_REF = 0.5 gwei            :1495  // FINAL/locked — DO NOT TOUCH
  CRANK_RESOLVE_BET_GAS_UNITS = 120_000     :1501  // PLACEHOLDER — calibrate to per-1-spin-item marginal (Plan 02 Test B)
  CRANK_OPEN_BOX_GAS_UNITS    = 120_000     :1502  // PLACEHOLDER — calibrate to per-box marginal (Plan 02 Task 2)
  reward usage: bets :1568 (FLAT per item), boxes :1622 (FLAT per box)

Test mirror (MUST sync if the constants change):
  CrankFaucetResistance.t.sol:74  uint256 private constant CRANK_RESOLVE_BET_GAS_UNITS = 120_000;
  CrankFaucetResistance.t.sol:75  uint256 private constant CRANK_OPEN_BOX_GAS_UNITS = 120_000;
  peg-equality assertions consume these at :145, :239, :265, :335, :443 (and the SAFE-01 round-trip ≤ 0 test)
  If any new Plan-02/03 harness also mirrors the constants, sync those too.

Deploy param (AGENT-editable — NOT a frozen contract gate):
  AfKing constructor: new AfKing(subCost, bounty, lootboxMin)
  DeployProtocol.sol:126  new AfKing(5_000_000_000, 885_000_000, 10_000_000_000)  // arg2 885_000_000 = _bountyEthTarget
  BOUNTY_ETH_TARGET immutable AfKing.sol:252, set :268. scripts/deploy.js does NOT reference AfKing (the production keeper deploy is the paired degenerus-utilities repo) — confirmed: not a frozen contracts/*.sol path.
  SUB_COST_ETH_TARGET (arg1) is economic/design, OUT of GAS-06 scope unless gas-coupled.

Calibration policy (LOCKED — REW-03 + A4 + SAFE-01):
  - CRANK_RESOLVE_BET_GAS_UNITS = the per-1-spin-item MARGINAL (NOT the 10-spin worst case — per-spin peg deliberately under-reimburses big wins; pegging to the worst case over-reimburses and risks the faucet floor).
  - CRANK_OPEN_BOX_GAS_UNITS = the per-box MARGINAL.
  - FAUCET FLOOR (SAFE-01, HARD): gasUnits · 0.5 gwei → BURNIE reward must NOT exceed the cranker's actual marginal gas cost (self-crank round-trip ≤ 0). Calibrate to the marginal or slightly BELOW, never above. 318-02 proved round-trip ≤ 0 at 120_000 — the calibrated value MUST preserve that.

Placement +0% (GAS-06): forge snapshot --check vs the Plan-01 .gas-snapshot baseline; the crank relaxes RESOLVE only, placement byte-identical.

Contract-gate rules (project HARD): isolate ALL contracts/*.sol edits into THIS plan; present ONE batched diff; explicit USER approval; NEVER pre-approve; never push before review; a PreToolUse hook blocks commits while contracts/*.sol is dirty (bypass CONTRACTS_COMMIT_APPROVED=1 only after approval). ContractAddresses.sol is freely modifiable; every other contracts/*.sol needs approval.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GAS-06 placement +0% verification + calibration decision (compute, do NOT edit contracts yet)</name>
  <files>.planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-GAS-06-CALIBRATION.md</files>
  <read_first>
    - .planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-02-SUMMARY.md (the per-1-spin-item resolve marginal + the per-box marginal — the calibration inputs)
    - .planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-03-SUMMARY.md (the per-successful-player sweep marginal — the BOUNTY_ETH_TARGET input)
    - .planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-04-SUMMARY.md + 319-GAS-05-GUARDRAILS.md (the GAS-05 verdict + the optional GAS-02 hoist disposition)
    - .planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-RESEARCH.md (§GAS-06 Calibration + Contract-Edit Scope — the policy + the constant table)
    - test/fuzz/CrankFaucetResistance.t.sol:74-75,177-182 (the SAFE-01 round-trip ≤ 0 test + the peg mirror + the peg-equality assertions)
    - .gas-snapshot (the Plan-01 placement baseline)
  </read_first>
  <action>
    Compute the calibration WITHOUT editing any `contracts/*.sol` yet.
    1. Placement +0%: run `forge snapshot --check` against the Plan-01 `.gas-snapshot` placement reference subset (documented in 319-01-SUMMARY). Confirm the placement-hot-path rows are +0% (the crank relaxes RESOLVE only). Any non-zero placement delta is a defect — investigate before proceeding. Record the result.
    2. Read the measured marginals from the Plan-02/03 SUMMARYs: the per-1-spin-item resolve marginal (Plan 02 Test B), the per-box marginal (Plan 02 Task 2), the per-successful-player sweep marginal (Plan 03).
    3. Decide the calibration per the LOCKED policy (REW-03 + A4 + SAFE-01): `CRANK_RESOLVE_BET_GAS_UNITS` → the per-1-spin-item marginal; `CRANK_OPEN_BOX_GAS_UNITS` → the per-box marginal; the value must be AT or BELOW the marginal (faucet floor — round-trip ≤ 0). For EACH constant, decide: (a) if the measured marginal ≈ the 120_000 placeholder within the chosen safety margin → NO edit needed (attest "placeholder validated"); (b) if it differs materially → propose the exact calibrated integer. Document the chosen safety-margin policy and the resulting decision (edit / no-edit) for each constant with the measured numbers.
    4. Decide the `BOUNTY_ETH_TARGET` deploy-param (`DeployProtocol.sol:126` arg2 = 885_000_000) from the Plan-03 per-player sweep marginal — this is AGENT-editable (not a frozen gate). Decide edit/no-edit and the proposed value.
    5. Fold in the GAS-05 verdict: if the optional GAS-02 hoist was approved (Plan 04) AND a before/after measurement shows a real saving, include it in the proposed batched diff; otherwise note it as a no-op / not-shipped.
    Produce the calibration decision record at `319-GAS-06-CALIBRATION.md`: per-constant measured marginal, chosen value, edit/no-edit, and the faucet-floor check (calibrated value · 0.5 gwei · PRICE_COIN_UNIT / price ≤ the cranker's marginal gas cost). Do NOT touch `contracts/*.sol` in this task.
  </action>
  <verify>
    <automated>forge snapshot --check 2>&1 | tail -5 ; git diff --name-only -- contracts/ | grep -q . && echo "CONTRACT DIRTY FAIL" || echo "contracts clean OK"</automated>
  </verify>
  <acceptance_criteria>
    - Placement +0% confirmed via `forge snapshot --check` against the Plan-01 baseline (or any non-zero delta investigated + explained).
    - `319-GAS-06-CALIBRATION.md` exists with a per-constant calibration decision (measured marginal, chosen value, edit/no-edit) + the faucet-floor check (calibrated value ≤ the marginal; round-trip ≤ 0 preserved).
    - The BOUNTY_ETH_TARGET deploy-param decision (edit/no-edit + value) is recorded.
    - The GAS-05 optional-hoist disposition is folded in (ship-if-real-saving / no-op).
    - `git diff --name-only -- contracts/` is EMPTY (no contract edit in this task).
  </acceptance_criteria>
  <done>The calibration is computed and the edit/no-edit decision per constant is recorded with the faucet-floor proof; placement is confirmed +0%; no contract touched yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: USER-APPROVED contract gate — present the batched diff (CONDITIONAL) for explicit approval</name>
  <action>Present the Task-1 calibration decision (`319-GAS-06-CALIBRATION.md`) to the user and BLOCK for explicit approval BEFORE any `contracts/*.sol` write. This is a checkpoint, not an autonomous edit — the contents to present and the resume condition are in `<what-built>` / `<how-to-verify>` / `<resume-signal>`. Nothing is pre-approved (`feedback_never_preapprove_contracts`); the executor writes no contract until the user approves here. If OUTCOME B, the exact batched diff (DegenerusGame.sol:1501-1502 integers + the CrankFaucetResistance.t.sol:74-75 mirror sync + any approved GAS-02 hoist) is shown for review; if OUTCOME A, the "placeholders validated, no change" attestation is confirmed.</action>
  <what-built>
    Task 1 computed the GAS-06 calibration decision. This checkpoint presents the result for explicit USER approval BEFORE any `contracts/*.sol` write. NOTHING is pre-approved (project HARD rule `feedback_never_preapprove_contracts`).

    Two possible outcomes, both presented:
    - OUTCOME A (no contract edit): if the measured per-item marginals ≈ the 120_000 placeholders within the chosen safety margin, the recommendation is "placeholders validated, no contract change." The user confirms the attestation; the phase needs no DegenerusGame.sol edit.
    - OUTCOME B (contract edit): if a marginal differs materially, present the EXACT proposed batched diff: the calibrated CRANK_RESOLVE_BET_GAS_UNITS / CRANK_OPEN_BOX_GAS_UNITS integers at DegenerusGame.sol:1501-1502 (CRANK_GAS_PRICE_REF :1495 UNTOUCHED) + (if approved in Plan 04) the GAS-02 loop-invariant hoist + the required CrankFaucetResistance.t.sol:74-75 mirror sync. The user reviews the full diff and approves or requests changes.

    In BOTH outcomes also surface the BOUNTY_ETH_TARGET deploy-param decision (AGENT-editable, NOT part of the frozen-contract approval — but show the proposed value for visibility).
  </what-built>
  <how-to-verify>
    1. Review the Task-1 calibration decision record: the measured per-1-spin-item resolve marginal, the per-box marginal, the per-player sweep marginal, and the chosen safety-margin policy.
    2. Confirm the faucet-floor check: each proposed gasUnits value (or the retained 120_000) · 0.5 gwei → BURNIE reward is AT or BELOW the cranker's measured marginal gas cost (self-crank round-trip ≤ 0 — SAFE-01, HARD). Reject any value that would open the faucet.
    3. If OUTCOME B, review the exact DegenerusGame.sol:1501-1502 integer change + the CrankFaucetResistance.t.sol:74-75 mirror sync + any GAS-02 hoist. Confirm CRANK_GAS_PRICE_REF (:1495, 0.5 gwei) is NOT touched.
    4. Review the BOUNTY_ETH_TARGET deploy-param decision (DeployProtocol.sol:126 arg2).
    5. Approve, or request changes.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the batched diff (or confirm OUTCOME-A no-change attestation), or describe the changes you want. The executor will NOT write any contracts/*.sol until you approve.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply the approved calibration + sync the test mirror + verify green (post-approval only)</name>
  <files>contracts/DegenerusGame.sol, test/fuzz/CrankFaucetResistance.t.sol, test/fuzz/helpers/DeployProtocol.sol, .gas-snapshot</files>
  <read_first>
    - .planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-RESEARCH.md (§GAS-06 — the edit shape: ONLY the numeric values change; names/shape fixed; CRANK_GAS_PRICE_REF untouched; mirror sync mandatory)
    - .planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-GAS-06-CALIBRATION.md (the Task-1 decision + the Task-2 approved values)
    - contracts/DegenerusGame.sol:1495-1502 (the constant block — the ONLY mainnet-contract mutation, and only if OUTCOME B)
    - test/fuzz/CrankFaucetResistance.t.sol:74-75,177-182 (the mirror + the peg-equality + SAFE-01 round-trip ≤ 0 assertions)
    - test/fuzz/helpers/DeployProtocol.sol:126 (the AfKing deploy arg — for the BOUNTY_ETH_TARGET tune)
  </read_first>
  <action>
    Execute ONLY what Task 2 approved. NEVER proceed without the explicit approval from Task 2.
    - OUTCOME A (no contract edit approved): do NOT touch DegenerusGame.sol. Attest "placeholders validated, no change" in the SUMMARY. Apply the approved BOUNTY_ETH_TARGET deploy-param tune to `DeployProtocol.sol:126` arg2 if any (AGENT-editable). If the deploy-param changed, re-run the AfKing/sweep suites green.
    - OUTCOME B (contract edit approved): edit `contracts/DegenerusGame.sol:1501-1502` to the approved calibrated integers (ONLY the numeric values; CRANK_GAS_PRICE_REF :1495 UNTOUCHED). Apply the approved GAS-02 hoist if any. CRITICAL — sync the test mirror in the SAME change: update `CrankFaucetResistance.t.sol:74-75` to the new values (and any new Plan-02/03 harness that mirrors the constants) or the peg-equality assertions (:177-182) break. Apply the approved BOUNTY_ETH_TARGET deploy-param tune to `DeployProtocol.sol:126` arg2 if any.
    Then in BOTH outcomes:
    - Re-run the faucet-resistance + crank-reward suites GREEN: `forge test --match-contract CrankFaucetResistance` (the SAFE-01 round-trip ≤ 0 + peg-equality must stay green) + the Plan-02/03 gas harnesses.
    - Re-run `forge snapshot` to capture the new crank/sweep/jackpot rows and confirm placement is still +0% (`forge snapshot --check`).
    - Confirm no NEW failures vs the 44-failure 318 baseline.
    For the contract commit (OUTCOME B only): the batched contract commit requires the user's approval (already given in Task 2) and the PreToolUse hook bypass `CONTRACTS_COMMIT_APPROVED=1`. ContractAddresses.sol stays clean (restore via `git checkout` if the build patched it). Defer any non-contract commit until after the approved contract commit lands (the hook blocks commits while contracts/*.sol is dirty).
  </action>
  <verify>
    <automated>forge test --match-contract CrankFaucetResistance -vv 2>&1 | grep -qE "[0-9]+ passed" && forge snapshot --check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Only the Task-2-approved change is applied (OUTCOME A: no DegenerusGame.sol edit; OUTCOME B: the exact approved integers at :1501-1502 with CRANK_GAS_PRICE_REF untouched).
    - If OUTCOME B, the CrankFaucetResistance.t.sol:74-75 mirror (and any other mirror) is synced in the SAME change; `forge test --match-contract CrankFaucetResistance` is GREEN (peg-equality + SAFE-01 round-trip ≤ 0 hold).
    - Placement is still +0% (`forge snapshot --check`); no NEW failures vs the 44-failure 318 baseline.
    - The BOUNTY_ETH_TARGET deploy-param tune (if any) is applied to DeployProtocol.sol:126 and the AfKing/sweep suites are green.
    - ContractAddresses.sol is clean; no unapproved contracts/*.sol mutation.
  </acceptance_criteria>
  <done>GAS-06 closed: the peg constants are either validated as-is or recalibrated per the approved decision, the test mirror is synced, placement is +0%, the faucet floor holds, and the deploy-param is tuned — all within the USER-approved boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured marginal → peg constant | A peg calibrated ABOVE the marginal opens a self-crank Sybil faucet (round-trip > 0) |
| contract edit → USER approval | Any contracts/*.sol write without explicit approval violates the project HARD gate (never-preapprove + batch-approval + manual-review-before-push) |
| contract constant ↔ test mirror | If the constant changes but the CrankFaucetResistance mirror does not, the peg-equality assertions break (a green-suite illusion or a false RED) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-319-15 | EoP (self-crank Sybil faucet) | peg constant calibration | mitigate | Calibrate to the per-1-spin-item / per-box MARGINAL or BELOW, never the worst case (REW-03 + A4); the faucet-floor check (gasUnits · 0.5 gwei ≤ marginal gas cost) is verified in Task 1 and re-proven by the CrankFaucetResistance SAFE-01 round-trip ≤ 0 test in Task 3 (318-02 proved it at 120_000; the calibrated value preserves it) |
| T-319-16 | Tampering (unapproved contract edit) | DegenerusGame.sol | mitigate | The contract edit is the ONLY contracts/*.sol mutation, isolated into this LAST plan behind a blocking-human checkpoint (Task 2); CONDITIONAL (OUTCOME A = no edit); nothing pre-approved; the PreToolUse hook blocks commits while contracts/*.sol is dirty (bypass only after approval); CRANK_GAS_PRICE_REF (0.5 gwei) explicitly NOT touched |
| T-319-17 | Tampering (stale test mirror) | CrankFaucetResistance.t.sol:74-75 | mitigate | The mirror sync is mandatory IN THE SAME change as any contract-constant edit (Task 3); the peg-equality assertions (:177-182) flip RED if it drifts, so green proves the sync |
| T-319-18 | DoS (placement regression) | placement hot path | mitigate | `forge snapshot --check` against the Plan-01 baseline proves placement +0%; any non-zero delta is investigated before the gate (Task 1) and re-verified after the edit (Task 3) |
| T-319-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH §Package Legitimacy Audit = N/A) |
</threat_model>

<verification>
- Placement +0% via `forge snapshot --check` (before the gate AND after any edit).
- The faucet floor holds: the calibrated (or retained) gasUnits · 0.5 gwei reward ≤ the cranker's marginal gas cost; CrankFaucetResistance SAFE-01 round-trip ≤ 0 GREEN.
- CONDITIONAL contract edit: OUTCOME A = no edit (attest); OUTCOME B = the exact approved integers + the synced test mirror.
- The USER-approval gate (Task 2) precedes any contracts/*.sol write; nothing pre-approved.
- CRANK_GAS_PRICE_REF (0.5 gwei, :1495) untouched; ContractAddresses.sol clean; no NEW failures vs the 44-failure 318 baseline.
</verification>

<success_criteria>
- GAS-06 closed: peg constants validated-or-recalibrated, placement +0%, faucet floor preserved, test mirror synced, deploy-param tuned.
- All contracts/*.sol edits (if any) are isolated, batched, and USER-approved; nothing pre-approved.
</success_criteria>

<output>
Create `.planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-05-SUMMARY.md` when done.
</output>
