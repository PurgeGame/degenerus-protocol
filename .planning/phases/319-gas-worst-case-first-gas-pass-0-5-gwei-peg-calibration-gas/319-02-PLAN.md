---
phase: 319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas
plan: 02
type: execute
wave: 2
depends_on: ["319-01"]
files_modified:
  - test/gas/CrankResolveBetWorstCaseGas.t.sol
  - test/gas/CrankOpenBoxWorstCaseGas.t.sol
  - test/fuzz/JackpotSingleCallCorrectness.t.sol
autonomous: true
requirements: [GAS-01, JGAS-04, GAS-06]
must_haves:
  truths:
    - "The resolve-bet 10-spin-all-match worst case is constructed, asserted to BE the max (ticketCount==10, all-match), and measured under the REAL 30M mainnet block gas limit"
    - "The open-box single-materialization worst case is measured and the per-box marginal isolated"
    - "The per-1-spin-item resolve-bet MARGINAL is isolated (the calibration target for CRANK_RESOLVE_BET_GAS_UNITS, distinct from the 10-spin worst case)"
    - "The 305-winner daily-ETH single-call jackpot worst case is re-framed worst-case-FIRST, confirmed < 30M with margin, and the enabling delta is attributed to the removed per-winner autoRebuyState SLOAD"
  artifacts:
    - path: "test/gas/CrankResolveBetWorstCaseGas.t.sol"
      provides: "GAS-01 resolve-bet 10-spin-all-match worst-case measurement + per-1-spin-item marginal isolation"
      contains: "MAINNET_BLOCK_GAS_LIMIT"
    - path: "test/gas/CrankOpenBoxWorstCaseGas.t.sol"
      provides: "GAS-01 open-box single-materialization worst-case + per-box marginal"
      contains: "crankBoxes"
    - path: "test/fuzz/JackpotSingleCallCorrectness.t.sol"
      provides: "JGAS-04 worst-case-first re-frame + ~1.3M freed-SLOAD delta attribution (extends 318-06)"
      contains: "autoRebuyState"
  key_links:
    - from: "test/gas/CrankResolveBetWorstCaseGas.t.sol"
      to: "the measured per-1-spin-item marginal that calibrates CRANK_RESOLVE_BET_GAS_UNITS"
      via: "emit log_named_uint of the per-item marginal; Plan 05 reads it from the test log"
      pattern: "emit log_named_uint"
    - from: "test/gas/CrankOpenBoxWorstCaseGas.t.sol"
      to: "the measured per-box marginal that calibrates CRANK_OPEN_BOX_GAS_UNITS"
      via: "emit log_named_uint of the per-box marginal"
      pattern: "emit log_named_uint"
---

<objective>
Measure the two crank work-type worst cases derived in Plan 01 (resolve-bet 10-spin-all-match + open-box single materialization), isolate the per-item MARGINALS that calibrate the two `*_GAS_UNITS` peg constants, and complete JGAS-04 by re-framing the existing 305-winner single-call jackpot measurement worst-case-FIRST and attributing the enabling delta to the removed per-winner `autoRebuyState` SLOAD.

Purpose: GAS-01 requires measuring the THEORETICAL worst case derived in Plan 01 (not an average); JGAS-04 requires empirically confirming the JGAS-01 305-winner theory with margin and attributing the freed delta. The measured per-item marginals are the calibration inputs Plan 05 (the contract gate) consumes.

Output: two new `test/gas/*.t.sol` harnesses + JGAS-04 assertions added to the existing `JackpotSingleCallCorrectness.t.sol`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-RESEARCH.md
@.planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-PATTERNS.md
@.planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-GAS-DERIVATION.md

<interfaces>
<!-- Fixture + measurement idioms, source-verified. Use directly. -->

Crank fixture (clone CrankFaucetResistance.t.sol):
  contract X is DeployProtocol { ... }          CrankFaucetResistance.t.sol:43
  LOOTBOX_RNG_PACKED_SLOT = 35                    :49
  LOOTBOX_RNG_WORD_SLOT   = 36                    :52
  DEGENERETTE_BETS_SLOT   = 43                    :55
  setUp: _deployProtocol(); seed lootboxRngIndex (word stays 0); game.setOperatorApproval(address(game), true)  :97,:111-124
  WHY setOperatorApproval: the crank onlySelf delegatecall resolves with msg.sender==address(game); without
    approval _requireApproved reverts and every item silently skips (zero-reward vacuous test).
  word-inject helper writes lootboxRngWordByIndex[index]: slot = keccak256(abi.encode(index, LOOTBOX_RNG_WORD_SLOT))  :535
  bet slot helper: inner = keccak256(abi.encode(owner, DEGENERETTE_BETS_SLOT))  :541

Gas-measurement idiom (clone RedemptionGas.t.sol:197-205):
  uint256 gasBefore = gasleft();  target.call(...);  uint256 g = gasBefore - gasleft();
  emit log_named_uint("name", g);  assertLe/assertLt(g, LIMIT, "...");

Real-30M block limit (clone JackpotSingleCallCorrectness.t.sol:80-82):
  uint256 internal constant MAINNET_BLOCK_GAS_LIMIT = 30_000_000;  // foundry.toml inflates to 30e9

Per-iteration micro-bench (clone RollRemainderGas.t.sol:57-64): loop N, divide delta by N → per-unit marginal.

JGAS-04 existing test to re-frame (JackpotSingleCallCorrectness.t.sol):
  testWorstCaseSingleCallFitsBlockGasLimit  :243-269  (measured 7,503,715 gas < 30M)
  harness is module-EXTENDING: JackpotSingleCallHarness extends DegenerusGameJackpotModule  :21 (KEEP — do not switch to DeployProtocol)
  2-arg _addClaimableEth(w, perWinner) post-RM-02 falls straight to _creditClaimable; OLD 3-arg did cold
    autoRebuyState[beneficiary] SLOAD + _processAutoRebuy branch per winner.

Crank entry points: crankBets(address[] players, uint64[] betIds) DegenerusGame.sol:1543; crankBoxes(uint256 maxCount) :1592.
Box enqueue: first-deposit lootboxEthBase==0 signal -> enqueueBoxForCrank :1526 (fired from mint-module first deposit).
MAX_SPINS_PER_BET=10 (DegeneretteModule:226). reward usage bets :1568 / boxes :1622 (FLAT per item).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CrankResolveBetWorstCaseGas — 10-spin all-match worst case + per-1-spin-item marginal</name>
  <read_first>
    - test/fuzz/CrankFaucetResistance.t.sol (the crank fixture: DeployProtocol base :43, slot constants :49-55, setUp index-seed + setOperatorApproval :97-124, the bet-placement + word-inject + crankBets path :144-170, the bet/index slot helpers :530-545)
    - test/fuzz/RedemptionGas.t.sol:181-205 (the gasleft-delta + named-log + assertLe idiom + the structural-attribution NatSpec precedent)
    - test/fuzz/RollRemainderGas.t.sol:57-64 (the loop-N-divide per-iteration micro-bench idiom)
    - .planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-GAS-DERIVATION.md (the resolve-bet derivation + the per-1-spin-item-vs-10-spin distinction)
    - contracts/DegenerusGame.sol:1543-1590 (crankBets, the FLAT per-item reward at :1568)
    - contracts/modules/DegenerusGameDegeneretteModule.sol:226,389,553-801 (MAX_SPINS_PER_BET, the spin loop, _distributePayout, _resolveLootboxDirect)
  </read_first>
  <behavior>
    - Test A (worst case): a single crankBets item resolving a bet with ticketCount==MAX_SPINS_PER_BET==10 where every spin wins ETH above the lootbox-conversion threshold (10x lootbox materialization) measures gas; assert the constructed scenario IS the max (assert ticketCount==10 and the all-match condition) BEFORE bracketing; assert measured < MAINNET_BLOCK_GAS_LIMIT (30M); emit the measured worst-case gas.
    - Test B (per-1-spin-item marginal): crank N typical 1-spin items, divide the delta by N → the per-item marginal that calibrates CRANK_RESOLVE_BET_GAS_UNITS; emit it via log_named_uint so Plan 05 reads it. Assert the per-1-spin marginal is materially BELOW the 10-spin worst case (confirms the per-spin peg under-reimburses big wins by construction — REW-03 faucet-safe).
    - Non-vacuity guard: assert the crank actually resolved (reward emitted / bet slot zeroed), so the measurement is not of a silently-skipped no-op (the setOperatorApproval gotcha).
  </behavior>
  <action>
    Create `test/gas/CrankResolveBetWorstCaseGas.t.sol` extending `DeployProtocol` (the live fixture — the crank writes Game storage, so a module-extending harness will NOT work here, unlike the jackpot). Clone the `CrankFaucetResistance` setUp: `_deployProtocol()`, warp +1 day, seed `lootboxRngIndex` with the word at 0 (placement requires word==0), `vm.prank(player); game.setOperatorApproval(address(game), true)` (REQUIRED — the crank's onlySelf delegatecall resolves with `msg.sender==address(game)`; without approval `_requireApproved` reverts and items silently skip → vacuous test). Define `MAINNET_BLOCK_GAS_LIMIT = 30_000_000` (assert the REAL 30M, NOT foundry.toml's inflated 30e9). Place a real degenerette bet via the public API crafted with `ticketCount == MAX_SPINS_PER_BET == 10` and `packedTraitsDegenerette` such that every spin's `_countMatches` drives the ETH-above-threshold lootbox branch (the 10x `_resolveLootboxDirect` materialization cost center — RESEARCH lines 99-128). Inject the RNG word post-placement, then crank. Bracket `game.crankBets(players, betIds)` with `gasleft()` delta (idiom RedemptionGas:197-205). Implement Tests A + B + the non-vacuity guard per `<behavior>`. For Test B, use the loop-N-divide idiom (RollRemainderGas:57-64) over N 1-spin items. Emit both the 10-spin worst case AND the per-1-spin marginal via `emit log_named_uint` (Plan 05 reads the marginal). Do NOT mirror or peg-assert the `*_GAS_UNITS` constants here (Plan 05 owns the calibration); this harness MEASURES. Zero `contracts/*.sol` mutation. If the verify gate patches ContractAddresses.sol for the build, restore it via `git checkout -- contracts/ContractAddresses.sol` before finishing (per the 318-06 contract-cleanliness precedent).
  </action>
  <verify>
    <automated>forge test --match-contract CrankResolveBetWorstCaseGas -vv 2>&1 | grep -qE "worst_case_resolve_bet|[0-9]+ passed" && git diff --name-only -- contracts/ | grep -q . && echo "CONTRACT DIRTY FAIL" || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-contract CrankResolveBetWorstCaseGas -vv` passes; Test A asserts `ticketCount==10` + all-match BEFORE measuring and asserts measured `< 30_000_000`.
    - Test B emits a per-1-spin-item marginal via `emit log_named_uint` and asserts it is materially below the 10-spin worst case.
    - The non-vacuity guard proves the crank actually resolved (not a silently-skipped no-op).
    - `git diff --name-only -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>The resolve-bet 10-spin all-match worst case is measured under 30M and the per-1-spin-item calibration marginal is logged for Plan 05; contracts/ clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CrankOpenBoxWorstCaseGas — single-materialization worst case + per-box marginal</name>
  <read_first>
    - test/fuzz/CrankFaucetResistance.t.sol (the index-seed + word-inject helpers + the crankBoxes path at :371-380 — the wordless-index early-return precedent)
    - test/fuzz/RedemptionGas.t.sol:197-205 (gasleft-delta idiom)
    - .planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-GAS-DERIVATION.md (the open-box derivation)
    - contracts/DegenerusGame.sol:1592-1640 (crankBoxes, the :1603 wordless-index gate, the FLAT per-box reward :1622, enqueueBoxForCrank :1526)
    - contracts/modules/DegenerusGameMintModule.sol:1004-1008 (first-deposit lootboxEthBase==0 signal that enqueues the box)
  </read_first>
  <behavior>
    - Test A (single-box marginal): with exactly one box queued (via a real lootbox-mode deposit firing the first-deposit enqueue) and the RNG word present at the index, measure `game.crankBoxes(1)` gas → the per-box marginal that calibrates CRANK_OPEN_BOX_GAS_UNITS; emit it; assert measured < MAINNET_BLOCK_GAS_LIMIT.
    - Test B (non-vacuity): assert the box actually opened (lootboxEth zeroed / reward emitted), not a wordless-index early-return (the :1603 skip).
    - Test C (calibration sanity): assert the single-box marginal is materially LESS than the resolve-bet 10-spin worst case (RESEARCH line 144: resolve-bet worst case ~= 10x a single box) — confirms the two constants will differ.
  </behavior>
  <action>
    Create `test/gas/CrankOpenBoxWorstCaseGas.t.sol` extending `DeployProtocol`. Reuse the `CrankFaucetResistance` index-seed + word-inject helpers. The box enqueues at first deposit via the `lootboxEthBase == 0` signal (MintModule:1004-1008 → enqueueBoxForCrank :1526), so place a real lootbox-mode deposit so the box enqueues, then inject the RNG word at the index (the :1603 gate requires word != 0). Define `MAINNET_BLOCK_GAS_LIMIT = 30_000_000`. Bracket `game.crankBoxes(1)` (open exactly one queued box — the per-box marginal) with the `gasleft()` delta. Implement Tests A/B/C per `<behavior>`. Emit the per-box marginal via `emit log_named_uint` (Plan 05 reads it). Do NOT peg-assert the `*_GAS_UNITS` constants (Plan 05 owns calibration). Zero `contracts/*.sol` mutation; restore ContractAddresses.sol via `git checkout` if the build patched it.
  </action>
  <verify>
    <automated>forge test --match-contract CrankOpenBoxWorstCaseGas -vv 2>&1 | grep -qE "worst_case_open_box|[0-9]+ passed" && git diff --name-only -- contracts/ | grep -q . && echo "CONTRACT DIRTY FAIL" || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-contract CrankOpenBoxWorstCaseGas -vv` passes; the single-box marginal is emitted via `emit log_named_uint` and asserted `< 30_000_000`.
    - The non-vacuity guard proves the box actually opened (not a :1603 wordless-index early-return).
    - Test C asserts the single-box marginal is materially less than the resolve-bet 10-spin worst case.
    - `git diff --name-only -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>The open-box single-materialization worst case is measured and the per-box calibration marginal is logged for Plan 05; contracts/ clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: JGAS-04 — worst-case-first re-frame + freed-SLOAD delta attribution (extend 318-06)</name>
  <read_first>
    - test/fuzz/JackpotSingleCallCorrectness.t.sol (the existing 318-06 harness: module-extending JackpotSingleCallHarness :21, testWorstCaseSingleCallFitsBlockGasLimit :243-269, the MAINNET_BLOCK_GAS_LIMIT constant :80-82, the _seedAllBuckets + runTerminalJackpot driver)
    - .planning/phases/318-tst-subscription-crank-correctness-removal-proofs-tst/318-06-SUMMARY.md (the JGAS-03 result: 7,503,715 gas, 305-winner worst case, the "Phase 319 / JGAS-04" handoff for delta attribution)
    - .planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-RESEARCH.md (§JGAS-04 Reconciliation — the delta-attribution method, option (a) structural attribution preferred; ~4.2k x 305 ≈ 1.28M; A2 risk note)
    - .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md:357-366 (the JGAS-01 §(2) theoretical worst-case derivation — 9-12M, ~2.5-3.3x margin, the RM-02 frees ~1.3M argument)
  </read_first>
  <behavior>
    - Test A (worst-case-FIRST re-frame): assert the 305-winner max-scale single call IS the daily-ETH worst case (DAILY_ETH_MAX_WINNERS=305 hard cap; MAX_BUCKET_WINNERS=250 never clips a 159 bucket) BEFORE measuring (already structurally true in 318-06; make the worst-case-first framing explicit if not already a standalone assertion), then assert measured < MAINNET_BLOCK_GAS_LIMIT with margin; emit the margin (30M - measured).
    - Test B (delta attribution): structurally attribute the enabling delta to the removed per-winner cold autoRebuyState SLOAD + _processAutoRebuy branch. Compute the freed estimate (1 cold slot ~2100 + 1 cold account ~2100 = ~4.2k per winner x 305 ≈ 1.28M) and assert the measured 7.5M sits in the `theory - freed`-consistent band (option (a) structural attribution — do NOT re-introduce dead code; option (b) comparison harness is explicitly rejected). Reconcile against the 316-SPEC §J4.2 9-12M theory. Treat the exact 1.3M as "consistent with" not "exactly" per A2.
  </behavior>
  <action>
    EXTEND `test/fuzz/JackpotSingleCallCorrectness.t.sol` (do NOT create a new file — RESEARCH §Don't Hand-Roll: extend the existing 305-winner harness). Add a JGAS-04 worst-case-first re-frame assertion and a delta-attribution assertion per `<behavior>`. Keep the module-extending `JackpotSingleCallHarness extends DegenerusGameJackpotModule` pattern (:21) — it is the right choice for the jackpot (it drives the production `_processDailyEth → _processBucket → _addClaimableEth` path in the harness's own storage; the crank harnesses use DeployProtocol but the jackpot does NOT). Use the existing `MAINNET_BLOCK_GAS_LIMIT` constant. For the attribution, use structural option (a): define a named constant for the freed-per-winner cold-access estimate (~4.2k), compute `freed = perWinnerFreed * 305`, and assert the measured 7.5M is consistent with the 316-SPEC theoretical 9-12M minus the freed band — do NOT re-introduce a no-op SLOAD. Add a NatSpec block citing 316-SPEC §J4.2/J4.3 and the A2 "consistent-with not exactly" caveat. The new assertions exercise live production logic only (no new fixture, no stubs). Zero `contracts/*.sol` mutation; restore ContractAddresses.sol via `git checkout` if the build patched it.
  </action>
  <verify>
    <automated>forge test --match-contract JackpotSingleCallCorrectness -vv 2>&1 | grep -qE "[0-9]+ passed" && grep -q "autoRebuyState" test/fuzz/JackpotSingleCallCorrectness.t.sol && git diff --name-only -- contracts/ | grep -q . && echo "CONTRACT DIRTY FAIL" || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-contract JackpotSingleCallCorrectness -vv` passes (all 8 prior tests + the new JGAS-04 assertions).
    - The worst-case-first re-frame asserts 305 IS the max BEFORE measuring and asserts measured < 30M with the margin emitted.
    - The delta-attribution test uses structural option (a) (no re-introduced dead code), references `autoRebuyState`, computes the ~1.3M freed band, and reconciles against the 316-SPEC 9-12M theory with the A2 "consistent-with" caveat.
    - `git diff --name-only -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>JGAS-04 complete: the 305-winner single-call worst case is re-framed worst-case-first, confirmed < 30M with margin, and the ~1.3M freed-SLOAD delta is structurally attributed without re-introducing dead code; contracts/ clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → crank onlySelf delegatecall | A missing `setOperatorApproval(address(game), true)` makes every item silently skip → a vacuous "measurement" of a no-op |
| measured marginal → Plan 05 peg calibration | A marginal measured on the WRONG scenario (average not worst, or a skipped no-op) mis-calibrates the faucet-safety floor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-319-04 | Tampering (faucet via mis-peg) | per-item marginal measurement | mitigate | The bet harness isolates the per-1-spin-item marginal (the REW-03 calibration target) separately from the 10-spin worst case; Test B asserts the marginal is below the worst case so Plan 05 cannot peg above the faucet floor (SAFE-01 round-trip ≤ 0) |
| T-319-05 | Repudiation (vacuous test) | crank fixture | mitigate | Every harness includes a non-vacuity guard (bet slot zeroed / box opened / reward emitted) proving the crank actually did work; the `setOperatorApproval` gotcha is documented in `<interfaces>` and the setUp |
| T-319-06 | Information disclosure (wrong-scenario worst case) | JGAS-04 re-frame | mitigate | Worst-case-FIRST: assert 305 IS the daily-ETH max (hard cap + MAX_BUCKET_WINNERS never clips) BEFORE measuring; assert against the REAL 30M, not foundry.toml's inflated 30e9 |
| T-319-07 | Tampering (dead-code re-introduction) | JGAS-04 delta attribution | mitigate | Use structural attribution (option a) — compute the freed estimate from EIP-2929 cold-access constants; do NOT re-introduce a no-op SLOAD (option b rejected). Treat the figure as "consistent with" not "exactly" per A2 |
| T-319-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH §Package Legitimacy Audit = N/A) |
</threat_model>

<verification>
- All three suites green: `forge test --match-contract CrankResolveBetWorstCaseGas` + `--match-contract CrankOpenBoxWorstCaseGas` + `--match-contract JackpotSingleCallCorrectness`.
- Each harness asserts its scenario IS the worst case BEFORE bracketing, asserts against the REAL 30M, and includes a non-vacuity guard.
- The per-1-spin-item resolve marginal + the per-box marginal are emitted via `log_named_uint` for Plan 05.
- JGAS-04 attributes the ~1.3M freed delta structurally (no dead-code re-introduction).
- `git diff --name-only -- contracts/` empty across all tasks; no NEW failures vs the 44-failure 318 baseline.
</verification>

<success_criteria>
- GAS-01 (resolve-bet + open-box worst cases) and JGAS-04 (305-winner re-frame + delta attribution) measured and proven.
- The two crank per-item marginals are logged as the calibration inputs for Plan 05.
- Zero `contracts/*.sol` mutation; suite recompiles green.
</success_criteria>

<output>
Create `.planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-02-SUMMARY.md` when done.
</output>
