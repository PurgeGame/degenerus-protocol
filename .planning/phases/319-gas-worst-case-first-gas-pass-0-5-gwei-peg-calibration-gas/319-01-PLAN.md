---
phase: 319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-GAS-DERIVATION.md
  - .gas-snapshot
autonomous: true
requirements: [GAS-01, GAS-06]
must_haves:
  truths:
    - "The theoretical worst case per work-type (resolve-bet 10-spin-all-match, open-box single materialization, sweep-per-player) is derived from source SLOAD/SSTORE/delegatecall/loop-bound counts BEFORE any measurement"
    - "Each derivation cites the exact cost-center file:line chain and states why the constructed scenario IS the max"
    - "A placement-hot-path .gas-snapshot baseline exists so GAS-06 can prove placement +0%"
  artifacts:
    - path: ".planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-GAS-DERIVATION.md"
      provides: "Paper-first worst-case derivation per work-type (the GAS-01 derive-before-measure deliverable)"
      contains: "MAX_SPINS_PER_BET"
    - path: ".gas-snapshot"
      provides: "Placement-hot-path gas baseline for the GAS-06 +0% regression check"
  key_links:
    - from: "319-GAS-DERIVATION.md"
      to: "the Wave-2 measurement harnesses (319-02, 319-03)"
      via: "each derivation names the harness that will measure it + the assert-is-worst-case precondition"
      pattern: "CrankResolveBetWorstCaseGas|CrankOpenBoxWorstCaseGas|SweepPerPlayerWorstCaseGas"
---

<objective>
Produce the GAS-01 worst-case-FIRST derivation document (paper-first, per the project HARD rule `feedback_gas_worst_case` — derive the theoretical worst case from source BEFORE measuring), and capture the placement-hot-path `.gas-snapshot` baseline that GAS-06 (Plan 05) uses to prove placement gas is +0%.

Purpose: `feedback_gas_worst_case` requires that every gas claim derives the theoretical worst case first, then measures THAT specific scenario. This plan is the "derive" half; Plans 02/03 are the "measure" half. Structuring the derivation as a committed artifact forces the worst-case framing to precede (and constrain) the harnesses, and gives the measurement harnesses their assert-is-worst-case preconditions.

Output: `319-GAS-DERIVATION.md` (the per-work-type structural cost derivation) + a `.gas-snapshot` baseline including the placement hot path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-RESEARCH.md
@.planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-PATTERNS.md

<interfaces>
<!-- Worst-case cost-center chains, source-verified at HEAD 0d9d321f. Use directly — no codebase exploration needed. -->

Resolve-bet worst case (the headline cost center):
  crankBets(players[], betIds[])              DegenerusGame.sol:1543
    reads lvl = _activeTicketLevel() ONCE      DegenerusGame.sol:1556
    per item: try this._crankResolveBet         DegenerusGame.sol:1562 (onlySelf :1641)
      delegatecall resolveBets                  DegeneretteModule:389
        _resolveFullTicketBet spin loop 0..ticketCount-1, ticketCount <= MAX_SPINS_PER_BET=10  DegeneretteModule:226
          per winning spin: _distributePayout   DegeneretteModule:705
            future-prize-pool SSTORE + _addClaimableEth SSTORE  DegeneretteModule:1117
            _resolveLootboxDirect                DegeneretteModule:783
              DELEGATECALL resolveLootboxDirect  LootboxModule:628
                _resolveLootboxCommon            LootboxModule:917
                  nested DELEGATECALL BoonModule  LootboxModule:992
                  _queueTickets SSTORE
    if reward != 0: coinflip.creditFlip ONCE     DegenerusGame.sol:1578

Open-box worst case:
  crankBoxes(maxCount)                          DegenerusGame.sol:1592
    if lootboxRngWordByIndex[index]==0 return    DegenerusGame.sol:1603 (orphan-index/RngNotReady skip)
    reads lvl = _activeTicketLevel() ONCE        DegenerusGame.sol:1610
    per box: try this._crankOpenBox -> _openLootBoxFor -> same _resolveLootboxCommon body (ONE materialization)
    one SSTORE boxCursor; if reward != 0 creditFlip ONCE  DegenerusGame.sol:1632

Sweep-per-player worst case:
  AfKing.sweep(maxCount)                         AfKing.sol:522
    if game.rngLocked() revert SweepAborted      AfKing.sol:523
    reads mp = game.mintPrice() ONCE             AfKing.sol:527
    per player: hasAnyLazyPass + isOperatorApproved + claimableWinningsOf(x2 if reinvest) + the
      batchPurchase._batchPurchaseUnit -> _purchaseFor slice (full mint->lootbox->prize-pool->EV-cap->quest)
    ONE game.batchPurchase{value: totalValue}(...)  + ONE creditFlip bounty (stall-scaled)

Constants: MAX_SPINS_PER_BET=10 (DegeneretteModule:226); DAILY_ETH_MAX_WINNERS=305; foundry.toml block_gas_limit=30_000_000_000 (inflated — assert REAL 30_000_000).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the GAS-01 worst-case derivation doc (paper-first, per feedback_gas_worst_case)</name>
  <read_first>
    - .planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-RESEARCH.md (§Architecture Patterns — the three worst-case derivations; §JGAS-04 Reconciliation; §GAS-06 calibration policy)
    - .planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-PATTERNS.md (§Shared Patterns → "Worst-case-FIRST framing"; the "No Analog Found" note citing 306-05-GAS-BASELINE.md as the derivation-doc precedent format)
    - contracts/DegenerusGame.sol:1490-1640 (crank machinery: peg constants, crankBets, crankBoxes, _crankResolveBet, _crankOpenBox)
    - contracts/modules/DegenerusGameDegeneretteModule.sol:226,389,553-801,1117 (MAX_SPINS_PER_BET, resolveBets, _resolveFullTicketBet spin loop, _distributePayout, _resolveLootboxDirect, 2-arg _addClaimableEth)
    - .planning/phases/318-tst-subscription-crank-correctness-removal-proofs-tst/318-06-SUMMARY.md (the JGAS-03 305-winner worst-case derivation precedent — 7,503,715 gas; structural derivation method)
  </read_first>
  <action>
    Create `.planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-GAS-DERIVATION.md`. This is the GAS-01 paper-first deliverable (`feedback_gas_worst_case`: derive the theoretical worst case from source BEFORE measuring). Mirror the structure of the JGAS-03 worst-case derivation in 318-06-SUMMARY §"Worst-Case Gas Derivation" (precedent: 306-05-GAS-BASELINE.md per PATTERNS). For EACH of the three work-types, document a section with: (a) the cost-center file:line chain (use the RESEARCH §Architecture Patterns traces, source-verified); (b) the structural per-item cost as a count of cold/warm SLOADs, SSTOREs, delegatecall depth, loop bound, and events — NOT a number read off a benchmark; (c) the explicit statement of WHY the constructed scenario is the maximum and the assert-is-worst-case precondition the Wave-2 harness will check BEFORE bracketing the call; (d) the name of the harness that will measure it.

    Three sections:
    - RESOLVE-BET worst case: one degenerette bet with `ticketCount == MAX_SPINS_PER_BET == 10` (DegeneretteModule:226) where EVERY spin wins ETH above the lootbox-conversion threshold → 10 iterations each doing 1 future-prize-pool SSTORE + 1 claimable SSTORE + a 2-level delegatecall (`_resolveLootboxDirect` → LootboxModule:628 → nested BoonModule delegatecall :992) + a `_queueTickets` SSTORE + events. Precondition the harness asserts: `ticketCount == 10` and all-match (per RESEARCH lines 99-128). State the cost-center is ~10× a single box (10 lootbox materializations vs 1) and that the REWARD is FLAT per item (`:1568`), so a 10-spin all-match UNDER-reimburses the cranker per REW-03. Name the calibration-target distinction up front: the 10-spin all-match IS the GAS-01 worst case to MEASURE, but the `CRANK_RESOLVE_BET_GAS_UNITS` peg target is the per-1-spin-item MARGINAL (per A4 / REW-03 "bet pegs to per-spin gas, accepting big-win under-reimbursement"), NOT the worst case — this is the load-bearing distinction Plan 05 calibrates against.
    - OPEN-BOX worst case: one `_crankOpenBox` materialization = the same `_resolveLootboxCommon` body (targetLevel roll + EV-cap + nested BoonModule delegatecall + `_queueTickets` SSTORE + events). Flat reward per box. The per-box marginal calibrates `CRANK_OPEN_BOX_GAS_UNITS`.
    - SWEEP-PER-PLAYER worst case: the dominant per-player cost is the cross-contract calls (`hasAnyLazyPass`, `isOperatorApproved`, `claimableWinningsOf` x2 if reinvest+drainGameCreditFirst) + the per-player `batchPurchase._batchPurchaseUnit → _purchaseFor` slice (full mint→lootbox→prize-pool→EV-cap→quest), worst case a reinvest sub whose effective buy triggers multiple lootbox materializations. The per-successful-player marginal calibrates `BOUNTY_ETH_TARGET` (a DEPLOY-SCRIPT param / AfKing constructor immutable, NOT a frozen Game constant — per RESEARCH §GAS-06).
    Also add a JGAS-04 cross-reference section noting the 305-winner daily-ETH single-call worst case is already structurally derived (318-06 / 316-SPEC §J4.2 theory 9-12M; measured 7.5M) and that Plan 02 re-frames it worst-case-FIRST + attributes the ~1.3M RM-02 freed-SLOAD delta.
    Note that structural counts UNDER-count wall-clock (codegen overhead) so measured > structural is expected — the derivation is the lower-bound floor, the harness measures the actual.
    Use the `read_first` source line numbers as the authoritative cite set; do NOT invent line numbers. No fenced code in the action — the doc itself is prose + tables.
  </action>
  <verify>
    <automated>test -f .planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-GAS-DERIVATION.md && grep -q "MAX_SPINS_PER_BET" .planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-GAS-DERIVATION.md && grep -qE "CrankResolveBetWorstCaseGas" .planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-GAS-DERIVATION.md && grep -qiE "per-spin|per-1-spin|per-item marginal" .planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-GAS-DERIVATION.md</automated>
  </verify>
  <acceptance_criteria>
    - The doc has three work-type derivation sections (resolve-bet / open-box / sweep-per-player), each with the cost-center file:line chain, a structural SLOAD/SSTORE/delegatecall/loop count, the assert-is-worst-case precondition, and the named harness.
    - The resolve-bet section explicitly distinguishes the 10-spin all-match MEASUREMENT worst case from the per-1-spin-item peg CALIBRATION target (REW-03 / A4).
    - A JGAS-04 cross-reference section cites the 305-winner single-call worst case (7.5M measured, theory 9-12M) and the ~1.3M RM-02 freed-SLOAD delta to attribute.
    - Every cited line number matches the `read_first` source set (no invented citations).
  </acceptance_criteria>
  <done>319-GAS-DERIVATION.md exists with three derivation sections + JGAS-04 cross-reference; the worst-case framing precedes (and names) every Wave-2 measurement harness.</done>
</task>

<task type="auto">
  <name>Task 2: Capture the placement-hot-path .gas-snapshot baseline (GAS-06 +0% reference)</name>
  <read_first>
    - .planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-RESEARCH.md (§GAS-06 regression bound — "placement hot path +0%"; §Runtime State Inventory — ".gas-snapshot will gain new crank/sweep/jackpot entries (currently has NONE)")
    - .planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-PATTERNS.md (§Shared Patterns → ".gas-snapshot baseline for placement +0% regression")
    - foundry.toml (confirm forge config: viaIR, optimizer_runs=200, the inflated block_gas_limit)
    - .gas-snapshot (the existing 108-line snapshot — confirm it has no crank/afking/jackpot rows yet)
  </read_first>
  <action>
    Capture a `.gas-snapshot` baseline that includes the bet/box PLACEMENT hot path so Plan 05 (GAS-06) can later assert placement gas is +0% after the constant recalibration. The crank only relaxes RESOLVE; placement must be byte-for-byte unchanged. Run `forge snapshot` (or `forge snapshot --snap .gas-snapshot`) so the snapshot regenerates with current placement-path entries, capturing the deterministic placement-gas rows. Do NOT yet add the new Wave-2 gas suites (they do not exist until Plans 02/03) — this baseline is the placement reference only. If the existing `.gas-snapshot` already covers the placement entries deterministically, regenerate it cleanly so Plan 05 can diff against a known-good baseline. Commit the regenerated `.gas-snapshot`. Document in the SUMMARY the exact `forge snapshot` invocation + which placement-path test rows are the GAS-06 reference (so Plan 05 knows precisely what "+0%" is measured against). This is a test/artifact-only change — zero `contracts/*.sol` mutation.
    Constraint: if any placement-path test is in the 44-failure pre-existing baseline (318 notes — zero AfKing/crank involvement), exclude it from the snapshot reference and note why; the GAS-06 reference must be a green, deterministic placement subset.
  </action>
  <verify>
    <automated>test -f .gas-snapshot && git diff --name-only -- contracts/ | grep -q . && echo "CONTRACT DIRTY — FAIL" || echo "contracts clean OK"</automated>
  </verify>
  <acceptance_criteria>
    - `.gas-snapshot` exists and is regenerated via a documented `forge snapshot` invocation.
    - `git diff --name-only -- contracts/` is EMPTY (zero production-contract mutation).
    - The SUMMARY records the exact placement-path test rows that form the GAS-06 +0% reference set.
    - Any placement test that sits in the 44-failure pre-existing baseline is excluded from the reference set with a one-line reason.
  </acceptance_criteria>
  <done>.gas-snapshot regenerated with a documented placement-hot-path reference subset; contracts/ clean; the GAS-06 +0% baseline is captured for Plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning doc → downstream measurement | A wrong worst-case derivation propagates a wrong scenario into Plans 02/03, producing a misleading "fits 30M" / a mis-calibrated peg |
| .gas-snapshot baseline → GAS-06 calibration | A baseline that includes non-deterministic or failing placement rows makes the +0% claim unverifiable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-319-01 | Information disclosure (wrong-scenario) | 319-GAS-DERIVATION.md | mitigate | Derivation is source-traced (cost-center file:line chains from RESEARCH §Architecture Patterns, all verified at HEAD); the assert-is-worst-case precondition is written INTO the doc so Plan 02/03 harnesses enforce it before measuring |
| T-319-02 | Tampering (faucet-opening via mis-peg) | derivation → calibration target | mitigate | Doc locks the per-1-spin-item MARGINAL as the bet peg target (NOT the 10-spin worst case) per REW-03 — pegging to the worst case would over-reimburse and risk the SAFE-01 round-trip ≤ 0 faucet floor; Plan 05 calibrates against this |
| T-319-03 | Tampering | .gas-snapshot | accept | Snapshot is a test artifact; zero contract bytecode change. The only risk is a stale baseline, mitigated by regenerating cleanly and documenting the reference subset |
| T-319-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH §Package Legitimacy Audit = N/A); no install task exists |
</threat_model>

<verification>
- `319-GAS-DERIVATION.md` exists with three work-type derivation sections + JGAS-04 cross-reference, each source-traced.
- `.gas-snapshot` regenerated; `git diff --name-only -- contracts/` empty.
- The worst-case framing (derive-first) is committed BEFORE any Wave-2 measurement runs (`feedback_gas_worst_case`).
</verification>

<success_criteria>
- GAS-01 derive-before-measure deliverable exists and constrains the Wave-2 harnesses (each named in the doc with its assert-is-worst-case precondition).
- GAS-06 placement +0% baseline captured for Plan 05.
- Zero `contracts/*.sol` mutation.
</success_criteria>

<output>
Create `.planning/phases/319-gas-worst-case-first-gas-pass-0-5-gwei-peg-calibration-gas/319-01-SUMMARY.md` when done.
</output>
