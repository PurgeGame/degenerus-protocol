---
phase: 329-spec-design-lock-call-graph-attestation-4-structural-invaria
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md
autonomous: true
requirements: [BATCH-01, ROUTER-07, ADV-04, GAS-03]
must_haves:
  truths:
    - "Every cited file:line on the router/advance surface (AfKing CEI :99-106 / BOUNTY_ETH_TARGET :263 / bounty+creditFlip :845-846 / _currentDay :886-889 / stall ladder :823-838 / cursor :577 / claimableWinningsOf hoist :691+:722; AdvanceModule advanceGame :155 / 3 creditFlip sites :189/:225/:468 / ADVANCE_BOUNTY_ETH :147 / stall+epoch :238-255 / totalFlipReversals nudge :1834-1844 / 30-min bypass ~:1012 / death-clock :109+:1198; DegenerusGame advanceGame wrapper :275 / currentDayView :462 / gas-peg :1536-1546; GASOPT-01 MintModule :393+:670) has a grep-verified MATCH / SHIFTED(±N) / ABSENT verdict resolved at HEAD 0cc5d10f"
    - "ADV-04 (invariant b): the totalFlipReversals nudge (:1838-1844) is grep-attested as the player-controllable VRF-window variable; the SPEC-relevant fact recorded — it is consumed-and-reset inside _applyDailyRng, so the freeze window the v45 invariant protects is request→consume; no NEW mutable SLOAD is introduced by the router (router consumes via the design-1 return, recomputes nothing)"
    - "ROUTER-07 (D-01/D-01a): the no-untrusted-ETH-send claim is grep-attested PER LEG (advance / autoOpen / _autoBuy) at 0cc5d10f — each leg routes player value through the claimableWinnings pull ledger and sends ETH only to ContractAddresses.* pinned protocol contracts; the keeper-never-a-payee + creditFlip-last CEI ordering is confirmed (AfKing :99-106 + :845-846), so the no-guard disposition rests on a checked fact"
    - "GAS-03 (D-03/D-03a, invariant d): BOTH day-start epoch formulas are grep-attested — AfKing absolute-day (today*1days+82620, :829) vs AdvanceModule game-day ((day-1+DEPLOY_DAY_BOUNDARY)*1days+82620, :243-246) — with the verdict that they intentionally measure different things and design-1 single-sources the advance multiplier so they need NOT be physically merged"
    - "Invariant (c) (D-04): the existing structural free-fallback advanceGame() caller paths are grep-attested — the 30-min universal bypass (:1012 unconditional return), DegenerusVault.gameAdvance() :527-528, StakedDegenerusStonk.gameAdvance() :421-422, and the 120-day death-clock :109/:1198 — confirming re-homing the bounty adds NO single-point liveness risk"
    - "The Claude's-Discretion attestations are resolved by grep: the advanceGame return-encoding decode site (:275 wrapper), each of the 3 creditFlip sites classified (rewardable advance-leg work incl. the ADV-05 mid-day :225 vs not-router-rewardable), the 3 O(1) discovery-view predicates (advanceDue new-day+partial-drain / boxesPending / buys-pending via AfKing-local cursor), maxCount per-leg semantics, KEEP-04 affiliate-code passthrough survival, and the GASOPT-01/02 loop-invariant hoist sites"
    - "D-06 baseline (maxCount==0 fixed gas-budget-sized default): the CURRENT count-handling sites are grep-attested — autoBuy(0) reverts EmptyAutoBuy (AfKing.sol:569), autoOpen(0) no-ops (the DegenerusGame.sol:1656 loop `while (cursor < qlen && opened < maxCount)` never enters), advanceGame takes no count arg — and confirmed that NO existing fixed-default / gasleft-bounded-loop pattern exists on these legs (so D-06 is a NEW IMPL behavior the SPEC locks for Phase 330)"
  artifacts:
    - path: ".planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md"
      provides: "Per-anchor grep tables for the router (AfKing) + advance (AdvanceModule + DegenerusGame wrapper) surface, the per-leg no-untrusted-ETH-send attestation (D-01a), the dual-epoch attestation (D-03), the invariant-(c) fallback-caller attestation (D-04), the totalFlipReversals freeze attestation (ADV-04), and the GASOPT-01/02 hoist-site confirmation"
      contains: "Verdict legend"
  key_links:
    - from: "the SUMMARY + CONTEXT + ROADMAP cited file:line anchors for the router/advance surface"
      to: "contracts/ source at HEAD 0cc5d10f"
      via: "git show 0cc5d10f:contracts/... | grep -n + live-tree grep (tree == baseline, zero drift verified)"
      pattern: "MATCH|SHIFTED|ABSENT"
    - from: "each doWork leg (advance / autoOpen / _autoBuy)"
      to: "claimableWinnings pull ledger + ContractAddresses.* pinned sends only"
      via: "per-leg no-untrusted-ETH-send grep attestation (D-01a)"
      pattern: "claimableWinnings|ContractAddresses\\.|creditFlip"
---

<objective>
Grep-attest every cited `file:line` anchor on the v49.0 **router + advance-bounty** surface against
the v48.0-closure HEAD `0cc5d10fbc1232a6d2e7b0464fe21541b9812029` (`0cc5d10f`), recording per-anchor
MATCH / SHIFTED(±N) / ABSENT verdicts in `329-ATTEST-ROUTER-ADVANCE.md`. Resolve the
Claude's-Discretion grep facts on this surface (the `advanceGame` return decode site, the 3
`creditFlip` site classifications, the 3 O(1) discovery-view predicates, `maxCount` per-leg
semantics, the KEEP-04 affiliate-code passthrough survival, the GASOPT-01/02 hoist sites) and the
three load-bearing decision attestations:
- **ROUTER-07 / D-01a** — the per-leg "no untrusted ETH send" attestation underpinning the no-guard
  disposition (keeper-never-a-payee + pull-pattern winnings + `creditFlip`-last CEI).
- **GAS-03 / D-03** — both day-start epoch formulas grep-attested as intentionally distinct, so
  design-1 single-sources the advance multiplier (no physical merge).
- **ADV-04 (invariant b)** — the `totalFlipReversals` VRF-window nudge frozen between rng-request and
  consume; no new mutable SLOAD introduced by the router composition.
- **Invariant (c) / D-04** — the existing structural free-fallback `advanceGame()` caller paths.

Purpose: produce the load-bearing call-graph evidence so Plan 03's reconciliation + Phase 330's
batched diff carry zero un-grepped "by construction" assumptions (the `DegenerusGame` mint/jackpot
inline-duplication precedent — `feedback_verify_call_graph_against_source`). This is the BATCH-01
attestation half for the core router/advance surface.

Output: one `329-ATTEST-ROUTER-ADVANCE.md` under the phase directory. ZERO `contracts/*.sol` mutation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-CONTEXT.md

# The locked milestone design (the binding spec — no research sub-phase):
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/PLAN-CRANK-DO-WORK-INCENTIVE.md

# The v48 precedent ATTEST format to mirror (verdict legend + per-anchor reconciliation table + exact-body excerpt):
@.planning/milestones/v48.0-phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-KEEP-POOL.md

<attestation_method>
The current working tree's `contracts/` is byte-identical to the baseline HEAD
`0cc5d10fbc1232a6d2e7b0464fe21541b9812029` (verified during planning: `git diff --name-only 0cc5d10f
HEAD -- contracts/` returns ZERO files). So grep the LIVE `contracts/` tree directly; each verdict is
implicitly resolved at the baseline. For belt-and-suspenders on any anchor,
`git show 0cc5d10f:contracts/<path> | grep -n`. Read source from `contracts/` ONLY — stale copies
elsewhere must be ignored (`feedback_contract_locations`). The doc header MUST state this
byte-identical-to-0cc5d10f fact so the verdicts are baseline-anchored.

Verdict legend (mirror 325-ATTEST): `MATCH` (anchor lands on the claimed line) · `SHIFTED(±N)`
(content present, N lines off — record the actual line) · `ABSENT` (content not found / materially
diverged — flag as an IMPL blocker in the Roll-up).
</attestation_method>

<interfaces>
<!-- Grep-confirmed anchors at the baseline 0cc5d10f (verified during planning). Executor extends, does not re-discover from scratch. -->
AfKing.sol (the router's home):
  :99-106  CEI invariant comment block ("No reentrancy guard — strict CEI everywhere; the keeper is never a payee in any contract it calls" + caller-scoped writes + un-spoofable VAULT/SDGNRS identity)
  :263     `uint256 public immutable BOUNTY_ETH_TARGET;` (peg ETH target — re-pegged at GAS, placeholder at SPEC)
  :304     `function depositFor(address player) external payable` (permissionless ingress — the Pitfall-5 set-inflation surface)
  :318     `function withdraw(uint256 amount) external` (pool withdraw)
  :567     `function autoBuy(uint256 maxCount) external returns (uint256 bountyEarned)` (refactored to internal `_autoBuy` at IMPL); :569 `if (maxCount == 0) revert EmptyAutoBuy();`
  :577     `uint256 cursor = _autoBuyDay == today ? uint256(_autoBuyCursor) : 0;` (the resuming daily-reset cursor — buys-pending discovery reads this AfKing-local state)
  :691 + :722  two `IGame(ContractAddresses.GAME).claimableWinningsOf(player)` calls inside the autoBuy loop (GASOPT-02 hoist sites — laziness: only when `reinvestPct>0 || FLAG_DRAIN_FIRST`)
  :823-838 stall ladder block — `dayStart = today * 1 days + 82_620` (:829), elapsed bands 2h→6x / 1h→4x / 20m→2x (ABSOLUTE-day epoch, autoBuy's own multiplier)
  :845     `bountyEarned = batchLen * ((BOUNTY_ETH_TARGET * PRICE_COIN_UNIT * bountyMultiplier) / mp);`
  :846     `ICoinflip(ContractAddresses.COINFLIP).creditFlip(msg.sender, bountyEarned);` (the ONE creditFlip per tx, fired LAST — CEI)
  :886-889 `function _currentDay() internal view returns (uint32) { return uint32((block.timestamp - 82620) / 1 days); }`
  errors: `EmptyAutoBuy()` :143, `NoSubscribersAutoBought()` :146 (the ROUTER-06 `NoWork()` revert-idiom precedent)
  affiliate: AfKing's autoBuy passes an affiliate code into `IGame.batchPurchase(...)` (the KEEP-04 v48 two-tier 75/20/5 passthrough survival to re-confirm — grep the batchPurchase call site + the code arg)

DegenerusGameAdvanceModule.sol (the advance multiplier single-source-of-truth):
  :109     `DEPLOY_IDLE_TIMEOUT_DAYS = 365` (L0); comment "level 1+ uses hardcoded 120 days" (the tertiary death-clock backstop, invariant c)
  :147     `uint256 private constant ADVANCE_BOUNTY_ETH = 0.005 ether;` (deleted at IMPL per ADV-01)
  :155     `function advanceGame() external` (gains design-1 return `(uint8 mult, bool rewardable)` at IMPL)
  :189     creditFlip site #1 (new-day advance bounty) — `(ADVANCE_BOUNTY_ETH * PRICE_COIN_UNIT) / ...` :191
  :225     creditFlip site #2 (MID-DAY partial-drain bounty, ADV-05 rewardable advance-leg work) — :227
  :238-255 stall multiplier block — `dayStart = (uint256(day-1) + ContractAddresses.DEPLOY_DAY_BOUNDARY) * 1 days + 82_620` (:243-246, GAME-DAY epoch), elapsed bands 2h→6x / 1h→4x / 20m→2x
  :468     creditFlip site #3 — `(ADVANCE_BOUNTY_ETH * PRICE_COIN_UNIT * bountyMultiplier) / ...` :470
  :989     `function _enforceDailyMintGate(` ; :1012 `if (elapsed >= 30 minutes) return;` (the UNCONDITIONAL 30-min universal bypass — invariant c; CONTEXT cited ~:1008, this is SHIFTED +4)
  :1198    death-clock extend-by-stall-duration (invariant c tertiary)
  :1834    `function _applyDailyRng(` ; :1838 `uint256 nudges = totalFlipReversals;` ; :1844 `totalFlipReversals = 0;` (the ADV-04 player-controllable VRF-window nudge — consumed+reset inside the consume path)

DegenerusGame.sol:
  :275     `function advanceGame() external` wrapper — delegatecalls GAME_ADVANCE_MODULE.advanceGame.selector, `if (!ok) _revertDelegate(data);` (gains decode of the design-1 `(uint8,bool)` return + new signature at IMPL)
  :462     `function currentDayView() external view returns (uint32)` (the advanceDue() new-day predicate basis: currentDayView() != dailyIdx)
  :1536-1546 gas-peg constants — `AUTO_GAS_PRICE_REF = 0.5 gwei` :1539, `AUTO_RESOLVE_BET_GAS_UNITS = 66_528` :1545, `AUTO_OPEN_BOX_GAS_UNITS = 71_203` :1546 (placeholders; calibrated at GAS)
  :1636    `function autoOpen(uint256 maxCount) external` (the open leg — already-permissionless game entrypoint the router calls externally; pays its own in-callee bounty); :1656 `while (cursor < qlen && opened < maxCount)` — autoOpen(0) no-ops (the D-06 baseline: no fixed-default count today)
  :2413    `function rngLocked() external view returns (bool)` (the freeze gate)
  boxesPending discovery basis: grep `boxPlayers` / `boxCursor` / `lootboxRngWordByIndex[activeIndex]` (the O(1) predicate `boxPlayers[activeIndex].length > boxCursor AND lootboxRngWordByIndex[activeIndex] != 0`)

Invariant-(c) fallback wrappers (read-only — confirm, do NOT modify):
  DegenerusVault.sol:527 `function gameAdvance() external onlyVaultOwner` → :528 `gamePlayer.advanceGame();`
  StakedDegenerusStonk.sol:421 `function gameAdvance() external` (permissionless) → :422 `game.advanceGame();`

DegenerusGameMintModule.sol (GASOPT-01 hoist sites):
  :393     `function processFutureTicketBatch(` — `ticketsOwedPacked[rk][player]` repeated reads (:431/:441/:453/:463/:498) — `rk` loop-invariant (CONTEXT cited :398, SHIFTED)
  :670     `function processTicketBatch(uint24 lvl) external returns (bool finished)` — `ticketsOwedPacked[rk][player]` repeated (:741/:748/:754) — `rk` loop-invariant (CONTEXT cited :671, SHIFTED −1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attest AfKing router anchors + the per-leg no-untrusted-ETH-send (D-01a) + the dual-epoch (D-03) + GASOPT hoist sites → 329-ATTEST-ROUTER-ADVANCE.md sections A/B/C</name>
  <read_first>
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-01-PLAN.md (this plan, the `<interfaces>` block + attestation_method)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-CONTEXT.md (D-01/D-01a no-guard disposition; D-03/D-03a dual-epoch; the Claude's-Discretion attestation list)
    - .planning/milestones/v48.0-phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-KEEP-POOL.md (the table + exact-body-excerpt + Roll-up format to mirror)
    - .planning/research/PITFALLS.md (Pitfall 6 reentrancy / OPEN-C; Pitfall 7 dual day-start epoch; Pitfall 1 faucet locks)
    - contracts/AfKing.sol (CEI :99-106; BOUNTY_ETH_TARGET :263; depositFor :304; withdraw :318; autoBuy :567 + cursor :577; claimableWinningsOf :691/:722; stall ladder + absolute-day epoch :823-838; bounty+creditFlip :845-846; _currentDay :886-889; EmptyAutoBuy/NoSubscribersAutoBought :143/:146; the batchPurchase affiliate-code arg)
    - contracts/modules/DegenerusGameAdvanceModule.sol (the game-day epoch :238-255 — for the D-03 dual-epoch comparison row only)
    - contracts/DegenerusGame.sol (autoOpen :1636 — for the D-01a autoOpen-leg send-path attestation)
    - contracts/modules/DegenerusGameMintModule.sol (GASOPT-01: processFutureTicketBatch :393 + processTicketBatch :670 + the repeated ticketsOwedPacked[rk][player] reads)
  </read_first>
  <action>Create 329-ATTEST-ROUTER-ADVANCE.md mirroring 325-ATTEST-KEEP-POOL.md: a `## Scope` + `## Sources of truth` header naming the exact contracts/ files + the byte-identical-to-0cc5d10f attestation-method note, a `## Verdict legend`, then the following sections (each a `## Anchor reconciliation table` with columns `# | Anchor (claimed) | ACTUAL (contracts/<file>) | Verdict`).
Section A — AfKing router anchors: grep-verify each AfKing anchor in the `<interfaces>` block (CEI :99-106, BOUNTY_ETH_TARGET :263, depositFor :304, withdraw :318, autoBuy :567 + cursor :577, the two claimableWinningsOf calls :691/:722, the stall ladder :823-838, bounty :845 + creditFlip :846, _currentDay :886-889, the EmptyAutoBuy/NoSubscribersAutoBought reverts :143/:146) and the batchPurchase affiliate-code arg site (KEEP-04 passthrough survival — confirm the autoBuy → IGame.batchPurchase call still carries the v48 two-tier 75/20/5 registered-code arg, give an explicit survives-the-_autoBuy-refactor verdict). Record every divergence as SHIFTED(±N) with the actual line.
Section B — ROUTER-07 / D-01a per-leg no-untrusted-ETH-send: a table with ONE row per doWork leg (advance / autoOpen / _autoBuy) stating, grep-shown, that the leg sends ETH only to ContractAddresses.* pinned protocol contracts and routes player value through the claimableWinnings pull ledger (NOT a synchronous push to msg.sender / an untrusted address), plus a row confirming the bounty pays as creditFlip flip-credit to the player (keeper-never-a-payee) and fires LAST (CEI, AfKing :846). State the formal no-guard basis verbatim: *keeper-never-a-payee + no untrusted ETH send + one-category structural early-return + creditFlip-last CEI ordering*. If ANY leg is found to push ETH to an untrusted address, flag it as a ROUTER-07 BLOCKER (the no-guard disposition would not hold).
Section C — GAS-03 / D-03 dual-epoch + GASOPT-01 hoist: a row attesting AfKing's absolute-day epoch (`today * 1 days + 82_620`, :829) and a row attesting AdvanceModule's game-day epoch (`(day-1 + DEPLOY_DAY_BOUNDARY) * 1 days + 82_620`, :243-246), with an explicit verdict that they intentionally measure different things (per-day buying window vs lagging-game-day advance liveness) so design-1 single-sources the advance multiplier and they need NOT be physically merged (D-03a). Then two GASOPT-01 rows: processFutureTicketBatch (:393, `rk` loop-invariant, multiple ticketsOwedPacked[rk][player] reads) + processTicketBatch (:670, same) — confirm the storage-pointer hoist is behavior-identical (gas-only) and note the CONTEXT line drift. NO contracts/*.sol edit.</action>
  <verify>
    <automated>bash -c 'F=.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md; test -f "$F" && grep -q "Verdict legend" "$F" && grep -Eqi "MATCH|SHIFTED|ABSENT" "$F" && grep -q "0cc5d10f" "$F" && grep -q "creditFlip" "$F" && grep -q "claimableWinnings" "$F" && grep -Eq "82.?620" "$F" && grep -q "DEPLOY_DAY_BOUNDARY" "$F" && grep -q "ticketsOwedPacked" "$F"'</automated>
  </verify>
  <acceptance_criteria>
    - 329-ATTEST-ROUTER-ADVANCE.md exists with a Verdict legend (MATCH/SHIFTED/ABSENT) and the byte-identical-to-0cc5d10f header note.
    - Section A has a grep-verified verdict row for every AfKing anchor in the interfaces block (CEI :99-106, BOUNTY_ETH_TARGET :263, depositFor :304, withdraw :318, autoBuy :567 + cursor :577, claimableWinningsOf :691/:722, stall ladder :823-838, bounty+creditFlip :845-846, _currentDay :886-889, the two anti-spam reverts) plus an explicit KEEP-04 affiliate-code-survives-the-_autoBuy-refactor verdict.
    - Section B has one no-untrusted-ETH-send row PER doWork leg (advance / autoOpen / _autoBuy), each grep-shown to send ETH only to ContractAddresses.* + route player value through claimableWinnings; the keeper-never-a-payee + creditFlip-last row is present; the formal no-guard basis sentence is recorded verbatim; any untrusted-push leg is flagged a ROUTER-07 blocker.
    - Section C attests BOTH epoch formulas with the intentionally-distinct verdict (no physical merge, D-03a) AND the two GASOPT-01 hoist rows (processFutureTicketBatch :393 + processTicketBatch :670, gas-only behavior-identical, CONTEXT drift noted).
    - No contracts/*.sol modified.
  </acceptance_criteria>
  <done>329-ATTEST-ROUTER-ADVANCE.md sections A/B/C committed: every AfKing anchor grep-verified, the per-leg no-untrusted-ETH-send (D-01a) attested as the no-guard basis, the dual-epoch (D-03) attested intentionally-distinct, GASOPT-01 hoist confirmed; zero .sol mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Attest AdvanceModule + DegenerusGame-wrapper anchors + the 3 creditFlip classifications + the discovery views + the totalFlipReversals freeze (ADV-04) + the invariant-(c) fallback callers (D-04) → 329-ATTEST-ROUTER-ADVANCE.md sections D/E/F/G</name>
  <read_first>
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-01-PLAN.md (this plan, the `<interfaces>` block)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-CONTEXT.md (ADV-04 invariant b; D-04 invariant c free-fallback hierarchy; the advanceGame return-encoding + 3-creditFlip-classification + discovery-view + maxCount Discretion items)
    - .planning/research/PITFALLS.md (Pitfall 3 advance-timing/VRF-freeze + the totalFlipReversals nudge; Pitfall 8 unrewarded-advance liveness + the free-fallback hierarchy; Pitfall 5 O(1) discovery / no unbounded scans)
    - $HOME/.claude/get-shit-done/references/... (none — skip)
    - contracts/modules/DegenerusGameAdvanceModule.sol (advanceGame :155; ADVANCE_BOUNTY_ETH :147; the 3 creditFlip sites :189 new-day / :225 mid-day partial-drain / :468 + their bounty math :191/:227/:470; stall+epoch :238-255; _enforceDailyMintGate :989 + the 30-min bypass :1012; death-clock :109 + :1198; _applyDailyRng :1834 + the totalFlipReversals nudge :1838 + reset :1844 — backward-trace per feedback_rng_backward_trace)
    - contracts/DegenerusGame.sol (advanceGame wrapper :275 + _revertDelegate; currentDayView :462; rngLocked :2413; autoOpen :1636; the boxPlayers / boxCursor / lootboxRngWordByIndex[activeIndex] discovery state for boxesPending)
    - contracts/DegenerusVault.sol (gameAdvance :527-528 onlyVaultOwner — read-only)
    - contracts/StakedDegenerusStonk.sol (gameAdvance :421-422 permissionless — read-only)
  </read_first>
  <action>Append sections D, E, F, G to 329-ATTEST-ROUTER-ADVANCE.md (same legend + table format).
Section D — AdvanceModule + DegenerusGame-wrapper anchors + the 3 creditFlip classifications: grep-verify advanceGame :155, ADVANCE_BOUNTY_ETH :147, the stall+epoch block :238-255, and the DegenerusGame advanceGame wrapper :275 (the delegatecall + `if (!ok) _revertDelegate(data)` — note it currently returns NOTHING; the design-1 `(uint8 mult, bool rewardable)` return + the decode of `data` is the IMPL producer edit, record the exact decode site). Then a classification sub-table with ONE row per creditFlip site: :189 (new-day advance — REWARDABLE advance-leg, the rewardable flag must cover it), :225 (MID-DAY partial-drain — REWARDABLE advance-leg per ADV-05, the rewardable flag must cover it), :468 (classify: which condition it fires under — REWARDABLE vs not-router-rewardable). Confirm deletion of all 3 leaves advanceGame fully functional + unrewarded standalone (ADV-01/ADV-03). Settle the design-1 return encoding question: confirm `rewardable` is a DISTINCT bool (a leg can do work but be non-rewardable, and `mult` can be ≥1 independent of rewardability) — give the explicit tuple verdict.
Section E — ADV-04 (invariant b) totalFlipReversals freeze: grep-show _applyDailyRng :1834 reads `totalFlipReversals` (:1838) into the consumed VRF word and resets it (:1844) — record that this is the player-controllable in-window nudge the v45 freeze invariant protects, the protected window is rng-request→consume, and (the SPEC-relevant fact) the router consumes the daily tick via the design-1 RETURN and recomputes NOTHING, so it introduces NO new mutable SLOAD into the rng window. State that the empirical freeze proof is TST-01's job (extend RngLockDeterminism); this attestation only confirms the variable + window exist at 0cc5d10f and the router adds no new in-window read. Backward-trace note per feedback_rng_window_storage_read_freshness: enumerate any OTHER SLOAD read alongside the word in the consume path and confirm none is newly router-mutable.
Section F — ROUTER-04 O(1) discovery views: a table with one row per predicate — advanceDue() (covers BOTH new-day `currentDayView() != dailyIdx` :462 AND mid-day partial-drain `day == dailyIdx but tickets-not-fully-processed`; grep the dailyIdx/processed state), boxesPending() (`boxPlayers[activeIndex].length > boxCursor AND lootboxRngWordByIndex[activeIndex] != 0`), buys-pending (AfKing-local cursor reads :577) — each confirmed O(1) (no unbounded scan over a growable set, Pitfall 5). Settle WHERE each view lives (advance/boxes on DegenerusGame, buys on AfKing-local) and the maxCount-per-leg mapping (advance / autoOpen(maxCount) / _autoBuy(maxCount)) as explicit Discretion-resolution rows. Add a D-06 baseline row: grep-attest the CURRENT count==0 handling — autoBuy(0) reverts EmptyAutoBuy (AfKing.sol:569), autoOpen(0) no-ops (the DegenerusGame.sol:1656 loop never enters), advanceGame has no count arg — and confirm NO existing fixed-default / gasleft-bounded-loop pattern on these legs, so the SPEC's locked D-06 (maxCount==0 → a fixed `≈10M/avg` default count, NOT a gasleft loop) is a NEW Phase-330 IMPL behavior with a clean baseline.
Section G — invariant (c) / D-04 free-fallback callers: a table with one row per structurally-guaranteed unrewarded advanceGame() caller path — the 30-min universal bypass (_enforceDailyMintGate :989 + the UNCONDITIONAL `if (elapsed >= 30 minutes) return;` :1012, note CONTEXT ~:1008 SHIFTED +4), DegenerusVault.gameAdvance() :527-528 (onlyVaultOwner, DGVE-majority always-bypass), StakedDegenerusStonk.gameAdvance() :421-422 (permissionless wrapper), and the 120-day death-clock (:109 + extend :1198) as the tertiary failsafe — each with the verdict that re-homing the bounty into doWork reduces NO structural caller (it only moves the payment). End the whole doc with a `## Roll-up` line stating: the total IMPL-blocker count across all sections, the ROUTER-07 no-guard-basis-holds verdict, the GAS-03 epochs-intentionally-distinct verdict, the ADV-04 no-new-in-window-read verdict, and the invariant-(c) fallback-callers-intact verdict. NO contracts/*.sol edit.</action>
  <verify>
    <automated>bash -c 'F=.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md; test -f "$F" && grep -q "totalFlipReversals" "$F" && grep -q ":189" "$F" && grep -q ":225" "$F" && grep -q ":468" "$F" && grep -Eqi "advanceDue|boxesPending|buys.pending" "$F" && grep -q "gameAdvance" "$F" && grep -Eqi "30 minutes|death.clock|120" "$F" && grep -Eqi "rewardable" "$F" && grep -q "Roll-up" "$F"'</automated>
  </verify>
  <acceptance_criteria>
    - Section D attests advanceGame :155, ADVANCE_BOUNTY_ETH :147, stall+epoch :238-255, the DegenerusGame :275 wrapper decode site, AND a per-site classification of all 3 creditFlip sites (:189 new-day rewardable / :225 mid-day partial-drain rewardable per ADV-05 / :468 classified) with the all-3-deleted-leaves-advanceGame-functional-unrewarded verdict; the design-1 `(uint8 mult, bool rewardable)` distinct-bool tuple verdict is recorded.
    - Section E grep-shows totalFlipReversals read (:1838) + reset (:1844) inside _applyDailyRng (:1834), records the request→consume protected window, and states the router consumes via the design-1 return and introduces NO new mutable in-window SLOAD; the empirical proof is handed to TST-01.
    - Section F has an O(1) verdict row per discovery predicate (advanceDue covering BOTH new-day + partial-drain, boxesPending, buys-pending via AfKing cursor), the per-view location, the maxCount-per-leg mapping — all no-unbounded-scan — AND the D-06 baseline row (autoBuy(0) revert :569 / autoOpen(0) no-op :1656 / advanceGame no-count, no existing fixed-default-or-gasleft pattern).
    - Section G has a verdict row per free-fallback caller (30-min bypass :1012, Vault gameAdvance :527-528, sStonk gameAdvance :421-422, 120-day death-clock :109/:1198) confirming re-homing removes no structural caller.
    - The final Roll-up states the blocker count + the ROUTER-07 / GAS-03 / ADV-04 / invariant-(c) verdicts.
    - No contracts/*.sol modified.
  </acceptance_criteria>
  <done>329-ATTEST-ROUTER-ADVANCE.md sections D/E/F/G + Roll-up committed: advance anchors + 3-creditFlip classification + design-1 return + ADV-04 freeze + O(1) discovery views + invariant-(c) fallback callers all grep-attested; zero .sol mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a paper-only attestation plan — ZERO code mutation this phase; the mitigations below are
LOCKED in the ATTEST evidence for the IMPL (Phase 330) diff to apply, and proven empirically at TST
(332) / TERMINAL (333). The "trust boundary" here is between the milestone docs' *claimed*
`file:line` anchors + design assumptions and the *actual* baseline source — an under-attested anchor
or an un-grepped "by construction" claim lets a stale reference or a false security disposition reach
Phase 330's batched diff.

| Boundary | Description |
|----------|-------------|
| SUMMARY/CONTEXT/ROADMAP anchor → contracts/ source @ 0cc5d10f | Drafted against an earlier read; must be grep-reconciled before the IMPL diff trusts it |
| each doWork leg → ETH send target (pinned protocol vs untrusted address) | The ROUTER-07 no-guard disposition (D-01) rests on "no untrusted ETH send" being a checked fact, not an assumption |
| router advance-consume → the VRF-window frozen state | A new mutable in-window SLOAD introduced by the router composition would break the v45 freeze invariant (ADV-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-329-01 | Tampering | A cited router/advance anchor drifted/was deleted post-v48 and the attestation rubber-stamps it | mitigate | Every anchor verdict is grep-derived from the live tree (== 0cc5d10f, verified zero-drift); ABSENT anchors flagged as IMPL blockers in the Roll-up, never silently passed |
| T-329-02 | Spoofing / Elevation (false security disposition) | ROUTER-07 no-guard (D-01) is recorded without per-leg grep proof, masking an untrusted ETH-send leg that would make the no-guard composition reentrant (Pitfall 6) | mitigate | Section B emits one no-untrusted-ETH-send row PER leg (advance/autoOpen/_autoBuy); any untrusted-push leg is flagged a ROUTER-07 blocker — the no-guard disposition rests on a checked fact + the empirical TST-02 double-pay regression backstop (D-01b) |
| T-329-03 | Tampering (RNG) | The router composition introduces a new mutable in-window SLOAD into the advance-consume → totalFlipReversals/freeze break (Pitfall 3, ADV-04, the v45 freeze floor) | mitigate | Section E backward-traces the consume path, confirms the router consumes via the design-1 return (recomputes nothing) and adds no new in-window read; the empirical freeze fuzz is locked to TST-01 |
| T-329-04 | Information disclosure (false-negative) | A "single fn reaches all paths" / "by construction" claim survives un-grepped (the DegenerusGame mint/jackpot inline-duplication precedent) | mitigate | Every design claim is decomposed to concrete anchors + grepped; the 3-creditFlip-site classification + the dual-epoch attestation + the invariant-(c) fallback enumeration are explicit verdict rows |
| T-329-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this paper-only plan; no install task exists. RESEARCH Package Legitimacy Gate N/A (no dependency adds) |
</threat_model>

<verification>
- 329-ATTEST-ROUTER-ADVANCE.md exists with a Verdict legend and the byte-identical-to-0cc5d10f header note.
- Every router/advance anchor cited in the interfaces block has a verdict row (MATCH/SHIFTED(±N)/ABSENT); ABSENT rows are surfaced as blockers in the Roll-up.
- ROUTER-07 (D-01a per-leg no-untrusted-ETH-send), GAS-03 (D-03 dual-epoch), ADV-04 (totalFlipReversals freeze + no new in-window read), and invariant (c) (D-04 free-fallback callers) are each resolved with explicit grep-derived verdict rows.
- The 3 creditFlip sites are classified; the design-1 return tuple + the O(1) discovery views + maxCount-per-leg + KEEP-04 affiliate survival + GASOPT-01/02 hoist are resolved.
- Zero contracts/*.sol files modified: `git diff --name-only -- 'contracts/*.sol'` is empty.
</verification>

<success_criteria>
- The router + advance surface anchor attestation is complete with grep-derived verdicts (BATCH-01 attestation half).
- ROUTER-07 / D-01a, GAS-03 / D-03, ADV-04, invariant-(c) / D-04, and the Claude's-Discretion grep facts (return encoding, 3 creditFlip classifications, discovery views, maxCount, KEEP-04, GASOPT) are resolved in writing.
- No "by construction" claim left un-grepped for this surface.
- ZERO contracts/*.sol mutation (paper-only phase).
</success_criteria>

<output>
Create `.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-01-SUMMARY.md` when done, listing the ATTEST doc, the aggregate blocker count, and the ROUTER-07 / GAS-03 / ADV-04 / invariant-(c) verdicts + the design-1-return / 3-creditFlip-classification / discovery-view / KEEP-04 / GASOPT resolutions for Plan 03 to consume.
</output>
</content>
</invoke>
