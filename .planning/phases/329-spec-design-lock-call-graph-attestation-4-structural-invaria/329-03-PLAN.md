---
phase: 329-spec-design-lock-call-graph-attestation-4-structural-invaria
plan: 03
type: execute
wave: 2
depends_on: ["329-01", "329-02"]
files_modified:
  - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md
autonomous: true
requirements: [BATCH-01, ROUTER-07, ADV-04, GAS-03]
must_haves:
  truths:
    - "329-SPEC.md §2 LOCKS the 4 structural invariants in writing: (a) one-category structural early-return on doWork (advance→autoOpen→autoBuy, return after the first rewarded category — a code invariant not a comment, BATCH-01); (b) frozen advance-consume re-attested under the new router composition incl. the totalFlipReversals nudge (AdvanceModule ~:1838-1844), the router consuming via the design-1 return and adding no new in-window SLOAD (ADV-04 / v45-vrf-freeze-invariant); (c) guaranteed free-fallback advanceGame() caller from EXISTING structural paths only (30-min bypass + Vault/sDGNRS wrappers + 120-day death-clock — D-04, NO new mechanism); (d) single day-start epoch satisfied by design-1 (GAS-03 / D-03 — the advance multiplier returned, never recomputed; the two intentionally-distinct epoch formulas NOT physically merged)"
    - "329-SPEC.md §1 settles the shared signatures so no downstream file ships a broken intermediate: the advanceGame return shape (design-1 (uint8 mult, bool rewardable)) + its decode in the DegenerusGame :275 wrapper, doWork(maxCount) + the ROUTER-06 NoWork() revert signal (D-02), and the O(1) discovery views (advanceDue() covering BOTH new-day AND mid-day partial-drain, boxesPending(), buys-pending via AfKing-local cursor — ROUTER-04, no unbounded scans), each with the per-leg maxCount semantics"
    - "329-SPEC.md §2 RESOLVES the two flagged dispositions: ROUTER-07 reentrancy = NO nonReentrant guard (D-01) on the grep-checked per-leg no-untrusted-ETH-send basis (D-01a: keeper-never-a-payee + claimableWinnings pull + ContractAddresses.* sends + creditFlip-last CEI + one-category early-return) with the empirical TST-02 double-pay backstop scoped to PROVE the no-guard disposition (D-01b); and GAS-03 single-epoch = design-1-satisfies, the two epochs documented as intentionally-distinct (D-03/D-03a)"
    - "329-SPEC.md folds the two ATTEST docs' blocker roll-ups + verdicts into §0 (attestation roll-up: total blocker count, carried line-drift corrections vs CONTEXT/SUMMARY, the ROUTER-07/GAS-03/ADV-04/invariant-(c) verdicts, the D-05f losing-bet-liveness finding) and §3 (per-item IMPL blueprint + the producer-before-consumer edit-order map: AdvanceModule bounty-removal + design-1 return → Game wrapper/views → interfaces → AfKing router/_autoBuy/re-peg-placeholders/micro-opts)"
    - "329-SPEC.md LOCKS the D-05 degeneretteResolve design item (rename + flat ~1-BURNIE/≥3-NON-WWXRP-gate/revert-on-no-work re-peg + the D-05c real-gas basis + the D-05f finding + router-fold-OUT) as a v49.0 design item even though its code lands at Phase 330 (BATCH-02) and its REQ-IDs (GAS-06/TST-05) are owned by 331/332; if 329-02 surfaced a losing-bet-liveness dependency, the SPEC carries the SURFACE-TO-USER flag, does not soften it"
  artifacts:
    - path: ".planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md"
      provides: "The reconciled v49.0 design-lock blueprint: §0 attestation verdict roll-up, §1 settled shared signatures (advanceGame return / doWork+NoWork / O(1) discovery views), §2 the 4 structural invariants + the ROUTER-07/GAS-03 dispositions + the D-05 design-lock, §3 per-item IMPL blueprint + producer-before-consumer edit-order map"
      contains: "Shared Signatures"
  key_links:
    - from: "the two 329-ATTEST-*.md docs (ROUTER-ADVANCE + DEGENERETTE-RESOLVE)"
      to: "329-SPEC.md §0 verdict roll-up + §1 settled signatures + §2 invariants/dispositions"
      via: "fold attestation drift + decision verdicts + the D-05f finding into the locked design"
      pattern: "advanceGame|doWork|NoWork|advanceDue|degeneretteResolve"
    - from: "the 4 structural invariants + design-1 advanceGame return"
      to: "the producer-before-consumer edit-order map for Phase 330"
      via: "§3 Files in the diff + edit-order"
      pattern: "edit.order|Files in the diff|producer.before.consumer"
---

<objective>
Author `329-SPEC.md` — the reconciled v49.0 design-lock blueprint (the direct analog of v48's
325-SPEC.md) — consuming the two `329-ATTEST-*.md` docs from Plans 01 and 02. Four jobs:
(1) **§2 LOCK the 4 structural invariants** in writing (BATCH-01): (a) one-category structural
    early-return; (b) frozen advance-consume under the new router composition (ADV-04); (c) guaranteed
    free-fallback `advanceGame()` caller from existing paths only (D-04); (d) single day-start epoch
    satisfied by design-1 (GAS-03 / D-03);
(2) **§1 settle the shared signatures** so no downstream file ships a broken intermediate: the
    `advanceGame` return shape (design-1 `(uint8 mult, bool rewardable)`) + the `:275` wrapper decode,
    `doWork(maxCount)` + the ROUTER-06 `NoWork()` revert (D-02), and the O(1) discovery views
    (`advanceDue()` covering new-day AND mid-day partial-drain, `boxesPending()`, buys-pending via
    AfKing-local cursor — ROUTER-04);
(3) **§2 resolve the two flagged dispositions** on paper: ROUTER-07 reentrancy (D-01 NO guard, on the
    grep-checked D-01a basis + the D-01b empirical backstop) and GAS-03 single epoch (D-03/D-03a);
(4) **§0 + §3** produce the attestation verdict roll-up + the per-item IMPL blueprint + the
    producer-before-consumer edit-order map that is the load-bearing input to Phase 330 — and LOCK the
    D-05 `degeneretteResolve` design item (rename + flat ~1-BURNIE re-peg + the D-05f finding +
    router-fold-OUT) as a v49.0 design item.

Purpose: this is the BATCH-01 reconciliation half plus the three SPEC-owned decision requirements
(ROUTER-07, ADV-04, GAS-03). After this, Phase 330 applies one fully reconciled batched diff with zero
"by construction" assumptions, zero shared-signature conflicts, and the VRF-freeze invariant proven to
survive the new router composition on paper before any code is written.

Output: one `329-SPEC.md` under the phase directory. ZERO `contracts/*.sol` mutation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-CONTEXT.md

# The precedent SPEC to mirror (§0 corrections / §1 reconciliation / §2 blueprint + edit-order map):
@.planning/milestones/v48.0-phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md

# The locked milestone design (the binding spec — read for the router shape, design-1, the 8 pitfalls + phase-prevention mapping, the 5 SPEC-time gaps):
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/PLAN-CRANK-DO-WORK-INCENTIVE.md

<wave1_inputs>
# Read BOTH ATTEST docs produced by Plans 01 + 02 — they are the verdict inputs to the SPEC:
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-DEGENERETTE-RESOLVE.md
# And the two SUMMARYs (the consumable roll-ups):
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-01-SUMMARY.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-02-SUMMARY.md
</wave1_inputs>

<design_anchors>
<!-- The decisions + grep-confirmed anchors the SPEC settles. Sourced from CONTEXT <decisions> + the Wave-1 ATTEST docs. -->
The 4 structural invariants (BATCH-01, ROADMAP SC1):
  (a) one-category structural early-return — doWork routes advance→autoOpen→autoBuy, returns after the FIRST rewarded category (a code invariant; Pitfall 4 bounty-stacking).
  (b) frozen advance-consume — totalFlipReversals nudge (AdvanceModule ~:1838-1844) frozen request→consume; router consumes via the design-1 return, no new in-window SLOAD (ADV-04 / Pitfall 3 / v45-vrf-freeze-invariant).
  (c) guaranteed free-fallback caller — EXISTING paths only (D-04): 30-min universal bypass (AdvanceModule :1012) + DegenerusVault.gameAdvance() :527-528 + StakedDegenerusStonk.gameAdvance() :421-422 + 120-day death-clock :109/:1198 (Pitfall 8).
  (d) single day-start epoch — design-1 single-sources the advance multiplier (GAS-03 / D-03); the two intentionally-distinct epochs (AfKing absolute-day :829 vs AdvanceModule game-day :243-246) NOT physically merged (D-03a / Pitfall 7).
The shared signatures (ROADMAP SC2):
  advanceGame returns (uint8 mult, bool rewardable) — design-1; decoded in the DegenerusGame :275 wrapper; rewardable is a DISTINCT bool.
  doWork(maxCount) — ROUTER-06 no-work signal = revert NoWork() (D-02; the AfKing EmptyAutoBuy/NoSubscribersAutoBought idiom precedent); fires only when all 3 O(1) predicates are empty.
  O(1) discovery views (ROUTER-04): advanceDue() (new-day currentDayView() != dailyIdx :462 AND mid-day partial-drain), boxesPending() (boxPlayers[activeIndex].length > boxCursor AND lootboxRngWordByIndex[activeIndex] != 0), buys-pending (AfKing-local cursor :577) — advance/boxes on DegenerusGame, buys on AfKing-local; per-leg maxCount mapping.
The two dispositions (ROADMAP SC3):
  ROUTER-07 = NO nonReentrant guard (D-01) — basis: keeper-never-a-payee + no untrusted ETH send (per-leg grep, D-01a) + one-category early-return + creditFlip-last CEI (AfKing :99-106/:846); empirical TST-02 double-pay backstop scoped to PROVE it (D-01b).
  GAS-03 = design-1-satisfies (D-03); the two epochs documented intentionally-distinct (D-03a).
The D-05 design-lock (scope-add — code at 330/BATCH-02, REQ GAS-06/TST-05 at 331/332):
  rename autoResolve→degeneretteResolve + _autoResolveBet→_degeneretteResolveBet (D-05a); flat literal ~1 BURNIE once/tx, ≥3-NON-WWXRP gate, revert NoWork() at 0, lean resolve-always-pay-at-≥3-revert-only-at-0 (D-05b); the D-05c real-gas (NOT peg-ref) net-loss basis; WWXRP-excluded + AUTO-02/REW-04 preserved (D-05d); GAS sanity-check handoff (D-05e); the D-05f losing-bet-liveness finding (carry from 329-02 — if surfaced, do NOT soften); router-fold OUT (architecturally blocked).
The edit-order (ROADMAP SC4, producer-before-consumer): AdvanceModule (delete 3 creditFlip sites :189/:225/:468 + add design-1 return) → DegenerusGame (wrapper :275 decode + discovery views + MintModule GASOPT-01 pointer) → interfaces (IDegenerusGame + IDegenerusGameModules new views + updated advanceGame sig) → AfKing (doWork router + _autoBuy refactor + re-peg placeholder + GASOPT-02 claimable hoist) — so no intermediate state ships where advancing is unrewarded.
</design_anchors>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 329-SPEC.md §0 (attestation roll-up) + §1 (shared-signature reconciliation: advanceGame return / doWork+NoWork / O(1) discovery views)</name>
  <read_first>
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-03-PLAN.md (this plan, the design_anchors + wave1_inputs)
    - .planning/milestones/v48.0-phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md (the §0 carried-corrections + §1 R-row reconciliation format to mirror)
    - BOTH 329-ATTEST-*.md docs (the verdict inputs — every SHIFTED/ABSENT row, the ROUTER-07/GAS-03/ADV-04/invariant-(c) verdicts, the design-1-return / 3-creditFlip-classification / discovery-view verdicts, and the D-05f finding)
    - .planning/phases/329-.../329-01-SUMMARY.md + 329-02-SUMMARY.md (the consumable roll-ups)
    - .planning/research/SUMMARY.md (design-1, the discovery-view shapes, the 5 SPEC-time gaps) + .planning/research/PITFALLS.md (the pitfall-to-prevention mapping the invariants close)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-CONTEXT.md (D-02 NoWork; the Claude's-Discretion return-encoding + discovery-view + maxCount items)
  </read_first>
  <action>Create 329-SPEC.md with a header (Milestone v49.0, Baseline HEAD 0cc5d10f, Inputs = the milestone scope + research SUMMARY/ARCHITECTURE/PITFALLS + the 2 ATTEST docs, Owns = BATCH-01 + ROUTER-07 + ADV-04 + GAS-03, SOURCE-TREE not mutated at SPEC).
§0 ATTESTATION VERDICT ROLL-UP: fold the two ATTEST docs into one roll-up — the total IMPL-blocker count across both surfaces, the list of carried line-drift corrections vs CONTEXT/SUMMARY (mirror the v48 C1..Cn style: e.g. the 30-min bypass :1012 vs CONTEXT ~:1008 SHIFTED +4; the GASOPT-01 :393/:670 vs CONTEXT :398/:671; the gas-peg :1536-1546 vs cited :1539-1546), and a re-grep-at-edit-time note (the batched diff shifts lines). State explicitly: the ROUTER-07 no-guard-basis-holds verdict, the GAS-03 epochs-intentionally-distinct verdict, the ADV-04 no-new-in-window-read verdict, the invariant-(c) fallback-callers-intact verdict, and the D-05f losing-bet-liveness finding (inert-safe OR carry the 329-02 SURFACE-TO-USER block verbatim — do NOT soften).
§1 SHARED SIGNATURES (BATCH-01 reconciliation, ROADMAP SC2) — a "## Shared Signatures" heading with one settled signature + decode/consume note per shared construct, mirroring v48's R-rows:
  R1 advanceGame return — the FINAL tuple `(uint8 mult, bool rewardable)` (design-1; rewardable a DISTINCT bool per the 329-01 verdict), how AdvanceModule produces it, and how the DegenerusGame :275 wrapper decodes the delegatecall `data` and forwards it to external callers + the IGame interface.
  R2 doWork(maxCount) signature + the ROUTER-06 NoWork() revert — settle the entrypoint signature, the priority routing (advance→autoOpen→autoBuy), and the revert-NoWork()-when-all-3-predicates-empty signal (D-02; the per-leg internal revert never trips because doWork enters a leg only when that predicate has work). State the per-leg maxCount mapping (advance / autoOpen(maxCount) / _autoBuy(maxCount)).
  R3 the O(1) discovery views (ROUTER-04) — settle advanceDue() (covering BOTH new-day currentDayView() != dailyIdx AND mid-day partial-drain), boxesPending(), buys-pending (AfKing-local cursor), each as O(1) (no unbounded scan), and WHERE each lives (advance/boxes on DegenerusGame, buys on AfKing-local).
  R4 the bounty-payment composition — the router pays ONLY the re-homed advance bounty (one creditFlip, last/CEI); the autoOpen/autoBuy legs keep their OWN in-callee bounty (router does NOT double-pay); confirm the KEEP-04 affiliate-code passthrough survives the _autoBuy refactor (ROUTER-05 pre-condition) per the 329-01 verdict.
For each R-row state the producing file and the consuming file(s) + the order to apply them inside the single diff. NO contracts/*.sol edit.</action>
  <verify>
    <automated>bash -c 'F=.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md; test -f "$F" && grep -q "Shared Signatures" "$F" && grep -q "0cc5d10f" "$F" && grep -Eqi "uint8 mult, bool rewardable|\(uint8.*bool rewardable\)" "$F" && grep -q "NoWork" "$F" && grep -Eqi "advanceDue|boxesPending|buys.pending" "$F" && grep -Eqi "ROUTER-07|GAS-03|ADV-04" "$F"'</automated>
  </verify>
  <acceptance_criteria>
    - 329-SPEC.md exists with a header naming baseline HEAD 0cc5d10f + Owns = BATCH-01/ROUTER-07/ADV-04/GAS-03 + a SOURCE-TREE-not-mutated line.
    - §0 rolls up BOTH ATTEST docs into a single IMPL-blocker count + the carried line-drift corrections + the ROUTER-07/GAS-03/ADV-04/invariant-(c) verdicts + the D-05f finding (inert-safe or the carried SURFACE-TO-USER block, not softened).
    - A "## Shared Signatures" section settles ONE signature each for: R1 advanceGame return tuple + :275 wrapper decode, R2 doWork(maxCount) + NoWork() revert + per-leg maxCount, R3 the three O(1) discovery views + their locations, R4 the one-creditFlip-router-pays-only-advance + no-double-pay + KEEP-04 affiliate survival.
    - Every R-row names the producing + consuming files and an explicit apply-order within the single diff.
    - No contracts/*.sol modified.
  </acceptance_criteria>
  <done>329-SPEC.md §0 + §1 committed: attestation roll-up folded (incl. the D-05f finding), the shared signatures (advanceGame return / doWork+NoWork / O(1) discovery views / bounty composition) settled with apply-order so no downstream file ships a broken intermediate; zero .sol mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Author 329-SPEC.md §2 (the 4 structural invariants + the ROUTER-07/GAS-03 dispositions + the D-05 design-lock) + §3 (per-item IMPL blueprint + producer-before-consumer edit-order map)</name>
  <read_first>
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-03-PLAN.md (this plan)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md (§0 + §1 just authored by Task 1 — §2/§3 must be consistent with the settled signatures)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-CONTEXT.md (the LOCKED decisions D-01..D-05g + the Claude's-Discretion edit-order-map item — §2 resolves exactly these)
    - .planning/milestones/v48.0-phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md (§2 per-item IMPL blueprint + the "Files in the diff / Order" edit-order map format to mirror)
    - BOTH 329-ATTEST-*.md docs (the per-leg no-untrusted-ETH-send rows feed the ROUTER-07 disposition; the dual-epoch + totalFlipReversals + fallback-caller rows feed the invariants; the D-05 rename/shape/non-foldability/finding rows feed the D-05 design-lock)
    - .planning/research/PITFALLS.md (Pitfalls 3/4/6/7/8 — the invariants + dispositions close these; the pitfall-to-phase mapping)
    - .planning/PLAN-CRANK-DO-WORK-INCENTIVE.md (§7 the three faucet locks invariant (a) preserves; §9 OPEN-C the ROUTER-07 disposition closes for the router)
  </read_first>
  <action>Append §2 and §3 to 329-SPEC.md.
§2 STRUCTURAL INVARIANTS + DISPOSITIONS + D-05 DESIGN-LOCK — labeled subsections, each recording the lock + its source (decision id D-NN or the ATTEST grep verdict) + the IMPL instruction it produces:
  Invariant (a) ONE-CATEGORY STRUCTURAL EARLY-RETURN — doWork routes advance→autoOpen→autoBuy and RETURNS after the first rewarded category (a code invariant, not a comment; preserves the one-creditFlip-per-tx faucet bound from PLAN-CRANK §7 + the AfKing :846 model; Pitfall 4). IMPL instruction: explicit early-return after the chosen category pays.
  Invariant (b) FROZEN ADVANCE-CONSUME (ADV-04) — the totalFlipReversals nudge (AdvanceModule ~:1838-1844) is frozen request→consume; the router consumes the daily tick via the design-1 RETURN (R1) and adds NO new mutable in-window SLOAD (carry the 329-01 §E verdict). Lock the router's internal ordering: advance-if-due reads only frozen state; whichever order the legs run, the consume reads only the frozen word. IMPL instruction + the TST-01 freeze-fuzz handoff (empirical proof at 332).
  Invariant (c) GUARANTEED FREE-FALLBACK CALLER (D-04) — EXISTING structural paths ONLY (no new mechanism, feedback_frozen_contracts_no_future_proofing): PRIMARY the router's advance leg keeps the stall-escalating bounty (re-homed not removed); SECONDARY the 30-min universal bypass (:1012) + Vault/sDGNRS gameAdvance() wrappers (:527-528 / :421-422); TERTIARY the 120-day death-clock (:109/:1198). Carry the 329-01 §G verdict: re-homing removes no structural caller.
  Invariant (d) SINGLE DAY-START EPOCH (GAS-03 / D-03) — design-1 single-sources the advance multiplier (computed ONCE in AdvanceModule :243-246, returned, consumed without recompute); the two intentionally-distinct epochs (AfKing absolute-day :829 vs AdvanceModule game-day :243-246) are NOT physically merged (D-03a — document WHY they differ so a future auditor doesn't flag it). Carry the 329-01 §C verdict.
  Disposition ROUTER-07 (D-01) — NO nonReentrant guard on doWork, on the grep-checked per-leg no-untrusted-ETH-send basis (D-01a, carry the 329-01 §B per-leg rows): the formal basis = keeper-never-a-payee + no untrusted ETH send + one-category structural early-return + creditFlip-last CEI ordering. D-01b: the TST-02 router→game→creditFlip double-pay reentrancy regression stays as the empirical backstop scoped to PROVE the no-guard disposition. If 329-01 flagged any untrusted-push leg, carry it as a ROUTER-07 blocker (do not soften).
  Disposition GAS-03 single-epoch — design-1-satisfies (folds into invariant d; cross-reference, no duplicate decision).
  D-05 DEGENERETTE-RESOLVE DESIGN-LOCK — LOCK the rename (autoResolve→degeneretteResolve + _autoResolveBet→_degeneretteResolveBet, D-05a) + the flat literal ~1-BURNIE once/tx, ≥3-NON-WWXRP gate, revert NoWork() at 0, lean resolve-always-pay-at-≥3-revert-only-at-0 (D-05b) + the D-05c real-gas (NOT peg-ref) net-loss basis + WWXRP-excluded + AUTO-02/REW-04 preserved (D-05d) + the GAS sanity-check handoff (D-05e) + router-fold-OUT (architecturally blocked) as a v49.0 design item, even though code lands at 330 (BATCH-02) and REQ-IDs GAS-06/TST-05 are owned by 331/332. Carry the 329-02 D-05f finding: if inert-safe, record it; if a dependency was SURFACED, carry the SURFACE-TO-USER block verbatim and mark it a USER-decision gate for 330.
§3 PER-ITEM IMPL BLUEPRINT + EDIT-ORDER MAP — a "## Files in the diff" list with the producer-before-consumer ORDER (AdvanceModule delete 3 creditFlip sites :189/:225/:468 + add design-1 return → DegenerusGame :275 wrapper decode + the discovery views + the GASOPT-01 MintModule pointer → interfaces IDegenerusGame + IDegenerusGameModules new views + updated advanceGame sig → AfKing doWork router + _autoBuy refactor + re-peg placeholder + GASOPT-02 claimable hoist) so no intermediate state ships where advancing is unrewarded; then one blueprint paragraph per work-area (the doWork router incl. the one-category early-return + the discovery predicates; the advance bounty-removal + design-1 return; the discovery views + wrapper; the interfaces; the GASOPT-01/02 micro-opts; the D-05 degeneretteResolve rename+re-peg riding the same diff but staying a separate call). Note the break-even peg + the ~1-BURNIE constant are SPEC PLACEHOLDERS calibrated at GAS (331). End with a §N success-criteria checklist mapped 1:1 to ROADMAP Phase 329 SC1..SC4 + a "SOURCE-TREE not mutated at SPEC" line. NO contracts/*.sol edit.</action>
  <verify>
    <automated>bash -c 'F=.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md; test -f "$F" && grep -Eqi "early-return|early return" "$F" && grep -q "totalFlipReversals" "$F" && grep -Eqi "death.clock|30 minutes|gameAdvance" "$F" && grep -q "nonReentrant" "$F" && grep -q "degeneretteResolve" "$F" && grep -Eqi "Files in the diff|edit.order|producer.before.consumer" "$F" && grep -Eqi "placeholder" "$F" && grep -Eqi "SC1|SC4|success.criteria" "$F"'</automated>
  </verify>
  <acceptance_criteria>
    - §2 LOCKS each of the 4 structural invariants with a labeled subsection citing its decision id + ATTEST verdict + the IMPL instruction: (a) one-category early-return, (b) frozen advance-consume (ADV-04 + TST-01 handoff), (c) free-fallback caller from existing paths only (D-04, PRIMARY/SECONDARY/TERTIARY), (d) single day-start epoch satisfied by design-1 + the two-epochs-intentionally-distinct rationale (D-03/D-03a).
    - §2 RESOLVES ROUTER-07 = NO guard (D-01) on the grep-checked per-leg no-untrusted-ETH-send basis (D-01a) + the D-01b TST-02 empirical backstop; GAS-03 single-epoch cross-referenced to invariant (d).
    - §2 LOCKS the D-05 degeneretteResolve design item (rename + flat ~1-BURNIE/≥3-NON-WWXRP-gate/revert-on-no-work + D-05c real-gas basis + D-05f finding + router-fold-OUT) as a v49.0 design item; a surfaced losing-bet dependency is carried verbatim as a USER gate, not softened.
    - §3 has a "## Files in the diff" producer-before-consumer edit-order map (AdvanceModule → Game wrapper/views → interfaces → AfKing router/_autoBuy/re-peg/micro-opts) + one blueprint paragraph per work-area; the peg + ~1-BURNIE constant are flagged SPEC placeholders calibrated at GAS.
    - A success-criteria checklist maps to ROADMAP Phase 329 SC1..SC4 + a SOURCE-TREE-not-mutated line.
    - No contracts/*.sol modified.
  </acceptance_criteria>
  <done>329-SPEC.md §2 + §3 committed: the 4 structural invariants locked, the ROUTER-07/GAS-03 dispositions resolved, the D-05 design item locked, the per-item IMPL blueprint + producer-before-consumer edit-order map produced as the Phase 330 input, SC1..SC4 checklist present; zero .sol mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Paper-only reconciliation — ZERO code mutation this phase; the SPEC LOCKS the mitigations for IMPL
(330) to apply and GAS/TST/TERMINAL (331/332/333) to prove. The boundary is the SPEC itself: an
under-specified shared signature, an unlocked structural invariant, or a softened disposition lets a
conflicting intermediate diff, a faucet, a freeze-break, or a silent liveness regression slip into
Phase 330's IMPL diff. This plan consumes the Wave-1 attestation evidence and must not contradict it.

| Boundary | Description |
|----------|-------------|
| ATTEST verdicts → SPEC settled signatures/invariants | A signature settled ignoring a co-edit, or an invariant left as a comment not a code lock, produces a conflicting/broken IMPL diff |
| decision (D-NN) → IMPL instruction | A disposition softened (e.g. recording the no-guard without the per-leg basis, or burying a surfaced D-05f dependency) ships the wrong behavior |
| design-1 return signature → all downstream files | A broken intermediate (advance unrewarded before the router pays) if the edit-order is not producer-before-consumer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-329-R1 | Tampering | A shared signature (advanceGame return / doWork / discovery views) is settled inconsistently with the ATTEST verdicts → IMPL produces a conflicting or broken-intermediate diff | mitigate | §1 R-rows each name the producing + consuming files + an explicit apply-order; §3 enforces producer-before-consumer (AdvanceModule → Game → interfaces → AfKing) so advancing is never unrewarded mid-diff |
| T-329-R2 | Spoofing / Elevation | ROUTER-07 no-guard recorded WITHOUT the per-leg no-untrusted-ETH-send basis (Pitfall 6 composed reentrancy) | mitigate | §2 carries the 329-01 §B per-leg rows verbatim as the no-guard basis (D-01a) + the D-01b TST-02 double-pay empirical backstop scoped to PROVE it; any untrusted-push leg is carried as a blocker |
| T-329-R3 | Tampering (RNG) | Invariant (b) left as prose, not a locked router-ordering instruction → the freeze invariant is not actually enforced under the new composition (Pitfall 3, ADV-04, v45 floor) | mitigate | §2 invariant (b) locks the router ordering (consume reads only frozen state via the design-1 return, no new in-window SLOAD) + hands the empirical freeze-fuzz to TST-01 |
| T-329-R4 | Denial of service (liveness) | A surfaced D-05f losing-bet-liveness dependency is softened/buried in the SPEC → IMPL drops the incentive and silently starves a needed cleanup/accounting path | mitigate | §0 + §2 carry the 329-02 SURFACE-TO-USER block verbatim and mark it a USER-decision gate for 330; the SPEC does not record "safe" when 329-02 surfaced a dependency |
| T-329-R5 | Repudiation | A structural invariant or disposition (a/b/c/d, ROUTER-07, GAS-03) is left ambiguous → IMPL improvises | mitigate | §2 locks each with a labeled subsection citing D-NN or the ATTEST verdict + the concrete IMPL instruction; the SC1..SC4 checklist maps 1:1 to the ROADMAP acceptance bar |
| T-329-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this paper-only plan; RESEARCH Package Legitimacy Gate N/A |
</threat_model>

<verification>
- 329-SPEC.md exists with §0 (attestation roll-up), §1 (Shared Signatures R-rows w/ apply-order), §2 (4 structural invariants + ROUTER-07/GAS-03 dispositions + D-05 design-lock), §3 (IMPL blueprint + producer-before-consumer edit-order map).
- All 4 SPEC-owned requirements (BATCH-01 + ROUTER-07 + ADV-04 + GAS-03) are resolved in writing.
- The D-05f losing-bet-liveness finding from Plan 02 is folded into §0 + §2 (inert-safe recorded, or the SURFACE-TO-USER block carried verbatim).
- The 4 structural invariants are locked as code invariants (not comments); the no-guard disposition rests on the per-leg grep basis; the edit-order is producer-before-consumer.
- Zero contracts/*.sol modified: `git diff --name-only -- 'contracts/*.sol'` is empty.
</verification>

<success_criteria>
- The 4 structural invariants locked in writing (SC1 / BATCH-01).
- The shared signatures settled (SC2): advanceGame return / doWork+NoWork / O(1) discovery views.
- The two dispositions resolved (SC3): ROUTER-07 no-guard on the per-leg basis + GAS-03 design-1-satisfies.
- Every cited file:line verdict folded; no un-grepped "by construction" claim survives (SC4, leaning on Wave 1) + the producer-before-consumer edit-order map produced.
- The D-05 degeneretteResolve design item locked + the D-05f finding carried (not softened).
- ZERO contracts/*.sol mutation (paper-only phase).
</success_criteria>

<output>
Create `.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-03-SUMMARY.md` when done, listing 329-SPEC.md's sections, the 4 locked structural invariants, the settled shared signatures, the ROUTER-07/GAS-03 dispositions, the D-05 design-lock + the D-05f finding, and confirming SC1..SC4 coverage for the Phase 329 verification.
</output>
</content>
</invoke>
