---
phase: 329-spec-design-lock-call-graph-attestation-4-structural-invaria
plan: 03
type: execute
wave: 2
depends_on: ["329-01", "329-02"]
files_modified:
  - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [BATCH-01, ROUTER-07, ADV-04, GAS-03]
must_haves:
  truths:
    - "329-SPEC.md §2 LOCKS the 4 structural invariants under the REDESIGNED composition: (a) one-category structural early-return on the REDESIGNED order autoBuy→advance→autoOpen (RD-1; return after the first rewarded category — a code invariant not a comment, BATCH-01); (b) frozen advance-consume re-attested under the new order + the dropped rngLock guards (RD-2) incl. the totalFlipReversals nudge (AdvanceModule ~:1838-1844), with autoBuy running pre-entropy at day-open and advance consuming via the design-1 return adding no new in-window SLOAD (ADV-04 / v45-vrf-freeze-invariant); (c) guaranteed free-fallback advanceGame() caller from EXISTING paths only, AMENDED under autoBuy-highest-priority (D-04a — the rewarded advance leg is blocked while buys pend, so first-30-min advance relies on the bypass tiers not the router bounty); (d) single day-start epoch SATISFIED BY DELETION (GAS-03 / D-03 dissolved by D-07 — advance is the SOLE stall epoch; the autoBuy stall ladder + absolute epoch are deleted, no two epochs to collapse)"
    - "329-SPEC.md §1 settles the REDESIGNED shared signatures: advanceGame returns (uint8 mult, bool rewardable) with mid-day mult=1 (no escalation, D-07/ADV-05) decoded in the DegenerusGame :275 wrapper; PARAMETERLESS doWork() (D-07 — determines its own per-leg default batch, NO maxCount, supersedes D-06) + the standalone parametered+UNREWARDED autoOpen(count)/autoBuy(count) emergency path + the ROUTER-06 NoWork() revert; the legs return raw counts (autoOpen/autoBuy) + the (mult,rewardable) tuple (advance), and the UNIFIED single creditFlip lives in doWork (RD-4 — direct calls unrewarded); the O(1) discovery views (advanceDue() new-day OR mid-day partial-drain, boxesPending() rngLock-aware per RD-3, buys-pending TRUE-during-rngLock per RD-2 — ROUTER-04)"
    - "329-SPEC.md §1 LOCKS the D-07 flat-per-tx bounty model: per-category multiples of a unit 1× (≈ a max-laden tx's gas at 0.5 gwei worth of BURNIE flip) — advance 2×·mult (mult = stall ladder NEW-DAY only, mid-day=1), buy 1.5×, open 1× pro-rated below the knee (1× × min(opened,KNEE)/KNEE, KNEE≈5); ONE base unit + one conversion (× PRICE_COIN_UNIT / mp) + one creditFlip in doWork + one-category early-return preserved; the 1× unit / the 1/1.5/2 ratios / the open KNEE are GAS-331 PLACEHOLDERS (SPEC starting estimates, NOT locked values); the faucet-safety constraint GAS-331 must satisfy + WR-01/GAS-05 must re-prove (per-box reward below the open knee ≤ a one-box tx's gas at 0.5 gwei → tiny mid-day open is −EV) is recorded; the advance/buy no-gate faucet doctrine is recorded"
    - "329-SPEC.md §2 RESOLVES the two dispositions under the redesign: ROUTER-07 = NO nonReentrant guard (D-01) RE-GROUNDED on the unified single creditFlip (under RD-4 there is exactly ONE creditFlip in doWork, CEI-last, fed by legs that return counts/mult and never self-credit — the no-guard case is STRONGER); the per-leg + single-creditFlip no-untrusted-ETH-send basis (D-01a, carried from 329-01) is the formal basis = keeper-never-a-payee + no untrusted ETH send + one-category structural early-return + single-creditFlip-last CEI ordering, with the D-01b TST-02 double-pay backstop; GAS-03 = satisfied-by-deletion (cross-ref invariant d, D-03 dissolved by D-07)"
    - "329-SPEC.md LOCKS the 5 redesign changes (RD-1 order / RD-2 dropped rngLock guards + Q5 batchPurchase-caller verdict / RD-3 autoOpen rngLock-block + boxesPending-rngLock-aware / RD-4 unify-bounty-into-doWork / RD-5 drop-try/catch + entry-gate on `rngLocked()||_livenessTriggered()`) + the D-08 GASOPT-03/04/05 (batched keeper read SUBSUMING GASOPT-02 / drop AutoBought event + the test-oracle migration / drop per-iteration isOperatorApproved :676 KEEP subscribe-time :401, the 333-SWEEP-re-attests-4-OPEN-E-protections blocking-condition) as v49.0 design items, folding the two ATTEST docs' Roll-ups + the D-05f finding into §0 and the producer-before-consumer survivors-vs-reworked edit-order map into §3"
    - "329-SPEC.md LOCKS the D-05 degeneretteResolve design item (rename + flat ~1-BURNIE/≥3-NON-WWXRP-gate/revert-on-no-work re-peg + the D-05c real-gas basis + the D-05f finding + router-fold-OUT) — survives the redesign verbatim; code lands at Phase 330 (BATCH-02), REQ-IDs GAS-06/TST-05 owned by 331/332; if 329-02 surfaced a losing-bet-liveness dependency the SPEC carries the SURFACE-TO-USER flag verbatim, does not soften it"
    - "REQUIREMENTS.md + ROADMAP.md are AMENDED per the CONTEXT-enumerated list (count moves 31 → ~34): ROUTER-01 → parameterless doWork() (drop maxCount==0 sentinel; add standalone parametered+unrewarded autoOpen(count)/autoBuy(count)); ROUTER-02 → order autoBuy→advance→autoOpen; ROUTER-04 → boxesPending() rngLock-aware + buys-pending true during rngLock + still O(1); ROUTER-05 → _autoBuy refactor + dropped rngLock guard + unified bounty; NEW REQ homes ROUTER-08 (RD-2 autoBuy=normal-buy/drop-guards + batchPurchase:1737 removal + Q5)/ROUTER-09 (RD-3/RD-5 block-autoOpen-during-rngLock + drop-try/catch + entry-gate)/ROUTER-10 (RD-4 unify-bounty-into-doWork, reverses old R4); ADV-02/GAS-03 → advance returns mult with mid-day mult=1 + GAS-03 satisfied-by-deletion; GAS-02 → flat-per-tx per-category; GAS-04 → stall ladder advance-only; GAS-05/WR-01 → re-prove no +EV under flat-per-tx; NEW GASOPT-03/04/05 (fold GASOPT-02 into GASOPT-03); TST-01/TST-04 additions"
    - "ROADMAP.md Phase 329 GOAL + Success Criteria are UPDATED to the redesign (they currently describe the OLD design — doWork(maxCount), GAS-03 epoch-collapse, advance→open→buy, dual-epoch single-source — all reversed/dissolved): SC1 reads the 4 invariants under the new composition (one-category early-return on autoBuy→advance→autoOpen, frozen-consume under dropped guards, free-fallback under autoBuy-first D-04a, single-epoch SATISFIED BY DELETION); SC2 reads parameterless doWork() + the (mult,rewardable) return + rngLock-aware views; SC3 reads ROUTER-07 no-guard re-grounded on the unified creditFlip; SC4 reads grep-attested-vs-0cc5d10f with the corrected anchors (the 3 advance creditFlips :189/:225/:468 + the pulled-out open :1676 + buy :846 + the RD-2 :568/:1737 guards + the GASOPT baselines)"
    - "Phase 329's own requirements coverage stays BATCH-01/ROUTER-07/ADV-04/GAS-03 (the SPEC-phase reqs); the newly-registered ROUTER-08/09/10 + GASOPT-03/04/05 are IMPL reqs OWNED by Phase 330 — registered in REQUIREMENTS.md/ROADMAP.md Traceability + Coverage + the Phase 330 Requirements line, but NOT made phase-329 plan-coverage targets; the milestone count + per-category split + per-phase count tables are updated consistently (31 → ~34) and the v49.0 closure re-attest set widened to match"
  artifacts:
    - path: ".planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md"
      provides: "The reconciled v49.0 keeper-router-REDESIGN design-lock blueprint: §0 attestation verdict roll-up (incl. the Q5 + D-05f findings), §1 settled REDESIGNED shared signatures (advanceGame (mult,rewardable) / parameterless doWork() + standalone unrewarded escapes / NoWork / rngLock-aware O(1) views / D-07 flat-per-tx model), §2 the 4 structural invariants under the new composition + ROUTER-07 no-guard re-grounded + GAS-03 satisfied-by-deletion + the 5 RD changes + D-08 + the D-05 design-lock, §3 per-item IMPL blueprint + producer-before-consumer survivors-vs-reworked edit-order map"
      contains: "Shared Signatures"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Amended v49.0 requirements (~34): ROUTER-01/02/04/05 reworded, NEW ROUTER-08/09/10 + GASOPT-03/04/05, ADV-02/GAS-02/03/04/05 reworded, TST-01/04 additions, updated Traceability + per-phase count + per-category split"
      contains: "ROUTER-08"
    - path: ".planning/ROADMAP.md"
      provides: "Amended v49.0 roadmap: updated Phase 329 GOAL + SC1..SC4 (the redesign), updated Phase 330/331/332 Requirements lines (ROUTER-08/09/10 + GASOPT-03/04/05 homed), updated Coverage + per-category split tables (31 → ~34), updated Phase 329 plan list"
      contains: "satisfied by deletion"
  key_links:
    - from: "the two 329-ATTEST-*.md docs (ROUTER-ADVANCE + DEGENERETTE-RESOLVE) + their SUMMARYs"
      to: "329-SPEC.md §0 verdict roll-up + §1 settled signatures + §2 invariants/dispositions"
      via: "fold attestation drift + decision verdicts + the Q5 + D-05f findings into the locked redesign"
      pattern: "advanceGame|doWork|NoWork|advanceDue|degeneretteResolve|satisfied by deletion"
    - from: "the CONTEXT <decisions> enumerated REQUIREMENTS/ROADMAP amendments list"
      to: ".planning/REQUIREMENTS.md + .planning/ROADMAP.md (incl. the Phase 329 goal/SCs)"
      via: "apply each enumerated edit; register ROUTER-08/09/10 + GASOPT-03/04/05; update count tables 31 → ~34"
      pattern: "ROUTER-08|ROUTER-09|ROUTER-10|GASOPT-03|GASOPT-04|GASOPT-05|parameterless"
---

<objective>
Author `329-SPEC.md` — the reconciled v49.0 keeper-router-REDESIGN design-lock blueprint (the direct
analog of v48's 325-SPEC.md) — consuming the two redesigned `329-ATTEST-*.md` docs from Plans 01 and
02, AND amend `.planning/REQUIREMENTS.md` + `.planning/ROADMAP.md` to the redesign (count 31 → ~34,
incl. the Phase 329 goal/SCs which still describe the OLD design). Five jobs:
(1) **§2 LOCK the 4 structural invariants** under the REDESIGNED composition (BATCH-01): (a) one-category
    early-return on the new order autoBuy→advance→autoOpen (RD-1); (b) frozen advance-consume under the
    dropped rngLock guards (RD-2) + the new order (ADV-04); (c) guaranteed free-fallback `advanceGame()`
    AMENDED under autoBuy-highest-priority (D-04a); (d) single day-start epoch SATISFIED BY DELETION
    (GAS-03 / D-03 dissolved by D-07);
(2) **§1 settle the REDESIGNED shared signatures**: `advanceGame` returns `(uint8 mult, bool rewardable)`
    with mid-day mult=1; **parameterless `doWork()`** (D-07, supersedes D-06) + the standalone
    unrewarded `autoOpen(count)`/`autoBuy(count)` escapes + the ROUTER-06 `NoWork()` revert; the unified
    single creditFlip in doWork (RD-4); the rngLock-aware O(1) discovery views (ROUTER-04); the D-07
    flat-per-tx bounty model (GAS-331 placeholders);
(3) **§2 resolve the two dispositions**: ROUTER-07 = NO guard re-grounded on the unified single
    creditFlip (D-01/D-01a) + the D-01b backstop; GAS-03 = satisfied-by-deletion (D-03 dissolved);
(4) **§0 + §3** produce the attestation verdict roll-up (incl. the Q5 + D-05f findings) + the per-item
    IMPL blueprint + the producer-before-consumer survivors-vs-reworked edit-order map; LOCK the 5 RD
    changes + D-08 GASOPT-03/04/05 + the D-05 degeneretteResolve design item;
(5) **amend REQUIREMENTS.md + ROADMAP.md** per the CONTEXT-enumerated list (the redesign reverses/
    dissolves/adds requirements; the Phase 329 goal/SCs still describe the superseded design).

Purpose: this is the BATCH-01 reconciliation half plus the three SPEC-owned decision requirements
(ROUTER-07, ADV-04, GAS-03) plus the mandatory roadmap/requirements re-alignment. After this, Phase 330
re-authors one fully reconciled batched diff reflecting the keeper-router REDESIGN with zero "by
construction" assumptions and the VRF-freeze invariant proven to survive the new composition on paper.

Output: `329-SPEC.md` (rewritten from the superseded design) + amended `.planning/REQUIREMENTS.md` +
`.planning/ROADMAP.md`. ZERO `contracts/*.sol` mutation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-CONTEXT.md

# The binding re-SPEC input (the 5 locked changes + D-07 + Q-resolutions + survivors-vs-reworked):
@.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-ROUTER-REDESIGN-INTENT.md

# The precedent SPEC to mirror (§0 corrections / §1 reconciliation / §2 blueprint + edit-order map):
@.planning/milestones/v48.0-phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md

# The locked milestone design (no research sub-phase):
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/PLAN-CRANK-DO-WORK-INCENTIVE.md

<wave1_inputs>
# Read BOTH redesigned ATTEST docs produced by Plans 01 + 02 — they are the verdict inputs to the SPEC:
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-DEGENERETTE-RESOLVE.md
# And the two SUMMARYs (the consumable roll-ups):
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-01-SUMMARY.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-02-SUMMARY.md
</wave1_inputs>

<design_anchors>
<!-- The decisions + grep-confirmed-vs-0cc5d10f anchors the SPEC settles. Sourced from CONTEXT <decisions> + the Wave-1 ATTEST docs. ALL anchors are 0cc5d10f truth (the held-tree lines are drift). -->
The 4 structural invariants under the REDESIGN (BATCH-01, ROADMAP SC1 — to be UPDATED):
  (a) one-category structural early-return — doWork routes autoBuy→advance→autoOpen (RD-1), returns after the FIRST rewarded category (a code invariant; Pitfall 4 bounty-stacking).
  (b) frozen advance-consume — totalFlipReversals nudge (AdvanceModule :1838/:1844) frozen request→consume; under RD-1 autoBuy runs pre-entropy at day-open (rngLock false) BEFORE advance requests the word; advance consumes via the design-1 return, no new in-window SLOAD (ADV-04 / Pitfall 3 / v45-vrf-freeze-invariant).
  (c) guaranteed free-fallback caller — EXISTING paths only (D-04), AMENDED (D-04a) under autoBuy-highest-priority: PRIMARY the router advance leg (blocked while buys pend); SECONDARY 30-min universal bypass (AdvanceModule :1012) + DegenerusVault.gameAdvance() :527 + StakedDegenerusStonk.gameAdvance() :421; TERTIARY 120-day death-clock (:109/:1233/:1296). First-30-min advance during a buy backlog relies on SECONDARY, not the bounty.
  (d) single day-start epoch — SATISFIED BY DELETION (GAS-03 / D-03 dissolved by D-07): the autoBuy stall ladder + absolute epoch (AfKing :823-846) are DELETED (only advance escalates); advance is the SOLE stall epoch (AdvanceModule game-day :241-253); no two epochs to collapse.
The REDESIGNED shared signatures (ROADMAP SC2 — to be UPDATED):
  advanceGame returns (uint8 mult, bool rewardable) — design-1; mid-day mult=1 (no escalation, D-07/ADV-05); decoded in the DegenerusGame :275 wrapper; rewardable a DISTINCT bool (gameover-only false).
  PARAMETERLESS doWork() (D-07) — determines its own fixed per-leg default batch, NO maxCount (supersedes D-06; the maxCount==0 sentinel no longer exists). Priority autoBuy→advance→autoOpen (RD-1), one-category early-return, single creditFlip.
  standalone autoOpen(count) / autoBuy(count) — parametered + UNREWARDED (D-07; manual/emergency clears; direct calls unrewarded per RD-4).
  ROUTER-06 NoWork() revert — fires only when all 3 O(1) predicates are empty (the AfKing EmptyAutoBuy/NoSubscribersAutoBought idiom precedent).
  unified bounty — ONE creditFlip in doWork (RD-4): legs return raw counts (autoOpen/autoBuy) + the (mult,rewardable) tuple (advance); direct (non-router) calls unrewarded.
  D-07 flat-per-tx model — advance 2×·mult (mult = stall NEW-DAY only; mid-day=1), buy 1.5×, open 1× pro-rated below the knee (1× × min(opened,KNEE)/KNEE, KNEE≈5); one base unit (≈ max-laden tx gas at 0.5 gwei worth of BURNIE flip) × PRICE_COIN_UNIT / mp; the 1× unit / 1·1.5·2 ratios / KNEE are GAS-331 PLACEHOLDERS; the faucet-safety constraint (per-box reward below the knee ≤ a one-box tx's 0.5-gwei gas → tiny mid-day open −EV) + the advance/buy-no-gate doctrine recorded.
  O(1) discovery views (ROUTER-04): advanceDue() (new-day currentDayView():462 != dailyIdx OR mid-day partial-drain), boxesPending() (rngLock-aware per RD-3; covers mid-day-resolved rounds), buys-pending (AfKing-local cursor; TRUE during rngLock per RD-2) — buys on AfKing-local, advance/boxes on DegenerusGame.
The two dispositions (ROADMAP SC3 — to be UPDATED):
  ROUTER-07 = NO nonReentrant guard (D-01) — RE-GROUNDED on the unified single creditFlip: under RD-4 exactly ONE creditFlip in doWork, CEI-last, fed by legs that return counts/mult and never self-credit (STRONGER than the old per-leg-bounty case). Basis = keeper-never-a-payee + no untrusted ETH send (per-leg grep D-01a) + one-category early-return + single-creditFlip-last CEI (AfKing :100). D-01b TST-02 double-pay backstop.
  GAS-03 = satisfied-by-deletion (D-03 dissolved by D-07; cross-ref invariant d).
The 5 RD changes (LOCK in §2):
  RD-1 order autoBuy→advance→autoOpen; RD-2 drop rngLock guards (AfKing :568 + game-side :1737, KEEP gameOver :1738; Q5 batchPurchase-caller verdict from 329-01); RD-3 boxesPending rngLock-aware + block autoOpen during rngLock; RD-4 unify bounty into doWork (reverses old R4); RD-5 drop autoOpen try/catch + entry-gate on `rngLocked()||_livenessTriggered()` (basis: the _queueTickets liveness control at contracts/storage/DegenerusGameStorage.sol:571, _autoOpenBox made INTERNAL).
The D-08 GASOPT (LOCK in §2/§3 as v49.0 design items, REQ-IDs registered, code at 330):
  GASOPT-03 batched keeper read (batchPurchaseForKeeper/keeperSnapshot collapsing claimableWinningsOf :691/:722; SUBSUMES GASOPT-02); GASOPT-04 drop AutoBought event (:171/:785) + the test-oracle migration to lastAutoBoughtDay (the AfKing test files); GASOPT-05 drop per-iteration isOperatorApproved(player) :676, KEEP subscribe-time :401 (333-SWEEP re-attests the 4 OPEN-E protections — blocking-condition).
The D-05 design-lock (survives verbatim; code at 330/BATCH-02, REQ GAS-06/TST-05 at 331/332):
  rename autoResolve→degeneretteResolve + _autoResolveBet→_degeneretteResolveBet (D-05a; NO interface row — defined on DegenerusGame.sol); flat ~1 BURNIE once/tx, ≥3-NON-WWXRP gate, revert NoWork() at 0, lean resolve-always-pay-at-≥3-revert-only-at-0 (D-05b); D-05c real-gas net-loss basis; WWXRP-excluded + AUTO-02/REW-04 preserved (D-05d); D-05f finding (carry from 329-02 — if surfaced, do NOT soften); router-fold OUT.
The edit-order (ROADMAP SC4, producer-before-consumer, survivors-vs-reworked):
  SURVIVORS (carry from the held diff): advance bounty re-home + (mult,rewardable) tuple; degeneretteResolve rename + ≥3 re-peg; GASOPT-01 MintModule [rk] hoists; the interface tuple + advanceDue()/boxesPending() views.
  REWORKED (re-derive on top): AfKing doWork (parameterless + order + unified bounty + flat-per-tx), _autoBuy (drop rngLock guard), DegenerusGame.autoOpen (rngLock-block + drop try/catch + entry-gate + return reward), autoBuy (return reward), IDegenerusGame (autoOpen/autoBuy signatures), boxesPending (rngLock-aware), + GASOPT-03/04/05.
  Order: AdvanceModule (delete 3 creditFlip sites :189/:225/:468 + add (mult,rewardable) return) → DegenerusGame (wrapper :275 decode + rngLock-aware views + autoOpen rework + degeneretteResolve rename + GASOPT-01 MintModule pointer + GASOPT-03 keeperSnapshot) → interfaces (advanceGame tuple + autoOpen/autoBuy return shapes + views + keeperSnapshot) → AfKing (parameterless doWork router + _autoBuy refactor + drop guards + unified flat-per-tx bounty + GASOPT-04/05) → tests (rename-fixes + GASOPT-04 oracle migration).
The mandatory amendments (CONTEXT <decisions> "REQUIREMENTS.md / ROADMAP.md amendments the planner MUST make"):
  ROUTER-01 → parameterless doWork() (drop maxCount==0 sentinel; add standalone unrewarded autoOpen(count)/autoBuy(count)); ROUTER-02 → order autoBuy→advance→autoOpen; ROUTER-04 → boxesPending() rngLock-aware + buys-pending true during rngLock + O(1); ROUTER-05 → _autoBuy refactor + dropped rngLock guard + unified bounty; NEW ROUTER-08 (RD-2 + batchPurchase:1737 removal + Q5) / ROUTER-09 (RD-3/RD-5) / ROUTER-10 (RD-4, reverses old R4); ADV-02/GAS-03 → mid-day mult=1 + GAS-03 satisfied-by-deletion; GAS-02 → flat-per-tx per-category; GAS-04 → stall ladder advance-only; GAS-05/WR-01 → re-prove no +EV under flat-per-tx; NEW GASOPT-03/04/05 (fold GASOPT-02 into GASOPT-03); TST-01 (autoBuy-during-rngLock safe / autoOpen-blocked-during-rngLock + no marooned boxes / unified-bounty one-category no-double-pay) + TST-04 (GASOPT-04 event→storage oracle migration net-zero). Phase 329 own coverage stays BATCH-01/ROUTER-07/ADV-04/GAS-03; ROUTER-08/09/10 + GASOPT-03/04/05 are Phase 330 IMPL reqs.
</design_anchors>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 329-SPEC.md §0 (attestation roll-up, incl. Q5 + D-05f) + §1 (REDESIGNED shared signatures: advanceGame (mult,rewardable) / parameterless doWork() + unrewarded escapes / NoWork / rngLock-aware O(1) views / D-07 flat-per-tx model)</name>
  <read_first>
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-03-PLAN.md (this plan — the design_anchors + wave1_inputs)
    - .planning/milestones/v48.0-phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md (the §0 carried-corrections + §1 R-row reconciliation format to mirror)
    - BOTH 329-ATTEST-*.md docs (the verdict inputs — every SHIFTED/ABSENT row, the Q5 verdict, the ROUTER-07/GAS-03/ADV-04/invariant-(c)-D-04a verdicts, the RD-5 entry-gate basis, the design-1-return / discovery-view verdicts, and the D-05f finding)
    - .planning/phases/329-.../329-01-SUMMARY.md + 329-02-SUMMARY.md (the consumable roll-ups)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-CONTEXT.md (D-07 flat-per-tx model + the faucet constraint; D-02 NoWork; RD-1..RD-5; the Claude's-Discretion return-encoding + discovery-view items)
    - .planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-ROUTER-REDESIGN-INTENT.md (the survivors-vs-reworked split + the autoBuy-gas analysis feeding the flat-per-tx ratios)
    - .planning/research/SUMMARY.md (the discovery-view shapes) + .planning/research/PITFALLS.md (the pitfall-to-prevention mapping)
  </read_first>
  <action>Create 329-SPEC.md with a header (Milestone v49.0 keeper-router REDESIGN, Baseline HEAD 0cc5d10f, Inputs = the redesign-intent doc + the milestone scope + research SUMMARY/ARCHITECTURE/PITFALLS + the 2 redesigned ATTEST docs, Owns = BATCH-01 + ROUTER-07 + ADV-04 + GAS-03, SOURCE-TREE not mutated at SPEC — the held-330 diff is the re-IMPL base, untouched here).
§0 ATTESTATION VERDICT ROLL-UP: fold the two ATTEST docs into one roll-up — the total IMPL-blocker count across both surfaces, the list of carried line-drift corrections (held-tree-vs-0cc5d10f; mirror the v48 C1..Cn style: e.g. the rngLock guard held :717 → 0cc5d10f :568; isOperatorApproved held :838 → :676; claimableWinningsOf held :854/:888 → :691/:722; AutoBought emit held :954 → :785 / decl :186 → :171; batchPurchase pre-checks held :1693/:1694 → :1737/:1738; autoOpen try/catch held :1701/1707/1719 → :1664/:1672/:1676; the _queueTickets liveness control is at contracts/storage/DegenerusGameStorage.sol:571), and a re-grep-at-edit-time note (the re-IMPL diff shifts lines). State explicitly: the Q5 no-other-dependent verdict, the ROUTER-07 no-guard-basis-holds (re-grounded on the unified creditFlip) verdict, the GAS-03 satisfied-by-deletion verdict, the ADV-04 no-new-in-window-read (autoBuy-pre-entropy) verdict, the RD-5 entry-gate-replicates-both-revert-sources verdict, the invariant-(c) D-04a fallback-callers-intact verdict, and the D-05f losing-bet-liveness finding (inert-safe OR carry the 329-02 SURFACE-TO-USER block verbatim — do NOT soften).
§1 SHARED SIGNATURES (BATCH-01 reconciliation, ROADMAP SC2) — a "## Shared Signatures" heading with one settled signature + decode/consume note per shared construct, mirroring v48's R-rows:
  R1 advanceGame return — the FINAL tuple (uint8 mult, bool rewardable) with mid-day mult=1 (D-07/ADV-05, no escalation); how AdvanceModule produces it (deleting the 3 creditFlip sites :189/:225/:468), and how the DegenerusGame :275 wrapper decodes the delegatecall data + forwards it to the IGame interface.
  R2 PARAMETERLESS doWork() + the ROUTER-06 NoWork() revert — settle doWork() (NO maxCount; determines its own fixed per-leg default batch; D-07 supersedes D-06), the priority routing autoBuy→advance→autoOpen (RD-1), the one-category early-return, the revert-NoWork()-when-all-3-predicates-empty signal (D-02), AND the standalone parametered+UNREWARDED autoOpen(count)/autoBuy(count) emergency escapes (RD-4: direct calls unrewarded). Note: there is NO maxCount==0 sentinel (the prior D-06 design is gone).
  R3 the rngLock-aware O(1) discovery views (ROUTER-04) — advanceDue() (new-day currentDayView():462 != dailyIdx OR mid-day partial-drain), boxesPending() (rngLock-aware per RD-3, covering mid-day-resolved rounds), buys-pending (AfKing-local cursor, TRUE during rngLock per RD-2), each O(1), each located (buys on AfKing-local; advance/boxes on DegenerusGame).
  R4 the UNIFIED bounty composition (RD-4) — the router pays exactly ONE creditFlip (in doWork, CEI-last) fed by legs that return raw counts (autoOpen/autoBuy) + the (mult,rewardable) tuple (advance); legs NEVER self-credit; direct calls unrewarded. The 3 advance creditFlips (:189/:225/:468) + the autoOpen in-callee creditFlip (:1676) + the autoBuy in-callee creditFlip (:846) are all pulled out. Confirm the KEEP-04 bytes32("DGNRS") affiliate passthrough survives the _autoBuy refactor (ROUTER-05 pre-condition) per the 329-01 verdict.
  R5 the D-07 FLAT-PER-TX bounty model — advance 2×·mult (mult = stall NEW-DAY only, mid-day=1), buy 1.5×, open 1× pro-rated below the knee (1× × min(opened,KNEE)/KNEE, KNEE≈5); ONE base unit (≈ a max-laden tx's 0.5-gwei gas worth of BURNIE flip) × PRICE_COIN_UNIT / mp; flag the 1× unit / the 1·1.5·2 ratios / the open KNEE as GAS-331 PLACEHOLDERS (starting estimates, NOT locked); record the faucet-safety constraint GAS-331 must satisfy + WR-01/GAS-05 re-prove (per-box reward below the knee ≤ a one-box tx's 0.5-gwei gas → tiny mid-day open −EV) + the advance/buy-no-gate doctrine.
For each R-row state the producing file and the consuming file(s) + the order to apply them inside the single re-IMPL diff. NO contracts/*.sol edit.</action>
  <verify>
    <automated>bash -c 'F=.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md; test -f "$F" && grep -q "Shared Signatures" "$F" && grep -q "0cc5d10f" "$F" && grep -Eqi "uint8 mult, bool rewardable|\(uint8.*bool rewardable\)" "$F" && grep -Eqi "parameterless|doWork\(\)" "$F" && grep -q "NoWork" "$F" && grep -Eqi "advanceDue|boxesPending|buys.pending" "$F" && grep -Eqi "rngLock-aware|rngLock.aware" "$F" && grep -Eqi "flat.per.tx|1.5|KNEE|placeholder" "$F" && grep -Eqi "ROUTER-07|GAS-03|ADV-04" "$F"'</automated>
  </verify>
  <acceptance_criteria>
    - 329-SPEC.md exists with a header naming baseline HEAD 0cc5d10f + the keeper-router-REDESIGN scope + Owns = BATCH-01/ROUTER-07/ADV-04/GAS-03 + a SOURCE-TREE-not-mutated (held-330 diff untouched) line.
    - §0 rolls up BOTH ATTEST docs into a single IMPL-blocker count + the carried held-tree-vs-0cc5d10f line-drift corrections + the Q5 / ROUTER-07(re-grounded) / GAS-03(satisfied-by-deletion) / ADV-04 / RD-5-entry-gate / invariant-(c)-D-04a verdicts + the D-05f finding (inert-safe or the carried SURFACE-TO-USER block, not softened).
    - A "## Shared Signatures" section settles ONE signature each for: R1 advanceGame (mult,rewardable)+mid-day-mult=1 + :275 wrapper decode, R2 parameterless doWork() + NoWork() + the standalone unrewarded autoOpen(count)/autoBuy(count) (NO maxCount==0 sentinel), R3 the rngLock-aware O(1) discovery views + their locations, R4 the unified single-creditFlip-in-doWork (the 5 pull-out sites) + no-self-credit + KEEP-04 affiliate survival, R5 the D-07 flat-per-tx model (advance 2×·mult / buy 1.5× / open 1× pro-rated, GAS-331 placeholders, the faucet constraint).
    - Every R-row names the producing + consuming files and an explicit apply-order within the single re-IMPL diff.
    - No contracts/*.sol modified.
  </acceptance_criteria>
  <done>329-SPEC.md §0 + §1 committed: attestation roll-up folded (incl. Q5 + D-05f), the REDESIGNED shared signatures (advanceGame (mult,rewardable) / parameterless doWork() + unrewarded escapes / NoWork / rngLock-aware O(1) views / D-07 flat-per-tx) settled with apply-order; zero .sol mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Author 329-SPEC.md §2 (the 4 invariants under the redesign + ROUTER-07 re-grounded + GAS-03 satisfied-by-deletion + the 5 RD changes + D-08 + the D-05 design-lock) + §3 (per-item IMPL blueprint + producer-before-consumer survivors-vs-reworked edit-order map)</name>
  <read_first>
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-03-PLAN.md (this plan)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md (§0 + §1 just authored by Task 1 — §2/§3 must be consistent with the settled signatures)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-CONTEXT.md (the LOCKED decisions RD-1..RD-5, D-07, D-08, D-01/ROUTER-07, D-03/GAS-03, D-04/D-04a, D-05 — §2 resolves exactly these)
    - .planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-ROUTER-REDESIGN-INTENT.md (the survivors-vs-reworked split — the §3 edit-order basis)
    - .planning/milestones/v48.0-phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md (§2 per-item IMPL blueprint + the "Files in the diff / Order" edit-order map format to mirror)
    - BOTH 329-ATTEST-*.md docs (the Q5 + per-leg no-untrusted-ETH-send rows feed ROUTER-07; the dual-epoch-deleted + totalFlipReversals + fallback-caller rows feed the invariants; the RD-5 entry-gate basis feeds invariant work; the D-05 rows feed the D-05 design-lock)
    - .planning/research/PITFALLS.md (Pitfalls 3/4/6/7/8) + .planning/PLAN-CRANK-DO-WORK-INCENTIVE.md (§7 faucet locks; §9 OPEN-C/OPEN-D)
  </read_first>
  <action>Append §2 and §3 to 329-SPEC.md.
§2 STRUCTURAL INVARIANTS + DISPOSITIONS + RD CHANGES + D-08 + D-05 DESIGN-LOCK — labeled subsections, each recording the lock + its source (D-NN / RD-N or the ATTEST grep verdict) + the IMPL instruction it produces:
  Invariant (a) ONE-CATEGORY STRUCTURAL EARLY-RETURN — doWork routes autoBuy→advance→autoOpen (RD-1) and RETURNS after the first rewarded category (a code invariant, not a comment; preserves the one-creditFlip-per-tx faucet bound; Pitfall 4). IMPL instruction: explicit early-return after the chosen category, single creditFlip after the return point.
  Invariant (b) FROZEN ADVANCE-CONSUME (ADV-04) — the totalFlipReversals nudge (AdvanceModule :1838/:1844) is frozen request→consume; under RD-1 autoBuy runs pre-entropy at day-open (rngLock false) BEFORE advance requests the word; advance consumes via the design-1 RETURN (R1) and adds NO new mutable in-window SLOAD (carry the 329-01 §F verdict; note the :270 read enumerated). IMPL instruction + the TST-01 freeze-fuzz handoff (incl. autoBuy-during-rngLock-safe + autoOpen-blocked-during-rngLock empirical proofs).
  Invariant (c) GUARANTEED FREE-FALLBACK CALLER (D-04/D-04a) — EXISTING structural paths ONLY (no new mechanism): PRIMARY the router advance leg (blocked while subscriber buys pend under autoBuy-first); SECONDARY the 30-min universal bypass (:1012) + Vault/sStonk gameAdvance() (:527/:421); TERTIARY the 120-day death-clock (:109/:1233/:1296). Carry the D-04a amendment: first-30-min advance during a buy backlog relies on SECONDARY not the bounty (buys drain monotonically, bounded delay, ACCEPTED); re-homing removes no structural caller.
  Invariant (d) SINGLE DAY-START EPOCH (GAS-03 / D-03) — SATISFIED BY DELETION: dropping the autoBuy stall multiplier (D-07) deletes AfKing's autoBuy stall ladder + absolute epoch (:823-846); advance becomes the SOLE stall epoch (AdvanceModule game-day :241-253); there are NO two epochs to collapse (D-03 dissolved). IMPL instruction: delete the AfKing stall ladder + absolute-day computation; advance is the only escalating path.
  RD CHANGES (LOCK each) — RD-1 (order, folds into invariant a), RD-2 (drop rngLock guards: AfKing :568 + game-side DegenerusGame :1737, KEEP gameOver :1738; carry the Q5 no-other-dependent verdict + the orphan-defended-on-the-resolution-side basis), RD-3 (boxesPending rngLock-aware + block autoOpen during rngLock, still opens mid-day-resolved boxes), RD-4 (unify the bounty into doWork; reverses old R4), RD-5 (drop autoOpen try/catch + entry-gate `if (rngLocked() || _livenessTriggered()) return;` + make _autoOpenBox INTERNAL; basis = the EXACTLY-TWO revert sources rngLock + the _queueTickets liveness control at contracts/storage/DegenerusGameStorage.sol:571; the terminal-jackpot guard stays for direct openLootBox; carry the USER-accepted frozen-contract trade).
  Disposition ROUTER-07 (D-01) — NO nonReentrant guard on doWork, RE-GROUNDED on the unified single creditFlip (under RD-4 exactly ONE creditFlip in doWork, CEI-last, fed by non-self-crediting legs — STRONGER than per-leg bounties); formal basis = keeper-never-a-payee + no untrusted ETH send (per-leg + single-creditFlip grep D-01a, carry the 329-01 §B rows) + one-category structural early-return + single-creditFlip-last CEI (AfKing :100). D-01b: the TST-02 router→game→creditFlip double-pay regression stays as the empirical backstop scoped to PROVE the no-guard disposition (near-trivial under the unified model). If 329-01 flagged any untrusted-push leg, carry it as a ROUTER-07 blocker.
  Disposition GAS-03 — satisfied-by-deletion (folds into invariant d; cross-reference, no duplicate).
  D-08 GASOPT-03/04/05 — LOCK as v49.0 design items (code at 330, REQ-IDs registered by Task 3): GASOPT-03 batched keeper read (batchPurchaseForKeeper/keeperSnapshot collapsing claimableWinningsOf :691/:722; SUBSUMES GASOPT-02); GASOPT-04 drop AutoBought (:171/:785) + the test-oracle migration to lastAutoBoughtDay (the AfKing test files, the no-double-buy invariant re-expressed in storage/balance-delta terms WITHOUT weakening SAFE-03 / H-CANCEL-SWAP); GASOPT-05 drop per-iteration isOperatorApproved(player) :676 + KEEP subscribe-time :401 + the 333-SWEEP-re-attests-the-4-OPEN-E-protections BLOCKING-CONDITION (reverted before milestone ship if it fails).
  D-05 DEGENERETTE-RESOLVE DESIGN-LOCK — LOCK the rename (autoResolve→degeneretteResolve + _autoResolveBet→_degeneretteResolveBet, D-05a; NO interface row — defined on DegenerusGame.sol) + the flat ~1-BURNIE once/tx, ≥3-NON-WWXRP gate, revert NoWork() at 0, lean (D-05b) + the D-05c real-gas basis + WWXRP-excluded + AUTO-02/REW-04 preserved (D-05d) + the GAS sanity-check handoff + router-fold-OUT as a v49.0 design item (code at 330/BATCH-02; REQ GAS-06/TST-05 at 331/332). Carry the 329-02 D-05f finding: inert-safe recorded, or the SURFACE-TO-USER block carried verbatim + marked a USER-decision gate for 330.
§3 PER-ITEM IMPL BLUEPRINT + EDIT-ORDER MAP — a "## Files in the diff" list distinguishing SURVIVORS (carry from the held diff: advance bounty re-home + (mult,rewardable) tuple; degeneretteResolve rename + ≥3 re-peg; GASOPT-01 MintModule [rk] hoists; the interface tuple + advanceDue()/boxesPending() views) from REWORKED (re-derive: AfKing parameterless doWork + order + unified flat-per-tx bounty; _autoBuy drop-guard; DegenerusGame.autoOpen rngLock-block + drop-try/catch + entry-gate + return-reward; autoBuy return-reward; the interface autoOpen/autoBuy signatures; boxesPending rngLock-aware; GASOPT-03/04/05), with the producer-before-consumer ORDER (AdvanceModule delete 3 creditFlip sites + (mult,rewardable) return → DegenerusGame wrapper decode + rngLock-aware views + autoOpen rework + degeneretteResolve rename + GASOPT-01 MintModule pointer + GASOPT-03 keeperSnapshot → interfaces advanceGame tuple + autoOpen/autoBuy return shapes + views + keeperSnapshot → AfKing parameterless doWork router + _autoBuy refactor + drop guards + unified flat-per-tx + GASOPT-04/05 → tests rename-fixes + GASOPT-04 oracle migration) so no intermediate state ships where advancing is unrewarded; then one blueprint paragraph per work-area. Note the flat-per-tx peg (1× unit / ratios / KNEE) + the ~1-BURNIE constant are SPEC PLACEHOLDERS calibrated at GAS (331). End with a §N success-criteria checklist mapped 1:1 to the UPDATED ROADMAP Phase 329 SC1..SC4 (Task 3 updates the SCs; this checklist references the new wording) + a "SOURCE-TREE not mutated at SPEC (held-330 diff untouched)" line. NO contracts/*.sol edit.</action>
  <verify>
    <automated>bash -c 'F=.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md; test -f "$F" && grep -Eqi "early-return|early return" "$F" && grep -q "totalFlipReversals" "$F" && grep -Eqi "satisfied by deletion|sole stall epoch" "$F" && grep -Eqi "death.clock|30 minutes|gameAdvance" "$F" && grep -q "nonReentrant" "$F" && grep -Eqi "RD-1|RD-2|RD-3|RD-4|RD-5" "$F" && grep -Eqi "GASOPT-03|GASOPT-04|GASOPT-05" "$F" && grep -q "degeneretteResolve" "$F" && grep -Eqi "Files in the diff|edit.order|survivors" "$F" && grep -Eqi "placeholder" "$F" && grep -Eqi "SC1|SC4|success.criteria" "$F"'</automated>
  </verify>
  <acceptance_criteria>
    - §2 LOCKS each of the 4 structural invariants under the REDESIGN with a labeled subsection citing its decision id + ATTEST verdict + the IMPL instruction: (a) one-category early-return on autoBuy→advance→autoOpen, (b) frozen advance-consume (autoBuy-pre-entropy + ADV-04 + TST-01 handoff), (c) free-fallback caller D-04a (PRIMARY blocked-while-buys-pend / SECONDARY / TERTIARY), (d) single epoch SATISFIED BY DELETION (autoBuy stall ladder deleted, advance the sole epoch).
    - §2 LOCKS the 5 RD changes (RD-1 order / RD-2 dropped guards + Q5 verdict / RD-3 boxesPending rngLock-aware / RD-4 unify bounty / RD-5 try/catch-drop + entry-gate at storage/DegenerusGameStorage.sol:571) and RESOLVES ROUTER-07 = NO guard re-grounded on the unified single creditFlip (D-01a + D-01b backstop); GAS-03 cross-referenced to invariant (d).
    - §2 LOCKS the D-08 GASOPT-03/04/05 (with the GASOPT-04 no-double-buy migration constraint + the GASOPT-05 333-SWEEP blocking-condition) and the D-05 degeneretteResolve design item (rename + flat ~1-BURNIE/≥3-NON-WWXRP-gate/revert-on-no-work + D-05c + D-05f finding + router-fold-OUT); a surfaced losing-bet dependency is carried verbatim as a USER gate, not softened.
    - §3 has a "## Files in the diff" producer-before-consumer edit-order map distinguishing SURVIVORS from REWORKED (AdvanceModule → Game wrapper/views/autoOpen-rework/rename → interfaces → AfKing parameterless-doWork/_autoBuy/drop-guards/unified-flat-per-tx/GASOPT-04/05 → tests) + one blueprint paragraph per work-area; the flat-per-tx peg + ~1-BURNIE constant flagged SPEC placeholders calibrated at GAS.
    - A success-criteria checklist maps to the UPDATED ROADMAP Phase 329 SC1..SC4 + a SOURCE-TREE-not-mutated (held-330-untouched) line.
    - No contracts/*.sol modified.
  </acceptance_criteria>
  <done>329-SPEC.md §2 + §3 committed: the 4 invariants locked under the redesign (incl. GAS-03 satisfied-by-deletion + D-04a free-fallback), the 5 RD changes + ROUTER-07 re-grounded + D-08 + the D-05 design item locked, the producer-before-consumer survivors-vs-reworked edit-order map produced as the Phase 330 re-IMPL input, SC1..SC4 checklist present; zero .sol mutation.</done>
</task>

<task type="auto">
  <name>Task 3: Amend .planning/REQUIREMENTS.md + .planning/ROADMAP.md to the redesign (the CONTEXT-enumerated list; Phase 329 goal/SCs; register ROUTER-08/09/10 + GASOPT-03/04/05; count 31 → ~34)</name>
  <read_first>
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-03-PLAN.md (this plan — the design_anchors "mandatory amendments" block)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-CONTEXT.md (the <decisions> "REQUIREMENTS.md / ROADMAP.md amendments the planner MUST make" enumerated list — the binding edit list)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md (§0/§1/§2/§3 — the amendments must match the SPEC's settled wording)
    - .planning/REQUIREMENTS.md (the full current v49.0 requirements + Traceability + per-phase count + Out-of-Scope — the edit target)
    - .planning/ROADMAP.md (the v49.0 section: Phase 329 Goal + SC1..SC4, the Phase 330/331/332 Requirements lines, the Coverage + per-category split tables, the Phase 329 plan list — the edit target)
  </read_first>
  <action>Apply the CONTEXT-enumerated amendments to BOTH .planning/REQUIREMENTS.md and .planning/ROADMAP.md. Use the Edit tool for surgical edits (do NOT rewrite whole files).
REQUIREMENTS.md:
  (1) ROUTER-01 → reword to parameterless doWork() (determines its own fixed per-leg default batch; DROP the maxCount==0 sentinel + the ~10M-budget language; ADD the standalone parametered+UNREWARDED autoOpen(count)/autoBuy(count) emergency path).
  (2) ROUTER-02 → reword the routing order to autoBuy → advance → autoOpen (RD-1).
  (3) ROUTER-04 → add boxesPending() is rngLock-aware (RD-3, covers mid-day-resolved rounds) + buys-pending true during rngLock (RD-2); still O(1).
  (4) ROUTER-05 → reword to _autoBuy refactor + the dropped rngLock guard (RD-2) + the unified bounty (RD-4); degeneretteResolve rename/re-peg unchanged (D-05).
  (5) ADD three NEW REQ-IDs under the ROUTER category: ROUTER-08 (RD-2 autoBuy=normal-buy / drop the AfKing :568 + game-side DegenerusGame :1737 rngLock guards / KEEP gameOver :1738 / the Q5 batchPurchase-dependent grep), ROUTER-09 (RD-3/RD-5: block autoOpen during rngLock + drop the try/catch + the entry-gate on `rngLocked()||_livenessTriggered()` + make _autoOpenBox internal), ROUTER-10 (RD-4: unify the bounty into doWork — reverses the old per-leg-bounty model). Mark all three Phase 330 (IMPL).
  (6) ADV-02 → add advance returns mult with mid-day mult=1 (no escalation, ADV-05). GAS-03 → reword to "satisfied by deletion — advance is the sole stall epoch" (the dual-epoch dissolved by D-07).
  (7) GAS-02 → reword from per-item MARGINAL break-even to flat-per-tx per-category (advance 2×·mult, buy 1.5×, open 1× pro-rated below the knee). GAS-04 → stall ladder (1/2/4/6) kept, ADVANCE-ONLY. GAS-05/WR-01 → re-prove no +EV loop under flat-per-tx (esp. the open small-batch + low-gas corner).
  (8) ADD three NEW REQ-IDs under a GASOPT category: GASOPT-03 (batched keeper read, SUBSUMES GASOPT-02), GASOPT-04 (drop the per-player AutoBought event + the test-oracle migration), GASOPT-05 (drop the per-iteration isOperatorApproved(player) :676, KEEP subscribe-time :401; 333-SWEEP re-attests the 4 OPEN-E protections — blocking-condition). FOLD GASOPT-02 into GASOPT-03 (reword GASOPT-02 to point at GASOPT-03, or mark it SUBSUMED). Mark GASOPT-03/04/05 Phase 330 (IMPL).
  (9) TST-01 → add: autoBuy-during-rngLock safe / autoOpen-blocked-during-rngLock + no marooned boxes / unified-bounty one-category + no-double-pay (Q4). TST-04 → add: the GASOPT-04 test-oracle migration (event → storage/balance) keeps the suite net-zero vs the v48 baseline.
  (10) Update the Traceability table (add ROUTER-08/09/10 + GASOPT-03/04/05 rows mapped to their phases), the per-phase count line, and the count header (31 → ~34). The D-06 refinement note becomes stale — reword it to "D-06 superseded by D-07 parameterless doWork()".
ROADMAP.md:
  (11) Update the Phase 329 GOAL + Success Criteria SC1..SC4 to the redesign (they currently describe doWork(maxCount), GAS-03 epoch-collapse, advance→open→buy, dual-epoch single-source): SC1 = the 4 invariants under autoBuy→advance→autoOpen with single-epoch SATISFIED BY DELETION + free-fallback under autoBuy-first (D-04a); SC2 = parameterless doWork() + (mult,rewardable) + rngLock-aware views; SC3 = ROUTER-07 no-guard re-grounded on the unified creditFlip; SC4 = grep-attested-vs-0cc5d10f with the corrected anchors (the 3 advance creditFlips :189/:225/:468 + the pulled-out open :1676 + buy :846 + the RD-2 :568/:1737 guards + the GASOPT baselines).
  (12) Update the Phase 330 Requirements line to add ROUTER-08/09/10 + GASOPT-03/04/05 (and reword the existing ROUTER/ADV/GAS reqs to match). Update the Phase 331 (GAS-02/04/05 reworded) + Phase 332 (TST-01/04 additions) lines as needed.
  (13) Update the Coverage section + the per-category split table + the per-phase count (31 → ~34; ROUTER 7→10, GASOPT 2→4-with-02-subsumed-or-3-net; recompute the totals consistently) + the milestone-creation footnote noting the keeper-router-REDESIGN re-spec.
  (14) Update the Phase 329 plan list (it currently describes the OLD ATTEST/SPEC content) to the redesigned 329-01/02/03 objectives.
Keep Phase 329's OWN plan-coverage at BATCH-01/ROUTER-07/ADV-04/GAS-03 — the new ROUTER-08/09/10 + GASOPT-03/04/05 are Phase 330 IMPL reqs (registered, NOT phase-329 targets). After editing, run a self-consistency check: the per-phase counts sum to the new total, and every REQ-ID appears in exactly one phase. NO contracts/*.sol edit.</action>
  <verify>
    <automated>bash -c 'R=.planning/REQUIREMENTS.md; M=.planning/ROADMAP.md; grep -q "ROUTER-08" "$R" && grep -q "ROUTER-09" "$R" && grep -q "ROUTER-10" "$R" && grep -q "GASOPT-03" "$R" && grep -q "GASOPT-04" "$R" && grep -q "GASOPT-05" "$R" && grep -Eqi "parameterless" "$R" && grep -q "ROUTER-08" "$M" && grep -q "GASOPT-03" "$M" && grep -Eqi "satisfied by deletion" "$M" && ! grep -q "doWork(maxCount)" <(sed -n "/### Phase 329:/,/### Phase 330:/p" "$M")'</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md: ROUTER-01 (parameterless doWork(), maxCount==0 sentinel dropped), ROUTER-02 (order autoBuy→advance→autoOpen), ROUTER-04 (boxesPending rngLock-aware + buys-pending-during-rngLock), ROUTER-05 (_autoBuy refactor + dropped guard + unified bounty) are reworded; NEW ROUTER-08/09/10 + GASOPT-03/04/05 are added (mapped Phase 330); GASOPT-02 folded into GASOPT-03; ADV-02 (mid-day mult=1), GAS-03 (satisfied-by-deletion), GAS-02 (flat-per-tx), GAS-04 (advance-only), GAS-05 (re-prove under flat-per-tx), TST-01/TST-04 additions are reworded; the Traceability + per-phase count + count header are updated (31 → ~34) consistently.
    - ROADMAP.md: the Phase 329 GOAL + SC1..SC4 are updated to the redesign (no `doWork(maxCount)` / epoch-collapse / advance→open→buy / dual-epoch language remains in the Phase 329 block); the Phase 330 Requirements line adds ROUTER-08/09/10 + GASOPT-03/04/05; the Coverage + per-category split + per-phase count tables are updated to the new total; the Phase 329 plan list reflects the redesigned 329-01/02/03 objectives.
    - Phase 329's own coverage stays BATCH-01/ROUTER-07/ADV-04/GAS-03; the new reqs are Phase 330 IMPL reqs (not phase-329 targets); every REQ-ID maps to exactly one phase and the per-phase counts sum to the new total.
    - The automated check confirms ROUTER-08/09/10 + GASOPT-03/04/05 + "parameterless" in REQUIREMENTS.md, ROUTER-08 + GASOPT-03 + "satisfied by deletion" in ROADMAP.md, and that the Phase 329 ROADMAP block no longer contains "doWork(maxCount)".
    - No contracts/*.sol modified.
  </acceptance_criteria>
  <done>REQUIREMENTS.md + ROADMAP.md amended to the keeper-router redesign: ROUTER-01/02/04/05 reworded + ROUTER-08/09/10 + GASOPT-03/04/05 registered (GASOPT-02 subsumed) + ADV-02/GAS-02/03/04/05 + TST-01/04 reworded + the Phase 329 goal/SCs updated + the Coverage/Traceability/count tables updated (31 → ~34) consistently; Phase 329 own coverage unchanged; zero .sol mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Paper-only reconciliation + doc-amendment — ZERO code mutation this phase; the SPEC LOCKS the
mitigations for the re-IMPL (330) to apply and GAS/TST/TERMINAL (331/332/333) to prove. The boundary is
the SPEC + the amended roadmap/requirements: an under-specified shared signature, an unlocked structural
invariant, a softened disposition, or a stale-design requirement left un-amended lets a conflicting
intermediate diff, a faucet, a freeze-break, a silent liveness regression, or a coverage gap slip into
Phase 330's re-IMPL diff. This plan consumes the Wave-1 attestation evidence and must not contradict it.

| Boundary | Description |
|----------|-------------|
| ATTEST verdicts → SPEC settled signatures/invariants | A signature settled ignoring a co-edit, or an invariant left as a comment not a code lock, produces a conflicting/broken re-IMPL diff |
| decision (RD-N / D-NN) → IMPL instruction | A disposition softened (e.g. ROUTER-07 no-guard without the per-leg basis, the RD-5 entry-gate without the two-revert-source proof, or a buried D-05f dependency) ships the wrong behavior |
| the CONTEXT amendment list → REQUIREMENTS.md + ROADMAP.md | A stale-design requirement (doWork(maxCount), GAS-03 epoch-collapse) left un-amended, or a new REQ (ROUTER-08/09/10, GASOPT-03/04/05) unregistered, leaves the IMPL phase under-specified or a coverage gap |
| design-1 return + parameterless doWork() → all downstream files | A broken intermediate (advance unrewarded before the router pays) if the edit-order is not producer-before-consumer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-329-R1 | Tampering | A shared signature (advanceGame return / parameterless doWork / rngLock-aware views) settled inconsistently with the ATTEST verdicts → re-IMPL produces a conflicting or broken-intermediate diff | mitigate | §1 R-rows each name the producing + consuming files + an explicit apply-order; §3 enforces producer-before-consumer (AdvanceModule → Game → interfaces → AfKing → tests) so advancing is never unrewarded mid-diff |
| T-329-R2 | Spoofing / Elevation | ROUTER-07 no-guard recorded WITHOUT the per-leg + single-creditFlip no-untrusted-ETH-send basis (Pitfall 6 composed reentrancy) | mitigate | §2 carries the 329-01 §B per-leg + single-creditFlip rows verbatim as the no-guard basis (D-01a, re-grounded on the unified model) + the D-01b TST-02 double-pay empirical backstop; any untrusted-push leg carried as a blocker |
| T-329-R3 | Tampering (RNG) | Invariant (b) left as prose, not a locked router-ordering instruction → the freeze invariant is not enforced under the new autoBuy-first composition (Pitfall 3, ADV-04, v45 floor) | mitigate | §2 invariant (b) locks the autoBuy-pre-entropy-at-day-open ordering (consume reads only frozen state via the design-1 return, no new in-window SLOAD) + hands the empirical freeze-fuzz to TST-01 |
| T-329-R4 | Denial of service (brick / liveness) | The RD-5 entry-gate recorded without the EXACTLY-TWO-revert-source proof → a third revert source bricks the autoOpen tail; OR a surfaced D-05f losing-bet-liveness dependency softened/buried | mitigate | §2 carries the 329-01 RD-5 two-revert-source verdict (rngLock + the _queueTickets liveness control at storage/DegenerusGameStorage.sol:571) + the USER-accepted frozen-contract trade; §0 + §2 carry the 329-02 D-05f SURFACE-TO-USER block verbatim as a USER-decision gate |
| T-329-R5 | Repudiation / coverage gap | A stale-design requirement (doWork(maxCount), GAS-03 epoch-collapse, advance→open→buy) left un-amended in REQUIREMENTS/ROADMAP, or a new REQ (ROUTER-08/09/10, GASOPT-03/04/05) unregistered → the IMPL phase improvises or a req is uncovered | mitigate | Task 3 applies the full CONTEXT-enumerated amendment list + the Phase 329 goal/SC update + registers the new REQ-IDs + a self-consistency check (per-phase counts sum to the new total; each REQ in exactly one phase) |
| T-329-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this paper-only plan; RESEARCH Package Legitimacy Gate N/A |
</threat_model>

<verification>
- 329-SPEC.md exists with §0 (attestation roll-up, incl. Q5 + D-05f), §1 (Shared Signatures R-rows w/ apply-order — advanceGame (mult,rewardable) / parameterless doWork() + unrewarded escapes / NoWork / rngLock-aware O(1) views / D-07 flat-per-tx), §2 (4 invariants under the redesign + ROUTER-07 re-grounded + GAS-03 satisfied-by-deletion + the 5 RD changes + D-08 + the D-05 design-lock), §3 (IMPL blueprint + producer-before-consumer survivors-vs-reworked edit-order map).
- All 4 SPEC-owned requirements (BATCH-01 + ROUTER-07 + ADV-04 + GAS-03) are resolved in writing under the redesign.
- The D-05f losing-bet-liveness finding from Plan 02 is folded into §0 + §2 (inert-safe recorded, or the SURFACE-TO-USER block carried verbatim).
- REQUIREMENTS.md + ROADMAP.md are amended per the CONTEXT-enumerated list (ROUTER-01/02/04/05 reworded + ROUTER-08/09/10 + GASOPT-03/04/05 registered + ADV-02/GAS-02/03/04/05 + TST-01/04 + the Phase 329 goal/SCs updated + the Coverage/Traceability/count tables 31 → ~34); Phase 329's own coverage stays BATCH-01/ROUTER-07/ADV-04/GAS-03.
- The 4 structural invariants are locked as code invariants (not comments); the no-guard disposition rests on the re-grounded unified-creditFlip basis; GAS-03 is satisfied-by-deletion; the edit-order is producer-before-consumer survivors-vs-reworked.
- Zero contracts/*.sol modified: `git status --porcelain -- 'contracts/*.sol'` is UNCHANGED from planning.
</verification>

<success_criteria>
- The 4 structural invariants locked in writing under the REDESIGN (SC1 / BATCH-01).
- The REDESIGNED shared signatures settled (SC2): advanceGame (mult,rewardable) / parameterless doWork() + unrewarded escapes / NoWork / rngLock-aware O(1) views / D-07 flat-per-tx.
- The two dispositions resolved (SC3): ROUTER-07 no-guard re-grounded on the unified creditFlip + GAS-03 satisfied-by-deletion.
- Every cited file:line verdict folded; no un-grepped "by construction" survives (SC4, leaning on Wave 1) + the producer-before-consumer survivors-vs-reworked edit-order map produced.
- The 5 RD changes + D-08 GASOPT-03/04/05 + the D-05 degeneretteResolve design item locked + the D-05f finding carried (not softened).
- REQUIREMENTS.md + ROADMAP.md amended to the redesign (incl. the Phase 329 goal/SCs; ROUTER-08/09/10 + GASOPT-03/04/05 registered; count 31 → ~34) consistently.
- ZERO contracts/*.sol mutation (paper-only phase; the held-330 diff untouched).
</success_criteria>

<output>
Create `.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-03-SUMMARY.md` when done, listing 329-SPEC.md's sections, the 4 locked structural invariants under the redesign, the settled REDESIGNED shared signatures, the ROUTER-07(re-grounded)/GAS-03(satisfied-by-deletion) dispositions, the 5 RD changes + D-08 + the D-05 design-lock + the D-05f finding, the REQUIREMENTS.md/ROADMAP.md amendments applied (ROUTER-08/09/10 + GASOPT-03/04/05 + the Phase 329 goal/SC update + the count 31 → ~34), and confirming SC1..SC4 coverage for the Phase 329 verification.
</output>
