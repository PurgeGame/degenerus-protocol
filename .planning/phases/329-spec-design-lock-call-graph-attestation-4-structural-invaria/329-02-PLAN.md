---
phase: 329-spec-design-lock-call-graph-attestation-4-structural-invaria
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-DEGENERETTE-RESOLVE.md
autonomous: true
requirements: [BATCH-01]
must_haves:
  truths:
    - "Every autoResolve→degeneretteResolve rename-surface anchor is grep-attested at 0cc5d10f: external autoResolve DegenerusGame.sol:1587, the onlySelf internal wrapper _autoResolveBet :1684, the per-item peg _ethToBurnieValue(AUTO_RESOLVE_BET_GAS_UNITS * AUTO_GAS_PRICE_REF, ...) :1612, the AUTO-02 probe BatchAlreadyTaken :1596, the per-item try/catch isolation, the WWXRP (currency==3) exclusion (AUTO-04), self-resolve-allowed (REW-04), and the IDegenerusGame / IDegenerusGameModules signature + every caller/test referencing autoResolve / _autoResolveBet (D-05a rename closure)"
    - "The D-05f losing-bet-liveness grep-verification is PERFORMED and recorded: every degeneretteBets[player][betId] consumer/cleanup/accounting/RNG-slot path is enumerated (write :526, read :605/:1601, delete :634, view :2319) with an explicit finding — does ANY invariant/accounting/RNG-slot/cleanup path REQUIRE losing Degenerette bets to be resolved? If inert cruft → safe-to-drop-the-break-even-incentive; if a backlog/invariant risk → flagged SURFACE-TO-USER (do NOT silently starve a needed path)"
    - "The D-05c CORRECTED real-gas exploitability basis is recorded: ~1 BURNIE flip-credit ≤ mintPrice/1000 ETH (≤0.00024 ETH even at the 0.24-ETH milestone price) AND illiquid (coinflip-locked), compared against REAL prevailing tx gas (5–50+ gwei, base 21k + ≥3 resolutions + overhead) — NOT the 0.5-gwei AUTO_GAS_PRICE_REF peg constant (the corrected basis per feedback_bounty_exploit_uses_real_gas_not_peg_ref) → every qualifying tx is a net loss → no positive-EV farm"
    - "The D-05b payment shape + gate is attested as implementable on the existing surface: flat literal ~1 BURNIE (1e18) creditFlip ONCE per tx (count-independent), gated at ≥3 successfully-resolved NON-WWXRP bets, revert NoWork() at ZERO resolved, lean = resolve-always-pay-at-≥3-revert-only-at-0 (1–2 resolved → resolved-but-unpaid, NOT reverted so a trailing tail is never stranded) — confirm the per-item resolution loop + the NON-WWXRP success-counting + the revert/unpaid boundary are expressible against the current _autoResolveBet/resolveBets structure"
    - "degeneretteResolve is confirmed architecturally NON-foldable into the router (ROUTER-05 / D-05): degeneretteBets[player][betId] has NO O(1) on-chain enumeration — it requires caller-supplied (players[], betIds[]) from off-chain indexing; on-chain enumeration = unbounded scan = violates ROUTER-04, so it stays a SEPARATE call and the unified one-button is a frontend concern"
  artifacts:
    - path: ".planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-DEGENERETTE-RESOLVE.md"
      provides: "The autoResolve→degeneretteResolve rename-surface attestation, the D-05f losing-bet-liveness grep-finding (the load-bearing deliverable), the D-05c real-gas exploitability basis, the D-05b flat-~1-BURNIE/≥3-gate/revert-on-no-work shape feasibility, and the architectural-non-foldability confirmation — all at HEAD 0cc5d10f"
      contains: "SURFACE-TO-USER"
  key_links:
    - from: "the flat ~1 BURNIE (1e18) flip-credit per tx"
      to: "REAL prevailing tx gas (5–50+ gwei) for a ≥3-resolution call"
      via: "the D-05c corrected real-gas (NOT 0.5-gwei-peg) net-loss derivation"
      pattern: "net loss|real gas|mintPrice/1000|illiquid"
    - from: "the flat count-independent reward (dropping the per-item break-even incentive)"
      to: "every degeneretteBets cleanup/accounting/RNG-slot/invariant consumer"
      via: "the D-05f losing-bet-liveness grep-enumeration"
      pattern: "delete degeneretteBets|SURFACE-TO-USER|inert"
---

<objective>
Re-attest the load-bearing **`autoResolve` → `degeneretteResolve` rename + flat ~1-BURNIE "lose"
re-peg** (the D-05 family — scope-added at the Phase 329 discussion, code lands at Phase 330
BATCH-02, REQ-IDs GAS-06/TST-05 owned by Phases 331/332) against the v48.0-closure HEAD
`0cc5d10fbc1232a6d2e7b0464fe21541b9812029` (`0cc5d10f`). Produce `329-ATTEST-DEGENERETTE-RESOLVE.md`
covering:
(A) the **rename surface** (D-05a) — every `autoResolve` / `_autoResolveBet` anchor + interface +
    caller/test, so the mechanical rename is fully enumerated;
(B) the **D-05f losing-bet-liveness grep-verification** (the single most important deliverable of this
    plan) — does ANY invariant/accounting/RNG-slot/cleanup path REQUIRE losing Degenerette bets to be
    resolved before the per-item break-even incentive is dropped? Inert cruft → safe; backlog/invariant
    risk → SURFACE-TO-USER;
(C) the **D-05c CORRECTED real-gas exploitability basis** — ~1 BURNIE vs REAL prevailing tx gas
    (NOT the 0.5-gwei `AUTO_GAS_PRICE_REF` peg constant — the USER-corrected basis,
    `feedback_bounty_exploit_uses_real_gas_not_peg_ref`);
(D) the **D-05b payment-shape feasibility** — flat literal ~1 BURNIE once per tx, ≥3-NON-WWXRP gate,
    revert `NoWork()` at zero, lean resolve-always-pay-at-≥3-revert-only-at-0 — expressible on the
    current resolution structure;
(E) the **architectural non-foldability** (ROUTER-05 / D-05) — `degeneretteBets[player][betId]` has no
    O(1) enumeration → stays a SEPARATE call, router-fold OUT.

Purpose: D-05 is LOCKED design, but the SPEC MUST grep-verify it from live source — especially the
D-05f liveness nuance (a flat "lose" removes the rational-keeper incentive to resolve LOSING bets; if
any path needs them resolved, that must surface, not silently starve). This is the BATCH-01
attestation half for the load-bearing D-05 item — the security/liveness analog of v48's SWAP plan.

Output: one `329-ATTEST-DEGENERETTE-RESOLVE.md` under the phase directory. ZERO `contracts/*.sol` mutation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-CONTEXT.md

# The locked milestone design (D-05 family lives in CONTEXT <decisions>; the GAS-06/TST-05 scope in REQUIREMENTS/ROADMAP):
@.planning/research/SUMMARY.md

# The v48 precedent ATTEST format to mirror — the LOAD-BEARING security/economics attestation with a STOP/SURFACE rule:
@.planning/milestones/v48.0-phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-SWAP.md

<attestation_method>
The current working tree's `contracts/` is byte-identical to the baseline HEAD `0cc5d10f` (verified
during planning: `git diff --name-only 0cc5d10f HEAD -- contracts/` returns ZERO files). Grep the LIVE
tree; verdicts are baseline-anchored. For belt-and-suspenders: `git show 0cc5d10f:contracts/<path> |
grep -n`. Read source from `contracts/` ONLY — stale copies elsewhere must be ignored
(`feedback_contract_locations`). State the byte-identical-to-0cc5d10f fact in the doc header.

The D-05f losing-bet-liveness grep-verification is the load-bearing deliverable. If the enumeration
finds ANY invariant/accounting/RNG-slot/cleanup path that REQUIRES losing Degenerette bets to be
resolved, emit a `## SURFACE-TO-USER — LOSING-BET LIVENESS DEPENDENCY` block naming the path and the
risk; do NOT silently record "safe". The D-05c real-gas basis MUST compare ~1 BURNIE against REAL
prevailing gas (5–50+ gwei), NEVER the 0.5-gwei `AUTO_GAS_PRICE_REF` peg constant
(`feedback_bounty_exploit_uses_real_gas_not_peg_ref` — the USER corrected this twice).
</attestation_method>

<interfaces>
<!-- Grep-confirmed D-05 anchors at the baseline 0cc5d10f (verified during planning). Executor extends, does not re-discover from scratch. -->
DegenerusGame.sol:
  :1539  `uint256 private constant AUTO_GAS_PRICE_REF = 0.5 gwei;` (the PEGGING reference — D-05c: NOT real gas)
  :1545  `uint256 private constant AUTO_RESOLVE_BET_GAS_UNITS = 66_528;` (the per-item peg basis being REMOVED for a flat literal)
  :1578  comment: `(degeneretteBets[players[0]][betIds[0]] == 0) a competitor got ahead` (AUTO-02 probe doc)
  :1587  `function autoResolve(` external — RENAME TARGET → degeneretteResolve (D-05a); takes caller-supplied (players[], betIds[])
  :1596  `if (degeneretteBets[players[0]][betIds[0]] == 0) revert BatchAlreadyTaken();` (AUTO-02 probe — preserved D-05d)
  :1601  `uint256 betPacked = degeneretteBets[players[i]][betIds[i]];` (the per-item loop read)
  :1612  `AUTO_RESOLVE_BET_GAS_UNITS * AUTO_GAS_PRICE_REF,` (the per-item break-even peg being REPLACED by the flat ~1 BURNIE)
  :1684  `function _autoResolveBet(address player, uint64 betId) external` onlySelf wrapper — RENAME TARGET → _degeneretteResolveBet (D-05a); the per-item try/catch isolation callee
  :2319  `return degeneretteBets[player][betId];` (a public view getter)
DegenerusGameDegeneretteModule.sol:
  :526   `degeneretteBets[player][nonce] = packed;` (bet SUBMISSION — the prepay/purchase-gate that creates a resolvable item)
  :605   `uint256 packed = degeneretteBets[player][betId];` (resolution read)
  :634   `delete degeneretteBets[player][betId];` (the resolution CLEANUP — the D-05f liveness anchor: who/what depends on this delete happening for LOSING bets?)
  (executor greps: resolveBets / _resolveFullTicketBet, the WWXRP currency==3 exclusion + zero-reward, the success-count / per-item try/catch, any pendingDegenerette / outstanding-bet counter / RNG-slot reuse keyed on degeneretteBets, any gameOver/sweep/cleanup that iterates or requires-empty degeneretteBets)
contracts/interfaces/IDegenerusGame.sol + contracts/interfaces/IDegenerusGameModules.sol:
  the autoResolve / _autoResolveBet signatures (RENAME TARGETS — grep both interface files + any test/ referencing autoResolve)
Pricing peg basis (D-05c): mintPrice() (DegenerusGame.sol:2398) — the ~1 BURNIE ≤ mintPrice/1000 ETH derivation; PRICE_COIN_UNIT / _ethToBurnieValue conversion.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attest the autoResolve→degeneretteResolve rename surface (D-05a) + the D-05b flat-~1-BURNIE/≥3-gate/revert-on-no-work shape feasibility + architectural non-foldability (ROUTER-05) → 329-ATTEST-DEGENERETTE-RESOLVE.md sections A/B/E</name>
  <read_first>
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-02-PLAN.md (this plan, the `<interfaces>` block + attestation_method)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-CONTEXT.md (D-05/D-05a/D-05b/D-05d/D-05g — the full rename + payment-shape + gate + WWXRP-exclusion + scope decisions)
    - .planning/milestones/v48.0-phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-SWAP.md (the load-bearing-attestation section/header/STOP-rule format to mirror)
    - contracts/DegenerusGame.sol (autoResolve :1587 + the (players[],betIds[]) args; AUTO-02 probe :1596; per-item loop read :1601; the per-item peg :1612; _autoResolveBet :1684 onlySelf; the public view :2319; AUTO_GAS_PRICE_REF :1539; AUTO_RESOLVE_BET_GAS_UNITS :1545; mintPrice :2398)
    - contracts/modules/DegenerusGameDegeneretteModule.sol (resolveBets / _resolveFullTicketBet; the WWXRP currency==3 exclusion + zero-reward; the per-item try/catch; degeneretteBets submission :526 / read :605 / delete :634)
    - contracts/interfaces/IDegenerusGame.sol + contracts/interfaces/IDegenerusGameModules.sol (the autoResolve / _autoResolveBet signatures — grep BOTH)
    - test/ (grep for `autoResolve` / `_autoResolveBet` references that the rename must update — enumerate them as rename-surface rows)
  </read_first>
  <action>Create 329-ATTEST-DEGENERETTE-RESOLVE.md mirroring 325-ATTEST-SWAP.md: a `## Scope` + `## Sources of truth` + byte-identical-to-0cc5d10f header, a `## Verdict legend` (MATCH/SHIFTED/ABSENT), then sections A, B, E.
Section A — RENAME SURFACE (D-05a): a per-anchor table enumerating EVERY symbol the rename touches — external `autoResolve` :1587 → `degeneretteResolve`, the `onlySelf` internal `_autoResolveBet` :1684 → `_degeneretteResolveBet`, the IDegenerusGame + IDegenerusGameModules signatures (grep both, give exact line + verdict), and every test/ caller (grep `autoResolve`/`_autoResolveBet` across test/, one row each). Note that the rename deliberately leaves the `auto*` family — it is no longer a gas-pegged router/keeper action but a flat-"lose" Degenerette-resolution helper. Confirm the AUTO-02 probe (BatchAlreadyTaken :1596), the per-item try/catch isolation, the WWXRP (currency==3) exclusion + zero-reward (AUTO-04), and self-resolve-allowed (REW-04) all exist and are PRESERVED (D-05d).
Section B — D-05b PAYMENT-SHAPE FEASIBILITY: confirm, against the current per-item resolution loop structure, that the flat shape is expressible: (i) a flat literal ~1 BURNIE (1e18) creditFlip ONCE per tx (count-independent) REPLACES the per-item `_ethToBurnieValue(AUTO_RESOLVE_BET_GAS_UNITS * AUTO_GAS_PRICE_REF, ...)` peg at :1612; (ii) gated at ≥3 successfully-resolved NON-WWXRP bets (the ≥3 count is NON-WWXRP only — a WWXRP-only batch can't trip it); (iii) revert `NoWork()` at ZERO resolved; (iv) the LEAN: resolve-always, pay 0 below 3, revert ONLY at 0 — so a legit 1–2-bet trailing tail is resolved-but-unpaid, NEVER reverted (reverting would roll back the resolutions and strand the tail). Give an explicit feasible/blocker verdict that the per-item loop can count NON-WWXRP successes and fire ONE flat creditFlip after the loop (CEI-last). Record the exact peg constant being removed (:1612) and the flat-literal that replaces it as the IMPL edit target (the exact ~1 BURNIE value is soft — confirmed-sub-real-gas at GAS / D-05e, NOT decided here).
Section E — ARCHITECTURAL NON-FOLDABILITY (ROUTER-05 / D-05): grep-confirm `degeneretteBets[player][betId]` is a nested mapping with NO O(1) on-chain enumeration (the resolver takes caller-supplied (players[], betIds[]) at :1587), so on-chain enumeration would be an unbounded scan that violates ROUTER-04 — therefore degeneretteResolve stays a SEPARATE call, the router-fold is OUT, and the unified one-button is a frontend concern. NO contracts/*.sol edit.</action>
  <verify>
    <automated>bash -c 'F=.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-DEGENERETTE-RESOLVE.md; test -f "$F" && grep -q "Verdict legend" "$F" && grep -Eqi "MATCH|SHIFTED|ABSENT" "$F" && grep -q "0cc5d10f" "$F" && grep -q "degeneretteResolve" "$F" && grep -q "_degeneretteResolveBet\|_autoResolveBet" "$F" && grep -Eqi "NoWork" "$F" && grep -Eqi "WWXRP|currency==3|currency == 3" "$F" && grep -Eq "1587|1684|1612" "$F"'</automated>
  </verify>
  <acceptance_criteria>
    - 329-ATTEST-DEGENERETTE-RESOLVE.md exists with a Verdict legend and the byte-identical-to-0cc5d10f header.
    - Section A enumerates every rename-surface anchor (autoResolve :1587, _autoResolveBet :1684, BOTH interface signatures, and each test/ caller) with a verdict, and confirms AUTO-02 probe + per-item try/catch + WWXRP exclusion + self-resolve PRESERVED (D-05d).
    - Section B gives a feasible/blocker verdict for the flat-~1-BURNIE-once-per-tx shape: count-independent creditFlip replacing the :1612 per-item peg, ≥3-NON-WWXRP gate, revert NoWork() at zero, lean = resolve-always/pay-at-≥3/revert-only-at-0 (1–2 resolved → unpaid not reverted) — the exact ~1 BURNIE constant deferred to GAS (D-05e).
    - Section E grep-confirms degeneretteBets has no O(1) enumeration → degeneretteResolve stays a separate call, router-fold OUT (ROUTER-05/D-05).
    - No contracts/*.sol modified.
  </acceptance_criteria>
  <done>329-ATTEST-DEGENERETTE-RESOLVE.md sections A/B/E committed: rename surface fully enumerated, flat-payment shape feasibility attested, architectural non-foldability confirmed; zero .sol mutation.</done>
</task>

<task type="auto">
  <name>Task 2: PERFORM the D-05f losing-bet-liveness grep-verification + record the D-05c CORRECTED real-gas exploitability basis → 329-ATTEST-DEGENERETTE-RESOLVE.md sections C/D + Roll-up</name>
  <read_first>
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-02-PLAN.md (this plan, attestation_method — the SURFACE-TO-USER rule)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-CONTEXT.md (D-05c CORRECTED real-gas basis; D-05f the liveness nuance the SPEC author MUST verify; D-05e the GAS sanity-check handoff)
    - .planning/milestones/v48.0-phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-SWAP.md (the STOP/SURFACE-block + numeric-derivation format to mirror)
    - contracts/modules/DegenerusGameDegeneretteModule.sol (degeneretteBets submission :526 / read :605 / delete :634; resolveBets / _resolveFullTicketBet; ANY pendingDegenerette / outstanding-bet counter / per-day bet accounting / RNG-slot reuse keyed on degeneretteBets; the WWXRP zero-reward branch)
    - contracts/DegenerusGame.sol (the public degeneretteBets view :2319; any gameOver / sweep / liveness path that reads-or-requires-empty degeneretteBets; mintPrice :2398 for the ~1-BURNIE ≤ mintPrice/1000 ETH derivation; PRICE_COIN_UNIT / _ethToBurnieValue for the BURNIE↔ETH conversion)
    - contracts/modules/DegenerusGameGameOverModule.sol (grep: does the gameover/final-sweep path iterate or require-resolved degeneretteBets? — a candidate D-05f dependency)
    - contracts/modules/DegenerusGameJackpotModule.sol (grep: does any jackpot/daily-hero path read degeneretteBets state that an UNRESOLVED losing bet would corrupt? — a candidate D-05f dependency)
  </read_first>
  <action>Append sections C, D and the final Roll-up to 329-ATTEST-DEGENERETTE-RESOLVE.md.
Section C — D-05c CORRECTED REAL-GAS EXPLOITABILITY BASIS: show the arithmetic that ~1 BURNIE flip-credit is sub-real-gas. (i) ~1 BURNIE (1e18) is worth at most `mintPrice/1000` ETH by the protocol's own (generous) peg — grep mintPrice and show ≤ 0.00024 ETH even at the 0.24-ETH milestone price; (ii) it is ILLIQUID (locked in coinflip → real extractable value is a fraction after the house edge); (iii) the keeper pays REAL prevailing tx gas every call (base 21k + ≥3 resolutions + overhead) at 5–50+ gwei. State EXPLICITLY that the basis is REAL gas, NOT the 0.5-gwei AUTO_GAS_PRICE_REF peg constant (:1539) — the earlier draft wrongly compared against the peg ref; USER corrected it (feedback_bounty_exploit_uses_real_gas_not_peg_ref). Conclude: every qualifying ≥3-resolution tx is a net loss at any realistic gas price → no positive-EV farm; the ≥3 gate only widens the margin. Name the only theoretical positive corner {late game mintPrice 0.24 ∧ gas < ~3.6 gwei ∧ flip-credit fully extractable at the peg} and note it is gated by an almost-certainly-false illiquidity assumption; hand the empirical confirm to GAS (D-05e — NOT a blocker here).
Section D — D-05f LOSING-BET-LIVENESS GREP-VERIFICATION (the load-bearing deliverable): ENUMERATE in a table EVERY consumer/cleanup/accounting/RNG-slot/invariant path keyed on `degeneretteBets[player][betId]` — the submission :526, the resolution read :605/:1601, the cleanup `delete` :634, the public view :2319, plus every grep hit across DegeneretteModule / GameOverModule / JackpotModule / DegenerusGame for a pendingDegenerette / outstanding-bet counter / per-day bet tally / RNG-slot reuse / gameOver-requires-empty / sweep-iterates dependency. For EACH row state: does this path REQUIRE a LOSING bet to be RESOLVED (its `delete` :634 to fire) for correctness? Then the explicit FINDING: if NO path requires it (winning bets are self-resolved by owners claiming winnings; losing bets left unresolved are inert storage cruft) → record `FINDING: inert — safe to drop the per-item break-even incentive; the flat count-independent reward actually NUDGES clearing the whole backlog in one tx`. If ANY path DOES require it (e.g. a gameOver/sweep that iterates-or-requires-empty, an accounting counter that only decrements on `delete`, an RNG slot reused only after resolution) → emit a `## SURFACE-TO-USER — LOSING-BET LIVENESS DEPENDENCY` block naming the exact path + line + the backlog/invariant risk, and DO NOT record "safe". End the doc with a `## Roll-up` stating: the IMPL-blocker count, the D-05f finding (inert-safe OR surfaced-dependency), the D-05c net-loss verdict, the D-05b shape-feasible verdict, and the ROUTER-05 non-foldable confirmation. NO contracts/*.sol edit.</action>
  <verify>
    <automated>bash -c 'F=.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-DEGENERETTE-RESOLVE.md; test -f "$F" && grep -Eqi "real gas|prevailing gas" "$F" && grep -q "AUTO_GAS_PRICE_REF" "$F" && grep -Eqi "mintPrice/1000|0.00024" "$F" && grep -q "delete degeneretteBets\|:634" "$F" && grep -Eqi "inert|SURFACE-TO-USER" "$F" && grep -q "Roll-up" "$F"'</automated>
  </verify>
  <acceptance_criteria>
    - Section C shows the ~1 BURNIE ≤ mintPrice/1000 ETH (≤0.00024 ETH) derivation + illiquidity + the REAL-prevailing-gas (5–50+ gwei) comparison, states EXPLICITLY the basis is real gas NOT the 0.5-gwei AUTO_GAS_PRICE_REF peg (the corrected basis), concludes net-loss/no-positive-EV-farm, names the only theoretical positive corner, and hands the empirical confirm to GAS (D-05e, not a blocker).
    - Section D enumerates EVERY degeneretteBets consumer/cleanup/accounting/RNG-slot/invariant path (submission :526, read :605/:1601, delete :634, view :2319, + every GameOverModule/JackpotModule/DegenerusGame grep hit) with a per-row require-losing-bet-resolved verdict, and records an explicit FINDING — `inert — safe` OR a `## SURFACE-TO-USER — LOSING-BET LIVENESS DEPENDENCY` block naming the path + risk (never silently "safe" if a dependency exists).
    - The Roll-up states the blocker count + the D-05f finding + the D-05c net-loss verdict + the D-05b shape-feasible verdict + the ROUTER-05 non-foldable confirmation.
    - No contracts/*.sol modified.
  </acceptance_criteria>
  <done>329-ATTEST-DEGENERETTE-RESOLVE.md sections C/D + Roll-up committed: the D-05c real-gas net-loss basis derived, the D-05f losing-bet-liveness grep-verification PERFORMED with an explicit inert-safe-or-surfaced finding; zero .sol mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Paper-only attestation — ZERO code mutation this phase; the mitigations are LOCKED in the ATTEST
evidence for IMPL (330) to apply and GAS/TST (331/332) to prove. The boundary is between D-05's
*claimed* economics/liveness safety and what the baseline source *actually* implements — an
under-derived exploitability basis (using the peg ref instead of real gas) or a missed
losing-bet-liveness dependency lets a faucet or a silently-starved cleanup path reach the IMPL diff.

| Boundary | Description |
|----------|-------------|
| ~1 BURNIE flip-credit → REAL prevailing tx gas | If compared against the 0.5-gwei peg ref (not real gas), the "never exploitable" claim is unfounded (the USER-corrected error) |
| flat count-independent reward → degeneretteBets cleanup/accounting/RNG-slot consumers | Dropping the per-item incentive starves losing-bet resolution; if any invariant needs it, that is a silent liveness/accounting regression |
| degeneretteResolve → the on-chain router | Folding it in would require unbounded on-chain enumeration of degeneretteBets (violates ROUTER-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-329-D1 | Elevation of privilege (economic) | The "~1 BURNIE never exploitable" basis is recorded against the 0.5-gwei AUTO_GAS_PRICE_REF peg ref instead of REAL prevailing gas → a false no-farm claim | mitigate | Section C derives the basis against REAL prevailing gas (5–50+ gwei) + flip-credit illiquidity, explicitly NOT the peg ref (the USER-corrected basis per feedback_bounty_exploit_uses_real_gas_not_peg_ref); the empirical confirm is handed to GAS (D-05e) |
| T-329-D2 | Denial of service (liveness/accounting) | Dropping the per-item break-even incentive silently starves losing-Degenerette-bet resolution that some invariant/cleanup/RNG-slot path REQUIRES | mitigate | Section D PERFORMS the D-05f grep-enumeration of every degeneretteBets consumer with a per-row require-resolved verdict; any dependency triggers a SURFACE-TO-USER block, never a silent "safe" |
| T-329-D3 | Tampering | A rename-surface symbol (interface signature, a test caller, the onlySelf wrapper) is missed → the BATCH-02 rename ships a broken intermediate / a dangling reference | mitigate | Section A enumerates every autoResolve/_autoResolveBet anchor across DegenerusGame + BOTH interfaces + all test/ callers with a verdict row |
| T-329-D4 | Tampering | The ≥3-NON-WWXRP gate / revert-on-no-work / resolve-always-pay-at-≥3 lean is not expressible on the current loop → IMPL improvises a wrong boundary that strands a 1–2-bet tail | mitigate | Section B gives an explicit feasible/blocker verdict against the current per-item loop + try/catch + WWXRP-exclusion structure, pinning the revert-only-at-0 lean |
| T-329-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this paper-only plan; RESEARCH Package Legitimacy Gate N/A |
</threat_model>

<verification>
- 329-ATTEST-DEGENERETTE-RESOLVE.md exists with sections A (rename surface), B (payment-shape feasibility), C (real-gas exploitability basis), D (D-05f losing-bet-liveness grep-finding), E (architectural non-foldability), and a Roll-up.
- The D-05f losing-bet-liveness grep-verification is PERFORMED with an explicit inert-safe finding OR a SURFACE-TO-USER dependency block.
- The D-05c basis is derived against REAL prevailing gas, explicitly NOT the 0.5-gwei peg ref.
- Every rename-surface anchor (autoResolve, _autoResolveBet, both interfaces, all test callers) has a verdict row; the flat-shape feasibility + non-foldability are resolved.
- Zero contracts/*.sol modified: `git diff --name-only -- 'contracts/*.sol'` is empty.
</verification>

<success_criteria>
- The D-05 family (rename surface D-05a + payment shape D-05b + real-gas basis D-05c + WWXRP/invariants D-05d + GAS handoff D-05e + losing-bet liveness D-05f + scope D-05g) is grep-attested at 0cc5d10f (BATCH-01 attestation half for the load-bearing item).
- The D-05f losing-bet-liveness question is actually answered from source (inert-safe or surfaced).
- degeneretteResolve confirmed router-non-foldable (ROUTER-05).
- ZERO contracts/*.sol mutation (paper-only phase).
</success_criteria>

<output>
Create `.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-02-SUMMARY.md` when done, stating the rename-surface enumeration result, the D-05f finding (inert-safe or the surfaced dependency), the D-05c net-loss verdict, the D-05b shape-feasibility verdict, and the ROUTER-05 non-foldability — for Plan 03 to fold into the SPEC.
</output>
</content>
</invoke>
