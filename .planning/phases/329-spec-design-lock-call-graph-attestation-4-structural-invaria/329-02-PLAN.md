---
phase: 329-spec-design-lock-call-graph-attestation-4-structural-invaria
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-DEGENERETTE-RESOLVE.md
autonomous: true
requirements: [BATCH-01]
must_haves:
  truths:
    - "Every autoResolve→degeneretteResolve rename-surface anchor is grep-attested against the FROZEN HEAD 0cc5d10f via `git show 0cc5d10f:contracts/...` (NOT the dirty working tree): external autoResolve DegenerusGame.sol:1587, the AUTO-02 probe `if (degeneretteBets[players[0]][betIds[0]] == 0) revert BatchAlreadyTaken()` :1596 (error decl :95), the per-item betPacked read :1601, the WWXRP currency==3 zero-reward exclusion :1607-1608, the per-item _ethToBurnieValue(AUTO_RESOLVE_BET_GAS_UNITS:1545 * AUTO_GAS_PRICE_REF, ...) peg :1611-1612, the onlySelf internal wrapper _autoResolveBet :1684, the view :2319, plus the IDegenerusGame / IDegenerusGameModules signature + EVERY caller/test reference to autoResolve / _autoResolveBet (D-05a rename closure)"
    - "The D-05 carry-forward is confirmed: D-05 (the autoResolve→degeneretteResolve rename + flat ≥3-gate re-peg) SURVIVES the keeper-router redesign VERBATIM — it is NOT a router leg, the redesign's RD-1..RD-5 do not touch the Degenerette resolve path; the SPEC re-attests it unchanged. The ROUTER-05 'autoResolve excluded from the router' wording and the architectural-non-foldability finding carry forward intact"
    - "The D-05f losing-bet-liveness grep-verification is PERFORMED and recorded against 0cc5d10f: every degeneretteBets[player][betId] consumer/cleanup/accounting/RNG-slot path is enumerated (write DegeneretteModule:526, read :605, delete :634; DegenerusGame read :1601, view :2319) with an explicit finding — does ANY invariant/accounting/RNG-slot/cleanup path REQUIRE losing Degenerette bets to be resolved? If inert cruft → safe-to-drop-the-break-even-incentive; if a backlog/invariant risk → flagged SURFACE-TO-USER (do NOT silently starve a needed path). The prior 329 run found this INERT-SAFE; the re-attestation either re-confirms or surfaces"
    - "The D-05c CORRECTED real-gas exploitability basis is recorded: ~1 BURNIE (1e18) flip-credit ≤ mintPrice/1000 ETH (≤0.00024 ETH even at the 0.24-ETH milestone price) AND illiquid (coinflip-locked), compared against REAL prevailing tx gas (5–50+ gwei, base 21k + ≥3 resolutions + overhead) — NOT the 0.5-gwei AUTO_GAS_PRICE_REF peg constant (the corrected basis per feedback_bounty_exploit_uses_real_gas_not_peg_ref) → every qualifying tx is a net loss → no positive-EV farm; the ≥3 gate widens the margin"
    - "The D-05b payment shape + gate is attested as implementable on the existing surface: flat literal ~1 BURNIE (1e18) creditFlip ONCE per tx (count-independent, replacing the per-item _ethToBurnieValue :1611 sum), gated at ≥3 successfully-resolved NON-WWXRP bets, revert NoWork() at ZERO resolved, lean = resolve-always-pay-at-≥3-revert-only-at-0 (1–2 resolved → resolved-but-unpaid, NOT reverted so a trailing tail is never stranded) — the per-item resolution loop + NON-WWXRP success-counting + the revert/unpaid boundary are confirmed expressible against the current _autoResolveBet/resolveBets structure"
    - "degeneretteResolve is confirmed architecturally NON-foldable into the redesigned router (ROUTER-05 / D-05): degeneretteBets[player][betId] has NO O(1) on-chain enumeration — it requires caller-supplied (players[], betIds[]) from off-chain indexing; on-chain enumeration = unbounded scan = violates ROUTER-04, so it stays a SEPARATE call (the redesign's autoBuy→advance→autoOpen router does NOT add it) and the unified one-button is a frontend concern"
    - "The GASOPT-04 cross-coordination is recorded: the D-05/D-02 rename test-fix surface and the GASOPT-04 AutoBought-event-removal test-oracle migration TOUCH THE SAME AfKing test files (AfKingConcurrency / AfKingSubscription / AfKingFundingWaterfall / SweepPerPlayerWorstCaseGas + CrankNonBrick + CrankLeversAndPacking), so both test-side changes must land together in the Phase 330 BATCH-02 diff; the attestation notes the held-tree 'D-02 5 test files / 57 refs autoResolve' count is a HELD-TREE artifact — at 0cc5d10f the autoResolve rename is a CONTRACT+INTERFACE-only surface (grep `autoResolve` in test/ returns the actual count, recorded)"
  artifacts:
    - path: ".planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-DEGENERETTE-RESOLVE.md"
      provides: "The autoResolve→degeneretteResolve rename-surface attestation (vs 0cc5d10f), the D-05f losing-bet-liveness grep-finding (the load-bearing deliverable), the D-05c real-gas exploitability basis, the D-05b flat-~1-BURNIE/≥3-gate/revert-on-no-work shape feasibility, the architectural-non-foldability confirmation under the redesigned router, and the GASOPT-04 test-file cross-coordination note"
      contains: "SURFACE-TO-USER"
  key_links:
    - from: "the flat ~1 BURNIE (1e18) flip-credit per tx"
      to: "REAL prevailing tx gas (5–50+ gwei) for a ≥3-resolution call"
      via: "the D-05c corrected real-gas (NOT 0.5-gwei-peg) net-loss derivation"
      pattern: "net loss|real gas|mintPrice/1000|illiquid"
    - from: "the flat count-independent reward (dropping the per-item break-even incentive)"
      to: "every degeneretteBets cleanup/accounting/RNG-slot/invariant consumer"
      via: "the D-05f losing-bet-liveness grep-enumeration at 0cc5d10f"
      pattern: "delete degeneretteBets|SURFACE-TO-USER|inert"
---

<objective>
Re-attest the **`autoResolve` → `degeneretteResolve` rename + flat ~1-BURNIE "lose" re-peg** (the D-05
family — code lands at Phase 330 BATCH-02, REQ-IDs GAS-06/TST-05 owned by Phases 331/332) against the
FROZEN v48.0-closure HEAD `0cc5d10fbc1232a6d2e7b0464fe21541b9812029` (`0cc5d10f`). **D-05 SURVIVES the
keeper-router redesign VERBATIM** — it is NOT a router leg, so RD-1..RD-5 do not touch it; this
re-attestation re-confirms the carry-forward (the prior superseded 329-02 attested the same item under
the old design; the substance is unchanged, only the SPEC it feeds changes). Produce
`329-ATTEST-DEGENERETTE-RESOLVE.md` covering:
(A) the **rename surface** (D-05a) — every `autoResolve` / `_autoResolveBet` anchor + interface +
    caller/test, so the mechanical rename is fully enumerated;
(B) the **D-05f losing-bet-liveness grep-finding** (the load-bearing deliverable) — does any invariant
    REQUIRE losing-bet resolution before the break-even incentive is dropped?;
(C) the **D-05c real-gas exploitability basis** (corrected — NOT the peg ref);
(D) the **D-05b flat-~1-BURNIE / ≥3-NON-WWXRP-gate / revert-on-no-work** shape feasibility;
(E) the **architectural non-foldability** into the REDESIGNED router (ROUTER-05);
(F) the **GASOPT-04 test-file cross-coordination** (the rename test-fixes + the AutoBought-event-removal
    test-oracle migration touch the same AfKing test files → both land together in 330).

**CRITICAL — attest against the FROZEN HEAD, not the dirty tree.** The working tree's `contracts/*.sol`
is DIRTY (the superseded held-330 diff). Grep via `git show 0cc5d10f:contracts/<path> | grep -n`. Read
from `contracts/` ONLY (`feedback_contract_locations`).

Output: one `329-ATTEST-DEGENERETTE-RESOLVE.md` under the phase directory. ZERO `contracts/*.sol`
mutation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-CONTEXT.md

# The binding re-SPEC input (the redesign + the GASOPT-04 test-file list in the autoBuy-gas analysis):
@.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-ROUTER-REDESIGN-INTENT.md

# The locked milestone design (no research sub-phase):
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/PLAN-CRANK-DO-WORK-INCENTIVE.md
@.planning/degenerette-resolution-gas-cleanup.md

# The v48 ATTEST format to mirror (verdict legend + per-anchor table + Roll-up):
@.planning/milestones/v48.0-phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-KEEP-POOL.md

<attestation_method>
**The working tree's `contracts/*.sol` is DIRTY** (it carries the superseded held-330 diff). The
COMMITTED HEAD tree is byte-identical to baseline `0cc5d10f`. Grep via
`git show 0cc5d10f:contracts/<path> | grep -n` — NOT the live tree. Read from `contracts/` ONLY. The
doc header MUST state this. Verdict legend: `MATCH` / `SHIFTED(±N)` (record the actual 0cc5d10f line) /
`ABSENT` (IMPL blocker in the Roll-up).
</attestation_method>

<interfaces>
<!-- Grep-confirmed anchors at the FROZEN baseline 0cc5d10f (verified during planning via `git show 0cc5d10f:...`). -->
DegenerusGame.sol @ 0cc5d10f (the autoResolve home — rename target):
  :95       `error BatchAlreadyTaken();`
  :1545     `uint256 private constant AUTO_RESOLVE_BET_GAS_UNITS = 66_528;` (the per-item peg basis — D-05 replaces with the flat ~1 BURNIE)
  :1587     `function autoResolve(` (rename → degeneretteResolve; caller-supplied (players[], betIds[]))
  :1596     `if (degeneretteBets[players[0]][betIds[0]] == 0) revert BatchAlreadyTaken();` (AUTO-02 probe — KEEP)
  :1601     `uint256 betPacked = degeneretteBets[players[i]][betIds[i]];` (the per-item loop read)
  :1607-1608 `// WWXRP work (currency == 3) is resolvable but earns zero reward.` + `if (currency == 3)` (AUTO-04 WWXRP exclusion — KEEP, excluded from BOTH the ≥3 count and the reward)
  :1611-1612 `reward += _ethToBurnieValue(AUTO_RESOLVE_BET_GAS_UNITS * AUTO_GAS_PRICE_REF, ...)` (the per-item break-even sum — D-05 replaces with ONE flat ~1 BURNIE creditFlip)
  :1684     `function _autoResolveBet(address player, uint64 betId) external` (onlySelf internal wrapper — rename → _degeneretteResolveBet; the per-item try/catch isolation)
  :1790     `function _ethToBurnieValue(` (the conversion helper — used by the per-item peg today)
  :2319     `return degeneretteBets[player][betId];` (the public view)
DegeneretteModule.sol @ 0cc5d10f (the degeneretteBets lifecycle — D-05f basis):
  :526      `degeneretteBets[player][nonce] = packed;` (WRITE — bet placed)
  :605      `uint256 packed = degeneretteBets[player][betId];` (READ — resolveBets)
  :634      `delete degeneretteBets[player][betId];` (DELETE — on resolution)
Interfaces (rename surface):
  IDegenerusGame.sol / IDegenerusGameModules.sol — grep the autoResolve / _autoResolveBet selectors (D-05a rename; per CONTEXT D-05 there is NO degeneretteResolve interface ROW — it is defined on DegenerusGame.sol, confirm)
Tests (D-02 rename surface + GASOPT-04 cross-coordination — the rename touches CONTRACT+INTERFACE; grep test/ for the ACTUAL autoResolve reference count at 0cc5d10f):
  AutoBought-keyed (GASOPT-04 migration overlap): test/fuzz/AfKingConcurrency.t.sol, test/fuzz/AfKingSubscription.t.sol, test/fuzz/AfKingFundingWaterfall.t.sol, test/gas/SweepPerPlayerWorstCaseGas.t.sol, test/fuzz/CrankNonBrick.t.sol, test/gas/CrankLeversAndPacking.t.sol
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attest the degeneretteResolve rename surface (D-05a) + the D-05c real-gas basis + the D-05b flat-shape feasibility + the architectural non-foldability + the GASOPT-04 test cross-coordination → 329-ATTEST-DEGENERETTE-RESOLVE.md sections A/B/C/D</name>
  <read_first>
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-02-PLAN.md (this plan — the `<interfaces>` block + attestation_method)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-CONTEXT.md (D-05 full text — rename + flat ≥3-gate re-peg + real-gas basis + WWXRP exclusion + non-foldability + the "NO interface row, defined on DegenerusGame.sol" note)
    - .planning/degenerette-resolution-gas-cleanup.md (the D-05 design memo — same-results-gas-only constraint, the resolution-batch-invariant)
    - .planning/milestones/v48.0-phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-KEEP-POOL.md (the table + Roll-up format to mirror)
    - .planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-ROUTER-REDESIGN-INTENT.md (the GASOPT-04 AutoBought test-file list — the cross-coordination)
    - `git show 0cc5d10f:contracts/DegenerusGame.sol` (autoResolve :1587; BatchAlreadyTaken :95/:1596; betPacked read :1601; WWXRP :1607-1608; AUTO_RESOLVE_BET_GAS_UNITS :1545; the per-item _ethToBurnieValue :1611-1612 + helper :1790; _autoResolveBet :1684; view :2319)
    - `git show 0cc5d10f:contracts/interfaces/IDegenerusGame.sol` + `git show 0cc5d10f:contracts/interfaces/IDegenerusGameModules.sol` (grep autoResolve / _autoResolveBet — confirm the NO-degeneretteResolve-interface-row claim)
    - test/ (grep `autoResolve` for the ACTUAL 0cc5d10f reference count; grep `AutoBought` for the GASOPT-04 overlap file list)
  </read_first>
  <action>Create 329-ATTEST-DEGENERETTE-RESOLVE.md mirroring 325-ATTEST-KEEP-POOL.md: a `## Scope` + `## Sources of truth` header naming the exact `contracts/` files + the DIRTY-tree / grep-against-`git show 0cc5d10f` note + a one-line "D-05 SURVIVES the keeper-router redesign verbatim — re-attested unchanged" carry-forward statement, then a `## Verdict legend`, then:
Section A — rename surface (D-05a): an anchor reconciliation table grep-verifying every autoResolve / _autoResolveBet anchor against `git show 0cc5d10f:contracts/DegenerusGame.sol` (autoResolve :1587, BatchAlreadyTaken decl :95 + probe :1596, betPacked read :1601, WWXRP currency==3 :1607-1608, the per-item peg :1611-1612 + AUTO_RESOLVE_BET_GAS_UNITS :1545 + _ethToBurnieValue :1790, _autoResolveBet :1684, view :2319) + the interface grep verdict (confirm autoResolve/_autoResolveBet selectors in IDegenerusGame/IDegenerusGameModules OR confirm the CONTEXT "NO degeneretteResolve interface row — defined on DegenerusGame.sol" claim) + the test reference verdict (grep `autoResolve` in test/ for the ACTUAL count; record it and note the held-tree "5 files / 57 refs" is a HELD-TREE artifact). End Section A with the GASOPT-04 cross-coordination note: the D-02 rename test-fixes + the GASOPT-04 AutoBought-event-removal test-oracle migration touch the SAME AfKing test files (AfKingConcurrency / AfKingSubscription / AfKingFundingWaterfall / SweepPerPlayerWorstCaseGas + CrankNonBrick + CrankLeversAndPacking) → both land together in the Phase 330 BATCH-02 diff.
Section B — D-05c real-gas exploitability basis: record the corrected basis — ~1 BURNIE (1e18) flip-credit ≤ mintPrice/1000 ETH (≤0.00024 ETH even at the 0.24-ETH milestone price) AND illiquid (coinflip-locked), vs REAL prevailing tx gas (5–50+ gwei, base 21k + ≥3 resolutions + overhead), NOT the 0.5-gwei AUTO_GAS_PRICE_REF peg (the corrected basis per feedback_bounty_exploit_uses_real_gas_not_peg_ref) → every qualifying tx is a net loss → no positive-EV farm; the ≥3 gate widens the margin. State this is the GAS-06 sanity-check basis (a real corner-check at GAS, not a blocker).
Section C — D-05b flat-shape + ≥3-NON-WWXRP-gate feasibility: confirm against the current loop structure that (1) the per-item _ethToBurnieValue sum :1611-1612 can be replaced by ONE flat literal ~1 BURNIE (1e18) creditFlip per tx (count-independent); (2) the ≥3 gate counts only NON-WWXRP (currency != 3, the AUTO-04 exclusion at :1607-1608); (3) revert NoWork() at ZERO resolved; (4) the lean boundary — 1–2 resolved → resolved-but-UNPAID (do NOT revert, never strand a trailing tail). Confirm the AUTO-02 probe :1596 + the per-item try/catch isolation (_autoResolveBet :1684) + self-resolve-allowed (REW-04) are all PRESERVED.
Section D — architectural non-foldability into the REDESIGNED router (ROUTER-05): record that degeneretteBets[player][betId] has NO O(1) on-chain enumeration (requires caller-supplied (players[], betIds[]) from off-chain indexing; on-chain enumeration = unbounded scan = violates ROUTER-04) → it stays a SEPARATE call, NOT a leg of the redesigned autoBuy→advance→autoOpen router; the unified one-button is a frontend concern. Explicitly note the redesign's RD-1..RD-5 do not touch this path. NO contracts/*.sol edit.</action>
  <verify>
    <automated>bash -c 'F=.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-DEGENERETTE-RESOLVE.md; test -f "$F" && grep -q "Verdict legend" "$F" && grep -q "0cc5d10f" "$F" && grep -q ":1587" "$F" && grep -Eqi "BatchAlreadyTaken" "$F" && grep -Eqi "currency == 3|WWXRP" "$F" && grep -Eqi "1 BURNIE|1e18" "$F" && grep -Eqi "real gas|mintPrice/1000|illiquid" "$F" && grep -Eqi "non.foldab|unbounded scan|ROUTER-04" "$F" && grep -q "AutoBought" "$F"'</automated>
  </verify>
  <acceptance_criteria>
    - 329-ATTEST-DEGENERETTE-RESOLVE.md exists with a Verdict legend, the DIRTY-tree / grep-against-0cc5d10f header, and the "D-05 survives the redesign verbatim" carry-forward line.
    - Section A grep-verifies every autoResolve/_autoResolveBet anchor (with verdicts) + the interface verdict (selectors OR the NO-interface-row confirmation) + the actual test autoResolve count (with the held-tree-artifact note) + the GASOPT-04 same-test-files cross-coordination note.
    - Section B records the D-05c corrected real-gas net-loss basis (NOT the 0.5-gwei peg).
    - Section C confirms the flat-~1-BURNIE / ≥3-NON-WWXRP-gate / revert-on-zero / 1-2-resolved-but-unpaid shape is expressible on the current loop, with AUTO-02 probe + per-item try/catch + self-resolve preserved.
    - Section D records the architectural non-foldability (no O(1) enumeration → unbounded scan → violates ROUTER-04) and that RD-1..RD-5 do not touch the path.
    - No contracts/*.sol modified.
  </acceptance_criteria>
  <done>329-ATTEST-DEGENERETTE-RESOLVE.md sections A/B/C/D committed: the rename surface, the D-05c real-gas basis, the D-05b flat-shape feasibility, the architectural non-foldability under the redesigned router, and the GASOPT-04 test cross-coordination all grep-attested vs 0cc5d10f; zero .sol mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Perform + record the D-05f losing-bet-liveness grep-finding (the load-bearing deliverable) + the Roll-up → 329-ATTEST-DEGENERETTE-RESOLVE.md section E + Roll-up</name>
  <read_first>
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-02-PLAN.md (this plan)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-DEGENERETTE-RESOLVE.md (sections A-D from Task 1 — section E must be consistent)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-CONTEXT.md (D-05f — "verify no invariant requires losing-bet resolution before the break-even incentive is dropped"; the prior 329 run found this INERT-SAFE — re-confirm or surface)
    - `git show 0cc5d10f:contracts/modules/DegenerusGameDegeneretteModule.sol` (degeneretteBets write :526, read :605, delete :634 — the full bet lifecycle; trace whether a LOSING (unresolved) bet's slot is consumed/required by any later accounting/RNG/jackpot/cleanup path — backward-trace per feedback_rng_window_storage_read_freshness for any RNG-slot dependency)
    - `git show 0cc5d10f:contracts/DegenerusGame.sol` (degeneretteBets read :1601, view :2319 — the resolve-side consumers)
    - grep degeneretteBets across ALL `git show 0cc5d10f:contracts/*.sol` modules to ensure no consumer is missed (the DegenerusGame mint/jackpot inline-duplication precedent — feedback_verify_call_graph_against_source)
  </read_first>
  <action>Append section E + the Roll-up to 329-ATTEST-DEGENERETTE-RESOLVE.md.
Section E — D-05f losing-bet-liveness grep-finding (the load-bearing deliverable): grep degeneretteBets[player][...] across EVERY `git show 0cc5d10f:contracts/*.sol` module (not just DegenerusGame + DegeneretteModule — enumerate the full consumer set to avoid the inline-duplication false-negative). Build a consumer table: write (DegeneretteModule:526), read (DegeneretteModule:605, DegenerusGame:1601), delete (DegeneretteModule:634), view (DegenerusGame:2319), plus any additional consumer the grep surfaces. For EACH, classify whether it REQUIRES a LOSING (unresolved) Degenerette bet to be RESOLVED for an invariant / accounting balance / RNG-slot reuse / jackpot / cleanup path to hold. State the explicit finding: if every path treats an unresolved losing bet as inert cruft (no invariant needs it resolved, no accounting drifts, no RNG slot is blocked) → SAFE to drop the per-item break-even incentive (the flat ≥3-gate leaves 1-2-bet tails unpaid-but-resolvable, and nothing forces resolution). If ANY path REQUIRES losing-bet resolution (a backlog grows, an accounting term drifts, an RNG slot is stranded) → flag it `SURFACE-TO-USER` with the exact path + the consequence, and DO NOT soften it (the SPEC will carry it verbatim as a USER-decision gate for 330). Record whether this re-attestation re-confirms the prior 329 run's INERT-SAFE finding or surfaces a new dependency.
Roll-up — end the doc with a `## Roll-up` stating: the total IMPL-blocker count, the rename-surface-complete verdict, the D-05f finding (INERT-SAFE re-confirmed OR the SURFACE-TO-USER block), the D-05c real-gas-net-loss verdict, the D-05b flat-shape-feasible verdict, and the architectural-non-foldable verdict. NO contracts/*.sol edit.</action>
  <verify>
    <automated>bash -c 'F=.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-DEGENERETTE-RESOLVE.md; test -f "$F" && grep -q "SURFACE-TO-USER" "$F" && grep -Eqi "inert|losing.bet|D-05f" "$F" && grep -q ":526" "$F" && grep -q ":634" "$F" && grep -q "Roll-up" "$F"'</automated>
  </verify>
  <acceptance_criteria>
    - Section E enumerates EVERY degeneretteBets consumer across all `git show 0cc5d10f:contracts/*.sol` modules (write :526 / read :605/:1601 / delete :634 / view :2319 + any grep surfaces), each classified for whether it requires losing-bet resolution.
    - Section E states the explicit D-05f finding: INERT-SAFE (re-confirming the prior 329 run) OR a SURFACE-TO-USER block with the exact path + consequence (not softened).
    - The Roll-up states the blocker count + the rename-complete / D-05f / D-05c / D-05b / non-foldability verdicts.
    - The `SURFACE-TO-USER` token appears in the doc (either as the finding, or as the legend for "if surfaced").
    - No contracts/*.sol modified.
  </acceptance_criteria>
  <done>329-ATTEST-DEGENERETTE-RESOLVE.md section E + Roll-up committed: the D-05f losing-bet-liveness grep-finding performed across all modules and recorded (INERT-SAFE re-confirmed or SURFACE-TO-USER carried), the Roll-up verdicts present; zero .sol mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Paper-only attestation — ZERO code mutation this phase. The boundary is between the D-05 design claims
+ cited anchors and the FROZEN baseline source. The load-bearing risk is the D-05f finding: if a
losing-bet-liveness dependency exists and the attestation misses it (or softens it), Phase 330 drops the
break-even incentive and silently starves a needed cleanup/accounting/RNG-slot path.

| Boundary | Description |
|----------|-------------|
| D-05 cited anchor → frozen contracts/ source @ 0cc5d10f | The held tree is DIRTY; grep via `git show 0cc5d10f` |
| the flat ≥3-gate (drops per-item break-even) → every degeneretteBets consumer | A consumer that REQUIRES losing-bet resolution would be silently starved if missed |
| ~1 BURNIE flip-credit → REAL tx gas | The no-farm basis must be the real-gas comparison, not the 0.5-gwei peg ref |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-329-D1 | Denial of service (liveness) | A degeneretteBets consumer REQUIRES losing-bet resolution and the D-05f grep misses it (the inline-duplication false-negative) → Phase 330 starves it | mitigate | Section E greps degeneretteBets across EVERY module (not just the two obvious ones); any required-resolution path is flagged SURFACE-TO-USER verbatim, never softened |
| T-329-D2 | Spoofing (false economic disposition) | The no-farm basis is recorded against the 0.5-gwei peg ref instead of real gas → an exploitable corner is masked | mitigate | Section B records the CORRECTED real-gas basis (≤mintPrice/1000 illiquid vs 5-50 gwei real tx); the GAS-06 sanity-check is the empirical corner-check |
| T-329-D3 | Tampering | The rename surface is incomplete (a caller/interface/test ref missed) → Phase 330 ships a broken compile | mitigate | Section A greps autoResolve/_autoResolveBet across contracts + interfaces + test/ and records the ACTUAL count + the held-tree-artifact note |
| T-329-D4 | Tampering | The D-02 rename test-fixes and the GASOPT-04 AutoBought-removal test-oracle migration collide in the same files but are planned separately → one overwrites the other | mitigate | Section A records the same-test-files cross-coordination so both land together in the 330 BATCH-02 diff |
| T-329-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this paper-only plan; RESEARCH Package Legitimacy Gate N/A |
</threat_model>

<verification>
- 329-ATTEST-DEGENERETTE-RESOLVE.md exists with a Verdict legend, the DIRTY-tree / grep-against-0cc5d10f header, and the D-05-survives-verbatim carry-forward line.
- The rename surface (D-05a), the D-05c real-gas basis, the D-05b flat-shape feasibility, the architectural non-foldability (ROUTER-05), and the GASOPT-04 test cross-coordination are each attested.
- The D-05f losing-bet-liveness grep-finding is PERFORMED across all modules and recorded (INERT-SAFE re-confirmed or SURFACE-TO-USER carried verbatim).
- Zero contracts/*.sol files modified: `git status --porcelain -- 'contracts/*.sol'` is UNCHANGED from planning.
</verification>

<success_criteria>
- The degeneretteResolve (D-05) attestation is complete with grep-derived verdicts vs 0cc5d10f, confirming the verbatim carry-forward through the keeper-router redesign.
- The D-05f losing-bet-liveness finding is the load-bearing deliverable: inert-safe re-confirmed, or surfaced-to-user without softening.
- The rename surface, real-gas basis, flat-shape feasibility, non-foldability, and the GASOPT-04 test cross-coordination are resolved in writing.
- ZERO contracts/*.sol mutation (paper-only phase; the pre-existing held-330 diff is untouched).
</success_criteria>

<output>
Create `.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-02-SUMMARY.md` when done, listing the ATTEST doc, the rename surface completeness, the D-05f finding (inert-safe or surface-to-user), the D-05c/D-05b/non-foldability verdicts, and the GASOPT-04 test cross-coordination note for Plan 03 to consume.
</output>
