# Phase 329 SPEC — v49.0 Design-Lock + Call-Graph Attestation + 4 Structural Invariants

**Milestone:** v49.0 (Unified Keeper "do-work" Router on AfKing + advance-bounty re-home + break-even @0.5gwei re-peg + the `degeneretteResolve` rename/flat-~1-BURNIE re-peg + AfKing keeper gas/adversarial sweep).
**Baseline HEAD attested against:** `0cc5d10fbc1232a6d2e7b0464fe21541b9812029` (`0cc5d10f`, `MILESTONE_V48_AT_HEAD_0cc5d10fbc1232a6d2e7b0464fe21541b9812029`, the v48.0-closure HEAD; the live `contracts/` tree is byte-identical to baseline — `git diff --name-only 0cc5d10f HEAD -- 'contracts/*.sol'` is EMPTY, zero drift).
**Inputs:** the milestone scope + the locked research (`.planning/research/SUMMARY.md` design-1 / the 8 pitfalls + phase-prevention map / the 5 SPEC-time gaps, `ARCHITECTURE.md`, `PITFALLS.md`) + `.planning/PLAN-CRANK-DO-WORK-INCENTIVE.md` (§7 the three faucet locks; §9 OPEN-C the router reentrancy disposition) + the 2 Wave-1 attestation files in this dir (`329-ATTEST-ROUTER-ADVANCE.md` [Plan 01, the router/advance surface], `329-ATTEST-DEGENERETTE-RESOLVE.md` [Plan 02, the D-05 family]) + the locked decisions in `329-CONTEXT.md` (D-01..D-06g).
**Owns:** BATCH-01 (shared-surface reconciliation, the SPEC half) + ROUTER-07 (router reentrancy disposition) + ADV-04 (the `totalFlipReversals` freeze under the new composition) + GAS-03 (single day-start epoch).

> **This is the direct structural analog of v48's `325-SPEC.md`** — same §0 attestation-verdict roll-up + carried-corrections, §1 shared-signature reconciliation (R-rows + apply-order), §2 the structural invariants + the flagged-disposition resolutions + the D-05 design-lock, §3 per-item IMPL blueprint + producer-before-consumer edit-order map. **The router/advance/`degeneretteResolve` work is ONE batched diff at Phase 330** — it spans `AdvanceModule.sol` (the design-1 producer + the 3 deleted caller-reward `creditFlip` sites), `DegenerusGame.sol` (the wrapper decode + the new discovery views + the `degeneretteResolve` rename/re-peg + the MintModule GASOPT-01 pointer), the `interfaces/*`, and `AfKing.sol` (the `doWork` router + the `_autoBuy` refactor + the re-peg placeholders + GASOPT-02). §1 settles ONE final signature + an apply-order per shared construct so no downstream file ships a broken intermediate (an advance that is unrewarded before the router pays).

> **SOURCE-TREE not mutated at SPEC.** This phase is paper-only — ZERO `contracts/*.sol` mutation. Phase 330 (IMPL / BATCH-01 + BATCH-02) applies the single batched diff, HELD at the contract-commit boundary for explicit user hand-review. The break-even peg constants + the flat ~1-BURNIE literal + the D-06 default-count constants are SPEC PLACEHOLDERS, calibrated at the GAS phase (331).

---

## 0. Attestation verdict roll-up (BATCH-01, Wave-1 fold-in)

**0 IMPL blockers across both attestation surfaces.** Every cited `file:line` across the router/advance surface (Plan 01) and the D-05 `degeneretteResolve` family (Plan 02) is grep-attested against the v48.0-closure HEAD `0cc5d10f` (live tree == baseline). **Plan 01: 34 anchors — 34 MATCH / 0 ABSENT / 0 blockers** (with 4 recorded CONTEXT/v48-ATTEST citation line-drifts, all content-present). **Plan 02: rename surface fully enumerated + the D-05f liveness question answered from source + the D-05c real-gas basis re-derived + D-05b feasibility proven + ROUTER-05 non-foldability confirmed — 0 blockers** (with 2 recorded NON-blocking corrections: the interface-file rename rows are ABSENT/no-op, and `AUTO_RESOLVE_BET_GAS_UNITS` likely goes dead post-re-peg). Raw per-anchor tables: the 2 `329-ATTEST-*.md` files. **Re-grep at edit time — the Phase 330 batched diff shifts every line below.**

**Aggregate verdict table (from Plans 01 + 02):**

| Doc | Surface | Anchors | MATCH | SHIFTED | ABSENT | Blockers |
|-----|---------|---------|-------|---------|--------|----------|
| 329-ATTEST-ROUTER-ADVANCE | router (AfKing) + advance (AdvanceModule + Game wrapper) | 34 | 34 | 4 (CONTEXT line-drifts, content-present) | 0 | 0 |
| 329-ATTEST-DEGENERETTE-RESOLVE | D-05 family (rename + flat-~1-BURNIE re-peg + liveness + non-foldability) | rename surface (2 contract + 5 test/57 refs) + B/C/D/E re-derivations | n/a (verdict-tables, not anchor-count) | 1 (KEEP-04 carried below) | 1 (interface-file rows — corrects the plan, SHRINKS surface) | 0 |
| **Total** | **both BATCH-01 halves** | **34 + the D-05 surface** | — | — | **0 material ABSENT** | **0** |

### Carried corrections (override the plan/CONTEXT prose at IMPL time — re-anchor at the Phase 330 diff)

- **C1. KEEP-04 affiliate-code wiring line-drift (Plan 01 §A13).** The `bytes32("DGNRS")` two-tier 75/20/5 affiliate code is wired **game-side** at `DegenerusGame.sol:1781` (inside `_batchPurchaseUnit`, reached from `batchPurchase`'s per-player `try this._batchPurchaseUnit{value:slice}`), **NOT** in `AfKing.sol` (`AfKing.batchPurchase` carries NO affiliate argument — its interface decl `:26-30` is `batchPurchase(address[],uint256[],uint8[])`). The v48 ATTEST cited `:1778` → **SHIFTED +3 (now `:1781`)**. Consequence: because the affiliate wiring lives game-side and AfKing only forwards `totalValue`, the `_autoBuy` internal refactor (the router work) **does NOT touch the affiliate-code path** — the ROUTER-05 KEEP-04 passthrough survives unchanged. IMPL must re-anchor at `DegenerusGame.sol:1781`, not in AfKing.
- **C2. 30-min universal bypass line-drift (Plan 01 §G1).** The unconditional `if (elapsed >= 30 minutes) return;` 30-min universal advance bypass is at `AdvanceModule.sol:1012` (`_enforceDailyMintGate`, the `elapsed = (block.timestamp - 82620) % 1 days` at :1009 + the `// Anyone after 30 min` comment :1011). CONTEXT cited `~:1008` → **SHIFTED +4 (now `:1012`)**. This is the structural underpinning of invariant (c) SECONDARY (§2).
- **C3. Death-clock extend line-drift (Plan 01 §G4).** The stall-duration death-clock extend (`purchaseStartDay += gapCount;`) is at `AdvanceModule.sol:1200` (+ `gapDays = gapCount;` :1201); the L1+ 120-day timeout constant is `DEPLOY_IDLE_TIMEOUT_DAYS` :109 (Level-0=365, level-1+=hardcoded 120). CONTEXT cited `:1198` → **SHIFTED +2 (now `:1200`)**. The invariant (c) TERTIARY failsafe (§2).
- **C4. GASOPT-01 `rk`-set sites (Plan 01 §C.2).** The `rk`-loop-invariant hoist targets are: `processFutureTicketBatch` body sets `rk` at `DegenerusGameMintModule.sol:398` (function header `:393`); `processTicketBatch` sets `rk` at `:671` (function header `:670`). Both `rk` values are computed ONCE before the player `while` loop and reused across the repeated `ticketsOwedPacked[rk][player]` reads — the hoist (`mapping(address=>uint40) storage owedMap = ticketsOwedPacked[rk]`) is **behavior-identical, gas-only**.
- **C5. The gas-peg constants live at `DegenerusGame.sol:1539-1546`** (`AUTO_GAS_PRICE_REF = 0.5 gwei` :1539, `AUTO_RESOLVE_BET_GAS_UNITS = 66_528` :1545, `AUTO_OPEN_BOX_GAS_UNITS` :1546) — CONTEXT cited the range `:1539-1546` (the v46 Phase 319 calibrated values; re-pegged at GAS, placeholders at SPEC).
- **C6. `degeneretteResolve` interface-file rename rows are ABSENT (Plan 02 §A.2 — Rule-1 correction).** The plan `<interfaces>` and CONTEXT D-05a both assumed an `IDegenerusGame`/`IDegenerusGameModules` signature for `autoResolve`/`_autoResolveBet` to rename. **Grep proved none exists** (`grep -rln "autoResolve\|_autoResolveBet" contracts/interfaces/` → ZERO). `autoResolve` is defined **directly on `DegenerusGame.sol`** (`:1587`, called as concrete `game.autoResolve(...)`); `_autoResolveBet` (`:1684`) is invoked via `this._autoResolveBet(...)` (a self-`this.` external call resolving against the concrete contract's own ABI, NOT an imported interface). So the interface-file rename rows are a **no-op** — BATCH-02 must NOT add or chase an interface edit for the rename. This SHRINKS the rename surface by one edit class and removes a false IMPL target. (The broader `IDegenerusGameModules.resolveBets` selector at `:1692` is the *resolveBets* selector, NOT renamed by D-05 — only the two `autoResolve*` symbols rename.)

> **Re-grep at edit time.** Every line cited above (and below) shifts once the Phase 330 batched diff lands. The 4 Plan-01 drifts (C1 KEEP-04 +3 / C2 30-min +4 / C3 death-clock +2 / C4 GASOPT-01 header-vs-key offset) and the C6 interface-ABSENT correction are recorded so IMPL re-anchors against live source, carrying zero un-grepped "by construction" claim (`feedback_verify_call_graph_against_source`).

### Load-bearing decision verdicts (folded from the 2 ATTEST docs)

- **ROUTER-07 / D-01a — NO-GUARD BASIS HOLDS.** Per-leg grep (Plan 01 §B): the **advance** leg makes ZERO direct ETH sends (`grep '\.call{value\|\.transfer(\|\.send('` over `AdvanceModule.sol` → ZERO sites; jackpot winnings credit the `claimablePool` pull ledger :894); **autoOpen** and **_autoBuy** route player value through the `claimableWinnings` pull ledger and send ETH only to pinned `ContractAddresses.*` / the keeper-contract (the only buy-path push is the unspent-value refund back to `msg.sender` = the pinned keeper-contract AfKing itself, `DegenerusGame.sol:1763`); the bounty in all three legs pays as `creditFlip` flip-credit (keeper-never-a-payee) fired LAST (CEI — AfKing `:846` after `batchPurchase` `:821`). The out-of-leg redemption sends (`:1907`/`:2213`/`:2230`/`:2251`) are confirmed NOT reachable from the doWork legs. **0 untrusted-push legs → 0 ROUTER-07 blockers.** Formal basis (recorded verbatim, §2): *keeper-never-a-payee + no untrusted ETH send + one-category structural early-return + `creditFlip`-last CEI ordering.* (Empirical double-pay backstop = TST-02, D-01b.)
- **GAS-03 / D-03 — EPOCHS INTENTIONALLY DISTINCT.** AfKing absolute-day epoch (`today*1days+82_620`, `AfKing.sol:829`) vs AdvanceModule game-day epoch (`(day-1+DEPLOY_DAY_BOUNDARY)*1days+82_620`, `AdvanceModule.sol:243-246`) measure DIFFERENT things (per-day buying window that resets at each midnight boundary vs the lagging game-day `dailyIdx` advance-liveness escalation that keeps growing across a multi-day stall). Design-1 single-sources the ADVANCE multiplier in `AdvanceModule` and returns it via the `(uint8 mult, bool rewardable)` tuple → the router consumes the returned value and never recomputes (Pitfall 4 "no-recompute / no off-by-one" met); AfKing's autoBuy epoch is left UNTOUCHED. **No physical merge (D-03a)** — a future auditor seeing the divergence reads this attestation: it is intentional, not a bug.
- **ADV-04 — NO NEW IN-WINDOW READ.** `totalFlipReversals` (the player-controllable VRF-window nudge) is read (`:1838`) INTO the consumed VRF word `finalWord` and reset to 0 (`:1844`) inside `_applyDailyRng` (`:1834`); the request→consume freeze window (`v45-vrf-freeze-invariant`) is intact. The router consumes the daily tick via the design-1 RETURN and **recomputes nothing** — it neither reads nor writes any rng-window state, introducing NO new mutable in-window SLOAD. (Empirical freeze fuzz = TST-01, Phase 332.)
- **Invariant (c) / D-04 — FALLBACK CALLERS INTACT.** The 30-min universal bypass (`:1012`, C2), `DegenerusVault.gameAdvance()` (`:527-528`, `onlyVaultOwner`), `StakedDegenerusStonk.gameAdvance()` (`:421-422`, permissionless), and the 120-day death-clock (`:109`/`:1200`, C3) all confirm re-homing the advance bounty into `doWork` reduces NO structural `advanceGame()` caller — it only moves the PAYMENT.
- **D-05f — INERT-SAFE (losing-bet liveness; SURFACE-TO-USER: NONE).** Plan 02 §D grep-verified all 8 `degeneretteBets` consumers (Storage decl `:1449` + submission `:526` + resolution read `:605` + delete `:634` + AUTO-02 probe `:1596` + per-item read `:1601` + view `:2319`): every one treats an unresolved (losing) Degenerette bet as **INERT storage cruft** — a nonzero nested-mapping slot that is never iterated, never counted, never reconciled against, and never required-empty by any invariant / accounting / RNG-slot / gameOver / sweep / cleanup path. The `delete` at `DegeneretteModule:634` exists ONLY for LOCAL double-resolve idempotency (the `packed == 0` revert `:606` + the AUTO-02 probe `:1596`); nothing CONSUMES the fact that the delete happened. The three flagged candidate modules — `GameOverModule` / `JackpotModule` / `AdvanceModule` — are all **grep-CLEAN of `degeneretteBets` (0 hits each)**; there is NO outstanding-bet counter, no per-day tally, no require-empty anywhere (`grep "pendingDegenerette\|outstandingBet\|degeneretteCount\|pendingBet" contracts/` → ZERO). Winning bets are self-resolved by owners claiming winnings; losing bets left unresolved cost nothing and break nothing. **No path REQUIRES losing Degenerette bets to be resolved → safe to drop the per-item break-even incentive.** Moreover, a FLAT count-independent reward NUDGES clearing the WHOLE backlog in one tx (the keeper pays the same flat ~1 BURNIE whether it resolves 3 or 300) → the flat re-peg mildly IMPROVES backlog liveness rather than starving it. **SURFACE-TO-USER: NONE** — no losing-bet-liveness dependency exists at `0cc5d10f` (this SPEC records the inert-safe finding; it does NOT invent a SURFACE-TO-USER block where none is warranted).

---

## 1. Shared Signatures (BATCH-01 reconciliation, ROADMAP SC2)

> The cross-file shared constructs the router/advance rework settles. ONE final signature + an explicit **apply-order** per construct, so no downstream file ships a broken intermediate. R1 (the `advanceGame` design-1 return) is the PRODUCER that R3 (the discovery views) and the AfKing `doWork` router (R2) consume; R4 settles the bounty-payment composition that the one-category early-return (§2 invariant a) enforces. The producer-before-consumer order is the load-bearing constraint: **AdvanceModule (R1 produce) → DegenerusGame wrapper + views (R1 decode / R3) → interfaces → AfKing router (R2/R4)** so advancing is never unrewarded mid-diff.

### R1 — `advanceGame` design-1 return tuple `(uint8 mult, bool rewardable)` + the `:275` wrapper decode

**Co-edits this construct:** the PRODUCER edit on `AdvanceModule.sol` + the CONSUMER decode on `DegenerusGame.sol` + the interface signature update. This is the keystone — every other signature consumes it.

- **Final return shape (design-1):** `function advanceGame() external returns (uint8 mult, bool rewardable)` — currently `advanceGame()` is **void** (`AdvanceModule.sol:155`, body resolves `caller`/`ts`/`day`/`inJackpot`/`lvl` :156-160). The IMPL PRODUCER edit adds the tuple return.
  - `mult` (uint8) = the stall multiplier computed ONCE in `AdvanceModule` at `:243-254` (game-day epoch, bands 1/2×@20m/4×@1h/6×@2h). Single-sourced — the router never recomputes (GAS-03 / §2 invariant d).
  - `rewardable` (bool) = a **DISTINCT bool**, NOT implied by `mult > 0` (Plan 01 §D.2 verdict). A leg can do advance work but be non-rewardable (e.g. a standalone fallback caller, or the `:876`-only SDGNRS-merge-credit path), and `mult` can be ≥1 independent of rewardability. The router pays the re-homed advance bounty IFF `rewardable`, scaled by `mult`.
- **The decode site (CONSUMER):** `DegenerusGame.advanceGame` wrapper at `:275` — currently `(bool ok, bytes memory data) = ContractAddresses.GAME_ADVANCE_MODULE.delegatecall(abi.encodeWithSelector(IDegenerusGameAdvanceModule.advanceGame.selector));` (:276-282) captures `data` ONLY for the revert path (`if (!ok) _revertDelegate(data);` :284, `_revertDelegate` :1022). The IMPL CONSUMER edit decodes `data` into `(uint8 mult, bool rewardable)` on the success branch (:283-284) + updates the wrapper's external signature + forwards the tuple to external callers (the AfKing router via `IGame`).
- **Producing file → consuming files:** PRODUCER `modules/DegenerusGameAdvanceModule.sol` (`advanceGame` :155 gains the tuple) → CONSUMER `DegenerusGame.sol` (`:275` wrapper decodes + forwards) → `interfaces/IDegenerusGame.sol` + `interfaces/IDegenerusGameModules.sol` (the updated `advanceGame` signature) → `AfKing.sol` `doWork` (reads the tuple via `IGame.advanceGame()`).
- **Apply-order:** R1 is the FIRST edit in the diff (the AdvanceModule producer + the Game wrapper decode + the interface signature) — nothing else can be wired until the return shape exists. The 3 caller-reward `creditFlip` deletions (§3) land in the SAME AdvanceModule edit.

### R2 — `doWork(uint256 maxCount)` entrypoint + the ROUTER-06 `NoWork()` revert + per-leg `maxCount` + the LOCKED D-06 `maxCount==0` default

**Co-edits this construct:** the NEW `AfKing.sol` router entrypoint. Consumes R1 (the advance tuple), R3 (the discovery predicates), R4 (the bounty composition).

- **Final signature:** `function doWork(uint256 maxCount) external returns (...)` on `AfKing.sol` — the unified keeper entrypoint. Priority routing: **advance → autoOpen → autoBuy**, RETURNING after the first rewarded category (§2 invariant a, the one-category structural early-return).
- **Per-leg `maxCount` mapping (D-06c):** **advance** — NO count arg (`advanceGame()` takes none; it does its own internally-bounded ticket batch incl. the mid-day partial-drain at `AdvanceModule:225`); **autoOpen** — `autoOpen(maxCount)` (the `while (cursor < qlen && opened < maxCount)` loop, `DegenerusGame.sol:1656`); **_autoBuy** — `_autoBuy(maxCount)` (the internal refactor of `autoBuy(uint256)` `:567`, the `processed < maxCount` bound). So `maxCount` applies ONLY to the two count-bounded legs.
- **ROUTER-06 `NoWork()` revert (D-02):** revert a dedicated `error NoWork()` at the `doWork` level when ALL THREE O(1) predicates are empty (no advance due, no boxes pending, no buys pending). This is the established AfKing anti-spam idiom (`EmptyAutoBuy` `:143` raised at `autoBuy(0)` `:569`; `NoSubscribersAutoBought` `:146` raised `if (!didWork) revert` `:806`). Because `doWork` enters a leg ONLY when that leg's predicate has work, the per-leg internal revert never trips — `NoWork()` is the single no-work signal. NOT a bool/sentinel return (rejected — breaks the idiom; the discovery views ARE the keeper's pre-call probe).
- **LOCKED D-06 — `maxCount == 0` default (refines ROUTER-01/GAS-01/TST-02, NO new REQ-IDs, D-06g):**
  - `maxCount == 0` resolves to a **FIXED per-leg default count** ≈ `~10M-gas-budget ÷ avg-marginal-per-item` → `DEFAULT_AUTO_OPEN_COUNT` / `DEFAULT_AUTO_BUY_COUNT`, applied to the count-bounded legs ONLY (advance is self-bounded, no count, D-06c).
  - Implemented as a plain `while (processed < DEFAULT_COUNT && cursor < len)` — **NOT a `gasleft()`-bounded loop** (D-06a: `gasleft()` is cheap — the `GAS` opcode is 2 gas — but a fixed count is SIMPLER with no fragile per-iteration reserve-floor constant; a `gasleft` loop only "never OOGs" if its reserve floor ≥ the true worst-case single iteration + finalization, the same worst-case-estimation problem relocated).
  - **EmptyAutoBuy reconciliation (D-06e):** the router's `_autoBuy(0)` path MUST NOT revert `EmptyAutoBuy` — it processes the default-count batch. **Settled (the lean):** the default lives in the SHARED INTERNAL `_autoBuy`, so `0`=default everywhere; the standalone external `autoBuy(uint256)` also adopts `0`=default and **`EmptyAutoBuy` is removed/repurposed** (no longer a revert path). Verify at TST-02 (Phase 332).
  - **Worst-case OOG (D-06d):** if a pathological worst-case-clustered batch exceeds the supplied gas, the tx reverts OUT-OF-GAS — a CLEAN state-reverting revert (no fund loss, no invariant break, no partial corruption); the keeper retries with a smaller EXPLICIT `maxCount` (the manual escape hatch). never-OOG here is keeper-UX/liveness, **NOT a security invariant** → the statistical (not absolute) guarantee is accepted under the security floor. Advance liveness has its own independent fallbacks (D-04), unaffected.
  - **Faucet-neutrality (D-06f):** the autoBuy/autoOpen bounties are per-item break-even (the `AfKing.sol:845` `bountyEarned = batchLen * peg` per-item MARGINAL pattern). A larger default batch pays proportionally more bounty AND costs proportionally more gas → still per-item break-even, NO positive-EV from batching; the GAS-05 round-trip guard must hold at the MAX default-count batch. The one-category early-return is preserved: `doWork(0)` does ONE category's default batch, one `creditFlip` per tx.
  - **Placeholders (D-06g):** the `~10M` GAS_BUDGET + the per-leg `DEFAULT_*_COUNT` constants are **SPEC PLACEHOLDERS** — calibrated at GAS (331) from GAS-01's avg + worst-case marginal per-item gas (avg sizes the count, worst-case sizes the ≈3× headroom margin to the ~30M block limit; the v46 Phase 319 CR-01 "peg to the marginal, never a single total" discipline applies). D-06 REFINES ROUTER-01 (`doWork(maxCount)` signature, IMPL 330) + GAS-01 (marginal/avg gas calibration, 331) + TST-02 (router behavior proof, 332) — **NO new REQ-IDs minted.**
- **Producing file → consuming files:** PRODUCER `AfKing.sol` (the `doWork` router + the `NoWork()` error decl + the `_autoBuy` refactor that hosts the D-06 default). Consumes `IGame.advanceGame()` (R1), `IGame.autoOpen(maxCount)` + the AfKing-local buys predicate (R3).
- **Apply-order:** R2 (the AfKing router + `_autoBuy` refactor) lands LAST in the diff — after R1's tuple, the wrapper decode, the discovery views (R3), and the interface adds all exist, so `doWork` wires against settled signatures.

### R3 — the three O(1) discovery views (ROUTER-04, no unbounded scans)

**Co-edits this construct:** `DegenerusGame.sol` (advance + boxes views) + `AfKing.sol`-local (buys-pending) + the `interfaces/*` view adds. All O(1) — Pitfall 5 (no growable-set iteration).

- **`advanceDue()` — covers BOTH new-day AND mid-day partial-drain (Plan 01 §F1):**
  - new-day: `currentDayView() != dailyIdx` (`currentDayView()` `:462` returns `_simulatedDayIndex()`; `dailyIdx` is the monotonic day counter, `DegenerusGameStorage.sol:231`).
  - mid-day partial-drain: the `LR_MID_DAY` packed flag — `_lrRead(LR_MID_DAY_SHIFT, LR_MID_DAY_MASK) != 0` (set when a ticket-batch is mid-processing `AdvanceModule:1085`, cleared when finished `:222`).
  - **O(1)** — two scalar/packed SLOADs, no scan. Lives on `DegenerusGame`.
- **`boxesPending()` (Plan 01 §F2):** `boxPlayers[activeIndex].length > boxCursor` AND `lootboxRngWordByIndex[activeIndex] != 0` (`boxCursor` `:1551`, `boxPlayers` mapping `:1562`, the open gated on `lootboxRngWordByIndex[index] != 0` `:1647`). **O(1)** — `.length` (one SLOAD of the dynamic-array length slot) + two scalars, NO iteration over the array. Lives on `DegenerusGame` (the box-open state is game state).
- **buys-pending — via AfKing-local cursor (Plan 01 §F3):** compare `cursor = _autoBuyDay == today ? uint256(_autoBuyCursor) : 0;` (`AfKing.sol:577`, `_autoBuyDay` :215 / `_autoBuyCursor` :216 in one slot) vs the subscriber-set length (`subscriberCount()` :514, `_subscribers.length`). **O(1)** — two scalar SLOADs + `.length`. Lives **AfKing-local** (the cursor is AfKing state, not game state).
- **View-location resolution:** advance + boxes views on `DegenerusGame` (they read game state); buys-pending AfKing-local. None iterates over a growable set → all O(1), no unbounded scans (ROUTER-04 satisfied).
- **Producing file → consuming files:** PRODUCER `DegenerusGame.sol` (`advanceDue()`, `boxesPending()`) + `AfKing.sol` (the buys-pending predicate, inline in `doWork`'s routing). Added to `interfaces/IDegenerusGame.sol` / `IDegenerusGameModules.sol` (the new view signatures). Consumed by `AfKing.doWork`'s routing (R2) AND by the off-chain keeper UI as the pre-call probe (so the keeper avoids `NoWork()` reverts).
- **Apply-order:** R3 lands with the `DegenerusGame` wrapper edit (after R1's tuple producer, with/before the interface adds), so the AfKing router (R2) can consume the view signatures.

### R4 — the bounty-payment composition (one creditFlip, router pays ONLY the re-homed advance bounty)

**Co-edits this construct:** `AfKing.sol` `doWork` (the advance-leg payment) + the existing in-callee bounties on autoOpen/autoBuy (UNCHANGED).

- **The router pays ONLY the re-homed advance bounty** — ONE `creditFlip(msg.sender, advanceBounty)`, fired LAST (CEI), scaled by the design-1 `mult` and gated on `rewardable` (R1). The advance bounty is the one re-homed FROM the 3 deleted `AdvanceModule.sol:189/:225/:468` caller-reward sites (§3 / Plan 01 §D.2) INTO the router's advance leg.
- **The autoOpen / autoBuy legs keep their OWN in-callee bounty** — `autoOpen` pays `if (reward != 0) coinflip.creditFlip(msg.sender, reward);` (`DegenerusGame.sol:1676`); `_autoBuy` pays `creditFlip(msg.sender, bountyEarned)` (`AfKing.sol:846`). The router does NOT double-pay: it routes into a leg, that leg pays its own bounty internally, and the one-category early-return (§2 invariant a) returns after the first rewarded category fires its single `creditFlip`. So `doWork` results in exactly ONE `creditFlip` per tx, preserving the PLAN-CRANK §7 one-creditFlip-per-tx faucet bound.
- **KEEP-04 affiliate-code passthrough survives the `_autoBuy` refactor (ROUTER-05 pre-condition, C1):** the `bytes32("DGNRS")` two-tier 75/20/5 wiring is game-side at `DegenerusGame.sol:1781` (inside `_batchPurchaseUnit`); AfKing only forwards `totalValue` via `batchPurchase` (no affiliate arg). The internal `autoBuy → _autoBuy` refactor does NOT touch the affiliate path → the passthrough is unchanged. Confirmed LIVE at `0cc5d10f` (Plan 01 §A13).
- **Producing file → consuming files:** `AfKing.sol` `doWork` (the advance-leg `creditFlip`); the autoOpen bounty stays in `DegenerusGame.sol:1676`, the autoBuy bounty in `AfKing.sol:846`.
- **Apply-order:** R4 is part of R2's AfKing router edit (LAST). The 3 advance `creditFlip` deletions that re-home the bounty land in R1's AdvanceModule edit (FIRST) — so between R1 and R2 there is a transient where the advance bounty is deleted but the router has not yet re-homed it. The producer-before-consumer order ensures the FULL diff lands atomically (one batched commit) — there is no intermediate SHIPPED state where advancing is unrewarded (§3 edit-order map).

---

## 2. Structural invariants + dispositions + the D-05 design-lock (ROADMAP SC1 + SC3)

> Each subsection records the **lock + its source (decision id D-NN or the ATTEST grep verdict) + the IMPL instruction it produces**. The 4 invariants are CODE invariants (not comments). Each closes a named pitfall from `.planning/research/PITFALLS.md`.

### Invariant (a) — ONE-CATEGORY STRUCTURAL EARLY-RETURN (BATCH-01; closes Pitfall 4 bounty-stacking)

- **Lock:** `doWork` routes by priority **advance → autoOpen → autoBuy** and **RETURNS after the first rewarded category pays** — a CODE invariant, NOT a comment. A single tx can never collect advance + open + buy bounties (no reward fall-through). This preserves the one-`creditFlip`-per-tx faucet bound (PLAN-CRANK §7 the three faucet locks + the `AfKing.sol:846` one-`creditFlip`-last model) AND keeps the priority order from being a farming vector beyond a single category's per-item break-even.
- **Source:** D-02 / R2 / R4 routing; PITFALLS Pitfall 4 ("Reward fall-through (router pays >1 category per tx) → Never" in the Technical-Debt table); the `creditFlip`-per-tx model attested at `AfKing.sol:846` (Plan 01 §A10).
- **IMPL instruction:** an EXPLICIT `return` after the chosen category fires its single `creditFlip` — advance-if-due runs first (highest priority, reachable + rewarded independent of the open/buy legs per Pitfall 8 / invariant c), and if it does rewardable work the function returns BEFORE reaching the autoOpen/autoBuy legs; likewise autoOpen returns before autoBuy. Within a category, the per-item accumulation stays caller-bounded by `maxCount` (the anti-gas-DoS property, Pitfall 5) and each item individually clears the marginal-cost bar (Pitfall 1 — no manufactured-rewardable-item-cheaper-than-its-bounty). The router NEVER falls through to a second category in one tx.

### Invariant (b) — FROZEN ADVANCE-CONSUME under the new router composition (ADV-04; closes Pitfall 3 advance-timing/MEV + the v45 VRF-freeze floor)

- **Lock:** the player-controllable `totalFlipReversals` nudge (`AdvanceModule.sol:1838`, read INTO `finalWord` :1840-1842, reset to 0 :1844, all inside `_applyDailyRng` :1834) is FROZEN request→consume per `v45-vrf-freeze-invariant`. The router consumes the daily tick via the design-1 RETURN (R1's `(uint8 mult, bool rewardable)` tuple) and **recomputes nothing** — it adds NO new mutable in-window SLOAD. Lock the router's internal ordering: the advance-if-due leg reads only frozen state (the returned tuple); whichever order the legs run, the consume reads only the frozen word — no leg introduces a fresh rng-window read.
- **Source:** ADV-04 / Plan 01 §E verdict (3 anchors MATCH; `totalFlipReversals` :1838 + reset :1844 in `_applyDailyRng` :1834; the backward-trace per `feedback_rng_window_storage_read_freshness` enumerated the consume-path SLOADs `totalFlipReversals` + `rawWord` and confirmed the router adds none). The non-reset gap-day path :1724 + the gap-day zero-nudges path :1777 are EXISTING behavior, not router-introduced.
- **IMPL instruction:** the router consumes `(mult, rewardable)` from `advanceGame()`'s return — it does NOT re-read `totalFlipReversals`, `rngWordCurrent`, `rngWordByDay`, or any rng-window state, and does NOT recompute the multiplier (single-sourced in AdvanceModule, GAS-03 / invariant d). **TST-01 handoff (Phase 332):** the EMPIRICAL freeze proof (rotation/perturbation between rng-request and fulfilment asserts byte-identical output; extend `RngLockDeterminism`/`RngFreezeAndRemovalProofs`) proves the freeze survives the new composition. This SPEC attests on paper that the router adds no new in-window read; TST-01 proves it empirically.

### Invariant (c) — GUARANTEED FREE-FALLBACK `advanceGame()` CALLER (D-04; closes Pitfall 8 unrewarded-advance liveness)

- **Lock:** re-homing the advance bounty into `doWork` reduces NO structural `advanceGame()` caller — EXISTING structural paths ONLY, NO new mechanism (`feedback_frozen_contracts_no_future_proofing`). Backstop hierarchy:
  - **PRIMARY (rewarded):** the router's advance leg keeps the stall-escalating advance bounty (re-homed, NOT removed — the payment moves from standalone `advanceGame()` into `doWork`'s advance leg).
  - **SECONDARY (structurally-guaranteed, unrewarded):** standalone `advanceGame()` is permissionless to ANYONE 30+ min after the day boundary (the unconditional `if (elapsed >= 30 minutes) return;` at `AdvanceModule.sol:1012`, C2 — covers ~23.5h/day with no caller predicate); the first-30-min window is covered with no gap by the router bounty (escalating 2× at 20 min) + the 15-min pass-holder / DGVE-majority bypass tiers. Plus the explicit protocol-owned wrappers `DegenerusVault.gameAdvance()` (`onlyVaultOwner` → `advanceGame()`, `DegenerusVault.sol:527-528`, always-bypass via DGVE majority) and `StakedDegenerusStonk.gameAdvance()` (permissionless → `advanceGame()`, `StakedDegenerusStonk.sol:421-422`).
  - **TERTIARY (failsafe):** the ~120-day death-clock (`AdvanceModule.sol:109` L1+ 120-day timeout; extends by stall duration `:1200`, C3) latches gameOver if the game is truly abandoned.
- **Source:** D-04 / Plan 01 §G verdict (4 anchors MATCH; the 30-min bypass +4 and death-clock +2 line-drifts carried as C2/C3). The Vault/sStonk wrappers are read-only — NOT modified by this milestone.
- **IMPL instruction:** delete the 3 standalone advance caller-reward `creditFlip` sites (`:189/:225/:468`, §3) and re-home the bounty into the router's advance leg ONLY — touch NO fallback caller. Do NOT add a new fallback mechanism (the existing 3-tier hierarchy is sufficient; no future-proofing). `advanceGame()` stays a void→`(uint8,bool)` permissionless function reachable by all the SECONDARY callers unrewarded.

### Invariant (d) — SINGLE DAY-START EPOCH satisfied by design-1 (GAS-03 / D-03; closes Pitfall 7 dual-epoch off-by-one)

- **Lock:** design-1 single-sources the ADVANCE multiplier — computed ONCE in `AdvanceModule` at `:243-246` (game-day epoch `(day-1+DEPLOY_DAY_BOUNDARY)*1days+82_620`, bands 1/2/4/6× :248-254), RETURNED via R1's tuple, consumed by the router WITHOUT recompute (no off-by-one, Pitfall 4 money-path "no-recompute"). The two intentionally-distinct epochs — AfKing absolute-day (`today*1days+82_620`, `AfKing.sol:829`, the autoBuy per-day-buying-window multiplier that resets at each midnight boundary) vs AdvanceModule game-day (`:243-246`, the advance-liveness multiplier that keeps growing across a multi-day stall) — are **NOT physically merged (D-03a)**.
- **Source:** GAS-03 / D-03 / Plan 01 §C verdict (4 anchors MATCH; the two formulas measure DIFFERENT things → no duplicated number). AfKing's autoBuy epoch is left UNTOUCHED.
- **IMPL instruction:** do NOT physically merge the two epoch formulas. The advance multiplier is returned from `advanceGame()` (R1) and the router consumes the returned `mult` — it never recomputes a day-start. **Document WHY the two epochs differ** (a code comment at the AfKing autoBuy epoch site and/or the AdvanceModule advance epoch site referencing this attestation) so a future auditor seeing the divergence does not flag it as a bug — it is intentional, not duplication. GAS-03 cross-references invariant (d): design-1-satisfies; no separate decision.

### Disposition ROUTER-07 — NO `nonReentrant` guard on `doWork` (D-01)

- **Lock:** NO `nonReentrant` guard on `doWork`, on the grep-checked per-leg **no-untrusted-ETH-send** basis (D-01a, Plan 01 §B per-leg rows). The formal basis (recorded verbatim): *keeper-never-a-payee + no untrusted ETH send + one-category structural early-return + `creditFlip`-last CEI ordering.* Preserves AfKing's documented invariant `AfKing.sol:100-101` ("No reentrancy guard — strict CEI everywhere; the keeper is never a payee in any contract it calls").
- **Source:** D-01 / Plan 01 §B verdict (0 untrusted-push legs → 0 ROUTER-07 blockers): the **advance** leg makes ZERO direct ETH sends; **autoOpen** + **_autoBuy** route player value through `claimableWinnings` (pull) and send ETH only to pinned `ContractAddresses.*` / the keeper-contract; the bounty pays as `creditFlip` flip-credit (keeper-never-a-payee) fired LAST (CEI). The out-of-leg redemption sends (`:1907`/`:2213`/`:2230`/`:2251`) are NOT reachable from the doWork legs. PITFALLS Pitfall 6 (router→game→creditFlip/batchPurchase reentrancy) — there is no untrusted control-flow handoff anywhere in the composition, so there is nothing to re-enter through and a guard would guard nothing.
- **IMPL instruction:** do NOT add a `nonReentrant` guard to `doWork` or any routed leg. Keep strict CEI: all state effects before external calls, `creditFlip` LAST in each leg. **D-01b (empirical backstop, scoped to PROVE the disposition):** the `router→game→creditFlip` double-pay reentrancy regression stays a Phase 332 TST-02 success criterion REGARDLESS of the guard decision — the SPEC scopes TST-02 to empirically prove the no-guard disposition (a malicious reentrant `creditFlip`/`batchPurchase` callee cannot double-collect the bounty or escape the one-category early-return). No untrusted-push leg was found at `0cc5d10f` → no ROUTER-07 blocker is carried.

### Disposition GAS-03 — single day-start epoch = design-1-satisfies

- **Lock:** design-1-satisfies (cross-reference to invariant (d) — no duplicate decision). The advance multiplier is single-sourced in `AdvanceModule` and returned; the two epochs are intentionally-distinct and NOT merged (D-03/D-03a).
- **Source:** see invariant (d) / Plan 01 §C.
- **IMPL instruction:** see invariant (d). No separate edit.

### D-05 — `degeneretteResolve` design-lock (v49.0 design item; code at 330/BATCH-02, REQ GAS-06/TST-05 at 331/332)

> This is a v49.0 DESIGN ITEM locked here even though its code lands at Phase 330 (BATCH-02) and its REQ-IDs (GAS-06 / TST-05) are owned by 331/332. The router-fold is OUT (architecturally blocked).

- **D-05a — RENAME (mechanical, rides BATCH-02):** external `autoResolve(address[] players, uint64[] betIds)` (`DegenerusGame.sol:1587`) → `degeneretteResolve`; `onlySelf` internal `_autoResolveBet(address,uint64)` (`:1684`) → `_degeneretteResolveBet`; the self-call site `try this._autoResolveBet(players[i], betIds[i])` (`:1606`) → `this._degeneretteResolveBet(...)` in lock-step (same contract; missing it dangles the selector). **NO interface-file edit (C6 / Plan 02 §A.2 — the rows are ABSENT/no-op; both symbols are defined directly on `DegenerusGame.sol`, never via `IDegenerusGame`/`IDegenerusGameModules`).** Leaves the `auto*` family (`autoOpen`, AfKing `autoBuy`) UNTOUCHED — post-re-peg `autoResolve` is no longer a gas-pegged keeper-router action but a distinct flat-"lose" Degenerette-resolution helper, so its name should reflect the domain. The 5 test files / 57 references (incl. the `CrankLeversAndPacking.t.sol` LITERAL source-string assertions `_countOccurrences(game_, "function autoResolve(")` at :277/:290/:415 and `"function _autoResolveBet("` :381) MUST update atomically with the contract rename or they BREAK at compile/run (TST-05 re-greens; `test/` is agent-committable).
- **D-05b — PAYMENT SHAPE (FEASIBLE, Plan 02 §B):** a FLAT literal ~1 BURNIE (1e18) `creditFlip` ONCE per tx, count-INDEPENDENT, IFF ≥3 NON-WWXRP bets resolve successfully; revert `NoWork()` when ZERO bets resolve; for 1–2 resolved, **resolve-and-pay-0 (do NOT revert — reverting would roll back the resolutions and strand the tail)**. The lean = resolve-always, pay-at-≥3, revert-only-at-0. EXACT IMPL edit targets (Plan 02 §B.2): REMOVE the per-item peg at `:1611-1614` (`reward += _ethToBurnieValue(AUTO_RESOLVE_BET_GAS_UNITS * AUTO_GAS_PRICE_REF, priceForLevel(lvl))`), replace with a per-item `++successCount` in the NON-WWXRP arm; REPLACE the post-loop `if (reward != 0) coinflip.creditFlip(msg.sender, reward);` at `:1622` with `if (successCount >= 3) coinflip.creditFlip(msg.sender, RESOLVE_FLAT_BURNIE);` + the `NoWork()`-at-zero-resolution revert; ADD a `RESOLVE_FLAT_BURNIE` constant (~1e18, exact value SOFT — deferred to GAS, D-05e). Reuse the ROUTER-06 `NoWork()` selector (D-02). The flat shape is expressible on the existing `:1587-1622` per-item loop (per-item currency decode + WWXRP fork + try/catch + single post-loop creditFlip all already exist) — a localized arithmetic + boundary swap.
- **D-05c — REAL-GAS net-loss basis (Plan 02 §C; `feedback_bounty_exploit_uses_real_gas_not_peg_ref`):** the "~1 BURNIE never remotely exploitable" claim rests on REAL prevailing tx gas, **explicitly NOT the 0.5-gwei `AUTO_GAS_PRICE_REF` peg** (a deliberately below-market accounting figure the earlier draft wrongly used — USER corrected this twice). 1 BURNIE ≤ `mintPrice/1000` ≤ `0.24/1000 = 0.00024 ETH` (derived by inverting `PRICE_COIN_UNIT = 1000 ether`, `DegenerusAdmin.sol:393`) AND ILLIQUID (paid as coinflip flip-credit, not transferable ETH — real extractable value is a fraction behind the house edge). The keeper pays REAL gas: `G_total ≥ 21_000 + 3 × ~66_528 + overhead ≈ 220k+` for the ≥3 minimum × 5–50+ gwei = 0.0011–0.011+ ETH = **~4.6–46× the peg ceiling → every qualifying tx is a NET LOSS → no positive-EV farm**. The ≥3 gate only WIDENS the margin. The only theoretical positive corner {late game mintPrice 0.24 ∧ gas < ~3.6 gwei ∧ flip-credit fully extractable at peg} collapses once the ≥3 minimum's 220k+ gas pushes break-even below ~1.1 gwei AND is gated by an almost-certainly-FALSE illiquidity assumption → not realistically reachable.
- **D-05d — preserved invariants (Plan 02 §A.4, all grep-confirmed PRESERVED):** WWXRP (`currency == 3`) stays EXCLUDED — the ≥3 count is NON-WWXRP only (a WWXRP-only batch can never reach 3 → never pays; AUTO-04 intent); the count increments ONLY in the `try`-success non-WWXRP arm (counts SUCCESSES, not attempts). AUTO-02 probe (`:1596` item-0 already-resolved → `BatchAlreadyTaken`), per-item try/catch isolation (`:1606`/`:1616`), self-resolve allowed (REW-04, `:1587` no caller gate — even safer under a flat "lose"), ONE `creditFlip`-CEI-last (REW-02, `:1622`) all unchanged.
- **D-05e — GAS-06 sanity-check handoff (NOT a blocker):** at Phase 331, confirm the literal ~1 BURNIE stays below the REAL gas of a 3-resolution tx across the plausible gas-band (specifically the low-gas / high-mintPrice corner), factoring flip-credit illiquidity; only lower the constant or add a scaled gate if a REALISTIC corner actually flips positive-EV. The exact constant is SOFT — confirmed-sub-real-gas at GAS, NOT pinned at SPEC. The D-05c basis already shows a comfortable net-loss margin at every realistic gas price. (IMPL housekeeping: `AUTO_RESOLVE_BET_GAS_UNITS` `:1545` likely goes DEAD after the per-item peg is removed — grep at IMPL; `autoOpen` uses the separate `AUTO_OPEN_BOX_GAS_UNITS` :1546. Recorded, not a blocker.)
- **D-05f — losing-bet liveness = INERT-SAFE (Plan 02 §D; the load-bearing deliverable):** **INERT — SAFE; SURFACE-TO-USER: NONE.** All 8 `degeneretteBets` consumers treat an unresolved losing bet as inert nested-mapping cruft; the `delete` at `DegeneretteModule:634` is LOCAL-double-resolve-idempotency-ONLY (serving the `packed == 0` revert :606 + the AUTO-02 probe :1596); nothing consumes the fact that the delete happened. GameOver/Jackpot/Advance modules are grep-CLEAN of `degeneretteBets` (0 hits each); no outstanding-bet counter / per-day tally / require-empty exists anywhere. **No path REQUIRES losing Degenerette bets to be resolved** → safe to drop the per-item incentive. A flat count-independent reward NUDGES clearing the whole backlog in one tx (max work per paid tx) → mildly IMPROVES backlog liveness. **This SPEC records the inert-safe finding; it does NOT invent a SURFACE-TO-USER block where none is warranted.** (Had any consumer required the delete to fire — a counter decrementing on delete, a gameOver require-empty, an RNG slot freed only on resolution — this would carry a `## SURFACE-TO-USER — LOSING-BET LIVENESS DEPENDENCY` block verbatim as a USER-decision gate for 330. None exists at `0cc5d10f`.)
- **D-05 / ROUTER-05 — router-fold OUT (architecturally blocked, Plan 02 §E):** `degeneretteResolve` stays a SEPARATE permissionless call taking caller-supplied `(players[], betIds[])`. `degeneretteBets` is a nested mapping (`DegenerusGameStorage.sol:1449`) with NO O(1) enumeration and NO pending-count sidecar → on-chain discovery is impossible (nothing to iterate in a nested mapping) or unbounded (violates ROUTER-04 "no unbounded scans"). The unified "one button" (firing `doWork()` + `degeneretteResolve(...)` together) is a FRONTEND concern — the keeper UI already indexes the arrays from `BetPlaced`-class events; NO router/signature change. ROUTER-05's "keeps its own in-game bounty unchanged" is amended to "…RENAMED + RE-PEGGED per GAS-06 (still a separate call)" (D-05g).

---

## 3. Per-item IMPL blueprint + producer-before-consumer edit-order map (the load-bearing input to Phase 330)

> **Files in the diff** (degenerus-audit `contracts/`, the ONE batched diff at Phase 330): `modules/DegenerusGameAdvanceModule.sol` (the design-1 producer + the 3 deleted caller-reward `creditFlip` sites), `DegenerusGame.sol` (the `:275` wrapper decode + the new `advanceDue()`/`boxesPending()` discovery views + the MintModule GASOPT-01 pointer hoist + the `degeneretteResolve` rename/re-peg [BATCH-02]), `interfaces/IDegenerusGame.sol` + `interfaces/IDegenerusGameModules.sol` (the new view signatures + the updated `advanceGame` signature — NO `degeneretteResolve` interface row, C6), `AfKing.sol` (the `doWork` router + the `NoWork()` error decl + the `_autoBuy` internal refactor + the re-peg/default-count placeholders + the GASOPT-02 `claimableWinningsOf` hoist), `modules/DegenerusGameMintModule.sol` (the GASOPT-01 `rk`-loop-invariant storage-pointer hoist), `modules/DegenerusGameDegeneretteModule.sol` (read-only for D-05; the resolution path / `delete` :634 is UNCHANGED). `DegenerusVault.sol` / `StakedDegenerusStonk.sol` (the invariant-(c) fallback wrappers) are read-only — NOT modified.

> **Edit-order (producer-before-consumer — so no intermediate state ships where advancing is unrewarded):**
> 1. **`AdvanceModule.sol`** — delete the 3 caller-reward `creditFlip` sites (`:189` new-day / `:225` mid-day partial-drain ADV-05 / `:468` main new-day; the `:876` SDGNRS merge-credit STAYS — NOT a caller bounty) + add the design-1 `(uint8 mult, bool rewardable)` return to `advanceGame()` (`:155`). PRODUCER FIRST.
> 2. **`DegenerusGame.sol`** — the `:275` wrapper decode of the delegatecall `data` into `(uint8 mult, bool rewardable)` + the new `advanceDue()` / `boxesPending()` discovery views + the MintModule GASOPT-01 pointer. (BATCH-02 rides here: the `degeneretteResolve`/`_degeneretteResolveBet` rename at :1587/:1684/:1606 + the flat-re-peg at :1611-1614/:1622 + `RESOLVE_FLAT_BURNIE`.)
> 3. **`interfaces/IDegenerusGame.sol` + `IDegenerusGameModules.sol`** — the new view signatures + the updated `advanceGame` signature. (NO `degeneretteResolve` row — C6.)
> 4. **`AfKing.sol`** — the `doWork` router (consuming R1's tuple via `IGame`, R3's views, R4's bounty composition) + the `NoWork()` error decl + the `_autoBuy` internal refactor (hosting the D-06 default-count) + the re-peg placeholder + the GASOPT-02 claimable-hoist. CONSUMER LAST.
> 5. **`MintModule.sol`** — the GASOPT-01 hoist (independent, gas-only; lands with step 2's pointer reference).
>
> So the FULL diff lands atomically (one batched commit, HELD for hand-review): there is no SHIPPED intermediate where the advance bounty is deleted (step 1) but the router has not re-homed it (step 4).

**Work-area blueprints (one paragraph each):**

- **The `doWork` router (AfKing):** the NEW `doWork(uint256 maxCount)` entrypoint — routes advance→autoOpen→autoBuy by the 3 O(1) discovery predicates (R3), RETURNS after the first rewarded category (invariant a, explicit early-return), reverts `NoWork()` when all 3 predicates are empty (R2/D-02). NO `nonReentrant` guard (ROUTER-07/D-01), strict CEI, `creditFlip` last. The D-06 `maxCount==0` fixed default-count lives in the shared `_autoBuy` (and the autoOpen leg's analog) — a plain `while (processed < DEFAULT_COUNT && cursor < len)`, NOT a `gasleft()` loop.
- **The advance bounty-removal + design-1 return (AdvanceModule):** delete the 3 caller-reward `creditFlip` sites (`:189/:225/:468`) — advance stays fully functional + unrewarded standalone (the state machine does not depend on them); add the `(uint8 mult, bool rewardable)` return (`mult` from the single-sourced :243-254 multiplier, `rewardable` a DISTINCT bool covering the 3 classified sites' work). The `:876` SDGNRS merge-credit STAYS.
- **The discovery views + wrapper (DegenerusGame):** `advanceDue()` (new-day `currentDayView() != dailyIdx` OR mid-day `LR_MID_DAY != 0`) + `boxesPending()` (`boxPlayers[idx].length > boxCursor` AND `lootboxRngWordByIndex[idx] != 0`) — both O(1) on `DegenerusGame`; the `:275` wrapper decodes the delegatecall `data` into the tuple on the success branch and forwards it. Buys-pending is AfKing-local (no game-side view).
- **The interfaces:** add the `advanceDue()`/`boxesPending()` view signatures + the updated `advanceGame` (returning the tuple) to `IDegenerusGame`/`IDegenerusGameModules`. The `degeneretteResolve` rename adds NO interface row (C6).
- **The GASOPT-01 / GASOPT-02 micro-opts (gas-only, behavior-identical):** GASOPT-01 — hoist `mapping(address=>uint40) storage owedMap = ticketsOwedPacked[rk]` once per call in `processFutureTicketBatch` (`MintModule:398`) + `processTicketBatch` (`:671`), dropping the outer keccak per ticket. GASOPT-02 — hoist the two `claimableWinningsOf(player)` reads in `autoBuy` (`AfKing.sol:691`/`:722`) to one call/iteration, keeping the existing laziness (both fire in ONE iteration only when `reinvestPct>0 && FLAG_DRAIN_FIRST`).
- **The D-05 `degeneretteResolve` rename + re-peg (DegenerusGame, BATCH-02 riding the same diff but a SEPARATE call):** rename at :1587/:1684/:1606; re-peg at :1611-1614 (remove per-item peg → `++successCount`) and :1622 (swap to `successCount >= 3` flat `RESOLVE_FLAT_BURNIE` + `NoWork()`-at-0); add `RESOLVE_FLAT_BURNIE`; update the 5 test files atomically. STAYS out of the router (ROUTER-05/D-05). The D-05f finding is INERT-SAFE — no USER gate.

> **Placeholders (calibrated at GAS / Phase 331, NOT pinned at SPEC):** the break-even peg constants (`AUTO_GAS_PRICE_REF`/`AUTO_*_GAS_UNITS` at `DegenerusGame.sol:1539-1546`, C5), the `~1 BURNIE` `RESOLVE_FLAT_BURNIE` literal (D-05e), and the D-06 `~10M` GAS_BUDGET + per-leg `DEFAULT_AUTO_OPEN_COUNT`/`DEFAULT_AUTO_BUY_COUNT` constants (D-06g) are all SPEC PLACEHOLDERS, calibrated at the GAS phase from worst-case marginal gas (the v46 Phase 319 CR-01 "peg to the marginal, never a single total" discipline).

### Success-criteria checklist (mapped 1:1 to ROADMAP Phase 329 SC1..SC4)

1. ✅ **SC1 — the 4 structural invariants locked in writing (BATCH-01).** §2: (a) one-category structural early-return (a CODE invariant, explicit `return` after the first rewarded category, Pitfall 4); (b) frozen advance-consume (ADV-04 — router consumes via the design-1 return, NO new in-window SLOAD; TST-01 empirical handoff); (c) guaranteed free-fallback caller from EXISTING paths only (D-04 — PRIMARY router advance leg / SECONDARY 30-min bypass + Vault/sStonk wrappers / TERTIARY 120-day death-clock); (d) single day-start epoch satisfied by design-1 (GAS-03/D-03 — multiplier single-sourced + returned, the two intentionally-distinct epochs NOT merged + the WHY-they-differ rationale).
2. ✅ **SC2 — the shared signatures settled.** §1 R1-R4: R1 the `advanceGame` `(uint8 mult, bool rewardable)` return + the `:275` wrapper decode; R2 `doWork(maxCount)` + the ROUTER-06 `NoWork()` revert + per-leg maxCount + the LOCKED D-06 fixed gas-budget-sized default count (fixed-count not gasleft, EmptyAutoBuy reconciled away, OOG-clean-revert-+-manual-retry, faucet-neutral, ~10M/DEFAULT_*_COUNT GAS-331 placeholders, refines ROUTER-01/GAS-01/TST-02); R3 the 3 O(1) discovery views (advanceDue covering new-day AND mid-day, boxesPending, buys-pending AfKing-local) + locations; R4 one-creditFlip router-pays-only-advance + no-double-pay + KEEP-04 affiliate survival. Each R-row names producer + consumer files + apply-order.
3. ✅ **SC3 — the two flagged dispositions resolved.** §2: ROUTER-07 = NO guard (D-01) on the grep-checked per-leg no-untrusted-ETH-send basis (D-01a, formal basis *keeper-never-a-payee + no untrusted ETH send + one-category early-return + creditFlip-last CEI*) + the D-01b TST-02 empirical double-pay backstop scoped to PROVE it; GAS-03 single-epoch = design-1-satisfies (cross-referenced to invariant d, D-03/D-03a).
4. ✅ **SC4 — every cited file:line verdict folded; no un-grepped "by construction" survives + the producer-before-consumer edit-order map produced.** §0: 34 router/advance anchors + the full D-05 surface attested vs `0cc5d10f`, 0 blockers; corrections C1-C6 carried (the 4 Plan-01 line-drifts + the C6 interface-ABSENT Rule-1 correction); the `DegenerusGame` inline-duplication precedent re-checked (C1 — the affiliate site is the game self-call wrapper :1781, not AfKing). §3: the producer-before-consumer edit-order map (AdvanceModule → Game wrapper/views → interfaces → AfKing router/_autoBuy/re-peg/micro-opts) so no intermediate ships where advancing is unrewarded.
5. ✅ **D-05 design item LOCKED + the D-05f finding carried (not softened).** §2 D-05a-g: the rename (NO interface edit, C6) + the flat ~1-BURNIE/≥3-NON-WWXRP-gate/revert-on-no-work re-peg + the D-05c REAL-gas net-loss basis + the D-05f INERT-SAFE losing-bet-liveness finding (SURFACE-TO-USER: NONE — recorded, not softened, not invented) + the ROUTER-05 router-fold-OUT, as a v49.0 design item (code at 330/BATCH-02, REQ GAS-06/TST-05 at 331/332).

**SOURCE-TREE not mutated at SPEC.** Phase 330 applies the single batched diff (HELD at the contract-commit boundary for explicit user hand-review). `git diff --name-only 0cc5d10f HEAD -- 'contracts/*.sol'` is EMPTY.
