# Phase 329 SPEC — v49.0 Keeper-Router REDESIGN Design-Lock + Call-Graph Attestation + 4 Structural Invariants

**Milestone:** v49.0 (Unified Keeper Router — keeper-router REDESIGN: `autoBuy → advance → autoOpen`, dropped rngLock guards, dropped autoOpen try/catch + entry-gate, unified single `creditFlip` in `doWork`, the D-07 flat-per-tx bounty model — re-SPECced after the 330-07 hand-review pivot).
**Baseline HEAD attested against:** `0cc5d10f` (`MILESTONE_V48_AT_HEAD_0cc5d10fbc1232a6d2e7b0464fe21541b9812029`, the v48.0-closure HEAD / frozen audit subject). The COMMITTED HEAD tree is byte-identical to the baseline (`git diff --name-only 0cc5d10f HEAD -- 'contracts/*.sol'` EMPTY); the working tree carries the SUPERSEDED held-330 diff (dirty, NOT this phase's surface — every grep is run against the frozen blob via `git show 0cc5d10f:`, NOT the held tree).
**Inputs:** `330-ROUTER-REDESIGN-INTENT.md` (the 5 locked changes RD-1..RD-5 + Q1/Q2/Q5 resolutions + the autoBuy-gas analysis feeding the flat-per-tx ratios + the survivors-vs-reworked split) + the milestone scope (`.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`) + `.planning/research/{SUMMARY,ARCHITECTURE,PITFALLS}.md` + the 2 REGENERATED attestation docs in this dir (`329-ATTEST-ROUTER-ADVANCE.md` [Plan 01], `329-ATTEST-DEGENERETTE-RESOLVE.md` [Plan 02]) + the locked decisions in `329-CONTEXT.md` (RD-1..RD-5, D-01/D-01a/D-01b, D-03, D-04/D-04a, D-05, D-06-superseded, D-07, D-08).
**Owns:** BATCH-01 (the 4 structural invariants + the shared-signature reconciliation + the grep-attestation roll-up) + ROUTER-07 (the no-guard disposition) + ADV-04 (the frozen advance-consume invariant) + GAS-03 (the single-epoch disposition, now satisfied-by-deletion).

> **RE-SPEC.** The original 329 SPEC + Phase 330 IMPL executed and verified, then the held 330 batched diff was SUPERSEDED at the 330-07 hand-review when the user pivoted to a keeper-router REDESIGN. This SPEC is RE-ISSUED from the regenerated Wave-1 ATTEST docs. It REVERSES / DISSOLVES the superseded design (`doWork(maxCount)`, the dual-stall-epoch GAS-03 collapse, the advance→open→buy order, the per-leg bounty) and LOCKS the redesign. The held-330 diff sits uncommitted on the working tree as the re-IMPL base — **SOURCE-TREE not mutated at SPEC (the held-330 diff is untouched here).**

> **The redesign reworks the bounty implementation but KEEPS the survivors.** The held diff shipped the OLD per-item + two-stall-epoch + `doWork(maxCount)` + autoOpen gas-units model — all superseded by D-07. Phase 330 re-IMPL keeps the survivors (advance bounty re-home + `(mult, rewardable)` tuple; `degeneretteResolve` rename + ≥3 re-peg; GASOPT-01 MintModule `[rk]` hoists; the interface tuple + `advanceDue()`/`boxesPending()` views) and reworks the now-superseded bounty portions on top (§3).

---

## 0. Attestation verdict roll-up (BATCH-01, Wave-1 — folds both `329-ATTEST-*.md`)

**0 IMPL blockers across BOTH surfaces.** Every cited `file:line` across the two regenerated Wave-1 ATTEST docs is grep-attested against the frozen v48.0-closure HEAD `0cc5d10f` (via `git show 0cc5d10f:`, NOT the dirty held-330 tree). **Router+advance surface (`329-ATTEST-ROUTER-ADVANCE.md`): 0 blockers** (Section A 14 anchors 12 MATCH / 2 immaterial SHIFTED; B/C/D/E/F/G all 0 ABSENT). **Degenerette-resolve surface (`329-ATTEST-DEGENERETTE-RESOLVE.md`): 0 blockers** (rename surface enumerated; the interface-file rename rows ABSENT/no-op — SHRINKS the surface; the D-05f losing-bet liveness re-confirmed INERT-SAFE). No "by construction" / "single fn reaches all paths" claim survives un-grepped (the `DegenerusGame` mint/jackpot inline-duplication precedent: the `degeneretteBets` consumer set was re-enumerated across ALL modules, and the `batchPurchase` dependent grep was PERFORMED — Q5). **Re-grep at edit time — the Phase 330 re-IMPL batched diff shifts every line below.**

**Aggregate anchor tally:**

| Doc | Surface | Anchors | MATCH | SHIFTED | ABSENT | Blockers |
|-----|---------|---------|-------|---------|--------|----------|
| 329-ATTEST-ROUTER-ADVANCE | RD-1..5 + D-07 + ROUTER-07/D-01a + ADV-04 + invariant-(c)/D-04a + D-08 baselines | 45+ (A 14 · B 3+Q5+5 · C 7 · D 4+6 · E 9+2 · F 4+3 · G 4) | all but 4 | A13 `-1`, A14 wiring `+3`, G4 death-clock cited-line drift, (immaterial) | 0 | 0 |
| 329-ATTEST-DEGENERETTE-RESOLVE | D-05a rename + D-05b shape + D-05c real-gas + D-05f liveness + ROUTER-05 non-foldability | rename (2 defs + 1 self-call) + interface ABSENT + 5 test files/57 refs + 8 `degeneretteBets` consumers + 10 modules grep-clean | (qualitative) | — | A.2 interface rows ABSENT (no-op, shrinks surface) | 0 | 0 |
| **Total** | **both BATCH-01 halves** | — | — | — | — | **0** |

### Carried line-drift corrections (held-tree-vs-`0cc5d10f`; override the redesign-doc/CONTEXT prose at IMPL — mirror v48's C1..Cn style)

The held-330 working tree shifted lines off the frozen baseline. The redesign docs / CONTEXT cite a mix of held-tree and baseline numbers. For every anchor, the `0cc5d10f` line is the TRUTH (the held lines are drift). Recorded so Phase 330 re-IMPL re-greps and does not chase a stale number:

- **C1. AfKing `AutoBought` event decl** held `:186` → `0cc5d10f` **`:171`** (`event AutoBought(address indexed player, uint32 day, uint256 cost);`). [GASOPT-04]
- **C2. AfKing `AutoBought` emit** held `:954` → `0cc5d10f` **`:785`** (`emit AutoBought(player, today, msgValue);`, after the `:784` day-stamp). [GASOPT-04]
- **C3. AfKing subscribe-time `isOperatorApproved(fundingSource, subscriber)` — KEEP** redesign-doc `:443` → `0cc5d10f` **`:401`** (inside the `fundingSource != address(0) && …` alt-account funding-consent gate → `:403 revert NotApproved()`). [GASOPT-05 KEEPS this]
- **C4. AfKing autoBuy-entry rngLock guard — RD-2 REMOVE** held `:717` → `0cc5d10f` **`:568`** (`if (rngLocked()) revert AutoBuyAborted(msg.sender, 1);`, reason-1 = RngLocked). [RD-2]
- **C5. AfKing per-iteration `isOperatorApproved(player, address(this))` — GASOPT-05 REMOVE** held `:838` → `0cc5d10f` **`:676`** (skip-emit `PlayerSkipped(player, 5)` + `++cursor`). [GASOPT-05]
- **C6. AfKing two in-loop `claimableWinningsOf(player)` STATICCALLs — GASOPT-03 batched-read** held `:854`/`:888` → `0cc5d10f` **`:691`/`:722`**. [GASOPT-03; SUBSUMES GASOPT-02]
- **C7. AfKing autoBuy stall ladder + ABSOLUTE-day epoch — D-07 DEAD** held `:823-846`/`:992-993` → `0cc5d10f` **`:823-838`** (ladder) / **`:829`** (epoch `today * 1 days + 82_620`; bands 2h→6× / 1h→4× / 20m→2×). [D-07/D-03 delete]
- **C8. AfKing per-tx bounty + in-callee `creditFlip` — RD-4 PULL OUT** math `:845`, `creditFlip` `0cc5d10f` **`:846`** (the buy-leg in-callee credit pulled into `doWork`). [RD-4]
- **C9. Game-side `batchPurchase` rngLock pre-check — RD-2 REMOVE** held `:1693` → `0cc5d10f` **`:1737`** (`if (rngLockedFlag) revert RngLocked();`); the gameOver pre-check held `:1694` → `0cc5d10f` **`:1738`** (`if (gameOver) revert E();` — **KEEP**). [RD-2]
- **C10. Game-side `autoOpen` try/catch — RD-5 DROP** held `:1701/1707/1719` → `0cc5d10f` **`:1664` (`try this._autoOpenBox`) / `:1672` (`catch {}`) / `:1676` (in-callee `creditFlip` — RD-4 pull-out U4)**; `++cursor` BEFORE the try at `:1656-1660`; `_autoOpenBox` external onlySelf at `:1705`. [RD-5/RD-4]
- **C11. `bytes32("DGNRS")` KEEP-04 affiliate wiring** redesign-doc `DegenerusGame.sol:1778` → `0cc5d10f` **`:1781`** (`_purchaseFor(player, 0, msg.value, bytes32("DGNRS"), payKind);` inside `_batchPurchaseUnit` `:1773`, onlySelf `:1777`, reached from `batchPurchase`'s per-player `this._batchPurchaseUnit{value:slice}` `:1749`). The v48 KEEP-04 wiring ALREADY landed at baseline. [ROUTER-05 pre-condition — survives the `_autoBuy` refactor]
- **C12. RD-5 entry-gate liveness-control basis** CONTEXT/redesign wrote the bare "`DegenerusGameStorage:571`"; the actual path is **`contracts/storage/DegenerusGameStorage.sol:571`** (`if (_livenessTriggered()) revert E();` inside `_queueTickets` `:560`; `_livenessTriggered()` `:1213`). `:571` is CORRECT; the path prefix is `storage/`.
- **C13. invariant-(c) death-clock anchors** CONTEXT/redesign cited "extend ~`:1233/:1296`" → the actual death-clock constant is **`AdvanceModule:109`** (`DEPLOY_IDLE_TIMEOUT_DAYS = 365`; level-1+ uses hardcoded 120 days) and the extend-on-stall is **`:1199-1200`** (`purchaseStartDay += gapCount;`) + the 120-day daysRemaining **`:1898`**. The mechanism is present and intact; the cited `:1233/:1296` are within the gameover-RNG-fallback region. [invariant (c)]
- **C14. `_currentDay()`** claim `:887` → `0cc5d10f` **`:886`** (immaterial `-1`). **`advanceGame` module entry** `:155`; **`DegenerusGame.advanceGame` wrapper** `:275` (delegatecall, currently discards `data` on success at `:283` — the design-1 decode site). The 3 advance `creditFlip` sites are **`AdvanceModule:189/225/468`** (MATCH). The KEPT advance stall + GAME-day epoch block is **`AdvanceModule:241-253`** (epoch `:243-246`).
- **C15. degeneretteResolve rename targets** external `autoResolve(address[] players, uint64[] betIds)` **`DegenerusGame.sol:1587`**; `onlySelf _autoResolveBet` **`:1684`**; the self-call site `try this._autoResolveBet(...)` **`:1606`**. **Interface rows: ABSENT** — `autoResolve`/`_autoResolveBet` do NOT appear in any file under `contracts/interfaces/` (defined directly on `DegenerusGame.sol`, called as the concrete `game.autoResolve(...)`). The rename test surface is **5 files / 57 refs** (baseline-verified, NOT a held-tree artifact); `CrankLeversAndPacking.t.sol` asserts the LITERAL source string `"function autoResolve("` (`:277/:278/:279/:290`) → must update atomically.

### Locked-change / discretion verdicts (stated explicitly, per the plan)

- **Q5 (dependent `batchPurchase` grep): NO OTHER DEPENDENT.** `batchPurchase` is defined ONCE (`DegenerusGame:1731`), gated AF_KING-only (`:1736 if (msg.sender != ContractAddresses.AF_KING) revert E();`), sole external caller `AfKing.sol:821`; NOT declared in either interface (no other contract holds a typed handle). Removing the `:1737` game-side rngLock pre-check (RD-2) affects ONLY the keeper path; normal-player mint flows through a DIFFERENT entrypoint (`_purchaseFor` via the public mint path, NOT `batchPurchase`) and is unaffected. RD-2 is safe on this surface.
- **ROUTER-07 (D-01a): NO-GUARD BASIS HOLDS — RE-GROUNDED on the unified single `creditFlip`.** Under RD-4 there is exactly ONE `creditFlip` in `doWork`, CEI-last, fed by legs that return raw counts/mult and never self-credit (STRONGER than the old per-leg-bounty case). Per-leg + single-`creditFlip` grep: every ETH send is to a pinned `ContractAddresses.*` (GAME/COINFLIP), player value flows through the `claimableWinnings` pull ledger, the bounty is minted FLIP CREDIT to `msg.sender` (a ledger entry on the pinned COINFLIP, never an ETH push the keeper-contract receives). Keeper-never-a-payee (AfKing CEI invariant `:100-101`). **0 untrusted-push legs → 0 ROUTER-07 blocker; NO `nonReentrant` guard on `doWork`.**
- **GAS-03 / D-03: SATISFIED BY DELETION (D-03 dissolved by D-07).** Dropping the autoBuy stall multiplier (D-07: only advance escalates) deletes AfKing's autoBuy stall ladder + its absolute-day epoch (`:823-838`/`:829`). **Advance (`AdvanceModule:241-253` GAME-day epoch) becomes the SOLE stall epoch.** There are no longer two epochs to "collapse / single-source" → the original D-03 (keep both, single-source via design-1) is MOOT. No dual-epoch reconciliation needed.
- **ADV-04 (invariant b): NO NEW IN-WINDOW READ.** `totalFlipReversals` is frozen request→consume (two in-window reads `:270` + `:1838` into the consumed word, reset `:1844`); under RD-1 (autoBuy → advance → autoOpen) autoBuy runs at day-open BEFORE advance requests the day's word (rngLock false at day-open) → autoBuy is structurally PRE-ENTROPY and cannot mutate any value the advance-consume reads inside the window; the advance leg consumes via the design-1 return (recomputes nothing new); autoOpen runs AFTER and is rngLock-blocked (RD-3). The REDESIGNED router adds NO new mutable in-window SLOAD. (Empirical freeze proof = TST-01.)
- **RD-5 entry-gate: REPLICATES BOTH REVERT SOURCES.** The open path has EXACTLY TWO revert sources — rngLock + the deliberate terminal-jackpot liveness control (`storage/DegenerusGameStorage.sol:571 _livenessTriggered`). The `if (rngLocked() || _livenessTriggered()) return;` entry-gate excludes both PRE-LOOP → brick-proof (neither flips mid-tx; no half-walk marooning since the cursor only advances inside a guaranteed-non-reverting body); the terminal-jackpot guard stays intact for DIRECT `openLootBox` callers. The third `_queueTickets` revert (`:573 isFarFuture && rngLockedFlag`) is itself an rngLock revert and does not fire on the current-level open path. USER-accepted frozen-contract trade (the entry-gate trusts rngLock + liveness are the ONLY revert sources forever; the trace is high-confidence). **The entry-gate is MANDATORY if the try/catch goes.**
- **Invariant (c) / D-04a: FALLBACK CALLERS INTACT.** The 30-min permissionless bypass (`AdvanceModule:1012`), `DegenerusVault.gameAdvance()` (`:527`), `StakedDegenerusStonk.gameAdvance()` (`:421`), and the ~120-day death-clock (`:109`/`:1199-1200`/`:1898`) all present. Under autoBuy-first the rewarded advance leg is BLOCKED while buys pend → first-30-min advance during a buy backlog relies on these bypass tiers, NOT the router bounty. Re-homing the bounty into `doWork` removes NO structural caller.
- **RD-4 unification: 6 `creditFlip` sites → 1 in `doWork`.** 5 pull-out (U1 `AdvanceModule:189` + U2 `:225` + U3 `:468` advance; U4 `DegenerusGame.autoOpen:1676`; U5 `AfKing:846` buy), 1 STAYS (U6 `AdvanceModule:876` — the gameover-RNG SDGNRS credit; payee is `ContractAddresses.SDGNRS`, NOT the keeper, so it is NOT a doWork bounty). Legs return raw counts/mult and never self-credit.
- **design-1 return: `(uint8 mult, bool rewardable)`** — decode at the `DegenerusGame.advanceGame` wrapper (`:275`/`:283`, currently discards `data`); `mult` = the stall ladder (NEW-DAY path U3, `1/2/4/6`), `1` for the mid-day partial-drain (U2) and the gameover partial-drain (U1) per D-07 (NO mid-day escalation, confirmed by the absence of a `bountyMultiplier` term at `:191`/`:227`); `rewardable` = the distinct gameover-only-false bool (KEEP — harmless, worthless coin by design).
- **KEEP-04 affiliate (ROUTER-05): SURVIVES.** `bytes32("DGNRS")` at `DegenerusGame:1781` (game-side `_batchPurchaseUnit`), independent of AfKing's `autoBuy → _autoBuy` refactor; the keeper call site `:821` carries no affiliate arg; two-tier 75/20/5 (primary SDGNRS / secondary VAULT) preserved.
- **D-05f LOSING-BET LIVENESS: INERT-SAFE (re-confirmed; the clean negative).** All 8 `degeneretteBets` consumers (storage decl `Storage:1449` + submission `DegeneretteModule:526` + resolution read `:605-606` + delete `:634` + AUTO-02 probe `DegenerusGame:1596` + per-item read `:1601` + view `:2319`) treat an unresolved (losing) bet as INERT storage cruft — never iterated, never counted, never reconciled, never required-empty. The `delete` `:634` exists ONLY for LOCAL double-resolve idempotency. GameOver / Jackpot / Advance — and every other module (8 modules) — are grep-CLEAN of `degeneretteBets` (0 hits each); no outstanding-bet counter / per-day tally anywhere (`pendingDegenerette|outstandingBet|degeneretteCount|pendingBet|betCount|openBets` → 0 matches). No path REQUIRES losing Degenerette bets resolved. A flat count-independent reward mildly IMPROVES backlog liveness (max work per paid tx). **SURFACE-TO-USER: NONE.** No losing-bet-liveness dependency exists at `0cc5d10f` — this is a clean negative; nothing to carry as a USER gate. (Had any consumer required the `delete` to fire — a counter decremented only on delete, a gameOver require-empty, an RNG slot freed only on resolution — §0 would instead carry a `SURFACE-TO-USER — LOSING-BET LIVENESS DEPENDENCY` block VERBATIM, not softened. It does not, because none exists.)
- **GASOPT-01 hoist sites: MATCH.** `processFutureTicketBatch` (`:393`/`rk:398`) + `processTicketBatch` (`:670`/`rk:671`) — `[rk]` loop-invariant, behavior-identical gas-only.
- **GASOPT-03/04/05 baselines: ATTESTED.** GASOPT-03 batched read (`claimableWinningsOf :691/:722` → `keeperSnapshot`, SUBSUMES GASOPT-02); GASOPT-04 drop `AutoBought` (`:171`/`:785` → `lastAutoBoughtDay` `:81`/`:784`; 4-AfKing-file test-oracle migration + the no-double-buy `_countAutoBoughtFor==1` hardest case; the rename∩AutoBought collision set = `CrankNonBrick.t.sol` + `CrankLeversAndPacking.t.sol`); GASOPT-05 drop per-iteration `isOperatorApproved` (`:676`), KEEP subscribe-time (`:401`), the 333-SWEEP-re-attests-the-4-OPEN-E-protections BLOCKING-CONDITION.

---

## 1. Shared Signatures (BATCH-01 reconciliation — the REDESIGNED surface)

> The cross-file joint edits of the keeper-router redesign. One settled signature + a decode/consume note + the producing file → consuming file(s) + an explicit **apply-order within the single re-IMPL diff** per shared construct, so no intermediate state ships where advancing is unrewarded (T-329-R1). Mirrors v48's R-rows.

### R1 — `advanceGame` return: `(uint8 mult, bool rewardable)` (design-1; ADV-02, ADV-05, D-07)

**Final signature:** the module fn `DegenerusGameAdvanceModule.advanceGame()` (`:155`) gains `returns (uint8 mult, bool rewardable)`; the `DegenerusGame.advanceGame()` wrapper (`:275`) gains the same `returns` clause and DECODES the delegatecall return `data` (currently discarded on success at `:283`) instead of throwing it away, forwarding the tuple to the `IDegenerusGameAdvanceModule.advanceGame` interface row.
- `mult` = the stall ladder (NEW-DAY path only, `1/2/4/6` from the KEPT GAME-day epoch `AdvanceModule:241-253`); **mid-day partial-drain returns `mult = 1`** (NO escalation, D-07/ADV-05 — confirmed by the absence of a `bountyMultiplier` term at the mid-day `:227` and the gameover partial-drain `:191`); the gameover path `mult = 1`.
- `rewardable` = a DISTINCT bool (gameover-only-false path; KEEP per the redesign — `doWork` reads it to know advance did rewardable work).
- **How AdvanceModule produces it:** delete the 3 in-callee advance `creditFlip` sites (`:189`/`:225`/`:468`, ADV-01) and instead RETURN the `(mult, rewardable)` basis; the ticket-drain / day-advance logic is untouched (only the bounty is removed — standalone `advanceGame()` stays functional + UNREWARDED, ADV-03).
- **Producer → consumer:** `AdvanceModule` (produce the tuple, delete the 3 creditFlips) → `DegenerusGame` wrapper `:275` (decode `data` at `:283`) → `IDegenerusGameAdvanceModule` (the tuple sig) → `AfKing.doWork` (consume the tuple to compute the unified bounty). **Apply-order:** FIRST (AdvanceModule is the root producer).

### R2 — PARAMETERLESS `doWork()` + the ROUTER-06 `NoWork()` revert + the standalone UNREWARDED escapes (D-07 supersedes D-06; RD-1; RD-4)

**Final signature:** `function doWork() external` on `AfKing.sol` — **NO `maxCount` argument** (D-07 supersedes D-06; the `maxCount == 0` sentinel no longer exists). `doWork` determines its OWN fixed per-leg default batch internally.
- **Priority routing (RD-1):** `autoBuy → advance → autoOpen`. autoBuy runs first so subscriber boxes/tickets queue at day-open BEFORE advance requests the day's RNG → same-cycle reveal + same-day quest credit; one-category structural early-return after the first category that has work (invariant a); the single unified `creditFlip` (R4) fires LAST after the early-return point.
- **ROUTER-06 `NoWork()` revert:** fires only when ALL 3 O(1) discovery predicates (R3) are empty — the clean "no work done" signal (the AfKing `EmptyAutoBuy` / `NoSubscribersAutoBought` idiom precedent); never pays a bounty for no work.
- **Standalone parametered + UNREWARDED escapes (RD-4):** `autoOpen(uint256 count)` / `autoBuy(uint256 count)` stay as manual/emergency clears — parametered, and UNREWARDED (direct, non-router calls do NOT credit a bounty; only `doWork` credits). (`degeneretteResolve(players[], betIds[])` likewise stays a separate parametered call with its own flat ≥3 reward — D-05, §2.)
- **Producer → consumer:** `AfKing` is the home (produces `doWork()` + the unrewarded escapes + `NoWork()`) → consumes `IDegenerusGame` advance tuple (R1) + `autoOpen`/`autoBuy` return shapes (R4) + the rngLock-aware views (R3) + the GASOPT-03 `keeperSnapshot`. **Apply-order:** AfKing is the LAST producer (consumes everything above) — applied AFTER AdvanceModule → Game → interfaces.

### R3 — the rngLock-aware O(1) discovery views (ROUTER-04; RD-2/RD-3)

Each view is O(1) (no unbounded scan, Pitfall 5); each located by where it lives:
- **`advanceDue()` (on DegenerusGame):** `currentDayView() :462 (= _simulatedDayIndex() :463) != dailyIdx` (new-day) **OR** a mid-day partial-drain pending (`LR_MID_DAY != 0` / unprocessed read slot). True regardless of rngLock (advance is liveness-critical).
- **`boxesPending()` (on DegenerusGame, RD-3 rngLock-aware):** `boxPlayers[activeIndex].length > boxCursor AND lootboxRngWordByIndex[activeIndex] != 0 AND !rngLocked()`. **FALSE during rngLock** (the open leg no-ops during the freeze) but TRUE for mid-day-resolved rounds whose word has landed and we are not currently locked (the user wants those opened). O(1) reads: `boxPlayers[index]` `:1562`, `boxCursor` `:1551`, `boxCursorIndex` `:1554`, `lootboxRngWordByIndex[index]` `:1647`.
- **buys-pending (on AfKing-local):** AfKing-local cursor reads (`autoBuyProgress() :527` day/cursor) — active subscribers remain ahead of the keeper cursor. **TRUE even during rngLock** (RD-2: autoBuy no longer aborts on rngLock; buys queue pre-entropy).
- **Producer → consumer:** `DegenerusGame` produces `advanceDue()`/`boxesPending()` (rngLock-aware) → `IDegenerusGame` declares them → `AfKing.doWork` routes on all three. **Apply-order:** the Game views + the `boxesPending` rngLock-awareness land in the DegenerusGame edit (SECOND), the interface rows THIRD, consumed by AfKing FOURTH.

### R4 — the UNIFIED bounty composition: ONE `creditFlip` in `doWork` (RD-4)

**Final shape:** the router pays exactly ONE `ICoinflip(ContractAddresses.COINFLIP).creditFlip(msg.sender, bountyEarned)` — in `doWork`, CEI-LAST, after the one-category early-return — fed by legs that return their raw reward basis: `advanceGame` returns `(uint8 mult, bool rewardable)` (R1); `autoOpen`/`_autoBuy` return raw counts. **Legs NEVER self-credit; direct (non-router) calls are unrewarded.**
- **The 5 pull-out sites (RD-4):** the 3 advance `creditFlip`s (`AdvanceModule:189`/`:225`/`:468`) + the autoOpen in-callee `creditFlip` (`DegenerusGame:1676`) + the autoBuy in-callee `creditFlip` (`AfKing:846`) ALL move OUT into `doWork`. The 6th `creditFlip` (`AdvanceModule:876`, the gameover-RNG SDGNRS credit) STAYS — its payee is `ContractAddresses.SDGNRS`, not the keeper, so it is NOT a doWork bounty.
- **KEEP-04 affiliate survival (ROUTER-05 pre-condition):** `bytes32("DGNRS")` is wired GAME-side at `DegenerusGame.sol:1781` (in `_batchPurchaseUnit`), independent of AfKing's `autoBuy → _autoBuy` refactor; the keeper buy call `AfKing:821` carries no affiliate arg and is untouched by RD-4 except for the bounty pull-out. Two-tier 75/20/5 (primary SDGNRS / secondary VAULT) preserved.
- **Producer → consumer:** `AfKing.doWork` is the sole crediting site; `AdvanceModule`/`DegenerusGame.autoOpen`/`AfKing._autoBuy` are the producers that DELETE their in-callee credits and return the basis instead. **Apply-order:** the pull-out deletes happen in each producer's edit; the single `creditFlip` is authored in the AfKing `doWork` edit (LAST).

### R5 — the D-07 FLAT-PER-TX bounty model (GAS-331 placeholders)

**Final model:** flat per-tx, per-category multiples of a unit `1×` (where `1×` ≈ the BURNIE flip-credit worth of a max-laden tx's gas at 0.5 gwei):

| Leg | Reward | Gate / scaling |
|-----|--------|----------------|
| **advance** | `2× × mult` | none — `mult` = the stall ladder (`1/2/4/6`) **NEW-DAY path only**; mid-day partial-drain `mult = 1` (NO escalation, D-07/ADV-05). Real ticket-queue batches (can't be fabricated), liveness-critical, multi-call drain. |
| **buy** | `1.5×` | none — bounded once/day/sub + the 2 standing protocol auto-subs; a farmer must fund real subscription buys ≫ the bounty. "No real faucet ability there." |
| **open** | `1× × min(opened, KNEE)/KNEE` | pro-rate below the knee (per-box up to the knee, flat `1×` at/above; `KNEE ≈ 5`). Mid-day round resolutions give frequent, small open opportunities (RD-3) → the pro-rate kills the small-batch corner. |

- **One base unit, one conversion** (`× PRICE_COIN_UNIT / mp`), one `creditFlip` in `doWork` (R4), one-category early-return preserved (invariant a). The autoBuy + advance legs ALREADY share the base+conversion (`BOUNTY_ETH_TARGET * PRICE_COIN_UNIT / mp`); D-07 unifies the open leg onto it (deleting the open-leg gas-units conversion `AUTO_OPEN_BOX_GAS_UNITS`/`_ethToBurnieValue`) and flattens to per-tx.
- **GAS-331 PLACEHOLDERS (NOT locked):** the `1×` unit, the `1 / 1.5 / 2` ratios (relative per-category max-laden gas at 0.5 gwei), and the open `KNEE (~5)` are SPEC starting estimates calibrated under the USER-gated GAS phase (the v46 Phase-319 CR-01 precedent — peg to the worst-case MARGINAL, never a single-item total; real prevailing gas, not the 0.5-gwei ref).
- **Faucet-safety constraint GAS-331 must satisfy + WR-01/GAS-05 must re-prove:** the implied per-box reward below the open knee (`1× / KNEE`) ≤ a one-box tx's gas at 0.5 gwei → even a tiny mid-day open is −EV at any real gas. The advance/buy legs have NO gate (advance = real un-fakeable ticket batches, liveness-critical, multi-call drain gated by `advanceDue()`; buy = bounded once/day/sub + real-subscription-cost) — neither needs the open leg's pro-rate (the faucet doctrine for advance/buy = no gate).
- **Producer → consumer:** the flat formula + the conversion live in `AfKing.doWork` (the single `creditFlip` site, R4); the `mult` it scales the advance leg by comes from R1's tuple. **Apply-order:** authored in the AfKing `doWork` edit (LAST), consuming R1's `mult`. The peg numbers are placeholders re-derived at GAS (Phase 331).

---

## 2. Structural invariants + dispositions + RD changes + D-08 + the D-05 design-lock

> Each subsection records the **lock + its source (D-NN / RD-N or the ATTEST grep verdict) + the IMPL instruction it produces.** The 4 invariants are CODE invariants (not comments). They map 1:1 to the UPDATED ROADMAP Phase 329 SC1.

### Invariant (a) — ONE-CATEGORY STRUCTURAL EARLY-RETURN (RD-1; BATCH-01)

- **Lock:** `doWork()` routes `autoBuy → advance → autoOpen` (RD-1) and RETURNS after the FIRST category that has work (a CODE invariant, not a comment); this preserves the one-`creditFlip`-per-tx faucet bound (Pitfall 4 bounty-stacking — advance/open/buy bounties can never stack in one tx).
- **Source:** RD-1 (`330-ROUTER-REDESIGN-INTENT.md` #1) + the ATTEST B.3 single-`creditFlip` verdict.
- **IMPL instruction:** explicit early-return after the chosen category; the single unified `creditFlip` (R4) fires AFTER the return point (CEI-last), never inside a leg.

### Invariant (b) — FROZEN ADVANCE-CONSUME (ADV-04)

- **Lock:** the player-controllable `totalFlipReversals` nudge (`AdvanceModule:1838`/`:1844`, plus the second in-window read `:270`) is frozen request→consume; under RD-1 `autoBuy` runs PRE-ENTROPY at day-open (rngLock false) BEFORE the advance leg requests the day's word; the advance leg consumes via the design-1 RETURN (R1) and adds NO new mutable in-window SLOAD; `autoOpen` runs AFTER and is rngLock-blocked (RD-3) so it never executes during the freeze. The protected window = rng-request → consume; the reset `:1844` closes it.
- **Source:** ADV-04 + the ATTEST §F verdict (both in-window reads `:270`/`:1838` enumerated per `feedback_rng_window_storage_read_freshness`; the autoBuy-pre-entropy ordering; `v45-vrf-freeze-invariant`; Pitfall 3).
- **IMPL instruction:** the RD-1 ordering IS the invariant — autoBuy must run before advance requests the word; no leg may introduce a fresh SLOAD into the advance-consume. **TST-01 handoff (empirical):** autoBuy-during-rngLock safe + autoOpen-blocked-during-rngLock + perturb `totalFlipReversals` (and every in-window SLOAD) between rng-request and unlock, assert byte-identical consumed VRF output even when fired same-tx as autoBuy/autoOpen.

### Invariant (c) — GUARANTEED FREE-FALLBACK CALLER (D-04/D-04a)

- **Lock:** EXISTING structural paths ONLY (no new mechanism, `feedback_frozen_contracts_no_future_proofing`): **PRIMARY** the rewarded router advance leg (BLOCKED while subscriber buys pend under autoBuy-first); **SECONDARY** the 30-min universal permissionless bypass (`AdvanceModule:1012`) + `DegenerusVault.gameAdvance()` (`:527`) + `StakedDegenerusStonk.gameAdvance()` (`:421`); **TERTIARY** the ~120-day death-clock (`:109`/`:1199-1200`/`:1898`). **D-04a amendment:** under autoBuy-highest-priority the rewarded advance leg waits while buys pend; buys drain monotonically (finite subscriber set, the AfKing cursor advances each call) so advance runs once buys clear; first-30-min advance during a buy backlog relies on SECONDARY, NOT the bounty — a bounded delay, ACCEPTED (death-clock tolerates it; daily mechanics tolerate ≤30-min). NO advance-preemption carve-out (it would defeat RD-1's same-cycle-reveal rationale).
- **Source:** D-04/D-04a + the ATTEST §G verdict (G1-G4 fallback callers intact; the D-04a re-attestation under the new order).
- **IMPL instruction:** re-homing the bounty into `doWork` removes NO structural caller — it only moves WHERE the payment is made; do NOT add a new fallback mechanism. Confirm the 3 SECONDARY/TERTIARY callers are untouched by the diff (read-only confirm Vault/sStonk `gameAdvance()`).

### Invariant (d) — SINGLE DAY-START EPOCH: SATISFIED BY DELETION (GAS-03 / D-03 dissolved by D-07)

- **Lock:** dropping the autoBuy stall multiplier (D-07: only advance escalates) DELETES AfKing's autoBuy stall ladder (`:823-838`) + its absolute-day epoch (`:829` `today * 1 days + 82_620`); advance (`AdvanceModule:241-253` GAME-day epoch) becomes the SOLE stall epoch → there are NO two epochs to collapse (D-03 dissolved; the old "keep both, single-source via design-1" is MOOT).
- **Source:** GAS-03 / D-03 dissolved by D-07 + the ATTEST C.1 GAS-03-satisfied-by-deletion verdict.
- **IMPL instruction:** delete the AfKing stall ladder (`:823-838`) + the absolute-day computation (`:829`); advance is the only escalating path. (Cross-ref the GAS-03 disposition below.)

### RD CHANGES (LOCK each)

- **RD-1 — order `autoBuy → advance → autoOpen`.** Folds into invariant (a). autoBuy queues subscriber boxes/tickets at day-open before advance requests RNG → same-cycle reveal + same-day quest credit.
- **RD-2 — `autoBuy` = normal buy; DROP the rngLock guards.** Remove BOTH the AfKing autoBuy-entry guard (`:568`) AND the game-side `batchPurchase` rngLock pre-check (`:1737`); **KEEP `gameOver` (`:1738`)**. Basis: Q1 RESOLVED SAFE — the guard was v46 batch-hygiene (Phase 317 `df4ef365`), NOT orphan-index defense; buying is freeze-safe by construction (box queues at current `LR_INDEX`, word lands at `LR_INDEX-1`, `LR_INDEX` advances only in `advanceGame`); the v45 orphan hazard is defended on the RESOLUTION side. **Q5 verdict carried: NO other `batchPurchase` dependent** (AF_KING-gated `:1736`, sole external caller `AfKing.sol:821`).
- **RD-3 — `boxesPending()` rngLock-aware + BLOCK `autoOpen` during rngLock.** `boxesPending()` returns false during rngLock so the open leg no-ops during the freeze; it STILL opens mid-day-resolved rounds whose word has landed and we are not locked. O(1) (R3).
- **RD-4 — UNIFY the bounty into `doWork`.** 6 `creditFlip` sites → 1 (R4): 5 pull-out (U1/U2/U3 advance `:189/:225/:468` + U4 autoOpen `:1676` + U5 autoBuy `:846`), 1 stays (U6 `AdvanceModule:876` SDGNRS gameover credit — not a keeper bounty). Reverses the old per-leg-bounty model R4. Requires signature changes to `autoOpen`/`autoBuy` + `IDegenerusGame`.
- **RD-5 — DROP `autoOpen` try/catch + add an entry-gate.** `autoOpen(...) { if (rngLocked() || _livenessTriggered()) return; … _autoOpenBox(index, player) /* INTERNAL */ }`; make `_autoOpenBox` INTERNAL (drop the `this.`/try-catch). **Basis: EXACTLY TWO open-path revert sources** — rngLock + the deliberate terminal-jackpot liveness control (`contracts/storage/DegenerusGameStorage.sol:571` `_livenessTriggered()` inside `_queueTickets`; `_livenessTriggered()` `:1213`). The entry-gate replicates BOTH pre-loop → brick-proof; the terminal-jackpot guard stays for DIRECT `openLootBox`. The third `_queueTickets` revert (`:573`) is itself an rngLock revert (far-future only) and does not fire on the current-level open path. **USER-accepted frozen-contract trade** (trusts rngLock + liveness are the ONLY revert sources forever; high-confidence trace). **The entry-gate is MANDATORY if the try/catch goes** (without it, an atomic loop reverting at queue pos K rolls back `boxCursor`..K and freezes the tail).

### Disposition ROUTER-07 (D-01) — NO `nonReentrant` guard, re-grounded on the unified single `creditFlip`

- **Lock:** NO `nonReentrant` guard on `doWork`. **RE-GROUNDED on RD-4:** under the unified model there is exactly ONE `creditFlip` in `doWork`, CEI-LAST, fed by legs that return raw counts/mult and never self-credit — STRONGER than the old per-leg-bounty case. Formal basis (recorded verbatim): **keeper-never-a-payee + no untrusted ETH send (per-leg + single-`creditFlip` grep D-01a, carried from ATTEST §B.3) + one-category structural early-return + single-`creditFlip`-last CEI ordering** (AfKing CEI invariant `:100-101`). Every external call is to a pinned `ContractAddresses.*` (GAME/COINFLIP); player value flows through the `claimableWinnings` pull ledger; the bounty is minted FLIP CREDIT, never an ETH push the keeper-contract receives. **0 untrusted-push legs → 0 ROUTER-07 blocker.**
- **D-01b backstop:** the TST-02 `router→game→creditFlip` double-pay regression STAYS as the empirical backstop scoped to PROVE the no-guard disposition (near-trivial under the unified model — legs structurally cannot credit; only `doWork` credits once after the early-return). If 329-01 had flagged any untrusted-push leg it would be carried as a ROUTER-07 blocker; it did not.
- **Source:** D-01/D-01a/D-01b + the ATTEST §B.3 per-leg + single-`creditFlip` no-untrusted-ETH-send verdict.

### Disposition GAS-03 — satisfied-by-deletion

- **Lock:** SATISFIED BY DELETION (cross-reference invariant (d) — D-03 dissolved by D-07; advance is the sole stall epoch). No duplicate; see invariant (d) for the IMPL instruction.

### D-08 GASOPT-03/04/05 (LOCK as v49.0 design items; code at 330, REQ-IDs registered by §3/Task 3)

- **GASOPT-03 — batched keeper read.** NEW game-side `batchPurchaseForKeeper` / `keeperSnapshot` collapsing the two per-player `claimableWinningsOf` STATICCALLs (`AfKing:691`/`:722`) into ONE batched call (~2-3k/player). New function + interface surface. **SUBSUMES GASOPT-02** (the per-iteration hoist is the subset).
- **GASOPT-04 — drop the per-player `AutoBought` event** (decl `:171`, emit `:785`, ~1.5k/player) + the test-oracle migration to `lastAutoBoughtDay` (`:81`/`:784`). **NON-TRIVIAL constraint:** the no-double-buy invariant `_countAutoBoughtFor(sub)==1` (`AfKingConcurrency.t.sol`, `SweepPerPlayerWorstCaseGas.t.sol`) MUST be re-expressed in `lastAutoBoughtDay` storage + pool/balance-delta terms WITHOUT weakening SAFE-03 / H-CANCEL-SWAP; the event-removal AND the oracle migration land TOGETHER in 330 (the suite breaks the moment the event is gone). The rename∩AutoBought collision set = `CrankNonBrick.t.sol` + `CrankLeversAndPacking.t.sol`.
- **GASOPT-05 — drop the per-iteration `isOperatorApproved(player, AfKing)` check** (`:676`, ~2.8k/player); **KEEP the subscribe-time `isOperatorApproved(fundingSource, subscriber)` gate** (`:401`). The SUB is the consent unit (revoke = `setDailyQuantity(0)` → tombstone-skip; OPEN-E "consent-gate-at-subscribe + trust-the-sub"). **BLOCKING CONDITION:** the 333 SWEEP must re-attest the 4 OPEN-E protections hold WITHOUT `:676` BEFORE closure; if it fails, this removal is REVERTED before the milestone ships.

### D-05 DEGENERETTE-RESOLVE design-lock (survives the redesign VERBATIM; code at 330/BATCH-02, REQ GAS-06/TST-05 at 331/332)

- **D-05a rename** — `autoResolve → degeneretteResolve` (`DegenerusGame.sol:1587`) + `_autoResolveBet → _degeneretteResolveBet` (`:1684`) + the self-call site (`:1606`). **NO interface row** — defined on `DegenerusGame.sol`, called as the concrete `game.autoResolve(...)` (interface rows ABSENT — SHRINKS the surface). Test surface = 5 files / 57 refs incl. the `CrankLeversAndPacking.t.sol` literal source-string assertions (`:277/:278/:279/:290`) that BREAK without atomic update.
- **D-05b shape** — flat ~1 BURNIE (1e18) `creditFlip` ONCE per tx (count-independent), gated at **≥3 successfully-resolved NON-WWXRP** bets; revert `NoWork()` at zero; 1–2 resolved → resolved but UNPAID (lean = do-NOT-revert, never strand a trailing tail). FEASIBLE on the existing `:1587-1622` per-item loop (per-item-accumulate `:1611-1614` → `++successCount`; swap the post-loop `:1622` creditFlip to the `≥3` flat + `NoWork()`-at-zero).
- **D-05c real-gas basis** — the ~1 BURNIE is illiquid flip-credit worth ≤ `mintPrice/1000` ETH (≤0.00024 ETH at the 0.24-ETH peak); the keeper pays REAL prevailing gas (≥220k for the ≥3 min × 5–50+ gwei = 0.0011–0.011+ ETH = ~4.6–46× the peg) → net loss at any realistic gas; the ≥3 gate WIDENS the margin. (NOT the 0.5-gwei `AUTO_GAS_PRICE_REF` peg ref.) GAS-06 sanity-check handoff to 331.
- **D-05d preserved** — WWXRP excluded from BOTH the gate count and the reward (AUTO-04); AUTO-02 probe + per-item try/catch + self-resolve-allowed (REW-04) + one-`creditFlip`-CEI-last (REW-02) all unchanged.
- **D-05f finding** — **INERT-SAFE (re-confirmed; the clean negative — §0).** No path requires losing Degenerette bets resolved; the flat reward mildly IMPROVES backlog liveness. **SURFACE-TO-USER: NONE** (no losing-bet-liveness dependency exists at `0cc5d10f`; nothing to carry as a USER gate — not softened, simply absent).
- **router-fold OUT (ROUTER-05)** — `degeneretteBets` is a nested mapping (`Storage:1449`) with no O(1) enumeration + no pending-count sidecar → on-chain discovery is impossible-or-unbounded (ROUTER-04 violation). Stays a SEPARATE caller-supplied-arrays call; the unified one-button is a frontend concern. RD-1..RD-5 do NOT touch the path.

---

## 3. Per-item IMPL blueprint + producer-before-consumer survivors-vs-reworked edit-order map

> **Files in the diff** (degenerus-audit `contracts/` + `test/`): `modules/DegenerusGameAdvanceModule.sol` (R1 producer), `DegenerusGame.sol` (R1 wrapper decode + R3 rngLock-aware views + RD-3/RD-5 autoOpen rework + D-05 rename + GASOPT-01 MintModule pointer reference + GASOPT-03 `keeperSnapshot`), `interfaces/IDegenerusGame.sol` + `interfaces/IDegenerusGameModules.sol` (advance tuple + autoOpen/autoBuy return shapes + views + `keeperSnapshot`), `AfKing.sol` (R2 parameterless `doWork` router + `_autoBuy` refactor + RD-2 drop-guard + R4/R5 unified flat-per-tx bounty + GASOPT-04/05), `modules/DegenerusGameMintModule.sol` (GASOPT-01 `[rk]` hoists), the 5 rename test files + the 4 AfKing `AutoBought`-oracle test files (GASOPT-04 migration; rename∩AutoBought collision = `CrankNonBrick.t.sol` + `CrankLeversAndPacking.t.sol`). `ContractAddresses.sol` freely modifiable (none expected).

> **SURVIVORS (carry from the held diff, re-derive only the bounty portions on top):** the advance bounty re-home + the `(uint8 mult, bool rewardable)` tuple (AdvanceModule + the `:275` wrapper decode); the `autoResolve → degeneretteResolve` rename + the flat ≥3-gate re-peg; the GASOPT-01 MintModule `[rk]` hoists; the interface tuple + `advanceDue()`/`boxesPending()` views.
>
> **REWORKED (re-derive on top of the survivors — superseded by D-07/RD-1..RD-5):** AfKing `doWork` (PARAMETERLESS + order `autoBuy→advance→autoOpen` + unified single `creditFlip` + the D-07 flat-per-tx model); `_autoBuy` (drop the rngLock guard `:568`); `DegenerusGame.autoOpen` (RD-3 rngLock-block + RD-5 drop-try/catch + entry-gate `rngLocked()||_livenessTriggered()` + `_autoOpenBox` internal + return raw count); `autoBuy` (return raw count); `IDegenerusGame` (autoOpen/autoBuy return signatures); `boxesPending()` (rngLock-aware); + GASOPT-03/04/05.

> **Edit-order (producer-before-consumer, so no intermediate state ships where advancing is unrewarded):**
> 1. **AdvanceModule** — DELETE the 3 advance `creditFlip` sites (`:189`/`:225`/`:468`, U1/U2/U3) + ADD the `(uint8 mult, bool rewardable)` return (mid-day/gameover `mult=1`, new-day `mult`=stall ladder). [R1 producer; ADV-01/02/03/05]
> 2. **DegenerusGame** — wrapper `:275` decode `data` into the tuple at `:283` + the rngLock-aware `advanceDue()`/`boxesPending()` views (R3) + the `autoOpen` rework (RD-3 block + RD-5 drop-try/catch + entry-gate + `_autoOpenBox` internal + pull out the `:1676` creditFlip U4, return raw count) + the `autoResolve → degeneretteResolve` rename (D-05; `RESOLVE_FLAT_BURNIE` named-placeholder marker) + the GASOPT-03 `keeperSnapshot`/`batchPurchaseForKeeper`. [R1 consumer/R3/R4 producer/D-05]
> 3. **interfaces** — `IDegenerusGameAdvanceModule.advanceGame()` tuple sig + `IDegenerusGame` `advanceDue()`/`boxesPending()` + the `autoOpen`/`autoBuy` return shapes + `keeperSnapshot` (NO `degeneretteResolve` row — ABSENT). [declares R1/R3/R4/GASOPT-03]
> 4. **AfKing** — PARAMETERLESS `doWork()` one-category router (RD-1 order + invariant-(a) early-return + `NoWork()` ROUTER-06) + the `autoBuy → _autoBuy` refactor + RD-2 drop the `:568` guard + the UNIFIED single `creditFlip` (R4) with the D-07 flat-per-tx formula (R5) + the standalone UNREWARDED `autoOpen(count)`/`autoBuy(count)` escapes + DELETE the autoBuy stall ladder/epoch (`:823-838`/`:829`, invariant d) + GASOPT-04 (drop `AutoBought`) + GASOPT-05 (drop `:676`, keep `:401`); NO `nonReentrant` guard (ROUTER-07); the D-01b TST-02 marker. [R2/R4/R5 consumer/producer; ROUTER-01..06; GASOPT-04/05]
> 5. **MintModule** — GASOPT-01 `[rk]` storage-pointer hoists in `processFutureTicketBatch` (`:398`) + `processTicketBatch` (`:671`); gas-only, behavior-identical. (Independent; can land anywhere — placed here for grouping.)
> 6. **tests** — the 5-file/57-ref `autoResolve → degeneretteResolve` rename-fixes incl. the `CrankLeversAndPacking.t.sol` literal source-string assertions + the GASOPT-04 `AutoBought`→`lastAutoBoughtDay`/balance-delta oracle migration across the 4 AfKing-family files (re-express `_countAutoBoughtFor==1` without weakening SAFE-03 / H-CANCEL-SWAP). The collision files land both edits together.

**Blueprint per work-area:**

- **Advance bounty re-home (SURVIVOR).** Delete the 3 creditFlips, return `(mult, rewardable)`; standalone `advanceGame()` stays functional + unrewarded; the ticket-drain/day-advance logic untouched. The KEPT GAME-day stall epoch (`:241-253`) becomes the SOLE stall epoch (invariant d).
- **AfKing `doWork` router (REWORKED).** Parameterless; routes `autoBuy→advance→autoOpen`; one-category early-return; single `creditFlip` CEI-last; the D-07 flat-per-tx formula (advance `2×·mult` / buy `1.5×` / open `1×` pro-rated below KNEE); `NoWork()` when all 3 O(1) predicates empty.
- **`_autoBuy` (REWORKED).** Internal; drop the `:568` rngLock guard (RD-2); return the raw buy-count; the KEEP-04 `bytes32("DGNRS")` affiliate passthrough (GAME-side `:1781`) survives the refactor.
- **`autoOpen` (REWORKED).** RD-3 block during rngLock (`boxesPending()` rngLock-aware) + RD-5 drop try/catch + the entry-gate `rngLocked()||_livenessTriggered()` + `_autoOpenBox` internal; pull out the `:1676` creditFlip; return the raw open-count; opens mid-day-resolved boxes.
- **`degeneretteResolve` (SURVIVOR).** Rename + the flat ≥3-NON-WWXRP-gate / `NoWork()`-at-zero re-peg (`:1611-1614` → `++successCount`; `:1622` → `≥3` flat + revert); WWXRP excluded; AUTO-02/try-catch/self-resolve/one-creditFlip preserved; router-fold OUT.
- **GASOPT-01/03/04/05.** 01 MintModule `[rk]` hoists (SURVIVOR); 03 batched keeper read `keeperSnapshot` (REWORKED, SUBSUMES 02); 04 drop `AutoBought` + the oracle migration (REWORKED, no-double-buy constraint); 05 drop `:676` keep `:401` (REWORKED, 333-SWEEP blocking-condition).

> **The flat-per-tx peg (the `1×` unit / the `1·1.5·2` ratios / the open `KNEE`) and the `~1-BURNIE` `RESOLVE_FLAT_BURNIE` constant are SPEC PLACEHOLDERS** — wired as named-marker placeholders at IMPL (330), calibrated under the USER-gated GAS phase (331).

### Success-criteria checklist (mapped 1:1 to the UPDATED ROADMAP Phase 329 SC1..SC4 — Task 3 updates the SC wording)

1. ✅ **SC1 — the 4 invariants locked under the redesign.** §2: (a) one-category early-return on `autoBuy→advance→autoOpen` (RD-1); (b) frozen advance-consume — autoBuy-pre-entropy + no new in-window SLOAD + TST-01 handoff (ADV-04); (c) free-fallback under autoBuy-first D-04a (PRIMARY blocked-while-buys-pend / SECONDARY 30-min + Vault/sStonk / TERTIARY death-clock); (d) single epoch SATISFIED BY DELETION (autoBuy stall ladder/epoch deleted, advance the sole epoch).
2. ✅ **SC2 — the redesigned shared signatures settled.** §1: R1 `advanceGame (mult,rewardable)` + mid-day `mult=1` + `:275` wrapper decode; R2 PARAMETERLESS `doWork()` + `NoWork()` + the standalone unrewarded `autoOpen(count)`/`autoBuy(count)` (NO `maxCount==0` sentinel); R3 the rngLock-aware O(1) views; R4 the unified single `creditFlip`; R5 the D-07 flat-per-tx model (GAS-331 placeholders).
3. ✅ **SC3 — ROUTER-07 no-guard re-grounded + GAS-03 satisfied-by-deletion.** §2: NO `nonReentrant` guard, re-grounded on the unified single `creditFlip` (D-01a basis + D-01b TST-02 backstop); GAS-03 cross-referenced to invariant (d).
4. ✅ **SC4 — every cited `file:line` grep-attested vs `0cc5d10f`; the producer-before-consumer survivors-vs-reworked edit-order map produced.** §0: 0 blockers, C1-C15 corrected anchors (the 3 advance creditFlips `:189/:225/:468` + the pulled-out open `:1676` + buy `:846` + the RD-2 `:568`/`:1737` guards + the GASOPT baselines); §3: the edit-order map (AdvanceModule → Game → interfaces → AfKing → MintModule → tests).

**SOURCE-TREE not mutated at SPEC (the held-330 diff untouched).** Phase 330 re-IMPL applies the single reconciled batched diff (HELD at the contract-commit boundary for explicit user hand-review). `git diff --name-only 0cc5d10f HEAD -- 'contracts/*.sol'` is EMPTY.
