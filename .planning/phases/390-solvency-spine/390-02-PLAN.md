---
phase: 390-solvency-spine
plan: 02
type: execute
wave: 2
depends_on: ["390-01"]
files_modified:
  - .planning/phases/390-solvency-spine/390-02-CLAUDE-NET.md
  - .planning/phases/390-solvency-spine/390-FINDINGS.md
autonomous: true
requirements: [SOLV-01, SOLV-02, SOLV-03, SOLV-04, SOLV-05, SOLV-06, SOLV-07]
user_setup: []

must_haves:
  truths:
    - "NET 2 (the Claude adversarial net) is on record for the full SOLVENCY surface, independent of the council"
    - "Every SOLV-01..07 requirement has an explicit ADJUDICATED verdict (CONFIRMED / REFUTED / BY-DESIGN / MONITOR) with the bound or source-cite that settles it"
    - "Every FC-390-01..07 lead AND every inherited cross-ref (FC-389-02, FC-389-08, FC-392-08, FC-393-02, FC-393-03) has an explicit ADJUDICATED verdict folded with the matching council lead"
    - "SOLV-05 / FC-390-01 (redemption-claim liveness-window ordering) is settled by a dedicated multi-tx ordering analysis across the livenessTriggered→gameOver latch and handleGameOverDrain totalFunds snapshot — proven strand/double-credit-free OR surfaced as a finding, not hand-waved"
    - "SOLV-04 / FC-390-02 (dust-forfeit self-credit) is settled by proving the credit is always backed by value leaving sDGNRS (msg.value + stETH-pull) under the MAX-reservation guarantee — proof or finding"
    - "SOLV-06 (the V62-03 CEI / yield-surplus reentrancy class) is settled by a CEI-ordering trace over every changed ETH/stETH payout leg — proof or finding"
    - "Both nets are on record before the verdict is issued; the skeptic gate is run before any CATASTROPHE/HIGH"
    - "Any CONFIRMED finding is DOCUMENTED and ROUTED to a gated fix (USER hand-review, batched) — NOT fixed in this phase; the subject stays byte-frozen"
  artifacts:
    - path: ".planning/phases/390-solvency-spine/390-02-CLAUDE-NET.md"
      provides: "NET 2 adversarial analysis over the SOLVENCY surface, independent of the council, with per-req / per-lead attack attempts + multi-tx ordering traces"
      min_lines: 80
    - path: ".planning/phases/390-solvency-spine/390-FINDINGS.md"
      provides: "The per-item adjudication table: every SOLV req + every FC-390 lead + every inherited cross-ref with a CONFIRMED/REFUTED/BY-DESIGN/MONITOR verdict, both-nets-on-record, skeptic gate, routing of any CONFIRMED finding"
      min_lines: 90
  key_links:
    - from: "390-FINDINGS.md"
      to: "390-01 council outputs (council/*.txt)"
      via: "both-nets-on-record fold"
      pattern: "council"
    - from: "390-FINDINGS.md"
      to: "SOLV-01..07 + FC-390-01..07 + inherited cross-refs"
      via: "per-item adjudicated verdict rows"
      pattern: "SOLV-05|FC-390-01"
---

<objective>
Run NET 2 (the deep Claude adversarial net) over the SAME solvency-spine surface the council swept,
then ADJUDICATE every SOLV-01..07 requirement, every FC-390-01..07 lead, and every inherited cross-ref
(FC-389-02, FC-389-08, FC-392-08, FC-393-02, FC-393-03) against the byte-frozen subject `a8b702a7`,
folding in the NET-1 council leads from 390-01. Run the skeptic gate before presenting anything as
CATASTROPHE/HIGH, and write `390-FINDINGS.md` with a per-item verdict table (CONFIRMED / REFUTED /
BY-DESIGN / MONITOR) each carrying the bound or source-cite that settles it.

Purpose: this is the verdict step for the SPINE. A no-finding verdict for any sweep slice requires BOTH
nets on record (AUDIT-V63-PLAN §2) — this plan puts NET 2 on record and synthesizes both nets. Solvency
is the highest-priority dimension (§4) and SOLV-06 is the V62-03 CEI class the council caught Claude-only
missing before, so the three prime targets (liveness ordering, dust-forfeit backing, CEI/yield-surplus)
get dedicated rigorous treatment. Any CONFIRMED finding is DOCUMENTED + ROUTED to a gated
USER-hand-review fix, never fixed here (the subject is byte-frozen and re-freezes only after a separate
gated boundary).

Output: 390-02-CLAUDE-NET.md (the independent adversarial analysis) and 390-FINDINGS.md (the
adjudication of the full phase-390 surface). NO contract source is touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/AUDIT-V63-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/phases/388-foundation-subject-freeze-green-baseline/388-02-FINDING-CANDIDATES.md
@.planning/phases/388-foundation-subject-freeze-green-baseline/388-02-ORACLE-HOLES.md
@.planning/v63-surface-map/solvency.md
@.planning/phases/390-solvency-spine/390-01-COUNCIL-NET.md
@.planning/phases/390-solvency-spine/council/solv.gemini.txt
@.planning/phases/390-solvency-spine/council/solv.codex.txt
@test/REGRESSION-BASELINE-v63.md

<interfaces>
<!-- The 7 reqs to adjudicate (REQUIREMENTS.md SOLV): -->
<!--   SOLV-01 claimablePool == Σ claimable+afking across changed credit/debit (_debitClaimableAndAfking); -->
<!--   SOLV-02 sDGNRS pendingRedemptionEthValue backing identity (balance+stETH >= obligations); -->
<!--   SOLV-03 redemption submit/claim conservation (ethDirect+lootboxEth+forfeitEth == released); -->
<!--   SOLV-04 dust-forfeit self-credit always backed, never bumps claimable beyond pending release; -->
<!--   SOLV-05 redemption CLAIM liveness-window ordering vs handleGameOverDrain snapshot / gameOver latch; -->
<!--   SOLV-06 CEI / yield-surplus reentrancy on ETH/stETH legs (stETH-before-ETH; V62-03 class); -->
<!--   SOLV-07 JackpotModule delta-fold completeness (no pool credited/deleted twice). -->
<!-- The 7 owned leads (388-02-FINDING-CANDIDATES row C): FC-390-01..07. -->
<!-- The 5 inherited cross-refs: FC-389-02 + FC-389-08 (uint96/uint128 narrowing solvency-conservation -->
<!--   half), FC-392-08 (redemption ETH-spin pool RMW + recirc vs CEI), FC-393-02 (forfeit-to-self -->
<!--   timing), FC-393-03 (partial-balance redemption-leg solvency under same-block bursts). -->
<!-- Authoritative frozen line-cites (verified at a8b702a7 — re-read via git show, do not trust blind): -->
<!--   StakedDegenerusStonk.sol: claimRedemption @771; claimRedemptionMany @787; _claimRedemptionFor -->
<!--     @821; dust-forfeit @845-848 + @897-900 (creditRedemptionDirect{value}(address(this),forfeitEth) -->
<!--     @900; ethForForfeit=min(bal,forfeitEth) @899); direct leg @890; release @854; submit MAX -->
<!--     reservation @1066; BurnsBlockedBeforeDailyRng @991; _pendingRedemptionEthValue uint96 @223; -->
<!--     both-leg GAME pre-approve @431-432; sDGNRS backing read totalMoney = ethBal+stethBal+ -->
<!--     claimableEth - _pendingRedemptionEthValue @674/924/1023. -->
<!--   DegenerusGameGameOverModule.sol: handleGameOverDrain @73; totalFunds snapshot @78; reserved= -->
<!--     claimablePool @85; gameOver=true @135; final sweep claimablePool=0 @206; FinalSwept owed-split -->
<!--     @213-222. -->
<!--   DegenerusGameLootboxModule.sol: creditRedemptionDirect body @1004-1015; resolveRedemptionLootbox -->
<!--     @926-957. -->
<!--   DegenerusGame.sol: claimWinnings CEI @1209 (_debitClaimableAndAfking @1247; claimablePool -= -->
<!--     payout @1248 before .call); _payoutWithStethFallback @1888 (53cd25cf reorder); -->
<!--     pullRedemptionReserve @1572 (claimablePool -= amount @1583); creditRedemptionDirect stub @1552. -->
<!--   DegenerusGameJackpotModule.sol: distributeYieldSurplus @682 (obligations @686-690; cached-fold -->
<!--     write @713); _processSoloBucketWinner @1247; daily-jackpot fold claimablePool += liabilityDelta -->
<!--     @1119; _addClaimableEth REMOVED. -->
<!--   DegenerusGameDecimatorModule.sol: claimDecimatorJackpot permissionless @283-322; batch @339-380. -->
<!--   DegenerusVault.sol: burnEth claim-trigger @751-770; gameDegeneretteBet overpay @577-589. -->
<!--   Green oracle: test/REGRESSION-BASELINE-v63.md = forge 854/0/110. STRONG redemption coverage = -->
<!--     RedemptionStethFallback.t.sol (10/10 branch-proofs, V62-03 CEI class EXERCISED) + -->
<!--     RedemptionAccounting.t.sol (per-(player,day) EXERCISED). EthSolvency.inv.t.sol EXERCISED -->
<!--     game-side but the redemption-credit legs are NOT in its action set (gap routed here). The legacy -->
<!--     RedemptionInvariants 7-INV harness is a HOLE (388-02 #2: un-wired claim/resolve + stale slots -->
<!--     10/13/14/15 → INV-05/07 vacuous, INV-08 a 0==0 tautology) — SUPERSEDED; do NOT rely on its green. -->
<!--   Frozen-source read convention: git show a8b702a7:contracts/<File>.sol (ignore working tree). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NET 2 adversarial analysis + per-req / per-lead adjudication vs frozen source</name>
  <files>.planning/phases/390-solvency-spine/390-02-CLAUDE-NET.md</files>
  <read_first>
    - .planning/phases/388-foundation-subject-freeze-green-baseline/388-02-FINDING-CANDIDATES.md (rows FC-390-01..07 verbatim + the inherited cross-refs FC-389-02, FC-389-08, FC-392-08, FC-393-02, FC-393-03 — the leads to adjudicate)
    - .planning/v63-surface-map/solvency.md (the master invariant + the change inventory A1..G5 + the "where the identity holds" reconciliation 1..5 + CF-1..CF-7 to attack)
    - .planning/phases/388-foundation-subject-freeze-green-baseline/388-02-ORACLE-HOLES.md (test #2 the superseded legacy RedemptionInvariants HOLE — do NOT lean on its green; tests #3/#4 carry the real redemption coverage; test #5 EthSolvency redemption-leg gap routed here)
    - the frozen source via git show a8b702a7:contracts/StakedDegenerusStonk.sol (claimRedemption/_claimRedemptionFor/dust-forfeit ~771-901, the sDGNRS backing reads totalMoney ~674/924/1023, the submit MAX reservation ~1066), contracts/modules/DegenerusGameGameOverModule.sol (handleGameOverDrain ~73-222), contracts/modules/DegenerusGameLootboxModule.sol (creditRedemptionDirect ~1004-1015, resolveRedemptionLootbox ~926-957), contracts/DegenerusGame.sol (claimWinnings CEI ~1209-1255, _payoutWithStethFallback ~1888-1909, pullRedemptionReserve ~1572-1600), contracts/modules/DegenerusGameJackpotModule.sol (distributeYieldSurplus ~682-716, _processSoloBucketWinner ~1247-1282, daily-jackpot fold ~1118-1120), contracts/modules/DegenerusGameDecimatorModule.sol (permissionless claim + batch ~283-380), contracts/DegenerusVault.sol (burnEth ~751-770), and the redemption ETH-spin recirc path in contracts/modules/DegenerusGameDegeneretteModule.sol (~1400) + _resolveRedemptionChunk for FC-392-08
    - .planning/phases/390-solvency-spine/390-01-COUNCIL-NET.md + council/*.txt (NET 1 leads to consider — but attack INDEPENDENTLY first, then compare)
  </read_first>
  <action>
    First confirm `git diff a8b702a7 -- contracts/` is empty (read-only over the frozen subject). Read
    all source via `git show a8b702a7:contracts/<File>.sol` — ignore the working tree.

    Run the Claude adversarial net INDEPENDENTLY of the council (do not read the council outputs as the
    starting point — attack the identity/conservation claims first, then fold the council leads in at the
    end of this task). For each of the 7 reqs, 7 owned leads, and 5 inherited cross-refs, attempt to
    BREAK the identity/safety claim with a concrete reachable call sequence (multi-tx where the ordering
    matters), then record the result.

    SOLV adjudication (SOLV-01..07 + FC-390-01..07 + the inherited cross-refs):

    - SOLV-01 (claimablePool == Σ claimable + afking): enumerate EVERY changed credit/debit touch on the
      spine (the change inventory A1..G5) and confirm each pairs an equal claimablePool move — the
      JackpotModule manual delta-fold (claimableDelta accumulation → one claimablePool += per path, daily
      @1119, yield @713), the _processSoloBucketWinner whale-pass split (ETH portion → claimableDelta;
      whalePassCost → futurePrizePool not claimablePool), creditRedemptionDirect (_creditClaimable +
      claimablePool += amount @LootboxModule:1004-1015), pullRedemptionReserve (_debitClaimable + 
      claimablePool -= amount @Game:1583), claimWinnings CEI (_debitClaimableAndAfking + claimablePool -=
      payout @Game:1247-1248). For each, state the bound/cite that proves the pairing. SOLV-07 folds
      here for the JackpotModule delta-fold (the _addClaimableEth inline removal must capture EVERY
      _creditClaimable into the manual delta — find any path with a _creditClaimable NOT folded into a
      claimablePool +=, or a pool credited/deleted twice).

    - SOLV-02 (sDGNRS backing identity): trace the backing read totalMoney = ethBal + stethBal +
      claimableEth − _pendingRedemptionEthValue (StakedDegenerusStonk:674/924/1023) and confirm
      balance+stETH >= obligations holds across the rework. FC-389-02 / FC-389-08 fold here: confirm NO
      redemption path inflates `_pendingRedemptionEthValue` (uint96 @223) or a `poolBalances` (uint128)
      beyond width — a silent truncating cast on release/segregation would UNDERSTATE segregated ETH →
      backing drift. State the real-ETH bound (<<uint96) and pool-conservation bound (<<uint128) and
      check the cast sites (the release @854, the submit increment @1066) operate on CHECKED arithmetic.

    - SOLV-03 (submit/claim conservation): prove `ethDirect + lootboxEth + forfeitEth == totalRolledEth`
      in EVERY branch of _claimRedemptionFor (the full-lootbox branch, the dust-forfeit branch @845-848,
      the post-gameOver 100% direct branch) and that `_pendingRedemptionEthValue -= totalRolledEth` @854
      releases EXACTLY that — the submit-side MAX(175%) over-reservation @1066 staying in sDGNRS as free
      backing. Confirm no double-count or leak across the submit↔claim boundary.

    - SOLV-04 / FC-390-02 (the §6 prime — DEDICATED rigorous treatment): the forfeit leg
      creditRedemptionDirect{value: ethForForfeit}(address(this), forfeitEth) @900 credits sDGNRS's OWN
      claimable. PROVE the credit is ALWAYS backed by value actually leaving sDGNRS, OR surface a finding.
      Trace: `ethForForfeit = min(bal, forfeitEth)` @899; the GAME-side creditRedemptionDirect
      (LootboxModule:1004-1015) requires msg.value<=amount and pulls `amount − msg.value` as stETH via
      transferFrom, THEN _creditClaimable(address(this), amount) + claimablePool += amount. The credit is
      `amount` (= forfeitEth). For the credit to be fully backed, sDGNRS must release exactly forfeitEth =
      (the ETH sent ethForForfeit) + (the stETH the GAME pulls = forfeitEth − ethForForfeit). Attack the
      MAX-reservation guarantee that ETH+stETH >= rolled: find a sequence where address(this).balance is
      drained below forfeitEth (by an earlier leg / many partial claims) AND sDGNRS lacks sufficient
      stETH balance/approval for the remainder pull, so the GAME pull reverts (fail-closed) OR — worse —
      succeeds while crediting MORE than sDGNRS released (a phantom claimable bump). In the stETH-leg
      submit case (no claimable debited at submit), confirm the forfeit credit still nets against the
      pendingRedemptionEthValue release, not an over-credit. Settle either way — no hand-waving.

    - SOLV-05 / FC-390-01 (the §6 prime — DEDICATED multi-tx ordering treatment): claimRedemption /
      claimRedemptionMany read `isGameOver = game.gameOver()` ONCE (@771/787) then _claimRedemptionFor
      routes the live path when !isGameOver. PROVE no strand/double-credit across the
      livenessTriggered→gameOver latch and handleGameOverDrain, OR surface a finding. Trace
      handleGameOverDrain (GameOverModule:73): totalFunds snapshot @78 (= balance + stETH), reserved =
      claimablePool @85, refunds may grow claimablePool @130, gameOver=true latch @135, recalculated
      available @150-154, decimator spend @169, final sweep claimablePool=0 @206 + FinalSwept owed-split
      @213-222. Enumerate the interleavings: (a) a live claimRedemption mined in the SAME block as / just
      after the drain's totalFunds snapshot but before gameOver latches — does the live credit (which
      bumps claimablePool via creditRedemptionDirect AND pushes real ETH/stETH back into the game) land
      consistently with the drain's reserved=claimablePool, or can the drain sweep funds the live claim
      then needs? (b) a claim that observes isGameOver=false, releases pendingRedemptionEthValue, then
      gameOver latches before the credit legs complete — can value be released-but-not-credited
      (stranded)? (c) the post-gameOver path (100% direct push) double-paying a claim that already settled
      live. Note the submit side IS gated (BurnsBlockedDuringLiveness / RedemptionSweptDuringLiveness) but
      the claim side is not — assess whether the claimablePool reserve + the segregated
      pendingRedemptionEthValue together make the claim side safe-by-construction, or whether a concrete
      ordering breaks it. Settle either way.

    - SOLV-06 (the §6 prime — DEDICATED CEI / V62-03 treatment): _payoutWithStethFallback @1888 carries
      the 53cd25cf reorder (stETH leg out FIRST, untrusted ETH .call LAST). Trace EVERY changed ETH/stETH
      payout leg on the spine — claimWinnings @1209 (state debit @1247-1248 BEFORE the call), the
      redemption credit legs, pullRedemptionReserve @1572, the dust-forfeit ETH send — and confirm CEI
      ordering (state mutated before the external call) AND that a reentrant distributeYieldSurplus @682
      (whose obligations sum @686-690 reads claimablePool + the pending pools) cannot observe in-flight
      stETH as unreserved backing and over-distribute. Lean on RedemptionStethFallback.t.sol's 10/10
      branch-proofs (the V62-03 class is EXERCISED) but ALSO attack independently — find any leg where
      the reorder was NOT applied or a new redemption credit leg reopens the vector. Settle either way.

    - FC-390-03 (permissionless decimator/redemption claim + post-gameOver/freeze edge, cross-ref 393):
      the batch caches `over`/`frozen` ONCE — find any path where it flips mid-batch and splits a batch
      across the boundary inconsistently, or a permissionless trigger resolves a victim's claim into
      claimable that is then forfeited (DecimatorModule:283-380). REFUTE or surface.
    - FC-390-04 (burnEth claim-trigger simplification): confirm the edge `amount == supplyBefore` (last
      DGVE burn → REFILL_SUPPLY mint) can't let claimValue slightly exceed combined with claimable==0
      (sentinel-only) → a wasted claimWinnings(vault) for a 0-net amount (DegenerusVault:751-770).
      LOW-impact; REFUTE/MONITOR with the proof-comment check.
    - FC-390-05 (gameDegeneretteBet overpay guard relocated game-side): re-confirm _collectBetFunds:596
      `if (ethPaid > totalBet) revert` with totalBet = amountPerTicket*ticketCount matches the removed
      vault formula — INFO/verified.
    - FC-390-06 (keeper-bounty creditFlip issuance bounded, cross-ref 393/ACCESS-02): confirm issuance is
      bounded (one box/wallet/day redemption; one decimator entry per real burn) so a keeper cannot mint
      unbounded BURNIE claim-value — note BURNIE is off the direct ETH spine; the downstream-dilution
      assessment is owned by ACCESS-02/393, adjudicate only the issuance bound here.
    - FC-390-07 (vault deposit() removal completeness): grep-confirm no remaining game-side
      `vault.deposit(...)` call (dead/reverting) and that vaultMintAllowance() over-mint reverts inside
      vaultMintTo against the live allowance — INFO.
    - FC-392-08 (redemption ETH-spin pool RMW + recirc vs CEI, cross-ref 392/393): the redemption path
      (allowEthSpin=true) reaches the ETH-spin whose _distributePayout does a live ETH-pool RMW + recirc
      box INSIDE the redemption claim (DegeneretteModule ~1400, _resolveRedemptionChunk). Trace whether
      the ETH-spin pool writes + the dust-forfeit + the pendingRedemptionEthValue release reconcile
      EXACTLY, and whether the recirc box's _applyEvMultiplierWithCap cap RMW can be raced across
      redemption chunks. Settle the solvency/CEI half here (the ECON EV half is owned by 392).
    - FC-393-02 (forfeit-to-self timing, cross-ref 393): confirm a keeper choosing WHICH redemptions to
      settle can only bias the TIMING of benign backing accrual (the per-claim split is deterministic
      from the fixed roll + owed value), with NO per-victim value extraction — INFO.
    - FC-393-03 (partial-balance redemption-leg solvency under same-block bursts, cross-ref 393): under
      an adversarial sequence of many partial-balance live claims in ONE block, each leg sends
      `min(address(this).balance, legAmount)` ETH and pulls the rest as stETH; the MAX(175%) reservation
      is claimed to guarantee ETH+stETH >= rolled. Confirm segregation accounting
      (_pendingRedemptionEthValue release == total leg movement) never strands ETH or under-pulls stETH
      across the burst. Settle the solvency half here (the ACCESS-04 access half coordinated with 393).

    For each item: attempt the break, record PROPERTY · attack/call-sequence tried (multi-tx ordering for
    the prime targets) · STATE VAR + file:line at a8b702a7 · the bound/cite that settles it · a
    provisional verdict (CONFIRMED / REFUTED / BY-DESIGN / MONITOR). Note where the green baseline's
    EXERCISED redemption tests (RedemptionStethFallback 10/10, RedemptionAccounting, EthSolvency
    game-side) already prove a leg — and where the EthSolvency redemption-leg gap (routed here from
    388-02 #5) leaves a property un-netted by an always-on invariant (record whether a sweep wants a
    redemption action added to a solvency handler, as a routed test-hardening note, NOT a contract
    change). Do NOT lean on the superseded legacy RedemptionInvariants 7-INV green. At the END of the
    task, read the council outputs (390-01 council/*.txt) and note for each item whether the council
    raised a convergent/divergent lead — fold any council-only lead in as an additional row to
    adjudicate. Write all of this to 390-02-CLAUDE-NET.md.

    Re-verify `git diff a8b702a7 -- contracts/` empty at the end (read-only). Do NOT run hardhat. Use
    neutral defensive-engineering vocabulary throughout.
  </action>
  <verify>
    <automated>test -s .planning/phases/390-solvency-spine/390-02-CLAUDE-NET.md && for id in SOLV-01 SOLV-02 SOLV-03 SOLV-04 SOLV-05 SOLV-06 SOLV-07 FC-390-01 FC-390-02 FC-390-03 FC-390-04 FC-390-05 FC-390-06 FC-390-07 FC-389-02 FC-389-08 FC-392-08 FC-393-02 FC-393-03; do grep -q "$id" .planning/phases/390-solvency-spine/390-02-CLAUDE-NET.md || { echo "MISSING $id"; exit 1; }; done && grep -Eq "handleGameOverDrain|liveness" .planning/phases/390-solvency-spine/390-02-CLAUDE-NET.md && grep -Eq "forfeit" .planning/phases/390-solvency-spine/390-02-CLAUDE-NET.md && grep -Eq "CEI|_payoutWithStethFallback" .planning/phases/390-solvency-spine/390-02-CLAUDE-NET.md && [ -z "$(git -C /home/zak/Dev/PurgeGame/degenerus-audit diff a8b702a7 -- contracts/)" ]</automated>
  </verify>
  <acceptance_criteria>
    390-02-CLAUDE-NET.md contains an independent adversarial analysis with an attack attempt + a
    provisional verdict for EACH of SOLV-01..07, FC-390-01..07, and the inherited cross-refs FC-389-02,
    FC-389-08, FC-392-08, FC-393-02, FC-393-03; SOLV-05/FC-390-01 carries a dedicated multi-tx ordering
    treatment across the gameOver-drain snapshot / latch (proof or finding, not hand-waved); SOLV-04/
    FC-390-02 carries a dedicated dust-forfeit backing proof under the MAX-reservation guarantee; SOLV-06
    carries a dedicated CEI-ordering trace over the changed payout legs; the EthSolvency redemption-leg
    coverage gap is noted as a routed test-hardening item; the council leads are folded in at the end;
    `git diff a8b702a7 -- contracts/` is empty.
  </acceptance_criteria>
  <done>
    NET 2 (the Claude adversarial net) is on record, independent of the council, with a per-item attack
    attempt + provisional verdict over the full phase-390 solvency surface, the three prime targets given
    dedicated rigorous treatment, the council leads folded in.
  </done>
</task>

<task type="auto">
  <name>Task 2: Synthesize both nets, run the skeptic gate, write 390-FINDINGS.md with the per-item verdict table</name>
  <files>.planning/phases/390-solvency-spine/390-FINDINGS.md</files>
  <read_first>
    - .planning/phases/390-solvency-spine/390-02-CLAUDE-NET.md (Task 1 — NET 2 provisional verdicts)
    - .planning/phases/390-solvency-spine/390-01-COUNCIL-NET.md + council/*.txt (NET 1 leads)
    - .planning/phases/389-packing-identity/389-FINDINGS.md (the just-approved FINDINGS structure to match — header, both-nets table, per-item verdict table, skeptic gate, routing block)
    - .planning/AUDIT-V63-PLAN.md §2 (both-nets-on-record rule) + §4 (threat weighting: solvency = SPINE) + §5 (design-intent anchor) + §8 (process rules: audit-only, routing)
    - .planning/REQUIREMENTS.md (the exact SOLV-01..07 text to attest)
    - the skeptic-gate posture: [[feedback_skeptic_pass_before_catastrophe]] (structural-protection check + 3-condition EV lens before any CATASTROPHE/HIGH)
  </read_first>
  <action>
    Synthesize NET 1 (council) and NET 2 (Claude) into 390-FINDINGS.md — the phase-390 adjudication
    deliverable. Structure:

    (1) Header: subject `a8b702a7` byte-frozen; baseline = REGRESSION-BASELINE-v63 (854/0/110, strong
    redemption coverage RedemptionStethFallback 10/10 + RedemptionAccounting EXERCISED; the legacy
    RedemptionInvariants 7-INV harness is a SUPERSEDED HOLE — not relied on); method = COUNCIL + CLAUDE
    both; posture = AUDIT-ONLY (a CONFIRMED finding routes to a gated USER-hand-review fix, never fixed
    here); solvency = the SPINE (§4 — the highest-priority dimension).

    (2) Both-nets-on-record attestation: a short table confirming NET 1 (council — available/skipped per
    390-01-COUNCIL-NET.md) and NET 2 (Claude — 390-02-CLAUDE-NET) are both on record for the SOLVENCY
    slice. A no-finding (REFUTED/BY-DESIGN) verdict for any item MUST cite both nets.

    (3) The per-item adjudication table — ONE row per item, covering ALL 7 reqs (SOLV-01..07), ALL 7
    owned leads (FC-390-01..07), AND the 5 inherited cross-refs (FC-389-02, FC-389-08, FC-392-08,
    FC-393-02, FC-393-03). Columns: ITEM · what it claims · NET-1 result · NET-2 result · ADJUDICATED
    VERDICT (CONFIRMED / REFUTED / BY-DESIGN / MONITOR) · the settling bound/source-cite (file:line at
    a8b702a7 or the green-baseline anchor). SOLV-05 / FC-390-01 MUST cite the multi-tx ordering bound (or
    carry a CONFIRMED finding); SOLV-04 / FC-390-02 MUST cite the dust-forfeit backing proof under the
    MAX-reservation; SOLV-06 MUST cite the CEI-ordering trace + the RedemptionStethFallback anchor.
    Weight each finding per AUDIT-V63-PLAN §4 (solvency is the spine — a real claimablePool/backing break
    is HIGH-or-above; a benign timing/INFO item is not). Apply the §5 design-intent anchor (the
    dust-drop forfeit-to-claimable is an intended anti-dust-farm mechanism — verify it's BACKED, do not
    flag the policy; the keeper creditFlip bounty's downstream BURNIE dilution is owned by ACCESS-02/393).

    (4) Skeptic gate: before tagging ANY item CATASTROPHE/HIGH, run the dual-gate filter
    (structural-protection check + the 3-condition EV lens) and record the gate result for each elevated
    item. If nothing reaches HIGH (the surface-maps found 0 HIGH on inspection — the change set preserves
    the claimablePool + sDGNRS-backing identities), state that explicitly with the gate applied to the
    MED-attention items (FC-390-01 liveness, FC-390-02 dust-forfeit, FC-390-03 permissionless-claim,
    FC-392-08 ETH-spin-recirc-CEI, FC-393-03 partial-balance). If the dedicated treatment of a prime
    target surfaces a real break, run the full skeptic gate and weight it.

    (5) Routing: for any CONFIRMED finding, write a CONTRACT-CHANGE-NEEDED / ROUTED block (the finding,
    its weight, the proposed fix shape, and the note that it goes to a SEPARATE gated USER-hand-review
    boundary — batched, never pre-approved, never auto-committed; the subject re-freezes after). If there
    are zero CONFIRMED findings, state "0 CONFIRMED — document-only; SOLV-01..07 attested at a8b702a7"
    and note any INFO/MONITOR items carried forward (e.g. FC-390-04 wasted-call edge, FC-390-05 verified,
    FC-390-06 → ACCESS-02/393, FC-392-08 ECON-half → 392, FC-393-02/-03 → 393, the FC-389-02/-08
    narrowing-conservation confirmation, and the EthSolvency redemption-leg test-hardening note routed as
    a possible always-on net at a later test phase). Any test-only (oracle-integrity) gap is routed, NOT
    a contract finding.

    (6) Re-attestation line: each of SOLV-01..07 marked attested-or-finding.

    Re-confirm `git diff a8b702a7 -- contracts/` empty at the end. Neutral vocabulary throughout. No
    contract source is modified — this is a documentation deliverable.
  </action>
  <verify>
    <automated>test -s .planning/phases/390-solvency-spine/390-FINDINGS.md && for id in SOLV-01 SOLV-02 SOLV-03 SOLV-04 SOLV-05 SOLV-06 SOLV-07 FC-390-01 FC-390-02 FC-390-03 FC-390-04 FC-390-05 FC-390-06 FC-390-07 FC-389-02 FC-389-08 FC-392-08 FC-393-02 FC-393-03; do grep -q "$id" .planning/phases/390-solvency-spine/390-FINDINGS.md || { echo "MISSING $id"; exit 1; }; done && grep -Eq "both.nets|NET 1.*NET 2|both nets on record" .planning/phases/390-solvency-spine/390-FINDINGS.md && grep -Eq "skeptic" .planning/phases/390-solvency-spine/390-FINDINGS.md && [ -z "$(git -C /home/zak/Dev/PurgeGame/degenerus-audit diff a8b702a7 -- contracts/)" ]</automated>
  </verify>
  <acceptance_criteria>
    390-FINDINGS.md contains: a both-nets-on-record attestation; a per-item verdict row for EACH of
    SOLV-01..07, FC-390-01..07, and the 5 inherited cross-refs with a CONFIRMED/REFUTED/BY-DESIGN/MONITOR
    verdict + settling cite; SOLV-05/FC-390-01 cites the multi-tx ordering bound or carries a finding;
    SOLV-04/FC-390-02 cites the dust-forfeit backing proof; SOLV-06 cites the CEI-ordering trace; the
    skeptic gate is run and recorded; any CONFIRMED finding is documented + ROUTED (not fixed);
    `git diff a8b702a7 -- contracts/` is empty.
  </acceptance_criteria>
  <done>
    The phase-390 solvency-spine surface is adjudicated with both nets on record, the skeptic gate
    applied, every req + owned lead + inherited cross-ref carrying an explicit verdict, the three prime
    targets settled by dedicated proof-or-finding, and any CONFIRMED finding documented + routed to a
    gated fix — the subject byte-frozen throughout.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit-process → byte-frozen subject | the risk is a MISSED solvency-identity break or a FALSE-GREEN adjudication (a real claimablePool / sDGNRS-backing defect waved through), or the audit mutating the frozen subject — not a new feature threat |
| single-net verdict | the risk is issuing a no-finding verdict with only one net on record (the v60/v62 lesson: Claude-only missed council findings, esp. the V62-03 CEI class) |

## STRIDE Threat Register (audit-only posture)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-390-04 | Tampering | byte-frozen subject contracts/ | mitigate | git-status-verify `git diff a8b702a7 -- contracts/` empty at the end of each task; all source read via `git show a8b702a7:` |
| T-390-05 | Repudiation / false-green | a REFUTED/BY-DESIGN solvency verdict issued without both nets, or leaning on the superseded RedemptionInvariants HOLE green | mitigate | the both-nets-on-record table; every no-finding verdict cites NET 1 + NET 2; the superseded HOLE is explicitly not relied on, the EXERCISED redemption tests are the anchor |
| T-390-06 | Elevation / under-weighting | a CONFIRMED solvency break under-weighted, or a HIGH tagged without the skeptic filter | mitigate | the skeptic gate runs before any CATASTROPHE/HIGH; solvency weighted as the SPINE per AUDIT-V63-PLAN §4 (a real claimablePool/backing break is HIGH-or-above) |
| T-390-07 | Tampering | a CONFIRMED finding silently fixed in-phase | mitigate | audit-only posture: CONFIRMED findings are DOCUMENTED + ROUTED to a gated USER-hand-review boundary, never fixed here |
</threat_model>

<verification>
- NET 2 (Claude) on record, independent, per-item attack + verdict over all 7 reqs + 7 owned leads + 5
  inherited cross-refs.
- Both nets on record before the verdict; the both-nets table present.
- SOLV-05 / FC-390-01 settled (multi-tx ordering proof or finding); SOLV-04 / FC-390-02 settled
  (dust-forfeit backing proof); SOLV-06 settled (CEI-ordering trace).
- Skeptic gate run before any CATASTROPHE/HIGH; result recorded; solvency weighted as the spine.
- Any CONFIRMED finding documented + ROUTED (not fixed); subject byte-frozen.
- `git diff a8b702a7 -- contracts/` empty.
</verification>

<success_criteria>
The full phase-390 solvency-spine surface (SOLV-01..07 + the 7 FC-390 leads + the 5 inherited
cross-refs) is adjudicated against the byte-frozen subject `a8b702a7` with BOTH nets on record, the
skeptic gate applied, and every item carrying an explicit CONFIRMED/REFUTED/BY-DESIGN/MONITOR verdict
backed by a bound or source-cite. The three prime targets (redemption-claim liveness ordering,
dust-forfeit backing, CEI/yield-surplus reentrancy) are each settled by dedicated rigorous treatment.
Any CONFIRMED finding is documented + routed to a gated fix; the subject stays byte-frozen.
</success_criteria>

<output>
Create `.planning/phases/390-solvency-spine/390-02-SUMMARY.md` when done
</output>
</content>
</invoke>
