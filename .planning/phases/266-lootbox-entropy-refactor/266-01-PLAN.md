---
phase: 266-lootbox-entropy-refactor
phase_number: 266
plan: 266-01
plan_id: 266-01
plan_number: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/modules/DegenerusGameLootboxModule.sol
  - test/stat/LootboxEntropyDistribution.test.js
  - test/gas/LootboxOpenGas.test.js
  - test/gas/AdvanceGameGas.test.js
  - test/stat/SurfaceRegression.test.js
  - package.json
  - audit/FINDINGS-v36.0.md
  - KNOWN-ISSUES.md
  - .planning/phases/266-lootbox-entropy-refactor/266-01-ADVERSARIAL-LOG.md
  - .planning/phases/266-lootbox-entropy-refactor/266-01-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/MILESTONES.md
autonomous: false
milestone: v36.0
milestone_name: Lootbox-Path Entropy Refactor
deliverable: audit/FINDINGS-v36.0.md
phase_status: terminal
write_policy: "Mixed-shape phase per CONTEXT.md (1 batched contract commit + N batched test commits + AGENT-COMMITTED audit artifacts). USER-APPROVED batched gates at end of Wave 1 (LootboxModule contract diff per `feedback_batch_contract_approval.md`) and end of Wave 2 (test diff). Audit deliverable `audit/FINDINGS-v36.0.md` + `266-01-ADVERSARIAL-LOG.md` + `266-01-SUMMARY.md` + KNOWN-ISSUES.md edit + ROADMAP/STATE/MILESTONES flips ARE agent-committed atomic-per-task. Per `feedback_never_preapprove_contracts.md`, agent does NOT pre-approve any contract or test commit — wait for explicit 'approved' string before commit at Task 5 + Task 10. Final user-review gate at Task 21 per `feedback_manual_review_before_push.md` before READ-only flip on `audit/FINDINGS-v36.0.md`."

requirements:
  - ENT-01
  - ENT-02
  - ENT-03
  - ENT-04
  - ENT-05
  - ENT-06
  - STAT-01
  - STAT-02
  - STAT-03
  - GAS-01
  - GAS-02
  - SURF-01
  - SURF-02
  - SURF-03
  - SURF-04
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04
  - AUDIT-05
  - REG-01
  - REG-02
  - REG-03
  - REG-04

# Audit anchors (per CONTEXT.md <domain> + RESEARCH.md Sources)
baseline: 5db8682bd7b811437f0c1cf47e832619d1478ac6
baseline_short: 5db8682b
baseline_signal: MILESTONE_V35_AT_HEAD_5db8682bd7b811437f0c1cf47e832619d1478ac6
v34_baseline: 6b63f6d4daf346a53a1d463790f637308ea8d555
v34_baseline_signal: MILESTONE_V34_AT_HEAD_6b63f6d4daf346a53a1d463790f637308ea8d555
v33_baseline: 4ce3703d740d3707c88a1af595618120a8168399
v33_baseline_signal: MILESTONE_V33_AT_HEAD_4ce3703d740d3707c88a1af595618120a8168399

# Closure signal (resolved at Task 19/20 per RESEARCH.md Pitfall 4)
milestone_closure_signal: "MILESTONE_V36_AT_HEAD_<sha>"
milestone_closure_signal_resolution: "<sha> = `git rev-parse HEAD` AFTER Task 19's §9 attestation commit lands; resolved by Task 20's atomic-update task (5 occurrences across §9c + ROADMAP + STATE + MILESTONES + 266-01-SUMMARY)."

# Refactor consumer inventory (verified via grep at HEAD `5db8682b` per RESEARCH.md Pitfall 3 + PATTERNS.md table)
entropy_step_callsites:
  - { line: 813,  func: "_rollTargetLevel",     description: "levelEntropy = EntropyLib.entropyStep(entropy);  // entry advance" }
  - { line: 817,  func: "_rollTargetLevel",     description: "farEntropy = EntropyLib.entropyStep(levelEntropy);  // 10% far branch" }
  - { line: 1548, func: "_resolveLootboxRoll",  description: "nextEntropy = EntropyLib.entropyStep(entropy);  // entry advance" }
  - { line: 1569, func: "_resolveLootboxRoll",  description: "nextEntropy = EntropyLib.entropyStep(nextEntropy);  // DGNRS path advance" }
  - { line: 1585, func: "_resolveLootboxRoll",  description: "nextEntropy = EntropyLib.entropyStep(nextEntropy);  // WWXRP path advance — DEAD per RESEARCH.md Open Question 3 + Assumption A3; remove entirely" }
  - { line: 1599, func: "_resolveLootboxRoll",  description: "nextEntropy = EntropyLib.entropyStep(nextEntropy);  // large-BURNIE path advance" }
  - { line: 1635, func: "_lootboxTicketCount",  description: "nextEntropy = EntropyLib.entropyStep(entropy);  // ticket variance entry" }

entropy_modulo_consumers:
  - { line: 814,  consumer: "_rollTargetLevel rangeRoll",                      modulus: 100,        bits: 16, slice: "uint16(seed)",                  bias_pct: 0.05 }
  - { line: 823,  consumer: "_rollTargetLevel near-level offset (90% branch)", modulus: 5,          bits: 8,  slice: "uint8(seed >> 16)",             bias_pct: 0.39 }
  - { line: 818,  consumer: "_rollTargetLevel far-level offset (10% branch)",  modulus: 46,         bits: 16, slice: "uint16(seed >> 24)",            bias_pct: 0.05 }
  - { line: 1551, consumer: "_resolveLootboxRoll path-roll",                   modulus: 20,         bits: 16, slice: "uint16(seed >> 40)",            bias_pct: 0.02 }
  - { line: 1680, consumer: "_lootboxDgnrsReward tierRoll (sub-call)",         modulus: 1000,       bits: 24, slice: "uint24(seed >> 56)",            bias_pct: 0.0024 }
  - { line: 1600, consumer: "_resolveLootboxRoll varianceRoll (large-BURNIE)", modulus: 20,         bits: 16, slice: "uint16(seed >> 80)",            bias_pct: 0.02 }
  - { line: 1636, consumer: "_lootboxTicketCount varianceRoll",                modulus: 10000,      bits: 24, slice: "uint24(seed >> 96)",            bias_pct: 0.045 }
  - { line: 1059, consumer: "_rollLootboxBoons roll (sub-call)",               modulus: 1000000,    bits: 32, slice: "uint32(seed >> 120)",           bias_pct: 0.022 }
  # Cumulative: 152 bits used / 256 available; ETH-amount-second branch uses Option A counter-tagged seed2 = EntropyLib.hash2(seed, 1)

# Surface preservation protected ranges (verified via grep per PATTERNS.md + RESEARCH.md Pitfall 6)
surf_01_protected_ranges:
  - { name: "EntropyLib.sol body L1-43 (SURF-01 — ENT-04 stable API)", file: "contracts/libraries/EntropyLib.sol", lo: 1, hi: 43 }
surf_02_protected_ranges:
  - { name: "_jackpotTicketRoll body L2186-2229 (SURF-02 — ENT-05 deferral)", file: "contracts/modules/DegenerusGameJackpotModule.sol", lo: 2186, hi: 2229 }
surf_03_protected_ranges:
  - { name: "MintModule L652 EntropyLib.hash2(entropy, rollSalt) (SURF-03)", file: "contracts/modules/DegenerusGameMintModule.sol", lo: 652, hi: 652 }
surf_04_protected_ranges:
  - { name: "L285 EntropyLib.hash2(rngWord, targetLvl)",       file: "contracts/modules/DegenerusGameJackpotModule.sol", lo: 285, hi: 285 }
  - { name: "L453 EntropyLib.hash2(randWord, lvl)",            file: "contracts/modules/DegenerusGameJackpotModule.sol", lo: 453, hi: 453 }
  - { name: "L532 EntropyLib.hash2(randWord, lvl)",            file: "contracts/modules/DegenerusGameJackpotModule.sol", lo: 532, hi: 532 }
  - { name: "L610 EntropyLib.hash2(randWord, lvl)",            file: "contracts/modules/DegenerusGameJackpotModule.sol", lo: 610, hi: 610 }
  - { name: "L612 EntropyLib.hash2(randWord, sourceLevel)",    file: "contracts/modules/DegenerusGameJackpotModule.sol", lo: 612, hi: 612 }
  - { name: "L886 EntropyLib.hash2(randWord, lvl) (arg pos)",  file: "contracts/modules/DegenerusGameJackpotModule.sol", lo: 886, hi: 886 }
  - { name: "L1176 EntropyLib.hash2(randWord, lvl)",           file: "contracts/modules/DegenerusGameJackpotModule.sol", lo: 1176, hi: 1176 }
  - { name: "L1873 entropy = EntropyLib.hash2(entropy, s)",    file: "contracts/modules/DegenerusGameJackpotModule.sol", lo: 1873, hi: 1873 }
  - { name: "L2192 BAF entropy = EntropyLib.entropyStep(entropy) (inside SURF-02 range)", file: "contracts/modules/DegenerusGameJackpotModule.sol", lo: 2192, hi: 2192 }

# §4 6-surface draft expected verdicts (per CONTEXT.md AUDIT-02 + RESEARCH.md Assumption A6)
adversarial_surfaces:
  a:
    name: "Bit-slice modulo-bias bound per draw within documented bound"
    expected_verdict: SAFE_BY_DESIGN
    rationale: "Per D-266-BIT-BUDGET-01: every `% small` slice has documented bias ≤ 1% (max 0.39% at `% 5` from 8 bits; min 0.0024% at `% 1000` from 24 bits). STAT-01 chi² empirical proof at `test/stat/LootboxEntropyDistribution.test.js` over 6 buckets. Backward-trace per `feedback_rng_backward_trace.md`: each consumer ultimately reads VRF-derived bits (via the entry-point `keccak256(abi.encode(rngWord, player, day, amount))` seed); rngWord unknown at input commitment time."
  b:
    name: "Seed-reuse cross-correlation across sub-rolls within same resolution"
    expected_verdict: SAFE_BY_DESIGN
    rationale: "Bit-budget table at §3a documents disjoint bit ranges per sub-roll within a single seed chunk (152 bits across 8 consumers; rest reserved). Two consumers reading the same bit range from the same seed is structurally impossible per the inline NatSpec slice-allocation table in `_resolveLootboxCommon`. ETH-amount-second branch uses counter-tagged `seed2 = EntropyLib.hash2(seed, 1)` (Option A from RESEARCH.md Pitfall 2) — distinct from `seed` chunk."
  c:
    name: "hash2(seed, N) chunk indexing collision-free across consumers"
    expected_verdict: SAFE_BY_DESIGN
    rationale: "Counter convention: seed (chunk 0, the entry-point keccak result) + seed2 = EntropyLib.hash2(seed, 1) (chunk 1, only invoked on ETH-amount-second branch). No third chunk in scope. AUDIT-02 surface (c) is a one-liner — counter `1` distinct from `0`; no other consumer derives a hash2(seed, N) chunk."
  d:
    name: "Gas-griefing delta bounded"
    expected_verdict: SAFE_BY_DESIGN
    rationale: "Theoretical worst case derived in `test/gas/LootboxOpenGas.test.js` header (per `feedback_gas_worst_case.md`): per-open delta +20 to +80 g (single-amount path) or +100 to +160 g (ETH-amount-second branch). REF-CAPTURE protocol pins measured value; `PER_OPEN_GAS_DELTA_BOUND = 300` asserted. AdvanceGame ≥ 1.99× margin preserved per `test/gas/AdvanceGameGas.test.js` v36.0 describe block. Decimator settlement is the only advanceGame-resident lootbox-resolution caller; envelope ±2K per GAS-02."
  e:
    name: "Byte-identity preserved on the deferred BAF jackpot xorshift path (ENT-05 verification)"
    expected_verdict: SAFE_BY_STRUCTURAL_CLOSURE
    rationale: "SURF-02 grep-proof asserts `_jackpotTicketRoll` body L2186-2229 byte-identical between baseline `5db8682b` and v36.0 closure HEAD via per-line modified-set walk. Same xorshift pattern remains in BAF jackpot per ENT-05 deferral. KNOWN-ISSUES.md EntropyLib XOR-shift entry rephrased to BAF-only scope per AUDIT-05."
  f:
    name: "Commitment-window check (player-controllable state between VRF request and fulfillment)"
    expected_verdict: SAFE_BY_DESIGN
    rationale: "Per `feedback_rng_commitment_window.md`: trace what player-controllable state can change between VRF request and fulfillment. Lootbox VRF index advances past current fulfillment index per the existing index-advance isolation pattern (KNOWN-ISSUES.md `Lootbox RNG uses index advance isolation` entry). The structured-input `(rngWord, player, day, amount)` is committed at request time; player cannot bias rngWord post-commit. Bit-slice consumers read deterministically from the seed; no player-controllable mutation of slice values between commit and fulfillment."

# §5 REG-01 single-PASS-row (v35.0 carry-forward)
reg_01_candidates:
  - row_id: REG-v35.0-PPL
    source: "v35.0 closure signal `MILESTONE_V35_AT_HEAD_5db8682bd7b811437f0c1cf47e832619d1478ac6` carry-forward. v35 audit deliverable `audit/FINDINGS-v35.0.md` 6 of 6 §4 surfaces SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE at HEAD `5db8682b`; `_awardDailyCoinToTraitWinners` per-pull-level resample helper + 2 callsite rewires + COIN_LEVEL_TAG constant + dead-code removal all closed."
    delta_sha: "5db8682b..<v36-close-sha> (1 v36 contract commit `feat(266): lootbox-path entropy refactor [ENT-01..06]` + N v36 test commits — only contracts/modules/DegenerusGameLootboxModule.sol modified; no other contract or library files touched)"
    subject_at_head: "v36.0 SURF-04 grep-proof asserts JackpotModule 9 callsites (L285/L453/L532/L610/L612/L886/L1176/L1873/L2192) byte-identical between baseline `5db8682b` and HEAD `<v36-close-sha>` — including `_awardDailyCoinToTraitWinners` body, `COIN_LEVEL_TAG` constant, and Phase 263 helper at JackpotModule. Per-line modified-set walk via `git diff 5db8682b..HEAD -- contracts/modules/DegenerusGameJackpotModule.sol` returns ZERO `-` deletions inside the v35-protected ranges."
    re_verify_evidence: "v36 modifies ONLY `contracts/modules/DegenerusGameLootboxModule.sol`. JackpotModule + EntropyLib + GameStorage + DegenerusTraitUtils + GNRUS UNTOUCHED. v35 §4 6-surface verdicts carry forward unchanged at v36 HEAD."
    verdict: PASS

# §5 REG-02 single-PASS-row (v34.0 carry-forward)
reg_02_candidates:
  - row_id: REG-v34.0-TRAIT-SOLO
    source: "v34.0 closure signal `MILESTONE_V34_AT_HEAD_6b63f6d4daf346a53a1d463790f637308ea8d555` carry-forward. v34 audit deliverable 6 of 6 §4 surfaces SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE at HEAD `6b63f6d4`."
    delta_sha: "6b63f6d4..<v36-close-sha> (1 v35 contract commit `cf564816` + 6 v35 test commits + 1 v36 contract commit + N v36 test commits since v34 baseline)"
    subject_at_head: "TraitUtils + `_pickSoloQuadrant` + JackpotBucketLib + EntropyLib byte-identical at v36 HEAD per `git diff 6b63f6d4..HEAD -- contracts/DegenerusTraitUtils.sol contracts/libraries/JackpotBucketLib.sol contracts/libraries/EntropyLib.sol` (expected: empty)."
    re_verify_evidence: "v36 modifies LootboxModule only. v34 §4 6-surface trait/solo verdicts orthogonal to lootbox-entropy refactor; carry forward unchanged."
    verdict: PASS

# §6b KI envelope re-verifications (per AUDIT-05 + REG-03)
ki_envelope_re_verifications:
  - exc: EXC-01
    description: "Non-VRF entropy for affiliate winner roll"
    carrier: "n/a (NEGATIVE-scope at v36; lootbox-entropy refactor does not consume affiliate-roll RNG)"
    verdict: "NEGATIVE-scope at v36"
    evidence: "v36 modifies only `contracts/modules/DegenerusGameLootboxModule.sol`; affiliate-roll path in MintModule untouched (verified via SURF-03 grep-proof on L652 and zero v36 commits targeting MintModule)."
  - exc: EXC-02
    description: "Gameover prevrandao fallback"
    carrier: "n/a (NEGATIVE-scope at v36; AdvanceModule untouched)"
    verdict: "NEGATIVE-scope at v36"
    evidence: "AdvanceModule prevrandao site untouched; v34/v35 carry-forward holds. No v36 commit modifies DegenerusGameAdvanceModule.sol."
  - exc: EXC-03
    description: "Gameover RNG substitution for mid-cycle write-buffer tickets (F-29-04 class)"
    carrier: "n/a (NEGATIVE-scope at v36; AdvanceModule + swap path untouched)"
    verdict: "NEGATIVE-scope at v36"
    evidence: "_swapAndFreeze / _swapTicketSlot / _gameOverEntropy sites untouched; v36 lootbox-entropy refactor does not interact with mid-cycle write-buffer ticket substitution."
  - exc: EXC-04
    description: "EntropyLib XOR-shift PRNG — scope NARROWS at v36 (lootbox-path consumption removed; remaining consumer is BAF jackpot only per ENT-05 deferral)"
    carrier: "DegenerusGameJackpotModule.sol _jackpotTicketRoll (L2186-2229) — single entropyStep at L2192; SURF-02 byte-identical at v36 HEAD"
    verdict: "RE_VERIFIED — scope NARROWS to BAF-only; KNOWN-ISSUES.md entry rephrased per AUDIT-05"
    evidence: "Backward trace per `feedback_rng_backward_trace.md`: lootbox path now consumes inline bit-sliced reads from `keccak256(abi.encode(rngWord, player, day, amount))` seed (NOT XOR-shift); STAT-01 chi² empirical proof at `test/stat/LootboxEntropyDistribution.test.js` (6 buckets — `% 100` Z<1.645 df=99; `% 5` chi²<9.488 df=4; `% 46` Z<1.645 df=45; `% 20` path Z<1.645 df=19; `% 20` variance Z<1.645 df=19; `% 10000` Z<1.645 df=9999 over 100K samples). BAF jackpot consumer remains; same security argument carries (single per-ticket step + small-modulus arithmetic + VRF-derived seed at upstream call boundary). Future-phase candidate: BAF refactor following the same bit-sliced keccak pattern."

ki_verdict_summary: "0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_REPHRASED (1 entry rephrased to BAF-only scope under Design Decisions per AUDIT-05)"

# Goal-backward must-haves (per quality_gate)
must_haves:
  truths:
    - "v36.0 phase goal: `EntropyLib.entropyStep` (XOR-shift PRNG) consumption removed from lootbox path; replaced with inline bit-sliced reads from a single `keccak256(abi.encode(rngWord, player, day, amount))` seed per resolution. 7 entropyStep callsites in `DegenerusGameLootboxModule.sol` at L813/L817/L1548/L1569/L1585/L1599/L1635 all replaced; L1585 dead WWXRP advance removed entirely per `feedback_no_dead_guards.md` + RESEARCH.md Open Question 3 + Assumption A3. (Ref: D-266-CONSUMER-LIST-01 + D-266-API-01)"
    - "8 entropy modulo consumers (7 named callsite consumers + 2 sub-call consumers `_lootboxDgnrsReward` L1680 + `_rollLootboxBoons` L1059) consume disjoint bit ranges from the per-resolution seed per the bit-budget table (152 bits cumulative / 256 available). Per-consumer NatSpec inline bit-budget block documents which bits each `% small` consumes per ENT-06. (Ref: D-266-BIT-BUDGET-01 + RESEARCH.md Pitfall 3 + Assumption A2)"
    - "EntropyLib.sol body byte-identical at v36 HEAD vs baseline `5db8682b` per SURF-01 (`git diff 5db8682b..HEAD -- contracts/libraries/EntropyLib.sol` returns empty); ENT-04 stable-API constraint honored. BAF jackpot `_jackpotTicketRoll` body L2186-2229 byte-identical per SURF-02 (ENT-05 deferral verification). MintModule L652 byte-identical per SURF-03. JackpotModule 9 callsites at L285/L453/L532/L610/L612/L886/L1176/L1873/L2192 byte-identical per SURF-04. (Ref: ENT-04 + ENT-05 + SURF-01..04)"
    - "STAT-01..03 chi² uniformity proven over 6 sub-roll buckets (`% 100` Z<1.645 df=99 N=10K; `% 5` chi²<9.488 df=4 N=5K; `% 46` Z<1.645 df=45 N=10K; `% 20` path Z<1.645 df=19 N=10K; `% 20` variance Z<1.645 df=19 N=5K; `% 10000` Z<1.645 df=9999 N=100K). Reuse Phase 261/264 chi² infrastructure (`makeRng` + `CHI2_CRIT_05` + `wilsonHilfertyZ`) verbatim re-declared in test header per STAT-03. (Ref: D-266-API-01 chi² infra discipline + RESEARCH.md Pitfall 1 + Assumption A1)"
    - "GAS-01 per-open delta within ±300 g for `openLootBox` / `openBurnieLootBox` / `resolveLootboxDirect` (theoretical worst case +20 to +160 g derived in test header per `feedback_gas_worst_case.md`; REF-CAPTURE protocol pins measured value; `PER_OPEN_GAS_DELTA_BOUND = 300` asserted). GAS-02 advanceGame envelope unchanged within ±2K; ≥ 1.99× margin preserved per AdvanceGameGas v36.0 describe block."
    - "audit/FINDINGS-v36.0.md exists at phase close, READ-only (chmod a-w + `status: FINAL — READ-ONLY` + `read_only: true` in YAML frontmatter), with all 9 sections populated. §3d AUDIT-01 delta-surface table enumerates all changed declarations in `DegenerusGameLootboxModule.sol` vs baseline `5db8682b` with hunk-level evidence + classification {NEW: per-resolution seed derivation in `_resolveLootboxCommon`; MODIFIED_LOGIC: `_rollTargetLevel` + `_resolveLootboxRoll` + `_lootboxTicketCount` + `_lootboxDgnrsReward` + `_rollLootboxBoons` signatures + bodies; REFACTOR_ONLY: 4 entry-point callers (`openLootBox` + `openBurnieLootBox` + `resolveLootboxDirect` + `resolveRedemptionLootbox`) seed plumbing; DELETED: L1585 WWXRP-path dead entropyStep advance + 5 other entropyStep calls + nextEntropy return contract}. (Ref: AUDIT-01 + RESEARCH.md Architectural Responsibility Map)"
    - "§4 6-surface adversarial sweep verdicts every refactor surface SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE: (a) bit-slice modulo-bias bound; (b) seed-reuse cross-correlation; (c) hash2(seed, N) chunk-collision-free; (d) gas-griefing delta bounded; (e) BAF byte-identity (ENT-05 verification); (f) commitment-window check. Default zero F-36-NN finding blocks per RESEARCH.md Assumption A6. (Ref: AUDIT-02 + D-266-ADVERSARIAL-02)"
    - "§3e AUDIT-03 conservation re-proof: (1) lootbox payouts (BURNIE / DGNRS / WWXRP / tickets) preserved within statistical uniformity vs xorshift baseline per STAT-01 chi² empirical evidence; (2) no new mint sites — only pre-existing BURNIE / DGNRS / WWXRP transfer paths exercised; (3) solvency invariant `claimablePool ≤ ETH balance + stETH balance` PRESERVED (no ETH/stETH balance interactions in the refactored code). §3d Part C AUDIT-04 zero-new-state attestation: zero new storage slots; zero new public/external mutation entry points; zero new admin functions; zero new modifiers; EntropyLib API stable (ENT-04). (Ref: AUDIT-03 + AUDIT-04)"
    - "§5 regression appendix: §5a REG-01 single-PASS-row v35.0 closure signal `MILESTONE_V35_AT_HEAD_5db8682b` non-widening (JackpotModule + per-pull-level resample helper byte-identical); §5b REG-02 single-PASS-row v34.0 closure signal `MILESTONE_V34_AT_HEAD_6b63f6d4` non-widening (TraitUtils + JackpotBucketLib + EntropyLib + GameStorage byte-identical); §5c REG-04 prior-finding spot-check across audit/FINDINGS-v25..v35 for v36-touched function set (`EntropyLib.entropyStep`, `_rollTargetLevel`, `_resolveLootboxRoll`, `_lootboxTicketCount`, `openLootBox`, `openBurnieLootBox`, `resolveLootboxDirect`, `resolveRedemptionLootbox`) — expected ALL PASS. (Ref: REG-01 + REG-02 + REG-04)"
    - "§6 KI gating walk: §6a Non-Promotion Ledger (default zero rows); §6b 4-row KI envelope re-verifications (EXC-01..03 NEGATIVE-scope at v36; EXC-04 RE_VERIFIED with scope NARROWS to BAF-only — backward-trace cite to `feedback_rng_backward_trace.md` + chi² empirical cross-cite to STAT-01); §6c verdict summary `0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_REPHRASED (1 entry rephrased to BAF-only scope under Design Decisions)`. KNOWN-ISSUES.md modified by 1 entry per AUDIT-05 (EntropyLib XOR-shift entry rephrased to BAF-jackpot-only scope per CONTEXT.md `<specifics>` + PATTERNS.md prose). (Ref: REG-03 + AUDIT-05)"
    - "§9c emits closure signal `MILESTONE_V36_AT_HEAD_<sha>` with concrete SHA (HEAD at Task 19/20 commit time per RESEARCH.md Pitfall 4 atomic-update pattern), present verbatim in 5 locations: `audit/FINDINGS-v36.0.md` frontmatter `closure_signal:` field + §2 Closure Verdict Summary + §9c trailing line + `.planning/MILESTONES.md` v36.0 row + `.planning/ROADMAP.md` Phase 266 line + `266-01-SUMMARY.md` frontmatter. (Ref: D-266-CLOSURE-01)"
    - "§9.NN commit-readiness register has TWO subsections per D-266-CLOSURE-02: §9.NN.i USER-APPROVED contracts (cites Wave 1 single batched commit `feat(266): lootbox-path entropy refactor [ENT-01..06]`) + §9.NN.ii USER-APPROVED tests (cites all Wave 2 test/chore commits via `git log --oneline 5db8682b..HEAD -- test/ package.json`) + §9.NN.iii AGENT-COMMITTED audit artifacts (Tasks 11-21 atomic-commit chain). NO §9.NN.iv awaiting-approval subsection."
    - "D-266-ADVERSARIAL-01..03 three-step pattern executed: Step 1 (Task 13) plan author writes full §4 inline 6-surface draft (a-f); Step 2 (Task 14) `/contract-auditor` + `/zero-day-hunter` spawn IN PARALLEL in single message AFTER §4 draft (NOT `/economic-analyst`, NOT `/degen-skeptic`); Step 3 (still Task 14) disagreement disposition surfaces any skill flag or 7th-surface NEW_VECTOR to user before READ-only flip per `feedback_wait_for_approval.md`. (Ref: D-266-ADVERSARIAL-01..03 + RESEARCH.md Pitfall 5)"
    - "`.planning/phases/266-lootbox-entropy-refactor/266-01-ADVERSARIAL-LOG.md` exists with `## /contract-auditor` and `## /zero-day-hunter` H2 headers populated by Task 14 (or `STATUS: SPAWN_FAILED` with executor-manual fallback per Phase 257 Task 7 / Phase 265 Task 7 retry-semantics)."
    - "§8 Forward-Cite Closure emits zero forward-cites: grep recipe `! grep -rE 'Phase (267|268|269|270|271)|v37\\.0|v38\\.0' audit/FINDINGS-v36.0.md .planning/phases/266-*/` returns no matches. Phase 266 = terminal v36.0 phase per CONTEXT.md `<domain>` single-phase patch shape; D-266-FCITE carry of D-265-FCITE-01."
    - "USER-APPROVED batched gates honored: Wave 1 contract diff presented at Task 5 — explicit `approved` string awaited before commit; Wave 2 test diff presented at Task 10 — explicit `approved` string awaited before commit; Final user-review gate at Task 21 — full audit deliverable + KNOWN-ISSUES.md + ROADMAP/STATE/MILESTONES diff presented; explicit `approved` string awaited before READ-only flip + closure-attestation final commit. Per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md`."

  artifacts:
    - path: contracts/modules/DegenerusGameLootboxModule.sol
      provides: "MODIFIED in single batched commit at Wave 1 close. 7 entropyStep callsites replaced with inline bit-sliced reads from per-resolution `keccak256(abi.encode(rngWord, player, day, amount))` seed (preserve existing keccak per RESEARCH.md Open Question 2 — minimize SURF diff noise). L1585 dead WWXRP advance removed entirely per RESEARCH.md Open Question 3 + `feedback_no_dead_guards.md`. Sub-call entropy consumers `_lootboxDgnrsReward` (L1680, `% 1000` 24-bit slice) + `_rollLootboxBoons` (L1059, `% BOON_PPM_SCALE` 32-bit slice) updated per RESEARCH.md Pitfall 3 + Assumption A2. ETH-amount-second branch uses Option A counter-tagged `seed2 = EntropyLib.hash2(seed, 1)` per RESEARCH.md Pitfall 2 + Assumption A4. Per-consumer NatSpec inline bit-budget block per ENT-06."
      contains: "uint16(seed)"
      contains_alt: "uint24(seed >>"
    - path: test/stat/LootboxEntropyDistribution.test.js
      provides: "NEW. STAT-01..03 chi² uniformity over 6 sub-roll buckets per REGIMES table (per Pitfall 1 sample-budget calibration: `% 100` N=10K + `% 5` N=5K + `% 46` N=10K + `% 20` path N=10K + `% 20` variance N=5K + `% 10000` N=100K). Verbatim re-declaration of `makeRng` + `CHI2_CRIT_05` + `wilsonHilfertyZ` from `test/stat/PerPullLevelDistribution.test.js` L78-102 (STAT-03 reuse-existing-tooling discipline). 6 describe blocks + 1 STAT-04-equivalent infrastructure-reuse sanity describe block."
      min_lines: 200
      contains: "wilsonHilfertyZ"
    - path: test/gas/LootboxOpenGas.test.js
      provides: "NEW. GAS-01 per-open ±300 g assertion for `openLootBox` / `openBurnieLootBox` / `resolveLootboxDirect`. Theoretical worst-case derivation header (per `feedback_gas_worst_case.md`) — opcode-by-opcode walk: keccak ~80 g + per-consumer inline shifts ~70-90 g + per-consumer modulo ~56 g - 5 saved entropyStep calls × ~20-30 g - dead L1585 advance ~25 g + Option A seed2 keccak ~80 g (ETH-amount-second branch only). REF-CAPTURE protocol with 3 placeholder constants (`OPEN_LOOTBOX_GAS_REF`, `OPEN_BURNIE_LOOTBOX_GAS_REF`, `RESOLVE_LOOTBOX_DIRECT_GAS_REF`) pinned post-first-run. `PER_OPEN_GAS_DELTA_BOUND = 300` + `ENTRY_POINT_DELTA_TOLERANCE = 2000`."
      min_lines: 100
      contains: "PER_OPEN_GAS_DELTA_BOUND = 300"
    - path: test/gas/AdvanceGameGas.test.js
      provides: "EXTENDED with v36.0 1.99× margin describe block (alongside existing Phase 264 v35.0 block). GAS-02 advanceGame envelope unchanged within ±2K; Decimator settlement is the one advanceGame-resident lootbox-resolution caller. ≥ 1.99× margin from v34/v35 SURF-05 baseline preserved."
      contains: "v36.0"
    - path: test/stat/SurfaceRegression.test.js
      provides: "EXTENDED with v36.0 SURF-01..04 describe block. PROTECTED_RANGES arrays per SURF-01 (EntropyLib.sol L1-43) + SURF-02 (JackpotModule L2186-2229) + SURF-03 (MintModule L652) + SURF-04 (JackpotModule 9 lines L285/L453/L532/L610/L612/L886/L1176/L1873/L2192). Per-line modified-set walk vs `git diff 5db8682b..HEAD` returns ZERO `-` deletions inside any protected range. D-IMPL-11 soft-skip on unreachable baseline + fail-loud-on-empty-diff guard."
      contains: "5db8682bd7b811437f0c1cf47e832619d1478ac6"
    - path: package.json
      provides: "MODIFIED. Wire NEW test files (`test/stat/LootboxEntropyDistribution.test.js` + `test/gas/LootboxOpenGas.test.js`) into `npm run test:stat` / `npm run test:gas` scripts (mirror `833b341d  chore(264-02): wire Phase 264 test files into npm scripts` pattern)."
    - path: audit/FINDINGS-v36.0.md
      provides: "Single-file 9-section v36.0 milestone-closure deliverable mirroring v33/v34/v35 §1-§9 shape. §1 Frontmatter (YAML with closure_signal + status FINAL READ-ONLY + supersedes:none) + §2 Executive Summary (Closure Verdict + Severity Counts + D-08 + D-09 + Forward-Cite + Attestation Anchor) + §3 Per-Phase Sections (§3a Phase 266 single-phase + §3d AUDIT-01 Delta-Surface Table + Part C AUDIT-04 Zero-New-State Attestation + §3e AUDIT-03 Conservation Re-Proof) + §4 F-36-NN Finding Blocks (default zero blocks per D-265-FIND-01 carry; 6-surface row table covering (a)..(f) per AUDIT-02) + §5 Regression Appendix (§5a REG-01 v35.0 + §5b REG-02 v34.0 + §5c REG-04 prior-finding sweep) + §6 KI Gating Walk (§6a Non-Promotion Ledger zero-row + §6b 4-row KI Envelope Re-Verifications EXC-01..03 NEGATIVE-scope + EXC-04 RE_VERIFIED scope NARROWS BAF-only + §6c Verdict Summary `0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_REPHRASED`) + §7 Prior-Artifact Cross-Cites + §8 Forward-Cite Closure (terminal-phase zero-emission verification) + §9 Milestone Closure Attestation (§9a Verdict Distribution + §9b Attestation block + §9c Closure Signal `MILESTONE_V36_AT_HEAD_<sha>` + §9.NN TWO-subsection commit-readiness register: USER-APPROVED contracts + USER-APPROVED tests + AGENT-COMMITTED audit artifacts; NO awaiting-approval subsection). FINAL READ-only at Task 21."
      min_lines: 500
      contains: "MILESTONE_V36_AT_HEAD_"
      contains_alt: "## 4. F-36-NN Finding Blocks"
    - path: KNOWN-ISSUES.md
      provides: "MODIFIED. ONE entry rephrased — `**EntropyLib XOR-shift PRNG for lootbox outcome rolls.**` (current L31) → `**EntropyLib XOR-shift PRNG for BAF jackpot ticket rolls.**` per CONTEXT.md `<specifics>` + PATTERNS.md proposed prose. Scope NARROWS from lootbox-path consumer (removed at v36.0) to BAF-jackpot-only consumer (remaining per ENT-05 deferral). Per `feedback_no_history_in_comments.md` — describes what IS at v36.0 close, with one forward-disclosure sentence noting lootbox-path consumption was removed at v36 + future-phase candidate."
      contains: "BAF jackpot ticket rolls"
    - path: .planning/phases/266-lootbox-entropy-refactor/266-01-ADVERSARIAL-LOG.md
      provides: "Adversarial validation log capturing `/contract-auditor` + `/zero-day-hunter` outputs from Task 14 parallel-spawn step. Two H2 sections (`## /contract-auditor` and `## /zero-day-hunter`) populated with each skill's red-team output against the finished §4 6-surface inline draft. Task 14 disposition note(s) appended below skill outputs documenting any disagreement + user disposition. Mirrors v35 `265-01-ADVERSARIAL-LOG.md` format precedent."
      min_lines: 50
    - path: .planning/phases/266-lootbox-entropy-refactor/266-01-SUMMARY.md
      provides: "Phase-closure summary mirroring `265-01-SUMMARY.md` format (per-task atomic-commit log + per-REQ tally for 24 v36.0 requirements + cross-phase cross-cite density + project-feedback-rules-honored table + closure signal `MILESTONE_V36_AT_HEAD_<sha>`)."
      min_lines: 80

  key_links:
    - from: contracts/modules/DegenerusGameLootboxModule.sol _resolveLootboxCommon entry point (L554/L628/L673/L708)
      to: "_rollTargetLevel + _resolveLootboxRoll + _lootboxTicketCount + _lootboxDgnrsReward + _rollLootboxBoons (downstream sub-roll consumers)"
      via: "single-keccak-per-resolution seed plumbing — `uint256 seed = uint256(keccak256(abi.encode(rngWord, player, day, amount)))` derived once at entry; threaded through downstream functions via parameter"
      pattern: "uint256 seed = uint256\\(keccak256\\(abi\\.encode\\("
    - from: audit/FINDINGS-v36.0.md §3d AUDIT-01 delta-surface table
      to: "contracts/modules/DegenerusGameLootboxModule.sol (HEAD <v36-close-sha>) + git diff 5db8682b..HEAD"
      via: "every changed declaration classified {NEW: per-resolution seed derivation; MODIFIED_LOGIC: 5 refactored functions; REFACTOR_ONLY: 4 entry-point callers seed plumbing; DELETED: L1585 dead WWXRP advance + 5 other entropyStep calls + nextEntropy return contract} with hunk-level evidence (file:line cite per row)"
      pattern: "DegenerusGameLootboxModule\\.sol|_rollTargetLevel|_resolveLootboxRoll|_lootboxTicketCount|NEW|MODIFIED_LOGIC|REFACTOR_ONLY|DELETED"
    - from: audit/FINDINGS-v36.0.md §4 6-surface row table
      to: "contracts/modules/DegenerusGameLootboxModule.sol refactored bodies + test/stat/LootboxEntropyDistribution.test.js (STAT-01 chi² evidence) + test/stat/SurfaceRegression.test.js (SURF-01..04 byte-identity) + test/gas/LootboxOpenGas.test.js (GAS-01 envelope)"
      via: "6 rows (a..f) with verdict {SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE} + grep recipe + line cite + prose justification per row + STAT-01 chi² cross-cite + RNG backward-trace cite (`feedback_rng_backward_trace.md`) + commitment-window cite (`feedback_rng_commitment_window.md`)"
      pattern: "Surface \\([a-f]\\)|SAFE_BY_DESIGN|SAFE_BY_STRUCTURAL_CLOSURE"
    - from: audit/FINDINGS-v36.0.md §6b KI envelope re-verifications row 4 (EXC-04 NARROWS)
      to: "KNOWN-ISSUES.md L31 (rephrased entry) + test/stat/LootboxEntropyDistribution.test.js (STAT-01 chi² evidence)"
      via: "backward-trace cite per `feedback_rng_backward_trace.md` + chi² empirical cross-cite (6 buckets) + scope-narrowing prose (lootbox-path consumption removed at v36; BAF-only remains per ENT-05)"
      pattern: "EXC-04|NARROWS|BAF jackpot|RE_VERIFIED"
    - from: audit/FINDINGS-v36.0.md §9c
      to: '/gsd-complete-milestone for v36.0'
      via: "closure signal `MILESTONE_V36_AT_HEAD_<sha>` triggers milestone-completion handler per D-266-CLOSURE-01"
      pattern: "MILESTONE_V36_AT_HEAD_"
    - from: audit/FINDINGS-v36.0.md §9.NN commit-readiness register
      to: "1 USER-APPROVED contract commit (Wave 1) + N USER-APPROVED test/chore commits (Wave 2) + Phase 266 plan-close commits Tasks 11-21 (AGENT-COMMITTED audit artifacts)"
      via: "§9.NN.i cites the v36 contract-tree commit; §9.NN.ii cites Wave 2 test commits via `git log --oneline 5db8682b..HEAD -- test/ package.json`; §9.NN.iii cites Phase 266 plan-close commits"
      pattern: "feat\\(266\\)|test\\(266\\)|chore\\(266\\)|audit\\(266\\)|docs\\(266\\)"

tags:
  - audit
  - findings-consolidation
  - milestone-closure
  - terminal-phase
  - v36-deliverable
  - lootbox-entropy-refactor
  - bit-sliced-keccak
  - xorshift-removal
  - ENT-01
  - ENT-02
  - ENT-03
  - ENT-04
  - ENT-05
  - ENT-06
  - STAT-01
  - STAT-02
  - STAT-03
  - GAS-01
  - GAS-02
  - SURF-01
  - SURF-02
  - SURF-03
  - SURF-04
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04
  - AUDIT-05
  - REG-01
  - REG-02
  - REG-03
  - REG-04
  - adversarial-validation
  - contract-auditor-spawn
  - zero-day-hunter-spawn
---

<objective>

Produce the v36.0 milestone-closure deliverable for the Lootbox-Path Entropy Refactor: (1) refactor 7 `EntropyLib.entropyStep` callsites in `contracts/modules/DegenerusGameLootboxModule.sol` (L813/L817/L1548/L1569/L1585/L1599/L1635) plus 2 sub-call entropy consumers (`_lootboxDgnrsReward` L1680, `_rollLootboxBoons` L1059) to inline bit-sliced reads from a single per-resolution `keccak256(abi.encode(rngWord, player, day, amount))` seed; (2) prove statistical uniformity (STAT-01..03 chi² over 6 sub-roll buckets), gas envelope (GAS-01 ±300 g per open + GAS-02 advanceGame ±2K + 1.99× margin), and cross-surface byte-identity (SURF-01..04); (3) author single-canonical 9-section `audit/FINDINGS-v36.0.md` deliverable with adversarial sweep over the lootbox-entropy refactor surfaces verdicting all 6 surfaces SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE; (4) re-verify v35.0 + v34.0 closure signals non-widening (REG-01 + REG-02), KI envelopes EXC-01..03 NEGATIVE-scope + EXC-04 RE_VERIFIED scope NARROWS to BAF-only (REG-03), prior-finding spot-check across audit/FINDINGS-v25..v35 (REG-04); (5) rephrase KNOWN-ISSUES.md `EntropyLib XOR-shift PRNG for lootbox outcome rolls` entry to BAF-jackpot-only scope per AUDIT-05; (6) emit closure signal `MILESTONE_V36_AT_HEAD_<sha>` in §9c + 4 other locations.

**Per CONTEXT.md locked decisions:**

- D-266-API-01: NO EntropyLib helper additions — inline shifts only (`uint16(seed) % 100` etc.). EntropyLib.sol body byte-identical at v36 close.
- D-266-SEED-01: single-keccak-per-resolution seed at `_resolveLootboxCommon` entry; threaded through sub-rolls; ETH-amount-second branch uses Option A counter-tagged `seed2 = EntropyLib.hash2(seed, 1)`.
- D-266-BIT-BUDGET-01: per-consumer ≤ 1% modulo bias (max 0.39% at `% 5` from 8 bits).
- D-266-CONSUMER-LIST-01: 7 entropyStep callsites + 2 sub-call entropy consumers (planner enumerated per RESEARCH.md Pitfall 3 + Assumption A2).
- D-266-SCOPE-OUT-01: BAF jackpot `_jackpotTicketRoll` (JackpotModule:2186-2229) deferred per ENT-05; SURF-02 byte-identity verifies discipline.
- D-266-SCOPE-OUT-04: KNOWN-ISSUES.md "Lootbox RNG uses index advance" entry — DEFERRED per RESEARCH.md Open Question 4 (single-entry modification discipline at v36 close per AUDIT-05).
- D-266-ADVERSARIAL-01..03: `/contract-auditor` + `/zero-day-hunter` parallel spawn AFTER full §4 draft; NOT `/economic-analyst` or `/degen-skeptic`; disagreement disposition surfaces to user inline.
- D-266-FILES-01: single canonical `audit/FINDINGS-v36.0.md`; no per-AUDIT-NN working files.
- D-266-CLOSURE-01..02: closure signal SHA = `git rev-parse HEAD` at audit-pass-close commit; TWO-subsection §9.NN format (NO awaiting-approval subsection).
- D-266-PLAN-01: single multi-task plan (this file).
- D-266-APPROVAL-01..02: USER-APPROVED batched gates at Wave 1 close (contract diff) + Wave 2 close (test diff) + Wave 5 close (final review).

**Output:** Single batched contract commit (Wave 1) + N batched test commits (Wave 2) + 9-section `audit/FINDINGS-v36.0.md` deliverable + `266-01-ADVERSARIAL-LOG.md` + `266-01-SUMMARY.md` + KNOWN-ISSUES.md L31 rephrased entry + ROADMAP/STATE/MILESTONES flips.

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/266-lootbox-entropy-refactor/266-CONTEXT.md
@.planning/phases/266-lootbox-entropy-refactor/266-RESEARCH.md
@.planning/phases/266-lootbox-entropy-refactor/266-PATTERNS.md
@audit/FINDINGS-v35.0.md
@audit/FINDINGS-v34.0.md
@.planning/phases/265-delta-audit-findings-consolidation/265-01-PLAN.md
@.planning/phases/265-delta-audit-findings-consolidation/265-01-ADVERSARIAL-LOG.md
@.planning/phases/265-delta-audit-findings-consolidation/265-01-SUMMARY.md
@contracts/modules/DegenerusGameLootboxModule.sol
@contracts/libraries/EntropyLib.sol
@contracts/modules/DegenerusGameJackpotModule.sol
@contracts/modules/DegenerusGameMintModule.sol
@KNOWN-ISSUES.md
@test/stat/PerPullLevelDistribution.test.js
@test/stat/SurfaceRegression.test.js
@test/gas/Phase264GasRegression.test.js
@test/gas/AdvanceGameGas.test.js
@./CLAUDE.md

<!-- Project feedback rules — applicable in Phase 266 per CONTEXT.md citations -->
@$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_contract_commits.md
@$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_never_preapprove_contracts.md
@$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_batch_contract_approval.md
@$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_manual_review_before_push.md
@$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_wait_for_approval.md
@$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md
@$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_dead_guards.md
@$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_backward_trace.md
@$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_commitment_window.md
@$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_gas_worst_case.md
@$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_contract_locations.md

<canonical_grep_recipes>
<!-- Bake these into task <action> blocks. Executors copy verbatim — do NOT re-derive. -->

ENT-01..03 entropyStep removed from lootbox path:
  grep -nE "EntropyLib\.entropyStep" contracts/modules/DegenerusGameLootboxModule.sol
  # Expected: ZERO hits at v36 close.

ENT-04 EntropyLib API stable (SURF-01):
  git diff 5db8682bd7b811437f0c1cf47e832619d1478ac6..HEAD -- contracts/libraries/EntropyLib.sol
  # Expected: empty output.

ENT-05 BAF jackpot byte-identity (SURF-02):
  git diff 5db8682bd7b811437f0c1cf47e832619d1478ac6..HEAD -- contracts/modules/DegenerusGameJackpotModule.sol | grep -E "^[+-].*entropyStep|^[+-].*_jackpotTicketRoll"
  # Expected: ZERO `+` or `-` lines (BAF body untouched).

ENT-06 NatSpec bit-budget present:
  grep -cE "bits\[[0-9]+\.\.[0-9]+\]" contracts/modules/DegenerusGameLootboxModule.sol
  # Expected: ≥ 8 hits (one per consumer in the cumulative bit-budget table).

SURF-03 MintModule L652 byte-identity:
  git diff 5db8682bd7b811437f0c1cf47e832619d1478ac6..HEAD -- contracts/modules/DegenerusGameMintModule.sol
  # Expected: empty output.

SURF-04 JackpotModule 9 callsites byte-identity (per PATTERNS.md inventory):
  for L in 285 453 532 610 612 886 1176 1873 2192; do
    git diff 5db8682bd7b811437f0c1cf47e832619d1478ac6..HEAD -- contracts/modules/DegenerusGameJackpotModule.sol \
      | grep -E "^[+-]" | grep -nE "EntropyLib\." || echo "L$L: byte-identical";
  done

AUDIT-01 raw delta:
  git diff 5db8682bd7b811437f0c1cf47e832619d1478ac6..HEAD -- contracts/modules/DegenerusGameLootboxModule.sol

AUDIT-01 contract-commit inventory:
  git log --oneline 5db8682bd7b811437f0c1cf47e832619d1478ac6..HEAD -- contracts/

AUDIT-01 test-commit inventory:
  git log --oneline 5db8682bd7b811437f0c1cf47e832619d1478ac6..HEAD -- test/ package.json

AUDIT-04 storage-slot scan:
  git diff 5db8682bd7b811437f0c1cf47e832619d1478ac6..HEAD --stat -- contracts/storage/ contracts/modules/DegenerusGameLootboxModule.sol

AUDIT-04 GameStorage byte-identity:
  git diff 5db8682bd7b811437f0c1cf47e832619d1478ac6..HEAD -- contracts/storage/GameStorage.sol
  # Expected: empty.

AUDIT-04 zero-new-public-fn grep:
  git diff 5db8682bd7b811437f0c1cf47e832619d1478ac6..HEAD -- contracts/ | grep -E '^\+.*function .* (public|external)'
  # Expected: zero hits in non-test contract files.

REG-01 byte-identity (JackpotModule):
  git diff 5db8682bd7b811437f0c1cf47e832619d1478ac6..HEAD -- contracts/modules/DegenerusGameJackpotModule.sol
  # Expected: empty.

REG-02 v34 carry (TraitUtils + JackpotBucketLib + EntropyLib + GameStorage):
  for f in contracts/DegenerusTraitUtils.sol contracts/libraries/JackpotBucketLib.sol contracts/libraries/EntropyLib.sol contracts/storage/GameStorage.sol; do
    echo "=== $f ===";
    git diff 6b63f6d4daf346a53a1d463790f637308ea8d555..HEAD -- "$f" | head -3;
  done
  # Expected: each empty.

REG-04 prior-finding sweep (v25..v35):
  for f in audit/FINDINGS-v25.0.md audit/FINDINGS-v27.0.md audit/FINDINGS-v28.0.md audit/FINDINGS-v29.0.md audit/FINDINGS-v30.0.md audit/FINDINGS-v31.0.md audit/FINDINGS-v32.0.md audit/FINDINGS-v33.0.md audit/FINDINGS-v34.0.md audit/FINDINGS-v35.0.md; do
    echo "=== $f ===";
    grep -nE '(EntropyLib\.entropyStep|_rollTargetLevel|_resolveLootboxRoll|_lootboxTicketCount|_lootboxDgnrsReward|_rollLootboxBoons|openLootBox|openBurnieLootBox|resolveLootboxDirect|resolveRedemptionLootbox)' "$f";
  done

§8 forward-cite verifier:
  ! grep -rE 'Phase (267|268|269|270|271)|v37\.0|v38\.0' audit/FINDINGS-v36.0.md .planning/phases/266-*/
  # Expected: zero matches qualifying as Phase-266-emitted forward-cites.

§9c closure SHA:
  git rev-parse HEAD
  # At Task 19 commit time → resolved by Task 20 atomic-update.

KNOWN-ISSUES.md modification proof:
  git diff 5db8682b..HEAD -- KNOWN-ISSUES.md
  # Expected: 1 entry rephrased — pre L31 "lootbox outcome rolls" → post "BAF jackpot ticket rolls".

Section-header structural sanity:
  grep -nE '^## [0-9]\.' audit/FINDINGS-v36.0.md
  # Expected: 8 hits (§2..§9; §1 is YAML frontmatter, not numbered header).

Closure signal verbatim presence:
  grep -c "MILESTONE_V36_AT_HEAD_" audit/FINDINGS-v36.0.md
  # Expected: ≥ 3 (frontmatter + §2 + §9c).

§4 6-surface presence:
  grep -cE 'Surface \(a\)|Surface \(b\)|Surface \(c\)|Surface \(d\)|Surface \(e\)|Surface \(f\)' audit/FINDINGS-v36.0.md
  # Expected: ≥ 6.

EXC-04 NARROWS row marker:
  grep -cE 'EXC-04|NARROWS|BAF jackpot' audit/FINDINGS-v36.0.md
  # Expected: ≥ 3.

Adversarial skill names (266-01-ADVERSARIAL-LOG.md):
  grep -E '^## /contract-auditor|^## /zero-day-hunter' .planning/phases/266-lootbox-entropy-refactor/266-01-ADVERSARIAL-LOG.md
  # Expected: 2 hits, NOT including /economic-analyst or /degen-skeptic.
</canonical_grep_recipes>

</context>
</content>
</invoke>
<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Player → VRF request (lootbox open entry points: `openLootBox`, `openBurnieLootBox`, `resolveLootboxDirect`, `resolveRedemptionLootbox`) | Player commits structured input `(player, day, amount)` at request time; rngWord is filled by Chainlink VRF post-commit; player has zero post-commit influence over rngWord. |
| VRF fulfillment → `_resolveLootboxCommon` seed derivation | Seed = `keccak256(abi.encode(rngWord, player, day, amount))` — VRF-derived high-entropy bits mixed with structured input. Player cannot bias the seed (rngWord is post-commit); structured input is committed at request. |
| Seed → bit-sliced consumers (`_rollTargetLevel`, `_resolveLootboxRoll`, `_lootboxTicketCount`, `_lootboxDgnrsReward`, `_rollLootboxBoons`) | Disjoint bit-range slices per consumer (152 bits / 256 available). Each consumer reads deterministically from the seed; no inter-consumer mutation. |
| Lootbox path ↔ BAF jackpot path | BAF jackpot `_jackpotTicketRoll` (JackpotModule:2186-2229) explicitly NOT touched at v36 (ENT-05 deferral). SURF-02 byte-identity verifies isolation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-266-01 | Tampering | Pre-fulfillment seed manipulation (player-controllable state changing between VRF request and fulfillment) | mitigate | Per `feedback_rng_commitment_window.md`: structured input `(player, day, amount)` is committed at request time; rngWord is post-commit unknown. Lootbox VRF index advances past current fulfillment index per existing isolation pattern (see KNOWN-ISSUES.md `Lootbox RNG uses index advance isolation` entry — out-of-scope edit per D-266-SCOPE-OUT-04). §4 surface (f) commitment-window check verdicts SAFE_BY_DESIGN with backward-trace cite per `feedback_rng_backward_trace.md`. Acceptance criteria: §4 surface (f) row populated with grep-cited evidence; STAT-01 chi² uniformity over 6 buckets passes. |
| T-266-02 | Information Disclosure / Bias | Bit-slice modulo bias (drawing `% K` from N bits where `2^N mod K ≠ 0`) | mitigate | D-266-BIT-BUDGET-01: every `% small` slice uses ≥ enough bits to keep bias ≤ 1% (max 0.39% at `% 5` from 8 bits; min 0.0024% at `% 1000` from 24 bits). All 8 consumers documented inline (NatSpec) per ENT-06. STAT-01 chi² empirical proof at `test/stat/LootboxEntropyDistribution.test.js` over 6 buckets (`% 100` Z<1.645 df=99 N=10K; `% 5` chi²<9.488 df=4 N=5K; `% 46` Z<1.645 df=45 N=10K; `% 20` path Z<1.645 df=19 N=10K; `% 20` variance Z<1.645 df=19 N=5K; `% 10000` Z<1.645 df=9999 N=100K). §4 surface (a) verdicts SAFE_BY_DESIGN. |
| T-266-03 | Tampering / Replay | Seed-reuse cross-correlation across sub-rolls within same resolution (two consumers reading the same bit range of the same seed → statistical dependence) | mitigate | D-266-SEED-01 + D-266-BIT-BUDGET-01: bit-budget table documents disjoint bit ranges per consumer (8 consumers × disjoint slices = 152 bits total). NatSpec inline bit-budget block per refactored function per ENT-06. ETH-amount-second branch uses Option A counter-tagged `seed2 = EntropyLib.hash2(seed, 1)` (RESEARCH.md Pitfall 2 + Assumption A4) — distinct from `seed` chunk. §4 surface (b) verdicts SAFE_BY_DESIGN with grep-cited slice-overlap audit; §4 surface (c) verdicts SAFE_BY_DESIGN (counter convention `1` distinct from `0`). |
| T-266-04 | Denial of Service / Resource Exhaustion | Gas-griefing — refactor delta exceeds documented envelope, breaks block gas limit safety margin | mitigate | Per `feedback_gas_worst_case.md`: theoretical worst case derived FIRST in `test/gas/LootboxOpenGas.test.js` header (opcode-by-opcode walk: keccak ~80 g + per-consumer inline shifts ~70-90 g + per-consumer modulo ~56 g − 5 saved entropyStep ~100-150 g − dead L1585 advance ~25 g + Option A seed2 ~80 g for ETH-amount-second branch). Net per-open delta: +20 to +160 g; `PER_OPEN_GAS_DELTA_BOUND = 300` asserted (2× headroom). REF-CAPTURE protocol pins measured value. AdvanceGame ≥ 1.99× margin preserved per `test/gas/AdvanceGameGas.test.js` v36.0 describe block. §4 surface (d) verdicts SAFE_BY_DESIGN. |
| T-266-05 | Tampering / Scope Creep | Surface-preservation breakage — accidental refactor scope creep modifies BAF jackpot, MintModule, or non-lootbox JackpotModule callsites | mitigate | SURF-01..04 per-line modified-set walk via `git diff 5db8682b..HEAD` returns ZERO `-` deletions inside any PROTECTED_RANGES range. SURF-01 (EntropyLib.sol body L1-43) + SURF-02 (BAF `_jackpotTicketRoll` L2186-2229) + SURF-03 (MintModule L652) + SURF-04 (9 JackpotModule callsites). `test/stat/SurfaceRegression.test.js` v36.0 describe block enforces. §4 surface (e) verdicts SAFE_BY_STRUCTURAL_CLOSURE for BAF byte-identity (ENT-05 verification). |
| T-266-06 | Repudiation / Audit-Trail Gap | Adversarial-pass spawns mid-§4 draft and re-derives surfaces from scratch instead of red-teaming the finished draft (RESEARCH.md Pitfall 5) | mitigate | D-266-ADVERSARIAL-02 mandates sequential timing: full §4 inline draft FIRST (Task 13 — all 6 surfaces verdicted with grep-cited evidence); THEN spawn `/contract-auditor` + `/zero-day-hunter` in parallel as a single message (Task 14). Both red-team the FINISHED draft. Document spawn message + responses in `266-01-ADVERSARIAL-LOG.md`. Disagreement disposition per D-266-ADVERSARIAL-03 surfaces inline to user before READ-only flip per `feedback_wait_for_approval.md`. |

**Block-on-high test:** ZERO unmitigated High threats. T-266-01 (commitment window) and T-266-02 (modulo bias) are the load-bearing RNG-audit threats; both have explicit mitigation paths backed by chi² empirical evidence (STAT-01) + structured-input commitment + grep-cited NatSpec bit-budget.

</threat_model>

<tasks>

<!-- ================================================================ -->
<!-- WAVE 1 — CONTRACT REFACTOR (BATCHED, USER-APPROVED at end)        -->
<!-- ================================================================ -->

<task type="auto">
  <name>Task 1 [Wave 1]: Refactor `_rollTargetLevel` to bit-sliced + drop nextEntropy return + update 4 entry-point callers</name>
  <files>contracts/modules/DegenerusGameLootboxModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameLootboxModule.sol (lines 800-830 — `_rollTargetLevel` body; lines 540-720 — 4 entry-point caller sites at L548-555 `openLootBox`, L622-629 `openBurnieLootBox`, L668-675 `resolveLootboxDirect`, L703-710 `resolveRedemptionLootbox`)
    - .planning/phases/266-lootbox-entropy-refactor/266-RESEARCH.md (lines 158-200 — Bit-Budget Worked Table for `_rollTargetLevel` ENT-01; lines 232-271 — `<specifics>` bit-slice convention example)
    - .planning/phases/266-lootbox-entropy-refactor/266-PATTERNS.md (lines 52-179 — refactor pattern + cumulative bit-budget table)
    - .planning/phases/266-lootbox-entropy-refactor/266-CONTEXT.md (D-266-API-01 + D-266-SEED-01 + D-266-BIT-BUDGET-01 + D-266-CONSUMER-LIST-01)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md
  </read_first>
  <action>
Refactor `_rollTargetLevel(uint24 baseLevel, uint256 entropy) -> (uint24 targetLevel, uint256 nextEntropy)` at L809-827 to a single-output bit-sliced version per CONTEXT.md `<specifics>` and RESEARCH.md Code Examples.

**Post-refactor signature + body (verbatim — copy this):**

```solidity
/// @dev Roll a target level for lootbox resolution.
///      90% chance: 0-4 levels above base. 10% chance: 5-50 levels above base.
///      Bit budget (consumed from `seed`):
///        - rangeRoll: bits[0..15]   via uint16(seed)         % 100   (bias 0.05%)
///        - near-level offset: bits[16..23] via uint8(seed >> 16) % 5 (bias 0.39%)
///        - far-level offset:  bits[24..39] via uint16(seed >> 24) % 46 (bias 0.05%)
/// @param baseLevel The base level to roll from
/// @param seed Per-resolution 256-bit keccak seed (derived once at _resolveLootboxCommon entry)
/// @return targetLevel The rolled target level
function _rollTargetLevel(
    uint24 baseLevel,
    uint256 seed
) private pure returns (uint24 targetLevel) {
    uint256 rangeRoll = uint16(seed) % 100;
    if (rangeRoll < 10) {
        uint256 farOffset = uint16(seed >> 24) % 46;
        targetLevel = baseLevel + uint24(farOffset + 5);
    } else {
        uint256 nearOffset = uint8(seed >> 16) % 5;
        targetLevel = baseLevel + uint24(nearOffset);
    }
}
```

**Update 4 entry-point callers** (live HEAD line numbers may have drifted ±5 — locate via grep `_rollTargetLevel\(` at HEAD):
- `openLootBox` callsite block (~L548-555)
- `openBurnieLootBox` callsite block (~L622-629)
- `resolveLootboxDirect` callsite block (~L668-675)
- `resolveRedemptionLootbox` callsite block (~L703-710)

Each caller currently does `(uint24 targetLevel, uint256 nextEntropy) = _rollTargetLevel(baseLevel, entropy);`. Replace with seed plumbing — derive `seed` once at entry; call new single-output `_rollTargetLevel(baseLevel, seed)`. Per RESEARCH.md Open Question 2: PRESERVE the existing `uint256 entropy = uint256(keccak256(abi.encode(rngWord, player, day, amount)));` pattern (rename local to `seed`); do NOT migrate to `EntropyLib.hash2(...)` — minimizes SURF diff noise. Thread `seed` into `_resolveLootboxCommon` via the existing `entropy` parameter slot (rename the parameter).

**NatSpec discipline (per `feedback_no_history_in_comments.md`):** Comment block above `_rollTargetLevel` describes the CURRENT bit-slice scheme. Do NOT write "this was xorshift before" or "previously used entropyStep". Comments describe what IS, not what changed.

Compile after this task: `npx hardhat compile`. Expected: 4 callers compile against the new signature; if any caller still passes `nextEntropy` through, compile fails — fix before moving on.

DO NOT COMMIT. Wave 1 batches all changes into one batched commit at Task 5 after user approval.
  </action>
  <verify>
    <automated>npx hardhat compile</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "EntropyLib\.entropyStep" contracts/modules/DegenerusGameLootboxModule.sol` does NOT show L813 or L817 (both removed; remaining hits at L1548/L1569/L1585/L1599/L1635 are still pre-Tasks 2-3).
    - `grep -nE "function _rollTargetLevel" contracts/modules/DegenerusGameLootboxModule.sol` returns exactly 1 hit with signature `(uint24 baseLevel, uint256 seed) private pure returns (uint24 targetLevel)` — single return value, parameter renamed to `seed`.
    - `grep -nE "uint16\(seed\) % 100" contracts/modules/DegenerusGameLootboxModule.sol` returns >= 1 hit (rangeRoll bit-slice).
    - `grep -nE "uint8\(seed >> 16\) % 5" contracts/modules/DegenerusGameLootboxModule.sol` returns >= 1 hit (near-offset bit-slice).
    - `grep -nE "uint16\(seed >> 24\) % 46" contracts/modules/DegenerusGameLootboxModule.sol` returns >= 1 hit (far-offset bit-slice).
    - `grep -nE "bits\[0\.\.15\]|bits\[16\.\.23\]|bits\[24\.\.39\]" contracts/modules/DegenerusGameLootboxModule.sol` returns >= 3 hits (NatSpec bit-budget block per ENT-06).
    - `grep -cE "(this was xorshift|previously used entropyStep)" contracts/modules/DegenerusGameLootboxModule.sol` returns 0 (no history-in-comments per `feedback_no_history_in_comments.md`).
    - `grep -nE "_rollTargetLevel\(baseLevel," contracts/modules/DegenerusGameLootboxModule.sol` returns >= 4 caller-side hits at the 4 entry points; each call site has matching arity (single return).
    - `npx hardhat compile` exits 0 with no errors.
  </acceptance_criteria>
  <done>`_rollTargetLevel` refactored to single-output bit-sliced version with NatSpec bit-budget block; 4 entry-point callers updated; `npx hardhat compile` green; no commit.</done>
</task>

<task type="auto">
  <name>Task 2 [Wave 1]: Refactor `_resolveLootboxRoll` 4 entropyStep callsites + drop dead L1585 + sub-call `_lootboxDgnrsReward` slicing</name>
  <files>contracts/modules/DegenerusGameLootboxModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameLootboxModule.sol (lines 1530-1700 — `_resolveLootboxRoll` body L1530-1616 + `_lootboxDgnrsReward` body around L1670-1700)
    - .planning/phases/266-lootbox-entropy-refactor/266-RESEARCH.md (lines 178-213 — Bit-Budget Worked Table for ENT-02 + sub-call enumeration; lines 405-414 — Pitfall 2 ETH-amount-second branch slice-overlap; lines 415-438 — Pitfall 3 sub-call entropy consumers; lines 633-636 — Open Question 3 dead L1585 advance removal recommendation)
    - .planning/phases/266-lootbox-entropy-refactor/266-PATTERNS.md (lines 86-131 — `_resolveLootboxRoll` pre-refactor body; lines 162-179 — cumulative bit-budget table)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_dead_guards.md
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md
  </read_first>
  <action>
Refactor `_resolveLootboxRoll(... uint256 entropy) -> (... uint256 nextEntropy, ...)` at L1530-1616 to bit-sliced reads from threaded `seed` parameter. Apply 4 changes + 1 deletion:

**Change 1 (L1548 — entry advance REPLACE):**
- DELETE: `nextEntropy = EntropyLib.entropyStep(entropy);`
- REPLACE downstream usage: `uint256 roll = nextEntropy % 20;` (L1551) -> `uint256 roll = uint16(seed >> 40) % 20;`  // bits[40..55]

**Change 2 (L1569 — DGNRS-path advance REPLACE + sub-call slicing):**
- DELETE: `nextEntropy = EntropyLib.entropyStep(nextEntropy);`
- The DGNRS path calls `_lootboxDgnrsReward(amount, entropy)` (currently passes nextEntropy). REPLACE with: `_lootboxDgnrsReward(amount, seed)` (pass the same `seed`). The `_lootboxDgnrsReward` body at L1680 currently does `uint256 tierRoll = entropy % 1000;` — UPDATE that line to `uint256 tierRoll = uint24(entropy >> 56) % 1000;`  // bits[56..79] (24 bits, bias 0.0024% per RESEARCH.md Assumption A5).
- The `_lootboxDgnrsReward` parameter name stays `entropy` for the function-internal slice convention (callers see `seed` threading; callee receives the seed, slices bits[56..79]).
- Add NatSpec bit-budget comment to `_lootboxDgnrsReward` documenting bits[56..79] consumption per ENT-06.

**Change 3 (L1585 — WWXRP-path DEAD ADVANCE REMOVE per `feedback_no_dead_guards.md`):**
- DELETE the line entirely: `nextEntropy = EntropyLib.entropyStep(nextEntropy);`
- WWXRP path uses literal `LOOTBOX_WWXRP_PRIZE`; `applyPresaleMultiplier = false` (deterministic). No downstream entropy consumer reads bits sourced from this advance per RESEARCH.md Assumption A3.
- Save ~40 g per WWXRP-path lootbox.
- Document the deletion in §3d AUDIT-01 delta-surface table later (Task 12) as a DELETED row.

**Change 4 (L1599 — large-BURNIE-path advance REPLACE):**
- DELETE: `nextEntropy = EntropyLib.entropyStep(nextEntropy);`
- REPLACE downstream usage: `uint256 varianceRoll = nextEntropy % 20;` (L1600) -> `uint256 varianceRoll = uint16(seed >> 80) % 20;`  // bits[80..95]

**Signature change:** Drop `nextEntropy` from the return tuple of `_resolveLootboxRoll`. Caller in `_resolveLootboxCommon` no longer threads `nextEntropy` between sub-calls; all sub-calls receive `seed` directly.

**ETH-amount-second branch (RESEARCH.md Pitfall 2 + Assumption A4 — Option A):**
Locate the `_resolveLootboxRoll` second invocation in `_resolveLootboxCommon` (where `amountSecond != 0`). The first invocation uses `seed`; the second invocation MUST use `seed2 = EntropyLib.hash2(seed, 1)` (counter-tagged distinct chunk per Option A — RESEARCH.md recommended). Add at the second-invocation site `uint256 seed2 = EntropyLib.hash2(seed, 1);` then `_resolveLootboxRoll(..., seed2);`. Document the chunk-counter convention inline (NatSpec) so AUDIT-02 surface (c) hash2 chunk-collision-free analysis is one-liner.

**NatSpec bit-budget block** above `_resolveLootboxRoll` per ENT-06. Document bits[40..55] -> path-roll (% 20); bits[56..79] -> DGNRS tier sub-call slice; bits[80..95] -> large-BURNIE varianceRoll (% 20); L1585 advance REMOVED (dead in WWXRP path).

**Per `feedback_no_history_in_comments.md`:** describe what IS at HEAD; do NOT annotate "previously had a 5th entropy step" or "this used to chain xorshift". The dead-advance removal goes into the §3d audit-deliverable DELETED row, not into a contract comment.

Compile: `npx hardhat compile`. DO NOT COMMIT.
  </action>
  <verify>
    <automated>npx hardhat compile</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "EntropyLib\.entropyStep" contracts/modules/DegenerusGameLootboxModule.sol` does NOT show L1548 / L1569 / L1585 / L1599 (4 callsites removed in this task; only L1635 remains pending Task 3).
    - `grep -cE "EntropyLib\.entropyStep" contracts/modules/DegenerusGameLootboxModule.sol` returns 1 (only L1635 remains; Task 3 removes it).
    - `grep -nE "uint16\(seed >> 40\) % 20" contracts/modules/DegenerusGameLootboxModule.sol` returns >= 1 hit (path-roll bit-slice).
    - `grep -nE "uint16\(seed >> 80\) % 20" contracts/modules/DegenerusGameLootboxModule.sol` returns >= 1 hit (varianceRoll bit-slice).
    - `grep -nE "uint24\(entropy >> 56\) % 1000" contracts/modules/DegenerusGameLootboxModule.sol` returns >= 1 hit (DGNRS tier sub-call slice).
    - `grep -nE "EntropyLib\.hash2\(seed, 1\)" contracts/modules/DegenerusGameLootboxModule.sol` returns >= 1 hit (Option A counter-tagged seed2 for ETH-amount-second branch).
    - `grep -nE "bits\[40\.\.55\]|bits\[56\.\.79\]|bits\[80\.\.95\]" contracts/modules/DegenerusGameLootboxModule.sol` returns >= 3 hits (NatSpec bit-budget block per ENT-06).
    - `grep -nE "applyPresaleMultiplier = false" contracts/modules/DegenerusGameLootboxModule.sol` shows the WWXRP branch present but with NO `entropyStep` call immediately preceding it (L1585 deletion verified by absence).
    - `grep -cE "function _resolveLootboxRoll" contracts/modules/DegenerusGameLootboxModule.sol` returns 1; signature drops `nextEntropy` return.
    - `npx hardhat compile` exits 0.
  </acceptance_criteria>
  <done>4 entropyStep callsites in `_resolveLootboxRoll` removed (L1548 + L1569 + L1599 replaced with bit-slices; L1585 deleted as dead per `feedback_no_dead_guards.md`); `_lootboxDgnrsReward` sub-call updated to consume bits[56..79]; ETH-amount-second branch uses `seed2 = EntropyLib.hash2(seed, 1)` per Option A; NatSpec bit-budget block populated; compile green; no commit.</done>
</task>

<task type="auto">
  <name>Task 3 [Wave 1]: Refactor `_lootboxTicketCount` L1635 + sub-call `_rollLootboxBoons` L1059 slicing</name>
  <files>contracts/modules/DegenerusGameLootboxModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameLootboxModule.sol (lines 1626-1669 — `_lootboxTicketCount` body; lines 1013-1071 — `_rollLootboxBoons` body)
    - .planning/phases/266-lootbox-entropy-refactor/266-RESEARCH.md (lines 196-212 — ENT-03 24-bit slice + sub-call enumeration; lines 415-438 — Pitfall 3 sub-call entropy consumers)
    - .planning/phases/266-lootbox-entropy-refactor/266-PATTERNS.md (lines 117-131 — `_lootboxTicketCount` + sub-call slicing patterns)
  </read_first>
  <action>
Refactor `_lootboxTicketCount(uint256, uint256, uint256) -> uint256` at L1626-1669 + sub-call `_rollLootboxBoons` at L1013-1071.

**Change 1 — `_lootboxTicketCount` L1635 (REPLACE):**
- Function signature currently has an `entropy` parameter (3rd arg). Rename internally to `seed` (keep parameter slot — caller passes the threaded seed).
- DELETE: `nextEntropy = EntropyLib.entropyStep(entropy);` (L1635)
- REPLACE downstream usage: `uint256 varianceRoll = nextEntropy % 10_000;` (L1636) -> `uint256 varianceRoll = uint24(seed >> 96) % 10_000;`  // bits[96..119] (24 bits, bias 0.045%)
- DROP any `nextEntropy` return; signature becomes `(... uint256 seed) -> uint256` (single return).

**Change 2 — `_rollLootboxBoons` L1059 (sub-call slicing):**
- Function signature currently has an `entropy` parameter. Rename internally to `seed`. Update L1059: `uint256 roll = entropy % BOON_PPM_SCALE;` -> `uint256 roll = uint32(seed >> 120) % BOON_PPM_SCALE;`  // bits[120..151] (32 bits, bias 0.022%; BOON_PPM_SCALE = 1_000_000)
- The `_boonFromRoll` interior selection at L1062 consumes the same `roll` value via deterministic conditional — no fresh slice needed; preserve byte-identical post-rename.

**NatSpec bit-budget block** for both functions per ENT-06: `_lootboxTicketCount` bits[96..119] -> varianceRoll (% 10000); `_rollLootboxBoons` bits[120..151] -> boon roll (% BOON_PPM_SCALE).

**Caller updates in `_resolveLootboxCommon`:** thread `seed` (single 256-bit chunk) into both `_lootboxTicketCount` and `_rollLootboxBoons` calls. Both consume bits from disjoint ranges of the same `seed` (bits[96..119] + bits[120..151] respectively); no chunk-overlap risk.

Compile: `npx hardhat compile`. DO NOT COMMIT.

**Note on cumulative bit budget for the primary seed chunk:** total consumption is now 152 bits (40 + 16 + 24 + 16 + 24 + 32) across 8 consumers. Comfortably under 256 bits. No third chunk required for the single-amount path; ETH-amount-second branch (Task 2) uses `seed2 = EntropyLib.hash2(seed, 1)` for its full 256-bit budget.
  </action>
  <verify>
    <automated>npx hardhat compile</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "EntropyLib\.entropyStep" contracts/modules/DegenerusGameLootboxModule.sol` returns 0 (all 7 lootbox-path entropyStep callsites removed: L813 + L817 in Task 1; L1548 + L1569 + L1585 + L1599 in Task 2; L1635 in this task).
    - `grep -nE "uint24\(seed >> 96\) % 10_000" contracts/modules/DegenerusGameLootboxModule.sol` returns >= 1 hit (`_lootboxTicketCount` varianceRoll).
    - `grep -nE "uint32\(seed >> 120\) % BOON_PPM_SCALE" contracts/modules/DegenerusGameLootboxModule.sol` returns >= 1 hit (`_rollLootboxBoons` roll).
    - `grep -nE "bits\[96\.\.119\]|bits\[120\.\.151\]" contracts/modules/DegenerusGameLootboxModule.sol` returns >= 2 hits (NatSpec bit-budget per ENT-06).
    - `grep -cE "function _lootboxTicketCount|function _rollLootboxBoons" contracts/modules/DegenerusGameLootboxModule.sol` returns 2 (both signatures preserved structurally).
    - `npx hardhat compile` exits 0.
  </acceptance_criteria>
  <done>L1635 entropyStep replaced with `uint24(seed >> 96) % 10_000`; `_rollLootboxBoons` L1059 sliced as `uint32(seed >> 120) % BOON_PPM_SCALE`; NatSpec bit-budget blocks populated; ALL 7 lootbox-path entropyStep callsites now removed; compile green; no commit.</done>
</task>

<task type="auto">
  <name>Task 4 [Wave 1]: Plumb single-keccak seed at `_resolveLootboxCommon` entry + thread through 4 entry-point callers</name>
  <files>contracts/modules/DegenerusGameLootboxModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameLootboxModule.sol (lines 540-720 — 4 entry-point callsites at L548-555, L622-629, L668-675, L703-710; lines 847-1000 — `_resolveLootboxCommon` body)
    - .planning/phases/266-lootbox-entropy-refactor/266-RESEARCH.md (lines 514-529 — Caller pattern for entry-point seed; lines 626-631 — Open Question 2 recommendation: PRESERVE existing keccak)
    - .planning/phases/266-lootbox-entropy-refactor/266-PATTERNS.md (lines 134-198 — refactor pattern + entry-point caller pattern + update points)
  </read_first>
  <action>
Wire the single-keccak-per-resolution seed plumbing across `_resolveLootboxCommon` and its 4 entry-point callers per CONTEXT.md D-266-SEED-01 + RESEARCH.md Open Question 2 recommendation.

**Per RESEARCH.md Open Question 2 — PRESERVE existing keccak:** L554 / L628 / L673 / L708 currently use `uint256 entropy = uint256(keccak256(abi.encode(rngWord, player, day, amount)));`. Keep this pattern. Do NOT migrate to `EntropyLib.hash2(...)`. Rename the local variable from `entropy` to `seed` to match the new naming convention used in Tasks 1-3. Document inline in NatSpec at each callsite that this is the per-resolution seed used by all downstream sub-rolls.

**`_resolveLootboxCommon` parameter rename:** the function currently receives `entropy` as one of its parameters. Rename to `seed`. All internal references update — `seed` is now the canonical name for the per-resolution 256-bit keccak result.

**Sub-call threading:** verify that `_resolveLootboxCommon` body now passes `seed` (not `nextEntropy` chains) to:
- `_rollTargetLevel(baseLevel, seed)` (Task 1 signature)
- `_resolveLootboxRoll(... seed)` for the first invocation (Task 2 signature)
- `_resolveLootboxRoll(... seed2)` for the ETH-amount-second branch where `seed2 = EntropyLib.hash2(seed, 1)` (Task 2)
- `_lootboxTicketCount(... seed)` (Task 3 signature)
- `_rollLootboxBoons(... seed)` (Task 3 signature)
- `_lootboxDgnrsReward(amount, seed)` (Task 2 sub-call update)

NO `nextEntropy` chaining anywhere. NO `EntropyLib.entropyStep` anywhere in the lootbox path.

**NatSpec block at `_resolveLootboxCommon` entry** documenting the single-keccak-per-resolution + bit-allocation map (per ENT-06):

```solidity
/// @dev Single-keccak-per-resolution entropy: derive `seed` once at entry
///      via keccak256(abi.encode(rngWord, player, day, amount)); thread
///      through downstream sub-rolls.
///      Bit allocation in primary chunk:
///        bits[0..15]    rangeRoll % 100         (_rollTargetLevel)
///        bits[16..23]   near-offset % 5         (_rollTargetLevel)
///        bits[24..39]   far-offset % 46         (_rollTargetLevel)
///        bits[40..55]   pathRoll % 20           (_resolveLootboxRoll)
///        bits[56..79]   tierRoll % 1000         (_lootboxDgnrsReward sub-call)
///        bits[80..95]   varianceRoll % 20       (_resolveLootboxRoll large-BURNIE)
///        bits[96..119]  ticketVariance % 10000  (_lootboxTicketCount)
///        bits[120..151] boon roll % BOON_PPM_SCALE (_rollLootboxBoons)
///      ETH-amount-second branch uses seed2 = EntropyLib.hash2(seed, 1)
///      (counter-tagged chunk 1; distinct from chunk 0).
```

**Per `feedback_no_history_in_comments.md`:** describe what IS, not what changed. NO "previously chained nextEntropy". The block describes the current bit-allocation contract.

**Run existing test suite:** `npx hardhat compile && npx hardhat test test/unit` (existing lootbox unit tests should pass — uniformity equivalence preserved per STAT-02; specific outcomes diverge but unit-test assertions are distribution-shape, not specific-outcome). If any unit test fails because it asserts a specific-outcome replay against the old xorshift output, surface to user inline before proceeding (per RESEARCH.md Assumption A6 + CONTEXT.md `<deferred>` "Behavioral-replay tests").

DO NOT COMMIT. Wave 1 batches all changes into Task 5.
  </action>
  <verify>
    <automated>npx hardhat compile && npx hardhat test test/unit 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "EntropyLib\.entropyStep" contracts/modules/DegenerusGameLootboxModule.sol` returns 0 (zero lootbox-path entropyStep consumers).
    - `grep -nE "uint256 seed = uint256\(keccak256\(abi\.encode\(rngWord" contracts/modules/DegenerusGameLootboxModule.sol` returns >= 4 hits (one per entry-point caller at the 4 callsites).
    - `grep -nE "EntropyLib\.hash2\(seed, 1\)" contracts/modules/DegenerusGameLootboxModule.sol` returns >= 1 hit (Option A counter-tagged seed2 for ETH-amount-second branch).
    - `grep -v '^//' contracts/modules/DegenerusGameLootboxModule.sol | grep -cE "bits\["` returns >= 8 (one NatSpec bit-budget annotation per consumer per ENT-06; comments-stripped form to avoid self-invalidating grep gate).
    - `grep -nE "function _resolveLootboxCommon" contracts/modules/DegenerusGameLootboxModule.sol` returns 1 hit; parameter renamed from `entropy` to `seed`.
    - `npx hardhat compile` exits 0.
    - `npx hardhat test test/unit` exits 0 OR (if any specific-outcome-replay test fails) the failure is surfaced inline to user — no silent override per RESEARCH.md Assumption A6.
  </acceptance_criteria>
  <done>Single-keccak-per-resolution seed plumbed at `_resolveLootboxCommon` entry; existing `keccak256(abi.encode(rngWord, player, day, amount))` preserved per RESEARCH.md Open Question 2; threaded through 5 sub-roll consumers; NatSpec bit-allocation block populated; ETH-amount-second branch uses Option A counter-tagged seed2; compile + existing unit tests green (or failures surfaced); no commit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5 [Wave 1 GATE]: Present batched LootboxModule diff to user; finalize Wave 1 ONLY after explicit "approved"</name>
  <files>contracts/modules/DegenerusGameLootboxModule.sol</files>
  <read_first>
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_contract_commits.md
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_batch_contract_approval.md
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_never_preapprove_contracts.md
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_wait_for_approval.md
  </read_first>
  <action>
This is a checkpoint:human-verify task. Present the Wave 1 batched LootboxModule diff to the user per the <what-built> block below; wait for explicit "approved" string before staging the file and recording the entry with subject `feat(266): lootbox-path entropy refactor [ENT-01..06]`. See <how-to-verify> for the diff-review checklist and <acceptance_criteria> for the approval-gate exit conditions. Per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md`: agent does NOT pre-approve.
  </action>
  <verify>
    <automated>git log --oneline -1 -- contracts/modules/DegenerusGameLootboxModule.sol | grep -E "feat\\(266\\): lootbox-path entropy refactor \\[ENT-01\\.\\.06\\]"</automated>
  </verify>
  <what-built>
Wave 1 contract refactor batched diff covering Tasks 1-4:
- `_rollTargetLevel` (L809-827) — bit-sliced single-output; nextEntropy return dropped.
- `_resolveLootboxRoll` (L1530-1616) — 4 entropyStep callsites removed (L1548 + L1569 + L1599 replaced; L1585 dead WWXRP advance deleted entirely per `feedback_no_dead_guards.md`).
- `_lootboxTicketCount` (L1626-1669) — L1635 entropyStep replaced with 24-bit slice.
- `_lootboxDgnrsReward` (L1670-1700) — L1680 sub-call slice updated to bits[56..79] % 1000.
- `_rollLootboxBoons` (L1013-1071) — L1059 sub-call slice updated to bits[120..151] % BOON_PPM_SCALE.
- `_resolveLootboxCommon` (L847+) — single-keccak seed plumbed at entry; threaded through 5 sub-rolls; ETH-amount-second branch uses `seed2 = EntropyLib.hash2(seed, 1)` per Option A.
- 4 entry-point callers updated (`openLootBox` + `openBurnieLootBox` + `resolveLootboxDirect` + `resolveRedemptionLootbox`).
- Per-consumer NatSpec inline bit-budget block per ENT-06.
- Zero changes to `EntropyLib.sol` (ENT-04).
- Zero changes to `JackpotModule.sol` (SURF-02 + SURF-04).
- Zero changes to `MintModule.sol` (SURF-03).
  </what-built>
  <how-to-verify>
1. Run `git diff --stat 5db8682b..HEAD -- contracts/`
   - Expected: only `contracts/modules/DegenerusGameLootboxModule.sol` shows changes; zero other files modified.
2. Run `git diff 5db8682b..HEAD -- contracts/libraries/EntropyLib.sol`
   - Expected: empty output (SURF-01 / ENT-04).
3. Run `git diff 5db8682b..HEAD -- contracts/modules/DegenerusGameJackpotModule.sol`
   - Expected: empty output (SURF-02 + SURF-04).
4. Run `git diff 5db8682b..HEAD -- contracts/modules/DegenerusGameMintModule.sol`
   - Expected: empty output (SURF-03).
5. Run `grep -cE "EntropyLib\.entropyStep" contracts/modules/DegenerusGameLootboxModule.sol`
   - Expected: 0 (all 7 lootbox-path callsites removed).
6. Run `npx hardhat compile`
   - Expected: success.
7. Run `npx hardhat test test/unit`
   - Expected: pre-existing unit tests pass (uniformity preserved per STAT-02; specific-outcome divergence acceptable per CONTEXT.md `<deferred>`).
8. Review the full diff: `git diff 5db8682b..HEAD -- contracts/modules/DegenerusGameLootboxModule.sol | less`
   - Verify no history-in-comments per `feedback_no_history_in_comments.md`.
   - Verify NatSpec bit-budget blocks present at each refactored function.
   - Verify L1585 dead advance fully deleted (not commented out).

**Per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md`:** the agent does NOT pre-approve this finalization. Wait for the user's explicit `approved` string before staging the LootboxModule file and recording it with subject `feat(266): lootbox-path entropy refactor [ENT-01..06]`.

If the user requests changes, return to the relevant Task 1-4 and revise; re-present the diff at this gate.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize finalizing Wave 1, or describe required changes.</resume-signal>
  <acceptance_criteria>
    - User responds with explicit `approved` string before any commit runs.
    - After approval: `git log --oneline -1 -- contracts/modules/DegenerusGameLootboxModule.sol` shows subject `feat(266): lootbox-path entropy refactor [ENT-01..06]`.
    - `git log --name-only HEAD -1` shows ONLY `contracts/modules/DegenerusGameLootboxModule.sol` (no other files in this commit per batched-discipline).
  </acceptance_criteria>
  <done>User approved batched LootboxModule diff; single contract commit landed with subject `feat(266): lootbox-path entropy refactor [ENT-01..06]`; ENT-01..06 + ENT-04 (EntropyLib stable) satisfied at HEAD.</done>
</task>

</content>

<!-- ================================================================ -->
<!-- WAVE 2 — TESTS (BATCHED, USER-APPROVED at end)                    -->
<!-- ================================================================ -->

<task type="auto">
  <name>Task 6 [Wave 2]: Author `test/stat/LootboxEntropyDistribution.test.js` chi² uniformity over 6 sub-roll buckets</name>
  <files>test/stat/LootboxEntropyDistribution.test.js</files>
  <read_first>
    - test/stat/PerPullLevelDistribution.test.js (lines 1-110 — chi² infrastructure: imports + `makeRng` + `CHI2_CRIT_05` + `wilsonHilfertyZ`; lines 222-282 — REGIMES table + per-regime test body pattern)
    - test/stat/TraitDistribution.test.js (lines 48-100 — chi² helpers origin)
    - .planning/phases/266-lootbox-entropy-refactor/266-RESEARCH.md (lines 366-403 — Pitfall 1 sample-budget calibration with per-bucket df + critical value; lines 531-557 — chi² code example for `% 100` Wilson-Hilferty; lines 622-627 — Open Question 1 sample-budget recommendation 100K for `% 10000`)
    - .planning/phases/266-lootbox-entropy-refactor/266-PATTERNS.md (lines 240-348 — chi² infrastructure pattern + REGIMES table + STAT-04-equivalent infra-reuse sanity describe)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_skip_research_test_phases.md
  </read_first>
  <action>
Create `test/stat/LootboxEntropyDistribution.test.js` covering STAT-01..03. Single file with 6 chi² describe blocks (one per sub-roll bucket) + 1 STAT-04-equivalent infrastructure-reuse sanity describe block.

**Imports + chi² infrastructure (verbatim re-declaration per STAT-03 reuse-existing-tooling discipline; copy from `test/stat/PerPullLevelDistribution.test.js` L78-102 — do NOT import from a shared module per RESEARCH.md Anti-Patterns):**

```javascript
import { loadFixture } from "@nomicfoundation/hardhat-toolbox/network-helpers.js";
import { expect } from "chai";
import hre from "hardhat";

// === Phase 266 chi² infrastructure (verbatim re-declaration per STAT-03) ===
// Source: test/stat/PerPullLevelDistribution.test.js L78-102 (Phase 264 STAT-04 pattern)
// Origin:  test/stat/TraitDistribution.test.js L48-100 (Phase 261 origin)
// STAT-03 reuse-existing-tooling discipline: re-declare verbatim, do NOT import.

function makeRng(seed) {
  const seedHex = "0x" + BigInt.asUintN(256, BigInt(seed)).toString(16).padStart(64, "0");
  let counter = 0n;
  return function next256() {
    const counterHex = counter.toString(16).padStart(64, "0");
    counter++;
    return BigInt(hre.ethers.keccak256(seedHex + counterHex));
  };
}

const CHI2_CRIT_05 = { 1: 3.841, 2: 5.991, 3: 7.815, 4: 9.488, 5: 11.070, 6: 12.592, 7: 14.067 };

function wilsonHilfertyZ(chi2, df) {
  const term = Math.cbrt(chi2 / df) - (1 - 2 / (9 * df));
  return term / Math.sqrt(2 / (9 * df));
}
```

**REGIMES table (sample-budget calibration per RESEARCH.md Pitfall 1 + Open Question 1; pre-commit budget per bucket):**

```javascript
// Source: RESEARCH.md Pitfall 1 sample-budget table.
// Bucket          Samples   Expected/bucket  df    Critical
// % 100           10_000    100              99    wilsonHilfertyZ < 1.645
// % 5             5_000     1_000            4     CHI2_CRIT_05[4] = 9.488
// % 46            10_000    217              45    wilsonHilfertyZ < 1.645
// % 20 (path)     10_000    500              19    wilsonHilfertyZ < 1.645
// % 20 (variance) 5_000     250              19    wilsonHilfertyZ < 1.645
// % 10000         100_000   10               9999  wilsonHilfertyZ < 1.645  -- marginal expected/bucket per Pitfall 1
const REGIMES = [
  { name: "rangeRoll % 100",        bitMaskHex: "0xFFFFn",          shift: 0n,   modulus: 100,    samples: 10_000,  highDf: true,  seed: 0xC036_0001 },
  { name: "near-offset % 5",        bitMaskHex: "0xFFn",            shift: 16n,  modulus: 5,      samples: 5_000,   highDf: false, seed: 0xC036_0002 },
  { name: "far-offset % 46",        bitMaskHex: "0xFFFFn",          shift: 24n,  modulus: 46,     samples: 10_000,  highDf: true,  seed: 0xC036_0003 },
  { name: "pathRoll % 20",          bitMaskHex: "0xFFFFn",          shift: 40n,  modulus: 20,     samples: 10_000,  highDf: true,  seed: 0xC036_0004 },
  { name: "varianceRoll % 20",      bitMaskHex: "0xFFFFn",          shift: 80n,  modulus: 20,     samples: 5_000,   highDf: true,  seed: 0xC036_0005 },
  { name: "ticketVariance % 10000", bitMaskHex: "0xFFFFFFn",        shift: 96n,  modulus: 10000,  samples: 100_000, highDf: true,  seed: 0xC036_0006 },
];
```

**Per-regime test body** (mirror RESEARCH.md Code Examples L536-557): for each regime, run a `describe("STAT-01 — <regime.name> chi² uniformity")` block. Inside, draw `regime.samples` from `makeRng(regime.seed)`; for each sample, slice `Number((seed >> shift) & bitMaskHex)` then `% modulus`; bucket; compute chi²; assert against the appropriate critical value (`CHI2_CRIT_05[df]` if `!highDf`, else `wilsonHilfertyZ(chi2, df) < 1.645`).

**STAT-02 (pre-/post distribution shape preserved)**: a single `describe("STAT-02 — distribution shape preserved (uniformity-equivalence)")` block. Note in the test docstring that pre-refactor (xorshift) and post-refactor (bit-sliced keccak) BOTH produce uniform draws within statistical tolerance per CONTEXT.md `<deferred>` "Behavioral-replay tests"; this test asserts uniformity-equivalence by re-running 2 of the 6 STAT-01 buckets (`% 100` + `% 5`) and confirming they pass against critical value (the empirical proof that bit-sliced keccak is uniformly distributed). No specific-outcome replay against pre-refactor binary.

**STAT-04-equivalent infrastructure-reuse sanity describe** (final block): pattern from `test/stat/PerPullLevelDistribution.test.js` describe block "STAT-04 — Phase 261 infrastructure reuse". Cross-cite the origin file path (`test/stat/TraitDistribution.test.js` L48-100) + assert structural equality of the re-declaration via comment block (no runtime check needed; STAT-03 is a documentation-only structural pin):

```javascript
describe("STAT-03 — Phase 261/264 chi² infrastructure reuse (verbatim re-declaration)", function () {
  it("re-declares makeRng / CHI2_CRIT_05 / wilsonHilfertyZ verbatim from origin", function () {
    // Source: test/stat/TraitDistribution.test.js L48-100 (Phase 261 origin)
    //         test/stat/PerPullLevelDistribution.test.js L78-102 (Phase 264 carry)
    // Phase 266 STAT-03 reuse-existing-tooling discipline: re-declared verbatim above.
    expect(typeof makeRng).to.equal("function");
    expect(typeof wilsonHilfertyZ).to.equal("function");
    expect(CHI2_CRIT_05[4]).to.equal(9.488);  // df=4 critical at α=0.05
  });
});
```

Run: `npx hardhat test test/stat/LootboxEntropyDistribution.test.js`. Expected: all 7 describe blocks pass (6 STAT-01 + STAT-02 + STAT-03). DO NOT COMMIT — Wave 2 batches at Task 10.
  </action>
  <verify>
    <automated>npx hardhat test test/stat/LootboxEntropyDistribution.test.js 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - File `test/stat/LootboxEntropyDistribution.test.js` exists.
    - `grep -cE "function makeRng|function wilsonHilfertyZ|const CHI2_CRIT_05" test/stat/LootboxEntropyDistribution.test.js` returns >= 3 (verbatim chi² infra re-declaration).
    - `grep -cE "describe\(\"STAT-0[1-3]" test/stat/LootboxEntropyDistribution.test.js` returns >= 7 (6 STAT-01 buckets + STAT-02 + STAT-03 — counting `STAT-01 —` rows).
    - `grep -cE "% 100|% 5|% 46|% 20|% 10000" test/stat/LootboxEntropyDistribution.test.js` returns >= 6 (one per sub-roll bucket).
    - `grep -cE "0xC036_000[1-6]" test/stat/LootboxEntropyDistribution.test.js` returns >= 6 (Phase 266 seed convention).
    - `grep -cE "wilsonHilfertyZ\(chi2, [0-9]+\)" test/stat/LootboxEntropyDistribution.test.js` returns >= 5 (high-df cases use Wilson-Hilferty per Pitfall 1).
    - `npx hardhat test test/stat/LootboxEntropyDistribution.test.js` exits 0 (all 7 describe blocks pass).
  </acceptance_criteria>
  <done>STAT-01..03 chi² test file authored; 6 sub-roll bucket describes + STAT-02 distribution-shape describe + STAT-03 infra-reuse sanity describe; all pass; no commit.</done>
</task>

<task type="auto">
  <name>Task 7 [Wave 2]: Author `test/gas/LootboxOpenGas.test.js` GAS-01 per-open ±300 g + theoretical-worst-case header</name>
  <files>test/gas/LootboxOpenGas.test.js</files>
  <read_first>
    - test/gas/Phase264GasRegression.test.js (lines 18-110 — REF-CAPTURE protocol + theoretical-worst-case derivation header pattern; lines 60-110 — pinning protocol)
    - .planning/phases/266-lootbox-entropy-refactor/266-RESEARCH.md (lines 354-356 — Don't Hand-Roll table for gas regression; lines 745-746 — `feedback_gas_worst_case.md` discipline)
    - .planning/phases/266-lootbox-entropy-refactor/266-PATTERNS.md (lines 357-397 — REF-CAPTURE + ENTRY_POINT_DELTA_TOLERANCE protocol)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_gas_worst_case.md
    - test/helpers/deployFixture.js (deployFullProtocol fixture pattern)
    - test/helpers/testUtils.js (eth + advanceToNextDay + getEvents + getLastVRFRequestId)
  </read_first>
  <action>
Create `test/gas/LootboxOpenGas.test.js` covering GAS-01.

**Theoretical worst-case derivation header** (per `feedback_gas_worst_case.md` — derive worst case FIRST in test header, THEN test). Mirror `test/gas/Phase264GasRegression.test.js` L18-50 pattern.

```javascript
// ============================================================================
// THEORETICAL WORST-CASE DERIVATION (Phase 266 lootbox-open envelope — GAS-01)
// ============================================================================
//
// Per-open seed-derivation cost (single-keccak-per-resolution + inline bit-slice):
//   + keccak256(abi.encode(rngWord, player, day, amount))     ~  80 gas
//     (entry-point keccak — preserved from pre-refactor per RESEARCH.md Open Question 2;
//      MSTORE × 4 + KECCAK256(128 bytes))
//   + per-consumer inline shifts (uint8/uint16/uint24 + masks) ~ 6-12 gas each × 7 consumers ≈ 70-90 gas
//   + per-consumer % small modulo                              ~ 8 gas each × 7 consumers ≈ 56 gas
//   - SAVED: 5 entropyStep calls × ~20-30 g each = ~100-150 g per resolution
//   - SAVED: 1 dead L1585 entropyStep advance × ~20-30 g = ~25 g (WWXRP path; per Open Question 3)
//   + ETH-amount-second branch: + 1 hash2 keccak (~80 g) for seed2 chunk (Option A)
//
// Net per-open delta:
//   single-amount path:        +(80+90+56) - (100-150) - 25 = -(0..40) to +101 g  -> +20 to +80 g typical
//   ETH-amount-second branch:  same + (80 g for seed2)                            -> +100 to +160 g typical
//
// GAS-01 envelope: ±300 g per-open. Headroom 2× over theoretical worst case.
//
// ============================================================================
// REFERENCE-CAPTURE PROTOCOL
// ============================================================================
// On first run: test prints `[REF-CAPTURE] OPEN_LOOTBOX_GAS_REF = <gasNumber>`.
// Executor pins captured value into the literal constant below.
// Subsequent runs assert |measured - REF| <= ENTRY_POINT_DELTA_TOLERANCE = 2000 (compiler-codegen variance)
// AND (measured - REF) <= PER_OPEN_GAS_DELTA_BOUND = 300 (refactor envelope per GAS-01).

const PER_OPEN_GAS_DELTA_BOUND       = 300;       // GAS-01 ±300 g per-open
const ENTRY_POINT_DELTA_TOLERANCE    = 2000;      // ±2000 gas per-site tolerance vs pinned REF (codegen variance)
const OPEN_LOOTBOX_GAS_REF             = 0;       // executor-pinned post REF-CAPTURE first run
const OPEN_BURNIE_LOOTBOX_GAS_REF      = 0;       // executor-pinned post REF-CAPTURE first run
const RESOLVE_LOOTBOX_DIRECT_GAS_REF   = 0;       // executor-pinned post REF-CAPTURE first run
```

**Imports + fixture** (mirror `Phase264GasRegression.test.js` L112-120):

```javascript
import { loadFixture } from "@nomicfoundation/hardhat-toolbox/network-helpers.js";
import { expect } from "chai";
import hre from "hardhat";
import { deployFullProtocol, restoreAddresses } from "../helpers/deployFixture.js";
import { eth, advanceToNextDay, getEvents, getLastVRFRequestId } from "../helpers/testUtils.js";
```

**3 describe blocks** — one each for `openLootBox` / `openBurnieLootBox` / `resolveLootboxDirect`. Each block:

1. Loads protocol fixture, advances to a day with VRF resolved.
2. Captures gas via `tx.gasUsed` for the entry-point invocation.
3. On first run (REF == 0): prints `[REF-CAPTURE] OPEN_LOOTBOX_GAS_REF = <measured>`; this.skip() with a clear message asking executor to pin the value.
4. On subsequent runs: asserts both bounds (`|measured - REF| <= ENTRY_POINT_DELTA_TOLERANCE` AND `(measured - REF) <= PER_OPEN_GAS_DELTA_BOUND`).

**Test execution flow:** the FIRST `npx hardhat test test/gas/LootboxOpenGas.test.js` run after Wave 1 contract refactor lands prints REF values for all 3 entry points and skips assertion. The executor edits the literal constants in this file (3 numeric pinnings) and re-runs. On the SECOND run, the assertions execute and the test passes.

**Per `feedback_gas_worst_case.md`:** derived theoretical worst case FIRST in the header (above), THEN test. The PER_OPEN_GAS_DELTA_BOUND = 300 envelope is 2× the theoretical worst case (160 g typical for the ETH-amount-second branch); explicit 2× headroom documented.

DO NOT COMMIT yet. Wave 2 batches at Task 10.

**Note on `_lootboxDgnrsReward` and `_rollLootboxBoons` views:** these are sub-call paths only reachable on certain `roll` outcomes. The 3 entry-point tests cover the full per-open envelope; sub-call cost is included in the entry-point measurement.
  </action>
  <verify>
    <automated>npx hardhat test test/gas/LootboxOpenGas.test.js 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - File `test/gas/LootboxOpenGas.test.js` exists.
    - `grep -cE "PER_OPEN_GAS_DELTA_BOUND\s*=\s*300" test/gas/LootboxOpenGas.test.js` returns >= 1.
    - `grep -cE "ENTRY_POINT_DELTA_TOLERANCE\s*=\s*2000" test/gas/LootboxOpenGas.test.js` returns >= 1.
    - `grep -cE "OPEN_LOOTBOX_GAS_REF|OPEN_BURNIE_LOOTBOX_GAS_REF|RESOLVE_LOOTBOX_DIRECT_GAS_REF" test/gas/LootboxOpenGas.test.js` returns >= 3 (3 REF constants).
    - `grep -cE "THEORETICAL WORST-CASE DERIVATION" test/gas/LootboxOpenGas.test.js` returns >= 1 (header per `feedback_gas_worst_case.md`).
    - `grep -cE "REF-CAPTURE" test/gas/LootboxOpenGas.test.js` returns >= 1 (REF-CAPTURE protocol comment).
    - `grep -cE "describe\(" test/gas/LootboxOpenGas.test.js` returns >= 3 (3 entry-point describes).
    - On first run: prints `[REF-CAPTURE] OPEN_LOOTBOX_GAS_REF = <number>` for all 3 entry points; tests skip with clear pinning message.
    - On second run (after pinning): all 3 describe blocks pass assertion.
  </acceptance_criteria>
  <done>GAS-01 test file authored; theoretical worst-case derived in header per `feedback_gas_worst_case.md`; REF-CAPTURE protocol with 3 placeholder constants; first-run prints REF values for executor pinning; second-run asserts envelope; no commit.</done>
</task>

<task type="auto">
  <name>Task 8 [Wave 2]: Extend `test/gas/AdvanceGameGas.test.js` with v36.0 1.99× margin describe block</name>
  <files>test/gas/AdvanceGameGas.test.js</files>
  <read_first>
    - test/gas/AdvanceGameGas.test.js (lines 1-100 — existing structure; existing v35.0 1.99× margin describe block from Phase 264 SURF-05)
    - .planning/phases/266-lootbox-entropy-refactor/266-RESEARCH.md (lines 86-87 — GAS-02 envelope ±2K; lines 117-118 — v36.0 1.99× margin extension)
    - .planning/phases/266-lootbox-entropy-refactor/266-PATTERNS.md (lines 405-422 — existing test structure pattern)
  </read_first>
  <action>
Extend `test/gas/AdvanceGameGas.test.js` with a v36.0 describe block adjacent to the existing Phase 264 v35.0 1.99× margin block. The extension asserts that the lootbox-entropy refactor preserves the advanceGame per-day gas envelope within ±2K and the 1.99× margin from v34/v35 SURF-05 baseline.

**Decimator settlement is the one advanceGame-resident lootbox-resolution caller** per CONTEXT.md `<domain>` and ROADMAP success criterion 4. The v36.0 describe block measures the day-end advanceGame stages (specifically those triggering Decimator settlement / lootbox resolution) and asserts:

1. `advanceGame` per-stage gas remains within ±2K of the pre-refactor v35.0 baseline (carried over from `Phase264GasRegression.test.js` advanceGame harness, or re-measured if stage-specific REF constants are needed).
2. ≥ 1.99× margin from the block-gas-limit / pool-budget remains preserved.

**Pattern** (mirror PATTERNS.md L405-422):

```javascript
describe("v36.0 — AdvanceGame Gas Envelope (Phase 266 lootbox-entropy refactor)", function () {
  this.timeout(600_000);

  it("decimator-settlement-stage gas within ±2K of v35.0 baseline (GAS-02)", async function () {
    const ADVANCE_GAME_DECIMATOR_STAGE_REF = 0;  // executor-pinned post REF-CAPTURE first run
    const STAGE_DELTA_TOLERANCE_GAS_02 = 2000;   // GAS-02 ±2K envelope

    // ... measurement: advance to a day where Decimator settlement triggers; capture stage gasUsed ...
    // ... if REF == 0: print [REF-CAPTURE] ADVANCE_GAME_DECIMATOR_STAGE_REF = <measured>; this.skip() ...
    // ... else: expect(Math.abs(measured - REF) <= STAGE_DELTA_TOLERANCE_GAS_02).to.equal(true) ...
  });

  it("advanceGame total per-day envelope ≥ 1.99× margin preserved at v36.0 HEAD", async function () {
    // Mirror Phase 264 SURF-05 1.99× margin assertion (measured 9.42× at HEAD `5db8682b` per AdvanceGameGas.test.js).
    // v36.0 expectation: lootbox-entropy refactor delta is bounded; margin shifts by < 0.01× (refactor saves
    // ~100-150 g per lootbox open × handful of opens per advanceGame × 1 advanceGame day = trivial).
    // ... assert measured ratio >= 1.99 ...
  });
});
```

Existing v35.0 describe block stays unchanged (REG-01 carry-forward verified by SURF-04 byte-identity at v36 close — JackpotModule UNCHANGED).

DO NOT COMMIT. Wave 2 batches at Task 10.
  </action>
  <verify>
    <automated>npx hardhat test test/gas/AdvanceGameGas.test.js 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "describe\(\"v36\.0|v36\.0 — AdvanceGame|Phase 266 lootbox-entropy refactor" test/gas/AdvanceGameGas.test.js` returns >= 1 (new v36.0 describe block present).
    - `grep -cE "STAGE_DELTA_TOLERANCE_GAS_02\s*=\s*2000|GAS-02 ±2K" test/gas/AdvanceGameGas.test.js` returns >= 1 (GAS-02 envelope constant).
    - `grep -cE "1\.99|1\.99×|1\.99x" test/gas/AdvanceGameGas.test.js` returns >= 1 (margin assertion present).
    - Existing v35.0 / Phase 264 describe block UNTOUCHED — REG-01 carry-forward; verify via `git diff HEAD~ -- test/gas/AdvanceGameGas.test.js` shows only ADD lines (no `-` deletions inside the v35.0 describe block).
    - `npx hardhat test test/gas/AdvanceGameGas.test.js` exits 0 (existing v35.0 block + new v36.0 block both pass; first-run REF-capture path prints values + skips per pattern).
  </acceptance_criteria>
  <done>v36.0 1.99× margin describe block added to AdvanceGameGas.test.js alongside existing v35.0 block; GAS-02 envelope ±2K asserted; existing v35.0 block byte-identical (REG-01 carry); test green; no commit.</done>
</task>

<task type="auto">
  <name>Task 9 [Wave 2]: Extend `test/stat/SurfaceRegression.test.js` with v36.0 SURF-01..04 describe block + PROTECTED_RANGES</name>
  <files>test/stat/SurfaceRegression.test.js</files>
  <read_first>
    - test/stat/SurfaceRegression.test.js (lines 1-50 — imports + baselines + path constants; lines 249-405 — existing v35.0 SURF-01..04 describe block + per-line modified-set walk algorithm)
    - .planning/phases/266-lootbox-entropy-refactor/266-RESEARCH.md (lines 460-489 — Pitfall 6 SURF-04 9-line inventory; lines 562-582 — Code Examples PROTECTED_RANGES pattern)
    - .planning/phases/266-lootbox-entropy-refactor/266-PATTERNS.md (lines 426-523 — extended pattern with PROTECTED_RANGES arrays + per-line modified-set walk)
  </read_first>
  <action>
Extend `test/stat/SurfaceRegression.test.js` with a v36.0 SURF-01..04 describe block adjacent to the existing Phase 264 v35.0 block. Reuse the per-line modified-set walk algorithm (single canonical pattern). The v36.0 block uses baseline `5db8682bd7b811437f0c1cf47e832619d1478ac6` and 4 PROTECTED_RANGES arrays.

**Pattern** (mirror PATTERNS.md L426-523):

```javascript
describe("v36.0 SURF-01..04 — protected ranges byte-identical vs v35.0 baseline 5db8682b", function () {
  this.timeout(120_000);

  const V35_BASELINE = "5db8682bd7b811437f0c1cf47e832619d1478ac6";
  const ENTROPY_PATH = "contracts/libraries/EntropyLib.sol";
  const JACKPOT_PATH = "contracts/modules/DegenerusGameJackpotModule.sol";
  const MINT_PATH    = "contracts/modules/DegenerusGameMintModule.sol";

  // SURF-01: EntropyLib.sol body BYTE-IDENTICAL (whole file; ENT-04 stable API).
  const SURF_01_PROTECTED_RANGES = [
    { name: "EntropyLib.sol body L1-43 (SURF-01 — ENT-04 stable API)", lo: 1, hi: 43 },
  ];

  // SURF-02: BAF jackpot _jackpotTicketRoll body L2186-2229 (ENT-05 deferral verification).
  const SURF_02_PROTECTED_RANGES = [
    { name: "_jackpotTicketRoll body L2186-2229 (SURF-02 — ENT-05 deferral)", lo: 2186, hi: 2229 },
  ];

  // SURF-03: MintModule L652 single-line callsite.
  const SURF_03_PROTECTED_RANGES = [
    { name: "MintModule L652 EntropyLib.hash2(entropy, rollSalt) (SURF-03)", lo: 652, hi: 652 },
  ];

  // SURF-04: 9 non-lootbox JackpotModule EntropyLib callsites (verified inventory at HEAD 5db8682b
  //          per RESEARCH.md Pitfall 6 grep).
  const SURF_04_PROTECTED_RANGES = [
    { name: "L285 EntropyLib.hash2(rngWord, targetLvl)",                   lo: 285,  hi: 285  },
    { name: "L453 EntropyLib.hash2(randWord, lvl)",                        lo: 453,  hi: 453  },
    { name: "L532 EntropyLib.hash2(randWord, lvl)",                        lo: 532,  hi: 532  },
    { name: "L610 EntropyLib.hash2(randWord, lvl)",                        lo: 610,  hi: 610  },
    { name: "L612 EntropyLib.hash2(randWord, sourceLevel)",                lo: 612,  hi: 612  },
    { name: "L886 EntropyLib.hash2(randWord, lvl) (arg pos)",              lo: 886,  hi: 886  },
    { name: "L1176 EntropyLib.hash2(randWord, lvl)",                       lo: 1176, hi: 1176 },
    { name: "L1873 entropy = EntropyLib.hash2(entropy, s)",                lo: 1873, hi: 1873 },
    { name: "L2192 BAF entropy = EntropyLib.entropyStep(entropy)",         lo: 2192, hi: 2192 },
  ];

  // === Reuse the canonical per-line modified-set walk from existing v35.0 describe block ===
  // (Source: PATTERNS.md L474-523 — D-IMPL-11 soft-skip on unreachable baseline + fail-loud-on-empty-diff guard.)

  it("SURF-01 EntropyLib.sol body byte-identical vs 5db8682b", function () {
    walkAndAssert(V35_BASELINE, ENTROPY_PATH, SURF_01_PROTECTED_RANGES);
  });
  it("SURF-02 BAF _jackpotTicketRoll byte-identical vs 5db8682b", function () {
    walkAndAssert(V35_BASELINE, JACKPOT_PATH, SURF_02_PROTECTED_RANGES);
  });
  it("SURF-03 MintModule L652 byte-identical vs 5db8682b", function () {
    walkAndAssert(V35_BASELINE, MINT_PATH, SURF_03_PROTECTED_RANGES);
  });
  it("SURF-04 9 non-lootbox JackpotModule callsites byte-identical vs 5db8682b", function () {
    walkAndAssert(V35_BASELINE, JACKPOT_PATH, SURF_04_PROTECTED_RANGES);
  });
});
```

`walkAndAssert(baseline, path, ranges)` is the existing helper in the file (or factored out from the v35.0 describe block). If not yet factored, factor it now into a top-level function so v35.0 + v36.0 describes both call it with their own ranges. Per PATTERNS.md L497-523 it includes:
- D-IMPL-11 soft-skip on unreachable baseline (`git rev-parse --verify` fails -> this.skip())
- HEAD == BASELINE early-return ("trivially preserved")
- fail-loud on empty diff with HEAD ≠ baseline
- Per-line modified-set walk via diff hunk parsing
- Assert each protected line NOT in modifiedOldLines set

Existing v35.0 describe block stays unchanged.

DO NOT COMMIT. Wave 2 batches at Task 10.
  </action>
  <verify>
    <automated>npx hardhat test test/stat/SurfaceRegression.test.js 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "v36\.0 SURF-01\.\.04|5db8682bd7b811437f0c1cf47e832619d1478ac6" test/stat/SurfaceRegression.test.js` returns >= 2 (new describe block + V35_BASELINE constant).
    - `grep -cE "SURF_01_PROTECTED_RANGES|SURF_02_PROTECTED_RANGES|SURF_03_PROTECTED_RANGES|SURF_04_PROTECTED_RANGES" test/stat/SurfaceRegression.test.js` returns >= 4 (4 PROTECTED_RANGES arrays for v36.0).
    - SURF-04 array has exactly 9 entries: `grep -A 12 "SURF_04_PROTECTED_RANGES = \[" test/stat/SurfaceRegression.test.js | grep -cE "lo: [0-9]+, *hi: [0-9]+"` returns >= 9.
    - Existing v34.0/v35.0 describe blocks UNTOUCHED — verify via `git diff HEAD~ -- test/stat/SurfaceRegression.test.js` shows only ADD lines outside pre-existing describe blocks.
    - `npx hardhat test test/stat/SurfaceRegression.test.js` exits 0 (all SURF-01..04 v36.0 assertions pass — Wave 1 contract refactor scoped to LootboxModule only; EntropyLib + JackpotModule + MintModule byte-identical).
  </acceptance_criteria>
  <done>v36.0 SURF-01..04 describe block added with 4 PROTECTED_RANGES arrays + per-line modified-set walk; SURF-04 array has all 9 line entries per RESEARCH.md Pitfall 6 inventory; existing v35.0 describe block untouched; test green; no commit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 10 [Wave 2 GATE]: Wire NEW test files into npm scripts; present batched test diff to user; finalize Wave 2 ONLY after explicit "approved"</name>
  <files>package.json, test/stat/LootboxEntropyDistribution.test.js, test/gas/LootboxOpenGas.test.js, test/gas/AdvanceGameGas.test.js, test/stat/SurfaceRegression.test.js</files>
  <read_first>
    - package.json (current `scripts` section — `test:stat`, `test:gas` patterns from Phase 264 commit `833b341d`)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_contract_commits.md
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_batch_contract_approval.md
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_wait_for_approval.md
  </read_first>
  <action>
This is a checkpoint:human-verify task. Present the Wave 2 batched test diff (4 test files + package.json) to the user per the <what-built> block below; wait for explicit "approved" string before staging the test files + package.json and recording the entry with subject `test(266): chi\u00b2 + gas + surface preservation [STAT-01..03 + GAS-01..02 + SURF-01..04]` (or split per-file under the same approval gate). See <how-to-verify> for the npm-script verification checklist and <acceptance_criteria> for the approval-gate exit conditions. REF-CAPTURE pinning prerequisite: `test/gas/LootboxOpenGas.test.js` first run prints REF values; executor pins the 3 numeric constants before this gate per `feedback_gas_worst_case.md`. Agent does NOT pre-approve per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md`.
  </action>
  <verify>
    <automated>npm run test:stat 2>&amp;1 | tail -5 &amp;&amp; npm run test:gas 2>&amp;1 | tail -5</automated>
  </verify>
  <what-built>
Wave 2 test artifacts batched diff covering Tasks 6-9 + package.json wiring:
- `test/stat/LootboxEntropyDistribution.test.js` (NEW) — STAT-01..03 chi² over 6 buckets + STAT-02 distribution-shape + STAT-03 infra-reuse sanity.
- `test/gas/LootboxOpenGas.test.js` (NEW) — GAS-01 per-open ±300 g + theoretical-worst-case header + REF-CAPTURE protocol.
- `test/gas/AdvanceGameGas.test.js` (EXTENDED) — v36.0 1.99× margin describe block alongside existing v35.0 block.
- `test/stat/SurfaceRegression.test.js` (EXTENDED) — v36.0 SURF-01..04 describe block + 4 PROTECTED_RANGES arrays.
- `package.json` (MODIFIED) — wire `test/stat/LootboxEntropyDistribution.test.js` + `test/gas/LootboxOpenGas.test.js` into existing `test:stat` / `test:gas` script patterns (mirror `833b341d  chore(264-02): wire Phase 264 test files into npm scripts`).

**REF pinning prerequisite:** `test/gas/LootboxOpenGas.test.js` first run prints `[REF-CAPTURE] OPEN_LOOTBOX_GAS_REF = <N>` for 3 entry points; executor pins the 3 numeric constants in the file before this gate (per `feedback_gas_worst_case.md` REF-CAPTURE protocol). Same for AdvanceGameGas.test.js v36.0 stage REF if applicable.
  </what-built>
  <how-to-verify>
1. Run `npm run test:stat`
   - Expected: includes `LootboxEntropyDistribution.test.js`; all 7 describe blocks pass; existing Phase 264 stat tests still pass.
2. Run `npm run test:gas`
   - Expected: includes `LootboxOpenGas.test.js`; per-open assertions pass (post-pin); `AdvanceGameGas.test.js` v36.0 block passes; existing Phase 264 gas tests still pass.
3. Run `npx hardhat test test/stat/SurfaceRegression.test.js`
   - Expected: v36.0 SURF-01..04 describe + existing v34.0/v35.0 blocks all pass.
4. Run `git diff --stat 5db8682b..HEAD -- test/ package.json`
   - Expected: 4 NEW + EXTENDED test files + 1 modified package.json.
5. Run `git diff 5db8682b..HEAD -- contracts/`
   - Expected: ONLY `contracts/modules/DegenerusGameLootboxModule.sol` (Wave 1 commit) — Wave 2 should add ZERO new contract changes.
6. Review test diff: `git diff 5db8682b..HEAD -- test/ package.json | less`
   - Verify chi² infrastructure verbatim re-declaration (STAT-03).
   - Verify GAS-01 theoretical-worst-case header populated.
   - Verify SURF-04 PROTECTED_RANGES has all 9 line entries.
   - Verify package.json wiring follows `833b341d` pattern.

**Per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md`:** the agent does NOT pre-approve this finalization. Wait for the user's explicit `approved` string before staging the test files + package.json and recording them with subject `test(266): chi² + gas + surface preservation [STAT-01..03 + GAS-01..02 + SURF-01..04]` (or the executor may split into a per-file batch if smaller commits are preferred — agent decision under same approval gate).

If the user requests changes, return to the relevant Task 6-9 and revise; re-present the diff at this gate.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize finalizing Wave 2 test commits, or describe required changes.</resume-signal>
  <acceptance_criteria>
    - User responds with explicit `approved` string before any commit runs.
    - After approval: `git log --oneline 5db8682b..HEAD -- test/ package.json` shows test commit(s) with `test(266):` or `chore(266):` subject prefix.
    - `git log --name-only 5db8682b..HEAD -- contracts/` shows ONLY the Wave 1 LootboxModule commit (Wave 2 has zero contract changes).
    - `npm run test:stat && npm run test:gas` exit 0.
  </acceptance_criteria>
  <done>User approved batched Wave 2 test diff; test commit(s) landed; STAT-01..03 + GAS-01..02 + SURF-01..04 satisfied at HEAD; Wave 2 closed.</done>
</task>

</content>

<!-- ================================================================ -->
<!-- WAVE 3 — AUDIT DELIVERABLE §1-§4 + ADVERSARIAL PASS               -->
<!-- (AGENT-COMMITTED atomic per task)                                 -->
<!-- ================================================================ -->

<task type="auto">
  <name>Task 11 [Wave 3]: Author `audit/FINDINGS-v36.0.md` §1 frontmatter + §2 Executive Summary skeleton</name>
  <files>audit/FINDINGS-v36.0.md</files>
  <read_first>
    - audit/FINDINGS-v35.0.md (lines 1-91 — §1 frontmatter + §2 Executive Summary template — primary copy source)
    - audit/FINDINGS-v34.0.md (lines 1-115 — secondary template precedent)
    - .planning/phases/266-lootbox-entropy-refactor/266-CONTEXT.md (D-266-FILES-01 + D-266-CLOSURE-01..02)
    - .planning/phases/266-lootbox-entropy-refactor/266-PATTERNS.md (lines 533-562 — frontmatter pattern; lines 564-575 — section structure pattern)
  </read_first>
  <action>
Create `audit/FINDINGS-v36.0.md` with §1 frontmatter + §2 Executive Summary. Copy structural skeleton from `audit/FINDINGS-v35.0.md` L1-91; substitute v35→v36 milestone IDs.

**§1 YAML frontmatter** (per PATTERNS.md L535-560 — fill `<sha>` placeholders; closure_signal SHA stays as literal `<sha>` string until Task 20 atomic-update per RESEARCH.md Pitfall 4):

```yaml
---
phase: 266-lootbox-entropy-refactor
plan: 01
milestone: v36.0
milestone_name: Lootbox-Path Entropy Refactor
head_anchor: <sha>
audit_baseline: 5db8682bd7b811437f0c1cf47e832619d1478ac6
audit_baseline_signal: MILESTONE_V35_AT_HEAD_5db8682bd7b811437f0c1cf47e832619d1478ac6
v34_baseline: 6b63f6d4daf346a53a1d463790f637308ea8d555
v34_baseline_signal: MILESTONE_V34_AT_HEAD_6b63f6d4daf346a53a1d463790f637308ea8d555
deliverable: audit/FINDINGS-v36.0.md
requirements: [ENT-01, ENT-02, ENT-03, ENT-04, ENT-05, ENT-06,
               STAT-01, STAT-02, STAT-03,
               GAS-01, GAS-02,
               SURF-01, SURF-02, SURF-03, SURF-04,
               AUDIT-01, AUDIT-02, AUDIT-03, AUDIT-04, AUDIT-05,
               REG-01, REG-02, REG-03, REG-04]
phase_status: terminal
write_policy: "Phase 266 introduces 1 batched contract commit (LootboxModule refactor) + N batched test commits (chi² + gas + surface), all USER-APPROVED per feedback_no_contract_commits.md + feedback_batch_contract_approval.md. KNOWN-ISSUES.md modified by 1 entry per AUDIT-05 (EntropyLib XOR-shift entry rephrased to BAF-jackpot-only scope). Audit deliverable + 266-01-ADVERSARIAL-LOG + 266-01-SUMMARY + ROADMAP/STATE/MILESTONES updates AGENT-COMMITTED atomic-per-task."
supersedes: none
status: FINAL — READ-ONLY
read_only: true
closure_signal: MILESTONE_V36_AT_HEAD_<sha>
generated_at: 2026-05-10T<HH>:<MM>:<SS>Z
---
```

**§2 Executive Summary** — copy structural skeleton from `audit/FINDINGS-v35.0.md` §2 (L32-91). Includes:
- Closure Verdict Summary (placeholder text — final-update at Task 19/20)
- Severity Counts (default per RESEARCH.md Assumption A6 + D-265-FIND-01 carry: CRITICAL: 0, HIGH: 0, MEDIUM: 0, LOW: 0, INFO: 0; total_F36NN: 0)
- D-08 5-bucket severity rubric reference (carry-forward from D-265-SEV-01)
- D-09 KI gating rubric reference
- Forward-Cite Closure Summary (placeholder — finalized at Task 18)
- Attestation Anchor (placeholder `<sha>` — resolved at Task 20)

**Section header** for §2: `## 2. Executive Summary` (per `grep -nE '^## [0-9]\.' audit/FINDINGS-v35.0.md` shape).

NO §3..§9 yet — those land in subsequent tasks. Reserve placeholder sections at the bottom of the file with stub headers (e.g., `## 3. Per-Phase Sections [populated by Task 12]`) so that Task 12-19 fill in via Edit operations.

Commit subject: `audit(266): Task 11 — §1 frontmatter + §2 Executive Summary skeleton`. Atomic commit at end of this task per D-266-APPROVAL-01 agent-committed audit-artifacts pattern (D-265-APPROVAL-01 carry).
  </action>
  <verify>
    <automated>grep -cE '^## 2\. Executive Summary' audit/FINDINGS-v36.0.md</automated>
  </verify>
  <acceptance_criteria>
    - File `audit/FINDINGS-v36.0.md` exists.
    - `head -1 audit/FINDINGS-v36.0.md` returns `---` (YAML frontmatter present).
    - `grep -c "phase: 266-lootbox-entropy-refactor" audit/FINDINGS-v36.0.md` returns >= 1.
    - `grep -c "MILESTONE_V36_AT_HEAD_" audit/FINDINGS-v36.0.md` returns >= 1 (closure_signal frontmatter field with `<sha>` placeholder).
    - `grep -c "MILESTONE_V35_AT_HEAD_5db8682b" audit/FINDINGS-v36.0.md` returns >= 1 (baseline signal in frontmatter).
    - `grep -c "requirements:" audit/FINDINGS-v36.0.md` returns >= 1; the requirements list contains all 24 IDs (`grep -cE "ENT-0[1-6]|STAT-0[1-3]|GAS-0[1-2]|SURF-0[1-4]|AUDIT-0[1-5]|REG-0[1-4]"` returns >= 24).
    - `grep -cE "^## 2\. Executive Summary" audit/FINDINGS-v36.0.md` returns 1.
    - `grep -cE "CRITICAL: 0|HIGH: 0|MEDIUM: 0|LOW: 0|INFO: 0" audit/FINDINGS-v36.0.md` returns 5 (default severity counts per Assumption A6).
    - Atomic commit landed: `git log --oneline -1 -- audit/FINDINGS-v36.0.md` shows subject `audit(266): Task 11 — §1 frontmatter + §2 Executive Summary skeleton`.
  </acceptance_criteria>
  <done>§1 frontmatter + §2 Executive Summary skeleton authored with all 24 requirement IDs + default zero severity counts + closure_signal placeholder; atomic-per-task commit landed.</done>
</task>

<task type="auto">
  <name>Task 12 [Wave 3]: Author §3a Phase 266 per-phase + §3d AUDIT-01 delta-surface table + §3d Part C AUDIT-04 zero-new-state + §3e AUDIT-03 conservation re-proof</name>
  <files>audit/FINDINGS-v36.0.md</files>
  <read_first>
    - audit/FINDINGS-v35.0.md (lines 93-261 — §3 per-phase sections + §3d AUDIT-01 delta-surface table + §3e AUDIT-03 conservation re-proof — primary template)
    - .planning/phases/266-lootbox-entropy-refactor/266-PATTERNS.md (lines 577-590 — §3d AUDIT-01 delta-surface table pattern)
    - .planning/phases/266-lootbox-entropy-refactor/266-RESEARCH.md (lines 91-93 — AUDIT-01..04 success criteria mapping; lines 109-118 — Architectural Responsibility Map)
    - contracts/modules/DegenerusGameLootboxModule.sol (HEAD — for live line numbers post-refactor; baseline `5db8682b` for comparison)
    - .planning/phases/266-lootbox-entropy-refactor/266-01-PLAN.md frontmatter `entropy_step_callsites` + `entropy_modulo_consumers` (consumer inventory)
  </read_first>
  <action>
Append §3 to `audit/FINDINGS-v36.0.md`. Three subsections:

**§3a Phase 266 per-phase section.** Single phase (no §3a/3b/3c per-phase split — Phase 266 is its own only-phase per CONTEXT.md `<decisions>` Claude's Discretion + RESEARCH.md Architectural Responsibility Map). Cross-cite to:
- `.planning/phases/266-lootbox-entropy-refactor/266-CONTEXT.md` (locked decisions D-266-NN)
- `.planning/phases/266-lootbox-entropy-refactor/266-RESEARCH.md` (bit-budget table + chi² calibration)
- `.planning/phases/266-lootbox-entropy-refactor/266-PATTERNS.md` (refactor pattern + analog source)
- Wave 1 commit `feat(266): lootbox-path entropy refactor [ENT-01..06]` (recall actual SHA via `git log --oneline 5db8682b..HEAD -- contracts/`)
- Wave 2 commits `test(266): ...` (recall actual SHAs via `git log --oneline 5db8682b..HEAD -- test/ package.json`)

Change-count card + per-REQ summary table covering 24 requirement IDs. Pattern per `audit/FINDINGS-v35.0.md` §3a (carry-adapted).

**§3d AUDIT-01 delta-surface table.** Mirror `audit/FINDINGS-v35.0.md` §3d Part A. Enumerate every changed declaration in `contracts/modules/DegenerusGameLootboxModule.sol` vs baseline `5db8682b` with hunk-level evidence + classification.

Use the markdown table pattern from PATTERNS.md L580-588:

```markdown
| Declaration | Classification | Live Line(s) at HEAD | Hunk Evidence | Phase 266 REQ |
|---|---|---|---|---|
| `_rollTargetLevel(uint24 baseLevel, uint256 seed)` | MODIFIED_LOGIC (signature change: drop `nextEntropy` return; switch to bit-sliced reads) | live L<NEW>-<NEW> | `git diff 5db8682b..HEAD ...` shows L809-827 hunk | ENT-01 |
| `_resolveLootboxRoll(... uint256 seed)` | MODIFIED_LOGIC (4 entropyStep callsites replaced with bit-slices; L1585 dead WWXRP advance DELETED entirely) | live L<NEW>-<NEW> | hunk evidence | ENT-02 |
| `_lootboxTicketCount(... uint256 seed)` | MODIFIED_LOGIC (L1635 entropyStep replaced with `uint24(seed >> 96) % 10_000`) | live L<NEW>-<NEW> | hunk evidence | ENT-03 |
| `_lootboxDgnrsReward(uint256 amount, uint256 entropy)` | MODIFIED_LOGIC (L1680 sub-call slice updated to `uint24(entropy >> 56) % 1000` per RESEARCH.md Pitfall 3) | live L<NEW>-<NEW> | hunk evidence | ENT-02 (sub-call) |
| `_rollLootboxBoons(uint256 amount, uint256 entropy)` | MODIFIED_LOGIC (L1059 sub-call slice updated to `uint32(seed >> 120) % BOON_PPM_SCALE` per Pitfall 3) | live L<NEW>-<NEW> | hunk evidence | ENT-02 (sub-call) |
| `_resolveLootboxCommon(... uint256 seed)` parameter rename + entry-point seed plumbing | REFACTOR_ONLY (parameter rename `entropy` → `seed`; `seed = uint256(keccak256(abi.encode(rngWord, player, day, amount)))` preserved per RESEARCH.md Open Question 2) | live L<NEW>-<NEW> | hunk evidence | ENT-02 / ENT-06 |
| `openLootBox` + `openBurnieLootBox` + `resolveLootboxDirect` + `resolveRedemptionLootbox` callsite plumbing | REFACTOR_ONLY (4 callers update local `entropy` → `seed`; thread into `_resolveLootboxCommon`) | live L<NEW>-<NEW> | hunk evidence | ENT-01..03 caller-side |
| `seed2 = EntropyLib.hash2(seed, 1)` ETH-amount-second branch chunk | NEW (counter-tagged second seed per RESEARCH.md Pitfall 2 Option A) | live L<NEW>-<NEW> | hunk evidence | ENT-02 / AUDIT-02 surface (c) |
| Inline NatSpec bit-budget block (per refactored function) | NEW | live (NatSpec) | hunk evidence | ENT-06 |
| `EntropyLib.entropyStep` callsites in lootbox path (7 sites: L813, L817, L1548, L1569, L1585, L1599, L1635) | DELETED | n/a (removed) | `git diff 5db8682b..HEAD -- contracts/modules/DegenerusGameLootboxModule.sol \| grep "^-.*entropyStep"` shows 7 deletions | ENT-01..03 |
| L1585 WWXRP-path dead `entropyStep` advance | DELETED (RESEARCH.md Open Question 3 + Assumption A3 + `feedback_no_dead_guards.md`) | n/a (removed) | hunk evidence | ENT-02 / `feedback_no_dead_guards` |
| `nextEntropy` return contract on `_rollTargetLevel` / `_resolveLootboxRoll` / `_lootboxTicketCount` | DELETED (signature change; seed plumbing replaces nextEntropy threading) | n/a (removed) | hunk evidence | ENT-01..03 |
| `EntropyLib.sol` body | REFACTOR_ONLY (zero changes — file byte-identical) | L1-43 | `git diff 5db8682b..HEAD -- contracts/libraries/EntropyLib.sol` returns empty | ENT-04 / SURF-01 |
```

Plus Part B grep recipe (downstream-caller inventory):
```
grep -rn "_rollTargetLevel|_resolveLootboxRoll|_lootboxTicketCount|_lootboxDgnrsReward|_rollLootboxBoons|EntropyLib\.entropyStep" contracts/
```

**§3d Part C AUDIT-04 zero-new-state attestation.** 5 grep-reproducible checks per `audit/FINDINGS-v35.0.md` §3d Part C (L236-247):
1. Storage-slot scan: `git diff 5db8682b..HEAD --stat -- contracts/storage/ contracts/modules/DegenerusGameLootboxModule.sol` (expect: only DegenerusGameLootboxModule.sol shows changes).
2. GameStorage byte-identity: `git diff 5db8682b..HEAD -- contracts/storage/GameStorage.sol` (expect: empty).
3. Zero-new-public-fn grep: `git diff 5db8682b..HEAD -- contracts/ | grep -E '^\+.*function .* (public|external)'` (expect: zero hits).
4. Zero-new-modifier grep: `git diff 5db8682b..HEAD -- contracts/ | grep -E '^\+.*modifier '` (expect: zero hits).
5. EntropyLib API stable (ENT-04): `git diff 5db8682b..HEAD -- contracts/libraries/EntropyLib.sol` (expect: empty).

Each check prose: "PASS" verdict + grep recipe + line cite.

**§3e AUDIT-03 conservation re-proof.** Mirror `audit/FINDINGS-v35.0.md` §3e. Three invariants:
1. **Lootbox payout uniformity preserved** — STAT-01 chi² empirical proof at `test/stat/LootboxEntropyDistribution.test.js` (6 buckets) confirms uniformly distributed draws across all 8 sub-roll consumers. Pre-refactor (xorshift) and post-refactor (bit-sliced keccak) BOTH produce uniform draws within statistical tolerance per CONTEXT.md `<decisions>` "Behavioral equivalence — NOT required". No payout direction change; only the specific concrete winners shift.
2. **No new mint sites** — `git diff 5db8682b..HEAD -- contracts/ | grep -E '^\+.*mintForGame|burnieMint|coinflip\.creditFlip'` returns ZERO new mint paths. Refactor exercises only pre-existing payout call paths.
3. **Solvency invariant preserved** — `claimablePool ≤ ETH balance + stETH balance` UNCHANGED. The lootbox-entropy refactor does not interact with ETH/stETH balance variables. No `_payEth` / `_payoutWithStethFallback` callsites added or modified.

Each invariant: SAFE verdict + grep recipe + line cite + prose.

Atomic commit at end of this task: `audit(266): Task 12 — §3a + §3d AUDIT-01/04 + §3e AUDIT-03 conservation`.
  </action>
  <verify>
    <automated>grep -cE '^### 3[ade]\.' audit/FINDINGS-v36.0.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^## 3\." audit/FINDINGS-v36.0.md` returns 1.
    - `grep -cE "AUDIT-01|delta-surface|MODIFIED_LOGIC|REFACTOR_ONLY|DELETED|NEW" audit/FINDINGS-v36.0.md` returns >= 10 (delta-surface table populated).
    - `grep -cE "AUDIT-04|zero-new-state|GameStorage" audit/FINDINGS-v36.0.md` returns >= 3 (Part C zero-new-state attestation).
    - `grep -cE "AUDIT-03|conservation|claimablePool|mintForGame" audit/FINDINGS-v36.0.md` returns >= 3 (§3e conservation re-proof).
    - `grep -cE "ENT-01|ENT-02|ENT-03|ENT-04|ENT-05|ENT-06" audit/FINDINGS-v36.0.md` returns >= 6 (every ENT requirement cross-cited).
    - `grep -cE "L1585|dead WWXRP|feedback_no_dead_guards" audit/FINDINGS-v36.0.md` returns >= 2 (DELETED row for L1585 dead advance present).
    - Atomic commit landed: `git log --oneline -1 -- audit/FINDINGS-v36.0.md` shows subject containing `Task 12`.
  </acceptance_criteria>
  <done>§3a Phase 266 + §3d AUDIT-01 delta-surface table + §3d Part C AUDIT-04 zero-new-state + §3e AUDIT-03 conservation re-proof authored with hunk-level evidence; atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 13 [Wave 3]: Author §4 inline 6-surface adversarial-sweep draft (a-f) — pre-adversarial-pass</name>
  <files>audit/FINDINGS-v36.0.md</files>
  <read_first>
    - audit/FINDINGS-v35.0.md (lines 264-318 — §4 6-surface row table — primary template)
    - .planning/phases/266-lootbox-entropy-refactor/266-PATTERNS.md (lines 593-602 — §4 6-surface table pattern)
    - .planning/phases/266-lootbox-entropy-refactor/266-01-PLAN.md frontmatter `adversarial_surfaces` (a-f expected verdicts + rationale prose)
    - .planning/phases/266-lootbox-entropy-refactor/266-RESEARCH.md (lines 91-93 — AUDIT-02 6-surface mapping; lines 612-614 — Assumption A6 surface row count)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_backward_trace.md
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_commitment_window.md
  </read_first>
  <action>
Append §4 to `audit/FINDINGS-v36.0.md`. Author the FULL 6-surface inline draft FIRST per D-266-ADVERSARIAL-02 / RESEARCH.md Pitfall 5. The adversarial-pass at Task 14 red-teams this finished draft — do NOT spawn skills mid-draft.

**§4 structure** (mirror `audit/FINDINGS-v35.0.md` §4 + PATTERNS.md L591-602):

- §4.0 default zero F-36-NN finding-block disclosure: "Per RESEARCH.md Assumption A6 + D-265-FIND-01 carry, default expected severity counts CRITICAL/HIGH/MEDIUM/LOW/INFO = 0/0/0/0/0; total F-36-NN = 0. If the §4 6-surface inline draft (below) or adversarial-pass at Task 14 surfaces a FINDING_CANDIDATE, this section will be updated to add an F-36-NN block."
- §4.1 Adversarial Sweep 6-Surface Table — populate from frontmatter `adversarial_surfaces` array:

```markdown
**Surface (a) — Bit-slice modulo-bias bound per draw within documented bound.**

- **Verdict:** SAFE_BY_DESIGN
- **Grep recipe / line cite:** Bit-budget table at §3a; per-consumer slice + bias documented inline (NatSpec) per ENT-06; STAT-01 chi² cross-cite at `test/stat/LootboxEntropyDistribution.test.js` (6 buckets — `% 100` Z<1.645 df=99 N=10K; `% 5` chi²<9.488 df=4 N=5K; `% 46` Z<1.645 df=45 N=10K; `% 20` path Z<1.645 df=19 N=10K; `% 20` variance Z<1.645 df=19 N=5K; `% 10000` Z<1.645 df=9999 N=100K).
- **Prose justification:** Per D-266-BIT-BUDGET-01: every `% small` slice has documented bias ≤ 1% (max 0.39% at `% 5` from 8 bits; min 0.0024% at `% 1000` from 24 bits). Backward-trace per `feedback_rng_backward_trace.md`: each consumer ultimately reads VRF-derived bits via the entry-point keccak `keccak256(abi.encode(rngWord, player, day, amount))`; rngWord is post-VRF-fulfillment-unknown to player. Empirical chi² evidence at STAT-01 covers uniformity end-to-end.
```

Repeat for surfaces (b), (c), (d), (e), (f) per the frontmatter `adversarial_surfaces` array — each with Verdict + Grep recipe / line cite + Prose justification. Verbatim copy from frontmatter; no rewriting.

- §4.2 Verdict Roll-Up:

```markdown
| Surface | Verdict |
|---------|---------|
| (a) Bit-slice modulo-bias bound | SAFE_BY_DESIGN |
| (b) Seed-reuse cross-correlation | SAFE_BY_DESIGN |
| (c) hash2(seed, N) chunk-collision-free | SAFE_BY_DESIGN |
| (d) Gas-griefing delta bounded | SAFE_BY_DESIGN |
| (e) BAF byte-identity (ENT-05 verification) | SAFE_BY_STRUCTURAL_CLOSURE |
| (f) Commitment-window check | SAFE_BY_DESIGN |

**Roll-up:** 6 of 6 surfaces SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE; ZERO FINDING_CANDIDATE rows; ZERO F-36-NN finding blocks emitted at pre-adversarial-pass draft. Adversarial pass at Task 14 will validate.
```

Atomic commit at end of this task: `audit(266): Task 13 — §4 6-surface inline draft (a-f) — AUDIT-02 pre-adversarial-pass`.

**Per RESEARCH.md Pitfall 5:** the `/contract-auditor` + `/zero-day-hunter` adversarial pass MUST run AFTER this task lands. Do NOT spawn skills inside this task; the spawn is Task 14's responsibility.
  </action>
  <verify>
    <automated>grep -cE 'Surface \([a-f]\)' audit/FINDINGS-v36.0.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^## 4\." audit/FINDINGS-v36.0.md` returns 1.
    - `grep -cE "Surface \([a-f]\)" audit/FINDINGS-v36.0.md` returns >= 6 (all 6 surfaces drafted).
    - `grep -cE "SAFE_BY_DESIGN" audit/FINDINGS-v36.0.md` returns >= 5 (5 of 6 surfaces SAFE_BY_DESIGN).
    - `grep -cE "SAFE_BY_STRUCTURAL_CLOSURE" audit/FINDINGS-v36.0.md` returns >= 1 (surface (e) BAF byte-identity).
    - `grep -cE "feedback_rng_backward_trace|feedback_rng_commitment_window|feedback_gas_worst_case" audit/FINDINGS-v36.0.md` returns >= 3 (RNG-audit + gas-audit methodology cites).
    - `grep -cE "F-36-NN" audit/FINDINGS-v36.0.md` returns >= 1 (default zero F-36-NN block disclosure prose).
    - Atomic commit landed: `git log --oneline -1 -- audit/FINDINGS-v36.0.md` shows subject containing `Task 13` + `§4` + `AUDIT-02 pre-adversarial-pass`.
  </acceptance_criteria>
  <done>§4 inline 6-surface adversarial-sweep draft (a-f) authored with verdicts + grep cites + prose; pre-adversarial-pass commit landed; ready for Task 14 adversarial validation.</done>
</task>

<task type="auto">
  <name>Task 14 [Wave 3]: Adversarial pass — `/contract-auditor` + `/zero-day-hunter` parallel spawn AFTER §4 draft; populate `266-01-ADVERSARIAL-LOG.md`; surface disagreements inline</name>
  <files>.planning/phases/266-lootbox-entropy-refactor/266-01-ADVERSARIAL-LOG.md, audit/FINDINGS-v36.0.md</files>
  <read_first>
    - audit/FINDINGS-v36.0.md §4 (Task 13 output — full 6-surface draft to red-team)
    - .planning/phases/265-delta-audit-findings-consolidation/265-01-ADVERSARIAL-LOG.md (full file — adversarial-log shape template per PATTERNS.md L668-702)
    - .planning/phases/266-lootbox-entropy-refactor/266-CONTEXT.md (D-266-ADVERSARIAL-01..03 — methodology + disagreement disposition)
    - .planning/phases/266-lootbox-entropy-refactor/266-RESEARCH.md (lines 450-458 — Pitfall 5 sequential-after-draft mandate)
    - .claude/skills/contract-auditor/SKILL.md (project skill — adversarial review)
    - .claude/skills/zero-day-hunter/SKILL.md (project skill — novel-attack-surface review)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_wait_for_approval.md
  </read_first>
  <action>
**Step 1: Create the adversarial-log skeleton.** Mirror `265-01-ADVERSARIAL-LOG.md` per PATTERNS.md L674-702. Frontmatter + 2 H2 sections (`## /contract-auditor` and `## /zero-day-hunter`).

```markdown
# Phase 266 Plan 01 — Adversarial Validation Log

**Phase:** 266-lootbox-entropy-refactor
**Plan:** 266-01
**Target:** `audit/FINDINGS-v36.0.md` §4 6-surface draft (a-f)
**Methodology:** D-266-ADVERSARIAL-01..03 (parallel `/contract-auditor` + `/zero-day-hunter` spawn AFTER finished §4 draft; NOT `/economic-analyst`, NOT `/degen-skeptic`)
**Spawned:** 2026-05-10T<HH>:<MM>:<SS>Z

## /contract-auditor

[populated by parallel spawn output]

## /zero-day-hunter

[populated by parallel spawn output]
```

Initial atomic commit: `docs(266): Task 14 — adversarial-pass log skeleton`.

**Step 2: Spawn `/contract-auditor` + `/zero-day-hunter` IN PARALLEL in a single message** (per D-266-ADVERSARIAL-01..02 + RESEARCH.md Pitfall 5). Both red-team the FINISHED §4 draft (do NOT re-derive surfaces from scratch). Send a single tool-call batch invoking both skills with the same prompt:

```
PROMPT (verbatim — send to both /contract-auditor and /zero-day-hunter in parallel):

Phase 266 §4 inline 6-surface adversarial-sweep draft (a-f) is finished at `audit/FINDINGS-v36.0.md` §4. Red-team the FINISHED draft — do NOT re-derive from scratch. Tasks:

1. AGREE / DISAGREE per surface row (a..f). For each surface, cite:
   - Concrete code-path counterexample attempt against `contracts/modules/DegenerusGameLootboxModule.sol` (live HEAD body — refactored bit-sliced version)
   - Cross-cited evidence (file:line + grep recipe)
   - Mechanism-strength rating: STRUCTURAL / DESIGN / ASSUMPTIONAL / WEAK

2. 7th-surface novel composition candidates (NEW_VECTOR investigation):
   - Hypothesize at least 3 attack surfaces beyond (a)..(f)
   - For each: investigation + why-it-fails OR escalate as FINDING_CANDIDATE
   - Examples to consider (non-exhaustive): (g) re-entrancy via lootbox open + boon side-effects; (h) integer-overflow on cumulative bit shifts; (i) keccak256 input collision via abi.encode boundary edge cases; (j) gas-bomb via cold SLOAD warming pattern shift; (k) cross-resolution seed correlation (same player, same day, sequential opens — does seed collide?)

3. Concrete code-path counterexample audit:
   - Map every if/branch in `_rollTargetLevel` + `_resolveLootboxRoll` + `_lootboxTicketCount` + `_lootboxDgnrsReward` + `_rollLootboxBoons`
   - Assert no silent miscount, no off-by-one, no signed-vs-unsigned mask error

4. Final verdict block: "X of X row verdicts AGREE. Y FINDING_CANDIDATEs surfaced. ..."

Constraints:
- /contract-auditor: focus on contract-level invariants + exploitability + state machine.
- /zero-day-hunter: focus on novel composition + cross-feature interaction + edge-case slips.
- Both: cite `feedback_rng_backward_trace.md` + `feedback_rng_commitment_window.md` methodology.
- Both: log output verbatim under your respective H2 section in `266-01-ADVERSARIAL-LOG.md`.
- Phase 266 full context: read `.planning/phases/266-lootbox-entropy-refactor/266-CONTEXT.md` + `266-RESEARCH.md` + `266-PATTERNS.md` + `audit/FINDINGS-v36.0.md`.
```

**Step 3: Capture skill outputs.** Append each skill's verbatim response under its H2 section in `266-01-ADVERSARIAL-LOG.md`. Atomic commit per skill output: `audit(266): Task 14 — /contract-auditor adversarial pass output` + `audit(266): Task 14 — /zero-day-hunter adversarial pass output`.

**Step 4: Disagreement disposition (per D-266-ADVERSARIAL-03 + `feedback_wait_for_approval.md`).** If either skill:
- DISAGREES with a SAFE_* verdict on any surface (a-f), OR
- Surfaces a FINDING_CANDIDATE on a 7th surface (g, h, i, ...),

THEN: SURFACE INLINE TO USER as a checkpoint message before completing this task. Do NOT silently override the skill's flag. The user decides:
- Keep current verdict + add prose addressing the skill's concern
- Update verdict to FINDING_CANDIDATE + author F-36-NN block
- Add a 7th-surface row to §4
- Other disposition

User's disposition logged inline below the skill output in `266-01-ADVERSARIAL-LOG.md`. If `/contract-auditor` and `/zero-day-hunter` BOTH AGREE on all 6 surfaces with ZERO 7th-surface FINDING_CANDIDATEs, no user-checkpoint needed; record "Adversarial pass complete — zero disagreements" verbatim and proceed.

**Step 5: SPAWN-FAILED fallback** (per Phase 257 Task 7 + Phase 265 Task 7 retry-semantics): if either skill spawn returns no output OR errors, fall back to executor-manual adversarial pass — explicitly inhabit each skill's role, write the red-team analysis manually, log under the appropriate H2 section with `STATUS: SPAWN_FAILED — executor-manual fallback`. This preserves the audit-trail invariant.

**Step 6: Update §4 in `audit/FINDINGS-v36.0.md`** if any verdict changes per disagreement disposition. If zero disagreements, append a single-line attestation: "Adversarial pass complete (Task 14): /contract-auditor + /zero-day-hunter parallel spawn returned ZERO disagreements; 6 of 6 surfaces SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE; ZERO F-36-NN finding blocks." Cross-cite to `266-01-ADVERSARIAL-LOG.md`.

Final atomic commit for this task: `audit(266): Task 14 — adversarial pass complete (/contract-auditor + /zero-day-hunter, N disagreements)`.
  </action>
  <verify>
    <automated>grep -E '^## /contract-auditor|^## /zero-day-hunter' .planning/phases/266-lootbox-entropy-refactor/266-01-ADVERSARIAL-LOG.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/266-lootbox-entropy-refactor/266-01-ADVERSARIAL-LOG.md` exists.
    - `grep -E '^## /contract-auditor|^## /zero-day-hunter' .planning/phases/266-lootbox-entropy-refactor/266-01-ADVERSARIAL-LOG.md | wc -l` returns 2 (both H2 sections present).
    - `grep -cE '^## /economic-analyst|^## /degen-skeptic' .planning/phases/266-lootbox-entropy-refactor/266-01-ADVERSARIAL-LOG.md` returns 0 (per D-266-ADVERSARIAL-01: NOT spawning these skills).
    - Each H2 section is populated (at least 20 lines per skill OR `STATUS: SPAWN_FAILED — executor-manual fallback` annotation).
    - Disagreement disposition recorded: either "Adversarial pass complete — zero disagreements" verbatim line + cross-cite, OR per-disagreement user-disposition log entries with explicit `approved` / `disposition: <action>` lines.
    - `audit/FINDINGS-v36.0.md` §4 contains adversarial-pass attestation line referencing `266-01-ADVERSARIAL-LOG.md`.
    - Final atomic commit landed: `git log --oneline 5db8682b..HEAD -- .planning/phases/266-lootbox-entropy-refactor/266-01-ADVERSARIAL-LOG.md` shows `Task 14 — adversarial pass complete` subject.
  </acceptance_criteria>
  <done>Adversarial-log skeleton + skill spawn outputs (or executor-manual fallback) populated; disagreement disposition resolved (zero disagreements default; user-decided otherwise); §4 attestation line added; atomic commits landed.</done>
</task>

</content>

<!-- ================================================================ -->
<!-- WAVE 4 — AUDIT DELIVERABLE §5-§8 + KNOWN-ISSUES.md EDIT           -->
<!-- (AGENT-COMMITTED atomic per task)                                 -->
<!-- ================================================================ -->

<task type="auto">
  <name>Task 15 [Wave 4]: Author §5 regression appendix — REG-01 v35.0 + REG-02 v34.0 + REG-04 prior-finding spot-check sweep</name>
  <files>audit/FINDINGS-v36.0.md</files>
  <read_first>
    - audit/FINDINGS-v35.0.md (lines 322-367 — §5 regression appendix template; §5a REG-01 single-row + §5b REG-02 single-row + §5c REG-04 prior-finding sweep)
    - .planning/phases/266-lootbox-entropy-refactor/266-01-PLAN.md frontmatter `reg_01_candidates` + `reg_02_candidates` (single-PASS-row contents)
    - .planning/phases/266-lootbox-entropy-refactor/266-PATTERNS.md (§5 pattern at L572 — `audit/FINDINGS-v35.0.md` 5a-5d carry)
    - audit/FINDINGS-v25.0.md, audit/FINDINGS-v27.0.md, audit/FINDINGS-v28.0.md, audit/FINDINGS-v29.0.md, audit/FINDINGS-v30.0.md, audit/FINDINGS-v31.0.md, audit/FINDINGS-v32.0.md, audit/FINDINGS-v33.0.md, audit/FINDINGS-v34.0.md, audit/FINDINGS-v35.0.md (REG-04 prior-finding sweep targets — grep for v36-touched function set)
  </read_first>
  <action>
Append §5 to `audit/FINDINGS-v36.0.md`. Three subsections:

**§5a REG-01 single-PASS-row** covering v35.0 closure signal `MILESTONE_V35_AT_HEAD_5db8682bd7b811437f0c1cf47e832619d1478ac6` non-widening. Use the `reg_01_candidates` content from this plan's frontmatter verbatim. Mirror `audit/FINDINGS-v35.0.md` §5a single-row table:

```markdown
### 5a. REG-01 — v35.0 Closure Signal Non-Widening

| row_id | source | delta_sha | subject_at_head | re_verify_evidence | verdict |
|---|---|---|---|---|---|
| REG-v35.0-PPL | v35.0 closure signal `MILESTONE_V35_AT_HEAD_5db8682b` carry-forward. v35 audit deliverable `audit/FINDINGS-v35.0.md` 6 of 6 §4 surfaces SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE at HEAD `5db8682b`. | `5db8682b..<v36-close-sha>` (1 v36 contract commit + N v36 test commits since v35 baseline; only `contracts/modules/DegenerusGameLootboxModule.sol` modified) | v36.0 SURF-04 grep-proof asserts JackpotModule 9 callsites byte-identical between baseline `5db8682b` and HEAD `<v36-close-sha>` — including `_awardDailyCoinToTraitWinners` body + `COIN_LEVEL_TAG` + Phase 263 helper. Per-line modified-set walk via `git diff 5db8682b..HEAD -- contracts/modules/DegenerusGameJackpotModule.sol` returns ZERO `-` deletions inside the v35-protected ranges. | v36 modifies ONLY `DegenerusGameLootboxModule.sol`; JackpotModule + EntropyLib + GameStorage + DegenerusTraitUtils + GNRUS UNTOUCHED. v35 §4 6-surface verdicts carry forward unchanged at v36 HEAD. | PASS |
```

**§5b REG-02 single-PASS-row** covering v34.0 closure signal `MILESTONE_V34_AT_HEAD_6b63f6d4daf346a53a1d463790f637308ea8d555` non-widening. Use `reg_02_candidates` content verbatim from frontmatter. Single-row table mirroring §5a shape.

**§5c REG-04 prior-finding spot-check sweep.** Grep recipe across audit/FINDINGS-v25..v35 for findings referencing v36-touched function set:

```bash
for f in audit/FINDINGS-v25.0.md audit/FINDINGS-v27.0.md audit/FINDINGS-v28.0.md audit/FINDINGS-v29.0.md audit/FINDINGS-v30.0.md audit/FINDINGS-v31.0.md audit/FINDINGS-v32.0.md audit/FINDINGS-v33.0.md audit/FINDINGS-v34.0.md audit/FINDINGS-v35.0.md; do
  echo "=== $f ==="
  grep -nE '(EntropyLib\.entropyStep|_rollTargetLevel|_resolveLootboxRoll|_lootboxTicketCount|_lootboxDgnrsReward|_rollLootboxBoons|openLootBox|openBurnieLootBox|resolveLootboxDirect|resolveRedemptionLootbox)' "$f"
done
```

Run this grep at execute-phase time; transcribe the hit summary into a 6-column table (REG-04 row per relevant prior-finding referencing the v36-touched function set; columns: row_id / source / delta_sha / subject_at_head / re_verify_evidence / verdict).

Default expectation: ALL rows PASS (the v36 lootbox-entropy refactor preserves uniformity-equivalence; pre-existing prior-findings about lootbox paths re-verify non-widening at v36 HEAD per SURF-01..04 byte-identity grep-proof on adjacent surfaces + v36 lootbox-side STAT-01 chi² uniformity at `test/stat/LootboxEntropyDistribution.test.js`).

If any prior-finding is SUPERSEDED by v36 (e.g., the "EntropyLib XOR-shift" warden surface is REMOVED for the lootbox path; remaining BAF-jackpot consumer covered by KNOWN-ISSUES.md rephrased entry per AUDIT-05), record verdict SUPERSEDED with cross-cite to AUDIT-05 prose.

**§5d distribution summary** (mirror `audit/FINDINGS-v35.0.md` §5d): aggregate table — N rows REG-01 PASS + M rows REG-02 PASS + K rows REG-04 PASS/SUPERSEDED breakdown. Default expectation: 1 PASS REG-01 + 1 PASS REG-02 + ALL PASS REG-04 (with possible 1+ SUPERSEDED at v36 close for lootbox-EntropyLib references).

Atomic commit at end of this task: `audit(266): Task 15 — §5 regression appendix (REG-01 + REG-02 + REG-04)`.
  </action>
  <verify>
    <automated>grep -cE '^### 5[abcd]\.' audit/FINDINGS-v36.0.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^## 5\." audit/FINDINGS-v36.0.md` returns 1.
    - `grep -cE "^### 5[abcd]\." audit/FINDINGS-v36.0.md` returns >= 3 (§5a + §5b + §5c at minimum; §5d optional).
    - `grep -c "MILESTONE_V35_AT_HEAD_5db8682b" audit/FINDINGS-v36.0.md` returns >= 2 (§5a row + §7 cross-cite later).
    - `grep -c "MILESTONE_V34_AT_HEAD_6b63f6d4" audit/FINDINGS-v36.0.md` returns >= 2 (§5b row + §7 cross-cite later).
    - `grep -cE "REG-v35\.0|REG-v34\.0|REG-v[0-9]+\.0" audit/FINDINGS-v36.0.md` returns >= 3 (REG-01 + REG-02 + ≥ 1 REG-04 row).
    - `grep -cE "PASS|SUPERSEDED" audit/FINDINGS-v36.0.md` returns >= 3 (verdicts populated).
    - Atomic commit landed: `git log --oneline -1 -- audit/FINDINGS-v36.0.md` shows subject containing `Task 15` + `§5` + `REG-01` (or similar).
  </acceptance_criteria>
  <done>§5 regression appendix authored: REG-01 v35.0 PASS + REG-02 v34.0 PASS + REG-04 prior-finding spot-check sweep across v25..v35 (all PASS or SUPERSEDED); atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 16 [Wave 4]: Author §6 KI gating walk + EXC-04 NARROWS row + AUDIT-05 verdict summary</name>
  <files>audit/FINDINGS-v36.0.md</files>
  <read_first>
    - audit/FINDINGS-v35.0.md (lines 371-403 — §6 KI gating walk template; §6a Non-Promotion Ledger + §6b 4-row KI Envelope + §6c verdict summary)
    - .planning/phases/266-lootbox-entropy-refactor/266-01-PLAN.md frontmatter `ki_envelope_re_verifications` (4-row content) + `ki_verdict_summary`
    - .planning/phases/266-lootbox-entropy-refactor/266-PATTERNS.md (§6 pattern at L572)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_backward_trace.md
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_commitment_window.md
    - test/stat/LootboxEntropyDistribution.test.js (STAT-01 chi² evidence — for EXC-04 NARROWS empirical cross-cite)
  </read_first>
  <action>
Append §6 to `audit/FINDINGS-v36.0.md`. Three subsections:

**§6a Non-Promotion Ledger.** Default zero-row table per `audit/FINDINGS-v35.0.md` §6a. Phase 266 default-path: zero promotion candidates (no FINDING_CANDIDATEs from §4 6-surface adversarial sweep per Task 13/14 — the only KI doc edit is the rephrasing of an EXISTING entry per AUDIT-05, not a new promotion).

**§6b 4-row KI Envelope Re-Verifications.** Mirror `audit/FINDINGS-v35.0.md` §6b. Use frontmatter `ki_envelope_re_verifications` content verbatim. Four rows EXC-01..04:

```markdown
| EXC | Description | Carrier at v36 | Verdict | Evidence |
|-----|-------------|----------------|---------|----------|
| EXC-01 | Non-VRF entropy for affiliate winner roll | n/a (NEGATIVE-scope at v36) | NEGATIVE-scope at v36 | v36 modifies only `contracts/modules/DegenerusGameLootboxModule.sol`; affiliate-roll path in MintModule untouched (verified via SURF-03 grep-proof on L652 and zero v36 commits targeting MintModule). |
| EXC-02 | Gameover prevrandao fallback | n/a (NEGATIVE-scope at v36; AdvanceModule untouched) | NEGATIVE-scope at v36 | AdvanceModule prevrandao site untouched; v34/v35 carry-forward holds. No v36 commit modifies DegenerusGameAdvanceModule.sol. |
| EXC-03 | Gameover RNG substitution for mid-cycle write-buffer tickets (F-29-04 class) | n/a (NEGATIVE-scope at v36; AdvanceModule + swap path untouched) | NEGATIVE-scope at v36 | _swapAndFreeze / _swapTicketSlot / _gameOverEntropy sites untouched; v36 lootbox-entropy refactor does not interact with mid-cycle write-buffer ticket substitution. |
| EXC-04 | EntropyLib XOR-shift PRNG — scope NARROWS at v36 (lootbox-path consumption removed; remaining consumer is BAF jackpot only per ENT-05 deferral) | DegenerusGameJackpotModule.sol _jackpotTicketRoll (L2186-2229) — single entropyStep at L2192; SURF-02 byte-identical at v36 HEAD | RE_VERIFIED — scope NARROWS to BAF-only; KNOWN-ISSUES.md entry rephrased per AUDIT-05 (Task 17) | Backward trace per `feedback_rng_backward_trace.md`: lootbox path now consumes inline bit-sliced reads from `keccak256(abi.encode(rngWord, player, day, amount))` seed (NOT XOR-shift); STAT-01 chi² empirical proof at `test/stat/LootboxEntropyDistribution.test.js` (6 buckets — `% 100` Z<1.645 df=99; `% 5` chi²<9.488 df=4; `% 46` Z<1.645 df=45; `% 20` path Z<1.645 df=19; `% 20` variance Z<1.645 df=19; `% 10000` Z<1.645 df=9999 over 100K samples). BAF jackpot consumer remains; same security argument carries (single per-ticket step + small-modulus arithmetic + VRF-derived seed at upstream call boundary). Future-phase candidate: BAF refactor following the same bit-sliced keccak pattern. |
```

**§6c verdict summary** (use frontmatter `ki_verdict_summary` verbatim):

```
0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_REPHRASED (1 entry rephrased to BAF-only scope under Design Decisions per AUDIT-05)
```

Note: AUDIT-05 KI doc edit is a SCOPE REPHRASING of an existing entry (NOT a D-09 promotion). The edit is performed in Task 17. §6c verdict reflects the rephrasing, not a promotion.

Cross-reference STAT-01 chi² evidence file path (`test/stat/LootboxEntropyDistribution.test.js`) at least once in §6b row 4 prose — load-bearing for `feedback_rng_backward_trace.md` invariant: per-pull-bit consumer ultimately reads VRF-derived bits, so the entropy-quality envelope around xorshift is functionally OBVIATED in the lootbox path (NEW envelope width = ZERO at v36 for the lootbox surface).

Atomic commit at end of this task: `audit(266): Task 16 — §6 KI gating walk + EXC-04 NARROWS row + AUDIT-05 verdict summary`.
  </action>
  <verify>
    <automated>grep -cE 'EXC-0[1-4]' audit/FINDINGS-v36.0.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^## 6\." audit/FINDINGS-v36.0.md` returns 1.
    - `grep -cE "^### 6[abc]\." audit/FINDINGS-v36.0.md` returns 3.
    - `grep -cE "EXC-0[1-4]" audit/FINDINGS-v36.0.md` returns >= 4 (4 rows in §6b).
    - `grep -cE "NEGATIVE-scope at v36|NARROWS|RE_VERIFIED" audit/FINDINGS-v36.0.md` returns >= 4 (3 NEGATIVE-scope + 1 NARROWS).
    - `grep -cE "BAF jackpot|_jackpotTicketRoll" audit/FINDINGS-v36.0.md` returns >= 2 (EXC-04 NARROWS prose).
    - `grep -cE "feedback_rng_backward_trace" audit/FINDINGS-v36.0.md` returns >= 1 (methodology cite).
    - `grep -c "0 of 0 KI_ELIGIBLE_PROMOTED" audit/FINDINGS-v36.0.md` returns >= 1 (verdict summary string).
    - `grep -cE "test/stat/LootboxEntropyDistribution\.test\.js" audit/FINDINGS-v36.0.md` returns >= 1 (STAT-01 cross-cite).
    - Atomic commit landed: `git log --oneline -1 -- audit/FINDINGS-v36.0.md` shows subject containing `Task 16` + `§6` + `EXC-04`.
  </acceptance_criteria>
  <done>§6 KI gating walk authored: §6a zero-row Non-Promotion Ledger + §6b 4-row KI envelope (EXC-01..03 NEGATIVE + EXC-04 NARROWS with backward-trace + chi² cross-cite) + §6c verdict summary; atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 17 [Wave 4]: Rephrase `KNOWN-ISSUES.md` EntropyLib XOR-shift entry to BAF-jackpot-only scope (AUDIT-05)</name>
  <files>KNOWN-ISSUES.md</files>
  <read_first>
    - KNOWN-ISSUES.md (line 31 — current `**EntropyLib XOR-shift PRNG for lootbox outcome rolls.**` entry)
    - .planning/phases/266-lootbox-entropy-refactor/266-CONTEXT.md (`<specifics>` block — proposed v36-close prose verbatim)
    - .planning/phases/266-lootbox-entropy-refactor/266-PATTERNS.md (lines 633-651 — KNOWN-ISSUES.md edit pattern + proposed rephrased prose; line 649 — edit discipline note)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_contract_commits.md (note: KNOWN-ISSUES.md is NOT under `contracts/` or `test/`; agent-committable per CONTEXT.md `<canonical_refs>` "Active KI Envelope")
  </read_first>
  <action>
Edit `KNOWN-ISSUES.md` L31 — replace the existing `**EntropyLib XOR-shift PRNG for lootbox outcome rolls.**` entry with the v36-close BAF-jackpot-only scope prose per CONTEXT.md `<specifics>` + PATTERNS.md L645.

**Current entry text (DELETE — verbatim L31 at HEAD `5db8682b`):**

```
**EntropyLib XOR-shift PRNG for lootbox outcome rolls.** `EntropyLib.entropyStep()` uses a 256-bit XOR-shift PRNG (shifts 7/9/8) for lootbox outcome derivation (target level, ticket counts, BURNIE amounts, boons). XOR-shift has known theoretical weaknesses (cannot produce zero state, fixed cycle, correlated consecutive outputs). Exploitation is infeasible: the PRNG is seeded per-player, per-day, per-amount via `keccak256(rngWord, player, day, amount)` where `rngWord` is VRF-derived. The small number of entropy steps per resolution (5-10) and modular arithmetic over small ranges further mask any non-uniformity.
```

**New entry text (INSERT — v36-close BAF-only scope per CONTEXT.md `<specifics>` + PATTERNS.md L645):**

```
**EntropyLib XOR-shift PRNG for BAF jackpot ticket rolls.** `EntropyLib.entropyStep()` (256-bit XOR-shift, shifts 7/9/8) is consumed by `_jackpotTicketRoll` (`DegenerusGameJackpotModule.sol:2186-2229`) for BAF jackpot ticket-distribution path (target level + offset selection per ticket). XOR-shift has known theoretical weaknesses (cannot produce zero state, fixed cycle, correlated consecutive outputs). Exploitation is infeasible: the PRNG is seeded by VRF-derived `keccak256` mix at the upstream call boundary; the single per-ticket step + modular arithmetic over small ranges (`% 100` + `% 4` / `% 46`) mask any non-uniformity. Lootbox-path consumption was removed at v36.0 per Phase 266 refactor (now uses bit-sliced `EntropyLib.hash2` keccak draws); remaining xorshift consumer is BAF jackpot only — candidate for future-phase refactor following the same bit-sliced keccak pattern.
```

**Edit discipline (per `feedback_no_history_in_comments.md` per PATTERNS.md L649):** describes what IS at v36.0 close. The "Lootbox-path consumption was removed at v36.0..." sentence is a forward-disclosure (current scope clarification + future-phase pointer), NOT a code-history annotation. Acceptable in KNOWN-ISSUES.md prose because the doc itself is a snapshot-in-time pre-disclosure for audit wardens — needs to convey what is currently in scope and what is moving out.

**Use Edit tool** to perform a single targeted replacement at L31. Verify via grep that the replacement is exact:
- Pre-edit: `grep -c "for lootbox outcome rolls" KNOWN-ISSUES.md` returns 1.
- Post-edit: `grep -c "for lootbox outcome rolls" KNOWN-ISSUES.md` returns 0; `grep -c "for BAF jackpot ticket rolls" KNOWN-ISSUES.md` returns 1.

**Single-entry modification discipline** per CONTEXT.md `<deferred>` "KNOWN-ISSUES.md `Lootbox RNG uses index advance isolation` entry (separate cleanup)" + RESEARCH.md Open Question 4: do NOT modify any other KNOWN-ISSUES.md entry in this task. Only the EntropyLib XOR-shift entry at L31 changes.

Atomic commit at end of this task: `docs(266): Task 17 — KNOWN-ISSUES.md rephrase EntropyLib XOR-shift entry to BAF-only scope [AUDIT-05]`.
  </action>
  <verify>
    <automated>diff <(grep -c "for lootbox outcome rolls" KNOWN-ISSUES.md) <(echo 0) && grep -c "for BAF jackpot ticket rolls" KNOWN-ISSUES.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "for lootbox outcome rolls" KNOWN-ISSUES.md` returns 0 (old text removed).
    - `grep -c "for BAF jackpot ticket rolls" KNOWN-ISSUES.md` returns 1 (new text present).
    - `grep -c "_jackpotTicketRoll" KNOWN-ISSUES.md` returns 1 (BAF callsite cited).
    - `grep -c "Lootbox-path consumption was removed at v36.0" KNOWN-ISSUES.md` returns 1 (forward-disclosure prose present).
    - `git diff --shortstat 5db8682b..HEAD -- KNOWN-ISSUES.md` shows ~1 entry-equivalent change (1 line replaced/expanded; not a wholesale rewrite).
    - Verify no other entries modified: `git diff 5db8682b..HEAD -- KNOWN-ISSUES.md | grep -cE '^[+-]' | awk '{ print $1 }'` shows a small change footprint consistent with single-entry rephrasing (typical ~6-12 lines of diff, not 50+).
    - Atomic commit landed: `git log --oneline -1 -- KNOWN-ISSUES.md` shows subject `docs(266): Task 17 — KNOWN-ISSUES.md rephrase EntropyLib XOR-shift entry to BAF-only scope [AUDIT-05]`.
  </acceptance_criteria>
  <done>KNOWN-ISSUES.md L31 EntropyLib XOR-shift entry rephrased to BAF-jackpot-only scope per CONTEXT.md `<specifics>` + PATTERNS.md prose; single-entry modification discipline honored; atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 18 [Wave 4]: Author §7 prior-artifact cross-cites + §8 forward-cite closure (terminal-phase zero-emission verification)</name>
  <files>audit/FINDINGS-v36.0.md</files>
  <read_first>
    - audit/FINDINGS-v35.0.md (lines 407-475 — §7 + §8 templates; §8a residual + §8b emission + §8c combined verdict)
    - .planning/phases/266-lootbox-entropy-refactor/266-PATTERNS.md (§7 + §8 pattern at L573-575)
    - .planning/phases/266-lootbox-entropy-refactor/266-CONTEXT.md (D-266-FCITE carry of D-265-FCITE-01 — terminal-phase zero-emission)
  </read_first>
  <action>
Append §7 + §8 to `audit/FINDINGS-v36.0.md`.

**§7 Prior-Artifact Cross-Cites table.** Mirror `audit/FINDINGS-v35.0.md` §7 (L407-429). Cross-cite to:
- v35.0 closure signal `MILESTONE_V35_AT_HEAD_5db8682bd7b811437f0c1cf47e832619d1478ac6` + `audit/FINDINGS-v35.0.md` (carry-forward; NEGATIVE-scope-or-better re-verification).
- v34.0 closure signal `MILESTONE_V34_AT_HEAD_6b63f6d4daf346a53a1d463790f637308ea8d555` + `audit/FINDINGS-v34.0.md` (deeper carry; v34 §6b row 4 EXC-04 STAT-05 chi² cross-cite is the methodology precedent for v36 EXC-04 NARROWS).
- v33.0 closure signal `MILESTONE_V33_AT_HEAD_4ce3703d740d3707c88a1af595618120a8168399` + `audit/FINDINGS-v33.0.md` (REG-02-equivalent prior carry).
- Phase 266 plan artifacts: `.planning/phases/266-lootbox-entropy-refactor/266-CONTEXT.md` + `266-RESEARCH.md` + `266-PATTERNS.md` + `266-01-PLAN.md` + `266-01-SUMMARY.md` (post-Task-21) + `266-01-ADVERSARIAL-LOG.md` (Task 14).
- Project skill artifacts: `.claude/skills/contract-auditor/SKILL.md` + `.claude/skills/zero-day-hunter/SKILL.md` (D-266-ADVERSARIAL-01 in-scope skills).
- KI document: `KNOWN-ISSUES.md` (1 entry rephrased at v36 close per AUDIT-05).
- Project feedback artifacts (load-bearing): `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md` + `feedback_wait_for_approval.md` + `feedback_no_history_in_comments.md` + `feedback_no_dead_guards.md` + `feedback_rng_backward_trace.md` + `feedback_rng_commitment_window.md` + `feedback_gas_worst_case.md`.

Format per `audit/FINDINGS-v35.0.md` §7 — N-row table with columns: source / kind (closure-signal / phase-artifact / skill / feedback-rule / KI-doc) / cite-context.

**§8 Forward-Cite Closure (terminal-phase zero-emission verification).** Three subsections per `audit/FINDINGS-v35.0.md` §8 + RESEARCH.md "Forward-Cite Zero-Emission" pattern:

**§8a Residual Forward-Cites.** Default zero-row table. Phase 266 = terminal v36.0 phase per CONTEXT.md `<domain>` single-phase patch shape. Zero residual forward-cite obligations from upstream phases.

**§8b Emission Verifier (grep recipe + result).** Grep:
```bash
! grep -rE 'Phase (267|268|269|270|271)|v37\.0|v38\.0' audit/FINDINGS-v36.0.md .planning/phases/266-lootbox-entropy-refactor/
```
Expected: zero matches. Document the grep recipe + run-time result inline. Note the `Phase (266|267|...)` is the domain-specific token grep; literal "v37.0+" string occurrences in §1 / §2 / §8 meta-prose discussing the terminal-phase invariant are NOT forward-cites — they are self-referential to the closure-invariant discipline (per RESEARCH.md "Forward-Cite Zero-Emission" anti-pattern note).

**§8c Combined Verdict.** Mirror `audit/FINDINGS-v35.0.md` §8c. Single-line verdict: "Phase 266 is the terminal phase of v36.0; zero residual forward-cites from upstream; zero emission of Phase 267+ / v37.0+ forward-cite tokens. D-266-FCITE invariant honored; D-265-FCITE-01 / D-262-FCITE-01 / D-257-FCITE-01 carry chain extended."

Atomic commit at end of this task: `audit(266): Task 18 — §7 prior-artifact cross-cites + §8 forward-cite closure`.
  </action>
  <verify>
    <automated>grep -cE '^## [78]\.' audit/FINDINGS-v36.0.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^## [78]\." audit/FINDINGS-v36.0.md` returns 2.
    - `grep -cE "^### 8[abc]\." audit/FINDINGS-v36.0.md` returns 3 (§8a + §8b + §8c).
    - §7 cross-cites all 3 prior closure signals: `grep -cE "MILESTONE_V35_AT_HEAD_|MILESTONE_V34_AT_HEAD_|MILESTONE_V33_AT_HEAD_" audit/FINDINGS-v36.0.md` returns >= 6 (each signal cited >= 2 times across §5 + §7).
    - §7 cites Phase 266 plan artifacts: `grep -cE "266-CONTEXT|266-RESEARCH|266-PATTERNS|266-01-PLAN|266-01-ADVERSARIAL-LOG" audit/FINDINGS-v36.0.md` returns >= 5.
    - §7 cites project skills: `grep -cE "/contract-auditor|/zero-day-hunter" audit/FINDINGS-v36.0.md` returns >= 2.
    - §8 emission verifier grep recipe present + result inline: `grep -cE "Phase \(267" audit/FINDINGS-v36.0.md` returns >= 1 (the grep recipe documents the token; the recipe itself is meta-prose, NOT a forward-cite emission).
    - Forward-cite verifier passes: `! grep -rE 'Phase (267|268|269|270|271)|v37\.0|v38\.0' audit/FINDINGS-v36.0.md .planning/phases/266-lootbox-entropy-refactor/ | grep -vE '(8a|8b|8c|forward-cite|terminal-phase)' | head -1` shows zero qualifying matches (excluding meta-prose).
    - Atomic commit landed: `git log --oneline -1 -- audit/FINDINGS-v36.0.md` shows subject containing `Task 18` + `§7` + `§8`.
  </acceptance_criteria>
  <done>§7 prior-artifact cross-cites table + §8 forward-cite closure (residual + emission verifier + combined verdict) authored; terminal-phase zero-emission invariant honored; atomic commit landed.</done>
</task>

</content>

<!-- ================================================================ -->
<!-- WAVE 5 — §9 CLOSURE ATTESTATION + FLIPS + FINAL REVIEW            -->
<!-- (AGENT-COMMITTED atomic per task; final user-review gate at 21)   -->
<!-- ================================================================ -->

<task type="auto">
  <name>Task 19 [Wave 5]: Author §9 milestone closure attestation — §9a verdict distribution + §9b attestation block + §9c closure signal placeholder + §9.NN TWO-subsection commit-readiness register</name>
  <files>audit/FINDINGS-v36.0.md</files>
  <read_first>
    - audit/FINDINGS-v35.0.md (lines 479-560 — §9 milestone closure attestation template; §9a + §9b + §9c + §9.NN three-subsection commit-readiness register)
    - .planning/phases/266-lootbox-entropy-refactor/266-PATTERNS.md (lines 604-629 — §9.NN TWO-subsection pattern — D-266-CLOSURE-02)
    - .planning/phases/266-lootbox-entropy-refactor/266-CONTEXT.md (D-266-CLOSURE-01..02 — closure signal SHA + TWO-subsection format)
    - .planning/phases/266-lootbox-entropy-refactor/266-RESEARCH.md (lines 440-448 — Pitfall 4 closure-signal SHA placeholder + atomic-update pattern)
  </read_first>
  <action>
Append §9 to `audit/FINDINGS-v36.0.md`. Four subsections + commit-readiness register:

**§9a Verdict Distribution.** Single-table aggregation of all verdicts across §3-§8:
- §3d AUDIT-01 delta-surface table row count + classification breakdown (NEW / MODIFIED_LOGIC / REFACTOR_ONLY / DELETED).
- §3d Part C AUDIT-04 zero-new-state checks (5 PASS).
- §3e AUDIT-03 conservation re-proof (3 SAFE invariants).
- §4 6-surface adversarial sweep (default 6 SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE; ZERO FINDING_CANDIDATEs).
- §5a REG-01 (1 PASS) + §5b REG-02 (1 PASS) + §5c REG-04 (N PASS / M SUPERSEDED).
- §6 KI gating walk (zero promotions; 1 entry rephrased per AUDIT-05).
- §8 forward-cite closure (zero emission — terminal-phase invariant).

Mirror `audit/FINDINGS-v35.0.md` §9a verdict-distribution shape.

**§9b Attestation Block.** Mirror `audit/FINDINGS-v35.0.md` §9b prose:
- "I, the agent operating Phase 266 single-plan multi-task atomic-commit chain, attest that..."
- 24 of 24 v36.0 requirements (ENT-01..06 + STAT-01..03 + GAS-01..02 + SURF-01..04 + AUDIT-01..05 + REG-01..04) satisfied at HEAD `<sha>`.
- Default zero F-36-NN finding blocks emitted; 6 of 6 §4 surfaces SAFE_*.
- Adversarial pass via `/contract-auditor` + `/zero-day-hunter` (D-266-ADVERSARIAL-01..03) returned <N> disagreements (default: ZERO; record actual outcome from Task 14).
- v35.0 + v34.0 closure signals re-verified non-widening; KI envelopes EXC-01..03 NEGATIVE-scope + EXC-04 NARROWS to BAF-only (per Task 16 + Task 17).
- Per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md`: ZERO agent pre-approvals on contract or test commits; user-approved batched gates honored at Wave 1 close (Task 5) + Wave 2 close (Task 10) + final review at Task 21.

**§9c Closure Signal.** Use placeholder SHA per RESEARCH.md Pitfall 4:

```markdown
**Closure Signal:** `MILESTONE_V36_AT_HEAD_<sha>`

(`<sha>` resolved via `git rev-parse HEAD` AT Task 19's atomic commit creation time — see Task 20 atomic-update task that replaces this placeholder across 5 occurrences post-commit.)
```

**§9.NN Commit-Readiness Register — TWO-subsection format per D-266-CLOSURE-02 + PATTERNS.md L604-629:**

```markdown
### 9.NN Commit-Readiness Register

#### 9.NN.i USER-APPROVED contracts (1 commit)

\`\`\`
<sha-wave1>  feat(266): lootbox-path entropy refactor [ENT-01..06]
\`\`\`

User-approval audit trail per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md`: Phase 266 single batched contract-tree commit, user explicitly approved diff at Task 5 gate before commit ran.

#### 9.NN.ii USER-APPROVED tests (N commits — re-enumerated via `git log --oneline 5db8682b..HEAD -- test/ package.json`)

\`\`\`
<sha-N>  <subject-N>
... (additional test/chore commits enumerated at Task 19 commit time)
\`\`\`

User-approval audit trail per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md`: Phase 266 Wave 2 batched at Task 10 gate; user reviewed diff before commits landed.

#### 9.NN.iii AGENT-COMMITTED audit artifacts (Phase 266 plan-close commits — N commits enumerated at Task 19 commit time via `git log --oneline --grep='audit(266)' --grep='docs(266)' <pre-Task-11-sha>..HEAD`)

\`\`\`
<sha-N>  <subject-N>
... (Tasks 11-21 atomic-commit chain populated at Task 19 commit time)
\`\`\`

(Task 19's own commit appended retroactively after this task's commit lands; Task 20 + Task 21 close-out commits appended after final user-review gate. Final 266-01-SUMMARY.md will render the complete chain.)

**No §9.NN.iv awaiting-approval subsection.** Zero awaiting-approval files in v36; mirrors v34/v35 D-262-CLOSURE-02 / D-265-CLOSURE-02 carry.
```

Atomic commit at end of this task: `audit(266): Task 19 — §9 milestone closure attestation + §9.NN commit-readiness register`. Note: this commit's SHA is the resolution target for `<sha-wave1>` placeholder if Task 20 captures HEAD here; OR Task 20 captures post-Task-19 HEAD.

**Note on closure-signal SHA timing:** §9c stays as placeholder `<sha>` until Task 20 atomic-update. Per RESEARCH.md Pitfall 4: closure-signal SHA captured AT FINAL §9 commit (this task's commit), but the resolved-SHA edit happens in Task 20 because Task 19's commit-creation time precedes the resolved SHA being known.
  </action>
  <verify>
    <automated>grep -cE '^## 9\.|^### 9[abc]\.|^### 9\.NN' audit/FINDINGS-v36.0.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^## 9\." audit/FINDINGS-v36.0.md` returns 1.
    - `grep -cE "^### 9[abc]\." audit/FINDINGS-v36.0.md` returns 3 (§9a + §9b + §9c).
    - `grep -c "9.NN Commit-Readiness Register" audit/FINDINGS-v36.0.md` returns 1.
    - `grep -cE "9\.NN\.i USER-APPROVED contracts|9\.NN\.ii USER-APPROVED tests|9\.NN\.iii AGENT-COMMITTED audit artifacts" audit/FINDINGS-v36.0.md` returns 3 (TWO-subsection format + AGENT-COMMITTED — three pieces total per D-266-CLOSURE-02).
    - `grep -c "9\.NN\.iv awaiting-approval" audit/FINDINGS-v36.0.md` returns 0 (mirrors v34/v35 — NO §9.NN.iv subsection).
    - `grep -c "MILESTONE_V36_AT_HEAD_<sha>" audit/FINDINGS-v36.0.md` returns >= 1 (placeholder present; resolved at Task 20).
    - `grep -c "feat(266): lootbox-path entropy refactor" audit/FINDINGS-v36.0.md` returns >= 1 (Wave 1 commit subject cited in §9.NN.i).
    - Atomic commit landed: `git log --oneline -1 -- audit/FINDINGS-v36.0.md` shows subject containing `Task 19` + `§9`.
  </acceptance_criteria>
  <done>§9a verdict distribution + §9b attestation block + §9c closure signal (placeholder) + §9.NN TWO-subsection commit-readiness register authored per D-266-CLOSURE-02; atomic commit landed; closure-signal SHA placeholder resolved by Task 20.</done>
</task>

<task type="auto">
  <name>Task 20 [Wave 5]: Resolve closure-signal SHA — `git rev-parse HEAD`; atomic-update §9c + ROADMAP.md + STATE.md + MILESTONES.md + 266-01-SUMMARY.md (5 occurrences)</name>
  <files>audit/FINDINGS-v36.0.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/MILESTONES.md, .planning/phases/266-lootbox-entropy-refactor/266-01-SUMMARY.md</files>
  <read_first>
    - audit/FINDINGS-v36.0.md §9c (Task 19 placeholder)
    - .planning/ROADMAP.md (Phase 266 line + closure-signal emission paragraph location — see existing v35.0 structure for reference)
    - .planning/STATE.md (Active Milestone v36.0 section — to flip to Last Shipped Milestone)
    - .planning/MILESTONES.md (top of file — v36.0 entry to prepend)
    - .planning/phases/265-delta-audit-findings-consolidation/265-01-SUMMARY.md (closure-summary template)
    - .planning/phases/266-lootbox-entropy-refactor/266-RESEARCH.md (lines 440-448 — Pitfall 4 atomic-update pattern)
    - .planning/phases/266-lootbox-entropy-refactor/266-PATTERNS.md (lines 654-665 — closure-flip pattern)
  </read_first>
  <action>
**Step 1: Capture closure-signal SHA.**
```bash
CLOSURE_SHA=$(git rev-parse HEAD)
```
This is the post-Task-19 HEAD — the audit-pass-close commit per D-266-CLOSURE-01.

**Step 2: Atomic-update 5 occurrences of `<sha>` placeholder** with the resolved SHA. The 5 occurrences:

1. **`audit/FINDINGS-v36.0.md`** — frontmatter `closure_signal:` field + `head_anchor:` field + §2 Closure Verdict Summary + §9c trailing line (verify count via `grep -c "MILESTONE_V36_AT_HEAD_" audit/FINDINGS-v36.0.md` — expect >= 3 hits post-update).

2. **`.planning/ROADMAP.md`** — flip Phase 266 status to SHIPPED. Update the v36.0 milestone bullet + Phase 266 entry + emit closure-signal paragraph `MILESTONE_V36_AT_HEAD_<sha>`. Demote v35.0 line if needed.

3. **`.planning/STATE.md`** — update `## Last Shipped Milestone` from v35.0 to v36.0. Demote v35.0 to `### Prior Shipped Milestone`. Update STATUS / Last Activity / progress block. Update milestone v36.0 closure-signal field.

4. **`.planning/MILESTONES.md`** — prepend v36.0 entry to top with closure-signal recorded. Mirror existing v35.0 row shape.

5. **`.planning/phases/266-lootbox-entropy-refactor/266-01-SUMMARY.md`** — Phase-closure summary mirroring `265-01-SUMMARY.md` format. Frontmatter `closure_signal:` field + body cite. Per-task atomic-commit log + per-REQ tally for 24 v36.0 requirements + cross-phase cross-cite density + project-feedback-rules-honored table + closure signal `MILESTONE_V36_AT_HEAD_<sha>`. Mirror PATTERNS.md L666-702 SUMMARY shape.

**Step 3: Verify the atomic-update.** After substitutions, run:

```bash
grep -rE "MILESTONE_V36_AT_HEAD_<sha>|MILESTONE_V36_AT_HEAD_$" \
  audit/FINDINGS-v36.0.md \
  .planning/ROADMAP.md \
  .planning/STATE.md \
  .planning/MILESTONES.md \
  .planning/phases/266-lootbox-entropy-refactor/266-01-SUMMARY.md
```
Expected: zero matches (zero unresolved `<sha>` placeholders).

```bash
grep -rcE "MILESTONE_V36_AT_HEAD_[0-9a-f]{40}" \
  audit/FINDINGS-v36.0.md \
  .planning/ROADMAP.md \
  .planning/STATE.md \
  .planning/MILESTONES.md \
  .planning/phases/266-lootbox-entropy-refactor/266-01-SUMMARY.md
```
Expected: >= 5 hits (≥1 in each file with full 40-char SHA).

**Step 4: §9.NN.iii final-update.** Now that the audit-artifact chain is complete, regenerate the §9.NN.iii commit list via:
```bash
git log --oneline --grep='audit(266)' --grep='docs(266)' --grep='test(266)' --grep='chore(266)' <pre-Task-11-sha>..HEAD
```
Replace the placeholder commit list in §9.NN.iii with the resolved enumeration.

Atomic commit at end of this task: `docs(266): Task 20 — close v36.0 — closure signal MILESTONE_V36_AT_HEAD_<closure-sha-short> + ROADMAP/STATE/MILESTONES flips + 266-01-SUMMARY [MILESTONE_V36_AT_HEAD_<closure-sha-short>]`.

(The commit subject itself contains the closure signal trailing tag — mirrors v35.0 close commit `85539a0f docs(265): Task 14 — close v35.0 — READ-only FINDINGS + ROADMAP/STATE/MILESTONES flips + 265-01-SUMMARY [MILESTONE_V35_AT_HEAD_5db8682b]`.)
  </action>
  <verify>
    <automated>grep -rcE "MILESTONE_V36_AT_HEAD_[0-9a-f]{40}" audit/FINDINGS-v36.0.md .planning/ROADMAP.md .planning/STATE.md .planning/MILESTONES.md .planning/phases/266-lootbox-entropy-refactor/266-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rcE "MILESTONE_V36_AT_HEAD_<sha>" audit/FINDINGS-v36.0.md .planning/ROADMAP.md .planning/STATE.md .planning/MILESTONES.md .planning/phases/266-lootbox-entropy-refactor/266-01-SUMMARY.md` returns 0 (zero unresolved placeholders).
    - `grep -rcE "MILESTONE_V36_AT_HEAD_[0-9a-f]{40}" audit/FINDINGS-v36.0.md .planning/ROADMAP.md .planning/STATE.md .planning/MILESTONES.md .planning/phases/266-lootbox-entropy-refactor/266-01-SUMMARY.md` returns >= 5 (each file has at least 1 resolved SHA).
    - `grep -c "MILESTONE_V36_AT_HEAD_" audit/FINDINGS-v36.0.md` returns >= 3 (frontmatter + §2 + §9c).
    - File `.planning/phases/266-lootbox-entropy-refactor/266-01-SUMMARY.md` exists with frontmatter `closure_signal:` field + per-task atomic-commit log + per-REQ tally for 24 v36.0 requirements.
    - `.planning/ROADMAP.md` Phase 266 status flipped to SHIPPED; v36.0 line updated.
    - `.planning/STATE.md` `## Last Shipped Milestone` is v36.0; v35.0 demoted to `### Prior Shipped Milestone`.
    - `.planning/MILESTONES.md` v36.0 entry prepended at top with closure-signal recorded.
    - §9.NN.iii commit list regenerated with resolved enumeration (no placeholder commit subjects).
    - Atomic commit landed: `git log --oneline -1` shows subject containing `Task 20` + `close v36.0` + `MILESTONE_V36_AT_HEAD_`.
  </acceptance_criteria>
  <done>Closure-signal SHA resolved across 5 occurrences (FINDINGS + ROADMAP + STATE + MILESTONES + SUMMARY); §9.NN.iii commit list final-updated; atomic-update commit landed with closure signal in commit subject.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 21 [Wave 5 GATE]: Final user-review gate — present complete diff to user; READ-only flip on `audit/FINDINGS-v36.0.md` ONLY after explicit "approved"</name>
  <files>audit/FINDINGS-v36.0.md, KNOWN-ISSUES.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/MILESTONES.md, .planning/phases/266-lootbox-entropy-refactor/266-01-SUMMARY.md, .planning/phases/266-lootbox-entropy-refactor/266-01-ADVERSARIAL-LOG.md</files>
  <read_first>
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_manual_review_before_push.md
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_wait_for_approval.md
    - .planning/phases/266-lootbox-entropy-refactor/266-CONTEXT.md (D-266-APPROVAL-01 final user-review gate)
  </read_first>
  <action>
This is a checkpoint:human-verify task \u2014 the FINAL user-review gate per `feedback_manual_review_before_push.md` + D-266-APPROVAL-01. Present the complete Phase 266 closure deliverable bundle (audit/FINDINGS-v36.0.md + KNOWN-ISSUES.md + ROADMAP.md + STATE.md + MILESTONES.md + 266-01-SUMMARY.md + 266-01-ADVERSARIAL-LOG.md) to the user per the <what-built> block below. See <how-to-verify> for the 10-step diff-review checklist (full git log + per-file diffs + grep verifications + final test-suite green). Wait for explicit "approved" string before performing the READ-only flip on `audit/FINDINGS-v36.0.md` (chmod a-w) and recording the closure-attestation final entry with subject `docs(266): Task 21 \u2014 close v36.0 \u2014 READ-only FINDINGS-v36.0.md + final review approved [MILESTONE_V36_AT_HEAD_<closure-sha-short>]`. Agent does NOT `git push` per `feedback_manual_review_before_push.md`. See <acceptance_criteria> for the post-approval validation conditions.
  </action>
  <verify>
    <automated>test ! -w audit/FINDINGS-v36.0.md &amp;&amp; head -20 audit/FINDINGS-v36.0.md | grep -cE "status: FINAL \u2014 READ-ONLY|read_only: true" | grep -E "^\\s*2$"</automated>
  </verify>
  <what-built>
Phase 266 closure deliverable bundle (post-Wave-1 + Wave-2 + Wave-3 + Wave-4 + Tasks 19-20):
- `audit/FINDINGS-v36.0.md` — 9-section deliverable with closure signal `MILESTONE_V36_AT_HEAD_<resolved-sha>` in 3+ locations (frontmatter + §2 + §9c).
- `KNOWN-ISSUES.md` — 1 entry rephrased (EntropyLib XOR-shift -> BAF-jackpot-only scope per AUDIT-05).
- `.planning/ROADMAP.md` — Phase 266 SHIPPED + v36.0 milestone closure paragraph + closure-signal recorded.
- `.planning/STATE.md` — v36.0 = Last Shipped Milestone; v35.0 demoted to Prior.
- `.planning/MILESTONES.md` — v36.0 entry prepended at top.
- `.planning/phases/266-lootbox-entropy-refactor/266-01-SUMMARY.md` — phase-closure summary with frontmatter closure_signal + per-task atomic-commit log + per-REQ tally.
- `.planning/phases/266-lootbox-entropy-refactor/266-01-ADVERSARIAL-LOG.md` — adversarial-pass log (Task 14 output).

24 of 24 v36.0 requirements satisfied at HEAD: ENT-01..06 + STAT-01..03 + GAS-01..02 + SURF-01..04 + AUDIT-01..05 + REG-01..04.
  </what-built>
  <how-to-verify>
**Per `feedback_manual_review_before_push.md`:** user reviews the COMPLETE diff bundle before READ-only flip + before any `git push`. Agent does NOT push.

1. Run `git log --oneline 5db8682b..HEAD` — review the full chain of Wave 1 (1 contract commit) + Wave 2 (N test/chore commits) + Wave 3-5 (Task 11-20 audit-artifact commits, ~10-12 commits).
2. Run `git diff 5db8682b..HEAD -- audit/FINDINGS-v36.0.md | less` — review the 9-section deliverable.
3. Run `git diff 5db8682b..HEAD -- KNOWN-ISSUES.md | less` — verify single-entry rephrasing only.
4. Run `git diff 5db8682b..HEAD -- .planning/ROADMAP.md .planning/STATE.md .planning/MILESTONES.md | less` — verify closure-flip updates.
5. Run `cat .planning/phases/266-lootbox-entropy-refactor/266-01-SUMMARY.md` — review summary.
6. Run `cat .planning/phases/266-lootbox-entropy-refactor/266-01-ADVERSARIAL-LOG.md` — review adversarial-pass log.
7. Run `grep -rcE "MILESTONE_V36_AT_HEAD_<sha>" audit/FINDINGS-v36.0.md .planning/` — expect 0 (zero unresolved placeholders).
8. Run `grep -c "MILESTONE_V36_AT_HEAD_" audit/FINDINGS-v36.0.md` — expect >= 3.
9. Run `grep -cE "EntropyLib\.entropyStep" contracts/modules/DegenerusGameLootboxModule.sol` — expect 0.
10. Run `npx hardhat compile && npm test 2>&1 | tail -10` — full suite green (compile + unit + stat + gas + surface).

**If user requests changes:** return to the relevant prior task and revise; re-present at this gate.

**Per `feedback_wait_for_approval.md`:** wait for the user's explicit `approved` string. Then perform the READ-only flip:

```
chmod a-w audit/FINDINGS-v36.0.md
```
And finalize with the closure-attestation final commit.

**Final commit subject** (per Phase 265 Task 14 / v35.0 close `85539a0f` precedent):
`docs(266): Task 21 — close v36.0 — READ-only FINDINGS-v36.0.md + final review approved [MILESTONE_V36_AT_HEAD_<closure-sha-short>]`

This commit may be empty (if Task 20 already updated all files and chmod is the only change) or include any final touch-ups requested by user during review.

**Per `feedback_manual_review_before_push.md`:** agent does NOT `git push`. User pushes when ready. Phase 266 + v36.0 milestone CLOSED at this commit.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize READ-only flip on FINDINGS-v36.0.md + final closure commit, or describe required changes.</resume-signal>
  <acceptance_criteria>
    - User responds with explicit `approved` string before READ-only flip runs.
    - After approval: `ls -l audit/FINDINGS-v36.0.md` shows file is read-only (mode 0444 or similar — write bit cleared).
    - `head -20 audit/FINDINGS-v36.0.md | grep -c "status: FINAL — READ-ONLY"` returns 1.
    - `head -20 audit/FINDINGS-v36.0.md | grep -c "read_only: true"` returns 1.
    - Final commit landed: `git log --oneline -1` shows subject containing `Task 21` + `close v36.0` + `READ-only` + `MILESTONE_V36_AT_HEAD_`.
    - 24 of 24 v36.0 requirements satisfied: per-REQ tally in `266-01-SUMMARY.md` shows COMPLETE for all 24 IDs.
    - Phase 266 + v36.0 milestone CLOSED.
  </acceptance_criteria>
  <done>User approved final review; READ-only flip applied; closure-attestation final commit landed with `MILESTONE_V36_AT_HEAD_<sha>` in subject; v36.0 milestone CLOSED; Phase 266 SHIPPED.</done>
</task>

</tasks>

<verification>

## Phase 266 Wave-Gate Verification (executor runs after each wave)

**After Wave 1 (Task 5 approval + commit):**
```bash
grep -cE "EntropyLib\.entropyStep" contracts/modules/DegenerusGameLootboxModule.sol  # expect 0
git diff 5db8682b..HEAD -- contracts/libraries/EntropyLib.sol  # expect empty (SURF-01)
git diff 5db8682b..HEAD -- contracts/modules/DegenerusGameJackpotModule.sol  # expect empty (SURF-02 + SURF-04)
git diff 5db8682b..HEAD -- contracts/modules/DegenerusGameMintModule.sol  # expect empty (SURF-03)
npx hardhat compile  # expect success
```

**After Wave 2 (Task 10 approval + commits):**
```bash
npm run test:stat   # expect all stat tests pass (including new LootboxEntropyDistribution)
npm run test:gas    # expect all gas tests pass (including new LootboxOpenGas + extended AdvanceGameGas)
npx hardhat test test/stat/SurfaceRegression.test.js  # expect SURF-01..04 v36.0 + existing v35.0 pass
```

**After Wave 3 (Tasks 11-14 atomic commits):**
```bash
grep -cE "^## [1-4]\." audit/FINDINGS-v36.0.md  # expect 4 (§2 + §3 + §4; §1 is YAML frontmatter)
grep -cE "Surface \([a-f]\)" audit/FINDINGS-v36.0.md  # expect >= 6
grep -E '^## /contract-auditor|^## /zero-day-hunter' .planning/phases/266-lootbox-entropy-refactor/266-01-ADVERSARIAL-LOG.md | wc -l  # expect 2
```

**After Wave 4 (Tasks 15-18 atomic commits):**
```bash
grep -cE "^## [5-8]\." audit/FINDINGS-v36.0.md  # expect 4 (§5 + §6 + §7 + §8)
grep -cE "EXC-0[1-4]" audit/FINDINGS-v36.0.md  # expect >= 4
grep -c "for BAF jackpot ticket rolls" KNOWN-ISSUES.md  # expect 1
grep -c "for lootbox outcome rolls" KNOWN-ISSUES.md    # expect 0
! grep -rE 'Phase (267|268|269|270|271)|v37\.0|v38\.0' audit/FINDINGS-v36.0.md .planning/phases/266-lootbox-entropy-refactor/ | grep -vE '(8a|8b|8c|forward-cite|terminal-phase)' | head -1  # expect empty
```

**After Wave 5 Task 20 (closure-signal resolution):**
```bash
grep -rcE "MILESTONE_V36_AT_HEAD_<sha>" audit/FINDINGS-v36.0.md .planning/ROADMAP.md .planning/STATE.md .planning/MILESTONES.md .planning/phases/266-lootbox-entropy-refactor/266-01-SUMMARY.md  # expect 0 (no placeholders)
grep -rcE "MILESTONE_V36_AT_HEAD_[0-9a-f]{40}" audit/FINDINGS-v36.0.md .planning/ROADMAP.md .planning/STATE.md .planning/MILESTONES.md .planning/phases/266-lootbox-entropy-refactor/266-01-SUMMARY.md  # expect >= 5
```

**After Wave 5 Task 21 (final user-review gate):**
```bash
ls -l audit/FINDINGS-v36.0.md  # expect read-only (no write bit)
grep -c "status: FINAL — READ-ONLY" audit/FINDINGS-v36.0.md | head -20  # expect 1 in frontmatter
grep -c "read_only: true" audit/FINDINGS-v36.0.md | head -20  # expect 1 in frontmatter
git log --oneline -1  # expect Task 21 close commit with MILESTONE_V36_AT_HEAD_ in subject
```

</verification>

<success_criteria>

Phase 266 SHIPPED + v36.0 milestone CLOSED when ALL of the following hold:

1. **Contract refactor (Wave 1):** 7 lootbox-path entropyStep callsites removed; 8 entropy modulo consumers (7 named + 2 sub-call) consume disjoint bit ranges from per-resolution seed; per-consumer NatSpec bit-budget block per ENT-06; L1585 dead WWXRP advance deleted per `feedback_no_dead_guards.md`; ETH-amount-second branch uses Option A counter-tagged `seed2 = EntropyLib.hash2(seed, 1)`. Single batched USER-APPROVED commit landed. `EntropyLib.sol` byte-identical (ENT-04). [ENT-01..06 satisfied]

2. **Tests (Wave 2):** STAT-01..03 chi² uniformity passes over 6 sub-roll buckets per RESEARCH.md Pitfall 1 sample-budget calibration; GAS-01 per-open ±300 g asserted with theoretical-worst-case header per `feedback_gas_worst_case.md`; GAS-02 advanceGame ±2K + 1.99× margin preserved; SURF-01..04 byte-identity proven via per-line modified-set walk vs `5db8682b`. Tests USER-APPROVED batched at Task 10 gate. [STAT-01..03 + GAS-01..02 + SURF-01..04 satisfied]

3. **Audit deliverable (Waves 3-5):** `audit/FINDINGS-v36.0.md` 9-section single-canonical deliverable FINAL READ-only at v36.0 closure HEAD; §3d AUDIT-01 delta-surface table classifies every changed declaration; §3d Part C AUDIT-04 zero-new-state attestation passes 5 grep-reproducible checks; §3e AUDIT-03 conservation re-proof preserves 3 invariants; §4 6-surface adversarial sweep verdicts ALL surfaces SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE (default expected per Assumption A6 — 6 surfaces; ZERO F-36-NN finding blocks); §5a REG-01 v35.0 carry PASS + §5b REG-02 v34.0 carry PASS + §5c REG-04 prior-finding spot-check sweep ALL PASS / SUPERSEDED; §6 KI envelope re-verifications EXC-01..03 NEGATIVE-scope + EXC-04 NARROWS to BAF-only with backward-trace + chi² empirical cross-cite to STAT-01; §6c verdict summary `0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_REPHRASED (1 entry rephrased to BAF-only scope under Design Decisions per AUDIT-05)`; §7 prior-artifact cross-cites; §8 forward-cite zero-emission verified; §9.NN TWO-subsection commit-readiness register per D-266-CLOSURE-02. [AUDIT-01..05 + REG-01..04 satisfied]

4. **KNOWN-ISSUES.md edit (AUDIT-05):** L31 EntropyLib XOR-shift entry rephrased to BAF-jackpot-only scope per CONTEXT.md `<specifics>` + PATTERNS.md prose; single-entry modification discipline honored.

5. **Adversarial pass:** `/contract-auditor` + `/zero-day-hunter` parallel spawn AFTER full §4 draft per D-266-ADVERSARIAL-01..02 + RESEARCH.md Pitfall 5; outputs logged in `266-01-ADVERSARIAL-LOG.md` under `## /contract-auditor` + `## /zero-day-hunter` H2 sections; disagreement disposition surfaced inline per D-266-ADVERSARIAL-03 (default zero disagreements expected).

6. **Closure signal emitted:** `MILESTONE_V36_AT_HEAD_<sha>` resolved via `git rev-parse HEAD` at Task 19 commit time; atomic-update across 5 occurrences (`audit/FINDINGS-v36.0.md` + `.planning/ROADMAP.md` + `.planning/STATE.md` + `.planning/MILESTONES.md` + `266-01-SUMMARY.md`); zero unresolved `<sha>` placeholders post-Task-20.

7. **Approval discipline honored:** USER-APPROVED batched gates at Wave 1 close (Task 5) + Wave 2 close (Task 10) + final review gate (Task 21) per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md`. Zero agent pre-approvals on contract or test commits.

8. **Phase 266 = terminal v36.0 phase:** §8 forward-cite closure verifies zero forward-cite emission to v37.0+ phases per D-266-FCITE carry of D-265-FCITE-01.

9. **24 of 24 requirements satisfied at HEAD:** ENT-01..06 + STAT-01..03 + GAS-01..02 + SURF-01..04 + AUDIT-01..05 + REG-01..04 — each requirement ID maps to ≥ 1 task in this plan + ≥ 1 evidence artifact at v36.0 closure HEAD.

</success_criteria>

<output>
After completion, create `.planning/phases/266-lootbox-entropy-refactor/266-01-SUMMARY.md` per Task 20 (mirror `265-01-SUMMARY.md` format with closure-signal frontmatter field + per-task atomic-commit log + per-REQ tally + cross-phase cross-cite density + project-feedback-rules-honored table).
</output>
</content>
