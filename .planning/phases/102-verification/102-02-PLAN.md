---
phase: 102-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 102-01
files_modified: []
autonomous: true
requirements:
  - TEST-02
  - TEST-03

must_haves:
  truths:
    - "The full Hardhat suite produces exactly 1209 pass / 33 fail — zero new failures introduced by the bug fix"
    - "The full Foundry suite produces exactly 354 pass / 14 fail — zero new failures introduced by the bug fix"
    - "Any pre-existing failures are confirmed as pre-existing (match the baseline counts)"
  artifacts:
    - path: "contracts/modules/DegenerusGameEndgameModule.sol"
      provides: "The fixed contract under test (modified in Phase 101, not by this plan)"
      contains: "rebuyDelta = _getFuturePrizePool() - baseFuturePool"
  key_links:
    - from: "Full Hardhat suite"
      to: "contracts/modules/DegenerusGameEndgameModule.sol"
      via: "JavaScript tests exercising endgame paths"
      pattern: "runRewardJackpots|endgame"
    - from: "Full Foundry suite"
      to: "contracts/modules/DegenerusGameEndgameModule.sol"
      via: "Foundry integration tests (includes BafRebuyReconciliation.t.sol from plan 01)"
      pattern: "forge test"
---

<objective>
Run the complete Hardhat and Foundry test suites and confirm that the Phase 101 bug fix introduces zero new failures relative to the pre-existing baselines.

Purpose: TEST-02 (Hardhat zero regressions) and TEST-03 (Foundry zero regressions). This plan runs after plan 01's checkpoint is approved and the test file is committed.

Output: Documented pass/fail counts matching or bettering the pre-established baselines. Hardhat baseline: 1209 pass / 33 fail. Foundry baseline: 354 pass / 14 fail (new test from plan 01 adds 1 to Foundry pass count, so expected post-plan: 355 pass / 14 fail).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-bug-fix/101-01-SUMMARY.md
@.planning/phases/102-verification/102-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full Foundry suite and verify zero regressions</name>
  <files></files>
  <read_first>
    - .planning/phases/102-verification/102-01-SUMMARY.md (confirm test from plan 01 was committed and is part of the suite)
    - contracts/modules/DegenerusGameEndgameModule.sol lines 238-241 (confirm fix is in place before running)
  </read_first>
  <action>
    Run the complete Foundry test suite:

    ```
    forge test 2>&1 | tail -30
    ```

    Count pass and fail from the summary line. Pre-existing baseline: 354 pass / 14 fail. The new BafRebuyReconciliation.t.sol adds 1 test, so expected: 355 pass / 14 fail (or higher pass if previously failing tests are now fixed, but no new fails).

    If fail count exceeds 14:
    1. Identify new failures: compare failing test names to known pre-existing failures.
    2. Investigate whether failures are caused by the Phase 101 fix or are unrelated.
    3. If caused by the fix, document the regression in the SUMMARY.md as a blocker.
    4. If unrelated (pre-existing but not in the 14 baseline), document as environment issue.

    Pre-existing Foundry failures (14 baseline) are typically in invariant tests or tests that depend on specific gas/timing conditions. Do not attempt to fix pre-existing failures in this plan.

    If forge test is slow, use --match-path to run targeted subsets first:
    ```
    forge test --match-path "test/fuzz/BafRebuyReconciliation.t.sol" -vv
    forge test 2>&1 | tail -10
    ```

    Note: ContractAddresses.sol may have unstaged changes (different deploy addresses per STATE.md blocker). Verify forge build is clean before running tests. If ContractAddresses.sol has changes that cause build failures, stash or restore them first (the unstaged changes are for mainnet deploy addresses, not test addresses — the patchForFoundry equivalent is baked into DeployProtocol.sol's nonce order).
  </action>
  <verify>
    <automated>forge test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - forge test completes without new failures beyond the 14 pre-existing baseline
    - Pass count is >= 355 (354 baseline + 1 new test from plan 01)
    - Fail count is <= 14 (pre-existing baseline, no regressions)
    - testBafRebuyContributionPreserved is in the passing tests
  </acceptance_criteria>
  <done>Foundry suite: pass count >= 355, fail count <= 14, zero new regressions. TEST-03 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Run full Hardhat suite and verify zero regressions</name>
  <files></files>
  <read_first>
    - package.json (confirm test script command — typically "npx hardhat test" or "npm test")
  </read_first>
  <action>
    Identify the Hardhat test command from package.json scripts. Run:

    ```
    npm test 2>&1 | tail -30
    ```

    or if the script is not "test":
    ```
    npx hardhat test 2>&1 | tail -30
    ```

    Count pass and fail from the Mocha summary line. Pre-existing baseline: 1209 pass / 33 fail.

    If fail count exceeds 33:
    1. Identify the new failures by name.
    2. Check whether they exercise endgame/jackpot paths that the Phase 101 fix touches.
    3. If yes, document as regression and flag as blocker in SUMMARY.md.
    4. If no, document as environment noise.

    The Hardhat tests run against compiled artifacts. If contracts/modules/DegenerusGameEndgameModule.sol was recompiled after the fix, the artifacts should already be up to date. If not, run `npx hardhat compile` first.

    Note: Hardhat tests are slower than Foundry. If the full suite takes > 5 minutes, run only the most relevant test files first (DegenerusGame.test.js, DegenerusJackpots.test.js) to catch regressions quickly, then confirm with the full suite.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "passing|failing" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Hardhat suite completes with passing >= 1209 and failing <= 33
    - No new failures introduced by the Phase 101 delta-reconciliation fix
    - If failing count is exactly 33, document the pre-existing 33 failures as confirmed pre-existing
    - If failing count is < 33, note the improvement in the SUMMARY
  </acceptance_criteria>
  <done>Hardhat suite: pass >= 1209, fail <= 33, zero new regressions. TEST-02 satisfied.</done>
</task>

</tasks>

<verification>
Overall regression check:

1. `forge test 2>&1 | tail -5` — Foundry: >= 355 pass, <= 14 fail
2. `npm test 2>&1 | grep -E "passing|failing"` — Hardhat: >= 1209 pass, <= 33 fail
3. `forge test --match-test testBafRebuyContributionPreserved -v` — the fix proof test passes
</verification>

<success_criteria>
1. Foundry suite: zero new failures vs 14-failure baseline (plus the new test passes)
2. Hardhat suite: zero new failures vs 33-failure baseline
3. Both results documented in SUMMARY.md with exact counts
</success_criteria>

<output>
After completion, create .planning/phases/102-verification/102-02-SUMMARY.md
</output>
