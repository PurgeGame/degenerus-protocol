---
phase: 177-infrastructure-libraries-misc-comment-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/177-infrastructure-libraries-misc-comment-sweep/177-01-FINDINGS.md
autonomous: true
requirements:
  - CMT-04
must_haves:
  truths:
    - "Every inline comment and NatSpec entry in DegenerusAdmin, DegenerusVault, DegenerusAffiliate, and DegenerusDeityPass has been read against actual code"
    - "Every discrepancy between a comment and code behavior is captured as a LOW or INFO finding"
    - "DegenerusAffiliate v17.1 tiered bonus rate comments (4pts/ETH first 5 ETH, 1.5pts/ETH next 20) have been explicitly verified"
    - "177-01-FINDINGS.md is self-contained and reviewable without re-reading source"
  artifacts:
    - path: ".planning/phases/177-infrastructure-libraries-misc-comment-sweep/177-01-FINDINGS.md"
      provides: "Comment audit findings for DegenerusAdmin, DegenerusVault, DegenerusAffiliate, DegenerusDeityPass"
      contains: "CMT-04"
  key_links:
    - from: "contracts/DegenerusAffiliate.sol"
      to: ".planning/phases/177-infrastructure-libraries-misc-comment-sweep/177-01-FINDINGS.md"
      via: "line-by-line comment vs code comparison, focus on v17.1 tiered rate"
      pattern: "DegenerusAffiliate"
    - from: "contracts/DegenerusAdmin.sol"
      to: ".planning/phases/177-infrastructure-libraries-misc-comment-sweep/177-01-FINDINGS.md"
      via: "line-by-line comment vs code comparison"
      pattern: "DegenerusAdmin"
---

<objective>
Sweep all inline comments and NatSpec in DegenerusAdmin (1134 lines), DegenerusVault (1065 lines),
DegenerusAffiliate (824 lines), and DegenerusDeityPass (391 lines) against actual code behavior.
Log every discrepancy as a LOW or INFO finding with contract name, line reference, and plain-English
description of what the comment says vs what the code does.

Purpose: These four contracts are the core infrastructure layer. DegenerusAffiliate was updated in
v17.1 (tiered bonus rate: 4pts/ETH first 5 ETH, 1.5pts/ETH next 20) — any comment describing the
old flat bonus rate is a discrepancy. DegenerusAdmin and DegenerusVault are long, governance-heavy
contracts that may have stale access control descriptions. DegenerusDeityPass is relatively short
but has NatSpec on pass issuance and privilege logic that needs verification.
Output: 177-01-FINDINGS.md with all comment discrepancies found across these four contracts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep DegenerusAdmin.sol and DegenerusVault.sol comments</name>
  <read_first>
    contracts/DegenerusAdmin.sol
    contracts/DegenerusVault.sol
  </read_first>
  <action>
Read contracts/DegenerusAdmin.sol (1134 lines) and contracts/DegenerusVault.sol (1065 lines) in
full. For every comment (single-line `//`, block `/* */`, and NatSpec `///` / `/** */`), verify
that the comment accurately describes the code immediately below or around it.

Focus areas for DegenerusAdmin:

1. **Access control comments** — DegenerusAdmin contains governance and admin functions.
   Verify that any comment describing who can call each function (owner, operator, multisig,
   game contract) accurately reflects the actual `require` checks, modifiers, or role mappings.
   Pay special attention to any comment that restricts a function to a single caller if the code
   actually permits multiple callers, or vice versa.

2. **Governance mechanism comments** — Any comment describing vote weight calculation, proposal
   lifecycle, threshold values, or quorum. Verify numeric values and formulas match code.
   Note: Phase 176 found (G03-02) that vault owner vote weight is balance + 5% bonus, not a fixed
   5% — if DegenerusAdmin has any related comment, cross-check.

3. **Event comments** — NatSpec on all emitted events. Verify parameter names, types, and
   descriptions match actual event declarations.

4. **General NatSpec** — Every `@notice`, `@param`, `@return`, `@dev` tag on public/external
   functions. Verify parameter names, descriptions, and return values match actual signatures.

5. **Inline clarifying comments** — Any `// e.g.`, `// Note:`, `// only`, etc. Verify accuracy.

Focus areas for DegenerusVault:

1. **Vault accounting comments** — Any comment describing how ETH or stETH is accounted for,
   what constitutes the vault balance, or how withdrawals are calculated. Verify against code.

2. **Access control** — Comments describing which contracts or roles can pull funds from the vault,
   trigger distributions, or update parameters. Verify against actual guards.

3. **stETH integration comments** — Any comment describing the vault's interaction with stETH:
   rebasing behavior, shares vs balances, yield capture. Verify accuracy.

4. **NatSpec completeness** — Every `@notice`, `@param`, `@return`, `@dev` tag. Verify that
   descriptions match the actual behavior including side effects.

For each discrepancy found, record:
- Severity: LOW (misleads a reader about behavior, access control, or accounting) or
  INFO (minor imprecision, stale wording, typo)
- Location: contract filename + line N (or line range N-M)
- What the comment says: exact quote or close paraphrase
- What the code actually does: plain-English correct description

If no discrepancy exists for a section, no finding is needed. Do not force findings.

Create `.planning/phases/177-infrastructure-libraries-misc-comment-sweep/177-01-FINDINGS.md`
with sections `## DegenerusAdmin` and `## DegenerusVault`. If zero findings for a contract,
write "No discrepancies found." under that section heading — do not leave it empty.
  </action>
  <verify>
    <automated>grep -l "DegenerusAdmin\|DegenerusVault" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/177-infrastructure-libraries-misc-comment-sweep/177-01-FINDINGS.md 2>/dev/null && echo "sections present"</automated>
  </verify>
  <acceptance_criteria>
    - 177-01-FINDINGS.md exists and contains a `## DegenerusAdmin` section
    - 177-01-FINDINGS.md contains a `## DegenerusVault` section
    - Every finding has a line number reference in the format "line N" or "lines N-M"
    - Every finding has an explicit severity of LOW or INFO
    - Every finding has a "comment says" and "code does" description
    - If zero findings for a contract: section contains "No discrepancies found"
    - Access control comments in DegenerusAdmin have been explicitly checked
    - Vault accounting and stETH integration comments in DegenerusVault have been explicitly checked
    - Both contracts read in full (not sampled)
  </acceptance_criteria>
  <done>DegenerusAdmin and DegenerusVault swept. All comment discrepancies logged as LOW/INFO
findings with line references. Both sections written to 177-01-FINDINGS.md.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep DegenerusAffiliate.sol and DegenerusDeityPass.sol comments</name>
  <read_first>
    contracts/DegenerusAffiliate.sol
    contracts/DegenerusDeityPass.sol
  </read_first>
  <action>
Read contracts/DegenerusAffiliate.sol (824 lines) and contracts/DegenerusDeityPass.sol (391 lines)
in full. For every comment, verify it accurately describes the code.

Focus areas for DegenerusAffiliate:

1. **v17.1 tiered bonus rate** — The affiliate bonus rate changed to tiered in v17.1:
   4 points per ETH for the first 5 ETH, 1.5 points per ETH for the next 20 ETH.
   Verify:
   - Any comment describing the bonus rate as flat (e.g., "X points per ETH" without tiers)
     is a discrepancy if the code implements tiered logic
   - Any comment quoting specific numeric thresholds (5 ETH, 20 ETH) must match the code constants
   - Any NatSpec on `_playerActivityScore` or related bonus computation functions accurately
     describes the tiered calculation
   - The contract-level `@dev` block mentioning bonus rates reflects the current tiered structure

2. **Affiliate reward percentages** — The architecture block states:
   "Fresh ETH rewards: 25% (levels 0-3), 20% (levels 4+)" and "Recycled ETH rewards: 5% (all levels)"
   Verify these percentages match the actual constants or inline math in the code.

3. **3-tier referral comment** — Architecture block says:
   "3-tier referral: Player → Affiliate (75%) / Upline1 (20%) / Upline2 (5%) winner-takes-all roll"
   Verify this split matches actual code logic (probability weights, payout percentages).

4. **Affiliate bonus cache note (v17.0)** — mintPacked_ bits [185-214] now cache the affiliate
   bonus. If DegenerusAffiliate contains any comment claiming the bonus is "tracked separately"
   or "read fresh from storage each time," that is a discrepancy (related to Phase 175 MINT-CMT-01).

5. **Access control** — Comments on `payAffiliate`, `recordMintData`, and any admin-gated function.
   Verify the described callers match actual `require` or modifier guards.

6. **Event and error NatSpec** — All `@notice`, `@param`, `@return`, `@dev` tags on events,
   errors, and functions. Verify names and descriptions match code.

Focus areas for DegenerusDeityPass:

1. **Pass issuance and privilege comments** — Any comment describing how a Deity Pass is issued,
   what privileges it confers (e.g., reduced fees, trait bonuses, skip conditions), and who can
   issue passes. Verify these match actual code.

2. **HAS_DEITY_PASS_SHIFT flag** — BitPackingLib bit 184 is the deity pass flag. If DegenerusDeityPass
   has any comment describing how this bit is set or cleared, verify it matches the actual write path.

3. **General NatSpec** — Every `@notice`, `@param`, `@return`, `@dev` on all public/external
   functions. Verify accuracy.

4. **Inline comments** — Any `// Note:`, `// only`, `// e.g.` etc. Verify accuracy.

For each discrepancy, record:
- Severity: LOW (misleads about behavior, bonus math, access control, or pass logic) or
  INFO (minor imprecision, stale wording, typo)
- Location: contract filename + line N (or line range N-M)
- What the comment says: exact quote or close paraphrase
- What the code actually does: plain-English correct description

Append findings to `.planning/phases/177-infrastructure-libraries-misc-comment-sweep/177-01-FINDINGS.md`
under sections `## DegenerusAffiliate` and `## DegenerusDeityPass`.

After writing all four contract sections, prepend a header to the file:

```
# Phase 177 Comment Audit — Plan 01 Findings
**Contracts:** DegenerusAdmin, DegenerusVault, DegenerusAffiliate, DegenerusDeityPass
**Requirement:** CMT-04
**Date:** [today]
**Total findings this plan:** [count LOW] LOW, [count INFO] INFO
```

Place this header at the TOP of the file (above all per-contract sections).
  </action>
  <verify>
    <automated>grep "Total findings" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/177-infrastructure-libraries-misc-comment-sweep/177-01-FINDINGS.md 2>/dev/null && grep -c "## Degenerus" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/177-infrastructure-libraries-misc-comment-sweep/177-01-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - 177-01-FINDINGS.md contains a `## DegenerusAffiliate` section
    - 177-01-FINDINGS.md contains a `## DegenerusDeityPass` section
    - 177-01-FINDINGS.md has a header with "Total findings this plan:" summary
    - Every finding has line reference, severity (LOW or INFO), "comment says", "code does"
    - If zero findings for a contract: section contains "No discrepancies found"
    - v17.1 tiered bonus rate has been explicitly checked in DegenerusAffiliate (finding or accuracy confirmation)
    - The 3-tier referral split percentages have been explicitly checked
    - The affiliate reward percentages (25%/20%/5%) have been explicitly verified against code
    - File is self-contained — a reader can understand each finding without opening the contract
  </acceptance_criteria>
  <done>DegenerusAffiliate and DegenerusDeityPass swept. All comment discrepancies logged.
177-01-FINDINGS.md complete with header summary and all four contract sections.</done>
</task>

</tasks>

<verification>
177-01-FINDINGS.md exists with:
- A header section naming all four contracts and providing a finding count summary
- A `## DegenerusAdmin` section (findings or "No discrepancies found")
- A `## DegenerusVault` section (findings or "No discrepancies found")
- A `## DegenerusAffiliate` section (findings or "No discrepancies found")
- A `## DegenerusDeityPass` section (findings or "No discrepancies found")
- Every finding has severity, line reference, and comment-vs-code description
- v17.1 tiered affiliate bonus rate explicitly verified
</verification>

<success_criteria>
- DegenerusAdmin (1134 lines) swept end-to-end with explicit focus on access control and governance comments
- DegenerusVault (1065 lines) swept end-to-end with explicit focus on vault accounting and stETH integration comments
- DegenerusAffiliate (824 lines) swept end-to-end with explicit focus on v17.1 tiered rate, 3-tier referral split, and bonus cache
- DegenerusDeityPass (391 lines) swept end-to-end with explicit focus on pass issuance and privilege comments
- Every comment-code discrepancy captured as LOW or INFO finding with line reference
- 177-01-FINDINGS.md is self-contained and reviewable without opening source contracts
</success_criteria>

<output>
After completion, create `.planning/phases/177-infrastructure-libraries-misc-comment-sweep/177-01-SUMMARY.md`
</output>
