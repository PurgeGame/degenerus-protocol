---
phase: 177-infrastructure-libraries-misc-comment-sweep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/177-infrastructure-libraries-misc-comment-sweep/177-02-FINDINGS.md
autonomous: true
requirements:
  - CMT-04
must_haves:
  truths:
    - "Every inline comment and NatSpec entry in DegenerusQuests, DegenerusJackpots, and DeityBoonViewer has been read against actual code"
    - "Every discrepancy between a comment and code behavior is captured as a LOW or INFO finding"
    - "DegenerusQuests v13.0 Level Quests NatSpec has been explicitly verified against the quest handler and roll chain logic"
    - "177-02-FINDINGS.md is self-contained and reviewable without re-reading source"
  artifacts:
    - path: ".planning/phases/177-infrastructure-libraries-misc-comment-sweep/177-02-FINDINGS.md"
      provides: "Comment audit findings for DegenerusQuests, DegenerusJackpots, DeityBoonViewer"
      contains: "CMT-04"
  key_links:
    - from: "contracts/DegenerusQuests.sol"
      to: ".planning/phases/177-infrastructure-libraries-misc-comment-sweep/177-02-FINDINGS.md"
      via: "line-by-line comment vs code comparison, focus on v13.0 quest logic"
      pattern: "DegenerusQuests"
    - from: "contracts/DegenerusJackpots.sol"
      to: ".planning/phases/177-infrastructure-libraries-misc-comment-sweep/177-02-FINDINGS.md"
      via: "line-by-line comment vs code comparison"
      pattern: "DegenerusJackpots"
---

<objective>
Sweep all inline comments and NatSpec in DegenerusQuests (1915 lines), DegenerusJackpots (650 lines),
and DeityBoonViewer (184 lines) against actual code behavior. Log every discrepancy as a LOW or INFO
finding with contract name, line reference, and plain-English description of what the comment says
vs what the code does.

Purpose: DegenerusQuests is the newest contract (v13.0 Level Quests), so its NatSpec may be fresh
but untested against the final implementation. The quest roll chain and carryover redesign (v13.0
Phase 158.1) may have left comments behind that describe an earlier design. DegenerusJackpots had
jackpot bucket comments updated in earlier phases but `runTerminalJackpot` caller attribution was
flagged as stale in Phase 175 (GameOverModule, not EndgameModule) — verify DegenerusJackpots has
no similar stale attribution. DeityBoonViewer is a read-only view contract; verify its comments
describe correct boon resolution behavior.
Output: 177-02-FINDINGS.md with all comment discrepancies found across these three contracts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep DegenerusQuests.sol comments</name>
  <read_first>
    contracts/DegenerusQuests.sol
  </read_first>
  <action>
Read contracts/DegenerusQuests.sol (1915 lines) in full. For every comment (single-line `//`,
block `/* */`, and NatSpec `///` / `/** */`), verify that the comment accurately describes
the code immediately below or around it.

Focus areas given v13.0 Level Quests history:

1. **Quest roll chain comments** — DegenerusQuests implements the quest roll chain introduced in
   v13.0 and refined in v13.0 Phase 158.1 (carryover redesign). Verify:
   - Any comment describing the roll chain sequence (which quest types trigger in what order)
     matches the actual function call order in the code
   - Any comment describing carryover behavior (how unspent quest progress carries to the next
     level) reflects the final carryover-redesign implementation, not an earlier design
   - Comments on `_rollQuestChain`, `handleAffiliate`, `handleMint`, or similar entry points
     accurately describe inputs, outputs, and which path triggers each type

2. **Quest completion and reward comments** — Any comment describing when a quest is considered
   complete, how the reward is computed, or what happens on completion. Verify against code.

3. **Quest state storage comments** — Any comment describing how quest state is packed or stored
   (bit fields, mappings, expiry). Verify against actual struct declarations and storage mappings.

4. **Access control** — Comments describing who can call `handleAffiliate`, `handleMint`,
   and other entry points. Verify against actual guards in the code.

5. **NatSpec completeness** — Every `@notice`, `@param`, `@return`, `@dev` on all
   public/external functions. Verify parameter names and descriptions match signatures.
   Pay attention to `@return` tags on multi-return functions — does each return value have
   its own description and does the order match the actual return tuple?

6. **Inline clarifying comments** — Any `// e.g.`, `// Note:`, `// carryover`, etc.
   Verify these are accurate given the final post-v158.1 implementation.

For each discrepancy found, record:
- Severity: LOW (misleads about quest logic, carryover behavior, access control, or reward math) or
  INFO (minor imprecision, stale wording, typo)
- Location: `DegenerusQuests.sol` line N (or line range N-M)
- What the comment says: exact quote or close paraphrase
- What the code actually does: plain-English correct description

If no discrepancy exists for a section, no finding is needed. Do not force findings.

Create `.planning/phases/177-infrastructure-libraries-misc-comment-sweep/177-02-FINDINGS.md`
with section `## DegenerusQuests`. If zero findings, write "No discrepancies found." — not empty.
  </action>
  <verify>
    <automated>grep -l "DegenerusQuests" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/177-infrastructure-libraries-misc-comment-sweep/177-02-FINDINGS.md 2>/dev/null && echo "section present"</automated>
  </verify>
  <acceptance_criteria>
    - 177-02-FINDINGS.md exists and contains a `## DegenerusQuests` section
    - Every finding has a line number reference in the format "line N" or "lines N-M"
    - Every finding has an explicit severity of LOW or INFO
    - Every finding has a "comment says" and "code does" description
    - If zero findings: section contains "No discrepancies found"
    - Quest roll chain and carryover behavior comments have been explicitly checked
    - Access control on quest entry points has been explicitly checked
    - The full 1915-line contract has been read (not sampled)
  </acceptance_criteria>
  <done>DegenerusQuests swept. All comment discrepancies logged as LOW/INFO findings with line
references. Section written to 177-02-FINDINGS.md.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep DegenerusJackpots.sol and DeityBoonViewer.sol comments</name>
  <read_first>
    contracts/DegenerusJackpots.sol
    contracts/DeityBoonViewer.sol
  </read_first>
  <action>
Read contracts/DegenerusJackpots.sol (650 lines) and contracts/DeityBoonViewer.sol (184 lines)
in full. For every comment, verify it accurately describes the code.

Focus areas for DegenerusJackpots:

1. **Caller attribution for runTerminalJackpot** — Phase 175 found (LOW finding) that
   `runTerminalJackpot` caller attribution was stale: it listed EndgameModule but only
   GameOverModule calls it. Verify whether DegenerusJackpots has its own NatSpec or comment
   on `runTerminalJackpot` (or the terminal jackpot trigger path) and whether it still
   references EndgameModule. If so, that is a discrepancy.

2. **Jackpot bucket comments** — JackpotBucketLib manages bucket definitions; DegenerusJackpots
   holds the jackpot accounting logic. Any comment describing bucket allocation percentages,
   thresholds, or payout tiers. Verify against actual constants or JackpotBucketLib values
   used at runtime.

3. **_runRewardJackpots caller/timing comment** — Phase 175 found (ADV-CMT-03, LOW) that the
   call site for `_runRewardJackpots` moved from jackpot-phase end to purchase-phase close.
   If DegenerusJackpots has any comment describing when `_runRewardJackpots` is triggered or
   which phase it runs in, verify it matches the actual call site in DegenerusGame/modules.

4. **Access control comments** — Comments on who can trigger jackpot payouts, accumulate jackpot
   funds, or modify jackpot parameters. Verify against actual guards.

5. **NatSpec completeness** — Every `@notice`, `@param`, `@return`, `@dev`. Verify accuracy.

6. **Payout math comments** — Any comment describing jackpot payout formulas, splits, or
   ETH-denomination conversion. Verify against code.

Focus areas for DeityBoonViewer:

1. **Boon resolution comments** — DeityBoonViewer is a read-only view contract that resolves
   which boons a deity pass holder receives. Any comment describing the boon selection logic,
   seed derivation, or output format. Verify against actual code.

2. **Input/output NatSpec** — All `@notice`, `@param`, `@return` on public/external view
   functions. Verify parameter names, descriptions, and return value descriptions match code.

3. **Dependency comments** — Any comment describing which contracts or storage slots
   DeityBoonViewer reads from. Verify these references are still accurate.

4. **Inline comments** — Any `// e.g.`, `// Note:`, etc. Verify accuracy.

For each discrepancy, record:
- Severity: LOW (misleads about jackpot logic, caller attribution, boon resolution, or access
  control) or INFO (minor imprecision, stale wording, typo)
- Location: contract filename + line N (or line range N-M)
- What the comment says: exact quote or close paraphrase
- What the code actually does: plain-English correct description

Append findings to `.planning/phases/177-infrastructure-libraries-misc-comment-sweep/177-02-FINDINGS.md`
under sections `## DegenerusJackpots` and `## DeityBoonViewer`.

After writing all three contract sections, prepend a header to the file:

```
# Phase 177 Comment Audit — Plan 02 Findings
**Contracts:** DegenerusQuests, DegenerusJackpots, DeityBoonViewer
**Requirement:** CMT-04
**Date:** [today]
**Total findings this plan:** [count LOW] LOW, [count INFO] INFO
```

Place this header at the TOP of the file (above all per-contract sections).
  </action>
  <verify>
    <automated>grep "Total findings" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/177-infrastructure-libraries-misc-comment-sweep/177-02-FINDINGS.md 2>/dev/null && grep -c "##" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/177-infrastructure-libraries-misc-comment-sweep/177-02-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - 177-02-FINDINGS.md contains a `## DegenerusJackpots` section
    - 177-02-FINDINGS.md contains a `## DeityBoonViewer` section
    - 177-02-FINDINGS.md has a header with "Total findings this plan:" summary
    - Every finding has line reference, severity (LOW or INFO), "comment says", "code does"
    - If zero findings for a contract: section contains "No discrepancies found"
    - runTerminalJackpot caller attribution has been explicitly checked in DegenerusJackpots
    - _runRewardJackpots timing comment has been explicitly checked
    - Boon resolution and output format comments in DeityBoonViewer have been explicitly checked
    - File is self-contained — a reader can understand each finding without opening the contract
  </acceptance_criteria>
  <done>DegenerusJackpots and DeityBoonViewer swept. All comment discrepancies logged.
177-02-FINDINGS.md complete with header summary and all three contract sections.</done>
</task>

</tasks>

<verification>
177-02-FINDINGS.md exists with:
- A header section naming all three contracts and providing a finding count summary
- A `## DegenerusQuests` section (findings or "No discrepancies found")
- A `## DegenerusJackpots` section (findings or "No discrepancies found")
- A `## DeityBoonViewer` section (findings or "No discrepancies found")
- Every finding has severity, line reference, and comment-vs-code description
- runTerminalJackpot caller attribution explicitly verified in DegenerusJackpots
</verification>

<success_criteria>
- DegenerusQuests (1915 lines) swept end-to-end with explicit focus on v13.0 quest roll chain and
  carryover redesign comment accuracy
- DegenerusJackpots (650 lines) swept end-to-end with explicit focus on caller attribution and
  jackpot timing comments (cross-referenced against Phase 175 findings)
- DeityBoonViewer (184 lines) swept end-to-end with explicit focus on boon resolution comments
- Every comment-code discrepancy captured as LOW or INFO finding with line reference
- 177-02-FINDINGS.md is self-contained and reviewable without opening source contracts
</success_criteria>

<output>
After completion, create `.planning/phases/177-infrastructure-libraries-misc-comment-sweep/177-02-SUMMARY.md`
</output>
