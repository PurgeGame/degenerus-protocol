---
phase: 327-tst-repro-same-results-no-arb-ev-regression-proofs
plan: 05
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [SWAP-08, SWAP-09]
files_modified:
  - test/fuzz/FarFutureSalvageSwap.t.sol
must_haves:
  truths:
    - "The no-arb inequality is PROVEN at the jitter band CEILING: max full payout 110% x fractionBps(6) = 16.5% of face @ d6 < the cheapest far-entry acquisition (~21%, re-confirmed); margin ~4.5pp; the ceiling holds for every d in [6,100]"
    - "BURNIE cannot mint a far-future (d>=6) entry: purchaseCoin has no level arg and every mint targets cachedLevel/+1; proven BEHAVIORALLY (a BURNIE purchase never creates a far entry; the entrypoint to do so is absent), NOT by a runtime source grep (ffi is disabled)"
    - "The base fractionBps keeps >= ~10% margin below the far ticket's present EV so a 110%-jitter-day pawn does not overpay sDGNRS"
    - "Solvency is never violated: claimablePool <= address(this).balance + steth.balanceOf(this) holds across a salvage swap (ticket leg adds pool slack, cash leg is a claimant-to-claimant relabel); array length is bounded <= 32; the >=1 ETH claimable floor + ticket-floor are enforced"
  artifacts:
    - path: "test/fuzz/FarFutureSalvageSwap.t.sol"
      provides: "Foundry tests for the SWAP no-arb ceiling proof + solvency + BURNIE-can't-mint-far + swap-pop membership"
      contains: "sellFarFutureTickets"
  key_links:
    - from: "test/fuzz/FarFutureSalvageSwap.t.sol"
      to: "contracts/modules/DegenerusGameMintModule.sol"
      via: "sellFarFutureTickets executing path + _quoteFarFutureSwap jitter ceiling + _removeFarFutureTickets swap-pop"
      pattern: "sellFarFutureTickets|_quoteFarFutureSwap|FarFutureSwap"
    - from: "test/fuzz/FarFutureSalvageSwap.t.sol"
      to: "contracts/DegenerusGame.sol"
      via: "solvency invariant claimablePool <= balance + stETH across the swap"
      pattern: "claimablePool"
---

<objective>
Prove the LOAD-BEARING salvage-swap economic-security attestations against the applied Phase-326 diff:
- **SWAP-08** (load-bearing) — the no-arb inequality holds at the jitter band CEILING: max full payout `110% × fractionBps(6) = 16.5% of face @ d6` < cheapest far-entry acquisition (~21%, re-confirmed at the v47.0-closure HEAD in 325-ATTEST-SWAP); margin ~4.5pp; and the ceiling holds for EVERY distance `d ∈ [6,100]` (the d6 ceiling is the binding case). BURNIE cannot mint a far entry. The base `fractionBps` keeps ≥~10% margin below the far ticket's present EV so a 110%-jitter-day pawn does not overpay sDGNRS.
- **SWAP-09** — solvency-safe: `claimablePool ≤ address(this).balance + steth.balanceOf(this)` is never violated across a salvage swap (ticket leg routes ETH into pools = gains slack; cash leg is a claimant-to-claimant relabel = neutral); array length bounded ≤32; the `≥1 ETH` claimable floor + the ticket-floor (`require(totalBudget >= oneTicketWei)`) are enforced; and the swap-pop maintains `membership ⟺ packed != 0`.

Purpose: this is the milestone's economic-security headline. The 325-ATTEST-SWAP paper proof must be made EMPIRICAL. The threat (per the phase brief's priority) is a no-arb test that never drives the JITTER TO THE CEILING (so it proves -EV at the mean, not at the band a grinder/waiter captures), or a solvency test that never routes the ticket leg.
Output: `test/fuzz/FarFutureSalvageSwap.t.sol`. `forge test --match-path test/fuzz/FarFutureSalvageSwap.t.sol` exits 0.

NO `contracts/*.sol` (mainnet) edits (subject FROZEN at the Phase-326 diff). Editing `test/` + `contracts/test/*.sol` helpers IS allowed. If a real defect surfaces (esp. a no-arb violation), STOP and surface it with a `## STOP — NO-ARB MARGIN VIOLATED` block naming the violating distance — do NOT widen any band, do NOT weaken assertions, do NOT edit contracts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/326-impl-the-one-batched-contract-diff-all-7-items/326-07-SUMMARY.md
@.planning/phases/326-impl-the-one-batched-contract-diff-all-7-items/326-08-SUMMARY.md
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-SWAP.md

<interfaces>
<!-- Anchors from the APPLIED Phase-326 diff. SWAP was relocated to MintModule (EIP-170). Re-grep at execute-time. -->

contracts/modules/DegenerusGameMintModule.sol:
  function sellFarFutureTickets(address player, uint32[] levels, uint256[] quantities, uint256[] queueIndices) external;  // :907
    // guards: rngLockedFlag revert; gameOver revert; _livenessTriggered revert; len in [1,32]; arrays equal length
    // oneTicketWei = PriceLookupLib.priceForLevel(_activeTicketLevel());  (NOT /4)
    // (_, totalBudget, ticketWei, _) = _quoteFarFutureSwap(player, levels, quantities);
    // require totalBudget >= oneTicketWei  (ticket floor)
    // require claimable[SDGNRS] >= totalBudget + 1 ether  (>=1 ETH floor; NO pendingRedemptionEthValue term; NO daily cap)
    // per line: entries = quantities[i]*4; _removeFarFutureTickets(player,L,entries,queueIndices[i]); _queueTickets(SDGNRS,L,entries,false)
    // claimable[SDGNRS] -= totalBudget; claimable[player] += totalBudget;  (relabel; claimablePool UNCHANGED)
    // qty = ticketWei*4*TICKET_SCALE/oneTicketWei; _purchaseFor(player, qty, 0, bytes32(0), Claimable);  (NORMAL recycled mint)
    // emit FarFutureSwap(player, len, totalBudget, ticketWei, totalBudget - ticketWei);
  function _removeFarFutureTickets(player, L, entries, queueIndex) internal;  // :971 — O(1) caller-verified swap-pop ONLY on full sell-out (packed==0); maintains membership <=> packed != 0
  // BURNIE far-mint absence (the behavioral basis for SWAP-08(d)): purchaseCoin has NO level/distance arg (:858);
  // every BURNIE mint targets cachedLevel / cachedLevel+1; there is NO entrypoint that lets BURNIE create a d>=6 entry.

contracts/modules/DegenerusGameMintStreakUtils.sol:
  function _farFutureFractionBps(uint256 d) internal pure returns (uint256);  // :79  (15%@d6 -> 10%@d20 -> 5%@d100)
  function _quoteFarFutureSwap(player, levels, quantities) internal view returns (totalFaceWei, totalBudget, ticketWei, cashWei);  // :97
    // jitter from rngWordByDay[currentDay-1] (SETTLED prior-day word, freeze-safe): fraction x in [70%,110%], cash share in [20%,60%]
    // faceWei per line = priceForLevel(L) * wholeTickets; budget = faceWei * fractionBps(d) * jitterMult / scale

contracts/DegenerusGame.sol:
  function sellFarFutureTickets(...) external;  // :1933 thin wrapper (resolve + delegatecall MINT_MODULE)
  function previewSellFarFutureTickets(player, levels, quantities) external view returns (...);  // :1964 -> _quoteFarFutureSwap (shares EXACT valuation)

Solvency invariant (DegenerusGame NatSpec): address(this).balance + steth.balanceOf(this) >= claimablePool.

325-ATTEST-SWAP locked figures (the references the test asserts against):
  - salvage ceiling @ d6 = 110% × fractionBps(6)=1500bps = 16.50% of face
  - cheapest realistic far-entry acquisition (lootbox tier-1 conditional, EV-capped) ~21% of face; whale bundle ~45%; systematic lootbox ~1437%
  - margin @ d6 = 21% - 16.5% = +4.50 pp (binding case); for d>6 fraction falls (->5%), margin only widens
  - BURNIE cannot mint d>=6 (purchaseCoin no level arg :858; mint targets cachedLevel/+1; BURNIE-lootbox->future REMOVED)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SWAP-08 — no-arb at the jitter band CEILING (sweep d in [6,100]) + BURNIE-can't-mint-far</name>
  <files>test/fuzz/FarFutureSalvageSwap.t.sol</files>
  <read_first>
    325-ATTEST-SWAP.md (sections A no-arb, A.5 margin table, B BURNIE-can't-mint, the HARD STOP rule —
    READ ALL; this is the locked reference), contracts/modules/DegenerusGameMintStreakUtils.sol
    (`_farFutureFractionBps` :79, `_quoteFarFutureSwap` :97-135 — the jitter source + bands),
    contracts/modules/DegenerusGameMintModule.sol `sellFarFutureTickets` :907 + `purchaseCoin` (no level arg :858),
    contracts/libraries/PriceLookupLib.sol `priceForLevel`, contracts/DegenerusGame.sol `rngWordByDay`
    + `previewSellFarFutureTickets` :1964; the deploy harness (test/fuzz/helpers/DeployProtocol.sol) +
    how to seed a far-future ticket balance + how to set `rngWordByDay[currentDay-1]`; 326-07-SUMMARY.md.
  </read_first>
  <action>
    Create `test/fuzz/FarFutureSalvageSwap.t.sol`. Tests:

    (a) `test_SWAP08_NoArbAtCeiling_SweepAllDistances`: for EVERY distance `d ∈ [6,100]` (loop), set
        `rngWordByDay[currentDay-1]` to the seed that produces the JITTER CEILING (fraction multiplier = 110%,
        the worst case for the protocol). Compute the salvage payout fraction via `previewSellFarFutureTickets`
        (which shares `_quoteFarFutureSwap` with the executing path) for a single far entry at distance `d`, and
        derive `maxPayoutFraction(d) = totalBudget / faceWei` at the ceiling. Assert
        `maxPayoutFraction(d) <= CEILING_BOUND(d)` where `CEILING_BOUND(6) ≈ 16.5%` and the curve falls for larger
        d; AND assert `maxPayoutFraction(d) < ACQUISITION_FLOOR` where `ACQUISITION_FLOOR = 21% of face` (the
        cheapest re-confirmed acquisition). If ANY `d` violates `maxPayoutFraction(d) < ACQUISITION_FLOOR`, the
        test must FAIL and the executor emits a `## STOP — NO-ARB MARGIN VIOLATED` block in the SUMMARY naming
        the distance (do NOT widen bands). Assert the binding margin at d6 is `>= +4.0 pp` (≈4.5pp).
    (b) `test_SWAP08_JitterCeilingIsActuallyReached`: prove the 110% ceiling is the seed-reachable maximum — search
        a band of `rngWordByDay` seeds and assert the realized fraction multiplier never exceeds 110% and DOES
        reach ~110% for some seed (so (a)'s ceiling is the true worst case, not a number the contract can never
        produce). This is the anti-false-confidence guard for the ceiling.
    (c) `test_SWAP08_BaseFractionBelowFarTicketPresentEv`: assert the BASE `fractionBps(d)` (jitter = 100%/mean,
        not ceiling) keeps `>= ~10% margin` below the far ticket's present EV per the SWAP-08 clause (the
        110%-day pawn doesn't overpay sDGNRS). Use the present-EV reference from 325-ATTEST-SWAP / the contract's
        own `priceForLevel` discounting.
    (d) `test_SWAP08_BurnieCannotMintFarEntry`: prove BEHAVIORALLY (the SOLE approach — `vm.ffi` is DISABLED in
        `foundry.toml`, there is no `ffi = true`, and `fs_permissions` is read-only; do NOT attempt a runtime
        shell `grep` of source) that BURNIE cannot mint a far-future (d>=6) entry:
        - Assert the BURNIE mint surface has NO distance/level argument: drive a BURNIE purchase via `purchaseCoin`
          (the only BURNIE mint entrypoint; it takes no level/distance arg) and assert the entries it creates land
          ONLY at `cachedLevel` / `cachedLevel + 1` — never at a far (`currentLevel + 6..100`) level. Exercise it
          and inspect the resulting ticketQueue: assert zero entries appear at any d>=6 key.
        - Assert the ABSENCE of any BURNIE→far entrypoint by exercised behavior: there is no callable function that
          accepts a far level from a BURNIE-funded path (the v47 BURNIE-lootbox→future path was removed in Phase
          326). Probe each plausible far-creating call from a BURNIE-funded caller and assert it either does not
          exist (no such selector to call) or reverts / cannot place a d>=6 entry. The proof is the EXERCISED
          behavior (no far entry ever appears from a BURNIE mint) and the absence of the entrypoint — NOT a
          source-text grep performed at test runtime.
    Run command: `forge test --match-path test/fuzz/FarFutureSalvageSwap.t.sol -vv`.
  </action>
  <verify>
    <automated>forge test --match-path test/fuzz/FarFutureSalvageSwap.t.sol --match-test "SWAP08" -vv 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    (a) PASSES proving `maxPayoutFraction(d) < 21% of face` for ALL `d ∈ [6,100]` at the 110% jitter CEILING,
    with the d6 margin `>= +4.0 pp`; (b) PASSES proving the 110% ceiling is the true seed-reachable maximum and
    is actually reached; (c) PASSES proving base fractionBps keeps ≥~10% margin below far-ticket present EV;
    (d) PASSES proving BURNIE cannot mint a far entry — the BURNIE mint surface has no distance arg and a BURNIE
    purchase only ever lands at cachedLevel/+1 (never d>=6), and no BURNIE-funded entrypoint can place a far entry
    — proven BEHAVIORALLY by exercised reverts / absence of the entrypoint, NOT by a runtime source grep
    (`vm.ffi` is disabled). Any no-arb violation triggers a `## STOP` (test fails), not a band-widen. Zero
    `contracts/*.sol` (mainnet) modifications.
  </acceptance_criteria>
  <done>The no-arb floor is proven EMPIRICALLY at the jitter band ceiling across all distances, the ceiling is proven reachable, and BURNIE is proven unable to mint a far entry behaviorally (no ffi grep) — the 325-ATTEST-SWAP paper proof made empirical.</done>
</task>

<task type="auto">
  <name>Task 2: SWAP-09 — solvency-safe + ticket/ETH floors + swap-pop membership + bounded array</name>
  <files>test/fuzz/FarFutureSalvageSwap.t.sol</files>
  <read_first>
    contracts/modules/DegenerusGameMintModule.sol `sellFarFutureTickets` :907-970 (the ticket-floor require,
    the `>=1 ether` claimable floor, the claimable relabel, the normal-mint ticket leg, the len<=32 gate) +
    `_removeFarFutureTickets` :971 (swap-pop on full sell-out, membership <=> packed!=0),
    contracts/DegenerusGame.sol solvency invariant NatSpec + `claimablePool`, the far-future ticketQueue
    samplers (`sampleFarFutureTickets`, `_awardFarFutureCoinJackpot`) the swap-pop must keep correct,
    the Task-1 harness; 325-ATTEST-SWAP.md §D (swap-pop consumer enumeration + H-CANCEL-SWAP-MISS proof).
  </read_first>
  <action>
    Add SWAP-09 tests to `test/fuzz/FarFutureSalvageSwap.t.sol`:

    (a) `test_SWAP09_SolvencyAcrossSwap`: seed a player with far-future entries and sDGNRS with claimable >
        budget + 1 ether. Snapshot `claimablePool` and `address(this).balance + steth.balanceOf(this)` before;
        execute `sellFarFutureTickets`; assert the solvency invariant `claimablePool <= balance + stETH` holds
        AFTER, and that `claimablePool` is unchanged or INCREASED slack (cash leg is a claimant-to-claimant
        relabel = neutral; ticket leg routes ETH into pools = gains slack). Drive the ticket leg explicitly
        (assert a current-level ticket was minted to the player and a cash residual landed in player claimable).
    (b) `test_SWAP09_TicketFloorEnforced`: a swap whose `totalBudget < oneTicketWei` REVERTS (`vm.expectRevert`)
        — too small to deliver one whole ticket; and a swap that JUST clears the floor mints exactly ≥1 whole
        current-level ticket.
    (c) `test_SWAP09_EthFloorEnforced`: a swap that would leave `claimable[SDGNRS] < 1 ether` REVERTS; one that
        leaves `>= 1 ether` succeeds. Proves the ≥1 ETH redemption-desk floor.
    (d) `test_SWAP09_ArrayBound`: `len == 33` (or arrays of mismatched length) REVERTS; `len == 32` is accepted.
    (e) `test_SWAP09_SwapPopMembershipMaintained`: full sell-out of a level pops the seller from
        `ticketQueue[ffk]` (caller-verified `q[idx]==player`) so `membership ⟺ packed != 0` holds; a PARTIAL
        sell does NOT pop (seller stays enrolled, packed still != 0); then confirm the far-future jackpot
        sampler still returns only live holders after the pop (no address(0) / stale entry leaks into a draw).
        Also probe the residual fail-safe: a stale `queueIndex` (`q[idx] != player`) REVERTS the line (no silent
        corruption).
    Run command: `forge test --match-path test/fuzz/FarFutureSalvageSwap.t.sol`.
  </action>
  <verify>
    <automated>forge test --match-path test/fuzz/FarFutureSalvageSwap.t.sol --match-test "SWAP09" -vv 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    All SWAP09 tests PASS: solvency `claimablePool <= balance + stETH` holds across the swap with the ticket leg
    explicitly exercised; the ticket-floor and ≥1 ETH floor both revert when unmet and succeed when met; `len>32`
    reverts and `len==32` is accepted; the swap-pop maintains `membership ⟺ packed != 0` (full sell-out pops,
    partial does not) with the far-future sampler returning only live holders, and a stale `queueIndex` reverts.
    Zero `contracts/*.sol` (mainnet) modifications.
  </acceptance_criteria>
  <done>The salvage swap is proven solvency-safe (invariant holds, ticket leg routes slack), the ticket/ETH floors + array bound are enforced, and the swap-pop maintains the membership invariant the far-future samplers rely on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seller (operator/drone) → game | Untrusted levels/quantities/queueIndices; the swap must be -EV at the band a grinder/waiter captures |
| salvage swap → claimablePool / pools | The cash relabel + ticket mint must never break `claimablePool <= balance + stETH` |

## STRIDE Threat Register (LOAD-BEARING economic-security attestation — priority)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-327-05-FC1 | Information (false confidence) | no-arb test proves -EV only at the MEAN, never at the 110% jitter CEILING | mitigate | Task 1(a) drives `rngWordByDay[currentDay-1]` to the CEILING seed for EVERY d; Task 1(b) proves 110% is the true seed-reachable max and is actually reached |
| T-327-05-FC2 | Information (false confidence) | no-arb only checked at d6, not the full distance range | mitigate | Task 1(a) loops ALL d ∈ [6,100] and asserts `< 21%` acquisition floor at each |
| T-327-05-FC3 | Spoofing | BURNIE-can't-mint-far asserted by hand-wave or a runtime source grep that ffi cannot run | mitigate | Task 1(d) proves it BEHAVIORALLY (the SOLE approach; ffi disabled): a BURNIE purchase only lands at cachedLevel/+1, no BURNIE-funded entrypoint can place a d>=6 entry — proven by exercised reverts / absence of the entrypoint, never by a runtime `grep` |
| T-327-05-FC4 | Information (false confidence) | solvency test never routes the ticket leg (so the slack-gain claim is unproven) | mitigate | Task 2(a) explicitly asserts a current-level ticket was minted + cash residual landed, and the invariant holds after |
| T-327-05-ARB | Elevation | a genuine no-arb violation gets silently "fixed" by widening a band | mitigate | The plan FORBIDS band-widening; a violation emits `## STOP — NO-ARB MARGIN VIOLATED` and fails the test |
| T-327-05-SC | Tampering | npm/forge install of a new test dep | accept | No new package installs; existing forge-std + repo helpers only |
</threat_model>

<verification>
- `forge test --match-path test/fuzz/FarFutureSalvageSwap.t.sol` exits 0.
- SWAP-08: no-arb `maxPayoutFraction(d) < 21%` at the 110% CEILING for ALL d ∈ [6,100]; d6 margin ≥ +4.0pp; ceiling reachable; base fractionBps ≥~10% below far-ticket present EV; BURNIE can't mint far (proven behaviorally — no ffi grep).
- SWAP-09: solvency `claimablePool <= balance + stETH` holds across the swap (ticket leg exercised); ticket-floor + ≥1 ETH floor enforced; len>32 reverts; swap-pop maintains membership ⟺ packed != 0; stale index reverts.
- Any no-arb violation → `## STOP` (no band-widen). Zero `contracts/*.sol` (mainnet) modifications.
</verification>

<success_criteria>
The load-bearing salvage-swap no-arb floor is proven EMPIRICALLY at the jitter band ceiling across all distances (margin ~4.5pp @ d6), BURNIE is proven unable to mint a far entry behaviorally (the SOLE approach — `vm.ffi` is disabled; no runtime source grep), and the swap is proven solvency-safe with the ticket/ETH floors, array bound, and swap-pop membership invariant all enforced — all against the applied Phase-326 diff.
</success_criteria>

<output>
Create `.planning/phases/327-tst-repro-same-results-no-arb-ev-regression-proofs/327-05-SUMMARY.md` when done.
Record: the per-distance ceiling margins (esp. d6), the proof the 110% ceiling is seed-reachable, the BURNIE-can't-mint behavioral-probe result (no ffi grep used), the solvency before/after deltas, and the floor/array/swap-pop assertions. If a no-arb violation surfaced, the `## STOP — NO-ARB MARGIN VIOLATED` block.
</output>
</content>
