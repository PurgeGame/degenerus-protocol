---
phase: 08-burnie-economics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v1.1-burnie-supply.md
autonomous: true
requirements:
  - BURN-02
  - BURN-03
  - BURN-04

must_haves:
  truths:
    - "A game theory agent can enumerate every path that creates new BURNIE supply (mintForCoinflip, mintForGame, vaultMintTo) vs virtual credits (creditFlip)"
    - "A game theory agent can enumerate every BURNIE burn sink (coinflip loss, decimator burn, ticket purchase burn, vault transfer) with exact conditions and minimums"
    - "A game theory agent can verify the vault invariant (totalSupply + vaultAllowance = supplyIncUncirculated) and trace how each operation affects it"
    - "Lootbox BURNIE output formulas (low/high path BPS values, presale bonus) are documented with exact conversion expressions"
  artifacts:
    - path: "audit/v1.1-burnie-supply.md"
      provides: "Complete BURNIE supply dynamics reference for game theory agents"
      contains: "supplyIncUncirculated"
  key_links:
    - from: "audit/v1.1-burnie-supply.md"
      to: "contracts/BurnieCoin.sol"
      via: "Supply invariant and all mint/burn entry points"
      pattern: "totalSupply.*vaultAllowance"
    - from: "audit/v1.1-burnie-supply.md"
      to: "contracts/modules/DegenerusGameLootboxModule.sol"
      via: "Lootbox BURNIE output formulas"
      pattern: "LOOTBOX_LARGE_BURNIE"
    - from: "audit/v1.1-burnie-supply.md"
      to: "contracts/DegenerusVault.sol"
      via: "Vault claim and DGVB share redemption mechanics"
      pattern: "vaultMintTo|DGVB"
---

<objective>
Document the complete BURNIE supply model: all earning paths that bring BURNIE into circulation, all burn sinks that remove it, and the vault reserve mechanics that constrain supply.

Purpose: Enable game theory agents to model BURNIE supply dynamics over time -- inflows from coinflip wins/lootboxes/quests/vault, outflows from coinflip losses/decimator/ticket burns, and the vault invariant that governs the reserve.

Output: `audit/v1.1-burnie-supply.md`
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-burnie-economics/08-RESEARCH.md

@audit/v1.1-purchase-phase-distribution.md (style reference)
@audit/v1.1-transition-jackpots.md (decimator cross-reference)

Primary source contracts:
@contracts/BurnieCoin.sol (ERC20, supply invariant, vault escrow, decimator burns, mint/burn entry points)
@contracts/modules/DegenerusGameLootboxModule.sol (lootbox BURNIE output formulas, boon BPS values)
@contracts/DegenerusQuests.sol (quest types, fixed rewards 100/200 BURNIE)
@contracts/DegenerusVault.sol (vault deposit/claim/mint, DGVB share redemption)
@contracts/modules/DegenerusGameMintModule.sol (BURNIE ticket purchases via burnCoin)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document BURNIE supply dynamics -- earning paths, burn sinks, and vault mechanics</name>
  <files>audit/v1.1-burnie-supply.md</files>
  <action>
Create a reference document following the established v1.1 style (purpose statement, date, source contracts header, table of contents, numbered sections with entry points, formulas in Solidity code blocks, constants reference table).

Structure the document with these sections:

1. **Overview** -- BURNIE is an ERC20 (18 decimals) with no hard supply cap. Supply is governed by: earning paths (inflows), burn sinks (outflows), and the vault reserve invariant. Diagram the supply flow: Vault Reserve <-> Circulation <-> Burns, with coinflip minting as an independent supply source outside the vault.

2. **Supply Invariant (BURN-04 core)** -- Document the fundamental constraint:
   - `totalSupply + vaultAllowance = supplyIncUncirculated`
   - Initial state: totalSupply=0, vaultAllowance=2,000,000 BURNIE, supplyIncUncirculated=2,000,000 BURNIE
   - Complete operation impact table (from Research BURN-04):
     | Operation | totalSupply | vaultAllowance | supplyIncUncirculated |
     Including: _mint, _burn, _mint(VAULT), _burn(VAULT), transfer to VAULT, vaultEscrow, vaultMintTo, mintForCoinflip, burnForCoinflip
   - Critical note: supplyIncUncirculated is NOT constant (changes with every mint/burn/vaultEscrow)
   - Critical note: No hard supply cap -- mintForCoinflip creates supply without vault constraint
   - Include exact Solidity from BurnieCoin.sol with line numbers for each operation

3. **Earning Paths (BURN-02)** -- Document each path that brings BURNIE into circulation or credits:

   3a. **Coinflip Winnings (mint, primary)**: mintForCoinflip in _claimCoinflipsInternal. Creates new supply. Payout = stake + (stake * rewardPercent / 100). Cross-reference audit/v1.1-burnie-coinflip.md for payout details.

   3b. **Lootbox BURNIE Rewards (credit)**: DegenerusGameLootboxModule._resolveLootboxRoll. 25% chance of large BURNIE roll from _resolveCommon:
   - Low path (80% of 25%): LOOTBOX_LARGE_BURNIE_LOW_BASE_BPS=5808 + roll*LOOTBOX_LARGE_BURNIE_LOW_STEP_BPS=477, range [58.08%-130.43%]
   - High path (20% of 25%): LOOTBOX_LARGE_BURNIE_HIGH_BASE_BPS=30705 + (roll-16)*LOOTBOX_LARGE_BURNIE_HIGH_STEP_BPS=9430, range [307.05%-589.95%]
   - Conversion: burnieOut = (amount * largeBurnieBps / 10000) * PRICE_COIN_UNIT / targetPrice
   - Presale bonus: LOOTBOX_PRESALE_BURNIE_BONUS_BPS=6200 (62% extra)
   - Delivery: creditFlip (virtual coinflip stake, NOT minted supply)
   - Include exact Solidity expressions with line numbers from LootboxModule

   3c. **Quest Rewards (credit)**: DegenerusQuests.sol:
   - Slot 0 (deposit ETH): QUEST_SLOT0_REWARD = 100 BURNIE
   - Slot 1 (random type): QUEST_RANDOM_REWARD = 200 BURNIE
   - Quest types: MINT_ETH, MINT_BURNIE, FLIP, AFFILIATE, DECIMATOR, LOOTBOX, DEGENERETTE_ETH, DEGENERETTE_BURNIE
   - Delivery: creditFlip (virtual coinflip stake)

   3d. **Bounty Payout (credit)**: Half of bounty pool on win days, credited via _addDailyFlip. Cross-reference audit/v1.1-burnie-coinflip.md.

   3e. **LINK Donation Credit**: creditLinkReward, admin-credited flip stake for LINK donors.

   3f. **Vault Mint (mint)**: vaultMintTo called by DegenerusVault for share redemptions. Decreases vaultAllowance, increases totalSupply. Constrained by current vaultAllowance.

   3g. **Game Direct Mint (mint)**: mintForGame for degenerette wins. Creates new supply.

   Include summary table: Path | Delivery Method | Creates New Supply?

   CRITICAL: Distinguish clearly between "creates new supply" (mint functions) vs "virtual credit" (creditFlip). Per Research Pitfall 1, agents must NOT count creditFlip as circulating supply.

4. **Burn Sinks (BURN-03)** -- Document each path that removes BURNIE:

   4a. **Coinflip Losses**: 50% of deposited BURNIE permanently destroyed (burned on deposit via burnForCoinflip, never returned on loss). Cross-reference coinflip doc.

   4b. **Decimator Burns**: decimatorBurn in BurnieCoin.sol:
   - Minimum: DECIMATOR_MIN = 1000 BURNIE
   - Precondition: decWindow() returns true
   - Bucket weighting: DECIMATOR_BUCKET_BASE=12 (worst), DECIMATOR_MIN_BUCKET_NORMAL=5, DECIMATOR_MIN_BUCKET_100=2
   - Activity score scaling: DECIMATOR_ACTIVITY_CAP_BPS=23500 (235% max)
   - Burn multiplier: BPS_DENOMINATOR + (bonusBps / 3) = 1x + 1/3 of activity bonus
   - Boon system: DECIMATOR_BOON_CAP = 50,000 BURNIE max base for boon boost (10%, 25%, 50% tiers)
   - Cross-reference audit/v1.1-transition-jackpots.md for decimator jackpot distribution
   - Include exact Solidity with line numbers

   4c. **BURNIE Ticket Purchases**: DegenerusGameMintModule.purchaseCoin / _purchaseBurnieLootboxFor:
   - Burns via BurnieCoin.burnCoin (calls _burn)
   - Pricing: ethToBurnieValue(costWei, priceWei) using PRICE_COIN_UNIT / priceWei
   - BURNIE lootbox minimum: BURNIE_LOOTBOX_MIN = 1000 BURNIE
   - Restriction: Blocked within 30 days of liveness-guard timeout

   4d. **Transfers to Vault Address**: BurnieCoin._transfer special case when `to == VAULT`:
   - Does NOT credit VAULT balance; instead decreases totalSupply, increases vaultAllowance
   - Effect: BURNIE exits circulation, returns to virtual reserve
   - Invariant preserved: totalSupply + vaultAllowance unchanged

   Include summary table: Sink | Permanent? | Condition | Minimum Amount

5. **Vault Claim Mechanics (BURN-04 detail)** -- Document DGVB share redemption from DegenerusVault:
   - Formula: coinOut = (DGVB reserve * sharesBurned) / totalDGVBSupply
   - Source priority: vault balance first, then claimable coinflips, then vaultMintTo remainder
   - This is the primary mechanism for BURNIE to flow from vault reserve to circulation
   - Include exact Solidity from DegenerusVault.sol with line numbers

6. **Supply Dynamics Summary for Agents** -- Key observations:
   - Coinflip is net-negative on supply (~1.5% expected loss per cycle) -- acts as gradual sink
   - Vault allowance grows via game deposits (vaultEscrow) -- expanding potential future supply
   - Claim window expiry is an additional supply sink (unclaimed flips permanently lost)
   - Practical supply constraint: negative-EV coinflip + permanent decimator burns create deflationary pressure counterbalancing minting
   - Include simplified supply flow equation for agent modeling

7. **Worked Example** -- Trace a complete BURNIE lifecycle:
   - Player deposits 1000 BURNIE into coinflip (burned immediately, totalSupply -1000)
   - Win with 96% bonus: minted 1960 BURNIE (totalSupply +1960, net +960)
   - Player burns 1000 BURNIE for decimator (totalSupply -1000)
   - Lootbox credits 500 BURNIE via creditFlip (NO supply change -- virtual only)
   - Track totalSupply, vaultAllowance, supplyIncUncirculated at each step

8. **Constants Reference Table** -- All constants: name, exact value, source file, line number. Include all from earning paths, burn sinks, vault mechanics, and lootbox formulas.

IMPORTANT: Every formula must include the Solidity expression AND constant values substituted, verified against contract source with file and line reference. Use exact variable names from contracts. Clearly label each earning path and burn sink as "creates supply" vs "virtual credit" vs "permanent burn" vs "vault return".
  </action>
  <verify>
    <automated>grep -c "supplyIncUncirculated\|vaultAllowance\|mintForCoinflip\|burnForCoinflip\|decimatorBurn\|creditFlip\|LOOTBOX_LARGE_BURNIE\|DECIMATOR_MIN\|vaultMintTo" audit/v1.1-burnie-supply.md</automated>
  </verify>
  <done>Document exists with all 8 sections. Supply invariant table covers all 8+ operations. Earning paths table distinguishes mint vs credit for all 7 paths. Burn sinks table covers all 4 sinks with conditions. Vault claim formula includes DGVB share redemption. Worked example traces supply numbers at each step. Constants table has at least 20 entries. BURN-02, BURN-03, and BURN-04 are fully addressed.</done>
</task>

</tasks>

<verification>
- Supply invariant operation table matches BurnieCoin.sol source exactly
- All lootbox BPS values match DegenerusGameLootboxModule.sol constants
- Decimator parameters match BurnieCoin.sol constants
- Quest rewards match DegenerusQuests.sol constants
- Vault claim formula matches DegenerusVault.sol source
- Earning paths correctly distinguished as mint vs credit (Research Pitfall 1)
- Worked example arithmetic is internally consistent
</verification>

<success_criteria>
A game theory agent reading only this document (plus the coinflip companion doc) can model complete BURNIE supply dynamics: predict circulating supply changes from any combination of earning/burning activities, verify the vault invariant, and compute lootbox BURNIE output for any ETH value and price level.
</success_criteria>

<output>
After completion, create `.planning/phases/08-burnie-economics/08-02-SUMMARY.md`
</output>
