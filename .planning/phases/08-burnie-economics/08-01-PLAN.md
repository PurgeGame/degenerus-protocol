---
phase: 08-burnie-economics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v1.1-burnie-coinflip.md
autonomous: true
requirements:
  - BURN-01

must_haves:
  truths:
    - "A game theory agent can compute exact coinflip EV including all payout tiers (1.5x, 2.5x, 1.78x-2.15x) with correct probabilities"
    - "A game theory agent can model the bounty system lifecycle: accumulation, arming, resolution (win/loss), and clearing"
    - "A game theory agent can compute recycling bonuses (normal 1% capped 1000, afKing 1.6%+ with deity scaling) for auto-rebuy EV modeling"
    - "Claim window expiry (30d first-time, 90d subsequent) is documented as a supply sink with exact conditions"
  artifacts:
    - path: "audit/v1.1-burnie-coinflip.md"
      provides: "Complete coinflip mechanics reference for game theory agents"
      contains: "COINFLIP_EXTRA_MIN_PERCENT"
  key_links:
    - from: "audit/v1.1-burnie-coinflip.md"
      to: "contracts/BurnieCoinflip.sol"
      via: "Verified formulas with exact line numbers"
      pattern: "BurnieCoinflip.*line"
    - from: "audit/v1.1-burnie-coinflip.md"
      to: "contracts/BurnieCoin.sol"
      via: "burnForCoinflip and mintForCoinflip mechanics"
      pattern: "burnForCoinflip|mintForCoinflip"
---

<objective>
Document the complete BurnieCoinflip mechanics: stake requirements, 50/50 odds, variable payout multipliers, bounty system, recycling bonuses, auto-rebuy, claim windows, coinflip boons, and last-purchase-day EV adjustment.

Purpose: Enable game theory agents to compute exact coinflip EV across all scenarios (fresh deposit, auto-rebuy, afKing, bounty-armed, boon-boosted, last-day adjusted) with precise formulas and constants.

Output: `audit/v1.1-burnie-coinflip.md`
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-burnie-economics/08-RESEARCH.md

@audit/v1.1-purchase-phase-distribution.md (style reference)

Primary source contracts:
@contracts/BurnieCoinflip.sol (all coinflip constants, payout formulas, bounty system, auto-rebuy, recycling)
@contracts/BurnieCoin.sol (burnForCoinflip, mintForCoinflip, creditFlip)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document BurnieCoinflip mechanics</name>
  <files>audit/v1.1-burnie-coinflip.md</files>
  <action>
Create a reference document following the established v1.1 style (purpose statement, date, source contracts header, table of contents, numbered sections with entry points, formulas in Solidity code blocks, constants reference table).

Structure the document with these sections:

1. **Overview** -- BURNIE coinflip is the central wagering mechanic. Daily windows, VRF-based 50/50 outcomes, variable payout multipliers. Reference BurnieCoinflip.sol as primary source.

2. **Core Mechanics (BURN-01 primary)** -- Document the coinflip lifecycle:
   - Deposit: `depositCoinflip()` burns BURNIE via `burnForCoinflip`, minimum 100 BURNIE (MIN = 100 ether)
   - Window resolution: One flip per day, VRF determines win/loss (`win = (rngWord & 1) == 1`)
   - Claim: `_claimCoinflipsInternal` mints winnings via `mintForCoinflip` (creates new supply)
   - Loss: Principal permanently destroyed, 1 WWXRP consolation (COINFLIP_LOSS_WWXRP_REWARD = 1 ether)

3. **Payout Multiplier Distribution** -- Document the `rewardPercent` system from `seedWord % 20`:
   - Roll 0 (5%): rewardPercent = 50%, total 1.50x
   - Roll 1 (5%): rewardPercent = 150%, total 2.50x
   - Roll 2-19 (90%): rewardPercent = COINFLIP_EXTRA_MIN_PERCENT (78) + (seedWord % COINFLIP_EXTRA_RANGE (38)) = [78%, 115%], total [1.78x, 2.15x]
   - Presale bonus: +6pp added to rewardPercent
   - Calculate weighted average EV: 5%*1.5x + 5%*2.5x + 90%*avg(1.78x,2.15x) = expected ~0.985x per flip
   - Include exact Solidity expressions with line numbers

4. **Last Purchase Day EV Adjustment** -- Document non-presale bonus flip day adjustment:
   - Ratio calculation: current day flips / previous day flips
   - EV adjustment range: 0 bps (ratio <= 1.0x) to +300 bps (ratio >= 3.0x), linear interpolation between
   - Formula: `adjustedBps = rewardPercent * 100 + (targetRewardBps - COINFLIP_REWARD_MEAN_BPS)` where COINFLIP_REWARD_MEAN_BPS = 9685
   - Document `targetRewardBps = 10000 + evBps * 2`

5. **Bounty System** -- Document complete bounty lifecycle:
   - Accumulation: +PRICE_COIN_UNIT (1000 BURNIE) per resolved window
   - Arming: Player sets new all-time `biggestFlipEver`; if bounty already armed, must exceed by 1% (min +1 wei)
   - Resolution: Half of bounty pool removed. WIN = half credited as flip stake + DGNRS reward. LOSS = half destroyed
   - Clearing: `bountyOwedTo` cleared after each resolution
   - Restriction: Cannot arm during RNG lock; only direct deposits (not credited flips) can arm
   - Include exact Solidity expressions with line numbers

6. **Recycling Bonuses** -- Document bonus mechanics on rebet/auto-rebuy carry:
   - Normal recycling: 1% of rebet amount, capped at 1000 BURNIE
   - AfKing recycling: base AFKING_RECYCLE_BONUS_BPS = 160 (1.60%)
   - Deity bonus: AFKING_DEITY_BONUS_PER_LEVEL_HALF_BPS = 2 per level since activation, max AFKING_DEITY_BONUS_MAX_HALF_BPS = 300
   - Deity cap: DEITY_RECYCLE_CAP = 1,000,000 BURNIE; excess gets base only
   - Formula: `bonus = (amount * (baseHalfBps + deityBonusHalfBps)) / (BPS_DENOMINATOR * 2)`
   - Include worked example: 50,000 BURNIE rebet with 10 levels of deity = exact bonus calculation

7. **Claim Windows and Expiry** -- Document claim timing:
   - First-time: COIN_CLAIM_FIRST_DAYS = 30 days from flipsClaimableDay
   - Subsequent: COIN_CLAIM_DAYS = 90 days
   - Auto-rebuy: Extends to autoRebuyStartDay (no expiry while enabled)
   - Unclaimed beyond window: permanently lost (supply sink)
   - AUTO_REBUY_OFF_CLAIM_DAYS_MAX = 1095 (max days processed in deep auto-rebuy off)

8. **Coinflip Boons** -- Document deity lootbox coinflip boons:
   - COINFLIP_5: 500 bps (5%), max deposit cap 100,000 BURNIE, max bonus 5,000 BURNIE
   - COINFLIP_10: 1000 bps (10%), max deposit cap 100,000 BURNIE, max bonus 10,000 BURNIE
   - COINFLIP_25: 2500 bps (25%), max deposit cap 100,000 BURNIE, max bonus 25,000 BURNIE
   - Single-use, consumed on manual deposit only

9. **Agent Simulation Notes** -- Summary for agent consumption:
   - Expected EV per flip cycle (~0.985x without recycling, slightly better with)
   - Auto-rebuy sequence modeling (compound wins, recycling bonuses)
   - Bounty as probabilistic income stream
   - Claim expiry as supply sink to model

10. **Constants Reference Table** -- All constants from this document: name, exact value, source file, line number.

IMPORTANT: Every formula must include the Solidity expression AND constant values substituted, verified against contract source with file and line reference. Use exact variable names from contracts. Follow the style of audit/v1.1-purchase-phase-distribution.md.
  </action>
  <verify>
    <automated>grep -c "COINFLIP_EXTRA_MIN_PERCENT\|COINFLIP_EXTRA_RANGE\|PRICE_COIN_UNIT\|biggestFlipEver\|AFKING_RECYCLE_BONUS_BPS\|COIN_CLAIM_FIRST_DAYS\|COINFLIP_REWARD_MEAN_BPS" audit/v1.1-burnie-coinflip.md</automated>
  </verify>
  <done>Document exists with all 10 sections. Every formula includes Solidity expression with file and line reference. Payout multiplier table covers all 3 tiers. Bounty lifecycle is complete (accumulate, arm, resolve, clear). Recycling formulas include worked example. Constants table has at least 15 entries. BURN-01 is fully addressed.</done>
</task>

</tasks>

<verification>
- All BPS and constant values in document match BurnieCoinflip.sol source
- Every Solidity expression includes file name and line number
- Document follows v1.1 style (purpose header, ToC, numbered sections, code blocks)
- Payout multiplier distribution probabilities sum to 100%
- Bounty system covers all states (unarmed, armed, win-resolution, loss-resolution)
- Recycling bonus worked example arithmetic is correct
</verification>

<success_criteria>
A game theory agent reading only this document can compute the exact coinflip EV for any scenario (fresh deposit, auto-rebuy, afKing mode, bounty-armed, boon-boosted, last-day adjusted) without reading contract source.
</success_criteria>

<output>
After completion, create `.planning/phases/08-burnie-economics/08-01-SUMMARY.md`
</output>
