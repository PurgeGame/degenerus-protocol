---
phase: 109-decimator-system
plan: 02
type: execute
wave: 2
depends_on: [109-01]
files_modified:
  - audit/unit-07/ATTACK-REPORT.md
autonomous: true
requirements: [ATK-01, ATK-02, ATK-03, ATK-04, ATK-05]

must_haves:
  truths:
    - "Every Category B function has a dedicated section with Call Tree, Storage Writes, Cached-Local-vs-Storage Check, and Attack Analysis"
    - "Every Category C function is traced within its parent's call tree with storage writes mapped"
    - "The BAF-critical chain (claimDecimatorJackpot -> _creditDecJackpotClaimCore -> _addClaimableEth -> _processAutoRebuy) has been traced line-by-line"
    - "The decBucketOffsetPacked collision between runDecimatorJackpot and runTerminalDecimatorJackpot has been analyzed"
    - "Every attack angle from ULTIMATE-AUDIT-DESIGN.md has been applied to every Category B function"
  artifacts:
    - path: "audit/unit-07/ATTACK-REPORT.md"
      provides: "Complete adversarial analysis of every state-changing function"
      contains: "Attack Analysis"
---

<objective>
Mad Genius Agent: Full adversarial attack analysis of every state-changing function in DegenerusGameDecimatorModule.

For each Category B function: read line-by-line, build complete recursive call tree, map every storage write in full tree, check cached-local-vs-storage, attack from all 10 angles per ULTIMATE-AUDIT-DESIGN.md.

PRIORITY: The auto-rebuy chain in claimDecimatorJackpot (B4) and the decBucketOffsetPacked collision between B2/B6.

Output: ATTACK-REPORT.md
</objective>

<context>
@.planning/ULTIMATE-AUDIT-DESIGN.md
@.planning/phases/109-decimator-system/109-CONTEXT.md
@.planning/phases/109-decimator-system/109-RESEARCH.md
@audit/unit-07/COVERAGE-CHECKLIST.md
@audit/KNOWN-ISSUES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mad Genius attack analysis on all DecimatorModule functions</name>
  <files>audit/unit-07/ATTACK-REPORT.md</files>

  <action>
YOU ARE THE MAD GENIUS. Read your identity and mandate from ULTIMATE-AUDIT-DESIGN.md, Agent 1.

Read EVERY LINE of DegenerusGameDecimatorModule.sol (930 lines) and DegenerusGamePayoutUtils.sol (92 lines). For each Category B function, produce:
1. Full recursive call tree with line numbers
2. Storage Writes (Full Tree) -- every storage variable written by any function in the call tree
3. Cached-Local-vs-Storage Check -- explicit check for BAF pattern
4. Attack Analysis from all 10 angles with VERDICT per angle

**PRIORITY ORDER (Tier 1 first):**
1. B4: claimDecimatorJackpot -- BAF-critical auto-rebuy chain
2. B1: recordDecBurn -- bucket migration, aggregate accounting
3. B2: runDecimatorJackpot -- VRF selection, shared offset writes
4. B5: recordTerminalDecBurn -- self-call, time multiplier, activity score
5. B6: runTerminalDecimatorJackpot -- shared offset collision, uint96 truncation
6. B7: claimTerminalDecimatorJackpot -- GAMEOVER claim
7. B3: consumeDecClaim -- thin wrapper

**DEDICATED SECTION: decBucketOffsetPacked Collision Analysis**
Both B2 and B6 write to decBucketOffsetPacked[lvl]. Analyze whether they can execute at the same level and whether one overwrites the other's winning subbuckets.

Each finding must have VERDICT: VULNERABLE / INVESTIGATE / SAFE with exact line numbers and proof.
  </action>

  <acceptance_criteria>
    - Every Category B function has a complete section with all 4 required subsections
    - Every call tree is fully expanded to leaf functions
    - Every storage write in every descendant is listed
    - Cached-local-vs-storage check present for every function
    - All 10 attack angles applied to every Category B function
    - BAF-critical chain traced line-by-line with explicit verdict
    - decBucketOffsetPacked collision analyzed with verdict
    - No function dismissed without proof
  </acceptance_criteria>

  <done>Complete attack report covering every state-changing function with full call trees, storage maps, cache checks, and 10-angle attack analysis</done>
</task>

</tasks>
