# 352-01 — v55.0 DELTA AUDIT (the SC1 delta-surface + composition + regression half of AUDIT-01)

**Phase:** 352 — TERMINAL (FULL close) · **Plan:** 352-01 · **Authored:** 2026-06-01
**Requirements:** AUDIT-01 (the delta-audit half; the 3-skill adversarial sweep is 352-02, the findings deliverable is 352-03)
**Audit subject HEAD (FROZEN):** `453f8073` — the last `contracts/*.sol` mutation (the 349.2 IMPL fix). The v55 contract landing is exactly **TWO** commits on top of the baseline: `77c3d9ef` (349.1 box redesign — live-level resolve, drop `_afkingEpoch`/`index`, ticket/lootbox split, `mintBurnie` rename, no-orphan guard) + `453f8073` (349.2 — restore quest-credit + affiliate BURNIE for afking LOOTBOX subs, drop the autoBuy stub). **350 GAS was Outcome A = NO net contract change.**
**Audit baseline:** v54 de-custody HEAD `20ca1f79` (v54.0 CLOSED-as-superseded with NO ship signal — there is intentionally no `MILESTONE_V54_AT_HEAD` string; cite the raw SHA).
**Read-only attestation:** `git diff 453f8073 HEAD -- contracts/` is **EMPTY** (re-verified at the start AND end of this plan — zero `contracts/*.sol` was opened or mutated; the entire delta surface was inspected via `git show 453f8073:…`, `git diff 20ca1f79 453f8073 -- contracts/`, and `grep`). This plan edits ONLY this markdown log.

This mirrors the **v49 Phase 333 §3.A / §3.B / §5** delta-surface + composition + regression structure (and the v48 Phase 328 precedent) so the findings deliverable (352-03) folds this log into its §3/§5. UNLIKE v49 (whose v48→v49 delta was 5 files), the v54→v55 step **IS the AfKing dissolution** — 13 contract files differ — so the composition matrix is load-bearing and the freeze/solvency/OPEN-E re-attestation is mandatory (this redesign touches the RNG-freeze + solvency spine, which is why v55 closes IN-MILESTONE, not deferred to v52).

> **⚠ STAMP-SHAPE CORRECTION (LOAD-BEARING — the as-built COMMITTED reality, re-grepped @ `453f8073`).** The COMMITTED `Sub` stamp is **4-field** `scorePlus1`(uint16) + `amount`(uint96) + `lastAutoBoughtDay`(uint32) + `lastOpenedDay`(uint32) (`DegenerusGameStorage.sol` struct `Sub` `:1867`). **`index` was DROPPED** — the box word is **DAY-keyed** `rngWordByDay[lastAutoBoughtDay]`, NOT lootbox-index-keyed — and the **LEVEL resolves LIVE at open** (`DegenerusGameLootboxModule.sol:767/:800` `currentLevel = level + 1`, `_rollTargetLevel(currentLevel, seed)`). The 349.1 commit `77c3d9ef` **SUPERSEDED** the 348-design-intent 5-field stamp `(index, amount, day, scorePlus1, baseLevelPlus1)` (D-348-07). The freeze proof targets the **SEED**; the EV-cap clamp `lootboxEvBenefitUsedByLevel[player][level+1]` is the **sole** live read at open; the differential oracle (afking-open ≡ human-`openLootBox` at the same live level) proves equivalence. The `_packLootboxPurchase(scorePlus1, adj, baseLevelPlus1)` packing in `DegenerusGameStorage.sol:1375` IS the **HUMAN** lootbox path (`lootboxPurchasePacked`, `:1430`) — NOT the afking Sub stamp; the afking Sub-stamp comment at `:1853` itself states "the LIVE level at open (no stored baseLevelPlus1 roll floor)". This audit cites the 4-field stamp throughout; it does **not** cite a 5-field stamp or a `baseLevelPlus1` Sub field.

---

## 1. The frozen subject + the delta-range provenance (re-derived @ execution time)

| Check | Command | Result |
| --- | --- | --- |
| Frozen subject | `git diff --quiet 453f8073 HEAD -- contracts/` | **EMPTY** (zero contract mutation in this phase) |
| Delta range (contracts only) | `git log --oneline 20ca1f79..453f8073 -- 'contracts/*.sol'` | exactly **2** commits: `453f8073` (fix 349.2) + `77c3d9ef` (feat 349.1) |
| Delta magnitude | `git diff --stat 20ca1f79 453f8073 -- contracts/` | **13 files**, **+1652 / −1165** |
| AfKing.sol presence | `git show 453f8073:contracts/AfKing.sol` | **DELETED** (git show fails) |
| Dangling `AfKing.sol` import | `git grep -n 'AfKing.sol' 453f8073 -- contracts/` | **ZERO** |
| `AF_KING` constant | `git show {baseline,subject}:contracts/ContractAddresses.sol \| grep -c AF_KING` | `20ca1f79`=**1** → `453f8073`=**0** |
| Dangling `AF_KING` ref | `git grep -nE '\bAF_KING\b' 453f8073 -- 'contracts/*.sol'` | **ZERO** |

The 13-file delta set is enumerated below exactly as `git diff --stat 20ca1f79 453f8073 -- contracts/` reports it (re-derived, not trusted from the plan list — reconciled identical).

---

## 2. §3.A Delta-Surface Table — every changed contract surface attested NON-WIDENING @ `453f8073`

Grouped by the v55 work-item family (the canonical design = `PLAN-V55-AFKING-IN-GAME-REDESIGN.md` §10, which supersedes the §0–§3 stamp framing). Columns mirror FINDINGS-v49 §3.A: **Surface (file, Δ)** | **Requirements** | **Re-grepped anchors @ `453f8073`** | **Disposition**.

### Family 1 — AfKing dissolution (ARCH-03)

| Surface (file, Δ) | Requirements | Re-grepped anchors @ `453f8073` | Disposition |
| --- | --- | --- | --- |
| **`contracts/AfKing.sol`** (**−952**, DELETED) | ARCH-03 | `git show 453f8073:contracts/AfKing.sol` **fails** (file absent). The standalone keeper contract — `_poolOf` custody, `subscribe`/`doWork`/`_autoBuy`/`_resolveBuy` slice-builder, `burnForKeeper` — is **gone**; its logic folded into the game-resident `GameAfkingModule` + `DegenerusGameStorage` (Families 2–4). **KILL-SET (grep-ZERO):** no tracked `contracts/*.sol` imports `AfKing.sol`; the `AF_KING` address constant is removed from `ContractAddresses.sol` (1→0) and has **ZERO** repo-wide references. | **NON-WIDENING** (a whole-contract DELETION — strictly removes attack surface; the subscriber custody + slice math relocate into the game-resident model, audited in Families 2–4) |
| **`contracts/DegenerusVault.sol`** (33) | ARCH-03 | The v54 `GAME \|\| AF_KING` receive/credit relaxation **dissolved → GAME-only**: `onlyGame` modifier (`:434`); the subscription surface is now GAME-resident (ARCH-03 doc `:14`), the vault self-subscribes (`player == address(this) == msg.sender`) so the GAME's pass-OR-pay gate sees it; `withdrawAfkingFunding`/`afkingFundingOf` are the new GAME-routed accessors (`:72-75`). No `AF_KING` symbol survives. | **NON-WIDENING** — STRICTLY TIGHTER (GAME-only is a narrowing of the v54 GAME‖AF_KING acceptor; the keeper-address acceptor is removed) |
| **`contracts/StakedDegenerusStonk.sol`** (34) | ARCH-03 | Same GAME-only retarget: `receive() external payable` gated to `msg.sender == GAME` (the afking-funding claim/withdraw send-back; doc `:429-433`); `burnAtGameOver() external onlyGame` (`:526`) is now a **pure local-token burn** (the v54 `afKing.withdraw(afKing.poolOf(this))` prepaid-pool recovery is gone — no game-resident recovery of a prepaid third-party pool; `withdrawAfkingFunding` recovers a player's OWN funding only). ARCH-03 dissolution doc `:379`. | **NON-WIDENING** — STRICTLY TIGHTER (the de-custody recovery path is removed; the receive acceptor narrows GAME‖AF_KING → GAME-only) |
| **`contracts/BurnieCoinflip.sol`** (6) | ARCH-03 | The `creditFlip` creditor allow-list comment + gate retargeted: allowed callers = **GAME** (the delegatecall modules incl. the afking router; `:191`); the `AF_KING` creditor is removed. No `AF_KING` symbol survives. | **NON-WIDENING** — STRICTLY TIGHTER (one creditor address removed from the allow-list) |
| **`contracts/ContractAddresses.sol`** (4) | ARCH-03 | `AF_KING` constant **removed** (1→0); `GAME_AFKING_MODULE` constant added (`:35`, the new delegatecall module address). Freely-modifiable per project policy. | **NON-WIDENING** — an address-constant swap (dead keeper address dropped, new module address added) |

### Family 2 — GameAfkingModule fold (ARCH-02 / PLACE-02 / CONSENT-01/02 + the 349.2 restore)

| Surface (file, Δ) | Requirements | Re-grepped anchors @ `453f8073` | Disposition |
| --- | --- | --- | --- |
| **`contracts/modules/GameAfkingModule.sol`** (**+1048**, NEW) | ARCH-02 · PLACE-02 · CONSENT-01/02 · BOX-01..05 · REVERT-01/02 · EVCAP-01 · (349.2 quest/affiliate) | **NEW delegatecall module**, `contract GameAfkingModule is DegenerusGameMintStreakUtils` (`:74` — which inherits `DegenerusGameStorage`, so all subscriber state is in-context SLOADs). Owns: **subscribe/setters** (`subscribe(…)` `:234` with the **subscribe-time `isOperatorApproved` consent gate**, CONSENT-01, doc `:209`); the **process-pass** (`processSubscriberStage(…)` `:539`); the **open-pass** (`_openAfkingBox(address, Sub storage)` `:888`); the **router** (`mintBurnie() external` `:985`, the single unified bounty — ONE `creditFlip(msg.sender, bountyEarned)` CEI-LAST `:1011-1015`). **349.2 quest/affiliate restore** (off the ETH/`claimablePool` path, BURNIE flip-credit only): `quests.handlePurchase(…)` `:760`, `affiliate.payAffiliate(_ethToBurnie(ethValue, mp), …)` `:806` + `payAffiliate(_ethToBurnie(amount − ethValue, mp), …)` `:816`, accumulated into `flipCredit` then a single `coinflip.creditFlip(player, flipCredit)` `:831` (handlers-before-score; `_ethToBurnie` is the valuation basis only — doc `:800`, `:62`). **No index-keyed box ledger** — the stamp is the 4-field Sub; `_openAfkingBox` seeds from `rngWordByDay[sub.lastAutoBoughtDay]` (`:888-905`). Module bytecode is its own EIP-170 budget, not the Game's (ARCH-02). | **NON-WIDENING** — a NEW module that *relocates* the deleted `AfKing.sol` surface into the game-resident model; every entrypoint maps to a v55 work item (Family 2/4); the 349.2 restore re-credits afking LOOTBOX subs (quest+affiliate) that the 349.1 box-redesign had dropped, as **BURNIE flip-credit only** (ETH/`claimablePool` UNCHANGED → solvency-neutral, GAS-03 re-confirmed) |
| **`contracts/interfaces/IDegenerusGameModules.sol`** (80) | ARCH-02 | The `IGameAfkingModule` interface signatures match the contract verbatim — incl. `processSubscriberStage` (the AdvanceModule dispatches `IGameAfkingModule.processSubscriberStage.selector`, see Family 4) + the subscribe/`mintBurnie`/`afkingFundingOf`/`withdrawAfkingFunding` surface. The standalone-AfKing interface rows are gone. | **NON-WIDENING** — the interface tracks the new module's external ABI; no surface beyond the fold |

### Family 3 — DegenerusGameStorage append (ARCH-01)

| Surface (file, Δ) | Requirements | Re-grepped anchors @ `453f8073` | Disposition |
| --- | --- | --- | --- |
| **`contracts/storage/DegenerusGameStorage.sol`** (**+117**) | ARCH-01 · BOX-02/03/04 · FREEZE-01 | The **layout-safe append** of the subscriber set + cursors + the 4-field stamp + the `afkingFunding` ledger: `mapping(address => Sub) internal _subOf` (`:1902`), `_fundingSourceOf` (`:1904`), `address[] internal _subscribers` (`:1914`), `mapping(address => uint256) internal _subscriberIndex` (`:1918`, swap-pop bookkeeping), `uint16 internal _subCursor` (`:1925`) + `_afkingResetDay` + `subsFullyProcessed` (`:343` — the FREEZE-02 drain gate), `mapping(address => uint256) internal afkingFunding` (`:420`, **rides inside `claimablePool`** — INVARIANT comment `:358` `claimablePool == Σ claimableWinnings[*] + Σ afkingFunding[*]`, every mutation moves both in tandem `:416`), and `mapping(uint32 => uint256) internal rngWordByDay` (`:454`, the DAY-keyed box word — pre-existing map reused). **`struct Sub` `:1867` = 4-field** `dailyQuantity`/`validThroughLevel`/`reinvestPct`/`flags` config + the per-sub **stamp** `uint16 scorePlus1` + `uint96 amount` + the markers `uint32 lastAutoBoughtDay` (the frozen seed `day`, doc `:1888-1891`) + `uint32 lastOpenedDay` (the day-keyed no-double-open marker). **NO `index`, NO `baseLevelPlus1`** in the Sub stamp (the 6 `baseLevelPlus1` hits in this file are all the HUMAN `_packLootboxPurchase`/`lootboxPurchasePacked` path, `:1372-1430`; the Sub-stamp doc `:1853` explicitly disclaims a stored roll floor). | **NON-WIDENING** — a pure append (pre-launch redeploy-fresh, so a storage-layout break is fine; every module shares the base, so the append is in-context for all); the `afkingFunding` aggregate rides inside the existing `claimablePool` (no new reserved aggregate → inherits the v54-correct solvency wiring) |

### Family 4 — Box stamp + process-pass + open-pass (BOX-01..05 / FREEZE-01/02/03 / REVERT-01/02 / EVCAP-01)

| Surface (file, Δ) | Requirements | Re-grepped anchors @ `453f8073` | Disposition |
| --- | --- | --- | --- |
| **`contracts/modules/DegenerusGameAdvanceModule.sol`** (85) | PLACE-02 · FREEZE-02 · REVERT-02 | The **required-path STAGE** (PLACE-01's required-path decision): the pre-RNG cursor-chunked process is inserted **strictly before `rngGate`** (`:294` "The STAGE runs strictly pre-RNG (before rngGate writes the day's word)"), dispatched via `IGameAfkingModule.processSubscriberStage.selector` (`:759`). The **FREEZE-02 no-straddle guard**: a forward-looking reset flips `subsFullyProcessed = false` + `_subCursor = 0` ONCE before that day's STAGE (`:300-308`), then the STAGE breaks-and-returns `mult` while `!subsFullyProcessed` (`:310-322`), setting `subsFullyProcessed = true` only at cursor-end before falling through to `rngGate` (`:325`). The STAGE does **not** straddle the mid-day `requestLootboxRng` (`:1096`) index advance. **REVERT-02 no-valve** doc `:749` ("fail loud — D-348-04 no-valve") — the `claimablePool` debit/release is a checked `uint128 -=` with no try/catch. | **NON-WIDENING** — inserts a pre-RNG STAGE on the new-day advance path; the mid-day same-day path returns earlier (`:287`); the drain/day-advance logic is otherwise the v54 shape (the guard is the new flag, authored exactly as the 348-FREEZE-PROOF FREEZE-02c spec required) |
| **`contracts/modules/DegenerusGameLootboxModule.sol`** (101) | BOX-01/04/05 · EVCAP-01 · FREEZE-03 | The afking open materializes from the 4-field stamp + `rngWordByDay[lastAutoBoughtDay]` with math byte-identical to `openLootBox`: the shared resolve helper computes `currentLevel = level + 1` (**LIVE level** `:767/:800`) → `_rollTargetLevel(currentLevel, seed)` (`:769/:802`) → `_applyEvMultiplierWithCap(player, currentLevel, amount, evMultiplierBps)` (`:772`). **EVCAP-01:** `_applyEvMultiplierWithCap(…)` (`:459`) reads `lootboxEvBenefitUsedByLevel[player][lvl]` (`:473`) and writes `usedBenefit + adjustedPortion` (`:488`) — the RMW at open, hard-clamped (the no-write 100%-EV short-circuit once ≥ cap). **BOX-01:** boons OFF for afking boxes → `amount == spend` (the stamp's `uint96 amount`). **BOX-05:** the human path keeps `lootboxPurchasePacked`/`_unpackLootboxPurchase` (`:530`) unchanged → the two open routes share no mutable-state hazard. | **NON-WIDENING** — the afking open re-uses the existing draw math (the differential oracle TST-01 proves byte-identical traits at the same live level); the only afking-specific addition is the stamp-sourced seed + the shared EV-cap RMW (same map/key as the human buy-time write) |
| **`contracts/modules/DegenerusGameMintModule.sol`** (14) | EVCAP-01 (the buy-time bypass) · (349.2 ticket leg) | The 14-line delta is the afking **ticket-buy** entry doc/path adjustment: the fresh-ETH portion is an explicit `ethValue` parameter (not `msg.value`) so the process STAGE drives a no-value self-call (the Game holds the prepaid `afkingFunding` ETH); the v54 `batchPurchase` framing is reworded to the ticket-mode `purchaseWith` routed from the process STAGE. This is the path that **bypasses the buy-time EV write** for afking boxes (EVCAP-01 double-draw guard — the EV RMW happens once, at open, in the LootboxModule). | **NON-WIDENING** — a comment/parameter-plumbing adjustment on the existing `purchaseWith` path (the afking ticket leg routes the prepaid ETH without a value-bearing self-call); no new emission, no new mutable read |

### Family 5 — Code-size reclaim (ARCH-04)

| Surface (file, Δ) | Requirements | Re-grepped anchors @ `453f8073` | Disposition |
| --- | --- | --- | --- |
| **`contracts/modules/DegenerusGameBingoModule.sol`** (**+110**) | ARCH-04 | The `claimAffiliateDgnrs` relocation (~1.3 KB reclaimed from the Game so it stays < 24,576 mid-flight) lands here. This is a **move** (behavior-identical relocation of an existing function to a different delegatecall module), not a behavior change — the affiliate-claim logic is unchanged, only its bytecode home moved off the Game. | **NON-WIDENING** — a code-size relocation (behavior-identical move; ARCH-04 was MEASURED Complete at 348-SPEC: Game 24,358 B / 218 B headroom, the reclaim clears the EIP-170 margin) |
| **`contracts/DegenerusGame.sol`** (233) | ARCH-04 + thin wiring | The reclaim edits (read-aggregators drop-`view`/→lens, the `claimAffiliateDgnrs` move-out, the thin afking-module wiring) + the AfKing-dissolution wiring churn. The afking subscribe/`mintBurnie`/`afkingFunding` surface is now thin delegatecall wiring to `GameAfkingModule`. Behavior-identical reclaim; the Game runtime code-size stayed < 24,576 (ARCH-04 — the SPEC reclaim plan + `forge build` clean / `npx hardhat compile` EXIT 0 per `REGRESSION-BASELINE-v55.md` §7a). | **NON-WIDENING** — behavior-identical reclaim + thin wiring; the largest single-file line count, but the hunks are the relocation + the dissolution wiring, no new economic surface |

**Per-file delta accounted: 5 (Family 1) + 2 (Family 2) + 1 (Family 3) + 3 (Family 4) + 2 (Family 5) = 13 files** — exactly the `git diff --stat 20ca1f79 453f8073 -- contracts/` set (+1652 / −1165). **Every file carries a NON-WIDENING verdict backed by a concrete grep/diff anchor @ `453f8073`, mapped to its owning v55 work item.**

---

## 3. §3.B Composition Attestation Matrix

### 3.1 No orphan hunks — every `contracts/` delta hunk maps to exactly ONE v55 work item

The +1652 / −1165 delta decomposes into the six v55 work-item families with **ZERO orphan hunks**:

| Work item (family) | Surfaces | Net intent |
| --- | --- | --- |
| **code-size reclaim** (ARCH-04) | `DegenerusGameBingoModule.sol` (+110), `DegenerusGame.sol`, `DegenerusGameMintModule.sol` | `claimAffiliateDgnrs`→BingoModule (~1.3 KB); read-aggregators drop-`view`/→lens; keep Game < 24,576 mid-flight |
| **GameAfkingModule fold** (ARCH-02 / PLACE-02 / CONSENT-01/02) | `GameAfkingModule.sol` (NEW +1048), `IDegenerusGameModules.sol` (80) | the delegatecall module owning subscribe/setters + process-pass + open-pass + the `mintBurnie` router |
| **DegenerusGameStorage append** (ARCH-01) | `DegenerusGameStorage.sol` (+117) | `_subOf`/`_subscribers`/`_subscriberIndex` + cursors + the 4-field Sub stamp + the `afkingFunding` ledger |
| **box stamp + process-pass + open-pass** (BOX-01..05 / FREEZE-01/02/03 / REVERT-01/02 / EVCAP-01) | `DegenerusGameLootboxModule.sol` (101), `DegenerusGameAdvanceModule.sol` (85 — the required-path STAGE), `GameAfkingModule.sol` | boons-OFF amount=spend; pre-RNG 4-field stamp; debit `afkingFunding` + `lastAutoBoughtDay` marker; post-RNG open from `rngWordByDay[day]`, byte-identical to `openLootBox`; `lastOpenedDay` no-double-open |
| **AfKing dissolution** (ARCH-03) | `AfKing.sol` (−952), `DegenerusVault.sol` (33), `StakedDegenerusStonk.sol` (34), `BurnieCoinflip.sol` (6), `ContractAddresses.sol` (4) | `AfKing.sol` DELETED; `AF_KING`-address dissolution; receive()/credit gates retargeted GAME-only |
| **349.2 quest/affiliate restore** | `GameAfkingModule.sol` (`:760`/`:806`/`:816`/`:831`), `DegenerusGameLootboxModule.sol` | per-buy BURNIE flip-credit off the ETH/`claimablePool` path; handlers-before-score; freeze-safe (folded into BOX/GAS) |

**The 350 GAS Outcome-A family is EMPTY** — explicitly asserted: `git log --oneline 20ca1f79..453f8073 -- contracts/` returns exactly **2** commits (`77c3d9ef` 349.1 + `453f8073` 349.2); there is **no 350 commit in the delta range**. The GAS-01/02 wins were CONFIRMED-STRUCTURAL (delivered by the 349/349.1 relocation — the cold box-ledger SSTOREs + `boxPlayers.push` → one warm Sub-stamp write; the cross-contract `afkingSnapshot`/`afkingFundingOf` staticcalls → in-context SLOADs), and **GAS-03 was REJECTED-with-reasoning** per the 350 close (warm SSTORE ~100 gas × (N−1) not ~2.9k; the 349.2-restored affiliate/quest/`creditFlip` are BURNIE flip-credit OFF the ETH+pool path → no new batchable shared additive slot; `prizePoolsPacked` grep-absent; the mixed-chunk `purchaseWith` interleave hazard breaks the accumulate-and-flush identity; ~0.04%-of-chunk saving vs the audit surface on the SOLVENCY-01 spine). **ZERO orphan hunks** — the v55 surface widens NOTHING beyond the six work-item families.

### 3.2 The freeze spine — FREEZE-01/02/03 re-attested against the AS-BUILT COMMITTED 4-field / DAY-keyed / live-level model

Each cross-ref'd to its paper proof (`348-FREEZE-PROOF.md`) **WITH the 349.1 supersession note** (the live-level collapse NARROWS the frozen set to the SEED — strictly safe because level/score resolve LIVE like the human path) AND its empirical re-proof (TST-01, `V55FreezeDeterminism.t.sol`, `351-VERIFICATION.md` truth 1).

- **FREEZE-01 (freeze-completeness) — HELD.** The 4-field stamp `(scorePlus1, amount, lastAutoBoughtDay, lastOpenedDay)` captures all **SEED**-determining state at process; the open re-derives nothing manipulable from mutable per-player state EXCEPT the documented benign EV-cap RMW down-clamp.
  - *Paper proof:* `348-FREEZE-PROOF.md` FREEZE-01 was written under the D-348-07 **5-field** framing `(index, amount, day, scorePlus1, baseLevelPlus1)`. **349.1 SUPERSESSION:** the committed shape DROPPED `index` (DAY-keyed `rngWordByDay[lastAutoBoughtDay]`) and resolves the **level LIVE at open** (`DegenerusGameLootboxModule.sol:767/:800`), so `scorePlus1` stays stamped (the per-sub EV-multiplier input) but `baseLevelPlus1` is **not** stamped — the target-level floor is rolled live like the human path. This **NARROWS** the frozen set to the SEED `keccak256(abi.encode(rngWordByDay[day], player, day, amount))` and is **STRICTLY SAFE** (the live-read of level/score is the same posture as the human `openLootBox`; per D-348-07's own reasoning a one-mint in the reveal→open window moves the EV multiplier a few % and costs ~a full box → −EV, so the live read is non-manipulable-for-profit).
  - *Residual live read:* the EV-cap accumulator `lootboxEvBenefitUsedByLevel[player][level+1]` (RMW at open via `_applyEvMultiplierWithCap`, `:459/:473/:488`) — a shared per-(player,level) running total that **cannot** be per-box-stamped; it is a **benign monotonic down-clamp** (hard ≤10 ETH, the no-write 100%-EV short-circuit once exhausted), no profitable timing. Noted, not findings-grade (carried into `audit/FINDINGS-v55.0.md`).
  - *Empirical:* TST-01 `testDifferentialAfkingVsHumanOpenSameTuple` + `testFuzzDifferentialAfkingVsHumanOpen` (afking-open ≡ human-`openLootBox` byte-identical for the same `(amount, level, rngWord, score)` tuple at the same live level). **VERIFIED.**
- **FREEZE-02 (index-binding, DAY-keyed) — HELD.** The stamp binds to `rngWordByDay[lastAutoBoughtDay]`; the process-pass MUST NOT straddle a mid-day `requestLootboxRng` advance (`AdvanceModule.sol:1096`). As-built: the STAGE runs strictly pre-`rngGate` (`AdvanceModule.sol:294`) behind the `subsFullyProcessed` drain gate (`:300-325`), so no sub stamped after a mid-day advance can attach to a freshly-requested word.
  - *Paper proof:* `348-FREEZE-PROOF.md` FREEZE-02 (the load-bearing D-348-02 obligation; the `subsFullyProcessed` guard SPEC). **Supersession note:** the paper proof reasoned over an `index`-keyed binding; the committed binding is DAY-keyed (`rngWordByDay[day]`), which is the same pre-RNG-separation guarantee expressed against the day word instead of the per-index word — the STAGE-before-`rngGate` ordering + the drain gate is the structural closure either way.
  - *Empirical:* TST-01 `testIndexBindingMidDayAdvanceDoesNotRebind` + `testFuzzIndexBindingAdvanceInvariant` (a modelled mid-day advance does NOT re-bind the box — byte-identical to the no-advance baseline). **VERIFIED.**
- **FREEZE-03 (stamped-day determinism) — HELD.** The seed `keccak256(abi.encode(rngWord, player, day, amount))` uses the **STAMPED** buy-day (`sub.lastAutoBoughtDay`, never open-time `_simulatedDayIndex()`), with no `block.timestamp/number/prevrandao/coinbase/blockhash` in the draw.
  - *Paper proof:* `348-FREEZE-PROOF.md` FREEZE-03 (the `abi.encode` seed at `LB:534`, ZERO `block.*` entropy verified, the boundary-pinned stamped-day closure). As-built: `_openAfkingBox` reads `uint32 day = sub.lastAutoBoughtDay` and seeds from `rngWordByDay[day]` (`GameAfkingModule.sol:888-905`) — exactly the stamped-day source the proof required.
  - *Empirical:* TST-01 `testStampedDayDeterminismOpenAtTwoBlocks` + `testFuzzNoBlockEntropyInTheDraw` (open the SAME stamp at two block contexts — `vm.roll`/`warp`/`prevrandao`/`coinbase` perturbed, the live level HELD fixed — → byte-identical traits). **VERIFIED.**

### 3.3 The discharged REVERT-FREE-CHAIN — REVERT-01/02 re-attested

Cross-ref'd to `PLAN-V55-REVERT-FREE-CHAIN-PROOF.md` §5 (the 4 LOCKED obligations; **obligation 4 — the thin per-sub try/catch valve — was DROPPED at Phase 348 / D-348-04 → no try/catch**) + TST-02 (`V55RevertFreeEvCap.t.sol`).

- **REVERT-01 (revert-free by construction) — HELD.** The process-pass slice preserves `_resolveBuy`'s validation invariants VERBATIM: `ev = cost − claimableUse` + the enum-typed payKind, the 1-wei claimable sentinel, the `LOOTBOX_MIN` transient skip, `quantity ≥ 1` (the migration-fidelity obligation, proof §5 obligation 1 — the sole no-brick guarantor). *Empirical:* TST-02 class A — `testFuzzClassA_FundedSliceNeverReverts` (fuzz amount/claimable-mix across the sentinel/quantity≥1) + `testClassA_ClaimableSentinelAndMinSkipNeverRevert` + `testClassA_FundedBoxOpenNeverReverts`. **VERIFIED.**
- **REVERT-02 (no valve) — HELD.** The no-brick guarantee rests on the 3 residual revert classes (proof §6): **(class A)** revert-free-by-construction (REVERT-01); **(class B)** fail-loud-on-solvency — a `claimablePool` underflow MUST revert `Panic(0x11)` at the checked `uint128 -=`, **never masked** (no try/catch; AdvanceModule doc `:749`); **(class C)** terminal-routing-unblocked — the afking STAGE is on the non-gameover new-day path and cannot block game-over routing. *Empirical:* TST-02 class B — `testClassB_StageDebitSolvencyFailsLoud` + `testClassB_WithdrawSolvencyFailsLoud` + `testFuzzClassB_SolvencyAlwaysFailsLoud` (trace-confirmed the underflow originates in `advanceGame → DegenerusGameAdvanceModule::advanceGame → GameAfkingModule::processSubscriberStage`); class C — `testClassC_GameOverRoutingUnblockedByStage`. **VERIFIED.**

### 3.4 EVCAP-01 — re-attested

The afking open increments `lootboxEvBenefitUsedByLevel[player][level+1]` via `_applyEvMultiplierWithCap` (read+write-at-open, **exactly once per open**, same map/key as MintModule's buy-time write, hard-clamped ≤10 ETH → no revert); the buy-time EV write is **bypassed** for afking boxes (the MintModule afking ticket-buy leg routes `purchaseWith` without the lootbox EV tally → no double-draw); proven equivalent to the v54 per-(sub,level) accumulator. *As-built anchor:* `DegenerusGameLootboxModule.sol:459/473/488` (the RMW), `:772` (the open call site, LIVE `currentLevel`). *Empirical:* TST-03 — `testEvCapExactlyOnceNoDoubleDraw` (slot read before STAGE stamp + after open → exactly one RMW, no buy-time write) + `testEvCapSharedBudgetAcrossAfkingAndHuman` (afking 3 ETH + human 4 ETH draw the SAME `[player][level+1]` key, cumulative 7 ETH = the v54 per-(sub,level) accumulator) + `testEvCapClampsAtTenEthNoRevert` + `testFuzzEvCapMultiOpenClampedCumulative`. **VERIFIED.**

### 3.5 The Phase-343 SOLVENCY-01 master invariant — re-attested HELD NET under the game-resident model

The master inequality `balance + steth.balanceOf(this) >= claimablePool` (inclusive of the afking total) was proven at Phase 343 SPEC for v54 and is carried into v55 as a discharged foundation. **NOTE:** the v54 cross-contract de-custody ledger machinery this proof reasoned over was **largely REPLACED** by the game-resident model — the `afkingFunding` ledger now rides **INSIDE** `claimablePool` (`DegenerusGameStorage.sol:358` INVARIANT `claimablePool == Σ claimableWinnings[*] + Σ afkingFunding[*]`; every `afkingFunding` mutation moves `claimablePool` in tandem, `:416`), so there is **no new aggregate** — the afking funding is a sub-account of `claimablePool` and the master inequality is structurally unchanged (it inherits the v54-correct yield-surplus / gameOver-drain / stETH-stake wiring for free). The empirical guard that an afking debit can never silently breach it is the **class-B fail-loud** (TST-02 §3.3 — a forced `claimablePool` underflow reverts `Panic(0x11)`, never masked). **HELD NET — re-attested.**

### 3.6 The OPEN-E 4-protection — BLOCKING re-attestation (CONSENT-01) + the CONSENT-02 set-mutation carry-over

Per `[[open-e-operator-approval-trust-boundary]]` + REQUIREMENTS CONSENT-01/02, the 4 OPEN-E structural protections are re-attested as a **HARD BLOCKING CONDITION** before closure (a failure routes to the 352-04 closure gate as a blocker → the relevant redesign element is REVERTED before ship; this is **NOT** a 352-01 fix). Cross-ref'd to TST-04 (`V55SetMutationOpenE.t.sol`).

1. **consent-gate-at-subscribe — HOLD.** The subscribe-time `isOperatorApproved` gate carries over (`GameAfkingModule.sol:234` `subscribe(…)`, doc `:209`); a player who never approved cannot be subscribed by a third party. *Empirical:* TST-04 `testOpenEConsentGateUnapprovedReverts`.
2. **default-self byte-identical — HOLD.** `player == address(0)` → `subscriber = msg.sender` (the self path); the VAULT/SDGNRS self-subscribe (`player == address(this) == msg.sender`) rides this path. *Empirical:* TST-04 `testOpenEDefaultSelfByteIdentical` + `testFuzzOpenEDefaultSelfHoldsUnderOrderings`.
3. **no-escalation — HOLD.** Dropping/holding the consent grants no new authority; the funder=`src` accounting + the spend stays bounded to the resolved funding source. *Empirical:* TST-04 `testOpenENoEscalation`.
4. **trust-the-sub temporal bound — HOLD.** The pass-gating `validThroughLevel` (the `Sub.validThroughLevel` snapshot at subscribe, refreshed on crossing; deity sentinel `type(uint24).max`) is the temporal bound; revocation is `subscribe(_, 0)` → in-set tombstone. *Empirical:* TST-04 `testOpenETrustTheSubRevokeDoesNotStop`.

**CONSENT-02 set-mutation carry-over — HOLD.** Evictions preserve "**no cursor advance after swap-pop**" (the H-CANCEL-SWAP-MISS / cancel-tombstone-streak class) + the tombstone-then-reclaim shape: the STAGE's `_removeFromSet`/`_subscribers.pop()` swap-pop does NOT advance `_subCursor` past the relocated tail, and the streak (`scorePlus1`) is preserved (re-derived per buy, byte-identical to an undisplaced control). *Empirical:* TST-04 `testStreakNotCorruptedBySwapPop` + the **NO-ORPHAN guard** trio `testNoOrphanControlInSetSubOpens` / `testNoOrphanRemovedSubGetsNoBox` / `testNoOrphanGuardLeavesPendingBoxSubUntouchedByStage` (a removed sub gets no free box — non-vacuous control proves the box would otherwise have materialized).

**OPEN-E re-attestation outcome: ALL 4 PROTECTIONS HOLD under the fold + the CONSENT-02 set-mutation carry-over HOLDS.** The HARD BLOCKING CONDITION is **SATISFIED** → no element requires reversion on this axis; closure is NOT blocked on OPEN-E. (Final blocking adjudication is the 352-04 closure gate's; 352-01 records the attestation.)

### 3.7 VRF / RNG-freeze — INTACT under the game-resident model (the v45 north-star)

Per `[[v45-vrf-freeze-invariant]]`: re-attested INTACT — **no in-window SLOAD a player can manipulate between rng-request and unlock**. The afking open is **post-RNG** (`_afkingBoxReady` is false until `rngWordByDay[lastAutoBoughtDay] != 0` lands — `GameAfkingModule.sol` readiness gate `:921-934`, so the open path is blocked in the protected window); the stamp froze the SEED at **process (pre-RNG)** behind the `subsFullyProcessed` STAGE-before-`rngGate` gate (§3.2 FREEZE-02). The game-resident fold introduced **NO new in-window mutable read** — the per-subscriber process/open passes are in-context SLOADs of the appended storage (Family 3), not new entropy-window levers; the box draw consumes only frozen/stamped inputs + the benign EV-cap clamp (§3.2 FREEZE-01b). GAS-02's no-STATICCALL trace (TST-06, `351-VERIFICATION.md` truth 6) empirically confirmed **0 foreign re-entrant afking-funding-view StaticCalls** on the STAGE + the open. **Composition verdict: RNG-freeze NON-WIDENING.**

---

*Section 4 (the Regression-Baseline Attestation) is appended by Task 2.*
