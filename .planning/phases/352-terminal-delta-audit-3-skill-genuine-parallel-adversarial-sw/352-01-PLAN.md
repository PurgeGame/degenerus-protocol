---
phase: 352-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/352-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/352-01-DELTA-AUDIT.md
autonomous: true
requirements: [AUDIT-01]

must_haves:
  truths:
    - "Every contract file changed vs the v54 baseline 20ca1f79 is enumerated from git diff 20ca1f79 453f8073 -- contracts/ and attested NON-WIDENING (the 13-file delta: AfKing.sol DELETED, GameAfkingModule.sol NEW +1048, DegenerusGameStorage.sol +117, DegenerusGameLootboxModule.sol, DegenerusGameMintModule.sol, DegenerusGameAdvanceModule.sol, DegenerusGameBingoModule.sol, DegenerusGame.sol, IDegenerusGameModules.sol, DegenerusVault.sol, StakedDegenerusStonk.sol, BurnieCoinflip.sol, ContractAddresses.sol)"
    - "Every contracts/ delta hunk maps to exactly one v55 work item (code-size reclaim / GameAfkingModule fold / DegenerusGameStorage append / box stamp+process-pass+open-pass / AfKing dissolution / 349.2 quest+affiliate restore / 350 GAS Outcome-A = no net contract change); the Composition Attestation Matrix attests ZERO orphan hunks"
    - "The freeze spine is re-attested against the AS-BUILT COMMITTED model: FREEZE-01 (freeze-completeness of the 4-field stamp scorePlus1+amount+lastAutoBoughtDay+lastOpenedDay; LEVEL resolves LIVE at open; the EV-cap clamp is the sole residual live read), FREEZE-02 (DAY-keyed index-binding via rngWordByDay[lastAutoBoughtDay], no mid-day straddle), FREEZE-03 (stamped-day determinism, seed keccak256(rngWord,player,day,amount) with no block.*) — each cross-ref'd to its proof (348-FREEZE-PROOF.md paper proof AND its 349.1 supersession note, re-proven empirically by TST-01 in 351-VERIFICATION.md)"
    - "The discharged REVERT-FREE-CHAIN is re-attested: REVERT-01 (preserve _resolveBuy validation invariants verbatim — revert-free by construction), REVERT-02 (no try/catch valve — the no-brick guarantee rests on revert-free-by-construction + fail-loud-on-solvency class B + terminal-routing-unblocked class C) cross-ref'd to PLAN-V55-REVERT-FREE-CHAIN-PROOF.md §5 (the 4 obligations) + TST-02/TST-03"
    - "EVCAP-01 (EV-cap-at-open RMW exactly once, buy-time write bypassed, clamp = the sole live read at open, equivalent to the v54 per-(sub,level) accumulator) is re-attested cross-ref'd to TST-03"
    - "The Phase-343 SOLVENCY-01 master invariant (balance + steth.balanceOf(this) >= claimablePool inclusive of the afking total) is re-attested HELD net under the game-resident model (the de-custody ledger machinery it proved was largely REPLACED by the game-resident afkingFunding riding inside claimablePool)"
    - "The OPEN-E 4-protection (CONSENT-01/02) is re-attested as a HARD BLOCKING condition: consent-gate-at-subscribe / default-self byte-identical / no-escalation / trust-the-sub temporal bound all HOLD under the redesign; set-mutation (no-cursor-advance-after-swap-pop / cancel-tombstone-streak) carries over — cross-ref'd to TST-04"
    - "VRF/RNG-freeze is attested INTACT under the game-resident model (no in-window SLOAD a player can manipulate between rng-request and unlock; the v45 north-star)"
    - "The v55 foundry regression baseline 603/134/16 is attested NON-WIDENING by failing-NAME-set subset vs the v54 baseline 20ca1f79 (the 134 live reds are a strict subset of the 148-name v54 union -> 0 NEW + 14 narrowed; cite test/REGRESSION-BASELINE-v55.md as authoritative; the D-351-01 rewrite map + the D-351-02 drops attributed BY NAME, not counted as regression)"
    - "git diff 453f8073 HEAD -- contracts/ is empty (zero contract mutation in this phase; subject frozen at 453f8073)"
  artifacts:
    - path: ".planning/phases/352-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/352-01-DELTA-AUDIT.md"
      provides: "The AUDIT-01 delta-audit log — per-surface NON-WIDENING disposition table + Composition Attestation Matrix (freeze spine + REVERT-FREE-CHAIN + EVCAP + SOLVENCY-01 + OPEN-E 4-protection + VRF-freeze) + regression-baseline attestation (603/134/16 subset by NAME)"
      contains: "NON-WIDENING"
  key_links:
    - from: "352-01-DELTA-AUDIT.md"
      to: "git diff 20ca1f79 453f8073 -- contracts/"
      via: "per-file delta enumeration mapped to v55 work items"
      pattern: "NON-WIDENING"
    - from: "352-01-DELTA-AUDIT.md"
      to: "test/REGRESSION-BASELINE-v55.md"
      via: "the NON-WIDENING failing-NAME-set subset attestation (134 in 148)"
      pattern: "603|134|net-zero"
---

<objective>
Author the AUDIT-01 DELTA AUDIT (the SC1 delta-audit half): enumerate every contract surface changed by the v55.0 AfKing-in-Game-redesign subject relative to the v54 baseline (`20ca1f79`), attest each NON-WIDENING, map every `contracts/` delta hunk to exactly one v55 work item, AND re-attest the structural spine the redesign rests on — the freeze spine (FREEZE-01/02/03) against the AS-BUILT COMMITTED 4-field stamp / DAY-keyed seed / live-level model, the discharged REVERT-FREE-CHAIN (REVERT-01/02), EVCAP-01, the Phase-343 SOLVENCY-01 master invariant, the OPEN-E 4-protection (CONSENT-01/02) as a HARD BLOCKING condition, and VRF/RNG-freeze. This is a read-only analysis — it produces ONE markdown log (`352-01-DELTA-AUDIT.md`) and makes ZERO `contracts/*.sol` edits. The audit subject is FROZEN at HEAD `453f8073`.

Purpose: the SC1 delta-audit half of the FULL-CLOSE terminal phase (AUDIT-01). Mirrors the v49 Phase 333 §3.A/§3.B/§5 delta-surface + composition + regression structure (and the v48 Phase 328 precedent) so the findings deliverable (352-03) folds this log into §3/§5. UNLIKE v49 (whose v48->v49 delta was 5 files), the v54->v55 step IS the AfKing dissolution — 13 contract files differ (AfKing.sol deleted, GameAfkingModule.sol added) — so the composition matrix is load-bearing and the freeze/solvency re-attestation is mandatory (this redesign touches the RNG-freeze + solvency spine, which is why v55 closes IN-MILESTONE, not deferred to v52).
Output: `352-01-DELTA-AUDIT.md` — a per-surface NON-WIDENING disposition table + a Composition Attestation Matrix (freeze spine + REVERT-FREE-CHAIN + EVCAP-01 + SOLVENCY-01 + OPEN-E 4-protection + VRF-freeze) + the regression-baseline attestation (603/134/16 subset by NAME, cite the ledger).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@audit/FINDINGS-v49.0.md
@.planning/PLAN-V55-AFKING-IN-GAME-REDESIGN.md
@.planning/PLAN-V55-REVERT-FREE-CHAIN-PROOF.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-FREEZE-PROOF.md
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-VERIFICATION.md
@test/REGRESSION-BASELINE-v55.md

<interfaces>
<!-- The frozen audit subject + the v55 blast radius. Executor reads these directly via git — no exploration. -->
- Audit subject HEAD: `453f8073` (the last contracts/ mutation = 349.2). The v55 contract landing = exactly TWO commits on top of the baseline: `77c3d9ef` (349.1 box redesign — live-level resolve, drop _afkingEpoch/index, ticket/lootbox split, mintBurnie rename, no-orphan guard) + `453f8073` (349.2 — restore quest-credit + affiliate BURNIE for afking LOOTBOX subs, drop autoBuy stub). 350 GAS was Outcome A = NO net contract change. The subject is FROZEN at `453f8073` — `git diff 453f8073 HEAD -- contracts/` is EMPTY. Do NOT plan any further contract landing.
- Audit baseline: v54 de-custody HEAD `20ca1f79` (v54.0 was CLOSED-as-superseded with NO ship signal; `20ca1f79` is the v55 baseline). NOTE: there is intentionally no MILESTONE_V54_AT_HEAD signal — cite the raw SHA `20ca1f79`, never a v54 signal string.
- The delta-surface command: `git diff 20ca1f79 453f8073 -- contracts/`. The 13 changed contract files (VERIFIED 2026-06-01, +1652 / -1165): AfKing.sol (DELETED, -952), modules/GameAfkingModule.sol (NEW, +1048), storage/DegenerusGameStorage.sol (+117), modules/DegenerusGameLootboxModule.sol (101), modules/DegenerusGameMintModule.sol (14), modules/DegenerusGameAdvanceModule.sol (85), modules/DegenerusGameBingoModule.sol (+110), DegenerusGame.sol (233), interfaces/IDegenerusGameModules.sol (80), DegenerusVault.sol (33), StakedDegenerusStonk.sol (34), BurnieCoinflip.sol (6), ContractAddresses.sol (4). ENUMERATE the actual per-file delta at execution time via `git diff --stat 20ca1f79 453f8073 -- contracts/` (do not trust this list blindly — re-run and reconcile).
- The two mainnet contract commits in the delta range (`git log --oneline 20ca1f79..453f8073 -- 'contracts/*.sol'`): `77c3d9ef` (feat(349.1): AfKing box redesign) + `453f8073` (fix(349.2): restore quest-credit + affiliate for afking LOOTBOX subs).
- The v55 work-item families (map every hunk to exactly ONE; canonical design = .planning/PLAN-V55-AFKING-IN-GAME-REDESIGN.md §10, which SUPERSEDES the §0–§3 stamp framing):
  | Work item | Surface(s) | Requirements |
  |-----------|------------|--------------|
  | code-size reclaim (Game stays < 24,576 mid-flight; claimAffiliateDgnrs to BingoModule ~1.3KB; read-aggregators drop-view/to-lens) | DegenerusGameBingoModule.sol (+110), DegenerusGame.sol, DegenerusGameMintModule.sol | ARCH-04 |
  | GameAfkingModule fold (delegatecall, inherits DegenerusGameStorage; owns subscribe/setters + process-pass + open-pass + router) | GameAfkingModule.sol (NEW +1048), IDegenerusGameModules.sol | ARCH-02 / PLACE-02 / CONSENT-01/02 |
  | DegenerusGameStorage append (_subOf/_subscribers/_subscriberIndex + cursors + the 4-field per-sub Sub box-stamp + the v54 afkingFunding ledger) | DegenerusGameStorage.sol (+117) | ARCH-01 |
  | box stamp + process-pass + open-pass (boons-OFF amount=spend; pre-RNG stamp; debit afkingFunding + lastAutoBoughtDay marker; post-RNG open from rngWordByDay[day], byte-identical to openLootBox; lastOpenedDay no-double-open) | DegenerusGameLootboxModule.sol (101), DegenerusGameAdvanceModule.sol (85 — the required-path STAGE), GameAfkingModule.sol | BOX-01..05 / FREEZE-01/02/03 / REVERT-01/02 / EVCAP-01 |
  | AfKing dissolution (AfKing.sol DELETED; AF_KING-address dissolution; receive()/credit gates retargeted GAME-only on Vault + sDGNRS) | AfKing.sol (-952), DegenerusVault.sol (33), StakedDegenerusStonk.sol (34), BurnieCoinflip.sol (6), ContractAddresses.sol (4) | ARCH-03 |
  | 349.2 quest-credit + affiliate restore (per-buy BURNIE flip-credit off the ETH/claimablePool path; handlers-before-score; freeze-safe) | GameAfkingModule.sol (the :760/:806/:816/:831 anchors from MEMORY), DegenerusGameLootboxModule.sol | (folded into BOX/GAS) |
  | 350 GAS Outcome A | (NONE — no net contract change; confirm git diff shows no 350 commit in 20ca1f79..453f8073) | GAS-01/02/03 |
- ⚠ STAMP-SHAPE CORRECTION (LOAD-BEARING — the as-built reality, per STATE.md + 351-VERIFICATION.md "Corrected Freeze Target Compliance"): the COMMITTED Sub stamp at 453f8073 is **4-field** scorePlus1(16) + amount(96) + markers lastAutoBoughtDay(32) + lastOpenedDay(32) (DegenerusGameStorage.sol struct Sub ~:1867-1896). index was DROPPED — the box word is DAY-keyed rngWordByDay[lastAutoBoughtDay], NOT lootbox-index-keyed — and the **LEVEL resolves LIVE at open** (349.1 SUPERSEDED the 348-design-intent 5-field stamp (index, amount, day, scorePlus1, baseLevelPlus1)). The freeze proof targets the SEED; the EV-cap clamp lootboxEvBenefitUsedByLevel[player][level+1] is the sole live read at open; the differential oracle (afking-open == human-openLootBox at the same live level) proves equivalence. DO NOT cite a 5-field stamp or a baseLevelPlus1 Sub field. The _packLootboxPurchase(scorePlus1,adj,baseLevelPlus1) packing in DegenerusGameStorage IS the HUMAN lootbox path — NOT the afking Sub stamp; do not conflate them.
- The freeze spine to re-attest (each cross-ref'd to its proof; re-attest against the AS-BUILT COMMITTED model, NOTING the 349.1 supersession where the paper proof's framing diverged):
  - FREEZE-01 (freeze-completeness): the 4-field stamp captures all SEED-determining state at process; the open re-derives nothing manipulable from mutable per-player state EXCEPT the documented benign EV-cap RMW down-clamp. Paper proof: 348-FREEZE-PROOF.md FREEZE-01 (written under the D-348-07 5-field framing — note it was superseded by 349.1's live-level/4-field commit, which NARROWS the frozen set to the SEED and is STRICTLY SAFE because level/score resolve LIVE like the human path). Empirical: TST-01 (V55FreezeDeterminism.t.sol, 351-VERIFICATION.md truth 1).
  - FREEZE-02 (index-binding, DAY-keyed): the stamp binds to rngWordByDay[lastAutoBoughtDay]; the process-pass MUST NOT straddle a mid-day requestLootboxRng advance (AdvanceModule.sol:1016). Paper proof: 348-FREEZE-PROOF.md FREEZE-02. Empirical: TST-01 testIndexBindingMidDayAdvanceDoesNotRebind.
  - FREEZE-03 (stamped-day determinism): the seed keccak256(rngWord, player, day, amount) uses the STAMPED buy-day (never open-time _simulatedDayIndex()), no block.timestamp/number/prevrandao/coinbase/blockhash in the draw. Paper proof: 348-FREEZE-PROOF.md FREEZE-03. Empirical: TST-01 testStampedDayDeterminismOpenAtTwoBlocks + testFuzzNoBlockEntropyInTheDraw + the differential oracle testDifferentialAfkingVsHumanOpenSameTuple.
- The discharged REVERT-FREE-CHAIN to re-attest (proof .planning/PLAN-V55-REVERT-FREE-CHAIN-PROOF.md §5 — the 4 LOCKED obligations; obligation 4 [the thin per-sub skip valve] was DROPPED at Phase 348 / D-348-04 -> no try/catch):
  - REVERT-01 (revert-free by construction): the process-pass slice preserves _resolveBuy's validation invariants VERBATIM — ev = cost − claimableUse + enum payKind, the 1-wei claimable sentinel, the LOOTBOX_MIN transient skip, quantity >= 1. Empirical: TST-02 class A (V55RevertFreeEvCap.t.sol testFuzzClassA_FundedSliceNeverReverts).
  - REVERT-02 (no valve): the no-brick guarantee rests on REVERT-01 (revert-free-by-construction, the sole no-brick guarantor) + fail-loud-on-solvency (class B — a claimablePool underflow MUST revert Panic(0x11), never masked) + terminal-routing-unblocked (class C — the afking STAGE cannot block game-over routing). Empirical: TST-02 class B (testClassB_StageDebitSolvencyFailsLoud + testClassB_WithdrawSolvencyFailsLoud) + class C (testClassC_GameOverRoutingUnblockedByStage).
- EVCAP-01 to re-attest: the afking open increments lootboxEvBenefitUsedByLevel[player][level+1] via _applyEvMultiplierWithCap (read+write-at-open, exactly once per open, same map/key as MintModule's buy-time write, hard-clamped <=10 ETH -> no revert); the buy-time EV write is bypassed for afking boxes (no double-draw); proven equivalent to the v54 per-(sub,level) accumulator. Empirical: TST-03 (testEvCapExactlyOnceNoDoubleDraw + testEvCapSharedBudgetAcrossAfkingAndHuman + testEvCapClampsAtTenEthNoRevert).
- The Phase-343 SOLVENCY-01 master invariant to re-attest: balance + steth.balanceOf(this) >= claimablePool inclusive of the afking total (proven at Phase 343 SPEC for v54; carried into v55 as a discharged foundation). NOTE: the v54 cross-contract de-custody ledger machinery this proved was largely REPLACED by the game-resident model (the game-side afkingFunding rides INSIDE claimablePool, no new aggregate). Re-attest the NET invariant holds under the game-resident model — the afking funding is a sub-account of claimablePool, the master inequality is unchanged, and the class-B fail-loud (TST-02) is the empirical guard that an afking debit can never silently breach it.
- The OPEN-E 4-protection BLOCKING re-attestation (CONSENT-01/02): re-attest the 4 OPEN-E structural protections HOLD under the redesign as a HARD BLOCKING condition before closure (per [[open-e-operator-approval-trust-boundary]]): (1) consent-gate-at-subscribe (the subscribe-time isOperatorApproved gate carries over), (2) default-self byte-identical (player==address(0) -> subscriber=msg.sender), (3) no-escalation, (4) trust-the-sub temporal bound (the pass-gating validThroughLevel / funder=src accounting). PLUS the CONSENT-02 set-mutation carry-over: evictions preserve "no cursor advance after swap-pop" (the H-CANCEL-SWAP-MISS / cancel-tombstone-streak class) + the tombstone-then-reclaim shape. If any fails, the relevant redesign element is REVERTED before the milestone ships (this is a 352-04-gate-routed blocker, NOT a 352-01 fix). Empirical: TST-04 (V55SetMutationOpenE.t.sol — the OPEN-E 4 tests + the NO-ORPHAN guard + the streak-preserved swap-pop).
- VRF/RNG-freeze (the v45 north-star, [[v45-vrf-freeze-invariant]]): re-attest INTACT under the game-resident model — no in-window SLOAD a player can manipulate between rng-request and unlock. The afking open is post-RNG (_afkingBoxReady false until rngWordByDay[day] lands -> blocked in the protected window); the stamp froze the SEED at process (pre-RNG). Confirm the game-resident fold introduced NO new in-window mutable read.
- The regression ledger (NON-WIDENING gate — AUTHORITATIVE): test/REGRESSION-BASELINE-v55.md = 603 passed / 134 failed / 16 skipped (whole-tree forge test). NON-WIDENING = a strict failing-NAME-set SUBSET vs the v54 baseline 20ca1f79 (NOT a count): the 134 live reds are a STRICT SUBSET of the 148-name v54 baseline union (live minus union == empty; intersection = 134; 14-name NARROWING = v54 reds FIXED by v55) -> 0 NEW regression. The v54 baseline was established EMPIRICALLY (checkout 20ca1f79 + full forge test, 11 uncompilable-at-v54 files sidelined). The D-351-01 rewrite map (11 uncompilable files -> their v55 adapted successors, BY NAME + commit) + the D-351-02 drops (D1 KeeperBatchAffiliateDeltaAudit, D2 RedemptionStethFallback::test_POOL04, D3 KeeperNonBrick 6 tests, D4 RouterWorstCaseGas 7 tests, D5 KeeperLeversAndPacking grep gates) are attributed via the ledger, NOT counted as regression (every drop is from the 11 uncompilable-at-v54 files -> zero compilable v54 reds lost). Cite the ledger; do NOT re-run forge or re-derive (it is authoritative).
</interfaces>
</context>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan introduces NO new code or attack surface — it is a read-only delta audit producing one markdown log. The audit SUBJECT's security is what is being audited, NOT modified: the v55.0 contract surfaces (the AfKing-in-Game fold + box redesign + 349.2 restore), already frozen at 453f8073. This phase makes ZERO contracts/ edits (git diff 453f8073 HEAD -- contracts/ empty). The adversarial probing itself is 352-02; this plan is the structural delta + spine re-attestation. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-352-01-RO | Tampering | this plan's deliverable | accept | Read-only analysis; the executor never edits contracts/*.sol — verified by git diff 453f8073 HEAD -- contracts/ empty. No new attack surface. |
| T-352-01-WIDEN | Elevation | v55 delta surfaces vs v54 (the AfKing dissolution) | mitigate | The audit itself IS the mitigation: every changed surface (13 files) is attested NON-WIDENING with grep/diff evidence before closure; each delta hunk maps to exactly one v55 work item; regression NON-WIDENING is a strict failing-NAME-set SUBSET vs the v54 union (134 in 148, not a count). |
| T-352-01-FREEZE | Elevation | the relocated box-stamp freeze (the RNG-freeze spine touch) | mitigate | The freeze spine (FREEZE-01/02/03) is re-attested against the AS-BUILT COMMITTED 4-field/DAY-keyed/live-level model, cross-ref'd to 348-FREEZE-PROOF.md (with the 349.1 supersession note) + TST-01. The live-level collapse NARROWS the frozen set to the SEED and is STRICTLY SAFE (level resolves live like the human path). |
| T-352-01-SOLVENCY | Tampering | the game-resident afkingFunding (the solvency-spine touch) | mitigate | SOLVENCY-01 is re-attested HELD net under the game-resident model (afkingFunding rides inside claimablePool, no new aggregate); the class-B fail-loud (TST-02) is the empirical guard against a silent breach. |
| T-352-01-OPENE | Elevation | CONSENT-01/02 OPEN-E 4-protection under the fold | mitigate | The OPEN-E 4-protection re-attestation is a HARD BLOCKING condition (a failure routes to the 352-04 closure gate as a blocker -> the relevant element is REVERTED before ship). Not patched here — surfaced. Cross-ref'd to TST-04. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate the 13-file delta surface, attest each NON-WIDENING, build the Composition Attestation Matrix (freeze spine + REVERT-FREE-CHAIN + EVCAP + SOLVENCY-01 + OPEN-E + VRF-freeze)</name>
  <read_first>
    - audit/FINDINGS-v49.0.md (the §3.A Delta-Surface Table format + the §3.B Composition Attestation Matrix to mirror — read the §3 region around line 156)
    - .planning/PLAN-V55-AFKING-IN-GAME-REDESIGN.md §10 (the canonical design that SUPERSEDES §0–§3; the AfKing-in-Game fold + box redesign + the surfaces — the authoritative work-item map)
    - .planning/PLAN-V55-REVERT-FREE-CHAIN-PROOF.md §5 (the 4 LOCKED obligations — REVERT-01/02 source; obligation 4 DROPPED at 348/D-348-04)
    - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-FREEZE-PROOF.md (the FREEZE-01/02/03 paper proofs — NOTE these were written under the D-348-07 5-field framing; re-attest against the AS-BUILT COMMITTED 4-field/live-level model per the 349.1 supersession, citing the paper proof as the design-intent foundation that 349.1 NARROWED to the SEED)
    - .planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-VERIFICATION.md (the empirical proofs TST-01..04 + the "Corrected Freeze Target Compliance" + the 4-field-stamp confirmation — the as-built attestation source)
    - .planning/REQUIREMENTS.md (the 29 v55.0 REQ rows — each delta hunk maps to a REQ; ARCH/BOX/FREEZE/REVERT/EVCAP/CONSENT/PLACE/GAS/TST/AUDIT)
    - the frozen subject files via git show 453f8073:contracts/<file>, git diff 20ca1f79 453f8073 -- contracts/<file>, and git diff --stat 20ca1f79 453f8073 -- contracts/ for the actual per-file delta
  </read_first>
  <files>.planning/phases/352-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/352-01-DELTA-AUDIT.md</files>
  <action>
    Run git diff --stat 20ca1f79 453f8073 -- contracts/ to ENUMERATE the actual changed-file set (reconcile against the interfaces 13-file list — re-derive, do not trust blindly), then git diff 20ca1f79 453f8073 -- contracts/ for the full delta. Build a per-surface NON-WIDENING disposition table grouped by the v55 work-item families (the interfaces table). For each changed file, attest NON-WIDENING with concrete grep/diff evidence drawn from the frozen subject 453f8073 (read via git show / git diff / grep):
    - contracts/AfKing.sol (AfKing dissolution, ARCH-03): confirm the file is DELETED (-952; git show 453f8073:contracts/AfKing.sol fails / the file absent); confirm no dangling import of AfKing.sol remains in any tracked contract (grep ZERO).
    - contracts/modules/GameAfkingModule.sol (the fold, ARCH-02 + PLACE-02 + CONSENT-01/02 + the process/open passes): confirm it is NEW (+1048), inherits DegenerusGameStorage (delegatecall), and owns subscribe/setters + the process-pass (processSubscriberStage) + the open-pass + the router (mintBurnie); confirm the subscribe-time isOperatorApproved consent gate present (CONSENT-01) + the 349.2 per-buy quest/affiliate BURNIE flip-credit anchors (the affiliate/quest handlers off the ETH/claimablePool path); confirm no index-keyed box ledger (the stamp is the 4-field Sub).
    - contracts/storage/DegenerusGameStorage.sol (the append, ARCH-01): confirm the _subOf/_subscribers/_subscriberIndex + cursors + the 4-field struct Sub (scorePlus1 uint16 / amount uint96 / lastAutoBoughtDay uint32 / lastOpenedDay uint32, ~:1867-1896) + the afkingFunding ledger appended (layout-safe append); confirm the Sub stamp has NO index and NO baseLevelPlus1 field (the 349.1 supersession — DAY-keyed seed, live-level open); attest the append is layout-safe (every module shares the base; pre-launch redeploy-fresh so a storage break is fine).
    - contracts/modules/DegenerusGameLootboxModule.sol (box redesign + open-pass, BOX-01..05 + EVCAP-01): confirm the afking open (resolveAfkingBox/_openAfkingBox) materializes from the 4-field stamp + rngWordByDay[lastAutoBoughtDay] with math byte-identical to openLootBox (the differential oracle proves this — TST-01); confirm boons OFF for afking boxes (amount = spend, BOX-01); confirm the EV-cap RMW _applyEvMultiplierWithCap at open (EVCAP-01); confirm the lastOpenedDay no-double-open marker monotonic (BOX-04).
    - contracts/modules/DegenerusGameAdvanceModule.sol (the required-path STAGE, PLACE-02 / FREEZE-02): confirm the pre-RNG cursor-chunked process STAGE inserted before the rngGate (PLACE-01's required-path decision); confirm the STAGE does NOT straddle the mid-day requestLootboxRng index advance (:1016, FREEZE-02).
    - contracts/modules/DegenerusGameBingoModule.sol (code-size reclaim, ARCH-04): confirm the claimAffiliateDgnrs relocation (~1.3KB reclaimed from the Game so it stays < 24,576) — attest it is a move (behavior-identical), not a behavior change.
    - contracts/DegenerusGame.sol (reclaim + thin wiring): confirm the reclaim edits (read-aggregators drop-view/to-lens) are behavior-identical; confirm the Game runtime code-size stayed < 24,576 (ARCH-04 — cite the SPEC reclaim plan / forge build clean).
    - contracts/interfaces/IDegenerusGameModules.sol: confirm the GameAfkingModule interface signatures match the contract verbatim.
    - contracts/DegenerusVault.sol + contracts/StakedDegenerusStonk.sol (AfKing dissolution side-effects, ARCH-03): confirm the receive()/credit gates retargeted GAME-only (the v54 GAME||AF_KING relaxation dissolved); attest STRICTLY TIGHTER (GAME-only is a narrowing, not a widening).
    - contracts/BurnieCoinflip.sol + contracts/ContractAddresses.sol: confirm the AF_KING-address dissolution (the small creditFlip-creditor / address-constant edits); attest NON-WIDENING.
    Mirror the FINDINGS-v49 §3.A table columns: Surface (file) | Requirements | Re-grepped anchors @ 453f8073 | Disposition (NON-WIDENING). Then build the Composition Attestation Matrix (mirror v49 §3.B AND extend with the v55-specific re-attestations):
    (1) every contracts/ delta hunk maps to exactly ONE v55 work item (no orphan hunks) — explicitly assert the 350 GAS Outcome-A family is EMPTY (no 350 commit in 20ca1f79..453f8073; the GAS-01/02 wins were structural to the 349/349.1 relocation, GAS-03 REJECTED-with-reasoning per the 350 close).
    (2) the freeze spine (FREEZE-01/02/03) re-attested against the AS-BUILT COMMITTED 4-field/DAY-keyed/live-level model, each cross-ref'd to its paper proof (348-FREEZE-PROOF.md) WITH the 349.1 supersession note (the live-level collapse NARROWS the frozen set to the SEED — strictly safe) AND its empirical re-proof (TST-01).
    (3) the discharged REVERT-FREE-CHAIN (REVERT-01 verbatim _resolveBuy invariants + REVERT-02 no-valve [the 3 residual revert classes: revert-free-by-construction / fail-loud-on-solvency class B / terminal-routing-unblocked class C]), cross-ref'd to PLAN-V55-REVERT-FREE-CHAIN-PROOF.md §5 + TST-02.
    (4) EVCAP-01 (RMW exactly-once at open, buy-time write bypassed, clamp = sole live read, equivalent to v54), cross-ref'd to TST-03.
    (5) the Phase-343 SOLVENCY-01 master invariant re-attested HELD NET under the game-resident model (afkingFunding rides inside claimablePool, no new aggregate; class-B fail-loud is the empirical guard).
    (6) the OPEN-E 4-protection BLOCKING re-attestation (CONSENT-01) — the 4 protections each attested HOLD under the fold + the CONSENT-02 set-mutation carry-over (no-cursor-advance-after-swap-pop / cancel-tombstone-streak) — recorded as a HARD blocking condition (a failure surfaces to the 352-04 closure gate, the element REVERTED, NOT patched here); cross-ref'd to TST-04.
    (7) VRF/RNG-freeze INTACT under the game-resident model (the v45 north-star) — no in-window SLOAD a player can manipulate between rng-request and unlock (the afking open is post-RNG, blocked until rngWordByDay[day] lands; the stamp froze the SEED pre-RNG).
    DO NOT edit any contracts/*.sol file — read-only via git show / git diff / grep.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && git diff --quiet 453f8073 HEAD -- contracts/ && grep -q NON-WIDENING .planning/phases/352-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/352-01-DELTA-AUDIT.md && grep -q OPEN-E .planning/phases/352-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/352-01-DELTA-AUDIT.md && grep -q SOLVENCY-01 .planning/phases/352-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/352-01-DELTA-AUDIT.md && grep -q FREEZE-01 .planning/phases/352-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/352-01-DELTA-AUDIT.md && grep -q 453f8073 .planning/phases/352-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/352-01-DELTA-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - 352-01-DELTA-AUDIT.md enumerates all changed contract files (re-derived from git diff --stat 20ca1f79 453f8073 -- contracts/), each carrying a NON-WIDENING verdict backed by a concrete grep/diff anchor @ 453f8073, mapped to its owning v55 work item(s).
    - Each delta hunk maps to exactly one v55 work item (the composition matrix attests no orphan hunks); the 350 GAS Outcome-A family is explicitly asserted EMPTY (no 350 commit in the delta range).
    - AfKing.sol is confirmed DELETED with zero dangling AfKing.sol imports; GameAfkingModule.sol confirmed NEW inheriting DegenerusGameStorage with the process/open passes + the subscribe-time consent gate + the 349.2 quest/affiliate restore.
    - The COMMITTED 4-field Sub stamp (scorePlus1/amount/lastAutoBoughtDay/lastOpenedDay) is confirmed in DegenerusGameStorage with NO index and NO baseLevelPlus1 field (the 349.1 supersession honored — DO NOT cite a 5-field stamp).
    - The freeze spine (FREEZE-01/02/03) is re-attested against the AS-BUILT COMMITTED 4-field/DAY-keyed/live-level model, each cross-ref'd to 348-FREEZE-PROOF.md (with the 349.1 supersession note) + TST-01.
    - The discharged REVERT-FREE-CHAIN (REVERT-01/02), EVCAP-01, the Phase-343 SOLVENCY-01 master invariant, and VRF/RNG-freeze are each re-attested intact, cross-ref'd to their proofs (PLAN-V55-REVERT-FREE-CHAIN-PROOF.md §5 / TST-02 / TST-03).
    - The OPEN-E 4-protection re-attestation (CONSENT-01) + the CONSENT-02 set-mutation carry-over are recorded as a HARD BLOCKING condition (each protection attested HOLD under the fold, cross-ref'd to TST-04).
    - git diff 453f8073 HEAD -- contracts/ is empty (zero contract mutation).
  </acceptance_criteria>
  <done>All changed surfaces (the 13-file delta) attested NON-WIDENING with grep/diff evidence; the delta-surface table + the Composition Attestation Matrix (the freeze spine against the as-built 4-field/DAY-keyed/live-level model + REVERT-FREE-CHAIN + EVCAP-01 + SOLVENCY-01 + OPEN-E 4-protection + VRF-freeze) are written to 352-01-DELTA-AUDIT.md; contracts/ byte-identical to 453f8073.</done>
</task>

<task type="auto">
  <name>Task 2: Regression-baseline attestation (603/134/16 NON-WIDENING by NAME subset, 134 in 148) + the D-351-01 rewrite map + the D-351-02 drops attributed</name>
  <read_first>
    - test/REGRESSION-BASELINE-v55.md (the 351-09 ledger: the 603/134/16 v55 run, the v54 baseline 461/148/16 established EMPIRICALLY at 20ca1f79, the live minus union == empty subset proof, the 14-name NARROWING, the D-351-01 rewrite map [11 uncompilable files -> their v55 successors BY NAME + commit], the D-351-02 drops [D1..D5 BY NAME + reason], the FC1-FC6 guards — the AUTHORITATIVE source; cite, do NOT re-derive)
    - audit/FINDINGS-v49.0.md §5 (the LEAN Regression Appendix format to mirror — read the §5 region around line 377) + §5b (the failing-NAME-set equality / Pitfall-3 guard — adapt to the v55 SUBSET form: 134 in 148, not an equality)
    - .planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-VERIFICATION.md (the "NON-WIDENING Empirical Derivation" section — the empirical v54-baseline method + the strongest-non-widening-position note)
    - .planning/phases/352-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/352-01-DELTA-AUDIT.md (the file being appended to)
  </read_first>
  <files>.planning/phases/352-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/352-01-DELTA-AUDIT.md</files>
  <action>
    Append a Regression-Baseline Attestation section to 352-01-DELTA-AUDIT.md mirroring v49 §5. Attest the v55 foundry baseline 603/134/16 NON-WIDENING BY NAME as a strict SUBSET: cite test/REGRESSION-BASELINE-v55.md as authoritative — the 134 live reds are a STRICT SUBSET of the 148-name v54 baseline union (live minus union == empty; intersection = 134; 14-name NARROWING = v54 reds FIXED by v55), so there are 0 NEW regressions. State the binding gate explicitly as a SUBSET relation (134 in 148), NOT an equality and NOT a count delta — adapt the v49 §5b Pitfall-3 guard: do NOT quote "134 failures, down from 148" as if a count delta; the binding gate is "live failing set minus v54 148-name union == empty". Record that the v54 baseline was established EMPIRICALLY (checkout 20ca1f79 + full forge test, 11 uncompilable-at-v54 files sidelined) — the plan's original "byte-identical contract tree" premise was wrong (the v54->v55 step IS the AfKing dissolution) and the empirical method yields the STRONGEST non-widening position (those 11 files contributed ZERO compilable v54 reds -> no baseline red could be lost by the wholesale rewrite or the D-351-02 drops).

    Then attribute the test-surface churn via the ledger (NOT counted as regression): the D-351-01 rewrite map (the ~13 stale AfKing/keeper test files + DeployProtocol.sol rewritten to the game-resident GameAfkingModule path -> their v55 adapted successors, BY NAME + commit) + the 4 dedicated v55 proof files authored (V55FreezeDeterminism / V55RevertFreeEvCap / V55SetMutationOpenE / V55AfkingGasMarginal); the D-351-02 drops BY NAME + reason (D1 KeeperBatchAffiliateDeltaAudit whole-file [removed batchPurchase, no successor], D2 RedemptionStethFallback::test_POOL04_BurnAtGameOverRecoversPool [v54 de-custody machinery removed], D3 KeeperNonBrick 6 tests [batchPurchase try/catch isolation, no successor], D4 RouterWorstCaseGas 7 tests [reframed into the STAGE/open tests], D5 KeeperLeversAndPacking grep gates [batchPurchase-asserted-ABSENT]) — every D-351-02 drop is from the 11 uncompilable-at-v54 files -> zero compilable v54 reds lost. State the SWEEP NON-WIDENING claim explicitly: every git diff 20ca1f79 453f8073 -- contracts/ test/ hunk is attributable to a known v55-scope commit (the 349.1 box-redesign 77c3d9ef + the 349.2 restore 453f8073 + the AGENT-committed 351 TST test work — the rewrites, the 4 proof files, the drops, and test/REGRESSION-BASELINE-v55.md). Note the Hardhat sanity: npx hardhat compile EXIT 0, DegenerusGame.test.js byte-identical v54->v55 (Foundry is the focused regression scope; Hardhat is the sanity check). Read-only — no contracts/ or test/ edits.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -q 603 .planning/phases/352-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/352-01-DELTA-AUDIT.md && grep -q 134 .planning/phases/352-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/352-01-DELTA-AUDIT.md && grep -q 148 .planning/phases/352-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/352-01-DELTA-AUDIT.md && grep -q D-351-02 .planning/phases/352-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/352-01-DELTA-AUDIT.md && git diff --quiet 453f8073 HEAD -- contracts/</automated>
  </verify>
  <acceptance_criteria>
    - The Regression-Baseline Attestation records 603/134/16 NON-WIDENING BY NAME with the 134 live reds as a strict SUBSET of the v54 148-name union (a SUBSET relation 134 in 148, not a count delta) cited from test/REGRESSION-BASELINE-v55.md.
    - The v54 baseline is recorded as established EMPIRICALLY (checkout 20ca1f79 + full forge test, 11 uncompilable-at-v54 files sidelined); the strongest-non-widening-position note is recorded (those 11 files contributed zero compilable v54 reds).
    - The D-351-01 rewrite map (the stale AfKing/keeper test files + DeployProtocol.sol to their v55 successors) + the 4 dedicated proof files + the D-351-02 drops (D1..D5 BY NAME + reason) are attributed via the ledger, NOT counted as new regression.
    - A SWEEP NON-WIDENING attestation states every changed test/source hunk vs 20ca1f79 is attributable to a known v55-scope commit (77c3d9ef + 453f8073 + the AGENT-committed 351 TST work); the Hardhat sanity (compile EXIT 0, DegenerusGame.test.js byte-identical) is noted.
    - The frozen-subject check (453f8073 to HEAD) stays empty.
  </acceptance_criteria>
  <done>The regression baseline 603/134/16 is attested NON-WIDENING by NAME as a strict subset (134 in 148) with the ledger cited; the D-351-01 rewrite map + the D-351-02 drops are attributed; the SWEEP NON-WIDENING claim is recorded — all appended to 352-01-DELTA-AUDIT.md; the frozen subject is byte-identical to 453f8073.</done>
</task>

</tasks>

<verification>
- The frozen-subject check (453f8073 to HEAD) is empty (zero source mutation throughout).
- 352-01-DELTA-AUDIT.md contains the per-surface NON-WIDENING table (the 13-file delta) + the Composition Attestation Matrix (the freeze spine against the as-built 4-field/DAY-keyed/live-level model + the discharged REVERT-FREE-CHAIN + EVCAP-01 + the Phase-343 SOLVENCY-01 + the OPEN-E 4-protection BLOCKING re-attestation + VRF-freeze-intact) + the Regression-Baseline Attestation (603/134/16 subset by NAME) + the SWEEP NON-WIDENING claim.
- Every v55 delta hunk maps to exactly one v55 work item (no orphan hunks); the COMMITTED 4-field stamp shape is honored (no 5-field / baseLevelPlus1 Sub-field citation).
</verification>

<success_criteria>
The SC1 delta-audit half of AUDIT-01 satisfied: every source surface changed vs the v54 baseline 20ca1f79 is enumerated and attested NON-WIDENING with each delta hunk mapped to exactly one v55 work item; the freeze spine (FREEZE-01/02/03 against the as-built 4-field/DAY-keyed/live-level model) + the discharged REVERT-FREE-CHAIN (REVERT-01/02) + EVCAP-01 + the Phase-343 SOLVENCY-01 + the OPEN-E 4-protection (CONSENT-01/02) + VRF-freeze are re-attested intact; the 603/134/16 regression baseline is attested NON-WIDENING by NAME as a strict subset (134 in 148); the frozen subject is byte-identical to 453f8073.
</success_criteria>

<output>
Create `.planning/phases/352-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/352-01-SUMMARY.md` when done
</output>
