---
phase: 01-storage-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - test/unit/StorageFoundation.t.sol
autonomous: true
requirements:
  - STOR-01
  - STOR-02
  - STOR-03
  - STOR-04

must_haves:
  truths:
    - "ticketWriteSlot, ticketsFullyProcessed, prizePoolFrozen are at the correct Slot 1 offsets verified by assembly sload"
    - "_getPrizePools/_setPrizePools round-trip uint128 boundary values (0, max, mixed) correctly"
    - "_getPendingPools/_setPendingPools round-trip uint128 boundary values correctly"
    - "_tqWriteKey != _tqReadKey for the same level, for both values of ticketWriteSlot"
    - "_swapTicketSlot reverts when read slot has entries, succeeds when drained"
    - "_swapAndFreeze activates freeze and zeros pending accumulators"
    - "_unfreezePool merges pending into live pools and clears freeze flag"
  artifacts:
    - path: "test/unit/StorageFoundation.t.sol"
      provides: "Unit tests for all STOR-01 through STOR-04 requirements plus swap/freeze/unfreeze"
      min_lines: 100
  key_links:
    - from: "test/unit/StorageFoundation.t.sol"
      to: "contracts/storage/DegenerusGameStorage.sol"
      via: "Test harness inherits DegenerusGameStorage, exposes internals"
      pattern: "StorageHarness is DegenerusGameStorage"
---

<objective>
Create a Foundry unit test file that validates all Phase 1 storage changes: field placement, packed pool round-trips, key encoding invariants, swap/freeze/unfreeze behavior.

Purpose: Prove correctness of the storage foundation before any module migration begins in Phase 2. These tests are the phase gate — they must all pass before proceeding.

Output: `test/unit/StorageFoundation.t.sol` with all tests green via `forge test --match-path test/unit/StorageFoundation.t.sol`.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-storage-foundation/01-CONTEXT.md
@.planning/phases/01-storage-foundation/01-RESEARCH.md
@.planning/phases/01-storage-foundation/01-01-SUMMARY.md
@contracts/storage/DegenerusGameStorage.sol

<interfaces>
<!-- From Plan 01, the following are now in DegenerusGameStorage.sol: -->

New Slot 1 fields (at offsets 24, 25, 26):
  uint8 internal ticketWriteSlot;
  bool internal ticketsFullyProcessed;
  bool internal prizePoolFrozen;

Constants:
  uint24 internal constant TICKET_SLOT_BIT = 1 << 23;

Packed pool variables:
  uint256 internal prizePoolsPacked;      // Slot 3
  uint256 internal prizePoolPendingPacked; // Slot 16

Helper functions (all internal):
  function _setPrizePools(uint128 next, uint128 future) internal;
  function _getPrizePools() internal view returns (uint128 next, uint128 future);
  function _setPendingPools(uint128 next, uint128 future) internal;
  function _getPendingPools() internal view returns (uint128 next, uint128 future);
  function _tqWriteKey(uint24 level) internal view returns (uint24);
  function _tqReadKey(uint24 level) internal view returns (uint24);
  function _swapTicketSlot(uint24 purchaseLevel) internal;
  function _swapAndFreeze(uint24 purchaseLevel) internal;
  function _unfreezePool() internal;

Error:
  error E();

Existing mappings used by swap:
  mapping(uint24 => address[]) internal ticketQueue;
  mapping(uint24 => mapping(address => uint40)) internal ticketsOwedPacked;

<!-- Test harness pattern: contract inherits DegenerusGameStorage and exposes internals -->
<!-- DegenerusGameStorage is abstract, so harness must provide any missing implementations -->
<!-- DegenerusGameStorage imports: ContractAddresses, IVRFCoordinator, IDegenerusStonk, BitPackingLib, GameTimeLib -->
<!-- ContractAddresses has constructor params (deployed addresses) — harness will need to handle this -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test harness and unit tests for storage field placement</name>
  <files>test/unit/StorageFoundation.t.sol</files>
  <behavior>
    - testSlot1FieldOffsets: ticketWriteSlot at Slot 1 offset 24, ticketsFullyProcessed at Slot 1 offset 25, prizePoolFrozen at Slot 1 offset 26 (verified via assembly sload + bit extraction, or by setting each field and reading slot via sload)
    - testPrizePoolsPackedSlot: prizePoolsPacked at Slot 3 offset 0
    - testPendingPoolsPackedSlot: prizePoolPendingPacked at Slot 16 offset 0
    - testNoExistingSlotShift: spot-check that currentPrizePool is still at Slot 2, ticketQueue mapping root still at Slot 17
  </behavior>
  <action>
  Create `test/unit/StorageFoundation.t.sol` with:

  1. A `StorageHarness` contract that inherits `DegenerusGameStorage` and exposes all internal functions as public/external wrappers. DegenerusGameStorage inherits from ContractAddresses which requires constructor arguments — check what ContractAddresses needs and provide dummy addresses (address(1), address(2), etc.). The harness needs:
     - `exposed_setPrizePools(uint128, uint128)` -> `_setPrizePools`
     - `exposed_getPrizePools()` -> `_getPrizePools`
     - `exposed_setPendingPools(uint128, uint128)` -> `_setPendingPools`
     - `exposed_getPendingPools()` -> `_getPendingPools`
     - `exposed_tqWriteKey(uint24)` -> `_tqWriteKey`
     - `exposed_tqReadKey(uint24)` -> `_tqReadKey`
     - `exposed_swapTicketSlot(uint24)` -> `_swapTicketSlot`
     - `exposed_swapAndFreeze(uint24)` -> `_swapAndFreeze`
     - `exposed_unfreezePool()` -> `_unfreezePool`
     - Direct setter/getter for `ticketWriteSlot`, `ticketsFullyProcessed`, `prizePoolFrozen` (for test setup)
     - A way to push entries into `ticketQueue[key]` (for swap revert tests)

  2. A `StorageFoundationTest` contract (is Test) that deploys the harness and tests:

  **STOR-01 tests (field placement):**
  - `testSlot1FieldOffsets`: Set `ticketWriteSlot = 1`, then load Slot 1 via `assembly { v := sload(1) }` and verify byte 24 is 1. Set `ticketsFullyProcessed = true`, verify byte 25 is 1. Set `prizePoolFrozen = true`, verify byte 26 is 1. Use `vm.store` or direct setters on the harness.
  - `testNoExistingSlotShift`: Set `currentPrizePool` to a known value via `vm.store(address(harness), bytes32(uint256(2)), ...)` at Slot 2 and read it back. Confirms Slot 2 is still currentPrizePool.

  **STOR-02 tests (prizePoolsPacked):**
  - `testPrizePoolPackingRoundTrip`: Call `_setPrizePools(0, 0)` then `_getPrizePools()`, assert both zero. Call with `(type(uint128).max, 0)`, verify. Call with `(0, type(uint128).max)`, verify. Call with `(12345, 67890)`, verify exact round-trip.
  - `testPrizePoolPackingBoundary`: Set `(type(uint128).max, type(uint128).max)`, verify both return max.

  **STOR-03 tests (prizePoolPendingPacked):**
  - `testPendingPoolPackingRoundTrip`: Same pattern as STOR-02 but for pending pools.

  **STOR-04 tests (key encoding):**
  - `testTicketSlotKeysDiffer`: For ticketWriteSlot=0: assert `_tqWriteKey(5) != _tqReadKey(5)`. For ticketWriteSlot=1: assert `_tqWriteKey(5) != _tqReadKey(5)`.
  - `testTicketSlotKeyBit23`: For ticketWriteSlot=0: `_tqWriteKey(5) == 5` (no bit set), `_tqReadKey(5) == 5 | TICKET_SLOT_BIT`. For ticketWriteSlot=1: `_tqWriteKey(5) == 5 | TICKET_SLOT_BIT`, `_tqReadKey(5) == 5`.
  - `testTicketSlotKeyMultipleLevels`: Test with levels 0, 1, 100, 8388607 (max valid level = 2^23 - 1) for both ticketWriteSlot values.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge test --match-path test/unit/StorageFoundation.t.sol -vvv 2>&1 | tail -30</automated>
  </verify>
  <done>All STOR-01 through STOR-04 tests pass. Field placement, packed pool round-trips, and key encoding invariants verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add swap, freeze, and unfreeze behavior tests</name>
  <files>test/unit/StorageFoundation.t.sol</files>
  <behavior>
    - testSwapTicketSlotSuccess: empty read queue -> swap succeeds, ticketWriteSlot toggles, ticketsFullyProcessed resets to false
    - testSwapTicketSlotRevertsNonEmpty: read queue has entries -> swap reverts with E()
    - testSwapAndFreezeActivates: swap + freeze sets prizePoolFrozen=true, zeros pending accumulators
    - testSwapAndFreezeAlreadyFrozen: when already frozen, swap succeeds but pending accumulators are NOT zeroed (they keep growing)
    - testUnfreezePoolMerges: pending pools merge into live pools, pending zeroed, freeze cleared
    - testUnfreezePoolNoop: when not frozen, _unfreezePool is a no-op
  </behavior>
  <action>
  Add to the existing `StorageFoundationTest` contract in `test/unit/StorageFoundation.t.sol`:

  **Swap tests:**
  - `testSwapTicketSlotSuccess`: Ensure ticketWriteSlot starts at 0. Call `_swapTicketSlot(level)` with an empty read queue. Assert ticketWriteSlot is now 1. Set ticketsFullyProcessed=true before the swap, assert it's false after.
  - `testSwapTicketSlotRevertsNonEmpty`: Push an address into `ticketQueue[_tqReadKey(level)]` via the harness. Call `_swapTicketSlot(level)` and expect revert. Use `vm.expectRevert(abi.encodeWithSignature("E()"))`.
  - `testSwapTicketSlotDoubleToggle`: Swap twice (with empty queues). ticketWriteSlot should return to 0.

  **Freeze tests:**
  - `testSwapAndFreezeActivates`: Set some pending pool values first. Call `_swapAndFreeze(level)`. Assert prizePoolFrozen == true. Assert pending pools are (0, 0) because the freeze zeroed them.
  - `testSwapAndFreezeAlreadyFrozen`: Manually set prizePoolFrozen=true and set pending pools to (100, 200). Call `_swapAndFreeze(level)`. Assert prizePoolFrozen still true. Assert pending pools are still (100, 200) — NOT zeroed.

  **Unfreeze tests:**
  - `testUnfreezePoolMerges`: Set live pools to (1000, 2000) and pending pools to (100, 200). Set prizePoolFrozen=true. Call `_unfreezePool()`. Assert live pools are now (1100, 2200). Assert pending pools are (0, 0). Assert prizePoolFrozen == false.
  - `testUnfreezePoolNoop`: Set live pools to (1000, 2000). Ensure prizePoolFrozen=false. Call `_unfreezePool()`. Assert live pools unchanged at (1000, 2000).
  - `testUnfreezePoolOverflow`: Set live pools to (type(uint128).max - 1, 0) and pending pools to (2, 0). Set prizePoolFrozen=true. This should overflow on addition — verify behavior (Solidity 0.8+ will revert). This documents the boundary.

  Run all tests: `forge test --match-path test/unit/StorageFoundation.t.sol -vvv`
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge test --match-path test/unit/StorageFoundation.t.sol -vvv 2>&1 | tail -30</automated>
  </verify>
  <done>All swap/freeze/unfreeze tests pass. Revert on non-empty read slot confirmed. Freeze activation, accumulator persistence during multi-freeze, and unfreeze merge all verified. Full test suite for Phase 1 storage foundation is green.</done>
</task>

</tasks>

<verification>
Full phase verification:
1. `forge test --match-path test/unit/StorageFoundation.t.sol -vvv` — all tests pass
2. `forge clean && forge build` — zero errors across entire codebase
3. `forge inspect DegenerusGameStorage storage-layout --json` — confirm final layout matches expectations
</verification>

<success_criteria>
- All unit tests in StorageFoundation.t.sol pass
- Tests cover: field offsets (STOR-01), packed pool round-trips (STOR-02, STOR-03), key encoding invariants (STOR-04), swap revert/success, freeze activation/persistence, unfreeze merge
- forge build succeeds for entire codebase (confirms compatibility shims work)
- Storage layout verified via forge inspect — no slot shifts
</success_criteria>

<output>
After completion, create `.planning/phases/01-storage-foundation/01-02-SUMMARY.md`
</output>
