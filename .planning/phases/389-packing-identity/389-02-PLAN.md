---
phase: 389-packing-identity
plan: 02
type: execute
wave: 2
depends_on: ["389-01"]
files_modified:
  - .planning/phases/389-packing-identity/389-02-CLAUDE-NET.md
  - .planning/phases/389-packing-identity/389-FINDINGS.md
autonomous: true
requirements: [STORAGE-01, STORAGE-02, STORAGE-03, STORAGE-04, STORAGE-05, STORAGE-06, STORAGE-07, GASID-01, GASID-02, GASID-03, GASID-04, GASID-05]
user_setup: []

must_haves:
  truths:
    - "NET 2 (the Claude adversarial net) is on record for the STORAGE + GASID surface, independent of the council"
    - "Every STORAGE-01..07 and GASID-01..05 requirement has an explicit ADJUDICATED verdict (CONFIRMED / REFUTED / BY-DESIGN / MONITOR) with the bound or source-cite that settles it"
    - "Every FC-389-01..09 lead has an explicit ADJUDICATED verdict folded with the matching council lead"
    - "STORAGE-04 / FC-389-01 (two-window lootboxEvCapPacked eviction) is settled by either proving the resolve cursor cannot lag >1 level OR surfacing it as a finding — not hand-waved"
    - "STORAGE-07 (capBucketCounts <=maxTotal+4) and GASID-02/03/04 (hash preimage / nibble-table / trait-roll) carry concrete equivalence arguments (differential or property check where cheap), not assertions"
    - "Both nets are on record before the verdict is issued; the skeptic gate is run before any CATASTROPHE/HIGH"
    - "Any CONFIRMED finding is DOCUMENTED and ROUTED to a gated fix (USER hand-review, batched) — NOT fixed in this phase; the subject stays byte-frozen"
  artifacts:
    - path: ".planning/phases/389-packing-identity/389-02-CLAUDE-NET.md"
      provides: "NET 2 adversarial analysis over STORAGE + GASID, independent of the council, with per-lead attack attempts"
      min_lines: 60
    - path: ".planning/phases/389-packing-identity/389-FINDINGS.md"
      provides: "The per-item adjudication table: every STORAGE/GASID req + every FC-389 lead with a CONFIRMED/REFUTED/BY-DESIGN/MONITOR verdict, both-nets-on-record, skeptic gate, routing of any CONFIRMED finding"
      min_lines: 80
  key_links:
    - from: "389-FINDINGS.md"
      to: "389-01 council outputs (council/*.txt)"
      via: "both-nets-on-record fold"
      pattern: "council"
    - from: "389-FINDINGS.md"
      to: "STORAGE-01..07 + GASID-01..05 + FC-389-01..09"
      via: "per-item adjudicated verdict rows"
      pattern: "STORAGE-04|FC-389-01"
---

<objective>
Run NET 2 (the deep Claude adversarial net) over the SAME packing-identity surface the council swept,
then ADJUDICATE every STORAGE-01..07 + GASID-01..05 requirement and every FC-389-01..09 lead against
the byte-frozen subject `a8b702a7`, folding in the NET-1 council leads from 389-01. Run the skeptic
gate before presenting anything as CATASTROPHE/HIGH, and write `389-FINDINGS.md` with a per-item
verdict table (CONFIRMED / REFUTED / BY-DESIGN / MONITOR) each carrying the bound or source-cite that
settles it.

Purpose: this is the verdict step. A no-finding verdict for any sweep slice requires BOTH nets on
record (AUDIT-V63-PLAN §2) — this plan puts NET 2 on record and synthesizes both nets. Any CONFIRMED
finding is DOCUMENTED + ROUTED to a gated USER-hand-review fix, never fixed here (the subject is
byte-frozen and re-freezes only after a separate gated boundary).

Output: 389-02-CLAUDE-NET.md (the independent adversarial analysis) and 389-FINDINGS.md (the
adjudication of the full phase-389 surface). NO contract source is touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/AUDIT-V63-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/phases/388-foundation-subject-freeze-green-baseline/388-02-FINDING-CANDIDATES.md
@.planning/phases/388-foundation-subject-freeze-green-baseline/388-01-LAYOUT-KEY.md
@.planning/v63-surface-map/storage-packing.md
@.planning/v63-surface-map/gas-identity.md
@.planning/phases/389-packing-identity/389-01-COUNCIL-NET.md
@.planning/phases/389-packing-identity/council/storage.gemini.txt
@.planning/phases/389-packing-identity/council/storage.codex.txt
@.planning/phases/389-packing-identity/council/gasid.gemini.txt
@.planning/phases/389-packing-identity/council/gasid.codex.txt
@test/REGRESSION-BASELINE-v63.md

<interfaces>
<!-- The 12 reqs to adjudicate (REQUIREMENTS.md): -->
<!--   STORAGE-01 narrowing width >= max; STORAGE-02 masked RMW preserves co-resident; -->
<!--   STORAGE-03 cross-module shift/mask conventions agree; STORAGE-04 two-window EV-cap eviction; -->
<!--   STORAGE-05 ABI getters preserved; STORAGE-06 no harness hardcodes a moved slot; -->
<!--   STORAGE-07 capBucketCounts <=maxTotal+4 exactness. -->
<!--   GASID-01 delegatecall(msg.data) selector/ABI identity; GASID-02 hash1/hash2 preimage identity; -->
<!--   GASID-03 PriceLookup nibble-table output identity; GASID-04 trait-roll + _farFutureSeed equivalence; -->
<!--   GASID-05 no externally-observable behavior change. -->
<!-- The 9 leads (388-02-FINDING-CANDIDATES rows A+B): FC-389-01..04 (STORAGE) + FC-389-05..09 (GASID). -->
<!-- Authoritative slots (388-01-LAYOUT-KEY): Game lootboxEvCapPacked@40, levelDgnrsPacked@26, -->
<!--   deityBoonPacked@36, bingoFirsts@53, DecClaimRound struct @43 off 0/12/28; sDGNRS slot-0 pack; -->
<!--   BurnieCoinflip stake@0/dayResult@1; the 17 SOUND verdicts + FA-1..4 in storage-packing.md. -->
<!-- Frozen-source read convention: git show a8b702a7:contracts/<File>.sol (ignore working tree). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NET 2 adversarial analysis + per-req / per-lead adjudication vs frozen source</name>
  <files>.planning/phases/389-packing-identity/389-02-CLAUDE-NET.md</files>
  <read_first>
    - .planning/phases/388-foundation-subject-freeze-green-baseline/388-02-FINDING-CANDIDATES.md (rows FC-389-01..09 verbatim — the leads to adjudicate)
    - .planning/v63-surface-map/storage-packing.md (FA-1..FA-4 + the 17 SOUND verdicts to attack)
    - .planning/v63-surface-map/gas-identity.md (§1-§9 identity claims + F-1..F-5 to attack)
    - .planning/phases/388-foundation-subject-freeze-green-baseline/388-01-LAYOUT-KEY.md (authoritative slots + DecClaimRound offsets)
    - the frozen source via git show a8b702a7:contracts/storage/DegenerusGameStorage.sol (the EV-cap helpers _lootboxEvUsedFor/_setLootboxEvUsedFor ~lines 1698-1710, _addLevelDgnrsClaimed ~1160, _debitClaimableAndAfking), contracts/modules/DegenerusGameLootboxModule.sol (resolve/open call sites 872/887/955/974/1097/1145), contracts/modules/DegenerusGameWhaleModule.sol / DegenerusGameAfkingModule.sol / DegenerusGameMintModule.sol (the level+1 deposit sites 852/970/1685/1706), contracts/libraries/PriceLookupLib.sol, contracts/libraries/EntropyLib.sol, contracts/libraries/JackpotBucketLib.sol (capBucketCounts/bucketShares), contracts/modules/DegenerusJackpots.sol (trait-roll _rollWinningTraitsPair/_applyHeroResult/_rollHeroSymbol), contracts/DegenerusGame.sol (the 30 delegatecall(msg.data) wrappers), contracts/StakedDegenerusStonk.sol (uint96/uint128 narrowings)
    - .planning/phases/389-packing-identity/389-01-COUNCIL-NET.md + council/*.txt (NET 1 leads to consider — but attack INDEPENDENTLY first, then compare)
  </read_first>
  <action>
    First confirm `git diff a8b702a7 -- contracts/` is empty (read-only over the frozen subject). Read
    all source via `git show a8b702a7:contracts/<File>.sol` — ignore the working tree.

    Run the Claude adversarial net INDEPENDENTLY of the council (do not read the council outputs as the
    starting point — attack the identity claims first, then fold the council leads in at the end of this
    task). For each of the 12 reqs and 9 leads, attempt to BREAK the identity/safety claim with a
    concrete reachable call sequence, then record the result.

    STORAGE adjudication (STORAGE-01..07 + FC-389-01..04):
    - STORAGE-01 (no truncating cast): enumerate EVERY narrowing cast in the changed packs
      (DecClaimRound poolWei uint96/totalBurn uint128/rngWord uint32; sDGNRS _totalSupply uint128/
      _pendingRedemptionEthValue uint96/poolBalances uint128[5]; bingoFirsts; deityBoonPacked;
      levelDgnrsPacked) and state the real-world-max bound that makes each safe (supply caps, ETH supply,
      pool conservation, day stamps). FC-389-02/-08 (the uint96/uint128 narrowings that truncate
      silently) fold here — confirm no path inflates segregated ETH / a pool beyond width; cross-ref 390.
    - STORAGE-02 (masked RMW preserves co-resident): round-trip each helper (_set/_add/_get) for the
      consolidated packs — _debitClaimableAndAfking (per-half guards), _setLootboxEvUsedFor / window
      eviction, _addLevelDgnrsClaimed / _setLevelDgnrsAllocation, bingoFirsts symbol/quad halves,
      deityBoonPacked day/mask, slot-5 totalFlipReversals+lastVrfProcessedTimestamp. Either argue the
      round-trip property by mask construction OR note where the green baseline's harness already proves
      it (StorageFoundation canary, V55RevertFreeEvCap, AffiliateDgnrsClaim, RngLockDeterminism).
    - STORAGE-03 (cross-module convention agreement): confirm the delegatecall-shared layout's single
      inherited DegenerusGameStorage base means all 13 modules + Game agree on slots by construction;
      verify the two cross-module-read packs (deityBoonPacked read at Game.sol:884 + Lootbox:1146;
      levelDgnrsPacked read/written by Bingo/Whale/Advance) use the IDENTICAL shift/mask convention.
    - STORAGE-04 / FC-389-01 (the §6 prime target — DEDICATED rigorous treatment): the live key set
      claim is {currentLevel, currentLevel+1}. PROVE the resolve cursor cannot lag >1 level behind a
      deposit, OR surface a finding. Trace every WRITE site of lootboxEvCapPacked (the deposit sites at
      level+1: Whale:852, Afking:970, Mint:1685/1706) and every _applyEvMultiplierWithCap call site
      (Lootbox:887/974/1097 at currentLevel) and check whether ANY deferred/queued resolve, far-future
      ticket path, or redemption-lootbox path can run _setLootboxEvUsedFor with a level NOT in
      {currentLevel, currentLevel+1} at write time (e.g. a resolve cursor that advanced two game levels
      while a queued window stayed stamped at an old level). If a third distinct live level key is
      reachable, eviction silently zeroes a live window → the 10 ETH per-level EV cap can be re-earned →
      CONFIRMED finding. If the cursor-lag bound holds (resolve always runs at currentLevel and deposits
      always at currentLevel/+1, so no third key is ever live), REFUTE with the call-graph cite. Settle
      it either way — no hand-waving.
    - STORAGE-05 (ABI getters preserved): confirm every privatized/packed field (sDGNRS _totalSupply/
      pendingRedemptionEthValue/pendingResolveDay; Admin votes/voteWeight/feedVotes/feedVoteWeight) keeps
      its same-name external view getter (ERC-20 totalSupply() still returns uint256) — no interface break.
    - STORAGE-06 (no harness hardcodes a moved slot): cross-check 388-01-LAYOUT-KEY §6 (the per-harness
      reconciliation ledger — every moved-field poke confirmed correct) against the green baseline
      (REGRESSION-BASELINE-v63 854/0/110, expected red name-set empty). FC-389-04 folds here.
    - STORAGE-07 (capBucketCounts <=maxTotal+4 — folded debt, concrete equivalence): read
      capBucketCounts in JackpotBucketLib, derive whether the per-bucket count can exceed maxTotal+4,
      and either prove the documented downstream clamps fully defend the imprecision (cite the clamp
      sites) or surface it. Use a cheap differential/property argument (e.g. enumerate the cap arithmetic
      over the bounded bucket-count domain) rather than asserting "defended."
    - FC-389-03 (DecClaimRound.totalBurn comment-vs-accumulator): confirm the accumulator stores RAW
      burns (delta=e.burn) bounded < effective < supply < uint128; flag the comment mismatch as INFO.

    GASID adjudication (GASID-01..05 + FC-389-05..09):
    - GASID-01 / FC-389-09 (delegatecall(msg.data) selector + ABI identity): re-derive (or spot-check
      against the gas-identity map's 30-selector table) that each wrapper selector == its module
      selector, and that the typed wrappers keep the same ABI decoder so short/dirty/malformed calldata
      reverts identically. For the 3 dynamic-array wrappers, argue the module shares the wrapper's
      decoder (no decoder-divergence corner) or surface a fuzz gap.
    - GASID-02 (hash1/hash2 preimage identity — concrete): apply the operand-width rule (encode/
      encodePacked diverge only on sub-word operands) to EACH migrated site (the gas-identity map's
      list), confirming every operand is a full 32-byte type. Where cheap, compute a couple of keccak
      preimages numerically to spot-check rather than trust prose.
    - GASID-03 (PriceLookup nibble-table output identity — concrete): independently recompute old-vs-new
      priceForLevel over the documented domain (level in a representative bounded set covering all decade
      buckets + milestone boundaries, cycleOffset in [0,99]) and assert 0 mismatches, OR cite the
      gas-identity map's exhaustive comparison and re-run a small differential spot-check.
    - GASID-04 (trait-roll + _farFutureSeed equivalence — concrete): trace _rollWinningTraitsPair vs the
      baseline _rollWinningTraits(w,false/true) — confirm the hero is rolled once and applied to both,
      rBonus=hash2(randWord,BONUS_TRAITS_TAG) matches the baseline bonus r, _soloAdjustedEntropy
      reproduces the inlined expression, and _farFutureSeed is a literal extraction (same value
      intra-tx). Check inputs/boundaries/revert paths.
    - GASID-05 (no externally-observable behavior change): assert behavior-identity (output/revert/event)
      for the refactor set, anchored on the above + the green baseline's empty expected-red set.
    - FC-389-05 (DecClaimRound.rngWord uint32 narrowing — gas-half): confirm the narrowing changes only
      the claim-time lootbox seed width (winner selection uses the full word at snapshot); the
      distribution-bias half is routed to 391 (RNG-02 / FC-391-04) — note the cross-ref, adjudicate only
      the narrowing-equivalence half here.
    - FC-389-06 (lootboxEvCapPacked level-0 stamp collision): confirm callers pass gameLevel+1>=1 so
      level 0 is unreachable, and check the uint24 level+1 wrap edge; REFUTE or MONITOR.
    - FC-389-07 (_addLevelDgnrsClaimed unclamped high-half): confirm EVERY claim path enforces
      claimed+add<=allocation<=2^128 (the caller invariant the unclamped newClaimed<<128 relies on);
      cite the claim sites (Bingo/Whale/Advance) or surface.

    For each item: attempt the break, record PROPERTY · attack/call-sequence tried · STATE VAR +
    file:line at a8b702a7 · the bound/cite that settles it · a provisional verdict (CONFIRMED / REFUTED /
    BY-DESIGN / MONITOR). At the END of the task, read the council outputs (389-01 council/*.txt) and
    note for each item whether the council raised a convergent/divergent lead — fold any council-only
    lead in as an additional row to adjudicate. Write all of this to 389-02-CLAUDE-NET.md.

    Re-verify `git diff a8b702a7 -- contracts/` empty at the end (read-only). Do NOT run hardhat. Use
    neutral defensive-engineering vocabulary throughout.
  </action>
  <verify>
    <automated>test -s .planning/phases/389-packing-identity/389-02-CLAUDE-NET.md && grep -q "STORAGE-04" .planning/phases/389-packing-identity/389-02-CLAUDE-NET.md && grep -q "FC-389-01" .planning/phases/389-packing-identity/389-02-CLAUDE-NET.md && grep -q "GASID-03" .planning/phases/389-packing-identity/389-02-CLAUDE-NET.md && grep -q "STORAGE-07" .planning/phases/389-packing-identity/389-02-CLAUDE-NET.md && grep -q "capBucketCounts" .planning/phases/389-packing-identity/389-02-CLAUDE-NET.md && [ -z "$(git -C /home/zak/Dev/PurgeGame/degenerus-audit diff a8b702a7 -- contracts/)" ]</automated>
  </verify>
  <acceptance_criteria>
    389-02-CLAUDE-NET.md contains an independent adversarial analysis with an attack attempt + a
    provisional verdict for EACH of STORAGE-01..07, GASID-01..05, and FC-389-01..09; STORAGE-04 /
    FC-389-01 carries a dedicated cursor-lag treatment (proof or finding, not hand-waved); STORAGE-07
    (capBucketCounts) and GASID-02/03/04 carry concrete equivalence arguments (differential/property,
    not assertion); the council leads are folded in at the end; `git diff a8b702a7 -- contracts/` is empty.
  </acceptance_criteria>
  <done>
    NET 2 (the Claude adversarial net) is on record, independent of the council, with a per-item attack
    attempt + provisional verdict over the full phase-389 surface, the council leads folded in.
  </done>
</task>

<task type="auto">
  <name>Task 2: Synthesize both nets, run the skeptic gate, write 389-FINDINGS.md with the per-item verdict table</name>
  <files>.planning/phases/389-packing-identity/389-FINDINGS.md</files>
  <read_first>
    - .planning/phases/389-packing-identity/389-02-CLAUDE-NET.md (Task 1 — NET 2 provisional verdicts)
    - .planning/phases/389-packing-identity/389-01-COUNCIL-NET.md + council/*.txt (NET 1 leads)
    - .planning/AUDIT-V63-PLAN.md §2 (both-nets-on-record rule) + §4 (threat weighting) + §5 (design-intent anchor) + §8 (process rules: audit-only, routing)
    - .planning/REQUIREMENTS.md (the exact STORAGE-01..07 + GASID-01..05 text to attest)
    - the skeptic-gate posture: [[feedback_skeptic_pass_before_catastrophe]] (structural-protection check + 3-condition EV lens before any CATASTROPHE/HIGH)
  </read_first>
  <action>
    Synthesize NET 1 (council) and NET 2 (Claude) into 389-FINDINGS.md — the phase-389 adjudication
    deliverable. Structure:

    (1) Header: subject `a8b702a7` byte-frozen; baseline = REGRESSION-BASELINE-v63 (854/0/110, expected
    red set empty); method = COUNCIL + CLAUDE both; posture = AUDIT-ONLY (a CONFIRMED finding routes to
    a gated USER-hand-review fix, never fixed here).

    (2) Both-nets-on-record attestation: a short table confirming, per slice (STORAGE / GASID), that
    NET 1 (council — available/skipped per 389-01-COUNCIL-NET.md) and NET 2 (Claude — 389-02-CLAUDE-NET)
    are both on record. A no-finding (REFUTED/BY-DESIGN) verdict for any item MUST cite both nets.

    (3) The per-item adjudication table — ONE row per item, covering ALL 12 reqs (STORAGE-01..07,
    GASID-01..05) AND ALL 9 leads (FC-389-01..09). Columns: ITEM · what it claims · NET-1 result · NET-2
    result · ADJUDICATED VERDICT (CONFIRMED / REFUTED / BY-DESIGN / MONITOR) · the settling bound/source-
    cite (file:line at a8b702a7 or the green-baseline anchor). STORAGE-04 / FC-389-01 MUST cite the
    cursor-lag bound (or carry a CONFIRMED finding); STORAGE-07 + GASID-02/03/04 MUST cite their concrete
    equivalence argument. Weight each finding per AUDIT-V63-PLAN §4 (most packing-identity issues are
    correctness/solvency-adjacent, not RNG); apply the §5 design-intent anchor (do not re-litigate
    intended mechanics — verify identity/safety, not desirability).

    (4) Skeptic gate: before tagging ANY item CATASTROPHE/HIGH, run the dual-gate filter (structural-
    protection check + the 3-condition EV lens) and record the gate result for each elevated item. If
    nothing reaches HIGH (the expected outcome — the surface-maps found 0 HIGH on inspection), state
    that explicitly with the gate applied to the MED-attention items (FC-389-01).

    (5) Routing: for any CONFIRMED finding, write a CONTRACT-CHANGE-NEEDED / ROUTED block (the finding,
    its weight, the proposed fix shape, and the note that it goes to a SEPARATE gated USER-hand-review
    boundary — batched, never pre-approved, never auto-committed; the subject re-freezes after). If
    there are zero CONFIRMED findings, state "0 CONFIRMED — document-only; STORAGE-01..07 + GASID-01..05
    attested at a8b702a7" and note any INFO/MONITOR items carried forward (e.g. FC-389-03 comment
    mismatch, the FC-389-05 distribution-bias half cross-ref to 391, the FC-389-02/-08 cross-ref to 390).

    (6) Re-attestation line: each of STORAGE-01..07 and GASID-01..05 marked attested-or-finding.

    Re-confirm `git diff a8b702a7 -- contracts/` empty at the end. Neutral vocabulary throughout. No
    contract source is modified — this is a documentation deliverable.
  </action>
  <verify>
    <automated>test -s .planning/phases/389-packing-identity/389-FINDINGS.md && for id in STORAGE-01 STORAGE-02 STORAGE-03 STORAGE-04 STORAGE-05 STORAGE-06 STORAGE-07 GASID-01 GASID-02 GASID-03 GASID-04 GASID-05 FC-389-01 FC-389-02 FC-389-03 FC-389-04 FC-389-05 FC-389-06 FC-389-07 FC-389-08 FC-389-09; do grep -q "$id" .planning/phases/389-packing-identity/389-FINDINGS.md || { echo "MISSING $id"; exit 1; }; done && grep -Eq "both.nets|NET 1.*NET 2|both nets on record" .planning/phases/389-packing-identity/389-FINDINGS.md && grep -Eq "skeptic" .planning/phases/389-packing-identity/389-FINDINGS.md && [ -z "$(git -C /home/zak/Dev/PurgeGame/degenerus-audit diff a8b702a7 -- contracts/)" ]</automated>
  </verify>
  <acceptance_criteria>
    389-FINDINGS.md contains: a both-nets-on-record attestation per slice; a per-item verdict row for
    EACH of STORAGE-01..07, GASID-01..05, and FC-389-01..09 with a CONFIRMED/REFUTED/BY-DESIGN/MONITOR
    verdict + settling cite; STORAGE-04/FC-389-01 cites the cursor-lag bound or carries a finding;
    STORAGE-07 + GASID-02/03/04 cite concrete equivalence arguments; the skeptic gate is run and
    recorded; any CONFIRMED finding is documented + ROUTED (not fixed); `git diff a8b702a7 -- contracts/`
    is empty.
  </acceptance_criteria>
  <done>
    The phase-389 packing/gas behavior-identity surface is adjudicated with both nets on record, the
    skeptic gate applied, every req + lead carrying an explicit verdict, and any CONFIRMED finding
    documented + routed to a gated fix — the subject byte-frozen throughout.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit-process → byte-frozen subject | the risk is a MISSED identity break or a FALSE-GREEN adjudication (a real packing/gas defect waved through), or the audit mutating the frozen subject — not a new feature threat |
| single-net verdict | the risk is issuing a no-finding verdict with only one net on record (the v60/v62 lesson: Claude-only missed council findings) |

## STRIDE Threat Register (audit-only posture)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-389-04 | Tampering | byte-frozen subject contracts/ | mitigate | git-status-verify `git diff a8b702a7 -- contracts/` empty at the end of each task; all source read via `git show a8b702a7:` |
| T-389-05 | Repudiation / false-green | a REFUTED/BY-DESIGN verdict issued without both nets | mitigate | the both-nets-on-record table; every no-finding verdict cites NET 1 + NET 2 |
| T-389-06 | Elevation / under-weighting | a CONFIRMED finding tagged HIGH without the skeptic filter, or a real break under-weighted | mitigate | the skeptic gate (structural-protection + EV lens) runs before any CATASTROPHE/HIGH; weighting per AUDIT-V63-PLAN §4 |
| T-389-07 | Tampering | a CONFIRMED finding silently fixed in-phase | mitigate | audit-only posture: CONFIRMED findings are DOCUMENTED + ROUTED to a gated USER-hand-review boundary, never fixed here |
</threat_model>

<verification>
- NET 2 (Claude) on record, independent, per-item attack + verdict over all 12 reqs + 9 leads.
- Both nets on record per slice before the verdict; the both-nets table present.
- STORAGE-04 / FC-389-01 settled (cursor-lag proof or finding); STORAGE-07 + GASID-02/03/04 carry
  concrete equivalence arguments.
- Skeptic gate run before any CATASTROPHE/HIGH; result recorded.
- Any CONFIRMED finding documented + ROUTED (not fixed); subject byte-frozen.
- `git diff a8b702a7 -- contracts/` empty.
</verification>

<success_criteria>
The full phase-389 packing/gas behavior-identity surface (STORAGE-01..07 + GASID-01..05 + the 9
FC-389 leads) is adjudicated against the byte-frozen subject `a8b702a7` with BOTH nets on record, the
skeptic gate applied, and every item carrying an explicit CONFIRMED/REFUTED/BY-DESIGN/MONITOR verdict
backed by a bound or source-cite. Any CONFIRMED finding is documented + routed to a gated fix; the
subject stays byte-frozen.
</success_criteria>

<output>
Create `.planning/phases/389-packing-identity/389-02-SUMMARY.md` when done
</output>
