Read `a8b702a7` via `git show`. I found no production packing bug in STORAGE-01/02/03/04/05/07. The concrete breaks are all STORAGE-06 stale test-harness slots.

**Findings**

1. **FINDING: STORAGE-06 stale `mintPacked_` invariant slot**
   Call sequence: `CompositionInvariant.setUp()` targets `CompositionHandler` (`test/fuzz/invariant/Composition.inv.t.sol:20-26`) -> `action_whaleThenPurchase()` / `action_purchaseThenAdvance()` runs real calls then `_checkCompositionInvariants()` (`test/fuzz/handlers/CompositionHandler.sol:66-90`, `97-124`) -> `_checkGapBits()` reads `keccak256(player, 10)` (`test/fuzz/handlers/CompositionHandler.sol:208-213`).
   State: `mintPacked_` is `contracts/storage/DegenerusGameStorage.sol:450`; slot 10 is now `rngWordByDay` at `contracts/storage/DegenerusGameStorage.sol:461`.
   Severity: LOW / confirmatory. The invariant can stay green while the real `mintPacked_` gap bits are never read.

2. **FINDING: STORAGE-06 stale box-sweep cursor/player slots**
   Call sequence: `SweepWorstCaseDrain.testFuzz_WorstCaseSweepDrainsBoundedNoBrick()` buys real boxes (`test/fuzz/SweepWorstCaseDrain.t.sol:249-260`) -> `_appendSkipPrefix()` writes mapping root slot 59 (`:267`, helper `:155-167`) -> `_parkFrontier()` writes slot 58 (`:291`, helper `:143-152`) -> `game.openBoxes()` (`:305-307`).
   State: slot 58 is `_subCursor/_subOpenCursor/_afkingResetDay` (`contracts/storage/DegenerusGameStorage.sol:2321`, `2325`, `2332`); real `boxCursor/boxCursorIndex/presaleCloseIndex` are slot 59 (`:2341`, `2346`, `2352`); real `boxPlayers` root is slot 60 (`:2360`).
   Also present in `RngLockDeterminism`: `_parkBoxFrontier()` hardcodes `SLOT_BOX_CURSORS = 58` (`test/fuzz/RngLockDeterminism.t.sol:2343-2352`) and is used before `openBoxes()` (`:2295-2312`).
   Severity: LOW / confirmatory, with HIGH-adjacent coverage impact because this is a gas/drain harness.

3. **FINDING: STORAGE-06 stale JS `lootboxRngPacked` slot**
   Call sequence: edge tests call `seedLootboxRngIndex()` then production `placeDegeneretteBet()`; examples at `test/edge/HeroOverrideDayIndex.test.js:167-168`, `239-245`, `604-611`, and `test/edge/HeroOverrideWeightedRoll.test.js:744-756`.
   State: tests write slot 35 as `LOOTBOX_RNG_PACKED_SLOT` (`HeroOverrideDayIndex.test.js:58-63`, `:82-94`; `HeroOverrideWeightedRoll.test.js:202-205`, `:336-346`). Real `lootboxRngPacked` is `contracts/storage/DegenerusGameStorage.sol:1516`; slot 35 is `lootboxRngWordByIndex`’s mapping root at `:1643`.
   Severity: LOW / confirmatory. Current initialization may mask this, but the harness seed writes the wrong field.

**Lead Verdicts**

- **FA-1 / FC-389-01:** VERIFIED SOUND. EV-cap writes use `level + 1` / live current level only: mint (`DegenerusGameMintModule.sol:1685-1692`, `1706-1713`), whale (`DegenerusGameWhaleModule.sol:852-879`, `897-904`), afking (`GameAfkingModule.sol:970-987`, `1007-1014`), direct/redemption/afking resolve (`DegenerusGameLootboxModule.sol:877-887`, `966-974`, `1089-1097`). Deferred human opens do not SSTORE cap; they consume frozen `adj` and clear `lootboxEth` (`DegenerusGameLootboxModule.sol:567-579`). The two-window helper masks correctly (`DegenerusGameStorage.sol:1698-1737`).
- **FC-389-02:** VERIFIED SOUND. `_pendingRedemptionEthValue` is `uint96` (`StakedDegenerusStonk.sol:221-223`) but increments only after `pullRedemptionReserve()` succeeds (`:1061-1066`; `DegenerusGame.sol:1572-1599`). Pool lanes are `uint128[5]` (`StakedDegenerusStonk.sol:249-253`), initialized from `INITIAL_SUPPLY` slices (`:384-409`), debited/clamped before transfer (`:548-570`), and conserved on pool-to-pool moves (`:579-592`).
- **FC-389-03:** VERIFIED SOUND. The lead premise is not true in this frozen source: regular `DecClaimRound.totalBurn` stores effective burn, not raw. `recordDecBurn()` computes `effectiveAmount`, stores it in `e.burn`, and adds `delta` to subbucket totals (`DegenerusGameDecimatorModule.sol:178-195`, `595-601`); the round snapshots those totals (`:252-274`). The imprecise comment is instead `DecEntry.burn` saying “BURNIE burned” at `DegenerusGameStorage.sol:1746-1749`; it is effective burn.
- **FC-389-04:** FINDING, see STORAGE-06 findings above. I also checked a positive case: `DecimatorBountyRegression` uses `SLOT_DEC_CLAIM_ROUNDS = 43` and packs `poolWei | totalBurn << 96 | rngWord << 224` (`test/fuzz/DecimatorBountyRegression.t.sol:31-68`), matching `DecClaimRound` (`DegenerusGameStorage.sol:1758-1775`, `1787-1789`).
- **STORAGE-07 lead:** VERIFIED DEFENDED, not cap-exact. `capBucketCounts()` can leave small slack because count-1 buckets pass through (`JackpotBucketLib.sol:147-191`), but production consumers are only the ETH paths (`DegenerusGameJackpotModule.sol:248-267`, `417-440`). `_processBucket()` clamps each bucket to `MAX_BUCKET_WINNERS = 250` before the `uint8` cast (`:186`, `1141-1152`), and `bucketShares()` assigns the remainder as `pool - distributed` (`JackpotBucketLib.sol:214-240`). Ticket paths do not consume `capBucketCounts`; they split exact `maxWinners` in `_computeBucketCounts()` (`DegenerusGameJackpotModule.sol:796-804`, `969-985`).

**Thesis Points**

- **STORAGE-01:** VERIFIED SOUND. Reviewed narrowed fields are bounded by explicit game/economic caps: EV used is capped at `10 ether < 2^64` (`DegenerusGameStorage.sol:1683-1690`), DGNRS halves are supply-bounded (`:1132-1137`), player ETH halves are total-ETH-bounded (`:416-423`), sDGNRS supply/pending/pool lanes are bounded as above, and daily/level counters are `uint24` with documented headroom (`:213-237`).
- **STORAGE-02:** VERIFIED SOUND. Masked RMW helpers preserve siblings: EV windows (`:1721-1736`), level allocation/claimed halves (`:1150-1164`), claimable/afking combined debit guards both halves (`:956-959`), and `_psWrite/_goWrite/_lrWrite` use mask-and-merge (`:1065-1067`, `1095-1097`, `1557-1559`).
- **STORAGE-03:** VERIFIED SOUND. Cross-module EV readers/writers use the same base helpers; decode is `used @ [0:64]/[88:152]`, level stamps at `[64:88]/[152:176]` (`:1698-1706`), and writer layout matches (`:1720-1737`).
- **STORAGE-04:** VERIFIED SOUND. No reachable cap write uses a third stale level key; queued/deferred human opens do not write the cap.
- **STORAGE-05:** VERIFIED SOUND. ABI-preserving getters exist for packed/private sDGNRS fields: `poolBalance()` (`StakedDegenerusStonk.sol:509-510`), `totalSupply()` (`:513-515`), `pendingRedemptionEthValue()` (`:518-521`), `pendingResolveDay()` (`:524-528`).
- **STORAGE-06:** BROKEN in test harnesses only; see findings.
- **STORAGE-07:** VERIFIED DEFENDED; not exact, but downstream consumers clamp/avoid over-distribution.

I did not run the test suite; this pass was source-only against the frozen tree.
