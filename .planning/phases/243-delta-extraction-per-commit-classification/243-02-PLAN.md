---
phase: 243-delta-extraction-per-commit-classification
phase_number: 243
plan: 243-02
plan_number: 02
type: execute
wave: 2
depends_on: [243-01]
files_modified:
  - audit/v31-243-DELTA-SURFACE.md
autonomous: true
requirements: [DELTA-02]
requirements_addressed: [DELTA-02]
baseline: 7ab515fe
baseline_sha: 7ab515fe
head: 771893d1
head_sha: 771893d1
head_anchor: 771893d1
must_haves:
  truths:
    - "`audit/v31-243-DELTA-SURFACE.md` Section 2 is populated in place: the literal header `## Section 2 — Aggregate Function Classification (RESERVED FOR 243-02)` is replaced with `## Section 2 — Aggregate Function Classification`, and the section body contains the D-04 5-bucket classification table covering every `func` or `modifier` row from Section 1."
    - "Row count integrity: `grep -c '^| D-243-F' audit/v31-243-DELTA-SURFACE.md` equals the count of Section 1 `func` + `modifier` rows. Every Section 1 `func`/`modifier` row has a 1:1 Section 2 classification row — no drops, no orphans."
    - "Per CONTEXT.md D-04: every Section 2 row's Classification column is strictly one of the 5 locked values: `NEW`, `MODIFIED_LOGIC`, `REFACTOR_ONLY`, `DELETED`, `RENAMED`. No other vocabulary permitted."
    - "Per CONTEXT.md D-05: the 11 pre-locked borderline verdicts are honored verbatim: (1) 16597cac `_distributeEthGameOver` (or equivalent two-call-split continuation fn) = MODIFIED_LOGIC — `_unlockRng(day)` removal is a side-effect removal; (2) 16597cac multi-line SLOAD + tuple destructuring reformat = REFACTOR_ONLY; (3) 6b3f4f3c `_callTicketPurchase` = MODIFIED_LOGIC — dropped `freshEth` return; (4) 6b3f4f3c `handlePurchase` `ethFreshWei → ethMintSpendWei` rename = REFACTOR_ONLY within rename hunk, MODIFIED_LOGIC where value semantics change (split into two rows if needed OR annotate both aspects); (5) ced654df BAF `_jackpotTicketRoll` emit-site add = MODIFIED_LOGIC; (6) ced654df Whale-pass fallback `_awardJackpotTickets` emit-site add = MODIFIED_LOGIC; (7) 771893d1 MintModule/WhaleModule `gameOver → _livenessTriggered` gate swaps = MODIFIED_LOGIC; (8) 771893d1 `sDGNRS.burn` / `burnWrapped` State-1 block = MODIFIED_LOGIC; (9) 771893d1 `handleGameOverDrain` pendingRedemptionEthValue subtraction = MODIFIED_LOGIC; (10) 771893d1 `_livenessTriggered` VRF-dead + day-math-first = MODIFIED_LOGIC; (11) 771893d1 `_gameOverEntropy` rngRequestTime clearing = MODIFIED_LOGIC; (11b) 771893d1 `_handleGameOverPath` check ordering = MODIFIED_LOGIC."
    - "Per CONTEXT.md D-06: every classification row cites the exact diff hunk at HEAD in the Hunk Ref column with format `contracts/<path>:<head-line-start>-<head-line-end>` for MODIFIED_LOGIC/REFACTOR_ONLY/NEW, `contracts/<path>:<baseline-line-start>-<baseline-line-end>@7ab515fe` for DELETED, or `<old-name>→<new-name>@contracts/<path>:<head-range>` for RENAMED."
    - "Per CONTEXT.md D-19: every MODIFIED_LOGIC / REFACTOR_ONLY row's One-Line Rationale column names the specific execution-trace-changing element (SSTORE / external call / branch / emit / return-path) OR the specific non-execution-changing element (whitespace / rename / multi-line split / local-variable-name / NatSpec). No hand-wave 'looks like a refactor' rationales — executor must name the concrete source element the verdict keys on."
    - "Per CONTEXT.md D-08 columns: `Row ID | Function Signature | File:Line | Classification | Hunk Ref | One-Line Rationale`. Every column populated in every row."
    - "Row ID format: `D-243-F###` zero-padded, monotonic starting at F001. No `D-243-C` / `D-243-S` / `D-243-X` / `D-243-I` collisions with Section 1 / 4 / 5."
    - "Change count cards in Section 1.1-1.5 subsections are updated IN PLACE: the `functions: N` entry now reflects the final classified count, and a classification breakdown is appended (e.g., `functions: 5 (NEW: 1 / MODIFIED_LOGIC: 3 / REFACTOR_ONLY: 1 / DELETED: 0 / RENAMED: 0)`). This is the ONLY permitted edit to Section 1 by this plan — row data is preserved byte-identical; only the change-count-card lines are updated."
    - "Per CONTEXT.md D-20: zero `F-31-NN` IDs. If 243-02 surfaces a borderline classification requiring a Phase 246 finding-candidate flag (e.g., a function that appears NEW at first read but is actually a rename of a deleted function with identical body — not caught by 243-01's hunk scan), append to Section 1.6 Finding Candidates list (preserving 243-01's entries) with file:line + rationale + suggested severity."
    - "Section 7.2 Reproduction Recipe Appendix — 243-02 commands is populated per CONTEXT.md D-18: replaces the literal header `### 7.2 Plan 243-02 commands (DELTA-02 classification) — RESERVED FOR 243-02` with populated content. Commands consist of the `git show -L` / `git blame` / `git log -S` invocations the executor used to confirm each classification verdict."
    - "Scope gate: zero writes to `contracts/` or `test/`. `git status --porcelain contracts/ test/` empty before and after every task. Zero writes to any file other than `audit/v31-243-DELTA-SURFACE.md`."
    - "Input integrity: verify `audit/v31-243-DELTA-SURFACE.md` from 243-01 is committed before this plan starts. `git log --oneline -1 -- audit/v31-243-DELTA-SURFACE.md` must show a `243-01` commit. Section 1 + 4 + 5 + 7.1 must be fully populated (no `RESERVED FOR TASK` markers remain from 243-01); Section 2 / 3 / 6 must still carry the `RESERVED FOR 243-02` / `RESERVED FOR 243-03` markers."
  artifacts:
    - path: "audit/v31-243-DELTA-SURFACE.md"
      provides: "DELTA-02 aggregate function classification appended in place (Section 2) + Section 1 change-count-card updates + Section 7.2 reproduction recipe"
      contains: "## Section 2 — Aggregate Function Classification"
  key_links:
    - from: "Section 1 `func`/`modifier` rows (D-243-C###)"
      to: "Section 2 classification rows (D-243-F###)"
      via: "1:1 per-function classification per CONTEXT.md D-04"
      pattern: "D-243-F[0-9]{3}"
    - from: "CONTEXT.md D-05 11 pre-locked borderline verdicts"
      to: "Section 2 rows for the specific named functions"
      via: "verdict must match D-05 exactly for those rows; deviation requires explicit rationale naming the source evidence that contradicts D-05"
      pattern: "(MODIFIED_LOGIC|REFACTOR_ONLY|NEW|DELETED|RENAMED)"
    - from: "git show {sha} -L <range>:<file>"
      to: "Section 2 Hunk Ref + One-Line Rationale columns"
      via: "CONTEXT.md D-06 + D-19 hunk citation + evidence-burden for every verdict"
      pattern: "contracts/[^:]+:[0-9]+-[0-9]+"
---

<objective>
DELTA-02 classification wave 2 (parallel with 243-03). For every `func` or `modifier` row in Section 1 of the committed 243-01 deliverable, apply the CONTEXT.md D-04 5-bucket taxonomy {NEW, MODIFIED_LOGIC, REFACTOR_ONLY, DELETED, RENAMED} with hunk citation + one-line rationale naming the specific execution-trace-changing element (per D-19).

Output: append Section 2 (Aggregate Function Classification) in place to `audit/v31-243-DELTA-SURFACE.md`; update the per-commit change-count cards in Sections 1.1-1.5 to include the classification breakdown (e.g., `functions: 5 (NEW: 1 / MODIFIED_LOGIC: 3 / REFACTOR_ONLY: 1 / DELETED: 0 / RENAMED: 0)`); append §7.2 to the reproduction recipe appendix.

Requirement: DELTA-02 — classify every changed function with {NEW, MODIFIED_LOGIC, REFACTOR_ONLY, DELETED, RENAMED}, hunk cited, zero function unclassified.

Input contract: `audit/v31-243-DELTA-SURFACE.md` (committed from 243-01) provides Section 1 row list as the authoritative function universe. Every `D-243-C###` row whose Symbol Kind is `func` or `modifier` gets a matching `D-243-F###` classification row in Section 2.

Parallelism: wave 2 alongside 243-03. No cross-dependency between 243-02 and 243-03 — each reads 243-01's committed output independently and writes non-overlapping sections of the same file. Ownership: 243-02 owns Section 2 body + Section 1 change-count-card lines + Section 7.2 body. 243-03 owns Section 3 body + Section 6 body + Section 7.3 body + the final READ-only lock note. Section 1 row-data / 4 / 5 / 7.1 are READ-ONLY for 243-02 per CONTEXT.md D-21.

CRITICAL — concurrent file-edit safety: 243-02 and 243-03 both edit `audit/v31-243-DELTA-SURFACE.md`. The orchestrator serializes them or runs them sequentially. If 243-02 runs first, it MUST leave all 243-03-reserved markers intact (`RESERVED FOR 243-03` in Section 3 / 6 / 7.3). If 243-03 runs first, it MUST leave all 243-02-reserved markers intact (`RESERVED FOR 243-02` in Section 2 / 7.2). Task 3 of each plan re-verifies the other plan's markers are intact before committing.

Finding-ID emission: NO `F-31-NN` IDs per CONTEXT.md D-20. Finding candidates surfaced during classification append to Section 1.6 (preserving 243-01's entries).

Scope: READ-only per CONTEXT.md D-22. Zero `contracts/` or `test/` writes. Only writes: `audit/v31-243-DELTA-SURFACE.md` (in-place append to Sections 2 + 7.2 + modification of Section 1 change-count-card lines).

Baseline + HEAD anchors (7ab515fe / 771893d1) frozen in frontmatter per CONTEXT.md D-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/243-delta-extraction-per-commit-classification/243-CONTEXT.md
@.planning/milestones/v30.0-phases/237-vrf-consumer-inventory-call-graph/237-02-PLAN.md
@audit/v31-243-DELTA-SURFACE.md
</context>

<threat_model>
**Scope:** READ-only audit catalog production. Zero runtime code modified, zero test modified. No attack surface added.

**Writes confined to:** `audit/v31-243-DELTA-SURFACE.md` only (in-place append to Sections 2 + 7.2; edit Section 1 change-count-card lines; Section 1.6 append-only for Finding Candidates). Zero companion files.

**Threat surface introduced:** NONE (no contract code, no runtime behavior change). No STRIDE category applies.

**Regression / accuracy risk:**
- Wrong classification verdict propagates into Phase 244's per-commit adversarial work. Mitigated by CONTEXT.md D-05 11 pre-locked borderline verdicts + D-19 evidence burden (hunk citation + named source element in every rationale) + Phase 244 scope-guard deferral rule catching verdict-disagreement before it reaches F-31-NN finding blocks.
- Concurrent file edit with 243-03 could race if both plans are wrongly run in parallel on the same working tree. Mitigated by orchestrator serialization + Task 3's explicit check that 243-03-reserved markers remain intact before commit.

Inherits parent phase threat model (READ-only milestone).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Read Section 1 function universe from audit/v31-243-DELTA-SURFACE.md and verify D-05 pre-locked verdicts are addressable</name>
  <files>audit/v31-243-DELTA-SURFACE.md</files>
  <read_first>
    - audit/v31-243-DELTA-SURFACE.md (committed from 243-01) — specifically Section 0.2 Row-ID Legend (confirms D-243-F### prefix ownership); Section 0.3 Commit Inventory (confirms 5-commit list unchanged); Section 1 all subsections (source of every `func`/`modifier` Row ID to classify); Section 4 (context for non-`func` rows that will NOT get classification rows — only `func`/`modifier` rows are scoped)
    - .planning/phases/243-delta-extraction-per-commit-classification/243-CONTEXT.md — specifically D-04 (5-bucket taxonomy definitions); D-05 (11 pre-locked borderline verdicts); D-06 (hunk citation format); D-08 (Section 2 column spec); D-19 (classification evidence burden); D-20 (no F-31-NN); D-21 (READ-only after 243-03 commits — Section 1 / 4 / 5 / 7.1 already READ-only for this plan); D-22 (no contracts/ or test/ writes)
    - .planning/REQUIREMENTS.md §EVT / §RNG / §QST / §GOX — narrative context for function-behavior semantics (but per CONTEXT.md D-02, fresh `git show` is the authoritative source — narrative is CONTEXT only, never replacement)
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD 771893d1 — source of truth for `_unlockRng`, `_distributeEthGameOver` (or equivalent two-call-split continuation fn), `_livenessTriggered`, `_gameOverEntropy`, `_handleGameOverPath` verdicts
    - contracts/modules/DegenerusGameGameOverModule.sol at HEAD 771893d1 — source of truth for `handleGameOverDrain` verdict
    - contracts/modules/DegenerusGameJackpotModule.sol at HEAD 771893d1 — source of truth for `_jackpotTicketRoll`, `_awardJackpotTickets` BAF / Whale-pass verdicts
    - contracts/modules/DegenerusGameMintModule.sol at HEAD 771893d1 — source of truth for `_callTicketPurchase`, `handlePurchase`, and the gameOver→_livenessTriggered gate-swap path verdicts
    - contracts/modules/DegenerusGameWhaleModule.sol at HEAD 771893d1 — source of truth for remaining gameOver→_livenessTriggered gate-swap path verdicts
    - contracts/StakedDegenerusStonk.sol at HEAD 771893d1 — source of truth for `burn` / `burnWrapped` State-1 block verdict
    - contracts/DegenerusGame.sol at HEAD 771893d1 — context for any top-level function changed by 771893d1
    - contracts/DegenerusQuests.sol at HEAD 771893d1 — source of truth for the MINT_ETH quest gross-spend verdict
    - Baseline-side reads via git show:
      - git show 7ab515fe:contracts/modules/DegenerusGameAdvanceModule.sol (for function-existed-at-baseline test — NEW vs MODIFIED_LOGIC differentiator)
      - git show 7ab515fe:contracts/modules/DegenerusGameGameOverModule.sol
      - git show 7ab515fe:contracts/modules/DegenerusGameJackpotModule.sol
      - git show 7ab515fe:contracts/modules/DegenerusGameMintModule.sol
      - git show 7ab515fe:contracts/modules/DegenerusGameWhaleModule.sol
      - git show 7ab515fe:contracts/StakedDegenerusStonk.sol
      - git show 7ab515fe:contracts/DegenerusGame.sol
      - git show 7ab515fe:contracts/DegenerusQuests.sol
  </read_first>
  <action>
Prepare the Section 2 classification input set by reading 243-01's output and verifying the D-05 pre-locked verdicts are applicable.

**Step A — Verify input prerequisites:**

```
test -f audit/v31-243-DELTA-SURFACE.md
git log --oneline -1 -- audit/v31-243-DELTA-SURFACE.md   # MUST show a 243-01 commit
git rev-parse 7ab515fe
git rev-parse 771893d1
git diff --stat 7ab515fe..771893d1 -- contracts/
git status --porcelain contracts/ test/   # MUST be empty
```

If the 243-01 commit is missing OR baseline drifted, STOP — record a scope-guard deferral and alert the orchestrator.

**Step B — Verify 243-01's Section 2 / 3 / 6 reserved markers are intact:**

```
grep -c '^## Section 2 — Aggregate Function Classification (RESERVED FOR 243-02)$' audit/v31-243-DELTA-SURFACE.md   # MUST return 1
grep -c '^## Section 3 — Downstream Call-Site Catalog (RESERVED FOR 243-03)$' audit/v31-243-DELTA-SURFACE.md          # MUST return 1
grep -c '^## Section 6 — Consumer Index (RESERVED FOR 243-03)$' audit/v31-243-DELTA-SURFACE.md                        # MUST return 1
grep -c '^### 7.2 Plan 243-02 commands (DELTA-02 classification) — RESERVED FOR 243-02$' audit/v31-243-DELTA-SURFACE.md  # MUST return 1
grep -c '^### 7.3 Plan 243-03 commands (DELTA-03 call-site catalog) — RESERVED FOR 243-03$' audit/v31-243-DELTA-SURFACE.md  # MUST return 1
```

If any count is not 1, STOP — the file is in an unexpected state (either 243-03 already ran or 243-01 produced an incorrect shell). Record a deferral.

**Step C — Extract every Section 1 `func`/`modifier` Row ID:**

Parse Section 1 (between literal headers `## Section 1 — Per-Commit Changelog` and `## Section 2`). For each row whose `Symbol Kind` column equals `func` or `modifier`, record a tuple:

```
{
  row_id: "D-243-C0NN",
  commit_sha: "ced654df | 16597cac | 6b3f4f3c | 771893d1",
  file_line_range: "contracts/<path>:<start>-<end>",
  symbol_name: "<fn-name>",
  change_type: "ADDED | REMOVED | MODIFIED | SIGNATURE-CHANGED",
  semantic_note: "<note>"
}
```

Scoping rule:
- Section 1.5 (ffced9ef NO_CHANGE docs-only) contributes ZERO rows to the classification set (Symbol Kind = `none (docs-only)`, not `func`/`modifier`).
- Section 4 rows (state / event / interface method / error) are NOT classified in Section 2 — only `func`/`modifier` rows. (Interface-method row changes ARE classified — see below? No: interface methods are in Section 4 with Symbol Kind = `interface-method`, and CONTEXT.md D-04 taxonomy applies to "every changed function." Interpretation: interface methods are not "functions" in the body-behavior sense — they are signatures. A signature change alone cannot be MODIFIED_LOGIC vs REFACTOR_ONLY because there's no body to evaluate. Skip interface methods from Section 2 — they remain in Section 4 with `SIGNATURE-CHANGED` / `ADDED` Change Type, which is sufficient for DELTA-02 completeness. If the executor reads CONTEXT.md and disagrees, surface in Section 1.6 as a finding candidate and classify them anyway.)

Expected classification set size: the union of function changes across the 4 code-touching commits. The 11 D-05 pre-locked verdicts define 11 concrete functions that MUST appear in the set. If the set size is less than 11, the executor has a 243-01 completeness gap — surface in Section 1.6 as a finding candidate with rationale "D-05 names N functions but Section 1 lists M<N; possible 243-01 enumeration gap."

**Step D — Pre-build the D-05 pre-locked verdict table:**

Cross-reference each extracted function tuple against CONTEXT.md D-05's 11 borderline verdicts. Record per-function the target classification verdict:

| CONTEXT.md D-05 ID | Symbol Name | Commit SHA | Pre-Locked Verdict | Source Element Driving Verdict |
|---|---|---|---|---|
| D-05.1 | `_distributeEthGameOver` (or two-call-split continuation fn — verify name at HEAD) | 16597cac | MODIFIED_LOGIC | Removal of `_unlockRng(day)` call (side-effect removal) |
| D-05.2 | `<reformat-only fn>` (identify by scanning 16597cac hunk for lines that are only whitespace/line-split changes in an SLOAD or tuple destructuring context) | 16597cac | REFACTOR_ONLY | Multi-line SLOAD + tuple destructuring split (execution-trace byte-equivalent per commit msg) |
| D-05.3 | `_callTicketPurchase` | 6b3f4f3c | MODIFIED_LOGIC | Dropped `freshEth` return — return-path evaluation changed for every caller |
| D-05.4a | `handlePurchase` (parameter rename hunk only — if Section 1 split this into 2 rows OR annotated both aspects, use the rename row here) | 6b3f4f3c | REFACTOR_ONLY | Parameter rename `ethFreshWei → ethMintSpendWei` |
| D-05.4b | `handlePurchase` (value-semantics row — if Section 1 emitted as a single row, the classification is MODIFIED_LOGIC and the rationale names the semantics change) | 6b3f4f3c | MODIFIED_LOGIC | Value passed shifts from fresh-only spend to gross spend (semantics change downstream) |
| D-05.5 | `_jackpotTicketRoll` | ced654df | MODIFIED_LOGIC | New `emit JackpotTicketWin` at BAF site |
| D-05.6 | `_awardJackpotTickets` | ced654df | MODIFIED_LOGIC | New `emit JackpotWhalePassWin` at Whale-pass fallback site |
| D-05.7a...h | 8 functions in MintModule + WhaleModule (enumerate from Section 1 `func` rows whose commit_sha=771893d1 and file ∈ {MintModule, WhaleModule} AND semantic_note mentions `gameOver → _livenessTriggered` gate swap) | 771893d1 | MODIFIED_LOGIC per-function | Guard-condition change (control-flow) |
| D-05.8a | `burn` (StakedDegenerusStonk.sol) | 771893d1 | MODIFIED_LOGIC | New State-1 revert path (control-flow) |
| D-05.8b | `burnWrapped` (StakedDegenerusStonk.sol) | 771893d1 | MODIFIED_LOGIC | New State-1 revert path (control-flow) |
| D-05.9 | `handleGameOverDrain` | 771893d1 | MODIFIED_LOGIC | New arithmetic subtraction of `pendingRedemptionEthValue` pre-split |
| D-05.10 | `_livenessTriggered` | 771893d1 | MODIFIED_LOGIC | New 14-day grace branch + day-math-first ordering swap |
| D-05.11a | `_gameOverEntropy` | 771893d1 | MODIFIED_LOGIC | New SSTORE clearing `rngRequestTime` on fallback commit |
| D-05.11b | `_handleGameOverPath` | 771893d1 | MODIFIED_LOGIC | Control-flow reorder (gameOver before liveness) |

Handling for D-05.2 (reformat-only function identification): CONTEXT.md does not name the specific function for 16597cac's reformat hunk — the executor identifies it by running `git show 16597cac -- contracts/modules/DegenerusGameAdvanceModule.sol` and locating the hunk whose only changes are line-wrapping / tuple-destructuring of an SLOAD expression, with no SSTORE / external-call / branch / emit / return changes. If this function is the SAME as D-05.1 (i.e., the `_unlockRng(day)` removal and the reformat landed in the same function), then D-05.2 collapses into a single MODIFIED_LOGIC row for that function (removal is the execution-trace-changing element; reformat is subordinate). The One-Line Rationale for such a merged row must mention BOTH elements: "removed `_unlockRng(day)` call + multi-line SLOAD reformat (the removal drives the MODIFIED_LOGIC verdict; reformat alone would be REFACTOR_ONLY)."

For every other function in the Section 1 set NOT matching D-05, derive the verdict from fresh `git show` hunk inspection per D-04 rules.

This Task 1 is a READ-only preparation step — no file writes. Output is a mental / scratch-file verdict map that Task 2 consumes. No commit.
  </action>
  <verify>
    <automated>test -f audit/v31-243-DELTA-SURFACE.md && git log --oneline -1 -- audit/v31-243-DELTA-SURFACE.md | grep -qE '243-01' && [ "$(grep -c '^## Section 2 — Aggregate Function Classification (RESERVED FOR 243-02)$' audit/v31-243-DELTA-SURFACE.md)" = "1" ] && [ "$(grep -c '^## Section 3 — Downstream Call-Site Catalog (RESERVED FOR 243-03)$' audit/v31-243-DELTA-SURFACE.md)" = "1" ] && [ "$(grep -c '^## Section 6 — Consumer Index (RESERVED FOR 243-03)$' audit/v31-243-DELTA-SURFACE.md)" = "1" ] && [ "$(grep -c '^### 7.2 Plan 243-02 commands' audit/v31-243-DELTA-SURFACE.md)" = "1" ] && [ "$(grep -c '^### 7.3 Plan 243-03 commands' audit/v31-243-DELTA-SURFACE.md)" = "1" ] && [ "$(grep -c '^| D-243-C' audit/v31-243-DELTA-SURFACE.md)" -ge 15 ] && test -z "$(git status --porcelain contracts/ test/)"</automated>
  </verify>
  <acceptance_criteria>
    - `audit/v31-243-DELTA-SURFACE.md` exists and shows a 243-01 commit in its git log
    - Section 2 / 3 / 6 reserved markers present exactly once each
    - §7.2 / §7.3 reserved markers present exactly once each
    - Section 1 contains at least 15 `D-243-C` rows (preserved from 243-01)
    - `git status --porcelain contracts/ test/` empty (no writes in this task)
  </acceptance_criteria>
  <deliverable>
Input validation complete: 243-01's output is committed and intact; reserved markers for Section 2 / 7.2 (243-02's own) and Section 3 / 6 / 7.3 (243-03's territory) present; baseline anchors valid; function universe extracted and D-05 verdict map pre-built (in-memory — no file written).
  </deliverable>
  <done>Input integrity verified; function universe extracted from Section 1; D-05 pre-locked verdicts mapped to Section 1 row IDs; baseline/HEAD anchors valid; zero file writes.</done>
</task>

<task type="auto">
  <name>Task 2: Write Section 2 classification table + update Section 1 change-count cards + append §7.2 reproduction recipe — in place to audit/v31-243-DELTA-SURFACE.md</name>
  <files>audit/v31-243-DELTA-SURFACE.md</files>
  <read_first>
    - audit/v31-243-DELTA-SURFACE.md (entire file — Task 1's verdict map references Row IDs from Section 1 here)
    - .planning/phases/243-delta-extraction-per-commit-classification/243-CONTEXT.md (D-04 5-bucket rules; D-05 11 pre-locked verdicts; D-06 hunk citation format; D-08 Section 2 column spec; D-19 evidence burden; D-20 no F-31-NN; D-21 READ-only-after-243-03; D-22 no contracts/ or test/ writes)
    - .planning/milestones/v30.0-phases/237-vrf-consumer-inventory-call-graph/237-02-PLAN.md — 237-02 classification-plan precedent (1:1 classification row per universe row; non-editing of upstream file; at-least-one-row-per-family rule adapted here to at-least-one-row-per-verdict-bucket-used)
    - contracts/modules/DegenerusGameAdvanceModule.sol + DegenerusGameGameOverModule.sol + DegenerusGameJackpotModule.sol + DegenerusGameMintModule.sol + DegenerusGameWhaleModule.sol + StakedDegenerusStonk.sol + DegenerusGame.sol + DegenerusQuests.sol at HEAD 771893d1 — any function-body fresh read needed to resolve non-D-05 verdicts (e.g., the 8 gate-swap paths in MintModule/WhaleModule require reading each path's hunk to confirm "guard-condition change" is the sole / dominant source element)
    - git show outputs captured in Task 1 preparation (per-commit diffs for ced654df / 16597cac / 6b3f4f3c / 771893d1)
  </read_first>
  <action>
Execute the DELTA-02 classification pass.

**Step A — Replace Section 2 header + populate body:**

Locate the line `## Section 2 — Aggregate Function Classification (RESERVED FOR 243-02)` and replace the entire section body (from that header down to the next `## Section 3` header, exclusive) with the populated table:

```markdown
## Section 2 — Aggregate Function Classification

Per CONTEXT.md D-04: every `func` or `modifier` row from Section 1 classified into exactly one of the 5 buckets {NEW, MODIFIED_LOGIC, REFACTOR_ONLY, DELETED, RENAMED}. Burden of proof is on REFACTOR_ONLY per D-04 — any doubt escalates to MODIFIED_LOGIC.

Per CONTEXT.md D-06: every row cites the exact diff hunk at HEAD (or baseline for DELETED, or old→new mapping for RENAMED).

Per CONTEXT.md D-19: every MODIFIED_LOGIC / REFACTOR_ONLY row names the specific execution-trace-changing element (SSTORE / external call / branch / emit / return-path) OR the specific non-execution-changing element (whitespace / rename / multi-line split / local-variable-name / NatSpec). Hand-wave rationales are forbidden.

Section 2 Row IDs use prefix `D-243-F###` (monotonic from F001 — no gaps; no overlap with Section 1's `D-243-C###` numbering).

### 2.1 5-Bucket Taxonomy (D-04 rules)

| Classification | Definition |
|---|---|
| `NEW` | Function did not exist at baseline `7ab515fe`; appears at HEAD `771893d1` with a body. |
| `MODIFIED_LOGIC` | Function existed at baseline and HEAD; any state write, external call, control-flow branch, emitted event, or return-path evaluation changed. Removal or addition of a side-effect call is MODIFIED_LOGIC. |
| `REFACTOR_ONLY` | Function existed at baseline and HEAD; source-level shape changed (whitespace, parens, local variable names, multi-line decomposition, tuple destructuring, NatSpec, parameter rename) but execution trace is byte-equivalent. |
| `DELETED` | Function existed at baseline; absent at HEAD. |
| `RENAMED` | Same signature body at HEAD under a different name (callee-side classification — caller hunks that only see the rename are REFACTOR_ONLY on the caller). |

### 2.2 D-05 Pre-Locked Verdicts Applied

Per CONTEXT.md D-05, the following named functions received pre-locked verdicts at phase-context time. This section applies those verdicts verbatim; any executor deviation from D-05 must be surfaced in Section 1.6 Finding Candidates with a ≥ 2-line rationale citing source evidence that contradicts D-05.

| D-05 ID | Symbol | Commit | Pre-Locked Verdict | Applied In Row |
|---|---|---|---|---|
| D-05.1 | `_distributeEthGameOver` (or the 16597cac two-call-split continuation fn) | 16597cac | MODIFIED_LOGIC | D-243-F0NN |
| D-05.2 | (same fn as D-05.1 if reformat landed in same fn — collapsed) OR separate reformat-only fn | 16597cac | REFACTOR_ONLY (or collapsed into D-05.1's MODIFIED_LOGIC rationale) | D-243-F0NN (collapsed) or D-243-F0NN+1 |
| D-05.3 | `_callTicketPurchase` | 6b3f4f3c | MODIFIED_LOGIC | D-243-F0NN |
| D-05.4a | `handlePurchase` (rename hunk) | 6b3f4f3c | REFACTOR_ONLY | D-243-F0NN |
| D-05.4b | `handlePurchase` (value-semantics) | 6b3f4f3c | MODIFIED_LOGIC | D-243-F0NN |
| D-05.5 | `_jackpotTicketRoll` | ced654df | MODIFIED_LOGIC | D-243-F0NN |
| D-05.6 | `_awardJackpotTickets` | ced654df | MODIFIED_LOGIC | D-243-F0NN |
| D-05.7 (8 paths) | MintModule + WhaleModule `gameOver → _livenessTriggered` gate-swap functions | 771893d1 | MODIFIED_LOGIC (×8) | D-243-F0NN..F0NN+7 |
| D-05.8 | `sDGNRS.burn` | 771893d1 | MODIFIED_LOGIC | D-243-F0NN |
| D-05.8b | `sDGNRS.burnWrapped` | 771893d1 | MODIFIED_LOGIC | D-243-F0NN |
| D-05.9 | `handleGameOverDrain` | 771893d1 | MODIFIED_LOGIC | D-243-F0NN |
| D-05.10 | `_livenessTriggered` | 771893d1 | MODIFIED_LOGIC | D-243-F0NN |
| D-05.11 | `_gameOverEntropy` | 771893d1 | MODIFIED_LOGIC | D-243-F0NN |
| D-05.11b | `_handleGameOverPath` | 771893d1 | MODIFIED_LOGIC | D-243-F0NN |

Replace every `D-243-F0NN` token with a real Row ID when writing the table below.

### 2.3 Classification Table

| Row ID | Section 1 Row (D-243-C###) | Function Signature | Commit | File:Line (at HEAD) | Classification | Hunk Ref | One-Line Rationale |
|---|---|---|---|---|---|---|---|
| D-243-F001 | D-243-C0NN | `<full solidity signature — visibility + return type>` | <sha> | contracts/<path>:<head-line-range> | <NEW/MODIFIED_LOGIC/REFACTOR_ONLY/DELETED/RENAMED> | contracts/<path>:<head-line-range> OR contracts/<path>:<baseline-line-range>@7ab515fe (for DELETED) OR <old-name>→<new-name>@contracts/<path>:<head-range> (for RENAMED) | <one-line rationale naming SPECIFIC source element — SSTORE line, emit line, branch line, return-path line, parameter rename, whitespace-only, etc.> |
| D-243-F002 | ... | ... | ... | ... | ... | ... | ... |

[Every `func`/`modifier` row from Section 1 gets a classification row. Row count in this table EQUALS the count of Section 1 `func`/`modifier` rows exactly.]

### 2.4 Verdict-Bucket Summary

| Classification | Row Count | Row IDs |
|---|---|---|
| NEW | N | D-243-F0NN, D-243-F0NN+1, … |
| MODIFIED_LOGIC | N | … (comma-separated) |
| REFACTOR_ONLY | N | … |
| DELETED | N | … (may be 0 — all 12 files are M-status per baseline verify) |
| RENAMED | N | … |
| **Total** | **N** | (= Section 1 `func`/`modifier` row count) |

**Sanity check:** the Total here equals the sum of Section 1's change-count-card `functions:` field across all 5 commit subsections.

### 2.5 Deviations From D-05 Pre-Locked Verdicts

If the executor concluded that any D-05 pre-locked verdict is wrong at HEAD `771893d1` (e.g., D-05.2 reformat-only hunk actually contains a subtle SSTORE-reorder that changes execution trace), record the deviation here:

| D-05 ID | Pre-Locked | Executor Verdict | Evidence | Finding Candidate Surface Ref |
|---|---|---|---|---|
| D-05.N | <pre-locked> | <executor-verdict> | <hunk + named source element> | Section 1.6 bullet N |

If no deviations, write literally "Zero deviations. All 11 D-05 pre-locked verdicts confirmed at HEAD `771893d1`."
```

Replace every `<…>` placeholder with concrete data. No placeholders may remain in the committed file.

**Step B — Update the Section 1 change-count cards:**

For each of Sections 1.1 / 1.2 / 1.3 / 1.4 (Section 1.5 `ffced9ef` docs-only has `functions: 0` already — unchanged), find the literal change-count-card line `**Change count card:** functions: N / state-vars: N / events: N / interfaces: N / errors: N / call-sites-changed: TBD-243-03.` and replace the `functions: N` portion with a breakdown in the format `functions: N (NEW: x / MODIFIED_LOGIC: y / REFACTOR_ONLY: z / DELETED: d / RENAMED: r)`.

Example after update:
```
**Change count card:** functions: 5 (NEW: 1 / MODIFIED_LOGIC: 3 / REFACTOR_ONLY: 1 / DELETED: 0 / RENAMED: 0) / state-vars: 0 / events: 1 / interfaces: 0 / errors: 0 / call-sites-changed: TBD-243-03.
```

CRITICAL: do NOT touch any row in Section 1's tables. Only the change-count-card line is edited (and only its `functions:` field is expanded — all other fields remain byte-identical). This preserves D-21 READ-only discipline on Section 1 row data.

**Step C — Replace §7.2 reserved header + populate body:**

Locate the line `### 7.2 Plan 243-02 commands (DELTA-02 classification) — RESERVED FOR 243-02` and replace the section body (from that header down to the next `### 7.3` header, exclusive) with:

```markdown
### 7.2 Plan 243-02 commands (DELTA-02 classification)

**Per-function hunk citation (D-06 format):**

```bash
# For a specific classification row, re-derive the hunk at HEAD:
git show 771893d1:contracts/modules/DegenerusGameAdvanceModule.sol | sed -n '<start>,<end>p'

# For the baseline-side body (to confirm a function existed at baseline):
git show 7ab515fe:contracts/modules/DegenerusGameAdvanceModule.sol | sed -n '<baseline-start>,<baseline-end>p'

# For MODIFIED_LOGIC rows, inspect the specific hunk in the per-commit diff:
git show <sha> -- contracts/modules/<file>.sol | sed -n '/<fn-header>/,/<closing brace>/p'
```

**REFACTOR_ONLY verification pattern (execution-trace byte-equivalence):**

```bash
# Extract the function body at baseline and head; compare after stripping whitespace:
git show 7ab515fe:contracts/modules/<file>.sol | awk '/^[[:space:]]*function <fn>/,/^[[:space:]]*\}$/' | tr -d '[:space:]' > /tmp/baseline-body.txt
git show 771893d1:contracts/modules/<file>.sol | awk '/^[[:space:]]*function <fn>/,/^[[:space:]]*\}$/' | tr -d '[:space:]' > /tmp/head-body.txt
diff /tmp/baseline-body.txt /tmp/head-body.txt
# If diff is empty, the function is whitespace-equivalent → REFACTOR_ONLY holds.
# If diff shows a token difference (e.g., renamed parameter), classification still REFACTOR_ONLY
# but rationale must explicitly name the rename. If diff shows a token difference that
# corresponds to a call / emit / SSTORE / branch / return-path, classification escalates
# to MODIFIED_LOGIC.
```

**RENAMED detection pattern:**

```bash
# Look for signature bodies that vanished at baseline but appear under a new name at head:
git show 7ab515fe:contracts/<path>.sol | grep -E '^[[:space:]]*function [a-zA-Z_]+\(' | sort > /tmp/baseline-fn-headers.txt
git show 771893d1:contracts/<path>.sol | grep -E '^[[:space:]]*function [a-zA-Z_]+\(' | sort > /tmp/head-fn-headers.txt
# Functions in baseline-fn-headers.txt but not head-fn-headers.txt are DELETED candidates.
# Functions in head-fn-headers.txt but not baseline-fn-headers.txt are NEW candidates.
# Among those pairs, if a DELETED candidate's body at baseline matches a NEW candidate's
# body at head (via the REFACTOR_ONLY verification pattern above), classify both as
# RENAMED (with rationale naming both names).
```

**`git log -S` for RENAMED corroboration (finds the commit that moved the body):**

```bash
# Find the commit that introduced a specific body token:
git log -S'<distinctive-body-token>' --oneline 7ab515fe..771893d1 -- contracts/
```

All commands are portable POSIX (sed / awk / grep / diff / sort). GNU-only flags (`grep -P`, `sed -i`, etc.) are not used.
```

Replace every `<…>` placeholder with the per-row concrete values the executor used. If fewer than the template commands were needed (e.g., no RENAMED candidates surfaced), keep the generic patterns as the reproducibility recipe but annotate with "(not applied in this phase — no RENAMED rows)".

**Step D — Append any Finding Candidates to Section 1.6:**

If the classification pass surfaced new finding candidates (e.g., a function whose classification could go either way depending on whether a NatSpec change counts as a "return-path annotation"), find Section 1.6 `### 1.6 Finding Candidates (fresh-eyes)` and append new bullet items after 243-01's existing entries (preserving their order and text byte-identically). If 243-01 wrote "None surfaced during fresh enumeration." and 243-02 has candidates, replace that literal with an actual bullet list of 243-02's candidates (because "None surfaced" is no longer accurate — the entry must reflect ALL candidates across 243-01 + 243-02 + eventually 243-03). Each bullet format: `- contracts/<path>:<line> — <symbol> — <rationale> — suggested severity: <INFO|LOW|MEDIUM|HIGH> — surfaced in 243-02`.

If no new candidates, leave Section 1.6 untouched.

After writing, verify:
```
grep -q '^## Section 2 — Aggregate Function Classification$' audit/v31-243-DELTA-SURFACE.md
! grep -q 'RESERVED FOR 243-02' audit/v31-243-DELTA-SURFACE.md
grep -q '^### 2.3 Classification Table' audit/v31-243-DELTA-SURFACE.md
grep -c '^| D-243-F' audit/v31-243-DELTA-SURFACE.md   # ≥ 11 (D-05 minimum); likely higher
# Check classification vocabulary appears at least once each (or the bucket is zero — verified via Summary):
grep -qE '\| (NEW|MODIFIED_LOGIC|REFACTOR_ONLY|DELETED|RENAMED) \|' audit/v31-243-DELTA-SURFACE.md
# Verify Section 3 / 6 / 7.3 markers still intact:
grep -q '^## Section 3 — Downstream Call-Site Catalog (RESERVED FOR 243-03)$' audit/v31-243-DELTA-SURFACE.md
grep -q '^## Section 6 — Consumer Index (RESERVED FOR 243-03)$' audit/v31-243-DELTA-SURFACE.md
grep -q '^### 7.3 Plan 243-03 commands (DELTA-03 call-site catalog) — RESERVED FOR 243-03$' audit/v31-243-DELTA-SURFACE.md
git status --porcelain contracts/ test/   # MUST be empty
```

Do NOT commit in this task — Task 3 commits the file.
  </action>
  <verify>
    <automated>grep -q '^## Section 2 — Aggregate Function Classification$' audit/v31-243-DELTA-SURFACE.md && ! grep -q 'RESERVED FOR 243-02' audit/v31-243-DELTA-SURFACE.md && grep -q '^### 2.1 5-Bucket Taxonomy' audit/v31-243-DELTA-SURFACE.md && grep -q '^### 2.2 D-05 Pre-Locked Verdicts Applied' audit/v31-243-DELTA-SURFACE.md && grep -q '^### 2.3 Classification Table' audit/v31-243-DELTA-SURFACE.md && grep -q '^### 2.4 Verdict-Bucket Summary' audit/v31-243-DELTA-SURFACE.md && grep -q '^### 2.5 Deviations From D-05' audit/v31-243-DELTA-SURFACE.md && [ "$(grep -c '^| D-243-F' audit/v31-243-DELTA-SURFACE.md)" -ge 11 ] && grep -qE '\| MODIFIED_LOGIC \|' audit/v31-243-DELTA-SURFACE.md && grep -q '^### 7.2 Plan 243-02 commands \(DELTA-02 classification\)$' audit/v31-243-DELTA-SURFACE.md && grep -q '^## Section 3 — Downstream Call-Site Catalog (RESERVED FOR 243-03)$' audit/v31-243-DELTA-SURFACE.md && grep -q '^## Section 6 — Consumer Index (RESERVED FOR 243-03)$' audit/v31-243-DELTA-SURFACE.md && grep -q '^### 7.3 Plan 243-03 commands (DELTA-03 call-site catalog) — RESERVED FOR 243-03$' audit/v31-243-DELTA-SURFACE.md && ! grep -q 'F-31-' audit/v31-243-DELTA-SURFACE.md && ! grep -qE '<(fn|fn-name|path|line|start|end|head-line-range|baseline-line-range|sha|note|old-name|new-name)[-><]' audit/v31-243-DELTA-SURFACE.md && grep -qE 'functions: [0-9]+ \(NEW:' audit/v31-243-DELTA-SURFACE.md && test -z "$(git status --porcelain contracts/ test/)"</automated>
  </verify>
  <acceptance_criteria>
    - `audit/v31-243-DELTA-SURFACE.md` contains literal header `## Section 2 — Aggregate Function Classification` (WITHOUT the ` (RESERVED FOR 243-02)` suffix)
    - Section 2 contains subheaders: `### 2.1 5-Bucket Taxonomy`, `### 2.2 D-05 Pre-Locked Verdicts Applied`, `### 2.3 Classification Table`, `### 2.4 Verdict-Bucket Summary`, `### 2.5 Deviations From D-05 Pre-Locked Verdicts`
    - Classification table (§2.3) has ≥ 11 `D-243-F` rows (D-05 floor; realistically more — every `func`/`modifier` row from Section 1)
    - Classification vocabulary restricted to `{NEW, MODIFIED_LOGIC, REFACTOR_ONLY, DELETED, RENAMED}` in the Classification column — automated check asserts MODIFIED_LOGIC appears (expected majority bucket given D-05)
    - Every row has non-empty Hunk Ref column (regex `contracts/[^:]+:[0-9]+-[0-9]+` or RENAMED mapping)
    - Every row has non-empty One-Line Rationale
    - §7.2 literal header present: `### 7.2 Plan 243-02 commands (DELTA-02 classification)` (WITHOUT the ` — RESERVED FOR 243-02` suffix)
    - §7.2 body contains at least one `git show 771893d1:contracts/` command and at least one `git show 7ab515fe:contracts/` command
    - Section 1 change-count cards updated: `grep -qE 'functions: [0-9]+ \(NEW:' audit/v31-243-DELTA-SURFACE.md` matches (at least one commit subsection has the expanded breakdown)
    - Section 3 / 6 / 7.3 reserved markers INTACT (grep confirms the literal headers with `(RESERVED FOR 243-03)` or `— RESERVED FOR 243-03` still present — 243-02 must NOT touch them)
    - NO `F-31-NN` references anywhere in the file
    - NO placeholder tokens `<fn>`, `<fn-name>`, `<path>`, `<line>`, `<start>`, `<end>`, `<head-line-range>`, `<sha>`, `<note>`, `<old-name>`, `<new-name>`, `<NEW/MODIFIED_LOGIC/REFACTOR_ONLY/DELETED/RENAMED>`, `<pre-locked>`, `<executor-verdict>`, `D-243-F0NN` (all replaced with real Row IDs)
    - `git status --porcelain contracts/ test/` empty
  </acceptance_criteria>
  <deliverable>
Section 2 (Aggregate Function Classification) populated with the D-04 5-bucket table covering every `func`/`modifier` row from Section 1; D-05 11 pre-locked verdicts honored + §2.5 records any deviations; Section 1 change-count cards updated with per-commit classification breakdown; §7.2 populated with reproducibility shell recipes for classification verification. Sections 3 / 6 / 7.3 reserved markers intact.
  </deliverable>
  <done>Section 2 populated; §7.2 populated; Section 1 change-count cards reflect per-commit classification breakdown; Row count in §2.3 matches Section 1 func/modifier row count; 11 D-05 pre-locked verdicts either applied verbatim in §2.2 or explicitly deviation-logged in §2.5; zero F-31-NN IDs; zero contracts/ or test/ writes; Section 3 / 6 / 7.3 reserved markers preserved byte-identical.</done>
</task>

<task type="auto">
  <name>Task 3: Commit 243-02 updates to audit/v31-243-DELTA-SURFACE.md (Section 2 + Section 1 change-count cards + §7.2)</name>
  <files>audit/v31-243-DELTA-SURFACE.md</files>
  <read_first>
    - audit/v31-243-DELTA-SURFACE.md (Task 2 populated Section 2 + §7.2 + change-count cards — must exist and not yet be committed for THIS plan)
  </read_first>
  <action>
Final sanity gate before committing Plan 243-02 deliverable.

**Final sanity gate:**
```
test -f audit/v31-243-DELTA-SURFACE.md
! grep -q 'F-31-' audit/v31-243-DELTA-SURFACE.md
! grep -q 'RESERVED FOR 243-02' audit/v31-243-DELTA-SURFACE.md       # 243-02's own reserved markers replaced
# 243-03's reserved markers MUST still be present (not yet their turn):
grep -q '^## Section 3 — Downstream Call-Site Catalog (RESERVED FOR 243-03)$' audit/v31-243-DELTA-SURFACE.md
grep -q '^## Section 6 — Consumer Index (RESERVED FOR 243-03)$' audit/v31-243-DELTA-SURFACE.md
grep -q '^### 7.3 Plan 243-03 commands (DELTA-03 call-site catalog) — RESERVED FOR 243-03$' audit/v31-243-DELTA-SURFACE.md
# Section 2 + §7.2 populated:
grep -q '^## Section 2 — Aggregate Function Classification$' audit/v31-243-DELTA-SURFACE.md
grep -q '^### 2.3 Classification Table' audit/v31-243-DELTA-SURFACE.md
[ "$(grep -c '^| D-243-F' audit/v31-243-DELTA-SURFACE.md)" -ge 11 ]
grep -q '^### 7.2 Plan 243-02 commands (DELTA-02 classification)$' audit/v31-243-DELTA-SURFACE.md
# Section 1 change-count cards updated (at least one has the breakdown):
grep -qE 'functions: [0-9]+ \(NEW:' audit/v31-243-DELTA-SURFACE.md
# No contracts / test writes:
test -z "$(git status --porcelain contracts/ test/)"
# No other unexpected file changes:
git status --porcelain | grep -v '^.M audit/v31-243-DELTA-SURFACE.md$' | grep -v '^??$' | grep -v '^$' || echo "ONLY target file changed — good"
```

If any gate fails, STOP and diagnose before committing.

**Commit:**
```
git add audit/v31-243-DELTA-SURFACE.md
git commit -m "docs(243-02): DELTA-02 aggregate function classification at HEAD 771893d1

Plan 243-02 Wave 2 deliverable. Appends Section 2 in place per CONTEXT.md
D-04/D-05/D-06/D-19.

Section 2 populated:
  - 2.1 5-bucket taxonomy definitions (NEW/MODIFIED_LOGIC/REFACTOR_ONLY/
    DELETED/RENAMED) per D-04
  - 2.2 D-05 11 pre-locked verdicts applied (16597cac _unlockRng removal;
    16597cac reformat; 6b3f4f3c _callTicketPurchase/handlePurchase; ced654df
    _jackpotTicketRoll/_awardJackpotTickets emit adds; 771893d1 8 MintModule
    +WhaleModule gate swaps; 771893d1 sDGNRS.burn/burnWrapped State-1 block;
    771893d1 handleGameOverDrain pendingRedemptionEthValue subtraction;
    771893d1 _livenessTriggered/_gameOverEntropy/_handleGameOverPath)
  - 2.3 Full classification table (1:1 per Section 1 func/modifier row);
    every row cites hunk + names specific source element per D-19
  - 2.4 Verdict-bucket summary (count per classification)
  - 2.5 D-05 deviations (or 'zero deviations' attestation)

Section 1 change-count cards updated: each commit subsection's
'functions: N' field now carries the (NEW/MODIFIED_LOGIC/REFACTOR_ONLY/
DELETED/RENAMED) breakdown.

Section 7.2 (reproduction recipe appendix for 243-02) populated with
git show / awk / diff patterns for per-function hunk verification,
REFACTOR_ONLY byte-equivalence check, RENAMED detection, and git log -S
corroboration.

Sections 3 / 6 / 7.3 reserved markers INTACT (243-03 territory) —
verified before commit.

NO F-31-NN IDs (D-20). READ-only per D-22 — zero contracts/ or test/ writes.

Baseline anchor: 7ab515fe (D-03). Head anchor: 771893d1 (D-03)."
```

Do NOT push.
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -qE '243-02.*(DELTA-02|classification).*771893d1' && test -z "$(git status --porcelain contracts/ test/)" && ! grep -q 'RESERVED FOR 243-02' audit/v31-243-DELTA-SURFACE.md && grep -q '^## Section 3 — Downstream Call-Site Catalog (RESERVED FOR 243-03)$' audit/v31-243-DELTA-SURFACE.md && grep -q '^## Section 6 — Consumer Index (RESERVED FOR 243-03)$' audit/v31-243-DELTA-SURFACE.md && grep -q '^### 7.3 Plan 243-03 commands (DELTA-03 call-site catalog) — RESERVED FOR 243-03$' audit/v31-243-DELTA-SURFACE.md && ! grep -q 'F-31-' audit/v31-243-DELTA-SURFACE.md && [ "$(grep -c '^| D-243-F' audit/v31-243-DELTA-SURFACE.md)" -ge 11 ]</automated>
  </verify>
  <acceptance_criteria>
    - One new commit with subject containing `243-02` + (`DELTA-02` OR `classification`) + `771893d1`
    - Commit touches only `audit/v31-243-DELTA-SURFACE.md`
    - Commit body references CONTEXT.md decisions D-04, D-05, D-06, D-19, D-20, D-22
    - `git status --porcelain contracts/ test/` empty
    - Zero `RESERVED FOR 243-02` markers remaining (243-02's own reserved territory replaced)
    - All three `RESERVED FOR 243-03` markers (Section 3, Section 6, §7.3) STILL PRESENT (243-03 territory preserved)
    - Classification table has ≥ 11 `D-243-F` rows
    - Zero `F-31-NN` IDs
    - Not pushed
  </acceptance_criteria>
  <deliverable>
`audit/v31-243-DELTA-SURFACE.md` updated-and-committed Section 2 + §7.2 + Section 1 change-count cards. 243-02's DELTA-02 work integrated; 243-03 territory untouched and ready for Wave 2 parallel completion.
  </deliverable>
  <done>Commit exists; contracts/ test/ unmodified; Section 2 fully populated (1:1 per Section 1 func/modifier row); D-05 11 pre-locked verdicts applied or deviations logged; §7.2 reproducibility recipes present; Section 3 / 6 / 7.3 reserved markers intact for 243-03; zero F-31-NN IDs.</done>
</task>

</tasks>

<verification>
- `audit/v31-243-DELTA-SURFACE.md` Section 2 populated in place (literal header `## Section 2 — Aggregate Function Classification` without the `RESERVED` suffix)
- Section 2 subheaders 2.1-2.5 all present
- §2.3 Classification Table row count equals Section 1 `func`/`modifier` row count exactly (1:1 verified by grep count comparison)
- Every §2.3 row has Classification ∈ {NEW, MODIFIED_LOGIC, REFACTOR_ONLY, DELETED, RENAMED}
- Every §2.3 row has non-empty Hunk Ref (regex `contracts/[^:]+:[0-9]+-[0-9]+` or RENAMED mapping format)
- Every §2.3 row has non-empty One-Line Rationale naming a specific source element per D-19
- All 11 D-05 pre-locked verdicts applied verbatim in §2.2 with their Section 2 Row-ID references filled in — OR deviations logged in §2.5 with evidence + Section 1.6 finding-candidate cross-ref
- §2.4 Verdict-Bucket Summary row counts sum to the §2.3 total
- Section 1 change-count cards updated: each of 1.1-1.4 carries the `functions: N (NEW: x / MODIFIED_LOGIC: y / REFACTOR_ONLY: z / DELETED: d / RENAMED: r)` breakdown (Section 1.5 ffced9ef remains `functions: 0`)
- Section 1 row-data tables (D-243-C### rows) are byte-identical to 243-01's commit (only change-count-card lines modified)
- §7.2 populated with portable POSIX shell commands for per-function hunk verification, REFACTOR_ONLY byte-equivalence check, RENAMED detection
- Sections 3, 6, §7.3 reserved markers INTACT (preserved byte-identical for 243-03)
- Zero `F-31-NN` references (D-20)
- Zero placeholder tokens remaining
- Zero contracts/ or test/ writes (D-22)
- Baseline + HEAD anchors in frontmatter match CONTEXT.md D-01 (7ab515fe / 771893d1)
- Committed locally; not pushed
</verification>

<success_criteria>
DELTA-02 satisfied: every changed function in the v31.0 delta surface has exactly one classification verdict from the D-04 5-bucket taxonomy, with hunk citation per D-06 and rationale naming the specific source element per D-19. The 11 CONTEXT.md D-05 pre-locked borderline verdicts are honored verbatim or their deviations are explicitly logged with evidence. Phase 244 per-commit adversarial plans (EVT / RNG / QST / GOX) inherit a complete and unambiguous classification lookup — any row they cite, they know whether to scrutinize as logic-changing (MODIFIED_LOGIC/NEW/DELETED/RENAMED) or confidence-check as no-op (REFACTOR_ONLY). Section 1 change-count cards now carry the classification breakdown, giving downstream plans a one-line summary per commit. Plan 243-03 can run in parallel (same wave) or sequentially — either way, Section 3 / 6 / 7.3 territory is intact for its append. Baseline + HEAD anchors preserved; READ-only scope maintained.
</success_criteria>

<output>
After completion, create `.planning/phases/243-delta-extraction-per-commit-classification/243-02-SUMMARY.md`
</output>
</content>
