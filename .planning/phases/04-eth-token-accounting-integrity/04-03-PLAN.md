---
phase: 04-eth-token-accounting-integrity
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-eth-token-accounting-integrity/04-03-FINDINGS-bps-fee-splits.md
autonomous: true
requirements: [ACCT-02, ACCT-03]

must_haves:
  truths:
    - "Every BPS fee split across all modules sums to the original input amount -- no wei leaks"
    - "The 90/10 prize pool split uses subtraction-remainder pattern ensuring exact conservation"
    - "All multi-way splits (BAF, Decimator, Degenerette, whale bundle) use remainder pattern or sum-then-subtract"
    - "Rounding accumulation at 1-wei inputs does not create exploitable dust"
  artifacts:
    - path: ".planning/phases/04-eth-token-accounting-integrity/04-03-FINDINGS-bps-fee-splits.md"
      provides: "Complete BPS split audit across all modules"
      contains: "ACCT-02"
  key_links:
    - from: "MintModule purchase path"
      to: "90/10 split"
      via: "subtraction remainder: nextShare = prizeContribution - futureShare"
      pattern: "prizeContribution.*futureShare"
    - from: "EndgameModule BAF/Decimator"
      to: "multi-way BPS splits"
      via: "percentage-based pool distribution"
      pattern: "BPS\\|bps\\|10000"
---

<objective>
Audit every BPS-based fee split across all modules, expanding beyond Phase 3a's MintModule coverage to JackpotModule, EndgameModule, DegeneretteModule, WhaleModule, and AdvanceModule. For each split, prove sum(parts) == whole using the remainder pattern or document the gap.

Purpose: ACCT-02 (90/10 split) and ACCT-03 (all BPS splits sum to input) require cross-module verification. Phase 3a confirmed MintModule splits; this plan completes the audit.
Output: 04-03-FINDINGS-bps-fee-splits.md with per-split verification and ACCT-02/ACCT-03 verdicts.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-eth-token-accounting-integrity/04-RESEARCH.md

Key source files:
@contracts/modules/DegenerusGameJackpotModule.sol
@contracts/modules/DegenerusGameEndgameModule.sol
@contracts/modules/DegenerusGameDegeneretteModule.sol
@contracts/modules/DegenerusGameWhaleModule.sol
@contracts/modules/DegenerusGameAdvanceModule.sol
@contracts/modules/DegenerusGameMintModule.sol
@contracts/modules/DegenerusGameDecimatorModule.sol

Prior findings:
@.planning/phases/03a-core-eth-flow-modules/03a-01-SUMMARY.md (MintModule BPS splits confirmed PASS)
@.planning/phases/03a-core-eth-flow-modules/03a-02-SUMMARY.md (JackpotModule consolidatePrizePools wei-exact)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all BPS splits in JackpotModule, EndgameModule, and DegeneretteModule</name>
  <files>.planning/phases/04-eth-token-accounting-integrity/04-03-FINDINGS-bps-fee-splits.md</files>
  <action>
READ-ONLY audit. Do NOT modify any contract files.

Identify and trace every BPS-based split in these three modules. For each split:

1. **Locate the split**: Find all divisions by 10000, 10_000, BPS constants, or percentage-based pool distributions
2. **Identify the pattern**: Is it subtraction-remainder (safe) or independent computation (riskier)?
3. **Verify conservation**: Prove sum(parts) == whole, or quantify the maximum gap in wei
4. **Test at edge values**: Mentally evaluate at 1 wei, 2 wei, 3 wei, max uint256

**JackpotModule splits to trace:**
- Daily jackpot slice: 6-14% random slice from currentPrizePool (line ~868+)
- Level jackpot split from currentPrizePool
- Carryover pool handling
- consolidatePrizePools rebalancing (Phase 3a-02 confirmed wei-exact -- verify this holds)

**EndgameModule splits to trace:**
- BAF pool percentages: 10%/25%/20% tier distribution
- Decimator pool percentages: 10%/30%
- runRewardJackpots fund distribution
- _addClaimableEth auto-rebuy take-profit split (the `reserved` vs `ethSpent` vs `rebuyAmount` arithmetic)

**DegeneretteModule splits to trace:**
- 25/75 ETH/lootbox payout split for degenerette wins
- ETH pool cap per-spin (10% cap confirmed in Phase 3c)
- Any BPS applied to bet amounts or payout calculations

For each split, document:
| Module | Function | Split Description | Pattern | Conservation Proof | Edge Case (1 wei) | Verdict |
  </action>
  <verify>
    <automated>grep -c "Verdict" .planning/phases/04-eth-token-accounting-integrity/04-03-FINDINGS-bps-fee-splits.md | grep -v "^0$"</automated>
  </verify>
  <done>All BPS splits in JackpotModule, EndgameModule, and DegeneretteModule traced with conservation proofs and edge-case analysis</done>
</task>

<task type="auto">
  <name>Task 2: Audit BPS splits in WhaleModule and AdvanceModule; write ACCT-02/ACCT-03 verdicts</name>
  <files>.planning/phases/04-eth-token-accounting-integrity/04-03-FINDINGS-bps-fee-splits.md</files>
  <action>
READ-ONLY audit. Do NOT modify any contract files.

Continue the BPS split audit for remaining modules:

**WhaleModule splits to trace:**
- Bundle fund distribution BPS (50% next/future + 50% future/reward at various levels)
- Deity pass fund routing (expected: 100% to futurePrizePool)
- Lazy pass pricing sum (expected: exact sum, no BPS split)
- Any percentage-based distribution of whale/lazy/deity purchase funds

**AdvanceModule splits to trace:**
- Time-based future take BPS (_processPhaseTransition)
- Draw-down percentages during level transitions
- _autoStakeExcessEth: ethBal - claimablePool (not a BPS split but verify no rounding issue)

**Cross-reference with Phase 3a findings:**
- MintModule 90/10 split: confirmed PASS by 03a-01. Re-verify the same `futureShare = (prizeContribution * FUTURE_POOL_BPS) / 10000; nextShare = prizeContribution - futureShare` pattern holds.
- Note any MODULE that does NOT use the remainder pattern

**Write summary table combining all modules:**

| Module | # Splits Found | All Use Remainder Pattern? | Max Gap (wei) | Verdict |
|--------|---------------|---------------------------|---------------|---------|
| MintModule | (from 3a-01) | Yes | 0 | PASS |
| JackpotModule | N | ... | ... | ... |
| EndgameModule | N | ... | ... | ... |
| DegeneretteModule | N | ... | ... | ... |
| WhaleModule | N | ... | ... | ... |
| AdvanceModule | N | ... | ... | ... |

**Write requirement verdicts:**
- ACCT-02: Does the 90/10 prize pool split sum correctly with no wei leak?
- ACCT-03: Do ALL BPS splits across ALL modules sum to input?

If any split uses independent computation (not remainder), flag it and compute the maximum possible gap.
  </action>
  <verify>
    <automated>grep -c "ACCT-02\|ACCT-03" .planning/phases/04-eth-token-accounting-integrity/04-03-FINDINGS-bps-fee-splits.md | grep -v "^0$"</automated>
  </verify>
  <done>All modules audited, cross-module summary table complete, ACCT-02 and ACCT-03 verdicts written with PASS/FAIL/CONDITIONAL</done>
</task>

</tasks>

<verification>
- Findings document covers splits in all 6 modules (MintModule via cross-reference)
- Each split has conservation proof or gap quantification
- Edge cases at 1 wei documented for all splits
- ACCT-02 and ACCT-03 verdicts present
- Any non-remainder patterns flagged explicitly
</verification>

<success_criteria>
- Every BPS split across all modules identified and verified
- No unaccounted wei leak exceeding 1 wei per split operation
- ACCT-02 verdict for 90/10 split
- ACCT-03 verdict for all BPS splits
- Auto-rebuy take-profit arithmetic from EndgameModule fully traced
</success_criteria>

<output>
After completion, create `.planning/phases/04-eth-token-accounting-integrity/04-03-SUMMARY.md`
</output>
