---
phase: 04-eth-token-accounting-integrity
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-eth-token-accounting-integrity/04-06-FINDINGS-game-over-settlement.md
autonomous: true
requirements: [ACCT-07]

must_haves:
  truths:
    - "After handleGameOverDrain completes, all non-claimable funds are distributed to BAF/Decimator winners, vault, and DGNRS"
    - "After handleFinalSweep completes, only claimablePool remains -- no funds are permanently locked"
    - "The 30-day delay between game-over and final sweep is correctly enforced"
    - "Deity pass refunds during game-over correctly reduce the distributable pool"
  artifacts:
    - path: ".planning/phases/04-eth-token-accounting-integrity/04-06-FINDINGS-game-over-settlement.md"
      provides: "Complete game-over settlement trace to zero terminal balance"
      contains: "ACCT-07"
  key_links:
    - from: "handleGameOverDrain"
      to: "deity refunds + BAF/Decimator + vault/DGNRS"
      via: "sequential distribution"
      pattern: "handleGameOverDrain"
    - from: "handleFinalSweep"
      to: "totalFunds - claimablePool"
      via: "30-day delayed sweep"
      pattern: "handleFinalSweep"
---

<objective>
Trace the complete multi-step game-over settlement sequence from liveness guard trigger through handleGameOverDrain and handleFinalSweep to verify zero terminal balance (no funds permanently locked).

Purpose: ACCT-07 requires confirming game-over settlement distributes ALL prize pool funds. Phase 3b-02 audited GameOverModule paths; this plan focuses specifically on the accounting flow to zero balance.
Output: 04-06-FINDINGS-game-over-settlement.md with step-by-step balance trace and ACCT-07 verdict.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-eth-token-accounting-integrity/04-RESEARCH.md

Key source files:
@contracts/DegenerusGame.sol
@contracts/modules/DegenerusGameGameOverModule.sol
@contracts/modules/DegenerusGameAdvanceModule.sol
@contracts/modules/DegenerusGameEndgameModule.sol
@contracts/DegenerusVault.sol

Prior findings:
@.planning/phases/03b-vrf-dependent-modules/03b-02-SUMMARY.md (GameOverModule audit, GO-F01 MEDIUM double refund)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace game-over settlement balance flow from handleGameOverDrain through handleFinalSweep</name>
  <files>.planning/phases/04-eth-token-accounting-integrity/04-06-FINDINGS-game-over-settlement.md</files>
  <action>
READ-ONLY audit. Do NOT modify any contract files.

Trace the complete settlement sequence with explicit balance tracking at each step:

**Step 0: Pre-game-over state**
Document the pool variables at the moment gameOver flag is set:
- currentPrizePool (should be 0 after final level transition)
- nextPrizePool (remaining funds)
- futurePrizePool (accumulated future)
- claimablePool (existing player claims)
- ETH balance = address(this).balance
- stETH balance = steth.balanceOf(address(this))
- totalFunds = ETH + stETH

**Step 1: handleGameOverDrain() -- first call**

Trace through GameOverModule:
1. Deity pass refunds:
   - Level 0: full refund per pass. Verify: futurePrizePool/nextPrizePool reduced, claimablePool/claimableWinnings increased
   - Level 1-9: fixed 20 ETH per pass. Same pool flow.
   - Level 10+: no refund.
   - Verify: total deity refund amount reduces distributable pool

2. BAF distribution:
   - How much of remaining pool goes to BAF winners?
   - Verify: credited to claimableWinnings and claimablePool

3. Decimator distribution:
   - How much of remaining pool goes to Decimator winners?
   - Verify: credited to claimableWinnings and claimablePool (or pre-reserved)

4. _sendToVault:
   - remainder = totalFunds - claimablePool (after deity/BAF/Dec credits)
   - 50% to Vault, 50% to DGNRS
   - Verify: dust handling (max 1 wei)
   - Verify: the ternary underflow guard (totalFunds > claimablePool ? ... : 0)

**Step 2: handleFinalSweep() -- 30 days later**

1. Verify the 30-day guard: `block.timestamp < gameOverTime + 30 days` reverts
2. Calculate: totalFunds at time of sweep = address(this).balance + steth.balanceOf(address(this))
3. sweepable = totalFunds - claimablePool (players who haven't claimed yet)
4. Verify: 50/50 split to vault/DGNRS
5. Verify: after sweep, only claimablePool remains (for late claimers)

**Step 3: Terminal state verification**

After both steps complete:
- Can any player still claim? Yes -- claimableWinnings[player] is still set
- Is any ETH permanently locked? Only if claimablePool > 0 and no player ever claims
- Document: unclaimed funds after "reasonable period" -- is there a mechanism to sweep truly abandoned claims?

**Balance equation at each step:**
```
BEFORE: totalFunds = pools + claimable
AFTER drain: totalFunds = claimable (deity refunds added to claimable, BAF/Dec added to claimable, remainder sent to vault/DGNRS)
AFTER sweep: totalFunds = claimable (any additional stETH yield swept)
```

Cross-reference GO-F01 MEDIUM from Phase 3b-02: If a deity pass was already refunded via refundDeityPass() before game-over, handleGameOverDrain would refund it AGAIN. Quantify the accounting impact: this would increase claimablePool beyond what's backed, potentially violating the invariant.

Write ACCT-07 verdict:
- Normal case: Does settlement reach zero terminal balance? (expected: YES, only claimable remains)
- Edge case with GO-F01: Does the double refund break the invariant?
  </action>
  <verify>
    <automated>grep -c "ACCT-07" .planning/phases/04-eth-token-accounting-integrity/04-06-FINDINGS-game-over-settlement.md | grep -v "^0$"</automated>
  </verify>
  <done>Complete settlement traced with balance equations at each step, GO-F01 accounting impact quantified, ACCT-07 verdict written</done>
</task>

</tasks>

<verification>
- Findings document traces all settlement steps with explicit pool variable values
- Balance equation maintained at each step
- handleFinalSweep 30-day guard verified
- GO-F01 double refund accounting impact quantified
- ACCT-07 verdict present with PASS/FAIL/CONDITIONAL
</verification>

<success_criteria>
- Step-by-step balance tracking from pre-game-over through final sweep
- Deity refund, BAF, Decimator, and vault/DGNRS distribution all traced
- Terminal state documented: only claimablePool remains
- GO-F01 impact on invariant assessed
- ACCT-07 verdict with clear reasoning
</success_criteria>

<output>
After completion, create `.planning/phases/04-eth-token-accounting-integrity/04-06-SUMMARY.md`
</output>
