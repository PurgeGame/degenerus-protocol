---
phase: 04-eth-token-accounting-integrity
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-eth-token-accounting-integrity/04-05-FINDINGS-steth-rebasing.md
autonomous: true
requirements: [ACCT-05]

must_haves:
  truths:
    - "No state variable anywhere in the 22-contract codebase stores a steth.balanceOf() result"
    - "All stETH balance reads are fresh external calls"
    - "stETH rebasing (both positive yield and slashing) impact on the core invariant is documented"
  artifacts:
    - path: ".planning/phases/04-eth-token-accounting-integrity/04-05-FINDINGS-steth-rebasing.md"
      provides: "stETH caching search results and rebasing impact analysis"
      contains: "ACCT-05"
  key_links:
    - from: "DegenerusGame.sol steth.balanceOf"
      to: "no state variable"
      via: "direct external call, never cached"
      pattern: "steth\\.balanceOf"
---

<objective>
Formally document that stETH balance is never cached in a state variable (pre-confirmed PASS from research finding P4-F2) and analyze the impact of stETH rebasing (both positive yield and Lido slashing) on the core invariant.

Purpose: ACCT-05 requires confirming no cached stETH balance. Research pre-confirmed this, but formal documentation with exact search methodology is required for audit completeness.
Output: 04-05-FINDINGS-steth-rebasing.md with search evidence and rebasing impact analysis.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-eth-token-accounting-integrity/04-RESEARCH.md

Key source files:
@contracts/DegenerusGame.sol
@contracts/DegenerusVault.sol
@contracts/DegenerusStonk.sol
@contracts/modules/DegenerusGameGameOverModule.sol
@contracts/modules/DegenerusGameJackpotModule.sol
@contracts/modules/DegenerusGameAdvanceModule.sol
@contracts/storage/DegenerusGameStorage.sol
</context>

<tasks>

<task type="auto">
  <name>Task 1: Search for cached stETH balance and document rebasing impact</name>
  <files>.planning/phases/04-eth-token-accounting-integrity/04-05-FINDINGS-steth-rebasing.md</files>
  <action>
READ-ONLY audit. Do NOT modify any contract files.

**Part A: Exhaustive search for cached stETH balance**

Run the following searches across ALL contracts in /contracts/ (not contracts-testnet/):

1. Search for steth.balanceOf calls:
   ```
   grep -rn "steth\.balanceOf\|stETH\.balanceOf\|STETH\.balanceOf" contracts/
   ```
   Document every hit with file, line, and whether the result is stored in a local variable (OK) vs state variable (BAD).

2. Search for any state variable that could hold stETH balance:
   ```
   grep -rn "stethBalance\|stEthBalance\|stethBal\|cachedBalance\|savedBalance" contracts/
   ```

3. Search storage layout in DegenerusGameStorage.sol for any variable that could cache stETH:
   Read DegenerusGameStorage.sol and check every state variable name/type for potential stETH caching.

4. Verify all steth.balanceOf call sites from research P4-F2:
   - DegenerusGame.sol: lines 1836, 2003, 2025, 2177
   - DegenerusGameGameOverModule.sol: lines 74, 233
   - DegenerusGameJackpotModule.sol: line 868
   - DegenerusVault.sol: line 1031
   - DegenerusStonk.sol: lines 845, 863, 919, 943, 1027
   For each: confirm the result is used in a local variable only (not persisted to storage).

**Part B: stETH rebasing impact analysis**

Document the following:

1. **Positive rebasing (daily yield):** steth.balanceOf(address(this)) increases daily as Lido accrues staking yield. This STRENGTHENS the invariant: balance + stETH grows while claimablePool remains constant. Document: the protocol benefits from yield accrual between _autoStakeExcessEth calls.

2. **Negative rebasing (Lido slashing):** steth.balanceOf(address(this)) can DECREASE if Lido validators are slashed. Document:
   - At steady state, what fraction of contract backing is in stETH? (After _autoStakeExcessEth converts all excess ETH)
   - Maximum historical Lido slashing percentage (reference Lido docs if known, or note as unknown)
   - Worst case: if 100% of non-claimable ETH is staked and a slashing event occurs, can balance + stETH < claimablePool?
   - Mitigation: _payoutWithStethFallback tries ETH first in some code paths, reducing stETH dependency

3. **stETH 1-2 wei transfer rounding:** Document from research Pitfall 5 that stETH transfers may deliver 1-2 wei less than requested due to share-to-balance rounding. Impact: contract RETAINS slightly more than expected (invariant-safe), but recipients get slightly less.

4. **yieldPoolView():** Document how yield surplus is calculated: `totalBalance - (current + next + claimable + future)`. This represents unattributed stETH yield available for vault distribution.

**Write ACCT-05 verdict:**
- No cached stETH balance: PASS/FAIL
- Rebasing handling: Document as informational (positive rebasing is safe; slashing risk is inherent to stETH design, not a code bug)
  </action>
  <verify>
    <automated>grep -c "ACCT-05" .planning/phases/04-eth-token-accounting-integrity/04-05-FINDINGS-steth-rebasing.md | grep -v "^0$"</automated>
  </verify>
  <done>All steth.balanceOf call sites verified as non-cached, rebasing impact documented for both yield and slashing, ACCT-05 verdict written</done>
</task>

</tasks>

<verification>
- Findings document contains exhaustive grep results
- All 11 steth.balanceOf call sites verified with line numbers
- No state variable caching identified
- Rebasing analysis covers positive, negative, and transfer rounding
- ACCT-05 verdict present
</verification>

<success_criteria>
- All steth.balanceOf call sites in all 22 contracts documented
- Zero state variables found caching stETH balance
- Lido slashing worst-case impact quantified or bounded
- stETH transfer rounding documented as informational
- ACCT-05 PASS verdict with search evidence
</success_criteria>

<output>
After completion, create `.planning/phases/04-eth-token-accounting-integrity/04-05-SUMMARY.md`
</output>
