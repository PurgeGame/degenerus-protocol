---
phase: 04-eth-token-accounting-integrity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-eth-token-accounting-integrity/04-02-FINDINGS-invariant-manual-trace.md
autonomous: true
requirements: [ACCT-01]

must_haves:
  truths:
    - "Every claimablePool increment has a corresponding claimableWinnings increment (or documented exception like decimator pre-reservation)"
    - "Every claimableWinnings increment has a corresponding claimablePool increment (or documented auto-rebuy bypass)"
    - "The auto-rebuy path correctly avoids incrementing claimablePool when ETH routes to prize pools"
    - "No mutation site can increment claimablePool without adding equivalent ETH/stETH backing"
  artifacts:
    - path: ".planning/phases/04-eth-token-accounting-integrity/04-02-FINDINGS-invariant-manual-trace.md"
      provides: "Systematic trace of all 16 claimablePool mutation sites"
      contains: "claimablePool"
  key_links:
    - from: "DegenerusGamePayoutUtils.sol _creditClaimable"
      to: "claimablePool / claimableWinnings"
      via: "paired increment"
      pattern: "claimablePool.*claimableWinnings"
    - from: "DegenerusGameEndgameModule.sol _addClaimableEth"
      to: "auto-rebuy bypass"
      via: "conditional routing to nextPrizePool/futurePrizePool"
      pattern: "autoRebuy"
---

<objective>
Systematically trace all 16 claimablePool mutation sites across 7 modules to verify the core accounting invariant: `address(this).balance + steth.balanceOf(this) >= claimablePool`. For each mutation, verify symmetry between claimablePool and claimableWinnings, and verify ETH/stETH backing exists.

Purpose: Medusa/Echidna are unavailable. This manual trace with the complete mutation site inventory from research is the primary verification method for ACCT-01.
Output: 04-02-FINDINGS-invariant-manual-trace.md with per-site analysis and overall invariant verdict.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-eth-token-accounting-integrity/04-RESEARCH.md

Key source files (read relevant sections for each mutation site):
@contracts/DegenerusGame.sol
@contracts/modules/DegenerusGamePayoutUtils.sol
@contracts/modules/DegenerusGameEndgameModule.sol
@contracts/modules/DegenerusGameJackpotModule.sol
@contracts/modules/DegenerusGameDecimatorModule.sol
@contracts/modules/DegenerusGameDegeneretteModule.sol
@contracts/modules/DegenerusGameGameOverModule.sol
@contracts/modules/DegenerusGameAdvanceModule.sol
@contracts/modules/DegenerusGameMintModule.sol

<interfaces>
<!-- Complete mutation site inventory from 04-RESEARCH.md -->

claimablePool DECREMENTS (6 sites):
  DegenerusGame.sol:1081          _processMintPayment (spending claimable on tickets)
  DegenerusGame.sol:1448          _claimWinningsInternal (player withdrawal)
  DecimatorModule.sol:492         Decimator auto-rebuy ticket conversion
  DecimatorModule.sol:539         Decimator lootbox conversion
  DegeneretteModule.sol:585       ETH bet using claimable winnings

claimablePool INCREMENTS (10 sites):
  PayoutUtils.sol:90              _queueWhalePassClaimCore remainder
  EndgameModule.sol:202           runRewardJackpots BAF/Decimator credits
  EndgameModule.sol:251           _addClaimableEth auto-rebuy take profit
  JackpotModule.sol:894           Daily jackpot credits
  JackpotModule.sol:1427          Level jackpot credits
  JackpotModule.sol:1459          Level jackpot variant
  JackpotModule.sol:1507          Lootbox jackpot credits
  DegeneretteModule.sol:1173      Degenerette ETH win credit
  GameOverModule.sol:96,119       Deity pass refund on game over
  GameOverModule.sol:183,219      BAF/Decimator jackpot on game over

claimablePool READ-ONLY (2 sites):
  DegenerusGame.sol:1853          adminStakeEthForStEth guard
  AdvanceModule.sol:1003          _autoStakeExcessEth guard
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace all 16 claimablePool mutation sites for symmetry and backing</name>
  <files>.planning/phases/04-eth-token-accounting-integrity/04-02-FINDINGS-invariant-manual-trace.md</files>
  <action>
READ-ONLY audit. Do NOT modify any contract files.

For EACH of the 16 claimablePool mutation sites listed in the interfaces block above, perform the following analysis:

**For each DECREMENT site:**
1. Read the source code at the exact line number
2. Verify: what reduces claimablePool? (withdrawal, spending, conversion)
3. Verify: does the corresponding ETH/stETH leave the contract? Or does it move to another pool?
4. Verdict: Does this decrement maintain the invariant? (claimablePool goes down, and either ETH leaves or ETH moves to non-claimable pool)

**For each INCREMENT site:**
1. Read the source code at the exact line number
2. Verify: where did the ETH/stETH come from? (currentPrizePool, futurePrizePool, msg.value, etc.)
3. Verify: is claimableWinnings[player] also incremented by the same amount? Trace the call chain.
4. Special case: auto-rebuy paths -- verify claimablePool is NOT incremented when ETH routes to prize pools
5. Verdict: Does this increment maintain the invariant? (claimablePool goes up, and ETH/stETH backing exists)

**Special attention to:**
- **Auto-rebuy path** (EndgameModule.sol:251 _addClaimableEth): Research warns this is the most complex path. Trace the full auto-rebuy calculation: `rebuyAmount`, `ethSpent`, `reserved`. Verify `claimablePool += calc.reserved` only covers the take-profit portion, not the rebuy portion.
- **Decimator pre-reservation** (Research Pitfall 4): Verify that DecimatorModule.sol claimablePool increments (GameOverModule.sol:219) eventually match sum(claimableWinnings) after all claims resolve.
- **_queueWhalePassClaimCore remainder** (PayoutUtils.sol:90): Verify remainder = whale pass value - whale pass NFT mint cost, and this remainder is both credited to claimableWinnings AND claimablePool.

For each mutation site, write a row in a table:
| File:Line | Operation | claimablePool Delta | claimableWinnings Delta | ETH Backing | Verdict |

At the end, write the overall ACCT-01 verdict based on the complete trace.

Also address the three open questions from research:
1. Decimator pre-reservation cleanup -- does equality restore after all claims?
2. Auto-rebuy dust handling -- what happens to `rebuyAmount - ethSpent - reserved`?
3. Does any path credit claimableWinnings WITHOUT incrementing claimablePool?
  </action>
  <verify>
    <automated>grep -c "ACCT-01" .planning/phases/04-eth-token-accounting-integrity/04-02-FINDINGS-invariant-manual-trace.md | grep -v "^0$"</automated>
  </verify>
  <done>All 16 mutation sites traced with individual verdicts, auto-rebuy path fully analyzed, decimator pre-reservation cleanup verified, overall ACCT-01 verdict written with PASS/FAIL/CONDITIONAL rating</done>
</task>

</tasks>

<verification>
- Findings document exists with all 16 mutation sites analyzed
- Each site has a verdict row in the summary table
- Auto-rebuy path has detailed trace showing claimablePool is NOT incremented for rebuy portion
- Decimator pre-reservation cleanup is verified
- Three research open questions are answered
- Overall ACCT-01 verdict is present
</verification>

<success_criteria>
- 16/16 mutation sites traced with line-number evidence
- Auto-rebuy dust handling explained
- Decimator pre-reservation lifecycle documented
- No claimableWinnings increment found without claimablePool increment (or exception documented)
- ACCT-01 verdict is PASS, FAIL, or CONDITIONAL with specific reasoning
</success_criteria>

<output>
After completion, create `.planning/phases/04-eth-token-accounting-integrity/04-02-SUMMARY.md`
</output>
