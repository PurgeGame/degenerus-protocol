---
phase: 04-eth-token-accounting-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-eth-token-accounting-integrity/04-01-FINDINGS-eth-flow-trace.md
autonomous: true
requirements: [ACCT-01, ACCT-06]

must_haves:
  truths:
    - "Every ETH inflow path (purchase, purchaseWhaleBundle, purchaseDeityPass, placeFullTicketBets, receive) attributes msg.value to exactly one pool variable with no remainder"
    - "Every ETH outflow path (claimWinnings, refundDeityPass, adminSwapEthForStEth, handleGameOverDrain, handleFinalSweep) reduces pool variables by exactly the amount sent"
    - "receive() routes all incoming ETH exclusively to futurePrizePool"
    - "refundDeityPass pool reduction equals payout amount with no residual"
  artifacts:
    - path: ".planning/phases/04-eth-token-accounting-integrity/04-01-FINDINGS-eth-flow-trace.md"
      provides: "Complete ETH inflow/outflow trace with line-by-line verification"
      contains: "ACCT-01"
  key_links:
    - from: "DegenerusGame.sol receive()"
      to: "futurePrizePool"
      via: "direct += msg.value"
      pattern: "futurePrizePool.*msg\\.value"
    - from: "DegenerusGame.sol _claimWinningsInternal"
      to: "claimablePool"
      via: "claimablePool -= payout before external call"
      pattern: "claimablePool.*payout"
---

<objective>
Trace all ETH inflow and outflow paths across the DegenerusGame contract and all delegatecall modules. For each path, verify: (1) incoming ETH is fully attributed to a pool variable, (2) outgoing ETH reduces the correct pool by the exact payout amount, (3) no ETH can enter or leave without corresponding pool accounting.

Purpose: Establishes the complete ETH flow map that underpins ACCT-01 (core invariant). Without knowing every path where ETH enters and exits, the invariant cannot be verified.
Output: 04-01-FINDINGS-eth-flow-trace.md with per-path verdict and overall ACCT-01/ACCT-06 assessment.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-eth-token-accounting-integrity/04-RESEARCH.md

Key source files:
@contracts/DegenerusGame.sol
@contracts/storage/DegenerusGameStorage.sol
@contracts/modules/DegenerusGamePayoutUtils.sol
@contracts/modules/DegenerusGameAdvanceModule.sol
@contracts/modules/DegenerusGameWhaleModule.sol
@contracts/modules/DegenerusGameGameOverModule.sol
@contracts/modules/DegenerusGameEndgameModule.sol
@contracts/modules/DegenerusGameMintModule.sol
@contracts/modules/DegenerusGameDegeneretteModule.sol

Prior findings (reference only):
@.planning/phases/03a-core-eth-flow-modules/03a-01-SUMMARY.md
@.planning/phases/03b-vrf-dependent-modules/03b-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace all 5 ETH inflow paths and verify pool attribution</name>
  <files>.planning/phases/04-eth-token-accounting-integrity/04-01-FINDINGS-eth-flow-trace.md</files>
  <action>
READ-ONLY audit. Do NOT modify any contract files.

Trace each of the 5 ETH inflow paths in DegenerusGame.sol and its modules. For each path, document:
- Entry function name and line number
- Where msg.value is split/assigned to pool variables
- Whether sum(pool increments) == msg.value (exact attribution)
- Any conditional branches that could leave ETH unattributed

**Inflow paths to trace:**

1. **purchase()** -> recordMint -> 90% nextPrizePool + 10% futurePrizePool
   - Verify the BPS split: `futureShare = (prizeContribution * FUTURE_POOL_BPS) / 10000`, `nextShare = prizeContribution - futureShare`
   - Confirm `nextShare + futureShare == prizeContribution` (remainder pattern)
   - Note: Phase 3a-01 already confirmed this PASS; cross-reference and re-verify in accounting context

2. **purchaseWhaleBundle()** -> WhaleModule delegatecall
   - Trace msg.value distribution across pool variables (50% next/future + 50% future/reward, level-dependent)
   - Verify total attribution == msg.value for each level tier (0-3, x49, x99)

3. **purchaseDeityPass()** -> WhaleModule delegatecall
   - Trace msg.value -> futurePrizePool attribution
   - Verify full amount goes to futurePrizePool

4. **placeFullTicketBets()** -> DegeneretteModule delegatecall
   - Trace msg.value -> futurePrizePool for ETH bets
   - Verify msg.value is fully attributed

5. **receive()** -> futurePrizePool += msg.value
   - Confirm this is the only code path and msg.value is fully attributed

For each path, write a verdict: PASS (exact attribution), FAIL (wei leak), or INFO (acceptable rounding).

Also trace adminSwapEthForStEth() which receives ETH but sends stETH back -- verify net-neutrality for the contract's ETH balance.
  </action>
  <verify>
    <automated>grep -c "PASS\|FAIL\|INFO" .planning/phases/04-eth-token-accounting-integrity/04-01-FINDINGS-eth-flow-trace.md | grep -v "^0$"</automated>
  </verify>
  <done>All 5 inflow paths traced with line numbers, pool attribution verified, verdict assigned per path</done>
</task>

<task type="auto">
  <name>Task 2: Trace all 5 ETH outflow paths and verify pool reduction matches payout</name>
  <files>.planning/phases/04-eth-token-accounting-integrity/04-01-FINDINGS-eth-flow-trace.md</files>
  <action>
READ-ONLY audit. Do NOT modify any contract files.

Trace each of the 5 ETH outflow paths. For each, verify the pool variable is reduced by EXACTLY the amount sent to the recipient.

**Outflow paths to trace:**

1. **claimWinnings()** -> _claimWinningsInternal
   - Verify: claimablePool -= payout BEFORE external call
   - Verify: payout == amount - 1 (sentinel subtraction)
   - Verify: the ETH/stETH sent == payout (trace _payoutWithStethFallback and _payoutWithEthFallback)
   - Cross-reference: Phase 3a confirmed CEI pattern; verify payout amount consistency

2. **refundDeityPass()** -> WhaleModule delegatecall
   - Verify: futurePrizePool/nextPrizePool reduced by exactly the refund amount
   - Verify: _payoutWithStethFallback sends exactly the same amount
   - CRITICAL: Phase 3b-02 found GO-F01 MEDIUM (double refund). Note this in context but focus on single-path pool reduction correctness here.
   - Check: does refundDeityPass touch claimablePool? (Expected: NO -- it bypasses claimablePool entirely)

3. **adminSwapEthForStEth()** -> sends stETH to recipient, receives ETH from admin
   - Verify: this is admin-provided ETH, not pool ETH
   - Trace the stETH transfer amount vs ETH received

4. **handleGameOverDrain()** -> GameOverModule delegatecall
   - Verify: deity pass refunds reduce futurePrizePool/nextPrizePool correctly
   - Verify: BAF/Decimator distribution credits claimablePool correctly
   - Verify: _sendToVault distributes remainder to vault/DGNRS

5. **handleFinalSweep()** -> GameOverModule delegatecall
   - Verify: totalFunds - claimablePool = sweepable amount
   - Verify: 50/50 split to vault/DGNRS with correct dust handling

Also trace the internal conversion:
- **_autoStakeExcessEth()**: verify it stakes `ethBal - claimablePool` (never touching claimable ETH)
- **adminStakeEthForStEth()**: verify same guard pattern

Write overall ACCT-01 and ACCT-06 verdicts at the end of the findings document:
- ACCT-01: Does balance + stETH >= claimablePool hold if all inflows attribute correctly and all outflows reduce correctly?
- ACCT-06: Does receive() correctly route all incoming ETH?
  </action>
  <verify>
    <automated>grep -c "ACCT-01\|ACCT-06" .planning/phases/04-eth-token-accounting-integrity/04-01-FINDINGS-eth-flow-trace.md | grep -v "^0$"</automated>
  </verify>
  <done>All 5 outflow paths traced with line numbers, pool reduction verified against payout amounts, internal conversions traced, ACCT-01 and ACCT-06 verdicts written</done>
</task>

</tasks>

<verification>
- Findings document exists at .planning/phases/04-eth-token-accounting-integrity/04-01-FINDINGS-eth-flow-trace.md
- All 5 inflow and 5 outflow paths have individual verdicts
- ACCT-01 and ACCT-06 requirement verdicts are written with PASS/FAIL/CONDITIONAL
- Every verdict includes specific line numbers from source contracts
</verification>

<success_criteria>
- Complete ETH flow map with 10+ paths traced
- Each path has line-number evidence and PASS/FAIL verdict
- ACCT-01 preliminary verdict based on flow analysis
- ACCT-06 verdict for receive() routing
- refundDeityPass bypass of claimablePool documented
- adminSwapEthForStEth net-neutrality verified
</success_criteria>

<output>
After completion, create `.planning/phases/04-eth-token-accounting-integrity/04-01-SUMMARY.md`
</output>
