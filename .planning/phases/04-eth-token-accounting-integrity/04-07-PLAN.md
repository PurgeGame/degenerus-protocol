---
phase: 04-eth-token-accounting-integrity
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-eth-token-accounting-integrity/04-07-FINDINGS-stall-recovery.md
autonomous: true
requirements: [ACCT-08]

must_haves:
  truths:
    - "912-day deploy idle timeout at level 0 uses correct arithmetic and cannot be triggered prematurely"
    - "365-day inactivity timeout at level 1+ uses correct timestamp comparison and cannot be triggered early"
    - "30-day final sweep delay after game-over is correctly enforced"
    - "All stall recovery paths correctly preserve claimablePool and do not mis-attribute pool funds"
  artifacts:
    - path: ".planning/phases/04-eth-token-accounting-integrity/04-07-FINDINGS-stall-recovery.md"
      provides: "Stall recovery path audit with premature-trigger resistance analysis"
      contains: "ACCT-08"
  key_links:
    - from: "AdvanceModule liveness guards"
      to: "gameOver flag"
      via: "timeout arithmetic"
      pattern: "912 days\\|365 days\\|30 days"
---

<objective>
Audit all stall recovery paths (912-day idle, 365-day inactivity, 30-day final sweep) for correct timeout arithmetic, premature-trigger resistance, and correct pool fund attribution during recovery.

Purpose: ACCT-08 requires verifying stall recovery paths cannot be triggered prematurely and correctly attribute all pool funds. Phase 2 confirmed recovery paths are reachable; this plan focuses on accounting impact.
Output: 04-07-FINDINGS-stall-recovery.md with timeout arithmetic verification and ACCT-08 verdict.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-eth-token-accounting-integrity/04-RESEARCH.md

Key source files:
@contracts/modules/DegenerusGameAdvanceModule.sol
@contracts/modules/DegenerusGameGameOverModule.sol
@contracts/DegenerusGame.sol
@contracts/storage/DegenerusGameStorage.sol
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify timeout arithmetic and premature-trigger resistance for all stall recovery paths</name>
  <files>.planning/phases/04-eth-token-accounting-integrity/04-07-FINDINGS-stall-recovery.md</files>
  <action>
READ-ONLY audit. Do NOT modify any contract files.

Trace each stall recovery path in AdvanceModule and GameOverModule:

**Path 1: 912-day deploy idle timeout (level 0)**

1. Find the guard condition in AdvanceModule -- the exact comparison expression
2. Verify: the timestamp comparison uses correct units (seconds vs days)
3. Verify: `block.timestamp - deployTime >= 912 days` or equivalent
4. Check: can block.timestamp manipulation by a validator trigger this early? (Validators can only skew ~15s)
5. Accounting impact: when this triggers, what happens to pools? Does it call handleGameOverDrain?
6. Verify: claimablePool is preserved during the transition

**Path 2: 365-day inactivity timeout (level 1+)**

1. Find the guard condition in AdvanceModule -- `ts - 365 days > lst` or equivalent
2. Verify: correct timestamp comparison (no off-by-one, no overflow)
3. Verify: `lst` (last sale timestamp) is correctly maintained -- when was it last updated?
4. Check: can an attacker reset the inactivity timer cheaply? (Any purchase resets it)
5. Accounting impact: same as Path 1 -- does it trigger handleGameOverDrain?

**Path 3: 18-hour VRF retry timeout**

1. Find the guard in AdvanceModule that allows VRF re-request after 18 hours
2. Verify: this does NOT trigger game-over, only retries VRF
3. Accounting impact: should be neutral (no pool changes during VRF retry)

**Path 4: 3-day emergency stall**

1. Find the emergency path in AdvanceModule
2. Verify: what happens if VRF never fulfills for 3 days?
3. Accounting impact: does this trigger game-over? Or does it use fallback randomness?
4. Verify: pool funds are correctly attributed during emergency resolution

**Path 5: 30-day final sweep delay**

1. Find the guard in GameOverModule: `block.timestamp < gameOverTime + 30 days`
2. Verify: the arithmetic is correct and not overflowable
3. Verify: calling handleFinalSweep before 30 days reverts
4. Accounting impact: sweep correctly distributes `totalFunds - claimablePool`

**For each path, document:**
| Path | Guard Expression | Line # | Arithmetic Correct? | Premature Trigger Possible? | Pool Attribution | Verdict |

**Write ACCT-08 verdict:**
- Are all stall recovery paths correctly guarded against premature triggering?
- Do all paths correctly preserve claimablePool?
- Do all paths correctly attribute remaining pool funds?
  </action>
  <verify>
    <automated>grep -c "ACCT-08" .planning/phases/04-eth-token-accounting-integrity/04-07-FINDINGS-stall-recovery.md | grep -v "^0$"</automated>
  </verify>
  <done>All 5 stall recovery paths traced with exact guard expressions, premature-trigger analysis, pool attribution verification, ACCT-08 verdict written</done>
</task>

</tasks>

<verification>
- Findings document covers all 5 stall recovery paths
- Each path has the exact guard expression with line numbers
- Premature-trigger resistance confirmed for each path
- Pool attribution during recovery verified
- ACCT-08 verdict present
</verification>

<success_criteria>
- 912-day, 365-day, 18-hour, 3-day, and 30-day timeouts all verified
- No premature trigger possible (validator timestamp skew < 15s insufficient)
- claimablePool preserved across all recovery transitions
- ACCT-08 PASS/FAIL/CONDITIONAL verdict
</success_criteria>

<output>
After completion, create `.planning/phases/04-eth-token-accounting-integrity/04-07-SUMMARY.md`
</output>
