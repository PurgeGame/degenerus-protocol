---
phase: 241-exception-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v30-EXCEPTION-CLOSURE.md
  - .planning/phases/241-exception-closure/241-01-SUMMARY.md
autonomous: true
requirements: [EXC-01, EXC-02, EXC-03, EXC-04]
head_anchor: 7ab515fe
tags: [audit, rng, exception-closure, known-issues, determinism, vrf]

must_haves:
  truths:
    - "The 4 KNOWN-ISSUES RNG entries are confirmed as the ONLY violations of the RNG-consumer determinism invariant at HEAD 7ab515fe (universal ONLY-ness claim holds)."
    - "EXC-01 affiliate winner roll is the sole non-VRF-seeded randomness consumer in contracts/; no other deterministic-seed surface leaks into any RNG-derived payout or winner-selection path reachable by a player."
    - "EXC-02 prevrandao fallback (_getHistoricalRngFallback) is reachable only inside _gameOverEntropy AND only when an in-flight VRF request has been outstanding >= GAMEOVER_RNG_FALLBACK_DELAY = 14 days at HEAD."
    - "EXC-03 F-29-04 mid-cycle RNG substitution remains tri-gated at HEAD: terminal-state only + no player-reachable timing + post-swap write buffer only."
    - "EntropyLib.entropyStep() XOR-shift body at contracts/libraries/EntropyLib.sol:16-23 is unchanged at HEAD 7ab515fe (pure uint256 XOR-shift: state<<7, state>>9, state<<8 inside an unchecked block); every call site to entropyStep in contracts/ receives state/entropy pre-derived via keccak256(abi.encode(rngWord, ...)) with rngWord VRF-derived; no new entry point bypasses the caller-site keccak seed construction."
    - "Phase 240's 29 forward-cite tokens (17 EXC-02 + 12 EXC-03) are each discharged line-item in a Forward-Cite Discharge Ledger."
    - "Dual-gate closure passes: Gate A set-equality with Phase 238's 22-EXCEPTION distribution + Gate B grep backstop over player-reachable non-VRF seed surface universe."
    - "Zero contracts/ or test/ writes; Phase 237/238/239/240 outputs unmodified; KNOWN-ISSUES.md untouched."
  artifacts:
    - path: "audit/v30-EXCEPTION-CLOSURE.md"
      provides: "Single consolidated Phase 241 deliverable per ROADMAP SC-1 literal"
      contains: "10 sections per D-24: Frontmatter+HEAD, Executive Summary, EXC-01 ONLY-ness Table (22 rows), EXC-01 Grep Backstop Classification, EXC-02 Predicate Re-Verification, EXC-03 Tri-Gate Predicate Re-Verification, EXC-04 EntropyLib Seed Re-Verification, Forward-Cite Discharge Ledger (29 rows), Prior-Artifact Cross-Cites, Finding Candidates + Scope-Guard Deferrals + Attestation"
      min_lines: 400
    - path: ".planning/phases/241-exception-closure/241-01-SUMMARY.md"
      provides: "Plan execution summary with decisions, verdict distribution, scope-guard deferrals (if any), forward-cite discharge count"
  key_links:
    - from: "audit/v30-EXCEPTION-CLOSURE.md § 3 EXC-01 Consolidated ONLY-ness Table"
      to: "audit/v30-CONSUMER-INVENTORY.md 22 EXCEPTION rows"
      via: "Gate A set-equality check — 22 EXC-241-NNN rows set-equal with Phase 237 Consumer Index KI-exception distribution (2 EXC-01 + 8 EXC-02 + 4 EXC-03 + 8 EXC-04) and Phase 238 22-EXCEPTION / 124-SAFE split"
      pattern: "^\\| EXC-241-0[0-2][0-9] \\|"
    - from: "audit/v30-EXCEPTION-CLOSURE.md § 8 Forward-Cite Discharge Ledger"
      to: "audit/v30-GAMEOVER-JACKPOT-SAFETY.md 17 'See Phase 241 EXC-02' + 12 'See Phase 241 EXC-03' forward-cite tokens"
      via: "explicit line-item discharge row per forward-cite; verdict DISCHARGED_RE_VERIFIED_AT_HEAD or CANDIDATE_FINDING per D-11"
      pattern: "DISCHARGES Phase 240 GO-0[12] forward-cite"
    - from: "audit/v30-EXCEPTION-CLOSURE.md § 5 EXC-02 Predicate Re-Verification"
      to: "contracts/modules/DegenerusGameAdvanceModule.sol:109 (GAMEOVER_RNG_FALLBACK_DELAY = 14 days), :1252 (_gameOverEntropy trigger), :1301 (_getHistoricalRngFallback)"
      via: "grep callers of _getHistoricalRngFallback → sole caller _gameOverEntropy:1252; grep 14 days constant @ :109 intact"
      pattern: "GAMEOVER_RNG_FALLBACK_DELAY = 14 days"
    - from: "audit/v30-EXCEPTION-CLOSURE.md § 6 EXC-03 Tri-Gate"
      to: "contracts/modules/DegenerusGameAdvanceModule.sol:292 (_swapAndFreeze), :1082 (_swapTicketSlot), :1222-1246 (_gameOverEntropy substitution)"
      via: "tri-gate: terminal-state + no-player-reachable-timing (cite Phase 240 GO-04 DISPROVEN_PLAYER_REACHABLE_VECTOR) + buffer-scope (cite Phase 240 GO-05 BOTH_DISJOINT)"
      pattern: "terminal-state|no-player-reachable-timing|buffer-scope"
    - from: "audit/v30-EXCEPTION-CLOSURE.md § 7 EXC-04 EntropyLib body intact (EXC-04-P1a)"
      to: "contracts/libraries/EntropyLib.sol:16-23 entropyStep() XOR-shift body"
      via: "read file lines 16-23; confirm function signature 'function entropyStep(uint256 state) internal pure returns (uint256)' at :16; confirm body contains exactly 'state ^= state << 7;', 'state ^= state >> 9;', 'state ^= state << 8;' inside an 'unchecked' block"
      pattern: "function entropyStep\\(uint256 state\\) internal pure"
    - from: "audit/v30-EXCEPTION-CLOSURE.md § 7 EXC-04 caller-site keccak seed construction (EXC-04-P1b)"
      to: "contracts/modules/DegenerusGameLootboxModule.sol (LootboxModule entropy caller sites), contracts/modules/DegenerusGameJackpotModule.sol:2119 (Jackpot caller site), and other entropyStep consumers"
      via: "grep contracts/ for 'keccak256\\(abi\\.encode\\(rngWord' — MUST produce hits at Lootbox + Jackpot + Staked caller sites (seed construction lives at CALLER sites, NOT inside entropyStep); enumerate each hit and trace rngWord provenance to a VRF callback"
      pattern: "keccak256\\(abi\\.encode\\(rngWord"
---

<objective>
Confirm the 4 KNOWN-ISSUES RNG entries are the *only* violations of the RNG-consumer determinism invariant at HEAD `7ab515fe` via a universal ONLY-ness claim (EXC-01 consolidated 22-row table covering all 4 KI groups per D-06), EXC-02/EXC-03/EXC-04 predicate re-verifications at HEAD (D-10), and an explicit line-item Forward-Cite Discharge Ledger closing Phase 240's 29 cross-phase forward-cites (17 EXC-02 + 12 EXC-03 per D-11).

Purpose: Close ROADMAP Phase 241 SC-1/SC-2/SC-3/SC-4; discharge the 29 forward-cite tokens Phase 240 emitted expecting Phase 241 closure; produce the single consolidated audit artifact `audit/v30-EXCEPTION-CLOSURE.md` per ROADMAP SC-1 literal (D-22) that Phase 242 will consume directly for FIND-01 intake.

Output: One new audit file — `audit/v30-EXCEPTION-CLOSURE.md` — structured as 10 sections per D-24, with 22 EXC-241-NNN ONLY-ness rows + EXC-02 single-call-site + 14-day gate predicate table + EXC-03 tri-gate predicate table + EXC-04 keccak seed predicate table + 29-row Forward-Cite Discharge Ledger + Prior-Artifact Cross-Cites (>=3 `re-verified at HEAD 7ab515fe` instances per D-13) + Finding Candidates + Attestation. Plus one plan SUMMARY file under `.planning/phases/241-exception-closure/`. Zero `contracts/` or `test/` writes. Phase 237/238/239/240 outputs are READ-only per D-27.

Scope frame: EXPLOITABILITY per user D-05 (`"just worried about exploitability"`) — NOT randomness distribution quality. Randomness distribution concessions (XOR-shift theoretical weaknesses, prevrandao 1-bit proposer bias, deterministic affiliate seed) are already KI-accepted and MUST NOT be re-litigated in this phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/241-exception-closure/241-CONTEXT.md
@CLAUDE.md

# Phase 237 scope anchor (READ-only per D-27 — source of truth for 146-row universe + 22-EXCEPTION distribution)
@audit/v30-CONSUMER-INVENTORY.md

# Phase 238 output (READ-only — Gate A set-equality cross-check)
@audit/v30-FREEZE-PROOF.md

# Phase 239 outputs (READ-only — corroborating per D-12)
@audit/v30-RNGLOCK-STATE-MACHINE.md
@audit/v30-PERMISSIONLESS-SWEEP.md
@audit/v30-ASYMMETRY-RE-JUSTIFICATION.md

# Phase 240 outputs (READ-only — corroborating + 29 forward-cite discharge targets per D-11)
@audit/v30-GAMEOVER-JACKPOT-SAFETY.md
@audit/v30-240-01-INV-DET.md
@audit/v30-240-02-STATE-TIMING.md
@audit/v30-240-03-SCOPE.md

# SUBJECTS of Phase 241 re-verification (NOT warrants per D-12) — KI entries
@KNOWN-ISSUES.md

# Contract subject files at HEAD 7ab515fe (READ-only per D-26 — feedback_no_contract_commits.md, feedback_contract_locations.md)
@contracts/DegenerusAffiliate.sol
@contracts/modules/DegenerusGameAdvanceModule.sol
@contracts/libraries/EntropyLib.sol
@contracts/modules/DegenerusGameLootboxModule.sol
@contracts/modules/DegenerusGameJackpotModule.sol

# Project rules
@memory/feedback_no_contract_commits.md
@memory/feedback_never_preapprove_contracts.md
@memory/feedback_contract_locations.md
@memory/feedback_rng_backward_trace.md
@memory/feedback_rng_commitment_window.md

<interfaces>
<!-- Pre-computed expected constants the executor MUST cross-check literally against HEAD. -->
<!-- Copied verbatim from 241-CONTEXT.md D-06/D-07/D-10/D-11 so the executor can proceed without re-reading context. -->

## D-06 — Expected 22-row ONLY-ness distribution (Gate A target)

Total: 22 EXCEPTION rows = set-equal with Phase 238 22-EXCEPTION / 124-SAFE split.

- **EXC-01 group (2 rows) — affiliate non-VRF seed:**
  - INV-237-005 — `contracts/DegenerusAffiliate.sol:568` — `processAffiliatePayment` no-referrer 50/50 VAULT/DGNRS flip
  - INV-237-006 — `contracts/DegenerusAffiliate.sol:585` — `processAffiliatePayment` referred 75/20/5 weighted winner roll

- **EXC-02 group (8 rows) — gameover prevrandao fallback:**
  - 8 rows tagged `exception-prevrandao-fallback` subcategory in Phase 237 Consumer Index (see `audit/v30-CONSUMER-INVENTORY.md` INV-237-055..062 KI Cross-Ref column per Phase 238-03 decision log)

- **EXC-03 group (4 rows) — F-29-04 mid-cycle substitution:**
  - INV-237-024, INV-237-045, INV-237-053, INV-237-054

- **EXC-04 group (8 rows) — EntropyLib XOR-shift seed:**
  - 8 rows tagged `exception-xor-shift-seed`-adjacent in Phase 237 Consumer Index (INV-237-124, INV-237-131, INV-237-132, INV-237-134..138 per Phase 238 Plan 03 decision log)

Total = 2 + 8 + 4 + 8 = 22. Any row outside this set with a HEAD seed-source verdict != `VRF_DERIVED` is a `CANDIDATE_FINDING`. Any row in this set missing at HEAD is a `CANDIDATE_FINDING`.

## D-07 — Grep backstop surface universe (player-reachable non-VRF entropy)

Enumerate over entire `contracts/` tree:
- `block.timestamp` (direct or via `currentDayIndex()`)
- `block.number`
- `block.prevrandao`
- `blockhash(...)`
- packed state counters used as entropy (e.g., `currentDayIndex()`, `storedCode`, nonce-like reads)
- `msg.sender` used as seed input
- `keccak256(...)` hashes over non-VRF-committed state feeding an RNG consumer

Classification vocabulary per hit (closed set per D-07):
- `ORTHOGONAL_NOT_RNG_CONSUMED`
- `BELONGS_TO_KI_EXC_01`
- `BELONGS_TO_KI_EXC_02`
- `BELONGS_TO_KI_EXC_03`
- `BELONGS_TO_KI_EXC_04`
- `CANDIDATE_FINDING`

Note: `block.coinbase`, `tx.origin`-as-entropy, `block.difficulty` included ONLY if present in contract tree; not exhaustively enumerated as theoretical candidates.

## D-08 — Dual-gate closure rule

- **Gate A (set-equality):** Phase 237 inventory rows with HEAD-re-verified seed source != `VRF_DERIVED` == the 22-row set above (verbatim per D-06).
- **Gate B (grep backstop):** negative-space grep (D-07 surface) surfaces zero `CANDIDATE_FINDING` hits — every hit classified as `ORTHOGONAL_NOT_RNG_CONSUMED` or `BELONGS_TO_KI_EXC_NN`.
- **Combined:** `ONLY_NESS_HOLDS_AT_HEAD` iff BOTH Gate A and Gate B pass. Any failure on either gate → scope-guard deferral + Phase 242 FIND-01 intake. Grep does not have co-equal warrant weight (D-04); a Gate B miss blocks ONLY-ness but does not retroactively amend Phase 237 inventory.

## D-09 — Closed-verdict taxonomy (per ONLY-ness table row — no hedged verdicts)

Each row ends in exactly one of: `CONFIRMED_SOLE_EXCEPTION_GROUP_EXC_01` | `CONFIRMED_SOLE_EXCEPTION_GROUP_EXC_02` | `CONFIRMED_SOLE_EXCEPTION_GROUP_EXC_03` | `CONFIRMED_SOLE_EXCEPTION_GROUP_EXC_04` | `NOT_PLAYER_REACHABLE_OUT_OF_SCOPE` | `CANDIDATE_FINDING`.

Overall ONLY-ness claim ends in exactly one of: `ONLY_NESS_HOLDS_AT_HEAD` | `CANDIDATE_FINDING`.

Per-section re-verification verdicts (§ 5 / § 6 / § 7) end in exactly one of: `RE_VERIFIED_AT_HEAD` | `CANDIDATE_FINDING`. Per-discharge rows in § 8 end in exactly one of: `DISCHARGED_RE_VERIFIED_AT_HEAD` | `CANDIDATE_FINDING`.

## D-10 — Predicate re-verification targets (EXC-02 / EXC-03 / EXC-04)

### EXC-02 predicates (two — both must hold)
1. **Single-call-site predicate:** grep `contracts/` for all callers of `_getHistoricalRngFallback`; sole caller MUST be `_gameOverEntropy` at `contracts/modules/DegenerusGameAdvanceModule.sol:1252`.
2. **14-day gate predicate:** confirm `GAMEOVER_RNG_FALLBACK_DELAY = 14 days` constant intact at `contracts/modules/DegenerusGameAdvanceModule.sol:109`; confirm the gate check still guards every reachable path into the fallback at HEAD.

Section-level verdict: `EXC-02 RE_VERIFIED_AT_HEAD` iff both predicates hold.

### EXC-03 predicates (tri-gate — all three must hold)
1. **Terminal-state predicate:** substitution fires only inside `_gameOverEntropy` reachable control flow at `contracts/modules/DegenerusGameAdvanceModule.sol:1222-1246`; no non-gameover caller at HEAD.
2. **No-player-reachable-timing predicate:** gameover trigger surfaces (120-day liveness stall + pool deficit) are the ONLY entries; no player call can time a game-over against a specific mid-cycle write-buffer state. CROSS-CITE Phase 240 GO-04 `DISPROVEN_PLAYER_REACHABLE_VECTOR` as corroborating.
3. **Buffer-scope predicate:** substitution applies only to tickets in the post-swap write buffer populated by `_swapAndFreeze(purchaseLevel)` at `contracts/modules/DegenerusGameAdvanceModule.sol:292` or `_swapTicketSlot(purchaseLevel_)` at `:1082`. CROSS-CITE Phase 240 GO-05 `BOTH_DISJOINT` as corroborating.

Section-level verdict: `EXC-03 RE_VERIFIED_AT_HEAD` iff all three predicates hold.

### EXC-04 predicate — two-part (both must hold)

**Actual EntropyLib architecture at HEAD (READ-verified from `contracts/libraries/EntropyLib.sol:16-23`):** `entropyStep(uint256 state)` is a pure XOR-shift function with ZERO keccak calls inside it — body is `state ^= state << 7;`, `state ^= state >> 9;`, `state ^= state << 8;` inside an `unchecked` block. The keccak seed construction `keccak256(abi.encode(rngWord, ...))` lives at **caller sites**, NOT inside entropyStep. `EntropyLib.hash2(uint256 a, uint256 b)` is a separate scratch-slot keccak helper (also not part of entropyStep).

1. **EXC-04-P1a — EntropyLib.entropyStep body intact:** the XOR-shift body at `contracts/libraries/EntropyLib.sol:16-23` is unchanged at HEAD `7ab515fe`. Verify by: (a) function signature `function entropyStep(uint256 state) internal pure returns (uint256)` present at `:16`; (b) body contains exactly `state ^= state << 7;`, `state ^= state >> 9;`, `state ^= state << 8;` inside an `unchecked { ... }` block at `:17-21`.
2. **EXC-04-P1b — Caller-site keccak seed construction:** every `EntropyLib.entropyStep` call site in `contracts/` receives `state`/`entropy` pre-derived via `keccak256(abi.encode(rngWord, ...))` where `rngWord` is VRF-derived. Grep target: `keccak256\(abi\.encode\(rngWord` — MUST produce hits at caller sites across Lootbox + Jackpot + Staked modules (NOT zero). Enumerate each hit and trace `rngWord` provenance to a VRF callback (`rawFulfillRandomWords` / `lootboxRngWordByIndex` / `rngWordByDay` / daily-RNG slot).

Section-level verdict: `EXC-04 RE_VERIFIED_AT_HEAD` iff BOTH P1a AND P1b hold across all 8 Phase 237 EXC-04 rows AND no new non-VRF call site exists.

## D-11 — Forward-Cite Discharge Ledger expected counts

- **EXC-02 forward-cites to discharge:** 17 rows. Source: `audit/v30-GAMEOVER-JACKPOT-SAFETY.md` `See Phase 241 EXC-02` tokens.
- **EXC-03 forward-cites to discharge:** 12 rows. Source: `audit/v30-GAMEOVER-JACKPOT-SAFETY.md` `See Phase 241 EXC-03` tokens.
- **Total:** 29 discharge rows.

Ledger row format (minimum columns per D-24 Claude's Discretion): `EXC-241-NNN | forward-cite source file:line | Phase 241 discharging row ID | discharge verdict | predicate used`.

Verdict per discharge: `DISCHARGED_RE_VERIFIED_AT_HEAD` or `CANDIDATE_FINDING` (if predicate fails at HEAD).

## D-24 — Mandatory 10-section structure for `audit/v30-EXCEPTION-CLOSURE.md`

**Structural convention (matches Phase 238 / Phase 240 precedent in `audit/v30-FREEZE-PROOF.md` + `audit/v30-GAMEOVER-JACKPOT-SAFETY.md`):** § 1 is a YAML `---` frontmatter block at the very top of the file (NO markdown heading). § 2 Executive Summary is the FIRST markdown heading (`## 2. Executive Summary`). Per-section markdown headings start at § 2 and use numeric prefix `## N.` where `N` runs 2..10.

1. Frontmatter + HEAD anchor (`7ab515fe`) — YAML `---` block at top (NO `## 1.` heading; D-24 section number assigned to the YAML block as a logical section)
2. Executive Summary (closure verdicts for EXC-01/02/03/04 + combined ONLY-ness verdict) — FIRST markdown heading `## 2. Executive Summary`
3. EXC-01 Consolidated ONLY-ness Table (22 rows, closed set of 4 KI groups) — `## 3.`
4. EXC-01 Grep Backstop Classification (Gate B evidence over D-07 surface universe) — `## 4.`
5. EXC-02 Predicate Re-Verification (single-call-site + 14-day gate) — `## 5.`
6. EXC-03 Tri-Gate Predicate Re-Verification (terminal + no-player-timing + buffer-scope) — `## 6.`
7. EXC-04 EntropyLib Seed-Construction Re-Verification — `## 7.`
8. Forward-Cite Discharge Ledger (29 discharges per D-11) — `## 8.` with sub-headings `### 8a.` + `### 8b.`
9. Prior-Artifact Cross-Cites (minimum 3 `re-verified at HEAD 7ab515fe` instances per D-13) — `## 9.`
10. Finding Candidates + Scope-Guard Deferrals + Attestation — `## 10.`

## Row-ID Prefix (D-23)

All rows in `v30-EXCEPTION-CLOSURE.md` use `EXC-241-NNN` three-digit zero-padded (matches Phase 237 D-06 / Phase 239 D-25 / Phase 240 D-04 naming convention).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 — EXC-01 ONLY-ness Table + Dual-Gate Closure (D-06 / D-07 / D-08 / D-09)</name>

  <read_first>
    - `audit/v30-EXCEPTION-CLOSURE.md` (if it exists from prior task run — seed the file fresh if not)
    - `audit/v30-CONSUMER-INVENTORY.md` (Phase 237 — READ the 146-row Consumer Index + 22 EXCEPTION rows + KI Cross-Ref Distribution table)
    - `audit/v30-FREEZE-PROOF.md` (Phase 238 — READ the 22-EXCEPTION / 124-SAFE distribution for Gate A cross-check)
    - `KNOWN-ISSUES.md` (READ the 4 KI entries: affiliate / prevrandao / F-29-04 / EntropyLib as SUBJECTS)
    - `contracts/DegenerusAffiliate.sol` line range covering `:568` and `:585` (EXC-01 subject sites)
    - `contracts/modules/DegenerusGameAdvanceModule.sol` line range covering `:109`, `:292`, `:1082`, `:1222-1246`, `:1252`, `:1301` (EXC-02/03 subject sites — needed for row-level seed classification)
    - `contracts/libraries/EntropyLib.sol` (EXC-04 subject — `entropyStep()` definition region)
    - Entire `contracts/` tree via grep for D-07 surface universe (Gate B backstop): `block.timestamp`, `block.number`, `block.prevrandao`, `blockhash`, `msg.sender`, `keccak256`, `currentDayIndex`, `storedCode`
  </read_first>

  <files>audit/v30-EXCEPTION-CLOSURE.md</files>

  <action>
    Create `audit/v30-EXCEPTION-CLOSURE.md` (new file) with a YAML `---` frontmatter block at the very top of the file (NO `## 1.` markdown heading — see D-24 convention in `<interfaces>`; matches Phase 238 `audit/v30-FREEZE-PROOF.md` + Phase 240 `audit/v30-GAMEOVER-JACKPOT-SAFETY.md`). Include `head_anchor: 7ab515fe` + phase + requirements + write policy READ-only inside the YAML block. Do NOT emit a `## 1. Frontmatter` heading. Task 5 will later write § 2 (Executive Summary) as the FIRST markdown heading; for this task, § 3 and § 4 are the only markdown-heading sections emitted.

    Build two top-of-file sections per D-24 § 3 and § 4:

    **§ 3 — EXC-01 Consolidated ONLY-ness Table (22 rows, closed set per D-06):**
    - Walk Phase 237 Consumer Index 146 rows at HEAD `7ab515fe`. For each row, derive seed-source verdict from the exact set: `VRF_DERIVED` / `NON_VRF_PER_KI_EXC_01` / `NON_VRF_PER_KI_EXC_02` / `NON_VRF_PER_KI_EXC_03` / `NON_VRF_PER_KI_EXC_04` / `CANDIDATE_FINDING` (D-04 inventory-walk primary).
    - Emit table with EXACTLY 22 rows (the 2 EXC-01 + 8 EXC-02 + 4 EXC-03 + 8 EXC-04 rows — any row with verdict `VRF_DERIVED` is EXCLUDED from this table; the remaining 124 `VRF_DERIVED` rows are implicitly certified by Gate A's set-equality check against Phase 238's 124-SAFE count, they are NOT enumerated here).
    - Row-ID prefix `EXC-241-NNN` (D-23), three-digit zero-padded, sequential 001..022.
    - Required columns: `EXC-241-NNN | INV-237-NNN | File:Line | KI Group | Seed-Source Verdict | Player-Reachable Exploitability | Closed Verdict`.
    - `Closed Verdict` column values per D-09: `CONFIRMED_SOLE_EXCEPTION_GROUP_EXC_01` (2 rows for affiliate) | `CONFIRMED_SOLE_EXCEPTION_GROUP_EXC_02` (8 rows for prevrandao) | `CONFIRMED_SOLE_EXCEPTION_GROUP_EXC_03` (4 rows for F-29-04) | `CONFIRMED_SOLE_EXCEPTION_GROUP_EXC_04` (8 rows for EntropyLib) | `NOT_PLAYER_REACHABLE_OUT_OF_SCOPE` (per D-05 non-player surfaces — expected 0 rows if all 22 are KI-categorized) | `CANDIDATE_FINDING` (if a row cannot be placed in any of the four KI groups at HEAD).
    - `Player-Reachable Exploitability` column per D-05 exploitability frame: for EXC-01 describe the player-reachable timing-of-purchase influence on `currentDayIndex()` → affiliate credit redirect (no protocol value extraction); for EXC-02 record `VALIDATOR_ONLY_AFTER_14_DAYS` (not player-reachable in the required 14-day window); for EXC-03 record `NO_PLAYER_REACHABLE_TIMING` cross-citing Phase 240 GO-04 `DISPROVEN_PLAYER_REACHABLE_VECTOR`; for EXC-04 record `VRF_SEED_FROZEN_BY_CALLER_SITE_KECCAK` (caller-site `keccak256(abi.encode(rngWord, ...))` construction with VRF-derived `rngWord`) cross-citing Phase 238-03 `lootbox-index-advance` Named Gate.
    - Immediately below the table, emit a `Gate A — Set-Equality Check` sub-section that performs the set-equality assertion: the 22 EXC-241-NNN rows listed ↔ the exact set `{INV-237-005, INV-237-006} ∪ {8 EXC-02 prevrandao rows} ∪ {INV-237-024, INV-237-045, INV-237-053, INV-237-054} ∪ {8 EXC-04 XOR-shift-seed rows}`. Cite Phase 238 `audit/v30-FREEZE-PROOF.md` 22-EXCEPTION / 124-SAFE distribution as cross-check corroboration with `re-verified at HEAD 7ab515fe` backtick-quoted note per D-13. Verdict line: `GATE_A_PASSES` or `GATE_A_FAILS → scope-guard deferral to Phase 242 FIND-01`.

    **§ 4 — EXC-01 Grep Backstop Classification (Gate B evidence over D-07 surface universe):**
    - Run grep across `contracts/` (excluding `contracts/test/` and `contracts/mocks/` if present) for EACH surface in the D-07 universe copied verbatim:
      - `block.timestamp` (direct or indirect via `currentDayIndex()`)
      - `block.number`
      - `block.prevrandao`
      - `blockhash(...)`
      - packed state counters used as entropy (`currentDayIndex()`, `storedCode`, nonce-like reads)
      - `msg.sender` used as seed input
      - `keccak256(...)` hashes over non-VRF-committed state feeding an RNG consumer
    - Preserve the literal grep commands in a reproducibility sub-block (Phase 239 Plan 02 precedent — carry forward the grep-preserve pattern).
    - Classify EACH grep hit into the closed D-07 classification vocabulary: `ORTHOGONAL_NOT_RNG_CONSUMED` | `BELONGS_TO_KI_EXC_01` | `BELONGS_TO_KI_EXC_02` | `BELONGS_TO_KI_EXC_03` | `BELONGS_TO_KI_EXC_04` | `CANDIDATE_FINDING`.
    - Required columns: `Surface | Grep Pattern | Hits | file:line | Classification | Notes`.
    - Verdict line: `GATE_B_PASSES` (zero `CANDIDATE_FINDING` rows) or `GATE_B_FAILS → scope-guard deferral to Phase 242 FIND-01`.

    **Combined closure verdict** (at end of § 4): `ONLY_NESS_HOLDS_AT_HEAD` iff BOTH Gate A and Gate B pass (D-08). On failure, emit per-gate `CANDIDATE_FINDING` entry for § 10 (Task 5 will consolidate).

    D-05 frame compliance: verdicts on the 22 rows MUST frame exploitability (player-reachable timing / no-player-reachable / validator-only-after-14-days / caller-site-keccak-seeded-by-VRF), NOT distribution quality. Do NOT re-litigate XOR-shift distribution, prevrandao 1-bit proposer bias, or deterministic affiliate seed quality.

    D-12/D-13 discipline: every cross-cite to Phase 237/238 MUST carry a `re-verified at HEAD 7ab515fe` backtick-quoted note with a one-line structural-equivalence statement. Target minimum 1 such note in § 3 (remaining instances accumulated in later tasks to reach >=3 plan-wide per D-13).

    D-20 discipline: zero `F-30-NN` finding IDs emitted. Any gap → `CANDIDATE_FINDING` entry routed to Task 5 § 10 (Phase 242 FIND-01 intake, NOT F-30-NN).

    D-26/D-27 discipline: zero `contracts/` or `test/` writes. Phase 237/238/239/240 outputs READ-only — verify via `git status --porcelain audit/` at end of task (no unexpected modifications outside `audit/v30-EXCEPTION-CLOSURE.md`).
  </action>

  <verify>
    <automated>test -f audit/v30-EXCEPTION-CLOSURE.md && head -1 audit/v30-EXCEPTION-CLOSURE.md | grep -q '^---$' && grep -q '^head_anchor: 7ab515fe$' audit/v30-EXCEPTION-CLOSURE.md && grep -q '^## 3\. EXC-01 Consolidated ONLY-ness Table' audit/v30-EXCEPTION-CLOSURE.md && grep -q '^## 4\. EXC-01 Grep Backstop Classification' audit/v30-EXCEPTION-CLOSURE.md && [ "$(grep -cE '^\| EXC-241-0(0[1-9]|1[0-9]|2[0-2]) \|' audit/v30-EXCEPTION-CLOSURE.md)" -ge 22 ] && grep -qE 'GATE_A_(PASSES|FAILS)' audit/v30-EXCEPTION-CLOSURE.md && grep -qE 'GATE_B_(PASSES|FAILS)' audit/v30-EXCEPTION-CLOSURE.md && grep -qE 'ONLY_NESS_HOLDS_AT_HEAD|CANDIDATE_FINDING' audit/v30-EXCEPTION-CLOSURE.md && ! grep -q '^## 1\. Frontmatter' audit/v30-EXCEPTION-CLOSURE.md && [ -z "$(git status --porcelain contracts/ test/)" ]</automated>
  </verify>

  <acceptance_criteria>
    - `audit/v30-EXCEPTION-CLOSURE.md` exists with a YAML `---` frontmatter block at line 1 (NO `## 1. Frontmatter` markdown heading — D-24 convention; matches Phase 238/240 precedent)
    - YAML frontmatter contains `head_anchor: 7ab515fe`
    - `§ 3 EXC-01 Consolidated ONLY-ness Table` present and contains exactly 22 rows matching the regex `^\| EXC-241-0(0[1-9]|1[0-9]|2[0-2]) \|` (grep -cE yields 22)
    - The 22 EXC-241-NNN rows map bijectively to Phase 237 INV-237-NNN EXCEPTION rows: 2 rows in EXC-01 group (INV-237-005, -006), 8 rows in EXC-02 group, 4 rows in EXC-03 group (INV-237-024, -045, -053, -054), 8 rows in EXC-04 group
    - Every row's `Closed Verdict` column falls in the closed vocabulary per D-09: one of `CONFIRMED_SOLE_EXCEPTION_GROUP_EXC_01|02|03|04` / `NOT_PLAYER_REACHABLE_OUT_OF_SCOPE` / `CANDIDATE_FINDING`
    - `Gate A — Set-Equality Check` sub-section present; verdict line matches `GATE_A_PASSES` or `GATE_A_FAILS`
    - `§ 4 EXC-01 Grep Backstop Classification` present with grep-reproducibility sub-block citing the D-07 surface universe verbatim (`block.timestamp`, `block.number`, `block.prevrandao`, `blockhash`, `msg.sender`, `keccak256`, `currentDayIndex`, `storedCode`)
    - `Gate B` verdict line matches `GATE_B_PASSES` or `GATE_B_FAILS`
    - Combined closure verdict emitted: `ONLY_NESS_HOLDS_AT_HEAD` or gate-failure-routed `CANDIDATE_FINDING`
    - Minimum 1 `re-verified at HEAD 7ab515fe` backtick-quoted note in § 3 or § 4 (plan-wide threshold >=3 verified in Task 5)
    - Zero `F-30-NN` literal strings present: `grep -c 'F-30-' audit/v30-EXCEPTION-CLOSURE.md` yields 0
    - `git status --porcelain contracts/ test/` is empty (READ-only discipline D-26)
  </acceptance_criteria>

  <done>
    § 3 and § 4 of `audit/v30-EXCEPTION-CLOSURE.md` emit the 22-row ONLY-ness table + Gate B grep classification with dual-gate verdict computed. File exists; YAML frontmatter block at top (no `## 1.` heading); `contracts/` and `test/` untouched; Phase 237/238 outputs untouched; no `F-30-NN` IDs emitted.
  </done>
</task>

<task type="auto">
  <name>Task 2 — EXC-02 Predicate Re-Verification + 17-row EXC-02 Forward-Cite Discharge (D-10 / D-11)</name>

  <read_first>
    - `audit/v30-EXCEPTION-CLOSURE.md` (current state as seeded by Task 1)
    - `audit/v30-GAMEOVER-JACKPOT-SAFETY.md` (Phase 240 consolidated — locate ALL 17 `See Phase 241 EXC-02` forward-cite tokens; note the consumer `INV-237-NNN` row each token decorates and the Phase 240 source row ID per token)
    - `audit/v30-240-01-INV-DET.md` (Phase 240 Plan 01 — GO-02 VRF-available-branch determinism proof; 8 of the 17 EXC-02 tokens originate here on `8 EXCEPTION (KI: EXC-02)` rows per STATE.md 240-01 decision log)
    - `audit/v30-240-02-STATE-TIMING.md` (Phase 240 Plan 02 — GO-03 EXC-02 rows + 6 Phase 240 `See Phase 241 EXC-02` forward-cites per STATE.md 240-02 decision log)
    - `audit/v30-RNGLOCK-STATE-MACHINE.md` (Phase 239 RNG-01 — corroborating reachability closure at HEAD `7ab515fe`)
    - `KNOWN-ISSUES.md § "Gameover prevrandao fallback"` — SUBJECT of EXC-02 re-verification (D-12)
    - `contracts/modules/DegenerusGameAdvanceModule.sol` line ranges covering `:109` (constant `GAMEOVER_RNG_FALLBACK_DELAY = 14 days`), `:1240-1260` (around `_gameOverEntropy:1252` trigger site and the delay-gate branch), `:1290-1330` (around `_getHistoricalRngFallback:1301` definition and early-return guards), plus any other reachable caller sites discovered via grep
    - Grep over `contracts/` for ALL callers of `_getHistoricalRngFallback` — sole caller MUST resolve to `_gameOverEntropy:1252` per D-10
  </read_first>

  <files>audit/v30-EXCEPTION-CLOSURE.md</files>

  <action>
    Append `§ 5 — EXC-02 Predicate Re-Verification` and (begin assembling) `§ 8 — Forward-Cite Discharge Ledger` (EXC-02 portion, 17 rows) to `audit/v30-EXCEPTION-CLOSURE.md`.

    **§ 5 — EXC-02 Predicate Re-Verification:**

    Two-predicate table per D-10 (both must hold for EXC-02 verdict to be `RE_VERIFIED_AT_HEAD`):

    - **Predicate EXC-02-P1 — Single-call-site predicate:**
      - Run grep across `contracts/` for all string occurrences of `_getHistoricalRngFallback`.
      - Enumerate hits by `file:line`; classify each hit as `DEFINITION_SITE` (the function declaration at `DegenerusGameAdvanceModule.sol:1301`) or `CALL_SITE` (expected to be exactly one: `_gameOverEntropy` at `DegenerusGameAdvanceModule.sol:1252`) or `COMMENT_OR_DOC` (string in comment, not reachable call).
      - Verdict `RE_VERIFIED_AT_HEAD` iff call-site count == 1 AND the sole call site is `_gameOverEntropy` at `DegenerusGameAdvanceModule.sol:1252`. Otherwise `CANDIDATE_FINDING`.
      - Preserve the literal grep command in a reproducibility sub-block.

    - **Predicate EXC-02-P2 — 14-day gate predicate:**
      - Confirm `GAMEOVER_RNG_FALLBACK_DELAY = 14 days` constant intact at `contracts/modules/DegenerusGameAdvanceModule.sol:109` via grep for the exact literal `GAMEOVER_RNG_FALLBACK_DELAY = 14 days`.
      - Enumerate ALL reachable paths into `_getHistoricalRngFallback` from `_gameOverEntropy`; demonstrate each path is gated by a delay comparison `block.timestamp >= <vrfRequestTimestamp> + GAMEOVER_RNG_FALLBACK_DELAY` (or semantically-equivalent guard). Cite the specific conditional expression and its line.
      - Verdict `RE_VERIFIED_AT_HEAD` iff constant == `14 days` AND every reachable fallback path is gated by the constant. Otherwise `CANDIDATE_FINDING`.

    Required columns in the table: `Predicate ID | Predicate Name | Target file:line | Grep Command | Hits | Gate Expression | HEAD Verdict`.

    **Section-level verdict:** `EXC-02 RE_VERIFIED_AT_HEAD` iff BOTH predicates yield `RE_VERIFIED_AT_HEAD`.

    **CROSS-CITES per D-12 (corroborating only, never sole warrant):**
    - CITE `audit/v30-RNGLOCK-STATE-MACHINE.md` Phase 239 RNG-01 `AIRTIGHT` verdict as corroborating reachability closure for gameover-entry paths into `_gameOverEntropy`. Add a `re-verified at HEAD 7ab515fe` backtick-quoted note with a one-line structural-equivalence statement.
    - CITE `audit/v30-GAMEOVER-JACKPOT-SAFETY.md` Phase 240 GO-02 VRF-available-branch determinism for consumer-level EXC-02 presence. Add a `re-verified at HEAD 7ab515fe` note.

    **§ 8 — Forward-Cite Discharge Ledger (EXC-02 portion — 17 rows):**

    Introduce the ledger table (to be completed in Task 3 with the 12 EXC-03 rows — for now, emit the EXC-02 portion labeled with a sub-heading `### 8a. EXC-02 Forward-Cite Discharges (17 rows)`).

    Columns per D-11 + D-24 Claude's Discretion:
    `EXC-241-NNN | Forward-Cite Source (Phase 240 file:line token) | Phase 240 Source Row ID | Phase 241 Discharging Row/Predicate | Discharge Verdict | Predicate Used`.

    Locate ALL 17 `See Phase 241 EXC-02` forward-cite tokens in `audit/v30-GAMEOVER-JACKPOT-SAFETY.md` (the Phase 240 STATE.md log records 17 EXC-02 tokens across 240-01 + 240-02 + consolidated file — verify count at HEAD by `grep -c 'See Phase 241 EXC-02' audit/v30-GAMEOVER-JACKPOT-SAFETY.md` which MUST equal 17; if fewer, emit `CANDIDATE_FINDING` with scope-guard deferral rather than fabricating missing rows).

    Row-ID prefix `EXC-241-NNN` continuation from Task 1 (so if Task 1 ends at EXC-241-022, EXC-02 discharges occupy EXC-241-023..EXC-241-039).

    Per discharge row:
    - `Forward-Cite Source`: exact `audit/v30-GAMEOVER-JACKPOT-SAFETY.md:<line>` where the `See Phase 241 EXC-02` token appears.
    - `Phase 240 Source Row ID`: the GO-240-NNN or GOVAR-240-NNN or INV-237-NNN row the token decorates.
    - `Phase 241 Discharging Row/Predicate`: name the predicate combination that closes it — typically `EXC-02-P1 (single-call-site) + EXC-02-P2 (14-day gate)`.
    - `Discharge Verdict`: literal string `DISCHARGED_RE_VERIFIED_AT_HEAD` (expected 17 rows) OR literal string `CANDIDATE_FINDING` (if any predicate fails). Verdict column MUST contain exactly one of these two literal strings so ledger-scoped greps discriminate discharge rows from other content.
    - `Predicate Used`: one-sentence justification with literal citation, e.g., `"Fallback reachable only inside _gameOverEntropy:1252 + 14-day delay enforced at :109 — both predicates hold at HEAD 7ab515fe"`.

    D-11 residual handling: if ANY forward-cite cannot be discharged, that row's verdict is `CANDIDATE_FINDING` and routes to § 10 (Task 5 consolidates). Do NOT emit fresh forward-cites (D-11 residual rule).

    D-12/D-13: add at least 2 `re-verified at HEAD 7ab515fe` backtick-quoted notes in § 5 and § 8a (on Phase 239 and Phase 240 cross-cites respectively).

    D-26/D-27/D-20 discipline: zero `contracts/` / `test/` writes; Phase 237/238/239/240 outputs READ-only; no `F-30-NN` IDs.
  </action>

  <verify>
    <automated>grep -q '^## 5\. EXC-02 Predicate Re-Verification' audit/v30-EXCEPTION-CLOSURE.md && grep -q '^## 8\. Forward-Cite Discharge Ledger' audit/v30-EXCEPTION-CLOSURE.md && grep -q '^### 8a\. EXC-02 Forward-Cite Discharges' audit/v30-EXCEPTION-CLOSURE.md && grep -qE 'EXC-02-P1|single-call-site' audit/v30-EXCEPTION-CLOSURE.md && grep -qE 'EXC-02-P2|14-day|GAMEOVER_RNG_FALLBACK_DELAY = 14 days' audit/v30-EXCEPTION-CLOSURE.md && [ "$(awk '/^### 8a\. EXC-02/,/^### 8b\.|^## 9\./' audit/v30-EXCEPTION-CLOSURE.md | grep -cE '^\| EXC-241-[0-9]{3} \|.*(DISCHARGED_RE_VERIFIED_AT_HEAD|CANDIDATE_FINDING) \|')" -ge 17 ] && [ "$(grep -c 'DISCHARGED_RE_VERIFIED_AT_HEAD' audit/v30-EXCEPTION-CLOSURE.md)" -ge 17 ] && grep -q 're-verified at HEAD 7ab515fe' audit/v30-EXCEPTION-CLOSURE.md && [ "$(grep -c 'F-30-' audit/v30-EXCEPTION-CLOSURE.md)" -eq 0 ] && [ -z "$(git status --porcelain contracts/ test/)" ]</automated>
  </verify>

  <acceptance_criteria>
    - `§ 5 EXC-02 Predicate Re-Verification` section present
    - Predicate EXC-02-P1 (single-call-site) table row present; HEAD verdict column populated; grep command preserved
    - Predicate EXC-02-P2 (14-day gate) table row present; targets `DegenerusGameAdvanceModule.sol:109`; HEAD verdict column populated
    - Section-level verdict line `EXC-02 RE_VERIFIED_AT_HEAD` or `CANDIDATE_FINDING` emitted
    - `§ 8 Forward-Cite Discharge Ledger` introduced with subsection `§ 8a EXC-02 Forward-Cite Discharges (17 rows)`
    - The 8a table contains 17 discharge rows, one per Phase 240 `See Phase 241 EXC-02` token (row-based count scoped to § 8a via awk range `/^### 8a\. EXC-02/,/^### 8b\.|^## 9\./` yields ≥17 rows matching `^\| EXC-241-[0-9]{3} \|.*(DISCHARGED_RE_VERIFIED_AT_HEAD|CANDIDATE_FINDING) \|`)
    - Every EXC-02 discharge row cites a concrete `audit/v30-GAMEOVER-JACKPOT-SAFETY.md:<line>` source token
    - Every EXC-02 discharge verdict is literally `DISCHARGED_RE_VERIFIED_AT_HEAD` (expected 17 / 17) or literally `CANDIDATE_FINDING` — the verdict literal `DISCHARGED_RE_VERIFIED_AT_HEAD` is specific to ledger discharge rows and MUST appear ≥17 times file-wide
    - Plan-wide `re-verified at HEAD 7ab515fe` backtick-quoted-phrase instance count >= 2 (accumulating toward >=3 per D-13; final threshold verified in Task 5)
    - Zero `F-30-NN` literal strings present
    - `git status --porcelain contracts/ test/` empty (READ-only D-26)
    - Phase 237/238/239/240 outputs untouched (`git status --porcelain audit/v30-CONSUMER-INVENTORY.md audit/v30-FREEZE-PROOF.md audit/v30-RNGLOCK-STATE-MACHINE.md audit/v30-PERMISSIONLESS-SWEEP.md audit/v30-ASYMMETRY-RE-JUSTIFICATION.md audit/v30-GAMEOVER-JACKPOT-SAFETY.md audit/v30-240-01-INV-DET.md audit/v30-240-02-STATE-TIMING.md audit/v30-240-03-SCOPE.md` all empty — D-27)
  </acceptance_criteria>

  <done>
    § 5 EXC-02 predicate re-verification complete (single-call-site + 14-day gate both verdicted); § 8a Forward-Cite Discharge Ledger contains the 17 EXC-02 discharge rows each tied to a Phase 240 source token with the literal verdict string `DISCHARGED_RE_VERIFIED_AT_HEAD` or `CANDIDATE_FINDING`. Phase 240 forward-cite closure for EXC-02 portion complete.
  </done>
</task>

<task type="auto">
  <name>Task 3 — EXC-03 Tri-Gate Predicate Re-Verification + 12-row EXC-03 Forward-Cite Discharge (D-10 / D-11)</name>

  <read_first>
    - `audit/v30-EXCEPTION-CLOSURE.md` (current state with Task 1 + Task 2 output)
    - `audit/v30-GAMEOVER-JACKPOT-SAFETY.md` (Phase 240 consolidated — locate ALL 12 `See Phase 241 EXC-03` forward-cite tokens; note the consumer row each decorates)
    - `audit/v30-240-01-INV-DET.md` (Phase 240 — GO-02 4 F-29-04 rows `EXCEPTION (KI: EXC-03)`)
    - `audit/v30-240-02-STATE-TIMING.md` (Phase 240 — GO-04 2 `GOTRIG-240-NNN DISPROVEN_PLAYER_REACHABLE_VECTOR` rows for no-player-timing cross-cite; GO-03 3 EXC-03 rows)
    - `audit/v30-240-03-SCOPE.md` (Phase 240 — GO-05 `BOTH_DISJOINT` verdict for buffer-scope cross-cite)
    - `KNOWN-ISSUES.md § "Gameover RNG substitution for mid-cycle write-buffer tickets"` — SUBJECT of EXC-03 (D-12)
    - `contracts/modules/DegenerusGameAdvanceModule.sol` line ranges covering `:292` (`_swapAndFreeze(purchaseLevel)` daily RNG request trigger), `:1082` (`_swapTicketSlot(purchaseLevel_)` mid-day lootbox RNG request trigger), `:1222-1246` (`_gameOverEntropy` substitution site), plus enumeration of gameover-trigger surfaces (120-day liveness stall + pool deficit — roughly `advanceGame` early branches) to confirm no-player-reachable-timing predicate
    - v29.0 Phase 235 Plan 04 F-29-04 commitment-window trace artifact (if present as `.planning/milestones/v29.0-phases/235-*/audit/...` — corroborating prior-milestone per D-12; READ-only)
  </read_first>

  <files>audit/v30-EXCEPTION-CLOSURE.md</files>

  <action>
    Append `§ 6 — EXC-03 Tri-Gate Predicate Re-Verification` and extend `§ 8` with sub-heading `### 8b. EXC-03 Forward-Cite Discharges (12 rows)`.

    **§ 6 — EXC-03 Tri-Gate Predicate Re-Verification:**

    Three-predicate table per D-10 (all three MUST hold for EXC-03 verdict to be `RE_VERIFIED_AT_HEAD`):

    - **Predicate EXC-03-P1 — Terminal-state predicate:**
      - Confirm the substitution fires only inside `_gameOverEntropy` reachable control flow at `contracts/modules/DegenerusGameAdvanceModule.sol:1222-1246`.
      - Grep for all reachable call chains leading to the substitution region; enumerate them; confirm no non-gameover caller at HEAD.
      - Verdict `RE_VERIFIED_AT_HEAD` iff the substitution site is reachable ONLY via `_gameOverEntropy` control flow.

    - **Predicate EXC-03-P2 — No-player-reachable-timing predicate:**
      - Enumerate gameover trigger surfaces (120-day liveness stall + pool deficit) as the ONLY entries into `_gameOverEntropy`.
      - Confirm no player call can time a game-over against a specific mid-cycle write-buffer state.
      - CROSS-CITE Phase 240 GO-04 2 `GOTRIG-240-NNN DISPROVEN_PLAYER_REACHABLE_VECTOR` rows as corroborating (one cite per trigger: 120-day liveness + pool deficit). Add `re-verified at HEAD 7ab515fe` backtick-quoted note.
      - Verdict `RE_VERIFIED_AT_HEAD` iff both triggers remain player-unreachable for mid-cycle state alignment.

    - **Predicate EXC-03-P3 — Buffer-scope predicate:**
      - Confirm the substitution applies only to tickets in the post-swap write buffer populated by `_swapAndFreeze(purchaseLevel)` at `contracts/modules/DegenerusGameAdvanceModule.sol:292` or `_swapTicketSlot(purchaseLevel_)` at `:1082`.
      - Enumerate the 6 F-29-04 write-buffer-swap primitive storage slots per Phase 240 GO-05 decision log (e.g., `ticketWriteSlot`, `ticketsFullyProcessed`, `ticketQueue[]`, `ticketsOwedPacked[][]`, `ticketCursor`, `ticketLevel`).
      - CROSS-CITE Phase 240 GO-05 `BOTH_DISJOINT` verdict (inventory-level + state-variable-level disjointness) as corroborating. Add `re-verified at HEAD 7ab515fe` backtick-quoted note.
      - Verdict `RE_VERIFIED_AT_HEAD` iff the buffer primitive boundary is unchanged at HEAD.

    Required columns: `Predicate ID | Predicate Name | Target file:line(s) | HEAD Evidence | Cross-Cite | HEAD Verdict`.

    **Section-level verdict:** `EXC-03 RE_VERIFIED_AT_HEAD` iff all THREE predicates yield `RE_VERIFIED_AT_HEAD`.

    **§ 8b — EXC-03 Forward-Cite Discharges (12 rows):**

    Locate ALL 12 `See Phase 241 EXC-03` forward-cite tokens in `audit/v30-GAMEOVER-JACKPOT-SAFETY.md`. The Phase 240 STATE.md log records 12 EXC-03 tokens (4 from Plan 240-01 on `EXCEPTION (KI: EXC-03)` rows + balance from Plan 240-02 GO-03 / Plan 240-03 GO-05). Verify count at HEAD: `grep -c 'See Phase 241 EXC-03' audit/v30-GAMEOVER-JACKPOT-SAFETY.md` MUST equal 12; if fewer, emit `CANDIDATE_FINDING` with scope-guard deferral (D-11 residual handling).

    Row-ID prefix `EXC-241-NNN` continuation from Task 2 — EXC-03 discharges occupy `EXC-241-NNN` in sequence (so if EXC-02 discharges ended at EXC-241-039, EXC-03 discharges occupy EXC-241-040..EXC-241-051).

    Columns identical to § 8a:
    `EXC-241-NNN | Forward-Cite Source (Phase 240 file:line token) | Phase 240 Source Row ID | Phase 241 Discharging Row/Predicate | Discharge Verdict | Predicate Used`.

    Per discharge row:
    - `Phase 241 Discharging Row/Predicate`: name the predicate combination that closes it — typically `EXC-03-P1 (terminal-state) + EXC-03-P2 (no-player-reachable-timing) + EXC-03-P3 (buffer-scope)`.
    - `Discharge Verdict`: literal string `DISCHARGED_RE_VERIFIED_AT_HEAD` (expected 12 rows) OR literal string `CANDIDATE_FINDING`. Verdict column MUST contain exactly one of these two literal strings so ledger-scoped greps discriminate discharge rows from other content.
    - `Predicate Used`: one-sentence justification referencing tri-gate predicates + specific buffer primitive, e.g., `"Terminal-state-only via _gameOverEntropy:1222-1246; no player-reachable trigger timing (Phase 240 GO-04 DISPROVEN_PLAYER_REACHABLE_VECTOR); buffer scope bounded to post-swap primitives at :292, :1082 (Phase 240 GO-05 BOTH_DISJOINT)"`.

    D-17 discipline: Phase 241 CITES Phase 240 GO-05 `BOTH_DISJOINT` but does NOT re-derive it. Tri-gate here ADDS terminal-state + no-player-timing + buffer-scope predicates on top of the existing Phase 240 disjointness verdict (which stands).

    D-12/D-13: add at least 2 `re-verified at HEAD 7ab515fe` backtick-quoted notes in § 6 and § 8b on Phase 240 cross-cites (GO-04, GO-05) + v29.0 Phase 235 Plan 04 F-29-04 commitment-window (if referenced as prior-milestone corroborating).

    D-26/D-27/D-20 discipline: zero `contracts/` / `test/` writes; Phase 237/238/239/240 outputs READ-only; no `F-30-NN` IDs.
  </action>

  <verify>
    <automated>grep -q '^## 6\. EXC-03 Tri-Gate Predicate Re-Verification' audit/v30-EXCEPTION-CLOSURE.md && grep -qE 'EXC-03-P1|terminal-state' audit/v30-EXCEPTION-CLOSURE.md && grep -qE 'EXC-03-P2|no-player-reachable-timing|DISPROVEN_PLAYER_REACHABLE_VECTOR' audit/v30-EXCEPTION-CLOSURE.md && grep -qE 'EXC-03-P3|buffer-scope|BOTH_DISJOINT' audit/v30-EXCEPTION-CLOSURE.md && grep -q '^### 8b\. EXC-03 Forward-Cite Discharges' audit/v30-EXCEPTION-CLOSURE.md && [ "$(awk '/^### 8b\. EXC-03/,/^## 9\./' audit/v30-EXCEPTION-CLOSURE.md | grep -cE '^\| EXC-241-[0-9]{3} \|.*(DISCHARGED_RE_VERIFIED_AT_HEAD|CANDIDATE_FINDING) \|')" -ge 12 ] && [ "$(grep -c 'DISCHARGED_RE_VERIFIED_AT_HEAD' audit/v30-EXCEPTION-CLOSURE.md)" -ge 29 ] && [ "$(grep -c 're-verified at HEAD 7ab515fe' audit/v30-EXCEPTION-CLOSURE.md)" -ge 3 ] && [ "$(grep -c 'F-30-' audit/v30-EXCEPTION-CLOSURE.md)" -eq 0 ] && [ -z "$(git status --porcelain contracts/ test/)" ]</automated>
  </verify>

  <acceptance_criteria>
    - `§ 6 EXC-03 Tri-Gate Predicate Re-Verification` section present
    - All three predicate rows present: EXC-03-P1 (terminal-state), EXC-03-P2 (no-player-reachable-timing), EXC-03-P3 (buffer-scope)
    - EXC-03-P1 cites `contracts/modules/DegenerusGameAdvanceModule.sol:1222-1246` as target
    - EXC-03-P2 cross-cites Phase 240 GO-04 `DISPROVEN_PLAYER_REACHABLE_VECTOR` (both 120-day liveness + pool-deficit triggers)
    - EXC-03-P3 cites buffer primitives `_swapAndFreeze:292` + `_swapTicketSlot:1082` + cross-cites Phase 240 GO-05 `BOTH_DISJOINT`
    - Section-level verdict line `EXC-03 RE_VERIFIED_AT_HEAD` or `CANDIDATE_FINDING` emitted
    - `§ 8b EXC-03 Forward-Cite Discharges (12 rows)` present with 12 discharge rows (row-based count scoped to § 8b via awk range `/^### 8b\. EXC-03/,/^## 9\./` yields ≥12 rows matching `^\| EXC-241-[0-9]{3} \|.*(DISCHARGED_RE_VERIFIED_AT_HEAD|CANDIDATE_FINDING) \|`)
    - File-wide count of literal string `DISCHARGED_RE_VERIFIED_AT_HEAD` ≥ 29 (17 EXC-02 from Task 2 + 12 EXC-03 from Task 3)
    - Every EXC-03 discharge row cites a concrete `audit/v30-GAMEOVER-JACKPOT-SAFETY.md:<line>` source token AND the predicate combination used
    - Plan-wide `re-verified at HEAD 7ab515fe` count >= 3 (D-13 minimum met by end of Task 3)
    - Zero `F-30-NN` literal strings present
    - `git status --porcelain contracts/ test/` empty (D-26)
  </acceptance_criteria>

  <done>
    § 6 EXC-03 tri-gate predicate re-verification complete (terminal-state + no-player-timing + buffer-scope all verdicted). § 8b Forward-Cite Discharge Ledger contains the 12 EXC-03 discharge rows each tied to a Phase 240 source token with the literal verdict string `DISCHARGED_RE_VERIFIED_AT_HEAD` or `CANDIDATE_FINDING`. Combined with § 8a, all 29 Phase 240 forward-cite tokens are line-item discharged.
  </done>
</task>

<task type="auto">
  <name>Task 4 — EXC-04 EntropyLib Body Intact + Caller-Site Keccak Seed Re-Verification (D-10)</name>

  <read_first>
    - `audit/v30-EXCEPTION-CLOSURE.md` (current state with Tasks 1/2/3 output)
    - `audit/v30-CONSUMER-INVENTORY.md` (Phase 237 — READ rows tagged `exception-xor-shift-seed`-adjacent — 8 rows per 237-02 decision log; per Phase 238-03 decision log: INV-237-124, -131, -132, -134, -135, -136, -137, -138)
    - `audit/v30-FREEZE-PROOF.md` (Phase 238 — READ the `lootbox-index-advance` Named Gate assignment for EXC-04 rows per 238-03 decision log)
    - `KNOWN-ISSUES.md § "EntropyLib XOR-shift PRNG for lootbox outcome rolls"` — SUBJECT of EXC-04 (D-12)
    - `contracts/libraries/EntropyLib.sol` (entire file — READ lines 16-23 for the `entropyStep` XOR-shift body; confirm ZERO keccak calls inside entropyStep; note the separate `hash2` helper at lines 36-42 is NOT part of entropyStep and is NOT the EXC-04 subject)
    - `contracts/modules/DegenerusGameLootboxModule.sol` (READ lines around the 7 `EntropyLib.entropyStep` call sites at `:813`, `:817`, `:1548`, `:1569`, `:1585`, `:1599`, `:1635` AND the caller-site keccak seed constructions at `:554`, `:628`, `:673`, `:708`, `:1753` — verify each entropyStep call receives state pre-derived from a caller-site `keccak256(abi.encode(rngWord, ...))`)
    - `contracts/modules/DegenerusGameJackpotModule.sol` (READ lines around `:1799` caller-site `keccak256(abi.encode(rngWord, lvl, FAR_FUTURE_COIN_TAG))` AND the `EntropyLib.entropyStep(entropy)` call at `:2119`)
    - `contracts/StakedDegenerusStonk.sol` (READ lines around `:660` caller-site `keccak256(abi.encode(rngWord, player))` — note whether this feeds an entropyStep consumer or is an independent keccak-only derivation)
    - `contracts/DegenerusGame.sol` (READ lines around `:1769` caller-site `keccak256(abi.encode(rngWord))` single-arg chain)
    - Grep over `contracts/` for ALL call sites of `EntropyLib.entropyStep` — enumerate every call site (expect 8 actual call sites matching Phase 237 inventory; do NOT count the docstring comment at `DegenerusGameJackpotModule.sol:43` which is a doc-comment line, not a call site); confirm each passes entropy pre-derived from a caller-site keccak
    - Grep over `contracts/` for `keccak256\(abi\.encode\(rngWord` — MUST produce hits at caller sites (NOT zero); enumerate each hit; trace `rngWord` provenance to a VRF callback
  </read_first>

  <files>audit/v30-EXCEPTION-CLOSURE.md</files>

  <action>
    Append `§ 7 — EXC-04 EntropyLib Seed-Construction Re-Verification` to `audit/v30-EXCEPTION-CLOSURE.md`.

    **CRITICAL ARCHITECTURAL NOTE (READ-verified from `contracts/libraries/EntropyLib.sol:16-23` at HEAD `7ab515fe`):** `EntropyLib.entropyStep(uint256 state)` is a pure XOR-shift function with ZERO keccak calls inside it. The keccak seed construction lives at **caller sites**, NOT inside `entropyStep`. Any plan language implying `entropyStep` contains a keccak call is factually wrong; EXC-04 re-verification must address TWO separate artifacts: (P1a) the `entropyStep` XOR-shift body at `EntropyLib.sol:16-23`, and (P1b) the caller-site `keccak256(abi.encode(rngWord, ...))` constructions at Lootbox / Jackpot / Staked / DegenerusGame modules that feed entropy into `entropyStep`.

    **§ 7 — EXC-04 EntropyLib Seed-Construction Re-Verification:**

    Two-part predicate per D-10 (BOTH parts must hold for `EXC-04 RE_VERIFIED_AT_HEAD`):

    - **Predicate EXC-04-P1a — EntropyLib.entropyStep body intact:**
      - READ `contracts/libraries/EntropyLib.sol:16-23` at HEAD `7ab515fe`.
      - Verify function signature at `:16`: `function entropyStep(uint256 state) internal pure returns (uint256)`.
      - Verify body wrapped in `unchecked { ... }` block at `:17-21` and contains EXACTLY these three XOR-shift lines (in this order): `state ^= state << 7;`, `state ^= state >> 9;`, `state ^= state << 8;`.
      - Verify the function returns `state` at `:22` and closes at `:23`.
      - Confirm there are ZERO keccak/hash/library calls inside `entropyStep`'s body.
      - Grep target: `function entropyStep\(uint256 state\) internal pure` in `contracts/libraries/EntropyLib.sol` — MUST produce exactly 1 hit (the definition site).
      - Verdict `RE_VERIFIED_AT_HEAD` iff: (a) signature intact at `:16`; (b) 3 XOR-shift lines intact inside `unchecked` block; (c) no non-XOR-shift statement introduced; (d) no new overload of `entropyStep` that bypasses the XOR-shift body.

    - **Predicate EXC-04-P1b — Caller-site keccak seed construction intact and VRF-sourced:**
      - Grep target: `keccak256\(abi\.encode\(rngWord` in `contracts/` — MUST produce hits at caller sites across Lootbox + Jackpot + Staked + DegenerusGame modules (NOT zero). Expected HEAD hits (READ-verified): `DegenerusGame.sol:1769` (single-arg `keccak256(abi.encode(rngWord))` chain), `StakedDegenerusStonk.sol:660` (`keccak256(abi.encode(rngWord, player))`), `DegenerusGameJackpotModule.sol:1799` (`keccak256(abi.encode(rngWord, lvl, FAR_FUTURE_COIN_TAG))`), `DegenerusGameLootboxModule.sol:554/628/673/708` (`keccak256(abi.encode(rngWord, player, day, amount))` / `..., amountEth)`), `DegenerusGameLootboxModule.sol:1753` (`keccak256(abi.encode(rngWordByDay[day], deity, day, slot))` — note: `rngWordByDay[day]` is the daily VRF-derived word, which qualifies as a VRF-derived `rngWord` variant per Phase 237 D-06).
      - Preserve the literal grep command in a reproducibility sub-block.
      - Enumerate EACH hit with file:line and the full keccak argument list. For each hit, trace the `rngWord` (or `rngWordByDay[day]`) variable backward to its VRF-callback source (`rawFulfillRandomWords` storage / `lootboxRngWordByIndex` mapping / `rngWordByDay` mapping / daily-RNG slot / jackpot-RNG slot). Cite the VRF-callback write site by file:line.
      - Grep target: `EntropyLib\.entropyStep` in `contracts/` — enumerate ALL actual call sites (skip doc-comment lines containing `*` or `//`). Expected HEAD call sites (READ-verified): `DegenerusGameLootboxModule.sol:813, :817, :1548, :1569, :1585, :1599, :1635` (7 call sites) + `DegenerusGameJackpotModule.sol:2119` (1 call site) = **8 call sites** matching Phase 237 inventory's 8 EXC-04 rows exactly.
      - For EACH of the 8 `entropyStep` call sites, trace the `state`/`entropy` argument backward to the caller-site keccak construction that produced it. Every trace MUST terminate at a `keccak256(abi.encode(rngWord, ...))` construction whose `rngWord` is VRF-derived. If ANY trace does NOT terminate at a VRF-derived keccak construction, that call site is `CANDIDATE_FINDING`.
      - Verdict `RE_VERIFIED_AT_HEAD` iff: (a) `keccak256(abi.encode(rngWord` grep produces hits at expected caller sites (not zero); (b) every `rngWord` argument resolves to a VRF-derived value; (c) every `entropyStep` call site receives state pre-derived from a caller-site keccak whose `rngWord` is VRF-sourced; (d) no new non-VRF entry point (new public/external wrapper, new library function, new callable path) bypasses the caller-site keccak seed construction between a VRF word and `entropyStep` entry.

    Required columns in the predicate table: `Predicate ID | Predicate Name | Target file:line | Grep Command | Hits | rngWord / state Source | HEAD Verdict`.

    **Call-Site Inventory sub-table:** list the 8 Phase 237 EXC-04 rows (INV-237-124, -131, -132, -134, -135, -136, -137, -138). For each row, cite the Phase 237 file:line consumption site (the `entropyStep` call), the upstream caller-site keccak construction (file:line of the `keccak256(abi.encode(rngWord, ...))` that produced the `state`/`entropy` passed to entropyStep), the VRF-callback source of `rngWord`, and the per-row EXC-04 Verdict. Columns: `INV-237-NNN | entropyStep Call Site (file:line) | Upstream Caller-Site Keccak (file:line) | rngWord VRF Source (file:line) | EXC-04 Verdict`.

    **Phase 237 inventory reconciliation note:** Phase 237 Consumer Index records 8 rows tagged `exception-xor-shift-seed`-adjacent (INV-237-124, -131, -132, -134, -135, -136, -137, -138). A fresh grep of `contracts/` for `EntropyLib\.entropyStep` at HEAD `7ab515fe` (excluding doc-comment lines) yields exactly 8 actual call sites — set-equal match with Phase 237 inventory. If the executor's grep yields a different count (e.g., 9 including a doc-comment, or 7 missing a call site), document the delta and route as a Phase 242 scope-guard note per D-27 + D-14 (no Phase 237 inventory edit).

    **Section-level verdict:** `EXC-04 RE_VERIFIED_AT_HEAD` iff BOTH EXC-04-P1a (EntropyLib body intact) AND EXC-04-P1b (caller-site keccak + VRF-derived rngWord at all 8 call sites) hold.

    **CROSS-CITES per D-12:**
    - CITE `audit/v30-FREEZE-PROOF.md` Phase 238-03 `lootbox-index-advance` Named Gate assignment for the 8 EXC-04 rows. Add `re-verified at HEAD 7ab515fe` backtick-quoted note.
    - CITE `audit/v30-ASYMMETRY-RE-JUSTIFICATION.md § Asymmetry A` Phase 239-03 for the lootbox index-advance equivalence proof corroborating EXC-04's `rngWord` source freezing. Add `re-verified at HEAD 7ab515fe` backtick-quoted note.

    D-05 frame compliance: this section re-verifies (a) `entropyStep` XOR-shift body integrity and (b) caller-site keccak SEED CONSTRUCTION + VRF-sourcing (exploitability — no new non-VRF entry point), NOT XOR-shift distribution quality. Do NOT re-litigate theoretical XOR-shift non-uniformity — that is KI-accepted.

    D-11 discipline: Phase 240 did NOT emit EXC-04 forward-cites (Phase 240 scope was gameover-VRF-available branch, per 240-CONTEXT.md). Therefore no EXC-04 forward-cite discharge rows are added to § 8.

    D-26/D-27/D-20: zero `contracts/` / `test/` writes; no `F-30-NN` IDs.
  </action>

  <verify>
    <automated>grep -q '^## 7\. EXC-04 EntropyLib Seed-Construction Re-Verification' audit/v30-EXCEPTION-CLOSURE.md && grep -qE 'EXC-04-P1a|EntropyLib\.sol:16-23|entropyStep.*body intact' audit/v30-EXCEPTION-CLOSURE.md && grep -qE 'EXC-04-P1b|keccak256\(abi\.encode\(rngWord|caller-site keccak' audit/v30-EXCEPTION-CLOSURE.md && grep -qE 'function entropyStep\(uint256 state\) internal pure' audit/v30-EXCEPTION-CLOSURE.md && grep -q 'EntropyLib\.entropyStep' audit/v30-EXCEPTION-CLOSURE.md && grep -qE 'INV-237-124' audit/v30-EXCEPTION-CLOSURE.md && grep -qE 'INV-237-138' audit/v30-EXCEPTION-CLOSURE.md && grep -qE 'EXC-04 RE_VERIFIED_AT_HEAD|CANDIDATE_FINDING' audit/v30-EXCEPTION-CLOSURE.md && [ "$(awk '/^## 7\. EXC-04/,/^## 8\./' audit/v30-EXCEPTION-CLOSURE.md | grep -cE 'EXC-04-P1a|EXC-04-P1b')" -ge 2 ] && [ "$(awk '/^## 7\. EXC-04/,/^## 8\./' audit/v30-EXCEPTION-CLOSURE.md | grep -cE '^\| INV-237-(124|131|132|134|135|136|137|138) \|')" -ge 8 ] && [ "$(grep -c 'F-30-' audit/v30-EXCEPTION-CLOSURE.md)" -eq 0 ] && [ -z "$(git status --porcelain contracts/ test/)" ]</automated>
  </verify>

  <acceptance_criteria>
    - `§ 7 EXC-04 EntropyLib Seed-Construction Re-Verification` section present
    - Two predicate rows present: EXC-04-P1a (EntropyLib.entropyStep body intact at `contracts/libraries/EntropyLib.sol:16-23`) AND EXC-04-P1b (caller-site `keccak256(abi.encode(rngWord, ...))` construction with VRF-derived rngWord at all 8 `entropyStep` call sites)
    - EXC-04-P1a cites the literal signature `function entropyStep(uint256 state) internal pure` and the 3 XOR-shift lines (`state ^= state << 7`, `state ^= state >> 9`, `state ^= state << 8`) inside an `unchecked` block
    - EXC-04-P1b cites the caller-site grep pattern `keccak256\(abi\.encode\(rngWord` and enumerates hits at Lootbox + Jackpot + Staked + DegenerusGame modules (NOT zero)
    - Grep commands for both P1a and P1b preserved for reproducibility
    - Call-Site Inventory sub-table present with all 8 Phase 237 EXC-04 rows listed (INV-237-124, -131, -132, -134, -135, -136, -137, -138 all visible — awk-scoped regex `^\| INV-237-(124|131|132|134|135|136|137|138) \|` yields ≥8 matches inside § 7)
    - Every entropyStep call site has its `state`/`entropy` argument traced backward to a caller-site keccak construction + VRF-derived `rngWord` source (or marked `CANDIDATE_FINDING`)
    - Section-level verdict line `EXC-04 RE_VERIFIED_AT_HEAD` (iff both P1a + P1b hold) or `CANDIDATE_FINDING` emitted
    - Phase 237 inventory reconciliation note present (confirming 8 Phase 237 rows set-equal with 8 actual call sites at HEAD; any delta routes to Phase 242 scope-guard note per D-27 + D-14, NOT a Phase 237 inventory edit)
    - Cross-cites to Phase 238-03 `lootbox-index-advance` Named Gate + Phase 239-03 § Asymmetry A each carry `re-verified at HEAD 7ab515fe` backtick-quoted note
    - Zero `F-30-NN` literal strings present
    - No EXC-04 forward-cite discharge rows added to § 8 (Phase 240 emitted none per D-11)
    - `git status --porcelain contracts/ test/` empty (D-26)
  </acceptance_criteria>

  <done>
    § 7 EXC-04 EntropyLib seed-construction re-verification complete. EntropyLib.entropyStep XOR-shift body at `contracts/libraries/EntropyLib.sol:16-23` confirmed intact (P1a). All 8 `entropyStep` call sites enumerated; every one confirmed to receive state pre-derived from a caller-site `keccak256(abi.encode(rngWord, ...))` construction with VRF-derived `rngWord` (P1b). No new entry point bypasses the caller-site keccak seed construction. Phase 237's 8-row EXC-04 inventory set-equal with 8 actual call sites at HEAD. `contracts/` and `test/` untouched.
  </done>
</task>

<task type="auto">
  <name>Task 5 — Consolidation into `audit/v30-EXCEPTION-CLOSURE.md` (§ 1 YAML frontmatter, § 2, § 9, § 10) + SUMMARY + git commit (D-22 / D-24)</name>

  <read_first>
    - `audit/v30-EXCEPTION-CLOSURE.md` (ALL existing content from Tasks 1–4: § 3 + § 4 + § 5 + § 8a + § 6 + § 8b + § 7)
    - `audit/v30-CONSUMER-INVENTORY.md`, `audit/v30-FREEZE-PROOF.md`, `audit/v30-RNGLOCK-STATE-MACHINE.md`, `audit/v30-PERMISSIONLESS-SWEEP.md`, `audit/v30-ASYMMETRY-RE-JUSTIFICATION.md`, `audit/v30-GAMEOVER-JACKPOT-SAFETY.md` (READ-only — for Prior-Artifact Cross-Cites section § 9 AND to confirm the frontmatter-block convention: Phase 238 uses a `# v30.0 ... Title` heading with no YAML; Phase 240 uses YAML `---` frontmatter + Title heading — Phase 241 MUST follow Phase 240's YAML frontmatter convention per D-24)
    - `audit/FINAL-FINDINGS-REPORT.md` (if present — v29.0 / v25.0 prior-milestone corroborating artifact per D-12)
    - `.planning/phases/241-exception-closure/241-CONTEXT.md` (for decision lineage summary in SUMMARY)
    - `$HOME/.claude/get-shit-done/templates/summary.md` (SUMMARY template scaffold)
  </read_first>

  <files>
    audit/v30-EXCEPTION-CLOSURE.md
    .planning/phases/241-exception-closure/241-01-SUMMARY.md
  </files>

  <action>
    Assemble the remaining sections of `audit/v30-EXCEPTION-CLOSURE.md` (§ 1 YAML frontmatter block, § 2 Executive Summary, § 9 Prior-Artifact Cross-Cites, § 10 Finding Candidates + Scope-Guard + Attestation) in their correct document positions, write the plan SUMMARY, and commit.

    **Step A — Re-order the file per D-24 10-section structure:**

    The final file MUST flow in exact order: § 1 YAML frontmatter block (NO markdown heading; see Step B) → § 2 Executive Summary (FIRST markdown heading: `## 2. Executive Summary`) → § 3 EXC-01 ONLY-ness Table → § 4 EXC-01 Grep Backstop → § 5 EXC-02 Predicate Re-Verification → § 6 EXC-03 Tri-Gate → § 7 EXC-04 EntropyLib → § 8 Forward-Cite Discharge Ledger (with 8a + 8b sub-sections) → § 9 Prior-Artifact Cross-Cites → § 10 Finding Candidates + Scope-Guard Deferrals + Attestation.

    Tasks 1–4 appended § 3/4/5/8a/6/8b/7 in build order. Reorganize: move § 8 (containing 8a + 8b) to its correct position AFTER § 7, and prepend § 1 YAML frontmatter + § 2 at top, append § 9 + § 10 at bottom. The file may be fully rewritten from the content Tasks 1–4 produced; preserve ALL row content verbatim (22 ONLY-ness rows + 17 EXC-02 discharges + 12 EXC-03 discharges + all predicate tables + all cross-cite text + all grep reproducibility sub-blocks).

    **Step B — Write § 1 — YAML Frontmatter Block (NO markdown heading):**

    § 1 is a YAML `---` frontmatter block at the very top of the file. This matches the Phase 240 precedent in `audit/v30-GAMEOVER-JACKPOT-SAFETY.md` (which uses `---` YAML frontmatter immediately at line 1, followed by a `# v30.0 ... Title` heading, and then `## 2.` Executive Summary logic). Do NOT emit a `## 1. Frontmatter` markdown heading — the YAML block IS § 1 per D-24.

    YAML frontmatter block (at line 1):
    ```
    ---
    phase: 241-exception-closure
    plan: 01
    milestone: v30.0
    head_anchor: 7ab515fe
    audit_baseline: 7ab515fe
    deliverable: audit/v30-EXCEPTION-CLOSURE.md
    requirements: [EXC-01, EXC-02, EXC-03, EXC-04]
    write_policy: READ-only on contracts/ and test/; writes confined to .planning/ and audit/
    supersedes: none
    generated_at: <ISO8601 date>
    ---
    ```

    Immediately below the closing `---`, emit a level-1 title heading (`# v30.0 Exception Closure — Phase 241 Final Consolidated Deliverable`) followed by a short `Audit Baseline` paragraph citing HEAD `7ab515fe` (identical contract tree to v29.0 `1646d5af` per PROJECT.md; all post-v29 commits are docs-only). These mirror the Phase 240 convention and precede § 2.

    **Step C — Write § 2 — Executive Summary (FIRST `##` markdown heading):**

    Section heading: `## 2. Executive Summary` — this is the FIRST `##` markdown heading in the file.

    Compact verdict block per D-09 closed-verdict vocabulary:
    - `EXC-01 Closure Verdict`: `CONFIRMED_SOLE_EXCEPTION_GROUPS` or `CANDIDATE_FINDING` — covering the 22-row ONLY-ness table; reference Gate A / Gate B outcomes.
    - `EXC-02 Closure Verdict`: `RE_VERIFIED_AT_HEAD` or `CANDIDATE_FINDING` — with predicate summary (single-call-site + 14-day gate).
    - `EXC-03 Closure Verdict`: `RE_VERIFIED_AT_HEAD` or `CANDIDATE_FINDING` — with tri-gate summary.
    - `EXC-04 Closure Verdict`: `RE_VERIFIED_AT_HEAD` or `CANDIDATE_FINDING` — with two-part predicate summary (P1a EntropyLib body intact + P1b caller-site keccak seed VRF-sourced at all 8 call sites).
    - `Combined ONLY-ness Claim Verdict`: `ONLY_NESS_HOLDS_AT_HEAD` (iff all of Gate A + Gate B + EXC-02 + EXC-03 + EXC-04 pass) or `CANDIDATE_FINDING`.
    - `Forward-Cite Discharge Count`: `29/29` (17 EXC-02 + 12 EXC-03) or the actual discharged count with unclosed-count routed to § 10 scope-guard deferral.
    - `Finding Candidates Count`: 0 (ideal) or actual count routed to Phase 242 FIND-01 intake per D-20/D-21.

    **Step D — Write § 9 — Prior-Artifact Cross-Cites:**

    Section heading: `## 9. Prior-Artifact Cross-Cites`.

    One-row-per-artifact table listing ALL cross-cited prior artifacts with `re-verified at HEAD 7ab515fe` backtick-quoted note + structural-equivalence statement per D-13. Required entries:
    - `audit/v30-CONSUMER-INVENTORY.md` — Phase 237 146-row index + 22 EXCEPTION distribution
    - `audit/v30-FREEZE-PROOF.md` — Phase 238 22-EXCEPTION / 124-SAFE set-equality cross-check (Gate A)
    - `audit/v30-RNGLOCK-STATE-MACHINE.md` — Phase 239 RNG-01 AIRTIGHT corroborating EXC-02 reachability
    - `audit/v30-PERMISSIONLESS-SWEEP.md` — Phase 239 RNG-02 no-permissionless-path-touches-RNG-state baseline
    - `audit/v30-ASYMMETRY-RE-JUSTIFICATION.md § Asymmetry A` — Phase 239 RNG-03 corroborating EXC-04 rngWord source freezing
    - `audit/v30-GAMEOVER-JACKPOT-SAFETY.md` — Phase 240 29 forward-cite source tokens + GO-04 + GO-05 corroborating EXC-02/03
    - `audit/FINAL-FINDINGS-REPORT.md` (v29.0 F-29-04 entry) — CORROBORATING prior-milestone for EXC-03 subject
    - `KNOWN-ISSUES.md` — 4 KI entries as SUBJECTS (not warrants per D-12)

    Verify plan-wide instance count of `re-verified at HEAD 7ab515fe` backtick-quoted phrase is >= 3 (D-13 minimum; target >= 7 given the cross-cite table above).

    Include a closing paragraph per D-19 clarifying: prior-milestone cross-cites are CORROBORATING CONTEMPORANEOUS EVIDENCE, NOT regression verdicts. Regression (PASS/REGRESSED/SUPERSEDED) against prior-milestone findings is Phase 242 REG-01/REG-02 territory.

    **Step E — Write § 10 — Finding Candidates + Scope-Guard Deferrals + Attestation:**

    Section heading: `## 10. Finding Candidates + Scope-Guard Deferrals + Attestation`.

    Three sub-sections per D-20 / D-21 / D-27 / D-28:

    - `### 10a. Finding Candidates (Phase 242 FIND-01 intake)`:
      - Enumerate every row across § 3 / § 4 / § 5 / § 6 / § 7 / § 8a / § 8b with verdict `CANDIDATE_FINDING`.
      - Expected: `None surfaced` (ideal — if all predicates pass and all discharges succeed).
      - If any: list row ID + section + reason + proposed Phase 242 severity. Do NOT emit `F-30-NN` IDs (D-20).

    - `### 10b. Scope-Guard Deferrals`:
      - If Phase 241 surfaced any consumer not in Phase 237's 146-row inventory OR any non-VRF seed surface absent from Phase 237's 22-EXCEPTION set, record here (D-27 routing).
      - If Task 4's fresh grep of `EntropyLib\.entropyStep` yielded a call-site count ≠ 8 (i.e., set-delta from Phase 237's 8 EXC-04 rows), record the delta here per D-27 + D-14 (no Phase 237 inventory edit).
      - Expected: `None surfaced` (ideal).
      - Per CONTEXT.md D-28: Phase 241 does NOT discharge any Phase 239 RNG-01/RNG-03 audit assumption beyond the Forward-Cite Discharge Ledger. If any discharge claim exists, route it to Phase 242 REG-02 (not this section).

    - `### 10c. Attestation`:
      - Single-paragraph statement attesting:
        1. All verdicts derived at HEAD `7ab515fe` per D-12 fresh-re-prove discipline.
        2. Zero `contracts/` or `test/` writes (verify via `git status --porcelain contracts/ test/` empty).
        3. Phase 237/238/239/240 outputs unmodified (verify via `git status --porcelain` on each audit file).
        4. `KNOWN-ISSUES.md` untouched per D-26 (KI promotions are Phase 242 FIND-03).
        5. Zero `F-30-NN` IDs emitted per D-20.
        6. All 29 Phase 240 forward-cite tokens addressed in § 8 Forward-Cite Discharge Ledger per D-11.

    **Step F — Write plan SUMMARY:**

    Create `.planning/phases/241-exception-closure/241-01-SUMMARY.md` using the SUMMARY template. Include:
    - Plan objective recap (universal ONLY-ness claim + EXC-02/03/04 predicate re-verifications + 29-row forward-cite discharge)
    - Tasks completed (Task 1..5)
    - Verdict distribution: 22-row ONLY-ness table closure counts (expected 2+8+4+8 = 22 CONFIRMED); predicate re-verification verdicts per EXC-NN; forward-cite discharge count
    - Plan decisions taken (D-01 single plan, D-03 5-task sequential, D-06 fold-all-4-KIs, D-08 dual-gate, D-10 fresh-re-derive predicates, D-11 explicit line-item discharge, D-22 single consolidated deliverable, D-24 10-section with YAML frontmatter convention matching Phase 240 precedent, D-25 HEAD anchor locked)
    - Cross-cite count (number of `re-verified at HEAD 7ab515fe` instances — must be >= 3)
    - Finding Candidates surfaced (expected 0; route to Phase 242 FIND-01 if any)
    - Scope-Guard Deferrals (expected 0; any EXC-04 call-site count delta recorded here)
    - Commit SHA references (HEAD `7ab515fe` baseline + this plan's commit SHA)
    - Attestation: zero F-30-NN; zero contracts/test writes; Phase 237/238/239/240 outputs unmodified; KNOWN-ISSUES untouched.

    **Step G — Git commit + verification:**

    1. Stage the two files ONLY: `git add audit/v30-EXCEPTION-CLOSURE.md .planning/phases/241-exception-closure/241-01-SUMMARY.md`
    2. Run `git status --porcelain contracts/ test/` — MUST be empty.
    3. Run `git status --porcelain audit/v30-CONSUMER-INVENTORY.md audit/v30-FREEZE-PROOF.md audit/v30-RNGLOCK-STATE-MACHINE.md audit/v30-PERMISSIONLESS-SWEEP.md audit/v30-ASYMMETRY-RE-JUSTIFICATION.md audit/v30-GAMEOVER-JACKPOT-SAFETY.md audit/v30-240-01-INV-DET.md audit/v30-240-02-STATE-TIMING.md audit/v30-240-03-SCOPE.md KNOWN-ISSUES.md` — MUST be empty.
    4. Commit with message `docs(241-01): EXC-01 ONLY-ness + EXC-02/03/04 predicate re-verification + 29-row forward-cite discharge at HEAD 7ab515fe`.
    5. Update STATE.md and ROADMAP.md plan-list entries for Phase 241 per standard phase-close workflow (mark 241-01 complete, record commit SHA, record verdict distribution summary). These STATE/ROADMAP updates may land in a separate plan-close commit per the Phase 239 / Phase 240 two-commit precedent.

    D-25 HEAD-anchor freeze check: if `git rev-parse HEAD` at commit time differs from `7ab515fe` in any way that modifies `contracts/` (as opposed to docs-only), abort and request scope addendum (D-25 reset rule). Contract tree identity check: `git diff 7ab515fe -- contracts/` MUST be empty.
  </action>

  <verify>
    <automated>test -f audit/v30-EXCEPTION-CLOSURE.md && test -f .planning/phases/241-exception-closure/241-01-SUMMARY.md && head -1 audit/v30-EXCEPTION-CLOSURE.md | grep -q '^---$' && grep -q '^## 2\. Executive Summary' audit/v30-EXCEPTION-CLOSURE.md && grep -q '^## 9\. Prior-Artifact Cross-Cites' audit/v30-EXCEPTION-CLOSURE.md && grep -q '^## 10\. Finding Candidates' audit/v30-EXCEPTION-CLOSURE.md && grep -q '^### 10c\. Attestation' audit/v30-EXCEPTION-CLOSURE.md && ! grep -q '^## 1\. Frontmatter' audit/v30-EXCEPTION-CLOSURE.md && [ "$(grep -c 're-verified at HEAD 7ab515fe' audit/v30-EXCEPTION-CLOSURE.md)" -ge 3 ] && grep -qE 'ONLY_NESS_HOLDS_AT_HEAD|CANDIDATE_FINDING' audit/v30-EXCEPTION-CLOSURE.md && grep -qE 'EXC-02 RE_VERIFIED_AT_HEAD|CANDIDATE_FINDING' audit/v30-EXCEPTION-CLOSURE.md && grep -qE 'EXC-03 RE_VERIFIED_AT_HEAD|CANDIDATE_FINDING' audit/v30-EXCEPTION-CLOSURE.md && grep -qE 'EXC-04 RE_VERIFIED_AT_HEAD|CANDIDATE_FINDING' audit/v30-EXCEPTION-CLOSURE.md && [ "$(grep -c 'F-30-' audit/v30-EXCEPTION-CLOSURE.md)" -eq 0 ] && [ -z "$(git status --porcelain contracts/ test/)" ] && [ -z "$(git diff 7ab515fe -- contracts/)" ]</automated>
  </verify>

  <acceptance_criteria>
    - `audit/v30-EXCEPTION-CLOSURE.md` contains all 10 logical sections in correct order per D-24: § 1 YAML frontmatter block (NO `## 1.` heading — matches Phase 240 precedent) → § 2 Executive Summary (FIRST `##` markdown heading) → § 3 EXC-01 ONLY-ness → § 4 EXC-01 Grep Backstop → § 5 EXC-02 Predicate → § 6 EXC-03 Tri-Gate → § 7 EXC-04 EntropyLib → § 8 Forward-Cite Discharge Ledger (8a + 8b) → § 9 Prior-Artifact Cross-Cites → § 10 Finding Candidates + Scope-Guard + Attestation
    - Line 1 of the file is a literal `---` (start of YAML frontmatter block); YAML frontmatter contains `head_anchor: 7ab515fe` and `requirements: [EXC-01, EXC-02, EXC-03, EXC-04]`
    - No `## 1. Frontmatter` markdown heading appears anywhere (verify via `! grep -q '^## 1\. Frontmatter' audit/v30-EXCEPTION-CLOSURE.md`)
    - `## 2. Executive Summary` is the first `##` markdown heading
    - § 2 Executive Summary lists closure verdicts for EXC-01, EXC-02, EXC-03, EXC-04 and combined ONLY-ness claim
    - § 8 Forward-Cite Discharge Ledger contains 17 EXC-02 discharges (§ 8a) + 12 EXC-03 discharges (§ 8b) = 29 total; file-wide count of literal `DISCHARGED_RE_VERIFIED_AT_HEAD` (or routed `CANDIDATE_FINDING` for unclosed cites) accounts for all 29
    - § 9 lists at least 6 prior-artifact cross-cites each with `re-verified at HEAD 7ab515fe` backtick-quoted note; plan-wide count >= 3 (target >= 7)
    - § 10a Finding Candidates sub-section present (may be `None surfaced`)
    - § 10b Scope-Guard Deferrals sub-section present (may be `None surfaced`; records any EXC-04 call-site count delta if surfaced)
    - § 10c Attestation sub-section present with 6-point attestation (all 6 items enumerated)
    - `.planning/phases/241-exception-closure/241-01-SUMMARY.md` exists with the plan-close summary structure
    - Zero `F-30-NN` literal strings present in `audit/v30-EXCEPTION-CLOSURE.md`
    - `git status --porcelain contracts/ test/` empty (D-26 READ-only)
    - `git diff 7ab515fe -- contracts/` empty (D-25 HEAD-anchor freeze honoured)
    - Phase 237/238/239/240 outputs + KNOWN-ISSUES.md unmodified (verify via `git status --porcelain` on each path)
    - Commit created with a message referencing `docs(241-01)` scope
  </acceptance_criteria>

  <done>
    `audit/v30-EXCEPTION-CLOSURE.md` is the complete 10-section single consolidated deliverable per ROADMAP SC-1 literal. § 1 is a YAML `---` frontmatter block at line 1 (NO `## 1.` heading — Phase 240 precedent convention); § 2 Executive Summary is the FIRST markdown heading. Plan SUMMARY exists. Commit staged and created with only the two plan-owned files (audit deliverable + SUMMARY). `contracts/`, `test/`, Phase 237/238/239/240 outputs, and `KNOWN-ISSUES.md` all unmodified. HEAD-anchor freeze `7ab515fe` honoured. All ROADMAP Phase 241 Success Criteria SC-1..SC-4 closed; 29 Phase 240 forward-cite tokens discharged; ready for Phase 242 FIND-01 intake.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 241 is a READ-only audit phase. The deliverable is markdown text — no code, no configuration, no schema, no test changes. There is no new attack surface against the runtime protocol introduced by this phase.

| Boundary | Description |
|----------|-------------|
| Audit-process integrity → Phase 242 consumption | Markdown text consumed by a downstream phase. A corrupted ledger could mislead Phase 242's FIND-01 intake. |
| Row-ID namespace integrity | `EXC-241-NNN` row IDs must not collide with other phase row-ID namespaces. |
| HEAD-anchor integrity | If `contracts/` drifts from `7ab515fe` during the phase, every verdict becomes stale. |

## STRIDE Threat Register

Phase 241 is READ-only re-verification of existing contract state; no code, test, or schema changes. Threats below target the audit process itself, not the runtime protocol. All threat dispositions flow from D-25 HEAD-anchor freeze + D-27 prior-phase READ-only + D-20 no-F-30-NN + D-26 contracts/test READ-only.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-241-01 | Tampering | `audit/v30-EXCEPTION-CLOSURE.md` 22-row ONLY-ness table | mitigate | Gate A set-equality cross-check against Phase 238 `audit/v30-FREEZE-PROOF.md` 22-EXCEPTION / 124-SAFE distribution (D-08). Any inventory mismatch surfaces as `CANDIDATE_FINDING` and blocks `ONLY_NESS_HOLDS_AT_HEAD` closure. |
| T-241-02 | Information Disclosure | Phase 237/238/239/240 prior-phase artifacts | mitigate | D-27 READ-only enforcement on all prior-phase audit files. Task 5 verify step includes `git status --porcelain` on every prior-phase audit file — non-empty status aborts commit. |
| T-241-03 | Elevation of Privilege | Audit verdict emission beyond Phase 241's charter (e.g., F-30-NN finding IDs) | mitigate | D-20 no-F-30-NN rule + verify-block check `grep -c 'F-30-' audit/v30-EXCEPTION-CLOSURE.md` must equal 0 across every task. Phase 241 emits `CANDIDATE_FINDING` ONLY; F-30-NN promotion is Phase 242 FIND-01 territory. |
| T-241-04 | Spoofing | Forward-Cite Discharge Ledger (29 rows) citing fabricated `audit/v30-GAMEOVER-JACKPOT-SAFETY.md:<line>` tokens | mitigate | Per-discharge row MUST cite a concrete `audit/v30-GAMEOVER-JACKPOT-SAFETY.md:<line>` verifiable via grep. Task 2 and Task 3 verify blocks use discharge-row-scoped counts (awk range delimited by `### 8a.`/`### 8b.` + `|.*(DISCHARGED_RE_VERIFIED_AT_HEAD|CANDIDATE_FINDING) |` pattern) so token-counting cannot spoof discharge-row counts via quoted source strings. D-11 residual rule routes undischargeable forward-cites to `CANDIDATE_FINDING` rather than fabrication. |
| T-241-05 | Repudiation | Disputed HEAD-anchor or contract-tree drift between phase start and phase commit | mitigate | D-25 HEAD-anchor `7ab515fe` locked in plan frontmatter + `audit/v30-EXCEPTION-CLOSURE.md` YAML frontmatter + § 1 Audit Baseline paragraph. Task 5 verify block includes `git diff 7ab515fe -- contracts/` which MUST be empty. |
| T-241-06 | Tampering | `contracts/` or `test/` unauthorized edits during Phase 241 execution | mitigate | D-26 READ-only + `memory/feedback_no_contract_commits.md` + `memory/feedback_never_preapprove_contracts.md`. Every task's verify block includes `git status --porcelain contracts/ test/` which MUST be empty. |
| T-241-07 | Denial of Service | Scope creep bloating phase beyond budget | accept | D-05 exploitability frame + D-14/D-15/D-16/D-17 scope-boundary decisions prevent re-enumeration of Phase 237 inventory, re-derivation of Phase 238 freeze-proofs, re-derivation of Phase 239 global invariants, re-derivation of Phase 240 VRF-available-branch jackpot determinism. Scope creep detected via scope-guard deferral routing to § 10b. |
| T-241-08 | Information Disclosure | Inclusion of randomness distribution analysis (out-of-scope per D-05) | accept | D-05 locked exploitability frame. Tasks 1–4 action blocks explicitly prohibit re-litigating XOR-shift distribution, prevrandao 1-bit proposer bias, and deterministic affiliate seed quality. |
| T-241-09 | Tampering | EXC-04 predicate factually wrong about EntropyLib architecture (keccak seed inside entropyStep vs at caller sites) | mitigate | Task 4 rewritten to reflect READ-verified architecture at HEAD `7ab515fe`: `EntropyLib.entropyStep(uint256 state)` at `:16-23` is pure XOR-shift with ZERO keccak calls; caller-site `keccak256(abi.encode(rngWord, ...))` constructions at Lootbox/Jackpot/Staked/DegenerusGame modules feed entropy into entropyStep. EXC-04 predicate split into P1a (EntropyLib body intact) + P1b (caller-site keccak + VRF-derived rngWord at all 8 call sites). Task 4 verify uses discriminating greps for both artifacts. |

All mitigations are enforced mechanically via verify-block greps at every task boundary. No hypothetical mitigations; every defense maps to a concrete grep invariant or file-state check runnable by the executor.
</threat_model>

<verification>

## Plan-Level Verification (run at end of Task 5, before commit)

All checks must pass before the single consolidated commit for `audit/v30-EXCEPTION-CLOSURE.md` + `241-01-SUMMARY.md`:

1. `test -f audit/v30-EXCEPTION-CLOSURE.md` — deliverable exists
2. `head -1 audit/v30-EXCEPTION-CLOSURE.md | grep -q '^---$'` — § 1 YAML frontmatter block at line 1 (D-24 convention, Phase 240 precedent)
3. `grep -q '^head_anchor: 7ab515fe$' audit/v30-EXCEPTION-CLOSURE.md` — HEAD anchor locked in YAML frontmatter (D-25)
4. `! grep -q '^## 1\. Frontmatter' audit/v30-EXCEPTION-CLOSURE.md` — no `## 1.` markdown heading (D-24: § 1 is YAML block, not markdown heading)
5. `grep -q '^## 2\. Executive Summary' audit/v30-EXCEPTION-CLOSURE.md` — § 2 is first markdown heading
6. `grep -q '^## 3\. EXC-01 Consolidated ONLY-ness Table' audit/v30-EXCEPTION-CLOSURE.md` through `grep -q '^## 10\. Finding Candidates' audit/v30-EXCEPTION-CLOSURE.md` — all remaining sections § 3..§ 10 present (D-24)
7. `[ "$(grep -cE '^\| EXC-241-0(0[1-9]|1[0-9]|2[0-2]) \|' audit/v30-EXCEPTION-CLOSURE.md)" -ge 22 ]` — § 3 contains exactly 22 ONLY-ness rows (D-06)
8. `grep -qE 'GATE_A_(PASSES|FAILS)' audit/v30-EXCEPTION-CLOSURE.md` — Gate A verdict emitted (D-08)
9. `grep -qE 'GATE_B_(PASSES|FAILS)' audit/v30-EXCEPTION-CLOSURE.md` — Gate B verdict emitted (D-08)
10. `grep -qE 'ONLY_NESS_HOLDS_AT_HEAD|CANDIDATE_FINDING' audit/v30-EXCEPTION-CLOSURE.md` — combined ONLY-ness verdict emitted
11. Forward-Cite Discharge Ledger counts (discriminating grep scoped to ledger sub-sections): `[ "$(awk '/^### 8a\. EXC-02/,/^### 8b\.|^## 9\./' audit/v30-EXCEPTION-CLOSURE.md | grep -cE '^\| EXC-241-[0-9]{3} \|.*(DISCHARGED_RE_VERIFIED_AT_HEAD|CANDIDATE_FINDING) \|')" -ge 17 ]` + `[ "$(awk '/^### 8b\. EXC-03/,/^## 9\./' audit/v30-EXCEPTION-CLOSURE.md | grep -cE '^\| EXC-241-[0-9]{3} \|.*(DISCHARGED_RE_VERIFIED_AT_HEAD|CANDIDATE_FINDING) \|')" -ge 12 ]` + file-wide `[ "$(grep -c 'DISCHARGED_RE_VERIFIED_AT_HEAD' audit/v30-EXCEPTION-CLOSURE.md)" -ge 29 ]` (the literal verdict string is specific to ledger discharge rows and cannot be spoofed by quoted source tokens) (D-11)
12. EXC-04 two-part predicate presence: `grep -qE 'EXC-04-P1a|EntropyLib\.sol:16-23|entropyStep.*body intact' audit/v30-EXCEPTION-CLOSURE.md` + `grep -qE 'EXC-04-P1b|keccak256\(abi\.encode\(rngWord|caller-site keccak' audit/v30-EXCEPTION-CLOSURE.md` — both P1a (EntropyLib body) and P1b (caller-site keccak) present; no claim of keccak construction inside entropyStep body
13. `[ "$(grep -c 're-verified at HEAD 7ab515fe' audit/v30-EXCEPTION-CLOSURE.md)" -ge 3 ]` — D-13 minimum cross-cite note count (target >= 7)
14. `[ "$(grep -c 'F-30-' audit/v30-EXCEPTION-CLOSURE.md)" -eq 0 ]` — no F-30-NN IDs emitted (D-20)
15. `[ -z "$(git status --porcelain contracts/ test/)" ]` — no contracts/test writes (D-26)
16. `[ -z "$(git diff 7ab515fe -- contracts/)" ]` — contract tree unchanged since HEAD baseline (D-25)
17. `git status --porcelain audit/v30-CONSUMER-INVENTORY.md audit/v30-FREEZE-PROOF.md audit/v30-RNGLOCK-STATE-MACHINE.md audit/v30-PERMISSIONLESS-SWEEP.md audit/v30-ASYMMETRY-RE-JUSTIFICATION.md audit/v30-GAMEOVER-JACKPOT-SAFETY.md audit/v30-240-01-INV-DET.md audit/v30-240-02-STATE-TIMING.md audit/v30-240-03-SCOPE.md KNOWN-ISSUES.md` — all empty (D-27 / D-26)
18. `test -f .planning/phases/241-exception-closure/241-01-SUMMARY.md` — plan SUMMARY exists

</verification>

<success_criteria>

Phase 241 ROADMAP Success Criteria — closure mapping:

- **SC-1** (`audit/v30-EXCEPTION-CLOSURE.md` EXC-01 ONLY-ness proof): Closed by § 3 + § 4 + Gate A + Gate B combined `ONLY_NESS_HOLDS_AT_HEAD` verdict
- **SC-2** (EXC-02 trigger-gating re-verification): Closed by § 5 two-predicate table (single-call-site + 14-day gate) + § 8a 17-row EXC-02 Forward-Cite Discharge Ledger
- **SC-3** (EXC-03 F-29-04 scope re-verification): Closed by § 6 tri-gate table (terminal-state + no-player-reachable-timing + buffer-scope) + § 8b 12-row EXC-03 Forward-Cite Discharge Ledger
- **SC-4** (EXC-04 EntropyLib keccak seed re-verification): Closed by § 7 two-part predicate table (P1a EntropyLib.entropyStep XOR-shift body intact at `contracts/libraries/EntropyLib.sol:16-23` + P1b caller-site `keccak256(abi.encode(rngWord, ...))` seed construction with VRF-derived `rngWord` at all 8 entropyStep call sites)

Additional plan-level criteria:
- Zero `contracts/` or `test/` writes
- Zero `F-30-NN` finding IDs emitted
- All 29 Phase 240 forward-cite tokens discharged (or routed to Phase 242 CANDIDATE_FINDING with explicit scope-guard deferral)
- Minimum 3 `re-verified at HEAD 7ab515fe` backtick-quoted notes (D-13)
- HEAD-anchor freeze honoured: `git diff 7ab515fe -- contracts/` empty
- Phase 237/238/239/240 outputs and `KNOWN-ISSUES.md` unmodified
- § 1 is YAML frontmatter block (NO `## 1.` markdown heading); § 2 Executive Summary is first markdown heading (D-24 convention, Phase 240 precedent)

</success_criteria>

<output>
After completion, create `.planning/phases/241-exception-closure/241-01-SUMMARY.md` per Task 5 Step F, then update STATE.md and ROADMAP.md in a separate plan-close commit per the Phase 239 / Phase 240 two-commit precedent.

Primary deliverable: `audit/v30-EXCEPTION-CLOSURE.md` (10 sections per D-24, single consolidated file per ROADMAP SC-1 literal per D-22).
Handoff to Phase 242: § 10a Finding Candidates (Phase 242 FIND-01 intake) + § 8 Forward-Cite Discharge Ledger (closes Phase 240's 29 cross-phase forward-cites) + § 10c Attestation (evidence of HEAD-anchor freeze + READ-only discipline).
</output>
