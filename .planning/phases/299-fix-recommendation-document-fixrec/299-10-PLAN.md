---
phase: 299-fix-recommendation-document-fixrec
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/299-fix-recommendation-document-fixrec/299-10-FIXREC-cluster.md
autonomous: true
requirements: [FIXREC-01, FIXREC-02, FIXREC-03, FIXREC-04, FIXREC-05]
must_haves:
  truths:
    - "Cluster-J FIXREC contribution file exists"
    - "Every V-NNN row in scope (V-168, V-169, V-170, V-171, V-172, V-174, V-175, V-176, V-177, V-179, V-182, V-184, V-186, V-188, V-190, V-191, V-192, V-193, V-201, V-202) has a §N analytical entry"
    - "V-179 is treated as ONE logical §N entry whose §N.B actor-walk enumerates all 9 EOA callsites"
    - "V-179 has all 9 sub-anchors H-101..H-109 cited within its single §N.D handoff entry"
    - "Every §N entry has all 4 sub-sections"
    - "Every §N.D anchor matches catalog (H-92..H-119)"
    - "§12 sStonk cross-day re-roll exploit (V-184) is surfaced as Tier-1 headline finding per §0 headline #1"
    - "Zero SAFE_BY_DESIGN tokens"
    - "Zero contracts/ + test/ source-tree mutations"
  artifacts:
    - path: ".planning/phases/299-fix-recommendation-document-fixrec/299-10-FIXREC-cluster.md"
      provides: "Per-VIOLATION analytical entries for Cluster J (ticketQueue + ticketsOwedPacked V-179 + sStonk redemption + bountyOwedTo + decBurn)"
      contains: "§N.A"
  key_links:
    - from: "299-10-FIXREC-cluster.md §N.D"
      to: ".planning/RNGLOCK-CATALOG.md §16"
      via: "D-43N-V44-HANDOFF-NN"
      pattern: "D-43N-V44-HANDOFF-(9[2-9]|1[0-1][0-9])"
---

<objective>
Author per-VIOLATION analytical FIXREC entries for **Cluster J** — ticket queue + ticketsOwedPacked (V-179 fan-out) + sStonk redemption family + bountyOwedTo + decBurn. 20 logical VIOLATIONs in scope:

- ticketQueue[rk] EOA writers (S-52): V-168 (_queueTickets purchaseWhaleBundle), V-169 (_queueTickets purchaseLazyPass), V-170 (_queueTickets purchaseDeityPass — runtime gate at :543), V-171 (_queueTickets openLootBox), V-172 (_queueTickets openBurnieLootBox), V-174 (_queueTicketsScaled _purchaseFor), V-175 (_queueTicketRange _awardDecimatorLootbox EOA), V-176 (_queueTicketRange claimWhalePass), V-177 (_queueTicketRange _redeemWhalePassRange)
- ticketsOwedPacked V-179 (S-53; 1 logical with 9 EOA callsites = H-101..H-109)
- sStonk redemption family (S-55, S-56..S-60): V-182 (bountyOwedTo _addDailyFlip arming EOA depositCoinflip — existing :664 gate), V-184 (redemptionPeriodIndex _submitGamblingClaimFrom — §0 headline #1 Tier-1 cross-day re-roll exploit), V-186 (pendingRedemptionEthBase += subsumed by V-184), V-188 (pendingRedemptionBurnieBase += subsumed by V-184), V-190 (pendingRedemptionBurnie += subsumed by V-184), V-191 (pendingRedemptions[player] _submitGamblingClaimFrom writes subsumed by V-184), V-192 (pendingRedemptions[player] claimRedemption delete subsumed by V-184), V-193 (pendingRedemptions[player] claimRedemption partial clear subsumed by V-184)
- decBurn (S-66, S-67): V-201 (decBurn[lvl][player].burn recordDecBurn BurnieCoin.decimatorBurn), V-202 (terminalDecBucketBurnTotal recordTerminalDecBurn BurnieCoin.terminalDecimatorBurn during pre-gameOver post-VRF window)

Purpose: Per §0 headline #1, **the sStonk cross-day re-roll exploit (V-184) is the milestone's most economically significant Tier-1 hazard** — burn 1 wei on future day re-arms pendingRedemptionEthBase for already-resolved period; forces next advanceGame to overwrite redemptionPeriods[period].roll with fresh independent roll. Each iteration ~19% positive EV. Same-day re-resolution blocked by rngWordByDay short-circuit; cross-day is not. The catalog's tactic-(a) revert in `_submitGamblingClaimFrom` is the minimal structural fix. The 20-entry cluster also handles V-179's 9-callsite fan-out as ONE logical entry per FIXREC budget rules.

Output: `.planning/phases/299-fix-recommendation-document-fixrec/299-10-FIXREC-cluster.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/299-fix-recommendation-document-fixrec/299-CONTEXT.md
@.planning/RNGLOCK-CATALOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Cluster-J FIXREC contribution (ticketQueue + V-179 fan-out + sStonk redemption + bountyOwedTo + decBurn)</name>
  <files>.planning/phases/299-fix-recommendation-document-fixrec/299-10-FIXREC-cluster.md</files>
  <read_first>
    - .planning/RNGLOCK-CATALOG.md §14 rows S-52, S-53, S-55, S-56, S-57, S-58, S-59, S-60, S-61, S-66, S-67
    - .planning/RNGLOCK-CATALOG.md §15 writer rows for S-52 + S-53 + S-55 + S-56..S-60 + S-66 + S-67
    - .planning/RNGLOCK-CATALOG.md §16 rows V-167..V-179, V-182..V-184, V-186, V-188, V-190..V-193, V-201, V-202
    - .planning/RNGLOCK-CATALOG.md §0 (headline #1 — sStonk cross-day re-roll exploit)
    - .planning/RNGLOCK-CATALOG.md §10 (MintModule trait-generation consumer for ticket queue context); §11 (BurnieCoinflip._resolveFlip consumer for bountyOwedTo); §12 (StakedDegenerusStonk.resolveRedemptionPeriod consumer — the V-184 exploit origin); §13 (DecimatorModule._awardDecimatorLootbox consumer for decBurn)
    - .planning/phases/299-fix-recommendation-document-fixrec/299-CONTEXT.md
    - contracts/StakedDegenerusStonk.sol around :486, :492 (burn/burnWrapped + BurnsBlockedDuringLiveness modifier), :593, :594, :600, :601, :604 (resolveRedemptionPeriod), :657, :661, :664 (claimRedemption), :760 (_submitGamblingClaimFrom — V-184 site), :789, :790, :791, :792, :803, :805, :806, :810 (write fields)
    - contracts/AdvanceModule.sol around :1187 (rngWordByDay short-circuit — blocks same-day re-resolution; gap closer is cross-day)
    - contracts/BurnieCoinflip.sol around :664 (existing !rngLocked() gate), :681 (_addDailyFlip arming arm), :730 (RngLocked convention cite), :865, :874 (processCoinflipPayouts self-clear / self-write)
    - contracts/modules/DegenerusGameDecimatorModule.sol around :252 (runDecimatorJackpot), :388 (_awardDecimatorLootbox), :582 (_queueTicketRange callsite), :731 (recordDecBurn / recordTerminalDecBurn), :795 (runTerminalDecimatorJackpot)
    - contracts/DegenerusGameStorage.sol around :580 (_queueTickets), :585, :612 (_queueTicketsScaled), :636, :666 (_queueTicketRange), :671, :1135 (whale-pass redemption)
    - contracts/modules/DegenerusGameWhaleModule.sol around :313, :482, :625, :957, :973 (ticketQueue callsites)
    - contracts/modules/DegenerusGameLootboxModule.sol around :1067, :1190 (_queueTickets callsites)
    - contracts/modules/DegenerusGameMintModule.sol around :1129 (_purchaseFor / _queueTicketsScaled)
    - contracts/DegenerusGame.sol around :1116 (recordTerminalDecBurn)
    - contracts/BurnieCoin.sol around :634 (terminalDecimatorBurn entry)
    - .planning/milestones/v41.0-phases/288-*/288-01-DESIGN-INTENT-TRACE.md (Phase 288 dailyIdx anchor precedent)
    - .planning/milestones/v42.0-phases/296-cross-surface-adversarial-sweep-sweep/296-CONTEXT.md (D-42N-RETRY-RNG-DOMAIN-SEP-01 context)
    - Project memory: feedback_design_intent_before_deletion.md
    - Project memory: feedback_rng_window_storage_read_freshness.md (V-184 commitment-side staleness exploit is the canonical instance)
  </read_first>
  <action>
    Author per-VIOLATION FIXREC entries per `D-299-FIXREC-LAYOUT-01` 4-sub-section template. Section ordering: §1=V-168 → §2=V-169 → §3=V-170 → §4=V-171 → §5=V-172 → §6=V-174 → §7=V-175 → §8=V-176 → §9=V-177 → §10=V-179 (single logical entry; §N.B enumerates all 9 callsites; §N.D emits all 9 anchors H-101..H-109) → §11=V-182 → §12=V-184 (HEADLINE TIER-1) → §13=V-186 → §14=V-188 → §15=V-190 → §16=V-191 → §17=V-192 → §18=V-193 → §19=V-201 → §20=V-202.

    **§N.A — Design-intent backward-trace**:
    - For S-52 ticketQueue + S-53 ticketsOwedPacked, trace the ticket-queue architecture (pre-v25 baseline; round-key-indexed push-array; consumed by AdvanceModule trait-generation + JackpotModule self-stack). Document the existing partial gates: `purchaseDeityPass` runtime gate at WhaleModule:543; far-future loop revert (RngLocked at MintModule:572); lootbox VRF domain-separation per `D-42N-RETRY-RNG-DOMAIN-SEP-01`. Note that V-179 is the co-located write that mirrors S-52 — a single gate covers both slots per the catalog.
    - For V-179 specifically: it is ONE logical VIOLATION (per `D-299-FIXREC-LAYOUT-01` 82-logical-entry budget; catalog §0 footnote explicitly states V-179 fans out into 9 V-179.A..V-179.I sub-rows but is counted as one entry). The §N.B actor walk enumerates all 9 EOA callsites (mirror V-167..V-177 from S-52); the §N.D emits all 9 handoff anchors HANDOFF-101 through HANDOFF-109 inside the single §N entry.
    - For S-55 bountyOwedTo + BurnieCoinflip surface, trace Phase 296 retryLootboxRng / coinflip domain separation; existing !rngLocked() gate at :664; tactic-(a) extension is fail-closed revert addition.
    - For S-56 redemptionPeriodIndex + S-57..S-61 sStonk redemption family: trace the per-period redemption architecture. Document the V-184 design-intent: `redemptionPeriodIndex` is incremented in `_submitGamblingClaimFrom` (advancing the pointer), but `resolveRedemptionPeriod` does NOT advance the index. Result: after period N resolves, redemptionPeriodIndex still points at N; burn(1 wei) calls `_submitGamblingClaimFrom` which increments → period N+1 (correct), but `pendingRedemptionEthBase` += continues to accumulate against the OLD pointer that the next resolveRedemptionPeriod call re-resolves with fresh roll. Per `feedback_rng_window_storage_read_freshness.md`, this is the canonical commitment-side staleness exploit. Headline finding per §0 #1.
    - For S-66/S-67 decBurn + terminalDecBucketBurnTotal, trace recordDecBurn/recordTerminalDecBurn (BurnieCoin.decimatorBurn → DegenerusGame.recordDecBurn callback). Phase 293/294 DPNERF context applies (cite Phase 294 DESIGN-INTENT-TRACE if present).

    **§N.B — Actor game-theory walk**:
    - V-168, V-169, V-170, V-171, V-172, V-174, V-175, V-176, V-177 (ticketQueue EOA writers): exploit-actor = player buying tickets / opening lootbox / claiming whale-pass during rngLock window; mutates ticketQueue rk-keyed array before AdvanceModule trait-generation consumes it; EV magnitude per-callsite: HIGH for lootbox open paths (V-171, V-172 — per §0 headline #2), MEDIUM for purchase-bundle / whale-pass paths, LOW for V-170/V-176/V-177 (existing partial coverage).
    - V-179: same vectors as S-52, applied to co-located ticketsOwedPacked slot. §N.B enumerates each of the 9 EOA callsites (mirroring V-168..V-177) and notes the co-location optimization (single gate covers both slots).
    - V-182 bountyOwedTo arming during rngLock: exploit-actor = player firing depositCoinflip during rngLock to arm bounty for self before the coinflip resolver consumes; EV magnitude: MEDIUM-HIGH (bounty magnitude varies; existing :664 gate already partially blocks); recommended tactic extends to fail-closed revert.
    - **V-184 sStonk cross-day re-roll exploit (TIER-1 HEADLINE)**: exploit-actor = sStonk holder with prior pending redemption; action sequence detailed per §0 headline #1 — (1) burn(1 wei) wraps to `_submitGamblingClaimFrom` writing redemptionPeriodIndex + pendingRedemptionEthBase += ethValueOwed; (2) advanceGame VRF resolves redemptionPeriods[redemptionPeriodIndex].roll with the period's accumulated base; (3) on a future day, attacker calls burn(1 wei) again → redemptionPeriodIndex advances or stays stale (verify exact mechanics); pendingRedemptionEthBase re-accumulates; (4) next advanceGame() overwrites redemptionPeriods[period].roll with a FRESH independent roll. Each iteration ~19% positive EV (rough estimate; sub-agent verifies during execution). Supply-cap (50%) bounds intra-period scale but does not block 1-wei re-burns. Same-day blocked at AdvanceModule:1187 rngWordByDay short-circuit; cross-day is the open exploit. EV magnitude: CATASTROPHE-tier in aggregate (compounding 19% rolls until supply cap).
    - V-186, V-188, V-190 subsumed by V-184: same exploit-actor + action sequence; closing V-184 closes these collateraly per the catalog's "Subsumed by V-184" notation.
    - V-191, V-192, V-193 subsumed by V-184: same collateral-closure pattern; the writes are legitimate downstream effects once V-184 index-advance is enforced.
    - V-201 decBurn[lvl][player].burn: exploit-actor = player calling BurnieCoin.decimatorBurn during the post-VRF-pre-snapshot window of `runDecimatorJackpot`; mutates the burn record before the bucket-offset snapshot is committed; EV magnitude: MEDIUM-HIGH (decimator jackpot magnitude per bucket).
    - V-202 terminalDecBucketBurnTotal: exploit-actor = player calling BurnieCoin.terminalDecimatorBurn during the pre-`gameOver` post-VRF window; EV magnitude: HIGH (terminal decimator jackpot is bigger absolute).

    **§N.C — Recommended tactic + rationale + impact**: Reproduce catalog tactics:
    - V-168: (a) Add `if (rngLockedFlag) revert RngLocked()` at WhaleModule:purchaseWhaleBundle entry
    - V-169: (a) Add gated-revert at WhaleModule:purchaseLazyPass entry; mirrors purchaseDeityPass:543
    - V-170: (a) Existing gate at :543 satisfies; verdict-matrix is stack-strict; gate verified
    - V-171: (a) Gate lootbox-resolution writes via rngLockedFlag; daily-VRF freshness invariant
    - V-172: (a) Same as V-171 — write-target shared
    - V-174: (a) Gate purchase() against daily VRF window; level-target redirect at :1221 insufficient
    - V-175: (a) Gate EOA-reach (recordDecBurn); advance-stack reach is EXEMPT per-callsite
    - V-176: (a) Add top-level rngLockedFlag gate; far-future loop revert is partial coverage
    - V-177: (a) Same as V-176 — whale-pass redemption path
    - V-179: (a) Same gate as each S-52 row; co-located write — single gate covers both slots; emit all 9 sub-anchors (H-101..H-109) within the single §N.D
    - V-182: (a) Bounty arming already gated by `!rngLocked()` at :664; extend to fail-closed revert
    - **V-184 (HEADLINE)**: (a) `revert` in `_submitGamblingClaimFrom` when `redemptionPeriods[redemptionPeriodIndex].roll != 0` — this is the minimal structural fix; closes the cross-day re-roll. Alternative tactic-(c) "advance the index inside `resolveRedemptionPeriod` itself" follows the Phase 288 `dailyIdx` snapshot precedent and is structurally cleaner. v44.0 plan-phase consumes both options.
    - V-186, V-188, V-190, V-191, V-192, V-193: (a) Subsumed by V-184 — same writer fn (_submitGamblingClaimFrom) and same callsite for V-186/V-188/V-190; legitimate downstream effects (claimRedemption delete + partial clear) once V-184 enforced.
    - V-201: (a) Gate `recordDecBurn` on `decClaimRounds[lvl].poolWei == 0` to close burn at snapshot
    - V-202: (a) Gate `recordTerminalDecBurn` on `rngWordByDay[day] == 0` so window closes at RNG publish

    Bytecode impact: tactic (a) gates ~30-50 bytes per site; V-184 fix ~50-80 bytes (one extra SLOAD + revert). V-179 single gate at the each-callsite layer covers both slots at no incremental cost (co-located writes). Storage-layout: byte-identical (no new slots). Public ABI: NON-BREAKING (new RngLocked revert paths only). Cite `RngLocked` convention (MintModule:1221 / BurnieCoinflip:730 / sStonk:492).

    **§N.D — v44.0 handoff anchor**: V-168→H-92, V-169→H-93, V-170→H-94, V-171→H-95, V-172→H-96, V-174→H-97, V-175→H-98, V-176→H-99, V-177→H-100, V-179→H-101..H-109 (ALL 9 within the single §N.D for V-179; one bullet per sub-callsite enumerating the 9 EOA writers), V-182→H-110, V-184→H-111 (HEADLINE), V-186→H-112, V-188→H-113, V-190→H-114, V-191→H-115, V-192→H-116, V-193→H-117, V-201→H-118, V-202→H-119.

    **Headline emphasis for V-184**: §12 (in the §N counter, this is the §12 entry for V-184) MUST cross-reference §0 headline #1 verbatim and explicitly mark `**HEADLINE TIER-1 — §0 finding #1**` at the section heading. The aggregator (Wave 2) flags V-184's handoff anchor as the v44.0 priority-1 sub-phase.

    Do NOT modify `contracts/` or `test/`. Do NOT use SAFE_BY_DESIGN. Cite project memory verbatim. For V-179, do NOT split into 9 separate §N entries — it is ONE logical §N entry per `D-299-FIXREC-LAYOUT-01` 82-budget rule.
  </action>
  <verify>
    <automated>
test -f .planning/phases/299-fix-recommendation-document-fixrec/299-10-FIXREC-cluster.md \
  && for V in V-168 V-169 V-170 V-171 V-172 V-174 V-175 V-176 V-177 V-179 V-182 V-184 V-186 V-188 V-190 V-191 V-192 V-193 V-201 V-202; do grep -q "$V" .planning/phases/299-fix-recommendation-document-fixrec/299-10-FIXREC-cluster.md || { echo "MISSING: $V"; exit 1; }; done \
  && for N in 92 93 94 95 96 97 98 99 100 101 102 103 104 105 106 107 108 109 110 111 112 113 114 115 116 117 118 119; do grep -q "D-43N-V44-HANDOFF-$N" .planning/phases/299-fix-recommendation-document-fixrec/299-10-FIXREC-cluster.md || { echo "MISSING anchor: D-43N-V44-HANDOFF-$N"; exit 1; }; done \
  && [ "$(grep -c "§[0-9]\+\.A" .planning/phases/299-fix-recommendation-document-fixrec/299-10-FIXREC-cluster.md)" -ge 20 ] \
  && [ "$(grep -c "§[0-9]\+\.B" .planning/phases/299-fix-recommendation-document-fixrec/299-10-FIXREC-cluster.md)" -ge 20 ] \
  && [ "$(grep -c "§[0-9]\+\.C" .planning/phases/299-fix-recommendation-document-fixrec/299-10-FIXREC-cluster.md)" -ge 20 ] \
  && [ "$(grep -c "§[0-9]\+\.D" .planning/phases/299-fix-recommendation-document-fixrec/299-10-FIXREC-cluster.md)" -ge 20 ] \
  && grep -qE "HEADLINE|Tier-1|cross-day re-roll" .planning/phases/299-fix-recommendation-document-fixrec/299-10-FIXREC-cluster.md \
  && ! grep -q "SAFE_BY_DESIGN" .planning/phases/299-fix-recommendation-document-fixrec/299-10-FIXREC-cluster.md \
  && [ -z "$(git status --porcelain contracts/ test/ 2>/dev/null)" ] \
  && echo "OK"
    </automated>
  </verify>
  <done>
    Cluster-J file exists; all 20 V-NNN entries (V-168..V-179 incl. V-179-as-single-logical, V-182..V-193, V-201, V-202); all 28 handoff anchors (H-92..H-119); V-184 headline emphasis; V-179 single-entry treatment with 9 sub-anchors; zero SAFE_BY_DESIGN; zero source-tree mutations.
  </done>
</task>

</tasks>

<verification>
- 20 logical V-NNN entries (V-179 counts as 1 with 9 sub-callsites)
- 28 handoff anchors (H-92..H-119) — 19 entries with single anchors + V-179 with 9 sub-anchors
- ≥80 sub-section markers
- HEADLINE / Tier-1 marker present for V-184
- Zero SAFE_BY_DESIGN
- Zero source-tree mutations
</verification>

<success_criteria>
Single AGENT-COMMITTED Cluster-J file covering 20 logical VIOLATIONs including the §0 headline #1 V-184 Tier-1 sStonk cross-day re-roll exploit and V-179's 9-callsite single-logical treatment.
</success_criteria>

<output>
Create `.planning/phases/299-fix-recommendation-document-fixrec/299-10-SUMMARY.md` when done.
</output>
