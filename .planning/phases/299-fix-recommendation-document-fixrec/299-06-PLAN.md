---
phase: 299-fix-recommendation-document-fixrec
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/299-fix-recommendation-document-fixrec/299-06-FIXREC-cluster.md
autonomous: true
requirements: [FIXREC-01, FIXREC-02, FIXREC-03, FIXREC-04, FIXREC-05]
must_haves:
  truths:
    - "Cluster-F FIXREC contribution file exists"
    - "Every V-NNN row in scope (V-066, V-068, V-069, V-070, V-071, V-072, V-073, V-074, V-080) has a §N analytical entry"
    - "Every §N entry has all 4 sub-sections"
    - "Every §N.D anchor matches catalog (H-34, H-35, H-36, H-37, H-38, H-39, H-40, H-41, H-42)"
    - "Zero SAFE_BY_DESIGN tokens"
    - "Zero contracts/ + test/ source-tree mutations"
  artifacts:
    - path: ".planning/phases/299-fix-recommendation-document-fixrec/299-06-FIXREC-cluster.md"
      provides: "Per-VIOLATION analytical entries for Cluster F (pendingRedemptionEthValue + deityPass + ETH/stETH balance)"
      contains: "§N.A"
  key_links:
    - from: "299-06-FIXREC-cluster.md §N.D"
      to: ".planning/RNGLOCK-CATALOG.md §16"
      via: "D-43N-V44-HANDOFF-NN"
      pattern: "D-43N-V44-HANDOFF-(3[4-9]|4[0-2])"
---

<objective>
Author per-VIOLATION analytical FIXREC entries for **Cluster F** — game-over magnitude inputs: `pendingRedemptionEthValue` (S-17), `deityPassOwners`/`deityPassPurchasedCount` (S-18, S-19), `address(this).balance` ETH-balance (S-20), and `stETH.balanceOf(game)` Lido cross-contract balance (S-21). 9 logical VIOLATIONs: V-066 (pendingRedemptionEthValue beginRedemption/_submitGamblingClaimFrom += via burn/burnWrapped; BurnsBlockedDuringLiveness covers), V-068 (claimRedemption -=; subsumed by S-56 fix), V-069 (deityPassOwners.push), V-070 (deityPassPurchasedCount += 1; subsumed by V-069), V-071 (receive() payable fallback), V-072 (payable purchase functions; existing per-fn gates cover), V-073 (claimWinnings outflow; same gate as V-063), V-074 (sDGNRS / vault / GNRUS withdrawals; BurnsBlockedDuringLiveness covers transitively), V-080 (stETH external IN transfers; snapshot pattern).

Purpose: Theme is game-over drain magnitude inputs (preRefundAvailable + deity refund + stETH conversion) that consume both ETH balance and cross-contract sStonk pending values. Tactic-(b) snapshot pattern dominates for balance-based inputs; tactic-(a) for explicit writers.

Output: `.planning/phases/299-fix-recommendation-document-fixrec/299-06-FIXREC-cluster.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/299-fix-recommendation-document-fixrec/299-CONTEXT.md
@.planning/RNGLOCK-CATALOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Cluster-F FIXREC contribution (pendingRedemption + deityPass + ETH/stETH balance)</name>
  <files>.planning/phases/299-fix-recommendation-document-fixrec/299-06-FIXREC-cluster.md</files>
  <read_first>
    - .planning/RNGLOCK-CATALOG.md §14 rows S-17, S-18, S-19, S-20, S-21
    - .planning/RNGLOCK-CATALOG.md §15 writer rows for S-17 + S-18 + S-19 + S-20 + S-21
    - .planning/RNGLOCK-CATALOG.md §16 rows V-066, V-068, V-069, V-070, V-071, V-072, V-073, V-074, V-080
    - .planning/RNGLOCK-CATALOG.md §5 (GameOverModule rngWordByDay substitution consumer; handleGameOverDrain preRefundAvailable gate); §12 (sStonk resolveRedemptionPeriod consumer)
    - .planning/phases/299-fix-recommendation-document-fixrec/299-CONTEXT.md
    - contracts/StakedDegenerusStonk.sol around :492 (BurnsBlockedDuringLiveness runtime gate convention; sStonk:492 cited in CONTEXT.md as a `RngLocked` convention site), :657 (claimRedemption -=), :789 (beginRedemption / _submitGamblingClaimFrom +=)
    - contracts/modules/DegenerusGameWhaleModule.sol around :595, :596 (deity pass owners.push and count += 1 in _purchaseDeityPass)
    - contracts/DegenerusGame.sol around :1408 (claimWinnings outflow), implicit receive() payable
    - contracts/modules/DegenerusGameGameOverModule.sol around :243, :247 (handleFinalSweep stETH outgoing)
    - contracts/modules/DegenerusGameAdvanceModule.sol around :1555 (_stakeEth Lido conversion)
    - .planning/milestones/v41.0-phases/281-mint-batch-determinism-fix-fix/281-01-DESIGN-INTENT-TRACE.md (Phase 281 owed-salt snapshot pattern — load-bearing for tactic (b))
    - Project memory: feedback_design_intent_before_deletion.md
    - Project memory: feedback_rng_window_storage_read_freshness.md (storage-read freshness during rng-window)
  </read_first>
  <action>
    Author per-VIOLATION FIXREC entries per `D-299-FIXREC-LAYOUT-01` 4-sub-section template. Section ordering: §1=V-066 → §2=V-068 → §3=V-069 → §4=V-070 → §5=V-071 → §6=V-072 → §7=V-073 → §8=V-074 → §9=V-080.

    **§N.A — Design-intent backward-trace**: For S-17 pendingRedemptionEthValue, trace the sStonk redemption-claim flow design (pre-v25 baseline sStonk; per-period redemption accumulator); the BurnsBlockedDuringLiveness modifier at sStonk:492 is the existing gate convention (catalog CONTEXT.md cites sStonk:492 as a `RngLocked` precedent — verify the exact modifier name). For S-18 deityPassOwners + S-19 deityPassPurchasedCount, trace the deity-pass architecture (pre-v25 WhaleModule special purchase variant) — the runtime rngLockedFlag + _livenessTriggered gates at :543/596 are partial coverage. For S-20 ETH balance, this is EVM-intrinsic; no writer source under `contracts/`. The `receive()` payable fallback is implicit Solidity. For S-21 stETH balance, this is cross-contract Lido (trace-stop per `D-298-EXEMPT-CROSSCONTRACT-01`); the only contract-side outflows are AdvanceModule._stakeEth and GameOverModule.handleFinalSweep.

    **§N.B — Actor game-theory walk**: For V-066 (pendingRedemptionEthValue += via burn/burnWrapped during rngLock), exploit-actor = sStonk holder; action sequence = burn 1 wei during rngLock to inflate pendingRedemptionEthValue before handleGameOverDrain reads it for the terminal magnitude computation; EV magnitude: HIGH per `feedback_rng_window_storage_read_freshness.md`. Existing BurnsBlockedDuringLiveness covers — coverage gap verification. For V-068 (claimRedemption -= subsumed by S-56 redemptionPeriodIndex fix per §12), EV magnitude: LOW once S-56 fix lands. For V-069/V-070 (deityPass purchase during rngLock; deity owners is consumed by §5 game-over drain to compute deity refunds), exploit-actor = whale; EV magnitude: HIGH (deity refund is a large per-pass refund). For V-071 (receive() payable fallback during rngLock), exploit-actor = any EOA sending ETH to game contract; EV magnitude: HIGH — inflates address(this).balance which is the totalFunds basis for game-over drain. Catalog tactic (b) snapshot at _gameOverEntropy time. For V-072 (payable purchase functions inflate balance), per-fn gates cover; verification only. For V-073 (claimWinnings outflow), same gate as V-063 (Cluster E coordination) — single revert closes both claimablePool and balance writers. For V-074 (sDGNRS/vault/GNRUS withdrawals cross-contract), gated transitively via sDGNRS liveness convention. For V-080 (stETH external IN), exploit-actor = any EOA `IStETH.transfer(game, amount)`; EV magnitude: MEDIUM (stETH transfer dilutes per-share rebase math). Same snapshot tactic as V-071.

    **§N.C — Recommended tactic + rationale + impact**: Reproduce catalog tactics:
    - V-066: (a) Existing `BurnsBlockedDuringLiveness` covers — verify branch coverage via FUZZ-301
    - V-068: (a) Subsumed by S-56 redemptionPeriodIndex fix (V-184; cross-cluster coordination with Cluster K) — re-resolution lock covers
    - V-069: (a) Gate `_purchaseDeityPass` (buyDeityPass) when any lootbox's RNG word is fresh in the open window
    - V-070: (a) Subsumed by V-069 (co-located write at WhaleModule:595/:596)
    - V-071: (b) Snapshot `totalFunds` at `_gameOverEntropy` time; consume snapshot in drain (Phase 281 owed-salt pattern)
    - V-072: (a) Existing per-fn gates (`_livenessTriggered() && rngLockedFlag`) cover; verify coverage during liveness window
    - V-073: (a) Same gate as V-063 — single revert at DegenerusGame:1408 closes both claimablePool and balance writers
    - V-074: (a) Gate at sDGNRS callsite (BurnsBlockedDuringLiveness) covers — verification
    - V-080: (b) Same snapshot as V-071 — covers both ETH balance and stETH balance inputs at snapshot time

    Bytecode impact: tactic (a) verification rows = 0 bytes; new gates ~30-50 bytes each; tactic (b) snapshot ~100-200 bytes for one new packed slot covering totalFunds + stETH. Storage-layout: tactic (a) = byte-identical; tactic (b) = new packed snapshot slot in Storage. Public ABI: NON-BREAKING.

    **§N.D — v44.0 handoff anchor**: V-066→H-34, V-068→H-35, V-069→H-36, V-070→H-37, V-071→H-38, V-072→H-39, V-073→H-40, V-074→H-41, V-080→H-42.

    Do NOT modify `contracts/` or `test/`. Do NOT use SAFE_BY_DESIGN. Cite project memory verbatim.
  </action>
  <verify>
    <automated>
test -f .planning/phases/299-fix-recommendation-document-fixrec/299-06-FIXREC-cluster.md \
  && for V in V-066 V-068 V-069 V-070 V-071 V-072 V-073 V-074 V-080; do grep -q "$V" .planning/phases/299-fix-recommendation-document-fixrec/299-06-FIXREC-cluster.md || { echo "MISSING: $V"; exit 1; }; done \
  && for H in HANDOFF-34 HANDOFF-35 HANDOFF-36 HANDOFF-37 HANDOFF-38 HANDOFF-39 HANDOFF-40 HANDOFF-41 HANDOFF-42; do grep -q "D-43N-V44-$H" .planning/phases/299-fix-recommendation-document-fixrec/299-06-FIXREC-cluster.md || { echo "MISSING anchor: D-43N-V44-$H"; exit 1; }; done \
  && [ "$(grep -c "§[0-9]\+\.A" .planning/phases/299-fix-recommendation-document-fixrec/299-06-FIXREC-cluster.md)" -ge 9 ] \
  && [ "$(grep -c "§[0-9]\+\.B" .planning/phases/299-fix-recommendation-document-fixrec/299-06-FIXREC-cluster.md)" -ge 9 ] \
  && [ "$(grep -c "§[0-9]\+\.C" .planning/phases/299-fix-recommendation-document-fixrec/299-06-FIXREC-cluster.md)" -ge 9 ] \
  && [ "$(grep -c "§[0-9]\+\.D" .planning/phases/299-fix-recommendation-document-fixrec/299-06-FIXREC-cluster.md)" -ge 9 ] \
  && ! grep -q "SAFE_BY_DESIGN" .planning/phases/299-fix-recommendation-document-fixrec/299-06-FIXREC-cluster.md \
  && [ -z "$(git status --porcelain contracts/ test/ 2>/dev/null)" ] \
  && echo "OK"
    </automated>
  </verify>
  <done>
    Cluster-F file exists; all 9 V-NNN entries; all 9 H-NN anchors (H-34..H-42); 4 sub-sections per entry; zero SAFE_BY_DESIGN; zero source-tree mutations.
  </done>
</task>

</tasks>

<verification>
- 9 V-NNN entries
- 9 handoff anchors (H-34..H-42)
- ≥36 sub-section markers
- Zero SAFE_BY_DESIGN
- Zero source-tree mutations
</verification>

<success_criteria>
Single AGENT-COMMITTED Cluster-F file covering 9 pendingRedemption + deityPass + ETH/stETH VIOLATIONs.
</success_criteria>

<output>
Create `.planning/phases/299-fix-recommendation-document-fixrec/299-06-SUMMARY.md` when done.
</output>
