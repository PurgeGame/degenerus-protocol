---
phase: 299-fix-recommendation-document-fixrec
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/299-fix-recommendation-document-fixrec/299-03-FIXREC-cluster.md
autonomous: true
requirements: [FIXREC-01, FIXREC-02, FIXREC-03, FIXREC-04, FIXREC-05]
must_haves:
  truths:
    - "Cluster-C FIXREC contribution file exists"
    - "Every V-NNN row in scope (V-024, V-025, V-026, V-027, V-030, V-031, V-032) has a §N analytical entry"
    - "Every §N entry has all 4 sub-sections"
    - "Every §N.D anchor cites D-43N-V44-HANDOFF-NN matching catalog (H-13..H-19)"
    - "Zero SAFE_BY_DESIGN tokens"
    - "Zero contracts/ + test/ source-tree mutations"
  artifacts:
    - path: ".planning/phases/299-fix-recommendation-document-fixrec/299-03-FIXREC-cluster.md"
      provides: "Per-VIOLATION analytical entries for Cluster C (prizePoolsPacked EOA purchase/payout writers)"
      contains: "§N.A"
  key_links:
    - from: "299-03-FIXREC-cluster.md §N.D handoff anchor"
      to: ".planning/RNGLOCK-CATALOG.md §16"
      via: "D-43N-V44-HANDOFF-NN cross-reference"
      pattern: "D-43N-V44-HANDOFF-(1[3-9])"
---

<objective>
Author per-VIOLATION analytical FIXREC entries for **Cluster C** — `prizePoolsPacked` (S-09) EOA purchase + payout writers. 7 logical VIOLATIONs in scope: V-024 (MintModule purchase/purchaseCoin/purchaseBurnieLootbox), V-025 (WhaleModule purchaseWhaleBundle/purchaseLazyPass), V-026 (WhaleModule purchaseDeityPass — runtime gate at :543 already), V-027 (recordDecBurn BurnieCoin callback), V-030 (claimWhalePass → _queueTicketRange), V-031 (placeDegeneretteBet → _collectBetFunds), V-032 (openLootBox / openBurnieLootBox lootbox payout consolidation).

Purpose: Theme is `prizePoolsPacked` cross-pool accumulator (next + future) mutation by EOA-reachable entry points without top-level rngLockedFlag gate. Top-level ungated EOA entry points cluster per §0 headline #3.

Output: `.planning/phases/299-fix-recommendation-document-fixrec/299-03-FIXREC-cluster.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/299-fix-recommendation-document-fixrec/299-CONTEXT.md
@.planning/RNGLOCK-CATALOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Cluster-C FIXREC contribution (prizePoolsPacked EOA writers)</name>
  <files>.planning/phases/299-fix-recommendation-document-fixrec/299-03-FIXREC-cluster.md</files>
  <read_first>
    - .planning/RNGLOCK-CATALOG.md §14 row S-09
    - .planning/RNGLOCK-CATALOG.md §15 writer rows for S-09 (MintModule, WhaleModule, DegeneretteModule, LootboxModule, DegenerusGame writers)
    - .planning/RNGLOCK-CATALOG.md §16 rows V-024, V-025, V-026, V-027, V-030, V-031, V-032
    - .planning/RNGLOCK-CATALOG.md §1 (JackpotModule.payDailyJackpot consumer) and §8 (DegeneretteModule._resolveLootboxDirect consumer)
    - .planning/phases/299-fix-recommendation-document-fixrec/299-CONTEXT.md
    - contracts/modules/DegenerusGameMintModule.sol around purchase/purchaseCoin/purchaseBurnieLootbox entries; :1221 (existing cachedJpFlag && rngLockedFlag gate)
    - contracts/modules/DegenerusGameWhaleModule.sol around :187 (purchaseWhaleBundle), :380 (purchaseLazyPass), :538 + :543 (purchaseDeityPass), :957/:973 (claimWhalePass)
    - contracts/DegenerusGame.sol around :665, :673 (lootbox payout consolidation), :714 (parent dispatch), :1029 (recordDecBurn)
    - contracts/modules/DegenerusGameDegeneretteModule.sol around :367, :405, :553 (placeDegeneretteBet + _collectBetFunds + _placeDegeneretteBetCore)
    - .planning/milestones/v42.0-phases/290-mint-batch-event-sig-cleanup-mintcln/290-01-DESIGN-INTENT-TRACE.md (MINTCLN owed-in-baseKey collapse precedent — informs tactic-(a) gate placement)
    - Project memory: feedback_design_intent_before_deletion.md
    - Project memory: feedback_rng_commitment_window.md (commitment-window discipline for cross-call prize pool reads)
  </read_first>
  <action>
    Author per-VIOLATION FIXREC entries per `D-299-FIXREC-LAYOUT-01` 4-sub-section template. Section ordering: §1=V-024 → §2=V-025 → §3=V-026 → §4=V-027 → §5=V-030 → §6=V-031 → §7=V-032.

    **§N.A — Design-intent backward-trace**: For each EOA writer, document why the writer exists (player-side purchase entry; coin callback; lootbox payout consolidation) and trace the design phase (pre-v25 baseline for top-level purchase entries; Phase 290 MINTCLN for owed-in-baseKey discipline; Phase 296 RETRY_LOOTBOX_RNG for the lootbox VRF domain separation that V-032 leans on). The cachedJpFlag && rngLockedFlag last-jackpot-day target-level redirect at `MintModule:1221` is a partial gate — explain why it does not cover the full rngLock window (jackpot-day-only scope; rngLock can be open on non-jackpot phases). For V-026, document the existing runtime gate at WhaleModule:543 and why the catalog still classifies as VIOLATION (per-callsite + stack-strict per `D-298-EXEMPT-REACH-01`). For V-032, document the lootbox VRF domain-separation per `D-42N-RETRY-RNG-DOMAIN-SEP-01` — the manual lootbox open path is in a separate VRF surface from the daily jackpot VRF, but prizePoolsPacked is shared accounting.

    **§N.B — Actor game-theory walk**: Each VIOLATION enables a player or callback-initiator to mutate prizePoolsPacked next/future fields during the rngLock window before the daily jackpot consumer reads them. V-024 (MintModule purchases) = LOW per-actor EV (mint is fee-paid, attacker still loses on ETH), but aggregate EV across all mint paths during a high-stakes jackpot window can be HIGH-tier. V-025 (WhaleModule purchases) = MEDIUM (whale-bundle is large per-purchase mutation). V-026 = subsumed by V-025 + runtime gate. V-027 (recordDecBurn via BurnieCoin) = MEDIUM-HIGH (decimator burn is fee-cheap; high attack ROI per burn). V-030 (claimWhalePass) = LOW per single claim, but cumulative ticket-queue mutation matters. V-031 (placeDegeneretteBet) = HIGH (degenerette is the cheap-bet entry point, easily fired mid-window). V-032 (manual lootbox open during rngLock) = HIGH per `feedback_rng_window_storage_read_freshness.md` — lootbox open reads many SLOADs mid-resolution.

    **§N.C — Recommended tactic + rationale + impact**: Reproduce catalog tactics:
    - V-024: (a) Add top-level `if (rngLockedFlag) revert RngLocked();` to MintModule.purchase / purchaseCoin / purchaseBurnieLootbox entry points (3 sites)
    - V-025: (a) Top-level rngLockedFlag revert at WhaleModule:187 + :380
    - V-026: (a) Existing gate at WhaleModule:543 covers; this row is coverage-verification-only (no source-tree change required; just FUZZ-301 branch coverage)
    - V-027: (a) Gate at DegenerusGame:1029 OR upstream in DegenerusCoin.burnCoin
    - V-030: (a) Effective gate via downstream _queueTicketRange revert; add explicit top-level gate at DegenerusGame:1692 / WhaleModule:957 for clarity
    - V-031: (a) Add `rngLockedFlag` revert to `_placeDegeneretteBetCore` at DegeneretteModule:405
    - V-032: (b) Domain-separated lootbox VRF; snapshot prizePoolsPacked next/future at lootbox-buy-time, NOT at open-time — Phase 281 owed-salt + Phase 288 dailyIdx snapshot precedent applies

    Bytecode impact per tactic (a) gate: ~30-50 bytes added per site. Tactic (b) snapshot for V-032: ~100-200 bytes (new per-index packed snapshot field or extension of an existing per-index struct). Storage-layout: tactic (a) = byte-identical; tactic (b) = new packed slot in lootbox per-index commitment struct. Public ABI: NON-BREAKING. Cite `RngLocked` convention.

    **§N.D — v44.0 handoff anchor**: V-024→H-13, V-025→H-14, V-026→H-15, V-027→H-16, V-030→H-17, V-031→H-18, V-032→H-19.

    Do NOT modify `contracts/` or `test/`. Do NOT use SAFE_BY_DESIGN. Cite feedback memory verbatim.
  </action>
  <verify>
    <automated>
test -f .planning/phases/299-fix-recommendation-document-fixrec/299-03-FIXREC-cluster.md \
  && for V in V-024 V-025 V-026 V-027 V-030 V-031 V-032; do grep -q "$V" .planning/phases/299-fix-recommendation-document-fixrec/299-03-FIXREC-cluster.md || { echo "MISSING: $V"; exit 1; }; done \
  && for H in HANDOFF-13 HANDOFF-14 HANDOFF-15 HANDOFF-16 HANDOFF-17 HANDOFF-18 HANDOFF-19; do grep -q "D-43N-V44-$H" .planning/phases/299-fix-recommendation-document-fixrec/299-03-FIXREC-cluster.md || { echo "MISSING anchor: D-43N-V44-$H"; exit 1; }; done \
  && [ "$(grep -c "§[0-9]\+\.A" .planning/phases/299-fix-recommendation-document-fixrec/299-03-FIXREC-cluster.md)" -ge 7 ] \
  && [ "$(grep -c "§[0-9]\+\.B" .planning/phases/299-fix-recommendation-document-fixrec/299-03-FIXREC-cluster.md)" -ge 7 ] \
  && [ "$(grep -c "§[0-9]\+\.C" .planning/phases/299-fix-recommendation-document-fixrec/299-03-FIXREC-cluster.md)" -ge 7 ] \
  && [ "$(grep -c "§[0-9]\+\.D" .planning/phases/299-fix-recommendation-document-fixrec/299-03-FIXREC-cluster.md)" -ge 7 ] \
  && ! grep -q "SAFE_BY_DESIGN" .planning/phases/299-fix-recommendation-document-fixrec/299-03-FIXREC-cluster.md \
  && [ -z "$(git status --porcelain contracts/ test/ 2>/dev/null)" ] \
  && echo "OK"
    </automated>
  </verify>
  <done>
    Cluster-C file exists; all 7 V-NNN entries; all 7 H-NN anchors (H-13..H-19); 4 sub-sections per entry; zero SAFE_BY_DESIGN; zero source-tree mutations.
  </done>
</task>

</tasks>

<verification>
- Cluster-C FIXREC contribution exists
- 7 V-NNN entries (V-024, V-025, V-026, V-027, V-030, V-031, V-032)
- 7 handoff anchors (H-13..H-19)
- ≥28 sub-section markers
- Zero SAFE_BY_DESIGN
- Zero source-tree mutations
</verification>

<success_criteria>
Single AGENT-COMMITTED Cluster-C file covering 7 prizePoolsPacked EOA-writer VIOLATIONs.
</success_criteria>

<output>
Create `.planning/phases/299-fix-recommendation-document-fixrec/299-03-SUMMARY.md` when done.
</output>
