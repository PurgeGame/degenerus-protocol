---
phase: 310-implementation-single-batched-user-approved-contract-diff-im
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/storage/DegenerusGameStorage.sol
autonomous: true
requirements: [IMPL-02]
must_haves:
  truths:
    - "DegenerusGameStorage exposes _packLootboxPurchase / _unpackLootboxPurchase as internal pure, reachable by Lootbox, Mint, and Whale modules"
    - "DegenerusGameStorage exposes _lootboxEvMultiplierFromScore as internal pure and the EV constants it needs as internal constant, so deposit-time tally in Mint/Whale can classify mult > NEUTRAL and read CAP"
    - "The per-(index, player) snapshot is a single packed uint256 word (lootboxPurchasePacked); lootboxBaseLevelPacked and lootboxEvScorePacked no longer exist"
    - "No new storage slot is introduced; the change is net -1 slot per (index, player)"
  artifacts:
    - path: "contracts/storage/DegenerusGameStorage.sol"
      provides: "Packed lootboxPurchasePacked mapping + pack/unpack helpers + relocated EV multiplier fn + EV constants (single source of truth for all 4 modules)"
      contains: "function _packLootboxPurchase("
  key_links:
    - from: "Mint/Whale deposit sites (Plan 03)"
      to: "DegenerusGameStorage._packLootboxPurchase / _lootboxEvMultiplierFromScore / LOOTBOX_EV_* constants"
      via: "inherited internal members of shared base"
      pattern: "_packLootboxPurchase|_lootboxEvMultiplierFromScore|LOOTBOX_EV_NEUTRAL_BPS"
    - from: "openLootBox (Plan 02)"
      to: "DegenerusGameStorage.lootboxPurchasePacked + _unpackLootboxPurchase"
      via: "single SLOAD + unpack"
      pattern: "lootboxPurchasePacked|_unpackLootboxPurchase"
---

<objective>
Build the SHARED-BASE foundation for the v45.0 lootbox EV-cap refactor (IMPL-02): in
`contracts/storage/DegenerusGameStorage.sol`, replace the two per-(index, player) snapshot
mappings with one packed `uint256` word, add the pack/unpack helpers, and relocate the EV
multiplier function plus the EV constants it needs into the shared base so all three modules
(Lootbox, Mint, Whale) can reach them.

This plan implements the SPEC-01 packed layout (309-SPEC §1) with the CONTEXT.md D-01/D-02
placement override: the helpers AND the EV multiplier fn/constants live in Storage.sol (NOT
LootboxModule as SPEC §1.7 literally stated), because Mint/Whale are separate delegatecall
contracts that do NOT inherit LootboxModule and cannot reach Lootbox-`private` members.

Purpose: This is the contract-of-record every downstream edit (Plan 02 open-apply, Plan 03
deposit tally) consumes. Interface-first ordering — define the shared symbols before any
consumer references them.

Output: A modified `DegenerusGameStorage.sol` (uncommitted — the single batched diff is
committed at the END of Plan 03 after the USER-APPROVAL gate). NO commit in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

CONTRACT-COMMIT POLICY (binding for this entire phase):
- `contracts/*.sol` changes MUST NOT be committed or pushed without explicit USER approval of
  the full diff. Contract changes are NOT pre-approved.
- This phase produces exactly ONE batched contract commit covering all 4 files, gated on the
  USER-APPROVAL checkpoint in Plan 03. This plan (01) leaves its edit UNCOMMITTED in the working
  tree. Do NOT run `git commit` for any `contracts/*.sol` file here.
- A planning/summary commit (`.planning/**` only) is allowed at plan end if your workflow emits
  one; never stage `contracts/*.sol`.
</execution_context>

<context>
@.planning/phases/310-implementation-single-batched-user-approved-contract-diff-im/310-CONTEXT.md
@.planning/phases/309-spec-locked-layout-bonus-only-cap-shared-cap-disposition-spe/309-SPEC.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- The exact symbols this plan must CREATE in DegenerusGameStorage.sol. Downstream plans -->
<!-- 02/03 reference these directly — they must match these signatures byte-for-byte. -->

Helper signatures (SPEC §1.7; home file = Storage.sol per D-01):
```solidity
function _packLootboxPurchase(uint16 scorePlus1, uint64 adj, uint24 baseLevelPlus1)
    internal pure returns (uint256);
function _unpackLootboxPurchase(uint256 word)
    internal pure returns (uint16 scorePlus1, uint64 adj, uint24 baseLevelPlus1);
```

Packed uint256 word layout (SPEC §1.1 — LOCKED, do not alter):
| Bit range  | Field           | Width  | Encoding |
| `[0:16]`   | `score + 1`     | uint16 | 0 = unset |
| `[16:80]`  | `adjustedPortion` | uint64 | cap-eligible ETH (<= 10 ETH) |
| `[80:104]` | `baseLevel + 1` | uint24 | 0 = unset; per-module sentinel offset (DIV-1) |
| `[104:256]`| free            | 152 b  | reserved, written as zero |

EV multiplier fn relocated from LootboxModule (D-02) — keep its body byte-identical, change
only visibility `private pure` -> `internal pure`:
```solidity
function _lootboxEvMultiplierFromScore(uint256 score) internal pure returns (uint256);
```
Its compile-time inputs that MUST also relocate (D-02): ACTIVITY_SCORE_NEUTRAL_BPS,
ACTIVITY_SCORE_MAX_BPS, LOOTBOX_EV_MIN_BPS, LOOTBOX_EV_NEUTRAL_BPS, LOOTBOX_EV_MAX_BPS,
LOOTBOX_EV_BENEFIT_CAP — relocate as `internal constant`.
</interfaces>

<existing_anchors>
<!-- Re-grep-verified against live HEAD bfe25567 (NOT the SPEC baseline 6f0ba29). Line numbers -->
<!-- happen to match SPEC §0, but the executor MUST locate each anchor by its MATCHED SUBSTRING, -->
<!-- never trust the bare line number (per feedback_verify_call_graph_against_source). -->

Storage.sol (target file):
- `function _packEthToMilliEth(uint256 wei_) internal pure returns (uint64) {`   (~1347) — PLACEMENT PRECEDENT for D-01/D-02
- `mapping(uint48 => mapping(address => uint32)) internal lootboxDay;`            (~1370) — DO NOT TOUCH (seed input, co-pack rejected)
- `internal lootboxBaseLevelPacked;`                                             (~1375) — REMOVE (merged into word)
- `mapping(uint48 => mapping(address => uint16)) internal lootboxEvScorePacked;` (~1379) — RENAME+WIDEN -> lootboxPurchasePacked (uint256)
- `internal lootboxEvBenefitUsedByLevel;`                                        (~1428) — DO NOT TOUCH (shared cap accumulator)

LootboxModule.sol (SOURCE of relocated symbols — this plan moves them OUT):
- `uint16 private constant ACTIVITY_SCORE_NEUTRAL_BPS = 6_000;`  (~304)
- `uint16 private constant ACTIVITY_SCORE_MAX_BPS = 25_500;`     (~306)
- `uint16 private constant LOOTBOX_EV_MIN_BPS = 8_000;`          (~308)
- `uint16 private constant LOOTBOX_EV_NEUTRAL_BPS = 10_000;`     (~310)
- `uint16 private constant LOOTBOX_EV_MAX_BPS = 13_500;`         (~312)
- `uint256 private constant LOOTBOX_EV_BENEFIT_CAP =` / `10 ether;` (~314-315)
- `function _lootboxEvMultiplierFromScore(` / `uint256 score` / `) private pure returns (uint256) {` (~444-446)
</existing_anchors>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pack the snapshot word — rename + widen lootboxEvScorePacked, remove lootboxBaseLevelPacked, add pack/unpack helpers</name>
  <files>contracts/storage/DegenerusGameStorage.sol</files>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol (the target — read the mapping decls ~1370-1428 AND the existing internal-pure pack/unpack helpers ~1347-1365 as the placement precedent)
    - 309-SPEC.md §1.1 (word layout), §1.2 (uint64 width proof), §1.3 (baseLevel co-pack), §1.6 (rename), §1.7 (helper signatures), §1.8 (no-new-slot)
    - 310-CONTEXT.md D-01 (helpers -> Storage.sol)
  </read_first>
  <action>
    Locate the snapshot mappings by matched substring (NOT line number). Rename
    `lootboxEvScorePacked` to `lootboxPurchasePacked` and widen its value type
    `mapping(uint48 => mapping(address => uint16))` -> `mapping(uint48 => mapping(address => uint256))`.
    Delete the entire `lootboxBaseLevelPacked` declaration (matched substring
    `internal lootboxBaseLevelPacked;` and its `mapping(uint48 => mapping(address => uint24))`
    type line) — its field merges into the packed word. Do NOT touch `lootboxDay` (~1370) or
    `lootboxEvBenefitUsedByLevel` (~1428).

    Add `_packLootboxPurchase(uint16 scorePlus1, uint64 adj, uint24 baseLevelPlus1)` and
    `_unpackLootboxPurchase(uint256 word)` as `internal pure`, placed adjacent to the existing
    `_packEthToMilliEth`/`_unpackMilliEthToWei` cluster (~1347-1365) to match precedent. Encode
    per the LOCKED SPEC §1.1 layout: score+1 at `[0:16]`, adjustedPortion at `[16:80]`,
    baseLevel+1 at `[80:104]`, bits `[104:256]` written/left as zero. Use standard
    mask-and-shift mechanics (encoding mechanics are Claude's discretion per CONTEXT.md; the
    layout is fixed). The pack helper takes an ALREADY-ENCODED `baseLevelPlus1` — it MUST NOT
    add, subtract, or normalize any sentinel offset (DIV-1: Mint passes `cachedLevel + 1`, Whale
    passes `level + 2`; both flow through unchanged). Mask each input to its field width before
    shifting so an over-wide argument cannot alias an adjacent field (defense against
    packed-field aliasing, threat T-310-01).

    Comments describe what IS (the layout), never what changed (no "was uint16, now uint256")
    per feedback_no_history_in_comments.
  </action>
  <verify>
    <automated>grep -c "lootboxPurchasePacked" contracts/storage/DegenerusGameStorage.sol | grep -qv '^0$' && grep -q "function _packLootboxPurchase(uint16 scorePlus1, uint64 adj, uint24 baseLevelPlus1)" contracts/storage/DegenerusGameStorage.sol && grep -q "function _unpackLootboxPurchase(uint256" contracts/storage/DegenerusGameStorage.sol && ! grep -q "lootboxBaseLevelPacked\|lootboxEvScorePacked" contracts/storage/DegenerusGameStorage.sol && grep -q "internal pure returns (uint256)" contracts/storage/DegenerusGameStorage.sol && echo PASS</automated>
  </verify>
  <done>
    `lootboxPurchasePacked` is declared as `mapping(uint48 => mapping(address => uint256))`;
    `lootboxBaseLevelPacked` and `lootboxEvScorePacked` no longer appear anywhere in
    Storage.sol; `_packLootboxPurchase` and `_unpackLootboxPurchase` exist as `internal pure`
    with the SPEC §1.7 signatures; `lootboxDay` and `lootboxEvBenefitUsedByLevel` are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Relocate _lootboxEvMultiplierFromScore + the EV constants it needs into Storage.sol (D-02)</name>
  <files>contracts/storage/DegenerusGameStorage.sol, contracts/modules/DegenerusGameLootboxModule.sol</files>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol (destination — place the relocated fn + constants in the existing constants/helpers region)
    - contracts/modules/DegenerusGameLootboxModule.sol (SOURCE — read `_lootboxEvMultiplierFromScore` ~444-465 and the EV constants ~304-315 to copy the body byte-identical)
    - 310-CONTEXT.md D-02 (EV fn + constants -> shared base; _applyEvMultiplierWithCap STAYS in Lootbox)
    - 309-SPEC.md §0.B (the constant + fn anchors), §4.A (the fn is `pure` — zero SLOADs, multiplier fully determined by its score arg)
  </read_first>
  <action>
    Move `_lootboxEvMultiplierFromScore` from LootboxModule into Storage.sol, changing ONLY its
    visibility from `private pure` to `internal pure`; the body (the score->bps piecewise-linear
    map at ~447-465) is copied BYTE-IDENTICAL. Relocate the six compile-time constants its body
    references — `ACTIVITY_SCORE_NEUTRAL_BPS`, `ACTIVITY_SCORE_MAX_BPS`, `LOOTBOX_EV_MIN_BPS`,
    `LOOTBOX_EV_NEUTRAL_BPS`, `LOOTBOX_EV_MAX_BPS`, `LOOTBOX_EV_BENEFIT_CAP` — into Storage.sol as
    `internal constant` (same values: 6_000 / 25_500 / 8_000 / 10_000 / 13_500 / 10 ether). DELETE
    all six from LootboxModule and DELETE the `_lootboxEvMultiplierFromScore` definition from
    LootboxModule (its callers — open via Plan 02, the two resolvers, the new deposit tally via
    Plan 03 — will resolve to the inherited Storage member).

    Do NOT relocate or alter `_applyEvMultiplierWithCap` — it STAYS `private` in LootboxModule
    (only the two resolvers call it post-IMPL; CONTEXT.md D-02). Do NOT relocate
    `ACTIVITY_SCORE_NEUTRAL_BPS`/etc. by reference — physically move the declarations so
    LootboxModule no longer declares duplicates (avoids the inline-duplication drift bug class,
    Phase 294 precedent). Verify by grep that LootboxModule retains no stale duplicate of any of
    the six constants or the fn.
  </action>
  <verify>
    <automated>grep -q "function _lootboxEvMultiplierFromScore(" contracts/storage/DegenerusGameStorage.sol && grep -q "internal pure returns (uint256)" contracts/storage/DegenerusGameStorage.sol && grep -q "uint16 internal constant LOOTBOX_EV_NEUTRAL_BPS = 10_000;" contracts/storage/DegenerusGameStorage.sol && grep -q "internal constant LOOTBOX_EV_BENEFIT_CAP" contracts/storage/DegenerusGameStorage.sol && grep -q "internal constant ACTIVITY_SCORE_NEUTRAL_BPS" contracts/storage/DegenerusGameStorage.sol && ! grep -q "_lootboxEvMultiplierFromScore" contracts/modules/DegenerusGameLootboxModule.sol && [ "$(grep -c 'LOOTBOX_EV_NEUTRAL_BPS' contracts/modules/DegenerusGameLootboxModule.sol)" = "0" ] && echo PASS</automated>
  </verify>
  <done>
    `_lootboxEvMultiplierFromScore` is `internal pure` in Storage.sol with a byte-identical body;
    all six EV/activity constants are `internal constant` in Storage.sol; LootboxModule contains
    ZERO declarations of those six constants and ZERO definition of
    `_lootboxEvMultiplierFromScore`; `_applyEvMultiplierWithCap` is still defined in LootboxModule.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player -> deposit/open entrypoints | untrusted ETH/BURNIE amounts and call ordering cross here; all packing operates on player-influenced values |
| VRF word -> resolution window | the `[rng request -> unlock]` freeze window; any slot consumed alongside the live word must be frozen pre-word |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-310-01 | Tampering | `_packLootboxPurchase` field encoding | mitigate | Mask each input to its declared bit-width before shifting so an over-wide `scorePlus1`/`adj`/`baseLevelPlus1` cannot alias an adjacent field. Layout fixed by SPEC §1.1: [0:16]/[16:80]/[80:104]. Width proof (SPEC §1.2): adjustedPortion <= 10 ETH < 2^64. Ties to INV-01/INV-03 (allocation correctness). |
| T-310-06 | Information disclosure / freeze | relocation of EV fn must not introduce an SLOAD | mitigate | `_lootboxEvMultiplierFromScore` stays `pure` (zero storage reads) — moving it from `private pure` to `internal pure` changes only visibility, not its read set. No new in-window SLOAD introduced. Ties to INV-06 (no freeze regression). |
| T-310-07 | Tampering | duplicate EV constants across modules (drift) | mitigate | Physically MOVE the six EV constants + the fn to one home (Storage); grep-assert LootboxModule retains zero duplicates. Single source of truth eliminates the inline-duplication drift bug class (Phase 294 BURNIE precedent / feedback_verify_call_graph_against_source). |
| T-310-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan; pure Solidity edits to an existing tree. No new dependency surface. |
</threat_model>

<verification>
- `grep` gates in each task PASS (substring presence/absence checks above).
- Compilation is NOT gated in this plan — `forge build` runs as the cross-cutting build gate in
  Plan 03 AFTER all consumers are wired (Storage symbols are referenced by Lootbox/Mint/Whale,
  so an isolated build here would surface only Storage-internal errors). The executor MAY run
  `forge build` informationally but a transient unresolved-reference error from not-yet-edited
  consumer modules is expected until Plan 03.
</verification>

<success_criteria>
- `lootboxPurchasePacked` (uint256) replaces both `lootboxEvScorePacked` and
  `lootboxBaseLevelPacked` — net -1 slot, no new slot (SPEC §1.8).
- `_packLootboxPurchase` / `_unpackLootboxPurchase` exist as `internal pure` per SPEC §1.7,
  encoding the SPEC §1.1 layout, taking an already-encoded `baseLevelPlus1` (no offset
  normalization, DIV-1 preserved).
- `_lootboxEvMultiplierFromScore` + its six compile-time constants are relocated to Storage.sol
  as `internal`/`internal constant` (D-02); LootboxModule holds no duplicates.
- `_applyEvMultiplierWithCap` remains in LootboxModule; `lootboxDay` and
  `lootboxEvBenefitUsedByLevel` are untouched.
- NO contract commit in this plan (deferred to the Plan 03 USER-APPROVAL gate).
</success_criteria>

<output>
Create `.planning/phases/310-implementation-single-batched-user-approved-contract-diff-im/310-01-SUMMARY.md` when done.
Record: the relocated symbol set, the final packed-word layout as implemented, and an explicit
note that `contracts/storage/DegenerusGameStorage.sol` is left UNCOMMITTED for the Plan 03
batched diff.
</output>
