---
phase: 238-backward-forward-freeze-proofs
plan: 238-03
type: execute
wave: 2
depends_on: [238-01, 238-02]
files_modified:
  - audit/v30-238-03-GATING.md
  - audit/v30-FREEZE-PROOF.md
autonomous: true
requirements: [FWD-03]
requirements_addressed: [FWD-03]
head_anchor: 7ab515fe
head_sha: 7ab515fe
must_haves:
  truths:
    - "`audit/v30-238-03-GATING.md` contains a `## Gating Verification Table` with exactly 146 rows, one per INV-237-NNN Row ID in `audit/v30-CONSUMER-INVENTORY.md` Universe List (no rows added, no rows dropped)."
    - "Per D-06 the Gating Verification Table has these 6 columns in this exact order: `Row ID | Forward Mutation Paths (from 238-02) | Named Gate | Gate Site File:Line | Mutation-Path Coverage | Effectiveness Proof`."
    - "Per D-06/D-13 the Named Gate column is exactly one of 5 locked values: `rngLocked` / `lootbox-index-advance` / `phase-transition-gate` / `semantic-path-gate` / `NO_GATE_NEEDED_ORTHOGONAL`. Per D-14: any row that would require a gate OUTSIDE this 4-gate taxonomy (or whose FWD-02 mutation paths are not covered by `NO_GATE_NEEDED_ORTHOGONAL`) is a `CANDIDATE_FINDING` and produces a Finding Candidate block."
    - "Per D-06 the Mutation-Path Coverage column is exactly one of 3 locked values: `EVERY_PATH_BLOCKED` / `PARTIAL_COVERAGE` / `NO_GATE_NEEDED_ORTHOGONAL`. Per D-14: `PARTIAL_COVERAGE` rows are `CANDIDATE_FINDING` rows — any ungated path is a finding candidate routed to Phase 242 FIND-01."
    - "Per D-11/D-14 exactly 22 rows are routed through the EXCEPTION path: EXC-01 = 2 rows (Named Gate `NO_GATE_NEEDED_ORTHOGONAL` or `semantic-path-gate` — non-VRF seed path has no VRF mutation window; verdict cites KI-1), EXC-02 = 8 rows (Named Gate `semantic-path-gate` citing `GAMEOVER_RNG_FALLBACK_DELAY = 14 days` at `DegenerusGameAdvanceModule.sol:109` + gate at `:1252` per KI-2), EXC-03 = 4 rows (Named Gate `semantic-path-gate` citing terminal-state gameover invariant — `_swapAndFreeze` at `:292` / `_swapTicketSlot` at `:1082` / `_gameOverEntropy` at `:1222-1246` per KI-3), EXC-04 = 8 rows (Named Gate `lootbox-index-advance` — EntropyLib.entropyStep is seeded per-player-per-day-per-amount via keccak(rngWord, player, day, amount) with VRF-derived `rngWord`; the XOR-shift determinism is accepted per KI-4 — gating is still `lootbox-index-advance` since consumption happens inside the mid-day-lootbox fulfillment window)."
    - "A `## Gate Coverage Heatmap` sub-section is present (optional per CONTEXT.md Claude's Discretion — MANDATORY in this plan) at the top of the Gating Verification results: Path Family × Named Gate matrix showing row counts — this gives an at-a-glance readability aid and surfaces any gate-taxonomy outliers before the full 146-row table."
    - "Per D-09/D-10 a `## Prior-Artifact Cross-Cites` section lists cross-cites for the 4 named gates: 235-05-TRNX-01.md for `rngLocked` state-machine re-proof, 232.1-03-PFTB-AUDIT.md for `semantic-path-gate` archetype (non-zero-entropy + pre-finalize + queue-length + do-while gates), Phase 235 D-13 Path 4 for `phase-transition-gate` (admits only advanceGame-origin writes); every cite carries a `re-verified at HEAD 7ab515fe` note + structural-equivalence statement. Per Claude's Discretion in CONTEXT.md: if Phase 239 RNG-01/RNG-03 has not committed at this task's run time, 238-03 states the `rngLocked` + `lootbox-index-advance` gate correctness as an audit assumption pending Phase 239 re-proof (NOT as a first-principles re-derivation). If Phase 239 has committed, cross-cite 239 directly."
    - "`audit/v30-FREEZE-PROOF.md` is the FINAL CONSOLIDATED deliverable per D-16 — assembled in Task 3 of this plan by merging `audit/v30-238-01-BWD.md` + `audit/v30-238-02-FWD.md` + `audit/v30-238-03-GATING.md` row-by-row. Contains: 1-row-per-INV-237-NNN Consolidated Freeze-Proof Table (BWD-Verdict + BWD-03 Verdict + FWD-Verdict + Named Gate + Mutation-Path Coverage + Effectiveness Verdict + KI Cross-Ref) + Consumer Index section (per D-16 + 237 D-10 pattern) mapping v30.0 BWD-01/02/03 + FWD-01/02/03 requirements to `ALL` 146 rows and cross-citing Phase 239 RNG / Phase 240 GO / Phase 241 EXC / Phase 242 FIND consumer subsets."
    - "Per D-17 a merged `## Finding Candidates` appendix in `audit/v30-FREEZE-PROOF.md` consolidates Finding Candidates from all three plan outputs (238-01 + 238-02 + 238-03) into a single Phase 242 FIND-01 intake — deduplicated, with per-item source attribution (`— surfaced in 238-01 BWD-03` / `— surfaced in 238-02 FWD-02` / `— surfaced in 238-03 FWD-03 gating`)."
    - "Per D-15 NO `F-30-NN` finding IDs appear anywhere in `audit/v30-238-03-GATING.md` OR `audit/v30-FREEZE-PROOF.md`. Phase 242 FIND-01..03 owns ID assignment."
    - "Per D-20 READ-only scope holds throughout all 3 tasks: `git diff --name-only 7ab515fe..HEAD -- contracts/ test/` returns empty at task start and end; `git status --porcelain contracts/ test/` returns empty before and after each task."
    - "Per D-19 HEAD anchor `7ab515fe` is locked in frontmatter + echoed in both files' `Audit baseline` lines."
    - "Per D-18 `audit/v30-CONSUMER-INVENTORY.md`, `audit/v30-238-01-BWD.md`, `audit/v30-238-02-FWD.md` are NOT edited by this plan — all READ-only after their respective commits. `audit/v30-FREEZE-PROOF.md` is a NEW file assembled from them, not an edit in place."
    - "Row-ID integrity across both deliverables: every Gating Verification Table Row ID also appears in `audit/v30-CONSUMER-INVENTORY.md` Universe List, `audit/v30-238-01-BWD.md` Backward Freeze Table, and `audit/v30-238-02-FWD.md` Forward Enumeration Table. The consolidated `audit/v30-FREEZE-PROOF.md` has the same Row ID set. Verifiable via sorted-unique-diff of INV-237-NNN grep extractions across all 4 files returning empty."
  artifacts:
    - path: "audit/v30-238-03-GATING.md"
      provides: "FWD-03 per-consumer gating verification — 146-row Gating Verification Table + 4-gate D-13 taxonomy + Gate Coverage Heatmap + Finding Candidates"
      contains: "## Gating Verification Table"
    - path: "audit/v30-FREEZE-PROOF.md"
      provides: "FINAL consolidated v30.0 Backward & Forward Freeze Proof deliverable per D-16 — merged 238-01 + 238-02 + 238-03 output with Consumer Index mapping BWD-01/02/03 + FWD-01/02/03 to ALL 146 rows and cross-citing Phase 239 RNG / Phase 240 GO / Phase 241 EXC / Phase 242 FIND subsets"
      contains: "## Consolidated Freeze-Proof Table"
  key_links:
    - from: "audit/v30-238-02-FWD.md ## Forward Mutation Paths table (per-Row-ID slot/actor/gate-required tuples)"
      to: "audit/v30-238-03-GATING.md ## Gating Verification Table (per-Row-ID gating verification)"
      via: "D-02/D-05 Wave-2 dependency — 238-03 joins 238-02 Forward Mutation Paths by Row ID"
      pattern: "INV-237-[0-9]{3}"
    - from: "CONTEXT.md D-13 closed 4-gate taxonomy (rngLocked / lootbox-index-advance / phase-transition-gate / semantic-path-gate)"
      to: "Gating Verification Table Named Gate column + Gate Coverage Heatmap"
      via: "D-06/D-14 closed taxonomy — any row outside taxonomy is CANDIDATE_FINDING per D-14"
      pattern: "rngLocked|lootbox-index-advance|phase-transition-gate|semantic-path-gate"
    - from: "audit/v30-238-01-BWD.md + audit/v30-238-02-FWD.md + audit/v30-238-03-GATING.md"
      to: "audit/v30-FREEZE-PROOF.md Consolidated Freeze-Proof Table"
      via: "D-16 single-consolidated-deliverable per Phase 237 D-08 precedent — row-by-row merge"
      pattern: "v30-238-(01|02|03)"
    - from: "v30.0 REQUIREMENTS.md BWD-01/02/03 + FWD-01/02/03 (Phase 238 scope) + RNG/GO/EXC/REG/FIND (Phase 239-242 scope)"
      to: "audit/v30-FREEZE-PROOF.md ## Consumer Index"
      via: "D-16 requirement → Row-ID-subset mapping; cross-cites downstream-phase subsets per 237 D-10 pattern"
      pattern: "BWD-0[1-3]|FWD-0[1-3]|RNG-0[1-3]|GO-0[1-5]|EXC-0[1-4]|REG-0[1-2]|FIND-0[1-3]"
---

<objective>
FWD-03 per-consumer gating verification + FINAL CONSOLIDATED `audit/v30-FREEZE-PROOF.md` assembly. Runs in Wave 2 per D-01/D-02, depends on Wave 1 plans 238-01 + 238-02 being committed. Two-file deliverable per D-16:
- `audit/v30-238-03-GATING.md` — FWD-03 plan output: 146-row Gating Verification Table + 4-gate D-13 taxonomy application + Gate Coverage Heatmap + Finding Candidates
- `audit/v30-FREEZE-PROOF.md` — FINAL consolidated Phase 238 deliverable assembled in Task 3 by merging 238-01 + 238-02 + 238-03 outputs row-by-row + Consumer Index mapping every BWD/FWD requirement back to Row IDs + downstream-phase cross-cites (Phase 239 RNG, Phase 240 GO, Phase 241 EXC, Phase 242 FIND)

**Single requirement satisfied (directly):**
- **FWD-03** — per-consumer gating verification: for each consumer, the specific gating mechanism (`rngLocked` / `lootbox-index-advance` / `phase-transition-gate` / `semantic-path-gate`) actually blocks every forward mutation path identified in FWD-01/02. Gating must be demonstrated effective, never assumed. Per D-13 the named-gate taxonomy is closed to 4 values; per D-14 any row whose FWD-02 mutation paths are not covered by one of the 4 named gates is a `CANDIDATE_FINDING`.

**Input contracts (Wave 1 dependencies — MUST be committed before this plan's Task 1 runs):**
- `audit/v30-CONSUMER-INVENTORY.md` — Phase 237 authoritative inventory (146 INV-237-NNN rows; READ-only per D-18)
- `audit/v30-238-01-BWD.md` — Plan 238-01 Backward Freeze Table + Backward Adversarial Closure Table (READ-only per D-16 — source merge input)
- `audit/v30-238-02-FWD.md` — Plan 238-02 Forward Enumeration Table + Forward Mutation Paths (direct input to FWD-03; READ-only per D-16 — source merge input)

**Deliverables per D-16 (single consolidated + 3 plan files — Phase 237 D-08 precedent):** 4 files total across the phase, of which this plan produces 2:
- `audit/v30-238-03-GATING.md` (this plan Task 1 output)
- `audit/v30-FREEZE-PROOF.md` (this plan Task 3 final consolidation output)

**Scope fidelity:** ALL 146 rows in the Gating Verification Table. 22 KI-exception rows carry EXCEPTION routing per D-11 (acceptance re-verification is Phase 241 scope). 19 gameover-flow rows audited on same 146-row basis per D-12. 13 mid-day-lootbox rows mapped to `lootbox-index-advance` gate per D-13 (CONTEXT.md `<code_context>` — Phase 239 RNG-03 asymmetry re-justification informs this gate).

**Phase 239 dependency (per CONTEXT.md Claude's Discretion):** The `rngLocked` state-machine correctness is Phase 239 RNG-01's scope; `lootbox-index-advance` asymmetry correctness is Phase 239 RNG-03's scope. Per ROADMAP, Phase 239 may execute in parallel with Phase 238. Handling:
- IF Phase 239 has committed RNG-01 / RNG-03 before this task's run time (detectable via `git log --oneline -- .planning/phases/239-*/239-*-SUMMARY.md`): cross-cite directly with `re-verified at HEAD 7ab515fe` note.
- IF Phase 239 has NOT committed: state `rngLocked` + `lootbox-index-advance` correctness as an **audit assumption pending Phase 239 RNG-01/RNG-03 re-proof**, cite 235-05-TRNX-01.md (v29.0 rngLocked 4-path re-proof) + the 232.1 non-zero-entropy invariants as corroborating evidence, mark a Scope-Guard Deferral recording the assumption, and route the cross-check to Phase 242 FIND consolidation.

**Fresh-eyes mandate (per D-09/D-10):** Every gating verdict is re-derived at HEAD `7ab515fe` from reading `contracts/` + 238-02 Forward Mutation Paths. Prior-milestone artifacts CROSS-CITED with re-verified-at-HEAD notes.

**No F-30-NN emission per D-15.** `CANDIDATE_FINDING` rows produce Finding Candidate blocks in the plan's appendix + the consolidated file's merged Finding Candidates appendix (per D-17).

**READ-only scope per D-20.** Only writes: `audit/v30-238-03-GATING.md` + `audit/v30-FREEZE-PROOF.md`. Prior Wave 1 outputs (238-01, 238-02) and Phase 237 output (CONSUMER-INVENTORY) MUST remain unmodified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/238-backward-forward-freeze-proofs/238-CONTEXT.md
@audit/v30-CONSUMER-INVENTORY.md
@KNOWN-ISSUES.md
</context>

<threat_model>
READ-only audit milestone: no runtime code modified, no test modified, no contracts/ or test/ writes. Attack surface added: NONE. Only writes: markdown under `audit/v30-238-03-GATING.md` + `audit/v30-FREEZE-PROOF.md` (both read-only post-commit per D-18). No STRIDE category applies to documentation-only output; threat register empty by construction. Inherits parent phase summary.

Residual risk: an incorrect gating verdict (assigning `EVERY_PATH_BLOCKED` to a row whose FWD-02 mutation paths are actually ungated) masks a real determinism violation from Phase 242 FIND-01 intake. Mitigation: (a) the Gating Verification Table joins against 238-02's Forward Mutation Paths table by Row ID — every mutation path from 238-02 MUST appear in a Gate-Column cell; if a path from 238-02 is not covered by a D-13 named gate, per D-14 it is a `CANDIDATE_FINDING` and surfaces in Finding Candidates; (b) Gate Coverage Heatmap (Path Family × Named Gate) makes distribution anomalies visible at a glance; (c) the `rngLocked` gate cross-cites 235-05-TRNX-01.md (v29.0 RNG-01 state-machine re-proof) or Phase 239 RNG-01 if committed, with re-verified-at-HEAD notes making drift detectable; (d) the 22 KI-exception rows carry exact quoted KI headers in the `EXCEPTION` column, so divergence from KNOWN-ISSUES.md acceptance envelope surfaces as grep-diff; (e) the consolidated file's Consumer Index is cross-checked against the 237 Consumer Index distribution (EXC 22 / GO 19 / etc.) at assembly time.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build the 146-row FWD-03 Gating Verification Table + Gate Coverage Heatmap + Finding Candidates → audit/v30-238-03-GATING.md</name>
  <files>audit/v30-238-03-GATING.md</files>
  <read_first>
    - .planning/phases/238-backward-forward-freeze-proofs/238-CONTEXT.md (D-06 Gating Verification Table shape; D-13 closed 4-gate taxonomy; D-14 escape rule; D-09/D-10 fresh re-prove + cross-cite; D-15 no F-30-NN; D-18 prior-artifact READ-only; D-19 HEAD anchor; D-20 READ-only scope)
    - .planning/REQUIREMENTS.md (FWD-03 exact wording — "gating must be proven effective, not assumed"; names the 4 gates `rngLocked` / `lootbox-index-advance` / `phase-transition-gate` / `semantic-path-gate`)
    - audit/v30-238-02-FWD.md ## Forward Mutation Paths section — DIRECT INPUT (authoritative per-Row-ID slot/actor/gate-required tuples that this table verifies gating against); MUST be committed before this task starts
    - audit/v30-238-01-BWD.md — reference for row set integrity check; MUST be committed
    - audit/v30-CONSUMER-INVENTORY.md — authoritative 146-row Universe List (READ-only per D-18); section `## Universe List` (line 64); Path Family + Subcategory columns (drive Gate Coverage Heatmap rows); Consumer Index (line 2310)
    - KNOWN-ISSUES.md — 5 quoted KI headers for EXCEPTION routing (per 238-01/238-02)
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD 7ab515fe — gate site lines (load-bearing for Gate Site File:Line column):
        * `rngLocked` — `rngLockedFlag` SSTORE set at `requestRandomWords(...)` call site; cleared at `rawFulfillRandomWords(...)` :1690; gate check at `rngGate(...)` :1141
        * `phase-transition-gate` — `phaseTransitionActive` branch at `:283` (per D-13 + Phase 235 D-13 Path 4: admits only advanceGame-origin writes)
        * `semantic-path-gate` archetypes:
            - `rawFulfillRandomWords` L1698 zero-guard
            - `rngGate` L291 sentinel-1 break (cross-cite Phase 235 D-13)
            - `_processFutureTicketBatch` pre-finalize gate + queue-length gate + do-while gate (per 232.1-03-PFTB-AUDIT.md 4-path walk)
            - `GAMEOVER_RNG_FALLBACK_DELAY` 14-day gate at `:109` + gameover-entropy gating at `:1252` (KI EXC-02 path)
    - contracts/modules/DegenerusGameLootboxModule.sol at HEAD 7ab515fe — `lootbox-index-advance` gate sites: `lootboxRngWordByIndex[advancedIndex]` SSTORE at RNG-request time (index increment) versus `lootboxRngWordByIndex[consumerIndex]` SLOAD at consumption time (index asymmetry isolation per KI-5 + D-13)
    - contracts/storage/DegenerusGameStorage.sol at HEAD 7ab515fe — `rngLockedFlag`, `lootboxRngWordByIndex`, `phaseTransitionActive`, `vrfRequestId` slot layout
    - ACCESS-CONTROL-MATRIX.md at HEAD 7ab515fe (repo-root audit artifact) — `onlyAdmin` modifier surface (corroboration for admin-actor `PATH_BLOCKED_BY_GATE` claims from 238-02)
    - STORAGE-WRITE-MAP.md at HEAD 7ab515fe (repo-root audit artifact) — SSTORE site enumeration (corroboration for gate site lines)
    - .planning/milestones/v29.0-phases/235-conservation-rng-commitment-re-proof-phase-transition/235-05-AUDIT.md (TRNX-01 rngLocked invariant 4-path re-proof — load-bearing cross-cite for `rngLocked` gate per D-09)
    - .planning/milestones/v29.0-phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-PFTB-AUDIT.md (non-zero-entropy guarantee at 4 _processFutureTicketBatch call sites — cross-cite source for `semantic-path-gate` archetype per D-13)
    - .planning/milestones/v29.0-phases/235-conservation-rng-commitment-re-proof-phase-transition/235-CONTEXT.md D-13 — Path 4 phaseTransitionActive proof (cross-cite source for `phase-transition-gate` per D-13)
    - .planning/phases/239-*/239-*-SUMMARY.md (conditionally — ONLY if Phase 239 RNG-01 / RNG-03 has committed by this task's run time; otherwise treat as not-yet-available and state audit assumption per CONTEXT.md Claude's Discretion)
  </read_first>
  <action>
Execute the FWD-03 per-consumer gating verification per CONTEXT.md D-06/D-13/D-14.

**Verify Wave 2 prerequisites — all 3 Wave 1 inputs MUST be committed:**
```
test -f audit/v30-CONSUMER-INVENTORY.md
test -f audit/v30-238-01-BWD.md
test -f audit/v30-238-02-FWD.md
[ "$(grep -c '^| INV-237-' audit/v30-CONSUMER-INVENTORY.md)" = "146" ]
git log --oneline -1 -- audit/v30-CONSUMER-INVENTORY.md   # MUST show Phase 237 commit
git log --oneline -1 -- audit/v30-238-01-BWD.md           # MUST show Plan 238-01 commit
git log --oneline -1 -- audit/v30-238-02-FWD.md           # MUST show Plan 238-02 commit
git diff --name-only 7ab515fe..HEAD -- contracts/ test/   # MUST be empty (D-19/D-20)
git status --porcelain contracts/ test/                   # MUST be empty (D-20)
```
If ANY check fails, STOP — Wave 1 (238-01 + 238-02) has not completed. Per D-02 topology, 238-03 Wave 2 runs strictly after Wave 1.

**Check Phase 239 availability (for CONTEXT.md Claude's Discretion cross-cite decision):**
```
ls .planning/phases/239-*/239-*-SUMMARY.md 2>/dev/null
```
- If Phase 239 RNG-01 SUMMARY exists and is committed: cross-cite directly with re-verified-at-HEAD-7ab515fe note.
- If not: state `rngLocked` + `lootbox-index-advance` correctness as audit assumption pending Phase 239 RNG-01/RNG-03 re-proof; cite 235-05-TRNX-01.md as v29.0 corroborating re-proof; record a Scope-Guard Deferral per D-18 flagging the assumption for Phase 242 cross-check.

**Step 1 — Parse 238-02's `## Forward Mutation Paths` table** from `audit/v30-238-02-FWD.md`. For each Row ID, extract the set of (storage slot, actor, gate-required) tuples. This is the authoritative FWD-03 input per D-05.

**Step 2 — For each of the 146 Universe List rows, derive the FWD-03 gating verification row.** Per D-06 Gating Verification Table shape — 6 columns in this exact order:

```
| Row ID | Forward Mutation Paths (from 238-02) | Named Gate | Gate Site File:Line | Mutation-Path Coverage | Effectiveness Proof |
```

Row construction rules:
- **Row ID** — copy from inventory.
- **Forward Mutation Paths (from 238-02)** — cite 238-02 Forward Mutation Paths entries for this Row ID (e.g., `{slot: rngWordCurrent, actor: admin, gate-required: rngLocked}; {slot: rngLockedFlag, actor: admin, gate-required: rngLocked}` — or `∅ (no mutation paths — all actor-cells NO_REACHABLE_PATH)`).
- **Named Gate** — exactly one of 5 locked values per D-06/D-13:
    - `rngLocked` — for `daily` path-family rows whose FWD-02 admin/validator paths are gated by `rngLockedFlag` (set at VRF request, cleared at fulfillment). Per D-13 Phase 239 RNG-01 re-proves state-machine from first principles; cross-cite 235-05-TRNX-01.md as v29.0 4-path re-proof.
    - `lootbox-index-advance` — for `mid-day-lootbox` path-family rows (19 rows; 13 of them flagged "index-advance" per 237 D-06 distribution). Consumption-time read uses `consumerIndex` frozen at a prior index value; RNG-request time SSTOREs write to `lootboxRngWordByIndex[advancedIndex]` which is a DIFFERENT slot. Per D-13 Phase 239 RNG-03 re-justifies asymmetry.
    - `phase-transition-gate` — for any row whose FWD-02 mutation path crosses a `phaseTransitionActive` branch. Per D-13 + Phase 235 D-13 Path 4: admits only advanceGame-origin writes; creates no player-reachable mutation path.
    - `semantic-path-gate` — for rows whose gating is a function-specific early-return / revert / sentinel. Archetypes per D-13: `rawFulfillRandomWords` L1698 zero-guard, `rngGate` L291 sentinel-1 break, `_processFutureTicketBatch` pre-finalize + queue-length + do-while gates (232.1 fix series), `GAMEOVER_RNG_FALLBACK_DELAY` 14-day gate at `:109` + gameover-entropy gating at `:1252` (KI EXC-02). Also covers the 7 pure `gameover-entropy` rows (post-finalize one-way).
    - `NO_GATE_NEEDED_ORTHOGONAL` — for rows whose FWD-02 Forward Mutation Paths are empty (`∅`) — no mutation paths exist for any actor, so no gate is needed. Typically applies to rows whose consumption-site storage reads are Oracle-only (VRF coordinator writes via `rawFulfillRandomWords`) with no admin/player/validator write path to the same slot.
- **Gate Site File:Line** — cite the specific gate site in `contracts/` at HEAD 7ab515fe. Examples:
    - `rngLocked` → `contracts/modules/DegenerusGameAdvanceModule.sol:1141` (rngGate check) + `contracts/storage/DegenerusGameStorage.sol:<line>` (rngLockedFlag SSTORE)
    - `lootbox-index-advance` → `contracts/modules/DegenerusGameAdvanceModule.sol:1088` (requestLootboxRng — index-advance write site) + `contracts/modules/DegenerusGameLootboxModule.sol:<line>` (consumerIndex SLOAD)
    - `phase-transition-gate` → `contracts/modules/DegenerusGameAdvanceModule.sol:283` (phaseTransitionActive branch)
    - `semantic-path-gate` → specific to row; e.g., `contracts/modules/DegenerusGameAdvanceModule.sol:109` + `:1252` for EXC-02 prevrandao 14-day gate; or `contracts/modules/DegenerusGameAdvanceModule.sol:1698` for rawFulfillRandomWords zero-guard
    - `NO_GATE_NEEDED_ORTHOGONAL` → `—` (no gate site)
- **Mutation-Path Coverage** — exactly one of 3 D-06 values:
    - `EVERY_PATH_BLOCKED` — every (slot, actor, gate-required) tuple from 238-02's Forward Mutation Paths for this Row ID is covered by the Named Gate. Default for SAFE rows.
    - `PARTIAL_COVERAGE` — at least one mutation path from 238-02 is NOT covered by any D-13 named gate. Per D-14: this is a `CANDIDATE_FINDING` and MUST produce a Finding Candidate block.
    - `NO_GATE_NEEDED_ORTHOGONAL` — 238-02 Forward Mutation Paths was `∅` (no mutation paths); no gate needed.
- **Effectiveness Proof** — one-line justification citing the gate's effectiveness evidence. Examples:
    - `rngLocked` → "rngLockedFlag set at requestRandomWords; cleared at rawFulfillRandomWords; intermediate admin/player writes revert via rngGate guard. Cross-cite 235-05-TRNX-01.md 4-path re-proof (re-verified at HEAD 7ab515fe — rngGate body unchanged from v29.0 1646d5af)."
    - `lootbox-index-advance` → "consumerIndex frozen at prior value at request time; next RNG-request increments index; next fulfillment writes to lootboxRngWordByIndex[advancedIndex] — a DIFFERENT slot. No write to consumerIndex slot between request and consumption. Per KI-5 'Lootbox RNG uses index advance isolation'. Cross-cite Phase 239 RNG-03 (if committed) or 237 Plan 03 callgraph (re-verified at HEAD 7ab515fe)."
    - `phase-transition-gate` → "phaseTransitionActive branch at AdvanceModule:283 admits only advanceGame-origin writes. Cross-cite Phase 235 D-13 Path 4 (re-verified at HEAD 7ab515fe — branch condition unchanged)."
    - `semantic-path-gate` → row-specific; e.g. for EXC-02: "GAMEOVER_RNG_FALLBACK_DELAY = 14 days at AdvanceModule:109; gate at AdvanceModule:1252; path only reachable after 14-day VRF outage. Accepted per KI-2. Cross-cite KNOWN-ISSUES.md Gameover prevrandao fallback entry (re-verified at HEAD 7ab515fe — gating constants unchanged)."
    - `NO_GATE_NEEDED_ORTHOGONAL` → "FWD-02 Forward Mutation Paths = ∅. No actor has reachable write path to any consumption-site storage slot between VRF request and consumption. No gate required by construction."

**EXC row gating routing (enforced):**
- EXC-01 (2 rows INV-237-005, INV-237-006): non-VRF seed has no VRF-request commitment window; Named Gate `NO_GATE_NEEDED_ORTHOGONAL` (no mutation-path universe applies) OR `semantic-path-gate` (the affiliate-roll function's internal structure is the gate — no interleaved VRF consumption). Effectiveness Proof cites `[KI: "Non-VRF entropy for affiliate winner roll"]`.
- EXC-02 (8 rows prevrandao-fallback): Named Gate `semantic-path-gate`; Gate Site `contracts/modules/DegenerusGameAdvanceModule.sol:109` + `:1252`; Effectiveness Proof cites `[KI: "Gameover prevrandao fallback"]` + 14-day delay.
- EXC-03 (4 rows mid-cycle-substitution): Named Gate `semantic-path-gate` (terminal-state gameover); Gate Site `contracts/modules/DegenerusGameAdvanceModule.sol:292` + `:1082` + `:1222-1246`; Effectiveness Proof cites `[KI: "Gameover RNG substitution for mid-cycle write-buffer tickets"]`.
- EXC-04 (8 rows EntropyLib): Named Gate `lootbox-index-advance` (consumption happens inside mid-day-lootbox fulfillment window); Effectiveness Proof cites `[KI: "EntropyLib XOR-shift PRNG for lootbox outcome rolls"]` — XOR-shift determinism is accepted; seeding via keccak(rngWord, player, day, amount) with VRF-derived rngWord is the freeze envelope.

**Step 3 — Build the Gate Coverage Heatmap (per CONTEXT.md Claude's Discretion — MANDATORY in this plan).** Path Family × Named Gate matrix showing row counts. Expected distribution (executor re-derives at HEAD from inventory Path Family + this plan's Named Gate column):

```
|                       | rngLocked | lootbox-index-advance | phase-transition-gate | semantic-path-gate | NO_GATE_NEEDED_ORTHOGONAL | Total |
|-----------------------|-----------|-----------------------|------------------------|--------------------|----------------------------|-------|
| daily (91 rows)       |  91       |   —                   |   (subset incl)        |   (subset incl)    |   —                        |  91   |
| mid-day-lootbox (19)  |  —        |   19 (incl 8 EXC-04)  |   —                    |   —                |   —                        |  19   |
| gap-backfill (3)      |  —        |   —                   |   —                    |    3               |   —                        |   3   |
| gameover-entropy (7)  |  —        |   —                   |   (subset)             |    7               |   —                        |   7   |
| other (26)            |  mixed    |   mixed               |   mixed                |   mixed            |   2 (EXC-01 affiliate)     |  26   |
| **Total (146)**       |   N       |    N                  |    N                   |    N               |    N                       | 146   |
```

The executor re-derives exact counts from the 146-row Gating Verification Table at HEAD 7ab515fe; the table above is a shape guide, not a committed distribution. Row totals MUST sum to 146.

**Step 4 — Write `audit/v30-238-03-GATING.md` with EXACTLY these sections in this order:**

```markdown
# v30.0 Phase 238 Plan 03 — FWD-03 Per-Consumer Gating Verification

**Audit baseline:** HEAD `7ab515fe` (contract tree identical to v29.0 `1646d5af`; post-v29 commits are docs-only per D-19).
**Requirement addressed:** FWD-03 — per-consumer gating verification: the specific gating mechanism actually blocks every forward mutation path identified in FWD-01/02. Gating demonstrated effective, never assumed.
**Scope:** ALL 146 INV-237-NNN rows from `audit/v30-CONSUMER-INVENTORY.md` — no sampling (per D-11).
**Input:** `audit/v30-238-02-FWD.md` ## Forward Mutation Paths table (per D-05 — Plan 238-02 output).
**Closed 4-gate taxonomy (per D-13):** `rngLocked` / `lootbox-index-advance` / `phase-transition-gate` / `semantic-path-gate` + `NO_GATE_NEEDED_ORTHOGONAL` for orthogonal rows. Per D-14: any row outside this taxonomy is `CANDIDATE_FINDING`.
**Phase 239 dependency (per Claude's Discretion):** `rngLocked` + `lootbox-index-advance` gate correctness cross-cites Phase 239 RNG-01/RNG-03 if committed; otherwise stated as audit assumption pending 239 re-proof with 235-05-TRNX-01.md cited as v29.0 corroborating evidence.
**Fresh-eyes re-derivation (per D-09/D-10):** Every verdict re-derived at HEAD 7ab515fe.
**Finding-ID emission (per D-15):** NO F-30-NN. CANDIDATE_FINDING + EXCEPTION rows surface in Finding Candidates.
**READ-only scope (per D-20):** zero contracts/ test/ writes; zero edits to `audit/v30-CONSUMER-INVENTORY.md`, `audit/v30-238-01-BWD.md`, `audit/v30-238-02-FWD.md`.

## Table of Contents

1. [Gate Coverage Heatmap](#gate-coverage-heatmap)
2. [Gating Verification Table](#gating-verification-table) — FWD-03 (146 rows × 6 columns)
3. [Prior-Artifact Cross-Cites](#prior-artifact-cross-cites) — D-09/D-10 + Phase 239 handling
4. [Finding Candidates](#finding-candidates) — D-14/D-15/D-17
5. [Scope-Guard Deferrals](#scope-guard-deferrals) — D-18 (includes Phase 239 assumption if applicable)
6. [Attestation](#attestation)

## Gate Coverage Heatmap

**Path Family × Named Gate distribution.** Row totals sum to 146.

[Executor derives exact counts at HEAD from the Gating Verification Table; table above is a shape guide.]

## Gating Verification Table

**One row per INV-237-NNN (146 rows total).** Columns per D-06:

| Row ID | Forward Mutation Paths (from 238-02) | Named Gate | Gate Site File:Line | Mutation-Path Coverage | Effectiveness Proof |
|---|---|---|---|---|---|
| INV-237-001 | {slot: rngWordCurrent, actor: admin, gate-required: rngLocked}; {slot: rngLockedFlag, actor: admin, gate-required: rngLocked} | rngLocked | contracts/modules/DegenerusGameAdvanceModule.sol:1141 (rngGate check) + storage SSTORE at :<line> | EVERY_PATH_BLOCKED | rngLockedFlag set at requestRandomWords; cleared at rawFulfillRandomWords; intermediate writes revert via rngGate guard. Cross-cite 235-05-TRNX-01.md 4-path re-proof (re-verified at HEAD 7ab515fe). |
| ... | ... | ... | ... | ... | ... |
| INV-237-146 | ... | ... | ... | ... | ... |

[Every Row ID from the inventory gets exactly one row. Sort by Row ID ascending. No placeholder tokens. No F-30-NN. Every Named Gate cell is one of 5 locked values; every Mutation-Path Coverage cell is one of 3 locked values.]

## Prior-Artifact Cross-Cites

Per D-09/D-10 — every cross-cite carries a `re-verified at HEAD 7ab515fe` note.

| Prior Artifact | Gate Type Cross-Cited | Re-Verified Statement |
|---|---|---|
| `.planning/milestones/v29.0-phases/235-*/235-05-AUDIT.md` (TRNX-01 rngLocked invariant 4-path re-proof) | `rngLocked` | re-verified at HEAD 7ab515fe — rngGate body unchanged from 1646d5af; state-machine 4 paths (set, clear-on-fulfill, revert-on-interim-write, revert-on-double-set) unchanged |
| `.planning/milestones/v29.0-phases/232.1-*/232.1-03-PFTB-AUDIT.md` (non-zero-entropy + pre-finalize + queue-length + do-while gates) | `semantic-path-gate` (archetypes) | re-verified at HEAD 7ab515fe — 4 _processFutureTicketBatch call sites unchanged; invariant tests 8/8 PASS |
| `.planning/milestones/v29.0-phases/235-*/235-CONTEXT.md` D-13 Path 4 | `phase-transition-gate` | re-verified at HEAD 7ab515fe — phaseTransitionActive branch at AdvanceModule:283 unchanged; admits only advanceGame-origin writes |
| KNOWN-ISSUES.md ("Lootbox RNG uses index advance isolation") | `lootbox-index-advance` | re-verified at HEAD 7ab515fe — index-advance asymmetry intact; lootboxRngWordByIndex write at request = advancedIndex, read at consumption = consumerIndex (frozen at prior value); pending Phase 239 RNG-03 first-principles re-justification |
| Phase 239 RNG-01 / RNG-03 SUMMARY (conditional — ONLY if committed before this task's run time) | `rngLocked` + `lootbox-index-advance` first-principles re-proof | [if Phase 239 committed: "re-verified at HEAD 7ab515fe via Phase 239 re-proof"; if NOT committed: "Phase 239 pending — stated as audit assumption per CONTEXT.md Claude's Discretion; recorded in Scope-Guard Deferrals"] |

## Finding Candidates

Per D-14/D-15/D-17. Rows with Mutation-Path Coverage = `PARTIAL_COVERAGE` are `CANDIDATE_FINDING` per D-14 (a mutation path is not covered by any D-13 named gate). Plus informational `EXCEPTION` rows (22 KI-accepted). Severity INFO per D-17 unless unambiguously higher. NO F-30-NN IDs.

Format:
```
- **[CANDIDATE_FINDING | EXCEPTION]** INV-237-NNN — contracts/<path>:<line> — <fn> — <gate analysis + uncovered mutation path, if CANDIDATE> — severity: <INFO|LOW|MEDIUM|HIGH>
```

Expected EXCEPTION entries (22 — informational, KI-accepted): EXC-01 (2), EXC-02 (8), EXC-03 (4), EXC-04 (8).
Expected CANDIDATE_FINDING entries: 0 expected (if any surface, enumerate; otherwise write "No CANDIDATE_FINDING entries surfaced during FWD-03 gating verification — every 238-02 Forward Mutation Path is covered by one of the 4 D-13 named gates or is NO_GATE_NEEDED_ORTHOGONAL.").

## Scope-Guard Deferrals

Per D-18. Includes:
1. **Phase 239 assumption (if applicable):** if Phase 239 RNG-01 / RNG-03 has not committed at this task's run time, record: "The `rngLocked` + `lootbox-index-advance` gate correctness is stated as an audit assumption pending Phase 239 RNG-01/RNG-03 re-proof (per CONTEXT.md Claude's Discretion). Phase 242 cross-checks this assumption against Phase 239 output at consolidation time."
2. **Any gate-taxonomy outlier:** if any row's FWD-02 mutation path cannot be mapped to one of the 4 D-13 named gates, record the Row ID + uncovered mutation path + proposed D-13 extension (routed to Phase 242 FIND-01 per D-14).
3. **Any inventory gap:** if any 238-02 Forward Mutation Paths entry references a storage slot not in `audit/v30-CONSUMER-INVENTORY.md` Per-Consumer Call Graphs at HEAD, record the gap (routed to Phase 242 FIND-01 per D-18 — Phase 237 output READ-only).

If none of the three cases apply, write "None surfaced during FWD-03 gating verification."

## Attestation

- Audit baseline HEAD: `7ab515fe`
- `git diff --name-only 7ab515fe..HEAD -- contracts/ test/` returned empty at task start and end
- `git status --porcelain contracts/ test/` returned empty before and after this task
- Wave 1 input files present and committed before this task started: `audit/v30-CONSUMER-INVENTORY.md`, `audit/v30-238-01-BWD.md`, `audit/v30-238-02-FWD.md`
- Input Row ID integrity: this file contains a Gating Verification Table entry for every INV-237-NNN Row ID in the inventory (row count = 146)
- EXCEPTION row distribution routed through gating: EXC-01=2 (NO_GATE_NEEDED_ORTHOGONAL or semantic-path-gate), EXC-02=8 (semantic-path-gate + 14-day delay), EXC-03=4 (semantic-path-gate terminal-state), EXC-04=8 (lootbox-index-advance)
- Named Gate vocabulary: 5 locked values; Mutation-Path Coverage vocabulary: 3 locked values
- Gate Coverage Heatmap row totals sum to 146
- Phase 239 cross-cite handling: [record which branch of Claude's Discretion was taken — direct cross-cite if Phase 239 committed, or audit assumption if not]
- Contract source reads limited to `contracts/**/*.sol` at HEAD `7ab515fe`
- Prior Wave 1 outputs NOT modified by this task (D-16/D-18 enforcement): `audit/v30-CONSUMER-INVENTORY.md`, `audit/v30-238-01-BWD.md`, `audit/v30-238-02-FWD.md`
- Zero F-30-NN IDs (D-15 enforcement)
```

Every Gating Verification Table row MUST:
- Have Row ID matching `INV-237-[0-9]{3}` AND appearing in all 3 Wave 1 inputs (inventory + 238-01 + 238-02)
- Have Named Gate exactly one of: `rngLocked`, `lootbox-index-advance`, `phase-transition-gate`, `semantic-path-gate`, `NO_GATE_NEEDED_ORTHOGONAL`
- Have Mutation-Path Coverage exactly one of: `EVERY_PATH_BLOCKED`, `PARTIAL_COVERAGE`, `NO_GATE_NEEDED_ORTHOGONAL`
- Have Gate Site File:Line rooted in `contracts/` at HEAD (or `—` for `NO_GATE_NEEDED_ORTHOGONAL`)
- Never contain F-30-NN references (D-15)
- Never contain placeholder tokens `<path>`, `<line>`, `<fn`, `TBD-*`

READ-only per D-20. Only writes: `audit/v30-238-03-GATING.md`. `git status --porcelain contracts/ test/` MUST be empty after the write. Wave 1 outputs (inventory + 238-01 + 238-02) MUST remain unmodified.
  </action>
  <verify>
    <automated>test -f audit/v30-238-03-GATING.md && grep -q "^## Gate Coverage Heatmap" audit/v30-238-03-GATING.md && grep -q "^## Gating Verification Table" audit/v30-238-03-GATING.md && grep -q "^## Prior-Artifact Cross-Cites" audit/v30-238-03-GATING.md && grep -q "^## Finding Candidates" audit/v30-238-03-GATING.md && grep -q "^## Scope-Guard Deferrals" audit/v30-238-03-GATING.md && grep -q "^## Attestation" audit/v30-238-03-GATING.md && [ "$(grep -c '^| INV-237-' audit/v30-238-03-GATING.md)" -ge 146 ] && [ "$(grep -Eo 'INV-237-[0-9]{3}' audit/v30-238-03-GATING.md | sort -u | wc -l)" = "146" ] && grep -q "rngLocked" audit/v30-238-03-GATING.md && grep -q "lootbox-index-advance" audit/v30-238-03-GATING.md && grep -q "phase-transition-gate" audit/v30-238-03-GATING.md && grep -q "semantic-path-gate" audit/v30-238-03-GATING.md && grep -q "NO_GATE_NEEDED_ORTHOGONAL" audit/v30-238-03-GATING.md && grep -q "EVERY_PATH_BLOCKED" audit/v30-238-03-GATING.md && ! grep -qE "F-30-[0-9]+" audit/v30-238-03-GATING.md && ! grep -q "<line>" audit/v30-238-03-GATING.md && ! grep -q "<path>" audit/v30-238-03-GATING.md && ! grep -q "TBD-" audit/v30-238-03-GATING.md && grep -q "7ab515fe" audit/v30-238-03-GATING.md && grep -q "re-verified at HEAD 7ab515fe" audit/v30-238-03-GATING.md && test -z "$(git status --porcelain contracts/ test/)" && test -z "$(git status --porcelain audit/v30-CONSUMER-INVENTORY.md audit/v30-238-01-BWD.md audit/v30-238-02-FWD.md 2>/dev/null)"</automated>
  </verify>
  <acceptance_criteria>
    - File `audit/v30-238-03-GATING.md` exists
    - File contains 6 literal section headers in this order: `## Gate Coverage Heatmap`, `## Gating Verification Table`, `## Prior-Artifact Cross-Cites`, `## Finding Candidates`, `## Scope-Guard Deferrals`, `## Attestation`
    - Gating Verification Table has ≥ 146 INV-237-NNN Row ID lines
    - Distinct INV-237-NNN IDs = exactly 146
    - All 5 Named Gate values appear: `rngLocked`, `lootbox-index-advance`, `phase-transition-gate`, `semantic-path-gate`, `NO_GATE_NEEDED_ORTHOGONAL`
    - Mutation-Path Coverage value `EVERY_PATH_BLOCKED` appears (dominant verdict)
    - File does NOT contain `F-30-NN` references (D-15)
    - File does NOT contain placeholder tokens `<line>`, `<path>`, `<fn`, `TBD-`
    - File contains literal `7ab515fe` in Audit baseline line
    - File contains literal `re-verified at HEAD 7ab515fe` (D-09/D-10)
    - `audit/v30-CONSUMER-INVENTORY.md`, `audit/v30-238-01-BWD.md`, `audit/v30-238-02-FWD.md` NOT edited
    - `git status --porcelain contracts/ test/` empty
    - `git diff --name-only 7ab515fe..HEAD -- contracts/ test/` empty
  </acceptance_criteria>
  <deliverable>`audit/v30-238-03-GATING.md` — FWD-03 Gating Verification deliverable. 146-row Gating Verification Table with 6 columns per D-06, 5 Named Gate values per D-13 + NO_GATE_NEEDED_ORTHOGONAL, Gate Coverage Heatmap, Prior-Artifact Cross-Cites, Finding Candidates, Scope-Guard Deferrals, Attestation. Zero F-30-NN. Zero contracts/ test/ writes. Zero edits to Wave 1 outputs.</deliverable>
  <done>File exists; 6 required sections; 146 distinct INV-237-NNN Row IDs; all 5 Named Gate values present; EVERY_PATH_BLOCKED vocabulary present; no F-30-NN; no placeholder tokens; HEAD anchor echoed; cross-cites carry re-verified-at-HEAD note; Wave 1 outputs unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Commit audit/v30-238-03-GATING.md</name>
  <files>audit/v30-238-03-GATING.md</files>
  <read_first>
    - audit/v30-238-03-GATING.md (must exist from Task 1)
  </read_first>
  <action>
Stage and commit `audit/v30-238-03-GATING.md` only. Use `git add` with explicit path — do NOT `git add .`.

```
git add audit/v30-238-03-GATING.md
git commit -m "docs(238-03): FWD-03 per-consumer gating verification at HEAD 7ab515fe

146-row Gating Verification Table (one per INV-237-NNN from audit/v30-
CONSUMER-INVENTORY.md) with 6 columns per D-06: Row ID / Forward Mutation
Paths (from 238-02) / Named Gate / Gate Site File:Line / Mutation-Path
Coverage / Effectiveness Proof.

Named Gate vocabulary is closed per D-13 4-gate taxonomy: rngLocked /
lootbox-index-advance / phase-transition-gate / semantic-path-gate +
NO_GATE_NEEDED_ORTHOGONAL for orthogonal rows. Per D-14: any row outside
this taxonomy is CANDIDATE_FINDING routed to Phase 242 FIND-01.

Mutation-Path Coverage: EVERY_PATH_BLOCKED / PARTIAL_COVERAGE /
NO_GATE_NEEDED_ORTHOGONAL. PARTIAL_COVERAGE rows produce Finding
Candidate blocks per D-14.

EXCEPTION row routing: EXC-01=2 (NO_GATE_NEEDED_ORTHOGONAL or semantic-
path-gate — non-VRF seed), EXC-02=8 (semantic-path-gate: 14-day delay at
AdvanceModule:109 + gate at :1252), EXC-03=4 (semantic-path-gate: terminal-
state gameover at :292 + :1082 + :1222-1246), EXC-04=8 (lootbox-index-
advance: EntropyLib seeded per-player-per-day via keccak+VRF).

Gate Coverage Heatmap (Path Family × Named Gate) surfaces distribution
anomalies at a glance — row totals sum to 146.

Prior-milestone artifacts CROSS-CITED with re-verified-at-HEAD-7ab515fe
notes (per D-09/D-10): 235-05-TRNX-01.md (rngLocked 4-path re-proof),
232.1-03-PFTB-AUDIT.md (semantic-path-gate archetype), Phase 235 D-13
Path 4 (phase-transition-gate).

Phase 239 dependency (per Claude's Discretion): rngLocked + lootbox-
index-advance gate correctness cross-cites Phase 239 RNG-01/RNG-03 if
committed; otherwise stated as audit assumption pending 239 re-proof with
Scope-Guard Deferral recorded for Phase 242 cross-check.

NO F-30-NN IDs (D-15 — Phase 242 owns). READ-only per D-20 — zero
contracts/ test/ writes; Wave 1 outputs (v30-CONSUMER-INVENTORY.md,
v30-238-01-BWD.md, v30-238-02-FWD.md) unmodified (D-16/D-18).

HEAD anchor 7ab515fe (D-19).

Wave 2 depends_on [238-01, 238-02]. Task 3 of this plan assembles the
final consolidated audit/v30-FREEZE-PROOF.md per D-16."
```

Do NOT push.
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -qE "238-03.*(FWD-03|gating).*7ab515fe" && test -z "$(git status --porcelain contracts/ test/)" && test -z "$(git status --porcelain audit/v30-CONSUMER-INVENTORY.md audit/v30-238-01-BWD.md audit/v30-238-02-FWD.md 2>/dev/null)"</automated>
  </verify>
  <acceptance_criteria>
    - One new commit with subject containing `238-03` + (`FWD-03` OR `gating`) + `7ab515fe`
    - Commit touches only `audit/v30-238-03-GATING.md`
    - `git status --porcelain contracts/ test/` empty
    - `git status --porcelain audit/v30-CONSUMER-INVENTORY.md audit/v30-238-01-BWD.md audit/v30-238-02-FWD.md` empty
    - Not pushed
  </acceptance_criteria>
  <deliverable>`audit/v30-238-03-GATING.md` committed; Wave 1 outputs untouched; ready for Task 3 final consolidation.</deliverable>
  <done>Commit exists; contracts/ test/ unmodified; Wave 1 outputs unmodified; FWD-03 deliverable committed.</done>
</task>

<task type="auto">
  <name>Task 3: Assemble final consolidated audit/v30-FREEZE-PROOF.md from 238-01 + 238-02 + 238-03 outputs (per D-16)</name>
  <files>audit/v30-FREEZE-PROOF.md</files>
  <read_first>
    - audit/v30-238-01-BWD.md (Backward Freeze Table + Backward Adversarial Closure Table + shared-prefix chains + Gameover-Flow subset + Prior-Artifact Cross-Cites + Finding Candidates — MUST be committed from Plan 238-01 Task 2)
    - audit/v30-238-02-FWD.md (Forward Enumeration Table + Forward Mutation Paths + shared-prefix chains + Gameover-Flow subset + Prior-Artifact Cross-Cites + Finding Candidates — MUST be committed from Plan 238-02 Task 2)
    - audit/v30-238-03-GATING.md (Gating Verification Table + Gate Coverage Heatmap + Prior-Artifact Cross-Cites + Finding Candidates + Scope-Guard Deferrals — MUST be committed from Task 2 of THIS plan)
    - audit/v30-CONSUMER-INVENTORY.md (146-row Universe List + Consumer Index — MUST be committed per D-18 Phase 237 lock)
    - .planning/REQUIREMENTS.md (26 v30.0 requirement IDs for Consumer Index scope — INV/BWD/FWD/RNG/GO/EXC/REG/FIND series)
    - .planning/phases/238-backward-forward-freeze-proofs/238-CONTEXT.md (D-16 consolidated-file shape — Phase 237 D-08 precedent; D-17 merged Finding Candidates appendix; D-18 READ-only-after-commit for 238-01/02/03 files; D-20 READ-only)
    - .planning/phases/237-vrf-consumer-inventory-call-graph/237-03-PLAN.md (Phase 237 Plan 03 Task 3 precedent for consolidated-file assembly — 13-section structure, Consumer Index mapping, Python merge script reference)
  </read_first>
  <action>
Assemble the FINAL consolidated Phase 238 deliverable per D-16 (Phase 237 D-08 precedent).

**Verify Wave 2 prerequisites — all 3 Phase 238 plan outputs MUST be committed:**
```
test -f audit/v30-238-01-BWD.md && git log --oneline -1 -- audit/v30-238-01-BWD.md
test -f audit/v30-238-02-FWD.md && git log --oneline -1 -- audit/v30-238-02-FWD.md
test -f audit/v30-238-03-GATING.md && git log --oneline -1 -- audit/v30-238-03-GATING.md
test -f audit/v30-CONSUMER-INVENTORY.md
git diff --name-only 7ab515fe..HEAD -- contracts/ test/   # MUST be empty
git status --porcelain contracts/ test/                   # MUST be empty
```
If ANY of the three plan outputs is missing OR not committed, STOP — prerequisites not met.

**Step 1 — Read all four source files + REQUIREMENTS.md.**

**Step 2 — Merge row-by-row into the Consolidated Freeze-Proof Table.** For each INV-237-NNN Row ID:
- Copy Consumer Function + Consumption File:Line + Path Family + KI Cross-Ref from `audit/v30-CONSUMER-INVENTORY.md` Universe List.
- Copy Backward-Trace Verdict + Write-Site Classification from `audit/v30-238-01-BWD.md` Backward Freeze Table.
- Copy BWD-03 Verdict from `audit/v30-238-01-BWD.md` Backward Adversarial Closure Table.
- Copy FWD-Verdict + Actor-Class Closure summary from `audit/v30-238-02-FWD.md` Forward Enumeration Table.
- Copy Named Gate + Mutation-Path Coverage from `audit/v30-238-03-GATING.md` Gating Verification Table.
- Derive overall Effectiveness Verdict: `SAFE` if all of {BWD-Verdict, BWD-03 Verdict, FWD-Verdict} = SAFE AND Mutation-Path Coverage ∈ {EVERY_PATH_BLOCKED, NO_GATE_NEEDED_ORTHOGONAL}; `EXCEPTION` if any is EXCEPTION (with KI cross-ref populated); `CANDIDATE_FINDING` if Mutation-Path Coverage = PARTIAL_COVERAGE or any BWD-03 actor-cell is CANDIDATE_FINDING.

**Step 3 — Build the Consumer Index (per D-16 + Phase 237 D-10 pattern).** Map every v30.0 requirement ID to the Row-ID subset:

| Requirement | Phase | Scope | Row IDs |
|---|---|---|---|
| INV-01 / INV-02 / INV-03 | 237 | inventory + classification + call graphs | `ALL` (cites `audit/v30-CONSUMER-INVENTORY.md`) |
| BWD-01 / BWD-02 / BWD-03 | 238 | per-consumer backward trace + storage-read enum + 4-actor closure | `ALL` 146 rows (this phase) |
| FWD-01 / FWD-02 / FWD-03 | 238 | per-consumer forward enum + 4-actor closure + gating verification | `ALL` 146 rows (this phase) |
| RNG-01 | 239 | rngLocked state machine | enumerate Row IDs whose Named Gate in 238-03 = `rngLocked` |
| RNG-02 | 239 | Permissionless sweep | `ALL` 146 rows (each permissionless function classified against each Row's state set) |
| RNG-03 | 239 | Index-advance + phaseTransitionActive asymmetries | Row IDs whose Named Gate = `lootbox-index-advance` OR `phase-transition-gate` |
| GO-01 / GO-02 / GO-03 / GO-04 | 240 | Gameover-flow consumer inventory + determinism + state-freeze + trigger-timing | 19 gameover-flow rows per 237 Consumer Index (7 gameover-entropy + 2 mid-cycle-substitution gameover-reached + 8 exception-prevrandao-fallback + 4 F-29-04 overlap minus duplicates) |
| GO-05 | 240 | F-29-04 scope containment | 4 EXC-03 `exception-mid-cycle-substitution` rows |
| EXC-01 | 241 | Affiliate winner-roll exhaustiveness | 2 rows (INV-237-005, INV-237-006) — `[KI: "Non-VRF entropy for affiliate winner roll"]` |
| EXC-02 | 241 | Prevrandao-fallback gating | 8 rows — `[KI: "Gameover prevrandao fallback"]` |
| EXC-03 | 241 | F-29-04 scope re-verification | 4 rows — `[KI: "Gameover RNG substitution for mid-cycle write-buffer tickets"]` |
| EXC-04 | 241 | EntropyLib keccak-seed re-verification | 8 rows — `[KI: "EntropyLib XOR-shift PRNG for lootbox outcome rolls"]` |
| REG-01 / REG-02 | 242 | v29.0 F-29-03/F-29-04 + v25.0/v3.7/v3.8 regression | enumerate Row IDs flagged in 237-01 Reconciliation (confirmed-fresh-matches-prior for REG-02; F-29-03/04 overlap for REG-01) |
| FIND-01 / FIND-02 / FIND-03 | 242 | consolidated findings + regression appendix + KI promotions | enumerate Row IDs with Finding Candidate entries in 238-01 / 238-02 / 238-03 + cross-reference Phase 237's 17-item merged FC pool |

**Step 4 — Merge Finding Candidates (per D-17).** Deduplicate identical items; preserve per-item source attribution (`— surfaced in 238-01 BWD-03 adversarial closure` / `— surfaced in 238-02 FWD-02 adversarial closure` / `— surfaced in 238-03 FWD-03 gating`). Expected merged count: 22 EXCEPTION entries (informational) + 0 CANDIDATE_FINDING entries (expected) = 22 total.

**Step 5 — Merge Scope-Guard Deferrals** from all 3 plan outputs (238-01/02/03) + Phase 239 assumption from 238-03 if applicable. Per-item source attribution.

**Step 6 — Write `audit/v30-FREEZE-PROOF.md` with EXACTLY these sections in this order:**

```markdown
# v30.0 Backward & Forward Freeze Proof — Phase 238 Final Consolidated Deliverable

**Audit baseline:** HEAD `7ab515fe` (contract tree identical to v29.0 `1646d5af`; post-v29 commits are docs-only per D-19).
**Requirements satisfied:** BWD-01, BWD-02, BWD-03, FWD-01, FWD-02, FWD-03 (Phase 238 scope = 6/6).
**Status post-commit:** READ-only per D-16/D-18. Downstream Phases 239-242 cite Row IDs; they do NOT edit this file. Inventory gaps discovered downstream are recorded as scope-guard deferrals in each phase's own SUMMARY.
**Source sub-deliverables:** assembled from `audit/v30-238-01-BWD.md` + `audit/v30-238-02-FWD.md` + `audit/v30-238-03-GATING.md` (each committed separately per Plan 238-01 / 238-02 / 238-03 Task 2). All 3 sources READ-only post-commit per D-16 — this file is a new assembly, not an edit in place.

## Table of Contents

1. [Consolidated Freeze-Proof Table](#consolidated-freeze-proof-table) — 146 rows × 9 columns (BWD + FWD + gating merged)
2. [Gate Coverage Heatmap](#gate-coverage-heatmap) — Path Family × Named Gate
3. [Shared-Prefix Chain Summary](#shared-prefix-chain-summary) — 6 chains covering 130/146 rows
4. [Gameover-Flow Freeze-Proof Subset](#gameover-flow-freeze-proof-subset) — 19 rows for Phase 240 GO-01..05 hand-off
5. [KI-Exception Freeze-Proof Subset](#ki-exception-freeze-proof-subset) — 22 rows for Phase 241 EXC-01..04 hand-off
6. [Prior-Artifact Cross-Cites](#prior-artifact-cross-cites) — merged from 238-01/02/03
7. [Finding Candidates](#finding-candidates) — merged appendix for Phase 242 FIND-01 intake
8. [Scope-Guard Deferrals](#scope-guard-deferrals) — merged from 238-01/02/03 + Phase 239 assumption if applicable
9. [Consumer Index](#consumer-index) — v30.0 requirement → Row-ID subset mapping
10. [Attestation](#attestation)

## Consolidated Freeze-Proof Table

**One row per INV-237-NNN (146 rows total).** Columns:

| Row ID | Consumer | Path Family | BWD-Trace Verdict | BWD-03 Verdict | FWD-Verdict | Named Gate | Mutation-Path Coverage | Effectiveness Verdict | KI Cross-Ref |
|---|---|---|---|---|---|---|---|---|---|
| INV-237-001 | processCoinflipPayouts | daily | SAFE | SAFE | SAFE | rngLocked | EVERY_PATH_BLOCKED | SAFE | N/A |
| ... | ... | ... | ... | ... | ... | ... | ... | ... | ... |
| INV-237-146 | ... | ... | ... | ... | ... | ... | ... | ... | ... |

[Every Row ID from `audit/v30-CONSUMER-INVENTORY.md` Universe List gets exactly one row. Source-column fidelity: BWD-Trace Verdict from 238-01; BWD-03 Verdict from 238-01; FWD-Verdict from 238-02; Named Gate + Mutation-Path Coverage from 238-03; Effectiveness Verdict derived per Step 2 rule. No placeholder tokens. No F-30-NN.]

## Gate Coverage Heatmap

[Copy verbatim from audit/v30-238-03-GATING.md — Path Family × Named Gate matrix; row totals sum to 146.]

## Shared-Prefix Chain Summary

[Per D-04/D-05. Summary of the 6 shared-prefix chains (PREFIX-DAILY / PREFIX-MIDDAY / PREFIX-GAMEOVER / PREFIX-PREVRANDAO / PREFIX-AFFILIATE / PREFIX-GAP) covering 130 of 146 rows per 237-03. Body citation only — full chain bodies remain in 238-01 + 238-02 source files.]

## Gameover-Flow Freeze-Proof Subset

**Per D-12 — Phase 240 GO-01..05 hand-off.** 19 gameover-flow rows × the full BWD + FWD + gating verdict set:

[Merge of 238-01 Gameover-Flow subset + 238-02 Gameover-Flow subset + 238-03 rows filtered by path family. Row count = 19. SAFE subset = 7 (gameover-entropy); EXCEPTION subset = 12 (8 prevrandao + 4 F-29-04).]

## KI-Exception Freeze-Proof Subset

**Per D-11 — Phase 241 EXC-01..04 hand-off.** 22 KI-accepted EXCEPTION rows × the full BWD + FWD + gating verdict set:

[Filter Consolidated Freeze-Proof Table by Effectiveness Verdict = EXCEPTION. Row count = 22. Distribution: EXC-01=2, EXC-02=8, EXC-03=4, EXC-04=8. Each row cites exact quoted KI header.]

## Prior-Artifact Cross-Cites

[Merge Prior-Artifact Cross-Cites sections from 238-01, 238-02, 238-03. Deduplicate; preserve structural-equivalence statements; every cite carries `re-verified at HEAD 7ab515fe` note per D-09/D-10.]

## Finding Candidates

[Per D-17. Merged appendix — deduplicated, with per-item source attribution (— surfaced in 238-01 / 238-02 / 238-03). NO F-30-NN IDs. Expected: 22 EXCEPTION entries (informational) + 0 CANDIDATE_FINDING entries. If any CANDIDATE_FINDING surfaces, enumerate with file:line + rationale + severity. Routes to Phase 242 FIND-01.]

## Scope-Guard Deferrals

[Merge Scope-Guard Deferrals from 238-01/02/03 + Phase 239 assumption from 238-03 if applicable. Per-item source attribution. If all source deferrals are empty, write "None surfaced across all three Phase 238 sub-plans."]

## Consumer Index

**Per D-16 + Phase 237 D-10 pattern — maps every v30.0 requirement ID to the Row-ID subset that satisfies it.**

| Requirement | Phase | Scope | Row IDs |
|---|---|---|---|
| INV-01 | 237 | Universe list | `ALL` (see `audit/v30-CONSUMER-INVENTORY.md`) |
| INV-02 | 237 | Classification | `ALL` |
| INV-03 | 237 | Call graphs | `ALL` |
| BWD-01 | 238 | Per-consumer backward trace | `ALL` 146 (this file §Consolidated Freeze-Proof Table, BWD-Trace Verdict column) |
| BWD-02 | 238 | Per-consumer storage-read enumeration | `ALL` 146 (this file §Consolidated Freeze-Proof Table + 238-01 Backward Freeze Table Storage Reads column) |
| BWD-03 | 238 | Per-consumer adversarial closure (backward) | `ALL` 146 (this file §Consolidated Freeze-Proof Table, BWD-03 Verdict column) |
| FWD-01 | 238 | Per-consumer consumption-state read universe | `ALL` 146 (this file §Consolidated Freeze-Proof Table + 238-02 Forward Enumeration Table) |
| FWD-02 | 238 | Per-consumer adversarial closure (forward) | `ALL` 146 (this file §Consolidated Freeze-Proof Table, FWD-Verdict column) |
| FWD-03 | 238 | Per-consumer gating verification | `ALL` 146 (this file §Consolidated Freeze-Proof Table, Named Gate + Mutation-Path Coverage columns) |
| RNG-01 | 239 | rngLocked state machine | [enumerate Row IDs whose Named Gate = `rngLocked`] |
| RNG-02 | 239 | Permissionless sweep | `ALL` (each permissionless function classified against each Row's state set) |
| RNG-03 | 239 | Index-advance + phaseTransitionActive asymmetries | [enumerate Row IDs whose Named Gate = `lootbox-index-advance` OR `phase-transition-gate`] |
| GO-01 / GO-02 / GO-03 / GO-04 | 240 | Gameover consumer inventory + determinism + state-freeze + trigger-timing | [enumerate 19 gameover-flow rows — see this file §Gameover-Flow Freeze-Proof Subset] |
| GO-05 | 240 | F-29-04 scope containment | [enumerate 4 EXC-03 rows — subcategory `exception-mid-cycle-substitution`] |
| EXC-01 | 241 | Affiliate winner-roll exhaustiveness | INV-237-005, INV-237-006 |
| EXC-02 | 241 | Prevrandao-fallback gating | [enumerate 8 EXC-02 rows — subcategory `exception-prevrandao-fallback`] |
| EXC-03 | 241 | F-29-04 scope re-verification | [enumerate 4 EXC-03 rows] |
| EXC-04 | 241 | EntropyLib keccak-seed re-verification | [enumerate 8 EXC-04 rows] |
| REG-01 | 242 | v29.0 F-29-03 / F-29-04 regression | [enumerate Row IDs flagged in 237-01 Reconciliation vs F-29-03 / F-29-04] |
| REG-02 | 242 | v25.0 / v3.7 / v3.8 RNG regression | [enumerate Row IDs with Reconciliation verdict `confirmed-fresh-matches-prior`] |
| FIND-01 | 242 | Consolidated findings | [enumerate every Row ID in this file's §Finding Candidates + merge with Phase 237 17-item FC pool] |
| FIND-02 | 242 | Regression appendix | Same as REG-01 + REG-02 |
| FIND-03 | 242 | KI promotions | [enumerate Row IDs that might qualify for new KI entries — 0 expected per D-15 + Phase 242 scope] |

Every Row IDs column MUST be either `ALL` OR a concrete list of `INV-237-NNN` IDs. No `TBD` and no placeholder tokens.

## Attestation

- Audit baseline HEAD: `7ab515fe`
- `git diff --name-only 7ab515fe..HEAD -- contracts/ test/` returned empty at task start and end
- `git status --porcelain contracts/ test/` returned empty before and after this task
- Source sub-deliverables (all committed before this task started):
  - `audit/v30-CONSUMER-INVENTORY.md` — Phase 237 final consolidated inventory (146 rows)
  - `audit/v30-238-01-BWD.md` — Plan 238-01 BWD-01/02/03 deliverable
  - `audit/v30-238-02-FWD.md` — Plan 238-02 FWD-01/02 deliverable
  - `audit/v30-238-03-GATING.md` — Plan 238-03 FWD-03 deliverable (this plan Task 1 output)
- Assembly integrity: Row ID set in this file is identical to each of the 4 source files (verified by `diff` of sorted-unique INV-237-NNN extractions; all 4 return 146 distinct IDs with zero differences)
- Assembly integrity: no placeholder tokens (`<line>`, `<path>`, `<fn`, `TBD-*`) remain
- EXCEPTION row distribution: EXC-01=2, EXC-02=8, EXC-03=4, EXC-04=8, total=22 (matches 237-02 Plan SUMMARY + 238-01 + 238-02 + 238-03)
- Gameover-Flow subset count: 19 (matches 237 Consumer Index GO-01..04)
- Shared-prefix chains: 6 chains covering 130/146 rows
- Phase 239 handling: [direct cross-cite if Phase 239 RNG-01/RNG-03 committed, or audit assumption with Scope-Guard Deferral if not]
- READ-only policy (D-16/D-18): source sub-deliverables were NOT modified by this task (verified by `git status --porcelain` empty for their paths)
- READ-only policy (D-20): zero `contracts/` or `test/` writes
- Zero F-30-NN IDs (D-15 enforcement)
```

The consolidated file is large (~1500-2500 lines depending on row-citation verbosity). Use verbatim copies from source files where possible (Gate Coverage Heatmap from 238-03; Gameover-Flow subset from 238-01+238-02 unions; Finding Candidates from 3-way merge). An implementation via a Python merge script (~`/tmp/build_freeze_proof.py`-style, per Phase 237 Plan 03 Task 3 precedent) is acceptable and recommended for accuracy; alternative is a bash awk/join pipeline. Implementation is planner's discretion per CONTEXT.md Claude's Discretion; the OUTPUT CONTRACT is the row-for-row merge + Consumer Index + merged Finding Candidates + Attestation.

After writing, commit:
```
git add audit/v30-FREEZE-PROOF.md
git commit -m "docs(238-03): final consolidated audit/v30-FREEZE-PROOF.md per D-16

Assembles the three Phase 238 sub-deliverables into the single
authoritative freeze-proof file for v30.0 downstream Phases 239-242:
  - audit/v30-238-01-BWD.md (BWD-01/02/03 per-consumer backward freeze)
  - audit/v30-238-02-FWD.md (FWD-01/02 per-consumer forward freeze)
  - audit/v30-238-03-GATING.md (FWD-03 per-consumer gating verification)

Consolidated Freeze-Proof Table: 146 rows × 10 columns (Row ID / Consumer
/ Path Family / BWD-Trace Verdict / BWD-03 Verdict / FWD-Verdict / Named
Gate / Mutation-Path Coverage / Effectiveness Verdict / KI Cross-Ref).

Consumer Index (per D-16 + Phase 237 D-10 pattern) maps every v30.0
requirement (INV / BWD / FWD / RNG / GO / EXC / REG / FIND — 26 total) to
its INV-237-NNN Row-ID subset. Phases 239-242 can plan without additional
discovery.

Gameover-Flow Freeze-Proof Subset (19 rows) prepped for Phase 240 GO-01..05.
KI-Exception Freeze-Proof Subset (22 rows: EXC-01=2/EXC-02=8/EXC-03=4/
EXC-04=8) prepped for Phase 241 EXC-01..04.

Finding Candidates merged across 238-01/02/03 (dedup + per-item source
attribution) for Phase 242 FIND-01 intake. NO F-30-NN IDs (D-15 — Phase
242 owns).

Source sub-deliverables NOT modified (D-16/D-18 READ-only-after-commit).

READ-only per D-20. HEAD anchor 7ab515fe (D-19).

Phase 238 (BWD-01/02/03 + FWD-01/02/03) complete."
```

Do NOT push.
  </action>
  <verify>
    <automated>test -f audit/v30-FREEZE-PROOF.md && grep -q "^## Consolidated Freeze-Proof Table" audit/v30-FREEZE-PROOF.md && grep -q "^## Gate Coverage Heatmap" audit/v30-FREEZE-PROOF.md && grep -q "^## Shared-Prefix Chain Summary" audit/v30-FREEZE-PROOF.md && grep -q "^## Gameover-Flow Freeze-Proof Subset" audit/v30-FREEZE-PROOF.md && grep -q "^## KI-Exception Freeze-Proof Subset" audit/v30-FREEZE-PROOF.md && grep -q "^## Prior-Artifact Cross-Cites" audit/v30-FREEZE-PROOF.md && grep -q "^## Finding Candidates" audit/v30-FREEZE-PROOF.md && grep -q "^## Scope-Guard Deferrals" audit/v30-FREEZE-PROOF.md && grep -q "^## Consumer Index" audit/v30-FREEZE-PROOF.md && grep -q "^## Attestation" audit/v30-FREEZE-PROOF.md && [ "$(grep -c '^| INV-237-' audit/v30-FREEZE-PROOF.md)" -ge 146 ] && [ "$(grep -Eo 'INV-237-[0-9]{3}' audit/v30-FREEZE-PROOF.md | sort -u | wc -l)" = "146" ] && grep -q "BWD-01" audit/v30-FREEZE-PROOF.md && grep -q "BWD-02" audit/v30-FREEZE-PROOF.md && grep -q "BWD-03" audit/v30-FREEZE-PROOF.md && grep -q "FWD-01" audit/v30-FREEZE-PROOF.md && grep -q "FWD-02" audit/v30-FREEZE-PROOF.md && grep -q "FWD-03" audit/v30-FREEZE-PROOF.md && grep -q "RNG-01" audit/v30-FREEZE-PROOF.md && grep -q "RNG-02" audit/v30-FREEZE-PROOF.md && grep -q "RNG-03" audit/v30-FREEZE-PROOF.md && grep -q "GO-01" audit/v30-FREEZE-PROOF.md && grep -q "GO-05" audit/v30-FREEZE-PROOF.md && grep -q "EXC-01" audit/v30-FREEZE-PROOF.md && grep -q "EXC-04" audit/v30-FREEZE-PROOF.md && grep -q "REG-01" audit/v30-FREEZE-PROOF.md && grep -q "REG-02" audit/v30-FREEZE-PROOF.md && grep -q "FIND-01" audit/v30-FREEZE-PROOF.md && grep -q "FIND-02" audit/v30-FREEZE-PROOF.md && grep -q "FIND-03" audit/v30-FREEZE-PROOF.md && grep -q "rngLocked" audit/v30-FREEZE-PROOF.md && grep -q "lootbox-index-advance" audit/v30-FREEZE-PROOF.md && grep -q "phase-transition-gate" audit/v30-FREEZE-PROOF.md && grep -q "semantic-path-gate" audit/v30-FREEZE-PROOF.md && ! grep -qE "F-30-[0-9]+" audit/v30-FREEZE-PROOF.md && ! grep -q "<line>" audit/v30-FREEZE-PROOF.md && ! grep -q "<path>" audit/v30-FREEZE-PROOF.md && ! grep -q "TBD-" audit/v30-FREEZE-PROOF.md && grep -q "7ab515fe" audit/v30-FREEZE-PROOF.md && test -z "$(git status --porcelain contracts/ test/)" && test -z "$(git status --porcelain audit/v30-CONSUMER-INVENTORY.md audit/v30-238-01-BWD.md audit/v30-238-02-FWD.md audit/v30-238-03-GATING.md 2>/dev/null)" && git log -1 --oneline | grep -qE "238-03.*consolidated.*(v30-FREEZE-PROOF|D-16)"</automated>
  </verify>
  <acceptance_criteria>
    - File `audit/v30-FREEZE-PROOF.md` exists
    - File contains 10 literal section headers in this order: `## Consolidated Freeze-Proof Table`, `## Gate Coverage Heatmap`, `## Shared-Prefix Chain Summary`, `## Gameover-Flow Freeze-Proof Subset`, `## KI-Exception Freeze-Proof Subset`, `## Prior-Artifact Cross-Cites`, `## Finding Candidates`, `## Scope-Guard Deferrals`, `## Consumer Index`, `## Attestation`
    - Consolidated Freeze-Proof Table has ≥ 146 INV-237-NNN Row ID lines
    - Distinct INV-237-NNN IDs = exactly 146
    - Consumer Index references all 26 v30.0 requirements (INV-01..03, BWD-01..03, FWD-01..03, RNG-01..03, GO-01..05, EXC-01..04, REG-01..02, FIND-01..03) — verified by 18 grep assertions (one per requirement ID series endpoint)
    - All 4 D-13 named gates appear: `rngLocked`, `lootbox-index-advance`, `phase-transition-gate`, `semantic-path-gate`
    - No `F-30-NN` references (D-15)
    - No placeholder tokens `<line>`, `<path>`, `<fn`, `TBD-`
    - File contains literal `7ab515fe` in the Audit baseline line
    - Wave 1/Wave 2 source files NOT edited: `audit/v30-CONSUMER-INVENTORY.md`, `audit/v30-238-01-BWD.md`, `audit/v30-238-02-FWD.md`, `audit/v30-238-03-GATING.md`
    - `git status --porcelain contracts/ test/` empty
    - `git diff --name-only 7ab515fe..HEAD -- contracts/ test/` empty
    - One new commit with subject containing `238-03` + `consolidated` + (`v30-FREEZE-PROOF` OR `D-16`)
    - Commit touches only `audit/v30-FREEZE-PROOF.md`
    - Not pushed
  </acceptance_criteria>
  <deliverable>
`audit/v30-FREEZE-PROOF.md` — FINAL consolidated Phase 238 deliverable per D-16. Single authoritative file for Phases 239-242 downstream cite. Consolidated Freeze-Proof Table (146 rows, 10 columns) merging BWD + FWD + gating verdicts; Gate Coverage Heatmap; Gameover-Flow + KI-Exception subsets for Phase 240/241 hand-offs; merged Finding Candidates + Scope-Guard Deferrals for Phase 242 FIND-01 intake; Consumer Index mapping all 26 v30.0 requirements to Row-ID subsets. Zero F-30-NN IDs. Zero contracts/ test/ writes. Zero edits to Wave 1 outputs or Phase 237 output. Committed locally; not pushed.
  </deliverable>
  <done>Consolidated freeze-proof file exists; 10 required sections present; Row ID set matches sub-deliverables (146 distinct); Consumer Index covers all 26 v30.0 requirements; all 4 D-13 named gates referenced; sub-deliverables intact (D-16/D-18); committed. Phase 238 complete.</done>
</task>

</tasks>

<verification>
- `audit/v30-238-03-GATING.md` exists with all 6 required sections (Gate Coverage Heatmap, Gating Verification Table, Prior-Artifact Cross-Cites, Finding Candidates, Scope-Guard Deferrals, Attestation)
- Gating Verification Table has 146 rows × 6 columns per D-06
- Named Gate column uses only the 5 locked values (`rngLocked` / `lootbox-index-advance` / `phase-transition-gate` / `semantic-path-gate` / `NO_GATE_NEEDED_ORTHOGONAL`)
- Mutation-Path Coverage column uses only the 3 locked values (`EVERY_PATH_BLOCKED` / `PARTIAL_COVERAGE` / `NO_GATE_NEEDED_ORTHOGONAL`)
- Gate Coverage Heatmap row totals sum to 146
- 22 KI-EXCEPTION rows routed correctly (EXC-01=2 NO_GATE_NEEDED_ORTHOGONAL or semantic-path-gate; EXC-02=8 semantic-path-gate; EXC-03=4 semantic-path-gate; EXC-04=8 lootbox-index-advance)
- Phase 239 dependency handled per Claude's Discretion (direct cross-cite if committed; audit assumption with Scope-Guard Deferral if not)
- `audit/v30-FREEZE-PROOF.md` exists with all 10 required sections
- Consolidated Freeze-Proof Table: 146 rows × 10 columns merged from 238-01/02/03 + inventory
- Consumer Index maps all 26 v30.0 requirements (INV/BWD/FWD/RNG/GO/EXC/REG/FIND)
- Gameover-Flow Freeze-Proof Subset: 19 rows
- KI-Exception Freeze-Proof Subset: 22 rows (EXC distribution matches)
- Merged Finding Candidates appendix: deduplicated with per-item source attribution
- Zero F-30-NN references in either deliverable (D-15)
- Zero contracts/ test/ writes (D-20)
- Zero edits to `audit/v30-CONSUMER-INVENTORY.md`, `audit/v30-238-01-BWD.md`, `audit/v30-238-02-FWD.md` (D-16/D-18)
- HEAD anchor 7ab515fe attested in both files
- Both deliverables committed; not pushed
</verification>

<success_criteria>
FWD-03 satisfied: every one of 146 VRF-consuming call sites has its forward mutation paths (from 238-02) verified against a Named Gate (one of 4 D-13 values or NO_GATE_NEEDED_ORTHOGONAL) with effectiveness proof. Gate Coverage Heatmap surfaces distribution at a glance.

D-16 satisfied: `audit/v30-FREEZE-PROOF.md` is the single authoritative consolidated deliverable. 146-row merged table + Consumer Index + Gameover/KI subsets + merged Finding Candidates. Phases 239-242 planners inherit scope without additional discovery. The three plan files remain intact and committed separately — the consolidated file is a new assembly, not a replacement.

Phase 238 complete: BWD-01/02/03 (238-01) + FWD-01/02 (238-02) + FWD-03 (238-03) + consolidation (238-03 Task 3) all satisfied. 22 KI-exception proof-subject rows prepped for Phase 241 EXC-01..04. 19 gameover-flow rows prepped for Phase 240 GO-01..05. Merged Finding Candidate pool prepped for Phase 242 FIND-01. Zero F-30-NN IDs. Zero contracts/ test/ writes. Zero edits to Phase 237 inventory or Wave 1 outputs.
</success_criteria>

<output>
After completion, create `.planning/phases/238-backward-forward-freeze-proofs/238-03-SUMMARY.md`
</output>
