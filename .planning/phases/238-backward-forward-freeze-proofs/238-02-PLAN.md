---
phase: 238-backward-forward-freeze-proofs
plan: 238-02
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v30-238-02-FWD.md
autonomous: true
requirements: [FWD-01, FWD-02]
requirements_addressed: [FWD-01, FWD-02]
head_anchor: 7ab515fe
head_sha: 7ab515fe
must_haves:
  truths:
    - "`audit/v30-238-02-FWD.md` contains a `## Forward Enumeration Table` with exactly 146 rows, one per INV-237-NNN Row ID in `audit/v30-CONSUMER-INVENTORY.md` Universe List (no rows added, no rows dropped)."
    - "Per D-05 the Forward Enumeration Table has these 7 columns in this exact order: `Row ID | Consumer | Consumption-Site Storage Reads | Write Paths To Each Read | Mutable-After-Request Actors | Actor-Class Closure | FWD-Verdict`."
    - "Per D-05 Actor-Class Closure cell enumerates exactly the 4 D-07 actor classes (player / admin / validator / VRF oracle) with a per-actor verdict from the closed 4-value D-08 vocabulary: `NO_REACHABLE_PATH` / `PATH_BLOCKED_BY_GATE (<gate-name>)` / `EXCEPTION (KI: <header>)` / `CANDIDATE_FINDING`."
    - "Per D-05 FWD-Verdict cell is exactly one of 2 values: `SAFE` / `EXCEPTION`. Rows where all 4 actor-cells are `NO_REACHABLE_PATH` OR `PATH_BLOCKED_BY_GATE` → `SAFE`; rows where ANY actor-cell is `EXCEPTION` → `EXCEPTION` (MUST have populated KI Cross-Ref). Rows with any `CANDIDATE_FINDING` actor-cell → produce a Finding Candidate block per D-14 and route to Phase 242 FIND-01 (do NOT emit F-30-NN)."
    - "Per D-11 exactly 22 rows carry an `EXCEPTION` verdict and a populated KI reference in at least one Actor-Class Closure actor-cell, matching the 237 Consumer Index distribution: EXC-01 = 2 rows (`[KI: \"Non-VRF entropy for affiliate winner roll\"]` — INV-237-005, INV-237-006), EXC-02 = 8 rows (`[KI: \"Gameover prevrandao fallback\"]`), EXC-03 = 4 rows (`[KI: \"Gameover RNG substitution for mid-cycle write-buffer tickets\"]`), EXC-04 = 8 rows (`[KI: \"EntropyLib XOR-shift PRNG for lootbox outcome rolls\"]`). The remaining 124 rows carry `SAFE`."
    - "A `## Forward Mutation Paths` section is present, enumerating per Row ID the set of storage slots touched at consumption time (Consumption-Site Storage Reads column) + the complete write-path universe to each slot (Write Paths To Each Read column) — this is the direct input to Plan 238-03 FWD-03 gating verification. Per D-05: 'Output consumed by 238-03: the Mutable-After-Request Actors + Actor-Class Closure columns become FWD-03 Forward Mutation Paths input.'"
    - "Per D-05 a shared-prefix chain dedup sub-table is present: `## Shared-Prefix Forward-Enumeration Chains` enumerates the 6 chains from 237-03 Plan (PREFIX-DAILY 91 rows, PREFIX-MIDDAY 19, PREFIX-GAMEOVER 7, PREFIX-PREVRANDAO 8, PREFIX-AFFILIATE 2, PREFIX-GAP 3) — body text written once per chain; Forward Enumeration Table per-row cells cite chain name."
    - "A `## Gameover-Flow Forward-Enumeration Subset` section is present per D-12, enumerating the 19 gameover-flow Row IDs with their FWD-Verdict + KI Cross-Ref for Phase 240 GO-01..05 hand-off."
    - "Per D-09/D-10 a `## Prior-Artifact Cross-Cites` section lists every prior-milestone artifact referenced as corroborating evidence (235-04-COMMITMENT-WINDOW.md for 12 v29.0-delta rows, 215-03-COMMITMENT-WINDOW.md for 45 confirmed-fresh-matches-prior rows, ACCESS-CONTROL-MATRIX.md for admin-actor closure, 232.1-03-PFTB-AUDIT.md for non-zero-entropy corroboration); every cite carries a `re-verified at HEAD 7ab515fe` note + one-line structural-equivalence statement."
    - "Per D-15 NO `F-30-NN` finding IDs appear anywhere in the file. A `## Finding Candidates` section enumerates rows with `CANDIDATE_FINDING` actor-cells + informational `EXCEPTION` row list; severity INFO per D-17."
    - "Per D-20 READ-only scope holds: `git diff --name-only 7ab515fe..HEAD -- contracts/ test/` returns empty at task start and end; `git status --porcelain contracts/ test/` returns empty before and after each task."
    - "Per D-19 HEAD anchor `7ab515fe` is locked in frontmatter + echoed in the file's `Audit baseline` line."
    - "Per D-18 `audit/v30-CONSUMER-INVENTORY.md` is NOT edited by this plan — READ-only after Phase 237 commit."
    - "Row-ID integrity: every Forward Enumeration Table Row ID also appears in `audit/v30-CONSUMER-INVENTORY.md` Universe List; no orphan Row IDs; no dropped Row IDs. Verifiable via `diff <(grep -Eo 'INV-237-[0-9]{3}' audit/v30-CONSUMER-INVENTORY.md | sort -u) <(grep -Eo 'INV-237-[0-9]{3}' audit/v30-238-02-FWD.md | sort -u)` returning empty."
  artifacts:
    - path: "audit/v30-238-02-FWD.md"
      provides: "FWD-01 per-consumer consumption-site storage-read enumeration + FWD-02 per-consumer 4-actor adversarial closure — 146-row Forward Enumeration Table + Forward Mutation Paths (direct input to Plan 238-03 FWD-03 gating verification) + 6 shared-prefix chains + Gameover-Flow subset + Finding Candidates"
      contains: "## Forward Enumeration Table"
  key_links:
    - from: "audit/v30-CONSUMER-INVENTORY.md Universe List (146 INV-237-NNN rows)"
      to: "audit/v30-238-02-FWD.md Forward Enumeration Table"
      via: "1:1 row-for-row — same Row ID set, no adds, no drops (FWD-01/02 scope is ALL 146 per Consumer Index)"
      pattern: "INV-237-[0-9]{3}"
    - from: "audit/v30-238-02-FWD.md Forward Mutation Paths + Actor-Class Closure columns"
      to: "Plan 238-03 FWD-03 Gating Verification Table (Wave 2 dependency per D-02)"
      via: "D-05 output contract: 'Mutable-After-Request Actors + Actor-Class Closure columns become FWD-03 Forward Mutation Paths input'"
      pattern: "Forward Mutation"
    - from: "KNOWN-ISSUES.md 4 RNG exception entries (22 proof-subject rows)"
      to: "Forward Enumeration Table EXCEPTION actor-cells + FWD-Verdict EXCEPTION cells"
      via: "D-11 KI-accepted violation envelope; exact quoted KI header"
      pattern: "\\[KI: \""
    - from: "D-07 4-actor taxonomy (player / admin / validator / VRF oracle)"
      to: "Actor-Class Closure column"
      via: "D-08 closed 4-value actor-cell vocabulary"
      pattern: "NO_REACHABLE_PATH|PATH_BLOCKED_BY_GATE|EXCEPTION|CANDIDATE_FINDING"
---

<objective>
FWD-01 + FWD-02 per-consumer forward freeze proof for ALL 146 VRF-consuming call sites enumerated in `audit/v30-CONSUMER-INVENTORY.md` at HEAD `7ab515fe`. Per CONTEXT.md D-01/D-02 this plan runs in Wave 1 in parallel with Plan 238-01 (BWD-01/02/03) — the two plans share zero inputs (both consume the 146-row inventory independently) and have no file-modification overlap.

**Two requirements satisfied:**
- **FWD-01** — per-consumer enumeration of every piece of state read at consumption time + its write path(s); the "what will be read" universe for that consumer.
- **FWD-02** — per-consumer adversarial closure on forward mutability over the 4-actor taxonomy locked in D-07 (player / admin / validator / VRF oracle). Can any actor mutate any consumption-site state between VRF request and consumption? Exhaustive per consumer.

**FWD-03 (gating verification) is Plan 238-03's scope**, not this plan's. This plan produces the Forward Mutation Paths that 238-03 verifies gating against — per D-05: "Output consumed by 238-03: the Mutable-After-Request Actors + Actor-Class Closure columns become FWD-03 Forward Mutation Paths input." 238-03 is Wave 2, depends_on: [238-01, 238-02].

**Deliverable:** `audit/v30-238-02-FWD.md` — STANDALONE sub-deliverable per D-16. Plan 238-03 merges 238-01 + 238-02 + 238-03 outputs into the final consolidated `audit/v30-FREEZE-PROOF.md` at its own final task. This plan does NOT touch the consolidated file.

**Input contract:** `audit/v30-CONSUMER-INVENTORY.md` (146 INV-237-NNN rows + Consumer Index + Per-Consumer Call Graphs + Classification Summary + KI Cross-Ref Summary). Phase 237 output is READ-only per D-18 — this plan does NOT edit it.

**Scope fidelity (per D-11/D-12):** ALL 146 rows are in scope — no sampling, no shortcuts. The 22 KI-exception proof-subject rows carry `EXCEPTION` verdict with KI cross-ref (acceptance re-verification is Phase 241's scope; this plan does NOT re-litigate acceptance). The 19 gameover-flow rows are audited on the same 146-row basis; Phase 240 GO-01..05 layers gameover-jackpot-specific overlay on top.

**Fresh-eyes mandate (per D-09/D-10):** Every FWD verdict is re-derived at HEAD `7ab515fe` from reading `contracts/` directly. Prior-milestone artifacts (235-04-COMMITMENT-WINDOW.md, 215-03-COMMITMENT-WINDOW.md, ACCESS-CONTROL-MATRIX.md, 232.1-03-PFTB-AUDIT.md) are CROSS-CITED as corroborating evidence with a `re-verified at HEAD 7ab515fe` note + structural-equivalence statement. No verdict depends on a prior-milestone artifact alone.

**Presentation dedup (per D-05):** The 6 shared-prefix chains from 237-03 Plan (PREFIX-DAILY 91 rows, PREFIX-MIDDAY 19, PREFIX-GAMEOVER 7, PREFIX-PREVRANDAO 8, PREFIX-AFFILIATE 2, PREFIX-GAP 3 = 130 of 146 rows) are leveraged: body enumeration text written once per chain in a `## Shared-Prefix Forward-Enumeration Chains` sub-table; the Forward Enumeration Table's per-row cells cite the chain by name. The remaining 16 bespoke-tail rows carry inline enumeration bodies.

**No F-30-NN emission per D-15.** Finding candidates (rows with `CANDIDATE_FINDING` actor-cells + informational `EXCEPTION` row list) are collected for Phase 242 FIND-01 intake.

**READ-only scope per D-20.** Only writes: `audit/v30-238-02-FWD.md`. HEAD anchor `7ab515fe` locked per D-19.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/238-backward-forward-freeze-proofs/238-CONTEXT.md
@audit/v30-CONSUMER-INVENTORY.md
@KNOWN-ISSUES.md
</context>

<threat_model>
READ-only audit milestone: no runtime code modified, no test modified, no contracts/ or test/ writes. Attack surface added: NONE. Only writes: markdown under `audit/v30-238-02-FWD.md` (read-only post-commit per D-18). No STRIDE category applies to documentation-only output; threat register empty by construction. Inherits parent phase summary.

Residual risk: an incorrect forward-enumeration verdict (missing a `Mutable-After-Request Actors` path or miscategorizing an actor-cell as `NO_REACHABLE_PATH` when a path exists) would propagate a false-SAFE verdict into Plan 238-03 gating verification, masking an ungated forward mutation path. Mitigation: (a) every Forward Mutation Paths cell cross-cites ACCESS-CONTROL-MATRIX.md for admin-actor closure + 235-04-COMMITMENT-WINDOW.md for commitment-window corroboration with re-verified-at-HEAD notes; (b) the 6 shared-prefix chains deduplicate forward-enumeration bodies, so an error in a shared chain is surfaced at 91× / 19× amplification; (c) the 4-actor taxonomy makes each actor's reachability explicit — `NO_REACHABLE_PATH` cells MUST cite the gating site (access-control modifier, state-machine guard, delegatecall boundary) in the Mutable-After-Request Actors column if the actor has ANY reachable path to the storage slot in question; (d) the 22 KI-exception rows carry `EXCEPTION` actor-cell with exact quoted KI header, so drift from KNOWN-ISSUES.md acceptance envelope surfaces as a grep-diff.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build the 146-row FWD-01/FWD-02 Forward Enumeration Table + 6 shared-prefix chains + Forward Mutation Paths + Gameover-Flow subset → audit/v30-238-02-FWD.md</name>
  <files>audit/v30-238-02-FWD.md</files>
  <read_first>
    - .planning/phases/238-backward-forward-freeze-proofs/238-CONTEXT.md (D-05 Forward Enumeration Table shape; D-07 4-actor taxonomy; D-08 4-value actor-cell vocabulary; D-09/D-10 fresh re-prove + cross-cite; D-11 KI-exception-in-scope; D-12 gameover-flow-in-scope; D-15 no F-30-NN; D-18 prior-artifact READ-only; D-19 HEAD anchor; D-20 READ-only scope)
    - .planning/REQUIREMENTS.md (FWD-01/02 exact wording — "what will be read" universe + "can any actor mutate" adversarial closure)
    - audit/v30-CONSUMER-INVENTORY.md — authoritative 146-row Universe List (section `## Universe List` at line 64); Per-Consumer Call Graphs section (line 273 — every FWD-01 enumeration traverses the same path; STOP at consumption per 237 D-11); Shared-Prefix Notes section (line 2081 — the 6 shared-prefix chains); Consumer Index section (line 2310 — `FWD-01 = ALL`, `FWD-02 = ALL`, EXC + GO distribution)
    - KNOWN-ISSUES.md — 5 quoted KI headers (same set as 238-01)
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD 7ab515fe — load-bearing file for FWD enumeration:
        * `requestRandomWords(...)` at :1521 / `requestLootboxRng(...)` at :1088 — VRF request origin lines
        * `rawFulfillRandomWords(...)` at :1690 — fulfillment SSTORE line (reference commitment window start = `requestRandomWords` + end = `rawFulfillRandomWords`)
        * `rngGate(...)` at :1141-1185 — rngLockedFlag gate (enforces `rngLocked` D-13 named gate)
        * `_gameOverEntropy(...)` at :1222-1246 + `_getHistoricalRngFallback(...)` at :1301 (with 14-day delay at :109 + gate at :1252) — prevrandao KI-exception path
        * `_swapAndFreeze` at :292 + `_swapTicketSlot` at :1082 — F-29-04 write-buffer swap sites
        * `phaseTransitionActive` branch at :283 — `phase-transition-gate` D-13 named gate
        * `_processFutureTicketBatch`, `_prepareFutureTickets`, `_backfillGapDays`, `_finalizeRngRequest`, `_finalizeLootboxRng`
    - contracts/modules/DegenerusGameJackpotModule.sol at HEAD 7ab515fe — 16 daily-path consumer sites' storage-read sets at consumption time (the FWD-01 storage-read universe for daily family)
    - contracts/modules/DegenerusGameLootboxModule.sol at HEAD 7ab515fe — mid-day-lootbox forward enumeration: `lootboxRngWordByIndex[idx]` SLOAD + storage slots feeding the rarity/ticket-count/BURNIE/boon rolls; index-advance isolation (D-13 `lootbox-index-advance` named gate)
    - contracts/modules/DegenerusGameMintModule.sol at HEAD 7ab515fe — entropy-passthrough forward enumeration: `processFutureTicketBatch`, `_prepareFutureTickets`, `_raritySymbolBatch`, `_rollRemainder`
    - contracts/modules/DegenerusGameGameOverModule.sol at HEAD 7ab515fe — gameover consumer forward enumeration
    - contracts/modules/DegenerusGameBoonModule.sol + DegenerusGameDecimatorModule.sol + DegenerusGameDegeneretteModule.sol + DegenerusGameWhaleModule.sol + DegenerusGameMintStreakUtils.sol + DegenerusGamePayoutUtils.sol at HEAD 7ab515fe — auxiliary consumer forward enumeration
    - contracts/DegenerusGame.sol at HEAD 7ab515fe — delegatecall boundaries + any onlyAdmin/onlyOwner modifier enumeration (load-bearing for `admin` actor-cell closure)
    - contracts/DegenerusAffiliate.sol at HEAD 7ab515fe — EXC-01 affiliate winner roll non-VRF seed (INV-237-005, INV-237-006)
    - contracts/BurnieCoinflip.sol at HEAD 7ab515fe — INV-237-001..004 daily coinflip consumers
    - contracts/DegenerusQuests.sol at HEAD 7ab515fe — daily quest consumers
    - contracts/storage/DegenerusGameStorage.sol at HEAD 7ab515fe — storage slot layout (FWD-01 enumeration source: for each consumption-time read, what are ALL SSTORE sites to that slot?)
    - contracts/libraries/EntropyLib.sol + BitPackingLib.sol + JackpotBucketLib.sol at HEAD 7ab515fe — library internal storage reads
    - ACCESS-CONTROL-MATRIX.md at HEAD 7ab515fe (repo-root audit artifact) — admin-actor closure corroboration for every `onlyAdmin` / `onlyOwner` / `onlyGovernance` modifier surface (load-bearing for `admin` actor-cell verdicts per D-07/D-08)
    - STORAGE-WRITE-MAP.md at HEAD 7ab515fe (repo-root audit artifact) — write-site enumeration for every storage slot (direct input to FWD-01 Write Paths column)
    - .planning/milestones/v29.0-phases/235-conservation-rng-commitment-re-proof-phase-transition/235-04-AUDIT.md (RNG-02 commitment-window enumeration — cross-cite source for the 12 v29.0-delta rows per D-10; mirror of FWD-01 structure)
    - .planning/milestones/v25.0-phases/215-rng-fresh-eyes/215-03-COMMITMENT-WINDOW.md (v25.0 commitment-window format — cross-cite source for the 45 confirmed-fresh-matches-prior rows)
    - .planning/milestones/v29.0-phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-PFTB-AUDIT.md (non-zero-entropy guarantee at 4 `_processFutureTicketBatch` call sites — cross-cite source for the `semantic-path-gate` D-13 archetype touched by forward-mutation paths through `_processFutureTicketBatch`)
    - .planning/milestones/v29.0-phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §2 Cross-Module Interaction Map (IM-10..IM-16 delegatecall boundaries — structural corroboration for `admin` and `player` actor-cell reachability analysis)
    - .planning/phases/237-vrf-consumer-inventory-call-graph/237-03-SUMMARY.md — 6 shared-prefix chains definition (same set as 238-01)
  </read_first>
  <action>
Execute the FWD-01 + FWD-02 forward freeze proof for all 146 VRF-consumer rows per CONTEXT.md D-05/D-07/D-08/D-11/D-12.

**Verify prerequisites first:**
```
test -f audit/v30-CONSUMER-INVENTORY.md
[ "$(grep -c '^| INV-237-' audit/v30-CONSUMER-INVENTORY.md)" = "146" ]   # MUST equal 146
git log --oneline -1 -- audit/v30-CONSUMER-INVENTORY.md   # MUST show Phase 237 commit
git diff --name-only 7ab515fe..HEAD -- contracts/ test/   # MUST be empty (D-19/D-20)
git status --porcelain contracts/ test/   # MUST be empty (D-20)
```
If ANY check fails, STOP — the plan cannot proceed without the Phase 237 inventory at HEAD 7ab515fe.

**Step 1 — Parse the 146-row Universe List from `audit/v30-CONSUMER-INVENTORY.md`.** Extract: Row ID, Consumer Function, Consumption File:Line, Path Family, Subcategory, VRF Request Origin File:Line, Fulfillment Site File:Line, KI Cross-Ref, Notes. Every row becomes a row in this plan's Forward Enumeration Table.

**Step 2 — Parse the 6 shared-prefix chains from `audit/v30-CONSUMER-INVENTORY.md`** (same set as 238-01: PREFIX-DAILY 91 rows, PREFIX-MIDDAY 19, PREFIX-GAMEOVER 7, PREFIX-PREVRANDAO 8, PREFIX-AFFILIATE 2, PREFIX-GAP 3).

**Step 3 — For each of the 6 shared-prefix chains, construct the shared forward-enumeration body.** The FWD-01 forward-enumeration is the mirror of BWD-02's storage-read set — same storage slots — but the verdict dimension changes: instead of classifying the write site's temporal relation to the request, we enumerate the set of SSTORE sites per slot and who can reach them in the request→consumption window (the FWD-02 adversarial closure).

Example prose sketch for PREFIX-DAILY (executor re-derives at HEAD):

```
### PREFIX-DAILY — daily path-family shared forward-enumeration body (91 rows)

**Consumption-Site Storage Reads (FWD-01 "what will be read" universe for daily consumers):**

| Slot | Consumption-Site SLOAD Location | Write Paths (all SSTORE sites in contracts/) |
|---|---|---|
| `rngWordCurrent` | rngGate → AdvanceModule:1143 | rawFulfillRandomWords @ AdvanceModule:1702 + _finalizeRngRequest @ AdvanceModule:<line> |
| `rngLockedFlag` | rngGate → AdvanceModule:1141 | requestRandomWords setter @ AdvanceModule:<line> (set to true) + rawFulfillRandomWords @ AdvanceModule:<line> (cleared to false) |
| `rngWordByDay[day]` | rngGate → AdvanceModule:1143 | _applyDailyRng @ AdvanceModule:1164 |
| `phaseTransitionActive` | AdvanceModule:283 branch | advanceGame-origin writes only (per D-13 phase-transition-gate) |
| [per-row tail slot] | [tail consumption line] | [tail SSTORE sites] |

**FWD-02 Adversarial Closure (same for all 91 member rows up to bifurcation):**

| Actor | Verdict | Path (if exists) | Gate (if blocked) |
|---|---|---|---|
| player | NO_REACHABLE_PATH | — | — |
| admin | PATH_BLOCKED_BY_GATE (rngLocked) | admin setters exist for ancillary state, but ACCESS-CONTROL-MATRIX.md shows no onlyAdmin modifier writes to rngWordCurrent or rngWordByDay or lootboxRngWordByIndex; rngLockedFlag set/clear is automatic | rngLocked |
| validator | NO_REACHABLE_PATH | validator can reorder txs but commitment window is single-block on rawFulfillRandomWords → consumption chain; short-range reorg CAN'T introduce SSTOREs to rngWordCurrent mid-window (only VRF oracle can) | — |
| VRF oracle | NO_REACHABLE_PATH (word binding) | can delay fulfillment but cannot bias word; 14-day fallback at AdvanceModule:109/:1252 per KI EXC-02 | — |

Chain-level FWD-Verdict: SAFE (all 91 member rows). Cross-cite: 235-04-AUDIT.md Category 1-2; 215-03-COMMITMENT-WINDOW.md §1. Re-verified at HEAD 7ab515fe — structural equivalence: rngGate body unchanged; admin-actor ACCESS-CONTROL-MATRIX.md surface identical to v29.0 baseline.
```

Each of the 6 chains gets an analogous body. Chain-level FWD-Verdicts:
- **PREFIX-DAILY** (91 rows) — SAFE (`rngLocked` named gate)
- **PREFIX-MIDDAY** (19 rows) — SAFE (`lootbox-index-advance` named gate per KI-5 + D-13)
- **PREFIX-GAMEOVER** (7 rows) — SAFE (`phase-transition-gate` + `semantic-path-gate` — gameover-entropy is post-finalize one-way)
- **PREFIX-PREVRANDAO** (8 rows) — EXCEPTION `[KI: "Gameover prevrandao fallback"]`; the 14-day `GAMEOVER_RNG_FALLBACK_DELAY` is the effective gate per D-13 `semantic-path-gate` archetype, cited from AdvanceModule:109 + :1252
- **PREFIX-AFFILIATE** (2 rows) — EXCEPTION `[KI: "Non-VRF entropy for affiliate winner roll"]`; no VRF-request window (non-VRF seed — degenerate FWD-enumeration; the "forward mutation path" for these rows is the non-VRF-seed computation itself — accepted per KI)
- **PREFIX-GAP** (3 rows) — SAFE (gap-backfill uses `keccak(vrfWord, gapDay)` derivation; gate is `semantic-path-gate` — backfill executes in a single `_backfillGapDays` loop iteration, no interleaved player mutations)

**Step 4 — For each of the 146 Universe List rows, write one row in the `## Forward Enumeration Table`.** Columns in this exact order (per D-05):

```
| Row ID | Consumer | Consumption-Site Storage Reads | Write Paths To Each Read | Mutable-After-Request Actors | Actor-Class Closure | FWD-Verdict |
```

Row construction rules:
- **Row ID** — copy from inventory.
- **Consumer** — copy from inventory Consumer Function column.
- **Consumption-Site Storage Reads** — for shared-prefix chain members, cite the chain name + tail-specific reads (e.g. `PREFIX-DAILY + tail: keccak(rngWord, epoch) seed @ :808 reads rngWord param (passthrough, not SLOAD)`). For bespoke-tail rows, inline-enumerate every SLOAD on the consumption path.
- **Write Paths To Each Read** — per storage slot, list every SSTORE site that writes to that slot in `contracts/` at HEAD. For slot `rngWordCurrent`: `rawFulfillRandomWords @ AdvanceModule:1702 + _finalizeRngRequest @ AdvanceModule:<line>`. Cross-cite STORAGE-WRITE-MAP.md for corroboration. For shared-chain members, cite the chain body; inline only the tail-specific reads.
- **Mutable-After-Request Actors** — list the set of actors (subset of player / admin / validator / VRF oracle) that have ANY reachable call path to ANY SSTORE site for ANY storage slot in the Consumption-Site Storage Reads column between VRF request and consumption. For most rows, this is EMPTY (`∅`) because all SSTORE sites are either: Oracle-only (`rawFulfillRandomWords`), guarded by `rngLockedFlag` (set at request time, cleared at fulfillment — see `rngLocked` D-13 gate), guarded by index-advance asymmetry (mid-day-lootbox), or guarded by `phaseTransitionActive` (gameover). For EXC rows, list the actor class matching the KI envelope (e.g., for EXC-02 prevrandao: `validator (via block.prevrandao bias)` — accepted envelope per KI-2).
- **Actor-Class Closure** — the 4-actor per-cell closure per D-07/D-08. Format as a compact 4-tuple string: `[player: NO_REACHABLE_PATH; admin: PATH_BLOCKED_BY_GATE (rngLocked); validator: NO_REACHABLE_PATH; VRF oracle: NO_REACHABLE_PATH]`. Cell vocabulary strictly limited to the 4 D-08 values:
    - `NO_REACHABLE_PATH` — actor cannot mutate any consumption-site storage slot between VRF request and consumption.
    - `PATH_BLOCKED_BY_GATE (<gate-name>)` — path exists, gate blocks it; gate MUST be one of D-13 4-value taxonomy: `rngLocked` / `lootbox-index-advance` / `phase-transition-gate` / `semantic-path-gate`.
    - `EXCEPTION (KI: <header>)` — accepted violation per KNOWN-ISSUES.md.
    - `CANDIDATE_FINDING` — actor path exists + gate absent or ineffective; route to Phase 242 FIND-01 + produce a Finding Candidate block per D-14.
- **FWD-Verdict** — exactly one of `SAFE` / `EXCEPTION`. Rule: any actor-cell `EXCEPTION` → FWD-Verdict = `EXCEPTION`; any actor-cell `CANDIDATE_FINDING` → produce Finding Candidate per D-14 (FWD-Verdict may still be `SAFE` or `EXCEPTION` depending on other cells, but the CANDIDATE_FINDING must be surfaced); otherwise FWD-Verdict = `SAFE`.

**EXC row distribution check (enforced — MUST match 237-02 Plan SUMMARY):**
- EXC-01 (affiliate) = 2 EXCEPTION rows — INV-237-005, INV-237-006
- EXC-02 (prevrandao) = 8 EXCEPTION rows — `exception-prevrandao-fallback` subcategory
- EXC-03 (F-29-04) = 4 EXCEPTION rows — `exception-mid-cycle-substitution` subcategory
- EXC-04 (EntropyLib) = 8 EXCEPTION rows — EntropyLib.entropyStep() callers
- **Total EXCEPTION rows = 22** (124 SAFE + 22 EXCEPTION = 146)

If the derived count diverges, STOP and record a scope-guard deferral per D-18. Do NOT adjust — flag for Phase 242 FIND-01.

**Step 5 — Write the `## Forward Mutation Paths` section.** This is the direct input to Plan 238-03 FWD-03 per D-05. Format: per Row ID, list the set of (storage slot, actor, gate name) tuples that Plan 238-03 must verify gating against. For most rows the Mutable-After-Request Actors column is empty and the corresponding entry is `∅ (no forward mutation paths — all actor-cells NO_REACHABLE_PATH or PATH_BLOCKED_BY_GATE)`. For rows with `PATH_BLOCKED_BY_GATE` cells, enumerate the (slot, actor, gate-name) tuple for Plan 238-03's gating verification.

Example:
```
| Row ID | Storage Slot | Actor | Gate Required |
|---|---|---|---|
| INV-237-001 | rngWordCurrent | admin | rngLocked |
| INV-237-001 | rngLockedFlag | admin | rngLocked (self-gating) |
| INV-237-005 | (non-VRF seed) | — | EXCEPTION (KI: Non-VRF entropy for affiliate winner roll) |
| INV-237-021 | lootboxRngWordByIndex[idx] | admin | lootbox-index-advance |
| ... | ... | ... | ... |
```

This table is the authoritative FWD-03 input; Plan 238-03's Gating Verification Table joins against it.

**Step 6 — Write `audit/v30-238-02-FWD.md` with EXACTLY these sections in this order:**

```markdown
# v30.0 Phase 238 Plan 02 — FWD-01/02 Per-Consumer Forward Freeze Proof

**Audit baseline:** HEAD `7ab515fe` (contract tree identical to v29.0 `1646d5af`; post-v29 commits are docs-only per D-19).
**Requirements addressed:** FWD-01 (per-consumer consumption-site storage-read enumeration + write-path universe), FWD-02 (per-consumer 4-actor adversarial closure on forward mutability).
**Scope:** ALL 146 INV-237-NNN rows from `audit/v30-CONSUMER-INVENTORY.md` Universe List — no sampling (per D-11).
**Downstream consumer (per D-02/D-05):** `## Forward Mutation Paths` section is direct input to Plan 238-03 FWD-03 gating verification (Wave 2). Plan 238-03 joins the Forward Mutation Paths table by Row ID.
**KI handling (per D-11):** 22 EXCEPTION rows (EXC-01=2, EXC-02=8, EXC-03=4, EXC-04=8); acceptance re-verification is Phase 241 scope.
**Gameover scope (per D-12):** 19 gameover-flow rows audited on same 146-row basis; Phase 240 GO-01..05 overlay.
**Presentation dedup (per D-05):** 6 shared-prefix chains from 237-03 cover 130/146 rows.
**Fresh-eyes re-derivation (per D-09/D-10):** Every verdict re-derived at HEAD 7ab515fe; prior-milestone artifacts CROSS-CITED with `re-verified at HEAD 7ab515fe` note.
**Finding-ID emission (per D-15):** NO F-30-NN. Candidate pool routes to Phase 242 FIND-01.
**READ-only scope (per D-20):** zero contracts/ test/ writes; zero edits to `audit/v30-CONSUMER-INVENTORY.md`.

## Table of Contents

1. [Shared-Prefix Forward-Enumeration Chains](#shared-prefix-forward-enumeration-chains)
2. [Forward Enumeration Table](#forward-enumeration-table) — FWD-01 + FWD-02 (146 rows × 7 columns)
3. [Forward Mutation Paths](#forward-mutation-paths) — Plan 238-03 FWD-03 input
4. [Gameover-Flow Forward-Enumeration Subset](#gameover-flow-forward-enumeration-subset) — D-12 Phase 240 hand-off (19 rows)
5. [Prior-Artifact Cross-Cites](#prior-artifact-cross-cites) — D-09/D-10
6. [Finding Candidates](#finding-candidates) — D-15/D-17
7. [Scope-Guard Deferrals](#scope-guard-deferrals) — D-18
8. [Attestation](#attestation)

## Shared-Prefix Forward-Enumeration Chains

[Per D-05. 6 chains from 237-03 Plan: PREFIX-DAILY (91 rows, SAFE via rngLocked), PREFIX-MIDDAY (19 rows, SAFE via lootbox-index-advance), PREFIX-GAMEOVER (7 rows, SAFE via phase-transition-gate + semantic-path-gate), PREFIX-PREVRANDAO (8 rows, EXCEPTION KI-2), PREFIX-AFFILIATE (2 rows, EXCEPTION KI-1), PREFIX-GAP (3 rows, SAFE via semantic-path-gate). Each chain gets a subsection with: member Row IDs list; Consumption-Site Storage Reads sub-table (slot | read-site | write-paths); FWD-02 4-actor closure sub-table (actor | verdict | path | gate); chain-level FWD-Verdict.]

## Forward Enumeration Table

**One row per INV-237-NNN (146 rows total).** Columns per D-05:

| Row ID | Consumer | Consumption-Site Storage Reads | Write Paths To Each Read | Mutable-After-Request Actors | Actor-Class Closure | FWD-Verdict |
|---|---|---|---|---|---|---|
| INV-237-001 | processCoinflipPayouts | PREFIX-DAILY + tail: rngWord param passthrough @ :808 | PREFIX-DAILY body (see shared-prefix chain) | ∅ | [player: NO_REACHABLE_PATH; admin: PATH_BLOCKED_BY_GATE (rngLocked); validator: NO_REACHABLE_PATH; VRF oracle: NO_REACHABLE_PATH] | SAFE |
| ... | ... | ... | ... | ... | ... | ... |
| INV-237-146 | ... | ... | ... | ... | ... | ... |

[Every Row ID from `audit/v30-CONSUMER-INVENTORY.md` Universe List gets exactly one row. Sort by Row ID ascending. No placeholder tokens. No F-30-NN. Every Actor-Class Closure cell contains exactly 4 actor entries from the D-07 taxonomy with verdicts from the D-08 4-value vocabulary.]

## Forward Mutation Paths

**Direct input to Plan 238-03 FWD-03 Gating Verification Table.** Per D-05: "Output consumed by 238-03: the Mutable-After-Request Actors + Actor-Class Closure columns become FWD-03 Forward Mutation Paths input."

For each INV-237-NNN, enumerate every (storage slot, actor, required gate) tuple that Plan 238-03 must verify. Rows with empty Mutable-After-Request Actors appear here with `∅ (no forward mutation paths — all actor-cells NO_REACHABLE_PATH)` for completeness.

| Row ID | Storage Slot | Actor | Gate Required (D-13 taxonomy) | FWD-02 Actor Verdict |
|---|---|---|---|---|
| INV-237-001 | rngWordCurrent | admin | rngLocked | PATH_BLOCKED_BY_GATE (rngLocked) |
| INV-237-001 | rngLockedFlag | admin | rngLocked (self-gating) | PATH_BLOCKED_BY_GATE (rngLocked) |
| INV-237-001 | — | player | — | NO_REACHABLE_PATH |
| INV-237-001 | — | validator | — | NO_REACHABLE_PATH |
| INV-237-001 | — | VRF oracle | — | NO_REACHABLE_PATH |
| INV-237-005 | (non-VRF seed) | — | EXCEPTION | EXCEPTION (KI: Non-VRF entropy for affiliate winner roll) |
| INV-237-021 | lootboxRngWordByIndex[idx] | admin | lootbox-index-advance | PATH_BLOCKED_BY_GATE (lootbox-index-advance) |
| ... | ... | ... | ... | ... |

## Gameover-Flow Forward-Enumeration Subset

**Per D-12 — Phase 240 GO-01..05 hand-off.** 19 gameover-flow Row IDs with FWD-Verdict + KI Cross-Ref:

| Row ID | Path Family + Subcategory | FWD-Verdict | KI Cross-Ref |
|---|---|---|---|
| INV-237-NNN | gameover-entropy | SAFE | N/A |
| INV-237-NNN | other / exception-prevrandao-fallback | EXCEPTION | [KI: "Gameover prevrandao fallback"] |
| INV-237-NNN | other / exception-mid-cycle-substitution | EXCEPTION | [KI: "Gameover RNG substitution for mid-cycle write-buffer tickets"] |
| ... | ... | ... | ... |

Total gameover-flow rows: exactly 19. SAFE subset = 7 (gameover-entropy); EXCEPTION subset = 12 (8 prevrandao + 4 F-29-04).

## Prior-Artifact Cross-Cites

Per D-09/D-10 — every cross-cite carries a `re-verified at HEAD 7ab515fe` note + one-line structural-equivalence statement.

| Prior Artifact | Rows Covered | Cross-Cite Relationship | Re-Verified Statement |
|---|---|---|---|
| `.planning/milestones/v29.0-phases/235-*/235-04-AUDIT.md` (RNG-02 commitment-window enumeration) | 12 v29.0-delta rows | Per-consumer commitment-window template; mirror of FWD-01 structure | re-verified at HEAD 7ab515fe — structural equivalence: commitment-window start/end sites unchanged since v29.0 1646d5af |
| `.planning/milestones/v25.0-phases/215-rng-fresh-eyes/215-03-COMMITMENT-WINDOW.md` | 45 `confirmed-fresh-matches-prior` rows | v25.0 per-consumer commitment-window format | re-verified at HEAD 7ab515fe — commitment-window state set unchanged |
| ACCESS-CONTROL-MATRIX.md (repo-root) | ALL 146 rows (admin-actor closure corroboration) | `onlyAdmin` / `onlyOwner` / `onlyGovernance` modifier surface enumeration | re-verified at HEAD 7ab515fe — modifier surface identical to v29.0 baseline; zero new onlyAdmin functions |
| `.planning/milestones/v29.0-phases/232.1-*/232.1-03-PFTB-AUDIT.md` | 4 `_processFutureTicketBatch` call sites (non-zero-entropy guarantee) | D-13 `semantic-path-gate` archetype corroboration | re-verified at HEAD 7ab515fe — 4-path walk results unchanged; invariant tests 8/8 PASS |
| `.planning/milestones/v29.0-phases/230-*/230-01-DELTA-MAP.md` §2 IM-10..IM-16 | 12 v29.0-delta rows (delegatecall boundaries) | Structural corroboration for admin + player actor reachability across delegatecall | re-verified at HEAD 7ab515fe — IM surface unchanged |

## Finding Candidates

Per D-15/D-17. Informational `EXCEPTION` rows (KI-accepted — not new findings) and first-principles `CANDIDATE_FINDING` rows (fresh-eyes discoveries for Phase 242 intake). Severity INFO per D-17 unless unambiguously higher. NO F-30-NN IDs.

Format for each candidate:
```
- **[CANDIDATE_FINDING | EXCEPTION]** INV-237-NNN — contracts/<path>:<line> — <fn> — <rationale> — severity: <INFO|LOW|MEDIUM|HIGH>
```

Expected EXCEPTION entries (22 — informational, KI-accepted):
- EXC-01 — INV-237-005, INV-237-006 (affiliate winner roll non-VRF seed)
- EXC-02 — 8 rows (gameover prevrandao fallback, 14-day gate)
- EXC-03 — 4 rows (F-29-04 mid-cycle write-buffer substitution)
- EXC-04 — 8 rows (EntropyLib.entropyStep() XOR-shift)

Expected CANDIDATE_FINDING entries: 0 expected (if any surface during fresh re-derivation, enumerate them with file:line + rationale + severity; otherwise write "No CANDIDATE_FINDING entries surfaced during FWD-01/02 re-derivation.").

## Scope-Guard Deferrals

Per D-18 — if any row's forward enumeration requires editing `audit/v30-CONSUMER-INVENTORY.md` (e.g., a new SSTORE site to an inventoried storage slot not in STORAGE-WRITE-MAP.md at HEAD, an EXC row count that diverges from 22, a new admin actor path not in ACCESS-CONTROL-MATRIX.md), record the deferral here without editing Phase 237 output. Route to Phase 242 FIND-01. If none surface, write "None surfaced during forward-freeze re-derivation."

## Attestation

- Audit baseline HEAD: `7ab515fe`
- `git diff --name-only 7ab515fe..HEAD -- contracts/ test/` returned empty at task start and end
- `git status --porcelain contracts/ test/` returned empty before and after this task
- Input file `audit/v30-CONSUMER-INVENTORY.md` was present and committed before this task started; row count = 146
- Input Row ID integrity: this file contains a Forward Enumeration Table entry for every INV-237-NNN Row ID in the inventory (row count = 146)
- EXCEPTION row distribution: EXC-01=2, EXC-02=8, EXC-03=4, EXC-04=8, total=22 (matches 237-02 Plan SUMMARY)
- Gameover-flow subset: 19 rows (matches 237 Consumer Index GO-01..04)
- Shared-prefix chains: 6 chains covering 130/146 rows (matches 237-03 Plan)
- Contract source reads limited to `contracts/**/*.sol` at HEAD `7ab515fe`
- `audit/v30-CONSUMER-INVENTORY.md` NOT modified by this task (D-18 enforcement — READ-only after Phase 237 commit)
- Prior-milestone artifacts CROSS-CITED only (not relied upon per D-09); every cite carries `re-verified at HEAD 7ab515fe` note
- Zero F-30-NN IDs (D-15 enforcement)
- Forward Mutation Paths table contains entries for every Row ID (for Plan 238-03 FWD-03 input)
```

Every Forward Enumeration Table row MUST:
- Have Row ID matching `INV-237-[0-9]{3}` regex AND appearing in `audit/v30-CONSUMER-INVENTORY.md` Universe List
- Have Actor-Class Closure cell containing exactly 4 actor entries: `player`, `admin`, `validator`, `VRF oracle`
- Have each actor-cell verdict exactly one of: `NO_REACHABLE_PATH`, `PATH_BLOCKED_BY_GATE (<gate-name>)`, `EXCEPTION (KI: <header>)`, `CANDIDATE_FINDING`
- PATH_BLOCKED_BY_GATE cells MUST name the gate — one of 4 D-13 values: `rngLocked` / `lootbox-index-advance` / `phase-transition-gate` / `semantic-path-gate`
- Have FWD-Verdict exactly one of: `SAFE`, `EXCEPTION`
- Never contain F-30-NN references (D-15)
- Never contain placeholder tokens `<path>`, `<line>`, `<fn`, `<family>`, `TBD-*`

Every Forward Mutation Paths row MUST:
- Cite a Row ID present in the Forward Enumeration Table
- Name the storage slot (or `(non-VRF seed)` / `(passthrough)` where applicable)
- Name the actor (one of player / admin / validator / VRF oracle, or `—` for `(non-VRF seed)` rows)
- Name the required gate (one of 4 D-13 values, or `EXCEPTION` for KI rows, or `—` for NO_REACHABLE_PATH rows)
- Cite the FWD-02 Actor Verdict consistent with the Forward Enumeration Table

READ-only per D-20. Only writes: `audit/v30-238-02-FWD.md`. `git status --porcelain contracts/ test/` MUST be empty after the write. `audit/v30-CONSUMER-INVENTORY.md` MUST remain unmodified.
  </action>
  <verify>
    <automated>test -f audit/v30-238-02-FWD.md && grep -q "^## Shared-Prefix Forward-Enumeration Chains" audit/v30-238-02-FWD.md && grep -q "^## Forward Enumeration Table" audit/v30-238-02-FWD.md && grep -q "^## Forward Mutation Paths" audit/v30-238-02-FWD.md && grep -q "^## Gameover-Flow Forward-Enumeration Subset" audit/v30-238-02-FWD.md && grep -q "^## Prior-Artifact Cross-Cites" audit/v30-238-02-FWD.md && grep -q "^## Finding Candidates" audit/v30-238-02-FWD.md && grep -q "^## Scope-Guard Deferrals" audit/v30-238-02-FWD.md && grep -q "^## Attestation" audit/v30-238-02-FWD.md && [ "$(grep -c '^| INV-237-' audit/v30-238-02-FWD.md)" -ge 146 ] && [ "$(grep -Eo 'INV-237-[0-9]{3}' audit/v30-238-02-FWD.md | sort -u | wc -l)" = "146" ] && [ "$(grep -c 'Non-VRF entropy for affiliate winner roll' audit/v30-238-02-FWD.md)" -ge 2 ] && [ "$(grep -c 'Gameover prevrandao fallback' audit/v30-238-02-FWD.md)" -ge 8 ] && [ "$(grep -c 'Gameover RNG substitution for mid-cycle write-buffer tickets' audit/v30-238-02-FWD.md)" -ge 4 ] && [ "$(grep -c 'EntropyLib XOR-shift PRNG' audit/v30-238-02-FWD.md)" -ge 8 ] && grep -q "PREFIX-DAILY" audit/v30-238-02-FWD.md && grep -q "PREFIX-MIDDAY" audit/v30-238-02-FWD.md && grep -q "PREFIX-GAMEOVER" audit/v30-238-02-FWD.md && grep -q "PREFIX-PREVRANDAO" audit/v30-238-02-FWD.md && grep -q "PREFIX-AFFILIATE" audit/v30-238-02-FWD.md && grep -q "PREFIX-GAP" audit/v30-238-02-FWD.md && grep -q "NO_REACHABLE_PATH" audit/v30-238-02-FWD.md && grep -q "PATH_BLOCKED_BY_GATE" audit/v30-238-02-FWD.md && grep -q "rngLocked" audit/v30-238-02-FWD.md && grep -q "lootbox-index-advance" audit/v30-238-02-FWD.md && grep -q "phase-transition-gate" audit/v30-238-02-FWD.md && grep -q "semantic-path-gate" audit/v30-238-02-FWD.md && grep -q "player" audit/v30-238-02-FWD.md && grep -q "admin" audit/v30-238-02-FWD.md && grep -q "validator" audit/v30-238-02-FWD.md && grep -q "VRF oracle" audit/v30-238-02-FWD.md && ! grep -qE "F-30-[0-9]+" audit/v30-238-02-FWD.md && ! grep -q "<line>" audit/v30-238-02-FWD.md && ! grep -q "<path>" audit/v30-238-02-FWD.md && ! grep -q "TBD-" audit/v30-238-02-FWD.md && grep -q "7ab515fe" audit/v30-238-02-FWD.md && grep -q "re-verified at HEAD 7ab515fe" audit/v30-238-02-FWD.md && test -z "$(git status --porcelain contracts/ test/)" && test -z "$(git status --porcelain audit/v30-CONSUMER-INVENTORY.md 2>/dev/null)"</automated>
  </verify>
  <acceptance_criteria>
    - File `audit/v30-238-02-FWD.md` exists
    - File contains 8 literal section headers in this order: `## Shared-Prefix Forward-Enumeration Chains`, `## Forward Enumeration Table`, `## Forward Mutation Paths`, `## Gameover-Flow Forward-Enumeration Subset`, `## Prior-Artifact Cross-Cites`, `## Finding Candidates`, `## Scope-Guard Deferrals`, `## Attestation`
    - Forward Enumeration Table has ≥ 146 INV-237-NNN Row ID lines
    - Distinct INV-237-NNN IDs in file = exactly 146
    - EXC distribution grep counts: `Non-VRF entropy for affiliate winner roll` ≥ 2 (EXC-01); `Gameover prevrandao fallback` ≥ 8 (EXC-02); `Gameover RNG substitution for mid-cycle write-buffer tickets` ≥ 4 (EXC-03); `EntropyLib XOR-shift PRNG` ≥ 8 (EXC-04)
    - All 6 shared-prefix chain names appear
    - All 4 actor class names appear: `player`, `admin`, `validator`, `VRF oracle`
    - All 4 actor-cell verdict values appear: `NO_REACHABLE_PATH`, `PATH_BLOCKED_BY_GATE`, and at least one instance mentioning `EXCEPTION` verdict context; CANDIDATE_FINDING vocabulary present or Finding Candidates section states "No CANDIDATE_FINDING entries"
    - All 4 D-13 named gates appear: `rngLocked`, `lootbox-index-advance`, `phase-transition-gate`, `semantic-path-gate`
    - File does NOT contain `F-30-NN` references (D-15)
    - File does NOT contain placeholder tokens `<line>`, `<path>`, `<fn`, `TBD-`
    - File contains literal `7ab515fe` in the Audit baseline line
    - File contains literal `re-verified at HEAD 7ab515fe` (D-09/D-10 enforcement)
    - Forward Mutation Paths table present (input to Plan 238-03)
    - `audit/v30-CONSUMER-INVENTORY.md` NOT edited (D-18)
    - `git status --porcelain contracts/ test/` returns empty
    - `git diff --name-only 7ab515fe..HEAD -- contracts/ test/` returns empty
  </acceptance_criteria>
  <deliverable>
`audit/v30-238-02-FWD.md` — STANDALONE FWD-01/02 deliverable. 146-row Forward Enumeration Table (7 columns per D-05) + Forward Mutation Paths (direct input to Plan 238-03) + 6 shared-prefix chains + 19-row Gameover-Flow subset + Prior-Artifact Cross-Cites + Finding Candidates + Scope-Guard Deferrals + Attestation. 22 EXCEPTION rows matching 237-02 EXC distribution. All 4 actor classes + all 4 D-13 gates covered. Zero F-30-NN. Zero contracts/ test/ writes. Zero edits to Phase 237 output.
  </deliverable>
  <done>File exists; 8 required sections present; 146 distinct INV-237-NNN Row IDs; 22 EXCEPTION rows matching EXC distribution; 4 actor classes + 4 D-13 gates present; no F-30-NN; no placeholder tokens; HEAD anchor echoed; cross-cites carry re-verified-at-HEAD note; Forward Mutation Paths table ready for Plan 238-03 consumption; `audit/v30-CONSUMER-INVENTORY.md` unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Commit audit/v30-238-02-FWD.md</name>
  <files>audit/v30-238-02-FWD.md</files>
  <read_first>
    - audit/v30-238-02-FWD.md (must exist from Task 1)
  </read_first>
  <action>
Stage and commit `audit/v30-238-02-FWD.md` only. Use `git add` with explicit path — do NOT `git add .`.

```
git add audit/v30-238-02-FWD.md
git commit -m "docs(238-02): FWD-01/02 per-consumer forward freeze proof at HEAD 7ab515fe

146-row Forward Enumeration Table (one per INV-237-NNN from audit/v30-
CONSUMER-INVENTORY.md) with 7 columns per D-05: Row ID / Consumer /
Consumption-Site Storage Reads / Write Paths To Each Read / Mutable-
After-Request Actors / Actor-Class Closure / FWD-Verdict.

Actor-Class Closure uses D-07 4-actor taxonomy (player / admin /
validator / VRF oracle) with D-08 4-value cell vocabulary
(NO_REACHABLE_PATH / PATH_BLOCKED_BY_GATE (+gate name) / EXCEPTION (KI:
header) / CANDIDATE_FINDING).

PATH_BLOCKED_BY_GATE cells cite one of 4 D-13 named gates: rngLocked /
lootbox-index-advance / phase-transition-gate / semantic-path-gate.

EXCEPTION row count: 22 — matches 237-02 Plan SUMMARY EXC-01=2 / EXC-02=8
/ EXC-03=4 / EXC-04=8 distribution.

## Forward Mutation Paths section is direct input to Plan 238-03 FWD-03
gating verification (Wave 2). Plan 238-03 joins by Row ID.

Presentation dedup (per D-05): 6 shared-prefix chains from 237-03 cover
130/146 rows.

19-row Gameover-Flow Forward-Enumeration Subset for Phase 240 GO-01..05
hand-off per D-12.

Prior-milestone artifacts CROSS-CITED with re-verified-at-HEAD-7ab515fe
notes (per D-09/D-10): 235-04-AUDIT.md, 215-03-COMMITMENT-WINDOW.md,
ACCESS-CONTROL-MATRIX.md, 232.1-03-PFTB-AUDIT.md, 230-01-DELTA-MAP §2.

NO F-30-NN IDs (D-15 — Phase 242 owns). READ-only per D-20 — zero
contracts/ test/ writes; audit/v30-CONSUMER-INVENTORY.md unmodified (D-18).

HEAD anchor 7ab515fe (D-19).

Wave 1 parallel with Plan 238-01 (BWD-01/02/03)."
```

Do NOT push.
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -qE "238-02.*(FWD|forward freeze).*7ab515fe" && test -z "$(git status --porcelain contracts/ test/)" && test -z "$(git status --porcelain audit/v30-CONSUMER-INVENTORY.md 2>/dev/null)"</automated>
  </verify>
  <acceptance_criteria>
    - One new commit with subject containing `238-02` + (`FWD` OR `forward freeze`) + `7ab515fe`
    - Commit touches only `audit/v30-238-02-FWD.md`
    - `git status --porcelain contracts/ test/` empty
    - `git status --porcelain audit/v30-CONSUMER-INVENTORY.md` empty (Phase 237 output untouched)
    - Not pushed
  </acceptance_criteria>
  <deliverable>`audit/v30-238-02-FWD.md` committed; local-only; Wave 1 deliverable ready for Plan 238-03 FWD-03 gating verification in Wave 2.</deliverable>
  <done>Commit exists; contracts/ test/ unmodified; `audit/v30-CONSUMER-INVENTORY.md` unmodified; FWD-01/02 deliverable committed.</done>
</task>

</tasks>

<verification>
- `audit/v30-238-02-FWD.md` exists with all 8 required sections (Shared-Prefix Forward-Enumeration Chains, Forward Enumeration Table, Forward Mutation Paths, Gameover-Flow Forward-Enumeration Subset, Prior-Artifact Cross-Cites, Finding Candidates, Scope-Guard Deferrals, Attestation)
- Exactly 146 distinct INV-237-NNN Row IDs appear; Forward Enumeration Table has 146 rows
- 22 EXCEPTION rows: EXC-01=2 / EXC-02=8 / EXC-03=4 / EXC-04=8 (matches 237-02 SUMMARY)
- 124 SAFE rows
- 19 gameover-flow rows in dedicated subset
- 6 shared-prefix chains named
- 4-actor taxonomy applied (player / admin / validator / VRF oracle)
- 4-value actor-cell vocabulary + 4 D-13 named gates present (rngLocked / lootbox-index-advance / phase-transition-gate / semantic-path-gate)
- Forward Mutation Paths table present (Plan 238-03 input)
- No F-30-NN IDs (D-15)
- No placeholder tokens
- HEAD anchor `7ab515fe` echoed
- At least one `re-verified at HEAD 7ab515fe` cross-cite
- Zero contracts/ test/ writes (D-20)
- Zero edits to `audit/v30-CONSUMER-INVENTORY.md` (D-18)
- Deliverable committed; not pushed
</verification>

<success_criteria>
FWD-01 + FWD-02 satisfied: every one of 146 VRF-consuming call sites has its consumption-time storage-read set enumerated (FWD-01 "what will be read" universe) with complete write-path universe per slot, plus 4-actor adversarial closure (FWD-02) using the D-07 taxonomy + D-08 closed 4-value vocabulary + D-13 4-value gate taxonomy. 22 KI-accepted EXCEPTION rows match 237-02 distribution. 19 gameover-flow rows prepped for Phase 240 hand-off. 6 shared-prefix chains leverage 237-03 dedup. Prior-milestone artifacts CROSS-CITED with re-verified-at-HEAD notes. Forward Mutation Paths table is the authoritative FWD-03 input for Plan 238-03 (Wave 2 consumer per D-02/D-05). Phase 238 Plan 03's consolidation task inherits this file intact.
</success_criteria>

<output>
After completion, create `.planning/phases/238-backward-forward-freeze-proofs/238-02-SUMMARY.md`
</output>
