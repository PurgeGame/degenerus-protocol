---
phase: 238-backward-forward-freeze-proofs
plan: 238-01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v30-238-01-BWD.md
autonomous: true
requirements: [BWD-01, BWD-02, BWD-03]
requirements_addressed: [BWD-01, BWD-02, BWD-03]
head_anchor: 7ab515fe
head_sha: 7ab515fe
must_haves:
  truths:
    - "`audit/v30-238-01-BWD.md` contains a `## Backward Freeze Table` with exactly 146 rows, one per INV-237-NNN Row ID in `audit/v30-CONSUMER-INVENTORY.md` Universe List (no rows added, no rows dropped)."
    - "Per D-04 the Backward Freeze Table has these 7 columns in this exact order: `Row ID | Consumer | Consumption File:Line | Storage Reads On Consumption Path | Write-Site Classification | KI Cross-Ref | Backward-Trace Verdict`."
    - "Per D-04 every row's Write-Site Classification cell is exactly one of the 3 locked values: `written-before-request` / `unreachable-after-request` / `EXCEPTION`. Per REQUIREMENTS.md BWD-02 wording: NO row may carry the verdict `mutable-after-request` unless it is explicitly mapped to a KI entry — `EXCEPTION` is the only escape hatch."
    - "Per D-04 every row's Backward-Trace Verdict cell is exactly one of the 2 locked values: `SAFE` / `EXCEPTION`. Rows with Write-Site Classification `EXCEPTION` MUST have Backward-Trace Verdict `EXCEPTION`; rows with Write-Site Classification `written-before-request` OR `unreachable-after-request` MUST have Backward-Trace Verdict `SAFE`."
    - "Per D-11 exactly 22 rows carry an `EXCEPTION` verdict and a populated KI Cross-Ref cell, matching the 237 Consumer Index distribution: EXC-01 = 2 rows (`[KI: \"Non-VRF entropy for affiliate winner roll\"]` — INV-237-005, INV-237-006), EXC-02 = 8 rows (`[KI: \"Gameover prevrandao fallback\"]`), EXC-03 = 4 rows (`[KI: \"Gameover RNG substitution for mid-cycle write-buffer tickets\"]`), EXC-04 = 8 rows (`[KI: \"EntropyLib XOR-shift PRNG for lootbox outcome rolls\"]`). The remaining 124 rows carry `SAFE` + KI Cross-Ref `N/A` (or `[KI: \"Lootbox RNG uses index advance isolation instead of rngLockedFlag\"]` for mid-day-lootbox rows whose only KI linkage is the gating asymmetry — these still carry a SAFE verdict, not EXCEPTION)."
    - "Per D-07/D-08 a `## Backward Adversarial Closure Table` is present with these 6 columns in this exact order: `Row ID | Player | Admin | Validator | VRF Oracle | BWD-03 Verdict`. One row per INV-237-NNN. Each actor cell is one of the 4 locked values: `NO_REACHABLE_PATH` / `PATH_BLOCKED_BY_GATE` / `EXCEPTION (KI: <header>)` / `CANDIDATE_FINDING`. BWD-03 Verdict cell is exactly one of `SAFE` / `EXCEPTION` / `CANDIDATE_FINDING`."
    - "Per D-09/D-10 a `## Prior-Artifact Cross-Cites` section lists every prior-milestone artifact referenced as corroborating evidence (235-03-BWD-TRACE.md for 12 v29.0-delta rows, 215-02-BACKWARD-TRACE.md for 45 confirmed-fresh-matches-prior rows, STORAGE-WRITE-MAP.md for write-site classification, 230-01-DELTA-MAP §1 for v29.0 delta overlap); every cite carries a `re-verified at HEAD 7ab515fe` note with a one-line structural-equivalence statement."
    - "Per D-04 a shared-prefix chain dedup sub-table is present: `## Shared-Prefix Backward-Trace Chains` enumerates the 6 chains from 237-03 Plan (PREFIX-DAILY covering the 91 `daily` path-family rows, PREFIX-MIDDAY covering the 19 `mid-day-lootbox` rows, PREFIX-GAMEOVER covering the 7 `gameover-entropy` rows, PREFIX-PREVRANDAO covering the 8 `exception-prevrandao-fallback` rows, PREFIX-AFFILIATE covering the 2 `exception-non-VRF-seed` rows, PREFIX-GAP covering the 3 `gap-backfill` rows) — body text written once per chain; the Backward Freeze Table's per-row cells cite the chain by name instead of repeating the trace prose."
    - "A `## Gameover-Flow Backward-Freeze Subset` section is present per D-12, enumerating the 19 gameover-flow Row IDs (7 `gameover-entropy` + 2 mid-cycle-substitution gameover-reached + 8 `exception-prevrandao-fallback` + 4 F-29-04 overlap, minus duplicates — matching the 237 Consumer Index GO-01..04 count of 19) and confirming each has a Backward-Trace Verdict in this plan's output (routes to Phase 240 GO-02/GO-03)."
    - "Per D-15 NO `F-30-NN` finding IDs appear anywhere in the file. A `## Finding Candidates` section enumerates rows with verdict `CANDIDATE_FINDING` (first-principles fresh-eyes findings) and rows with verdict `EXCEPTION` (informational — KI-accepted, not new finding); per D-17 severity is `INFO` unless unambiguously higher; Phase 242 FIND-01 owns final ID assignment."
    - "Per D-20 READ-only scope holds: `git diff --name-only 7ab515fe..HEAD -- contracts/ test/` returns empty at task start and end; `git status --porcelain contracts/ test/` returns empty before and after each task."
    - "Per D-19 HEAD anchor `7ab515fe` is locked in frontmatter + echoed in the file's `Audit baseline` line."
    - "Per D-18 `audit/v30-CONSUMER-INVENTORY.md` is NOT edited by this plan — READ-only after Phase 237 commit."
    - "Row-ID integrity: every Backward Freeze Table Row ID also appears in `audit/v30-CONSUMER-INVENTORY.md` Universe List; no orphan Row IDs; no dropped Row IDs. Verifiable via `diff <(grep -Eo 'INV-237-[0-9]{3}' audit/v30-CONSUMER-INVENTORY.md | sort -u) <(grep -Eo 'INV-237-[0-9]{3}' audit/v30-238-01-BWD.md | sort -u)` returning empty."
  artifacts:
    - path: "audit/v30-238-01-BWD.md"
      provides: "BWD-01 + BWD-02 + BWD-03 per-consumer backward freeze proof — 146-row Backward Freeze Table + 146-row Backward Adversarial Closure Table + 6 shared-prefix chains + Gameover-Flow subset + Finding Candidates"
      contains: "## Backward Freeze Table"
  key_links:
    - from: "audit/v30-CONSUMER-INVENTORY.md Universe List (146 INV-237-NNN rows)"
      to: "audit/v30-238-01-BWD.md Backward Freeze Table + Backward Adversarial Closure Table"
      via: "1:1 row-for-row — same Row ID set, no adds, no drops (D-11/D-12 scope is ALL 146 rows)"
      pattern: "INV-237-[0-9]{3}"
    - from: "KNOWN-ISSUES.md 4 RNG exception entries (22 proof-subject rows per 237 Consumer Index EXC-01..04)"
      to: "Backward Freeze Table KI Cross-Ref column + Backward-Trace Verdict `EXCEPTION` cells"
      via: "D-11 KI-accepted violation envelope; exact quoted KI header format"
      pattern: "\\[KI: \""
    - from: "237-03 Plan 6 shared-prefix chains (PREFIX-DAILY / PREFIX-MIDDAY / PREFIX-GAMEOVER / PREFIX-PREVRANDAO / PREFIX-AFFILIATE / PREFIX-GAP)"
      to: "## Shared-Prefix Backward-Trace Chains sub-table"
      via: "D-04 presentation dedup — body text once, row cells cite chain name"
      pattern: "PREFIX-(DAILY|MIDDAY|GAMEOVER|PREVRANDAO|AFFILIATE|GAP)"
    - from: "D-07 4-actor taxonomy (player / admin / validator / VRF oracle)"
      to: "## Backward Adversarial Closure Table 4 actor columns + BWD-03 Verdict column"
      via: "D-08 closed 4-value actor-cell vocabulary"
      pattern: "NO_REACHABLE_PATH|PATH_BLOCKED_BY_GATE|EXCEPTION|CANDIDATE_FINDING"
---

<objective>
BWD-01 + BWD-02 + BWD-03 per-consumer backward freeze proof for ALL 146 VRF-consuming call sites enumerated in `audit/v30-CONSUMER-INVENTORY.md` at HEAD `7ab515fe`. Per CONTEXT.md D-01/D-02, this plan runs in Wave 1 in parallel with Plan 238-02 (FWD-01/02) — the two plans share zero inputs (both consume the 146-row inventory independently) and no file-modification overlap.

**Three requirements satisfied:**
- **BWD-01** — backward trace from each consumption site to VRF request origin; every storage read on the consumption path maps to a write site classified `written-before-request` OR `unreachable-after-request`, OR `EXCEPTION` with an explicit KI citation.
- **BWD-02** — per-consumer enumeration of every storage variable read at consumption time; verdict vocabulary per D-04: `written-before-request` / `unreachable-after-request` / `EXCEPTION (KI-cross-ref)`. NO row carries verdict `mutable-after-request` without a KI citation — `EXCEPTION` is the only escape hatch.
- **BWD-03** — per-consumer adversarial closure over the 4-actor taxonomy locked in D-07 (player / admin / validator / VRF oracle). Each Row × Actor cell records one of the 4 D-08 verdicts: `NO_REACHABLE_PATH` / `PATH_BLOCKED_BY_GATE` / `EXCEPTION (KI: <header>)` / `CANDIDATE_FINDING`.

**Deliverable:** `audit/v30-238-01-BWD.md` — STANDALONE sub-deliverable per D-16. Plan 238-03 merges 238-01 + 238-02 + 238-03 outputs into the final consolidated `audit/v30-FREEZE-PROOF.md` at its own final task. This plan does NOT touch the consolidated file.

**Input contract:** `audit/v30-CONSUMER-INVENTORY.md` (146 INV-237-NNN rows + Consumer Index + Per-Consumer Call Graphs + Classification Summary + KI Cross-Ref Summary). Phase 237 output is READ-only per D-18 — this plan does NOT edit it.

**Scope fidelity (per D-11/D-12):** ALL 146 rows are in scope — no sampling, no shortcuts. The 22 KI-exception proof-subject rows (EXC-01=2, EXC-02=8, EXC-03=4, EXC-04=8) carry `EXCEPTION` verdict with KI cross-ref (Phase 241 EXC-01..04 closes acceptance re-verification; this plan does NOT re-litigate acceptance). The 19 gameover-flow rows (7 `gameover-entropy` + 2 mid-cycle-substitution gameover-reached + 8 `exception-prevrandao-fallback` + 4 F-29-04 overlap minus duplicates) are audited on the same 146-row basis; Phase 240 GO-01..05 layers gameover-jackpot-specific overlay on top.

**Fresh-eyes mandate (per D-09/D-10):** Every BWD verdict is re-derived at HEAD `7ab515fe` from reading `contracts/` directly. Prior-milestone artifacts (235-03-BWD-TRACE.md, 215-02-BACKWARD-TRACE.md, STORAGE-WRITE-MAP.md, 230-01-DELTA-MAP §1, ACCESS-CONTROL-MATRIX.md, 232.1-03-PFTB-AUDIT.md) are CROSS-CITED as corroborating evidence with a `re-verified at HEAD 7ab515fe` note + structural-equivalence statement. No verdict depends on a prior-milestone artifact alone.

**Presentation dedup (per D-04):** The 6 shared-prefix chains from 237-03 Plan (PREFIX-DAILY absorbing 91 rows, PREFIX-MIDDAY 19, PREFIX-GAMEOVER 7, PREFIX-PREVRANDAO 8, PREFIX-AFFILIATE 2, PREFIX-GAP 3 = 130 of 146 rows) are leveraged: body trace text written once per chain in a `## Shared-Prefix Backward-Trace Chains` sub-table; the Backward Freeze Table's per-row cells cite the chain by name. The remaining 16 bespoke-tail rows carry inline trace bodies.

**No F-30-NN emission per D-15.** Finding candidates (rows with verdict `CANDIDATE_FINDING` + informational `EXCEPTION` row list) are collected for Phase 242 FIND-01 intake.

**READ-only scope per D-20.** Only writes: `audit/v30-238-01-BWD.md`. HEAD anchor `7ab515fe` locked per D-19.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/238-backward-forward-freeze-proofs/238-CONTEXT.md
@audit/v30-CONSUMER-INVENTORY.md
@KNOWN-ISSUES.md
</context>

<threat_model>
READ-only audit milestone: no runtime code modified, no test modified, no contracts/ or test/ writes. Attack surface added: NONE. Only writes: markdown under `audit/v30-238-01-BWD.md` (read-only post-commit per D-18). No STRIDE category applies to documentation-only output; threat register empty by construction. Inherits parent phase summary.

Residual risk: an incorrect backward-trace verdict (classifying a `mutable-after-request` storage read as `written-before-request`) propagates a false-SAFE verdict into Phase 238 consolidation and Phase 242 findings-consolidation, masking a real determinism violation. Mitigation: (a) every verdict cross-cites STORAGE-WRITE-MAP.md + 235-03-BWD-TRACE.md where overlap exists, with re-verified-at-HEAD notes making drift detectable; (b) the 6 shared-prefix chains deduplicate backward-trace bodies, so an error in a shared chain is surfaced at 91× / 19× amplification rather than hidden in 91 redundant traces; (c) the 4-actor BWD-03 adversarial closure makes each actor path explicit — actor cells with `NO_REACHABLE_PATH` verdicts MUST cite the gating site (delegatecall boundary, access-control modifier, or state-machine guard); (d) the 22 KI-exception rows carry `EXCEPTION` verdict with exact quoted KI header, so any drift from KNOWN-ISSUES.md acceptance envelope surfaces as a grep-diff against KI headers.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build the 146-row BWD-01/BWD-02 Backward Freeze Table + 6 shared-prefix chains + Gameover-Flow subset → audit/v30-238-01-BWD.md</name>
  <files>audit/v30-238-01-BWD.md</files>
  <read_first>
    - .planning/phases/238-backward-forward-freeze-proofs/238-CONTEXT.md (D-04 Backward Freeze Table shape; D-09/D-10 fresh re-prove + cross-cite; D-11 KI-exception-in-scope; D-12 gameover-flow-in-scope; D-15 no F-30-NN; D-18 prior-artifact READ-only; D-19 HEAD anchor; D-20 READ-only scope)
    - .planning/REQUIREMENTS.md (BWD-01/02 exact wording — locks the verdict vocabulary `written-before-request` / `unreachable-after-request` / `EXCEPTION`; NO `mutable-after-request` without KI citation)
    - audit/v30-CONSUMER-INVENTORY.md — authoritative 146-row Universe List (section `## Universe List` at line 64); Per-Consumer Call Graphs section (line 273 — every BWD-01 backward trace traverses the same path; STOP at consumption per 237 D-11); Shared-Prefix Notes section (line 2081 — the 6 shared-prefix chains); Consumer Index section (line 2310 — `BWD-01 = ALL`, `BWD-02 = ALL`, `BWD-03 = ALL`, `EXC-01..04` = 22 proof subjects, `GO-01..04` = 19 gameover-flow)
    - KNOWN-ISSUES.md — specifically the 4 quoted KI headers used in the `[KI: "..."]` format per D-06/D-11:
        1. `[KI: "Non-VRF entropy for affiliate winner roll"]` — EXC-01 (2 rows: INV-237-005, INV-237-006)
        2. `[KI: "Gameover prevrandao fallback"]` — EXC-02 (8 rows — `exception-prevrandao-fallback` subcategory; cites `DegenerusGameAdvanceModule.sol:1301` + `:1252` + `:109`)
        3. `[KI: "Gameover RNG substitution for mid-cycle write-buffer tickets"]` — EXC-03 (4 rows — `exception-mid-cycle-substitution` subcategory; cites `DegenerusGameAdvanceModule.sol:292` + `:1082` + `:1222-1246`)
        4. `[KI: "EntropyLib XOR-shift PRNG for lootbox outcome rolls"]` — EXC-04 (8 rows — every caller of `EntropyLib.entropyStep()`)
        5. `[KI: "Lootbox RNG uses index advance isolation instead of rngLockedFlag"]` — applied as gating flag on mid-day-lootbox rows; does NOT trigger `EXCEPTION` verdict (not a determinism violation — this is the D-13 `lootbox-index-advance` named gate, a SAFE outcome)
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD 7ab515fe — VRF request origin + fulfillment + gameover entropy + gap backfill (load-bearing):
        * `requestRandomWords(...)` at :1521 — daily VRF request origination site (referenced in 137 of 146 inventory rows' VRF Request Origin column)
        * `requestLootboxRng(...)` at :1088 — mid-day lootbox VRF request origination site (referenced in 19 mid-day-lootbox rows)
        * `rawFulfillRandomWords(...)` at :1690 — VRF callback; SSTORE of `rngWordCurrent` + `rngWordByDay[day]` + `lootboxRngWordByIndex[idx]`
        * `rngGate(...)` at :1141-1185 — rngLockedFlag gate + `rngWordByDay[day]` SLOAD + `rngWordCurrent` SLOAD + `_applyDailyRng` + redemption roll + `_finalizeLootboxRng`
        * `_gameOverEntropy(...)` at :1222-1246 — gameover VRF word derivation (normal VRF-available branch)
        * `_getHistoricalRngFallback(...)` at :1301 — prevrandao KI-exception path (KI EXC-02 gate at :1252 + 14-day delay at :109)
        * `_swapAndFreeze(purchaseLevel)` at :292 — daily write-buffer swap (KI EXC-03 site 1)
        * `_swapTicketSlot(purchaseLevel_)` at :1082 — mid-day write-buffer swap (KI EXC-03 site 2)
        * `_finalizeRngRequest` + `_finalizeLootboxRng` + `_backfillGapDays` — intermediate touchpoints
    - contracts/modules/DegenerusGameJackpotModule.sol at HEAD 7ab515fe — 16 hash2/keccak consumer sites (daily path entropy-passthrough); intermediate SLOAD of `rngWord` parameter (never a storage read — param is VRF word passed through `rngGate`)
    - contracts/modules/DegenerusGameLootboxModule.sol at HEAD 7ab515fe — mid-day-lootbox consumer chain: `lootboxRngWordByIndex[idx]` SLOAD at consumption time; index-advance isolation sites
    - contracts/modules/DegenerusGameMintModule.sol at HEAD 7ab515fe — entropy passthrough: `processFutureTicketBatch(uint24 lvl, uint256 entropy)` receiver, `_prepareFutureTickets`, `_raritySymbolBatch`, `_rollRemainder`
    - contracts/modules/DegenerusGameGameOverModule.sol at HEAD 7ab515fe — gameover consumer chain
    - contracts/modules/DegenerusGameBoonModule.sol + DegenerusGameDecimatorModule.sol + DegenerusGameDegeneretteModule.sol + DegenerusGameWhaleModule.sol + DegenerusGameMintStreakUtils.sol + DegenerusGamePayoutUtils.sol at HEAD 7ab515fe — auxiliary consumer chains
    - contracts/DegenerusGame.sol at HEAD 7ab515fe — delegatecall boundaries (IM-10..IM-16 per 230-01-DELTA-MAP §2)
    - contracts/DegenerusAffiliate.sol at HEAD 7ab515fe — EXC-01 affiliate winner roll non-VRF seed (INV-237-005 at :568, INV-237-006 at :585)
    - contracts/BurnieCoinflip.sol at HEAD 7ab515fe — INV-237-001..004 (daily coinflip payout consumers)
    - contracts/DegenerusQuests.sol at HEAD 7ab515fe — daily quest RNG consumers
    - contracts/storage/DegenerusGameStorage.sol at HEAD 7ab515fe — storage slot layout for `rngWordCurrent`, `rngWordByDay`, `lootboxRngWordByIndex`, `rngLockedFlag`, `midDayTicketRngPending`, `vrfRequestId`, `totalFlipReversals`, `phaseTransitionActive`, `prizePoolsPacked`, `claimableWinnings`, `decimatorPool`, `earlybirdDgnrsPoolStart`, `futureTicketsByDay`, `bonusTraitsPacked`, `traitAlignment`
    - contracts/libraries/EntropyLib.sol + BitPackingLib.sol + JackpotBucketLib.sol at HEAD 7ab515fe — library-function storage read surface (library calls are NOT rows per 237 D-02 but their internal SLOADs are backward-trace steps)
    - STORAGE-WRITE-MAP.md at HEAD 7ab515fe (repo-root audit artifact) — write-site classification corroboration for every storage slot
    - .planning/milestones/v29.0-phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1 Delta Summary — structural corroboration for v29.0-delta overlap rows (the 12 new-since-prior-audit rows per 237-01 reconciliation)
    - .planning/milestones/v29.0-phases/235-conservation-rng-commitment-re-proof-phase-transition/235-03-AUDIT.md (per-consumer backward-trace template — the 5-category RNG-01 scope; cross-cite source for the 12 v29.0-delta rows per D-10)
    - .planning/milestones/v25.0-phases/215-rng-fresh-eyes/215-02-BACKWARD-TRACE.md (v25.0 per-consumer backward-trace table — cross-cite source for the 45 confirmed-fresh-matches-prior rows per D-10)
    - .planning/phases/237-vrf-consumer-inventory-call-graph/237-03-SUMMARY.md — 6 shared-prefix chains definition (PREFIX-DAILY 91 rows, PREFIX-MIDDAY 19, PREFIX-GAMEOVER 7, PREFIX-PREVRANDAO 8, PREFIX-AFFILIATE 2, PREFIX-GAP 3); loaded into this plan's shared-prefix-chain presentation
  </read_first>
  <action>
Execute the BWD-01 + BWD-02 backward freeze proof for all 146 VRF-consumer rows per CONTEXT.md D-04/D-11/D-12.

**Verify prerequisites first:**
```
test -f audit/v30-CONSUMER-INVENTORY.md
[ "$(grep -c '^| INV-237-' audit/v30-CONSUMER-INVENTORY.md)" = "146" ]   # MUST equal 146
git log --oneline -1 -- audit/v30-CONSUMER-INVENTORY.md   # MUST show Phase 237 commit
git diff --name-only 7ab515fe..HEAD -- contracts/ test/   # MUST be empty (D-19/D-20)
git status --porcelain contracts/ test/   # MUST be empty (D-20)
```
If ANY check fails, STOP — the plan cannot proceed without the Phase 237 inventory at HEAD 7ab515fe.

**Step 1 — Parse the 146-row Universe List from `audit/v30-CONSUMER-INVENTORY.md`.** Extract: Row ID, Consumer Function, Consumption File:Line, Path Family, Subcategory, VRF Request Origin File:Line, Fulfillment Site File:Line, KI Cross-Ref, Notes. Every row in Universe List becomes a row in this plan's Backward Freeze Table (1:1 mapping, no adds, no drops).

**Step 2 — Parse the 6 shared-prefix chains from `audit/v30-CONSUMER-INVENTORY.md` `## Shared-Prefix Notes` section.** Extract each chain's member Row IDs + prefix body. Confirmed chain names + coverage counts (per 237-03 Plan):

| Chain Name | Path Family Covered | Row Count | Representative Row |
|---|---|---|---|
| PREFIX-DAILY | `daily` | 91 | INV-237-001 (BurnieCoinflip.sol:808) |
| PREFIX-MIDDAY | `mid-day-lootbox` | 19 | INV-237-021 (DegenerusGameAdvanceModule.sol:204) |
| PREFIX-GAMEOVER | `gameover-entropy` | 7 | (gameover winner selection + terminal drain) |
| PREFIX-PREVRANDAO | `other / exception-prevrandao-fallback` | 8 | (EXC-02 rows — `_getHistoricalRngFallback` callers) |
| PREFIX-AFFILIATE | `other / exception-non-VRF-seed` | 2 | INV-237-005, INV-237-006 |
| PREFIX-GAP | `gap-backfill` | 3 | (`_backfillGapDays` consumers) |

The 6 chains cover 91 + 19 + 7 + 8 + 2 + 3 = 130 of 146 rows. The remaining 16 rows have bespoke backward-trace tails (likely including `other / library-wrapper`, `other / request-origination`, `other / fulfillment-callback`, `other / view-deterministic-fallback`, `other / exception-mid-cycle-substitution` non-gameover sites — enumerate by grep on the Universe List's Subcategory column once the inventory is parsed).

**Step 3 — For each of the 6 shared-prefix chains, construct the shared backward-trace body.** Each shared-prefix backward trace is a tabular body with the columns: Step | File:Line | Storage Variable Read | Write Site (file:line) | Write-Site Classification. Example prose sketch for PREFIX-DAILY (executor re-derives at HEAD):

```
### PREFIX-DAILY — daily path-family shared backward trace (91 rows)

**Backward-trace body (same for all 91 member rows up to the consumption-site bifurcation):**

| Step | File:Line | Storage Variable Read | Write Site | Write-Site Classification |
|---|---|---|---|---|
| 1 | rngGate → AdvanceModule:1143 | `rngWordCurrent` | AdvanceModule:1702 (rawFulfillRandomWords SSTORE) | written-before-request |
| 2 | rngGate → AdvanceModule:1141 | `rngLockedFlag` | AdvanceModule:<requestRandomWords setter line> (set at request time) → cleared at rawFulfillRandomWords :<line> | written-before-request (set at request) |
| 3 | advanceGame → AdvanceModule:<line> | `vrfRequestId` | AdvanceModule:<requestRandomWords setter line> | written-before-request |
| 4 | Member-row tail varies — cite per Row ID in Backward Freeze Table |

Per STORAGE-WRITE-MAP.md at HEAD 7ab515fe: `rngWordCurrent` write sites limited to `rawFulfillRandomWords` (VRF callback) + `_finalizeRngRequest` (finalization). Neither is player-reachable between VRF request and fulfillment — Oracle-only. Verdict: `written-before-request` (the "before" being before the request was fulfilled with the current word; the SSTORE at fulfillment IS the request's commitment that binds the word being read at consumption).

Cross-cite: 235-03-AUDIT.md Category 1-2 (daily path backward-trace); 215-02-BACKWARD-TRACE.md §1 (v25.0 daily path). Re-verified at HEAD 7ab515fe — structural equivalence: `rngGate` function body unchanged, `rngWordCurrent` SSTORE sites unchanged.
```

Each of the 6 chains gets an analogous body. The PREFIX-AFFILIATE body will be degenerate (KI EXC-01 non-VRF seed — SLOAD of `block.timestamp` or packed counter; no VRF word; Backward-Trace Verdict for all 2 member rows is `EXCEPTION`).

**Step 4 — For each of the 146 Universe List rows, write one row in the `## Backward Freeze Table`.** Columns in this exact order (per D-04):

```
| Row ID | Consumer | Consumption File:Line | Storage Reads On Consumption Path | Write-Site Classification | KI Cross-Ref | Backward-Trace Verdict |
```

Row construction rules:
- **Row ID** — copy from inventory (e.g. `INV-237-001`).
- **Consumer** — copy from inventory Consumer Function column (e.g. `processCoinflipPayouts`).
- **Consumption File:Line** — copy from inventory Consumption File:Line column (e.g. `contracts/BurnieCoinflip.sol:808`).
- **Storage Reads On Consumption Path** — for shared-prefix-chain member rows, cite the chain name (e.g. `PREFIX-DAILY + tail: keccak(rngWord, epoch) seed at :808`). For bespoke-tail rows (~16 rows), enumerate inline: each distinct SLOAD on the path from request to consumption (e.g. `rngWordByDay[day] @ :1143; rngWordCurrent @ :1143; tail-specific read @ <consumer-site>`).
- **Write-Site Classification** — exactly one of `written-before-request` / `unreachable-after-request` / `EXCEPTION`. Rules per D-04 + BWD-02 wording:
    - `written-before-request` — the SLOAD target's only SSTORE sites executed before the VRF request was fired (Oracle-only writes in `rawFulfillRandomWords`; state frozen by `rngLockedFlag` guard; state set at request time and not cleared before consumption).
    - `unreachable-after-request` — the SLOAD target's SSTORE sites exist but are gated by `rngLockedFlag` (revert path) or semantic-path-gate (e.g. post-finalize one-way transition) so they cannot execute between request and consumption.
    - `EXCEPTION` — KI-accepted violation envelope (the 22 rows). MUST have populated KI Cross-Ref cell.
- **KI Cross-Ref** — exactly one of:
    - `N/A` for rows with no KI linkage (124 rows).
    - `[KI: "Non-VRF entropy for affiliate winner roll"]` for INV-237-005 + INV-237-006 (EXC-01 — 2 rows).
    - `[KI: "Gameover prevrandao fallback"]` for the 8 `exception-prevrandao-fallback` subcategory rows (EXC-02).
    - `[KI: "Gameover RNG substitution for mid-cycle write-buffer tickets"]` for the 4 `exception-mid-cycle-substitution` subcategory rows (EXC-03 — includes INV-237-024, INV-237-045, plus 2 more per inventory).
    - `[KI: "EntropyLib XOR-shift PRNG for lootbox outcome rolls"]` for the 8 `EntropyLib.entropyStep()` caller rows (EXC-04).
    - `[KI: "Lootbox RNG uses index advance isolation instead of rngLockedFlag"]` MAY appear on mid-day-lootbox rows as a supplementary gating annotation (e.g. INV-237-021); these rows still carry `SAFE` verdict because index-advance isolation is a SAFE gate (D-13 `lootbox-index-advance` named gate), NOT a determinism violation.
- **Backward-Trace Verdict** — exactly one of `SAFE` / `EXCEPTION`. Rule: if Write-Site Classification = `EXCEPTION`, verdict = `EXCEPTION`; otherwise verdict = `SAFE`. The 22 EXC proof-subject rows carry `EXCEPTION`; the remaining 124 rows carry `SAFE`.

**EXC row distribution check (enforced — MUST match 237-02 Plan SUMMARY):**
- EXC-01 (affiliate) = 2 EXCEPTION rows — INV-237-005, INV-237-006
- EXC-02 (prevrandao) = 8 EXCEPTION rows — subcategory `exception-prevrandao-fallback`
- EXC-03 (F-29-04) = 4 EXCEPTION rows — subcategory `exception-mid-cycle-substitution`
- EXC-04 (EntropyLib) = 8 EXCEPTION rows — EntropyLib.entropyStep() caller sites
- **Total EXCEPTION rows = 22** (124 SAFE + 22 EXCEPTION = 146)

If the executor's derived count diverges from 22/124, STOP and record a scope-guard deferral per D-18 (inventory may contain an EXC-row mis-assignment). Do NOT adjust — flag for Phase 242 FIND-01.

**Step 5 — Write `audit/v30-238-01-BWD.md` with EXACTLY these sections in this order:**

```markdown
# v30.0 Phase 238 Plan 01 — BWD-01/02 Per-Consumer Backward Freeze Proof

**Audit baseline:** HEAD `7ab515fe` (contract tree identical to v29.0 `1646d5af`; post-v29 commits are docs-only per D-19).
**Requirements addressed:** BWD-01 (per-consumer backward trace), BWD-02 (per-consumer storage-read enumeration with verdict vocabulary `written-before-request` / `unreachable-after-request` / `EXCEPTION`), BWD-03 (per-consumer 4-actor adversarial closure).
**Scope:** ALL 146 INV-237-NNN rows from `audit/v30-CONSUMER-INVENTORY.md` Universe List — no sampling (per D-11).
**KI handling (per D-11):** 22 EXCEPTION rows (EXC-01=2, EXC-02=8, EXC-03=4, EXC-04=8); KI acceptance NOT re-litigated (Phase 241 scope).
**Gameover scope (per D-12):** 19 gameover-flow rows audited on same 146-row basis; Phase 240 GO-01..05 overlays gameover-jackpot-specific proofs.
**Presentation dedup (per D-04):** 6 shared-prefix chains from 237-03 cover 130/146 rows; body written once, row cells cite chain name.
**Fresh-eyes re-derivation (per D-09/D-10):** Every verdict re-derived at HEAD 7ab515fe. Prior-milestone artifacts CROSS-CITED with `re-verified at HEAD 7ab515fe` note.
**Finding-ID emission (per D-15):** NO F-30-NN. Candidate pool routes to Phase 242 FIND-01.
**READ-only scope (per D-20):** zero contracts/ test/ writes; zero edits to `audit/v30-CONSUMER-INVENTORY.md`.

## Table of Contents

1. [Shared-Prefix Backward-Trace Chains](#shared-prefix-backward-trace-chains)
2. [Backward Freeze Table](#backward-freeze-table) — BWD-01 + BWD-02 (146 rows)
3. [Backward Adversarial Closure Table](#backward-adversarial-closure-table) — BWD-03 (146 rows × 4 actors)
4. [Gameover-Flow Backward-Freeze Subset](#gameover-flow-backward-freeze-subset) — D-12 Phase 240 hand-off (19 rows)
5. [Prior-Artifact Cross-Cites](#prior-artifact-cross-cites) — D-09/D-10
6. [Finding Candidates](#finding-candidates) — D-15/D-17
7. [Scope-Guard Deferrals](#scope-guard-deferrals) — D-18
8. [Attestation](#attestation)

## Shared-Prefix Backward-Trace Chains

[Per D-04. 6 chains from 237-03 Plan: PREFIX-DAILY (91 rows), PREFIX-MIDDAY (19 rows), PREFIX-GAMEOVER (7 rows), PREFIX-PREVRANDAO (8 rows), PREFIX-AFFILIATE (2 rows), PREFIX-GAP (3 rows). Each chain gets a subsection with: member Row IDs list; tabular backward-trace body (Step | File:Line | Storage Variable Read | Write Site | Write-Site Classification); one-line verdict summary for the chain (e.g. "PREFIX-DAILY chain: all 91 member rows carry Backward-Trace Verdict SAFE — every storage read maps to rawFulfillRandomWords SSTORE pre-consumption under the rngLockedFlag guard, re-verified at HEAD 7ab515fe"). The PREFIX-AFFILIATE subsection explicitly marks the chain-level verdict as EXCEPTION (KI: "Non-VRF entropy for affiliate winner roll") since all 2 member rows are KI EXC-01.]

## Backward Freeze Table

**One row per INV-237-NNN (146 rows total).** Columns per D-04:

| Row ID | Consumer | Consumption File:Line | Storage Reads On Consumption Path | Write-Site Classification | KI Cross-Ref | Backward-Trace Verdict |
|---|---|---|---|---|---|---|
| INV-237-001 | processCoinflipPayouts | contracts/BurnieCoinflip.sol:808 | PREFIX-DAILY + tail: keccak(rngWord, epoch) seed @ :808 | written-before-request | N/A | SAFE |
| INV-237-002 | ... | ... | ... | ... | ... | ... |
| ... | ... | ... | ... | ... | ... | ... |
| INV-237-146 | ... | ... | ... | ... | ... | ... |

[Every Row ID from `audit/v30-CONSUMER-INVENTORY.md` Universe List gets exactly one row. Sort by Row ID ascending. No placeholder tokens <line>, <path>, <fn>, TBD-*. No F-30-NN IDs.]

## Backward Adversarial Closure Table

**BWD-03: 4-actor adversarial closure per D-07 — one row per INV-237-NNN; 4 actor columns + verdict.**

Actor taxonomy (per D-07):
- **player** — any EOA with standard game access (purchase, claim, burn, boon, quest, admin-on-own-tokens); covers direct calls, delegation, MEV bundle inclusion.
- **admin** — any admin role (multisig, deployer, governance, operator); covers setters, rescue, ownership transfers, every `onlyAdmin`-gated path.
- **validator** — block proposer / miner; covers transaction ordering, censorship, short-range reorgs within the commitment window.
- **VRF oracle** — Chainlink VRF coordinator; can delay/withhold fulfillment but cannot bias the word (accepted Chainlink trust model). The 14-day prevrandao fallback (KI EXC-02) is the documented escape hatch for indefinite withholding.

Actor-cell verdict vocabulary (per D-08, closed 4-value set):
- `NO_REACHABLE_PATH` — actor cannot mutate any backward-input state between request and consumption.
- `PATH_BLOCKED_BY_GATE` — actor path exists but gate blocks it; MUST name the gate (one of D-13 four values: `rngLocked` / `lootbox-index-advance` / `phase-transition-gate` / `semantic-path-gate`).
- `EXCEPTION (KI: <header>)` — accepted violation per KNOWN-ISSUES.md.
- `CANDIDATE_FINDING` — actor path exists + gate absent or ineffective — route to Phase 242 FIND-01; MUST produce a Finding Candidate block in the `## Finding Candidates` section.

| Row ID | Player | Admin | Validator | VRF Oracle | BWD-03 Verdict |
|---|---|---|---|---|---|
| INV-237-001 | NO_REACHABLE_PATH | PATH_BLOCKED_BY_GATE (rngLocked) | NO_REACHABLE_PATH (rngLocked holds across reorg) | NO_REACHABLE_PATH | SAFE |
| INV-237-002 | ... | ... | ... | ... | ... |
| ... | ... | ... | ... | ... | ... |
| INV-237-146 | ... | ... | ... | ... | ... |

[Every Row ID from the Backward Freeze Table gets exactly one row. BWD-03 Verdict column: `SAFE` if all 4 actor cells are `NO_REACHABLE_PATH` or `PATH_BLOCKED_BY_GATE`; `EXCEPTION` if any actor cell is `EXCEPTION`; `CANDIDATE_FINDING` if any actor cell is `CANDIDATE_FINDING`.]

## Gameover-Flow Backward-Freeze Subset

**Per D-12 — Phase 240 GO-01..05 hand-off.** The 19 gameover-flow Row IDs (per 237 Consumer Index GO-01..04 count: 7 `gameover-entropy` + 2 mid-cycle-substitution gameover-reached + 8 `exception-prevrandao-fallback` + 4 F-29-04 overlap minus duplicates = 19) with their Backward-Trace Verdict + KI Cross-Ref for Phase 240 intake:

| Row ID | Path Family + Subcategory | Backward-Trace Verdict | KI Cross-Ref |
|---|---|---|---|
| INV-237-NNN | gameover-entropy | SAFE | N/A |
| INV-237-NNN | other / exception-prevrandao-fallback | EXCEPTION | [KI: "Gameover prevrandao fallback"] |
| INV-237-NNN | other / exception-mid-cycle-substitution | EXCEPTION | [KI: "Gameover RNG substitution for mid-cycle write-buffer tickets"] |
| ... | ... | ... | ... |

Total gameover-flow rows: exactly 19. SAFE subset = 7 (pure gameover-entropy); EXCEPTION subset = 12 (8 prevrandao + 4 F-29-04).

## Prior-Artifact Cross-Cites

Per D-09/D-10 — every prior-phase cite carries a `re-verified at HEAD 7ab515fe` note + one-line structural-equivalence statement.

| Prior Artifact | Rows Covered (approx) | Cross-Cite Relationship | Re-Verified Statement |
|---|---|---|---|
| `.planning/milestones/v29.0-phases/235-*/235-03-AUDIT.md` (RNG-01 backward-trace, 5 consumer categories) | 12 v29.0-delta rows per 237-01 Reconciliation `new-since-prior-audit` verdict | Category 1-5 per-consumer backward-trace template | re-verified at HEAD 7ab515fe — structural equivalence: rngGate body unchanged from `1646d5af`; rngWordCurrent SSTORE sites unchanged |
| `.planning/milestones/v25.0-phases/215-rng-fresh-eyes/215-02-BACKWARD-TRACE.md` | 45 `confirmed-fresh-matches-prior` rows per 237-01 Reconciliation | v25.0 per-consumer backward-trace table format | re-verified at HEAD 7ab515fe — structural equivalence: contract tree unchanged since v5.0 contract-delta baseline; storage read sites unchanged |
| STORAGE-WRITE-MAP.md (repo-root) | ALL 146 rows (write-site classification corroboration) | Every storage slot SSTORE site enumerated; backward-trace classification cross-checked | re-verified at HEAD 7ab515fe — map generated against contract tree at HEAD; zero SSTORE sites added since v29.0 |
| `.planning/milestones/v29.0-phases/230-*/230-01-DELTA-MAP.md` §1 Delta Summary | 12 v29.0-delta rows | Structural corroboration for `new-since-prior-audit` rows | re-verified at HEAD 7ab515fe — 230-01 delta map covers v27→v29.0 deltas; contract tree identical post-v29 (docs-only commits) |

## Finding Candidates

Per D-15/D-17. Informational `EXCEPTION` rows (KI-accepted — not new findings) and first-principles `CANDIDATE_FINDING` rows (fresh-eyes discoveries for Phase 242 intake). Severity INFO per D-17 unless unambiguously higher. NO F-30-NN IDs.

Format for each candidate:
```
- **[CANDIDATE_FINDING | EXCEPTION]** INV-237-NNN — contracts/<path>:<line> — <fn> — <rationale> — severity: <INFO|LOW|MEDIUM|HIGH>
```

Expected EXCEPTION entries (22 — informational, KI-accepted):
- EXC-01 — INV-237-005, INV-237-006 (affiliate winner roll non-VRF seed)
- EXC-02 — 8 rows (gameover prevrandao fallback)
- EXC-03 — 4 rows (F-29-04 mid-cycle write-buffer substitution)
- EXC-04 — 8 rows (EntropyLib.entropyStep() XOR-shift)

Expected CANDIDATE_FINDING entries: 0 expected (if any surface during fresh re-derivation, enumerate them; otherwise write "No CANDIDATE_FINDING entries surfaced during BWD-01/02/03 re-derivation.").

## Scope-Guard Deferrals

Per D-18 — if any row's backward trace requires editing `audit/v30-CONSUMER-INVENTORY.md` (e.g., a new SLOAD not in the 237-03 Per-Consumer Call Graph, an EXC row count that diverges from the 237 Consumer Index 22 total), record the deferral here without editing Phase 237 output. Route to Phase 242 FIND-01. If none surface, write "None surfaced during backward-freeze re-derivation."

## Attestation

- Audit baseline HEAD: `7ab515fe`
- `git diff --name-only 7ab515fe..HEAD -- contracts/ test/` returned empty at task start and end
- `git status --porcelain contracts/ test/` returned empty before and after this task
- Input file `audit/v30-CONSUMER-INVENTORY.md` was present and committed before this task started; row count = 146
- Input Row ID integrity: this file contains a Backward Freeze Table entry for every INV-237-NNN Row ID in the inventory (row count = 146)
- EXCEPTION row distribution: EXC-01=2, EXC-02=8, EXC-03=4, EXC-04=8, total=22 (matches 237-02 Plan SUMMARY)
- Gameover-flow subset: 19 rows (matches 237 Consumer Index GO-01..04)
- Shared-prefix chains: 6 chains covering 130/146 rows (matches 237-03 Plan)
- Contract source reads limited to `contracts/**/*.sol` at HEAD `7ab515fe`
- `audit/v30-CONSUMER-INVENTORY.md` NOT modified by this task (D-18 enforcement — READ-only after Phase 237 commit)
- Prior-milestone artifacts CROSS-CITED only (not relied upon per D-09); every cite carries `re-verified at HEAD 7ab515fe` note
- Zero F-30-NN IDs (D-15 enforcement)
```

Every Backward Freeze Table row MUST:
- Have Row ID matching `INV-237-[0-9]{3}` regex AND appearing in `audit/v30-CONSUMER-INVENTORY.md` Universe List
- Have Write-Site Classification exactly one of: `written-before-request`, `unreachable-after-request`, `EXCEPTION`
- Have Backward-Trace Verdict exactly one of: `SAFE`, `EXCEPTION`
- Have KI Cross-Ref either `N/A` or exactly one of the 5 quoted KI headers (per D-06/D-11)
- Never contain `mutable-after-request` (BWD-02 wording forbids without KI citation)
- Never contain F-30-NN references (D-15)
- Never contain placeholder tokens `<path>`, `<line>`, `<fn`, `<family>`, `TBD-*`

Every Backward Adversarial Closure Table row MUST:
- Have actor-cell verdict exactly one of: `NO_REACHABLE_PATH`, `PATH_BLOCKED_BY_GATE (<gate-name>)`, `EXCEPTION (KI: <header>)`, `CANDIDATE_FINDING`
- PATH_BLOCKED_BY_GATE cells MUST name the gate (one of 4 D-13 values)
- BWD-03 Verdict cell exactly one of: `SAFE`, `EXCEPTION`, `CANDIDATE_FINDING`

READ-only per D-20. Only writes: `audit/v30-238-01-BWD.md`. `git status --porcelain contracts/ test/` MUST be empty after the write. `audit/v30-CONSUMER-INVENTORY.md` MUST remain unmodified.
  </action>
  <verify>
    <automated>test -f audit/v30-238-01-BWD.md && grep -q "^## Shared-Prefix Backward-Trace Chains" audit/v30-238-01-BWD.md && grep -q "^## Backward Freeze Table" audit/v30-238-01-BWD.md && grep -q "^## Backward Adversarial Closure Table" audit/v30-238-01-BWD.md && grep -q "^## Gameover-Flow Backward-Freeze Subset" audit/v30-238-01-BWD.md && grep -q "^## Prior-Artifact Cross-Cites" audit/v30-238-01-BWD.md && grep -q "^## Finding Candidates" audit/v30-238-01-BWD.md && grep -q "^## Scope-Guard Deferrals" audit/v30-238-01-BWD.md && grep -q "^## Attestation" audit/v30-238-01-BWD.md && [ "$(grep -c '^| INV-237-' audit/v30-238-01-BWD.md)" -ge 146 ] && [ "$(grep -Eo 'INV-237-[0-9]{3}' audit/v30-238-01-BWD.md | sort -u | wc -l)" = "146" ] && [ "$(grep -c 'Non-VRF entropy for affiliate winner roll' audit/v30-238-01-BWD.md)" -ge 2 ] && [ "$(grep -c 'Gameover prevrandao fallback' audit/v30-238-01-BWD.md)" -ge 8 ] && [ "$(grep -c 'Gameover RNG substitution for mid-cycle write-buffer tickets' audit/v30-238-01-BWD.md)" -ge 4 ] && [ "$(grep -c 'EntropyLib XOR-shift PRNG' audit/v30-238-01-BWD.md)" -ge 8 ] && grep -q "PREFIX-DAILY" audit/v30-238-01-BWD.md && grep -q "PREFIX-MIDDAY" audit/v30-238-01-BWD.md && grep -q "PREFIX-GAMEOVER" audit/v30-238-01-BWD.md && grep -q "PREFIX-PREVRANDAO" audit/v30-238-01-BWD.md && grep -q "PREFIX-AFFILIATE" audit/v30-238-01-BWD.md && grep -q "PREFIX-GAP" audit/v30-238-01-BWD.md && ! grep -q "mutable-after-request" audit/v30-238-01-BWD.md && ! grep -qE "F-30-[0-9]+" audit/v30-238-01-BWD.md && ! grep -q "<line>" audit/v30-238-01-BWD.md && ! grep -q "<path>" audit/v30-238-01-BWD.md && ! grep -q "TBD-" audit/v30-238-01-BWD.md && grep -q "7ab515fe" audit/v30-238-01-BWD.md && grep -q "re-verified at HEAD 7ab515fe" audit/v30-238-01-BWD.md && grep -q "NO_REACHABLE_PATH" audit/v30-238-01-BWD.md && grep -q "PATH_BLOCKED_BY_GATE" audit/v30-238-01-BWD.md && test -z "$(git status --porcelain contracts/ test/)" && test -z "$(git status --porcelain audit/v30-CONSUMER-INVENTORY.md 2>/dev/null)"</automated>
  </verify>
  <acceptance_criteria>
    - File `audit/v30-238-01-BWD.md` exists
    - File contains 8 literal section headers in this order: `## Shared-Prefix Backward-Trace Chains`, `## Backward Freeze Table`, `## Backward Adversarial Closure Table`, `## Gameover-Flow Backward-Freeze Subset`, `## Prior-Artifact Cross-Cites`, `## Finding Candidates`, `## Scope-Guard Deferrals`, `## Attestation`
    - Backward Freeze Table has ≥ 146 INV-237-NNN Row ID lines (`grep -c '^| INV-237-' audit/v30-238-01-BWD.md` ≥ 146)
    - Distinct INV-237-NNN IDs in file = exactly 146 (`grep -Eo 'INV-237-[0-9]{3}' audit/v30-238-01-BWD.md | sort -u | wc -l` = 146)
    - Every KI Cross-Ref cell is `N/A` or one of the 5 locked quoted KI headers
    - EXC distribution grep counts: `Non-VRF entropy for affiliate winner roll` ≥ 2 (EXC-01); `Gameover prevrandao fallback` ≥ 8 (EXC-02); `Gameover RNG substitution for mid-cycle write-buffer tickets` ≥ 4 (EXC-03); `EntropyLib XOR-shift PRNG` ≥ 8 (EXC-04)
    - All 6 shared-prefix chain names appear: `PREFIX-DAILY`, `PREFIX-MIDDAY`, `PREFIX-GAMEOVER`, `PREFIX-PREVRANDAO`, `PREFIX-AFFILIATE`, `PREFIX-GAP`
    - File does NOT contain `mutable-after-request` (BWD-02 wording enforcement — only the 3 locked Write-Site Classification values)
    - File does NOT contain `F-30-NN` references (D-15)
    - File does NOT contain placeholder tokens `<line>`, `<path>`, `<fn`, `TBD-`
    - File contains literal `7ab515fe` in the Audit baseline line
    - File contains literal `re-verified at HEAD 7ab515fe` (D-09/D-10 enforcement — at least one cross-cite)
    - Backward Adversarial Closure Table vocabulary present: `NO_REACHABLE_PATH` AND `PATH_BLOCKED_BY_GATE` appear
    - `audit/v30-CONSUMER-INVENTORY.md` NOT edited (D-18 enforcement — READ-only after Phase 237)
    - `git status --porcelain contracts/ test/` returns empty
    - `git diff --name-only 7ab515fe..HEAD -- contracts/ test/` returns empty
  </acceptance_criteria>
  <deliverable>
`audit/v30-238-01-BWD.md` — STANDALONE BWD-01/02/03 deliverable. 146-row Backward Freeze Table + 146-row Backward Adversarial Closure Table + 6 shared-prefix chains + 19-row Gameover-Flow subset + Prior-Artifact Cross-Cites + Finding Candidates + Scope-Guard Deferrals + Attestation. 22 EXCEPTION rows matching 237-02 EXC distribution. Zero F-30-NN IDs. Zero contracts/ test/ writes. Zero edits to Phase 237 output.
  </deliverable>
  <done>File exists; 8 required sections present; 146 distinct INV-237-NNN Row IDs; 22 EXCEPTION rows matching EXC-01=2/EXC-02=8/EXC-03=4/EXC-04=8 distribution; all 6 shared-prefix chains named; no mutable-after-request occurrences; no F-30-NN; no placeholder tokens; HEAD anchor echoed; cross-cites carry re-verified-at-HEAD note; `audit/v30-CONSUMER-INVENTORY.md` unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Commit audit/v30-238-01-BWD.md</name>
  <files>audit/v30-238-01-BWD.md</files>
  <read_first>
    - audit/v30-238-01-BWD.md (must exist from Task 1)
  </read_first>
  <action>
Stage and commit `audit/v30-238-01-BWD.md` only. Use `git add` with explicit path — do NOT `git add .`.

```
git add audit/v30-238-01-BWD.md
git commit -m "docs(238-01): BWD-01/02/03 per-consumer backward freeze proof at HEAD 7ab515fe

146-row Backward Freeze Table (one per INV-237-NNN from audit/v30-CONSUMER-
INVENTORY.md) + 146-row Backward Adversarial Closure Table (4-actor
taxonomy per D-07: player / admin / validator / VRF oracle) + 19-row
Gameover-Flow subset (Phase 240 GO-01..05 hand-off per D-12).

Write-Site Classification vocabulary (per D-04/BWD-02): written-before-
request / unreachable-after-request / EXCEPTION. NO row carries
mutable-after-request — EXCEPTION is the only escape hatch.

EXCEPTION row count: 22 — matches 237-02 Plan SUMMARY EXC-01=2 /
EXC-02=8 / EXC-03=4 / EXC-04=8 distribution.

Actor-cell vocabulary (per D-08): NO_REACHABLE_PATH / PATH_BLOCKED_BY_GATE
(+ gate name from D-13 4-value taxonomy) / EXCEPTION (KI: header) /
CANDIDATE_FINDING.

Presentation dedup (per D-04): 6 shared-prefix chains from 237-03 cover
130/146 rows — body written once per chain; Backward Freeze Table cells
cite chain name.

Prior-milestone artifacts CROSS-CITED with re-verified-at-HEAD-7ab515fe
notes (per D-09/D-10): 235-03-AUDIT.md, 215-02-BACKWARD-TRACE.md,
STORAGE-WRITE-MAP.md, 230-01-DELTA-MAP §1.

NO F-30-NN IDs (D-15 — Phase 242 owns). READ-only per D-20 — zero
contracts/ test/ writes; audit/v30-CONSUMER-INVENTORY.md unmodified (D-18).

HEAD anchor 7ab515fe (D-19).

Wave 1 parallel with Plan 238-02 (FWD-01/02)."
```

Do NOT push.
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -qE "238-01.*(BWD|backward freeze).*7ab515fe" && test -z "$(git status --porcelain contracts/ test/)" && test -z "$(git status --porcelain audit/v30-CONSUMER-INVENTORY.md 2>/dev/null)"</automated>
  </verify>
  <acceptance_criteria>
    - One new commit with subject containing `238-01` + (`BWD` OR `backward freeze`) + `7ab515fe`
    - Commit touches only `audit/v30-238-01-BWD.md`
    - `git status --porcelain contracts/ test/` empty
    - `git status --porcelain audit/v30-CONSUMER-INVENTORY.md` empty (Phase 237 output untouched)
    - Not pushed
  </acceptance_criteria>
  <deliverable>`audit/v30-238-01-BWD.md` committed; local-only; Wave 1 deliverable ready for Plan 238-03 consolidation in Wave 2.</deliverable>
  <done>Commit exists; contracts/ test/ unmodified; `audit/v30-CONSUMER-INVENTORY.md` unmodified; BWD-01/02/03 deliverable committed.</done>
</task>

</tasks>

<verification>
- `audit/v30-238-01-BWD.md` exists with all 8 required sections (Shared-Prefix Backward-Trace Chains, Backward Freeze Table, Backward Adversarial Closure Table, Gameover-Flow Backward-Freeze Subset, Prior-Artifact Cross-Cites, Finding Candidates, Scope-Guard Deferrals, Attestation)
- Exactly 146 distinct INV-237-NNN Row IDs appear; Backward Freeze Table + Backward Adversarial Closure Table each have 146 rows
- 22 EXCEPTION rows: EXC-01=2 (INV-237-005, INV-237-006) / EXC-02=8 (prevrandao) / EXC-03=4 (F-29-04) / EXC-04=8 (EntropyLib XOR-shift)
- 124 SAFE rows
- 19 gameover-flow rows in dedicated subset (7 gameover-entropy SAFE + 8 prevrandao EXCEPTION + 4 F-29-04 EXCEPTION)
- 6 shared-prefix chains named (PREFIX-DAILY / PREFIX-MIDDAY / PREFIX-GAMEOVER / PREFIX-PREVRANDAO / PREFIX-AFFILIATE / PREFIX-GAP)
- 4-actor taxonomy applied (NO_REACHABLE_PATH / PATH_BLOCKED_BY_GATE / EXCEPTION / CANDIDATE_FINDING vocabulary)
- No `mutable-after-request` occurrences (BWD-02 wording enforcement)
- No F-30-NN IDs (D-15)
- No placeholder tokens `<line>`, `<path>`, `<fn`, `TBD-`
- HEAD anchor `7ab515fe` echoed in Audit baseline line + frontmatter
- At least one `re-verified at HEAD 7ab515fe` cross-cite statement (D-09/D-10)
- Zero contracts/ test/ writes (D-20); `git diff --name-only 7ab515fe..HEAD -- contracts/ test/` empty
- Zero edits to `audit/v30-CONSUMER-INVENTORY.md` (D-18)
- Deliverable committed; not pushed
</verification>

<success_criteria>
BWD-01 + BWD-02 + BWD-03 satisfied: every one of 146 VRF-consuming call sites in `audit/v30-CONSUMER-INVENTORY.md` has a backward-trace verdict (SAFE or EXCEPTION) with every storage read on the consumption path classified `written-before-request` / `unreachable-after-request` / `EXCEPTION` (NO `mutable-after-request` without KI citation). Per-row 4-actor adversarial closure records player / admin / validator / VRF-oracle mutability between VRF request and consumption with closed 4-value vocabulary. 22 KI-accepted EXCEPTION rows match 237-02 SUMMARY distribution (EXC-01=2, EXC-02=8, EXC-03=4, EXC-04=8). 19 gameover-flow rows prepped for Phase 240 GO-01..05 hand-off. 6 shared-prefix chains leverage 237-03 dedup. Prior-milestone artifacts CROSS-CITED with re-verified-at-HEAD-7ab515fe notes — not relied upon. Phase 238 Plan 03's consolidation task inherits this file intact.
</success_criteria>

<output>
After completion, create `.planning/phases/238-backward-forward-freeze-proofs/238-01-SUMMARY.md`
</output>
