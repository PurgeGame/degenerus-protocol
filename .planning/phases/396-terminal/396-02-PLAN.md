---
phase: 396-terminal
plan: 02
type: execute
wave: 2
depends_on: ["396-01"]
files_modified:
  - audit/FINDINGS-v63.0.md
  - AUDIT-V63-REPORT.html
autonomous: true
requirements: [TERM-02]
must_haves:
  truths:
    - "audit/FINDINGS-v63.0.md consolidates every requirement + lead with its final adjudicated verdict vs the frozen subject a8b702a7, matching the FINDINGS-v62.0 format"
    - "The one CONFIRMED finding (BURNIE-04) is recorded with its routed gated-fix state (fix NOT applied — separate post-audit USER-reviewed change, 5 pending decisions)"
    - "Both nets (council + Claude) are on record per sweep area; the council-on-refuted re-run result is documented"
    - "AUDIT-V63-REPORT.html is produced as a dark-themed static report matching AUDIT-V62-REPORT.html"
    - "audit/FINDINGS-v63.0.md is chmod 444 (immutable deliverable) at the end"
  artifacts:
    - path: "audit/FINDINGS-v63.0.md"
      provides: "The canonical v63.0 consolidated findings deliverable (every req + lead + verdict + BURNIE-04 routed fix), chmod 444"
      min_lines: 100
      contains: "BURNIE-04"
    - path: "AUDIT-V63-REPORT.html"
      provides: "Dark-themed static HTML report mirroring AUDIT-V62-REPORT.html, summarizing the v63.0 audit + findings"
      min_lines: 80
  key_links:
    - from: "audit/FINDINGS-v63.0.md"
      to: ".planning/phases/396-terminal/396-CONSOLIDATED-LEDGER.md"
      via: "every ledger entry's final verdict carried into the deliverable"
      pattern: "BURNIE-04|REFUTED|CONFIRMED"
    - from: "AUDIT-V63-REPORT.html"
      to: "audit/FINDINGS-v63.0.md"
      via: "the HTML report renders the same findings/verdicts as the markdown deliverable"
      pattern: "BURNIE-04|v63"
---

<objective>
Author the two v63.0 audit deliverables: audit/FINDINGS-v63.0.md (the canonical consolidated findings document, matching the FINDINGS-v62.0 format, chmod 444 at the end) and AUDIT-V63-REPORT.html (the dark-themed static report matching AUDIT-V62-REPORT.html). Every lead and requirement is recorded with its final adjudicated verdict vs the frozen subject a8b702a7; the one CONFIRMED finding (BURNIE-04) is routed to its gated post-audit fix without applying it.

Purpose: TERM-02 produces the milestone's primary deliverable. The closure flip (Plan 03) re-attests the requirements against this document. The deliverable must faithfully carry the Plan 01 consolidated ledger + council-on-refuted + skeptic-gate results, and must be byte-correct in the v62 house style so the milestone record stays consistent.

Output: audit/FINDINGS-v63.0.md (chmod 444) + AUDIT-V63-REPORT.html.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/AUDIT-V63-PLAN.md
@.planning/REQUIREMENTS.md

<read_first>
The Plan 01 outputs (consume these — they are the source of truth for the deliverable):
@.planning/phases/396-terminal/396-CONSOLIDATED-LEDGER.md
@.planning/phases/396-terminal/396-COUNCIL-ON-REFUTED.md
@.planning/phases/396-terminal/396-SKEPTIC-GATE.md

The format templates to MATCH (replicate the structure, section ordering, table style, and tone):
@audit/FINDINGS-v62.0.md
@AUDIT-V62-REPORT.html

The BURNIE-04 fix-design state (the one CONFIRMED finding routed to a gated fix):
@.planning/phases/392-entropy-and-econ/392-BURNIE-04-FIX-DESIGN.md
@.planning/phases/392-entropy-and-econ/392-BURNIE-04-ALT-DESIGN-REVIEW.md

Subject facts for the document header:
- Frozen audit subject SHA: a8b702a7 (byte-frozen at FOUNDATION / Phase 388).
- Baseline (last formally audited): 77580320 (v62.0 closure subject).
- Contracts tree-hash: 2934d3d8987a09c5f073549a0cb499f6c5f28620.
- Method: COUNCIL + CLAUDE BOTH (dual-net) — see AUDIT-V63-PLAN.md §2.
- Council pipeline + raw outputs: .planning/audit-v52/cross-model/{389..396}/.

Posture: AUDIT-ONLY — no contract edits. The BURNIE-04 fix is NOT applied here; it is a separate post-audit gated change (USER hand-review + 5 pending design decisions). Neutral defensive-engineering vocabulary throughout. chmod 444 the FINDINGS doc at the very end (per the v62 immutable-deliverable pattern).
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author audit/FINDINGS-v63.0.md in the FINDINGS-v62.0 format</name>
  <files>audit/FINDINGS-v63.0.md</files>
  <read_first>
    audit/FINDINGS-v62.0.md (the exact format to match — header block, executive summary disposition table, the durable-foundation section, actionable findings, lower-severity, refuted/by-design, reproduction artifacts, remediation order); 396-CONSOLIDATED-LEDGER.md + 396-COUNCIL-ON-REFUTED.md + 396-SKEPTIC-GATE.md (the content source); 392-BURNIE-04-FIX-DESIGN.md + 392-BURNIE-04-ALT-DESIGN-REVIEW.md (BURNIE-04 detail + its pending decisions).
  </read_first>
  <action>
    Write audit/FINDINGS-v63.0.md mirroring the FINDINGS-v62.0.md structure. Header block: frozen subject a8b702a7 (byte-identical, zero contracts/*.sol mutation; git diff empty; tree-hash 2934d3d8987a09c5f073549a0cb499f6c5f28620), baseline 77580320, date, the dual-net method (COUNCIL + CLAUDE BOTH per AUDIT-V63-PLAN §2), and the council pipeline pointer to the raw outputs under .planning/audit-v52/cross-model/. Executive summary: a disposition-count table (CONFIRMED = 1 BURNIE-04 MED; BY-DESIGN/WONTFIX = BURNIE-05; LOW test-only = R-389-01; mutation test-gaps KILLED = G-BPL-01 + K1-K6; council candidates refuted = the 4 HIGH + others) stating the v63 outcome: a near-clean delta with one bounded MED gap routed to a gated fix. The durable foundation section: summarize the Phase 388 green oracle + the Phase 395 mutation campaign as the regression net (cite CAMPAIGN-REPORT-v63.md). Per-sweep-area section: for each of 389-395, record both nets on record (council + Claude) + the area verdict. Actionable findings section: BURNIE-04 in full (origin, defect — auto-rebuy carry excluded from sDGNRS redemption backing so redeemers are progressively under-credited, impact, severity MED, and the ROUTED gated-fix state — fix NOT applied, the aggregate-burnieBase-lane direction with the uint64→uint96 re-pack and submit-time carry decrement + D+1 day-result read, 5 USER decisions pending). Lower-severity: R-389-01 + the mutation test-gaps, all recorded as test-coverage holes KILLED by Phase 395 regression tests (not contract defects). Refuted/by-design section: the 4 refuted HIGH candidates (ECON-04, ECON-06, SOLV-07, RNG-04) with the council-on-refuted re-run result (they remained refuted, or whatever Task 2 of Plan 01 recorded) + BURNIE-05 BY-DESIGN + the standing by-design rulings. Reproduction artifacts: cite the Phase 395 regression tests that kill the test-gaps. Close with the remediation note: BURNIE-04 is the sole open gated item (separate USER-reviewed change). Use neutral defensive-engineering vocabulary. Do NOT chmod yet (Task 3 does that, after the HTML report reads it).
  </action>
  <verify>
    <automated>test -f audit/FINDINGS-v63.0.md && grep -q "BURNIE-04" audit/FINDINGS-v63.0.md && grep -q "a8b702a7" audit/FINDINGS-v63.0.md && grep -q "77580320" audit/FINDINGS-v63.0.md && grep -qiE "council|cross-model" audit/FINDINGS-v63.0.md && grep -qiE "ECON-04|RNG-04|SOLV-07" audit/FINDINGS-v63.0.md && [ $(wc -l < audit/FINDINGS-v63.0.md) -ge 100 ]</automated>
  </verify>
  <acceptance_criteria>
    - audit/FINDINGS-v63.0.md exists, mirrors the FINDINGS-v62.0 section structure, ≥100 lines.
    - Header records frozen subject a8b702a7, baseline 77580320, tree-hash, and the dual-net method.
    - Executive summary disposition table records CONFIRMED = 1 (BURNIE-04 MED) and the refuted/by-design/test-gap counts.
    - BURNIE-04 is documented in full with its ROUTED gated-fix state (fix NOT applied, 5 pending USER decisions).
    - The 4 refuted HIGH candidates carry the council-on-refuted re-run result; R-389-01 + mutation test-gaps recorded as KILLED test-coverage holes.
    - File is NOT yet chmod 444 (left writable for the HTML task + the final chmod in Task 3).
  </acceptance_criteria>
  <done>audit/FINDINGS-v63.0.md is authored in the v62 house style, records every req + lead + final verdict vs frozen a8b702a7, with BURNIE-04 routed to its gated fix; file left writable for downstream tasks.</done>
</task>

<task type="auto">
  <name>Task 2: Produce AUDIT-V63-REPORT.html matching the AUDIT-V62-REPORT.html style</name>
  <files>AUDIT-V63-REPORT.html</files>
  <read_first>
    AUDIT-V62-REPORT.html (the exact dark-themed static-report template — replicate the CSS theme, layout, section structure, disposition cards/tables, and tone); audit/FINDINGS-v63.0.md (Task 1 — the content the report renders).
  </read_first>
  <action>
    Write AUDIT-V63-REPORT.html as a self-contained dark-themed static HTML report mirroring AUDIT-V62-REPORT.html (reuse its CSS theme + section layout). Render: the v63.0 audit header (subject a8b702a7, baseline 77580320, dual-net method), the disposition summary (the same counts as the FINDINGS markdown — 1 CONFIRMED MED BURNIE-04, the refuted/by-design/test-gap breakdown), a per-sweep-area table (389-395) showing both nets on record + each area verdict, the BURNIE-04 finding card (defect + MED severity + routed-gated-fix status), the refuted-HIGH section (the 4 candidates + council-on-refuted outcome), and a closing posture note (AUDIT-ONLY, contracts byte-frozen, BURNIE-04 the sole open gated item). Keep it fully self-contained (inline CSS, no external assets). Use neutral defensive-engineering vocabulary. The report content must agree with audit/FINDINGS-v63.0.md (same findings, same verdicts, same counts).
  </action>
  <verify>
    <automated>test -f AUDIT-V63-REPORT.html && grep -qiE "<html|<!DOCTYPE" AUDIT-V63-REPORT.html && grep -q "BURNIE-04" AUDIT-V63-REPORT.html && grep -qiE "a8b702a7|v63" AUDIT-V63-REPORT.html && [ $(wc -l < AUDIT-V63-REPORT.html) -ge 80 ]</automated>
  </verify>
  <acceptance_criteria>
    - AUDIT-V63-REPORT.html is a self-contained dark-themed static report mirroring AUDIT-V62-REPORT.html's theme + layout.
    - It renders the same disposition counts, per-area verdicts, and BURNIE-04 finding as audit/FINDINGS-v63.0.md.
    - Subject a8b702a7 + the dual-net method + the AUDIT-ONLY posture are present; BURNIE-04 shown as the sole open gated item.
    - No external assets (inline CSS only).
  </acceptance_criteria>
  <done>AUDIT-V63-REPORT.html renders the v63.0 audit + findings in the v62 dark-theme house style, content-consistent with the markdown deliverable.</done>
</task>

<task type="auto">
  <name>Task 3: Lock audit/FINDINGS-v63.0.md immutable (chmod 444)</name>
  <files>audit/FINDINGS-v63.0.md</files>
  <read_first>
    audit/FINDINGS-v62.0.md (note it is mode 444 — the immutable-deliverable pattern); audit/FINDINGS-v63.0.md (Task 1 — confirm content is final and the HTML report Task 2 has consumed it).
  </read_first>
  <action>
    After Tasks 1 and 2 are complete and the markdown content is final (the HTML report has already read it), make audit/FINDINGS-v63.0.md immutable: chmod 444 audit/FINDINGS-v63.0.md. This matches the v62 immutable-deliverable pattern (FINDINGS-v62.0.md is mode 444). Verify the mode took effect. Do NOT modify the file content in this task — chmod only.
  </action>
  <verify>
    <automated>test -f audit/FINDINGS-v63.0.md && [ "$(stat -c '%a' audit/FINDINGS-v63.0.md)" = "444" ] && echo CHMOD_444_OK</automated>
  </verify>
  <acceptance_criteria>
    - audit/FINDINGS-v63.0.md is mode 444 (read-only, immutable), matching the FINDINGS-v62.0.md pattern.
    - The file content is unchanged by this task (chmod only).
  </acceptance_criteria>
  <done>audit/FINDINGS-v63.0.md is chmod 444 — the immutable canonical v63.0 deliverable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ledger → deliverable | a finding's verdict could be altered/dropped between the Plan 01 ledger and the published FINDINGS doc |
| markdown ↔ HTML | the two deliverables could disagree on counts/verdicts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-396-04 | Tampering | published findings diverge from the adjudicated ledger | mitigate | FINDINGS-v63.0.md verdicts sourced verbatim from 396-CONSOLIDATED-LEDGER.md; BURNIE-04 routed-fix state from the 392 fix-design docs |
| T-396-05 | Information disclosure | over-disclosing intended game mechanics as findings | mitigate | by-design rulings recorded as BY-DESIGN only; no intended-mechanic moat entries (per the locked ruling) |
| T-396-06 | Tampering | the immutable deliverable left writable | mitigate | chmod 444 (Task 3) after content is final, matching the v62 pattern |
</threat_model>

<verification>
- audit/FINDINGS-v63.0.md exists, ≥100 lines, in the v62 format, records every req + lead + verdict, BURNIE-04 routed to its gated fix.
- AUDIT-V63-REPORT.html is a self-contained dark-themed report consistent with the markdown.
- audit/FINDINGS-v63.0.md is mode 444.
- No contract files modified (files_modified = audit/ + repo-root HTML only).
</verification>

<success_criteria>
Both deliverables are produced in the v62 house style; every lead is adjudicated vs frozen a8b702a7; BURNIE-04 (the sole CONFIRMED finding) is routed to its gated post-audit fix without applying it; FINDINGS-v63.0.md is chmod 444.
</success_criteria>

<output>
Create `.planning/phases/396-terminal/396-02-SUMMARY.md` when done.
</output>
