---
phase: 82-ticket-processing-mechanics
plan: 02
type: execute
wave: 2
depends_on: ["82-01"]
files_modified:
  - audit/v4.0-82-ticket-processing.md
  - audit/v4.0-findings-consolidated.md
autonomous: true
requirements: [TPROC-04, TPROC-05, TPROC-06]

must_haves:
  truths:
    - "ticketLevel, ticketCursor, and ticketsFullyProcessed full lifecycle is documented with every read and write site"
    - "traitBurnTicket storage layout is documented including assembly write pattern and all read paths"
    - "All discrepancies between prior audit prose and current code are flagged with [DISCREPANCY] or [NEW FINDING] tags"
    - "v3.8 commitment window inventory Section 1.13 claims are cross-referenced against current code"
  artifacts:
    - path: "audit/v4.0-82-ticket-processing.md"
      provides: "Ticket processing audit document sections 6-9"
      contains: "traitBurnTicket"
    - path: "audit/v4.0-findings-consolidated.md"
      provides: "Updated findings if new discrepancies found"
      contains: "Phase 82"
  key_links:
    - from: "ticketLevel (GS:477)"
      to: "processTicketBatch (JM:1895)"
      via: "cursor level check and reset"
      pattern: "ticketLevel.*GS:477"
    - from: "_raritySymbolBatch assembly"
      to: "traitBurnTicket (GS:417)"
      via: "inline assembly sstore"
      pattern: "traitBurnTicket.*GS:417"
    - from: "v3.8 Section 1.13"
      to: "current code"
      via: "cross-reference verification"
      pattern: "DISCREPANCY\\|CONFIRMED"
---

<objective>
Complete the ticket processing audit by documenting cursor lifecycle (ticketLevel, ticketCursor, ticketsFullyProcessed), traitBurnTicket storage layout with all write/read paths, and cross-referencing all prior audit claims against current code.

Purpose: Close out the ticket processing mechanics audit with cursor state machine documentation, storage layout verification, and discrepancy detection against v3.8/v3.9 prior audit documents.
Output: audit/v4.0-82-ticket-processing.md sections 6-9, updated audit/v4.0-findings-consolidated.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-ticket-processing-mechanics/82-RESEARCH.md
@.planning/phases/82-ticket-processing-mechanics/82-01-SUMMARY.md

Plan 01 created sections 1-5 of audit/v4.0-82-ticket-processing.md covering processTicketBatch, processFutureTicketBatch, advanceGame triggers, and RNG derivation chain. Plan 02 adds cursor lifecycle, traitBurnTicket storage, and discrepancy detection.

Key context from research:
- ticketLevel at GS:477 (uint24), ticketCursor at GS:474 (uint32), ticketsFullyProcessed at GS:332 (bool)
- traitBurnTicket at GS:417: mapping(uint24 => address[][256])
- Assembly writes in _raritySymbolBatch (JM:2187-2221 and MM:521-555) compute storage slots manually
- v3.8 Section 1.13 claims processTicketBatch at JM:1890-1951 (current: JM:1889-1950, 1-line shift)
- v3.8 claims lastLootboxRngWord at slot 70, traitBurnTicket at slot 11, ticketLevel at slot 17 offset 4, ticketCursor at slot 17 offset 0
- v3.8 Section line 766 claims "No permissionless external function directly writes traitBurnTicket outside of processTicketBatch" -- STALE since processFutureTicketBatch also writes

File abbreviations: JM = DegenerusGameJackpotModule.sol, MM = DegenerusGameMintModule.sol, AM = DegenerusGameAdvanceModule.sol, GS = DegenerusGameStorage.sol, DG = DegenerusGame.sol
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace cursor lifecycle and traitBurnTicket storage layout</name>
  <files>audit/v4.0-82-ticket-processing.md</files>
  <read_first>
    audit/v4.0-82-ticket-processing.md
    contracts/storage/DegenerusGameStorage.sol
    contracts/modules/DegenerusGameJackpotModule.sol
    contracts/modules/DegenerusGameMintModule.sol
    contracts/modules/DegenerusGameAdvanceModule.sol
    contracts/DegenerusGame.sol
  </read_first>
  <action>
Append to audit/v4.0-82-ticket-processing.md:

**Section 6: Cursor Lifecycle (TPROC-04)**

Document the complete lifecycle of the three cursor state variables:

**6.1 Variable Declarations:**
- ticketCursor: GS:474, uint32 -- index into ticketQueue for current level
- ticketLevel: GS:477, uint24 -- which level is being processed (bit 22 = FF marker)
- ticketsFullyProcessed: GS:332, bool -- read slot fully drained for current purchase level

**6.2 State Machine:**
Document the four states:
- IDLE: ticketLevel=0, ticketCursor=0
- PROCESSING: ticketLevel=lvl, ticketCursor=idx (advancing through queue)
- FF_PROCESSING: ticketLevel=lvl|TICKET_FAR_FUTURE_BIT, ticketCursor=idx
- DONE: ticketLevel=0, ticketCursor=0 (returned to IDLE)

**6.3 All Write Sites (enumerate every write with file:line):**

ticketLevel writes:
- processTicketBatch level set: JM:1896 (ticketLevel = lvl)
- processTicketBatch done reset: JM:1905 and JM:1946 (ticketLevel = 0)
- processFutureTicketBatch done reset: MM:316, MM:337, MM:446, MM:450 (ticketLevel = 0)
- processFutureTicketBatch FF transition: MM:310, MM:332, MM:442 (ticketLevel = lvl | TICKET_FAR_FUTURE_BIT)

ticketCursor writes:
- processTicketBatch cursor set: JM:1897 (ticketCursor = 0 on level change)
- processTicketBatch cursor save: JM:1940 (ticketCursor = uint32(idx))
- processTicketBatch done reset: JM:1904 and JM:1945 (ticketCursor = 0)
- processFutureTicketBatch cursor save: MM:434 (ticketCursor = uint32(idx))
- processFutureTicketBatch done reset: MM:315, MM:336, MM:445, MM:449 (ticketCursor = 0)
- processFutureTicketBatch FF transition cursor reset: MM:311, MM:333, MM:443 (ticketCursor = 0)

ticketsFullyProcessed writes:
- advanceGame mid-day: AM:173 (= true, after _runProcessTicketBatch returns finished)
- advanceGame daily drain: AM:218 (= true, when read slot empty or just finished)
- advanceGame new-day post-RNG: AM:276 (= true, "ADV-03: set before jackpot/phase logic")
- _swapTicketSlot: GS:713 (= false)

**VERIFY every line number above against the actual code.** If any have drifted from the research estimates, document the actual line numbers. Grep for each variable name to ensure no write sites are missed.

**6.4 All Read Sites:**
Enumerate every read of ticketLevel, ticketCursor, ticketsFullyProcessed with file:line, grouped by function.

**6.5 _prepareFutureTickets Resume Logic:**
Document the resume mechanism at AM:1156-1184: checks if ticketLevel (with FF bit stripped via ~TICKET_FAR_FUTURE_BIT) falls within the [startLevel, endLevel] range, and if so, processes that level first before iterating remaining levels.

**Section 7: traitBurnTicket Storage Layout (TPROC-05)**

**7.1 Declaration:**
traitBurnTicket at GS:417: mapping(uint24 => address[][256]) internal traitBurnTicket
- Outer key: uint24 level
- Inner key: uint8 traitId (0-255)
- Value: address[] dynamic array of player addresses

**7.2 Assembly Write Pattern:**
Document the inline assembly in _raritySymbolBatch (JM:2187-2221 and MM:521-555):
1. levelSlot = keccak256(abi.encode(lvl, traitBurnTicket.slot)) -- JM:2189-2191
2. elemSlot = levelSlot + traitId -- per-trait array length slot
3. oldLen = sload(elemSlot) -- current array length
4. sstore(elemSlot, oldLen + occurrences) -- update length
5. dataSlot = keccak256(abi.encode(elemSlot)) -- first data element
6. For k in [0, occurrences): sstore(dataSlot + oldLen + k, player) -- push player addresses

Verify the assembly matches the standard Solidity storage layout for nested mappings with fixed-size arrays.

**7.3 All Write Paths (complete enumeration):**
- _raritySymbolBatch in JackpotModule: JM:2187-2221 (called by processTicketBatch chain)
- _raritySymbolBatch in MintModule: MM:521-555 (called by processFutureTicketBatch)
Confirm these are the ONLY two write paths. Grep for traitBurnTicket writes and sstore patterns.

**7.4 All Read Paths (complete enumeration):**
Grep contracts/ for all reads of traitBurnTicket. Expected:
- _randTraitTicket: JM:2237-2280 (jackpot winner selection)
- Additional jackpot reads: JM:2294 area
- View functions in DegenerusGame.sol: DG:2618, DG:2647, DG:2730
- _hasTraitTickets: JM:1040 area
Document each read with file:line and purpose (winner selection, view, existence check).

**7.5 Storage Slot Verification:**
If feasible, run `forge inspect DegenerusGame storage-layout 2>/dev/null | grep -i traitBurnTicket` to confirm the slot number. Compare against v3.8 claim (slot 11). If forge inspect is not available or too slow, note the v3.8 claim and flag for manual verification.
  </action>
  <verify>
    <automated>grep -c 'ticketLevel\|ticketCursor\|ticketsFullyProcessed\|traitBurnTicket' audit/v4.0-82-ticket-processing.md | awk '{if ($1 >= 30) print "PASS: " $1 " cursor/storage references"; else print "FAIL: only " $1 " references"}'</automated>
  </verify>
  <acceptance_criteria>
    - audit/v4.0-82-ticket-processing.md contains "## Section 6" or "## 6." with "Cursor" in heading
    - File contains "ticketLevel" with "GS:477"
    - File contains "ticketCursor" with "GS:474"
    - File contains "ticketsFullyProcessed" with "GS:332"
    - File contains at least 10 write site citations for the three cursor variables
    - File contains "IDLE" and "PROCESSING" and "FF_PROCESSING" state labels
    - File contains "_prepareFutureTickets" resume logic documentation
    - File contains "## Section 7" or "## 7." with "traitBurnTicket" in heading
    - File contains "mapping(uint24 => address[][256])" or equivalent declaration reference
    - File contains assembly write pattern documentation with "levelSlot" and "elemSlot" and "dataSlot"
    - File contains "JM:2187" or nearby for JackpotModule _raritySymbolBatch
    - File contains "MM:521" or nearby for MintModule _raritySymbolBatch
    - File contains at least 3 read path citations for traitBurnTicket
    - At least 30 combined references to cursor variables and traitBurnTicket across sections 6-7
  </acceptance_criteria>
  <done>Complete cursor state machine documented with every read/write site and file:line citations; traitBurnTicket storage layout documented with assembly write pattern and all read/write paths enumerated</done>
</task>

<task type="auto">
  <name>Task 2: Cross-reference prior audit claims and flag discrepancies</name>
  <files>audit/v4.0-82-ticket-processing.md, audit/v4.0-findings-consolidated.md</files>
  <read_first>
    audit/v4.0-82-ticket-processing.md
    audit/v3.8-commitment-window-inventory.md
    audit/v4.0-findings-consolidated.md
    audit/v4.0-ticket-creation-queue-mechanics.md
    audit/v4.0-ticket-queue-double-buffer.md
  </read_first>
  <action>
Append to audit/v4.0-82-ticket-processing.md:

**Section 8: Cross-Reference with Prior Audit Claims (TPROC-06)**

Systematically verify these specific v3.8 claims from Section 1.13 of the commitment window inventory against current code:

| # | v3.8 Claim | v3.8 Location | Current Code | Verdict |
|---|-----------|--------------|-------------|---------|
| 1 | processTicketBatch at JM:1890-1951 | Section 1.13 ~line 171 | JM:1889-1950 (1-line shift) | CONFIRMED with drift |
| 2 | lastLootboxRngWord at slot 70 | Section 1.13 ~line 178 | Verify via GS comments or forge inspect | CONFIRMED or [DISCREPANCY] |
| 3 | traitBurnTicket at slot 11 | Section 1.13 ~line 180 | Verify via GS comments or forge inspect | CONFIRMED or [DISCREPANCY] |
| 4 | ticketLevel at slot 17 offset 4 | Section 1.13 ~line 175 | GS:477 comment | CONFIRMED or [DISCREPANCY] |
| 5 | ticketCursor at slot 17 offset 0 | Section 1.13 ~line 176 | GS:474 comment | CONFIRMED or [DISCREPANCY] |
| 6 | "No permissionless external function directly writes traitBurnTicket outside of processTicketBatch" | Section ~line 766 | processFutureTicketBatch also writes | [DISCREPANCY] — must now include processFutureTicketBatch |

For each claim: read the actual v3.8 inventory text, find the corresponding current code, and assign a verdict:
- CONFIRMED: claim matches current code
- CONFIRMED with drift: claim is correct but line numbers shifted slightly
- [DISCREPANCY]: claim is incorrect or incomplete relative to current code
- [NEW FINDING]: behavior not documented in any prior audit

**Known discrepancy from research:** v3.8 claim #6 is stale because processFutureTicketBatch (added v3.9 Phase 76) also writes to traitBurnTicket via _raritySymbolBatch. This MUST be flagged as [DISCREPANCY].

**Additional cross-references:**
- Check if v3.8 documents the processFutureTicketBatch function AT ALL. If absent, flag as [DISCREPANCY] (expected: absent, since it was added in v3.9).
- Check v3.9 audit documents for any claims about ticket processing. Cross-reference against current code.
- Check Phase 81 findings (DSC-01 through DSC-03) for any relevance to ticket processing documentation.

For each discrepancy or new finding, document:
1. What the prior audit says
2. What the current code actually does
3. The severity (likely INFO for documentation issues, potentially higher for logic errors)
4. Recommended action

**Section 9: Phase 82 Summary and Finding Catalog**

Create a summary table of all findings from this phase:

| Finding ID | Type | Severity | Description | Location |
|-----------|------|----------|-------------|----------|
| ... | [DISCREPANCY] or [NEW FINDING] | INFO/LOW/MED/HIGH | ... | file:line |

Include count of total file:line citations in the document, total functions traced, and total prior-audit claims verified.

**Update audit/v4.0-findings-consolidated.md:**
- Add a "Phase 82" section with any new DSC-XX findings
- Update the header to reflect Phase 82 completion
- Update the "Phases Pending" table (Phase 82 -> Complete)
- If no new findings above INFO, note "Phase 82: No new findings above INFO severity"
  </action>
  <verify>
    <automated>grep -c '\[DISCREPANCY\]\|\[NEW FINDING\]\|CONFIRMED' audit/v4.0-82-ticket-processing.md | awk '{if ($1 >= 3) print "PASS: " $1 " cross-reference verdicts"; else print "FAIL: only " $1 " verdicts"}'</automated>
  </verify>
  <acceptance_criteria>
    - audit/v4.0-82-ticket-processing.md contains "## Section 8" or "## 8." with "Cross-Reference" or "Prior Audit" in heading
    - File contains at least one [DISCREPANCY] tag (the stale traitBurnTicket writer claim is a known discrepancy)
    - File contains "CONFIRMED" for at least 2 claims
    - File contains a cross-reference table with at least 6 rows
    - File contains "processFutureTicketBatch" in a [DISCREPANCY] context (v3.8 missing it as a traitBurnTicket writer)
    - File contains "## Section 9" or "## 9." with summary/catalog
    - File contains a finding catalog table
    - audit/v4.0-findings-consolidated.md contains "Phase 82"
    - audit/v4.0-findings-consolidated.md "Phases Pending" table shows Phase 82 as complete or moved to a completed section
    - At least 3 cross-reference verdicts (CONFIRMED, DISCREPANCY, or NEW FINDING)
  </acceptance_criteria>
  <done>All v3.8 Section 1.13 claims cross-referenced against current code with verdicts; all discrepancies tagged; findings consolidated; audit/v4.0-findings-consolidated.md updated with Phase 82 results</done>
</task>

</tasks>

<verification>
- audit/v4.0-82-ticket-processing.md contains sections 6-9
- ticketLevel, ticketCursor, ticketsFullyProcessed write sites all documented with file:line
- traitBurnTicket assembly write pattern documented with storage slot derivation
- All traitBurnTicket read paths enumerated (jackpot selection, view functions, existence checks)
- v3.8 Section 1.13 claims systematically verified with CONFIRMED/DISCREPANCY verdicts
- processFutureTicketBatch flagged as missing from v3.8 traitBurnTicket writer list
- audit/v4.0-findings-consolidated.md updated with Phase 82 findings
</verification>

<success_criteria>
- Sections 6-9 complete with 30+ additional file:line citations
- Complete cursor state machine with every write and read site enumerated
- traitBurnTicket storage layout documented with assembly write pattern verified
- At least 6 prior-audit claims cross-referenced with verdicts
- All discrepancies tagged with [DISCREPANCY], all new issues tagged with [NEW FINDING]
- Consolidated findings document updated
</success_criteria>

<output>
After completion, create `.planning/phases/82-ticket-processing-mechanics/82-02-SUMMARY.md`
</output>
