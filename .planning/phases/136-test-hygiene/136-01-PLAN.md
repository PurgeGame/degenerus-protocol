---
phase: 136-test-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/deploy/DeployScript.test.js
  - test/unit/DGNRSLiquid.test.js
  - test/unit/DegenerusDeityPass.test.js
  - test/fuzz/LootboxBoonCoexistence.t.sol
  - test/fuzz/SimAdvanceOverflow.t.sol
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]

must_haves:
  truths:
    - "All 5 pending test files are committed to git"
    - "Full Hardhat test suite passes with zero failures"
    - "Full Foundry fuzz/invariant test suite passes with zero failures"
  artifacts:
    - path: "test/fuzz/LootboxBoonCoexistence.t.sol"
      provides: "Foundry fuzz test for lootbox boon coexistence"
      contains: "function"
    - path: "test/fuzz/SimAdvanceOverflow.t.sol"
      provides: "Foundry fuzz test for sim advance overflow"
      contains: "function"
  key_links:
    - from: "test files"
      to: "test suite results"
      via: "test runners"
      pattern: "passing|ok"
---

<objective>
Commit all pending test updates and verify both test suites (Hardhat + Foundry) pass with zero failures.

Purpose: Ensure all test work from v8.0 and post-v8.0 is committed and the full test suite is green before C4A submission.
Output: All test files committed, both suites green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-test-hygiene/136-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run test suites to verify all tests pass BEFORE committing</name>
  <files>test/deploy/DeployScript.test.js, test/unit/DGNRSLiquid.test.js, test/unit/DegenerusDeityPass.test.js, test/fuzz/LootboxBoonCoexistence.t.sol, test/fuzz/SimAdvanceOverflow.t.sol</files>
  <read_first>
    - .planning/phases/136-test-hygiene/136-CONTEXT.md (D-01 through D-06: file list and test execution requirements)
    - test/deploy/DeployScript.test.js (modified test — review changes)
    - test/unit/DGNRSLiquid.test.js (modified test — review changes)
    - test/unit/DegenerusDeityPass.test.js (modified test — review changes)
    - test/fuzz/LootboxBoonCoexistence.t.sol (new Foundry fuzz test)
    - test/fuzz/SimAdvanceOverflow.t.sol (new Foundry fuzz test)
  </read_first>
  <action>
**Step 1: Run Hardhat test suite**

```bash
npx hardhat test
```

Verify: all tests pass, zero failures. Record the passing count.

If any test fails: diagnose and fix before proceeding. DO NOT commit broken tests.

**Step 2: Run Foundry test suite**

```bash
forge test
```

Verify: all tests pass (including the 2 new fuzz tests), zero failures. Record the passing count.

If any test fails: diagnose and fix before proceeding.

**Step 3: Commit test files**

IMPORTANT: Do NOT stage or commit `contracts/ContractAddresses.sol` — user manages this file separately.

Commit the 3 modified test files:
```bash
git add test/deploy/DeployScript.test.js test/unit/DGNRSLiquid.test.js test/unit/DegenerusDeityPass.test.js
git commit -m "test: update DeployScript, DGNRSLiquid, and DeityPass tests for v8.0+ changes"
```

Commit the 2 new Foundry fuzz tests:
```bash
git add test/fuzz/LootboxBoonCoexistence.t.sol test/fuzz/SimAdvanceOverflow.t.sol
git commit -m "test: add LootboxBoonCoexistence and SimAdvanceOverflow Foundry fuzz tests"
```

**Step 4: Verify commits**

```bash
git log --oneline -3
```

Confirm both commits appear with the test files.
  </action>
  <verify>
    <automated>git log --oneline -2 | grep -c "test:"</automated>
  </verify>
  <acceptance_criteria>
    - `npx hardhat test` exits with code 0 and reports zero failures
    - `forge test` exits with code 0 and reports zero failures
    - `git log --oneline -2` shows two commits with "test:" prefix
    - `git status -- test/` shows no modified or untracked test files (all committed)
    - `git show --stat HEAD` includes LootboxBoonCoexistence.t.sol and SimAdvanceOverflow.t.sol
    - `git show --stat HEAD~1` includes DeployScript.test.js, DGNRSLiquid.test.js, DegenerusDeityPass.test.js
    - `contracts/ContractAddresses.sol` is NOT in any commit (still shows as modified in working tree)
  </acceptance_criteria>
  <done>All 5 test files committed. Hardhat and Foundry suites both green with zero failures.</done>
</task>

</tasks>

<verification>
- All 5 pending test files committed to git
- Hardhat test suite passes with zero failures
- Foundry test suite passes with zero failures
- ContractAddresses.sol was NOT committed
</verification>

<success_criteria>
- TEST-01: All pending test updates committed
- TEST-02: Full Hardhat suite green
- TEST-03: Full Foundry suite green
</success_criteria>

<output>
After completion, create `.planning/phases/136-test-hygiene/136-01-SUMMARY.md`
</output>
