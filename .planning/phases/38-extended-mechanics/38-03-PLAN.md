---
phase: 38-extended-mechanics
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ../simulator/src/mechanics/affiliate.ts
  - ../simulator/src/mechanics/affiliate.test.ts
  - ../simulator/src/types/state.ts
  - ../simulator/src/mechanics/index.ts
autonomous: true
requirements: [CORE-13, CORE-18, CORE-19, CORE-20, CORE-21, CORE-22, CORE-23, CORE-30]

must_haves:
  truths:
    - "3-tier referral: affiliate base -> upline1 at 20% -> upline2 at 4%"
    - "Fresh ETH rates: 25% levels 1-3, 20% levels 4+"
    - "Recycled ETH rate: 5% all levels"
    - "3 payout modes: Coinflip (FLIP credit), Degenerette (pending credit), SplitCoinflipCoin (50% direct BURNIE, 50% discarded)"
    - "Multi-tier weighted roll selects one recipient for combined total (EV-preserved)"
    - "Lootbox taper: score 15000-25500 BPS linearly tapers 100%->50%, floor at 50%"
    - "Leaderboard tracks untapered amount, top affiliate rewarded at level end"
    - "Referral locking: first-come permanent, invalid codes default to VAULT"
    - "Rakeback 0-25% returned to referred player"
    - "Quest bonus added on top of base affiliate share"
  artifacts:
    - path: "../simulator/src/mechanics/affiliate.ts"
      provides: "Full 3-tier affiliate system with all payout modes"
      exports: ["payAffiliate", "createAffiliateCode", "setReferral", "getTopAffiliate", "AffiliateResult"]
    - path: "../simulator/src/types/state.ts"
      provides: "Extended affiliate state fields on PlayerState and GlobalState"
  key_links:
    - from: "../simulator/src/mechanics/affiliate.ts"
      to: "../simulator/src/types/state.ts"
      via: "affiliateLeaderboard, affiliateCode, referrerCode"
      pattern: "affiliateLeaderboard|affiliateCode|referrerCode"
    - from: "../simulator/src/mechanics/affiliate.ts"
      to: "../simulator/src/mechanics/activityScore.ts"
      via: "activity score for lootbox taper"
      pattern: "activityScore"
---

<objective>
Implement the full affiliate system: 3-tier referral with fresh/recycled rates, 3 payout modes, weighted multi-tier roll, lootbox taper, leaderboard, rakeback, referral locking, quest bonus, and top affiliate reward.

Purpose: The affiliate system is the largest single subsystem in Phase 38, touching referral economics, payout routing, and leaderboard tracking. It must be built as a cohesive module.

Output: Tested affiliate.ts module with all 8 requirement IDs covered.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@../simulator/src/types/state.ts
@../simulator/src/types/events.ts
@../simulator/src/mechanics/activityScore.ts

Solidity reference:
@contracts/DegenerusAffiliate.sol (3-tier logic, payout modes, taper, leaderboard)

<interfaces>
<!-- From DegenerusAffiliate.sol -->
Constants:
- AFFILIATE_BONUS_MAX = 50
- MAX_RAKEBACK_PCT = 25
- REWARD_SCALE_FRESH_L1_3_BPS = 2500 (25%)
- REWARD_SCALE_FRESH_L4P_BPS = 2000 (20%)
- REWARD_SCALE_RECYCLED_BPS = 500 (5%)
- BPS_DENOMINATOR = 10000
- LOOTBOX_TAPER_START_SCORE = 15000
- LOOTBOX_TAPER_END_SCORE = 25500
- LOOTBOX_TAPER_MIN_BPS = 5000 (50%)

PayoutMode enum: Coinflip=0, Degenerette=1, SplitCoinflipCoin=2

3-tier roll: when uplines exist, weighted random selects ONE winner who gets combined amount.
- Weights: affiliate (base share), upline1 (20% of base), upline2 (4% of base)
- Roll selects recipient; entire combined payout goes to winner
- EV preserved: each participant's expected payout = their weight share

Referral locking:
- First referral code set is permanent
- Invalid codes (self-referral, unknown) lock slot to VAULT sentinel
- During presale, codes can be changed (mutability window)

Leaderboard:
- Per-level tracking of affiliate earnings (untapered for leaderboard)
- Top affiliate per level gets bonus reward at level end
- Bonus points: 1pt per 1 ETH earned over last 5 levels, capped at 50

From ../simulator/src/types/state.ts:
```typescript
export interface PlayerState {
  affiliateCode: number;    // 0 = none
  referrerCode: number;     // 0 = none
  affiliateEarnings: bigint;
}

export interface GlobalState {
  affiliateLeaderboard: Map<number, bigint>; // code -> earnings
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Affiliate code registration, referral locking, and payout rate calculation</name>
  <files>../simulator/src/mechanics/affiliate.ts, ../simulator/src/mechanics/affiliate.test.ts, ../simulator/src/types/state.ts</files>
  <behavior>
    - createAffiliateCode(player, code, rakeback, payoutMode): registers code
    - setReferral(player, code, affiliateRegistry): locks referral permanently
    - Invalid code -> lock to VAULT (code 0 sentinel)
    - calculateAffiliateReward(ethAmount, level, isFreshEth): returns reward in BPS
      - Fresh: 2500 BPS (lvl 1-3), 2000 BPS (lvl 4+)
      - Recycled: 500 BPS always
    - applyLootboxTaper(rewardAmount, activityScoreBps): tapered reward
      - Score < 15000: 100% (no taper)
      - Score 15000-25500: linear 100% -> 50%
      - Score >= 25500: floor at 50%
    - payAffiliate(context): full 3-tier distribution with weighted roll
      - Roll weighted random among affiliate + up to 2 uplines
      - Winner gets combined total (EV preserved)
      - Apply payout mode: Coinflip -> creditFlip, Degenerette -> pending credit, SplitCoinflipCoin -> 50% creditCoin + 50% discarded
    - Rakeback: 0-25% of reward returned to referred player
    - Quest bonus: added on top of base share
  </behavior>
  <action>
    Extend PlayerState in state.ts with:
    - affiliatePayoutMode: number (0/1/2)
    - affiliateRakeback: number (0-25)
    - referralLocked: boolean
    - degeneretteCredit: bigint (pending credit for Degenerette payout mode)

    Extend GlobalState with:
    - affiliateRegistry: Map<number, AffiliateCodeInfo> (code -> owner info)
    - affiliateLevelEarnings: Map<string, bigint> (level:code key -> earnings for leaderboard)

    Create affiliate.ts implementing the full system:

    1. Code registration: store code -> {ownerId, rakeback, payoutMode}
    2. Referral: first-come permanent binding, invalid -> VAULT (sentinel 0)
    3. Rate calculation: fresh vs recycled, level-dependent
    4. 3-tier payout: resolve affiliate + upline1 (20%) + upline2 (4%), roll weighted random winner
    5. Payout modes: route winner's reward through correct channel
    6. Lootbox taper: linear interpolation between start/end scores
    7. Quest bonus: flat addition to base reward before distribution
    8. Leaderboard: track untapered earnings per level, expose getTopAffiliate(level)
    9. Top affiliate reward: awardTopAffiliate(level, rewardAmount) for level-end bonus

    Use Prng for weighted roll (not Math.random).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/simulator && npx vitest run src/mechanics/affiliate.test.ts</automated>
  </verify>
  <done>All 8 affiliate requirements covered: 3-tier referral, fresh/recycled rates, 3 payout modes, weighted roll, lootbox taper, leaderboard, referral locking, rakeback, quest bonus, top affiliate reward.</done>
</task>

<task type="auto">
  <name>Task 2: Barrel export and state factory updates</name>
  <files>../simulator/src/mechanics/index.ts, ../simulator/src/state/player.ts, ../simulator/src/state/global.ts</files>
  <action>
    1. Add `export * from './affiliate.js';` to mechanics/index.ts
    2. Update player factory in state/player.ts to initialize new fields:
       - affiliatePayoutMode: 0
       - affiliateRakeback: 0
       - referralLocked: false
       - degeneretteCredit: 0n
    3. Update global factory in state/global.ts to initialize:
       - affiliateRegistry: new Map()
       - affiliateLevelEarnings: new Map()
    4. Ensure TypeScript compiles cleanly with `npx tsc --noEmit`
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/simulator && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>All mechanics exported from barrel. State factories initialize affiliate fields. Full test suite passes. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
- `cd /home/zak/Dev/PurgeGame/simulator && npx vitest run` — all tests pass
- Fresh ETH reward at level 2: 25%, at level 5: 20%
- Recycled reward: 5%
- Taper at score 20250 (midpoint): ~75% of full reward
- Weighted roll preserves EV across 10000 trials
</verification>

<success_criteria>
Affiliate module covers all 8 requirement IDs. 3-tier weighted roll statistically EV-preserving. Lootbox taper linear between breakpoints. Leaderboard tracks untapered amounts. Referral locking prevents code changes. All payout modes route correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/38-extended-mechanics/38-03-SUMMARY.md`
</output>
