---
phase: 38-extended-mechanics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ../simulator/src/mechanics/degenerette.ts
  - ../simulator/src/mechanics/degenerette.test.ts
  - ../simulator/src/mechanics/index.ts
autonomous: true
requirements: [CORE-12]

must_haves:
  truths:
    - "Degenerette match-based betting resolves 0-8 matches against random result"
    - "3 currencies supported: ETH (min 0.005), BURNIE (min 100), WWXRP (min 1)"
    - "ROI curve from activity score: 90% at 0, 95% at 75%, 99.5% at 255%, 99.9% at 305%"
    - "ETH wins: 25% paid as ETH (capped 10% futurePool), 75% as lootbox rewards"
    - "Hero quadrant both-match boost, miss penalty 95%"
    - "Consolation 1 WWXRP on all-loss qualifying bets"
  artifacts:
    - path: "../simulator/src/mechanics/degenerette.ts"
      provides: "Match-based betting with 3 currencies and ROI curve"
      exports: ["resolveDegenerette", "calculateRoi", "BASE_PAYOUTS"]
  key_links:
    - from: "../simulator/src/mechanics/degenerette.ts"
      to: "../simulator/src/mechanics/activityScore.ts"
      via: "activity score BPS for ROI calculation"
      pattern: "activityScore.*BPS"
    - from: "../simulator/src/mechanics/degenerette.ts"
      to: "../simulator/src/types/state.ts"
      via: "PoolState.futurePool for ETH win cap"
      pattern: "futurePool"
---

<objective>
Implement the Degenerette match-based betting module with 3 currencies, piecewise ROI curve, hero quadrant mechanics, and ETH win capping.

Purpose: Degenerette is a core revenue sink and player engagement mechanic. Its ROI curve ties directly to activity score, creating incentive alignment.

Output: Tested degenerette.ts module added to barrel export.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@../simulator/src/mechanics/activityScore.ts
@../simulator/src/types/state.ts
@../simulator/src/types/events.ts

Solidity reference:
@contracts/modules/DegenerusGameDegeneretteModule.sol (ROI constants, base payouts, match logic)

<interfaces>
<!-- From DegenerusGameDegeneretteModule.sol -->
Constants:
- ACTIVITY_SCORE_MID_BPS = 7500 (75%)
- ACTIVITY_SCORE_HIGH_BPS = 25500 (255%)
- ACTIVITY_SCORE_MAX_BPS = 30500 (305%)
- ROI_MIN_BPS = 9000 (90%)
- ROI_MID_BPS = 9500 (95%)
- ROI_HIGH_BPS = 9950 (99.5%)
- ROI_MAX_BPS = 9990 (99.9%)
- ETH_ROI_BONUS_BPS = 500 (+5% for ETH bets)
- ETH_WIN_CAP_BPS = 1000 (10% of futurePool)
- CURRENCY_ETH = 0, CURRENCY_BURNIE = 1, CURRENCY_WWXRP = 3
- MIN_BET_ETH = 0.005 ether, MIN_BET_BURNIE = 100 ether, MIN_BET_WWXRP = 1 ether
- DEITY_PASS_ACTIVITY_BONUS_BPS = 8000
- WHALE_PASS_STREAK_FLOOR_POINTS = 50
- WHALE_PASS_MINT_COUNT_FLOOR_POINTS = 25

Base payouts at 100% ROI (matches 0-8):
0: 0x, 1: 0x, 2: 1.9x, 3: 4.75x, 4: 15x, 5: 42.5x, 6: 195x, 7: 1000x, 8: 100000x

ROI curve (piecewise linear):
- Score [0, 7500] -> ROI [9000, 9500]
- Score [7500, 25500] -> ROI [9500, 9950]
- Score [25500, 30500] -> ROI [9950, 9990]

Hero quadrant: when player ticket and result both match in a quadrant, apply boost multiplier. Miss penalty: 95% of normal payout.

Match counting: 4 quadrants, each with 2 attributes (symbol + color) = 8 total. Compare player ticket vs random result per attribute.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Degenerette ROI curve and match resolution</name>
  <files>../simulator/src/mechanics/degenerette.ts, ../simulator/src/mechanics/degenerette.test.ts</files>
  <behavior>
    - calculateRoi(activityScoreBps: number): piecewise ROI in BPS
      - 0 -> 9000, 7500 -> 9500, 25500 -> 9950, 30500 -> 9990
    - countMatches(playerTicket: number[], resultTicket: number[]): 0-8 count
      - Each of 4 quadrants has 2 attributes (symbol, color)
      - Compare element-by-element
    - calculatePayout(matches, betAmount, roiBps, currency): bigint
      - Apply base payout table scaled by ROI
      - ETH: 25% ETH (capped 10% futurePool), 75% lootbox reward
      - BURNIE/WWXRP: full payout in same currency
    - Hero quadrant: both attributes match -> boost, either misses -> 95% penalty
    - Consolation: 0 matches on qualifying bet -> 1 WWXRP
    - resolveDegenerette(bet, prng, state): DegeneretteResult
  </behavior>
  <action>
    Create degenerette.ts implementing:

    1. ROI curve: piecewise linear matching contract constants (3 segments, same as requirements)

    2. Match system: 4 quadrants x 2 attributes = 8 comparisons. Player provides 8-element ticket (4x[symbol, color]), result generated from PRNG. Count matches.

    3. Base payout table (at 100% ROI = 10000 BPS):
       ```
       [0, 0, 19000, 47500, 150000, 425000, 1950000, 10000000, 1000000000]
       ```
       (stored as BPS for bigint math: payout = betAmount * basePayout[matches] * roiBps / (10000 * 10000))

    4. ETH win handling: if currency is ETH, split 25/75 (ETH/lootbox). ETH portion capped at 10% of futurePool (ETH_WIN_CAP_BPS=1000). Excess converts to lootbox.

    5. Hero quadrant detection: for each quadrant, if both symbol AND color match, apply hero boost. If hero quadrant is active and player misses that quadrant, apply 95% penalty (multiply payout by 9500/10000).

    6. EV normalization: scale payouts so expected value = ROI (not more). This preserves house edge.

    7. Consolation: 0 matches returns {consolation: true, wwxrpReward: 1n * 10n**18n}

    Export types: DegeneretteBet, DegeneretteResult, Currency enum.

    Update mechanics/index.ts to export degenerette.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/simulator && npx vitest run src/mechanics/degenerette.test.ts</automated>
  </verify>
  <done>ROI curve matches contract at all breakpoints. Match counting correct for 0-8 matches. ETH win cap at 10% futurePool. Hero boost/penalty applied. Consolation WWXRP on 0 matches. Module exported from barrel.</done>
</task>

</tasks>

<verification>
- `cd /home/zak/Dev/PurgeGame/simulator && npx vitest run` — all tests pass
- ROI at score breakpoints: 0->9000, 7500->9500, 25500->9950, 30500->9990
- Payout at 2 matches with 100% ROI: 1.9x bet amount
- ETH payout capped at 10% of futurePool
</verification>

<success_criteria>
Degenerette module passes all tests with ROI curve matching contract constants, correct match counting, ETH win capping, hero quadrant mechanics, and consolation prizes.
</success_criteria>

<output>
After completion, create `.planning/phases/38-extended-mechanics/38-02-SUMMARY.md`
</output>
