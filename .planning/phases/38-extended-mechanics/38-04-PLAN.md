---
phase: 38-extended-mechanics
plan: 04
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - ../simulator/src/mechanics/ethClaims.ts
  - ../simulator/src/mechanics/ethClaims.test.ts
  - ../simulator/src/mechanics/gameOver.ts
  - ../simulator/src/mechanics/gameOver.test.ts
  - ../simulator/src/mechanics/index.ts
autonomous: true
requirements: [CORE-14, CORE-15]

must_haves:
  truths:
    - "Pull-pattern ETH claims use 1 wei sentinel (set to 1 not 0 after claim)"
    - "claimablePool decremented before transfer (CEI pattern)"
    - "Game-over triggers: 912-day idle at level 0, 365-day inactivity at level 1+"
    - "Terminal distribution: 10% to Decimator, remaining to next-level ticketholders"
    - "Unsold remainder -> 50/50 VAULT/DGNRS"
    - "30-day sweep: remaining above claimablePool -> 50/50 VAULT/DGNRS"
  artifacts:
    - path: "../simulator/src/mechanics/ethClaims.ts"
      provides: "Pull-pattern claim logic with 1 wei sentinel"
      exports: ["claimEth", "hasClaimable", "SENTINEL_WEI"]
    - path: "../simulator/src/mechanics/gameOver.ts"
      provides: "Game-over detection and terminal distribution"
      exports: ["checkGameOver", "distributeTerminal", "sweepRemaining"]
  key_links:
    - from: "../simulator/src/mechanics/ethClaims.ts"
      to: "../simulator/src/types/state.ts"
      via: "PlayerState.claimable and PoolState.claimablePool"
      pattern: "claimable|claimablePool"
    - from: "../simulator/src/mechanics/gameOver.ts"
      to: "../simulator/src/types/state.ts"
      via: "GlobalState.gameOver and pool distribution"
      pattern: "gameOver|futurePool|claimablePool"
---

<objective>
Implement pull-pattern ETH claims with 1 wei sentinel and full game-over mechanics including terminal distribution, timeout triggers, and 30-day sweep.

Purpose: Claims and game-over are the endgame mechanics. Claims use a gas-optimized sentinel pattern from the contracts. Game-over distributes remaining funds and terminates the simulation.

Output: Two tested modules (ethClaims.ts, gameOver.ts) covering terminal game lifecycle.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-extended-mechanics/38-01-PLAN.md

@../simulator/src/types/state.ts
@../simulator/src/types/events.ts
@../simulator/src/engine.ts

Solidity reference:
@contracts/modules/DegenerusGameJackpotModule.sol (game-over, terminal distribution)
@contracts/DegenerusGame.sol (claimableWinnings, 1 wei sentinel, timeout constants)

<interfaces>
From contracts (game-over constants):
- Level 0 timeout: 912 days
- Level 1+ inactivity timeout: 365 days
- Decimator split: 10% of terminal pool
- Next-level ticketholders get remaining 90%
- Unsold tickets remainder -> 50/50 VAULT/DGNRS
- Post-game-over 30-day sweep: excess above claimablePool -> 50/50 VAULT/DGNRS
- VRF needed for game-over (3-day fallback to historical word) — sim uses PRNG

1 wei sentinel pattern:
- claimableWinnings[player] starts at 0
- When credited: set to amount (or add to existing)
- After claim: set to 1 (not 0) — saves 15000 gas on subsequent credits (warm SSTORE)
- hasClaimable: check > 1 (not > 0)

From ../simulator/src/types/state.ts:
```typescript
export interface PlayerState {
  claimable: bigint;          // 1n = sentinel (already claimed)
  ticketHoldings: Map<number, number>;
}
export interface GlobalState {
  gameOver: boolean;
  totalDay: number;
  currentLevel: number;
  pools: PoolState;
  vaultState: VaultState;
  dgnrsReserves: DgnrsReserves;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ETH claims with 1 wei sentinel</name>
  <files>../simulator/src/mechanics/ethClaims.ts, ../simulator/src/mechanics/ethClaims.test.ts</files>
  <behavior>
    - SENTINEL_WEI = 1n
    - hasClaimable(player): returns true if claimable > 1n
    - claimableAmount(player): returns claimable - 1n (or 0n if <= 1n)
    - claimEth(player, pools): deducts from claimablePool, sets player.claimable to SENTINEL (1n), returns claimed amount
    - creditClaimable(player, amount, pools): adds to player.claimable and pools.claimablePool. If player.claimable was 0n, sets to amount + SENTINEL. If already > 0, just adds amount.
    - CEI pattern: pool decremented before "transfer" (in sim, just balance update)
  </behavior>
  <action>
    Create ethClaims.ts with:

    1. SENTINEL_WEI = 1n — exported constant
    2. hasClaimable(player: PlayerState): boolean — returns player.claimable > SENTINEL_WEI
    3. claimableAmount(player: PlayerState): bigint — returns max(0n, player.claimable - SENTINEL_WEI)
    4. creditClaimable(player: PlayerState, amount: bigint, pools: PoolState): void
       - If amount === 0n, return
       - pools.claimablePool += amount
       - If player.claimable === 0n: player.claimable = amount + SENTINEL_WEI (first credit includes sentinel)
       - Else: player.claimable += amount
    5. claimEth(player: PlayerState, pools: PoolState): bigint
       - const amount = claimableAmount(player)
       - If amount === 0n, return 0n
       - pools.claimablePool -= amount (CEI: deduct first)
       - player.claimable = SENTINEL_WEI
       - player.ethBalance += amount
       - Return amount

    Note: The sentinel means first creditClaimable adds an extra 1 wei to the stored value, and claimableAmount subtracts it. This mirrors the contract's SSTORE optimization.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/simulator && npx vitest run src/mechanics/ethClaims.test.ts</automated>
  </verify>
  <done>1 wei sentinel correctly applied. Claim sets to sentinel not zero. claimablePool tracks aggregate. CEI ordering verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Game-over detection and terminal distribution</name>
  <files>../simulator/src/mechanics/gameOver.ts, ../simulator/src/mechanics/gameOver.test.ts, ../simulator/src/mechanics/index.ts</files>
  <behavior>
    - checkGameOver(state, daysSinceLastActivity): returns GameOverReason | null
      - Level 0: 912 days -> 'idle_timeout'
      - Level 1+: 365 days -> 'inactivity_timeout'
    - distributeTerminal(state, players): distributes remaining pools
      - 10% to Decimator (return refunds to pool)
      - Remaining to next-level ticketholders (proportional to ticket count)
      - Unsold -> 50/50 VAULT/DGNRS
    - sweepRemaining(state): 30-day post-game sweep
      - Any funds above claimablePool -> 50/50 VAULT/DGNRS
    - In sim: game-over VRF simulated as PRNG (already established pattern)
  </behavior>
  <action>
    Create gameOver.ts implementing:

    1. IDLE_TIMEOUT_LVL0 = 912, INACTIVITY_TIMEOUT = 365 (in days)
    2. SWEEP_DAYS = 30

    3. checkGameOver(globalState: GlobalState, daysSinceLastPurchase: number): GameOverReason | null
       - If level === 0 && daysSinceLastPurchase >= 912: return 'idle_timeout'
       - If level > 0 && daysSinceLastPurchase >= 365: return 'inactivity_timeout'
       - Else: return null

    4. distributeTerminal(globalState: GlobalState, players: PlayerState[]): TerminalDistribution
       - Aggregate remaining: currentPool + nextPool + futurePool (everything not in claimablePool)
       - Decimator share: 10% of aggregate (in sim, Decimator refunds just return to pool since no on-chain burn)
       - Remaining after Decimator: distribute to players holding tickets for currentLevel+1
       - For each player with tickets at currentLevel+1: credit proportional share via creditClaimable
       - Any undistributed (no next-level holders) -> 50/50 VAULT/DGNRS reserves
       - Zero out nextPool, currentPool, futurePool
       - Set globalState.gameOver = true

    5. sweepRemaining(globalState: GlobalState): SweepResult
       - total = sum of all pool balances
       - excess = total - claimablePool (should be 0 or small dust)
       - If excess > 0: split 50/50 to vaultState.ethBalance and dgnrsReserves.eth
       - Return {swept: excess}

    Export GameOverReason type, TerminalDistribution result.
    Update index.ts barrel to export ethClaims and gameOver.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/simulator && npx vitest run src/mechanics/gameOver.test.ts src/mechanics/ethClaims.test.ts</automated>
  </verify>
  <done>Game-over triggers at correct timeouts. Terminal distribution splits 10% Decimator + 90% to next-level holders. Unsold -> 50/50 VAULT/DGNRS. Sweep cleans remaining. Both modules exported.</done>
</task>

</tasks>

<verification>
- `cd /home/zak/Dev/PurgeGame/simulator && npx vitest run` — all tests pass
- 1 wei sentinel: after claim, claimable === 1n (not 0n)
- Game-over at 912 days (lvl 0) and 365 days (lvl 1+)
- Terminal 10% Decimator, 90% to ticketholders
</verification>

<success_criteria>
ETH claims use 1 wei sentinel matching contract convention. Game-over detects both timeout conditions. Terminal distribution proportionally credits next-level ticketholders. Sweep moves dust to VAULT/DGNRS.
</success_criteria>

<output>
After completion, create `.planning/phases/38-extended-mechanics/38-04-SUMMARY.md`
</output>
