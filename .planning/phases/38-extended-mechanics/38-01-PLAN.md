---
phase: 38-extended-mechanics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ../simulator/src/mechanics/lootboxEv.ts
  - ../simulator/src/mechanics/lootboxEv.test.ts
  - ../simulator/src/mechanics/coinflip.ts
  - ../simulator/src/mechanics/coinflip.test.ts
  - ../simulator/src/mechanics/index.ts
autonomous: true
requirements: [CORE-10, CORE-11, CORE-27]

must_haves:
  truths:
    - "Lootbox EV multiplier returns 80% at score 0, 100% at 60%, 135% at 255%, capped at 135% above"
    - "Lootbox boost adds 5/15/25% BPS ticket quantity with 10 ETH cap"
    - "creditFlip queues BURNIE as coinflip stake for next flip day"
    - "creditCoin mints BURNIE directly to player balance"
    - "Coinflip 50/50 with tiered win payout: 5% at 1.5x, 90% at 1.78-2.15x, 5% at 2.5x"
    - "Bounty increases 1000 BURNIE per window, armed on new records"
  artifacts:
    - path: "../simulator/src/mechanics/lootboxEv.ts"
      provides: "Lootbox EV multiplier and boost calculation"
      exports: ["calculateLootboxEv", "applyLootboxBoost", "LOOTBOX_BOOST_MAX_VALUE"]
    - path: "../simulator/src/mechanics/coinflip.ts"
      provides: "Coinflip resolution and BURNIE credit routing"
      exports: ["resolveCoinflip", "creditFlip", "creditCoin", "calculateBounty"]
  key_links:
    - from: "../simulator/src/mechanics/lootboxEv.ts"
      to: "../simulator/src/mechanics/activityScore.ts"
      via: "activity score BPS input"
      pattern: "activityScore.*BPS"
    - from: "../simulator/src/mechanics/coinflip.ts"
      to: "../simulator/src/types/state.ts"
      via: "PlayerState.flipStake and burnieBalance"
      pattern: "flipStake|burnieBalance"
---

<objective>
Implement lootbox EV multiplier (with boost system) and BURNIE coinflip mechanics as standalone modules.

Purpose: These are two independent economic subsystems that other Phase 38 mechanics (Degenerette, affiliate payouts) depend on. Lootbox EV determines reward value; coinflip is the primary BURNIE sink/source.

Output: Two tested mechanics modules (lootboxEv.ts, coinflip.ts) added to barrel export.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-core-game-loop/37-05-SUMMARY.md

@../simulator/src/mechanics/activityScore.ts
@../simulator/src/types/state.ts
@../simulator/src/types/events.ts
@../simulator/src/mechanics/index.ts

Solidity references (for formula parity):
@contracts/modules/DegenerusGameMintModule.sol (lootbox EV breakpoints, boost constants)
@contracts/BurnieCoinflip.sol (coinflip payout tiers, bounty system)
@contracts/BurnieCoin.sol (creditFlip vs creditCoin distinction)

<interfaces>
<!-- Key types and contracts the executor needs -->

From ../simulator/src/types/state.ts:
```typescript
export interface PlayerState {
  ethBalance: bigint;
  burnieBalance: bigint;
  flipStake: bigint;
  activityScore: number; // BPS (0-30500)
  claimable: bigint;
  // ... other fields
}
```

From ../simulator/src/mechanics/activityScore.ts:
```typescript
export function calculateActivityScore(params: ActivityScoreParams): number;
// Returns BPS value (0-30500)
```

From contracts/DegenerusGameMintModule.sol:
- LOOTBOX_BOOST_5_BONUS_BPS = 500
- LOOTBOX_BOOST_15_BONUS_BPS = 1500
- LOOTBOX_BOOST_25_BONUS_BPS = 2500
- LOOTBOX_BOOST_MAX_VALUE = 10 ether
- LOOTBOX_BOOST_EXPIRY_DAYS = 2

From contracts/BurnieCoinflip.sol:
- MIN = 100 ether (100 BURNIE minimum)
- COINFLIP_EXTRA_MIN_PERCENT = 78
- COINFLIP_EXTRA_RANGE = 38
- COINFLIP_REWARD_MEAN_BPS = 9685
- Win tiers: 5% chance 1.5x, 90% chance ~1.78-2.15x, 5% chance 2.5x
- Bounty: +1000 BURNIE per window, armed on new biggest flip record
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lootbox EV multiplier and boost system</name>
  <files>../simulator/src/mechanics/lootboxEv.ts, ../simulator/src/mechanics/lootboxEv.test.ts</files>
  <behavior>
    - calculateLootboxEv(activityScoreBps: number): returns EV multiplier in BPS
    - Score 0 BPS -> 8000 BPS (80%)
    - Score 6000 BPS (60%) -> 10000 BPS (100%)
    - Score 15000 BPS (150%) -> interpolated between 10000 and 13500
    - Score 25500 BPS (255%) -> 13500 BPS (135%)
    - Score 30500 BPS (305%) -> 13500 BPS (135%, capped)
    - applyLootboxBoost(baseQty: number, boostTier: 0|1|2|3, ethValue: bigint): returns boosted qty
    - Tier 0 = no boost, 1 = 500 BPS, 2 = 1500 BPS, 3 = 2500 BPS
    - Boost capped at 10 ETH equivalent value
    - BURNIE lootboxes use 80% conversion rate, NOT affected by EV multiplier
    - Per-account benefit cap: 10 ETH/level tracked externally
  </behavior>
  <action>
    Create lootboxEv.ts with piecewise linear interpolation matching the contract:
    - Segment 1: score [0, 6000] -> EV [8000, 10000] (linear)
    - Segment 2: score [6000, 25500] -> EV [10000, 13500] (linear)
    - Segment 3: score >= 25500 -> EV capped at 13500

    Boost function: calculate boosted = base + (base * boostBps / 10000), then cap boosted ticket value at LOOTBOX_BOOST_MAX_VALUE (10 ETH). Export constants for boost tiers.

    BURNIE lootbox helper: calculateBurnieLootboxValue(burnieAmount, ticketPrice) returns ETH-equivalent at 80% rate.

    All calculations use bigint for ETH values, number for BPS/scores.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/simulator && npx vitest run src/mechanics/lootboxEv.test.ts</automated>
  </verify>
  <done>Lootbox EV matches contract breakpoints at all 5 test points. Boost caps at 10 ETH. BURNIE 80% rate applied correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Coinflip resolution and BURNIE credit routing</name>
  <files>../simulator/src/mechanics/coinflip.ts, ../simulator/src/mechanics/coinflip.test.ts, ../simulator/src/mechanics/index.ts</files>
  <behavior>
    - creditFlip(player, amount): adds to player.flipStake (queued for next day)
    - creditCoin(player, amount): adds to player.burnieBalance (direct mint)
    - resolveCoinflip(player, prng): 50/50 win/loss
      - Win: roll payout tier (5% -> 1.5x, 90% -> uniform [1.78x, 2.15x], 5% -> 2.5x)
      - Loss: forfeit entire stake, award 1 WWXRP consolation
    - Mean win payout approx 1.97x (verify statistically over 10000 rolls)
    - calculateBounty(currentBounty, isNewRecord): +1000 per window, armed on record
    - Minimum stake: 100 BURNIE (100e18)
  </behavior>
  <action>
    Create coinflip.ts implementing:

    1. creditFlip(player: PlayerState, amount: bigint): void — adds amount to player.flipStake
    2. creditCoin(player: PlayerState, amount: bigint, globalState: GlobalState): void — adds to player.burnieBalance, increments globalState.burnieSupply
    3. resolveCoinflip(player: PlayerState, prng: Prng): CoinflipResult —
       - Roll 0-9999: < 5000 = win, >= 5000 = loss
       - On win, roll payout tier: < 500 = 1.5x (150%), 500-9499 = variable (COINFLIP_EXTRA_MIN_PERCENT=78 + random(0,38) = 78-116% bonus, so 1.78x-2.16x), >= 9500 = 2.5x (250%)
       - Mean of variable tier: 78 + 19 = 97% bonus = 1.97x
       - Credit winnings to player.burnieBalance, deduct flipStake
       - On loss: set flipStake to 0
    4. updateBounty(currentBounty: bigint, stakeAmount: bigint, biggestFlipEver: bigint): BountyResult

    Export CoinflipResult type with { win: boolean, payout: bigint, bonusPercent: number }.

    Update index.ts barrel to include both new modules.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/simulator && npx vitest run src/mechanics/coinflip.test.ts src/mechanics/lootboxEv.test.ts</automated>
  </verify>
  <done>Coinflip 50/50 split verified. Win payout tiers match contract constants. Mean payout over 10000 trials within 5% of 1.97x. Bounty increments correctly. Both modules exported from barrel.</done>
</task>

</tasks>

<verification>
- `cd /home/zak/Dev/PurgeGame/simulator && npx vitest run` — all tests pass including new modules
- Lootbox EV at score breakpoints: 0->8000, 6000->10000, 25500->13500, 30500->13500
- Coinflip mean payout for wins statistically near 1.97x
</verification>

<success_criteria>
Two new mechanics modules (lootboxEv, coinflip) pass all tests and are exported from barrel. EV interpolation matches contract formula exactly. Coinflip payout distribution matches BurnieCoinflip.sol constants.
</success_criteria>

<output>
After completion, create `.planning/phases/38-extended-mechanics/38-01-SUMMARY.md`
</output>
