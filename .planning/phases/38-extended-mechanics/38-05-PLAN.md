---
phase: 38-extended-mechanics
plan: 05
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - ../simulator/src/mechanics/afKingRebuy.ts
  - ../simulator/src/mechanics/afKingRebuy.test.ts
  - ../simulator/src/mechanics/futureTickets.ts
  - ../simulator/src/mechanics/futureTickets.test.ts
  - ../simulator/src/mechanics/index.ts
autonomous: true
requirements: [CORE-16, CORE-17, CORE-28]

must_haves:
  truths:
    - "afKing auto-rebuy: standard 130% bonus (2.30x total), afKing 145% bonus (2.45x total)"
    - "Target level: currentLevel + random(1-4) offset"
    - "Take-profit reservation before rebuy calculation"
    - "Future tickets: 95% near k in [0,5], 5% far k in [5,50]"
    - "Floor-clamped to currentLevel"
    - "DGNRS final-day 1% reward to solo-bucket winner"
  artifacts:
    - path: "../simulator/src/mechanics/afKingRebuy.ts"
      provides: "Auto-rebuy with standard and afKing bonus tiers"
      exports: ["calculateAutoRebuy", "AUTO_REBUY_BONUS_BPS", "AFKING_AUTO_REBUY_BONUS_BPS"]
    - path: "../simulator/src/mechanics/futureTickets.ts"
      provides: "Future ticket award distribution and DGNRS reward"
      exports: ["awardFutureTickets", "awardDgnrsDayReward"]
  key_links:
    - from: "../simulator/src/mechanics/afKingRebuy.ts"
      to: "../simulator/src/mechanics/priceLookup.ts"
      via: "priceForLevel for ticket cost calculation"
      pattern: "priceForLevel"
    - from: "../simulator/src/mechanics/futureTickets.ts"
      to: "../simulator/src/types/state.ts"
      via: "PlayerState.ticketHoldings for future level awards"
      pattern: "ticketHoldings"
---

<objective>
Implement afKing auto-rebuy mechanics (with standard/afKing bonus tiers), future ticket near/far distribution, and DGNRS final-day reward.

Purpose: Auto-rebuy converts jackpot winnings into future tickets with bonus multipliers. Future ticket awards distribute tickets to levels ahead. DGNRS reward is a day-5 jackpot special.

Output: Two tested modules covering ticket rebuy, future distribution, and DGNRS reward.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@../simulator/src/types/state.ts
@../simulator/src/mechanics/priceLookup.ts
@../simulator/src/mechanics/index.ts

Solidity reference:
@contracts/modules/DegenerusGamePayoutUtils.sol (auto-rebuy calc, bonus BPS)
@contracts/modules/DegenerusGameDecimatorModule.sol (AUTO_REBUY_BONUS_BPS=13000, AFKING=14500)
@contracts/modules/DegenerusGameJackpotModule.sol (future ticket distribution, DGNRS reward)

<interfaces>
From contracts/modules/DegenerusGameDecimatorModule.sol:
- AUTO_REBUY_BONUS_BPS = 13000 (130% bonus = base + 130% = 2.30x total tickets)
- AFKING_AUTO_REBUY_BONUS_BPS = 14500 (145% bonus = base + 145% = 2.45x total tickets)
- BPS_DENOMINATOR = 10000

From contracts/modules/DegenerusGamePayoutUtils.sol:
- _calcAutoRebuy(beneficiary, weiAmount, entropy, state, currentLevel, bonusBps, bonusBpsAfKing)
- levelOffset = (entropy & 3) + 1 → 1 to 4 levels ahead
- toFuture = levelOffset > 1 (25% next level, 75% future)
- ticketPrice = PriceLookupLib.priceForLevel(targetLevel) >> 2 (quarter of level price = 1 ticket unit)
- baseTickets = rebuyAmount / ticketPrice
- bonusTickets = baseTickets * bonusBps / 10000
- takeProfit reservation: reserved = (weiAmount / takeProfit) * takeProfit, rebuyAmount = weiAmount - reserved

Future ticket awards (from jackpot module):
- 95% chance: near-future k in [0, 5] (uniform over 6 values → levels +0 to +5)
- 5% chance: far-future k in [5, 50] (uniform over 46 values → levels +5 to +50)
- Floor-clamped to currentLevel (can't award tickets for past levels)

DGNRS final-day reward:
- On jackpot day 5: 1% of DGNRS reward pool to solo-bucket winner
- Solo bucket = quadrant with smallest pool or single holder
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: afKing auto-rebuy with bonus tiers</name>
  <files>../simulator/src/mechanics/afKingRebuy.ts, ../simulator/src/mechanics/afKingRebuy.test.ts</files>
  <behavior>
    - AUTO_REBUY_BONUS_BPS = 13000 (total multiplier: (10000+13000)/10000 = 2.30x)
    - AFKING_AUTO_REBUY_BONUS_BPS = 14500 (total multiplier: (10000+14500)/10000 = 2.45x)
    - calculateAutoRebuy(params): AutoRebuyResult
      - Take-profit: reserve floor(weiAmount / takeProfit) * takeProfit, rebuy the rest
      - Target level: currentLevel + prng.nextRange(1, 5) (1 to 4 inclusive)
      - Ticket price: priceForLevel(targetLevel) / 4 (quarter = 1 unit)
      - Base tickets: floor(rebuyAmount / ticketPrice)
      - Bonus tickets: floor(baseTickets * bonusBps / 10000) where bonusBps is afKing or standard
      - Total tickets ON TOP of base: base receives from winning, bonus is additional
      - Actually per contract: bonusTickets = (baseTickets * bonusBps) / 10000, result is just bonusTickets (the base is the "purchase", bonus is the extra)
      - Wait - re-read: "bonus tickets ON TOP of base: standard 130% bonus (total 2.30x base)"
      - So: totalTickets = baseTickets * (10000 + bonusBps) / 10000. With 13000: 2.30x. With 14500: 2.45x.
      - ETH spent: baseTickets * ticketPrice. Remainder returned to claimable.
    - Return { targetLevel, totalTickets, ethSpent, reserved, remainder }
  </behavior>
  <action>
    Create afKingRebuy.ts:

    1. Export constants: AUTO_REBUY_BONUS_BPS = 13_000n, AFKING_AUTO_REBUY_BONUS_BPS = 14_500n

    2. interface AutoRebuyParams {
         weiAmount: bigint;
         currentLevel: number;
         isAfKing: boolean;
         takeProfit: bigint; // 0 = no take-profit
         prng: Prng;
       }

    3. interface AutoRebuyResult {
         targetLevel: number;
         totalTickets: number;
         ethSpent: bigint;
         reserved: bigint;
         remainder: bigint;
         toFuture: boolean; // true if target > currentLevel+1
       }

    4. calculateAutoRebuy(params): AutoRebuyResult
       - reserved = takeProfit > 0 ? (weiAmount / takeProfit) * takeProfit : 0n
       - rebuyAmount = weiAmount - reserved
       - levelOffset = prng.nextRange(1, 5) // 1, 2, 3, or 4
       - targetLevel = currentLevel + levelOffset
       - toFuture = levelOffset > 1
       - ticketPrice = priceForLevel(targetLevel) / 4n (bigint division)
       - If ticketPrice === 0n: return zeroed result
       - baseTickets = rebuyAmount / ticketPrice
       - bonusBps = isAfKing ? 14500n : 13000n
       - totalTickets = Number((baseTickets * (10000n + bonusBps)) / 10000n)
       - ethSpent = baseTickets * ticketPrice
       - remainder = rebuyAmount - ethSpent
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/simulator && npx vitest run src/mechanics/afKingRebuy.test.ts</automated>
  </verify>
  <done>Standard rebuy yields 2.30x tickets. afKing yields 2.45x. Take-profit correctly reserved. Target level offset 1-4 from PRNG. ETH dust returned as remainder.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Future ticket distribution and DGNRS day-5 reward</name>
  <files>../simulator/src/mechanics/futureTickets.ts, ../simulator/src/mechanics/futureTickets.test.ts, ../simulator/src/mechanics/index.ts</files>
  <behavior>
    - awardFutureTickets(player, currentLevel, prng): awards tickets to a future level
      - 95% near: k = prng.nextRange(0, 6) -> level = currentLevel + k, floor-clamped
      - 5% far: k = prng.nextRange(5, 51) -> level = currentLevel + k
      - Tickets added to player.ticketHoldings
    - awardDgnrsDayReward(globalState, players, traitBuckets): awards 1% of DGNRS pool
      - Only on day 5 of jackpot
      - Solo bucket: smallest bucket by ticket count
      - Winner: random holder from solo bucket
      - 1% of dgnrsReserves.eth awarded
    - NEAR_FUTURE_CHANCE_BPS = 9500 (95%)
    - NEAR_FUTURE_RANGE = 6 (0 to 5 inclusive)
    - FAR_FUTURE_RANGE_START = 5, FAR_FUTURE_RANGE_END = 51 (5 to 50 inclusive)
  </behavior>
  <action>
    Create futureTickets.ts:

    1. awardFutureTickets(player, ticketQty, currentLevel, prng):
       - Roll: prng.nextRange(0, 10000) < 9500 ? 'near' : 'far'
       - Near: offset = prng.nextRange(0, 6) (uniform 0-5)
       - Far: offset = prng.nextRange(5, 51) (uniform 5-50)
       - targetLevel = max(currentLevel, currentLevel + offset) (floor clamp — but offset >= 0, so just currentLevel + offset)
       - Actually floor-clamped means it can't go below currentLevel. Since offset >= 0, this is automatic.
       - Add ticketQty to player.ticketHoldings.get(targetLevel)
       - Return { targetLevel, isNear, ticketQty }

    2. awardDgnrsDayReward(globalState, players, traitBuckets, prng):
       - budget = globalState.dgnrsReserves.eth / 100n (1%)
       - If budget === 0n, return null
       - Find solo bucket: bucket with fewest totalTickets
       - Pick random holder from solo bucket (weighted by ticket count)
       - Credit winner via claimable += budget, pools.claimablePool += budget
       - Deduct from dgnrsReserves.eth
       - Return { winnerId, amount }

    Update index.ts to export afKingRebuy and futureTickets.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/simulator && npx vitest run src/mechanics/futureTickets.test.ts src/mechanics/afKingRebuy.test.ts</automated>
  </verify>
  <done>Future tickets: 95% near (0-5), 5% far (5-50) verified statistically. DGNRS 1% reward to solo bucket winner. Both modules exported from barrel.</done>
</task>

</tasks>

<verification>
- `cd /home/zak/Dev/PurgeGame/simulator && npx vitest run` — all tests pass
- Auto-rebuy at 1 ETH with standard bonus: ~2.30x ticket count
- Future ticket distribution: statistical test over 10000 trials shows ~95% near, ~5% far
- DGNRS reward: 1% of reserves awarded
</verification>

<success_criteria>
Auto-rebuy bonus multipliers match contract constants exactly. Future ticket near/far split matches 95/5 probability. DGNRS final-day reward correctly picks solo bucket winner.
</success_criteria>

<output>
After completion, create `.planning/phases/38-extended-mechanics/38-05-SUMMARY.md`
</output>
