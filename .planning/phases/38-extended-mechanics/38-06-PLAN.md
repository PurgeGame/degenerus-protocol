---
phase: 38-extended-mechanics
plan: 06
type: execute
wave: 3
depends_on: ["38-01", "38-02", "38-03", "38-04", "38-05"]
files_modified:
  - ../simulator/src/engine.ts
  - ../simulator/src/engine.test.ts
  - ../simulator/src/types/state.ts
autonomous: false
requirements: [CORE-10, CORE-11, CORE-12, CORE-13, CORE-14, CORE-15, CORE-16, CORE-17, CORE-18, CORE-19, CORE-20, CORE-21, CORE-22, CORE-23, CORE-27, CORE-28, CORE-30]

must_haves:
  truths:
    - "Engine purchase phase triggers lootbox decisions and affiliate payouts"
    - "Engine processes coinflip resolution on each day (queued stakes resolved)"
    - "Degenerette bets placed and resolved within engine tick"
    - "Affiliate payouts routed through correct mode on each purchase"
    - "Game-over detection runs each day and triggers terminal distribution"
    - "afKing rebuy converts jackpot winnings to future tickets during jackpot phase"
    - "Future tickets awarded during carryover/BURNIE jackpot phases"
    - "DGNRS day-5 reward distributed during jackpot day 5"
    - "ETH claims available to players during engine run"
    - "All Phase 38 events emitted for downstream visualization"
  artifacts:
    - path: "../simulator/src/engine.ts"
      provides: "Full engine orchestration of all Phase 38 mechanics"
    - path: "../simulator/src/engine.test.ts"
      provides: "Integration tests for extended mechanics in engine loop"
  key_links:
    - from: "../simulator/src/engine.ts"
      to: "../simulator/src/mechanics/lootboxEv.ts"
      via: "calculateLootboxEv called during lootbox open"
      pattern: "calculateLootboxEv"
    - from: "../simulator/src/engine.ts"
      to: "../simulator/src/mechanics/coinflip.ts"
      via: "resolveCoinflip called during daily coinflip resolution"
      pattern: "resolveCoinflip"
    - from: "../simulator/src/engine.ts"
      to: "../simulator/src/mechanics/degenerette.ts"
      via: "resolveDegenerette called for player bets"
      pattern: "resolveDegenerette"
    - from: "../simulator/src/engine.ts"
      to: "../simulator/src/mechanics/affiliate.ts"
      via: "payAffiliate called on purchase/lootbox events"
      pattern: "payAffiliate"
    - from: "../simulator/src/engine.ts"
      to: "../simulator/src/mechanics/gameOver.ts"
      via: "checkGameOver called each tick"
      pattern: "checkGameOver"
    - from: "../simulator/src/engine.ts"
      to: "../simulator/src/mechanics/afKingRebuy.ts"
      via: "calculateAutoRebuy during jackpot winnings"
      pattern: "calculateAutoRebuy"
---

<objective>
Wire all Phase 38 mechanics modules into the engine's day-tick loop, adding lootbox decisions, coinflip resolution, Degenerette bets, affiliate payouts, ETH claims, game-over detection, afKing rebuy, future ticket awards, and DGNRS reward.

Purpose: This is the integration plan. All 6 modules from plans 01-05 are independently tested; now they need to be orchestrated within the engine's purchase/jackpot cycle. This plan also adds a human-verify checkpoint to confirm the integrated engine runs correctly.

Output: Updated engine.ts with all Phase 38 mechanics wired in, integration tests, and human verification.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-core-game-loop/37-05-SUMMARY.md

@../simulator/src/engine.ts
@../simulator/src/types/state.ts
@../simulator/src/types/events.ts
@../simulator/src/types/config.ts
@../simulator/src/mechanics/index.ts

<!-- Prior plan SUMMARYs needed for wiring -->
Phase 38 plans 01-05 create these modules:
- lootboxEv.ts: calculateLootboxEv, applyLootboxBoost
- coinflip.ts: resolveCoinflip, creditFlip, creditCoin
- degenerette.ts: resolveDegenerette, calculateRoi
- affiliate.ts: payAffiliate, createAffiliateCode, setReferral, getTopAffiliate
- ethClaims.ts: claimEth, creditClaimable, hasClaimable
- gameOver.ts: checkGameOver, distributeTerminal, sweepRemaining
- afKingRebuy.ts: calculateAutoRebuy
- futureTickets.ts: awardFutureTickets, awardDgnrsDayReward
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire all Phase 38 mechanics into engine day-tick loop</name>
  <files>../simulator/src/engine.ts, ../simulator/src/types/state.ts</files>
  <action>
    Update engine.ts to import and orchestrate all Phase 38 modules. Add these to the day-tick loop:

    **Purchase phase (stepPurchase) additions:**

    1. After ticket purchase: call payAffiliate if player has referrer
       - Calculate affiliate reward from ticket cost
       - Route through affiliate's payout mode
       - Emit 'affiliate_payout' event

    2. After purchase decisions: lootbox decision per player
       - Some players (based on archetype probability) open lootboxes
       - Calculate EV using calculateLootboxEv(player.activityScore)
       - Apply boost if deity boon active (applyLootboxBoost)
       - Route lootbox ETH: 10% next / 90% future (non-presale)
       - Emit 'lootbox_purchase' event

    3. Coinflip resolution: once per day at end of purchase step
       - For each player with flipStake > 0: resolveCoinflip(player, prng)
       - Credit winnings or forfeit stake
       - Update bounty tracking
       - Emit 'coinflip_result' event

    4. Degenerette: some players place bets (archetype-based probability)
       - Generate bet ticket, resolve against random result
       - Route payouts by currency type
       - ETH wins: 25% ETH (capped), 75% lootbox
       - Emit 'degenerette_result' event

    5. ETH claims: players periodically claim (e.g., every 5th day or when claimable > threshold)
       - Call claimEth for eligible players
       - Emit 'eth_claim' event

    6. Game-over check: at end of each day
       - Track daysSinceLastPurchase (increment if no purchases happened)
       - Call checkGameOver(state, daysSinceLastPurchase)
       - If triggered: distributeTerminal, set done=true
       - Emit 'game_over' event

    **Jackpot phase (stepJackpot) additions:**

    7. afKing rebuy: after crediting jackpot winners
       - For winners with auto-rebuy enabled: calculateAutoRebuy
       - Add bonus tickets to ticketHoldings at target level
       - Emit auto_rebuy event (use 'pool_transfer' with type='auto_rebuy')

    8. Future ticket awards: during carryover (days 2-4) and BURNIE jackpot
       - Call awardFutureTickets for selected winners
       - Track awarded tickets in ticketHoldings

    9. DGNRS day-5 reward: on jackpot day 5
       - Call awardDgnrsDayReward with solo bucket
       - Emit event

    **State updates:**

    Add to GlobalState: daysSinceLastPurchase: number (initialize to 0)
    Add to PlayerState: autoRebuyEnabled: boolean, isAfKing: boolean, takeProfit: bigint, lootboxBenefitThisLevel: bigint (for 10 ETH cap tracking)

    Update state factories for new fields.

    **Archetype-based decision probabilities** (placeholders for Phase 40 profiles):
    - Lootbox open: degen 0.6, evMaximizer 0.3 (waits for high score), whale 0.4, hybrid 0.4
    - Degenerette: degen 0.5, evMaximizer 0.1, whale 0.3, hybrid 0.2
    - Coinflip deposit: degen 0.3, evMaximizer 0.5, whale 0.6, hybrid 0.3
    - Claim frequency: every 5 days for simplicity (Phase 40 adds strategy)

    Keep the engine readable — extract helper functions for each mechanic's integration (e.g., processLootboxes, processCoinflips, processDegenerette, processAffiliatePayouts).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/simulator && npx vitest run</automated>
  </verify>
  <done>Engine compiles, all existing tests pass, new mechanics wired into day-tick loop with events emitted for each subsystem.</done>
</task>

<task type="auto">
  <name>Task 2: Integration tests for extended mechanics in engine</name>
  <files>../simulator/src/engine.test.ts</files>
  <action>
    Add integration tests to engine.test.ts verifying Phase 38 mechanics work within the engine:

    1. "coinflip events emitted during simulation" — run 5 levels, verify coinflip_result events exist with win/loss outcomes
    2. "lootbox purchases route to pools correctly" — verify lootbox_purchase events, check futurePool grew from lootbox contributions
    3. "affiliate payouts occur on purchases" — set up players with referrals, verify affiliate_payout events with correct tiers
    4. "degenerette results include all 3 currencies" — verify degenerette_result events
    5. "game-over triggers on inactivity" — create engine with levelCount=0, simulate 365+ days of inactivity at level 1, verify game_over event
    6. "afKing rebuy creates future tickets" — verify auto_rebuy events during jackpot phase
    7. "DGNRS day-5 reward emitted" — verify event on jackpot day 5
    8. "ETH claims reduce claimable to sentinel" — verify players can claim and claimable resets to 1n
    9. "full simulation with extended mechanics completes" — run 3 levels with all mechanics, verify no errors, events emitted for each subsystem, final state has BURNIE supply > 0, some affiliate earnings > 0
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/simulator && npx vitest run src/engine.test.ts</automated>
  </verify>
  <done>9+ integration tests pass covering all Phase 38 subsystems within the engine loop.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 38 engine integration</name>
  <files>../simulator/src/engine.ts</files>
  <action>Human verification of the complete Phase 38 integration.</action>
  <what-built>Complete Phase 38 engine integration — all extended mechanics (lootbox EV, coinflip, Degenerette, affiliate, claims, game-over, afKing rebuy, future tickets, DGNRS reward) wired into the simulation engine.</what-built>
  <how-to-verify>
    1. cd /home/zak/Dev/PurgeGame/simulator && npx vitest run — all tests pass (should be 250+ tests)
    2. cd /home/zak/Dev/PurgeGame/simulator && npx tsc --noEmit — no type errors
    3. Spot-check engine.ts: verify it imports and calls all 8 mechanics modules
    4. Verify events: run simulation with 5 levels and check that events include ticket_purchase, lootbox_purchase, coinflip_result, degenerette_result, affiliate_payout, jackpot_draw, level_advance
  </how-to-verify>
  <verify>Human inspects test output and engine imports</verify>
  <done>All Phase 38 mechanics confirmed working in integrated engine.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cd /home/zak/Dev/PurgeGame/simulator && npx vitest run` — all tests pass
- `cd /home/zak/Dev/PurgeGame/simulator && npx tsc --noEmit` — no type errors
- Event types emitted: all Phase 38 event types present in a 5-level simulation
</verification>

<success_criteria>
All Phase 38 mechanics integrated into engine day-tick loop. 9+ integration tests pass. All event types emitted. Full test suite (250+ tests) passes. TypeScript compiles cleanly. Engine runs a complete multi-level simulation with extended mechanics without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/38-extended-mechanics/38-06-SUMMARY.md`
</output>
