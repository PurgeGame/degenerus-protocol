---
phase: 354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/DegenerusQuests.sol
  - contracts/interfaces/IDegenerusQuests.sol
autonomous: false
requirements: [QST-01, QST-02, QST-03, QST-04, QST-05]
must_haves:
  truths:
    - "A new onlyGame-gated batched-settle entrypoint exists in DegenerusQuests for the GAME-context afking quest settle"
    - "The entrypoint calls _questSyncState first and touches only slot-0 / streak / shield state (NEVER slot-1: progress[1], lastProgressDay[1], lastQuestVersion[1])"
    - "The QST-03 double-credit guard reads the quest-core lastCompletedDay + the completionMask bit-7 STREAK_CREDITED bit (the per-day in-core gate); the GAME-side afkCoveredThroughDay (354-01/354-03) decides WHICH days are delivered — the two layers together credit streak at most once per delivered day"
    - "The O1 double-credit is fixed: the internal creditFlip at DegenerusQuests.sol:890 is removed; lootboxReward stays in the :893 return so the caller credits it exactly once"
    - "The dead handleLootBox function + its IDegenerusQuests interface entry are removed"
    - "The manual/bingo/degenerette/boon callers produce byte-identical PlayerQuestState with the entrypoint present vs absent"
  artifacts:
    - path: "contracts/DegenerusQuests.sol"
      provides: "onlyGame afking-settle entrypoint + O1 fix + dead-code removal"
      contains: "onlyGame"
    - path: "contracts/interfaces/IDegenerusQuests.sol"
      provides: "interface with handleLootBox removed + the new settle entrypoint added"
  key_links:
    - from: "GameAfkingModule._settleQuest (Wave 2, 354-03)"
      to: "DegenerusQuests new onlyGame settle entrypoint"
      via: "GAME-context call on the global settle day / claimQuest fallback / unsub"
      pattern: "awardQuestStreakBonus|_questSyncState"
    - from: "GameAfkingModule afkCoveredThroughDay (354-01 field / 354-03 advance)"
      to: "DegenerusQuests entrypoint lastCompletedDay + completionMask STREAK_CREDITED bit"
      via: "the GAME-side delivered-day count + the in-core per-day STREAK_CREDITED guard together credit streak at most once per delivered day (QST-03)"
      pattern: "lastCompletedDay|completionMask"
---

<objective>
Prepare the shared `DegenerusQuests` core for the v56 afking batched-settle: add the NEW `onlyGame`-gated streak-settle entrypoint (the contract Wave 2's `_settleQuest` calls), fix the pre-existing O1 lootbox-quest double-credit at the SOURCE (QST-05), and remove the dead `handleLootBox` function + its interface entry. This file is INDEPENDENT of the storage re-pack (354-01) — it has its own state — so it runs in Wave 1 in parallel.

Purpose: the entrypoint must be PROVABLY non-perturbing to the manual/bingo/degenerette/boon callers (QST-04) — it touches only the same slot-0/streak/shield fields `awardQuestStreakBonus` + `_questSyncState` already write, never slot-1. The O1 fix routes the lootbox-quest reward through exactly ONE creditFlip (QST-05), inheriting the post-fix invariant the Wave 2 afking settle relies on.
Output: the shared-core entrypoint + the O1 fix + the dead-code removal, all compiling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

@.planning/phases/353-spec-design-lock-unmanipulable-solvency-re-attestation-xmode/353-SPEC.md

<interfaces>
<!-- The exact current DegenerusQuests anchors the executor edits. DO NOT re-explore the codebase. -->

// awardQuestStreakBonus (:365-384) — the onlyGame streak-add the new entrypoint MIRRORS:
function awardQuestStreakBonus(address player, uint16 amount, uint32 currentDay) external onlyGame {
    // returns early on player==0 || amount==0 || currentDay==0
    // state = questPlayerState[player]; _questSyncState(state, player, currentDay);
    // state.streak += amount (clamped uint24); advances lastActiveDay; emits QuestStreakBonusAwarded
}

// _questSyncState (:1285-1318) — the day-reset/anchor logic the entrypoint MUST call first:
function _questSyncState(PlayerQuestState storage state, address player, uint32 currentDay) private {
    // anchorDay = lastActiveDay != 0 ? lastActiveDay : lastCompletedDay
    // gap-reset (:1306): zeroes state.streak when currentDay > anchorDay + 1 unless shields cover
    // resets completionMask + snapshots baseStreak when lastSyncDay != currentDay
}

// PlayerQuestState struct fields (:264-274):
//   uint24 lastCompletedDay; lastActiveDay; streak; baseStreak; lastSyncDay;
//   uint24[2] lastProgressDay; uint24[2] lastQuestVersion; uint128[2] progress; uint8 completionMask;
// SLOT-1 = index 1 of the [2] arrays — the player's MANUAL quest, MUST NOT be touched by the entrypoint.
// completionMask bit-7 STREAK_CREDITED — the per-day "streak already credited this day" guard the entrypoint reads.

// onlyGame modifier (:321-324): if (msg.sender != ContractAddresses.GAME) revert OnlyGame();

// O1 site — handlePurchase tail (:886-897):
//   if (burnieMintReward != 0) coinflip.creditFlip(player, burnieMintReward);   // :887 KEEP
//   if (lootboxReward != 0) coinflip.creditFlip(player, lootboxReward);          // :890 DROP (the duplicate)
//   uint256 totalReturned = ethMintReward + lootboxReward;                       // :893 KEEP
// MintModule caller already re-credits the return: lootboxFlipCredit += questReward (:1232) →
//   batched coinflip.creditFlip(buyer, lootboxFlipCredit) (:1366-1367).

// Dead code: handleLootBox (:698-741, internal creditFlip :739, NO production caller) +
//   IDegenerusQuests.sol:107 (the interface decl) + :126 (a comment ref).
// QUEST_SLOT0_REWARD = 100 ether (:144); slot-0 completion = MINT_ETH per delivered day.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the onlyGame afking batched-settle entrypoint (QST-01/02/03/04)</name>
  <files>contracts/DegenerusQuests.sol, contracts/interfaces/IDegenerusQuests.sol</files>
  <behavior>
    - With the entrypoint present, an afk-covered settle for a player advances `state.streak` exactly as `awardQuestStreakBonus` would, via `_questSyncState`-first day-reset logic.
    - The entrypoint touches ONLY slot-0 / streak / lastActiveDay / lastCompletedDay / lastSyncDay / shield state — `progress[1]`, `lastProgressDay[1]`, `lastQuestVersion[1]` are byte-unchanged before vs after.
    - For any interleaving of manual/bingo/degenerette/boon calls, the resulting `PlayerQuestState` is byte-identical with the entrypoint present vs absent (QST-04 non-perturbation — proven at TST-356).
    - Double-credit guard (QST-03): a streak credit on an afk-covered day is applied at most once. The IN-CORE guard READS the quest-core `lastCompletedDay` + the `completionMask` bit-7 STREAK_CREDITED bit (per-day "streak already credited this day" guard); the GAME-side `afkCoveredThroughDay` (354-01 field, advanced debit-gated in 354-03) decides WHICH days were delivered and is reflected in the `deliveredStreakDays` count the caller passes. The guard gates streak advancement only, NEVER slot-reward accrual.
  </behavior>
  <read_first>
    `contracts/DegenerusQuests.sol:264-274` (PlayerQuestState — note `lastCompletedDay` + `completionMask`), `:321-324` (onlyGame), `:365-384` (awardQuestStreakBonus — the shape to mirror), `:1285-1318` (_questSyncState — call it first; note the gap-reset at :1306 + the active-pass anti-reset requirement), `:1550-1600` (lastCompletedDay write at :1596 + QUEST_SLOT0_REWARD at :1600 + the completionMask bit-7 STREAK_CREDITED set/read). SPEC `## QST` (353-SPEC.md:206-238) — the batched-settle entrypoint shape, the non-perturbation approach (QST-04), the delivered-day streak gate (QST-02/03), the QST-03 double-credit guard naming "the `lastCompletedDay` / `afkCoveredThroughDay` guard gates `state.streak` advancement" (353-SPEC.md:237), and the active-pass anti-reset (353-SPEC.md:238). SPEC AFF-PULL guardrail 3 (353-SPEC.md:314) — preserve the `lastCompletedDay` write + ordering. SPEC `## Threat Model Re-Attestation` QST-04 row (353-SPEC.md:166).
  </read_first>
  <action>
    Add a NEW `onlyGame`-gated entrypoint to `DegenerusQuests.sol` (signature shaped for the GAME-context afking settle — e.g. `settleAfkingQuest(address player, uint16 deliveredStreakDays, uint32 currentDay)` or the minimal set the Wave-2 `_settleQuest` needs; choose the parameters that let GameAfkingModule pass the per-sub delivered-day streak advance + the current day). The entrypoint MUST: (1) be `onlyGame` (admits GAME only, `:321-324`); (2) call `_questSyncState(state, player, currentDay)` FIRST (mirroring `awardQuestStreakBonus:369`) so it respects the SAME day-reset / anchor / shield logic; (3) advance `state.streak` for DELIVERED days only (QST-02 — the caller passes the delivered-day count `deliveredStreakDays`; this entrypoint does NOT itself decide deliveredness — that GAME-side gate lives in GameAfkingModule's debit-gated `afkCoveredThroughDay` (354-01 field / 354-03 advance), which is the monotone delivered-day high-water mark that bounds the count passed in), clamped uint24 like `awardQuestStreakBonus`; (4) apply the QST-03 double-credit guard IN-CORE — READ the quest-core `lastCompletedDay` + the `completionMask` bit-7 STREAK_CREDITED bit (the existing per-day "streak already credited this day" mechanism) so a streak credit on an afk-covered day is applied at most once even if a manual completion already credited that day (NEVER suppress slot rewards, only the duplicate streak credit). This in-core `lastCompletedDay`/STREAK_CREDITED read is the implementation path for the SPEC QST-03 "the `lastCompletedDay` / `afkCoveredThroughDay` guard gates `state.streak` advancement" (353-SPEC.md:237): the GAME-side `afkCoveredThroughDay` decides the delivered-day COUNT, the in-core `lastCompletedDay`/`completionMask` STREAK_CREDITED bit prevents a per-day double-credit when a path already touched that day; (5) implement the active-pass anti-reset (QST-03) by advancing `lastActiveDay`/`lastCompletedDay` to the last afk-covered delivered day so `_questSyncState`'s anchorDay stays current (NO shield-balance accounting, NO daily write). CRITICAL non-perturbation guard (QST-04): the entrypoint MUST NOT touch `progress[1]`, `lastProgressDay[1]`, `lastQuestVersion[1]` (slot-1 = the player's manual quest, QST-01) — only the streak/slot-0/shield/anchor fields the existing handlers already write. PRESERVE the `lastCompletedDay` write + its ordering exactly (AFF-PULL guardrail 3 — the v56 refactor changes WHEN BURNIE mints, never WHAT streak/day is recorded). Note: the actual `creditFlip` of `questProgress × QUEST_SLOT0_REWARD` + `buyerOwedBurnie` is minted by GameAfkingModule's `_settleQuest` (354-03), not here — this entrypoint is the streak-machinery half of the settle. Add the entrypoint's declaration to `IDegenerusQuests.sol`.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The new entrypoint exists in `DegenerusQuests.sol`, is declared `onlyGame`, and calls `_questSyncState` before mutating streak (grep shows both `onlyGame` and `_questSyncState` in the new function body).
    - The new function body READS the per-day double-credit guard: grep shows the new entrypoint references `lastCompletedDay` and the `completionMask` STREAK_CREDITED bit (the QST-03 in-core guard).
    - The new function body contains ZERO references to `progress[1]`, `lastProgressDay[1]`, or `lastQuestVersion[1]` (slot-1 untouched — QST-04).
    - The entrypoint is declared in `contracts/interfaces/IDegenerusQuests.sol`.
    - `forge build` exits 0.
  </acceptance_criteria>
  <done>The onlyGame afking-settle entrypoint compiles, mirrors awardQuestStreakBonus + _questSyncState semantics, touches only slot-0/streak/shield/anchor state, reads the lastCompletedDay/completionMask STREAK_CREDITED per-day guard for QST-03, and is wired into the interface.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the O1 lootbox-quest double-credit (QST-05) + remove dead handleLootBox</name>
  <files>contracts/DegenerusQuests.sol, contracts/interfaces/IDegenerusQuests.sol</files>
  <read_first>
    `contracts/DegenerusQuests.sol:886-897` (the O1 site — the burnie credit :887 KEEP, the lootbox credit :890 DROP, the totalReturned :893 KEEP), `:698-741` (the dead `handleLootBox`, internal creditFlip :739). `contracts/interfaces/IDegenerusQuests.sol:107` (the `handleLootBox` decl) + `:126` (a comment ref). SPEC `## QST-05 O1 Fix + Dead-Code Removal` (353-SPEC.md:240-246) — drop :890, keep :887 + :893; the MintModule caller already re-credits the return (`lootboxFlipCredit += questReward :1232` → batched `creditFlip :1366-1367`); the v56 nuance (the afking per-buy `handlePurchase` call disappears under the aggregator, so the afking SETTLE in 354-03 must route the deferred lootbox-quest reward through exactly ONE creditFlip — inheriting this post-fix invariant). SPEC Anchor Attestation A11 (353-SPEC.md:30) + A19 (353-SPEC.md:38) — the exact drift-corrected line numbers.
  </read_first>
  <action>
    O1 fix (QST-05): DROP the internal `coinflip.creditFlip(player, lootboxReward)` at `DegenerusQuests.sol:890` (inside the `if (lootboxReward != 0)` at `:889` — remove that whole guarded internal credit). KEEP `lootboxReward` in the return `totalReturned = ethMintReward + lootboxReward` at `:893` so the caller's single batched `creditFlip` pays it EXACTLY ONCE. KEEP the burnie credit at `:887` (always internal-only, correct). This is non-perturbing to the MintModule caller — it already re-credits the return (`lootboxFlipCredit += questReward :1232` → batched `creditFlip(buyer, lootboxFlipCredit) :1366-1367`); dropping `:890` removes the duplicate, leaving the lootbox reward credited once via `:1367`. Dead-code removal (D-05): remove the `handleLootBox` function (`:698-741`, its internal creditFlip `:739` is unreachable — ZERO production callers), the interface entry (`IDegenerusQuests.sol:107`), the interface comment ref (`:126`), and the access-control tests for it. Pre-launch redeploy-fresh accepts the interface break.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The internal `creditFlip(player, lootboxReward)` that was at `DegenerusQuests.sol:890` is removed (grep for the lootbox-leg internal creditFlip inside `handlePurchase` returns zero — only the burnie-leg credit remains).
    - `grep -n "totalReturned = ethMintReward + lootboxReward" contracts/DegenerusQuests.sol` still present (the return keeps lootboxReward).
    - `grep -c "function handleLootBox" contracts/DegenerusQuests.sol` returns 0; `grep -c "handleLootBox" contracts/interfaces/IDegenerusQuests.sol` returns 0.
    - `forge build` exits 0.
  </acceptance_criteria>
  <done>The O1 duplicate credit is removed at the source (lootbox reward credited once via the caller's batched flip), and the dead handleLootBox is gone from both the contract and the interface; forge build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GAME → DegenerusQuests | the onlyGame settle entrypoint is the only new cross-contract write to the shared quest core; all other callers (bingo/degenerette/boon/manual) must be unperturbed |

## STRIDE Threat Register (this plan's slice of the SPEC Threat Model Re-Attestation)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-354-02-QST04 | Tampering (state perturbation) | the new onlyGame entrypoint vs the manual/bingo/degenerette/boon callers | mitigate | SPEC QST-04 row (353-SPEC.md:166/231): the entrypoint adds writes ONLY to the streak/slot-0/anchor/shield fields the existing handlers already write; it calls `_questSyncState` first (same day-reset) and NEVER touches slot-1. **TST-356 QST-04:** byte-identical PlayerQuestState with the entrypoint present vs absent for any interleaving. |
| T-354-02-QST05-O1 | Repudiation (over-mint) | the lootbox-quest double-credit at :890 | mitigate | SPEC QST-05 (353-SPEC.md:163/242-244): drop :890; the caller credits once via :1367. **TST-356 SEC-01 no-double-credit (O1):** a completed lootbox quest credits exactly once on BOTH the manual and the afking-settle paths. |
| T-354-02-QST02 | Elevation (streak-dodge) | streak advancement on non-delivered days | mitigate | SPEC QST-02/03 (353-SPEC.md:162/236): the entrypoint advances streak for DELIVERED days only (the debit-gated count is bounded by GameAfkingModule's `afkCoveredThroughDay` in 354-03); the in-core `lastCompletedDay`/`completionMask` STREAK_CREDITED bit prevents a per-day double-credit; the delivered-day gate (XMODEL C3-a fix) stays in force. **TST-356:** an N-funded-day-skip sub == a manual buyer missing N days. |

No package installs in this plan (no `T-{phase}-SC` row).
</threat_model>

<verification>
- `forge build` clean.
- The new entrypoint is onlyGame, calls _questSyncState first, touches only streak/slot-0/anchor/shield (never slot-1), and reads the lastCompletedDay/completionMask STREAK_CREDITED per-day guard (QST-03).
- The O1 :890 internal credit is removed; the :893 return keeps lootboxReward.
- handleLootBox is removed from both the contract and the interface.
</verification>

<success_criteria>
The shared-core afking settle entrypoint compiles and is provably non-perturbing (QST-04), the O1 double-credit is fixed at the source (QST-05), the streak machinery respects the delivered-day gate via the GAME-side afkCoveredThroughDay count + the in-core lastCompletedDay/STREAK_CREDITED per-day guard (QST-02/03), and the dead handleLootBox is removed. Wave 2's `_settleQuest` (354-03) can call this entrypoint and inherit the single-creditFlip invariant.
</success_criteria>

<output>
Create `.planning/phases/354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre/354-02-SUMMARY.md` when done.
</output>
