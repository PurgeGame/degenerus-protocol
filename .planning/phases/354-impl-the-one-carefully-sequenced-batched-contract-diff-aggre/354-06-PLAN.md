---
phase: 354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre
plan: 06
type: execute
wave: 4
depends_on: ["354-01", "354-02", "354-03", "354-04", "354-05"]
files_modified: []
autonomous: false
requirements: [AGG-01, AGG-02, AGG-03, AGG-04, AGG-05, TKT-01, TKT-02, QST-01, QST-02, QST-03, QST-04, QST-05, OPEN-01, OPEN-02]
must_haves:
  truths:
    - "forge build is clean across the WHOLE batched diff (the single reconciled contracts diff compiles)"
    - "The diff was authored producer-before-consumer (accumulator storage then aggregator accrue/settle then DegenerusQuests entrypoint then ticket primitive then affiliate PULL then open re-verify then wiring)"
    - "Every one of the 14 IMPL requirements has its contract site present in the working tree"
    - "The SOLVENCY-01 ETH/claimablePool debit at GameAfkingModule.sol lines 709-710 is byte-unchanged vs 453f8073"
    - "NO contracts file is auto-committed — the diff is HELD at the boundary for explicit USER hand-review"
  artifacts:
    - path: ".planning/phases/354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre/354-06-DIFF-REVIEW.md"
      provides: "the per-requirement contract-site checklist + the SOLVENCY-01 byte-unchanged attestation for the USER hand-review"
  key_links:
    - from: "the whole batched contracts diff"
      to: "the USER hand-review-and-commit gate"
      via: "autonomous:false checkpoint — never auto-commit contracts"
      pattern: "git diff 453f8073 HEAD contracts"
---

<objective>
The single dedicated CONTRACT-COMMIT BOUNDARY for Phase 354. All contract authoring (354-01..354-05) lands in the working tree across Waves 1-3; this final-wave plan verifies the whole batched diff compiles (`forge build` clean), attests producer-before-consumer ordering + the per-requirement contract sites + the SOLVENCY-01 byte-unchanged invariant, then HALTS for explicit USER hand-review of the single batched contracts diff. NO contract is auto-committed.

Purpose: this honors the ROADMAP Type — "the ONE carefully-sequenced batched USER-APPROVED contracts diff; autonomous:false at the commit gate; never auto-commit contracts" — and the project's `feedback_no_contract_commits` / `feedback_never_preapprove_contracts` / `feedback_batch_contract_approval` / `feedback_manual_review_before_push` rules. The empirical proofs are TST 356 (NOT this phase); the IMPL acceptance bar is `forge build` clean.
Output: the diff-review checklist + the blocking USER checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

@.planning/phases/353-spec-design-lock-unmanipulable-solvency-re-attestation-xmode/353-SPEC.md

@.planning/phases/354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre/354-01-SUMMARY.md
@.planning/phases/354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre/354-02-SUMMARY.md
@.planning/phases/354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre/354-03-SUMMARY.md
@.planning/phases/354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre/354-04-SUMMARY.md
@.planning/phases/354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre/354-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the whole batched diff compiles + author the per-requirement contract-site review checklist</name>
  <files>.planning/phases/354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre/354-06-DIFF-REVIEW.md</files>
  <read_first>
    All five Wave 1-3 SUMMARYs (the landed work). REQUIREMENTS.md (the 14 IMPL requirement bodies AGG-01..05 / TKT-01/02 / QST-01..05 / OPEN-01/02). SPEC Threat Model Re-Attestation SOLVENCY-01 assertion (353-SPEC.md:169) — the ETH/claimablePool debit at GameAfkingModule.sol lines 709-710 MUST be byte-unchanged vs 453f8073. The baseline subject SHA is 453f8073 (the v55 closure HEAD; the working tree was byte-identical at the start of 354).
  </read_first>
  <action>
    Run `forge build` and confirm it exits 0 across the whole reconciled diff. Then author 354-06-DIFF-REVIEW.md with: (1) a producer-before-consumer ordering attestation (accumulator storage 354-01, then aggregator accrue/settle 354-03 + DegenerusQuests entrypoint 354-02, then ticket primitive 354-05, then affiliate PULL 354-04, then open re-verify 354-05, then wiring 354-05); (2) a per-requirement contract-site checklist — for EACH of the 14 IMPL requirements, the file:symbol where it landed (AGG-01 the in-slot accrue in GameAfkingModule.processSubscriberStage; AGG-02 _settleQuest + the settle-day hook + claimQuest; AGG-03 the unsub-settle in subscribe; AGG-04 the shared questProgress across both modes; AGG-05 the dropped markers + the self-marking zeroing; TKT-01/02 the ticket primitive + buyerOwedBurnie accrue + century parity; QST-01/02 the first-sub head-start; QST-03 the double-credit guard; QST-04 the onlyGame entrypoint non-perturbation; QST-05 the O1 line-890 drop; OPEN-01/02 the open re-verification); (3) the SOLVENCY-01 byte-unchanged attestation: capture `git diff 453f8073 -- contracts/modules/GameAfkingModule.sol` and confirm the two debit lines (afkingFunding[src] minus-equals ethValue, and claimablePool minus-equals uint128(ethValue)) appear as CONTEXT (unchanged), NOT as removed/added hunks; (4) a `git diff 453f8073 -- contracts/ --stat` capture so the USER sees the full file scope of the single batched diff. DO NOT commit anything — this is the review artifact the USER reads at the checkpoint.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` exits 0.
    - 354-06-DIFF-REVIEW.md exists with the producer-before-consumer ordering attestation, the 14-requirement contract-site checklist (every ID maps to a file:symbol), the SOLVENCY-01 byte-unchanged attestation, and the `--stat` file-scope capture.
    - The SOLVENCY-01 debit lines are confirmed UNCHANGED in `git diff 453f8073 -- contracts/modules/GameAfkingModule.sol` (context lines, not removed/added).
    - NO commit was made by this task (`git log --oneline -1` is unchanged from before the task; the working tree carries the staged-or-unstaged contract edits, uncommitted).
  </acceptance_criteria>
  <done>forge build is clean, the diff-review checklist proves all 14 requirements landed producer-before-consumer with SOLVENCY-01 byte-unchanged, and nothing is committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: USER hand-review-and-commit gate (the ONE v56 contract boundary)</name>
  <action>HALT for explicit USER hand-review of the single batched contracts diff. The agent does NOT commit contracts. Present the diff-review artifact + `git diff 453f8073 -- contracts/`; wait for the USER to hand-review and commit the contract diff themselves (the only action requiring their approval per the project rule). On approval, record the USER commit SHA in the SUMMARY; on issues, route to a revision pass.</action>
  <what-built>The single carefully-sequenced batched contracts/*.sol diff for v56.0: the re-packed Sub accumulator (354-01), the DegenerusQuests onlyGame settle entrypoint + O1 fix + dead-code removal (354-02), the mode-agnostic aggregator accrue + _settleQuest + claimQuest + unsub-settle + first-sub head-start (354-03), the affiliate flat-7% deterministic-split PULL claim/withdraw (354-04), the ticket minimal-write primitive + buyerOwedBurnie accrual + century parity + the open-end re-verification (354-05). Applied to the working tree, locally compiling (forge build clean), UNCOMMITTED.</what-built>
  <how-to-verify>
    1. Read `.planning/phases/354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre/354-06-DIFF-REVIEW.md` (the per-requirement contract-site checklist + the SOLVENCY-01 byte-unchanged attestation + the file-scope --stat).
    2. Review the full batched diff: `git diff 453f8073 -- contracts/` — hand-review every hunk against the SPEC's LOCKED design (353-SPEC.md). Confirm in particular: the SOLVENCY-01 debit at GameAfkingModule.sol:709-710 is byte-unchanged; affiliate/quest credits are BURNIE creditFlip only (off the ETH path); the open path freeze/parity is intact (only the milli-ETH→wei rescale changed); the affiliate claim has no roll/seed; the O1 line-890 internal credit is gone.
    3. Confirm `forge build` is clean on your machine.
    4. If approved, YOU commit the contracts diff (the only commit that needs your approval per the project rule). The agent will NOT commit contracts. If issues, describe them for a revision pass.
  </how-to-verify>
  <resume-signal>Type "approved — committed" (after you commit the contract diff), or describe the issues to revise.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → git history | the contract diff must NEVER be auto-committed; only the USER commits contracts |

## STRIDE Threat Register (this plan's slice of the SPEC Threat Model Re-Attestation)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-354-06-SC | Tampering | the batched contracts diff committed without review | mitigate | `autonomous:false` blocking-human checkpoint; the agent never commits contracts (`feedback_no_contract_commits`). The USER hand-reviews the full `git diff 453f8073 -- contracts/` and commits. |
| T-354-06-SEC02 | Tampering | SOLVENCY-01 drift slipped into the batch | mitigate | the diff-review checklist explicitly attests the GameAfkingModule.sol:709-710 debit lines are byte-unchanged (context, not +/- hunks). **TST-356 SEC-02 delta-audit re-confirms.** |
</threat_model>

<verification>
- `forge build` clean across the whole batched diff.
- The diff-review checklist maps all 14 requirements to contract sites, producer-before-consumer.
- SOLVENCY-01 debit byte-unchanged vs 453f8073.
- Nothing committed by the agent; the USER commits the contract diff at the checkpoint.
</verification>

<success_criteria>
The whole single batched contracts diff compiles, every IMPL requirement has a verified contract site, SOLVENCY-01 is byte-unchanged, and the diff is HELD at the blocking USER hand-review-and-commit boundary (the ONE contract gate for Phase 354).
</success_criteria>

<output>
Create `.planning/phases/354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre/354-06-SUMMARY.md` when done.
</output>
