---
phase: 354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/storage/DegenerusGameStorage.sol
autonomous: false
requirements: [AGG-05]
must_haves:
  truths:
    - "The Sub struct carries the in-slot accumulator: affiliateBase (uint32, whole-BURNIE), questProgress (uint8), buyerOwedBurnie (uint32, whole-BURNIE)"
    - "The Sub struct carries a per-account hasEverSubscribed 1-bit latch"
    - "The Sub struct carries afkCoveredThroughDay (uint24, monotone debit-gated delivered-day high-water marker — the QST-03 double-credit / streak-dodge guard reads it; ADVANCED in 354-03, DECLARED here)"
    - "windowStartDay / settledThroughDay / lastSettledDay markers are ABSENT (never introduced — there is no scheduled affiliate settle, no window) — NOTE afkCoveredThroughDay is a DIFFERENT marker (a delivered-day high-water mark, not a settle-day marker) and IS present"
    - "The Sub record stays a SINGLE 256-bit slot (no new cold slot) — the re-pack reclaims the bits the accumulator + afkCoveredThroughDay need"
    - "amount is denominated in milli-ETH (0.001-ETH units) as ~uint32; validThroughLevel / lastAutoBoughtDay / lastOpenedDay / afkCoveredThroughDay are uint24"
  artifacts:
    - path: "contracts/storage/DegenerusGameStorage.sol"
      provides: "Re-packed single-slot Sub struct with the in-slot accumulator fields + hasEverSubscribed latch + afkCoveredThroughDay delivered-day marker"
      contains: "struct Sub"
  key_links:
    - from: "contracts/storage/DegenerusGameStorage.sol struct Sub"
      to: "GameAfkingModule.sol per-buy accrue + _settleQuest + open"
      via: "the accumulator fields + afkCoveredThroughDay read/written on the buy-accrue path; lastOpenedDay/lastAutoBoughtDay on the open path (disjoint fields, shared warm slot)"
      pattern: "affiliateBase|questProgress|buyerOwedBurnie|afkCoveredThroughDay"
---

<objective>
Re-pack the `Sub` struct in `DegenerusGameStorage.sol` so the v56 per-sub accumulator (`affiliateBase` + `questProgress` + `buyerOwedBurnie`), the `hasEverSubscribed` first-sub head-start latch, AND the `afkCoveredThroughDay` delivered-day high-water marker all fit IN the SAME single 256-bit slot — NO new cold slot — by narrowing wastefully-wide fields. This is the FOUNDATION of the producer-before-consumer diff: every consumer (the aggregator accrue, the quest STAGE-settle, the affiliate PULL accrue, the ticket buyer-bonus accrue, the QST-03 double-credit/streak-dodge guard) reads/writes these fields.

Purpose: the in-slot accumulator is the GAS-02 hot-path win — one warm per-buy SSTORE into the slot already warmed by the stamp read, ZERO new cold SSTORE. Self-marking running balances zeroed at settle mean the `windowStartDay`/`lastSettledDay` double-settle markers are DROPPED (AGG-05); `afkCoveredThroughDay` is the ONE retained per-sub marker (the monotone delivered-day high-water mark gating the QST-03 streak credit), advanced debit-gated in the same warm accrue write in 354-03.
Output: the re-packed `Sub` struct (incl. `afkCoveredThroughDay`) + a one-line ownership comment locking the `pendingClaim` mapping to `DegenerusAffiliate.sol` (Wave 2 consumers compile against the contracts they need).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# THE locked design contract (these ARE the build spec — honor verbatim):
@.planning/phases/353-spec-design-lock-unmanipulable-solvency-re-attestation-xmode/353-SPEC.md
@.planning/phases/353-spec-design-lock-unmanipulable-solvency-re-attestation-xmode/353-AFKING-READS-WRITES.md

<interfaces>
<!-- The current Sub struct (attested 232/256, DegenerusGameStorage.sol:1867-1899) the executor re-packs.
     DO NOT re-read the codebase to find these — they are the exact current widths: -->

struct Sub {
    // config (56 bits)
    uint8  dailyQuantity;       // 0 = paused/never-subscribed; min 1 active
    uint32 validThroughLevel;   // pass horizon; deity sentinel type(uint24).max
    uint8  reinvestPct;         // 0..100
    uint8  flags;               // bit1 FLAG_DRAIN_FIRST; bit2 FLAG_USE_TICKETS; bit (FLAG_EXTERNAL_FUNDING=1)
    // per-sub stamp (112 bits)
    uint16 scorePlus1;          // frozen activityScore + 1
    uint96 amount;              // stamped spend in WEI (boons off ⇒ amount == spend)
    // markers (64 bits)
    uint32 lastAutoBoughtDay;   // success-marker + frozen seed day
    uint32 lastOpenedDay;       // day-keyed no-double-open marker
}
// mapping(address => Sub) internal _subOf;   (:1902)

// Century bonus storage REUSED by the ticket primitive (Wave 3) — DO NOT add a new slot:
//   uint24 internal centuryBonusLevel;                 (:1563)
//   mapping(address => uint256) internal centuryBonusUsed;  (:1567)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-pack the Sub struct + add the in-slot accumulator + hasEverSubscribed latch + afkCoveredThroughDay marker</name>
  <files>contracts/storage/DegenerusGameStorage.sol</files>
  <read_first>
    `contracts/storage/DegenerusGameStorage.sol:1860-1902` (the `Sub` struct + the `_subOf` mapping + the NatSpec on `amount`/`lastAutoBoughtDay`/`lastOpenedDay`). SPEC section `## Accumulator Layout` (353-SPEC.md:116-147) — the LOCKED decision: fit in the SAME single Sub slot, NO new cold slot. SPEC `## QST` delivered-day derivation (353-SPEC.md:235-237) — `afkCoveredThroughDay` is a per-sub MONOTONE, debit-gated delivered-day high-water marker (monotone like `lastOpenedDay`/`lastAutoBoughtDay`); the QST-03 double-credit guard (353-SPEC.md:237) gates `state.streak` advancement on it. `353-AFKING-READS-WRITES.md:66-75` (the "Irreducible afking buy (v56 target)" map + the accumulator LOCKED note). SPEC `## Threat Model Re-Attestation` RNG-freeze row (353-SPEC.md:165/170-171) — the accumulator fields MUST be disjoint from RNG-window state.
  </read_first>
  <action>
    Re-pack `struct Sub` per the LOCKED `## Accumulator Layout`. Narrow the wastefully-wide fields to reclaim bits: `amount` uint96 → ~uint32 stored in MILLI-ETH / 0.001-ETH units (USER "round amount to .001 eth"; ~4.3M ETH headroom in milli-ETH, granularity well below the min box size); `validThroughLevel` uint32 → uint24 (16,777,215 levels headroom; keep the deity sentinel as type(uint24).max); `lastAutoBoughtDay` uint32 → uint24; `lastOpenedDay` uint32 → uint24 (45,000-year day-index headroom). ADD the in-slot accumulator fields: `uint32 affiliateBase` (whole-BURNIE running balance, 100M saturating clamp applied at the accrue write in Wave 2, NOT here), `uint8 questProgress` (delivered-day completion COUNTER), `uint32 buyerOwedBurnie` (whole-BURNIE running balance, the accrued 10%/20% ticket buyer-bonus, ticket-mode-specific, 100M clamp at accrue), `bool hasEverSubscribed` (the first-sub `+daysToNextSettle` head-start latch, set-once), AND `uint24 afkCoveredThroughDay` (the per-sub delivered-day high-water marker: monotone, advanced ONLY on a debit-DELIVERED day in 354-03's accrue write, read by the QST-03 double-credit / streak-dodge guard so `state.streak` is advanced at most once per delivered day; same uint24 day-index width as `lastAutoBoughtDay`/`lastOpenedDay`). Do NOT introduce `windowStartDay`/`settledThroughDay`/`lastSettledDay` (DROPPED per AGG-05 — the running balances self-mark); `afkCoveredThroughDay` is a DIFFERENT marker (a delivered-day high-water mark, NOT a settle-day marker) and IS retained. Keep the record in ONE 256-bit slot: 56-config (now ~48: dailyQuantity8 + validThroughLevel24 + reinvestPct8 + flags8 = 48) + stamp (scorePlus1 16 + amount32 = 48) + markers (lastAutoBoughtDay24 + lastOpenedDay24 + afkCoveredThroughDay24 = 72) + accumulator (affiliateBase32 + questProgress8 + buyerOwedBurnie32 + hasEverSubscribed1 = 73) ≈ 241/256 — comfortably under 256 with margin (the SPEC says "comfortable, NOT a tight fit"; the reclaimed bits from dropping the three settle markers + narrowing absorb the extra uint24 marker). The exact bit allocation is the executor's to confirm under the layout principles; the field set + denominations + the "single slot, no new cold slot" constraint are LOCKED. Update the NatSpec on `amount` to record the milli-ETH denomination and that the SOLVENCY-01 debit (GameAfkingModule.sol:709-710) consumes the FULL wei `ethValue` — the milli-ETH rounding is on the STAMP's recorded value (the EV/seed input), NEVER on the ETH cut. NatSpec `afkCoveredThroughDay` as "monotone delivered-day high-water mark; advanced only when the day's ETH debit fired (debit-gated); the QST-03 streak guard gates state.streak advancement on it; advanced in the same warm Sub-slot accrue write in GameAfkingModule (354-03)". Per AFF-PULL-Adversarial-Re-Review guardrail 2, NatSpec the 100M saturating clamp + that it can only ever UNDER-credit. DO NOT alter the `amount` semantic of "boons off ⇒ amount == spend" beyond the denomination change.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "struct Sub" contracts/storage/DegenerusGameStorage.sol` shows the struct, and within it: `uint32 affiliateBase`, `uint8 questProgress`, `uint32 buyerOwedBurnie`, a `hasEverSubscribed` bool/bit, and a `uint24 afkCoveredThroughDay` are PRESENT.
    - `grep -vn '^[[:space:]]*//' contracts/storage/DegenerusGameStorage.sol | grep -E "windowStartDay|settledThroughDay|lastSettledDay"` returns ZERO matches (the dropped settle markers were never introduced — `afkCoveredThroughDay` is NOT one of these).
    - `amount`, `validThroughLevel`, `lastAutoBoughtDay`, `lastOpenedDay` are narrowed (no remaining `uint96 amount;` or `uint32 validThroughLevel;` in the Sub struct; they are ~uint32 / uint24 respectively).
    - `forge build` exits 0.
  </acceptance_criteria>
  <done>The re-packed single-slot Sub struct compiles with all four accumulator fields + the hasEverSubscribed latch + the afkCoveredThroughDay delivered-day marker present, the three dropped settle markers absent, and forge build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the cross-contract pendingClaim ownership boundary (no-op on game storage)</name>
  <files>contracts/storage/DegenerusGameStorage.sol</files>
  <read_first>
    SPEC `## Accumulator Layout` principle 4 (353-SPEC.md:137) — "A new `mapping(address => uint256) pendingClaim` holds post-split per-recipient affiliate ledger entries (touched at `claim`/`withdraw`, NEVER per-buy → no per-buy cold SSTORE → GAS-02 preserved)." Confirm whether `pendingClaim` should live in `DegenerusGameStorage` or in `DegenerusAffiliate.sol` by checking where the affiliate PULL `claim`/`withdraw` (Wave 2, 354-04) will be authored: the affiliate distribution lives in `contracts/DegenerusAffiliate.sol` (own contract, own storage), so `pendingClaim` belongs THERE, not in `DegenerusGameStorage`.
  </read_first>
  <action>
    DECISION (resolve, do not defer): the `pendingClaim` mapping is consumed exclusively by the affiliate PULL `claim`/`withdraw` in `DegenerusAffiliate.sol` (Wave 2, plan 354-04), which is a SEPARATE contract with its own storage. Therefore declare `pendingClaim` in `DegenerusAffiliate.sol` (plan 354-04 owns it), NOT in `DegenerusGameStorage.sol`. In THIS plan, make NO `pendingClaim` declaration in `DegenerusGameStorage.sol` — only document in a one-line comment near the `Sub` struct that `affiliateBase` is the per-sub running balance the uplines PULL via `DegenerusAffiliate.claim` (which drains it via the GAME-side `drainAffiliateBase` accessor authored in 354-03), and `buyerOwedBurnie` pays the SUB directly via the quest PUSH (so it needs NO `pendingClaim` entry). This task is a no-op on storage layout beyond a clarifying comment; it exists to LOCK the cross-contract ownership boundary so 354-04 declares `pendingClaim` in the affiliate contract.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -3 && echo "--- pendingClaim must NOT be in game storage ---" && (grep -c "pendingClaim" contracts/storage/DegenerusGameStorage.sol || true)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -vn '^[[:space:]]*//' contracts/storage/DegenerusGameStorage.sol | grep -c "mapping.*pendingClaim"` returns 0 (the mapping is NOT declared in game storage — it belongs to DegenerusAffiliate per the cross-contract boundary).
    - A clarifying comment near `struct Sub` documents the PULL ownership of `affiliateBase` (drained by 354-03's `drainAffiliateBase` accessor) and the PUSH ownership of `buyerOwedBurnie`.
    - `forge build` exits 0.
  </acceptance_criteria>
  <done>The cross-contract ledger ownership is locked in a comment; pendingClaim is reserved for DegenerusAffiliate (354-04); forge build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storage layout → all consumers | the re-packed slot is the substrate the buy-accrue, the quest-settle, and the open all share — a mis-pack collides fields |

## STRIDE Threat Register (this plan's slice of the SPEC Threat Model Re-Attestation)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-354-01-SEC02-RNG | Tampering | the in-slot accumulator + `afkCoveredThroughDay` vs the open's seed-source | mitigate | SPEC RNG-freeze row (353-SPEC.md:165/171): the accumulator fields (`affiliateBase`/`questProgress`/`buyerOwedBurnie`) + `afkCoveredThroughDay` are DISJOINT from the RNG-window state; the open keeps `rngWordByDay[lastAutoBoughtDay]` (the frozen stamp-day word). This plan adds NO field that overlaps the open's seed/day. **TST-356 OPEN-02:** the open still uses the frozen stamp-day word + day; the accumulator slot is disjoint. |
| T-354-01-SEC02-SOLV | Information Disclosure / Tampering | the `amount` milli-ETH re-denomination | accept (off-solvency) | SPEC SOLVENCY-01 (353-SPEC.md:131/169): the ETH/`claimablePool` debit (`GameAfkingModule.sol:709-710`) consumes the FULL wei `ethValue` and is byte-UNCHANGED. The milli-ETH rounding is on the STAMP's recorded value (the EV/seed input), NOT the ETH cut. **TST-356 SEC-02 delta-audit:** the debit lines byte-unchanged. |
| T-354-01-AGG05 | Tampering (double-settle) | dropped `windowStartDay`/`lastSettledDay` | mitigate | SPEC AGG-05 (353-SPEC.md:187): self-marking running balances make a settle-day marker unnecessary — `affiliateBase`/`questProgress`/`buyerOwedBurnie` are zeroed at settle, so a double-fire finds 0 → no-op. This plan deliberately does NOT introduce the three settle markers; the retained `afkCoveredThroughDay` is a delivered-day high-water mark (the QST-03 streak guard), NOT a settle marker. **TST-356 SEC-01 no-double-settle.** |
| T-354-01-QST03 | Elevation (streak double-credit) | the `afkCoveredThroughDay` delivered-day marker | mitigate | SPEC QST-03 (353-SPEC.md:237): `afkCoveredThroughDay` is monotone + debit-gated; the streak guard advances `state.streak` at most once per delivered day. This plan DECLARES the field (uint24); 354-03 advances it in the warm accrue write. **TST-356:** a manual completion on an afk-covered day does not double-credit the streak. |

No package installs in this plan (no `T-{phase}-SC` row).
</threat_model>

<verification>
- `forge build` clean.
- The Sub struct is a single 256-bit slot with the four accumulator fields + `hasEverSubscribed` + `afkCoveredThroughDay` present, the three dropped settle markers absent, and the narrowed widths applied.
- No collision between the accumulator fields (buy-accrue path) and the open markers (`lastOpenedDay`/`lastAutoBoughtDay`, open path) — different fields, same warm slot.
</verification>

<success_criteria>
The re-packed `Sub` struct compiles, fits one slot, carries `affiliateBase`/`questProgress`/`buyerOwedBurnie`/`hasEverSubscribed`/`afkCoveredThroughDay`, drops the three settle markers, and re-denominates `amount` to milli-ETH with the SOLVENCY-01 debit left for Wave 2 to keep byte-unchanged. The cross-contract `pendingClaim` ownership is locked to DegenerusAffiliate.
</success_criteria>

<output>
Create `.planning/phases/354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre/354-01-SUMMARY.md` when done.
</output>
