---
phase: 354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre
plan: 04
type: execute
wave: 2
depends_on: ["354-01"]
files_modified:
  - contracts/DegenerusAffiliate.sol
  - contracts/interfaces/IDegenerusAffiliate.sol
autonomous: false
requirements: [AGG-01, AGG-04, AGG-05]
must_haves:
  truths:
    - "DegenerusAffiliate exports claim(address[] calldata subs) and withdraw()"
    - "claim reads-and-zeroes affiliateBase[sub] ATOMICALLY per loop iteration (never pre-loads bases into a memory array)"
    - "claim splits the batch sumB by the fixed deterministic 75/20/5 (floor + remainder-to-A so Σshares ≤ sumB, never over-mints); the buyer never receives the base (A ≠ sub guaranteed + the rare U1/U2==sub cycle skip)"
    - "noReferrer subs split deterministically 50/50 VAULT/DGNRS (remainder to VAULT), NO entropy"
    - "withdraw is CEI — pendingClaim[msg.sender] is zeroed BEFORE the creditFlip (recordAmount=0)"
    - "There is NO roll, NO seed, NO scheduled/mutation affiliate flush"
    - "The affiliate leaderboard (earned / _totalAffiliateScore / _updateTopAffiliate) credits at CLAIM time to the direct affiliate A"
  artifacts:
    - path: "contracts/DegenerusAffiliate.sol"
      provides: "the flat-7% deterministic-split PULL: claim + withdraw + pendingClaim ledger"
      contains: "function claim"
    - path: "contracts/interfaces/IDegenerusAffiliate.sol"
      provides: "claim + withdraw declarations"
  key_links:
    - from: "DegenerusAffiliate.claim"
      to: "the per-sub Sub.affiliateBase running balance (read via GAME / accessor)"
      via: "read-and-zero per iteration → 75/20/5 split → pendingClaim[A/U1/U2]"
      pattern: "affiliateBase|pendingClaim"
    - from: "DegenerusAffiliate.withdraw"
      to: "coinflip.creditFlip"
      via: "CEI — zero pendingClaim before the mint"
      pattern: "pendingClaim|creditFlip"
---

<objective>
Build the affiliate flat-7% deterministic-split PULL in `DegenerusAffiliate.sol`: a permissionless `claim(address[] subs)` that reads-and-zeroes each sub's accrued `affiliateBase` atomically, splits the batch total by the fixed 75/20/5 (buyer-never-wins, floor + remainder-to-A), and credits `pendingClaim[A/U1/U2]`; plus a CEI `withdraw()` that mints `pendingClaim[msg.sender]` via the only cross-contract `creditFlip`. This is INDEPENDENT of GameAfkingModule (separate contract) — Wave 2 in parallel with 354-03.

Purpose: the PULL has NO roll and NO seed — deleting the entire settle-timing/free-option manipulability surface (the XMODEL C1/C2 finding is MOOT). The accrue of `affiliateBase` lives in GameAfkingModule (354-03); this plan owns the SETTLE side: `claim`/`withdraw`/`pendingClaim`. The four AFF-PULL adversarial guardrails are load-bearing IMPL-correctness obligations.
Output: `claim`, `withdraw`, the `pendingClaim` mapping, the noReferrer 50/50 deterministic group, and the claim-time leaderboard write — all compiling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

@.planning/phases/353-spec-design-lock-unmanipulable-solvency-re-attestation-xmode/353-SPEC.md
@.planning/phases/353-spec-design-lock-unmanipulable-solvency-re-attestation-xmode/AFF-PULL-DESIGN-DRAFT.md

# Wave-1 producer this plan consumes (the affiliateBase field lives in the Sub slot):
@.planning/phases/354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre/354-01-SUMMARY.md

<interfaces>
<!-- Exact DegenerusAffiliate primitives the executor reuses — DO NOT re-explore. -->

// payAffiliate (:380, the LIVE manual path — REFERENCE for the 75/20/5 + leaderboard, NOT removed):
//   leaderboard writes: earned[affiliateAddr] = newTotal (:510); _totalAffiliateScore[lvl] += scaledAmount (:511);
//     _updateTopAffiliate(affiliateAddr, newTotal, lvl) (:521).
//   live 75/20/5 WTA roll (:556-583): roll<15 → affiliate; 15-18 → upline1; 19 → upline2;
//     buyer-never-wins: if (winner != sender) { ... } (:579).
//   noReferrer live (:539-554): 50/50 VAULT/DGNRS via keccak entropy → _routeAffiliateReward.
// _referrerAddress(player) (:701-707): real upline / else VAULT (unset → VAULT; REF_CODE_LOCKED → VAULT).
// _routeAffiliateReward(player, amount) (:745-751): coinflip.creditFlip(player, amount) (BURNIE, the only mint).
// _updateTopAffiliate (:768+, :776-783 read-once-compare): 1 SLOAD + conditional SSTORE.
// affiliateCoinEarned[lvl][addr] (:201) — the per-level earned ledger (= `earned` alias at :481/:510).
// _totalAffiliateScore[lvl] (:216). affiliateTopByLevel[lvl] (:211).
// ContractAddresses.VAULT / ContractAddresses.DGNRS — the noReferrer recipients.
// IDegenerusAffiliate.sol — exports payAffiliate (:20), affiliateTop/Score/totalAffiliateScore, getReferrer.

// The per-sub affiliateBase running balance lives in DegenerusGameStorage.Sub (354-01).
// DegenerusAffiliate is a SEPARATE contract → it cannot read _subOf directly; it reads the base via
// a GAME accessor / a cross-contract view, OR the GAME pushes the base. RESOLVE the read mechanism
// in Task 1 (see <action>); the canonical SPEC shape is `affiliateBase[sub]` read-and-zeroed at claim.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the pendingClaim ledger + the claim(address[] subs) deterministic-split PULL</name>
  <files>contracts/DegenerusAffiliate.sol, contracts/interfaces/IDegenerusAffiliate.sol</files>
  <read_first>
    `contracts/DegenerusAffiliate.sol:380-588` (payAffiliate — the 75/20/5 + leaderboard + noReferrer reference), `:701-707` (_referrerAddress), `:745-751` (_routeAffiliateReward), `:768-783` (_updateTopAffiliate). SPEC AFF-01 "Mechanism LOCKED" (353-SPEC.md:70-78) — the EXACT claim algorithm: same-affiliate batch, ONE `sumB`, read-and-zero `affiliateBase[sub]` ATOMICALLY per iteration, accumulate `skipU1`/`skipU2` only for the rare U1/U2==sub cycle, then `u1=(sumB−skipU1)×20/100`, `u2=(sumB−skipU2)×5/100`, `aShare=sumB−u1−u2` (A never skips, remainder→A), `pendingClaim[A/U1/U2] +=`. noReferrer group → 50/50 VAULT/DGNRS deterministic (remainder→VAULT). SPEC `## AFF-PULL Adversarial Re-Review` guardrails 1 + 4 (353-SPEC.md:312/315) — atomic read-and-zero (NEVER pre-load bases to memory); kickback-dropped-for-afking; noReferrer deterministic 50/50. SPEC AFF-02 (b) leaderboard at claim time (353-SPEC.md:94-100). SPEC `## Threat Model Re-Attestation` SEC-01 churn/timing/double-settle rows (353-SPEC.md:159-161).
  </read_first>
  <action>
    Declare `mapping(address => uint256) pendingClaim` in `DegenerusAffiliate.sol` (the post-split per-recipient ledger — touched at claim/withdraw only, NEVER per-buy). Add `function claim(address[] calldata subs) external`: it is permissionless + SAME-AFFILIATE batch. Resolve how the contract reads each sub's accrued `affiliateBase`: since `affiliateBase` lives in the Game's `Sub` slot (354-01) and DegenerusAffiliate is a separate contract, add a GAME-only accessor on the Game side that read-and-zeroes a sub's `affiliateBase` atomically and returns it (so the atomic read-and-zero guardrail is honored at the storage owner), OR have `claim` call a Game function `drainAffiliateBase(address sub) returns (uint256)` that does the atomic read-and-zero and the GAME asserts the caller is AFFILIATE — choose the mechanism that keeps the read-and-zero ATOMIC per sub (the load-bearing guardrail) and document it. Read the direct affiliate `A` from `subs[0]` via `_referrerAddress`/the referral layer; `require` every other sub resolves to the SAME `A` (mixed arrays revert) so the upline chain `A / U1 / U2` is read ONCE. Walk `subs[]` accumulating ONE running `sumB`: per sub `B = drainAffiliateBase(sub)` (atomic read-and-zero AT THE OWNER — NEVER pre-load all bases first, per guardrail 1); if `B == 0` skip; `sumB += B`; accumulate `skipU1 += B` / `skipU2 += B` only when `U1 == sub` / `U2 == sub` (the rare cycle; `A ≠ sub` guaranteed so the 75% leg never skips, `:579` semantic preserved). After the walk: `u1 = (sumB − skipU1) × 20 / 100`, `u2 = (sumB − skipU2) × 5 / 100`, `aShare = sumB − u1 − u2` (floor + remainder-to-A → `Σshares ≤ sumB`, never over-mints); `pendingClaim[A] += aShare; pendingClaim[U1] += u1; pendingClaim[U2] += u2`. Leaderboard ONCE per claim batch at the CLAIM-time level (the live `level + 1` at claim): `earned[A] += sumB` (`:510` analog), `_totalAffiliateScore[lvl] += sumB` (`:511` analog), `_updateTopAffiliate(A, newTotal, lvl)` (`:521` analog) — the USER-accepted claim-time distortion (AFF-02). For noReferrer subs (whose `_referrerAddress` resolves to VAULT — group them separately or detect at `subs[0]`): split deterministically `pendingClaim[VAULT] += ΣB/2; pendingClaim[DGNRS] += ΣB/2` (remainder to VAULT), NO entropy (guardrail 4). NO roll, NO seed, NO `currentDayIndex()`. NO kickback for afking (the gross base accrued; the live manual `payAffiliate` keeps its kickback). Declare `claim` on `IDegenerusAffiliate.sol`.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function claim(address\[\] calldata subs)" contracts/DegenerusAffiliate.sol` ≥ 1; `claim` is declared in `contracts/interfaces/IDegenerusAffiliate.sol`.
    - `claim` splits 75/20/5 with floor+remainder-to-A: grep shows `× 20 / 100` (or `* 20 / 100`) and `× 5 / 100` and an `aShare = sumB - u1 - u2`-style remainder assignment.
    - `claim` body contains NO `keccak256` and NO `currentDayIndex` (no roll, no seed): `sed -n` over the claim function shows zero `keccak256`/`currentDayIndex`.
    - The per-sub base is drained atomically (the read-and-zero happens at the owner, not via a pre-loaded memory array): grep shows a `drainAffiliateBase`/equivalent atomic read-and-zero call inside the loop, NOT a `for` that fills a `uint256[] memory bases`.
    - `pendingClaim` mapping is declared in `DegenerusAffiliate.sol`.
    - `forge build` exits 0.
  </acceptance_criteria>
  <done>claim atomically drains each sub's base, splits the batch by the fixed deterministic 75/20/5 (buyer-never-wins, never over-mints), credits pendingClaim + the claim-time leaderboard, and handles noReferrer 50/50 deterministically; forge build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add the CEI withdraw() — the only cross-contract creditFlip</name>
  <files>contracts/DegenerusAffiliate.sol, contracts/interfaces/IDegenerusAffiliate.sol</files>
  <read_first>
    `contracts/DegenerusAffiliate.sol:745-751` (_routeAffiliateReward → coinflip.creditFlip — the only mint shape). SPEC AFF-01 "withdraw()" bullet (353-SPEC.md:76) — mints `pendingClaim[msg.sender]` BURNIE via `coinflip.creditFlip`, zeroing it FIRST (CEI), batched per recipient. SPEC `## AFF-PULL Adversarial Re-Review` (353-SPEC.md:316/336) — CEI: zero `pendingClaim` BEFORE the `creditFlip` (`recordAmount=0` = pure ledger add, no callback re-entrancy). SPEC `## Threat Model Re-Attestation` SEC-02 row (353-SPEC.md:164 — `withdraw`'s `creditFlip` is CEI).
  </read_first>
  <action>
    Add `function withdraw() external`: read `uint256 owed = pendingClaim[msg.sender]`; if `owed == 0` return (no-op); ZERO `pendingClaim[msg.sender] = 0` FIRST (CEI — before any external call); then `coinflip.creditFlip(msg.sender, owed)` (with `recordAmount=0` = a pure ledger add, no callback re-entrancy — match the existing `_routeAffiliateReward`/`creditFlip` call shape). This is the ONLY cross-contract leg of the PULL; it is batched per recipient across all their claims (one `creditFlip` regardless of how many subs fed them). Declare `withdraw` on `IDegenerusAffiliate.sol`.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function withdraw()" contracts/DegenerusAffiliate.sol` ≥ 1; `withdraw` is declared in `contracts/interfaces/IDegenerusAffiliate.sol`.
    - `withdraw` zeroes `pendingClaim[msg.sender]` BEFORE the `creditFlip` (the `pendingClaim[msg.sender] = 0` assignment textually precedes the `creditFlip` call in the function body — CEI).
    - `forge build` exits 0.
  </acceptance_criteria>
  <done>withdraw mints pendingClaim[msg.sender] via the single CEI creditFlip (zero-before-mint), the only cross-contract leg of the PULL; forge build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anyone → claim | claim is permissionless; it must credit only the resolved upline chain, never the buyer, and never double-credit a duplicate sub |
| recipient → withdraw | withdraw mints to msg.sender; must be CEI (no re-entrancy) and idempotent |
| DegenerusAffiliate → GAME (affiliateBase drain) | the cross-contract atomic read-and-zero of the Sub.affiliateBase must be at the storage owner |

## STRIDE Threat Register (this plan's slice of the SPEC Threat Model Re-Attestation)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-354-04-SEC01-FREEOPT | Elevation (settle-timing / free-option) | the affiliate distribution path | mitigate (removed surface) | SPEC AFF-01 (353-SPEC.md:62-80) + the supersession note (353-SPEC.md:263): there is NO roll, NO seed, exactly ONE deterministic 75/20/5 split → no favorable seed to wait for, no two-distribution free option (C1/C2 MOOT). **TST-356 SEC-01:** claiming day N vs N+1 yields the identical split. |
| T-354-04-SEC01-DUPSUB | Repudiation (double-credit) | a duplicate sub in subs[] | mitigate | SPEC guardrail 1 (353-SPEC.md:312): read-and-zero `affiliateBase[sub]` ATOMICALLY per iteration at the owner — NEVER pre-load to a memory array. A second occurrence of the same sub drains 0. **TST-356:** claim idempotent; duplicate sub not double-credited. |
| T-354-04-SEC01-OVERMINT | Repudiation (over-mint) | the 75/20/5 split arithmetic | mitigate | SPEC AFF-01 (353-SPEC.md:74/80): floor splits + remainder-to-A → `Σshares ≤ sumB`, never over-mints. **TST-356:** `Σshares ≤ sumB`. |
| T-354-04-SEC01-BUYERWIN | Elevation | buyer receiving the base | mitigate | SPEC AFF-01 fact 2 (353-SPEC.md:66): `A ≠ sub` guaranteed (self-referral → VAULT); the only skip is the rare U1/U2==sub cycle (dropped, not redirected) — `:579` semantic preserved. **TST-356:** buyer never receives the base. |
| T-354-04-SEC02-CEI | Tampering (re-entrancy) | withdraw's creditFlip | mitigate | SPEC SEC-02 (353-SPEC.md:164/336): `pendingClaim[msg.sender]` zeroed BEFORE the `creditFlip` (recordAmount=0 = pure ledger add, no callback). **TST-356 SEC-02.** |

No package installs in this plan (no `T-{phase}-SC` row).
</threat_model>

<verification>
- `forge build` clean.
- claim: atomic per-sub read-and-zero at the owner, same-affiliate batch revert on mix, 75/20/5 floor+remainder-to-A, buyer-never-wins, noReferrer 50/50 deterministic, claim-time leaderboard, NO roll/seed.
- withdraw: CEI (zero-before-mint), the only creditFlip.
</verification>

<success_criteria>
The affiliate flat-7% deterministic-split PULL is built: `claim` settles the accrued bases into `pendingClaim` with the fixed non-gameable split (no roll, no seed, no free option), `withdraw` is the single CEI mint, and the leaderboard credits at claim time. The accrue side (`affiliateBase +=`) lives in GameAfkingModule (354-03).
</success_criteria>

<output>
Create `.planning/phases/354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre/354-04-SUMMARY.md` when done.
</output>
