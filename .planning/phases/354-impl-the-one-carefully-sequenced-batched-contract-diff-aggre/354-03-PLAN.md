---
phase: 354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre
plan: 03
type: execute
wave: 2
depends_on: ["354-01", "354-02"]
files_modified:
  - contracts/modules/GameAfkingModule.sol
  - contracts/interfaces/IDegenerusGameModules.sol
autonomous: false
requirements: [AGG-01, AGG-02, AGG-03, AGG-04, AGG-05, QST-01, QST-02, QST-03]
must_haves:
  truths:
    - "Per afking buy, the STAGE does ONE warm SSTORE into the Sub accumulator (affiliateBase += flat-7% whole-BURNIE; questProgress delivered-day counter ++) and ZERO cross-contract calls on the hot path"
    - "The lootbox per-buy path no longer calls quests.handlePurchase / affiliate.payAffiliate / coinflip.creditFlip (the storm is removed; deferred to settle)"
    - "On the global settle day (currentDay % settlePeriod == 0), the inline _settleQuest mints questProgress × QUEST_SLOT0_REWARD + buyerOwedBurnie in ONE creditFlip to the sub + advances the streak, zeroing both counters"
    - "An unsub runs _settleQuest first (drains questProgress + buyerOwedBurnie); the affiliate base is NOT flushed on mutation (persists for the uplines to PULL)"
    - "A permissionless claimQuest(address[] subs) runs the SAME _settleQuest (always credits the sub, never the caller)"
    - "The SOLVENCY-01 ETH/claimablePool debit (GameAfkingModule.sol:709-710) is BYTE-UNCHANGED"
    - "The first-sub-only +daysToNextSettle head-start is granted once per account via hasEverSubscribed (bounded +0..+9); the per-window streak advances only on debit-DELIVERED days"
    - "The 100M whole-BURNIE saturating clamp is applied BEFORE the affiliateBase += write"
  artifacts:
    - path: "contracts/modules/GameAfkingModule.sol"
      provides: "the mode-agnostic accrue + the inline _settleQuest + claimQuest fallback + unsub-settle + first-sub head-start"
      contains: "_settleQuest"
    - path: "contracts/interfaces/IDegenerusGameModules.sol"
      provides: "claimQuest declaration on IGameAfkingModule"
  key_links:
    - from: "GameAfkingModule._settleQuest"
      to: "DegenerusQuests onlyGame settle entrypoint + coinflip.creditFlip"
      via: "GAME-context streak advance + ONE BURNIE credit on the settle day / claimQuest / unsub"
      pattern: "creditFlip|settleAfking|awardQuestStreak"
    - from: "GameAfkingModule per-buy accrue"
      to: "Sub.affiliateBase / Sub.questProgress"
      via: "one warm in-slot SSTORE, no cross-contract"
      pattern: "affiliateBase|questProgress"
---

<objective>
Build the mode-agnostic aggregator in `GameAfkingModule.sol`: replace the per-buy cross-contract storm (`:749-833` — `quests.handlePurchase`, `affiliate.payAffiliate` ×2, `coinflip.creditFlip`) with a cheap in-slot accrue (flat-7% `affiliateBase` + the `questProgress` delivered-day counter), and add the inline `_settleQuest` that RIDES the daily buy STAGE on the global settle day to mint the quest BURNIE + the accrued ticket buyer-bonus in ONE creditFlip — plus the `claimQuest` keeper fallback, the quest-settle-on-unsub, and the first-sub-only `+daysToNextSettle` streak head-start.

Purpose: this is the GAS-01 hot-path collapse + the AGG mechanism. SOLVENCY-01 stays byte-unchanged (the affiliate/quest credits are BURNIE-off-the-ETH-path — a BURNIE-emission-TIMING change only). The affiliate base is a running balance the uplines PULL (354-04) — it is NOT flushed on mutation.
Output: the accrue, the `_settleQuest`, the `claimQuest`, the unsub-settle, and the first-sub head-start, all compiling against the re-packed Sub slot (354-01) and the DegenerusQuests entrypoint (354-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

@.planning/phases/353-spec-design-lock-unmanipulable-solvency-re-attestation-xmode/353-SPEC.md
@.planning/phases/353-spec-design-lock-unmanipulable-solvency-re-attestation-xmode/353-AFKING-READS-WRITES.md

# Wave-1 producers this plan consumes:
@.planning/phases/354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre/354-01-SUMMARY.md
@.planning/phases/354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre/354-02-SUMMARY.md

<interfaces>
<!-- Exact GameAfkingModule anchors the executor edits — DO NOT re-explore. -->

// processSubscriberStage(uint32 processDay, uint256 maxCount) (:539) — the per-sub STAGE loop.
//   Hoisted: mp=_mintPriceInContext() (:543); currentLevel=level (:546); cursor=_subCursor (:548).
//   Per sub: player=_subscribers[cursor] (:551); Sub storage sub=_subOf[player] (:552).
//   Guards: no-orphan (:570); cancel-tombstone reclaim (:586); AlreadyAutoBought (:598);
//           pass-validity (:612); _resolveBuy (:639); src (:648); funding-skip (:661).
//   DEBIT [💰 BYTE-UNCHANGED]:  if (ethValue != 0) { afkingFunding[src] -= ethValue; claimablePool -= uint128(ethValue); }  (:708-711)
//   TICKET branch (:713-731): purchaseWith delegatecall — REPLACED in 354-05, NOT here.
//   LOOTBOX branch (:735-833): THE STORM TO REMOVE —
//     (a) quests.handlePurchase (:760) + recordMintQuestStreak (:773)
//     (b) STAMP: sub.scorePlus1 (:793) + sub.amount (:794)  [KEEP the stamp; amount now milli-ETH per 354-01]
//     (c) affiliate.payAffiliate ×2 (:806 fresh, :816 claimable)
//     (d) coinflip.creditFlip(player, flipCredit) (:831)
//   MARKER: sub.lastAutoBoughtDay = processDay (:840).
// _ethToBurnie(amount, mp) (:1041) — the BURNIE valuation helper (copy of MintModule._ethToBurnieValue).
// subscribe(...) (:234-342): CANCEL branch (:291-304, dailyQuantity==0); UPSERT branch (:309-341).
//   FLAG_DRAIN_FIRST / FLAG_USE_TICKETS / FLAG_EXTERNAL_FUNDING(=1) (:154).
// SUBSCRIBER_CAP = 500 (:164). The STAGE is driven from DegenerusGameAdvanceModule._runSubscriberStage
//   (:754-765) with SUB_STAGE_BATCH=50 (AdvanceModule:149) — the shrink is 355 GAS, NOT here.
//   currentDay is available via _simulatedDayIndexAt (AdvanceModule:1102) / processDay is passed in.

// DegenerusQuests onlyGame settle entrypoint — added in 354-02 (call it from _settleQuest, GAME context).
// QUEST_SLOT0_REWARD = 100 ether (DegenerusQuests.sol:144).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the per-buy lootbox storm with the in-slot accrue (AGG-01/AGG-04/AGG-05)</name>
  <files>contracts/modules/GameAfkingModule.sol</files>
  <read_first>
    `contracts/modules/GameAfkingModule.sol:708-711` (the SOLVENCY-01 debit — MUST stay byte-unchanged), `:735-833` (the lootbox storm to remove — KEEP the stamp at :793-794, REMOVE the cross-contract calls), `:840` (the lastAutoBoughtDay marker), `:1041` (the `_ethToBurnie` helper). SPEC `## AGG` (353-SPEC.md:179-191) — accrue-cheap-per-buy: ONE warm SSTORE (`affiliateBase += flat-7% whole-BURNIE`; `questProgress` delivered-day counter advanced) + NOTHING cross-contract. SPEC `## Accumulator Layout` magnitude math (353-SPEC.md:126/142) — `_ethToBurnie ≈ qty × 1000` whole BURNIE; flat 7% ≈ `qty × 70` whole BURNIE/buy. SPEC AFF-PULL guardrail 2 (353-SPEC.md:313) — clamp saturates BEFORE the `+=` (`affiliateBase = min(affiliateBase + base, 100_000_000)`). SPEC AFF-01 (353-SPEC.md:72) — flat 7%, NO taper on afking, NO kickback for afking. SPEC `## Threat Model Re-Attestation` SEC-02 SOLVENCY-01 row (353-SPEC.md:164/169).
  </read_first>
  <action>
    In the lootbox branch (`:735-833`), REMOVE the cross-contract storm: delete the `quests.handlePurchase` call + its `recordMintQuestStreak` follow-up (the `:749-778` quest block), the two `affiliate.payAffiliate` calls (`:806`/`:816`), and the per-buy `coinflip.creditFlip(player, flipCredit)` (`:831`). KEEP the box STAMP (`sub.scorePlus1 = scorePlus1; sub.amount = uint96(amount)` at `:793-794` — note `amount` is now milli-ETH per 354-01, so cast/round the stamped spend to milli-ETH units; the SOLVENCY-01 debit at `:708-711` still consumes the FULL wei `ethValue` and stays BYTE-UNCHANGED). The `scorePlus1` stamp needs the activity score — currently it sources from the POST-buy streak the handler returned; since the handler is gone, source the score from the existing pre-action streak read path (the activity score the AFF-02 taper / EV-cap stamp needs is still computed; preserve the FREEZE-03 stamp semantics — the stamped score must match a manual buy's EV input as the box-freeze design requires). REPLACE the storm with the cheap accrue: compute `base = _ethToBurnie(ethValue, mp) × 7 / 100` valued in WHOLE BURNIE (flat 7%, NO taper, NO kickback — afking-N/A per AFF-02), apply the 100M saturating clamp BEFORE the write (`sub.affiliateBase = min(uint256(sub.affiliateBase) + base, 100_000_000)`), and advance the `questProgress` delivered-day counter (`++sub.questProgress`, the slot-0 completion = MINT_ETH per delivered day; it is a COUNT, not a BURNIE amount). This accrue is mode-agnostic (AGG-04 — `questProgress` is mode-independent; the ticket leg in 354-05 advances the same counter + accrues `buyerOwedBurnie`). The accrue is ONE warm in-slot SSTORE into the already-warm Sub slot, ZERO cross-contract (AGG-01). NO `windowStartDay`/`lastSettledDay` marker (AGG-05 — the running balances self-mark).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - In `processSubscriberStage`, the lootbox per-buy path no longer calls `quests.handlePurchase`, `affiliate.payAffiliate`, or `coinflip.creditFlip`: `sed -n '539,851p' contracts/modules/GameAfkingModule.sol | grep -cE "quests\.handlePurchase|affiliate\.payAffiliate|coinflip\.creditFlip"` returns 0.
    - The accrue writes `affiliateBase` and `questProgress`: `sed -n '539,851p' contracts/modules/GameAfkingModule.sol | grep -cE "affiliateBase|questProgress"` is ≥ 2.
    - The SOLVENCY-01 debit lines `afkingFunding[src] -= ethValue;` and `claimablePool -= uint128(ethValue);` are byte-unchanged at their site: `grep -n "afkingFunding\[src\] -= ethValue" contracts/modules/GameAfkingModule.sol` and `grep -n "claimablePool -= uint128(ethValue)" contracts/modules/GameAfkingModule.sol` both present.
    - `forge build` exits 0.
  </acceptance_criteria>
  <done>The per-buy lootbox storm is replaced by a single warm in-slot accrue (flat-7% affiliateBase + questProgress counter), the box stamp + the SOLVENCY-01 debit are preserved, and forge build is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add the inline _settleQuest + the STAGE-riding settle-day hook + claimQuest fallback (AGG-02)</name>
  <files>contracts/modules/GameAfkingModule.sol, contracts/interfaces/IDegenerusGameModules.sol</files>
  <read_first>
    `contracts/modules/GameAfkingModule.sol:539-851` (the STAGE loop — the settle rides the per-sub iteration), `:1041` (`_ethToBurnie`). SPEC `## AGG` AGG-02 bullet (353-SPEC.md:184) + `## QST` "Quest settle is AUTOMATIC, RIDING THE BUY STAGE" (353-SPEC.md:214-219) — on the global settle day (`currentDay % settlePeriod == 0`, ~10-day cadence) the inline `_settleQuest(sub)` mints `questProgress × QUEST_SLOT0_REWARD` + the accrued `buyerOwedBurnie` in ONE `creditFlip` to the SUB + advances the streak via the DegenerusQuests onlyGame entrypoint, then zeroes both counters; the separate `mintBurnie` settlement-due router leg is REJECTED. The `claimQuest(address[] subs)` permissionless fallback runs the SAME `_settleQuest` (always credits the sub, never the caller). SPEC `## Threat Model Re-Attestation` SEC-02 row (353-SPEC.md:164 — the quest credit is OFF the ETH path) + the no-double-settle row (353-SPEC.md:161). The DegenerusQuests onlyGame settle entrypoint (354-02 SUMMARY for its exact signature). `contracts/interfaces/IDegenerusGameModules.sol` (the IGameAfkingModule interface — add `claimQuest`).
  </read_first>
  <action>
    Add an internal `_settleQuest(address player, Sub storage sub)` that: (1) reads `questProgress` and `buyerOwedBurnie`; (2) if BOTH are zero, returns (self-marking no-op — AGG-05); (3) computes `owed = uint256(questProgress) × QUEST_SLOT0_REWARD + _wholeBurnieToBaseUnits(buyerOwedBurnie)` (the quest slot-0 BURNIE + the accrued ticket buyer-bonus — note `buyerOwedBurnie` is whole-BURNIE so convert to base units for the mint); (4) calls the DegenerusQuests `onlyGame` settle entrypoint (354-02) IN GAME CONTEXT to advance the streak for the delivered days (pass the delivered-day count derived from the debit-gated counter + the current day); (5) `coinflip.creditFlip(player, owed)` in ONE call to the SUB (OFF the ETH/`claimablePool` solvency path — a BURNIE-emission change only); (6) zeroes `sub.questProgress` and `sub.buyerOwedBurnie`. Hook the settle into the STAGE: on the global settle day (`currentDay % settlePeriod == 0` — derive `currentDay`/`settlePeriod` cheaply globally, NOT a per-sub cold marker; `settlePeriod` ≈ 10), call `_settleQuest(player, sub)` INLINE in the per-sub STAGE iteration (riding the warm Sub-slot write the buy already does). Add a `settlePeriod` constant (~10; the exact value is design, the SUB_STAGE_BATCH shrink to keep the settle-day chunk under 16.7M is DEFERRED to 355 GAS — do NOT pin the batch number here). Add a permissionless `function claimQuest(address[] calldata subs)` that walks the array calling the SAME `_settleQuest(sub, _subOf[sub])` for each (always credits the SUB, never `msg.sender`) — the keeper-liveness fallback covering off-cadence. Declare `claimQuest` on `IGameAfkingModule` in `IDegenerusGameModules.sol`. NO affiliate distribution runs in `_settleQuest` (the affiliate leg is PULL — 354-04). NO per-sub double-settle day marker (the global-day check + the self-marking counters suffice — AGG-05).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function _settleQuest" contracts/modules/GameAfkingModule.sol` ≥ 1; `grep -c "function claimQuest" contracts/modules/GameAfkingModule.sol` ≥ 1.
    - `_settleQuest` body mints via `coinflip.creditFlip` and zeroes both `questProgress` and `buyerOwedBurnie` (grep shows `creditFlip` + both field assignments to 0/`delete`).
    - `claimQuest` is declared in `contracts/interfaces/IDegenerusGameModules.sol`.
    - The settle-day gate uses `% settlePeriod` (grep shows a `% settlePeriod` or equivalent modulo against a ~10 global epoch).
    - `_settleQuest` contains NO `affiliate.` call (the affiliate leg is PULL).
    - `forge build` exits 0.
  </acceptance_criteria>
  <done>The inline _settleQuest mints the quest BURNIE + buyerOwedBurnie in one creditFlip to the sub on the global settle day (riding the STAGE), drains both counters, advances the streak via the onlyGame entrypoint, and the claimQuest fallback runs the same path; forge build clean.</done>
</task>

<task type="auto">
  <name>Task 3: Quest-settle-on-unsub + first-sub-only head-start (AGG-03 / QST-01 / QST-02)</name>
  <files>contracts/modules/GameAfkingModule.sol</files>
  <read_first>
    `contracts/modules/GameAfkingModule.sol:234-342` (subscribe — the CANCEL branch :291-304 = unsub; the UPSERT branch :309-341 = create/replace). SPEC `## AGG` AGG-03 (353-SPEC.md:185) — an unsub triggers `_settleQuest` draining `questProgress`+`buyerOwedBurnie` BEFORE applying the change; the affiliate base is NOT flushed (persists for uplines to PULL). SPEC `## QST` "Streak model" (353-SPEC.md:221-227) + the `## QST-02 REFRAME` (353-SPEC.md:227) — first-sub-only `+daysToNextSettle` head-start via `hasEverSubscribed` 1-bit, bounded +0..+9, once/account, no provisional; −10/reset on unsub. SPEC `## Threat Model Re-Attestation` SEC-01 no-streak-dodge row (353-SPEC.md:162). The DegenerusQuests onlyGame streak entrypoint (354-02).
  </read_first>
  <action>
    UNSUB-settle (AGG-03): in the subscribe CANCEL branch (`dailyQuantity == 0`, `:291-304`), BEFORE writing the `dailyQuantity = 0` tombstone, call `_settleQuest(subscriber, c)` to drain the accrued `questProgress` + `buyerOwedBurnie` (one `creditFlip` to the sub). Do NOT flush `affiliateBase` (it persists in the slot for the uplines to PULL via 354-04 — an unsub does not forfeit the uplines' accrued affiliate). Apply the streak −10/reset semantics the design specifies for an unsub (same as a manual buyer's gap-reset — route through the onlyGame entrypoint or the existing reset logic; do NOT introduce a new player-callable streak lever). FIRST-SUB head-start (QST-01/QST-02): in the UPSERT branch (`:309-341`), when `s.hasEverSubscribed == false` on a create, grant the first-sub-only `+daysToNextSettle` streak head-start — compute `daysToNextSettle = settlePeriod - (currentDay % settlePeriod)` (bounded +0..+9, always < settlePeriod≈10), advance `state.streak += daysToNextSettle` via the onlyGame entrypoint (GAME context), and SET `s.hasEverSubscribed = true` (set-once latch — re-subs after a cancel get NO fresh head-start). This is a REAL `state.streak` advance applied immediately (NO provisional/escrow — QST-02 reframe, USER-ACCEPTED-BY-DESIGN). The per-window streak still advances only on debit-DELIVERED days (the delivered-day gate stays in force, enforced via the debit-gated counter in Task 1 + the entrypoint in 354-02) — the head-start is the ONLY direct streak grant.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The CANCEL branch calls `_settleQuest` before the tombstone write: `sed -n '285,305p' contracts/modules/GameAfkingModule.sol | grep -c "_settleQuest"` ≥ 1.
    - The CANCEL branch does NOT flush `affiliateBase` (grep shows no `affiliateBase = 0` / `delete .*affiliateBase` in the cancel branch).
    - The UPSERT branch gates the head-start on `hasEverSubscribed` and sets it true: `grep -c "hasEverSubscribed" contracts/modules/GameAfkingModule.sol` ≥ 2 (one read-gate, one set).
    - The head-start uses `daysToNextSettle` (or an equivalent `settlePeriod - (currentDay % settlePeriod)` expression).
    - `forge build` exits 0.
  </acceptance_criteria>
  <done>An unsub settles the quest counters first (affiliate base preserved for the uplines), and the first-ever subscribe grants the bounded +0..+9 head-start once per account via hasEverSubscribed; forge build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keeper/advance → STAGE | the STAGE is permissionless-driven; the per-buy accrue + the settle-day mint must be unmanipulable by sub/unsub timing |
| player → subscribe/unsub | the first-sub head-start + the unsub-settle are player-triggered; the head-start must be once/account and the unsub must not forfeit uplines' affiliate |
| GameAfkingModule → coinflip / DegenerusQuests | the only cross-contract legs are the deferred settle credit + the streak advance (both OFF the ETH path) |

## STRIDE Threat Register (this plan's slice of the SPEC Threat Model Re-Attestation)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-354-03-SEC01-CHURN | Elevation (positive-EV churn) | strategic sub/unsub re the affiliate base + the quest settle | mitigate | SPEC SEC-01 churn row (353-SPEC.md:159): the `affiliateBase` persists for the uplines on unsub (churn forfeits nothing AND gains nothing); `_settleQuest` drains `questProgress`+`buyerOwedBurnie` on unsub so a re-sub cannot double-harvest. **TST-356 SEC-01:** churn ≤ steady; the affiliate base is the uplines' money. |
| T-354-03-SEC01-DBLSETTLE | Tampering (double-settle) | _settleQuest fired twice (settle-day + claimQuest + unsub) | mitigate | SPEC AGG-05 / no-double-settle row (353-SPEC.md:161/187): `_settleQuest` zeroes both counters; a second fire finds 0 → no-op (self-marking, no day marker). **TST-356:** idempotent settle. |
| T-354-03-SEC01-STREAK | Elevation (streak-dodge / pre-credit inflation) | the first-sub head-start + the per-window streak | accept-by-design (head-start) / mitigate (per-window) | SPEC QST-02 reframe (353-SPEC.md:162/227): the bounded +0..+9 once/account head-start is USER-ACCEPTED-BY-DESIGN (pass-cost-gated sybil); the per-window streak advances only on debit-DELIVERED days (the C3-a fix stays in force — Task 1's debit-gated counter). **TST-356:** N-skip == manual N-miss; head-start ≤+9 once/account. |
| T-354-03-SEC02-SOLV | Tampering | the SOLVENCY-01 debit | mitigate | SPEC SEC-02 (353-SPEC.md:164/169): the debit at :708-711 is BYTE-UNCHANGED; affiliate/quest credits are BURNIE `creditFlip` only (a timing change, never an ETH cut). **TST-356 SEC-02 delta-audit.** |
| T-354-03-SEC02-RNG | Tampering | the accrue/settle vs RNG-window state | mitigate | SPEC RNG-freeze row (353-SPEC.md:165/171): the accrue/settle touch no `rngWordByDay`/frozen-window slot; the open path is unchanged. **TST-356 OPEN-02.** |

No package installs in this plan (no `T-{phase}-SC` row).
</threat_model>

<verification>
- `forge build` clean.
- The per-buy lootbox storm is gone; the accrue is one warm in-slot SSTORE.
- The SOLVENCY-01 debit is byte-unchanged.
- _settleQuest mints once to the sub and drains both counters; claimQuest runs the same path; unsub settles first without flushing affiliateBase; the first-sub head-start is once/account.
</verification>

<success_criteria>
The mode-agnostic aggregator accrues cheap per buy (AGG-01/04), settles the quest leg by riding the STAGE on the global settle day + the claimQuest fallback (AGG-02), settles the quest on unsub without flushing the affiliate base (AGG-03), uses self-marking running balances (AGG-05), grants the bounded once/account head-start (QST-01/02), and keeps SOLVENCY-01 byte-unchanged. SUB_STAGE_BATCH shrink is deferred to 355.
</success_criteria>

<output>
Create `.planning/phases/354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre/354-03-SUMMARY.md` when done.
</output>
