---
phase: 395-mutation
plan: 02
type: execute
wave: 2
depends_on: [395-01]
files_modified:
  - audit/mutation/CAMPAIGN-REPORT-v63.md
  - audit/mutation/SURVIVOR-TRIAGE-v63.md
autonomous: true
requirements: [MUT-01, MUT-02]
must_haves:
  truths:
    - "The campaign runs over the frozen subject with the fix-site target set + the comprehensive oracle (via_ir), paced/resumable so a 5h cap never strands it mid-mutant."
    - "The mutation score (killed / total) is measured and recorded per target."
    - "Every surviving mutant is triaged: FALSE survivor (oracle gap / equivalent mutant / unreachable) vs GENUINE oracle gap."
    - "contracts/ is byte-identical to a8b702a7 after the campaign completes."
  artifacts:
    - path: "audit/mutation/CAMPAIGN-REPORT-v63.md"
      provides: "Per-target mutation score + total + the run pacing/resumption record + the byte-freeze attestation"
      contains: "mutation score"
    - path: "audit/mutation/SURVIVOR-TRIAGE-v63.md"
      provides: "Every survivor classified FALSE vs GENUINE with the per-survivor reasoning + the mutated line"
      contains: "GENUINE"
  key_links:
    - from: "audit/mutation/CAMPAIGN-REPORT-v63.md"
      to: "audit/mutation/PROGRESS-v63.log"
      via: "per-target DONE killed=/uncaught= lines"
      pattern: "killed=|uncaught="
    - from: "audit/mutation/SURVIVOR-TRIAGE-v63.md"
      to: "the per-target -mut-v63 slither output"
      via: "each survivor's mutated line cited"
      pattern: "UNCAUGHT|survivor"
---

<objective>
Run the corrected campaign (built in 395-01) over the frozen subject `a8b702a7`, measure and record
the mutation score per target, and triage every survivor as FALSE (oracle gap caused by an
unreachable/equivalent mutant, NOT a net hole) versus GENUINE (a real oracle gap the regression net
missed). This is the long-pole — it runs paced and resumable so a 5h cap stop never strands a mutant
in `contracts/`.

Purpose: quantify whether the regression net actually CATCHES injected defects in the highest-value
v63-changed + spine functions, and isolate the genuine oracle gaps for Plan 03 to kill or route.
Output: the campaign report (per-target score + total) and the survivor-triage ledger (every survivor
classified with reasoning).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@test/REGRESSION-BASELINE-v63.md
@audit/mutation/TARGETS-v63.md
@audit/mutation/HARNESS-VALIDATION-v63.md

<read_first>
- audit/mutation/run-campaign-v63.sh (the kill-safe resumable runner from 395-01; drive it, do not edit it)
- audit/mutation/oracle-comprehensive.sh (the comprehensive oracle from 395-01)
- audit/mutation/TARGETS-v63.md (the named target set + which oracle test exercises each — the triage key)
- .planning/v63-surface-map/{solvency,coinflip-burnie,storage-packing}.md (to judge equivalent/unreachable mutants per-target)
</read_first>

<interfaces>
<!-- Mutation-score arithmetic. slither-mutate logs each mutant as caught (oracle failed = killed) or
     UNCAUGHT (oracle still green = survivor). Per target: total = killed + uncaught; score = killed/total.
     A survivor is FALSE if the mutant is EQUIVALENT (no behavioral change — e.g. a redundant mask on an
     already-bounded value, a dead branch, an unreachable revert) or the line is genuinely unreachable
     under the subject's invariants; GENUINE if a real behavioral defect on a reachable path went green. -->
PROGRESS-v63.log line shape (written by run-campaign-v63.sh):
  <contract-name> DONE killed=<K> uncaught=<U> elapsed=<N>s <iso8601>
Survivor source: audit/mutation/<contract-name>-mut-v63/ + the grepped UNCAUGHT entries in
  audit/mutation/<contract-name>-v63.log (each carries the mutated file:line + the mutator + the diff).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| long-running campaign → 5h cap | a cap-stop mid-mutant must leave contracts/ restored, not dirty |
| survivor classification → net-hole conclusion | mislabeling a GENUINE survivor as FALSE hides a real net hole |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-395-04 | Tampering | a cap-stop / kill mid-campaign strands a mutant | mitigate | the runner's EXIT/INT/TERM trap restores; this plan re-asserts `git diff a8b702a7 -- contracts/` empty after each resumed segment and before writing any report |
| T-395-05 | Repudiation (false-green) | a GENUINE survivor mislabeled FALSE | mitigate | every FALSE classification cites the equivalence/unreachability reason against the surface-map invariant; ambiguous survivors default to GENUINE → carried to Plan 03 |
| T-395-06 | DoS (pacing) | the campaign exceeds a single 5h window | mitigate | run per-target via `--single` in the background, checkpoint per target `.DONE`; resume the next target on re-invoke |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Run the paced, resumable campaign over the fix-site target set</name>
  <files>audit/mutation/CAMPAIGN-REPORT-v63.md</files>
  <read_first>
    - audit/mutation/run-campaign-v63.sh + audit/mutation/oracle-comprehensive.sh (drive these)
    - audit/mutation/TARGETS-v63.md (the target order)
    - test/REGRESSION-BASELINE-v63.md (the green oracle the per-target baseline gate confirms)
  </read_first>
  <action>
    Drive the campaign target-by-target using the runner from 395-01, paced for the 5h cap. For each
    target in TARGETS-v63.md order (smallest/highest-signal first): invoke `audit/mutation/run-campaign-v63.sh
    --single <contract-name>` as a BACKGROUND run (it is long: via_ir compile ~30s/mutant × the mutant
    count); when the target's `.DONE` checkpoint appears, read its `<name>-v63.log` and the
    `<name>-mut-v63/` output, then move to the next target. If a 5h cap stops a run mid-target,
    re-invoke the same `--single <contract-name>` — slither-mutate restarts that target from a clean
    (trap-restored) tree; the prior completed targets stay `.DONE` and are skipped. Before reading any
    log, assert `git diff a8b702a7 -- contracts/` is EMPTY (the trap restored). Do NOT lower the target
    set to "fit" — every named fix-site target is run; if the total run cannot complete in the available
    windows, leave the remaining targets' `.DONE` absent and record the campaign as IN-PROGRESS with the
    completed targets scored (resumable), never silently dropped.

    Record `audit/mutation/CAMPAIGN-REPORT-v63.md`: a per-target table (contract · target functions ·
    mutants total · killed · UNCAUGHT survivors · mutation score killed/total) sourced from the
    PROGRESS-v63.log DONE lines + the per-target logs; the aggregate score across all completed targets;
    the run-pacing record (which targets ran in which window, any cap-stop + resume); the bounded-oracle
    note (FOUNDRY_FUZZ_RUNS=64 etc. for the campaign — survivors re-verified at full runs in Task 2 /
    Plan 03); and the byte-freeze attestation (`git diff a8b702a7 -- contracts/` empty + tree-hash
    `2934d3d8987a09c5f073549a0cb499f6c5f28620` after the last completed target). Do NOT inline raw
    slither output beyond the per-survivor file:line needed for Task 2. The campaign edits contracts/
    only transiently (slither-mutate + trap-restore) — this plan persists only audit/ report files.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && git checkout -- contracts/ 2>/dev/null; test "$(git rev-parse HEAD:contracts)" = "2934d3d8987a09c5f073549a0cb499f6c5f28620" && test -z "$(git diff a8b702a7 -- contracts/)" && grep -qiE 'mutation score' audit/mutation/CAMPAIGN-REPORT-v63.md && grep -cE 'killed=|uncaught=' audit/mutation/PROGRESS-v63.log && echo "CAMPAIGN report present, contracts clean"</automated>
  </verify>
  <done>
    The campaign ran over the fix-site target set with the comprehensive oracle (via_ir), paced and
    resumable; CAMPAIGN-REPORT-v63.md records the per-target + aggregate mutation score, the pacing/resume
    record, and the byte-freeze attestation; PROGRESS-v63.log carries the per-target killed/uncaught lines;
    `git diff a8b702a7 -- contracts/` is EMPTY and the tree-hash is `2934d3d8…`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Triage every survivor — FALSE vs GENUINE (SURVIVOR-TRIAGE-v63.md)</name>
  <files>audit/mutation/SURVIVOR-TRIAGE-v63.md</files>
  <read_first>
    - audit/mutation/CAMPAIGN-REPORT-v63.md (the per-target survivor counts, from Task 1)
    - audit/mutation/<contract-name>-v63.log + <contract-name>-mut-v63/ (the UNCAUGHT entries with file:line + mutator + diff)
    - .planning/v63-surface-map/{solvency,coinflip-burnie,storage-packing}.md (the invariant that decides equivalent/unreachable)
    - test/REGRESSION-BASELINE-v63.md (the oracle whose coverage a GENUINE survivor exposes a hole in)
  </read_first>
  <action>
    For EVERY UNCAUGHT survivor across all completed targets, write a row in
    `audit/mutation/SURVIVOR-TRIAGE-v63.md`: the contract · the mutated file:line · the mutator
    (AOR/ROR/LOR/etc.) · the mutated diff · which oracle test SHOULD have killed it · the verdict
    FALSE or GENUINE · the reasoning. Classify FALSE only with an explicit reason: (i) EQUIVALENT
    mutant — the mutation produces no observable behavioral change (e.g. a redundant mask on a value
    already bounded by an economic invariant per the surface-map, a `<=`→`<` on a bound that the
    surface-map proved can never be hit, a literal change inside a dead/unreachable branch); or (ii)
    UNREACHABLE — the line cannot execute under the subject's invariants (cite the surface-map proof,
    e.g. the lootboxEvCapPacked live-key-set ⊆ {currentLevel, currentLevel+1}). Classify GENUINE when
    the mutation changes observable behavior on a REACHABLE path and the oracle stayed green — that is
    a real net hole. RE-VERIFY each GENUINE candidate at FULL oracle runs before finalizing: re-run the
    exercising oracle test(s) at the default profile (FOUNDRY_FUZZ_RUNS=1000, INVARIANT runs=256) with
    the mutation re-applied IN PLACE, confirm it still survives green, then `git checkout -- contracts/`
    — a survivor that the bounded run missed but full runs kill is reclassified FALSE (bounded-run
    artifact, not a net hole). Ambiguous survivors default to GENUINE (carry to Plan 03 rather than
    dismiss). Note: a GENUINE survivor in a SPINE function (solvency/RNG) is the high-signal output —
    flag it as SPINE.

    End SURVIVOR-TRIAGE-v63.md with the GENUINE survivor SET (the input to Plan 03's kill/route) and a
    byte-freeze attestation (`git diff a8b702a7 -- contracts/` empty + tree-hash) after the
    re-verification mutations were restored. NEVER commit while a re-verification mutant is in place.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && git checkout -- contracts/ 2>/dev/null; test "$(git rev-parse HEAD:contracts)" = "2934d3d8987a09c5f073549a0cb499f6c5f28620" && test -z "$(git diff a8b702a7 -- contracts/)" && grep -qE 'FALSE' audit/mutation/SURVIVOR-TRIAGE-v63.md && grep -qE 'GENUINE' audit/mutation/SURVIVOR-TRIAGE-v63.md && grep -qiE 'byte-identical|tree-hash|2934d3d8' audit/mutation/SURVIVOR-TRIAGE-v63.md && echo "TRIAGE complete, contracts clean"</automated>
  </verify>
  <done>
    Every survivor is classified FALSE (with an equivalence/unreachability reason cited against the
    surface-map invariant) or GENUINE (reachable behavioral change the oracle missed), GENUINE
    candidates re-verified at full oracle runs, the GENUINE set enumerated as the Plan-03 input, and the
    byte-freeze attestation recorded; `git diff a8b702a7 -- contracts/` is EMPTY and the tree-hash is
    `2934d3d8…`.
  </done>
</task>

</tasks>

<verification>
- The campaign ran over the fix-site/spine target set with the comprehensive oracle (via_ir), paced
  and resumable (MUT-01).
- The mutation score is measured + recorded per target and in aggregate (MUT-02).
- Every survivor is triaged FALSE vs GENUINE with explicit reasoning; GENUINE candidates re-verified
  at full oracle runs (MUT-02).
- `git diff a8b702a7 -- contracts/` is EMPTY and the tree-hash is `2934d3d8987a09c5f073549a0cb499f6c5f28620`
  after the campaign and after the triage re-verification.
</verification>

<success_criteria>
The full fix-site campaign is run + scored over the frozen subject with the comprehensive oracle; the
per-target + aggregate mutation score is recorded; every survivor is triaged FALSE vs GENUINE with the
GENUINE set isolated for Plan 03; the subject is byte-frozen at a8b702a7 throughout.
</success_criteria>

<output>
Create `.planning/phases/395-mutation/395-02-SUMMARY.md` when done.
</output>
