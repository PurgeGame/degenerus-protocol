---
phase: 22-warden-simulation-regression-check
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/warden-01-contract-auditor.md
  - audit/warden-02-zero-day-hunter.md
  - audit/warden-03-economic-analyst.md
autonomous: true
requirements: [NOVEL-07]

must_haves:
  truths:
    - "Three independent warden agent reports exist, each with distinct focus areas and no cross-contamination"
    - "Each warden report uses C4A severity calibration (H/M/L/QA) with file:line citations for every finding"
    - "Each warden agent covers at least 8 of the 10 required audit areas from EXTERNAL-AUDIT-PROMPT.md"
    - "Warden agents operate blind -- they do not reference prior audit findings or Phase 19-21 results"
  artifacts:
    - path: "audit/warden-01-contract-auditor.md"
      provides: "Contract auditor warden report focused on storage, reentrancy, CEI, access control"
      min_lines: 200
    - path: "audit/warden-02-zero-day-hunter.md"
      provides: "Zero-day hunter warden report focused on EVM exploits, composition, unchecked, assembly"
      min_lines: 200
    - path: "audit/warden-03-economic-analyst.md"
      provides: "Economic analyst warden report focused on MEV, flash loans, pricing, solvency"
      min_lines: 200
  key_links:
    - from: "audit/warden-01-contract-auditor.md"
      to: "contracts/*.sol"
      via: "file:line code references"
      pattern: "\\w+\\.sol:\\d+"
    - from: "audit/warden-02-zero-day-hunter.md"
      to: "contracts/*.sol"
      via: "file:line code references"
      pattern: "\\w+\\.sol:\\d+"
    - from: "audit/warden-03-economic-analyst.md"
      to: "contracts/*.sol"
      via: "file:line code references"
      pattern: "\\w+\\.sol:\\d+"
---

<objective>
Run three independent adversarial warden agent simulations against the current Degenerus Protocol codebase, each with a different specialization (contract auditor, zero-day hunter, economic analyst), producing C4A-format findings reports.

Purpose: NOVEL-07 requires 3+ independent adversarial agents cross-referencing findings. This plan produces the three raw warden reports. The wardens operate BLIND -- they review the code fresh using EXTERNAL-AUDIT-PROMPT.md as their base prompt, augmented by role-specific focus constraints. They must NOT reference prior audit findings from Phases 19-21.

Output: Three C4A warden-format reports with severity-calibrated findings and file:line citations.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@audit/EXTERNAL-AUDIT-PROMPT.md
@audit/KNOWN-ISSUES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Warden Agent 1 -- Contract Auditor</name>
  <files>audit/warden-01-contract-auditor.md</files>
  <read_first>
    - audit/EXTERNAL-AUDIT-PROMPT.md (base prompt for all warden agents -- 258 lines)
    - contracts/DegenerusGame.sol (main game contract, delegatecall dispatcher)
    - contracts/storage/DegenerusGameStorage.sol (shared storage layout)
    - contracts/StakedDegenerusStonk.sol (sDGNRS -- soulbound, holds reserves)
    - contracts/DegenerusStonk.sol (DGNRS -- transferable ERC20 wrapper)
    - contracts/DegenerusAdmin.sol (VRF + admin management)
    - contracts/modules/DegenerusGameAdvanceModule.sol (level advancement, VRF)
    - contracts/modules/DegenerusGameMintModule.sol (ticket purchasing)
    - contracts/modules/DegenerusGameJackpotModule.sol (jackpot drawings)
    - contracts/modules/DegenerusGameGameOverModule.sol (game over distribution)
    - contracts/libraries/BitPackingLib.sol (bit packing)
  </read_first>
  <action>
Assume the role of a C4A warden with the specialization of a **Contract Auditor**. You are one of three independent wardens. Your specific focus areas are:

**Primary (80% of effort):**
- Storage layout and delegatecall safety: Validate storage-slot consistency across all 10 modules + base storage. Check packed-field reads/writes (BitPackingLib) for overlap/collision risks. Verify every delegatecall site handles return data and revert bubbling correctly. Check function-selector collision risk.
- Reentrancy and CEI: Check all external call sites (ETH/stETH/LINK/token interactions). Validate checks-effects-interactions ordering. Check cross-function reentrancy and callback reachability. Include read-only reentrancy considerations.
- Access Control: Enumerate privileged functions and effective authority. Verify post-deploy admin boundaries. Confirm initialization paths are one-time and cannot be replayed. Find privilege-escalation paths.

**Secondary (20% of effort):**
- ETH Accounting and Solvency: Trace ETH entry/exit paths. Verify solvency. Validate BPS splits.
- Precision and Rounding: Find exploitable div-before-mul. Check zero-rounding edge cases.

**Non-Negotiable Rules:**
- Perform a BLIND review. Do NOT reference any prior audit findings.
- Verify claims directly from source code, not comments.
- Do NOT inflate severity. H = direct loss/theft. M = meaningful value loss under specific conditions. L = minor risk. QA = informational.
- Every finding MUST have specific file:line references and a concrete attack path.
- If no valid findings exist in an area, state that explicitly with evidence.
- Spend time on the CODE, not on speculative claims.

**Output Format:** Follow the exact output format from EXTERNAL-AUDIT-PROMPT.md:
```
## [H-01] Title (if any)
### Impact / Attack Path / Code References / Proof of Concept / Recommended Mitigation

## [M-01] Title (if any)
### Impact / Attack Path / Code References / Proof of Concept / Recommended Mitigation

## [L-01] Title (if any)
### Description / Code References

## [QA-01] Title (if any)
### Description / Code References

## Confidence by Area (all 10 areas, High/Medium/Low)
## Coverage Gaps
## Limitations
```

Begin the file with:
```
# Warden Report: Contract Auditor
**Agent:** 1 of 3 (Contract Auditor)
**Date:** 2026-03-17
**Scope:** Degenerus Protocol -- 14 core contracts + 10 delegatecall modules
**Focus:** Storage layout, delegatecall safety, reentrancy, CEI, access control
**Methodology:** Blind adversarial review per C4A warden methodology
```

Read each contract file line by line for your focus areas. Trace all delegatecall sites in DegenerusGame.sol. Trace all external calls in sDGNRS and DGNRS. Map the complete access control model. For each finding, include the exact Solidity code snippet and line number.
  </action>
  <verify>
    <automated>grep -c "\.sol:" audit/warden-01-contract-auditor.md | awk '{if ($1 >= 20) print "PASS: 20+ file:line citations"; else print "FAIL: fewer than 20 file:line citations"}'</automated>
  </verify>
  <acceptance_criteria>
    - audit/warden-01-contract-auditor.md exists and has 200+ lines
    - File contains "Warden Report: Contract Auditor" header
    - File contains "Agent: 1 of 3" identifier
    - File contains "Confidence by Area" section with all 10 audit areas rated
    - File contains "Coverage Gaps" section
    - File contains "Limitations" section
    - File contains 20+ distinct file:line citations (pattern: SomeContract.sol:NNN)
    - Each finding (if any) has Impact, Code References, and either Attack Path or Description
    - File does NOT contain references to "Phase 19", "Phase 20", "Phase 21", "DELTA-", "NOVEL-", or "CORR-" (blind review)
  </acceptance_criteria>
  <done>Contract auditor warden report complete with C4A-format findings, 10-area confidence ratings, and 20+ file:line code citations. Report is blind -- no prior audit references.</done>
</task>

<task type="auto">
  <name>Task 2: Warden Agent 2 -- Zero-Day Hunter</name>
  <files>audit/warden-02-zero-day-hunter.md</files>
  <read_first>
    - audit/EXTERNAL-AUDIT-PROMPT.md (base prompt for all warden agents)
    - contracts/DegenerusGame.sol (main game contract)
    - contracts/storage/DegenerusGameStorage.sol (shared storage)
    - contracts/StakedDegenerusStonk.sol (sDGNRS)
    - contracts/DegenerusStonk.sol (DGNRS)
    - contracts/BurnieCoin.sol (BURNIE ERC20)
    - contracts/BurnieCoinflip.sol (coinflip)
    - contracts/DegenerusVault.sol (stETH yield)
    - contracts/modules/DegenerusGameAdvanceModule.sol (VRF, level advancement)
    - contracts/modules/DegenerusGameEndgameModule.sol (endgame)
    - contracts/modules/DegenerusGameGameOverModule.sol (game over)
    - contracts/modules/DegenerusGameDecimatorModule.sol (decimator)
    - contracts/modules/DegenerusGameLootboxModule.sol (lootbox)
    - contracts/libraries/BitPackingLib.sol (assembly SLOAD/SSTORE)
    - contracts/libraries/EntropyLib.sol (xorshift entropy)
  </read_first>
  <action>
Assume the role of a C4A warden with the specialization of a **Zero-Day Hunter**. You are one of three independent wardens. Your specific focus areas are:

**Primary (80% of effort):**
- EVM-Level Risks: Forced ETH via selfdestruct and accounting impact. Improper dependence on address(this).balance. Semantic correctness of ALL unchecked blocks (enumerate every `unchecked` in every contract). Assembly SLOAD/SSTORE layout correctness in BitPackingLib. ABI/delegatecall encoding collision risks.
- Cross-Contract Composition: Module A->B sequence integrity and state assumptions. Shared-storage composition safety across all module combinations. States valid locally but invalid globally. Focus on unusual code paths (lootbox->advance->jackpot interactions, game-over multi-step sequences).
- Temporal and Lifecycle Edge Cases: Test timestamp +/-15s sensitivity around boundaries. Level 0 pre-first-purchase behavior for all callable paths. Level transitions N->N+1 for state consistency. Post-gameOver callable behavior. Multi-step gameOver interleaving race windows.

**Secondary (20% of effort):**
- VRF/RNG Security: Validate RNG lock state machine. Check callback manipulation vectors. Verify entropy derivation.
- Precision and Rounding: Find exploitable rounding. Quantify dust accumulation.

**Non-Negotiable Rules:**
- Perform a BLIND review. Do NOT reference any prior audit findings.
- Think like an attacker: look for UNUSUAL code paths, not standard patterns.
- Every finding MUST have a concrete exploit path with step-by-step instructions.
- If you find an `unchecked` block, enumerate the exact overflow/underflow conditions and whether they are reachable.
- If you find assembly, verify slot calculations against the storage layout.
- Do NOT inflate severity. H = direct loss/theft. M = meaningful value loss under specific conditions.
- If no valid findings exist in an area, state that explicitly with evidence.

**Output Format:** Follow the exact output format from EXTERNAL-AUDIT-PROMPT.md (same as Task 1).

Begin the file with:
```
# Warden Report: Zero-Day Hunter
**Agent:** 2 of 3 (Zero-Day Hunter)
**Date:** 2026-03-17
**Scope:** Degenerus Protocol -- 14 core contracts + 10 delegatecall modules
**Focus:** EVM-level exploits, unchecked arithmetic, assembly, composition, temporal edge cases
**Methodology:** Blind adversarial review per C4A warden methodology
```

Systematically enumerate every `unchecked` block in the codebase. For each: state the file:line, the arithmetic operation, and whether overflow/underflow is reachable. Trace all assembly in BitPackingLib for slot correctness. Map the game-over state machine transitions and look for interleaving races.
  </action>
  <verify>
    <automated>grep -c "\.sol:" audit/warden-02-zero-day-hunter.md | awk '{if ($1 >= 20) print "PASS: 20+ file:line citations"; else print "FAIL: fewer than 20 file:line citations"}'</automated>
  </verify>
  <acceptance_criteria>
    - audit/warden-02-zero-day-hunter.md exists and has 200+ lines
    - File contains "Warden Report: Zero-Day Hunter" header
    - File contains "Agent: 2 of 3" identifier
    - File contains "Confidence by Area" section with all 10 audit areas rated
    - File contains "Coverage Gaps" section
    - File contains "Limitations" section
    - File contains 20+ distinct file:line citations
    - File contains at least one section analyzing `unchecked` blocks with reachability assessment
    - File does NOT contain references to "Phase 19", "Phase 20", "Phase 21", "DELTA-", "NOVEL-", or "CORR-" (blind review)
  </acceptance_criteria>
  <done>Zero-day hunter warden report complete with C4A-format findings, unchecked block enumeration, assembly verification, 10-area confidence ratings, and 20+ file:line citations. Report is blind.</done>
</task>

<task type="auto">
  <name>Task 3: Warden Agent 3 -- Economic Analyst</name>
  <files>audit/warden-03-economic-analyst.md</files>
  <read_first>
    - audit/EXTERNAL-AUDIT-PROMPT.md (base prompt for all warden agents)
    - contracts/StakedDegenerusStonk.sol (sDGNRS -- burn/redeem economics, pool management)
    - contracts/DegenerusStonk.sol (DGNRS -- transferable wrapper, wrap/unwrap economics)
    - contracts/DegenerusVault.sol (stETH yield sharing, vault share math)
    - contracts/DegenerusDeityPass.sol (deity pass pricing T(n))
    - contracts/BurnieCoin.sol (BURNIE mint paths, supply)
    - contracts/BurnieCoinflip.sol (coinflip economics, bounty)
    - contracts/DegenerusAffiliate.sol (affiliate farming)
    - contracts/modules/DegenerusGameMintModule.sol (ticket pricing, ETH splitting)
    - contracts/modules/DegenerusGameWhaleModule.sol (whale/lazy/deity pass pricing)
    - contracts/modules/DegenerusGameLootboxModule.sol (lootbox EV calculation)
    - contracts/modules/DegenerusGameDegeneretteModule.sol (degenerette betting)
    - contracts/modules/DegenerusGameJackpotModule.sol (jackpot distribution)
  </read_first>
  <action>
Assume the role of a C4A warden with the specialization of an **Economic Analyst**. You are one of three independent wardens. Your specific focus areas are:

**Primary (80% of effort):**
- Economic Attack Vectors: Model Sybil purchase influence and cost. Analyze whale/lazy/deity pass pricing for arbitrage and griefing. Check deity triangular pricing T(n)=n*(n+1)/2 for manipulation. Verify lootbox EV never exceeds intended caps. Analyze affiliate self-referral/circular farming. Check vault share math against donation/inflation attacks. Check cross-system arbitrage (Game vs Vault vs DGNRS vs Degenerette). Model DGNRS burn-redeem economics for flash loan, sandwich, and accumulation attacks.
- ETH Accounting and Solvency: Trace every ETH entry and exit path. Verify solvency under normal + adversarial flows. Validate BPS splits (no value creation/leakage). Verify pull-withdraw accounting and claim invariants.
- Precision and Rounding: Find div-before-mul precision loss that is exploitable. Check zero-rounding buy/mint/claim edge cases. Quantify dust accumulation across split/credit/claim cycles. Validate vault rounding directions (mint/redeem fairness).

**Secondary (20% of effort):**
- VRF/RNG Security: Check if RNG outcomes have MEV extractable value.
- Temporal Edge Cases: Analyze stETH yield timing, burn timing, and multi-step game-over settlement ordering.

**Non-Negotiable Rules:**
- Perform a BLIND review. Do NOT reference any prior audit findings.
- For every economic attack vector: provide explicit COST vs PROFIT math. No "possible" attacks without numbers.
- Model attacks with concrete ETH amounts (use 1000 ETH budget as baseline).
- Check every BPS constant in the codebase. Verify denominators match (10_000 for BPS, 1_000_000 for PPM).
- If you find a profitable attack, provide the exact EV calculation and minimum capital required.
- If no valid findings exist in an area, state that explicitly with evidence.
- Do NOT inflate severity. H = direct loss/theft. M = meaningful value loss under specific conditions.

**Output Format:** Follow the exact output format from EXTERNAL-AUDIT-PROMPT.md (same as Tasks 1-2).

Begin the file with:
```
# Warden Report: Economic Analyst
**Agent:** 3 of 3 (Economic Analyst)
**Date:** 2026-03-17
**Scope:** Degenerus Protocol -- 14 core contracts + 10 delegatecall modules
**Focus:** MEV, flash loans, pricing manipulation, solvency invariants, economic game theory
**Methodology:** Blind adversarial review per C4A warden methodology
```

For DGNRS burn-redeem: trace the exact formula in StakedDegenerusStonk.sol burn(), compute proportional shares, and model flash loan / sandwich / accumulation attacks with explicit math. For vault: model first-depositor attacks, share inflation, and donation attacks. For each BPS split in the codebase: verify the remainder goes to the correct recipient and no wei leaks.
  </action>
  <verify>
    <automated>grep -c "\.sol:" audit/warden-03-economic-analyst.md | awk '{if ($1 >= 20) print "PASS: 20+ file:line citations"; else print "FAIL: fewer than 20 file:line citations"}'</automated>
  </verify>
  <acceptance_criteria>
    - audit/warden-03-economic-analyst.md exists and has 200+ lines
    - File contains "Warden Report: Economic Analyst" header
    - File contains "Agent: 3 of 3" identifier
    - File contains "Confidence by Area" section with all 10 audit areas rated
    - File contains "Coverage Gaps" section
    - File contains "Limitations" section
    - File contains 20+ distinct file:line citations
    - File contains at least one section with explicit COST vs PROFIT calculations for an economic attack vector
    - File does NOT contain references to "Phase 19", "Phase 20", "Phase 21", "DELTA-", "NOVEL-", or "CORR-" (blind review)
  </acceptance_criteria>
  <done>Economic analyst warden report complete with C4A-format findings, explicit economic attack math, BPS verification, 10-area confidence ratings, and 20+ file:line citations. Report is blind.</done>
</task>

</tasks>

<verification>
All three warden reports exist with distinct focus areas:
- warden-01: Storage, delegatecall, reentrancy, CEI, access control
- warden-02: EVM-level, unchecked, assembly, composition, temporal
- warden-03: Economic, solvency, precision, pricing, flash loan

Each report follows C4A output format with severity calibration.
No report references prior audit findings (blind review check).
Each report has 20+ file:line citations proving code was actually read.
</verification>

<success_criteria>
- Three independent warden reports exist in audit/ directory
- Each report has 200+ lines with 20+ file:line citations
- Each report covers its designated focus areas with High/Medium/Low confidence ratings
- Each report uses strict C4A severity calibration
- No cross-contamination between agent reports (no references to each other's findings)
- No anchoring on prior audit findings (blind review)
</success_criteria>

<output>
After completion, create `.planning/phases/22-warden-simulation-regression-check/22-01-SUMMARY.md`
</output>
