---
phase: 12-rng-state-function-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v1.2-rng-storage-variables.md
autonomous: true
requirements:
  - RVAR-01
  - RVAR-02
  - RVAR-04

must_haves:
  truths:
    - "Every storage variable holding a VRF word or derived entropy is listed with slot, type, and lifecycle"
    - "Every storage variable influencing RNG outcome selection (bucket counts, queue indices, ticket counts) is listed with slot, type, and lifecycle"
    - "lastLootboxRngWord and midDayTicketRngPending have complete write/read/lifecycle traces"
  artifacts:
    - path: "audit/v1.2-rng-storage-variables.md"
      provides: "Complete catalogue of RNG-related storage variables"
      contains: "rngWordCurrent"
  key_links:
    - from: "contracts/storage/DegenerusGameStorage.sol"
      to: "audit/v1.2-rng-storage-variables.md"
      via: "Variable extraction with slot numbers"
      pattern: "Slot.*rngWord|lootboxRng|midDayTicket"
---

<objective>
Catalogue every storage variable in DegenerusGameStorage.sol that holds VRF entropy or influences RNG outcome selection.

Purpose: Foundation inventory for the RNG security audit -- downstream phases (delta verification, manipulation window analysis) need an authoritative list of what state touches RNG.
Output: `audit/v1.2-rng-storage-variables.md` with two tables (direct RNG vars, RNG-influencing vars) plus detailed lifecycle traces for `lastLootboxRngWord` and `midDayTicketRngPending`.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@audit/v1.0-rng-and-changes-audit.md
@contracts/storage/DegenerusGameStorage.sol
</context>

<tasks>

<task type="auto">
  <name>Task 1: Catalogue direct RNG storage variables (RVAR-01)</name>
  <files>audit/v1.2-rng-storage-variables.md</files>
  <action>
Read `contracts/storage/DegenerusGameStorage.sol` completely. Identify every storage variable that directly holds a VRF word or value derived from VRF entropy. For each variable, record:

1. **Variable name** (exact Solidity identifier)
2. **EVM slot number** -- use the slot layout comment at the top of the file; for mapping types note the base slot and key type
3. **Solidity type** (uint256, mapping(uint48 => uint256), bool, etc.)
4. **Lifecycle**: Who writes it (function + module + line), who reads it (function + module + line), when it is cleared/overwritten

Known direct RNG variables to find (non-exhaustive -- discover any others):
- `rngWordCurrent` (daily VRF word staging)
- `rngWordByDay` mapping (daily VRF word storage)
- `vrfRequestId` (pending VRF request tracker)
- `lootboxRngWordByIndex` mapping (lootbox VRF words)
- `lootboxRngRequestIndexById` mapping (VRF request to index mapping)
- `lootboxRngIndex` (current lootbox RNG index counter)
- `lastLootboxRngWord` (most recent lootbox word cache)
- `midDayTicketRngPending` (flag for mid-day ticket RNG)

To find writers/readers: grep each variable name across ALL contracts in `contracts/` and `contracts/modules/`. Record every occurrence with file:line.

Create the first section of `audit/v1.2-rng-storage-variables.md` with a markdown table:

```
| Variable | Slot | Type | Writers (function:line) | Readers (function:line) | Cleared When |
```

Include a header explaining scope and methodology.
  </action>
  <verify>
    <automated>grep -c "rngWordCurrent\|rngWordByDay\|vrfRequestId\|lootboxRngWordByIndex\|lastLootboxRngWord\|midDayTicketRngPending\|lootboxRngIndex" audit/v1.2-rng-storage-variables.md | awk '{if ($1 >= 7) print "PASS: all 7+ key vars found"; else print "FAIL: missing key vars, found " $1}'</automated>
  </verify>
  <done>Table lists every direct RNG variable with slot, type, and complete writer/reader/lifecycle analysis. At minimum: rngWordCurrent, rngWordByDay, vrfRequestId, lootboxRngWordByIndex, lootboxRngRequestIndexById, lootboxRngIndex, lastLootboxRngWord, midDayTicketRngPending.</done>
</task>

<task type="auto">
  <name>Task 2: Catalogue RNG-influencing variables and trace lastLootboxRngWord/midDayTicketRngPending (RVAR-02, RVAR-04)</name>
  <files>audit/v1.2-rng-storage-variables.md</files>
  <action>
Continue building `audit/v1.2-rng-storage-variables.md`. Add two more sections:

**Section 2: RNG-Influencing Variables (RVAR-02)**

These are storage variables that do NOT hold entropy but influence which outcome RNG selects. Search `DegenerusGameStorage.sol` and modules for variables that:
- Determine bucket composition for jackpot draws (ticket counts per trait, deity pass arrays, etc.)
- Control queue indices that determine which tickets get processed (readKey/writeKey, ticket queue mappings)
- Hold counts used in modular arithmetic with RNG words (total ticket counts, active player counts, bucket sizes)
- Gate or route RNG consumption (rngLockedFlag, prizePoolFrozen, rngRequestTime, ticketsFullyProcessed)

For each, record: Variable name, EVM slot, Solidity type, how it influences RNG outcome selection (brief description), writers/readers.

Key variables to find (non-exhaustive):
- `rngLockedFlag`, `prizePoolFrozen` (RNG gates)
- `rngRequestTime`, `ticketsFullyProcessed` (timing/sequencing)
- Ticket queue variables (readKey, writeKey, ticketQueue mappings)
- Deity pass arrays (`deityPassOwners`, `deityBySymbol`)
- Bucket/trait count variables used in jackpot selection
- `lootboxRngPendingEth`, `lootboxRngPendingBurnie` (lootbox thresholds)
- `lootboxRngThreshold`, `lootboxRngMinLinkBalance`
- `totalFlipReversals` / nudge-related state

Create a second table with same format plus an "Influence" column.

**Section 3: Detailed Lifecycle Traces (RVAR-04)**

For `lastLootboxRngWord` and `midDayTicketRngPending` specifically, write a detailed narrative trace:

For each variable:
1. Where it is declared (file:line, slot)
2. Every write site -- exact function, module, line number, what value is written and under what condition
3. Every read site -- exact function, module, line number, what decision the read value drives
4. When/how it is cleared or overwritten
5. Lifecycle state machine diagram (text-based, like the rngLockedFlag diagram in v1.0 audit)

Grep these variable names across all `.sol` files to ensure completeness.
  </action>
  <verify>
    <automated>grep -c "rngLockedFlag\|prizePoolFrozen\|ticketsFullyProcessed\|readKey\|writeKey\|lastLootboxRngWord.*Lifecycle\|midDayTicketRngPending.*Lifecycle" audit/v1.2-rng-storage-variables.md | awk '{if ($1 >= 5) print "PASS: influencing vars and lifecycles present"; else print "FAIL: missing content, found " $1}'</automated>
  </verify>
  <done>RNG-influencing variables table is complete with gate/queue/bucket variables. lastLootboxRngWord and midDayTicketRngPending each have a full lifecycle trace with every write site, read site, clear condition, and state machine diagram.</done>
</task>

</tasks>

<verification>
- Document contains three distinct sections: Direct RNG Variables, RNG-Influencing Variables, Detailed Lifecycle Traces
- Every variable has slot number (or base slot for mappings), Solidity type, and writer/reader references with line numbers
- lastLootboxRngWord and midDayTicketRngPending each have state machine diagrams
- Cross-reference against v1.0 audit: all variables mentioned there must appear here plus any new ones
</verification>

<success_criteria>
- All direct VRF entropy variables catalogued (minimum 8)
- All RNG-influencing variables catalogued (minimum 10)
- lastLootboxRngWord lifecycle: every write/read/clear with exact line references
- midDayTicketRngPending lifecycle: every write/read/clear with exact line references
- No storage variable touching RNG omitted (verified by grepping storage contract)
</success_criteria>

<output>
After completion, create `.planning/phases/12-rng-state-function-inventory/12-01-SUMMARY.md`
</output>
