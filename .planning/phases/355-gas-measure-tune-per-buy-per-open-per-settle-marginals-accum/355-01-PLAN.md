---
phase: 355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/gas/V56AfkingGasMarginal.t.sol
autonomous: true
requirements: [GAS-01, GAS-02]
must_haves:
  truths:
    - "A v56 gas-marginal harness measures the per-buy lootbox marginal, the per-buy ticket marginal, the per-open marginal, the per-settle marginal, and the worst-case settle-day STAGE chunk at SUB_STAGE_BATCH — each via the (gasN − gasN−1) snapshot/revert loop-N-divide MARGINAL (never a single-item total)"
    - "Every measured marginal AND the worst-case settle-day STAGE chunk are recorded as named-uint logs and asserted under the 16.7M HARD per-tx ceiling"
    - "The harness proves GAS-02: the per-buy accrue makes NO new cold per-buy SSTORE (the affiliateBase/questProgress/buyerOwedBurnie accumulator lives in the already-warm Sub slot), confirmed via a state-diff trace over a per-buy STAGE iteration"
  artifacts:
    - path: "test/gas/V56AfkingGasMarginal.t.sol"
      provides: "the v56 per-buy / per-open / per-settle marginal-gas harness + the GAS-02 warm-slot/no-new-cold-SSTORE trace, on the e18af451 applied tree"
      contains: "EFFECTIVE_GAS_CEILING"
      min_lines: 200
  key_links:
    - from: "test/gas/V56AfkingGasMarginal.t.sol"
      to: "GameAfkingModule.processSubscriberStage"
      via: "new-day advanceGame() STAGE over N funded subs"
      pattern: "advanceGame"
    - from: "test/gas/V56AfkingGasMarginal.t.sol"
      to: "GameAfkingModule._settleQuest settle-day ride"
      via: "warp the STAGE onto a processDay % SETTLE_PERIOD == 0 boundary"
      pattern: "SETTLE_PERIOD"
---

<objective>
Build the v56 gas-marginal measurement harness on the IMPL-applied tree (commit `e18af451`, baseline `453f8073`) and measure every everyday-afking marginal the GAS phase needs: the per-buy LOOTBOX marginal, the per-buy TICKET marginal (the new minimal-write primitive, off the old ~262k `purchaseWith`), the per-open marginal, the per-SETTLE marginal (the settle-day chunk that rides the daily buy STAGE), and the worst-case settle-day STAGE chunk at `SUB_STAGE_BATCH`. Also prove GAS-02 empirically: the per-buy in-slot accrue makes NO new cold per-buy SSTORE.

Purpose: every downstream GAS decision (the `SUB_STAGE_BATCH` re-tune in 355-03, the scavenger candidate sizing in 355-02, the Outcome-A-vs-net-tune call) is grounded in MEASURED numbers, not SPEC placeholders. The numbers ARE the GAS-01/GAS-02 deliverable.

Output: `test/gas/V56AfkingGasMarginal.t.sol` — the measured marginals as named-uint logs, each asserted under the 16.7M ceiling, plus the GAS-02 no-new-cold-SSTORE trace. Test-only; agent-committable (no contract gate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre/354-06-SUMMARY.md

<interfaces>
<!-- The v55 harness (test/gas/V55AfkingGasMarginal.t.sol) is the PATTERN to fork. Its comment header describes the OLD v55/349.2 tree (the per-buy cross-contract storm, the 232-bit Sub slot) — those facts are STALE under v56 and MUST be re-derived against the applied tree, not copied. Reuse its proven driving harness (DeployProtocol fixture, _settleGame/_settleClean VRF-drain, _setupFundedLootboxSubs, depositAfkingFunding, _grantDeityPass, the Sub-stamp slot reads, the snapshot/revert two-near-N marginal form). -->

The CR-01 / 350-SPEC §0 MARGINAL rule (load-bearing): every per-item number is the loop-N-divide MARGINAL — (gas for N items − gas for N−1 items) measured from ONE clean baseline via vm.snapshotState()/vm.revertToState() — NEVER a single-item TOTAL (a single-item total over-states cost and re-introduces the Phase-319 self-crank faucet).

The 16.7M HARD effective per-tx ceiling: EFFECTIVE_GAS_CEILING = 16_700_000 (foundry.toml inflates block_gas_limit to 30e9 for the harness; the bar is the 16.7M).

Key constants on the applied tree (RE-CONFIRM by reading the source, do not trust these line numbers blindly):
- SUB_STAGE_BATCH = 50 — DegenerusGameAdvanceModule.sol:149 (the STAGE chunk; the per-day STAGE caller passes this to processSubscriberStage)
- SETTLE_PERIOD = 10 — GameAfkingModule.sol:172 (the quest leg settles inline on processDay % SETTLE_PERIOD == 0)
- SUBSCRIBER_CAP = 500 — GameAfkingModule.sol:164
- OPEN_BATCH = 200 — GameAfkingModule.sol:206

The v56 per-buy hot path (GameAfkingModule.sol ~:743-918): SOLVENCY-01 debit (afkingFunding[src] -= ethValue; claimablePool -= uint128(ethValue), byte-frozen) → ticket minimal-write primitive (_queueTicketsScaled + buyerOwedBurnie accrue) OR lootbox box-stamp → the mode-agnostic in-slot accrue (affiliateBase flat-7% += / ++questProgress / afkCoveredThroughDay = processDay) → lastAutoBoughtDay marker → IF processDay % SETTLE_PERIOD == 0 THEN _settleQuest (the per-settle cost: questProgress×QUEST_SLOT0_REWARD + buyerOwedBurnie in ONE coinflip.creditFlip + quests.settleAfkingQuest streak-advance).

The re-packed Sub slot (DegenerusGameStorage.sol:1894-1962) is a SINGLE 256-bit slot, 241/256 bits used (15 spare): config 48 + stamp 48 (scorePlus1 uint16 / amount uint32 milli-ETH) + markers 72 (lastAutoBoughtDay/lastOpenedDay/afkCoveredThroughDay uint24 each) + accumulator 73 (affiliateBase uint32 / questProgress uint8 / buyerOwedBurnie uint32 / hasEverSubscribed bool). The accumulator is IN-SLOT — the per-buy accrue is a warm SLOAD-mask-SSTORE on the same slot the stamp already dirtied, NOT a new cold slot. THIS is the GAS-02 property to prove.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fork the v55 harness into V56AfkingGasMarginal and re-derive the applied-tree facts</name>
  <files>test/gas/V56AfkingGasMarginal.t.sol</files>
  <read_first>
    - test/gas/V55AfkingGasMarginal.t.sol (the PATTERN: the DeployProtocol fixture, the snapshot/revert two-near-N marginal form, _settleGame/_settleClean, _setupFundedLootboxSubs, depositAfkingFunding, _grantDeityPass, the Sub-stamp slot reads — fork these helpers)
    - contracts/storage/DegenerusGameStorage.sol:1894-2002 (the re-packed Sub slot field order + the cursor slot — the byte offsets for lastAutoBoughtDay/lastOpenedDay shifted vs v55 because amount narrowed to uint32 milli-ETH; RE-DERIVE the offsets)
    - contracts/modules/GameAfkingModule.sol:700-929 (the v56 per-buy hot path + the settle-day ride — the exact accrue/settle shape being measured)
    - contracts/modules/DegenerusGameAdvanceModule.sol:149, :754-765 (SUB_STAGE_BATCH + _runSubscriberStage)
  </read_first>
  <action>
    Create test/gas/V56AfkingGasMarginal.t.sol as a fork of V55AfkingGasMarginal.t.sol's driving harness (NOT a copy of its stale comment header). RE-DERIVE the game-resident storage slots via `forge inspect storage DegenerusGame` (the Sub slot's field byte offsets changed under the v56 re-pack — amount is now uint32 milli-ETH, the day markers are uint24, the accumulator fields are new; do not trust the v55 OFF_LASTBOUGHT=21 / OFF_LASTOPENED=25 constants). Port the proven helpers: _settleGame/_settleClean VRF-drain, _setupFundedLootboxSubs, _fundPool/depositAfkingFunding, _grantDeityPass, _measureStageAdvanceGas, _readStampDay, rngWordByDay/_injectRngWordByDay, the snapshot/revert two-near-N form, _u. Add a _setupFundedTicketSubs sibling (subscribe with useTickets=true — the `subscribe(..., useTickets, ...)` flag, RE-DERIVE the exact subscribe signature from the contract) so the ticket-mode marginal can be measured against the new minimal-write primitive. Declare EFFECTIVE_GAS_CEILING = 16_700_000 and read SUB_STAGE_BATCH/SETTLE_PERIOD/OPEN_BATCH/SUBSCRIBER_CAP as local constants mirroring the source. Write the contract header to describe the v56 applied tree (in-slot accumulator + deferred settle), explicitly noting the v55 storm is GONE.
  </action>
  <verify>
    <automated>forge build 2>&1 | tail -5 && forge test --match-contract V56AfkingGasMarginal --list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` exits 0 with the new test file present.
    - `forge test --match-contract V56AfkingGasMarginal --list` lists the contract and its test fns (compiles + is discoverable).
    - The file declares EFFECTIVE_GAS_CEILING = 16_700_000 and re-derived slot/offset constants (no copied stale v55 232-bit / OFF_LASTBOUGHT=21 premise).
    - A _setupFundedTicketSubs helper exists (useTickets=true subscribe) alongside the lootbox helper.
  </acceptance_criteria>
  <done>The v56 harness compiles, is test-discoverable, and carries the re-derived applied-tree slot facts + both lootbox and ticket funded-sub setup helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Measure the four marginals + the worst-case settle-day STAGE chunk, assert under 16.7M</name>
  <files>test/gas/V56AfkingGasMarginal.t.sol</files>
  <read_first>
    - test/gas/V55AfkingGasMarginal.t.sol:136-304 (the per-buy / per-open / 16.7M-ceiling test bodies — the shape to adapt for the four v56 marginals)
    - contracts/modules/GameAfkingModule.sol:908-929 (the settle-day ride: processDay % SETTLE_PERIOD == 0 → _settleQuest — the per-settle cost the new per-settle test must warp onto)
    - contracts/modules/GameAfkingModule.sol:1141-1190 (_settleQuest + claimQuest — what the per-settle marginal includes)
  </read_first>
  <action>
    Add five marginal measurements, each via the snapshot/revert (gasN − gasN−1) form:
    (a) testPerBuyLootboxMarginal — N vs N−1 funded LOOTBOX subs over a NON-settle-day STAGE advance (warp so processDay % SETTLE_PERIOD != 0); record per_buy_lootbox_marginal_gas; assert < EFFECTIVE_GAS_CEILING AND marginal × SUB_STAGE_BATCH < EFFECTIVE_GAS_CEILING. Emit a comparison log against the v55 ~206k reference and the ~130-140k GAS-01 target (informational, NOT a hard pin — the measured number is the deliverable).
    (b) testPerBuyTicketMarginal — same, funded TICKET subs (the minimal-write primitive); record per_buy_ticket_marginal_gas; assert under the ceiling + × SUB_STAGE_BATCH under the ceiling. Emit a log vs the old ~262k purchaseWith reference (the structural win).
    (c) testPerOpenMarginal — N vs N−1 ready stamped boxes via mintBurnie() open leg (port _measureOpenLegGas); record per_open_marginal_gas; assert uniform-O(1) under a generous bound + × OPEN_BATCH < ceiling.
    (d) testPerSettleMarginal — measure the ADDED cost of the settle-day ride: gasN = a STAGE advance landing on a settle boundary (processDay % SETTLE_PERIOD == 0) over N subs that have accrued questProgress+buyerOwedBurnie; gasNm1 the same minus one sub — so (gasN − gasNm1) isolates ONE sub's _settleQuest cost (the creditFlip + settleAfkingQuest). Record per_settle_marginal_gas; assert under the ceiling. To accrue questProgress before the settle day, run prior non-settle STAGE days for the funded set.
    (e) testWorstCaseSettleDayStageChunkAtCap — seed SUB_STAGE_BATCH funded subs (mix is allowed; the binding case is whichever mode is heavier — measure BOTH a full lootbox chunk and a full ticket chunk on a settle-day boundary), drive ONE settle-day advance, assert the whole advance (which BOUNDS the chunk) < EFFECTIVE_GAS_CEILING. Non-vacuity: assert the cursor advanced a full SUB_STAGE_BATCH chunk AND ≥ SUB_STAGE_BATCH−2 subs newly stamped AND the settle actually fired (questProgress drained to 0 on the settled subs). Record stage_settle_day_full_chunk_whole_gas for both modes.
    Every measured number is an emit log_named_uint. The settle-day chunk number is the load-bearing input to GAS-03 (355-03).
  </action>
  <verify>
    <automated>forge test --match-contract V56AfkingGasMarginal -vv 2>&1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - All five tests pass (`forge test --match-contract V56AfkingGasMarginal` exits 0).
    - Each test emits a named-uint log of its measured marginal (per_buy_lootbox_marginal_gas, per_buy_ticket_marginal_gas, per_open_marginal_gas, per_settle_marginal_gas, stage_settle_day_full_chunk_whole_gas for both lootbox and ticket modes).
    - testWorstCaseSettleDayStageChunkAtCap asserts the full SUB_STAGE_BATCH settle-day advance < 16_700_000 for BOTH lootbox and ticket modes, with non-vacuity (full chunk advanced, ≥ cap−2 newly stamped, ≥1 settle fired drained questProgress to 0).
    - Each per-item marginal is the (gasN − gasN−1) snapshot/revert form (no single-item total used as a per-item number).
  </acceptance_criteria>
  <done>The harness reports all four marginals + the worst-case settle-day STAGE chunk (both modes) as logged numbers, each asserted under the 16.7M ceiling; the settle-day chunk number is available to GAS-03.</done>
</task>

<task type="auto">
  <name>Task 3: Prove GAS-02 — the per-buy accrue makes no new cold per-buy SSTORE (warm in-slot)</name>
  <files>test/gas/V56AfkingGasMarginal.t.sol</files>
  <read_first>
    - test/gas/V55AfkingGasMarginal.t.sol:335-412, :603-628 (the vm.startStateDiffRecording / stopAndReturnStateDiff trace + the StorageAccess filtering helpers — the pattern for a per-buy SSTORE trace)
    - contracts/modules/GameAfkingModule.sol:873-907 (the mode-agnostic accrue + the lastAutoBoughtDay marker — the writes that must all land on the ONE warm Sub slot)
    - contracts/storage/DegenerusGameStorage.sol:1894-1967 (the single-slot Sub layout — _subOf is the slot whose writes the trace must confirm are all the same slot)
  </read_first>
  <action>
    Add testGas02AccrueIsWarmInSlotNoNewColdSstore. Drive a per-buy STAGE iteration (a new-day advanceGame over a small funded set) under vm.startStateDiffRecording / vm.stopAndReturnStateDiff. RE-DERIVE the _subOf slot for the buyer (keccak256(abi.encode(player, SUBOF_SLOT))). Assert: (1) all StorageAccess writes attributed to account == game on the buyer's Sub record target the SAME single slot (the stamp + the accumulator accrue + the markers are co-located — no second distinct cold slot is written for affiliateBase/questProgress/buyerOwedBurnie). Concretely, filter the per-buy iteration's write set for the buyer's Sub slot and assert the accumulator fields do not introduce a write to any slot other than the buyer's single _subOf slot. (2) Non-vacuity: assert ≥1 write DID land on the buyer's Sub slot (the accrue ran). Emit the per-buy distinct-Sub-slot-write count as a named-uint log (must be 1 for the buyer's record). Record a short note in the test NatSpec that the off-slot affiliate ledger is touched only at claim (DegenerusAffiliate.claim mints A/U1/U2 directly — single-step, no pendingClaim), NEVER per-buy.
  </action>
  <verify>
    <automated>forge test --match-test testGas02AccrueIsWarmInSlotNoNewColdSstore -vv 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - testGas02AccrueIsWarmInSlotNoNewColdSstore passes.
    - The test asserts the per-buy accrue writes only the buyer's single _subOf slot (no new distinct cold slot for the accumulator fields) AND non-vacuously that ≥1 write landed on that slot.
    - The test emits a named-uint distinct-Sub-slot-write count (= 1 for the buyer's record).
    - Whole-contract run still green: `forge test --match-contract V56AfkingGasMarginal` exits 0.
  </acceptance_criteria>
  <done>GAS-02 is proven empirically: the per-buy accumulator accrue is a warm in-slot write on the same Sub slot the stamp dirties, introducing no new cold per-buy SSTORE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → contracts | The harness only READS gas + state via the live DeployProtocol fixture and vm cheatcodes; it MUTATES no `contracts/*.sol`. The risk is a MIS-MEASUREMENT that mis-informs the GAS-03 sizing decision (a too-large SUB_STAGE_BATCH → settle-day STAGE breach → advanceGame DoS). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-01-MEASURE | Information disclosure (wrong measurement) | the per-item marginal | mitigate | enforce the CR-01 (gasN − gasN−1) snapshot/revert MARGINAL form — never a single-item total; assert non-vacuity (cursor advanced a full chunk, ≥cap−2 newly stamped) so a vacuous/skip advance cannot pass as a measurement |
| T-355-01-SETTLEDAY | Denial of service (under-measured worst case) | the settle-day STAGE chunk | mitigate | measure the SETTLE-DAY chunk (processDay % SETTLE_PERIOD == 0, where every sub fires _settleQuest) as the binding case for both modes, NOT a cheap non-settle day; assert the full SUB_STAGE_BATCH advance < 16.7M with the settle actually fired (questProgress drained) |
| T-355-01-GAS02VAC | Tampering (vacuous proof) | the GAS-02 no-cold-SSTORE trace | mitigate | the trace asserts non-vacuously that ≥1 write landed on the buyer's Sub slot (the accrue ran) AND no second distinct cold slot is written — a no-op advance cannot satisfy the no-new-cold-slot claim vacuously |
| T-355-01-SC | Tampering | npm/forge installs | accept | no package installs in this plan — pure test authoring against the existing Foundry toolchain (foundry.toml present); no new dependency surface |
</threat_model>

<verification>
- `forge build` exits 0 with `test/gas/V56AfkingGasMarginal.t.sol` present.
- `forge test --match-contract V56AfkingGasMarginal` exits 0 (all six+ tests green).
- All five marginals (per-buy lootbox, per-buy ticket, per-open, per-settle, worst-case settle-day chunk × both modes) are emitted as named-uint logs and asserted under the 16.7M ceiling.
- GAS-02 proven: per-buy accrue is a warm in-slot write, no new cold per-buy SSTORE.
- ZERO `contracts/*.sol` mutation (`git diff e18af451 -- contracts/` empty).
</verification>

<success_criteria>
- The v56 gas-marginal harness exists and measures the per-buy (both modes) + per-open + per-settle marginals + the worst-case settle-day STAGE chunk at SUB_STAGE_BATCH, each as the CR-01 (gasN − gasN−1) MARGINAL, asserted under 16.7M.
- The measured numbers are logged and available as the load-bearing input to 355-02 (scavenger sizing) and 355-03 (the SUB_STAGE_BATCH re-tune / Outcome-A call).
- GAS-01 measurement requirement satisfied (the per-buy + per-settle marginals measured). GAS-02 packing/no-new-cold-SSTORE proven empirically.
- Agent-committable (test only); no contract-commit gate.
</success_criteria>

<output>
Create `.planning/phases/355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum/355-01-SUMMARY.md` when done — record the MEASURED numbers (per_buy_lootbox, per_buy_ticket, per_open, per_settle, settle_day_full_chunk for both modes) verbatim, since 355-02 and 355-03 consume them.
</output>
