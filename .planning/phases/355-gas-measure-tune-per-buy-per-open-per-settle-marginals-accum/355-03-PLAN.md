---
phase: 355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum
plan: 03
type: execute
wave: 3
depends_on: ["355-01", "355-02"]
files_modified:
  - contracts/modules/DegenerusGameAdvanceModule.sol
  - contracts/modules/GameAfkingModule.sol
  - test/gas/V56AfkingGasMarginal.t.sol
autonomous: false
requirements: [GAS-03, GAS-04]
user_setup: []
must_haves:
  truths:
    - "SUB_STAGE_BATCH is SET (GAS-03) — sized so the settle-day STAGE chunk (the heaviest day, where every sub in the chunk fires _settleQuest) fits the 16.7M HARD per-tx ceiling at the SUBSCRIBER_CAP, for the binding (heavier) of the ticket and lootbox modes"
    - "The per-day STAGE is PROVEN under 16.7M at the chosen SUB_STAGE_BATCH via the 355-01 worst-case settle-day harness re-run against that value"
    - "Every skeptic-APPROVED GAS-04 mode/SLOAD-collapse change from 355-02 is landed (and ONLY those) — unmanipulability + SOLVENCY-01 byte-frozen + RNG-freeze preserved; OR if 355-02's APPROVED set is empty, Outcome-A is recorded (no net diff beyond the SUB_STAGE_BATCH setting)"
    - "Any contract change is applied to contracts/ + forge build clean, then HELD at the contract-commit boundary for explicit USER hand-review (NEVER auto-committed)"
  artifacts:
    - path: "contracts/modules/DegenerusGameAdvanceModule.sol"
      provides: "the re-tuned SUB_STAGE_BATCH constant (GAS-03)"
      contains: "SUB_STAGE_BATCH"
    - path: "test/gas/V56AfkingGasMarginal.t.sol"
      provides: "the settle-day STAGE-at-cap re-proof against the chosen SUB_STAGE_BATCH"
      contains: "EFFECTIVE_GAS_CEILING"
  key_links:
    - from: "contracts/modules/DegenerusGameAdvanceModule.sol SUB_STAGE_BATCH"
      to: "GameAfkingModule.processSubscriberStage settle-day chunk"
      via: "_runSubscriberStage passes SUB_STAGE_BATCH to the STAGE"
      pattern: "SUB_STAGE_BATCH"
    - from: "test/gas/V56AfkingGasMarginal.t.sol"
      to: "the chosen SUB_STAGE_BATCH"
      via: "settle-day full-chunk advance asserted under the ceiling"
      pattern: "EFFECTIVE_GAS_CEILING"
---

<objective>
Land the residual GAS tune as the v56 contract-boundary HARD STOP: SET `SUB_STAGE_BATCH` (GAS-03, the number DEFERRED from 354 to here) sized so the settle-day STAGE chunk fits 16.7M at the cap, apply ONLY the skeptic-APPROVED GAS-04 mode/SLOAD-collapse changes from 355-02 (if any), prove the settle-day STAGE under the ceiling, then HOLD at the contract-commit boundary for USER hand-review. If 355-02's APPROVED set is empty and the savings are fully structural to the 354 refactor, record Outcome-A (no net diff beyond the SUB_STAGE_BATCH setting) per the v55 Phase 350 precedent.

Purpose: `SUB_STAGE_BATCH` is the load-bearing throughput/headroom decision — too large and the settle-day STAGE breaches 16.7M (advanceGame DoS / forced game-over); it MUST be set against the MEASURED settle-day chunk, not guessed. The GAS-04 collapse only ever lands what the skeptic cleared.

Output: the re-tuned `SUB_STAGE_BATCH` in `DegenerusGameAdvanceModule.sol` (+ any APPROVED GAS-04 edit in `GameAfkingModule.sol`), the settle-day-at-cap re-proof in the harness, and a diff-review record — applied + `forge build` clean, HELD at the contract-commit boundary. This is the ONE contract gate in Phase 355.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum/355-01-SUMMARY.md
@.planning/phases/355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum/355-02-GAS-AUDIT.md
@.planning/phases/355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum/355-02-SUMMARY.md

<interfaces>
<!-- The ONE Phase-355 contract gate. Per project rules feedback_batch_contract_approval / feedback_never_preapprove_contracts / feedback_no_contract_commits: a contracts/*.sol commit is the ONLY action needing approval. ContractAddresses.sol is freely modifiable; tests/docs are agent-committable. Sequential-on-main, worktrees OFF (submodule + node_modules). Pre-launch redeploy-fresh (storage break fine). -->

SUB_STAGE_BATCH lives in DegenerusGameAdvanceModule.sol:149 (uint256 private constant SUB_STAGE_BATCH = 50;). DegenerusGameAdvanceModule._runSubscriberStage (~:754) passes it to GameAfkingModule.processSubscriberStage as the chunk size. This is the constant to SET (GAS-03). It is the v55-era value (50) carried forward; the v56 settle-day cost profile differs (the per-buy storm is gone, so most days are cheaper, but the settle-day ride adds a creditFlip + settleAfkingQuest per sub on the ~1/10 settle day — the binding day).

SETTLE_PERIOD = 10, SUBSCRIBER_CAP = 500, OPEN_BATCH = 200 (GameAfkingModule.sol). The settle-day chunk is the heaviest STAGE day: when processDay % SETTLE_PERIOD == 0, each of the up-to-SUB_STAGE_BATCH subs in the chunk runs _settleQuest. SIZE SUB_STAGE_BATCH so this settle-day chunk x the measured per-sub settle-day cost stays under 16.7M with headroom, for the BINDING (heavier) mode.

The SOLVENCY-01 byte-frozen debit: GameAfkingModule.sol:744-745 (afkingFunding[src] -= ethValue; claimablePool -= uint128(ethValue)). NEVER touched by this plan — assert byte-unchanged vs 453f8073 at the gate.

Outcome-A precedent (v55 Phase 350): if the gas-skeptic rejected every GAS-04 candidate and the savings are structural to the 354 refactor, the phase records Outcome-A — no net contract diff. NOTE: setting SUB_STAGE_BATCH IS a constant edit, so that single-constant change rides the gate even under Outcome-A UNLESS 354 already left the correct value AND the 355-01 measurement confirms it, in which case recording that confirmation is doc-only (no contract diff). The executor MUST determine which case applies from the 355-01 settle-day measurement: if 50 already fits with comfortable headroom AND no GAS-04 change is APPROVED, then doc-only Outcome-A (no contracts/ edit); else SET the value (a contracts/ edit, so the gate fires).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set SUB_STAGE_BATCH (GAS-03) + apply the skeptic-APPROVED GAS-04 collapse (or record Outcome-A)</name>
  <files>contracts/modules/DegenerusGameAdvanceModule.sol, contracts/modules/GameAfkingModule.sol</files>
  <read_first>
    - .planning/phases/355-01-SUMMARY.md (the MEASURED settle-day full-chunk gas at 50, both modes — the load-bearing number for the SUB_STAGE_BATCH decision)
    - .planning/phases/355-02-GAS-AUDIT.md (the `## Decision for 355-03` section — the APPROVED GAS-04 set with file:line + implementation notes, and the REJECTED set)
    - contracts/modules/DegenerusGameAdvanceModule.sol:145-160, :743-765 (the SUB_STAGE_BATCH constant + _runSubscriberStage that consumes it)
    - contracts/modules/GameAfkingModule.sol:660-929, :1141-1190 (the STAGE hot path + settle-day ride + _settleQuest — the GAS-04 edit target, IF any APPROVED candidate touches it)
    - contracts/modules/GameAfkingModule.sol:744-745 (the SOLVENCY-01 debit — must stay byte-frozen)
  </read_first>
  <action>
    Determine SUB_STAGE_BATCH from the 355-01 measured settle-day full-chunk cost: pick the LARGEST batch N such that N x (the binding-mode per-sub settle-day cost) stays under 16.7M with headroom (the v55 harness used 50 comfortably under; if the v56 settle-day cost per sub is lower the batch may rise, if the settle-day ride pushes it higher the batch may need to drop — the measurement decides, not a guess). Then branch on the 355-02 decision:
    - IF 355-02's APPROVED set is non-empty: apply ONLY those GAS-04 changes to GameAfkingModule.sol exactly per the implementation notes (hoist a repeated SLOAD, collapse a redundant mode branch, share a clamp helper — whatever was APPROVED). Apply each verbatim to the cited file:line; do NOT improvise extra changes.
    - Set SUB_STAGE_BATCH in DegenerusGameAdvanceModule.sol to the chosen value if it differs from the current 50. If the measurement confirms 50 is already correct with comfortable headroom AND the APPROVED set is empty, record Outcome-A: make NO contracts/ edit (the doc-only case), and proceed to Task 2 to RE-PROVE 50 fits (the measurement IS the deliverable).
    - Comment style (USER rule lean-code-comments-no-procedural-meta): comments explain how the code works — no plan-IDs, requirement-tags, or spec-line citations.
    - In ALL cases: the SOLVENCY-01 debit (:744-745) stays byte-frozen; the open freeze shape, the self-marking settle balances, and the double-credit guards are untouched. `forge build` must exit 0.
  </action>
  <verify>
    <automated>forge build 2>&1 | tail -5; grep -n "SUB_STAGE_BATCH = " contracts/modules/DegenerusGameAdvanceModule.sol; git diff 453f8073 -- contracts/modules/GameAfkingModule.sol | grep -E "^[-+].*(afkingFunding\[src\] -=|claimablePool -= uint128)"</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` exits 0.
    - SUB_STAGE_BATCH is set to a value justified by the 355-01 settle-day measurement (either re-tuned, or 50 confirmed with comfortable headroom under 16.7M at the cap for the binding mode).
    - If 355-02 APPROVED set non-empty: each APPROVED GAS-04 change is present at its cited file:line and NO un-approved change is introduced. If empty: Outcome-A — no GAS-04 contracts/ edit.
    - The SOLVENCY-01 debit lines (:744-745) show NO +/- in `git diff 453f8073 -- contracts/modules/GameAfkingModule.sol` (the grep returns no added/removed lines on those two statements).
  </acceptance_criteria>
  <done>SUB_STAGE_BATCH is set against the measured settle-day chunk; only skeptic-APPROVED GAS-04 changes are landed (or Outcome-A recorded); SOLVENCY-01 byte-frozen; forge build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Re-prove the settle-day STAGE chunk fits 16.7M at the chosen SUB_STAGE_BATCH</name>
  <files>test/gas/V56AfkingGasMarginal.t.sol</files>
  <read_first>
    - test/gas/V56AfkingGasMarginal.t.sol (the 355-01 worst-case settle-day chunk test — update its SUB_STAGE_BATCH local constant + re-run against the chosen value)
    - .planning/phases/355-01-SUMMARY.md (the measured baseline to compare against)
    - contracts/modules/DegenerusGameAdvanceModule.sol (the SUB_STAGE_BATCH value just set)
  </read_first>
  <action>
    Update the harness's SUB_STAGE_BATCH local constant to mirror the value set in Task 1, and re-run the worst-case settle-day STAGE chunk test (both lootbox and ticket modes) so it proves the FULL chunk on a settle-day boundary stays under EFFECTIVE_GAS_CEILING at the new value. If any APPROVED GAS-04 change altered a per-buy/per-settle marginal, re-run the affected marginal tests and update their logged-comparison notes (the measured number is the deliverable — re-record it). Keep the non-vacuity assertions (full chunk advanced, ≥ cap−2 newly stamped, ≥1 settle fired drained questProgress to 0). If the chosen batch is larger than the harness's prior N_HI staging supported, extend the funded-set seeding so a full SUB_STAGE_BATCH chunk is exercised in one advance.
  </action>
  <verify>
    <automated>forge test --match-contract V56AfkingGasMarginal -vv 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-contract V56AfkingGasMarginal` exits 0.
    - The worst-case settle-day chunk test asserts the full SUB_STAGE_BATCH advance (at the value set in Task 1) < 16_700_000 for BOTH lootbox and ticket modes, with non-vacuity intact.
    - The harness SUB_STAGE_BATCH local constant matches DegenerusGameAdvanceModule.sol.
    - Any re-measured marginal affected by an APPROVED GAS-04 change is re-logged.
  </acceptance_criteria>
  <done>The settle-day STAGE chunk is proven under the 16.7M ceiling at the chosen SUB_STAGE_BATCH for the binding mode; GAS-03 is empirically satisfied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Contract-commit HARD STOP — USER hand-review + commit the net diff (or confirm Outcome-A)</name>
  <files>contracts/modules/DegenerusGameAdvanceModule.sol, contracts/modules/GameAfkingModule.sol</files>
  <action>
    HOLD at the contract-commit boundary. Do NOT auto-commit any `contracts/*.sol` change. Assemble the diff-review artifact, run the build + harness, present the diff to the USER, and commit ONLY on explicit USER authorization (or, under Outcome-A, make no contract commit). This task is a blocking human gate, not autonomous work — its concrete steps are in `<how-to-verify>`.
  </action>
  <what-built>
    The Phase 355 GAS tune: SUB_STAGE_BATCH set against the measured settle-day chunk (GAS-03) + any skeptic-APPROVED GAS-04 mode/SLOAD-collapse change, applied to contracts/ and locally compiling (forge build clean), with the settle-day STAGE proven under 16.7M at the cap. The SOLVENCY-01 debit (GameAfkingModule.sol:744-745) is byte-frozen vs 453f8073; the open freeze shape + self-marking settle balances + double-credit guards are untouched. If 355-02 rejected every candidate AND 50 was confirmed correct, this is Outcome-A (no contracts/ diff — doc-only).
  </what-built>
  <how-to-verify>
    1. Author a `355-03-DIFF-REVIEW.md` (gitignored `*REVIEW*` artifact, on disk only): the full `git diff 453f8073 -- contracts/` for Phase 355, the per-requirement mapping (GAS-03 -> SUB_STAGE_BATCH line; GAS-04 -> each APPROVED change line or "Outcome-A, none"), the SOLVENCY-01 byte-unchanged attestation, and the settle-day-at-cap measured number.
    2. Run `forge build` (exit 0) and `forge test --match-contract V56AfkingGasMarginal` (exit 0) and paste the settle-day-at-cap result.
    3. Present the diff to the USER. If Outcome-A (no contracts/ diff), state that explicitly and ask the USER to confirm the no-diff close.
    4. The USER hand-reviews. ONLY on explicit USER "approved" (or equivalent authorization) does the agent commit the single batched `contracts/*.sol` change as `feat(355): ...` (rides the same v56 contract boundary). NEVER auto-commit contracts. If Outcome-A, no contract commit is made — only the docs/SUMMARY commit.
  </how-to-verify>
  <resume-signal>USER types "approved" (commit the contract diff) or "outcome-a confirmed" (no contract commit) or describes required changes</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keeper/advanceGame -> STAGE | The per-day STAGE runs in the advanceGame chain; a too-large SUB_STAGE_BATCH on the settle day breaches 16.7M -> the advance reverts -> the game cannot advance -> DoS / forced game-over. This is the HIGH-severity threat (the only gas-DoS that matters per the STATE.md weighting). |
| any GAS-04 edit -> the afking buy/open/settle | A "gas win" that silently widens an attack surface (unmanipulability, double-credit, RNG-freeze, SOLVENCY-01) is the security-over-gas violation; gated by the 355-02 skeptic + this gate's byte-frozen attestation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-03-BATCHDOS | Denial of service | SUB_STAGE_BATCH too large | mitigate | size against the MEASURED settle-day full-chunk cost (the binding heaviest day, both modes), re-prove the full chunk < 16.7M at the cap in Task 2 with non-vacuity; HIGH severity — block on a breach |
| T-355-03-SOLVENCY | Tampering | the ETH/claimablePool debit | mitigate | the SOLVENCY-01 debit (:744-745) is byte-frozen; the gate attests `git diff 453f8073` shows no +/- on those two lines; never a candidate |
| T-355-03-UNAPPROVED | Elevation of privilege (unvetted change) | a GAS-04 edit | mitigate | ONLY the 355-02 skeptic-APPROVED set is landed, verbatim at the cited file:line; no improvised edit; the USER hand-review at Task 3 is the final gate |
| T-355-03-FREEZE | Tampering | the RNG-freeze open shape / self-marking settle | accept | this plan does NOT touch the open seed (rngWordByDay[lastAutoBoughtDay]), the frozen scorePlus1, or the self-marking settle balances — confirmed unchanged in the diff review (the SUB_STAGE_BATCH constant + an APPROVED SLOAD/branch collapse cannot reach them) |
| T-355-03-SC | Tampering | npm/forge installs | mitigate | no package installs; the only contract change is a constant + an APPROVED collapse; the USER hand-review gate (blocking-human, never auto-approved) covers the contract-commit boundary |
</threat_model>

<verification>
- `forge build` exits 0 with the Phase-355 contract change (or no change under Outcome-A) applied.
- `forge test --match-contract V56AfkingGasMarginal` exits 0; the settle-day STAGE chunk at the chosen SUB_STAGE_BATCH is proven < 16.7M for both modes.
- `git diff 453f8073 -- contracts/modules/GameAfkingModule.sol` shows NO +/- on the SOLVENCY-01 debit lines (:744-745).
- Only skeptic-APPROVED GAS-04 changes are present (or none under Outcome-A); the open freeze shape + self-marking settle balances + double-credit guards are untouched.
- The contract change is HELD at the commit boundary — committed ONLY on explicit USER hand-review (or no contract commit under Outcome-A).
</verification>

<success_criteria>
- GAS-03: SUB_STAGE_BATCH is set against the measured settle-day chunk and the per-day STAGE is proven under 16.7M at the SUBSCRIBER_CAP for the binding mode.
- GAS-04: every skeptic-APPROVED mode/SLOAD-collapse change is landed (and only those), proven unmanipulable; OR Outcome-A recorded (no net diff beyond the SUB_STAGE_BATCH setting).
- The SOLVENCY-01 debit is byte-frozen; RNG-freeze + unmanipulability + double-credit guards intact.
- The contract change rides the single v56 USER-approved diff, HELD at the contract-commit boundary (the ONE Phase-355 gate), or recorded as Outcome-A no-diff.
</success_criteria>

<output>
Create `.planning/phases/355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum/355-03-SUMMARY.md` when done — record: the chosen SUB_STAGE_BATCH + its justification (the measured settle-day chunk), the landed GAS-04 changes (or Outcome-A), the SOLVENCY-01 byte-frozen attestation, the settle-day-at-cap measured number, and the commit hash (or "Outcome-A — no contract commit").
</output>
