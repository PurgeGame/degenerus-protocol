---
phase: 355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum
plan: 02
type: execute
wave: 2
depends_on: ["355-01"]
files_modified:
  - .planning/phases/355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum/355-02-GAS-AUDIT.md
autonomous: true
requirements: [GAS-02, GAS-04]
must_haves:
  truths:
    - "gas-scavenger surfaces every removal/collapse candidate across the v56 afking STAGE (the per-buy accrue, the settle-day ride, the ticket minimal-write primitive, the open leg) — redundant payment-mode branches + repeated SLOADs (GAS-04) + the accumulator-packing finalization (GAS-02)"
    - "gas-skeptic validates EACH candidate with a verdict (APPROVED / REJECTED / PARTIAL / NEEDS_HUMAN_REVIEW) and explicit reasoning — any candidate that trades away an invariant (RNG-freeze, SOLVENCY-01, unmanipulability of buy/open/settle, double-credit guards) is REJECTED, not re-litigated"
    - "The audit produces a candidate ledger that feeds 355-03: each APPROVED candidate has an exact file:line + a one-line implementation note; each REJECTED candidate has the invariant it would have traded"
  artifacts:
    - path: ".planning/phases/355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum/355-02-GAS-AUDIT.md"
      provides: "the scavenger candidate list + the skeptic verdicts + the GAS-04 collapse decisions + the GAS-02 packing finalization + the input to the 355-03 SUB_STAGE_BATCH / net-tune decision"
      min_lines: 60
  key_links:
    - from: ".planning/phases/355-02-GAS-AUDIT.md"
      to: "GameAfkingModule.processSubscriberStage (the STAGE)"
      via: "scavenger candidate file:line citations"
      pattern: "GameAfkingModule"
    - from: ".planning/phases/355-02-GAS-AUDIT.md"
      to: "355-01 measured marginals"
      via: "candidate sizing against the measured per-buy / per-settle gas"
      pattern: "marginal"
---

<objective>
Run the gas-scavenger over the v56 afking STAGE/accrue/settle/open surface and the gas-skeptic to validate every candidate, producing the candidate ledger that 355-03 consumes. This is where the GAS-04 mode/SLOAD collapse candidates and the GAS-02 accumulator-packing finalization fall out, each gated by the skeptic against the security-over-gas floor.

Purpose: the security floor OVERRIDES gas. The scavenger proposes; the skeptic disposes. Any "win" that silently widens an attack surface (unmanipulability of the afking buy/open/settle, the double-credit guards, RNG-freeze, the byte-frozen SOLVENCY-01 debit) is REJECTED WITH REASONING here so 355-03 only ever lands skeptic-APPROVED changes — or records Outcome-A no-diff if everything is rejected.

Output: `355-02-GAS-AUDIT.md` — the candidate list, the per-candidate skeptic verdicts, the APPROVED set (file:line + implementation note) and the REJECTED set (the invariant each would have traded). Doc only; agent-committable (no contract gate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum/355-01-SUMMARY.md
@.planning/phases/354-impl-the-one-carefully-sequenced-batched-contract-diff-aggre/354-06-SUMMARY.md

<interfaces>
<!-- The gas-audit skill orchestrates two probing skills. They live at ~/.claude/skills/. Invoke them inline (read their SKILL.md, apply their method); do NOT spawn them as a council. -->
- ~/.claude/skills/gas-scavenger/SKILL.md — surfaces removal/collapse candidates (unused storage, dead branches, redundant requires, repeated SLOADs, redundant payment-mode branches), emits JSON-style candidates with file:line.
- ~/.claude/skills/gas-skeptic/SKILL.md — validates each candidate; verdicts APPROVED / REJECTED / PARTIAL / NEEDS_HUMAN_REVIEW; delegatecall-aware (a var "unused" in one module may be read by another; storage-slot alignment is critical); risk matrix: funds-at-risk / state-corruption / functionality-change High = REJECT.

The SECURITY-OVER-GAS floor (the hard gate, from STATE.md threat weighting): DOMINANT = RNG/freeze; HIGH = gas-DoS only in the advanceGame chain (16.7M = gg); SPINE = solvency. The non-negotiable invariants any candidate must NOT trade:
- SOLVENCY-01: the ETH/claimablePool debit (GameAfkingModule.sol:744-745) is BYTE-FROZEN — never a candidate.
- RNG-freeze: the open seeds from rngWordByDay[lastAutoBoughtDay] (the frozen stamp-day word) + the frozen scorePlus1; the accrue/settle touch no frozen RNG-window slot.
- Unmanipulability of buy/open/settle: no settle-timing edge (no roll, no seed under the flat-7% deterministic-split claim), no strategic sub/unsub edge (the accrued affiliateBase PERSISTS for the uplines on unsub — churn forfeits-nothing-gains-nothing), the self-marking running balances make double-settle a no-op (re-claim sees affiliateBase==0; double quest-flush finds questProgress==0).
- The double-credit guards: lastCompletedDay / afkCoveredThroughDay (QST-03), the no-double-open lastOpenedDay monotone (OPEN-02), the single EV-cap RMW per (player,level).

The candidate surface (the v56 afking STAGE only — NOT a whole-ecosystem sweep):
- GameAfkingModule.processSubscriberStage hot path (~:660-929) — the funding-skip read, the mode if/else, the in-slot accrue, the settle-day ride. Hunt: repeated SLOADs (currentLevel, mp/MintPaymentKind, the Sub slot fields read more than once), redundant payment-mode branches (GAS-04), and whether the accumulator clamp/arithmetic can be cheapened without changing the under-credit-only behaviour.
- _settleQuest (~:1141-1190) — the settle-day creditFlip + settleAfkingQuest path; hunt redundant reads, but the creditFlip + cross-contract settle are load-bearing (cannot be removed).
- The ticket minimal-write primitive (~:748-838) — the century-bonus block, the buyerOwedBurnie accrue; hunt repeated reads (the activity-score is computed for the century block).
- The open leg (_openAfkingBox ~:945-1000 → resolveAfkingBox) — already re-verified at 354-05; hunt only cheap reads, NEVER the freeze/no-double-open shape.
- DegenerusGameStorage.sol Sub slot (1894-1962) — the GAS-02 packing is ALREADY in-slot (241/256, 15 spare); the scavenger CONFIRMS the packing is final (no field can be cheaper-packed without risking a neighbor) OR proposes a tighter pack the skeptic must clear against bit-overflow.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scavenger pass over the v56 afking STAGE/accrue/settle/open surface</name>
  <files>.planning/phases/355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum/355-02-GAS-AUDIT.md</files>
  <read_first>
    - ~/.claude/skills/gas-scavenger/SKILL.md (the scavenger method + the candidate output shape)
    - ~/.claude/skills/gas-audit/SKILL.md (the orchestration workflow — chunking + the candidate ledger format)
    - contracts/modules/GameAfkingModule.sol:660-1000 (the STAGE hot path + the settle-day ride + the ticket primitive + the open leg — the candidate surface)
    - contracts/modules/GameAfkingModule.sol:1141-1215 (_settleQuest + claimQuest + drainAffiliateBase)
    - contracts/storage/DegenerusGameStorage.sol:1894-2002 (the Sub slot — the GAS-02 packing surface)
    - .planning/phases/355-01-SUMMARY.md (the MEASURED marginals — size each candidate's potential saving against the measured per-buy/per-settle gas)
  </read_first>
  <action>
    Apply the gas-scavenger method to the v56 afking STAGE surface ONLY (per-buy accrue, settle-day ride, ticket minimal-write primitive, open leg, the Sub slot). For each candidate produce: an id (SCAV-355-NN), file:line, the candidate (e.g. "currentLevel SLOAD'd in both the ticket branch and the lootbox branch — hoist once"; "the two whole-BURNIE 100M clamps share a helper"; "mp/MintPaymentKind re-read per iteration"), the category (redundant SLOAD / redundant payment-mode branch / packing / dead-branch), and an estimated marginal saving sized against the 355-01 measured numbers. Explicitly enumerate: (a) the GAS-04 mode/SLOAD collapse candidates in the STAGE if/else; (b) the GAS-02 packing finalization — confirm the 241/256 in-slot pack is final OR propose a tighter pack with the exact bit budget. Do NOT propose anything touching the SOLVENCY-01 debit (:744-745), the RNG-freeze open shape, or the self-marking settle balances — mark those as OUT-OF-BOUNDS in the ledger. Write the candidate list into 355-02-GAS-AUDIT.md under a `## Scavenger Candidates` section.
  </action>
  <verify>
    <automated>test -f .planning/phases/355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum/355-02-GAS-AUDIT.md && grep -c "SCAV-355" .planning/phases/355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum/355-02-GAS-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - 355-02-GAS-AUDIT.md exists with a `## Scavenger Candidates` section.
    - Each candidate has an id (SCAV-355-NN), a file:line, a category, and an estimated saving sized against the 355-01 measured marginals.
    - The GAS-04 mode/SLOAD collapse candidates and the GAS-02 packing finalization are both explicitly present (a packing entry confirming the in-slot pack is final, or a tighter-pack proposal with a bit budget).
    - The SOLVENCY-01 debit, the RNG-freeze open shape, and the self-marking settle balances are listed as OUT-OF-BOUNDS (not candidates).
  </acceptance_criteria>
  <done>The scavenger candidate ledger exists, covers the GAS-04 and GAS-02 surfaces, sizes each candidate against the measured marginals, and fences off the load-bearing invariants.</done>
</task>

<task type="auto">
  <name>Task 2: Skeptic validation — verdict each candidate against the security-over-gas floor</name>
  <files>.planning/phases/355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum/355-02-GAS-AUDIT.md</files>
  <read_first>
    - ~/.claude/skills/gas-skeptic/SKILL.md (the verdict method + the risk matrix + the delegatecall/storage-slot trace checklist)
    - contracts/modules/GameAfkingModule.sol:660-1215 (re-trace each candidate's claim against the actual code — cross-contract usage, edge cases level 0 / gameover / RNG-locked)
    - .planning/STATE.md (the USER-LOCKED audit weighting — RNG/freeze dominant, gas-DoS only in advanceGame, solvency spine — read the threat-model weighting block)
    - .planning/REQUIREMENTS.md (SEC-01/SEC-02 — the hard security floor each verdict defends)
  </read_first>
  <action>
    Apply the gas-skeptic method to EACH SCAV-355-NN candidate. For each, emit a verdict block: verdict (APPROVED / REJECTED / PARTIAL / NEEDS_HUMAN_REVIEW), reasoning, edge cases checked (level 0, gameover state 86, RNG-locked, the settle-day boundary, a duplicate/churned sub), the cross-contract/storage-slot trace (delegatecall-aware — the module runs in Game storage; a packing change must not shift a neighbor field or break the warm in-slot accrue), and the risk level. REJECT any candidate that trades an invariant from the security floor — state the exact invariant it would have traded (e.g. "REJECTED — collapsing the mode branch would skip the ticket buyerOwedBurnie accrue, a dropped-bonus regression"). For a packing-tighten candidate, the skeptic MUST verify the bit budget does not overflow a neighbor (the corruption threat). Write the verdicts under `## Skeptic Verdicts`, then a `## Decision for 355-03` section: the APPROVED set (each with exact file:line + a one-line implementation note for the 355-03 executor) and the REJECTED set (each with the traded invariant). If the APPROVED set is empty, state explicitly that 355-03 trends toward Outcome-A (no net diff beyond the SUB_STAGE_BATCH setting).
  </action>
  <verify>
    <automated>grep -Eqc "APPROVED|REJECTED" .planning/phases/355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum/355-02-GAS-AUDIT.md && grep -q "Decision for 355-03" .planning/phases/355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum/355-02-GAS-AUDIT.md && echo DECISION_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - Every SCAV-355-NN candidate has a skeptic verdict block (verdict + reasoning + edge cases + cross-contract/slot trace + risk level).
    - Every REJECTED candidate names the exact invariant it would have traded.
    - A `## Decision for 355-03` section exists listing the APPROVED set (file:line + implementation note) and the REJECTED set; if APPROVED is empty it explicitly states 355-03 trends to Outcome-A.
    - No APPROVED candidate touches the SOLVENCY-01 debit, the RNG-freeze open shape, or the self-marking settle balances.
  </acceptance_criteria>
  <done>Every candidate is skeptic-verdicted against the security floor; the APPROVED set (with implementation notes) and the REJECTED set (with traded invariants) are the decision input to 355-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analysis → 355-03 contract change | This plan writes NO code; it produces the candidate ledger 355-03 acts on. The risk is an UNSOUND APPROVAL — a candidate marked safe that actually trades an invariant, which 355-03 would then land. The skeptic is the mitigation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-355-02-FALSEAPPROVE | Elevation of privilege (unsound approval) | a skeptic verdict | mitigate | every APPROVED candidate carries an explicit cross-contract + storage-slot trace and an edge-case list (level 0 / gameover / RNG-locked / settle-day / churned sub); the risk matrix forces REJECT on any functionality-change-High; the load-bearing invariants are fenced OUT-OF-BOUNDS in Task 1 so they are never candidates |
| T-355-02-PACKOVERFLOW | Tampering (accumulator bit-overflow) | a GAS-02 tighter-pack candidate | mitigate | a packing-tighten candidate is APPROVED only if the skeptic verifies the exact bit budget does not overflow a neighbor field in the single Sub slot; otherwise REJECTED (the corruption threat) |
| T-355-02-SETTLEDAY | Denial of service | a SUB_STAGE_BATCH-relevant candidate | mitigate | candidates are sized against the 355-01 settle-day worst-case chunk; the skeptic confirms no candidate raises the per-sub settle-day cost (which would shrink the safe batch) |
| T-355-02-SC | Tampering | npm/forge installs | accept | no package installs — pure analysis + doc authoring; no dependency surface |
</threat_model>

<verification>
- 355-02-GAS-AUDIT.md exists with `## Scavenger Candidates`, `## Skeptic Verdicts`, and `## Decision for 355-03`.
- Each candidate is sized against the 355-01 measured marginals and has a skeptic verdict.
- The APPROVED set (with file:line + implementation notes) and the REJECTED set (with traded invariants) are explicit.
- The SOLVENCY-01 debit, RNG-freeze open shape, and self-marking settle balances are fenced OUT-OF-BOUNDS.
- ZERO `contracts/*.sol` mutation (`git diff e18af451 -- contracts/` empty).
</verification>

<success_criteria>
- The gas-scavenger + gas-skeptic pass is complete over the v56 afking STAGE surface; every candidate has a verdict.
- The GAS-04 mode/SLOAD collapse decisions and the GAS-02 packing finalization are settled (APPROVED with notes, or REJECTED with reasoning).
- 355-03 has an unambiguous decision input: either a skeptic-APPROVED change set to land, or an empty APPROVED set pointing to Outcome-A.
- Agent-committable (doc only); no contract-commit gate.
</success_criteria>

<output>
Create `.planning/phases/355-gas-measure-tune-per-buy-per-open-per-settle-marginals-accum/355-02-SUMMARY.md` when done — record the APPROVED set, the REJECTED set (with traded invariants), and whether the phase trends to a net tune or Outcome-A.
</output>
