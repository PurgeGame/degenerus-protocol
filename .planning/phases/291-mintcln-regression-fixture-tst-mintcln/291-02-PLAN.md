---
phase: 291-mintcln-regression-fixture-tst-mintcln
plan: 02
type: execute
wave: 2
depends_on: [291-01]
files_modified:
  - test/edge/MintCleanupRegression.test.js
autonomous: false
requirements: [TST-MINTCLN-01, TST-MINTCLN-02, TST-MINTCLN-03, TST-MINTCLN-04, TST-MINTCLN-05]
requirements_addressed: [TST-MINTCLN-01, TST-MINTCLN-02, TST-MINTCLN-03, TST-MINTCLN-04, TST-MINTCLN-05]

must_haves:
  truths:
    - "A new test file `test/edge/MintCleanupRegression.test.js` exists; covers TST-MINTCLN-01..05 in a single describe block"
    - "Phase 282's `test/edge/MintBatchDeterminism.test.js` is NOT modified (frozen v41-closure artifact)"
    - "Zero `contracts/` mutations across the entire phase"
    - "JSDoc header at the top of the new test file cites both topic hashes from 290-MEASUREMENT.md §5 (v41 `0x5e96bf2d...` retired + v42 `0x279edf1c...` new) AND references inherited D-40N-EVT-BREAK-01 AND forward-cites Phase 297 §9 indexer-migration handoff per D-42N-EVT-BREAK-01 (TST-MINTCLN-05)"
    - "Multi-call drain trait-multiset equivalence: JS-replay against captured `(player, baseKey, take)` emissions reconstructs the on-chain credited multiset trait-by-trait (TST-MINTCLN-01)"
    - "Each captured `TraitsGenerated` log decodes to exactly 3 fields `(player, baseKey, take)`; baseKey low 32 bits decodes to expected owed; upper bits decode to `(lvl, queueIdx, player)` per mint:426-429 construction (TST-MINTCLN-02)"
    - "Both B2 paths exercised in one drain run: Path B at lvl=1 (current-level via `_processOneTicketEntry`) AND Path A at lvl=2..5 (future-pool via `processFutureTicketBatch`) with `path-accumulator=A|B` log discrimination per Phase 282 pattern (TST-MINTCLN-03)"
    - "Direct `ethers.provider.getStorageAt` slot read on the post-drain `ticketsOwedPacked[rk][player]` slot decodes to the expected 40-bit packed form `(uint40(owed) << 8) | uint40(rem)` matching the runtime layout at storage:465 (TST-MINTCLN-04); slot key `rk` is derived per-path via `_tqWriteKey` (Path B, current-level) and `_tqFarFutureKey` (Path A, future-pool) — NOT raw lvl"
    - "Drain runs strictly within one VRF-day: `dailyIdx` captured pre-drain equals `dailyIdx` post-drain (single PINNED_DAILY_ENTROPY word stays valid for the entire JS-replay window); test FAILS with a clear 'drain crossed day boundary — scenario invalid' message if the day rolls"
    - "Single USER-APPROVED batched commit at phase close (helper extension from Plan 01 + new test file from Plan 02 in one diff); per `feedback_batch_contract_approval.md` (TST-MINTCLN-05 close-out)"
    - "No empirical gas measurement, no `console.log` gas log, no gas assertion — D-291-GAS-01 SKIP-GAS lock is STRICTLY zero: no `gas` token in any line (comment OR code); the topic-hash transition and storage-layout regression are the load-bearing audit attestations, gas is OUT"
  artifacts:
    - path: "test/edge/MintCleanupRegression.test.js"
      provides: "TST-MINTCLN-01..05 regression assertions over the post-MINTCLN audit subject"
      contains: "describe('MintCleanupRegression"
    - path: "test/helpers/raritySymbolBatchRef.mjs"
      provides: "v42 3-input JS-replay oracle (from Plan 01)"
      contains: "export function raritySymbolBatchRefV42"
  key_links:
    - from: "test/edge/MintCleanupRegression.test.js"
      to: "test/helpers/raritySymbolBatchRef.mjs::raritySymbolBatchRefV42"
      via: "ES module import"
      pattern: "import.*raritySymbolBatchRefV42.*raritySymbolBatchRef"
    - from: "test/edge/MintCleanupRegression.test.js (TST-MINTCLN-04 slot read)"
      to: "contracts/storage/DegenerusGameStorage.sol:465 ticketsOwedPacked declaration + L187 TICKET_SLOT_BIT (0x800000) + L195 TICKET_FAR_FUTURE_BIT (0x400000) + L325 ticketWriteSlot bool (slot 0, byte offset 28) + L718 _tqWriteKey + L731 _tqFarFutureKey"
      via: "rk = computeRk(lvl, path, ticketWriteSlot); slot = keccak256(abi.encode(player, keccak256(abi.encode(rk, BASE_SLOT)))); BASE_SLOT pinned via `forge inspect` (= 13n)"
      pattern: "getStorageAt.*keccak256.*computeRk"
    - from: "test/edge/MintCleanupRegression.test.js (TST-MINTCLN-02 decode)"
      to: "contracts/storage/DegenerusGameStorage.sol:484-488 event TraitsGenerated"
      via: "storage.interface.parseLog(log) → args.player + args.baseKey + args.take"
      pattern: "parseLog.*TraitsGenerated"
---

<objective>
Ship `test/edge/MintCleanupRegression.test.js` — the single regression fixture covering all 5 TST-MINTCLN-01..05 assertions against the post-MINTCLN audit subject (commit `e5665117`). Scenario reuses Phase 282's whale-bundle pattern (2000-ticket purchase at lvl=1 + 10× `purchaseWhaleBundle` queuing future-level tickets at lvl=2..5) so a single multi-call drain naturally exercises Path B at lvl=1 AND Path A at lvl=2..5 (per `path-accumulator=A|B` log discrimination from Phase 282 precedent). Phase close = USER-APPROVED single batched commit containing the helper extension from Plan 01 + the new test file from Plan 02.

Purpose: Carry the v41 Phase 281 algorithmic invariant forward at v42 close HEAD — cross-call seed separation is preserved by the owed-in-baseKey carry (3-input keccak still produces pairwise-distinct inputs across multi-call drains); the new 3-field `TraitsGenerated` shape parses correctly; both B2-symmetric callsites are exercised; the `ticketsOwedPacked` 40-bit packed form is byte-identical at runtime to what Phase 290 MINTCLN-08 attested structurally via `forge inspect`; and the breaking-topic-hash transition is documented at the test-file header per the v40 D-40N-EVT-BREAK-01 precedent.

Output: One new test file + one USER-APPROVED batched commit covering both Plan 01 + Plan 02 deliverables. Zero `contracts/` mutations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/291-mintcln-regression-fixture-tst-mintcln/291-CONTEXT.md
@.planning/phases/291-mintcln-regression-fixture-tst-mintcln/291-01-PLAN.md
@.planning/phases/290-mint-batch-event-sig-cleanup-mintcln/290-01-DESIGN-INTENT-TRACE.md
@.planning/phases/290-mint-batch-event-sig-cleanup-mintcln/290-01-MEASUREMENT.md
@.planning/phases/290-mint-batch-event-sig-cleanup-mintcln/290-02-SUMMARY.md
@test/edge/MintBatchDeterminism.test.js
@test/helpers/raritySymbolBatchRef.mjs
@test/helpers/deployFixture.js
@test/helpers/testUtils.js
@contracts/modules/DegenerusGameMintModule.sol
@contracts/storage/DegenerusGameStorage.sol

<interfaces>
Live v42 `TraitsGenerated` declaration (contracts/storage/DegenerusGameStorage.sol:484-488):
```
event TraitsGenerated(address indexed player, uint256 baseKey, uint32 take);
```

`baseKey` decode (mint:426-429 Path A; mint:763-766 Path B; structurally B2-symmetric):
- bits 255..232: lvl       — `Number(BigInt(baseKey) >> 224n) & 0xFFFFFF`
- bits 231..192: queueIdx  — `Number((BigInt(baseKey) >> 192n) & 0xFFFFFFFFFFFFFFFFn)`
- bits 191..32:  player    — `BigInt(baseKey) >> 32n & ((1n<<160n)-1n)` → recover via `ethers.getAddress("0x" + hex.padStart(40,"0"))`
- bits 31..0:    owed-at-call-entry — `Number(BigInt(baseKey) & 0xFFFFFFFFn)` (the v42 carry per MINTCLN-02)

Helper from Plan 01 (test/helpers/raritySymbolBatchRef.mjs):
```
export function raritySymbolBatchRefV42({ baseKey, entropyWord, startIndex, count })  -> Uint8Array
export function decodeOwedFromBaseKey(baseKey)  -> number (uint32)
export function computeBaseKey(lvl, queueIdx, player)  -> BigInt (legacy v41 layout — NB: does NOT include owed; this plan composes via `computeBaseKey(...) | BigInt(owed)` when manually reconstructing the v42 form)
```

Live `ticketsOwedPacked` slot (contracts/storage/DegenerusGameStorage.sol:465):
```
mapping(uint24 => mapping(address => uint40)) internal ticketsOwedPacked;
// Layout in slot:  bits 39..8 = owed (uint32);  bits 7..0 = rem (uint8)
```

**Storage-key derivation — CRITICAL: the outer mapping key is NOT raw `lvl`.** The contract uses three disjoint key spaces over the uint24 keyspace, all derived from the runtime `ticketWriteSlot` bool + the path being exercised:

```
// contracts/storage/DegenerusGameStorage.sol
L187: uint24 internal constant TICKET_SLOT_BIT       = 1 << 23;  // 0x800000
L195: uint24 internal constant TICKET_FAR_FUTURE_BIT = 1 << 22;  // 0x400000
L325: bool   internal ticketWriteSlot;                            // slot 0, byte offset 28
L718: function _tqWriteKey(uint24 lvl)     view returns (uint24) { return ticketWriteSlot ? lvl | TICKET_SLOT_BIT : lvl; }
L731: function _tqFarFutureKey(uint24 lvl) pure returns (uint24) { return lvl | TICKET_FAR_FUTURE_BIT; }
```

Path-to-key mapping the test MUST honor:
- **Path B (current-level via `_processOneTicketEntry`):** `rk = _tqWriteKey(lvl) = ticketWriteSlot ? (lvl | 0x800000) : lvl` — for lvl=1 with the standard whale-bundle fixture's post-buyTickets `ticketWriteSlot=true`, `rk = 0x800001`.
- **Path A (future-pool via `_processFutureTicketBatch`):** `rk = _tqFarFutureKey(lvl) = lvl | 0x400000` — for lvl=2..5, `rk` is `0x400002`..`0x400005` respectively.

Solidity nested-mapping slot derivation for `ticketsOwedPacked[rk][player]`:
```
// Outer mapping: uint24 key → uint256 (Solidity zero-extends uint24 to uint256 word for the encoder)
parentSlot = keccak256(abi.encode(uint256(rk), uint256(BASE_SLOT)))
// Inner mapping: address → uint40
slot       = keccak256(abi.encode(address(player), uint256(parentSlot)))
```

`BASE_SLOT` is the storage index of `ticketsOwedPacked`. **Pinned literal: `const TICKETS_OWED_PACKED_BASE_SLOT = 13n;`** — sourced from `forge inspect contracts/storage/DegenerusGameStorage.sol:DegenerusGameStorage storage-layout` (line: `ticketsOwedPacked  mapping(uint24 => mapping(address => uint40))  slot=13  offset=0  bytes=32`). Per 290-MEASUREMENT.md §2 the storage layout is byte-identical to v41 close, so the index is v41-frozen at 13. The test re-runs `forge inspect` at execute time and asserts the file's literal matches (see Task 2 verify gates).

Reading `ticketWriteSlot` from slot 0 at runtime (it's a bool packed at byte offset 28 — see L325 comment in storage). Slot 0 hex returns a 32-byte big-endian word; byte 28 from the low (right) end maps to bit 224. Simplest read:
```
const slot0Hex = await ethers.provider.getStorageAt(addr, 0);
const ticketWriteSlot = ((BigInt(slot0Hex) >> 224n) & 0xFFn) !== 0n;
```

JS port of the on-chain key derivation:
```
function computeRk(lvl, path, ticketWriteSlot) {
  const lvlBig = BigInt(lvl);
  if (path === "B") {                                // _tqWriteKey
    return ticketWriteSlot ? (lvlBig | 0x800000n) : lvlBig;
  }
  if (path === "A") {                                // _tqFarFutureKey
    return lvlBig | 0x400000n;
  }
  throw new Error(`unknown path: ${path}`);
}
```

Live v42 `_raritySymbolBatch` callsite arguments (mint:470, mint:793):
```
_raritySymbolBatch(player, baseKey, processed, take, entropy);
emit TraitsGenerated(player, baseKey, take);
```

Phase 282 reusable scaffolding patterns (test/edge/MintBatchDeterminism.test.js):
- `loadFixture(deployFullProtocol)` boot
- `DAILY_ENTROPY` pin: `0x2f02_3456_789a_bcde_f012_3456_789a_bcde_f012_3456_789a_bcde_f012_3456_789a_bcden`
- **`buyTickets(game, buyer, ticketCount, ethValue)`** — verbatim from `test/edge/MintBatchDeterminism.test.js` L121-L135. 4th positional param is named `ethValue` and is consumed via `eth(ethValue)` to produce the `value:` field of the `purchase()` tx. The wrapper body is `game.connect(buyer).purchase(ZERO_ADDRESS, BigInt(ticketCount) * 400n, 0n, ZERO_BYTES32, MintPaymentKind.DirectEth, { value: eth(ethValue) })`. Reuse this signature verbatim — do not invent a `multiplier` or rename `ethValue` to something else.
- `purchaseWhaleBundle(alice.address, 10, { value: eth(24) })` — queues tickets at future levels 2..5 (Path A)
- `pinDailyEntropy(game, deployer, mockVRF, DAILY_ENTROPY)` — VRF fulfillment of the daily word
- `drainViaAdvanceGame(game, deployer, storage, 300)` — repeated `advanceGame()` calls, returns captured events (Plan 02's port REMOVES the per-call gas accumulation; D-291-GAS-01 SKIP-GAS)

Inline duplication of ~50 LOC of scenario setup is acceptable per 291-CONTEXT.md decisions. Do NOT refactor `test/edge/MintBatchDeterminism.test.js` (frozen v41-closure artifact).

Topic-hash literals from .planning/phases/290-mint-batch-event-sig-cleanup-mintcln/290-01-MEASUREMENT.md §5:
- v41 (retired): `0x5e96bf2d5c935864be60ff066e1f498150a446b5b8b94321b0097276c61ec7c9`
- v42 (new):    `0x279edf1ccbf5db78a99006a6861b4d49de10ed6016d8400ce6a1d5e415d2ebc3`

Day-pin invariance (WRN-2 lock): the v42 `TraitsGenerated` event drops the `entropy` field, so the JS-replay uses the pinned `DAILY_ENTROPY` constant for keccak input. This is only valid if the drain stays within one VRF-day. The test reads `dailyIdx` (uint32, slot 0 byte offset 4..7) pre- and post-drain and asserts they're equal; a day rollover invalidates the scenario and the test FAILS with a clear message rather than silently mismatching.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold test/edge/MintCleanupRegression.test.js with JSDoc header (TST-MINTCLN-05) + shared whale-bundle drain setup</name>
  <files>test/edge/MintCleanupRegression.test.js</files>
  <read_first>
    - test/edge/MintCleanupRegression.test.js (target file; may not exist yet)
    - test/edge/MintBatchDeterminism.test.js (Phase 282 sister fixture — reference for fixture boot, DAILY_ENTROPY pin, buyTickets, purchaseWhaleBundle, pinDailyEntropy, drainViaAdvanceGame helpers; lift the shape inline, do not refactor the source file). NB the helper signature at L121-L135 is `buyTickets(game, buyer, ticketCount, ethValue)` — reuse verbatim.
    - test/helpers/raritySymbolBatchRef.mjs (Plan 01 output — `raritySymbolBatchRefV42` + `decodeOwedFromBaseKey` exports; existing `computeBaseKey`)
    - test/helpers/deployFixture.js (`deployFullProtocol`, `restoreAddresses`)
    - test/helpers/testUtils.js (`eth`, `advanceToNextDay`, `getLastVRFRequestId`, `ZERO_BYTES32`)
    - contracts/modules/DegenerusGameMintModule.sol L415-L503 + L750-L817 (post-MINTCLN B2-symmetric callsites; baseKey carries owed in low 32 bits)
    - contracts/storage/DegenerusGameStorage.sol L187 + L195 + L325 + L463-L488 + L718 + L731 (TICKET_SLOT_BIT / TICKET_FAR_FUTURE_BIT / ticketWriteSlot bool / ticketsOwedPacked declaration / v42 TraitsGenerated declaration / _tqWriteKey / _tqFarFutureKey — all load-bearing for the Task 2 storage-slot derivation)
    - .planning/phases/290-mint-batch-event-sig-cleanup-mintcln/290-01-MEASUREMENT.md §5 (topic-hash literals for the JSDoc header) + §2 (storage-layout EMPTY-diff attestation) + §6 (B2-symmetric callsite diff confirmation)
    - .planning/phases/290-mint-batch-event-sig-cleanup-mintcln/290-01-DESIGN-INTENT-TRACE.md (D-42N-MINTCLN-SCOPE-01 + D-42N-EVT-BREAK-01 anchor IDs for JSDoc cross-cite)
    - .planning/phases/291-mintcln-regression-fixture-tst-mintcln/291-CONTEXT.md (D-291-GAS-01 SKIP-GAS lock — no gas log; JSDoc header content spec from `## Specific Ideas`)
  </read_first>
  <action>
    Create `test/edge/MintCleanupRegression.test.js` from scratch (do not extend Phase 282's file). File structure:

    1. SPDX header `// SPDX-License-Identifier: AGPL-3.0-only`.
    2. JSDoc-style block-comment header at the top of the file (TST-MINTCLN-05 deliverable). Header content MUST include, in this order:
       - One-line title: `MintCleanupRegression.test.js — Phase 291 v42.0 MINTCLN regression fixture.`
       - Audit subject: cite Phase 290 audit-subject commit (`e5665117`) and the v42 cleanup of `_raritySymbolBatch` (3-input keccak; owed-in-baseKey carry) + `TraitsGenerated` (3-field event).
       - One-paragraph BREAKING-TOPIC-HASH note documenting the indexer-rebuild handoff. MUST include the verbatim string `0x5e96bf2d5c935864be60ff066e1f498150a446b5b8b94321b0097276c61ec7c9` (v41 retired) AND `0x279edf1ccbf5db78a99006a6861b4d49de10ed6016d8400ce6a1d5e415d2ebc3` (v42 new) AND the literal anchor ID `D-40N-EVT-BREAK-01` (inherited pre-launch posture) AND the literal anchor ID `D-42N-EVT-BREAK-01` (v42 carry). Forward-cite Phase 297 §9 "Deferred to Future Milestones" for the migration-tooling handoff.
       - Per `feedback_no_history_in_comments.md`: describe what IS — the v42 fixture asserts the post-MINTCLN audit subject; do NOT include comparative language like "previously had startIndex field" beyond the structural topic-hash transition citation. The topic-hash dual citation IS the structural attestation, not historical narrative.
       - Per-test mapping (5 lines, one per TST-MINTCLN-NN, mapping to the test name(s) that cover it). TST-MINTCLN-05 line MUST read: `TST-MINTCLN-05 — satisfied by this JSDoc header (no separate test case)`.
    3. Imports: `chai.expect`, `hardhat` (`hre`), `loadFixture` from `@nomicfoundation/hardhat-toolbox/network-helpers.js`, `deployFullProtocol` + `restoreAddresses` from `../helpers/deployFixture.js`, `eth` + `advanceToNextDay` + `getLastVRFRequestId` + `ZERO_BYTES32` from `../helpers/testUtils.js`, and from `../helpers/raritySymbolBatchRef.mjs`: `raritySymbolBatchRefV42`, `decodeOwedFromBaseKey`, `computeBaseKey`.
    4. Module-level constants:
       - `ZERO_ADDRESS = hre.ethers.ZeroAddress`
       - `MintPaymentKind = { DirectEth: 0, Claimable: 1, Combined: 2 }`
       - `DAILY_ENTROPY = 0x2f02_3456_789a_bcde_f012_3456_789a_bcde_f012_3456_789a_bcde_f012_3456_789a_bcden` (reuse Phase 282 pin for invariant continuity)
       - `TRAITS_GENERATED_V42_TOPIC_HASH = "0x279edf1ccbf5db78a99006a6861b4d49de10ed6016d8400ce6a1d5e415d2ebc3"` (used for parse + cross-check in Task 2; reuse the literal from MEASUREMENT.md §5 — same string that lives in the JSDoc header)
       - `TICKETS_OWED_PACKED_BASE_SLOT = 13n` (pinned literal; sourced from `forge inspect contracts/storage/DegenerusGameStorage.sol:DegenerusGameStorage storage-layout` line `ticketsOwedPacked  mapping(uint24 => mapping(address => uint40))  slot=13`. Re-validated at test runtime via the Task 2 forge-inspect gate. Reference: 290-MEASUREMENT.md §2 storage-layout EMPTY-diff attestation.)
       - `TICKET_SLOT_BIT = 0x800000n` (mirrors storage:187 `TICKET_SLOT_BIT = 1 << 23` for the `_tqWriteKey` formula port)
       - `TICKET_FAR_FUTURE_BIT = 0x400000n` (mirrors storage:195 `TICKET_FAR_FUTURE_BIT = 1 << 22` for the `_tqFarFutureKey` formula port)
    5. Module-level helper functions (inline copies of the Phase 282 patterns, adapted for v42's 3-field event):
       - `parseTraitsGeneratedEvents(receipt, storage)` — iterates `receipt.logs`, parses via `storage.interface.parseLog(log)`, filters `parsed.name === "TraitsGenerated"`, returns `{ player, baseKey, take, lvl, queueIdx, owedAtCallEntry, txHash }` where the four decoded fields come from `baseKey` bit-slicing (lvl = bits 255..232; queueIdx = bits 231..192; player = bits 191..32 cross-checked against `args.player`; owedAtCallEntry = `decodeOwedFromBaseKey(args.baseKey)`). Reject (throw) on any log whose decoded `player` bits do NOT match the indexed `args.player` (TST-MINTCLN-02 invariant).
       - `buyTickets(game, buyer, ticketCount, ethValue)` — identical to Phase 282 helper L121-L135 (4-entry × 100 scaling unit math). Verbatim signature `(game, buyer, ticketCount, ethValue)`; cite project memory `project_ticket_entry_price_units.md`. Do NOT rename `ethValue` to `multiplier` or any other token.
       - `pinDailyEntropy(game, deployer, mockVRF, word)` — identical to Phase 282 helper L254-L268.
       - `drainViaAdvanceGame(game, caller, storage, maxIters = 300)` — port the Phase 282 helper L272-L303 with TWO deletions: (a) REMOVE the per-call `perCallGas` accumulation block; the returned object MUST NOT contain a `totalGas` or `perCallGas` field; (b) Do NOT add a substitute `gas`-named field. D-291-GAS-01 SKIP-GAS lock STRICTLY: this fixture does not measure or surface gas anywhere — not even in commented-out code.
       - `readPlayerTraitMultiset(game, lvl, player)` — identical to Phase 282 helper L137-L156.
       - `readTicketWriteSlot(addr)` — `const s0 = await hre.ethers.provider.getStorageAt(addr, 0); return ((BigInt(s0) >> 224n) & 0xFFn) !== 0n;`. The runtime bool read for the `_tqWriteKey` formula in Task 2 storage derivation.
       - `readDailyIdx(addr)` — `const s0 = await hre.ethers.provider.getStorageAt(addr, 0); return Number((BigInt(s0) >> 32n) & 0xFFFFFFFFn);`. The slot-0 byte-4..7 read for day-pin invariance (WRN-2; see Task 2). NB: storage L228-L236 packs `purchaseStartDay` at bytes 0..3 + `dailyIdx` at bytes 4..7 of slot 0; `dailyIdx >> 32` extracts the byte-4..7 word.
       - `computeRk(lvl, path, ticketWriteSlot)` — JS port of `_tqWriteKey` / `_tqFarFutureKey`. Body:
           `const v = BigInt(lvl); if (path === "B") return ticketWriteSlot ? (v | TICKET_SLOT_BIT) : v; if (path === "A") return v | TICKET_FAR_FUTURE_BIT; throw new Error("path " + path);`.
         Used by Task 2 to derive the outer-mapping key for both Path A and Path B storage slots.
    6. The `describe("MintCleanupRegression — Phase 291 v42.0 MINTCLN regression fixture", function () { this.timeout(900_000); after(() => restoreAddresses()); ... })` block. Inside, declare the nested `describe("TST-MINTCLN-01..04 — end-to-end whale-bundle multi-call drain via advanceGame()", function () { ... })`. Implement a shared `setupWhaleBundleAndDrain()` async function that:
       a. `loadFixture(deployFullProtocol)`
       b. `await buyTickets(game, alice, 2000, 30)` — drives Path B at lvl=1
       c. `await game.connect(alice).purchaseWhaleBundle(alice.address, 10, { value: eth(24) })` — drives Path A at lvl=2..5 via `_prepareFutureTickets`
       d. `await pinDailyEntropy(game, deployer, mockVRF, DAILY_ENTROPY)`
       e. `const storage = await hre.ethers.getContractAt("DegenerusGameStorage", await game.getAddress())`
       f. **Day-pin pre-capture (WRN-2):** `const gameAddr = await game.getAddress(); const dayBefore = await readDailyIdx(gameAddr); const ticketWriteSlotBefore = await readTicketWriteSlot(gameAddr);` — capture both pre-drain. These values are returned for Task 2 assertions.
       g. `const { events } = await drainViaAdvanceGame(game, deployer, storage, 300)`
       h. **Day-pin post-check (WRN-2):** `const dayAfter = await readDailyIdx(gameAddr); if (dayAfter !== dayBefore) throw new Error(\`drain crossed day boundary — scenario invalid; dayBefore=${dayBefore} dayAfter=${dayAfter}\`);` — abort the test with a clear message if the drain rolled into day+1 (would invalidate the PINNED_DAILY_ENTROPY replay). Capture `ticketWriteSlotAfter` for downstream Path-B `rk` derivation in Task 2 (note: post-drain queue-slot swap may flip the bool; the test uses `ticketWriteSlotAfter` for the storage-slot read).
       i. Filter to `aliceEvents = events.filter(e => e.player.toLowerCase() === alice.address.toLowerCase())`
       j. Return `{ fixture, allEvents: events, aliceEvents, dayPinned: dayBefore, ticketWriteSlotPostDrain: ticketWriteSlotAfter, gameAddr }`

       Add the first test as an anchor sanity check:
       `it("TST-MINTCLN-03 anchor — whale-bundle drain emits TraitsGenerated at lvl=1 (Path B) AND at lvl>=2 (Path A) with path-accumulator=A|B discrimination", async function () { ... })`
       Inside: assert `aliceEvents.length >= 2`; group by `lvl`; assert at least one emission at `lvl === 1` (Path B) and at least one emission at some `lvl >= 2` (Path A); `console.log` the per-level `path-accumulator=A|B` breakdown matching the Phase 282 pattern. Do NOT log gas; D-291-GAS-01 STRICTLY zero.

    Do NOT yet implement TST-MINTCLN-01 (multiset replay), TST-MINTCLN-02 (per-emission shape), TST-MINTCLN-04 (storage slot read) — those come in Task 2. This task ships the file skeleton + JSDoc header + shared setup + the B2-coverage anchor test.

    Do NOT create `test/mint/` (single file does not justify a new directory; per 291-CONTEXT.md decisions).
    Do NOT extend or modify `test/edge/MintBatchDeterminism.test.js` (Phase 282 frozen v41-closure artifact).
    **D-291-GAS-01 STRICTLY zero:** do NOT add any `gas` token in the file — not in code, not in commented-out code, not in JSDoc, not in `console.log` strings. The grep gate at the bottom of `<verify>` filters comments and asserts count=0; any `gas` occurrence anywhere fails the gate.
  </action>
  <verify>
    <automated>
test -f test/edge/MintCleanupRegression.test.js
    </automated>
    <automated>
grep -c "0x5e96bf2d5c935864be60ff066e1f498150a446b5b8b94321b0097276c61ec7c9" test/edge/MintCleanupRegression.test.js
    </automated>
    <automated>
grep -c "0x279edf1ccbf5db78a99006a6861b4d49de10ed6016d8400ce6a1d5e415d2ebc3" test/edge/MintCleanupRegression.test.js
    </automated>
    <automated>
grep -c "D-40N-EVT-BREAK-01" test/edge/MintCleanupRegression.test.js
    </automated>
    <automated>
grep -c "D-42N-EVT-BREAK-01" test/edge/MintCleanupRegression.test.js
    </automated>
    <automated>
grep -c "describe(\"MintCleanupRegression" test/edge/MintCleanupRegression.test.js
    </automated>
    <automated>
grep -c "raritySymbolBatchRefV42\|decodeOwedFromBaseKey" test/edge/MintCleanupRegression.test.js
    </automated>
    <automated>
grep -c "function computeRk\|readTicketWriteSlot\|readDailyIdx" test/edge/MintCleanupRegression.test.js
    </automated>
    <automated>
grep -c "drain crossed day boundary" test/edge/MintCleanupRegression.test.js
    </automated>
    <automated>
grep -vE '^\s*(//|\*|/\*)' test/edge/MintCleanupRegression.test.js | grep -ci '\bgas\b'
    </automated>
    <automated>
git diff --name-only contracts/ test/edge/MintBatchDeterminism.test.js
    </automated>
    <automated>
npx hardhat test test/edge/MintCleanupRegression.test.js 2>&1 | tee /tmp/291-02-task1.log; grep -E "passing|TST-MINTCLN-03 anchor" /tmp/291-02-task1.log
    </automated>
  </verify>
  <done>
    - `test/edge/MintCleanupRegression.test.js` exists.
    - JSDoc header contains BOTH topic-hash literals + both anchor IDs `D-40N-EVT-BREAK-01` AND `D-42N-EVT-BREAK-01` AND the forward-cite to Phase 297 §9 AND the explicit line `TST-MINTCLN-05 — satisfied by this JSDoc header (no separate test case)` in the per-test mapping (INFO-1 resolution).
    - File imports `raritySymbolBatchRefV42` and `decodeOwedFromBaseKey` from the Plan 01 helper.
    - Module-level helpers `computeRk(lvl, path, ticketWriteSlot)`, `readTicketWriteSlot(addr)`, `readDailyIdx(addr)` exist (grep #8 returns ≥ 3).
    - `setupWhaleBundleAndDrain` captures `dailyIdx` pre/post-drain and throws `drain crossed day boundary — scenario invalid` on mismatch (grep #9 returns ≥ 1).
    - Anchor test `TST-MINTCLN-03 anchor — whale-bundle drain emits TraitsGenerated at lvl=1 (Path B) AND at lvl>=2 (Path A) with path-accumulator=A|B discrimination` passes locally (`npx hardhat test test/edge/MintCleanupRegression.test.js` exits 0; at least 1 passing test recorded).
    - **D-291-GAS-01 STRICTLY zero (WRN-3 resolution):** verification grep #10 excludes comment lines AND asserts count is `0` — no `gas` token anywhere in code lines. No commented `// gas:` annotations either (the comment-stripping grep applies to single-line `//`, multi-line `/*`/`*` continuation; the regex catches all three forms before piping to the `gas` count). No loophole. No exceptions.
    - No `contracts/` or `test/edge/MintBatchDeterminism.test.js` line is mutated (verification grep #11 returns empty).
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement TST-MINTCLN-01/02/04 assertions (multiset replay + per-emission decode + storage-slot read via path-aware rk derivation)</name>
  <files>test/edge/MintCleanupRegression.test.js</files>
  <read_first>
    - test/edge/MintCleanupRegression.test.js (post-Task-1 scaffold — extend, do not rewrite)
    - test/edge/MintBatchDeterminism.test.js L193-L252 (Phase 282 `reconstructMultisetWithAccumulator` + `reconstructMultisetViaReference` + `multisetEquals` patterns — reuse the path-A/path-B accumulator selector approach since the v42 fixture exercises both paths in one drain)
    - test/helpers/raritySymbolBatchRef.mjs (Plan 01 — `raritySymbolBatchRefV42`, `decodeOwedFromBaseKey`)
    - contracts/modules/DegenerusGameMintModule.sol L415-L503 (Path A processed accumulator `processed += take` at L493) + L750-L817 (Path B processed accumulator `processed += writesUsed >> 1` is the within-call accumulator for `_processOneTicketEntry`, computed at the caller `processTicketBatch` outer loop)
    - contracts/storage/DegenerusGameStorage.sol L187 + L195 + L325 + L463-L488 + L718 + L731 (CRITICAL: rk derivation depends on `TICKET_SLOT_BIT`, `TICKET_FAR_FUTURE_BIT`, `ticketWriteSlot`, `_tqWriteKey`, `_tqFarFutureKey` — the storage-slot read does NOT use raw `lvl` as the outer-mapping key)
    - .planning/phases/290-mint-batch-event-sig-cleanup-mintcln/290-01-MEASUREMENT.md §2 (storage layout EMPTY diff — slot index unchanged from v41) + §6 (B2-symmetric callsite diff)
    - .planning/phases/291-mintcln-regression-fixture-tst-mintcln/291-CONTEXT.md `<decisions>` (TST-MINTCLN-04 technique: `eth_getStorageAt` direct slot reads; slot derivation formula)
  </read_first>
  <action>
    Extend `test/edge/MintCleanupRegression.test.js` (do NOT rewrite the file; APPEND inside the existing describe block created in Task 1). Add three test cases inside the same nested `describe("TST-MINTCLN-01..04 — end-to-end whale-bundle multi-call drain via advanceGame()", ...)` and a separate `describe("TST-MINTCLN-04 — storage-layout regression at runtime", ...)` for the slot read:

    A. Module-level helper additions (above the describe block):

       1. `function annotateStartIndices(events, pathAccumulator)` — identical to Phase 282's helper at MintBatchDeterminism.test.js L167-L181 (groups events by `(txHash, player, lvl, queueIdx)` and walks the per-group `startIndexForReplay` cursor using the supplied accumulator).
       2. `PATH_A_ACCUMULATOR = (e, prev) => prev + e.take` (post-MINTCLN field is `take`, not `count`).
       3. `PATH_B_ACCUMULATOR = (e, prev) => { const baseOv = prev === 0 && e.owedAtCallEntry <= 2 ? 4 : 2; const writesUsed = (e.take <= 256 ? e.take * 2 : e.take + 256) + baseOv + (e.take === e.owedAtCallEntry ? 1 : 0); return prev + (writesUsed >> 1); }` — port the Phase 282 Path B formula (test/edge/MintBatchDeterminism.test.js L184-L191) with `count → take` field rename only.
       4. `function reconstructMultisetWithAccumulator(events, pathAccumulator)` — port Phase 282 L193-L210, but replace the Phase 282 call `raritySymbolBatchRef({ baseKey, entropyWord, ownedSalt, startIndex, count })` with `raritySymbolBatchRefV42({ baseKey: e.baseKey, entropyWord: DAILY_ENTROPY, startIndex: e.startIndexForReplay, count: e.take })`. NB: the v42 `TraitsGenerated` event does NOT carry `entropy` as a field; the test uses the pinned `DAILY_ENTROPY` constant (same value the on-chain VRF was fulfilled with via `pinDailyEntropy`). **Day-pin invariance lock (WRN-2):** this replay is only valid because `setupWhaleBundleAndDrain` asserted `dailyIdx` did not change during the drain. If a future scenario expansion adds emissions across days, the test must capture a per-event `entropyAtEmission` instead — out of scope for this phase.
       5. `function multisetEquals(a, b)` — identical to Phase 282 L212-L218.
       6. `function reconstructMultisetViaReference(events, onChainMultiset)` — identical to Phase 282 L229-L252 (tries Path A accumulator first; falls back to Path B; returns `{ multiset, pathUsed }`).

    B. Test `it("TST-MINTCLN-02 — each emission decodes to (player, baseKey, take) 3-tuple with baseKey low-32 = owed-at-call-entry + upper bits = (lvl, queueIdx, player); event topic-hash matches v42 literal", ...)`:
       - Run `setupWhaleBundleAndDrain()`.
       - For each `aliceEvent`, assert the parsed log has exactly 3 args (`player`, `baseKey`, `take`) and zero `startIndex` / `count` / `entropy` fields. Cross-check via `Object.keys(storage.interface.getEvent("TraitsGenerated").inputs).length === 3` (single one-time check via the ABI fragment).
       - For each event, recompute `expectedBaseKey = computeBaseKey(e.lvl, e.queueIdx, e.player) | BigInt(e.owedAtCallEntry)` (the v42 owed carry) and assert `BigInt(e.baseKey) === expectedBaseKey`.
       - Assert `decodeOwedFromBaseKey(e.baseKey) === e.owedAtCallEntry` (round-trip consistency with the parser).
       - Assert at least one raw log on the drain receipts has `log.topics[0] === TRAITS_GENERATED_V42_TOPIC_HASH` (proves the v42 topic-hash IS emitted on-chain — structural breaking-change attestation).

    C. Test `it("TST-MINTCLN-01 — multi-call drain trait-multiset equivalence: v42 3-input JS-replay reconstructs on-chain credited multiset trait-by-trait + cross-call seed separation evidence (pairwise-distinct keccak inputs)", ...)`:
       - Run `setupWhaleBundleAndDrain()` (which already asserted day-pin invariance per WRN-2).
       - Group `aliceEvents` by `lvl`; for each `lvl` with emissions, read on-chain multiset via `readPlayerTraitMultiset(game, lvl, alice.address)`.
       - Call `reconstructMultisetViaReference(levelEvents, onChain)`; assert `pathUsed !== "neither"`; assert trait-by-trait equality with the on-chain multiset using `multisetEquals`. `console.log` the per-level `path-accumulator=A|B` discrimination line matching Phase 282 format `[W2 lvl=X] num-emissions=N | emitted-count-sum=Y | on-chain=Z | reconstructed=R | path-accumulator=A`.
       - Cross-call seed separation evidence (TST-MINTCLN-01 invariant continuity): group `aliceEvents` by `(lvl, queueIdx)`; for each slot with ≥2 emissions, assert the SET of `baseKey` values across the slot's emissions has cardinality equal to the slot's emission count (i.e. pairwise-distinct `baseKey` values, evidencing the keccak input `(baseKey, entropy, groupIdx)` varies per call because `owed` shrinks per drain emission and is carried inside `baseKey`).

    D. Separate top-level (sibling) describe block `describe("TST-MINTCLN-04 — storage-layout regression at runtime", function () { this.timeout(900_000); ...})`. Inside, a single test:

       `it("ticketsOwedPacked[rk][player] slot reads decode to the expected (rem | (owed<<8)) 40-bit packed form at runtime post-drain — Path A (lvl=2..5 far-future) AND Path B (lvl=1 current-level) outer-mapping keys both resolve to non-zero packed values", async function () { ... })`

       **CRITICAL (BLK-1 resolution): the outer-mapping key is NOT raw `lvl`.** It is `_tqWriteKey(lvl)` for Path B and `_tqFarFutureKey(lvl)` for Path A — per storage L718/L731. Using raw `lvl` reads an unwritten slot and silently passes against zero. The test MUST derive `rk` per `(lvl, path)` pair via the `computeRk(lvl, path, ticketWriteSlot)` helper from Task 1.

       Implementation:
       1. Reuse `setupWhaleBundleAndDrain()`. Destructure `{ aliceEvents, ticketWriteSlotPostDrain, gameAddr }`.
       2. **forge-inspect runtime gate (BLK-2 resolution):** as the FIRST step inside the `it(...)`, run `const { execSync } = require("node:child_process");` (or import at module top) and execute `execSync("forge inspect contracts/storage/DegenerusGameStorage.sol:DegenerusGameStorage storage-layout 2>/dev/null").toString()`. Parse the output for the `ticketsOwedPacked` row. The forge output is pipe-delimited (`| name | type | slot | offset | bytes | contract |`); in JS, split each line on `|`, trim each cell, find the row whose cell #2 equals `ticketsOwedPacked`, and read cell #4 as the slot index (matches the shell gate `awk -F'|' '/ticketsOwedPacked/ {gsub(/[[:space:]]/, "", $4); print $4}'`). Assert it equals `13`. If `forge` is unavailable in CI (catch the `execSync` throw), `console.log` a warning `[TST-MINTCLN-04 storage-layout gate SKIPPED — forge not on PATH]` and continue — the pinned literal `TICKETS_OWED_PACKED_BASE_SLOT = 13n` is still the authoritative reference (Phase 290 MINTCLN-08 attested EMPTY diff). The gate is a belt-and-braces sanity check; it does not gate the test pass/fail in CI environments without forge.
       3. Build the (lvl, path) pairs from `aliceEvents`: group by `lvl`; for each `lvl`, determine `path` by the dominant emission source (lvl === 1 → "B"; lvl >= 2 → "A" — matches the whale-bundle scenario shape per 291-CONTEXT.md Code Insights). Track the unique set of `(lvl, path)` pairs exercised in the drain.
       4. For each `(lvl, path)` pair, derive `rk` and the storage slot:
          ```
          const rk = computeRk(lvl, path, ticketWriteSlotPostDrain);   // path === "B" ? (lvl | 0x800000n if ticketWriteSlot else lvl) : (lvl | 0x400000n)
          const parentSlot = ethers.keccak256(
            ethers.AbiCoder.defaultAbiCoder().encode(["uint256","uint256"], [rk, TICKETS_OWED_PACKED_BASE_SLOT])
          );
          const slot = ethers.keccak256(
            ethers.AbiCoder.defaultAbiCoder().encode(["address","uint256"], [alice.address, parentSlot])
          );
          ```
          NB: outer-mapping key type in Solidity is `uint24`, but `keccak256(abi.encode(...))` zero-extends to a 32-byte word per the Solidity reference manual — the `uint256` ABI tag in the encoder produces the correct byte layout. Confirm the path-B derived `rk` matches the expected formula `lvl=1, ticketWriteSlot=true → rk = 0x800001`; the path-A derived `rk` matches `lvl=2 → rk = 0x400002`, `lvl=3 → 0x400003`, etc.
       5. Read the slot: `const slotBytes = await ethers.provider.getStorageAt(gameAddr, slot)`.
       6. Decode: `const packed = BigInt(slotBytes); const rem = Number(packed & 0xFFn); const owed = Number((packed >> 8n) & 0xFFFFFFFFn);`.
       7. **Per-path assertions (covers cross-path coverage gap from BLK-1):**
          - For EVERY `(lvl, path)` pair tested, assert `packed > 0n` (the slot must be non-zero — proves the test is reading from a slot the contract actually wrote, not a silent default-zero read from a wrong `rk`). If `packed === 0n` for any pair, fail with message `[TST-MINTCLN-04 lvl=${lvl} path=${path}] storage slot resolved to zero — likely wrong rk derivation; expected rk=0x${rk.toString(16)}`.
          - Assert `packed < (1n << 40n)` (40-bit packed form per storage:465 layout).
          - Assert `(packed >> 40n) === 0n` (high-order bits above bit-39 must be zero).
          - **Path B (lvl=1) ONLY:** cross-check `owed` against `Number(await game.ticketsOwedView(1, alice.address))` (the contract's own getter reads the same slot through `_tqWriteKey(1)` internally — an independent on-chain readback that MUST agree with the direct slot read; if they disagree, `rk` derivation is wrong). Path A slot is internal-only and has no public view; the non-zero + sub-40-bit assertions are the coverage proof.
       8. `console.log` the slot read for traceability: `[TST-MINTCLN-04 storage-slot lvl=X path=A|B rk=0x... slot=0x... packed=0x... rem=N owed=M]` per pair. Do NOT log gas.

       Do NOT compare bytes against a v41 baseline tree at runtime (the structural pre+post diff was already attested by Phase 290 MINTCLN-08 `forge inspect` EMPTY at 290-MEASUREMENT.md §2 — TST-MINTCLN-04's runtime job is to confirm the packed-form bytes decode correctly post-drain on the post-patch deployment AND that BOTH B2-symmetric path's storage keys are reachable via the derived `rk`).

    E. Do NOT add a TST-MINTCLN-05 test case. TST-MINTCLN-05 is a documentation-only deliverable satisfied entirely by the Task 1 JSDoc header — explicit tag `TST-MINTCLN-05 — satisfied by this JSDoc header (no separate test case)` lives in the JSDoc per-test mapping (INFO-1 resolution).

    **D-291-GAS-01 STRICTLY zero:** as in Task 1, do NOT add any `gas` token in any line — code, comment, or string. Verification grep #7 excludes comments and asserts count=0.
  </action>
  <verify>
    <automated>
grep -c "TST-MINTCLN-02" test/edge/MintCleanupRegression.test.js
    </automated>
    <automated>
grep -c "TST-MINTCLN-01" test/edge/MintCleanupRegression.test.js
    </automated>
    <automated>
grep -c "TST-MINTCLN-04" test/edge/MintCleanupRegression.test.js
    </automated>
    <automated>
grep -c "getStorageAt\|TICKETS_OWED_PACKED_BASE_SLOT" test/edge/MintCleanupRegression.test.js
    </automated>
    <automated>
grep -c "const TICKETS_OWED_PACKED_BASE_SLOT = 13n" test/edge/MintCleanupRegression.test.js
    </automated>
    <automated>
grep -c "computeRk(" test/edge/MintCleanupRegression.test.js
    </automated>
    <automated>
forge inspect contracts/storage/DegenerusGameStorage.sol:DegenerusGameStorage storage-layout 2>/dev/null | awk -F'|' '/ticketsOwedPacked/ {gsub(/[[:space:]]/, "", $4); print $4; exit}'
    </automated>
    <automated>
grep -c "raritySymbolBatchRefV42" test/edge/MintCleanupRegression.test.js
    </automated>
    <automated>
grep -c "TRAITS_GENERATED_V42_TOPIC_HASH" test/edge/MintCleanupRegression.test.js
    </automated>
    <automated>
grep -vE '^\s*(//|\*|/\*)' test/edge/MintCleanupRegression.test.js | grep -ci '\bgas\b'
    </automated>
    <automated>
npx hardhat test test/edge/MintCleanupRegression.test.js 2>&1 | tee /tmp/291-02-task2.log; tail -20 /tmp/291-02-task2.log
    </automated>
    <automated>
grep -E "TST-MINTCLN-(01|02|03|04)" /tmp/291-02-task2.log | grep -c "✓\|passing"
    </automated>
    <automated>
git diff --name-only contracts/ test/edge/MintBatchDeterminism.test.js
    </automated>
  </verify>
  <done>
    - 4 test cases pass under `npx hardhat test test/edge/MintCleanupRegression.test.js` (TST-MINTCLN-03 anchor from Task 1 + TST-MINTCLN-01 + TST-MINTCLN-02 + TST-MINTCLN-04).
    - The `TST-MINTCLN-01` test logs `path-accumulator=A|B` discrimination matching Phase 282 format AND asserts `dailyIdx` invariance via the `setupWhaleBundleAndDrain` guard (WRN-2 resolution).
    - The `TST-MINTCLN-02` test successfully cross-checks `BigInt(e.baseKey) === computeBaseKey(lvl, queueIdx, player) | BigInt(owed)` for every emission.
    - **The `TST-MINTCLN-04` test (BLK-1 resolution):** asserts a 40-bit-packed-form slot read for BOTH Path B (lvl=1, `rk = ticketWriteSlot ? 0x800001 : 0x000001`) AND Path A (lvl=2..5, `rk = 0x400002..0x400005`) — derives `rk` via the `computeRk(lvl, path, ticketWriteSlot)` helper; reads `ticketWriteSlot` at runtime via `readTicketWriteSlot`; cross-checks Path B `owed` against `ticketsOwedView(1, alice)`; asserts EVERY exercised `(lvl, path)` pair's slot is non-zero (cross-path coverage proof).
    - **BLK-2 resolution:** the `TICKETS_OWED_PACKED_BASE_SLOT` literal is pinned to `13n` (grep #5 returns 1); the `forge inspect storage-layout` runtime gate at the top of the storage-layout test asserts the slot index matches `13` AND verify command #7 above re-runs the same inspect command in CI and emits the expected output `13`.
    - **WRN-3 resolution:** D-291-GAS-01 is strictly zero — verification grep #10 (`grep -vE '^\s*(//|\*|/\*)' ... | grep -ci '\bgas\b'`) returns `0`. No `gas`-token loophole, no commented-out gas annotation, no informational gas log. The grep filters comments first, then asserts zero gas occurrences in the remaining code lines.
    - **INFO-1 resolution:** the JSDoc per-test mapping includes the explicit line `TST-MINTCLN-05 — satisfied by this JSDoc header (no separate test case)` (verified by Task 1 done block).
    - `git diff --name-only` shows ONLY `test/edge/MintCleanupRegression.test.js` and `test/helpers/raritySymbolBatchRef.mjs` modified (last line check returns no `contracts/` or `MintBatchDeterminism.test.js` matches).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: USER-APPROVED single batched commit at phase close</name>
  <what-built>
    Plans 01 + 02 ship two test-tree files:
    1. `test/helpers/raritySymbolBatchRef.mjs` — additive `raritySymbolBatchRefV42` + `decodeOwedFromBaseKey` exports (v41 export `raritySymbolBatchRef` UNCHANGED). Plan 01 deliverable.
    2. `test/edge/MintCleanupRegression.test.js` — new file; 4 passing tests covering TST-MINTCLN-01/02/03/04 + JSDoc header satisfying TST-MINTCLN-05. Plan 02 deliverable.

    Both deliverables stage into ONE batched commit per `feedback_batch_contract_approval.md`. Per `feedback_no_contract_commits.md`: zero `contracts/` mutations. Per `feedback_never_preapprove_contracts.md`: this is the USER-APPROVED close-out and the USER MUST review the diff before approving.

    The agent prepares the diff for review and waits for explicit user approval before running `git commit`. The commit body cites the Phase 290 audit-subject commit (`e5665117`) as the audit subject under regression and copies forward the TST-MINTCLN-NN→test-name mapping from the JSDoc header.
  </what-built>
  <how-to-verify>
    1. Run `git status` and confirm exactly two files are staged-or-modified: `test/helpers/raritySymbolBatchRef.mjs` and `test/edge/MintCleanupRegression.test.js`. Nothing else (no `contracts/`, no `.planning/STATE.md`, no orchestrator territory).
    2. Run `git diff --name-only HEAD -- contracts/` and confirm output is EMPTY (zero `contracts/` mutations — `feedback_no_contract_commits.md` lock).
    3. Run `git diff --name-only HEAD -- test/edge/MintBatchDeterminism.test.js` and confirm output is EMPTY (Phase 282 frozen artifact UNCHANGED).
    4. Run `git diff -- test/helpers/raritySymbolBatchRef.mjs` and confirm the diff is purely additive (new `raritySymbolBatchRefV42` + `decodeOwedFromBaseKey` exports + their docstrings; zero deletions in the existing `raritySymbolBatchRef` body).
    5. Run `git diff -- test/edge/MintCleanupRegression.test.js` and review the new file end-to-end. Confirm the JSDoc header at the top contains:
       - v41 topic hash `0x5e96bf2d5c935864be60ff066e1f498150a446b5b8b94321b0097276c61ec7c9` (retired)
       - v42 topic hash `0x279edf1ccbf5db78a99006a6861b4d49de10ed6016d8400ce6a1d5e415d2ebc3` (new)
       - anchor IDs `D-40N-EVT-BREAK-01` AND `D-42N-EVT-BREAK-01`
       - forward-cite to Phase 297 §9 "Deferred to Future Milestones"
       - per-test mapping for TST-MINTCLN-01..05 (5 lines) INCLUDING the explicit line `TST-MINTCLN-05 — satisfied by this JSDoc header (no separate test case)`
    6. Run the full test invocation once more: `npx hardhat test test/edge/MintCleanupRegression.test.js` and confirm 4 passing tests; AND confirm `npx hardhat test test/edge/MintBatchDeterminism.test.js` STILL passes (Phase 282 fixture compatibility lock).
    7. Confirm the storage-layout pin: `forge inspect contracts/storage/DegenerusGameStorage.sol:DegenerusGameStorage storage-layout 2>/dev/null | awk -F'|' '/ticketsOwedPacked/ {gsub(/[[:space:]]/, "", $4); print $4; exit}'` returns `13` AND `grep -c "const TICKETS_OWED_PACKED_BASE_SLOT = 13n" test/edge/MintCleanupRegression.test.js` returns `1`.
    8. Confirm D-291-GAS-01 strictly zero: `grep -vE '^\s*(//|\*|/\*)' test/edge/MintCleanupRegression.test.js | grep -ci '\bgas\b'` returns `0`.
    9. Read the proposed commit message body in full. The body MUST include:
       - Subject line `tests(291-02): ship TST-MINTCLN-01..05 mint-cleanup regression fixture [USER-APPROVED]` (or equivalent; planner-default subject form follows the Phase 290 batched-commit convention).
       - Body cites the Phase 290 audit-subject commit `e5665117` (the v42 MINTCLN cleanup commit under regression).
       - Body cites D-291-GAS-01 SKIP-GAS posture explicitly (no empirical gas measurement; theoretical attestation at 290-MEASUREMENT.md §3 is the audit-cite source).
       - Body lists the 5 TST-MINTCLN-NN requirement IDs each with the test name (or test block name) that covers it.
       - Body cites `feedback_batch_contract_approval.md` (single batched commit) + `feedback_no_contract_commits.md` (zero `contracts/` mutations).
       - No emoji per `./CLAUDE.md` global posture (check the project-skills env for emoji policy).
    10. Approve OR reject. If approving, the agent runs `git commit` (using the heredoc commit-body pattern from the role prompt). Reject if any of the above checks fail.
  </how-to-verify>
  <resume-signal>Type "approved — commit the batched tests" to authorize the commit. Type "reject" + describe the issue to send the agent back to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Captured `TraitsGenerated` event payload → JS-side decode | Untrusted log payload values cross into JS numeric/BigInt ops; risk is misdecoding the new 3-field event or u32/u256 boundary precision loss. |
| Derived storage slot → `eth_getStorageAt` | Slot-derivation correctness for nested mapping `mapping(uint24 => mapping(address => uint40))`; outer-mapping key is NOT raw `lvl` — it's `_tqWriteKey(lvl)` for Path B (current-level) and `_tqFarFutureKey(lvl)` for Path A (far-future). Wrong `rk` → silent default-zero slot read → false-positive pass. Mitigated by per-pair non-zero assertion + Path-B `ticketsOwedView` cross-check (BLK-1 resolution). |
| `TICKETS_OWED_PACKED_BASE_SLOT` literal → on-chain layout | Slot index pinned to `13n` from `forge inspect`. Runtime gate re-validates via `execSync` (BLK-2 resolution). Drift would silently break the test. |
| Plan 01 helper invocation → contract keccak input shape | The v42 3-input JS replay must match `abi.encode(['uint256','uint256','uint32'], ...)` byte-equivalence at `contracts/modules/DegenerusGameMintModule.sol:563-565`; a mis-encoded helper produces a silent off-by-mismatch trait sequence. |
| `DAILY_ENTROPY` pinned constant → on-chain entropy at drain | Day-pin invariance lock (WRN-2): replay is valid only if the drain stays within one VRF-day. `setupWhaleBundleAndDrain` asserts `dailyIdx` pre/post-drain equality and throws on rollover. |
| Test-file JSDoc header → audit-deliverable cite chain | Topic-hash literal correctness for TST-MINTCLN-05 indexer-migration handoff; risk is typo → broken cite chain in Phase 297 §9. |

(No `contracts/` mutations in this phase → no on-chain attack-surface change.)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-291-04 | Tampering | `parseTraitsGeneratedEvents` event-shape decode | mitigate | Cross-check decoded `player` (recovered from `baseKey` bits 191..32) against the indexed `args.player`; throw on mismatch. Verification command runs the full test suite which exercises this code path with real on-chain emissions. |
| T-291-05 | Tampering | TST-MINTCLN-04 slot-derivation correctness | mitigate | `computeRk(lvl, path, ticketWriteSlot)` ports the on-chain `_tqWriteKey` / `_tqFarFutureKey` formulas (storage L718/L731). `ticketWriteSlot` read at runtime via slot-0 byte 28 (storage L325 layout). Per-pair non-zero packed value assertion catches wrong `rk` (default-zero slot read fails). Path-B `owed` cross-checked against `ticketsOwedView(1, alice)` — the contract's own getter — for a third independent confirmation. |
| T-291-06 | Information disclosure / numeric overflow | u32 owed precision at `decodeOwedFromBaseKey` + slot decode | mitigate | All BigInt masks use literal `0xFFFFFFFFn` (32-bit) before `Number(...)` cast; precision-loss boundary is `Number.MAX_SAFE_INTEGER = 2^53-1` which contains the full u32 range. Slot-decode masks use `0xFFn` (rem byte) + `0xFFFFFFFFn` (owed) explicitly. |
| T-291-07 | Tampering | helper extension accidentally modifies v41 export | mitigate | Plan 01 verification commands assert both v41 and v42 exports exist as distinct named functions; Plan 02 verification re-runs Phase 282's fixture (`npx hardhat test test/edge/MintBatchDeterminism.test.js`) at Task 3 step 6 as part of the user-approval checkpoint. |
| T-291-08 | Tampering | breaking-topic-hash cite typo in JSDoc header | mitigate | Two literal-string grep gates in Task 1 verification (full 64-char hash literals must appear verbatim — typos fail the grep gate). |
| T-291-09 | Tampering | `TICKETS_OWED_PACKED_BASE_SLOT` index drift from on-chain layout | mitigate | (BLK-2) Pinned literal `13n` sourced from `forge inspect`. Plan-time verify gate #7 re-runs `forge inspect ... | awk -F'|' '/ticketsOwedPacked/ {gsub(/[[:space:]]/, "", $4); print $4}'` and asserts output is `13`. Runtime gate at the top of the storage-layout test re-runs the same inspect inside the test and asserts the file's literal matches. Drift fails both gates. |
| T-291-10 | Tampering / replay-invariance | day-boundary rollover during drain invalidates `DAILY_ENTROPY` replay | mitigate | (WRN-2) `setupWhaleBundleAndDrain` reads `dailyIdx` from slot 0 byte 4..7 pre/post-drain and throws `drain crossed day boundary — scenario invalid` if they differ. Forces a hard test failure with a diagnostic message rather than silent multiset mismatch. |
| T-291-SC | Tampering | npm install / new dependency | accept | No new dependencies installed in this phase. Verification: `git diff --name-only HEAD -- package.json package-lock.json` returns EMPTY at Task 3 (manual check by user during approval). Package-legitimacy gate not applicable. |

(No `[ASSUMED]`/`[SUS]`/`[SLOP]` package installs in this phase → no human-blocking checkpoint required beyond Task 3's USER-APPROVED commit checkpoint.)
</threat_model>

<verification>
- `npx hardhat test test/edge/MintCleanupRegression.test.js` exits 0 with at least 4 passing tests (TST-MINTCLN-03 anchor + TST-MINTCLN-01 + TST-MINTCLN-02 + TST-MINTCLN-04).
- `npx hardhat test test/edge/MintBatchDeterminism.test.js` STILL exits 0 (Phase 282 fixture compatibility — helper extension is additive only).
- `git diff --name-only HEAD -- contracts/` returns EMPTY at every checkpoint.
- `git diff --name-only HEAD -- test/edge/MintBatchDeterminism.test.js` returns EMPTY.
- JSDoc header at the top of `test/edge/MintCleanupRegression.test.js` contains both topic-hash literals + both anchor IDs + the TST-MINTCLN-05 explicit-tag line (verified by grep).
- `forge inspect contracts/storage/DegenerusGameStorage.sol:DegenerusGameStorage storage-layout 2>/dev/null | awk -F'|' '/ticketsOwedPacked/ {gsub(/[[:space:]]/, "", $4); print $4; exit}'` returns `13` AND `grep -c "const TICKETS_OWED_PACKED_BASE_SLOT = 13n" test/edge/MintCleanupRegression.test.js` returns `1` (BLK-2 lock).
- `grep -vE '^\s*(//|\*|/\*)' test/edge/MintCleanupRegression.test.js | grep -ci '\bgas\b'` returns `0` (WRN-3 lock — strictly zero, no exceptions, no commented annotations).
- A single USER-APPROVED batched commit lands at phase close containing the helper extension + the new test file (and nothing else).
</verification>

<success_criteria>
- All 5 TST-MINTCLN-NN requirements satisfied:
  - TST-MINTCLN-01 (multi-call drain trait-multiset equivalence + cross-call seed separation; day-pin invariance asserted via `dailyIdx` pre/post-drain equality) — Task 2 test passes.
  - TST-MINTCLN-02 (TraitsGenerated 3-field decode + baseKey decomposition + v42 topic-hash structural attestation) — Task 2 test passes.
  - TST-MINTCLN-03 (Path A + Path B coverage with `path-accumulator=A|B` log discrimination) — Task 1 anchor test passes.
  - TST-MINTCLN-04 (storage-slot read decodes to 40-bit packed form matching `ticketsOwedView` for Path B + non-zero for Path A; `rk` derived via `computeRk(lvl, path, ticketWriteSlot)` from `_tqWriteKey` / `_tqFarFutureKey` — NOT raw lvl; `TICKETS_OWED_PACKED_BASE_SLOT = 13n` pinned + runtime forge-inspect gate) — Task 2 storage-layout test passes.
  - TST-MINTCLN-05 (JSDoc header documents breaking topic-hash transition + cites both anchor IDs + forward-cites Phase 297 §9; explicit per-test-mapping line names this requirement as JSDoc-only) — Task 1 JSDoc deliverable.
- Phase 282's `MintBatchDeterminism.test.js` keeps passing without modification (helper additive only).
- Zero `contracts/` mutations across the entire phase (git diff confirms).
- D-291-GAS-01 honored: STRICTLY zero `gas` tokens in either deliverable — code or comment (WRN-3 lock).
- Single USER-APPROVED batched commit lands at phase close (helper + test file in one diff).
</success_criteria>

<output>
Create `.planning/phases/291-mintcln-regression-fixture-tst-mintcln/291-02-SUMMARY.md` when done. The SUMMARY notes the USER-APPROVED batched commit SHA (filled in post-commit). Per `feedback_batch_contract_approval.md` the SUMMARY is included IN the same batched commit as the test deliverables (Phase 290 precedent — Wave-2-finish protocol; avoids a separate metadata commit). Do NOT `git push`.
</output>
</content>
</invoke>