---
phase: 291-mintcln-regression-fixture-tst-mintcln
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/helpers/raritySymbolBatchRef.mjs
autonomous: true
requirements: [TST-MINTCLN-01]
requirements_addressed: [TST-MINTCLN-01]

must_haves:
  truths:
    - "test/helpers/raritySymbolBatchRef.mjs exports a v42 3-input reference function alongside the existing v41 4-input export"
    - "The v41 4-input export raritySymbolBatchRef remains byte-identical (Phase 282 fixture must keep passing without modification)"
    - "The v42 reference replays the live `keccak256(abi.encode(baseKey, entropyWord, groupIdx))` 3-input form at contracts/modules/DegenerusGameMintModule.sol:563-565 (3 inputs; no ownedSalt)"
    - "The v42 reference accepts `baseKey` as the post-MINTCLN form with `owed` ORed into the low 32 bits (mint:426-429 + mint:763-766) and recovers owed-at-call-entry from `baseKey & 0xFFFFFFFF`"
    - "No empirical gas measurement, gas helper, or gas constant is added — D-291-GAS-01 SKIP-GAS posture preserved"
  artifacts:
    - path: "test/helpers/raritySymbolBatchRef.mjs"
      provides: "v42 3-input reference function (additive export) + retained v41 4-input export"
      contains: "export function raritySymbolBatchRefV42"
  key_links:
    - from: "test/helpers/raritySymbolBatchRef.mjs::raritySymbolBatchRefV42"
      to: "contracts/modules/DegenerusGameMintModule.sol:563-565 _raritySymbolBatch keccak input"
      via: "abi.encode(['uint256','uint256','uint32'], [baseKey, entropyWord, groupIdx]) byte-equivalence"
      pattern: "abiCoder.encode.*uint256.*uint256.*uint32"
    - from: "test/helpers/raritySymbolBatchRef.mjs"
      to: "test/edge/MintBatchDeterminism.test.js (Phase 282)"
      via: "existing import of raritySymbolBatchRef + computeBaseKey (UNCHANGED)"
      pattern: "import.*raritySymbolBatchRef.*from.*raritySymbolBatchRef"
---

<objective>
Extend `test/helpers/raritySymbolBatchRef.mjs` with a v42 3-input reference implementation of `_raritySymbolBatch` (post-MINTCLN). The existing v41 4-input export stays byte-identical so Phase 282's `MintBatchDeterminism.test.js` keeps passing. The new function is the JS-replay oracle that Plan 291-02 will use to validate the post-MINTCLN trait multiset against the on-chain credited state.

Purpose: TST-MINTCLN-01 requires "trait multiset equals v41 Phase 281 owed-salt multiset at same `(level, queueIdx, player, dailyEntropy)`" — interpreted in 291-CONTEXT.md as "preserves the algorithmic invariant of cross-call seed separation, NOT bit-identical hash outputs." The v42 oracle replays the live 3-input keccak against captured `TraitsGenerated(player, baseKey, take)` tuples; cross-call seed separation is evidenced by pairwise-distinct keccak inputs across multi-call emissions (owed varies inside `baseKey`, so the 3-tuple `(baseKey, entropyWord, groupIdx)` differs per call).

Output: One additive export in `test/helpers/raritySymbolBatchRef.mjs`. Zero `contracts/` mutations. No commit at this plan (single USER-APPROVED batched commit lives at Plan 02 close per `feedback_batch_contract_approval.md`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/291-mintcln-regression-fixture-tst-mintcln/291-CONTEXT.md
@.planning/phases/290-mint-batch-event-sig-cleanup-mintcln/290-01-MEASUREMENT.md
@.planning/phases/290-mint-batch-event-sig-cleanup-mintcln/290-02-SUMMARY.md
@test/helpers/raritySymbolBatchRef.mjs
@contracts/modules/DegenerusGameMintModule.sol
@contracts/storage/DegenerusGameStorage.sol

<interfaces>
Live post-MINTCLN keccak input shape (contracts/modules/DegenerusGameMintModule.sol:563-565):

```
uint256 seed = uint256(keccak256(abi.encode(baseKey, entropyWord, groupIdx)));
uint64 s = uint64(seed) | 1;
uint8 offset = uint8(i & 15);
s = s * (TICKET_LCG_MULT + uint64(offset)) + uint64(offset);
// inner LCG loop unchanged from v41
```

baseKey construction (mint:426-429 future-pool Path A; mint:763-766 current-level Path B; B2-symmetric):
```
uint256 baseKey = (uint256(lvl) << 224) |
                  (idx << 192) |
                  (uint256(uint160(player)) << 32) |
                  uint256(owed);
```

Live v42 `TraitsGenerated` event (contracts/storage/DegenerusGameStorage.sol:484-488):
```
event TraitsGenerated(address indexed player, uint256 baseKey, uint32 take);
```

`baseKey` bit-layout the JS reference must understand:
- bits 255..232: lvl (uint24)
- bits 191..32:  player (uint160) — NB: bits 231..192 carry queueIdx (64-bit field, but Solidity passes queueIdx as uint64 padded into the 64-bit slot via `idx << 192`)
- bits 31..0:    owed (uint32) — NEW post-MINTCLN per MINTCLN-02

Existing v41 helper export (test/helpers/raritySymbolBatchRef.mjs:126-183) — DO NOT MODIFY this export; Phase 282 fixture imports it and must keep passing. Add the v42 form as a NEW additive export.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add v42 3-input reference export to test/helpers/raritySymbolBatchRef.mjs</name>
  <files>test/helpers/raritySymbolBatchRef.mjs</files>
  <read_first>
    - test/helpers/raritySymbolBatchRef.mjs (entire 183-line file — verbatim port of v41 4-input `_raritySymbolBatch`; the v41 export `raritySymbolBatchRef` and `computeBaseKey` + `TICKET_LCG_MULT` constants stay untouched)
    - contracts/modules/DegenerusGameMintModule.sol L537-L588 (live v42 `_raritySymbolBatch` body — 3-input keccak at L563-L565; baseKey decoding at L591)
    - contracts/modules/DegenerusGameMintModule.sol L426-L429 + L763-L766 (B2-symmetric baseKey construction with `| uint256(owed)`)
    - contracts/storage/DegenerusGameStorage.sol L484-L488 (v42 `TraitsGenerated(address indexed player, uint256 baseKey, uint32 take)` declaration)
    - .planning/phases/290-mint-batch-event-sig-cleanup-mintcln/290-01-MEASUREMENT.md §5 (v41 topic-hash `0x5e96bf2d...` retired + v42 topic-hash `0x279edf1c...` new — for awareness; this task does not assert on them)
    - .planning/phases/290-mint-batch-event-sig-cleanup-mintcln/290-02-SUMMARY.md (Phase 290 audit-subject context)
    - test/edge/MintBatchDeterminism.test.js L66-L82 (existing import sites that MUST keep working — `import { computeBaseKey, raritySymbolBatchRef }`)
  </read_first>
  <action>
    Add a new exported function `raritySymbolBatchRefV42({ baseKey, entropyWord, startIndex, count })` to `test/helpers/raritySymbolBatchRef.mjs`. The function is a verbatim port of the live post-MINTCLN `_raritySymbolBatch` body at `contracts/modules/DegenerusGameMintModule.sol:537-588`. Differences from the existing v41 export:

    1. Drop the `ownedSalt` parameter from the API surface entirely. The v42 contract no longer takes `ownedSalt` as an arg — `owed` is carried inside `baseKey` low 32 bits.
    2. Replace the v41 4-input keccak `abi.encode(['uint256','uint256','uint32','uint32'], [baseKey, entropyWord, groupIdx, ownedSalt])` with the v42 3-input form `abi.encode(['uint256','uint256','uint32'], [baseKey, entropyWord, groupIdx])`. This matches L563-L565 of the live contract verbatim.
    3. Preserve every other line of the body (uint64 LCG mask, `s = uint64(seed) | 1`, `offset = i & 15`, `s = s * (TICKET_LCG_MULT + offset) + offset`, inner-loop LCG step `s = s * TICKET_LCG_MULT + 1`, trait extraction via `traitFromWord(s) + (quadrant << 6)`).
    4. Reuse the existing module-local helpers (`weightedColorBucket`, `traitFromWord`, `U64_MASK`, `TICKET_LCG_MULT`, `abiCoder`) without duplication. The existing v41 export `raritySymbolBatchRef` MUST remain present and byte-identical — Phase 282's fixture imports it and must keep passing.

    Add a JSDoc block above the new function describing it as the v42 3-input reference and citing the live-contract line range (`contracts/modules/DegenerusGameMintModule.sol:537-588`). Per `feedback_no_history_in_comments.md`: describe what IS, never what changed. Do NOT write "previously took ownedSalt" or "v41 form" in the JSDoc body (the function name `raritySymbolBatchRefV42` is the only version marker).

    Also add a small helper export `decodeOwedFromBaseKey(baseKey)` that returns `Number(BigInt(baseKey) & 0xFFFFFFFFn)` — Plan 02's drain test will use this to recover owed-at-call-entry from the emitted `baseKey` (replaces the v41 `owedAtCallEntry = Number(parsed.args.startIndex)` extraction since v42's `TraitsGenerated` no longer carries a separate `startIndex` field).

    **Runtime environment note (WRN-4 resolution):** The verify commands below use a BigInt literal with ES2021 numeric separators (`0x2f02_3456_..._n`). The project's `package.json` baseline is Node ≥ 18 (Hardhat + ethers v6 hard requirement); ES2021 numeric separators have been supported since Node 12.5, so the literal is safe under the project baseline. No code changes required from this note — it documents intent so the executor does not "simplify" the literal by removing the underscores.

    Do NOT add gas helpers, gas constants, or any informational gas log — D-291-GAS-01 SKIP-GAS lock.
  </action>
  <verify>
    <automated>
node -e "import('./test/helpers/raritySymbolBatchRef.mjs').then(m => { if (typeof m.raritySymbolBatchRef !== 'function') process.exit(1); if (typeof m.raritySymbolBatchRefV42 !== 'function') process.exit(2); if (typeof m.computeBaseKey !== 'function') process.exit(3); if (typeof m.decodeOwedFromBaseKey !== 'function') process.exit(4); if (typeof m.TICKET_LCG_MULT !== 'bigint') process.exit(5); console.log('helpers OK'); })"
    </automated>
    <automated>
node -e "import('./test/helpers/raritySymbolBatchRef.mjs').then(m => { const bk = m.computeBaseKey(1, 6, '0x1234567890123456789012345678901234567890'); const bkWithOwed = bk | 8000n; if (m.decodeOwedFromBaseKey(bkWithOwed) !== 8000) process.exit(1); const traits = m.raritySymbolBatchRefV42({ baseKey: bkWithOwed, entropyWord: 0x2f02_3456_789a_bcde_f012_3456_789a_bcde_f012_3456_789a_bcde_f012_3456_789a_bcden, startIndex: 0, count: 4 }); if (traits.length !== 4) process.exit(2); console.log('v42 ref returned 4 traits:', Array.from(traits)); })"
    </automated>
    <automated>
grep -n "export function raritySymbolBatchRef\b" test/helpers/raritySymbolBatchRef.mjs | grep -v "V42"
    </automated>
    <automated>
grep -nE "abi\\.encode|abiCoder\\.encode" test/helpers/raritySymbolBatchRef.mjs
    </automated>
  </verify>
  <done>
    - `test/helpers/raritySymbolBatchRef.mjs` exports BOTH `raritySymbolBatchRef` (v41 4-input; unchanged) AND `raritySymbolBatchRefV42` (v42 3-input; new) AND `decodeOwedFromBaseKey` (new helper).
    - `import { raritySymbolBatchRef, computeBaseKey } from "../helpers/raritySymbolBatchRef.mjs"` continues to resolve identical bindings (Phase 282 fixture compatibility).
    - The v42 function's keccak step uses `abiCoder.encode(['uint256','uint256','uint32'], [baseKey, entropyWord, groupIdx])` — three types, no fourth `ownedSalt` arg.
    - Running `npx hardhat test test/edge/MintBatchDeterminism.test.js` still passes (verified by Plan 02's smoke check; this plan ships only the helper extension and does not run the full Phase 282 suite).
    - Node ≥ 18 required (project baseline); BigInt numeric-separator literal in verify command #2 is ES2021 and safe under the baseline — executor must not strip the underscores.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS test helper → on-chain emit data | Untrusted event-payload values flow into JS-side numeric ops; risk is unsafe BigInt → Number coercion at the u32/u256 boundary inside the JS replay. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-291-01 | Tampering | `raritySymbolBatchRefV42` keccak input encoding | mitigate | Use ethers `AbiCoder.defaultAbiCoder().encode(['uint256','uint256','uint32'], ...)` — matches Solidity `abi.encode` byte layout exactly (verified at v41 helper via Phase 282 W2 invariant pass). Verification command above re-runs the helper against a known `(baseKey, entropy, count)` triple. |
| T-291-02 | Information disclosure / numeric overflow | `decodeOwedFromBaseKey` BigInt mask | mitigate | Mask with literal `0xFFFFFFFFn` (32-bit) before `Number(...)` cast — `Number.MAX_SAFE_INTEGER = 2^53-1` so a 32-bit value never loses precision. Verification command #2 exercises `decodeOwedFromBaseKey(bk | 8000n) === 8000` round-trip. |
| T-291-03 | Tampering | accidental in-place mutation of `raritySymbolBatchRef` (v41 export) | mitigate | Verification command #3 greps for the v41 export name + asserts it remains a distinct symbol from `raritySymbolBatchRefV42`. Phase 282 fixture import surface unchanged. |

(No `contracts/` mutations in this plan → no on-chain attack surface change. Package-legitimacy gate not applicable — no `npm install` step.)
</threat_model>

<verification>
- `test/helpers/raritySymbolBatchRef.mjs` parses as a valid ES module (`node -e "import(...)"` succeeds).
- Both `raritySymbolBatchRef` (v41) and `raritySymbolBatchRefV42` (v42) exports exist and are functions.
- `decodeOwedFromBaseKey` round-trips a 32-bit owed value embedded in a baseKey via `bk | BigInt(owed)`.
- The v42 reference produces a `count`-length `Uint8Array` for a smoke-test input.
- The v41 export name is still present as a standalone symbol (no in-place rename).
</verification>

<success_criteria>
- TST-MINTCLN-01 oracle prerequisite ready: v42 3-input JS reference + owed-from-baseKey decoder exported from `test/helpers/raritySymbolBatchRef.mjs`.
- Zero modifications to existing v41 export — Phase 282 fixture compatibility preserved structurally.
- Zero `contracts/` files modified (`git diff --name-only contracts/` returns empty for this plan).
- D-291-GAS-01 honored: no gas constant, helper, or log added.
</success_criteria>

<output>
Create `.planning/phases/291-mintcln-regression-fixture-tst-mintcln/291-01-SUMMARY.md` when done. Defer all git commits to Plan 02's USER-APPROVED batched-commit checkpoint (per `feedback_batch_contract_approval.md`). Stage the modified helper file but DO NOT `git commit` from this plan.
</output>
</content>
</invoke>