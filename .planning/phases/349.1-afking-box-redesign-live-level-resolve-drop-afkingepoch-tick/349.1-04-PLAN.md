---
phase: 349.1-afking-box-redesign-live-level-resolve-drop-afkingepoch-tick
plan: 04
type: execute
wave: 4
depends_on: ["349.1-03"]
files_modified:
  - contracts/modules/DegenerusGameAdvanceModule.sol
  - contracts/interfaces/IDegenerusGameModules.sol
  - contracts/DegenerusGame.sol
  - contracts/BurnieCoinflip.sol
  - contracts/ContractAddresses.sol
autonomous: true
requirements: []   # No formal REQ-IDs — must_haves derive from 349.1-DESIGN.md §6 + §7 (P5 cleanup, STAGE caller, interfaces).

must_haves:
  truths:
    - "The AdvanceModule STAGE caller (_runSubscriberStage) takes only processDay and no longer reads LR_INDEX as an epochIndex."
    - "The interface decls match the implementations: processSubscriberStage(uint32, uint256); resolveAfkingBox(player, amount, day, rngWord, activityScore)."
    - "The dead AF_KING-gated batchPurchase + BatchBuy struct are removed from DegenerusGame."
    - "The dead AF_KING entry is removed from BurnieCoinflip.onlyFlipCreditors and the ContractAddresses.AF_KING constant is removed."
  artifacts:
    - path: "contracts/interfaces/IDegenerusGameModules.sol"
      provides: "interface decls matching the new STAGE + resolveAfkingBox signatures"
      contains: "function processSubscriberStage("
  key_links:
    - from: "DegenerusGameAdvanceModule._runSubscriberStage"
      to: "IGameAfkingModule.processSubscriberStage"
      via: "delegatecall encoding (processDay, SUB_STAGE_BATCH) — no epochIndex"
      pattern: "processSubscriberStage.selector"
---

<objective>
Land the CONSUMER + CLEANUP edits that follow the box redesign:

1. **AdvanceModule STAGE caller (§7).** `processSubscriberStage` dropped its
   `epochIndex` param (plan 02). The AdvanceModule's `_runSubscriberStage` and its
   call site must drop the pre-RNG `LR_INDEX` read and pass only `processDay`. The
   STAGE still runs pre-RNG (before `rngGate`) — that ordering is the freeze
   property and is UNCHANGED; only the now-unused epoch index plumbing is removed.

2. **Interfaces (§7).** `IDegenerusGameModules` declares the relocated surface so
   the Game stubs + the AdvanceModule STAGE resolve against a real ABI. Update both
   decls to match the new implementations: `processSubscriberStage(uint32
   processDay, uint256 maxCount)` and `resolveAfkingBox(address player, uint256
   amount, uint32 day, uint256 rngWord, uint16 activityScore)`.

3. **P5 dead-code removal (§6).** The AF_KING-gated `batchPurchase` + the
   `BatchBuy` struct in DegenerusGame have ZERO callers (the standalone AfKing that
   called them was deleted in the held 349 diff). Remove both. Remove the dead
   `AF_KING` entry from `BurnieCoinflip.onlyFlipCreditors` (the relocated afking
   pays its bounty as the GAME, not as AF_KING — `creditFlip` is reached
   in-context). The `ContractAddresses.AF_KING` constant is then unused — remove it
   too.

Purpose: these are the downstream consumers of the signature changes (plans 02/03)
plus the dead-surface sweep the design folds in (§6 P5). The `mintBurnie` rename is
deliberately NOT here — it is plan 05 (atomic across interface + impl + stub, and
it lands with the build gate).

Output: revised AdvanceModule STAGE caller, matching interface decls, and the P5
dead code removed across DegenerusGame / BurnieCoinflip / ContractAddresses.

NOTE — REVISION of a HELD diff. `read_first` each file; anchors are guidance.
Built once at the end (plan 05). Do NOT build here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/349.1-afking-box-redesign-live-level-resolve-drop-afkingepoch-tick/349.1-DESIGN.md
@.planning/phases/349.1-afking-box-redesign-live-level-resolve-drop-afkingepoch-tick/349.1-03-SUMMARY.md

<interfaces>
<!-- Current held-diff shapes this plan revises. Anchors are guidance — verify by name. -->

DegenerusGameAdvanceModule.sol — current STAGE call site (~:300-313) + the
_runSubscriberStage helper (~:748-760):
  // call site:
  if (!subsFullyProcessed) {
    if (_subscribers.length != 0) {
      uint48 epochIndex = uint48(_lrRead(LR_INDEX_SHIFT, LR_INDEX_MASK));  // DROP this read
      _runSubscriberStage(epochIndex, day);                                // → _runSubscriberStage(day)
      ...
  // helper:
  function _runSubscriberStage(uint48 epochIndex, uint32 processDay) private {
    ... delegatecall(processSubscriberStage.selector, epochIndex, processDay, SUB_STAGE_BATCH) ...
  }
  // → function _runSubscriberStage(uint32 processDay) ... encode(processDay, SUB_STAGE_BATCH)

IDegenerusGameModules.sol — current decls to update:
  function resolveAfkingBox(address player, uint48 index, uint256 amount,
      uint32 day, uint24 baseLevel, uint16 activityScore) external;          // ~:359-366
  function processSubscriberStage(uint48 epochIndex, uint32 processDay,
      uint256 maxCount) external returns (uint256 processed);                // ~:505-509

DegenerusGame.sol — DEAD code to remove:
  struct BatchBuy { address funder; address player; uint256 ethValue;
      uint256 amount; bool isTicket; uint8 mode; }                           // ~:1886-1893
  function batchPurchase(BatchBuy[] calldata buys) external { ... }          // ~:1921-1955 (gated `msg.sender != ContractAddresses.AF_KING`)

BurnieCoinflip.sol — onlyFlipCreditors modifier (~:194-205): remove the
  `sender != ContractAddresses.AF_KING &&` line (and update the doc-comment ~:191-193).

ContractAddresses.sol — `address internal constant AF_KING = ...;` (~:57): remove
  once no contract references it (grep-confirm zero refs after the above).

KEEP (do NOT remove): afkingFunding / afkingFundingOf / depositAfkingFunding /
withdrawAfkingFunding / afkingSnapshot in DegenerusGame — these are the live
funding ledger, still used. Only batchPurchase + BatchBuy are dead.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop epochIndex from the AdvanceModule STAGE caller + the LR_INDEX read</name>
  <files>contracts/modules/DegenerusGameAdvanceModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameAdvanceModule.sol (the afking process STAGE call-site block ~:283-319 incl. the FREEZE-02b epoch comment, and the _runSubscriberStage helper ~:736-760)
    - .planning/phases/349.1-afking-box-redesign-live-level-resolve-drop-afkingepoch-tick/349.1-DESIGN.md §1/§2 (no epoch; the STAGE still runs pre-RNG — that is the freeze property, unchanged)
  </read_first>
  <action>
    In the STAGE call-site block: delete the
    `uint48 epochIndex = uint48(_lrRead(LR_INDEX_SHIFT, LR_INDEX_MASK));` read and
    change the call to `_runSubscriberStage(day);` (pass only the process day).
    Trim the `FREEZE-02b uniform epoch` comment that explains reading LR_INDEX once
    for the epoch — replace it with a note that the STAGE still runs pre-RNG
    (strictly before `rngGate`), which keeps `rngWordByDay[processDay]` uncommitted
    at stamp (the freeze property, §2); there is no longer a stamped index.
    Keep the partial-drain / `subsFullyProcessed` / `_subCursor < _subscribers.length`
    chunk logic UNCHANGED (it is orthogonal to the epoch removal). Do NOT change
    where the STAGE is inserted relative to rngGate.
    In `_runSubscriberStage`: change the signature to
    `_runSubscriberStage(uint32 processDay) private` (drop `epochIndex`), and update
    the delegatecall encoding to
    `abi.encodeWithSelector(IGameAfkingModule.processSubscriberStage.selector,
    processDay, SUB_STAGE_BATCH)` (drop the `epochIndex` arg). Update the helper's
    doc-comment to strike the "writes the per-day _afkingEpoch (the uniform index +
    baseLevelPlus1)" sentence — the callee no longer writes an epoch; it stamps the
    two per-sub fields (or queues tickets) + the lastAutoBoughtDay marker.
    Keep the `_revertDelegate(data)` failure tail.
  </action>
  <verify>
    <automated>grep -c "epochIndex\|_afkingEpoch" contracts/modules/DegenerusGameAdvanceModule.sol</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "epochIndex\|_afkingEpoch" contracts/modules/DegenerusGameAdvanceModule.sol` == 0.
    - `_runSubscriberStage` signature is `(uint32 processDay)` and the delegatecall encodes `(processDay, SUB_STAGE_BATCH)`.
    - The call site reads no `LR_INDEX` for an epoch and calls `_runSubscriberStage(day)`.
    - The `subsFullyProcessed` partial-drain chunk logic and the STAGE's pre-rngGate insertion point are unchanged.
  </acceptance_criteria>
  <done>The STAGE caller passes only processDay; the LR_INDEX epoch read is gone; the pre-RNG ordering (freeze property) is intact.</done>
</task>

<task type="auto">
  <name>Task 2: Update the IDegenerusGameModules decls to match the new signatures</name>
  <files>contracts/interfaces/IDegenerusGameModules.sol</files>
  <read_first>
    - contracts/interfaces/IDegenerusGameModules.sol (resolveAfkingBox decl ~:349-366 + processSubscriberStage decl ~:497-509, with their doc-comments)
    - contracts/modules/DegenerusGameLootboxModule.sol (the NEW resolveAfkingBox signature from plan 03) and contracts/modules/GameAfkingModule.sol (the NEW processSubscriberStage signature from plan 02) — the decls must match these exactly
  </read_first>
  <action>
    Update `resolveAfkingBox` in the interface to
    `function resolveAfkingBox(address player, uint256 amount, uint32 day,
    uint256 rngWord, uint16 activityScore) external;` — drop `index` and
    `baseLevel`, add `rngWord`. Update its doc-comment to describe the
    caller-passed day-word + frozen stamped day (no index, live level).
    Update `processSubscriberStage` to
    `function processSubscriberStage(uint32 processDay, uint256 maxCount) external
    returns (uint256 processed);` — drop `epochIndex`. Update its doc-comment to
    strike the "epochIndex read once at pass start" / "D-348-07 5-field box stamp"
    references — the callee stamps the per-sub box fields (lootbox mode) or queues
    tickets (ticket mode), debits afkingFunding, advances _subCursor; no epoch.
    Verify the param order/types byte-match the implementations from plans 02/03
    (a mismatch would break the selector resolution at the delegatecall).
  </action>
  <verify>
    <automated>grep -c "function resolveAfkingBox(\|function processSubscriberStage(" contracts/interfaces/IDegenerusGameModules.sol</automated>
  </verify>
  <acceptance_criteria>
    - The interface `resolveAfkingBox` decl is `(address player, uint256 amount, uint32 day, uint256 rngWord, uint16 activityScore)` — no `index`, no `baseLevel`.
    - The interface `processSubscriberStage` decl is `(uint32 processDay, uint256 maxCount)` — no `epochIndex`.
    - Both decls byte-match the implementation signatures (resolveAfkingBox in LootboxModule, processSubscriberStage in GameAfkingModule).
  </acceptance_criteria>
  <done>The interface decls match the new STAGE + resolveAfkingBox implementation signatures.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the dead batchPurchase + BatchBuy + AF_KING (P5 cleanup)</name>
  <files>contracts/DegenerusGame.sol, contracts/BurnieCoinflip.sol, contracts/ContractAddresses.sol</files>
  <read_first>
    - contracts/DegenerusGame.sol (the BatchBuy struct ~:1878-1893 + the batchPurchase function ~:1895-1955, incl. the `if (msg.sender != ContractAddresses.AF_KING) revert E();` gate)
    - contracts/BurnieCoinflip.sol (onlyFlipCreditors ~:190-205 + its doc-comment)
    - contracts/ContractAddresses.sol (the AF_KING constant ~:57)
    - .planning/phases/349.1-afking-box-redesign-live-level-resolve-drop-afkingepoch-tick/349.1-DESIGN.md §6 (P5 dead-code: remove batchPurchase + BatchBuy [zero callers] + the dead AF_KING entry in onlyFlipCreditors → ContractAddresses.AF_KING then unused)
  </read_first>
  <action>
    In DegenerusGame.sol: delete the `BatchBuy` struct and the entire
    `batchPurchase(BatchBuy[] calldata buys)` function (both dead — the only caller
    was the deleted standalone AfKing). Do NOT remove afkingFunding /
    afkingFundingOf / depositAfkingFunding / withdrawAfkingFunding / afkingSnapshot
    (the live funding ledger).
    In BurnieCoinflip.sol: in the `onlyFlipCreditors` modifier, remove the
    `sender != ContractAddresses.AF_KING &&` clause; update the modifier's
    doc-comment to drop "AF_KING (afking sweep bounty…)" from the allowed-callers
    list (the relocated afking pays via the GAME caller now).
    In ContractAddresses.sol: AFTER confirming via grep that no contract references
    `ContractAddresses.AF_KING` anymore, remove the `AF_KING` constant declaration.
    (Tests may still reference AF_KING — that is the 351 test sweep's charge; do
    NOT let a test reference block removing the contract-side constant. Only gate
    the removal on `grep -rn "AF_KING" contracts/` returning zero, NOT on test/.)
  </action>
  <verify>
    <automated>grep -rc "batchPurchase\|struct BatchBuy\|AF_KING" contracts/ | grep -v ":0" || echo "ALL_CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "batchPurchase\|struct BatchBuy" contracts/` returns ZERO matches.
    - `grep -rn "AF_KING" contracts/` returns ZERO matches (the constant + the onlyFlipCreditors clause both gone).
    - DegenerusGame still has `afkingFunding`, `afkingFundingOf`, `depositAfkingFunding`, `withdrawAfkingFunding`, `afkingSnapshot` (the live ledger, untouched).
    - BurnieCoinflip onlyFlipCreditors still allows GAME / QUESTS / AFFILIATE / ADMIN / SDGNRS.
  </acceptance_criteria>
  <done>batchPurchase + BatchBuy + the AF_KING constant + the onlyFlipCreditors AF_KING clause are all removed; the live afkingFunding ledger is untouched.</done>
</task>

</tasks>

<verification>
- `grep -rn "epochIndex\|_afkingEpoch\|batchPurchase\|struct BatchBuy\|AF_KING" contracts/` returns ZERO matches across ALL of contracts/.
- The interface decls (processSubscriberStage, resolveAfkingBox) byte-match the implementations.
- The AdvanceModule STAGE caller passes only processDay; the pre-rngGate ordering is intact.
- NOTE: the project still will NOT `forge build` cleanly until the `mintBurnie` rename lands (plan 05 keeps `doWork` consistent across interface + impl + stub). Built once at the end (plan 05). Do NOT build here.
</verification>

<success_criteria>
- The AdvanceModule STAGE caller drops epochIndex + the LR_INDEX read; freeze ordering unchanged.
- The interface decls match the new STAGE + resolveAfkingBox signatures.
- The P5 dead code (batchPurchase, BatchBuy, AF_KING constant + onlyFlipCreditors clause) is removed; the live ledger survives.
</success_criteria>

<output>
Create `.planning/phases/349.1-afking-box-redesign-live-level-resolve-drop-afkingepoch-tick/349.1-04-SUMMARY.md` when done.
</output>
