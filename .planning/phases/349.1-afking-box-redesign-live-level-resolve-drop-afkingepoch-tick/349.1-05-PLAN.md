---
phase: 349.1-afking-box-redesign-live-level-resolve-drop-afkingepoch-tick
plan: 05
type: execute
wave: 5
depends_on: ["349.1-04"]
files_modified:
  - contracts/interfaces/IDegenerusGameModules.sol
  - contracts/modules/GameAfkingModule.sol
  - contracts/DegenerusGame.sol
  - contracts/modules/DegenerusGameAdvanceModule.sol
autonomous: false   # USER CONTRACT-COMMIT GATE — the revised diff is applied + forge-build-clean, then HELD for explicit USER hand-review. Only the contract COMMIT needs approval. Do NOT commit.
requirements: []    # No formal REQ-IDs — must_haves derive from 349.1-DESIGN.md §6 (mintBurnie rename) + §7 (forge build < 24,576, HOLD).

must_haves:
  truths:
    - "The permissionless router is named mintBurnie() across the interface decl, the GameAfkingModule impl, and the DegenerusGame dispatch stub (selector encoding included) — no `doWork` token remains anywhere in contracts/ (incl. comments)."
    - "`forge build` (contracts-only) is clean: exit 0, no errors."
    - "DegenerusGame runtime bytecode is < 24,576 bytes."
    - "The revised single batched contracts/*.sol diff is applied + locally compiling, HELD at the contract-commit gate (uncommitted)."
  artifacts:
    - path: "contracts/modules/GameAfkingModule.sol"
      provides: "the renamed router entrypoint mintBurnie()"
      contains: "function mintBurnie()"
  key_links:
    - from: "DegenerusGame.mintBurnie (stub)"
      to: "IGameAfkingModule.mintBurnie (module)"
      via: "delegatecall encoding IGameAfkingModule.mintBurnie.selector"
      pattern: "IGameAfkingModule.mintBurnie.selector"
---

<objective>
Final landing wave of the 349.1 batched contract diff: the `mintBurnie` rename and
the build/size gate + the contract-commit HOLD.

1. **mintBurnie rename (§6).** Rename the permissionless router entrypoint from
   `doWork()` to `mintBurnie()` — it names the REWARD (the minted BURNIE
   flip-credit), not the action (the router still runs advance → afking-box-open
   internally). This is ATOMIC across the three CODE sites in three files (the
   interface decl, the module impl, and the Game dispatch stub incl. the
   `IGameAfkingModule.doWork.selector` → `.mintBurnie.selector` encoding) — they
   must all change together or the selector resolution breaks — PLUS one stale
   `doWork` mention in an AdvanceModule comment (`:196`) so the
   `grep "doWork" contracts/` == 0 invariant holds. The rename is LAST in the diff
   because it has the widest blast radius (and tests reference it too — ~156
   occurrences across test/ — but the test sweep is Phase 351; this plan does NOT
   touch test/ and does NOT require the suite green).

2. **Build + size gate (§7).** Verify `forge build` (CONTRACTS-ONLY — the held tree
   still has tests importing the deleted `contracts/AfKing.sol`, a 351 concern) is
   clean, and that `DegenerusGame` runtime bytecode stays < 24,576 bytes. Dropping
   `_afkingEpoch` + pack/unpack + the P5 dead code REDUCES size vs the held-349
   baseline (23,499 B / 1,077 margin), so this is a net win — the ceiling stays
   satisfied.

3. **HOLD at the contract-commit gate.** The revised single batched
   `contracts/*.sol` diff is now fully applied and locally compiling. HOLD it for
   explicit USER hand-review. **Only the contract COMMIT needs approval** — do NOT
   plan or perform a commit. `autonomous: false` marks the USER contract gate.

Purpose: close the diff with the wide-blast-radius rename and prove it compiles +
fits the ceiling, then hand the whole reconciled diff to the USER.

Output: the renamed router + a clean contracts-only build under the ceiling + the
held diff awaiting USER review.

NOTE — REVISION of a HELD diff. `read_first` each file; anchors are guidance. This
is the LAST plan — after it, the whole 349.1 diff is applied and HELD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/349.1-afking-box-redesign-live-level-resolve-drop-afkingepoch-tick/349.1-DESIGN.md
@.planning/phases/349.1-afking-box-redesign-live-level-resolve-drop-afkingepoch-tick/349.1-04-SUMMARY.md

<interfaces>
<!-- The `doWork` rename sites. Anchors are guidance — verify by name. The
     human-box autoOpen(uint256) in DegenerusGame (~:1807) and the AfKing stubs
     subscribe/autoBuy are NOT renamed — only doWork → mintBurnie. -->

contracts/interfaces/IDegenerusGameModules.sol (~:486-488):
  /// @notice Unified permissionless router ...
  function doWork() external;                          // → function mintBurnie() external;

contracts/modules/GameAfkingModule.sol (~:927):
  function doWork() external { ... }                   // → function mintBurnie() external { ... }
  // body uses IGameRouter(address(this)).advanceDue()/advanceGame() + _autoOpen + creditFlip — UNCHANGED.
  // The NoWork() error + the doc-comments referencing "doWork" → rename the prose to mintBurnie.

contracts/DegenerusGame.sol (~:391-398) — the dispatch stub:
  function doWork() external {                         // → function mintBurnie() external {
    ... delegatecall(abi.encodeWithSelector(IGameAfkingModule.doWork.selector)) ...
    //                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ → IGameAfkingModule.mintBurnie.selector
  }
  // Also the AFKING DISPATCH STUBS comment block (~:342-358) mentions "doWork" — rename prose.
  // Also the human-box autoOpen doc-comment (~:1795) says "AfKing.doWork" — update prose to mintBurnie.

contracts/modules/DegenerusGameAdvanceModule.sol (~:196) — a COMMENT-ONLY mention:
  // ... return mult = 0 so doWork pays nothing.    → "so mintBurnie pays nothing."
  // (No code here — just the stale name in the gameover-mult comment. Needed so
  //  `grep "doWork" contracts/` == 0.)

GATE COMMAND (contracts-only — the held tree's tests import the deleted AfKing.sol):
  forge build --sizes --skip 'test/**' --skip '*.t.sol'
Held-349 baseline before this phase: DegenerusGame 23,499 B (margin 1,077). After
349.1 (drops _afkingEpoch/pack/unpack + P5 dead code) it should be <= that.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename doWork → mintBurnie across interface + module + Game stub + the AdvanceModule comment</name>
  <files>contracts/interfaces/IDegenerusGameModules.sol, contracts/modules/GameAfkingModule.sol, contracts/DegenerusGame.sol, contracts/modules/DegenerusGameAdvanceModule.sol</files>
  <read_first>
    - contracts/interfaces/IDegenerusGameModules.sol (the IGameAfkingModule.doWork decl ~:486-488)
    - contracts/modules/GameAfkingModule.sol (the doWork router impl ~:910-959, incl. its doc-comment + the NoWork() usage + the contract-header @dev refs ~:57-64/:95/:186/:919)
    - contracts/DegenerusGame.sol (the doWork dispatch stub ~:388-398 + the AFKING DISPATCH STUBS comment block ~:342-358 + the human-box autoOpen doc-comment ~:1795 mentioning "AfKing.doWork")
    - contracts/modules/DegenerusGameAdvanceModule.sol (the gameover-mult comment ~:196 "so doWork pays nothing")
    - .planning/phases/349.1-afking-box-redesign-live-level-resolve-drop-afkingepoch-tick/349.1-DESIGN.md §6 (mintBurnie rename: doWork → mintBurnie; cosmetic; names the reward, not the action; router still runs autoBuy→advance→autoOpen)
  </read_first>
  <action>
    Rename `doWork` → `mintBurnie` everywhere in contracts/ — the three CODE sites
    atomically plus the one stale comment:
    - **Interface** (IDegenerusGameModules.sol): `function doWork() external;` →
      `function mintBurnie() external;`. Update the doc-comment prose.
    - **Module** (GameAfkingModule.sol): `function doWork() external { ... }` →
      `function mintBurnie() external { ... }`. The body is UNCHANGED (advanceDue /
      advanceGame / _autoOpen / creditFlip / NoWork). Update ALL doc-comments that
      say "doWork" → "mintBurnie" (the `@notice`, the `@dev` text, and the
      contract-header `@dev PART B` / router/PLACE-02 descriptions referencing
      doWork — search the whole file for "doWork").
    - **Game stub** (DegenerusGame.sol): `function doWork() external { ... }` →
      `function mintBurnie() external { ... }`, AND change the encoded selector
      `IGameAfkingModule.doWork.selector` → `IGameAfkingModule.mintBurnie.selector`
      inside the delegatecall. Update the AFKING DISPATCH STUBS comment block prose
      and the human-box `autoOpen` doc-comment "AfKing.doWork" → "mintBurnie".
    - **AdvanceModule comment** (DegenerusGameAdvanceModule.sol ~:196): change the
      comment "so doWork pays nothing." → "so mintBurnie pays nothing." (comment
      only — no code change).
    Do NOT rename `subscribe`, `autoBuy`, or the human-box `autoOpen(uint256)` —
    only `doWork` → `mintBurnie`. Do NOT touch any test/ file (the ~156 test
    references are the Phase 351 test sweep; renaming them now is out of scope and
    the suite is not required green this phase).
  </action>
  <verify>
    <automated>grep -rc "doWork" contracts/ | grep -v ":0" || echo "NO_DOWORK_IN_CONTRACTS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "doWork" contracts/` returns ZERO matches (code AND comments).
    - `grep -rn "function mintBurnie()" contracts/` returns exactly 3 matches (interface decl, module impl, Game stub).
    - The Game stub encodes `IGameAfkingModule.mintBurnie.selector` (not `.doWork.selector`).
    - `subscribe` / `autoBuy` / the human-box `autoOpen(uint256)` are unchanged.
  </acceptance_criteria>
  <done>The router is mintBurnie() across the interface, the module, and the Game stub (selector included), and the AdvanceModule comment is updated; no `doWork` token remains in contracts/; subscribe/autoBuy/human-autoOpen untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Contracts-only forge build clean + DegenerusGame < 24,576 — gate the HOLD</name>
  <files>contracts/ (build verification only — no edits)</files>
  <read_first>
    - .planning/phases/349.1-afking-box-redesign-live-level-resolve-drop-afkingepoch-tick/349.1-DESIGN.md §7 (forge build --sizes; DegenerusGame < 24,576; HOLD at the contract-commit gate; forge test is 351's charge — do NOT require the suite green)
    - .planning/phases/349.1-afking-box-redesign-live-level-resolve-drop-afkingepoch-tick/349.1-04-SUMMARY.md (the prior wave's final state)
  </read_first>
  <action>
    Run the CONTRACTS-ONLY build (the held tree's tests still import the deleted
    `contracts/AfKing.sol`, which is a Phase 351 concern — a full `forge build`
    that includes test/ will fail on those stale imports, expectedly):
    `forge build --sizes --skip 'test/**' --skip '*.t.sol'`.
    Confirm: exit code 0, no compiler ERRORS (lint warnings like unsafe-typecast /
    divide-before-multiply are pre-existing and acceptable), and that the
    `DegenerusGame` runtime bytecode size printed in the --sizes table is
    < 24,576 bytes. The size should be <= the held-349 baseline of 23,499 B since
    this phase deletes storage (_afkingEpoch + pack/unpack) and dead code (P5).
    If the build fails or DegenerusGame >= 24,576, STOP and report the error +
    the offending size; do NOT attempt to commit. (Do NOT run the full test suite —
    stale `_afkingEpoch` / ABI / AfKing.sol-import test reds are expected until
    Phase 351; the suite being red here is NOT a failure of this phase.)
  </action>
  <verify>
    <automated>forge build --sizes --skip 'test/**' --skip '*.t.sol' 2>&1 | grep -E "DegenerusGame " | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `forge build --sizes --skip 'test/**' --skip '*.t.sol'` exits 0 with no compiler errors.
    - The --sizes table line for `DegenerusGame` shows a runtime size < 24,576 (and <= 23,499).
    - The GameAfkingModule, DegenerusGameLootboxModule, and DegenerusGameAdvanceModule all compile (present in the --sizes table).
  </acceptance_criteria>
  <done>The contracts-only build is clean and DegenerusGame is under the 24,576-byte ceiling; the diff is ready for USER review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: USER hand-review of the revised batched 349.1 contracts diff (contract-commit gate)</name>
  <files>contracts/ (the full revised batched diff — review only, no edits)</files>
  <what-built>
    The revised single batched 349.1 `contracts/*.sol` diff (superseding-in-place
    the held 349 diff) is fully applied to the working tree and locally compiling
    (contracts-only `forge build` clean; DegenerusGame < 24,576). It delivers:
    - Storage: Sub.lastOpenedIndex → lastOpenedDay; `_afkingEpoch` + pack/unpack +
      shift constant deleted.
    - GameAfkingModule: the NO-ORPHAN guard FIRST in the process STAGE (dominates
      re-stamp / cancel-reclaim / evict / funding-kill); the epoch write +
      epochIndex param dropped; the P2 ticket/lootbox split (ticket subs queue
      whole tickets via purchaseWith, lootbox subs stamp a box); the open-leg
      (`_afkingBoxReady` / `_openAfkingBox`) keyed on `rngWordByDay[lastAutoBoughtDay]`
      + the day-keyed `lastOpenedDay` marker.
    - LootboxModule: `resolveAfkingBox` resolving at the LIVE level
      (`_rollTargetLevel(level+1, seed)`) with a caller-passed day-word + the FROZEN
      stamped day in the seed (no index, no stored baseLevel).
    - AdvanceModule STAGE caller + interfaces updated to the new signatures.
    - P5 dead-code removed (batchPurchase + BatchBuy + the AF_KING constant +
      the onlyFlipCreditors AF_KING clause).
    - `doWork` → `mintBurnie` rename across interface + module + Game stub
      (+ the AdvanceModule comment).
    NOTHING is committed.
  </what-built>
  <how-to-verify>
    1. Review the full diff: `git diff -- contracts/` (the held 349 diff further
       revised into its final 349.1 form). Pay attention to the freeze-critical
       core in `resolveAfkingBox` (live level + frozen-day seed +
       `rngWordByDay[day]` word) and the NO-ORPHAN guard placement (FIRST in the
       STAGE loop, before all four orphan paths).
    2. Confirm the contracts-only build: `forge build --sizes --skip 'test/**'
       --skip '*.t.sol'` — exit 0, DegenerusGame < 24,576.
    3. (Optional) cross-check the design intent in
       `349.1-DESIGN.md` §1/§2/§3/§5/§6 against the diff.
    Note: a full `forge build`/`forge test` (incl. test/) will FAIL on stale
    `AfKing.sol` imports + `_afkingEpoch`/ABI test reds — that is EXPECTED and is
    Phase 351's charge, NOT a defect of this diff.
  </how-to-verify>
  <verify>
    <automated>forge build --skip 'test/**' --skip '*.t.sol' 2>&1 | grep -ciE "^error" | grep -qx 0 && echo "BUILD_CLEAN"</automated>
  </verify>
  <done>The USER has hand-reviewed the full revised 349.1 contracts diff; the diff is HELD (uncommitted), ready to commit only on explicit USER instruction.</done>
  <resume-signal>
    Type "approved" to record the diff as USER-reviewed and ready to commit (the
    commit itself is performed only on explicit USER instruction — this plan does
    NOT commit). Or describe required changes for a revision pass.
  </resume-signal>
</task>

</tasks>

<verification>
- `grep -rn "doWork" contracts/` returns ZERO matches; `grep -rn "function mintBurnie()" contracts/` returns 3.
- `forge build --sizes --skip 'test/**' --skip '*.t.sol'` exits 0; DegenerusGame < 24,576.
- The full 349.1 contracts diff is applied + HELD (uncommitted) at the contract-commit gate for USER hand-review.
- `forge test` is NOT required green (stale _afkingEpoch / AfKing.sol-import test reds expected until Phase 351).
</verification>

<success_criteria>
- The router is renamed mintBurnie() atomically across the interface, the module, and the Game stub (selector included), and the stale AdvanceModule comment is updated.
- The contracts-only build is clean and DegenerusGame is under the byte ceiling.
- The revised single batched contracts/*.sol diff is HELD for explicit USER review; nothing is committed.
</success_criteria>

<output>
Create `.planning/phases/349.1-afking-box-redesign-live-level-resolve-drop-afkingepoch-tick/349.1-05-SUMMARY.md` when done.
</output>
