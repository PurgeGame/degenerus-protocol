---
phase: 313-tst-vrf-regression-freeze-invariant-fuzz-under-rotation-tst
plan: 06
type: execute
wave: 2
depends_on: ['313-01', '313-02', '313-03', '313-04', '313-05']
files_modified: []
autonomous: true
requirements: [VTST-01, VTST-02, VTST-03, VTST-04]
tags: [solidity, foundry, vrf-rotation, suite-verification, regression, agent-commit, degenerus]

must_haves:
  truths:
    - "forge build exits 0; the four new VTST test contracts (VrfRotationOrphanIndex, VrfRotationLiveness, RngLockRotationDeterminism, VrfWireOneShot) ALL PASS"
    - "The v43 canonical freeze-invariant harness test/fuzz/RngLockDeterminism.t.sol still PASSES (it was NOT modified by this phase) — confirmed by running it and by git showing it byte-identical to the pre-phase baseline"
    - "The full Foundry suite has NO NEW failures introduced by the Phase 312 fix or by this phase: the post-313 failure set equals the pre-fix-baseline (41546f16) failure set (the 17 fix-induced regressions are now migrated/passing; only pre-existing baseline debt remains)"
    - "The test bundle is AGENT-COMMITTED per D-43N-TEST-COMMITS-AUTO-01 (no mainnet contract change, so no contract-commit approval needed); git diff over contracts/ is empty across the whole phase"
  artifacts: []
  key_links:
    - from: "the four new VTST test files + the migrated regression files"
      to: "an AGENT-COMMITTED test commit"
      via: "git add test/ + git commit (no contracts/ change; D-43N-TEST-COMMITS-AUTO-01)"
      pattern: "test"
---

<objective>
Suite-wide verification + AGENT-COMMITTED test bundle for Phase 313. ZERO contracts/ mutation across the entire phase (audit-only per D-43N-AUDIT-ONLY-01).

This plan runs LAST (Wave 2, after 313-01..05). It proves the four ROADMAP success criteria are jointly satisfied and SC-5 holds, then commits the test bundle autonomously (test-tree only, no mainnet contract change — D-43N-TEST-COMMITS-AUTO-01; no contract-commit approval needed; the PreToolUse contract-commit guard does not fire because contracts/ is clean).

SC-5 interpretation (LOCKED): "full suite PASS" means the suite is restored to its PRE-FIX baseline pass/fail set — i.e. the Phase 312 fix and this phase introduce NO NEW failures. A large set of tests fail on BOTH the pre-fix parent `41546f16` AND HEAD (pre-existing suite debt: affiliate E(), solvency violations, arithmetic panics, InvalidBet degenerette, testFreezeUnfreezeRoundTrip, index-increment-0 tests) — these are explicitly OUT OF SCOPE (documented in plan 313-05). The bar is: the 17 fix-induced regressions are migrated/passing, the four new VTST contracts pass, and the v43 RngLockDeterminism harness still passes.

Purpose: confirm VRF-01..05 are proven by VTST-01..04, the v43 freeze harness survives, and no fix-induced regression remains. Output: a passing verification + one AGENT-COMMITTED test commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/313-tst-vrf-regression-freeze-invariant-fuzz-under-rotation-tst/313-01-PLAN.md
@.planning/phases/313-tst-vrf-regression-freeze-invariant-fuzz-under-rotation-tst/313-02-PLAN.md
@.planning/phases/313-tst-vrf-regression-freeze-invariant-fuzz-under-rotation-tst/313-03-PLAN.md
@.planning/phases/313-tst-vrf-regression-freeze-invariant-fuzz-under-rotation-tst/313-04-PLAN.md
@.planning/phases/313-tst-vrf-regression-freeze-invariant-fuzz-under-rotation-tst/313-05-PLAN.md

<interfaces>
<!-- The pre-fix baseline classification (run during planning; the OUT-OF-SCOPE pre-existing failures). -->

Pre-existing baseline (41546f16) failures — fail on BOTH pre-fix and post-fix, NOT this phase's concern:
  affiliate: test_orderIndependence, test_revertDoubleClaim, test_claimedTrackingAccumulates, test_proportionalDistribution, test_threeAffiliatesProportional, test_totalClaimsLeAllocation, test_totalClaimsMatchPoolDelta, test_claimWindowMovesWithLevel (E());
  solvency: Degenerette/ETH/MultiLevel/VaultShareMath solvency-violation tests;
  arithmetic panics: testMidDaySwapAtThreshold, test_wordWriteBackfill, testBoundaryLevel5/6, testQueue*UsesWriteKey, test_zeroGuardBackfill, etc.;
  InvalidBet: testDegeneretteFreezeResolution*, testDegeneretteUnfrozenPathRegression;
  misc: testFreezeUnfreezeRoundTrip, test_indexIncrementsOnFreshDaily, test_indexIncrementsOnMidDay, test_wordWriteDaily, test_wordWriteMidDay, test_fullLifecycle*, test_indexLifecycleAcrossStall_fuzz, test_retryDetection_fresh, testBindingConsistencyDailyDrain.

The 17 fix-induced regressions (migrated by 313-05) MUST be PASS post-phase:
  VRFStallEdgeCases: test_coordinatorSwapResetsAllVrfState, test_tryRequestRngGuardBranches, test_coordinatorSwapClearsMidDayPending, test_zeroSeedAtGameStart, test_zeroSeedUnreachableAfterSwap, test_manipulationWindowIdenticalToDaily, test_gapDayPositionsPreCommitted, test_gapDaysSkipResolveRedemptionPeriod, test_gapBackfillSingleDayGap, test_gapBackfillZeroGuard, test_gapBackfillGas30Days, test_gapBackfillGas120Days;
  VRFCore: test_coordinatorSwap_clearsRngLocked;
  StallResilience: test_stallSwapResume, test_coinflipClaimsAcrossGapDays, test_lootboxOpenAfterOrphanedIndexBackfill;
  VRFPathCoverage: test_gapBackfillEntropyUnique_fuzz.

The four NEW VTST contracts: VrfRotationOrphanIndex (313-01), VrfRotationLiveness (313-02), RngLockRotationDeterminism (313-03), VrfWireOneShot (313-04).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the four new VTST contracts + the v43 harness + the 17 migrated regressions all PASS, contracts/ clean</name>
  <files></files>
  <read_first>
    - .planning/phases/313-tst-vrf-regression-freeze-invariant-fuzz-under-rotation-tst/313-01-PLAN.md through 313-05-PLAN.md — confirm the artifact names + the migrated-test list.
    - test/fuzz/RngLockDeterminism.t.sol — confirm it is the v43 harness and that it must NOT have been modified by this phase.
  </read_first>
  <action>
    Run the verification battery (no code edits — verification only). If any check fails, STOP and surface it (do NOT commit a failing suite).

    1. forge build — assert exit 0, no new error lines (pre-existing unsafe-typecast lint notes acceptable).
    2. Run the four new VTST contracts and assert all PASS: `forge test --match-contract VrfRotationOrphanIndex`, `forge test --match-contract VrfRotationLiveness`, `forge test --match-contract RngLockRotationDeterminism`, `forge test --match-contract VrfWireOneShot` (the fuzz contracts must show non-zero runs).
    3. Run the v43 harness `forge test --match-contract RngLockDeterminism` and assert it still PASSES (the live non-vm.skip tests pass; the vm.skip count is unchanged). Confirm via `git diff --name-only` that test/fuzz/RngLockDeterminism.t.sol is NOT listed (byte-identical to the pre-phase baseline).
    4. Run the 17 migrated fix-induced regressions and assert all PASS (the VRFStallEdgeCases / VRFCore / StallResilience / VRFPathCoverage tests listed in the interfaces block).
    5. Assert `git diff --stat` over contracts/ is empty (ZERO mainnet contract mutation across the whole phase).
    6. Run the full Foundry suite once and confirm NO NEW failures beyond the pre-fix baseline set (the interfaces block lists the allowed pre-existing failures). The post-313 failing-test set MUST be a subset of the pre-fix baseline failing set — no new test name fails that passed at 41546f16. If a new failure appears that is NOT in the documented baseline, STOP and surface it.

    Record the exact pass/fail counts and the confirmation that contracts/ is clean for the SUMMARY.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -2; forge test --match-contract VrfRotationOrphanIndex 2>&1 | tail -4; forge test --match-contract VrfRotationLiveness 2>&1 | tail -4; forge test --match-contract RngLockRotationDeterminism 2>&1 | tail -4; forge test --match-contract VrfWireOneShot 2>&1 | tail -4; forge test --match-contract RngLockDeterminism 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - forge build exits 0.
    - All four new VTST contracts PASS (the two fuzz contracts with non-zero runs).
    - The v43 RngLockDeterminism harness PASSES and its file is byte-identical to the pre-phase baseline (not listed in git diff --name-only).
    - All 17 migrated fix-induced regressions PASS.
    - git diff over contracts/ is empty (ZERO mainnet contract mutation).
    - The full-suite post-313 failing set is a subset of the documented pre-fix baseline failing set (no new failure).
  </acceptance_criteria>
  <done>Build green; the four VTST contracts, the v43 harness, and the 17 migrated regressions all pass; contracts/ is clean; no new suite failures beyond the pre-fix baseline.</done>
</task>

<task type="auto">
  <name>Task 2: AGENT-COMMIT the Phase 313 test bundle (test-tree only, no contract change)</name>
  <files></files>
  <read_first>
    - .planning/phases/313-tst-vrf-regression-freeze-invariant-fuzz-under-rotation-tst/ — confirm the SUMMARY files for 313-01..05 exist (each plan writes its own).
    - The memory note feedback_no_contract_commits + feedback_contract_commit_guard_hook: ONLY mainnet contracts/*.sol commits are hard-blocked; test/ + .planning/ are free to commit autonomously. Since this phase touches ZERO contracts/ files, no contract-commit approval and no CONTRACTS_COMMIT_APPROVED bypass is needed.
  </read_first>
  <action>
    AGENT-COMMIT the Phase 313 test bundle per D-43N-TEST-COMMITS-AUTO-01. NO contracts/ change exists, so the PreToolUse contract-commit guard does not fire (contracts/ is clean).

    1. Re-confirm `git status --porcelain contracts/` is empty (no mainnet contract change). If it is NOT empty, STOP — that would be a scope violation requiring investigation, not a commit.
    2. Stage the test bundle: the four new test files (test/fuzz/VrfRotationOrphanIndex.t.sol, test/fuzz/VrfRotationLiveness.t.sol, test/fuzz/RngLockRotationDeterminism.t.sol, test/fuzz/VrfWireOneShot.t.sol) + the four migrated regression files (test/fuzz/VRFStallEdgeCases.t.sol, test/fuzz/StallResilience.t.sol, test/fuzz/VRFCore.t.sol, test/fuzz/VRFPathCoverage.t.sol). Also force-add the .planning/ phase docs (.planning/ is gitignored — use git add -f for the 313 SUMMARY/PLAN/STATE/ROADMAP docs per feedback_contract_commit_guard_hook).
    3. Commit with a message like: `test(313): VRF-rotation regression + freeze-invariant fuzz under rotation [VTST-01..04]` summarizing the four new contracts (orphan-index VTST-01, liveness VTST-02, freeze-invariant fuzz VTST-03, wireVrf one-shot VTST-04) + the 17 fix-induced regression migrations. End the commit message with the Co-Authored-By trailer.
    4. Report the commit SHA.

    Do NOT commit any contracts/*.sol change (there is none). Do NOT use CONTRACTS_COMMIT_APPROVED (not needed — no contract change).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && git log --oneline -3; git status --porcelain contracts/</automated>
  </verify>
  <acceptance_criteria>
    - A test commit `test(313): ...` exists at HEAD with the four new test files + the four migrated regression files staged.
    - `git status --porcelain contracts/` is empty before and after the commit (no mainnet contract change committed).
    - The commit message ends with the Co-Authored-By trailer.
    - The commit SHA is recorded in the SUMMARY.
  </acceptance_criteria>
  <done>The Phase 313 test bundle is AGENT-COMMITTED with zero contracts/ change; the commit SHA is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (TEST-INTEGRITY — zero mainnet-contract mutation; no production attack surface)

| Boundary | Description |
|----------|-------------|
| Verification gate to commit | The bundle is committed ONLY if the verification battery passes; a failing suite blocks the commit |
| Test commit vs contract commit | Only test/ + .planning/ are committed; contracts/ MUST be clean (the guard would block otherwise, and a dirty contracts/ here is a scope violation) |

## STRIDE Threat Register (test-integrity)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-313-06-01 | Information Disclosure (false-pass: v43 harness silently modified) | RngLockDeterminism.t.sol integrity | mitigate | Task 1 asserts the v43 harness file is NOT in git diff --name-only (byte-identical) AND that it still PASSES — so a silent edit or a regression in the canonical freeze harness is caught before commit. |
| T-313-06-02 | Information Disclosure (new failure masked as pre-existing) | full-suite subset check | mitigate | Task 1 requires the post-313 failing set to be a SUBSET of the documented pre-fix baseline failing set; any failing test name NOT in the baseline list halts the phase. |
| T-313-06-03 | Tampering (contract change sneaks into the test commit) | git staging | mitigate | Task 2 re-confirms git status --porcelain contracts/ is empty before committing and stages only the named test files + .planning docs; a dirty contracts/ halts the commit (and the PreToolUse guard would block it anyway). |
| T-313-06-SC | Tampering | npm/pip/cargo installs | n/a | NO package installs. Verification + git only. Slopcheck not applicable. |

**No production attack surface introduced — zero contracts/ mutation (D-43N-AUDIT-ONLY-01).**
</threat_model>

<verification>
- forge build exits 0.
- The four new VTST contracts + the v43 RngLockDeterminism harness + the 17 migrated regressions all PASS.
- git diff over contracts/ is empty across the whole phase.
- The full-suite post-313 failing set is a subset of the pre-fix baseline failing set (no NEW failure).
- The Phase 313 test bundle is AGENT-COMMITTED (test-tree only).
</verification>

<success_criteria>
ROADMAP Phase 313 Success Criterion 5: forge build + full suite PASS (restored to the pre-fix baseline — no NEW failures); the v43 RngLockDeterminism.t.sol harness still PASSES; AGENT-COMMITTED test commit(s) per D-43N-TEST-COMMITS-AUTO-01. Jointly with plans 313-01..04, all five ROADMAP success criteria (VTST-01..04 proving VRF-01..05) are satisfied.
</success_criteria>

<output>
Create `.planning/phases/313-tst-vrf-regression-freeze-invariant-fuzz-under-rotation-tst/313-06-SUMMARY.md` when done.
</output>
