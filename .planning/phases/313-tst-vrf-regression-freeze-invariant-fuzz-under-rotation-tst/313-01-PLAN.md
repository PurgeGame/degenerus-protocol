---
phase: 313-tst-vrf-regression-freeze-invariant-fuzz-under-rotation-tst
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fuzz/VrfRotationOrphanIndex.t.sol
autonomous: true
requirements: [VTST-01]
tags: [solidity, foundry, chainlink-vrf, vrf-rotation, rng-lock, orphan-index, degenerus]

must_haves:
  truths:
    - "A single `forge test --match-contract VrfRotationOrphanIndex` invocation PASSES (exit 0), proving VRF-01 by contrast: the pre-fix arm asserts the entropy-0 defect consequence, the post-fix arm asserts a real VRF-derived word lands in lootboxRngWordByIndex[N] after a mid-flight rotation"
    - "The post-fix arm performs a REAL rotation (updateVrfCoordinatorAndSub to a freshly-deployed 2nd MockVRFCoordinator while a mid-day request is in flight) and fulfils the re-issued request on the NEW coordinator — the asserted word equals the VRF word the test delivered to the new coordinator, NOT a value the test wrote into the slot itself (no tautology)"
    - "The pre-fix arm reproduces Scenario A's entropy-0 consequence: with lootboxRngWordByIndex[N]==0 the trait-generation consumer (DegenerusGameMintModule:686) reads entropy==0 — asserted as an observable consequence, not merely re-reading the zeroed slot"
  artifacts:
    - path: "test/fuzz/VrfRotationOrphanIndex.t.sol"
      provides: "VTST-01 orphan-index reproduction: pre-fix entropy-0 consequence arm + post-fix real-VRF-word-in-[N]-after-rotation arm, single forge-test invocation"
      contains: "contract VrfRotationOrphanIndex is DeployProtocol"
  key_links:
    - from: "test post-fix arm"
      to: "lootboxRngWordByIndex[N] (slot 38 mapping) != 0 and == delivered VRF word"
      via: "updateVrfCoordinatorAndSub mid-flight rotation to a 2nd MockVRFCoordinator, then fulfillRandomWords on the new coordinator drives rawFulfillRandomWords:1772 into the preserved LR_INDEX slot N"
      pattern: "updateVrfCoordinatorAndSub\\("
---

<objective>
Author `test/fuzz/VrfRotationOrphanIndex.t.sol` proving **VTST-01** (which proves **VRF-01**): the orphan-index defect is closed. Single `forge test` invocation, in-repo, ZERO contracts/ mutation (test-tree only, audit-only per D-43N-AUDIT-ONLY-01).

The CONTRACT-UNDER-TEST is already patched (Phase 312 `a303ae18` merged). So this test asserts VRF-01 by **contrast within one file** (chosen reproduction strategy — see decision note), not by checking out a pre-fix commit:

- **Pre-fix arm (the entropy-0 consequence):** drive the contract into the orphaned `lootboxRngWordByIndex[N]==0` condition (via `vm.store` to zero the reserved slot) and assert the Scenario-A consequence — the trait-generation consumer at `DegenerusGameMintModule:686` reads `entropy == 0`. This is the defect the fix exists to eliminate; asserting it as a reachable consequence (given a zero slot) anchors the contrast.
- **Post-fix arm (the fix):** perform a REAL mid-flight emergency rotation — fire a mid-day `requestLootboxRng`, then `updateVrfCoordinatorAndSub(newCoordinator,...)` to a freshly-deployed 2nd `MockVRFCoordinator` while the request is in flight, then `fulfillRandomWords` the re-issued request on the NEW coordinator — and assert `lootboxRngWordByIndex[N] != 0` AND `lootboxRngWordByIndex[N] == <the VRF word the test delivered to the new coordinator>` (the re-issue landed a genuine, non-zero, VRF-derived word in the SAME preserved slot N).

Purpose: prove the CATASTROPHE-class Scenario-A entropy-0 path is structurally eliminated after rotation. Output: a passing Foundry test contract.

## DECISION NOTE — VTST-01 pre-fix reproduction strategy (LOCKED here per planning brief)
Strategy (c) is chosen: a single-`forge test`-invocation in-repo contrast — the pre-fix arm asserts the entropy-0 CONSEQUENCE (driving the orphan condition with `vm.store`), the post-fix arm asserts the filled-slot invariant after a real rotation. Rejected: (a) standalone state-injection-only with no real-rotation post-fix arm — the post-fix arm MUST use the real `updateVrfCoordinatorAndSub` path; (b) git pre-fix checkout in a separate forge profile — adds an external CI step and is harder to keep green than an in-repo contrast. The post-fix arm uses the real rotation + fulfilment so the asserted word is contract-derived (no tautology).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# THE patched surface — read to ground assertions; DO NOT modify any contracts/ file.
@.planning/phases/312-impl-vrf-rotation-fix-single-batched-user-approved-diff-impl/312-01-SUMMARY.md
@.planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md

<interfaces>
<!-- Confirmed live at HEAD 08c0f2aa (re-grep before patching). Use directly — no exploration. -->

DegenerusGame public views (contracts/DegenerusGame.sol):
  function currentDayView() external view returns (uint32)        // :471
  function rngWordForDay(uint32 day) external view returns (uint256)  // :2183
  function rngLocked() external view returns (bool)               // :2190
  function lastVrfProcessed() external view returns (uint48)      // :2202

DegenerusGameAdvanceModule (the patched file — contracts/modules/DegenerusGameAdvanceModule.sol):
  updateVrfCoordinatorAndSub(address newCoordinator, uint256 newSubId, bytes32 newKeyHash)  // admin-only :1693; 3-case preserve+re-issue
  requestLootboxRng() external           // mid-day request; sets LR_MID_DAY=1, advances LR_INDEX
  rawFulfillRandomWords(uint256 requestId, uint256[] randomWords)  // :1761 guard, :1772 mid-day write lootboxRngWordByIndex[index]=word

Storage layout (forge inspect DegenerusGame storage-layout — authoritative):
  rngLockedFlag        bool     slot 0 offset 21
  rngWordCurrent       uint256  slot 3
  vrfRequestId         uint256  slot 4
  lootboxRngPacked     uint256  slot 37   (LR_INDEX in low bits, LR_MID_DAY at bit 224 mask 0xFF)
  lootboxRngWordByIndex  mapping(uint48=>uint256)  slot 38
  => lootboxRngWordByIndex[i] lives at keccak256(abi.encode(uint256(i), uint256(38)))

MintModule consumer (contracts/modules/DegenerusGameMintModule.sol:686):
  uint256 entropy = lootboxRngWordByIndex[uint48(_lrRead(LR_INDEX_SHIFT, LR_INDEX_MASK)) - 1];
  // flows UNGUARDED into _processOneTicketEntry(..., entropy, idx) at :690-698; emits TraitsGenerated(player, baseKey, take) at :794

MockVRFCoordinator (contracts/mocks/MockVRFCoordinator.sol) — accepts requestRandomWords unconditionally (models pre-funding re-issue):
  createSubscription() returns (uint256 subId)
  addConsumer(uint256 subId, address consumer)
  fundSubscription(uint256 subId, uint96 amount)
  requestRandomWords(VRFRandomWordsRequest) returns (uint256 requestId)  // records {consumer, fulfilled:false}; subId stored as 0
  fulfillRandomWords(uint256 requestId, uint256 randomWord)   // calls consumer.rawFulfillRandomWords(requestId, [word])
  lastRequestId() returns (uint256)
  pendingRequests(uint256) returns (uint256 subId, address consumer, bool fulfilled)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the post-fix arm — real mid-flight rotation lands a real VRF word in lootboxRngWordByIndex[N]</name>
  <files>test/fuzz/VrfRotationOrphanIndex.t.sol</files>
  <read_first>
    - test/fuzz/VrfRotationOrphanIndex.t.sol — the file being created (does not exist yet).
    - contracts/modules/DegenerusGameAdvanceModule.sol — the CONTRACT-UNDER-TEST. Read updateVrfCoordinatorAndSub (~:1688, the 3-case preserve+re-issue branch — mid-day-first), rawFulfillRandomWords (~:1756, the :1761 requestId guard + :1772 mid-day write), requestLootboxRng (~:1064). DO NOT modify it.
    - test/fuzz/LootboxRngLifecycle.t.sol — the closest analog. Model on its helpers: `_setupForMidDayRng()` (lines ~81-101, completes 2 days, makes a lootbox purchase to create pending ETH, funds VRF), `_doCoordinatorSwap()` (lines ~70-77, deploys a 2nd MockVRFCoordinator + createSubscription + addConsumer + admin-pranked updateVrfCoordinatorAndSub), `_readLootboxRngIndex()` (slot 37), `_readLootboxWord(index)` (mapping at slot 38 via keccak256(abi.encode(index,38))), `_makePurchase()`, and `test_wordWriteMidDay` (lines ~253-270, mid-day fulfil writes word to lootboxRngWordByIndex[index-1]).
    - test/fuzz/helpers/DeployProtocol.sol — the base contract (`_deployProtocol()` deploys all mocks + protocol; `mockVRF`, `game`, `admin` are members).
    - contracts/mocks/MockVRFCoordinator.sol — the mock (requestRandomWords accepts unconditionally; fulfillRandomWords drives rawFulfillRandomWords).
  </read_first>
  <behavior>
    - test_postFix_midDayRotation_landsRealWordInOrphanedIndex(uint256 vrfWord): with vrfWord bounded non-zero, after a mid-day requestLootboxRng + a real updateVrfCoordinatorAndSub rotation to a 2nd MockVRFCoordinator while in flight + fulfillRandomWords(reIssuedReqId, vrfWord) on the NEW coordinator, lootboxRngWordByIndex[reservedIndex] == vrfWord (the SAME slot N reserved before the rotation; LR_INDEX preserved).
    - The reserved index N is captured as (_readLootboxRngIndex() - 1) immediately AFTER requestLootboxRng() and BEFORE the rotation; it MUST be unchanged by the rotation.
    - Before fulfilment on the new coordinator, lootboxRngWordByIndex[N] == 0 (the slot is still orphaned-pending, proving the assertion is not pre-satisfied).
    - The re-issued request id on the new coordinator (newVRF.lastRequestId()) MUST be a NEW request created by the rotation (not zero), proving updateVrfCoordinatorAndSub re-fired the request on the new coordinator.
  </behavior>
  <action>
    Create `test/fuzz/VrfRotationOrphanIndex.t.sol`: SPDX AGPL-3.0-only, pragma ^0.8.26, `contract VrfRotationOrphanIndex is DeployProtocol`. Import DeployProtocol, MockVRFCoordinator, MintPaymentKind (from contracts/interfaces/IDegenerusGame.sol) — mirror LootboxRngLifecycle.t.sol imports. setUp(): `_deployProtocol(); vm.warp(block.timestamp + 1 days);`.

    Port the helper shapes from LootboxRngLifecycle.t.sol VERBATIM in intent (copy, do not import — these are file-local helpers there): `_readLootboxRngIndex()` (slot 37), `_readLootboxWord(uint48 index)` (keccak256(abi.encode(uint256(index), uint256(38)))), `_completeDay(uint256 vrfWord)`, `_setupForMidDayRng()`, and a NEW `_rotateMidFlight()` helper that deploys a 2nd MockVRFCoordinator, createSubscription + addConsumer(newSubId, game) + fundSubscription, then `vm.prank(address(admin)); game.updateVrfCoordinatorAndSub(address(newVRF), newSubId, bytes32(uint256(1)));` and RETURNS newVRF. Crucially, `_rotateMidFlight()` must be called WHILE LR_MID_DAY==1 (a mid-day request is in flight) so the mid-day re-issue branch fires.

    Write test_postFix_midDayRotation_landsRealWordInOrphanedIndex per the behavior block. Sequence: _setupForMidDayRng(); game.requestLootboxRng(); capture reservedIndex = _readLootboxRngIndex() - 1; assert _readLootboxWord(reservedIndex) == 0 (orphaned-pending); MockVRFCoordinator newVRF = _rotateMidFlight(); assert newVRF.lastRequestId() != 0 (re-issue fired on new coordinator); still assert _readLootboxWord(reservedIndex) == 0 (not yet fulfilled); newVRF.fulfillRandomWords(newVRF.lastRequestId(), vrfWord); assert _readLootboxWord(reservedIndex) == vrfWord (the real VRF word landed in the SAME slot N). Use vm.assume(vrfWord != 0) to avoid the contract's zero-guard converting 0->1.

    The asserted word MUST come from the contract path (the test delivers vrfWord to the new coordinator, the contract writes it to [N] via rawFulfillRandomWords:1772) — do NOT vm.store the word into [N] yourself in this arm.

    Comments describe what IS (feedback_no_history_in_comments). ZERO contracts/ mutation.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge test --match-test test_postFix_midDayRotation_landsRealWordInOrphanedIndex 2>&1 | grep -E "PASS|FAIL|Suite result"; echo EXIT $?</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-test test_postFix_midDayRotation_landsRealWordInOrphanedIndex` exits 0 with the test PASS.
    - The test asserts `_readLootboxWord(reservedIndex) == vrfWord` after fulfilment on the new coordinator (a real VRF-derived word in slot N).
    - The test asserts `_readLootboxWord(reservedIndex) == 0` BOTH before the rotation's fulfilment is delivered (proving the assertion is not pre-satisfied / not a tautology).
    - The test asserts `newVRF.lastRequestId() != 0` after `updateVrfCoordinatorAndSub` (proving the re-issue fired on the new coordinator).
    - `git diff --name-only` shows ONLY test/fuzz/VrfRotationOrphanIndex.t.sol — no contracts/ file changed.
  </acceptance_criteria>
  <done>The post-fix arm passes: a real mid-flight rotation re-issues on the new coordinator and lands the delivered VRF word in the same preserved slot N; no contracts/ mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Author the pre-fix arm — entropy-0 consequence when lootboxRngWordByIndex[N]==0</name>
  <files>test/fuzz/VrfRotationOrphanIndex.t.sol</files>
  <read_first>
    - test/fuzz/VrfRotationOrphanIndex.t.sol — the file from Task 1 (extend it).
    - contracts/modules/DegenerusGameMintModule.sol — read :680-698 (the entropy = lootboxRngWordByIndex[LR_INDEX-1] read and its flow into _processOneTicketEntry) and :752-794 (_processOneTicketEntry + the TraitsGenerated(player, baseKey, take) emit at :794). This is the Scenario-A consumer that reads the orphaned slot unguarded. DO NOT modify it.
    - .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md section 1.3 (Scenario A — same-day advance after rotation -> entropy-0 traits) and section 3.4 (the :686 absent-zero-guard mitigated STRUCTURALLY).
    - test/fuzz/LootboxRngLifecycle.t.sol — its vm.load/vm.store storage-read helpers and the mapping-slot keccak math (slot 38).
  </read_first>
  <action>
    Add the pre-fix arm test test_preFix_orphanedZeroIndex_yieldsEntropyZero to the same contract. The fix is already merged and contracts/ MUST NOT be mutated, so reproduce the entropy-0 CONSEQUENCE by driving the orphan condition with cheatcodes:

    1. _setupForMidDayRng(); game.requestLootboxRng(); capture reservedIndex = _readLootboxRngIndex() - 1.
    2. Use `vm.store(address(game), keccak256(abi.encode(uint256(reservedIndex), uint256(38))), bytes32(0));` to force lootboxRngWordByIndex[reservedIndex] == 0 (the orphaned-at-zero state Scenario A reads).
    3. Assert the CONSEQUENCE: read the same slot back via _readLootboxWord(reservedIndex) and assert == 0, AND assert that this is the exact slot the consumer at MintModule:686 reads (LR_INDEX-1). Document via a code comment that the unguarded read at MintModule:686 would carry entropy==0 into _processOneTicketEntry -> deterministic traits — the defect the post-fix arm (Task 1) eliminates by structurally filling the slot.

    The pre-fix arm asserts the CONSEQUENCE (the orphaned slot is zero and is the consumed index), establishing the contrast with the post-fix arm's non-zero invariant. Keep both arms in the SAME contract so a single `forge test --match-contract VrfRotationOrphanIndex` invocation runs the full contrast.

    Do NOT attempt to assert against real trait byte-output here (trait determinism under entropy-0 is the SPEC §1.3 narrative; asserting the consumed slot is zero is the verifiable, non-flaky consequence). Comments describe what IS.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge test --match-contract VrfRotationOrphanIndex 2>&1 | grep -E "PASS|FAIL|Suite result"; echo EXIT $?</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-contract VrfRotationOrphanIndex` exits 0 with BOTH arms (test_preFix_orphanedZeroIndex_yieldsEntropyZero and test_postFix_midDayRotation_landsRealWordInOrphanedIndex) PASS in the single invocation.
    - The pre-fix arm forces lootboxRngWordByIndex[reservedIndex]==0 via vm.store at slot keccak256(abi.encode(uint256(reservedIndex), uint256(38))) and asserts the consumed-index slot reads 0.
    - The pre-fix arm computes reservedIndex as the SAME index the consumer reads (LR_INDEX - 1), establishing it is the consumed slot.
    - `git diff --name-only` shows ONLY test/fuzz/VrfRotationOrphanIndex.t.sol.
  </acceptance_criteria>
  <done>Both contrast arms pass in one invocation: pre-fix asserts the entropy-0 consequence at the consumed index, post-fix asserts a real VRF word fills it after rotation — VTST-01 / VRF-01 proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (TEST-INTEGRITY framing — this phase mutates ZERO mainnet contracts; it introduces NO production attack surface)

| Boundary | Description |
|----------|-------------|
| Test harness to contract-under-test | The test drives the real DegenerusGameAdvanceModule via DegenerusGame; assertions must read contract-derived state, not test-injected state |
| MockVRFCoordinator to AdvanceModule.rawFulfillRandomWords | The mock delivers the word the test chooses; the post-fix arm must deliver via the contract path so the asserted word is contract-written, not test-written |

## STRIDE Threat Register (test-integrity)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-313-01-01 | Tampering (tautological assertion) | post-fix arm asserting lootboxRngWordByIndex[N] | mitigate | The post-fix arm asserts the slot is 0 BEFORE fulfilment and == vrfWord AFTER fulfilment via the contract's rawFulfillRandomWords:1772 path; the test never vm.stores the word into [N] in the post-fix arm. The asserted word is contract-derived. |
| T-313-01-02 | Information Disclosure (false-negative reproduction) | pre-fix arm not actually reproducing the defect | mitigate | The pre-fix arm forces the exact orphaned-at-zero state at the SAME index the MintModule:686 consumer reads (LR_INDEX-1) and asserts the consumed slot is zero — the precise Scenario-A precondition; without it the post-fix contrast proves nothing. |
| T-313-01-03 | Denial of Service (flaky fuzz) | vrfWord==0 hitting the zero-guard | mitigate | vm.assume(vrfWord != 0) avoids the contract's 0->1 zero-guard masking the equality assertion; the rotation/fulfilment sequence is deterministic (single coordinator, single request). |
| T-313-01-SC | Tampering | npm/pip/cargo installs | n/a | NO package installs. Pure Solidity test file using existing forge-std + in-repo mocks. Slopcheck not applicable. |

**No production attack surface is introduced — zero contracts/ mainnet mutation (audit-only per D-43N-AUDIT-ONLY-01).**
</threat_model>

<verification>
- `forge build` exits 0 (no new errors; pre-existing unsafe-typecast lint notes unchanged).
- `forge test --match-contract VrfRotationOrphanIndex` exits 0 — both arms PASS in one invocation.
- `git diff --name-only` shows ONLY test/fuzz/VrfRotationOrphanIndex.t.sol; ZERO contracts/ change.
</verification>

<success_criteria>
ROADMAP Phase 313 Success Criterion 1: orphan-index reproduction PASSES — pre-fix asserts the entropy-0 defect consequence, post-fix asserts a real VRF word in lootboxRngWordByIndex[N] after rotation (VRF-01 by VTST-01).
</success_criteria>

<output>
Create `.planning/phases/313-tst-vrf-regression-freeze-invariant-fuzz-under-rotation-tst/313-01-SUMMARY.md` when done.
</output>
