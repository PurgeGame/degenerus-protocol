---
phase: 81-ticket-creation-queue-mechanics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v4.0-ticket-queue-double-buffer.md
autonomous: true
requirements:
  - TKT-05
  - TKT-06
  - DSC-01
  - DSC-02

must_haves:
  truths:
    - "The three key encoding formulas (_tqReadKey, _tqWriteKey, _tqFarFutureKey) are documented with exact Solidity and file:line citations"
    - "The ticketWriteSlot variable's role in toggling read/write buffers is fully explained"
    - "The three disjoint key spaces (Slot 0, Slot 1, Far-Future) are proven disjoint for all valid levels"
    - "_swapAndFreeze and _swapTicketSlot trigger conditions are listed with every call site and file:line"
    - "The relationship between swap triggers and prizePoolFrozen is documented"
    - "Every prior audit claim about double-buffer mechanics is cross-referenced and confirmed or flagged as [DISCREPANCY]"
  artifacts:
    - path: "audit/v4.0-ticket-queue-double-buffer.md"
      provides: "Double-buffer mechanics and swap trigger audit with file:line citations"
      contains: "## Key Encoding Formulas"
  key_links:
    - from: "audit/v4.0-ticket-queue-double-buffer.md"
      to: "contracts/storage/DegenerusGameStorage.sol"
      via: "file:line citations for _tqReadKey (GS:692), _tqWriteKey (GS:688), _tqFarFutureKey (GS:698), _swapTicketSlot (GS:709), _swapAndFreeze (GS:719)"
      pattern: "GS:\\d+"
    - from: "audit/v4.0-ticket-queue-double-buffer.md"
      to: "contracts/modules/DegenerusGameAdvanceModule.sol"
      via: "file:line citations for swap triggers (AM:233 _swapAndFreeze, AM:720 _swapTicketSlot)"
      pattern: "AM:\\d+"
---

<objective>
Document the double-buffer queuing system -- key encoding formulas, ticketWriteSlot mechanics, three disjoint key spaces, and all swap trigger conditions -- with exhaustive file:line citations against current Solidity. Cross-reference against prior audit documentation.

Purpose: The double-buffer is the infrastructure all ticket creation feeds into (documented in Plan 01). This plan covers the buffer-swap mechanics that determine WHEN the write buffer becomes the read buffer and WHEN the prize pool freezes. Understanding these mechanics is critical for later phases (82-84) that trace ticket processing and jackpot payouts.

Output: `audit/v4.0-ticket-queue-double-buffer.md` -- comprehensive audit document covering TKT-05, TKT-06, DSC-01, DSC-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-ticket-creation-queue-mechanics/81-RESEARCH.md

Key contract files to trace:
- contracts/storage/DegenerusGameStorage.sol (key encoding GS:686-700, swap mechanics GS:709-735, constants GS:154/162)
- contracts/modules/DegenerusGameAdvanceModule.sol (_swapAndFreeze at AM:233, _swapTicketSlot at AM:720)
- contracts/DegenerusGame.sol (sampleFarFutureTickets at DG:2681 for key usage audit)

Prior audit docs for cross-reference:
- audit/v3.8-commitment-window-inventory.md
- audit/v3.9-rng-commitment-window-proof.md

Existing tests for verification:
- test/fuzz/QueueDoubleBuffer.t.sol (double-buffer invariant tests)
- test/fuzz/PrizePoolFreeze.t.sol (prize pool freeze tests)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document key encoding formulas, three key spaces, and ticketWriteSlot mechanics</name>
  <files>audit/v4.0-ticket-queue-double-buffer.md</files>
  <read_first>
    contracts/storage/DegenerusGameStorage.sol
    contracts/modules/DegenerusGameAdvanceModule.sol
    contracts/DegenerusGame.sol
    audit/v3.8-commitment-window-inventory.md
    audit/v3.9-rng-commitment-window-proof.md
  </read_first>
  <action>
Create `audit/v4.0-ticket-queue-double-buffer.md` with the following structure and content, built entirely from reading current Solidity:

**Document header:**
```
# v4.0 Ticket Queue Double-Buffer Mechanics — Exhaustive Audit
Phase 81 | Audited: {date}
Methodology: Every claim verified against current Solidity with file:line citations.
Prior audit prose (v3.8, v3.9) treated as unverified per STATE.md.
```

**Section 1: Constants (TKT-05 partial)**
Document the bit constants from DegenerusGameStorage.sol with exact line numbers:
- `TICKET_SLOT_BIT = 1 << 23` (0x800000) at GS:154 -- confirm exact line, value, and purpose
- `TICKET_FAR_FUTURE_BIT = 1 << 22` (0x400000) at GS:162 -- confirm exact line, value, and purpose
- `ticketWriteSlot` storage variable -- find its declaration line, confirm its type (uint8/uint256), document its two possible values (0 and 1)

For each constant, quote the exact Solidity line and provide the hex value.

**Section 2: Key Encoding Formulas (TKT-05)**
Document the three key encoding functions at GS:686-700 with exact line numbers and full function bodies:

1. `_tqWriteKey(uint24 lvl)` -- confirm at GS:688 (or actual line). Quote the full function body:
   ```solidity
   function _tqWriteKey(uint24 lvl) internal view returns (uint24) {
       return ticketWriteSlot != 0 ? lvl | TICKET_SLOT_BIT : lvl;
   }
   ```
   Document: when ticketWriteSlot=0, write key = lvl (Slot 0). When ticketWriteSlot=1, write key = lvl | 0x800000 (Slot 1).

2. `_tqReadKey(uint24 lvl)` -- confirm exact line. Quote the full function body. Document: inverse of write key. When ticketWriteSlot=0, read key = lvl | 0x800000 (Slot 1). When ticketWriteSlot=1, read key = lvl (Slot 0). Read key is always the opposite slot from write key.

3. `_tqFarFutureKey(uint24 lvl)` -- confirm exact line. Quote the full function body. Document: `lvl | 0x400000` (Slot FF). Note this is a `pure` function -- independent of ticketWriteSlot. Far-future key space does NOT participate in the double-buffer swap.

**Section 3: Three Disjoint Key Spaces (TKT-05)**
Prove the three key spaces are disjoint for all valid levels:

```
Key Space 0 (Slot 0): [0x000000, 0x3FFFFF] — lvl with no bits set
Key Space 1 (FF):     [0x400000, 0x7FFFFF] — lvl | TICKET_FAR_FUTURE_BIT (bit 22)
Key Space 2 (Slot 1): [0x800000, 0xBFFFFF] — lvl | TICKET_SLOT_BIT (bit 23)
```

Show that for any level < 2^22 (4,194,303):
- Bit 22 is 0 in the raw level, so Slot 0 keys have bit 22 = 0 and bit 23 = 0
- FF keys have bit 22 = 1 and bit 23 = 0
- Slot 1 keys have bit 22 = 0 and bit 23 = 1
- Therefore: no overlap is possible (bit 22 and bit 23 distinguish all three)

State the maximum valid level (2^22 - 1 = 4,194,303) and note the practical impossibility of exceeding this (game has ~1000 levels).

**Section 4: ticketWriteSlot State Machine (TKT-05)**
Document the two states and their meanings:

```
State A: ticketWriteSlot = 0
  _tqWriteKey(lvl) = lvl           (writes go to Slot 0)
  _tqReadKey(lvl)  = lvl | 0x800000 (reads come from Slot 1)

State B: ticketWriteSlot = 1
  _tqWriteKey(lvl) = lvl | 0x800000 (writes go to Slot 1)
  _tqReadKey(lvl)  = lvl            (reads come from Slot 0)
```

Describe the invariant: at any given time, one slot accumulates new tickets (write buffer) while the other is being processed by jackpot draws (read buffer). The far-future key space is independent of this toggle.

Document where ticketWriteSlot is modified: only in `_swapTicketSlot` via `ticketWriteSlot ^= 1` at GS:712 (confirm exact line). This XOR toggle switches between state A and state B.

**Section 5: All ticketQueue Key Consumers (TKT-05 partial)**
Search the entire codebase for every usage of `_tqWriteKey`, `_tqReadKey`, `_tqFarFutureKey`, and direct `ticketQueue[` access. Document each with file:line:

For `_tqWriteKey`:
- All three queue helpers (GS:547/GS:575/GS:624 approximately) -- used for near-future routing
- `sampleFarFutureTickets` at DG:2681 -- flag as [NEW FINDING] (uses write key for far-future sampling, should use _tqFarFutureKey)
- Any other callers found by grep

For `_tqReadKey`:
- `processTicketBatch` and related processing functions (document file:line)
- Swap functions (drain check at GS:710)
- Any jackpot winner selection reads
- Any other callers found by grep

For `_tqFarFutureKey`:
- All three queue helpers (GS:546/GS:574/GS:623 approximately) -- used for far-future routing
- `_awardFarFutureCoinJackpot` at JM:2543 -- confirm reads ONLY from FF key (not combined pool)
- Any other callers found by grep

For direct `ticketQueue[` access:
- Queue helper push operations
- Any direct reads not going through key encoding functions
- Document each with file:line

Run these greps:
```
grep -rn "_tqWriteKey" contracts/
grep -rn "_tqReadKey" contracts/
grep -rn "_tqFarFutureKey" contracts/
grep -rn "ticketQueue\[" contracts/
```

Present results as a consolidated table:
```
| Key Function | Caller | File:Line | Purpose |
```
  </action>
  <verify>
    <automated>grep -c "GS:\|AM:\|DG:\|JM:" audit/v4.0-ticket-queue-double-buffer.md | awk '{if ($1 > 25) print "PASS: " $1 " file:line citations"; else print "FAIL: only " $1 " citations"}'</automated>
  </verify>
  <acceptance_criteria>
    - audit/v4.0-ticket-queue-double-buffer.md exists and is non-empty
    - Document contains "## Constants" section with TICKET_SLOT_BIT and TICKET_FAR_FUTURE_BIT citations
    - Document contains "## Key Encoding Formulas" section with all 3 functions quoted with file:line
    - Document contains "## Three Disjoint Key Spaces" section with disjointness proof
    - Document contains "## ticketWriteSlot State Machine" section with both states documented
    - Document contains "## All ticketQueue Key Consumers" section with grep results
    - grep for "_tqWriteKey" in the document returns at least 5 matches
    - grep for "_tqReadKey" in the document returns at least 3 matches
    - grep for "_tqFarFutureKey" in the document returns at least 3 matches
    - grep for "sampleFarFutureTickets" in the document returns at least 1 match with [NEW FINDING] tag
    - grep for "ticketWriteSlot" in the document returns at least 5 matches
    - Key consumer table has columns: Key Function, Caller, File:Line, Purpose
  </acceptance_criteria>
  <done>All three key encoding formulas are documented with exact Solidity, the three key spaces are proven disjoint, ticketWriteSlot state machine is fully described, and every consumer of queue keys in the codebase is enumerated with file:line.</done>
</task>

<task type="auto">
  <name>Task 2: Document swap trigger conditions, freeze behavior, and cross-reference prior audit</name>
  <files>audit/v4.0-ticket-queue-double-buffer.md</files>
  <read_first>
    audit/v4.0-ticket-queue-double-buffer.md
    contracts/storage/DegenerusGameStorage.sol
    contracts/modules/DegenerusGameAdvanceModule.sol
    audit/v3.8-commitment-window-inventory.md
    audit/v3.9-rng-commitment-window-proof.md
    test/fuzz/QueueDoubleBuffer.t.sol
    test/fuzz/PrizePoolFreeze.t.sol
  </read_first>
  <action>
Append to `audit/v4.0-ticket-queue-double-buffer.md` the swap trigger and cross-reference sections.

**Section 6: _swapTicketSlot Function (TKT-06)**
Document the function from DegenerusGameStorage.sol at GS:709-714 (confirm exact lines):

Quote the full function body. Document line-by-line:
1. `uint24 rk = _tqReadKey(purchaseLevel)` -- compute read key for current level
2. `if (ticketQueue[rk].length != 0) revert E()` -- precondition: read buffer must be fully drained
3. `ticketWriteSlot ^= 1` -- flip the buffer toggle (0->1 or 1->0)
4. `ticketsFullyProcessed = false` -- reset the processing flag for the new read buffer

Explain the semantics: swap is only allowed when the current read buffer is empty (all tickets processed). After swap, the old write buffer becomes the new read buffer (for processing), and the old read buffer becomes the new write buffer (for accumulating).

**Section 7: _swapAndFreeze Function (TKT-06)**
Document the function from DegenerusGameStorage.sol at GS:719-725 (confirm exact lines):

Quote the full function body. Document line-by-line:
1. `_swapTicketSlot(purchaseLevel)` -- perform the buffer swap
2. `if (!prizePoolFrozen)` -- check if pools are already frozen
3. `prizePoolFrozen = true` -- freeze the prize pools
4. `prizePoolPendingPacked = 0` -- zero the pending accumulator

Explain: `_swapAndFreeze` adds prize pool freezing on top of the buffer swap. If already frozen (multi-day jackpot scenario), the freeze step is skipped and pending accumulators keep growing.

**Section 8: Swap Trigger Conditions — Complete Call Site List (TKT-06)**
Run grep to find every caller:
```
grep -rn "_swapAndFreeze\|_swapTicketSlot" contracts/
```

Document each call site with full context:

**Trigger 1: _swapAndFreeze in daily RNG request path**
- Location: AdvanceModule at approximately AM:233
- When: `advanceGame()` -> new-day path -> daily RNG request
- Precondition: `ticketsFullyProcessed` is true AND `ticketQueue[_tqReadKey(purchaseLevel)].length > 0` ... actually no, the precondition is inside _swapTicketSlot itself (read buffer empty). Document the exact condition that gates the call at AM:233 -- what `if` block surrounds it?
- Read the actual code around AM:233 and document: what condition must be true for _swapAndFreeze to be called? Is it always called on new-day, or only conditionally?

**Trigger 2: _swapTicketSlot in mid-day lootbox RNG path**
- Location: AdvanceModule at approximately AM:720
- When: `requestLootboxRng()` path
- Precondition: `ticketsFullyProcessed && ticketQueue[wk].length > 0` where wk is the write key
- Document: this is a swap WITHOUT freeze (calls _swapTicketSlot directly, not _swapAndFreeze). Document why -- mid-day swap does not need pool freeze because it is not starting a new jackpot round.
- Read the actual code around AM:720 and document the exact condition.

If grep reveals any other call sites, document them.

Create a consolidated trigger table:
```
| Trigger | Function Called | Location | Condition | Freezes Pools? |
|---------|---------------|----------|-----------|---------------|
| Daily RNG request | _swapAndFreeze | AM:{line} | {condition from code} | Yes (unless already frozen) |
| Mid-day lootbox RNG | _swapTicketSlot | AM:{line} | {condition from code} | No |
```

**Section 9: ticketsFullyProcessed Flag (TKT-06 partial)**
Document where `ticketsFullyProcessed` is:
- Set to `false`: _swapTicketSlot at GS:{line} (confirm)
- Set to `true`: find by grep -- should be in processTicketBatch or similar. Document file:line.
- Read: find by grep -- where is it checked? Document each location.

This flag controls whether a swap is eligible -- you cannot swap buffers until the current read buffer is fully processed.

**Section 10: Relationship to processTicketBatch (TKT-06 partial)**
Briefly describe (for context, detailed trace in Phase 82):
- processTicketBatch reads from the read buffer via `_tqReadKey`
- When processing completes, `ticketsFullyProcessed` is set to true
- This enables the next swap (either via _swapAndFreeze or _swapTicketSlot)

Note: full processTicketBatch trace is in Phase 82 (TKT-08). Only document the interface points here.

**Section 11: Cross-Reference Against Prior Audit (DSC-01, DSC-02)**
For every claim in v3.8-commitment-window-inventory.md and v3.9-rng-commitment-window-proof.md that relates to:
- Double-buffer formulas
- Key encoding
- Swap triggers
- ticketsFullyProcessed
- prizePoolFrozen in context of swap

Quote the prior claim, then state CONFIRMED or [DISCREPANCY] with current code file:line.

Check specifically:
- v3.9 RNG proof Section 1 (if it describes swap mechanics): verify against current code
- v3.8 inventory claims about ticketWriteSlot, _swapAndFreeze, _swapTicketSlot: verify against current code
- Any claim about "read slot fully processed" precondition: verify against _swapTicketSlot's actual check

**Section 12: Existing Test Coverage**
Read `test/fuzz/QueueDoubleBuffer.t.sol` and `test/fuzz/PrizePoolFreeze.t.sol`. Document:
- What invariants do these tests verify?
- What edge cases do they cover?
- Are there gaps relative to TKT-05/TKT-06 requirements?

Run `forge test --match-contract QueueDoubleBuffer -vvv` and `forge test --match-contract PrizePoolFreeze -vvv` to confirm tests pass. Include output summary.

**Section 13: Audit Metadata**
```
## Audit Metadata

**Methodology:** Every claim derived from current Solidity source. No claims copied from prior audits without verification.
**Files read:** {list}
**Grep commands:** {list with counts}
**Discrepancies found:** {count}
**New findings:** {count}
**Test verification:** QueueDoubleBuffer {PASS/FAIL}, PrizePoolFreeze {PASS/FAIL}

**Auditor:** Claude (automated)
**Date:** {date}
**Phase:** 81 (Ticket Creation & Queue Mechanics)
**Milestone:** v4.0 (Ticket Lifecycle & RNG-Dependent Variable Re-Audit)
```
  </action>
  <verify>
    <automated>forge test --match-contract QueueDoubleBuffer -vvv --no-match-test "SKIP" 2>&1 | tail -5 && grep -c "_swapAndFreeze\|_swapTicketSlot\|ticketsFullyProcessed" audit/v4.0-ticket-queue-double-buffer.md | awk '{if ($1 > 15) print "PASS: " $1 " swap-related references"; else print "FAIL: only " $1 " swap references"}'</automated>
  </verify>
  <acceptance_criteria>
    - audit/v4.0-ticket-queue-double-buffer.md contains "## _swapTicketSlot Function" section with full function body quoted
    - audit/v4.0-ticket-queue-double-buffer.md contains "## _swapAndFreeze Function" section with full function body quoted
    - audit/v4.0-ticket-queue-double-buffer.md contains "## Swap Trigger Conditions" section with at least 2 triggers
    - Trigger table has columns: Trigger, Function Called, Location, Condition, Freezes Pools?
    - Each trigger has a file:line citation (AM:{line} format)
    - audit/v4.0-ticket-queue-double-buffer.md contains "## ticketsFullyProcessed Flag" section
    - ticketsFullyProcessed section documents where it is set to true, set to false, and read, each with file:line
    - audit/v4.0-ticket-queue-double-buffer.md contains "## Cross-Reference Against Prior Audit" section
    - At least 1 [DISCREPANCY] or CONFIRMED tag in the cross-reference section
    - audit/v4.0-ticket-queue-double-buffer.md contains "## Existing Test Coverage" section
    - Test coverage section mentions QueueDoubleBuffer and PrizePoolFreeze test results
    - audit/v4.0-ticket-queue-double-buffer.md contains "## Audit Metadata" section
    - forge test --match-contract QueueDoubleBuffer passes (exit code 0)
  </acceptance_criteria>
  <done>Swap trigger conditions are fully documented with every call site, ticketsFullyProcessed lifecycle is traced, existing test coverage is reviewed, and all prior audit claims about double-buffer mechanics are cross-referenced.</done>
</task>

</tasks>

<verification>
- `audit/v4.0-ticket-queue-double-buffer.md` exists with all required sections (1-13)
- Three key encoding formulas documented with full Solidity quotes and file:line
- Three key spaces proven disjoint with bit-level analysis
- ticketWriteSlot state machine fully documented
- All ticketQueue key consumers enumerated with file:line
- _swapTicketSlot and _swapAndFreeze documented with full function bodies
- Every swap trigger call site documented with surrounding conditions
- ticketsFullyProcessed lifecycle (set true, set false, read) documented
- Cross-reference section verifies prior audit claims
- `forge test --match-contract QueueDoubleBuffer -vvv` passes
- `forge test --match-contract PrizePoolFreeze -vvv` passes
</verification>

<success_criteria>
Requirements TKT-05, TKT-06, DSC-01, DSC-02 are satisfied: the double-buffer formulas, three key spaces, and ticketWriteSlot state machine are exhaustively documented, all swap trigger conditions are listed with file:line citations, and prior audit claims are cross-referenced.
</success_criteria>

<output>
After completion, create `.planning/phases/81-ticket-creation-queue-mechanics/81-02-SUMMARY.md`
</output>
