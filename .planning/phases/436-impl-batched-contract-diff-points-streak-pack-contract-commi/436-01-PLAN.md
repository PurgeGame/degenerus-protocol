---
phase: 436-impl-batched-contract-diff-points-streak-pack-contract-commi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/modules/DegenerusGameMintStreakUtils.sol
  - contracts/storage/DegenerusGameStorage.sol
  - contracts/modules/GameAfkingModule.sol
  - contracts/DegenerusQuests.sol
  - contracts/modules/DegenerusGameDegeneretteModule.sol
  - contracts/modules/DegenerusGameDecimatorModule.sol
autonomous: false
requirements: [POINTS-01, POINTS-02, STREAK-01, STREAK-02, PACK-01]

must_haves:
  truths:
    - "The activity score is computed in whole points: every additive leg of _playerActivityScoreAt is point-domain and the sole sub-point leg (quest streak) is floored floor(questStreak/2)"
    - "The activity-score hard cap constant is ACTIVITY_SCORE_HARD_CAP_POINTS = 65_534 (D-436-04 reconciliation: NOT 655)"
    - "All six score-INPUT TABLE-A anchors are converted to the point domain and renamed _BPS -> _POINTS; all TABLE-B output bps constants are unchanged and keep _BPS"
    - "The three score-driven consumers (Lootbox EV multiplier, Degenerette ROI/WWXRP, Decimator) read the point-domain score; the Decimator multiplier uses the (points*100)/3 re-scale"
    - "Sub.subStreakLatch is uint16, SUB_STREAK_MASK is 0xffff, _streakBaseOf returns uint16, _setStreakBase clamps at type(uint16).max (not 255), and the DegenerusQuests finalize floor-hack is deleted"
    - "Sub.pendingFlip is uint24 with both accrue clamps re-pinned to type(uint24).max and uint24(newOwed) casts; affiliateBase is untouched (uint32, 100M clamp)"
    - "The accumulator slot is affiliateBase(32)+pendingFlip(24)+subStreakLatch(16)=72 bits and Sub stays exactly one 256-bit slot, 0 free"
    - "forge build is clean, the Game deployed bytecode stays under the 24,576-byte EIP-170 ceiling, and the batched .sol diff lands in ONE atomic USER-approved commit"
  artifacts:
    - path: "contracts/modules/DegenerusGameMintStreakUtils.sol"
      provides: "Point-domain _playerActivityScoreAt with floor(questStreak/2) + point cap 65_534"
      contains: "ACTIVITY_SCORE_HARD_CAP_POINTS"
    - path: "contracts/storage/DegenerusGameStorage.sol"
      provides: "Renamed point-domain input anchors, repacked accumulator, widened latch"
      contains: "uint16 subStreakLatch"
    - path: "contracts/modules/GameAfkingModule.sol"
      provides: "pendingFlip uint24 clamps + uint16 latch follow-through"
      contains: "type(uint24).max"
    - path: "contracts/DegenerusQuests.sol"
      provides: "Floor-hack deleted; streak source consistent with uint16 latch"
      contains: "state.streak"
    - path: "contracts/modules/DegenerusGameDegeneretteModule.sol"
      provides: "Point-domain MID/HIGH/MAX anchors; ROI shapes unchanged"
      contains: "ACTIVITY_SCORE_MID_POINTS"
    - path: "contracts/modules/DegenerusGameDecimatorModule.sol"
      provides: "Point-domain CAP anchor + (points*100)/3 multiplier re-scale"
      contains: "TERMINAL_DEC_ACTIVITY_CAP_POINTS"
  key_links:
    - from: "contracts/modules/DegenerusGameMintStreakUtils.sol"
      to: "ACTIVITY_SCORE_HARD_CAP_POINTS"
      via: "score-ceiling clamp"
      pattern: "ACTIVITY_SCORE_HARD_CAP_POINTS"
    - from: "contracts/storage/DegenerusGameStorage.sol"
      to: "Sub accumulator slot"
      via: "affiliateBase(32)+pendingFlip(24)+subStreakLatch(16)=72"
      pattern: "uint24 pendingFlip"
    - from: "contracts/modules/DegenerusGameDecimatorModule.sol"
      to: "point-domain multiplier"
      via: "(points*100)/3 re-scale"
      pattern: "100\\) / 3"
---

<objective>
Land the v69 activity-score change as ONE batched, USER-approved `contracts/*.sol` diff across three tracks — POINTS (bps -> whole points + migrated consumer thresholds), STREAK (single exact integer streak path + reworked pre-streak cap), PACK (`pendingFlip` uint32->uint24 + net-zero accumulator repack). This is the sole `.sol` change of the v69 milestone and the milestone's only approval gate.

The substance is FULLY DESIGN-LOCKED in `435-DESIGN-LOCK.md`. This plan translates the consolidated "436 Edit Surface" into serialized, mechanical executable tasks — it does NOT re-litigate any locked decision. The IMPL-discretion knobs are resolved in `436-CONTEXT.md` (D-436-01..09).

Purpose: produce the new v69 subject, byte-frozen for all later re-audit work (437 TST, 438 REAUDIT).
Output: a single atomic contract commit across six `.sol` files (plus comment-only edits in the same files), behind the contract-commit approval gate.

⚠ CRITICAL EXECUTION INVARIANTS (read before starting any task):
- **ONE atomic contract commit.** ALL `.sol` edits across all three tracks land as a SINGLE commit (Task 6). There is NO intermediate commit of any `contracts/*.sol` file in any earlier task. Tasks 1-5 edit files but do NOT commit.
- **Serialized edits.** Tasks 1-5 run sequentially (no concurrent `.sol` writes). Worktrees are disabled for `contracts/`.
- **D-436-09 reconciliation (655 -> 65_534).** Wherever `435-DESIGN-LOCK.md` says the cap becomes `655`, the executor MUST use `65_534` (rename-only, value unchanged). This is called out explicitly in Tasks 1 and 2.
- **Baseline.** Every edit-surface anchor is against the frozen v68 subject (`contracts/` tree `e9a5fc24`); HEAD sits atop it and `contracts/` is currently clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md
@.planning/phases/436-impl-batched-contract-diff-points-streak-pack-contract-commi/436-CONTEXT.md

<interfaces>
<!-- The load-bearing spec is 435-DESIGN-LOCK.md §"436 Edit Surface" (lines 417-462) + the TABLE A / TABLE B classification (lines 318-345) + the DO-NOT-TOUCH list (lines 457-462). Read those before touching anything. The reconciliation D-436-09 (655 -> 65_534) is in 436-CONTEXT.md §D-436-04 and §D-436-09. -->

TABLE A — score-INPUT anchors that CONVERT (÷100) AND rename _BPS -> _POINTS:
- DegenerusGameStorage.sol:141  ACTIVITY_SCORE_HARD_CAP_BPS = 65_534  -> ACTIVITY_SCORE_HARD_CAP_POINTS = 65_534  [D-436-09: value STAYS 65_534, NOT 655]
- DegenerusGameStorage.sol:135  DEITY_PASS_ACTIVITY_BONUS_BPS = 8000  -> DEITY_PASS_ACTIVITY_BONUS_POINTS = 80
- DegenerusGameStorage.sol:1553 LOOTBOX_EV_ACTIVITY_NEUTRAL_BPS = 6_000 -> LOOTBOX_EV_ACTIVITY_NEUTRAL_POINTS = 60
- DegenerusGameStorage.sol:1555 LOOTBOX_EV_ACTIVITY_MAX_BPS = 40_000   -> LOOTBOX_EV_ACTIVITY_MAX_POINTS = 400
- DegeneretteModule.sol:188     ACTIVITY_SCORE_MID_BPS = 7_500         -> ACTIVITY_SCORE_MID_POINTS = 75
- DegeneretteModule.sol:191     ACTIVITY_SCORE_HIGH_BPS = 25_500       -> ACTIVITY_SCORE_HIGH_POINTS = 255
- DegeneretteModule.sol:194     ACTIVITY_SCORE_MAX_BPS = 30_500        -> ACTIVITY_SCORE_MAX_POINTS = 305
- DecimatorModule.sol:772       TERMINAL_DEC_ACTIVITY_CAP_BPS = 23_500 -> TERMINAL_DEC_ACTIVITY_CAP_POINTS = 235

TABLE B — output / out-of-domain bps: DO NOT convert, DO NOT rename (keep _BPS):
- Degenerette ROI_MIN/MID/HIGH/MAX_BPS (:197-206), WWXRP_HIGH_ROI_BASE/MAX_BPS (:214/:217), the quadratic coefficients 1000/500 (:1152-1153), DEGEN_DGNRS_*_BPS
- Lootbox LOOTBOX_EV_MIN/NEUTRAL/MAX_BPS (Storage:1557/:1559/:1561)
- Decimator BPS_DENOMINATOR = 10_000 (:104)
- The LootboxModule.sol:304 "= 40000" comment (presale-box EV mean, NOT a score anchor) — IGNORE
- lootboxRngPendingFlip (uint40, Storage:1525) — SEPARATE field, out of scope (D-08)
- affiliateBase (uint32, its 100M clamp at GameAfkingModule:920-922) — UNTOUCHED (D-436-08)
- Sub.score stamp (Storage:2127) — stays uint16
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: POINTS — whole-point _playerActivityScoreAt + floor + point cap (MintStreakUtils)</name>
  <files>contracts/modules/DegenerusGameMintStreakUtils.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameMintStreakUtils.sol:282-380 (the full `_playerActivityScoreAt` + `_playerActivityScore`; the additive legs at :310-311, :329, :330, :335, :346, :350, :354, :356, :363-366; the cap application site)
    - .planning/phases/435-.../435-DESIGN-LOCK.md:24-92 (DESIGN-01: the additive-contributor inventory TABLE, D-02 floor rule, D-03 cap, the locked-outputs table) AND :421-426 (the consolidated 436 Edit Surface for MintStreakUtils)
    - .planning/phases/436-.../436-CONTEXT.md:28-38 (D-436-04 + D-436-09: the cap STAYS 65_534, NOT 655)
  </read_first>
  <action>
    Convert every additive leg of `_playerActivityScoreAt` to the point domain by dropping the `× 100` on the clean legs (mint streak :329, mint count :330, affiliate :346, deity base :310-311 `50*100`/`25*100` -> `50`/`25`, deity pass :350, whale :354 `+1000`->`+10` / :356 `+4000`->`+40`, curse penalty :363-366 `- curse*100` -> `- curse`). The deity-pass leg uses the renamed `DEITY_PASS_ACTIVITY_BONUS_POINTS = 80` (see Task 2 for the constant rename in Storage; it is consumed here).

    The SOLE sub-point leg — quest streak at :335 `uint256(questStreak) * 50` (= 0.5 pt each) — becomes `floor(questStreak / 2)` per D-02 (the only place precision is intentionally lost). Use integer division `questStreak / 2`. IMPL discretion: helper/constant naming and exact placement of the floor leg — pick the cleanest; do NOT introduce a half-point intermediate anywhere.

    `PASS_STREAK_FLOOR_POINTS = 50` / `PASS_MINT_COUNT_FLOOR_POINTS = 25` (Storage:144/147) are ALREADY point-domain (they floor the pre-`×100` point counts) — do NOT convert them.

    Cap application (D-03 as amended by D-436-09): wherever the score is clamped against the hard cap, use the renamed point-domain `ACTIVITY_SCORE_HARD_CAP_POINTS` constant. ⚠ Its value STAYS `65_534` (the uint16 storage limit, one below max for the sDGNRS `+1` sentinel) — it is NOT floored to `655`. The 435 edit surface text saying the cap becomes `655` is superseded by D-436-09: rename only, value unchanged.

    Comment rule (D-436-03): reword unit tokens (`bps`/`basis points` -> `points`) on touched lines and on the `@param score ... in basis points` natspec; keep rationale that stays true (the hard-cap overflow rationale becomes true verbatim at cap = 65_534 — KEEP it), drop rationale that goes false. Comments describe what IS — no history/changed-from notes.

    Do NOT commit in this task.
  </action>
  <acceptance_criteria>
    - `grep -n "ACTIVITY_SCORE_HARD_CAP_POINTS" contracts/modules/DegenerusGameMintStreakUtils.sol` shows the cap clamp uses the renamed constant (no remaining `ACTIVITY_SCORE_HARD_CAP_BPS` reference in this file)
    - The quest-streak leg is `questStreak / 2` (integer floor), NOT `* 50` and NOT `* 100`: `grep -n "questStreak" contracts/modules/DegenerusGameMintStreakUtils.sol | grep -v '^#'` shows a `/ 2` form and NO `* 50`
    - No additive leg multiplies by 100 anymore: `grep -nE "\* *100" contracts/modules/DegenerusGameMintStreakUtils.sol | grep -v '^#'` returns no activity-score additive leg (the `(points*100)/3` re-scale lives in Decimator, not here)
    - The deity-pass leg references `DEITY_PASS_ACTIVITY_BONUS_POINTS` (renamed in Task 2)
    - `PASS_STREAK_FLOOR_POINTS` / `PASS_MINT_COUNT_FLOOR_POINTS` are unchanged (still 50 / 25, still used pre-`×100`)
    - The cap NUMERAL is `65_534` everywhere it appears in this file (NOT `655`)
  </acceptance_criteria>
  <verify>grep -n "ACTIVITY_SCORE_HARD_CAP_POINTS\|questStreak" contracts/modules/DegenerusGameMintStreakUtils.sol | grep -v '^#'</verify>
  <done>`_playerActivityScoreAt` returns a point-domain score, the quest-streak leg is `floor(questStreak/2)`, no `×100` additive legs remain, and the cap uses `ACTIVITY_SCORE_HARD_CAP_POINTS` with value `65_534`. No commit performed.</done>
</task>

<task type="auto">
  <name>Task 2: POINTS+STREAK+PACK — Storage renames, cap reconcile, latch widen, pendingFlip narrow, accumulator repack (DegenerusGameStorage)</name>
  <files>contracts/storage/DegenerusGameStorage.sol</files>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol — these exact regions: :135 (DEITY_PASS_ACTIVITY_BONUS_BPS), :141 (ACTIVITY_SCORE_HARD_CAP_BPS), :1525 (lootboxRngPendingFlip — OUT OF SCOPE, do not touch), :1553/:1555 (Lootbox EV input anchors), :1557/:1559/:1561 (Lootbox EV OUTPUT anchors — DO NOT TOUCH), :1633-1654 (_lootboxEvMultiplierFromScore), :2126-2162 (Sub slot doc-comments + field-section headers), :2169-2245 (the full Sub struct field declarations), :2237 (pendingFlip), :2244 (subStreakLatch), :2251 (SUB_STREAK_MASK), :2254-2261 (_streakBaseOf / _setStreakBase), :2271 (_afkingStreak cast), :2295-2304 (_effectiveQuestStreak)
    - .planning/phases/435-.../435-DESIGN-LOCK.md:129-172 (D-04 latch-widen edit table + locked outputs), :180-296 (D-06/D-07/D-08 pendingFlip narrow + repack + comment-fix table + the BEFORE/AFTER 72-bit field table), :427-433 (the consolidated 436 Edit Surface for Storage)
    - .planning/phases/436-.../436-CONTEXT.md:23-45 (D-436-01/02/03 rename rules + D-436-04/09 cap reconcile + D-436-06 latch clamp + D-436-07/08 pendingFlip clamp / affiliateBase untouched)
  </read_first>
  <action>
    **POINTS — input-anchor renames + conversions (TABLE A; D-436-01):**
    - `ACTIVITY_SCORE_HARD_CAP_BPS = 65_534` (:141) -> `ACTIVITY_SCORE_HARD_CAP_POINTS = 65_534`. ⚠ D-436-09: VALUE STAYS `65_534` (rename-only), NOT `655`. Keep the existing overflow/uint16-sentinel rationale comment (it becomes true verbatim: `65534 + 1 = 65535` fits uint16, one below max, 0 reserved as the sDGNRS unset sentinel) — reword only the unit token `bps`->`points`.
    - `DEITY_PASS_ACTIVITY_BONUS_BPS = 8000` (:135) -> `DEITY_PASS_ACTIVITY_BONUS_POINTS = 80` (8000 bps ÷ 100 = 80 pt; multiple of 100, clean).
    - `LOOTBOX_EV_ACTIVITY_NEUTRAL_BPS = 6_000` (:1553) -> `LOOTBOX_EV_ACTIVITY_NEUTRAL_POINTS = 60`.
    - `LOOTBOX_EV_ACTIVITY_MAX_BPS = 40_000` (:1555) -> `LOOTBOX_EV_ACTIVITY_MAX_POINTS = 400`.
    - DO NOT touch the Lootbox EV OUTPUT anchors `LOOTBOX_EV_MIN/NEUTRAL/MAX_BPS` (:1557/:1559/:1561 — TABLE B, keep `_BPS`, value unchanged). `_lootboxEvMultiplierFromScore` (:1633-1654): the formula SHAPE is unchanged (scale-invariant); only the two renamed input anchors change; reword the `@param score ... in basis points` natspec -> point-domain.

    **STREAK — latch widening (D-04/D-436-06):**
    - `Sub.subStreakLatch` (:2244) `uint8` -> `uint16`.
    - `SUB_STREAK_MASK` (:2251) `0xff` -> `0xffff`.
    - `_streakBaseOf` (:2254-2256) return type `uint8` -> `uint16` (the `& SUB_STREAK_MASK` now masks 16 bits).
    - `_setStreakBase` (:2259-2261): re-pin the saturating clamp `value > 255 ? 255 : uint8(value)` -> `value > type(uint16).max ? type(uint16).max : uint16(value)`. ⚠ D-436-06: KEEP the clamp (do NOT delete it) — it guards the `recordAfkingSecondary` live `+1` bump (`_setStreakBase(s, _streakBaseOf(s)+1)`) from wrapping 65536 -> 0 at the ceiling. The `255` numeral is removed; the saturating clamp shape stays. IMPL discretion: use `type(uint16).max` literal (most self-documenting).
    - `_afkingStreak` (:2271) `uint32(_streakBaseOf(sub))` cast holds (uint16 ⊂ uint32) — no change beyond the return-type follow-through.
    - `_effectiveQuestStreak` (:2295-2304) — afking-XOR-manual semantics PRESERVED; no logic change.

    **PACK — pendingFlip narrow + accumulator repack (D-06/D-07/D-436-07/08):**
    - `Sub.pendingFlip` (:2237) `uint32` -> `uint24`. (The clamp re-pin + casts are in GameAfkingModule — Task 3.)
    - `affiliateBase` stays `uint32` with its 100M clamp — UNTOUCHED (D-436-08).
    - `Sub.score` stamp (:2127) stays `uint16` (now fits exactly: cap 65_534, `65534+1=65535`).
    - The repacked accumulator is `affiliateBase(32) + pendingFlip(24) + subStreakLatch(16) = 72` bits; `Sub` stays exactly one 256-bit slot, 0 free. `lootboxRngPendingFlip` (:1525) is a SEPARATE uint40 field — DO NOT touch (D-08).

    **Comment-only fixes (part of the same diff; D-07 reconciliations):**
    - Slot doc-comment `config (40b)` (:2126) -> `(48b)`.
    - Field-section header `per-sub stamp (48 bits)` (:2182) -> `(40b)`.
    - Field-section header `markers (72 bits)` (:2194) -> `(96b)`.
    - Accumulator field comments (:2129, :2154-2162, :2230-2237): `pendingFlip(32)` -> `(24)`, `subStreakLatch(8)` -> `(16)`, `100M-whole-FLIP` notes -> `~16.7M (2^24-1)`.
    - Stale `streakAtAfkingStart (bits 0-6)` (:2144) and the `0..255` / `Clamped at 255` subStreakLatch comments (:2238-2250) -> uint16 width.
    - Comments describe what IS — no history/changed-from notes.

    Do NOT commit in this task.
  </action>
  <acceptance_criteria>
    - `grep -c "_BPS" ...` for the renamed input anchors returns 0: `grep -nE "ACTIVITY_SCORE_HARD_CAP_BPS|DEITY_PASS_ACTIVITY_BONUS_BPS|LOOTBOX_EV_ACTIVITY_NEUTRAL_BPS|LOOTBOX_EV_ACTIVITY_MAX_BPS" contracts/storage/DegenerusGameStorage.sol | grep -v '^#'` returns no matches
    - The renamed constants exist with the correct point values: `ACTIVITY_SCORE_HARD_CAP_POINTS = 65_534` (NOT 655), `DEITY_PASS_ACTIVITY_BONUS_POINTS = 80`, `LOOTBOX_EV_ACTIVITY_NEUTRAL_POINTS = 60`, `LOOTBOX_EV_ACTIVITY_MAX_POINTS = 400`
    - TABLE-B output anchors UNCHANGED: `grep -nE "LOOTBOX_EV_MIN_BPS|LOOTBOX_EV_NEUTRAL_BPS|LOOTBOX_EV_MAX_BPS" contracts/storage/DegenerusGameStorage.sol | grep -v '^#'` still shows the three `_BPS` output anchors at 9_000/10_000/14_500
    - `Sub.subStreakLatch` is `uint16`, `SUB_STREAK_MASK == 0xffff`, `_streakBaseOf` returns `uint16`, `_setStreakBase` clamp uses `type(uint16).max` (no `255` numeral in the clamp), `Sub.pendingFlip` is `uint24`, `affiliateBase` is still `uint32`, `Sub.score` is still `uint16`
    - `lootboxRngPendingFlip` (uint40) is untouched
    - Accumulator field widths total 72 (32+24+16); the four groups total 256 bits, 0 free
  </acceptance_criteria>
  <verify>grep -nE "ACTIVITY_SCORE_HARD_CAP_POINTS|DEITY_PASS_ACTIVITY_BONUS_POINTS|LOOTBOX_EV_ACTIVITY_(NEUTRAL|MAX)_POINTS|uint16 subStreakLatch|uint24 pendingFlip|SUB_STREAK_MASK" contracts/storage/DegenerusGameStorage.sol | grep -v '^#'</verify>
  <done>All four TABLE-A input anchors in Storage are renamed `_POINTS` with correct point values (cap = 65_534 per D-436-09); the latch is uint16 with the type(uint16).max clamp; pendingFlip is uint24; affiliateBase/score/lootboxRngPendingFlip untouched; the accumulator is 72 bits in one 256-bit slot, 0 free; comment-fixes applied. No commit performed.</done>
</task>

<task type="auto">
  <name>Task 3: STREAK+PACK — pendingFlip clamps/casts + latch follow-through (GameAfkingModule)</name>
  <files>contracts/modules/GameAfkingModule.sol</files>
  <read_first>
    - contracts/modules/GameAfkingModule.sol — these exact regions: :861-863 (ticket buyer-bonus accrue: newOwed/clamp/cast), :920-922 (affiliateBase clamp — UNTOUCHED), :925-929 (slot-0 quest-reward accrue: newOwed/clamp/cast), :1097-1100 (_settlePendingFlip), :521/:523/:533/:555/:573 (_setStreakBase run-start + 0-on-lapse writes), :1726 (the +1-bump saturation comment), :1734 (recordAfkingSecondary +1 bump)
    - .planning/phases/435-.../435-DESIGN-LOCK.md:194-208 (D-06 accrue/clamp/settle edit table), :435-438 (the consolidated 436 Edit Surface for GameAfkingModule)
    - .planning/phases/436-.../436-CONTEXT.md:32-35 (D-436-06 latch +1 bump rationale + D-436-07 pendingFlip clamp = type(uint24).max + D-436-08 affiliateBase untouched)
  </read_first>
  <action>
    **PACK — pendingFlip accrue clamps + casts (D-06/D-436-07):** At BOTH accrue sites — the ticket buyer-bonus block (:861-863) and the slot-0 quest-reward block (:925-929) — re-pin the saturating clamp constant `100_000_000` -> the uint24 ceiling `type(uint24).max` (= 16_777_215), and change the write cast `uint32(newOwed)` -> `uint24(newOwed)`. The clamp is `min(newOwed, ceiling)` BEFORE the cast, so the cast input is provably <= 2^24-1 and the narrowed write never truncates/wraps (lossless by construction). IMPL discretion: use the `type(uint24).max` literal (most self-documenting; the exact type ceiling makes the cast lossless by construction).

    `_settlePendingFlip` (:1097-1100): `uint256(s.pendingFlip)` widen-back holds (uint24 ⊂ uint256), the `s.pendingFlip = 0` zero-write still fits — NO logic change beyond Task 2's field type. Reword the `// Same uint32 + 100M…` comment to the new width/ceiling (`uint24` + `~16.7M (2^24-1)`).

    ⚠ DO NOT touch the `affiliateBase` clamp at :920-922 — it stays `100_000_000` on the uint32 field (D-436-08).

    **STREAK — latch follow-through (D-04/D-05/D-436-06):** The latch writes/reads `_setStreakBase`/`_streakBaseOf` (run-start :521/:523/:533/:555, 0-on-lapse :573, the in-run `+1` secondary bump `recordAfkingSecondary` :1734) follow the uint16 widening from Task 2 — NO behaviour change. The `+1` bump now saturates at 65535 instead of 255 (via the Task 2 clamp), far past where the 655-effective consumer clamp matters; reword the :1726 saturation comment to the new width (`65535`).

    Comments describe what IS — no history/changed-from notes. Do NOT commit in this task.
  </action>
  <acceptance_criteria>
    - Both pendingFlip accrue sites clamp at `type(uint24).max` and cast `uint24(newOwed)`: `grep -nE "type\(uint24\)\.max|uint24\(newOwed\)" contracts/modules/GameAfkingModule.sol | grep -v '^#'` shows exactly the two accrue blocks (2 clamps + 2 casts)
    - NO pendingFlip accrue site still uses `uint32(newOwed)` or `> 100_000_000` for pendingFlip: the only remaining `100_000_000` is the affiliateBase clamp at :920-922
    - `grep -c "100_000_000" contracts/modules/GameAfkingModule.sol` (filtered: `grep -v '^#'`) corresponds ONLY to the untouched affiliateBase clamp
    - `_settlePendingFlip` compiles with the uint24 field (no cast-width error)
    - The latch follow-through sites are unchanged in behaviour (uint16 reads/writes via the Task 2 getter/setter)
  </acceptance_criteria>
  <verify>grep -nE "type\(uint24\)\.max|uint24\(newOwed\)|uint32\(newOwed\)|100_000_000" contracts/modules/GameAfkingModule.sol | grep -v '^#'</verify>
  <done>Both pendingFlip accrue sites clamp at `type(uint24).max` with `uint24(newOwed)` casts; affiliateBase's 100M clamp is untouched; the latch follow-through rides the uint16 widening; settle holds. No commit performed.</done>
</task>

<task type="auto">
  <name>Task 4: STREAK — delete the finalize floor-hack (DegenerusQuests)</name>
  <files>contracts/DegenerusQuests.sol</files>
  <read_first>
    - contracts/DegenerusQuests.sol:543-551 (the decay logic :543-545 [KEEP], the floor-hack comment :546-548 [DELETE], the `preRun` read + restore :549-550 [DELETE], the final uint16 assignment :551 [KEEP]), :281 (PlayerQuestState.streak source — uint16), :501-511 (beginAfking), ~:1779 (the pendingFlip accrual note)
    - .planning/phases/435-.../435-DESIGN-LOCK.md:99-140 (D-04 grievance + the floor-hack anchor-note :118-122 pinning the exact lines to delete), :440-443 (the consolidated 436 Edit Surface for DegenerusQuests)
    - .planning/phases/436-.../436-CONTEXT.md (the STREAK track summary; the floor-hack is no longer needed because the uint16 latch carries the pre-run snapshot exactly)
  </read_first>
  <action>
    DELETE the finalize floor-hack in `finalizeAfking` (D-04): remove the explanatory comment (:546-548, the "uint8-clamped at 255 …" block) AND the restore lines `uint16 preRun = state.streak; if (finalStreak != 0 && finalStreak < preRun) finalStreak = preRun;` (:549-550). The uint16 latch (Task 2) now carries the pre-run snapshot EXACTLY, so `finalStreak` (= true base + funded days) already reflects the real streak and needs no restore.

    KEEP: the decay logic (:543-545) and the final assignment `state.streak = finalStreak > type(uint16).max ? type(uint16).max : uint16(finalStreak);` (:551) — that final uint16 safety clamp is a separate concern and stays.

    The streak source stays the uint16 `PlayerQuestState.streak` (:281), now symmetric with the uint16 latch — no change. `beginAfking` (:501-511) return is `uint24 streak` sourced from the uint16 field — no change. The `pendingFlip` accrual note (~:1779) stays consistent (the streak-width change does not touch accrual semantics) — comment-only consistency check, no logic change.

    Comments describe what IS — no history/changed-from notes. Do NOT commit in this task.
  </action>
  <acceptance_criteria>
    - The floor-hack is gone: `grep -nE "preRun|Clamped at 255|uint8-clamped" contracts/DegenerusQuests.sol | grep -v '^#'` returns no match for `preRun` and no surviving floor-hack comment
    - The final uint16 assignment is RETAINED: `grep -nE "type\(uint16\)\.max" contracts/DegenerusQuests.sol | grep -v '^#'` still shows the `state.streak = ... ? type(uint16).max : uint16(finalStreak)` safety clamp at the finalize site
    - The decay logic (:543-545) is unchanged
    - `PlayerQuestState.streak` (:281) is still uint16; `beginAfking` is unchanged
  </acceptance_criteria>
  <verify>grep -nE "preRun|type\(uint16\)\.max" contracts/DegenerusQuests.sol | grep -v '^#'</verify>
  <done>The `preRun` restore block + its comment are deleted; the decay logic and the final uint16 safety clamp are retained; the streak source stays uint16. No commit performed.</done>
</task>

<task type="auto">
  <name>Task 5: POINTS — consumer thresholds: Degenerette anchors + Decimator anchor & the (points*100)/3 multiplier re-scale</name>
  <files>contracts/modules/DegenerusGameDegeneretteModule.sol, contracts/modules/DegenerusGameDecimatorModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameDegeneretteModule.sol:188/:191/:194 (MID/HIGH/MAX input anchors), :197-206 (ROI output anchors — DO NOT TOUCH), :214/:217 (WWXRP output anchors — DO NOT TOUCH), :1141-1170 (_roiBpsFromScore incl. the :1148-1154 QUADRATIC low leg with coeffs 1000/500 — DO NOT convert the coeffs), :1179-1190 (_wwxrpHighRoiFromScore)
    - contracts/modules/DegenerusGameDecimatorModule.sol:104 (BPS_DENOMINATOR — DO NOT TOUCH), :770-771 (BUCKET_BASE/MIN_BUCKET — bucket counts, DO NOT convert), :772 (TERMINAL_DEC_ACTIVITY_CAP_BPS), :793/:913 (the playerActivityScore self-calls), :796-797/:916-917 (the clamps), :799-801/:913-919 (the multiplier `BPS_DENOMINATOR + bonusBps/3`), :1133-1144 (_terminalDecBucket)
    - .planning/phases/435-.../435-DESIGN-LOCK.md:318-345 (TABLE A / TABLE B), :353-413 (DESIGN-04 per-consumer scale-invariance proof, esp. :383-385 the Decimator multiplier re-scale + :379-382 the bucket), :445-455 (the consolidated 436 Edit Surface for Degenerette + Decimator)
    - .planning/phases/436-.../436-CONTEXT.md:24-25 (D-436-01/02: rename input anchors, keep output bps)
  </read_first>
  <action>
    **Degenerette (TABLE A renames + conversions; D-436-01):**
    - `ACTIVITY_SCORE_MID_BPS = 7_500` (:188) -> `ACTIVITY_SCORE_MID_POINTS = 75`.
    - `ACTIVITY_SCORE_HIGH_BPS = 25_500` (:191) -> `ACTIVITY_SCORE_HIGH_POINTS = 255`.
    - `ACTIVITY_SCORE_MAX_BPS = 30_500` (:194) -> `ACTIVITY_SCORE_MAX_POINTS = 305`.
    - DO NOT convert/rename TABLE B: `ROI_MIN/MID/HIGH/MAX_BPS` (:197-206), `WWXRP_HIGH_ROI_BASE/MAX_BPS` (:214/:217), the quadratic coefficients `1000`/`500` (:1152-1153), `DEGEN_DGNRS_*_BPS`. The `_roiBpsFromScore` (:1141-1170, incl. the QUADRATIC low leg) and `_wwxrpHighRoiFromScore` (:1179-1190) formula SHAPES are unchanged (scale-invariant: the 100 / 100^2 cancels); only the three renamed input anchors change; reword the `@param score ... in basis points` natspecs -> point-domain.

    **Decimator (TABLE A rename + the one non-trivial re-scale; D-436-01 + D-04.2):**
    - `TERMINAL_DEC_ACTIVITY_CAP_BPS = 23_500` (:772) -> `TERMINAL_DEC_ACTIVITY_CAP_POINTS = 235`. The clamps `if (bonusBps > CAP) bonusBps = CAP` (:796-797, :916-917) compare against the converted CAP — shape unchanged.
    - ⚠ MULTIPLIER RE-SCALE (the ONE non-scale-invariant migration): `multBps = BPS_DENOMINATOR + (bonusBps / 3)` (:799-801 and the keep-alive mirror in :913-919) MUST be re-expressed so the `/3` divisor sees the bps-equivalent magnitude — `BPS_DENOMINATOR + (points * 100) / 3` (multiply the point score back up by 100 BEFORE the `/3`). A naive `points/3` is ~100× wrong (off by ~100×) and would collapse the active-vs-inactive spread. `BPS_DENOMINATOR = 10_000` (:104, TABLE B) UNCHANGED.
    - `_terminalDecBucket` (:1133-1144): `(range · score + CAP/2) / CAP` is scale-invariant; `range = BUCKET_BASE − MIN_BUCKET = 10` is a BUCKET COUNT (dimensionless) — DO NOT convert (:770-771). Only the CAP constant in the denominator follows TABLE A; shape unchanged.
    - The two self-calls read the point-domain `playerActivityScore` (:793, :913) — they now consume points after the boundary return changes; no edit, attested in 438.

    Comments describe what IS — no history/changed-from notes. Do NOT commit in this task.
  </action>
  <acceptance_criteria>
    - Degenerette input anchors renamed: `grep -nE "ACTIVITY_SCORE_(MID|HIGH|MAX)_BPS" contracts/modules/DegenerusGameDegeneretteModule.sol | grep -v '^#'` returns no match; `ACTIVITY_SCORE_MID_POINTS = 75`, `ACTIVITY_SCORE_HIGH_POINTS = 255`, `ACTIVITY_SCORE_MAX_POINTS = 305` exist
    - Degenerette TABLE B untouched: `ROI_MIN/MID/HIGH/MAX_BPS` (9_000/9_500/9_950/9_990), `WWXRP_HIGH_ROI_BASE/MAX_BPS` (9_000/10_990), and the quadratic coeffs 1000/500 are unchanged
    - Decimator CAP renamed: `grep -nE "TERMINAL_DEC_ACTIVITY_CAP_BPS" contracts/modules/DegenerusGameDecimatorModule.sol | grep -v '^#'` returns no match; `TERMINAL_DEC_ACTIVITY_CAP_POINTS = 235` exists
    - Decimator multiplier RE-SCALE present: `grep -nE "100\) */ *3|100 */ *3" contracts/modules/DegenerusGameDecimatorModule.sol | grep -v '^#'` shows the `(points * 100) / 3` form at BOTH multiplier sites (burn :799-801 + keep-alive mirror); NO bare `points / 3` or surviving `bonusBps / 3` on a point-domain value
    - `BPS_DENOMINATOR = 10_000` and `BUCKET_BASE`/`MIN_BUCKET` (range = 10) unchanged
  </acceptance_criteria>
  <verify>grep -nE "ACTIVITY_SCORE_(MID|HIGH|MAX)_POINTS|TERMINAL_DEC_ACTIVITY_CAP_POINTS" contracts/modules/DegenerusGameDegeneretteModule.sol contracts/modules/DegenerusGameDecimatorModule.sol | grep -v '^#'; echo '--- rescale ---'; grep -nE "100\) */ *3|100 */ *3" contracts/modules/DegenerusGameDecimatorModule.sol | grep -v '^#'</verify>
  <done>Degenerette MID/HIGH/MAX renamed to point values; Decimator CAP renamed to 235; the Decimator multiplier uses the `(points*100)/3` re-scale at both sites; all TABLE-B output bps + bucket counts untouched. No commit performed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 6: Empirical pre-approval gate + single atomic USER-gated contract commit</name>
  <files>contracts/modules/DegenerusGameMintStreakUtils.sol, contracts/storage/DegenerusGameStorage.sol, contracts/modules/GameAfkingModule.sol, contracts/DegenerusQuests.sol, contracts/modules/DegenerusGameDegeneretteModule.sol, contracts/modules/DegenerusGameDecimatorModule.sol</files>
  <read_first>
    - .planning/phases/436-.../436-CONTEXT.md:40-44 (Claude's discretion: the empirical pre-approval gate + diff presentation)
    - .planning/phases/435-.../435-DESIGN-LOCK.md:260-264 (D-07 EIP-170 + layout-golden note: the Sub intra-slot field move is the EXPECTED new golden, recaptured in 438 — NOT a drift here), :457-462 (DO-NOT-TOUCH list — final cross-check)
    - The MEMORY rule: only `contracts/*.sol` commits need approval; the commit-guard PreToolUse hook blocks ALL commits while `contracts/*.sol` is dirty (bypass: `CONTRACTS_COMMIT_APPROVED=1`). The commit is NEVER pre-approved.
  </read_first>
  <what-built>
    Tasks 1-5 produced the batched POINTS + STREAK + PACK diff across six `.sol` files (plus comment-only edits in the same files). This task runs the empirical pre-approval gate, then — and ONLY after explicit USER hand-review and approval — performs the SINGLE atomic contract commit.

    BEFORE presenting the diff, run the empirical gate (all must pass):
    1. **forge build clean** — `forge build` exits 0 with no errors (warnings tolerated if pre-existing).
    2. **EIP-170 ceiling** — the `DegenerusGame` deployed bytecode stays under 24,576 bytes (PACK-01 requires the re-check; the Game is perpetually near the ceiling). Use `forge inspect DegenerusGame deployedBytecode` byte-length (or the repo's existing EIP-170 size check) and report the byte count + headroom.
    3. **Sub storage-layout slot sanity** — `forge inspect <contract-defining-Sub> storage-layout` (or the equivalent) confirms `Sub` stays exactly ONE 256-bit slot, 0 free, with the accumulator `affiliateBase(32)+pendingFlip(24)+subStreakLatch(16)=72`. The intra-slot field offsets MOVING is the INTENDED repack — the NEW layout golden is recaptured in 438 REAUDIT-01, NOT here; this gate only confirms no NEW slot was added and no collision.
    4. **Baseline-parity smoke** — a fast targeted smoke (e.g. `forge test --match-contract` on an activity-score / streak / afking touchpoint, NOT the full suite per the user's "don't run the whole forge suite for small changes" rule) confirms nothing structurally broke. This is NOT the behavioural proof — that is 437 TST / 438 REAUDIT.

    Then present the batched diff for hand-review, GROUPED BY THE THREE TRACKS (POINTS / STREAK / PACK), with a per-file change map and per-line TABLE-A-convert / TABLE-B-do-not-convert annotations, so the single review is auditable. Explicitly confirm in the presentation: the cap is `65_534` (D-436-09, NOT 655); `affiliateBase`, `Sub.score`, `lootboxRngPendingFlip`, and all TABLE-B output bps are untouched.
  </what-built>
  <how-to-verify>
    1. Confirm the empirical gate results: `forge build` exit 0; Game deployed bytecode byte-count < 24,576 (report headroom); `Sub` one 256-bit slot 0-free with the 72-bit accumulator; baseline-parity smoke green.
    2. Review the batched diff grouped POINTS / STREAK / PACK. Spot-check: the quest-streak leg is `floor(questStreak/2)`; the cap constant is `ACTIVITY_SCORE_HARD_CAP_POINTS = 65_534`; the six TABLE-A anchors are renamed `_POINTS` with the correct point values; the Decimator multiplier is `(points*100)/3`; the latch is uint16 with the `type(uint16).max` clamp; the floor-hack is deleted; pendingFlip is uint24 with `type(uint24).max` clamps; affiliateBase + lootboxRngPendingFlip + all TABLE-B output bps untouched.
    3. If approved: the executor performs ONE atomic commit of ALL six `.sol` files with `CONTRACTS_COMMIT_APPROVED=1` set for the commit command (the commit-guard hook bypass). The `.planning` docs (this PLAN + SUMMARY) commit separately/autonomously (force-add — `.planning/` is gitignored).
  </how-to-verify>
  <acceptance_criteria>
    - `forge build` exits 0
    - `DegenerusGame` deployed bytecode < 24,576 bytes (EIP-170), with the measured byte count + headroom reported (PACK-01)
    - `Sub` occupies exactly one 256-bit slot, 0 free; the accumulator is `affiliateBase(32)+pendingFlip(24)+subStreakLatch(16)=72`; no new slot added, no collision
    - Baseline-parity smoke green on the touched-surface match-contract run
    - The diff is presented grouped POINTS / STREAK / PACK with the per-file map + TABLE-A/TABLE-B annotations
    - EXACTLY ONE atomic commit contains all six `.sol` files; no `contracts/*.sol` was committed in any earlier task
    - The commit used the `CONTRACTS_COMMIT_APPROVED=1` bypass and was performed ONLY after explicit USER approval (never pre-approved)
  </acceptance_criteria>
  <action>Run the empirical pre-approval gate (forge build clean; EIP-170 `DegenerusGame` deployed bytecode < 24,576 bytes; `Sub` one 256-bit slot 0-free with the 72-bit accumulator; touched-surface baseline-parity smoke). Then present the batched diff grouped POINTS / STREAK / PACK with a per-file change map and TABLE-A-convert / TABLE-B-do-not-convert annotations. ONLY after explicit USER hand-review approval, perform ONE atomic commit of all six `.sol` files using the `CONTRACTS_COMMIT_APPROVED=1` commit-guard bypass. Never pre-approve; no `.sol` was committed in any earlier task.</action>
  <verify>forge build exits 0; `DegenerusGame` deployed bytecode byte-count < 24,576 (report headroom); `Sub` = one 256-bit slot, 0 free; baseline smoke green; exactly one atomic `contracts/*.sol` commit after USER approval.</verify>
  <done>The empirical gate passed, the diff was presented grouped by track, USER approved, and all six `.sol` files landed in ONE atomic commit via `CONTRACTS_COMMIT_APPROVED=1`.</done>
  <resume-signal>Type "approved" to perform the single atomic contract commit, or describe required diff changes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storage slot ↔ field widths | The repacked `Sub` accumulator (`affiliateBase(32)+pendingFlip(24)+subStreakLatch(16)`) shares one 256-bit slot; a width-arithmetic error collides fields or overflows the slot |
| accrue write → narrowed field | `pendingFlip` accrue (untrusted-magnitude buyer bonus + per-day reward) crosses into a uint24 field; an unclamped cast wraps |
| live `+1` bump → uint16 latch ceiling | `recordAfkingSecondary` does `_setStreakBase(s, _streakBaseOf(s)+1)`; at the ceiling a bare cast wraps and silently resets the streak |
| external `playerActivityScore` return | Domain changes bps→points; on-chain consumers all clamp (no behaviour change) but the value feeds the RNG-freeze consumers and the sDGNRS snapshot |
| deployed bytecode ↔ EIP-170 | The Game is perpetually near 24,576 bytes; the diff must not push it over |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-436-01 | Tampering | `Sub` accumulator repack (Storage:2229-2244) | mitigate | Task 2 keeps widths summing to exactly 72 (32+24+16); Task 6 storage-layout sanity confirms `Sub` stays one 256-bit slot, 0 free, no new slot/collision; full layout-golden recapture deferred to 438 REAUDIT-01 (the intra-slot move is the EXPECTED new golden, not a drift) |
| T-436-02 | Tampering / DoS | `pendingFlip` uint24 cast (GameAfkingModule:863,:928) | mitigate | Task 3 clamps to `type(uint24).max` BEFORE the `uint24(newOwed)` cast at both accrue sites — lossless by construction; 437 TST-02 owns the saturation property (clamps, never wraps) |
| T-436-03 | Tampering | uint16 latch `+1` bump (GameAfkingModule:1734 via Storage:_setStreakBase) | mitigate | Task 2 KEEPS the saturating clamp re-pinned to `type(uint16).max` (D-436-06) so the live `+1` at the 65535 ceiling saturates instead of wrapping 65536→0 (silent streak reset) |
| T-436-04 | Information disclosure / Tampering | external `playerActivityScore` bps→points (boundary) | accept (this phase) / mitigate (438) | On-chain consumers all clamp (EV≤400, ROI/WWXRP≤305, Decimator≤235) so no on-chain behaviour change; the RNG-freeze re-attestation + sDGNRS point-domain re-confirm + off-chain indexer re-vendor are explicitly deferred to 438 REAUDIT-02 per the design-lock 438 checklist |
| T-436-05 | Denial of service | EIP-170 deployed-bytecode ceiling | mitigate | Task 6 measures `DegenerusGame` deployed bytecode < 24,576 bytes as a blocking acceptance criterion (PACK-01); the diff is register-level (width swaps, constant-value changes, one `×100` re-scale) and expected net-neutral; 438 re-attests |
| T-436-06 | Tampering | Decimator multiplier `(points*100)/3` re-scale (DecimatorModule:799-801,:913-919) | mitigate | Task 5 re-expresses the multiplier to `(points*100)/3` so the `/3` divisor sees bps-equivalent magnitude (a naive `points/3` is ~100× wrong); 437 TST-03 + 438 re-attest the consumer equivalence |
| T-436-SC | Tampering | npm/forge dependency installs | accept | No new package-manager installs in this phase — it is a pure source edit of existing `.sol` files against the frozen baseline; no Package Legitimacy Gate required |

**438 deferral note:** the full RNG-freeze proof, mutation campaign, deep-invariant suite, and storage-layout golden re-attestation against the repacked point-domain subject are explicitly deferred to 438 REAUDIT (v68 methodology carries forward), per the design-lock's "438 RNG-Freeze Re-Audit Checklist." This phase's threat mitigations are the source-correctness + build + EIP-170 + slot-sanity gate only.
</threat_model>

<verification>
Phase-level checks (the empirical pre-approval gate in Task 6 is the consolidated gate):
- `forge build` exits 0.
- `DegenerusGame` deployed bytecode < 24,576 bytes (EIP-170, PACK-01) — measured byte count + headroom reported.
- `Sub` stays exactly one 256-bit slot, 0 free; accumulator = `affiliateBase(32)+pendingFlip(24)+subStreakLatch(16)=72`; no new slot, no collision.
- Source assertions (per-task `<acceptance_criteria>`): six TABLE-A anchors renamed `_POINTS` with correct point values; cap = `65_534` (D-436-09); quest-streak leg = `floor(questStreak/2)`; Decimator multiplier = `(points*100)/3`; latch uint16 + `type(uint16).max` clamp; pendingFlip uint24 + `type(uint24).max` clamps; floor-hack deleted; all TABLE-B output bps + affiliateBase + Sub.score + lootboxRngPendingFlip untouched.
- Baseline-parity smoke green on the touched-surface match-contract run (NOT the full suite).
- EXACTLY ONE atomic `contracts/*.sol` commit (Task 6), USER-approved with `CONTRACTS_COMMIT_APPROVED=1`; no intermediate `.sol` commit anywhere.
</verification>

<success_criteria>
1. The activity score is computed in whole points: `_playerActivityScoreAt` has no `×100` additive legs, the quest-streak leg is `floor(questStreak/2)`, and the cap is `ACTIVITY_SCORE_HARD_CAP_POINTS = 65_534`. (POINTS-01)
2. Every consumer threshold is migrated to the point domain: the six TABLE-A input anchors are renamed `_POINTS` with point values; Lootbox EV multiplier, Degenerette ROI/WWXRP, and Decimator read the point-domain score; the Decimator multiplier uses the `(points*100)/3` re-scale; all TABLE-B output bps are unchanged. (POINTS-02)
3. The manual + afking `subStreakLatch` streak base feeds the score through one exact integer path (uint16 latch, no fractional/bps intermediate), preserving `_effectiveQuestStreak` afking-XOR-manual semantics. (STREAK-01)
4. The pre-streak cap is reworked: the latch is uint16 with the `type(uint16).max` saturating clamp, the finalize floor-hack is deleted, and the `DegenerusQuests` streak source + accrual stay consistent. (STREAK-02)
5. `Sub.pendingFlip` is uint24 with both accrue clamps re-pinned to `type(uint24).max`, the 72-bit accumulator is repacked (one 256-bit slot, 0 free, no field range violated), and the Game stays under the EIP-170 ceiling. (PACK-01)
6. All `.sol` edits land in ONE atomic, USER-approved commit (the milestone's sole approval gate); no intermediate `contracts/*.sol` commit.
</success_criteria>

<output>
Create `.planning/phases/436-impl-batched-contract-diff-points-streak-pack-contract-commi/436-01-SUMMARY.md` when done. Record: the per-track per-file change map, the empirical-gate results (forge build, EIP-170 byte count + headroom, Sub slot sanity, baseline smoke), the D-436-09 reconciliation confirmation (cap = 65_534), the single-commit SHA, and the handoff note that 437 TST + 438 REAUDIT run against this new byte-frozen subject.
</output>
