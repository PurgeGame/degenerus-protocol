---
phase: 328-terminal-delta-audit-3-skill-adversarial-sweep-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/328-terminal-delta-audit-3-skill-adversarial-sweep-closure/328-01-DELTA-AUDIT.md
autonomous: true
requirements: [BATCH-03]

must_haves:
  truths:
    - "Every one of the 12 contract files changed vs the v47.0 baseline da5c9d50 is enumerated and attested NON-WIDENING"
    - "All 7 v48.0 work-item surfaces (PFIX, RFALL, KEEP, POOL, BTOMB, HERO, SWAP) carry a NON-WIDENING verdict backed by grep/diff evidence and each delta change maps to exactly one surface"
    - "The two v47-deferred findings F-47-01 (presale closing-box DGNRS over-distribution) + F-47-02 (redemption submit ETH-empty stETH-fallback gap) are recorded RESOLVED-AT-V48"
    - "The v48 foundry regression baseline 632/42 is attested NON-WIDENING (net-zero new regression vs the 326-08 594/42 baseline; Hardhat PASS_ALL GREEN after the HERO-04 landing)"
    - "git diff 1575f4a9 -- contracts/ is empty (zero contract mutation in this phase; subject frozen at 1575f4a9)"
  artifacts:
    - path: ".planning/phases/328-terminal-delta-audit-3-skill-adversarial-sweep-closure/328-01-DELTA-AUDIT.md"
      provides: "The SC1 delta-audit log — per-surface NON-WIDENING disposition table + composition matrix + regression-baseline attestation + F-47-01/F-47-02 RESOLVED-AT-V48 dispositions"
      contains: "Delta-Surface Table"
  key_links:
    - from: "328-01-DELTA-AUDIT.md"
      to: "git diff da5c9d50..1575f4a9 -- contracts/"
      via: "per-file delta enumeration"
      pattern: "NON-WIDENING"
---

<objective>
Author the SC1 DELTA AUDIT: enumerate every contract surface changed by the v48.0 batched diff relative to the v47.0 baseline (`MILESTONE_V47_AT_HEAD_da5c9d50989707c8964a9411e68c51ca1b1a25f2`), attest each NON-WIDENING, map each delta change to exactly one of the 7 v48 surfaces, and record the two v47-deferred findings (F-47-01, F-47-02) as RESOLVED-AT-V48. This is a read-only analysis — it produces ONE markdown log (`328-01-DELTA-AUDIT.md`) and makes ZERO `contracts/*.sol` edits. The audit subject is FROZEN at HEAD `1575f4a9`.

Purpose: SC1 of the terminal phase. Mirrors the v47 §3.A/§3.B/§5 delta-surface + composition + regression structure so the findings deliverable (328-03) can fold this log into §3/§5.
Output: `328-01-DELTA-AUDIT.md` — a per-surface disposition table covering all 7 work items + a composition attestation matrix + the regression-baseline attestation + the F-47-01/F-47-02 RESOLVED-AT-V48 dispositions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@audit/FINDINGS-v47.0.md
@test/REGRESSION-BASELINE-v48.md

<interfaces>
<!-- The frozen audit subject. Executor reads these directly — no exploration needed. -->
- Audit subject HEAD: `1575f4a9` (= the Phase 326 IMPL batched diff `f50cc634` + the Phase 327 HERO-04 byte-reproduced Degenerette payout finals constant-landing `1575f4a9`). The HERO-04 landing is constant-only — 15 byte-reproduced finals into `DegenerusGameDegeneretteModule.sol`, 0 storage impact (USER-approved hand-review). Both are part of the FROZEN subject; do NOT plan any further contract landing.
- Audit baseline: v47.0 closure HEAD `da5c9d50989707c8964a9411e68c51ca1b1a25f2` (signal `MILESTONE_V47_AT_HEAD_da5c9d50989707c8964a9411e68c51ca1b1a25f2`).
- The delta-surface set = 12 contract files (`git diff da5c9d50..1575f4a9 -- contracts/`, 611 insertions / 324 deletions): AfKing.sol (146), BurnieCoin.sol (+23), DegenerusGame.sol (160), DegenerusVault.sol (+29), StakedDegenerusStonk.sol (39), interfaces/IDegenerusGame.sol (6), interfaces/IDegenerusGameModules.sol (+12), modules/DegenerusGameDegeneretteModule.sol (311), modules/DegenerusGameGameOverModule.sol (+10), modules/DegenerusGameLootboxModule.sol (15), modules/DegenerusGameMintModule.sol (116), modules/DegenerusGameMintStreakUtils.sol (+68).
- The two mainnet contract commits in the delta range (`git log da5c9d50..1575f4a9 -- contracts/`): `f50cc634` (feat(326): v48 batched contract diff — PFIX/RFALL/KEEP/POOL/BTOMB/HERO/SWAP) + `1575f4a9` (feat(327): land byte-reproduced Degenerette payout finals + enforce neutral baseline EV). `AfKing.sol` IS changed by the diff (the keeper rename autoBuy/autoOpen/autoResolve, item 3 KEEP) — note this distinction vs the SPEC text "AfKing.sol UNCHANGED" which refers ONLY to item 4 POOL (the recovery interface adds do not modify AfKing's logic).
- Final foundry regression baseline (327-06 ledger `test/REGRESSION-BASELINE-v48.md`): 632 pass / 42 fail of 674. Baseline computed against the 326-08 594/42 baseline: +38 NEW_PASSING (all from the 5 wave-1 test files + the redemption invariant extension), +0 net-new failures. The 42 reds classify into named buckets: bucket A = 8 VRF/RNG pre-existing (VRFPathInvariants 3 + VRFCore 1 + RngLockDeterminism 1 + VRFPathCoverage 1 + 2 more) + bucket B = 34 stale-harness/v48-behavioral + bucket C = 0. Hardhat PASS_ALL byte-reproduce gate: was 15/20-diverge RED pre-landing → 0-diff GREEN at `1575f4a9` (per-N EV == 100, ETH bonus == 5.000%); the byte-reproduce red was Hardhat-only and is now resolved.
- The 7 v48 work-item -> surface map (REQUIREMENTS.md categories):
  1. PFIX (F-47-01 fix): LootboxModule (`_presaleBoxDgnrsReward` divisor `1_000→400`, base `poolStart/40`) — ISOLATED.
  2. RFALL (F-47-02 fix): StakedDegenerusStonk (`_submitGamblingClaimFrom` maxIncrement pull, pure-ETH-OR-pure-stETH segregation) + DegenerusGame (`pullRedemptionReserve` coverage branch, fail-closed, donation-robust).
  3. KEEP (keeper rename + VAULT-code): AfKing.sol (`sweep→autoBuy`, `crankBets→autoResolve`, `crankBoxes→autoOpen`; crank/sweep/do-work purged) + DegenerusGame (renamed crank entrypoints) + DegenerusVault (`affiliateCode` pass-through `bytes32("DGNRS")` two-tier 75/20/5) + IDegenerusGame/IDegenerusGameModules interface renames + MintStreakUtils.
  4. POOL (AfKing pool recovery): DegenerusVault (`recoverAfKingPool()`) + StakedDegenerusStonk (`receive()` AF_KING relax + `burnAtGameOver` pool-recover) + the IAfKing/IAfKingSubscribe `withdraw(uint256)`/`poolOf(address)` interface adds (AfKing.sol's recovery-interface logic UNCHANGED).
  5. BTOMB (gameover tombstone): BurnieCoin.sol (`_supply.vaultAllowance` +1e36 one-shot, checked add/cap) + GameOverModule (`gameOver()` one-shot bump via GAME-gated escrow path).
  6. HERO (Degenerette hero=2pt rescale): DegeneretteModule (`_countMatches→_score(...)∈{0..9}`, `S=A+2H`, standalone multiplier net-deleted, per-N tables recalibrated to basePayoutEV=100 centi-x, S=9≡old M=8 relabel, byte-reproduced finals).
  7. SWAP (sDGNRS far-future salvage swap): DegenerusGame (`sellFarFutureTickets(...)` + `_removeFarFutureTickets` swap-pop, operator-honor, rngLocked-gated, d-curve + settled-word jitter, ticket floor, inline fail-closed claimable debit) + DegenerusVault (`gameSellFarFutureTickets onlyVaultOwner` wrapper) + IDegenerusGame interface.
- F-47-01 (the v47-deferred MEDIUM): presale closing-box DGNRS over-distribution (~60% windfall to the closing buyer because the per-box draw did not scale for the 40% DGNRS branch rate). Fix LANDED at v48 = `_presaleBoxDgnrsReward` divisor `1_000→400` (base `poolStart/40`); record RESOLVED-AT-V48 with the PFIX-01 anchor + the PFIX-02/03 empirical dust-bound proof (327-01).
- F-47-02 (the v47-deferred MEDIUM): redemption submit ETH-empty stETH-fallback gap (`pullRedemptionReserve` segregated from `claimableWinnings[SDGNRS]` ALONE, fail-closed, no fallback to sDGNRS's stETH/ETH balance → mid-game ETH depletion could brick submit). Fix LANDED at v48 = pure-ETH OR pure-stETH (no mix), mid-game ETH→stETH fallback, revert if neither covers, donation-robust. Record RESOLVED-AT-V48 with the RFALL-01/02/03 anchors + the RFALL-05 regression proof (327-02).
</interfaces>
</context>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan introduces NO new code or attack surface — it is a read-only delta audit producing one markdown log. The audit SUBJECT's security is what is being audited, NOT modified: the v48.0 contract surfaces (PFIX/RFALL/KEEP/POOL/BTOMB/HERO/SWAP), already frozen at `1575f4a9`. This phase makes ZERO `contracts/` edits. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-328-01-RO | Tampering | this plan's deliverable | accept | Read-only analysis; the executor never edits `contracts/*.sol` — verified by `git diff 1575f4a9 HEAD -- contracts/` being empty. No new attack surface. |
| T-328-01-WIDEN | Elevation | v48 delta surfaces vs v47 | mitigate | The audit itself IS the mitigation: every changed surface is attested NON-WIDENING with grep/diff evidence before closure can proceed; each delta change maps to exactly one of the 7 v48 surfaces. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate the 12-file delta surface and attest each NON-WIDENING (mapped to the 7 surfaces)</name>
  <read_first>
    - audit/FINDINGS-v47.0.md (the §3.A Delta-Surface Table format + §3.B Composition Attestation Matrix to mirror)
    - .planning/REQUIREMENTS.md (the 40 v48.0 REQ rows: PFIX-01..03, RFALL-01..05, KEEP-01..05, POOL-01..06, BTOMB-01..03, HERO-01..06, SWAP-01..09, BATCH-01..03 — each maps to a surface)
    - .planning/ROADMAP.md (Phase 326 SC1-SC7 + Phase 327 SC1-SC5 — the per-item IMPL + TST attestations to cross-reference)
    - the frozen subject files via `git show 1575f4a9:contracts/<file>` and `git diff da5c9d50 1575f4a9 -- contracts/<file>` for each of the 12 files
  </read_first>
  <files>.planning/phases/328-terminal-delta-audit-3-skill-adversarial-sweep-closure/328-01-DELTA-AUDIT.md</files>
  <action>
    Run `git diff da5c9d50989707c8964a9411e68c51ca1b1a25f2..1575f4a9 -- contracts/` and build a per-surface disposition table grouped by the 7 work items (the surface map in the interfaces block). For each of the 7 surfaces, attest NON-WIDENING with concrete grep/diff evidence drawn from the frozen subject `1575f4a9` (read via `git show`/`git diff`/`grep`):
    - PFIX (item 1, F-47-01 fix): confirm `_presaleBoxDgnrsReward` divisor moved `1_000→400` (base `poolStart/40`) in `DegenerusGameLootboxModule.sol`, the inline `base` derivation comment updated, the tier shape preserved (tier-1 still 3× tier-5 DGNRS-per-ETH), and the `transferFromPool` clamp held — an ISOLATED edit touching no other item's surface. Cite the PFIX-02/03 dust-bound proof (327-01 `test/fuzz/PresaleBoxDrain.t.sol`).
    - RFALL (item 2, F-47-02 fix): confirm the 175% reservation segregates pure-ETH OR pure-stETH (no mix), falls back to pure-stETH when ETH can't cover, reverts fail-closed if neither covers (`StakedDegenerusStonk.sol` `_submitGamblingClaimFrom` maxIncrement pull + `DegenerusGame.sol` `pullRedemptionReserve` coverage branch); confirm donation-robust (coverage checked against the same asset basis the base is inflated by). Cite the RFALL-05 regression proof (327-02).
    - KEEP (item 3): confirm `crank`/`sweep`/`do-work` purged from AfKing.sol + the in-game entrypoints (grep ZERO in mainnet code AND comments via `git show 1575f4a9:contracts/AfKing.sol | grep -niE 'crank|sweep|do.?work'` returning 0 — use `grep -v` comment-filter only if a doc survivor needs calling out); confirm `autoBuy`/`autoOpen`/`autoResolve` present; confirm `creditFlip`/`BOUNTY_ETH_TARGET` KEPT; confirm AfKing passes `bytes32("DGNRS")` two-tier 75/20/5 affiliate routing into `game.purchase(...)` (was `0`) per the USER-LOCKED KEEP-04 owner attribution (SDGNRS primary / VAULT secondary).
    - POOL (item 4): confirm VAULT permissionless `recoverAfKingPool()` → `afKing.withdraw(afKing.poolOf(address(this)))`; sDGNRS `receive()` relaxed to accept `AF_KING` in addition to `GAME`; sDGNRS auto-recovers in `burnAtGameOver()` BEFORE the `balanceOf(this)==0` early-return; the `IAfKing`/`IAfKingSubscribe` `withdraw(uint256)`/`poolOf(address)` interface adds match AfKing's signatures verbatim (AfKing's recovery-interface LOGIC unchanged — the only AfKing diff is the item-3 rename). Cite the POOL-04 `address(this).balance` accounting-safety proof (327-02).
    - BTOMB (item 5): confirm `gameOver()` one-shot bumps BURNIE's `_supply.vaultAllowance` by 1e36 wei via the GAME-gated escrow/allowance path with a checked add/cap (no uint128 overflow, strictly one-shot latch); confirm `totalSupply()` untouched. Cite the BTOMB-03 non-circulating proof (327-03 `test/fuzz/BurnieTombstone.t.sol`).
    - HERO (item 6): confirm `_countMatches→_score(...)∈{0..9}` (`S=A+2H`), standalone multiplier net-deleted (`_applyHeroMultiplier`/`HERO_BOOST_*`/`HERO_PENALTY`/`HERO_SCALE` grep ZERO; `heroQuadrant>=4` revert + `FT_HERO_SHIFT` decode kept), per-N tables recalibrated to basePayoutEV=100 centi-x, S=9≡old M=8 relabel preserved, the 15 byte-reproduced finals landed (the `1575f4a9` constant-only landing). Cite the HERO-04 PASS_ALL byte-reproduce gate (327-04, Hardhat 0-diff GREEN) + the HERO-06 write-batch DGAS equivalence + dailyHeroWagers no-leak.
    - SWAP (item 7): confirm `sellFarFutureTickets(...)` + `_removeFarFutureTickets` swap-pop (operator-honor via `_resolvePlayer`, `rngLocked`-gated, d-curve `6≤d≤100`, settled-word jitter, ticket-floor-first, inline fail-closed `claimableWinnings[SDGNRS]` debit down to ≥1 ETH floor, swap-pop maintaining `membership ⟺ packed != 0`) + the VAULT `gameSellFarFutureTickets onlyVaultOwner` wrapper. Cite the SWAP-08 no-arb-at-ceiling proof (327-05, margin ~4.5pp @d6) + SWAP-09 solvency.
    Mirror the v47 §3.A table columns: Surface | Requirements | Re-grepped anchors @ `1575f4a9` | Disposition. Add a Composition Attestation Matrix mirroring v47 §3.B (each delta change maps to exactly ONE of the 7 surfaces; claimable-balance preserved; BURNIE-net / tombstone non-circulating; RNG-freeze-intact: the SWAP jitter is seeded from a SETTLED past VRF word and the Degenerette write-batch is bookkeeping-only post-outcome). DO NOT edit any contracts/*.sol file — read-only via `git show`/`git diff`/`grep`.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && test -z "$(git diff 1575f4a9 HEAD -- contracts/)" && grep -c "NON-WIDENING" .planning/phases/328-terminal-delta-audit-3-skill-adversarial-sweep-closure/328-01-DELTA-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - 328-01-DELTA-AUDIT.md enumerates all 12 changed contract files (from `git diff da5c9d50..1575f4a9 -- contracts/`), grouped by the 7 work items (PFIX/RFALL/KEEP/POOL/BTOMB/HERO/SWAP), each surface carrying a NON-WIDENING verdict backed by a concrete grep/diff anchor @ `1575f4a9`.
    - Each delta change maps to exactly one of the 7 surfaces (the composition matrix attests no orphan hunks).
    - The keeper-rename kill-set (crank/sweep/do-work) is shown grep-ZERO in mainnet AfKing.sol + the in-game entrypoints (any doc-comment survivor explicitly noted); the standalone-hero-multiplier kill-set (`_applyHeroMultiplier`/`HERO_BOOST_*`/`HERO_PENALTY`/`HERO_SCALE`) is grep-ZERO.
    - The PFIX divisor `1_000→400`, the RFALL pure-ETH/stETH segregation, the BTOMB 1e36 one-shot, the HERO `S=A+2H` scoring + byte-reproduced finals, and the SWAP `sellFarFutureTickets` + swap-pop are each anchor-confirmed against the frozen subject.
    - A Composition Attestation Matrix (surface-mapping / claimable-balance / tombstone non-circulating / RNG-freeze-intact) mirrors v47 §3.B.
    - `git diff 1575f4a9 HEAD -- contracts/` is empty (zero contract mutation).
  </acceptance_criteria>
  <done>All 7 surfaces attested NON-WIDENING with grep/diff evidence; the delta-surface table + composition matrix are written to 328-01-DELTA-AUDIT.md; contracts/ byte-identical to 1575f4a9.</done>
</task>

<task type="auto">
  <name>Task 2: Regression-baseline attestation (632/42 net-zero) + F-47-01/F-47-02 RESOLVED-AT-V48 dispositions</name>
  <read_first>
    - test/REGRESSION-BASELINE-v48.md (the 327-06 ledger: the 594→632 +38 NEW_PASSING reconciliation, the 42-failure bucket A/B/C classification, the conditional HERO byte-reproduce delta + the post-landing 0-diff GREEN flip)
    - audit/FINDINGS-v47.0.md §5 (the LEAN regression appendix format to mirror) + §4.2 (the F-47-01 + F-47-02 finding write-ups recorded as DEFERRED→v48 — the source the RESOLVED-AT-V48 disposition closes)
    - .planning/REQUIREMENTS.md (the PFIX/RFALL requirement rows: PFIX-01/02/03 + RFALL-01/02/03/04/05)
    - 328-01-DELTA-AUDIT.md (the file being appended to)
  </read_first>
  <files>.planning/phases/328-terminal-delta-audit-3-skill-adversarial-sweep-closure/328-01-DELTA-AUDIT.md</files>
  <action>
    Append a Regression-Baseline Attestation section to 328-01-DELTA-AUDIT.md mirroring v47 §5. Attest the v48 foundry baseline 632/42 NON-WIDENING: cite the 327-06 ledger classification (594/42 326-08 baseline + 38 NEW_PASSING from the 5 wave-1 test files + the redemption invariant extension + 0 net-new failures; the 42 reds = bucket A 8 VRF/RNG pre-existing + bucket B 34 stale-harness/v48-behavioral + bucket C 0). Record the conditional HERO byte-reproduce delta resolution: the Hardhat PASS_ALL gate was 15/20-diverge RED pre-landing and flipped to 0-diff GREEN at `1575f4a9` (per-N EV == 100, ETH bonus == 5.000%); the byte-reproduce red was Hardhat-only and the forge 42-count is unchanged. State the REG-01-equivalent NON-WIDENING claim: every `git diff da5c9d50..1575f4a9 -- contracts/ test/` hunk is attributable to a known v48-scope commit (the batched IMPL diff `f50cc634` + the HERO-04 finals landing `1575f4a9` + the AGENT-committed wave-1 test files).

    Then append the F-47-01 + F-47-02 RESOLVED-AT-V48 dispositions (the two v47-deferred MEDIUM findings this milestone closes):
    - F-47-01 (presale closing-box DGNRS over-distribution, MEDIUM) → RESOLVED-AT-V48: the `_presaleBoxDgnrsReward` divisor `1_000→400` (base `poolStart/40`) scales the draw by the ~40% DGNRS branch rate so the closing sweep transfers only variance dust (PFIX-01 IMPL; PFIX-02/03 empirical dust-bound proof @ 327-01). Run the economic skeptic-filter lens (structural-protection + 3-condition EV) and record that the fix closes the concentration concern without re-opening an over-drain/inflation axis (`transferFromPool` clamp held).
    - F-47-02 (redemption submit ETH-empty stETH-fallback gap, MEDIUM) → RESOLVED-AT-V48: the pure-ETH-OR-pure-stETH segregation + mid-game ETH→stETH fallback + revert-if-neither + donation-robust coverage (RFALL-01/02/03 IMPL; RFALL-05 + POOL-04 regression proof @ 327-02). Run the skeptic-filter lens and record that the fix restores liveness while preserving the v47 REDEEM-08 solvency invariants (two same-period claimants, BURNIE-can't-block-ETH, `balance ≥ pending`).
    Read-only — no contracts/ or test/ edits.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -c "632" .planning/phases/328-terminal-delta-audit-3-skill-adversarial-sweep-closure/328-01-DELTA-AUDIT.md && grep -c "F-47-01" .planning/phases/328-terminal-delta-audit-3-skill-adversarial-sweep-closure/328-01-DELTA-AUDIT.md && grep -c "RESOLVED-AT-V48\|RESOLVED_AT_V48" .planning/phases/328-terminal-delta-audit-3-skill-adversarial-sweep-closure/328-01-DELTA-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - The Regression-Baseline Attestation records 632/42 NON-WIDENING with the 594/42 + 38 NEW_PASSING + 0 net-new classification cited from the 327-06 ledger.
    - The conditional HERO byte-reproduce delta is recorded as resolved (Hardhat PASS_ALL 15/20-diverge RED → 0-diff GREEN at `1575f4a9`; forge 42 unchanged).
    - F-47-01 is recorded RESOLVED-AT-V48 with the PFIX-01 IMPL anchor + the PFIX-02/03 dust-bound proof, passing the economic skeptic-filter.
    - F-47-02 is recorded RESOLVED-AT-V48 with the RFALL-01/02/03 IMPL anchor + the RFALL-05/POOL-04 regression proof, passing the skeptic-filter.
    - A REG-01-equivalent NON-WIDENING attestation states every contracts/+test/ hunk vs da5c9d50 is attributable to a known v48-scope commit (`f50cc634` + `1575f4a9` + the AGENT-committed wave-1 tests).
  </acceptance_criteria>
  <done>The regression baseline 632/42 is attested NON-WIDENING; F-47-01 + F-47-02 recorded RESOLVED-AT-V48 with the skeptic-filter; the REG-01-equivalent claim is recorded — all appended to 328-01-DELTA-AUDIT.md.</done>
</task>

</tasks>

<verification>
- `git diff 1575f4a9 HEAD -- contracts/` empty (zero contract mutation throughout).
- 328-01-DELTA-AUDIT.md contains the per-surface NON-WIDENING table (7 surfaces) + Composition Attestation Matrix + Regression-Baseline Attestation (632/42) + the F-47-01/F-47-02 RESOLVED-AT-V48 dispositions.
- All 40 v48 REQ IDs across the 8 categories appear referenced in the surface table (each surface lists its owning REQ-IDs).
</verification>

<success_criteria>
SC1 satisfied: every contract surface changed vs the v47.0 baseline da5c9d50 is enumerated and attested NON-WIDENING with each delta change mapped to exactly one of the 7 v48 surfaces; the 632/42 regression baseline is attested NON-WIDENING; F-47-01 + F-47-02 are recorded RESOLVED-AT-V48; contracts/ is byte-identical to 1575f4a9.
</success_criteria>

<output>
Create `.planning/phases/328-terminal-delta-audit-3-skill-adversarial-sweep-closure/328-01-SUMMARY.md` when done
</output>
