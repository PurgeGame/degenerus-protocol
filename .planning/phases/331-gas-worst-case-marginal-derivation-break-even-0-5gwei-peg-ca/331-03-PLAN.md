---
phase: 331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca
plan: 03
type: execute
wave: 2
depends_on: ["331-01"]
files_modified:
  - test/fuzz/KeeperBatchAffiliateDeltaAudit.t.sol
  - test/fuzz/CrankNonBrick.t.sol
  - .planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-SEEDS-DESIGN.md
autonomous: true
requirements: [GAS-02]
must_haves:
  truths:
    - "The Seed 1 (shared-slot DGNRS affiliate write aggregation) + Seed 2 (batchPurchase try/catch -> pre-validation keeper batch path) contract design is enumerated in writing: every _batchPurchaseUnit revert source, every coalescible vs non-coalescible affiliate SSTORE, and the chosen path shape (new function vs internal helper)."
    - "A money-path delta-audit test proves byte-identical affiliate/claimable/DGNRS outcomes between the current try/catch path and the proposed keeper-batch path (no double-credit, no skipped-player drain)."
    - "A no-brick liveness test proves a single reverting/funding-skipped player can never brick the keeper batch under the pre-validated path (the security floor preserved)."
  artifacts:
    - path: ".planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-SEEDS-DESIGN.md"
      provides: "Seed 1+2 revert-source enumeration + coalescible-SSTORE table + the proposed keeper-batch path shape"
    - path: "test/fuzz/KeeperBatchAffiliateDeltaAudit.t.sol"
      provides: "money-path delta-audit harness (current vs proposed-path equivalence)"
      contains: "DeltaAudit"
    - path: "test/fuzz/CrankNonBrick.t.sol"
      provides: "extended no-brick proof for the pre-validated keeper batch path"
      contains: "poison"
  key_links:
    - from: "test/fuzz/CrankNonBrick.t.sol"
      to: "the keeper batch path (batchPurchase / batchPurchaseForKeeper)"
      via: "poisoned-player skip-and-continue assertion"
      pattern: "poison|brick"
---

<objective>
Design Seed 1 (aggregate the whole keeper batch's shared-slot `bytes32("DGNRS")` affiliate writes — sum in memory, ONE SSTORE at the tail) + Seed 2 (replace `batchPurchase`'s per-player `this._batchPurchaseUnit` try/catch with a pre-validated keeper-specialized batch path), and author the money-path delta-audit + no-brick liveness test scaffolds the gated 331-05 contract diff must pass. Covers CONTEXT D-06 (Seed 1) + D-07 (Seed 2); rides the GAS-02 contract diff.

Purpose: These two seeds touch the affiliate MONEY path. The HARD CONSTRAINT (`feedback_security_over_gas`): no single reverting/funding-skipped player may brick the keeper batch — the per-player isolation is a SECURITY property, not just gas hygiene. So the seeds need their OWN delta-audit (byte-identical money outcomes, no double-credit, no skipped-player drain) + a no-brick proof BEFORE any contract code lands. This plan produces the design + the tests; 331-05 (gated) writes the contract code that must pass them.

Output: `331-SEEDS-DESIGN.md` (the enumeration + chosen shape) + `test/fuzz/KeeperBatchAffiliateDeltaAudit.t.sol` (new) + extended `test/fuzz/CrankNonBrick.t.sol`. All test/doc, AGENT-committable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-CONTEXT.md
@.planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-PATTERNS.md

<interfaces>
<!-- The Seed 2 surface (current try/catch isolation, DegenerusGame.sol:1757-1791): -->
- `batchPurchase(address[] players, uint256[] amounts, uint8[] modes)` — AF_KING-gated (:1762), gameOver-gated (:1763); per-player `try this._batchPurchaseUnit{value: slice}(players[i], MintPaymentKind(modes[i])) {} catch {}` (:1774); ONE refund of unspent at the tail.
- `_batchPurchaseUnit(address player, MintPaymentKind payKind)` onlySelf (:1798) → `_purchaseFor(player, 0, msg.value, bytes32("DGNRS"), payKind)` (:1806).
- Keeper-side funding pre-check ALREADY pure-view: `AfKing._resolveBuy(...)` (:793) + `_autoBuy` pre-skips InsufficientPool/lootboxSkip BEFORE building the slice.

<!-- The Seed 1 coalescible affiliate SSTOREs (DegenerusAffiliate.payAffiliate :388-615) for a DGNRS batch (constant lvl+affiliateAddr across players): -->
- COALESCIBLE (same slot N times): affiliateCoinEarned[lvl][affiliateAddr] (:537), _totalAffiliateScore[lvl] (:538), _updateTopAffiliate leaderboard (:548), _routeAffiliateReward -> creditFlip(winner,...) (:581/:608), quests.handleAffiliate(winner,...) (:607)
- NON-coalescible (per-player slot): affiliateCommissionFromSender[lvl][affiliateAddr][sender] (:527) — keyed on the player

<!-- The acc-flush precedent to mirror (DegenerusGameDegeneretteModule.sol:407-426): -->
- sum acc.burnieMint/claimable across bets, ONE coin.mintForGame + ONE claimable write at the tail.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate Seed 1+2 revert sources + coalescible writes + choose the path shape</name>
  <files>.planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-SEEDS-DESIGN.md</files>
  <read_first>
    - .planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-PATTERNS.md (the Seed 1 + Seed 2 sections: the SSTORE-count table, the autoOpen RD-5 entry-gate model, the no-brick security-property note)
    - contracts/DegenerusGame.sol:513-540 (_purchaseFor signature + the MintModule delegatecall), :1757-1810 (batchPurchase + _batchPurchaseUnit + the DGNRS wiring)
    - contracts/DegenerusAffiliate.sol:388-615 (payAffiliate — the per-buy writes) + :772-803 (_routeAffiliateReward + _updateTopAffiliate)
    - contracts/modules/DegenerusGameMintModule.sol (purchase — the lootbox/prize-pool/EV-cap/quest revert sources reached via _purchaseFor)
    - contracts/AfKing.sol:561-720 (_autoBuy pre-skips), :793-840 (_resolveBuy pure-view funding waterfall)
    - contracts/storage/DegenerusGameStorage.sol around :560-573 (the _queueTickets liveness revert — the RD-5 entry-gate precedent)
    - contracts/modules/DegenerusGameDegeneretteModule.sol:407-426 (the acc-flush precedent for Seed 1)
  </read_first>
  <action>
    Author `.planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-SEEDS-DESIGN.md`. SEED 2 section: grep-enumerate EVERY revert source reachable from `_batchPurchaseUnit -> _purchaseFor -> MintModule.purchase` (level/state guards, funding, EV-cap, liveness `_livenessTriggered`, lootbox-floor, prize-pool). For each, decide pre-validate (cheap view check before the call) OR cheap-skip (continue, never reverts) — mirroring the autoOpen RD-5 entry-gate technique (DegenerusGame.sol:1666-1701) that made its loop body guaranteed-non-reverting. Note which sources AfKing._resolveBuy already pre-validates view-side (funding waterfall, InsufficientPool, lootboxSkip) vs the residual GAME-side mint-path sources Seed 2 must handle. DECIDE the path shape per CONTEXT "Claude's Discretion": a NEW `batchPurchaseForKeeper` function vs a parameterized internal helper — justify from the revert-source count (a path that cannot make the unit non-reverting must keep a cheap-skip, not a try/catch). SEED 1 section: confirm the coalescible vs non-coalescible SSTORE table (affiliateCommissionFromSender :527 is the ONLY non-coalescible write — keyed on sender; everything else accumulates to the same lvl+affiliateAddr/winner slot across a DGNRS batch). Quantify the win (~N-1 saved SSTOREs per coalescible slot). Specify the acc-flush shape: keeper units RETURN their affiliate contribution; sum into a struct across the loop; ONE flush at the tail (mirror the degenerette `resolveBets` acc-flush). State the HARD CONSTRAINT explicitly: only SUCCESSFUL (non-reverted/non-skipped) units sum; a poisoned player must never brick the batch nor leak another player's credit.
  </action>
  <verify>
    <automated>test -f .planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-SEEDS-DESIGN.md && grep -q -i "revert source\|coalescible\|no-brick\|brick" .planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-SEEDS-DESIGN.md</automated>
  </verify>
  <acceptance_criteria>
    - 331-SEEDS-DESIGN.md enumerates every `_batchPurchaseUnit -> _purchaseFor` revert source with a pre-validate-or-cheap-skip disposition each.
    - The chosen Seed 2 path shape (new function vs internal helper) is decided and justified from the revert-source enumeration.
    - The Seed 1 coalescible/non-coalescible SSTORE table is confirmed against source (affiliateCommissionFromSender the sole non-coalescible).
    - The acc-flush tail-flush shape + the only-successful-units rule + the no-brick security floor are stated.
  </acceptance_criteria>
  <done>The Seed 1+2 contract design is fully enumerated; the gated 331-05 implementer has a concrete blueprint with no open revert source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Money-path delta-audit harness (current path vs the proposed keeper-batch path)</name>
  <files>test/fuzz/KeeperBatchAffiliateDeltaAudit.t.sol</files>
  <read_first>
    - .planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-SEEDS-DESIGN.md (Task 1 — the chosen path + the coalescible-write set)
    - test/fuzz/CrankNonBrick.t.sol (the AF_KING-pinned batchPurchase harness setup + the fixture)
    - test/fuzz/CrankFaucetResistance.t.sol (the affiliate/claimable read helpers)
    - contracts/DegenerusAffiliate.sol (the getters: affiliateCoinEarned getter :642, _totalAffiliateScore getter :653)
    - contracts/DegenerusGame.sol:1757-1810 (the current path being audited against)
  </read_first>
  <behavior>
    - For an N-player keeper batch (mix of fundable + un-fundable/poisoned players), the per-recipient affiliate accumulators (affiliateCoinEarned[lvl][DGNRS-owner], _totalAffiliateScore[lvl], the SDGNRS/VAULT flip-credit, the per-player affiliateCommissionFromSender, claimable deltas) MUST be byte-identical between the current try/catch path and the proposed aggregated keeper-batch path.
    - No double-credit: the summed-then-flushed total equals the sum of per-unit contributions of ONLY the successful units.
    - No skipped-player drain: a skipped/poisoned player contributes ZERO to the aggregate and its slice is refunded, exactly as today.
  </behavior>
  <action>
    Create `test/fuzz/KeeperBatchAffiliateDeltaAudit.t.sol` extending the CrankNonBrick fixture (AF_KING-pinned). Until 331-05 lands the proposed path, this harness ESTABLISHES THE BASELINE: drive the CURRENT `batchPurchase` try/catch path over an N-player mix (fundable + poisoned/un-fundable) and SNAPSHOT every money outcome — `affiliateCoinEarned[lvl][owner]`, `_totalAffiliateScore[lvl]`, the SDGNRS/VAULT flip-credit balances, each player's `affiliateCommissionFromSender`, each player's claimable delta, and the keeper refund. Assert the snapshot internal-consistency invariants that the proposed path must also satisfy (aggregate == sum-of-successful-units; poisoned player == zero contribution + slice refunded; per-player commission keyed correctly). Write the harness so that after 331-05 lands `batchPurchaseForKeeper`, a `_drive(useKeeperPath)` toggle re-runs the SAME assertions against the new path and asserts byte-identical accumulators (a parameterized equivalence test). Mark the not-yet-existing-path assertions with a clear TODO-331-05 gate that 331-05 flips on. Read all values via the existing getters / `vm.load` (re-confirm slots via `forge inspect`).
  </action>
  <verify>
    <automated>forge test --match-path test/fuzz/KeeperBatchAffiliateDeltaAudit.t.sol -vv 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-path test/fuzz/KeeperBatchAffiliateDeltaAudit.t.sol` exits 0 against the CURRENT path (baseline established).
    - The harness snapshots affiliateCoinEarned, _totalAffiliateScore, SDGNRS/VAULT flip-credit, per-player commission, claimable deltas, and the keeper refund.
    - The aggregate == sum-of-successful-units and poisoned-player == zero invariants are asserted.
    - A `_drive(useKeeperPath)` toggle + a TODO-331-05 gate exists so the equivalence assertions activate once `batchPurchaseForKeeper` lands.
  </acceptance_criteria>
  <done>The money-path delta-audit baseline is established and parameterized for the gated path-equivalence proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend CrankNonBrick — poisoned-player no-brick under the pre-validated path</name>
  <files>test/fuzz/CrankNonBrick.t.sol</files>
  <read_first>
    - test/fuzz/CrankNonBrick.t.sol (the WHOLE file — Task 1 skip-and-continue idiom :125-217, the poisoned-entry construction, the AF_KING-pinned batchPurchase setup :46)
    - .planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-SEEDS-DESIGN.md (the chosen Seed 2 path + the cheap-skip vs pre-validate dispositions)
    - contracts/DegenerusGame.sol:1757-1791 (the current try/catch batch loop)
  </read_first>
  <behavior>
    - A keeper batch whose middle player is poisoned (un-fundable / a revert source that cannot be pre-validated away → must be cheap-skipped) still purchases for the healthy players, skips the poisoned one, and refunds its slice.
    - The poisoned player NEVER bricks the whole batch (the SECURITY-property liveness floor).
    - The skipped player's slice is refunded to the keeper; no other player's purchase or affiliate credit is corrupted.
  </behavior>
  <action>
    Add `testKeeperBatchSkipsPoisonedMiddlePlayer` (+ a fuzzed poison-position variant) to `CrankNonBrick.t.sol`, mirroring the existing `testCrankBetsSkipsPoisonedMiddleItem` skip-and-continue idiom but for the keeper batch-buy path: construct [healthyA, poisoned, healthyB] where the poisoned player hits a revert source the Seed 2 design classifies as cheap-skip, drive the batch, and assert (1) healthyA + healthyB purchased, (2) the poisoned player did NOT purchase + its slice was refunded, (3) the batch did NOT revert (no-brick). Parameterize on the path via the same `_drive(useKeeperPath)` toggle as Task 2 so the SAME no-brick proof runs against `batchPurchaseForKeeper` once 331-05 lands it (gate the new-path branch with TODO-331-05). Run against the CURRENT path now to establish the GREEN baseline. This is the `feedback_security_over_gas` HARD CONSTRAINT proof — the per-player isolation liveness must survive the try/catch -> pre-validation swap.
  </action>
  <verify>
    <automated>forge test --match-path test/fuzz/CrankNonBrick.t.sol -vv 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-path test/fuzz/CrankNonBrick.t.sol` exits 0.
    - `testKeeperBatchSkipsPoisonedMiddlePlayer` proves healthy players purchase, the poisoned player is skipped + refunded, and the batch does not revert.
    - A fuzzed poison-position variant covers the poisoned player at first/middle/last position.
    - The proof is parameterized via `_drive(useKeeperPath)` with a TODO-331-05 gate for the new path.
  </acceptance_criteria>
  <done>The no-brick liveness floor is proven for the keeper batch path and parameterized for the gated path equivalence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keeper batch → per-player mint (_batchPurchaseUnit) | a poisoned/un-fundable player is untrusted input; isolation must hold |
| keeper batch → shared DGNRS affiliate slot | the whole batch credits one accumulator; aggregation must not double-credit or leak across players |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-331-06 | Denial of service | one poisoned player bricks the whole keeper batch | mitigate | extend CrankNonBrick skip-and-continue proof to the pre-validated path; the per-player isolation is a SECURITY property (`feedback_security_over_gas`), preserved by cheap-skip where pre-validation is impossible |
| T-331-07 | Tampering | Seed 1 aggregation double-credits / leaks across players | mitigate | money-path delta-audit asserts byte-identical accumulators vs the current path; only successful units sum; affiliateCommissionFromSender stays per-player (non-coalescible) |
| T-331-08 | Repudiation | skipped-player slice drain | mitigate | delta-audit asserts a skipped player contributes zero AND its slice is refunded (unchanged from today) |
| T-331-SC | Tampering | npm/forge deps | accept | test/doc-only plan; no package installs; no contract mutation here (contract code lands in gated 331-05) |
</threat_model>

<verification>
- All three new/extended test files exit 0 against the CURRENT contract path (baselines established).
- 331-SEEDS-DESIGN.md leaves no un-dispositioned revert source.
- No `contracts/*.sol` modified by this plan (the seed contract code is 331-05, gated).
</verification>

<success_criteria>
The Seed 1+2 design is enumerated with every revert source dispositioned and the path shape chosen; the money-path delta-audit + no-brick liveness tests are written, GREEN on the current path, and parameterized so the gated 331-05 diff is proven byte-identical + un-brickable before it ships. Rides the GAS-02 contract diff.
</success_criteria>

<output>
Create `.planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-03-SUMMARY.md` when done.
</output>
