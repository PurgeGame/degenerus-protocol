---
phase: 331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/gas/RouterWorstCaseGas.t.sol
  - .planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-GAS-DERIVATION.md
autonomous: true
requirements: [GAS-01]
must_haves:
  truths:
    - "The theoretical worst-case marginal gas for each keeper category (autoBuy buy-leg, autoOpen open-leg, advance-leg) + the doWork() router dispatch overhead is derived in writing BEFORE measurement."
    - "Each category's per-item MARGINAL is measured via the established gasleft()-bracket idiom under --isolate at N>=32 items (per-tx fixed overhead amortized away — the CR-01 lesson)."
    - "Each measured worst-case scenario asserts it IS the maximum (cap-laden) AND is non-vacuous (real work happened, not a silent skip) BEFORE the number is trusted."
    - "Every measured worst case fits the real 30M mainnet block-gas limit."
    - "The per-item marginals + the router dispatch overhead are emitted as log_named_uint values that 331-04 reads as the calibration input."
  artifacts:
    - path: "test/gas/RouterWorstCaseGas.t.sol"
      provides: "GAS-01 router worst-case marginal gas harness (buy/open/advance/dispatch)"
      contains: "RouterWorstCaseGas"
    - path: ".planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-GAS-DERIVATION.md"
      provides: "Theoretical-first worst-case derivation per category + the measured marginals table"
  key_links:
    - from: "test/gas/RouterWorstCaseGas.t.sol"
      to: "contracts/AfKing.sol doWork()/autoBuy/_autoBuy + contracts/DegenerusGame.sol autoOpen + advanceGame"
      via: "gasleft() bracket around the keeper-category entrypoints"
      pattern: "gasBefore - gasleft"
---

<objective>
Derive the v49 keeper-router worst-case marginal gas theoretically (per `feedback_gas_worst_case`), THEN measure it with a new `test/gas/RouterWorstCaseGas.t.sol` harness, producing the per-category per-item MARGINAL numbers (buy / open / advance) + the `doWork()` router dispatch overhead that 331-04 calibrates the break-even peg from. GAS-01.

Purpose: The break-even bounty peg cannot be a guess — it is a function of measured worst-case marginal gas at 0.5 gwei. This plan produces that measurement. The CR-01 lesson (319) is load-bearing: peg to the per-item MARGINAL at N>=32, never a single-item TOTAL (which over-pegs ~2x and opens a Sybil faucet).

Output: `test/gas/RouterWorstCaseGas.t.sol` (test-only, AGENT-committable) + `331-GAS-DERIVATION.md` (the theoretical-first derivation + the measured marginals).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-CONTEXT.md
@.planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-PATTERNS.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md

<interfaces>
<!-- The committed 330 doWork() body (AfKing.sol:868) — the subject this harness measures.
     NOTE the USER-approved 330 deviation: advanceGame() returns a SINGLE `uint8 mult`
     (mult==0 = gameover/no-bounty sentinel), NOT the (mult,rewardable) tuple the 329 SPEC drafted.
     The harness measures against the COMMITTED contract, not the SPEC draft. -->

From contracts/AfKing.sol (doWork dispatch — the three legs to measure):
- buy leg:    `_autoBuy(DOWORK_BATCH)` returns `uint256 boughtCount`  (AfKing.sol:561 internal; external `autoBuy(uint256 count)` :908 wraps it UNREWARDED)
- advance:    `IGame(GAME).advanceGame()` returns `uint8 mult`        (router dispatch path)
- open leg:   `IGame(GAME).autoOpen(DOWORK_BATCH)` returns `uint256 opened`
- O(1) predicates routed on: `advanceDue()`, `boxesPending()`, autoBuy day/cursor (`autoBuyProgress() :525`)

From contracts/DegenerusGame.sol:
- `keeperSnapshot(address[] players) returns (uint256 mintPriceWei, bool rngLocked_, uint256[] claimables)` (:2595) — GASOPT-03 batched read, already landed
- `degeneretteResolve(address[] players, uint64[] betIds)` — the renamed resolve helper (separate call, NOT in doWork)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive the theoretical worst case per keeper category (writing-first)</name>
  <files>.planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-GAS-DERIVATION.md</files>
  <read_first>
    - .planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-PATTERNS.md (the "Derive-then-measure protocol" + CR-01 loop-N-divide section)
    - test/gas/CrankResolveBetWorstCaseGas.t.sol (the derive-then-measure NatSpec idiom + the assert-is-worst-case preconditions)
    - test/gas/SweepPerPlayerWorstCaseGas.t.sol (the per-player marginal idiom + the BOUNTY_ETH_TARGET ctor-immutable note + the deploy-sub-exclusion)
    - test/gas/CrankOpenBoxWorstCaseGas.t.sol (the per-box marginal + the corrected loop-N-divide)
    - contracts/AfKing.sol:561-720 (_autoBuy body — the per-player buy work), :868-907 (doWork dispatch)
    - contracts/DegenerusGame.sol around autoOpen + advanceDue/boxesPending views
  </read_first>
  <action>
    Author `.planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-GAS-DERIVATION.md` (analog `319-GAS-DERIVATION.md`). For EACH keeper category, write the theoretical max-laden worst case FIRST, before any measurement: (1) the BUY leg — per-subscriber `_autoBuy` work at the heaviest funding path (drain-first waterfall + reinvest + the full mint→affiliate→quest write chain through `batchPurchase`→`_batchPurchaseUnit`→`_purchaseFor`); (2) the OPEN leg — per-box `autoOpen` work at the heaviest lootbox-conversion branch (mirror `CrankOpenBoxWorstCaseGas` worst-case construction); (3) the ADVANCE leg — the `advanceGame()` mid-day partial-drain + new-day cost (the router consumes the returned `mult`); (4) the `doWork()` router DISPATCH overhead itself (the O(1) predicate reads `advanceDue()`/`boxesPending()`/`autoBuyProgress()` + the `keeperSnapshot` mintPrice read + the single `creditFlip`). State the cap each leg runs to (per-currency spin caps, subscriber count, box-queue length) and WHY it is the maximum. Record that the peg target is break-even at 0.5 gwei (`AUTO_GAS_PRICE_REF`), and that the calibration uses the per-item MARGINAL at N>=32 (the CR-01 rule), never a single-item total. Leave the measured-numbers table as a "FILLED BY Task 2" placeholder section.
  </action>
  <verify>
    <automated>test -f .planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-GAS-DERIVATION.md && grep -q -i "worst case\|marginal" .planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-GAS-DERIVATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 331-GAS-DERIVATION.md exists with a per-category theoretical worst-case section (buy, open, advance, dispatch) written BEFORE the measured numbers.
    - Each category states the cap it runs to and why that is the maximum.
    - The 0.5 gwei break-even target + the N>=32 per-item-marginal (CR-01) rule are stated.
  </acceptance_criteria>
  <done>The theoretical worst case is in writing for all four measurement targets; the measured-table section is a marked placeholder.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build RouterWorstCaseGas.t.sol and measure the per-item marginals</name>
  <files>test/gas/RouterWorstCaseGas.t.sol, .planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-GAS-DERIVATION.md</files>
  <read_first>
    - test/gas/CrankResolveBetWorstCaseGas.t.sol (the WHOLE file — copy the imports/fixture/gasleft-bracket/loop-N-divide/non-vacuity/30M-bar/log_named_uint idiom wholesale)
    - test/gas/SweepPerPlayerWorstCaseGas.t.sol (the per-player buy marginal + the AfKing slot helpers + the deploy-sub exclusion so the 2 standing protocol subs do not contaminate the marginal)
    - test/gas/CrankOpenBoxWorstCaseGas.t.sol (the per-box open marginal construction)
    - test/fuzz/helpers/DeployProtocol.sol (the _deployProtocol() fixture + the BOUNTY_ETH_TARGET deploy arg)
    - contracts/AfKing.sol:561-720 (_autoBuy), :868-907 (doWork), :525 (autoBuyProgress)
    - .planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-GAS-DERIVATION.md (Task 1 output — the worst cases to construct)
  </read_first>
  <behavior>
    - Test BUY: seed N>=32 worst-case-funded subscribers, measure `gasBefore - gasleft()` around ONE `_autoBuy(N)` drive (or doWork buy leg), divide by N → `router_dowork_buy_per_player_marginal_gas`. Assert each sub bought (non-vacuity via `lastAutoBoughtDay == today` per-sub). Assert total < 30M.
    - Test OPEN: queue N>=32 worst-case boxes with the RNG word landed, measure `autoOpen(N)` (or doWork open leg), divide by N → `router_dowork_open_per_box_marginal_gas`. Assert each box opened (non-vacuity via the box signal zeroed). Assert total < 30M.
    - Test ADVANCE: measure the `advanceGame()` mid-day partial-drain + new-day cost via the doWork advance dispatch → `router_dowork_advance_marginal_gas`. Assert the day actually advanced (non-vacuity). Assert < 30M.
    - Test DISPATCH: measure the bare `doWork()` overhead (predicate reads + keeperSnapshot + one creditFlip) isolating it from leg work where feasible → `router_dowork_dispatch_overhead_gas`.
  </behavior>
  <action>
    Create `test/gas/RouterWorstCaseGas.t.sol` extending `DeployProtocol`, copying the established `*WorstCaseGas.t.sol` idiom: imports per `CrankResolveBetWorstCaseGas.t.sol:1-6`; `MAINNET_BLOCK_GAS_LIMIT = 30_000_000`; the gasleft()-bracket MEASUREMENT (NOT `vm.snapshotGas` — follow the live repo idiom per PATTERNS.md reconciliation #2); the loop-N-divide per-item MARGINAL at N>=32 (the CR-01 converged regime, never N=1 single-item total). Re-confirm every `vm.store`/`vm.load` slot constant via `forge inspect DegenerusGame storage` against the 63bc16ca layout BEFORE any inject (the 330 diff added `boxCursor`/`boxCursorIndex`/`boxPlayers` at DegenerusGame.sol:1548-1559; the harness slot comments carry pre-330 `v47:+2` drift). Run `--isolate`. Each test: (1) assert the constructed scenario IS the cap (assert-is-worst-case), (2) assert non-vacuity (real work happened), (3) assert < 30M, (4) `emit log_named_uint(...)` for `router_dowork_buy_per_player_marginal_gas` / `router_dowork_open_per_box_marginal_gas` / `router_dowork_advance_marginal_gas` / `router_dowork_dispatch_overhead_gas`. Then back-fill the measured table in `331-GAS-DERIVATION.md` with the emitted numbers (include the N-amortization gradient to show convergence at N>=32). This is a MEASUREMENT harness only — it does NOT calibrate constants (331-04 does, gated).
  </action>
  <verify>
    <automated>forge test --match-path test/gas/RouterWorstCaseGas.t.sol --isolate -vv 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-path test/gas/RouterWorstCaseGas.t.sol --isolate` exits 0.
    - Each test uses N>=32 for the per-item marginal (grep the harness for the N constant >= 32).
    - Each test emits its `router_dowork_*_marginal_gas` / `_overhead_gas` log_named_uint AND asserts < MAINNET_BLOCK_GAS_LIMIT.
    - Each test has both an assert-is-worst-case (cap) assertion and a non-vacuity assertion (no silent skip).
    - 331-GAS-DERIVATION.md measured-numbers table is filled from the emitted values (no longer a placeholder).
  </acceptance_criteria>
  <done>The four marginals (buy/open/advance/dispatch) are measured, non-vacuous, worst-case-asserted, under 30M, and recorded as the calibration input for 331-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| permissionless keeper → AfKing.doWork() | any address calls doWork() and earns the BURNIE flip-credit bounty (untrusted caller) |
| test harness → contract storage (vm.store) | injected worst-case state must match the 63bc16ca slot layout or the measurement is vacuous |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-331-01 | Information disclosure | the measured marginal (under-stated) | mitigate | Derive theoretical worst case FIRST and assert-is-worst-case (cap reached) + non-vacuity BEFORE trusting the number — an under-stated marginal would set a too-low peg and starve keepers; a vacuous (silent-skip) measurement is rejected by the non-vacuity assert |
| T-331-02 | Tampering | slot-inject drift (vm.store) | mitigate | Re-confirm every slot via `forge inspect DegenerusGame storage` against 63bc16ca before injecting (330 added box cursors at :1548-1559); a stale slot writes garbage and produces a fake marginal |
| T-331-SC | Tampering | npm/forge deps | accept | test-only plan; no package installs; no contract mutation |
</threat_model>

<verification>
- `forge test --match-path test/gas/RouterWorstCaseGas.t.sol --isolate` exits 0.
- The four marginals are emitted and recorded in 331-GAS-DERIVATION.md.
- No `contracts/*.sol` modified (this plan is test + doc only): `git diff --name-only -- contracts/` is empty for this plan's changes.
</verification>

<success_criteria>
GAS-01 satisfied: worst-case-first marginal gas derived per keeper category + router overhead, then measured via the established harness idiom at N>=32 per-item marginal, all under the 30M mainnet bar, emitted as the calibration input.
</success_criteria>

<output>
Create `.planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-01-SUMMARY.md` when done.
</output>
