---
phase: 331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fuzz/CrankFaucetResistance.t.sol
autonomous: true
requirements: [GAS-05, GAS-06]
must_haves:
  truths:
    - "A WR-01-style round-trip guard proves no positive-EV self-crank loop under the flat-per-tx router model for the buy leg (flat 1.5x) and the open leg (1x pro-rated below the OPEN_KNEE)."
    - "The open small-batch + low-gas hot corner (per-box reward below the knee <= a one-box tx 0.5-gwei gas) is proven -EV at every realistic market gas price."
    - "Exploitability is judged against REAL prevailing gas (1-2000 gwei fuzzed), NOT the 0.5-gwei peg reference."
    - "A degeneretteResolve flat ~1-BURNIE round-trip is proven a net loss at any realistic gas price (the >=3 non-WWXRP gate, illiquid flip-credit <= mintPrice/1000 ETH)."
  artifacts:
    - path: "test/fuzz/CrankFaucetResistance.t.sol"
      provides: "GAS-05 router round-trip guard (buy/open/resolve) extending the 319 WR-01 idiom"
      contains: "RoundTrip"
  key_links:
    - from: "test/fuzz/CrankFaucetResistance.t.sol"
      to: "the calibrated peg unit (BOUNTY_ETH_TARGET * ratios) + RESOLVE_FLAT_BURNIE"
      via: "summed reward valued at peg <= real gas cost at >=1 gwei"
      pattern: "round-trip|NonPositive"
---

<objective>
Extend `test/fuzz/CrankFaucetResistance.t.sol` with WR-01-style round-trip guards proving no positive-EV self-crank loop exists under the v49 flat-per-tx router model — covering the open small-batch hot corner (the structural faucet risk the `OPEN_KNEE` pro-rate exists to close) and the `degeneretteResolve` flat ~1-BURNIE "lose" reward. GAS-05 + the GAS-06 anti-exploit basis.

Purpose: GAS-05 is the SAFE-01 hard floor — the bounty must never fund a self-crank farm. Under the flat-per-tx model the dangerous corner is a tiny mid-day open (per-box reward below the knee `1x/KNEE` vs a one-box tx cost). The guard proves round-trip <= 0 at the 0.5-gwei reference and strictly negative at every realistic market price, judged against REAL gas + flip-credit illiquidity (`feedback_bounty_exploit_uses_real_gas_not_peg_ref`).

Output: extended `test/fuzz/CrankFaucetResistance.t.sol` (test-only, AGENT-committable).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-CONTEXT.md
@.planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-PATTERNS.md

<interfaces>
<!-- The flat-per-tx reward formula (committed 330, AfKing.sol:868-892) the guard checks: -->
- unit       = (BOUNTY_ETH_TARGET * PRICE_COIN_UNIT) / mintPrice     (level-invariant break-even unit; PRICE_COIN_UNIT = 1000 ether)
- buy leg    = (unit * BUY_RATIO_NUM) / BUY_RATIO_DEN                 (flat 1.5x per tx; NUM/DEN = 3/2)
- advance    = unit * ADVANCE_RATIO_NUM * mult                        (2x * stall mult; NEW-DAY only escalates)
- open leg   = (unit * min(opened, OPEN_KNEE)) / OPEN_KNEE            (1x at/above knee, pro-rated below; OPEN_KNEE = 5)
- RESOLVE    = RESOLVE_FLAT_BURNIE (1e18 placeholder) once per tx at >=3 non-WWXRP resolutions  (DegenerusGame.sol:1543)
- CRANK_GAS_PRICE_REF = 0.5 gwei (the peg reference; NOT the exploitability lens)

<!-- The 319 WR-01 idiom already in this file (the analog to extend): -->
- testSelfCrankRoundTripNonPositive (:147) — single round-trip <= 0 at 1 gwei + spot 20 gwei
- testFuzz_RoundTripNonPositiveAcrossGasPrices (:224) — fuzzed 1..2000 gwei
- testMultiBoxSelfCrankRoundTripNonPositive / testFuzz_MultiBoxRoundTripNonPositiveAcrossGasPrices (319 CR-01 fix)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the router open-leg + buy-leg round-trip guards (the flat-per-tx hot corner)</name>
  <files>test/fuzz/CrankFaucetResistance.t.sol</files>
  <read_first>
    - test/fuzz/CrankFaucetResistance.t.sol (the WHOLE file — the existing WR-01 round-trip idiom: testSelfCrankRoundTripNonPositive :147, testFuzz_RoundTripNonPositiveAcrossGasPrices :224, the multi-box CR-01 guards, CRANK_GAS_PRICE_REF :76)
    - .planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-PATTERNS.md (the "Faucet-floor / round-trip <= 0" + the flat-per-tx hot corner section)
    - contracts/AfKing.sol:868-892 (the doWork flat-per-tx reward formula + OPEN_KNEE pro-rate)
    - test/gas/CrankOpenBoxWorstCaseGas.t.sol (the per-box open construction + real gasUsed measurement to compare against the reward)
  </read_first>
  <behavior>
    - OPEN hot corner: a self-cranker opens k boxes via doWork (k from 1 to OPEN_KNEE and above), the open-leg reward `(unit * min(k, OPEN_KNEE)) / OPEN_KNEE` valued back at 0.5 gwei MUST be <= the real measured `gasUsed * realPrice` at the >=1 gwei floor — proving the small-batch corner is -EV (the OPEN_KNEE pro-rate is the structural closer).
    - BUY leg: the flat 1.5x buy reward per tx valued at peg <= the real autoBuy tx gas cost; a farmer must fund real subscription buys >> the bounty (bounded once/day/sub).
    - Both fuzzed across 1..2000 gwei realistic submission prices (the exploitability lens is REAL gas, not the 0.5-gwei ref).
  </behavior>
  <action>
    Add to `CrankFaucetResistance.t.sol` (mirroring `testMultiBoxSelfCrankRoundTripNonPositive` + `testFuzz_MultiBoxRoundTripNonPositiveAcrossGasPrices`): a `testRouterOpenSelfCrankRoundTripNonPositive` + fuzzed variant that drives `doWork()` opens at small batch sizes k in {1,2,3,4,5,>5}, computes the open-leg reward `(unit * min(k, OPEN_KNEE)) / OPEN_KNEE` valued back at `CRANK_GAS_PRICE_REF` (0.5 gwei), and asserts it is strictly below `gasUsed * gasPrice` for gasPrice fuzzed in [1 gwei, 2000 gwei]; and a `testRouterBuySelfCrankRoundTripNonPositive` doing the same for the flat `(unit * BUY_RATIO_NUM)/BUY_RATIO_DEN` buy reward vs the real autoBuy gas. Read the live `unit`/ratio/knee values from the contract (do not hardcode — the guard must hold for whatever 331-04 lands). Use the existing helper that converts BURNIE flip-credit back to ETH-at-peg (`_ethToBurnieValue` round-trip). The guards must be parameterized on the contract constants so they stay GREEN after 331-04 lands the calibrated values.
  </action>
  <verify>
    <automated>forge test --match-path test/fuzz/CrankFaucetResistance.t.sol -vv 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-path test/fuzz/CrankFaucetResistance.t.sol` exits 0.
    - The new open round-trip guard exercises k from 1 to >OPEN_KNEE and asserts reward-at-peg < real gas at the 1 gwei floor.
    - The new buy round-trip guard asserts the flat 1.5x reward-at-peg < real autoBuy gas.
    - Both fuzz across 1..2000 gwei; neither hardcodes the peg constant (reads from the contract).
  </acceptance_criteria>
  <done>The flat-per-tx open small-batch corner and the buy leg are both proven -EV round-trips at every realistic gas price.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the degeneretteResolve flat ~1-BURNIE round-trip guard (GAS-06 anti-exploit basis)</name>
  <files>test/fuzz/CrankFaucetResistance.t.sol</files>
  <read_first>
    - test/fuzz/CrankFaucetResistance.t.sol (after Task 1 — the round-trip helper pattern)
    - contracts/DegenerusGame.sol:1540-1620 (RESOLVE_FLAT_BURNIE :1543 + the >=3 non-WWXRP gate + NoWork()-at-zero + 1-2-resolved-unpaid)
    - test/gas/CrankResolveBetWorstCaseGas.t.sol (the real 3-resolution gas measurement to compare the reward against)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md (D-05c real-gas basis: >=220k for the >=3 min, illiquid credit <= mintPrice/1000 ETH)
  </read_first>
  <behavior>
    - A self-cranker resolves exactly 3 non-WWXRP bets (the minimum paid case), earns the flat ~1-BURNIE flip-credit ONCE, valued at <= mintPrice/1000 ETH. The real measured gas for >=3 resolutions * realPrice MUST exceed that credit at every realistic price → net loss.
    - Resolving 1-2 non-WWXRP bets earns NOTHING (unpaid-but-not-reverted) → trivially -EV.
    - Resolving 0 reverts NoWork().
    - WWXRP resolutions never count toward the >=3 gate nor the reward.
  </behavior>
  <action>
    Add a `testDegeneretteResolveFlatRewardRoundTripNonPositive` (+ fuzzed-gas variant) that: constructs >=3 resolvable non-WWXRP bets, measures the real `degeneretteResolve` gasUsed, computes the flat `RESOLVE_FLAT_BURNIE` credit valued back to ETH at <= `mintPrice/1000` (read `mintPrice()` live), and asserts that credit < `gasUsed * gasPrice` across 1..2000 gwei. Add a `testDegeneretteResolveBelowGateUnpaid` proving 1-2 resolutions commit but pay zero (read the keeper's flip-credit delta == 0), and a `testDegeneretteResolveZeroReverts` proving 0 resolutions reverts `NoWork()`. Read `RESOLVE_FLAT_BURNIE`'s effect via the keeper credit delta (do not hardcode 1e18 — the guard must hold for whatever 331-04 confirms/re-pegs). This is the GAS-06 sanity-check harness (TST-05 in Phase 332 owns the byte-identical-results proof; this plan only proves the reward stays sub-real-gas).
  </action>
  <verify>
    <automated>forge test --match-path test/fuzz/CrankFaucetResistance.t.sol -vv 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-path test/fuzz/CrankFaucetResistance.t.sol` exits 0.
    - The resolve round-trip guard proves the flat ~1-BURNIE credit < real >=3-resolution gas at 1..2000 gwei.
    - The 1-2-resolved-unpaid case asserts keeper credit delta == 0.
    - The 0-resolved case asserts a `NoWork()` revert.
    - WWXRP is excluded from both the gate count and the reward in the constructed scenario.
  </acceptance_criteria>
  <done>The degeneretteResolve flat reward is proven a net loss at any realistic gas; the >=3 gate / unpaid-tail / revert-on-zero / WWXRP-exclusion behaviors are guarded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| permissionless keeper → doWork() / degeneretteResolve() | untrusted caller earns minted BURNIE flip-credit; the faucet must never be positive-EV |
| flip-credit → ETH value | the bounty is ILLIQUID minted credit (<= mintPrice/1000 ETH for resolve); over-valuing it as liquid would mis-judge exploitability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-331-03 | Elevation of privilege | self-crank faucet (open small-batch corner) | mitigate | WR-01 round-trip guard proves per-box reward below the OPEN_KNEE <= one-box tx gas at >=1 gwei; the knee pro-rate is the structural closer (GAS-05) |
| T-331-04 | Elevation of privilege | degeneretteResolve flat-reward farm | mitigate | round-trip guard proves ~1-BURNIE credit < real >=3-resolution gas at 1..2000 gwei; judged vs REAL gas + flip-credit illiquidity, NOT the 0.5-gwei peg ref (`feedback_bounty_exploit_uses_real_gas_not_peg_ref`) |
| T-331-05 | Spoofing | Sybil multi-account self-crank | accept | bounty is bounded once/day/sub (buy) + un-fakeable real ticket batches (advance) + per-box knee (open); no per-account amplification beyond the proven -EV per-tx floor |
| T-331-SC | Tampering | npm/forge deps | accept | test-only plan; no package installs; no contract mutation |
</threat_model>

<verification>
- `forge test --match-path test/fuzz/CrankFaucetResistance.t.sol` exits 0.
- All new guards fuzz against REAL gas (1..2000 gwei), not the 0.5-gwei reference.
- No `contracts/*.sol` modified by this plan.
</verification>

<success_criteria>
GAS-05 satisfied: a WR-01-style round-trip guard proves no positive-EV self-crank loop under the flat-per-tx model (open small-batch corner + buy leg), and the GAS-06 degeneretteResolve flat reward is proven sub-real-gas — all judged against real prevailing gas + flip-credit illiquidity.
</success_criteria>

<output>
Create `.planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-02-SUMMARY.md` when done.
</output>
