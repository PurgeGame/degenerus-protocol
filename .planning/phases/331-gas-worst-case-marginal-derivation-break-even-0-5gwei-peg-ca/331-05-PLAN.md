---
phase: 331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca
plan: 05
type: execute
wave: 3
depends_on: ["331-02", "331-03", "331-04"]
files_modified:
  - contracts/AfKing.sol
  - contracts/DegenerusGame.sol
  - contracts/interfaces/IDegenerusGame.sol
autonomous: false
requirements: [GAS-02, GAS-04, GAS-06]
user_setup: []
must_haves:
  truths:
    - "The five frozen AfKing peg constants are landed at the 331-04 calibrated values (break-even 0.5 gwei, per-item marginal)."
    - "RESOLVE_FLAT_BURNIE is confirmed or re-pegged to the 331-04 sub-real-gas value; the >=3-non-WWXRP gate / NoWork()-at-zero / 1-2-unpaid logic is unchanged (incentive number only, no result change)."
    - "Seed 1 (shared-slot DGNRS affiliate write aggregation) + Seed 2 (pre-validated keeper batch path replacing the try/catch) land per the 331-03 design, preserving the per-player no-brick liveness floor and byte-identical money outcomes."
    - "The single batched diff compiles, the full suite is at or above the v48 baseline, and the diff is HELD at the contract-commit boundary for explicit USER hand-review (no commit without approval)."
  artifacts:
    - path: "contracts/AfKing.sol"
      provides: "calibrated peg constants (DOWORK_BATCH/ADVANCE_RATIO_NUM/BUY_RATIO_NUM/BUY_RATIO_DEN/OPEN_KNEE)"
      contains: "DOWORK_BATCH"
    - path: "contracts/DegenerusGame.sol"
      provides: "RESOLVE_FLAT_BURNIE re-peg + Seed 1 aggregation + Seed 2 keeper batch path"
      contains: "RESOLVE_FLAT_BURNIE"
  key_links:
    - from: "contracts/DegenerusGame.sol (keeper batch path)"
      to: "test/fuzz/KeeperBatchAffiliateDeltaAudit.t.sol + test/fuzz/CrankNonBrick.t.sol"
      via: "the _drive(useKeeperPath) equivalence + no-brick proofs flip GREEN"
      pattern: "batchPurchaseForKeeper|useKeeperPath"
---

<objective>
Land the v49 keeper-router peg calibration + the Seed 1/2 money-path gas code as ONE batched `contracts/*.sol` diff: the five frozen AfKing constants at the 331-04 values, the `RESOLVE_FLAT_BURNIE` confirm/re-peg, Seed 1 (aggregate shared-slot DGNRS affiliate writes), and Seed 2 (the pre-validated keeper batch path replacing the per-player try/catch). HELD at the contract-commit boundary for explicit USER hand-review. GAS-02 + GAS-04 + GAS-06.

Purpose: This is the SECOND USER-approved contract gate of the milestone (the v46 Phase 319 precedent). The pre-gate plans (331-01..04) measured, proposed, and pre-wrote the verifying tests autonomously; this plan applies the exact diff and proves it against those tests, then STOPS for hand-review. INCENTIVE/gas-shape changes ONLY — must NOT change RNG/freeze/payout-tables/resolution RESULTS.

Output: the applied (uncommitted) `contracts/*.sol` diff + a verification record; HARD STOP for USER approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-CONTEXT.md
@.planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-PATTERNS.md
@.planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-CALIBRATION.md
@.planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-SEEDS-DESIGN.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md

<interfaces>
<!-- The exact edit targets (re-grep at edit time; the committed 330 tree is 63bc16ca): -->
- contracts/AfKing.sol:847-854 — the five frozen `internal constant`s (land 331-04 values)
- contracts/DegenerusGame.sol:1543 — RESOLVE_FLAT_BURNIE (confirm/re-peg per 331-04)
- contracts/DegenerusGame.sol:1757-1810 — batchPurchase try/catch + _batchPurchaseUnit + DGNRS wiring (Seed 1/2 surface)
- contracts/DegenerusAffiliate.sol — payAffiliate per-buy writes (Seed 1 coalescible targets; edit only if the aggregation reaches in, per 331-03)
- contracts/interfaces/IDegenerusGame.sol — add a `batchPurchaseForKeeper` row IF 331-03 chose a new external function (else no interface change)

<!-- The pre-written verifying tests (331-02 / 331-03) this diff must turn GREEN: -->
- test/fuzz/CrankFaucetResistance.t.sol — round-trip <= 0 against the LANDED peg
- test/fuzz/KeeperBatchAffiliateDeltaAudit.t.sol — byte-identical money outcomes current-vs-keeper path (_drive(useKeeperPath))
- test/fuzz/CrankNonBrick.t.sol — poisoned-player no-brick under the keeper path
</interfaces>

<!-- CONTRACT-COMMIT POLICY (project feedback, non-negotiable):
  - Commit ALL planning/test/doc artifacts BEFORE touching any contracts/*.sol (the commit-guard
    hook blocks commits while contracts/*.sol is dirty; .planning/ is gitignored — force-add).
  - Apply ALL contract edits as ONE batched diff; present it for ONE approval at the end.
  - NEVER commit a mainnet contracts/*.sol change without explicit USER hand-review of the diff.
  - ContractAddresses.sol is freely modifiable; every other mainnet contracts/*.sol is gated.
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-gate hygiene + apply the calibrated constants + the RESOLVE_FLAT_BURNIE re-peg</name>
  <files>contracts/AfKing.sol, contracts/DegenerusGame.sol</files>
  <read_first>
    - .planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-CALIBRATION.md (the exact-diff table: file:line + current + proposed for every constant)
    - contracts/AfKing.sol:847-892 (the five constants + the unit conversion + the leg formulas — re-grep the current literals)
    - contracts/DegenerusGame.sol:1540-1543 (RESOLVE_FLAT_BURNIE) + :1585-1622 (the >=3 gate it feeds — MUST NOT change)
    - test/fuzz/CrankFaucetResistance.t.sol (the round-trip guard the landed peg must satisfy)
  </read_first>
  <action>
    First confirm `contracts/*.sol` is CLEAN on disk before editing (`git status --porcelain contracts/` empty) and that all .planning/test artifacts from 331-01..04 are committed (the commit-guard hook blocks commits once contracts/ is dirty). Then EDIT the five frozen `internal constant`s in `contracts/AfKing.sol:847-854` to the 331-04 calibrated literals (DOWORK_BATCH, ADVANCE_RATIO_NUM, BUY_RATIO_NUM, BUY_RATIO_DEN, OPEN_KNEE) and remove the `GAS-331 PLACEHOLDER` markers from those NatSpec lines (replace with the calibration provenance — "calibrated at GAS phase 331 from the worst-case marginal at 0.5 gwei", NO change-history per `feedback_no_history_in_comments`). EDIT `contracts/DegenerusGame.sol:1543` `RESOLVE_FLAT_BURNIE` to the 331-04 confirmed/re-pegged value and drop its placeholder marker. Do NOT touch the >=3-non-WWXRP gate logic, the NoWork() revert, the 1-2-unpaid path, or any resolution result (incentive number only). Grep-sync any `test/` mirror that hardcodes a peg constant (`grep -rln "DOWORK_BATCH\|OPEN_KNEE\|RESOLVE_FLAT_BURNIE\|ADVANCE_RATIO_NUM\|BUY_RATIO" test/`) so peg-equality assertions stay GREEN.
  </action>
  <verify>
    <automated>forge build 2>&1 | tail -5 && forge test --match-path test/fuzz/CrankFaucetResistance.t.sol 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - The five AfKing constants + RESOLVE_FLAT_BURNIE hold the exact 331-04 calibrated values (grep confirms the literals).
    - The `GAS-331 PLACEHOLDER` markers are removed from those constants (no remaining "PLACEHOLDER" on the calibrated lines).
    - `forge build` exits 0 and `CrankFaucetResistance.t.sol` round-trip guards are GREEN against the LANDED peg.
    - The >=3-non-WWXRP gate / NoWork() / 1-2-unpaid logic is byte-unchanged (no diff in DegenerusGame.sol:1585-1622).
  </acceptance_criteria>
  <done>The calibrated peg constants + RESOLVE_FLAT_BURNIE are landed; the round-trip guard is GREEN; no resolution logic changed.</done>
</task>

<task type="auto">
  <name>Task 2: Land Seed 1 + Seed 2 (keeper batch path + shared-slot affiliate aggregation)</name>
  <files>contracts/DegenerusGame.sol, contracts/interfaces/IDegenerusGame.sol</files>
  <read_first>
    - .planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-SEEDS-DESIGN.md (the revert-source dispositions + the chosen path shape + the coalescible-SSTORE set + the acc-flush shape)
    - contracts/DegenerusGame.sol:1757-1810 (batchPurchase try/catch + _batchPurchaseUnit + the DGNRS wiring)
    - contracts/DegenerusAffiliate.sol:388-615 + :772-803 (the coalescible affiliate writes Seed 1 aggregates)
    - contracts/modules/DegenerusGameDegeneretteModule.sol:407-426 (the acc-flush precedent)
    - contracts/AfKing.sol:561-720 (_autoBuy — the keeper-side pre-validation already done view-side) + :793-840 (_resolveBuy)
    - test/fuzz/KeeperBatchAffiliateDeltaAudit.t.sol + test/fuzz/CrankNonBrick.t.sol (the pre-written verifying tests, incl. the TODO-331-05 gates to flip ON)
  </read_first>
  <action>
    Implement Seed 2 per 331-SEEDS-DESIGN.md: the chosen keeper-specialized batch path (new `batchPurchaseForKeeper` external function OR parameterized internal helper — per the design's decision) that PRE-VALIDATES or cheap-skips each `_batchPurchaseUnit` revert source so the unit cannot revert (mirroring the autoOpen RD-5 entry-gate technique), replacing the per-player `try this._batchPurchaseUnit{value:slice} {} catch {}`. The HARD CONSTRAINT: a reverting/funding-skipped player MUST still be cheap-skipped + refunded — the per-player isolation liveness is preserved by construction. Leave the normal player-facing `_purchaseFor`/`batchPurchase` player semantics untouched (the keeper path is separate). Implement Seed 1 per the design: keeper units RETURN their affiliate contribution; sum the COALESCIBLE shared-slot DGNRS writes (affiliateCoinEarned[lvl][owner], _totalAffiliateScore[lvl], the leaderboard, the SDGNRS/VAULT flip-credit, the winner quest accumulators) into an acc struct across the loop; ONE flush at the tail (mirror the degenerette resolveBets acc-flush). Keep the per-player `affiliateCommissionFromSender` non-coalescible (per-player). Sum ONLY successful (non-reverted/non-skipped) units. If 331-03 chose a new external function, add the `batchPurchaseForKeeper` row to `contracts/interfaces/IDegenerusGame.sol` and update the AfKing call site (`_autoBuy` -> the new path) accordingly. Then flip the `_drive(useKeeperPath)` + TODO-331-05 gates in `KeeperBatchAffiliateDeltaAudit.t.sol` + `CrankNonBrick.t.sol` to exercise the new path.
  </action>
  <verify>
    <automated>forge build 2>&1 | tail -5 && forge test --match-path test/fuzz/KeeperBatchAffiliateDeltaAudit.t.sol --match-path test/fuzz/CrankNonBrick.t.sol 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` exits 0.
    - `KeeperBatchAffiliateDeltaAudit.t.sol` proves byte-identical affiliate/claimable/DGNRS accumulators between the current path and the new keeper path (`_drive(useKeeperPath)` GREEN, gates flipped).
    - `CrankNonBrick.t.sol` proves a poisoned/un-fundable player is skip-and-continued + refunded under the new path (no-brick), at first/middle/last position.
    - Seed 1 aggregates ONLY coalescible shared-slot writes; affiliateCommissionFromSender stays per-player; only successful units sum.
    - The normal player-facing `batchPurchase`/`_purchaseFor` semantics are unchanged (the keeper path is separate).
  </acceptance_criteria>
  <done>Seed 1+2 land with byte-identical money outcomes, the no-brick liveness floor preserved, and the player-facing path untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: BATCH contract-diff hand-review gate (HARD STOP)</name>
  <files>contracts/AfKing.sol, contracts/DegenerusGame.sol, contracts/interfaces/IDegenerusGame.sol</files>
  <action>
    HARD STOP at the contract-commit boundary. Present the single batched `contracts/*.sol` diff for ONE explicit USER hand-review; do NOT commit any mainnet contract until the USER types "approved" (`feedback_wait_for_approval`, `feedback_never_preapprove_contracts`, `feedback_batch_contract_approval`). On approval: commit the planning/test/doc artifacts FIRST, then commit the contract diff with CONTRACTS_COMMIT_APPROVED=1.
    What was built: the five calibrated AfKing peg constants (AfKing.sol:847-854), the RESOLVE_FLAT_BURNIE re-peg (DegenerusGame.sol:1543), Seed 1 (shared-slot DGNRS affiliate write aggregation), Seed 2 (the pre-validated keeper batch path replacing the per-player try/catch), and any IDegenerusGame.sol interface row — applied to contracts/ and locally compiling/tested, HELD at the contract-commit boundary.
  </action>
  <what-built>
    The single batched `contracts/*.sol` diff for Phase 331 GAS: the five calibrated AfKing peg constants (AfKing.sol:847-854), the RESOLVE_FLAT_BURNIE re-peg (DegenerusGame.sol:1543), Seed 1 (shared-slot DGNRS affiliate write aggregation), and Seed 2 (the pre-validated keeper batch path replacing the per-player try/catch) — plus any IDegenerusGame.sol interface row. Applied to `contracts/` and locally compiling/tested, HELD at the contract-commit boundary.
  </what-built>
  <how-to-verify>
    1. Review the full diff: `git diff -- contracts/`
    2. Confirm the calibrated constant values match 331-CALIBRATION.md and that exploitability is judged vs real prevailing gas (5-50+ gwei), not the 0.5-gwei peg ref.
    3. Confirm `forge build` exits 0: `forge build`
    4. Confirm the verifying suite is GREEN and the full suite is at/above the v48 baseline (616/58 = v48 632/42 + 16 deferred-to-332): `forge test 2>&1 | tail -20`
    5. Confirm the Seed 1/2 money-path is byte-identical + un-brickable: `forge test --match-path test/fuzz/KeeperBatchAffiliateDeltaAudit.t.sol --match-path test/fuzz/CrankNonBrick.t.sol`
    6. Confirm INCENTIVE/gas-shape changes ONLY — no RNG/freeze/payout-table/resolution-result change (read the degeneretteResolve gate + the player-facing batchPurchase are untouched).
    7. If approved, the agent commits the planning/test artifacts first, then commits the contract diff with CONTRACTS_COMMIT_APPROVED=1.
  </how-to-verify>
  <verify>
    <human-check>USER reviews `git diff -- contracts/`, confirms forge build/test green + suite at-or-above v48 baseline + incentive-only (no result change), and types "approved" before any contract commit.</human-check>
  </verify>
  <acceptance_criteria>
    - The full contract diff is presented to the USER for hand-review.
    - No mainnet `contracts/*.sol` is committed before the USER types "approved".
    - On approval, planning/test artifacts are committed first, then the contract diff with CONTRACTS_COMMIT_APPROVED=1.
  </acceptance_criteria>
  <done>The batched contract diff is approved by the USER and committed (or revised per USER feedback); no contract committed without explicit hand-review.</done>
  <resume-signal>Type "approved" to commit the batched contract diff, or describe required changes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| permissionless keeper → calibrated bounty | the landed peg must keep the self-crank round-trip <= 0 at the 0.5-gwei ref and strictly -EV at real gas |
| keeper batch → per-player mint (Seed 2) | the pre-validated path must preserve per-player isolation (no-brick) — a SECURITY property |
| keeper batch → shared DGNRS affiliate slot (Seed 1) | aggregation must not double-credit or leak across players |
| agent → mainnet contracts/*.sol | no commit without explicit USER hand-review |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-331-11 | Elevation of privilege | over-pegged landed constant (faucet) | mitigate | the LANDED peg is re-proven <= 0 round-trip by CrankFaucetResistance.t.sol (Task 1 verify); pegged at/below the per-item marginal (CR-01) |
| T-331-12 | Denial of service | poisoned player bricks the keeper batch (Seed 2) | mitigate | CrankNonBrick.t.sol proves skip-and-continue + refund under the pre-validated path at every poison position; the isolation liveness is preserved by construction |
| T-331-13 | Tampering | Seed 1 double-credit / cross-player leak | mitigate | KeeperBatchAffiliateDeltaAudit.t.sol asserts byte-identical accumulators vs the current path; only successful units sum; commission stays per-player |
| T-331-14 | Tampering | resolution RESULT drift from the re-peg | mitigate | RESOLVE_FLAT_BURNIE is an incentive number; the >=3 gate / NoWork() / payout logic is byte-unchanged; TST-05 (Phase 332) proves byte-identical resolution results |
| T-331-SC | Tampering | npm/forge deps | accept | no package installs in this phase; the diff is local contract edits only; commit-guard hook + the blocking hand-review gate are the supply-chain backstop |
| T-331-AUTH | Spoofing | unapproved contract commit | mitigate | autonomous:false + the blocking-human gate; commit only with CONTRACTS_COMMIT_APPROVED=1 after USER approval (`feedback_wait_for_approval`, `feedback_never_preapprove_contracts`) |
</threat_model>

<verification>
- `forge build` exits 0.
- `CrankFaucetResistance.t.sol` round-trip guards GREEN against the landed peg.
- `KeeperBatchAffiliateDeltaAudit.t.sol` + `CrankNonBrick.t.sol` GREEN with the keeper-path gates flipped.
- Full suite at/above the v48 baseline (616/58, = v48 632/42 + 16 reward-rehoming deferred to Phase 332).
- The diff is HELD for USER hand-review; no contract commit until "approved".
</verification>

<success_criteria>
GAS-02 + GAS-04 + GAS-06 landed: the calibrated peg constants + the RESOLVE_FLAT_BURNIE re-peg + Seed 1/2 money-path gas code applied as ONE batched diff, proven by the pre-written round-trip / delta-audit / no-brick tests, INCENTIVE/gas-shape only (no result change), and HELD at the contract-commit boundary for explicit USER hand-review.
</success_criteria>

<output>
Create `.planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-05-SUMMARY.md` when done.
</output>
