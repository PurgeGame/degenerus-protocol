---
phase: 331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca
plan: 04
type: execute
wave: 2
depends_on: ["331-01"]
files_modified:
  - .planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-CALIBRATION.md
autonomous: true
requirements: [GAS-02, GAS-04]
must_haves:
  truths:
    - "The five frozen AfKing constants (DOWORK_BATCH, ADVANCE_RATIO_NUM, BUY_RATIO_NUM, BUY_RATIO_DEN, OPEN_KNEE) + the RESOLVE_FLAT_BURNIE literal have proposed exact calibrated values derived from the 331-01 measured marginals at break-even 0.5 gwei."
    - "The peg uses the per-item MARGINAL (CR-01 rule), never a per-call/single-box total; the proposed values are pegged AT or BELOW the marginal (with any keeper-liveness round-up not breaching the faucet floor)."
    - "The BOUNTY_ETH_TARGET deploy-param recommendation is surfaced (it is an AfKing ctor immutable / deploy-script param, NOT a gated frozen constant) with its break-even ETH value derived from the marginals."
    - "The conversion math is confirmed level-invariant across >=2 mintPrice levels."
    - "The 1/2/4/6 stall ladder is kept ADVANCE-ONLY; whether to extend the ceiling ABOVE the 2h tier is decided from the GAS data and any extension is capped against the finite faucet pool."
  artifacts:
    - path: ".planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-CALIBRATION.md"
      provides: "The proposed exact constant values + the break-even derivation + the level-invariance proof + the stall-ceiling decision + the exact diff 331-05 applies"
  key_links:
    - from: ".planning/phases/331-CALIBRATION.md"
      to: "contracts/AfKing.sol:847-854 + contracts/DegenerusGame.sol:1543"
      via: "the exact proposed literal values + file:line targets"
      pattern: "DOWORK_BATCH|RESOLVE_FLAT_BURNIE"
---

<objective>
Convert the 331-01 measured worst-case marginals into the proposed exact calibrated values for the five frozen AfKing peg constants + the `RESOLVE_FLAT_BURNIE` literal + the `BOUNTY_ETH_TARGET` deploy-param recommendation, at break-even 0.5 gwei using the per-item MARGINAL (CR-01 rule), prove the conversion level-invariant, and decide the stall-ceiling. Produce `331-CALIBRATION.md` — the exact diff the gated 331-05 plan applies. GAS-02 + GAS-04.

Purpose: The peg cannot be a guess; it is a deterministic function of the measured marginals at 0.5 gwei. This is the AUTONOMOUS pre-gate half of the v46 Phase 319 precedent — derive + propose the exact constants here; 331-05 (autonomous:false) lands them under the USER gate. The CR-01 rule is the load-bearing constraint: peg the per-item MARGINAL at N>=32, never a single-item total, pegged AT or BELOW the marginal so the 0.5-gwei round-trip is <= 0.

Output: `331-CALIBRATION.md` (doc-only, AGENT-committable). NO contract edits in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-CONTEXT.md
@.planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-PATTERNS.md
@.planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-GAS-DERIVATION.md

<interfaces>
<!-- The frozen GATED constants (AfKing.sol:847-854) — calibrate these literals: -->
- DOWORK_BATCH = 100        (fixed per-leg default batch)
- ADVANCE_RATIO_NUM = 2     (advance reward = unit * 2 * mult)
- BUY_RATIO_NUM = 3 / BUY_RATIO_DEN = 2   (buy reward = unit * 3/2 = flat 1.5x)
- OPEN_KNEE = 5             (open reward = unit * min(opened, KNEE)/KNEE)

<!-- The GATED resolve literal (DegenerusGame.sol:1543): -->
- RESOLVE_FLAT_BURNIE = 1e18  (flat ~1-BURNIE once/tx at >=3 non-WWXRP)

<!-- The SURFACED-NOT-GATED deploy param (AfKing.sol:261, ctor immutable, set :277): -->
- BOUNTY_ETH_TARGET   (the break-even ETH target; AGENT-editable in the degenerus-utilities deploy script, NOT a frozen constant — surfaced for the USER like 319)

<!-- The conversion (AfKing.sol:869-892): -->
- unit = (BOUNTY_ETH_TARGET * PRICE_COIN_UNIT) / mintPrice   (PRICE_COIN_UNIT = 1000 ether; mp cancels => ETH-equiv recovers to BOUNTY_ETH_TARGET wei at every level)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive the proposed constants + the break-even unit from the measured marginals</name>
  <files>.planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-CALIBRATION.md</files>
  <read_first>
    - .planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-GAS-DERIVATION.md (the 331-01 measured marginals: buy/open/advance/dispatch)
    - .planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-PATTERNS.md (the "Rounding/landing policy" + "Level-invariance check" + the 319 calibration analog)
    - contracts/AfKing.sol:222-277 (TICKET_SCALE/PRICE_COIN_UNIT/BOUNTY_ETH_TARGET defs + the ctor), :847-892 (the constants + the unit conversion + the three leg formulas)
    - .planning/milestones (the v46 Phase 319 calibration records `319-GAS-DERIVATION.md`/`319-GAS-06-CALIBRATION.md`/`319-CR01-FIX.md` if present, for the AT-or-BELOW-marginal landing policy)
  </read_first>
  <action>
    Author `.planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-CALIBRATION.md` (analog `319-GAS-06-CALIBRATION.md`). Compute the break-even at 0.5 gwei from the 331-01 marginals: the heaviest single-category max-laden per-tx gas at 0.5 gwei fixes the `1x` base unit; derive `BOUNTY_ETH_TARGET` (the deploy-param) as the break-even ETH value such that `unit * ratio` reimburses each leg's real worst-case gas at 0.5 gwei. Set the per-category ratios from the RELATIVE per-category marginals: `ADVANCE_RATIO_NUM` (advance vs unit), `BUY_RATIO_NUM/BUY_RATIO_DEN` (buy vs unit, the flat 1.5x), and `OPEN_KNEE` (the pro-rate knee sized so a single mid-day open below the knee is -EV — the CR-01 small-batch closer). Validate or re-propose the current placeholders (2, 3/2, 5, batch 100): if the measured marginals support them, confirm; else propose exact new literals with the arithmetic shown. Apply the AT-or-BELOW-marginal landing policy (peg never above the marginal — a value above opens a Sybil faucet at 0.5 gwei); any keeper-liveness round-up must not breach the faucet floor (the floor lives in BOUNTY_ETH_TARGET * ratio, not the dimensionless ratio). Record `DOWORK_BATCH` sized so the worst-case per-tx for the default batch stays well under 30M (from 331-01). Present the proposed values as an exact-diff table: file:line target, current literal, proposed literal, derivation.
  </action>
  <verify>
    <automated>test -f .planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-CALIBRATION.md && grep -q "DOWORK_BATCH\|ADVANCE_RATIO_NUM\|OPEN_KNEE\|RESOLVE_FLAT_BURNIE\|BOUNTY_ETH_TARGET" .planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-CALIBRATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 331-CALIBRATION.md proposes exact literal values for all five frozen constants + RESOLVE_FLAT_BURNIE + the BOUNTY_ETH_TARGET deploy-param, each with the break-even-at-0.5-gwei arithmetic from the 331-01 marginals.
    - Every proposed value is pegged AT or BELOW the per-item marginal (CR-01); no per-call/single-box total is used as a peg.
    - The exact-diff table lists file:line target + current + proposed for each constant (AfKing.sol:847-854 + DegenerusGame.sol:1543).
    - BOUNTY_ETH_TARGET is clearly marked SURFACED-NOT-GATED (deploy param, not a frozen constant).
  </acceptance_criteria>
  <done>Every peg constant has a proposed exact value with shown derivation; the exact diff 331-05 applies is specified.</done>
</task>

<task type="auto">
  <name>Task 2: Prove level-invariance + decide the stall ceiling (GAS-04)</name>
  <files>.planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-CALIBRATION.md</files>
  <read_first>
    - .planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-CALIBRATION.md (Task 1 — the proposed values)
    - contracts/AfKing.sol:222-227 (TICKET_SCALE NatSpec), :869-870 (the unit conversion), :241-253 reference in 329-SPEC (the AdvanceModule GAME-day stall epoch — the SOLE epoch after D-07)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md (the D-07 ADVANCE-ONLY stall + the 1/2/4/6 ladder + the faucet-bound constraint + invariant (d) single-epoch-by-deletion)
    - test/gas/SweepPerPlayerWorstCaseGas.t.sol:188-211 (the shape-insensitivity / level-invariance idiom)
  </read_first>
  <action>
    In `331-CALIBRATION.md` add a level-invariance section: show `unit = (BOUNTY_ETH_TARGET * 1000e18) / mp` recovers the same ETH-equivalent reward at >=2 mintPrice levels (mp cancels the BURNIE<->ETH conversion), citing the SweepPerPlayer shape-insensitivity idiom as the empirical template (note that the empirical level-invariance assertion is proven at TST Phase 332; here, show the arithmetic). Add a GAS-04 stall-ceiling decision section: confirm the 1/2/4/6 ladder stays ADVANCE-ONLY (the autoBuy stall ladder was deleted per D-07; advance is the sole stall epoch — invariant d satisfied-by-deletion). DECIDE from the GAS data whether 6x covers stressed mainnet gas (5-50+ gwei) at a plausible deep stall: compute the advance reward at 6x valued at peg vs the real advance gas at the high end; if 6x does NOT cover a plausible deep-stall advance at stressed gas, propose an extension tier ADDED ABOVE the 2h tier (never lowering existing thresholds) capped against the finite faucet pool (state the faucet-pool ceiling and the cap arithmetic). If 6x suffices, record NO EXTENSION with the supporting math. State explicitly that exploitability is judged against REAL prevailing gas + flip-credit illiquidity, not the 0.5-gwei ref.
  </action>
  <verify>
    <automated>grep -q -i "level-invarian\|stall\|1/2/4/6\|ceiling" .planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-CALIBRATION.md</automated>
  </verify>
  <acceptance_criteria>
    - The conversion is shown level-invariant across >=2 mintPrice levels with the mp-cancellation arithmetic.
    - The 1/2/4/6 ladder is confirmed ADVANCE-ONLY; existing thresholds are not lowered.
    - The stall-ceiling decision (extend above 2h vs no extension) is made FROM the GAS data with the supporting math; any extension is faucet-pool-capped.
    - Exploitability is stated to be judged vs real prevailing gas, not the 0.5-gwei reference.
  </acceptance_criteria>
  <done>Level-invariance is proven arithmetically; the GAS-04 stall-ceiling decision is data-derived and faucet-bounded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured marginal → proposed peg | an over-stated peg (above the marginal) opens a Sybil self-crank faucet at 0.5 gwei |
| 0.5-gwei reference → real prevailing gas | the peg references 0.5 gwei but exploitability must be judged at real 5-50+ gwei |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-331-09 | Elevation of privilege | over-pegged constant (faucet open) | mitigate | peg AT or BELOW the per-item marginal (CR-01); the 331-02 round-trip guard re-proves <= 0 against the proposed values; any liveness round-up checked against the faucet floor |
| T-331-10 | Elevation of privilege | stall-multiplier abuse (6x faucet) | mitigate | 1/2/4/6 ADVANCE-ONLY (un-fakeable real ticket batches, liveness-critical); any ceiling extension faucet-pool-capped; judged vs real prevailing gas (`feedback_bounty_exploit_uses_real_gas_not_peg_ref`) |
| T-331-SC | Tampering | npm/forge deps | accept | doc-only plan; no package installs; no contract mutation (constants land in gated 331-05) |
</threat_model>

<verification>
- 331-CALIBRATION.md proposes exact values for every gated constant + the deploy-param, each with derivation.
- Level-invariance + the stall-ceiling decision are recorded.
- No `contracts/*.sol` modified by this plan (`git diff --name-only -- contracts/` empty for this plan's changes).
</verification>

<success_criteria>
GAS-02 + GAS-04 derived: the exact calibrated constants are proposed from the measured marginals at break-even 0.5 gwei using the per-item MARGINAL (pegged at/below it), the conversion is level-invariant, and the ADVANCE-ONLY 1/2/4/6 ladder + faucet-bounded ceiling decision are data-derived — the exact diff for the gated 331-05 plan.
</success_criteria>

<output>
Create `.planning/phases/331-gas-worst-case-marginal-derivation-break-even-0-5gwei-peg-ca/331-04-SUMMARY.md` when done.
</output>
