# Phase 320 Adversarial Sweep — Per-Skill Disposition: `/economic-analyst` (PARALLEL_SUBAGENT)

```yaml
[invocation]
skill: /economic-analyst
mode: PARALLEL_SUBAGENT
dispatch_timestamp: "2026-05-24T00:00:00Z"
runner: task-subagent
fallback_reason: null
charge_anchor: ".planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-ADVERSARIAL-CHARGE.md"
note: "dispatched in the PARALLEL multi-Task message alongside /zero-day-hunter, anchored on the /contract-auditor disposition MD (the FIRST/anchor). Owns the EV/incentive layer; diverges from the anchor by proving non-positive EV even where the structural path is reachable, not re-confirming the structural gates."
```

```yaml
[skeptic-filter]
discarded: []
```

> **Source-tree note (load-bearing).** This subagent was spawned in worktree `.claude/worktrees/agent-a84d76d4ee3148736`. The STALE-WORKTREE GUARD `grep -c "fundingSource" contracts/AfKing.sol` returned **21** at dispatch — this worktree carries the OPEN-E-bearing audit-subject HEAD (NOT stale). All re-greps in this MD were run against this tree's `contracts/`, which equals the byte-frozen `30b5c89c..HEAD` MAIN audit subject (`AfKing.sol` carrying the 21 `fundingSource` references = OPEN-E diff `42140ceb` + WR-01 event `e1baa978`). Every `file:line` below was independently re-grepped per `feedback_verify_call_graph_against_source.md`; the CHARGE's `:396`/`:587` `burnForKeeper` anchors were confirmed STALE (live = `:438` subscribe-time / `:634` day-31), and the anchor MD's drifted lines were re-verified, not copied.

---

## §0 — Charged SWP sub-charges (verbatim re-anchor)

This skill (`/economic-analyst`) owns the **incentive / EV** surfaces (CHARGE §0 composition + §SWP-ECON + §SWP-SKIP griefer + §SWP-OPENE.4 + §SWP-REMOVE 75bps + beyond-charge):

- **SWP-ECON — coinflip-credit recycle + BURNIE-supply interaction.** "**Coinflip-credit recycle:** can a player recycle coinflip credit / claimable through the BURNIE flip + the crank bounty to extract positive EV (a perpetual-motion credit loop)? Model the 75bps flat unconditional recycle EV (cross-ref SWP-REMOVE). **BURNIE-supply interaction:** BURNIE is uncapped-mintable for game rewards but the crank bounty is funded from a FINITE pool (transfer, mirror `_awardDegeneretteDgnrs`), NOT `mintForGame`. Disposition whether the subscription/crank emission can inflate BURNIE supply beyond the intended faucet ceiling, or interact with the flip economy to mint-and-recycle."

- **SWP-SKIP (griefer arm) — funding-margin force-cancel.** "Can a funding-margin griefer force-cancel a fragile claimable-only sub — i.e., push a victim sub just under its funding margin at the skip-kill boundary to trigger the auto-pause? Disposition the kill trigger's caller-independence (does any third-party crank action change WHO gets killed, or only WHETHER the victim's own funding covers the charge?)."

- **SWP-OPENE.4 — trust-the-sub temporal-bound EV + D-02 overload cost/benefit.** "A later `setOperatorApproval(M,false)` does NOT retroactively stop an active sub; an active funding sub's drain is bounded only by sub lifetime + S-defunding. This is SAFE_BY_DESIGN per D-03.4 — confirm the drain IS bounded… so it is not an UNBOUNDED drain. Also model the D-02 BURNIE-funding-overload cost/benefit… record SAFE_BY_DESIGN per D-01/D-02 (operator-approval IS the trust boundary; do NOT model a 'tricked into approving' actor; the `allowBurnieFunding` flag is DROPPED)."

- **SWP-REMOVE.B (75bps arm) — BURNIE flip-autorebuy collapse to flat 75bps unconditional.** "Cost/benefit of any rounding/scaling residue; can it be gamed for under/over-credit?"

- **Beyond-charge rows** — any additional incentive misalignment / MEV / coordination vector in the v46.0 add+remove+OPEN-E surface.

---

## §1 — Per-hypothesis disposition table

All `file:line` anchors RE-GREPPED against this worktree's HEAD (= byte-frozen audit subject). Decisive economic primitives confirmed:

- **The crank bounty is a deferred 50/50 coinflip STAKE, never liquid BURNIE.** `sweep()` pays exactly ONE `creditFlip(msg.sender, bountyEarned)` per tx (`AfKing.sol:802`), `bountyEarned = batchLen * ((BOUNTY_ETH_TARGET * PRICE_COIN_UNIT * bountyMultiplier) / mp)` (`:801`). `creditFlip` (`BurnieCoinflip.sol:849-854`) routes to `_addDailyFlip` (`:854`) → `coinflipBalance[targetDay][cranker]` (`:615`). It is a STAKE on a future 50/50 flip, NOT a mint.
- **The flip is a true 50/50 with a sub-2× win multiplier; loss forfeits principal.** `processCoinflipPayouts` sets `win = (rngWord & 1) == 1` (`BurnieCoinflip.sol:788`). On a WIN, `payout = stake + stake * rewardPercent% / 100` (`:484-486`) where `rewardPercent ∈ [78,115]` normal (`:774-777`) + 5%-chance `50` + 5%-chance `150` (`:767-772`). On a LOSS the stake is **forfeited** (`:477` "loss = forfeit principal", `:478` clears `coinflipBalance` to 0); only `lossCount` increments → a WWXRP (separate token) consolation of `1 ether` each (`:575` `wwxrp.mintPrize`, `COINFLIP_LOSS_WWXRP_REWARD = 1 ether` `:126`).
- **BURNIE only mints via the COINFLIP/GAME-gated `mintForGame`.** `BurnieCoin.mintForGame` reverts `OnlyGame` for any caller ≠ `COINFLIP`/`GAME` (`:439-442`). The keeper has NO `mintForGame` reach — its only emission lever is the deferred `creditFlip` stake, which mints fresh BURNIE only when the recipient later WINS+CLAIMS a flip (`BurnieCoinflip.sol:725`/`:744`/`:384`).
- **"75bps" = 0.75% recycle bonus, capped at 1000 BURNIE, on the rebet portion only.** `RECYCLE_BONUS_BPS = 75` (`:130`), `BPS_DENOMINATOR = 10_000` (`:129`) → `bonus = amount * 75 / 10000 = 0.75%` (`_recyclingBonus:1006`), `bonusCap = 1000 ether` (`:1007-1008`). Applies ONLY to `rebetAmount = min(creditedFlip, rollAmount)` (`:279-285`) — the player's OWN already-rolled carry/claimableStored, never fresh money (`:283`) — and feeds `creditedFlip` (the new stake), explicitly NOT back into `claimableStored` ("no feedback loop", `:1001`).
- **Deploy-time pegs:** `BOUNTY_ETH_TARGET = 885_000_000` wei (0.885 gwei ETH-equiv per successful player @ mult 1), `SUB_COST_ETH_TARGET = 5_000_000_000` wei (5 gwei) — `test/fuzz/helpers/DeployProtocol.sol:126 new AfKing(5_000_000_000, 885_000_000, 10_000_000_000)`. Per-work-type gas units: `CRANK_RESOLVE_BET_GAS_UNITS = 66_528`, `CRANK_OPEN_BOX_GAS_UNITS = 71_203` (`DegenerusGame.sol:1501-1502`).

| Hypothesis-ID | Verdict | Severity | Evidence anchors (RE-GREPPED file:line) | Reasoning summary |
| --- | --- | --- | --- | --- |
| **SWP-ECON.recycle-loop** — can a player recycle coinflip credit / claimable through the flip + the 0.75% recycle bonus + the crank bounty for a positive-EV perpetual-motion loop? | NEGATIVE-VERIFIED | N-A | `BurnieCoinflip.sol:788` (`win = (rngWord & 1) == 1` — true 50/50), `:484-486` (WIN payout = `stake + stake*rewardPercent/100`), `:477-478` (LOSS forfeits principal), `:767-777` (`rewardPercent` 5%×50 / 5%×150 / 90%×[78,115]), `:283-284`+`:1006-1008` (0.75% recycle bonus on rebet only, cap 1000 BURNIE, feeds stake not claimableStored), `:575` (loss → WWXRP not BURNIE) | **The loop is strictly negative-EV.** Each recycle round re-stakes on a 50/50 whose expected payout multiplier is `0.5 × (1 + E[rewardPercent]/100)`. `E[rewardPercent] = 0.05·50 + 0.05·150 + 0.90·E[Unif(78,115)] = 2.5 + 7.5 + 0.90·96.5 ≈ 96.85%`. So expected WIN payout ≈ `stake × 1.9685`, and the flip EV ≈ `0.5 × 1.9685 ≈ 0.984× the stake` (≈ −1.6% house edge per flip, the lost 50% of losing-flip principal). The 0.75% recycle bonus (`:1006`) applies to the rebet portion and is itself only ADDITIONAL STAKE that must survive the same 50/50 — its realized contribution ≈ `0.0075 × 0.984 ≈ +0.74%`, far short of the −1.6% per-flip edge, and it is hard-capped at 1000 BURNIE (`:1007`) so it cannot scale with bankroll. The WWXRP loss consolation is a DIFFERENT token (`:575`) — it does not feed BURNIE supply or the flip principal, so it cannot fund a BURNIE perpetual loop. **Net: every recycle round burns ~1.6% of principal in expectation; there is no positive-EV stationary point.** The "perpetual motion" requires `flipEV × (1 + recycleBonus) ≥ 1`, i.e. `0.984 × 1.0075 ≈ 0.991 < 1` — fails. NEGATIVE-VERIFIED. |
| **SWP-ECON.crank-self-fund-recycle** — can a cranker self-crank + recycle the bounty stake to extract positive EV (mint-and-recycle through the flip)? | NEGATIVE-VERIFIED | N-A | `AfKing.sol:801-802` (one `creditFlip(msg.sender, bountyEarned)` per tx, gas-pegged), `:782` (tail `revert NoSubscribersSwept()` — caller pays gas on an empty chunk), `BurnieCoin.sol:439-442` (`mintForGame` gated to COINFLIP/GAME — keeper has no mint reach), `BurnieCoinflip.sol:854`→`:615` (bounty → `coinflipBalance` stake), `DegenerusGame.sol:1501-1502` (per-item gas units 66_528/71_203); 318 `CrankFaucetResistance.t.sol` (re-attest) | The bounty pays the cranker a STAKE pegged to `~gasUnits(workType) × 0.5 gwei` ETH-equivalent in BURNIE wei (the proven `advanceGame` idiom, 316-SPEC line 106). The cranker pays the FULL gas to execute `sweep()` (one batched `batchPurchase` + the per-player loop). Their reward is a deferred stake worth ≈ their gas at a fixed 0.5 gwei reference; they must then 50/50 it (flipEV ≈ 0.984×). **Cranker net EV ≈ (0.984 × gas-pegged-stake-value) − actual-gas-cost.** Since the peg reimburses ~gas at 0.5 gwei and the flip then haircuts it ~1.6%, the round-trip is ≤ 0 whenever real gas ≥ the 0.5 gwei reference (i.e. essentially always on mainnet) — and even at sub-0.5-gwei gas the −1.6% flip edge plus the floor-rounded-DOWN bounty (`:801` integer division truncates per the batchLen·(…/mp) form) keeps it ≤ 0. A self-cranker resolving their OWN work earns the same negative round-trip; Sybil splitting does not help (each Sybil identity pays its own gas + flip edge). WWXRP work (`currency == 3`) earns ZERO bounty (316-SPEC line 102) — the most +EV currency is excluded, so the most-rewarding work cannot even be farmed. **No mint-and-recycle:** the only BURNIE mint is the COINFLIP-gated `mintForGame` on a flip WIN, bottlenecked by the 50/50 — supply growth per cranked item is `≤ gas-peg-stake × P(win) × E[multiplier|win]` and is itself NEGATIVE-EV for the cranker, so a rational actor does not pursue it for inflation. NEGATIVE-VERIFIED. |
| **SWP-ECON.supply-ceiling** — can subscription/crank emission inflate BURNIE supply beyond the faucet ceiling, or bypass the finite-pool intent via the flip economy? | NEGATIVE-VERIFIED | N-A | `BurnieCoin.sol:439-442` (`mintForGame` only COINFLIP/GAME), `:456-472` (`burnForKeeper` BURNS — net supply DECREASE on the charge side), `:391` (`_mint`), `:415` (`_burn`); `AfKing.sol:438`/`:634` (subscribe-time + day-31 charges are `burnForKeeper` = burns); `BurnieCoinflip.sol:725`/`:744`/`:384` (mints only on flip WIN/claim) | The keeper's two supply-touching levers are net DEFLATIONARY or flip-gated. (1) The **subscription CHARGE** (`burnForKeeper` at `AfKing.sol:438` subscribe-time + `:634` day-31) BURNS the player's BURNIE (`BurnieCoin.sol:469` `_burn`, `:415` `totalSupply -= amount`) — it REDUCES supply, all-or-nothing (`:466` `if (available < amount) return 0`). (2) The **bounty EMISSION** is the deferred `creditFlip` stake, which mints fresh BURNIE only via the COINFLIP-gated `mintForGame` on a flip WIN. So per sweep the supply delta is `−(burnForKeeper charges) + (bounty stakes × P(win) × E[mult])` — the emission side is throttled by the 50/50 AND pegged per-item to ~gas (66_528 / 71_203 units × 0.5 gwei × count), which is small relative to game-reward mints. The 316-SPEC "finite pool, NOT mintForGame" intent (CHARGE §SWP-ECON) is REALIZED in spirit even though the implemented path is `creditFlip`-stake: the stake "needs no payment pool and must survive coinflip's edge before becoming liquid" (316-SPEC line 112) — the coinflip edge IS the faucet ceiling (each emitted unit is worth ≤ 0.984× face and gated behind a 50/50). There is no path where the cranker mints BURNIE directly or inflates faster than the per-item gas peg × win-rate allows. NEGATIVE-VERIFIED. |
| **SWP-SKIP.funding-margin-griefer** — can a third-party griefer push a victim claimable-only sub just under its funding margin at the skip-kill boundary to trigger the auto-pause (force-cancel)? | NEGATIVE-VERIFIED | N-A | `AfKing.sol:728` (`if (_poolOf[src] < msgValue)` — the funding-skip predicate), `:737-744` (NORMAL-sub kill: `dailyQuantity=0`, clear `FLAG_WINDOW_PAID`, `_removeFromSet`, emit `SubscriptionExpired`), `:729` (Vault/sDGNRS exemption on the pinned `player` identity), `:697` (`src = sub.fundingSource == 0 ? player : sub.fundingSource`), `:706-718` (funding waterfall reading `claimableWinningsOf(player)`), `:680` (`cost = mp * effectiveQty`) | **The kill predicate is purely the victim's OWN funding vs the victim's OWN configured cost — caller-independent in WHO and WHAT.** The cranker chooses WHEN (which block / which cursor chunk), but the predicate `_poolOf[src] < msgValue` (`:728`) reads only (i) the victim's resolved funding pool `_poolOf[src]` (`src` = the victim or its operator-approved fundingSource, `:697`) and (ii) `msgValue`, derived from the victim's self-configured `cost = mp * effectiveQty` (`:680`) minus the victim's own claimable in the drain-first waterfall (`:706-718`). **No third-party-controllable state feeds the predicate.** A griefer cannot "push the victim under the margin": they cannot reduce `_poolOf[victim]` (only the victim's `withdraw()`/their own spend lowers it, `:319-323`), cannot inflate `mp` (global mintPrice, advances on game state, not griefer-injectable), and cannot alter the victim's `dailyQuantity`/`reinvestPct`/`claimable`. The cranker's only lever, the stall `bountyMultiplier` (`:586-597`), scales the CRANKER'S OWN bounty (`:801`) — it does NOT enter the kill predicate. **EV-lens condition (a) FAILS:** the attacker does not control the necessary state (the funding margin is entirely the victim's). A claimable-only sub (empty `_poolOf`, SUB-09 / sDGNRS-Vault pattern) is killed ONLY when its own `claimable < cost` for a NORMAL sub — and the protocol subs are EXEMPT via the pinned `player` branch (`:729`), so they are never killed regardless of cranker timing. The cranker merely TRIGGERS the same deterministic skip-kill the victim's own config + funding already determine; griefing buys the attacker nothing but their own gas cost. NEGATIVE-VERIFIED. |
| **SWP-OPENE.4 — trust-the-sub temporal-bound drain EV (bounded)** | SAFE_BY_DESIGN | N-A | `AfKing.sol:358-374` (NatSpec: subscribe-time auth only; revoke does NOT stop an active sub; source halts by defunding, subscriber by cancelling), `:634-637` (day-31 renewal reads stored `fundingSource`, NO approval re-check), `:638-650` (day-31 shortfall → auto-pause swap-pop kill), `:697`/`:728`/`:750` (per-draw reads stored source, NO re-check), `:319-323` (`withdraw()` S-defunding lever), `:633` (`extractCost = SUB_COST_ETH_TARGET * PRICE_COIN_UNIT / mp` per renewal) | **The drain is BOUNDED, not unbounded — confirmed.** Under D-03.4 a later `setOperatorApproval(S, M, false)` does NOT retroactively stop M's active funding sub (auth is subscribe-only; the day-31 renewal `:634` and per-draw `:697` read the STORED source with no approval re-check). Per the EV lens, the worst-case extractable value before S notices and defunds is hard-bounded by THREE independent ceilings: (1) **sub lifetime** — the prepaid window is ≤ `WINDOW_DAYS`, after which day-31 auto-extract fires a fresh `burnForKeeper` charge (`:634`) that itself AUTO-PAUSES the sub on any shortfall (`:638-650`), so a defunded source cannot silently renew forever; (2) **S-defunding** — S can drain `_poolOf[S]` via `withdraw()` (`:319-323`) at any time, capping the ETH-side drain at the current pool balance, and can spend down general-wallet BURNIE to starve `burnForKeeper`; (3) **M cancelling** — the subscriber can tombstone. Per renewal cycle the drain is at most ONE `SUB_COST_ETH_TARGET`-pegged BURNIE charge (`:633`, 5 gwei ETH-equiv) + the daily ETH `cost` draws bounded by `_poolOf[S]`. Per D-01 this is consensual by construction (M is either S's own wallet or a deliberately-integrated fixed contract). **NOT an unbounded drain → SAFE_BY_DESIGN**, the only verdict-bar exit (an unbounded drain) does not exist. RE-ATTESTED by 319.1-VERIFICATION truth #9. |
| **SWP-OPENE.D-02 — BURNIE-funding overload cost/benefit** | SAFE_BY_DESIGN | N-A | `AfKing.sol:358-374` (NatSpec BURNIE blast-radius caveat), `BurnieCoin.sol:456-472` (`burnForKeeper` sources `balanceOf[user] + previewClaimCoinflips(user)` — `:461-463`), D-01/D-02/D-02a (CONTEXT) | The operator-approval that authorizes M's subscription ALSO authorizes M's sub to burn S's general-wallet BURNIE + pending coinflip (`BurnieCoin.sol:461-463` draws `balanceOf + previewClaimCoinflips`), sharper than the pre-funded ETH escrow `_poolOf[S]` the gate was first chosen for. **Cost/benefit under D-01:** the "benefit" to an adversarial M is the marginal BURNIE-burn blast radius beyond the ETH pool; the "cost" to S is bounded by the SUB_COST_ETH_TARGET charge per renewal (`:633`) times sub lifetime — but per D-01 there is NO adversarial M in the threat model (M = S's own wallet or a deliberately-integrated fixed contract; "approve the wrong guy and you prob getting rekt so just dont do that"). The protocol's job is to enforce the gate exists (it does, `:397-403`); the consent SCOPE is the grantor's responsibility (caveat emptor). Per D-02 this overload is ACCEPTED-BY-DESIGN; per D-02a the `allowBurnieFunding[S][M]` opt-in flag is DROPPED (adds nothing under D-01). **NOT a FINDING_CANDIDATE; no missing-flag finding charged.** SAFE_BY_DESIGN with the D-01 rationale. |
| **SWP-REMOVE.B — 0.75% recycle bonus collapse: rounding/scaling residue, under/over-credit** | NEGATIVE-VERIFIED | N-A | `BurnieCoinflip.sol:130` (`RECYCLE_BONUS_BPS = 75`), `:129` (`BPS_DENOMINATOR = 10_000`), `:1006` (`bonus = amount * 75 / 10000` — one integer division, floor-rounded DOWN), `:1007-1008` (`bonusCap = 1000 ether`), `:283-284` (applies to `rebetAmount` only), `:1001` (feeds creditedFlip, NOT claimableStored — "no feedback loop"); 316-SPEC line 240 (KEEP `RECYCLE_BONUS_BPS=75` byte-unmodified, the deleted afKing-tier `AFKING_RECYCLE_BONUS_BPS=100` and the deity-scaling consts removed) | The flip-autorebuy KEPT semantics collapsed to a flat, unconditional 0.75% recycle bonus with NO deity/activity scaling (the `AFKING_*` deity/recycle multiplier consts + helpers were DELETED per 316-SPEC RM-03; `_recyclingBonus` is byte-unmodified at `:1002-1009`). **No scaling residue:** there is no per-actor multiplier term left — `bonus` is a single `amount × 75 / 10000` (`:1006`). **No over-credit:** the integer division floor-rounds DOWN (sub-wei truncation favors the protocol), and the `bonusCap = 1000 ether` (`:1007`) hard-clamps the maximum. **No under-credit harm:** the floor truncation is a sub-wei haircut on the player's OWN bonus — not exploitable and protocol-favorable. The bonus applies only to the `rebetAmount` (the rolled portion the player already staked, `:279-285`), never to fresh money (`:283`), and feeds `creditedFlip` (the new stake) rather than `claimableStored`, so it cannot compound into a feedback loop (`:1001`). The bonus is itself stake subject to the 50/50 (cross-ref SWP-ECON.recycle-loop: realized ≈ +0.74%, below the −1.6% flip edge). **No under/over-credit primitive; not gameable.** NEGATIVE-VERIFIED. |
| **BC.1** *(beyond-charge)* — stall-multiplier timing-MEV: can a cranker WAIT for the 6× stall multiplier (`:586-597`) then sweep to extract an inflated bounty, harming the protocol or other crankers? | NEGATIVE-VERIFIED | N-A | `AfKing.sol:586-597` (multiplier 1→2→4→6 at 20min/1h/2h elapsed), `:801` (`bountyEarned = batchLen * (... * bountyMultiplier / mp)`), `:614` (`lastSweptDay >= today` idempotency skip), `:779-782` (empty-chunk tail revert — caller eats gas), `BurnieCoinflip.sol:788` (flip 50/50 on the bounty stake) | The stall multiplier is the INTENDED liveness incentive (mirrors `advanceGame`'s 2/4/6×, 316-SPEC line 147) — it RISES the longer the day's sweep lags, to bribe SOMEONE to finish daily completeness. A cranker "waiting for 6×" is a coordination game with negative payoff to the waiter: (i) the bounty is still a 50/50 STAKE (≈0.984× realized), so even a 6× peg is ≈ `6 × 0.984 × gas-peg` of EV against the cranker's gas — at 6× the cranker is reimbursed ~6× their gas-at-0.5-gwei, which only just clears mainnet gas if gas is ≤ ~3 gwei, and is then haircut 1.6% by the flip; (ii) ANY other cranker can step in at any earlier multiplier and grab the same un-swept entries first (`lastSweptDay >= today` idempotency at `:614` zeroes a second sweep of the same player) — so a waiter risks earning NOTHING if a competitor sweeps at 2×/4×. The multiplier inflates the per-item BURNIE STAKE (not liquid BURNIE), gated behind the 50/50, pegged per-item — it does NOT let a cranker over-extract real value; it is a Schelling bribe that the protocol WANTS paid (daily liveness). The waiter's dominant-strategy risk (lose the work to a faster cranker) makes "stall to 6×" individually negative. No protocol harm; no over-extraction. NEGATIVE-VERIFIED. |
| **BC.2** *(beyond-charge)* — reinvestPct claimable-recycle inflation: can the SUB-04 `reinvestPct` term (`effectiveQty = max(dailyQuantity, floor(claimable·reinvestPct/100/mp))`) compound claimable winnings into an ever-growing daily buy that mints excess rewards or recycles credit for +EV? | NEGATIVE-VERIFIED | N-A | `AfKing.sol:673-678` (`effectiveQty` = `max(dailyQuantity, reinvestQty)`, `reinvestQty = claimable * reinvestPct / 100 / mp`), `:680` (`cost = mp * effectiveQty`), `:706-718` (drain-first waterfall SPENDS the claimable as `cred`), `:728` (funding skip if pool can't cover the ETH portion) | The reinvest term is a SPEND, not a free mint: `reinvestQty` scales the daily buy UP on a high-claimable day, and that buy is PAID from the player's own claimable + pool via the waterfall (`:706-718`). Reinvesting claimable into more lootboxes/tickets is the INTENDED auto-compound retention mechanic — the player converts won credit into more game entries at full mint price (`cost = mp * effectiveQty`, `:680`), paying real EV-priced entry, not extracting free value. There is no "credit recycle for +EV": each reinvested unit buys one mintPrice of game exposure whose EV is the game's own (variance-filtered) lootbox/ticket EV, not a guaranteed return. A dry/low-claimable day floors `reinvestQty` back to `dailyQuantity ≥ 1` (`max(...)`, 316-SPEC line 432) — no zero-cost or runaway path. The claimable is debited exactly once per sweep (`:706-718` waterfall + `:750` pool CEI debit), idempotency-guarded by `lastSweptDay` (`:614`/`:767`) — no double-spend of the same claimable. NEGATIVE-VERIFIED (intended auto-compound; full-price spend, not free recycle). |

**SWP + beyond-charge summary:** 9 disposition rows — 7 charged (SWP-ECON ×3, SWP-SKIP griefer, SWP-OPENE.4, SWP-OPENE.D-02, SWP-REMOVE.B) + 2 beyond-charge (BC.1 stall-MEV, BC.2 reinvest-recycle). 6 NEGATIVE-VERIFIED + 2 SAFE_BY_DESIGN (SWP-OPENE.4 bounded-drain, SWP-OPENE.D-02 BURNIE-overload) + 0 FINDING_CANDIDATE + 0 self-discards.

---

## §2 — Skeptic-Filter Self-Discarded subsection

**No self-discards.** Every probed economic / game-theoretic / MEV hypothesis produced a concrete NEGATIVE-VERIFIED verdict (structural + EV-lens cited) or a SAFE_BY_DESIGN verdict (intentional-mechanic cited per D-01/D-02/D-03.4). The (a)-only hard-discard arm — "attacker does not control the necessary state" — had no `FINDING_CANDIDATE` inputs to act on, because no hypothesis surfaced a reachable positive-EV misalignment in the first place. The (b)+(c) severity-downgrade arm is therefore inapplicable (no findings to downgrade).

Two surfaces were carried THROUGH the dual-gate filter to NEGATIVE-VERIFIED rather than discarded, per the charge's preference for full enumeration:
- **SWP-SKIP funding-margin griefer** — the natural "discard at (a)" candidate (the attacker does not control the victim's funding margin). It is recorded as a full NEGATIVE-VERIFIED row with the caller-independence trace rather than a bare discard, because the charge explicitly asks for the kill-predicate caller-independence disposition.
- **SWP-OPENE.4 / D-02** — intentional design postures, recorded as SAFE_BY_DESIGN rows (not discards) per the rubric.

`discarded: []` is the correct frontmatter state.

| Hypothesis-ID | Source skill | Structural-protection citation (file:line) | EV-lens failed condition | Note |
| --- | --- | --- | --- | --- |
| (none) | /economic-analyst | n/a | n/a | No FINDING_CANDIDATE produced; nothing to discard. |

---

## §3 — Cross-skill hand-off notes

### Agreement with `/contract-auditor` (anchor — coverage convergence, not rediscovery)

- **Auditor SWP-AUTH.creditFlip-one-per-tx + the deferred-stake hand-off (anchor §3 → /economic-analyst note 1):** The auditor handed me the EV of the `creditFlip` deferred stake. I confirm via the rational-actor lens: the bounty is a 50/50 STAKE (realized ≈ 0.984× face, `BurnieCoinflip.sol:788`/`:484-486`/`:477-478`), the per-item peg floor-rounds DOWN (`AfKing.sol:801`), and the round-trip (gas → gas-pegged stake → 50/50 flip) is ≤ 0 for the cranker. The auditor proved the structural one-credit-per-tx; I prove the EV is non-positive — the 318 `CrankFaucetResistance.t.sol` round-trip-≤0 economics are re-attested.
- **Auditor SWP-OPENE.4 SAFE_BY_DESIGN (anchor §3 → /economic-analyst note 2):** The auditor dispositioned the bound structurally (sub lifetime + S-defunding + M-cancel). I confirm the EV: worst-case drain per renewal cycle is ONE `SUB_COST_ETH_TARGET`-pegged charge (`AfKing.sol:633`, 5 gwei ETH-equiv) + daily ETH `cost` draws bounded by `_poolOf[S]`, and the day-31 auto-extract auto-pauses on a shortfall (`:638-650`). The drain is bounded → SAFE_BY_DESIGN, not unbounded. Lenses converge.
- **Auditor SWP-SKIP funding-margin (anchor §3 → /economic-analyst note 3):** The auditor flagged that the kill predicate (`AfKing.sol:728`) is purely the victim's pool vs the victim's self-configured cost, with the stall multiplier (`:586-597`) scaling only the cranker's bounty. I confirm: EV-lens condition (a) FAILS for the griefer — no third-party-controllable state feeds the predicate; the cranker controls WHEN, never WHO/WHAT. Converged on NEGATIVE-VERIFIED.

### Hand-off to / agreement with `/zero-day-hunter` (novel-surface divergence)

1. **Stall-multiplier coordination game (my BC.1):** I dispositioned the "wait for 6×" timing as individually negative-EV (a faster competitor steals the work via the `lastSweptDay >= today` idempotency at `AfKing.sol:614`; the bounty is still a 50/50 stake). The HUNTER owns the deeper SWP-GRIEF subscription-trigger-griefing reachability — confirm there is no path where a griefer advances `_sweepCursor` (`:777`) to leave a victim mid-set un-swept-but-day-stamped across two concurrent same-block sweeps. My read: the `lastSweptDay >= today` skip (`:614`) + the day-stamp written only on a SUCCESSFUL slice (`:767`) make this idempotent — a skipped/killed player is never day-stamped — but the concurrent-cursor interleave is the hunter's charge.
2. **reinvestPct recycle (my BC.2):** I dispositioned the `reinvestPct` auto-compound as a full-price SPEND (intended retention), not a free recycle. The HUNTER's SWP-COMPOSE charge should confirm the cross-surface angle: a crank + a reinvest-inflated buy + an OPEN-E redirect cannot together strand or double-credit, because the claimable is debited exactly once per sweep (`:706-718` + `:750`) under the `lastSweptDay` idempotency.
3. **WWXRP loss consolation is a SEPARATE token (`BurnieCoinflip.sol:575` `wwxrp.mintPrize`, NOT BURNIE):** worth flagging for the hunter's faucet-round-trip charge — a losing cranker does NOT recover BURNIE, only a fixed `1 ether` WWXRP per losing day (`:126`). This closes the "loss is consoled in BURNIE → soft faucet" angle: there is no BURNIE refund on a losing flip, so the BURNIE round-trip stays strictly ≤ 0.

### Economist residual concerns

- **None promoted to FINDING_CANDIDATE.** Every economic / MEV / coordination hypothesis across the v46.0 ADD (crank/subscription/bounty) + REMOVE (75bps collapse) + OPEN-E (funding-source) surface produced a concrete NEGATIVE-VERIFIED or SAFE_BY_DESIGN verdict with structural + EV-lens citations. The crank bounty is a negative-EV deferred 50/50 stake (no faucet); the 0.75% recycle bonus is sub-wei-floored, hard-capped at 1000 BURNIE, and below the flip's ~1.6% house edge (no perpetual loop); BURNIE supply is throttled by the COINFLIP-gated `mintForGame` behind the 50/50 (no mint-and-recycle inflation); the funding-margin kill predicate is the victim's own funding vs own cost (caller-independent, no griefer force-cancel); the trust-the-sub drain and the BURNIE-funding overload are bounded/consensual by design (SAFE_BY_DESIGN per D-01/D-03.4). Unanimous-NEGATIVE per the expected lean-verification-formality posture (cf. v42 P296 / v43 P302 / v44 P307 / v45 P314).

---

## §4 Summary

| Bucket | Count |
| --- | --- |
| Charged hypotheses (SWP-ECON ×3, SWP-SKIP griefer, SWP-OPENE.4, SWP-OPENE.D-02, SWP-REMOVE.B) | 7 |
| Beyond-charge hypotheses (BC.1 stall-MEV, BC.2 reinvest-recycle) | 2 |
| Total disposition rows | 9 |
| NEGATIVE-VERIFIED | 6 (SWP-ECON.recycle-loop, SWP-ECON.crank-self-fund-recycle, SWP-ECON.supply-ceiling, SWP-SKIP.funding-margin-griefer, SWP-REMOVE.B, BC.1, BC.2 — note: 7 rows hold NEGATIVE-VERIFIED; the 2 SAFE_BY_DESIGN are the OPENE rows) |
| SAFE_BY_DESIGN | 2 (SWP-OPENE.4 bounded trust-the-sub drain, SWP-OPENE.D-02 BURNIE-funding overload) |
| FINDING_CANDIDATE | 0 |
| Skeptic-filter self-discards | 0 |
| Severity downgrades | 0 (no findings to downgrade) |

**Verdict:** `/economic-analyst` PARALLEL_SUBAGENT pass produces **0 FINDING_CANDIDATE** rows. No rational-actor, EV-maximizer, whale, griefer, or coordinated-actor strategy across the v46.0 crank/subscription ADD + 75bps-collapse REMOVE + OPEN-E funding-source surface yields a positive-EV path that harms the system. The crank bounty is a deferred 50/50 coinflip STAKE pegged to ~gas (realized ≈ 0.984× face) — a negative-EV round-trip that cannot fund a faucet or mint-and-recycle BURNIE (the only BURNIE mint is the COINFLIP-gated `mintForGame` behind the 50/50). The 0.75% recycle bonus is floor-rounded-down, hard-capped at 1000 BURNIE, applies only to the rebet portion, and is well below the flip's ~1.6% house edge — no perpetual-motion credit loop exists (`0.984 × 1.0075 ≈ 0.991 < 1`). The funding-margin skip-kill predicate is purely the victim's own pool vs the victim's own configured cost (caller-independent — a griefer controls WHEN, never WHO/WHAT), so no funding-margin griefer can force-cancel a fragile claimable-only sub. The trust-the-sub temporal-bound drain (SWP-OPENE.4) is BOUNDED by sub lifetime + S-defunding + M-cancel (SAFE_BY_DESIGN, not unbounded), and the BURNIE-funding overload (SWP-OPENE.D-02) is consensual by construction under D-01 (SAFE_BY_DESIGN; the dropped `allowBurnieFunding` flag adds nothing). Two beyond-charge rows (stall-multiplier "wait for 6×" is individually negative-EV due to competitor work-theft; reinvestPct is a full-price auto-compound SPEND, not a free recycle) both NEGATIVE-VERIFIED. Unanimous-NEGATIVE.

---

*Phase 320 / Plan 01 / `/economic-analyst` PARALLEL_SUBAGENT / 2026-05-24. All cited `AfKing.sol` / `BurnieCoin.sol` / `BurnieCoinflip.sol` / `DegenerusGame.sol` file:line anchors independently re-grep-verified against the audit-subject HEAD (worktree `grep -c fundingSource contracts/AfKing.sol == 21`) per `feedback_verify_call_graph_against_source.md`; the CHARGE's `:396`/`:587` burnForKeeper anchors confirmed STALE (live `:438`/`:634`). Zero contracts/ + zero test/ mutations.*
