---
phase: 320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-03-REGRESSION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "RNG-freeze is intact under permissionless resolve (resolution stays post-unlock behind the RngNotReady guard, the placement guard is untouched) AND the freeze obligations are RETIRED by the ETH-auto-rebuy removal (one fewer VRF consumer + three fewer player-mutable in-window inputs)"
    - "The faucet is bounded (self-crank/Sybil round-trip ≤ 0, WWXRP earns zero) — re-attested from the 318 SAFE-01 proof"
    - "v44.0 + v45.0 closure surfaces are NON-WIDENING — every contracts/+test/ diff vs the v45 baseline 62fb514b… is attributable to a known v46-scope commit, and the v44 audit-subject surfaces not in v46 scope are byte-unchanged"
    - "KNOWN-ISSUES.md and the BURNIE win/loss RNG path (processCoinflipPayouts, rngWord & 1) are byte-UNMODIFIED vs the v45 baseline"
    - "The full forge test suite shows 559 pass / 44 fail = EXACT v45 baseline — any NEW failure is a regression signal"
    - "SOURCE-TREE FROZEN is attestable: git diff against the phase-start HEAD (30b5c89c) returns no contracts/ or test/ change absent a RE-PASS"
  artifacts:
    - path: ".planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-03-REGRESSION.md"
      provides: "REG-01 NON-WIDENING attestation + RNG-freeze-intact + freeze-obligation-retirement + faucet-bounded re-attestation + KNOWN-ISSUES/RNG-path byte-unmodified + the 559/44 suite baseline check + the SOURCE-TREE FROZEN attestation"
  key_links:
    - from: "320-03-REGRESSION.md NON-WIDENING attestation"
      to: "the v45 baseline 62fb514b…"
      via: "git diff 62fb514b..HEAD -- contracts/ test/ every hunk attributable to a v46-scope commit"
      pattern: "62fb514b|NON-WIDENING|non-widening"
    - from: "320-03-REGRESSION.md suite baseline"
      to: "the 559/44 v45 baseline"
      via: "forge test count comparison"
      pattern: "559|44"
    - from: "320-03-REGRESSION.md"
      to: "audit/FINDINGS-v46.0.md §5 (Plan 04)"
      via: "forward-cite — the REG-01 attestation populates the FINDINGS §5 LEAN regression appendix"
      pattern: "FINDINGS-v46.0"
---

<objective>
Author the regression + SOURCE-TREE FROZEN attestation (workstream 3): prove RNG-freeze is intact under permissionless resolve AND the freeze obligations are retired by the ETH-auto-rebuy removal, the faucet is bounded, the v44.0+v45.0 closure surfaces are NON-WIDENING, KNOWN-ISSUES.md + the BURNIE win/loss RNG path are byte-unmodified, the full suite holds the 559/44 v45 baseline, and the source tree is frozen (no in-phase contracts/+test/ mutation absent a RE-PASS).

Purpose: This is the substantive proof behind the closure verdict's `RNG_FREEZE_INTACT (+ obligations RETIRED by removal)`, `FAUCET_BOUNDED`, `WWXRP_ZERO_REWARD`, and `KNOWN_ISSUES_UNMODIFIED` clauses, plus the ROADMAP success-criterion-5 SOURCE-TREE FROZEN attestation. Baseline: v45 closure HEAD 62fb514bfcc8ad042a45cef960e5ff0ff6fbb801. Phase-start HEAD: 30b5c89c.

Output: 320-03-REGRESSION.md — an AGENT-COMMITTED .planning/ artifact; zero contracts/ + zero test/ mutation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-CONTEXT.md
@.planning/REQUIREMENTS.md

<!-- The LEAN regression appendix template (§5) + KI walkthrough (§6) to mirror -->
@audit/FINDINGS-v44.0.md

<!-- The RNG-freeze + faucet test-proven facts to re-attest (do not re-derive) -->
@.planning/phases/318-tst-subscription-crank-correctness-removal-proofs-tst/318-05-SUMMARY.md
@.planning/phases/318-tst-subscription-crank-correctness-removal-proofs-tst/318-06-SUMMARY.md

<!-- The byte-unmodified target -->
@KNOWN-ISSUES.md
</context>

<interfaces>
<!-- Regression baselines verified at planning time. -->

v45 closure baseline HEAD: 62fb514bfcc8ad042a45cef960e5ff0ff6fbb801.
Phase-start HEAD (SOURCE-TREE FROZEN reference): 30b5c89c.
Test baseline (the EXACT v45 baseline): suite 559 pass / 44 fail (the 44 are unrelated pre-existing failures — zero v46 regression). Any NEW failure is a regression signal.
RNG-freeze facts (318-05 RngFreezeAndRemovalProofs.t.sol 13/13): crank bet/box resolution stays post-unlock behind RngNotReady; placement guard at DegenerusGame.sol:452 untouched; ETH winnings always land in claimable; flat 75bps unconditional; processCoinflipPayouts + `rngWord & 1` byte-present; KNOWN-ISSUES sha256-anchored byte-unmodified.
Faucet facts (318-02 SAFE-01 CrankFaucetResistance.t.sol): self-crank/Sybil round-trip ≤ 0; WWXRP currency==3 zero reward; one-reward-per-item; no pre-RNG-word resolution.
Freeze-obligation retirement (SAFE-04 / RM-02): ETH auto-rebuy was a VRF consumer + 3 player-mutable in-window inputs; its removal RETIRES those obligations (does not weaken any).
KNOWN-ISSUES.md is at the REPO ROOT (./KNOWN-ISSUES.md), not under .planning/.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: REG-01 NON-WIDENING + RNG-freeze-intact + freeze-obligation-retirement + faucet-bounded</name>
  <files>.planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-03-REGRESSION.md</files>
  <read_first>
    - audit/FINDINGS-v44.0.md §5 (the LEAN regression appendix format — REG-01 closure-signal non-widening + regression distribution summary) + §3.B (the per-exempt-entry attestation style)
    - .planning/phases/318-tst-subscription-crank-correctness-removal-proofs-tst/318-05-SUMMARY.md (RngFreezeAndRemovalProofs.t.sol 13/13 — the RNG-freeze + REMOVE proofs to re-attest)
    - .planning/phases/318-tst-subscription-crank-correctness-removal-proofs-tst/318-02-SUMMARY.md (SAFE-01 faucet resistance — locate it; if the exact file differs, read the 318 phase SUMMARY ledger)
    - .planning/REQUIREMENTS.md §SAFE (SAFE-01 faucet, SAFE-04 RNG-freeze + obligation retirement) + the v45 baseline note
    - contracts/DegenerusGame.sol (re-grep the placement guard at :452 + the RngNotReady guard + processCoinflipPayouts + `rngWord & 1` against HEAD)
  </read_first>
  <action>
    Author 320-03-REGRESSION.md opening with §1 Regression Frame (baseline 62fb514b… → subject HEAD 30b5c89c; the LEAN posture matching v44 §5). Then:

    §2 REG-01 NON-WIDENING: run `git diff 62fb514bfcc8ad042a45cef960e5ff0ff6fbb801..HEAD -- contracts/ test/ --stat` and attribute EVERY changed file/hunk to a known v46-scope commit (df4ef365 317 batch, 42140ceb OPEN-E, e1baa978 WR-01, e4014f91/795e679d GAS pegs, the 317-08 keeper/slot family, the 95e96532/4654a56e test sync). Attest that the v44.0 audit-subject surfaces NOT in v46 scope are byte-unchanged (spot-check the v44 sStonk per-day redemption surfaces + the v45 VRF-rotation fix surfaces are untouched by v46). Record any diff hunk NOT attributable to v46 scope as a WIDENING signal (expected: none).

    §3 RNG-freeze intact under permissionless resolve: re-attest the 318-05 facts — crank bet/box resolution stays post-unlock behind the RngNotReady guard (the do-work crank relaxes WHO resolves, not WHEN), the placement guard at DegenerusGame.sol:452 is untouched (re-grep the live line). §4 Freeze-obligation RETIREMENT: attest the RM-02 ETH-auto-rebuy removal retires one VRF consumer + three player-mutable in-window inputs (the obligations are removed, not weakened) — cite the RM-02 deletion footprint + the SAFE-04 deterministic-no-VRF-word-credit proof.

    §5 Faucet bounded: re-attest the SAFE-01 facts — self-crank/Sybil round-trip ≤ 0 (the three caller-independent locks: purchase-gate + gas-peg + coinflip-credit illiquidity), WWXRP currency==3 earns zero, one-reward-per-item, no pre-RNG-word resolution. Each section carries a re-grepped evidence anchor + a cross-ref to the originating 318 test. Apply grep-gate hygiene where header prose could self-invalidate a count.
  </action>
  <verify>
    <automated>test -f .planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-03-REGRESSION.md && grep -qiE "NON-WIDENING|non-widening" .planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-03-REGRESSION.md && grep -qiE "RngNotReady|placement guard|:452" .planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-03-REGRESSION.md && grep -qiE "faucet|WWXRP|round.?trip" .planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-03-REGRESSION.md && grep -q "62fb514b" .planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-03-REGRESSION.md</automated>
  </verify>
  <acceptance_criteria>
    - §2 REG-01 attributes every `git diff 62fb514b..HEAD -- contracts/ test/` hunk to a known v46-scope commit, with a NON-WIDENING verdict and zero unattributable hunks
    - §3 attests RNG-freeze intact (RngNotReady guard + the re-grepped placement guard at DegenerusGame.sol:452 untouched) re-attesting 318-05
    - §4 attests the freeze obligations are RETIRED by the RM-02 ETH-auto-rebuy removal (one fewer VRF consumer + three fewer player-mutable in-window inputs), not weakened
    - §5 attests the faucet is bounded (round-trip ≤ 0, WWXRP zero reward) re-attesting SAFE-01
    - Every evidence anchor is a HEAD-re-grepped file:line
  </acceptance_criteria>
  <done>320-03-REGRESSION.md opens with REG-01 NON-WIDENING + RNG-freeze-intact + freeze-obligation-retirement + faucet-bounded, each re-attested against the 318 proofs with HEAD-verified anchors.</done>
</task>

<task type="auto">
  <name>Task 2: Suite baseline check + KNOWN-ISSUES/RNG-path byte-unmodified + SOURCE-TREE FROZEN attestation</name>
  <files>.planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-03-REGRESSION.md</files>
  <read_first>
    - .planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-03-REGRESSION.md (the regression doc from Task 1 to extend)
    - audit/FINDINGS-v44.0.md §6 (the KI gating walk + KNOWN-ISSUES.md re-verification format)
    - KNOWN-ISSUES.md (the byte-unmodified target at repo root)
    - contracts/BurnieCoinflip.sol + contracts/DegenerusGame.sol (re-grep processCoinflipPayouts + `rngWord & 1` — the BURNIE win/loss RNG path that MUST be byte-unmodified)
  </read_first>
  <action>
    Extend 320-03-REGRESSION.md with:

    §6 Suite baseline: run the full forge test suite (the technical command is execution detail — use the project's standard forge invocation/profile) and record the pass/fail counts. Compare against the EXACT v45 baseline 559 pass / 44 fail: a match = NON-WIDENING confirmed; any NEW failure = a regression signal that MUST be surfaced (and would block the closure verdict). If the count differs, enumerate the delta failures and classify each as pre-existing-baseline vs v46-regression.

    §7 KNOWN-ISSUES.md byte-unmodified: compute `git diff 62fb514bfcc8ad042a45cef960e5ff0ff6fbb801..HEAD -- KNOWN-ISSUES.md` (expected: empty) AND a sha256 anchor of the current file; attest byte-identity vs the v45 baseline. §8 BURNIE win/loss RNG path byte-unmodified: re-grep processCoinflipPayouts + `rngWord & 1` and attest those hunks are byte-identical vs the v45 baseline via `git diff 62fb514b..HEAD` scoped to those lines (re-grep the live locations first).

    §9 SOURCE-TREE FROZEN attestation: run `git diff 30b5c89c -- contracts/ test/` and attest it returns NO output (zero in-phase contracts/+test/ mutation) — UNLESS a Tier-1/Tier-2 RE-PASS (Plan 01) was triggered + user-approved, in which case §9 documents the single SEPARATE USER-APPROVED commit + the otherwise-frozen attestation. Close with §10 a forward-cite placeholder for the FINDINGS-v46.0.md §5 LEAN regression appendix + §6 KI walkthrough (resolved by Plan 04).
  </action>
  <verify>
    <automated>grep -qiE "559|44" .planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-03-REGRESSION.md && grep -qiE "KNOWN.?ISSUES|byte.?unmodified|byte.?identical" .planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-03-REGRESSION.md && grep -qiE "processCoinflipPayouts|rngWord" .planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-03-REGRESSION.md && grep -qiE "SOURCE-TREE FROZEN|30b5c89c" .planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-03-REGRESSION.md</automated>
  </verify>
  <acceptance_criteria>
    - §6 records the full forge suite pass/fail counts compared against the EXACT v45 baseline 559/44, with any NEW failure surfaced as a regression signal (expected: exact match)
    - §7 attests KNOWN-ISSUES.md byte-unmodified vs v45 (git diff 62fb514b..HEAD -- KNOWN-ISSUES.md empty + sha256 anchor)
    - §8 attests the BURNIE win/loss RNG path (processCoinflipPayouts, `rngWord & 1`) byte-unmodified vs v45 with re-grepped live locations
    - §9 attests SOURCE-TREE FROZEN via `git diff 30b5c89c -- contracts/ test/` returning empty (or documents the single user-approved RE-PASS commit if one fired)
    - §10 forward-cite placeholder for FINDINGS-v46.0.md §5 + §6 present
    - `git status --porcelain contracts/ test/` shows no mutation
  </acceptance_criteria>
  <done>320-03-REGRESSION.md extended with the 559/44 suite baseline check, the KNOWN-ISSUES + RNG-path byte-unmodified attestations, and the SOURCE-TREE FROZEN attestation.</done>
</task>

</tasks>

<verification>
- 320-03-REGRESSION.md exists with REG-01 NON-WIDENING + RNG-freeze + freeze-obligation-retirement + faucet + suite-baseline + KI/RNG byte-unmodified + SOURCE-TREE FROZEN.
- `git diff 30b5c89c -- contracts/ test/` returns empty (this is a read-only regression pass).
- The full suite holds 559/44 (any NEW failure surfaced as a regression signal).
- KNOWN-ISSUES.md + processCoinflipPayouts/`rngWord & 1` byte-unmodified vs the v45 baseline.
</verification>

<success_criteria>
RNG-freeze is intact under permissionless resolve and the freeze obligations are retired by the ETH-auto-rebuy removal; the faucet is bounded; the v44.0+v45.0 closure surfaces are NON-WIDENING; KNOWN-ISSUES + the BURNIE RNG path are byte-unmodified; the suite holds 559/44; SOURCE-TREE FROZEN is attestable. (ROADMAP success criteria 3 + 5 regression/frozen facets.)
</success_criteria>

<output>
Create `.planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-03-SUMMARY.md` when done, recording the NON-WIDENING verdict, the suite pass/fail counts vs 559/44, the byte-unmodified attestations, and the SOURCE-TREE FROZEN result.
</output>
