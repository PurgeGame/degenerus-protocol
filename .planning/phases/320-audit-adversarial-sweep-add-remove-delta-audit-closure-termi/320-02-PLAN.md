---
phase: 320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-02-DELTA-AUDIT.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every v45→v46 contracts/ change is enumerated as the delta-audit subject set: the 317 batched ADD+REMOVE diff (df4ef365) + the keeper reconciliation/slot gap-closure (317-08 family) + the 319.1 OPEN-E diff (42140ceb) + WR-01 event (e1baa978) + the 319 GAS pegs (e4014f91) + CR-01 fix (795e679d) + the JGAS jackpot-split removal across DegenerusGameJackpotModule + DegenerusGameAdvanceModule"
    - "The add surfaces (PROTO/CRANK/REW/SUB + OPEN-E funding-source routing) and the remove surfaces (RM + JGAS) compose cleanly — no orphaned winnings, no double-credit"
    - "The daily ETH jackpot pays all 305 winners in ONE call with nothing stranded by the dropped resumeEthPool carry (the JGAS split-removal delta composes cleanly with the RM-02 ETH-auto-rebuy removal)"
    - "The OPEN-E default-self path (fundingSource == 0) is behavior-identical to pre-OPEN-E; no cross-account spend is possible without isOperatorApproved consent at subscribe()"
    - "Every cited AfKing.sol/module file:line is re-grep-verified against HEAD before the delta-audit writes any claim"
  artifacts:
    - path: ".planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-02-DELTA-AUDIT.md"
      provides: "The §3.A-style delta-surface table + add/remove composition attestation + JGAS single-call composition proof + OPEN-E default-self equivalence attestation"
  key_links:
    - from: "320-02-DELTA-AUDIT.md delta-surface table"
      to: "the v46 subject commit set"
      via: "each surface row cites the v45→v46 commit SHA + the re-grepped contracts/ file:line"
      pattern: "df4ef365|42140ceb|e4014f91|795e679d|e1baa978"
    - from: "320-02-DELTA-AUDIT.md"
      to: "316-SPEC.md (the locked 'intended' design)"
      via: "each delta row confirms the diff matches the SPEC lock"
      pattern: "316-SPEC"
    - from: "320-02-DELTA-AUDIT.md"
      to: "audit/FINDINGS-v46.0.md §3 (Plan 04)"
      via: "forward-cite — the delta-surface table populates the FINDINGS §3 delta section"
      pattern: "FINDINGS-v46.0"
---

<objective>
Author the add/remove + OPEN-E + JGAS delta-audit (workstream 2): audit EVERY v45→v46 contracts/ change together against the locked 316-SPEC, proving the ADD surfaces and REMOVE surfaces compose cleanly with no orphaned/double-credited winnings, the daily ETH jackpot pays all 305 winners in one call with nothing stranded by the dropped resumeEthPool carry, and the OPEN-E default-self path stays behavior-identical to pre-OPEN-E.

Purpose: This is the substantive proof behind the closure verdict's `LEGACY_AFKING_MODE + FREE_ETH_AUTOREBUY REMOVED`, `JACKPOT_ETH_SPLIT REMOVED (single-call fits @305-ceiling)`, and the add/remove composition cleanliness. Baseline → subject: v45 closure HEAD MILESTONE_V45_AT_HEAD_62fb514bfcc8ad042a45cef960e5ff0ff6fbb801 → v46 closure HEAD (current HEAD 30b5c89c).

Output: 320-02-DELTA-AUDIT.md — an AGENT-COMMITTED .planning/ artifact; zero contracts/ + zero test/ mutation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-CONTEXT.md
@.planning/REQUIREMENTS.md

<!-- The locked "intended" design the delta-audit confirms the diff matches -->
@.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md

<!-- The delta-surface table template (§3.A) to mirror -->
@audit/FINDINGS-v44.0.md

<!-- The OPEN-E substrate -->
@.planning/phases/319.1-impl-open-e-shared-funding-source-burnie-and-eth-pool/319.1-RESEARCH.md
@.planning/phases/319.1-impl-open-e-shared-funding-source-burnie-and-eth-pool/319.1-VERIFICATION.md
</context>

<interfaces>
<!-- v46 delta subject commit set (verified to exist at planning time against HEAD 30b5c89c; the CONTEXT-cited keeper-remap 8e137e2 was folded into the 317-08 gap-closure family, NOT a standalone short SHA — resolve the actual keeper-reconciliation commits via git log). -->

VERIFIED-PRESENT subject SHAs: df4ef365 (317 batched ADD+REMOVE), 42140ceb (319.1 OPEN-E), e1baa978 (319.1 WR-01 indexed fundingSource event), e4014f91 (319 GAS OUTCOME-B calibration), 795e679d (319 CR-01 box-peg correction), 95e96532 + 4654a56e (319.1 test sync — test/, in the delta but not contract subject).
KEEPER RECONCILIATION / SLOT GAP-CLOSURE: the 317-08 family — c800520a (re-derive INLINE lootboxRng-family slot literals) + 6265d572 (phase-317 complete: gap-closure 317-08). Resolve the exact keeper-side reconciliation commit(s) via `git log --oneline 62fb514b..HEAD -- contracts/AfKing.sol`.
JGAS SPLIT REMOVAL: landed inside df4ef365 across contracts/modules/DegenerusGameJackpotModule.sol + contracts/modules/DegenerusGameAdvanceModule.sol (verify grep-clean: SPLIT_CALL1/SPLIT_CALL2/resumeEthPool/_resumeDailyEth/STAGE_JACKPOT_ETH_RESUME/call1Bucket).

Baseline: 62fb514bfcc8ad042a45cef960e5ff0ff6fbb801. Subject HEAD: 30b5c89c (current).

AfKing.sol HEAD-verified OPEN-E anchors (re-grep before citing — CONTEXT ~:396/~:587 are STALE): fundingSource resolve `s.fundingSource == address(0) ? subscriber : s.fundingSource` at :439; ETH `_poolOf[src]` draw + InsufficientPool skip at :728; burnForKeeper subscribe-time charge at :438; burnForKeeper day-31 auto-extract at :634; Vault SUB-09 self-subscribe at DegenerusVault.sol:474 (address(0)=self); sStonk at StakedDegenerusStonk.sol:380 (address(0)=self).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate the v45→v46 delta subject set + author the delta-surface table</name>
  <files>.planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-02-DELTA-AUDIT.md</files>
  <read_first>
    - audit/FINDINGS-v44.0.md §3.A (the delta-surface table format to mirror — one row per surface with the commit + the contracts/ file:line + the disposition)
    - .planning/REQUIREMENTS.md (the PROTO/CRANK/REW/SUB add reqs + the RM/JGAS/OPENE remove+funding reqs — the surface enumeration source)
    - .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md (the locked "intended" footprint each delta row confirms — the RM-01..06 deletion footprint, the JGAS-01/02 split-removal footprint across both modules, the compounded −2 slot-shift)
    - .planning/STATE.md §"Current Milestone Phases" (the per-phase commit ledger: df4ef365, 42140ceb, e1baa978, e4014f91, 795e679d, the 317-08 family)
  </read_first>
  <action>
    Run `git log --oneline 62fb514bfcc8ad042a45cef960e5ff0ff6fbb801..HEAD -- contracts/` to enumerate EVERY v45→v46 contracts/ commit; cross-check it against the STATE.md ledger so the subject set is complete (no surface silently omitted). Resolve the keeper-reconciliation commit(s) via `git log --oneline 62fb514b..HEAD -- contracts/AfKing.sol`. Author 320-02-DELTA-AUDIT.md opening with §1 Audit Subject + Baseline (baseline 62fb514b… → subject HEAD; the full subject commit set with each SHA + one-line scope) then a §3.A-style Delta-Surface Table: one row per audited surface (PROTO-01..05, CRANK-01..04, REW-01..04, SUB-01..09, RM-01..06, JGAS-01/02, OPENE-01..04), each row carrying the originating commit SHA, the re-grepped contracts/ file:line(s), and a NEGATIVE-VERIFIED / SAFE_BY_DESIGN disposition confirming the landed diff matches the 316-SPEC lock. Every file:line MUST be re-grepped against HEAD before the row cites it (per feedback_verify_call_graph_against_source.md).

    For the RM rows, record the grep-clean attestation: `grep -rnE "afKing|AFKING_|setAutoRebuy|autoRebuyState|AutoRebuyState|_processAutoRebuy|_calcAutoRebuy|settleFlipModeChange|_afKingRecyclingBonus|deactivateAfKingFromCoin|syncAfKingLazyPassFromCoin" contracts/ --include=*.sol` filtered to exclude `contracts/test`, `mocks`, and the KEPT canonical keeper `contracts/AfKing.sol` returns ZERO matches; note the lone surviving afKing-named symbol is the kept _hasAnyLazyPass/hasAnyLazyPass (PROTO-01/RM-04). For the JGAS rows, record `grep -rnE "SPLIT_CALL1|SPLIT_CALL2|resumeEthPool|_resumeDailyEth|STAGE_JACKPOT_ETH_RESUME|call1Bucket" contracts/ --include=*.sol` excluding `contracts/test`+`mocks` returns ZERO. Apply grep-gate hygiene (`grep -v '^#'`-style) where header prose could self-invalidate a count.
  </action>
  <verify>
    <automated>test -f .planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-02-DELTA-AUDIT.md && grep -q "df4ef365" .planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-02-DELTA-AUDIT.md && grep -q "42140ceb" .planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-02-DELTA-AUDIT.md && grep -q "62fb514b" .planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-02-DELTA-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - 320-02-DELTA-AUDIT.md exists with §1 Audit Subject + Baseline citing baseline 62fb514b… → subject HEAD and the full subject commit set
    - The delta-surface table has one row per audited surface across PROTO/CRANK/REW/SUB/RM/JGAS/OPENE, each with the originating commit SHA + re-grepped contracts/ file:line + a disposition confirming the diff matches the 316-SPEC lock
    - The RM grep-clean gate (the full kill-set regex, excluding contracts/test+mocks+the kept AfKing.sol) is recorded returning ZERO matches
    - The JGAS grep-clean gate (SPLIT_CALL1/2/resumeEthPool/_resumeDailyEth/STAGE_JACKPOT_ETH_RESUME/call1Bucket, excluding contracts/test+mocks) is recorded returning ZERO matches
    - `git log --oneline 62fb514b..HEAD -- contracts/` enumeration is cross-checked against the STATE.md ledger with no surface omitted
  </acceptance_criteria>
  <done>320-02-DELTA-AUDIT.md opens with the baseline+subject set and a complete delta-surface table; every file:line re-grep-verified; RM + JGAS kill sets grep-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Compose-cleanly proofs — add/remove + JGAS single-call + OPEN-E default-self</name>
  <files>.planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-02-DELTA-AUDIT.md</files>
  <read_first>
    - .planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-02-DELTA-AUDIT.md (the delta-surface table from Task 1 to extend)
    - contracts/modules/DegenerusGameJackpotModule.sol + contracts/modules/DegenerusGameAdvanceModule.sol (re-grep the daily-ETH single-call path + confirm no resume stage; the 305-winner buckets 159/95/50/1)
    - contracts/AfKing.sol (re-grep the OPEN-E default-self short-circuit at :439, the _poolOf[src] draw at :728, both burnForKeeper sites :438 + :634 — the LIVE lines, NOT the stale ~:396/~:587)
    - .planning/phases/318-tst-subscription-crank-correctness-removal-proofs-tst/318-06-SUMMARY.md + 318-05-SUMMARY.md (the 318-06 JackpotSingleCallCorrectness + 318-05 RngFreezeAndRemovalProofs proven assertions the delta-audit re-attests, does NOT re-derive — locate the exact 318 phase dir under .planning/phases/ first)
    - .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md §JGAS-01 Decision Gate (the intended single-call composition)
  </read_first>
  <action>
    Extend 320-02-DELTA-AUDIT.md with §2 Composition Attestations.

    (a) ADD×REMOVE composition: prove the do-work crank reward path + the subscription claimable-read path do NOT collide with the removed ETH-auto-rebuy interception — ETH jackpot winnings always credit to claimable (no ticket-conversion interception), the BURNIE flip recycle is flat 75bps unconditional (no deity scaling, no under/over-credit), and the kept _hasAnyLazyPass (PROTO-01/RM-04) is the only afKing-named symbol retained. Cite the live contracts/ file:line for each and cross-reference the 318-05 behavioral proof.

    (b) JGAS single-call composition: prove the daily ETH jackpot completes in ONE advanceGame stage at the 305-winner ceiling (buckets 159/95/50/1) with NO resume stage entered and NOTHING stranded by the dropped resumeEthPool carry — re-attest the 318-06 JackpotSingleCallCorrectness proof (305 emissions, per-bucket exact share, conservation sum(claimable)+whale-pass==paidWei≤pool, single-call fully resolves with no resume carry) and confirm the split-removal delta composes cleanly with the RM-02 ETH-auto-rebuy removal (no orphaned/double-credited winnings).

    (c) OPEN-E default-self equivalence: prove fundingSource==0 short-circuits the approval read and resolves src=subscriber at :439, SLOADing the same single _poolOf slot at :728 as pre-OPEN-E (per-draw gas unchanged, behavior-identical), and that the Vault/sDGNRS SUB-09 callers both pass address(0)=self (re-grep the :474 / :380 call sites); confirm no cross-account spend is possible without isOperatorApproved(S,M) consent checked AT subscribe() only (cite the live gate lines). Each attestation row carries a NEGATIVE-VERIFIED or SAFE_BY_DESIGN verdict + the re-grepped evidence anchors. Close with §3 a forward-cite placeholder for the FINDINGS-v46.0.md §3 delta section (resolved by Plan 04).
  </action>
  <verify>
    <automated>grep -qiE "single.?call|305|resumeEthPool" .planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-02-DELTA-AUDIT.md && grep -qiE "default.?self|fundingSource == 0|fundingSource==0|behavior.?identical" .planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-02-DELTA-AUDIT.md && grep -qiE "75[ ]?bps|claimable" .planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-02-DELTA-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - §2 ADD×REMOVE attestation proves ETH winnings always credit to claimable, BURNIE recycle is flat 75bps unconditional, and _hasAnyLazyPass is the only retained afKing-named symbol — each with a live contracts/ file:line + cross-ref to 318-05
    - §2 JGAS attestation proves single-call at the 305 ceiling (buckets 159/95/50/1), no resume stage, nothing stranded by the dropped resumeEthPool carry, re-attesting the 318-06 conservation proof
    - §2 OPEN-E attestation proves fundingSource==0 default-self is behavior-identical (short-circuit at :439, same _poolOf slot at :728, per-draw gas unchanged) and that the Vault/sDGNRS callers pass address(0)=self (:474 / :380)
    - §2 confirms no cross-account spend without isOperatorApproved consent at subscribe() with the live gate lines cited
    - §3 forward-cite placeholder for FINDINGS-v46.0.md §3 present
    - `git status --porcelain contracts/ test/` shows no mutation
  </acceptance_criteria>
  <done>320-02-DELTA-AUDIT.md extended with the three composition attestations (ADD×REMOVE clean, JGAS single-call clean, OPEN-E default-self identical) and the FINDINGS §3 forward-cite.</done>
</task>

</tasks>

<verification>
- 320-02-DELTA-AUDIT.md exists with the complete delta-surface table + the three composition attestations.
- `git diff 30b5c89c -- contracts/ test/` returns empty (SOURCE-TREE FROZEN — this is a read-only delta audit).
- Every file:line cited is HEAD-verified; the stale CONTEXT ~:396/~:587 cites are NOT carried.
- RM + JGAS kill sets grep-clean; OPEN-E default-self behavior-identical; daily ETH jackpot single-call at 305 ceiling with nothing stranded.
</verification>

<success_criteria>
Every v45→v46 contracts/ change is audited together; the ADD surfaces and REMOVE surfaces compose cleanly with no orphaned/double-credited winnings; the daily ETH jackpot pays all 305 winners in one call with nothing stranded by the dropped resumeEthPool carry; the OPEN-E default-self path is behavior-identical with no cross-account spend without consent. (ROADMAP success criterion 2.)
</success_criteria>

<output>
Create `.planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-02-SUMMARY.md` when done, recording the subject commit set, the delta-surface row count + verdicts, and the three composition attestations.
</output>
