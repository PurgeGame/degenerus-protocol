# Phase 320 Adversarial Sweep — Per-Skill Disposition: `/contract-auditor` (ANCHOR)

```yaml
[invocation]
skill: /contract-auditor
mode: PARALLEL_SUBAGENT
dispatch_timestamp: "2026-05-24T12:11:10Z"
runner: task-subagent
fallback_reason: null
charge_anchor: ".planning/phases/320-audit-adversarial-sweep-add-remove-delta-audit-closure-termi/320-ADVERSARIAL-CHARGE.md"
note: "dispatched first as the sequential anchor subagent; orchestrator held the Task tool"
```

```yaml
[skeptic-filter]
discarded: []
```

> **Source-tree note (load-bearing).** The git worktree this subagent was spawned in (`.claude/worktrees/agent-a52e10c6df1dea8ba`) carries a STALE pre-OPEN-E copy of `contracts/AfKing.sol` (bool fields, no `fundingSource`). The audit-subject HEAD is the MAIN working tree at `/home/zak/Dev/PurgeGame/degenerus-audit/contracts/`, which is byte-frozen `30b5c89c..HEAD` and carries the 21 `fundingSource` references (the OPEN-E diff `42140ceb` + WR-01 event `e1baa978`). Per `feedback_contract_locations.md` ("stale copies exist elsewhere") ALL re-greps in this MD were run against the MAIN tree. The worktree copies were NOT audited.

---

## §0 — Charged SWP sub-charges (verbatim re-anchor)

This skill (`/contract-auditor`, the FIRST/anchor) owns:

- **SWP-AUTH** — "Prove the three authority primitives cannot be driven by an unauthorized caller or be made to over/under-pay. `burnForKeeper` is all-or-nothing and `onlyAfKing`-gated on the BURNIE side… `creditFlip` is `onlyFlipCreditors`-gated (extended to include the keeper) and pays the do-work bounty as ONE gas-pegged credit per tx (never per-item). `batchPurchase` is keeper-gated with a per-player try/catch + slice-refund. Red-team: can a non-keeper reach `burnForKeeper`/`creditFlip`? can the all-or-nothing semantics be partially-applied to double-spend a charge? can `batchPurchase`'s per-player try/catch swallow a failure that strands or double-credits a winning?"

- **SWP-OPENE (the four D-03 residual STRUCTURAL must-pass charges)** —
  - **.1** No cross-account draw without consent: a non-approved `M` pointing `fundingSource = S` MUST revert (`NotApproved`); `isOperatorApproved(S, M)` genuinely enforced AT `subscribe()` (subscribe-only, never per-draw, never at day-31). Bar: a non-zero, non-self `fundingSource` whose owner has NOT approved the subscriber reaching the ETH `_poolOf` draw or a `burnForKeeper` charge = CATASTROPHE.
  - **.2** Default-self identity byte-identical: `fundingSource == 0` (self) stays behavior-identical to pre-OPEN-E — short-circuits the approval read, SLOADs the same single `_poolOf` slot, per-draw gas unchanged.
  - **.3** No escalation beyond the grant: `M` cannot redirect to drain a *different* non-approving address; the `fundingSource` redirect cannot spoof the Vault/sDGNRS subscriber-identity skip-kill exemption (exemption keyed on the un-spoofable SUBSCRIBER identity, never the source).
  - **.4** Trust-the-sub temporal bound (documented, accepted): a later `setOperatorApproval(M,false)` revoke does NOT retroactively stop an active sub; drain bounded only by sub lifetime + `S`-defunding or `M` cancelling → SAFE_BY_DESIGN.
  - **D-02 BURNIE-funding overload** — recorded as a SAFE_BY_DESIGN row per D-01 (operator-approval IS the trust boundary; do NOT model a "tricked into approving" actor; `allowBurnieFunding` flag DROPPED, do NOT charge a missing-flag finding).

- **SWP-REMOVE** — the grep-clean kill sets (RM kill-set + JGAS split-removal kill-set) + the JGAS daily-ETH SINGLE-CALL payout structure at the 305-winner ceiling (buckets 159/95/50/1, no resume stage, nothing stranded by the dropped `resumeEthPool` carry) + the flat-75bps no-under/over-credit collapse.
  - **.A** ETH-auto-rebuy strand (RM-02): do ETH jackpot winnings now ALWAYS credit to `claimable`? RM kill-set grep-clean.
  - **.B** BURNIE 75bps collapse under/over-credit.
  - **.C** JGAS jackpot two-call-split removal (JGAS-01/02): single-call daily-ETH at 305-winner ceiling, no resume, JGAS kill-set grep-clean, conservation `sum(claimable) + whale-pass == paidWei ≤ pool`.

---

## §1 — Per-hypothesis disposition table

All `file:line` anchors below are RE-GREPPED against the MAIN-tree HEAD (frozen `30b5c89c..HEAD`). The CHARGE's anchors were treated as STARTING points; the live lines below supersede them where they drifted (e.g. the WR-01 event commit shifted the gate from the verification's `:393-398` to the live `:397-402`, the subscribe-burn from `:434` to `:438`, the src-resolve from `:693` to `:697`, the ETH draw from `:724/:746` to `:728/:750`).

| Hypothesis-ID | Verdict | Severity | Evidence anchors (RE-GREPPED file:line) | Reasoning |
| --- | --- | --- | --- | --- |
| **SWP-AUTH.burnForKeeper-ACL** | NEGATIVE-VERIFIED | N-A | `BurnieCoin.sol:456-459` (`burnForKeeper(...) external onlyAfKing`), `:533-534` (`modifier onlyAfKing { if (msg.sender != ContractAddresses.AF_KING) revert OnlyAfKing(); }`) | The BURNIE-side burn is reachable ONLY by the pinned `ContractAddresses.AF_KING`. Any non-keeper caller reverts `OnlyAfKing` at the modifier. Structurally unreachable by an unauthorized caller (STRICT structural arm). |
| **SWP-AUTH.burnForKeeper-all-or-nothing** | NEGATIVE-VERIFIED | N-A | `BurnieCoin.sol:460-471` (capacity check `if (available < amount) return 0;` at `:466` PRECEDES any state change; on success exactly `amount` burned via `_consumeCoinflipShortfall` + `_burn`, returns `amount`); caller assert `AfKing.sol:442` (`if (burned != cost) revert BurnieChargeFailed();`) + `:638` (`if (burned != extractCost) {…auto-pause}`) | All-or-nothing cannot be partially applied: the capacity check is the first effect-free statement; either the full `amount` burns (return `amount`) or nothing burns (return 0). The burn is atomic inside the single tx — there is no intermediate state a re-entrant call could observe to double-spend a charge. The two keeper call sites both hard-check `burned == cost`/`extractCost` and revert/auto-pause on any shortfall, so a partial burn can never be silently accepted. No double-spend primitive exists. |
| **SWP-AUTH.creditFlip-ACL** | NEGATIVE-VERIFIED | N-A | `BurnieCoinflip.sol:849-855` (`creditFlip(...) external onlyFlipCreditors`), `:191-201` (`modifier onlyFlipCreditors` — sender ∈ {GAME, QUESTS, AFFILIATE, ADMIN, AF_KING}, else `revert OnlyFlipCreditors()`); keeper-extension at `:198` (`sender != ContractAddresses.AF_KING`) | `creditFlip` is reachable only by the five pinned creditors; the keeper (`AF_KING`) is the extended fifth. A non-creditor reverts `OnlyFlipCreditors`. The bounty recipient is `msg.sender` of `sweep()` (`AfKing.sol:802` → `creditFlip(msg.sender, bountyEarned)`), so a cranker can only credit ITSELF — it cannot credit an arbitrary third party. Structurally closed. |
| **SWP-AUTH.creditFlip-one-per-tx** | NEGATIVE-VERIFIED | N-A | `AfKing.sol:801` (`bountyEarned = batchLen * ((BOUNTY_ETH_TARGET * PRICE_COIN_UNIT * bountyMultiplier) / mp);`), `:802` (single `ICoinflip(...).creditFlip(msg.sender, bountyEarned)`) | Exactly ONE `creditFlip` external call per `sweep()` tx (`:802`). The amount scales `batchLen × per-item-marginal` — the CR-01 faucet fix pegs the per-item term to the marginal box gas (71_203), NOT a single-box total, so N successful items → one credit of N×marginal. There is no per-item `creditFlip` loop. The bounty is a deferred coinflip-STAKE (no liquid BURNIE leaves the keeper); supply/recycle EV is handed to `/economic-analyst` (SWP-ECON). |
| **SWP-AUTH.batchPurchase-keeper-gate** | NEGATIVE-VERIFIED | N-A | `DegenerusGame.sol:1687-1697` (`batchPurchase` — `:1692` `if (msg.sender != ContractAddresses.AF_KING) revert E();`, `:1693` `rngLockedFlag` abort, `:1694` `gameOver` abort, `:1697` length-equality guard), `:1729-1735` (`_batchPurchaseUnit` — `:1733` `if (msg.sender != address(this)) revert E();` onlySelf) | Only the pinned keeper can call `batchPurchase`; the per-player unit is `onlySelf`. `rngLocked`/`gameOver`/length-mismatch are pre-checked ONCE at entry → whole-batch abort (no partial-state). Structurally closed against unauthorized callers. The keeper "trusts itself" because it structurally only iterates its OWN subscriber set (`_subscribers`). |
| **SWP-AUTH.batchPurchase-try/catch-strand-or-double-credit** | NEGATIVE-VERIFIED | N-A | `DegenerusGame.sol:1700-1715` (per-player `try this._batchPurchaseUnit{value: slice}(...) { spent += slice; } catch {}`), `:1717-1721` (ONE post-loop refund `if (msg.value > spent) payable(msg.sender).call{value: msg.value - spent}`); `AfKing.sol:569`/`:794` (loop + `batchPurchase` in ONE tx), `:750` (loop debit `_poolOf[src] -= msgValue`), `:767` (loop day-stamp `sub.lastSweptDay = today`), `:287-290` (`receive()` credits `_poolOf[msg.sender]` = GAME on refund) | **Double-credit: impossible.** A winning is credited only inside `_batchPurchaseUnit → _purchaseFor`, atomic per `try`; a reverting slice rolls back its entire sub-call frame (no stored batch-debit a re-entrant sweep/cancel could replay — CEI-proof per the SPEC OPEN-C lock). A success adds `spent += slice` exactly once. **Strand: not a third-party griefing vector + not a winnings-strand.** The slice is an INPUT (the subscriber's self-configured daily `cost`), not a payout. On a deep per-player revert the slice is refunded once to `_poolOf[GAME]`, while the subscriber was already debited (`:750`) + day-stamped (`:767`). The day-stamp bounds the loss to ONE day's self-configured spend, and ONLY in the rare case the subscriber's OWN purchase reverts deep in the mint→lootbox→prize-pool→EV-cap→quest path. EV-lens (a) FAILS for a griefing finding: a third party cannot inject a revert against a *specific targeted victim* — the reverts are functions of the victim's own account + global state (not attacker-injectable per-victim). A batch-ENTRY abort (rngLocked/gameOver) reverts the WHOLE `sweep()` tx (loop + batch in one frame) → all debits/day-stamps roll back. See SWP-REMOVE.A. Proven by `test/fuzz/CrankNonBrick.t.sol:26-29` (failed slice isolated + refunded, no brick). |
| **SWP-OPENE.1 — no cross-account draw without consent** | NEGATIVE-VERIFIED | N-A (bar: CATASTROPHE on failure) | `AfKing.sol:375-382` (`subscribe(...)` 6th param `address fundingSource`), `:397-403` (gate: `if (fundingSource != address(0) && fundingSource != subscriber && !IGame(GAME).isOperatorApproved(fundingSource, subscriber)) revert NotApproved();`), `:130` (`error NotApproved();`), `:426` (`s.fundingSource = fundingSource;` — the SOLE set point), `:438-441` (subscribe-burn resolves stored source), `:634-637` (day-31 burn resolves stored source — NO re-check), `:697`/`:728`/`:750` (per-draw resolve/read/debit — NO re-check) | A non-zero, non-self `fundingSource` whose owner has NOT operator-approved the subscriber reverts `NotApproved()` at `subscribe()` (`:402`) BEFORE `s.fundingSource` is written (`:426`) and before any burn/draw. The gate fires exactly once, at subscribe — never at day-31 renewal (`:634` reads the stored source unconditionally), never per-draw (`:697` reads stored source). The set-point is the ONLY place an attacker can introduce a foreign source, and it is gated. A non-approved cross-account source can therefore NEVER reach the `_poolOf` draw or a `burnForKeeper` charge. Verdict bar met (CATASTROPHE would require a non-approved source reaching a draw — physically reverts). |
| **SWP-OPENE.2 — default-self byte-identical** | NEGATIVE-VERIFIED | N-A (bar: FINDING on divergence) | `AfKing.sol:397-398` (`fundingSource != address(0) && …` — `address(0)` short-circuits the `&&`, NO `isOperatorApproved` SLOAD/STATICCALL on the self path), `:439` (`s.fundingSource == address(0) ? subscriber : s.fundingSource` → `subscriber`), `:635` (`… ? player : …` → `player`), `:697` (`address src = sub.fundingSource == address(0) ? player : sub.fundingSource` → `player`), `:728` (`_poolOf[src]` read), `:750` (`_poolOf[src]` debit) | When `fundingSource == 0` (self), the OPENE-04 gate short-circuits at the first `&&` operand → zero approval read. Every routing point resolves to the SAME identity the pre-OPEN-E code used (`subscriber` at subscribe-burn, `player` at day-31/draw). The ETH-draw gate `:728` and CEI debit `:750` both key on the single `src` slot resolved once at `:697` → one `_poolOf` slot SLOAD, identical to pre-OPEN-E. No per-draw behavioral or gas divergence on the default path. RE-ATTESTED by 319.1-VERIFICATION truth #12 (`testFundingSourceDefaultSelfIsByteEquivalent`). |
| **SWP-OPENE.3 — no escalation / no skip-kill spoof** | NEGATIVE-VERIFIED | N-A (bar: FINDING on escalation) | Escalation: `AfKing.sol:397-403` gate (a redirect to a *different* address still requires that address's `isOperatorApproved(fundingSource, subscriber)`); skip-kill exemption: `:728-736` — the funding-skip branch reads `_poolOf[src]` (`:728`) but the Vault/sDGNRS exemption at `:729` keys on `if (player == ContractAddresses.VAULT \|\| player == ContractAddresses.SDGNRS)` where `player = _subscribers[cursor]` (`:610`, the iterable-SET subscriber identity), NEVER `src`; comment `:693-697` + `:721-727` ("VAULT/SDGNRS exemption stays keyed on the un-spoofable `player`, never `src`"; "no settable exemption flag"); contract-level invariant note `:99-106` | (i) **No escalation:** `M` cannot redirect `fundingSource` to a *different* non-approving address — every non-zero, non-self source independently requires its OWN `isOperatorApproved(source, subscriber)` at `:400`. (ii) **No skip-kill spoof:** the exemption is the pinned-`player`-identity branch ONLY (`:729`). A NORMAL sub setting `fundingSource = VAULT` is still cancelled because `player` (the subscriber) ≠ VAULT — the exemption never reads `src`/`fundingSource`. There is no settable exemption flag. RE-ATTESTED by 319.1-VERIFICATION LANDMINE A (`testFundingSourceVaultDoesNotInheritExemption`) + LANDMINE B (`:660` per-draw `isOperatorApproved(player, address(this))` byte-unchanged). |
| **SWP-OPENE.4 — trust-the-sub temporal bound** | SAFE_BY_DESIGN | N-A | `AfKing.sol:358-374` (NatSpec: "Subscribe-time auth only: a later `setOperatorApproval(subscriber, false)` by the source does NOT stop an active sub… The source halts draws by defunding… the subscriber halts by cancelling"), `:634` (day-31 renewal reads stored source with NO approval re-check), `:697` (per-draw reads stored source) | This is the intentional D-03.4 posture: revocation does NOT retroactively stop an active sub. The drain is BOUNDED — by sub lifetime (≤ the 30-day prepaid window, then day-31 auto-extract which itself can auto-pause on a shortfall) + S-defunding (`withdraw()` drains `_poolOf[S]`; spending down general-wallet BURNIE) + M cancelling. It is NOT unbounded. Accepted by design under D-01 (operator-approval = the trust boundary). RE-ATTESTED by 319.1-VERIFICATION truth #9 (`testRevokeDoesNotStopActiveSubButDefundDoes`). Verdict bar: only an UNBOUNDED drain would be a FINDING — none exists. |
| **SWP-OPENE.D-02 — BURNIE-funding overload** | SAFE_BY_DESIGN | N-A | `AfKing.sol:358-374` NatSpec ("BURNIE blast-radius caveat: the same approval also authorizes burning the source's general-wallet BURNIE and pending coinflip — sharper than the pre-funded ETH escrow"); `BurnieCoin.sol:446-471` (`burnForKeeper` sources from `balanceOf[user] + previewClaimCoinflips(user)`) | The operator-approval that authorizes `M`'s subscription ALSO authorizes burning `S`'s general-wallet BURNIE + pending coinflip (sharper than the pre-funded ETH escrow `_poolOf[S]`). Per D-01 this is consensual by construction: any `M` holding `setOperatorApproval(S, M)=true` is, by the threat model, either the same person as `S` (multi-wallet) or a fixed/known contract `S` deliberately integrated with. The protocol's job is to enforce the gate exists (it does, `:397-403`); the consent SCOPE is the grantor's responsibility. NOT a FINDING_CANDIDATE. The `allowBurnieFunding[S][M]` opt-in flag is DROPPED per D-02a (adds nothing under D-01) — NO missing-flag finding charged. (NatSpec at `:371-374` references the dropped alternative as a "named deferred tighter alternative" — purely documentary, not a live gap.) |
| **SWP-REMOVE.A — ETH-auto-rebuy strand (RM-02) + RM kill-set grep-clean** | NEGATIVE-VERIFIED | N-A | RM kill-set grep output (below); `DegenerusGame.sol:1472` + `interfaces/IDegenerusGame.sol:370` (kept `hasAnyLazyPass`); consumed at `AfKing.sol:433`/`:625` | The RM kill set for the REMOVED legacy symbols (`setAutoRebuy\|autoRebuyState\|AutoRebuyState\|_processAutoRebuy\|_calcAutoRebuy\|settleFlipModeChange\|_afKingRecyclingBonus\|deactivateAfKingFromCoin\|syncAfKingLazyPassFromCoin\|AFKING_`), excluding `contracts/test`+`mocks`+`contracts/AfKing.sol`, returns **ZERO** matches. The only surviving `afKing`-named symbols are (a) the new SUB-09 self-subscribe wiring `DegenerusVault.sol:407/474` + `StakedDegenerusStonk.sol:318/380` (an ADD-surface variable name `IAfKingSubscribe afKing`, NOT a removed legacy symbol), and (b) the KEPT `_hasAnyLazyPass`/`hasAnyLazyPass` (`DegenerusGame.sol:1472`, the PROTO-01/RM-04 keeper gate). With the ETH-auto-rebuy interception path removed, ETH jackpot winnings always credit to `claimable` via `_creditClaimable` (`DegenerusGameJackpotModule.sol:745`) — no ticket-conversion interception left behind; no orphaned winning. RE-ATTESTED by 318-05 SAFE-04 (deterministic-no-VRF-word-credit). |
| **SWP-REMOVE.B — BURNIE 75bps collapse** | NEGATIVE-VERIFIED | N-A | The flip-autorebuy is the KEPT 75bps flat unconditional credit (REW/SUB scope; no deity/activity scaling). The collapse removes the per-actor scaling residue; the bounty itself is the `creditFlip` deferred stake (`AfKing.sol:801-802`), pegged flat per item × count. | The 75bps semantics collapsed to a flat, unconditional credit with NO deity/activity multiplier — there is no scaling term left to produce a rounding leak or over-credit residue. The credit is integer-pegged (`bountyEarned = batchLen * ((BOUNTY_ETH_TARGET * PRICE_COIN_UNIT * bountyMultiplier) / mp)` — one division, deterministic, floor-rounded down → never over-credits; the floor truncation is a sub-wei under-credit that favors the protocol, not the cranker). No under/over-credit primitive. (Deeper recycle-EV of the 75bps is handed to `/economic-analyst` SWP-ECON; this row dispositions only the structural no-leak.) |
| **SWP-REMOVE.C — JGAS single-call + JGAS kill-set grep-clean** | NEGATIVE-VERIFIED | N-A | JGAS kill-set grep output (below, ZERO); `DegenerusGameAdvanceModule.sol:60-70` (stage constants STAGE_GAMEOVER(0)…STAGE_JACKPOT_DAILY_STARTED(10) — NO `STAGE_JACKPOT_ETH_RESUME`), `:462-464` (single dispatch at `STAGE_JACKPOT_DAILY_STARTED`); `DegenerusGameJackpotModule.sol:208-210` (`DAILY_ETH_MAX_WINNERS = 305`, comment `159 + 95 + 50 + 1 = 305`), `:457-466` (`_processDailyEth` single call, cap 305 at `:445`), `:467-479` (conservation), `runTerminalJackpot:286` (same single `_processDailyEth`, cap at `:278`), `_processDailyEth:1088`, `:1143` (`claimablePool += liabilityDelta`), `:1275` (`_addClaimableEth` per winner) | The JGAS kill set (`SPLIT_CALL1\|SPLIT_CALL2\|resumeEthPool\|_resumeDailyEth\|STAGE_JACKPOT_ETH_RESUME\|call1Bucket`), excluding `contracts/test`+`mocks`, returns **ZERO** matches (broader resume/split scan also ZERO). The daily-ETH jackpot completes in ONE call to `_processDailyEth` (both `payDailyJackpot:457` and `runTerminalJackpot:286` funnel there), capped at the 305-winner ceiling (159/95/50/1). NO resume stage exists in the Advance stage enum; NOTHING is stranded by a dropped `resumeEthPool` carry (it does not exist). **Conservation:** per-winner `_addClaimableEth` accumulates `paidWei`; `claimablePool += liabilityDelta` (`:1143`); non-final day deducts exactly `paidDailyEth` from the pool (`:478`); final day deducts the full budget and returns `unpaidDailyEth = budget - paidWei` to `futurePrizePool` (`:468-475`) — so `sum(claimable credits) + whale-pass == paidWei ≤ budget ≤ pool`, no under/over-pay of a bucket at the single-call ceiling. (The separate `dailyJackpotCoinTicketsPending` Phase-2 `payDailyJackpotCoinAndTickets:559` is a PRE-EXISTING coin+ticket gas split documented at `:550-551`, NOT the removed ETH-jackpot two-call split — distinct mechanism, never part of JGAS.) RE-ATTESTED by 318-06 `JackpotSingleCallCorrectness` + 318-05 `RngFreezeAndRemovalProofs`. |

### Kill-set grep outputs (recorded verbatim per re-grep mandate)

**RM kill set** — removed legacy symbols, excluding `contracts/test`, `mocks`, `contracts/AfKing.sol`:
```
grep -rnE "AFKING_|setAutoRebuy|autoRebuyState|AutoRebuyState|_processAutoRebuy|_calcAutoRebuy|settleFlipModeChange|_afKingRecyclingBonus|deactivateAfKingFromCoin|syncAfKingLazyPassFromCoin" contracts/ --include=*.sol | grep -viE "contracts/test|/mocks/"
→ (ZERO matches)
```
The broad `afKing`-named survivors (ADD surface, NOT removed legacy):
```
contracts/DegenerusVault.sol:407   IAfKingSubscribe internal constant afKing =
contracts/DegenerusVault.sol:474   afKing.subscribe(address(this), true, false, 1, 0, address(0));
contracts/StakedDegenerusStonk.sol:318  IAfKingSubscribe private constant afKing =
contracts/StakedDegenerusStonk.sol:380  afKing.subscribe(address(this), true, false, 1, 2, address(0));
```
Kept gate: `hasAnyLazyPass` at `DegenerusGame.sol:1472` + `interfaces/IDegenerusGame.sol:370`; consumed `AfKing.sol:433`/`:625`.

**JGAS kill set** — excluding `contracts/test`, `mocks`:
```
grep -rnE "SPLIT_CALL1|SPLIT_CALL2|resumeEthPool|_resumeDailyEth|STAGE_JACKPOT_ETH_RESUME|call1Bucket" contracts/ --include=*.sol | grep -viE "contracts/test|/mocks/"
→ (ZERO matches)
broad resume/split scan → (ZERO matches)
```

---

## §2 — Skeptic-Filter Self-Discarded

**No self-discards.** The one nuanced surface examined (SWP-AUTH.batchPurchase failed-slice refund-to-`_poolOf[GAME]` accounting) was carried THROUGH the dual-gate filter to a NEGATIVE-VERIFIED verdict (not discarded): the structural arm shows the slice is an INPUT not a winnings payout (no winnings-strand), and the 3-condition EV lens FAILS at condition (a) for any griefing framing — a third party cannot inject a revert against a specific targeted victim, since the reachable per-player reverts are functions of the victim's own account state + global game state, not attacker-injectable per-victim. The day-stamp bounds the worst case to one day of the subscriber's OWN self-configured spend in the rare deep-revert case. This is recorded as a NEGATIVE-VERIFIED disposition row with the full trace rather than a discard, per the charge's preference for full enumeration. `discarded: []` is the correct frontmatter state.

---

## §3 — Cross-skill hand-off notes

### To `/zero-day-hunter` (composition, redirect, stranding)

1. **batchPurchase failed-slice refund lands in `_poolOf[GAME]`, not the subscriber pool** (`DegenerusGame.sol:1717-1721` refund → `AfKing.sol:287-290` `receive()` credits `_poolOf[msg.sender]` = the GAME contract). I dispositioned this NEGATIVE-VERIFIED on the structural+EV arm (no per-victim revert injection; bounded to one day's self-cost). Probe the COMPOSITION angle I did not own: can a griefer who controls a contract subscriber's *fallback/receive* (e.g. a contract `S` whose mint path re-enters or whose lootbox callback reverts deterministically) chain that with the OPEN-E redirect or the crank bounty to manufacture a differential? My read: no (the slice is refunded, not double-spent; `_poolOf[GAME]` is inert), but the cross-surface interleave is your charge (SWP-COMPOSE).
2. **The day-stamp (`AfKing.sol:767`) is written for a player BEFORE the batch fires (`:794`), inside the same tx.** A batch-ENTRY abort rolls the whole tx back (loop + batch one frame). A per-player catch does NOT. Confirm there is no SWP-GRIEF path where a griefer's crank advances the cursor (`:777` `_sweepCursor` persists) leaving a victim mid-set un-swept-but-day-stamped across two concurrent same-block sweeps — the `lastSweptDay >= today` skip at `:614` should make this idempotent, but it's your subscription-trigger-griefing charge to confirm.
3. **SWP-OPENE.3 escalation/spoof is structurally closed** (exemption keyed on `player` not `src`, `:729`). The redirect-to-different-address escalation is doubly gated (each non-self source needs its own approval, `:400`). Your SWP-OPENE.3 redirect charge should find the same — flagging so we converge, not rediscover.

### To `/economic-analyst` (recycle, supply, trust-the-sub EV, griefer)

1. **The crank bounty is a deferred coinflip-STAKE, not a liquid mint** (`AfKing.sol:801-802` `creditFlip` → `BurnieCoinflip._addDailyFlip`). BURNIE is only minted on a coinflip CLAIM/WIN via `mintForGame` (`BurnieCoinflip.sol:384/725/744`), subject to the 50/50 flip outcome — the bounty is NOT a guaranteed BURNIE faucet. This is your SWP-ECON recycle/supply surface: model whether `creditFlip` stake → flip → claim → re-stake can clear a positive-EV perpetual loop given the flat 75bps and the per-item marginal peg (71_203 box / 66_528 resolve at 0.5 gwei). My structural read: the bounty floor-rounds DOWN (`bountyEarned = batchLen * ((... ) / mp)`), so each item under-credits sub-wei — the protocol never over-pays per item. The round-trip ≤ 0 claim is the 318 SAFE-01 (`CrankFaucetResistance.t.sol`) — re-attest the economics.
2. **SWP-OPENE.4 trust-the-sub temporal bound** I dispositioned SAFE_BY_DESIGN (bounded by sub lifetime + S-defunding). Your charge is the EV of that bound: model the worst-case drain a revoked-but-active funding sub can extract before S notices and defunds (`withdraw()`), and the D-02 BURNIE-overload cost/benefit. My structural anchor: the drain per renewal cycle is one `SUB_COST_ETH_TARGET` BURNIE charge + the daily ETH `cost` draws, bounded by `_poolOf[S]` + S's BURNIE balance, and the day-31 auto-extract auto-pauses on a shortfall (`AfKing.sol:638-650`).
3. **SWP-SKIP funding-margin griefer** (your + zero-day's shared charge): the kill at `AfKing.sol:737-744` fires ONLY when `_poolOf[src] < msgValue` for a NORMAL sub (`:728`). The exemption (`:729`) protects Vault/sDGNRS. Confirm a third-party cranker cannot change WHO is killed — only WHETHER the victim's OWN funding covers its OWN configured `cost`. The cranker chooses WHEN (timing), but the funding-skip predicate is purely the victim's pool vs the victim's self-configured cost; no third-party state feeds it. The stall-multiplier (`:586-597`) only scales the cranker's bounty, not the kill predicate.
