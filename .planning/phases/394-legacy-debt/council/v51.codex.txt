At `a8b702a7`, I found **no confirmed break** in this slice.

**LEGACY-03 — VERIFIED SOUND:** `claimBingo` is freeze-safe and CEI-tight. The consumed state is `traitBurnTicket` at `contracts/storage/DegenerusGameStorage.sol:441`; `claimBingo` only reads it at `contracts/modules/DegenerusGameBingoModule.sol:135-140`. The only append path I found is ticket resolution in `_raritySymbolBatch`, which writes the resolved holder arrays at `contracts/modules/DegenerusGameMintModule.sol:773-812`. Queue freezing prevents post-word steering: `_swapTicketSlot` flips the write/read buffer and resets drain state at `contracts/storage/DegenerusGameStorage.sol:780-784`; `_swapAndFreeze` calls it before RNG processing at `:793-805`; daily/midday RNG advances the lootbox index before the word lands at `contracts/modules/DegenerusGameAdvanceModule.sol:1136-1151` and `:1689-1699`. Far-future sale also reverts during the RNG lock at `contracts/modules/DegenerusGameMintModule.sol:1214`.

Tier precedence holds. `bingoFirsts` is the controlling state at `contracts/storage/DegenerusGameStorage.sol:1933-1936`; the quadrant-first branch marks both the symbol and quadrant bits before interactions at `contracts/modules/DegenerusGameBingoModule.sol:157-169`, so it suppresses the symbol bonus. The symbol-first branch preserves the high quadrant-mask bits with `(bf & ~uint64(0xFFFFFFFF)) | ...` at `:173-176`.

Dedup and empty-pool behavior hold. `bingoClaimed` is at `contracts/storage/DegenerusGameStorage.sol:1929-1931`; the per-player `(level, quadrant)` bit is checked and set at `contracts/modules/DegenerusGameBingoModule.sol:148-151` before the sDGNRS and BURNIE calls at `:188-196`. `transferFromPool` clamps and returns zero on empty pool at `contracts/StakedDegenerusStonk.sol:548-570`, so an empty Reward pool consumes the bingo bit and pays only BURNIE. `gameOver` is checked before all of this at `contracts/modules/DegenerusGameBingoModule.sol:122`.

**LEGACY-04a — VERIFIED SOUND:** the rebalance conserves supply and clamps draws. Pool state is `poolBalances` at `contracts/StakedDegenerusStonk.sol:253`; enum members are only `Whale`, `Affiliate`, `Lootbox`, `Reward`, `PresaleBox` at `:241-247`. The allocations are `CREATOR=2000`, `WHALE=1000`, `AFFILIATE=3000`, `LOOTBOX=2000`, `REWARD=1000`, `PRESALE_BOX=1000`, summing exactly to `BPS_DENOM=10000` at `:299-312`. Genesis seeding computes and mints those exact slices at `:384-408`; `INITIAL_SUPPLY = 1e30` is divisible by 10,000, so the dust branch at `:391-397` is a no-op here.

No overdraw: `transferFromPool` clamps to `available` before decrementing at `contracts/StakedDegenerusStonk.sol:548-570`, and `transferBetweenPools` does the same at `:579-593`. The `uint128` narrowing is safe because total pool supply is bounded by `1e30`, far below `2^128-1`.

No live contract consumer hard-codes the old Reward split. Reward-pool consumers read live balances: Bingo at `contracts/modules/DegenerusGameBingoModule.sol:188-193`, Degenerette at `contracts/modules/DegenerusGameDegeneretteModule.sol:1220-1230`, and the coinflip bounty at `contracts/DegenerusGame.sol:465-475`. The affiliate path also reads the live Affiliate pool and subtracts the actual `paid` return before snapshotting the level allocation at `contracts/modules/DegenerusGameAdvanceModule.sol:753-775`.

**LEGACY-04b — VERIFIED SOUND:** I found no final-day sDGNRS `Pool.Reward` deletion/draw path. Grepping the frozen contracts shows `Pool.Reward` only in seeding plus Bingo, Degenerette, and coinflip bounty. Jackpot final-day code mutates ETH prize-pool state (`currentPrizePool`, `claimablePool`, `prizePoolsPacked` at `contracts/storage/DegenerusGameStorage.sol:354-379`), not `StakedDegenerusStonk.poolBalances[Pool.Reward]`.

The ETH final-day accounting conserves backing. Jackpot final physical day computes a current-pool budget, calls `_processDailyEth`, then sets `currentPrizePool` down by the full `dailyEthBudget` and returns unpaid ETH to future at `contracts/modules/DegenerusGameJackpotModule.sol:323-329` and `:433-449`. `_processDailyEth` credits claimable liabilities at `:1058-1120`. The solo whale-pass path moves the pass-cost portion into future and includes that amount in `paidEth` at `:1183-1215` and `:1241-1281`, so the current-pool decrement equals claimable ETH plus whale-pass future funding plus unpaid returned future.

No double-spend with concurrent Bingo/Degenerette Reward draws: final-day jackpot/consolidation does not touch sDGNRS `Pool.Reward`; Bingo/Degenerette each read the fresh Reward balance and rely on the clamp. Same-block cases are transaction-ordered by the EVM, so there is no stale overlapping read/transfer window. I did notice a stale comment at `contracts/modules/DegenerusGameJackpotModule.sol:1047` saying the solo bucket gets “DGNRS on final day,” but the frozen code path does not implement a `Pool.Reward` transfer there.
