Read against `a8b702a7` via `git show`.

**LEGACY-01**
**FINDING: value non-equivalence in deferred whale-pass claim horizon. Severity: SPINE.**

State: `whalePassClaims` at `contracts/storage/DegenerusGameStorage.sol:1107`, live `level` at `contracts/storage/DegenerusGameStorage.sol:237`.

Reachable sequence:
1. A lootbox opens and rolls whale-pass boon type 28. `_activateWhalePass(player)` records only `whalePassClaims[player] += 1` at `contracts/modules/DegenerusGameLootboxModule.sol:1486-1489`, called at `:1900-1901`.
2. No one calls `claimWhalePass(player)`.
3. The game advances one or more levels. The daily RNG request path sets `level = lvl` at `contracts/modules/DegenerusGameAdvanceModule.sol:1701-1709`.
4. Later, anyone calls `claimWhalePass(player)`. The claim reads the stored count at `contracts/modules/DegenerusGameWhaleModule.sol:993`, clears it at `:997`, then recomputes `startLevel = level + 1` at `:1003`, applies stats at `:1005`, and queues `100` levels at `:1007`.

Why guards do not stop it: `whalePassClaims` stores only a count, not the award/open level. Clear-before-award prevents replay, but not delayed materialization. `_livenessTriggered()` only blocks terminal timeout, not a normal level shift. Permissionless claiming also is not a contract-enforced invariant; if nobody claims before `level` moves, the horizon moves.

Impact: for the same `halfPasses`, the ticket count arithmetic is the same, but the pass horizon/stat freeze is not. An inline award at open time would apply from open-time `level + 1`; this path applies from claim-time `level + 1`, so the player can receive 100 future levels instead of the originally-opened 100-level window.

Sound subparts: the pass-bundled box enqueue itself is freeze-safe and single-enqueue. `_recordLootboxEntry` snapshots `lootboxRngPacked` once and derives `index` at `contracts/modules/DegenerusGameWhaleModule.sol:850-851`; first deposit only pushes `boxPlayers[index]` at `:861-888`; later deposits reuse the existing packed record at `:889-906`; the event carries the same index at `:931`. The consumer gates on `lootboxRngWordByIndex[index] != 0` at `contracts/modules/DegenerusGameLootboxModule.sol:681-682` and `_openLootBoxLegWith` reverts if the word is zero at `:545`.

**LEGACY-02a**
**VERIFIED SOUND: AFSUB inclusive boundary and subscribe-time consent gate hold.**

`subscribe` resolves `subscriber` first at `contracts/modules/GameAfkingModule.sol:314-315`. Non-self subscription requires `operatorApprovals[subscriber][msg.sender]` at `:316-320`. A non-zero, non-self `fundingSource` requires `operatorApprovals[fundingSource][subscriber]` at `:322-330`. The storage backing is `operatorApprovals` at `contracts/storage/DegenerusGameStorage.sol:1119`.

The pass horizon snapshot is written to `Sub.validThroughLevel` at `contracts/modules/GameAfkingModule.sol:419`; the field is defined at `contracts/storage/DegenerusGameStorage.sol:2162-2166`. The no-pass guard rejects non-exempt subscribers with zero horizon or a horizon below live `level` at `contracts/modules/GameAfkingModule.sol:414-428`.

The walk uses the documented inclusive boundary: it only crosses when `currentLevel > sub.validThroughLevel` at `contracts/modules/GameAfkingModule.sol:1246`. On crossing it re-reads `_passHorizonOf(player)` at `:1247`; refreshes only if `currentLevel <= h` at `:1248-1250`; otherwise finalizes, deletes, and swap-pops at `:1252-1264`. `_passHorizonOf` uses the same canonical source, deity sentinel or `frozenUntilLevel`, at `:596-605`.

**LEGACY-02b**
**VERIFIED SOUND: MINTDIV count/index accounting stays in lockstep for the charged property.**

The durable ticket debt is `ticketsOwedPacked[levelKey][player]`, defined at `contracts/storage/DegenerusGameStorage.sol:489-491`. Enqueue adds the player only when the packed debt was zero and then increments owed at `:701-710`.

In the future-ticket loop, `_raritySymbolBatch` receives the current `processed` cursor and `take` at `contracts/modules/DegenerusGameMintModule.sol:637-645`. The stored debt is reduced by exactly `take` at `:653-665`, then the v50 index advance uses `processed += take` at `:667-669`. Completion advances `idx` and resets `processed = 0` only when `remainingOwed == 0` at `:672-676`; skip/cleanup paths also reset before moving to the next player at `:597-618`.

The current-level path delegates the same invariant through `_processOneTicketEntry`: caller passes `processed` at `contracts/modules/DegenerusGameMintModule.sol:881-891`; the helper writes exactly `take` traits at `:993-1001`, decrements owed by exactly `take` at `:1010-1023`, and returns `advance = remainingOwed == 0` at `:1024`. The caller either resets `processed` on advance or does `processed += take` at `:893-904`.

So the write count equals the consumed ticket count across budget splits; no path advances by `writesUsed`, and the next-player boundary is off-by-one clean.
