---
phase: 394-legacy-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/394-legacy-debt/394-01-COUNCIL-PROMPT-V50.md
  - .planning/phases/394-legacy-debt/council/v50.gemini.txt
  - .planning/phases/394-legacy-debt/council/v50.codex.txt
  - .planning/phases/394-legacy-debt/council/v50.council.json
  - .planning/phases/394-legacy-debt/394-01-COUNCIL-NET.md
autonomous: true
requirements: [LEGACY-01, LEGACY-02]
user_setup: []

must_haves:
  truths:
    - "The cross-model council (NET 1) is on record for the full v50 legacy-debt slice — the whale-pass O(1) deferred-claim path (claimWhalePass + the box-open record), AFSUB pass-gating (validThroughLevel eviction/refresh + the OPEN-E operator-approval re-attestation), and the MINTDIV index alignment (processed += take)"
    - "The council prompt is charged neutrally against the frozen subject a8b702a7, with the by-design rulings explicitly excluded so the council does not waste passes on settled intent: the AFSUB pass-eviction inclusive boundary (keep funded sub while currentLevel <= validThroughLevel, evict at +1) is INTENDED; the OPEN-E operator-approval IS the trust boundary (BURNIE-funding overload accepted, no 'tricked into approving' actor); the whale-pass / WWXRP worthlessness is by-design — the council VERIFIES freeze-safety / accounting-correctness / O(1)-equivalence / index-alignment, NOT the design intent"
    - "The v50 charge is encoded as three dedicated named break-targets: (1) the whale-pass O(1) deferred-claim path is value-equivalent to the retired inline mint and freeze-safe (WHALE-04 freeze + claimWhalePass + the box-open record); (2) AFSUB pass-gating refreshes/evicts at the correct inclusive boundary and the OPEN-E operator-approval gate is checked at the right point; (3) the MINTDIV index alignment (processed += take, not writesUsed>>1) keeps every cross-path mint index in lockstep with no drift / double-write / skip"
    - "Either both gemini+codex outputs exist, OR the unavailable CLI is recorded in the council.json skipped[] (codex has been usage-capped since 392 and will likely skip — record faithfully; gemini + the Claude net satisfy the dual-NET; flag a post-reset codex re-run → 396)"
    - "The subject is byte-frozen throughout (git diff a8b702a7 -- contracts/ empty after the council fan-out)"
  artifacts:
    - path: ".planning/phases/394-legacy-debt/394-01-COUNCIL-PROMPT-V50.md"
      provides: "Neutral council prompt for the v50 slice (LEGACY-01 whale-pass O(1) + box-open record; LEGACY-02 AFSUB pass-gating + OPEN-E re-attest + MINTDIV alignment) charged against a8b702a7 with the by-design exclusions + the authoritative frozen line-cites"
      min_lines: 45
    - path: ".planning/phases/394-legacy-debt/394-01-COUNCIL-NET.md"
      provides: "Council-net capture record (v50 slice): raw output paths, available/skipped, byte-freeze attestation, NET 1 ON RECORD line, codex-skip flag → 396"
      min_lines: 20
  key_links:
    - from: "394-01-COUNCIL-PROMPT-V50.md"
      to: "council.sh --label v50"
      via: "council.sh fan-out to gemini+codex"
      pattern: "council\\.sh.*--label v50"
    - from: "394-01-COUNCIL-NET.md"
      to: "council/v50.council.json"
      via: "manifest available/skipped capture"
      pattern: "v50\\.council\\.json"
---

<objective>
Run NET 1 (the cross-model council) over the v50 legacy-debt slice — the long-deferred v50 contract
surface that never got the Phase 338 internal adversarial sweep: the whale-pass O(1) deferred-claim path
(claimWhalePass + the box-open record retiring the inline ~100-loop mint), AFSUB pass-gating
(validThroughLevel eviction/refresh + the OPEN-E operator-approval re-attestation), and the MINTDIV index
alignment (processed += take) — against the byte-frozen subject `a8b702a7`. Author ONE neutral council
prompt for the v50 slice (LEGACY-01, LEGACY-02), fan it to the gemini+codex CLIs via `council.sh`, capture
the raw output under the phase `council/` dir, and record the council-net capture (available/skipped,
byte-freeze attestation).

Purpose: per AUDIT-V63-PLAN §2, a no-finding verdict for any sweep slice requires BOTH nets on record.
This v50 surface is folded audit debt — it was closed via a MINIMAL CLOSE without the internal sweep, so
the cross-model council (the PRIMARY finder, [[cross-model-led-audits-over-claude-only]]) goes on record
FIRST so the Wave-2 Claude-net + adjudication plan (394-03) can fold the council leads in before any
verdict and author the deferred `audit/FINDINGS-v50.0.md`. The council VERIFIES freeze-safety /
accounting-correctness / O(1)-equivalence / index-alignment — the locked threat weighting puts solvency on
the spine and access/reentrancy as confirmatory; it does NOT re-litigate the by-design AFSUB inclusive
boundary, the OPEN-E operator-approval trust boundary, or the whale-pass economics (§5 design-intent
anchor + the standing by-design rulings).

Output: the COUNCIL-PROMPT-V50 doc, the raw gemini/codex output, the council.json manifest, and
394-01-COUNCIL-NET.md (the capture record). NO contract source is touched — this is read-only over the
subject; the council writes only to its out-dir.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/AUDIT-V63-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

<interfaces>
<!-- council.sh contract (read from .planning/audit-v52/cross-model/bin/council.sh). -->
<!-- Usage: council.sh --out-dir DIR [--schema SCHEMA.json] --label NAME PROMPT_FILE -->
<!-- Produces in DIR: <label>.gemini.txt, <label>.codex.txt, <label>.council.json -->
<!-- (no --schema => .txt extension). council.json shape: -->
<!-- { "label":..., "models":[available], "skipped":[...], "outputs":{model:path} } -->
<!-- It runs gemini + codex in PARALLEL internally; a missing/unauthed/usage-capped CLI is recorded in -->
<!-- skipped[] and is NEVER fatal. gemini + codex CLIs confirmed at ~/.local/bin. codex has been -->
<!-- usage-capped since Phase 392 (skipped[] in 392-01..04, 393-01) — expect a codex skip; record it -->
<!-- faithfully and flag a post-reset codex re-run → 396. gemini + the Wave-2 Claude net satisfy dual-NET. -->

<!-- Council-prompt shape reference (the approved 391-01 / 392-01 / 392-02 slices to MATCH): -->
<!--   .planning/phases/391-rng-spine/391-01-COUNCIL-PROMPT-RNG.md -->
<!--   .planning/phases/392-entropy-and-econ/392-02-COUNCIL-PROMPT-BURNIE.md -->
<!--   header role + "read EXACT frozen source at a8b702a7 via git show a8b702a7:contracts/<File>.sol" -->
<!--   + threat-priority line + trust-boundary framing + KNOWN-BY-DESIGN exclusion list + numbered -->
<!--   break-targets + per-finding output format (FINDING: PROPERTY · reachable CALL SEQUENCE · STATE -->
<!--   VAR + file:line at a8b702a7 · SEVERITY · why protections don't stop it | or VERIFIED SOUND + why). -->

<!-- The 2 v50 reqs to charge (REQUIREMENTS.md): -->
<!--   LEGACY-01 the v50 whale-pass O(1) deferred-claim path + the box-open record; -->
<!--   LEGACY-02 the v50 AFSUB pass-gating (validThroughLevel eviction/refresh + OPEN-E re-attest) + the -->
<!--     MINTDIV index alignment. -->

<!-- Authoritative frozen v50 line-cites (verified at a8b702a7 — supply so the council reads the right -->
<!-- code; the council MUST re-read via git show a8b702a7:..., not trust the cite blindly): -->
<!--   WHALE-PASS O(1) deferred-claim: -->
<!--     DegenerusGame.sol: claimWhalePass(address) @1503 → _claimWhalePassFor @1508 → delegatecall -->
<!--       IDegenerusGameWhaleModule.claimWhalePass.selector @1513. -->
<!--     DegenerusGameWhaleModule.sol: claimWhalePass(address) @991 (the O(1) deferred materialization that -->
<!--       retired the inline ~100-loop mint); the box-open record on pass-bundled deposits -->
<!--       _recordLootboxEntry @371/514 + the LootBoxBuy event @52 (index=the lootbox RNG index the box -->
<!--       queued at @49; presale always false for a pass box @51). -->
<!--   AFSUB PASS-GATING (validThroughLevel eviction/refresh + OPEN-E re-attest): -->
<!--     GameAfkingModule.sol: subscribe gate SUB-02 self-consent OR operatorApprovals[subscriber] -->
<!--       [msg.sender] @314-317; OPENE-04 non-zero non-self fundingSource must operator-approve the -->
<!--       subscriber operatorApprovals[fundingSource][subscriber] @322-328 (checked at subscribe ONLY -->
<!--       @299/337); refresh set s.validThroughLevel = _passHorizonOf(subscriber) @419; the no-pass / -->
<!--       expired-pass eviction guard @427 (exemptSub || validThroughLevel==0 || validThroughLevel<level); -->
<!--       the per-walk inclusive-boundary compare currentLevel > sub.validThroughLevel @1246 → refresh -->
<!--       sub.validThroughLevel = h @1250 (the INTENDED inclusive boundary: keep while currentLevel <= -->
<!--       validThroughLevel, evict at +1 — 1 level more lenient than the strict gates, BY-DESIGN). -->
<!--   MINTDIV index alignment: -->
<!--     DegenerusGameMintModule.sol: the per-player trait write loop @581-681 — processed @581, take = -->
<!--       min(owed, maxT) @634, the writes accounting writesThis @649-651, processed += take @668 (the -->
<!--       v50 alignment: advance the within-player index by the ACTUAL tickets taken, not by a -->
<!--       writes-derived half-count); _processOneTicketEntry caller @881-894 (the cross-path ticket walk -->
<!--       that must stay in lockstep with the trait index). -->
<!--   Green oracle: test/REGRESSION-BASELINE-v63.md = forge 854/0/110 (v50 empirical coverage carried -->
<!--     from Phase 336: whale-pass equivalence + uniform-O(1) + freeze fuzz; AFSUB sweep/evict/refresh + -->
<!--     no-pass-SLOAD oracle; MINTDIV cross-path equality). -->
<!--   Frozen-source read convention: git show a8b702a7:contracts/<File>.sol (ignore working tree). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the neutral council prompt for the v50 legacy-debt slice</name>
  <files>.planning/phases/394-legacy-debt/394-01-COUNCIL-PROMPT-V50.md</files>
  <read_first>
    - .planning/phases/391-rng-spine/391-01-COUNCIL-PROMPT-RNG.md AND .planning/phases/392-entropy-and-econ/392-02-COUNCIL-PROMPT-BURNIE.md (the approved prompt shape to match — header role, threat-priority line, trust-boundary framing, KNOWN-BY-DESIGN list, dedicated numbered prime targets, per-finding output format)
    - .planning/STATE.md (the "⚠ v50.0 + v51.0 AUDIT DEBT → FOLDED INTO v63.0 Phase 394" section — the authoritative charge spec for the v50 surface: what the whale-pass O(1) path / AFSUB pass-gating / MINTDIV alignment are and what the v50 close already mitigated [WHALE-04 freeze at SPEC 334, TST-01/03 at 336, pre-launch no live funds])
    - .planning/AUDIT-V63-PLAN.md §2 (both-nets-on-record), §4 threat weighting (RNG/freeze DOMINANT; solvency SPINE; access/reentrancy/MEV LOW/confirmatory), §5 design-intent anchor (respect the standing by-design rulings)
    - .planning/REQUIREMENTS.md (the exact LEGACY-01 + LEGACY-02 text to charge)
    - the frozen source for the v50 prime sites via git show a8b702a7:contracts/DegenerusGame.sol (claimWhalePass @1503-1513), contracts/modules/DegenerusGameWhaleModule.sol (claimWhalePass @991, _recordLootboxEntry @371/514, LootBoxBuy event @52), contracts/modules/GameAfkingModule.sol (the subscribe consent + OPENE-04 gate @314-337, refresh @419, eviction guard @427, the inclusive-boundary walk @1242-1250), contracts/modules/DegenerusGameMintModule.sol (the trait write loop @581-681 — processed/take/writesThis/processed+=take, the _processOneTicketEntry caller @881-894)
    - the standing by-design rulings to encode as exclusions: [[afking-pass-eviction-inclusive-boundary-intended]] (the inclusive boundary keep-while-<= / evict-at-+1 is INTENDED — do NOT 'fix'); [[open-e-operator-approval-trust-boundary]] (operator-approval IS the trust boundary, BURNIE-funding overload accepted-by-design, no 'tricked into approving' actor); [[degenerette-wwxrp-rtp-by-design]] (whale-pass / WWXRP worthlessness by-design); [[neutral-defensive-audit-vocabulary]]
  </read_first>
  <action>
    Write 394-01-COUNCIL-PROMPT-V50.md, charged NEUTRALLY ("here is what we believe is true about the v50
    contract surface — the whale-pass deferred claim is an O(1) value-equivalent + freeze-safe replacement
    for the retired inline mint; the AFSUB pass-gating refreshes/evicts at the intended inclusive boundary
    with the OPEN-E operator-approval checked at the right point; the MINTDIV index alignment keeps every
    cross-path mint index in lockstep — find where one of these breaks") and instructing the council to read
    the EXACT frozen source at `a8b702a7` via `git show a8b702a7:contracts/<File>.sol` (ignore the working
    tree). Use neutral defensive-engineering vocabulary throughout per [[neutral-defensive-audit-vocabulary]]
    (deferred materialization, value-equivalence, freeze-window, index alignment / lockstep, write-budget,
    consent gate, eviction boundary, refresh horizon).

    The prompt MUST include:
    (a) the threat-priority line from AUDIT-V63-PLAN §4 so the council weights correctly: an RNG/freeze
    break on the whale-pass deferred claim (a word read after it is public, an index/seed that becomes
    player-controllable post-reveal) is DOMINANT; a solvency / value-non-equivalence break (the O(1) path
    materializes MORE or LESS box/pass value than the retired inline mint, or the MINTDIV alignment
    double-writes / skips a ticket so a player mints extra or loses paid traits) is SPINE-level; an
    access-control / reentrancy issue on the consent gate is confirmatory; a pure desirability complaint
    about the documented inclusive boundary or the operator-approval overload is NOT a finding;
    (b) the trust-boundary framing: the whale-pass claim is permissionless-for-beneficiary (claimWhalePass
    delivers value to `player`); the AFSUB subscribe consent gate is the boundary between a self/operator-
    approved subscribe and an unauthorized one; the MINTDIV index walk runs inside the GAME advance/mint
    path;
    (c) a KNOWN-BY-DESIGN exclusion list so the council does not re-flag settled intent: the AFSUB
    pass-eviction INCLUSIVE boundary (keep funded sub while currentLevel <= validThroughLevel, evict at +1 —
    deliberately 1 level more lenient than the strict-`>` gates, [[afking-pass-eviction-inclusive-boundary-
    intended]]) is INTENDED — verify the boundary is CODED as the documented inclusive compare, do NOT flag
    the leniency; the OPEN-E operator-approval IS the trust boundary ([[open-e-operator-approval-trust-
    boundary]]) — the BURNIE-funding overload is accepted-by-design, do NOT model a "tricked into approving"
    actor (the question is whether the gate is CHECKED at the right point, not whether approval can be
    socially engineered); the whale-pass / WWXRP economics are by-design ([[degenerette-wwxrp-rtp-by-design]])
    — verify the O(1) path's value-equivalence and freeze-safety, not the pass's worth; an admin breaking
    their own game at genesis is a non-finding;
    (d) the per-finding output format (FINDING: PROPERTY broken · reachable ordered CALL SEQUENCE [the
    multi-tx claim/subscribe/advance interleaving where ordering matters] · STATE VAR + file:line at
    a8b702a7 · SEVERITY per the threat priority · WHY the existing guard / freeze-snapshot / write-budget /
    consent-check does not stop it | or VERIFIED SOUND: the property and the specific reason it holds — cite
    the freeze snapshot, the value-equivalence arithmetic, the index-alignment invariant, or the consent
    gate — so the adjudicator can confirm).

    State the master thesis the slice rests on, mapped to LEGACY-01..02, as the claims to BREAK, then name
    the three v50 charge items as dedicated numbered break-targets:

    1. LEGACY-01 (the whale-pass O(1) deferred-claim path — value-equivalence + freeze-safety): the v50
       rework replaced the inline ~100-loop pass mint with an O(1) deferred claimWhalePass
       (WhaleModule:991, dispatched via DegenerusGame:1503-1513) that records a box-open entry
       (_recordLootboxEntry @371/514, LootBoxBuy event @52 carrying the lootbox RNG index). Charge the
       council to find (i) any value NON-equivalence — the deferred O(1) claim materializes a DIFFERENT pass
       horizon / stat set / box value than the retired inline mint would have (over- or under-delivery); (ii)
       any FREEZE break — the deferred claim or the box-open record reads a VRF word / index that became
       player-controllable or publicly known AFTER the player's commitment point (the WHALE-04 freeze-safety
       claim proven at SPEC 334 — re-verify in code at the frozen source, do not trust the prior paper
       proof); (iii) any double-claim / replay where claimWhalePass can be re-driven to materialize the pass
       or the box twice. VERIFIED SOUND requires the value-equivalence reasoning + the freeze snapshot cite.

    2. LEGACY-02a (AFSUB pass-gating — refresh/evict at the correct boundary + OPEN-E consent point): the
       validThroughLevel refresh (@419) / eviction guard (@427) / per-walk inclusive-boundary compare
       (@1246-1250) gate a funded subscriber's continuation; the OPEN-E operator-approval (OPENE-04 @322-328,
       checked at subscribe ONLY @299/337) gates a non-self funding source. Charge the council to find (i)
       any path where a subscriber is evicted EARLY (before currentLevel > validThroughLevel) or retained
       LATE (a stale validThroughLevel kept funded past its horizon) — confirm the inclusive boundary is
       CODED as documented (not flagging the intended leniency, but confirming the compare matches the
       documented keep-while-<= / evict-at-+1); (ii) any path where the OPEN-E consent gate can be BYPASSED
       at subscribe (a non-self fundingSource subscribed without operatorApprovals[fundingSource][subscriber],
       or the subscriber-consent SUB-02 check skipped) — NOTE the "later revoke does not stop an active sub"
       and "re-pointing the source IS a re-subscribe which re-checks" are documented intent (@291/299/430),
       so the question is the SUBSCRIBE-time check, not post-subscribe revocation. Confirm the gate is checked
       at the right point or surface a finding.

    3. LEGACY-02b (the MINTDIV index alignment — lockstep, no drift / double-write / skip): the v50
       alignment advances the within-player trait index by `processed += take` (MintModule:668), where take =
       min(owed, maxT) (@634) is the ACTUAL tickets taken this iteration, with the writes-budget accounting
       (writesThis @649-651) kept SEPARATE from the index advance. Charge the council to find (i) any cross-
       path divergence where the trait write index and the ticket walk index (the _processOneTicketEntry
       caller @881-894) fall OUT of lockstep — a ticket processed twice (double-write / extra mint) or
       skipped (paid traits lost) under a write-budget boundary that splits a player's mint across multiple
       advance chunks; (ii) any off-by-one at the per-player boundary reset (processed = 0 @608/617/676) when
       a player completes mid-budget and the next player starts. Confirm every path advances `processed` by
       exactly `take` and the budget split never drops or duplicates a ticket, or surface a finding.

    Do NOT pre-state any verdict in the prompt — the council finds, Claude adjudicates at 394-03. No fenced
    solidity blocks beyond the minimal identifier/selector/line citations the council needs to locate the
    code.
  </action>
  <verify>
    <automated>test -s .planning/phases/394-legacy-debt/394-01-COUNCIL-PROMPT-V50.md && grep -q "a8b702a7" .planning/phases/394-legacy-debt/394-01-COUNCIL-PROMPT-V50.md && for id in LEGACY-01 LEGACY-02; do grep -q "$id" .planning/phases/394-legacy-debt/394-01-COUNCIL-PROMPT-V50.md || { echo "MISSING $id"; exit 1; }; done && grep -Eqi "claimWhalePass|whale.?pass|O\\(1\\)|deferred" .planning/phases/394-legacy-debt/394-01-COUNCIL-PROMPT-V50.md && grep -Eqi "validThroughLevel|OPEN-?E|operator.?approv" .planning/phases/394-legacy-debt/394-01-COUNCIL-PROMPT-V50.md && grep -Eqi "MINTDIV|processed|lockstep|index align" .planning/phases/394-legacy-debt/394-01-COUNCIL-PROMPT-V50.md && grep -Eqi "value.?equivalen|freeze" .planning/phases/394-legacy-debt/394-01-COUNCIL-PROMPT-V50.md</automated>
  </verify>
  <acceptance_criteria>
    394-01-COUNCIL-PROMPT-V50.md exists and is non-empty; instructs reading frozen source at `a8b702a7` via
    git show; names LEGACY-01 + LEGACY-02; charges the three v50 items as dedicated numbered break-targets
    (the whale-pass O(1) value-equivalence + freeze-safety; the AFSUB refresh/evict boundary + the OPEN-E
    consent point; the MINTDIV index alignment lockstep); carries the threat-priority line (freeze DOMINANT /
    value-non-equivalence SPINE / access confirmatory); carries the KNOWN-BY-DESIGN exclusion list (the AFSUB
    inclusive boundary intended, the OPEN-E operator-approval trust boundary, the whale-pass economics by-
    design); uses neutral vocabulary; no verdict is pre-stated.
  </acceptance_criteria>
  <done>
    The neutral v50 council prompt is authored against the frozen subject, covering LEGACY-01 + LEGACY-02
    with the whale-pass O(1)/freeze, AFSUB pass-gating/OPEN-E, and MINTDIV alignment charged hard and the
    by-design exclusions encoded, ready to fan to the council.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the v50 council fan-out and record the council-net capture</name>
  <files>.planning/phases/394-legacy-debt/council/v50.gemini.txt, .planning/phases/394-legacy-debt/council/v50.codex.txt, .planning/phases/394-legacy-debt/council/v50.council.json, .planning/phases/394-legacy-debt/394-01-COUNCIL-NET.md</files>
  <read_first>
    - .planning/audit-v52/cross-model/bin/council.sh (the exact CLI contract + skipped[] behavior)
    - .planning/phases/394-legacy-debt/394-01-COUNCIL-PROMPT-V50.md (Task 1 output)
    - .planning/phases/393-permissionless-composition/393-01-COUNCIL-NET.md (the approved COUNCIL-NET capture record shape to match, including the codex-skip-recorded → 396 flag pattern)
  </read_first>
  <action>
    Run `.planning/audit-v52/cross-model/bin/council.sh --out-dir
    .planning/phases/394-legacy-debt/council --label v50
    .planning/phases/394-legacy-debt/394-01-COUNCIL-PROMPT-V50.md`. council.sh fans gemini+codex in parallel
    internally; this is ONE invocation (one slice) so no extra pacing of multiple invocations is needed
    beyond letting the single fan-out complete per [[pace-runs-to-survive-5h-cap]]. A skipped CLI is recorded
    in the council.json skipped[] and is NOT fatal; codex has been usage-capped since 392 and will likely
    skip — do not treat a codex skip as a failure (gemini + the Wave-2 Claude net satisfy the dual-NET).

    IMMEDIATELY after the fan-out, git-status-verify the subject was not mutated:
    `git diff a8b702a7 -- contracts/` MUST be empty and `git status --porcelain contracts/` MUST be empty
    ([[feedback_verify_writecapable_agents]] — the council writes only to its out-dir, but verify; remove any
    stray file a council model writes outside the out-dir). Do NOT run `hardhat compile --force` (regenerates
    ContractAddresses source).

    Write 394-01-COUNCIL-NET.md recording: the v50.council.json manifest's available[]/skipped[], the raw
    output file paths, a one-line characterization of each model's output (raw, NOT adjudicated —
    adjudication is 394-03), the byte-freeze attestation (`git diff a8b702a7 -- contracts/` empty;
    `git status --porcelain contracts/` empty), and an explicit "NET 1 ON RECORD for v50" line so 394-03 can
    confirm both-nets-on-record before any verdict. If codex (or any CLI) is in skipped[], record WHICH, note
    the slice still has the other model on record (a single available model satisfies "council on record"
    with the skip documented), and FLAG a post-reset codex re-run → 396 (both-unavailable would require a
    re-run and must be surfaced, not silently passed).
  </action>
  <verify>
    <automated>test -f .planning/phases/394-legacy-debt/council/v50.council.json && test -s .planning/phases/394-legacy-debt/394-01-COUNCIL-NET.md && grep -q "NET 1 ON RECORD" .planning/phases/394-legacy-debt/394-01-COUNCIL-NET.md && [ -z "$(git -C /home/zak/Dev/PurgeGame/degenerus-audit diff a8b702a7 -- contracts/)" ]</automated>
  </verify>
  <acceptance_criteria>
    `v50.council.json` exists; either both gemini+codex `.txt` outputs are non-empty OR the unavailable CLI
    is in the council.json skipped[]; 394-01-COUNCIL-NET.md records the available/skipped + raw output paths +
    the byte-freeze attestation + an explicit "NET 1 ON RECORD" line + (if codex skipped) the post-reset
    codex re-run flag → 396; `git diff a8b702a7 -- contracts/` is empty.
  </acceptance_criteria>
  <done>
    NET 1 (the cross-model council) is on record for the v50 slice with raw output captured under the phase
    council/ dir, the subject proven byte-frozen, any codex skip documented + flagged → 396, and the capture
    recorded for the Wave-2 adjudication to fold in.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit-process → subject source | the risk is the AUDIT MUTATING the byte-frozen subject (a Write-capable fan-out edits contracts/), not a new feature threat |
| council CLI → out-dir | the council writes external-model output; risk = an unavailable/capped CLI silently dropping the only net (handled by skipped[] + the 396 re-run flag) or a CLI given write access beyond out-dir |

## STRIDE Threat Register (audit-only posture)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-394-01 | Tampering | byte-frozen subject contracts/ | mitigate | git-status-verify `git diff a8b702a7 -- contracts/` empty after the council fan-out (Task 2); council writes only to its out-dir; remove any stray file |
| T-394-02 | Information disclosure / false-green | the v50 slice with both CLIs unavailable silently treated as "on record" | mitigate | the council.json skipped[] is recorded in 394-01-COUNCIL-NET.md; a codex skip is documented + flagged for a post-reset re-run → 396; both-unavailable is surfaced, never passed silently |
| T-394-03 | Repudiation / scope-drift | the council re-litigating the documented AFSUB inclusive boundary or the OPEN-E operator-approval overload instead of verifying the code, wasting the pass | mitigate | the prompt carries the KNOWN-BY-DESIGN list so the council verifies boundary-as-coded / consent-point / value-equivalence, not the design intent |
| T-394-SC | Tampering | `hardhat compile --force` regenerating ContractAddresses source | accept/avoid | the plan forbids hardhat; only `git show`/read tools touch the subject |
</threat_model>

<verification>
- The council prompt exists, is neutral, charged against `a8b702a7`, covers LEGACY-01 + LEGACY-02, with the
  whale-pass O(1)/freeze + AFSUB pass-gating/OPEN-E + MINTDIV alignment charged hard and the by-design
  exclusions encoded.
- `council.sh` ran for `--label v50`; the council.json manifest exists.
- Both model outputs non-empty OR the missing CLI in skipped[] (codex skip documented + flagged → 396).
- 394-01-COUNCIL-NET.md records the capture + byte-freeze attestation + "NET 1 ON RECORD".
- `git diff a8b702a7 -- contracts/` empty; `git status --porcelain contracts/` empty.
</verification>

<success_criteria>
NET 1 (the cross-model council) is on record for the full v50 legacy-debt slice (LEGACY-01 whale-pass O(1)
+ box-open record; LEGACY-02 AFSUB pass-gating + OPEN-E re-attest + MINTDIV alignment), with raw
gemini/codex output captured under the phase council/ dir and the capture documented, so the Wave-2
Claude-net + adjudication plan (394-03) can fold the council leads in before issuing any per-item verdict
and author the deferred FINDINGS-v50.0.md. The subject is proven byte-frozen throughout.
</success_criteria>

<output>
Create `.planning/phases/394-legacy-debt/394-01-SUMMARY.md` when done
</output>
