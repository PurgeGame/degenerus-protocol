---
phase: 394-legacy-debt
plan: 04
type: execute
wave: 2
depends_on: ["394-02", "394-03"]
files_modified:
  - .planning/phases/394-legacy-debt/394-04-CLAUDE-NET.md
  - .planning/phases/394-legacy-debt/394-FINDINGS-V51.md
  - .planning/phases/394-legacy-debt/394-FINDINGS.md
  - audit/FINDINGS-v51.0.md
autonomous: true
requirements: [LEGACY-03, LEGACY-04, LEGACY-06]
user_setup: []

must_haves:
  truths:
    - "NET 2 (the Claude adversarial net) is on record for the full v51 surface (claimBingo/BingoModule; sDGNRS Pool.Reward rebalance; jackpot final-day Pool.Reward deletion), independent of the council"
    - "Every LEGACY-03 + LEGACY-04 sub-item has an explicit ADJUDICATED verdict (CONFIRMED / REFUTED / BY-DESIGN / MONITOR) with the freeze snapshot / tier-precedence logic / dedup bit / CEI ordering / BPS-sum / clamp / conservation cite (file:line at a8b702a7) that settles it"
    - "claimBingo / BingoModule is settled by a dedicated freeze-safety (the traitBurnTicket read is over a pre-resolved/snapshotted population) + tier-precedence (quadrant-first suppresses the symbol bonus) + per-player (level,quadrant) dedup (the CEI bit set before the external call so no reentrant re-claim) + empty-pool no-op + gameOver-cutoff analysis"
    - "The sDGNRS Pool.Reward rebalance is settled by a split-conservation proof (the 8 pool BPS sum to BPS_DENOM at the frozen source so genesis seeding conserves INITIAL_SUPPLY) + a no-over-draw confirmation (transferFromPool clamps; no stale-BPS consumer)"
    - "The jackpot final-day Pool.Reward deletion is settled by a backing-conservation analysis (no stranded balance, no double-spend across the final-day consolidation + concurrent Reward draws), anchored on POOL-CONSERVATION (FUZZ-05) + an independent attack"
    - "Both nets are on record before the verdict; the skeptic gate is run before any CATASTROPHE/HIGH (the bingo traitBurnTicket freeze read DOMINANT; the Pool.Reward rebalance + final-day deletion SPINE)"
    - "audit/FINDINGS-v51.0.md is authored (LEGACY-06), matching the audit/FINDINGS-v62.0.md format; AND the consolidated 394-FINDINGS.md index ties both slices (v50 + v51) with all 6 LEGACY reqs re-attested + both nets on record for both slices"
    - "Any CONFIRMED finding is DOCUMENTED and ROUTED to a gated fix (USER hand-review, batched) — NOT fixed in this phase; the subject stays byte-frozen"
  artifacts:
    - path: ".planning/phases/394-legacy-debt/394-04-CLAUDE-NET.md"
      provides: "NET 2 adversarial analysis over the v51 surface, independent of the council — the claimBingo freeze + tier-precedence + dedup + CEI analysis, the Pool.Reward split-conservation proof, the jackpot final-day deletion backing-conservation analysis"
      min_lines: 80
    - path: ".planning/phases/394-legacy-debt/394-FINDINGS-V51.md"
      provides: "The v51 per-item adjudication table: every LEGACY-03/04 sub-item with a CONFIRMED/REFUTED/BY-DESIGN/MONITOR verdict, both-nets-on-record, skeptic gate, routing"
      min_lines: 60
    - path: ".planning/phases/394-legacy-debt/394-FINDINGS.md"
      provides: "The consolidated phase-394 index tying the v50 + v51 slices: all 6 LEGACY reqs re-attested, both nets on record for both slices, the two authored audit/FINDINGS-v5x.0.md deliverables linked, routing summary"
      min_lines: 30
    - path: "audit/FINDINGS-v51.0.md"
      provides: "The deferred v51 FINDINGS deliverable (LEGACY-06), matching the FINDINGS-v62.0 format, authored from the dual-net adjudication of the v51 surface"
      min_lines: 60
  key_links:
    - from: "394-FINDINGS-V51.md"
      to: "394-02 council outputs (council/v51.*.txt)"
      via: "both-nets-on-record fold"
      pattern: "council"
    - from: "audit/FINDINGS-v51.0.md"
      to: "LEGACY-03 + LEGACY-04 surface (claimBingo / Pool.Reward / jackpot final-day)"
      via: "the authored deliverable covering the v51 surface"
      pattern: "claimBingo|Pool.Reward|final.?day"
---

<objective>
Run NET 2 (the deep Claude adversarial net) over the SAME v51 surface the council swept (394-02), then
ADJUDICATE every LEGACY-03 + LEGACY-04 sub-item (the claimBingo color-completion / BingoModule 3-tier
selection + tier-precedence + per-player (level,quadrant) dedup + empty-pool no-op + gameOver cutoff +
freeze-safe traitBurnTicket read; the sDGNRS Pool.Reward rebalance AFFILIATE 3500→3000 / REWARD 500→1000;
the jackpot final-day Pool.Reward deletion side-effects) against the byte-frozen subject `a8b702a7`, folding
in the NET-1 council leads. Give claimBingo a dedicated freeze + tier-precedence + dedup + CEI analysis,
prove the Pool.Reward rebalance conserves the genesis split with no over-draw, analyze the jackpot final-day
Pool.Reward deletion for stranded/double-spent backing, run the skeptic gate before presenting anything as
CATASTROPHE/HIGH, write `394-FINDINGS-V51.md` (the slice adjudication), AUTHOR `audit/FINDINGS-v51.0.md`
(LEGACY-06 — the deferred v51 deliverable matching the FINDINGS-v62.0 format), and write the consolidated
`394-FINDINGS.md` index tying both slices (v50 + v51) with all 6 LEGACY reqs re-attested.

Purpose: a no-finding verdict for any sweep slice requires BOTH nets on record (AUDIT-V63-PLAN §2). This
plan puts NET 2 on record for v51, synthesizes both nets, discharges the deferred v51 FINDINGS debt, and
closes the phase index. The bingo traitBurnTicket read is RNG/freeze-adjacent (DOMINANT, §4); the
Pool.Reward rebalance + the jackpot final-day deletion are solvency-spine — so the freeze + the conservation
proofs get dedicated rigorous treatment. The v51 sweep VERIFIES freeze / tier-precedence / dedup-integrity /
pool-conservation; it does NOT re-litigate the by-design no-level-guard or the RTP (§5 + the standing by-
design rulings). Any CONFIRMED finding is DOCUMENTED + ROUTED to a gated USER-hand-review fix, never fixed
here.

Output: 394-04-CLAUDE-NET.md (the independent adversarial analysis), 394-FINDINGS-V51.md (the slice
adjudication), audit/FINDINGS-v51.0.md (the deferred deliverable), and 394-FINDINGS.md (the consolidated
phase index). NO contract source is touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/AUDIT-V63-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/394-legacy-debt/394-02-COUNCIL-NET.md
@.planning/phases/394-legacy-debt/council/v51.gemini.txt
@.planning/phases/394-legacy-debt/council/v51.codex.txt
@.planning/phases/394-legacy-debt/394-FINDINGS-V50.md
@audit/FINDINGS-v62.0.md
@test/REGRESSION-BASELINE-v63.md

<interfaces>
<!-- The 2 v51 reqs + LEGACY-06 to adjudicate/discharge (REQUIREMENTS.md): -->
<!--   LEGACY-03 claimBingo color-completion / BingoModule (3-tier reward selection, per-player -->
<!--     (level,quadrant) dedup, freeze-safety of the post-resolution traitBurnTicket read); -->
<!--   LEGACY-04 sDGNRS Pool.Reward rebalance + jackpot final-day Pool.Reward deletion side-effects; -->
<!--   LEGACY-06 author audit/FINDINGS-v51.0.md (the deferred v51 deliverable). -->
<!-- Authoritative frozen v51 line-cites (verified at a8b702a7 — re-read via git show, do not trust blind): -->
<!--   DegenerusGameBingoModule.sol: 3-tier doc @14-17; CEI doctrine effects-before-interactions @19-22; -->
<!--     AlreadyClaimed (level,quadrant) error @38; BASELINE_REWARD_BPS 0.05% @48; claimBingo(uint24 level, -->
<!--     uint8 symbol, uint32[8] slots) @114; gameOver cutoff @122; quadrant = symbol>>3 @125, qMask @127; -->
<!--     READ-ONLY ownership read over traitBurnTicket[level] @130-136; per-player (level,quadrant) dedup -->
<!--     EFFECT @148; tier cascade bit-sets @153-174 (bf, fq = bf>>32 quadrant mask bits [32:36), quadrant- -->
<!--     first marks BOTH bits @ ~165, symbol-first marks only symbol bit preserving co-resident quadrant -->
<!--     @174); sDGNRS draw poolBalance(Pool.Reward) @188 + transferFromPool(Pool.Reward,…) @189-190 (clamps); -->
<!--     creditFlip @196 (always paid); the second draw helper @240/259. -->
<!--   StakedDegenerusStonk.sol: CREATOR_BPS 2000 @305, WHALE_POOL_BPS 1000 @308, AFFILIATE_POOL_BPS 3000 -->
<!--     @309 (was 3500), LOOTBOX_POOL_BPS 2000 @310, REWARD_POOL_BPS 1000 @311 (was 500), PRESALE_BOX_POOL_ -->
<!--     BPS 1000 @312; genesis seeding @384-408 (rewardAmount @389, poolBalances[Pool.Reward] @408); -->
<!--     poolBalance @509; transferFromPool onlyGame @548; transferBetweenPools onlyGame @579. -->
<!--   Pool.Reward consumers (conservation surface): BingoModule @188-190; DegeneretteModule @1221/1230; -->
<!--     DegenerusGame.sol @466/472. -->
<!--   jackpot final-day deletion: AdvanceModule _consolidatePoolsAndRewardJackpots @833 (called @530; -->
<!--     RewardJackpotsSettled @1015; affiliate-top DGNRS dgnrsReward = poolBalance*AFFILIATE_POOL_REWARD_BPS -->
<!--     @757-762; dispatch table @1189); JackpotModule final-day solo-bucket whale pass + DGNRS @1047/1160. -->
<!--   Green oracle: test/REGRESSION-BASELINE-v63.md = forge 854/0/110 (v51 coverage carried from Phase 341: -->
<!--     per-tier rewards / dedup / empty-pool / jackpot-final-day regression; freeze + tier-precedence proven -->
<!--     at SPEC 339; POOL-CONSERVATION FUZZ-05 attests the 4-pool total fully backed + transfers conserve). -->
<!--   FINDINGS-v62.0 format to MATCH (audit/FINDINGS-v62.0.md): same structure as 394-03 uses for v50. -->
<!--   Frozen-source read convention: git show a8b702a7:contracts/<File>.sol (ignore working tree). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NET 2 adversarial v51 analysis — claimBingo freeze + tier-precedence + dedup + CEI + Pool.Reward split-conservation + jackpot final-day deletion backing-conservation vs frozen source</name>
  <files>.planning/phases/394-legacy-debt/394-04-CLAUDE-NET.md</files>
  <read_first>
    - .planning/STATE.md (the v51 charge spec section — the claimBingo/BingoModule internals + the Pool.Reward rebalance + the jackpot final-day deletion + what the v51 close already mitigated [SPEC 339 freeze + tier-precedence; TST 341 per-tier / dedup / empty-pool / jackpot-final-day])
    - .planning/AUDIT-V63-PLAN.md §4 threat weighting (the bingo traitBurnTicket read DOMINANT; the Pool.Reward rebalance + final-day deletion SPINE; access/reentrancy confirmatory) + §5 design-intent anchor
    - .planning/REQUIREMENTS.md (the exact LEGACY-03 + LEGACY-04 + LEGACY-06 text)
    - the frozen source via git show a8b702a7:contracts/modules/DegenerusGameBingoModule.sol (the 3-tier doc @14-17, the CEI doctrine @19-22, claimBingo @114-196, the dedup @148, the tier cascade @153-174, the Pool.Reward draw @188-190, creditFlip @196, the helper @240/259), contracts/StakedDegenerusStonk.sol (the 8 pool BPS @305-312, the genesis seeding @384-408, transferFromPool @548, transferBetweenPools @579), contracts/modules/DegenerusGameAdvanceModule.sol (_consolidatePoolsAndRewardJackpots @833 called @530, the affiliate DGNRS reward @757-762, RewardJackpotsSettled @1015), contracts/modules/DegenerusGameJackpotModule.sol (the final-day solo-bucket @1047/1160), contracts/modules/DegenerusGameDegeneretteModule.sol (the Reward draw @1221/1230), contracts/DegenerusGame.sol (the Reward consume @466/472)
    - the standing by-design rulings: [[claimbingo-no-level-guard]], [[degenerette-wwxrp-rtp-by-design]], [[intended-game-mechanics-not-findings]]
    - .planning/phases/394-legacy-debt/394-02-COUNCIL-NET.md + council/v51.*.txt (NET 1 leads to consider — but attack INDEPENDENTLY first, then compare)
  </read_first>
  <action>
    First confirm `git diff a8b702a7 -- contracts/` is empty (read-only over the frozen subject). Read all
    source via `git show a8b702a7:contracts/<File>.sol` — ignore the working tree.

    Run the Claude adversarial net INDEPENDENTLY of the council (do not read the council outputs as the
    starting point — attack the freeze / tier-precedence / dedup / conservation claims first, then fold the
    council leads in at the end of this task). For each v51 item, attempt to BREAK the claim with a concrete
    reachable call sequence (the multi-claim / reentrancy / final-day-advance interleaving where ordering
    matters), then record the result.

    v51 adjudication (LEGACY-03 + LEGACY-04 sub-items):

    - LEGACY-03a (claimBingo FREEZE-safety — DEDICATED, re-verify SPEC-339 in code): claimBingo
      (@BingoModule:114) reads traitBurnTicket[level] (@130-136) READ-ONLY. Trace BACKWARD: confirm the trait
      population it reads is a PRE-RESOLVED / snapshotted set (the traits resolved to currentLevel+5 before
      the player commits, [[claimbingo-no-level-guard]]) so the read is over a frozen population, NOT a live
      read a player can steer after the level's word is public. Do NOT trust the SPEC-339 paper proof —
      re-verify the population's provenance at the frozen source. State the freeze snapshot that settles it,
      or surface a finding.

    - LEGACY-03b (tier-precedence + dedup + CEI — DEDICATED): confirm (i) TIER-PRECEDENCE — a quadrant-first
      claim marks BOTH bits and SUPPRESSES the symbol bonus, a symbol-first marks only the symbol bit
      preserving the co-resident quadrant mask (the bf / fq = bf>>32 logic @153-174); attack any path where a
      player double-collects the symbol + quadrant tier (precedence computed wrong); (ii) per-player
      (level,quadrant) DEDUP — the AlreadyClaimed guard (@38) + the dedup EFFECT bit (@148) prevent a second
      claim for the same (level,quadrant); attack a reentrant re-claim (a reentrant creditFlip @196 /
      transferFromPool @189-190 re-entering claimBingo before the dedup/tier bits are durable — verify the CEI
      ordering sets the bits @148/153-174 BEFORE the external calls @189-196, so reentrancy hits a set bit and
      reverts); (iii) EMPTY-POOL no-op — the transferFromPool clamp (@189) returns 0 on an empty Reward pool
      and creditFlip stays safe (a clean no-op, not a revert that strands the dedup bit); (iv) gameOver cutoff
      (@122) blocks a post-game claim. Settle each with the cite, or surface.

    - LEGACY-04a (sDGNRS Pool.Reward rebalance — DEDICATED split-conservation): the v51 rebalance moved
      AFFILIATE 3500→3000 (@309) and REWARD 500→1000 (@311). SUM the 8 pool BPS at the frozen source
      (CREATOR 2000, WHALE 1000, AFFILIATE 3000, LOOTBOX 2000, REWARD 1000, PRESALE_BOX 1000, + the remaining
      splits @305-312) and confirm == BPS_DENOM so the genesis seeding (@384-408) conserves INITIAL_SUPPLY
      exactly (no over-/under-allocation). Attack: any transferFromPool (@548) over-draw past the now-larger
      Reward / now-smaller Affiliate pool balance; any transferBetweenPools (@579) underflow; any downstream
      consumer that hard-coded the OLD 500/3500 split (a Reward draw computed against a stale BPS). Settle the
      conservation + clamp, or surface.

    - LEGACY-04b (jackpot final-day Pool.Reward deletion — DEDICATED backing-conservation): the final-day
      reward consolidation (_consolidatePoolsAndRewardJackpots @AdvanceModule:833, called @530), the
      affiliate-top DGNRS reward (poolBalance*AFFILIATE_POOL_REWARD_BPS @757-762), and the jackpot-phase
      final-day solo-bucket whale pass + DGNRS (@JackpotModule:1047/1160) touch Pool.Reward on the final day.
      Attack: any STRANDED backing (a Reward balance deleted/zeroed without the value leaving, or vice versa);
      any DOUBLE-SPEND (the same Reward backing consumed by both the final-day consolidation AND a concurrent
      claimBingo / Degenerette Reward draw in the same advance window); any ORDERING hazard mis-sizing a draw.
      Anchor on the POOL-CONSERVATION invariant (FUZZ-05) but attack independently for any final-day path its
      action set does not cover. Settle the backing-conservation, or surface.

    For each item: attempt the break, record PROPERTY · attack/call-sequence tried · STATE VAR + file:line at
    a8b702a7 · the freeze/tier-precedence/dedup/CEI/BPS-sum/clamp/conservation reasoning + cite that settles
    it · a provisional verdict (CONFIRMED / REFUTED / BY-DESIGN / MONITOR). Note where the green baseline
    already proves a property (the Phase-341 per-tier / dedup / empty-pool / jackpot-final-day regression, the
    POOL-CONSERVATION FUZZ-05 net in REGRESSION-BASELINE-v63). At the END of the task, read the council
    outputs (394-02 council/v51.*.txt) and note for each item whether the council raised a convergent/
    divergent lead — fold any council-only lead in as an additional row to adjudicate. Write all of this to
    394-04-CLAUDE-NET.md.

    Re-verify `git diff a8b702a7 -- contracts/` empty at the end (read-only). Do NOT run hardhat. Use neutral
    defensive-engineering vocabulary throughout.
  </action>
  <verify>
    <automated>test -s .planning/phases/394-legacy-debt/394-04-CLAUDE-NET.md && for id in LEGACY-03 LEGACY-04; do grep -q "$id" .planning/phases/394-legacy-debt/394-04-CLAUDE-NET.md || { echo "MISSING $id"; exit 1; }; done && grep -Eqi "claimBingo|traitBurnTicket|freeze" .planning/phases/394-legacy-debt/394-04-CLAUDE-NET.md && grep -Eqi "tier.?preceden|quadrant|dedup|CEI" .planning/phases/394-legacy-debt/394-04-CLAUDE-NET.md && grep -Eqi "Pool.Reward|BPS|conserv|3000|1000" .planning/phases/394-legacy-debt/394-04-CLAUDE-NET.md && grep -Eqi "final.?day|consolidat|strand|double.?spend" .planning/phases/394-legacy-debt/394-04-CLAUDE-NET.md && [ -z "$(git -C /home/zak/Dev/PurgeGame/degenerus-audit diff a8b702a7 -- contracts/)" ]</automated>
  </verify>
  <acceptance_criteria>
    394-04-CLAUDE-NET.md contains an independent adversarial analysis with an attack attempt + a provisional
    verdict for EACH v51 sub-item (claimBingo freeze; tier-precedence + dedup + CEI + empty-pool + gameOver;
    Pool.Reward split-conservation; jackpot final-day deletion backing-conservation); the claimBingo freeze
    cites the snapshot provenance (not the prior paper proof); the tier-precedence/dedup/CEI cites the bit-set
    ordering; the Pool.Reward sums the 8 BPS == BPS_DENOM + cites the clamp; the final-day deletion carries the
    backing-conservation analysis anchored on FUZZ-05; the council leads are folded in at the end;
    `git diff a8b702a7 -- contracts/` is empty.
  </acceptance_criteria>
  <done>
    NET 2 (the Claude adversarial net) is on record for the v51 slice, independent of the council, with a
    per-item attack + provisional verdict, the claimBingo freeze + tier-precedence + dedup + CEI analyzed, the
    Pool.Reward split-conservation proven, the jackpot final-day deletion backing-conservation analyzed, and
    the council leads folded in.
  </done>
</task>

<task type="auto">
  <name>Task 2: Synthesize both nets, run the skeptic gate, write 394-FINDINGS-V51.md + author audit/FINDINGS-v51.0.md + write the consolidated 394-FINDINGS.md phase index</name>
  <files>.planning/phases/394-legacy-debt/394-FINDINGS-V51.md, audit/FINDINGS-v51.0.md, .planning/phases/394-legacy-debt/394-FINDINGS.md</files>
  <read_first>
    - .planning/phases/394-legacy-debt/394-04-CLAUDE-NET.md (Task 1 — NET 2 provisional verdicts)
    - .planning/phases/394-legacy-debt/394-02-COUNCIL-NET.md + council/v51.*.txt (NET 1 leads)
    - .planning/phases/394-legacy-debt/394-FINDINGS-V50.md (the v50 slice adjudication — to tie into the consolidated index)
    - audit/FINDINGS-v62.0.md (the canonical FINDINGS deliverable FORMAT to match)
    - .planning/phases/392-entropy-and-econ/392-FINDINGS.md (the approved consolidated phase-index shape tying two slices)
    - .planning/AUDIT-V63-PLAN.md §2 (both-nets-on-record) + §4 (threat weighting) + §5 (design-intent anchor) + §8 (process rules: audit-only, routing)
    - .planning/REQUIREMENTS.md (the exact LEGACY-03 + LEGACY-04 + LEGACY-06 text + all 6 LEGACY reqs to re-attest in the index)
    - the skeptic-gate posture: [[feedback_skeptic_pass_before_catastrophe]]
  </read_first>
  <action>
    Synthesize NET 1 (council) and NET 2 (Claude) into THREE deliverables: the v51 slice adjudication
    394-FINDINGS-V51.md, the deferred v51 deliverable audit/FINDINGS-v51.0.md (LEGACY-06), and the
    consolidated phase index 394-FINDINGS.md.

    (A) 394-FINDINGS-V51.md — the v51 slice adjudication. Same structure as 394-FINDINGS-V50.md:
    (1) Header (subject `a8b702a7` byte-frozen; baseline REGRESSION-BASELINE-v63 854/0/110; method COUNCIL +
    CLAUDE both; AUDIT-ONLY posture; threat weighting [the bingo traitBurnTicket read DOMINANT / the
    Pool.Reward rebalance + final-day deletion SPINE / access confirmatory]; design-intent anchor [claimBingo
    no-level-guard + the RTP by-design — verify, do not re-litigate]).
    (2) Both-nets-on-record attestation (NET 1 per 394-02-COUNCIL-NET.md incl. codex skip + 396 flag; NET 2
    per 394-04-CLAUDE-NET). Every no-finding verdict cites both nets.
    (3) The per-item adjudication table — ONE row per v51 sub-item (claimBingo freeze; tier-precedence + dedup
    + CEI + empty-pool + gameOver; Pool.Reward split-conservation; jackpot final-day deletion), covering
    LEGACY-03 + LEGACY-04. Columns: ITEM · claims · NET-1 · NET-2 · ADJUDICATED VERDICT · settling cite
    (file:line at a8b702a7 or green-baseline anchor). Weight per §4; apply the §5 anchor.
    (4) Skeptic gate before any CATASTROPHE/HIGH (the dual-gate filter), result recorded. If nothing reaches
    HIGH, state it with the gate applied to the value-bearing items (the freeze read, the conservation, the
    final-day deletion). If a real break surfaces, run the full gate + weight it.
    (5) Routing: any CONFIRMED finding → a CONTRACT-CHANGE-NEEDED / ROUTED block (gated USER-hand-review,
    batched, never auto-committed). If zero CONFIRMED, state "0 CONFIRMED — document-only; LEGACY-03..04
    attested at a8b702a7" + any INFO/MONITOR.
    (6) Re-attestation: each of LEGACY-03 + LEGACY-04 marked attested-or-finding.

    (B) audit/FINDINGS-v51.0.md — the deferred v51 deliverable (LEGACY-06). AUTHOR it MATCHING the
    FINDINGS-v62.0 format (mirror what 394-03 did for v50):
    - Header: title "FINDINGS — v51.0 (claimBingo Color-Completion + BingoModule + sDGNRS Pool.Reward
      Rebalance + Jackpot Final-Day Deletion — deferred deliverable, swept under v63.0 Phase 394 LEGACY-DEBT,
      CROSS-MODEL-LED)"; frozen subject SHA `a8b702a7` (the v51 surface as it stands in the byte-frozen v63
      subject; NOTE it was not separately re-frozen); the v51 close history (closed minimally 2026-05-28, the
      internal sweep + external second-source deferred → folded into v63 Phase 394); date; method = COUNCIL +
      CLAUDE both; the council pipeline (codex skip documented if applicable).
    - Executive summary + a disposition table reflecting the ACTUAL v51 adjudication outcome.
    - The v51 surface coverage stated: claimBingo/BingoModule (3-tier + tier-precedence + dedup + empty-pool +
      gameOver + freeze-safe traitBurnTicket read); the sDGNRS Pool.Reward rebalance (AFFILIATE 3000 / REWARD
      1000); the jackpot final-day Pool.Reward deletion — each with its verdict + settling cite.
    - Actionable findings (if any) in the v62 per-finding shape; lower-severity; refuted/by-design (the
      claimBingo no-level-guard, the RTP — recorded so not re-flagged); the prior mitigations (SPEC 339 freeze
      + tier-precedence, TST 341 coverage, the POOL-CONSERVATION net); any routed item.
    - Factual + neutral; do NOT chmod 444 (the terminal v63 close handles sealing).

    (C) 394-FINDINGS.md — the consolidated phase-394 index. Tie the v50 + v51 slices: a short header; a
    both-nets-on-record line for BOTH slices (gemini + Claude on record for v50 and v51; codex skip +
    post-reset re-run flagged → 396 if applicable); a re-attestation table of ALL 6 LEGACY reqs (LEGACY-01..06)
    with each marked attested-or-finding and pointing to its slice adjudication (394-FINDINGS-V50 / -V51) + the
    authored deliverable (audit/FINDINGS-v50.0.md / -v51.0.md); a routing summary (any CONFIRMED findings
    across both slices routed to the gated USER-hand-review boundary, batched; carry the codex second-source →
    396); a closing line that the two deferred FINDINGS deliverables are discharged (LEGACY-05 + LEGACY-06).

    Re-confirm `git diff a8b702a7 -- contracts/` empty at the end. Neutral vocabulary throughout. No contract
    source is modified.
  </action>
  <verify>
    <automated>test -s .planning/phases/394-legacy-debt/394-FINDINGS-V51.md && test -s audit/FINDINGS-v51.0.md && test -s .planning/phases/394-legacy-debt/394-FINDINGS.md && for id in LEGACY-03 LEGACY-04; do grep -q "$id" .planning/phases/394-legacy-debt/394-FINDINGS-V51.md || { echo "MISSING $id in slice"; exit 1; }; done && grep -Eqi "both.nets|NET 1.*NET 2|both nets on record" .planning/phases/394-legacy-debt/394-FINDINGS-V51.md && grep -Eqi "skeptic" .planning/phases/394-legacy-debt/394-FINDINGS-V51.md && grep -q "a8b702a7" audit/FINDINGS-v51.0.md && grep -Eqi "claimBingo|BingoModule" audit/FINDINGS-v51.0.md && grep -Eqi "Pool.Reward|rebalance" audit/FINDINGS-v51.0.md && grep -Eqi "final.?day|jackpot" audit/FINDINGS-v51.0.md && for id in LEGACY-01 LEGACY-02 LEGACY-03 LEGACY-04 LEGACY-05 LEGACY-06; do grep -q "$id" .planning/phases/394-legacy-debt/394-FINDINGS.md || { echo "MISSING $id in index"; exit 1; }; done && [ -z "$(git -C /home/zak/Dev/PurgeGame/degenerus-audit diff a8b702a7 -- contracts/)" ]</automated>
  </verify>
  <acceptance_criteria>
    394-FINDINGS-V51.md contains: a both-nets-on-record attestation; a per-item verdict row for EACH v51
    sub-item (covering LEGACY-03 + LEGACY-04) with a verdict + settling cite; the skeptic gate run + recorded;
    any CONFIRMED finding documented + ROUTED. audit/FINDINGS-v51.0.md is authored matching the FINDINGS-v62.0
    format: frozen subject SHA `a8b702a7` + the v51 close history + date + method header; executive summary +
    disposition table; the v51 surface coverage (claimBingo / Pool.Reward rebalance / jackpot final-day) each
    with a verdict; the refuted/by-design section (the no-level-guard + RTP recorded). 394-FINDINGS.md ties
    both slices with ALL 6 LEGACY reqs (LEGACY-01..06) re-attested + both nets on record for both slices + the
    routing summary + the two deliverables linked. `git diff a8b702a7 -- contracts/` is empty.
  </acceptance_criteria>
  <done>
    The v51 slice is adjudicated with both nets on record, the skeptic gate applied, every LEGACY-03/04
    sub-item carrying an explicit verdict, the claimBingo freeze + tier-precedence + dedup + CEI analyzed, the
    Pool.Reward split-conservation proven, the jackpot final-day deletion backing-conservation analyzed, the
    deferred audit/FINDINGS-v51.0.md authored (LEGACY-06), and the consolidated 394-FINDINGS.md index tying
    both slices with all 6 LEGACY reqs re-attested — the subject byte-frozen throughout.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit-process → byte-frozen subject | the risk is a MISSED freeze break / tier-precedence-or-dedup defect / pool-conservation break, or a FALSE-GREEN adjudication; or the audit mutating the frozen subject — not a new feature threat |
| single-net verdict | the risk is issuing a no-finding verdict with only one net on record (the v60/v62 lesson: Claude-only missed council findings) |

## STRIDE Threat Register (audit-only posture)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-394-11 | Tampering | byte-frozen subject contracts/ | mitigate | git-status-verify `git diff a8b702a7 -- contracts/` empty at the end of each task; all source read via `git show a8b702a7:` |
| T-394-12 | Repudiation / false-green | a REFUTED/BY-DESIGN v51 verdict issued without both nets, or the Pool.Reward conservation attested without the BPS sum, or the claimBingo freeze/dedup waved without a CEI proof | mitigate | the both-nets-on-record table; every no-finding verdict cites NET 1 + NET 2; the Pool.Reward conservation requires the 8-BPS-sum; the claimBingo dedup requires the CEI bit-ordering cite |
| T-394-13 | Elevation / under-weighting | a CONFIRMED freeze / dedup / conservation break under-weighted, or a HIGH tagged without the skeptic filter | mitigate | the skeptic gate runs before any CATASTROPHE/HIGH; the bingo freeze read weighted DOMINANT, the Pool.Reward rebalance + final-day deletion SPINE per §4 |
| T-394-14 | Tampering | a CONFIRMED finding silently fixed in-phase | mitigate | audit-only posture: CONFIRMED findings are DOCUMENTED + ROUTED to a gated USER-hand-review boundary, never fixed here |
</threat_model>

<verification>
- NET 2 (Claude) on record, independent, per-item attack + verdict over the v51 sub-items.
- Both nets on record before the verdict; the both-nets table present in 394-FINDINGS-V51.md.
- The claimBingo freeze cites the snapshot; the tier-precedence/dedup/CEI cites the bit-ordering; the
  Pool.Reward cites the 8-BPS-sum + clamp; the final-day deletion cites the backing-conservation.
- Skeptic gate run before any CATASTROPHE/HIGH; result recorded.
- audit/FINDINGS-v51.0.md authored matching the FINDINGS-v62.0 format, covering the v51 surface.
- 394-FINDINGS.md ties both slices; all 6 LEGACY reqs re-attested; both nets on record for both slices.
- Any CONFIRMED finding documented + ROUTED (not fixed); subject byte-frozen.
- `git diff a8b702a7 -- contracts/` empty.
</verification>

<success_criteria>
The v51 legacy-debt slice (LEGACY-03 + LEGACY-04) is adjudicated against the byte-frozen subject `a8b702a7`
with BOTH nets on record, the skeptic gate applied, and every sub-item carrying an explicit
CONFIRMED/REFUTED/BY-DESIGN/MONITOR verdict backed by a freeze snapshot, tier-precedence logic, dedup bit,
CEI ordering, BPS-sum, clamp, conservation argument, or source-cite. claimBingo's freeze + tier-precedence +
dedup + CEI is verified in code; the Pool.Reward rebalance split-conservation is proven; the jackpot
final-day deletion backing-conservation is analyzed. The deferred audit/FINDINGS-v51.0.md is authored
(LEGACY-06) matching the FINDINGS-v62.0 format, and the consolidated 394-FINDINGS.md index ties both slices
with all 6 LEGACY reqs re-attested + both nets on record for both. Any CONFIRMED finding is documented +
routed to a gated fix; the subject stays byte-frozen.
</success_criteria>

<output>
Create `.planning/phases/394-legacy-debt/394-04-SUMMARY.md` when done
</output>
