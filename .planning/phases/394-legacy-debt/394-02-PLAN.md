---
phase: 394-legacy-debt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/394-legacy-debt/394-02-COUNCIL-PROMPT-V51.md
  - .planning/phases/394-legacy-debt/council/v51.gemini.txt
  - .planning/phases/394-legacy-debt/council/v51.codex.txt
  - .planning/phases/394-legacy-debt/council/v51.council.json
  - .planning/phases/394-legacy-debt/394-02-COUNCIL-NET.md
autonomous: true
requirements: [LEGACY-03, LEGACY-04]
user_setup: []

must_haves:
  truths:
    - "The cross-model council (NET 1) is on record for the full v51 legacy-debt slice — the claimBingo color-completion entrypoint / DegenerusGameBingoModule.sol (3-tier reward selection + tier-precedence suppression, per-player (level,quadrant) dedup, transferFromPool(Pool.Reward,…) + coinflip.creditFlip draws, empty-pool no-op, gameOver cutoff, freeze-safety of the post-resolution traitBurnTicket read), the sDGNRS Pool.Reward rebalance (AFFILIATE 3500→3000 / REWARD 500→1000), and the jackpot final-day Pool.Reward deletion side-effects"
    - "The council prompt is charged neutrally against the frozen subject a8b702a7, with the by-design rulings explicitly excluded so the council does not waste passes on settled intent: claimBingo has NO level guard BY-DESIGN (traits pre-resolve to currentLevel+5; the 8-color ownership check self-gates; signature uint24 level); the whale-pass / WWXRP / Degenerette RTP economics are by-design — the council VERIFIES freeze-safety / accounting-correctness / dedup-integrity / tier-precedence / pool-conservation, NOT the design intent"
    - "The v51 charge is encoded as three dedicated named break-targets: (1) claimBingo / BingoModule 3-tier selection + tier-precedence suppression + per-player (level,quadrant) dedup + empty-pool no-op + gameOver cutoff + the freeze-safety of the post-resolution traitBurnTicket read; (2) the sDGNRS Pool.Reward rebalance (AFFILIATE 3000 / REWARD 1000 bps) — verify the new split conserves total supply and no over-draw; (3) the jackpot final-day Pool.Reward deletion / draw side-effects don't strand or double-spend Reward backing"
    - "The RNG/freeze threat priority is encoded HARD: the claimBingo traitBurnTicket read happens POST-resolution (CEI: effects/bit-sets precede the transferFromPool/creditFlip interactions) — the council must verify the read is freeze-safe (the trait population is snapshotted before the word is public) and the bingo draw entropy (if any) is not grindable"
    - "Either both gemini+codex outputs exist, OR the unavailable CLI is recorded in the council.json skipped[] (codex usage-capped since 392 — record faithfully; gemini + the Claude net satisfy the dual-NET; flag a post-reset codex re-run → 396)"
    - "The subject is byte-frozen throughout (git diff a8b702a7 -- contracts/ empty after the council fan-out)"
  artifacts:
    - path: ".planning/phases/394-legacy-debt/394-02-COUNCIL-PROMPT-V51.md"
      provides: "Neutral council prompt for the v51 slice (LEGACY-03 claimBingo/BingoModule; LEGACY-04 sDGNRS Pool.Reward rebalance + jackpot final-day deletion) charged against a8b702a7 with the by-design exclusions + the authoritative frozen line-cites"
      min_lines: 45
    - path: ".planning/phases/394-legacy-debt/394-02-COUNCIL-NET.md"
      provides: "Council-net capture record (v51 slice): raw output paths, available/skipped, byte-freeze attestation, NET 1 ON RECORD line, codex-skip flag → 396"
      min_lines: 20
  key_links:
    - from: "394-02-COUNCIL-PROMPT-V51.md"
      to: "council.sh --label v51"
      via: "council.sh fan-out to gemini+codex"
      pattern: "council\\.sh.*--label v51"
    - from: "394-02-COUNCIL-NET.md"
      to: "council/v51.council.json"
      via: "manifest available/skipped capture"
      pattern: "v51\\.council\\.json"
---

<objective>
Run NET 1 (the cross-model council) over the v51 legacy-debt slice — the long-deferred v51 contract surface
that never got the Phase 339/341 internal adversarial sweep's external second-source: the claimBingo
color-completion entrypoint / DegenerusGameBingoModule.sol (3-tier reward selection + tier-precedence
suppression, per-player (level,quadrant) dedup, transferFromPool(Pool.Reward,…) + coinflip.creditFlip
draws, empty-pool no-op, gameOver cutoff, freeze-safety of the post-resolution traitBurnTicket read), the
sDGNRS Pool.Reward rebalance (AFFILIATE 3500→3000 / REWARD 500→1000), and the jackpot final-day Pool.Reward
deletion side-effects — against the byte-frozen subject `a8b702a7`. Author ONE neutral council prompt for
the v51 slice (LEGACY-03, LEGACY-04), fan it to the gemini+codex CLIs via `council.sh`, capture the raw
output under the phase `council/` dir, and record the council-net capture (available/skipped, byte-freeze
attestation).

Purpose: per AUDIT-V63-PLAN §2, a no-finding verdict for any sweep slice requires BOTH nets on record.
This v51 surface is folded audit debt — closed minimally without the external second-source — so the
cross-model council (the PRIMARY finder, [[cross-model-led-audits-over-claude-only]]) goes on record FIRST
so the Wave-2 Claude-net + adjudication plan (394-04) can fold the council leads in before any verdict and
author the deferred `audit/FINDINGS-v51.0.md`. The bingo traitBurnTicket read is RNG/freeze-adjacent
(DOMINANT, §4); the Pool.Reward rebalance + the jackpot final-day deletion are solvency-spine. The council
VERIFIES freeze-safety / accounting-correctness / dedup-integrity / tier-precedence / pool-conservation; it
does NOT re-litigate the by-design no-level-guard, the Degenerette RTP, or the whale-pass economics (§5
design-intent anchor + the standing by-design rulings).

Output: the COUNCIL-PROMPT-V51 doc, the raw gemini/codex output, the council.json manifest, and
394-02-COUNCIL-NET.md (the capture record). NO contract source is touched — this is read-only over the
subject; the council writes only to its out-dir.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/AUDIT-V63-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

<interfaces>
<!-- council.sh contract (read from .planning/audit-v52/cross-model/bin/council.sh). -->
<!-- Usage: council.sh --out-dir DIR [--schema SCHEMA.json] --label NAME PROMPT_FILE -->
<!-- Produces in DIR: <label>.gemini.txt, <label>.codex.txt, <label>.council.json -->
<!-- (no --schema => .txt extension). council.json shape: -->
<!-- { "label":..., "models":[available], "skipped":[...], "outputs":{model:path} } -->
<!-- gemini + codex run in PARALLEL internally; a missing/unauthed/usage-capped CLI is recorded in -->
<!-- skipped[] and is NEVER fatal. codex usage-capped since Phase 392 — expect a codex skip; record it -->
<!-- faithfully and flag a post-reset codex re-run → 396. gemini + the Wave-2 Claude net satisfy dual-NET. -->

<!-- Council-prompt shape reference (the approved 391-01 / 392-01 / 392-02 slices to MATCH): -->
<!--   .planning/phases/391-rng-spine/391-01-COUNCIL-PROMPT-RNG.md -->
<!--   .planning/phases/392-entropy-and-econ/392-02-COUNCIL-PROMPT-BURNIE.md -->
<!--   header role + "read EXACT frozen source at a8b702a7 via git show a8b702a7:contracts/<File>.sol" -->
<!--   + threat-priority line + trust-boundary framing + KNOWN-BY-DESIGN exclusion list + numbered -->
<!--   break-targets + per-finding output format (FINDING / VERIFIED SOUND with the settling cite). -->

<!-- The 2 v51 reqs to charge (REQUIREMENTS.md): -->
<!--   LEGACY-03 the v51 claimBingo color-completion / BingoModule (3-tier reward selection, per-player -->
<!--     (level,quadrant) dedup, freeze-safety of the post-resolution traitBurnTicket read); -->
<!--   LEGACY-04 the v51 sDGNRS Pool.Reward rebalance + the jackpot final-day Pool.Reward deletion side- -->
<!--     effects. -->

<!-- Authoritative frozen v51 line-cites (verified at a8b702a7 — supply so the council reads the right -->
<!-- code; the council MUST re-read via git show a8b702a7:..., not trust the cite blindly): -->
<!--   claimBingo / DegenerusGameBingoModule.sol: -->
<!--     the 3-tier doc-table @14-17 (regular 0.05% Pool.Reward + 1_000e18 BURNIE / symbol-first / -->
<!--       quadrant-first 0.5% + 5_000e18 BURNIE, suppresses symbol bonus); the CEI doctrine @19-22 -->
<!--       (claimBingo is a strict READ-ONLY consumer of traitBurnTicket; effects [bit sets] precede -->
<!--       interactions [transferFromPool / creditFlip]); the AlreadyClaimed-this-(level,quadrant) error @38; -->
<!--       BASELINE_REWARD_BPS 0.05% @48; claimBingo(uint24 level, uint8 symbol, uint32[8] slots) @114; the -->
<!--       gameOver cutoff @122 (if gameOver revert E()); quadrant = symbol>>3 @125, qMask = 1<<quadrant -->
<!--       @127; the READ-ONLY ownership read over traitBurnTicket[level] @130-136 (traitId = -->
<!--       (quadrant<<6)|(c<<3)|symInQ, index-guarded); the per-player (level,quadrant) dedup EFFECT @148; -->
<!--       the tier cascade EFFECTS bit-sets @153-174 (the bingoFlags bf, fq = bf>>32 quadrant mask in bits -->
<!--       [32:36), quadrant-first marks BOTH bits, symbol-first marks only the symbol bit preserving the -->
<!--       co-resident quadrant mask); the sDGNRS draw poolBalance(Pool.Reward) @188 + transferFromPool -->
<!--       (Pool.Reward,…) @189-190 (clamps to available); the BURNIE flip credit creditFlip @196 (always -->
<!--       paid, tier amount non-zero); the second draw helper @240/259 (transferFromPool + creditFlip). -->
<!--   sDGNRS Pool.Reward rebalance / StakedDegenerusStonk.sol: -->
<!--     CREATOR_BPS 2000 @305; WHALE_POOL_BPS 1000 @308; AFFILIATE_POOL_BPS 3000 @309 (was 3500); -->
<!--     LOOTBOX_POOL_BPS 2000 @310; REWARD_POOL_BPS 1000 @311 (was 500); PRESALE_BOX_POOL_BPS 1000 @312 -->
<!--     (the 8 pool BPS must sum to BPS_DENOM); the genesis seeding poolBalances[uint8(Pool.Reward)] = -->
<!--     uint128(rewardAmount) @408 (rewardAmount = INITIAL_SUPPLY*REWARD_POOL_BPS/BPS_DENOM @389); -->
<!--     poolBalance @509; transferFromPool onlyGame @548; transferBetweenPools onlyGame @579. -->
<!--   Pool.Reward consumers (the conservation surface to verify): -->
<!--     BingoModule @188-190 (the bingo draw); DegeneretteModule @1221/1230 (a Degenerette Reward draw); -->
<!--     DegenerusGame.sol @466/472 (a Game-level Reward consume). -->
<!--   jackpot final-day Pool.Reward deletion side-effects: -->
<!--     DegenerusGameAdvanceModule.sol _consolidatePoolsAndRewardJackpots @833 (called @530; the -->
<!--       RewardJackpotsSettled event @1015); the affiliate-top DGNRS reward dgnrsReward = poolBalance* -->
<!--       AFFILIATE_POOL_REWARD_BPS @757-762; the BAF/decimator dispatch table @1189 (BAF fire gate -->
<!--       rngWord&1); the jackpot-phase solo bucket whale pass + DGNRS on final day @1047/1160 in -->
<!--       DegenerusGameJackpotModule.sol. -->
<!--   Green oracle: test/REGRESSION-BASELINE-v63.md = forge 854/0/110 (v51 empirical coverage carried from -->
<!--     Phase 341: per-tier rewards / dedup / empty-pool / jackpot-final-day regression; freeze-safety + -->
<!--     tier-precedence proven at SPEC 339). The POOL-CONSERVATION invariant net (FUZZ-05, carried from v62 -->
<!--     foundation) attests the 4-pool total is fully backed + transfers conserve. -->
<!--   Frozen-source read convention: git show a8b702a7:contracts/<File>.sol (ignore working tree). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the neutral council prompt for the v51 legacy-debt slice</name>
  <files>.planning/phases/394-legacy-debt/394-02-COUNCIL-PROMPT-V51.md</files>
  <read_first>
    - .planning/phases/391-rng-spine/391-01-COUNCIL-PROMPT-RNG.md AND .planning/phases/392-entropy-and-econ/392-02-COUNCIL-PROMPT-BURNIE.md (the approved prompt shape to match — header role, threat-priority line, trust-boundary framing, KNOWN-BY-DESIGN list, dedicated numbered prime targets, per-finding output format)
    - .planning/STATE.md (the "⚠ v50.0 + v51.0 AUDIT DEBT → FOLDED INTO v63.0 Phase 394" section — the authoritative charge spec for the v51 surface: the claimBingo / BingoModule internals to cover and what the v51 close already mitigated [SPEC 339 freeze-safety + tier-precedence; TST 341 per-tier rewards / dedup / empty-pool / jackpot-final-day regression; pre-launch no live funds])
    - .planning/AUDIT-V63-PLAN.md §2 (both-nets-on-record), §4 threat weighting (RNG/freeze DOMINANT — the bingo traitBurnTicket read; solvency SPINE — the Pool.Reward rebalance + jackpot final-day deletion; access/reentrancy confirmatory), §5 design-intent anchor (respect the standing by-design rulings)
    - .planning/REQUIREMENTS.md (the exact LEGACY-03 + LEGACY-04 text to charge)
    - the frozen source for the v51 prime sites via git show a8b702a7:contracts/modules/DegenerusGameBingoModule.sol (the 3-tier doc @14-17, the CEI doctrine @19-22, claimBingo @114-196, the dedup @148, the tier cascade @153-174, the Pool.Reward draw @188-190, creditFlip @196, the helper @240/259), contracts/StakedDegenerusStonk.sol (the 8 pool BPS constants @305-312 — AFFILIATE 3000 @309, REWARD 1000 @311; the genesis seeding @384-408; transferFromPool @548; transferBetweenPools @579), contracts/modules/DegenerusGameAdvanceModule.sol (_consolidatePoolsAndRewardJackpots @833 called @530, the affiliate DGNRS reward @757-762, the RewardJackpotsSettled event @1015, the dispatch table @1189), contracts/modules/DegenerusGameJackpotModule.sol (the final-day solo-bucket whale pass + DGNRS @1047/1160), contracts/modules/DegenerusGameDegeneretteModule.sol (the Reward draw @1221/1230), contracts/DegenerusGame.sol (the Reward consume @466/472)
    - the standing by-design rulings to encode as exclusions: [[claimbingo-no-level-guard]] (claimBingo has NO level guard — traits pre-resolve to currentLevel+5, the 8-color ownership check self-gates, signature uint24 level — do NOT flag the missing level guard); [[degenerette-wwxrp-rtp-by-design]] + [[intended-game-mechanics-not-findings]] (RTP / refund floors / charity governance not findings); [[neutral-defensive-audit-vocabulary]]
  </read_first>
  <action>
    Write 394-02-COUNCIL-PROMPT-V51.md, charged NEUTRALLY ("here is what we believe is true about the v51
    contract surface — claimBingo is a CEI-correct read-only consumer of a pre-resolved trait population with
    correct 3-tier selection / tier-precedence suppression / per-player (level,quadrant) dedup / empty-pool
    no-op / gameOver cutoff and a freeze-safe traitBurnTicket read; the sDGNRS Pool.Reward rebalance keeps
    the 8 pool splits summing to the denominator with no over-draw; the jackpot final-day Pool.Reward
    deletion/draw conserves backing — find where one of these breaks") and instructing the council to read
    the EXACT frozen source at `a8b702a7` via `git show a8b702a7:contracts/<File>.sol` (ignore the working
    tree). Use neutral defensive-engineering vocabulary throughout per [[neutral-defensive-audit-vocabulary]]
    (read-only consumer, CEI effects-before-interactions, tier precedence, dedup integrity, pool
    conservation, clamp-to-available, freeze snapshot, final-day consolidation).

    The prompt MUST include:
    (a) the threat-priority line from AUDIT-V63-PLAN §4 so the council weights correctly: an RNG/freeze break
    on the claimBingo traitBurnTicket read (the trait population read AFTER the level's word is public so a
    player can steer which color-completion they hold, or a bingo-draw entropy that is grindable) is
    DOMINANT; a solvency break on the Pool.Reward rebalance (the 8 splits no longer sum to the denominator so
    genesis over-/under-seeds, or transferFromPool over-draws past the pool balance) or the jackpot final-day
    deletion (Reward backing stranded or double-spent on the final-day consolidation) is SPINE-level; an
    access-control / reentrancy issue (claimBingo's effects-before-interactions CEI broken so a reentrant
    creditFlip/transferFromPool re-enters the dedup) is confirmatory; a desirability complaint about the
    no-level-guard or the RTP is NOT a finding;
    (b) the trust-boundary framing: claimBingo is permissionless-for-beneficiary (the reward credits
    msg.sender, gated by the 8-color ownership read + the per-player (level,quadrant) dedup); transferFromPool
    / creditFlip are onlyGame / onlyFlipCreditors callees; the Pool.Reward genesis split + the final-day
    consolidation run inside the contract's own accounting;
    (c) a KNOWN-BY-DESIGN exclusion list so the council does not re-flag settled intent: claimBingo has NO
    level guard BY-DESIGN ([[claimbingo-no-level-guard]] — the traits pre-resolve to currentLevel+5 and the
    8-color ownership check self-gates; the signature is uint24 level — do NOT flag the absence of a level
    guard; the question is whether the ownership read + the dedup + the gameOver cutoff are CORRECT, not
    whether a level guard is missing); the Degenerette RTP > 100% / WWXRP worthlessness / refund floors /
    charity governance are by-design ([[degenerette-wwxrp-rtp-by-design]], [[intended-game-mechanics-not-
    findings]]) — verify pool-conservation + freeze + dedup, not desirability; an admin breaking their own
    game at genesis is a non-finding;
    (d) the per-finding output format (FINDING: PROPERTY broken · reachable ordered CALL SEQUENCE [the
    multi-claim / reentrancy / final-day-advance interleaving where ordering matters] · STATE VAR + file:line
    at a8b702a7 · SEVERITY per the threat priority · WHY the existing dedup-bit / clamp / CEI ordering /
    freeze-snapshot / BPS-sum does not stop it | or VERIFIED SOUND: the property and the specific reason it
    holds — cite the dedup bit, the clamp-to-available, the BPS-sum-to-denominator, the freeze snapshot, or
    the CEI ordering — so the adjudicator can confirm).

    State the master thesis the slice rests on, mapped to LEGACY-03..04, as the claims to BREAK, then name
    the three v51 charge items as dedicated numbered break-targets:

    1. LEGACY-03 (claimBingo / BingoModule — 3-tier selection + tier-precedence + dedup + empty-pool +
       gameOver + freeze-safe traitBurnTicket read): claimBingo (@BingoModule:114) is documented as a strict
       READ-ONLY consumer of traitBurnTicket with effects (the dedup bit @148 + the tier cascade bit-sets
       @153-174) BEFORE the interactions (transferFromPool @189-190 + creditFlip @196). Charge the council to
       find (i) any FREEZE break — the traitBurnTicket ownership read (@130-136) consumes a trait population
       that became player-steerable AFTER the level's word was public (verify the population is snapshotted /
       pre-resolved before the player commits, the SPEC-339 freeze-safety claim — re-verify in code, do not
       trust the prior paper proof); (ii) any TIER-PRECEDENCE error — a quadrant-first claim that does NOT
       suppress the symbol bonus (the fq quadrant mask @158 / the both-bits-vs-symbol-bit-only logic @153-174
       computed wrong so a player double-collects the symbol + quadrant tier); (iii) any DEDUP bypass — the
       per-player (level,quadrant) dedup (@148) re-claimed (a second claimBingo for the same (level,quadrant)
       passing the AlreadyClaimed guard @38, e.g. via a reentrant creditFlip/transferFromPool re-entering
       claimBingo before the bit is observed — verify the CEI ordering sets the bit BEFORE the external call);
       (iv) any EMPTY-POOL / gameOver edge — confirm the empty-Reward-pool path is a clean no-op (the clamp
       @189 returns 0, creditFlip still safe) and the gameOver cutoff (@122) blocks a post-game claim. VERIFIED
       SOUND requires the freeze snapshot + the tier-precedence logic + the dedup bit + the CEI ordering cites.

    2. LEGACY-04a (the sDGNRS Pool.Reward rebalance — split conservation + no over-draw): the v51 rebalance
       moved AFFILIATE 3500→3000 and REWARD 500→1000 bps (StakedStonk:309/311). Charge the council to find
       (i) any conservation break — the 8 pool BPS (CREATOR 2000, WHALE 1000, AFFILIATE 3000, LOOTBOX 2000,
       REWARD 1000, PRESALE_BOX 1000, + the remaining splits @305-312) no longer sum to BPS_DENOM so the
       genesis seeding (@384-408) over- or under-allocates INITIAL_SUPPLY (sum the constants at the frozen
       source and confirm == BPS_DENOM); (ii) any over-draw — transferFromPool (@548) on the now-larger
       Reward pool (or the now-smaller Affiliate pool) over-spends past the pool balance, or
       transferBetweenPools (@579) underflows; (iii) any downstream assumption that hard-coded the OLD 500/
       3500 split (a consumer that computes a Reward draw against a stale BPS). Confirm the new split conserves
       supply and every pool draw clamps, or surface a finding.

    3. LEGACY-04b (the jackpot final-day Pool.Reward deletion / draw side-effects): the final-day reward
       consolidation (_consolidatePoolsAndRewardJackpots @AdvanceModule:833, called @530) and the affiliate-
       top DGNRS reward (poolBalance*AFFILIATE_POOL_REWARD_BPS @757-762) + the jackpot-phase final-day solo
       bucket whale pass + DGNRS (@JackpotModule:1047/1160) touch Pool.Reward on the final day. Charge the
       council to find (i) any STRANDED backing — a Reward draw on the final-day path that deletes/zeroes the
       pool balance WITHOUT the corresponding value leaving (a balance decremented but the transfer not made,
       or vice versa); (ii) any DOUBLE-SPEND — the same Reward backing consumed by both the final-day
       consolidation AND a concurrent claimBingo / Degenerette Reward draw in the same advance window; (iii)
       any ORDERING hazard where the final-day deletion runs before/after the per-level affiliate DGNRS reward
       in a way that mis-sizes the draw. Anchor on the POOL-CONSERVATION invariant (FUZZ-05) but attack
       independently for any final-day path its action set does not cover. Confirm the final-day Reward
       deletion conserves backing, or surface a finding.

    Do NOT pre-state any verdict in the prompt — the council finds, Claude adjudicates at 394-04. No fenced
    solidity blocks beyond the minimal identifier/selector/line citations the council needs to locate the
    code.
  </action>
  <verify>
    <automated>test -s .planning/phases/394-legacy-debt/394-02-COUNCIL-PROMPT-V51.md && grep -q "a8b702a7" .planning/phases/394-legacy-debt/394-02-COUNCIL-PROMPT-V51.md && for id in LEGACY-03 LEGACY-04; do grep -q "$id" .planning/phases/394-legacy-debt/394-02-COUNCIL-PROMPT-V51.md || { echo "MISSING $id"; exit 1; }; done && grep -Eqi "claimBingo|BingoModule|tier.?preceden|quadrant" .planning/phases/394-legacy-debt/394-02-COUNCIL-PROMPT-V51.md && grep -Eqi "traitBurnTicket|freeze|dedup" .planning/phases/394-legacy-debt/394-02-COUNCIL-PROMPT-V51.md && grep -Eqi "Pool.Reward|rebalance|3000|1000|conserv" .planning/phases/394-legacy-debt/394-02-COUNCIL-PROMPT-V51.md && grep -Eqi "final.?day|consolidat|deletion" .planning/phases/394-legacy-debt/394-02-COUNCIL-PROMPT-V51.md</automated>
  </verify>
  <acceptance_criteria>
    394-02-COUNCIL-PROMPT-V51.md exists and is non-empty; instructs reading frozen source at `a8b702a7` via
    git show; names LEGACY-03 + LEGACY-04; charges the three v51 items as dedicated numbered break-targets
    (claimBingo/BingoModule 3-tier + tier-precedence + dedup + empty-pool + gameOver + freeze-safe
    traitBurnTicket read; the Pool.Reward rebalance split-conservation + no-over-draw; the jackpot final-day
    Pool.Reward deletion no-strand / no-double-spend); carries the threat-priority line (the bingo
    traitBurnTicket read DOMINANT / the Pool.Reward rebalance + final-day deletion SPINE / access
    confirmatory); carries the KNOWN-BY-DESIGN exclusion list (claimBingo no-level-guard by-design, the RTP
    by-design); uses neutral vocabulary; no verdict is pre-stated.
  </acceptance_criteria>
  <done>
    The neutral v51 council prompt is authored against the frozen subject, covering LEGACY-03 + LEGACY-04
    with claimBingo/BingoModule freeze + tier-precedence + dedup, the Pool.Reward rebalance conservation, and
    the jackpot final-day deletion charged hard and the by-design exclusions encoded, ready to fan to the
    council.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the v51 council fan-out and record the council-net capture</name>
  <files>.planning/phases/394-legacy-debt/council/v51.gemini.txt, .planning/phases/394-legacy-debt/council/v51.codex.txt, .planning/phases/394-legacy-debt/council/v51.council.json, .planning/phases/394-legacy-debt/394-02-COUNCIL-NET.md</files>
  <read_first>
    - .planning/audit-v52/cross-model/bin/council.sh (the exact CLI contract + skipped[] behavior)
    - .planning/phases/394-legacy-debt/394-02-COUNCIL-PROMPT-V51.md (Task 1 output)
    - .planning/phases/393-permissionless-composition/393-01-COUNCIL-NET.md (the approved COUNCIL-NET capture record shape to match, including the codex-skip-recorded → 396 flag pattern)
  </read_first>
  <action>
    Run `.planning/audit-v52/cross-model/bin/council.sh --out-dir
    .planning/phases/394-legacy-debt/council --label v51
    .planning/phases/394-legacy-debt/394-02-COUNCIL-PROMPT-V51.md`. council.sh fans gemini+codex in parallel
    internally; this is ONE invocation (one slice) so no extra pacing of multiple invocations is needed
    beyond letting the single fan-out complete per [[pace-runs-to-survive-5h-cap]]. A skipped CLI is recorded
    in the council.json skipped[] and is NOT fatal; codex has been usage-capped since 392 and will likely
    skip — do not treat a codex skip as a failure (gemini + the Wave-2 Claude net satisfy the dual-NET).

    NOTE: this plan and 394-01 both write into the SHARED .planning/phases/394-legacy-debt/council/ dir, but
    with DISTINCT labels (v50 vs v51) → no filename overlap. Run independently of 394-01.

    IMMEDIATELY after the fan-out, git-status-verify the subject was not mutated:
    `git diff a8b702a7 -- contracts/` MUST be empty and `git status --porcelain contracts/` MUST be empty
    ([[feedback_verify_writecapable_agents]] — the council writes only to its out-dir, but verify; remove any
    stray file a council model writes outside the out-dir). Do NOT run `hardhat compile --force` (regenerates
    ContractAddresses source).

    Write 394-02-COUNCIL-NET.md recording: the v51.council.json manifest's available[]/skipped[], the raw
    output file paths, a one-line characterization of each model's output (raw, NOT adjudicated —
    adjudication is 394-04), the byte-freeze attestation (`git diff a8b702a7 -- contracts/` empty;
    `git status --porcelain contracts/` empty), and an explicit "NET 1 ON RECORD for v51" line so 394-04 can
    confirm both-nets-on-record before any verdict. If codex (or any CLI) is in skipped[], record WHICH, note
    the slice still has the other model on record, and FLAG a post-reset codex re-run → 396 (both-unavailable
    would require a re-run and must be surfaced, not silently passed).
  </action>
  <verify>
    <automated>test -f .planning/phases/394-legacy-debt/council/v51.council.json && test -s .planning/phases/394-legacy-debt/394-02-COUNCIL-NET.md && grep -q "NET 1 ON RECORD" .planning/phases/394-legacy-debt/394-02-COUNCIL-NET.md && [ -z "$(git -C /home/zak/Dev/PurgeGame/degenerus-audit diff a8b702a7 -- contracts/)" ]</automated>
  </verify>
  <acceptance_criteria>
    `v51.council.json` exists; either both gemini+codex `.txt` outputs are non-empty OR the unavailable CLI
    is in the council.json skipped[]; 394-02-COUNCIL-NET.md records the available/skipped + raw output paths +
    the byte-freeze attestation + an explicit "NET 1 ON RECORD" line + (if codex skipped) the post-reset
    codex re-run flag → 396; `git diff a8b702a7 -- contracts/` is empty.
  </acceptance_criteria>
  <done>
    NET 1 (the cross-model council) is on record for the v51 slice with raw output captured under the phase
    council/ dir, the subject proven byte-frozen, any codex skip documented + flagged → 396, and the capture
    recorded for the Wave-2 adjudication to fold in.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit-process → subject source | the risk is the AUDIT MUTATING the byte-frozen subject (a Write-capable fan-out edits contracts/), not a new feature threat |
| council CLI → out-dir | the council writes external-model output; risk = an unavailable/capped CLI silently dropping the only net (handled by skipped[] + the 396 re-run flag) or a CLI given write access beyond out-dir |

## STRIDE Threat Register (audit-only posture)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-394-04 | Tampering | byte-frozen subject contracts/ | mitigate | git-status-verify `git diff a8b702a7 -- contracts/` empty after the council fan-out (Task 2); council writes only to its out-dir; remove any stray file |
| T-394-05 | Information disclosure / false-green | the v51 slice with both CLIs unavailable silently treated as "on record" | mitigate | the council.json skipped[] is recorded in 394-02-COUNCIL-NET.md; a codex skip is documented + flagged for a post-reset re-run → 396; both-unavailable is surfaced, never passed silently |
| T-394-06 | Repudiation / scope-drift | the council re-litigating the documented claimBingo no-level-guard or the RTP instead of verifying freeze/dedup/conservation, wasting the pass | mitigate | the prompt carries the KNOWN-BY-DESIGN list so the council verifies freeze-safety / tier-precedence / dedup / pool-conservation, not the design intent |
| T-394-SC | Tampering | `hardhat compile --force` regenerating ContractAddresses source | accept/avoid | the plan forbids hardhat; only `git show`/read tools touch the subject |
</threat_model>

<verification>
- The council prompt exists, is neutral, charged against `a8b702a7`, covers LEGACY-03 + LEGACY-04, with the
  claimBingo/BingoModule freeze + tier-precedence + dedup, the Pool.Reward rebalance conservation, and the
  jackpot final-day deletion charged hard and the by-design exclusions encoded.
- `council.sh` ran for `--label v51`; the council.json manifest exists.
- Both model outputs non-empty OR the missing CLI in skipped[] (codex skip documented + flagged → 396).
- 394-02-COUNCIL-NET.md records the capture + byte-freeze attestation + "NET 1 ON RECORD".
- `git diff a8b702a7 -- contracts/` empty; `git status --porcelain contracts/` empty.
</verification>

<success_criteria>
NET 1 (the cross-model council) is on record for the full v51 legacy-debt slice (LEGACY-03 claimBingo /
BingoModule; LEGACY-04 sDGNRS Pool.Reward rebalance + jackpot final-day deletion), with raw gemini/codex
output captured under the phase council/ dir and the capture documented, so the Wave-2 Claude-net +
adjudication plan (394-04) can fold the council leads in before issuing any per-item verdict and author the
deferred FINDINGS-v51.0.md. The subject is proven byte-frozen throughout.
</success_criteria>

<output>
Create `.planning/phases/394-legacy-debt/394-02-SUMMARY.md` when done
</output>
