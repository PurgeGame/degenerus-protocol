---
phase: 435-design-design-lock-the-point-unit-streak-path-packing-equiva
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md
autonomous: true
requirements: [DESIGN-01, DESIGN-02]
must_haves:
  truths:
    - "The design-lock document exists with a DESIGN-01 section locking whole-points (1 pt = 100 bps), the floor(questStreak/2) rule, and the 655-point cap with sDGNRS-sentinel headroom justification"
    - "The design-lock document has a DESIGN-02 section specifying the uint16 subStreakLatch widening, the dropped 255 clamp, the deleted finalize floor-hack, and the actor/game-theory walk preserving _effectiveQuestStreak afking-XOR-manual semantics"
    - "Every additive bps contributor in _playerActivityScoreAt is inventoried with its multiple-of-100 status (mint streak/count/affiliate/curse/deity/whale = clean; quest-streak ×50 = the sole sub-point leg)"
  artifacts:
    - path: ".planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md"
      provides: "DESIGN-01 and DESIGN-02 design-lock sections"
      contains: "floor(questStreak / 2)"
      min_lines: 120
  key_links:
    - from: "435-DESIGN-LOCK.md DESIGN-01"
      to: "MintStreakUtils._playerActivityScoreAt:335"
      via: "quest-streak ×50 bps leg → floor(q/2) point conversion"
      pattern: "floor\\(questStreak"
    - from: "435-DESIGN-LOCK.md DESIGN-02"
      to: "DegenerusQuests.sol:546-551"
      via: "finalize floor-hack deletion specified"
      pattern: "546-551|floor-hack"
---

<objective>
Author the DESIGN-01 (point unit + quest-streak floor + point cap) and DESIGN-02 (single exact integer streak-base path + carried-in pre-streak-cap rework) sections of the v69 design-lock document, read-only against the byte-frozen v68 baseline (`contracts/` tree `e9a5fc24`).

Purpose: These two decisions both feed `MintStreakUtils._playerActivityScore`; they are the load-bearing point-domain definition the 436 IMPL diff implements and 437 TST proves. The USER has already locked every decision (D-01..D-05); this plan records them precisely with source-anchored rationale and the exact per-symbol edit surface for 436. NO `contracts/*.sol` change.
Output: `435-DESIGN-LOCK.md` created with the DESIGN-01 and DESIGN-02 sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-CONTEXT.md
@.planning/PLAN-V69-ACTIVITY-SCORE-POINTS.md

<interfaces>
<!-- Confirmed anchors from the byte-frozen v68 baseline (contracts/ tree e9a5fc24). -->
<!-- Executor uses these directly; re-read the cited files to confirm before authoring. -->

MintStreakUtils._playerActivityScoreAt (DegenerusGameMintStreakUtils.sol:282-372):
  - deity branch: bonusBps = 50*100 + 25*100 (lines 310-311), then += DEITY_PASS_ACTIVITY_BONUS_BPS (line 350)
  - non-deity: streakPoints (cap 50) ×100 + mintCountPoints ×100 (lines 329-330); pass-active floors PASS_STREAK_FLOOR_POINTS / PASS_MINT_COUNT_FLOOR_POINTS (lines 322-326, already POINT-domain values 50/25)
  - quest-streak leg: bonusBps += uint256(questStreak) * 50 (line 335) — THE SOLE 50-multiple
  - affiliate: affPoints ×100 (line 346)
  - whale: += 1000 / += 4000 (lines 354-356)
  - curse penalty: curse ×100, floored at 0 (lines 363-366)
  - cap: scoreBps = bonusBps > ACTIVITY_SCORE_HARD_CAP_BPS ? cap : bonusBps (lines 369-371)

Constants (DegenerusGameStorage.sol):
  - DEITY_PASS_ACTIVITY_BONUS_BPS = 8000 (:135) — multiple of 100 ✓ → 80 points
  - ACTIVITY_SCORE_HARD_CAP_BPS = 65_534 (:141) — one below uint16 max because sDGNRS stores uint16(score)+1
  - PASS_STREAK_FLOOR_POINTS = 50 (:144), PASS_MINT_COUNT_FLOOR_POINTS = 25 (:147) — already POINTS, not bps

Streak path (DegenerusGameStorage.sol):
  - Sub.subStreakLatch uint8 (:2244); SUB_STREAK_MASK = 0xff (:2251)
  - _streakBaseOf (:2254-2256); _setStreakBase clamps >255→255 (:2259-2261)
  - _afkingStreak (:2268-2272) = _streakBaseOf + (afkCoveredThroughDay - afkingStartDay), decay-on-read
  - _effectiveQuestStreak (:2295-2304) = afking-XOR-manual; live afking → _liveAfkingStreak else manual

GameAfkingModule.sol:
  - snap = quests.beginAfking(...) (:509); _setStreakBase(s, snap) at run-start (:521/523/533/555); 0 on lapse (:573/932/1081)
  - in-run secondary bump: _setStreakBase(s, _streakBaseOf(s) + 1) (:1734)
  - _finalizeAfking → quests.finalizeAfking(...) (:1066-1081)

DegenerusQuests.sol:
  - beginAfking returns state.streak (uint16) as snapshot (:501-511)
  - finalizeAfking floor-hack to DELETE (:546-551): preRun = state.streak; if finalStreak<preRun finalStreak=preRun; clamp type(uint16).max
  - PlayerQuestState.streak is uint16 (manual streak)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the DESIGN-01 section — point unit, quest-streak floor, point cap</name>
  <read_first>
    - contracts/modules/DegenerusGameMintStreakUtils.sol:282-372 (the full `_playerActivityScoreAt` body — confirm every additive contributor and the cap clamp)
    - contracts/storage/DegenerusGameStorage.sol:135-147 (DEITY_PASS_ACTIVITY_BONUS_BPS=8000, ACTIVITY_SCORE_HARD_CAP_BPS=65_534, PASS_STREAK_FLOOR_POINTS=50, PASS_MINT_COUNT_FLOOR_POINTS=25 — confirm each value)
    - contracts/sDGNRS.sol:1135-1142 (the `uint16(playerActivityScore())+1` snapshot with 0 = unset sentinel — confirm the +1 and the sentinel)
    - .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-CONTEXT.md (D-01, D-02, D-03 + the executor checklist in `<code_context>`)
  </read_first>
  <action>
    Create `435-DESIGN-LOCK.md` in the phase directory with a header (phase 435, baseline `contracts/` tree `e9a5fc24`, v68 closure subject `3cc51d00`, NO `.sol` change, neutral defensive-engineering vocabulary) and a `## DESIGN-01 — Point Unit, Quest-Streak Floor, Point Cap` section recording the locked decisions D-01/D-02/D-03.

    Record the point unit: 1 point = 100 bps; the activity score is the bps representation ÷100 (per D-01). Produce an **additive-contributor inventory table** for `_playerActivityScoreAt` listing each leg, its source line, its current bps form, and its multiple-of-100 status: mint streak `streakPoints×100` (:329, clean), mint count `mintCountPoints×100` (:330, clean), affiliate `affPoints×100` (:346, clean), deity `50×100 + 25×100` (:310-311, clean) `+ DEITY_PASS_ACTIVITY_BONUS_BPS=8000` (:350, clean = 80 pts — explicitly confirm 8000 mod 100 == 0), whale `+1000`/`+4000` (:354-356, clean = 10/40 pts), curse penalty `curse×100` (:363-366, clean), and the quest-streak leg `questStreak×50` (:335) as the **sole 50-multiple / sub-point contributor**. Note that `PASS_STREAK_FLOOR_POINTS=50` and `PASS_MINT_COUNT_FLOOR_POINTS=25` are already point-domain floor values (applied to `streakPoints`/`mintCountPoints` before the `×100`), so they need NO conversion.

    Lock the floor rule (D-02): the quest-streak point contribution is `floor(questStreak / 2)` (1 pt per 2 quests; the trailing 0.5 pt is dropped at odd streak counts). State this is the ONLY place precision is intentionally lost, chosen by the USER over round-half-up and over keeping a half-point internal unit.

    Lock the point cap (D-03): `ACTIVITY_SCORE_HARD_CAP_BPS = 65_534` → point cap `floor(65534/100) = 655` points. Record the arithmetic and that `Sub.score` (the frozen stamp) STAYS `uint16` (655 fits with wide headroom; the slot is otherwise exactly packed so narrowing nets nothing). **Explicitly carry the sDGNRS sentinel constraint**: `sDGNRS.sol:1140` stores `uint16(playerActivityScore())+1` with `0` as the unset sentinel; at the point cap `655+1 = 656` fits `uint16` with no collision (the old `65534` reason — one below uint16 max so `+1` did not overflow — is satisfied with vast margin in the point domain).

    Note in Claude's-discretion: exact constant naming and where the floor + point cap live in source is 436 IMPL detail.
  </action>
  <verify>
    <automated>test -f .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md && grep -q 'floor(questStreak / 2)' .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md && grep -q '655' .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md && grep -Eq '8000|DEITY_PASS_ACTIVITY_BONUS_BPS' .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md</automated>
  </verify>
  <acceptance_criteria>
    - `435-DESIGN-LOCK.md` exists with a `## DESIGN-01` section.
    - The additive-contributor inventory table lists ALL legs (mint streak/count/affiliate/deity/whale/curse/quest-streak) with source lines and multiple-of-100 status; quest-streak is marked the sole 50-multiple.
    - `DEITY_PASS_ACTIVITY_BONUS_BPS = 8000` is explicitly confirmed as a multiple of 100 (→ 80 points).
    - The floor rule is recorded verbatim as `floor(questStreak / 2)`.
    - The point cap 655 is recorded with the `floor(65534/100)` arithmetic AND the sDGNRS `+1` sentinel headroom check (`655+1=656` fits uint16).
    - `Sub.score` is locked to stay uint16.
    - No `contracts/*.sol` file is modified.
  </acceptance_criteria>
  <done>The DESIGN-01 section is authored with the contributor inventory, the floor rule, and the point cap + sentinel justification.</done>
</task>

<task type="auto">
  <name>Task 2: Author the DESIGN-02 section — single exact integer streak path + pre-streak-cap rework</name>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol:2244-2304 (Sub.subStreakLatch uint8, SUB_STREAK_MASK, _streakBaseOf, _setStreakBase 255-clamp, _afkingStreak, _liveAfkingStreak, _effectiveQuestStreak)
    - contracts/modules/GameAfkingModule.sol:500-573 (the run-start snapshot `_setStreakBase(s, snap)`), :1066-1081 (`_finalizeAfking` → `quests.finalizeAfking`), :1734 (the in-run `+1` secondary bump)
    - contracts/DegenerusQuests.sol:498-558 (`beginAfking` returns uint16 `state.streak`; `finalizeAfking` + the floor-hack at :546-551 to delete; `PlayerQuestState.streak` is uint16)
    - .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-CONTEXT.md (D-04, D-05 + the `<specifics>` pre-streak grievance framing)
  </read_first>
  <action>
    Append a `## DESIGN-02 — Single Exact Integer Streak Path + Pre-Streak-Cap Rework` section to `435-DESIGN-LOCK.md` recording D-04 and D-05.

    State the grievance precisely (per `<specifics>`): the carried-in pre-streak is snapshotted from the manual `PlayerQuestState.streak` (uint16) into `Sub.subStreakLatch` (uint8) at run start via `_setStreakBase`, which **clamps to 255** (`DegenerusGameStorage.sol:2259-2261`) — silently truncating a high-streak player's carried-in streak for the run's activity score — and a compensating **finalize floor-hack** (`DegenerusQuests.sol:546-551`) reaches back into the dormant uint16 `state.streak` to restore it on exit. A width mismatch papered over twice.

    Lock the fix (D-04): widen `Sub.subStreakLatch` `uint8 → uint16` (consuming the 8 bits freed by the DESIGN-03 pendingFlip narrowing — cross-reference plan 02), making it symmetric with the manual uint16 `state.streak`. Specify the exact edit surface: (a) the `subStreakLatch` field declaration in `Sub`; (b) `SUB_STREAK_MASK` widens to `0xffff`; (c) `_streakBaseOf` returns `uint16`; (d) `_setStreakBase` DROPS the `>255 → 255` clamp (now snapshots exactly into the full uint16); (e) `_afkingStreak`'s `uint32(_streakBaseOf(sub))` cast continues to hold; (f) `DegenerusQuests.sol:546-551` finalize floor-hack is **DELETED** (the `preRun = state.streak; if (finalStreak<preRun) finalStreak=preRun;` block) because the latch now carries the pre-run snapshot exactly — `finalStreak` already reflects the true base + funded days, so no restore is needed.

    Lock the single exact path (D-05): with the full-width latch, the manual + afking streak base feeds `_playerActivityScore` through one exact integer path (no fractional/bps intermediate). Confirm the `DegenerusQuests` streak source and the `pendingFlip` accrual note (`~:1779`) stay consistent with the new path.

    Provide the **actor/game-theory walk** preserving semantics: (1) a non-afker reads the manual streak unchanged (`_effectiveQuestStreak` returns `manualStreak` when `!afking`, :2301); (2) a live afking sub reads the compute-on-read `_liveAfkingStreak` = `streakBase + (afkCoveredThroughDay - afkingStartDay)` (:2271, :2302); (3) the afking-XOR-manual exclusivity is preserved — only one source ever drives the score at a time; (4) decay-on-read (`covered + 1 < currentDay → 0`, :2270) is unchanged; (5) the in-run `+1` secondary bump (`GameAfkingModule:1734`) and the run-start re-base / 0-on-lapse writes are unchanged in behaviour, only the field width changes; (6) a high-streak player (>255) now carries the exact base into the run instead of a 255-truncated one, AND on finalize hands back the exact streak with no floor-hack — proving the rework is behaviour-preserving except for removing the prior silent truncation.
  </action>
  <verify>
    <automated>grep -q 'DESIGN-02' .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md && grep -Eq 'uint8 *(→|->|to) *uint16|uint8.{0,4}uint16' .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md && grep -Eq '546-551|floor-hack' .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md && grep -q '_effectiveQuestStreak' .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md</automated>
  </verify>
  <acceptance_criteria>
    - The `## DESIGN-02` section exists in `435-DESIGN-LOCK.md`.
    - The grievance is stated (uint8/255 clamp at `_setStreakBase:2259-2261` + the finalize floor-hack at `DegenerusQuests.sol:546-551`).
    - The fix enumerates the exact edits: `subStreakLatch` uint8→uint16, `SUB_STREAK_MASK` → 0xffff, `_streakBaseOf` returns uint16, drop the 255 clamp, delete the finalize floor-hack block.
    - The 8 freed bits are cross-referenced to plan 02 (DESIGN-03 pendingFlip narrowing).
    - The actor/game-theory walk covers non-afker, live-afking, afking-XOR-manual exclusivity, decay-on-read, the in-run +1 bump, and the >255 high-streak case proving the truncation removal is the only behaviour change.
    - `_effectiveQuestStreak` afking-XOR-manual semantics are explicitly stated as preserved.
    - No `contracts/*.sol` file is modified.
  </acceptance_criteria>
  <done>The DESIGN-02 section is authored with the grievance, the exact widening/deletion edit surface, the freed-bits cross-reference, and the semantics-preserving actor walk.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design-lock doc → 436 IMPL diff | A design error here propagates into the only `.sol` change of the milestone (no code ships in 435 itself) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-435-01 | Tampering | quest-streak floor conversion (`floor(q/2)`) | mitigate | Inventory every additive contributor with its multiple-of-100 status so the floor is applied ONLY to the sole 50-multiple leg; the de-minimis odd-half-point shift is the documented accepted boundary (D-09, proven in plan 03) |
| T-435-02 | Information disclosure | point cap vs sDGNRS sentinel | mitigate | Carry the `uint16(score)+1` sentinel check into the cap derivation (`655+1=656` fits uint16) so the point cap cannot collide with the unset-sentinel encoding |
| T-435-03 | Tampering | subStreakLatch widening / pre-streak snapshot | mitigate | Specify the exact field/mask/getter/setter edits and prove via the actor walk that the only behaviour change is removing the prior silent 255-truncation; the freed-bits source is locked to the DESIGN-03 narrowing (plan 02 owns the slot arithmetic) |
| T-435-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in a read-only docs-only phase; no install task exists |
</threat_model>

<verification>
- `435-DESIGN-LOCK.md` exists with `## DESIGN-01` and `## DESIGN-02` sections.
- The DESIGN-01 contributor inventory confirms every additive bps leg is a multiple of 100 except the quest-streak ×50 leg.
- The floor rule (`floor(questStreak / 2)`), the point cap (655 with the sDGNRS sentinel check), and the uint16 `Sub.score` lock are recorded.
- The DESIGN-02 section specifies the uint8→uint16 latch widening, the dropped clamp, the deleted floor-hack, the freed-bits cross-reference to plan 02, and the semantics-preserving actor walk.
- No `contracts/*.sol` file was modified (read-only phase).
</verification>

<success_criteria>
DESIGN-01 and DESIGN-02 are design-locked in `435-DESIGN-LOCK.md` with source-anchored rationale and an executor-ready per-symbol edit surface for 436. Zero Solidity source changed.
</success_criteria>

<output>
Create `.planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-01-SUMMARY.md` when done.
</output>
