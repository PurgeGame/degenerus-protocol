---
phase: 435-design-design-lock-the-point-unit-streak-path-packing-equiva
plan: 03
type: execute
wave: 3
depends_on: ["435-01", "435-02"]
files_modified:
  - .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md
autonomous: true
requirements: [DESIGN-04]
must_haves:
  truths:
    - "The design-lock document has a DESIGN-04 section with a per-consumer scale-invariance proof (Lootbox EV multiplier, Degenerette ROI, Decimator) showing ÷100 of score AND range anchors leaves the integer interpolation result identical for clean-multiple scores"
    - "An input-vs-output constant inventory separates the score-INPUT thresholds that convert ÷100 (Degenerette MID/HIGH/MAX 7500/25500/30500→75/255/305, Lootbox NEUTRAL/MAX 6000/40000→60/400, Decimator CAP 23500→235) from the OUTPUT bps that must NOT convert (ROI 9000-9990, EV-mult MIN/NEUTRAL/MAX 9000/10000/14500, DGNRS reward bps)"
    - "The only divergence — the odd-half-point threshold-tip case — is enumerated and confirmed the accepted de-minimis shift (D-09)"
    - "A consolidated per-file/per-symbol 436 edit-surface list and the 438 RNG-freeze re-audit executor checklist are recorded"
  artifacts:
    - path: ".planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md"
      provides: "DESIGN-04 equivalence proof, input-vs-output inventory, consolidated 436 edit surface, 438 checklist"
      contains: "scale-invarianc"
  key_links:
    - from: "435-DESIGN-LOCK.md DESIGN-04"
      to: "DegenerusGameStorage._lootboxEvMultiplierFromScore:1633"
      via: "scale-invariance: input anchors ÷100, output bps unchanged"
      pattern: "_lootboxEvMultiplierFromScore"
    - from: "435-DESIGN-LOCK.md DESIGN-04"
      to: "sDGNRS.sol:47 / IDegenerusGame.sol:65"
      via: "external playerActivityScore bps→points return-semantics change flagged for 438"
      pattern: "sDGNRS|IDegenerusGame"
---

<objective>
Author the DESIGN-04 (consumer-threshold behaviour-equivalence) section, the consolidated 436 edit surface, and the 438 RNG-freeze re-audit executor checklist of the v69 design-lock document — read-only against the byte-frozen v68 baseline (`contracts/` tree `e9a5fc24`).

Purpose: This is the load-bearing correctness argument. It proves that converting the activity score bps→points does not materially shift the Lootbox EV multiplier, the Degenerette ROI, or the Decimator outcome (DESIGN-04 / D-09 / D-10), separates the score-INPUT thresholds that convert from the OUTPUT bps that must NOT, consolidates the exact per-file edit surface so 436 is a mechanical batched diff, and hands the cross-boundary `playerActivityScore` return-semantics change to 438 REAUDIT-02. NO `contracts/*.sol` change.
Output: The `## DESIGN-04`, `## 436 Edit Surface (consolidated)`, and `## 438 RNG-Freeze Re-Audit Checklist` sections appended to `435-DESIGN-LOCK.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-CONTEXT.md
@.planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-01-SUMMARY.md
@.planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-02-SUMMARY.md

<interfaces>
<!-- Confirmed anchors from the byte-frozen v68 baseline (contracts/ tree e9a5fc24). -->

LOOTBOX EV MULTIPLIER — DegenerusGameStorage._lootboxEvMultiplierFromScore (:1633-1656):
  INPUT anchors (convert ÷100): LOOTBOX_EV_ACTIVITY_NEUTRAL_BPS = 6_000 (:1553) → 60 ; LOOTBOX_EV_ACTIVITY_MAX_BPS = 40_000 (:1555) → 400
  OUTPUT bps (DO NOT convert): LOOTBOX_EV_MIN_BPS = 9_000 (:1557) ; LOOTBOX_EV_NEUTRAL_BPS = 10_000 (:1559) ; LOOTBOX_EV_MAX_BPS = 14_500 (:1561)
  low leg:  EV_MIN + score*(EV_NEUTRAL-EV_MIN)/EV_ACTIVITY_NEUTRAL   (score & EV_ACTIVITY_NEUTRAL both ÷100 → ratio invariant)
  high leg: EV_NEUTRAL + (score-EV_ACTIVITY_NEUTRAL)*(EV_MAX-EV_NEUTRAL)/(EV_ACTIVITY_MAX-EV_ACTIVITY_NEUTRAL)
  NOTE: the "40000" comment at DegenerusGameLootboxModule.sol:304 is the derived EV mean (a different value) — IGNORE, do not convert.

DEGENERETTE ROI — DegenerusGameDegeneretteModule.sol (:1144-1165+):
  INPUT anchors (convert ÷100): ACTIVITY_SCORE_MID_BPS = 7_500 (:188) → 75 ; ACTIVITY_SCORE_HIGH_BPS = 25_500 (:191) → 255 ; ACTIVITY_SCORE_MAX_BPS = 30_500 (:194) → 305
  OUTPUT bps (DO NOT convert): ROI_MIN_BPS=9_000 (:197) ; ROI_MID_BPS=9_500 (:200) ; ROI_HIGH_BPS=9_950 (:203) ; ROI_MAX_BPS=9_990 (:206) ; WWXRP_HIGH_ROI_MAX_BPS=10_990 (:217)
  clamp score>MAX→MAX (:1144-1145); piecewise legs key on score vs MID/HIGH/MAX; ROI = ROI_anchor + delta*roiDelta/span (input anchors all ÷100 → ratio invariant)

DECIMATOR — DegenerusGameDecimatorModule.sol:
  INPUT anchor (convert ÷100): TERMINAL_DEC_ACTIVITY_CAP_BPS = 23_500 (:772) → 235
  reads playerActivityScore (:793, :913); clamps bonusBps>CAP→CAP (:796-797, :916-917); uses CAP in (CAP/2)/CAP ratio (:1139-1140 — pure ratio, invariant)

EXTERNAL boundary (return-semantics change bps→points):
  IDegenerusGame.playerActivityScore (interfaces/IDegenerusGame.sol:65) → DegenerusGame.playerActivityScore (:2210-2218, returns _playerActivityScore)
  consumers: sDGNRS.sol:47 (decl) + sDGNRS.sol:1140 (`uint16(game.playerActivityScore(...))+1`, 0=unset sentinel) ; DecimatorModule self-call (:793, :913)

FROZEN-AT-DEPOSIT score crosses module calls as uint16 activityScore (the anti-gaming knob, LootboxModule.sol:551):
  resolveLootboxDirect (:873), resolveRedemptionLootbox (:928), _resolveRedemptionChunk (:967), the boon dispatch (:1076) — all take `uint16 activityScore`; point-domain max 655 fits uint16.

OUT-OF-DOMAIN bps that MUST NOT convert (score has no input role): ROI_* (above), LOOTBOX_EV_MIN/NEUTRAL/MAX (above), DGNRS reward bps (DEGEN_DGNRS_*_BPS).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author the DESIGN-04 equivalence proof + the input-vs-output constant inventory</name>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol:1553-1561 (the Lootbox EV input anchors NEUTRAL=6000/MAX=40000 vs the output EV_MIN=9000/EV_NEUTRAL=10000/EV_MAX=14500) and :1633-1656 (`_lootboxEvMultiplierFromScore` — confirm both legs use `score` and the input anchors in the denominator, the output anchors in the numerator)
    - contracts/modules/DegenerusGameDegeneretteModule.sol:188-217 (the MID/HIGH/MAX input anchors vs the ROI_MIN/MID/HIGH/MAX output anchors) and :1144-1170 (the piecewise ROI interpolation — confirm `score` is clamped/compared to the input anchors, ROI uses the output anchors)
    - contracts/modules/DegenerusGameDecimatorModule.sol:772 (`TERMINAL_DEC_ACTIVITY_CAP_BPS = 23_500`), :793/:913 (reads `playerActivityScore`, clamps to CAP at :796-797/:916-917), :1139-1140 (the `(CAP/2)/CAP` ratio)
    - contracts/modules/DegenerusGameLootboxModule.sol:304 (the coincidental "40000" EV-mean comment to IGNORE)
    - .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-CONTEXT.md (D-09, D-10 + the `<code_context>` input-vs-output checklist)
    - .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-01-SUMMARY.md (the locked floor rule + point cap)
  </read_first>
  <action>
    Append a `## DESIGN-04 — Consumer-Threshold Behaviour-Equivalence` section to `435-DESIGN-LOCK.md` recording D-09 and D-10.

    First produce the **input-vs-output constant inventory** as two explicit tables. TABLE A — score-INPUT thresholds that CONVERT ÷100 (constant name, file:line, current bps, new points): Degenerette `ACTIVITY_SCORE_MID_BPS` 7500→75 (:188), `ACTIVITY_SCORE_HIGH_BPS` 25500→255 (:191), `ACTIVITY_SCORE_MAX_BPS` 30500→305 (:194); Lootbox `LOOTBOX_EV_ACTIVITY_NEUTRAL_BPS` 6000→60 (:1553), `LOOTBOX_EV_ACTIVITY_MAX_BPS` 40000→400 (:1555); Decimator `TERMINAL_DEC_ACTIVITY_CAP_BPS` 23500→235 (:772). TABLE B — OUTPUT/out-of-domain bps that MUST NOT convert (constant name, file:line, value, why it is not a score input): Degenerette `ROI_MIN/MID/HIGH/MAX_BPS` 9000/9500/9950/9990 (:197-206) + `WWXRP_HIGH_ROI_MAX_BPS` 10990 (:217); Lootbox `LOOTBOX_EV_MIN/NEUTRAL/MAX_BPS` 9000/10000/14500 (:1557-1561); DGNRS reward bps `DEGEN_DGNRS_*_BPS`. Explicitly note the roadmap "Lootbox EV-cap 40000" = the score-input anchor `LOOTBOX_EV_ACTIVITY_MAX_BPS` (TABLE A), and the coincidentally-equal "40000" comment at `LootboxModule:304` is the derived EV mean (a DIFFERENT value, ignore).

    Then produce the **per-consumer scale-invariance proof** (D-10). For EACH of the three interpolating consumers state the formula shape `result = anchor_out + (score - anchor_in_lo) * (out_hi - out_lo) / (anchor_in_hi - anchor_in_lo)` and prove: dividing `score` AND both input range anchors by 100 multiplies numerator and denominator of the ratio term by `1/100` each → the ratio `(score - anchor_in_lo)/(anchor_in_hi - anchor_in_lo)` is unchanged, the output anchors are untouched, so for any score that is a clean multiple of 100 bps (i.e. a whole point) the integer interpolation result is IDENTICAL. Do this concretely per consumer:
      - Lootbox EV (`_lootboxEvMultiplierFromScore`, :1633): low leg `EV_MIN + score*(EV_NEUTRAL-EV_MIN)/EV_ACTIVITY_NEUTRAL` (score & 6000→60 both ÷100); high leg over `(EV_ACTIVITY_MAX - EV_ACTIVITY_NEUTRAL)` (both ÷100); `>= EV_ACTIVITY_MAX → EV_MAX` clamp invariant.
      - Degenerette ROI (:1144-1170): the `score > MAX → MAX` clamp and each piecewise leg over the MID/HIGH/MAX input spans (all ÷100), ROI output anchors unchanged.
      - Decimator (:793-797, :1139-1140): the `bonusBps > CAP → CAP` clamp (CAP 23500→235) and the `(CAP/2)/CAP` ratio (pure ratio, invariant).

    Then enumerate the **ONLY divergence** (D-09): flooring `floor(questStreak/2)` can change a consumer outcome ONLY at the exact boundary where one odd quest's trailing 0.5 pt used to tip a player across an input threshold (MID/HIGH/MAX, NEUTRAL/MAX, CAP) — a single half-point threshold-tip. Confirm this is the ACCEPTED, documented de-minimis shift (USER selected accept+document over nudging thresholds or strict exact-equivalence); NO threshold nudging. Walk one worked example of an odd-half-point input landing one half-point below a converted threshold to show the shift magnitude is at most one grid step at one boundary.
  </action>
  <verify>
    <automated>grep -q 'DESIGN-04' .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md && grep -Eq 'scale-invarianc' .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md && grep -q '_lootboxEvMultiplierFromScore' .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md && grep -Eq '7500.{0,6}75|->.?75|→.?75' .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md && grep -Eq 'odd-half-point|de-minimis' .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md</automated>
  </verify>
  <acceptance_criteria>
    - The `## DESIGN-04` section exists.
    - TABLE A lists all 6 score-INPUT thresholds (Degenerette MID/HIGH/MAX 7500/25500/30500→75/255/305, Lootbox NEUTRAL/MAX 6000/40000→60/400, Decimator CAP 23500→235) with file:line.
    - TABLE B lists the OUTPUT/out-of-domain bps that MUST NOT convert (ROI 9000-9990 + WWXRP 10990, EV-mult MIN/NEUTRAL/MAX 9000/10000/14500, DGNRS reward bps) with the "not a score input" rationale.
    - The `LootboxModule:304` "40000" EV-mean comment is explicitly flagged as a coincidental non-anchor to ignore.
    - The scale-invariance proof is given per consumer (Lootbox EV, Degenerette ROI, Decimator) showing the ÷100 ratio invariance for clean-multiple scores, output anchors untouched.
    - The only divergence (odd-half-point threshold-tip) is enumerated, confirmed the accepted de-minimis shift (D-09), with one worked boundary example; no threshold nudging.
    - No `contracts/*.sol` file is modified.
  </acceptance_criteria>
  <done>The DESIGN-04 section is authored with the input-vs-output inventory, the per-consumer scale-invariance proof, and the bounded odd-half-point divergence.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Consolidate the 436 edit surface + author the 438 RNG-freeze re-audit checklist</name>
  <read_first>
    - contracts/interfaces/IDegenerusGame.sol:65 (`playerActivityScore` external decl) and contracts/DegenerusGame.sol:2210-2218 (the body returning `_playerActivityScore`)
    - contracts/sDGNRS.sol:47 (the decl) and :1135-1142 (the `uint16(game.playerActivityScore(...))+1`, 0=unset sentinel snapshot — confirm the point-domain return is still correct: `score+1` with cap 655 → 656 fits uint16)
    - contracts/modules/DegenerusGameLootboxModule.sol:551 (the "anti-gaming knob, FROZEN at deposit" comment), :873/:928/:967/:1076 (the four entrypoints taking `uint16 activityScore`)
    - .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-01-SUMMARY.md and 435-02-SUMMARY.md (the DESIGN-01/02/03 edit surfaces already locked — consolidate, do not re-derive)
    - .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-CONTEXT.md (the full `<code_context>` executor checklist + the integration-points note on resetting the v68 RNG-freeze proof / layout golden / mutation)
  </read_first>
  <action>
    Append two sections to `435-DESIGN-LOCK.md`.

    Section `## 436 Edit Surface (consolidated)` — a single per-file / per-symbol change list aggregating every edit locked across DESIGN-01..04, so 436 is a mechanical batched diff. Group by file with the exact symbol and the point-domain value:
      - `MintStreakUtils` (`_playerActivityScoreAt`): the quest-streak leg `questStreak×50` → `floor(questStreak/2)` point contribution (:335); the point cap 655 enforcement replacing the bps clamp (:369-371); all other `×100` legs collapse to point form per DESIGN-01.
      - `DegenerusGameStorage`: `ACTIVITY_SCORE_HARD_CAP_BPS 65534` → point cap 655 (:141); `Sub.subStreakLatch` uint8→uint16 + `SUB_STREAK_MASK` 0xff→0xffff (:2244/:2251); `_streakBaseOf` return uint16 + `_setStreakBase` drop 255 clamp (:2254-2261); `Sub.pendingFlip` uint32→uint24 (:2237); the accumulator slot-doc-comment `pendingFlip(32)+subStreakLatch(8)` → `pendingFlip(24)+subStreakLatch(16)` (:2126-2168); the Lootbox EV input anchors NEUTRAL 6000→60 / MAX 40000→400 (:1553/:1555) [OUTPUT EV_MIN/NEUTRAL/MAX UNCHANGED].
      - `GameAfkingModule`: the pendingFlip accrue `uint32(...)`→`uint24(...)` casts + clamp re-pin (:861-863, :925-928) and the settle read (:1098-1100); the latch writes (`_setStreakBase`/`_streakBaseOf`/the +1 bump :1734) follow the uint16 widening.
      - `DegenerusQuests`: DELETE the finalize floor-hack (:546-551); the streak source + pendingFlip accrual note (`~:1779`) stay consistent.
      - `DegenerusGameDegeneretteModule`: MID/HIGH/MAX 7500/25500/30500→75/255/305 (:188/:191/:194) [ROI_* output anchors UNCHANGED].
      - `DegenerusGameDecimatorModule`: `TERMINAL_DEC_ACTIVITY_CAP_BPS` 23500→235 (:772) — also re-confirm the self-call reads the point-domain `playerActivityScore` (:793/:913).
      - Add an explicit DO-NOT-TOUCH list (the OUTPUT bps from DESIGN-04 TABLE B + `lootboxRngPendingFlip` uint40 + the LootboxModule:304 EV-mean comment).

    Section `## 438 RNG-Freeze Re-Audit Checklist (handoff to REAUDIT-02)` — the cross-boundary / freeze items 438 must re-attest under the point domain:
      - The external `playerActivityScore` return-semantics change bps→points crosses the contract boundary: `IDegenerusGame.sol:65` → `DegenerusGame.sol:2210-2218` → consumed by `sDGNRS.sol:47/:1140` and the decimator self-call (`DecimatorModule:793/:913`). Re-verify `sDGNRS`'s `uint16(score)+1` use is point-domain-correct (656 fits uint16, the 0=unset sentinel still holds) and re-attest in REAUDIT-02; flag indexer / off-chain parity as a downstream consumer of the changed return semantics.
      - The frozen-at-commitment score crosses module calls as `uint16 activityScore` (the anti-gaming knob, `LootboxModule:551`) in the four entrypoints `:873/:928/:967/:1076`; point-domain max 655 fits uint16 → re-confirm the snapshot-at-deposit freeze in the point domain (no in-window score-bump bias).
      - The layout-golden recapture (the `forge inspect` MECH-02 v68 oracle) from the accumulator repack is the EXPECTED new golden, handled in 438 REAUDIT-01, not a drift; the ~30 slot-hardcoded harnesses re-pin there.
      - Confirm `lootboxRngPendingFlip` uint40 (`DegenerusGameStorage:1525`) is a distinct out-of-scope field (carried from DESIGN-03) so the re-audit does not chase it.
  </action>
  <verify>
    <automated>grep -q '436 Edit Surface' .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md && grep -Eq '438 RNG-Freeze Re-Audit|REAUDIT-02' .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md && grep -q 'sDGNRS' .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md && grep -Eq 'anti-gaming|FROZEN at deposit|snapshot-at-deposit' .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md && grep -Eq 'DO-NOT-TOUCH|DO NOT TOUCH|do not convert' .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md</automated>
  </verify>
  <acceptance_criteria>
    - The `## 436 Edit Surface (consolidated)` section lists, per file, every locked edit with the exact symbol + point-domain value (MintStreakUtils floor + cap, Storage cap/latch/mask/pendingFlip/slot-comment/EV-input-anchors, GameAfkingModule accrue/settle/latch, DegenerusQuests floor-hack deletion, Degenerette MID/HIGH/MAX, Decimator CAP).
    - An explicit DO-NOT-TOUCH list (OUTPUT bps from TABLE B + lootboxRngPendingFlip + the :304 EV-mean comment) is present.
    - The `## 438 RNG-Freeze Re-Audit Checklist` section flags the external `playerActivityScore` bps→points boundary (IDegenerusGame:65 → sDGNRS:47/:1140 + Decimator self-call), the `uint16 activityScore` frozen-at-deposit anti-gaming knob across the four lootbox entrypoints (655 fits uint16), the layout-golden recapture (expected, not drift), and the lootboxRngPendingFlip out-of-scope confirmation.
    - The sDGNRS `uint16(score)+1` point-domain correctness (656 fits uint16, 0=unset sentinel) is recorded.
    - No `contracts/*.sol` file is modified.
  </acceptance_criteria>
  <done>The consolidated 436 edit surface and the 438 RNG-freeze re-audit checklist are authored, completing the load-bearing design-lock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design-lock doc → 436 IMPL diff | An equivalence miss or input/output misclassification here propagates into the only `.sol` change of the milestone |
| DegenerusGame.playerActivityScore → sDGNRS / decimator self-call / off-chain indexer | The external return-semantics change bps→points crosses the contract boundary and an off-chain parity surface |
| activity score → VRF/RNG consumers (lootbox EV / Degenerette / decimator) | The score is the anti-gaming knob snapshotted at deposit; the point-domain representation must preserve the freeze |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-435-07 | Tampering | EV/ROI/decimator behaviour-equivalence | mitigate | Per-consumer scale-invariance proof (÷100 of score AND input anchors preserves the ratio; output anchors untouched → integer result identical for clean scores); the sole divergence (odd-half-point tip) is bounded to one grid step at one boundary and accepted (D-09) |
| T-435-08 | Tampering | input-vs-output constant misclassification | mitigate | Two explicit tables (CONVERT vs DO-NOT-CONVERT) with file:line; the coincidental `:304` 40000 EV-mean comment flagged as a non-anchor; the consolidated 436 surface carries a DO-NOT-TOUCH list |
| T-435-09 | Information disclosure | external playerActivityScore bps→points boundary | mitigate | Hand the return-semantics change to 438 REAUDIT-02 (sDGNRS `uint16(score)+1` re-verified point-correct, 656 fits uint16, 0=unset sentinel holds; indexer/off-chain parity flagged) |
| T-435-10 | Elevation of privilege | RNG-freeze (anti-gaming knob) under the point domain | mitigate | Re-confirm the `uint16 activityScore` snapshot-at-deposit freeze across the four lootbox entrypoints (655 fits uint16, no in-window score-bump bias); re-attested in 438 REAUDIT-02 |
| T-435-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in a read-only docs-only phase; no install task exists |
</threat_model>

<verification>
- `435-DESIGN-LOCK.md` has a `## DESIGN-04` section with TABLE A (convert), TABLE B (do not convert), the per-consumer scale-invariance proof, and the bounded odd-half-point divergence.
- The `## 436 Edit Surface (consolidated)` section lists every locked edit per file with a DO-NOT-TOUCH list.
- The `## 438 RNG-Freeze Re-Audit Checklist` flags the external boundary, the frozen-at-deposit anti-gaming knob, the layout-golden recapture, and the out-of-scope field.
- No `contracts/*.sol` file was modified (read-only phase).
</verification>

<success_criteria>
DESIGN-04 is design-locked with the per-consumer scale-invariance proof, the input-vs-output inventory, the bounded de-minimis divergence, the consolidated mechanical 436 edit surface, and the 438 re-audit checklist — completing the load-bearing design-lock document. Zero Solidity source changed.
</success_criteria>

<output>
Create `.planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-03-SUMMARY.md` when done.
</output>
