---
phase: 435-design-design-lock-the-point-unit-streak-path-packing-equiva
plan: 02
type: execute
wave: 2
depends_on: ["435-01"]
files_modified:
  - .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md
autonomous: true
requirements: [DESIGN-03]
must_haves:
  truths:
    - "The design-lock document has a DESIGN-03 section locking Sub.pendingFlip uint32→uint24 with a ~16.7M-whole-FLIP saturating clamp"
    - "The 72-bit accumulator slot is shown to repack as affiliateBase(32)+pendingFlip(24)+subStreakLatch(16)=72 with explicit bit arithmetic proving Sub stays exactly one 256-bit slot, 0 free"
    - "lootboxRngPendingFlip uint40 (DegenerusGameStorage:1525) is confirmed a separate, out-of-scope field"
  artifacts:
    - path: ".planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md"
      provides: "DESIGN-03 design-lock section with the 72-bit slot arithmetic"
      contains: "affiliateBase(32) + pendingFlip(24) + subStreakLatch(16)"
  key_links:
    - from: "435-DESIGN-LOCK.md DESIGN-03"
      to: "DegenerusGameStorage.sol:2169-2245"
      via: "Sub struct field-width repack, 256-bit-exact"
      pattern: "256.?bit|256-bit|one slot"
    - from: "435-DESIGN-LOCK.md DESIGN-03"
      to: "GameAfkingModule.sol pendingFlip accrual"
      via: "saturating clamp re-pin to uint24 ceiling"
      pattern: "uint24|16.7M|16,7|16_7"
---

<objective>
Author the DESIGN-03 (pendingFlip narrowing + 72-bit accumulator-slot repack) section of the v69 design-lock document, read-only against the byte-frozen v68 baseline (`contracts/` tree `e9a5fc24`).

Purpose: Lock the `Sub.pendingFlip` width change and prove the accumulator slot stays exactly one 256-bit slot after the repack — the packing decision the 436 IMPL diff implements and 437/438 verify (EIP-170 + layout golden). The 8 bits freed here are what plan 01's DESIGN-02 latch widening consumes. NO `contracts/*.sol` change.
Output: The `## DESIGN-03` section appended to `435-DESIGN-LOCK.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-CONTEXT.md
@.planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-01-SUMMARY.md

<interfaces>
<!-- Confirmed anchors from the byte-frozen v68 baseline (contracts/ tree e9a5fc24). -->

DegenerusGameStorage.sol — Sub struct (:2169-2245), exactly ONE 256-bit slot, 0 free:
  config (40b): dailyQuantity(8) + validThroughLevel(24) + reinvestPct(8) + flags(8)   [NOTE: doc comment says config (48 bits) at :2170; the field widths sum 8+24+8+8 = 48]
  per-sub stamp (48b): score(16) + amount(24, milli-ETH)                                [the slot-comment groups stamp as 48b incl. score(16)+amount(24)=40; the accumulator carries the balance]
  markers (96b): lastAutoBoughtDay(24) + lastOpenedDay(24) + afkCoveredThroughDay(24) + afkingStartDay(24)
  accumulator (72b): affiliateBase(32) + pendingFlip(32) + subStreakLatch(8)            [TODAY]
  - The executor MUST re-derive the EXACT current field-width sum from the struct source (:2169-2245) and the slot doc-comment (:2126-2168) and reconcile any comment-vs-field discrepancy in the design-lock — the 256-bit-exact claim is load-bearing.
  pendingFlip: uint32 + 100,000,000-whole-FLIP saturating clamp at the accrue write (:2237 + doc :2159-2162)

Accrue/clamp sites (GameAfkingModule.sol):
  - sub.pendingFlip = uint32(newOwed) ticket buyer-bonus (:861-863)
  - sub.pendingFlip = uint32(newOwed) slot-0 quest reward (:925-928)
  - _settle reads/zeros pendingFlip (:1098-1100)
  - the 100M clamp is the value-cap that binds before the uint32 ceiling

Out-of-scope field (DegenerusGameStorage.sol):
  - lootboxRngPendingFlip uint40 at :1525, bits [184:223], scaled /1e18, max ~1.1T FLIP — SEPARATE from Sub.pendingFlip, NOT narrowed (D-08)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the DESIGN-03 section — pendingFlip uint24 narrowing + 72-bit slot repack arithmetic</name>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol:2126-2168 (the accumulator-slot doc-comment: `affiliateBase(32) + pendingFlip(32) + subStreakLatch(8) = 72b`, the "EXACTLY one 256-byte slot, 0 free" claim, and the uint32/100M clamp note)
    - contracts/storage/DegenerusGameStorage.sol:2169-2245 (the full `Sub` struct — re-derive every field width: config, stamp, markers, accumulator — and total to confirm 256 bits)
    - contracts/storage/DegenerusGameStorage.sol:2237 (the `uint32 pendingFlip` field) and :2259-2261 (`_setStreakBase` — confirms subStreakLatch is the field whose widening consumes the freed 8 bits, cross-ref plan 01)
    - contracts/modules/GameAfkingModule.sol:861-863, :925-928, :1098-1100 (the pendingFlip accrue/clamp/settle sites)
    - contracts/storage/DegenerusGameStorage.sol:1525 (`lootboxRngPendingFlip` uint40 — confirm it is a distinct field, bits [184:223], scaled /1e18)
    - .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-CONTEXT.md (D-06, D-07, D-08 + the Claude's-discretion note on the exact uint24 clamp value)
  </read_first>
  <action>
    Append a `## DESIGN-03 — pendingFlip Narrowing + Accumulator Repack` section to `435-DESIGN-LOCK.md` recording D-06, D-07, D-08.

    Lock the narrowing (D-06): `Sub.pendingFlip` narrows `uint32 → uint24`, with its saturating clamp re-pinned to the uint24 ceiling `~16.7M whole FLIP` (16,777,215 = `2^24 - 1`). Justify against the realistic per-sub claimable bank: 16.7M whole FLIP is far above any realistic per-sub `pendingFlip` accrual (the running slot-0 quest reward + ticket-mode 10%/20% buyer bonus, whole FLIP, per delivered day), so the clamp can only ever UNDER-credit a pathological reinvest-whale, off the solvency path — identical risk shape to the current uint32+100M clamp. Record the exact accrue/clamp/settle edit surface: the `uint32(newOwed)` casts at `GameAfkingModule.sol:861-863` and `:925-928` become `uint24(...)` with the clamp re-pinned (the prior 100M-whole-FLIP clamp constant is replaced by the uint24 ceiling), and the `_settle` read at `:1098-1100` widens-back from uint24. Note in Claude's-discretion (D-06): the precise uint24 clamp constant value (exact `2^24-1 = 16,777,215` vs a rounded ~16.7M) is a 436 pick, justified against the realistic bank.

    Lock the repack (D-07) with **explicit bit arithmetic**. First re-state the CURRENT accumulator: `affiliateBase(32) + pendingFlip(32) + subStreakLatch(8) = 72 bits`. Then the NEW accumulator: `affiliateBase(32) + pendingFlip(24) + subStreakLatch(16) = 72 bits` — net-zero (`-8` from pendingFlip, `+8` to subStreakLatch). Produce a full `Sub`-struct field-width table (config / per-sub stamp / markers / accumulator) summing to **exactly 256 bits, 0 free**, BEFORE and AFTER, proving: no new cold slot, no field value-range violated, no slot collision. Reconcile any field-width vs slot-doc-comment discrepancy you find when re-deriving from source (the executor must total the actual field declarations, not just trust the comment). State EIP-170 is re-checked in 436 and the `forge inspect` storage-layout golden recapture (the MECH-02 v68 oracle) is the EXPECTED new golden handled in 438 REAUDIT-01, NOT a drift.

    Lock the out-of-scope confirmation (D-08): `lootboxRngPendingFlip` (uint40, `DegenerusGameStorage.sol:1525`, bits [184:223], scaled /1e18, max ~1.1T FLIP) is a SEPARATE field unrelated to `Sub.pendingFlip` and is NOT narrowed. Record the distinguishing facts (different type, different slot region, different scaling, different purpose) so 436 cannot confuse them.
  </action>
  <verify>
    <automated>grep -q 'DESIGN-03' .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md && grep -q 'affiliateBase(32) + pendingFlip(24) + subStreakLatch(16)' .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md && grep -Eq '16,777,215|16.7M|2\^24|2\*\*24' .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md && grep -q 'lootboxRngPendingFlip' .planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-DESIGN-LOCK.md</automated>
  </verify>
  <acceptance_criteria>
    - The `## DESIGN-03` section exists in `435-DESIGN-LOCK.md`.
    - `Sub.pendingFlip` uint32→uint24 with the ~16.7M (`2^24-1`) clamp is locked, justified against the realistic per-sub bank, with the accrue/clamp/settle edit sites named (`GameAfkingModule.sol:861-863`, `:925-928`, `:1098-1100`).
    - The new accumulator is recorded verbatim as `affiliateBase(32) + pendingFlip(24) + subStreakLatch(16) = 72` and shown net-zero vs the current `...pendingFlip(32) + subStreakLatch(8)`.
    - A full Sub-struct field-width table (BEFORE + AFTER) sums to exactly 256 bits / 0 free, with any field-vs-comment discrepancy reconciled from source.
    - EIP-170 re-check + the layout-golden recapture are flagged as handled in 438 (expected new golden, not drift).
    - `lootboxRngPendingFlip` uint40 (`:1525`) is confirmed distinct and out of scope with its distinguishing facts.
    - No `contracts/*.sol` file is modified.
  </acceptance_criteria>
  <done>The DESIGN-03 section is authored with the uint24 narrowing + clamp, the net-zero 72-bit repack with 256-bit-exact slot arithmetic, and the lootboxRngPendingFlip out-of-scope confirmation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design-lock doc → 436 IMPL diff | A packing error here propagates into the only `.sol` change of the milestone (no code ships in 435) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-435-04 | Tampering | accumulator-slot repack | mitigate | Explicit BEFORE/AFTER field-width arithmetic proving the net-zero repack keeps `Sub` at exactly one 256-bit slot (0 free) — no new cold slot, no slot collision |
| T-435-05 | Denial of service | pendingFlip uint24 value-range | mitigate | The ~16.7M (`2^24-1`) clamp is justified above any realistic per-sub bank; the clamp can only under-credit (off the solvency path), identical risk shape to the current uint32+100M clamp; saturation (not overflow) is asserted and handed to 437 TST-02 |
| T-435-06 | Spoofing | lootboxRngPendingFlip vs Sub.pendingFlip confusion | mitigate | Record the distinguishing facts (type uint40, slot bits [184:223], /1e18 scaling, distinct purpose) so 436 cannot narrow the wrong field |
| T-435-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in a read-only docs-only phase; no install task exists |
</threat_model>

<verification>
- `435-DESIGN-LOCK.md` has a `## DESIGN-03` section.
- The pendingFlip uint32→uint24 narrowing + ~16.7M clamp + accrue/settle edit sites are recorded.
- The 72-bit repack is shown net-zero with a full 256-bit-exact Sub-struct field-width table (BEFORE + AFTER).
- `lootboxRngPendingFlip` is confirmed distinct/out-of-scope.
- No `contracts/*.sol` file was modified (read-only phase).
</verification>

<success_criteria>
DESIGN-03 is design-locked in `435-DESIGN-LOCK.md` with the narrowing, the clamp, the proven net-zero 256-bit-exact slot arithmetic, and the out-of-scope field confirmation — an executor-ready packing spec for 436. Zero Solidity source changed.
</success_criteria>

<output>
Create `.planning/phases/435-design-design-lock-the-point-unit-streak-path-packing-equiva/435-02-SUMMARY.md` when done.
</output>
