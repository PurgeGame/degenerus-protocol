---
phase: "114"
plan: "02"
type: auto
autonomous: true
wave: 2
depends_on: ["114-01"]
requirements: [UNIT-12, COV-01, COV-02, COV-03, ATK-01, ATK-02, ATK-03, ATK-04, ATK-05, VAL-01, VAL-02, VAL-03, VAL-04]
---

# Plan 114-02: Mad Genius Attack Report

## Objective
Execute the Mad Genius adversarial analysis on every state-changing function in DegenerusVault.sol (including DegenerusVaultShare) and WrappedWrappedXRP.sol. Build complete call trees, map every storage write, check for cached-local-vs-storage overwrites, and attack from all 10 angles.

## Context
- @.planning/phases/114-vault-wwxrp/114-CONTEXT.md
- @.planning/phases/114-vault-wwxrp/114-RESEARCH.md
- @.planning/ULTIMATE-AUDIT-DESIGN.md
- @audit/unit-12/COVERAGE-CHECKLIST.md
- @contracts/DegenerusVault.sol
- @contracts/WrappedWrappedXRP.sol
- @audit/KNOWN-ISSUES.md

## Tasks

### Task 1: Attack Analysis - CRITICAL Tier Functions
type="auto"

Perform full Mad Genius analysis on CRITICAL-tier functions:
- `DegenerusVault::burnCoin` (L749) + `_burnCoinFor` (L762)
- `DegenerusVault::burnEth` (L816) + `_burnEthFor` (L833)

For each: complete call tree with line numbers, storage write map (full tree), cached-local-vs-storage check, 10-angle attack analysis. Special focus on:
- Share math correctness (rounding direction, first-deposit, full-burn)
- Refill mechanism weaponization
- Re-entrancy via external calls (stETH transfer, ETH send, coinToken transfer)
- Interaction between claimable winnings and share calculation

### Task 2: Attack Analysis - HIGH Tier Functions
type="auto"

Full Mad Genius analysis on HIGH-tier functions:
- `DegenerusVault::deposit` (L454)
- `WrappedWrappedXRP::unwrap` (L290)
- `DegenerusVault::gamePurchaseDeityPassFromBoon` (L536)

Special focus on:
- deposit: virtual BURNIE escrow accounting, stETH pull
- unwrap: undercollateralized race, CEI pattern, wXRP transfer reentrancy
- deity pass: complex ETH flow with balance check + winnings claim

### Task 3: Attack Analysis - MEDIUM Tier Functions
type="auto"

Full Mad Genius analysis on MEDIUM-tier functions:
- `DegenerusVault::gamePurchase` (L489)
- `DegenerusVault::gameDegeneretteBetEth` (L569)
- `DegenerusVault::wwxrpMint` (L723)
- `WrappedWrappedXRP::mintPrize` (L342)
- `WrappedWrappedXRP::vaultMintTo` (L363)
- `WrappedWrappedXRP::donate` (L314)
- `WrappedWrappedXRP::burnForGame` (L384)
- `DegenerusVault::_syncCoinReserves` (L980)

### Task 4: Attack Analysis - LOW Tier + VaultShare Functions
type="auto"

Full Mad Genius analysis on remaining Category B functions:
- All DegenerusVaultShare functions (constructor, approve, transfer, transferFrom, vaultMint, vaultBurn)
- All DegenerusVault LOW-tier proxy functions (gameAdvance through jackpotsClaimDecimator)
- DegenerusVault constructor, receive()
- WrappedWrappedXRP ERC20 functions (approve, transfer, transferFrom)

For proxy functions: verify access control, trace call far enough to confirm no vault state corruption from callback.

Compile all findings into `audit/unit-12/ATTACK-REPORT.md`.

**Done criteria:**
- Every Category B function has full analysis section
- Call trees are FULLY expanded with line numbers
- Storage writes mapped for every function in every call tree
- Cached-local-vs-storage check present for every function
- Every finding has verdict: VULNERABLE / INVESTIGATE / SAFE
- Output: `audit/unit-12/ATTACK-REPORT.md`

## Verification
- [ ] ATTACK-REPORT.md exists in audit/unit-12/
- [ ] Every Category B function from COVERAGE-CHECKLIST has a section
- [ ] Call trees include line numbers for every call
- [ ] Storage write maps are complete
- [ ] Cached-local-vs-storage checks present everywhere
- [ ] All 10 attack angles considered for critical functions
- [ ] No "similar to above" shortcuts

## Output
- `audit/unit-12/ATTACK-REPORT.md`
