---
phase: 481-event-abi-surface-view-selectors-gated-contract-diff-3
plan: 01
type: gated-contract-diff
autonomous: true
wave: 1
depends_on: [480-01, 480-02]
requirements: [EVT-01, EVT-02, EVT-03, EVT-04, EVT-05, EVT-06]
---

# Phase 481 / Plan 01 — Event/ABI surface + view selectors + docs (gated contract diff #3, ABI not behavior)

## Objective

Align the **public event + view surface** with the `entries` convention shipped internally in Phase 480. This is an ABI change (event names / fields / 3 view-selector names + emitted-unit normalization) but **NOT a behavior change**: no logic, value, ordering, or emitted-value changes — only event/field/selector LABELS. Per the owner decision (FULL rename) + ledger §2B / §10.2 / §10.5.

@.planning/v75-grounding/v75.0-ticket-entry-disambiguation-ledger.md (§2B, §10.2, §10.4, §10.5, §10.6, §10.7)

## Constraints

- contracts/ left UNCOMMITTED (gated diff captured to `481-CONTRACT-DIFF.patch`). test/ + docs committed autonomously.
- KEEP the activation-queue mechanism selectors and the "Ticket Jackpot" mechanism event/selector NAMES (only their entry-count FIELDS rename). KEEP `LootBoxOpened.futureTickets` (scaled-WHOLE, unit-correct).
- Comments describe what IS (no phase IDs / history) per CLAUDE.md.

---

## Rename map (derived from frozen post-480 source @ `39919230`)

### A. Event NAME renames (topic0 CHANGES) + their field renames

| File:line (decl) | OLD event | NEW event | field renames |
|---|---|---|---|
| Storage:550 | `TicketsQueued` | `EntriesQueued` | `quantity` → `entries` |
| Storage:557 | `TicketsQueuedScaled` | `EntriesQueuedScaled` | `quantityScaled` → `entriesScaled` |
| Storage:564 | `TicketsQueuedRange` | `EntriesQueuedRange` | `ticketsPerLevel` → `entriesPerLevel` |
| Mint:165 | `TicketsBought` | `EntriesBought` | `ticketQuantity` → `entryQuantityScaled` (§10.6) |
| Degenerette:87 | `FullTicketResolved` | `DegeneretteResolved` | `ticketCount` → `spinCount`, `resultTicket` → `resultTraits` |
| Degenerette:103 | `FullTicketResult` | `DegeneretteResult` | `ticketIndex` → `spinIndex`, `playerTicket` → `playerTraits` |

> emit-arg locals already carry the renamed names from 480 (`entries`/`entriesScaled`/`entriesPerLevel`/`entryQuantityScaled`/`spinCount`/`playerTraits`/`firstResultTraits`); only the event NAME at the emit site + the decl/NatSpec change here.

### B. Event FIELD-only renames (event NAME KEPT — mechanism/win events; topic0 UNCHANGED, types identical)

| File:line (decl) | event (NAME kept) | field renames |
|---|---|---|
| Jackpot:78 | `JackpotTicketWin` (mechanism KEEP) | `ticketLevel` → `entryLevel`, `ticketCount` → `entryCount`, `ticketIndex` → `entryIndex` |
| Jackpot:63 | `JackpotEthWin` | `ticketIndex` → `entryIndex` |
| Jackpot:89 | `JackpotFlipWin` | `ticketIndex` → `entryIndex` |
| Lootbox:91 | `LootBoxWhalePassJackpot` | `tickets` → `entriesPerLevel` |
| FoilPack:112 | `FoilMatchClaimed` | `ticketIndex` → `foilSlotIndex` (NOT `entryIndex` — it is a draw-slot 0-3) |

### C. View SELECTOR renames (interface + impl + caller lockstep; selectors CHANGE)

| OLD selector | NEW selector | refs (decl / impl / callers) |
|---|---|---|
| `ticketsOwedView` | `entriesOwedView` | IDegenerusGame:115 (decl); DegenerusGame:2088 (impl). No on-chain callers. |
| `sampleTraitTicketsAtLevel` | `sampleTraitEntriesAtLevel` | IDegenerusGame:327 (decl); DegenerusGame:2429 (impl); DegenerusJackpots:389 (caller) |
| `getTickets` | `getEntries` | DegenerusGame:2510 (impl) — facade-only, no interface decl, no on-chain callers |

### KEEP (do NOT rename)

- `LootBoxOpened.futureTickets` (scaled-WHOLE ×QTY_SCALE, unit-correct — §10.5 / R2).
- Event NAMES: `JackpotTicketWin`, `JackpotEthWin`, `JackpotFlipWin`, `LootBoxWhalePassJackpot`, `FoilMatchClaimed`, `LootBoxOpened` (win/mechanism events).
- Mechanism selectors: `processTicketBatch`, `processFutureTicketBatch`, `payDailyJackpotCoinAndTickets`, `initPerpetualTickets`, `sampleFarFutureTickets`, `getPlayerPurchases`.
- The Jackpot mechanism cluster + activation-queue cluster + everything 480 shipped.

### FLAGGED for AM review (judgment calls — renames are cheap to adjust)

1. **Degenerette event target names** — `FullTicketResolved→DegeneretteResolved`, `FullTicketResult→DegeneretteResult` (per §2B). Consistent-form alternative: `DegeneretteBetResolved` / `DegeneretteSpinResult`. These interplay with Phase 482's `_packFullTicketBet→_packDegeneretteBet` repack.
2. **`JackpotTicketWin` NAME kept** (only fields renamed) — per task KEEP-set + §10.1 mechanism-name principle. The ROADMAP §481 criterion #1 wording was `JackpotTicketWin→JackpotEntryWin`; resolved to KEEP-name as the mechanism. Flag if AM prefers the name rename.
3. **FoilPack `claimFoilMatch`/`claimFoilMatchMany` params** `ticketIndex`/`ticketIndexes` (+ `_tryClaimFoilMatch`, `NoClaimableMatch` comment) remain `ticketIndex` — they feed the now-`foilSlotIndex` field. Selector-safe 480-internal GAP, out of strict 481 (event-field) scope. Rename to `foilSlotIndex*` in a follow-up if AM wants full consistency.

---

## Tasks

1. **Apply the rename** across contracts/: A (event names + fields + emit-site names + NatSpec), B (event fields + NatSpec), C (view selectors + interface + caller + NatSpec). Regenerate ABIs (`npx hardhat compile`).
2. **Sweep test/** for every renamed event NAME / selector / field — both source-string assertions (FIX-05 class: `.includes`/`indexOf`/`getEvent("…")`/`iface`/keccak topics) AND runtime ABI consumers (`event.args.<field>`, `.queryFilter`, decode-by-name). Regenerate committed ABI artifacts (`deployments/*-abis/*.json`). Update `docs/JACKPOT-EVENT-CATALOG.md` + `docs/JACKPOT-PAYOUT-REFERENCE.md`.
3. **Verify:** `npx hardhat compile` green; `npm test` green (≥ the 480-02 floor of 1362 passing, modulo renamed literals); targeted `forge test --match-contract` green for affected suites; no stale old event/field/selector name under contracts/ or test/.
4. **Capture** `481-CONTRACT-DIFF.patch`; commit test/ + plan/summary docs (NOT contracts/). Write `481-01-SUMMARY.md`.

## Success criteria

- [ ] All A/B/C renames applied; ABIs regenerated; KEEP-set intact.
- [ ] `npm test` green; targeted forge suites green; no stale name under contracts/ or test/.
- [ ] contracts/ UNCOMMITTED + patch captured; test/+docs committed; hooks/guard untouched; STATE.md/ROADMAP.md untouched.
