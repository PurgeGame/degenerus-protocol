---
phase: 380-foundation-test-fix-green-baseline
plan: 04
type: execute
wave: 2
depends_on: [380-01, 380-02, 380-03]
files_modified:
  - test/REGRESSION-BASELINE-v62.md
autonomous: true
requirements: [FOUND-06]

must_haves:
  truths:
    - "A full forge run at c4d48008 shows 0 failing deterministic test names (any residual is a documented carried bucket-A non-deterministic invariant, reproduced at the baseline)"
    - "The Hardhat deterministic suite is run (or its limitation explicitly documented) and any residual is characterized as not-a-v62-regression"
    - "A GREEN full-suite baseline is recorded in REGRESSION-BASELINE-v62.md, superseding the carried-red v61 ledger — future regressions caught by '0 failures', not a by-name non-widening diff"
  artifacts:
    - path: "test/REGRESSION-BASELINE-v62.md"
      provides: "The green full-suite baseline record for subject c4d48008 (the audit oracle)"
      min_lines: 40
  key_links:
    - from: "REGRESSION-BASELINE-v62.md green baseline"
      to: "subject c4d48008 contracts/ fingerprint"
      via: "recorded fingerprint + green forge counts"
      pattern: "c4d48008"
---

<objective>
Establish + RECORD a GREEN full-suite baseline (forge + Hardhat, 0 failures) at subject c4d48008,
superseding the carried-red `test/REGRESSION-BASELINE-v61.md` ledger (533/183/103 baseline ceiling,
711/66/103 v61-HEAD non-widening). After Plans 01-03 repair the slot-drift, event-schema, deity,
invariant-seeding and untracked-file debt, this is the FINAL serial gate that proves the regression
net is whole and writes the new oracle the council sweeps (382+) reproduce findings against.

Purpose: the whole point of the foundation — replace "a large red count is expected, certify via a
by-name non-widening diff" with "0 failures is the signal." Depends on ALL repair plans (01+02+03).

Output: a full green forge run + Hardhat disposition, recorded in REGRESSION-BASELINE-v62.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# The ledger being SUPERSEDED — read it for the carried bucket-A non-deterministic invariants that
# are legitimately allowed to remain red (reproduced at the baseline with byte-identical messages):
#   invariant_allGapDaysBackfilled, invariant_rngUnlockedAfterSwap, invariant_stallRecoveryValid
#   (test/fuzz/invariant/VRFPathInvariants.inv.t.sol) — §7. Also the documented Hardhat limitation
#   (§6): `npm test` globs test/adversarial/*.test.js which is absent → MODULE_NOT_FOUND before any
#   spec loads (env-not-v62). Run the runnable subset (test/unit, test/edge) explicitly instead.
@test/REGRESSION-BASELINE-v61.md

# foundry.toml: default profile [fuzz] runs=1000 seed=0xdeadbeef (deterministic); [invariant]
# runs=256 depth=128 fail_on_revert=false. via_ir=true.
</context>

<landmine>
The Hardhat suite compiles on run and `hardhat compile` REGENERATES `contracts/ContractAddresses.sol`,
breaking the forge fixture. Sequence the runs so the forge baseline is captured on a clean fixture:
run forge FIRST, then any hardhat. After ANY hardhat invocation, restore the fixture
(`git checkout HEAD -- contracts/ContractAddresses.sol`) and re-verify `git status --porcelain
contracts/` is empty AND `forge build` is still clean before recording the green baseline.
Comparison is always BY NAME, never by raw count.
</landmine>

<tasks>

<task type="auto">
  <name>Task 1: Run the full forge suite at c4d48008 and drive any residual deterministic red to green</name>
  <files>(no file edits expected — verification + triage; if a residual deterministic red is a slot/schema miss missed by 01-03, fix it in the owning test file)</files>
  <read_first>
    - test/REGRESSION-BASELINE-v61.md §3 (the 172-name carried union) + §7 (the 3 carried VRFPath bucket-A invariants reproduced at baseline) — the ONLY reds allowed to remain, and only if they reproduce at the c4d48008 contract subject
    - The 380-01/02/03 SUMMARYs (what was repaired + any reds those plans explicitly carried forward)
  </read_first>
  <action>
    Run `forge clean &amp;&amp; forge build` (clean), then the FULL forge suite: `forge test`. Collect the
    failing test NAMES (dedupe across fuzz seeds/params). For each failing name: if it is a
    deterministic `test*` red caused by a slot/event/semantics miss that Plans 01-03 should have
    caught, fix it in the owning test file now (same techniques: re-derive the slot from
    `forge inspect`, align to the current event/field). If it is one of the documented carried
    bucket-A non-deterministic `invariant_*` reds (VRFPathInvariants — allGapDaysBackfilled /
    rngUnlockedAfterSwap / stallRecoveryValid), CONFIRM it reproduces at the c4d48008 subject (it is
    a pre-existing ghost-counter property, not a v62 regression) and record it as a documented carried
    exception — do NOT force it green by editing the contract (contracts are read-only here). Iterate
    until every deterministic `test*` name is green. Do NOT run hardhat in this task (forge-only —
    keep the fixture clean). Record the contracts/ fingerprint (`git rev-parse HEAD:contracts` or a
    sha256 of the tree) so the baseline is pinned to the exact subject.
  </action>
  <verify>
    <automated>forge clean &amp;&amp; forge build 1>/dev/null 2>&amp;1; forge test 2>&amp;1 | grep -E "Suite result|failed" | tail -40; [ -z "$(git status --porcelain contracts/)" ] &amp;&amp; echo CONTRACTS_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `forge test` shows 0 failing deterministic `test*` names. The ONLY permitted residual reds are
      the documented carried bucket-A `invariant_*` non-deterministic exceptions, each CONFIRMED to
      reproduce at the c4d48008 subject and each named in the SUMMARY.
    - `git status --porcelain contracts/` empty (no hardhat run in this task; ContractAddresses.sol intact).
    - The contracts/ fingerprint at c4d48008 is captured for the baseline record.
  </acceptance_criteria>
  <done>The full forge suite is green at c4d48008 except for documented carried bucket-A invariants confirmed pre-existing; contracts/ byte-untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Run the Hardhat deterministic suite + record the green baseline (FOUND-06)</name>
  <files>test/REGRESSION-BASELINE-v62.md</files>
  <read_first>
    - test/REGRESSION-BASELINE-v61.md §6 + §7 (the documented Hardhat limitation: the `npm test` script globs test/adversarial/*.test.js which is absent at the subject → MODULE_NOT_FOUND before any spec loads; run the runnable test/unit + test/edge subset explicitly instead; test:stat is probabilistic and excluded)
    - Task 1's forge result + fingerprint (the primary green baseline)
  </read_first>
  <action>
    Run the deterministic Hardhat subset explicitly (avoid the broken `npm test` adversarial glob):
    `npx hardhat test test/unit/*.test.js test/edge/*.test.js` (exclude `npm run test:stat` — it is
    probabilistic chi²/EV, not part of the regression ceiling). Capture passing/failing/pending
    counts and the failing NAMES. Triage each failure: a green-foundation goal means 0 failing in
    this deterministic subset; if a residual JS failure is a pre-existing family unrelated to the
    v62 subject (e.g. an affiliate-cap / pool-split / access-control family the v61 ledger §7 already
    documented), confirm it is not accounting-insolvency or RNG-freeze and record it as a carried
    corroborating exception, otherwise fix it in the owning test file. If the Hardhat environment
    cannot run at all (the documented adversarial-glob / module-resolution limitation persists even
    for the explicit subset), record that limitation explicitly and rely on the forge by-name green
    as the PRIMARY baseline (the v61 ledger's stated "forge is primary, Hardhat corroborating"
    allowance). After ANY hardhat run, restore the fixture (`git checkout HEAD -- contracts/ContractAddresses.sol`)
    and re-verify `git status --porcelain contracts/` empty + `forge build` clean. Then WRITE
    `test/REGRESSION-BASELINE-v62.md`: state subject c4d48008 + the captured contracts/ fingerprint,
    the GREEN forge counts (passed / 0-deterministic-failed / skipped) with the carried bucket-A
    invariant exceptions named, the Hardhat subset result (or documented limitation), and an explicit
    statement that this SUPERSEDES REGRESSION-BASELINE-v61.md's carried-red by-name ledger — going
    forward, regressions are caught by "0 failures" against this green baseline, not a non-widening
    diff. Comparison BY NAME, never by count.
  </action>
  <verify>
    <automated>test -f test/REGRESSION-BASELINE-v62.md &amp;&amp; grep -q "c4d48008" test/REGRESSION-BASELINE-v62.md &amp;&amp; grep -qiE "supersede" test/REGRESSION-BASELINE-v62.md &amp;&amp; grep -qiE "green|0 (deterministic )?fail" test/REGRESSION-BASELINE-v62.md; git checkout HEAD -- contracts/ContractAddresses.sol 2>/dev/null; [ -z "$(git status --porcelain contracts/)" ] &amp;&amp; echo CONTRACTS_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `test/REGRESSION-BASELINE-v62.md` exists, names subject c4d48008 + the contracts/ fingerprint,
      records the GREEN forge counts (0 deterministic failing names) with the carried bucket-A
      invariant exceptions enumerated, and the Hardhat subset result or its documented limitation.
    - The doc explicitly states it SUPERSEDES REGRESSION-BASELINE-v61.md and that future regressions
      are caught by "0 failures", not a by-name non-widening diff.
    - `git status --porcelain contracts/` empty (ContractAddresses.sol restored after hardhat);
      `forge build` clean.
  </acceptance_criteria>
  <done>The Hardhat deterministic subset is run (or its limitation documented), and a GREEN full-suite baseline is recorded in REGRESSION-BASELINE-v62.md superseding the carried-red v61 ledger.</done>
</task>

</tasks>

<threat_model>
N/A — test-only + a baseline doc; no `contracts/*.sol` modification, no new attack surface, no
production behavior change. Subject c4d48008 is read-only (the fixture is restored after any hardhat
compile).
</threat_model>

<verification>
- `forge test` shows 0 failing deterministic `test*` names (only documented carried bucket-A
  invariants remain, confirmed pre-existing at c4d48008).
- `test/REGRESSION-BASELINE-v62.md` records the green baseline, names c4d48008 + the fingerprint,
  and states it supersedes the v61 ledger.
- `git status --porcelain contracts/` empty; `forge build` clean.
</verification>

<success_criteria>
A GREEN full-suite baseline (forge 0-deterministic-failures + the Hardhat deterministic disposition)
is established at subject c4d48008 and recorded in REGRESSION-BASELINE-v62.md, superseding the
carried-red v61 ledger so future regressions are caught by "0 failures"; contracts/ is byte-untouched.
</success_criteria>

<output>
Create `.planning/phases/380-foundation-test-fix-green-baseline/380-04-SUMMARY.md` when done.
</output>
