---
phase: 380-foundation-test-fix-green-baseline
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fuzz/helpers/SolvencyObligations.sol
  - test/fuzz/invariant/DegeneretteBet.inv.t.sol
  - test/fuzz/ActivityScoreStreakGas.t.sol
  - test/gas/AdvanceStageWorstCaseGas.t.sol
autonomous: true
requirements: [FOUND-04, FOUND-05]

must_haves:
  truths:
    - "SolvencyObligations reads prizePoolPendingPacked via a slot re-attested against c4d48008 (confirmed slot 11) or a named accessor"
    - "DegeneretteBet's invariant_solvencyUnderDegenerette is seeded (handlers/targetSender) and passes deterministically — characterized as test-infra, not a contract solvency finding"
    - "The two untracked gas-probe files are committed-or-removed by an explicit decision; obsolete/superseded SKIP-marked tests deleted; per-fix PoCs consolidated"
  artifacts:
    - path: "test/fuzz/helpers/SolvencyObligations.sol"
      provides: "prizePoolPendingPacked read at the c4d48008-attested slot"
      contains: "PRIZE_POOL_PENDING_PACKED_SLOT"
  key_links:
    - from: "SolvencyObligations PRIZE_POOL_PENDING_PACKED_SLOT"
      to: "forge inspect DegenerusGame storageLayout prizePoolPendingPacked (c4d48008)"
      via: "slot re-attestation"
      pattern: "PRIZE_POOL_PENDING_PACKED_SLOT"
---

<objective>
Two test-infra cleanups that round out the green net:
(FOUND-04) re-attest the `prizePoolPendingPacked` slot in the `SolvencyObligations` test helper
(hard-coded `PRIZE_POOL_PENDING_PACKED_SLOT = 11`, pinned at v55) against c4d48008, and SEED the
unseeded `[invariant]` `DegeneretteBet::invariant_solvencyUnderDegenerette` so it runs against a
real action sequence instead of an empty/garbage one. The CONTRACT solvency is fine — this is
pure test-infra; do NOT flag it as a contract finding.
(FOUND-05) commit-or-remove the two untracked gas-probe files
(`test/fuzz/ActivityScoreStreakGas.t.sol`, `test/gas/AdvanceStageWorstCaseGas.t.sol`); delete
obsolete/superseded SKIP-marked tests; consolidate per-fix PoCs.

Purpose: an unseeded flaky invariant and untracked/obsolete files pollute the "0 failures" signal
Plan 04 establishes. File set is disjoint from 01 and 02 → Wave 1 parallel.

Output: a seeded, slot-attested DegeneretteBet invariant + a deliberate disposition of the
untracked/obsolete test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Confirmed at c4d48008: prizePoolPendingPacked is at slot 11 (UNCHANGED from the v55 pin — the
# field's slot held across v56-v61 + the forgiving-funding add). The PRIMARY FOUND-04 defect at
# this subject is the UNSEEDED [invariant], not a wrong slot — but re-attest the slot anyway
# (deep_work_rule: derive from forge inspect, don't assume).
@.planning/milestones/v61.0-phases/378-tst-proving-tests-rng-freeze-solvency/378-01-RECALIBRATION-KEY.md

# foundry.toml [invariant] block: runs=256 depth=128 fail_on_revert=false - the harness must
# register handlers via targetContract/targetSelector/targetSender so the fuzzer drives real txns.
</context>

<landmine>
Do NOT run `hardhat compile --force` / `npx hardhat compile` in this plan (regenerates
ContractAddresses.sol, breaks the forge fixture). This plan is forge-only. If contracts/ becomes
dirty, restore: `git checkout HEAD -- contracts/ContractAddresses.sol`.
</landmine>

<tasks>

<task type="auto">
  <name>Task 1: Re-attest the SolvencyObligations slot + seed the DegeneretteBet invariant (FOUND-04)</name>
  <files>test/fuzz/helpers/SolvencyObligations.sol, test/fuzz/invariant/DegeneretteBet.inv.t.sol</files>
  <read_first>
    - test/fuzz/helpers/SolvencyObligations.sol — `PRIZE_POOL_PENDING_PACKED_SLOT = 11` (line 42) + `pendingPools()` (the slot-11 vm.load of the packed `[future&lt;&lt;128 | next]` buffer, lines 69-72)
    - test/fuzz/invariant/DegeneretteBet.inv.t.sol — setUp() (line 22) + the targetContract registrations (lines 31-33) + `invariant_solvencyUnderDegenerette` (line 39) + the SolvencyObligations.obligations(game) call (line 46) — check whether targetSender / targetSelector / a bounded handler is registered (the unseeded symptom: the invariant passes vacuously or reverts on garbage state)
    - Run `forge inspect DegenerusGame storageLayout` and confirm prizePoolPendingPacked's slot (expected 11 at c4d48008)
  </read_first>
  <action>
    First confirm via `forge inspect DegenerusGame storageLayout` that prizePoolPendingPacked is at
    slot 11 at c4d48008 (it is — record the confirmation in the SUMMARY). If a named accessor for
    the pending buffer exists on the contract, prefer routing the helper read through it; otherwise
    keep the slot-11 vm.load but annotate it as re-attested-against-c4d48008 (drop the "pinned at
    v55" staleness wording). Then SEED the invariant: in DegeneretteBet.inv.t.sol ensure the
    handlers driving sub/buy/bet/advance actions are registered so the 256-run/128-depth fuzzer
    actually exercises a reachable action sequence (targetContract for each handler is present at
    lines 31-33 — verify each handler EXPOSES the right public action selectors, add targetSelector
    allow-lists if the fuzzer is calling no-op/view selectors, and add a targetSender if the actions
    require a funded/authorized caller). The goal: `invariant_solvencyUnderDegenerette` runs against
    non-empty state and asserts `balance + stETH >= obligations` holds. Characterize explicitly in
    the SUMMARY that any prior flakiness was test-infra (stale slot / unseeded), NOT a contract
    solvency defect (per FOUND-04 — do not raise a contract finding).
  </action>
  <verify>
    <automated>forge test --match-contract "DegeneretteBet" 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-contract DegeneretteBet` exits 0 and `invariant_solvencyUnderDegenerette`
      runs with a non-zero call count (seeded — `show_metrics` reports calls greater than 0, not a
      vacuous pass).
    - SolvencyObligations.sol's prizePoolPendingPacked read is re-attested against c4d48008 (slot 11
      confirmed, or routed through a named accessor); no "pinned at v55" staleness claim remains.
    - The SUMMARY states the slot-11 confirmation and that the fix is test-infra (not a contract finding).
  </acceptance_criteria>
  <done>The DegeneretteBet solvency invariant is seeded + passes deterministically with a c4d48008-attested pending-buffer read; characterized as test-infra.</done>
</task>

<task type="auto">
  <name>Task 2: Commit-or-remove the untracked gas-probes + delete obsolete SKIP-marked tests (FOUND-05)</name>
  <files>test/fuzz/ActivityScoreStreakGas.t.sol, test/gas/AdvanceStageWorstCaseGas.t.sol</files>
  <read_first>
    - test/fuzz/ActivityScoreStreakGas.t.sol (untracked — `git status` shows `??`) — read it; decide commit-vs-delete on whether it compiles/passes at c4d48008 and adds non-duplicate coverage
    - test/gas/AdvanceStageWorstCaseGas.t.sol (untracked — the isolated-stage gas harness AUDIT-V62-PLAN section 5a flags as "useful reference; decide commit or delete"; note Phase 384 will build the END-TO-END advanceGame harness that supersedes the isolated-stage approach)
    - Run `grep -rln "vm.skip(true)" test/` to enumerate SKIP-marked tests; for each, check its header/NatSpec for a "superseded by" / "obsolete" citation (the REQUIREMENTS note: KeeperNonBrick supersession cites)
  </read_first>
  <action>
    For each untracked gas-probe: run it (`forge test --match-path &lt;file&gt;`). If it compiles and
    passes at c4d48008 and provides coverage not duplicated elsewhere, COMMIT it (it is test-only,
    agent-committable, no contract gate). If it is a stale WIP, a duplicate of an existing harness,
    or it references slots/signatures that no longer exist and adds nothing the recalibrated suites
    cover, DELETE it. Record the decision + rationale for BOTH files in the SUMMARY (an explicit
    commit-or-remove decision IS the deliverable — do not leave them untracked). For
    AdvanceStageWorstCaseGas specifically: note that Phase 384 builds the end-to-end advanceGame gas
    harness; if this isolated-stage probe is kept, keep it as a labeled reference and ensure it is
    green, otherwise delete it as superseded-by-design. Then enumerate `vm.skip(true)`-marked tests;
    DELETE any whose own NatSpec/comment cites supersession or obsolescence (e.g. KeeperNonBrick
    supersession cites); leave SKIP-marked tests that are intentionally-pending (a real future gate)
    in place but note them in the SUMMARY. Consolidate obvious per-fix PoC duplicates only where two
    files prove the identical property (do not over-merge — when in doubt, leave separate). Do NOT
    run hardhat.
  </action>
  <verify>
    <automated>UNTRACKED=$(git status --porcelain test/ | grep -E '^\?\?.*(ActivityScoreStreakGas|AdvanceStageWorstCaseGas)' | wc -l); [ "$UNTRACKED" -eq 0 ] &amp;&amp; echo PROBES_RESOLVED || echo PROBES_STILL_UNTRACKED</automated>
  </verify>
  <acceptance_criteria>
    - Neither gas-probe is left untracked: `git status --porcelain test/ | grep -E '\?\?.*(ActivityScoreStreakGas|AdvanceStageWorstCaseGas)'` returns nothing (each was either committed/staged or deleted).
    - If kept, `forge test --match-path test/fuzz/ActivityScoreStreakGas.t.sol` and/or
      `forge test --match-path test/gas/AdvanceStageWorstCaseGas.t.sol` exit 0.
    - The SUMMARY records the explicit commit-or-remove decision + rationale for both files and lists
      every deleted obsolete SKIP-marked test (with its supersession cite) and every intentionally
      retained SKIP.
  </acceptance_criteria>
  <done>Both untracked gas-probes have an explicit committed-or-removed disposition; obsolete SKIP-marked tests are deleted with cited rationale; per-fix PoCs consolidated where identical.</done>
</task>

</tasks>

<threat_model>
N/A — test-only changes; no `contracts/*.sol` modification, no new attack surface, no production
behavior change. FOUND-04 is explicitly pure test-infra (the contract solvency is sound — do not
raise a contract finding).
</threat_model>

<verification>
- `forge test --match-contract DegeneretteBet` exits 0 with a seeded (non-vacuous) invariant.
- `git status --porcelain test/` shows no `??` for the two named gas-probes.
- `git status --porcelain contracts/` empty.
</verification>

<success_criteria>
The DegeneretteBet solvency invariant is seeded + green with a c4d48008-attested pending-buffer
read (characterized as test-infra); the two untracked gas-probes have a deliberate committed-or-
removed disposition; obsolete SKIP-marked tests are deleted; contracts/ is byte-untouched.
</success_criteria>

<output>
Create `.planning/phases/380-foundation-test-fix-green-baseline/380-03-SUMMARY.md` when done.
</output>
