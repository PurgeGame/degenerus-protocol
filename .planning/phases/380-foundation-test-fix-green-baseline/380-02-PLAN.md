---
phase: 380-foundation-test-fix-green-baseline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fuzz/V55FreezeDeterminism.t.sol
  - test/fuzz/V55RevertFreeEvCap.t.sol
  - test/fuzz/V56FreezeSolvency.t.sol
  - test/edge/LootboxAutoResolveRegression.test.js
  - test/unit/LootboxWholeBurnieFloor.test.js
  - test/unit/EventSurfaceUnification.test.js
  - test/unit/LootboxAutoResolveSilentColdBust.test.js
  - test/unit/LootboxWholeTicket.test.js
  - test/edge/GameOver.test.js
  - test/unit/SecurityEconHardening.test.js
autonomous: true
requirements: [FOUND-02, FOUND-03]

must_haves:
  truths:
    - "Every test that hashes/parses LootBoxOpened uses the current 7-arg signature (no `day` arg)"
    - "The deity-refund tests assert the c4d48008 deityPassPricePaid + min(pricePaid,20e) semantics, not the removed deityPassPurchasedCount / deityPassPaidTotal / FIX-05 behavior"
    - "The named event-schema-delta + deity storage-collapse suites pass at subject c4d48008"
  artifacts:
    - path: "test/fuzz/V56FreezeSolvency.t.sol"
      provides: "LootBoxOpened topic-hash matching the current 7-arg signature"
      contains: "LootBoxOpened(address,uint48,uint256,uint24,uint32,uint256,bool)"
    - path: "test/unit/SecurityEconHardening.test.js"
      provides: "Deity refund test using deityPassPricePaid semantics"
  key_links:
    - from: "test LootBoxOpened topic-hash / arg-list parsing"
      to: "contracts/modules/DegenerusGameLootboxModule.sol event LootBoxOpened (7 args, no day)"
      via: "event signature string"
      pattern: "LootBoxOpened\\(address,uint48,uint256,uint24,uint32,uint256,bool\\)"
    - from: "test deity-refund assertions"
      to: "contracts deityPassPricePaid + GameOverModule refund min(pricePaid,20e)"
      via: "renamed storage field"
      pattern: "deityPassPricePaid"
---

<objective>
Refresh two stale-test-debt classes that do NOT involve raw storage slots:
(FOUND-02) event-schema-delta — the `LootBoxOpened` event dropped its `day` argument (commit
`4cb9ccbf` "lootbox event day cleanup"); tests that hard-code the old 8-arg topic hash or parse an
8-positional-arg list now mismatch the current 7-arg signature.
(FOUND-03) v60 whale/pass storage-collapse — `test/edge/GameOver.test.js` +
`test/unit/SecurityEconHardening.test.js` reference the REMOVED `deityPassPurchasedCount` /
`deityPassPaidTotal` and the dropped FIX-05 "refund clears count" behavior; the contract now uses
`deityPassPricePaid` with a `min(pricePaid, 20e18)` early-gameover refund cap.

Purpose: these reds are part of the broken net the council needs green. Both classes are
schema/semantics drift, NOT slot drift — keep them in one plan, separate from the slot work (01)
and the invariant/cleanup work (03). File sets are disjoint from 01 and 03 → Wave 1 parallel.

Output: the named event-schema + deity-refund suites refreshed to the c4d48008 source-of-truth.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# SOURCE OF TRUTH for FOUND-02 — the current LootBoxOpened event (7 args, NO day):
#   event LootBoxOpened(address indexed player, uint48 indexed lootboxIndex, uint256 amount,
#                       uint24 futureLevel, uint32 futureTickets, uint256 burnie, bool roundedUp)
#   → topic-0 = keccak256("LootBoxOpened(address,uint48,uint256,uint24,uint32,uint256,bool)")
@contracts/modules/DegenerusGameLootboxModule.sol

# SOURCE OF TRUTH for FOUND-03 — deityPassPricePaid + the refund cap:
@contracts/storage/DegenerusGameStorage.sol
@contracts/modules/DegenerusGameGameOverModule.sol
@contracts/modules/DegenerusGameWhaleModule.sol
</context>

<landmine>
⚠ `npx hardhat compile` regenerates `contracts/ContractAddresses.sol` and breaks the forge fixture.
The Hardhat (`.test.js`) suites in this plan run via `npx hardhat test <file>` which compiles —
that is expected and fine for running JS tests, BUT after any hardhat run verify
`git status --porcelain contracts/` is empty; if ContractAddresses.sol regenerated, restore it
with `git checkout HEAD -- contracts/ContractAddresses.sol` before finishing.
</landmine>

<tasks>

<task type="auto">
  <name>Task 1: Refresh the LootBoxOpened event-schema-delta tests (FOUND-02)</name>
  <files>test/fuzz/V55FreezeDeterminism.t.sol, test/fuzz/V55RevertFreeEvCap.t.sol, test/fuzz/V56FreezeSolvency.t.sol, test/edge/LootboxAutoResolveRegression.test.js, test/unit/LootboxWholeBurnieFloor.test.js, test/unit/EventSurfaceUnification.test.js, test/unit/LootboxAutoResolveSilentColdBust.test.js, test/unit/LootboxWholeTicket.test.js</files>
  <read_first>
    - contracts/modules/DegenerusGameLootboxModule.sol — the `event LootBoxOpened(...)` declaration (the 7-arg source of truth) AND the `emit LootBoxOpened(...)` call site (~L1189) for the exact positional order
    - test/fuzz/V56FreezeSolvency.t.sol — currently hashes `keccak256("LootBoxOpened(address,uint48,uint32,uint256,uint24,uint32,uint256,bool)")` (8 args, the stale `uint32` day in position 3) — the canonical stale instance
    - test/unit/LootboxWholeBurnieFloor.test.js — asserts the emit "must supply 8 positional args" + "the 7th arg" — stale arg-count assertions
    - test/unit/EventSurfaceUnification.test.js — builds the LootBoxOpened fragment + asserts a topic-0 hash
  </read_first>
  <action>
    The current event is 7 args: `LootBoxOpened(address indexed player, uint48 indexed lootboxIndex,
    uint256 amount, uint24 futureLevel, uint32 futureTickets, uint256 burnie, bool roundedUp)` →
    canonical type-list `LootBoxOpened(address,uint48,uint256,uint24,uint32,uint256,bool)`. In the
    forge files, replace any stale 8-arg topic-hash string (e.g. the `...uint48,uint32,uint256...`
    8-tuple in V56FreezeSolvency:71) with the 7-arg canonical string, and fix any struct/decoder
    that carries a `day` field for the LootBoxOpened payload (drop the `day` member + adjust the
    abi.decode tuple to the 7 fields, indexed player+lootboxIndex excluded from data). In the JS
    files, update the ethers interface fragment / arg-count assertions: the emit now supplies 7
    positional args (not 8); re-point any "Nth arg" assertion to the correct position in the 7-arg
    list (amount=3rd, futureLevel=4th, futureTickets=5th, burnie=6th, roundedUp=7th); update any
    hard-coded topic-0 hash to the recomputed value. Run each suite. For JS files, run
    `npx hardhat test <file>` and after the run verify contracts/ untouched (restore
    ContractAddresses.sol if regenerated — see landmine).
  </action>
  <verify>
    <automated>forge test --match-contract "V55FreezeDeterminism|V55RevertFreeEvCap|V56FreezeSolvency" 2>&amp;1 | tail -15 &amp;&amp; npx hardhat test test/unit/EventSurfaceUnification.test.js test/unit/LootboxWholeBurnieFloor.test.js test/unit/LootboxWholeTicket.test.js test/unit/LootboxAutoResolveSilentColdBust.test.js test/edge/LootboxAutoResolveRegression.test.js 2>&amp;1 | tail -20; git checkout HEAD -- contracts/ContractAddresses.sol 2>/dev/null; [ -z "$(git status --porcelain contracts/)" ] &amp;&amp; echo CONTRACTS_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-contract "V55FreezeDeterminism|V55RevertFreeEvCap|V56FreezeSolvency"`
      shows 0 failing deterministic test* names.
    - The 5 named JS suites pass under `npx hardhat test`.
    - No touched file references the 8-arg LootBoxOpened signature or a `day` field on the
      LootBoxOpened payload: `grep -rn "LootBoxOpened(address,uint48,uint32" test/` returns nothing,
      and the JS arg-count assertions reference 7 positional args.
    - `git status --porcelain contracts/` empty after the hardhat runs.
  </acceptance_criteria>
  <done>Every LootBoxOpened topic-hash / arg-list parse matches the current 7-arg signature; the named suites pass; contracts/ untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Update the v60 whale/pass deity storage-collapse tests (FOUND-03)</name>
  <files>test/edge/GameOver.test.js, test/unit/SecurityEconHardening.test.js</files>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol — `deityPassPricePaid` mapping (the field that REPLACED deityPassPurchasedCount / deityPassPaidTotal)
    - contracts/modules/DegenerusGameGameOverModule.sol — the early-gameover deity refund: `refund = deityPassPricePaid[owner]` then capped (the `min(pricePaid, 20e18)` semantics) at ~L111
    - contracts/modules/DegenerusGameWhaleModule.sol — `deityPassPricePaid[buyer] = uint96(totalPrice)` at purchase (~L605) — how the field is populated
    - test/unit/SecurityEconHardening.test.js — the "FIX-05: Deity pass refund clears deityPassPurchasedCount" + "FIX-06 no voluntary refund" describes referencing the removed field (~L175-297)
    - test/edge/GameOver.test.js — the "deity pass refund at level 0" describe + the header comment citing `deityPassPaidTotal` (~L32-337)
  </read_first>
  <action>
    The contract no longer has `deityPassPurchasedCount` or `deityPassPaidTotal`; it tracks
    `deityPassPricePaid[buyer]` (a uint96 = the ETH price paid) and the early-gameover (level < 10)
    refund is `min(deityPassPricePaid[owner], 20e18)` per owner (the FIX-05 "refund clears count"
    behavior no longer exists; the boon-ownership gate is now the HAS_DEITY_PASS bit, not a count).
    In SecurityEconHardening.test.js: rewrite the FIX-05 describe to assert the c4d48008 semantics —
    a deity holder's early-gameover refund equals min(price-paid, 20 ETH) (a single pass bought at
    the standard price → ~20 ETH cap), drop any assertion that a "purchasedCount" is read or
    cleared; keep/adjust FIX-06 (no voluntary refund function) since that still holds. Update the
    comment at :175-177 to describe what IS (price-paid refund), not the removed count. In
    GameOver.test.js: update the header comment (:32-35) and the "deity pass refund at level 0"
    assertions to the price-paid + 20 ETH cap model; "flat 20 ETH refund per pass at level 0" stays
    valid for a standard-priced pass (the cap binds), but the SOURCE of the number is
    min(pricePaid,20e), not a per-count flat — assert via the claimable delta as the tests already
    do. Run via `npx hardhat test`; verify contracts/ untouched after (restore ContractAddresses.sol
    if regenerated — see landmine).
  </action>
  <verify>
    <automated>npx hardhat test test/edge/GameOver.test.js test/unit/SecurityEconHardening.test.js 2>&amp;1 | tail -20; git checkout HEAD -- contracts/ContractAddresses.sol 2>/dev/null; [ -z "$(git status --porcelain contracts/)" ] &amp;&amp; echo CONTRACTS_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `npx hardhat test test/edge/GameOver.test.js test/unit/SecurityEconHardening.test.js` passes
      (0 failing).
    - Neither file references the removed fields: `grep -n "deityPassPurchasedCount\|deityPassPaidTotal"
      test/edge/GameOver.test.js test/unit/SecurityEconHardening.test.js` returns nothing.
    - The deity-refund assertions express the `min(pricePaid, 20e18)` model (verifiable by the
      claimable-delta assertion + a comment naming deityPassPricePaid).
    - `git status --porcelain contracts/` empty after the hardhat run.
  </acceptance_criteria>
  <done>The deity-refund tests assert the c4d48008 deityPassPricePaid + min(pricePaid,20e) semantics and pass; the removed fields are gone; contracts/ untouched.</done>
</task>

</tasks>

<threat_model>
N/A — test-only changes; no `contracts/*.sol` modification, no new attack surface, no production
behavior change. (The deity-refund SEMANTICS being asserted are the already-shipped c4d48008
behavior; this plan only realigns the tests to it.)
</threat_model>

<verification>
- `forge test --match-contract "V55FreezeDeterminism|V55RevertFreeEvCap|V56FreezeSolvency"` 0 failing test* names.
- The 7 named JS suites pass under `npx hardhat test`.
- `grep -rn "LootBoxOpened(address,uint48,uint32" test/` and
  `grep -rn "deityPassPurchasedCount\|deityPassPaidTotal" test/edge/GameOver.test.js test/unit/SecurityEconHardening.test.js` both return nothing.
- `git status --porcelain contracts/` empty.
</verification>

<success_criteria>
All event-schema-delta tests use the current 7-arg LootBoxOpened signature; the deity storage-
collapse tests assert the deityPassPricePaid + min(pricePaid,20e) refund; every named suite passes
at c4d48008; contracts/ is byte-untouched.
</success_criteria>

<output>
Create `.planning/phases/380-foundation-test-fix-green-baseline/380-02-SUMMARY.md` when done.
</output>
