---
phase: 274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal
phase_number: 274
plan: 274-01
plan_id: 274-01
plan_number: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/modules/DegenerusGameLootboxModule.sol
  - contracts/interfaces/IDegenerusGameModules.sol
  - test/unit/LootboxWholeTicket.test.js
  - test/unit/LootboxConsolation.test.js
  - test/stat/LootboxBernoulliEv.test.js
  - test/edge/LootboxAutoResolveRegression.test.js
  - audit/FINDINGS-v39.0.md
  - .planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/274-01-ADVERSARIAL-LOG.md
  - .planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/274-01-SUMMARY.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/MILESTONES.md
  - .planning/PROJECT.md
autonomous: false
milestone: v39.0
milestone_name: Lootbox Whole-Ticket Rounding + WWXRP Consolation
deliverable: audit/FINDINGS-v39.0.md
phase_status: terminal
write_policy: "Multi-wave phase per D-274-CLOSURE-01 + D-274-APPROVAL-01. Wave 1: single batched USER-APPROVED contract commit (LBX-WT-01..05 + LBX-WX-01..04 + LBX-EVT-01..06). Wave 2: single batched USER-APPROVED test commit (TST-WT-01..07 + TST-WX-01..03 + TST-REG-01..04). Wave 3+: AGENT-COMMITTED atomic-per-task for audit/FINDINGS-v39.0.md, 274-01-ADVERSARIAL-LOG.md, 274-01-SUMMARY.md, REQUIREMENTS/ROADMAP/STATE/MILESTONES/PROJECT closure flips. Per feedback_never_preapprove_contracts.md, agent does NOT pre-approve any Wave 1 or Wave 2 commit — wait for explicit `approved` string. Final user-review gate before READ-only flip on audit/FINDINGS-v39.0.md per feedback_manual_review_before_push.md. Agent does NOT push."

requirements:
  - LBX-WT-01
  - LBX-WT-02
  - LBX-WT-03
  - LBX-WT-04
  - LBX-WT-05
  - LBX-WX-01
  - LBX-WX-02
  - LBX-WX-03
  - LBX-WX-04
  - LBX-EVT-01
  - LBX-EVT-02
  - LBX-EVT-03
  - LBX-EVT-04
  - LBX-EVT-05
  - LBX-EVT-06
  - TST-WT-01
  - TST-WT-02
  - TST-WT-03
  - TST-WT-04
  - TST-WT-05
  - TST-WT-06
  - TST-WT-07
  - TST-WX-01
  - TST-WX-02
  - TST-WX-03
  - TST-REG-01
  - TST-REG-02
  - TST-REG-03
  - TST-REG-04
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04
  - AUDIT-05
  - AUDIT-06
  - REG-01
  - REG-02
  - REG-03
  - REG-04

# Audit anchors (per CONTEXT.md <canonical_refs>)
baseline: 06623edb
baseline_signal: MILESTONE_V38_AT_HEAD_06623edb
v34_baseline: 6b63f6d4daf346a53a1d463790f637308ea8d555
v34_baseline_signal: MILESTONE_V34_AT_HEAD_6b63f6d4daf346a53a1d463790f637308ea8d555
phase_273_included_commits:
  - ff929948  # fix(273): BAF credit routing — day-D orphan + RngLocked predicate + jackpot-phase override + tests
  - e9807891  # test(273): BAF-ROUTE-06/07/08 expansion
  - e04d3333  # chore(273): phase SUMMARY
  - 1eb1ecb5  # docs: _livenessTriggered NatSpec clarification

# Closure signal (resolved by Wave 4 atomic-update per D-274-CLOSURE-01)
milestone_closure_signal: "MILESTONE_V39_AT_HEAD_<sha>"
milestone_closure_signal_resolution: "<sha> = `git rev-parse HEAD` AFTER Wave 3 final §9 attestation commit lands (the mutation-inclusive closure commit). Verbatim emission across 5 FINDINGS locations + 3 cross-document propagation targets (ROADMAP / STATE / MILESTONES) per D-274-CLOSURE-01."

# Locked-decision honor map (9 anchors from CONTEXT.md <locked_decisions>)
locked_decisions_honored:
  - id: D-274-MANUAL-ONLY-01
    where: "Wave 1 Task 1.2 (behavior gating), Wave 2 Task 2.4 (auto-resolve byte-equivalence)"
    note: "Manual-only Bernoulli + consolation + LootboxTicketRoll; gated by `index != type(uint48).max`. Auto-resolve paths unchanged."
  - id: D-274-NO-EVT-BREAK-01
    where: "Wave 1 Task 1.2 (scaledPre snapshot + _resolveLootboxCommon return-value preservation), Wave 2 Task 2.1 TST-WT-05"
    note: "LootBoxOpened.futureTickets + BurnieLootOpen.tickets emit SCALED on both paths. _resolveLootboxCommon returns scaledPre (not whole) so BurnieLootOpen.tickets at L658 stays scaled."
  - id: D-274-EVT-ROLL-01
    where: "Wave 1 Task 1.1 (event decl) + Task 1.2 (emit site), Wave 2 Task 2.1 TST-WT-06..07"
    note: "Minimal 4-field schema: `event LootboxTicketRoll(address indexed player, uint48 indexed lootboxIndex, uint32 preRollTickets, bool roundedUp);`. Declared on IDegenerusGameLootboxModule (in IDegenerusGameModules.sol) + LootboxModule contract event block."
  - id: D-274-EVT-INDEX-SENTINEL-01
    where: "Wave 1 Task 1.2 (sentinel passing) + Task 1.3 (4 callers updated)"
    note: "`type(uint48).max` = `0xFFFFFFFFFFFF` sentinel for auto-resolve. Dual-purpose: behavioral gate + non-emitted index."
  - id: D-274-WX-AMOUNT-01
    where: "Wave 1 Task 1.1 (constant), Wave 2 Task 2.2 TST-WX-03"
    note: "`uint256 private constant LOOTBOX_WWXRP_CONSOLATION = 1 ether;` — magnitude-equal to LOOTBOX_WWXRP_PRIZE."
  - id: D-274-BIT-SLICE-01
    where: "Wave 1 Task 1.2 (Bernoulli) + Task 1.4 (bit-allocation NatSpec), Wave 2 Task 2.1 TST-WT-03 chi²"
    note: "`uint16(seed >> 152) % uint16(TICKET_SCALE)` vs `uint16(frac)`. Consumes bits[152..167]; total 168/256 on manual paths only."
  - id: D-274-CLOSURE-01
    where: "Wave 3+ (audit deliverable + closure flips), final attestation commit"
    note: "Single-phase, multi-wave structure. Closure signal MILESTONE_V39_AT_HEAD_<sha> in §9c with 5 FINDINGS locations + 3 cross-document targets (ROADMAP / STATE / MILESTONES)."
  - id: D-274-APPROVAL-01
    where: "Wave 1 Task 1.5 (Wave 1 batched commit gate), Wave 2 Task 2.5 (Wave 2 batched commit gate)"
    note: "Per-commit explicit user `approved` string awaited before each batched commit. Per feedback_never_preapprove_contracts.md, agent must NOT pre-approve. Per feedback_manual_review_before_push.md, agent must NOT push."
  - id: D-274-ADVERSARIAL-01
    where: "Wave 3 Task 3.7 (3-skill PARALLEL spawn on finished §4 draft)"
    note: "Single-message PARALLEL spawn: `/contract-auditor` + `/zero-day-hunter` + `/economic-analyst`. `/degen-skeptic` OUT OF SCOPE per D-271-ADVERSARIAL-02 carry."
  - id: D-274-JACKPOT-OUT-01
    where: "Wave 1 (zero jackpot-module file in files_modified), audit §7 forward-cite ledger"
    note: "Jackpot ticket-award sites (DegenerusGameJackpotModule.sol lines 702 / 835 / 1005 / 2216) explicitly OUT OF SCOPE. Cosmetic × TICKET_SCALE cleanup + BAF Bernoulli + v36.0 ENT-05 xorshift refactor deferred to v40.0+."

# Goal-backward must-haves (per ROADMAP Phase 274 Success Criteria 1-18)
must_haves:
  truths:
    - "v39.0 phase goal: On MANUAL lootbox opens (`openLootBox` + `openBurnieLootBox`), `_resolveLootboxCommon` in `contracts/modules/DegenerusGameLootboxModule.sol` collapses scaled `futureTickets` to whole via a single Bernoulli round-up on `bits[152..167]` of the per-resolution seed, queues whole tickets via `_queueTickets`, pays `LOOTBOX_WWXRP_CONSOLATION = 1 ether` when whole=0 from non-zero scaled, and emits new additive `LootboxTicketRoll` event. Auto-resolve paths (`resolveLootboxDirect` + `resolveRedemptionLootbox`) UNCHANGED — still call `_queueTicketsScaled`, still produce `rem` byte residues, never emit `LootboxTicketRoll`, never pay consolation. (Ref: LBX-WT-01..03 + LBX-WX-01..03 + LBX-EVT-01..06 + D-274-MANUAL-ONLY-01)"
    - "`_resolveLootboxCommon` signature gains exactly one new parameter `uint48 index` placed immediately after `uint32 day` (parameter order: `address player, uint32 day, uint48 index, uint256 amount, uint24 targetLevel, uint24 currentLevel, uint256 seed, bool presale, bool allowWhalePass, bool allowLazyPass, bool emitLootboxEvent, bool allowBoons, uint256 distressEth, uint256 totalPackedEth`). 4 callers updated: `openLootBox` (L583-597) passes its `index` arg; `openBurnieLootBox` (L637-651) passes its `index` arg; `resolveLootboxDirect` (L679-693) passes `type(uint48).max` sentinel; `resolveRedemptionLootbox` (L714-728) passes `type(uint48).max` sentinel. (Ref: LBX-EVT-05 + D-274-EVT-INDEX-SENTINEL-01)"
    - "Manual-branch behavior gating: `bool manualOpen = (index != type(uint48).max);` evaluated at function entry (or just-in-time inside the `if (futureTickets != 0)` block). The existing distress-bonus computation block at L975-983 stays inside the `if (futureTickets != 0)` guard on BOTH paths (so small distress bonuses don't truncate to 0 on auto-resolve). After distress bonus, snapshot `uint32 scaledPre = futureTickets;` (LOCAL to the function; used by both the Bernoulli math and the `LootboxTicketRoll.preRollTickets` emit). Then branch: if `manualOpen` → Bernoulli + `_queueTickets` + (consolation OR queue) + `LootboxTicketRoll` emit; else → today's `_queueTicketsScaled(player, targetLevel, futureTickets, false)` call. (Ref: LBX-WT-02 + LBX-EVT-05)"
    - "Bernoulli implementation in manual branch: `uint32 whole = futureTickets / uint32(TICKET_SCALE); uint32 frac  = futureTickets % uint32(TICKET_SCALE); bool roundedUp = false; if (frac != 0 && (uint16(seed >> 152) % uint16(TICKET_SCALE)) < uint8(frac)) { unchecked { whole += 1; } roundedUp = true; }`. **CRITICAL non-mutation rule:** Do NOT reassign the function-scope `futureTickets` variable to `whole` until AFTER the `LootBoxOpened` emit (or use a separate `uint32 queueWhole = whole;` local for the queue call and leave function-scope `futureTickets` as the scaled value). The reason: `_resolveLootboxCommon` returns `futureTickets` (L1009) which is consumed by `openBurnieLootBox` at L658 as `BurnieLootOpen.tickets`, AND emitted by the function itself at L1004 as `LootBoxOpened.futureTickets` — both of those must continue emitting SCALED per D-274-NO-EVT-BREAK-01. (Ref: LBX-WT-02 + LBX-EVT-01 + D-274-NO-EVT-BREAK-01)"
    - "Manual-branch queue + consolation switch: if `whole > 0` → `_queueTickets(player, targetLevel, whole, false)` (emits `TicketsQueued(buyer, level, qty=whole)` via the storage helper at L562-589); else (whole == 0 from non-zero scaledPre) → `wwxrp.mintPrize(player, LOOTBOX_WWXRP_CONSOLATION)` and `emit LootBoxWwxrpReward(player, day, amount, LOOTBOX_WWXRP_CONSOLATION);` (reuse existing event signature at L127-132 — magnitude same as 10%-path WWXRP win at L1585-1595). Then AFTER the if/else, emit exactly once: `emit LootboxTicketRoll(player, index, scaledPre, roundedUp);`. The `LootboxTicketRoll` emit is OUTSIDE the if/else, but INSIDE the outer `if (futureTickets != 0)` guard so it only fires when the ticket-path produced non-zero pre-Bernoulli scaled. (Ref: LBX-WT-03 + LBX-WX-02..04 + LBX-EVT-03..06 + D-274-EVT-ROLL-01 + D-274-WX-AMOUNT-01)"
    - "Auto-resolve branch unchanged: when `!manualOpen` (sentinel passed), call `_queueTicketsScaled(player, targetLevel, futureTickets, false)` — byte-equivalent to v38.0 behavior at L984. NO Bernoulli, NO consolation predicate, NO `LootboxTicketRoll` emit. `TicketsQueuedScaled` is the only queue-event emitted (via the storage helper). (Ref: LBX-WT-03 + LBX-EVT-02 + D-274-MANUAL-ONLY-01 + D-274-AUTORESOLVE-OUT-01)"
    - "New private constant `uint256 private constant LOOTBOX_WWXRP_CONSOLATION = 1 ether;` declared immediately after `LOOTBOX_WWXRP_PRIZE` at L278 (sibling constant; same magnitude per D-274-WX-AMOUNT-01). (Ref: LBX-WX-01)"
    - "New event `LootboxTicketRoll(address indexed player, uint48 indexed lootboxIndex, uint32 preRollTickets, bool roundedUp)` declared at the event block immediately AFTER `LootBoxWwxrpReward` (currently L127-132; post-edit position ~L132) in `contracts/modules/DegenerusGameLootboxModule.sol`, AND on the `IDegenerusGameLootboxModule` interface inside `contracts/interfaces/IDegenerusGameModules.sol` at L264-314. Placement at L127-region (NOT L278) — L278 hosts the `LOOTBOX_WWXRP_PRIZE` constant block and is the placement for the new `LOOTBOX_WWXRP_CONSOLATION` constant only, NOT the event. Both `player` and `lootboxIndex` indexed for filter efficiency. (Ref: LBX-EVT-03 + D-274-EVT-ROLL-01)"
    - "Bit-allocation NatSpec at L838-847 updated: add new entry `bits[152..167]   fracRoundUp % 100      (_resolveLootboxCommon manual-path ticket whole-collapse; auto-resolve paths leave slice unread)` and update the total-consumption line from `Total primary-chunk consumption: 152 bits / 256 available.` to `Total primary-chunk consumption: 168 bits / 256 available (bits[152..167] consumed only on manual paths; auto-resolve paths leave the slice unread).`. (Ref: LBX-WT-04 + D-274-BIT-SLICE-01)"
    - "Storage layout byte-identical at v39.0 phase-close HEAD vs v38.0 baseline `06623edb`. Zero new admin entry points; zero new external mutation entry points; zero new modifiers; zero changes to storage-layout-relevant declarations. `_queueTicketsScaled` + `_rollRemainder` + `rem` byte in `ticketsOwedPacked` retained (mint-boost at `DegenerusGameMintModule.sol` L1142 + auto-resolve lootbox paths still use them). (Ref: LBX-WT-05 + D-274-MINTBOOST-OUT-01 + D-274-AUTORESOLVE-OUT-01)"
    - "RNG audit discipline per `feedback_rng_backward_trace.md` + `feedback_rng_commitment_window.md`: per-resolution `seed` derivation UNCHANGED (still `keccak256(abi.encode(rngWord, player, day, amount))` at the 4 caller sites — L554 `openLootBox`, L628 `openBurnieLootBox`, L673 `resolveLootboxDirect`, L708 `resolveRedemptionLootbox`). For manual paths, `rngWord` is committed via VRF before the player can call `openLootBox`/`openBurnieLootBox`, so `seed` is unknown-at-input-commitment. Bits[152..159] is a previously-unallocated slice of the same single-source-of-entropy primary chunk — no player-controllable input mutates between VRF commit and the lootbox open that affects bits[152..167] independently of the other 8 sub-roll consumers. (Ref: LBX-WT-02 + AUDIT-03)"
    - "Test coverage (manual-path whole-ticket): TST-WT-01 EV-neutrality property at >=10K seeds across scaled values {47, 99, 100, 147, 250, 1000, 9999} within ±0.5% of `scaledPre/100`; TST-WT-02 boundary cases (0/1/99/100/101/199/200); TST-WT-03 bit-slice independence chi² at >=10K seeds; TST-WT-04 `TicketsQueued` emission assertion (not `TicketsQueuedScaled`) on manual paths; TST-WT-05 `LootBoxOpened.futureTickets` SCALED-preservation on both paths; TST-WT-06 `LootboxTicketRoll` 4-lattice emission cases + 3 negative cases (DGNRS/BURNIE/WWXRP path; truncated-scaled-0; auto-resolve); TST-WT-07 field-consistency invariants (derived whole matches queued count; `preRollTickets == LootBoxOpened.futureTickets` same-tx; `lootboxIndex == index` arg; whole==0 ⇒ same-tx `LootBoxWwxrpReward`). (Ref: TST-WT-01..07)"
    - "Test coverage (consolation): TST-WX-01 cold-bust seed-forced trigger asserts `LootBoxWwxrpReward(player, day, amount, 1 ether)` + `wwxrp.balanceOf(player)` += 1 ether + zero `TicketsQueued` + one `LootboxTicketRoll` with whole=0; TST-WX-02 non-trigger predicate matrix (ticket-path-not-selected; scaled-truncated-from-start; whole>=1; auto-resolve always); TST-WX-03 magnitude assertion `LOOTBOX_WWXRP_CONSOLATION == LOOTBOX_WWXRP_PRIZE == 1 ether` (defensive drift catch). (Ref: TST-WX-01..03 + D-274-WX-AMOUNT-01)"
    - "Test coverage (regression): TST-REG-01 manual-only player+level queues no longer enter `_rollRemainder` at activation time (no `rem != 0` state in `ticketsOwedPacked[wk][player]`); TST-REG-02 mint-boost fractional path (`DegenerusGameMintModule.sol` L1142 `_queueTicketsScaled` callsite) still produces `rem` and still resolves via `_rollRemainder` (status-quo preservation for mint-boost retirement deferred to future milestone); TST-REG-03 auto-resolve lootbox paths byte-equivalent — `resolveLootboxDirect` + `resolveRedemptionLootbox` still produce `rem` byte residues, still resolve via `_rollRemainder`, emit `TicketsQueuedScaled`, NEVER emit `LootboxTicketRoll`, NEVER pay consolation; TST-REG-04 cross-mixing variance test — same player opens 5 manual + 3 auto-resolve at same future level; assert manual contributions are per-lootbox-Bernoulli (higher variance), auto-resolve contributions pool deterministically via `rem` byte. (Ref: TST-REG-01..04)"
    - "Single USER-APPROVED batched Wave 1 contract commit lands per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` covering LBX-WT-01..05 + LBX-WX-01..04 + LBX-EVT-01..06. Commit body includes full per-requirement attribution + bytecode delta + storage-layout grep proof + RNG backward-trace attestation. Single USER-APPROVED batched Wave 2 test commit lands covering TST-WT-01..07 + TST-WX-01..03 + TST-REG-01..04. (Ref: D-274-APPROVAL-01)"
    - "`audit/FINDINGS-v39.0.md` exists at phase close, FINAL READ-only (chmod 444 + `status: FINAL — READ-ONLY` + `read_only: true` in YAML), with all 9 sections populated. §3.A AUDIT-01 delta-surface table enumerates LBX-WT-01..05 + LBX-WX-01..04 + LBX-EVT-01..06 with hunk-level evidence + {NEW, MODIFIED_LOGIC, REFACTOR_ONLY} classification. §3.A includes explicit rows for the 4 Phase 273 pre-shipped commits (ff929948 + e9807891 + e04d3333 + 1eb1ecb5) as included-since-baseline mutations — surface-coverage attestation only, no F-39-NN finding eligible per D-274-BAF273-INCLUDE-01. §3.A also includes rows for the Phase 274 Wave 1 + Wave 2 batched commits. (Ref: AUDIT-01 + AUDIT-02)"
    - "§4 adversarial-sweep verdicts SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE for 8 surfaces per AUDIT-03: (a) EV-neutrality of Bernoulli collapse on manual paths vs cross-lootbox accumulation; (b) Bit-slice `[152..159]` independence from other primary-chunk consumers; (c) Consolation trigger predicate cannot fire from non-ticket-path OR auto-resolve path; (d) Storage layout byte-identical; (e) Auto-resolve paths byte-equivalent to v38.0 behavior; (f) Index gating discriminator routes manual vs auto-resolve with zero crossover; (g) `LootboxTicketRoll` field-consistency invariants hold across both manual callers; (h) Phase 273 BAF-routing surface coverage at included-baseline. Default zero F-39-NN finding blocks. (Ref: AUDIT-03)"
    - "3-skill PARALLEL adversarial pass per D-274-ADVERSARIAL-01 spawned via SINGLE message AFTER §4 draft is finished: `/contract-auditor` + `/zero-day-hunter` + `/economic-analyst`. NOT `/degen-skeptic`. Logged in `.planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/274-01-ADVERSARIAL-LOG.md` with 3 H2 sections + Disposition note. Zero residual FINDING_CANDIDATE default; if any → escalate to user per `feedback_wait_for_approval.md`. (Ref: AUDIT-04)"
    - "§5 regression appendix: REG-01 v38.0 closure `MILESTONE_V38_AT_HEAD_06623edb` NON-WIDENING surfaces (Degenerette + BURNIE coinflip post-Phase-273 BAF routing + Mint module boost + auto-resolve lootbox paths byte-identical for surfaces NOT in v39 manual-lootbox scope); REG-02 v34.0 closure NON-WIDENING (TraitUtils + `_pickSoloQuadrant` + JackpotBucketLib byte-identical); REG-03 EXC-01..03 NEGATIVE-scope at v39; EXC-04 NARROWS retained (BAF-jackpot-only; EntropyLib byte-identical) via backward-trace per `feedback_rng_backward_trace.md`; REG-04 prior-finding spot-check across audit/FINDINGS-v25.0..v38.0 for v39-touched function set (`_resolveLootboxCommon` manual-path additions + `_queueTickets` new lootbox callsite + auto-resolve byte-equivalence). (Ref: REG-01..04)"
    - "§6 KI gating walk: §6a Non-Promotion Ledger zero rows (default per D-272-KI-01 carry); §6b 4-row KI envelope re-verifications (EXC-01..03 NEGATIVE-scope at v39; EXC-04 RE_VERIFIED NARROWS retained); §6c verdict summary `0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_UNMODIFIED`. KNOWN-ISSUES.md UNMODIFIED at v39 phase-close HEAD unless adversarial pass surfaces FINDING_CANDIDATE requiring explicit user disposition. (Ref: AUDIT-05)"
    - "§8 Forward-Cite Closure emits zero forward-cites per D-274-FCITE-01 terminal-phase invariant. Grep recipe: `! grep -rE 'Phase (275|276|277)|v40\\.0|v41\\.0' audit/FINDINGS-v39.0.md .planning/phases/274-*/` returns no matches. Deferred items (mint-boost retirement, auto-resolve retirement, jackpot ticket-award sites + BAF Bernoulli + v36.0 ENT-05 xorshift) recorded in §7/§8 as deferred-to-future-milestone (no specific phase/version forward-cite). (Ref: D-274-FCITE-01 + D-274-JACKPOT-OUT-01 + D-274-MINTBOOST-OUT-01 + D-274-AUTORESOLVE-OUT-01)"
    - "§9 closure-attestation: §9.NN.i USER-APPROVED contracts (Wave 1 batched commit) + §9.NN.ii USER-APPROVED tests (Wave 2 batched commit) + §9.NN.iii AGENT-COMMITTED audit artifacts (Wave 3+ atomic-commit chain) + §9.NN.iv Phase 273 included-since-baseline commits (4 SHAs surfaced from v38 closure → v39 audit-subject HEAD). §9c emits closure signal `MILESTONE_V39_AT_HEAD_<sha>` verbatim in 5 FINDINGS locations (frontmatter `closure_signal:` + §2 Closure Verdict Summary + §9c trailing line + §9.NN attestation block + Executive Summary anchor sentence) + 3 cross-document targets (ROADMAP / STATE / MILESTONES). (Ref: AUDIT-06 + D-274-CLOSURE-01)"
    - "USER-APPROVED batched gates honored: Wave 1 contract diff presented at Task 1.5 — explicit `approved` string awaited before commit; Wave 2 test diff presented at Task 2.5 — explicit `approved` string awaited before commit; Final user-review gate at Wave 3 final task before READ-only flip on `audit/FINDINGS-v39.0.md`. Per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md`. (Ref: D-274-APPROVAL-01)"

  artifacts:
    - path: contracts/modules/DegenerusGameLootboxModule.sol
      provides: "MODIFIED in single Wave 1 USER-APPROVED batched commit. LBX-WX-01: new private constant `LOOTBOX_WWXRP_CONSOLATION = 1 ether` near L278. LBX-EVT-03: new event `LootboxTicketRoll(address indexed player, uint48 indexed lootboxIndex, uint32 preRollTickets, bool roundedUp)` declared near L127 in event block. LBX-EVT-05: `_resolveLootboxCommon` signature gains `uint48 index` parameter after `uint32 day`. LBX-WT-02..03 + LBX-WX-02 + LBX-EVT-06: inside `if (futureTickets != 0)` block (current L974-985), retain distress-bonus block (L976-983) on BOTH paths, snapshot `scaledPre`, then branch on `index != type(uint48).max`: manual → Bernoulli on `bits[152..167]` + `_queueTickets(player, targetLevel, whole, false)` (or consolation if whole=0) + emit `LootboxTicketRoll(player, index, scaledPre, roundedUp)`; auto-resolve → `_queueTicketsScaled(player, targetLevel, futureTickets, false)` (today's behavior, UNCHANGED). LBX-WT-04: bit-allocation NatSpec at L838-847 adds bits[152..167] entry + total-consumption line updated 152 → 168. LBX-EVT-05 + LBX-EVT-04: 4 callers updated — openLootBox (L583) passes `index`; openBurnieLootBox (L637) passes `index`; resolveLootboxDirect (L679) passes `type(uint48).max`; resolveRedemptionLootbox (L714) passes `type(uint48).max`. LBX-EVT-01 + D-274-NO-EVT-BREAK-01: function returns `futureTickets` as SCALED (NOT reassigned to whole) so `LootBoxOpened.futureTickets` at L1004 and `BurnieLootOpen.tickets` at L658 (consumer of return value) both continue emitting scaled — non-breaking."
      contains: "LOOTBOX_WWXRP_CONSOLATION"
      contains_alt: "event LootboxTicketRoll"
      contains_alt_2: "uint48 index"
      contains_alt_3: "scaledPre"
      forbidden: "0xFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFE"
    - path: contracts/interfaces/IDegenerusGameModules.sol
      provides: "MODIFIED. LBX-EVT-03: add `event LootboxTicketRoll(address indexed player, uint48 indexed lootboxIndex, uint32 preRollTickets, bool roundedUp);` declaration on the `IDegenerusGameLootboxModule` interface block at L264-314. No other changes."
      contains: "event LootboxTicketRoll"
    - path: test/unit/LootboxWholeTicket.test.js
      provides: "NEW. Covers TST-WT-01..07 manual-path whole-ticket assertions. EV-neutrality property (TST-WT-01 at >=10K seeds across scaled values {47, 99, 100, 147, 250, 1000, 9999} within ±0.5% of scaledPre/100); boundary cases (TST-WT-02 at 0/1/99/100/101/199/200); bit-slice chi² (TST-WT-03 at >=10K seeds); `TicketsQueued` emission (TST-WT-04); `LootBoxOpened.futureTickets` scaled-preservation (TST-WT-05); `LootboxTicketRoll` 4-lattice + 3 negative cases (TST-WT-06); field-consistency (TST-WT-07). Existing JS hardhat test infra used (see test/stat/LootboxEntropyDistribution.test.js + test/unit/DistressLootbox.test.js + test/edge/BafCreditRouting.test.js for patterns)."
      contains: "openLootBox"
      contains_alt: "LootboxTicketRoll"
    - path: test/unit/LootboxConsolation.test.js
      provides: "NEW. Covers TST-WX-01..03. Cold-bust seed-forced trigger asserts `LootBoxWwxrpReward(player, day, amount, 1 ether)` + balance increment + zero `TicketsQueued` + one `LootboxTicketRoll` with whole=0 derived (TST-WX-01); non-trigger predicate matrix (TST-WX-02 — ticket-path-not-selected / scaled-truncated-from-start / whole>=1 / auto-resolve always); magnitude assertion `LOOTBOX_WWXRP_CONSOLATION == LOOTBOX_WWXRP_PRIZE == 1 ether` (TST-WX-03 defensive drift catch). Seed-forcing strategy documented in test header — uses existing rngWord mocking pattern from test/unit/DistressLootbox.test.js + test/fuzz/LootboxRngLifecycle.t.sol."
      contains: "LOOTBOX_WWXRP_CONSOLATION"
      contains_alt: "LootBoxWwxrpReward"
    - path: test/stat/LootboxBernoulliEv.test.js
      provides: "NEW. Hosts TST-WT-01 + TST-WT-03 statistical assertions (extracted from test/unit/LootboxWholeTicket.test.js if it gets too heavy; planner's discretion is to keep all 7 TST-WT cases in one unit file UNLESS the EV property suite exceeds 2-minute mocha timeout). If split, this file holds the >=10K-seed EV-neutrality test + chi² bit-slice independence test. Mirrors the pattern of test/stat/LootboxEntropyDistribution.test.js for chi² infra reuse (verbatim re-declaration of `makeRng` / `CHI2_CRIT_05` / `wilsonHilfertyZ` from test/stat/TraitDistribution.test.js per Phase 266 STAT-03 reuse-existing-tooling discipline)."
      contains: "Bernoulli"
      contains_alt: "scaledPre"
    - path: test/edge/LootboxAutoResolveRegression.test.js
      provides: "NEW. Covers TST-REG-01..04. Manual-only player+level queues no longer enter `_rollRemainder` at activation (TST-REG-01); mint-boost fractional path still works (TST-REG-02 — uses `DegenerusGameMintModule._queueTicketsScaled` L1142 callsite); auto-resolve lootbox paths byte-equivalent (TST-REG-03 — `resolveLootboxDirect` + `resolveRedemptionLootbox` still produce `rem`, still resolve via `_rollRemainder`, emit `TicketsQueuedScaled`, NEVER emit `LootboxTicketRoll` or consolation); cross-mixing variance (TST-REG-04 — same player opens 5 manual + 3 auto-resolve at same future level, asserts variance posture). Existing test/edge/BafCreditRouting.test.js + test/edge/BackfillIdempotency.test.js patterns provide the auto-resolve fixture template."
      contains: "resolveLootboxDirect"
      contains_alt: "_rollRemainder"
      contains_alt_2: "TicketsQueuedScaled"
    - path: audit/FINDINGS-v39.0.md
      provides: "Single canonical v39.0 milestone-closure deliverable per D-274-FILES-01. 9-section shape mirroring audit/FINDINGS-v38.0.md skeleton. §1 Frontmatter (YAML with `closure_signal: MILESTONE_V39_AT_HEAD_<sha>` + `status: FINAL — READ-ONLY` + `read_only: true` + `supersedes: none`). §2 Executive Summary (Closure Verdict + Severity Counts D-274-SEV-01 5-bucket carry + D-08 + D-09 + Forward-Cite + Attestation Anchor). §3 Per-Phase Sections (§3a Phase 274 single-phase + §3.A AUDIT-01 delta-surface table with LBX-WT-01..05 + LBX-WX-01..04 + LBX-EVT-01..06 rows + 4 Phase-273-included-since-baseline rows + Wave-1 + Wave-2 commit rows + §3.B AUDIT-04 zero-new-state attestation + §3.C AUDIT-03 conservation re-proof). §4 F-39-NN Finding Blocks (default zero; 8-surface row table covering (a)..(h) per AUDIT-03). §5 Regression Appendix (§5a REG-01 v38.0 + §5b REG-02 v34.0 + §5c REG-04 prior-finding sweep + §5d Distribution Summary). §6 KI Gating Walk (§6a Non-Promotion Ledger zero-row + §6b 4-row KI Envelope Re-Verifications + §6c Verdict Summary `0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_UNMODIFIED`). §7 Prior-Artifact Cross-Cites. §8 Forward-Cite Closure (terminal-phase zero-emission verification). §9 Milestone Closure Attestation (§9a Verdict Distribution + §9b Attestation Block + §9c Closure Signal + §9.NN commit-readiness register: §9.NN.i Wave 1 USER-APPROVED contract commit + §9.NN.ii Wave 2 USER-APPROVED test commit + §9.NN.iii Wave 3+ AGENT-COMMITTED audit-artifact chain + §9.NN.iv Phase 273 included-since-baseline 4-commit list). FINAL READ-only at final wave gate."
      min_lines: 500
      contains: "MILESTONE_V39_AT_HEAD_"
      contains_alt: "## 4. F-39-NN Finding Blocks"
      contains_alt_2: "LBX-WT-02"
      contains_alt_3: "LootboxTicketRoll"
    - path: .planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/274-01-ADVERSARIAL-LOG.md
      provides: "Adversarial validation log capturing `/contract-auditor` + `/zero-day-hunter` + `/economic-analyst` outputs from Wave 3 single-message PARALLEL spawn on finished §4 draft. THREE H2 sections (`## /contract-auditor`, `## /zero-day-hunter`, `## /economic-analyst`) populated with each skill's red-team output against the v39 §4 inline draft. Disposition note appended below skill outputs documenting any disagreement + user disposition + KI promotion candidates (default zero). Mirrors v38.0 `272-01-ADVERSARIAL-LOG.md` 3-skill format."
      min_lines: 50
    - path: .planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/274-01-SUMMARY.md
      provides: "Phase-closure summary mirroring `272-01-SUMMARY.md` format (per-task atomic-commit log + per-REQ tally for 39 v39.0 requirements + cross-phase cross-cite density + project-feedback-rules-honored table + closure signal `MILESTONE_V39_AT_HEAD_<sha>` + Phase 273 included-since-baseline 4-commit log)."
      min_lines: 80
    - path: .planning/REQUIREMENTS.md
      provides: "MODIFIED. 39 v39.0 Phase 274 requirements flipped to Complete (or formally re-deferred per planner picks if any surface — LBX-02 carry already RE-DEFERRED-V40+ at planning time so no action needed at requirement level; mint-boost / auto-resolve / jackpot retirement formally deferred to future milestones with carry-forward register prose). Traceability table updated; v40.0+ carry-forward register opens."
      contains: "Complete"
    - path: .planning/ROADMAP.md
      provides: "MODIFIED. v39.0 milestone row flipped to ✅ shipped with closure signal `MILESTONE_V39_AT_HEAD_<sha>`. Phase 274 entry checkbox ticked. v38.0 collapsed-summary block carried forward."
      contains: "MILESTONE_V39_AT_HEAD_"
    - path: .planning/STATE.md
      provides: "MODIFIED. Milestone v39.0 marked closed; Last Shipped Milestone updated to v39.0 with full closure-signal block; v38.0 demoted to Prior Shipped Milestone. Active Milestone reverted to between-milestones state. Roadmap Overview table updated. Operator Next Steps updated."
      contains: "MILESTONE_V39_AT_HEAD_"
    - path: .planning/MILESTONES.md
      provides: "MODIFIED. v39.0 entry prepended to top with closure-signal recorded; v38.0 demoted one position. Includes phase shape (single-phase), commit shape (1 USER-APPROVED contract + 1 USER-APPROVED test + N AGENT-COMMITTED audit), §4 surface count (8), REG row count (4), KI verdict, closure signal, Phase 273 included-since-baseline 4-commit log."
      contains: "MILESTONE_V39_AT_HEAD_"
    - path: .planning/PROJECT.md
      provides: "MODIFIED. v39.0 accumulated-context entry appended (decisions log + completed-milestone entry). Mirrors v38.0 / v37.0 / v36.0 entry shape. Updates Current Milestone block to between-milestones state."

  key_links:
    - from: "contracts/modules/DegenerusGameLootboxModule.sol _resolveLootboxCommon (L860)"
      to: "contracts/storage/DegenerusGameStorage.sol _queueTickets (L562) + _queueTicketsScaled (L596)"
      via: "manual branch (`index != type(uint48).max`) calls `_queueTickets(player, targetLevel, whole, false)` post-Bernoulli; auto-resolve branch calls `_queueTicketsScaled(player, targetLevel, futureTickets, false)` (status quo). Both helpers honor `_livenessTriggered()` + `rngLockedFlag` guards with `rngBypass=false` (identical to today's behavior — no new bypass path)"
      pattern: "_queueTickets\\(|_queueTicketsScaled\\(|_livenessTriggered\\(|rngLockedFlag"
    - from: "contracts/modules/DegenerusGameLootboxModule.sol _resolveLootboxCommon (L860, post-edit signature with `uint48 index`)"
      to: "4 callers: openLootBox (L583-597) + openBurnieLootBox (L637-651) + resolveLootboxDirect (L679-693) + resolveRedemptionLootbox (L714-728)"
      via: "manual callers pass real `index`; auto-resolve callers pass `type(uint48).max` sentinel. Behavioral gating + event-emission discriminator per D-274-MANUAL-ONLY-01 + D-274-EVT-INDEX-SENTINEL-01"
      pattern: "_resolveLootboxCommon\\(|type\\(uint48\\)\\.max|openLootBox|openBurnieLootBox|resolveLootboxDirect|resolveRedemptionLootbox"
    - from: "contracts/modules/DegenerusGameLootboxModule.sol new event LootboxTicketRoll declaration (event block ~L127 + interface)"
      to: "emit site inside `_resolveLootboxCommon` manual branch (post-Bernoulli, after queue/consolation if/else) + IDegenerusGameLootboxModule interface declaration in contracts/interfaces/IDegenerusGameModules.sol L264-314"
      via: "additive declaration in both files; emit fires exactly once per manual lootbox open that lands on ticket-path with non-zero pre-Bernoulli scaled; auto-resolve paths never emit. preRollTickets snapshotted as `scaledPre` before Bernoulli reassignment"
      pattern: "event LootboxTicketRoll|emit LootboxTicketRoll|address indexed player, uint48 indexed lootboxIndex"
    - from: "audit/FINDINGS-v39.0.md §3.A AUDIT-01 delta-surface table"
      to: "contracts/modules/DegenerusGameLootboxModule.sol (HEAD <v39-close-sha>) + contracts/interfaces/IDegenerusGameModules.sol (HEAD <v39-close-sha>) + git diff 06623edb..HEAD"
      via: "every changed declaration classified {NEW: LootboxTicketRoll event + LOOTBOX_WWXRP_CONSOLATION constant + uint48 index parameter; MODIFIED_LOGIC: _resolveLootboxCommon if (futureTickets != 0) block + 4 callers; REFACTOR_ONLY: bit-allocation NatSpec at L838-847} with hunk-level evidence per requirement ID + 4 Phase-273-included-since-baseline rows (ff929948 + e9807891 + e04d3333 + 1eb1ecb5)"
      pattern: "DegenerusGameLootboxModule\\.sol|_resolveLootboxCommon|LootboxTicketRoll|LOOTBOX_WWXRP_CONSOLATION|NEW|MODIFIED_LOGIC|REFACTOR_ONLY|ff929948|e9807891|e04d3333|1eb1ecb5"
    - from: "audit/FINDINGS-v39.0.md §4 8-surface adversarial row table"
      to: "v39.0 mutated bodies + test/unit/LootboxWholeTicket.test.js (TST-WT-01..07) + test/unit/LootboxConsolation.test.js (TST-WX-01..03) + test/edge/LootboxAutoResolveRegression.test.js (TST-REG-01..04)"
      via: "8 rows (a..h) with verdict {SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE} + grep recipe + line cite + prose justification + TST-WT-01 + TST-WT-03 empirical cross-cite + RNG backward-trace cite per `feedback_rng_backward_trace.md` + RNG commitment-window cite per `feedback_rng_commitment_window.md`"
      pattern: "Surface \\([a-h]\\)|SAFE_BY_DESIGN|SAFE_BY_STRUCTURAL_CLOSURE|EV-neutrality|Bernoulli|bit-slice"
    - from: "audit/FINDINGS-v39.0.md §6b KI envelope re-verifications row 4 (EXC-04 NARROWS-retained)"
      to: "KNOWN-ISSUES.md (UNMODIFIED at v39) + contracts/libraries/EntropyLib.sol (byte-identical at v39 HEAD)"
      via: "backward-trace cite per `feedback_rng_backward_trace.md` + EntropyLib byte-identity preserves BAF-jackpot-only NARROWS scope from v36.0; zero EntropyLib mutation at v39 (Phase 274 mutates only LootboxModule + interface) means commitment-window check is degenerate-PASS per `feedback_rng_commitment_window.md`"
      pattern: "EXC-04|NARROWS|BAF jackpot|RE_VERIFIED|UNMODIFIED|EntropyLib"
    - from: "audit/FINDINGS-v39.0.md §9c"
      to: "/gsd-complete-milestone for v39.0"
      via: "closure signal `MILESTONE_V39_AT_HEAD_<sha>` triggers milestone-completion handler per D-274-CLOSURE-01"
      pattern: "MILESTONE_V39_AT_HEAD_"
    - from: "audit/FINDINGS-v39.0.md §9.NN commit-readiness register"
      to: "1 USER-APPROVED Wave 1 contract commit + 1 USER-APPROVED Wave 2 test commit + Wave 3+ AGENT-COMMITTED audit/closure commits + 4 Phase 273 included-since-baseline commits"
      via: "§9.NN.i cites Wave 1 contract commit subject; §9.NN.ii cites Wave 2 test commit subject; §9.NN.iii cites Wave 3+ atomic-commit chain; §9.NN.iv lists 4 Phase 273 commit SHAs surfaced from v38 closure → v39 audit-subject HEAD"
      pattern: "feat\\(274\\)|test\\(274\\)|docs\\(274\\)|audit\\(274\\)|chore\\(274\\)|ff929948|e9807891|e04d3333|1eb1ecb5"

tags:
  - audit
  - findings-consolidation
  - milestone-closure
  - terminal-phase
  - v39-deliverable
  - lootbox
  - whole-ticket
  - bernoulli
  - wwxrp-consolation
  - additive-event
  - LBX-WT-01
  - LBX-WT-02
  - LBX-WT-03
  - LBX-WT-04
  - LBX-WT-05
  - LBX-WX-01
  - LBX-WX-02
  - LBX-WX-03
  - LBX-WX-04
  - LBX-EVT-01
  - LBX-EVT-02
  - LBX-EVT-03
  - LBX-EVT-04
  - LBX-EVT-05
  - LBX-EVT-06
  - TST-WT-01
  - TST-WT-02
  - TST-WT-03
  - TST-WT-04
  - TST-WT-05
  - TST-WT-06
  - TST-WT-07
  - TST-WX-01
  - TST-WX-02
  - TST-WX-03
  - TST-REG-01
  - TST-REG-02
  - TST-REG-03
  - TST-REG-04
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04
  - AUDIT-05
  - AUDIT-06
  - REG-01
  - REG-02
  - REG-03
  - REG-04
  - adversarial-validation
  - contract-auditor-spawn
  - zero-day-hunter-spawn
  - economic-analyst-spawn
  - phase-273-included-since-baseline
---

<objective>
Single-phase v39.0 milestone-closure patch mirroring v36.0 Phase 266 + v38.0 Phase 272 precedent. Add manual-path Bernoulli whole-ticket collapse + WWXRP cold-bust consolation + additive `LootboxTicketRoll` event to `contracts/modules/DegenerusGameLootboxModule.sol` `_resolveLootboxCommon` (LBX-WT-01..05 + LBX-WX-01..04 + LBX-EVT-01..06; MANUAL CALLERS ONLY per D-274-MANUAL-ONLY-01); produce manual-path test coverage + auto-resolve byte-equivalence regression + cross-mixing variance test (TST-WT-01..07 + TST-WX-01..03 + TST-REG-01..04); produce single-canonical `audit/FINDINGS-v39.0.md` 9-section terminal deliverable with closure signal `MILESTONE_V39_AT_HEAD_<sha>` emitted in §9c (5 FINDINGS locations + 3 cross-document targets); 3-skill PARALLEL adversarial pass logged in `274-01-ADVERSARIAL-LOG.md`; fold Phase 273 BAF-credit-routing pre-shipped commits (ff929948 + e9807891 + e04d3333 + 1eb1ecb5) into the §3.A delta-surface table as included-since-baseline surface-coverage attestation (no F-39-NN finding eligible); flip REQUIREMENTS / ROADMAP / STATE / MILESTONES / PROJECT.

Purpose: Close v39.0 milestone with a behaviorally accepted single-batched-commit pair (1 contract + 1 test, both USER-APPROVED), preserve EV-neutrality empirically (TST-WT-01), preserve auto-resolve byte-equivalence empirically (TST-REG-03), preserve cross-event no-breaking-change posture (TST-WT-05 + D-274-NO-EVT-BREAK-01), and deliver the audit-of-record (FINDINGS-v39.0.md) that records every delta + folds in Phase 273 maintenance.

Output: 2 USER-APPROVED batched commits (Wave 1 contracts + Wave 2 tests) + N AGENT-COMMITTED atomic-per-task commits (Wave 3+ audit deliverable + adversarial log + summary + REQUIREMENTS/ROADMAP/STATE/MILESTONES/PROJECT flips). Closure signal `MILESTONE_V39_AT_HEAD_<sha>` emitted verbatim across 8 locations (5 FINDINGS + ROADMAP + STATE + MILESTONES).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/274-CONTEXT.md

# Precedent templates (READ for shape; executor mirrors)
@.planning/milestones/v38.0-phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-PLAN.md
@.planning/milestones/v36.0-phases/266-lootbox-entropy-refactor/266-01-PLAN.md
@audit/FINDINGS-v38.0.md
@audit/FINDINGS-v37.0.md
@audit/FINDINGS-v36.0.md

# Live contract state (Phase 274 mutation subjects — baseline `06623edb`)
@contracts/modules/DegenerusGameLootboxModule.sol
@contracts/interfaces/IDegenerusGameModules.sol
@contracts/storage/DegenerusGameStorage.sol

# Cross-reference (verify byte-equivalence at v39 close; NOT mutated by Phase 274)
@contracts/modules/DegenerusGameMintModule.sol
@contracts/libraries/EntropyLib.sol

# Phase 273 inclusion context
@.planning/phases/273-baf-credit-routing-fix/273-CONTEXT.md

# Memory feedback (NON-NEGOTIABLE rules governing this phase)
<!-- All paths below are global memory paths; executor reads them as governing rules -->
<!-- @$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_contract_commits.md -->
<!-- @$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_batch_contract_approval.md -->
<!-- @$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_never_preapprove_contracts.md -->
<!-- @$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_manual_review_before_push.md -->
<!-- @$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md -->
<!-- @$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_dead_guards.md -->
<!-- @$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_design_intent_before_deletion.md -->
<!-- @$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_wait_for_approval.md -->
<!-- @$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_backward_trace.md -->
<!-- @$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_commitment_window.md -->
<!-- @$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_test_rnglock.md -->
<!-- @$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_skip_research_test_phases.md -->
<!-- @$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_gas_worst_case.md -->

<interfaces>
<!-- Key contracts that Wave 1 executor must understand. Extracted from baseline `06623edb`. -->

From contracts/storage/DegenerusGameStorage.sol L165 (TICKET_SCALE constant):
  `uint256 internal constant TICKET_SCALE = 100;`

From contracts/storage/DegenerusGameStorage.sol L562-589 (`_queueTickets` — whole-ticket helper, manual path target):
  signature: `function _queueTickets(address buyer, uint24 targetLevel, uint32 quantity, bool rngBypass) internal`
  emits: `TicketsQueued(buyer, targetLevel, quantity)` on entry
  guards: `if (_livenessTriggered()) revert E();` + `if (isFarFuture && rngLockedFlag && !rngBypass) revert RngLocked();`
  Note: invoking with `rngBypass=false` is IDENTICAL to the current `_queueTicketsScaled(..., false)` call at L984 in terms of liveness + rngLocked posture. No new bypass path.

From contracts/storage/DegenerusGameStorage.sol L596-641 (`_queueTicketsScaled` — auto-resolve target, UNCHANGED):
  signature: `function _queueTicketsScaled(address buyer, uint24 targetLevel, uint32 quantityScaled, bool rngBypass) internal`
  emits: `TicketsQueuedScaled(buyer, targetLevel, quantityScaled)` on entry
  Produces `rem` byte residue in `ticketsOwedPacked[wk][buyer]` for fractional accumulation. Resolved at activation by `_rollRemainder` in MintModule.processFutureTicketBatch.

From contracts/modules/DegenerusGameLootboxModule.sol L127-132 (existing `LootBoxWwxrpReward` event — REUSE for consolation):
  ```
  event LootBoxWwxrpReward(
      address indexed player,
      uint32 indexed day,
      uint256 lootboxAmount,
      uint256 wwxrpAmount
  );
  ```

From contracts/modules/DegenerusGameLootboxModule.sol L278 (existing `LOOTBOX_WWXRP_PRIZE` constant — new sibling at v39):
  `uint256 private constant LOOTBOX_WWXRP_PRIZE = 1 ether;`

From contracts/modules/DegenerusGameLootboxModule.sol L860-880 (current `_resolveLootboxCommon` signature — Wave 1 mutates to add `uint48 index`):
  ```
  function _resolveLootboxCommon(
      address player,
      uint32 day,
      uint256 amount,
      uint24 targetLevel,
      uint24 currentLevel,
      uint256 seed,
      bool presale,
      bool allowWhalePass,
      bool allowLazyPass,
      bool emitLootboxEvent,
      bool allowBoons,
      uint256 distressEth,
      uint256 totalPackedEth
  )
      private
      returns (
          uint32 futureTickets,
          uint256 burnieAmount,
          uint256 bonusBurnie
      )
  ```

From contracts/modules/DegenerusGameLootboxModule.sol L974-985 (current `if (futureTickets != 0)` block — mutation target):
  ```
  if (futureTickets != 0) {
      // Distress-mode ticket bonus: 25% extra on the fraction bought during distress
      if (distressEth != 0 && totalPackedEth != 0) {
          uint256 bonus = (uint256(futureTickets) * distressEth * DISTRESS_TICKET_BONUS_BPS)
              / (totalPackedEth * 10_000);
          if (bonus != 0) {
              uint256 boosted = uint256(futureTickets) + bonus;
              futureTickets = uint32(boosted);
          }
      }
      _queueTicketsScaled(player, targetLevel, futureTickets, false);
  }
  ```

From contracts/modules/DegenerusGameLootboxModule.sol L637-660 (BurnieLootOpen consumer of `_resolveLootboxCommon` return value — CRITICAL non-breaking constraint):
  ```
  (uint32 tickets, uint256 burnieReward, ) = _resolveLootboxCommon(...);
  emit BurnieLootOpen(player, uint32(index), burnieAmount, targetLevel, tickets, burnieReward);
  ```
  **CRITICAL:** `tickets` here is the function's first return value. Per D-274-NO-EVT-BREAK-01, `BurnieLootOpen.tickets` must continue emitting SCALED. Therefore the function-scope `futureTickets` MUST stay scaled at return time (do NOT reassign to `whole`). Use a separate local `uint32 queueWhole` for the `_queueTickets` call.

From contracts/modules/DegenerusGameLootboxModule.sol L999-1007 (LootBoxOpened emit — CRITICAL non-breaking constraint):
  ```
  emit LootBoxOpened(player, day, amount, targetLevel, futureTickets, burnieAmount, bonusBurnie);
  ```
  **CRITICAL:** `futureTickets` field must remain SCALED post-Bernoulli per D-274-NO-EVT-BREAK-01. If using separate `queueWhole` local for the `_queueTickets` call (recommended), function-scope `futureTickets` stays scaled — this emit needs no change.

From contracts/modules/DegenerusGameLootboxModule.sol L526 (openLootBox signature):
  `function openLootBox(address player, uint48 index) external`
  `index` is already a parameter. Threads directly into the new `_resolveLootboxCommon` parameter.

From contracts/modules/DegenerusGameLootboxModule.sol L606 (openBurnieLootBox signature):
  `function openBurnieLootBox(address player, uint48 index) external`
  Same — `index` already available.

From contracts/modules/DegenerusGameLootboxModule.sol L668 (resolveLootboxDirect — auto-resolve, NO index):
  `function resolveLootboxDirect(address player, uint256 amount, uint256 rngWord) external`
  Passes `type(uint48).max` sentinel into `_resolveLootboxCommon`.

From contracts/modules/DegenerusGameLootboxModule.sol L703 (resolveRedemptionLootbox — auto-resolve, NO index):
  `function resolveRedemptionLootbox(address player, uint256 amount, uint256 rngWord, uint16 activityScore) external`
  Passes `type(uint48).max` sentinel into `_resolveLootboxCommon`.

From contracts/modules/DegenerusGameLootboxModule.sol L838-847 (bit-allocation NatSpec — mutation target for LBX-WT-04):
  ```
  ///      Bit allocation in primary chunk (`seed`):
  ///        bits[0..15]    rangeRoll % 100         (_rollTargetLevel)
  ///        bits[16..23]   near-offset % 5         (_rollTargetLevel)
  ///        bits[24..39]   far-offset % 46         (_rollTargetLevel)
  ///        bits[40..55]   pathRoll % 20           (_resolveLootboxRoll)
  ///        bits[56..79]   tierRoll % 1000         (_lootboxDgnrsReward sub-call)
  ///        bits[80..95]   varianceRoll % 20       (_resolveLootboxRoll large-BURNIE)
  ///        bits[96..119]  ticketVariance % 10000  (_lootboxTicketCount)
  ///        bits[120..151] boon roll % BOON_PPM_SCALE (_rollLootboxBoons)
  ///      Total primary-chunk consumption: 152 bits / 256 available.
  ```
  New entry to insert (between boon roll and total line):
  ```
  ///        bits[152..167] fracRoundUp % 100      (_resolveLootboxCommon manual-path ticket whole-collapse; auto-resolve paths leave slice unread)
  ```
  New total line:
  ```
  ///      Total primary-chunk consumption: 168 bits / 256 available (bits[152..167] consumed only on manual paths; auto-resolve paths leave the slice unread).
  ```

From contracts/interfaces/IDegenerusGameModules.sol L264-314 (`IDegenerusGameLootboxModule` interface block — declaration target):
  Currently declares only the 4 external functions + 2 deity-boon entries. Add `event LootboxTicketRoll(address indexed player, uint48 indexed lootboxIndex, uint32 preRollTickets, bool roundedUp);` near the top of the interface body (events are declared inside interfaces in Solidity and have semantic equivalence to contract-level events for filtering).

From contracts/modules/DegenerusGameLootboxModule.sol L583-597 (openLootBox _resolveLootboxCommon call site):
  ```
  _resolveLootboxCommon(player, day, scaledAmount, targetLevel, currentLevel, seed, presale, true, true, true, true, distressEth, amount);
  ```
  Post-edit: insert `index` after `day`:
  ```
  _resolveLootboxCommon(player, day, index, scaledAmount, targetLevel, currentLevel, seed, presale, true, true, true, true, distressEth, amount);
  ```

From contracts/modules/DegenerusGameLootboxModule.sol L637-651 (openBurnieLootBox _resolveLootboxCommon call site):
  ```
  (uint32 tickets, uint256 burnieReward, ) = _resolveLootboxCommon(player, day, amountEth, targetLevel, currentLevel, seed, false, false, false, false, true, 0, 0);
  ```
  Post-edit: insert `index` after `day`:
  ```
  (uint32 tickets, uint256 burnieReward, ) = _resolveLootboxCommon(player, day, index, amountEth, targetLevel, currentLevel, seed, false, false, false, false, true, 0, 0);
  ```

From contracts/modules/DegenerusGameLootboxModule.sol L679-693 (resolveLootboxDirect _resolveLootboxCommon call site):
  ```
  _resolveLootboxCommon(player, day, scaledAmount, targetLevel, currentLevel, seed, false, true, true, true, false, 0, 0);
  ```
  Post-edit: insert sentinel after `day`:
  ```
  _resolveLootboxCommon(player, day, type(uint48).max, scaledAmount, targetLevel, currentLevel, seed, false, true, true, true, false, 0, 0);
  ```

From contracts/modules/DegenerusGameLootboxModule.sol L714-728 (resolveRedemptionLootbox _resolveLootboxCommon call site):
  ```
  _resolveLootboxCommon(player, day, scaledAmount, targetLevel, currentLevel, seed, false, true, true, true, false, 0, 0);
  ```
  Post-edit: insert sentinel after `day`:
  ```
  _resolveLootboxCommon(player, day, type(uint48).max, scaledAmount, targetLevel, currentLevel, seed, false, true, true, true, false, 0, 0);
  ```

</interfaces>

<canonical_grep_recipes>
<!-- Bake these into task <action> blocks. Executors copy verbatim — do NOT re-derive. -->

# G1 — LBX-WX-01 LOOTBOX_WWXRP_CONSOLATION constant declared
grep -nE "LOOTBOX_WWXRP_CONSOLATION\s*=\s*1 ether" contracts/modules/DegenerusGameLootboxModule.sol
# Expected: 1 match near L278 sibling to LOOTBOX_WWXRP_PRIZE

# G2 — LBX-EVT-03 LootboxTicketRoll event declared at contract level
grep -nE "event LootboxTicketRoll\(\s*address indexed player,\s*uint48 indexed lootboxIndex,\s*uint32 preRollTickets,\s*bool roundedUp\s*\)" contracts/modules/DegenerusGameLootboxModule.sol
# Expected: 1 match in event block

# G3 — LBX-EVT-03 LootboxTicketRoll event declared on interface
grep -nE "event LootboxTicketRoll\(" contracts/interfaces/IDegenerusGameModules.sol
# Expected: 1 match inside IDegenerusGameLootboxModule interface block (L264-314 region)

# G4 — LBX-EVT-05 _resolveLootboxCommon signature gains uint48 index parameter (placed after uint32 day)
grep -nA1 "function _resolveLootboxCommon" contracts/modules/DegenerusGameLootboxModule.sol | grep -E "uint48 index"
# Expected: 1 match showing `uint48 index,` in the signature

# G5 — Number of internal callsites of _resolveLootboxCommon (excludes the function definition at column 0)
grep -cE "^\s+_resolveLootboxCommon\(" contracts/modules/DegenerusGameLootboxModule.sol
# Expected: 4 (the 4 caller sites at L583, L637, L679, L714; function def at L860 starts at column 0 and is excluded)

# G6 — LBX-EVT-05 manual callers pass real `index` arg
grep -nB2 -A3 "_resolveLootboxCommon\(" contracts/modules/DegenerusGameLootboxModule.sol | grep -cE "day, index,"
# Expected: 2 (openLootBox + openBurnieLootBox)

# G7 — LBX-EVT-05 auto-resolve callers pass type(uint48).max sentinel
grep -cE "day, type\(uint48\)\.max" contracts/modules/DegenerusGameLootboxModule.sol
# Expected: 2 (resolveLootboxDirect + resolveRedemptionLootbox)

# G8 — LBX-WT-02 Bernoulli math present (bits[152..167] consumption)
grep -nE "uint8\(seed >> 152\) % uint8\(TICKET_SCALE\)" contracts/modules/DegenerusGameLootboxModule.sol
# Expected: 1 match in manual branch of _resolveLootboxCommon

# G9 — LBX-WT-02 scaledPre snapshot present
grep -nE "uint32 scaledPre = futureTickets" contracts/modules/DegenerusGameLootboxModule.sol
# Expected: 1 match in manual branch

# G10 — _queueTickets call count (1 new manual-branch invocation + 1 pre-existing whale-pass-loop at L1109)
grep -cE "_queueTickets\(\s*player," contracts/modules/DegenerusGameLootboxModule.sol
# Expected: 2 (manual-branch new + whale-pass-loop existing; loose 2nd-arg match captures both `targetLevel` and the pre-existing `lvl` formulation at L1109)

# G11 — LBX-WT-03 + LBX-EVT-02 _queueTicketsScaled still called on auto-resolve path (status quo)
grep -nE "_queueTicketsScaled\(player, targetLevel, futureTickets, false\)" contracts/modules/DegenerusGameLootboxModule.sol
# Expected: 1 match (auto-resolve branch inside _resolveLootboxCommon)

# G12 — LBX-WX-02 consolation emit present
grep -nE "wwxrp\.mintPrize\(player, LOOTBOX_WWXRP_CONSOLATION\)" contracts/modules/DegenerusGameLootboxModule.sol
# Expected: 1 match in manual-branch else clause

# G13 — LBX-WX-02 consolation LootBoxWwxrpReward emit
grep -nE "emit LootBoxWwxrpReward\(player, day, amount, LOOTBOX_WWXRP_CONSOLATION\)" contracts/modules/DegenerusGameLootboxModule.sol
# Expected: 1 match adjacent to G12 mintPrize call

# G14 — LBX-EVT-03 + LBX-EVT-06 LootboxTicketRoll emit present
grep -nE "emit LootboxTicketRoll\(player, index, scaledPre, roundedUp\)" contracts/modules/DegenerusGameLootboxModule.sol
# Expected: 1 match in manual branch (after queue/consolation if/else)

# G15 — LBX-WT-04 bit-allocation NatSpec updated (new bits[152..167] entry)
grep -nE "bits\[152\.\.159\]\s+fracRoundUp" contracts/modules/DegenerusGameLootboxModule.sol
# Expected: 1 match

# G16 — LBX-WT-04 bit-allocation NatSpec total line updated to 160
grep -nE "Total primary-chunk consumption: 168 bits / 256 available" contracts/modules/DegenerusGameLootboxModule.sol
# Expected: 1 match

# G17 — function-scope futureTickets reassignment count must not increase (D-274-NO-EVT-BREAK-01)
PRE_COUNT=$(git show 06623edb:contracts/modules/DegenerusGameLootboxModule.sol | grep -cE "futureTickets\s*=[^=]")
POST_COUNT=$(grep -cE "futureTickets\s*=[^=]" contracts/modules/DegenerusGameLootboxModule.sol)
[ "$PRE_COUNT" = "$POST_COUNT" ] && echo "G17_PASS" || echo "G17_FAIL pre=$PRE_COUNT post=$POST_COUNT"
# Expected: G17_PASS (zero new reassignments of function-scope futureTickets to whole or any other value)
# Note: [^=] filter excludes `==` comparison operators.

# G18 — LBX-EVT-01 LootBoxOpened emits function-scope futureTickets (scaled) UNCHANGED
grep -nA8 "emit LootBoxOpened" contracts/modules/DegenerusGameLootboxModule.sol | grep -E "futureTickets,"
# Expected: 1 match (existing line UNCHANGED at L1004)

# G19 — LBX-EVT-01 BurnieLootOpen consumes _resolveLootboxCommon first return value as `tickets` (scaled)
grep -nA1 "_resolveLootboxCommon\(" contracts/modules/DegenerusGameLootboxModule.sol | grep -E "uint32 tickets, uint256 burnieReward"
# Expected: 1 match (openBurnieLootBox callsite)

# G20 — LBX-WT-05 storage layout byte-identical (excludes constants)
diff \
  <(git show 06623edb:contracts/modules/DegenerusGameLootboxModule.sol \
    | grep -E "^\s*(uint|int|bool|address|bytes|mapping|string)\s+(internal|private|public)\s+" \
    | grep -vE "\bconstant\b") \
  <(grep -E "^\s*(uint|int|bool|address|bytes|mapping|string)\s+(internal|private|public)\s+" contracts/modules/DegenerusGameLootboxModule.sol \
    | grep -vE "\bconstant\b")
# Expected: empty diff (exit 0)

# G21 — LBX-WT-05 zero new external mutation entry points
diff <(git show 06623edb:contracts/modules/DegenerusGameLootboxModule.sol | grep -E "^\s*function\s.* external") <(grep -E "^\s*function\s.* external" contracts/modules/DegenerusGameLootboxModule.sol)
# Expected: exit 0 (4 external functions unchanged; openLootBox + openBurnieLootBox + resolveLootboxDirect + resolveRedemptionLootbox + deityBoonSlots + issueDeityBoon — 6 total at baseline)

# G22 — LBX-WT-05 zero new modifiers
diff <(git show 06623edb:contracts/modules/DegenerusGameLootboxModule.sol | grep -E "^\s*modifier\s") <(grep -E "^\s*modifier\s" contracts/modules/DegenerusGameLootboxModule.sol)
# Expected: exit 0

# G23 — LBX-WT-05 zero MintModule mutation (verify mint-boost path NOT touched)
cmp <(git show 06623edb:contracts/modules/DegenerusGameMintModule.sol) contracts/modules/DegenerusGameMintModule.sol
# Expected: exit 0

# G24 — LBX-WT-05 zero EntropyLib mutation (RNG layer untouched at v39)
cmp <(git show 06623edb:contracts/libraries/EntropyLib.sol) contracts/libraries/EntropyLib.sol
# Expected: exit 0

# G25 — No history language in new NatSpec per feedback_no_history_in_comments.md
grep -cEi "previously|v38|v39|removed|reduced from|used to|former" contracts/modules/DegenerusGameLootboxModule.sol
# Expected: 0 matches in new/edited regions (legacy unchanged comments don't count if untouched; planner discretion to interpret)

# G26 — Compile gate
npx hardhat compile 2>&1 | tail -3
# Expected: "Compiled" or "Nothing to compile"

# G27 — REG-01 v38.0 closure non-widening (Degenerette + Mint + EntropyLib + JackpotModule byte-identical)
for f in contracts/modules/DegenerusGameDegeneretteModule.sol contracts/modules/DegenerusGameMintModule.sol contracts/libraries/EntropyLib.sol contracts/modules/DegenerusGameJackpotModule.sol contracts/DegenerusTraitUtils.sol; do
  if ! cmp <(git show 06623edb:$f) $f >/dev/null 2>&1; then echo "DRIFT $f"; else echo "OK $f"; fi
done
# Expected: 5 lines all "OK ..."

# G28 — REG-02 v34.0 closure non-widening (TraitUtils + JackpotBucketLib byte-identical at v39 HEAD)
for f in contracts/DegenerusTraitUtils.sol contracts/libraries/JackpotBucketLib.sol; do
  if ! cmp <(git show 6b63f6d4daf346a53a1d463790f637308ea8d555:$f) $f >/dev/null 2>&1; then echo "DRIFT $f"; else echo "OK $f"; fi
done
# Expected: 2 lines all "OK ..."

# G29 — D-274-FCITE-01 zero forward-cite emission (terminal phase)
! grep -rE "Phase (275|276|277|278)|v40\\.0|v41\\.0" audit/FINDINGS-v39.0.md .planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/
# Expected: exit 0 (no matches)

# G30 — Closure-signal verbatim presence in >=5 FINDINGS locations
grep -cE "MILESTONE_V39_AT_HEAD_[0-9a-f]{8}" audit/FINDINGS-v39.0.md
# Expected: >=5

# G31 — Closure-signal cross-document propagation (3 targets)
grep -lE "MILESTONE_V39_AT_HEAD_[0-9a-f]{8}" .planning/ROADMAP.md .planning/STATE.md .planning/MILESTONES.md
# Expected: 3 file paths

# G32 — D-274-BAF273-INCLUDE-01 Phase 273 commits cited in §3.A
grep -cE "ff929948|e9807891|e04d3333|1eb1ecb5" audit/FINDINGS-v39.0.md
# Expected: >=4 (one per commit SHA in §3.A row coverage + §9.NN.iv)

# G33 — LBX-WT-05 storage-layout JSON parity (MANDATORY load-bearing proof per Success Criterion #8 + LBX-WT-05)
# Compile post-edit, capture storageLayout JSON; stash + checkout baseline; compile baseline; diff.
# NOTE: LootboxModule is a delegatecall module inheriting DegenerusGameStorage, so its storageLayout reflects parent storage which is the load-bearing invariant.
npx hardhat compile && cat artifacts/build-info/*.json 2>/dev/null | jq -r '.output.contracts["contracts/modules/DegenerusGameLootboxModule.sol"]["DegenerusGameLootboxModule"].storageLayout // empty' > /tmp/v39-storage.json
git show 06623edb -- contracts/modules/DegenerusGameLootboxModule.sol > /dev/null && git stash && npx hardhat compile && cat artifacts/build-info/*.json 2>/dev/null | jq -r '.output.contracts["contracts/modules/DegenerusGameLootboxModule.sol"]["DegenerusGameLootboxModule"].storageLayout // empty' > /tmp/v38-storage.json && git stash pop
diff /tmp/v38-storage.json /tmp/v39-storage.json
# Expected: empty diff (storage layout byte-identical at JSON level). Load-bearing proof for Success Criterion #8 + LBX-WT-05.
</canonical_grep_recipes>

</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VRF-fulfillment → manual lootbox resolve | `lootboxRngWordByIndex[index]` written by VRF callback before player can call `openLootBox(player, index)` or `openBurnieLootBox(player, index)`. Per-resolution `seed = keccak256(abi.encode(rngWord, player, day, amount))` derived once at function entry (L554 / L628). bits[152..167] of this seed is the new Bernoulli slice; cannot be ground by player-controllable input that mutates between VRF commit and lootbox open. |
| VRF-fulfillment → auto-resolve | `resolveLootboxDirect` / `resolveRedemptionLootbox` receive `rngWord` as parameter from caller (decimator claim path / sDGNRS redemption path); auto-resolve flow UNCHANGED at v39 — sentinel passing means bits[152..167] is not consumed on auto-resolve paths. |
| Player-side input → openLootBox/openBurnieLootBox | `player` + `index` are caller-validated state (lootbox must exist with non-zero amount; RNG must be ready). No new input surface introduced. `index` is dual-purpose (storage key + new behavioral discriminator); cannot equal `type(uint48).max` for any real lootbox (≥281 trillion lifetime lootboxes would be required). |
| `_resolveLootboxCommon` manual branch → `_queueTickets` | Whole-ticket queue helper with `rngBypass=false` — IDENTICAL liveness + rngLocked posture to the auto-resolve `_queueTicketsScaled(..., false)` call. No new bypass path. |
| `_resolveLootboxCommon` manual branch → `wwxrp.mintPrize` consolation | Reuses existing `IWrappedWrappedXRP.mintPrize` interface (L17-22); same path that the 10%-WWXRP-win at L1588 uses. No new external call introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-274-01 | Tampering | Bernoulli round-up at `bits[152..167]` | mitigate | Per-resolution seed is `keccak256(abi.encode(rngWord, player, day, amount))` derived once at function entry; rngWord committed via VRF before player can open. bits[152..167] previously unallocated; chi² independence test (TST-WT-03) confirms slice is uncorrelated with other 8 primary-chunk consumers. Backward-trace per `feedback_rng_backward_trace.md`: no player-controllable input mutates between VRF commit and lootbox open that affects bits[152..167] independently of the other slices. |
| T-274-02 | Tampering | `uint48 index` parameter spoofing to flip behavior gate | mitigate | The new `index` parameter is private (passed only by internal callers of `_resolveLootboxCommon`); manual callers (openLootBox + openBurnieLootBox) receive `index` from their `external` signature where it's already validated by lootbox-storage lookup (amount > 0 + rngWord ready). Auto-resolve callers pass the literal `type(uint48).max` sentinel — not under player control. No external surface accepts `type(uint48).max` as a real lootbox index. |
| T-274-03 | Information Disclosure | `LootboxTicketRoll` event field leakage | accept | Event is purely informational (player + index + scaledPre + roundedUp). `scaledPre` and `roundedUp` are mechanically derivable from the per-resolution seed which is itself derivable from the public `rngWord` post-fulfillment; no new information leaked. Indexed `player` + `lootboxIndex` for filter efficiency are public information by construction. |
| T-274-04 | Denial of Service | new `LootboxTicketRoll` emit increases gas in ticket-path | accept | Single LOG2 opcode (~750 gas + 256-bit topic costs); negligible per `feedback_gas_worst_case.md` — analytical worst-case derivation in §3.C of audit deliverable. Manual lootbox open already burns ~150k+ gas; +1 LOG2 is ~0.5% overhead bounded. |
| T-274-05 | Repudiation | unauthorized contract commit without user approval | mitigate | Per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md`: Wave 1 + Wave 2 both block on explicit `approved` string before `git commit`. Final push gated by `feedback_manual_review_before_push.md`. |
| T-274-06 | Elevation of Privilege | new admin / new entry points | accept | AUDIT-04 zero-new-state attestation confirms zero new public/external mutation entry points, zero new modifiers, zero new admin functions. The 4 existing external entry points (`openLootBox` + `openBurnieLootBox` + `resolveLootboxDirect` + `resolveRedemptionLootbox`) retain their byte-identical public ABI. |
| T-274-07 | Tampering | consolation predicate fires from non-manual path | mitigate | Consolation predicate is structurally gated by `manualOpen = (index != type(uint48).max)` AND nested inside the manual-branch of the `if (futureTickets != 0)` block. Auto-resolve callers cannot trigger consolation by construction (sentinel value falls into the else clause). TST-WX-02 non-trigger predicate matrix is the load-bearing test. |
| T-274-08 | Information Disclosure | scaled-vs-whole field semantic confusion in indexers | mitigate | D-274-NO-EVT-BREAK-01: `LootBoxOpened.futureTickets` + `BurnieLootOpen.tickets` + `TicketsQueuedScaled` semantics UNCHANGED on both paths. New `LootboxTicketRoll.preRollTickets` carries the SAME scaled value as same-tx `LootBoxOpened.futureTickets` on manual paths. TST-WT-05 + TST-WT-07 are the load-bearing field-consistency tests. UI/indexer consumers opt in to remainder visibility without rebasing existing reads. |
| T-274-09 | Tampering | function-scope `futureTickets` mutation breaks return-value contract | mitigate | CRITICAL implementation discipline: function-scope `futureTickets` (local + first return value) must NOT be reassigned to `whole` post-Bernoulli. Use a separate `uint32 queueWhole` local for the `_queueTickets` call. This preserves `BurnieLootOpen.tickets` (consumer of return value at L658) and `LootBoxOpened.futureTickets` (emitted by `_resolveLootboxCommon` at L1004) as SCALED per D-274-NO-EVT-BREAK-01. G17 + G18 + G19 grep gates enforce. |

</threat_model>

<tasks>

<!-- ================================================================ -->
<!-- WAVE 1 — CONTRACT MUTATIONS (BATCHED, USER-APPROVED at end)       -->
<!-- LBX-WT-01..05 + LBX-WX-01..04 + LBX-EVT-01..06                    -->
<!-- D-274-MANUAL-ONLY-01 + D-274-NO-EVT-BREAK-01 NON-NEGOTIABLE       -->
<!-- ================================================================ -->

<task type="auto">
  <name>Task 1.1: Declare LOOTBOX_WWXRP_CONSOLATION constant + LootboxTicketRoll event (contract + interface)</name>
  <files>contracts/modules/DegenerusGameLootboxModule.sol, contracts/interfaces/IDegenerusGameModules.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameLootboxModule.sol L127-132 (existing LootBoxWwxrpReward event — REUSE for consolation, do NOT introduce a new event variant)
    - contracts/modules/DegenerusGameLootboxModule.sol L278 (existing LOOTBOX_WWXRP_PRIZE constant — new constant placed as sibling)
    - contracts/modules/DegenerusGameLootboxModule.sol L60-160 (full event block — new event placement)
    - contracts/interfaces/IDegenerusGameModules.sol L264-314 (IDegenerusGameLootboxModule interface block — event declaration target)
    - .planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/274-CONTEXT.md (locked decisions D-274-EVT-ROLL-01 + D-274-WX-AMOUNT-01)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md (NatSpec discipline: describe what IS at v39, zero historical/comparative language)
  </read_first>
  <action>
LBX-WX-01: Insert new private constant in `contracts/modules/DegenerusGameLootboxModule.sol` immediately after the existing `LOOTBOX_WWXRP_PRIZE` at L278. Define `LOOTBOX_WWXRP_CONSOLATION` as `uint256 private constant ... = 1 ether;` with NatSpec describing the cold-bust consolation magnitude (matches LOOTBOX_WWXRP_PRIZE by design — booby trigger much rarer than 10%-path WWXRP win).

LBX-EVT-03: Insert new event declaration in `contracts/modules/DegenerusGameLootboxModule.sol` immediately after `LootBoxWwxrpReward` at L127-132 (the event block, NOT the L278 constants block which is the placement for LBX-WX-01 only). Post-edit event-declaration position is ~L132. Event signature exactly: `event LootboxTicketRoll(address indexed player, uint48 indexed lootboxIndex, uint32 preRollTickets, bool roundedUp);` with NatSpec describing: emitted on manual-path lootbox open landing on ticket-path with non-zero pre-Bernoulli scaled; auto-resolve never emits; consumer derives whole = (preRollTickets / 100) + (roundedUp ? 1 : 0).

LBX-EVT-03 (interface): Add the SAME event declaration inside the `IDegenerusGameLootboxModule` interface block in `contracts/interfaces/IDegenerusGameModules.sol` at L264-314. Insert near the top of the interface body (before the `openLootBox` external function declaration at L270). Solidity allows event declarations inside interfaces; they are semantically equivalent to contract-level events for off-chain filtering.

After applying both inserts, run hardhat compile and capture exit code. No commit yet (batched at Task 1.5).
  </action>
  <verify>
    <automated>
# G1 — LOOTBOX_WWXRP_CONSOLATION declared
grep -cE "LOOTBOX_WWXRP_CONSOLATION\s*=\s*1 ether" contracts/modules/DegenerusGameLootboxModule.sol   # expect 1
# G2 — event declared at contract level
grep -cE "event LootboxTicketRoll\(" contracts/modules/DegenerusGameLootboxModule.sol   # expect 1
# G3 — event declared on interface
grep -cE "event LootboxTicketRoll\(" contracts/interfaces/IDegenerusGameModules.sol   # expect 1
# Compile gate
npx hardhat compile 2>&1 | tail -3   # expect "Compiled" or "Nothing to compile"
    </automated>
  </verify>
  <done>G1 + G2 + G3 grep gates return expected values; `npx hardhat compile` exits 0. New constant + new event declared in both module and interface files. Task creates no commit (batched at Task 1.5).</done>
</task>

<task type="auto">
  <name>Task 1.2: Add uint48 index parameter + manual-branch Bernoulli + consolation + LootboxTicketRoll emit to _resolveLootboxCommon</name>
  <files>contracts/modules/DegenerusGameLootboxModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameLootboxModule.sol L860-1010 (full `_resolveLootboxCommon` function body — primary mutation target)
    - contracts/modules/DegenerusGameLootboxModule.sol L637-660 (`openBurnieLootBox` callsite — D-274-NO-EVT-BREAK-01 CRITICAL: function return value is consumed as `BurnieLootOpen.tickets` and must stay SCALED)
    - contracts/storage/DegenerusGameStorage.sol L562-589 (`_queueTickets` — manual-path queue helper; rngBypass=false posture)
    - contracts/storage/DegenerusGameStorage.sol L596-641 (`_queueTicketsScaled` — auto-resolve queue helper; UNCHANGED at v39)
    - .planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/274-CONTEXT.md (locked decisions D-274-MANUAL-ONLY-01 + D-274-NO-EVT-BREAK-01 + D-274-BIT-SLICE-01 + D-274-EVT-INDEX-SENTINEL-01 + D-274-EVT-ROLL-01)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_dead_guards.md (don't waste gas on dead branches; auto-resolve guard form `if (manualOpen) {...} else {...}` is load-bearing not dead)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_backward_trace.md (every RNG audit must trace BACKWARD from each consumer; new bits[152..167] consumer must demonstrate seed unknown at input commitment time)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_commitment_window.md (check what player-controllable state can change between VRF request and fulfillment)
  </read_first>
  <action>
LBX-EVT-05 — Signature mutation. Add `uint48 index` parameter to `_resolveLootboxCommon` (L860-873) immediately after `uint32 day`. Post-edit parameter order (14 params): `address player, uint32 day, uint48 index, uint256 amount, uint24 targetLevel, uint24 currentLevel, uint256 seed, bool presale, bool allowWhalePass, bool allowLazyPass, bool emitLootboxEvent, bool allowBoons, uint256 distressEth, uint256 totalPackedEth`. Return tuple `(uint32 futureTickets, uint256 burnieAmount, uint256 bonusBurnie)` UNCHANGED.

LBX-WT-02 + LBX-WT-03 + LBX-WX-02 + LBX-EVT-06 — Body mutation inside the `if (futureTickets != 0)` block (current L974-985). KEEP the distress-bonus accumulation block at L976-983 UNCHANGED on BOTH paths (computes in scaled space; small bonuses don't truncate to 0). After the distress block:

(a) Branch on `if (index != type(uint48).max) { /* manual */ } else { /* auto-resolve */ }`.

(b) Manual branch (top half):
   - Snapshot `uint32 scaledPre = futureTickets;` — used by both the Bernoulli math (frac/whole split) AND the `LootboxTicketRoll.preRollTickets` emit.
   - Compute `uint32 whole = futureTickets / uint32(TICKET_SCALE);` and `uint32 frac = futureTickets % uint32(TICKET_SCALE);`.
   - Initialize `bool roundedUp = false;`.
   - Bernoulli: `if (frac != 0 && (uint16(seed >> 152) % uint16(TICKET_SCALE)) < uint8(frac)) { unchecked { whole += 1; } roundedUp = true; }` — consumes bits[152..167] of the per-resolution seed per D-274-BIT-SLICE-01.
   - **CRITICAL non-mutation:** Do NOT assign `futureTickets = whole`. The function-scope `futureTickets` (which is the first return value AND the value emitted in `LootBoxOpened.futureTickets` at L1004) must stay carrying the scaled post-distress value per D-274-NO-EVT-BREAK-01. The `whole` value lives only as a separate manual-branch local for the `_queueTickets` call.
   - Queue/consolation switch: `if (whole != 0) { _queueTickets(player, targetLevel, whole, false); } else { wwxrp.mintPrize(player, LOOTBOX_WWXRP_CONSOLATION); emit LootBoxWwxrpReward(player, day, amount, LOOTBOX_WWXRP_CONSOLATION); }`.
   - Emit `emit LootboxTicketRoll(player, index, scaledPre, roundedUp);` AFTER the if/else (single emit per manual lootbox open with non-zero scaledPre).

(c) Auto-resolve branch (else clause): `_queueTicketsScaled(player, targetLevel, futureTickets, false);` — byte-equivalent to v38 line at current L984. NO Bernoulli, NO consolation, NO `LootboxTicketRoll` emit.

(d) The outer `if (futureTickets != 0)` guard is unchanged. The new `LootboxTicketRoll` emit is INSIDE this outer guard, so when ticket-path produced scaled-0 from the start (e.g. `_lootboxTicketCount` truncated to 0 from degenerate-tiny budget), the event does NOT fire — matches LBX-EVT-03 ticket-path-non-selection negative semantics.

Implementation discipline (CRITICAL — these are non-negotiable, encode in commit message body):
1. Function-scope `futureTickets` is NEVER reassigned to `whole`. Use a separate `uint32 whole` local declared inside the manual branch. This preserves return-value contract: `BurnieLootOpen.tickets` at openBurnieLootBox L658 continues to emit SCALED; `LootBoxOpened.futureTickets` at L1004 continues to emit SCALED.
2. `LootboxTicketRoll` emit lives INSIDE the outer `if (futureTickets != 0)` guard AND AFTER the queue/consolation if/else; fires at most once per manual call; never fires from auto-resolve branch.
3. `rngBypass=false` for `_queueTickets` matches the existing `rngBypass=false` for the displaced `_queueTicketsScaled` call. Liveness + rngLocked posture IDENTICAL.
4. New NatSpec inside the manual/auto-resolve branches describes what IS at v39 per `feedback_no_history_in_comments.md`. No "v38", "previously", "used to" comparative language.

Run hardhat compile and capture exit code.
  </action>
  <verify>
    <automated>
# G4 — _resolveLootboxCommon signature gains uint48 index
grep -nA15 "function _resolveLootboxCommon" contracts/modules/DegenerusGameLootboxModule.sol | grep -cE "uint48 index"   # expect 1
# G8 — Bernoulli math (bits[152..167] slice)
grep -cE "uint8\(seed >> 152\) % uint8\(TICKET_SCALE\)" contracts/modules/DegenerusGameLootboxModule.sol   # expect 1
# G9 — scaledPre snapshot present
grep -cE "uint32 scaledPre = futureTickets" contracts/modules/DegenerusGameLootboxModule.sol   # expect 1
# G10 — _queueTickets called on manual path (whole helper); loose 2nd-arg match (whale-pass L1109 uses `lvl`)
grep -cE "_queueTickets\(\s*player," contracts/modules/DegenerusGameLootboxModule.sol   # expect 2 (manual-branch new + whale-pass-loop existing)
grep -cE "_queueTickets\(player, targetLevel, whole, false\)" contracts/modules/DegenerusGameLootboxModule.sol   # expect 1 (precise manual-branch invocation)
# G11 — _queueTicketsScaled still on auto-resolve path
grep -cE "_queueTicketsScaled\(player, targetLevel, futureTickets, false\)" contracts/modules/DegenerusGameLootboxModule.sol   # expect 1
# G12 — consolation mintPrize present
grep -cE "wwxrp\.mintPrize\(player, LOOTBOX_WWXRP_CONSOLATION\)" contracts/modules/DegenerusGameLootboxModule.sol   # expect 1
# G13 — consolation LootBoxWwxrpReward emit
grep -cE "emit LootBoxWwxrpReward\(player, day, amount, LOOTBOX_WWXRP_CONSOLATION\)" contracts/modules/DegenerusGameLootboxModule.sol   # expect 1
# G14 — LootboxTicketRoll emit
grep -cE "emit LootboxTicketRoll\(player, index, scaledPre, roundedUp\)" contracts/modules/DegenerusGameLootboxModule.sol   # expect 1
# G17 — function-scope futureTickets reassignment count MUST NOT increase vs baseline (D-274-NO-EVT-BREAK-01 critical)
PRE_COUNT=$(git show 06623edb:contracts/modules/DegenerusGameLootboxModule.sol | grep -cE "futureTickets\s*=[^=]")
POST_COUNT=$(grep -cE "futureTickets\s*=[^=]" contracts/modules/DegenerusGameLootboxModule.sol)
[ "$PRE_COUNT" = "$POST_COUNT" ] && echo "G17_PASS" || echo "G17_FAIL pre=$PRE_COUNT post=$POST_COUNT"   # expect G17_PASS (note: [^=] excludes == comparisons; catches `= uint32(whole)` formatter variants)
# G18 — LootBoxOpened emit unchanged (still passes futureTickets which stays scaled)
grep -nA8 "emit LootBoxOpened" contracts/modules/DegenerusGameLootboxModule.sol | grep -cE "^\s+futureTickets,"   # expect 1
# Compile gate
npx hardhat compile 2>&1 | tail -3   # expect Compiled / Nothing to compile
    </automated>
  </verify>
  <done>G4 + G8..G14 + G17 + G18 grep gates all return expected values; hardhat compile exits 0. Function-scope `futureTickets` confirmed NEVER reassigned to `whole` (G17_PASS: post-edit reassignment count equals baseline; catches `= uint32(whole)` formatter variants). LootBoxOpened emit still references function-scope `futureTickets`. Manual + auto-resolve branches structurally split inside the `if (futureTickets != 0)` block. Task creates no commit (batched at Task 1.5).</done>
</task>

<task type="auto">
  <name>Task 1.3: Update 4 callers of _resolveLootboxCommon (openLootBox, openBurnieLootBox, resolveLootboxDirect, resolveRedemptionLootbox)</name>
  <files>contracts/modules/DegenerusGameLootboxModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameLootboxModule.sol L583-597 (openLootBox callsite)
    - contracts/modules/DegenerusGameLootboxModule.sol L637-651 (openBurnieLootBox callsite)
    - contracts/modules/DegenerusGameLootboxModule.sol L679-693 (resolveLootboxDirect callsite)
    - contracts/modules/DegenerusGameLootboxModule.sol L714-728 (resolveRedemptionLootbox callsite)
    - .planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/274-CONTEXT.md (D-274-EVT-INDEX-SENTINEL-01 sentinel value)
  </read_first>
  <action>
LBX-EVT-05 — Update all 4 callsites to thread the new `uint48 index` argument immediately after `day`:

(1) `openLootBox` at L583-597 — pass the function's `index` arg (already in scope from `function openLootBox(address player, uint48 index)` signature at L526). Insert `index,` as the 3rd positional argument (after `player,` and `day,`).

(2) `openBurnieLootBox` at L637-651 — pass the function's `index` arg (already in scope from `function openBurnieLootBox(address player, uint48 index)` signature at L606). Insert `index,` as the 3rd positional argument. Preserve the destructure pattern `(uint32 tickets, uint256 burnieReward, ) = _resolveLootboxCommon(...)` UNCHANGED — the first return value `tickets` continues to receive the function-scope `futureTickets` (scaled per D-274-NO-EVT-BREAK-01) and feeds `BurnieLootOpen.tickets` at L658.

(3) `resolveLootboxDirect` at L679-693 — pass `type(uint48).max` sentinel as the 3rd positional argument (auto-resolve caller has no real index in its external signature `function resolveLootboxDirect(address player, uint256 amount, uint256 rngWord)`).

(4) `resolveRedemptionLootbox` at L714-728 — pass `type(uint48).max` sentinel as the 3rd positional argument (auto-resolve caller has no real index in its external signature `function resolveRedemptionLootbox(address player, uint256 amount, uint256 rngWord, uint16 activityScore)`).

Run hardhat compile and confirm green.
  </action>
  <verify>
    <automated>
# G5 — exactly 4 callsites of _resolveLootboxCommon (leading whitespace required; excludes the function definition at column 0)
grep -cE "^\s+_resolveLootboxCommon\(" contracts/modules/DegenerusGameLootboxModule.sol   # expect 4 (L583, L637, L679, L714; function def at L860 is column-0 and excluded)
# G6 — manual callers pass real index (`index,` as own line indented)
grep -B1 -A3 "_resolveLootboxCommon(" contracts/modules/DegenerusGameLootboxModule.sol | grep -cE "^\s+index,"   # expect 2
# G7 — auto-resolve callers pass sentinel
grep -cE "type\(uint48\)\.max," contracts/modules/DegenerusGameLootboxModule.sol   # expect 2
# G19 — openBurnieLootBox still consumes first return value as `tickets` (scaled per D-274-NO-EVT-BREAK-01)
grep -cE "\(uint32 tickets, uint256 burnieReward, \) = _resolveLootboxCommon" contracts/modules/DegenerusGameLootboxModule.sol   # expect 1
# Compile gate
npx hardhat compile 2>&1 | tail -3
    </automated>
  </verify>
  <done>G5 + G6 + G7 + G19 grep gates return expected values. 4 callsites updated: 2 with real `index` arg (openLootBox + openBurnieLootBox), 2 with `type(uint48).max` sentinel (resolveLootboxDirect + resolveRedemptionLootbox). openBurnieLootBox destructure pattern UNCHANGED. hardhat compile exits 0. Task creates no commit (batched at Task 1.5).</done>
</task>

<task type="auto">
  <name>Task 1.4: Update bit-allocation NatSpec + measure storage layout + bytecode delta + RNG backward-trace attestation</name>
  <files>contracts/modules/DegenerusGameLootboxModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameLootboxModule.sol L838-847 (current bit-allocation NatSpec block — mutation target for LBX-WT-04)
    - .planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/274-CONTEXT.md (D-274-BIT-SLICE-01)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_backward_trace.md
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_commitment_window.md
  </read_first>
  <action>
LBX-WT-04 — Update the bit-allocation NatSpec block in `_resolveLootboxCommon` at L838-847.

(a) Insert new entry between the `bits[120..151] boon roll % BOON_PPM_SCALE` line and the `Total primary-chunk consumption:` line. New entry exactly: `///        bits[152..167] fracRoundUp % 100      (_resolveLootboxCommon manual-path ticket whole-collapse; auto-resolve paths leave slice unread)`.

(b) Update the total-consumption line from `///      Total primary-chunk consumption: 152 bits / 256 available.` to `///      Total primary-chunk consumption: 168 bits / 256 available (bits[152..167] consumed only on manual paths; auto-resolve paths leave the slice unread).`.

Storage layout proof. Run the storage-keyword diff vs baseline `06623edb` using the recipe at G20 in canonical_grep_recipes. Expected: empty diff (zero storage declarations changed; only constants added which do not affect storage layout).

Constant-decl diff (expected: exactly 1 `+` line for LOOTBOX_WWXRP_CONSOLATION; zero `-` lines). Recipe at G20 follow-up.

External-function diff (expected: empty; zero new external mutation entry points). Recipe at G21.

Modifier diff (expected: empty). Recipe at G22.

Bytecode delta measurement. Run pre/post compile and capture deployed-bytecode size for `DegenerusGameLootboxModule`:
- Pre-baseline: stash both files, compile, capture size to `/tmp/274-bytecode-pre.txt`
- Post-edit: unstash, compile, capture size to `/tmp/274-bytecode-post.txt`
- Delta: `paste /tmp/274-bytecode-pre.txt /tmp/274-bytecode-post.txt | awk '{print "pre="$1" post="$2" delta="$2-$1}'`

Expected delta: positive small (~+80 to +200 bytes). New surface: 1 new branch (sentinel comparison) + 1 new event LOG2 + Bernoulli math (4 opcodes) + 1 new external call (`wwxrp.mintPrize` consolation, already in scope at L1588) + 1 new constant (compile-time inlined). Document the actual delta in Task 1.5 commit message body.

RNG backward-trace attestation per `feedback_rng_backward_trace.md`. Author `.planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/.task-1.4-rng-trace.txt` (uncommitted scratch; promoted into Task 1.5 commit body) with these five items:

(i) `seed` derivation byte-identical at v39 at the 4 caller sites: `keccak256(abi.encode(rngWord, player, day, amount))` at L554 (openLootBox), L628 (openBurnieLootBox), L673 (resolveLootboxDirect), L708 (resolveRedemptionLootbox). Verify via `grep -nE "uint256 seed = uint256\(keccak256\(abi\.encode\(rngWord, player, day," contracts/modules/DegenerusGameLootboxModule.sol` returns 4 matches.

(ii) Manual paths: rngWord committed via VRF before player can open. `lootboxRngWordByIndex[index]` written by VRF callback; guard `if (rngWord == 0) revert RngNotReady();` at L534 (openLootBox) and L612 (openBurnieLootBox) prevents pre-fulfillment opens. Player-controllable state between VRF commit and lootbox open: `index` (constrained by storage existence + RNG readiness; caller-chosen but storage-validated), `day` (derived from `_simulatedDayIndex()`; not per-call mutable), `amount` (read from lootbox-storage at L529 / L608; fixed at lootbox-purchase time). Conclusion: `seed` is unknown-at-input-commitment; bits[152..167] cannot be ground.

(iii) Auto-resolve paths: bits[152..167] is NOT consumed (sentinel passing routes through the `_queueTicketsScaled` branch which never reads the slice). Auto-resolve callers (decimator claim + sDGNRS redemption) receive `rngWord` from their upstream callers; commitment-window analysis for these is UNCHANGED from v38 (no v39 mutation introduces a new auto-resolve commitment-window surface).

(iv) Bits[152..159] independence from the other 8 sub-roll consumers (rangeRoll bits[0..15], near-offset bits[16..23], far-offset bits[24..39], pathRoll bits[40..55], tierRoll bits[56..79], varianceRoll bits[80..95], ticketVariance bits[96..119], boonRoll bits[120..151]). `seed` is a single keccak hash; slices are pairwise independent by keccak output-entropy properties. Chi² test at TST-WT-03 is the empirical load-bearing evidence. Bias on bits[152..167] is 16-bit-mod-100 ≤ 0.10% relative (consistent with rangeRoll bits[0..15] uint16 % 100 precedent); acceptable per D-274-BIT-SLICE-01.

(v) RNG commitment-window check per `feedback_rng_commitment_window.md`: zero player-controllable state mutates between VRF request and fulfillment that affects bits[152..167] independently. Verified at (ii) above. The slice is part of the SAME `seed` chunk as the 8 other sub-rolls — no new commitment-window surface.

Capture all five attestation items in `.task-1.4-rng-trace.txt` plus the bytecode delta + storage/external/modifier parity exit codes for Task 1.5 commit-body promotion.

Run hardhat compile and confirm green.
  </action>
  <verify>
    <automated>
# G15 — bit-allocation NatSpec new entry
grep -cE "bits\[152\.\.159\]\s+fracRoundUp" contracts/modules/DegenerusGameLootboxModule.sol   # expect 1
# G16 — bit-allocation total-consumption line updated to 160
grep -cE "Total primary-chunk consumption: 168 bits" contracts/modules/DegenerusGameLootboxModule.sol   # expect 1
# G20 — storage-decl parity vs baseline 06623edb (excludes constants; POSIX-compatible)
diff \
  <(git show 06623edb:contracts/modules/DegenerusGameLootboxModule.sol \
    | grep -E "^\s*(uint|int|bool|address|bytes|mapping|string)\s+(internal|private|public)\s+" \
    | grep -vE "\bconstant\b") \
  <(grep -E "^\s*(uint|int|bool|address|bytes|mapping|string)\s+(internal|private|public)\s+" contracts/modules/DegenerusGameLootboxModule.sol \
    | grep -vE "\bconstant\b"); echo "storage_parity_exit=$?"   # expect 0
# G33 — MANDATORY storage-layout JSON parity (load-bearing proof per Success Criterion #8 + LBX-WT-05)
npx hardhat compile && cat artifacts/build-info/*.json 2>/dev/null | jq -r '.output.contracts["contracts/modules/DegenerusGameLootboxModule.sol"]["DegenerusGameLootboxModule"].storageLayout // empty' > /tmp/v39-storage.json
git show 06623edb -- contracts/modules/DegenerusGameLootboxModule.sol > /dev/null && git stash && npx hardhat compile && cat artifacts/build-info/*.json 2>/dev/null | jq -r '.output.contracts["contracts/modules/DegenerusGameLootboxModule.sol"]["DegenerusGameLootboxModule"].storageLayout // empty' > /tmp/v38-storage.json && git stash pop
diff /tmp/v38-storage.json /tmp/v39-storage.json; echo "storage_layout_json_parity_exit=$?"   # expect 0 (load-bearing JSON parity for storage byte-identity)
# Constant-decl diff: exactly 1 addition (LOOTBOX_WWXRP_CONSOLATION), 0 deletions
diff <(git show 06623edb:contracts/modules/DegenerusGameLootboxModule.sol | grep "private constant") <(grep "private constant" contracts/modules/DegenerusGameLootboxModule.sol) | grep -cE "^> .*LOOTBOX_WWXRP_CONSOLATION"   # expect 1
diff <(git show 06623edb:contracts/modules/DegenerusGameLootboxModule.sol | grep "private constant") <(grep "private constant" contracts/modules/DegenerusGameLootboxModule.sol) | grep -cE "^< "   # expect 0
# G21 — zero new external mutation entry points
diff <(git show 06623edb:contracts/modules/DegenerusGameLootboxModule.sol | grep -E "^\s*function\s.* external") <(grep -E "^\s*function\s.* external" contracts/modules/DegenerusGameLootboxModule.sol); echo "external_parity_exit=$?"   # expect 0
# G22 — zero new modifiers
diff <(git show 06623edb:contracts/modules/DegenerusGameLootboxModule.sol | grep -E "^\s*modifier\s") <(grep -E "^\s*modifier\s" contracts/modules/DegenerusGameLootboxModule.sol); echo "modifier_parity_exit=$?"   # expect 0
# G23 — MintModule untouched
cmp <(git show 06623edb:contracts/modules/DegenerusGameMintModule.sol) contracts/modules/DegenerusGameMintModule.sol; echo "mint_byte_eq_exit=$?"   # expect 0
# G24 — EntropyLib untouched
cmp <(git show 06623edb:contracts/libraries/EntropyLib.sol) contracts/libraries/EntropyLib.sol; echo "entropy_byte_eq_exit=$?"   # expect 0
# seed derivation unchanged at 4 caller sites
grep -cE "uint256 seed = uint256\(keccak256\(abi\.encode\(rngWord, player, day," contracts/modules/DegenerusGameLootboxModule.sol   # expect 4
# Scratch RNG-trace file exists
test -f .planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/.task-1.4-rng-trace.txt; echo "rng_trace_file_exit=$?"   # expect 0
# Compile gate
npx hardhat compile 2>&1 | tail -3
    </automated>
  </verify>
  <done>G15 + G16 + G20 + G21 + G22 + G23 + G24 + G33 grep gates return expected values (all parity exits = 0; bit-allocation NatSpec updated; MintModule + EntropyLib byte-identical; mandatory storage-layout JSON parity via G33 confirms byte-identity at the load-bearing storageLayout-JSON level per Success Criterion #8 + LBX-WT-05). Constant-decl diff shows exactly 1 addition (LOOTBOX_WWXRP_CONSOLATION) and 0 deletions. `seed` derivation byte-identical at 4 caller sites. Bytecode delta + 5-item RNG backward-trace attestation captured in `.task-1.4-rng-trace.txt`. hardhat compile exits 0. No commit yet (batched at Task 1.5).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1.5: WAVE 1 USER-APPROVED batched contract commit gate</name>
  <files>contracts/modules/DegenerusGameLootboxModule.sol, contracts/interfaces/IDegenerusGameModules.sol</files>
  <read_first>
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_contract_commits.md (NEVER commit contracts/ without explicit user approval — ALL agents)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_batch_contract_approval.md (batch all phase contract edits, present one diff, get one approval at the end)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_never_preapprove_contracts.md (orchestrator must NEVER tell agents contract changes are pre-approved)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_wait_for_approval.md (present fix and wait for explicit approval before editing code — extended to commits)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_manual_review_before_push.md (no push without explicit user review of the diff first)
    - .planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/.task-1.4-rng-trace.txt (bytecode delta + RNG backward-trace attestation to promote into commit message)
  </read_first>
  <action>See <what-built> + <how-to-verify> below — this is a checkpoint:human-verify task; action is to present the diff to the user and await explicit `approved` string before committing per `feedback_never_preapprove_contracts.md` + `feedback_wait_for_approval.md`. NEVER pre-approve.</action>
  <verify>
    <automated>Manual verification only — checkpoint:human-verify gate. User runs the steps in &lt;how-to-verify&gt; and types `approved` to resume. No automated pass criterion; resume-signal IS the verify.</automated>
  </verify>
  <what-built>
Wave 1 mutates the two contract files: `contracts/modules/DegenerusGameLootboxModule.sol` + `contracts/interfaces/IDegenerusGameModules.sol`. Manual-path Bernoulli whole-ticket collapse + WWXRP cold-bust consolation + additive `LootboxTicketRoll` event for `_resolveLootboxCommon` MANUAL CALLERS ONLY (openLootBox + openBurnieLootBox). Auto-resolve callers (resolveLootboxDirect + resolveRedemptionLootbox) UNCHANGED — sentinel `type(uint48).max` routes them through the status-quo `_queueTicketsScaled` branch. NO breaking change to `LootBoxOpened.futureTickets` / `BurnieLootOpen.tickets` / `TicketsQueuedScaled` semantics — function-scope `futureTickets` stays scaled across the Bernoulli math (separate `whole` local for `_queueTickets` call only). Bit-allocation NatSpec updated 152→160. Storage layout byte-identical; public ABI byte-identical (no signature changes on the 4 external functions). MintModule + EntropyLib byte-identical at v39 HEAD vs `06623edb`.
  </what-built>
  <how-to-verify>
1. Run `git diff contracts/modules/DegenerusGameLootboxModule.sol contracts/interfaces/IDegenerusGameModules.sol` and present the FULL diff to the user.
2. Concatenate `.planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/.task-1.4-rng-trace.txt` to the diff presentation: bytecode delta + storage parity proof + external-function parity proof + modifier parity proof + MintModule/EntropyLib byte-identity + 5-item RNG backward-trace attestation.
3. Present a per-requirement attribution summary listing LBX-WT-01..05 + LBX-WX-01..04 + LBX-EVT-01..06 with file:line for each.
4. Confirm grep gates G1..G24 all pass (re-run inline at presentation time).
5. **WAIT** for the user to type the literal string `approved` (case-sensitive) in their reply. Per `feedback_never_preapprove_contracts.md`: do NOT pre-approve. Per `feedback_wait_for_approval.md`: do NOT commit before explicit approval. Per `feedback_manual_review_before_push.md`: do NOT push after commit.
6. Upon `approved`, build the commit message via HEREDOC and run a `git commit` whose subject is `feat(274): manual lootbox Bernoulli whole-ticket + WWXRP consolation + LootboxTicketRoll event [LBX-WT-01..05, LBX-WX-01..04, LBX-EVT-01..06]`. The commit message body MUST include:
   - LBX-WT-01..05 attribution paragraph (Bernoulli math + manual/auto-resolve branching + bit-allocation NatSpec update + storage parity proofs)
   - LBX-WX-01..04 attribution paragraph (consolation constant + predicate + event reuse)
   - LBX-EVT-01..06 attribution paragraph (new event declaration in module + interface; no-breaking-change posture for `LootBoxOpened.futureTickets` / `BurnieLootOpen.tickets` / `TicketsQueuedScaled`; field-semantics table)
   - 4-callsite update summary (real index in 2; sentinel in 2)
   - Bytecode delta: `<PRE>` → `<POST>` (delta `<DELTA>`); target +80..+200 range
   - Storage layout parity: byte-identical vs `06623edb` (G20 + G21 + G22 parity exit 0)
   - Public ABI parity: byte-identical (4 external functions unchanged)
   - RNG backward-trace attestation (5 items from `.task-1.4-rng-trace.txt`)
   - RNG commitment-window: degenerate-PASS for v39 manual-path additions (no new commitment-window surface)
   - Out-of-scope reaffirmation per D-274-MANUAL-ONLY-01 + D-274-AUTORESOLVE-OUT-01 + D-274-MINTBOOST-OUT-01 + D-274-JACKPOT-OUT-01 + D-274-KI-01
   - Trailing line: `Per feedback_no_contract_commits.md + feedback_batch_contract_approval.md + feedback_never_preapprove_contracts.md: this is the single batched Wave 1 USER-APPROVED contract commit covering LBX-WT-01..05 + LBX-WX-01..04 + LBX-EVT-01..06.`
7. Stage the two files with explicit paths (avoid `git add -A`):
   `git add contracts/modules/DegenerusGameLootboxModule.sol contracts/interfaces/IDegenerusGameModules.sol`
   Then `git commit -m "$(cat <<'EOFCOMMIT' ... EOFCOMMIT)"` with the body assembled per step 6. Setting environment variable `CONTRACTS_COMMIT_APPROVED=1` may be required if a pre-commit guard intercepts (per `feedback_no_contract_commits.md` policy).
8. After commit, delete `.planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/.task-1.4-rng-trace.txt` (scratch file).
9. Per `feedback_manual_review_before_push.md`: do NOT `git push`. The push is the user's manual action after the audit deliverable lands at Wave 3 close.
  </how-to-verify>
  <resume-signal>User types `approved` → agent runs commit. User types anything else → agent does NOT commit; presents follow-up question or revision plan.</resume-signal>
  <done>Wave 1 batched commit lands. `git log --oneline -1` shows subject `feat(274): manual lootbox Bernoulli whole-ticket + WWXRP consolation + LootboxTicketRoll event [LBX-WT-01..05, LBX-WX-01..04, LBX-EVT-01..06]`. `.task-1.4-rng-trace.txt` removed. Commit message body contains bytecode delta + parity proofs + 5-item RNG backward-trace attestation + per-requirement attribution. NO push performed (manual review gate honored).</done>
</task>


<!-- ================================================================ -->
<!-- WAVE 2 — TEST MUTATIONS (BATCHED, USER-APPROVED at end)           -->
<!-- TST-WT-01..07 + TST-WX-01..03 + TST-REG-01..04                    -->
<!-- ================================================================ -->

<task type="auto">
  <name>Task 2.1: TST-WT-01..07 — manual-path whole-ticket assertions</name>
  <files>test/unit/LootboxWholeTicket.test.js, test/stat/LootboxBernoulliEv.test.js</files>
  <read_first>
    - test/unit/DistressLootbox.test.js (existing openLootBox fixture pattern + rngWord mocking)
    - test/edge/BafCreditRouting.test.js (existing day-advance + level-advance helpers + event-assertion patterns)
    - test/stat/LootboxEntropyDistribution.test.js (chi² infra + makeRng + CHI2_CRIT_05 + wilsonHilfertyZ pattern from Phase 261 origin)
    - test/stat/TraitDistribution.test.js (canonical chi² helper declarations — re-declare verbatim per Phase 266 STAT-03 reuse-existing-tooling discipline)
    - contracts/modules/DegenerusGameLootboxModule.sol post-Wave-1 (verify event ABI + scaledPre semantics)
    - .planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/274-CONTEXT.md (REQUIREMENTS.md TST-WT-01..07 success criteria)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_test_rnglock.md (must test rngLocked removal from coinflip claim paths — defensive; v39 does NOT touch coinflip but discipline applies to new _queueTickets manual-path invocation as well)
  </read_first>
  <action>
Author NEW file `test/unit/LootboxWholeTicket.test.js`. Use Hardhat + mocha + chai + ethers. Mirror the deployment/fixture pattern from `test/unit/DistressLootbox.test.js` + `test/edge/BafCreditRouting.test.js`. Strategy: deploy the full game stack, fund a player, purchase a lootbox, force a specific `rngWord`, call `openLootBox(player, index)`, and assert event emissions + storage state.

TST-WT-01 (EV-neutrality property — author SEPARATE file `test/stat/LootboxBernoulliEv.test.js` if the in-test seed loop exceeds 90 seconds of mocha runtime; otherwise keep in `LootboxWholeTicket.test.js`):
- ≥10K seeded `openLootBox` calls across a representative span of pre-Bernoulli scaled values: {47, 99, 100, 147, 250, 1000, 9999}. Force each scaled value by manipulating the rngWord input to land on a known ticket-path with a known `_lootboxTicketCount` output (use small priced lootboxes + decimator-claim-style direct-seed fixtures).
- Property: `mean(whole_post) × TICKET_SCALE` within `±0.5%` of pre-Bernoulli scaled value at N=10K. Use the same Wilson-Hilferty z-statistic helper from `test/stat/TraitDistribution.test.js`.
- Document the rngWord-forcing strategy inline in the test header. If a deployed mock contract is needed to deterministically force the seed (e.g., a test-only RNG injection at the BurnieCoinflip / DegenerusGame storage layer), declare it under `contracts/test/` (planner-discretion per CONTEXT.md `<claudes_discretion>` — recommend the directly-invoked internal-function fixture path via a tester contract similar to `contracts/test/PriceLookupTester.sol`, scope LOCAL to test only).

TST-WT-02 (boundary tests):
- 7 cases: scaledPre ∈ {0, 1, 99, 100, 101, 199, 200}.
- Deterministic assertions:
  - scaledPre=0 → outer `if (futureTickets != 0)` not entered → ZERO emissions of TicketsQueued / TicketsQueuedScaled / LootboxTicketRoll / LootBoxWwxrpReward; LootBoxOpened.futureTickets emits 0.
  - scaledPre=100 → whole=1 deterministically (frac=0; no Bernoulli roll); TicketsQueued(player, level, 1); LootboxTicketRoll(player, index, 100, false) emitted; queued count == 1.
  - scaledPre=199 → whole=1 + 99% Bernoulli; EV ~1.99; per-call assertion is roundedUp matches the seed-derived bit.

TST-WT-03 bits[152..167] independence: chi² over ≥10K seeds for pairwise correlation between
bits[152..167] and the 8 existing primary-chunk consumers. PLUS source-side proof that
auto-resolve paths never read the slice: grep the post-Wave-1 module for `seed >> 152`,
locate the unique match, walk 5 lines upward, assert the enclosing block predicate
contains `index != type(uint48).max`. Hardhat test reads the file, locates the line via
regex, traverses upward, asserts the manual-branch gate. If walking AST is excessive,
the simpler grep `grep -B5 "seed >> 152" contracts/modules/DegenerusGameLootboxModule.sol
| grep -c "if (index != type"` returning 1 is acceptable evidence.
- Chi² infra: `CHI2_CRIT_05` from `test/stat/TraitDistribution.test.js` (re-declared verbatim per Phase 266 STAT-03 reuse-existing-tooling discipline). Test passes if chi² statistic falls below critical at p=0.05 across each pair of 9 sub-roll consumers.
- Test-side seed reconstruction: recompute `keccak256(abi.encode(rngWord, player, day, amount))` for each call and slice bits[152..167] for the empirical sample.

TST-WT-04 (TicketsQueued emission on manual paths):
- Author `it("emits TicketsQueued (not TicketsQueuedScaled) on manual openLootBox when ticket-path produces whole >= 1")`.
- Force a seed where `whole >= 1` post-Bernoulli; call `openLootBox`; assert `TicketsQueued(buyer, level, qty=whole)` is emitted; assert `TicketsQueuedScaled` is NOT emitted in the same tx.
- Same assertion for `openBurnieLootBox`.

TST-WT-05 (LootBoxOpened.futureTickets scaled-preservation):
- Author `it("LootBoxOpened.futureTickets continues emitting scaled value on manual paths (D-274-NO-EVT-BREAK-01)")`.
- Force a seed where scaledPre=247 (e.g.) and openLootBox; assert `LootBoxOpened.futureTickets == 247` (NOT 2 or 3).
- Cross-correlate same-tx `LootboxTicketRoll.preRollTickets == 247`.
- Mirror assertion for openBurnieLootBox: `BurnieLootOpen.tickets == 247` post-Bernoulli regardless of whole outcome.
- Auto-resolve mirror: trigger `resolveLootboxDirect` with a known seed; assert `LootBoxOpened.futureTickets == scaled` (status-quo behavior).

TST-WT-06 (LootboxTicketRoll 4-lattice + 3 negative cases):
- 4 positive cases (manual-path emit verified via `(player, lootboxIndex)` topic filter):
  - (a) `preRollTickets=300, frac=0` → emit with `roundedUp=false`, derived whole=3.
  - (b) `preRollTickets=247, Bernoulli wins` → emit with `roundedUp=true`, derived whole=3.
  - (c) `preRollTickets=247, Bernoulli loses` → emit with `roundedUp=false`, derived whole=2.
  - (d) `preRollTickets=47, Bernoulli loses` → emit with `roundedUp=false`, derived whole=0, same-tx LootBoxWwxrpReward(player, day, amount, 1 ether) correlates consolation.
- 3 negative cases (NO emit):
  - (e) Manual lootbox open landing on DGNRS/large-BURNIE/regular-WWXRP path (pathRoll lands in 11..13 region) → NO LootboxTicketRoll.
  - (f) Manual lootbox open where `_lootboxTicketCount` math truncated scaled to 0 from a degenerate-tiny budget → NO LootboxTicketRoll (outer guard not entered).
  - (g) Auto-resolve via `resolveLootboxDirect` or `resolveRedemptionLootbox` regardless of outcome → NO LootboxTicketRoll.

TST-WT-07 (field-consistency invariants):
- For each emitted event in TST-WT-06 (a-d):
  - Derived `whole = (preRollTickets / 100) + (roundedUp ? 1 : 0)` equals queued ticket count at the level (via `ticketsOwedPacked[wk][player] >> 8`).
  - `preRollTickets > 0` always (guard invariant).
  - `preRollTickets` equals same-tx `LootBoxOpened.futureTickets` (scaled emission preservation).
  - `lootboxIndex` equals the `index` arg passed to `openLootBox` / `openBurnieLootBox`.
  - When derived whole == 0, same-tx `LootBoxWwxrpReward` exists (consolation correlation).
  - Same-tx storage at `lootboxEth[index][player]` / `lootboxBurnie[index][player]` was zeroed (open completion preserved).

Run `npx hardhat test test/unit/LootboxWholeTicket.test.js` and `npx hardhat test test/stat/LootboxBernoulliEv.test.js` (if split) and confirm both exit 0. No commit yet (batched at Task 2.5).

Open question for executor (raise as a follow-up if blocked): if the existing test infrastructure does NOT support deterministic seed forcing at the `lootboxRngWordByIndex[index]` level, a small test-only RNG injector contract may be required at `contracts/test/` (analogous to `contracts/test/PriceLookupTester.sol`). Planner's discretion to introduce this if required for TST-WT-01 + TST-WT-03 deterministic property assertions. Document the inclusion (if any) in Task 2.5 commit body.
  </action>
  <verify>
    <automated>
# TST-WT-01..07 test file authored
test -f test/unit/LootboxWholeTicket.test.js; echo "tst_wt_file_exit=$?"   # expect 0
# Tests pass
npx hardhat test test/unit/LootboxWholeTicket.test.js 2>&1 | tail -10   # expect "passing" lines, no failing
# TST-WT-01 EV-neutrality file (if split)
if test -f test/stat/LootboxBernoulliEv.test.js; then
  npx hardhat test test/stat/LootboxBernoulliEv.test.js 2>&1 | tail -10
fi
# TST-WT-03 bit-slice grep (static-analysis check): bits[152..167] consumed ONLY in manual branch
grep -nB3 -A1 "seed >> 152" contracts/modules/DegenerusGameLootboxModule.sol | grep -cE "index != type\(uint48\)\.max"   # expect >=1 (slice read sits inside manual branch)
    </automated>
  </verify>
  <done>`test/unit/LootboxWholeTicket.test.js` exists, contains 7 TST-WT-NN describe/it blocks (or 6 if TST-WT-01 split to `test/stat/LootboxBernoulliEv.test.js`), all tests pass via `npx hardhat test`. Bit-slice static-analysis check confirms `seed >> 152` read sits inside the manual branch guard. No commit yet (batched at Task 2.5).</done>
</task>

<task type="auto">
  <name>Task 2.2: TST-WX-01..03 — WWXRP consolation predicate tests</name>
  <files>test/unit/LootboxConsolation.test.js</files>
  <read_first>
    - test/unit/DistressLootbox.test.js (existing openLootBox + rngWord injection patterns)
    - test/unit/BurnieCoinflip.test.js (existing WWXRP balance assertion patterns)
    - contracts/modules/DegenerusGameLootboxModule.sol post-Wave-1 (LOOTBOX_WWXRP_CONSOLATION constant + manual-branch consolation predicate)
  </read_first>
  <action>
Author NEW file `test/unit/LootboxConsolation.test.js`.

TST-WX-01 (cold-bust trigger):
- Force a seed where `futureTickets` pre-Bernoulli is in (0, TICKET_SCALE) — i.e., scaledPre ∈ {1, 47, 99} — AND `(uint16(seed >> 152) % 100) >= frac` (Bernoulli fails).
- Call `openLootBox(player, index)`.
- Assertions:
  - ZERO `TicketsQueued` events.
  - ONE `LootBoxWwxrpReward(player, day, amount, 1 ether)` event.
  - `wwxrp.balanceOf(player)` increased by exactly `1 ether` (use ethers `parseEther("1")` and compare against pre-call balance).
  - ONE `LootboxTicketRoll(player, index, scaledPre, false)` event with derived whole=0.
- Mirror test for `openBurnieLootBox`.

TST-WX-02 (non-trigger predicate matrix):
- (a) Ticket-path not selected on manual path (pathRoll lands DGNRS / large-BURNIE / regular-WWXRP): force seed where `uint16(seed >> 40) % 20 >= 11`. Assert ZERO consolation `LootBoxWwxrpReward` (the 10%-path WWXRP win still fires regularly when applicable; distinguish by absence of corresponding LootboxTicketRoll).
- (b) Ticket-path selected but `_lootboxTicketCount` math truncated scaled to 0 from start: force degenerate-tiny lootbox budget + high-price target level so `_lootboxTicketCount` returns 0. Assert outer `if (futureTickets != 0)` guard not entered; ZERO consolation; ZERO LootboxTicketRoll.
- (c) Ticket-path selected with `whole >= 1` post-Bernoulli (success case): force scaledPre=247 + Bernoulli wins → whole=3. Assert ZERO consolation `LootBoxWwxrpReward(..., 1 ether)`; ONE TicketsQueued; ONE LootboxTicketRoll(..., true).
- (d) Auto-resolve open via `resolveLootboxDirect` regardless of pre-Bernoulli state: force seed where scaledPre ∈ {1, 47, 99} on auto-resolve path. Assert ZERO consolation `LootBoxWwxrpReward`; ONE `TicketsQueuedScaled`; ZERO `LootboxTicketRoll`. Mirror for `resolveRedemptionLootbox`.

TST-WX-03 (magnitude assertion — defensive drift catch):
- Read both constants via a public getter shim contract (`contracts/test/LootboxConstantsTester.sol` if needed) OR via storage-layout inspection.
- Assert `LOOTBOX_WWXRP_CONSOLATION == LOOTBOX_WWXRP_PRIZE == ethers.parseEther("1")`.
- If either constant drifts in a future commit (e.g., user changes one but not the other), this test catches it.

Run `npx hardhat test test/unit/LootboxConsolation.test.js` and confirm green.
  </action>
  <verify>
    <automated>
# Test file authored
test -f test/unit/LootboxConsolation.test.js; echo "tst_wx_file_exit=$?"   # expect 0
# Tests pass
npx hardhat test test/unit/LootboxConsolation.test.js 2>&1 | tail -10   # expect "passing", no failing
    </automated>
  </verify>
  <done>`test/unit/LootboxConsolation.test.js` exists with TST-WX-01..03 describe blocks; all tests pass. Cold-bust trigger fires `LootBoxWwxrpReward(..., 1 ether)` + balance increment + ZERO TicketsQueued + LootboxTicketRoll whole=0. Non-trigger predicate matrix (4 sub-cases) all verified. Magnitude assertion catches future constant drift. No commit yet (batched at Task 2.5).</done>
</task>

<task type="auto">
  <name>Task 2.3: TST-REG-01..04 — regression coverage (manual queue exits _rollRemainder; mint-boost + auto-resolve byte-equivalence; cross-mixing variance)</name>
  <files>test/edge/LootboxAutoResolveRegression.test.js</files>
  <read_first>
    - test/edge/BafCreditRouting.test.js (level-advance + ticket-resolution + _rollRemainder observability patterns)
    - test/edge/BackfillIdempotency.test.js (mint-boost fractional-path test patterns)
    - test/edge/MultiBoon.test.js (boost-applied mint patterns)
    - contracts/modules/DegenerusGameMintModule.sol L380-460 + L640-660 + L1142 (processFutureTicketBatch + _rollRemainder + mint-boost _queueTicketsScaled callsite)
    - contracts/storage/DegenerusGameStorage.sol L562-641 (_queueTickets + _queueTicketsScaled + rem byte semantics)
  </read_first>
  <action>
Author NEW file `test/edge/LootboxAutoResolveRegression.test.js`.

TST-REG-01 (manual-only queues no longer enter `_rollRemainder`):
- Setup: open N (e.g., 5) manual lootboxes via `openLootBox` targeting a single far-future level for a single player. Use distinct rngWords forcing varied scaledPre values. No mint-boost activity. No auto-resolve activity.
- Advance to the target level.
- Assert: at activation time, NO `_rollRemainder` invocation occurs for the manual player's queue entry. Observable proxy: `ticketsOwedPacked[wk][player] & 0xFF == 0` (rem byte is 0) at the moment immediately before `processFutureTicketBatch` would process it. Alternative observable: instrument the test to count emitted ticket-mint events vs queued whole count — equality with no remainder roll required.

TST-REG-02 (mint-boost fractional path still works):
- Setup: trigger a `mint` call with `boostBps != 0` (boost boon active) that produces a fractional `adjustedQty` in `DegenerusGameMintModule.sol` L1140-1142 `_queueTicketsScaled` callsite.
- Advance to target level.
- Assert: `_rollRemainder` fires for the boost-derived `rem` byte residue. Status-quo preservation: v39 narrowly retires the manual lootbox producer of fractional residues, NOT mint-boost.

TST-REG-03 (auto-resolve byte-equivalence — status-quo preservation):
- Setup: trigger `resolveLootboxDirect` (e.g., via decimator-claim path) and `resolveRedemptionLootbox` (via sDGNRS-redemption path) with seeds forcing scaled values in (0, 100), (100, ∞), and exactly 0.
- Assertions (per auto-resolve call):
  - `TicketsQueuedScaled(buyer, level, scaledQty)` emitted (NOT `TicketsQueued`).
  - `rem != 0` state observable in `ticketsOwedPacked[wk][player]` when scaledQty has fractional component.
  - Activation-time `_rollRemainder` fires for the auto-resolve residue.
  - ZERO `LootboxTicketRoll` events emitted.
  - ZERO consolation `LootBoxWwxrpReward(..., 1 ether)` events emitted on cold-bust scaled-low outcomes.

TST-REG-04 (cross-mixing variance test):
- Setup: same player opens 5 manual lootboxes via `openLootBox` + 3 auto-resolve via `resolveLootboxDirect`, all targeting the same future level. Each lootbox sized to produce scaledPre in a controlled range (e.g., 47, 99, 147, 250, 50 manual; 47, 99, 50 auto-resolve).
- Advance to target level.
- Assertions:
  - Manual contributions: each per-lootbox Bernoulli outcome observable via 5 separate `LootboxTicketRoll` events; sum of derived whole tickets in [4, 9] depending on Bernoulli outcomes. Per-lootbox variance higher than v38 cross-lootbox-deterministic-accumulation (documented EV-neutral tradeoff).
  - Auto-resolve contributions: 3 `TicketsQueuedScaled` events; total scaled value pools via `rem` byte accumulation; activation-time `_rollRemainder` rolls the combined remainder once (NOT per-lootbox).
  - Cross-mixing safety: manual `_queueTickets` calls and auto-resolve `_queueTicketsScaled` calls coexist in `ticketsOwedPacked[wk][player]` without corruption — the manual calls write to the `owed` high bits; the auto-resolve calls additively update both `owed` and `rem` low byte. Total `owed` count at activation reflects manual whole + auto-resolve whole-from-scaled (post-remainder).

Run `npx hardhat test test/edge/LootboxAutoResolveRegression.test.js` and confirm green.
  </action>
  <verify>
    <automated>
# Test file authored
test -f test/edge/LootboxAutoResolveRegression.test.js; echo "tst_reg_file_exit=$?"   # expect 0
# Tests pass
npx hardhat test test/edge/LootboxAutoResolveRegression.test.js 2>&1 | tail -10   # expect "passing", no failing
    </automated>
  </verify>
  <done>`test/edge/LootboxAutoResolveRegression.test.js` exists with TST-REG-01..04 describe blocks; all tests pass. Manual-only player queue activation confirmed to skip `_rollRemainder`. Mint-boost fractional path confirmed unaffected. Auto-resolve byte-equivalence verified across multiple seeded scenarios. Cross-mixing variance test confirms per-lootbox vs cross-lootbox-pooled distinction. No commit yet (batched at Task 2.5).</done>
</task>

<task type="auto">
  <name>Task 2.4: Run full test suite + capture clean-run regression baseline</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - test/ tree (full hardhat test runner output)
  </read_first>
  <action>
Run the full hardhat + foundry test suites and capture clean-run baseline:

(1) `npx hardhat test 2>&1 | tee /tmp/274-hardhat-suite.txt` — full hardhat test pass.
(2) `npm run test:stat 2>&1 | tee /tmp/274-test-stat.txt` — stat-only suite (if defined in package.json post-Phase-272 GASPIN-02 (a-alt) split).
(3) `npm run test:gas 2>&1 | tee /tmp/274-test-gas.txt` — gas-pin suite (if defined). Note v36+ acceptance "128k is fine approved" for SURF-05 gas-pin drift carries forward.
(4) `forge test 2>&1 | tee /tmp/274-foundry-suite.txt` — Foundry fuzz tests (LootboxRngLifecycle.t.sol + LootboxBoonCoexistence.t.sol + others touching lootbox surface).

For each: capture pass/fail counts. Document any pre-existing flakes in `.planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/.task-2.4-suite-baseline.txt` (scratch file; promoted into Task 2.5 commit body).

If ANY new failures surface that are attributable to Wave 1 contract changes (not pre-existing flakes):
- Halt and report to user. Do NOT proceed to Task 2.5 commit gate. Pre-existing flakes documented at v37/v38 close (e.g., SURF-05 ~120K combined-suite gas drift under `npm run test:stat` ordering — accepted at v36) carry forward unchanged and do not block Wave 2.

Capture the baseline counts for Task 2.5 commit-body promotion.
  </action>
  <verify>
    <automated>
test -f /tmp/274-hardhat-suite.txt; echo "hardhat_log_exit=$?"   # expect 0
test -f .planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/.task-2.4-suite-baseline.txt; echo "baseline_file_exit=$?"   # expect 0
# Hardhat suite passes (filter for "passing" count > 0 and no "failing" lines for v39-introduced tests)
grep -cE "^\s+[0-9]+ passing" /tmp/274-hardhat-suite.txt   # expect >=1
# Foundry suite (if compiles)
test -f /tmp/274-foundry-suite.txt && grep -cE "Suite result: ok" /tmp/274-foundry-suite.txt || echo "foundry_skipped_or_failed"
    </automated>
  </verify>
  <done>Full hardhat + foundry test suites run. Pass/fail counts captured in `.task-2.4-suite-baseline.txt`. NO new failures attributable to Wave 1 contract changes (any pre-existing flakes documented). Suite baseline promoted into Task 2.5 commit body. No commit yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2.5: WAVE 2 USER-APPROVED batched test commit gate</name>
  <files>test/unit/LootboxWholeTicket.test.js, test/unit/LootboxConsolation.test.js, test/stat/LootboxBernoulliEv.test.js (optional split), test/edge/LootboxAutoResolveRegression.test.js</files>
  <read_first>
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_contract_commits.md (extends to test/ commits per project policy — every test/ commit USER-APPROVED)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_batch_contract_approval.md (batch all phase test edits into single commit)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_never_preapprove_contracts.md (NEVER pre-approve)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_wait_for_approval.md
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_manual_review_before_push.md
    - .planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/.task-2.4-suite-baseline.txt (suite-run counts to promote into commit body)
  </read_first>
  <action>See <what-built> + <how-to-verify> below — checkpoint:human-verify gate; await explicit `approved` before committing.</action>
  <verify>
    <automated>Manual verification only. Resume-signal IS the verify.</automated>
  </verify>
  <what-built>
Wave 2 adds 3-4 new test files: `test/unit/LootboxWholeTicket.test.js` (TST-WT-01..07 if EV-property kept inline, else TST-WT-02 + TST-WT-04..07) + `test/unit/LootboxConsolation.test.js` (TST-WX-01..03) + `test/edge/LootboxAutoResolveRegression.test.js` (TST-REG-01..04) + (optional) `test/stat/LootboxBernoulliEv.test.js` (TST-WT-01 + TST-WT-03 if split). May also add `contracts/test/LootboxConstantsTester.sol` or similar test-only shim if needed for deterministic seed forcing or constant inspection. Full hardhat + foundry test suite green; no new failures attributable to Wave 1 contract changes.
  </what-built>
  <how-to-verify>
1. Run `git diff -- test/ contracts/test/` (the latter only if a test-only shim was added) and present the FULL diff to the user.
2. Concatenate `.planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/.task-2.4-suite-baseline.txt` to the diff presentation: full hardhat + foundry pass/fail counts + any pre-existing flakes documented.
3. Present a per-requirement attribution summary listing TST-WT-01..07 + TST-WX-01..03 + TST-REG-01..04 with file:test-block for each.
4. **WAIT** for the user to type the literal string `approved` (case-sensitive). Per `feedback_never_preapprove_contracts.md` (carrying to test/ commits per project policy): do NOT pre-approve. Per `feedback_wait_for_approval.md`: do NOT commit before explicit approval. Per `feedback_manual_review_before_push.md`: do NOT push after commit.
5. Upon `approved`, stage the test files explicitly (avoid `git add -A`) and run a `git commit` whose subject is `test(274): manual lootbox whole-ticket + consolation + auto-resolve regression + LootboxTicketRoll [TST-WT-01..07, TST-WX-01..03, TST-REG-01..04]`. The commit message body MUST include:
   - TST-WT-01..07 attribution paragraph (file:test-block per case; EV-neutrality + boundary + bit-slice chi² + emission + scaled-preservation + 4-lattice + field-consistency)
   - TST-WX-01..03 attribution paragraph (cold-bust trigger + non-trigger predicate matrix + magnitude defensive drift catch)
   - TST-REG-01..04 attribution paragraph (manual-only queue exits _rollRemainder + mint-boost preservation + auto-resolve byte-equivalence + cross-mixing variance)
   - Suite baseline: hardhat passing/failing counts + foundry passing/failing counts (from `.task-2.4-suite-baseline.txt`)
   - Any pre-existing flakes documented as carried-forward (v37/v38 known-flakes register)
   - Test-only shim disclosure (if any new file under contracts/test/ for deterministic seed forcing or constant inspection)
   - Trailing line: `Per feedback_no_contract_commits.md + feedback_batch_contract_approval.md + feedback_never_preapprove_contracts.md (extends to test/ commits per project policy): this is the single batched Wave 2 USER-APPROVED test commit covering TST-WT-01..07 + TST-WX-01..03 + TST-REG-01..04.`
6. Stage with explicit paths (the executor adapts the file list to whatever actually exists; the canonical set is the 3 new test files plus optional split file plus optional contracts/test/ shim):
   `git add test/unit/LootboxWholeTicket.test.js test/unit/LootboxConsolation.test.js test/edge/LootboxAutoResolveRegression.test.js` (extend with `test/stat/LootboxBernoulliEv.test.js` and/or `contracts/test/LootboxConstantsTester.sol` if added). Environment variable `CONTRACTS_COMMIT_APPROVED=1` may be required if a pre-commit guard intercepts a `contracts/test/` shim addition.
7. Build and run the commit via HEREDOC. After commit, delete `.task-2.4-suite-baseline.txt`.
8. Per `feedback_manual_review_before_push.md`: do NOT `git push`.
  </how-to-verify>
  <resume-signal>User types `approved` → agent runs commit. User types anything else → agent does NOT commit; presents follow-up question or revision plan.</resume-signal>
  <done>Wave 2 batched commit lands. Subject: `test(274): manual lootbox whole-ticket + consolation + auto-resolve regression + LootboxTicketRoll [TST-WT-01..07, TST-WX-01..03, TST-REG-01..04]`. `.task-2.4-suite-baseline.txt` removed. Commit body contains 3-paragraph attribution + suite baseline + flakes register + test-only shim disclosure (if any). No push performed.</done>
</task>


<!-- ================================================================ -->
<!-- WAVE 3 — AUDIT DELIVERABLE (AGENT-COMMITTED atomic-per-task)      -->
<!-- audit/FINDINGS-v39.0.md 9-section terminal deliverable            -->
<!-- ================================================================ -->

<task type="auto">
  <name>Task 3.1: audit/FINDINGS-v39.0.md §1 Frontmatter + §2 Executive Summary skeleton</name>
  <files>audit/FINDINGS-v39.0.md</files>
  <read_first>
    - audit/FINDINGS-v38.0.md §1-§2 (template shape — frontmatter YAML + Executive Summary)
    - audit/FINDINGS-v37.0.md §1-§2 (alternative template variant)
    - audit/FINDINGS-v36.0.md §1-§2 (single-phase milestone-closure precedent)
    - .planning/REQUIREMENTS.md (39 v39.0 requirements for severity rubric application)
    - .planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/274-CONTEXT.md (D-274-SEV-01 5-Bucket carry; D-274-FCITE-01 zero-emission)
  </read_first>
  <action>
Author NEW file `audit/FINDINGS-v39.0.md`. Mirror the audit/FINDINGS-v38.0.md skeleton structure. Pre-§3 sections to author at this task:

§1 Frontmatter (YAML):
```yaml
---
milestone: v39.0
title: "Lootbox Whole-Ticket Rounding + WWXRP Consolation"
audit_baseline: MILESTONE_V38_AT_HEAD_06623edb
audit_subject_head: "<v39-close-sha>"  # resolved at Task 3.10
closure_signal: "MILESTONE_V39_AT_HEAD_<sha>"  # resolved at Task 3.10
status: "FINAL — READ-ONLY"   # flipped at Wave 3 final task
read_only: true                # flipped at Wave 3 final task
supersedes: none
phase_shape: single-phase
phase: 274
included_since_baseline:
  - sha: ff929948
    subject: "fix(273): BAF credit routing — day-D orphan + RngLocked predicate + jackpot-phase override + tests"
  - sha: e9807891
    subject: "test(273): BAF-ROUTE-06/07/08 expansion — purchase-phase routing, mid-bracket override skip, markBafSkipped equivalence"
  - sha: e04d3333
    subject: "chore(273): phase SUMMARY — BAF credit routing complete (14/14 tests, security property attested)"
  - sha: 1eb1ecb5
    subject: "docs: clarify _livenessTriggered VRF-grace branch as stalled-advance bailout"
---
```

§2 Executive Summary:
- §2a Closure Verdict Summary (placeholder for §4 surface counts + §5 REG counts + §6 KI counts + §9c closure signal — populated at Task 3.6 + 3.10).
- §2b Severity Counts (D-274-SEV-01 5-Bucket Severity Rubric carry from v38 / D-08):
  - CRITICAL: 0
  - HIGH: 0
  - MEDIUM: 0
  - LOW: 0
  - INFO: 0
  - (Default zero per D-274-KI-01 carry; updated only if adversarial pass at Task 3.7 surfaces FINDING_CANDIDATE.)
- §2c D-08 5-Bucket Severity Rubric Reference (inline reproduction of the rubric per v38 precedent).
- §2d D-09 KI Gating Rubric Reference (inline reproduction).
- §2e Forward-Cite Closure Summary (terminal-phase zero-emission verification per D-274-FCITE-01 — populated at Task 3.8).
- §2f Attestation Anchor (placeholder `MILESTONE_V39_AT_HEAD_<sha>` — resolved at Task 3.10).

Commit subject: `audit(274): seed FINDINGS-v39.0.md §1 frontmatter + §2 executive summary skeleton`. AGENT-COMMITTED atomic per `feedback_no_history_in_comments.md` discipline (no v38 comparative language in §1/§2 prose; describe v39 what IS).
  </action>
  <verify>
    <automated>
test -f audit/FINDINGS-v39.0.md; echo "findings_file_exit=$?"   # expect 0
grep -cE "^## 1\.|^## 2\." audit/FINDINGS-v39.0.md   # expect 2 (§1 and §2 headers)
grep -cE "MILESTONE_V38_AT_HEAD_06623edb" audit/FINDINGS-v39.0.md   # expect >=1 (audit baseline)
grep -cE "MILESTONE_V39_AT_HEAD_" audit/FINDINGS-v39.0.md   # expect >=2 (closure_signal placeholder in YAML + §2f anchor)
grep -cE "ff929948|e9807891|e04d3333|1eb1ecb5" audit/FINDINGS-v39.0.md   # expect 4 (Phase 273 commits in YAML)
git log -1 --format='%s' | grep -cE "^audit\(274\): seed FINDINGS-v39\.0\.md"   # expect 1
    </automated>
  </verify>
  <done>`audit/FINDINGS-v39.0.md` exists with §1 + §2 skeleton; YAML frontmatter contains audit_baseline + closure_signal placeholder + 4 Phase 273 included-since-baseline commits; 5-Bucket Severity Rubric default zero counts populated; atomic commit landed with subject `audit(274): seed FINDINGS-v39.0.md §1 frontmatter + §2 executive summary skeleton`.</done>
</task>

<task type="auto">
  <name>Task 3.2: §3a Phase 274 + §3.A AUDIT-01 delta-surface table (LBX-WT + LBX-WX + LBX-EVT + Phase 273 included-since-baseline rows + Wave 1/2 commit rows)</name>
  <files>audit/FINDINGS-v39.0.md</files>
  <read_first>
    - audit/FINDINGS-v38.0.md §3a + §3.A (template shape — Phase header + delta-surface table)
    - .planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/274-CONTEXT.md (D-274-BAF273-INCLUDE-01 — 4 Phase 273 commits get §3.A row coverage as included-since-baseline)
    - .planning/phases/273-baf-credit-routing-fix/273-CONTEXT.md (Phase 273 scope + commit attribution)
    - Wave 1 commit SHA (the v39 contract commit subject + body)
    - Wave 2 commit SHA (the v39 test commit subject + body)
    - .planning/REQUIREMENTS.md (LBX-WT-01..05 + LBX-WX-01..04 + LBX-EVT-01..06 hunk-level evidence)
  </read_first>
  <action>
Author §3a (Phase 274 — Lootbox Whole-Ticket Rounding + WWXRP Consolation (Terminal)) + §3.A delta-surface table inside `audit/FINDINGS-v39.0.md`.

§3a Phase 274 prose (mirroring v38 §3a Phase 272 shape):
- Phase shape: single-phase, multi-wave structure per D-274-CLOSURE-01 (mirrors v36.0 Phase 266 + v38.0 Phase 272 precedent).
- Scope: MANUAL CALLERS ONLY per D-274-MANUAL-ONLY-01.
- 1 Wave 1 USER-APPROVED contract commit (subject + SHA) + 1 Wave 2 USER-APPROVED test commit (subject + SHA) + N Wave 3+ AGENT-COMMITTED audit-deliverable commits.
- Bytecode delta + storage-layout byte-identity + ABI byte-identity attestations.

§3.A AUDIT-01 Delta-Surface Table (markdown table with these column headers: `Source | Path | Line | Requirement | Classification | Hunk Evidence | Surface Verdict`). Row groups:

Row Group 1 — LBX-WT (manual-path whole-ticket Bernoulli):
- LBX-WT-01: `contracts/modules/DegenerusGameLootboxModule.sol _resolveLootboxCommon` distress-bonus block preservation | L976-983 | REFACTOR_ONLY | scaled accumulation preserved on BOTH paths | SAFE_BY_DESIGN
- LBX-WT-02: Bernoulli math (scaledPre + whole + frac + roundedUp) | L984-994 (post-edit) | MODIFIED_LOGIC | bits[152..167] consumed | SAFE
- LBX-WT-03: queue-helper switch (`_queueTickets` manual / `_queueTicketsScaled` auto-resolve) | L995-1008 (post-edit) | MODIFIED_LOGIC | branch on `index != type(uint48).max` | SAFE
- LBX-WT-04: bit-allocation NatSpec update | L838-848 (post-edit) | REFACTOR_ONLY | 152 → 160 total consumption | SAFE_BY_STRUCTURAL_CLOSURE
- LBX-WT-05: storage layout byte-identity | (entire file) | REFACTOR_ONLY | G20 + G21 + G22 parity exit 0 | SAFE_BY_STRUCTURAL_CLOSURE

Row Group 2 — LBX-WX (WWXRP cold-bust consolation):
- LBX-WX-01: new `LOOTBOX_WWXRP_CONSOLATION` constant | L279 (post-edit; sibling to L278) | NEW | `uint256 private constant = 1 ether` | SAFE_BY_DESIGN
- LBX-WX-02: consolation predicate (`if (whole == 0)` else branch) | L998-1002 (post-edit) | MODIFIED_LOGIC | `wwxrp.mintPrize` + `LootBoxWwxrpReward` emit | SAFE
- LBX-WX-03: predicate non-trigger gating | L995-1008 (post-edit) | MODIFIED_LOGIC | `if (index != type(uint48).max) AND (whole == 0) AND (futureTickets > 0)` | SAFE_BY_DESIGN
- LBX-WX-04: event-shape decision (reuse existing `LootBoxWwxrpReward`) | L127-132 (existing event) | REFACTOR_ONLY | no new event variant | SAFE_BY_DESIGN

Row Group 3 — LBX-EVT (new additive event):
- LBX-EVT-01: NO breaking change — `LootBoxOpened.futureTickets` + `BurnieLootOpen.tickets` + `TicketsQueuedScaled` semantics unchanged | (across module) | REFACTOR_ONLY | G17 + G18 + G19 parity | SAFE_BY_STRUCTURAL_CLOSURE
- LBX-EVT-02: `_queueTickets` emits `TicketsQueued`; `_queueTicketsScaled` emits `TicketsQueuedScaled` (origin-tracking by event type) | (storage helpers — UNCHANGED) | REFACTOR_ONLY | mint-boost + auto-resolve continue emitting Scaled | SAFE_BY_DESIGN
- LBX-EVT-03: new event declaration | `contracts/modules/DegenerusGameLootboxModule.sol` L127-region + `contracts/interfaces/IDegenerusGameModules.sol` L264-region | NEW | 4-field schema; both player + lootboxIndex indexed | SAFE_BY_DESIGN
- LBX-EVT-04: field semantics (preRollTickets = scaledPre = same-tx LootBoxOpened.futureTickets; roundedUp = Bernoulli outcome) | (emit site) | NEW | consumer derives whole = (preRollTickets / 100) + (roundedUp ? 1 : 0) | SAFE_BY_DESIGN
- LBX-EVT-05: index parameter threading (signature + 4 callers) | L860 (signature) + L583/L637/L679/L714 (callers) | MODIFIED_LOGIC | sentinel `type(uint48).max` for auto-resolve; real index for manual | SAFE
- LBX-EVT-06: event-emission ordering (TicketsQueued/LootBoxWwxrpReward → LootboxTicketRoll → LootBoxOpened) | (single-tx emit order) | MODIFIED_LOGIC | consumer groups by tx + player + lootboxIndex | SAFE_BY_DESIGN

Row Group 4 — Phase 273 included-since-baseline (D-274-BAF273-INCLUDE-01 surface-coverage attestation; no F-39-NN finding eligible):
- Commit `ff929948` `fix(273): BAF credit routing — day-D orphan + RngLocked predicate + jackpot-phase override + tests` | `contracts/BurnieCoinflip.sol :525, :585-598, :1035-1045` | MODIFIED_LOGIC | Surface verdict: SAFE_BY_DESIGN_PHASE_273
- Commit `e9807891` `test(273): BAF-ROUTE-06/07/08 expansion — purchase-phase routing, mid-bracket override skip, markBafSkipped equivalence` | `test/edge/BafCreditRouting.test.js` | REFACTOR_ONLY (TEST) | Surface verdict: SAFE_BY_DESIGN_PHASE_273
- Commit `e04d3333` `chore(273): phase SUMMARY` | `.planning/phases/273-baf-credit-routing-fix/273-01-SUMMARY.md` | DOCS_ONLY | Surface verdict: SAFE_BY_STRUCTURAL_CLOSURE
- Commit `1eb1ecb5` `docs: clarify _livenessTriggered VRF-grace branch as stalled-advance bailout` | NatSpec only | DOCS_ONLY | Surface verdict: SAFE_BY_STRUCTURAL_CLOSURE

Footnote — commit `1cae7682` `chore(273): scaffold Phase 273 in ROADMAP + STATE`: OUT-OF-AUDIT-SCOPE (planning-only commit between `ff929948` and `e9807891`; touches only ROADMAP/STATE planning artifacts, no contract/test changes; surfaces nothing for the audit-of-record). Recorded here for log completeness only; not eligible for §3.A row coverage.

Row Group 5 — Phase 274 Wave 1 + Wave 2 commit rows:
- Wave 1 contract commit `<wave1-sha>` `feat(274): manual lootbox Bernoulli whole-ticket + WWXRP consolation + LootboxTicketRoll event [LBX-WT-01..05, LBX-WX-01..04, LBX-EVT-01..06]` | `contracts/modules/DegenerusGameLootboxModule.sol + contracts/interfaces/IDegenerusGameModules.sol` | MODIFIED_LOGIC | Aggregates Row Groups 1-3 | SAFE per per-row verdicts
- Wave 2 test commit `<wave2-sha>` `test(274): manual lootbox whole-ticket + consolation + auto-resolve regression + LootboxTicketRoll [TST-WT-01..07, TST-WX-01..03, TST-REG-01..04]` | `test/unit/ + test/edge/ + test/stat/` | REFACTOR_ONLY (TEST) | Empirical evidence for §4 surfaces (a) + (b) + (c) + (e) + (g) | SAFE_BY_STRUCTURAL_CLOSURE

Commit subject: `audit(274): §3a + §3.A delta-surface table (LBX-WT + LBX-WX + LBX-EVT + Phase 273 included + Wave 1/2 rows)`. AGENT-COMMITTED atomic.
  </action>
  <verify>
    <automated>
grep -cE "^### 3a\.|^## 3\.|^### 3\.A" audit/FINDINGS-v39.0.md   # expect >=2 (§3a header + §3.A header)
grep -cE "LBX-WT-0[1-5]|LBX-WX-0[1-4]|LBX-EVT-0[1-6]" audit/FINDINGS-v39.0.md   # expect >=15 (each REQ ID appears in §3.A row)
grep -cE "ff929948|e9807891|e04d3333|1eb1ecb5" audit/FINDINGS-v39.0.md   # expect >=8 (4 in YAML + 4 in §3.A rows)
grep -cE "MODIFIED_LOGIC|REFACTOR_ONLY|NEW|DOCS_ONLY|SAFE_BY_DESIGN|SAFE_BY_STRUCTURAL_CLOSURE" audit/FINDINGS-v39.0.md   # expect >=20
git log -1 --format='%s' | grep -cE "^audit\(274\): §3a \+ §3\.A delta-surface table"   # expect 1
    </automated>
  </verify>
  <done>§3a + §3.A delta-surface table authored with 5 row groups (LBX-WT + LBX-WX + LBX-EVT + Phase 273 included-since-baseline 4-commit rows + Wave 1/2 commit rows). Atomic commit landed. AUDIT-01 + AUDIT-02 (Phase 273 row coverage) satisfied at this task.</done>
</task>

<task type="auto">
  <name>Task 3.3: §3.B AUDIT-04 zero-new-state attestation + §3.C AUDIT-03 conservation re-proof</name>
  <files>audit/FINDINGS-v39.0.md</files>
  <read_first>
    - audit/FINDINGS-v38.0.md §3.B + §3.C (template shape)
    - Wave 1 commit body (storage parity + external-function parity + modifier parity proofs)
    - .task-1.4-rng-trace.txt contents (already promoted into Wave 1 commit message; cite from there)
  </read_first>
  <action>
Author §3.B AUDIT-04 Zero-New-State Attestation inside `audit/FINDINGS-v39.0.md`:
- Zero new storage slots: storage-decl diff vs `06623edb` empty (G20 parity exit 0).
- Zero new admin functions: external-function diff vs `06623edb` empty (G21 parity exit 0). The 4 existing external entry points (openLootBox + openBurnieLootBox + resolveLootboxDirect + resolveRedemptionLootbox + deityBoonSlots + issueDeityBoon) retain byte-identical signatures.
- Zero new modifiers: modifier-decl diff empty (G22 parity exit 0).
- Zero new immutable variables.
- One new constant: `LOOTBOX_WWXRP_CONSOLATION = 1 ether` (compile-time inlined; not a storage slot).
- One new event declaration: `LootboxTicketRoll` (events do not occupy storage slots; event log is calldata-equivalent).
- Cross-module sanity: MintModule + EntropyLib + JackpotModule + Degenerette + TraitUtils byte-identical at v39 HEAD vs `06623edb` (G23 + G24 + G27 cmp exits 0).

Author §3.C AUDIT-03 Conservation Re-Proof inside `audit/FINDINGS-v39.0.md`:
- EV-neutrality re-proof on Bernoulli collapse: `E[whole_post]` per manual-path open = `(whole_floor) × P(Bernoulli_fails) + (whole_floor + 1) × P(Bernoulli_succeeds) = whole_floor + frac/100`. Since `scaledPre = whole_floor × 100 + frac`, we have `E[whole_post] × 100 = whole_floor × 100 + frac = scaledPre`. Therefore `E[whole_post] == scaledPre / 100` exactly. EV-preserving by construction; per-lootbox variance higher than v38 cross-lootbox-deterministic-accumulation is the documented tradeoff.
- WWXRP supply conservation re-proof: consolation magnitude `1 ether` matches regular 10%-path WWXRP win `1 ether`; both call the same `IWrappedWrappedXRP.mintPrize` interface. No new WWXRP supply path introduced. Cold-bust trigger probability bounded by `P(ticket-path-selected) × P(scaledPre ∈ (0, TICKET_SCALE)) × P(Bernoulli_fails)`; analytical worst-case for high-throughput periods documented in §3.C prose.
- Bit-slice independence re-proof: bits[152..167] is a previously-unallocated slice of the same single-source-of-entropy primary chunk; pairwise-independent of the other 8 slices by keccak output-entropy properties. Empirical evidence: TST-WT-03 chi² at ≥10K seeds passes at p=0.05.
- Storage `rem` byte invariant: manual-path queues no longer write to `rem` (whole-helper `_queueTickets` never touches the low byte). Mint-boost + auto-resolve continue producing `rem` residues; activation-time `_rollRemainder` continues to function unchanged.

Commit subject: `audit(274): §3.B AUDIT-04 zero-new-state + §3.C AUDIT-03 conservation re-proof`. AGENT-COMMITTED atomic.
  </action>
  <verify>
    <automated>
grep -cE "^### 3\.B|^### 3\.C" audit/FINDINGS-v39.0.md   # expect 2
grep -cE "Zero new storage slots|Zero new admin|Zero new modifiers" audit/FINDINGS-v39.0.md   # expect >=3
grep -cE "E\[whole_post\]|EV-neutrality|EV preserving by construction" audit/FINDINGS-v39.0.md   # expect >=2
git log -1 --format='%s' | grep -cE "^audit\(274\): §3\.B AUDIT-04 zero-new-state"   # expect 1
    </automated>
  </verify>
  <done>§3.B + §3.C authored. AUDIT-03 conservation re-proof (EV-neutrality + WWXRP supply + bit-slice independence + storage rem invariant) + AUDIT-04 zero-new-state attestation present. Atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 3.4: §4 F-39-NN finding blocks scaffold + 8-surface adversarial-sweep row table (a..h)</name>
  <files>audit/FINDINGS-v39.0.md</files>
  <read_first>
    - audit/FINDINGS-v38.0.md §4 (template shape — 7-surface table at v38; v39 adds 1 surface to total 8)
    - .planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/274-CONTEXT.md (AUDIT-03 enumerates surfaces (a)..(h))
    - .planning/REQUIREMENTS.md AUDIT-03 (8-surface row table specification)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_backward_trace.md
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_commitment_window.md
  </read_first>
  <action>
Author §4 (F-39-NN Finding Blocks) inside `audit/FINDINGS-v39.0.md`:

§4.0 Default zero F-39-NN finding blocks per D-274-KI-01 default zero-promotion path. (Updated only if adversarial pass at Task 3.7 surfaces FINDING_CANDIDATE.)

§4.1 Adversarial Sweep — 8-Surface Row Table. Author markdown table with columns `Surface | Description | Verdict | Grep Recipe | Line Cite | Prose Justification`. Rows:

(a) EV-neutrality of Bernoulli collapse on manual paths vs cross-lootbox accumulation: VERDICT = SAFE_BY_DESIGN. Grep recipe G8 + G9 (Bernoulli math + scaledPre snapshot). Empirical cite: TST-WT-01 EV-property test at ≥10K seeds. Prose: `E[whole_post] == scaledPre / 100` by construction; per-lootbox variance higher than cross-lootbox-deterministic-accumulation is documented tradeoff (CONTEXT.md `<specifics>`).

(b) Bit-slice `[152..159]` independence from other primary-chunk consumers: VERDICT = SAFE. Grep recipe G15 (bit-allocation NatSpec) + G24 (EntropyLib byte-identical). Empirical cite: TST-WT-03 chi² test at ≥10K seeds; passes at p=0.05 across each pair of 9 sub-roll consumers. Prose: single keccak source means slices are pairwise independent by hash-output entropy. Bias is 16-bit-mod-100 ≤ 0.10% relative (precedent: near-offset slice bias).

(c) Consolation trigger predicate cannot fire from non-ticket-path OR auto-resolve path: VERDICT = SAFE_BY_DESIGN. Grep recipe G12 + G13 (consolation emit gated structurally inside `if (whole != 0) else` of manual branch). Empirical cite: TST-WX-02 non-trigger predicate matrix (4 sub-cases). Prose: structural gating `manualOpen = (index != type(uint48).max) AND scaledPre > 0 AND whole == 0`; auto-resolve callers literally pass the sentinel; non-ticket-path outcomes skip the entire `if (futureTickets != 0)` block.

(d) Storage layout byte-identical: VERDICT = SAFE_BY_STRUCTURAL_CLOSURE. Grep recipe G20 + G21 + G22 (storage-decl + external-function + modifier parity). Empirical cite: cmp exits 0 for MintModule (G23) + EntropyLib (G24); cmp exits 0 for v37/v38/v34 baseline (G27 + G28). Prose: zero storage-decl mutations; new constant + new event do not consume storage slots; zero new admin or modifier paths.

(e) Auto-resolve paths byte-equivalent to v38.0 behavior: VERDICT = SAFE_BY_DESIGN. Grep recipe G11 (auto-resolve `_queueTicketsScaled` call) + G7 (sentinel passing at 2 callsites). Empirical cite: TST-REG-03 auto-resolve byte-equivalence regression. Prose: sentinel routes auto-resolve through the unchanged `_queueTicketsScaled` branch; `rem` byte production + activation-time `_rollRemainder` flow byte-identical to v38; zero `LootboxTicketRoll` or consolation `LootBoxWwxrpReward(..., 1 ether)` from auto-resolve callers.

(f) Index gating discriminator routes manual vs auto-resolve with zero crossover: VERDICT = SAFE_BY_DESIGN. Grep recipe G4 + G6 + G7 (signature + 2 manual-arg + 2 sentinel-arg callsites). Prose: `index` parameter is private (passed only by 4 internal callers); top-of-domain sentinel `type(uint48).max` (= `0xFFFFFFFFFFFF`) cannot collide with any real lootbox index (≥281 trillion lifetime lootboxes would be required to reach). External callers cannot inject `type(uint48).max` since the 2 manual external entry points (`openLootBox` + `openBurnieLootBox`) receive `index` from their callers but the storage lookup `lootboxEth[index][player]` requires non-zero amount which a sentinel index cannot have (would require 281T lootboxes to exist at that index).

(g) `LootboxTicketRoll` field-consistency invariants hold across both manual callers: VERDICT = SAFE_BY_DESIGN. Grep recipe G14 (emit site) + G17 (function-scope futureTickets not reassigned to whole). Empirical cite: TST-WT-07 field-consistency invariants test (derived whole = queued count; preRollTickets == same-tx LootBoxOpened.futureTickets; lootboxIndex == index arg; whole==0 ⇒ same-tx LootBoxWwxrpReward; storage zeroed). Prose: invariants hold by construction — `scaledPre` is snapshotted BEFORE any reassignment; `whole` is a separate local; emit args are direct reads of these locals.

(h) Phase 273 BAF-routing surface coverage at included-baseline: VERDICT = SAFE_BY_DESIGN_PHASE_273. Cross-cite to §3.A Row Group 4 (4 Phase 273 commit rows) + §9.NN.iv (included-since-baseline 4-commit list). Prose: Phase 273 maintenance shipped between v38.0 closure and v39.0 open; folds into v39.0 delta-audit baseline as included-since-baseline surface-coverage attestation; no F-39-NN finding eligible per D-274-BAF273-INCLUDE-01. Phase 273 tests (`test/edge/BafCreditRouting.test.js` 14 tests + BAF-ROUTE-06/07/08 expansion) are the load-bearing empirical evidence for the Phase 273 surface.

Plus 1-line RNG commitment-window degenerate-PASS attestation per `feedback_rng_commitment_window.md`: "Zero new RNG-path mutation at v39; bits[152..167] is a previously-unallocated slice of the unchanged `seed = keccak256(abi.encode(rngWord, player, day, amount))` primary chunk; commitment-window check is degenerate-PASS."

§4.2 Verdict Roll-Up + Adversarial-Pass Status (placeholder — populated at Task 3.7 after PARALLEL adversarial spawn). Default: 8 of 8 SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE; zero F-39-NN finding blocks; zero adversarial disagreements.

Commit subject: `audit(274): §4 8-surface adversarial-sweep row table (a..h) pre-adversarial-pass draft`. AGENT-COMMITTED atomic.
  </action>
  <verify>
    <automated>
grep -cE "^## 4\.|^### 4\.1\.|^### 4\.2\." audit/FINDINGS-v39.0.md   # expect 3
grep -cE "Surface \([a-h]\)|^\| \(a\)|^\| \(b\)|^\| \(c\)|^\| \(d\)|^\| \(e\)|^\| \(f\)|^\| \(g\)|^\| \(h\)" audit/FINDINGS-v39.0.md   # expect >=8
grep -cE "SAFE_BY_DESIGN|SAFE_BY_STRUCTURAL_CLOSURE|SAFE_BY_DESIGN_PHASE_273" audit/FINDINGS-v39.0.md   # expect >=8
grep -cE "feedback_rng_commitment_window|degenerate-PASS" audit/FINDINGS-v39.0.md   # expect >=1
git log -1 --format='%s' | grep -cE "^audit\(274\): §4 8-surface adversarial-sweep"   # expect 1
    
    </automated>
  </verify>
  <done>§4 8-surface adversarial-sweep row table authored with verdicts SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE / SAFE_BY_DESIGN_PHASE_273 across all 8 surfaces (a..h). §4.2 Verdict Roll-Up placeholder pending Task 3.7 adversarial-pass. RNG commitment-window degenerate-PASS attestation present. Atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 3.5: §5 LEAN regression appendix (REG-01..04)</name>
  <files>audit/FINDINGS-v39.0.md</files>
  <read_first>
    - audit/FINDINGS-v38.0.md §5 (template shape — REG-01..04 sub-sections)
    - .planning/REQUIREMENTS.md REG-01..04 specifications
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_backward_trace.md (governs REG-03 EXC-04 EntropyLib byte-identity backward-trace)
  </read_first>
  <action>
Author §5 (Regression Appendix) inside `audit/FINDINGS-v39.0.md`:

§5a REG-01 — v38.0 Intermediate Closure-Signal Non-Widening. Surface set: Degenerette + BURNIE coinflip post-Phase-273 BAF routing + Mint module boost path + auto-resolve lootbox paths (`resolveLootboxDirect` + `resolveRedemptionLootbox`). Run grep recipe G27. Verdict: byte-identical for surfaces NOT in v39 manual-lootbox scope. NOTE: BurnieCoinflip.sol mutated by Phase 273 (ff929948 / e9807891) — explicitly cite the Phase-273-included-since-baseline mutations and confirm they fold into the v39 baseline naturally (not byte-identical at v39 HEAD vs v38 baseline `06623edb` for BurnieCoinflip.sol, but byte-identical vs `1eb1ecb5` HEAD which is the post-Phase-273 v39 audit-subject baseline). REG-01 PASS at v39 HEAD.

§5b REG-02 — v34.0 Closure-Signal Non-Widening. Re-verify `MILESTONE_V34_AT_HEAD_6b63f6d4daf346a53a1d463790f637308ea8d555` non-widening at v39 HEAD. Surface set: TraitUtils + `_pickSoloQuadrant` + JackpotBucketLib. Run grep recipe G28. Verdict: 2 of 2 byte-identical. REG-02 PASS.

§5c REG-03 — KI Envelope Re-Verifications. EXC-01..03 NEGATIVE-scope at v39; EXC-04 NARROWS retained (BAF-jackpot-only; EntropyLib byte-identical via grep recipe G24). Backward-trace per `feedback_rng_backward_trace.md`: EntropyLib byte-identity preserves BAF-jackpot-only NARROWS scope from v36.0. Zero RNG-path mutation at v39 means commitment-window check is degenerate-PASS per `feedback_rng_commitment_window.md`. 4 RE_VERIFIED KI envelope rows.

§5d REG-04 — Prior-Finding Spot-Check Sweep. Sweep `audit/FINDINGS-v25.0.md` + `FINDINGS-v27.0..v38.0.md` for v39-touched function/surface set: `_resolveLootboxCommon` (manual-path additions only) + `_queueTickets` (new lootbox callsite) + auto-resolve callers byte-equivalence (`resolveLootboxDirect` + `resolveRedemptionLootbox`). For each historical finding referencing these surfaces: classify {SUPERSEDED, RE_VERIFIED, NEW_RELEVANT}. Default expectation: all SUPERSEDED-by-newer-baseline or RE_VERIFIED-at-v39 (no NEW_RELEVANT findings emitted by spot-check).

§5e Regression Distribution Summary. Markdown table summarizing the 4 REG sub-sections: row per REG-NN with PASS/FAIL verdict + cite.

Commit subject: `audit(274): §5 LEAN regression appendix (REG-01..04)`. AGENT-COMMITTED atomic.
  </action>
  <verify>
    <automated>
grep -cE "^### 5a\.|^### 5b\.|^### 5c\.|^### 5d\.|^### 5e\." audit/FINDINGS-v39.0.md   # expect 5
grep -cE "MILESTONE_V38_AT_HEAD_06623edb|MILESTONE_V34_AT_HEAD_6b63f6d4" audit/FINDINGS-v39.0.md   # expect >=2
grep -cE "REG-0[1-4]|PASS|SUPERSEDED|RE_VERIFIED" audit/FINDINGS-v39.0.md   # expect >=10
git log -1 --format='%s' | grep -cE "^audit\(274\): §5 LEAN regression"   # expect 1
    </automated>
  </verify>
  <done>§5 regression appendix authored: REG-01 v38.0 closure NON-WIDENING (with Phase 273 carve-out for BurnieCoinflip.sol) + REG-02 v34.0 closure NON-WIDENING + REG-03 KI envelope re-verifications (EXC-01..03 NEGATIVE + EXC-04 NARROWS retained via backward-trace) + REG-04 prior-finding spot-check sweep across v25..v38 + REG distribution summary table. Atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 3.6: §6 KI Gating Walk (§6a Non-Promotion Ledger + §6b 4-row KI Envelope + §6c Verdict Summary) + §7 Prior-Artifact Cross-Cites</name>
  <files>audit/FINDINGS-v39.0.md</files>
  <read_first>
    - audit/FINDINGS-v38.0.md §6 + §7 (template shape)
    - KNOWN-ISSUES.md (verify EXC-01..04 envelope state at v39 HEAD)
    - .planning/REQUIREMENTS.md AUDIT-05 (KI walkthrough + §6c verdict format)
  </read_first>
  <action>
Author §6 (KI Gating Walk) inside `audit/FINDINGS-v39.0.md`:

§6a Non-Promotion Ledger. Zero rows (default per D-274-KI-01 carry). Updated only if adversarial pass at Task 3.7 surfaces FINDING_CANDIDATE requiring KI promotion via explicit user disposition.

§6b KI Envelope Re-Verifications. 4-row table covering EXC-01..04:
- EXC-01: NEGATIVE-scope at v39 (re-verified).
- EXC-02: NEGATIVE-scope at v39 (re-verified).
- EXC-03: NEGATIVE-scope at v39 (re-verified).
- EXC-04: RE_VERIFIED NARROWS retained (BAF-jackpot-only scope; EntropyLib byte-identical at v39 HEAD via G24 cmp exit 0). Cross-cite to §5c REG-03 backward-trace.

§6c Verdict Summary. `0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_UNMODIFIED` (default per D-274-KI-01 + D-272-KI-01 carry). KNOWN-ISSUES.md UNMODIFIED at v39 phase-close HEAD.

Author §7 (Prior-Artifact Cross-Cites) inside `audit/FINDINGS-v39.0.md`:

§7.1 v39.0 Phase Artifacts. Table listing: 274-CONTEXT.md + 274-01-PLAN.md + 274-01-ADVERSARIAL-LOG.md (pending Task 3.7) + 274-01-SUMMARY.md (pending Task 3.9) + Phase 273 phase artifacts (273-CONTEXT.md + 273-01-PLAN.md + 273-01-SUMMARY.md).

§7.2 Prior Milestone FINDINGS Cross-Cites. Table listing audit/FINDINGS-v38.0.md (immediate baseline) + audit/FINDINGS-v37.0.md + audit/FINDINGS-v36.0.md (Phase 266 lootbox-path entropy refactor precedent — direct predecessor for v39 lootbox-path additions) + audit/FINDINGS-v34.0.md (REG-02 carry baseline) + audit/FINDINGS-v25.0..v33.0.md (REG-04 sweep set).

§7.3 Notes Cross-Cites. Any planning/notes/ entries referenced (none expected at v39; Phase 274 is mechanical).

§7.4 Project-State Cross-Cites. PROJECT.md + ROADMAP.md + STATE.md + MILESTONES.md + REQUIREMENTS.md cross-cite block (closure-flip targets at Task 3.10).

Commit subject: `audit(274): §6 KI gating walk + §7 prior-artifact cross-cites`. AGENT-COMMITTED atomic.
  </action>
  <verify>
    <automated>
grep -cE "^### 6a\.|^### 6b\.|^### 6c\." audit/FINDINGS-v39.0.md   # expect 3
grep -cE "EXC-0[1-4]|KNOWN_ISSUES_UNMODIFIED|0 of 0 KI_ELIGIBLE_PROMOTED" audit/FINDINGS-v39.0.md   # expect >=5
grep -cE "^## 7\.|^### 7\.1\.|^### 7\.2\." audit/FINDINGS-v39.0.md   # expect 3
git log -1 --format='%s' | grep -cE "^audit\(274\): §6 KI gating walk"   # expect 1
    </automated>
  </verify>
  <done>§6 KI gating walk + §7 prior-artifact cross-cites authored. EXC-01..03 NEGATIVE-scope at v39; EXC-04 RE_VERIFIED NARROWS retained. KNOWN-ISSUES.md UNMODIFIED. Atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 3.7: 3-skill PARALLEL adversarial pass on finished §4 draft + 274-01-ADVERSARIAL-LOG.md + §4.2 Verdict Roll-Up update</name>
  <files>.planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/274-01-ADVERSARIAL-LOG.md, audit/FINDINGS-v39.0.md</files>
  <read_first>
    - .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/ (v38 precedent location of 272-01-ADVERSARIAL-LOG.md if archived) OR `.planning/milestones/v38.0-phases/272-...` archived path
    - audit/FINDINGS-v39.0.md §4 draft (finished at Task 3.4)
    - .planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/274-CONTEXT.md (D-274-ADVERSARIAL-01 spawn composition)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_wait_for_approval.md (escalation procedure if FINDING_CANDIDATE surfaces)
  </read_first>
  <action>
Per D-274-ADVERSARIAL-01: spawn `/contract-auditor` + `/zero-day-hunter` + `/economic-analyst` PARALLEL via SINGLE message (one Bash/SlashCommand invocation containing all three spawn directives). NOT `/degen-skeptic` (OUT OF SCOPE per D-271-ADVERSARIAL-02 carry).

Each skill receives the finished §4 inline draft + Wave 1 + Wave 2 commit subjects/bodies + this plan's <threat_model> block as red-team input.

Author `.planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/274-01-ADVERSARIAL-LOG.md` with this structure:

```markdown
# 274-01 Adversarial Pass

**Phase:** 274 (v39.0 milestone-closure terminal)
**Pass type:** 3-skill PARALLEL spawn on finished §4 draft per D-274-ADVERSARIAL-01 carry
**Date:** <iso8601>
**§4 surfaces under red-team:** 8 (a..h)
**Composition:** /contract-auditor + /zero-day-hunter + /economic-analyst (per D-271-ADVERSARIAL-01 carry); /degen-skeptic OUT OF SCOPE per D-271-ADVERSARIAL-02 carry

## /contract-auditor

<verbatim output from /contract-auditor spawn>

## /zero-day-hunter

<verbatim output from /zero-day-hunter spawn>

## /economic-analyst

<verbatim output from /economic-analyst spawn>

## Disposition

<author after reviewing all 3 outputs>
- Disagreements vs §4 draft verdicts: <enumerate or "ZERO">
- FINDING_CANDIDATE surfaced: <enumerate or "ZERO">
- KI promotion candidates: <enumerate or "ZERO">
- 9th-surface NEW_VECTOR: <enumerate or "ZERO">
- Action: <ZERO disagreements → proceed to Task 3.8; otherwise escalate to user per feedback_wait_for_approval.md>
```

Update `audit/FINDINGS-v39.0.md` §4.2 Verdict Roll-Up:
- If zero disagreements + zero FINDING_CANDIDATE: `8 of 8 surfaces SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE / SAFE_BY_DESIGN_PHASE_273; zero F-39-NN finding blocks emitted; zero KI promotion candidates; 3-skill PARALLEL adversarial pass per D-274-ADVERSARIAL-01 disposition: ZERO DISAGREEMENTS.`
- If any disagreement OR FINDING_CANDIDATE: HALT and escalate to user per `feedback_wait_for_approval.md`. Do NOT auto-resolve. Document the candidate inline in §4 + §6a + offer user disposition options. After user disposition, update §4.2 + §6c accordingly.

Commit subject: `audit(274): 3-skill PARALLEL adversarial pass + §4.2 verdict roll-up + 274-01-ADVERSARIAL-LOG.md`. AGENT-COMMITTED atomic. If escalation required, the commit subject becomes `audit(274): adversarial pass surfaced FINDING_CANDIDATE — awaiting user disposition` and the agent STOPS until user resolves.
  </action>
  <verify>
    <automated>
test -f .planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/274-01-ADVERSARIAL-LOG.md; echo "advlog_file_exit=$?"   # expect 0
grep -cE "^## /contract-auditor|^## /zero-day-hunter|^## /economic-analyst|^## Disposition" .planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/274-01-ADVERSARIAL-LOG.md   # expect 4
grep -cE "8 of 8 surfaces|ZERO DISAGREEMENTS|FINDING_CANDIDATE" audit/FINDINGS-v39.0.md   # expect >=1
git log -1 --format='%s' | grep -cE "^audit\(274\): 3-skill PARALLEL adversarial pass"   # expect 1 (unless escalation)
    </automated>
  </verify>
  <done>3-skill PARALLEL adversarial pass complete; `274-01-ADVERSARIAL-LOG.md` populated with 3 H2 skill sections + Disposition note; §4.2 Verdict Roll-Up updated. If ZERO disagreements: 8 of 8 surfaces SAFE_*; zero F-39-NN; proceed to Task 3.8. If FINDING_CANDIDATE: halted for user disposition.</done>
</task>

<task type="auto">
  <name>Task 3.8: §8 Forward-Cite Closure (terminal-phase zero-emission verification per D-274-FCITE-01)</name>
  <files>audit/FINDINGS-v39.0.md</files>
  <read_first>
    - audit/FINDINGS-v38.0.md §8 (template shape — terminal-phase zero-emission verification)
    - .planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/274-CONTEXT.md (D-274-FCITE-01)
  </read_first>
  <action>
Author §8 (Forward-Cite Closure) inside `audit/FINDINGS-v39.0.md`:

§8a Phase 274 Intra-Milestone Forward-Cite Residual Verification. Grep recipe:
```
grep -rE "Phase (275|276|277|278)|v40\.0|v41\.0" audit/FINDINGS-v39.0.md .planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/
```
Expected: zero matches (terminal-phase zero-emission invariant).

NOTE: Deferred items (mint-boost retirement, auto-resolve retirement, jackpot ticket-award sites + BAF Bernoulli + v36.0 ENT-05 xorshift) MUST be cited as "deferred to a future milestone" without naming `v40.0` / `Phase 275` etc. Use generic phrasing like "deferred to a future milestone" + the locked-decision ID (D-274-MINTBOOST-OUT-01 etc.) to preserve forward-cite zero-emission.

§8b Phase 274 → Post-Milestone Forward-Cite Emission. Verify zero forward-cites in ROADMAP.md Phase 274 entry, STATE.md, MILESTONES.md v39.0 entry, REQUIREMENTS.md v39.0 section. Document any pre-existing forward-cites in carry-forward registers (e.g., v40.0+ bundle in PROJECT.md Deferred to Future Milestones block — these are EXPECTED forward-references, NOT forward-cites for D-274-FCITE-01 invariant which applies only to the FINDINGS deliverable and active phase artifacts).

§8c Combined §8 Verdict. `Terminal-phase zero-emission invariant honored at v39.0 phase close.` If any match in §8a grep: HALT and require rewrite of the offending citation.

Commit subject: `audit(274): §8 forward-cite closure (terminal-phase zero-emission verification)`. AGENT-COMMITTED atomic.
  </action>
  <verify>
    <automated>
grep -cE "^## 8\.|^### 8a\.|^### 8b\.|^### 8c\." audit/FINDINGS-v39.0.md   # expect 4
# G29 — D-274-FCITE-01 terminal-phase zero-emission verification
! grep -rE "Phase (275|276|277|278)|v40\.0|v41\.0" audit/FINDINGS-v39.0.md .planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/ 2>/dev/null; echo "fcite_zero_exit=$?"   # expect 0 (no matches)
git log -1 --format='%s' | grep -cE "^audit\(274\): §8 forward-cite closure"   # expect 1
    </automated>
  </verify>
  <done>§8 forward-cite closure authored; G29 grep recipe exits 0 (zero forward-cites to v40.0 / Phase 275+ in FINDINGS-v39.0.md or .planning/phases/274-*/). Deferred items cited via locked-decision IDs (D-274-*-OUT-01) without naming specific future milestones. Atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 3.9: §9 closure attestation block + §9.NN commit-readiness register + 274-01-SUMMARY.md</name>
  <files>audit/FINDINGS-v39.0.md, .planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/274-01-SUMMARY.md</files>
  <read_first>
    - audit/FINDINGS-v38.0.md §9 (template shape — §9a verdict distribution + §9b attestation block + §9c closure signal + §9.NN commit-readiness register)
    - .planning/phases/272-always-hero-simplification-maximal-dead-code-cleanup-terminal/272-01-SUMMARY.md (phase-summary precedent shape)
    - Wave 1 + Wave 2 commit SHAs + audit-commit SHAs from Tasks 3.1..3.8
    - .planning/REQUIREMENTS.md (39 v39.0 requirements final-state register)
  </read_first>
  <action>
Author §9 (Milestone Closure Attestation) inside `audit/FINDINGS-v39.0.md`:

§9a Verdict Distribution. Summary stats: 39 v39.0 requirements (LBX-WT-01..05 + LBX-WX-01..04 + LBX-EVT-01..06 + TST-WT-01..07 + TST-WX-01..03 + TST-REG-01..04 + AUDIT-01..06 + REG-01..04). Default state at v39 close: 39/39 Complete (or N/39 Complete + (39-N)/39 RE-DEFERRED-V40+ with explicit user disposition recorded).

§9b Attestation Block. Single-paragraph prose attesting: audit baseline `MILESTONE_V38_AT_HEAD_06623edb` → v39 audit-subject HEAD <sha>. 4 Phase 273 included-since-baseline commits folded in (ff929948 + e9807891 + e04d3333 + 1eb1ecb5). 1 Wave 1 USER-APPROVED contract commit (subject: feat(274): manual lootbox Bernoulli ...). 1 Wave 2 USER-APPROVED test commit (subject: test(274): manual lootbox whole-ticket ...). N AGENT-COMMITTED audit-deliverable commits at Tasks 3.1..3.9 + closure-flip commits at Task 3.10. 8 of 8 §4 surfaces SAFE_*; zero F-39-NN finding blocks; KNOWN-ISSUES.md UNMODIFIED; 4 KI envelope rows RE_VERIFIED (EXC-01..03 NEGATIVE + EXC-04 NARROWS retained); 4 REG sub-sections PASS; 3-skill PARALLEL adversarial pass: ZERO disagreements.

§9c Closure Signal. `MILESTONE_V39_AT_HEAD_<sha>` (placeholder; resolved at Task 3.10 atomic-update).

§9.NN Commit-Readiness Register:
- §9.NN.i USER-APPROVED contracts: 1 Wave 1 batched commit `<wave1-sha>` `feat(274): manual lootbox Bernoulli whole-ticket + WWXRP consolation + LootboxTicketRoll event [LBX-WT-01..05, LBX-WX-01..04, LBX-EVT-01..06]`.
- §9.NN.ii USER-APPROVED tests: 1 Wave 2 batched commit `<wave2-sha>` `test(274): manual lootbox whole-ticket + consolation + auto-resolve regression + LootboxTicketRoll [TST-WT-01..07, TST-WX-01..03, TST-REG-01..04]`.
- §9.NN.iii AGENT-COMMITTED audit artifacts (Wave 3+ atomic-commit chain). Sub-list each Task 3.1..3.10 commit SHA + subject.
- §9.NN.iv Phase 273 included-since-baseline commits (4 SHAs):
  - `ff929948` `fix(273): BAF credit routing — day-D orphan + RngLocked predicate + jackpot-phase override + tests`
  - `e9807891` `test(273): BAF-ROUTE-06/07/08 expansion — purchase-phase routing, mid-bracket override skip, markBafSkipped equivalence`
  - `e04d3333` `chore(273): phase SUMMARY — BAF credit routing complete (14/14 tests, security property attested)`
  - `1eb1ecb5` `docs: clarify _livenessTriggered VRF-grace branch as stalled-advance bailout`

Author NEW `.planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/274-01-SUMMARY.md` mirroring `272-01-SUMMARY.md` format:
- Per-task atomic-commit log (Wave 1 commit + Wave 2 commit + Wave 3+ atomic chain).
- Per-REQ tally for 39 v39.0 requirements (final state at v39 close).
- Cross-phase cross-cite density (which prior phases this milestone cites + which post-milestone phases will inherit).
- Project-feedback-rules-honored table (one row per `feedback_*.md` rule with "Honored" + cite).
- Closure signal `MILESTONE_V39_AT_HEAD_<sha>` (placeholder).
- Phase 273 included-since-baseline 4-commit log + surface-coverage attestation reference.

Commit subject: `audit(274): §9 closure attestation block + §9.NN commit-readiness register + 274-01-SUMMARY.md`. AGENT-COMMITTED atomic.
  </action>
  <verify>
    <automated>
grep -cE "^## 9\.|^### 9a\.|^### 9b\.|^### 9c\.|^### 9\.NN" audit/FINDINGS-v39.0.md   # expect >=4
grep -cE "MILESTONE_V39_AT_HEAD_<sha>|MILESTONE_V39_AT_HEAD_[0-9a-f]" audit/FINDINGS-v39.0.md   # expect >=4 (placeholder + several locations)
grep -cE "§9\.NN\.i|§9\.NN\.ii|§9\.NN\.iii|§9\.NN\.iv" audit/FINDINGS-v39.0.md   # expect 4
test -f .planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/274-01-SUMMARY.md; echo "summary_file_exit=$?"   # expect 0
git log -1 --format='%s' | grep -cE "^audit\(274\): §9 closure attestation"   # expect 1
    </automated>
  </verify>
  <done>§9 closure attestation + §9.NN commit-readiness register authored. 274-01-SUMMARY.md authored. Closure signal placeholder `MILESTONE_V39_AT_HEAD_<sha>` present in 4+ locations pending Task 3.10 SHA resolution. §9.NN.iv 4-commit Phase 273 included-since-baseline list present. Atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 3.10: Closure-signal SHA resolution + ROADMAP/STATE/MILESTONES/PROJECT/REQUIREMENTS atomic flips</name>
  <files>audit/FINDINGS-v39.0.md, .planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/274-01-SUMMARY.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/MILESTONES.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - audit/FINDINGS-v38.0.md §9c + cross-document closure-flip pattern (template for closure signal verbatim emission across 5 FINDINGS locations + 3 cross-document targets)
    - .planning/milestones/v38.0-phases/272-...-PLAN.md Task 20 (precedent for SHA-resolution atomic-update)
    - .planning/ROADMAP.md current v39.0 row (target for closure-flip)
    - .planning/STATE.md current state (target for closure-flip)
    - .planning/MILESTONES.md (target for v39.0 prepend)
    - .planning/PROJECT.md Current Milestone block (target for closure-flip)
    - .planning/REQUIREMENTS.md (target for 39/39 Complete tally)
  </read_first>
  <action>
This task RESOLVES the `<sha>` placeholder across all artifacts AND lands the atomic closure-flip commits.

Step 1 — capture current HEAD as the v39 closure SHA candidate:
```
V39_CLOSURE_SHA=$(git rev-parse HEAD)
echo "v39 closure-signal SHA: $V39_CLOSURE_SHA"
```

Step 2 — substitute `<sha>` → actual SHA across `audit/FINDINGS-v39.0.md` (>=5 verbatim locations per D-274-CLOSURE-01):
- §1 frontmatter `closure_signal: MILESTONE_V39_AT_HEAD_${V39_CLOSURE_SHA}`
- §1 frontmatter `audit_subject_head: ${V39_CLOSURE_SHA}`
- §2 Closure Verdict Summary trailing line
- §9c Closure Signal verbatim line
- §9.NN attestation block trailing line
- Executive Summary anchor sentence
(Confirm >=5 matches via G30 grep recipe.)

Step 3 — substitute in `.planning/phases/274-.../274-01-SUMMARY.md` closure-signal line.

Step 4 — flip `.planning/ROADMAP.md`:
- v39.0 milestone row → `✅ v39.0 Lootbox Whole-Ticket Rounding + WWXRP Consolation — Phase 274 (shipped <iso-date>; closure signal MILESTONE_V39_AT_HEAD_${V39_CLOSURE_SHA})`
- Phase 274 entry checkbox ticked
- v38.0 collapsed-summary block carried forward

Step 5 — flip `.planning/STATE.md`:
- Last Shipped Milestone → v39.0 with full closure-signal block
- v38.0 demoted to Prior Shipped Milestone
- Active Milestone reverted to between-milestones state
- Roadmap Overview table updated
- Operator Next Steps updated to reflect v39.0 close

Step 6 — flip `.planning/MILESTONES.md`:
- v39.0 entry prepended to top with closure-signal recorded
- v38.0 demoted one position
- Entry includes: phase shape (single-phase), commit shape (1 USER-APPROVED contract + 1 USER-APPROVED test + N AGENT-COMMITTED audit), §4 surface count (8), REG row count (4), KI verdict (KNOWN_ISSUES_UNMODIFIED), closure signal, Phase 273 included-since-baseline 4-commit log

Step 7 — flip `.planning/PROJECT.md`:
- Current Milestone block: shift v39.0 from Active → Completed Milestone (v39.0)
- Active Milestone → between-milestones state
- Last Shipped → v39.0 closure signal
- Closure summary paragraph mirroring v38.0 close shape
- v39.0 deferred items (mint-boost retirement + auto-resolve retirement + jackpot ticket-award + v36.0 ENT-05 carry) carried into "Deferred to Future Milestones" block

Step 8 — flip `.planning/REQUIREMENTS.md`:
- 39 v39.0 requirements tally flipped to Complete (or N Complete + (39-N) RE-DEFERRED with explicit user disposition if adversarial pass surfaced any)
- Carry-forward register opens for v40.0+ deferred items

Step 9 — verify closure signal verbatim presence in 5 FINDINGS locations (G30) and 3 cross-document targets (G31).

Step 10 — single atomic commit landing all 7 file modifications:

Commit subject: `audit(274): closure-signal SHA resolution + cross-document flips [MILESTONE_V39_AT_HEAD_${V39_CLOSURE_SHA::8}]`.
Stage files explicitly: `git add audit/FINDINGS-v39.0.md .planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/274-01-SUMMARY.md .planning/ROADMAP.md .planning/STATE.md .planning/MILESTONES.md .planning/PROJECT.md .planning/REQUIREMENTS.md`. AGENT-COMMITTED atomic.

NOTE: This is an AGENT-COMMITTED audit-artifact commit (not USER-APPROVED). Wave 1 + Wave 2 were the USER-APPROVED gates. Wave 3+ audit-deliverable commits including this closure-flip commit are AGENT-COMMITTED atomic-per-task per write_policy.
  </action>
  <verify>
    <automated>
# G30 — closure signal in >=5 FINDINGS locations
grep -cE "MILESTONE_V39_AT_HEAD_[0-9a-f]{8}" audit/FINDINGS-v39.0.md   # expect >=5
# G31 — cross-document propagation in 3 targets
grep -lE "MILESTONE_V39_AT_HEAD_[0-9a-f]{8}" .planning/ROADMAP.md .planning/STATE.md .planning/MILESTONES.md | wc -l   # expect 3
# PROJECT.md updated
grep -cE "MILESTONE_V39_AT_HEAD_[0-9a-f]{8}" .planning/PROJECT.md   # expect >=1
# REQUIREMENTS.md updated (39/39 Complete tally)
grep -cE "Complete" .planning/REQUIREMENTS.md   # expect >=39 (one per requirement)
# Atomic commit landed
git log -1 --format='%s' | grep -cE "^audit\(274\): closure-signal SHA resolution"   # expect 1
    </automated>
  </verify>
  <done>Closure signal `MILESTONE_V39_AT_HEAD_<sha>` resolved across 5 FINDINGS locations + 3 cross-document targets (ROADMAP / STATE / MILESTONES) + PROJECT.md + REQUIREMENTS.md. Atomic closure-flip commit landed with subject `audit(274): closure-signal SHA resolution + cross-document flips [MILESTONE_V39_AT_HEAD_<sha-8>]`. v39.0 milestone state = closed in all planning artifacts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3.11: Final user review + READ-only flip on audit/FINDINGS-v39.0.md</name>
  <files>audit/FINDINGS-v39.0.md</files>
  <read_first>
    - audit/FINDINGS-v39.0.md (final state post-Task-3.10)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_manual_review_before_push.md (final user-review gate before READ-only flip + push)
  </read_first>
  <action>See <what-built> + <how-to-verify>.</action>
  <verify>
    <automated>Manual verification only — checkpoint:human-verify gate. Resume-signal IS the verify.</automated>
  </verify>
  <what-built>
v39.0 milestone-closure deliverable `audit/FINDINGS-v39.0.md` is complete: §1-§9 populated, closure signal `MILESTONE_V39_AT_HEAD_<sha>` emitted in 5+ FINDINGS locations + 3 cross-document targets, 3-skill PARALLEL adversarial pass complete with disposition, 8 of 8 §4 surfaces SAFE_*, zero F-39-NN finding blocks, KNOWN-ISSUES.md UNMODIFIED, 39/39 v39.0 requirements Complete (or explicit RE-DEFERRED with user disposition), Phase 273 included-since-baseline 4-commit coverage attested.
  </what-built>
  <how-to-verify>
1. Present a final summary to the user: §-by-§ contents of `audit/FINDINGS-v39.0.md`; commit chain (Wave 1 + Wave 2 + Wave 3+ atomic) with subjects + SHAs; per-REQ tally; closure-signal SHA; 4-commit Phase 273 included-since-baseline log.
2. Present the full text of `audit/FINDINGS-v39.0.md` for one final read.
3. **WAIT** for the user to type the literal string `approved` (case-sensitive).
4. Upon `approved`:
   - Update `audit/FINDINGS-v39.0.md` YAML frontmatter: `status: "FINAL — READ-ONLY"`, `read_only: true`.
   - Run `chmod 444 audit/FINDINGS-v39.0.md` to flip filesystem permissions to read-only.
   - Single atomic commit: `git add audit/FINDINGS-v39.0.md && git commit -m "audit(274): finalize audit/FINDINGS-v39.0.md as READ-only at v39 closure HEAD"`.
5. Per `feedback_manual_review_before_push.md`: do NOT `git push`. The user runs the push manually after final review of the entire v39.0 commit chain.
6. Phase 274 SHIPPED. v39.0 milestone CLOSED.
  </how-to-verify>
  <resume-signal>User types `approved` → agent runs the READ-only flip + chmod + commit. User types anything else → agent does NOT flip; presents follow-up question or revision plan.</resume-signal>
  <done>User approved final review; READ-only flip applied (`chmod 444` + frontmatter `read_only: true`); closure-attestation final commit landed with `MILESTONE_V39_AT_HEAD_<sha>` in subject; v39.0 milestone CLOSED; Phase 274 SHIPPED. NO push performed (manual review gate).</done>
</task>

</tasks>

<verification>
## Phase 274 Wave-Gate Verification (executor runs after each wave)

### Wave 1 close (post-Task-1.5 batched contract commit)
- [ ] G1..G7 grep gates pass (constant + event + signature + 4 callers updated)
- [ ] G8..G14 grep gates pass (Bernoulli math + scaledPre + queue/consolation switch + LootboxTicketRoll emit)
- [ ] G15 + G16 grep gates pass (bit-allocation NatSpec updated 152 → 160)
- [ ] G17 grep gate: function-scope `futureTickets` NEVER reassigned to `whole` (D-274-NO-EVT-BREAK-01 critical)
- [ ] G18 + G19 grep gates: LootBoxOpened.futureTickets + BurnieLootOpen.tickets continue carrying scaled value
- [ ] G20 + G21 + G22 parity exits = 0 (storage + external + modifier layout byte-identical vs `06623edb`)
- [ ] G23 + G24 cmp exits = 0 (MintModule + EntropyLib byte-identical vs baseline)
- [ ] G26 hardhat compile green
- [ ] Wave 1 USER-APPROVED batched commit landed with full body (per-REQ attribution + bytecode delta + parity proofs + 5-item RNG backward-trace attestation)
- [ ] `.task-1.4-rng-trace.txt` scratch file removed post-commit

### Wave 2 close (post-Task-2.5 batched test commit)
- [ ] 3-4 new test files exist (LootboxWholeTicket + LootboxConsolation + LootboxAutoResolveRegression + optional LootboxBernoulliEv)
- [ ] `npx hardhat test test/unit/LootboxWholeTicket.test.js` exits 0
- [ ] `npx hardhat test test/unit/LootboxConsolation.test.js` exits 0
- [ ] `npx hardhat test test/edge/LootboxAutoResolveRegression.test.js` exits 0
- [ ] Full `npx hardhat test` suite green (no new failures attributable to Wave 1)
- [ ] Wave 2 USER-APPROVED batched commit landed with full body (per-REQ attribution + suite baseline + any test-only shim disclosure)
- [ ] `.task-2.4-suite-baseline.txt` scratch file removed post-commit

### Wave 3+ close (post-Task-3.11 final READ-only flip)
- [ ] `audit/FINDINGS-v39.0.md` populated §1-§9 (all section headers present)
- [ ] G30 grep: closure signal `MILESTONE_V39_AT_HEAD_<sha>` verbatim in >=5 FINDINGS locations
- [ ] G31 grep: closure signal in 3 cross-document targets (ROADMAP + STATE + MILESTONES)
- [ ] G32 grep: 4 Phase 273 commit SHAs cited in §3.A + §9.NN.iv
- [ ] G29 forward-cite check: zero matches in FINDINGS + .planning/phases/274-*/
- [ ] `274-01-ADVERSARIAL-LOG.md` exists with 3 H2 skill sections + Disposition note (3-skill PARALLEL spawn complete)
- [ ] `274-01-SUMMARY.md` exists with per-task commit log + per-REQ tally + closure signal
- [ ] 39/39 v39.0 requirements flipped to Complete (or RE-DEFERRED-V40+ with explicit user disposition)
- [ ] ROADMAP.md v39.0 row flipped to ✅ shipped
- [ ] STATE.md Last Shipped Milestone = v39.0
- [ ] MILESTONES.md v39.0 entry prepended at top
- [ ] PROJECT.md Active Milestone reverted to between-milestones state; v39.0 added as Completed Milestone
- [ ] `audit/FINDINGS-v39.0.md` chmod 444 + frontmatter `read_only: true` + final commit landed
- [ ] NO `git push` performed (manual user action per `feedback_manual_review_before_push.md`)
</verification>

<success_criteria>
Phase 274 SHIPPED when all 18 Success Criteria from ROADMAP.md detail section are TRUE at v39.0 closure HEAD:

1. `_resolveLootboxCommon` signature gains `uint48 index` parameter; 4 callers updated correctly (G4 + G6 + G7).
2. Manual branch Bernoulli-collapses scaled `futureTickets` to whole via bits[152..167] (G8 + G9); EV-neutrality empirically verified at TST-WT-01.
3. Manual branch routes whole via `_queueTickets`; auto-resolve routes scaled via `_queueTicketsScaled` (G10 + G11).
4. `LOOTBOX_WWXRP_CONSOLATION = 1 ether` declared; consolation predicate verified (G1 + G12 + G13 + TST-WX-01..03).
5. `LootboxTicketRoll` event declared on module + interface (G2 + G3); emitted exactly once per manual ticket-path open (TST-WT-06).
6. NO breaking event changes: LootBoxOpened.futureTickets + BurnieLootOpen.tickets + TicketsQueuedScaled semantics UNCHANGED (G17 + G18 + G19 + TST-WT-05).
7. Bit-allocation NatSpec updated 152 → 160 (G15 + G16).
8. Storage layout byte-identical (G20 + G21 + G22 parity exits 0); MintModule + EntropyLib byte-identical (G23 + G24).
9. Manual-path test coverage TST-WT-01..07 present (test/unit/LootboxWholeTicket.test.js + optional test/stat/LootboxBernoulliEv.test.js).
10. Consolation test coverage TST-WX-01..03 present (test/unit/LootboxConsolation.test.js).
11. Regression test coverage TST-REG-01..04 present (test/edge/LootboxAutoResolveRegression.test.js).
12. Wave 1 + Wave 2 batched USER-APPROVED commits land; bytecode delta + suite baseline reported in commit bodies.
13. `audit/FINDINGS-v39.0.md` 9-section deliverable FINAL READ-only at v39 closure HEAD; closure signal in 5 FINDINGS locations + 3 cross-document targets (G30 + G31).
14. §3.A row coverage for 4 Phase 273 commits + Phase 274 Wave 1 + Wave 2 commits (G32 + AUDIT-02).
15. §4 8-surface adversarial sweep SAFE_*; default zero F-39-NN.
16. 3-skill PARALLEL adversarial pass logged in `274-01-ADVERSARIAL-LOG.md`; zero residual FINDING_CANDIDATE (AUDIT-04).
17. KI walkthrough EXC-01..04 RE_VERIFIED; KNOWN_ISSUES_UNMODIFIED (AUDIT-05).
18. LEAN regression REG-01..04 NON-WIDENING / RE_VERIFIED / PASS (REG-01..04 + G27 + G28).

Closure signal: `MILESTONE_V39_AT_HEAD_<sha>` emitted in §9c of `audit/FINDINGS-v39.0.md`.
</success_criteria>

<output>
After completion:
- `audit/FINDINGS-v39.0.md` exists, FINAL READ-only (chmod 444 + `read_only: true`), 9 sections, ~700-900 lines.
- `.planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/274-01-ADVERSARIAL-LOG.md` exists with 3 H2 skill sections + Disposition.
- `.planning/phases/274-lootbox-whole-ticket-rounding-wwxrp-consolation-terminal/274-01-SUMMARY.md` exists with per-task commit log + per-REQ tally.
- ROADMAP.md / STATE.md / MILESTONES.md / PROJECT.md / REQUIREMENTS.md all flipped to v39.0-closed state with closure signal `MILESTONE_V39_AT_HEAD_<sha>` verbatim.
- 2 USER-APPROVED batched commits (Wave 1 contracts + Wave 2 tests) + N AGENT-COMMITTED audit-artifact commits.
- NO push performed (manual user action per `feedback_manual_review_before_push.md`).
</output>

</output>
