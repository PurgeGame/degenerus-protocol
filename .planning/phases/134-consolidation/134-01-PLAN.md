---
phase: 134-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - KNOWN-ISSUES.md
  - contracts/storage/DegenerusGameStorage.sol
  - audit/gas10-immutable-candidates.md
autonomous: false
requirements: [BOT-03]
must_haves:
  truths:
    - "Every DOCUMENT finding from Phases 130-133 has a corresponding entry in KNOWN-ISSUES.md"
    - "KNOWN-ISSUES.md is organized by severity/category with detector IDs per D-01/D-03"
    - "Dead code _lootboxBpsToTier is removed from DegenerusGameStorage.sol per D-04"
    - "GAS-10 immutable candidates are listed with locations for user approval per D-05"
  artifacts:
    - path: "KNOWN-ISSUES.md"
      provides: "Comprehensive pre-disclosure for C4A wardens"
      contains: "Automated Tool Findings"
    - path: "audit/gas10-immutable-candidates.md"
      provides: "GAS-10 immutable candidate review table"
      contains: "immutable"
    - path: "contracts/storage/DegenerusGameStorage.sol"
      provides: "Dead code removed"
  key_links:
    - from: "KNOWN-ISSUES.md"
      to: "audit/bot-race/slither-triage.md"
      via: "DOC-01 through DOC-05 entries with detector IDs"
      pattern: "arbitrary-send-eth|events-maths|dead-code|shadowing-local|redundant-statements"
    - from: "KNOWN-ISSUES.md"
      to: "audit/bot-race/4naly3er-triage.md"
      via: "22 DOCUMENT categories with detector IDs"
      pattern: "\\[M-|\\[L-|\\[NC-|\\[GAS-"
    - from: "KNOWN-ISSUES.md"
      to: "audit/erc-20-compliance.md"
      via: "5 ERC-20 deviation entries"
      pattern: "DGNRS|BURNIE"
---

<objective>
Consolidate all DOCUMENT findings from Phases 130-133 into KNOWN-ISSUES.md, remove the one dead code function (D-04), and present GAS-10 immutable candidates for user review (D-05).

Purpose: BOT-03 requires every bot-detectable finding to be either fixed in code or documented in KNOWN-ISSUES.md. This plan harvests all findings and produces the warden-facing deliverable.

Output: Expanded KNOWN-ISSUES.md with all pre-disclosed findings, cleaned DegenerusGameStorage.sol, and GAS-10 review table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@KNOWN-ISSUES.md
@audit/bot-race/slither-triage.md
@audit/bot-race/4naly3er-triage.md
@audit/erc-20-compliance.md
@audit/event-correctness.md
@audit/comment-rescan-summary.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand KNOWN-ISSUES.md with all DOCUMENT findings + remove dead code</name>
  <files>KNOWN-ISSUES.md, contracts/storage/DegenerusGameStorage.sol</files>
  <read_first>
    - KNOWN-ISSUES.md (current state — 5 entries in 2 sections)
    - audit/bot-race/slither-triage.md (DOC-01 through DOC-05 with detector IDs and reasoning)
    - audit/bot-race/4naly3er-triage.md (22 DOCUMENT categories with detector IDs, instance counts, reasoning)
    - audit/erc-20-compliance.md (5 ready-to-paste entries at line 265+)
    - audit/event-correctness.md (30 INFO findings summary)
    - audit/comment-rescan-summary.md (116 NC instance dispositions)
    - contracts/storage/DegenerusGameStorage.sol lines 1569-1576 (dead code to remove)
  </read_first>
  <action>
**KNOWN-ISSUES.md restructure per D-01:**

Keep the existing 2 sections (Intentional Design, Design Mechanics) with their current 5 entries unchanged. Add three new sections after them:

**Section 3: "Automated Tool Findings (Pre-disclosed)"** — per D-01
Add a stats line at the very top of the file per D-08: "Pre-audited with Slither v0.11.5 + 4naly3er. 113 detector categories triaged (5 Slither DOCUMENT, 22 4naly3er DOCUMENT, 84 FP). All DOCUMENT findings below."

Subsections within Automated Tool Findings, grouped by concern:

1. **ETH Transfer Safety** — DOC-01 (`arbitrary-send-eth`, 4 instances). 2-3 sentences: payout functions send ETH to user addresses via `.call{value:}`. Destinations are msg.sender or player addresses from game state. All paths have access control. Per D-03, include detector ID `arbitrary-send-eth`.

2. **Missing Event for claimablePool Decrement** — DOC-02 (`events-maths`, 1 instance). resolveRedemptionLootbox decrements claimablePool without dedicated event; higher-level redemption events capture context. Detector: `events-maths`.

3. **Local Variable Shadowing** — DOC-04 (`shadowing-local`, 1 instance). MintModule._callTicketPurchase local `ticketLevel` shadows DegenerusGameStorage.ticketLevel. Intentional local copy after state read. Detector: `shadowing-local`.

4. **Unused Parameter Silencer** — DOC-05 (`redundant-statements`, 1 instance). JackpotModule._computeBucketCounts `lvl;` no-op silences unused param warning. Required by function signature. Detector: `redundant-statements`.

5. **Centralization Risk** — 4naly3er `[M-2]` (7 instances). Admin functions gated by onlyOwner. DegenerusAdmin critical functions require sDGNRS governance vote. Remaining onlyOwner functions are operational (price feed, staking) and deity pass metadata. Admin cannot drain game funds. Detector: `[M-2]`.

6. **Chainlink Price Feed Staleness** — `[M-3]` (1 instance). LINK/ETH feed has updatedAt freshness check. Used for VRF cost estimation only, not user-facing pricing. Detector: `[M-3]`.

7. **No SafeERC20 Wrappers** — Merge `[M-5]`, `[M-6]`, `[L-19]` per discretion (all same concern). Protocol uses `.transfer()/.transferFrom()` with return value checks instead of SafeERC20. Only interacts with known tokens (stETH, BURNIE, LINK, wXRP) that return bool. SafeERC20 would add ~2,600 gas/call for no benefit. Detectors: `[M-5]`, `[M-6]`, `[L-19]`.

8. **abi.encodePacked Hash Collision** — `[L-4]` (35 instances). Used for entropy derivation (fixed-width inputs, no collision) and SVG strings (not keyed). No exploitable collision path. Detector: `[L-4]`.

9. **Division by Zero** — `[L-7]` (27 instances). All divisors have implicit guards: BPS constants, non-zero supply checks, level-derived values. Exhaustively audited in v3.3 + v5.0. Detector: `[L-7]`.

10. **External Call Gas Consumption** — `[L-9]` (11 instances). `.call{value:}("")` forwards all gas. Recipients are player addresses (self-grief only) or known protocol contracts. CEI pattern followed. Detector: `[L-9]`.

11. **Burn/Zero-Address Handling** — `[L-12]` (67 instances). Protocol burn functions are intentional (BURNIE, sDGNRS gambling burn, GNRUS redemption). Internal functions use msg.sender/contract-to-contract paths ensuring valid addresses. Detector: `[L-12]`.

12. **Rounding/Precision** — Merge `[L-13]`, `[L-14]` per discretion (same concern). All rounding favors protocol (down on payouts, up on burns). Solvency invariant strengthened. Already in "stETH rounding" entry above — add cross-reference. Detectors: `[L-13]`, `[L-14]`.

13. **Unchecked Downcasting** — `[L-18]` (50 instances). Intentional for storage packing. All downcasts preceded by range validation or mathematically guaranteed to fit. SafeCast would add redundant gas overhead. Detector: `[L-18]`.

14. **Missing address(0) Checks** — `[NC-2]` (2 instances). BurnieCoinflip bountyOwedTo from game logic (always valid), DeityPass renderer setter is admin-only. Detector: `[NC-2]`.

15. **Magic Numbers** — Merge `[NC-6]`, `[NC-34]`. Bit positions/masks in assembly, small obvious arithmetic values, BPS values documented in NatSpec. Detectors: `[NC-6]`, `[NC-34]`.

16. **Unused Event Declarations** — `[NC-9]` (2 instances). Cosmetic. Detectors: `[NC-9]`.

17. **Event Indexed Fields** — Merge `[NC-10]`, `[NC-33]`. Some events omit indexed on fields not useful as filter keys. Key indexer-critical events are properly indexed. Detectors: `[NC-10]`, `[NC-33]`.

18. **Event Missing Old+New Values** — `[NC-11]` (7 instances). Parameter-change events emit new value only. Admin operations are infrequent. Detector: `[NC-11]`.

19. **Long Functions** — `[NC-13]` (377 instances). Complex game logic necessarily exceeds 50 lines. Splitting increases gas via call overhead. Organized with NatSpec section banners. Detector: `[NC-13]`.

20. **Setter Validation** — `[NC-16]` (23 instances). Admin-only setters trust admin. Critical setters (VRF swap) have governance checks. Detector: `[NC-16]`.

21. **Missing Parameter Change Events** — `[NC-17]` (27 instances). Covered by Phase 132 event audit — 30 INFO findings all DOCUMENT. Cross-references event-correctness.md. Detector: `[NC-17]`.

22. **Unchecked Arithmetic** — `[GAS-7]` (1,054 instances). Protocol uses unchecked blocks strategically. Remaining checked arithmetic is intentional safety margin. Gas ceiling analysis (v3.5 Phase 57) confirmed all critical paths within block gas limit. Single grouped entry per discretion. Detector: `[GAS-7]`.

**Section 4: "ERC-20 Deviations"** — per D-01
Paste the 5 ready-to-paste entries from audit/erc-20-compliance.md lines 269-297:
- DGNRS blocks transfer to own address (Deviation #1)
- DGNRS transferFrom missing Approval event (Deviation #2)
- BURNIE game contract allowance bypass (Deviation #5)
- BURNIE auto-claim on transfer (Deviation #6)
- BURNIE transfer to VAULT burns (Deviation #7)

Add brief intro: "DGNRS and BURNIE are ERC-20 tokens with intentional deviations. sDGNRS and GNRUS are soulbound (not ERC-20) -- filing ERC-20 compliance issues against them is invalid."

**Section 5: "Event Design Decisions"** — per D-01
Brief section: "Phase 132 systematic event audit found 30 INFO-level findings across all 26 production contracts. All are DOCUMENT disposition. Key categories: 25 missing events for non-critical state changes (admin setters, internal bookkeeping), 2 stale parameter values (cosmetic), 2 missing indexed fields, 1 unused event declaration. Full details: audit/event-correctness.md."

**Dead code removal per D-04:**
Delete lines 1569-1575 in DegenerusGameStorage.sol (the `/// @dev Encode lootbox BPS...` NatSpec comment on line 1569 and the `_lootboxBpsToTier` function body lines 1570-1575). This removes the entire function including its NatSpec.

**Per D-02:** Each entry is 2-3 sentences: title + what the tool flags + why it's intentional. Not a novel.
**Per D-03:** Every entry includes the Slither detector name or 4naly3er category ID in parentheses.
  </action>
  <verify>
    <automated>grep -c "arbitrary-send-eth\|events-maths\|shadowing-local\|redundant-statements\|dead-code" KNOWN-ISSUES.md | head -1 && grep -c "\[M-\|L-\|NC-\|GAS-" KNOWN-ISSUES.md | head -1 && grep -c "DGNRS\|BURNIE" KNOWN-ISSUES.md | head -1 && grep "_lootboxBpsToTier" contracts/storage/DegenerusGameStorage.sol; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - KNOWN-ISSUES.md contains string "Automated Tool Findings (Pre-disclosed)"
    - KNOWN-ISSUES.md contains string "ERC-20 Deviations"
    - KNOWN-ISSUES.md contains string "Event Design Decisions"
    - KNOWN-ISSUES.md contains string "Pre-audited with Slither"
    - KNOWN-ISSUES.md contains "arbitrary-send-eth" (DOC-01 detector ID)
    - KNOWN-ISSUES.md contains "events-maths" (DOC-02 detector ID)
    - KNOWN-ISSUES.md contains "shadowing-local" (DOC-04 detector ID)
    - KNOWN-ISSUES.md contains "[M-2]" (centralization)
    - KNOWN-ISSUES.md contains "[M-3]" (price feed)
    - KNOWN-ISSUES.md contains "[M-5]" or "[M-6]" or "[L-19]" (SafeERC20)
    - KNOWN-ISSUES.md contains "[GAS-7]" (unchecked arithmetic)
    - KNOWN-ISSUES.md contains "DGNRS blocks transfer to its own contract address" or similar
    - KNOWN-ISSUES.md contains "BURNIE game contract bypasses" or similar
    - contracts/storage/DegenerusGameStorage.sol does NOT contain "_lootboxBpsToTier"
    - KNOWN-ISSUES.md section count >= 5 (Intentional Design, Design Mechanics, Automated Tool Findings, ERC-20 Deviations, Event Design Decisions)
  </acceptance_criteria>
  <done>KNOWN-ISSUES.md expanded from 5 entries to ~30+ entries covering all DOCUMENT findings from Phases 130-133, organized by category with detector IDs. Dead code _lootboxBpsToTier removed from DegenerusGameStorage.sol.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Review GAS-10 immutable candidates + verify KNOWN-ISSUES.md</name>
  <files>audit/gas10-immutable-candidates.md</files>
  <read_first>
    - audit/bot-race/4naly3er-triage.md (GAS-10 section at line 374)
    - KNOWN-ISSUES.md (just expanded)
  </read_first>
  <action>
**GAS-10 immutable review per D-05:**
Create `audit/gas10-immutable-candidates.md` with a table listing all 10 constructor-only variables that 4naly3er flagged as candidates for `immutable`. For each candidate, include:
- Contract name
- Variable name
- Line number
- Current declaration (e.g., `uint256 public varName`)
- Constructor assignment location
- Assessment: whether making it immutable is safe (no post-constructor writes, no assembly reads from storage slot)

To find the 10 candidates, search all production contracts for state variables that are only written in the constructor. The 4naly3er raw output should identify specific locations. If the raw output is not available, scan constructors in all production contracts and cross-reference with storage writes outside constructors.

Present the table for user to approve/reject each candidate individually. Include a note: "Per D-05, no code changes will be made until you approve/reject each candidate."
  </action>
  <what-built>
    1. KNOWN-ISSUES.md expanded with all DOCUMENT findings from Phases 130-133 (Slither DOC-01 through DOC-05, 22 4naly3er DOCUMENT categories, 5 ERC-20 deviations, event audit summary)
    2. Dead code _lootboxBpsToTier removed from DegenerusGameStorage.sol
    3. GAS-10 immutable candidate review table created
  </what-built>
  <how-to-verify>
    1. Read KNOWN-ISSUES.md — verify it has 5 sections (Intentional Design, Design Mechanics, Automated Tool Findings, ERC-20 Deviations, Event Design Decisions)
    2. Verify detector IDs are present (search for "arbitrary-send-eth", "[M-2]", "[GAS-7]")
    3. Verify entries are 2-3 sentences per D-02 (not paragraphs)
    4. Read audit/gas10-immutable-candidates.md — review each candidate, approve or reject
    5. Verify _lootboxBpsToTier is gone from DegenerusGameStorage.sol
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or list specific changes needed (e.g., "reject candidate 3, reword entry X")</resume-signal>
  <acceptance_criteria>
    - audit/gas10-immutable-candidates.md exists and contains a markdown table with columns: Contract, Variable, Line, Declaration, Assessment
    - audit/gas10-immutable-candidates.md contains at least 8 rows (may be fewer than 10 if some are FP)
    - User has reviewed and provided approve/reject decisions
  </acceptance_criteria>
  <done>User has reviewed KNOWN-ISSUES.md content and approved/rejected GAS-10 immutable candidates</done>
</task>

</tasks>

<verification>
- KNOWN-ISSUES.md contains entries for all 5 Slither DOCUMENT findings (DOC-01 through DOC-05, minus DOC-03 which was FIXED)
- KNOWN-ISSUES.md contains entries for all 22 4naly3er DOCUMENT categories (grouped where overlapping)
- KNOWN-ISSUES.md contains all 5 ERC-20 deviation entries
- KNOWN-ISSUES.md contains event audit summary
- Every entry includes detector ID per D-03
- Dead code removed per D-04
- GAS-10 review table exists per D-05
- No deferred ideas implemented
</verification>

<success_criteria>
BOT-03 partially satisfied: all bot-detectable findings are documented in KNOWN-ISSUES.md or fixed in code. GAS-10 candidates awaiting user decision.
</success_criteria>

<output>
After completion, create `.planning/phases/134-consolidation/134-01-SUMMARY.md`
</output>
