---
phase: 192-delta-extraction-behavioral-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/192-delta-extraction-behavioral-verification/192-01-AUDIT.md
autonomous: true
requirements:
  - DELTA-01
  - DELTA-02

must_haves:
  truths:
    - "Every changed, added, and deleted function/event/constant across JackpotModule, AdvanceModule, BurnieCoinflip, IDegenerusGameModules, and IBurnieCoinflip is listed in the changelog with a classification"
    - "Every deleted function (awardFinalDayDgnrsReward external+private+interface, _randTraitTicket address-only, _creditJackpot, _hasTraitTickets, _validateTicketBudget) has a grep proof showing zero remaining callers in contracts/"
    - "Every deleted event (JackpotTicketWinner, AutoRebuyProcessed from JackpotModule) and deleted constants (AWARD_ETH etc) have grep proof of zero remaining references"
    - "Every refactored function (_addClaimableEth, _processAutoRebuy, _processSoloBucketWinner, creditFlipBatch, _randTraitTicket rename, _awardDailyCoinToTraitWinners, distributeYieldSurplus, runBafJackpot) has an old-vs-new comparison proving identical core behavior"
    - "Every old JackpotTicketWinner emission site is mapped to its replacement specialized event with field-by-field comparison"
  artifacts:
    - path: ".planning/phases/192-delta-extraction-behavioral-verification/192-01-AUDIT.md"
      provides: "Function-level changelog, deleted-item unreachability proofs, refactor equivalence proofs, and event migration mapping"
      contains: "## Function-Level Changelog"
  key_links:
    - from: "_addClaimableEth"
      to: "JackpotEthWin event"
      via: "return signature change surfaces rebuy fields for event emission"
      pattern: "JackpotEthWin"
    - from: "_randTraitTicketWithIndices"
      to: "_randTraitTicket"
      via: "rename with identical body"
      pattern: "function _randTraitTicket"
    - from: "creditFlipBatch"
      to: "_awardDailyCoinToTraitWinners"
      via: "single call replaces batched-by-3 loop"
      pattern: "creditFlipBatch"
---

<objective>
Produce a complete function-level changelog for commits 93c05869 and 520249a2, then prove all REFACTOR-classified changes are behaviorally equivalent and all deleted items are unreachable.

Purpose: Satisfy DELTA-01 (complete extraction) and the refactor portion of DELTA-02. The intentional-change correctness proofs are handled in Plan 02.
Output: 192-01-AUDIT.md covering changelog, deleted-item proofs, refactor equivalence proofs, and event migration mapping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/192-delta-extraction-behavioral-verification/192-CONTEXT.md
@.planning/phases/192-delta-extraction-behavioral-verification/192-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Function-level changelog and deleted-item unreachability proofs</name>
  <files>.planning/phases/192-delta-extraction-behavioral-verification/192-01-AUDIT.md</files>
  <read_first>
    - contracts/modules/DegenerusGameJackpotModule.sol (full file -- 2556 lines, primary audit target)
    - contracts/modules/DegenerusGameAdvanceModule.sol (grep for awardFinalDayDgnrsReward, _awardFinalDayDgnrsReward, runBafJackpot, payDailyJackpot references)
    - contracts/BurnieCoinflip.sol (line 906+ for creditFlipBatch)
    - contracts/interfaces/IDegenerusGameModules.sol (grep for awardFinalDayDgnrsReward)
    - contracts/interfaces/IBurnieCoinflip.sol (line 122+ for creditFlipBatch)
    - Run: git diff 93c05869^..93c05869 (full diff for DGNRS solo reward fold commit)
    - Run: git diff 520249a2^..520249a2 (full diff for specialized events commit)
    - Run: git show 93c05869^:contracts/modules/DegenerusGameJackpotModule.sol (pre-93c05869 baseline for deleted functions)
    - Run: git show 93c05869^:contracts/modules/DegenerusGameAdvanceModule.sol (pre-93c05869 baseline for _awardFinalDayDgnrsReward)
  </read_first>
  <action>
Create 192-01-AUDIT.md. Start with the following sections:

**Section 1: Function-Level Changelog**

Build a comprehensive table covering ALL 38 items from the research change inventory. For each item include:
- Item number (1-38)
- Item name (function/event/constant)
- File
- Change type: ADDED / DELETED / SIGNATURE CHANGE / BEHAVIORAL CHANGE / COSMETIC
- Classification: REFACTOR / INTENTIONAL / DELETED / COSMETIC
- Proof location: which section in this audit document contains the proof

Use the complete inventory from 192-RESEARCH.md items 1-38. Verify completeness by running:
```
git diff 93c05869^..520249a2 --stat
```
to confirm every changed file is covered, and:
```
git diff 93c05869^..520249a2 -- contracts/ | grep -E '^[+-]\s*(function |event |uint\d+ (constant|internal constant|private constant))' | head -60
```
to confirm every added/removed function/event/constant signature is in the table.

**Section 2: Deleted Function Unreachability Proofs**

For each deleted item, run `grep -rn "ITEM_NAME" contracts/` and paste the output (or "0 results") as proof of unreachability. Items to verify:

1. `awardFinalDayDgnrsReward` (3 deletions: JackpotModule external, AdvanceModule private wrapper, IDegenerusGameModules interface)
2. `_randTraitTicket` (the old address-only version -- note the NEW function is named `_randTraitTicket` after rename from `_randTraitTicketWithIndices`, so grep must distinguish)
3. `_creditJackpot`
4. `_hasTraitTickets`
5. `_validateTicketBudget`
6. `JackpotTicketWinner` event
7. `AutoRebuyProcessed` event -- NOTE: this event still exists in DegenerusGameDecimatorModule.sol (line 29, emitted at line 411). The audit must note this is a SEPARATE declaration in a different module, not a leftover from JackpotModule.
8. Constants: `AWARD_ETH`, `AWARD_BURNIE`, `AWARD_TICKETS`, `AWARD_DGNRS`, `AWARD_WHALE_PASS`

For each grep, format as:
```
### [Item Name] — UNREACHABLE
grep -rn "itemName" contracts/
[paste output or "0 matches"]
Verdict: No remaining callers/references. Safe to delete.
```

**Section 3: Cosmetic Changes (brief)**

List items 37-38 (formatting-only changes to _setCurrentPrizePool/_setFuturePrizePool calls and runBafJackpot destructuring). Note these are whitespace/formatting only with no behavioral impact. No proof needed beyond visual inspection of the diff.

Mark the document as PARTIAL -- Sections 4-6 completed in Task 2 (refactor proofs and event mapping).
  </action>
  <verify>
    <automated>grep -c "UNREACHABLE\|COSMETIC\|DELETED" .planning/phases/192-delta-extraction-behavioral-verification/192-01-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - 192-01-AUDIT.md contains "## Function-Level Changelog" with a table of all 38 items
    - 192-01-AUDIT.md contains unreachability proof sections for all 8 deleted item groups (awardFinalDayDgnrsReward x3, _randTraitTicket old, _creditJackpot, _hasTraitTickets, _validateTicketBudget, JackpotTicketWinner, AutoRebuyProcessed, AWARD_* constants)
    - Every unreachability proof contains actual grep output showing zero callers (except AutoRebuyProcessed which must note DecimatorModule's independent copy)
    - 192-01-AUDIT.md contains a cosmetic changes section for items 37-38
  </acceptance_criteria>
  <done>Complete changelog table and all deleted-item unreachability proofs written with grep evidence</done>
</task>

<task type="auto">
  <name>Task 2: Refactor equivalence proofs and event migration mapping</name>
  <files>.planning/phases/192-delta-extraction-behavioral-verification/192-01-AUDIT.md</files>
  <read_first>
    - .planning/phases/192-delta-extraction-behavioral-verification/192-01-AUDIT.md (Task 1 output -- must append to this file)
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 756-860 for _addClaimableEth + _processAutoRebuy new signatures)
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 1511-1580 for _processSoloBucketWinner new signature)
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 1986-2090 for _randTraitTicket current -- was _randTraitTicketWithIndices)
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 2101-2170 for _awardDailyCoinToTraitWinners with new creditFlipBatch call)
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 708-750 for distributeYieldSurplus)
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 2375-2556 for runBafJackpot)
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 68-115 for new event declarations)
    - contracts/BurnieCoinflip.sol (line 906+ for creditFlipBatch new signature)
    - contracts/interfaces/IBurnieCoinflip.sol (line 122+ for creditFlipBatch interface)
    - Run: git show 93c05869^:contracts/modules/DegenerusGameJackpotModule.sol | grep -n "function _addClaimableEth\|function _processAutoRebuy\|function _processSoloBucketWinner\|function _randTraitTicketWithIndices\|function _randTraitTicket\|function _awardDailyCoinToTraitWinners\|function distributeYieldSurplus\|function runBafJackpot" (old line numbers for pre-commit state)
    - Run: git show 93c05869^:contracts/BurnieCoinflip.sol | grep -n "function creditFlipBatch" (old creditFlipBatch signature)
  </read_first>
  <action>
Append Sections 4-6 to the existing 192-01-AUDIT.md:

**Section 4: Refactor Equivalence Proofs**

For each REFACTOR-classified function, produce an old-vs-new comparison:

**4.1: _addClaimableEth (item 22) -- return signature change**
- OLD: `returns (uint256)` -- returned single value (weiAmount or calc.reserved)
- NEW: `returns (uint256, uint24, uint32)` -- returns (same value, rebuyLevel, rebuyTickets)
- Read the old function body (via `git show 93c05869^:contracts/modules/DegenerusGameJackpotModule.sol`) and compare line-by-line with current (lines 756-788)
- Prove: The first return value is computed identically. The additional return values (rebuyLevel, rebuyTickets) are new information surfaced for event emission only -- they do not change any state writes or the primary return value
- Check every caller of _addClaimableEth in the current code. Verify callers that destructure the new returns do so correctly (no mismatched tuple positions). Callers that use `(uint256 d, , )` pattern must be noted as intentionally discarding the new fields

**4.2: _processAutoRebuy (item 23) -- return signature change**
- OLD: `returns (uint256)` -- returned ethReserved
- NEW: `returns (uint256, uint24, uint32)` -- returns (ethReserved, rebuyLevel, rebuyTickets)
- Same proof pattern: first return identical, additional values surfaced for events

**4.3: _processSoloBucketWinner (item 24) -- return signature change**
- OLD: returned 4 values
- NEW: returns 6 values (adds rebuyLevel, rebuyTickets)
- Prove first 4 values identical, additional 2 are passed through from _addClaimableEth

**4.4: _randTraitTicket rename (item 26)**
- OLD: `_randTraitTicketWithIndices` with specific signature
- NEW: `_randTraitTicket` with identical signature and body
- Prove: the function body is character-for-character identical (modulo the name). Show all callers now use the new name. The old `_randTraitTicket` (address-only version) was a separate function that has been deleted (proven unreachable in Section 2)

**4.5: creditFlipBatch (items 27-28) -- array generalization**
- OLD: `address[3] memory players, uint256[3] memory amounts` (fixed-size)
- NEW: `address[] memory players, uint256[] memory amounts` (dynamic)
- Read old BurnieCoinflip.sol creditFlipBatch and new (line 906+). Prove: per-element behavior (the for-loop body) is identical. The only change is fixed-size to dynamic arrays. Interface (IBurnieCoinflip line 122+) matches implementation

**4.6: _awardDailyCoinToTraitWinners (item 31) -- batch logic change**
- OLD: batched creditFlipBatch calls in groups of 3 (with address[3] arrays)
- NEW: single creditFlipBatch call with full dynamic arrays
- Read old function body and compare with current (lines 2101-2170). Prove: same set of (player, amount) pairs are passed, just in a single call instead of chunked. The per-player credit inside BurnieCoinflip is identical

**4.7: distributeYieldSurplus (item 34)**
- OLD: single return from _addClaimableEth
- NEW: destructures 3-tuple, uses only first value (same as before)
- Prove: sum passed to claimablePool is identical

**4.8: runBafJackpot (item 35)**
- OLD: emitted JackpotTicketWinner events
- NEW: emits JackpotEthWin, JackpotTicketWin, JackpotBurnieWin events
- Prove: payout logic is unchanged -- same amounts to same recipients. Only event emissions differ

**4.9: payDailyJackpot coinJackpot guard removal (item 36)**
- OLD: `if (targetLevel != 0) { _awardDailyCoinToTraitWinners(...) }`
- NEW: always calls `_awardDailyCoinToTraitWinners(...)`
- Cross-reference with `_selectDailyCoinTargetLevel` simplification (this is the CALLER side; the function change is INTENTIONAL and covered in Plan 02). Document that the guard removal is a CONSEQUENCE of the function simplification, not an independent change

**Section 5: Event Migration Mapping (D-09)**

Build a comprehensive table mapping every old JackpotTicketWinner / AutoRebuyProcessed emission site to its replacement:

For each emission site in the pre-520249a2 code:
1. Identify the exact location (function name, approximate line) where JackpotTicketWinner was emitted
2. Find the corresponding location in the current code
3. Map old event fields to new event fields, noting:
   - Fields preserved (same name and value)
   - Fields renamed (different name, same value)
   - Fields dropped (present in old, absent in new)
   - Fields added (absent in old, present in new)

Use this table format:
```
| # | Old Site (function) | Old Event | New Event | Fields Preserved | Fields Dropped | Fields Added | Info Change |
```

Specific sites to map (find by searching pre-commit code for `emit JackpotTicketWinner`):
- Daily ETH winners (normal bucket)
- Daily ETH winners (solo bucket -- ETH portion)
- Daily whale pass (solo bucket -- pass portion)
- Daily DGNRS reward (solo bucket -- final day)
- Daily ticket awards
- Daily BURNIE awards
- BAF ETH winners
- BAF ticket/lootbox awards
- BAF BURNIE awards
- Early-burn lootbox awards
- Yield surplus distribution

For each, verify the new event signature from JackpotModule lines 68-115 and confirm indexed fields are appropriate for off-chain indexing.

**Section 6: Finding Register**

Create an empty finding register table:
```
| # | Severity | Item | Description | Status |
```
If any proof reveals a discrepancy, log it here. If all proofs pass, note "No findings -- all refactored paths proven equivalent."

Mark the document as COMPLETE for Plan 01 scope. Note that intentional-change correctness proofs (whale pass path, DGNRS fold, _selectDailyCoinTargetLevel, _validateTicketBudget removal) are covered in Plan 02.
  </action>
  <verify>
    <automated>grep -c "EQUIVALENT\|REFACTOR\|Section [4-6]" .planning/phases/192-delta-extraction-behavioral-verification/192-01-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - 192-01-AUDIT.md contains "## Section 4: Refactor Equivalence Proofs" with subsections 4.1-4.9
    - 192-01-AUDIT.md contains old-vs-new code comparison for each refactored function
    - 192-01-AUDIT.md contains "## Section 5: Event Migration Mapping" with a table covering every old JackpotTicketWinner emission site
    - Every event mapping row identifies preserved, dropped, and added fields
    - 192-01-AUDIT.md contains "## Section 6: Finding Register"
    - The document references items by number (1-38) matching the changelog table
  </acceptance_criteria>
  <done>All refactor equivalence proofs and event migration mapping complete, every REFACTOR item has old-vs-new comparison with equivalence verdict</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Audit-only phase -- no code changes, no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192-01 | Information Disclosure | Event field changes | accept | Events are public on-chain; field changes documented in Section 5 event migration mapping |
</threat_model>

<verification>
- 192-01-AUDIT.md exists with all 6 sections
- Changelog table covers all 38 items from research inventory
- Every deleted item has grep-verified unreachability proof
- Every REFACTOR item has old-vs-new equivalence proof
- Event migration table maps every old emission site to new event
- Finding register documents any discrepancies (or notes none found)
</verification>

<success_criteria>
- Complete function-level changelog exists covering all changes in both commits
- Every deleted function/event/constant proven unreachable via grep
- Every refactored function proven equivalent via old-vs-new comparison
- Every JackpotTicketWinner emission site mapped to its replacement event
</success_criteria>

<output>
After completion, create `.planning/phases/192-delta-extraction-behavioral-verification/192-01-SUMMARY.md`
</output>
