---
phase: 192-delta-extraction-behavioral-verification
plan: 02
type: execute
wave: 2
depends_on: [192-01]
files_modified:
  - .planning/phases/192-delta-extraction-behavioral-verification/192-02-AUDIT.md
autonomous: true
requirements:
  - DELTA-02

must_haves:
  truths:
    - "The whale pass is proven to be awarded ONLY on the daily jackpot path for solo bucket winners -- early-burn and terminal paths now pay straight ETH with no whale pass split"
    - "The DGNRS solo reward fold is proven correct: same total ETH amount goes to the daily ETH winner on the final physical day, via a single combined payout instead of a separate re-pick"
    - "The _selectDailyCoinTargetLevel simplification is proven safe: removing the _hasTraitTickets pre-check and always returning a level produces identical outcomes because _awardDailyCoinToTraitWinners naturally handles empty buckets"
    - "The _validateTicketBudget removal is proven safe: budget always allocated but unspent budget stays in pool when no tickets exist, producing identical pool balances"
    - "The entropy path through the DGNRS fold is documented as an INTENTIONAL change: the old code used a different salt (254) to re-pick a potentially different winner; the new code gives the reward to the same ETH winner"
  artifacts:
    - path: ".planning/phases/192-delta-extraction-behavioral-verification/192-02-AUDIT.md"
      provides: "Correctness proofs for all 4 intentional behavioral changes"
      contains: "## Intentional Change"
  key_links:
    - from: "_processDailyEth"
      to: "_handleSoloBucketWinner"
      via: "traitIdx == remainderIdx check"
      pattern: "_handleSoloBucketWinner"
    - from: "_resolveTraitWinners"
      to: "_addClaimableEth"
      via: "straight ETH payment (no whale pass split)"
      pattern: "_addClaimableEth"
    - from: "_selectDailyCoinTargetLevel"
      to: "_awardDailyCoinToTraitWinners"
      via: "always returns valid level, caller always invokes"
      pattern: "_selectDailyCoinTargetLevel"
---

<objective>
Prove correctness of all 4 intentional behavioral changes: whale pass path restriction, DGNRS solo reward fold, _selectDailyCoinTargetLevel simplification, and _validateTicketBudget removal.

Purpose: Complete DELTA-02 by providing correctness proofs for every INTENTIONAL-classified change from the Plan 01 changelog.
Output: 192-02-AUDIT.md with traced code paths, algebraic proofs, and worked examples for each intentional change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/192-delta-extraction-behavioral-verification/192-CONTEXT.md
@.planning/phases/192-delta-extraction-behavioral-verification/192-RESEARCH.md
@.planning/phases/192-delta-extraction-behavioral-verification/192-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Whale pass path restriction + DGNRS fold correctness proofs</name>
  <files>.planning/phases/192-delta-extraction-behavioral-verification/192-02-AUDIT.md</files>
  <read_first>
    - .planning/phases/192-delta-extraction-behavioral-verification/192-01-AUDIT.md (changelog and refactor proofs from Plan 01 -- understand what was classified INTENTIONAL)
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 1128-1425 for _processDailyEth -- the primary site of both changes)
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 1317-1425 for _resolveTraitWinners -- whale pass removed from here)
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 1426-1510 for _handleSoloBucketWinner -- new wrapper)
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 1511-1580 for _processSoloBucketWinner -- 75/25 ETH/whale-pass split)
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 1481-1510 for _payNormalBucket -- new extracted function)
    - Run: git show 93c05869^:contracts/modules/DegenerusGameJackpotModule.sol (pre-commit baseline for _processDailyEth, _resolveTraitWinners, awardFinalDayDgnrsReward old code)
    - Run: git show 93c05869^:contracts/modules/DegenerusGameAdvanceModule.sol (pre-commit baseline for _awardFinalDayDgnrsReward wrapper)
    - contracts/modules/DegenerusGameAdvanceModule.sol (grep for _awardFinalDayDgnrsReward to confirm removal)
  </read_first>
  <action>
Create 192-02-AUDIT.md with the following sections:

**Section 1: Intentional Change -- Whale Pass Daily Path Restriction (D-07)**

**1.1: Old Behavior Trace**

Reconstruct the old code path using `git show 93c05869^:contracts/modules/DegenerusGameJackpotModule.sol`:

1. Find old `_resolveTraitWinners` -- it had `isSoloBucket` detection: when `winnerCount == 1`, called `_processSoloBucketWinner` which did the 75/25 ETH/whale-pass split
2. Identify ALL callers of `_resolveTraitWinners` in the old code:
   - `_processDailyEth` (daily jackpot path)
   - `_resolveEarlyBurnJackpot` or equivalent (early-burn path)
   - `_resolveTerminalJackpot` or equivalent (terminal path -- if applicable)
3. Document: in the old code, whale pass was awarded on ANY path where a solo bucket winner occurred

**1.2: New Behavior Trace**

Trace the current code:

1. `_resolveTraitWinners` (lines 1317-1425): NO solo bucket detection. ALL winners (including solo bucket) get `_addClaimableEth` + `JackpotEthWin`. Straight ETH, no whale pass split
2. `_processDailyEth` (lines 1128-1316): the `traitIdx == remainderIdx` check detects a solo bucket ONLY in the daily path. When detected, calls `_handleSoloBucketWinner` (line 1426) which calls `_processSoloBucketWinner` for the 75/25 split + whale pass
3. Early-burn path (`_runEarlyBirdLootboxJackpot` line 626): calls `_resolveTraitWinners` -> straight ETH, no whale pass
4. BAF path (`runBafJackpot` line 2375): does NOT call `_resolveTraitWinners` at all (uses its own winner selection), so unaffected

Show the path enumeration:
```
| Path | Old: Whale Pass? | New: Whale Pass? | Change |
|------|------------------|------------------|--------|
| Daily normal bucket | No (multiple winners) | No | Unchanged |
| Daily solo bucket | Yes (75/25 split) | Yes (75/25 split via _handleSoloBucketWinner) | Unchanged |
| Early-burn solo bucket | Yes (75/25 split) | No (straight ETH) | INTENTIONAL |
| Terminal solo bucket | Yes (75/25 split) | No (straight ETH) | INTENTIONAL |
| BAF | N/A | N/A | Unchanged |
```

**1.3: Correctness Argument**

The whale pass restriction to daily-only is INTENTIONAL and correct because:
- The whale pass is a daily reward mechanic
- Early-burn and terminal paths now pay full ETH (100%) instead of 75% ETH + whale pass
- No whale pass is "lost" -- the ETH value goes directly to the winner
- The `_processSoloBucketWinner` function itself is unchanged (same 75/25 split logic) -- it's just called from a different location

**Section 2: Intentional Change -- DGNRS Solo Reward Fold (D-08)**

**2.1: Old Behavior Trace**

Using `git show 93c05869^`:

1. `awardFinalDayDgnrsReward` in JackpotModule: a separate external function called via delegatecall from AdvanceModule
2. Old caller in AdvanceModule `_awardFinalDayDgnrsReward`: called AFTER `payDailyJackpot`, used different entropy (rngWord with salt 254 via `_randTraitTicket`)
3. The old code re-picked a winner from the same solo bucket but with different randomness, meaning the DGNRS reward winner could be a DIFFERENT player than the ETH winner
4. Document the exact old entropy path: `keccak256(abi.encode(randomWord, trait, 254, i))` vs the daily ETH path's salt

**2.2: New Behavior Trace**

Trace current `_processDailyEth` (lines 1128-1316):

1. Find the `isFinalPhysicalDay_` parameter (added in commit 93c05869)
2. Find where DGNRS reward is now awarded: when `isFinalPhysicalDay_ && traitIdx == remainderIdx`, the DGNRS reward goes to address `w` -- the SAME address that won the solo bucket ETH
3. Document the exact code: `dgnrs.transferFromPool(w, (dgnrsPool * FINAL_DAY_DGNRS_BPS) / 10_000)`
4. Verify `FINAL_DAY_DGNRS_BPS` value matches the old code
5. Verify `dgnrsPool` source matches the old code (same storage read)

**2.3: Correctness Proof**

1. **Amount equivalence:** Show `(dgnrsPool * FINAL_DAY_DGNRS_BPS) / 10_000` is the same formula in both old and new code
2. **Winner change (INTENTIONAL):** Old = re-picked with different salt (could be different winner). New = same winner as ETH. Document this as the INTENTIONAL fix -- the old behavior was a bug where different salt picked a different person from the solo bucket
3. **Timing:** Old = called after `payDailyJackpot` in AdvanceModule. New = inline during `_processDailyEth`. Document that this changes when the transfer happens relative to other jackpot operations but does not affect the amount or the pool state (DGNRS pool is independent of ETH pools)
4. **Event:** Document which event now covers the DGNRS award (should be `JackpotDgnrsWin`)

**2.4: Entropy Path Analysis**

The old code consumed an additional VRF word derivation (salt 254) for the DGNRS pick. The new code does NOT consume this entropy path. Document:
- This means the entropy consumption pattern differs between old and new
- This does NOT affect security because: (a) the VRF word is the same regardless, (b) removing a consumption does not make any remaining consumptions less random, (c) the only effect is that downstream random operations (if any use sequential salts) may receive a different entropy derivation -- but the DGNRS fold was the LAST operation in the old flow, so no downstream operations are affected

Mark Section 2 as INTENTIONAL-CORRECT.
  </action>
  <verify>
    <automated>grep -c "INTENTIONAL\|Correctness\|Whale Pass\|DGNRS" .planning/phases/192-delta-extraction-behavioral-verification/192-02-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - 192-02-AUDIT.md contains "## Section 1: Intentional Change -- Whale Pass Daily Path Restriction"
    - Section 1 contains a path enumeration table showing old vs new whale pass behavior for daily/early-burn/terminal/BAF paths
    - Section 1 traces both old _resolveTraitWinners (with solo bucket detection) and new _resolveTraitWinners (without)
    - 192-02-AUDIT.md contains "## Section 2: Intentional Change -- DGNRS Solo Reward Fold"
    - Section 2 contains old-vs-new code traces for awardFinalDayDgnrsReward (old) vs inline in _processDailyEth (new)
    - Section 2 documents the INTENTIONAL winner change (re-pick with different salt vs same winner)
    - Section 2 contains entropy path analysis documenting the removed salt-254 derivation
    - Section 2 verifies FINAL_DAY_DGNRS_BPS and dgnrsPool match between old and new
  </acceptance_criteria>
  <done>Whale pass path restriction proven correct across all caller paths, DGNRS fold proven correct with documented intentional winner change and entropy path analysis</done>
</task>

<task type="auto">
  <name>Task 2: _selectDailyCoinTargetLevel + _validateTicketBudget removal proofs and overall verdict</name>
  <files>.planning/phases/192-delta-extraction-behavioral-verification/192-02-AUDIT.md</files>
  <read_first>
    - .planning/phases/192-delta-extraction-behavioral-verification/192-02-AUDIT.md (Task 1 output -- must append to this file)
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 2093-2100 for _selectDailyCoinTargetLevel current -- pure function)
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 2101-2170 for _awardDailyCoinToTraitWinners -- handles empty buckets)
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 1986-2090 for _randTraitTicket -- behavior on empty input)
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 320-540 for payDailyJackpot -- caller of _selectDailyCoinTargetLevel and budget validation)
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 626-706 for _runEarlyBirdLootboxJackpot -- other budget validation caller)
    - Run: git show 93c05869^:contracts/modules/DegenerusGameJackpotModule.sol (search for _selectDailyCoinTargetLevel, _validateTicketBudget, _hasTraitTickets old implementations)
    - contracts/modules/DegenerusGameJackpotModule.sol (search for _distributeTicketsToBuckets to trace empty-bucket handling)
  </read_first>
  <action>
Append Sections 3-5 to the existing 192-02-AUDIT.md:

**Section 3: Intentional Change -- _selectDailyCoinTargetLevel Simplification (D-09 items 25, 36)**

**3.1: Old Behavior Trace**

Using `git show 93c05869^`, find the old `_selectDailyCoinTargetLevel`:
1. Old signature: took `winningTraitsPacked` parameter
2. Old logic: selected random level in [lvl, lvl+4], then called `_hasTraitTickets` to check if tickets existed at that level
3. If no tickets: returned 0
4. Caller `payDailyJackpot` had guard: `if (targetLevel != 0) { _awardDailyCoinToTraitWinners(...) }`
5. Effect: coin jackpot was SKIPPED entirely when no tickets existed at the randomly selected level

**3.2: New Behavior Trace**

Current `_selectDailyCoinTargetLevel` (lines 2093-2100):
1. New signature: no `winningTraitsPacked` parameter, marked `pure`
2. New logic: always returns `lvl + uint24(entropy % 5)`, never returns 0
3. Caller `payDailyJackpot` (find the call site): always calls `_awardDailyCoinToTraitWinners` (no `targetLevel != 0` guard)
4. Effect: coin jackpot is ALWAYS attempted

**3.3: Empty Bucket Safety Proof**

Trace what happens when _awardDailyCoinToTraitWinners is called with a level that has no tickets:

1. `_awardDailyCoinToTraitWinners` calls `_randTraitTicket` for each trait
2. `_randTraitTicket` (lines 1986-2090): when the ticket array for that trait at that level is empty (`effectiveLen == 0`), what happens?
   - Read the function body carefully. Find the early return or zero-length handling
   - Document: returns empty arrays (0 winners)
3. With 0 winners from `_randTraitTicket`, `_awardDailyCoinToTraitWinners`:
   - Does NOT call `creditFlipBatch` (or calls it with empty arrays -- document which)
   - Does NOT emit any events
   - Does NOT modify any state
4. Therefore: calling with empty buckets is a no-op except for gas cost

**3.4: Correctness Verdict**

INTENTIONAL-CORRECT: The simplification removes a view-function optimization (skip empty buckets) but produces identical outcomes. The only difference is gas: a small additional gas cost when tickets don't exist at the selected level (the empty _randTraitTicket calls). This is acceptable because:
- The gas cost is bounded (5 trait lookups maximum)
- The code is simpler and removes the `_hasTraitTickets` dependency

**Section 4: Intentional Change -- _validateTicketBudget Removal (items 18, 19, 32, 33)**

**4.1: Old Behavior Trace**

Using `git show 93c05869^`, find the old `_validateTicketBudget`:
1. Old logic: called `_hasTraitTickets(lvl)` -- if no trait tickets existed, returned 0 budget
2. Effect: when no tickets existed, budget was 0 and no ticket distribution was attempted

Two call sites:
- `payDailyJackpot`: `ticketBudget = _validateTicketBudget(lvl, budget / 5)`
- `_runEarlyBirdLootboxJackpot`: `ticketBudget = _validateTicketBudget(lvl, ethPool * BPS / 10_000)`

**4.2: New Behavior Trace**

Current code (find the budget calculation in payDailyJackpot and _runEarlyBirdLootboxJackpot):
1. `payDailyJackpot`: `ticketBudget = budget / 5` (direct calculation, no validation)
2. `_runEarlyBirdLootboxJackpot`: `ticketBudget = ethPool * BPS / 10_000` (direct calculation)

**4.3: Unspent Budget Flow Proof**

When budget is allocated but no tickets exist:

1. Budget flows to `_distributeTicketsToBuckets` (find line number)
2. `_distributeTicketsToBuckets` calls `_randTraitTicket` which returns empty arrays when no tickets exist
3. No tickets are queued, no ETH is consumed from the budget
4. Trace what happens to the unspent budget:
   - **Daily path:** the budget was carved out of the jackpot pool. With no winners, the ETH remains in the pool (it was never moved to claimable). Show the exact code path: the budget subtraction from pool and the fact that no `_addClaimableEth` is called means the pool keeps the ETH
   - **Early-burn path:** the budget is calculated from `ethPool`. Show that `paidEth` only accumulates actual payments. With no winners, `paidEth` stays at 0 for the ticket portion, so the pool keeps the ETH

5. Compare with old behavior: old budget was 0, so no subtraction from pool. New budget is non-zero, subtracted from pool, but returned (no winners consume it). Show the net effect on pool balance is identical:
   - Old: pool unchanged (budget=0, nothing subtracted)
   - New: pool unchanged (budget subtracted, then implicitly returned because no _addClaimableEth calls)

**4.4: Correctness Verdict**

INTENTIONAL-CORRECT: Budget is always allocated but never consumed when no tickets exist. Pool balances are identical. Gas cost is slightly higher (empty distribution loop). Code is simpler (removes _validateTicketBudget and _hasTraitTickets functions).

**Section 5: Phase 192 Overall Verdict**

Summarize all findings across both Plan 01 and Plan 02:

```
| Req | Description | Verdict |
|-----|-------------|---------|
| DELTA-01 | Function-level delta extraction | PASS -- 38 items catalogued across 5 files |
| DELTA-02 | Behavioral equivalence / correctness | PASS -- N REFACTOR items proven equivalent, 4 INTENTIONAL items proven correct |
```

List the 4 intentional changes with their verdicts:
1. Whale pass path restriction: INTENTIONAL-CORRECT
2. DGNRS solo reward fold: INTENTIONAL-CORRECT (winner change is the intended fix)
3. _selectDailyCoinTargetLevel simplification: INTENTIONAL-CORRECT (empty bucket safe)
4. _validateTicketBudget removal: INTENTIONAL-CORRECT (unspent budget stays in pool)

List any findings from the Plan 01 finding register.

Note the AutoRebuyProcessed event observation: still exists independently in DecimatorModule (not a leftover).

State: "All changes in commits 93c05869 and 520249a2 have been extracted, classified, and proven correct. Phase 192 requirements DELTA-01 and DELTA-02 are satisfied."
  </action>
  <verify>
    <automated>grep -c "INTENTIONAL-CORRECT\|PASS\|Overall Verdict" .planning/phases/192-delta-extraction-behavioral-verification/192-02-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - 192-02-AUDIT.md contains "## Section 3: Intentional Change -- _selectDailyCoinTargetLevel Simplification"
    - Section 3 contains old _selectDailyCoinTargetLevel trace showing _hasTraitTickets pre-check
    - Section 3 contains empty-bucket safety proof tracing _randTraitTicket behavior on empty input
    - 192-02-AUDIT.md contains "## Section 4: Intentional Change -- _validateTicketBudget Removal"
    - Section 4 traces both daily and early-burn budget paths
    - Section 4 proves unspent budget stays in pool (net zero pool balance change)
    - 192-02-AUDIT.md contains "## Section 5: Phase 192 Overall Verdict"
    - Section 5 contains a verdict table with DELTA-01 PASS and DELTA-02 PASS
    - Section 5 lists all 4 intentional changes with INTENTIONAL-CORRECT verdicts
  </acceptance_criteria>
  <done>All 4 intentional changes proven correct, overall phase verdict written with DELTA-01 and DELTA-02 both PASS</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Audit-only phase -- no code changes, no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192-02 | Tampering | DGNRS solo reward winner selection | accept | Old behavior (different salt picking different winner) was the bug; new behavior (same winner) is the fix. Documented as intentional change in Section 2 |
| T-192-03 | Denial of Service | Empty bucket gas cost | accept | Removing _validateTicketBudget adds small gas for empty-bucket loops; bounded by 5 traits; well within block gas limit |
</threat_model>

<verification>
- 192-02-AUDIT.md exists with all 5 sections
- Each intentional change has old-vs-new code traces with concrete line references
- Whale pass path enumeration covers daily, early-burn, terminal, and BAF paths
- DGNRS fold proves amount equivalence and documents winner change as intentional fix
- _selectDailyCoinTargetLevel proof traces empty bucket through _randTraitTicket
- _validateTicketBudget proof traces unspent budget through both daily and early-burn paths
- Overall verdict table shows DELTA-01 PASS and DELTA-02 PASS
</verification>

<success_criteria>
- All 4 intentional behavioral changes have traced code paths with correctness proofs
- The whale pass restriction is proven correct across all 4 caller paths
- The DGNRS fold is proven correct with the winner change documented as the intended fix
- Empty bucket handling is proven safe for both _selectDailyCoinTargetLevel and _validateTicketBudget removal
- Phase 192 overall verdict declares both DELTA-01 and DELTA-02 as PASS
</success_criteria>

<output>
After completion, create `.planning/phases/192-delta-extraction-behavioral-verification/192-02-SUMMARY.md`
</output>
