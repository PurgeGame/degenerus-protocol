---
phase: 235-conservation-rng-commitment-re-proof-phase-transition
plan: 235-02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-02-AUDIT.md
autonomous: true
requirements: [CONS-02]
head_sha: 1646d5af
must_haves:
  truths:
    - "Every BURNIE mint site across the v29.0 delta is enumerated in the Per-Mint-Site Catalog, and every site is confirmed to route through `BurnieCoin.mintForGame` — the canonical mint gateway. No new mint site bypasses `mintForGame`."
    - "Every BURNIE burn site across the v29.0 delta is enumerated in the Per-Burn-Site Catalog — specifically `BurnieCoin.decimatorBurn` (3ad0f8d3 MODIFIED) + every caller chain that invokes it + every other burn path (if any)."
    - "The MintModule quest credit chain is algebraically walked: `_purchaseFor` → `quests.handlePurchase(player, ethFreshWei, burnieMintQty, lootBoxAmount, mintPrice, ...)` → `DegenerusQuests.handlePurchase` → `mint_ETH` / `mint_BURNIE` / quest accumulators. For each BURNIE-affecting quest slot, verify the accumulator delta matches the input `burnieMintQty` (d5284be5 — wei-direct `ethFreshWei` does NOT affect BURNIE conservation; the BURNIE accumulator is the `burnieMintQty` parameter which is a distinct argument)."
    - "The AUDIT.md output contains a named `## 232.1 Ticket-Processing Impact` sub-section (per D-06) confirming: (a) the 232.1 fix series introduces ZERO new BURNIE mint sites (pre-finalize gate writes `lootboxRngWordByIndex` — RNG namespace, not BURNIE namespace), (b) the lootbox BURNIE redirect to far-future key space (bit 22 per v11.0) behavior is unchanged, (c) `mintForGame` gating during `gameOverPossible` drip projection is preserved, (d) no new mint path exposed inside the rngLocked window"
    - "The AUDIT.md output cross-cites (per D-04 format) the following prior-phase verdicts with a `re-verified at HEAD 1646d5af` note per cited row: 232-01-AUDIT DCM-01 (BURNIE sum-in/sum-out handoff acceptance — confirming `decimatorBurn` lvl+1 keying does not introduce BURNIE supply change); 234-01-AUDIT QST-01 (`mint_ETH` quest wei credit — ETH unit; non-overlap with BURNIE conservation); 234-01-AUDIT QST-02 (`boonPacked` exposure — storage-read-only; no BURNIE delta); 234-01-AUDIT QST-03 (`BurnieCoin.sol` change handoff acceptance — confirming change confined to decimator-burn-key plumbing with no supply impact)"
    - "Every verdict row uses vocabulary exactly `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`"
    - "Every verdict row carries a `Finding Candidate: Y/N` column value"
    - "No F-29-NN finding IDs appear anywhere in 235-02-AUDIT.md (per D-14)"
    - "Scope-guard deferrals, if any, recorded in 235-02-SUMMARY.md per D-15 (230-01-DELTA-MAP.md and 230-02-DELTA-ADDENDUM.md NOT edited)"
    - "Findings-Candidate Block, Scope-guard Deferrals, and Downstream Hand-offs subsections present; Phase 236 FIND-01 hand-off explicitly named for ID assignment"
    - "Zero contracts/ and zero test/ writes occur during plan execution (READ-only per D-17)"
  artifacts:
    - path: ".planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-02-AUDIT.md"
      provides: "CONS-02 BURNIE conservation proof across v29.0 delta"
      contains: "## Per-Mint-Site Catalog"
  key_links:
    - from: "230-01-DELTA-MAP.md §1.4 (MintModule quest credit chain) + §1.7 (Quests.handlePurchase) + §1.8 (BurnieCoin.decimatorBurn) + §2 (all burn/mint chains crossing BurnieCoin) + §4 Consumer Index CONS-02 row"
      to: "235-02-AUDIT.md Per-Mint-Site + Per-Burn-Site Catalog rows"
      via: "Every BURNIE creation/destruction site owned by the v29.0 delta gets a catalog row"
      pattern: "mintForGame|decimatorBurn|burnieMintQty|burnFromGame"
    - from: "235-02-AUDIT.md VULNERABLE/DEFERRED/SAFE-INFO Candidate: Y verdicts"
      to: "Phase 236 FIND-01 finding-candidate pool"
      via: "Findings-Candidate Block (no F-29-NN IDs, Finding Candidate: Y/N column)"
      pattern: "## Findings-Candidate Block"
    - from: "235-02-AUDIT.md quest credit algebra sub-section"
      to: "Phase 236 FIND-01 BURNIE supply conservation closure"
      via: "Per-quest-slot delta matches burnieMintQty input; any gap recorded as Finding Candidate: Y"
      pattern: "burnieMintQty|burnie accumulator|quest delta"
---

<objective>
CONS-02 analytical re-proof of BURNIE conservation across the full v29.0 delta + 230-02 addendum + 232.1 fix series at HEAD `1646d5af`. Produces `235-02-AUDIT.md`: a Per-Mint-Site Catalog confirming every BURNIE creation flows through `BurnieCoin.mintForGame`, a Per-Burn-Site Catalog enumerating every burn path (primarily `BurnieCoin.decimatorBurn` MODIFIED by 3ad0f8d3), a Quest Credit Algebra sub-section walking the `_purchaseFor` → `handlePurchase` → `burnieMintQty` accumulator chain, a mandatory `## 232.1 Ticket-Processing Impact` sub-section (per D-06) confirming zero new mint sites in the fix series, cross-cites to 232-01 DCM-01 + 234-01 QST-01/02/03 each re-verified at HEAD `1646d5af`, Findings-Candidate Block, Scope-guard Deferrals, and Downstream Hand-offs.

Purpose: BURNIE conservation is a mint/burn balance invariant — the sum of `BurnieCoin.balanceOf(all holders)` must equal the cumulative sum of every `mintForGame(to, amount)` minus every burn. A C4A warden attacks BURNIE conservation by searching for (a) a new mint site that bypasses `mintForGame` (e.g. direct `_mint` calls inside BurnieCoin.sol without the gateway guard), (b) a burn site that doesn't decrement balance (e.g. misaccounted ticket burns), (c) quest credit arithmetic that mints unwarranted BURNIE into the accumulator. Phase 232 DCM-01 scoped `BurnieCoin.decimatorBurn` to burn-key correctness only and HANDED OFF supply conservation to Phase 235 CONS-02. Phase 234 QST-01/02/03 scoped the quest + boon + BurnieCoin changes to their isolated impact and HANDED OFF BURNIE supply conservation to CONS-02. Per D-03, Plan 235-02 re-proves BURNIE conservation FRESH at HEAD `1646d5af` and CROSS-CITES the two prior handoffs per D-04 with re-verify notes. Per D-14 no F-29-NN IDs; Phase 236 FIND-01 owns ID assignment. Per D-17 READ-only.

Output: `235-02-AUDIT.md` with Per-Mint-Site Catalog + Per-Burn-Site Catalog + Quest Credit Algebra + 232.1 Ticket-Processing Impact + Findings-Candidate Block + Scope-guard Deferrals + Downstream Hand-offs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-CONTEXT.md
@.planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md
@.planning/phases/230-delta-extraction-scope-map/230-02-DELTA-ADDENDUM.md
@.planning/phases/230-delta-extraction-scope-map/230-01-SUMMARY.md
@.planning/phases/232-decimator-audit/232-01-AUDIT.md
@.planning/phases/234-quests-boons-misc-audit/234-01-AUDIT.md
@.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-01-FIX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the CONS-02 Per-Mint-Site + Per-Burn-Site Catalogs + Quest Credit Algebra at HEAD 1646d5af; write 235-02-AUDIT.md</name>
  <files>.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-02-AUDIT.md</files>
  <read_first>
    - .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-CONTEXT.md (D-01 through D-17 — specifically D-06 mandatory 232.1 sub-section per-requirement / D-14 no F-29-NN IDs / D-15 scope-guard deferral / D-17 READ-only)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.4 (MintModule — `_purchaseFor` MODIFIED by f20a2b5e + d5284be5; `_callTicketPurchase` MODIFIED by f20a2b5e + d5284be5 — return tuple gained `freshEth`, dropped `ethMintUnits`; quest-credit argument switch from `ethMintUnits` to `ethFreshWei`)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.7 (core DegenerusQuests.sol — `handlePurchase` MODIFIED by d5284be5 — parameter switch `uint32 ethMintQty -> uint256 ethFreshWei` for MINT_ETH quest 1:1 wei credit; `burnieMintQty` parameter UNCHANGED — BURNIE quest credit path unchanged)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.8 (core BurnieCoin.sol — `decimatorBurn` MODIFIED by 3ad0f8d3 — `lvl = degenerusGame.level() + 1` keying; two new comment lines)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.12 (IDegenerusQuests.sol — `handlePurchase` MODIFIED by d5284be5 — interface decl matches implementer)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §2.4 (Quest/Boon/Misc chains — IM-17 MintModule._purchaseFor -> DegenerusQuests.handlePurchase direct external call; IM-18 MintModule._purchaseFor -> DegenerusGame.recordMintQuestStreak self-call; IM-19 UI readers -> DegenerusGame.boonPacked getter)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §4 Consumer Index CONS-02 row (authoritative scope anchor per D-04 of Phase 235 CONTEXT.md)
    - .planning/phases/230-delta-extraction-scope-map/230-02-DELTA-ADDENDUM.md — addendum commits 314443af + c2e5e0a9 modify entropy derivation + `_raritySymbolBatch` seed; neither introduces any BURNIE mint or burn site. Record explicitly: "230-02 addendum commits introduce zero new BURNIE mint/burn sites — CONS-02 surface unchanged by the addendum."
    - contracts/BurnieCoin.sol at HEAD — enumerate every function that mints or burns BURNIE:
        * `mintForGame(address to, uint256 amount)` — canonical mint gateway; record exact File:Line + caller-gating modifier (expected: onlyGame or equivalent)
        * `decimatorBurn(address player, uint256 amount)` (3ad0f8d3 MODIFIED) — burn path; record exact File:Line; verify body invokes ERC20 `_burn` or equivalent
        * grep the file for any other `_mint` / `_burn` / `mint` / `burn` calls — confirm zero unguarded paths
        * grep for `msg.sender` checks / `onlyGame` modifier / `degenerusGame` identity checks to confirm mint/burn gating
    - contracts/modules/DegenerusGameMintModule.sol at HEAD — `_purchaseFor` body (f20a2b5e + d5284be5 MODIFIED):
        * locate the `quests.handlePurchase(player, ethFreshWei, burnieMintQty, lootBoxAmount, mintPrice, ...)` call site. Record exact File:Line + full argument list.
        * locate the `BurnieCoin.mintForGame(player, burnieMintQty)` or equivalent call (NOTE: mint may go through `_callTicketPurchase` internals or via a separate module-side `burnie.mintForGame` call — grep MintModule for `mintForGame` to locate).
        * locate `_callTicketPurchase` at HEAD — record the return tuple (post-f20a2b5e: `(bonusCredit, adjustedQty32, targetLevel, burnieMintUnits, freshEth)`). `burnieMintUnits` is the BURNIE mint quantity returned to `_purchaseFor`.
        * grep for any BURNIE mint/burn site inside MintModule (expected: `burnie.mintForGame(buyer, burnieMintUnits)` or equivalent at the single mint site per `_purchaseFor`).
    - contracts/modules/DegenerusGameDecimatorModule.sol at HEAD — grep for any BURNIE mint/burn interaction. Expected: zero (decimator module interacts with DegenerusGame for pool state, not BurnieCoin directly). Record the absence explicitly.
    - contracts/DegenerusQuests.sol at HEAD — `handlePurchase` body (d5284be5 MODIFIED):
        * locate the function body and record every accumulator update for each quest slot.
        * specifically grep for BURNIE-affecting quest updates: any `playerQuestProgress[player][slotX].delta += burnieMintQty` or equivalent.
        * verify that `d5284be5` did NOT change the `burnieMintQty` parameter — only the `ethMintQty -> ethFreshWei` parameter (ETH unit switch, not BURNIE unit).
        * verify no new BURNIE mint site was added inside `handlePurchase` — quests track PROGRESS (delta accumulators) but do not mint BURNIE directly.
    - contracts/modules/DegenerusGameJackpotModule.sol at HEAD — grep for any `mintForGame` / `burnie.mint` interaction (expected: zero — jackpot module operates on ETH + ticket state, not BURNIE directly; record absence)
    - contracts/DegenerusGame.sol at HEAD — grep for any direct BurnieCoin mint/burn interaction beyond the external ABI exposure (expected: zero direct mint; potentially the BurnieCoin constructor-time mint via a separate path but not on the v29.0 delta path)
    - .planning/phases/232-decimator-audit/232-01-AUDIT.md — specifically the BurnieCoin rows (expected: 1 SAFE row for `decimatorBurn` burn-key correctness + BurnieCoin sum-in/sum-out HANDOFF to 235 CONS-02). Re-verify at HEAD `1646d5af` by re-reading `contracts/BurnieCoin.sol` `decimatorBurn` body.
    - .planning/phases/234-quests-boons-misc-audit/234-01-AUDIT.md — specifically the QST-01 `mint_ETH` wei-credit row (ETH unit, not BURNIE — explicitly non-overlap); the QST-02 `boonPacked` exposure row (storage-read-only accessor — no BURNIE delta); the QST-03 `BurnieCoin.sol` change row (confined to decimator-burn-key plumbing; no supply-conservation impact per 234-01-AUDIT QST-03 verdict). Each cross-cited must be re-verified at HEAD `1646d5af`.
    - .planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-01-FIX.md (pre-finalize gate diff — confirm gate body at AdvanceModule:257-279 has zero BurnieCoin interaction; specifically zero `mintForGame` / `decimatorBurn` calls inside the gate)
    - `head_sha: 1646d5af` is the LOCKED audit baseline SHA from phase start per CONTEXT.md D-05 — the audit is anchored to this SHA, NOT to dynamic `git HEAD`. Verify via `git diff --stat 1646d5af..HEAD -- contracts/ test/` (expected: empty output — docs-only commits may have advanced HEAD without changing the audit surface)
    - Output of `git log --oneline 14cb45e1..HEAD -- contracts/BurnieCoin.sol` — confirms 3ad0f8d3 is the sole commit touching BurnieCoin.sol in the v29.0 delta
  </read_first>
  <action>
Execute fresh-from-HEAD CONS-02 BURNIE conservation re-proof at HEAD `1646d5af` per CONTEXT.md D-01 through D-17.

**Step 1 — Enumerate every BURNIE mint site.** Grep `contracts/` at HEAD `1646d5af` for:
  - Direct `BurnieCoin._mint` calls (expected: only inside `mintForGame`, via the ERC20 `_mint` internal helper)
  - Calls to `burnie.mintForGame` / `BurnieCoin.mintForGame` / any alias — every external caller of the gateway
  - Confirm every mint flows through the single `mintForGame` entry point; any direct `_mint` call bypassing the gateway is a VULNERABLE row with Finding Candidate: Y

**Step 2 — Classify each mint site in the Per-Mint-Site Catalog table.** Columns:
  `Site | File:Line | Gateway | Amount Source | Guard | Verdict | Finding Candidate`
  - **Site:** function name + caller (e.g. `MintModule._purchaseFor → burnie.mintForGame(buyer, burnieMintUnits)`).
  - **File:Line:** absolute anchor into `contracts/` at HEAD.
  - **Gateway:** always `mintForGame` (if not, VULNERABLE with Finding Candidate: Y).
  - **Amount Source:** where the mint quantity comes from (e.g. `_callTicketPurchase.burnieMintUnits`, a fresh `burnieMintQty` argument, etc.).
  - **Guard:** caller-side gating (e.g. `onlyGame` modifier on `mintForGame`; `msg.sender == degenerusGame` inside the gateway).
  - **Verdict:** `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`.
  - **Finding Candidate:** `Y` or `N`.

**Step 3 — Enumerate every BURNIE burn site in the Per-Burn-Site Catalog.** Columns:
  `Site | File:Line | Burn Mechanism | Caller Guard | Amount Source | Verdict | Finding Candidate`
  Expected primary site: `BurnieCoin.decimatorBurn(player, amount)` (3ad0f8d3 MODIFIED) — lvl keying changed but burn mechanism preserved. Grep for any other burn path: `_burn(...)` internal calls, `burnFromGame`, etc. Verify every burn decrements balance.

**Step 4 — Write the Quest Credit Algebra sub-section.** Walk the chain:
  - `MintModule._purchaseFor` computes `burnieMintUnits` via `_callTicketPurchase` return tuple.
  - `_purchaseFor` calls `quests.handlePurchase(player, ethFreshWei, burnieMintQty, lootBoxAmount, mintPrice, ...)` at IM-17 (d5284be5 argument switch for `ethFreshWei` — NOT BURNIE).
  - Inside `DegenerusQuests.handlePurchase`, the quest accumulator updates for any BURNIE-relevant slot use `burnieMintQty` (unchanged by d5284be5).
  - Algebraic closure: the `burnieMintQty` passed to `handlePurchase` equals the `burnieMintUnits` returned by `_callTicketPurchase`; `_purchaseFor` also separately calls `burnie.mintForGame(buyer, burnieMintUnits)` (or equivalent — verify via grep) to actually mint the BURNIE. Quest accumulator delta and the actual mint are consistent by construction (same source value).
  - Verify no double-credit: the `handlePurchase` call does NOT mint BURNIE — it only updates quest accumulators. The single BURNIE mint per purchase is the `mintForGame` call in `_purchaseFor` itself.
  - Verify no missing-credit: if `burnieMintQty == 0`, the quest accumulator does not update and the mint does not fire — both paths gated on the same source value.

**Step 5 — Write the mandatory `## 232.1 Ticket-Processing Impact` sub-section (per D-06).** Walk each 232.1 fix series change against CONS-02:
  - **Pre-finalize gate at AdvanceModule:257-263** — writes `lootboxRngWordByIndex[idx]`; zero BurnieCoin interaction. Grep-confirmed: no `mintForGame` / `decimatorBurn` / `burnie.` inside gate body.
  - **Queue-length gate, nudged-word write, do-while integration, RngNotReady selector fix** — all AdvanceModule control-flow changes with zero BurnieCoin interaction.
  - **Game-over best-effort drain** (749192cd) — check `_handleGameOverPath` at AdvanceModule for any BurnieCoin interaction. Expected: zero (game-over path does not mint or burn BURNIE; terminal decimator path reads stored state).
  - **Buffer swap at RNG request time** (per D-12) — writes `LR_INDEX` + buffer state; zero BurnieCoin interaction.
  - Sub-section MUST state explicitly: "232.1 fix series introduces zero new BURNIE mint sites and zero new BURNIE burn sites. BURNIE supply conservation invariant UNCHANGED by the series. Per-Mint-Site + Per-Burn-Site Catalogs cover only the 10-commit baseline delta plus the two 230-02 addendum commits (both mint/burn-neutral — entropy derivation only)."
  - **Lootbox BURNIE redirect to far-future key space (bit 22 per v11.0)** — if this behavior exists at HEAD, record it. If not in the v29.0 delta, skip.
  - **`mintForGame` gating during `gameOverPossible` drip projection** — confirm the gateway's caller gate (onlyGame) is preserved; confirm the gateway does not allow mint when `gameOverPossible` is mid-transition (verify by re-reading `mintForGame` body).

**Step 6 — Write cross-cite rows (per D-04 format):**
  - 232-01-AUDIT DCM-01 (BURNIE sum-in/sum-out handoff acceptance): re-verified at HEAD `1646d5af` by re-reading `BurnieCoin.decimatorBurn` body; confirm only the lvl keying changed + two comment lines added; burn mechanism unchanged.
  - 234-01-AUDIT QST-01 (`mint_ETH` quest wei credit): re-verified at HEAD by re-reading `DegenerusQuests.handlePurchase` — confirm the `ethMintQty -> ethFreshWei` parameter change is ETH unit only; BURNIE path (`burnieMintQty` argument + its accumulator update) unchanged.
  - 234-01-AUDIT QST-02 (`boonPacked` exposure): re-verified at HEAD by re-reading `DegenerusGameStorage.boonPacked` mapping decl — confirm `public` visibility auto-generates getter; no BURNIE state delta.
  - 234-01-AUDIT QST-03 (`BurnieCoin.sol` change handoff acceptance): re-verified at HEAD by re-reading `BurnieCoin.sol` — confirm change confined to `decimatorBurn` lvl keying with no new mint site and no supply-conservation impact.

**Step 7 — Write `235-02-AUDIT.md` with the required structure.** Required sections in this exact order:

```markdown
# CONS-02 Analytical Audit: BURNIE Conservation Re-Proof (v29.0 Delta at HEAD 1646d5af)

**Scope:** Every BURNIE mint and burn site across the 10-commit v29.0 delta + 2 post-Phase-230 addendum commits (`314443af`, `c2e5e0a9`) + 232.1 fix series (`432fb8f9`, `d09e93ec`, `749192cd`, `26cea00b`). Anchored in `230-01-DELTA-MAP.md` §1.4 + §1.7 + §1.8 + §2.4 + §4 Consumer Index CONS-02 row.
**Method:** Fresh re-prove at HEAD `1646d5af` per D-03. Cross-cite prior-phase verdicts (232-01, 234-01) per D-04 with `re-verified at HEAD 1646d5af` note.
**Verdict vocabulary:** `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`
**Finding-ID policy:** No `F-29-NN` IDs emitted (per D-14) — Phase 236 FIND-01 owns ID assignment.
**Scope-guard policy:** Per D-15 — any gap recorded in 235-02-SUMMARY.md, not catalog.

## Methodology

[Paragraph: Per-Mint-Site + Per-Burn-Site Catalog enumeration method; `mintForGame` gateway invariant; Quest Credit Algebra for the `_purchaseFor` -> `handlePurchase` chain; 232.1 Ticket-Processing Impact sub-section per D-06; cross-cite format per D-04.]

## Findings-Candidate Block

[Prose, no F-29-NN IDs. Expected: "No on-chain candidate findings — BURNIE conservation closes cleanly at HEAD 1646d5af." If any site bypasses `mintForGame` or any burn misaccounts, record as VULNERABLE Finding Candidate: Y.]

## Per-Mint-Site Catalog

| Site | File:Line | Gateway | Amount Source | Guard | Verdict | Finding Candidate |
|---|---|---|---|---|---|---|
| [one row per BURNIE mint site at HEAD — expected ~2-5 rows: `mintForGame` gateway + callers in MintModule + any constructor-time mint] | ... | ... | ... | ... | ... | ... |

## Per-Burn-Site Catalog

| Site | File:Line | Burn Mechanism | Caller Guard | Amount Source | Verdict | Finding Candidate |
|---|---|---|---|---|---|---|
| `BurnieCoin.decimatorBurn(player, amount)` | contracts/BurnieCoin.sol:<line> | `_burn(player, amount)` ERC20 internal | [gating modifier] | caller-supplied `amount` via `DegenerusVault` or equivalent path | [verdict] | [Y/N] |
| [any additional burn sites — expected zero beyond decimatorBurn in the v29.0 delta] | ... | ... | ... | ... | ... | ... |

## Quest Credit Algebra

### `_purchaseFor` -> `handlePurchase` Chain (d5284be5 boundary)
[Prose algebra: `burnieMintUnits` returned by `_callTicketPurchase` flows to `_purchaseFor` -> (1) `quests.handlePurchase(..., burnieMintQty, ...)` for quest accumulator update, (2) `burnie.mintForGame(buyer, burnieMintUnits)` for actual BURNIE mint. Single source value, two consumers, no double-credit or missing-credit. Cite File:Line for each hop.]

### BURNIE Quest Accumulator Updates
[Enumerate every BURNIE-affecting quest slot in `DegenerusQuests.handlePurchase` body. For each slot, cite the accumulator write + confirm the delta equals `burnieMintQty`. Confirm no new BURNIE accumulator path added by d5284be5 — only the `ethMintQty -> ethFreshWei` (ETH unit) switch.]

### `d5284be5` Non-Overlap Confirmation
[Explicit statement: `d5284be5` changes the `ethMintQty -> ethFreshWei` parameter (ETH unit, 1:1 wei credit for MINT_ETH quest). The `burnieMintQty` parameter is UNCHANGED by this commit. BURNIE conservation invariant unaffected.]

## 232.1 Ticket-Processing Impact

[Mandatory per D-06. Walk each 232.1 fix series change against CONS-02 as detailed in Step 5 above. Must state explicitly: "232.1 fix series introduces zero new BURNIE mint sites and zero new BURNIE burn sites. BURNIE supply conservation invariant UNCHANGED by the series."]

## 230-02 Addendum Impact

[Addendum commits 314443af + c2e5e0a9 are entropy-derivation fixes (keccak-seed for `_raritySymbolBatch` + XOR→keccak for 17 entropy-mixing sites). Zero BURNIE mint/burn interaction in either commit. CONS-02 surface unchanged.]

## Cross-Cited Prior-Phase Verdicts (per D-04 format, re-verified at HEAD 1646d5af)

| Cited From | Verdict | Re-Verify Evidence at HEAD 1646d5af |
|---|---|---|
| 232-01-AUDIT DCM-01: BURNIE sum-in/sum-out handoff acceptance | PASS | [re-read `BurnieCoin.decimatorBurn` body — confirm only lvl keying + 2 comment lines changed; burn mechanism unchanged] |
| 234-01-AUDIT QST-01: `mint_ETH` quest wei credit (ETH unit, non-overlap with BURNIE) | PASS | [re-read `DegenerusQuests.handlePurchase` body — confirm `ethMintQty -> ethFreshWei` is ETH parameter only; `burnieMintQty` path unchanged] |
| 234-01-AUDIT QST-02: `boonPacked` exposure (storage-read accessor, no BURNIE delta) | PASS | [re-read `DegenerusGameStorage.boonPacked` mapping decl — confirm `public` visibility; no mint/burn introduced] |
| 234-01-AUDIT QST-03: `BurnieCoin.sol` change handoff acceptance | PASS | [re-read `contracts/BurnieCoin.sol` — confirm change confined to decimator-burn-key plumbing; no new mint site; no supply impact] |

## Scope-guard Deferrals

[Per D-15. If none, state "None surfaced during this audit."]

## Downstream Hand-offs

- **Phase 236 FIND-01:** Every VULNERABLE / DEFERRED / SAFE-INFO Finding Candidate: Y row routes here for ID assignment + severity classification.
- **Phase 236 REG-01:** BURNIE conservation regression cross-check against prior milestones — CONS-02 confirms no regression introduced by the delta.
- **Phase 235-01 CONS-01:** ETH conservation — sibling plan; no overlap (BURNIE is separate namespace).
- **Phase 235-05 TRNX-01:** Phase-transition rngLocked invariant — CONS-02 records "no new BURNIE mint site introduced by 2471f8e7".
```

Every verdict row MUST:
- Use locked column set per catalog.
- Cite real File:Line at HEAD `1646d5af`.
- Use verdict vocabulary strictly from `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`.
- Carry `Y` or `N` in Finding Candidate column.
- Never reference F-29-NN IDs.

Read contract sources ONLY from `contracts/` directory at HEAD per `feedback_contract_locations.md`.

READ-only per D-17. Only writes: `235-02-AUDIT.md` and (at output-step) `235-02-SUMMARY.md`. `git status --porcelain contracts/ test/` MUST be empty before/after task.
  </action>
  <verify>
    <automated>test -f .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-02-AUDIT.md && grep -q "## Per-Mint-Site Catalog" .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-02-AUDIT.md && grep -q "## Per-Burn-Site Catalog" .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-02-AUDIT.md && grep -q "## 232.1 Ticket-Processing Impact" .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-02-AUDIT.md && ! grep -q "F-29-" .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-02-AUDIT.md && test -z "$(git status --porcelain contracts/ test/)"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-02-AUDIT.md` exists
    - File contains the literal header strings: `## Per-Mint-Site Catalog`, `## Per-Burn-Site Catalog`, `## Quest Credit Algebra`, `## 232.1 Ticket-Processing Impact`, `## 230-02 Addendum Impact`, `## Cross-Cited Prior-Phase Verdicts`, `## Findings-Candidate Block`, `## Scope-guard Deferrals`, `## Downstream Hand-offs`
    - Per-Mint-Site Catalog has at least 2 rows (gateway + at least one caller; expected ~2-5)
    - Per-Burn-Site Catalog has at least 1 row (`BurnieCoin.decimatorBurn`)
    - Every row in both catalogs cites a File:Line anchor in `contracts/` starting with `contracts/BurnieCoin.sol:` or `contracts/modules/DegenerusGameMintModule.sol:` or `contracts/DegenerusQuests.sol:`
    - Every row's Verdict is exactly `SAFE`, `SAFE-INFO`, `VULNERABLE`, or `DEFERRED`
    - Every row's Finding Candidate is `Y` or `N`
    - `grep -c "1646d5af" .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-02-AUDIT.md` returns at least 5 (header + 4 cross-cites)
    - `grep -c "re-verified at HEAD 1646d5af" .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-02-AUDIT.md` returns at least 4
    - Cross-Cited Prior-Phase Verdicts table has at least 4 rows: 232-01 DCM-01, 234-01 QST-01, 234-01 QST-02, 234-01 QST-03
    - 232.1 Ticket-Processing Impact sub-section explicitly states "zero new BURNIE mint sites" (or equivalent exact phrase)
    - Quest Credit Algebra sub-section explicitly states the non-overlap between `d5284be5` (ETH unit change) and BURNIE (`burnieMintQty` unchanged)
    - The string `F-29-NN` does NOT appear in the file (per D-14)
    - The string `F-29-` does NOT appear in any form in the file (per D-14)
    - No placeholder line numbers like `:<line>` remain in the final file
    - Downstream Hand-offs names `Phase 236 FIND-01`, `Phase 236 REG-01`, `Phase 235-01 CONS-01`, `Phase 235-05 TRNX-01`
    - `git status --porcelain contracts/ test/` returns empty both before and after task (per D-17)
    - Frontmatter `head_sha: 1646d5af` is the locked audit baseline per D-05; verified via `git diff --stat 1646d5af..HEAD -- contracts/ test/` returning empty output (zero contract/test drift from baseline to current HEAD)
  </acceptance_criteria>
  <deliverable>
`235-02-AUDIT.md` with Per-Mint-Site + Per-Burn-Site Catalogs + Quest Credit Algebra + mandatory 232.1 Ticket-Processing Impact + 230-02 Addendum Impact + Cross-Cited Prior-Phase Verdicts (re-verified at HEAD 1646d5af per D-04) + Findings-Candidate Block + Scope-guard Deferrals + Downstream Hand-offs. Zero F-29-NN. Zero contracts/ or test/ writes.
  </deliverable>
  <done>`235-02-AUDIT.md` exists; every mint site routes through `mintForGame`; every burn accounts; quest credit algebra closes; 232.1 sub-section confirms zero new mint/burn sites; 4 cross-cites re-verified at HEAD 1646d5af per D-04.</done>
</task>

<task type="auto">
  <name>Task 2: Commit approved 235-02-AUDIT.md</name>
  <files>.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-02-AUDIT.md</files>
  <read_first>
    - .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-02-AUDIT.md (must exist from Task 1)
  </read_first>
  <action>
Stage and commit `235-02-AUDIT.md` only.

```
git add .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-02-AUDIT.md
git commit -m "docs(235-02): CONS-02 BURNIE conservation re-proof at HEAD 1646d5af

Analytical re-proof over v29.0 delta + 230-02 addendum + 232.1 fix series.
Per-Mint-Site Catalog confirms every BURNIE creation flows through
BurnieCoin.mintForGame gateway. Per-Burn-Site Catalog enumerates every burn
path (primarily BurnieCoin.decimatorBurn from 3ad0f8d3 MODIFIED).

Quest Credit Algebra walks _purchaseFor -> handlePurchase -> burnieMintQty
accumulator chain; confirms d5284be5 changes only ETH parameter
(ethMintQty -> ethFreshWei), NOT BURNIE (burnieMintQty unchanged).

232.1 Ticket-Processing Impact sub-section confirms zero new BURNIE mint
sites and zero new BURNIE burn sites introduced by pre-finalize gate +
queue-length gate + nudged-word write + do-while integration + game-over
drain + RngNotReady selector fix.

Cross-cites 232-01 DCM-01 + 234-01 QST-01/02/03 re-verified at HEAD per D-04.

Zero F-29-NN emissions (Phase 236 FIND-01 owns per D-14).
Zero contracts/ or test/ writes (READ-only per D-17)."
```

Do NOT push.
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -qE "235-02.*CONS-02.*1646d5af" && test -z "$(git status --porcelain contracts/ test/)"</automated>
  </verify>
  <acceptance_criteria>
    - One new commit with subject starting `docs(235-02):` containing `CONS-02` + `1646d5af`
    - Commit touches only `.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-02-AUDIT.md`
    - `git status --porcelain contracts/ test/` empty
    - Not pushed
  </acceptance_criteria>
  <deliverable>
Approved 235-02-AUDIT.md committed; no code mods; no push.
  </deliverable>
  <done>Commit exists with correct format; contracts/ test/ unmodified; local-only.</done>
</task>

</tasks>

<verification>
- `235-02-AUDIT.md` exists with Per-Mint-Site Catalog confirming every BURNIE creation flows through `mintForGame`
- Per-Burn-Site Catalog enumerates every burn path
- Quest Credit Algebra sub-section walks `_purchaseFor` -> `handlePurchase` -> `burnieMintQty` chain with explicit non-overlap to d5284be5 ETH-unit change
- Mandatory `## 232.1 Ticket-Processing Impact` sub-section present explicitly confirming zero new mint/burn sites from the fix series
- `## 230-02 Addendum Impact` sub-section explicitly confirming addendum commits are entropy-only
- 4 cross-cites (232-01 DCM-01, 234-01 QST-01/02/03) each with `re-verified at HEAD 1646d5af` per D-04
- Every File:Line anchor resolves into `contracts/` at HEAD
- Verdicts use `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`
- Every row has `Y` or `N` Finding Candidate
- Findings-Candidate / Scope-guard Deferrals / Downstream Hand-offs present
- Downstream Hand-offs names Phase 236 FIND-01 + 236 REG-01 + sibling plans CONS-01 + TRNX-01
- Zero `F-29-NN` or `F-29-` references (per D-14)
- Zero `contracts/` or `test/` writes (per D-17)
- Approved AUDIT.md committed with message `docs(235-02): CONS-02 BURNIE conservation re-proof at HEAD 1646d5af`
</verification>

<success_criteria>
Every BURNIE mint site across the v29.0 delta + addendum + 232.1 fix series is confirmed to route through `BurnieCoin.mintForGame`. Every burn site is enumerated and verified to decrement balance. Quest Credit Algebra closes — `burnieMintQty` passed to `handlePurchase` equals the value minted via `mintForGame`; no double-credit, no missing-credit. `d5284be5` is confirmed NON-OVERLAPPING with BURNIE conservation (ETH parameter change only). Mandatory 232.1 Ticket-Processing Impact (D-06) explicitly confirms zero new BURNIE mint/burn sites. Cross-cites (232-01, 234-01) each re-verified at HEAD `1646d5af` per D-04. Zero F-29-NN per D-14. Zero contracts/ or test/ writes per D-17.
</success_criteria>

<output>
After completion, create `.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-02-SUMMARY.md`
</output>
</content>
