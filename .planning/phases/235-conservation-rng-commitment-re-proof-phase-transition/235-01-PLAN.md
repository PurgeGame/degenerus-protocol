---
phase: 235-conservation-rng-commitment-re-proof-phase-transition
plan: 235-01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-01-AUDIT.md
autonomous: true
requirements: [CONS-01]
head_sha: 1646d5af
must_haves:
  truths:
    - "Every pool-mutating SSTORE site identified in 230-01-DELTA-MAP.md §1 that touches `currentPrizePool` / `nextPrizePool` / `futurePrizePool` / `claimablePool` / `decimatorPool` (encoded in `prizePoolsPacked` / `claimableWinnings` / `decimatorPool` storage slots) has a row in the Per-SSTORE Catalog table with columns `Site | File:Line | Pool | Direction | Guard | Mutation | Verdict`"
    - "Every path endpoint for ETH flows in the v29.0 delta has an algebraic sum-before = sum-after proof — specifically: `_consolidatePoolsAndRewardJackpots` (3ad0f8d3 consolidated block), `_finalizeEarlybird` (f20a2b5e new function — external `dgnrs.transferBetweenPools`), `_runEarlyBirdLootboxJackpot` (20a951df futurePool → nextPool), `runBafJackpot` (104b5d42 — emit-only, zero SSTORE delta), `runDecimatorJackpot` (3ad0f8d3 consolidated self-call), `claimDecimatorJackpot` + `claimTerminalDecimatorJackpot` (67031e7d post-SSTORE emit), MintModule `recordMint` + `_purchaseFor` + `_callTicketPurchase` (f20a2b5e + d5284be5)"
    - "The AUDIT.md output contains a named `## 232.1 Ticket-Processing Impact` sub-section (per D-06) walking the 232.1 fix series changes (pre-finalize gate at AdvanceModule:257-263, queue-length gate, nudged-word write via `rngWordCurrent + totalFlipReversals`, do-while integration with `stage = STAGE_TICKETS_WORKING; break;`, game-over best-effort drain in `_handleGameOverPath`, `RngNotReady` selector at L207 + L263) against CONS-01 — specifically proving the pre-drain gate + ticket-processing buffer-swap does NOT create any pool-SSTORE delta not already catalogued and does NOT allow ETH to leak into or out of ticket-state storage during the rngLocked window"
    - "The AUDIT.md output cross-cites (per D-04 format) the following prior-phase verdicts with a `re-verified at HEAD 1646d5af` note per cited row: 231-01-AUDIT EBD-01 `recordMint` award-block removal; 231-02-AUDIT EBD-02 futurePool → nextPool; 231-03-AUDIT EBD-03 orthogonal storage namespaces (DGNRS pool vs ETH futurePrizePool); 232-01-AUDIT DCM-01 decPool consolidated-block x00/x5 mutual exclusivity + `decPoolWei` determinism"
    - "Every verdict row uses vocabulary exactly `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (Phase 233 precedent)"
    - "Every verdict row carries a `Finding Candidate: Y/N` column value"
    - "No F-29-NN finding IDs appear anywhere in 235-01-AUDIT.md (per D-14)"
    - "Scope-guard deferrals, if any, recorded in 235-01-SUMMARY.md per D-15 (230-01-DELTA-MAP.md and 230-02-DELTA-ADDENDUM.md NOT edited)"
    - "Findings-Candidate Block, Scope-guard Deferrals, and Downstream Hand-offs subsections present; Phase 236 FIND-01 hand-off explicitly named for ID assignment"
    - "Zero contracts/ and zero test/ writes occur during plan execution (READ-only per D-17)"
  artifacts:
    - path: ".planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-01-AUDIT.md"
      provides: "CONS-01 ETH conservation proof across v29.0 delta"
      contains: "## Per-SSTORE Catalog"
  key_links:
    - from: "230-01-DELTA-MAP.md §1.1 / §1.2 / §1.3 / §1.4 / §1.6 (pool-SSTORE-producing functions) + §2 (all pool-SSTORE-producing chains) + §4 Consumer Index CONS-01 row"
      to: "235-01-AUDIT.md Per-SSTORE Catalog table rows"
      via: "Every pool-touching SSTORE site owned by the 10-commit delta + 232.1 fix series gets a catalog row"
      pattern: "currentPrizePool|nextPrizePool|futurePrizePool|claimablePool|decimatorPool|prizePoolsPacked|claimableWinnings"
    - from: "235-01-AUDIT.md VULNERABLE/DEFERRED/SAFE-INFO Candidate: Y verdicts"
      to: "Phase 236 FIND-01 finding-candidate pool"
      via: "Findings-Candidate Block (no F-29-NN IDs, Finding Candidate: Y/N column)"
      pattern: "## Findings-Candidate Block"
    - from: "235-01-AUDIT.md algebraic proof sub-sections (sum-before = sum-after per endpoint)"
      to: "Phase 236 FIND-01 ETH conservation closure"
      via: "Per-path algebraic proof — any gap recorded as Finding Candidate: Y"
      pattern: "sum-before|sum-after|algebraic|conservation"
---

<objective>
CONS-01 analytical re-proof of ETH conservation across the full v29.0 delta (10 contract-touching commits 14cb45e1..HEAD + 2 post-Phase-230 addendum commits `314443af` + `c2e5e0a9` + the 232.1 fix series of 4 commits `432fb8f9` / `d09e93ec` / `749192cd` / `26cea00b`). Produces `235-01-AUDIT.md`: a per-SSTORE catalog table covering every new or modified SSTORE site touching `currentPrizePool` / `nextPrizePool` / `futurePrizePool` / `claimablePool` / `decimatorPool` (encoded via `prizePoolsPacked` / `claimableWinnings` / `decimatorPool` in `contracts/storage/DegenerusGameStorage.sol`), a per-path algebraic proof (sum-before = sum-after at every endpoint), a named `## 232.1 Ticket-Processing Impact` sub-section walking the 232.1 fix series against CONS-01, cross-cites to prior-phase verdicts (231-01 EBD-01 / 231-02 EBD-02 / 231-03 EBD-03 / 232-01 DCM-01) each re-verified at HEAD `1646d5af`, Scope-guard Deferrals, Downstream Hand-offs, and a Findings-Candidate Block.

Purpose: ETH conservation is the milestone-wide invariant that a C4A warden attacks first — the sum of `currentPrizePool` + `nextPrizePool` + `futurePrizePool` + `claimablePool` + `decimatorPool` after every in-scope function returns must equal the sum at entry plus net ETH ingress (via `msg.value` on payable entry points) minus net ETH egress (via `_creditClaimable` → `msg.sender` or via `dgnrs.transferBetweenPools` / `dgnrs.transferFromPool` on the DGNRS external contract). Phase 231 EBD-01/02/03 verified the earlybird path; Phase 232 DCM-01 verified the decimator path; Phase 233 JKP-01/02/03 verified the jackpot path; Phase 234 verified quest paths. Phase 235 CONS-01 RE-PROVES the algebraic closure at the MILESTONE level at HEAD `1646d5af` (per D-03 fresh re-prove + D-04 cross-cite with re-verify). The 232.1 fix series added new control flow inside the rngLocked window (pre-finalize gate + ticket-drain-before-swap) — CONS-01 must prove that no new SSTORE writes ETH into or out of pool state during the packed housekeeping window that isn't already catalogued. Per D-14, no F-29-NN IDs are emitted; Phase 236 FIND-01 owns canonical ID assignment. Per D-17, READ-only scope: only write is `235-01-AUDIT.md` itself.

Output: `235-01-AUDIT.md` with Per-SSTORE Catalog + Per-Path Algebraic Proofs + 232.1 Ticket-Processing Impact sub-section + Findings-Candidate Block + Scope-guard Deferrals + Downstream Hand-offs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-CONTEXT.md
@.planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md
@.planning/phases/230-delta-extraction-scope-map/230-02-DELTA-ADDENDUM.md
@.planning/phases/230-delta-extraction-scope-map/230-01-SUMMARY.md
@.planning/phases/231-earlybird-jackpot-audit/231-01-AUDIT.md
@.planning/phases/231-earlybird-jackpot-audit/231-02-AUDIT.md
@.planning/phases/231-earlybird-jackpot-audit/231-03-AUDIT.md
@.planning/phases/232-decimator-audit/232-01-AUDIT.md
@.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-01-FIX.md
@.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the CONS-01 Per-SSTORE Catalog + Algebraic Proofs at HEAD 1646d5af; write 235-01-AUDIT.md</name>
  <files>.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-01-AUDIT.md</files>
  <read_first>
    - .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-CONTEXT.md (D-01 through D-17 — specifically D-01 5-plan shape / D-02 Wave 1 parallel / D-03 fresh re-prove + cross-cite / D-04 cite + re-verify at HEAD format / D-05 HEAD anchor `1646d5af` locked in frontmatter / D-06 mandatory `## 232.1 Ticket-Processing Impact` sub-section per-requirement / D-14 no F-29-NN IDs / D-15 scope-guard deferral recorded in SUMMARY not catalog / D-17 READ-only zero contracts/ or test/ writes)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.1 (AdvanceModule function-level changelog — `advanceGame` MODIFIED by 2471f8e7 + 52242a10; `_consolidatePoolsAndRewardJackpots` MODIFIED by 3ad0f8d3; `_finalizeRngRequest` MODIFIED by f20a2b5e; `_finalizeEarlybird` NEW by f20a2b5e — external dgnrs.transferBetweenPools)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.2 (JackpotModule `_runEarlyBirdLootboxJackpot` MODIFIED by 20a951df — futurePool → nextPool; `runBafJackpot` MODIFIED by 104b5d42 — emit-only zero SSTORE delta; `_setNextPrizePool` / `_setCurrentPrizePool` / `_setFuturePrizePool` residual flow)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.3 (DecimatorModule `claimDecimatorJackpot` + `claimTerminalDecimatorJackpot` MODIFIED by 67031e7d — post-SSTORE emit; in-file read/write of `decBurns` / `decBurnBuckets` / `decPool` via 3ad0f8d3 keying)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.4 (MintModule `processFutureTicketBatch` MODIFIED by 52242a10 — SLOAD removed; `_purchaseFor` MODIFIED by f20a2b5e + d5284be5; `_callTicketPurchase` MODIFIED by f20a2b5e + d5284be5)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.6 (core DegenerusGame.sol `recordMint` MODIFIED by f20a2b5e — trailing earlybird block REMOVED)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §2 (all pool-SSTORE-producing chains; specifically IM-01..IM-05 earlybird, IM-06 decimator consolidated, IM-07 decimator delegatecall, IM-08 terminal decimator wrapper, IM-21 RNG-lock removal, IM-22 entropy passthrough)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §4 Consumer Index CONS-01 row (authoritative scope anchor per D-04 of Phase 235 CONTEXT.md)
    - .planning/phases/230-delta-extraction-scope-map/230-02-DELTA-ADDENDUM.md — c2e5e0a9 entropy-mixing sites are RNG-only (no pool SSTORE); 314443af is seed-derivation-only (no pool SSTORE). Record this in audit: "Addendum commits 314443af + c2e5e0a9 introduce zero new pool-touching SSTORE sites — CONS-01 surface unchanged by the addendum."
    - contracts/storage/DegenerusGameStorage.sol at HEAD — locate `prizePoolsPacked` declaration (slot containing currentPrizePool / nextPrizePool / futurePrizePool packed encoding), `claimableWinnings` mapping decl, `decimatorPool` storage slot. Record exact File:Line of each storage variable. Also locate `_setCurrentPrizePool` / `_setNextPrizePool` / `_setFuturePrizePool` / `_addClaimableEth` / `_creditClaimable` internal helpers — these are the canonical SSTORE writers.
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD — grep for every call to `_setCurrentPrizePool` / `_setNextPrizePool` / `_setFuturePrizePool` / `_addClaimableEth` / `_creditClaimable` inside `advanceGame`, `_consolidatePoolsAndRewardJackpots`, `_finalizeRngRequest`, `_finalizeEarlybird`, `rngGate`. Record File:Line + caller context + pool direction per hit.
    - contracts/modules/DegenerusGameJackpotModule.sol at HEAD — grep for every pool-mutation call inside `_runEarlyBirdLootboxJackpot` (20a951df MODIFIED), `runBafJackpot` (104b5d42 MODIFIED — expected zero SSTORE delta beyond pre-existing flow), `payDailyJackpot`, `_awardFarFutureCoinJackpot`, `_distributePurchasePhaseTickets`, etc. Record File:Line + pool direction.
    - contracts/modules/DegenerusGameDecimatorModule.sol at HEAD — grep for every call that touches `decPool` / `decBurns` / `decBurnBuckets` / `decClaim` storage, and every `_creditClaimable` / `_addClaimableEth` / `_setFuturePrizePool` call inside `claimDecimatorJackpot` (67031e7d MODIFIED), `claimTerminalDecimatorJackpot` (67031e7d + 858d83e4 via IM-08), `runDecimatorJackpot`, `runTerminalDecimatorJackpot`, `_creditDecJackpotClaimCore`, `_consumeDecClaim`, `_consumeTerminalDecClaim`. Record File:Line + pool direction.
    - contracts/modules/DegenerusGameMintModule.sol at HEAD — grep for every pool-mutation and `msg.value`-ingress site inside `_purchaseFor`, `_callTicketPurchase`, `processTicketBatch`, `processFutureTicketBatch`, `recordMint`, `_rollRemainder`. Record File:Line + pool direction.
    - contracts/DegenerusGame.sol at HEAD — grep for every pool-mutation call in `recordMint` (f20a2b5e — trailing earlybird block REMOVED; body now terminates at `_recordMintDataModule`), `claimTerminalDecimatorJackpot` (858d83e4 NEW wrapper — delegatecalls module, zero direct SSTORE). Record File:Line + pool direction.
    - contracts/BurnieCoin.sol at HEAD — grep for `msg.value` / ETH ingress (expected: none — BurnieCoin is BURNIE-only for CONS-01 scope). Record the absence of ETH flow as a zero-row confirmation. (BURNIE conservation is Plan 235-02 scope — out of CONS-01 scope.)
    - .planning/phases/231-earlybird-jackpot-audit/231-01-AUDIT.md (EBD-01 21 PASS verdicts at HEAD at audit time — re-verify each `recordMint` award-block removal assertion at HEAD `1646d5af` per D-04 by re-reading `contracts/DegenerusGame.sol` `recordMint` body and confirming the earlybird block is still absent; re-verify the `_purchaseFor` unified `_awardEarlybirdDgnrs(buyer, ticketFreshEth + lootboxFreshEth)` call at MintModule.sol body is still present and single-instance)
    - .planning/phases/231-earlybird-jackpot-audit/231-02-AUDIT.md (EBD-02 6 PASS verdicts — re-verify futurePool → nextPool budget conservation in `_runEarlyBirdLootboxJackpot` at HEAD by re-reading JackpotModule `_runEarlyBirdLootboxJackpot` body; confirm `totalBudget` local is debited from futurePool and credited to nextPool with no mutation in between)
    - .planning/phases/231-earlybird-jackpot-audit/231-03-AUDIT.md (EBD-03 13 PASS verdicts — re-verify orthogonal storage namespaces: `_finalizeEarlybird` moves DGNRS tokens in `StakedDegenerusStonk` contract — EXTERNAL namespace — while `_runEarlyBirdLootboxJackpot` operates on ETH-side `futurePrizePool` in DegenerusGameStorage — INTERNAL namespace; confirm via re-read at HEAD)
    - .planning/phases/232-decimator-audit/232-01-AUDIT.md (DCM-01 23 verdict rows — re-verify `_consolidatePoolsAndRewardJackpots` consolidated block x00/x5 mutual exclusivity + `decPoolWei` determinism at HEAD by re-reading AdvanceModule `_consolidatePoolsAndRewardJackpots` body; confirm `if (decPoolWei != 0)` tail correctly skipped when neither branch fires)
    - .planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-01-FIX.md (pre-finalize gate diff + 4 revisions; final Revision-2 shape at AdvanceModule:257-279 — read the gate body at HEAD to confirm no new SSTORE writes `prizePoolsPacked` / `claimableWinnings` / `decimatorPool`; the gate writes only `lootboxRngWordByIndex[idx]` via `_finalizeLootboxRng(cw)` — OUT of CONS-01 pool-SSTORE scope)
    - .planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-CONTEXT.md (D-01 fix mechanism; D-02 selection rationale; D-05 game-over path isolation)
    - Output of `git rev-parse --short HEAD` — must match the frontmatter `head_sha: 1646d5af` value at audit time
    - Output of `git show 1646d5af --stat` to confirm the HEAD commit identity and verify no in-progress `contracts/` or `test/` modifications exist
  </read_first>
  <action>
Execute a fresh-from-HEAD CONS-01 ETH conservation re-proof at HEAD `1646d5af` per CONTEXT.md D-01 through D-17. Verdict vocabulary per Phase 233 precedent is strictly `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`. Column schema follows Phase 233 precedent with a `Finding Candidate: Y/N` column.

**Step 1 — Enumerate every pool-mutating SSTORE site.** Grep `contracts/` at HEAD `1646d5af` for:
  - Every call to `_setCurrentPrizePool` / `_setNextPrizePool` / `_setFuturePrizePool` / `_addClaimableEth` / `_creditClaimable` across all in-scope modules.
  - Every direct SSTORE to `prizePoolsPacked` (the packed encoding of currentPrizePool / nextPrizePool / futurePrizePool in DegenerusGameStorage).
  - Every write to `claimableWinnings[player]` mapping.
  - Every write to `decimatorPool` storage slot + every write to `decPool` / `decBurns[lvl]` / `decBurnBuckets[lvl]` / `decClaim[player][lvl]` (the decimator-side pool state).
  - Every `msg.value` ingress on payable external entry points: `DegenerusGame.advanceGame`, `DegenerusGame.recordMint` (payable), `_purchaseFor` ingress via payable wrappers, etc.
  - Every external ETH egress: `msg.sender.call{value: ...}` patterns, `_creditClaimable` → later `withdrawClaimable` paths, `dgnrs.transferBetweenPools` / `dgnrs.transferFromPool` (external to CONS-01 scope — DGNRS namespace, not ETH-pool namespace).

**Step 2 — Classify each SSTORE site in the Per-SSTORE Catalog table.** Columns:
  `Site | File:Line | Pool | Direction | Guard | Mutation | Verdict | Finding Candidate`
  - **Site:** function name + path (e.g. `AdvanceModule._consolidatePoolsAndRewardJackpots`).
  - **File:Line:** absolute anchor into `contracts/` at HEAD.
  - **Pool:** one of `currentPrizePool` / `nextPrizePool` / `futurePrizePool` / `claimablePool` / `decimatorPool`.
  - **Direction:** `IN` (pool gains ETH) / `OUT` (pool loses ETH) / `TRANSFER` (one pool → another, net zero).
  - **Guard:** the predicate gating the SSTORE (e.g. `decPoolWei != 0`, `prevMod100 == 0`, `rngLocked == true`).
  - **Mutation:** the algebraic change (e.g. `currentPrizePool -= spend`, `nextPrizePool += totalBudget`).
  - **Verdict:** `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`.
  - **Finding Candidate:** `Y` or `N`.
  Minimum row count: one row per distinct SSTORE site owned by any of the 10 in-scope commits + 232.1 fix series. Expected row count based on §1 function-level changelog: ~20-35 rows covering the earlybird + decimator + jackpot + mint + quest-credit paths.

**Step 3 — Write per-path algebraic proofs.** Group SSTORE sites into execution paths and prove sum-before = sum-after for each:
  - **Path A — Earlybird purchase (f20a2b5e):** Entry `_purchaseFor(buyer, ...)` with `msg.value = costWei`. Sum-in: `costWei` (external ETH ingress). Sum-out: `currentPrizePool += costWei_portion_1` + `nextPrizePool += costWei_portion_2` + `futurePrizePool += costWei_portion_3` + `claimablePool += freshEth_portion` (split per the ticket/lootbox accounting). Unified `_awardEarlybirdDgnrs(buyer, ticketFreshEth + lootboxFreshEth)` moves DGNRS in external namespace (no ETH delta). Proof: `sum_before + costWei == sum_after`.
  - **Path B — Earlybird jackpot resolution (20a951df):** Entry `_runEarlyBirdLootboxJackpot(lvl, rngWord)`. Sum-in: 0 (no external ingress). `totalBudget` local is debited from `futurePrizePool` at function start; credited to `nextPrizePool` at function end. CEI: no mutation between debit and credit. Proof: `futurePool_before + nextPool_before = futurePool_after + nextPool_after` (total conserved across the two pools).
  - **Path C — Decimator consolidated block (3ad0f8d3):** Entry `_consolidatePoolsAndRewardJackpots(lvl, prevMod100, prevMod10, rngWord, ...)`. x00 branch: `decPoolWei` assigned from `decPool` slot; `runDecimatorJackpot(decPoolWei, lvl, rngWord)` self-call credits `_addClaimableEth` / updates `decimatorPool`. x5 branch: same shape. Disjoint branches (mutual exclusivity proven in 232-01-AUDIT DCM-01). Proof: `decimatorPool_before = decimatorPool_after + claimable_delta` (pool reduces by exactly the amount that flows to claimable).
  - **Path D — Decimator claim (67031e7d — emit-only):** `claimDecimatorJackpot(lvl)` at DecimatorModule:313 / :343-349 — emit sites AFTER `_creditClaimable` / `_setFuturePrizePool`. Zero new SSTORE introduced by 67031e7d beyond pre-existing flow (232-02-AUDIT DCM-02 verified). Proof: pool delta governed by pre-existing `runDecimatorJackpot` math; emit adds no mutation.
  - **Path E — Terminal decimator claim (858d83e4 wrapper + 67031e7d emit):** `claimTerminalDecimatorJackpot()` delegatecalls module via IM-08. `_consumeTerminalDecClaim` one-shot-consume at DecimatorModule:880 (`e.weightedBurn = 0`); `_creditClaimable(msg.sender, amountWei)` at DecimatorModule:814. Proof: `terminalDecClaimable_before == amountWei`; `terminalDecClaimable_after == 0`; `claimablePool_delta == +amountWei`.
  - **Path F — Jackpot-phase BAF (104b5d42):** `runBafJackpot(poolWei, lvl, rngWord)` — emit sites tag with `BAF_TRAIT_SENTINEL = 420`; zero new SSTORE beyond pre-existing `_addClaimableEth` / `_awardJackpotTickets` / `_queueWhalePassClaimCore` pre-fix math (233-01-AUDIT JKP-01 verified). Proof: pool delta governed by pre-existing flow.
  - **Path G — Entropy passthrough (52242a10):** `processFutureTicketBatch(lvl, entropy)` — signature bump only; body SLOAD removed. Zero SSTORE delta introduced (233-02-AUDIT JKP-02 verified). Proof: pool state untouched by passthrough refactor.
  - **Path H — Phase-transition RNG lock removal (2471f8e7):** `advanceGame` at L425 `_unlockRng(day)` call REMOVED. Zero SSTORE delta in the housekeeping window for pool state (verified structurally — `_unlockRng` writes `rngLocked` storage only, not pool state). 235-05-AUDIT TRNX-01 owns the rngLocked invariant proof; CONS-01 records "no pool SSTORE introduced or eliminated by 2471f8e7".
  - **Path I — Quest wei credit (d5284be5):** `_purchaseFor` calls `quests.handlePurchase(..., ethFreshWei, ...)` with wei-direct credit. Inside `DegenerusQuests.handlePurchase`, the `mint_ETH` quest accumulator updates use BURNIE — NOT ETH (234-01-AUDIT QST-01 verified). CONS-01 records "quest accumulator is BURNIE namespace, not ETH pool — no ETH delta introduced".
  - **Path J — 232.1 pre-finalize gate (432fb8f9 / d09e93ec / 749192cd / 26cea00b):** Gate writes `lootboxRngWordByIndex[idx]` (RNG namespace, not pool namespace). Zero pool-SSTORE delta. Proof: grep pool-mutation functions inside gate body yields zero matches.
  Each path gets a named sub-section with the algebraic equation and a cite to the backing verdict rows.

**Step 4 — Write the mandatory `## 232.1 Ticket-Processing Impact` sub-section (per D-06).** Walk each 232.1 fix series change against CONS-01:
  - **Pre-finalize gate at AdvanceModule:257-263** — writes only `lootboxRngWordByIndex[idx]` via `_finalizeLootboxRng(cw)`. Reverts `RngNotReady()` on stall. Zero pool SSTORE. No CONS-01 impact beyond confirming the gate is not a pool-touching site.
  - **Queue-length gate** (R2.a Revision 2) — gates the pre-finalize block on `ticketQueue[preRk].length > 0`. Zero pool SSTORE. No CONS-01 impact.
  - **Nudged-word write** (R2.b Revision 2) — `cw = rngWordCurrent; cw += totalFlipReversals;` then `_finalizeLootboxRng(cw)`. Zero pool SSTORE.
  - **Do-while integration** (R2.c Revision 2) — `stage = STAGE_TICKETS_WORKING; break;` pattern. Zero pool SSTORE. Post-loop emit + creditFlip at L460 — `creditFlip` is coinflip-side, not game pool; confirm by reading `coinflip.creditFlip` signature.
  - **Game-over best-effort drain** (749192cd Rev 3) — extra drain rounds in `_handleGameOverPath`. Verify zero pool SSTORE delta beyond pre-existing `handleGameOverDrain` flow.
  - **RngNotReady selector fix** (26cea00b Rev 4 at L207 + L263) — pure revert-selector correction; zero pool SSTORE delta.
  - **Buffer swap at RNG request time** (per D-12 Phase 235 CONTEXT.md) — buffer swap fires at VRF request, not fulfillment. Writes the buffer index, not pool state. Zero CONS-01 delta.
  Sub-section MUST state explicitly: "232.1 fix series introduces zero new pool-mutating SSTORE sites. ETH conservation invariant UNCHANGED by the series. Per-SSTORE Catalog rows cover only the 10-commit baseline delta plus the two 230-02 addendum commits (both RNG-only)."

**Step 5 — Write cross-cite rows (per D-04 format).** Each cited prior-phase verdict must have the form:
  `[Cited: 231-01-AUDIT EBD-01 Row NN — re-verified at HEAD 1646d5af — evidence re-read at File:Line]`
  Cross-cites required:
  - 231-01-AUDIT EBD-01: `recordMint` award-block removal re-verified at HEAD `1646d5af` (re-read `contracts/DegenerusGame.sol` `recordMint` body; confirm trailing earlybird block absent).
  - 231-02-AUDIT EBD-02: futurePool → nextPool CEI re-verified at HEAD (re-read `_runEarlyBirdLootboxJackpot` body in JackpotModule).
  - 231-03-AUDIT EBD-03: orthogonal storage namespaces re-verified at HEAD (re-read `_finalizeEarlybird` external call pattern).
  - 232-01-AUDIT DCM-01: decPool consolidated block re-verified at HEAD (re-read `_consolidatePoolsAndRewardJackpots` body).

**Step 6 — Write `235-01-AUDIT.md` with the required structure.** Required sections in this exact order:

```markdown
# CONS-01 Analytical Audit: ETH Conservation Re-Proof (v29.0 Delta at HEAD 1646d5af)

**Scope:** Every new or modified SSTORE site touching `currentPrizePool` / `nextPrizePool` / `futurePrizePool` / `claimablePool` / `decimatorPool` across the 10-commit v29.0 delta + 2 post-Phase-230 addendum commits (`314443af`, `c2e5e0a9`) + 232.1 fix series (`432fb8f9`, `d09e93ec`, `749192cd`, `26cea00b`). Anchored in `230-01-DELTA-MAP.md` §1 + §2 + §4 Consumer Index CONS-01 row.
**Method:** Fresh re-prove at HEAD `1646d5af` per D-03. Cross-cite prior-phase verdicts (231/232) per D-04 with `re-verified at HEAD 1646d5af` note.
**Verdict vocabulary:** `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (Phase 233 precedent).
**Finding-ID policy:** No `F-29-NN` IDs emitted (per D-14) — `Finding Candidate: Y/N` column only; Phase 236 FIND-01 owns ID assignment.
**Scope-guard policy:** Any gap requiring edits to `230-01-DELTA-MAP.md` or `230-02-DELTA-ADDENDUM.md` recorded in `235-01-SUMMARY.md` (per D-15); catalog NOT edited in place.

## Methodology

[Paragraph: per-SSTORE catalog + per-path algebraic proof + 232.1 ticket-processing impact sub-section + cross-cite format per D-04; locked verdict vocabulary; locked columns.]

## Findings-Candidate Block

[Prose, no F-29-NN IDs. One block per VULNERABLE / DEFERRED / SAFE-INFO Finding Candidate: Y row. Expected: if conservation closes cleanly, state "No on-chain candidate findings — all ETH-conservation verdicts SAFE against the v29.0 delta at HEAD 1646d5af."]

## Per-SSTORE Catalog

| Site | File:Line | Pool | Direction | Guard | Mutation | Verdict | Finding Candidate |
|---|---|---|---|---|---|---|---|
| [one row per SSTORE site at HEAD — ~20-35 rows expected] | ... | ... | ... | ... | ... | ... | ... |

## Per-Path Algebraic Proofs

### Path A — Earlybird Purchase (f20a2b5e)
[Algebraic equation: `sum_before + costWei == sum_after` with cite to per-SSTORE catalog rows.]

### Path B — Earlybird Jackpot Resolution (20a951df)
[futurePool → nextPool budget conservation proof with cite + 231-02-AUDIT EBD-02 cross-cite re-verified at HEAD 1646d5af.]

### Path C — Decimator Consolidated Block (3ad0f8d3)
[x00 / x5 disjoint branches; decimatorPool → claimablePool flow proof + 232-01-AUDIT DCM-01 cross-cite re-verified at HEAD 1646d5af.]

### Path D — Decimator Claim Emit (67031e7d)
[Emit-only; zero SSTORE delta; pre-existing flow.]

### Path E — Terminal Decimator Claim (858d83e4 + 67031e7d)
[One-shot consume; claimablePool += amountWei.]

### Path F — Jackpot-phase BAF (104b5d42)
[Emit-only sentinel tagging; zero SSTORE delta.]

### Path G — Entropy Passthrough (52242a10)
[Signature bump only; body SLOAD removed; zero pool-state delta.]

### Path H — Phase-Transition RNG Lock Removal (2471f8e7)
[No pool SSTORE introduced/eliminated; rngLocked-namespace write only. TRNX-01 owns the deeper invariant proof — 235-05-AUDIT scope.]

### Path I — Quest Wei Credit (d5284be5)
[BURNIE namespace, not ETH pool. 235-02-AUDIT CONS-02 scope; CONS-01 records non-overlap.]

### Path J — 232.1 Pre-Finalize Gate (432fb8f9 + d09e93ec + 749192cd + 26cea00b)
[Gate writes lootboxRngWordByIndex; zero pool SSTORE. Verified by grep across gate body.]

## 232.1 Ticket-Processing Impact

[Mandatory per D-06. Walk each 232.1 fix series change against CONS-01 as detailed in Step 4 above. Must state explicitly: "232.1 fix series introduces zero new pool-mutating SSTORE sites."]

## 230-02 Addendum Impact

[Addendum commits 314443af + c2e5e0a9 introduce zero new pool-touching SSTORE. `_raritySymbolBatch` keccak-seed fix and EntropyLib.hash2 / 17 entropy-mixing sites are all RNG namespace. CONS-01 surface unchanged.]

## Cross-Cited Prior-Phase Verdicts (per D-04 format, re-verified at HEAD 1646d5af)

| Cited From | Verdict | Re-Verify Evidence at HEAD 1646d5af |
|---|---|---|
| 231-01-AUDIT EBD-01 Row(s): `recordMint` award-block removal | PASS | [re-read `contracts/DegenerusGame.sol` `recordMint` at current line — confirm trailing earlybird block absent] |
| 231-02-AUDIT EBD-02 Row(s): futurePool → nextPool CEI | PASS | [re-read `_runEarlyBirdLootboxJackpot` at JackpotModule — confirm totalBudget debit/credit pattern preserved] |
| 231-03-AUDIT EBD-03 Row(s): orthogonal storage namespaces | PASS | [re-read `_finalizeEarlybird` at AdvanceModule — confirm external dgnrs.transferBetweenPools; no ETH-pool delta] |
| 232-01-AUDIT DCM-01 Row(s): decPool consolidated block x00/x5 mutual exclusivity + decPoolWei determinism | PASS | [re-read `_consolidatePoolsAndRewardJackpots` at AdvanceModule — confirm branch disjointness and tail guard] |

## Scope-guard Deferrals

[Per D-15. If zero deferrals, state "None surfaced during this audit." If any, list concern + why not in CONS-01 scope + recommended receiving phase + recorded in 235-01-SUMMARY.md pending handoff.]

## Downstream Hand-offs

- **Phase 236 FIND-01:** Every VULNERABLE / DEFERRED row + every SAFE-INFO Finding Candidate: Y row routes here for ID assignment + severity classification.
- **Phase 236 REG-01:** Regression cross-check against v25.0 Phase 216 pool-accounting findings — CONS-01 confirms no regression introduced by the delta.
- **Phase 235-02 CONS-02:** BURNIE conservation — sibling plan; no overlap with CONS-01 (BURNIE is separate namespace).
- **Phase 235-05 TRNX-01:** Phase-transition rngLocked invariant — CONS-01 records "no pool SSTORE introduced by 2471f8e7"; TRNX-01 owns the rngLocked semantic proof.
```

Every verdict row MUST:
- Use exactly the locked column set `Site | File:Line | Pool | Direction | Guard | Mutation | Verdict | Finding Candidate`.
- Cite a real File:Line anchor from HEAD `1646d5af` (never `<line>` placeholder).
- Use verdict vocabulary strictly from `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`.
- Carry `Y` or `N` in Finding Candidate column.
- Never reference F-29-NN IDs.

Read contract sources ONLY from `contracts/` directory at HEAD per `feedback_contract_locations.md`.

READ-only constraint per D-17: Zero writes to `contracts/` or `test/`. Only writes allowed are `.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-01-AUDIT.md` and (at output-step) `235-01-SUMMARY.md`. `git status --porcelain contracts/ test/` MUST be empty before and after task execution.
  </action>
  <verify>
    <automated>test -f .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-01-AUDIT.md && grep -q "## Per-SSTORE Catalog" .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-01-AUDIT.md && grep -q "## 232.1 Ticket-Processing Impact" .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-01-AUDIT.md && ! grep -q "F-29-" .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-01-AUDIT.md && test -z "$(git status --porcelain contracts/ test/)"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-01-AUDIT.md` exists
    - File contains the literal header string `## Per-SSTORE Catalog`
    - File contains the literal header string `## Per-Path Algebraic Proofs`
    - File contains the literal header string `## 232.1 Ticket-Processing Impact` (per D-06)
    - File contains the literal header string `## 230-02 Addendum Impact`
    - File contains the literal header string `## Cross-Cited Prior-Phase Verdicts`
    - File contains the literal header string `## Findings-Candidate Block`
    - File contains the literal header string `## Scope-guard Deferrals`
    - File contains the literal header string `## Downstream Hand-offs`
    - Per-SSTORE Catalog table header row is exactly `| Site | File:Line | Pool | Direction | Guard | Mutation | Verdict | Finding Candidate |`
    - Every row in the Per-SSTORE Catalog cites a File:Line anchor starting with `contracts/` (e.g. `contracts/modules/DegenerusGameAdvanceModule.sol:` or `contracts/storage/DegenerusGameStorage.sol:` or `contracts/modules/DegenerusGameDecimatorModule.sol:` or `contracts/DegenerusGame.sol:`)
    - Every row's Verdict cell is exactly one of `SAFE`, `SAFE-INFO`, `VULNERABLE`, or `DEFERRED`
    - Every row's Finding Candidate cell is exactly `Y` or `N`
    - `grep -c "1646d5af" .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-01-AUDIT.md` returns at least 5 (one HEAD anchor in header + 4 cross-cite re-verify notes)
    - `grep -c "re-verified at HEAD 1646d5af" .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-01-AUDIT.md` returns at least 4 (D-04 format: one per cited row minimum)
    - `grep -cE "SAFE|SAFE-INFO|VULNERABLE|DEFERRED" .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-01-AUDIT.md` returns at least 20 (one per SSTORE catalog row minimum)
    - Per-SSTORE Catalog has at least 20 distinct rows covering earlybird + decimator + jackpot + mint + claim paths (minimum; ~20-35 expected)
    - Per-Path Algebraic Proofs section contains at least 10 named sub-sections: Path A Earlybird Purchase, Path B Earlybird Jackpot, Path C Decimator Consolidated, Path D Decimator Claim Emit, Path E Terminal Decimator Claim, Path F Jackpot BAF, Path G Entropy Passthrough, Path H Phase-Transition RNG Lock, Path I Quest Wei, Path J 232.1 Gate
    - 232.1 Ticket-Processing Impact sub-section explicitly states "zero new pool-mutating SSTORE sites" (or equivalent exact phrase)
    - 230-02 Addendum Impact sub-section explicitly states "314443af + c2e5e0a9 introduce zero new pool-touching SSTORE" (or equivalent exact phrase)
    - Cross-Cited Prior-Phase Verdicts table has at least 4 rows: 231-01 EBD-01, 231-02 EBD-02, 231-03 EBD-03, 232-01 DCM-01 — each with re-verify evidence at HEAD 1646d5af
    - The string `F-29-NN` does NOT appear in the file (per D-14)
    - The string `F-29-` does NOT appear in any form in the file (per D-14)
    - No placeholder line numbers like `:<line>` remain in the final file — every File:Line is a real integer or integer range
    - Downstream Hand-offs subsection explicitly names `Phase 236 FIND-01`, `Phase 236 REG-01`, `Phase 235-02 CONS-02`, `Phase 235-05 TRNX-01`
    - `git status --porcelain contracts/ test/` returns empty output both before and after task execution (READ-only per D-17)
    - Frontmatter `head_sha` value `1646d5af` matches current `git rev-parse --short HEAD` at audit time
  </acceptance_criteria>
  <deliverable>
`235-01-AUDIT.md` with full Per-SSTORE Catalog + Per-Path Algebraic Proofs + mandatory 232.1 Ticket-Processing Impact sub-section + 230-02 Addendum Impact sub-section + Cross-Cited Prior-Phase Verdicts (re-verified at HEAD 1646d5af per D-04) + Findings-Candidate Block + Scope-guard Deferrals + Downstream Hand-offs. Zero F-29-NN emissions. Zero contracts/ or test/ writes.
  </deliverable>
  <done>`235-01-AUDIT.md` exists; Per-SSTORE Catalog covers every pool-mutating site in the delta; Per-Path Algebraic Proofs close sum-before = sum-after at every endpoint; 232.1 Ticket-Processing Impact sub-section explicitly confirms zero new pool-SSTORE from the fix series; 4 cross-cites re-verified at HEAD 1646d5af per D-04 format; zero F-29-NN IDs; zero contracts/ or test/ writes.</done>
</task>

<task type="auto">
  <name>Task 2: Commit approved 235-01-AUDIT.md</name>
  <files>.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-01-AUDIT.md</files>
  <read_first>
    - .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-01-AUDIT.md (must exist from Task 1)
  </read_first>
  <action>
Stage and commit `235-01-AUDIT.md` only. Commit message subject `docs(235-01): CONS-01 ETH conservation re-proof at HEAD 1646d5af`. Body explains READ-only audit artifact per v29.0 + D-17; zero `contracts/` or `test/` changes in this commit.

```
git add .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-01-AUDIT.md
git commit -m "docs(235-01): CONS-01 ETH conservation re-proof at HEAD 1646d5af

Analytical re-proof over v29.0 delta + 230-02 addendum + 232.1 fix series.
Per-SSTORE Catalog covers every pool-mutating site touching currentPrizePool /
nextPrizePool / futurePrizePool / claimablePool / decimatorPool. Per-path
algebraic proofs close sum-before = sum-after at every endpoint across 10
named paths (earlybird purchase, earlybird jackpot, decimator consolidated,
decimator claim emit, terminal decimator, BAF, entropy passthrough,
phase-transition rnglock, quest wei, 232.1 pre-finalize gate).

232.1 Ticket-Processing Impact sub-section confirms zero new pool-SSTORE
sites introduced by pre-finalize gate + queue-length gate + nudged-word
write + do-while integration + game-over drain + RngNotReady selector fix.

230-02 Addendum Impact sub-section confirms 314443af + c2e5e0a9 are
RNG-namespace only — zero CONS-01 surface change.

Cross-cites 231-01 EBD-01 / 231-02 EBD-02 / 231-03 EBD-03 / 232-01 DCM-01
re-verified at HEAD 1646d5af per D-04 format.

Zero F-29-NN emissions (Phase 236 FIND-01 owns ID assignment per D-14).
Zero contracts/ or test/ writes (READ-only per D-17)."
```

Do NOT run `git push`.
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -qE "235-01.*CONS-01.*1646d5af" && test -z "$(git status --porcelain contracts/ test/)"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one new commit added via `git log -1`
    - Commit message subject starts with `docs(235-01):` and contains `CONS-01` + `1646d5af`
    - Commit touches exactly `.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-01-AUDIT.md` (no contracts/ or test/ files)
    - `git status --porcelain contracts/ test/` returns empty
    - Commit not pushed
  </acceptance_criteria>
  <deliverable>
Approved 235-01-AUDIT.md committed with `docs(235-01): CONS-01 ...` message; no code modifications; no push.
  </deliverable>
  <done>Commit exists with correct message format; repository contracts/ and test/ remain unmodified; commit is local only.</done>
</task>

</tasks>

<verification>
- `235-01-AUDIT.md` exists with full Per-SSTORE Catalog covering every pool-mutating site in the v29.0 delta (10 commits) + 230-02 addendum (2 commits) + 232.1 fix series (4 commits)
- Per-path algebraic proofs close sum-before = sum-after at 10 named endpoints
- Mandatory `## 232.1 Ticket-Processing Impact` sub-section present (per D-06) explicitly confirming zero new pool-SSTORE sites from the fix series
- `## 230-02 Addendum Impact` sub-section explicitly confirming addendum commits are RNG-namespace only
- 4 cross-cites (231-01 / 231-02 / 231-03 / 232-01) each with `re-verified at HEAD 1646d5af` note per D-04 format
- Every File:Line anchor resolves into `contracts/` at HEAD 1646d5af
- Verdicts exclusively use `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`
- Every row carries `Y` or `N` in Finding Candidate column
- Findings-Candidate / Scope-guard Deferrals / Downstream Hand-offs subsections present
- Downstream Hand-offs explicitly names Phase 236 FIND-01 + Phase 236 REG-01 + sibling plans CONS-02 + TRNX-01
- Zero `F-29-NN` or `F-29-` references anywhere in the file (per D-14)
- Zero writes to `contracts/` or `test/` (per D-17)
- Frontmatter `head_sha` value matches current HEAD
- Approved AUDIT.md committed with message `docs(235-01): CONS-01 ETH conservation re-proof at HEAD 1646d5af`
</verification>

<success_criteria>
Every pool-mutating SSTORE site across the v29.0 delta + addendum + 232.1 fix series has a catalog row with mutation direction, guard, and verdict. Every path endpoint has an algebraic sum-before = sum-after proof. The mandatory 232.1 Ticket-Processing Impact sub-section (D-06) walks each fix-series change against CONS-01 and confirms zero new pool-SSTORE introduced. 4 prior-phase verdicts (231-01 EBD-01, 231-02 EBD-02, 231-03 EBD-03, 232-01 DCM-01) cross-cited per D-04 format with `re-verified at HEAD 1646d5af` note. Scope-guard deferrals (if any) recorded in 235-01-SUMMARY.md per D-15; catalog NOT edited. Zero F-29-NN emissions per D-14. Zero contracts/ or test/ writes per D-17.
</success_criteria>

<output>
After completion, create `.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-01-SUMMARY.md`
</output>
</content>
