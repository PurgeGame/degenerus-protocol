---
phase: 235-conservation-rng-commitment-re-proof-phase-transition
plan: 235-04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-04-AUDIT.md
autonomous: true
requirements: [RNG-02]
head_sha: 1646d5af
must_haves:
  truths:
    - "Every new RNG consumer in the v29.0 delta has a row in the Per-Consumer Commitment-Window Enumeration Table covering 5 consumer categories: (1) earlybird bonus-trait roll (20a951df); (2) BAF sentinel emission (104b5d42); (3) entropy passthrough (52242a10); (4) 17 per-site commitment-window rows for c2e5e0a9 hash2/keccak entropy-mixing sites (per D-08); (5) `_raritySymbolBatch` keccak-seed (314443af per D-09)"
    - "Per D-08: 17 per-site commitment-window rows are present for the c2e5e0a9 entropy-mixing sites. Each row uses columns `Consumer | Inputs | Player-Controllable? | Mutation Between Request-Fulfillment | Non-Influential Proof`. Per-site enumeration — no equivalence-class shortcuts."
    - "Per D-09: a dedicated commitment-window verdict row for `_raritySymbolBatch` keccak-seed (314443af — `baseKey + entropyWord + groupIdx` via `keccak256(abi.encode(...))`) enumerates every input variable and verifies non-influential. Availability CROSS-CITED from 232.1-03-PFTB-AUDIT per D-09."
    - "The AUDIT.md output contains a named `## 232.1 Ticket-Processing Impact` sub-section (per D-06) walking the 232.1 fix series changes against RNG-02: (a) pre-finalize gate + queue-length gate + do-while integration structurally enforce drain-before-swap, preventing player-controllable state from changing between VRF request and fulfillment in a way that influences consumer behavior; (b) nudged-word write ensures consistent entropy across code paths; (c) game-over best-effort drain does not open a player-controllable window; (d) RngNotReady selector fix preserves the correct revert boundary without opening a new commitment hole"
    - "The AUDIT.md output cross-cites (per D-04 format) the following prior-phase verdicts with a `re-verified at HEAD 1646d5af` note per cited row: 233-02-AUDIT JKP-02 D-06 commitment-window enumeration; 232.1 Plan 02 forge invariant tests (drain-before-swap + no-zero-entropy — 8/8 PASS at HEAD)"
    - "The enumeration table explicitly covers the rngLocked invariant: during the VRF window (rngLocked == true), permissionless mutations to state variables that could influence consumer behavior are blocked. Every listed state variable has a `rngLocked-Guarded? Yes/No` annotation."
    - "Every verdict row uses vocabulary exactly `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`"
    - "Every verdict row carries a `Finding Candidate: Y/N` column value"
    - "No F-29-NN finding IDs appear anywhere in 235-04-AUDIT.md (per D-14)"
    - "Scope-guard deferrals, if any, recorded in 235-04-SUMMARY.md per D-15 (230-01-DELTA-MAP.md and 230-02-DELTA-ADDENDUM.md NOT edited)"
    - "Findings-Candidate Block, Scope-guard Deferrals, and Downstream Hand-offs subsections present; Phase 236 FIND-01 hand-off explicitly named for ID assignment"
    - "Zero contracts/ and zero test/ writes occur during plan execution (READ-only per D-17)"
  artifacts:
    - path: ".planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-04-AUDIT.md"
      provides: "RNG-02 per-consumer commitment-window enumeration across v29.0 delta"
      contains: "## Per-Consumer Commitment-Window Enumeration Table"
  key_links:
    - from: "230-01-DELTA-MAP.md §1 (player-controllable state reads inside new/modified RNG consumers) + §2.5 IM-21 + IM-22 + §3.4 + §3.5 (automated gates) + §4 Consumer Index RNG-02 row"
      to: "235-04-AUDIT.md Per-Consumer Commitment-Window Enumeration Table rows"
      via: "Every new RNG consumer in the delta has an enumeration row with rngLocked-guard annotation"
      pattern: "Player-Controllable|Mutation Between|Non-Influential"
    - from: "230-02-DELTA-ADDENDUM.md c2e5e0a9 + 314443af per-site inputs"
      to: "235-04-AUDIT.md 17 per-site commitment-window rows + 314443af commitment-window row"
      via: "Per D-08 + D-09, each c2e5e0a9 site + 314443af get a distinct enumeration row"
      pattern: "hash2|keccak256|baseKey|entropyWord|rollSalt"
    - from: "235-04-AUDIT.md VULNERABLE/DEFERRED/SAFE-INFO Candidate: Y verdicts"
      to: "Phase 236 FIND-01 finding-candidate pool"
      via: "Findings-Candidate Block (no F-29-NN IDs, Finding Candidate: Y/N column)"
      pattern: "## Findings-Candidate Block"
---

<objective>
RNG-02 analytical re-proof of commitment-window integrity for every new RNG consumer in the v29.0 delta at HEAD `1646d5af`. Produces `235-04-AUDIT.md`: a Per-Consumer Commitment-Window Enumeration Table covering all 5 new-consumer categories (same 5 as RNG-01), enumerating every player-controllable state variable that can change between VRF request and fulfillment and verifying non-influential for each new consumer. Plus a mandatory `## 232.1 Ticket-Processing Impact` sub-section (per D-06), cross-cites to 233-02 JKP-02 + 232.1 Plan 02 forge invariants each re-verified at HEAD `1646d5af`, Findings-Candidate Block, Scope-guard Deferrals, and Downstream Hand-offs.

Purpose: RNG-02 is the commitment-window half of the RNG commitment proof. A C4A warden attacks RNG-02 by searching for a player-controllable state variable that can change BETWEEN VRF request and fulfillment in a way that influences the consumer's output. Even if the VRF word itself was unknown at commitment time (RNG-01), an attacker can still bias consumer behavior if they can (a) influence the consumer's code path, (b) modify an input variable consumed alongside the VRF word, (c) alter a guard condition. The v29.0 delta introduces new consumers; RNG-02 enumerates every player-controllable state variable that could change during the VRF window and proves non-influential for each new consumer. Per D-08, the 17 c2e5e0a9 sites get per-site enumeration rows — each site has different input variables (`lvl`, `sourceLevel`, `traitIdx`, `ticketUnits`, `beneficiary`, `weiAmount`, `baseKey`, `rollSalt`, `groupIdx`, etc.) and each needs a distinct non-influential proof. The rngLocked invariant is the milestone-wide gate: during the VRF window, mutations to most game state are blocked. Per D-09, availability (entropy != 0) is cross-cited from 232.1-03-PFTB-AUDIT. Per D-14 no F-29-NN IDs. Per D-17 READ-only.

Output: `235-04-AUDIT.md` with Per-Consumer Commitment-Window Enumeration Table (all 5 categories, 17+ rows for c2e5e0a9) + 232.1 Ticket-Processing Impact + Findings-Candidate Block + Scope-guard Deferrals + Downstream Hand-offs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-CONTEXT.md
@.planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md
@.planning/phases/230-delta-extraction-scope-map/230-02-DELTA-ADDENDUM.md
@.planning/phases/230-delta-extraction-scope-map/230-01-SUMMARY.md
@.planning/phases/233-jackpot-baf-entropy-audit/233-02-AUDIT.md
@.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-02-SUMMARY.md
@.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-01-FIX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build RNG-02 Per-Consumer Commitment-Window Enumeration Table covering 5 consumer categories (with 17 per-site c2e5e0a9 rows per D-08 + 314443af row per D-09) at HEAD 1646d5af; write 235-04-AUDIT.md</name>
  <files>.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-04-AUDIT.md</files>
  <read_first>
    - .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-CONTEXT.md (D-01 through D-17 — specifically D-08 c2e5e0a9 per-site commitment-window enumeration / D-09 314443af + 232.1-03 cross-cite / D-06 mandatory 232.1 sub-section / D-14 no F-29-NN / D-15 scope-guard in SUMMARY / D-17 READ-only)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1 (all player-controllable state reads inside new/modified RNG consumers — grep the file for state-variable names that appear as inputs to keccak / hash2 calls)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §2.5 IM-21 (the deleted `_unlockRng(day)` call at advanceGame:425 — TRNX-01 scope but RNG-02 must verify no commitment-window widening on the 2471f8e7 surface) and IM-22 (entropy passthrough replay row)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §3.4 + §3.5 (automated gate output at HEAD — corroborating structural baseline: check-interfaces PASS, check-delegatecall 44/44 or 46/46 post-232.1, check-raw-selectors PASS, forge build PASS)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §4 Consumer Index RNG-02 row (authoritative scope anchor per D-04)
    - .planning/phases/230-delta-extraction-scope-map/230-02-DELTA-ADDENDUM.md — per-site inputs for all 17 entropy-mixing sites. Each site's inputs feed the commitment-window enumeration: e.g. site 277 inputs are `(rngWord, targetLvl)`; site 508 inputs are `(randWord, lvl, COIN_JACKPOT_TAG)`; site 1808 inputs are `(entropy, s)` per-sample iterator. Use the addendum's per-site table (page 2) as the authoritative inputs list.
    - contracts/storage/DegenerusGameStorage.sol at HEAD — enumerate every state variable that may be referenced by an RNG consumer at any of the 5 categories. Candidates include:
        * `rngLocked` — the guard itself (bool)
        * `rngWordCurrent` — the VRF word buffer (uint256)
        * `rngRequestTime` — timestamp when VRF was requested
        * `vrfRequestId` — VRF coordinator request ID
        * `rngWordByDay[day]` — memoized daily word mapping
        * `lootboxRngWordByIndex[idx]` — mid-day and daily slot storage mapping
        * `LR_INDEX` — lootbox RNG index (packed in another slot)
        * `LR_MID_DAY` — mid-day flag (packed)
        * `totalFlipReversals` — nudge count
        * `level` — game level (uint24)
        * `jackpotPhase` / `jackpotCounter` — jackpot-phase state
        * `phaseTransitionActive` — phase-transition freeze flag
        * `gameOver` — game-over flag
        * `currentPrizePool` / `nextPrizePool` / `futurePrizePool` / `claimablePool` / `decimatorPool` (pool state; CONS-01 scope, but RNG-02 must confirm these cannot influence RNG consumer behavior)
        * `ticketQueue` / `ticketsOwedPacked` — ticket state
        * `mintPacked_` — per-player mint state
        * `boonPacked` — per-player boon state (e0a7f7bc exposed)
        * `traitBurnTicket` — per-player trait state
        * `deityBoon*` / `lootboxPresale*` — special-class state
        * `burnieCoinflipPool` — coinflip state
        * `decBurns` / `decPool` — decimator state
        * `playerQuestProgress` — quest state
        * `earlybirdDgnrsPoolStart` — earlybird sentinel
        * Identify rngLocked-guarded vs unguarded state-variable writers by grepping for `rngLocked == true` / `rngLocked == false` / `rngLocked =` / `require(!rngLocked)` / equivalent guards across all modules.
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD — locate the VRF request site (typically `VRFCoordinatorV2_5.requestRandomWords` or similar coordinator call; grep for `requestRandomWords` or the coordinator interface identifier). Record the File:Line of the request + the File:Line of the fulfillment (rawFulfillRandomWords:1690). The "VRF window" is the span between these two sites.
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD — `rngLocked` writer sites: grep for `rngLocked = true` / `rngLocked = false` / `rngLockedFlag = ` (packed accessor). The invariant is `rngLocked == true` between VRF request and fulfillment; every permissionless mutation path that could influence consumer behavior must be gated by `rngLocked` or explicitly non-influential.
    - contracts/modules/DegenerusGameMintModule.sol at HEAD — grep for purchase-path writes (e.g. `ticketQueue.push`, `ticketsOwedPacked = ...`, `mintPacked_[player] = ...`). Each must be verified as either (a) gated by rngLocked (cannot fire during VRF window), or (b) not influencing any new RNG consumer at categories 1-5.
    - contracts/modules/DegenerusGameJackpotModule.sol at HEAD — for EACH of the 16 c2e5e0a9 sites, read the function containing the site + enumerate every state variable referenced as an input to the keccak/hash2 call:
        * Site 277 (`_runDailyEthOnlyAndBonus`): inputs `(rngWord, targetLvl)` — targetLvl is a function parameter derived from caller's level state; verify level changes are rngLocked-guarded
        * Site 443 (`payDailyJackpot`): inputs `(randWord, lvl)` — lvl is resolution level, post-bump; verify rngLocked window
        * Sites 508, 522 (coin branches): inputs `(randWord, lvl, COIN_JACKPOT_TAG)` — TAG is compile-time constant, immutable
        * Site 544 (`_executeJackpot`): inputs `(randWord, lvl)`
        * Site 594 (`payDailyJackpotCoinAndTickets`): inputs `(randWord, lvl)`
        * Site 596 (carryover decorrelation): inputs `(randWord, sourceLevel)` — sourceLevel is caller-derived from carryover state
        * Sites 607-609 (coin near-budget): inputs `(randWord, lvl, COIN_JACKPOT_TAG)` × 3
        * Site 874 (`_distributePurchasePhaseTickets`): inputs `(randWord, lvl)`
        * Site 937 (bucket loop): inputs `(entropy, traitIdx, ticketUnits)` — traitIdx is loop index (immutable); ticketUnits is caller-scoped local
        * Site 1134 (`_resumeDailyEth`): inputs `(randWord, lvl)`
        * Sites 1238, 1345 (bucket loops): inputs `(entropy, traitIdx, X)`
        * Site 1681 (`_awardDailyCoinJackpotInRange`): inputs `(randWord, lvl, COIN_JACKPOT_TAG)`
        * Sites 1682-1683: same formulation
        * Site 1741 (`_awardDailyCoinToTraitWinners`): inputs `(entropy, traitIdx, traitShare)`
        * Sites 1798-1800 (`_awardFarFutureCoinJackpot` initial seed): inputs `(rngWord, lvl, FAR_FUTURE_COIN_TAG)`
        * Site 1808 (`_awardFarFutureCoinJackpot` sample loop): inputs `(entropy, s)` — s is loop iterator (immutable within loop)
    - contracts/modules/DegenerusGameMintModule.sol at HEAD — `_rollRemainder` at line 647: inputs `(entropy, rollSalt)`. rollSalt is caller-supplied (from `_raritySymbolBatch` call context — derived from `baseKey` + `rem`). Enumerate rollSalt's derivation and verify non-influential for commitment window.
    - contracts/modules/DegenerusGameMintModule.sol at HEAD — `_raritySymbolBatch` at line 568: inputs `(baseKey, entropyWord, groupIdx)`. baseKey derived from `(player, lvl, idx)` tuple (player is msg.sender — IMMUTABLE within tx; lvl is function parameter; idx is loop index — immutable). entropyWord is the passed `entropy` parameter. groupIdx is `i >> 4` loop derivative.
    - contracts/modules/DegenerusGamePayoutUtils.sol at HEAD — `_calcAutoRebuy` at line 68-70: inputs `(entropy, beneficiary, weiAmount)`. beneficiary is caller-supplied (typically msg.sender or a passed address); weiAmount is caller-computed.
    - .planning/phases/233-jackpot-baf-entropy-audit/233-02-AUDIT.md — JKP-02 D-06 commitment-window enumeration for the 52242a10 passthrough (approximately 16 SAFE enumeration rows per 233-02-SUMMARY). Re-verify at HEAD by re-reading the 12+ variable enumeration; confirm each variable's rngLocked-guard status is unchanged.
    - .planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-02-SUMMARY.md — forge invariant suite 8/8 PASS at HEAD: drain-before-swap invariant + no-zero-entropy invariant + game-over path isolation. These are structural corroboration of the commitment-window integrity for the 232.1 ticket-processing surface. Cross-cite per D-04.
    - .planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-01-FIX.md — Revision 2 final shape + the pre-finalize gate at AdvanceModule:257-279 + queue-length gate + nudged-word write + do-while integration + RngNotReady selector fix. Each change narrows or preserves the commitment-window.
    - Output of `git rev-parse --short HEAD` — must match frontmatter `head_sha: 1646d5af`
  </read_first>
  <action>
Execute fresh-from-HEAD RNG-02 commitment-window enumeration re-proof at HEAD `1646d5af` per CONTEXT.md D-01 through D-17 and D-07 / D-08 / D-09 addendum treatment rules.

**Step 1 — Enumerate the global player-controllable state set.** Grep contracts/storage + contracts/modules for every state-variable write path that can be triggered by a non-privileged (non-onlyGame, non-admin) caller. Produce the Global State-Variable Enumeration sub-section listing every candidate with:
  - Variable name
  - File:Line (declaration)
  - Writer function(s)
  - rngLocked-Guarded? (Yes / No)
  - Can Change During VRF Window? (Yes / No)

**Step 2 — For each of the 5 consumer categories, produce a Per-Consumer Commitment-Window Enumeration Table.** Columns per D-08:
  `Consumer | Inputs | Player-Controllable? | Mutation Between Request-Fulfillment | Non-Influential Proof | Verdict | Finding Candidate`

**Category 1 — Earlybird Bonus-Trait Roll (20a951df)** — 1-3 rows:
  - `_runEarlyBirdLootboxJackpot` via `_rollWinningTraits(rngWord, true)` — Inputs: `rngWord`, `BONUS_TRAITS_TAG`. rngWord flows from rngGate; BONUS_TRAITS_TAG is compile-time constant. Neither player-controllable. Proof: TAG immutable, rngWord sourced from rngLocked-guarded rngGate.

**Category 2 — BAF Sentinel Emission (104b5d42)** — 1 row:
  - `runBafJackpot` rngWord consumption — Inputs: pre-existing. Sentinel is compile-time constant. No new player-controllable input introduced.

**Category 3 — Entropy Passthrough (52242a10)** — at least 3 rows for IM-10/11/12 + 1 for MintModule.processFutureTicketBatch receiver body + 1 for IM-13:
  - Each row lists inputs (e.g. `rngWord`, `lvl`, `entropy`). lvl is function parameter derived from caller. Cross-cite 233-02 D-06 enumeration (16 variables covered).

**Category 4 — c2e5e0a9 Entropy-Mixing Sites (per D-08, 17 per-site rows minimum)**: For each site, enumerate inputs + verify non-influential:
  - `lvl` / `targetLvl` / `sourceLevel` — level state; only changes at VRF request time via `_finalizeRngRequest` (rngLocked-gated); does NOT change during VRF window. NON-INFLUENTIAL.
  - `COIN_JACKPOT_TAG` / `FAR_FUTURE_COIN_TAG` — compile-time constants. IMMUTABLE. NON-INFLUENTIAL.
  - `traitIdx` / `s` / `groupIdx` / `i >> 4` — loop iterators within the consumer function. IMMUTABLE within tx. NON-INFLUENTIAL.
  - `ticketUnits` / `traitShare` / `X` (varies) — caller-scoped local values computed within the consumer's call frame. Not observed by attacker. NON-INFLUENTIAL.
  - `beneficiary` / `weiAmount` (PayoutUtils._calcAutoRebuy) — caller-supplied; typically msg.sender + msg.value-derived. msg.sender is IMMUTABLE within tx. weiAmount is function parameter bound at entry. NON-INFLUENTIAL.
  - `rollSalt` (MintModule._rollRemainder) — derived from baseKey + rem (function-scoped). NON-INFLUENTIAL.
  Each of the 17 sites gets a row documenting the input set + non-influential proof + rngLocked-guard status.

**Category 5 — `_raritySymbolBatch` Keccak-Seed (314443af per D-09)** — 1 row:
  - Inputs: `baseKey`, `entropyWord`, `groupIdx`. baseKey = `(uint256(lvl) << 224) | (uint256(player) << 64) | idx` (player is msg.sender — immutable within tx; lvl is function parameter from caller chain; idx is loop index — immutable). entropyWord is the passed `entropy` parameter. groupIdx is `i >> 4`.
  - Proof: none of the inputs can be player-manipulated between VRF request and fulfillment. Cross-cite 232.1-03-PFTB-AUDIT for availability (entropy != 0).

**Step 3 — Document the rngLocked invariant globally.** Produce a dedicated sub-section `## rngLocked Invariant` explaining:
  - `rngLocked` is set `true` at VRF request time (locate exact writer site)
  - `rngLocked` is cleared `false` at VRF fulfillment via `rawFulfillRandomWords` or `_unlockRng`
  - During `rngLocked == true`, the following mutation paths are BLOCKED (enumerate via grep for `require(!rngLocked)` / `if (rngLocked) revert` equivalents): purchase paths, flip-reversal, certain jackpot ops, any ticket-queueing operation that would alter far-future state per Plan 235-05 TRNX-01 invariant
  - Per 235 CONTEXT.md D-11: rngLocked blocks (a) far-future ticket queue writes AND (b) writes to the active (read-side) buffer. Writes to the write-side buffer at the current level ARE PERMITTED.
  - This sub-section is the milestone-wide commitment-window guard that backs every "NON-INFLUENTIAL" verdict in the enumeration table.

**Step 4 — Write the mandatory `## 232.1 Ticket-Processing Impact` sub-section (per D-06).** Walk each 232.1 fix series change against RNG-02:
  - **Pre-finalize gate** — structurally ENFORCES drain-before-swap; prevents player-controllable state (specifically ticket queue contents) from being observed by a consumer reading a pre-populated slot. The gate reverts `RngNotReady()` when the slot isn't ready, blocking the consumer from executing with stale state.
  - **Queue-length gate** — gates the pre-finalize on `ticketQueue[preRk].length > 0`; no new player-controllable mutation introduced (queue is caller-populated via purchase path, which is rngLocked-gated).
  - **Nudged-word write** — ensures consistent entropy across code paths. The nudge count (`totalFlipReversals`) is frozen within the advance tx via `rngLockedFlag` blocking `reverseFlip`. So between VRF request and fulfillment, `totalFlipReversals` cannot be player-influenced.
  - **Do-while integration** — preserves single-entropy-consumer invariant; commitment window unchanged.
  - **Game-over best-effort drain** — fires in `_handleGameOverPath` only after `gameOver = true` SSTORE at GameOverModule:136. Once gameOver is set, permissionless paths that could influence RNG consumers are halted (terminal state). No new commitment-window widening.
  - **RngNotReady selector fix** — pure revert-selector correction; no commitment-window impact.
  - **Buffer swap at RNG request time** (per D-12) — the buffer swap fires at VRF REQUEST TIME, not fulfillment. After the swap, the read buffer contains the tickets that will be drained with the in-flight VRF word; the write buffer is empty. Writes to the write buffer during the VRF window are permitted (per D-11 invariant) because they drain next round, not this one.
  Sub-section MUST explicitly state: "232.1 fix series structurally ENFORCES drain-before-swap, which closes a pre-existing RNG-02 commitment-window hole where `processTicketBatch` could read `lootboxRngWordByIndex[X]` while still zero and consume entropy=0 into `_raritySymbolBatch`. Every new RNG consumer's commitment window is tightly bounded by the gate."

**Step 5 — Write cross-cite rows (per D-04 format):**
  - 233-02-AUDIT JKP-02 D-06 commitment-window enumeration: re-verified at HEAD by re-reading the 16-variable enumeration + confirming rngLocked-guard status unchanged.
  - 232.1 Plan 02 forge invariant tests (drain-before-swap + no-zero-entropy, 8/8 PASS at HEAD): CROSS-CITE structural corroboration; reference `232.1-02-SUMMARY.md`.

**Step 6 — Write `235-04-AUDIT.md` with required structure. Sections in this exact order:**

```markdown
# RNG-02 Analytical Audit: Per-Consumer Commitment-Window Enumeration Re-Proof (v29.0 Delta at HEAD 1646d5af)

**Scope:** Every player-controllable state variable that can change between VRF request and fulfillment across all 5 new-RNG-consumer categories in the v29.0 delta + 2 post-Phase-230 addendum commits (`314443af`, `c2e5e0a9`) + 232.1 fix series. Anchored in 230-01 §1 + §2.5 + §3.4/§3.5 + §4 Consumer Index RNG-02.
**Method:** Fresh re-prove at HEAD `1646d5af` per D-03. Per-consumer enumeration per D-08 (17 c2e5e0a9 sites) + D-09 (314443af). rngLocked invariant as the milestone-wide guard. Cross-cite prior-phase verdicts per D-04 with `re-verified at HEAD 1646d5af` note.
**Verdict vocabulary:** `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`
**Finding-ID policy:** No `F-29-NN` IDs emitted (per D-14) — Phase 236 FIND-01 owns ID assignment.
**Scope-guard policy:** Per D-15 — any gap recorded in 235-04-SUMMARY.md, not catalog.
**D-09 split:** Commitment-window diffusion is Phase 235's question; availability CROSS-CITES 232.1-03-PFTB-AUDIT.

## Methodology

[Paragraph: 5 consumer categories; D-08 per-site enumeration for c2e5e0a9 17 sites; D-09 314443af enumeration + cross-cite for availability; rngLocked invariant as milestone-wide guard; mandatory 232.1 Ticket-Processing Impact sub-section per D-06.]

## Findings-Candidate Block

[Prose, no F-29-NN IDs. Expected: "No on-chain candidate findings — every new RNG consumer's commitment window is tightly bounded by rngLocked guard + the 232.1 pre-finalize gate structural enforcement." If any state variable can change during the VRF window AND influences consumer behavior, record as VULNERABLE / Finding Candidate: Y.]

## rngLocked Invariant (Milestone-Wide Guard)

### Writer Sites
[Enumerate every `rngLocked = true` / `rngLocked = false` write with File:Line. Typical: set true at VRF request; clear at rawFulfillRandomWords or _unlockRng.]

### Guarded Mutation Surface
[Enumerate every permissionless mutation path gated by `rngLocked`. Writers of these state variables are blocked during the VRF window:]
| State Variable | Guarded Writer | rngLocked Guard Site | File:Line |
|---|---|---|---|
| [enumerate] | [enumerate] | [enumerate] | [enumerate] |

### Phase 235 D-11 Invariant Annotation
[Cite the exact user-locked statement: "During the rngLocked window (VRF request → fulfillment), across the newly-packed housekeeping step: (a) NO far-future ticket queue write may occur, AND (b) NO write may land in the active (read-side) buffer. Writes to the write-side buffer at the current level ARE PERMITTED — they drain next round with the next VRF word. rngLocked is NOT a blanket ticket-queueing block." Cross-reference to 235-05-PLAN TRNX-01 for the full invariant proof.]

## Global State-Variable Enumeration

[Table listing every state variable that a player can write to, with rngLocked-guard annotation:]
| Variable | File:Line (decl) | Writer(s) | rngLocked-Guarded? | Can Change During VRF Window? | Observed By Any New RNG Consumer? |
|---|---|---|---|---|---|
| [enumerate 15-25 variables] | ... | ... | Yes / No | Yes / No | Yes / No |

## Per-Consumer Commitment-Window Enumeration Table

### Category 1 — Earlybird Bonus-Trait Roll (20a951df)
| Consumer | Inputs | Player-Controllable? | Mutation Between Request-Fulfillment | Non-Influential Proof | Verdict | Finding Candidate |
|---|---|---|---|---|---|---|
| `JackpotModule._runEarlyBirdLootboxJackpot` via `_rollWinningTraits(rngWord, true)` | `rngWord`, `BONUS_TRAITS_TAG` | No for both inputs | rngWord: set once at fulfillment via rngGate; TAG: compile-time constant | TAG immutable; rngWord sourced from rngLocked-guarded rngGate | SAFE | N |

### Category 2 — BAF Sentinel Emission (104b5d42)
| `JackpotModule.runBafJackpot` rngWord consumption | `rngWord` (pre-existing chain) + `BAF_TRAIT_SENTINEL` | No | No new player-controllable input introduced | SENTINEL is compile-time constant; rngWord pre-existing via rngLocked-guarded chain | SAFE | N |

### Category 3 — Entropy Passthrough (52242a10) — at least 5 rows
[IM-10 / IM-11 / IM-12 / IM-13 / MintModule.processFutureTicketBatch receiver each with enumeration row; cross-cite 233-02 D-06.]

### Category 4 — c2e5e0a9 Entropy-Mixing Sites (per D-08, at least 17 rows)

[Flatten the 17+ sites into per-site rows with inputs + non-influential proofs. Example rows:]

| `JackpotModule._runDailyEthOnlyAndBonus` | `rngWord`, `targetLvl` | rngWord: No (VRF-derived); targetLvl: No (caller-derived from level storage, rngLocked-gated) | No mutation during VRF window | level storage only changes via `_finalizeRngRequest` at VRF request (not during window) | SAFE | N |
| `JackpotModule.payDailyJackpot` L443 | `randWord`, `lvl` | No for both | No | Same as above | SAFE | N |
| `JackpotModule.payDailyJackpot` L508 coin branch | `randWord`, `lvl`, `COIN_JACKPOT_TAG` | No for all three | No | TAG immutable compile-time constant | SAFE | N |
| `JackpotModule.payDailyJackpot` L522 coin branch #2 | `randWord`, `lvl`, `COIN_JACKPOT_TAG` | No | No | Same as L508 | SAFE | N |
| `JackpotModule._executeJackpot` L544 | `randWord`, `lvl` | No | No | Same | SAFE | N |
| `JackpotModule.payDailyJackpotCoinAndTickets` L594 | `randWord`, `lvl` | No | No | Same | SAFE | N |
| `JackpotModule.payDailyJackpotCoinAndTickets` L596 carryover | `randWord`, `sourceLevel` | No | sourceLevel: caller-derived from jackpotCounter / carryover state (rngLocked-gated) | sourceLevel storage changes blocked during VRF window | SAFE | N |
| `JackpotModule.payDailyJackpotCoinAndTickets` L607 coin near-budget | `randWord`, `lvl`, `COIN_JACKPOT_TAG` | No | No | TAG immutable | SAFE | N |
| `JackpotModule.payDailyJackpotCoinAndTickets` L608 | same | No | No | same | SAFE | N |
| `JackpotModule.payDailyJackpotCoinAndTickets` L609 | same | No | No | same | SAFE | N |
| `JackpotModule._distributePurchasePhaseTickets` L874 | `randWord`, `lvl` | No | No | Same | SAFE | N |
| `JackpotModule._distributeTicketsToBuckets` L937 | `entropy`, `traitIdx`, `ticketUnits` | No | traitIdx: loop index immutable within tx; ticketUnits: caller-scoped local; entropy: rngLocked-guarded | Inputs are function-local or immutable | SAFE | N |
| `JackpotModule._resumeDailyEth` L1134 | `randWord`, `lvl` | No | No | Same | SAFE | N |
| `JackpotModule._processDailyEth` L1238 bucket loop | `entropy`, `traitIdx`, `X` | No | traitIdx/X: loop-scoped; entropy: rngLocked-guarded | function-local | SAFE | N |
| `JackpotModule._processDailyEth` L1345 bucket loop #2 | same | No | No | same | SAFE | N |
| `JackpotModule._awardDailyCoinJackpotInRange` L1681 | `randWord`, `lvl`, `COIN_JACKPOT_TAG` | No | No | TAG immutable | SAFE | N |
| `JackpotModule._awardDailyCoinJackpotInRange` L1682 | same | No | No | same | SAFE | N |
| `JackpotModule._awardDailyCoinJackpotInRange` L1683 | same | No | No | same | SAFE | N |
| `JackpotModule._awardDailyCoinToTraitWinners` L1741 | `entropy`, `traitIdx`, `traitShare` | No | traitIdx/traitShare: loop-scoped/function-local; entropy: rngLocked-guarded | function-local | SAFE | N |
| `JackpotModule._awardFarFutureCoinJackpot` L1798 initial seed | `rngWord`, `lvl`, `FAR_FUTURE_COIN_TAG` | No | No | TAG immutable | SAFE | N |
| `JackpotModule._awardFarFutureCoinJackpot` L1799 | same | No | No | same | SAFE | N |
| `JackpotModule._awardFarFutureCoinJackpot` L1800 | same | No | No | same | SAFE | N |
| `JackpotModule._awardFarFutureCoinJackpot` L1808 sample loop | `entropy`, `s` | No | s: loop iterator immutable within loop; entropy: rngLocked-guarded | function-local | SAFE | N |
| `MintModule._rollRemainder` L647 | `entropy`, `rollSalt` | No | rollSalt: caller-scoped derived from baseKey + rem (function-local); entropy: rngLocked-guarded | function-local | SAFE | N |
| `PayoutUtils._calcAutoRebuy` L68 | `entropy`, `beneficiary`, `weiAmount` | No for all three | beneficiary: function param (immutable within tx); weiAmount: function param; entropy: rngLocked-guarded | all function-local or rngLocked-guarded | SAFE | N |

### Category 5 — `_raritySymbolBatch` Keccak-Seed (314443af per D-09)
| `MintModule._raritySymbolBatch` L568 LCG seed | `baseKey`, `entropyWord`, `groupIdx` | No for all three | baseKey = (lvl, player=msg.sender, idx) — player immutable within tx, lvl is function parameter, idx is loop index; entropyWord: rngLocked-guarded; groupIdx: `i >> 4` loop derivative | function-local or rngLocked-guarded; availability (entropyWord != 0) CROSS-CITED from 232.1-03-PFTB-AUDIT | SAFE | N |

## D-09 Non-Zero-Entropy Availability Cross-Cite

[Explicit sub-section stating: "Availability (entropyWord != 0) at all 4 reachable `_processFutureTicketBatch` call sites is CROSS-CITED from `.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-PFTB-AUDIT.md` per D-09 clean split. Availability anchors: `rawFulfillRandomWords:1698` zero-guard + `rngGate:291` sentinel-1 break + Plan 01 pre-drain gate. Re-verify at HEAD: PASS — anchors present unchanged. Phase 235 RNG-02 focuses on commitment-window integrity (no player-controllable input to the consumer can change during the VRF window)."]

## 232.1 Ticket-Processing Impact

[Mandatory per D-06. Walk each 232.1 fix series change against RNG-02 as detailed in Step 4 above. Must state explicitly: "232.1 fix series structurally ENFORCES drain-before-swap, closing a pre-existing RNG-02 commitment-window hole."]

## 230-02 Addendum Impact

[Addendum commits 314443af + c2e5e0a9 introduce 17 new entropy-mixing sites (per D-08) + 1 keccak-seed fix (per D-09). All 17 sites have dedicated enumeration rows in Category 4; 314443af has a row in Category 5. No site introduces a new player-controllable input beyond the existing rngLocked-guarded state.]

## Cross-Cited Prior-Phase Verdicts (per D-04 format, re-verified at HEAD 1646d5af)

| Cited From | Verdict | Re-Verify Evidence at HEAD 1646d5af |
|---|---|---|
| 233-02-AUDIT JKP-02 D-06 commitment-window enumeration (16 variables covered) | PASS | [re-read 233-02-AUDIT enumeration table + confirm rngLocked-guard status for each variable at HEAD] |
| 232.1 Plan 02 forge invariant tests (drain-before-swap + no-zero-entropy, 8/8 PASS) | PASS | [re-read `.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-02-SUMMARY.md`; confirm 8/8 PASS on HEAD; structural corroboration of commitment-window integrity] |

## Scope-guard Deferrals

[Per D-15. If none, "None surfaced during this audit."]

## Downstream Hand-offs

- **Phase 236 FIND-01:** VULNERABLE/DEFERRED/SAFE-INFO Finding Candidate: Y rows route here.
- **Phase 235-03 RNG-01:** Backward-trace — sibling plan; RNG-01 owns the VRF-word-unknown-at-commitment-time side.
- **Phase 235-05 TRNX-01:** Phase-transition rngLocked invariant — RNG-02 cites rngLocked as the milestone-wide guard; TRNX-01 owns the 2471f8e7-specific invariant proof across the packed housekeeping window.
- **Phase 236 REG-01:** Regression cross-check against v25.0 Phase 215 RNG audit.
```

Every verdict row MUST:
- Use the locked D-08 column schema: `Consumer | Inputs | Player-Controllable? | Mutation Between Request-Fulfillment | Non-Influential Proof | Verdict | Finding Candidate`.
- Cite real File:Line at HEAD `1646d5af` where applicable (consumer site is real File:Line; inputs may reference data types or function params).
- Use verdict vocabulary strictly from `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`.
- Carry `Y` or `N` in Finding Candidate.
- Never reference F-29-NN IDs.

Read contract sources ONLY from `contracts/` at HEAD per `feedback_contract_locations.md`.

READ-only per D-17. Only writes: `235-04-AUDIT.md` + (at output-step) `235-04-SUMMARY.md`. `git status --porcelain contracts/ test/` MUST be empty before/after task.
  </action>
  <verify>
    <automated>test -f .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-04-AUDIT.md && grep -q "## Per-Consumer Commitment-Window Enumeration Table" .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-04-AUDIT.md && grep -q "## 232.1 Ticket-Processing Impact" .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-04-AUDIT.md && grep -q "## rngLocked Invariant" .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-04-AUDIT.md && ! grep -q "F-29-" .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-04-AUDIT.md && test -z "$(git status --porcelain contracts/ test/)"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-04-AUDIT.md` exists
    - File contains literal headers: `## Per-Consumer Commitment-Window Enumeration Table`, `## rngLocked Invariant`, `## Global State-Variable Enumeration`, `## D-09 Non-Zero-Entropy Availability Cross-Cite`, `## 232.1 Ticket-Processing Impact`, `## 230-02 Addendum Impact`, `## Cross-Cited Prior-Phase Verdicts`, `## Findings-Candidate Block`, `## Scope-guard Deferrals`, `## Downstream Hand-offs`
    - File contains 5 named sub-section headers under Per-Consumer Commitment-Window Enumeration Table: Category 1 (earlybird bonus-trait), Category 2 (BAF sentinel), Category 3 (entropy passthrough), Category 4 (c2e5e0a9), Category 5 (`_raritySymbolBatch`)
    - Category 4 has at least 17 distinct rows (one per c2e5e0a9 entropy-mixing site per D-08)
    - Category 5 has at least 1 row for 314443af keccak-seed commitment-window enumeration
    - rngLocked Invariant sub-section explicitly cites the D-11 invariant statement
    - Global State-Variable Enumeration table has at least 15 variables enumerated with rngLocked-guard annotation
    - Every Per-Consumer row cites consumer at a File:Line in `contracts/` at HEAD 1646d5af
    - Every row's Verdict is exactly `SAFE`, `SAFE-INFO`, `VULNERABLE`, or `DEFERRED`
    - Every row's Finding Candidate is `Y` or `N`
    - `grep -c "1646d5af" .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-04-AUDIT.md` returns at least 3 (header + 2 cross-cites)
    - `grep -c "re-verified at HEAD 1646d5af" .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-04-AUDIT.md` returns at least 2
    - Cross-Cited Prior-Phase Verdicts table has at least 2 rows: 233-02 JKP-02 commitment-window + 232.1 Plan 02 forge invariants
    - D-09 Non-Zero-Entropy Availability Cross-Cite sub-section explicitly mentions `rawFulfillRandomWords:1698` + `rngGate:291` + Plan 01 pre-drain gate
    - 232.1 Ticket-Processing Impact sub-section explicitly states "structurally ENFORCES drain-before-swap" or "closes a pre-existing RNG-02 commitment-window hole" (or equivalent)
    - The string `F-29-NN` does NOT appear in the file
    - The string `F-29-` does NOT appear in any form in the file
    - No placeholder line numbers like `:<line>` remain in the final file
    - Downstream Hand-offs names `Phase 236 FIND-01`, `Phase 235-03 RNG-01`, `Phase 235-05 TRNX-01`, `Phase 236 REG-01`
    - `git status --porcelain contracts/ test/` returns empty both before and after task
    - Frontmatter `head_sha: 1646d5af` matches current HEAD
  </acceptance_criteria>
  <deliverable>
`235-04-AUDIT.md` with Per-Consumer Commitment-Window Enumeration Table covering all 5 categories (including 17 per-site c2e5e0a9 rows per D-08 + 314443af keccak-seed row per D-09) + rngLocked Invariant sub-section with D-11 citation + Global State-Variable Enumeration + D-09 Availability Cross-Cite + 232.1 Ticket-Processing Impact + 230-02 Addendum Impact + Cross-Cited Prior-Phase Verdicts (re-verified at HEAD 1646d5af per D-04) + Findings-Candidate Block + Scope-guard Deferrals + Downstream Hand-offs. Zero F-29-NN. Zero contracts/ or test/ writes.
  </deliverable>
  <done>`235-04-AUDIT.md` exists; every new RNG consumer has a commitment-window enumeration row verifying every input is non-influential during the VRF window; 17 per-site c2e5e0a9 rows; 314443af keccak-seed row; rngLocked Invariant sub-section with D-11 citation; 2 cross-cites re-verified at HEAD 1646d5af per D-04.</done>
</task>

<task type="auto">
  <name>Task 2: Commit approved 235-04-AUDIT.md</name>
  <files>.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-04-AUDIT.md</files>
  <read_first>
    - .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-04-AUDIT.md (must exist from Task 1)
  </read_first>
  <action>
Stage and commit `235-04-AUDIT.md` only.

```
git add .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-04-AUDIT.md
git commit -m "docs(235-04): RNG-02 commitment-window enumeration re-proof at HEAD 1646d5af

Per-Consumer Commitment-Window Enumeration Table covers 5 categories:
earlybird bonus-trait roll (20a951df); BAF sentinel emission (104b5d42);
entropy passthrough (52242a10); 17 c2e5e0a9 entropy-mixing sites per D-08;
314443af _raritySymbolBatch keccak-seed per D-09.

Every new consumer's inputs enumerated; every player-controllable state
variable that could change during VRF window verified NON-INFLUENTIAL via
rngLocked guard + function-local scoping + compile-time TAG constants.

rngLocked Invariant sub-section cites the D-11 locked statement: blocks
far-future ticket queue writes + active read-buffer writes; permits current-
level write-buffer writes.

232.1 Ticket-Processing Impact sub-section confirms fix series structurally
ENFORCES drain-before-swap, closing a pre-existing commitment-window hole.

D-09 non-zero-entropy availability CROSS-CITED from 232.1-03-PFTB-AUDIT.

Cross-cites 233-02 JKP-02 D-06 commitment-window + 232.1 Plan 02 forge
invariants (8/8 PASS) re-verified at HEAD per D-04.

Zero F-29-NN emissions (Phase 236 FIND-01 owns per D-14).
Zero contracts/ or test/ writes (READ-only per D-17)."
```

Do NOT push.
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -qE "235-04.*RNG-02.*1646d5af" && test -z "$(git status --porcelain contracts/ test/)"</automated>
  </verify>
  <acceptance_criteria>
    - One new commit subject starting `docs(235-04):` containing `RNG-02` + `1646d5af`
    - Commit touches only `.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-04-AUDIT.md`
    - `git status --porcelain contracts/ test/` empty
    - Not pushed
  </acceptance_criteria>
  <deliverable>
Approved 235-04-AUDIT.md committed; local-only.
  </deliverable>
  <done>Commit exists; contracts/ test/ unmodified.</done>
</task>

</tasks>

<verification>
- `235-04-AUDIT.md` exists with Per-Consumer Commitment-Window Enumeration Table covering 5 consumer categories
- Category 4 has at least 17 per-site rows for c2e5e0a9 entropy-mixing sites (per D-08)
- Category 5 has a 314443af `_raritySymbolBatch` keccak-seed row (per D-09)
- rngLocked Invariant sub-section explicitly cites the D-11 locked statement
- Global State-Variable Enumeration table has at least 15 variables with rngLocked-guard annotation
- D-09 Availability Cross-Cite sub-section cites 232.1-03-PFTB-AUDIT anchors
- Mandatory `## 232.1 Ticket-Processing Impact` sub-section (per D-06) present confirming drain-before-swap structural enforcement
- 2 cross-cites (233-02 D-06, 232.1 Plan 02 forge invariants) re-verified at HEAD 1646d5af per D-04
- Every File:Line anchor resolves into `contracts/` at HEAD
- Verdicts use `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`
- Every row has `Y` or `N` Finding Candidate
- Zero `F-29-NN` or `F-29-` references (per D-14)
- Zero `contracts/` or `test/` writes (per D-17)
- Approved AUDIT.md committed with message `docs(235-04): RNG-02 commitment-window enumeration re-proof at HEAD 1646d5af`
</verification>

<success_criteria>
Every new RNG consumer has a Per-Consumer Commitment-Window Enumeration row with inputs + non-influential proof. 5 categories covered with at least 17 per-site rows for c2e5e0a9 (per D-08) + 1 row for 314443af (per D-09). rngLocked Invariant sub-section cites the D-11 locked statement. Global state-variable enumeration lists at least 15 variables with rngLocked-guard annotation. Availability cross-cited from 232.1-03-PFTB-AUDIT per D-09. 2 prior-phase verdicts re-verified at HEAD 1646d5af per D-04. Mandatory 232.1 sub-section (D-06) confirms drain-before-swap structural enforcement. Zero F-29-NN per D-14. Zero contracts/ or test/ writes per D-17.
</success_criteria>

<output>
After completion, create `.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-04-SUMMARY.md`
</output>
</content>
