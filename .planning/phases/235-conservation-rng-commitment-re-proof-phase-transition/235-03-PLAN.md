---
phase: 235-conservation-rng-commitment-re-proof-phase-transition
plan: 235-03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-03-AUDIT.md
autonomous: true
requirements: [RNG-01]
head_sha: 1646d5af
must_haves:
  truths:
    - "Every new RNG consumer in the v29.0 delta has a row in the Per-Consumer Backward-Trace Table covering 5 consumer categories: (1) earlybird bonus-trait roll in `_runEarlyBirdLootboxJackpot` (20a951df, via `_rollWinningTraits(rngWord, true)`); (2) BAF `traitId=420` sentinel emission in `runBafJackpot` (104b5d42); (3) `processFutureTicketBatch` entropy passthrough (52242a10); (4) 17 per-site backward-trace rows for c2e5e0a9 hash2/keccak entropy-mixing sites (per D-07 + full addendum — JackpotModule 16 sites at lines 277/443/508/522/544/594/596/607-609/874/937/1134/1238/1345/1681-1683/1741/1798-1800/1808 + MintModule `_rollRemainder` at line 647 + PayoutUtils `_calcAutoRebuy` at line 68-70); (5) `_raritySymbolBatch` keccak-seed diffusion via `keccak256(abi.encode(baseKey, entropyWord, groupIdx))` (314443af per D-09)"
    - "Per D-07: 17 per-site backward-trace rows are present in the Per-Consumer Backward-Trace Table for the c2e5e0a9 entropy-mixing sites. Each row uses columns `Consumer | Site | File:Line | Commitment Point | Input Variables | Proof Word-Was-Unknown | Verdict`. No equivalence-class shortcuts — each site is its own row."
    - "Per D-09: a dedicated verdict row for `_raritySymbolBatch` keccak-seed diffusion (314443af — `baseKey + entropyWord + groupIdx` via `keccak256(abi.encode(...))`) confirms the new keccak diffusion fixes the low-bit bug and proves the VRF word was unknown at input commitment time. Non-zero-entropy-at-call-site is CROSS-CITED (not re-derived) from `232.1-03-PFTB-AUDIT.md` — that doc already proved non-zero entropy at all 4 reachable `_processFutureTicketBatch` call sites via rawFulfillRandomWords L1698 zero-guard + rngGate L291 sentinel-1 break + Plan 01 pre-drain gate."
    - "The AUDIT.md output contains a named `## 232.1 Ticket-Processing Impact` sub-section (per D-06) walking the 232.1 fix series changes against RNG-01: (a) pre-finalize gate + queue-length gate close the commitment-window hole where `processTicketBatch:690` could read `lootboxRngWordByIndex[X]` while still zero; (b) nudged-word write (`rngWordCurrent + totalFlipReversals`) makes the normal daily path write the same value as the game-over path and rngGate fallback; (c) do-while integration preserves the single-entropy-consumer invariant; (d) game-over best-effort drain ensures no consumer receives stale entropy; (e) RngNotReady selector fix (L207 + L263) correctly shapes the commitment-window revert boundary"
    - "The AUDIT.md output cross-cites (per D-04 format) the following prior-phase verdicts with a `re-verified at HEAD 1646d5af` note per cited row: 233-02-AUDIT JKP-02 D-06 entropy-passthrough backward-trace; 231-02-AUDIT EBD-02 earlybird bonus-trait salt isolation (BONUS_TRAITS_TAG); 232.1-03-PFTB-AUDIT non-zero-entropy availability at all 4 reachable `_processFutureTicketBatch` call sites per D-09 (this is the CROSS-CITE source for availability; diffusion is Phase 235's question)"
    - "Every verdict row uses vocabulary exactly `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`"
    - "Every verdict row carries a `Finding Candidate: Y/N` column value"
    - "No F-29-NN finding IDs appear anywhere in 235-03-AUDIT.md (per D-14)"
    - "Scope-guard deferrals, if any, recorded in 235-03-SUMMARY.md per D-15 (230-01-DELTA-MAP.md and 230-02-DELTA-ADDENDUM.md NOT edited)"
    - "Findings-Candidate Block, Scope-guard Deferrals, and Downstream Hand-offs subsections present; Phase 236 FIND-01 hand-off explicitly named for ID assignment"
    - "Zero contracts/ and zero test/ writes occur during plan execution (READ-only per D-17)"
  artifacts:
    - path: ".planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-03-AUDIT.md"
      provides: "RNG-01 per-consumer backward-trace proof across v29.0 delta"
      contains: "## Per-Consumer Backward-Trace Table"
  key_links:
    - from: "230-01-DELTA-MAP.md §1.1 + §1.2 + §1.4 + §2.3 IM-10..IM-16 + §2.5 IM-22 + §4 Consumer Index RNG-01 row"
      to: "235-03-AUDIT.md Per-Consumer Backward-Trace Table rows"
      via: "Every new RNG consumer in the delta has a backward-trace row"
      pattern: "Commitment Point|Input Variables|Proof Word-Was-Unknown"
    - from: "230-02-DELTA-ADDENDUM.md c2e5e0a9 + 314443af per-file tables"
      to: "235-03-AUDIT.md 17 per-site backward-trace rows + 314443af keccak-seed row"
      via: "Per D-07 + D-09, each c2e5e0a9 site + 314443af get a distinct row"
      pattern: "hash2|keccak256|baseKey|entropyWord|groupIdx"
    - from: "235-03-AUDIT.md VULNERABLE/DEFERRED/SAFE-INFO Candidate: Y verdicts"
      to: "Phase 236 FIND-01 finding-candidate pool"
      via: "Findings-Candidate Block (no F-29-NN IDs, Finding Candidate: Y/N column)"
      pattern: "## Findings-Candidate Block"
---

<objective>
RNG-01 analytical re-proof of backward-trace for every new RNG consumer in the v29.0 delta at HEAD `1646d5af`. Produces `235-03-AUDIT.md`: a Per-Consumer Backward-Trace Table covering all 5 new-consumer categories (earlybird bonus-trait, BAF sentinel, entropy passthrough, 17 c2e5e0a9 entropy-mixing sites per D-07, `_raritySymbolBatch` keccak-seed per D-09), each row proving the VRF word was unknown at input commitment time. Plus a mandatory `## 232.1 Ticket-Processing Impact` sub-section (per D-06), cross-cites to 233-02 JKP-02 + 231-02 EBD-02 + 232.1-03 PFTB-AUDIT each re-verified at HEAD `1646d5af`, Findings-Candidate Block, Scope-guard Deferrals, and Downstream Hand-offs.

Purpose: RNG-01 is the backward-trace half of the RNG commitment proof. A C4A warden attacks RNG-01 by looking for consumer sites that read / derive from a VRF word that was KNOWN at the time the input commitment was made. The backward-trace walks FROM the consumer's entropy input BACKWARD to the first VRF appearance in storage, confirming the word was unknown (i.e. a future-block VRF coordinator callback value) at every upstream commitment point. The v29.0 delta introduces FIVE categories of new consumer: (1) earlybird bonus-trait roll (20a951df shifted from per-winner `entropyStep` to shared `_rollWinningTraits(rngWord, true)`); (2) BAF `traitId=420` sentinel emission (104b5d42 — no new entropy derivation, just sentinel labeling, but backward-trace confirms `rngWord` arrives via pre-existing chain); (3) entropy passthrough to `processFutureTicketBatch` (52242a10 removed the body-top SLOAD and added a caller-provided `entropy` parameter); (4) 17 entropy-mixing sites (c2e5e0a9 replaced XOR+xorshift with keccak-formulation — new diffusion, same commitment point; each site is its own consumer per D-07); (5) `_raritySymbolBatch` keccak-seed fix (314443af — new keccak formulation for the LCG seed; per D-09 the non-zero-entropy availability is CROSS-CITED from 232.1-03-PFTB-AUDIT).

The commitment-window half is Plan 235-04 scope (RNG-02). Clean split per D-09: diffusion is Phase 235's question; availability is 232.1's closed question. Per D-14 no F-29-NN IDs. Per D-17 READ-only.

Output: `235-03-AUDIT.md` with Per-Consumer Backward-Trace Table (all 5 categories) + 232.1 Ticket-Processing Impact + Findings-Candidate Block + Scope-guard Deferrals + Downstream Hand-offs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-CONTEXT.md
@.planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md
@.planning/phases/230-delta-extraction-scope-map/230-02-DELTA-ADDENDUM.md
@.planning/phases/230-delta-extraction-scope-map/230-01-SUMMARY.md
@.planning/phases/231-earlybird-jackpot-audit/231-02-AUDIT.md
@.planning/phases/233-jackpot-baf-entropy-audit/233-02-AUDIT.md
@.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-PFTB-AUDIT.md
@.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-01-FIX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build RNG-01 Per-Consumer Backward-Trace Table covering 5 consumer categories (with 17 per-site c2e5e0a9 rows per D-07 + 314443af row per D-09) at HEAD 1646d5af; write 235-03-AUDIT.md</name>
  <files>.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-03-AUDIT.md</files>
  <read_first>
    - .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-CONTEXT.md (D-01 through D-17 — specifically D-07 c2e5e0a9 17 sites per-site backward-trace / D-09 314443af keccak-seed diffusion + 232.1-03 cross-cite for availability / D-06 mandatory 232.1 sub-section / D-14 no F-29-NN / D-15 scope-guard in SUMMARY / D-17 READ-only)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.1 (AdvanceModule — `advanceGame` MODIFIED by 2471f8e7 + 52242a10 threads rngWord into FF-promotion + near-future prep + post-transition FF call sites; `_processFutureTicketBatch` / `_prepareFutureTickets` signature bumps)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.2 (JackpotModule — `_runEarlyBirdLootboxJackpot` MODIFIED by 20a951df via shared `_rollWinningTraits(rngWord, true)`; `runBafJackpot` MODIFIED by 104b5d42 sentinel tagging — entropy chain unchanged)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.4 (MintModule — `processFutureTicketBatch` MODIFIED by 52242a10 — SLOAD removed; `_raritySymbolBatch` and `_rollRemainder` are internal callees that NOW consume the passed `entropy` + per addendum got keccak-seed fixes)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §2.3 IM-10 through IM-16 (entropy-passthrough chain + earlybird bonus-trait chain + BAF chain — all consumer chains)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §2.5 IM-22 (entropy-commitment-boundary replay row for Phase 235 RNG-01/02 cross-reference)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §4 Consumer Index RNG-01 row (authoritative scope anchor per D-04)
    - .planning/phases/230-delta-extraction-scope-map/230-02-DELTA-ADDENDUM.md — ENTIRE FILE:
        * 314443af MintModule `_raritySymbolBatch` at lines 559-571 — LCG seed now `uint256(keccak256(abi.encode(baseKey, entropyWord, groupIdx)))` (replacing `(baseKey + groupIdx) ^ entropyWord`). Finding candidate: N per addendum.
        * c2e5e0a9 17 sites per-file verdict table (page 2 of addendum):
          - `EntropyLib.sol:26-37` NEW `hash2(uint256, uint256) -> uint256` helper — NOT a consumer (it's the helper); the 16 JackpotModule sites are consumers that INVOKE it
          - `MintModule._rollRemainder` at line 647 — `hash2(entropy, rollSalt)`
          - `JackpotModule` 16 sites at lines 277, 443, 508, 522, 544, 594, 596, 607-609, 874, 937, 1134, 1238, 1345, 1681-1683, 1741, 1798-1800, 1808
          - `PayoutUtils._calcAutoRebuy` at line 68-70 — `keccak256(abi.encode(entropy, beneficiary, weiAmount))`
          Grand total per planning_context = 17 entropy-mixing sites (JackpotModule 16 sites counted as 16 individual line groups per addendum, counting line ranges like `607-609` as a single grouping). Each site gets a distinct row in the backward-trace table per D-07.
        * Per-site formulation groups from addendum page 2:
          - Group A (line 277): `_runDailyEthOnlyAndBonus` — `hash2(rngWord, targetLvl)` — 1 site
          - Group B (lines 443, 544, 594, 874, 1134): `payDailyJackpot` / `_executeJackpot` / `payDailyJackpotCoinAndTickets` / `_distributePurchasePhaseTickets` / `_resumeDailyEth` — `hash2(randWord, lvl)` — 5 sites
          - Group C (line 596): `payDailyJackpotCoinAndTickets` — `hash2(randWord, sourceLevel)` carryover — 1 site
          - Group D (lines 508, 522, 1681): `payDailyJackpot` coin branches / `_awardDailyCoinJackpotInRange` — `keccak256(abi.encode(randWord, lvl, COIN_JACKPOT_TAG))` — 3 sites
          - Group E (lines 607-609): `payDailyJackpotCoinAndTickets` coin near-budget — same formulation as Group D — 3 sites (one row or three rows at auditor discretion; prefer 3 separate rows for granularity)
          - Group F (lines 937, 1238, 1345, 1741): bucket loops — `keccak256(abi.encode(entropy, traitIdx, X))` — 4 sites
          - Group G (lines 1798-1800): `_awardFarFutureCoinJackpot` initial seed — `keccak256(abi.encode(rngWord, lvl, FAR_FUTURE_COIN_TAG))` — 3 sites
          - Group H (line 1808): `_awardFarFutureCoinJackpot` sample loop — `hash2(entropy, s)` — 1 site
          16-site JackpotModule total: 1+5+1+3+3+4+3+1 = 21 individual sites if 607-609 and 1681-1683 and 1798-1800 are counted as 3 sites each; but addendum counts the ranges as groups yielding 16 total. Per D-07 no shortcut: enumerate individually — aim for per-site row count in the range 16-21 for JackpotModule + 1 for MintModule._rollRemainder + 1 for PayoutUtils._calcAutoRebuy = 18-23 rows total for c2e5e0a9 + 1 row for 314443af. Per planning_context quality gate: MUST have at least 17 per-site backward-trace rows for c2e5e0a9 (the quality gate value).
    - .planning/phases/230-delta-extraction-scope-map/230-02-DELTA-ADDENDUM.md cryptographic collision analysis (page 2): `hash2(a,b)` hashes 64 bytes; `keccak256(abi.encode(a,b,c))` hashes 96 bytes; different lengths → disjoint preimages → no cross-formulation collisions. Domain separation via compile-time TAGs.
    - contracts/modules/DegenerusGameJackpotModule.sol at HEAD — for EACH of the 16 sites, read the surrounding code context to identify:
        * The function containing the site
        * The VRF-word source flowing into the hash2 / keccak call (e.g. `rngWord` parameter, `randWord` parameter, `entropy` local derived from a prior call)
        * The commitment point — where the VRF word was first known by the caller (typically back to `rngGate` or `_applyDailyRng` in AdvanceModule)
        * The input variables to the keccak (e.g. `rngWord, lvl, COIN_JACKPOT_TAG`)
        * Proof that the VRF word was unknown at input commitment time (trace backward to `rawFulfillRandomWords` at AdvanceModule:1702 VRF callback)
    - contracts/modules/DegenerusGameMintModule.sol at HEAD — `_rollRemainder` at line 647 — post-c2e5e0a9 uses `hash2(entropy, rollSalt)`. Read the function body + signature. Trace `entropy` parameter back through `_raritySymbolBatch` → `processFutureTicketBatch(lvl, entropy)` → AdvanceModule `_processFutureTicketBatch(lvl, entropy)` → `rngWord` from `rngGate` → `rngWordCurrent` → `rawFulfillRandomWords`. Record commitment point as the rngGate return site (AdvanceModule:283).
    - contracts/modules/DegenerusGameMintModule.sol at HEAD — `_raritySymbolBatch` at lines 540-571 (314443af MODIFIED). Read the new seed derivation: `uint256 seed = uint256(keccak256(abi.encode(baseKey, entropyWord, groupIdx)));` at (approximately) line 568. Inputs: `baseKey` (player+lvl+idx-derived), `entropyWord` (the `entropy` parameter from caller), `groupIdx` (`i >> 4` loop index). Record commitment point + backward-trace + verdict.
    - contracts/modules/DegenerusGamePayoutUtils.sol at HEAD — `_calcAutoRebuy` at lines 68-70. Read the keccak formulation: `keccak256(abi.encode(entropy, beneficiary, weiAmount))`. Inputs: `entropy` (caller-provided), `beneficiary` (address), `weiAmount` (uint256). Trace `entropy` back through the caller (whale-bundle auto-rebuy path — per Phase 234 CONS-02 discussion).
    - contracts/libraries/EntropyLib.sol at HEAD — `hash2(uint256, uint256) → uint256` at lines 26-37. Read the asm-scratch-slot implementation. Confirm memory-safety. Not a consumer itself (callers invoke it); record as a single row explaining the helper is equivalent to `uint256(keccak256(abi.encode(a, b)))`.
    - contracts/modules/DegenerusGameJackpotModule.sol at HEAD — `_runEarlyBirdLootboxJackpot` (20a951df MODIFIED). Read the `_rollWinningTraits(rngWord, true)` call site at (approximately) line 677. Trace `rngWord` back through caller `_consolidatePoolsAndRewardJackpots` → `rngGate` → `rngWordCurrent` → `rawFulfillRandomWords`. The bonus-trait roll is the NEW RNG consumer introduced by 20a951df.
    - contracts/modules/DegenerusGameJackpotModule.sol at HEAD — `runBafJackpot` (104b5d42 MODIFIED). The four emit sites pass `BAF_TRAIT_SENTINEL = 420` — NOT a new entropy derivation (the sentinel is a compile-time constant, not RNG-derived). However, `runBafJackpot` does consume `rngWord` internally (via `_addClaimableEth`, `_awardJackpotTickets`, `_queueWhalePassClaimCore`). 104b5d42 did NOT change these consumers. Record a single row for `runBafJackpot`'s `rngWord` parameter as a pre-existing RNG consumer UNCHANGED by 104b5d42.
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD — `advanceGame` at line 283-289 `rngGate(...)` call returning `rngWord` local; three consumers that now THREAD this local into delegatecall payloads per 52242a10:
        * IM-10 at line 315: `_processFutureTicketBatch(ffLevel, rngWord)` for phase-transition FF drain
        * IM-11 at line 337: `_prepareFutureTickets(purchaseLevel, rngWord)` for near-future prep (internally forwards to `_processFutureTicketBatch` at lines 1418 + 1428)
        * IM-12 at line 407: `_processFutureTicketBatch(nextLevel, rngWord)` for post-transition FF
        Each site is a new consumer per 52242a10 (pre-fix code SLOAD'd `rngWordCurrent` inside MintModule). Record one row per site.
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD — `rngGate` at line 1133 internal. Read the `uint256 currentWord = rngWordCurrent;` SLOAD at line 1143 — this is the FIRST READ of the VRF word in the advanceGame call flow. Record as the commitment point for all passthrough consumers.
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD — `rawFulfillRandomWords` at line 1690. Read the `if (word == 0) word = 1;` zero-guard at line 1698. Read the `rngWordCurrent = word;` SSTORE at line 1702. This is the VRF callback; prior to this callback the word is unknown to any on-chain actor.
    - .planning/phases/231-earlybird-jackpot-audit/231-02-AUDIT.md — EBD-02 bonus-trait salt-space isolation (BONUS_TRAITS_TAG = keccak256("BONUS_TRAITS")). Re-verify at HEAD 1646d5af by re-reading `contracts/modules/DegenerusGameJackpotModule.sol` constant decl for BONUS_TRAITS_TAG + `_rollWinningTraits` body to confirm tag is used for bonus branch.
    - .planning/phases/233-jackpot-baf-entropy-audit/233-02-AUDIT.md — JKP-02 D-06 backward-trace for the 52242a10 entropy passthrough at the three AdvanceModule call sites. Re-verify at HEAD by re-reading AdvanceModule:283-289 rngGate return + line 315/337/407 call sites + MintModule:385 receiver.
    - .planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-PFTB-AUDIT.md — the non-zero-entropy guarantee at all 4 reachable `_processFutureTicketBatch` call sites via rawFulfillRandomWords L1698 zero-guard + rngGate L291 sentinel-1 break + Plan 01 pre-drain gate. CROSS-CITE per D-09 for AVAILABILITY; Phase 235 RNG-01 owns DIFFUSION (does keccak diffuse properly).
    - .planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-01-FIX.md — Plan 01 pre-finalize gate at AdvanceModule:257-279 (Rev 2 final shape) + queue-length gate + nudged-word write (`cw = rngWordCurrent + totalFlipReversals`) + do-while integration + RngNotReady selector at L207 + L263. These are the 232.1 ticket-processing changes that affect the commitment window.
    - Output of `git rev-parse --short HEAD` — must match frontmatter `head_sha: 1646d5af`
  </read_first>
  <action>
Execute fresh-from-HEAD RNG-01 backward-trace re-proof at HEAD `1646d5af` per CONTEXT.md D-01 through D-17 and D-07 / D-08 / D-09 addendum treatment rules.

**Step 1 — Enumerate all 5 new-consumer categories per §4 Consumer Index RNG-01 row + 230-02 addendum per D-07 + D-09.** Target row count minimum per per_plan_content_guidance:
  - Category 1 (earlybird bonus-trait): 1-3 rows (primary row for `_rollWinningTraits(rngWord, true)` at `_runEarlyBirdLootboxJackpot` line 677 + cross-cite to 231-02 EBD-02 BONUS_TRAITS_TAG isolation)
  - Category 2 (BAF sentinel emission): 1-4 rows (the four emit sites pass `BAF_TRAIT_SENTINEL` — not a new entropy derivation; but `runBafJackpot` consumes `rngWord` internally for `_addClaimableEth` / `_awardJackpotTickets` / `_queueWhalePassClaimCore` — record one row for `rngWord` consumption as pre-existing-but-unchanged)
  - Category 3 (entropy passthrough): 3 rows (IM-10 / IM-11 / IM-12) + 1 row for MintModule.processFutureTicketBatch receiver (body consumption at lines 443/469/476/489) + 1 row for IM-13 delegatecall boundary. Cross-cite 233-02 JKP-02 D-06.
  - Category 4 (17 c2e5e0a9 sites per D-07): At least 17 individual rows (one per site per planning_context quality gate). Cover:
    - MintModule._rollRemainder line 647 — 1 row
    - JackpotModule 16 sites — at least 16 rows (one per line/line-range in addendum; can count 607-609, 1681-1683, 1798-1800 as separate rows for granularity, targeting 16-21 rows total for JackpotModule)
    - PayoutUtils._calcAutoRebuy line 68-70 — 1 row
    Total c2e5e0a9 rows: at least 18 (1 + 16 + 1) per addendum count; planning_context quality gate requires 17 — this can be satisfied by counting JackpotModule as 16 sites + 1 MintModule site = 17 (treating PayoutUtils as a 18th site beyond the 17-count floor). Auditor may choose to include PayoutUtils in the 17-count or list it separately — either satisfies the `at least 17` constraint.
  - Category 5 (314443af keccak-seed diffusion per D-09): 1 row for `_raritySymbolBatch` at MintModule lines 540-571 — seed derivation via `keccak256(abi.encode(baseKey, entropyWord, groupIdx))` — backward-trace proving VRF word was unknown at input commitment time. Cross-cite 232.1-03-PFTB-AUDIT for non-zero-entropy availability.

**Step 2 — For each row, fill columns per the D-07 schema: `Consumer | Site | File:Line | Commitment Point | Input Variables | Proof Word-Was-Unknown | Verdict | Finding Candidate`:**
  - **Consumer:** function name + what consumes (e.g. `JackpotModule._runEarlyBirdLootboxJackpot bonus-trait roll`)
  - **Site:** the specific keccak or entropyStep or hash2 call (e.g. `_rollWinningTraits(rngWord, true)` at line 677)
  - **File:Line:** absolute anchor in `contracts/` at HEAD 1646d5af
  - **Commitment Point:** where the VRF word was first known (typically `rngGate:1143` SLOAD of `rngWordCurrent`)
  - **Input Variables:** the keccak inputs (e.g. `rngWord, lvl, COIN_JACKPOT_TAG`)
  - **Proof Word-Was-Unknown:** backward-trace chain ending at `rawFulfillRandomWords:1702` SSTORE (VRF callback — word unknown before this point; cryptographically infeasible to predict)
  - **Verdict:** `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`
  - **Finding Candidate:** `Y` or `N`

**Step 3 — Write the mandatory `## 232.1 Ticket-Processing Impact` sub-section (per D-06).** Walk each 232.1 fix series change against RNG-01:
  - **Pre-finalize gate at AdvanceModule:257-263** — CLOSES the commitment-window hole where `processTicketBatch:690` could read `lootboxRngWordByIndex[X]` while still zero. The gate's lazy-finalize writes `lootboxRngWordByIndex[idx] = rngWordCurrent + totalFlipReversals` before the drain reads it. Backward-trace: the slot VALUE flows from `rngWordCurrent` (which was written by `rawFulfillRandomWords:1702`), so the downstream `_raritySymbolBatch(entropyWord)` consumer receives a value that was unknown at input commitment time.
  - **Queue-length gate** (R2.a Revision 2) — gates on `ticketQueue[preRk].length > 0`; no new entropy consumer introduced.
  - **Nudged-word write** (R2.b Revision 2) — `cw = rngWordCurrent; cw += totalFlipReversals;` — the nudge count is frozen within the advance tx (rngLockedFlag blocks reverseFlip), so the nudge count at the gate equals the nudge count at downstream consumers. Same entropy word reaches all consumers consistently.
  - **Do-while integration** (R2.c Revision 2) — preserves single-entropy-consumer invariant.
  - **Game-over best-effort drain** (749192cd Rev 3) — extra drain rounds in `_handleGameOverPath`; each consumer still receives non-zero entropy via the cross-cite from 232.1-03-PFTB-AUDIT (availability proof).
  - **RngNotReady selector fix** (26cea00b Rev 4 at L207 + L263) — correct revert selector; does not affect backward-trace (revert before consumption).
  Sub-section MUST explicitly state: "232.1 fix series closes a pre-existing RNG-01 commitment-window hole; every new RNG consumer introduced by the series receives a VRF word that was unknown at input commitment time via the backward-trace chain: gate writes `rngWordCurrent + totalFlipReversals` → downstream consumer reads via `lootboxRngWordByIndex[idx]` → the value traces to `rawFulfillRandomWords:1702` VRF callback."

**Step 4 — Write cross-cite rows (per D-04 format):**
  - 233-02-AUDIT JKP-02 D-06 entropy-passthrough backward-trace: re-verified at HEAD by re-reading AdvanceModule:283-289 rngGate return + line 315/337/407 call sites. Confirm backward-trace chain unchanged.
  - 231-02-AUDIT EBD-02 earlybird bonus-trait salt isolation (`BONUS_TRAITS_TAG = keccak256("BONUS_TRAITS")`): re-verified at HEAD by re-reading JackpotModule constant decl + `_rollWinningTraits` body.
  - 232.1-03-PFTB-AUDIT non-zero-entropy availability per D-09: CROSS-CITE (not re-derived). State: "Availability (entropy != 0 at all 4 reachable `_processFutureTicketBatch` call sites) is the subject of 232.1-03-PFTB-AUDIT — that audit closes availability via rawFulfillRandomWords L1698 zero-guard + rngGate L291 sentinel-1 break + Plan 01 pre-drain gate. Phase 235 RNG-01 cross-cites (does not re-derive) and focuses on DIFFUSION (does the keccak formulation diffuse all 256 bits properly). Clean split per D-09."

**Step 5 — Write `235-03-AUDIT.md` with required structure. Sections in this exact order:**

```markdown
# RNG-01 Analytical Audit: Per-Consumer Backward-Trace Re-Proof (v29.0 Delta at HEAD 1646d5af)

**Scope:** Every new RNG consumer in the 10-commit v29.0 delta + 2 post-Phase-230 addendum commits (`314443af`, `c2e5e0a9`) + 232.1 fix series. 5 consumer categories per 230-01 §4 Consumer Index RNG-01 + 230-02 addendum per D-07 + D-09.
**Method:** Fresh re-prove at HEAD `1646d5af` per D-03. Per-consumer backward-trace from consumer input to `rawFulfillRandomWords:1702` VRF callback. Cross-cite prior-phase verdicts per D-04 with `re-verified at HEAD 1646d5af` note.
**Verdict vocabulary:** `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`
**Finding-ID policy:** No `F-29-NN` IDs emitted (per D-14) — Phase 236 FIND-01 owns ID assignment.
**Scope-guard policy:** Per D-15 — any gap recorded in 235-03-SUMMARY.md, not catalog.
**D-09 split:** Diffusion is Phase 235's question; availability CROSS-CITES 232.1-03-PFTB-AUDIT.

## Methodology

[Paragraph: 5 consumer categories; D-07 per-site enumeration for c2e5e0a9 17 sites; D-09 314443af keccak-seed diffusion verdict + 232.1-03 cross-cite for availability; mandatory 232.1 Ticket-Processing Impact sub-section per D-06; backward-trace chain format ending at VRF callback.]

## Findings-Candidate Block

[Prose, no F-29-NN IDs. Expected: "No on-chain candidate findings — every new RNG consumer receives a VRF word unknown at input commitment time." If any path breaks the chain, record as VULNERABLE / Finding Candidate: Y.]

## Per-Consumer Backward-Trace Table

| Consumer | Site | File:Line | Commitment Point | Input Variables | Proof Word-Was-Unknown | Verdict | Finding Candidate |
|---|---|---|---|---|---|---|---|

### Category 1 — Earlybird Bonus-Trait Roll (20a951df)
| `JackpotModule._runEarlyBirdLootboxJackpot` bonus-trait roll | `_rollWinningTraits(rngWord, true)` | contracts/modules/DegenerusGameJackpotModule.sol:<line for call site> | `rngGate:1143` SLOAD of `rngWordCurrent` | `rngWord` + `BONUS_TRAITS_TAG` | rngWord sourced from rngGate (AdvanceModule:283-289) → rngWordCurrent SLOAD (1143) → rawFulfillRandomWords SSTORE (1702) → VRF callback. BONUS_TRAITS_TAG = keccak256("BONUS_TRAITS") domain-separates from non-bonus consumers. | SAFE | N |
| [additional rows for BONUS_TRAITS_TAG isolation + cite 231-02 EBD-02] | ... | ... | ... | ... | ... | ... | ... |

### Category 2 — BAF traitId=420 Sentinel Emission (104b5d42)
| `JackpotModule.runBafJackpot` rngWord consumption | `_addClaimableEth` / `_awardJackpotTickets` / `_queueWhalePassClaimCore` | contracts/modules/DegenerusGameJackpotModule.sol:<line> | `rngGate` return via caller | `rngWord` parameter | Pre-existing chain unchanged by 104b5d42 (sentinel is compile-time constant, not RNG-derived) | SAFE | N |

### Category 3 — Entropy Passthrough to processFutureTicketBatch (52242a10)
| `AdvanceModule.advanceGame` IM-10 FF-promotion | `_processFutureTicketBatch(ffLevel, rngWord)` | contracts/modules/DegenerusGameAdvanceModule.sol:315 | `rngGate:1143` | `rngWord` | rngGate return (283) → rngWordCurrent SLOAD (1143) → rawFulfillRandomWords (1702) | SAFE | N |
| `AdvanceModule.advanceGame` IM-11 near-future prep | `_prepareFutureTickets(purchaseLevel, rngWord)` | contracts/modules/DegenerusGameAdvanceModule.sol:337 | `rngGate:1143` | `rngWord` | Same chain as IM-10 | SAFE | N |
| `AdvanceModule._consolidatePoolsAndRewardJackpots` IM-12 post-transition FF | `_processFutureTicketBatch(nextLevel, rngWord)` | contracts/modules/DegenerusGameAdvanceModule.sol:407 | `rngGate:1143` | `rngWord` | Same chain as IM-10 | SAFE | N |
| `MintModule.processFutureTicketBatch` receiver body | `entropy` param consumption at L443 / L469 / L476 / L489 | contracts/modules/DegenerusGameMintModule.sol:385 | Delegatecall payload from IM-13 | `entropy` | Same chain — VRF word SSTORE at rawFulfillRandomWords:1702 | SAFE | N |
| IM-13 delegatecall boundary | `abi.encodeWithSelector(IDegenerusGameMintModule.processFutureTicketBatch.selector, lvl, entropy)` | contracts/modules/DegenerusGameAdvanceModule.sol:1390-1394 | Caller chain | `lvl, entropy` | byte-identical forwarding | SAFE | N |

### Category 4 — c2e5e0a9 Entropy-Mixing Sites (per D-07, 17 per-site rows minimum)
[One row per site — minimum 17. Grouped by file for readability but each site has its own row per D-07:]

#### MintModule (1 site)
| `MintModule._rollRemainder` | `EntropyLib.hash2(entropy, rollSalt)` | contracts/modules/DegenerusGameMintModule.sol:647 | Caller chain from `processFutureTicketBatch` | `entropy`, `rollSalt` | Same chain → VRF callback | SAFE | N |

#### JackpotModule (16 sites)
| `JackpotModule._runDailyEthOnlyAndBonus` | `hash2(rngWord, targetLvl)` | contracts/modules/DegenerusGameJackpotModule.sol:277 | `rngGate` return via caller | `rngWord`, `targetLvl` | Same chain | SAFE | N |
| `JackpotModule.payDailyJackpot` | `hash2(randWord, lvl)` | contracts/modules/DegenerusGameJackpotModule.sol:443 | `rngGate` return | `randWord`, `lvl` | Same chain | SAFE | N |
| `JackpotModule.payDailyJackpot` coin branch | `keccak256(abi.encode(randWord, lvl, COIN_JACKPOT_TAG))` | contracts/modules/DegenerusGameJackpotModule.sol:508 | `rngGate` return | `randWord`, `lvl`, `COIN_JACKPOT_TAG` | Same chain; TAG domain-separates | SAFE | N |
| `JackpotModule.payDailyJackpot` coin branch #2 | `keccak256(abi.encode(randWord, lvl, COIN_JACKPOT_TAG))` | contracts/modules/DegenerusGameJackpotModule.sol:522 | `rngGate` return | `randWord`, `lvl`, `COIN_JACKPOT_TAG` | Same chain | SAFE | N |
| `JackpotModule._executeJackpot` | `hash2(randWord, lvl)` | contracts/modules/DegenerusGameJackpotModule.sol:544 | `rngGate` return | `randWord`, `lvl` | Same chain | SAFE | N |
| `JackpotModule.payDailyJackpotCoinAndTickets` | `hash2(randWord, lvl)` | contracts/modules/DegenerusGameJackpotModule.sol:594 | `rngGate` return | `randWord`, `lvl` | Same chain | SAFE | N |
| `JackpotModule.payDailyJackpotCoinAndTickets` carryover | `hash2(randWord, sourceLevel)` | contracts/modules/DegenerusGameJackpotModule.sol:596 | `rngGate` return | `randWord`, `sourceLevel` | Same chain; sourceLevel decorrelates | SAFE | N |
| `JackpotModule.payDailyJackpotCoinAndTickets` coin near-budget site 1 | `keccak256(abi.encode(randWord, lvl, COIN_JACKPOT_TAG))` | contracts/modules/DegenerusGameJackpotModule.sol:607 | `rngGate` return | `randWord`, `lvl`, `COIN_JACKPOT_TAG` | Same chain | SAFE | N |
| `JackpotModule.payDailyJackpotCoinAndTickets` coin near-budget site 2 | same formulation | contracts/modules/DegenerusGameJackpotModule.sol:608 | `rngGate` return | `randWord`, `lvl`, `COIN_JACKPOT_TAG` | Same chain | SAFE | N |
| `JackpotModule.payDailyJackpotCoinAndTickets` coin near-budget site 3 | same formulation | contracts/modules/DegenerusGameJackpotModule.sol:609 | `rngGate` return | `randWord`, `lvl`, `COIN_JACKPOT_TAG` | Same chain | SAFE | N |
| `JackpotModule._distributePurchasePhaseTickets` | `hash2(randWord, lvl)` | contracts/modules/DegenerusGameJackpotModule.sol:874 | `rngGate` return | `randWord`, `lvl` | Same chain | SAFE | N |
| `JackpotModule._distributeTicketsToBuckets` | `keccak256(abi.encode(entropy, traitIdx, ticketUnits))` | contracts/modules/DegenerusGameJackpotModule.sol:937 | `rngGate` return via caller | `entropy`, `traitIdx`, `ticketUnits` | Same chain | SAFE | N |
| `JackpotModule._resumeDailyEth` | `hash2(randWord, lvl)` | contracts/modules/DegenerusGameJackpotModule.sol:1134 | `rngGate` return | `randWord`, `lvl` | Same chain | SAFE | N |
| `JackpotModule._processDailyEth` bucket loop | `keccak256(abi.encode(entropy, traitIdx, X))` | contracts/modules/DegenerusGameJackpotModule.sol:1238 | caller chain | `entropy`, `traitIdx`, `X` | Same chain | SAFE | N |
| `JackpotModule._processDailyEth` bucket loop #2 | same formulation | contracts/modules/DegenerusGameJackpotModule.sol:1345 | caller chain | same | Same chain | SAFE | N |
| `JackpotModule._awardDailyCoinJackpotInRange` | `keccak256(abi.encode(randWord, lvl, COIN_JACKPOT_TAG))` | contracts/modules/DegenerusGameJackpotModule.sol:1681 | caller chain | same | Same chain | SAFE | N |
| `JackpotModule._awardDailyCoinJackpotInRange` #2 | same formulation | contracts/modules/DegenerusGameJackpotModule.sol:1682 | caller chain | same | Same chain | SAFE | N |
| `JackpotModule._awardDailyCoinJackpotInRange` #3 | same formulation | contracts/modules/DegenerusGameJackpotModule.sol:1683 | caller chain | same | Same chain | SAFE | N |
| `JackpotModule._awardDailyCoinToTraitWinners` | `keccak256(abi.encode(entropy, traitIdx, traitShare))` | contracts/modules/DegenerusGameJackpotModule.sol:1741 | caller chain | `entropy`, `traitIdx`, `traitShare` | Same chain | SAFE | N |
| `JackpotModule._awardFarFutureCoinJackpot` initial seed #1 | `keccak256(abi.encode(rngWord, lvl, FAR_FUTURE_COIN_TAG))` | contracts/modules/DegenerusGameJackpotModule.sol:1798 | caller chain | `rngWord`, `lvl`, `FAR_FUTURE_COIN_TAG` | Same chain | SAFE | N |
| `JackpotModule._awardFarFutureCoinJackpot` initial seed #2 | same formulation | contracts/modules/DegenerusGameJackpotModule.sol:1799 | caller chain | same | Same chain | SAFE | N |
| `JackpotModule._awardFarFutureCoinJackpot` initial seed #3 | same formulation | contracts/modules/DegenerusGameJackpotModule.sol:1800 | caller chain | same | Same chain | SAFE | N |
| `JackpotModule._awardFarFutureCoinJackpot` sample loop | `hash2(entropy, s)` | contracts/modules/DegenerusGameJackpotModule.sol:1808 | caller chain | `entropy`, `s` | Same chain | SAFE | N |

#### PayoutUtils (1 site)
| `PayoutUtils._calcAutoRebuy` | `keccak256(abi.encode(entropy, beneficiary, weiAmount))` | contracts/modules/DegenerusGamePayoutUtils.sol:68 | caller chain (whale-bundle auto-rebuy) | `entropy`, `beneficiary`, `weiAmount` | Same chain → VRF callback | SAFE | N |

### Category 5 — `_raritySymbolBatch` Keccak-Seed Diffusion (314443af per D-09)
| `MintModule._raritySymbolBatch` LCG seed | `uint256(keccak256(abi.encode(baseKey, entropyWord, groupIdx)))` | contracts/modules/DegenerusGameMintModule.sol:<line for seed — ~568> | caller chain from `processFutureTicketBatch` (entropy parameter) | `baseKey`, `entropyWord`, `groupIdx` | Same chain — entropyWord traces to VRF callback. Non-zero-entropy availability CROSS-CITED from 232.1-03-PFTB-AUDIT per D-09 (rawFulfillRandomWords:1698 zero-guard + rngGate:291 sentinel-1 break + Plan 01 pre-drain gate). Diffusion: keccak256(abi.encode(uint256, uint256, uint32)) diffuses all inputs across all 256 output bits; `uint64(seed) | 1` draws from a cryptographically uniform low word. | SAFE | N |

## D-09 Non-Zero-Entropy Availability Cross-Cite

[Explicit sub-section. State: "Availability (entropy != 0 at all 4 reachable `_processFutureTicketBatch` call sites) is CROSS-CITED from `.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-PFTB-AUDIT.md` per D-09 clean split. Availability closure anchors: `rawFulfillRandomWords:1698` `if (word == 0) word = 1;` zero-guard; `rngGate:291` sentinel-1 break; Plan 01 pre-drain gate at AdvanceModule:257-279 (Rev 2 final shape). Phase 235 RNG-01 does NOT re-derive availability; it re-verifies the cite at HEAD `1646d5af` by re-reading `rawFulfillRandomWords` and `rngGate`. Re-verify result: PASS — the three anchors are present at HEAD unchanged from the 232.1-03-PFTB-AUDIT cite. Phase 235 RNG-01 focuses on DIFFUSION: does the keccak formulation at each consumer diffuse all 256 bits of the VRF word uniformly (answer: yes — keccak256 is a uniform hash, and addendum page 2 collision analysis confirms cross-formulation disjointness)."]

## 232.1 Ticket-Processing Impact

[Mandatory per D-06. Walk each 232.1 fix series change against RNG-01 as detailed in Step 3 above. Must state explicitly: "232.1 fix series closes a pre-existing RNG-01 commitment-window hole; every new RNG consumer introduced by the series receives a VRF word unknown at input commitment time."]

## 230-02 Addendum Impact

[Addendum commits 314443af + c2e5e0a9 introduce 17 new entropy-mixing sites (per D-07) + 1 keccak-seed fix (per D-09). All 17 sites have dedicated rows in Category 4; 314443af has a dedicated row in Category 5. Cryptographic collision analysis from addendum page 2 reaffirmed at HEAD (hash2 vs keccak256(abi.encode) length-disjointness; TAG-constant domain separation).]

## Cross-Cited Prior-Phase Verdicts (per D-04 format, re-verified at HEAD 1646d5af)

| Cited From | Verdict | Re-Verify Evidence at HEAD 1646d5af |
|---|---|---|
| 233-02-AUDIT JKP-02 D-06 entropy-passthrough backward-trace | PASS | [re-read AdvanceModule:283-289 rngGate return + line 315/337/407 call sites + MintModule:385 receiver at HEAD] |
| 231-02-AUDIT EBD-02 earlybird bonus-trait salt isolation (BONUS_TRAITS_TAG) | PASS | [re-read JackpotModule BONUS_TRAITS_TAG constant decl + `_rollWinningTraits` body at HEAD] |
| 232.1-03-PFTB-AUDIT non-zero-entropy availability (per D-09) | PASS | [re-read `rawFulfillRandomWords:1698` zero-guard + `rngGate:291` sentinel-1 break + Plan 01 pre-drain gate at AdvanceModule:257-279 at HEAD] |

## Scope-guard Deferrals

[Per D-15. If none, "None surfaced during this audit."]

## Downstream Hand-offs

- **Phase 236 FIND-01:** VULNERABLE/DEFERRED/SAFE-INFO Finding Candidate: Y rows route here for ID assignment.
- **Phase 235-04 RNG-02:** Commitment-window enumeration — sibling plan; RNG-02 owns the player-controllable state enumeration side.
- **Phase 236 REG-01:** Regression cross-check against v25.0 Phase 215 RNG audit — RNG-01 confirms no regression.
- **Phase 235-05 TRNX-01:** Phase-transition rngLocked invariant — RNG-01 cites the rngLocked guard as part of the commitment-window proof; TRNX-01 owns the deeper invariant analysis.
```

Every verdict row MUST:
- Use the locked D-07 column schema: `Consumer | Site | File:Line | Commitment Point | Input Variables | Proof Word-Was-Unknown | Verdict | Finding Candidate`.
- Cite real File:Line at HEAD `1646d5af` (never `<line>`).
- Use verdict vocabulary strictly from `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`.
- Carry `Y` or `N` in Finding Candidate.
- Never reference F-29-NN IDs.

Read contract sources ONLY from `contracts/` at HEAD per `feedback_contract_locations.md`.

READ-only per D-17. Only writes: `235-03-AUDIT.md` + (at output-step) `235-03-SUMMARY.md`. `git status --porcelain contracts/ test/` MUST be empty before/after task.
  </action>
  <verify>
    <automated>test -f .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-03-AUDIT.md && grep -q "## Per-Consumer Backward-Trace Table" .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-03-AUDIT.md && grep -q "## 232.1 Ticket-Processing Impact" .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-03-AUDIT.md && grep -q "## D-09 Non-Zero-Entropy Availability Cross-Cite" .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-03-AUDIT.md && ! grep -q "F-29-" .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-03-AUDIT.md && test -z "$(git status --porcelain contracts/ test/)"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-03-AUDIT.md` exists
    - File contains literal headers: `## Per-Consumer Backward-Trace Table`, `## D-09 Non-Zero-Entropy Availability Cross-Cite`, `## 232.1 Ticket-Processing Impact`, `## 230-02 Addendum Impact`, `## Cross-Cited Prior-Phase Verdicts`, `## Findings-Candidate Block`, `## Scope-guard Deferrals`, `## Downstream Hand-offs`
    - File contains 5 named sub-section headers under Per-Consumer Backward-Trace Table: Category 1 (earlybird bonus-trait), Category 2 (BAF sentinel), Category 3 (entropy passthrough), Category 4 (c2e5e0a9), Category 5 (`_raritySymbolBatch` keccak-seed)
    - Category 4 has at least 17 distinct rows (one per c2e5e0a9 entropy-mixing site per D-07) — count includes MintModule._rollRemainder (1) + JackpotModule sites (at least 16) + PayoutUtils._calcAutoRebuy (1) = minimum 17 total with the PayoutUtils row counted in the 17
    - Category 5 has at least 1 row for 314443af `_raritySymbolBatch` keccak-seed diffusion
    - Every Per-Consumer Backward-Trace Table row cites a File:Line anchor starting with `contracts/` at HEAD 1646d5af
    - Every row's Verdict is exactly `SAFE`, `SAFE-INFO`, `VULNERABLE`, or `DEFERRED`
    - Every row's Finding Candidate is `Y` or `N`
    - `grep -c "1646d5af" .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-03-AUDIT.md` returns at least 4 (header + 3 cross-cites)
    - `grep -c "re-verified at HEAD 1646d5af" .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-03-AUDIT.md` returns at least 3
    - Cross-Cited Prior-Phase Verdicts table has at least 3 rows: 233-02 JKP-02, 231-02 EBD-02, 232.1-03 PFTB-AUDIT
    - D-09 Non-Zero-Entropy Availability Cross-Cite sub-section explicitly mentions `rawFulfillRandomWords:1698` + `rngGate:291` + Plan 01 pre-drain gate
    - 232.1 Ticket-Processing Impact sub-section explicitly states "closes a pre-existing RNG-01 commitment-window hole" (or equivalent)
    - 230-02 Addendum Impact sub-section explicitly references "17 new entropy-mixing sites" + "1 keccak-seed fix"
    - The string `F-29-NN` does NOT appear in the file
    - The string `F-29-` does NOT appear in any form in the file
    - No placeholder line numbers like `:<line>` remain in the final file — every File:Line is a real integer
    - Downstream Hand-offs names `Phase 236 FIND-01`, `Phase 235-04 RNG-02`, `Phase 236 REG-01`, `Phase 235-05 TRNX-01`
    - `git status --porcelain contracts/ test/` returns empty both before and after task
    - Frontmatter `head_sha: 1646d5af` matches current HEAD
  </acceptance_criteria>
  <deliverable>
`235-03-AUDIT.md` with Per-Consumer Backward-Trace Table covering all 5 categories (including 17 per-site c2e5e0a9 rows per D-07 + 314443af keccak-seed row per D-09) + D-09 Non-Zero-Entropy Availability Cross-Cite + 232.1 Ticket-Processing Impact + 230-02 Addendum Impact + Cross-Cited Prior-Phase Verdicts + Findings-Candidate Block + Scope-guard Deferrals + Downstream Hand-offs. Zero F-29-NN emissions. Zero contracts/ or test/ writes.
  </deliverable>
  <done>`235-03-AUDIT.md` exists; every new RNG consumer has a backward-trace row proving VRF word was unknown at input commitment time; 17 per-site c2e5e0a9 rows; 314443af keccak-seed row; D-09 cross-cite to 232.1-03 for availability; 232.1 Ticket-Processing Impact sub-section; 3 cross-cites re-verified at HEAD 1646d5af per D-04.</done>
</task>

<task type="auto">
  <name>Task 2: Commit approved 235-03-AUDIT.md</name>
  <files>.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-03-AUDIT.md</files>
  <read_first>
    - .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-03-AUDIT.md (must exist from Task 1)
  </read_first>
  <action>
Stage and commit `235-03-AUDIT.md` only.

```
git add .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-03-AUDIT.md
git commit -m "docs(235-03): RNG-01 backward-trace re-proof at HEAD 1646d5af

Per-Consumer Backward-Trace Table covers 5 categories: earlybird bonus-trait
roll (20a951df); BAF sentinel emission (104b5d42); entropy passthrough to
processFutureTicketBatch (52242a10 — 3 AdvanceModule call sites + IM-13
delegatecall); 17 c2e5e0a9 entropy-mixing sites per D-07 (MintModule
_rollRemainder L647 + JackpotModule 16 sites + PayoutUtils _calcAutoRebuy);
314443af _raritySymbolBatch keccak-seed diffusion per D-09.

Every new consumer backward-traces to rawFulfillRandomWords:1702 VRF callback
— word was unknown at input commitment time.

D-09 non-zero-entropy availability CROSS-CITED from 232.1-03-PFTB-AUDIT
(rawFulfillRandomWords:1698 zero-guard + rngGate:291 sentinel-1 break + Plan
01 pre-drain gate). Clean split: diffusion is Phase 235, availability is
232.1.

232.1 Ticket-Processing Impact sub-section confirms fix series closes a
pre-existing commitment-window hole.

Cross-cites 233-02 JKP-02 + 231-02 EBD-02 + 232.1-03 PFTB-AUDIT re-verified
at HEAD per D-04.

Zero F-29-NN emissions (Phase 236 FIND-01 owns per D-14).
Zero contracts/ or test/ writes (READ-only per D-17)."
```

Do NOT push.
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -qE "235-03.*RNG-01.*1646d5af" && test -z "$(git status --porcelain contracts/ test/)"</automated>
  </verify>
  <acceptance_criteria>
    - One new commit subject starting `docs(235-03):` containing `RNG-01` + `1646d5af`
    - Commit touches only `.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-03-AUDIT.md`
    - `git status --porcelain contracts/ test/` empty
    - Not pushed
  </acceptance_criteria>
  <deliverable>
Approved 235-03-AUDIT.md committed; local-only.
  </deliverable>
  <done>Commit exists; contracts/ test/ unmodified.</done>
</task>

</tasks>

<verification>
- `235-03-AUDIT.md` exists with Per-Consumer Backward-Trace Table covering 5 consumer categories
- Category 4 has at least 17 per-site rows for c2e5e0a9 entropy-mixing sites (per D-07)
- Category 5 has a 314443af `_raritySymbolBatch` keccak-seed row (per D-09)
- D-09 Non-Zero-Entropy Availability Cross-Cite sub-section present citing 232.1-03-PFTB-AUDIT
- Mandatory `## 232.1 Ticket-Processing Impact` sub-section present (per D-06) confirming fix series closes commitment-window hole
- 3 cross-cites (233-02, 231-02, 232.1-03) each with `re-verified at HEAD 1646d5af` per D-04
- Every File:Line anchor resolves into `contracts/` at HEAD
- Verdicts use `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`
- Every row has `Y` or `N` Finding Candidate
- Zero `F-29-NN` or `F-29-` references (per D-14)
- Zero `contracts/` or `test/` writes (per D-17)
- Approved AUDIT.md committed with message `docs(235-03): RNG-01 backward-trace re-proof at HEAD 1646d5af`
</verification>

<success_criteria>
Every new RNG consumer in the v29.0 delta has a Per-Consumer Backward-Trace row proving the VRF word was unknown at input commitment time. 5 consumer categories covered with at least 17 per-site rows for c2e5e0a9 (per D-07) + 1 row for 314443af keccak-seed diffusion (per D-09). Availability cross-cited from 232.1-03-PFTB-AUDIT per D-09 clean split (Phase 235 owns diffusion; 232.1 owns availability). 3 prior-phase verdicts re-verified at HEAD 1646d5af per D-04. Mandatory 232.1 sub-section (D-06). Zero F-29-NN per D-14. Zero contracts/ or test/ writes per D-17.
</success_criteria>

<output>
After completion, create `.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-03-SUMMARY.md`
</output>
</content>
