---
phase: 343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca
plan: 02
type: execute
wave: 2
depends_on: ["343-01"]
files_modified:
  - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-SOLVENCY-PROOF.md
  - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-SOLVENCY-REDTEAM.md
autonomous: false
requirements: [SOLVENCY-01, SOLVENCY-03]

must_haves:
  truths:
    - "All 5 SOLVENCY-01 reservation sites are walked by name against source and each shown to reserve claimablePool (inclusive of the keeper total) with NO change: distributeYieldSurplus, gameOver drain pre-refund, gameOver drain post-refund, adminStakeEthForStEth, final sweep"
    - "SOLVENCY-03 is proven: the sDGNRS redemption valuation (ethBal + stethBal + claimableEth − pendingRedemptionEthValue) reads sDGNRS's OWN balance + only what the Game owes sDGNRS; keeper ETH lives in the Game's balance, invisible — unchanged + correct, same property as the external AfKing pool today"
    - "The GO_SWEPT withdraw-guard gap is LOCKED on paper: the final sweep zeroes claimablePool but does NOT iterate per-player keeperFunding[*], so withdrawKeeperFunding must revert post-sweep via the same GO_SWEPT latch _claimWinningsInternal uses, else claimablePool -= amount underflows"
    - "The OPEN-E 4-protection disposition is confirmed to carry over verbatim (fundingSource storage + subscribe-time consent gate + src resolution unchanged; funding-source ETH now keeperFunding[src], withdrawable by the source)"
    - "A focused adversarial red-team (D-07, /economic-analyst and/or /contract-auditor) attacks the proof — scoped to the solvency proof, not a full re-audit — and dispositions each charged probe; the GO_SWEPT gap is surfaced explicitly to it"
  artifacts:
    - path: ".planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-SOLVENCY-PROOF.md"
      provides: "The SOLVENCY-01 reservation-site walk + SOLVENCY-03 valuation proof + the GO_SWEPT withdraw-guard lock + the OPEN-E carry-over confirmation (SC2, SC3)"
      contains: "GO_SWEPT"
    - path: ".planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-SOLVENCY-REDTEAM.md"
      provides: "The D-07 focused adversarial red-team disposition log on the solvency proof"
      contains: "GO_SWEPT"
  key_links:
    - from: "343-SOLVENCY-PROOF.md"
      to: "claimablePool (the reserved aggregate the keeper total rides inside, per D-CF-03)"
      via: "each of the 5 reservation sites sums/zeroes claimablePool unchanged"
      pattern: "claimablePool"
    - from: "343-SOLVENCY-PROOF.md"
      to: "GameOverModule.handleFinalSweep:215 (zeroes claimablePool, not per-player keeperFunding) ↔ withdrawKeeperFunding"
      via: "GO_SWEPT latch revert-post-sweep lock (mirror _claimWinningsInternal:1463)"
      pattern: "GO_SWEPT"
---

<objective>
Author `343-SOLVENCY-PROOF.md` — the SOLVENCY-01 reservation-site walk + the SOLVENCY-03 sDGNRS-valuation proof + the GO_SWEPT withdraw-guard lock + the OPEN-E 4-protection carry-over confirmation — and then run the D-07 focused adversarial red-team (`343-SOLVENCY-REDTEAM.md`) that attacks that proof. This is the load-bearing solvency-spine deliverable: the keeper total rides inside `claimablePool` (D-CF-03), so the whole safety claim is "every free-ETH reservation site already covers the keeper total with zero edits" — which must be PROVEN against source, not assumed, then adversarially probed before any code is written.

Purpose: SOLVENCY-01/03 are the design-gating attestations the 344 IMPL diff and the 346 SOLVENCY-02 empirical proof build on. The genuine planning risk (RESEARCH Pitfall 1 / Open Q1) — the final sweep zeroes `claimablePool` but not per-player `keeperFunding[*]`, so a post-sweep `withdrawKeeperFunding` underflows `claimablePool -= amount` — is locked here (revert via the `GO_SWEPT` latch) and surfaced to the red-team. The 347 TERMINAL 3-skill sweep is far off, so the spine is front-loaded.
Output: Two markdown artifacts, `343-SOLVENCY-PROOF.md` + `343-SOLVENCY-REDTEAM.md`, in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-CONTEXT.md
@.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-RESEARCH.md
@.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-GREP-ATTESTATION.md

<interfaces>
<!-- ACTUAL lines from 343-GREP-ATTESTATION.md (Wave 1) — cite these, not the doc-drifted lines. -->
<!-- The full Reservation-Site Walk is in 343-RESEARCH.md "Solvency Reservation-Site Walk". -->
- SOLVENCY-01 site 1: JackpotModule.distributeYieldSurplus def :688; obligations sum incl. `claimablePool` at :693 (+ pending pools :703) — the [[project_yield_surplus_omits_pending_pools]] prior-omission site, now structurally immune (same variable)
- SOLVENCY-01 site 2: GameOverModule.handleGameOverDrain pre-refund :98-99 — `reserved = uint256(claimablePool)`
- SOLVENCY-01 site 3: GameOverModule.handleGameOverDrain post-refund :163 — `postRefundReserved = uint256(claimablePool)`
- SOLVENCY-01 site 4: DegenerusGame.adminStakeEthForStEth def :2109; reserve calc :2116-2119 — `reserve = claimablePool > stethSettleable ? claimablePool - stethSettleable : 0` (keeper funding is NOT stETH-settleable → stays in the ETH reserve)
- SOLVENCY-01 site 5: GameOverModule.handleFinalSweep def :202; `claimablePool = 0` :215; GO_SWEPT latch :206-208 — sweeps the aggregate, but does NOT iterate per-player keeperFunding[*] (Pitfall 1)
- SOLVENCY-03: StakedDegenerusStonk valuation :609-612 / :769-772 / :858-861 = `ethBal + stethBal + claimableEth − pendingRedemptionEthValue`; _claimableWinnings :955 → `game.claimableWinningsOf(this)` (owed to sDGNRS only); keeper ETH in the Game's balance → invisible
- GO_SWEPT guard precedent: _claimWinningsInternal :1462 def; checks `_goRead(GO_SWEPT_SHIFT, GO_SWEPT_MASK) != 0` at :1463 and reverts post-sweep — the withdrawKeeperFunding sketch (PLAN-V54 §3) lacks this
- OPEN-E carry-over: AfKing fundingSource storage; subscribe-time consent gate :399-409 (revert :407); src resolution :686 — all UNCHANGED; funding-source ETH now keeperFunding[src]
- D-CF-03: no new aggregate; keeperFunding total rides inside claimablePool (claimablePool == Σ claimableWinnings + Σ keeperFunding)
- D-01: the Game debit keys on keeperFunding[b.funder] (= src), NOT keeperFunding[b.player] — affects the OPEN-E reservation identity
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the SOLVENCY-01/03 proof + the GO_SWEPT withdraw-guard lock + the OPEN-E carry-over</name>
  <files>.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-SOLVENCY-PROOF.md</files>
  <read_first>
    - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-GREP-ATTESTATION.md (the ACTUAL lines — cite these)
    - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-RESEARCH.md ("Solvency Reservation-Site Walk" — all 5 sites + the SOLVENCY-03 valuation walk; "Common Pitfalls → Pitfall 1"; "Open Questions → Q1"; the Architectural Responsibility Map)
    - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-CONTEXT.md (D-CF-02 Decision B; D-CF-03 no aggregate; D-01 funder; the OPEN-E carry-over in DECUSTODY-03)
    - contracts/modules/DegenerusGameJackpotModule.sol (distributeYieldSurplus — confirm the claimablePool obligations term against source)
    - contracts/modules/DegenerusGameGameOverModule.sol (the drain pre/post-refund reserve calcs + the final sweep + the GO_SWEPT latch — confirm each against source)
    - contracts/DegenerusGame.sol (adminStakeEthForStEth reserve calc; _claimWinningsInternal + the GO_SWEPT revert guard at :1463 — quote it; the bare receive())
    - contracts/StakedDegenerusStonk.sol (the 3 redemption valuations + _claimableWinnings — confirm keeper ETH is invisible)
    - contracts/AfKing.sol (subscribe OPEN-E gate + src resolution — confirm unchanged)
    - .claude memory open-e-operator-approval-trust-boundary + project_yield_surplus_omits_pending_pools (the dispositions being carried over / made structurally immune)
  </read_first>
  <action>
    Author `343-SOLVENCY-PROOF.md`. Open with the D-CF-03 framing: there is NO new aggregate — `keeperFunding`'s systemwide total rides inside the existing `claimablePool` (`claimablePool == Σ claimableWinnings + Σ keeperFunding`), so the safety claim is "every free-ETH reservation site already reserves the keeper total via `claimablePool`, with zero edits." Then prove it. Section A (SOLVENCY-01) — walk all FIVE reservation sites BY NAME against source, each as a row/subsection with the ACTUAL file:line (from 343-GREP-ATTESTATION.md), how "reserved" is computed, and whether the keeper total is reserved: (1) `JackpotModule.distributeYieldSurplus` (obligations sum `claimablePool` at :693 — call out that this is the [[project_yield_surplus_omits_pending_pools]] prior-omission site, now STRUCTURALLY IMMUNE because the keeper total is the same `claimablePool` variable, not a separate omittable pool); (2) gameOver drain pre-refund (`reserved = claimablePool` :98-99); (3) gameOver drain post-refund (`reserved = claimablePool` :163, recomputed after refunds); (4) `adminStakeEthForStEth` (reserve keys off `claimablePool` minus the two stETH-settleable claimable buckets; keeper funding is NOT stETH-settleable so it correctly stays inside the ETH reserve — keeper ETH never staked-away); (5) `handleFinalSweep` (`claimablePool = 0` sweeps the keeper reservation with the aggregate). State the verdict: all 5 reserve `claimablePool` inclusive of the keeper total with ZERO edits — no reservation site lets the keeper total escape. Section B (the GO_SWEPT withdraw-guard lock — the single genuine planning risk, RESEARCH Pitfall 1 / Open Q1) — record that `handleFinalSweep:215` zeroes `claimablePool` but does NOT (cannot — unbounded) iterate per-player `keeperFunding[*]`, so after the sweep a player still holding `keeperFunding[player] != 0` while `claimablePool == 0` would underflow on `withdrawKeeperFunding`'s `claimablePool -= amount`. LOCK the fix: `withdrawKeeperFunding` MUST revert post-sweep via the same `GO_SWEPT` latch `_claimWinningsInternal` uses at :1463 (quote the precedent guard), consistent with GAMEOVER-02's "both withdraw/claim paths stay open until that sweep." State this is within the locked mechanism (a CEI/lifecycle detail), not a re-design, and that the 344 edit-order map must carry it. Section C (SOLVENCY-03) — prove the sDGNRS redemption valuation (`StakedStonk:609-612/:769-772/:858-861`, `ethBal + stethBal + claimableEth − pendingRedemptionEthValue`) is UNCHANGED and CORRECT: `ethBal` is sDGNRS's OWN balance, `claimableEth = _claimableWinnings()` reads only what the Game owes sDGNRS (`claimableWinningsOf(this)`); keeper ETH lives in the Game's balance and is owed to subscribers, invisible to this valuation — exactly the property the external AfKing pool had today, so no redemption-valuation edit is needed. Section D (OPEN-E carry-over) — confirm the 4-protection disposition (`open-e-operator-approval-trust-boundary`) carries over verbatim: the `fundingSource` storage + the subscribe-time operator-approval consent gate (:399-409, revert :407) + the `src` resolution (:686) are UNCHANGED; funding-source ETH is now `keeperFunding[src]`, withdrawable by the source; note the D-01 interaction (the Game debit keys on `keeperFunding[funder=src]`, so the reservation identity tracks the source, preserving the operator-funded case). End with a "charged probes for the D-07 red-team" subsection listing the attack surfaces to hand Task 2: reservation escape at each of the 5 sites, the GO_SWEPT withdraw-guard, the deposit-then-spend / fresh-rate-labeling economics, the un-brickable withdraw CEI, the D-01 funder/beneficiary split (OPEN-E operator-funded mis-account), and the no-new-aggregate invariant. Cite ACTUAL file:line throughout.
  </action>
  <acceptance_criteria>
    - 343-SOLVENCY-PROOF.md exists and walks all 5 SOLVENCY-01 sites BY NAME (distributeYieldSurplus, drain pre-refund, drain post-refund, adminStakeEthForStEth, final sweep), each citing claimablePool and an actual file:line
    - The doc records the [[project_yield_surplus_omits_pending_pools]] site as structurally immune under D-CF-03 (same claimablePool variable)
    - The doc LOCKS the GO_SWEPT withdraw-guard: withdrawKeeperFunding reverts post-sweep via the same GO_SWEPT latch _claimWinningsInternal uses, and explains the underflow it prevents
    - The doc proves SOLVENCY-03: the sDGNRS valuation reads sDGNRS's own balance + only claimableWinningsOf(sDGNRS); keeper ETH in the Game's balance is invisible; unchanged + correct
    - The doc confirms the OPEN-E 4-protection carry-over (fundingSource + consent gate :399-409 + src :686 unchanged; funding-source ETH = keeperFunding[src]) and notes the D-01 funder-keyed reservation identity
    - The doc ends with a charged-probes list for the D-07 red-team (incl. the GO_SWEPT guard and the D-01 split)
    - No contracts/*.sol modified: `git diff --name-only -- contracts/` is empty
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-SOLVENCY-PROOF.md; test -f "$F" && grep -q "distributeYieldSurplus" "$F" && grep -q "adminStakeEthForStEth" "$F" && grep -qiE "final sweep|handleFinalSweep" "$F" && grep -q "GO_SWEPT" "$F" && grep -qE "claimableWinningsOf|own balance|own-balance" "$F" && grep -qiE "OPEN-E|open-e" "$F" && grep -c "claimablePool" "$F" | awk '{exit ($1>=5)?0:1}' && [ -z "$(git diff --name-only -- contracts/)" ]</automated>
  </verify>
  <done>343-SOLVENCY-PROOF.md walks all 5 SOLVENCY-01 reservation sites against source (each reserving claimablePool inclusive of the keeper total, zero edits), locks the GO_SWEPT withdraw-guard, proves SOLVENCY-03 (keeper ETH invisible to the sDGNRS own-balance valuation), confirms the OPEN-E carry-over, and lists the charged probes for the red-team. Zero contracts/ edits.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the D-07 focused adversarial red-team on the solvency proof + USER-adjudicate the verdict</name>
  <files>.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-SOLVENCY-REDTEAM.md</files>
  <read_first>
    - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-SOLVENCY-PROOF.md (the proof to attack + its charged-probes list)
    - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-RESEARCH.md ("User Constraints → D-07" scope; "Audit skill interfaces" table; Pitfall 1/2/3; Open Q1)
    - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-CONTEXT.md (D-07 — scoped to the proof, NOT a full re-audit; Claude's Discretion on which skill(s))
    - ~/.claude/skills/economic-analyst/SKILL.md and ~/.claude/skills/contract-auditor/SKILL.md (the persona contracts)
    - contracts/DegenerusGame.sol + contracts/modules/DegenerusGameGameOverModule.sol + contracts/modules/DegenerusGameJackpotModule.sol + contracts/AfKing.sol (the reservation sites + withdraw + auto-buy the red-team probes against source)
  </read_first>
  <action>
    This is a CHECKPOINT task — the detailed work is specified in `<what-built>` and `<how-to-verify>` below and the run PAUSES for USER adjudication. In short: invoke the D-07 focused adversarial red-team (`/economic-analyst` AND `/contract-auditor`) SCOPED TO THE SOLVENCY PROOF ONLY (not a full re-audit), author `343-SOLVENCY-REDTEAM.md` dispositioning every charged probe from `343-SOLVENCY-PROOF.md` (NEGATIVE-VERIFIED / SAFE_BY_DESIGN / FINDING_CANDIDATE) with source-cited reasoning — surfacing the GO_SWEPT withdraw-guard gap (RESEARCH Pitfall 1 / Open Q1) as an explicit charged probe — then HOLD for the USER to adjudicate the verdict before 344 IMPL (an unresolved solvency hole is the high-severity block-on item). Do NOT edit any `contracts/*.sol`.
  </action>
  <what-built>
    The executor invokes the D-07 focused adversarial red-team (per D-07: `/economic-analyst` and/or `/contract-auditor` — planner's call is BOTH, since the proof spans economics [deposit-then-spend / fresh-rate labeling] and security [reservation escape, un-brickable withdraw CEI, the GO_SWEPT underflow]) SCOPED TO THE SOLVENCY PROOF ONLY (not a full re-audit), and authors `343-SOLVENCY-REDTEAM.md` recording each charged probe with a disposition (NEGATIVE-VERIFIED / SAFE_BY_DESIGN / FINDING_CANDIDATE) and reasoning against source. The charged probes are the list from 343-SOLVENCY-PROOF.md: (a) reservation escape at each of the 5 SOLVENCY-01 sites; (b) the GO_SWEPT withdraw-guard — does the locked revert-post-sweep close the underflow, or is there a residual hole; (c) deposit-then-spend / fresh-rate economics (can an actor mislabel keeper ETH or drain the fresh rate); (d) the un-brickable withdraw CEI (re-entrant double-withdraw reverts on the debit); (e) the D-01 funder/beneficiary split (OPEN-E operator-funded mis-account if the debit ever keyed on player). The GO_SWEPT gap (RESEARCH Pitfall 1 / Open Q1) MUST be surfaced as an explicit charged probe. The red-team output is recorded; any FINDING_CANDIDATE is flagged for USER adjudication — an UNRESOLVED solvency hole is the high-severity block-on item for this phase.
  </what-built>
  <how-to-verify>
    1. Confirm `343-SOLVENCY-REDTEAM.md` exists and dispositions every charged probe from the proof's list (NEGATIVE-VERIFIED / SAFE_BY_DESIGN / FINDING_CANDIDATE), each with source-cited reasoning.
    2. Confirm the GO_SWEPT withdraw-guard probe is present and its disposition is recorded.
    3. Review whether any probe is FINDING_CANDIDATE (an unresolved "keeper total escapes a reservation site" or post-sweep underflow hole). If so, decide: does the SPEC's locked GO_SWEPT revert (and the D-CF-03 no-aggregate design) fully close it, or must the design be amended before 344 IMPL?
    4. Type "approved" to confirm the solvency proof survives the red-team (and the GO_SWEPT lock is sufficient), or describe the hole / required design amendment.
  </how-to-verify>
  <resume-signal>Type "approved" if the solvency proof + GO_SWEPT lock survive the red-team, or describe the unresolved solvency hole / required design change.</resume-signal>
  <acceptance_criteria>
    - 343-SOLVENCY-REDTEAM.md exists and was produced by invoking /economic-analyst and/or /contract-auditor scoped to the solvency proof (not a full re-audit)
    - Every charged probe from 343-SOLVENCY-PROOF.md is dispositioned (NEGATIVE-VERIFIED / SAFE_BY_DESIGN / FINDING_CANDIDATE) with source-cited reasoning
    - The GO_SWEPT withdraw-guard probe is present and dispositioned
    - The USER has adjudicated the verdict (approved, or a design amendment captured) — an unresolved solvency hole is the high-severity block-on item
    - No contracts/*.sol modified: `git diff --name-only -- contracts/` is empty
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-SOLVENCY-REDTEAM.md; test -f "$F" && grep -q "GO_SWEPT" "$F" && grep -qE "NEGATIVE-VERIFIED|SAFE_BY_DESIGN|FINDING_CANDIDATE" "$F" && grep -qiE "economic-analyst|contract-auditor" "$F" && [ -z "$(git diff --name-only -- contracts/)" ]</automated>
  </verify>
  <done>343-SOLVENCY-REDTEAM.md records the D-07 focused red-team disposition on every charged probe (incl. the GO_SWEPT guard), and the USER has adjudicated the verdict — confirming the solvency proof survives or capturing the required design amendment. Zero contracts/ edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Paper-only SPEC phase; no code ships. The threat surface is the design/solvency risks the SPEC must close on paper before the 344 IMPL diff is written. The keeper bucket rides inside `claimablePool` (the master invariant `balance + steth.balanceOf(this) >= claimablePool`), so the load-bearing concern is whether the keeper total can escape any reservation site.

| Boundary | Description |
|----------|-------------|
| keeperFunding total ↔ the 5 free-ETH reservation sites | If any "free ETH = totalBal − reserved" site fails to reserve the keeper total, keeper ETH is distributed/staked/drained away → insolvency vs the master invariant. |
| final sweep ↔ per-player withdrawKeeperFunding | The sweep zeroes the aggregate `claimablePool` but not per-player `keeperFunding[*]`; an un-guarded post-sweep withdraw underflows `claimablePool -= amount` (RESEARCH Pitfall 1). |
| OPEN-E operator (funder=src) ↔ subscriber (beneficiary=player) | If the reservation/debit keys on `player` instead of `funder` (D-01), the operator-funded case mis-accounts/reverts and the OPEN-E disposition breaks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-343-03 | Information Disclosure (of funds) / Denial of Service | the 5 SOLVENCY-01 reservation sites | mitigate | Walk each site against source; prove each reserves `claimablePool` inclusive of the keeper total with zero edits (D-CF-03); the D-07 red-team adversarially probes for an escape at each. The high-severity block-on item is any UNRESOLVED reservation escape. |
| T-343-04 | Denial of Service (un-withdrawable / underflow-bricked) | handleFinalSweep:215 ↔ withdrawKeeperFunding | mitigate | LOCK the GO_SWEPT revert-post-sweep guard (mirror `_claimWinningsInternal:1463`) so a post-sweep withdraw reverts cleanly instead of underflowing `claimablePool`; surface to the red-team as a charged probe; carry into the 344 edit-order map. |
| T-343-05 | Tampering (mis-accounting) | the D-01 funder/beneficiary split on the OPEN-E operator-funded slice | mitigate | The reservation identity tracks `keeperFunding[funder=src]`, not `keeperFunding[player]`; the SOLVENCY-03 / OPEN-E section confirms the funding-source ETH stays `src`-keyed and withdrawable by the source, preserving the 4-protection disposition. |
| T-343-06 | Spoofing (fresh-rate mislabel) | deposit-then-spend / fresh affiliate-rate economics | mitigate | The red-team's economic probe checks keeper ETH spent as `ethValue` (DirectEth/fresh) cannot be mislabeled or drained; keeper funding stays a SEPARATE bucket precisely to keep the fresh/recycled labeling honest. |
| T-343-SC | Tampering | npm/pip/cargo installs | accept | No package installs — paper-only Markdown authoring + skill invocations (RESEARCH "Standard Stack": no Package Legitimacy Audit needed). |
</threat_model>

<verification>
- 343-SOLVENCY-PROOF.md + 343-SOLVENCY-REDTEAM.md exist in the phase directory.
- All 5 SOLVENCY-01 sites walked by name; SOLVENCY-03 proven; the GO_SWEPT withdraw-guard locked; the OPEN-E carry-over confirmed.
- The D-07 red-team dispositioned every charged probe (incl. GO_SWEPT) and the USER adjudicated the verdict.
- No contracts/*.sol modified: `git diff --name-only -- contracts/` is empty.
</verification>

<success_criteria>
- 343-SOLVENCY-PROOF.md satisfies all Task 1 acceptance criteria (ROADMAP Phase 343 Success Criteria 2 + 3).
- 343-SOLVENCY-REDTEAM.md satisfies all Task 2 acceptance criteria (D-07 red-team + USER adjudication).
- The GO_SWEPT withdraw-guard is locked and the OPEN-E 4-protection carry-over confirmed.
- Zero contracts/*.sol edits.
</success_criteria>

<output>
Create `.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-02-SUMMARY.md` when done
</output>
