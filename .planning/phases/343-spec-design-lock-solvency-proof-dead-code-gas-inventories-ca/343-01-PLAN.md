---
phase: 343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-GREP-ATTESTATION.md
autonomous: true
requirements: [BATCH-01, GAS-01]

must_haves:
  truths:
    - "git diff --numstat 83a84431 HEAD -- contracts/ is recorded as EMPTY (the working tree IS the frozen v53 baseline; grep on the live tree IS grep on 83a84431)"
    - "Every cited file:line across the milestone scope is re-pinned to its ACTUAL current line with the matched source text quoted; drift is corrected"
    - "batchPurchase drift :1809 → :1824 is recorded with the matched function-definition text"
    - "The NAME DRIFT is recorded: DegenerusAffiliate.payAffiliate does NOT exist; the function is handleAffiliate (:36) with fresh constants REWARD_SCALE_FRESH_L1_3_BPS=2_500 / _L4P_BPS=2_000"
    - "The single-interface payable finding is recorded: batchPurchase is declared payable in exactly ONE interface (AfKing-local IGame :43), NOT in contracts/interfaces/ (only a comment at IDegenerusGameModules.sol:237)"
    - "The double invariant comment is recorded: the master-invariant comment appears TWICE (DegenerusGame.sol:5 AND :18); PLAN-V54 §5 cites only :18"
    - "No 'by construction' / 'single fn reaches all paths' claim survives un-checked (feedback_verify_call_graph_against_source floor attested)"
  artifacts:
    - path: ".planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-GREP-ATTESTATION.md"
      provides: "The call-graph attestation + Drift-Correction Table re-pinning every milestone-scope anchor vs the v53 HEAD 83a84431, with the 3 name/ABI/comment corrections (SC5)"
      contains: "83a84431"
  key_links:
    - from: "343-GREP-ATTESTATION.md"
      to: "the v53 HEAD 83a84431 (contracts/ byte-identical at working HEAD)"
      via: "git diff --numstat empty + per-anchor file:line confirmation with matched text"
      pattern: "83a84431"
    - from: "343-GREP-ATTESTATION.md"
      to: "contracts/DegenerusGame.sol:1824 (batchPurchase def — the :1809 drift)"
      via: "grep re-pin of the doc-cited line to the actual definition line"
      pattern: "batchPurchase.*1824"
---

<objective>
Author `343-GREP-ATTESTATION.md` — the call-graph attestation + Drift-Correction Table that re-pins EVERY cited `file:line` across the v54.0 milestone scope to its ACTUAL current line in the live tree (byte-identical to the v53 HEAD `83a84431`), with the matched source text quoted. This is the load-bearing deliverable that every other 343 doc cites — the solvency proof, the cleanup inventory, the gas inventory, and the edit-order map all build on the actual lines this plan pins.

The RESEARCH.md already performed this verification and produced the authoritative Drift-Correction Table. This plan's job is to FORMALIZE that table as the SPEC attestation deliverable and re-confirm each row against source — NOT to re-discover it.

Purpose: The IMPL phase (344) authors its single batched diff against ACTUAL lines, not the drifted doc lines (which are off by 1-15 and two are name-drifted). The audit-discipline maxim — "no 'by construction' / 'single fn reaches all paths' claim survives un-checked" — is enforced here.
Output: One markdown artifact, `343-GREP-ATTESTATION.md`, in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-CONTEXT.md
@.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-RESEARCH.md

<interfaces>
<!-- The authoritative Drift-Correction Table is in 343-RESEARCH.md "Call-Graph Attestation — Drift-Correction Table". -->
<!-- git diff --numstat 83a84431 HEAD -- contracts/ is EMPTY (RESEARCH-verified) — working tree IS the frozen baseline. -->
<!-- Key anchors to re-pin (actual lines, per RESEARCH): -->
- DegenerusGame.sol — batchPurchase :1824 (doc :1809 DRIFT +15); BatchBuy struct :1796; purchaseWith selector call :1838 (doc :1839); keeperSnapshot :2645; _claimWinningsInternal def :1462 (doc :1471 is a body line); adminStakeEthForStEth def :2109 (reserve calc :2116-2119); master-invariant comment :5 AND :18; bare receive() :2915; _processMintPayment prizeContribution :968/:981/:1003
- DegenerusGameStorage.sol — invariant comment :345-354, claimablePool decl :355, claimableWinnings :402; keeperFunding ABSENT (new in 344)
- AfKing.sol — _poolOf :214; receive :298; deposit :305; depositFor :314; withdraw :328 (CEI debit :334); subscribe def :381, OPEN-E gate :399-409 (revert :407), msg.value credit :413; poolOf view :492; src resolution :686; funding-skip :695 + VAULT/SDGNRS exemption on player :696; CEI debit :719; BatchBuy struct :20; buys build :726; batched call :768 ({value:totalValue}); IGame :40, batchPurchase payable ABI :43 (the ONLY interface decl), keeperSnapshot ABI :56; per-player snapshot fallback :809; "ABI-identical" doc notes :16/:30; sum(_poolOf)<=balance invariant doc :117
- DegenerusGameJackpotModule.sol — distributeYieldSurplus def :688, claimablePool term :693
- DegenerusGameGameOverModule.sol — drain pre-refund reserve :98-99, post-refund reserve :163, final sweep :215 (GO_SWEPT latch :206-208)
- StakedDegenerusStonk.sol — valuation :609-612/:769-772/:858-861, _claimableWinnings :955, burnAtGameOver def :535 + AfKing withdraw leg :539, receive() AF_KING relaxation :441
- DegenerusVault.sol — recoverAfKingPool def :516, leg :517
- DegenerusAffiliate.sol — NAME DRIFT: payAffiliate does NOT exist; fn is handleAffiliate :36; fresh constants :164 (REWARD_SCALE_FRESH_L1_3_BPS=2_500) / :165 (_L4P_BPS=2_000); doc :18-21
- IDegenerusGameModules.sol — batchPurchase comment-only :237 (no payable decl)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the call-graph attestation + Drift-Correction Table vs 83a84431</name>
  <files>.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-GREP-ATTESTATION.md</files>
  <read_first>
    - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-RESEARCH.md ("Call-Graph Attestation — Drift-Correction Table" — the complete authoritative table, every row with matched text; plus "State of the Art" Deprecated/outdated + "Common Pitfalls → Pitfall 4")
    - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-CONTEXT.md (D-MR-02 line drift is real; D-01 funder; the canonical_refs anchor list)
    - contracts/DegenerusGame.sol (re-confirm batchPurchase, BatchBuy, purchaseWith call, keeperSnapshot, _claimWinningsInternal, adminStakeEthForStEth, the two invariant comments :5/:18, receive, _processMintPayment — do NOT trust the doc-cited lines; grep/read each)
    - contracts/storage/DegenerusGameStorage.sol (invariant comment :345-354, claimablePool :355, claimableWinnings :402; confirm keeperFunding ABSENT)
    - contracts/AfKing.sol (every anchor in the interfaces block above — _poolOf, receive/deposit/depositFor/withdraw, subscribe + OPEN-E gate, poolOf, src, funding-skip + VAULT/SDGNRS exemption, CEI debit, BatchBuy, build, batched call, IGame block, fallback, invariant doc)
    - contracts/modules/DegenerusGameJackpotModule.sol (distributeYieldSurplus + the claimablePool obligations term)
    - contracts/modules/DegenerusGameGameOverModule.sol (drain pre/post-refund reserve, final sweep + GO_SWEPT latch)
    - contracts/StakedDegenerusStonk.sol (valuation x3, _claimableWinnings, burnAtGameOver + AfKing leg, receive AF_KING)
    - contracts/DegenerusVault.sol (recoverAfKingPool)
    - contracts/DegenerusAffiliate.sol (CONFIRM payAffiliate does NOT exist; handleAffiliate :36; fresh constants :164/:165)
    - contracts/interfaces/IDegenerusGameModules.sol (the batchPurchase comment-only :237 — confirm NO payable decl in contracts/interfaces/)
  </read_first>
  <action>
    First run `git diff --numstat 83a84431 HEAD -- contracts/` and confirm it returns empty — record at the top of the doc that the working tree's `contracts/` is byte-identical to the v53 HEAD `83a84431`, so grep against the working tree IS a valid attestation against `83a84431` (no checkout needed). Then author `343-GREP-ATTESTATION.md` as the call-graph attestation deliverable, structured as per-file Drift-Correction Tables with columns: Symbol | Doc-cited line | Actual line | Matched source text (quoted) | Status (MATCH / DRIFT(±N) / NAME-DRIFT / CONFIRMED-NEW / FOUND). Transcribe the complete authoritative table from 343-RESEARCH.md and RE-CONFIRM each row by grep/read against the live `contracts/` tree (verify, do not copy blindly). MUST cover every file in the interfaces block: DegenerusGame.sol, DegenerusGameStorage.sol, AfKing.sol, DegenerusGameJackpotModule.sol, DegenerusGameGameOverModule.sol, StakedDegenerusStonk.sol, DegenerusVault.sol, DegenerusAffiliate.sol, IDegenerusGameModules.sol. Call out THREE corrections prominently in their own labeled subsection ("Corrections beyond simple line drift"): (1) the batchPurchase :1809 → :1824 drift (+15); (2) the NAME DRIFT — `DegenerusAffiliate.payAffiliate` does NOT exist, the function is `handleAffiliate` at :36 with fresh-rate constants `REWARD_SCALE_FRESH_L1_3_BPS=2_500` (:164) / `REWARD_SCALE_FRESH_L4P_BPS=2_000` (:165) — PLAN-V54 §10 / AUTOBUY-03 cite the wrong name and the SPEC corrects it; (3) the single-interface payable finding — `batchPurchase` is declared `payable` in exactly ONE interface, the AfKing-local `IGame` at `AfKing.sol:43`, NOT in `contracts/interfaces/` (only a comment at `IDegenerusGameModules.sol:237`) — so the CLEANUP "IGame.batchPurchase payable ABI" target narrows to that one site + the comment. ALSO record the double invariant comment: the master-invariant comment appears TWICE (`DegenerusGame.sol:5` AND `:18`); PLAN-V54 §5 #1 cites only `:18` → flag that BOTH copies must be updated at 344. Confirm `keeperFunding` is ABSENT from storage (grep returns nothing → CONFIRMED-NEW in 344). End with a one-line attestation that NO "by construction" / "single fn reaches all paths" claim survives un-checked (the `feedback_verify_call_graph_against_source` floor) and a "Valid until" note (re-run if the subject HEAD moves before 344).
  </action>
  <acceptance_criteria>
    - 343-GREP-ATTESTATION.md exists and records that `git diff --numstat 83a84431 HEAD -- contracts/` is empty (working tree = frozen v53 baseline); contains the string "83a84431"
    - The Drift-Correction Table contains a row for `batchPurchase` showing doc-cited :1809 → actual :1824 with the matched function-definition text quoted
    - The doc records the NAME DRIFT: "handleAffiliate" is present, and it states that `payAffiliate` does NOT exist; the fresh constants 2_500 / 2_000 are recorded
    - The doc records the single-interface payable finding: `batchPurchase` payable ABI is at `AfKing.sol:43` (the only interface decl), with the `IDegenerusGameModules.sol:237` comment-only note
    - The doc records the double invariant comment at DegenerusGame.sol :5 AND :18 (both must update at 344)
    - The doc records that `keeperFunding` is ABSENT from DegenerusGameStorage.sol (CONFIRMED-NEW in 344)
    - The doc ends with the no-"by-construction"-survives-unchecked attestation
    - No contracts/*.sol modified: `git diff --name-only -- contracts/` is empty
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-GREP-ATTESTATION.md; test -f "$F" && grep -q "83a84431" "$F" && grep -qE "batchPurchase.*1824|1824.*batchPurchase" "$F" && grep -q "handleAffiliate" "$F" && grep -q "payAffiliate" "$F" && grep -q "AfKing.sol:43" "$F" && grep -qE ":5\b|:5 AND|:5 and" "$F" && grep -q ":18" "$F" && [ -z "$(git diff --name-only -- contracts/)" ]</automated>
  </verify>
  <done>343-GREP-ATTESTATION.md records the empty-diff baseline identity vs 83a84431, re-pins every milestone-scope anchor to its actual line with matched text and drift corrected, surfaces the three corrections (batchPurchase :1824, handleAffiliate name, single-interface payable) + the double invariant comment, confirms keeperFunding absent, and ends with the no-"by-construction" attestation. Zero contracts/ edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Paper-only SPEC phase; no code ships. The threat surface is the correctness + completeness of the anchor attestation that the rest of 343 (solvency proof, cleanup inventory, gas inventory, edit-order map) and the 344 IMPL diff cite. The design/solvency risk this plan closes on paper is "a stale or drifted anchor lets the IMPL author against the wrong line."

| Boundary | Description |
|----------|-------------|
| Doc-cited lines → live source | Doc citations (ROADMAP/REQUIREMENTS/PLAN-V54/CONTEXT) are off by 1-15 lines and two are name-drifted; an un-re-pinned anchor sends the 344 edit to the wrong place or a non-existent symbol. |
| SPEC attestation → IMPL 344 | An un-attested "by construction" claim survives into IMPL and a wrong-surface diff ships. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-343-01 | Tampering | grep-attestation Drift-Correction Table | mitigate | Every cited file:line re-confirmed by grep/read against the live `contracts/` tree at the empty-diff baseline (`83a84431`); drift corrected; the three name/ABI/comment corrections surfaced explicitly; no "by construction" survives un-checked (`feedback_verify_call_graph_against_source`). |
| T-343-02 | Spoofing | non-existent `payAffiliate` reference | mitigate | The NAME DRIFT is recorded — `payAffiliate` does not exist; the real fn is `handleAffiliate:36` — so the 344 edit-order map and the AUTOBUY-03 fresh-rate justification cite a real symbol. |
| T-343-SC | Tampering | npm/pip/cargo installs | accept | No package installs — paper-only Markdown authoring (RESEARCH "Standard Stack": no Package Legitimacy Audit needed; zero installs). |
</threat_model>

<verification>
- 343-GREP-ATTESTATION.md exists in the phase directory.
- Empty-diff baseline identity vs 83a84431 recorded; the batchPurchase :1824 drift, the handleAffiliate name correction, the single-interface payable finding, and the double invariant comment all present.
- keeperFunding confirmed absent from storage.
- No contracts/*.sol modified: `git diff --name-only -- contracts/` is empty.
</verification>

<success_criteria>
- 343-GREP-ATTESTATION.md satisfies all Task 1 acceptance criteria (ROADMAP Phase 343 Success Criterion 5, attestation slice).
- Every milestone-scope anchor re-pinned to its actual line with matched source text.
- Zero contracts/*.sol edits.
</success_criteria>

<output>
Create `.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-01-SUMMARY.md` when done
</output>
