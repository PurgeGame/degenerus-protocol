---
phase: 343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca
plan: 03
type: execute
wave: 2
depends_on: ["343-01"]
files_modified:
  - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-CLEANUP-INVENTORY.md
  - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-GAS-INVENTORY.md
autonomous: true
requirements: [CLEANUP-01, GAS-01]

must_haves:
  truths:
    - "The CLEANUP-01 de-custody kill-set lists every orphan with its actual location AND a repo-wide caller grep proving it is truly orphaned after the batched removal"
    - "recoverAfKingPool is listed with a `grep -rn recoverAfKingPool contracts/` caller-count of 0 external callers (only its own def)"
    - "AfKing.poolOf and AfKing.withdraw are shown orphaned ONLY after the v48 recovery legs (StakedStonk:539 + Vault:517) are removed — the kill-set integrity gate (D-06)"
    - "The IGame.batchPurchase payable ABI kill target is NARROWED to the single interface decl (AfKing.sol:43) + the comment at IDegenerusGameModules.sol:237 (NOT contracts/interfaces/)"
    - "The GAS-01 advisory candidate list is produced by running /gas-scavenger over the blast-radius files, each candidate tagged behavior-identical / same-results, marked ADVISORY/UNVALIDATED (345 gas-skeptic is the gate)"
    - "The claimableWinnings {uint128 normal, uint128 keeper} packing candidate is documented with the PLAN-V54 §2 framing (zero hot-path benefit; ~15+ access-site blast radius; trades against feedback_security_over_gas) and FLAGGED for the 345 gas-skeptic (D-04) — NOT evaluated/decided here; default = keep the separate mapping"
  artifacts:
    - path: ".planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-CLEANUP-INVENTORY.md"
      provides: "The CLEANUP-01 grep-attested de-custody kill-set with per-item orphan-after-removal proof + the D-06 integrity gate (SC4)"
      contains: "recoverAfKingPool"
    - path: ".planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-GAS-INVENTORY.md"
      provides: "The GAS-01 /gas-scavenger advisory candidate list + the packing-candidate framing flagged for 345 (SC5)"
      contains: "gas-scavenger"
  key_links:
    - from: "343-CLEANUP-INVENTORY.md"
      to: "AfKing.withdraw / AfKing.poolOf (orphaned only after StakedStonk:539 + Vault:517 recovery legs removed)"
      via: "repo-wide caller grep + the D-06 kill-set integrity gate (remove recovery legs before/with the views)"
      pattern: "recoverAfKingPool|poolOf|withdraw"
    - from: "343-GAS-INVENTORY.md"
      to: "the 345 gas-skeptic validation gate"
      via: "ADVISORY candidate list (D-02) + the packing-candidate framing (D-04), unvalidated"
      pattern: "gas-skeptic|345"
---

<objective>
Author two inventory deliverables, both grep-attested against the actual lines pinned in 343-GREP-ATTESTATION.md (Wave 1):

1. `343-CLEANUP-INVENTORY.md` (CLEANUP-01) — the de-custody dead-code kill-set: every item the AfKing de-custody orphans, each with its actual location AND a repo-wide caller grep proving it is truly orphaned after the batched removal, plus the D-06 kill-set integrity gate (the views `poolOf`/`withdraw` are orphaned ONLY after the v48 recovery legs are removed in the same batched diff).
2. `343-GAS-INVENTORY.md` (GAS-01) — the gas-opportunity ADVISORY candidate list produced by running `/gas-scavenger` (D-02) over the keeper/funding blast radius, each candidate tagged behavior-identical / same-results and marked UNVALIDATED (345 `/gas-skeptic` is the gate), PLUS the `claimableWinnings` packing candidate documented with the PLAN-V54 §2 framing and flagged for the 345 gas-skeptic (D-04).

Purpose: CLEANUP-01 feeds the CLEANUP-02 removal in the 344 batched diff (and proves nothing live is removed). GAS-01 feeds the 345 GAS phase's gas-skeptic validation. Both are paper-only enumerations — no application here.
Output: Two markdown artifacts in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-CONTEXT.md
@.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-RESEARCH.md
@.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-GREP-ATTESTATION.md

<interfaces>
<!-- ACTUAL lines from 343-GREP-ATTESTATION.md (Wave 1). The full kill-set + gas framing are in 343-RESEARCH.md. -->
<!-- CLEANUP-01 kill-set (RESEARCH "CLEANUP-01 Dead-Code Kill-Set Inventory") — 14 items: -->
- #1 AfKing._poolOf mapping (slot 0) :214 — internal refs only, all in kill-set/rewired fns
- #2 AfKing.receive() :298 ; #3 deposit() :305 ; #4 depositFor() :314 (grep `\.depositFor(`: 0 hits)
- #5 AfKing.withdraw() :328 — external callers StakedStonk:539 + Vault:517 (both in v48 recovery legs #9/#10)
- #6 AfKing.poolOf() view :492 — same two recovery legs only
- #7 local CEI debit `_poolOf[src] -= ethValue` :719
- #8 IGame.batchPurchase PAYABLE ABI :43 (the ONLY interface decl; NOT in contracts/interfaces/; comment-only at IDegenerusGameModules.sol:237) — changes payable→non-payable in place
- #9 DegenerusVault.recoverAfKingPool() :516 (grep `recoverAfKingPool`: 0 external callers)
- #10 StakedStonk.burnAtGameOver AfKing-withdraw leg :539 (the line; the fn body stays)
- #11 sDGNRS receive() AF_KING relaxation :441 (narrow to GAME-only after #10)
- #12 sum(_poolOf)<=balance invariant doc :117 ; #13 stale _poolOf comments :84,:117,:143,:193,:370,:447
- #14 Deposited event (emit sites :301,:308,:318,:414 — all in kill-set fns; FLAG for 344 confirmation)
<!-- D-06 kill-set integrity gate: #5/#6 orphaned ONLY after #9/#10 removed → producer-before-consumer order note for 344. -->
<!-- New AfKing IGame ABI to ADD (not a kill): depositKeeperFunding, withdrawKeeperFunding, keeperFundingOf, extended keeperSnapshot return; batchPurchase :43 goes non-payable. -->
<!-- GAS-01 blast-radius files (RESEARCH "GAS-01 Gas-Opportunity Inventory — Scope & Framing", D-03 reach): -->
- DegenerusGame.sol (batchPurchase :1824, _claimWinningsInternal :1462, keeperSnapshot :2645, adminStakeEthForStEth :2109; new deposit/withdraw/keeperFundingOf)
- DegenerusGameStorage.sol (claimablePool :355, claimableWinnings :402; new keeperFunding)
- AfKing.sol (de-custodied loop + _resolveBuy + :809 fallback + IGame block)
- contracts/interfaces/IDegenerusGame*.sol ; DegenerusGameJackpotModule.sol (:688) ; DegenerusGameGameOverModule.sol (drain :86, sweep :202)
- StakedDegenerusStonk.sol + DegenerusVault.sol (recovery-leg removals) ; (read-only) sDGNRS valuation :609-612/:769-772/:858-861
<!-- D-04 packing-candidate framing: {uint128 normal, uint128 keeper} — width-safe; zero hot-path benefit; ~15+ access-site blast radius; trades against feedback_security_over_gas; flag for 345 gas-skeptic; default = keep separate mapping; do NOT evaluate here. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the CLEANUP-01 grep-attested de-custody kill-set inventory</name>
  <files>.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-CLEANUP-INVENTORY.md</files>
  <read_first>
    - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-GREP-ATTESTATION.md (the ACTUAL lines — cite these for every kill-set item)
    - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-RESEARCH.md ("CLEANUP-01 Dead-Code Kill-Set Inventory" — the 14-item table with caller greps; "Kill-set integrity gate (D-06)"; "New AfKing IGame ABI to ADD")
    - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-CONTEXT.md (D-05 poolOf deleted entirely; D-06 v48 recovery hard-removed only after grep-attest each leg orphaned; DECUSTODY-04)
    - contracts/AfKing.sol (_poolOf, receive/deposit/depositFor/withdraw, poolOf, CEI debit, IGame block, invariant doc, stale comments, Deposited emit sites — confirm each location and internal-ref set)
    - contracts/StakedDegenerusStonk.sol (burnAtGameOver AfKing-withdraw leg :539, receive AF_KING relaxation :441)
    - contracts/DegenerusVault.sol (recoverAfKingPool :516)
    - contracts/interfaces/IDegenerusGameModules.sol (confirm batchPurchase is comment-only at :237; no payable decl)
  </read_first>
  <action>
    Author `343-CLEANUP-INVENTORY.md` as the grep-attested de-custody kill-set. Structure as a table with columns: # | Kill-set item | Location (actual file:line) | Kill-set grep target | Remaining external callers (repo-wide, non-test) | Orphaned after removal?. Transcribe the 14-item kill-set from 343-RESEARCH.md and RE-RUN each caller grep against the live tree to confirm (record the exact `grep -rn` command per item). MUST include: #1 `AfKing._poolOf` slot 0 (:214, internal-only refs all in kill-set/rewired fns); #2-#4 `receive`/`deposit`/`depositFor` (record `grep -rn '\.depositFor(' contracts/` returns 0 hits); #5 `withdraw()` + #6 `poolOf()` view — record their external callers are EXACTLY `StakedStonk:539` + `Vault:517`, both inside the v48 recovery legs (#9/#10) being removed, so they are orphaned ONLY after #9/#10 go (the D-06 integrity gate); #7 the local CEI debit `_poolOf[src] -= ethValue` :719; #8 the `IGame.batchPurchase` PAYABLE ABI — NARROW the kill target to the single interface decl at `AfKing.sol:43` (the ONLY interface decl) + the comment at `IDegenerusGameModules.sol:237`, and note it changes payable→non-payable IN PLACE (not a deletion); #9 `DegenerusVault.recoverAfKingPool()` :516 (record `grep -rn 'recoverAfKingPool' contracts/` returns 0 external callers — only its own def); #10 the `burnAtGameOver` AfKing-withdraw leg :539 (the line is removed; the fn body stays); #11 the sDGNRS `receive()` AF_KING relaxation :441 (narrow to GAME-only after #10); #12 the `sum(_poolOf) <= balance` invariant doc :117; #13 the stale `_poolOf`-referencing comments; #14 the `Deposited` event (all 4 emit sites in kill-set fns → FLAG for 344 confirmation, not asserted final). Add a "D-06 kill-set integrity gate" subsection: #5/#6 are orphaned ONLY after #9/#10 are removed in the same batched diff → the producer-before-consumer order must remove the recovery legs before/atomically-with deleting `poolOf`/`withdraw` (carry to the 344 edit-order map). Add a "New AfKing IGame ABI to ADD (not a kill)" note: `depositKeeperFunding` / `withdrawKeeperFunding` / `keeperFundingOf` / the extended `keeperSnapshot` return go INTO the same `IGame` block, and `batchPurchase:43` goes non-payable. Cite ACTUAL file:line per item.
  </action>
  <acceptance_criteria>
    - 343-CLEANUP-INVENTORY.md exists and lists every kill-set item with its actual location + a repo-wide caller grep
    - `recoverAfKingPool` is listed with a recorded `grep -rn recoverAfKingPool contracts/` showing 0 external callers (only its own def)
    - AfKing.withdraw + AfKing.poolOf are recorded as orphaned ONLY after the v48 recovery legs (StakedStonk:539 + Vault:517) are removed — the D-06 integrity gate is present
    - The IGame.batchPurchase payable ABI kill target is NARROWED to AfKing.sol:43 + the IDegenerusGameModules.sol:237 comment (NOT contracts/interfaces/), payable→non-payable in place
    - The "New AfKing IGame ABI to ADD" note lists depositKeeperFunding/withdrawKeeperFunding/keeperFundingOf + the extended keeperSnapshot
    - No contracts/*.sol modified: `git diff --name-only -- contracts/` is empty
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-CLEANUP-INVENTORY.md; test -f "$F" && grep -q "recoverAfKingPool" "$F" && grep -q "D-06" "$F" && grep -q "AfKing.sol:43" "$F" && grep -q "depositKeeperFunding" "$F" && grep -qE "StakedStonk.*539|539" "$F" && [ -z "$(git diff --name-only -- contracts/)" ]</automated>
  </verify>
  <done>343-CLEANUP-INVENTORY.md lists every de-custody orphan with its actual location + a repo-wide caller grep, records recoverAfKingPool at 0 external callers, gates poolOf/withdraw behind the v48-recovery-leg removal (D-06), narrows the payable-ABI kill target to AfKing.sol:43 + the IDegenerusGameModules.sol:237 comment, and notes the new IGame ABI additions. Zero contracts/ edits.</done>
</task>

<task type="auto">
  <name>Task 2: Run /gas-scavenger for the GAS-01 advisory candidate list + document the packing candidate</name>
  <files>.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-GAS-INVENTORY.md</files>
  <read_first>
    - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-GREP-ATTESTATION.md (the ACTUAL blast-radius lines)
    - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-RESEARCH.md ("GAS-01 Gas-Opportunity Inventory — Scope & Framing" — the blast-radius file list + the D-04 packing framing + "Note for the planner: gas-scavenger is aggressive-by-design")
    - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-CONTEXT.md (D-02 run gas-scavenger now, advisory ONLY; D-03 reach; D-04 packing candidate documented + flagged for 345, NOT decided here)
    - ~/.claude/skills/gas-scavenger/SKILL.md (the persona contract — aggressive, produces candidates for The Skeptic to validate)
    - contracts/DegenerusGame.sol, contracts/storage/DegenerusGameStorage.sol, contracts/AfKing.sol, contracts/modules/DegenerusGameJackpotModule.sol, contracts/modules/DegenerusGameGameOverModule.sol, contracts/StakedDegenerusStonk.sol, contracts/DegenerusVault.sol, contracts/interfaces/IDegenerusGame*.sol (the blast-radius — the gas-scavenger reads these)
  </read_first>
  <action>
    Invoke `/gas-scavenger` (D-02) over the GAS-01 blast-radius files (D-03 reach: the touched files + the accounting spine the solvency proof walks — `DegenerusGame.sol`, `DegenerusGameStorage.sol`, `AfKing.sol`, `contracts/interfaces/IDegenerusGame*.sol`, `DegenerusGameJackpotModule.sol`, `DegenerusGameGameOverModule.sol`, `StakedDegenerusStonk.sol`, `DegenerusVault.sol`, plus the read-only sDGNRS valuation) and author `343-GAS-INVENTORY.md` recording the raw ADVISORY candidate list. Each candidate: a row with location (actual file:line), the proposed change, the expected gas saving, and a behavior-identical / same-results tag. Mark the WHOLE list ADVISORY / UNVALIDATED at the top — explicitly state that `/gas-scavenger` is aggressive-by-design, candidates that trade an invariant or aren't real are EXPECTED, and the 345 `/gas-skeptic` is the validation gate (do NOT validate or apply anything here). Record that the baseline is the ~9k/buy already saved by removing the per-batch value call, and GAS-01 enumerates wins BEYOND that. Then, in a dedicated "Packing candidate (D-04 — flagged for 345, NOT decided here)" section, document the `claimableWinnings` `{uint128 normal, uint128 keeper}` packing alternative with the PLAN-V54 §2 framing: width-safe (uint128 ≫ ETH supply; `claimablePool` already uint128) but ZERO hot-path benefit (an auto-buy touches one player slot + `claimablePool` either way) and a ~15+ access-site blast radius on the central accounting variable (every `claimableWinnings` credit/debit + `_processMintPayment` / `_settleClaimableShortfall` / `claimWinnings` / `claimableWinningsOf` / StakedStonk reads), trading against `feedback_security_over_gas`. State the DEFAULT EXPECTATION = keep the separate `keeperFunding` mapping, and that this candidate is FLAGGED for the 345 gas-skeptic — NOT evaluated or decided at 343. Do NOT use any "v1/simplified/for-now" language.
  </action>
  <acceptance_criteria>
    - 343-GAS-INVENTORY.md exists, was produced by invoking /gas-scavenger, and records the candidate list with each candidate tagged behavior-identical / same-results
    - The doc is marked ADVISORY / UNVALIDATED with the 345 gas-skeptic named as the validation gate (no validation/application performed here)
    - The doc records the ~9k/buy baseline and that GAS-01 enumerates wins beyond it
    - The packing candidate is documented with the PLAN-V54 §2 framing (zero hot-path benefit; ~15+ access-site blast radius; trades against feedback_security_over_gas) and flagged for 345; default = keep the separate mapping
    - No "v1"/"simplified"/"for now" scope-reduction language
    - No contracts/*.sol modified: `git diff --name-only -- contracts/` is empty
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-GAS-INVENTORY.md; test -f "$F" && grep -qi "gas-scavenger" "$F" && grep -qiE "gas-skeptic|345" "$F" && grep -qi "advisory" "$F" && grep -qiE "packing|uint128" "$F" && grep -qi "security_over_gas\|security-over-gas\|security over gas" "$F" && ! grep -qiE "\bv1\b|simplified version|for now" "$F" && [ -z "$(git diff --name-only -- contracts/)" ]</automated>
  </verify>
  <done>343-GAS-INVENTORY.md records the /gas-scavenger advisory candidate list (behavior-identical tagged, UNVALIDATED, 345 is the gate) over the blast radius, and documents the claimableWinnings packing candidate with the PLAN-V54 §2 framing flagged for 345 — no validation, no application, no scope-reduction language. Zero contracts/ edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Paper-only SPEC phase; no code ships. The threat surface is the correctness of the kill-set (removing something still live bricks the build / a path) and the discipline of the gas inventory (a candidate applied without the 345 validation gate could trade an invariant).

| Boundary | Description |
|----------|-------------|
| CLEANUP-01 kill-set → CLEANUP-02 removal (344) | If an item is marked orphaned but still has a live caller, the 344 batched diff removes a referenced symbol → broken build / dangling reference. |
| v48 recovery legs ↔ poolOf/withdraw views | `poolOf`/`withdraw` are orphaned ONLY after the recovery legs go; wrong removal order leaves an intermediate dangling reference (D-06). |
| GAS-01 advisory list → 345 gas-skeptic | A gas candidate treated as validated (instead of advisory) could be applied at 345 without skeptic review, trading a solvency invariant against `feedback_security_over_gas`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-343-07 | Denial of Service (broken build / dangling ref) | the CLEANUP-01 kill-set | mitigate | Every kill-set item carries a repo-wide caller grep proving it is orphaned after removal; the D-06 integrity gate sequences the v48-recovery-leg removal before `poolOf`/`withdraw` deletion; the `Deposited` event + `IGame` ABI items are FLAGGED for 344 confirmation rather than asserted final. |
| T-343-08 | Tampering (silent invariant trade) | the GAS-01 candidate list | mitigate | The list is marked ADVISORY / UNVALIDATED with the 345 `/gas-skeptic` named as the only gate; the packing candidate carries the `feedback_security_over_gas` framing and a default of keeping the separate mapping — no win is applied at 343. |
| T-343-SC | Tampering | npm/pip/cargo installs | accept | No package installs — paper-only Markdown authoring + a `/gas-scavenger` skill invocation (RESEARCH "Standard Stack": no Package Legitimacy Audit needed). |
</threat_model>

<verification>
- 343-CLEANUP-INVENTORY.md + 343-GAS-INVENTORY.md exist in the phase directory.
- Kill-set: every item has a caller grep; recoverAfKingPool at 0 callers; poolOf/withdraw gated behind the recovery-leg removal (D-06); the payable-ABI target narrowed to AfKing.sol:43 + the comment.
- Gas inventory: /gas-scavenger advisory list (behavior-identical tagged), 345 gas-skeptic named as the gate, the packing candidate framed + flagged, no scope-reduction language.
- No contracts/*.sol modified: `git diff --name-only -- contracts/` is empty.
</verification>

<success_criteria>
- 343-CLEANUP-INVENTORY.md satisfies all Task 1 acceptance criteria (ROADMAP Phase 343 Success Criterion 4).
- 343-GAS-INVENTORY.md satisfies all Task 2 acceptance criteria (ROADMAP Phase 343 Success Criterion 5, gas slice).
- Zero contracts/*.sol edits.
</success_criteria>

<output>
Create `.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-03-SUMMARY.md` when done
</output>
