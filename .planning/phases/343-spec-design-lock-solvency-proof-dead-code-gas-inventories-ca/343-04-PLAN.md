---
phase: 343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca
plan: 04
type: execute
wave: 3
depends_on: ["343-01", "343-02", "343-03"]
files_modified:
  - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-IMPL-EDIT-ORDER-MAP.md
autonomous: true
requirements: [BATCH-01]

must_haves:
  truths:
    - "The final signatures are locked: non-payable batchPurchase(BatchBuy[]) (payable + spent==msg.value guard removed); depositKeeperFunding(address) payable; un-brickable CEI withdrawKeeperFunding(uint256) WITH the GO_SWEPT revert-post-sweep guard; keeperFundingOf(address) view; the extended keeperSnapshot returning keeperFunding[player]; the post-gameOver _claimWinningsInternal keeper-merge (Decision B)"
    - "The keeperFunding storage shape is locked: mapping(address => uint256) keeperFunding on DegenerusGameStorage.sol with NO separate aggregate; the invariant comment (BOTH copies DegenerusGame.sol:5 AND :18, + DegenerusGameStorage.sol:345-354) updated to claimablePool == Σ claimableWinnings + Σ keeperFunding"
    - "The D-01 BatchBuy.funder correction is recorded as an explicit correction to REQUIREMENTS AUTOBUY-02 and PLAN-V54 §4: both BatchBuy structs (AfKing:20 + Game:1796) gain a funder field; the Game debit keys on keeperFunding[b.funder] (=src), NOT keeperFunding[b.player]; the VAULT/SDGNRS exemption stays keyed on the un-spoofable player"
    - "The D-MR-01 keeperSnapshot src carve-out is recorded: AUTOBUY-05's 'ONE staticcall per player' is REFINED — the OPEN-E src≠player slice needs one extra game.keeperFundingOf(src) staticcall (mirror AfKing.sol:809); common path unchanged"
    - "The handleAffiliate name correction is recorded (payAffiliate does NOT exist) where AUTOBUY-03 / PLAN-V54 §10 justify the fresh-rate-separate-bucket"
    - "The producer-before-consumer edit-order map is fixed with ACTUAL lines: storage keeperFunding + BatchBuy.funder → Game (deposit/withdraw+GO_SWEPT/batchPurchase/keeperFundingOf/_claimWinningsInternal merge/extended keeperSnapshot/both invariant comments) → interfaces → AfKing de-custody + funder=src wiring + v48-recovery removal (recovery legs before poolOf/withdraw deletion, D-06); no file ships an intermediate broken state"
  artifacts:
    - path: ".planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-IMPL-EDIT-ORDER-MAP.md"
      provides: "The BATCH-01 design-lock (final signatures + storage shape + wiring) + the producer-before-consumer edit-order map for the 344 IMPL diff, with the D-01 / D-MR-01 / handleAffiliate / GO_SWEPT / double-invariant-comment corrections (SC1)"
      contains: "funder"
  key_links:
    - from: "343-IMPL-EDIT-ORDER-MAP.md"
      to: "REQUIREMENTS AUTOBUY-02 + PLAN-V54 §4 (both say keeperFunding[b.player])"
      via: "explicit D-01 correction → keeperFunding[b.funder] (=src); BatchBuy gains funder"
      pattern: "funder"
    - from: "343-IMPL-EDIT-ORDER-MAP.md"
      to: "withdrawKeeperFunding + GameOverModule.handleFinalSweep:215 (GO_SWEPT)"
      via: "the GO_SWEPT revert-post-sweep guard locked in 343-SOLVENCY-PROOF.md carried into the signature + edit order"
      pattern: "GO_SWEPT"
---

<objective>
Author `343-IMPL-EDIT-ORDER-MAP.md` — the BATCH-01 design-lock: the final reconciled signatures + the `keeperFunding` storage shape + the deposit/withdraw/claim-merge/snapshot wiring + the producer-before-consumer edit-order map for the single 344 IMPL diff, all citing the ACTUAL lines pinned in 343-GREP-ATTESTATION.md and carrying the GO_SWEPT withdraw-guard locked in 343-SOLVENCY-PROOF.md and the kill-set order from 343-CLEANUP-INVENTORY.md.

This is the consumer of the three Wave-1/2 deliverables — it must NOT re-discover lines or proofs; it composes them into the IMPL hand-off. It also records the four RESEARCH corrections that the IMPL author must apply: the D-01 `BatchBuy.funder` correction (to REQUIREMENTS AUTOBUY-02 + PLAN-V54 §4), the D-MR-01 `keeperSnapshot` src carve-out (refining AUTOBUY-05), the `handleAffiliate` name correction (payAffiliate does not exist), and the double invariant comment (`DegenerusGame.sol:5` AND `:18`).

Purpose: The 344 IMPL phase authors ONE fully-reconciled `contracts/*.sol` diff with zero "by construction" assumptions and zero intermediate broken state, against actual lines and corrected signatures.
Output: One markdown artifact, `343-IMPL-EDIT-ORDER-MAP.md`, in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-CONTEXT.md
@.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-RESEARCH.md
@.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-GREP-ATTESTATION.md
@.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-SOLVENCY-PROOF.md
@.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-CLEANUP-INVENTORY.md
@.planning/PLAN-V54-KEEPER-FUNDING-GAME-LEDGER.md

<interfaces>
<!-- ACTUAL lines from 343-GREP-ATTESTATION.md (Wave 1) — cite these. GO_SWEPT lock from 343-SOLVENCY-PROOF.md (Wave 2). Kill-set order from 343-CLEANUP-INVENTORY.md (Wave 2). -->
<!-- Final signatures to lock (PLAN-V54 §2/§3/§4 + the corrections): -->
- storage: `mapping(address => uint256) keeperFunding;` on DegenerusGameStorage.sol — NO keeperFundingPool aggregate (D-CF-03)
- `function batchPurchase(BatchBuy[] calldata buys) external` — NON-payable (was payable :1824; the `spent == msg.value` guard removed); AF_KING-gated, !gameOver, len!=0; per slice: `keeperFunding[b.funder] -= ev` & `claimablePool -= uint128(ev)` (revert if `keeperFunding[b.funder] < ev`) then delegatecall purchaseWith(b.player,…,ev)
- `struct BatchBuy { address funder; address player; uint256 ethValue; uint256 amount; bool isTicket; uint8 mode; }` — funder ADDED (D-01) to BOTH structs (AfKing:20 + Game:1796)
- `function depositKeeperFunding(address player) external payable` — mirror AfKing.depositFor :314; `keeperFunding[player] += msg.value` & `claimablePool += msg.value`; revert player==0; zero-value no-op; emit KeeperFunded
- `function withdrawKeeperFunding(uint256 amount) external` — un-brickable CEI (mirror AfKing.withdraw :328-341, debit before .call); moves claimablePool in tandem; **reverts post-sweep via the GO_SWEPT latch (mirror _claimWinningsInternal:1463)** — the GO_SWEPT lock from 343-SOLVENCY-PROOF.md; zero-value no-op; emit KeeperWithdrew
- `function keeperFundingOf(address) external view returns (uint256)`
- extended `keeperSnapshot(address[]) → (…, keeperFunding[player])` :2645 (GASOPT-03: one staticcall/player); D-MR-01 carve-out: OPEN-E src≠player slice needs one extra `keeperFundingOf(src)` staticcall (mirror :809)
- `_claimWinningsInternal` :1462 — post-gameOver also pays keeperFunding[caller] (Decision B); zero both; debit claimablePool by the sum; already has the GO_SWEPT guard :1463
- invariant comment — BOTH DegenerusGame.sol:5 AND :18 + DegenerusGameStorage.sol:345-354 → `claimablePool == Σ claimableWinnings + Σ keeperFunding`
- handleAffiliate (NOT payAffiliate) :36, fresh constants :164/:165 — the fresh-rate justification for the SEPARATE keeperFunding bucket (AUTOBUY-03 / PLAN-V54 §10)
<!-- AfKing de-custody wiring: subscribe stays payable, forwards msg.value → game.depositKeeperFunding{value}(subscriber) (A2); funder=src per slice at build :726; funding-skip reads keeperFunding[src]; batched call :768 {value:totalValue} → non-value; IGame ABI updates :43/:56 + adds. -->
<!-- D-06 order: remove v48 recovery legs (Vault:517 + StakedStonk:539) before/atomically-with deleting AfKing.poolOf:492 / withdraw:328. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the BATCH-01 design-lock + producer-before-consumer IMPL edit-order map</name>
  <files>.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-IMPL-EDIT-ORDER-MAP.md</files>
  <read_first>
    - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-GREP-ATTESTATION.md (the ACTUAL lines for every anchor — cite these, never the doc-drifted lines)
    - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-SOLVENCY-PROOF.md (the GO_SWEPT withdraw-guard lock — carry it into the withdrawKeeperFunding signature + the edit order)
    - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-CLEANUP-INVENTORY.md (the kill-set + the D-06 integrity gate — carry the recovery-leg-before-views removal order)
    - .planning/PLAN-V54-KEEPER-FUNDING-GAME-LEDGER.md (§2 ledger shape, §3 deposit/withdraw, §4 hot path [NOTE §4's keeperFunding[b.player] is SUPERSEDED by D-01], §5 solvency-wiring, §6 de-custody, §8 edit map, §10 fresh-rate rationale)
    - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-CONTEXT.md (D-CF-01/02/03; D-01 funder [CRITICAL]; D-MR-01 src carve-out; D-MR-02 drift; the Integration Points producer-before-consumer order)
    - .planning/REQUIREMENTS.md (AUTOBUY-02 [the b.player to correct], AUTOBUY-03, AUTOBUY-05, LEDGER-01..05, DECUSTODY-03/04 — the requirement text the corrections amend)
    - contracts/DegenerusGame.sol + contracts/AfKing.sol (re-confirm the signature sites for the lock — batchPurchase, BatchBuy, keeperSnapshot, _claimWinningsInternal, both invariant comments; AfKing subscribe/build/call/IGame block)
  </read_first>
  <action>
    Author `343-IMPL-EDIT-ORDER-MAP.md`. Section 1 (Final signatures — the BATCH-01 lock): write the FINAL reconciled signatures, each with its actual file:line: the NON-payable `batchPurchase(BatchBuy[] calldata buys) external` (the `payable` modifier + the `spent == msg.value` guard removed; AF_KING-gated / `!gameOver` / `len != 0` preserved; per slice `keeperFunding[b.funder] -= ev` & `claimablePool -= uint128(ev)` with revert-if-insufficient, then delegatecall `purchaseWith(b.player,…,ev)`); the `BatchBuy` struct WITH the new `funder` field (both structs — AfKing:20 + Game:1796); `depositKeeperFunding(address player) external payable` (mirror `AfKing.depositFor:314`; `keeperFunding[player] += msg.value` & `claimablePool += msg.value`; revert player==0; zero-value no-op; emit `KeeperFunded`); the un-brickable CEI `withdrawKeeperFunding(uint256 amount) external` (mirror `AfKing.withdraw:328-341`, debit before `.call`; moves `claimablePool` in tandem; **reverts post-sweep via the GO_SWEPT latch mirroring `_claimWinningsInternal:1463`** — carry the lock from 343-SOLVENCY-PROOF.md verbatim; zero-value no-op; emit `KeeperWithdrew`); `keeperFundingOf(address) external view returns (uint256)`; the extended `keeperSnapshot` returning `keeperFunding[player]` at :2645; the `_claimWinningsInternal:1462` post-gameOver keeper-merge (Decision B — pay `claimableWinnings[caller] + keeperFunding[caller]`, zero both, debit `claimablePool` by the sum; note it already has the GO_SWEPT guard at :1463). Section 2 (Storage shape lock): `mapping(address => uint256) keeperFunding;` on `DegenerusGameStorage.sol`, NO `keeperFundingPool` aggregate (D-CF-03), and the invariant comment updated in ALL copies — `DegenerusGame.sol:5` AND `:18` + `DegenerusGameStorage.sol:345-354` — to `claimablePool == Σ claimableWinnings + Σ keeperFunding`. Section 3 (Corrections the IMPL author MUST apply): (a) the **D-01 `BatchBuy.funder` correction** — record it as an EXPLICIT correction to REQUIREMENTS `AUTOBUY-02` and PLAN-V54 §4 (both literally say `keeperFunding[b.player] -= ev`): the correct debit is `keeperFunding[b.funder]` where `funder = src`; both `BatchBuy` structs gain `funder`; AfKing sets `funder: src` per slice at the build site; the VAULT/SDGNRS exemption stays keyed on the un-spoofable `player` (AfKing:696), NOT `funder`; explain WHY (the OPEN-E operator-funded case `src ≠ player` would mis-account/revert if keyed on `player`); (b) the **D-MR-01 `keeperSnapshot` src carve-out** — REFINE AUTOBUY-05's "ONE staticcall per player" claim: the OPEN-E `src ≠ player` slice needs one extra `game.keeperFundingOf(src)` staticcall (mirror `AfKing.sol:809`); common path unchanged; (c) the **`handleAffiliate` name correction** — `DegenerusAffiliate.payAffiliate` does NOT exist; the function is `handleAffiliate:36` (fresh constants :164/:165); correct the name wherever AUTOBUY-03 / PLAN-V54 §10 justify the fresh-rate separate-bucket; (d) the **double invariant comment** — BOTH `DegenerusGame.sol:5` AND `:18` must be updated (PLAN-V54 §5 #1 cites only :18). Section 4 (Producer-before-consumer edit-order map): a numbered ordered list of edits with ACTUAL file:line such that no file ships an intermediate broken state: (1) storage `keeperFunding` + `BatchBuy.funder` (both structs); (2) Game — `depositKeeperFunding` / `withdrawKeeperFunding` (with GO_SWEPT guard) / `keeperFundingOf` / non-payable `batchPurchase` with `funder` debit / `_claimWinningsInternal` keeper-merge / extended `keeperSnapshot` / both invariant comments; (3) interfaces (`IDegenerusGame*` + AfKing's `IGame` block) — non-payable `batchPurchase` + the new view/deposit/withdraw/extended-snapshot signatures; (4) AfKing de-custody — `subscribe` forwards `msg.value`→`depositKeeperFunding` (A2), `funder=src` wiring at the build site, funding-skip reads `keeperFunding[src]` via the extended snapshot + the `:809`-style fallback for OPEN-E, drop the local CEI debit, `{value:totalValue}` call → non-value; (5) the v48-recovery removal — per the D-06 integrity gate, remove the recovery legs (`Vault:517` + `StakedStonk:539`) BEFORE/atomically-with deleting `AfKing.poolOf:492` / `withdraw:328`, then narrow the sDGNRS `receive()` AF_KING relaxation to GAME-only. State explicitly this is a SINGLE batched diff so "before/atomically-with" is satisfied by authoring order. Cite ACTUAL file:line throughout; do NOT use any "v1/simplified/for-now" language.
  </action>
  <acceptance_criteria>
    - 343-IMPL-EDIT-ORDER-MAP.md exists and locks the final signatures (non-payable batchPurchase, depositKeeperFunding, withdrawKeeperFunding WITH the GO_SWEPT guard, keeperFundingOf, extended keeperSnapshot, the _claimWinningsInternal merge) each with an actual file:line
    - The storage shape is locked: mapping(address => uint256) keeperFunding, NO aggregate, invariant comment updated in BOTH DegenerusGame.sol:5 AND :18 (+ storage :345-354)
    - The D-01 funder correction is recorded as an explicit correction to AUTOBUY-02 + PLAN-V54 §4 (keeperFunding[b.funder], funder added to both structs, VAULT/SDGNRS exemption stays on player)
    - The D-MR-01 src carve-out refines AUTOBUY-05 (one extra keeperFundingOf(src) staticcall for the OPEN-E slice, mirror :809)
    - The handleAffiliate name correction is recorded (payAffiliate does not exist)
    - The producer-before-consumer edit-order map is present with the D-06 ordering (recovery legs before poolOf/withdraw deletion) and the withdrawKeeperFunding GO_SWEPT guard
    - No "v1"/"simplified"/"for now" language; no contracts/*.sol modified (`git diff --name-only -- contracts/` empty)
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-IMPL-EDIT-ORDER-MAP.md; test -f "$F" && grep -q "funder" "$F" && grep -q "GO_SWEPT" "$F" && grep -q "handleAffiliate" "$F" && grep -qE "AUTOBUY-02" "$F" && grep -qE ":5\b|:5 AND|:5 and" "$F" && grep -q ":18" "$F" && grep -qE "keeperFundingOf\(src\)|:809" "$F" && grep -qiE "producer-before-consumer|edit-order|edit order" "$F" && ! grep -qiE "\bv1\b|simplified version|for now" "$F" && [ -z "$(git diff --name-only -- contracts/)" ]</automated>
  </verify>
  <done>343-IMPL-EDIT-ORDER-MAP.md locks the final signatures + storage shape (incl. the GO_SWEPT withdraw guard + the no-aggregate invariant in both comment copies), records the four corrections (D-01 funder, D-MR-01 src carve-out, handleAffiliate name, double invariant comment), and fixes the producer-before-consumer edit order with the D-06 removal sequencing — all on actual lines, no scope-reduction language. Zero contracts/ edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Paper-only SPEC phase; no code ships. The threat surface is whether the design-lock + edit-order map lets the 344 IMPL diff ship a correct, fully-reconciled, no-intermediate-broken-state change. The design/solvency risks this plan closes on paper:

| Boundary | Description |
|----------|-------------|
| design-lock → 344 IMPL diff | A mis-locked signature (e.g. the un-corrected `keeperFunding[b.player]` debit, or a withdraw without the GO_SWEPT guard) ships a mis-accounting / underflow-bricking diff. |
| producer-before-consumer order ↔ the single batched diff | A wrong edit order (e.g. deleting `AfKing.poolOf`/`withdraw` before the v48 recovery legs) leaves an intermediate dangling reference / broken build. |
| corrected anchors ↔ the IMPL author | If the map cites drifted lines or the non-existent `payAffiliate`, the IMPL edit lands at the wrong place or references a missing symbol. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-343-09 | Tampering (mis-accounting) | the batchPurchase debit key (D-01) | mitigate | The lock records the explicit correction to `keeperFunding[b.funder]` (= src) with `funder` added to both `BatchBuy` structs and the VAULT/SDGNRS exemption kept on `player`; the OPEN-E operator-funded case is preserved. |
| T-343-10 | Denial of Service (post-sweep underflow brick) | withdrawKeeperFunding signature | mitigate | The locked signature carries the GO_SWEPT revert-post-sweep guard (mirror `_claimWinningsInternal:1463`) from 343-SOLVENCY-PROOF.md so a post-sweep withdraw reverts cleanly. |
| T-343-11 | Denial of Service (broken build / dangling ref) | the edit-order map | mitigate | The producer-before-consumer order sequences storage→Game→interfaces→AfKing-de-custody→v48-recovery-removal, with the D-06 gate (recovery legs before `poolOf`/`withdraw` deletion); a single batched diff satisfies "before/atomically-with." |
| T-343-12 | Spoofing (missing-symbol reference) | the `payAffiliate` name drift | mitigate | The `handleAffiliate` name correction is recorded so the fresh-rate justification cites a real symbol. |
| T-343-SC | Tampering | npm/pip/cargo installs | accept | No package installs — paper-only Markdown authoring (RESEARCH "Standard Stack": no Package Legitimacy Audit needed). |
</threat_model>

<verification>
- 343-IMPL-EDIT-ORDER-MAP.md exists in the phase directory.
- Final signatures + storage shape locked (incl. GO_SWEPT withdraw guard + the no-aggregate invariant in both comment copies).
- The four corrections recorded: D-01 funder, D-MR-01 src carve-out, handleAffiliate name, double invariant comment.
- The producer-before-consumer edit-order map present with the D-06 ordering; no scope-reduction language.
- No contracts/*.sol modified: `git diff --name-only -- contracts/` is empty.
</verification>

<success_criteria>
- 343-IMPL-EDIT-ORDER-MAP.md satisfies all Task 1 acceptance criteria (ROADMAP Phase 343 Success Criterion 1).
- The four RESEARCH corrections are assigned and recorded.
- Zero contracts/*.sol edits.
</success_criteria>

<output>
Create `.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-04-SUMMARY.md` when done
</output>
