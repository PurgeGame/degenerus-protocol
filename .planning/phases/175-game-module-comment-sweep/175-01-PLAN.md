---
phase: 175-game-module-comment-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/175-game-module-comment-sweep/175-01-FINDINGS.md
autonomous: true
requirements:
  - CMT-01
must_haves:
  truths:
    - "Every inline comment and NatSpec entry in AdvanceModule and MintModule has been read against the actual code"
    - "Every discrepancy between a comment and code behavior is captured as a LOW or INFO finding"
    - "175-01-FINDINGS.md is self-contained and reviewable without re-reading source"
  artifacts:
    - path: ".planning/phases/175-game-module-comment-sweep/175-01-FINDINGS.md"
      provides: "Comment audit findings for AdvanceModule and MintModule"
      contains: "CMT-01"
  key_links:
    - from: "contracts/modules/DegenerusGameAdvanceModule.sol"
      to: ".planning/phases/175-game-module-comment-sweep/175-01-FINDINGS.md"
      via: "line-by-line comment vs code comparison"
      pattern: "AdvanceModule"
    - from: "contracts/modules/DegenerusGameMintModule.sol"
      to: ".planning/phases/175-game-module-comment-sweep/175-01-FINDINGS.md"
      via: "line-by-line comment vs code comparison"
      pattern: "MintModule"
---

<objective>
Sweep all inline comments and NatSpec in DegenerusGameAdvanceModule and DegenerusGameMintModule
against actual code behavior. Log every discrepancy as a LOW or INFO finding with contract name,
line reference, and plain-English description of what the comment says vs what the code does.

Purpose: These are the two largest and most-changed modules since the v3.5 sweep. BURNIE endgame
gate, level quests, activity score refactoring, module consolidation (EndgameModule deleted with
functions absorbed into AdvanceModule), and affiliate bonus cache have all landed since then.
Output: 175-01-FINDINGS.md with all comment discrepancies found in AdvanceModule + MintModule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep DegenerusGameAdvanceModule.sol comments</name>
  <read_first>
    contracts/modules/DegenerusGameAdvanceModule.sol
  </read_first>
  <action>
Read contracts/modules/DegenerusGameAdvanceModule.sol in full (1673 lines). For every comment
(single-line `//`, block `/* */`, and NatSpec `///` / `/** */`), verify that the comment
accurately describes the code immediately below or around it. Focus areas given changes since v3.5:

1. **BURNIE endgame gate** — Any comment referencing the endgame flow, burnRequired, BURNIE burn
   conditions, or the game-over trigger. Verify the described conditions match the actual `if`
   branches and state checks.

2. **Absorbed EndgameModule functions** — Functions moved from the deleted EndgameModule into
   AdvanceModule (e.g., rewardTopAffiliate, runRewardJackpots or their callers). Verify NatSpec
   @param and @return tags match actual parameters and return values. Verify any comment describing
   "what this does" matches the actual logic.

3. **Activity score refactoring** — Any comment on `_playerActivityScore`, score components
   (mintScore, streakScore, burnScore, affiliateScore), or the weighting formula. Verify the
   described components and math match what the code computes.

4. **Level quest integration** — Any comment referencing quest completion, quest gates, or
   `_processQuestCompletion`. Verify the described trigger conditions and side effects are accurate.

5. **General NatSpec** — Every `@notice`, `@param`, `@return`, `@dev` tag. Verify:
   - `@param` names and descriptions match the actual parameter names and their roles
   - `@return` descriptions match what is actually returned
   - `@notice` summaries do not omit significant side effects
   - `@dev` notes do not describe stale implementation details

For each discrepancy found, record:
- Severity: LOW (misleads a reader about behavior) or INFO (minor imprecision, typo, stale wording)
- Location: `DegenerusGameAdvanceModule.sol` line N (or line range N-M)
- What the comment says: exact quote or close paraphrase
- What the code actually does: plain-English correct description

If no discrepancy exists for a section, no finding is needed. Do not force findings.

Write all findings to `.planning/phases/175-game-module-comment-sweep/175-01-FINDINGS.md`
under the section heading `## DegenerusGameAdvanceModule`.
  </action>
  <verify>
    <automated>grep -c "DegenerusGameAdvanceModule" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/175-game-module-comment-sweep/175-01-FINDINGS.md 2>/dev/null && echo "section present"</automated>
  </verify>
  <acceptance_criteria>
    - 175-01-FINDINGS.md exists and contains a `## DegenerusGameAdvanceModule` section
    - Every finding has a line number reference in the format "line N" or "lines N-M"
    - Every finding has an explicit severity of LOW or INFO
    - Every finding has a "comment says" and "code does" description
    - If zero findings: the section contains "No discrepancies found" — not just an empty section
    - The full 1673-line contract has been read (not just a sample) — this is verifiable by whether
      the findings cover the entire contract scope including the final functions
  </acceptance_criteria>
  <done>DegenerusGameAdvanceModule fully swept. All comment discrepancies logged as LOW/INFO findings
with line references. Section written to 175-01-FINDINGS.md.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep DegenerusGameMintModule.sol comments</name>
  <read_first>
    contracts/modules/DegenerusGameMintModule.sol
  </read_first>
  <action>
Read contracts/modules/DegenerusGameMintModule.sol in full (1133 lines). For every comment
(single-line `//`, block `/* */`, and NatSpec `///` / `/** */`), verify that the comment
accurately describes the code immediately below or around it. Focus areas given changes since v3.5:

1. **Affiliate bonus cache** — The Phase 173 addition of AFFILIATE_BONUS_LEVEL_SHIFT and
   AFFILIATE_BONUS_POINTS_SHIFT writes in recordMintData (around lines 277-282). Verify any
   comment describing the cache write correctly states what is cached, when it is written
   (only on level transitions where !sameLevel), and which bits are used.

2. **recordMintData function** — NatSpec @param tags, @return tags, and inline comments describing
   the logic flow. This is the most-changed function in MintModule across v11.0-v17.0. Verify
   every comment accurately describes the current logic.

3. **Activity score contributions from minting** — Any comment on how mint events affect
   `_playerActivityScore`, mintScore, or streak components. Verify the described behavior
   matches the actual calls.

4. **Level quest triggering from mint** — Any comment referencing `_processQuestCompletion`
   or quest-related logic within the mint path. Verify trigger conditions described are accurate.

5. **General NatSpec** — Every `@notice`, `@param`, `@return`, `@dev` tag across all functions.
   Verify parameter names, descriptions, and return values match actual code.

6. **Inline clarifying comments** — Any `// e.g.`, `// Note:`, `// sameLevel means`, etc.
   Verify these explanatory comments are still accurate given the current code.

For each discrepancy, record:
- Severity: LOW (misleads about behavior) or INFO (minor imprecision, stale wording, typo)
- Location: `DegenerusGameMintModule.sol` line N (or line range N-M)
- What the comment says: exact quote or close paraphrase
- What the code actually does: plain-English correct description

Append all findings to `.planning/phases/175-game-module-comment-sweep/175-01-FINDINGS.md`
under the section heading `## DegenerusGameMintModule`.

After writing both sections, add a final header to the file:

```
# Phase 175 Comment Audit — Plan 01 Findings
**Contracts:** DegenerusGameAdvanceModule, DegenerusGameMintModule
**Requirement:** CMT-01
**Date:** [today]
**Total findings this plan:** [count LOW] LOW, [count INFO] INFO
```

Place this header at the TOP of the file (above the per-contract sections).
  </action>
  <verify>
    <automated>grep -c "DegenerusGameMintModule" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/175-game-module-comment-sweep/175-01-FINDINGS.md 2>/dev/null && grep "Total findings" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/175-game-module-comment-sweep/175-01-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - 175-01-FINDINGS.md contains a `## DegenerusGameMintModule` section
    - 175-01-FINDINGS.md contains a header with "Total findings this plan:" summary
    - Every finding has line reference, severity (LOW or INFO), "comment says", "code does"
    - If zero findings for MintModule: section contains "No discrepancies found"
    - The affiliate bonus cache section (lines ~277-282) has been explicitly checked
      (either a finding exists there, or a note that the comment is accurate)
    - File is self-contained — a reader can understand each finding without opening the contract
  </acceptance_criteria>
  <done>DegenerusGameMintModule fully swept. All comment discrepancies logged. 175-01-FINDINGS.md
complete with header summary and both contract sections.</done>
</task>

</tasks>

<verification>
175-01-FINDINGS.md exists with:
- A header section naming both contracts and providing a finding count summary
- A `## DegenerusGameAdvanceModule` section (findings or "No discrepancies found")
- A `## DegenerusGameMintModule` section (findings or "No discrepancies found")
- Every finding has severity, line reference, and comment-vs-code description
</verification>

<success_criteria>
- Both AdvanceModule (1673 lines) and MintModule (1133 lines) swept end-to-end
- Every comment-code discrepancy captured as LOW or INFO finding with line reference
- 175-01-FINDINGS.md is self-contained and reviewable without opening source contracts
- The affiliate bonus cache (Phase 173) and EndgameModule absorption (Phase 171)
  comment accuracy have been explicitly verified
</success_criteria>

<output>
After completion, create `.planning/phases/175-game-module-comment-sweep/175-01-SUMMARY.md`
</output>
