---
phase: 175-game-module-comment-sweep
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/175-game-module-comment-sweep/175-04-FINDINGS.md
autonomous: true
requirements:
  - CMT-01
must_haves:
  truths:
    - "Every inline comment and NatSpec entry in BoonModule, DegeneretteModule, and DecimatorModule has been read against the actual code"
    - "Every discrepancy between a comment and code behavior is captured as a LOW or INFO finding"
    - "175-04-FINDINGS.md is self-contained and reviewable without re-reading source"
  artifacts:
    - path: ".planning/phases/175-game-module-comment-sweep/175-04-FINDINGS.md"
      provides: "Comment audit findings for BoonModule, DegeneretteModule, DecimatorModule"
      contains: "CMT-01"
  key_links:
    - from: "contracts/modules/DegenerusGameBoonModule.sol"
      to: ".planning/phases/175-game-module-comment-sweep/175-04-FINDINGS.md"
      via: "line-by-line comment vs code comparison"
      pattern: "BoonModule"
    - from: "contracts/modules/DegenerusGameDegeneretteModule.sol"
      to: ".planning/phases/175-game-module-comment-sweep/175-04-FINDINGS.md"
      via: "line-by-line comment vs code comparison"
      pattern: "DegeneretteModule"
    - from: "contracts/modules/DegenerusGameDecimatorModule.sol"
      to: ".planning/phases/175-game-module-comment-sweep/175-04-FINDINGS.md"
      via: "line-by-line comment vs code comparison"
      pattern: "DecimatorModule"
---

<objective>
Sweep all inline comments and NatSpec in DegenerusGameBoonModule (329 lines),
DegenerusGameDegeneretteModule (1122 lines), and DegenerusGameDecimatorModule (928 lines)
against actual code behavior. Log every discrepancy as a LOW or INFO finding.

Purpose: These three modules handle deity boons, the "Degenrette" spin mechanic, and the
"Decimator" burn mechanic. All three received changes from v11.0 (BURNIE endgame gate),
v12.0-v13.0 (level quests), v14.0 (activity score gas optimization), and v16.0 (storage repack).
Output: 175-04-FINDINGS.md with all comment discrepancies in these three contracts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep BoonModule and DegeneretteModule comments</name>
  <read_first>
    contracts/modules/DegenerusGameBoonModule.sol
    contracts/modules/DegenerusGameDegeneretteModule.sol
  </read_first>
  <action>
Read both contracts in full. For every comment (single-line `//`, block `/* */`, NatSpec `///` /
`/** */`), verify the comment accurately describes the code around it.

**DegenerusGameBoonModule.sol (329 lines):**

Focus areas:
1. **mintPacked_ write at line ~305** — BoonModule writes LEVEL_COUNT_SHIFT. Any comment describing
   the boon's effect on mintPacked_ or on a player's level count. Verify the described bit
   field, the shift constant used, and the resulting state change are accurate.

2. **BURNIE endgame gate** — Any comment describing boon availability after the endgame flag is set.
   Verify described conditions match actual `if` branches.

3. **Level quest integration** — Any comment on `_processQuestCompletion` calls triggered from
   boon redemption. Verify described trigger conditions are accurate.

4. **Activity score contributions** — Any comment describing how boon events affect
   `_playerActivityScore`. Verify described behavior matches actual scoring calls.

5. **General NatSpec** — All `@notice`, `@param`, `@return`, `@dev` tags. Verify accuracy.

**DegenerusGameDegeneretteModule.sol (1122 lines):**

Focus areas:
1. **BURNIE endgame gate** — Any comment describing spin availability conditions or endgame
   interaction. Verify described conditions match actual `if` branches.

2. **Activity score contributions from Degenrette spins** — Any comment on how spin results
   affect `_playerActivityScore`. Verify described scoring behavior matches actual calls.

3. **Level quest integration** — Any comment referencing quest completion from Degenrette events.
   Verify described trigger conditions are accurate.

4. **VRF commitment window** — Any comment describing when VRF is requested vs fulfilled for
   spin results. Verify described timing and which player-controllable state can change between
   request and fulfillment.

5. **Storage repack (v16.0)** — Any comment referencing currentPrizePool as uint256 would be
   stale (downsized to uint128). Verify any prize pool references are accurate.

6. **General NatSpec** — All `@notice`, `@param`, `@return`, `@dev` tags. Verify accuracy.

For each discrepancy in either contract, record:
- Severity: LOW (misleads about behavior) or INFO (minor imprecision, stale wording, typo)
- Location: `[ContractName].sol` line N (or line range N-M)
- What the comment says: exact quote or close paraphrase
- What the code actually does: plain-English correct description

Write findings to `.planning/phases/175-game-module-comment-sweep/175-04-FINDINGS.md`
under separate section headings:
- `## DegenerusGameBoonModule`
- `## DegenerusGameDegeneretteModule`

If no discrepancy found in a contract, write "No discrepancies found" for that section.
  </action>
  <verify>
    <automated>grep -c "DegenerusGameBoonModule\|DegenerusGameDegeneretteModule" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/175-game-module-comment-sweep/175-04-FINDINGS.md 2>/dev/null && echo "both sections present"</automated>
  </verify>
  <acceptance_criteria>
    - 175-04-FINDINGS.md exists and contains both `## DegenerusGameBoonModule` and
      `## DegenerusGameDegeneretteModule` sections
    - Every finding has a line number reference in the format "line N" or "lines N-M"
    - Every finding has an explicit severity of LOW or INFO
    - Every finding has a "comment says" and "code does" description
    - BoonModule line ~305 mintPacked_ write explicitly checked
    - If zero findings for a contract: section contains "No discrepancies found"
    - Both contracts read in full (not sampled)
  </acceptance_criteria>
  <done>BoonModule (329 lines) and DegeneretteModule (1122 lines) fully swept. All comment
discrepancies logged as LOW/INFO findings. Sections written to 175-04-FINDINGS.md.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep DecimatorModule comments and finalize</name>
  <read_first>
    contracts/modules/DegenerusGameDecimatorModule.sol
  </read_first>
  <action>
Read contracts/modules/DegenerusGameDecimatorModule.sol in full (928 lines). For every comment
(single-line `//`, block `/* */`, and NatSpec `///` / `/** */`), verify the comment accurately
describes the code around it. Focus areas given changes since v3.5:

1. **Terminal Decimator rescaling (v17.0 vicinity / recent commit)** — The terminal decimator
   burn mechanic was rescaled (20x@120d to 1x@10d, blocking burns at ≤7 days). Any comment
   describing the burn rate curve, the day thresholds, the multiplier formula, or the minimum
   days required before burns are allowed. Verify described values and conditions match the actual
   constants and `if` branches in the code.

2. **BURNIE endgame gate** — Any comment describing Decimator availability or behavior at game end.
   Verify described conditions match actual `if` branches and the endgame flag check.

3. **Activity score contributions from burns** — Any comment on how Decimator burns affect
   `_playerActivityScore` (burnScore component). Verify described scoring behavior, weighting,
   and calculation match actual calls and constants.

4. **Level quest integration** — Any comment referencing quest completion from Decimator burn
   events. Verify described trigger conditions are accurate.

5. **VRF commitment window** — Any comment describing the VRF request/fulfillment timing for
   Decimator RNG. Verify what player-controllable state can change between request and fulfillment.

6. **Storage repack (v16.0)** — Any comment referencing currentPrizePool as uint256 would be stale.
   Verify any prize pool references match the actual uint128 type.

7. **General NatSpec** — All `@notice`, `@param`, `@return`, `@dev` tags. Verify accuracy of
   parameter names, return descriptions, and @notice summaries.

8. **Inline clarifying comments** — Any `// e.g.`, `// Note:`, `// blocksBurned`, etc.
   Verify these explanatory comments still accurately describe the current code.

For each discrepancy, record:
- Severity: LOW (misleads about behavior) or INFO (minor imprecision, stale wording, typo)
- Location: `DegenerusGameDecimatorModule.sol` line N (or line range N-M)
- What the comment says: exact quote or close paraphrase
- What the code actually does: plain-English correct description

Append findings to `.planning/phases/175-game-module-comment-sweep/175-04-FINDINGS.md`
under the section heading `## DegenerusGameDecimatorModule`.

After writing all three sections, prepend a header at the TOP of the file:

```
# Phase 175 Comment Audit — Plan 04 Findings
**Contracts:** DegenerusGameBoonModule, DegenerusGameDegeneretteModule, DegenerusGameDecimatorModule
**Requirement:** CMT-01
**Date:** [today]
**Total findings this plan:** [count LOW] LOW, [count INFO] INFO
```
  </action>
  <verify>
    <automated>grep "Total findings" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/175-game-module-comment-sweep/175-04-FINDINGS.md && grep "DegenerusGameDecimatorModule" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/175-game-module-comment-sweep/175-04-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - 175-04-FINDINGS.md contains a `## DegenerusGameDecimatorModule` section
    - 175-04-FINDINGS.md contains a header with "Total findings this plan:" summary
    - Every finding has line reference, severity (LOW or INFO), comment says, code does
    - Terminal Decimator rescaling burn-rate comments explicitly checked
    - If zero findings for DecimatorModule: section contains "No discrepancies found"
    - File is self-contained — a reader understands each finding without opening the contract
    - All three contract sections present in the file
  </acceptance_criteria>
  <done>DecimatorModule (928 lines) fully swept. 175-04-FINDINGS.md complete with header summary
and all three contract sections (BoonModule, DegeneretteModule, DecimatorModule).</done>
</task>

</tasks>

<verification>
175-04-FINDINGS.md exists with:
- A header section naming all three contracts and providing a finding count summary
- A `## DegenerusGameBoonModule` section (findings or "No discrepancies found")
- A `## DegenerusGameDegeneretteModule` section (findings or "No discrepancies found")
- A `## DegenerusGameDecimatorModule` section (findings or "No discrepancies found")
- Every finding has severity, line reference, and comment-vs-code description
</verification>

<success_criteria>
- BoonModule (329 lines), DegeneretteModule (1122 lines), DecimatorModule (928 lines) swept end-to-end
- Every comment-code discrepancy captured as LOW or INFO finding with line reference
- Terminal Decimator rescaling comment accuracy explicitly verified
- 175-04-FINDINGS.md is self-contained and reviewable without opening source contracts
</success_criteria>

<output>
After completion, create `.planning/phases/175-game-module-comment-sweep/175-04-SUMMARY.md`
</output>
