---
phase: 175-game-module-comment-sweep
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/175-game-module-comment-sweep/175-05-FINDINGS.md
autonomous: true
requirements:
  - CMT-01
must_haves:
  truths:
    - "Every inline comment and NatSpec entry in WhaleModule, GameOverModule, and PayoutUtils has been read against the actual code"
    - "Every discrepancy between a comment and code behavior is captured as a LOW or INFO finding"
    - "175-05-FINDINGS.md is self-contained and reviewable without re-reading source"
  artifacts:
    - path: ".planning/phases/175-game-module-comment-sweep/175-05-FINDINGS.md"
      provides: "Comment audit findings for WhaleModule, GameOverModule, PayoutUtils"
      contains: "CMT-01"
  key_links:
    - from: "contracts/modules/DegenerusGameWhaleModule.sol"
      to: ".planning/phases/175-game-module-comment-sweep/175-05-FINDINGS.md"
      via: "line-by-line comment vs code comparison"
      pattern: "WhaleModule"
    - from: "contracts/modules/DegenerusGameGameOverModule.sol"
      to: ".planning/phases/175-game-module-comment-sweep/175-05-FINDINGS.md"
      via: "line-by-line comment vs code comparison"
      pattern: "GameOverModule"
    - from: "contracts/modules/DegenerusGamePayoutUtils.sol"
      to: ".planning/phases/175-game-module-comment-sweep/175-05-FINDINGS.md"
      via: "line-by-line comment vs code comparison"
      pattern: "PayoutUtils"
---

<objective>
Sweep all inline comments and NatSpec in DegenerusGameWhaleModule (989 lines),
DegenerusGameGameOverModule (245 lines), and DegenerusGamePayoutUtils (106 lines)
against actual code behavior. Log every discrepancy as a LOW or INFO finding.

Purpose: WhaleModule handles whale-tier purchases and received storage repack changes (v16.0)
and activity score refactoring (v14.0). GameOverModule owns the endgame trigger — any comment
describing the BURNIE gate, burn threshold, or game-over conditions is high priority to verify.
PayoutUtils is small but comments describing payout math must be exact. Output: 175-05-FINDINGS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep WhaleModule and GameOverModule comments</name>
  <read_first>
    contracts/modules/DegenerusGameWhaleModule.sol
    contracts/modules/DegenerusGameGameOverModule.sol
  </read_first>
  <action>
Read both contracts in full. For every comment (single-line `//`, block `/* */`, NatSpec `///` /
`/** */`), verify the comment accurately describes the code around it.

**DegenerusGameWhaleModule.sol (989 lines):**

Focus areas:
1. **mintPacked_ reads and writes** — WhaleModule reads HAS_DEITY_PASS_SHIFT (bit 184) around
   line 548 and writes LAST_LEVEL_SHIFT, LEVEL_COUNT_SHIFT, LEVEL_UNITS_SHIFT,
   LEVEL_UNITS_LEVEL_SHIFT, HAS_DEITY_PASS_SHIFT. Any comment describing the packed fields
   being read or written. Verify bit fields and their described semantics are accurate.

2. **Storage repack (v16.0)** — Slots 0-2 were repacked, currentPrizePool downsized to uint128.
   Any comment referencing currentPrizePool type or value range would be stale if it says uint256.
   Verify any prize pool storage references match the current type.

3. **Whale bundle types** — Any comment describing WHALE_BUNDLE_TYPE_SHIFT values (stored in
   bits 152-153). Verify the described bundle type encoding matches the actual constants used.

4. **Activity score contributions from whale purchases** — Any comment on how whale bundle
   purchases affect `_playerActivityScore`. Verify described scoring behavior matches actual calls.

5. **Level quest integration** — Any comment referencing quest completion from whale bundle
   purchase events. Verify described trigger conditions are accurate.

6. **BURNIE endgame gate** — Any comment describing whale bundle availability after game end.
   Verify described conditions match actual `if` branches.

7. **General NatSpec** — All `@notice`, `@param`, `@return`, `@dev` tags. Verify accuracy.

**DegenerusGameGameOverModule.sol (245 lines):**

This is the highest-priority contract in this plan. GameOverModule owns the endgame trigger logic.

Focus areas:
1. **BURNIE endgame gate (v11.0)** — Every comment describing the conditions that trigger game
   over: the burnRequired check, the BURNIE token burn threshold, the flag being set. Verify each
   described condition matches the actual `if` branch, constant value, or state variable read.

2. **Endgame trigger sequence** — Any comment describing what happens in order when game over is
   triggered (jackpot rewards, affiliate rewards, token distributions, state transitions). Verify
   the described sequence matches the actual function call order.

3. **runRewardJackpots callsite** — The runRewardJackpots function is now in JackpotModule
   (moved from EndgameModule in v16.0). Any comment in GameOverModule describing where
   runRewardJackpots lives or how it is called. Verify these references are not stale.

4. **rewardTopAffiliate callsite** — Similarly inlined/moved in v16.0. Verify callsite comments.

5. **General NatSpec** — All `@notice`, `@param`, `@return`, `@dev` tags. Verify accuracy.

For each discrepancy in either contract, record:
- Severity: LOW (misleads about behavior) or INFO (minor imprecision, stale wording, typo)
- Location: `[ContractName].sol` line N (or line range N-M)
- What the comment says: exact quote or close paraphrase
- What the code actually does: plain-English correct description

Write findings to `.planning/phases/175-game-module-comment-sweep/175-05-FINDINGS.md`
under separate section headings:
- `## DegenerusGameWhaleModule`
- `## DegenerusGameGameOverModule`

If no discrepancy found in a contract, write "No discrepancies found" for that section.
  </action>
  <verify>
    <automated>grep -c "DegenerusGameWhaleModule\|DegenerusGameGameOverModule" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/175-game-module-comment-sweep/175-05-FINDINGS.md 2>/dev/null && echo "both sections present"</automated>
  </verify>
  <acceptance_criteria>
    - 175-05-FINDINGS.md exists and contains both `## DegenerusGameWhaleModule` and
      `## DegenerusGameGameOverModule` sections
    - Every finding has a line number reference in the format "line N" or "lines N-M"
    - Every finding has an explicit severity of LOW or INFO
    - Every finding has a "comment says" and "code does" description
    - GameOverModule BURNIE endgame gate section explicitly checked (high priority)
    - WhaleModule mintPacked_ write fields explicitly checked (line ~548 read site and writes)
    - If zero findings for a contract: section contains "No discrepancies found"
    - Both contracts read in full (not sampled)
  </acceptance_criteria>
  <done>WhaleModule (989 lines) and GameOverModule (245 lines) fully swept. All comment
discrepancies logged as LOW/INFO findings. Sections written to 175-05-FINDINGS.md.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep PayoutUtils comments and finalize</name>
  <read_first>
    contracts/modules/DegenerusGamePayoutUtils.sol
  </read_first>
  <action>
Read contracts/modules/DegenerusGamePayoutUtils.sol in full (106 lines). For every comment
(single-line `//`, block `/* */`, and NatSpec `///` / `/** */`), verify the comment accurately
describes the code around it.

PayoutUtils is small but math-critical. Comments here describe ETH payout calculations, fee
splits, and distribution logic. Any comment that misstates a denominator, percentage, basis
points value, or distribution recipient is a meaningful discrepancy.

Focus areas:
1. **Payout math comments** — Any comment stating a percentage (e.g., "20% to vault", "10 basis
   points") or a calculation formula. Verify the described values match the actual constants,
   denominators, and arithmetic in the code.

2. **Fee split recipients** — Any comment naming who receives each portion of a payout. Verify
   the described recipients match the actual transfer targets.

3. **Storage repack (v16.0)** — Any comment referencing currentPrizePool type or size. Verify
   any prize pool references match the current uint128 type.

4. **General NatSpec** — All `@notice`, `@param`, `@return`, `@dev` tags. Given the small size,
   every tag should be checked. Verify parameter names match actual function signatures.

5. **Inline clarifying comments** — Any `// e.g.`, `// Note:`, `// denominator is`, etc.
   Verify these comments are numerically accurate.

For each discrepancy, record:
- Severity: LOW (misleads about behavior, especially math errors) or INFO (typo, stale wording)
- Location: `DegenerusGamePayoutUtils.sol` line N (or line range N-M)
- What the comment says: exact quote or close paraphrase
- What the code actually does: plain-English correct description

Append findings to `.planning/phases/175-game-module-comment-sweep/175-05-FINDINGS.md`
under the section heading `## DegenerusGamePayoutUtils`.

After writing all three sections, prepend a header at the TOP of the file:

```
# Phase 175 Comment Audit — Plan 05 Findings
**Contracts:** DegenerusGameWhaleModule, DegenerusGameGameOverModule, DegenerusGamePayoutUtils
**Requirement:** CMT-01
**Date:** [today]
**Total findings this plan:** [count LOW] LOW, [count INFO] INFO
```
  </action>
  <verify>
    <automated>grep "Total findings" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/175-game-module-comment-sweep/175-05-FINDINGS.md && grep "DegenerusGamePayoutUtils" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/175-game-module-comment-sweep/175-05-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - 175-05-FINDINGS.md contains a `## DegenerusGamePayoutUtils` section
    - 175-05-FINDINGS.md contains a header with "Total findings this plan:" summary
    - Every finding has line reference, severity (LOW or INFO), comment says, code does
    - Payout math comments (percentages, basis points, denominators) explicitly checked
    - If zero findings for PayoutUtils: section contains "No discrepancies found"
    - File is self-contained — a reader understands each finding without opening the contract
    - All three contract sections present in the file
  </acceptance_criteria>
  <done>PayoutUtils (106 lines) fully swept. 175-05-FINDINGS.md complete with header summary
and all three contract sections (WhaleModule, GameOverModule, PayoutUtils).</done>
</task>

</tasks>

<verification>
175-05-FINDINGS.md exists with:
- A header section naming all three contracts and providing a finding count summary
- A `## DegenerusGameWhaleModule` section (findings or "No discrepancies found")
- A `## DegenerusGameGameOverModule` section (findings or "No discrepancies found")
- A `## DegenerusGamePayoutUtils` section (findings or "No discrepancies found")
- Every finding has severity, line reference, and comment-vs-code description
</verification>

<success_criteria>
- WhaleModule (989 lines), GameOverModule (245 lines), PayoutUtils (106 lines) swept end-to-end
- BURNIE endgame gate comments in GameOverModule explicitly verified (highest priority)
- PayoutUtils math comments (percentages, denominators) explicitly verified
- 175-05-FINDINGS.md is self-contained and reviewable without opening source contracts
</success_criteria>

<output>
After completion, create `.planning/phases/175-game-module-comment-sweep/175-05-SUMMARY.md`
</output>
