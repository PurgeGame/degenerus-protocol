---
phase: 381-invariant-fuzz-durable-property-net
plan: 06
type: execute
wave: 2
depends_on: ["381-01", "381-02", "381-03", "381-04", "381-05"]
files_modified:
  - .planning/phases/381-invariant-fuzz-durable-property-net/381-06-COUNCIL-PROMPT.md
  - .planning/phases/381-invariant-fuzz-durable-property-net/381-06-COUNCIL-ADJUDICATION.md
autonomous: false
requirements: [FUZZ-06]
must_haves:
  truths:
    - "The assembled invariant-property SET (FUZZ-01..05 as built) plus the shared handler action-space summary is written into a single council prompt and dispatched to the external convergent council (Gemini + Codex) via council.sh"
    - "Every council-returned candidate gap is ADJUDICATED against the frozen c4d48008 source: convergent + reproducible gaps are folded in as new invariants; divergent / speculative / by-design-ruling gaps are recorded with the adjudication rationale and NOT folded (the suite never blocks on a speculative gap)"
    - "Any folded-in gap lands as a green always-on invariant in the same suite the sweep phases (382+) consume as their oracle"
  artifacts:
    - path: ".planning/phases/381-invariant-fuzz-durable-property-net/381-06-COUNCIL-PROMPT.md"
      provides: "The property-set + action-space completeness-review prompt handed to council.sh"
      min_lines: 40
    - path: ".planning/phases/381-invariant-fuzz-durable-property-net/381-06-COUNCIL-ADJUDICATION.md"
      provides: "The per-candidate adjudication record (convergent->fold / divergent->record) + the council.json reference"
      min_lines: 30
  key_links:
    - from: "the assembled property-set prompt"
      to: "the external council (Gemini + Codex)"
      via: ".planning/audit-v52/cross-model/bin/council.sh --out-dir <dir> --label fuzz-completeness <promptfile>"
      pattern: "council.sh"
---

<objective>
Close FUZZ-06 (XMODEL): have the external convergent council cross-check the invariant-property SET for
COMPLETENESS before the sweep phases consume it as their oracle. Per the v62 defining premise
(CROSS-MODEL-LED), the council (Gemini + Codex) is the PRIMARY finder; Claude orchestrates the dispatch,
adjudicates each returned candidate against frozen `c4d48008`, and folds ONLY convergent, reproducible
gaps into the suite. The question put to the council: given FUZZ-01..05 and the shared action-space,
"what property is missing? what reachable sequence violates an unstated invariant?"

Purpose: the durable property net is the regression oracle for 382 PRIME / 383 ASYM / 384 COMPO / 385
LOOP / 386 PERIPH — it must be council-reviewed for completeness so a missing property is found NOW, not
discovered as an un-caught finding later.

Output: the council prompt (property-set + action-space), the council dispatch, and the adjudication
record; plus any convergent gap folded in as a new green invariant. This is the orchestration +
adjudication + conditional fold-in — it does NOT block on speculative gaps. The dispatch + adjudication
step is `autonomous: false` (it needs the human-in-the-loop council run on record).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/381-invariant-fuzz-durable-property-net/381-01-SUMMARY.md
@.planning/phases/381-invariant-fuzz-durable-property-net/381-02-SUMMARY.md
@.planning/phases/381-invariant-fuzz-durable-property-net/381-03-SUMMARY.md
@.planning/phases/381-invariant-fuzz-durable-property-net/381-04-SUMMARY.md
@.planning/phases/381-invariant-fuzz-durable-property-net/381-05-SUMMARY.md

<interfaces>
<!-- The council dispatch contract. council.sh fans ONE prompt to Gemini + Codex and collects a merged
     council.json. The Claude lens comes from the orchestrator adjudicating. -->

From .planning/audit-v52/cross-model/bin/council.sh:
  Usage: council.sh --out-dir DIR [--schema SCHEMA.json] [--label NAME] PROMPT_FILE
  Output: <label>.gemini.json/.txt, <label>.codex.json/.txt,
          <label>.council.json = {"models":[...],"answers":{model:answer},"available":[...],"skipped":[...]}
  ~5 min/area, parallel, NO Claude-cap cost. The EXTERNAL council (Gemini + Codex).

The five built invariants (the SET to review — reference the SUMMARYs for the exact assertion + handler):
  FUZZ-01 V61SolvencyAfpay.inv.t.sol  -> packed-balance Σ identity + balance+stETH backing (wide action-space)
  FUZZ-02 RngWindowFreeze.inv.t.sol   -> every enumerated in-window SLOAD frozen [request->unlock]
  FUZZ-03 AdvanceGasCeiling(Fuzz)     -> every advanceGame tx <= 16,777,216 (reusable component)
  FUZZ-04 BoxEnqueue.inv.t.sol        -> every persisted box enqueued in boxPlayers[index] until opened
  FUZZ-05 PoolConservation.inv.t.sol  -> 4-pool total backed + no unbacked credit across transfers

The shared action-space (hand this to the council): the handlers under test/fuzz/handlers/ —
GameHandler / WhaleHandler / V61AfkingSpendHandler / SolvencyActionHandler / RngWindowFreezeHandler /
BoxCreationHandler / PoolFlowHandler / CompositionHandler — covering purchase (DirectEth/Claimable/
Combined + lootbox), whale/lazy/deity passes, presale/afking-cover boxes, afking fund/spend, stale
cashout, smite/decurse, advanceGame + VRF fulfill, openBoxes, claimWinnings.

The by-design rulings the council should be handed AS the spine to break (from REQUIREMENTS.md header /
MEMORY by-design list): presale over-credit · lootbox open/claim TIMING · degenerette/WWXRP RTP · afking
pass-eviction inclusive boundary · deity sybil self-grant · ticket dual-scale · open-E operator-approval
· claimBingo no level guard · affiliate single-step claim. Convergent push-back on one of these is a
signal, not a pre-counted finding.
</interfaces>
</context>

<threat_model>
## Trust boundary
The completeness of the property SET itself. The threat is a MISSING invariant — a reachable action
sequence that violates an unstated property no FUZZ-01..05 invariant asserts — that would let a real bug
through to the sweep phases un-caught.

## Threat register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-381-06-01 | (coverage gap) | a property class no FUZZ-01..05 invariant asserts (e.g. an unstated cross-mechanic conservation / a monotonicity / a freeze edge) | mitigate | council completeness review surfaces it; convergent+reproducible gaps folded in as new invariants |
| T-381-06-02 | (speculative-gap churn) | folding a speculative / divergent / by-design gap that bloats the suite or asserts a non-property | accept (with rationale) | only convergent + reproducible gaps are folded; divergent/by-design candidates recorded in the adjudication with rationale, NOT folded — the suite never blocks on a speculative gap |
| T-381-06-SC | Tampering (Write-capable subagent) | a Write-capable agent mutating contracts during the council run | mitigate | the council is dispatched read-only via council.sh; git-status-verify contracts/ unchanged after the run ([[feedback_verify_writecapable_agents]]) |

Severity is blocked on the council pass being ON RECORD: per the v62 cross-model gate, a "property set is
complete" verdict for FUZZ-06 REQUIRES the council pass on record — Claude saturation alone is not
evidence. The plan is not complete until council.json exists and every candidate is adjudicated.
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Assemble the property-set + action-space prompt, dispatch the council, capture council.json</name>
  <what-built>
    The five FUZZ-01..05 invariants are built and green (wave 1, this phase's plans 01-05). This task
    assembles them into ONE completeness-review prompt and fires the external council.
  </what-built>
  <read_first>
    - .planning/audit-v52/cross-model/bin/council.sh (the dispatch contract + output shape)
    - .planning/phases/381-invariant-fuzz-durable-property-net/381-01-SUMMARY.md through 381-05-SUMMARY.md (the exact assertion + handler + slot for each built invariant)
    - .planning/REQUIREMENTS.md (FUZZ-06 verbatim + the by-design rulings list to hand the council as the spine)
  </read_first>
  <action>
    Write `.planning/phases/381-invariant-fuzz-durable-property-net/381-06-COUNCIL-PROMPT.md`: state the
    five built invariants (assertion + handler + the slot/accessor each reads, from the SUMMARYs), the
    shared action-space (the handler list + the entrypoints each drives), the frozen subject c4d48008, and
    the by-design rulings as the spine. Pose the FUZZ-06 question explicitly: "Here is the durable
    invariant property SET we will use as the regression oracle for the upcoming sweeps. (1) What property
    is MISSING — a reachable action sequence that violates an unstated invariant none of these asserts?
    (2) For each named by-design ruling, is there a convergent reason it should be a property we assert
    rather than accept? Give each candidate a concrete reachable action sequence." Then DISPATCH:
    `bash .planning/audit-v52/cross-model/bin/council.sh --out-dir .planning/phases/381-invariant-fuzz-durable-property-net/council --label fuzz-completeness .planning/phases/381-invariant-fuzz-durable-property-net/381-06-COUNCIL-PROMPT.md`
    Confirm `council/fuzz-completeness.council.json` is produced (Gemini + Codex answers present, or the
    skipped list explains any unavailable model). After the run, `git status --porcelain contracts/` MUST
    be empty (the council is read-only).
  </action>
  <how-to-verify>
    1. `.planning/phases/381-invariant-fuzz-durable-property-net/council/fuzz-completeness.council.json` exists and contains the council answers.
    2. `git status --porcelain contracts/` is empty (no contract mutation during the council run).
    3. Eyeball the council answers for concrete candidate gaps (each with a reachable action sequence).
  </how-to-verify>
  <resume-signal>Type "council captured" once council.json exists and contracts/ is verified clean, with a one-line note of the candidate gaps returned (or "no gaps" if the council found the set complete).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Adjudicate each candidate vs frozen c4d48008 + fold ONLY convergent reproducible gaps</name>
  <read_first>
    - .planning/phases/381-invariant-fuzz-durable-property-net/council/fuzz-completeness.council.json (the council output from Task 1)
    - test/REGRESSION-BASELINE-v62.md §4 (FC1-FC6 + the affiliate-score asymmetry ALREADY routed to the sweeps — do NOT re-plan these as FUZZ-06 gaps; they belong to 382+)
    - the five built invariant files (to confirm whether a "gap" is already covered before folding)
  </read_first>
  <action>
    Write `.planning/phases/381-invariant-fuzz-durable-property-net/381-06-COUNCIL-ADJUDICATION.md`: for
    EACH council candidate gap, record (a) the candidate + its proposed reachable sequence, (b) whether it
    is CONVERGENT (both models) or divergent, (c) the adjudication vs frozen c4d48008 source (is it a real
    uncovered property, already covered by an existing invariant, a by-design ruling, or speculative/
    non-reproducible), and (d) the disposition: FOLD (convergent + reproducible + a genuine property gap)
    or RECORD-ONLY (divergent / already-covered / by-design / speculative) with rationale. Exclude the §4
    FC1-FC6 + affiliate-score items (already routed to 382+). For each FOLD candidate, add the missing
    invariant to the most appropriate existing suite file from plans 01-05 (or a new minimal *.inv.t.sol)
    reusing the relevant handler — built to be GREEN and non-vacuous, exactly like the wave-1 invariants.
    If the council returns NO convergent reproducible gap (the set is complete), record that verdict — the
    suite is unchanged and FUZZ-06 is satisfied by the council pass on record. The plan must NOT block on
    a speculative gap: a speculative candidate is RECORD-ONLY, not a fold. NO contracts/*.sol edit.
  </action>
  <verify>
    <automated>test -f .planning/phases/381-invariant-fuzz-durable-property-net/381-06-COUNCIL-ADJUDICATION.md && forge test --match-path "test/fuzz/invariant/*.inv.t.sol" 2>&1 | tail -6  # every invariant (incl. any folded gap) green</automated>
  </verify>
  <done>381-06-COUNCIL-ADJUDICATION.md records every council candidate with a convergent/divergent + vs-c4d48008 adjudication + a FOLD/RECORD-ONLY disposition (FC1-FC6 + affiliate-score excluded as already-routed); any folded gap is a green non-vacuous invariant in the suite; the full invariant suite is green; git diff contracts/ empty.</done>
</task>

</tasks>

<verification>
- `council/fuzz-completeness.council.json` exists (the council pass is ON RECORD — the v62 cross-model gate).
- `381-06-COUNCIL-ADJUDICATION.md` adjudicates every candidate vs frozen c4d48008 with a FOLD / RECORD-ONLY disposition + rationale.
- Any folded gap is a green, non-vacuous always-on invariant; the full `test/fuzz/invariant/*.inv.t.sol` suite is green.
- The §4 FC1-FC6 + affiliate-score asymmetry are NOT re-planned here (already routed to 382+).
- `git diff c4d48008 -- contracts/` is EMPTY (the council is read-only; no fold edits a contract).
</verification>

<success_criteria>
FUZZ-06 is satisfied: the external council cross-checked the assembled FUZZ-01..05 property SET +
action-space for completeness (pass on record), every candidate gap is adjudicated against frozen
c4d48008, and every convergent reproducible gap is folded into the suite as a green invariant before the
sweep phases (382+) consume it as their oracle — without blocking on any speculative gap.
</success_criteria>

<output>
Create `.planning/phases/381-invariant-fuzz-durable-property-net/381-06-SUMMARY.md` when done.
</output>
