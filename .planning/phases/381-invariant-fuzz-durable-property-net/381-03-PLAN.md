---
phase: 381-invariant-fuzz-durable-property-net
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - test/gas/AdvanceGasCeiling.sol
  - test/gas/AdvanceGasCeilingFuzz.t.sol
autonomous: true
requirements: [FUZZ-03]
must_haves:
  truths:
    - "A REUSABLE gas-ceiling property component exists: a shared base/helper that, given a seeded reachable advanceGame pre-state, drives the REAL advanceGame() to completion and asserts EVERY single tx consumed <= 16,777,216 gas (and surfaces the max for the < 10M soft target) — usable by Phase 384 / COMPO-02 against the real advanceGame over fuzzed states"
    - "The property is exercised over MULTIPLE reachable worst-case pre-states (fuzzed bucket geometry / owed sizes / level), not the single game-over composition scenario, and is GREEN at the frozen c4d48008 subject"
    - "The known game-over→terminal-jackpot composition (the v60 gasceil shape, fixed 6d2c8d0c) stays under the cap as a regression case driven through the reusable component"
  artifacts:
    - path: "test/gas/AdvanceGasCeiling.sol"
      provides: "The reusable GameSeeder-etch + drive-real-advanceGame + assert-every-tx-<=-16777216 component (a base contract or library Phase 384 imports and parameterizes)"
      min_lines: 90
      contains: "16_777_216"
    - path: "test/gas/AdvanceGasCeilingFuzz.t.sol"
      provides: "Fuzzed exercise of the reusable property over multiple reachable advanceGame pre-states"
      contains: "advanceGame"
  key_links:
    - from: "test/gas/AdvanceGasCeilingFuzz.t.sol"
      to: "the real game.advanceGame()"
      via: "etch GameSeeder -> seed worst-case pre-state -> restore real code -> measure gasleft() across each advanceGame tx"
      pattern: "advanceGame"
    - from: "test/gas/AdvanceGasCeiling.sol"
      to: "EIP-7825 cap 16,777,216"
      via: "assertLe(txGas, EIP7825_TX_GAS_CAP) per tx in the drain loop"
      pattern: "16_777_216"
---

<objective>
Make FUZZ-03 (GAS-CEILING) a durable AND REUSABLE property. The pattern already exists as a ONE-SHOT
scenario in `test/gas/GameOverCompositionAdvanceGas.t.sol` — a `GameSeeder` (DegenerusGame subclass with
seeders) etched via `type().runtimeCode`, used to write the worst-case game-over pre-state, then the real
code is restored so the measured tx runs production `advanceGame()` bytecode, asserting every tx stays
under EIP-7825's 16,777,216 cap. FUZZ-03's success criterion 3 (SC3) is the load-bearing twist: the
< 16.7M property must be authored as a REUSABLE component (a shared base/helper or parameterized harness)
that Phase 384 / COMPO-02 can drive against the REAL advanceGame over fuzzed reachable states. This plan
extracts the seeder + drive-and-measure loop into that reusable component and exercises it over multiple
reachable pre-states.

Purpose: HIGH-priority threat (gas-DoS in the advanceGame chain — 16.7M = permanent game-over brick); a
durable, reusable harness so Phase 384's composition sweep does not re-author the seeder/measure loop.

Output: `test/gas/AdvanceGasCeiling.sol` (the reusable seeder+drive+assert component) +
`AdvanceGasCeilingFuzz.t.sol` (fuzzes reachable pre-states through it). Case (b) PROMOTE TO REUSABLE — DO
NOT duplicate the existing one-shot scenario; factor its mechanism out so both this fuzz and Phase 384
consume one component. Reference GameOverCompositionAdvanceGas as the regression case driven THROUGH the
extracted component (do not delete it; it stays as the named game-over composition regression).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@test/REGRESSION-BASELINE-v62.md

<interfaces>
<!-- The one-shot pattern to factor out. The reusable component MUST expose the seeder + the
     drive-and-measure loop so Phase 384 can call it with its own seeded states. -->

From test/gas/GameOverCompositionAdvanceGas.t.sol (the source-of-truth pattern — extract, do not duplicate):
  EIP7825_TX_GAS_CAP = 16_777_216   // the hard per-tx cap (game over if breached)
  GAS_TARGET         = 10_000_000   // the USER soft comfort target
  contract GameSeeder is DegenerusGame { seedGameOverWorstCase(lvl, rngWord, readOwed, writeOwed,
                                          uint8[4] winTraits, uint16[4] bucketCounts, base) ... }
  _seedWorstCase(readOwed, writeOwed): vm.etch(game, type(GameSeeder).runtimeCode) -> seed -> vm.etch(real)
                                       -> vm.deal(game, GAME_FUNDS) -> vm.warp(+200 days)
  the drive loop: for i in 0..12 { g0=gasleft(); game.advanceGame(); used=g0-gasleft();
                  maxTxGas=max; over |= used>CAP; if gameOver() break } then assertFalse(over) + assertLt(maxTxGas, TARGET)
  _deriveJackpot(): JackpotBucketLib.getRandomTraits + bucketCountsForPoolCap (the 305-winner geometry)

The reusable seam Phase 384 needs: (a) etch-seed-restore a worst-case advanceGame pre-state from
PARAMETERS, (b) drive the real advanceGame() to gameOver()/quiescence measuring gasleft() per tx,
(c) return/assert the per-tx max <= 16,777,216 (and report the max for the 10M soft target). Keep the
GameSeeder as a DegenerusGame subclass (etch-safe: type().runtimeCode has no constructor side effects).
</interfaces>
</context>

<threat_model>
## Trust boundary
advanceGame() is the mandatory permissionless heartbeat. A single advanceGame tx that exceeds the
EIP-7825 per-tx gas cap can NEVER complete → permanent, unrecoverable game-over (the protocol bricks).

## Threat register

| Threat ID | Category | Component | Disposition | Mitigation (what the property asserts) |
|-----------|----------|-----------|-------------|------------------------------------------|
| T-381-03-01 | Denial of service | a reachable advanceGame pre-state whose single tx exceeds 16,777,216 gas | mitigate | the reusable component drives the REAL advanceGame and assertLe(txGas, 16_777_216) per tx across the drain |
| T-381-03-02 | Denial of service (composition) | two heavy stages falling through into one tx (the v60 gasceil shape) | mitigate | the game-over composition regression case is driven through the SAME component and stays < cap |
| T-381-03-VAC | (vacuous green) | the seeded pre-state is not actually the worst case, so the measured tx is light | mitigate | worst-case derivation (cold write budget / 305-winner geometry / deep bucket level) per the gas-worst-case rule; fuzz the geometry so a heavier reachable state cannot slip past |

Severity is blocked on the property being un-fuzzable / vacuously green: the seeded state must reach the
heavy branch (gameOver() latches / the terminal jackpot runs) — if the drive loop never reaches the heavy
work the measurement is meaningless and the plan FAILS acceptance.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extract the reusable AdvanceGasCeiling component (GameSeeder etch + drive-real-advanceGame + assert-every-tx-<=-16777216)</name>
  <read_first>
    - test/gas/GameOverCompositionAdvanceGas.t.sol (the source-of-truth one-shot — the GameSeeder subclass, _seedWorstCase, the per-tx gasleft drive loop, _deriveJackpot — this is what gets factored out, NOT duplicated)
    - contracts/storage/DegenerusGameStorage.sol (the seeder writes level/purchaseStartDay/dailyIdx/levelPrizePool/rngWordByDay/ticketQueue/ticketsOwedPacked/traitBurnTicket — confirm these are the live names at c4d48008)
    - contracts/libraries/JackpotBucketLib.sol (getRandomTraits + bucketCountsForPoolCap — the 305-winner geometry derivation) and EntropyLib.sol (hash2)
  </read_first>
  <action>
    Create `test/gas/AdvanceGasCeiling.sol` containing (1) a `GameSeeder is DegenerusGame` overlay with
    the worst-case seeder(s) — port `seedGameOverWorstCase` verbatim-equivalent from
    GameOverCompositionAdvanceGas (so the existing regression keeps the exact pre-state) AND a more
    general `seedAdvanceWorstCase(...)` taking parameters (level, rngWord, readOwed, writeOwed, the bucket
    geometry, base) so Phase 384 can seed arbitrary reachable states — and (2) a reusable
    `AdvanceGasCeilingBase is DeployProtocol` (abstract) exposing: `EIP7825_TX_GAS_CAP = 16_777_216`,
    `GAS_TARGET = 10_000_000`, an `_etchSeedRestore(...)` that vm.etch's the GameSeeder, calls the seeder
    with the given params, vm.etch's the real code back, funds + warps; and a
    `_driveAndAssertUnderCap(uint256 maxTxIters) returns (uint256 maxTxGas, bool reachedHeavy)` that runs
    the real game.advanceGame() in a bounded loop measuring gasleft() per tx, `assertLe(txGas,
    EIP7825_TX_GAS_CAP)` on EACH tx, tracks maxTxGas, and reports whether the heavy branch was reached
    (gameOver()/terminal jackpot). The component must NOT itself hard-code a single scenario — the
    scenario params are arguments. Keep the GameSeeder a clean DegenerusGame subclass (etch-safe). NO
    contracts/*.sol edit; comments explain how the etch-seed-measure works, no fenced code.
  </action>
  <verify>
    <automated>forge build 2>&1 | tail -3  # AdvanceGasCeiling.sol compiles; GameSeeder + base + 16_777_216 present</automated>
  </verify>
  <done>test/gas/AdvanceGasCeiling.sol compiles; exposes a parameterized GameSeeder seeder, the EIP7825_TX_GAS_CAP=16_777_216 constant, _etchSeedRestore, and _driveAndAssertUnderCap (per-tx assertLe + reachedHeavy report) as a reusable base Phase 384 can import. No contracts/*.sol mutated.</done>
</task>

<task type="auto">
  <name>Task 2: Fuzz the reusable property over multiple reachable advanceGame pre-states + drive the game-over composition regression through it</name>
  <read_first>
    - test/gas/AdvanceGasCeiling.sol (the reusable base + GameSeeder from Task 1)
    - test/gas/GameOverCompositionAdvanceGas.t.sol (the named regression case whose pre-state must stay < cap when driven through the extracted component)
    - foundry.toml ([fuzz] runs=1000 seed=0xdeadbeef — the per-property fuzz budget)
  </read_first>
  <action>
    Create `test/gas/AdvanceGasCeilingFuzz.t.sol` extending AdvanceGasCeilingBase. Add a FUZZ test
    `testFuzz_advanceGame_everyTxUnderCap(uint256 readOwed, uint256 writeOwed, uint256 geomSeed)` that
    bounds the owed sizes near the cold-write budget (so each ticket round is heavy but finishes — the
    worst-case branch, per the gas-worst-case rule), derives a 305-winner bucket geometry from geomSeed,
    calls _etchSeedRestore then _driveAndAssertUnderCap, and asserts reachedHeavy == true (non-vacuity —
    the heavy branch was exercised) so the per-tx assertLe inside the base is meaningful. Add a
    `test_gameOverComposition_regression_underCap` that seeds the EXACT game-over composition pre-state
    (the v60 gasceil shape, fixed 6d2c8d0c) via the ported seeder and drives it through
    _driveAndAssertUnderCap — asserting game-over completes AND every tx < cap AND maxTxGas < GAS_TARGET
    (the same assertions the one-shot made, now via the reusable component). Bound the fuzz so a cap-stop
    never lands mid-run (the [fuzz] 1000-run default is fine; do NOT switch to the deep profile here —
    pacing). NO contracts/*.sol edit.
  </action>
  <verify>
    <automated>forge test --match-contract "AdvanceGasCeilingFuzz" 2>&1 | tail -10  # fuzz + regression green; every advanceGame tx <= 16,777,216, max < 10M</automated>
  </verify>
  <done>AdvanceGasCeilingFuzz.t.sol is green: testFuzz_advanceGame_everyTxUnderCap holds (every tx <= 16,777,216, heavy branch reached) over the 1000-run fuzz, and test_gameOverComposition_regression_underCap confirms the game-over composition stays < cap and < 10M soft target through the reusable component. git diff contracts/ empty.</done>
</task>

</tasks>

<verification>
- `forge test --match-contract "AdvanceGasCeilingFuzz"` is green; every measured advanceGame tx <= 16,777,216, max < 10,000,000.
- `test/gas/AdvanceGasCeiling.sol` is a REUSABLE component (parameterized seeder + _etchSeedRestore + _driveAndAssertUnderCap base) — NOT a single hard-coded scenario — that Phase 384 / COMPO-02 imports.
- The existing GameOverCompositionAdvanceGas scenario is preserved; its pre-state is also driven through the extracted component as the named regression.
- `git diff c4d48008 -- contracts/` is EMPTY.
</verification>

<success_criteria>
FUZZ-03 asserts no advanceGame tx exceeds 16,777,216 gas (targets < 10M) across reachable seeded states,
GREEN at c4d48008, AND is authored as the REUSABLE < 16.7M property component (shared base + parameterized
GameSeeder) that Phase 384 / COMPO-02 drives against the real advanceGame over its own fuzzed reachable
states (FUZZ-03 SC3 — the reusability requirement, called out explicitly in must_haves).
</success_criteria>

<output>
Create `.planning/phases/381-invariant-fuzz-durable-property-net/381-03-SUMMARY.md` when done.
</output>
