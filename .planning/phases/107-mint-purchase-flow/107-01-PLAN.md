---
phase: 107-mint-purchase-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/unit-05/COVERAGE-CHECKLIST.md
autonomous: true
requirements: [COV-01, COV-02, COV-03]

must_haves:
  truths:
    - "Every state-changing function (external, public, internal, private) in DegenerusGameMintModule.sol and DegenerusGameMintStreakUtils.sol has a checklist entry"
    - "Each function is categorized as B (external state-changing), C (internal/private state-changing), or D (view/pure) per D-01"
    - "Multi-parent helpers (_callTicketPurchase, _purchaseBurnieLootboxFor, _maybeRequestLootboxRng) are flagged with [MULTI-PARENT] markers"
    - "The inline Yul assembly function _raritySymbolBatch is flagged with [ASSEMBLY] marker"
    - "Risk tiers (1/2/3) are assigned to all Category B functions matching the research risk tier assignments"
    - "Inherited helper functions from GameStorage that are called within MintModule call trees are documented"
  artifacts:
    - path: "audit/unit-05/COVERAGE-CHECKLIST.md"
      provides: "Complete function checklist for Taskmaster coverage enforcement"
      contains: "Function Checklist"
  key_links:
    - from: "audit/unit-05/COVERAGE-CHECKLIST.md"
      to: "contracts/modules/DegenerusGameMintModule.sol"
      via: "Every state-changing function listed with line numbers"
      pattern: "line \\d+"
    - from: "audit/unit-05/COVERAGE-CHECKLIST.md"
      to: "contracts/modules/DegenerusGameMintStreakUtils.sol"
      via: "Both MintStreakUtils functions listed with line numbers"
      pattern: "MintStreakUtils"
---

<objective>
Taskmaster Agent: Build the coverage checklist for Unit 5 (DegenerusGameMintModule + DegenerusGameMintStreakUtils).

Purpose: The Taskmaster ensures no function is skipped during the Mad Genius attack phase. With 18 functions across two contracts plus inherited helpers, completeness is critical. The inline Yul assembly (_raritySymbolBatch), self-call re-entry pattern, and ticket queue write paths need explicit flagging.

Output: COVERAGE-CHECKLIST.md (function-by-function audit tracking table covering both contracts as a single unit)
</objective>

<execution_context>
@.planning/ULTIMATE-AUDIT-DESIGN.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/107-mint-purchase-flow/107-CONTEXT.md
@.planning/phases/107-mint-purchase-flow/107-RESEARCH.md
@audit/KNOWN-ISSUES.md

<interfaces>
From audit/unit-02/COVERAGE-CHECKLIST.md:
- Category B/C/D table structure with Analyzed?, Call Tree?, Storage Map?, Cache Check? columns
- Risk tier assignments (Tier 1/2/3) for Category B
- Sequential C-numbering scheme
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build complete function coverage checklist for MintModule + MintStreakUtils</name>
  <files>audit/unit-05/COVERAGE-CHECKLIST.md</files>

  <read_first>
    - .planning/ULTIMATE-AUDIT-DESIGN.md (Agent 3: Taskmaster section -- adopt this persona)
    - .planning/phases/107-mint-purchase-flow/107-RESEARCH.md (complete function inventory with categories, risk tiers)
    - .planning/phases/107-mint-purchase-flow/107-CONTEXT.md (decisions D-01 through D-10)
    - contracts/modules/DegenerusGameMintModule.sol (~1,167 lines -- read the ENTIRE file, line by line)
    - contracts/modules/DegenerusGameMintStreakUtils.sol (62 lines -- read the ENTIRE file, line by line)
    - contracts/storage/DegenerusGameStorage.sol (storage layout reference -- needed for inherited helpers)
    - audit/unit-02/COVERAGE-CHECKLIST.md (FORMAT REFERENCE ONLY -- see how Phase 104 structured the checklist)
    - audit/KNOWN-ISSUES.md (context for known issues)
  </read_first>

  <action>
YOU ARE THE TASKMASTER. Read your identity and mandate from ULTIMATE-AUDIT-DESIGN.md, Agent 3.

Your job: Build the COVERAGE CHECKLIST that the Mad Genius must check off function-by-function. If a function is missing from your checklist, it will NOT be audited. Completeness is everything.

**IMPORTANT: Read EVERY LINE of DegenerusGameMintModule.sol and DegenerusGameMintStreakUtils.sol. Not skim. Not scan. READ. Line by line.**

**Step 1: Categorize every function into B/C/D per D-01.**

Per D-01: No Category A for modules. External/public state-changing -> Category B. Internal/private state-changing helpers -> Category C. View/pure -> Category D.

**Category B: External State-Changing (expected 5 functions)**

For each, record:
- Function name and line range (from actual source)
- Access control (exact modifier or require statement)
- Primary storage variables written
- External calls made
- Risk tier: Tier 1 (complex, assembly, deep call tree), Tier 2 (significant flows), Tier 3 (simpler)
- Subsystem tag: ETH-PURCHASE, COIN-PURCHASE, TICKET-DRAIN, MINT-DATA, BURNIE-LOOT

**Category C: Internal/Private State-Changing Helpers (expected ~11 functions)**

For each, record:
- Sequential number (C1-C11) and function name
- Line range and contract
- Called by which parent(s)
- Key storage writes
- Flags: [MULTI-PARENT], [ASSEMBLY], [INHERITED] as applicable

**Category D: View/Pure (expected ~4 functions)**

For each, record:
- Function name and line range
- What it reads/computes
- Security note

**Step 2: Build ticket queue write path trace.**

Document every path from a Category B entry point through to _queueTicketsScaled:

```
B3 -> C1 -> C3 -> _queueTicketsScaled (ETH purchase path)
B4 -> C2 -> C3 -> _queueTicketsScaled (BURNIE purchase path)
```

**Step 3: Build cross-module external call table.**

List every external call MintModule makes with target contract and state impact.

**Step 4: Produce COVERAGE-CHECKLIST.md with this structure:**

```
# Unit 5: Mint + Purchase Flow -- Coverage Checklist

## Contracts Under Audit
- contracts/modules/DegenerusGameMintModule.sol (~1,167 lines)
- contracts/modules/DegenerusGameMintStreakUtils.sol (62 lines)

## Methodology
[Per D-01 through D-10]

## Checklist Summary
[Category counts]

## Category B: External State-Changing Functions
[Table]

## Category C: Internal Helpers (State-Changing)
[Table]

## Category D: View/Pure Functions
[Table]

## Ticket Queue Write Paths
[Every path from B entry point to _queueTicketsScaled]

## Cross-Module External Calls
[Table]

## Self-Call Re-Entry Map
[Document the recordMint self-call pattern]
```

All "Analyzed?" columns start as "pending".
  </action>

  <verify>
    <automated>test -f audit/unit-05/COVERAGE-CHECKLIST.md && grep -c "pending" audit/unit-05/COVERAGE-CHECKLIST.md</automated>
  </verify>

  <acceptance_criteria>
    - File audit/unit-05/COVERAGE-CHECKLIST.md exists
    - Contains Category B table with exactly 5 external state-changing functions
    - Contains Category C table with all ~11 internal helpers
    - Contains Category D table with all ~4 view/pure functions
    - Every entry has line numbers from actual source code
    - _raritySymbolBatch is flagged [ASSEMBLY]
    - _callTicketPurchase, _purchaseBurnieLootboxFor, _maybeRequestLootboxRng are flagged [MULTI-PARENT]
    - Risk tiers assigned correctly (B2, B3 Tier 1; B1, B4 Tier 2; B5 Tier 3)
    - Ticket Queue Write Paths section traces all paths to _queueTicketsScaled
    - Cross-Module External Calls table present
    - Self-Call Re-Entry Map documents the recordMint pattern
    - All "Analyzed?" columns show "pending"
  </acceptance_criteria>

  <done>Complete coverage checklist with every state-changing function in both contracts listed with line numbers, categorized B/C/D, assembly and multi-parent helpers flagged, ticket queue write paths documented, ready for Mad Genius</done>
</task>

</tasks>

<verification>
- COVERAGE-CHECKLIST.md contains every state-changing function from both contracts
- All entries have line numbers from actual source
- Multi-parent helpers identified with complete parent lists
- Assembly function flagged
- Ticket queue write paths documented
</verification>

<success_criteria>
- Taskmaster has built a complete function checklist for the Mad Genius
- No state-changing function in either contract is missing from the checklist
- Self-call re-entry pattern is documented for the Mad Genius to trace
</success_criteria>

<output>
After completion, create `.planning/phases/107-mint-purchase-flow/107-01-SUMMARY.md`
</output>
