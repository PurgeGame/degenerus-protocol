---
phase: 08-eth-accounting-invariant-and-cei-verification
plan: "05"
type: execute
wave: 2
depends_on: []
files_modified:
  - .planning/phases/08-eth-accounting-invariant-and-cei-verification/08-05-SUMMARY.md
autonomous: true
requirements:
  - ACCT-03
  - ACCT-09
  - ACCT-10

must_haves:
  truths:
    - "All BPS fee split sites verified: for each purchase path, nextPrizePool delta + futurePrizePool delta == msg.value (or msg.value minus claimablePool usage)"
    - "recordMint() futureShare + nextShare rounding verified — no ETH silently drops to zero via floor division"
    - "adminStakeEthForStEth solvency guard confirmed: stakeable = balance - claimablePool prevents staking below obligations"
    - "receive() donation confirmed to route only to futurePrizePool with no state transition side effects"
    - "selfdestruct forced ETH injection assessed — surplus ETH is INFO-level (increases solvency margin, no extraction path)"
    - "ACCT-03, ACCT-09, and ACCT-10 each receive a final verdict"
  artifacts:
    - path: ".planning/phases/08-eth-accounting-invariant-and-cei-verification/08-05-SUMMARY.md"
      provides: "ACCT-03, ACCT-09, and ACCT-10 verdicts with BPS arithmetic evidence"
      contains: "ACCT-03"
  key_links:
    - from: "DegenerusGame.sol:404 recordMint()"
      to: "nextPrizePool and futurePrizePool"
      via: "PURCHASE_TO_FUTURE_BPS splits — verify sum equals msg.value"
      pattern: "PURCHASE_TO_FUTURE_BPS"
    - from: "DegenerusGame.sol:1877 adminStakeEthForStEth()"
      to: "claimablePool"
      via: "Guard: stakeable = ethBal - claimablePool — confirm reserve is claimablePool not nextPrizePool"
      pattern: "stakeable"
    - from: "DegenerusGame.sol:2856 receive()"
      to: "futurePrizePool"
      via: "Donation routes to futurePrizePool only — no conditional logic on amount"
      pattern: "futurePrizePool"
---

<objective>
Audit the three remaining ACCT requirements: BPS fee split correctness across all purchase paths (ACCT-03), admin staking solvency guard completeness (ACCT-09), and `receive()` donation safety including selfdestruct forced ETH (ACCT-10). All three are manual code-trace tasks with no new test infrastructure needed.

Purpose: BPS rounding bugs can silently drain ETH via fee path arithmetic. An incomplete staking guard lets an admin deplete the ETH backing for player winnings. A receive() function that triggers game transitions would be an access control bypass.

Output: ACCT-03, ACCT-09, and ACCT-10 verdicts written to 08-05-SUMMARY.md.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-eth-accounting-invariant-and-cei-verification/08-CONTEXT.md
@.planning/phases/08-eth-accounting-invariant-and-cei-verification/08-RESEARCH.md

<interfaces>
<!-- BPS sites from RESEARCH.md — verify all from source -->

BPS fee split sites to audit (ACCT-03):

1. recordMint() — DegenerusGame.sol:404-412
   Formula: futureShare = msg.value * PURCHASE_TO_FUTURE_BPS / 10_000
            nextShare = msg.value - futureShare
   Check: futureShare + nextShare == msg.value (remainder assigned, no drop)

2. WhaleModule._purchaseWhaleBundle() — DegenerusGameWhaleModule.sol:292-295
   Formula: nextShare = price * WHALE_BUNDLE_TO_NEXT_BPS / 10_000
            remainder → futurePrizePool
   Check: nextShare + remainder == price

3. WhaleModule._purchaseLazyPass() — DegenerusGameWhaleModule.sol:413-538
   Formula: futureShare = price * LAZY_PASS_TO_FUTURE_BPS / 10_000
            nextShare = price - futureShare
   Check: futureShare + nextShare == price

4. JackpotModule._resolveTraitWinners() — daily jackpot BPS distribution
   Check: sum of all BPS shares across trait buckets <= jackpot amount (floor rounding is safe)

Admin solvency guard (ACCT-09) — DegenerusGame.sol:1877-1882:
   stakeable = ethBal - reserve
   guard: if ethBal <= claimablePool, revert (or similar)
   Verify: `reserve` is exactly `claimablePool` (not currentPrizePool, not nextPrizePool)

receive() donation (ACCT-10) — DegenerusGame.sol:2856-2858:
   receive() external payable { futurePrizePool += msg.value; }
   Check: no conditional logic; no level threshold check; no pool trigger
   selfdestruct: forces ETH without calling receive(); balance increases without futurePrizePool +=
   Assess: does surplus balance (forced ETH) break solvency invariant or enable extraction?
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify BPS fee splits across all purchase paths (ACCT-03)</name>
  <files>
    contracts/DegenerusGame.sol,
    contracts/modules/DegenerusGameWhaleModule.sol,
    contracts/modules/DegenerusGameJackpotModule.sol
  </files>
  <action>
    Read each BPS fee split site from source. For each site:

    **Site 1 — recordMint() (DegenerusGame.sol ~404-412):**
    - Read the function body
    - Find `PURCHASE_TO_FUTURE_BPS` constant — read its value
    - Verify the formula: `futureShare = msg.value * BPS / 10_000; nextShare = msg.value - futureShare`
    - The subtraction pattern (`x - floor(x*BPS/10_000)`) assigns the rounding remainder to nextShare — verify this is the pattern used (not a second multiplication that could drop a wei)
    - Check: futureShare + nextShare == msg.value algebraically? YES for the subtraction pattern; NO if both use independent multiplication

    **Site 2 — _purchaseWhaleBundle() (DegenerusGameWhaleModule.sol ~292-295):**
    - Read the function body around lines 292-295
    - Find `WHALE_BUNDLE_TO_NEXT_BPS` constant — read its value
    - Verify: does the second split use subtraction of the first split (safe) or independent multiplication (loses rounding remainder)?
    - Confirm: price = eth sent for the bundle; verify total attribution equals price

    **Site 3 — _purchaseLazyPass() (DegenerusGameWhaleModule.sol ~413-538):**
    - Read the lazy pass ETH routing logic
    - The lazy pass price varies by level — confirm the BPS split applies to the same `price` variable that represents `msg.value` (or the relevant ETH amount)
    - Verify: subtraction pattern or independent multiplication?

    **Site 4 — JackpotModule._resolveTraitWinners():**
    - Read the jackpot distribution loop
    - The loop distributes jackpot ETH across trait buckets using BPS percentages
    - For this audit, verify: (a) the sum of all BPS constants for jackpot distribution does not exceed 10_000 (would overpay), and (b) the distribution uses subtraction of previously-assigned amounts (not independent multiplication per bucket)
    - If independent multiplication per bucket: confirm the residual (unallocated rounding dust) is explicitly directed somewhere (e.g., stays in pool, or added to one bucket's share)

    For each site, produce:
    - Site: [function name, file:line]
    - Pattern: SAFE (subtraction of prior allocation) or RISKY (independent multiplication)
    - Rounding remainder disposition: explicitly directed or silently dropped
    - Status: CORRECT or FINDING-[SEVERITY]

    A rounding remainder of 1 wei on a single transaction is INFO-level. A remainder that accumulates across thousands of transactions could be LOW or MEDIUM depending on amount and who captures it (protocol pool or void).
  </action>
  <verify>
    All 4 BPS sites read from source (not from research summary).
    Each site has a rounding pattern classification (subtraction vs. independent multiplication).
    JackpotModule BPS constants sum verified.
  </verify>
  <done>ACCT-03 verdict determined for all BPS sites with rounding pattern analysis</done>
</task>

<task type="auto">
  <name>Task 2: Verify adminStakeEthForStEth guard and receive() donation safety (ACCT-09, ACCT-10)</name>
  <files>contracts/DegenerusGame.sol</files>
  <action>
    Read DegenerusGame.sol for both functions.

    **ACCT-09 — adminStakeEthForStEth() solvency guard (~lines 1873-1888):**

    Read the full function body. Locate the solvency guard logic.

    Verify:
    1. The `reserve` variable that prevents over-staking — confirm it equals `claimablePool` (not `currentPrizePool` or some other pool)
    2. The guard condition: `if (ethBal - amount < reserve)` or equivalent — confirm the comparison correctly prevents staking below the claimablePool floor
    3. Edge case: if `claimablePool == 0`, can admin stake the ENTIRE contract balance? Is this safe or does it leave other pools (currentPrizePool, nextPrizePool) unbacked by ETH?
       - Note: if admin stakes currentPrizePool ETH into stETH, the invariant still holds because stETH balance replaces ETH balance in the solvency formula. So ETH → stETH conversion is safe for the invariant.
       - But confirm: after staking, does the stETH balance show up in `yieldPoolView()` / the solvency invariant formula?
    4. Authorization: confirm only `ADMIN` can call this function (check modifier or guard)

    ACCT-09 verdict:
    - PASS: guard is claimablePool-based, authorization correct, post-stake stETH balance included in solvency formula
    - FINDING: guard uses wrong reserve variable, or ETH can be staked below obligations

    **ACCT-10 — receive() and selfdestruct:**

    Read `receive()` (DegenerusGame.sol ~2856-2858):
    1. Confirm the function body: `futurePrizePool += msg.value` — no conditional logic
    2. Check: does the futurePrizePool increment trigger any game state transitions? (e.g., level threshold check, BAF activation check)
       - If there is no threshold check inside `receive()`, donations cannot directly trigger transitions — PASS
       - If there IS a threshold check: a donation could trigger a level advance — FINDING-HIGH
    3. Check: does `receive()` credit any player-facing state (claimablePool, claimableWinnings)? No → donations cannot be extracted by any player → PASS

    selfdestruct forced ETH:
    1. `selfdestruct(payable(game))` forces ETH into the game contract without calling receive()
    2. Effect on solvency invariant: `balance` increases, all four pool variables unchanged
    3. Since invariant is `balance + stethBal >= obligations`, increasing `balance` makes it MORE satisfied
    4. Can forced ETH be extracted? Only through `claimWinnings()` (requires claimableWinnings credit), `adminSwapEthForStEth()` (admin-only), or `adminStakeEthForStEth()` (admin-only and stakes, not withdraws)
    5. The forced ETH becomes permanent protocol reserve — it cannot be extracted by normal players
    6. Assess: PASS (INFO) or FINDING if any extraction path is found

    ACCT-10 verdict: PASS (with INFO for selfdestruct surplus) or FINDING if receive() triggers state transitions
  </action>
  <verify>
    adminStakeEthForStEth guard variable confirmed as claimablePool from source (not assumed from research).
    receive() function body read from source and conditional logic confirmed absent.
    selfdestruct extraction path explicitly assessed.
  </verify>
  <done>ACCT-09 and ACCT-10 verdicts determined with source line citations</done>
</task>

<task type="auto">
  <name>Task 3: Write 08-05-SUMMARY.md with ACCT-03, ACCT-09, ACCT-10 verdicts</name>
  <files>.planning/phases/08-eth-accounting-invariant-and-cei-verification/08-05-SUMMARY.md</files>
  <action>
    Write 08-05-SUMMARY.md using the summary template. Include:

    - Summary: 2-3 sentences on what was audited (BPS splits, staking guard, receive/selfdestruct) and outcome
    - ACCT-03 verdict block:
      `**ACCT-03**: [PASS|FINDING-SEVERITY] — <rationale>`
      Include: BPS site table (4 rows: site, pattern, rounding disposition, status)
    - ACCT-09 verdict block:
      `**ACCT-09**: [PASS|FINDING-SEVERITY] — <rationale> [file:line]`
      Include: guard variable confirmed as claimablePool, authorization confirmed, edge case (zero claimablePool) assessed
    - ACCT-10 verdict block:
      `**ACCT-10**: [PASS|FINDING-SEVERITY] — <rationale> [file:line]`
      Include: receive() body confirmed, no state transitions, selfdestruct surplus assessment
    - For any FINDING: function name, file:line, ETH impact, attacker scenario
    - Key artifacts created: [08-05-SUMMARY.md]
    - Decisions made: verdict classifications for all three requirements

    Verdict format per CONTEXT.md:
    `**ACCT-XX**: [PASS|FINDING-SEVERITY] — <one-sentence rationale> [optional: exact function:line]`
  </action>
  <verify>
    `grep "ACCT-03\|ACCT-09\|ACCT-10" .planning/phases/08-eth-accounting-invariant-and-cei-verification/08-05-SUMMARY.md` returns three verdict lines.
    BPS site table has 4 rows.
    selfdestruct assessment is present (not omitted as out-of-scope).
  </verify>
  <done>08-05-SUMMARY.md written with ACCT-03, ACCT-09, and ACCT-10 verdicts and supporting source evidence</done>
</task>

</tasks>

<verification>
1. `grep "ACCT-03\|ACCT-09\|ACCT-10" .planning/phases/08-eth-accounting-invariant-and-cei-verification/08-05-SUMMARY.md` returns three verdict lines
2. BPS split rounding pattern (subtraction vs. independent multiplication) is documented for each site
3. adminStakeEthForStEth guard variable confirmed as claimablePool (not assumed)
4. receive() conditional logic absence confirmed from source
5. selfdestruct extraction path explicitly assessed
</verification>

<success_criteria>
ACCT-03, ACCT-09, and ACCT-10 have final verdicts backed by source evidence. BPS rounding patterns are classified at each site. The staking guard and receive() donation paths are confirmed or flagged with specific function:line evidence. All verdicts written to 08-05-SUMMARY.md.
</success_criteria>

<output>
After completion, create `.planning/phases/08-eth-accounting-invariant-and-cei-verification/08-05-SUMMARY.md` using the summary template.
</output>
