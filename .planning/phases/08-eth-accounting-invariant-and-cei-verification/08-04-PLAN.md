---
phase: 08-eth-accounting-invariant-and-cei-verification
plan: "04"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - test/helpers/invariantUtils.js
  - test/unit/EthInvariant.test.js
  - .planning/phases/08-eth-accounting-invariant-and-cei-verification/08-04-SUMMARY.md
autonomous: true
requirements:
  - ACCT-01
  - ACCT-08

must_haves:
  truths:
    - "assertSolvencyInvariant helper exists in test/helpers/invariantUtils.js and passes a fresh deploy"
    - "Invariant test passes after purchase, after advanceGame, after VRF fulfillment, after claimWinnings, after adminStakeEthForStEth"
    - "Game-over terminal state invariant test passes (or captures a specific deficit amount)"
    - "ACCT-01 verdict is informed by the ACCT-02 findings from plan 08-01"
    - "ACCT-01 and ACCT-08 each receive a final verdict"
  artifacts:
    - path: "test/helpers/invariantUtils.js"
      provides: "assertSolvencyInvariant and assertClaimablePoolConsistency helpers"
      exports: ["assertSolvencyInvariant", "assertClaimablePoolConsistency"]
    - path: "test/unit/EthInvariant.test.js"
      provides: "Hardhat test exercising invariant across 7 state sequences"
      contains: "assertSolvencyInvariant"
    - path: ".planning/phases/08-eth-accounting-invariant-and-cei-verification/08-04-SUMMARY.md"
      provides: "ACCT-01 and ACCT-08 verdicts with test evidence"
      contains: "ACCT-01"
  key_links:
    - from: "test/helpers/invariantUtils.js assertSolvencyInvariant"
      to: "DegenerusGame.currentPrizePoolView / nextPrizePoolView / futurePrizePoolView / claimablePoolView"
      via: "Public view getters — do NOT read storage directly"
      pattern: "assertSolvencyInvariant"
    - from: "test/unit/EthInvariant.test.js"
      to: "test/helpers/deployFixture.js deployFullProtocol()"
      via: "Reuse existing 22-contract fixture — do not write new deploy logic"
      pattern: "deployFullProtocol"
---

<objective>
Write the `assertSolvencyInvariant` Hardhat helper and an `EthInvariant.test.js` test file that calls it after 7 state transitions (fresh deploy, purchase, advanceGame, VRF+jackpot, claimWinnings, adminStakeEthForStEth, game-over). Use the ACCT-02 verdict from plan 08-01 to interpret any test failures — a failing invariant at a call site confirmed as FINDING in 08-01 is expected; an unexpected failure elsewhere is a new finding. Produce ACCT-01 and ACCT-08 verdicts.

Purpose: The invariant test is both evidence for the audit and a regression fixture. If the test passes, ACCT-01 is PASS. If it fails at a state sequence linked to a confirmed ACCT-02 gap site, that validates the ACCT-02 finding with a concrete test. If it fails elsewhere, that is a new ACCT-01 finding.

Output: Working invariant test suite + ACCT-01 and ACCT-08 verdicts in 08-04-SUMMARY.md.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-eth-accounting-invariant-and-cei-verification/08-CONTEXT.md
@.planning/phases/08-eth-accounting-invariant-and-cei-verification/08-RESEARCH.md
@.planning/phases/08-eth-accounting-invariant-and-cei-verification/08-01-SUMMARY.md
@test/helpers/deployFixture.js
@test/helpers/testUtils.js
@test/edge/GameOver.test.js

<interfaces>
<!-- Exact helper code from RESEARCH.md — use this as the implementation template -->

assertSolvencyInvariant helper (test/helpers/invariantUtils.js):
```javascript
import hre from "hardhat";
import { expect } from "chai";

export async function assertSolvencyInvariant(game, steth) {
  const gameAddr = await game.getAddress();
  const ethBal = await hre.ethers.provider.getBalance(gameAddr);
  const stethBal = await steth.balanceOf(gameAddr);
  const total = ethBal + stethBal;

  const current = await game.currentPrizePoolView();
  const next = await game.nextPrizePoolView();
  const future = await game.futurePrizePoolView(0n);
  const claimable = await game.claimablePoolView();
  const obligations = current + next + future + claimable;

  expect(total).to.be.gte(
    obligations,
    [
      `Solvency invariant violated!`,
      `  balance:      ${total}`,
      `  obligations:  ${obligations}`,
      `  deficit:      ${obligations - total}`,
      `  current:      ${current}`,
      `  next:         ${next}`,
      `  future:       ${future}`,
      `  claimable:    ${claimable}`,
    ].join("\n")
  );
}

export async function assertClaimablePoolConsistency(game, players) {
  const claimablePool = await game.claimablePoolView();
  let sumWinnings = 0n;
  for (const p of players) {
    const w = await game.claimableWinningsOf(p);
    sumWinnings += w;
  }
  expect(claimablePool).to.be.gte(sumWinnings,
    `claimablePool ${claimablePool} < sum(claimableWinnings) ${sumWinnings}`
  );
}
```

Key invariant formula (4-pool, from yieldPoolView()):
  game.balance + steth.balanceOf(game) >= currentPrizePool + nextPrizePool + futurePrizePool + claimablePool

IMPORTANT — use public view getters, NOT raw storage reads:
  game.currentPrizePoolView()
  game.nextPrizePoolView()
  game.futurePrizePoolView(0n)   ← argument is level offset (0 = current future pool)
  game.claimablePoolView()

1-wei sentinel pitfall: After claimWinnings(), claimablePool is NOT zero — each claim leaves 1 wei sentinel per player. The invariant still holds (sentinel is included in claimablePool accounting). Do NOT assert claimablePool == 0 after claiming.

Project is ESM (type: "module") — use `import` not `require`. Test pattern from GameOver.test.js.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write assertSolvencyInvariant helper (test/helpers/invariantUtils.js)</name>
  <files>test/helpers/invariantUtils.js</files>
  <action>
    Create `test/helpers/invariantUtils.js` using the exact template from the `<interfaces>` block above.

    The file must:
    - Use ESM syntax (`import`/`export`) — project is `"type": "module"`
    - Export `assertSolvencyInvariant(game, steth)` using the 4-pool formula
    - Export `assertClaimablePoolConsistency(game, players)` for spot-check of per-player winnings
    - Use `game.futurePrizePoolView(0n)` with BigInt argument (not 0) — Solidity uint256 requires BigInt in ethers v6
    - Include JSDoc comments explaining: what the invariant checks, the 1-wei sentinel note, and the replicates-yieldPoolView comment

    Do NOT read storage directly. Only use the public view functions listed in the interfaces block.

    After writing, verify the file is syntactically valid by checking for common ESM pitfalls (no `require`, no `module.exports`).
  </action>
  <verify>
    `node --input-type=module --eval "import './test/helpers/invariantUtils.js'" 2>&1` exits cleanly (or equivalent ESM import check).
    `grep "assertSolvencyInvariant\|assertClaimablePoolConsistency" test/helpers/invariantUtils.js` shows both exports.
  </verify>
  <done>test/helpers/invariantUtils.js exists, exports both helpers, uses ESM syntax</done>
</task>

<task type="auto">
  <name>Task 2: Write EthInvariant.test.js across 7 state sequences</name>
  <files>test/unit/EthInvariant.test.js</files>
  <action>
    Read GameOver.test.js for the established test structure pattern. Read deployFixture.js for the `deployFullProtocol()` API and the fixture shape (what variables are returned: `game`, `steth`, `mockVRF`, `admin`, `players`, etc.).

    Write `test/unit/EthInvariant.test.js` with these 7 invariant checkpoints:

    1. **Fresh deploy**: Call `assertSolvencyInvariant(game, steth)` immediately after fixture deploy — no purchases made
    2. **After purchase**: One player calls `game.purchase(...)` — use the minimum valid purchase (1 ticket = qty 400). Call `assertSolvencyInvariant` after.
    3. **After advanceGame (level transition)**: Drive enough purchases to trigger a level advance (use a helper or directly set up state). Call `assertSolvencyInvariant` after advanceGame completes.
    4. **After VRF fulfillment + jackpot distribution**: Fulfill VRF using `fulfillVRF(mockVRF, requestId, word)` from testUtils. Call `assertSolvencyInvariant` after jackpot distribution is settled.
    5. **After claimWinnings**: Have a winner call `game.claimWinnings()`. Call `assertSolvencyInvariant` after. Do NOT assert claimablePool == 0 (1-wei sentinel remains).
    6. **After adminStakeEthForStEth**: Admin stakes some ETH into stETH (call `game.adminStakeEthForStEth(amount)` from admin signer). Call `assertSolvencyInvariant` after — balance + stethBalance should still cover obligations.
    7. **Game-over terminal state**: Use the GameOver.test.js pattern to drive the game to game-over state. Call `assertSolvencyInvariant` after the terminal settlement. Also call `assertClaimablePoolConsistency(game, [winner1, winner2])` to verify per-player accounting.

    Important implementation notes:
    - Use `loadFixture` from hardhat-network-helpers (established project pattern — check GameLifecycle.test.js if GameOver.test.js uses it)
    - For state sequences that require significant setup (level advance, VRF), use the helpers from testUtils.js: `advanceTime()`, `fulfillVRF()`, `advanceToNextDay()`
    - For purchase calls: use the correct signature `game.purchase(buyer, ticketQty, lootBoxAmount, affiliateCode, payKind)` where qty 400 = 1 full ticket
    - For ACCT-08 game-over: mirror the GameOver.test.js setup pattern — it already has the multi-step advance→VRF→fulfill→advanceGame→gameOver=true sequence
    - If a specific state sequence is too complex to set up in reasonable test code (e.g., full game-over requires 7+ steps), use a simplified approximation and document what is being tested

    If the ACCT-02 verdict from 08-01-SUMMARY.md identified FINDING sites: add a comment in the test at the relevant checkpoints (e.g., "// ACCT-02 FINDING at DecimatorModule:517 — invariant may fail here if decimator path was exercised") so failures are interpretable.

    Run the test after writing:
    `npx hardhat test test/unit/EthInvariant.test.js --no-compile`

    If the test fails:
    - If the failure is at a state sequence linked to a confirmed ACCT-02 FINDING: document the deficit amount — this validates the ACCT-02 finding
    - If the failure is at an unexpected state sequence: this is a new ACCT-01 finding — document the function name, state, and deficit amount
  </action>
  <verify>
    `npx hardhat test test/unit/EthInvariant.test.js` runs (even if some assertions fail — failures are findings, not test errors).
    All 7 state sequence checkpoints exist in the test file (grep for the checkpoint descriptions).
    Game-over checkpoint (ACCT-08) is present.
  </verify>
  <done>
    EthInvariant.test.js runs without syntax errors. All 7 checkpoints present. Test output (pass/fail per checkpoint) documented.
  </done>
</task>

<task type="auto">
  <name>Task 3: Interpret results and write 08-04-SUMMARY.md with ACCT-01 and ACCT-08 verdicts</name>
  <files>.planning/phases/08-eth-accounting-invariant-and-cei-verification/08-04-SUMMARY.md</files>
  <action>
    Based on the invariant test results from Task 2 and the ACCT-02 verdict from 08-01-SUMMARY.md, produce ACCT-01 and ACCT-08 verdicts.

    ACCT-01 verdict logic:
    - If all 7 checkpoints PASS: `**ACCT-01**: PASS — solvency invariant holds across all 7 tested state sequences (fresh deploy, purchase, level advance, VRF fulfillment, claimWinnings, adminStakeEthForStEth, game-over)`
    - If failures only at ACCT-02 FINDING sites: `**ACCT-01**: FINDING-HIGH — solvency invariant broken at [state]; deficit = [amount]; linked to confirmed ACCT-02 claimablePool sync gap at [function:line]`
    - If failures at unexpected sites: `**ACCT-01**: FINDING-[SEVERITY] — solvency invariant broken at [state]; deficit = [amount]; unrelated to ACCT-02; new finding at [function:line]`

    ACCT-08 verdict logic:
    - ACCT-08 is the game-over zero-balance proof
    - If checkpoint 7 (game-over) passes: `**ACCT-08**: PASS — game-over terminal state holds solvency invariant; all claimable amounts resolvable`
    - If checkpoint 7 fails: `**ACCT-08**: FINDING-[SEVERITY] — game-over state produces solvency deficit of [amount] at [function:line]`
    - Note: in game-over state, `claimablePool` will still contain 1-wei sentinels per prior claimer — this is expected and not a finding

    Write 08-04-SUMMARY.md using the summary template. Include:
    - Summary: 2-3 sentences (invariant helper written, test run across 7 sequences, results)
    - ACCT-01 verdict block
    - ACCT-08 verdict block
    - Test results table: 7 rows (state sequence | PASS or FAIL | deficit amount if FAIL)
    - If any new findings beyond ACCT-02: full finding details (function, line, impact, scenario)
    - Key artifacts created: [test/helpers/invariantUtils.js, test/unit/EthInvariant.test.js, 08-04-SUMMARY.md]
    - Patterns established: invariantUtils.js helper is reusable in future plans (e.g., plan 08-05 for staking state sequence)
  </action>
  <verify>
    `grep "ACCT-01\|ACCT-08" .planning/phases/08-eth-accounting-invariant-and-cei-verification/08-04-SUMMARY.md` returns two verdict lines.
    Test results table has 7 rows.
    Any test failure has deficit amount documented.
  </verify>
  <done>
    08-04-SUMMARY.md written with ACCT-01 and ACCT-08 verdicts and test evidence. The invariant test results are documented, not just the verdict.
  </done>
</task>

</tasks>

<verification>
1. `test/helpers/invariantUtils.js` exists with both exports
2. `npx hardhat test test/unit/EthInvariant.test.js` runs without syntax errors
3. `grep "ACCT-01\|ACCT-08" .planning/phases/08-eth-accounting-invariant-and-cei-verification/08-04-SUMMARY.md` returns two verdicts
4. Test results (pass/fail per checkpoint) are documented in the summary
</verification>

<success_criteria>
The assertSolvencyInvariant helper is written and the EthInvariant test runs across all 7 state sequences. ACCT-01 verdict is directly supported by test output (not just manual reasoning). ACCT-08 game-over verdict is confirmed by checkpoint 7. All test failures are classified as either confirming a known ACCT-02 finding or identifying a new ACCT-01 finding.
</success_criteria>

<output>
After completion, create `.planning/phases/08-eth-accounting-invariant-and-cei-verification/08-04-SUMMARY.md` using the summary template.
</output>
