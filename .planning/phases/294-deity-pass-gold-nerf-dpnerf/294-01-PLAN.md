---
phase: 294-deity-pass-gold-nerf-dpnerf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md
  - .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md
autonomous: true
requirements: [DPNERF-02, DPNERF-03, DPNERF-04, DPNERF-05, DPNERF-06]
tags: [audit, deity-pass, gold-nerf, design-intent-trace, measurement-scaffold, pre-patch-gate, v42.0]
must_haves:
  truths:
    # DPNERF-06 — anchor recording BEFORE the contract patch lands
    - "Design-intent trace doc `294-01-DESIGN-INTENT-TRACE.md` lands in the phase artifact directory and explicitly records the 3 roadmap-locked anchors `D-42N-GOLD-FLOOR-01` (flat 1 virtual entry on gold; user-locked 2026-05-17) + `D-42N-DEITY-EV-01` (intentional EV reduction; no common-tier compensation; user-locked 2026-05-17) + `D-42N-PATH-COVERAGE-01` (caller-uniform — ETH + BURNIE near-future coin jackpot paths both covered; user-locked 2026-05-17) plus the 2 CONTEXT.md-locked sub-anchors `D-294-CALLER-UNIFORM-01` (all-4-callsite uniform coverage extending PATH-COVERAGE) + `D-294-NATSPEC-01` (locked two-tier `what IS` comment shape) per `feedback_design_intent_before_deletion.md` (per DPNERF-06)"
    - "Design-intent trace covers section (i): original `virtualCount = len / 50; if (virtualCount < 2) virtualCount = 2` rationale — why 2% of bucket with min-2 floor was the v41 design for deity virtual entries (small-bucket minimum guarantee; deity earns a baseline of at least 2 virtual entries even when bucket is small) (per DPNERF-06(i))"
    - "Design-intent trace covers section (ii): gold-tile concentration issue — small-bucket × 2% floor disproportionately rewards deity-pass holders on gold-tier wins because the gold buckets are the smallest tier across the (quadrant, color, symbol) trait space; the min-2 floor produces an outsized EV concentration on those small buckets (per DPNERF-06(ii))"
    - "Design-intent trace covers section (iii): compensation trade-offs — keep EV constant via commons-bump vs intentional reduction vs intentional rebias-toward-commons; LOCKED at intentional reduction per `D-42N-DEITY-EV-01` (deity-pass weaker overall on gold; commons unchanged at `max(len/50, 2)`); the 3 alternatives are enumerated with their EV/incentive trade-offs (per DPNERF-06(iii))"
    - "Design-intent trace covers section (iv): path-coverage trade-offs — both paths symmetric vs ETH-only vs all-caller-uniform; LOCKED at all-4-callsite uniform per `D-294-CALLER-UNIFORM-01` (roadmap named ETH + BURNIE; the literal scope of the function-body change is broader — all 4 `_randTraitTicket` callsites at L698 / L988 / L1296 / L1399 are covered uniformly by construction with no callsite flag or path-discrimination logic) (per DPNERF-06(iv))"
    # Out-of-scope register — explicit enumeration per REQUIREMENTS.md lines 22-27
    - "Trace doc explicitly enumerates the OUT-OF-SCOPE items NOT touched by Phase 294 per the v42.0 REQUIREMENTS.md `## Out of Scope` register lines 22-27: (a) DPNERF common-tier compensation — explicitly excluded per D-42N-DEITY-EV-01; common-tier `virtualCount = max(len/50, 2)` UNCHANGED; (b) storage-layout changes — zero new slots, zero new SSTORE/SLOAD; (c) public ABI changes — `_randTraitTicket` is private; all 4 caller signatures unchanged; (d) boon distribution for deity-pass holders — `DegenerusGameBoonModule.sol` `BP_DEITY_PASS_TIER_SHIFT` flow UNCHANGED; (e) deity-pass purchase pricing — `DegenerusGameWhaleModule.sol` `DEITY_PASS_BASE` UNCHANGED; (f) soulbound NFT minting — `DegenerusDeityPass.sol` UNCHANGED. For each item state `out-of-scope per [register reason]; not addressed by Plan 02`"
    # SWEEP-02(iii) pre-emptive answers
    - "Trace doc pre-emptively answers the Phase 296 SWEEP DPNERF adversarial hypotheses (per CONTEXT.md `D-294-CALLER-UNIFORM-01` SWEEP-scope expansion): (i) all-4-callsite uniformity attestation — does the gold nerf produce any incentive shift in the early-bird lootbox path (callsite L698) where deity-pass holders might game the lootbox window differently? (ii) does the carryover-ticket-distribution path (callsite L988 via L652) leak un-nerfed gold EV across levels? (iii) does the EV reduction destabilize deity-pass holder behavior (secondary attack, e.g., deity owners pivot to non-gold strategies)? (iv) does ETH vs BURNIE path-coverage create any differential behavior an attacker can game (answer: no by construction — single-function body change reaches both paths)"
    # Measurement scaffold — load-bearing for Plan 02 batched commit body
    - "Measurement scaffold doc `294-01-MEASUREMENT.md` lands in the phase artifact directory with 6 section headers + the audit baseline anchor `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` (per v42 D-42N-MILESTONE-OPEN-01) plus the Phase 292 close as the most-recent comparison anchor; numerical fields are placeholders Plan 02 fills in post-patch"
    - "Measurement scaffold lists 6 attestation sections (per CONTEXT.md `<phase_specific_guidance>` D-294-SIDECARS-01): §1 audit baseline anchored to `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` + Phase 292 close; §2 storage byte-identity attestation placeholder for `forge inspect storageLayout` EMPTY diff (DPNERF-04); §3 callsite enumeration — all 4 `_randTraitTicket` callsites with their upstream call paths plus the BURNIE near-future coin jackpot resolution via `payDailyCoinJackpot` (L1767) → `_awardDailyCoinToTraitWinners` (L1816+) → `_randTraitTicket` per D-294-CALLER-UNIFORM-01; §4 public ABI byte-identity attestation placeholder for `forge inspect methodIdentifiers` EMPTY diff (DPNERF-05); §5 theoretical bytecode-delta estimate per `feedback_gas_worst_case.md` theoretical-first methodology — analytical derivation of one `EQ` + `JUMPI` + small constant pool + gold-tier `MSTORE`; expected ~10-30 byte addition; no empirical second-pass at Phase 294 (TST-DPNERF-01..05 ships no gas-regression test per Phase 295 scope); §6 zero-new-state grep-proof for `_randTraitTicket` body in DPNERF scope (placeholder for the post-patch grep evidence: zero new SSTORE / SLOAD callsites)"
    - "Measurement scaffold §3 callsite enumeration table records the 4 callsites with their pre-patch line numbers + upstream call paths verbatim from CONTEXT.md `<specifics>`: (i) L698 `_runEarlyBirdLootboxJackpot` — early-bird lootbox jackpot trait winners (purchase-phase tickets distributed at `lvl+1`); (ii) L988 `_distributeTicketsToBucket` (helper called by `_distributeTicketJackpot` from L637 daily-tickets / L652 carryover-tickets / L883 early-bird-post-purchase-tickets) — trait-bucket ticket winner selection; (iii) L1296 `_processDailyEth` — daily ETH jackpot trait winners (the path the roadmap names as `_runJackpotEthFlow`: L1142 → L1232 → L1296); (iv) L1399 `_resolveTraitWinners` — ETH trait-winner resolution sub-flow. Plus the BURNIE near-future coin jackpot path via `payDailyCoinJackpot` (L1767) → `_awardDailyCoinToTraitWinners` (L1816+) → trait-bucket sampling → `_randTraitTicket`"
    # Sidecar discipline — Plan 02 blocks on existence of both artifacts
    - "Trace + scaffold are AGENT-COMMITTED artifacts (no user approval needed — zero contract / test edits per `feedback_no_contract_commits.md`); Plan 02 cannot start its contract-edit task until both artifacts exist at the paths in `files_modified` (the design-intent-before-deletion gate per `feedback_design_intent_before_deletion.md`)"
    # Carry-forward locks
    - "v41 audit baseline `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` preserved as comparison anchor (v42.0 milestone open against v41 close per `D-42N-MILESTONE-OPEN-01`); Phase 292 close cited as the most-recent intermediate anchor for the storage / ABI diff baseline (Phase 290 + Phase 292 already attested zero storage / ABI delta vs v41; Phase 294 inherits and re-attests on top)"
    - "v41 Phase 281 owed-salt determinism path UNCHANGED at v42 Phase 294 (DPNERF doesn't touch the mint-batch determinism surface — distinct subsystem)"
    - "v41 Phase 288 `dailyIdx` structural anchor UNCHANGED at v42 Phase 294 (DPNERF doesn't touch the hero-override day-index path — distinct subsystem)"
    - "RNG commitment-window invariant NOT IMPACTED by Phase 294 per CONTEXT.md `<canonical_refs>` audit-methodology section: DPNERF doesn't introduce a new RNG consumer; `_randTraitTicket`'s `randomWord` consumption pattern is unchanged (still `keccak256(abi.encode(randomWord, trait, salt, i)) % effectiveLen` at L1743-1745); the gold-tier branch is deterministic on `trait` which is already committed at VRF-request time by the upstream `_rollWinningTraits` consumer — no commitment-window backward-trace burden added"
    # Frozen-contract discipline
    - "No extensibility-style refactors planned per `feedback_frozen_contracts_no_future_proofing.md`: no `GOLD_COLOR = 7` named constant (magic-7 well-established across `DegenerusTraitUtils.sol:50,97,112,185,193,194,214` + `DegenerusGameDegeneretteModule.sol:854,855` + `DegenerusGameJackpotModule.sol:1089,1105`); no `uint8 color = (trait >> 3) & 7;` local-variable cache (the gold-tier check fires once per `_randTraitTicket` invocation, not in a hot loop; inline expression has identical bytecode cost); branch shape reuses the existing precedent idiom at `_pickSoloQuadrant` L1105 (`((traits[i] >> 3) & 7) == 7`)"
  artifacts:
    - path: ".planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md"
      provides: "DPNERF-06 design-intent trace covering (i) original `virtualCount = len/50; if < 2: = 2` rationale, (ii) gold-tile concentration issue, (iii) compensation trade-offs locked at intentional reduction (D-42N-DEITY-EV-01), (iv) path-coverage trade-offs locked at all-4-callsite uniform (D-294-CALLER-UNIFORM-01); 5 decision anchors recorded (D-42N-GOLD-FLOOR-01 + D-42N-DEITY-EV-01 + D-42N-PATH-COVERAGE-01 + D-294-CALLER-UNIFORM-01 + D-294-NATSPEC-01); out-of-scope register enumerated; SWEEP-02(iii) DPNERF adversarial hypotheses pre-emptively answered"
      min_lines: 90
    - path: ".planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md"
      provides: "Scaffold for Plan 02's 6 load-bearing attestations (§1 audit baseline + Phase 292 close anchor; §2 storage byte-identity placeholder for forge inspect storageLayout EMPTY diff per DPNERF-04; §3 callsite enumeration — all 4 `_randTraitTicket` callsites + BURNIE near-future coin jackpot path; §4 public ABI byte-identity placeholder for forge inspect methodIdentifiers EMPTY diff per DPNERF-05; §5 theoretical bytecode-delta estimate per feedback_gas_worst_case.md analytical-first methodology — no empirical second-pass at Phase 294; §6 zero-new-state grep-proof placeholder); anchored to `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4`"
      min_lines: 40
  key_links:
    - from: ".planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md"
      to: ".planning/phases/294-deity-pass-gold-nerf-dpnerf/294-02-PLAN.md (contract patch task)"
      via: "design-intent-before-deletion gate per `feedback_design_intent_before_deletion.md`"
      pattern: "D-42N-GOLD-FLOOR-01"
    - from: ".planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md"
      to: "Plan 02 batched commit message body"
      via: "copy-forward source for the §2 storage byte-identity + §3 callsite enumeration + §4 public ABI byte-identity + §5 theoretical bytecode-delta + §6 zero-new-state grep-proof attestations"
      pattern: "MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4"
    - from: ".planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md (SWEEP-02(iii) pre-emptive answers)"
      to: ".planning/phases/296-sweep-cross-surface-adversarial/296-PLAN.md (Phase 296 SWEEP DPNERF adversarial hypothesis surface)"
      via: "D-294-CALLER-UNIFORM-01 SWEEP-scope expansion — all-4-callsite uniformity attestation + incentive-shift across early-bird lootbox + carryover-ticket-distribution paths"
      pattern: "D-294-CALLER-UNIFORM-01"
    - from: ".planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md §3 callsite enumeration"
      to: ".planning/phases/297-terminal-audit-findings-v42/297-PLAN.md §3.A delta-surface table (DPNERF row)"
      via: "all-4-callsite citation per D-294-CALLER-UNIFORM-01 — Phase 297 §3.A table cites every caller path by line number under the DPNERF row"
      pattern: "L698|L988|L1296|L1399"
    - from: ".planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md §3 callsite enumeration + §6 zero-new-state grep-proof"
      to: ".planning/phases/295-tst-dpnerf-regression-fixture/295-PLAN.md (TST-DPNERF-01..05 fixture)"
      via: "Phase 295 references Phase 294 audit-subject commit + the 4-callsite enumeration; TST-DPNERF-01 + 02 + 03 implicitly cover callsites 3 + 4 + BURNIE path; callsites 1 (L698) + 2 (L988) are NOT explicitly covered by TST-DPNERF — Phase 296 SWEEP attests their behavior"
      pattern: "TST-DPNERF"
---

<objective>
Land the DPNERF-06 design-intent-before-deletion gate as two AGENT-COMMITTED planning artifacts in `.planning/phases/294-deity-pass-gold-nerf-dpnerf/`: (1) `294-01-DESIGN-INTENT-TRACE.md` recording the 5 phase-scope decision anchors (`D-42N-GOLD-FLOOR-01` + `D-42N-DEITY-EV-01` + `D-42N-PATH-COVERAGE-01` + `D-294-CALLER-UNIFORM-01` + `D-294-NATSPEC-01`) with the 4-section trace DPNERF-06 requires (per CONTEXT.md `<phase_specific_guidance>` D-294-SIDECARS-01); (2) `294-01-MEASUREMENT.md` scaffold with the 6 load-bearing attestation section headers Plan 02 fills in post-patch.

Purpose: Satisfy the `feedback_design_intent_before_deletion.md` discipline — the user requires the original-design rationale for what is about to be changed (the `virtualCount = len / 50; if (virtualCount < 2) virtualCount = 2` floor logic + the 3-line comment block at L1721-1723) to be traced BEFORE the contract patch lands. Plan 02 cannot begin its contract-edit task until both artifacts exist. Also seed the measurement scaffold so Plan 02's batched commit message body has a verbatim copy-forward source for every numerical / structural attestation (`feedback_gas_worst_case.md` theoretical-first + `feedback_no_history_in_comments.md` no-history-in-comments disciplines combine to push the bytecode delta / storage layout / callsite enumeration / public ABI / zero-new-state grep numbers into the commit body, NOT into source comments).

Output:
- `294-01-DESIGN-INTENT-TRACE.md` — 4-section trace per DPNERF-06(i..iv) + 5 decision anchors + out-of-scope register + SWEEP-02(iii) DPNERF adversarial-hypothesis pre-emptive answers + Plan-02 pre-patch-gate statement; AGENT-COMMITTED.
- `294-01-MEASUREMENT.md` — 6-section scaffold (§1 audit baseline; §2 storage byte-identity placeholder; §3 callsite enumeration — all 4 callsites + BURNIE path; §4 public ABI byte-identity placeholder; §5 theoretical bytecode-delta estimate per `feedback_gas_worst_case.md` analytical-first method; §6 zero-new-state grep-proof placeholder); AGENT-COMMITTED; numerical fields populated by Plan 02 post-patch.

This plan makes ZERO contract / test / KNOWN-ISSUES.md edits and triggers ZERO user approval gates (per `feedback_no_contract_commits.md` — only contract / test edits need explicit approval).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/294-deity-pass-gold-nerf-dpnerf/294-CONTEXT.md

@contracts/modules/DegenerusGameJackpotModule.sol

<interfaces>
<!-- Load-bearing identifiers / line ranges / current code shapes. Extracted from live v42.0 HEAD relative to audit baseline `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4`. -->
<!-- Executor uses these directly; no codebase exploration needed beyond the @-references above. -->

Current `_randTraitTicket` function signature + body (`contracts/modules/DegenerusGameJackpotModule.sol:1706-1757` — REFERENCED ONLY in Plan 01; PATCHED by Plan 02):
```
/// @dev Selects random winners from a trait's ticket pool, returning both addresses and indices.
function _randTraitTicket(
    address[][256] storage traitBurnTicket_,
    uint256 randomWord,
    uint8 trait,
    uint8 numWinners,
    uint8 salt
)
    private
    view
    returns (address[] memory winners, uint256[] memory ticketIndexes)
{
    address[] storage holders = traitBurnTicket_[trait];
    uint256 len = holders.length;

    // Virtual deity entries: floor(2% of bucket tickets), minimum 2, if a deity exists for this symbol.
    // traitId layout: (quadrant << 6) | (color << 3) | symIdx
    // fullSymId = quadrant * 8 + symIdx
    uint8 fullSymId = (trait >> 6) * 8 + (trait & 0x07);
    address deity;
    uint256 virtualCount;
    if (fullSymId < 32) {
        deity = deityBySymbol[fullSymId];
        if (deity != address(0)) {
            virtualCount = len / 50;
            if (virtualCount < 2) virtualCount = 2;
        }
    }

    uint256 effectiveLen = len + virtualCount;
    // ...remainder unchanged: winner sampling loop
}
```

Existing gold-tier extraction idiom precedent at `contracts/modules/DegenerusGameJackpotModule.sol:1098-1115` `_pickSoloQuadrant` (REFERENCED ONLY in Plan 01; UNTOUCHED by Plan 02 — byte-identical post-patch per CONTEXT.md `<code_context>` "Out-of-Scope Source-Tree Surfaces"):
```
function _pickSoloQuadrant(uint8[4] memory traits, uint256 entropy) internal pure returns (uint8) {
    uint256 goldQuads;
    uint8 goldCount;
    for (uint8 i; i < 4; ++i) {
        if (((traits[i] >> 3) & 7) == 7) {
            goldQuads |= uint256(i) << (goldCount * 8);
            unchecked { ++goldCount; }
        }
    }
    if (goldCount == 0) {
        return uint8((3 - (entropy & 3)) & 3);
    }
    uint8 idx = uint8((entropy >> 4) % goldCount);
    return uint8((goldQuads >> (idx * 8)) & 0xFF);
}
```

The four `_randTraitTicket` callsites covered uniformly by the Plan 02 function-body change per D-294-CALLER-UNIFORM-01:
- `contracts/modules/DegenerusGameJackpotModule.sol:698` — `_runEarlyBirdLootboxJackpot`: `) = _randTraitTicket(bucket, rngWord, traitId, 25, t);` (early-bird lootbox jackpot trait winners; 3% of `futurePrizePool` distributed at `lvl+1`; 100 winners across 4 traits = 25/trait)
- `contracts/modules/DegenerusGameJackpotModule.sol:988` — `_distributeTicketsToBucket` (helper called by `_distributeTicketJackpot` from L637 daily-tickets / L652 carryover-tickets / L883 early-bird-post-purchase-tickets): `) = _randTraitTicket(` invocation in trait-bucket ticket winner selection
- `contracts/modules/DegenerusGameJackpotModule.sol:1296` — `_processDailyEth` (daily ETH jackpot trait winners; the roadmap-named `_runJackpotEthFlow` path resolves through here: `_runJackpotEthFlow` L1142 → `_processDailyEth` L1232 → `_randTraitTicket` L1296)
- `contracts/modules/DegenerusGameJackpotModule.sol:1399` — `_resolveTraitWinners` (ETH trait-winner resolution sub-flow; called from `_processDailyEth` ticket-payout sub-path)

The BURNIE near-future coin jackpot path (named in the roadmap as "BURNIE coin jackpot path") resolves via `payDailyCoinJackpot` (L1767) → `_awardDailyCoinToTraitWinners` (L1816+) → trait-bucket sampling → `_randTraitTicket`. The same function-body change reaches this path by construction.

Storage layout reference: `contracts/storage/DegenerusGameStorage.sol` — `deityBySymbol[uint8 => address]` mapping is the ONLY DPNERF-touched storage read (already read at L1728 pre-patch); ZERO new storage slots; ZERO new mappings; ZERO new SSTORE callsites; ZERO new SLOAD callsites in DPNERF scope.

Public ABI reference: `_randTraitTicket` is `private` — not in public ABI; the 4 caller paths above are private/internal helpers; the public entry points reachable through this chain (`payDailyCoinJackpot` at L1767 is the only `external` callsite within 2 hops; the other entry points are reachable through the `_runJackpotEthFlow` + `_distributeTicketJackpot` + `_runEarlyBirdLootboxJackpot` private chain) have UNCHANGED signatures + parameter types + return shapes.

Audit baseline reference: `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` (v41.0 closure HEAD; source of truth for "byte-identical to v41 close" assertions in `294-01-MEASUREMENT.md` scaffold and Plan 02's populated values). Phase 292 close cited as the most-recent intermediate anchor (after MINTCLN + HRROLL surfaces landed) for the storage / ABI diff baseline.

Decision anchor recap (all 5 in scope for `294-01-DESIGN-INTENT-TRACE.md`):
- `D-42N-GOLD-FLOOR-01` — flat 1 virtual entry on gold-tier (color == 7); user-locked 2026-05-17
- `D-42N-DEITY-EV-01` — intentional EV reduction; no common-tier compensation; user-locked 2026-05-17
- `D-42N-PATH-COVERAGE-01` — both ETH + BURNIE paths covered; user-locked 2026-05-17
- `D-294-CALLER-UNIFORM-01` — extends PATH-COVERAGE: all 4 `_randTraitTicket` callsites covered uniformly by the single function-body change (no callsite flag; no path-discrimination logic); Phase 296 SWEEP coverage extends from "ETH vs BURNIE differential-behavior" to "all-4-callsite uniformity attestation"
- `D-294-NATSPEC-01` — locked exact 5-line two-tier "what IS" comment block shape at L1721-1723 (Gold tier flat 1; Common tier floor(2%) min 2); zero history language; zero decision-anchor citations in source comments
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author `294-01-DESIGN-INTENT-TRACE.md` covering the 4 DPNERF-06 trace sections + 5 decision anchors + out-of-scope register + SWEEP-02(iii) pre-emptive answers</name>
  <files>
    .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md
  </files>
  <read_first>
    - .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-CONTEXT.md (load-bearing; locked decisions D-294-CALLER-UNIFORM-01 + D-294-SIDECARS-01 + D-294-NATSPEC-01 + D-294-RESEARCH-SKIP-01; Claude's-Discretion dispositions; out-of-scope source-tree surfaces; exact branch + comment shapes in `<specifics>`)
    - .planning/REQUIREMENTS.md lines 22-27 (DPNERF Out-of-Scope register) + lines 80-87 (DPNERF-01..06 verbatim)
    - .planning/ROADMAP.md lines 197-210 (Phase 294 detail block: Goal + Depends + Requirements + 5 Success Criteria) + line 41 (Phase 294 entry)
    - contracts/modules/DegenerusGameJackpotModule.sol L1700-L1760 (the `_randTraitTicket` function body — pre-patch state; load-bearing for the section-(i) original-rationale paraphrase)
    - contracts/modules/DegenerusGameJackpotModule.sol L1080-L1130 (the `_pickSoloQuadrant` precedent function — established gold-tier extraction idiom `((traits[i] >> 3) & 7) == 7` reused by DPNERF; OUT OF SCOPE for the Plan 02 patch — byte-identical post-patch)
    - contracts/DegenerusTraitUtils.sol (gold = color 7 semantic confirmation; load-bearing for the section-(ii) gold-tile concentration explanation)
    - .planning/phases/290-mint-batch-event-sig-cleanup-mintcln/290-01-DESIGN-INTENT-TRACE.md (sister precedent — section structure + decision-anchor recording form + out-of-scope register form + SWEEP pre-emptive-answers form)
    - .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md (sister precedent — 4-section trace shape for DPNERF-06's (i) (ii) (iii) (iv); reference for prose style + line-number citation discipline)
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_design_intent_before_deletion.md (the discipline this task satisfies)
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md (NOTE: this discipline applies to NatSpec/contract comments, NOT to planning artifacts — the trace doc DOES contain historical context by design)
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_frozen_contracts_no_future_proofing.md (the discipline that rejects `GOLD_COLOR = 7` named constant + `uint8 color = ...` local-var cache)
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_dead_guards.md (the discipline that rejects unreachable safety branches)
  </read_first>
  <action>
    Create `294-01-DESIGN-INTENT-TRACE.md` (markdown; agent-side planning artifact; ≥ 90 lines). Structure with the following sections in order:

    `# Phase 294 DPNERF — Design-Intent Trace (DPNERF-06)`

    `## Audit Baseline + Anchors` — list `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` (v41 close; per `D-42N-MILESTONE-OPEN-01`) + Phase 292 close as the most-recent intermediate anchor. Then list all 5 phase-scope decision anchors verbatim with disposition lines: `D-42N-GOLD-FLOOR-01` (flat 1 virtual entry on gold-tier; user-locked 2026-05-17) + `D-42N-DEITY-EV-01` (intentional EV reduction; no common-tier compensation; user-locked 2026-05-17) + `D-42N-PATH-COVERAGE-01` (both ETH + BURNIE paths covered; user-locked 2026-05-17) + `D-294-CALLER-UNIFORM-01` (all-4-callsite uniform extension of PATH-COVERAGE; planner-locked 2026-05-17 per CONTEXT.md) + `D-294-NATSPEC-01` (locked exact 5-line two-tier `what IS` comment shape; planner-locked 2026-05-17 per CONTEXT.md). Also cite carry-forward anchors: `D-42N-MILESTONE-OPEN-01` (v42 milestone open at v41 close HEAD); `D-281-FIX-SHAPE-01` (v41 owed-salt cross-call seed-separation; CITED FOR COMPLETENESS — DPNERF doesn't touch the mint-batch determinism path); `D-288-FIX-SHAPE-01` (v41 `dailyIdx` structural anchor; CITED FOR COMPLETENESS — DPNERF doesn't touch the hero-override day-index path).

    `## Section (i) — Original `virtualCount = len / 50; if (virtualCount < 2) virtualCount = 2` Rationale` — trace WHY the v41 design used 2% of bucket with a min-2 floor for deity virtual entries. Cite the current code at `contracts/modules/DegenerusGameJackpotModule.sol:1729-1731`. Explain the v41 design intent: deity-pass holders earn a "skim" on top of the trait-bucket distribution, sized at 2% of the bucket so the deity's expected share scales with the bucket's total ticket count; the min-2 floor exists to guarantee a baseline payout on small buckets (where `len / 50` rounds to 0 or 1) — without the floor, deity owners would earn zero or trivial value on small buckets even though deity ownership represents a meaningful capital commitment. Explicitly note: the v41 design treated all 8 colors uniformly (no per-tier differentiation); the 2% + floor was applied across the (quadrant × color × symbol) space.

    `## Section (ii) — Gold-Tile Concentration Issue` — explain the disproportionate-reward issue the DPNERF nerf addresses. Gold-tier traits (`color == 7`) sit in the smallest bucket tier across the trait-byte layout `(quadrant << 6) | (color << 3) | symIdx` (semantic confirmed via `contracts/DegenerusTraitUtils.sol:50,97,112,214` `scaled == 14 → gold (color 7)` mapping). On small gold buckets, `len / 50` rounds to 0 or 1, and the min-2 floor kicks in immediately — the deity earns a structurally outsized share of small-bucket gold wins (2 virtual entries out of a small bucket population produces a higher deity-win probability than 2% of a larger common-color bucket). The concentration compounds across the 4 ETH + 4 BURNIE coin-jackpot trait-winner selections per day: deity owners systematically over-extract on gold-tier wins relative to their 2%-target share. The DPNERF fix shrinks the gold-tier deity allocation to a flat 1 virtual entry (eliminating the floor escalation on small gold buckets while preserving meaningful deity participation).

    `## Section (iii) — Compensation Trade-Offs` — enumerate the 3 alternatives considered at user-disposition time on 2026-05-17 + the locked outcome:
    (a) Keep total EV constant via commons-bump — boost common-tier `virtualCount` to compensate for the gold-tier reduction (e.g., commons floor changes from min-2 to min-3); rejected because it complicates the comment block, increases bytecode delta, and introduces a coupled-pair-of-tunings rather than a single targeted nerf.
    (b) Intentional reduction (LOCKED per `D-42N-DEITY-EV-01`) — gold-tier `virtualCount = 1`; commons `virtualCount = max(len/50, 2)` UNCHANGED; net result: deity weaker overall on gold-tile wins, no rebalance toward commons. Selected because it's the minimum-surface change matching the user's stated intent ("deity earns less total EV across all 8 colors") and produces the cleanest audit-story.
    (c) Intentional rebias-toward-commons — boost commons floor while nerfing gold; explicitly different from (a) because (c) deliberately shifts EV toward common-tier wins (not just constant-EV); rejected because it goes beyond the targeted gold nerf and would require its own decision-anchor + economic-model surface that wasn't requested.

    Cross-reference: this section is the SWEEP-02(iii) pre-emptive answer to "does intentional EV reduction shift incentives in a way that opens secondary attacks (e.g., deity owners pivot to non-gold gameplay strategies that destabilize commons-tier dynamics)?" — the locked disposition explicitly accepts that secondary-strategy shifts may occur and treats them as Phase 296 SWEEP discovery space (not Phase 294 in-scope mitigation).

    `## Section (iv) — Path-Coverage Trade-Offs` — enumerate the 3 alternatives considered at user-disposition time on 2026-05-17 + the locked outcome:
    (a) ETH-only — apply the gold nerf only at the ETH jackpot path (`_processDailyEth` + `_resolveTraitWinners`); rejected because it would require a callsite flag or path-discrimination parameter on `_randTraitTicket` (or duplicated `_randTraitTicket` body for the BURNIE path), increasing surface complexity and creating a differential-behavior vector (Phase 296 SWEEP would have to attest the ETH vs BURNIE asymmetry as intentional vs accidental).
    (b) Both ETH + BURNIE paths symmetric (the roadmap's literal scope) — apply the nerf to both jackpot-class paths via the function-body change. Selected as a strict subset of (c) but the literal roadmap framing.
    (c) All-4-callsite uniform (LOCKED per `D-294-CALLER-UNIFORM-01`) — the function-body change reaches all 4 `_randTraitTicket` callsites (`_runEarlyBirdLootboxJackpot` L698 + `_distributeTicketsToBucket` L988 + `_processDailyEth` L1296 + `_resolveTraitWinners` L1399) plus the BURNIE near-future coin jackpot path via `payDailyCoinJackpot` (L1767) → `_awardDailyCoinToTraitWinners` (L1816+) by construction. Selected because: (i) it matches REQUIREMENTS.md DPNERF-03 framing "deity earns less total EV across all 8 colors" — the "total EV" measure would not hold if the early-bird lootbox or ticket-distribution paths leaked un-nerfed gold EV; (ii) it matches the roadmap's explicit "no callsite flag or path-discrimination logic" wording; (iii) it produces the minimum-surface contract change (single function-body edit reaches every caller). Phase 296 SWEEP coverage extends from "ETH vs BURNIE differential-behavior" to "all-4-callsite uniformity attestation + incentive-shift across early-bird lootbox + carryover-ticket-distribution paths" per the D-294-CALLER-UNIFORM-01 SWEEP-scope expansion.

    `## Decision Anchors (Full Restatement)` — restate all 5 phase-scope anchors verbatim with their disposition lines + user-decision date 2026-05-17. Cite the CONTEXT.md `<decisions>` section as the load-bearing source.

    `## Out-of-Scope Register (NOT touched by Phase 294 per REQUIREMENTS.md lines 22-27 + CONTEXT.md `<code_context>` Out-of-Scope Source-Tree Surfaces)` — enumerate:
    (a) DPNERF common-tier compensation — explicitly excluded per D-42N-DEITY-EV-01; common-tier `virtualCount = max(len/50, 2)` UNCHANGED in the `else` branch.
    (b) Storage-layout changes — single-function `_randTraitTicket` body change; zero new storage slots; zero new mappings; zero new SSTORE callsites; zero new SLOAD callsites in DPNERF scope (the `deityBySymbol[fullSymId]` read at L1728 pre-patch is the only DPNERF-touched storage access; UNCHANGED at v42).
    (c) Public ABI changes — `_randTraitTicket` is private; all 4 caller signatures + parameter types + return shapes UNCHANGED; `payDailyCoinJackpot` (the only `external` entry reachable within 2 hops) signature UNCHANGED.
    (d) `_pickSoloQuadrant` (L1080-1130) — adjacent gold-tier code path (picks among gold-quadrants when multiple winning traits are gold); orthogonal to `_randTraitTicket`'s deity-virtual-count behavior; byte-identical post-patch.
    (e) `DegenerusGameDegeneretteModule.sol` — boon distribution flow `BP_DEITY_PASS_TIER_SHIFT` UNCHANGED per REQUIREMENTS.md line 25.
    (f) `DegenerusGameWhaleModule.sol` — deity-pass purchase pricing `DEITY_PASS_BASE` UNCHANGED per REQUIREMENTS.md line 26.
    (g) `DegenerusDeityPass.sol` — soulbound NFT minting UNCHANGED per REQUIREMENTS.md line 27.
    (h) `DegenerusGameBoonModule.sol` — boon distribution flow UNCHANGED.
    (i) `DegenerusGameStorage.sol` — zero storage layout changes per DPNERF-04.
    (j) `IDegenerusGameModules.sol` — zero public ABI changes per DPNERF-05.
    (k) `test/` tree — zero `test/` mutations at Phase 294 (TST-DPNERF-01..05 ships at Phase 295).
    (l) `KNOWN-ISSUES.md` — UNMODIFIED per CONTEXT.md `<decisions>` Claude's-Discretion disposition (mirrors D-281-KI-01 + D-291-KI-01 + D-293-STALE-VIEW-01 pattern for surface-mutation phases).
    (m) `audit/FINDINGS-v42.0.md` — does not yet exist; closure-flip happens at Phase 297 terminal phase.
    (n) `GOLD_COLOR = 7` named constant — NOT introduced per `feedback_frozen_contracts_no_future_proofing.md`; magic-7 is well-established (10+ source citations across the codebase).
    (o) `uint8 color = (trait >> 3) & 7;` local-variable cache — NOT introduced (Claude's Discretion); gold-tier check fires once per `_randTraitTicket` invocation (not in a hot loop); precedent at L1105 uses inline.
    (p) Decision-anchor citation in source comments — NONE per `D-294-NATSPEC-01` + `feedback_no_history_in_comments.md`; audit-decision IDs live in `.planning/` artifacts + `audit/FINDINGS-v42.0.md`, not in source.
    (q) NatSpec change at `_randTraitTicket` function-level docstring (L1706) — UNCHANGED; only the inline virtual-entry comment block at L1721-1723 changes per D-294-NATSPEC-01.

    For each item state "out-of-scope per [register reason]; not addressed by Plan 02".

    `## SWEEP-02(iii) DPNERF Adversarial-Hypothesis Pre-Emptive Answers` — pre-emptively address the Phase 296 SWEEP DPNERF hypotheses per `D-294-CALLER-UNIFORM-01` SWEEP-scope expansion:
    (1) All-4-callsite uniformity — does the gold nerf produce any incentive shift in the early-bird lootbox path (callsite L698) where deity-pass holders might game the lootbox window differently than the daily-jackpot window? Pre-emptive answer: the lootbox window operates at `lvl+1` (3% of `futurePrizePool`) — deity owners could in principle delay deity-pass acquisition to align with high-`futurePrizePool` levels for non-gold trait wins (where commons floor preserves their EV) while skipping levels with gold-tier dominant winning traits. This is a player-strategy shift, not an exploit; SWEEP-02(iii) expected disposition: `SAFE_BY_DESIGN` (the mechanic accepts intentional EV reduction on gold per D-42N-DEITY-EV-01).
    (2) Carryover-ticket-distribution path uniformity — does the gold contribution to deity's bucket-share move across levels through the carryover path (callsite L988 via L652)? Pre-emptive answer: carryover tickets re-enter the bucket distribution at later levels with the SAME trait IDs; the gold nerf applies uniformly at the new level's `_randTraitTicket` invocation (same function body); no per-level differentiation. SWEEP-02(iii) expected disposition: `SAFE_BY_STRUCTURAL_UNIFORMITY`.
    (3) Secondary-strategy destabilization — does the EV reduction destabilize deity-pass holder behavior in unintended ways (secondary-market deity-pass price collapse; deity owners pivot to non-gold gameplay strategies that destabilize commons-tier dynamics)? Pre-emptive answer: deity-pass economics shift toward common-color EV emphasis; commons floor `max(len/50, 2)` is UNCHANGED so commons-tier dynamics are not directly perturbed by Phase 294; any secondary-market price shift is an intended outcome of the nerf, not an exploit. SWEEP-02(iii) expected disposition: `SAFE_BY_INTENT` (intentional EV reduction per D-42N-DEITY-EV-01 is the user's stated outcome).
    (4) ETH vs BURNIE differential-behavior — does the gold nerf create any asymmetry between the ETH jackpot path and the BURNIE near-future coin jackpot path that an attacker could game? Pre-emptive answer: NO by construction — single function-body change in `_randTraitTicket` reaches both paths via the same code path; no callsite flag; no path-discrimination logic. SWEEP-02(iii) expected disposition: `SAFE_BY_CONSTRUCTION`.

    `## RNG Audit Methodology Disposition` — explicit statement: per CONTEXT.md `<canonical_refs>` audit-methodology section, `feedback_rng_backward_trace.md` and `feedback_rng_commitment_window.md` are NOT applicable to Phase 294 because DPNERF doesn't introduce a new RNG consumer (`_randTraitTicket`'s `randomWord` consumption pattern at L1743-1745 is UNCHANGED; only the `virtualCount` allocation path changes) and DPNERF doesn't change any RNG commitment-window invariant (the gold-tier branch is deterministic on `trait`, which is already committed at VRF-request time by the upstream `_rollWinningTraits` consumer). No RNG audit burden added.

    `## Plan-02 Pre-Patch Gate` — explicit statement: "Plan 02 (`294-02-PLAN.md`) cannot begin its contract-patch task until `294-01-DESIGN-INTENT-TRACE.md` + `294-01-MEASUREMENT.md` exist at the paths in Plan 01 `files_modified`. This is the design-intent-before-deletion gate per `feedback_design_intent_before_deletion.md`. Plan 02's first task reads both artifacts."

    `## Sister-Plan Coverage Map` — table of DPNERF-NN → which Phase 294 plan covers it:
    | Requirement | Plan 01 coverage | Plan 02 coverage |
    |---|---|---|
    | DPNERF-01 (gold-tier branch shape) | — | Plan 02 contract patch |
    | DPNERF-02 (path coverage all callsites) | §3 callsite enumeration in MEASUREMENT scaffold + §(iv) trace | Plan 02 by construction (single function-body change) |
    | DPNERF-03 (common-tier preserved; intentional EV reduction) | §(iii) compensation trade-offs in trace | Plan 02 by construction (else branch byte-identical to v41 logic) |
    | DPNERF-04 (storage byte-identical) | §2 attestation scaffold in MEASUREMENT | Plan 02 post-patch `forge inspect storageLayout` empty-diff |
    | DPNERF-05 (public ABI byte-identical) | §4 attestation scaffold in MEASUREMENT | Plan 02 post-patch `forge inspect methodIdentifiers` empty-diff |
    | DPNERF-06 (decision anchors recorded BEFORE patch) | This trace doc | — (gates Plan 02 by depends_on) |

    Discipline: write in prose, not bulleted-only; include line-number citations to the source files for every claim about the current code shape; the trace is the PLANNING artifact (history is allowed here — this is NOT a NatSpec comment, so `feedback_no_history_in_comments.md` does NOT apply); the trace DOES anticipate and pre-emptively answer the SWEEP-02(iii) adversarial hypotheses so Phase 296 has a baseline disposition record to test against.

    Do NOT make any contract edits in this task. Do NOT modify any file under `contracts/`. Do NOT modify any file under `test/`. Do NOT modify `KNOWN-ISSUES.md`. Do NOT modify any sibling-phase planning artifact.
  </action>
  <verify>
    <automated>test -f .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md && grep -q "D-42N-GOLD-FLOOR-01" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md && grep -q "D-42N-DEITY-EV-01" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md && grep -q "D-42N-PATH-COVERAGE-01" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md && grep -q "D-294-CALLER-UNIFORM-01" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md && grep -q "D-294-NATSPEC-01" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md && grep -q "MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md && grep -qE "Section \(i\)" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md && grep -qE "Section \(ii\)" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md && grep -qE "Section \(iii\)" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md && grep -qE "Section \(iv\)" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md && grep -q "Out-of-Scope Register" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md && grep -qE "SWEEP-02\(iii\)" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md && grep -q "Sister-Plan Coverage Map" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md && grep -q "L698" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md && grep -q "L988" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md && grep -q "L1296" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md && grep -q "L1399" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md && [ $(wc -l < .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md) -ge 90 ] && [ -z "$(git status --porcelain contracts/ test/ KNOWN-ISSUES.md 2>/dev/null)" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md` exists, ≥ 90 lines.
    - All four required DPNERF-06 trace sections present and named: `Section (i)` original `virtualCount = len / 50` rationale; `Section (ii)` gold-tile concentration issue; `Section (iii)` compensation trade-offs; `Section (iv)` path-coverage trade-offs.
    - All 5 phase-scope decision anchors recorded verbatim: `D-42N-GOLD-FLOOR-01` + `D-42N-DEITY-EV-01` + `D-42N-PATH-COVERAGE-01` + `D-294-CALLER-UNIFORM-01` + `D-294-NATSPEC-01`.
    - Audit baseline `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` cited explicitly + Phase 292 close cited as intermediate anchor.
    - Out-of-Scope Register section enumerates the 17 items (a..q) above, each with explicit "out-of-scope per [register reason]; not addressed by Plan 02" framing.
    - All 4 callsite line numbers cited explicitly in the trace: L698 + L988 + L1296 + L1399.
    - SWEEP-02(iii) DPNERF adversarial hypotheses pre-emptively answered with the 4 dispositions (uniformity / structural uniformity / intent / construction).
    - RNG Audit Methodology Disposition section explicitly states `feedback_rng_backward_trace.md` + `feedback_rng_commitment_window.md` are NOT applicable to Phase 294.
    - Sister-Plan Coverage Map table maps each DPNERF-NN to its Phase 294 plan (DPNERF-06 → Plan 01 trace; DPNERF-01..05 → Plan 02 with sub-mapping).
    - Plan-02 pre-patch-gate statement present + cites `feedback_design_intent_before_deletion.md` by name.
    - ZERO files modified outside `files_modified` paths (`git status --porcelain contracts/ test/ KNOWN-ISSUES.md` returns empty).
    - Trace describes the v41 `len / 50` + min-2 floor as a deliberate design choice (small-bucket baseline payout), NOT as a defect — correct framing — the patch is a targeted nerf, not a bug fix.
  </acceptance_criteria>
  <done>
    `294-01-DESIGN-INTENT-TRACE.md` exists with the 4-section DPNERF-06 trace, all 5 decision anchors recorded verbatim, out-of-scope register enumerated (17 items), all 4 callsite line numbers cited, SWEEP-02(iii) adversarial-hypothesis pre-emptive answers present, RNG audit-methodology disposition stated, and sister-plan coverage map populated. The artifact is the load-bearing rationale-record Plan 02 cites in its commit message body. DPNERF-06's "BEFORE the contract patch lands" gate is satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author `294-01-MEASUREMENT.md` scaffold with 6 attestation section headers + audit-baseline anchor + callsite enumeration table</name>
  <files>
    .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md
  </files>
  <read_first>
    - .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-CONTEXT.md (load-bearing; `<specifics>` callsite table + the exact branch shape + the exact comment block; `<phase_specific_guidance>` D-294-SIDECARS-01 sidecar shape)
    - .planning/REQUIREMENTS.md lines 80-87 (DPNERF-01..06; load-bearing for §2 + §4 byte-identity attestations)
    - .planning/ROADMAP.md lines 197-210 (Phase 294 success criteria 1-5)
    - contracts/modules/DegenerusGameJackpotModule.sol L1700-L1760 (the `_randTraitTicket` function body — pre-patch state; load-bearing for the §5 theoretical bytecode-delta derivation + §6 zero-new-state grep-proof framework)
    - contracts/modules/DegenerusGameJackpotModule.sol L690-L700 (callsite 1: L698 `_runEarlyBirdLootboxJackpot`)
    - contracts/modules/DegenerusGameJackpotModule.sol L980-L995 (callsite 2: L988 `_distributeTicketsToBucket`)
    - contracts/modules/DegenerusGameJackpotModule.sol L1290-L1300 (callsite 3: L1296 `_processDailyEth`)
    - contracts/modules/DegenerusGameJackpotModule.sol L1395-L1405 (callsite 4: L1399 `_resolveTraitWinners`)
    - contracts/modules/DegenerusGameJackpotModule.sol L1760-L1830 (BURNIE near-future coin jackpot path: `payDailyCoinJackpot` L1767 → `_awardDailyCoinToTraitWinners` L1816+)
    - .planning/phases/290-mint-batch-event-sig-cleanup-mintcln/290-01-MEASUREMENT.md (sister precedent — scaffold-with-placeholders shape; method-line discipline; `<FILL-IN-Plan-02>` placeholder pattern)
    - .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md (sister precedent — theoretical-first bytecode/gas attestation framework; `feedback_gas_worst_case.md` discipline applied)
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_gas_worst_case.md (theoretical-first discipline; Plan 02 deferred-empirical disposition for §5)
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md (the measurement doc IS the copy-forward source for the commit message body — keeps numerical attestations OUT of NatSpec)
  </read_first>
  <action>
    Create `294-01-MEASUREMENT.md` (markdown; agent-side planning artifact; ≥ 40 lines). The doc is a SCAFFOLD with 6 section headers + placeholder lines Plan 02 fills in after the contract patch lands (with the EXCEPTION of §3 which is FINAL at Plan 01 time because the callsite enumeration is fully known at planning time per CONTEXT.md `<specifics>`). The scaffold serves as the verbatim copy-forward source for Plan 02's batched commit message body.

    Structure:

    `# Phase 294 DPNERF — Measurement Attestations (DPNERF-04 + DPNERF-05)`

    `## §1 Audit Baseline` — anchor: `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` (v41.0 closure HEAD; source of truth for "byte-identical to v41 close" assertions per `D-42N-MILESTONE-OPEN-01`). Intermediate anchor: Phase 292 close (most-recent v42 surface phase already attesting zero storage / ABI delta vs v41 — Phase 294 inherits and re-attests on top). All deltas measured against `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4`. State: "Storage + public ABI invariants compounded across Phase 290 MINTCLN + Phase 292 HRROLL closes; Phase 294 DPNERF must preserve the chain." This section is FINAL at Plan 01 time.

    `## §2 Storage Byte-Identity Attestation (DPNERF-04)` — placeholder: `forge inspect contracts/modules/DegenerusGameJackpotModule.sol:DegenerusGameJackpotModule storageLayout diff vs MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4 baseline = <EMPTY | <recorded-non-empty-value>>. forge inspect contracts/storage/DegenerusGameStorage.sol:DegenerusGameStorage storageLayout diff vs MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4 baseline = <EMPTY | <recorded-non-empty-value>>. <FILL-IN-Plan-02>`. Note: the DPNERF patch touches ONLY the `_randTraitTicket` function body at L1707-L1757; the only DPNERF-touched storage access is the existing `deityBySymbol[fullSymId]` SLOAD at L1728 (UNCHANGED in count, slot, type by the patch). Zero new storage slots; zero new mappings; zero new SSTORE callsites; zero new SLOAD callsites in DPNERF scope (per DPNERF-04). If diff is non-empty → escalate to user before Plan 02 commit. Method: `forge inspect <file>:<contract> storageLayout` against both the post-patch tree and the v41 baseline worktree (created via `git worktree add /tmp/v41-baseline 315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4`); compute `diff /tmp/v41-storage-294.json /tmp/v42-storage-294.json`.

    `## §3 Callsite Enumeration (D-294-CALLER-UNIFORM-01)` — FINAL at Plan 01 time. Verbatim from CONTEXT.md `<specifics>`. Table:

    | # | Pre-patch line | Function | Path | Top-Level Entry |
    |---|---|---|---|---|
    | 1 | L698 | `_runEarlyBirdLootboxJackpot` | Early-bird lootbox jackpot trait winners (3% of `futurePrizePool` distributed at `lvl+1`; 100 winners across 4 traits = 25/trait) | Daily jackpot cycle (purchase-phase tickets) |
    | 2 | L988 | `_distributeTicketsToBucket` (helper) | Trait-bucket ticket distribution | `_distributeTicketJackpot` via L637 daily-tickets / L652 carryover-tickets / L883 early-bird-post-purchase-tickets |
    | 3 | L1296 | `_processDailyEth` | Daily ETH jackpot trait winners | `_runJackpotEthFlow` (L1142) → `_processDailyEth` (L1232) → `_randTraitTicket` (L1296) |
    | 4 | L1399 | `_resolveTraitWinners` | ETH trait-winner resolution sub-flow | Called from `_processDailyEth` ticket-payout sub-path |

    BURNIE near-future coin jackpot path resolution (named in the roadmap as the "BURNIE coin jackpot path"): `payDailyCoinJackpot` (L1767, `external`) → `_awardDailyCoinToTraitWinners` (L1816+) → trait-bucket sampling → ultimately `_randTraitTicket` (resolves through callsite 2 or 3 depending on the BURNIE distribution sub-shape; same function-body change applies by construction).

    Coverage discipline: the function-body change at `_randTraitTicket` reaches ALL 4 callsites + the BURNIE path uniformly with no callsite flag and no path-discrimination logic per `D-294-CALLER-UNIFORM-01`. Phase 296 SWEEP DPNERF hypothesis surface MUST cover all 4 callsites per `D-294-CALLER-UNIFORM-01` SWEEP-scope expansion. Phase 297 §3.A delta-surface table cites all 4 callsites by line number under the DPNERF row.

    `## §4 Public ABI Byte-Identity Attestation (DPNERF-05)` — placeholder: `forge inspect contracts/modules/DegenerusGameJackpotModule.sol:DegenerusGameJackpotModule methodIdentifiers diff vs MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4 baseline = <EMPTY | <recorded-non-empty-value>>. <FILL-IN-Plan-02>`. Coverage: `_randTraitTicket` is `private` (not in public ABI); the 4 direct callers (`_runEarlyBirdLootboxJackpot` + `_distributeTicketsToBucket` + `_processDailyEth` + `_resolveTraitWinners`) are also private/internal helpers; the public entry points reachable through this chain are `payDailyCoinJackpot` (the only `external` entry within 2 hops at L1767) plus the entries reachable through `_runJackpotEthFlow` + `_distributeTicketJackpot` + `_runEarlyBirdLootboxJackpot` chains. ALL UNCHANGED at v42 close vs v41 close. Zero new public/external mutation entry points; zero new admin entry points; zero new modifiers; zero new upgrade hooks in DPNERF scope (per DPNERF-05). If diff is non-empty → escalate to user before Plan 02 commit. Method: `forge inspect contracts/modules/DegenerusGameJackpotModule.sol:DegenerusGameJackpotModule methodIdentifiers` against post-patch tree + v41 baseline worktree; diff the two JSON outputs.

    `## §5 Theoretical Bytecode-Delta Estimate (per feedback_gas_worst_case.md)` — placeholder + analytical framework Plan 02 fills in (the FRAMEWORK is FINAL at Plan 01 time; only the post-patch byte count fills at Plan 02). Framework:

    Pre-patch shape (current L1729-L1731 inside the `if (deity != address(0))` block):
    ```
    virtualCount = len / 50;
    if (virtualCount < 2) virtualCount = 2;
    ```
    Bytecode for the pre-patch shape: one `DIV` opcode + one comparison + one `JUMPI` + one constant push for `2` + one `MSTORE`. Estimated ~10-15 bytes of runtime bytecode.

    Post-patch shape (per CONTEXT.md `<specifics>` locked branch — copied verbatim into Plan 02):
    ```
    if (((trait >> 3) & 7) == 7) {
        virtualCount = 1;
    } else {
        virtualCount = len / 50;
        if (virtualCount < 2) virtualCount = 2;
    }
    ```
    Bytecode delta for the post-patch shape: one ADDITIONAL `SHR(3, trait)` + one `AND(7)` + one `EQ(7)` + one `JUMPI` (branch on gold) + one constant push for `1` + one `MSTORE` (the gold-tier `virtualCount = 1` assignment). The else branch retains the original 10-15 bytes verbatim. Net analytical delta: ~+10-30 bytes of runtime bytecode (one comparison + one conditional store + small constant pool entries; the existing v41 logic is preserved verbatim in the `else` branch — no overlapping bytecode removed).

    Empirical disposition: NO empirical second-pass needed at Phase 294 per `feedback_gas_worst_case.md` theoretical-first methodology + TST-DPNERF-01..05 scope (TST-DPNERF-04 is an EV regression at N=1000, not a gas regression). DPNERF runtime gas cost is negligible — single comparison + conditional store; well below any practical measurement noise floor. Decision: theoretical-only attestation at Phase 294; no Phase 295 gas-regression test.

    Plan 02 fills the actual post-patch bytecode delta number via `forge build` (or equivalent) twice — once at v42 HEAD post-patch, once at MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4 baseline via git worktree — size-compare deployed bytecode strings. Expected value bounded by the +10-30 byte analytical estimate above. `<FILL-IN-Plan-02 — actual byte delta>`.

    `## §6 Zero-New-State Grep-Proof (DPNERF-04 strengthening)` — placeholder: `Post-patch grep of contracts/modules/DegenerusGameJackpotModule.sol _randTraitTicket body (lines L1707-L17xx — post-patch line range to be recorded by Plan 02) for SSTORE-equivalent statements (assignments to mapping / storage-array elements) shows ZERO new entries vs v41 close. Post-patch grep for SLOAD-equivalent statements (mapping / storage-array reads) shows ZERO new entries vs v41 close. The only DPNERF-touched storage access remains the existing deityBySymbol[fullSymId] SLOAD at pre-patch L1728 (UNCHANGED in count, slot, type by the patch). <FILL-IN-Plan-02 — actual grep evidence>`. Method: extract the `_randTraitTicket` function body from both the post-patch tree and the v41 baseline worktree; diff structurally; confirm zero new lines touching storage. Strengthens the DPNERF-04 storage byte-identity attestation by attesting at the function-body level (not just at the storage layout level).

    Footer: `## Source-Doc Cross-Cite` — link back to `294-01-DESIGN-INTENT-TRACE.md` and forward to `294-02-PLAN.md`. Note: "Plan 02 fills the `<FILL-IN-Plan-02>` placeholders in §2 + §4 + §5 + §6 post-patch. §1 (audit baseline) + §3 (callsite enumeration) are FINAL at Plan 01 time. The populated values are the verbatim copy-forward source for the batched commit message body in Plan 02's USER-APPROVED commit task. The doc MUST be re-validated against the post-patch tree before the user approves the commit."

    Do NOT make any contract edits in this task. Do NOT modify any file under `contracts/`. Do NOT modify any file under `test/`. Do NOT modify `KNOWN-ISSUES.md`. Do NOT modify any sibling-phase planning artifact.
  </action>
  <verify>
    <automated>test -f .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -q "MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -qE "§1 Audit Baseline|## §1" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -qE "Storage Byte-Identity|§2" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -qE "Callsite Enumeration|§3" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -qE "Public ABI Byte-Identity|§4" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -qE "Theoretical Bytecode-Delta|§5" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -qE "Zero-New-State Grep-Proof|§6" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -q "L698" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -q "L988" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -q "L1296" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -q "L1399" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -q "_runEarlyBirdLootboxJackpot" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -q "_distributeTicketsToBucket" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -q "_processDailyEth" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -q "_resolveTraitWinners" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -q "payDailyCoinJackpot" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -q "_awardDailyCoinToTraitWinners" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -q "FILL-IN-Plan-02" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && grep -q "D-294-CALLER-UNIFORM-01" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md && [ $(wc -l < .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md) -ge 40 ] && [ -z "$(git status --porcelain contracts/ test/ KNOWN-ISSUES.md 2>/dev/null)" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md` exists, ≥ 40 lines.
    - All 6 attestation section headers present: §1 Audit Baseline / §2 Storage Byte-Identity Attestation / §3 Callsite Enumeration / §4 Public ABI Byte-Identity Attestation / §5 Theoretical Bytecode-Delta Estimate / §6 Zero-New-State Grep-Proof.
    - Audit baseline `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` cited in §1.
    - §3 callsite enumeration table FINAL at Plan 01 time — contains all 4 callsites with their pre-patch line numbers (L698, L988, L1296, L1399) + function names (`_runEarlyBirdLootboxJackpot`, `_distributeTicketsToBucket`, `_processDailyEth`, `_resolveTraitWinners`) + the BURNIE path resolution via `payDailyCoinJackpot` + `_awardDailyCoinToTraitWinners`.
    - §3 cites `D-294-CALLER-UNIFORM-01` as the controlling anchor.
    - §5 theoretical bytecode-delta estimate framework present — includes pre-patch shape + post-patch shape + ~+10-30 byte analytical estimate + the explicit "no empirical second-pass needed at Phase 294" disposition citing `feedback_gas_worst_case.md` + TST-DPNERF scope.
    - `<FILL-IN-Plan-02>` placeholders present in §2 + §4 + §5 + §6 (Plan 02 populates them post-patch).
    - §3 contains NO `<FILL-IN-Plan-02>` placeholder (callsite enumeration is FINAL at Plan 01 time).
    - §1 contains NO `<FILL-IN-Plan-02>` placeholder (audit baseline is FINAL at Plan 01 time).
    - Source-Doc Cross-Cite footer present linking back to `294-01-DESIGN-INTENT-TRACE.md` and forward to `294-02-PLAN.md`.
    - ZERO files modified outside `files_modified` paths (`git status --porcelain contracts/ test/ KNOWN-ISSUES.md` returns empty).
  </acceptance_criteria>
  <done>
    `294-01-MEASUREMENT.md` exists as the 6-section scaffold. §1 (audit baseline) + §3 (callsite enumeration) are FINAL at Plan 01 time. §2 + §4 + §5 + §6 carry `<FILL-IN-Plan-02>` placeholders Plan 02 fills in post-patch. The scaffold defines exactly what attestations Plan 02 must produce + how to derive them, anchored to `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4`. The DPNERF-04 + DPNERF-05 byte-identity attestation framework is now in place; only the post-patch evidence remains to fill. The DPNERF-02 callsite enumeration is fully recorded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planner output → Plan 02 executor | Trace + scaffold are the load-bearing inputs Plan 02 cites; corruption / omission of an anchor or section would let Plan 02 land the contract patch without satisfying DPNERF-06's "BEFORE the patch" gate. Mitigated by Task 1 + Task 2 acceptance criteria + grep gates on the trace anchors + scaffold headers. |
| Trace doc → audit/FINDINGS-v42.0.md §3.A (Phase 297) + §3.C conservation re-proof | The 5 phase-scope decision anchors + the 4-callsite enumeration + the SWEEP-02(iii) pre-emptive answers + the out-of-scope register all copy-forward into Phase 297's terminal audit deliverable. Mitigated by explicit anchor recording in Task 1 + the Sister-Plan Coverage Map. |
| Measurement scaffold → Plan 02 batched commit message body | The scaffold IS the verbatim copy-forward source for the commit body (per `feedback_no_history_in_comments.md` — numbers + attestations go in commit body, not NatSpec). Corruption of the scaffold framework would force Plan 02 to invent its own attestation structure ad-hoc. Mitigated by Task 2 acceptance criteria + the 6 fixed section headers + the FINAL §1 + §3 content. |
| Trace doc § SWEEP-02(iii) pre-emptive answers → Phase 296 SWEEP PLAN.md | Phase 296 SWEEP DPNERF hypothesis surface MUST cover all 4 callsites per `D-294-CALLER-UNIFORM-01`. The trace pre-emptively records expected dispositions (SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_UNIFORMITY / SAFE_BY_INTENT / SAFE_BY_CONSTRUCTION) so Phase 296 has a baseline to test against. Mitigated by Task 1 SWEEP-02(iii) acceptance criterion. |

## STRIDE Threat Register — Phase 294 Plan 01 (Planning-Artifact Scope)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-294-01-01 | Tampering | `294-01-DESIGN-INTENT-TRACE.md` anchor recording | mitigate | Task 1 acceptance criteria enforce explicit grep-gates on all 5 phase-scope anchors (`D-42N-GOLD-FLOOR-01` + `D-42N-DEITY-EV-01` + `D-42N-PATH-COVERAGE-01` + `D-294-CALLER-UNIFORM-01` + `D-294-NATSPEC-01`); missing anchor fails the automated verify and blocks plan completion. |
| T-294-01-02 | Repudiation | Out-of-scope register completeness | mitigate | Task 1 acceptance criterion requires enumeration of the 17 out-of-scope items (a..q) from REQUIREMENTS.md lines 22-27 + CONTEXT.md `<code_context>` Out-of-Scope Source-Tree Surfaces; trace explicitly states each item is "out-of-scope per [register reason]; not addressed by Plan 02" so a future audit cannot claim the planner silently omitted a topic. |
| T-294-01-03 | Information Disclosure | Pre-emptive answers to SWEEP-02(iii) adversarial hypotheses | mitigate | Task 1 trace pre-emptively answers the 4 SWEEP-02(iii) DPNERF adversarial questions (all-4-callsite uniformity / carryover-ticket-distribution uniformity / secondary-strategy destabilization / ETH vs BURNIE differential-behavior) so Phase 296 has a baseline disposition record to test against. Acceptance criterion enforces this content. |
| T-294-01-04 | Tampering | `294-01-MEASUREMENT.md` scaffold framework | mitigate | Task 2 acceptance criteria enforce all 6 attestation section headers + audit-baseline anchor + callsite enumeration table (FINAL at Plan 01 time) + theoretical bytecode-delta framework + `<FILL-IN-Plan-02>` placeholders; Plan 02 cannot improvise an alternative measurement structure. |
| T-294-01-05 | Spoofing | Callsite enumeration completeness | mitigate | Task 2 §3 records all 4 callsites verbatim from CONTEXT.md `<specifics>` (L698 / L988 / L1296 / L1399) + the BURNIE path resolution; the FINAL-at-Plan-01 disposition means Plan 02 inherits the enumeration unchanged. If Plan 02 attempts to ship a contract patch that misses any callsite, the §3 enumeration + the by-construction function-body change pattern catch it (the patch lands in `_randTraitTicket` which is the single shared callee). |
| T-294-01-06 | Denial of Service | Frozen-contract discipline drift | accept | Task 1 + Task 2 acceptance criteria each include `git status --porcelain contracts/ test/ KNOWN-ISSUES.md` zero-modification gates. Plan 01 is incapable of touching contract source by design — its mutation surface is bounded to two paths under `.planning/phases/294-*/`. |
| T-294-01-SC | Tampering | Package install (npm/pip/cargo) | accept (rationale: not applicable) | Phase 294 Plan 01 makes ZERO package installs; this is a planning-artifact-authoring plan. The package-legitimacy gate does not fire. |

(Severity bound for this plan: `low`. No `high` severity threat is in scope — Plan 01 cannot mutate contract bytecode, cannot affect on-chain state, cannot break any selector or storage layout on its own. The actual gold-tier nerf mutation lands in Plan 02 under `autonomous: false` + user-approval gate. The DPNERF patch itself is a contract-cleanup with zero new state surface — no new attack vectors introduced; the threat surface is the gold-tier branch correctness + the byte-identity attestation correctness, both bounded by Plan 02's task structure.)
</threat_model>

<verification>
After Task 1 + Task 2 complete:

1. `test -f .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md` returns 0.
2. `test -f .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md` returns 0.
3. `grep -cE "D-42N-GOLD-FLOOR-01|D-42N-DEITY-EV-01|D-42N-PATH-COVERAGE-01|D-294-CALLER-UNIFORM-01|D-294-NATSPEC-01" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md` returns ≥ 5 (all 5 phase-scope anchors present, possibly multiple times each).
4. `grep -cE "Section \(i\)|Section \(ii\)|Section \(iii\)|Section \(iv\)" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-DESIGN-INTENT-TRACE.md` returns ≥ 4 (four DPNERF-06 trace sections present).
5. `grep -cE "§1|§2|§3|§4|§5|§6" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md` returns ≥ 6 (all six attestation headers present).
6. `grep -cE "L698|L988|L1296|L1399" .planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-MEASUREMENT.md` returns ≥ 4 (all 4 callsite line numbers cited in §3).
7. `git diff --name-only HEAD -- contracts/ test/ KNOWN-ISSUES.md` is EMPTY (Plan 01 made zero contract / test / KI edits).
8. `git status --porcelain .planning/phases/294-deity-pass-gold-nerf-dpnerf/` shows exactly the two new files (`294-01-DESIGN-INTENT-TRACE.md` + `294-01-MEASUREMENT.md`) as untracked or modified.
9. Both files committed AGENT-COMMITTED (no user approval needed — zero contract edits per `feedback_no_contract_commits.md`).

If verification fails: re-author the failing artifact section before declaring the plan complete.
</verification>

<success_criteria>
- `294-01-DESIGN-INTENT-TRACE.md` lands with the 4-section DPNERF-06 trace, all 5 phase-scope decision anchors recorded verbatim, out-of-scope register enumerated (17 items), all 4 callsite line numbers cited, SWEEP-02(iii) DPNERF adversarial-hypothesis pre-emptive answers present, RNG audit-methodology disposition stated, sister-plan coverage map populated, and Plan-02 pre-patch-gate statement (per DPNERF-06).
- `294-01-MEASUREMENT.md` lands with the 6-section scaffold. §1 (audit baseline) + §3 (callsite enumeration — all 4 callsites + BURNIE path) are FINAL at Plan 01 time. §2 (storage byte-identity per DPNERF-04) + §4 (public ABI byte-identity per DPNERF-05) + §5 (theoretical bytecode-delta estimate per `feedback_gas_worst_case.md`) + §6 (zero-new-state grep-proof) carry `<FILL-IN-Plan-02>` placeholders Plan 02 populates post-patch.
- ZERO files touched under `contracts/` or `test/` or `KNOWN-ISSUES.md` (Plan 01 is planning-artifact-authoring only).
- The "BEFORE the contract patch lands" gate per `feedback_design_intent_before_deletion.md` is satisfied — Plan 02 cannot begin its contract-edit task until both Plan 01 artifacts exist.
- Both artifacts are AGENT-COMMITTED (no user approval needed; no contract or test edits made by this plan).
</success_criteria>

<output>
Create `.planning/phases/294-deity-pass-gold-nerf-dpnerf/294-01-SUMMARY.md` when this plan completes. Record: (a) trace doc anchor coverage (all 5 phase-scope anchors present); (b) scaffold doc section coverage (all 6 attestation headers present); (c) line counts for both files; (d) confirmation that `git diff --name-only HEAD -- contracts/ test/ KNOWN-ISSUES.md` is EMPTY post-plan; (e) explicit hand-off statement: "Plan 02 may now begin its contract-patch task per the design-intent-before-deletion gate per `feedback_design_intent_before_deletion.md`."
</output>
</content>
</invoke>