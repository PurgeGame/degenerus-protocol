---
phase: 181-advancegame-revert-safety-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/181-advancegame-revert-safety-audit/181-02-DELEGATECALL-TARGETS.md
autonomous: true
requirements: [AGSAFE-02]

must_haves:
  truths:
    - "Every revert in JackpotModule functions reachable via advanceGame delegatecall is proven unreachable during normal game progression or intentionally guarded"
    - "Every revert in MintModule.processFutureTicketBatch (the only MintModule function called from advanceGame) is proven unreachable during normal game progression"
    - "Every revert in GameOverModule.handleGameOverDrain and handleFinalSweep is proven unreachable during normal game progression or intentionally guarded"
  artifacts:
    - path: ".planning/phases/181-advancegame-revert-safety-audit/181-02-DELEGATECALL-TARGETS.md"
      provides: "Complete delegatecall target revert audit"
      contains: "AGSAFE-02"
  key_links:
    - from: "AdvanceModule.advanceGame"
      to: "JackpotModule.payDailyJackpot"
      via: "delegatecall"
      pattern: "payDailyJackpot"
    - from: "AdvanceModule.advanceGame"
      to: "JackpotModule.processTicketBatch"
      via: "delegatecall"
      pattern: "processTicketBatch"
    - from: "AdvanceModule._handleGameOverPath"
      to: "GameOverModule.handleGameOverDrain"
      via: "delegatecall"
      pattern: "handleGameOverDrain"
---

<objective>
Audit every revert path inside delegatecall targets reachable from advanceGame -- JackpotModule, MintModule, and GameOverModule entry points.

Purpose: Prove that no revert inside a delegatecall target can fire during normal game progression when called from advanceGame's internal logic.
Output: 181-02-DELEGATECALL-TARGETS.md with per-function, per-revert verdicts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all JackpotModule entry points reachable from advanceGame</name>
  <read_first>
    - contracts/modules/DegenerusGameJackpotModule.sol (FULL -- 2811 lines; the file under audit)
    - contracts/modules/DegenerusGameAdvanceModule.sol lines 589-701 (delegatecall wrappers: _runRewardJackpots, _consolidatePrizePools, _awardFinalDayDgnrsReward, payDailyJackpot, payDailyJackpotCoinAndTickets, _payDailyCoinJackpot)
    - contracts/modules/DegenerusGameAdvanceModule.sol lines 1268-1289 (_runProcessTicketBatch -- delegatecalls processTicketBatch)
  </read_first>
  <files>.planning/phases/181-advancegame-revert-safety-audit/181-02-DELEGATECALL-TARGETS.md</files>
  <action>
Read the full JackpotModule.sol. Identify EVERY entry point called from advanceGame via delegatecall:

1. `payDailyJackpot(bool isDaily, uint24 lvl, uint256 randWord)` -- line 305
2. `payDailyJackpotCoinAndTickets(uint256 randWord)` -- line 530
3. `payDailyCoinJackpot(uint24 lvl, uint256 randWord)` -- line 2164
4. `consolidatePrizePools(uint24 lvl, uint256 rngWord)` -- line 730
5. `awardFinalDayDgnrsReward(uint24 lvl, uint256 rngWord)` -- line 620
6. `runRewardJackpots(uint24 lvl, uint256 rngWord)` -- line 2516
7. `processTicketBatch(uint24 lvl)` -- line 1693

For EACH entry point:
- Trace every revert/require in the function and all internal functions it calls
- The ONLY revert in JackpotModule's external-facing entry points found by grep is line 255: `if (msg.sender != ContractAddresses.GAME) revert OnlyGame()` -- this fires when called externally but NOT via delegatecall (msg.sender is the original tx sender, but delegatecall preserves context). IMPORTANT: Verify this -- delegatecall preserves msg.sender from the OUTER call, which is the EOA calling advanceGame, NOT ContractAddresses.GAME. However, this guard is on `runTerminalJackpot` (line 250-255) which is called via `IDegenerusGame(address(this))` (a regular call, not delegatecall) from GameOverModule. Verify whether any of the 7 advanceGame-reachable entry points have this guard.
- Check for arithmetic overflow/underflow (Solidity 0.8.34 has checked arithmetic by default; unchecked blocks need manual review)
- Check for division by zero in jackpot share calculations
- Check for array out-of-bounds access in ticket processing

Then read MintModule.sol and audit:
8. `processFutureTicketBatch(uint24 lvl)` -- the ONLY MintModule function called from advanceGame

Then read GameOverModule.sol (245 lines) and audit:
9. `handleGameOverDrain(uint48 day)` -- called from _handleGameOverPath
10. `handleFinalSweep()` -- called from _handleGameOverPath post-gameover

For GameOverModule, the key reverts are:
- Line 232: `if (!steth.transfer(to, amount)) revert E()` -- stETH transfer failure
- Line 236: `if (!steth.transfer(to, stethBal)) revert E()` -- stETH transfer failure
- Line 241: `if (!ok) revert E()` -- ETH transfer failure
- These are in _sendToVault which is called from handleGameOverDrain and handleFinalSweep

For each revert found, classify as:
- **UNREACHABLE**: Cannot fire when called from advanceGame path -- state the invariant
- **INTENTIONAL**: Designed to halt game progress until external condition resolves (e.g., stETH transfer failure blocks gameover until Lido unfreezes)
- **FINDING**: Can fire unexpectedly during normal game progression

**Format:**
```
## JackpotModule Entry Points

### DCALL-01: payDailyJackpot
- Reverts found: [list each revert with line number]
- Per-revert analysis: [for each]
- Verdict: SAFE / FINDING

### DCALL-02: payDailyJackpotCoinAndTickets
...
```

End with:
```
## Overall Verdict
- AGSAFE-02: [VERIFIED / FINDINGS] -- N entry points audited, N reverts traced, N safe, N findings
```
  </action>
  <verify>
    <automated>grep -c "DCALL-" .planning/phases/181-advancegame-revert-safety-audit/181-02-DELEGATECALL-TARGETS.md | xargs test 7 -le</automated>
  </verify>
  <acceptance_criteria>
    - 181-02-DELEGATECALL-TARGETS.md exists and contains "AGSAFE-02" verdict
    - All 7 JackpotModule entry points (payDailyJackpot, payDailyJackpotCoinAndTickets, payDailyCoinJackpot, consolidatePrizePools, awardFinalDayDgnrsReward, runRewardJackpots, processTicketBatch) have verdicts
    - MintModule processFutureTicketBatch has a verdict
    - GameOverModule handleGameOverDrain and handleFinalSweep have verdicts
    - Each function's verdict includes specific revert line numbers from the source
    - Document ends with "Overall Verdict" or "AGSAFE-02" verdict line
  </acceptance_criteria>
  <done>All 10 delegatecall entry points audited. Every revert in each function classified. AGSAFE-02 has a verdict.</done>
</task>

</tasks>

<verification>
- 181-02-DELEGATECALL-TARGETS.md covers all 10 delegatecall entry points reachable from advanceGame
- JackpotModule gets the most scrutiny (7 entry points, 2811 lines of code)
- GameOverModule stETH transfer reverts are explicitly addressed
- MintModule processFutureTicketBatch revert paths are traced
</verification>

<success_criteria>
AGSAFE-02 has a verdict (VERIFIED or FINDINGS with details). All 10 entry points have per-revert analysis. No revert classified without a stated reason.
</success_criteria>

<output>
After completion, create `.planning/phases/181-advancegame-revert-safety-audit/181-02-SUMMARY.md`
</output>
