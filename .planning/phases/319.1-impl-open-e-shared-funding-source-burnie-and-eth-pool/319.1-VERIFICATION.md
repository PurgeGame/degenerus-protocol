---
phase: 319.1-impl-open-e-shared-funding-source-burnie-and-eth-pool
verified: 2026-05-24T00:00:00Z
status: human_needed
score: 13/13 must-haves verified
overrides_applied: 0
re_verification: false
human_verification:
  - test: "Confirm WR-01 observability gap is an accepted design decision"
    expected: "The SubscriptionUpdated event intentionally omits fundingSource; monitoring a source's standing bindings requires off-chain polling of subscriptionOf(). Phase 320 TERMINAL adversarial sweep is explicitly charged to attack fundingSource redirect — if WR-01 is judged acceptable, no action needed."
    why_human: "WR-01 is a robustness/auditability warning from the code review, not an exploit. The plan explicitly requires NO FundingSourceUpdated event (deliberate no-new-event design). Whether this observability gap is acceptable for real-money deployment is a product decision, not a code verification."
---

# Phase 319.1: OPEN-E Shared Funding Source Verification Report

**Phase Goal:** Promote OPEN-E (shared funding source for multi-wallet players) into v46.0 — one funding wallet covers BOTH the BURNIE subscription charge AND the ETH `_poolOf` auto-buy draw for a player's multiple subscriber addresses, via a `fundingSource` param on AfKing.subscribe() (no standalone setter), operator-approval-gated at subscribe() only, rippled to the Vault/sDGNRS SUB-09 callers. Single batched USER-APPROVED 3-contract diff (committed 42140ceb), with the test mirror re-synced.
**Verified:** 2026-05-24
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | One funded wallet S covers BOTH the BURNIE subscription charge AND the ETH `_poolOf` auto-buy draw for a subscriber M (OPENE-01/02/03 net effect) | ✓ VERIFIED | AfKing.sol:693 resolves `src` once per iteration; :724 reads `_poolOf[src]`, :746 debits `_poolOf[src]`; :434-435 burns from `s.fundingSource == 0 ? subscriber : s.fundingSource`; :630-631 burns from `sub.fundingSource == 0 ? player : sub.fundingSource`. 3 behavioral tests prove the cross-account ETH+BURNIE routing including window-1 burn. |
| 2 | Sub carries `address fundingSource` (default address(0) = self), set ONLY via new subscribe() param — NO standalone setFundingSource setter, NO FundingSourceUpdated event; re-subscribe to change | ✓ VERIFIED | AfKing.sol:85 has `address fundingSource` in Sub struct. `grep setFundingSource contracts/AfKing.sol` = 0 hits. `grep FundingSourceUpdated contracts/AfKing.sol` = 0 hits. SubscriptionUpdated event (:157-163) has 5 params, no fundingSource. |
| 3 | subscribe() signature ripples to DegenerusVault and StakedDegenerusStonk — IAfKingSubscribe interface decl gains 6th `address fundingSource` param; call sites pass `address(0)` (self) | ✓ VERIFIED | DegenerusVault.sol:81-88 IAfKingSubscribe.subscribe has 6 params incl. `address fundingSource`. DegenerusVault.sol:474 passes `address(0)`. StakedDegenerusStonk.sol:59-67 same interface. :380 passes `address(0)`. |
| 4 | Two standalone bools collapse into flags byte; Sub remains ONE 32-byte slot; slot COUNT (5 slots, 0-4) unchanged (OPENE-01) | ✓ VERIFIED | `forge inspect AfKing storage-layout` shows exactly slots 0-4 (`_poolOf`, `_subOf`, `_subscribers`, `_subscriberIndex`, `_sweepDay`+`_sweepCursor`). Sub struct declaration (:79-86) contains no standalone bool fields. FLAG_DRAIN_FIRST=2, FLAG_USE_TICKETS=4 at :240/:244. |
| 5 | subscribe() writes `s.fundingSource = fundingSource` BEFORE the SUB-01 pass-or-pay gate — S funds window 1 (OPENE-03) | ✓ VERIFIED | AfKing.sol:422 `s.fundingSource = fundingSource` in state-writes block. :434 `burnForKeeper(s.fundingSource == address(0) ? subscriber : s.fundingSource, cost)`. Write at :422 precedes interaction at :434. |
| 6 | ETH draw reads (:724) and debits (:746) `_poolOf[src]` (same once-per-iteration resolved source); gas-neutral single-key SLOAD (OPENE-02) | ✓ VERIFIED | AfKing.sol:693 `address src = sub.fundingSource == address(0) ? player : sub.fundingSource;` :724 `if (_poolOf[src] < msgValue)` :746 `_poolOf[src] -= msgValue`. Both keyed on `src`. |
| 7 | Both burnForKeeper sites — subscribe :434 and sweep day-31 :630 — pass the resolved fundingSource; all-or-nothing shortfall paths preserved (OPENE-03) | ✓ VERIFIED | :434-435 `burnForKeeper(s.fundingSource == address(0) ? subscriber : s.fundingSource, cost)`. :630-631 `burnForKeeper(sub.fundingSource == address(0) ? player : sub.fundingSource, extractCost)`. No approval re-check at :630. |
| 8 | A non-zero non-self fundingSource honored only when isOperatorApproved(fundingSource, subscriber) is true, checked at subscribe() ONLY — never at day-31 renewal, never per-draw; fundingSource==0 short-circuits; unapproved reverts NotApproved (OPENE-04) | ✓ VERIFIED | AfKing.sol:393-398: gate guarded by `fundingSource != address(0) && fundingSource != subscriber && !isOperatorApproved(fundingSource, subscriber)`. Exactly ONE new call site — not in renewal branch (:620-651) and not in per-draw loop body. testUnapprovedFundingSourceRefusedThenHonored proves the revert/honor behavior. |
| 9 | Revoke-does-not-stop-active-sub (by design): a post-subscribe setOperatorApproval(M,false) does NOT stop the active sub; the day-31 renewal keeps drawing/burning the stored source; S stops by defunding, M by cancelling (OPENE-04) | ✓ VERIFIED | NatSpec at :362-370 documents this. testRevokeDoesNotStopActiveSubButDefundDoes passes: renewal still charges S after revoke; auto-pause only occurs on defund. |
| 10 | LANDMINE A: InsufficientPool exemption at :725 compares the iterated un-spoofable `player` to VAULT/SDGNRS — NEVER the resolved src | ✓ VERIFIED | AfKing.sol:725 `if (player == ContractAddresses.VAULT || player == ContractAddresses.SDGNRS)`. Keyed on `player`, not `src`. testFundingSourceVaultDoesNotInheritExemption proves a NORMAL sub with fundingSource=VAULT is still cancelled. |
| 11 | LANDMINE B: existing per-draw isOperatorApproved(player, address(this)) at :656 is byte-unchanged; the new source-subscriber check is NOT in the per-draw loop | ✓ VERIFIED | AfKing.sol:656 `if (!IGame(ContractAddresses.GAME).isOperatorApproved(player, address(this)))` — unchanged. Only 2 total isOperatorApproved call sites beyond the interface decl and NatSpec: :385 (SUB-02 self-or-operator auth) and :396 (new OPENE-04 gate) — the :656 per-draw check is the third. No extra approval reads in the renewal branch (:620-651). |
| 12 | Default-self byte-equivalence: when fundingSource==0 every routing point resolves to the same identity the pre-OPEN-E code used (subscriber at :434, player at :630/:724/:746) | ✓ VERIFIED | All three routing points use ternary with `== address(0) ? <original identity> : fundingSource`. testFundingSourceDefaultSelfIsByteEquivalent passes. |
| 13 | Full AfKing-touching suite recompiles and passes green; re-derived Sub packed-byte offsets match the live layout; no stale subscribe call sites; no stale drainGameCreditFirst/useTickets typed accessors | ✓ VERIFIED | Suites: AfKingFundingWaterfall 14/14, AfKingSubscription 9/9, AfKingConcurrency 10/10, CrankNonBrick 12/12, RngFreezeAndRemovalProofs 13/13, SweepPerPlayerWorstCaseGas 3/3. Total 61/61. `grep setFundingSource|FundingSourceUpdated test/` = 0 hits. `grep \.drainGameCreditFirst\|\.useTickets test/` = 0 hits. |

**Score:** 13/13 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `contracts/AfKing.sol` | Sub.fundingSource field + subscribe() fundingSource param + subscribe-only auth + 3 routing points | ✓ VERIFIED | Struct :79-86 has `address fundingSource`. Subscribe :371-441 carries 6th param, writes at :422, gates at :393-398, routes at :434-435. Sweep routes at :631, :693, :724, :746. |
| `contracts/AfKing.sol` | FLAG_DRAIN_FIRST and FLAG_USE_TICKETS constants alongside FLAG_WINDOW_PAID | ✓ VERIFIED | :236 FLAG_WINDOW_PAID=1, :240 FLAG_DRAIN_FIRST=2, :244 FLAG_USE_TICKETS=4 |
| `contracts/DegenerusVault.sol` | IAfKingSubscribe interface decl + call site passing address(0) | ✓ VERIFIED | :81-88 interface with 6 params. :474 `afKing.subscribe(address(this), true, false, 1, 0, address(0))` |
| `contracts/StakedDegenerusStonk.sol` | IAfKingSubscribe interface decl + call site passing address(0) | ✓ VERIFIED | :59-67 interface with 6 params. :380 `afKing.subscribe(address(this), true, false, 1, 2, address(0))` |
| `test/fuzz/AfKingFundingWaterfall.t.sol` | Re-derived offsets + 6-arg subscribe + 5 OPEN-E tests + exemption-spoof | ✓ VERIFIED | OFF_LASTSWEPT=1, OFF_PAIDTHROUGH=5, OFF_FLAGS=10, OFF_FUNDING_SOURCE=11. 6-arg call site at :314. 5 OPEN-E test functions at :300/:330/:351/:397/:416. 14/14 passing. |
| `test/fuzz/AfKingSubscription.t.sol` | Re-derived offsets + 6-arg subscribe + 2 OPEN-E auth tests | ✓ VERIFIED | OFF_LASTSWEPT=1, OFF_PAIDTHROUGH=5, OFF_FLAGS=10. 6-arg call site at :282. testUnapprovedFundingSourceRefusedThenHonored at :260. testRevokeDoesNotStopActiveSubButDefundDoes at :287. 9/9 passing. |
| `test/fuzz/AfKingConcurrency.t.sol` | Re-derived offsets + 6-arg subscribe call site | ✓ VERIFIED | OFF_LASTSWEPT=1, OFF_PAIDTHROUGH=5, OFF_FLAGS=10. 6-arg call site at :416. 10/10 passing. |
| `test/gas/SweepPerPlayerWorstCaseGas.t.sol` | Updated subscribe call site (address(0)) | ✓ VERIFIED | 6-arg call site at :348. OFF_LASTSWEPT=1 re-derived. 3/3 passing. |
| `test/fuzz/CrankNonBrick.t.sol` | Two stale 5-arg subscribe sites updated (Wave-1 handoff deviation) | ✓ VERIFIED | :464 and :490 both carry `, address(0)` as the 6th arg. 12/12 passing. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| AfKing.sol sweep() ETH draw | `_poolOf[resolved fundingSource]` | once-per-iteration `src` used at :724 read and :746 debit | ✓ WIRED | :693 `address src = sub.fundingSource == address(0) ? player : sub.fundingSource;` both subsequent uses key on `src` |
| AfKing.sol subscribe() | `IGame.isOperatorApproved(fundingSource, subscriber)` | subscribe-only gate at :393-398, guarded `fundingSource != 0 && != subscriber` | ✓ WIRED | Exactly one new call site; absent from renewal branch (:620-651) and per-draw loop body |
| AfKing.sol burnForKeeper sites :434/:630 | `IBurnie.burnForKeeper(resolved fundingSource, cost)` | resolved-source `user` arg at both sites | ✓ WIRED | :434-435 `s.fundingSource == address(0) ? subscriber : s.fundingSource`; :630-631 `sub.fundingSource == address(0) ? player : sub.fundingSource` |
| DegenerusVault.sol + StakedDegenerusStonk.sol | `afKing.subscribe(..., address(0))` | rippled subscribe signature; SUB-09 protocol callers pass self | ✓ WIRED | Vault :474, sDGNRS :380 both pass `address(0)` as 6th arg; both IAfKingSubscribe interface decls carry the new param |
| test Sub offset constants | post-repack `forge inspect AfKing storage-layout` | re-derived OFF_* byte offsets matching struct declaration order | ✓ WIRED | Struct at :79-86 yields dailyQuantity@0, lastSweptDay@1, paidThroughDay@5, reinvestPct@9, flags@10, fundingSource@11. Test constants match exactly across all 4 test files. |
| every test subscribe() call site | rippled AfKing.subscribe(..., address fundingSource) signature | appended 6th address(0) argument at 6 call sites | ✓ WIRED | AfKingFundingWaterfall:314, AfKingConcurrency:416, AfKingSubscription:282, SweepPerPlayerWorstCaseGas:348, CrankNonBrick:464+490 — all 6 carry the new arg |

### Data-Flow Trace (Level 4)

Not applicable — AfKing.sol is a keeper contract (no UI/component rendering). The behavioral tests serve as the data-flow proof: they invoke the actual contract functions and assert that `_poolOf[src]` is debited (not `_poolOf[player]`) and `burnForKeeper(src, cost)` is called (not `burnForKeeper(player, cost)`).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full AfKing-touching suite green | `forge test --match-contract 'AfKingFundingWaterfall\|AfKingSubscription\|AfKingConcurrency\|CrankNonBrick\|RngFreezeAndRemovalProofs\|SweepPerPlayerWorstCaseGas'` | 61/61 passed, 0 failed | ✓ PASS |
| forge build (all contracts + tests) | `forge build` | No files changed, compilation skipped (all artifacts cached, 0 errors) | ✓ PASS |
| Storage slot count unchanged | `forge inspect AfKing storage-layout` | Slots 0-4 only (`_poolOf`=0, `_subOf`=1, `_subscribers`=2, `_subscriberIndex`=3, `_sweepDay`+`_sweepCursor`=4) | ✓ PASS |
| No setter / no event for fundingSource | `grep -n 'setFundingSource\|FundingSourceUpdated' contracts/AfKing.sol` | 0 hits | ✓ PASS |
| No stale typed accessor refs in test/ | `grep -rn 'setFundingSource\|FundingSourceUpdated\|\.drainGameCreditFirst\|\.useTickets' test/` | 0 hits | ✓ PASS |
| LANDMINE A: VAULT exemption keyed on `player` | AfKing.sol:725 source inspection | `player == ContractAddresses.VAULT \|\| player == ContractAddresses.SDGNRS` — not `src` | ✓ PASS |
| LANDMINE B: per-draw approval check unchanged | AfKing.sol:656 source inspection | `isOperatorApproved(player, address(this))` — byte-identical to pre-OPEN-E | ✓ PASS |

### Probe Execution

No phase-declared probe scripts. Not a migration/tooling phase. Step 7c: SKIPPED (no probe-*.sh files; behavioral coverage via forge test suites above).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| OPENE-01 | 319.1-01, 319.1-02 | Sub.fundingSource set only via subscribe() param; bools→flags repack; slot count unchanged; signature ripples to Vault+sDGNRS | Pending (intentional — Phase 320 TERMINAL attestation) | Struct at :79-86, FLAG constants at :236/:240/:244, no setter/event, Vault+sDGNRS interfaces updated, storage layout 5 slots unchanged |
| OPENE-02 | 319.1-01, 319.1-02 | ETH draw reads/debits `_poolOf[fundingSource]`; VAULT/SDGNRS exemption stays subscriber-keyed | Pending (intentional) | :693 src resolution; :724/:746 `_poolOf[src]`; :725 exemption on `player`; testCrossAccountEthDrawsSourcePool passes |
| OPENE-03 | 319.1-01, 319.1-02 | Both burnForKeeper sites route to resolved fundingSource; subscribe-time write makes S fund window 1; all-or-nothing preserved | Pending (intentional) | :422 write precedes :434 burn; :630-631 renewal burn routes to src; testCrossAccountBurnieFundsWindowOneAndRenewal passes |
| OPENE-04 | 319.1-01, 319.1-02 | Operator-approval auth at subscribe() only; revoke-does-not-stop-active-sub; BURNIE blast-radius documented | Pending (intentional) | :393-398 gate; no renewal/per-draw gate; NatSpec :362-370; testUnapprovedFundingSourceRefusedThenHonored + testRevokeDoesNotStopActiveSubButDefundDoes pass |

Note: OPENE-01..04 status is intentionally "Pending" in REQUIREMENTS.md — mirroring the v45 VRF-01..05 precedent. Requirements are test-proven at this IMPL phase but formally attested at Phase 320 TERMINAL adversarial sweep. The `[ ]` checkbox is the correct cross-phase bookkeeping, not a gap.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | — | No TBD/FIXME/XXX/PLACEHOLDER patterns in any modified file | — | — |

No debt markers, no stub returns, no hardcoded empty data in contract or test files modified by this phase. No anti-pattern blockers.

### Human Verification Required

#### 1. WR-01 Event Observability — Design Decision Confirmation

**Test:** Confirm the deliberate absence of a fundingSource event is an accepted product decision.
**Expected:** The SubscriptionUpdated event intentionally does NOT carry `fundingSource`. The plan spec explicitly required "NO FundingSourceUpdated event." A source S monitoring whether it has become a standing funding source for subscriber M must poll `subscriptionOf(M).fundingSource` off-chain or watch KeeperBurn/pool-debit side effects. This is documented in the code review (WR-01) as a robustness/auditability gap, not an exploit.
**Why human:** Whether this observability gap is acceptable for a real-money contract where the approval also authorizes burning S's general-wallet BURNIE is a product/security tradeoff. The verifier cannot determine product intent; the phase plan explicitly designed out the new event. If WR-01 is accepted, no action is needed. If it is not accepted, the Phase 320 TERMINAL adversarial sweep should include adding `fundingSource` to the event or emitting a dedicated binding event.

---

### Gaps Summary

No gaps. All 13 must-haves are VERIFIED. The phase's single batched 3-contract USER-approved diff (commit 42140ceb) is present, the test mirror is fully re-synced (commits 95e96532 + 4654a56e + 8c54f97a), all 61 AfKing-touching tests pass, and the two security landmines (LANDMINE A: VAULT/SDGNRS exemption keyed on `player`; LANDMINE B: per-draw approval check byte-unchanged) are confirmed present and proven behaviorally.

The `human_needed` status is driven by WR-01 from the code review — the observability gap is a deliberate design decision from the plan spec ("no FundingSourceUpdated event") but warrants explicit product-level confirmation for a real-money contract before Phase 320 proceeds.

---

_Verified: 2026-05-24_
_Verifier: Claude (gsd-verifier)_
