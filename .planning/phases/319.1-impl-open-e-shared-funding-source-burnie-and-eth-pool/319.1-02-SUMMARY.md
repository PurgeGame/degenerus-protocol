---
phase: 319.1-impl-open-e-shared-funding-source-burnie-and-eth-pool
plan: 02
subsystem: testing
tags: [foundry, afking, subscription, funding-source, operator-approval, storage-layout, vm.store]

# Dependency graph
requires:
  - phase: 319.1-01
    provides: "the committed Wave-1 3-contract OPEN-E diff (42140ceb) — subscribe() 6th fundingSource param + Sub repack (bools→flags, +20-byte address) + ETH/BURNIE routing + the AFTER storage-layout offset table"
provides:
  - "Test-mirror sync: all five stale 5-arg subscribe() call sites updated to the rippled 6-arg signature (the 4 listed in files_modified + CrankNonBrick :464/:490)"
  - "Re-derived Sub packed-byte offset constants (OFF_LASTSWEPT 3→1, OFF_PAIDTHROUGH 7→5, OFF_FLAGS 12→10; + OFF_REINVEST=9, OFF_FUNDING_SOURCE=11, FLAG_DRAIN_FIRST=2, FLAG_USE_TICKETS=4) across the three AfKing fuzz files + SweepPerPlayerWorstCaseGas"
  - "OPEN-E behavioral coverage: default-self byte-equivalence, cross-account ETH draw, cross-account BURNIE incl. window-1 burn, source-shortfall revert, LANDMINE-A exemption-spoof refusal, unapproved-source-refused, revoke-does-not-stop-active-sub"
affects: [Phase 320 TERMINAL adversarial sweep + OPEN-E delta audit + closure]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Cross-account source fixture: source S grants game operator-approval to subscriber M (game.setOperatorApproval(M,true) pranked as S), M self-subscribes with fundingSource=S, M approves the keeper — proves the money-holder-grants-spender direction"
    - "VAULT spendable-BURNIE seeding via direct balanceOf vm.store (NOT mintForGame) — mintForGame(VAULT,.) routes to the virtual escrow reserve, so burnForKeeper(VAULT,.) would see zero spendable balance"
    - "Same-day cursor-revisit limitation: re-reaching a past-cursor sub for a second renewal requires a +1 keeper-local-day warp (cursor resets at a new day) — used to prove the defund auto-pause after the trust-the-sub renewal"

key-files:
  created:
    - .planning/phases/319.1-impl-open-e-shared-funding-source-burnie-and-eth-pool/319.1-02-SUMMARY.md
  modified:
    - test/fuzz/AfKingFundingWaterfall.t.sol
    - test/fuzz/AfKingSubscription.t.sol
    - test/fuzz/AfKingConcurrency.t.sol
    - test/gas/SweepPerPlayerWorstCaseGas.t.sol
    - test/fuzz/CrankNonBrick.t.sol

key-decisions:
  - "OPENE-01..04 stay Pending in REQUIREMENTS.md — attested at Phase 320 TERMINAL, mirroring the v45 VRF-01..05 precedent (Wave 1 implemented + Wave 2 test-proven, but the formal mark-complete lands with the terminal adversarial sweep)"
  - "Per-task atomic commits (Task 1 + Task 2) rather than the plan's single combined Task-3 commit — the standard executor per-task-commit pattern; Task 3 produced no new test diff (it is the run-and-confirm gate), so no separate commit was needed for it"
  - "VAULT spendable BURNIE seeded via direct balanceOf vm.store in the exemption-spoof test (mintForGame(VAULT,.) escrow-routes and would not feed burnForKeeper)"

patterns-established:
  - "Re-derive vm.store offset constants from the contract struct declaration order after any repack — forge inspect shows top-level slots only and does NOT expand a struct inside a mapping"

requirements-completed: []  # OPENE-01..04 test-proven this wave but left Pending until Phase 320 TERMINAL attestation (VRF-01..05 precedent)

# Metrics
duration: ~35min
completed: 2026-05-24
---

# Phase 319.1 Plan 02: OPEN-E Test-Mirror Sync + Funding-Source Coverage Summary

**Re-synced all five stale subscribe() call sites to the rippled 6-arg signature, re-derived the post-repack Sub vm.store byte offsets, and added 7 OPEN-E behavioral tests proving cross-account ETH+BURNIE funding (incl. the window-1 burn), the LANDMINE-A exemption-spoof refusal, and the subscribe-only trust-the-sub auth — all AfKing-touching suites green with zero new failures vs the 318 baseline.**

## Performance

- **Duration:** ~35 min
- **Started:** 2026-05-24
- **Completed:** 2026-05-24
- **Tasks:** 3
- **Files modified:** 5 test files (no contracts/*.sol)

## Accomplishments

- **Test-mirror offset re-derivation (OPENE-01 slot facet):** the Sub packed offsets DRIFTED when the two standalone bools collapsed into `flags` and the 20-byte `fundingSource` was inserted. Re-derived all of them and audited every `vm.store` mask/shift to reference the updated constants. The passing `_forceRenewalDue` / `_markSweptToday` / windowPaid writes empirically prove the offsets hit the right bytes.
- **Subscribe-signature ripple (OPENE-01 signature facet):** appended the 6th `address fundingSource` arg (`address(0)` = self) to all five stale call sites.
- **OPEN-E behavioral lock (OPENE-02/03/04 + LANDMINE A):** 7 new tests across the two behavioral suites.
- **No slot-drift regression:** CrankNonBrick (12/12) and RngFreezeAndRemovalProofs (13/13) — the Sub-reading suites — match the 318 baseline exactly.

## Authoritative offset table — Wave-1 AFTER layout vs the re-derived test constants

`forge inspect AfKing storage-layout` confirms the top-level slots are UNCHANGED (slot count stays 5: `_poolOf`=0, `_subOf`=1, `_subscribers`=2, `_subscriberIndex`=3, `_sweepDay`+`_sweepCursor`=4). The Sub struct lives inside the `_subOf` mapping, so `forge inspect` does NOT expand its within-slot byte offsets — those are derived from the struct declaration order (AfKing.sol:79-86) under Solidity byte-tight packing, matching the 319.1-01 AFTER table:

| Field | OLD offset (stale) | AFTER offset (re-derived) | Test constant |
|-------|--------------------|---------------------------|---------------|
| dailyQuantity (uint8)   | 0  | **0**     | OFF_DAILY=0 |
| drainGameCreditFirst    | 1  | — (→ flags bit 1) | FLAG_DRAIN_FIRST=2 |
| useTickets              | 2  | — (→ flags bit 2) | FLAG_USE_TICKETS=4 |
| lastSweptDay (uint32)   | 3  | **1**     | OFF_LASTSWEPT=1 |
| paidThroughDay (uint32) | 7  | **5**     | OFF_PAIDTHROUGH=5 |
| reinvestPct (uint8)     | 11 | **9**     | OFF_REINVEST=9 |
| flags (uint8)           | 12 | **10**    | OFF_FLAGS=10 (bit0=windowPaid → FLAG_WINDOW_PAID=1) |
| fundingSource (address) | —  | **11–30** | OFF_FUNDING_SOURCE=11 |

Each `OFF_*` constant in the three fuzz files + SweepPerPlayerWorstCaseGas now equals these re-derived offsets field-by-field.

## Call-site inventory — every stale `afKing.subscribe(...)` → 6-arg

| File:line | Before | After (appended arg) |
|-----------|--------|----------------------|
| test/fuzz/AfKingFundingWaterfall.t.sol:314 (was :306) | `(address(0), drainFirst, true, 1, 0)` | `…, address(0))` |
| test/fuzz/AfKingConcurrency.t.sol:416 (was :411) | `(address(0), false, true, 1, 0)` | `…, address(0))` |
| test/fuzz/AfKingSubscription.t.sol:282 (was :277) | `(address(0), false, true, q, 0)` | `…, address(0))` |
| test/gas/SweepPerPlayerWorstCaseGas.t.sol:348 (was :346) | `(address(0), false, true, 1, reinvestPct)` | `…, address(0))` |
| test/fuzz/CrankNonBrick.t.sol:464 | `{value: poolEth}(address(0), false, true, 1, 0)` | `…, address(0))` |
| test/fuzz/CrankNonBrick.t.sol:490 | `{value: poolEth}(address(0), false, true, 1, 0)` | `…, address(0))` |

(The CrankNonBrick line numbers are unchanged; the line numbers shifted in the other files because offset-constant comments expanded above them. The new-test additions in the two behavioral files also shifted later line numbers — call-site grep at HEAD confirms each carries the 6th arg.)

## New-test inventory → requirement / landmine map

| Test | File | Proves |
|------|------|--------|
| testFundingSourceDefaultSelfIsByteEquivalent | AfKingFundingWaterfall | OPENE-02/03 default-self byte-equivalence (window-1 burn + per-day ETH draw both debit the subscriber's OWN identity when fundingSource==0) |
| testCrossAccountEthDrawsSourcePool | AfKingFundingWaterfall | OPENE-02 cross-account ETH draw debits `_poolOf[S]`, M's pool untouched |
| testCrossAccountBurnieFundsWindowOneAndRenewal | AfKingFundingWaterfall | OPENE-03 S funds BOTH the window-1 SUB-01 burn AND the day-31 auto-extract; M holds zero BURNIE throughout |
| testCrossAccountBurnieSourceShortfallRevertsSubscribe | AfKingFundingWaterfall | OPENE-03 source-shortfall → subscribe reverts BurnieChargeFailed (no half-paid sub) |
| testFundingSourceVaultDoesNotInheritExemption | AfKingFundingWaterfall | LANDMINE A — a NORMAL sub with fundingSource=VAULT is STILL cancelled on a funding skip (exemption keys on the un-spoofable `player`, never the source); the genuine VAULT self-sub stays |
| testUnapprovedFundingSourceRefusedThenHonored | AfKingSubscription | OPENE-04 unapproved non-zero source reverts NotApproved at subscribe; honored after S approves M |
| testRevokeDoesNotStopActiveSubButDefundDoes | AfKingSubscription | OPENE-04 subscribe-only auth — a post-subscribe revoke does NOT stop the active sub (day-31 renewal STILL burns S); the sub halts only when S defunds (→ day-31 auto-pause) |

## Suite pass counts vs the 318 baseline

| Suite | 318 baseline | This wave | Delta | Slot-drift? |
|-------|--------------|-----------|-------|-------------|
| AfKingConcurrency | 10/10 | 10/10 | +0 | none |
| AfKingFundingWaterfall | 9/9 | **14/14** | +5 OPEN-E | none |
| AfKingSubscription | 7/7 | **9/9** | +2 OPEN-E | none |
| CrankNonBrick | 12/12 | 12/12 | +0 | none |
| RngFreezeAndRemovalProofs | 13/13 | 13/13 | +0 | none |
| SweepPerPlayerWorstCaseGas (sanity) | 3/3 | 3/3 | +0 | none |

ZERO new failures introduced by the OPEN-E test sync. The Sub-reading families (CrankNonBrick, RngFreeze) confirm the repack introduced no slot-index drift regression.

## Task Commits

Each task committed atomically (test/ files only — no contracts/*.sol; AGENT-COMMITTED per feedback_no_contract_commits):

1. **Task 1: re-derive Sub offsets + ripple subscribe() 6th arg** — `95e96532` (test)
2. **Task 2: OPEN-E behavioral coverage** — `4654a56e` (test)
3. **Task 3: run full AfKing-touching suite + confirm no slot drift** — no new diff; the run-and-confirm gate (suites green, baseline held). The test sync was already committed across Tasks 1–2.

## Files Created/Modified

- `test/fuzz/AfKingFundingWaterfall.t.sol` — re-derived offsets + 6-arg call site + 5 OPEN-E waterfall/landmine cases + cross-account source helper
- `test/fuzz/AfKingSubscription.t.sol` — re-derived offsets + 6-arg call site + 2 OPEN-E subscribe-only-auth cases
- `test/fuzz/AfKingConcurrency.t.sol` — re-derived offsets + 6-arg call site
- `test/gas/SweepPerPlayerWorstCaseGas.t.sol` — re-derived OFF_LASTSWEPT (3→1) + 6-arg call site
- `test/fuzz/CrankNonBrick.t.sol` — two 6-arg call sites (:464, :490 — the Wave-1 handoff deviation)

## Decisions Made

- **OPENE-01..04 left Pending in REQUIREMENTS.md** — test-proven this wave, but the formal mark-complete is deferred to the Phase 320 TERMINAL adversarial sweep + OPEN-E delta audit, mirroring the v45 VRF-01..05 "Pending until TERMINAL" precedent.
- **Per-task atomic commits** rather than the plan's single Task-3 combined commit — standard executor pattern; Task 3 is a run-and-confirm gate with no additional test diff.
- **VAULT spendable BURNIE seeded via direct `balanceOf` vm.store** in the exemption-spoof test, because `mintForGame(VAULT,.)` routes to the virtual escrow reserve and would not feed `burnForKeeper`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Updated two stale subscribe() call sites NOT in files_modified**
- **Found during:** Task 1 (signature ripple)
- **Issue:** `test/fuzz/CrankNonBrick.t.sol` has two stale 5-arg `subscribe{value:…}(...)` calls (:464, :490) that break compilation against the new 6-arg signature, but the file is not in the plan's `files_modified`. The Wave-1 SUMMARY (319.1-01) explicitly flagged this handoff.
- **Fix:** Appended `, address(0)` to both call sites so the suite compiles.
- **Files modified:** test/fuzz/CrankNonBrick.t.sol
- **Verification:** `forge build` green; CrankNonBrick suite 12/12 (matches 318 baseline)
- **Committed in:** 95e96532 (Task 1 commit)

**2. [Rule 3 - Blocking] Re-derived a fifth stale offset constant (SweepPerPlayerWorstCaseGas OFF_LASTSWEPT)**
- **Found during:** Task 1 (offset re-derivation)
- **Issue:** `test/gas/SweepPerPlayerWorstCaseGas.t.sol:66` carried a stale `OFF_LASTSWEPT = 3` (old layout) used by a `lastSweptDay` slot read; left stale it would read the wrong bytes (it reads dailyQuantity+lastSweptDay-low instead of lastSweptDay).
- **Fix:** Re-derived to `OFF_LASTSWEPT = 1` per the AFTER layout.
- **Files modified:** test/gas/SweepPerPlayerWorstCaseGas.t.sol
- **Verification:** SweepPerPlayerWorstCaseGas suite 3/3 (the `lastSweptDay` read now resolves correctly)
- **Committed in:** 95e96532 (Task 1 commit)

**3. [Rule 1 - Bug] Exemption-spoof test BURNIE-seeded the wrong account**
- **Found during:** Task 2 (LANDMINE A test)
- **Issue:** The first draft funded the spoofer's BURNIE, but the window-1 burn routes to the resolved source (VAULT), so subscribe reverted BurnieChargeFailed before reaching the funding-skip surface under test. A second attempt via `mintForGame(VAULT,.)` also failed because that path escrow-routes (not balanceOf), so burnForKeeper still saw zero spendable.
- **Fix:** Seed VAULT's spendable balance directly via `balanceOf` vm.store so the window-1 burn succeeds and the sub reaches the sweep; the ETH-pool funding-skip is then the surface that proves LANDMINE A.
- **Files modified:** test/fuzz/AfKingFundingWaterfall.t.sol
- **Verification:** testFundingSourceVaultDoesNotInheritExemption passes
- **Committed in:** 4654a56e (Task 2 commit)

**4. [Rule 1 - Bug] Revoke test second-sweep cursor + NoSubscribersSwept masking**
- **Found during:** Task 2 (revoke-does-not-stop test)
- **Issue:** The defund half of the revoke test never re-reached M (the first sweep's advanced cursor had moved past M's slot — the cursor only resets at a new keeper-local day), and the bare auto-pause sweep produced no buy → NoSubscribersSwept revert masked the assertions.
- **Fix:** Added healthy buying subs to each sweep, and warped +1 keeper-local day before the defund sweep so the cursor resets to 0 and re-reaches M; re-force renewal-due relative to the new today.
- **Files modified:** test/fuzz/AfKingSubscription.t.sol
- **Verification:** testRevokeDoesNotStopActiveSubButDefundDoes passes (renewal-still-burns-S after revoke, then auto-pause on defund)
- **Committed in:** 4654a56e (Task 2 commit)

---

**Total deviations:** 4 auto-fixed (2 blocking signature/offset, 2 test-construction bugs)
**Impact on plan:** All four were necessary to make the suite compile and the OPEN-E behaviors testable. No scope creep — no contracts/*.sol touched; the two extra blocking fixes (CrankNonBrick + SweepGas offset) were pre-flagged in the Wave-1 handoff and the critical-context brief.

## Issues Encountered

- The Sub struct's within-slot byte offsets are NOT visible to `forge inspect AfKing storage-layout` (it expands top-level slots only; the struct lives inside the `_subOf` mapping). Re-derived them from the struct declaration order under byte-tight packing, cross-checked against the AfKing.sol:70-86 NatSpec offset table and the 319.1-01 AFTER table. The passing slot-write tests are the empirical confirmation.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- OPEN-E is now test-proven across the funding waterfall, both BURNIE charge sites (incl. window-1), the LANDMINE-A exemption-spoof refusal, and the subscribe-only trust-the-sub auth.
- Phase 320 TERMINAL is charged to adversarially attack the `fundingSource` redirect (can it spoof the Vault/sDGNRS skip-kill exemption?) and to formally attest OPENE-01..04 at closure.
- Nothing pushed.

## Self-Check: PASSED

- SUMMARY.md present at the phase dir.
- Task commits `95e96532` (Task 1) + `4654a56e` (Task 2) present in git log.
- All five modified test files present on disk.

---
*Phase: 319.1-impl-open-e-shared-funding-source-burnie-and-eth-pool*
*Completed: 2026-05-24*
