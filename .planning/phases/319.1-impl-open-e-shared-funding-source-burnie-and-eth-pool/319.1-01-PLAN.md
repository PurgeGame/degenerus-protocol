---
phase: 319.1-impl-open-e-shared-funding-source-burnie-and-eth-pool
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [contracts/AfKing.sol, contracts/DegenerusVault.sol, contracts/StakedDegenerusStonk.sol]
autonomous: false
requirements: [OPENE-01, OPENE-02, OPENE-03, OPENE-04]
user_setup: []

must_haves:
  truths:
    - "One funded wallet S covers BOTH the BURNIE subscription charge AND the ETH _poolOf auto-buy draw for a subscriber M's addresses (OPENE-01/02/03 net effect)"
    - "Sub carries `address fundingSource` (default address(0) = self), set ONLY via a NEW `address fundingSource` parameter appended to subscribe() — there is NO standalone setFundingSource setter and NO FundingSourceUpdated event; to change a source the user re-subscribes (which re-checks) (OPENE-01)"
    - "subscribe()'s signature ripples to the two SUB-09 protocol callers: DegenerusVault and StakedDegenerusStonk update their IAfKingSubscribe interface decl + their afKing.subscribe(...) call site to pass `address(0)` (self); no other behavior change in those two files (OPENE-01)"
    - "The two standalone bools drainGameCreditFirst + useTickets collapse into the existing flags byte; Sub remains ONE 32-byte slot; slot COUNT (5 slots, 0-4) unchanged (OPENE-01)"
    - "subscribe() writes `s.fundingSource = fundingSource` in the state-writes block (alongside :380-384, BEFORE the SUB-01 pass-or-pay gate at :389-399), so the :396 first-window burnForKeeper reads the just-set source → S funds window 1 (OPENE-03)"
    - "The sweep ETH draw reads (:672) and debits (:694) _poolOf[resolved source] (self when fundingSource==0); both use the SAME once-per-iteration resolved source; per-draw gas unchanged (same single-key SLOAD) (OPENE-02)"
    - "Both burnForKeeper sites — subscribe :396 and sweep day-31 :587 — pass the resolved fundingSource as the `user` arg; all-or-nothing shortfall paths (subscribe revert / day-31 auto-pause) preserved (OPENE-03)"
    - "A non-zero non-self fundingSource is honored only when isOperatorApproved(fundingSource, subscriber) is true, checked at subscribe() ONLY against the NEW param — NEVER at the day-31 renewal, NEVER per-draw; fundingSource==0 (self) short-circuits the approval read entirely; an unapproved non-zero source at subscribe reverts NotApproved (OPENE-04)"
    - "Revoke-does-not-stop-active-sub (by design): after subscribe with an approved source, a later setOperatorApproval(M,false) does NOT stop the active sub; the day-31 renewal trusts the subbed state and keeps drawing/burning the stored source; S stops draws by defunding (_poolOf[S] / spending down BURNIE), M stops by cancelling (OPENE-04)"
    - "LANDMINE A: the InsufficientPool exemption at :673 keeps comparing the iterated un-spoofable `player` to ContractAddresses.VAULT/SDGNRS — NEVER the resolved source — so a NORMAL sub cannot set fundingSource=VAULT to inherit the never-cancel exemption"
    - "LANDMINE B: the existing per-draw isOperatorApproved(player, address(this)) check at :610 (subscriber->keeper, PlayerSkipped reason 5) is LEFT BYTE-UNCHANGED; the new source->subscriber check is NOT folded into the per-draw loop body and NOT into the renewal branch"
    - "Default-self byte-equivalence: when fundingSource==0 every routing point resolves to the exact identity the shipped 317 code uses today (subscriber at :396, player at :587/:672/:694), so all untouched paths are behaviorally identical to pre-OPEN-E"
  artifacts:
    - path: "contracts/AfKing.sol"
      provides: "Sub.fundingSource field + new subscribe() fundingSource param + subscribe-only auth + 3 routing points, all in ONE batched diff (NO setter, NO new event)"
      contains: "address fundingSource"
    - path: "contracts/AfKing.sol"
      provides: "flag-bit constants for the two collapsed bools"
      contains: "FLAG_"
    - path: "contracts/DegenerusVault.sol"
      provides: "IAfKingSubscribe interface decl + the SUB-09 afKing.subscribe(...) call site updated for the new fundingSource param (passes address(0) = self)"
      contains: "address(0)"
    - path: "contracts/StakedDegenerusStonk.sol"
      provides: "IAfKingSubscribe interface decl + the SUB-09 afKing.subscribe(...) call site updated for the new fundingSource param (passes address(0) = self)"
      contains: "address(0)"
  key_links:
    - from: "contracts/AfKing.sol sweep() ETH draw"
      to: "_poolOf[resolved fundingSource]"
      via: "once-per-iteration source resolution used at both :672 read and :694 debit"
      pattern: "_poolOf\\["
    - from: "contracts/AfKing.sol subscribe()"
      to: "IGame.isOperatorApproved(fundingSource, subscriber)"
      via: "subscribe-time cross-account approval gate against the new param (fundingSource!=0 guarded), exactly ONE call site"
      pattern: "isOperatorApproved\\("
    - from: "contracts/AfKing.sol burnForKeeper sites :396/:587"
      to: "IBurnie.burnForKeeper(resolved fundingSource, cost)"
      via: "resolved-source `user` arg"
      pattern: "burnForKeeper\\("
    - from: "contracts/DegenerusVault.sol + contracts/StakedDegenerusStonk.sol"
      to: "afKing.subscribe(..., address(0))"
      via: "the rippled subscribe signature; SUB-09 protocol callers pass self"
      pattern: "subscribe\\("
---

<objective>
Apply the OPEN-E shared-funding-source design as ONE batched USER-APPROVED diff across THREE mainnet contracts — `contracts/AfKing.sol` + `contracts/DegenerusVault.sol` + `contracts/StakedDegenerusStonk.sol` — the SECOND IMPL diff on top of the shipped Phase 317 batch. Add an `address fundingSource` field to the `Sub` record (default `address(0)` = self), set ONLY via a NEW `fundingSource` parameter appended to `subscribe()` (no standalone setter), so one funded wallet S can cover BOTH the BURNIE subscription charge AND the ETH `_poolOf` auto-buy draw for a subscriber M's multiple addresses, authorized by the game's existing operator-approval mapping checked at `subscribe()` only.

Purpose: Multi-wallet players (OPENE-01..04) get a single funding wallet without a new approval primitive. The `subscribe()` signature change ripples to the two SUB-09 protocol callers (Vault + sDGNRS), which pass `address(0)` (self). Default-self keeps every untouched path byte-identical to pre-OPEN-E.

Output: A single batched 3-contract diff presented at the blocking USER-APPROVAL checkpoint, with the BEFORE storage-layout baseline captured for the Wave-2 test-mirror re-derivation. NOTHING is pre-approved; the diff is not committed or pushed until the user reviews and approves it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/319.1-impl-open-e-shared-funding-source-burnie-and-eth-pool/319.1-RESEARCH.md

<interfaces>
<!-- Grep-verified against the three contracts at HEAD this session. The executor uses these directly. -->
<!-- NOTE: RESEARCH.md still describes the EARLY model (a standalone setFundingSource setter + a -->
<!-- FundingSourceUpdated event + a renewal-branch auth re-check). Those are SUPERSEDED. The FINAL -->
<!-- model is the one encoded below and in the tasks: a fundingSource PARAM on subscribe(), NO setter, -->
<!-- NO new event, subscribe-ONLY auth. Where RESEARCH conflicts, this plan wins. -->

Current Sub struct (AfKing.sol:80-88, doc :69-79) — ONE 32-byte slot, reached via `_subOf` at slot 1:
  offset 0   uint8  dailyQuantity        (0 = paused/never-subscribed)
  offset 1   bool   drainGameCreditFirst    <-- COLLAPSE into flags
  offset 2   bool   useTickets              <-- COLLAPSE into flags
  offset 3   uint32 lastSweptDay
  offset 7   uint32 paidThroughDay
  offset 11  uint8  reinvestPct
  offset 12  uint8  flags                (bit 0 = FLAG_WINDOW_PAID = 1, AfKing.sol:238)
  offsets 13..31 = 19 free bytes  → a 20-byte address does NOT fit standalone.
  Collapsing the two bools frees room to 21 ≥ 20 → `address fundingSource` fits in the SAME slot.

Existing flag idiom (already in source): `s.flags &= ~FLAG;` / `s.flags |= FLAG;` at :238, :392, :398, :581, :593, :603, :682.

subscribe() shape (AfKing.sol:348-400) — VERIFIED HEAD:
  signature :348-354  (address player, bool drainGameCreditFirst, bool useTickets, uint8 dailyQuantity, uint8 reinvestPct)
            → APPEND a 6th param `address fundingSource`
  :355-356  input reverts (InvalidDailyQuantity / InvalidReinvestPct)
  :358-364  SUB-02 self-or-operator auth; `subscriber` resolved at :359 (player==0 ? msg.sender : player)
  :368-371  msg.value credits _poolOf[subscriber]
  :373      Sub storage s = _subOf[subscriber]
  :380-384  state writes (s.dailyQuantity/.drainGameCreditFirst/.useTickets/.reinvestPct/.paidThroughDay)
            → THIS is where `s.fundingSource = fundingSource` is written, BEFORE the :396 burn
  :386-387  _addToSet + emit SubscriptionUpdated
  :391-399  SUB-01 pass-or-pay gate; burnForKeeper(subscriber, cost) at :396; revert BurnieChargeFailed :397

Errors already declared (reuse, do NOT add new): NotApproved :132, NotSubscribed :135, BurnieChargeFailed :139.
Flag constant: `uint8 internal constant FLAG_WINDOW_PAID = 1;` at :238 (add FLAG_DRAIN_FIRST / FLAG_USE_TICKETS next to it).

Sweep load-bearing anchors in AfKing.sol (VERIFIED HEAD):
  :567-606  sweep day-31 renewal branch (paidThroughDay <= today); hasAnyLazyPass :578; burnForKeeper(player, extractCost) :587; auto-pause shortfall :588-600  — NO approval re-check here
  :610      EXISTING per-draw isOperatorApproved(player, address(this)) — LANDMINE B, leave BYTE-UNCHANGED
  :634      read of useTickets (`if (!sub.useTickets && cost < LOOTBOX_MIN)`)   <-- convert to flag-bit read
  :646      read of drainGameCreditFirst (`if (!sub.drainGameCreditFirst)`)     <-- convert to flag-bit read
  :672      ETH draw skip-gate `if (_poolOf[player] < msgValue)`               <-- route read to resolved source
  :673      exemption `if (player == ContractAddresses.VAULT || player == ContractAddresses.SDGNRS)`  <-- LANDMINE A, stays keyed on `player`
  :694      CEI debit `_poolOf[player] -= msgValue`                            <-- route debit to resolved source

The four NotSubscribed-gated in-place setters (TEMPLATE for the bool→flag conversions ONLY — do NOT add a fundingSource setter):
  setDailyQuantity :410, setDrainGameCreditFirst :430, setMode :438, setReinvestPct :446
  SubscriptionUpdated emit sites passing the two bools: :387, :418, :426, :434, :442, :451 → pass `(s.flags & FLAG_x) != 0`
SubscriptionUpdated event :159-165 — keep ABI byte-stable (the two bool params stay).

Cross-contract surfaces (READ-ONLY — NOT modified this phase; verified present):
  DegenerusGame.sol:436  isOperatorApproved(address owner, address operator) view returns (bool)
                         → operatorApprovals[owner][operator]; owner(money-holder) = S, operator = M
  BurnieCoin burnForKeeper(address user, uint256 amount) onlyAfKing returns (uint256) — all-or-nothing; passing fundingSource needs ZERO BurnieCoin change
  ContractAddresses VAULT / SDGNRS / GAME / COIN pins present

Vault + sDGNRS subscribe ripple (VERIFIED HEAD):
  contracts/DegenerusVault.sol:81-87   interface IAfKingSubscribe.subscribe(address,bool,bool,uint8,uint8) external payable  → add `, address fundingSource`
  contracts/DegenerusVault.sol:473     afKing.subscribe(address(this), true, false, 1, 0);   → afKing.subscribe(address(this), true, false, 1, 0, address(0));
  contracts/StakedDegenerusStonk.sol:59-65  interface IAfKingSubscribe.subscribe(address,bool,bool,uint8,uint8) external payable  → add `, address fundingSource`
  contracts/StakedDegenerusStonk.sol:379    afKing.subscribe(address(this), true, false, 1, 2);  → afKing.subscribe(address(this), true, false, 1, 2, address(0));
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the BEFORE storage-layout baseline + re-grep-verify every cited anchor across all THREE contracts</name>
  <read_first>contracts/AfKing.sol; contracts/DegenerusVault.sol; contracts/StakedDegenerusStonk.sol; .planning/phases/319.1-impl-open-e-shared-funding-source-burnie-and-eth-pool/319.1-RESEARCH.md (§OPENE-01..04 + Common Pitfalls)</read_first>
  <action>Before ANY edit: (1) run `forge clean && forge build` then `forge inspect AfKing storage-layout` (the established 317-06/318-01 cadence; the artifact needs a full clean to emit the layout) and record the BEFORE per-field slot/offset table into the SUMMARY — this is the baseline the Wave-2 test-mirror re-derivation diffs against. (2) Re-grep-verify, against the current working-tree source, that each anchor in the `<interfaces>` block still resolves to the named construct: in AfKing.sol — the subscribe signature/state-writes/:396 burn, the day-31 :587 burn, the per-draw :610 check, the ETH read/debit :672/:694, the exemption :673, the bool reads :634/:646, the FLAG_WINDOW_PAID constant :238; in DegenerusVault.sol — the IAfKingSubscribe.subscribe decl (~:81) AND the call site `afKing.subscribe(address(this), true, false, 1, 0)` (~:473); in StakedDegenerusStonk.sol — the IAfKingSubscribe.subscribe decl (~:59) AND the call site `afKing.subscribe(address(this), true, false, 1, 2)` (~:379). If ANY line has drifted, record the corrected line and proceed off the corrected anchor (executor reads source, not assumptions, per feedback_verify_call_graph_against_source). NO source mutation in this task.</action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge clean >/dev/null 2>&1 && forge build >/dev/null 2>&1 && forge inspect AfKing storage-layout 2>&1 | grep -v '^Warning\|nightly\|FOUNDRY_DISABLE' | grep -q 'dailyQuantity' && git diff --quiet contracts/AfKing.sol contracts/DegenerusVault.sol contracts/StakedDegenerusStonk.sol && echo LAYOUT_OK_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `forge inspect AfKing storage-layout` emits a layout table (after `forge clean && forge build`); the BEFORE field/offset table is recorded in the plan SUMMARY.
    - Every `<interfaces>` anchor across all three contracts is re-grep-confirmed against live source; any drift is documented with the corrected line.
    - All three contracts are byte-unchanged at the end of this task (`git diff --quiet contracts/AfKing.sol contracts/DegenerusVault.sol contracts/StakedDegenerusStonk.sol` exits 0).
  </acceptance_criteria>
  <done>The BEFORE storage layout is captured and all cited anchors across the three contracts are verified against HEAD; zero source mutation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Repack Sub (bools→flags) + add the fundingSource field (OPENE-01) — NO setter, NO new event</name>
  <read_first>contracts/AfKing.sol (struct :80-88, doc :69-79, setters :410-452, flag idiom sites, SubscriptionUpdated :159-165, FLAG_WINDOW_PAID :238); 319.1-RESEARCH.md §OPENE-01</read_first>
  <behavior>
    - The repacked `Sub` is still ONE 32-byte slot; slot COUNT stays 5 (slots 0-4); `address fundingSource` lands in the same Sub slot reached via `_subOf`.
    - `drainGameCreditFirst` and `useTickets` become `flags` bit constants (FLAG_DRAIN_FIRST, FLAG_USE_TICKETS) alongside FLAG_WINDOW_PAID; every prior struct-field read/write converts to the masked `&` / `|=` / `&= ~` idiom.
    - `SubscriptionUpdated` is emitted everywhere it is today, with the two bool args sourced from `(s.flags & FLAG_x) != 0` — event ABI byte-unchanged.
    - There is NO standalone setFundingSource setter and NO FundingSourceUpdated event in this design (the source is set only via the subscribe() param, added in Task 3).
    - Default `fundingSource` for a fresh subscribe is `address(0)` (= self) — the field exists in the struct; the subscribe param + writes come in Task 3.
  </behavior>
  <action>Edit the `Sub` struct: remove the two standalone `bool drainGameCreditFirst`/`bool useTickets` fields, append `address fundingSource`, and update the struct doc-comment (:69-79) to describe the new layout (comments describe what IS, never what changed, per feedback_no_history_in_comments). Add two `uint8 internal constant FLAG_*` bit constants next to FLAG_WINDOW_PAID at :238 (Claude's discretion on the exact bit indices, e.g. FLAG_DRAIN_FIRST = 2, FLAG_USE_TICKETS = 4). Convert every `s.drainGameCreditFirst` / `s.useTickets` read+write site to the flag-bit idiom: subscribe writes :381/:382 (these become flag set/clear from the still-existing bool PARAMS), the setters setDrainGameCreditFirst :430-435 + setMode :438-443 (masked set/clear), the sweep reads :646 + :634, and the SubscriptionUpdated emit sites that pass these as bool args (:387/:418/:426/:434/:442/:451) — pass `(s.flags & FLAG_x) != 0`. Do NOT yet add the subscribe `fundingSource` param, the routing, or the auth — those are Task 3 (kept in the same file/diff but logically separate so the diff reads cleanly). Do NOT add any setter or event for fundingSource. Maximal-packing per feedback_maximal_variable_packing; pre-launch redeploy-fresh means the layout break is free (no migration).</action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | grep -v '^Warning\|nightly\|FOUNDRY_DISABLE' | grep -qi 'error' && echo BUILD_FAIL || (grep -q 'address fundingSource' contracts/AfKing.sol && ! grep -q 'function setFundingSource' contracts/AfKing.sol && ! grep -q 'FundingSourceUpdated' contracts/AfKing.sol && echo OK_NO_SETTER)</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` exits 0 (no compile error) with the repacked struct.
    - `grep -n 'bool drainGameCreditFirst\|bool useTickets' contracts/AfKing.sol` shows the struct FIELD declarations gone (the subscribe/setter bool PARAMETERS may remain); the two `Sub` struct members are removed.
    - `contracts/AfKing.sol` contains `address fundingSource` and at least two new `FLAG_` constants beyond FLAG_WINDOW_PAID.
    - There is NO `function setFundingSource` and NO `FundingSourceUpdated` anywhere in `contracts/AfKing.sol` (assert both greps return zero).
    - The `SubscriptionUpdated` event signature is byte-unchanged: `grep -q 'event SubscriptionUpdated(' contracts/AfKing.sol` and the param list still includes the two bool params.
    - `forge inspect AfKing storage-layout` shows slot count unchanged (still slots 0-4; `fundingSource` resides in the Sub slot, not a new top-level slot).
  </acceptance_criteria>
  <done>Sub is repacked to one slot with fundingSource appended and the bools living in flags; NO setter and NO new event were added; the suite compiles.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add the subscribe() fundingSource param + subscribe-only auth + route all 3 spend points; ripple the Vault/sDGNRS callers (OPENE-02/03/04)</name>
  <read_first>contracts/AfKing.sol (subscribe :348-400, sweep day-31 :567-606, per-draw :610, ETH draw :643-695); contracts/DegenerusVault.sol (:79-88, :468-474); contracts/StakedDegenerusStonk.sol (:57-66, :374-381); 319.1-RESEARCH.md §OPENE-02/03/04 + Common Pitfalls 1/2/3</read_first>
  <behavior>
    - subscribe() gains a 6th param `address fundingSource` (appended after reinvestPct). After the SUB-02 auth (:358-364) and using the resolved `subscriber` (:359), add the cross-account gate: `if (fundingSource != address(0) && fundingSource != subscriber && !IGame(ContractAddresses.GAME).isOperatorApproved(fundingSource, subscriber)) revert NotApproved();`. When `fundingSource == address(0)` (self) NO approval read happens (default-self short-circuit, Pitfall 3); an approved or self source proceeds; an unapproved non-zero non-self source reverts NotApproved (fail-fast, mirrors :362).
    - subscribe() writes `s.fundingSource = fundingSource` in the state-writes block alongside :380-384 — BEFORE the SUB-01 gate at :389-399 — so the :396 first-window burnForKeeper reads the just-set source and S funds window 1.
    - subscribe burn :396 passes `(s.fundingSource == address(0) ? subscriber : s.fundingSource)`. sweep day-31 burn :587 passes `(sub.fundingSource == address(0) ? player : sub.fundingSource)` with NO approval re-check (the renewal trusts the subbed state — subscribe-only auth). All-or-nothing shortfall behavior (subscribe revert :397 / day-31 auto-pause :588-600) unchanged.
    - ETH draw: a once-per-iteration `address src = sub.fundingSource == address(0) ? player : sub.fundingSource;` is read at :672 (`_poolOf[src] < msgValue`) and debited at :694 (`_poolOf[src] -= msgValue`) — SAME src at both. The exemption at :673 still compares `player` (LANDMINE A — NOT src).
    - Revoke-does-not-stop-active-sub: auth is checked at subscribe() only; an active sub is NEVER re-checked. A later setOperatorApproval(M,false) does NOT stop the sub — the renewal keeps drawing/burning the stored source. S stops draws by defunding (withdraw _poolOf[S] / spend down BURNIE); M stops by cancelling; to re-point the source M re-subscribes (which re-checks). User-locked trust-the-sub posture (same-owner multi-wallet); per-draw AND renewal re-checks both rejected.
    - The EXISTING per-draw :610 check is byte-unchanged (LANDMINE B); the new approval read is NOT in the per-iteration loop body and NOT in the renewal branch — exactly ONE new isOperatorApproved call site, in subscribe().
    - Vault + sDGNRS: their IAfKingSubscribe interface decl gains the `address fundingSource` param and their afKing.subscribe(...) call site appends `address(0)` (self). No other behavior change in those two files.
  </behavior>
  <action>In AfKing.sol `subscribe()`: append `address fundingSource` to the signature (:348-354 → 6 params). After the SUB-02 auth block (:358-364), insert the cross-account gate exactly as in `<behavior>` (guarded on `fundingSource != address(0) && fundingSource != subscriber`, reads isOperatorApproved(fundingSource, subscriber), reverts the already-declared NotApproved). In the state-writes block (:380-384), add `s.fundingSource = fundingSource;`. Route the SUB-01 burn :396 to `(s.fundingSource == address(0) ? subscriber : s.fundingSource)`. In `sweep()`: compute the resolved source ONCE per iteration before the funding-waterfall block and use it at the :672 read and :694 debit; leave the :673 exemption comparing `player` (LANDMINE A — do NOT change to src). In the day-31 renewal branch (:567-606): route the burn at :587 to `(sub.fundingSource == address(0) ? player : sub.fundingSource)` WITHOUT any approval re-check. Leave :610 untouched. Document the BURNIE blast-radius caveat (the operator approval also authorizes burning S's general-wallet BURNIE + pending coinflip — sharper than the pre-funded ETH escrow; `allowBurnieFunding[S][M]` is the named deferred alternative) AND the revoke-does-not-stop-active-sub posture in the subscribe() NatSpec, per OPENE-04 + the threat-model gate. Then update DegenerusVault.sol — the IAfKingSubscribe.subscribe interface decl (~:81) gains `address fundingSource`, and the call site (~:473) becomes `afKing.subscribe(address(this), true, false, 1, 0, address(0));`. And StakedDegenerusStonk.sol — the IAfKingSubscribe.subscribe interface decl (~:59) gains `address fundingSource`, and the call site (~:379) becomes `afKing.subscribe(address(this), true, false, 1, 2, address(0));`. Comments describe what IS, never what changed.</action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | grep -v '^Warning\|nightly\|FOUNDRY_DISABLE' | grep -qi 'error' && echo BUILD_FAIL || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` exits 0 (the subscribe signature change compiles across all three contracts).
    - subscribe() takes a 6th `address fundingSource` param and writes `s.fundingSource = fundingSource` BEFORE the SUB-01 gate (`grep -n 's.fundingSource = fundingSource' contracts/AfKing.sol` shows the write inside subscribe, ahead of the :396 burn line).
    - Exactly ONE new cross-account `isOperatorApproved(<source>, subscriber)` call site, in subscribe(); it is NOT in the day-31 renewal branch and NOT inside the per-iteration loop body (verify there is exactly one NEW isOperatorApproved beyond the unchanged :610 `isOperatorApproved(player, address(this))`).
    - LANDMINE A held: the line comparing against `ContractAddresses.VAULT`/`SDGNRS` (former :673) still reads `player ==`, NOT `src ==`/`fundingSource ==` (`grep -n 'ContractAddresses.VAULT' contracts/AfKing.sol` shows the comparison keyed on `player`).
    - LANDMINE B held: `grep -q 'isOperatorApproved(player, address(this))' contracts/AfKing.sol` (the former :610 per-draw check is byte-present and unchanged).
    - Both `burnForKeeper` call sites pass a resolved-source expression (the `fundingSource == address(0) ? self : fundingSource` form), not the bare `subscriber`/`player`.
    - The ETH draw reads/debits `_poolOf[src]` (resolved source) at both the skip-gate and the CEI debit, using the same once-per-iteration `src`.
    - The BURNIE blast-radius caveat AND the revoke-does-not-stop-active-sub posture are present in the subscribe() NatSpec (`grep -qi 'allowBurnieFunding\|blast' contracts/AfKing.sol` and caveat/trust prose present).
    - DegenerusVault.sol + StakedDegenerusStonk.sol: both the IAfKingSubscribe.subscribe decl now carries a 6th `address` param and the call site passes `address(0)` as the new arg (`grep -q 'subscribe(address(this), true, false, 1, 0, address(0))' contracts/DegenerusVault.sol` and `grep -q 'subscribe(address(this), true, false, 1, 2, address(0))' contracts/StakedDegenerusStonk.sol`).
  </acceptance_criteria>
  <done>subscribe() carries the fundingSource param with subscribe-only auth and S-funds-window-1 ordering; all three spend points route through the resolved source; the renewal/per-draw paths add no approval read (LANDMINE B) and the exemption stays subscriber-keyed (LANDMINE A); the Vault/sDGNRS callers pass self; the suite compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: BLOCKING USER-APPROVAL gate — review the single batched 3-contract OPEN-E diff</name>
  <what-built>The complete OPEN-E diff across THREE mainnet contracts: (1) contracts/AfKing.sol — Sub repacked (bools→flags) with `address fundingSource` appended in one slot, subscribe() gains the `fundingSource` param (NO standalone setter, NO new event), the subscribe-time write makes S fund window 1, the cross-account operator-approval gate at subscribe() ONLY (renewal + per-draw add no approval read), the ETH draw + both burnForKeeper sites routed to the resolved fundingSource; (2) contracts/DegenerusVault.sol — IAfKingSubscribe decl + SUB-09 call site pass `address(0)` (self); (3) contracts/StakedDegenerusStonk.sol — same self ripple. Both landmines preserved (Vault/sDGNRS exemption keyed on `player`; the :610 per-draw check untouched). `forge build` PASSES. This is the SECOND batched contract diff on top of Phase 317 — NOTHING is pre-approved.</what-built>
  <how-to-verify>
    1. Review the full 3-contract diff: `cd /home/zak/Dev/PurgeGame/degenerus-audit && git diff contracts/AfKing.sol contracts/DegenerusVault.sol contracts/StakedDegenerusStonk.sol`
    2. Confirm `forge build` passes: `forge build 2>&1 | grep -i error` returns nothing.
    3. Confirm the storage layout: `forge inspect AfKing storage-layout` — slot count unchanged (slots 0-4), `fundingSource` in the Sub slot.
    4. Confirm NO standalone setter / new event: `grep -n 'setFundingSource\|FundingSourceUpdated' contracts/AfKing.sol` returns nothing.
    5. Confirm S-funds-window-1: the `s.fundingSource = fundingSource` write precedes the :396 SUB-01 burn in subscribe().
    6. Spot-check LANDMINE A (the VAULT/SDGNRS exemption still keys on `player`) and LANDMINE B (the per-draw `isOperatorApproved(player, address(this))` at the former :610 is unchanged); confirm exactly ONE new cross-account isOperatorApproved call site, in subscribe().
    7. Confirm the Vault + sDGNRS call sites pass `address(0)` and their interface decls carry the new param.
    8. Confirm the BURNIE blast-radius + revoke-does-not-stop-active-sub caveats are in the NatSpec.
    9. Decide whether the diff is approved for commit. Per project policy the diff is NOT committed or pushed until you approve.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the single batched commit of the three contracts, or describe required changes.</resume-signal>
  <action>HALT for blocking USER review of the complete 3-contract OPEN-E diff before any commit. Present the full `git diff contracts/AfKing.sol contracts/DegenerusVault.sol contracts/StakedDegenerusStonk.sol`, the `forge build` result, the `forge inspect AfKing storage-layout` before/after, the no-setter/no-event check, the S-funds-window-1 ordering check, and the two landmine spot-checks; do NOT commit or push until the user types "approved". NOTHING is pre-approved (feedback_never_preapprove_contracts).</action>
  <verify>
    <automated>forge build 2>&1 | grep -v '^Warning\|nightly\|FOUNDRY_DISABLE' | grep -qi 'error' && echo BUILD_FAIL || echo READY_FOR_REVIEW</automated>
  </verify>
  <done>The user has reviewed the single batched 3-contract diff and explicitly typed "approved" (or returned change requests, in which case loop back to Task 3).</done>
</task>

<task type="auto">
  <name>Task 5: Commit the USER-APPROVED batched 3-contract diff (only after approval)</name>
  <read_first>contracts/AfKing.sol; contracts/DegenerusVault.sol; contracts/StakedDegenerusStonk.sol</read_first>
  <action>ONLY after the Task 4 checkpoint returns "approved": commit the single batched diff across the three contracts. The repo's PreToolUse hook blocks commits while `contracts/*.sol` is dirty — set `CONTRACTS_COMMIT_APPROVED=1` for this one approved commit (per feedback_contract_commit_guard_hook). Commit message: `feat(319.1-01): OPEN-E shared funding source — subscribe() fundingSource param + operator-approval routing across AfKing/Vault/sDGNRS [OPENE-01..04]`. Do NOT push (feedback_manual_review_before_push — pushing is a separate user action). Do NOT bundle any test or planning file into this commit; those are Wave-2 / AGENT-COMMITTED work.</action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && git log -1 --pretty=format:'%s' | grep -q 'OPENE-01' && git show --stat HEAD --name-only | grep -q 'contracts/AfKing.sol' && git show --stat HEAD --name-only | grep -q 'contracts/DegenerusVault.sol' && git show --stat HEAD --name-only | grep -q 'contracts/StakedDegenerusStonk.sol' && echo COMMITTED</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one commit lands containing the three contracts (`contracts/AfKing.sol`, `contracts/DegenerusVault.sol`, `contracts/StakedDegenerusStonk.sol`) with the OPENE-01..04 message.
    - No test or planning file is in the commit.
    - `git status` shows the three contracts clean (committed).
    - Nothing pushed (no `git push` run).
  </acceptance_criteria>
  <done>The single USER-APPROVED 3-contract diff is committed (not pushed); the working tree's contracts are clean for Wave 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subscriber M → keeper (sweep) | M's stored `fundingSource` redirects keeper spends; M is untrusted, can set any address as source at subscribe time |
| funding source S → subscriber M | S's ETH `_poolOf` + general-wallet BURNIE are spendable on M's behalf iff S approved M (checked at subscribe) |
| keeper → DegenerusGame.operatorApprovals | the keeper READS the game's approval mapping at subscribe only; does not write it |
| AfKing.subscribe signature → Vault/sDGNRS callers | the param ripple crosses into two other mainnet contracts; both pass self (address(0)) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-319.1-01 | Spoofing | sweep :673 exemption (LANDMINE A) | mitigate | exemption compares the iterated un-spoofable `player` to VAULT/SDGNRS, NEVER the resolved source; a NORMAL sub with `fundingSource=VAULT` is still cancellable. Verified in Task 3 acceptance. |
| T-319.1-02 | Elevation of Privilege | M points fundingSource at non-consenting S | mitigate | `isOperatorApproved(S, M)` (S=owner grants M) required to honor a non-zero non-self source; checked at subscribe() only against the new param; unapproved → revert NotApproved. fundingSource is set ONLY via subscribe(), so the subscribe-time check is the complete gate. |
| T-319.1-03 | Elevation of Privilege | post-revoke continued drain | accept (by design) | auth checked at subscribe() only; an active sub is NEVER re-checked (no renewal/per-draw recheck), so a setOperatorApproval(M,false) revoke does NOT stop an active funding sub. Bound = sub lifetime + S's own controls: S empties `_poolOf[S]` via `withdraw()` (immediate ETH stop) / spends down BURNIE, or M cancels; to re-point M re-subscribes (which re-checks). User-locked trust-the-sub posture (same-owner multi-wallet); per-draw AND renewal re-checks both rejected. |
| T-319.1-04 | Information/Asset exposure | BURNIE blast-radius | accept (documented) | the same approval authorizes burning S's general-wallet BURNIE + pending coinflip (sharper than ETH escrow); caveat documented in subscribe() NatSpec; `allowBurnieFunding[S][M]` named as the deferred tighter alternative. |
| T-319.1-05 | Denial of Service | default-self regression | mitigate | `fundingSource == address(0)` short-circuits the approval read entirely; self subs (incl. the SUB-09 Vault/sDGNRS protocol subs that pass address(0)) never need a self-approval. |
| T-319.1-06 | Denial of Service / gas | per-draw extra STATICCALL (LANDMINE B) | mitigate | the new approval read lives ONLY in subscribe(); the existing per-draw :610 check is untouched and not joined by a second per-draw read; the renewal branch adds no approval read; ETH-draw routing is a same-key SLOAD (gas-neutral). |
| T-319.1-07 | Tampering | state integrity / CEI | mitigate | all routing edits sit inside the existing strict-CEI sweep; the :694 debit stays before the batched external call; the keeper is never a payee → no reentrancy guard needed. The subscribe write `s.fundingSource = fundingSource` is effects-before-interaction (precedes the :396 burn). |
| T-319.1-08 | Tampering | subscribe signature ripple to Vault/sDGNRS | mitigate | both SUB-09 callers pass `address(0)` (self) — no behavior change; their interface decls and call sites are updated in the same batched diff and compile-verified by `forge build`. |

No package-manager installs in this phase → no T-319.1-SC supply-chain row (no npm/pip/cargo; RESEARCH §Package Legitimacy Audit = N/A).
</threat_model>

<verification>
- `forge build` PASSES on the patched tree across all three contracts.
- `forge inspect AfKing storage-layout`: slot COUNT unchanged (slots 0-4); `fundingSource` resides in the Sub slot; bools gone as standalone struct fields.
- NO `setFundingSource` and NO `FundingSourceUpdated` anywhere in AfKing.sol.
- S-funds-window-1: `s.fundingSource = fundingSource` is written before the :396 SUB-01 burn in subscribe().
- LANDMINE A: the VAULT/SDGNRS exemption keys on `player`.
- LANDMINE B: the per-draw `isOperatorApproved(player, address(this))` is byte-unchanged.
- Exactly ONE new cross-account `isOperatorApproved(<source>, subscriber)` call site, in subscribe() (not renewal, not per-draw).
- Both burnForKeeper sites + the ETH read/debit route to the resolved fundingSource (self when 0).
- BURNIE blast-radius + revoke-does-not-stop-active-sub caveats present in NatSpec.
- Vault + sDGNRS interface decls carry the new param and their call sites pass `address(0)`.
- The single 3-contract commit lands only after the blocking USER-APPROVAL checkpoint; nothing pushed.
</verification>

<success_criteria>
- OPENE-01: `Sub.fundingSource` field set ONLY via the new subscribe() param (no standalone setter, no new event); the two bools collapsed into `flags`; slot count unchanged; the signature ripples to Vault + sDGNRS (pass self).
- OPENE-02: ETH draw reads/debits `_poolOf[fundingSource]` (self when 0), gas-neutral; the Vault/sDGNRS exemption stays subscriber-keyed.
- OPENE-03: both `burnForKeeper` sites (:396, :587) route to `fundingSource`; the subscribe-time write makes S fund window 1; all-or-nothing preserved.
- OPENE-04: operator-approval auth at subscribe() ONLY against the new param, never at renewal, never per-draw; revoke does NOT stop an active sub (trust-the-sub; S defunds or M cancels; re-point = re-subscribe); BURNIE caveat documented.
- The entire edit is ONE batched 3-contract diff behind ONE blocking USER-APPROVAL checkpoint; nothing pre-approved, nothing pushed.
- `forge build` PASSES.
</success_criteria>

<output>
Create `.planning/phases/319.1-impl-open-e-shared-funding-source-burnie-and-eth-pool/319.1-01-SUMMARY.md` when done — include the BEFORE storage-layout table (for the Wave-2 test-mirror re-derivation) and the AFTER layout, the per-OPENE-requirement edit summary, the three-contract scope confirmation, and the approved commit SHA.
</output>
