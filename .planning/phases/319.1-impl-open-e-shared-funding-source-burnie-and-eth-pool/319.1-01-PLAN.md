---
phase: 319.1-impl-open-e-shared-funding-source-burnie-and-eth-pool
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [contracts/AfKing.sol]
autonomous: false
requirements: [OPENE-01, OPENE-02, OPENE-03, OPENE-04]
user_setup: []

must_haves:
  truths:
    - "One funded wallet S covers BOTH the BURNIE subscription charge AND the ETH _poolOf auto-buy draw for a subscriber M's addresses (OPENE-01/02/03 net effect)"
    - "Sub carries `address fundingSource` (default address(0) = self), set via a caller-scoped NotSubscribed-gated setter that only msg.sender can call for their own record (OPENE-01)"
    - "The two standalone bools drainGameCreditFirst + useTickets collapse into the existing flags byte; Sub remains ONE 32-byte slot; slot COUNT (5 slots, 0-4) unchanged (OPENE-01)"
    - "The sweep ETH draw reads (:672) and debits (:694) _poolOf[fundingSource] resolved to self when 0; both use the SAME once-per-iteration resolved source; per-draw gas unchanged (OPENE-02)"
    - "Both burnForKeeper sites — subscribe :396 and sweep day-31 :587 — pass the resolved fundingSource as the `user` arg; all-or-nothing shortfall paths (subscribe revert / day-31 auto-pause) preserved (OPENE-03)"
    - "A non-zero fundingSource is honored only when isOperatorApproved(fundingSource, subscriber) is true, checked at subscribe + the day-31 renewal branch ONLY, never per-draw; fundingSource==0 short-circuits the approval read entirely (OPENE-04)"
    - "LANDMINE A: the InsufficientPool exemption at :673 keeps comparing the iterated un-spoofable `player` to ContractAddresses.VAULT/SDGNRS — NEVER the settable fundingSource — so a NORMAL sub cannot set fundingSource=VAULT to inherit the never-cancel exemption"
    - "LANDMINE B: the existing per-draw isOperatorApproved(player, address(this)) check at :610 (subscriber->keeper, PlayerSkipped reason 5) is LEFT BYTE-UNCHANGED; the new source->subscriber check is NOT folded into the per-draw loop body"
    - "Default-self byte-equivalence: when fundingSource==0 every routing point resolves to the exact identity the shipped 317 code uses today (subscriber at :396, player at :587/:672/:694), so all untouched paths are behaviorally identical to pre-OPEN-E"
  artifacts:
    - path: "contracts/AfKing.sol"
      provides: "Sub.fundingSource field + setFundingSource setter + 3 routing points + cross-account operator-approval auth, all in ONE batched diff"
      contains: "address fundingSource"
    - path: "contracts/AfKing.sol"
      provides: "the caller-scoped fundingSource setter mirroring the NotSubscribed-gated mutators"
      contains: "function setFundingSource(address"
    - path: "contracts/AfKing.sol"
      provides: "flag-bit constants for the two collapsed bools"
      contains: "FLAG_"
  key_links:
    - from: "contracts/AfKing.sol sweep() ETH draw"
      to: "_poolOf[resolved fundingSource]"
      via: "once-per-iteration source resolution used at both :672 read and :694 debit"
      pattern: "_poolOf\\["
    - from: "contracts/AfKing.sol subscribe() + day-31 renewal"
      to: "IGame.isOperatorApproved(fundingSource, subscriber)"
      via: "cross-account approval gate (subscribe + renewal only, fundingSource!=0 guarded)"
      pattern: "isOperatorApproved\\("
    - from: "contracts/AfKing.sol burnForKeeper sites :396/:587"
      to: "IBurnie.burnForKeeper(resolved fundingSource, cost)"
      via: "resolved-source `user` arg"
      pattern: "burnForKeeper\\("
---

<objective>
Apply the OPEN-E shared-funding-source design to `contracts/AfKing.sol` as ONE batched USER-APPROVED diff — the SECOND IMPL diff on top of the shipped Phase 317 batch. Add an `address fundingSource` field to the `Sub` record (default `address(0)` = self) so one funded wallet S can cover BOTH the BURNIE subscription charge AND the ETH `_poolOf` auto-buy draw for a subscriber M's multiple addresses, authorized by the game's existing operator-approval mapping.

Purpose: Multi-wallet players (OPENE-01..04) get a single funding wallet without a new approval primitive or any non-AfKing contract change. Default-self keeps every untouched path byte-identical to pre-OPEN-E.

Output: A single modified `contracts/AfKing.sol` presented as one diff at the blocking USER-APPROVAL checkpoint, with the BEFORE storage-layout baseline captured for the Wave-2 test-mirror re-derivation. NOTHING is pre-approved; the diff is not committed or pushed until the user reviews and approves it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/319.1-impl-open-e-shared-funding-source-burnie-and-eth-pool/319.1-RESEARCH.md

<interfaces>
<!-- Verified against contracts/AfKing.sol HEAD this session. The executor uses these directly. -->

Current Sub struct (AfKing.sol:80-88, doc :69-79) — ONE 32-byte slot, reached via `_subOf` at slot 1:
  offset 0   uint8  dailyQuantity        (0 = paused/never-subscribed)
  offset 1   bool   drainGameCreditFirst    <-- COLLAPSE into flags
  offset 2   bool   useTickets              <-- COLLAPSE into flags
  offset 3   uint32 lastSweptDay
  offset 7   uint32 paidThroughDay
  offset 11  uint8  reinvestPct
  offset 12  uint8  flags                (bit 0 = FLAG_WINDOW_PAID = 1, AfKing.sol:238)
  offsets 13..31 = 19 free bytes  → a 20-byte address does NOT fit standalone.
  Collapsing the two bools frees offsets to 21 ≥ 20 → `address fundingSource` fits in the SAME slot.

Existing flag idiom (already in source): `s.flags &= ~FLAG;` / `s.flags |= FLAG;` at :238, :392, :398, :581, :593, :603, :682.

Cross-contract surfaces (READ-ONLY — NOT modified this phase; verified present):
  DegenerusGame.sol:436  isOperatorApproved(address owner, address operator) view returns (bool)
                         → operatorApprovals[owner][operator]; owner = money-holder grants operator
  DegenerusGame.sol:426  setOperatorApproval(address operator, bool approved) — caller(owner) grants operator
  BurnieCoin.sol:456     burnForKeeper(address user, uint256 amount) onlyAfKing returns (uint256 burned)
                         → all-or-nothing; burns `user`'s wallet+pending; passing fundingSource needs ZERO BurnieCoin change
  ContractAddresses.sol  VAULT, SDGNRS, GAME, COIN, COINFLIP, AF_KING pins all present

Load-bearing line anchors in AfKing.sol (verified HEAD):
  :355-364  subscribe SUB-02 self-or-operator auth (the EXISTING subscriber->keeper-style consent at subscribe)
  :380-388  subscribe state writes (drainGameCreditFirst :381, useTickets :382)
  :391-399  subscribe SUB-01 pass-or-pay gate; burnForKeeper(subscriber, cost) at :396; revert BurnieChargeFailed :397
  :410-452  the four NotSubscribed-gated setters (template for setFundingSource):
            setDailyQuantity :410, setDrainGameCreditFirst :430, setMode :438, setReinvestPct :446
  :567-606  sweep day-31 renewal branch (paidThroughDay <= today); hasAnyLazyPass :578; burnForKeeper(player, extractCost) :587; auto-pause shortfall :588-600
  :610      EXISTING per-draw isOperatorApproved(player, address(this)) — LANDMINE B, leave untouched
  :646      read of drainGameCreditFirst (`if (!sub.drainGameCreditFirst)`)  <-- convert to flag-bit read
  :634      read of useTickets (`if (!sub.useTickets && ...)`)               <-- convert to flag-bit read
  :672      ETH draw skip-gate `if (_poolOf[player] < msgValue)`             <-- route read to resolved source
  :673      exemption `if (player == VAULT || player == SDGNRS)`             <-- LANDMINE A, stays keyed on `player`
  :694      CEI debit `_poolOf[player] -= msgValue`                          <-- route debit to resolved source
  SubscriptionUpdated event :159-165 — keep ABI stable; pass (s.flags & FLAG_x)!=0 as the bool args
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the BEFORE storage-layout baseline + re-grep-verify every cited anchor against LIVE source</name>
  <read_first>contracts/AfKing.sol; .planning/phases/319.1-impl-open-e-shared-funding-source-burnie-and-eth-pool/319.1-RESEARCH.md (§OPENE-01..04 + Common Pitfalls)</read_first>
  <action>Before ANY edit: (1) run `forge clean && forge build` then `forge inspect AfKing storage-layout` (the established 317-06/318-01 cadence; the artifact needs a full clean to emit the layout) and record the BEFORE per-field slot/offset table into the SUMMARY — this is the baseline the Wave-2 test-mirror re-derivation diffs against. (2) Re-grep-verify, against the current working-tree source, that each anchor in the `<interfaces>` block still resolves to the named construct: the burn sites at :396 and :587, the ETH read/debit at :672/:694, the exemption at :673, the per-draw check at :610, and the four setter templates at :410-452. If ANY line has drifted from RESEARCH, record the corrected line and proceed off the corrected anchor (executor reads source, not assumptions, per feedback_verify_call_graph_against_source). NO source mutation in this task.</action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge clean >/dev/null 2>&1 && forge build >/dev/null 2>&1 && forge inspect AfKing storage-layout 2>&1 | grep -v '^Warning\|nightly\|FOUNDRY_DISABLE' | grep -q 'fundingSource\|dailyQuantity' && echo LAYOUT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `forge inspect AfKing storage-layout` emits a layout table (after `forge clean && forge build`); the BEFORE field/offset table is recorded in the plan SUMMARY.
    - Every `<interfaces>` anchor is re-grep-confirmed against live source; any drift is documented with the corrected line.
    - `contracts/AfKing.sol` is byte-unchanged at the end of this task (`git diff --quiet contracts/AfKing.sol` exits 0).
  </acceptance_criteria>
  <done>The BEFORE storage layout is captured and all cited anchors are verified against HEAD; zero source mutation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Repack Sub (bools→flags) + add fundingSource field + the caller-scoped setFundingSource setter (OPENE-01)</name>
  <read_first>contracts/AfKing.sol (struct :80-88, setters :410-452, flag idiom sites, SubscriptionUpdated :159-165); 319.1-RESEARCH.md §OPENE-01</read_first>
  <behavior>
    - The repacked `Sub` is still ONE 32-byte slot; slot COUNT stays 5 (slots 0-4); `address fundingSource` lands in slot 1 with `_subOf`.
    - `drainGameCreditFirst` and `useTickets` become `flags` bit constants (e.g. FLAG_DRAIN_FIRST, FLAG_USE_TICKETS) alongside the existing FLAG_WINDOW_PAID; every prior struct-field read/write converts to the masked `&` / `|= / &= ~` idiom.
    - `setFundingSource(address source)` reverts NotSubscribed when `_subscriberIndex[msg.sender] == 0`, writes `_subOf[msg.sender].fundingSource = source`, and emits (a dedicated FundingSourceUpdated event — keep SubscriptionUpdated's ABI stable per RESEARCH Pitfall 5). Only msg.sender can set their own source.
    - `SubscriptionUpdated` is emitted everywhere it is today, with the two bool args sourced from `(s.flags & FLAG_x) != 0` — event ABI byte-unchanged.
    - Default `fundingSource` for a fresh subscribe is `address(0)` (= self).
  </behavior>
  <action>Edit the `Sub` struct: remove the two standalone `bool drainGameCreditFirst`/`bool useTickets` fields, append `address fundingSource`, and update the struct doc-comment (:69-79) to describe the new layout (comments describe what IS, never what changed, per feedback_no_history_in_comments). Add two `uint8 internal constant FLAG_*` bit constants next to FLAG_WINDOW_PAID at :238 (Claude's discretion on the exact bit indices). Convert every `s.drainGameCreditFirst` / `s.useTickets` read+write site to the flag-bit idiom: subscribe writes :381/:382, the setters setDrainGameCreditFirst :430-435 + setMode :438-443 (now masked set/clear), the sweep reads :646 + :634, and the six SubscriptionUpdated emit sites that pass these as bool args (:387/:418/:426/:434/:442/:451) — pass `(s.flags & FLAG_x) != 0`. Add a new `setFundingSource(address source)` external function mirroring the setReinvestPct :446 template (NotSubscribed guard, write, emit). Add a `FundingSourceUpdated(address indexed player, address source)` event. Do NOT yet wire the three routing points or the auth — those are Task 3 (kept in the same file/diff but logically separate so the diff reads cleanly). Maximal-packing per feedback_maximal_variable_packing; pre-launch redeploy-fresh means the layout break is free (no migration).</action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | grep -v '^Warning\|nightly\|FOUNDRY_DISABLE' | grep -qi 'error' && echo BUILD_FAIL || (grep -q 'function setFundingSource(address' contracts/AfKing.sol && grep -q 'address fundingSource' contracts/AfKing.sol && echo OK)</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` exits 0 (no compile error) with the repacked struct.
    - `grep -c 'bool drainGameCreditFirst\|bool useTickets' contracts/AfKing.sol` (filtered `grep -v '^[[:space:]]*//'`) returns 0 — the standalone bool struct fields are gone.
    - `contracts/AfKing.sol` contains `function setFundingSource(address` and `address fundingSource` and at least two new `FLAG_` constants beyond FLAG_WINDOW_PAID.
    - The `SubscriptionUpdated` event signature is byte-unchanged: `grep -q 'event SubscriptionUpdated(' contracts/AfKing.sol` and the param list still reads `bool drainGameCreditFirst, bool useTickets`.
    - A `forge inspect AfKing storage-layout` shows slot count unchanged (still slots 0-4; `fundingSource` resides in slot 1's struct, not a new top-level slot).
  </acceptance_criteria>
  <done>Sub is repacked to one slot with fundingSource appended, the bools live in flags, and the caller-scoped setFundingSource setter + FundingSourceUpdated event exist; the suite compiles.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Route the 3 spend points to fundingSource + gate the cross-account case on operator-approval at subscribe + renewal only (OPENE-02/03/04)</name>
  <read_first>contracts/AfKing.sol (subscribe :348-400, sweep day-31 :567-606, per-draw :610, ETH draw :643-695); 319.1-RESEARCH.md §OPENE-02/03/04 + Common Pitfalls 1/2/3</read_first>
  <behavior>
    - ETH draw: a once-per-iteration `address src = sub.fundingSource == address(0) ? player : sub.fundingSource;` is read at :672 (`_poolOf[src] < msgValue`) and debited at :694 (`_poolOf[src] -= msgValue`) — SAME src at both. The exemption at :673 still compares `player` (LANDMINE A).
    - subscribe burn :396 passes `(s.fundingSource == address(0) ? subscriber : s.fundingSource)`; sweep day-31 burn :587 passes the resolved source for `player`. All-or-nothing shortfall behavior (subscribe revert :397 / day-31 auto-pause :588-600) unchanged.
    - Cross-account auth: when `fundingSource != address(0)`, the keeper requires `isOperatorApproved(fundingSource, subscriber/player)` to honor the source, checked ONLY at subscribe (alongside :355-364) and at the day-31 renewal branch (:567-606). When `fundingSource == 0`, NO approval read happens (default-self short-circuit, Pitfall 3).
    - subscribe-time unapproved non-zero source: revert NotApproved (fail-fast, mirrors :362). renewal-time revoked source: fall back to self (draw _poolOf[player] / burn player) so the sub stays alive on the subscriber's own funds, degrading to the existing InsufficientPool/shortfall path if self has nothing — bounding post-revoke drain to <=1 renewal window.
    - The EXISTING per-draw :610 check is byte-unchanged (LANDMINE B); the new approval read is NOT in the per-iteration loop body.
  </behavior>
  <action>In `sweep()`: compute the resolved source ONCE per iteration before the funding-waterfall block and use it at the :672 read and :694 debit; leave the :673 exemption comparing `player` (LANDMINE A — do NOT change to src). In `subscribe()`: after the SUB-02 auth (:355-364) and before the pass-or-pay gate, add the cross-account gate — `if (fundingSourceArg != address(0) && fundingSourceArg != subscriber && !IGame(GAME).isOperatorApproved(fundingSourceArg, subscriber)) revert NotApproved();` (note: subscribe does not currently take a fundingSource param — per RESEARCH the setter is standalone, so subscribe sets fundingSource via the setter post-subscribe OR reads the already-stored `s.fundingSource`; honor the locked "standalone caller-scoped setter" design — the subscribe-time check reads `s.fundingSource` after the state writes). Route burnForKeeper at :396 to the resolved source. In the day-31 renewal branch (:567-606): before the burn at :587, re-verify `isOperatorApproved(s.fundingSource, player)` when `s.fundingSource != 0`; on failure fall back to self (treat the resolved source as `player`); route the :587 burn to the resolved source. Leave :610 untouched. Document the BURNIE blast-radius caveat (the operator approval also authorizes burning S's general-wallet BURNIE + pending coinflip — sharper than the pre-funded ETH escrow; `allowBurnieFunding[S][M]` is the named deferred alternative) in the setFundingSource and/or subscribe NatSpec, per OPENE-04 + the security_threat_model gate. Comments describe what IS.</action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | grep -v '^Warning\|nightly\|FOUNDRY_DISABLE' | grep -qi 'error' && echo BUILD_FAIL || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` exits 0.
    - LANDMINE A held: the line that compares against `ContractAddresses.VAULT`/`SDGNRS` (the former :673) still reads `player ==`, NOT `src ==`/`fundingSource ==` — assert `grep -n 'ContractAddresses.VAULT' contracts/AfKing.sol` shows the comparison keyed on `player`.
    - LANDMINE B held: the per-draw `isOperatorApproved(player, address(this))` check (the former :610) is byte-present and unchanged — `grep -q 'isOperatorApproved(player, address(this))' contracts/AfKing.sol`.
    - The new cross-account check reads `isOperatorApproved(<source>, <subscriber/player>)` (source-as-owner direction) and appears only in subscribe + the day-31 renewal branch — NOT inside the per-iteration loop body next to the former :610 (verify by inspecting the two call contexts).
    - Both `burnForKeeper` call sites pass a resolved-source expression (the `fundingSource == 0 ? self : fundingSource` form), not the bare `subscriber`/`player`.
    - The BURNIE blast-radius caveat text is present in the contract NatSpec (`grep -qi 'allowBurnieFunding\|blast' contracts/AfKing.sol` or equivalent caveat prose).
  </acceptance_criteria>
  <done>All three spend points route through the resolved fundingSource, the cross-account case is gated at subscribe + renewal only, both landmines are preserved, and the BURNIE caveat is documented; the suite compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: BLOCKING USER-APPROVAL gate — review the single batched contracts/AfKing.sol OPEN-E diff</name>
  <what-built>The complete OPEN-E `contracts/AfKing.sol` diff: Sub repacked (bools→flags) with `address fundingSource` appended in one slot, the caller-scoped `setFundingSource` setter + `FundingSourceUpdated` event, the ETH draw + both burnForKeeper sites routed to the resolved fundingSource, and the cross-account operator-approval gate at subscribe + day-31 renewal only. Both landmines (Vault/sDGNRS exemption keyed on `player`; the :610 per-draw check untouched) preserved. `forge build` PASSES. This is the SECOND batched contract diff on top of Phase 317 — NOTHING is pre-approved.</what-built>
  <how-to-verify>
    1. Review the full contract diff: `cd /home/zak/Dev/PurgeGame/degenerus-audit && git diff contracts/AfKing.sol`
    2. Confirm `forge build` passes: `forge build 2>&1 | grep -i error` returns nothing.
    3. Confirm the storage layout: `forge inspect AfKing storage-layout` — slot count unchanged (slots 0-4), `fundingSource` in the Sub slot.
    4. Spot-check LANDMINE A (the VAULT/SDGNRS exemption still keys on `player`) and LANDMINE B (the per-draw `isOperatorApproved(player, address(this))` at the former :610 is unchanged).
    5. Confirm the BURNIE blast-radius caveat NatSpec is present.
    6. Decide whether the diff is approved for commit. Per project policy the diff is NOT committed or pushed until you approve.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the single batched commit of `contracts/AfKing.sol`, or describe required changes.</resume-signal>
  <action>HALT for blocking USER review of the complete `contracts/AfKing.sol` OPEN-E diff before any commit. Present the full `git diff contracts/AfKing.sol`, the `forge build` result, the `forge inspect AfKing storage-layout` before/after, and the two landmine spot-checks; do NOT commit or push until the user types "approved". NOTHING is pre-approved (feedback_never_preapprove_contracts).</action>
  <verify>
    <automated>forge build 2>&1 | grep -v '^Warning\|nightly\|FOUNDRY_DISABLE' | grep -qi 'error' && echo BUILD_FAIL || echo READY_FOR_REVIEW</automated>
  </verify>
  <done>The user has reviewed the single batched diff and explicitly typed "approved" (or returned change requests, in which case loop back to Task 3).</done>
</task>

<task type="auto">
  <name>Task 5: Commit the USER-APPROVED batched contract diff (only after approval)</name>
  <read_first>contracts/AfKing.sol</read_first>
  <action>ONLY after the Task 4 checkpoint returns "approved": commit the single `contracts/AfKing.sol` diff. The repo's PreToolUse hook blocks commits while `contracts/*.sol` is dirty — set `CONTRACTS_COMMIT_APPROVED=1` for this one approved commit (per feedback_contract_commit_guard_hook). Commit message: `feat(319.1-01): OPEN-E shared funding source — Sub.fundingSource + operator-approval routing [OPENE-01..04]`. Do NOT push (feedback_manual_review_before_push — pushing is a separate user action). Do NOT bundle any test or planning file into this commit; those are Wave-2 / deferred AGENT-COMMITTED work.</action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && git log -1 --name-only --pretty=format:'%s' | grep -q 'OPENE-01' && git show --stat HEAD --name-only | grep -q 'contracts/AfKing.sol' && echo COMMITTED</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one commit lands containing only `contracts/AfKing.sol` with the OPENE-01..04 message.
    - `git status` shows `contracts/AfKing.sol` clean (committed).
    - Nothing pushed (no `git push` run).
  </acceptance_criteria>
  <done>The single USER-APPROVED `contracts/AfKing.sol` diff is committed (not pushed); the working tree's contract is clean for Wave 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subscriber M → keeper (sweep) | M's stored `fundingSource` redirects keeper spends; M is untrusted, can set any address as source |
| funding source S → subscriber M | S's ETH `_poolOf` + general-wallet BURNIE are spendable on M's behalf iff S approved M |
| keeper → DegenerusGame.operatorApprovals | the keeper READS the game's approval mapping; does not write it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-319.1-01 | Spoofing | sweep :673 exemption (LANDMINE A) | mitigate | exemption compares the iterated un-spoofable `player` to VAULT/SDGNRS, NEVER the settable `fundingSource`; a NORMAL sub with `fundingSource=VAULT` is still cancellable. Verified in Task 3 acceptance. |
| T-319.1-02 | Elevation of Privilege | M points fundingSource at non-consenting S | mitigate | `isOperatorApproved(S, M)` (S=owner grants M) required to honor a non-zero source; checked at subscribe + day-31 renewal; subscribe-time unapproved → revert NotApproved. |
| T-319.1-03 | Elevation of Privilege | post-revoke continued drain | accept (bounded) | auth re-checked only at renewal → ≤1 renewal window (30d) of residual drain; the ETH economic bound is immediate (S calls `withdraw()` to empty `_poolOf[S]`). Locked tradeoff per OPENE-04 (per-draw checks rejected for gas). |
| T-319.1-04 | Information/Asset exposure | BURNIE blast-radius | accept (documented) | the same approval authorizes burning S's general-wallet BURNIE + pending coinflip (sharper than ETH escrow); caveat documented in NatSpec; `allowBurnieFunding[S][M]` named as the deferred tighter alternative. |
| T-319.1-05 | Denial of Service | default-self regression | mitigate | `fundingSource == 0` short-circuits the approval read entirely; self subs (incl. the SUB-09 Vault/sDGNRS protocol subs) never need a self-approval. |
| T-319.1-06 | Denial of Service / gas | per-draw extra STATICCALL (LANDMINE B) | mitigate | the new approval read lives only in subscribe + renewal; the existing per-draw :610 check is left untouched and NOT joined by a second per-draw read; ETH-draw routing is a same-key SLOAD (gas-neutral). |
| T-319.1-07 | Tampering | state integrity / CEI | mitigate | all routing edits sit inside the existing strict-CEI sweep; the :694 debit stays before the batched :738 call; the keeper is never a payee → no reentrancy guard needed. |

No package-manager installs in this phase → no T-319.1-SC supply-chain row (no npm/pip/cargo; RESEARCH §Package Legitimacy Audit = N/A).
</threat_model>

<verification>
- `forge build` PASSES on the patched tree (SC-relevant for the downstream Wave-2 test run).
- `forge inspect AfKing storage-layout`: slot COUNT unchanged (slots 0-4); `fundingSource` resides in the Sub slot; bools gone as standalone fields.
- LANDMINE A: the VAULT/SDGNRS exemption keys on `player`.
- LANDMINE B: the per-draw `isOperatorApproved(player, address(this))` is byte-unchanged.
- The new cross-account `isOperatorApproved(<source>, <subscriber>)` appears ONLY at subscribe + the day-31 renewal branch.
- Both burnForKeeper sites + the ETH read/debit route to the resolved fundingSource (self when 0).
- BURNIE blast-radius caveat present in NatSpec.
- The single contract commit lands only after the blocking USER-APPROVAL checkpoint; nothing pushed.
</verification>

<success_criteria>
- OPENE-01: `Sub.fundingSource` field + caller-scoped `setFundingSource` setter; the two bools collapsed into `flags`; slot count unchanged.
- OPENE-02: ETH draw reads/debits `_poolOf[fundingSource]` (self when 0), gas-neutral; the Vault/sDGNRS exemption stays subscriber-keyed.
- OPENE-03: both `burnForKeeper` sites (:396, :587) route to `fundingSource`; all-or-nothing preserved.
- OPENE-04: operator-approval auth at subscribe + renewal only, never per-draw; revoke bounds drain ≤1 window; BURNIE caveat documented.
- The entire edit is ONE batched `contracts/AfKing.sol` diff behind ONE blocking USER-APPROVAL checkpoint; nothing pre-approved, nothing pushed.
- `forge build` PASSES.
</success_criteria>

<output>
Create `.planning/phases/319.1-impl-open-e-shared-funding-source-burnie-and-eth-pool/319.1-01-SUMMARY.md` when done — include the BEFORE storage-layout table (for the Wave-2 test-mirror re-derivation) and the AFTER layout, the per-OPENE-requirement edit summary, and the approved commit SHA.
</output>
