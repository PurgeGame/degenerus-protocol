---
phase: 319.1-impl-open-e-shared-funding-source-burnie-and-eth-pool
plan: 02
type: execute
wave: 2
depends_on: ["319.1-01"]
files_modified: [test/fuzz/AfKingFundingWaterfall.t.sol, test/fuzz/AfKingConcurrency.t.sol, test/fuzz/AfKingSubscription.t.sol]
autonomous: true
requirements: [OPENE-01, OPENE-02, OPENE-03, OPENE-04]
user_setup: []

must_haves:
  truths:
    - "The Sub packed-offset constants in every AfKing test file (OFF_DAILY/OFF_LASTSWEPT/OFF_PAIDTHROUGH/OFF_FLAGS) are re-derived from the post-repack `forge inspect AfKing storage-layout` and match the live layout — no silent vm.store mis-targeting"
    - "The full AfKing Foundry suite recompiles and PASSES with no slot-index drift after the OPEN-E repack (OPENE-01 slot-derivation facet)"
    - "Behavioral coverage proves OPENE-02/03: a funded source S covers M's ETH draw + BURNIE charge; default-self (fundingSource==0) is byte-equivalent to pre-OPEN-E"
    - "Behavioral coverage proves OPENE-04 + LANDMINE A: a NORMAL sub setting fundingSource=VAULT does NOT inherit the never-cancel exemption (it is still cancelled on a funding skip); an unapproved cross-account source is refused"
  artifacts:
    - path: "test/fuzz/AfKingFundingWaterfall.t.sol"
      provides: "re-derived Sub offsets + OPEN-E funding-source waterfall + exemption-spoof regression coverage"
      contains: "fundingSource"
    - path: "test/fuzz/AfKingConcurrency.t.sol"
      provides: "re-derived Sub offsets (OFF_LASTSWEPT/OFF_PAIDTHROUGH/OFF_FLAGS) matching the post-repack layout"
      contains: "OFF_FLAGS"
    - path: "test/fuzz/AfKingSubscription.t.sol"
      provides: "re-derived Sub offsets + the cross-account operator-approval auth coverage at subscribe + renewal"
      contains: "OFF_FLAGS"
  key_links:
    - from: "test Sub offset constants"
      to: "post-repack forge inspect AfKing storage-layout"
      via: "re-derived OFF_* byte offsets"
      pattern: "OFF_FLAGS"
    - from: "OPEN-E behavioral tests"
      to: "AfKing.setFundingSource / sweep routing / subscribe auth"
      via: "operator-approval-gated cross-account draw + exemption-spoof regression"
      pattern: "fundingSource"
---

<objective>
Re-derive the `Sub` packed-byte offset constants that the AfKing Foundry tests hardcode for `vm.store` manipulation — they DRIFT when the OPEN-E repack collapses the two bools into `flags` and inserts `fundingSource` — and add behavioral coverage proving the OPEN-E routing + the two security landmines. This runs AFTER the Wave-1 contract diff is approved + committed.

Purpose: The offset constants `OFF_LASTSWEPT=3`/`OFF_PAIDTHROUGH=7`/`OFF_FLAGS=12` were computed from the OLD layout; left stale, the `vm.store`-based tests read/write the wrong bytes and silently mis-test (RESEARCH Pitfall 4). Three test files carry these constants. The new behavioral tests lock the OPEN-E spec (default-self equivalence, cross-account funding, the Vault/sDGNRS exemption-spoof refusal).

Output: Three updated AfKing test files (AGENT-COMMITTED — test/ commits autonomously per feedback_no_contract_commits) with re-derived offsets + OPEN-E coverage; the full AfKing suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/319.1-impl-open-e-shared-funding-source-burnie-and-eth-pool/319.1-RESEARCH.md
@.planning/phases/319.1-impl-open-e-shared-funding-source-burnie-and-eth-pool/319.1-01-SUMMARY.md

<interfaces>
<!-- The three test files carrying drifting Sub-offset constants (grep-verified this session). -->

test/fuzz/AfKingFundingWaterfall.t.sol:42-48
  SUBOF_SLOT=1, OFF_LASTSWEPT=3, OFF_PAIDTHROUGH=7, OFF_FLAGS=12, FLAG_WINDOW_PAID=1
  Usage: line 354 builds a uint32 mask at OFF_LASTSWEPT/OFF_PAIDTHROUGH*8; line 356 writes paidThroughDay;
         lines 186-187 read subscriptionOf(...).flags & FLAG_WINDOW_PAID

test/fuzz/AfKingConcurrency.t.sol:41-49
  SUBOF_SLOT=1, OFF_DAILY=0, OFF_LASTSWEPT=3, OFF_PAIDTHROUGH=7, OFF_FLAGS=12, FLAG_WINDOW_PAID=1
  Usage: line 441 reads lastSweptDay >> OFF_LASTSWEPT*8; lines 455-458 mask+write paidThroughDay+flags;
         lines 368-391 read subscriptionOf(...).dailyQuantity/.flags/.paidThroughDay

test/fuzz/AfKingSubscription.t.sol:36-41
  SUBOF_SLOT=1, OFF_DAILY=0, OFF_LASTSWEPT=3, OFF_PAIDTHROUGH=7, OFF_FLAGS=12

AfKing external surface the tests drive (Wave-1 additions, from 319.1-01):
  setFundingSource(address source)         — caller-scoped; reverts NotSubscribed if not in set
  subscriptionOf(address) returns (Sub)    — Sub now carries `address fundingSource`
  FundingSourceUpdated(address indexed player, address source) — emitted by the setter
  subscribe(...)                            — cross-account source refused (revert NotApproved) when unapproved+non-zero
  sweep(uint256)                            — ETH draw + burnForKeeper route to the resolved source

Game / coin surfaces for cross-account setup (read-only, present):
  DegenerusGame.setOperatorApproval(operator, approved) — S (owner) grants M
  ContractAddresses.VAULT / SDGNRS — the exemption-keyed pinned identities

Note: the bools are now flag bits. Tests reading drainGameCreditFirst/useTickets must read the
raw flag bits via `(subscriptionOf(...).flags & FLAG_x) != 0` — the typed `.drainGameCreditFirst` /
`.useTickets` struct accessors are GONE after the repack (the compiler no longer exposes them as
named fields). Convert any such read to the flag-bit form.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive the Sub offsets from the post-repack layout and sync all three AfKing test files</name>
  <read_first>test/fuzz/AfKingFundingWaterfall.t.sol; test/fuzz/AfKingConcurrency.t.sol; test/fuzz/AfKingSubscription.t.sol; .planning/phases/319.1-impl-open-e-shared-funding-source-burnie-and-eth-pool/319.1-01-SUMMARY.md (AFTER layout table)</read_first>
  <action>Run `forge inspect AfKing storage-layout` against the committed Wave-1 tree to read the AUTHORITATIVE post-repack byte offsets for `dailyQuantity`, `lastSweptDay`, `paidThroughDay`, `reinvestPct`, `flags`, and `fundingSource` within the Sub slot (do NOT trust RESEARCH's illustrative offsets — re-derive). Update the `OFF_DAILY`/`OFF_LASTSWEPT`/`OFF_PAIDTHROUGH`/`OFF_FLAGS` constants in all three files to the inspected values, and update the trailing byte-range comments to match. Add `OFF_FUNDING_SOURCE` (and `FLAG_DRAIN_FIRST`/`FLAG_USE_TICKETS` mirror constants if a test needs to set those bits via vm.store) where a test will exercise the new field. Audit each file's `vm.store` mask/shift expressions (e.g. AfKingConcurrency :455-458, AfKingFundingWaterfall :354-356) to confirm they reference the updated constants — fix any that hardcode a literal offset. Where a test reads `subscriptionOf(...).drainGameCreditFirst`/`.useTickets`, convert it to the flag-bit form `(subscriptionOf(...).flags & FLAG_x) != 0` since the typed accessors are gone after the repack.</action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | grep -v '^Warning\|nightly\|FOUNDRY_DISABLE' | grep -qi 'error' && echo BUILD_FAIL || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` (incl. tests) exits 0 — the three files compile against the repacked Sub ABI.
    - The `OFF_*` constants in all three files equal the `forge inspect AfKing storage-layout` byte offsets (record the inspected table in the SUMMARY and assert equality field-by-field).
    - No test references the now-removed `.drainGameCreditFirst`/`.useTickets` typed struct accessors (`grep -n '\.drainGameCreditFirst\|\.useTickets' test/fuzz/AfKing*.t.sol` shows only flag-bit forms or zero hits).
  </acceptance_criteria>
  <done>All three AfKing test files compile with offsets matching the live post-repack layout; no stale typed-accessor reads remain.</done>
</task>

<task type="auto">
  <name>Task 2: Add OPEN-E behavioral coverage — default-self equivalence, cross-account funding, exemption-spoof refusal, auth gating</name>
  <read_first>test/fuzz/AfKingFundingWaterfall.t.sol; test/fuzz/AfKingSubscription.t.sol; contracts/AfKing.sol (subscribe + sweep routing); .planning/phases/319.1-impl-open-e-shared-funding-source-burnie-and-eth-pool/319.1-RESEARCH.md §OPENE-02/03/04</read_first>
  <action>Extend the existing AfKing test contracts (do not create a new fixture — reuse the 318-01-repaired DeployProtocol) with OPEN-E cases. In AfKingFundingWaterfall.t.sol: (1) default-self — a sub with `fundingSource==0` draws/burns exactly as today (regression that pre-OPEN-E paths are byte-equivalent); (2) cross-account ETH — S deposits to `_poolOf[S]`, S calls `setOperatorApproval(M, true)`, M sets `fundingSource=S`, the sweep draws/debits `_poolOf[S]` not `_poolOf[M]`; (3) LANDMINE A exemption-spoof — a NORMAL sub sets `fundingSource=VAULT`, hits a funding skip, and is STILL cancelled via swap-pop (the exemption did NOT transfer); the real VAULT/SDGNRS self-subs stay exempt. In AfKingSubscription.t.sol: (4) cross-account auth — subscribe/configure with an UNAPPROVED non-zero source reverts NotApproved (or is refused at subscribe); after S approves M it is honored; (5) renewal revoke — S revokes, and at the day-31 renewal branch the source falls back to self (draw `_poolOf[M]`/burn M), bounding drain to <=1 window. In AfKingFundingWaterfall.t.sol: (6) cross-account BURNIE — both `burnForKeeper` sites (subscribe + day-31) burn from S's balance when `fundingSource=S` and approved; all-or-nothing shortfall on S falls through (subscribe revert / day-31 auto-pause). Keep assertions specific (exact balances, exact set membership, exact revert selectors).</action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge test --match-contract 'AfKingFundingWaterfall|AfKingSubscription' 2>&1 | grep -v '^Warning\|nightly\|FOUNDRY_DISABLE' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-contract AfKingFundingWaterfall` exits 0 (all tests pass), including the new default-self, cross-account ETH/BURNIE, and LANDMINE-A exemption-spoof cases.
    - `forge test --match-contract AfKingSubscription` exits 0, including the unapproved-source-refused + renewal-revoke-fallback cases.
    - The exemption-spoof test asserts a NORMAL sub with `fundingSource=VAULT` IS cancelled (`subscriptionOf(victim).dailyQuantity == 0` + removed from the set) on a funding skip — proving LANDMINE A.
    - New tests reference `setFundingSource` / `setOperatorApproval` and assert balance movement on the SOURCE pool, not the subscriber pool.
  </acceptance_criteria>
  <done>OPEN-E behavioral coverage passes — default-self equivalence, cross-account ETH+BURNIE funding, the exemption-spoof refusal (LANDMINE A), and the subscribe/renewal auth gating are all proven.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full AfKing-touching suite, confirm no slot drift, and AGENT-COMMIT the test sync</name>
  <read_first>test/fuzz/AfKingFundingWaterfall.t.sol; test/fuzz/AfKingConcurrency.t.sol; test/fuzz/AfKingSubscription.t.sol</read_first>
  <action>Run the full AfKing-related suite (`AfKingConcurrency`, `AfKingFundingWaterfall`, `AfKingSubscription`, plus the crank/RNG-freeze suites that touch Sub via subscriptionOf: `CrankNonBrick`, `CrankFaucetResistance`, `RngFreezeAndRemovalProofs`) to confirm the repack introduced NO slot-index drift regression — the pre-OPEN-E pass counts for the unrelated families must hold (compare against the 318 baseline recorded in STATE.md: the suite carried pre-existing baseline failures unrelated to AfKing; the OPEN-E change must not ADD any new failure). Then AGENT-COMMIT the three modified test files (no contract files — those were committed in Wave 1). The contract commit guard hook permits test/.planning commits while contracts/*.sol is clean; if it is dirty, the Wave-1 commit did not complete — stop and surface that. Commit message: `test(319.1-02): OPEN-E test-mirror offset sync + funding-source coverage [OPENE-01..04]`.</action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge test --match-contract 'AfKingConcurrency|AfKingFundingWaterfall|AfKingSubscription|CrankNonBrick|RngFreezeAndRemovalProofs' 2>&1 | grep -v '^Warning\|nightly\|FOUNDRY_DISABLE' | grep -E 'Suite result|FAIL' | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - Every AfKing suite (`AfKingConcurrency`, `AfKingFundingWaterfall`, `AfKingSubscription`) reports `Suite result: ok` with 0 failures.
    - `CrankNonBrick` + `RngFreezeAndRemovalProofs` show no NEW failures vs the 318 baseline (the repack added no slot-drift regression to the Sub-reading suites).
    - The three test files are committed in one AGENT-COMMITTED commit with the OPENE-01..04 message; no `contracts/*.sol` is in the commit; nothing pushed.
  </acceptance_criteria>
  <done>The full AfKing-touching suite is green with no new slot-drift failures, and the test-mirror sync + OPEN-E coverage is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → AfKing storage | the tests use `vm.store` with hardcoded byte offsets; a stale offset writes the WRONG byte and silently mis-tests |
| OPEN-E behavioral surface | the tests are the verification that the Wave-1 contract correctly gates cross-account funding + preserves the exemption landmine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-319.1-T1 | Tampering (test fidelity) | stale `vm.store` byte offsets after the repack | mitigate | offsets re-derived from `forge inspect AfKing storage-layout`; field-by-field equality asserted in Task 1; RESEARCH Pitfall 4. |
| T-319.1-T2 | Spoofing (regression coverage) | the Vault/sDGNRS exemption-spoof vector | mitigate | a dedicated test sets `fundingSource=VAULT` on a NORMAL sub and asserts it is STILL cancelled (LANDMINE A behavioral proof, Task 2). |
| T-319.1-T3 | Elevation of Privilege (regression coverage) | unapproved cross-account funding | mitigate | tests assert an unapproved non-zero source reverts NotApproved at subscribe and falls back to self at renewal-revoke (≤1 window bound). |
| T-319.1-T4 | Tampering (no contract mutation) | test plan must not edit contracts/*.sol | mitigate | files_modified is test/ only; Task 3 asserts no `contracts/*.sol` in the commit and that the Wave-1 contract is clean. |

No package-manager installs in this phase → no supply-chain (SC) row (no npm/pip/cargo; RESEARCH §Package Legitimacy Audit = N/A).
</threat_model>

<verification>
- `forge inspect AfKing storage-layout` re-run on the committed tree; offsets match the test constants field-by-field.
- All three AfKing suites green; `CrankNonBrick`/`RngFreezeAndRemovalProofs` show no new slot-drift failures vs the 318 baseline.
- LANDMINE A proven behaviorally: a `fundingSource=VAULT` NORMAL sub is still cancelled on a funding skip.
- OPEN-E cross-account ETH + BURNIE funding proven; default-self regression holds.
- Auth gating proven: unapproved source refused; renewal-revoke falls back to self.
- Test commit is AGENT-COMMITTED, test-only, not pushed.
</verification>

<success_criteria>
- OPENE-01 (slot facet): test-mirror offsets re-derived from the live layout; suite recompiles green; no slot drift.
- OPENE-02/03: cross-account ETH draw + both BURNIE charge sites proven against the source; default-self byte-equivalence proven.
- OPENE-04 + LANDMINE A: operator-approval gating (subscribe + renewal) and the Vault/sDGNRS exemption-spoof refusal proven.
- The full AfKing-touching suite passes with zero NEW failures; tests AGENT-COMMITTED.
</success_criteria>

<output>
Create `.planning/phases/319.1-impl-open-e-shared-funding-source-burnie-and-eth-pool/319.1-02-SUMMARY.md` when done — include the re-derived offset table (vs the Wave-1 AFTER layout), the new-test inventory mapped to OPENE-02/03/04 + the two landmines, the suite pass counts vs the 318 baseline, and the test commit SHA.
</output>
