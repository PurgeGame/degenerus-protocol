---
phase: 332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fuzz/RngLockDeterminism.t.sol
autonomous: true
requirements: [TST-01]
must_haves:
  truths:
    - "The router advance-consume reads only FROZEN VRF-window state even when fired same-tx as autoBuy/autoOpen (totalFlipReversals byte-identity)"
    - "autoBuy during rngLock is SAFE (queues pre-entropy, never aborts) — fuzz proves byte-identical VRF output with a doWork/autoBuy perturbation mid-window"
    - "autoOpen during rngLock is a NO-OP (returns 0, opens nothing) and leaves NO marooned boxes — the SAME boxes open with the cursor intact after the word lands"
    - "The freeze-fuzz is non-vacuous — the perturbation actually moves totalFlipReversals before the assertion"
  artifacts:
    - path: "test/fuzz/RngLockDeterminism.t.sol"
      provides: "v49 router same-tx freeze perturbation classes + the autoBuy-safe / autoOpen-blocked-no-maroon fuzz functions"
      contains: "N_PERTURB_ACTIONS = 11"
  key_links:
    - from: "test/fuzz/RngLockDeterminism.t.sol::_perturb"
      to: "contracts/AfKing.sol::doWork / autoBuy"
      via: "vm.prank(actor); try IAfKing(AF_KING).doWork() {} catch"
      pattern: "doWork\\(\\)|autoBuy\\("
    - from: "test/fuzz/RngLockDeterminism.t.sol (no-maroon assert)"
      to: "contracts/DegenerusGame.sol::boxesPending / autoOpen entry-gate"
      via: "boxesPending()==false during lock + autoOpen(N)==0 + same boxes open post-unlock"
      pattern: "boxesPending|autoOpen"
---

<objective>
Prove the v49 unified keeper-router composition preserves the VRF-freeze invariant (ADV-04 /
`v45-vrf-freeze-invariant`) EMPIRICALLY by extending the v43 `RngLockDeterminism.t.sol` 6-phase
byte-identity harness with the new router same-tx perturbation: the advance-consume
(`cw += totalFlipReversals` inside the daily drain) reads only frozen state even when `doWork`/
`autoBuy`/`autoOpen` fire in the same locked window. ADDS the redesign's Q4 cases (TST-01):
autoBuy-during-rngLock SAFE, autoOpen-blocked-during-rngLock + no-marooned-boxes.

Purpose: TST-01 is one of the two load-bearing SECURITY proofs of the phase (the other is TST-02
no-stacking). Security/RNG-non-manipulability is the HARD FLOOR — this proof must never be vacuous.
Output: an extended `test/fuzz/RngLockDeterminism.t.sol` (router perturbation classes + 3 new fuzz
functions), green under the default profile and the gated `FOUNDRY_PROFILE=deep` deep freeze proof.

POSTURE (binding): `test/` + `.planning/` ONLY. ZERO `contracts/*.sol` (mainnet) mutation — the
audit subject is FROZEN at the committed v49 source (`63bc16ca` + the 331 GAS constants). All
contract `file:line` below are READ-ONLY anchors. If this proof surfaces a CONTRACT defect, STOP and
surface it — do NOT patch a mainnet contract under a TST phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg/332-RESEARCH.md
@.planning/phases/332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg/332-PATTERNS.md

<interfaces>
<!-- READ-ONLY contract anchors (FROZEN subject). Executor uses these directly, no exploration. -->

contracts/modules/DegenerusGameAdvanceModule.sol — the FROZEN advance-consume read (TST-01 target):
  :154  function advanceGame() external returns (uint8 mult)   // mult=1 default
  :254-259  uint256 cw = rngWordCurrent; if (cw == 0) revert RngNotReady();
            cw += totalFlipReversals;   // <-- THE FROZEN READ (ADV-04). Inside the daily drain.
            _finalizeLootboxRng(cw);
  :1822  uint256 nudges = totalFlipReversals;  (consume-and-zero on the drain path)

contracts/DegenerusGame.sol — the no-maroon / autoOpen-blocked surface:
  :1655  function boxesPending() external view returns (bool)
  :1656     if (rngLockedFlag) return false;   // rngLock-aware (RD-3)
  :1687  function autoOpen(uint256 maxCount) external returns (uint256 opened)
  :1692     if (rngLockedFlag || _livenessTriggered()) return 0;   // ENTRY-GATE (RD-5): NO-OP, never reverts

contracts/AfKing.sol — the perturbation drivers:
  :883  function doWork() external                 // one-category router
  :923  function autoBuy(uint256 count) external   // standalone UNREWARDED escape
  :929  function autoOpen(uint256 count) external  // standalone UNREWARDED escape
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add router perturbation classes to the _perturb library</name>

  <files>test/fuzz/RngLockDeterminism.t.sol</files>
  <read_first>
    - test/fuzz/RngLockDeterminism.t.sol (the file being modified — read the full `_perturb`
      library `:150-213`, `N_PERTURB_ACTIONS = 9` `:150`, the `seed % N_PERTURB_ACTIONS` dispatch
      `:153`, existing classes 0-8, and the `vm.prank(actor); try …() {} catch { return; }` idiom
      at cls 0 `:165` / cls 1 `:173`)
    - .planning/phases/332-.../332-PATTERNS.md (the "Perturbation action library — the EXTENSION
      SITE" block, lines 44-50)
    - .planning/phases/332-.../332-RESEARCH.md (Pitfall 3: autoOpen during rngLock NO-OP not revert;
      Pitfall 4: the frozen read is `cw += totalFlipReversals`)
    - contracts/AfKing.sol:883 (doWork), :923 (autoBuy escape) — READ-ONLY driver anchors
  </read_first>
  <behavior>
    - Test: a `doWork()` perturbation class (new cls 9) fires inside the locked window as
      `vm.prank(actor); try IAfKing(AF_KING).doWork() {} catch { return; }` and does NOT alter the
      VRF-derived advance output (byte-identity holds — proven by the existing 6-phase template that
      calls `_perturb`).
    - Test: an `autoBuy(0)` perturbation class (new cls 10) fires inside the locked window safely
      (queues pre-entropy, does not abort the freeze) and does NOT alter the VRF-derived output.
    - Edge: `N_PERTURB_ACTIONS` is bumped from 9 to 11 so the existing fuzz functions exercise the
      two new classes via `seed % N_PERTURB_ACTIONS` with no off-by-one (classes 0..10 all reachable).
  </behavior>
  <action>
    Extend the `_perturb` action library in `test/fuzz/RngLockDeterminism.t.sol`: append cls 9
    (`vm.prank(actor); try IAfKing(AF_KING).doWork() {} catch { return; }`) and cls 10
    (`vm.prank(actor); try IAfKing(AF_KING).autoBuy(0) {} catch { return; }`) to the existing
    `else if (cls == 8)` chain, and bump `N_PERTURB_ACTIONS` from 9 to 11. Use the existing
    `IAfKing`/`AF_KING` import surface if present; otherwise add the minimal interface declaration for
    `doWork()` / `autoBuy(uint256)` mirroring the `IAfKing` shape already used elsewhere in the test
    tree (do NOT redeclare a conflicting symbol). Do NOT add an autoOpen-during-lock perturbation
    that expects a revert — autoOpen NO-OPs during rngLock (entry-gate `DegenerusGame.sol:1692`); a
    no-op perturbation is fine but must not assert a revert. Leave `_perturbAdminOnly` (:215)
    untouched.
  </action>
  <verify>
    <automated>forge test --match-contract RngLockDeterminism 2>&1 | tail -20</automated>
  </verify>
  <done>`N_PERTURB_ACTIONS == 11`; the existing RngLockDeterminism fuzz functions pass with the two
  new perturbation classes reachable; no new red introduced in the suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the autoBuy-safe / autoOpen-blocked-no-maroon / non-vacuity fuzz functions</name>

  <files>test/fuzz/RngLockDeterminism.t.sol</files>
  <read_first>
    - test/fuzz/RngLockDeterminism.t.sol (the 6-phase helpers: `_snapshotPreLock` :130,
      `_revertToPreLock` :134, `_advanceToVrfRequestBoundary` :105, `_deliverMockVrf` :115,
      `_readRngWordCurrent` :86, `_assertVrfOutputByteIdentity` :138 — copy the CALL SHAPE, do not
      re-implement)
    - .planning/phases/332-.../332-PATTERNS.md (the "Non-vacuity guard for the totalFlipReversals
      class" + "no-marooned-boxes / autoOpen-blocked surface" blocks, lines 52-54)
    - contracts/DegenerusGame.sol:1655 (boxesPending rngLock-aware), :1692 (autoOpen entry-gate
      returns 0 during lock) — READ-ONLY anchors
    - contracts/modules/DegenerusGameAdvanceModule.sol:254-259 (the `cw += totalFlipReversals`
      frozen consume) — READ-ONLY anchor
  </read_first>
  <behavior>
    - Test (autoBuy-during-lock SAFE + frozen-consume byte-identity, non-vacuous): snapshot → advance
      to the VRF boundary so `rngLocked()` true → perturb with a flip-reversal-bearing action that
      MOVES `totalFlipReversals` AND fires `doWork`/`autoBuy` in the locked window → assert (a) the
      perturbation actually changed `totalFlipReversals` pre-revert (NON-VACUITY: `assertGt`/
      `assertTrue` the value moved), then deliver the SAME word + digest the VRF-derived output →
      revert → re-advance → re-deliver the SAME word WITHOUT the perturbation → digest baseline →
      `_assertVrfOutputByteIdentity(perturbed, baseline, ...)`.
    - Test (autoOpen-blocked-during-lock, no revert): during rngLock assert `boxesPending() == false`
      and `autoOpen(N) == 0` opens nothing — NO `expectRevert`.
    - Test (no-marooned-boxes): with boxes queued pre-lock, after the word lands and lock clears,
      the SAME boxes open with the cursor intact (count opened post-unlock == the pre-lock pending
      count; the entry-gate makes the loop body non-reverting so no tail is stranded).
  </behavior>
  <action>
    Add three new fuzz/test functions to `test/fuzz/RngLockDeterminism.t.sol` reusing the 6-phase
    template helpers. (1) `testFuzz_RngLockDeterminism_AutoBuyDuringLockSafe(uint256 seed)` — the
    byte-identity proof with a router same-tx perturbation; include a non-vacuity assertion that
    `totalFlipReversals` (read via the existing slot/getter the harness already uses, or
    `IGame.totalFlipReversals()` view if exposed) actually moved between request boundary and consume
    (per Pitfall 4 — otherwise the proof is vacuous). (2)
    `testAutoOpenBlockedDuringRngLockNoOps()` — asserts `boxesPending()==false` and `autoOpen(N)==0`
    during lock with NO revert. (3) `testAutoOpenNoMaroonedBoxesAfterUnlock()` — queues boxes, locks,
    confirms zero open during lock, delivers the word, then opens the SAME boxes post-unlock with the
    cursor intact. DO NOT alter `testFuzz_RngLockDeterminism_StakedStonkRedemption` (:1263) — it is
    v48-baseline red A7 (`vm.assume` over-rejection) and MUST carry forward UNCHANGED (Pitfall 2); do
    not refactor its `vm.assume` filters. Couple TST-01 to VRF-derived OUTPUT byte-identity, never to
    bounty/reward amounts (anti-pattern).
  </action>
  <verify>
    <automated>forge test --match-contract RngLockDeterminism 2>&1 | tail -25 && FOUNDRY_PROFILE=deep forge test --match-contract RngLockDeterminism --match-test "AutoBuyDuringLockSafe|AutoOpen" 2>&1 | tail -15</automated>
  </verify>
  <done>The three new functions pass under the default profile AND the
  `AutoBuyDuringLockSafe`/`AutoOpen` cases pass under `FOUNDRY_PROFILE=deep`; the non-vacuity
  assertion is present (the proof fails if the perturbation did not move `totalFlipReversals`); A7
  `StakedStonkRedemption` remains the SAME documented red, unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keeper → AfKing.doWork / autoBuy | permissionless keeper calls cross into the router during the VRF-locked window |
| AfKing → DegenerusGame/AdvanceModule | the advance-consume reads `totalFlipReversals` mid-tx; players can nudge it via flip-reversal buys before the consume |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-332-01-VAC | Tampering (RNG) | the freeze-fuzz proof itself | mitigate | NON-VACUITY assertion: the test must prove the perturbation actually moved `totalFlipReversals` before asserting byte-identity (Pitfall 4) — a vacuous pass (perturbation never reaches the consumed read) is forbidden by the explicit `assertGt`/`assertTrue` on the moved value |
| T-332-01-MEV | Tampering (MEV) | advance-consume bundled with buy/open in one tx | mitigate | proven: byte-identical VRF-derived output even with `doWork`/`autoBuy` in the SAME locked window (the consumed `cw += totalFlipReversals` is fixed request→consume) |
| T-332-01-MAROON | Denial of Service | autoOpen during rngLock | mitigate | proven: `boxesPending()==false` + `autoOpen(N)==0` during lock (no revert), SAME boxes open post-unlock with cursor intact (entry-gate non-reverting loop, RD-5) |
| T-332-01-SC | Tampering | test toolchain | accept | no package installs in this phase (test-authoring only); `forge`/`hardhat` present — no supply-chain surface |
</threat_model>

<verification>
- `forge test --match-contract RngLockDeterminism` green (default profile).
- `FOUNDRY_PROFILE=deep forge test --match-contract RngLockDeterminism` green for the new router cases.
- The non-vacuity assertion is structurally present (the byte-identity test cannot pass when the
  perturbation is a no-op).
- `testFuzz_RngLockDeterminism_StakedStonkRedemption` remains red (A7, unchanged) — the v48 baseline
  red carries forward; the file extension did NOT alter the existing red-set.
- Zero `contracts/*.sol` modifications: `git diff --name-only contracts/` returns nothing.
</verification>

<success_criteria>
TST-01 proven: the router advance-consume byte-identity holds under same-tx `doWork`/`autoBuy`
perturbation (non-vacuous); autoBuy-during-lock SAFE; autoOpen-blocked + no-marooned-boxes. The
extension adds only NEW passing functions and the one carried-forward A7 red; no new regression.
</success_criteria>

<output>
Create `.planning/phases/332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg/332-01-SUMMARY.md` when done
</output>
