---
phase: 332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg
plan: 05
type: execute
wave: 2
depends_on: ["332-02", "332-03", "332-04"]
files_modified:
  - test/fuzz/CrankFaucetResistance.t.sol
  - test/fuzz/CrankNonBrick.t.sol
  - test/gas/CrankLeversAndPacking.t.sol
  - test/gas/CrankOpenBoxWorstCaseGas.t.sol
  - test/gas/CrankResolveBetWorstCaseGas.t.sol
  - test/fuzz/RngFreezeAndRemovalProofs.t.sol
autonomous: true
requirements: [TST-04]
must_haves:
  truths:
    - "All 17 enumerated premise-retired reds are DELETED (re-authored fresh in the v49 proof files at plans 02/03/04) — not repaired in place"
    - "After deletion the forge failing set is EXACTLY the 42 v48.0-baseline reds by NAME — net-zero new regression"
    - "The 5 Crank*-named files are git mv'd to Keeper*-prefixed names with internal contract/symbol renames — pure rename, zero behavioral change"
    - "SAFE-03 / H-CANCEL-SWAP are PRESERVED (the no-double-buy invariant re-expressed in lastAutoBoughtDay storage / pool-balance-delta terms, never the retired AutoBought event)"
    - "Grep Crank across test/ returns only provenance comment prose (no Crank*-named file remains)"
  artifacts:
    - path: "test/fuzz/KeeperFaucetResistance.t.sol"
      provides: "renamed CrankFaucetResistance survivors (the 9 deleted reds removed)"
    - path: "test/gas/KeeperLeversAndPacking.t.sol"
      provides: "renamed CrankLeversAndPacking survivors (the 2 deleted reds removed)"
    - path: "test/fuzz/KeeperNonBrick.t.sol"
      provides: "renamed CrankNonBrick survivors (the 4 deleted reds removed)"
  key_links:
    - from: "the post-deletion forge failing set"
      to: "test/REGRESSION-BASELINE-v48.md §2 (the 42-name union)"
      via: "forge test --json failing names == the 42 v48 union"
      pattern: "42 v48 union by NAME"
---

<objective>
Execute TST-04 part A: DELETE all 17 enumerated premise-retired reds (their v49 invariants are
re-authored fresh in the new proof files at plans 332-02/03/04 — NOT repaired in place, per D-04),
and `git mv` the 5 `Crank*`-named test files to `Keeper*`-prefixed names with internal contract/
symbol renames (D-07 de-crank, pure rename, zero behavioral change). After this plan the `forge test`
failing set MUST return to EXACTLY the 42 v48.0-baseline reds by NAME (net-zero new regression, the
binding headline). The no-double-buy invariant carried by any deleted/renamed test is re-expressed in
storage-oracle terms (`lastAutoBoughtDay` / pool-balance-delta per GASOPT-04) — SAFE-03 /
H-CANCEL-SWAP MUST be PRESERVED, not weakened (TST-04 hard constraint).

RE-CONFIRM AT HEAD (binding correction): the count is 17 at the live HEAD `59f891d3` (re-verified
this planning session: `forge test --json` = 640 passed / 59 failed / 17 skipped; 59 − 17 = exactly
the 42 v48 union by name). The executor MUST re-run `forge test --json` at the actual execution HEAD
BEFORE deleting and gate on the 42-NAME union, not a bare count — if a commit lands between now and
execution the count can shift; any forge red NOT in the 42 union AND NOT in the 17-set is a NEW
regression → STOP and surface.

Purpose: restore a clean NON-WIDENING v49 red-set so the ledger (plan 332-06) can attest net-zero.
Output: 17 reds removed, 5 files renamed, the failing set == the 42 v48 union.

POSTURE (binding): `test/` ONLY. ZERO `contracts/*.sol` (mainnet) mutation — FROZEN subject. Tests
are agent-committable. If a deletion/rename surfaces a CONTRACT defect, STOP and surface it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg/332-RESEARCH.md
@.planning/phases/332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg/332-PATTERNS.md
@test/REGRESSION-BASELINE-v48.md

<interfaces>
<!-- The 17 premise-retired reds to DELETE (re-author homes already authored at 332-02/03/04). -->

CrankFaucetResistance.t.sol (9): testBatchEmitsExactlyOneCreditFlipWithSum,
  testCrankBeforeRngWordSkipsAndDoesNotReward, testDuplicateInBatchRewardsOnce,
  testFuzz_MultiBoxRoundTripNonPositiveAcrossGasPrices, testFuzz_RoundTripNonPositiveAcrossGasPrices,
  testMultiBoxSelfCrankRoundTripNonPositive, testSelfCrankRoundTripNonPositive,
  testWinningBetFullResolvePathStillPegsReward, testZeroSuccessBatchEmitsNoCreditFlip
CrankLeversAndPacking.t.sol (2): testCrankBetsEmitsExactlyOneCreditFlipForManyItems,
  testCrankBoxesEmitsExactlyOneCreditFlipForManyBoxes
CrankNonBrick.t.sol (4): testBatchPurchaseRngLockedRejectsWholeBatchAtEntry,
  testCrankBetsSkipsPoisonedMiddleItem, testCrankBoxesSkipsPoisonedEntryViaTryCatch,
  testFuzz_CrankBetsPoisonPositionNeverBricks
RngFreezeAndRemovalProofs.t.sol (2): testCrankBetResolutionStaysPostUnlock,
  testFuzz_CrankResolvesIffWordLanded

<!-- The 5 git mv rename targets (Crank*-NAMED files only; RngFreezeAndRemovalProofs is NOT renamed). -->
test/fuzz/CrankFaucetResistance.t.sol   contract CrankFaucetResistance (:45)   -> test/fuzz/KeeperFaucetResistance.t.sol  contract KeeperFaucetResistance
test/fuzz/CrankNonBrick.t.sol           contract CrankNonBrick (:48)           -> test/fuzz/KeeperNonBrick.t.sol          contract KeeperNonBrick
test/gas/CrankLeversAndPacking.t.sol    contract CrankLeversAndPacking (:48)   -> test/gas/KeeperLeversAndPacking.t.sol   contract KeeperLeversAndPacking
test/gas/CrankOpenBoxWorstCaseGas.t.sol contract CrankOpenBoxWorstCaseGas (:29)-> test/gas/KeeperOpenBoxWorstCaseGas.t.sol contract KeeperOpenBoxWorstCaseGas
test/gas/CrankResolveBetWorstCaseGas.t.sol contract CrankResolveBetWorstCaseGas (:48) -> test/gas/KeeperResolveBetWorstCaseGas.t.sol contract KeeperResolveBetWorstCaseGas

<!-- DO NOT TOUCH: the source-presence grep of `degeneretteResolve(` (a CONTRACT symbol, unchanged). -->
<!-- DO NOT TOUCH: testFuzz_RngLockDeterminism_StakedStonkRedemption (A7, v48 baseline red). -->
<!-- DO NOT DELETE: testCrankBoxOpenStaysPostUnlock (RngFreezeAndRemovalProofs; green, NOT in the 17-set). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-confirm the live red-set, then delete the 17 premise-retired reds</name>

  <files>test/fuzz/CrankFaucetResistance.t.sol, test/gas/CrankLeversAndPacking.t.sol, test/fuzz/CrankNonBrick.t.sol, test/fuzz/RngFreezeAndRemovalProofs.t.sol</files>
  <read_first>
    - test/REGRESSION-BASELINE-v48.md §2 (the AUTHORITATIVE 42-name v48 union — buckets A/B/C)
    - .planning/phases/332-.../332-RESEARCH.md (the "17 premise-retired reds" table + Pitfall 1 the
      count is 17 not 16 + the standing "re-run forge test at the actual HEAD and gate on the
      42-name union" instruction)
    - .planning/phases/332-.../332-PATTERNS.md (the "17 premise-retired reds — DELETE + re-author
      fresh" table, lines 150-168, with the re-author home per row)
    - test/fuzz/CrankFaucetResistance.t.sol / test/gas/CrankLeversAndPacking.t.sol /
      test/fuzz/CrankNonBrick.t.sol / test/fuzz/RngFreezeAndRemovalProofs.t.sol (READ each of the 17
      bodies to confirm the retired premise before deletion)
    - .planning/phases/332-.../332-02-SUMMARY.md / 332-03-SUMMARY.md / 332-04-SUMMARY.md (confirm the
      v49 re-author homes landed — the deletion must not lose coverage)
  </read_first>
  <action>
    First run `forge test --json` and parse the failing set; confirm `failing − the 17-set == the 42
    v48 union by NAME` (use the §2 enumeration). If any failing name is NOT in the 42 union AND NOT in
    the 17-set, STOP and emit `## STOP — NEW REGRESSION OUTSIDE BASELINE` naming the offending test.
    Then DELETE exactly the 17 enumerated test functions (listed in the interfaces block) from their 4
    files — remove the whole function body + its NatSpec, and any now-orphaned helper/constant used
    ONLY by a deleted test (e.g. the retired `3 * CRANK_RESOLVE_BET_GAS_UNITS * CRANK_GAS_PRICE_REF`
    summed-reward constant in `CrankLeversAndPacking.t.sol:153` if no surviving test references it —
    grep first). Confirm each deleted test's v49 invariant is re-authored in the home named in the
    PATTERNS table (TST-01/02/03/05). DO NOT delete `testCrankBoxOpenStaysPostUnlock` from
    `RngFreezeAndRemovalProofs.t.sol` (NOT in the 17-set — green, stays). DO NOT alter
    `testFuzz_RngLockDeterminism_StakedStonkRedemption` (A7). DO NOT touch the source-presence grep of
    the CONTRACT symbol `degeneretteResolve(`.
  </action>
  <verify>
    <automated>forge test --json 2>/dev/null > /tmp/forge332-post.json; python3 -c "import json; d=json.load(open('/tmp/forge332-post.json')); f=[t.split('(')[0] for s in d.values() for t,r in s.get('test_results',{}).items() if r.get('status')=='Failure']; print('failing count:', len(f)); print('still exactly 42:', len(f)==42)"</automated>
  </verify>
  <done>The pre-delete gate confirmed `failing − 17 == 42 v48 union by name`; the 17 functions (and
  any orphaned summed-reward helpers) are deleted; the post-delete forge failing set is exactly 42 by
  name (the 42 v48 union); no v49 coverage was lost (each re-authored at 02/03/04).</done>
</task>

<task type="auto">
  <name>Task 2: git mv the 5 Crank* files to Keeper* + internal symbol renames + zero-residual check</name>

  <files>test/fuzz/CrankFaucetResistance.t.sol, test/fuzz/CrankNonBrick.t.sol, test/gas/CrankLeversAndPacking.t.sol, test/gas/CrankOpenBoxWorstCaseGas.t.sol, test/gas/CrankResolveBetWorstCaseGas.t.sol</files>
  <read_first>
    - .planning/phases/332-.../332-PATTERNS.md (the "5 Crank* -> Keeper* renames" section, lines
      179-193, incl. the contract-decl lines + the keeper-* naming-convention precedent
      RouterWorstCaseGas / KeeperBatchAffiliateDeltaAudit)
    - .planning/phases/332-.../332-RESEARCH.md (Runtime State Inventory: the de-crank rename surface;
      A3 zero behavioral effect; the zero-residual `grep Crank test/` check excluding provenance prose)
    - the 5 Crank*-named files (READ the `contract X is DeployProtocol {` decl + any `is`/import of a
      renamed helper, e.g. CrankNonBrick `ReentrantWithdrawer` :1020 / `interface AfKingLike` :1062 —
      leave helper names if not "crank")
  </read_first>
  <action>
    For each of the 5 `Crank*`-named files: `git mv` to its `Keeper*` target (per the interfaces
    table), rename the internal `contract Crank<X>` decl to `contract Keeper<X>`, update its `@title`
    NatSpec and any in-repo cross-reference (import path / `is`/inheritance / helper symbol that
    embeds "Crank"). Leave non-"crank" helper names (`ReentrantWithdrawer`, `AfKingLike`) untouched.
    Pure rename — ZERO behavioral change (do not edit any test logic). After all 5, run
    `grep -rln Crank test/` and confirm the only residual hits are (a) provenance COMMENT prose in
    non-renamed files (`SweepPerPlayerWorstCaseGas.t.sol`, `RouterWorstCaseGas.t.sol`,
    `KeeperBatchAffiliateDeltaAudit.t.sol`, `RngFreezeAndRemovalProofs.t.sol`) documenting the v46
    crank lineage and (b) the (not-yet-written) v49 ledger's deliberate historical record — NO
    `Crank*`-NAMED file remains. Do NOT touch the source-presence grep of the CONTRACT symbol
    `degeneretteResolve(` (unchanged). `forge build` recompiles renamed artifacts automatically.
  </action>
  <verify>
    <automated>forge build 2>&1 | tail -3; ls test/fuzz/KeeperFaucetResistance.t.sol test/fuzz/KeeperNonBrick.t.sol test/gas/KeeperLeversAndPacking.t.sol test/gas/KeeperOpenBoxWorstCaseGas.t.sol test/gas/KeeperResolveBetWorstCaseGas.t.sol 2>&1; echo "--- residual Crank*-named files (should be none) ---"; ls test/fuzz/Crank*.t.sol test/gas/Crank*.t.sol 2>&1</automated>
  </verify>
  <done>All 5 files renamed to `Keeper*` with internal `contract`/symbol renames; `forge build` exit
  0; no `Crank*`-named test file remains; `grep -rln Crank test/` returns only provenance comment
  prose. `forge test` failing set still exactly the 42 v48 union (renames are behavior-neutral).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the test red-set | the NON-WIDENING gate keys on the failing NAME set, not a count |
| the de-crank rename | file-path + symbol churn that must not change any test's behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-332-05-WIDEN | Tampering (audit integrity) | the non-widening gate | mitigate | the gate is "`forge test` failing set == the 42 v48 names by NAME", NOT a bare count — a count gate would mask a real NEW regression that coincidentally offsets a deletion; the executor re-confirms the 42-NAME union pre- and post-delete |
| T-332-05-SAFE03 | Elevation of Privilege (reward) | no-double-buy oracle | mitigate | SAFE-03 / H-CANCEL-SWAP preserved — the re-authored no-double-buy is in `lastAutoBoughtDay` storage / pool-balance-delta terms (the migrated `AfKingConcurrency` helpers), NEVER the retired `AutoBought` event; the deletion removes only the retired-premise reds, not the SAFE-03 cases |
| T-332-05-RENAME | Tampering | de-crank rename | mitigate | pure `git mv` + symbol rename, zero behavioral edit; the post-rename `forge test` failing set is still exactly the 42 union (proves behavior-neutral); the source-presence CONTRACT-symbol grep is untouched |
| T-332-05-LOSS | Repudiation | coverage loss on delete | mitigate | each of the 17 deletions has a named re-author home (PATTERNS table); the deletion task READS the 02/03/04 SUMMARYs to confirm the v49 invariant landed before removing the old red |
| T-332-05-SC | Tampering | test toolchain | accept | no package installs (test-authoring only) |
</threat_model>

<verification>
- `forge test --json` post-delete failing set == exactly the 42 v48 union by NAME (not a count).
- `forge build` exit 0 after the renames; no `Crank*`-named test file remains.
- `grep -rln Crank test/` returns only provenance comment prose + (later) the v49 ledger.
- SAFE-03 / H-CANCEL-SWAP cases (in `AfKingConcurrency.t.sol`) still pass unchanged.
- Zero `contracts/*.sol` modifications: `git diff --name-only contracts/` returns nothing.
</verification>

<success_criteria>
TST-04 part A complete: the 17 premise-retired reds deleted (re-authored fresh elsewhere), the 5
Crank files renamed to Keeper, the failing set restored to exactly the 42 v48 union by name —
net-zero new regression, SAFE-03 / H-CANCEL-SWAP preserved.
</success_criteria>

<output>
Create `.planning/phases/332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg/332-05-SUMMARY.md` when done
</output>
