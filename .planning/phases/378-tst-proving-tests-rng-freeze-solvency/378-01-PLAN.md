---
phase: 378-tst-proving-tests-rng-freeze-solvency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fuzz/StorageFoundation.t.sol
  - test/fuzz/StakedStonkRedemption.t.sol
  - test/fuzz/invariant/RedemptionInvariants.inv.t.sol
  - test/fuzz/RedemptionStethFallback.t.sol
  - test/fuzz/RedemptionGas.t.sol
  - test/REGRESSION-BASELINE-v61.md
  - .planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-01-RECALIBRATION-KEY.md
autonomous: true
requirements: [TST-06]
must_haves:
  truths:
    - "The authoritative v61 DegenerusGame storage layout is captured from forge inspect, not guessed"
    - "The -1 post-balances slot shift is empirically confirmed (or corrected) against the captured layout"
    - "StorageFoundation pre-balances slot asserts (0/2/11) still pass unchanged"
    - "The Game-resident vm.store/vm.load pokes in the redemption harnesses target the correct v61 slots"
    - "The frozen-baseline 2bee6d6f forge red set is captured BY NAME and the tree is returned to HEAD"
  artifacts:
    - path: ".planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-01-RECALIBRATION-KEY.md"
      provides: "Authoritative v61 slot map + the -1 shift confirmation + per-harness recalibration ledger"
      contains: "balancesPacked"
    - path: "test/REGRESSION-BASELINE-v61.md"
      provides: "The frozen-baseline 2bee6d6f red set captured BY NAME for the TST-06 non-widening gate"
      contains: "2bee6d6f"
  key_links:
    - from: "378-01-RECALIBRATION-KEY.md"
      to: "forge inspect DegenerusGame storageLayout"
      via: "authoritative layout capture"
      pattern: "balancesPacked|_subOf|_subscribers|_subCursor"
---

<objective>
Establish the foundation for the entire 378 TST phase: capture the authoritative v61 storage layout, confirm the PACK fold's -1 post-balances slot shift, recalibrate the slot-stale harnesses that are NOT gas harnesses (StorageFoundation + the 4 redemption vm.load/store harnesses), and capture the frozen-baseline `2bee6d6f` forge red set BY NAME so the TST-06 non-widening comparison in 378-05 has a ceiling to compare against.

Purpose: The v61 PACK fold replaced two balance mappings (`claimableWinnings` + `afkingFunding`) with one (`balancesPacked`), a net -1 storage slot. Every slot hardcoded AFTER the balances region is now off by -1. This shift is the prerequisite for every downstream plan (gas harnesses in 378-02, behavior fixes in 378-03). The recalibration key is the single source of truth all later plans cite.

Output: `378-01-RECALIBRATION-KEY.md` (authoritative slot map + shift confirmation + per-harness ledger), recalibrated StorageFoundation + 4 redemption harnesses, and `test/REGRESSION-BASELINE-v61.md` (the captured `2bee6d6f` red set).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-PLAN-TRIAGE.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- v61 storage symbols (confirmed in source) the recalibration must reflect. -->
<!-- The PACK fold lives in contracts/storage/DegenerusGameStorage.sol. -->

The fold: `mapping(address => uint256) internal balancesPacked;` (DegenerusGameStorage.sol:418)
  - layout `[afking:high128 | claimable:low128]`
  - accessors: `_claimableOf` (`uint128(balancesPacked[p])`), `_afkingOf` (`balancesPacked[p] >> 128`),
    `_creditClaimable`/`_debitClaimable` (low half), `_creditAfking`/`_debitAfking` (high half)
  - REPLACED two pre-fold mappings: `claimableWinnings` (was at Game slot 7) + `afkingFunding`

Pre-balances slots — UNAFFECTED by the fold (StorageFoundation asserts these):
  - slot 0: flags (ticketWriteSlot bit 224, prizePoolFrozen bit 232, ticketsFullyProcessed bit 208, gameOverPossible bit 216)
  - slot 2: prizePoolsPacked
  - slot 11: prizePoolPendingPacked

Post-balances Game slots (hypothesis: shifted -1 from the e18af451 values pinned in the gas harnesses):
  - `_subOf` mapping root, `_subscribers` array length, `_subscriberIndex` mapping, `_subCursor` packed slot

Redemption harness slot classes (CRITICAL distinction):
  - sDGNRS-RESIDENT (on address(sdgnrs), NOT DegenerusGame) — UNAFFECTED by the Game fold:
    SLOT_PENDING_BY_DAY=10, SLOT_PENDING_REDEMPTIONS=7, SLOT_PENDING_RESOLVE_DAY=11
  - GAME-RESIDENT (written via address(game)) — AFFECTED: the old `claimableWinnings` mapping (slot 7)
    is now the low-128 half of `balancesPacked`; `claimablePool` stays slot 1 upper-128.
    StakedStonkRedemption.t.sol:111-115 + RedemptionGas.t.sol:32-38 poke these.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture authoritative v61 storage layout + confirm the -1 shift + write the recalibration key</name>
  <read_first>
    - .planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-PLAN-TRIAGE.md (§A the recalibration key, §B the harness list)
    - contracts/storage/DegenerusGameStorage.sol (lines ~410-420 the balancesPacked decl; ~2130-2160 the _subOf/_subscribers/_subCursor region)
    - test/gas/V56AfkingGasMarginal.t.sol (lines ~60-92 — the e18af451 pinned slots SUBOF_SLOT=65, SUBSCRIBERS_SLOT=67, SUBCURSOR_SLOT=69, MINTPACKED_SLOT=10, RNG_WORD_BY_DAY_SLOT=11 — these are the -1 reference points)
    - foundry.toml (note: only the [invariant]/[deep] profiles set include_storage=true; the default profile does NOT emit storageLayout)
  </read_first>
  <action>
    Run `forge clean && forge build` first (NOT while any other forge process runs — artifact contention regenerates ContractAddresses.sol). Then run `forge inspect DegenerusGame storageLayout` (this command forces layout emission regardless of profile). Extract the exact slot+offset for: balancesPacked, claimablePool, mintPacked_, _subOf, _subscribers, _subscriberIndex, _subCursor (and rngWordByDay if present).

    Confirm the -1 hypothesis: the gas harnesses pinned `_subOf=65`, `_subscribers=67`, `_subCursor=69` at e18af451 (pre-fold). The fold removed one mapping slot in the balances region, so the v61 authoritative values should be `_subOf=64`, `_subscribers=66`, `_subscriberIndex=67`, `_subCursor=68` (each -1). If forge inspect shows a DIFFERENT delta (e.g. -2, or 0 because the region moved), document the ACTUAL delta — the layout is authoritative, the -1 is only the hypothesis. Do NOT force the numbers to match the hypothesis.

    Write `378-01-RECALIBRATION-KEY.md` containing: (1) the raw `forge inspect DegenerusGame storageLayout` slot table for the affected symbols; (2) the confirmed delta (pre-fold slot -> v61 slot for each post-balances symbol); (3) a per-harness recalibration ledger listing EVERY slot-hardcoded harness (StorageFoundation, the 4 redemption harnesses, and the 6 gas harnesses owned by 378-02) with old-slot -> new-slot and whether each is Game-resident (recalibrate) or sDGNRS-resident (leave). This file is the single source of truth 378-02 and 378-03 cite.
  </action>
  <verify>
    <automated>test -f .planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-01-RECALIBRATION-KEY.md && grep -q "balancesPacked" .planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-01-RECALIBRATION-KEY.md && grep -qE "_subOf" .planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-01-RECALIBRATION-KEY.md</automated>
  </verify>
  <acceptance_criteria>
    - `forge inspect DegenerusGame storageLayout` was run and its output for balancesPacked / _subOf / _subscribers / _subscriberIndex / _subCursor is recorded verbatim in 378-01-RECALIBRATION-KEY.md
    - The pre-fold -> v61 slot delta is stated explicitly for each post-balances symbol (confirmed -1, or the actual measured delta if different)
    - The key includes the per-harness ledger covering all 11 slot-hardcoded harnesses with Game-resident vs sDGNRS-resident classification
    - `git status --porcelain contracts/` is EMPTY (no contract file modified; if `forge clean`/`forge build` regenerated contracts/ContractAddresses.sol, restore it so contracts/*.sol is byte-identical to HEAD)
  </acceptance_criteria>
  <done>378-01-RECALIBRATION-KEY.md exists with the authoritative v61 layout, the confirmed slot delta, and the per-harness ledger; contracts/ byte-identical to HEAD.</done>
</task>

<task type="auto">
  <name>Task 2: Recalibrate StorageFoundation + the 4 redemption Game-resident slot pokes</name>
  <read_first>
    - .planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-01-RECALIBRATION-KEY.md (Task 1 output — the authoritative slot map)
    - test/fuzz/StorageFoundation.t.sol (the slot-0/2/11 asserts are pre-balances → UNCHANGED; verify no post-balances slot is poked)
    - test/fuzz/StakedStonkRedemption.t.sol (lines ~79-90 the sDGNRS slot constants = UNCHANGED; lines ~108-115 the Game-resident claimableWinnings slot-7 / claimablePool slot-1 pokes = AFFECTED)
    - test/fuzz/RedemptionGas.t.sol (lines ~32-38 — the Game-resident claimable/pool seed pokes)
    - test/fuzz/RedemptionStethFallback.t.sol + test/fuzz/invariant/RedemptionInvariants.inv.t.sol (locate their Game-resident pokes; the sDGNRS pokes stay)
    - contracts/storage/DegenerusGameStorage.sol (confirm balancesPacked low-128 = the old claimableWinnings semantics; a Game-resident claimable seed now writes the low half of balancesPacked[key])
  </read_first>
  <action>
    Apply ONLY the recalibrations the key in Task 1 prescribes. For StorageFoundation: the slot-0/2/11 asserts are pre-balances and MUST stay byte-identical — confirm via the key that no assert in this file touches a post-balances slot; if confirmed, this file needs NO slot change (only re-run to prove it still passes green).

    For the redemption harnesses: leave EVERY sDGNRS-resident slot constant (SLOT_PENDING_BY_DAY=10, SLOT_PENDING_REDEMPTIONS=7, SLOT_PENDING_RESOLVE_DAY=11) untouched — those are on address(sdgnrs), unaffected by the Game fold. Recalibrate ONLY the Game-resident pokes: where a test writes the old `claimableWinnings` mapping via `keccak256(abi.encode(addr, uint256(7)))` against address(game), the value is now the LOW 128 bits of `balancesPacked` at the same conceptual key — confirm the balancesPacked mapping root slot from the key and update the slot index if it moved; ensure the seed writes only the low half (a claimable-only seed must not corrupt the afking high half). The `claimablePool` slot-1 upper-128 poke is unchanged (slot 1 is pre-balances).

    If forge inspect shows the balancesPacked mapping root is at the same slot the old claimableWinnings occupied (slot 7), the only change needed is the SEMANTIC one (write low-128, not full word). Document which it is.
  </action>
  <verify>
    <automated>forge test --match-path "test/fuzz/StorageFoundation.t.sol" 2>&1 | tail -5 && forge test --match-path "test/fuzz/StakedStonkRedemption.t.sol" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-path test/fuzz/StorageFoundation.t.sol` reports 0 failed (the pre-balances slot asserts pass)
    - `forge test --match-path test/fuzz/StakedStonkRedemption.t.sol` shows the slot-stale `NoPass`/`panic`/garbage-read failures resolved (run count drops to 0 failed OR every remaining failure is documented in the SUMMARY as a non-slot class-(b)/(c) carried to 378-03 — slot-stale ones must be gone)
    - `forge test --match-path "test/fuzz/RedemptionGas.t.sol"` and `--match-path "test/fuzz/RedemptionStethFallback.t.sol"` and `--match-path "test/fuzz/invariant/RedemptionInvariants.inv.t.sol"` each have their Game-resident slot pokes recalibrated; any residual red is documented (slot class gone)
    - The sDGNRS-resident slot constants (10/7/11) are byte-identical to HEAD (grep-confirm they were not touched)
    - `git status --porcelain contracts/` is EMPTY (contracts/*.sol byte-identical)
  </acceptance_criteria>
  <done>StorageFoundation green; the redemption harnesses' Game-resident slot pokes recalibrated per the key; sDGNRS slots untouched; contracts/ unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Capture the frozen-baseline 2bee6d6f forge red set BY NAME, return tree to HEAD</name>
  <read_first>
    - .planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-PLAN-TRIAGE.md (§C — the 525/396 HEAD baseline + the non-widening methodology)
    - test/REGRESSION-BASELINE-v56.md (lines ~1-200 — the established BY-NAME union format: "live − union == ∅" set-diff, Bucket A/B/F structure — mirror this format)
    - test/REGRESSION-BASELINE-v57.md (the harness-artifact characterization format for storage-shift + RNG-rebase reds)
  </read_first>
  <action>
    Capture the frozen-baseline `2bee6d6f` red set so the TST-06 non-widening gate (378-05) has a ceiling. Use a NON-DESTRUCTIVE method that returns the working tree to HEAD afterward (never leave the tree on the baseline; the v61 contract subject must remain at HEAD for all downstream plans).

    Approach: `git stash` any uncommitted test edits from Tasks 1-2 first (so they survive), then check out ONLY the baseline contract subject for measurement (e.g. `git checkout 2bee6d6f -- contracts/` into a scratch state), `forge clean && forge build`, run the FULL forge suite, capture every `[FAIL...]` test name to a name-keyed list. Then HARD-restore: `git checkout HEAD -- contracts/` and `forge clean && forge build` to put the v61 subject back, `git stash pop` to restore the test edits. Verify `git diff HEAD -- contracts/` is empty after restore.

    Write the captured baseline into `test/REGRESSION-BASELINE-v61.md` mirroring the v56 format: (1) the `2bee6d6f` pass/fail counts; (2) the baseline red union enumerated BY NAME (the ceiling); (3) bucket characterization (VRF/RNG-window vs stale-harness/behavioral vs flaky-invariant) carried from the v56/v57 precedent where the names overlap; (4) a stated non-widening rule: "any v61 HEAD forge red whose name is NOT in this union ∪ accepted-slot-shift-staleness ∪ accepted-v61-behavior-change is a candidate v61 finding." ALSO capture the Hardhat behavioral baseline DURING this same baseline window (run it after the forge capture, while contracts/ is still at `2bee6d6f`, BEFORE the `git checkout HEAD -- contracts/` restore): run the deterministic Hardhat suite via `npm test` and record its baseline pass/fail BY NAME in a clearly-labeled "Hardhat baseline" section of the doc. EXCLUDE the probabilistic `npm run test:stat` distribution tests (chi²/EV) — characterize them separately; they are NOT part of the non-widening ceiling. The existing `git checkout HEAD -- contracts/ && forge clean && forge build` restore step then also repairs the `ContractAddresses.sol` that a hardhat compile regenerates (the known forge-fixture landmine). If the Hardhat suite cannot complete cleanly in the autonomous environment (hang/timeout/flaky), document the limitation and proceed — the forge baseline is the PRIMARY TST-06 ceiling and the Hardhat baseline is corroborating, never a reason to fail this task.
  </action>
  <verify>
    <automated>test -f test/REGRESSION-BASELINE-v61.md && grep -q "2bee6d6f" test/REGRESSION-BASELINE-v61.md && git diff HEAD -- contracts/ | head -1 | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `test/REGRESSION-BASELINE-v61.md` exists, cites `2bee6d6f`, and enumerates the baseline forge red set BY NAME (the non-widening ceiling)
    - The doc states the explicit non-widening rule (HEAD red ∉ union ∪ accepted-staleness ∪ accepted-behavior ⇒ candidate finding) matching the v55/56/57 methodology
    - The doc includes a labeled "Hardhat baseline" section with the deterministic `npm test` baseline pass/fail BY NAME (`test:stat` excluded/characterized separately), OR an explicit note that the Hardhat run could not complete autonomously (in which case the forge baseline is the primary ceiling)
    - `git diff HEAD -- contracts/` is EMPTY after the capture (the tree was returned to the v61 HEAD subject; `git status` does not show contracts/ on the baseline)
    - The test edits from Tasks 1-2 survived the stash/pop (StorageFoundation + redemption recalibrations still present per `git status`)
    - `forge build` is clean at the end (v61 subject restored and compiling)
  </acceptance_criteria>
  <done>test/REGRESSION-BASELINE-v61.md captures the 2bee6d6f red set BY NAME with the non-widening rule; the working tree is back on the v61 HEAD contract subject (contracts/ byte-identical to HEAD); forge build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness slot constant -> Game storage | A wrong slot constant reads/writes the wrong field, producing a false-green that masks a real regression |
| baseline capture -> working tree | A baseline checkout that is not fully restored leaves the v61 subject contaminated for every downstream plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-378-01-01 | Tampering | recalibration slot constants | mitigate | Slots are taken from authoritative `forge inspect DegenerusGame storageLayout`, not guessed; the -1 hypothesis is confirmed against the captured layout, not assumed |
| T-378-01-02 | Information Disclosure | false-green from wrong slot | mitigate | StorageFoundation's pre-balances asserts (0/2/11) must still pass after recalibration — a regression there proves the layout read was wrong; the redemption sDGNRS slots are explicitly left untouched (proven unaffected) |
| T-378-01-03 | Tampering | baseline-checkout contamination | mitigate | The capture uses stash + scratch checkout + hard restore + `git diff HEAD -- contracts/` empty assertion in the acceptance criteria; the tree is provably back on the v61 HEAD subject before the plan completes |
| T-378-01-SC | Tampering | npm/forge artifact regeneration | mitigate | `forge clean`/`forge build` may regenerate contracts/ContractAddresses.sol; acceptance criteria assert `git status --porcelain contracts/` empty so any regenerated contract file is restored |
</threat_model>

<verification>
- `forge inspect DegenerusGame storageLayout` captured and the affected slots recorded in the recalibration key.
- StorageFoundation green (pre-balances slots unaffected, proving the layout read was correct).
- Redemption harnesses' Game-resident pokes recalibrated; sDGNRS pokes untouched.
- `test/REGRESSION-BASELINE-v61.md` captures the `2bee6d6f` red set BY NAME with the non-widening rule.
- `git diff HEAD -- contracts/` empty — the v61 subject is intact for downstream plans.
</verification>

<success_criteria>
- The authoritative v61 storage layout is captured (not guessed) and the post-balances slot shift is empirically confirmed.
- StorageFoundation passes (0 failed) and the 4 redemption harnesses' slot-stale failures are resolved (Game-resident pokes recalibrated, sDGNRS pokes preserved).
- The frozen-baseline `2bee6d6f` red set is enumerated BY NAME in test/REGRESSION-BASELINE-v61.md as the TST-06 non-widening ceiling.
- contracts/*.sol byte-identical to HEAD throughout (zero contract edits).
</success_criteria>

<output>
Create `.planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-01-SUMMARY.md` when done
</output>
