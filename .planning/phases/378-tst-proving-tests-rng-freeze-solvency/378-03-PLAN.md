---
phase: 378-tst-proving-tests-rng-freeze-solvency
plan: 03
type: execute
wave: 3
depends_on: ["378-02"]
files_modified:
  - test/fuzz/AfKingSubscription.t.sol
  - test/fuzz/AfKingConcurrency.t.sol
  - test/fuzz/DegeneretteFreezeResolution.t.sol
  - test/fuzz/DegeneretteHeroScore.t.sol
  - test/fuzz/DegeneretteResolveRepeg.t.sol
  - test/fuzz/VRFCore.t.sol
  - test/fuzz/VRFPathCoverage.t.sol
  - test/fuzz/VRFStallEdgeCases.t.sol
  - test/fuzz/VrfRotationLiveness.t.sol
  - test/fuzz/VrfRotationOrphanIndex.t.sol
  - test/fuzz/KeeperFaucetResistance.t.sol
  - test/fuzz/KeeperRewardRoutingSameResults.t.sol
  - test/fuzz/KeeperRouterOneCategory.t.sol
  - test/fuzz/LootboxBoonCoexistence.t.sol
  - test/fuzz/LootboxRngLifecycle.t.sol
  - test/fuzz/PresaleBoxDrain.t.sol
  - test/fuzz/PrizePoolFreeze.t.sol
  - test/fuzz/QueueDoubleBuffer.t.sol
  - test/fuzz/RngFreezeAndRemovalProofs.t.sol
  - test/fuzz/RngLockDeterminism.t.sol
  - test/fuzz/StallResilience.t.sol
  - test/fuzz/TicketEdgeCases.t.sol
  - test/fuzz/TicketLifecycle.t.sol
  - test/fuzz/TicketRouting.t.sol
  - test/fuzz/V56FreezeSolvency.t.sol
  - test/fuzz/V56SecUnmanipulable.t.sol
  - test/fuzz/V56SubHardening.t.sol
  - test/fuzz/AffiliateDgnrsClaim.t.sol
  - .planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-03-CANDIDATE-FINDINGS.md
autonomous: true
requirements: [TST-06]
must_haves:
  truths:
    - "Every remaining red is triaged into (a) slot-stale, (b) v61-behavior, or (c) candidate-bug"
    - "Residual slot-stale reds are recalibrated per the 378-01 key"
    - "v61-behavior reds have their expectation updated to the legitimately-changed behavior"
    - "Class-(c) failures NOT explained by (a)/(b) are documented as candidate v61 findings, NOT contract-fixed"
  artifacts:
    - path: ".planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-03-CANDIDATE-FINDINGS.md"
      provides: "Per-file triage ledger (a/b/c) + any class-(c) candidate v61 findings"
      contains: "class"
  key_links:
    - from: "378-03-CANDIDATE-FINDINGS.md"
      to: "378-01-RECALIBRATION-KEY.md"
      via: "residual slot-stale recalibration"
      pattern: "slot"
---

<objective>
Triage every remaining failing test file (the ~32-file tail in triage §C after 378-01/02 resolved the slot-hardcoded harnesses) into the three classes — (a) slot-stale, (b) v61-behavior, (c) candidate-bug — recalibrate residual slot-stale reds, update v61-behavior expectations to the legitimately-changed behavior, and DOCUMENT (never contract-fix) any class-(c) failure as a candidate v61 finding.

Purpose: The 525/396 HEAD baseline is dominated by the slot-shift signature, but a tail of failures asserts pre-v61 behavior that the AFPAY/PACK/CURSE/SMITE changes legitimately altered: new `AfkingSpent` emits, curse-penalized activity scores, afking-funded shortfalls now succeeding where they used to revert, the Degenerette `InvalidBet()` cross-check vs the `_collectBetFunds` afking-tier change, and the affiliate fresh/recycled split. These are class-(b) updates. Anything NOT explained by (a) or (b) is a candidate v61 finding that gates a contract fix at TERMINAL — it is documented here, never fixed (this is a TEST-ONLY phase).

Output: the ~32 files triaged and brought green where (a)/(b), plus `378-03-CANDIDATE-FINDINGS.md` recording the per-file disposition and any class-(c) candidates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-PLAN-TRIAGE.md
@.planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-01-RECALIBRATION-KEY.md
@test/REGRESSION-BASELINE-v61.md

<interfaces>
<!-- v61 behavior deltas that drive class-(b) expectation updates (confirmed in source). -->

AFPAY waterfall (DegenerusGame.sol _processMintPayment ~1054-1151; DegenerusGameStorage.sol _settleShortfall ~857):
  - DirectEth shortfalls now draw afking (no longer revert at the old lootbox :1135 DirectEth->revert)
  - `AfkingSpent(player, amount)` emitted at each afking debit (DegenerusGame.sol:1151, DegenerusGameStorage.sol:879, DegeneretteModule:599)
  - prizeContribution = msg.value + claimableUsed + afkingUsed
Degenerette ETH bet (DegenerusGameDegeneretteModule.sol _collectBetFunds ~579-599):
  - covers remainder from afking after claimable-to-sentinel; keeps `InvalidBet()` only if STILL short
CURSE APPLY (DegenerusGameMintStreakUtils.sol _playerActivityScore ~320-322):
  - `penaltyBps = curse * 100; bonusBps = bonusBps > penaltyBps ? bonusBps - penaltyBps : 0`
  - propagates to every activity-score consumer + the public view + frozen snapshots
Affiliate fresh/recycled split (MintModule ~1620-1692): freshEth = costWei - claimableUsedTicket
  (afking lands in fresh; byte-identical for existing no-afking DirectEth/Claimable/Combined)

Triage classes (triage §C):
  (a) slot-stale -> recalibrate per the 378-01 key
  (b) v61-behavior -> update the test's expectation to the legitimately-changed behavior
  (c) potential bug -> a failure NOT explained by (a)/(b) -> DOCUMENT as a candidate finding (no contract fix)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triage + fix the AfKing / Degenerette / affiliate / V56 behavior-delta cluster</name>
  <read_first>
    - .planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-01-RECALIBRATION-KEY.md (for any residual slot-stale pokes in these files)
    - test/REGRESSION-BASELINE-v61.md (the 2bee6d6f baseline reds BY NAME — a red present at baseline is NOT a v61 regression, do not "fix" it green)
    - test/fuzz/AfKingSubscription.t.sol, test/fuzz/AfKingConcurrency.t.sol (afking sub paths likely hit AFPAY emits / slot pokes)
    - test/fuzz/DegeneretteFreezeResolution.t.sol, test/fuzz/DegeneretteHeroScore.t.sol, test/fuzz/DegeneretteResolveRepeg.t.sol (the 24 InvalidBet reds — cross-check vs _collectBetFunds afking tier)
    - test/fuzz/AffiliateDgnrsClaim.t.sol (the affiliate fresh/recycled split)
    - test/fuzz/V56FreezeSolvency.t.sol, test/fuzz/V56SecUnmanipulable.t.sol, test/fuzz/V56SubHardening.t.sol (sub-hardening + freeze/solvency expectations)
    - contracts/modules/DegenerusGameDegeneretteModule.sol (lines ~579-599 _collectBetFunds — the afking remainder + InvalidBet-if-still-short)
    - contracts/DegenerusGame.sol (lines ~1054-1151 _processMintPayment — the AfkingSpent emit sites)
  </read_first>
  <action>
    For each file in this cluster, run it, read each `[FAIL...]` reason, and classify:
    - (a) slot-stale (garbage read / NoPass / panic from a hardcoded post-balances slot) -> recalibrate the slot per the 378-01 key.
    - (b) v61-behavior: the test asserts pre-v61 behavior. Examples to expect: a test expecting NO `AfkingSpent` event now sees one (add the expectEmit / update the assertion); a test expecting a DirectEth shortfall to revert now sees it succeed via afking (update to the new success path); a Degenerette bet test expecting `InvalidBet()` where afking now covers the remainder (update — but KEEP `InvalidBet()` where afking is also short, that revert is preserved by design); an affiliate split test expecting the old fresh/recycled boundary (update to freshEth = costWei - claimableUsedTicket). Update ONLY the expectation, never weaken the assertion to a tautology.
    - (c) anything not explained by (a)/(b): do NOT touch the test to force green and do NOT edit any contract. Record it in 378-03-CANDIDATE-FINDINGS.md with the file, test name, the `[FAIL]` reason, and why it is not (a)/(b).

    Cross-reference test/REGRESSION-BASELINE-v61.md: a failure whose name is in the 2bee6d6f baseline union is a PRE-EXISTING red (carried), not a v61 regression — leave it red and note it as a carried baseline red (it is neither a v61 fix target nor a candidate finding).
  </action>
  <verify>
    <automated>for g in "AfKing*" "Degenerette*" "Affiliate*" "V56*"; do echo "== $g =="; forge test --match-path "test/fuzz/$g.t.sol" 2>&1 | tail -5; done</automated>
  </verify>
  <acceptance_criteria>
    - Every failing test in this cluster is classified (a/b/c) in 378-03-CANDIDATE-FINDINGS.md
    - Class-(a) reds in these files are recalibrated per the key; class-(b) reds have their expectation updated to the v61 behavior (assertions strengthened/corrected, never reduced to tautologies)
    - The `InvalidBet()` revert is PRESERVED in tests where afking is also short (a Degenerette test must not be edited to remove a still-valid revert expectation)
    - Class-(c) failures are documented as candidate findings (file + test name + FAIL reason + why not a/b); ZERO contract edits made for them
    - Failures whose names are in test/REGRESSION-BASELINE-v61.md are left red and noted as carried baseline reds (not fixed, not candidate findings)
    - `git status --porcelain contracts/` is EMPTY (contracts/*.sol byte-identical)
  </acceptance_criteria>
  <done>The AfKing/Degenerette/affiliate/V56 cluster is triaged; (a)/(b) brought green; (c) documented; carried baseline reds noted; contracts/ unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Triage + fix the VRF / Rng / Lootbox / Ticket / Keeper / pool tail</name>
  <read_first>
    - .planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-01-RECALIBRATION-KEY.md
    - test/REGRESSION-BASELINE-v61.md (carried baseline reds — esp. the VRF/RNG-window bucket, historically the largest pre-existing red bucket)
    - test/fuzz/VRFCore.t.sol, test/fuzz/VRFPathCoverage.t.sol, test/fuzz/VRFStallEdgeCases.t.sol, test/fuzz/VrfRotationLiveness.t.sol, test/fuzz/VrfRotationOrphanIndex.t.sol
    - test/fuzz/RngFreezeAndRemovalProofs.t.sol, test/fuzz/RngLockDeterminism.t.sol, test/fuzz/StallResilience.t.sol
    - test/fuzz/LootboxBoonCoexistence.t.sol, test/fuzz/LootboxRngLifecycle.t.sol, test/fuzz/PresaleBoxDrain.t.sol
    - test/fuzz/TicketEdgeCases.t.sol, test/fuzz/TicketLifecycle.t.sol, test/fuzz/TicketRouting.t.sol
    - test/fuzz/KeeperFaucetResistance.t.sol, test/fuzz/KeeperRewardRoutingSameResults.t.sol, test/fuzz/KeeperRouterOneCategory.t.sol
    - test/fuzz/PrizePoolFreeze.t.sol, test/fuzz/QueueDoubleBuffer.t.sol
    - contracts/modules/DegenerusGameMintModule.sol (lines ~1126-1146 + ~1170-1254 the lootbox shortfall + the plain lootbox leg _recordLootboxMintDay wiring; ~1489-1537 presale)
  </read_first>
  <action>
    Same triage protocol as Task 1, applied to this tail. Expected dominant class here: most VRF/Rng-window reds are CARRIED baseline reds (present at 2bee6d6f — the historical 41-name VRF/RNG-window bucket) and must be left red, NOT fixed. Verify each against test/REGRESSION-BASELINE-v61.md before touching it. Residual class-(a) slot-stale reds (e.g. VRFCore's `vm.load(...slot 37...)` / `SLOT_PACKED_0` / `SLOT_VRF_REQUEST_ID` reads that underflow on a shifted slot — see the v57 baseline characterization for the same pattern) get recalibrated per the key. Class-(b): lootbox/presale/ticket tests asserting a DirectEth shortfall reverts where afking now covers it, or a manual lootbox buyer not being bounty-eligible where `_recordLootboxMintDay` now stamps them — update those expectations. Class-(c): document, no contract fix.

    Be conservative: when in doubt whether a VRF red is a carried baseline red vs a v61 regression, the test/REGRESSION-BASELINE-v61.md name set is authoritative — in-union ⇒ carried (leave red); out-of-union and not (a)/(b) ⇒ candidate finding (document).
  </action>
  <verify>
    <automated>for g in "VRF*" "Vrf*" "Rng*" "Lootbox*" "Ticket*" "Keeper*" "PrizePoolFreeze" "QueueDoubleBuffer" "PresaleBoxDrain" "StallResilience"; do echo "== $g =="; forge test --match-path "test/fuzz/$g.t.sol" 2>&1 | tail -4; done</automated>
  </verify>
  <acceptance_criteria>
    - Every failing test in this tail is classified (a/b/c) in 378-03-CANDIDATE-FINDINGS.md
    - Carried baseline reds (names in test/REGRESSION-BASELINE-v61.md, incl. the VRF/RNG-window bucket) are left red and NOT edited green
    - Class-(a) slot-stale reds recalibrated per the key; class-(b) v61-behavior reds updated (afking-covered shortfalls, manual-lootbox bounty eligibility) without weakening assertions
    - Class-(c) failures documented as candidate findings with reason; ZERO contract edits
    - `git status --porcelain contracts/` is EMPTY (contracts/*.sol byte-identical)
  </acceptance_criteria>
  <done>The VRF/Rng/Lootbox/Ticket/Keeper/pool tail is triaged; carried baseline reds preserved; (a)/(b) brought green; (c) documented; contracts/ unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Consolidate the triage ledger + interim non-widening sanity check</name>
  <read_first>
    - .planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-03-CANDIDATE-FINDINGS.md (the per-file dispositions from Tasks 1-2)
    - test/REGRESSION-BASELINE-v61.md (the 2bee6d6f baseline union — the non-widening ceiling)
    - .planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-PLAN-TRIAGE.md (§C the methodology + the accepted-staleness ∪ accepted-behavior union definition)
  </read_first>
  <action>
    Finalize 378-03-CANDIDATE-FINDINGS.md into a complete per-file triage ledger: every file from 378-01/02/03 with its disposition (recalibrated / behavior-updated / carried-baseline-red / candidate-finding) and a one-line reason. Run the full forge suite once and record the post-378-03 pass/fail counts.

    Perform an INTERIM non-widening sanity check (the FINAL by-name comparison is 378-05's job, this is a guardrail): collect the current HEAD forge red names, and confirm each is in (the 2bee6d6f baseline union ∪ the accepted-slot-shift-staleness set ∪ the accepted-v61-behavior-change set ∪ the documented class-(c) candidates). If ANY HEAD red name is NOT in that union and is NOT already logged as a class-(c) candidate, STOP and add it as a candidate finding — it is an unexplained regression. Record the interim subset check result (live − union, ideally empty modulo logged candidates) in the SUMMARY.

    Do not attempt to drive the suite fully green — carried baseline reds and documented class-(c) candidates stay red by design. The goal is that EVERY red is explained.
  </action>
  <verify>
    <automated>test -f .planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-03-CANDIDATE-FINDINGS.md && grep -qiE "class|disposition|candidate|carried" .planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-03-CANDIDATE-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - 378-03-CANDIDATE-FINDINGS.md is a complete per-file triage ledger covering every file touched in 378-01/02/03 with disposition + reason
    - The post-378-03 forge pass/fail counts are recorded in the SUMMARY
    - The interim non-widening check is run: every HEAD red name is accounted for in (baseline union ∪ accepted-staleness ∪ accepted-behavior ∪ logged class-(c) candidates) — any unexplained red is added as a candidate finding
    - No contract edits were made; `git status --porcelain contracts/` is EMPTY
  </acceptance_criteria>
  <done>The triage ledger is complete, the interim non-widening check passes (every red explained), candidate findings logged; contracts/ unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test expectation -> v61 behavior | Updating an expectation to a tautology (or to mask a real bug) would certify a regression as correct behavior |
| candidate finding -> contract fix | This is a TEST-ONLY phase; "fixing" a class-(c) failure by editing a contract bypasses the IMPL hand-review gate |
| HEAD red -> baseline union | A red silently dropped from the comparison (or mis-classified as carried) hides a v61 regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-378-03-01 | Tampering | test expectation updates | mitigate | Class-(b) updates change the expectation to the documented v61 behavior delta only; assertions are strengthened/corrected, never reduced to tautologies; the preserved `InvalidBet()` revert is explicitly protected |
| T-378-03-02 | Elevation of Privilege | class-(c) contract fix | mitigate | Class-(c) failures are DOCUMENTED as candidate findings, never contract-fixed; `git status --porcelain contracts/` empty is asserted in every task |
| T-378-03-03 | Information Disclosure | masked regression via misclassification | mitigate | Every HEAD red is reconciled against the authoritative 2bee6d6f by-name union; in-union ⇒ carried, out-of-union and not (a)/(b) ⇒ candidate finding; the interim subset check (live − union) catches any unexplained red |
| T-378-03-SC | Tampering | forge artifact regeneration | mitigate | acceptance criteria assert `git status --porcelain contracts/` empty |
</threat_model>

<verification>
- Every failing file in the ~32-file tail is triaged (a/b/c) and recorded in 378-03-CANDIDATE-FINDINGS.md.
- Class-(a) residual slot-stale reds recalibrated; class-(b) v61-behavior reds updated without weakening assertions.
- Class-(c) candidate findings documented, ZERO contract edits.
- The interim non-widening check confirms every HEAD red is explained.
- contracts/*.sol byte-identical to HEAD.
</verification>

<success_criteria>
- The remaining failing files are triaged into (a)/(b)/(c); (a)/(b) brought green, (c) documented as candidate v61 findings.
- Carried baseline reds (incl. the VRF/RNG-window bucket) are preserved, not force-fixed.
- The interim non-widening check passes (every red accounted for in the baseline union ∪ accepted-staleness ∪ accepted-behavior ∪ candidates).
- contracts/*.sol byte-identical to HEAD (zero contract edits).
</success_criteria>

<output>
Create `.planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-03-SUMMARY.md` when done
</output>
