---
phase: 378-tst-proving-tests-rng-freeze-solvency
plan: 02
type: execute
wave: 2
depends_on: ["378-01"]
files_modified:
  - test/gas/V56AfkingGasMarginal.t.sol
  - test/gas/SweepPerPlayerWorstCaseGas.t.sol
  - test/gas/RouterWorstCaseGas.t.sol
  - test/gas/KeeperResolveBetWorstCaseGas.t.sol
  - test/gas/KeeperOpenBoxWorstCaseGas.t.sol
  - test/gas/KeeperLeversAndPacking.t.sol
autonomous: true
requirements: [TST-06]
must_haves:
  truths:
    - "Every slot-hardcoded gas harness reads the correct v61 post-balances slots"
    - "V56AfkingGasMarginal's 16 NoPass reverts are resolved (deity-pass grant now writes the correct slot)"
    - "The binding STAGE_2 subscriber all-evict worst case is re-measured LIVE on v61 and stays under the 16.7M ceiling"
  artifacts:
    - path: "test/gas/V56AfkingGasMarginal.t.sol"
      provides: "Recalibrated afking-marginal gas harness with the v61 STAGE_2 live measurement"
      contains: "SUBOF_SLOT"
  key_links:
    - from: "test/gas/V56AfkingGasMarginal.t.sol"
      to: "378-01-RECALIBRATION-KEY.md"
      via: "v61 slot constants"
      pattern: "SUBOF_SLOT|SUBSCRIBERS_SLOT|SUBCURSOR_SLOT"
---

<objective>
Recalibrate the 6 slot-hardcoded gas harnesses to the authoritative v61 slot map from 378-01, then re-run V56AfkingGasMarginal to re-measure the binding STAGE_2 subscriber all-evict worst case LIVE on v61 and confirm it holds under the 16.7M ceiling.

Purpose: These 6 harnesses pin Game-resident storage slots (`SUBOF_SLOT`, `SUBSCRIBERS_SLOT`, `SUBCURSOR_SLOT`, plus mapping roots) at their pre-fold (e18af451) values. The PACK fold's -1 post-balances shift means the deity-pass grant and subscriber seeds now write the WRONG slot, causing the 98 repo-wide `NoPass()` reverts (V56AfkingGasMarginal's 16 tests are the canonical instance) and `panic` storage corruption. Recalibration is pure slot-constant editing — no logic change. After recalibration, the gas worst case must be re-measured on the live v61 subject (377 referenced STAGE_2 at 13.60M / 3.17M headroom; PACK is structurally 1-slot-neutral on the evict path so it should hold — confirm empirically per the worst-case-branch discipline).

Output: 6 recalibrated gas harnesses, all green, with the STAGE_2 live re-measurement recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-PLAN-TRIAGE.md
@.planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-01-RECALIBRATION-KEY.md

<interfaces>
<!-- The pinned (pre-fold e18af451) slot constants in each gas harness. -->
<!-- 378-01-RECALIBRATION-KEY.md holds the authoritative v61 replacements. -->

V56AfkingGasMarginal.t.sol (~32 slot refs): SUBOF_SLOT=65, SUBSCRIBERS_SLOT=67, SUBCURSOR_SLOT=69,
  MINTPACKED_SLOT=10, RNG_WORD_BY_DAY_SLOT=11, HEADER_SLOT=0; per-field byte OFF_* offsets within the Sub slot
SweepPerPlayerWorstCaseGas.t.sol (~14): CLAIMABLE_WINNINGS_SLOT=7, CLAIMABLE_POOL_SLOT=1, SUBOF_SLOT=65,
  SUBSCRIBERS_SLOT=67, SUBSCRIBER_INDEX_SLOT=68, MINTPACKED_SLOT=10
RouterWorstCaseGas.t.sol (~15): RNG_WORD_BY_DAY_SLOT=11, LOOTBOX_ETH_BASE_SLOT=23, LOOTBOX_RNG_PACKED_SLOT=38,
  LOOTBOX_RNG_WORD_BY_INDEX_SLOT=39, SUBOF_SLOT=65, SUBSCRIBERS_SLOT=67, SUBSCRIBER_INDEX_SLOT=68,
  SUBCURSOR_SLOT=69, MINTPACKED_SLOT=10
KeeperResolveBetWorstCaseGas.t.sol (~7): LOOTBOX_RNG_PACKED_SLOT=38, LOOTBOX_RNG_WORD_SLOT=39,
  DEGENERETTE_BETS_SLOT=45, DEGENERETTE_BET_NONCE_SLOT=46, PRIZE_POOLS_SLOT=2
KeeperOpenBoxWorstCaseGas.t.sol (~8): RNG_WORD_BY_DAY_SLOT=11, SUBOF_SLOT=65, SUBSCRIBERS_SLOT=67, MINTPACKED_SLOT=10
KeeperLeversAndPacking.t.sol (~8): LOOTBOX_RNG_PACKED_SLOT=38, LOOTBOX_RNG_WORD_SLOT=39, LOOTBOX_ETH_BASE_SLOT=23

Gas ceiling constants (V56AfkingGasMarginal): EFFECTIVE_GAS_CEILING=16_700_000, GAS_TARGET=10_000_000,
  EIP7825_TX_GAS_CAP=16_777_216. The STAGE_2 all-evict worst case (377 ref: 13.60M / 3.17M headroom).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recalibrate the 5 keeper/sweep/router gas-harness slot constants to the v61 map</name>
  <read_first>
    - .planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-01-RECALIBRATION-KEY.md (the authoritative pre-fold -> v61 slot delta + per-harness ledger)
    - test/gas/SweepPerPlayerWorstCaseGas.t.sol (lines ~49-60 the slot constants incl. CLAIMABLE_WINNINGS_SLOT=7 and CLAIMABLE_POOL_SLOT=1)
    - test/gas/RouterWorstCaseGas.t.sol (lines ~58-96 the slot constants)
    - test/gas/KeeperResolveBetWorstCaseGas.t.sol (lines ~55-63 the slot constants)
    - test/gas/KeeperOpenBoxWorstCaseGas.t.sol (lines ~53-62 the slot constants)
    - test/gas/KeeperLeversAndPacking.t.sol (lines ~48-52 the slot constants)
    - contracts/storage/DegenerusGameStorage.sol (confirm CLAIMABLE_WINNINGS_SLOT semantics — the old claimableWinnings is now balancesPacked low-128; any seed of that slot must write the low half only)
  </read_first>
  <action>
    Apply ONLY the slot-constant edits the 378-01 key prescribes to these 5 files. For each post-balances constant (`SUBOF_SLOT`, `SUBSCRIBERS_SLOT`, `SUBSCRIBER_INDEX_SLOT`, `SUBCURSOR_SLOT`), replace the pinned e18af451 value with the v61 value from the key (the confirmed -1, or whatever the key states). Pre-balances constants (`CLAIMABLE_POOL_SLOT=1`, `PRIZE_POOLS_SLOT=2`, `RNG_WORD_BY_DAY_SLOT=11`, `MINTPACKED_SLOT=10`, `LOOTBOX_*` slots if they sit before the balances region) stay as the key dictates — only change what the key says moved.

    For `CLAIMABLE_WINNINGS_SLOT` in SweepPerPlayerWorstCaseGas: this was the old `claimableWinnings` mapping root (slot 7). Per the key, update to the balancesPacked root slot AND ensure any seed write targets the low-128 half (a claimable seed must not corrupt the afking high half) — mirror the semantic handling 378-01 Task 2 used for the redemption harnesses. For DEGENERETTE_BETS_SLOT/DEGENERETTE_BET_NONCE_SLOT in KeeperResolveBetWorstCaseGas (post-balances), apply the key's delta. Update the trailing comment on each changed constant to describe what IS at the new slot (no change-history per the lean-comment rule).

    Do NOT alter any gas threshold constant, loop bound, measurement logic, or within-slot byte offset.
  </action>
  <verify>
    <automated>for f in SweepPerPlayerWorstCaseGas RouterWorstCaseGas KeeperResolveBetWorstCaseGas KeeperOpenBoxWorstCaseGas KeeperLeversAndPacking; do echo "== $f =="; forge test --match-path "test/gas/$f.t.sol" 2>&1 | tail -4; done</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 5 harnesses (`SweepPerPlayerWorstCaseGas`, `RouterWorstCaseGas`, `KeeperResolveBetWorstCaseGas`, `KeeperOpenBoxWorstCaseGas`, `KeeperLeversAndPacking`) reports 0 failed under `forge test --match-path`, OR any residual failure is documented in the SUMMARY as a non-slot class-(b)/(c) handed to 378-03 (the slot-stale `NoPass`/`panic` class must be gone)
    - Only the slots the 378-01 key marked as moved were changed; pre-balances constants are byte-identical (grep-confirm CLAIMABLE_POOL_SLOT, PRIZE_POOLS_SLOT, MINTPACKED_SLOT, RNG_WORD_BY_DAY_SLOT unchanged)
    - No gas threshold constant, loop bound, or byte offset was altered (diff is slot-constant + comment only)
    - `git status --porcelain contracts/` is EMPTY (contracts/*.sol byte-identical)
  </acceptance_criteria>
  <done>The 5 keeper/sweep/router gas harnesses are recalibrated and green (slot class resolved); only the moved slots changed; contracts/ unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Recalibrate V56AfkingGasMarginal + re-measure the STAGE_2 all-evict worst case live on v61</name>
  <read_first>
    - .planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-01-RECALIBRATION-KEY.md (the authoritative slot delta)
    - .planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-PLAN-TRIAGE.md (§B — V56AfkingGasMarginal's 16 NoPass reverts trace to the stale-slot deity-pass grant; the STAGE_2 13.60M / 3.17M-headroom reference from 377)
    - test/gas/V56AfkingGasMarginal.t.sol (lines ~60-100 the SUBOF/SUBSCRIBERS/SUBCURSOR/MINTPACKED/RNG_WORD_BY_DAY constants + the OFF_* field offsets; the EFFECTIVE_GAS_CEILING=16_700_000 + GAS_TARGET assertions; the STAGE_2 all-evict measurement test)
    - contracts/modules/GameAfkingModule.sol (the evict path — confirm PACK adds no slot on evict, so the STAGE_2 marginal is structurally unchanged)
  </read_first>
  <action>
    Recalibrate the ~32 slot refs in V56AfkingGasMarginal: replace `SUBOF_SLOT`, `SUBSCRIBERS_SLOT`, `SUBCURSOR_SLOT` (and any other post-balances constant) with the v61 values from the 378-01 key; leave the pre-balances `MINTPACKED_SLOT=10`, `RNG_WORD_BY_DAY_SLOT=11`, `HEADER_SLOT=0`, and the OFF_* within-Sub-slot byte offsets unchanged. This resolves the 16 `NoPass()` reverts (the deity-pass grant writes the correct Sub slot, so the subscribe pass-gate accepts).

    Then re-run the harness and READ the actual measured STAGE_2 subscriber all-evict gas from the test output (do not just check pass/fail — capture the number per the worst-case-branch discipline). Confirm it stays under EFFECTIVE_GAS_CEILING (16.7M) and record it against the 377 reference (13.60M / 3.17M headroom). If the measured value exceeds the prior reference by more than measurement noise (the PACK fold should be 1-slot-neutral on evict), investigate whether the gas threshold assertions in the harness need updating to the v61 measured value (a legitimate class-(b) re-measure) OR whether it signals a real regression (class-(c) finding to document, NOT a contract fix). Record the measured number and the disposition in the SUMMARY.

    Do NOT loosen any ceiling assertion to make a test pass — if the live measurement breaches 16.7M, that is a STOP/finding per the security-over-gas floor, not a threshold bump.
  </action>
  <verify>
    <automated>forge test --match-path "test/gas/V56AfkingGasMarginal.t.sol" -vv 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-path test/gas/V56AfkingGasMarginal.t.sol` reports 0 failed (the 16 `NoPass()` reverts are resolved by the slot recalibration)
    - The measured STAGE_2 subscriber all-evict gas is captured as a number in the SUMMARY and is < EFFECTIVE_GAS_CEILING (16_700_000)
    - The measured value is compared to the 377 reference (13.60M / 3.17M headroom); any delta beyond noise is dispositioned (class-(b) re-measure with the new value recorded, or class-(c) finding documented — never a silent ceiling loosening)
    - Only the post-balances slot constants changed; MINTPACKED_SLOT/RNG_WORD_BY_DAY_SLOT/HEADER_SLOT/OFF_* offsets byte-identical
    - `git status --porcelain contracts/` is EMPTY (contracts/*.sol byte-identical)
  </acceptance_criteria>
  <done>V56AfkingGasMarginal is green (0 NoPass), the STAGE_2 all-evict worst case is re-measured live on v61 and recorded under 16.7M; contracts/ unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gas-harness slot constant -> Game storage | A wrong slot makes the deity-pass grant / subscriber seed write the wrong field — either a false-green or a masked gas regression |
| ceiling assertion -> measured gas | A loosened ceiling to force-pass a test would hide a real advanceGame-chain gas-DoS regression (the HIGH-weight threat per the USER threat model) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-378-02-01 | Tampering | gas-harness slot constants | mitigate | Slots come from the 378-01 authoritative `forge inspect` key, not guessed; only the moved constants change, pre-balances ones grep-confirmed unchanged |
| T-378-02-02 | Denial of Service | STAGE_2 worst-case gas | mitigate | The worst-case branch is RE-MEASURED live on v61 (not assumed from 377), the actual number captured and asserted < 16.7M; the security-over-gas floor forbids loosening the ceiling to pass |
| T-378-02-03 | Information Disclosure | false-green from threshold bump | mitigate | A ceiling breach is a STOP/finding, never a silent threshold loosening; any threshold re-measure records the new measured value with its disposition |
| T-378-02-SC | Tampering | forge artifact regeneration | mitigate | acceptance criteria assert `git status --porcelain contracts/` empty after any forge build |
</threat_model>

<verification>
- All 6 gas harnesses green after slot recalibration (the 98-NoPass / 62-panic slot-shift signature resolved for these files).
- The STAGE_2 all-evict worst case re-measured live on v61, captured as a number, confirmed < 16.7M.
- Only moved slots changed; thresholds and offsets untouched.
- contracts/*.sol byte-identical to HEAD.
</verification>

<success_criteria>
- The 6 slot-hardcoded gas harnesses read the correct v61 slots and pass (0 failed each).
- V56AfkingGasMarginal's 16 NoPass reverts resolved; the binding STAGE_2 worst case re-measured live and proven under the 16.7M ceiling.
- contracts/*.sol byte-identical to HEAD (zero contract edits).
</success_criteria>

<output>
Create `.planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-02-SUMMARY.md` when done
</output>
