---
phase: 378-tst-proving-tests-rng-freeze-solvency
plan: 05
type: execute
wave: 5
depends_on: ["378-04"]
files_modified:
  - test/fuzz/V61CureBountyDecurse.t.sol
  - test/fuzz/V61Smite.t.sol
  - test/REGRESSION-BASELINE-v61.md
  - .planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-05-NONWIDENING-LEDGER.md
autonomous: true
requirements: [TST-04, TST-05, TST-06]
must_haves:
  truths:
    - "TST-04: curseCount resets to 0 on a >=1-ticket buy (any funding/path); a sub-ticket buy stamps DAY_SHIFT bounty-eligible but does NOT cure; manual lootbox buyer is bounty-eligible; decurse clears for 100 BURNIE (reverts if 0, emits Decursed)"
    - "TST-05: ownerOf gate rejects non-deity no-burn; active-afker smitee reverts pre-burn; >=5-stack target reverts; success burns 200 BURNIE + adds one stack +2 saturating at the shared cap + emits Smited; curse and smite share one counter; one ticket buy or decurse clears both"
    - "TST-06: the full forge + Hardhat suite is NON-WIDENING by name vs 2bee6d6f — every HEAD red is in the baseline union or accepted-staleness or accepted-v61-behavior; v61 changes add green; no new contract regression"
  artifacts:
    - path: "test/fuzz/V61CureBountyDecurse.t.sol"
      provides: "TST-04 cure + bounty-stamp + decurse proof"
      contains: "Decursed"
    - path: "test/fuzz/V61Smite.t.sol"
      provides: "TST-05 deity-smite proof"
      contains: "Smited"
    - path: ".planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-05-NONWIDENING-LEDGER.md"
      provides: "TST-06 final by-name non-widening comparison vs the 378-01 captured baseline"
      contains: "live"
  key_links:
    - from: "test/fuzz/V61Smite.t.sol"
      to: "GameAfkingModule smite"
      via: "ownerOf gate + burnCoin + shared curse counter"
      pattern: "Smited|ownerOf"
    - from: "378-05-NONWIDENING-LEDGER.md"
      to: "test/REGRESSION-BASELINE-v61.md"
      via: "by-name set-diff live − union"
      pattern: "union"
---

<objective>
Author the final two proving tests — TST-04 (CURE + bounty + decurse), TST-05 (SMITE) — then perform the TST-06 final NON-WIDENING by-name comparison of the full forge + Hardhat suite against the `2bee6d6f` baseline captured in 378-01, proving no new v61 contract regression.

Purpose: TST-04 and TST-05 complete the one-proving-test-per-surface set (cure/bounty/decurse and deity-smite, which share the curse counter). TST-06 is the milestone-level certification: the established v55/56/57 by-name methodology — collect the live HEAD red set, and prove it is a SUBSET of (the 2bee6d6f baseline union ∪ accepted-slot-shift-staleness ∪ accepted-v61-behavior-changes) BY NAME. Any HEAD red NOT in that union is a candidate v61 finding (already surfaced in 378-03's ledger, or a STOP if new).

Output: V61CureBountyDecurse.t.sol, V61Smite.t.sol (both green), and 378-05-NONWIDENING-LEDGER.md (the final by-name comparison) with the TST-06 verdict recorded into test/REGRESSION-BASELINE-v61.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@test/REGRESSION-BASELINE-v61.md
@.planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-03-CANDIDATE-FINDINGS.md

<interfaces>
<!-- v61 source symbols for TST-04/05 (confirmed in source). -->

CURE + bounty (TST-04):
  - CURE: curseCount reset to 0 when totalCost >= priceWei (>=1 ticket worth, funding-agnostic) in
    MintModule._purchaseWithFor (before the score calc) — direct/batch/affiliate/whale/lootbox>=ticket, fresh ETH or claimable
  - bounty stamp: a sub-ticket / small-lootbox buy stamps DAY_SHIFT (bounty-eligible, halts growth) but does NOT cure
  - manual lootbox buyer now bounty-eligible via _recordLootboxMintDay (MintStreakUtils:385, wired at MintModule:1215)
  - _bountyEligible(who) (MintStreakUtils:30)
  - decurse(address target) (GameAfkingModule:1696, DegenerusGame.sol:443): permissionless; revert if target curse==0;
    coin.burnCoin(msg.sender, PRICE_COIN_UNIT/10) = 100 BURNIE; clears curseCount=0; emits Decursed(msg.sender, target) (:1702)
  - Decursed declared GameAfkingModule:1658 `event Decursed(address indexed curer, address indexed target)`

SMITE (TST-05):
  - smite(uint256 deityId, address smitee) (GameAfkingModule:1710, DegenerusGame.sol:454):
    gate IDegenerusDeityPass(DEITY).ownerOf(deityId) == msg.sender (else revert, NO burn);
    validate BEFORE burn: revert on active afking sub (_subOf[smitee].dailyQuantity != 0 = SOLE immunity);
    revert if smitee curseCount >= 5 stacks (the smite ceiling); protocol-addr skip;
    burnCoin(msg.sender, PRICE_COIN_UNIT/5) = 200 BURNIE; curseCount += 2 (1 stack) saturating at the shared cap;
    write back mintPacked_[smitee]; emit Smited(deityId, smitee) (:1726); 1 smite/tx
  - Smited declared GameAfkingModule:1661 `event Smited(uint256 indexed deityId, address indexed smitee)`
  - cashout-curse and smite share ONE counter (CURSE_COUNT_SHIFT=215, cap CURSE_COUNT_CAP=20);
    one >=1-ticket buy OR decurse clears BOTH sources

TST-06 non-widening:
  - the by-name methodology: live HEAD red set − (2bee6d6f baseline union ∪ accepted-staleness ∪ accepted-behavior) == ∅
    (modulo documented class-(c) candidates); the v56 REGRESSION-BASELINE-v56.md "live − union == ∅" format is the model
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TST-04 — V61CureBountyDecurse.t.sol (cure + bounty-stamp + decurse proof)</name>
  <behavior>
    - curseCount resets to 0 on a >=1-ticket buy across each path: direct, batch, affiliate, whale bundle, lootbox >= ticket — with fresh ETH AND (separately) with claimable funding
    - the cure happens BEFORE the score calc (the curing buy itself gets the un-penalized score)
    - a sub-ticket / small-lootbox buy stamps DAY_SHIFT (the buyer becomes _bountyEligible, growth halts) but does NOT reset curseCount
    - a manual lootbox buyer is now _bountyEligible (the plain lootbox leg stamps DAY_SHIFT)
    - decurse(target) clears curseCount to 0 for 100 BURNIE (PRICE_COIN_UNIT/10), emits Decursed(msg.sender, target)
    - decurse reverts if target curseCount is already 0 (no wasted burn); decurse is permissionless (anyone can clear another's curse)
  </behavior>
  <read_first>
    - .planning/REQUIREMENTS.md (TST-04 + CURSE-04/05/06 acceptance text)
    - contracts/modules/DegenerusGameMintModule.sol (lines ~1170-1254 the plain lootbox leg + _recordLootboxMintDay wiring at :1215; ~1285 the cure-before-score; the totalCost>=priceWei cure condition)
    - contracts/modules/DegenerusGameMintStreakUtils.sol (lines ~30 _bountyEligible; ~382-397 _recordLootboxMintDay)
    - contracts/modules/GameAfkingModule.sol (lines ~1696-1702 decurse — the 100-BURNIE burn, revert-if-0, Decursed emit)
    - contracts/DegenerusGame.sol (line ~443 the decurse dispatch)
    - test/fuzz/V61CurseSet.t.sol (378-04 — reuse its curse-stamping setup to put a player at curseCount>0 before curing)
  </read_first>
  <action>
    Write V61CureBountyDecurse.t.sol. Put a player at curseCount>0 (reuse the stale-cashout setup from V61CurseSet). For each buy path (direct, batch, affiliate, whale bundle, lootbox>=ticket), prove a >=1-ticket buy resets curseCount to 0 — run each twice, once funded with fresh ETH and once with claimable, asserting the cure is funding-agnostic. Prove the cure precedes the score calc (the curing buy's own activity score is the un-penalized value). Add a sub-ticket / small-lootbox buy test asserting _bountyEligible becomes true (DAY_SHIFT stamped) AND curseCount is unchanged (NOT cured). Add a manual-lootbox-buyer test asserting _bountyEligible becomes true. Add decurse tests: a cleared-to-0 + 100-BURNIE-burned + `Decursed` expectEmit; a revert-if-already-0; and a permissionless clear (a non-owner clears another player's curse).

    Assertions must be falsifiable (a sub-ticket buy that is asserted NOT to cure must use a real sub-ticket amount; the cure-before-score must read the score the curing buy actually got).
  </action>
  <verify>
    <automated>forge test --match-path "test/fuzz/V61CureBountyDecurse.t.sol" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-path test/fuzz/V61CureBountyDecurse.t.sol` reports all tests passing, 0 failed
    - The file proves the cure on all 5 buy paths (each with fresh ETH and claimable), the cure-before-score ordering, the sub-ticket stamp-but-no-cure, manual-lootbox bounty eligibility, and decurse (clear + 100 BURNIE + Decursed emit + revert-if-0 + permissionless)
    - The sub-ticket no-cure test uses a genuine sub-priceWei amount; the decurse burn amount asserts exactly PRICE_COIN_UNIT/10 (100 BURNIE)
    - `git status --porcelain contracts/` is EMPTY (contracts/*.sol byte-identical)
  </acceptance_criteria>
  <done>V61CureBountyDecurse.t.sol proves TST-04 (cure all paths, bounty stamp, manual-lootbox eligibility, decurse) and is green; contracts/ unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TST-05 — V61Smite.t.sol (deity-smite proof)</name>
  <behavior>
    - a non-deity caller (ownerOf(deityId) != msg.sender) reverts with NO burn (BURNIE balance unchanged)
    - an active-afker smitee (_subOf[smitee].dailyQuantity != 0) reverts BEFORE the burn (the sole immunity)
    - a smitee already at >=5 stacks reverts (the smite ceiling)
    - a successful smite burns 200 BURNIE (PRICE_COIN_UNIT/5), adds one stack (+2 pts) saturating at the shared cap (CURSE_COUNT_CAP=20), emits Smited(deityId, smitee)
    - cashout-curse and smite share ONE counter: a smite on top of a cashout-curse stacks on the same value
    - one >=1-ticket buy OR decurse clears BOTH sources (a smited+cursed player is cleared by a single cure)
  </behavior>
  <read_first>
    - .planning/REQUIREMENTS.md (TST-05 + SMITE-01 acceptance text)
    - contracts/modules/GameAfkingModule.sol (lines ~1710-1726 smite — the ownerOf gate, the pre-burn validations, the 200-BURNIE burn, the saturating +2, the Smited emit)
    - contracts/DegenerusGame.sol (line ~454 the smite dispatch)
    - contracts/interfaces/IDegenerusGameModules.sol (lines ~528/531 the decurse/smite signatures)
    - contracts/DegenerusDeityPass.sol (the ownerOf / soulbound pass; tokenId = symbolId 0-31)
    - test/fuzz/V61CureBountyDecurse.t.sol (378-05 Task 1 — reuse its decurse + cure setup for the shared-counter clear leg)
    - test/fuzz/V61CurseSet.t.sol (reuse the active-afker + cashout-curse setup)
  </read_first>
  <action>
    Write V61Smite.t.sol. Set up a deity-pass holder and a smitee. Prove: a non-owner caller reverts with the BURNIE balance unchanged (assert no burn); an active-afker smitee reverts before the burn (assert no burn AND no curseCount change); a smitee at >=5 stacks reverts; a successful smite burns exactly PRICE_COIN_UNIT/5 (200 BURNIE), increments curseCount by 2 (saturating at 20 when near the cap — drive it to the cap and assert no overshoot), and `vm.expectEmit` the `Smited(deityId, smitee)` event. Prove the shared counter: apply a cashout-curse then a smite and assert curseCount reflects BOTH on one counter; then prove a single >=1-ticket buy (and separately decurse) clears the combined value to 0.

    Each revert leg must assert the burn did NOT happen (the validations are pre-burn) — read the caller's BURNIE balance before/after. The ceiling leg must use a smitee genuinely at >=5 stacks.
  </action>
  <verify>
    <automated>forge test --match-path "test/fuzz/V61Smite.t.sol" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-path test/fuzz/V61Smite.t.sol` reports all tests passing, 0 failed
    - The file proves the ownerOf-gate revert (no burn), the active-afker immunity (revert pre-burn, no burn, no curse change), the >=5-stack ceiling revert, the successful smite (exactly 200 BURNIE burned + 2-stack saturating + Smited emit), the shared-counter stacking, and the single-cure-clears-both (buy and decurse)
    - The revert legs assert the BURNIE balance is unchanged (proving pre-burn validation); the saturation leg asserts no overshoot past 20
    - `git status --porcelain contracts/` is EMPTY (contracts/*.sol byte-identical)
  </acceptance_criteria>
  <done>V61Smite.t.sol proves TST-05 (gate, immunity, ceiling, burn+saturate+emit, shared counter, single-cure) and is green; contracts/ unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: TST-06 — final NON-WIDENING by-name comparison vs the 2bee6d6f baseline</name>
  <read_first>
    - test/REGRESSION-BASELINE-v61.md (378-01 — the 2bee6d6f baseline red union BY NAME + the non-widening rule)
    - .planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-03-CANDIDATE-FINDINGS.md (the accepted-staleness ∪ accepted-behavior sets + any class-(c) candidates)
    - test/REGRESSION-BASELINE-v56.md (lines ~1-200 — the "live − union == ∅" by-name set-diff ledger format to mirror exactly)
    - .planning/REQUIREMENTS.md (TST-06 acceptance text — forge + Hardhat enumerated BY NAME)
  </read_first>
  <action>
    Run the FULL forge suite at the current v61 HEAD (with all 378-01..05 test edits + the 5 new proving tests present) and collect the complete live red set BY NAME. ALSO run the deterministic Hardhat suite at v61 HEAD via `npm test` (EXCLUDE the probabilistic `npm run test:stat` set) and collect its live red set BY NAME. Compute the Hardhat by-name non-widening vs the "Hardhat baseline" section captured in test/REGRESSION-BASELINE-v61.md (378-01), using the SAME union logic (`live − (baseline ∪ accepted-v61-behavior) == ∅` modulo documented candidates). If 378-01 recorded that the Hardhat baseline could not be captured autonomously, run `npm test` at HEAD anyway and triage each failure as known-v61-behavior (class-b) or candidate (class-c). A Hardhat suite that cannot complete in the autonomous environment is documented (with partial results) and the forge verdict stands as the PRIMARY TST-06 gate — Hardhat is corroborating, never the sole blocker.

    Compute the by-name set-diff: `live − (the 2bee6d6f baseline union ∪ accepted-slot-shift-staleness ∪ accepted-v61-behavior-changes)`. The accepted-staleness set = the slots recalibrated in 378-01/02/03; the accepted-behavior set = the class-(b) expectation updates from 378-03. Write 378-05-NONWIDENING-LEDGER.md mirroring the v56 format: the live counts, the union definition, the set-diff result, and the verdict. NON-WIDENING HOLDS iff `live − union == ∅` modulo the documented class-(c) candidates from 378-03 (which are pre-surfaced findings, not silent regressions). Also enumerate the additive GREEN the v61 changes contributed (the 5 new proving tests + any class-(b) flips).

    If `live − union` contains ANY name that is NOT a documented class-(c) candidate, that is a NEW unexplained regression -> STOP, record it as a new candidate finding, and surface it (do NOT widen the union to absorb it). Fold the final verdict into test/REGRESSION-BASELINE-v61.md (the canonical TST-06 ledger).

    CRITICAL: compare BY NAME (set membership), never by COUNT — a count match can hide a swapped red (one baseline red flips green while a new regression appears). The set-diff must be name-keyed in both directions where the methodology requires.
  </action>
  <verify>
    <automated>test -f .planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-05-NONWIDENING-LEDGER.md && grep -qiE "live|union|non-widening|set-diff" .planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-05-NONWIDENING-LEDGER.md && grep -q "V61" .planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-05-NONWIDENING-LEDGER.md</automated>
  </verify>
  <acceptance_criteria>
    - 378-05-NONWIDENING-LEDGER.md records the live HEAD forge AND deterministic-Hardhat (`npm test`, `test:stat` excluded) red sets BY NAME, the union definition, and the name-keyed set-diff result (Hardhat documented as corroborating; if it could not complete autonomously, the limitation + partial results are recorded and the forge verdict is primary)
    - The verdict is stated: NON-WIDENING HOLDS iff `live − union == ∅` modulo the documented 378-03 class-(c) candidates; the comparison is BY NAME not by count (explicitly noted)
    - The 5 new proving tests (V61AfpayWaterfall, V61Pack, V61CurseSet, V61CureBountyDecurse, V61Smite) are enumerated as additive green
    - Any `live − union` name not already a documented class-(c) candidate is surfaced as a NEW candidate finding (the union is NOT widened to absorb it); if the set-diff is empty modulo candidates, that is the passing verdict
    - The final TST-06 verdict is folded into test/REGRESSION-BASELINE-v61.md
    - `git status --porcelain contracts/` is EMPTY (contracts/*.sol byte-identical)
  </acceptance_criteria>
  <done>378-05-NONWIDENING-LEDGER.md proves TST-06 (live − union == ∅ by name modulo documented candidates), the new proving tests are counted as additive green, the verdict is in test/REGRESSION-BASELINE-v61.md; contracts/ unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smite revert validation -> burn | A test that does not check the BURNIE balance on a revert leg cannot prove the validation is pre-burn (a contract that burned-then-reverted-state would pass a weak test) |
| live red set -> baseline union | A by-COUNT comparison (or a silently dropped test name) hides a swapped red — a baseline red flips green while a new v61 regression appears at the same count |
| candidate finding -> widened union | Absorbing an unexplained red into the union to force a green verdict masks a real v61 regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-378-05-01 | Information Disclosure | TST-05 pre-burn validation | mitigate | Every smite revert leg asserts the caller's BURNIE balance is unchanged, proving the validation fires before the burn; the saturation leg asserts no overshoot past the cap |
| T-378-05-02 | Information Disclosure | TST-06 by-name comparison | mitigate | The comparison is name-keyed set membership, never count; the ledger states this explicitly and mirrors the v56 "live − union == ∅" format; a swapped red is caught because its name would appear in live − union |
| T-378-05-03 | Tampering | union widening to force green | mitigate | An unexplained `live − union` name is surfaced as a NEW candidate finding and the union is NOT widened; the passing verdict requires the set-diff empty MODULO the pre-documented 378-03 class-(c) candidates only |
| T-378-05-SC | Tampering | forge artifact regeneration | mitigate | acceptance criteria assert `git status --porcelain contracts/` empty |
</threat_model>

<verification>
- V61CureBountyDecurse.t.sol green — TST-04 (cure all paths, bounty stamp, manual-lootbox eligibility, decurse).
- V61Smite.t.sol green — TST-05 (gate, immunity, ceiling, burn+saturate+emit, shared counter, single-cure).
- 378-05-NONWIDENING-LEDGER.md — TST-06 (live − union == ∅ by name modulo documented candidates), new proving tests as additive green.
- The non-widening comparison is BY NAME, not by count; any unexplained red surfaced as a candidate, never absorbed.
- contracts/*.sol byte-identical to HEAD.
</verification>

<success_criteria>
- TST-04 and TST-05 proving tests pass and genuinely prove their surfaces with falsifiable assertions.
- TST-06 non-widening holds by name (or every exception is a documented candidate finding); the verdict is recorded in test/REGRESSION-BASELINE-v61.md.
- contracts/*.sol byte-identical to HEAD (zero contract edits).
</success_criteria>

<output>
Create `.planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-05-SUMMARY.md` when done
</output>
