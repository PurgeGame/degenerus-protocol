---
phase: 378-tst-proving-tests-rng-freeze-solvency
plan: 04
type: execute
wave: 4
depends_on: ["378-03"]
files_modified:
  - test/fuzz/V61AfpayWaterfall.t.sol
  - test/fuzz/V61Pack.t.sol
  - test/fuzz/V61CurseSet.t.sol
autonomous: true
requirements: [TST-01, TST-02, TST-03]
must_haves:
  truths:
    - "TST-01: the AFPAY waterfall (msg.value->claimable->afking) is proven across all 3 pay-kinds with AfkingSpent emitted and reverts when both short"
    - "TST-02: balancesPacked round-trips both halves at every accessor, claimablePool==Σ, gameOver preserves the afking half, no cross-half carry"
    - "TST-03: cashout-curse +2 fires only on a stale >=5d cashout, never for infra/gameOver/deity/whale/active-afker; penalty curse*100 bps floored 0; saturates at cap"
  artifacts:
    - path: "test/fuzz/V61AfpayWaterfall.t.sol"
      provides: "TST-01 AFPAY waterfall proof"
      contains: "AfkingSpent"
    - path: "test/fuzz/V61Pack.t.sol"
      provides: "TST-02 PACK accessor round-trip + solvency-identity proof"
      contains: "balancesPacked"
    - path: "test/fuzz/V61CurseSet.t.sol"
      provides: "TST-03 cashout-curse SET + exemptions + saturation proof"
      contains: "curseCountOf"
  key_links:
    - from: "test/fuzz/V61AfpayWaterfall.t.sol"
      to: "DegenerusGame _processMintPayment"
      via: "expectEmit AfkingSpent + shortfall draw"
      pattern: "AfkingSpent"
    - from: "test/fuzz/V61CurseSet.t.sol"
      to: "MintStreakUtils _playerActivityScore curse penalty"
      via: "curseCountOf + activity-score read"
      pattern: "curseCountOf|playerActivityScore"
---

<objective>
Author the first three proving tests: TST-01 (AFPAY waterfall), TST-02 (PACK), TST-03 (CURSE SET). Each is a new forge test file proving one v61 surface behaviorally correct against the live v61 subject.

Purpose: One proving test per surface is the core deliverable of the TST phase. These three cover the AfKing-as-payment waterfall, the claimable/afking slot-packing, and the cashout-curse SET semantics. They must assert the EXACT v61 contract (per REQUIREMENTS.md TST-01/02/03), with assertions that would genuinely FAIL under a seeded violation — not surface-level smoke tests.

Output: V61AfpayWaterfall.t.sol, V61Pack.t.sol, V61CurseSet.t.sol — all green, each proving its surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-01-RECALIBRATION-KEY.md

<interfaces>
<!-- v61 source symbols the proving tests assert against (confirmed in source). -->

AFPAY (TST-01):
  - DegenerusGame.sol _processMintPayment (~1054-1151): DirectEth (msg.value->afking, SKIP claimable);
    Claimable (claimable->sentinel->afking); Combined (msg.value->claimable->sentinel->afking);
    prizeContribution = msg.value + claimableUsed + afkingUsed; emits `AfkingSpent(player, afkingUsed)` (:1151)
  - DegenerusGameStorage.sol _settleShortfall (~857-879): draws claimable to 1-wei sentinel (if allowClaimable)
    then afking to 0; reverts E() if both short; emits AfkingSpent(buyer, afkingUsed) (:879)
  - afking auto-buy path (_deliverAfkingBuy/_queueTicketsScaled) NEVER reaches _processMintPayment (no double-draw)
  - AfkingSpent declared DegenerusGameStorage.sol:552 `event AfkingSpent(address indexed player, uint256 amount)`

PACK (TST-02):
  - DegenerusGameStorage.sol:418 `mapping(address => uint256) internal balancesPacked` [afking:high128 | claimable:low128]
  - accessors (~892-928): _claimableOf=uint128(packed), _afkingOf=packed>>128,
    _creditClaimable (low +=), _debitClaimable (low -=, revert E if short), _creditAfking (high += via <<128),
    _debitAfking (high -= via <<128); each paired with claimablePool update
  - gameOver claimable-zeroing must preserve the VAULT/SDGNRS/GNRUS afking halves
  - INVARIANT: claimablePool == Σ (claimable + afking halves) (DegenerusGameStorage.sol:358)
  - the SettleClaimableShortfallTester (contracts/test/SettleClaimableShortfallTester.sol) exposes
    setClaimable/setAfking/setClaimablePool/getClaimable/getAfking/getClaimablePool/settle/sentinelError —
    a direct accessor harness inheriting the canonical storage layout

CURSE SET (TST-03):
  - DegenerusGameMintStreakUtils.sol: CURSE_COUNT_CAP=20 (:348); _playerActivityScore penalty (~320-322)
    `penaltyBps = curse * 100; bonusBps = bonusBps > penaltyBps ? bonusBps - penaltyBps : 0`
  - _maybeCurse(player) from public claimWinnings (DegenerusGame.sol:1572) after a successful claim; bails
    infra(VAULT/SDGNRS/GNRUS) -> gameOver -> non-stale(lastEthDay+5 > _currentMintDay) -> deity-pass ->
    whale/lazy pass(frozenUntilLevel>=level) -> active afker(_subOf[p].dailyQuantity != 0) -> at cap;
    else curse += 2 (saturating at CURSE_COUNT_CAP), SSTORE only on a stale below-cap cashout
  - claimWinningsStethFirst (DegenerusGame.sol:1587) is vault-only -> NEVER triggers the curse
  - curseCountOf(address) external view (MintStreakUtils:378); CURSE_COUNT_SHIFT=215 (BitPackingLib:89)
  - same-day second claim reverts (sentinel) -> no in-day stacking
</interfaces>

<test_template>
<!-- Proving-test scaffold pattern (from test/fuzz/V56FreezeSolvency.t.sol). -->
- SPDX AGPL-3.0-only; pragma ^0.8.26
- import {DeployProtocol} from "./helpers/DeployProtocol.sol"
- import {ContractAddresses} from "../../contracts/ContractAddresses.sol"
- import {MintPaymentKind} from "../../contracts/interfaces/IDegenerusGame.sol"
- import the SettleClaimableShortfallTester for direct-accessor unit assertions where helpful
- Reuse the funded-sub + deity-pass + new-day STAGE harness pattern; seeded-fuzz deterministic (foundry seed 0xdeadbeef)
- Slot reads (claimablePool, balancesPacked) per the 378-01 recalibration key
</test_template>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TST-01 — V61AfpayWaterfall.t.sol (AFPAY waterfall proof)</name>
  <behavior>
    - DirectEth pay-kind: a shortfall draws afking, SKIPS claimable (claimable balance unchanged), emits AfkingSpent(player, afkingUsed); prizeContribution == msg.value + afkingUsed
    - Claimable pay-kind: draws claimable to the 1-wei sentinel, then afking to 0; emits AfkingSpent for the afking portion
    - Combined pay-kind: msg.value first, then claimable->sentinel, then afking; prizeContribution == msg.value + claimableUsed + afkingUsed
    - afking covers a shortfall that used to revert (the pre-v61 DirectEth-lootbox revert is lifted)
    - reverts E() when BOTH claimable and afking are short
    - the afking portion gets fresh-rate affiliate + lootbox activity score + presale credit, with NO rebuy bonus (the bonus reads claimableWinnings deltas only -> afking excluded)
    - NO double-draw: the afking auto-buy path does not also debit via _processMintPayment
  </behavior>
  <read_first>
    - .planning/REQUIREMENTS.md (TST-01 + AFPAY-01..07 acceptance text)
    - contracts/DegenerusGame.sol (lines ~1054-1151 _processMintPayment — the 3 pay-kinds + AfkingSpent emit)
    - contracts/storage/DegenerusGameStorage.sol (lines ~552 AfkingSpent decl; ~857-879 _settleShortfall)
    - contracts/modules/DegenerusGameMintModule.sol (lines ~1620-1692 the affiliate fresh/recycled split; the rebuy-bonus block)
    - test/fuzz/V56FreezeSolvency.t.sol (the STAGE harness + claimablePool slot-read pattern to reuse)
    - .planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-01-RECALIBRATION-KEY.md (claimablePool + balancesPacked slot reads)
  </read_first>
  <action>
    Write V61AfpayWaterfall.t.sol. Seed a buyer with a known claimable balance and a known afking balance via the STAGE harness (or the SettleClaimableShortfallTester for the direct-draw legs). For each of DirectEth / Claimable / Combined, execute a mint that creates a shortfall and assert: the draw ordering (DirectEth leaves claimable untouched; Claimable/Combined draw claimable to the 1-wei sentinel then afking); `vm.expectEmit` the `AfkingSpent(player, afkingUsed)` event with the exact amount; the prizeContribution composition; and that afking covers a shortfall that the pre-v61 path reverted. Add a both-short case asserting `E()` revert. Add an affiliate-credit assertion proving the afking portion lands in fresh (fresh-rate affiliate + activity score) with NO rebuy bonus. Add a no-double-draw assertion: drive the afking auto-buy path and prove it does not also go through _processMintPayment (the afking balance moves exactly once).

    Every assertion must be one that FAILS if the waterfall ordering or the emit is wrong — no smoke tests. Use seeded-deterministic fuzz where a property generalizes (e.g. ordering across random shortfall sizes).
  </action>
  <verify>
    <automated>forge test --match-path "test/fuzz/V61AfpayWaterfall.t.sol" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-path test/fuzz/V61AfpayWaterfall.t.sol` reports all tests passing, 0 failed
    - The file asserts all 3 pay-kinds' draw ordering, the `AfkingSpent` emit (via expectEmit with exact amount), the prizeContribution composition, the both-short `E()` revert, the fresh-affiliate-no-rebuy-bonus property, and the no-double-draw property
    - At least one assertion is demonstrably falsifiable: temporarily inverting an expected ordering/amount in a scratch run would make a test fail (note this reasoning in the SUMMARY; do not leave the inversion in)
    - `git status --porcelain contracts/` is EMPTY (contracts/*.sol byte-identical)
  </acceptance_criteria>
  <done>V61AfpayWaterfall.t.sol proves TST-01 (all 3 pay-kinds, AfkingSpent, both-short revert, fresh-affiliate-no-bonus, no double-draw) and is green; contracts/ unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TST-02 — V61Pack.t.sol (PACK accessor round-trip + solvency identity)</name>
  <behavior>
    - _creditClaimable then _claimableOf round-trips the low-128 half exactly; the afking high half is untouched
    - _creditAfking then _afkingOf round-trips the high-128 half exactly; the claimable low half is untouched
    - _debitClaimable / _debitAfking subtract from the correct half; _debitClaimable reverts E() when the low half is short
    - claimablePool == Σ(claimable + afking halves) holds after a sequence of credits/debits
    - gameOver claimable-zeroing preserves the VAULT/SDGNRS/GNRUS afking halves (high half survives)
    - no 127->128 cross-half carry at supply-bound magnitudes
    - behavior-identical to the conceptual two-mapping baseline (a packed credit/debit equals an independent claimable/afking pair)
  </behavior>
  <read_first>
    - .planning/REQUIREMENTS.md (TST-02 + PACK-01/02 acceptance text)
    - contracts/storage/DegenerusGameStorage.sol (lines ~358 the invariant; ~418 balancesPacked; ~892-928 the accessors)
    - contracts/test/SettleClaimableShortfallTester.sol (the direct-accessor harness: setClaimable/setAfking/setClaimablePool/getClaimable/getAfking/getClaimablePool/settle/sentinelError)
    - .planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-01-RECALIBRATION-KEY.md (the balancesPacked root slot + claimablePool slot)
    - contracts/modules/GameOverModule.sol (the gameOver claimable-zeroing path that must preserve the afking halves)
  </read_first>
  <action>
    Write V61Pack.t.sol. Use the SettleClaimableShortfallTester (it inherits the canonical layout and exposes the setters/getters) for the direct accessor round-trips, plus the live Game for the gameOver-preservation leg. Assert: credit/read round-trips for each half leave the OTHER half byte-identical (read the raw `balancesPacked` slot per the key and split it to prove non-interference); debit subtracts from the correct half and `_debitClaimable` reverts via the sentinelError selector when the low half is short; the `claimablePool == Σ` identity holds across a random credit/debit sequence (seeded fuzz); the gameOver claimable-zeroing preserves the VAULT/SDGNRS/GNRUS afking high halves (seed a high-half value, trigger the zeroing, assert the high half survives while the low half is zeroed); and a supply-bound magnitude (per-player ETH <= ~1.2e26) produces no cross-half carry (a max-realistic low-half value + a high-half credit does not bleed across bit 127).

    Add a two-mapping-equivalence property: a packed credit-then-debit yields the same observable (claimable, afking) pair as two independent counters would — proving the fold is value-identical.
  </action>
  <verify>
    <automated>forge test --match-path "test/fuzz/V61Pack.t.sol" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-path test/fuzz/V61Pack.t.sol` reports all tests passing, 0 failed
    - The file proves: per-half round-trip non-interference, correct-half debit + sentinel-short revert, the `claimablePool == Σ` identity under a seeded sequence, gameOver afking-half preservation, no cross-half carry at supply-bound magnitudes, and two-mapping value-equivalence
    - The non-interference assertions read the raw `balancesPacked` slot and split it (proving the OTHER half is untouched, not just that the read accessor returns the right value)
    - `git status --porcelain contracts/` is EMPTY (contracts/*.sol byte-identical)
  </acceptance_criteria>
  <done>V61Pack.t.sol proves TST-02 (round-trip, identity, gameOver preservation, no carry, two-mapping equivalence) and is green; contracts/ unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TST-03 — V61CurseSet.t.sol (cashout-curse SET + exemptions + saturation)</name>
  <behavior>
    - a stale (>=5d inactive) cashout via public claimWinnings adds +2 to curseCount
    - NO curse for: infra (VAULT/SDGNRS/GNRUS), gameOver, a deity-pass holder, a whale/lazy-pass holder (frozenUntilLevel>=level), an active afker (_subOf[p].dailyQuantity != 0), a non-stale claim (lastEthDay+5 > _currentMintDay)
    - claimWinningsStethFirst (vault-only) NEVER triggers the curse
    - the penalty curse*100 bps is subtracted from the activity score (floored 0), visible across consumers + the public playerActivityScore view + frozen snapshots
    - stacking N stale cashouts -> min(2N, CURSE_COUNT_CAP=20); saturates at the cap, never wraps the uint8
    - a same-day second claim reverts (sentinel) -> no in-day stacking
  </behavior>
  <read_first>
    - .planning/REQUIREMENTS.md (TST-03 + CURSE-02/03/07 acceptance text)
    - contracts/modules/DegenerusGameMintStreakUtils.sol (lines ~320-322 the penalty; ~348 CURSE_COUNT_CAP; ~350-379 _maybeCurse / setter / curseCountOf)
    - contracts/DegenerusGame.sol (lines ~1572 claimWinnings; ~1587 claimWinningsStethFirst — the vault-only path that must NOT curse)
    - contracts/libraries/BitPackingLib.sol (line ~89 CURSE_COUNT_SHIFT=215; the [215-222] free gap)
    - test/fuzz/V56SecUnmanipulable.t.sol (the STAGE harness + the activity-score read pattern + the deity/whale/afker setup to reuse)
  </read_first>
  <action>
    Write V61CurseSet.t.sol. Build a player with claimable winnings and drive them >=5 days inactive, then call the public claimWinnings and assert curseCountOf increments by 2 and the activity score drops by curse*100 bps (floored 0) across at least: a direct consumer, the public playerActivityScore view, and a frozen snapshot. Add an exemption test per bail condition (infra, gameOver, deity-pass, whale/lazy-pass, active-afker, non-stale) asserting curseCountOf stays 0. Add a claimWinningsStethFirst test asserting NO curse (the vault-only path). Add a stacking test: N stale cashouts across N distinct days -> curseCountOf == min(2N, 20), and pushing past the cap leaves it at exactly 20 (never wraps to a low uint8 value). Add a same-day-second-claim test asserting the sentinel revert (so no in-day double-stack).

    Each exemption assertion must FAIL if its bail were removed (i.e. assert curseCountOf==0 for the exempt actor AND curseCountOf==2 for an equivalent non-exempt actor, proving the exemption is what suppresses it). Use seeded fuzz for the stacking/saturation property.
  </action>
  <verify>
    <automated>forge test --match-path "test/fuzz/V61CurseSet.t.sol" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-path test/fuzz/V61CurseSet.t.sol` reports all tests passing, 0 failed
    - The file proves the +2 stale-cashout SET, every exemption (infra/gameOver/deity/whale/active-afker/non-stale + the vault-only claimWinningsStethFirst), the curse*100 bps penalty across consumers + public view + frozen snapshots, the min(2N, 20) stacking with cap saturation (no uint8 wrap), and the same-day-second-claim sentinel revert
    - Each exemption is proven by contrast (exempt actor curseCountOf==0 vs equivalent non-exempt actor curseCountOf==2), not just an absolute zero check
    - `git status --porcelain contracts/` is EMPTY (contracts/*.sol byte-identical)
  </acceptance_criteria>
  <done>V61CurseSet.t.sol proves TST-03 (SET, all exemptions, penalty propagation, saturation, same-day revert) and is green; contracts/ unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proving-test assertion -> v61 invariant | A too-weak assertion passes while the invariant is actually violated (false certification of TST-01/02/03) |
| seeded fuzz -> property coverage | A property test that never exercises the boundary (e.g. the both-short revert, the cap saturation) certifies a path it never ran |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-378-04-01 | Information Disclosure | TST-01 waterfall assertion | mitigate | The draw ordering is asserted by reading both balances + the exact AfkingSpent amount via expectEmit; a falsifiability check (inverting an expected value fails a test) is required in the SUMMARY |
| T-378-04-02 | Information Disclosure | TST-02 solvency-identity assertion | mitigate | Non-interference is proven by reading the raw packed slot and splitting it (not just trusting the accessor); the claimablePool==Σ identity runs under a seeded credit/debit sequence |
| T-378-04-03 | Information Disclosure | TST-03 exemption assertion | mitigate | Each exemption is proven by contrast (exempt==0 vs non-exempt==2), so a removed bail would flip the contrast and fail; the cap saturation explicitly asserts no uint8 wrap |
| T-378-04-SC | Tampering | forge artifact regeneration | mitigate | acceptance criteria assert `git status --porcelain contracts/` empty |
</threat_model>

<verification>
- V61AfpayWaterfall.t.sol green — TST-01 (3 pay-kinds, AfkingSpent, both-short revert, fresh-affiliate-no-bonus, no double-draw).
- V61Pack.t.sol green — TST-02 (round-trip, claimablePool==Σ, gameOver preservation, no carry, two-mapping equivalence).
- V61CurseSet.t.sol green — TST-03 (SET, exemptions, penalty propagation, saturation, same-day revert).
- Assertions proven falsifiable (would fail under a seeded violation).
- contracts/*.sol byte-identical to HEAD.
</verification>

<success_criteria>
- The three proving tests pass and each genuinely proves its surface (TST-01/02/03) with falsifiable assertions.
- contracts/*.sol byte-identical to HEAD (zero contract edits).
</success_criteria>

<output>
Create `.planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-04-SUMMARY.md` when done
</output>
