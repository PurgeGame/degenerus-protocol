---
phase: 378-tst-proving-tests-rng-freeze-solvency
plan: 06
type: execute
wave: 6
depends_on: ["378-05"]
files_modified:
  - test/fuzz/V61RngFreezeIntact.t.sol
  - test/fuzz/invariant/V61SolvencyAfpay.inv.t.sol
  - test/fuzz/handlers/V61AfkingSpendHandler.sol
autonomous: true
requirements: [SEC-01, SEC-02]
must_haves:
  truths:
    - "SEC-01: no AFPAY/PACK/CURSE/SMITE path reads rngWord or any VRF-derived state in a player-manipulable window; the changes are ledger/activity-score/curse-counter only; proven empirically by determinism"
    - "SEC-02: claimablePool == Σ(claimable + afking halves of balancesPacked) holds across the AFPAY waterfall + PACK repack + curse/smite; claimablePool never exceeds bal + stETH after an afking-funded buy / packed credit-debit / stale cashout / smite"
  artifacts:
    - path: "test/fuzz/V61RngFreezeIntact.t.sol"
      provides: "SEC-01 RNG-freeze determinism proof for the v61 surfaces"
      contains: "prevrandao"
    - path: "test/fuzz/invariant/V61SolvencyAfpay.inv.t.sol"
      provides: "SEC-02 SOLVENCY-01 invariant across the new afking spend paths"
      contains: "claimablePool"
    - path: "test/fuzz/handlers/V61AfkingSpendHandler.sol"
      provides: "Invariant handler exercising the afking-funded spend + curse/smite paths"
      contains: "afking"
  key_links:
    - from: "test/fuzz/invariant/V61SolvencyAfpay.inv.t.sol"
      to: "test/fuzz/handlers/V61AfkingSpendHandler.sol"
      via: "targetContract + ghost accounting"
      pattern: "targetContract"
    - from: "test/fuzz/V61RngFreezeIntact.t.sol"
      to: "AFPAY/CURSE/SMITE determinism"
      via: "perturbed prevrandao/coinbase yields identical ledger/score outcome"
      pattern: "prevrandao|vm.roll"
---

<objective>
Prove the v61 hard security floor empirically: SEC-01 (RNG-freeze intact — no new player-manipulable VRF-derived read/write across AFPAY/PACK/CURSE/SMITE) and SEC-02 (SOLVENCY-01 re-attested — `claimablePool == Σ(claimable + afking halves)` holds across the new afking spend paths, and `claimablePool` never exceeds `bal + stETH`).

Purpose: These are the two USER-locked hard-floor requirements (the audit's dominant threat = RNG/freeze; the spine = solvency). The v61 changes touch the claimable/afking ledger (AFPAY/PACK) and the activity-score/curse-counter slots (CURSE/SMITE) — none of which should read `rngWord` or break the master solvency identity. This plan proves both empirically (determinism for SEC-01, an invariant campaign for SEC-02) so the TERMINAL close (379) carries them as proven, not asserted.

Output: V61RngFreezeIntact.t.sol (SEC-01), V61SolvencyAfpay.inv.t.sol + V61AfkingSpendHandler.sol (SEC-02) — all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-01-RECALIBRATION-KEY.md

<interfaces>
<!-- The hard-floor anchors (confirmed in source). -->

SEC-01 RNG-freeze:
  - The master freeze property: every variable interacting with a VRF word must be frozen [rng request -> unlock]
    vs players (advanceGame exempt). The v61 surfaces to clear:
    AFPAY (_settleShortfall, _processMintPayment) — pure ledger (claimable/afking/claimablePool); reads no rngWord
    PACK (balancesPacked accessors) — pure ledger; reads no rngWord
    CURSE (_maybeCurse, _playerActivityScore penalty, decurse) — touches only activity-score / curse-counter slots; reads no rngWord
    SMITE (smite) — pure ledger/score + BURNIE burn; reads no rngWord, no prize-pool, no ETH
  - grep-confirmed: the only rngWord references in GameAfkingModule are in the afking-BOX path (out of v61 scope,
    documented freeze-safe); the curse/smite/settle functions do NOT read rngWord
  - SEC-01 model: the determinism proof in test/fuzz/V56FreezeSolvency.t.sol (Leg 3) +
    test/fuzz/RngFreezeAndRemovalProofs.t.sol — perturb prevrandao/coinbase + vm.roll/warp, assert identical outcome

SEC-02 SOLVENCY-01:
  - The master invariant (DegenerusGame.sol:18): `address(this).balance + steth.balanceOf(this) >= claimablePool`
  - The identity (DegenerusGameStorage.sol:358): `claimablePool == Σ (claimable + afking halves of balancesPacked[*])`
  - the AFPAY debits pair claimablePool -= with each draw (DegenerusGame.sol:1140 claimable, :1150 afking; pool-neutral)
  - the call-site pairing (USER-approved: pairing at call sites, not in the accessor) maintains the identity
  - SEC-02 model: the invariant pattern in test/fuzz/invariant/EthSolvency.inv.t.sol
    (invariant_ethSolvency + ghost accounting) with a GameHandler/WhaleHandler; NO existing handler exercises
    afking-spend -> this plan adds a V61AfkingSpendHandler that drives afking-funded buys + curse/smite
</interfaces>

<handler_template>
<!-- Invariant handler pattern (from test/fuzz/handlers/GameHandler.sol). -->
- ghost_ accumulators (ghost_totalDeposited / ghost_totalClaimed style)
- useActor(seed) modifier; bounded action methods (purchase/advanceGame/claimWinnings analogues)
- the V61 handler adds: an afking-funded buy (seed afking balance, buy with a shortfall), a stale-cashout
  (drive >=5d idle, claimWinnings -> curse), a smite (deity burns to stack the counter)
- registered via targetContract in the invariant test's setUp
</handler_template>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SEC-01 — V61RngFreezeIntact.t.sol (RNG-freeze determinism for the v61 surfaces)</name>
  <behavior>
    - an AFPAY waterfall spend (DirectEth/Claimable/Combined with a shortfall) produces a byte-identical ledger outcome (same claimable/afking/claimablePool deltas, same AfkingSpent amount) when replayed at a DIFFERENT block with perturbed prevrandao/coinbase — i.e. the spend carries no VRF/block entropy
    - a cashout-curse SET produces an identical curseCount + activity-score outcome under perturbed prevrandao/coinbase
    - a smite produces an identical curseCount outcome under perturbed prevrandao/coinbase
    - the curse*100 bps activity-score penalty is deterministic (a function of curseCount only, no RNG)
    - none of these paths' observable effects change when the VRF word / block randomness changes (the freeze property holds — the surfaces are RNG-insensitive)
  </behavior>
  <read_first>
    - .planning/REQUIREMENTS.md (SEC-01 acceptance text + the v45 VRF-freeze north-star)
    - contracts/storage/DegenerusGameStorage.sol (lines ~857-879 _settleShortfall; ~892-928 the accessors — confirm no rngWord read)
    - contracts/modules/DegenerusGameMintStreakUtils.sol (lines ~320-379 the curse penalty + _maybeCurse + curseCountOf — confirm no rngWord read)
    - contracts/modules/GameAfkingModule.sol (lines ~1696-1726 decurse/smite — confirm no rngWord read; the rngWordByDay references are the OUT-OF-SCOPE afking-box path)
    - test/fuzz/V56FreezeSolvency.t.sol (Leg 3 — the prevrandao/coinbase perturbation + vm.roll/warp + byte-identity oracle to reuse)
    - test/fuzz/RngFreezeAndRemovalProofs.t.sol (the freeze-proof harness pattern)
  </read_first>
  <action>
    Write V61RngFreezeIntact.t.sol. For each v61 surface (AFPAY spend, cashout-curse SET, smite), execute the operation twice from an identical seeded state but at two different blocks with perturbed `vm.prevrandao` / `vm.coinbase` / `vm.roll` / `vm.warp` between the runs, and assert the observable outcome is byte-identical: the claimable/afking/claimablePool deltas and the AfkingSpent amount for AFPAY; the curseCount and the resulting activity score for CURSE/SMITE. This proves the surfaces carry no VRF/block entropy in a player-manipulable window (the freeze property). Add a determinism assertion that the curse*100 bps penalty is a pure function of curseCount (same curseCount -> same penalty regardless of block state).

    Optionally add a source-grep attestation leg (the repo uses vm.readFile against ./contracts per foundry.toml fs_permissions) asserting the curse/smite/settle function bodies contain no `rngWord` token — a static guard complementing the dynamic determinism proof. Keep the dynamic proof as the primary evidence.

    Assertions must be falsifiable: if any v61 surface DID read block/VRF entropy, the two-block replay would diverge and the test would fail.
  </action>
  <verify>
    <automated>forge test --match-path "test/fuzz/V61RngFreezeIntact.t.sol" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-path test/fuzz/V61RngFreezeIntact.t.sol` reports all tests passing, 0 failed
    - The file proves AFPAY, CURSE, and SMITE outcomes are byte-identical across a two-block replay with perturbed prevrandao/coinbase (the freeze property)
    - The curse penalty determinism (pure function of curseCount) is asserted
    - The two-block replay is the primary evidence (a surface reading block entropy would diverge and fail); any source-grep leg is complementary, not the sole proof
    - `git status --porcelain contracts/` is EMPTY (contracts/*.sol byte-identical)
  </acceptance_criteria>
  <done>V61RngFreezeIntact.t.sol proves SEC-01 (AFPAY/PACK/CURSE/SMITE RNG-freeze intact via two-block determinism) and is green; contracts/ unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SEC-02 — V61AfkingSpendHandler.sol + V61SolvencyAfpay.inv.t.sol (SOLVENCY-01 across afking spend)</name>
  <behavior>
    - claimablePool == Σ(claimable + afking halves of balancesPacked) holds after every action in a random {afking-funded buy, packed credit/debit, stale cashout, smite, advance} sequence
    - claimablePool never exceeds address(game).balance + steth.balanceOf(game) after an afking-funded buy, a packed credit-debit, a stale cashout, and a smite
    - each AFPAY afking/claimable debit decrements claimablePool by exactly the drawn amount (pool-neutral; contract balance unchanged)
    - curse/smite are OFF the ETH path -> they move claimablePool by zero (a smite/curse leaves the solvency identity byte-unchanged)
  </behavior>
  <read_first>
    - .planning/REQUIREMENTS.md (SEC-02 acceptance text)
    - .planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-01-RECALIBRATION-KEY.md (the balancesPacked root slot + claimablePool slot, to compute Σ across players)
    - contracts/DegenerusGame.sol (lines ~18 the master invariant; ~1140/1150 the paired claimablePool -= debits)
    - contracts/storage/DegenerusGameStorage.sol (lines ~358 the identity; ~857-928 _settleShortfall + accessors)
    - test/fuzz/invariant/EthSolvency.inv.t.sol (the invariant + ghost-accounting + targetContract pattern to mirror)
    - test/fuzz/handlers/GameHandler.sol (the handler template — ghost_ accumulators, useActor modifier, bounded actions)
    - contracts/modules/GameOverModule.sol (the gameOver zeroing — the identity must hold through it)
  </read_first>
  <action>
    Write V61AfkingSpendHandler.sol mirroring GameHandler: ghost accumulators + a useActor modifier + bounded actions that drive the NEW v61 spend paths — (1) an afking-funded buy (seed an afking balance, execute a buy with a shortfall so afking is drawn), (2) a packed credit/debit (via the accessors / a real credit+spend), (3) a stale cashout (drive an actor >=5d idle, claimWinnings to trigger the curse), (4) a smite (a deity burns BURNIE to stack the counter), (5) an advance. Track ghost deposits/draws.

    Write V61SolvencyAfpay.inv.t.sol registering the handler via targetContract, with two invariants: `invariant_v61PoolEqualsSumOfHalves` (claimablePool == Σ over all tracked players of (claimable low half + afking high half of balancesPacked), read via the slot per the 378-01 key) and `invariant_v61PoolNeverExceedsBacking` (claimablePool <= address(game).balance + steth.balanceOf(game)). Run under the [invariant] profile (runs=256, depth=128). Add focused (non-fuzz) assertions for the four named scenarios from SEC-02 (afking-funded buy, packed credit-debit, stale cashout, smite) proving each leaves the identity intact and curse/smite move the pool by zero.

    The invariants must be ones that would genuinely FAIL under a seeded violation: e.g. an afking debit that forgot its paired `claimablePool -=` would break invariant_v61PoolEqualsSumOfHalves. Note this falsifiability reasoning in the SUMMARY.
  </action>
  <verify>
    <automated>forge test --match-path "test/fuzz/invariant/V61SolvencyAfpay.inv.t.sol" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-path test/fuzz/invariant/V61SolvencyAfpay.inv.t.sol` reports the invariants passing, 0 failed (under the [invariant] profile runs/depth)
    - V61AfkingSpendHandler.sol exercises all four SEC-02 spend paths (afking-funded buy, packed credit/debit, stale cashout, smite) plus advance, with ghost accounting
    - The invariants are `claimablePool == Σ(half sums)` AND `claimablePool <= bal + stETH`, computed from the real slots per the 378-01 key (not from a parallel mirror that could drift)
    - Focused assertions cover the four named scenarios and prove curse/smite are pool-neutral (move claimablePool by zero)
    - The invariant is demonstrably falsifiable (an unpaired debit would break the half-sum identity — reasoning noted in the SUMMARY)
    - `git status --porcelain contracts/` is EMPTY (contracts/*.sol byte-identical)
  </acceptance_criteria>
  <done>V61SolvencyAfpay.inv.t.sol + V61AfkingSpendHandler.sol prove SEC-02 (the half-sum identity + the backing bound across all afking spend paths) and are green; contracts/ unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| determinism replay -> freeze property | A replay that does not actually perturb prevrandao/coinbase (or replays the SAME block) cannot detect a VRF-entropy read — a false SEC-01 pass |
| invariant assertion -> solvency identity | An invariant computed from a parallel mirror (not the real slots) can drift green while the contract identity is broken — a false SEC-02 pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-378-06-01 | Information Disclosure | SEC-01 freeze determinism | mitigate | The replay genuinely perturbs prevrandao/coinbase + vm.roll/warp between runs and asserts byte-identical outcomes; a surface reading block/VRF entropy would diverge and fail; a complementary source-grep attests no rngWord token in the bodies |
| T-378-06-02 | Information Disclosure | SEC-02 solvency identity | mitigate | The half-sum identity is computed from the real balancesPacked slot + claimablePool slot (per the 378-01 key), not a mirror; the invariant would fail if a debit dropped its paired claimablePool -= |
| T-378-06-03 | Spoofing | RNG/freeze (USER dominant threat) | mitigate | SEC-01 is proven empirically (determinism) across all four v61 surfaces, satisfying the USER-locked dominant-threat floor; advanceGame remains the only RNG-exempt path |
| T-378-06-SC | Tampering | forge artifact regeneration | mitigate | acceptance criteria assert `git status --porcelain contracts/` empty |
</threat_model>

<verification>
- V61RngFreezeIntact.t.sol green — SEC-01 (AFPAY/PACK/CURSE/SMITE byte-identical under perturbed block/VRF state).
- V61SolvencyAfpay.inv.t.sol + V61AfkingSpendHandler.sol green — SEC-02 (the half-sum identity + the bal+stETH bound across afking spend paths).
- Proofs computed from real slots / genuine perturbation (falsifiable), not mirrors.
- contracts/*.sol byte-identical to HEAD.
</verification>

<success_criteria>
- SEC-01 proven: no v61 surface reads VRF-derived state in a player-manipulable window (empirical determinism).
- SEC-02 proven: `claimablePool == Σ(halves)` and `claimablePool <= bal + stETH` hold across the AFPAY waterfall + PACK repack + curse/smite.
- contracts/*.sol byte-identical to HEAD (zero contract edits).
</success_criteria>

<output>
Create `.planning/phases/378-tst-proving-tests-rng-freeze-solvency/378-06-SUMMARY.md` when done
</output>
