---
phase: 228
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-01-CURSOR-REORG-TRACE.md
  - .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-01-SUMMARY.md
autonomous: true
requirements: [IDX-04]
requirements_addressed: [IDX-04]
finding_id_block: F-28-228-01+
absorbs_227_deferrals: [1, 2, 4]
severity_taxonomy: [INFO, LOW, MEDIUM]
tags: [IDX-04, cursor, reorg, state-machine, catalog-audit, read-only]
must_haves:
  truths:
    - "228-01-CURSOR-REORG-TRACE.md enumerates every observable state transition in cursor-manager.ts and reorg-detector.ts with a PASS/FAIL verdict per transition"
    - "All 11 M-matrix rows assigned to 228-01 (M1, M2, M3, M4, M5, M6, M7, M8, M13, M14 plus intra-batch reorg edge case) appear verbatim as audit rows"
    - "All 3 Phase 227 deferrals assigned to 228-01 (#1 cursor rewind, #2 reorg overwrite ordering, #4 backfill gate boundary) appear as explicit labelled rows with '227-deferral-N' annotation"
    - "Every FAIL row emits an F-28-228-NN finding stub starting at F-28-228-01 (no overlap with 228-02's F-28-228-101+ block)"
    - "Every finding File:line cites an absolute path under /home/zak/Dev/PurgeGame/database/"
    - "Zero writes to /home/zak/Dev/PurgeGame/database/ (git status clean in that repo)"
    - "SC-4 file-touch evidence: cursor-manager.ts, reorg-detector.ts, main.ts, block-fetcher.ts, purge-block-range.ts cited at least once in the deliverable"
  artifacts:
    - path: ".planning/phases/228-cursor-reorg-view-refresh-state-machines/228-01-CURSOR-REORG-TRACE.md"
      provides: "Per-state-transition PASS/FAIL trace for cursor-manager.ts + reorg-detector.ts with F-28-228-NN finding stubs"
      contains: "| State transition | File:line | Verdict |"
    - path: ".planning/phases/228-cursor-reorg-view-refresh-state-machines/228-01-SUMMARY.md"
      provides: "Verdict counts, findings consumed, next available F-28-228-NN, handoff signal to Phase 229"
  key_links:
    - from: "228-01-CURSOR-REORG-TRACE.md audit rows"
      to: "/home/zak/Dev/PurgeGame/database/src/indexer/cursor-manager.ts + reorg-detector.ts + main.ts"
      via: "File:line citations for every verdict"
      pattern: "database/src/indexer/(cursor-manager|reorg-detector|main|block-fetcher|purge-block-range)\\.ts:[0-9]+"
    - from: "M-matrix rows M1..M8,M13,M14"
      to: "228-01-CURSOR-REORG-TRACE.md row IDs"
      via: "Verbatim row copy from 228-RESEARCH.md §Documented-Invariant vs Code-Behavior Matrix"
      pattern: "M[0-9]+"
---

<objective>
IDX-04 — Produce `228-01-CURSOR-REORG-TRACE.md`, a per-state-transition PASS/FAIL trace across `cursor-manager.ts` and `reorg-detector.ts` (wired through `main.ts`), covering documented AND undocumented transitions per D-228-07. Absorbs Phase 227 deferrals #1 (cursor-manager.ts:45 rewind guardrails), #2 (reorg-detector.ts:33 overwrite ordering), and #4 (main.ts:211 backfill gate `<=` boundary) per D-228-09.

Purpose: close the IDX-04 behavioral audit surface; feed Phase 229 with F-28-228-NN finding stubs.
Output: `228-01-CURSOR-REORG-TRACE.md` + `228-01-SUMMARY.md`.

Per D-228-01: cross-repo READ-only. Zero writes to `/home/zak/Dev/PurgeGame/database/`.
Per D-228-02: catalog-only — no runtime gate, no CI.
Per D-228-10: finding IDs start at `F-28-228-01` (disjoint from 228-02's `F-28-228-101+` block).
Per D-228-11: severity ∈ {INFO, LOW, MEDIUM}; HIGH/CRITICAL reserved for Phase 229 promotion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/228-cursor-reorg-view-refresh-state-machines/228-CONTEXT.md
@.planning/phases/228-cursor-reorg-view-refresh-state-machines/228-RESEARCH.md
@.planning/phases/228-cursor-reorg-view-refresh-state-machines/228-VALIDATION.md
@.planning/phases/227-indexer-event-processing-correctness/227-03-SUMMARY.md

<interfaces>
M-matrix rows pre-assigned to 228-01 (copied verbatim from 228-RESEARCH.md §Documented-Invariant vs Code-Behavior Matrix). Every row below MUST appear as an explicit audit row in `228-01-CURSOR-REORG-TRACE.md` with final verdict + rationale. Expected verdicts are priors, not binding — the executor re-verifies and may override with evidence.

| # | File:line | Claim (comment or docstring) | Code behavior | Expected verdict | Plan |
|---|-----------|------------------------------|---------------|------------------|------|
| M1 | cursor-manager.ts:45 | "handle re-initialization safely" | Unconditional overwrite; no rewind guard | LOW (comment overclaims); behavioral PASS — no live caller rewinds healthy cursor | 228-01 |
| M2 | cursor-manager.ts:70-72 | "MUST be called inside a database transaction" | Type signature accepts `DbOrTx`; no runtime enforcement | INFO (Tier B — wording accurate but enforcement is convention-only) | 228-01 |
| M3 | reorg-detector.ts:33 | "ON CONFLICT DO UPDATE on blockNumber for idempotent re-processing" | Overwrites hash/parentHash/timestamp/eventCount | PASS construct. Behavioral PASS for ordering (see M4). | 228-01 |
| M4 | reorg-detector.ts (undocumented invariant) | (No comment) — rollback must precede storeBlock on reorg'd block | `main.ts:155-161` guarantees `continue` after rollback | PASS — strict ordering holds for same-iteration. Intra-batch mid-range reorg: MEDIUM-risk edge case; likely mitigated by `config.confirmations`. | 228-01 |
| M5 | reorg-detector.ts:26-27 | `MAX_REORG_DEPTH = 128` | Loop bound at :113; throw at :140 | PASS | 228-01 |
| M6 | reorg-detector.ts:73-76 | Walk-back semantics (compare stored vs rpc at checkBlock+1) | :113-137 implements exactly as described | PASS | 228-01 |
| M7 | reorg-detector.ts:149 "all within a single database transaction" | db.transaction! wraps purge + cursor reset | PASS | 228-01 |
| M8 | main.ts:211 / view-refresh.ts:4 "skip during backfill" | `lag <= config.batchSize` inclusive boundary | PASS (see §Backfill-Gate) | 228-01 |
| M13 | ROADMAP IDX-04 claim "recovery-after-stall" | No stall detector; only 5-second catch-retry at main.ts:216-220 | INFO/LOW — requirement claims capability not present in code; RESOLVED-DOC or RESOLVED-CODE-FUTURE | 228-01 |
| M14 | REQUIREMENTS IDX-04 "gap handling" | No explicit gap detection; `batchStart = cursor + 1` never skips | INFO (implicit by construction — continuous cursor advance = no gaps); PASS | 228-01 |

Phase 227 deferrals absorbed into 228-01 (from 227-03-SUMMARY.md §Phase 228 Handoff):

| # | File:line | Deferred concern | 228-01 row label |
|---|-----------|------------------|------------------|
| 227-deferral-1 | `indexer/cursor-manager.ts:45` | `initializeCursor` rewind guardrail | Row overlaps M1 — annotate row with "227-deferral-1" |
| 227-deferral-2 | `indexer/reorg-detector.ts:33` | `storeBlock` ON CONFLICT ordering | Row overlaps M3/M4 — annotate with "227-deferral-2" |
| 227-deferral-4 | `indexer/main.ts:211` | Backfill gate `<=` boundary | Row overlaps M8 — annotate with "227-deferral-4" |

Control-flow anchors (from 228-RESEARCH.md §Control-Flow Map, all verified 2026-04-15):
- Cursor read — main.ts:97 (getCursorPosition)
- Cursor init (absent) — main.ts:103 (initializeCursor call a)
- Reindex cursor reset — main.ts:132-133 (initializeCursor call b)
- Reindex cursor restore — main.ts:225 (initializeCursor call c)
- Follow-wait guard — main.ts:143-147
- Reorg detect — main.ts:155
- Reorg rollback — main.ts:158
- Process batch tx — main.ts:188-201 (processBlockBatch → storeBlock + advanceCursor)
- Backfill-gate — main.ts:211-213
- Error catch + 5s retry — main.ts:216-220

Assumptions to verify (from 228-RESEARCH.md §Assumptions Log):
- A1: `processBlockBatch` (in event-processor.ts) invokes `storeBlock`
- A2: `processBlockBatch` invokes `advanceCursor` (confirming transactional cursor advance)
- A3: `config.confirmations` default value (calibrates M4 MEDIUM-severity edge case)

Severity thresholds (D-228-11):
- INFO: documented behavior matches; minor comment drift
- LOW: behavior diverges from comment but no data-integrity risk
- MEDIUM: behavior could cause silent data corruption or missed reorg recovery
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed 228-01-CURSOR-REORG-TRACE.md with the 10 pre-assigned M-rows + 3 absorbed 227 deferrals</name>
  <files>.planning/phases/228-cursor-reorg-view-refresh-state-machines/228-01-CURSOR-REORG-TRACE.md</files>
  <read_first>
    - /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/228-cursor-reorg-view-refresh-state-machines/228-RESEARCH.md (FULL file — §Cursor State Machine, §Reorg-Detector State Machine, §Documented-Invariant vs Code-Behavior Matrix, §Control-Flow Map, §Backfill-Gate Boundary, §Assumptions Log)
    - /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/228-cursor-reorg-view-refresh-state-machines/228-CONTEXT.md (all D-228-NN decisions)
    - /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/227-indexer-event-processing-correctness/227-03-SUMMARY.md (§Phase 228 Handoff — 4 deferrals)
    - /home/zak/Dev/PurgeGame/database/src/indexer/cursor-manager.ts (full file)
    - /home/zak/Dev/PurgeGame/database/src/indexer/reorg-detector.ts (full file)
  </read_first>
  <action>
Create `228-01-CURSOR-REORG-TRACE.md` with this exact top-matter and scaffold. Copy every M-row in `<interfaces>` into the `Audit Rows` table VERBATIM (File:line, Claim, Code behavior, Expected verdict columns) — do NOT shorten or paraphrase. Final Verdict + Rationale are left blank for Task 2.

```markdown
# 228-01 Cursor & Reorg State-Machine Trace (IDX-04)

**Phase:** 228 | **Plan:** 01 | **Requirement:** IDX-04
**Deliverable for:** D-228-06 (228-01 deliverable)
**Finding block:** F-28-228-01+ (per D-228-10; disjoint from 228-02's F-28-228-101+)
**Severity taxonomy:** INFO / LOW / MEDIUM (per D-228-11)
**Scope:** cross-repo READ-only per D-228-01; catalog-only per D-228-02.
**Audit surface:** `/home/zak/Dev/PurgeGame/database/src/indexer/{cursor-manager,reorg-detector,main,block-fetcher,purge-block-range}.ts`

## Absorbed Phase 227 Deferrals (D-228-09)

| # | 227 deferral | File:line | Handled by row |
|---|--------------|-----------|----------------|
| 1 | cursor-manager.ts:45 `initializeCursor` rewind guardrail | `/home/zak/Dev/PurgeGame/database/src/indexer/cursor-manager.ts:45` | M1 (annotated `227-deferral-1`) |
| 2 | reorg-detector.ts:33 `storeBlock` ON CONFLICT ordering | `/home/zak/Dev/PurgeGame/database/src/indexer/reorg-detector.ts:33` | M3 + M4 (annotated `227-deferral-2`) |
| 4 | main.ts:211 backfill gate `<=` boundary | `/home/zak/Dev/PurgeGame/database/src/indexer/main.ts:211` | M8 (annotated `227-deferral-4`) |

## Cursor-Manager Observable States (from 228-RESEARCH.md §Cursor State Machine)

[Copy the "Observable states" table verbatim from 228-RESEARCH.md — the table with columns State / Definition / Entry transition / Exit transition / Anchor.]

## Reorg-Detector Observable States

[Copy the "Observable states" table verbatim from 228-RESEARCH.md §Reorg-Detector State Machine.]

## Audit Rows (M-matrix — verbatim from 228-RESEARCH.md)

| Row ID | Annotations | File:line | Claim | Code behavior | Expected verdict | Final verdict | Rationale | Finding ID (if FAIL) |
|--------|-------------|-----------|-------|---------------|------------------|---------------|-----------|----------------------|
| M1 | 227-deferral-1 | /home/zak/Dev/PurgeGame/database/src/indexer/cursor-manager.ts:45 | "handle re-initialization safely" | Unconditional overwrite; no rewind guard | LOW (comment overclaims); behavioral PASS | TBD-Task2 | TBD | TBD |
| M2 | — | /home/zak/Dev/PurgeGame/database/src/indexer/cursor-manager.ts:70-72 | "MUST be called inside a database transaction" | Type signature accepts `DbOrTx`; no runtime enforcement | INFO (Tier B) | TBD | TBD | TBD |
| M3 | 227-deferral-2 | /home/zak/Dev/PurgeGame/database/src/indexer/reorg-detector.ts:33 | "ON CONFLICT DO UPDATE on blockNumber for idempotent re-processing" | Overwrites hash/parentHash/timestamp/eventCount | PASS construct | TBD | TBD | TBD |
| M4 | 227-deferral-2 | /home/zak/Dev/PurgeGame/database/src/indexer/reorg-detector.ts (undocumented invariant) | (No comment) — rollback must precede storeBlock on reorg'd block | main.ts:155-161 guarantees `continue` after rollback | PASS — intra-batch edge case MEDIUM-risk | TBD | TBD | TBD |
| M5 | — | /home/zak/Dev/PurgeGame/database/src/indexer/reorg-detector.ts:26-27 | `MAX_REORG_DEPTH = 128` | Loop bound at :113; throw at :140 | PASS | TBD | TBD | TBD |
| M6 | — | /home/zak/Dev/PurgeGame/database/src/indexer/reorg-detector.ts:73-76 | Walk-back semantics (compare stored vs rpc at checkBlock+1) | :113-137 implements exactly as described | PASS | TBD | TBD | TBD |
| M7 | — | /home/zak/Dev/PurgeGame/database/src/indexer/reorg-detector.ts:149 | "all within a single database transaction" | db.transaction! wraps purge + cursor reset | PASS | TBD | TBD | TBD |
| M8 | 227-deferral-4 | /home/zak/Dev/PurgeGame/database/src/indexer/main.ts:211 | "skip during backfill" | `lag <= config.batchSize` inclusive boundary | PASS | TBD | TBD | TBD |
| M13 | — | ROADMAP IDX-04 | "recovery-after-stall" | No stall detector; only 5-second catch-retry at main.ts:216-220 | INFO/LOW — RESOLVED-DOC or RESOLVED-CODE-FUTURE | TBD | TBD | TBD |
| M14 | — | REQUIREMENTS IDX-04 | "gap handling" | No explicit gap detection; `batchStart = cursor + 1` never skips | INFO / PASS by construction | TBD | TBD | TBD |
| E1 | intra-batch-edge | /home/zak/Dev/PurgeGame/database/src/indexer/main.ts:155-201 | (undocumented) — reorg occurring mid-batch between detectReorg(:155) and processBlockBatch(:188) could storeBlock a canonical row without rollback | Mitigation relies on `config.confirmations` — see A3 | MEDIUM unless confirmations ≥ 1 default | TBD | TBD | TBD |

**Note:** Row E1 is the intra-batch reorg edge case surfaced in 228-RESEARCH.md §Reorg-Detector State Machine M4 trace. It must be audited explicitly and its severity calibrated against the verified `config.confirmations` default (Assumption A3).

## Assumption-Check Worklist (must resolve before Task 2 closes)

| # | Assumption | How to verify | Resolution |
|---|------------|---------------|------------|
| A1 | `processBlockBatch` invokes `storeBlock` | `rg -n 'storeBlock' /home/zak/Dev/PurgeGame/database/src/indexer/event-processor.ts` | TBD |
| A2 | `processBlockBatch` invokes `advanceCursor` inside a tx | `rg -n 'advanceCursor' /home/zak/Dev/PurgeGame/database/src/indexer/event-processor.ts` | TBD |
| A3 | `config.confirmations` default value ≥ 1 | Read `/home/zak/Dev/PurgeGame/database/src/config/chains.ts` (or equivalent config module) | TBD |

## SC-4 File-Touch Evidence

| Indexer file | Touched by 228-01? | Where |
|--------------|---------------------|-------|
| cursor-manager.ts | ✓ | M1, M2 |
| reorg-detector.ts | ✓ | M3, M4, M5, M6, M7 |
| main.ts | ✓ | M8, E1, M13 (control-flow trace), assumption-check worklist |
| block-fetcher.ts | TBD | Task 2 verifies fetchLogs call site |
| purge-block-range.ts | TBD | Task 2 verifies rollbackToBlock → PURGEABLE_TABLES delete |
```

Do NOT fill in Final Verdict / Rationale / Finding ID columns — Task 2 does that. Task 1 produces the SCAFFOLD with verbatim M-rows so that Task 2 has a fixed target.
  </action>
  <verify>
    <automated>test -f .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-01-CURSOR-REORG-TRACE.md && grep -cE '^\| M(1|2|3|4|5|6|7|8|13|14) ' .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-01-CURSOR-REORG-TRACE.md | awk '{ if ($1 < 10) exit 1 }' && grep -q 'E1' .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-01-CURSOR-REORG-TRACE.md && grep -q '227-deferral-1' .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-01-CURSOR-REORG-TRACE.md && grep -q '227-deferral-2' .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-01-CURSOR-REORG-TRACE.md && grep -q '227-deferral-4' .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-01-CURSOR-REORG-TRACE.md</automated>
  </verify>
  <acceptance_criteria>
    - File `228-01-CURSOR-REORG-TRACE.md` exists.
    - All 10 M-rows assigned to 228-01 (M1, M2, M3, M4, M5, M6, M7, M8, M13, M14) appear in the Audit Rows table with columns copied VERBATIM from 228-RESEARCH.md (grep each row ID returns ≥1 hit).
    - Row E1 (intra-batch reorg edge case) appears in the Audit Rows table.
    - All 3 absorbed 227 deferrals appear as annotations (`227-deferral-1`, `227-deferral-2`, `227-deferral-4`) on the correct rows.
    - Every File:line citation is an absolute path rooted at `/home/zak/Dev/PurgeGame/database/` (or ROADMAP / REQUIREMENTS for M13/M14 requirement-origin rows).
    - Cursor-manager and reorg-detector Observable States tables copied verbatim from RESEARCH.
    - Assumption-Check Worklist contains A1, A2, A3 rows with "TBD" resolution.
    - SC-4 File-Touch Evidence table present with 5 indexer-file rows.
    - Zero writes outside `.planning/phases/228-cursor-reorg-view-refresh-state-machines/` (verify: `git -C /home/zak/Dev/PurgeGame/database status` clean).
  </acceptance_criteria>
  <done>Scaffold committed with all 10 M-rows + E1 + 3 deferral annotations verbatim from RESEARCH.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve assumptions A1/A2/A3, fill every row's Final Verdict + Rationale, emit F-28-228-NN finding stubs for FAIL rows</name>
  <files>
    .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-01-CURSOR-REORG-TRACE.md
    .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-01-SUMMARY.md
  </files>
  <read_first>
    - The 228-01-CURSOR-REORG-TRACE.md scaffold produced by Task 1
    - /home/zak/Dev/PurgeGame/database/src/indexer/cursor-manager.ts (full file)
    - /home/zak/Dev/PurgeGame/database/src/indexer/reorg-detector.ts (full file)
    - /home/zak/Dev/PurgeGame/database/src/indexer/main.ts (full file, lines 78-232 are the relevant orchestration)
    - /home/zak/Dev/PurgeGame/database/src/indexer/event-processor.ts (needed to resolve assumptions A1 + A2 — locate `processBlockBatch`, confirm it calls `storeBlock` and `advanceCursor`)
    - /home/zak/Dev/PurgeGame/database/src/indexer/block-fetcher.ts (full file — SC-4 touch evidence)
    - /home/zak/Dev/PurgeGame/database/src/indexer/purge-block-range.ts (full file — SC-4 touch evidence + rollbackToBlock detail)
    - /home/zak/Dev/PurgeGame/database/src/config/chains.ts OR equivalent (resolve A3: `config.confirmations` default)
    - /home/zak/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
Step A — Resolve assumptions A1/A2/A3 (fill Assumption-Check Worklist):

```
rg -n 'storeBlock|advanceCursor' /home/zak/Dev/PurgeGame/database/src/indexer/event-processor.ts
rg -n 'confirmations' /home/zak/Dev/PurgeGame/database/src/config/
rg -n 'confirmations' /home/zak/Dev/PurgeGame/database/src/indexer/main.ts
```

- A1: confirm `storeBlock` is invoked per block inside `processBlockBatch`; record the File:line.
- A2: confirm `advanceCursor` is called inside the same `db.transaction` as event inserts; record File:line.
- A3: record the numeric default of `config.confirmations`. If confirmations ≥ 1, E1's MEDIUM severity downgrades to INFO (with rationale: "confirmations window closes the mid-batch reorg edge case"). If confirmations === 0 in any chain config, E1 remains MEDIUM and emits a finding.

If A1 or A2 resolve negative (processBlockBatch does NOT call storeBlock / advanceCursor as assumed), the M4 ordering argument changes — re-derive the verdict before writing it.

Step B — For every row (M1..M14 assigned to 228-01, plus E1), fill:
- **Final verdict** — PASS / FAIL / PASS-with-INFO / PASS-with-LOW / FAIL-MEDIUM.
- **Rationale** — ≤3 sentences citing File:line evidence. Reference the enclosing function body, not just the claim line.
- **Finding ID** — only if verdict is FAIL (or PASS-with-{LOW,MEDIUM} where a stub is warranted per D-228-11). Assign sequentially from `F-28-228-01`.

Row-by-row guidance (priors from RESEARCH — executor verifies and may override):
- **M1** (227-deferral-1): PASS-with-LOW. Emit `F-28-228-NN` on the comment overclaim; resolution RESOLVED-DOC. Rationale: the 3 `initializeCursor` call sites in main.ts (103, 133, 225) are all either unreachable against a healthy cursor (103) or intentional rewinds (133, 225), so no live caller triggers silent rewind — but the "safely" docstring is still misleading.
- **M2**: PASS-with-INFO (Tier B). Docstring accurate about convention; type-system doesn't enforce. Resolution RESOLVED-DOC or INFO-ACCEPTED.
- **M3** (227-deferral-2): PASS. ON CONFLICT DO UPDATE is idempotent by construction.
- **M4** (227-deferral-2): PASS conditional on A3. If confirmations ≥ 1, ordering invariant holds with margin; add rationale citing confirmations window. Emit a LOW finding stub only if confirmations === 0 is possible for any configured chain.
- **M5**, **M6**, **M7**: PASS — verify by re-reading the cited line ranges.
- **M8** (227-deferral-4): PASS. Edge-case matrix in RESEARCH §Backfill-Gate Boundary already constructed. Inclusive `<=` is correct: the `lag == batchSize` case resolves in favor of refresh (conservative).
- **M13**: INFO or LOW — requirement ROADMAP/IDX-04 text mentions "recovery-after-stall" but the code has no stall detector beyond a 5s catch-sleep. Emit an INFO finding stub with direction `docs→code` and resolution `RESOLVED-DOC` (scope out "recovery-after-stall" from the requirement) OR `RESOLVED-CODE-FUTURE` (add stall detection later). Do NOT propose a design in-finding.
- **M14**: PASS-with-INFO. Gap handling is implicit by construction (`batchStart = cursor + 1n`); no explicit gap detector needed given reorg + confirmations architecture.
- **E1**: MEDIUM if A3 resolves to confirmations === 0 on any chain; otherwise INFO ("mitigated by confirmations ≥ N"). Emit a finding stub either way so Phase 229 can decide promotion.

Step C — Finding stub format (append section `## Findings` at the bottom of 228-01-CURSOR-REORG-TRACE.md):

```markdown
#### F-28-228-NN: <short title>

- **Severity:** INFO | LOW | MEDIUM
- **Direction:** comment→code | docs→code | schema↔view
- **Phase:** 228
- **Requirement:** IDX-04
- **Origin row:** M1 (or M4 / E1 / M13 ...)
- **File:** /home/zak/Dev/PurgeGame/database/src/indexer/<file>.ts:<line>
- **Resolution (proposed):** RESOLVED-DOC | RESOLVED-CODE | RESOLVED-CODE-FUTURE | INFO-ACCEPTED
- **Evidence:** <1-3 line explanation citing File:line>
```

Finding IDs run contiguously from `F-28-228-01`. Stop at the true count; do not reserve gaps.

Step D — SC-4 file-touch evidence. Ensure the deliverable cites at least one File:line from each of:
- cursor-manager.ts
- reorg-detector.ts
- main.ts
- block-fetcher.ts (note: fetchLogs is called at main.ts:164 — add an anchor row citing block-fetcher.ts)
- purge-block-range.ts (rollbackToBlock invokes `purgeBlockRange` per reorg-detector.ts:170-ish — cite the call site)

Update the SC-4 File-Touch Evidence table: replace every "TBD" with a concrete `File:line` reference.

Step E — Spot-recheck log (per 228-VALIDATION.md §Sampling Rate). Pick 2 of {M1, M3, M4, M5, M8} at random. For each, re-read the enclosing function body (≥15 lines around the cited line) and re-verify the verdict. Append a `## Spot-Recheck Log` section mirroring 227-03-SUMMARY.md Re-check 1 / Re-check 2 format.

Step F — Create `228-01-SUMMARY.md` with:
1. Verdict counts (PASS / PASS-with-INFO / PASS-with-LOW / LOW / MEDIUM / FAIL).
2. Findings consumed (list F-28-228-NN IDs and their severity).
3. Next available finding ID (must be `< F-28-228-101`; the 101+ block is reserved for 228-02 per D-228-10).
4. Handoff to Phase 229: list every finding stub with severity + direction + resolution for consolidation.
5. SC-4 coverage confirmation: 5/9 indexer files touched by 228-01 (cursor-manager, reorg-detector, main, block-fetcher, purge-block-range); the remaining 4 (event-processor, view-refresh, index, delegated handlers) are audit-touched by 227 and/or 228-02.
6. Scope-boundary reminder: per D-228-01/02, no writes to `database/`; per D-228-11, HIGH/CRITICAL reserved for Phase 229.
7. Resolution of assumptions A1/A2/A3 with File:line citations.
  </action>
  <verify>
    <automated>test -f .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-01-SUMMARY.md && grep -qE 'F-28-228-0[1-9]' .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-01-CURSOR-REORG-TRACE.md && ! grep -qE '^\|.*\| TBD \|' .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-01-CURSOR-REORG-TRACE.md && grep -q 'Spot-Recheck' .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-01-CURSOR-REORG-TRACE.md && grep -q 'block-fetcher.ts' .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-01-CURSOR-REORG-TRACE.md && grep -q 'purge-block-range.ts' .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-01-CURSOR-REORG-TRACE.md</automated>
  </verify>
  <acceptance_criteria>
    - Every row (M1, M2, M3, M4, M5, M6, M7, M8, M13, M14, E1) has a non-TBD Final Verdict and Rationale.
    - Assumption-Check Worklist A1/A2/A3 resolved with File:line citations.
    - At least one `F-28-228-NN` finding stub exists (expected per priors: ≥ 3 — M1 docstring overclaim, M13 docs→code stall gap, E1 intra-batch edge case; plus any FAIL overrides Task 2 discovers).
    - All finding IDs are contiguous starting at `F-28-228-01`; none ≥ `F-28-228-101`.
    - Every finding cites an absolute File:line under `/home/zak/Dev/PurgeGame/database/`.
    - Every finding has exactly one severity ∈ {INFO, LOW, MEDIUM} — no HIGH, no CRITICAL.
    - SC-4 File-Touch Evidence table has concrete File:line for all 5 indexer files (no TBD).
    - Spot-Recheck Log has ≥2 re-verifications.
    - `228-01-SUMMARY.md` exists with bucket counts, finding handoff, next-available ID (< F-28-228-101).
    - Zero writes outside `.planning/phases/228-cursor-reorg-view-refresh-state-machines/` (verify: `git -C /home/zak/Dev/PurgeGame/database status` clean).
  </acceptance_criteria>
  <done>All rows verdicted; findings emitted in F-28-228-01+ block; SUMMARY committed; Phase 229 handoff explicit.</done>
</task>

</tasks>

<threat_model>
N/A — catalog-only, no runtime gate per D-228-02. This plan produces markdown audit artifacts only; no code executed, no network surface changed, no trust boundary crossed. STRIDE register not applicable. Incidental security observations (e.g., cursor-rewind as potential DoS vector) are captured as INFO findings under the normal D-228-11 severity taxonomy.
</threat_model>

<verification>
- `228-01-CURSOR-REORG-TRACE.md` and `228-01-SUMMARY.md` exist.
- All 10 M-rows assigned to 228-01 + E1 edge case have verdicts + rationale.
- All 3 absorbed 227 deferrals (#1, #2, #4) appear as annotated rows.
- Finding IDs consumed in `F-28-228-01+` block (no collision with 228-02's `F-28-228-101+`).
- SC-4 evidence: 5 indexer files touched (cursor-manager, reorg-detector, main, block-fetcher, purge-block-range).
- Zero writes to `/home/zak/Dev/PurgeGame/database/` (git status clean there).
- Spot-recheck log has ≥2 re-verifications per 228-VALIDATION.md.
</verification>

<success_criteria>
- IDX-04 satisfied: cursor-manager + reorg-detector state-machine behavior catalogued with PASS/FAIL verdicts.
- All 3 227 deferrals (#1, #2, #4) resolved as explicit audit rows.
- Finding stubs handed to Phase 229 for consolidation.
- SC-4 indexer-file coverage advanced (5 of 9 files audit-touched by this plan).
</success_criteria>

<output>
After completion, `.planning/phases/228-cursor-reorg-view-refresh-state-machines/228-01-SUMMARY.md` exists with verdict counts, finding handoff, next available finding ID, and SC-4 coverage confirmation.
</output>
</content>
</invoke>