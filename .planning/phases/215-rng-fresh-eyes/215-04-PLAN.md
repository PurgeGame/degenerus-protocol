---
phase: 215-rng-fresh-eyes
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/215-rng-fresh-eyes/215-04-WORD-DERIVATION.md
autonomous: true
requirements: [RNG-04]

must_haves:
  truths:
    - "Every keccak256, shift, mask, or modulo producing a game outcome is traced to its VRF source word"
    - "Derivation steps are shown explicitly — not just 'word is used here'"
    - "The LCG PRNG in _raritySymbolBatch has seed provenance traced to VRF (per D-02)"
    - "No game outcome derives from a non-VRF source (except documented gameover prevrandao fallback)"
  artifacts:
    - path: ".planning/phases/215-rng-fresh-eyes/215-04-WORD-DERIVATION.md"
      provides: "Word derivation verification for every keccak/shift/mask/modulo producing a game outcome"
      contains: "RNG-04"
  key_links:
    - from: "VRF source word (rngWordByDay or lootboxRngWordByIndex)"
      to: "Game outcome (winner, trait, amount, etc.)"
      via: "keccak256 / shift / mask / modulo chain"
      pattern: "keccak256\\|>>\\|&\\|%"
---

<objective>
Verify every keccak/shift/mask producing a game outcome traces to its VRF source word with derivation steps shown (RNG-04).

Purpose: Prove that every piece of entropy used to determine game outcomes (winners, traits, amounts, boons) derives from VRF-sourced randomness, not from predictable or manipulable sources. Each derivation chain must be shown step by step. Per D-02, the LCG PRNG audit covers seed provenance only — not statistical properties.

Output: 215-04-WORD-DERIVATION.md — per-consumer derivation chain from VRF word to game outcome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/215-rng-fresh-eyes/215-CONTEXT.md

@.planning/phases/213-delta-extraction/213-DELTA-EXTRACTION.md (RNG chain definitions RNG-03 through RNG-11)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace every entropy derivation from VRF source word to game outcome</name>
  <files>.planning/phases/215-rng-fresh-eyes/215-04-WORD-DERIVATION.md</files>
  <read_first>
    contracts/modules/DegenerusGameJackpotModule.sol (lines 1603-1700 for _randTraitTicket — keccak per-winner; lines 1095-1180 for _runJackpotEthFlow — entropy rotation [20,12,6,1]; lines 1861-1960 for _rollWinningTraits — trait selection; lines 1537-1600 for _applyHeroOverride — hero override; lines 310-548 for payDailyJackpot — carryover tickets keccak modulo; lines 1182-1314 for _processDailyEth — winner selection per bucket; lines 1315-1535 for _resolveTraitWinners)
    contracts/modules/DegenerusGameMintModule.sol (lines 539-700 for _raritySymbolBatch — LCG PRNG assembly block)
    contracts/modules/DegenerusGameDegeneretteModule.sol (lines 416-555 for _placeDegeneretteBetCore; lines 556-620 for _resolveFullTicketBet — bet resolution)
    contracts/modules/DegenerusGameLootboxModule.sol (lines 1012-1100 for _rollLootboxBoons; lines 1742-1800 for _deityDailySeed/_deityBoonForSlot)
    contracts/modules/DegenerusGameAdvanceModule.sol (lines 1564-1682 for _backfillGapDays — keccak gap derivation; lines 1083-1100 for _gameOverEntropy)
    contracts/modules/DegenerusGameGameOverModule.sol (lines 79-248 for handleGameOverDrain — terminal RNG)
  </read_first>
  <action>
Produce 215-04-WORD-DERIVATION.md tracing every entropy derivation chain.

**For each RNG consumer (RNG-03 through RNG-11 plus backfill from RNG-01), show:**

### RNG-03: _randTraitTicket() — Per-Winner Trait Ticket Selection

1. VRF source: rngWordByDay[day] (daily word)
2. Derivation: keccak256(abi.encode(randomWord, trait, salt, i)) — show the exact Solidity line
3. Modulo: result % ticketCount — show how winner index is derived
4. Game outcome: which ticket holder wins for each trait
5. Entropy independence: each (trait, salt, i) tuple produces independent entropy via keccak domain separation

### RNG-04: _runJackpotEthFlow() — ETH Jackpot Bucket Winners

1. VRF source: daily word passed through call chain
2. Derivation: entropy rotation across [20, 12, 6, 1] = 39 fixed bucket counts — show the rotation mechanism (shift? keccak? iteration?)
3. Modulo: how winner selection works per bucket
4. Game outcome: ETH payout recipient for each bucket

### RNG-05: payDailyJackpot() — Carryover Ticket Sourcing

1. VRF source: daily word
2. Derivation: keccak256 modulo for source offset [1..4]
3. Game outcome: which day's tickets contribute to future pool jackpot

### RNG-06: _placeDegeneretteBetCore() — Degenerette Bet Resolution

1. VRF source: rngWordByDay[day] (daily word for resolution day)
2. Derivation: how _resolveFullTicketBet uses the word — shift/mask/modulo chain
3. Game outcome: bet win/loss determination

### RNG-07: _raritySymbolBatch() — LCG PRNG (Seed Provenance Only per D-02)

1. VRF source: lootboxRngWord (from lootbox VRF fulfillment)
2. Seed derivation: how the assembly LCG gets its initial seed from the VRF word — show exact assembly lines
3. Per D-02: Do NOT analyze LCG statistical properties, bias, period, or correlation
4. Verdict: seed traces to VRF? Yes/No with proof

### RNG-08/09: _gameOverEntropy() + handleGameOverDrain()

1. Source: block.prevrandao (NOT VRF — documented exception)
2. Derivation: how prevrandao feeds into terminal resolution
3. Fallback: RngNotReady() revert when unavailable
4. Game outcome: gameover jackpot resolution

### RNG-10: _deityDailySeed() / _deityBoonForSlot()

1. VRF source: rngWordByDay[day]
2. Derivation: how _deityDailySeed derives from daily word, how _deityBoonForSlot uses it
3. Game outcome: deity boon selection per slot

### RNG-11: _rollWinningTraits() / _applyHeroOverride()

1. VRF source: daily word
2. Derivation: how winning traits are selected from the word — shift/mask/modulo
3. Hero override: how _applyHeroOverride modifies trait results
4. Game outcome: daily winning trait set

### Backfill (from RNG-01): _backfillGapDays()

1. VRF source: daily word from rngGate
2. Derivation: keccak256(abi.encode(word, i)) for each gap day i
3. Game outcome: synthetic entropy for skipped days

**For each derivation chain, show:**
- Exact Solidity code (the keccak/shift/mask/modulo line)
- Line number in source file
- Input variables and their provenance
- Output: what game outcome this determines
- Verdict: VRF-SOURCED (traces to VRF) / NON-VRF (uses non-VRF entropy) / MIXED (partial VRF)

End with a summary table:
| Chain | Consumer | VRF Source | Derivation Op | Game Outcome | Verdict |
  </action>
  <verify>
    <automated>grep -c "VRF-SOURCED\|NON-VRF\|MIXED" .planning/phases/215-rng-fresh-eyes/215-04-WORD-DERIVATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/215-rng-fresh-eyes/215-04-WORD-DERIVATION.md exists
    - Contains sections for RNG-03, RNG-04, RNG-05, RNG-06, RNG-07, RNG-08, RNG-09, RNG-10, RNG-11, and Backfill
    - Each section has "VRF source:", derivation code snippet, line number, and "Verdict:"
    - grep -c for derivation verdicts returns at least 10 (one per chain)
    - RNG-07 section contains "seed provenance" and does NOT contain "statistical properties" or "bias" or "period length" per D-02
    - RNG-08/09 sections document prevrandao as documented NON-VRF exception
    - Contains summary table with "Chain" and "Verdict" columns
    - Every code snippet has a line number reference
    - Does NOT reference prior RNG audits (v3.7, v3.8, v3.9) per D-03
  </acceptance_criteria>
  <done>Every keccak/shift/mask/modulo producing a game outcome is traced to its VRF source word with derivation steps shown, line-number evidence, and per-chain verdicts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VRF word -> derived entropy | Derivation must preserve unpredictability |
| LCG seed -> PRNG output | Seed must trace to VRF for _raritySymbolBatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-215-10 | Tampering | keccak domain separation | mitigate | Verify each keccak includes unique domain parameters (trait, salt, index) preventing cross-chain collision |
| T-215-11 | Information Disclosure | LCG seed derivation | mitigate | Verify LCG seed in _raritySymbolBatch traces to VRF word, not predictable source |
| T-215-12 | Tampering | prevrandao in gameover | accept | Terminal one-time event; prevrandao bias is ~1 bit for validators, low impact |
</threat_model>

<verification>
- Every RNG chain (RNG-03 through RNG-11 plus backfill) has derivation steps shown
- Each chain traces from VRF word through operations to game outcome
- LCG audit is seed-provenance only per D-02
- No non-VRF entropy used for game outcomes except documented prevrandao fallback
</verification>

<success_criteria>
Every keccak/shift/mask producing a game outcome is traced to its VRF source word with derivation steps shown. LCG seed provenance verified. Gameover prevrandao documented as exception.
</success_criteria>

<output>
After completion, create `.planning/phases/215-rng-fresh-eyes/215-04-SUMMARY.md`
</output>
