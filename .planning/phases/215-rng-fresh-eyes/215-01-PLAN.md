---
phase: 215-rng-fresh-eyes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/215-rng-fresh-eyes/215-01-VRF-LIFECYCLE.md
autonomous: true
requirements: [RNG-01]

must_haves:
  truths:
    - "VRF request path is traced from advanceGame through _requestRng to VRF coordinator with all parameters documented"
    - "VRF fulfillment path is traced from rawFulfillRandomWords through rngGate to rngWordByDay storage with all state mutations documented"
    - "Lootbox VRF request/fulfillment is traced separately through requestLootboxRng and _finalizeLootboxRng with per-index word storage"
    - "Gap day backfill via _backfillGapDays is traced showing keccak derivation from existing word"
    - "Gameover fallback via _gameOverEntropy is traced showing prevrandao usage and RngNotReady revert"
    - "Every stage has explicit proof (line numbers, code snippets) — not assertions"
  artifacts:
    - path: ".planning/phases/215-rng-fresh-eyes/215-01-VRF-LIFECYCLE.md"
      provides: "End-to-end VRF lifecycle trace covering both daily and lootbox RNG paths"
      contains: "RNG-01"
  key_links:
    - from: "AdvanceModule._requestRng()"
      to: "VRF coordinator requestRandomWords()"
      via: "external call"
      pattern: "requestRandomWords"
    - from: "DegenerusGame.rawFulfillRandomWords()"
      to: "AdvanceModule.rawFulfillRandomWords()"
      via: "delegatecall"
      pattern: "rawFulfillRandomWords"
    - from: "AdvanceModule.rngGate()"
      to: "rngWordByDay mapping"
      via: "storage read"
      pattern: "rngWordByDay"
---

<objective>
Trace the VRF request/fulfillment lifecycle end-to-end from first principles (RNG-01).

Purpose: Establish the foundational RNG lifecycle trace that proves how random words enter the system, get stored, and become available to consumers. This is the base layer that all other RNG audit plans build upon. Per D-03, this is a fresh audit with no reliance on prior RNG conclusions.

Output: 215-01-VRF-LIFECYCLE.md — complete lifecycle trace for daily VRF, lootbox VRF, gap day backfill, and gameover fallback paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/215-rng-fresh-eyes/215-CONTEXT.md

@.planning/phases/213-delta-extraction/213-DELTA-EXTRACTION.md (RNG chain definitions RNG-01, RNG-02, RNG-08)

Phase 214 can be cited as supporting evidence per D-04:
@.planning/phases/214-adversarial-audit/214-01-REENTRANCY-CEI.md (rngLockedFlag verdicts)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace daily VRF request/fulfillment lifecycle and gap day backfill</name>
  <files>.planning/phases/215-rng-fresh-eyes/215-01-VRF-LIFECYCLE.md</files>
  <read_first>
    contracts/modules/DegenerusGameAdvanceModule.sol (lines 1386-1600 for _requestRng, rawFulfillRandomWords, _backfillGapDays; lines 1000-1070 for rngGate; lines 1083-1100 for _gameOverEntropy; lines 907-990 for requestLootboxRng; lines 1071-1082 for _finalizeLootboxRng; lines 140-200 for advanceGame entry calling rngGate)
    contracts/DegenerusGame.sol (lines 1470-1560 for rawFulfillRandomWords entry point and delegatecall routing)
    contracts/storage/DegenerusGameStorage.sol (lines 279 for rngLockedFlag; lines 430 for rngWordByDay; lines 1300-1380 for lootbox packed state and lootboxRngWordByIndex)
  </read_first>
  <action>
Read every function in the daily VRF path and lootbox VRF path from source. For each function, extract: (1) exact Solidity code for the RNG-critical logic, (2) line numbers, (3) state reads and writes, (4) external calls, (5) revert conditions.

Produce 215-01-VRF-LIFECYCLE.md with these sections:

**Section 1: Daily VRF Request Path**
Trace: advanceGame() -> _requestRng(isTicketJackpotDay, lvl) -> VRF coordinator.requestRandomWords()
- Document every parameter passed to _requestRng and where each comes from
- Document the VRF coordinator call: callbackGasLimit, numWords, confirmations
- Document state mutations: rngLockedFlag = true (line 1442 in AdvanceModule)
- Document the subscription ID and keyHash parameters
- Show the requestId return and any storage of it

**Section 2: Daily VRF Fulfillment Path**
Trace: VRF coordinator callback -> DegenerusGame.rawFulfillRandomWords() -> AdvanceModule.rawFulfillRandomWords() (delegatecall)
- Document the rawFulfillRandomWords entry in DegenerusGame.sol: how it validates the caller is the VRF coordinator
- Document the delegatecall to AdvanceModule.rawFulfillRandomWords
- Document what happens with the randomWords array: how word is stored to rngWordByDay[day]
- Document state mutations: rngLockedFlag = false, any lootbox word handling
- Document the _finalizeLootboxRng call if lootbox pending

**Section 3: rngGate — Word Retrieval**
Trace: advanceGame() -> rngGate() -> returns (uint256 word, uint32 gapDays)
- Document how rngGate reads rngWordByDay[currentDay - 1] (or equivalent)
- Document the gapDays calculation and when it returns nonzero
- Document the revert conditions (no word available)

**Section 4: Gap Day Backfill**
Trace: _backfillGapDays(word, gapDays)
- Document the loop: how keccak256(abi.encode(word, i)) generates entropy for each gap day
- Document the 120-day cap
- Document what gets stored for each gap day

**Section 5: Lootbox VRF Request/Fulfillment**
Trace: requestLootboxRng() -> VRF coordinator -> rawFulfillRandomWords() -> _finalizeLootboxRng()
- Document the packed lootbox state via _lrRead/_lrWrite
- Document how lootboxRngWordByIndex stores per-index words
- Document the midDayTicketRngPending flag handling

**Section 6: Gameover Fallback**
Trace: _gameOverEntropy()
- Document the prevrandao fallback
- Document the RngNotReady() revert when no entropy available
- Document when this path is used vs daily VRF

For EVERY stage, include:
- Contract name and exact line numbers
- The actual Solidity code snippet (key lines, not entire functions)
- State variables read and written
- Verdict: TRACED (lifecycle proven) or CONCERN (issue found)

End with a summary table:
| Path | Entry | Exit | Word Storage | State Mutations | Verdict |
  </action>
  <verify>
    <automated>grep -c "TRACED\|CONCERN" .planning/phases/215-rng-fresh-eyes/215-01-VRF-LIFECYCLE.md</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/215-rng-fresh-eyes/215-01-VRF-LIFECYCLE.md exists
    - Contains "Section 1: Daily VRF Request Path" through "Section 6: Gameover Fallback"
    - Contains at least 6 verdicts (one per section minimum)
    - Contains line number references (pattern: "line \d+" or "L\d+") for every code snippet
    - Contains "rngWordByDay" (daily word storage proof)
    - Contains "lootboxRngWordByIndex" (lootbox word storage proof)
    - Contains "_backfillGapDays" with keccak derivation shown
    - Contains "_gameOverEntropy" with prevrandao and RngNotReady
    - Contains "rngLockedFlag = true" and "rngLockedFlag = false" (lock lifecycle)
    - Contains summary table with "Path" and "Verdict" columns
    - Does NOT contain references to prior RNG audit phases (v3.7, v3.8, v3.9) per D-03
  </acceptance_criteria>
  <done>Complete VRF lifecycle trace covering daily request/fulfillment, rngGate retrieval, gap day backfill, lootbox request/fulfillment, and gameover fallback — all with line-number-backed proof and per-section verdicts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VRF coordinator -> rawFulfillRandomWords | External callback delivering randomness |
| advanceGame caller -> VRF request trigger | Who can trigger RNG requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-215-01 | Spoofing | rawFulfillRandomWords caller | mitigate | Verify DegenerusGame.rawFulfillRandomWords checks msg.sender == vrfCoordinator before delegatecall |
| T-215-02 | Tampering | rngWordByDay storage | mitigate | Verify word written exactly once per day and cannot be overwritten |
| T-215-03 | Denial | _gameOverEntropy revert | accept | RngNotReady revert is intentional defense — blocks gameover without VRF |
</threat_model>

<verification>
- 215-01-VRF-LIFECYCLE.md contains traced paths for all 6 sections
- Every path has line numbers and code snippets from current HEAD
- No references to prior RNG audits (v3.7/v3.8/v3.9)
- Summary table has verdicts for every path
</verification>

<success_criteria>
The VRF request/fulfillment lifecycle is traced end-to-end with explicit proof at each stage, covering daily VRF, lootbox VRF, gap day backfill, and gameover fallback paths.
</success_criteria>

<output>
After completion, create `.planning/phases/215-rng-fresh-eyes/215-01-SUMMARY.md`
</output>
