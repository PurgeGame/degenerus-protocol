---
phase: 215-rng-fresh-eyes
plan: 05
type: execute
wave: 2
depends_on: [215-01, 215-02, 215-03, 215-04]
files_modified:
  - .planning/phases/215-rng-fresh-eyes/215-05-RNGLOCKED-SYNTHESIS.md
autonomous: true
requirements: [RNG-05]

must_haves:
  truths:
    - "rngLocked mutual exclusion is verified across all state-changing paths that touch RNG state"
    - "Every rngLockedFlag guard site is catalogued with the exact revert path"
    - "Every state-changing function is classified as guarded or unguarded"
    - "Any unguarded path that touches RNG-affecting state is flagged"
    - "Synthesis of plans 01-04 findings produces a unified phase verdict"
  artifacts:
    - path: ".planning/phases/215-rng-fresh-eyes/215-05-RNGLOCKED-SYNTHESIS.md"
      provides: "rngLocked mutual exclusion verification plus synthesis of all RNG fresh-eyes findings"
      contains: "RNG-05"
  key_links:
    - from: "rngLockedFlag = true (AdvanceModule L1442)"
      to: "all guard sites across Storage, Game, modules"
      via: "if (rngLockedFlag) revert RngLocked()"
      pattern: "rngLockedFlag"
    - from: "Plans 01-04 findings"
      to: "Unified phase 215 verdict"
      via: "synthesis"
      pattern: "PHASE VERDICT"
---

<objective>
Verify rngLocked mutual exclusion across all state-changing paths (RNG-05) and synthesize all Phase 215 findings into a unified verdict.

Purpose: rngLockedFlag is the primary defense against state manipulation during VRF windows. This plan verifies it blocks every path that could affect RNG outcomes. It also synthesizes findings from plans 01-04 into a consolidated phase-level assessment. Per D-04, Phase 214's rngLockedFlag findings can be cited as supporting evidence.

Output: 215-05-RNGLOCKED-SYNTHESIS.md — rngLocked verification + unified phase findings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/215-rng-fresh-eyes/215-CONTEXT.md

Prior plan outputs from this phase:
@.planning/phases/215-rng-fresh-eyes/215-01-SUMMARY.md
@.planning/phases/215-rng-fresh-eyes/215-02-SUMMARY.md
@.planning/phases/215-rng-fresh-eyes/215-03-SUMMARY.md
@.planning/phases/215-rng-fresh-eyes/215-04-SUMMARY.md

Full audit artifacts for synthesis:
@.planning/phases/215-rng-fresh-eyes/215-01-VRF-LIFECYCLE.md
@.planning/phases/215-rng-fresh-eyes/215-02-BACKWARD-TRACE.md
@.planning/phases/215-rng-fresh-eyes/215-03-COMMITMENT-WINDOW.md
@.planning/phases/215-rng-fresh-eyes/215-04-WORD-DERIVATION.md

Phase 214 supporting evidence per D-04:
@.planning/phases/214-adversarial-audit/214-01-REENTRANCY-CEI.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify rngLocked mutual exclusion across all state-changing paths</name>
  <files>.planning/phases/215-rng-fresh-eyes/215-05-RNGLOCKED-SYNTHESIS.md</files>
  <read_first>
    contracts/storage/DegenerusGameStorage.sol (lines 279 for rngLockedFlag declaration; lines 540-670 for all three guard sites at lines 566, 596, 650)
    contracts/DegenerusGame.sol (full file — search all rngLockedFlag references: lines 1480, 1495, 1542, 1882, 2136, 2194)
    contracts/modules/DegenerusGameAdvanceModule.sol (lines 177 for purchaseLevel logic; line 908 for requestLootboxRng guard; line 1442 for lock set; lines 1492, 1515 for lock clear; line 1540 for rawFulfillRandomWords check)
    contracts/modules/DegenerusGameMintModule.sol (line 1231 for purchase path guard)
    contracts/modules/DegenerusGameWhaleModule.sol (line 543 for whale purchase guard)
    contracts/modules/DegenerusGameJackpotModule.sol (check for any rngLockedFlag references)
    contracts/modules/DegenerusGameDegeneretteModule.sol (check for any rngLockedFlag references)
    contracts/modules/DegenerusGameLootboxModule.sol (check for any rngLockedFlag references)
    contracts/modules/DegenerusGameGameOverModule.sol (check for any rngLockedFlag references)
    .planning/phases/215-rng-fresh-eyes/215-01-VRF-LIFECYCLE.md (lifecycle trace findings)
    .planning/phases/215-rng-fresh-eyes/215-02-BACKWARD-TRACE.md (backward trace findings)
    .planning/phases/215-rng-fresh-eyes/215-03-COMMITMENT-WINDOW.md (commitment window findings)
    .planning/phases/215-rng-fresh-eyes/215-04-WORD-DERIVATION.md (word derivation findings)
  </read_first>
  <action>
Produce 215-05-RNGLOCKED-SYNTHESIS.md in two parts: (A) rngLocked mutual exclusion verification, (B) phase synthesis.

**Part A: rngLocked Mutual Exclusion Verification**

**Section 1: rngLockedFlag Lifecycle**
Document the complete lifecycle of the flag:
- SET: AdvanceModule._requestRng() line 1442 — `rngLockedFlag = true`
- CLEAR (normal): AdvanceModule.rawFulfillRandomWords line 1492 — `rngLockedFlag = false` (after storing word)
- CLEAR (lootbox only): AdvanceModule.rawFulfillRandomWords line 1515 — `rngLockedFlag = false` (lootbox-only path)
- CHECK in rawFulfillRandomWords: line 1540 — what happens if flag is already false?

**Section 2: Guard Site Catalogue**
For EVERY rngLockedFlag check in the codebase, document:

| # | File | Line | Function | Guard Pattern | Blocks |
|---|------|------|----------|---------------|--------|
| 1 | DegenerusGameStorage.sol | 566 | [modifier/function name] | `if (isFarFuture && rngLockedFlag && !rngBypass) revert RngLocked()` | [what it blocks] |
| 2 | DegenerusGameStorage.sol | 596 | ... | ... | ... |
| 3 | DegenerusGameStorage.sol | 650 | ... | ... | ... |
| 4 | DegenerusGame.sol | 1480 | ... | `if (rngLockedFlag) revert RngLocked()` | ... |
| 5 | DegenerusGame.sol | 1495 | ... | ... | ... |
| 6 | DegenerusGame.sol | 1542 | ... | ... | ... |
| 7 | DegenerusGame.sol | 1882 | ... | ... | ... |
| 8 | WhaleModule.sol | 543 | ... | ... | ... |
| 9 | MintModule.sol | 1231 | ... | ... | ... |
| 10 | AdvanceModule.sol | 908 | requestLootboxRng | `if (rngLockedFlag) revert RngLocked()` | ... |
| 11 | AdvanceModule.sol | 177 | advanceGame | `(lastPurchase && rngLockedFlag) ? lvl : lvl + 1` | [price level logic] |

Note: the Storage guards at 566/596/650 use `isFarFuture && rngLockedFlag && !rngBypass` — document what isFarFuture and rngBypass mean and who can set rngBypass.

**Section 3: Coverage Analysis**
For every external/public function on DegenerusGame that changes state:
- Is it guarded by rngLockedFlag (directly or via Storage modifier)?
- If guarded: which guard site? Show the call path.
- If NOT guarded: does it touch any state that affects RNG outcomes?
  - If yes: FINDING (unguarded path touching RNG state)
  - If no: SAFE (unrelated to RNG)

Produce the complete classification table:
| Function | State-Changing? | Guarded? | Guard Site | Touches RNG State? | Verdict |
|----------|----------------|----------|------------|-------------------|---------|

**Section 4: Edge Cases**
- What happens if rawFulfillRandomWords is called when rngLockedFlag is already false? (stale/duplicate callback)
- What happens if _requestRng fails after setting rngLockedFlag = true? (stuck lock)
- Can rngBypass be set by anyone other than admin? Trace who sets it.
- The AdvanceModule line 177 pattern `(lastPurchase && rngLockedFlag)` — is this a guard or a different use?

**Part B: Phase 215 Synthesis**

Read all four prior audit artifacts (215-01 through 215-04) and produce:

**Section 5: Consolidated Findings**
Collect ALL findings (VULNERABLE, CONCERN, INFO) from:
- 215-01-VRF-LIFECYCLE.md verdicts
- 215-02-BACKWARD-TRACE.md verdicts per chain
- 215-03-COMMITMENT-WINDOW.md risk assessments
- 215-04-WORD-DERIVATION.md derivation verdicts

For each finding:
| Finding ID | Source Plan | Chain | Severity | Description | Status |
|------------|-----------|-------|----------|-------------|--------|

**Section 6: Phase Verdict**
Produce a unified verdict addressing all 5 success criteria from ROADMAP:
1. VRF lifecycle traced end-to-end? (from 215-01)
2. Every consumer backward traced? (from 215-02)
3. Commitment windows analyzed? (from 215-03)
4. Word derivations verified? (from 215-04)
5. rngLocked mutual exclusion verified? (from this plan)

Final verdict: SOUND / CONCERNS / VULNERABLE with rationale.
  </action>
  <verify>
    <automated>grep -c "SOUND\|CONCERNS\|VULNERABLE\|FINDING\|SAFE" .planning/phases/215-rng-fresh-eyes/215-05-RNGLOCKED-SYNTHESIS.md</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/215-rng-fresh-eyes/215-05-RNGLOCKED-SYNTHESIS.md exists
    - Contains "Part A: rngLocked Mutual Exclusion" and "Part B: Phase 215 Synthesis"
    - Contains guard site catalogue with at least 10 entries (all known rngLockedFlag sites)
    - Contains line number 1442 (lock set) and lines 1492/1515 (lock clear) from AdvanceModule
    - Contains coverage analysis table with every external/public function classified
    - Contains "rngBypass" analysis documenting who can set it
    - Contains consolidated findings table from plans 01-04
    - Contains "Phase Verdict" section with assessment of all 5 success criteria
    - Contains "SOUND" or "CONCERNS" or "VULNERABLE" as final phase verdict
    - Does NOT reference prior RNG audits (v3.7, v3.8, v3.9) per D-03
    - Does reference Phase 214 findings as supporting evidence per D-04
  </acceptance_criteria>
  <done>rngLocked mutual exclusion verified across all state-changing paths, all findings from plans 01-04 synthesized, and unified phase verdict produced</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rngLockedFlag guard -> unguarded functions | Any state-changing path not blocked by rngLockedFlag |
| rngBypass flag -> guard bypass | Admin override of rngLocked protection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-215-13 | Elevation of Privilege | rngBypass flag | mitigate | Verify only admin can set rngBypass and document the trust assumption |
| T-215-14 | Denial | Stuck rngLockedFlag | mitigate | Verify rawFulfillRandomWords clears flag on all paths, and admin bypass exists |
| T-215-15 | Tampering | Unguarded state-changing paths | mitigate | Coverage analysis proves every RNG-affecting function is guarded |
</threat_model>

<verification>
- Every rngLockedFlag guard site catalogued with line numbers
- Every external/public function classified as guarded or unguarded
- Unguarded paths assessed for RNG state impact
- Findings from plans 01-04 consolidated
- Phase verdict addresses all 5 ROADMAP success criteria
</verification>

<success_criteria>
rngLocked mutual exclusion is verified across all state-changing paths that touch RNG state. Phase 215 findings are consolidated with a unified verdict.
</success_criteria>

<output>
After completion, create `.planning/phases/215-rng-fresh-eyes/215-05-SUMMARY.md`
</output>
