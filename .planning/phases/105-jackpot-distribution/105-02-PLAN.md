---
phase: 105-jackpot-distribution
plan: 02
type: execute
wave: 2
depends_on: [105-01]
files_modified:
  - audit/unit-03/ATTACK-REPORT.md
autonomous: true
requirements: [ATK-01, ATK-02, ATK-03, ATK-04, ATK-05]

must_haves:
  truths:
    - "Every Category B function has a fully-expanded recursive call tree with line numbers for every call site"
    - "Every Category B function has a complete storage-write map listing every variable written by any function in its transitive call graph"
    - "Every Category B function has an explicit cached-local-vs-storage check identifying all (ancestor_local, descendant_write) pairs"
    - "Every Category B function is attacked from all 10 applicable angles with VULNERABLE / INVESTIGATE / SAFE verdicts"
    - "Every [MULTI-PARENT] Category C function has standalone analysis covering all calling contexts"
    - "The BAF-critical chain _addClaimableEth -> _processAutoRebuy -> futurePrizePool is re-audited from scratch per D-07"
    - "The inline Yul assembly in _raritySymbolBatch is verified for storage slot correctness"
    - "Every VULNERABLE or INVESTIGATE finding includes exact line numbers, a concrete attack scenario, and proof-of-concept steps"
  artifacts:
    - path: "audit/unit-03/ATTACK-REPORT.md"
      provides: "Complete function-by-function attack analysis"
      contains: "Call Tree"
    - path: "audit/unit-03/ATTACK-REPORT.md"
      provides: "Storage write maps for all functions"
      contains: "Storage Writes"
    - path: "audit/unit-03/ATTACK-REPORT.md"
      provides: "Cached-local-vs-storage checks"
      contains: "Cached-Local-vs-Storage"
  key_links:
    - from: "audit/unit-03/ATTACK-REPORT.md"
      to: "contracts/modules/DegenerusGameJackpotModule.sol"
      via: "Per-function analysis with line numbers"
      pattern: "line \\d+"
    - from: "audit/unit-03/ATTACK-REPORT.md"
      to: "audit/unit-03/COVERAGE-CHECKLIST.md"
      via: "Every checklist function has a corresponding analysis section"
      pattern: "JackpotModule::"
---

<objective>
Mad Genius Agent: Attack every state-changing function in DegenerusGameJackpotModule.sol and DegenerusGamePayoutUtils.sol with full call-tree expansion, storage-write mapping, cached-local-vs-storage checks, and multi-angle attack analysis.

Purpose: This is the core attack phase for the largest module in the protocol (2,715 lines). The BAF cache-overwrite bug's ORIGINAL LOCATION is in this module (_addClaimableEth, _processAutoRebuy). The Mad Genius re-audits these paths from scratch per D-07, treating the v4.4 fix as if it does not exist. Additionally, inline Yul assembly in _raritySymbolBatch and deep call chains (6+ levels in ticket distribution) require meticulous tracing.

Output: ATTACK-REPORT.md with one section per function, covering every function on the Taskmaster's checklist.
</objective>

<execution_context>
@.planning/ULTIMATE-AUDIT-DESIGN.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-jackpot-distribution/105-CONTEXT.md
@.planning/phases/105-jackpot-distribution/105-RESEARCH.md
@.planning/phases/105-jackpot-distribution/105-01-SUMMARY.md
@audit/KNOWN-ISSUES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attack all Category B functions and [MULTI-PARENT] Category C functions</name>
  <files>audit/unit-03/ATTACK-REPORT.md</files>

  <read_first>
    - .planning/ULTIMATE-AUDIT-DESIGN.md (Agent 1: Mad Genius section -- adopt this persona COMPLETELY)
    - audit/unit-03/COVERAGE-CHECKLIST.md (the Taskmaster's checklist -- you must cover EVERY function marked for full analysis)
    - contracts/modules/DegenerusGameJackpotModule.sol (2,715 lines -- read the ENTIRE file, line by line. No skimming. No "this looks standard.")
    - contracts/modules/DegenerusGamePayoutUtils.sol (92 lines -- read the ENTIRE file, line by line)
    - contracts/storage/DegenerusGameStorage.sol (storage layout -- needed for storage-write maps and cache checks)
    - contracts/interfaces/IDegenerusGameModules.sol (module interface signatures)
    - .planning/phases/105-jackpot-distribution/105-RESEARCH.md (BAF-critical chain analysis, subsystem map, inline assembly risk analysis, common pitfalls -- use as STARTING POINTS for your attack, not conclusions)
    - audit/KNOWN-ISSUES.md (do NOT re-report known issues)
  </read_first>

  <action>
YOU ARE THE MAD GENIUS. Read your identity and mandate from ULTIMATE-AUDIT-DESIGN.md, Agent 1. You are the most dangerous smart contract attacker alive. You have 1,000 ETH, unlimited patience, and the source code.

**IMPORTANT: You must read EVERY LINE of DegenerusGameJackpotModule.sol (2,715 lines) and DegenerusGamePayoutUtils.sol (92 lines). Not skim. Not scan. READ. Line by line. The BAF bug hid in a rare path inside a helper function -- AND THAT HELPER LIVES IN THIS CONTRACT.**

The COVERAGE-CHECKLIST.md from Wave 1 tells you exactly which functions to analyze. Work through them systematically in risk-tier order.

**PRIORITY ORDER (per D-06, D-07):**

**Tier 1 -- CRITICAL (analyze FIRST and DEEPEST):**

1. **B2 `payDailyJackpot()`** (~line 313-637, 325 lines) -- The largest function. 3 major paths (daily fresh, daily resume, early-burn). Manages currentPrizePool, futurePrizePool, nextPrizePool accounting. Calls _processDailyEth and _executeJackpot which both invoke _addClaimableEth. Trace EVERY local variable that caches a prize pool value. Does any local survive across a call to _addClaimableEth?

2. **B5 `consolidatePrizePools()`** (~line 850-879) -- Pool merging and yield surplus distribution. Writes currentPrizePool, prizePoolsPacked (future), yieldAccumulator. Calls _distributeYieldSurplus which calls _addClaimableEth for VAULT and SDGNRS. The research notes that `obligations` is a local snapshot of multiple pool values computed at lines 886-890, then _addClaimableEth can write futurePrizePool and claimablePool. VERIFY that `obligations` is not written back to storage.

3. **B6 `processTicketBatch()`** (~line 1812-1873) -- Gas-bounded ticket processing with inline Yul assembly. Manages ticketCursor, ticketLevel, ticketQueue cleanup. Deep call chain into _processOneTicketEntry -> _generateTicketBatch -> _raritySymbolBatch. Verify resume logic: if processing is interrupted mid-batch, does cursor state save correctly? Can tickets be skipped or double-processed?

4. **C3 `_addClaimableEth()`** (~line 928-949) [MULTI-PARENT] [BAF-CRITICAL] -- The BAF bug function. If auto-rebuy enabled, calls _processAutoRebuy which writes futurePrizePool or nextPrizePool. Returns claimableDelta (the take-profit portion only when auto-rebuy is on, vs full amount when off). Per D-07: re-audit from scratch as if v4.4 fix does not exist. For EVERY parent that calls this function (C2, C11, C13 via C14 via C15, C16 via C15), verify: does the parent cache futurePrizePool, nextPrizePool, or claimablePool before calling _addClaimableEth? If yes, does the parent write back the stale cache after the call returns?

5. **C4 `_processAutoRebuy()`** (~line 959-999) [BAF-CRITICAL] -- Writes futurePrizePool or nextPrizePool via _setFuturePrizePool/_setNextPrizePool. Also writes ticketQueue via _queueTickets and claimableWinnings via _creditClaimable. Verify that _calcAutoRebuy (PayoutUtils pure function) is actually pure. Verify the futurePrizePool/nextPrizePool path selection logic (level >= nextLevel ? future : next).

**Tier 2 -- HIGH (analyze after Tier 1):**

6. **B1 `runTerminalJackpot()`** (~line 272-308) -- Terminal x00-level ETH distribution via bucket system. Calls _distributeJackpotEth with BAF-path to _addClaimableEth.

7. **C2 `_distributeYieldSurplus()`** (~line 883-914) -- Yield accounting. Calls _addClaimableEth for VAULT and SDGNRS beneficiaries. The `obligations` local snapshot is the key BAF-check target.

8. **C11 `_processDailyEth()`** (~line 1338-1433) -- Per-winner ETH distribution loop calling _addClaimableEth. The research says it tracks `liabilityDelta` using the return value of _addClaimableEth (correct pattern). Verify independently.

9. **C14 `_resolveTraitWinners()`** (~line 1528-1655) -- Multi-path winner resolution (ETH vs COIN, solo vs normal). Multiple branches lead to _addClaimableEth via _creditJackpot or _processSoloBucketWinner.

10. **C16 `_processSoloBucketWinner()`** (~line 1684-1717) -- Whale pass conversion, futurePrizePool write. Calls _creditJackpot -> _addClaimableEth.

11. **C20 `_raritySymbolBatch()`** (~line 2050-2145) [ASSEMBLY] -- Inline Yul assembly with manual storage slot calculation for traitBurnTicket mapping. Attack angles per research: (a) storage slot calculation correctness vs Solidity layout, (b) array length accounting, (c) data slot calculation, (d) collision risk, (e) memory safety annotation.

**Tier 3 -- MEDIUM (remainder):**

12-19. All remaining Category B functions (B3, B4, B7) and significant Category C helpers not covered above.

**For EACH Category B function, produce a complete analysis section:**

```
## JackpotModule::functionName() (line X-Y)

### Call Tree
[Full recursive expansion. For every internal/external call, recursively expand it.
Write the call tree explicitly with line numbers for EVERY call site.
For cross-module calls (coin.creditFlip, dgnrs.transferFromPool, etc.), trace
only far enough to verify parent state coherence per D-08.]

### Storage Writes (Full Tree)
[Every storage variable written by ANY function in the call tree.
Format: variable_name (slot N) -- written by functionName() at line X
Include cross-module writes that affect parent's cached locals per D-09.]

### Cached-Local-vs-Storage Check
[THE BAF PATTERN CHECK -- #1 PRIORITY per D-06]
[List EVERY local variable that caches a storage value.]
[For each cached local, trace ALL descendant calls. Does any descendant write to the same storage slot?]
[List every (ancestor_local, descendant_write) pair and verdict: VULNERABLE / SAFE]
[If SAFE, explain exactly WHY -- which mechanism prevents stale writeback.]
[Per D-07: For paths through _addClaimableEth -> _processAutoRebuy, treat v4.4 fix as nonexistent.]

### Attack Analysis
**State Coherence:** [analysis] VERDICT: VULNERABLE / INVESTIGATE / SAFE
**Access Control:** [who can call, what modifier/require, is it only reachable via delegatecall from advanceGame?] VERDICT: ...
**RNG Manipulation:** [Per D-10: fresh analysis. Are RNG words unknown at commitment time? Can player-controllable state change between VRF request and fulfillment?] VERDICT: ...
**Cross-Contract State Desync:** [external calls to coin, dgnrs, steth -- can state change between reads?] VERDICT: ...
**Edge Cases:** [zero winners, empty buckets, max values, first day, game-over state, no trait tickets] VERDICT: ...
**Conditional Paths:** [EVERY branch. The BAF bug was on a rare path (auto-rebuy enabled). Check EVERY if/else.] VERDICT: ...
**Economic/MEV:** [can attacker profit from timing or ordering? Prize pool manipulation?] VERDICT: ...
**Griefing:** [can attacker cause permanent damage? Prize pool stranding?] VERDICT: ...
**Ordering/Sequencing:** [unexpected call order? Daily jackpot phases called out of sequence?] VERDICT: ...
**Silent Failures:** [does any path silently succeed when it should revert, or skip logic?] VERDICT: ...
```

**For EACH [MULTI-PARENT] Category C function, produce a standalone section:**

Per D-03, these functions need standalone analysis because different parents may have different cached-local states. For each multi-parent helper:

1. List ALL parents with the exact line where they call this function
2. For each parent: what storage values does the parent have cached locally at the point of the call?
3. For each parent: after this function returns, does the parent write back any cached value that this function may have modified?
4. Verdict per parent: SAFE / VULNERABLE

Multi-parent functions requiring standalone analysis (per research, verify against checklist):
- C3 `_addClaimableEth` -- parents: C2, C11, C13 (via C14/C15), C16 (via C15)
- C6 `_distributeTicketJackpot` -- parents: C5, B3
- C12 `_distributeJackpotEth` -- parents: B1, C10
- C22 `_awardDailyCoinToTraitWinners` -- parents: B3, B7
- C23 `_awardFarFutureCoinJackpot` -- parents: B3, B7
- C24 `_syncDailyWinningTraits` -- parents: B2, B7

**SPECIAL ANALYSIS: Inline Yul Assembly (_raritySymbolBatch)**

For the assembly block at lines ~2050-2145:
1. Manually compute the expected storage slot for `traitBurnTicket[lvl][traitId]` using Solidity's standard layout:
   - `keccak256(lvl, traitBurnTicket.slot)` gives the level mapping slot
   - The code uses `add(levelSlot, traitId)` for the trait -- verify this matches Solidity's layout for the inner dimension
2. Verify array length update: `sload(elem)` gets current length, `add(len, occurrences)` updates it, `sstore(elem, newLen)` saves it
3. Verify data slot: `keccak256(elem)` should give the dynamic array data start, then `add(data, len)` gives the next write position
4. Check for slot collision: can different (lvl, traitId) pairs produce the same slot?
5. Check the LCG: `seed * TICKET_LCG_MULT + 1` -- is this a valid Knuth MMIX LCG with guaranteed full period?

**FIVE COMMON PITFALLS TO HUNT (from research):**

1. **Liability tracking mismatch**: For every call to _addClaimableEth, verify the return value is accumulated into claimablePool (either inline or after the loop). A caller that ignores the return value or uses `weiAmount` instead of `claimableDelta` breaks the invariant.

2. **Auto-rebuy ETH diversion**: _addClaimableEth returns different values depending on auto-rebuy state. Callers must use the RETURNED claimableDelta, not the original weiAmount, for pool accounting.

3. **Assembly storage slot miscalculation**: _raritySymbolBatch manual keccak hash must match Solidity's standard layout.

4. **Gas-bounded iteration inconsistency**: processTicketBatch cursor/level state must be consistent at every exit point for correct resumption.

5. **Prize pool accounting drift**: currentPrizePool + nextPrizePool + futurePrizePool + claimablePool + yieldAccumulator must be conserved. Trace every deduction and credit.

**ANTI-SHORTCUTS DOCTRINE (from ULTIMATE-AUDIT-DESIGN.md):**
1. No "similar to above" -- every function gets its own full analysis
2. No skipping private helpers -- they get full scrutiny as part of call trees
3. No assuming guards work -- read the modifier/require, trace what it checks
4. No skipping the rare path -- if a function has an if(autoRebuy) branch, analyze it FULLY
5. No trusting previous audits -- start from zero (per D-07, D-10)
6. No batch dismissals -- "all coin jackpots are standard" is NOT acceptable
7. Call trees are MANDATORY
8. Storage write maps are MANDATORY

**BIAS: When in doubt, flag it. A false positive wastes 5 minutes of Skeptic review time. A missed bug costs the protocol.**

**What is NOT a valid attack (from ULTIMATE-AUDIT-DESIGN.md):**
- Arithmetic overflow/underflow (Solidity 0.8.34 built-in protection, audited in v3.0-v4.2)
- Classic reentrancy (no raw .call patterns, audited in v3.0-v3.3)
- Admin key compromise (owner trust assumption)
- VRF coordinator compromise (Chainlink trust assumption)
- Known issues listed in audit/KNOWN-ISSUES.md

**OUTPUT FORMAT:**
Start ATTACK-REPORT.md with a summary table, then per-function analysis sections organized by subsystem:

```
# Unit 3: Jackpot Distribution -- Attack Report

## Findings Summary
| ID | Function | Verdict | Severity | Title |
|----|----------|---------|----------|-------|
| F-01 | funcName | VULNERABLE | HIGH | [title] |

## Risk-Tier Analysis Order
1. Tier 1: B2, B5, B6, C3, C4
2. Tier 2: B1, C2, C11, C14, C16, C20
3. Tier 3: B3, B4, B7, remaining C functions

## ETH Distribution Subsystem
[B2, B1, B5, C2, C9, C10, C11, C12, C13, C14, C15, C16 analysis sections]

## BAF-Critical Payout Subsystem
[C3, C4, C26, C27, C28 analysis sections]

## Coin/BURNIE Jackpot Subsystem
[B7, B3, B4, C22, C23, C29 analysis sections]

## Ticket Distribution Subsystem
[B6, C5, C6, C7, C8, C17, C18, C19, C20, C21 analysis sections]

## Multi-Parent Standalone Analysis
[C3, C6, C12, C22, C23, C24 per-parent analysis]

## Inline Assembly Verification
[_raritySymbolBatch detailed assembly analysis]
```
  </action>

  <verify>
    <automated>test -f audit/unit-03/ATTACK-REPORT.md && echo "exists" && grep -c "## JackpotModule::\|## PayoutUtils::" audit/unit-03/ATTACK-REPORT.md && grep -c "VERDICT:" audit/unit-03/ATTACK-REPORT.md</automated>
  </verify>

  <acceptance_criteria>
    - File audit/unit-03/ATTACK-REPORT.md exists
    - Contains a Findings Summary table at the top
    - Has a dedicated analysis section for EVERY Category B function (7 external state-changing)
    - Has standalone analysis for EVERY [MULTI-PARENT] Category C function (at least C3, C6, C12, C22, C23, C24)
    - Every Category B section contains: Call Tree, Storage Writes (Full Tree), Cached-Local-vs-Storage Check, Attack Analysis with verdicts on all 10 angles
    - The BAF-critical chain (_addClaimableEth -> _processAutoRebuy -> futurePrizePool) is analyzed from scratch per D-07
    - _raritySymbolBatch inline Yul assembly has dedicated verification section
    - All line numbers reference actual source code lines (not estimates)
    - Every VULNERABLE or INVESTIGATE finding includes exact line numbers, concrete attack scenario, and proof-of-concept steps
    - No function uses "similar to above" or "standard pattern" as justification for skipping analysis
    - Known issues from audit/KNOWN-ISSUES.md are NOT re-reported
    - Cross-module external calls (coin, dgnrs, steth) are traced for state coherence per D-08
    - The 5 common pitfalls from research are explicitly hunted in relevant functions
  </acceptance_criteria>

  <done>Complete attack report with per-function analysis for all Category B functions (7) and all [MULTI-PARENT] Category C functions, with call trees, storage-write maps, BAF-pattern cache checks, multi-angle attack verdicts, inline assembly verification, and all Category C helpers traced through parent call trees</done>
</task>

</tasks>

<verification>
- ATTACK-REPORT.md covers every function on the COVERAGE-CHECKLIST.md
- Every Category B function has all four required sections (Call Tree, Storage Writes, Cache Check, Attack Analysis)
- Every [MULTI-PARENT] helper has per-parent standalone analysis
- _raritySymbolBatch assembly is independently verified
- The BAF-critical chain is re-audited from scratch (per D-07)
- All findings have verdicts (VULNERABLE / INVESTIGATE / SAFE)
- All VULNERABLE/INVESTIGATE findings have line numbers, attack scenarios, and PoC steps
</verification>

<success_criteria>
- The Mad Genius has systematically attacked every state-changing function in both contracts
- No function was skipped or dismissed without analysis
- The BAF-class cache-overwrite pattern was explicitly checked for every Category B function and every multi-parent helper
- The _addClaimableEth -> _processAutoRebuy chain was re-audited from scratch per D-07
- Inline Yul assembly storage slot correctness was independently verified
- Report is ready for Skeptic review in Wave 3
</success_criteria>

<output>
After completion, create `.planning/phases/105-jackpot-distribution/105-02-SUMMARY.md`
</output>
