---
phase: 440-verify
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []          # review-only; conditional gap-fix files listed in task <files> if a gap is found
autonomous: true
requirements: [VERIFY-02]
must_haves:
  truths:
    - "decBucket implements the absolute ladder 12@0,11@10,10@30,9@55,8@85,7@120,6@180,5@250,4@300,3@500,2@1000"
    - "decBucket floor clamp: normal decimator floor 5, century (lvl%100==0) floor 2, terminal-dec floor 2"
    - "minScoreForBucket is the exact band-floor inverse: 12->0,11->10,10->30,9->55,8->85,7->120,6->180,5->250,4->300,3->500,2->1000"
    - "FLIP.sol calls ActivityCurveLib.decBucket (no _adjustDecimatorBucket body left); DecimatorModule._terminalDecBucket delegates to the lib"
    - "DecimatorModule._minScoreForBucket(uint8) delegates to ActivityCurveLib.minScoreForBucket and feeds decimator-claim lootbox EV"
  artifacts:
    - path: "contracts/libraries/ActivityCurveLib.sol"
      provides: "decBucket forward ladder + minScoreForBucket inverse"
      contains: "function decBucket"
    - path: "contracts/FLIP.sol"
      provides: "decimator bucket call delegating to lib (no local body)"
      contains: "ActivityCurveLib.decBucket"
    - path: "contracts/modules/DegenerusGameDecimatorModule.sol"
      provides: "terminal-dec bucket + _minScoreForBucket inverse delegating to lib"
      contains: "ActivityCurveLib"
  key_links:
    - from: "contracts/modules/DegenerusGameDecimatorModule.sol"
      to: "ActivityCurveLib.minScoreForBucket"
      via: "_minScoreForBucket(winBucket) at decimator-claim EV"
      pattern: "minScoreForBucket"
    - from: "contracts/FLIP.sol"
      to: "ActivityCurveLib.decBucket"
      via: "decimator burn bucketing"
      pattern: "ActivityCurveLib\\.decBucket"
---

<objective>
Adversarially verify the bucket ladder + its exact inverse + the no-drift delegation wiring against the locked design (§3 of PLAN-ACTIVITY-CURVE-RESHAPE.md). Confirm: `decBucket` implements the absolute threshold ladder with the correct per-path floor clamp; `minScoreForBucket` is the EXACT band-floor inverse consistent with the forward ladder and feeds the decimator-claim lootbox EV; and BOTH FLIP.sol and DecimatorModule.sol delegate to the single lib with no duplicated body that could drift.

Purpose: VERIFY-02 is the ladder/inverse/anti-drift gate. A wrong threshold or a stale duplicated body silently mis-buckets or mis-prices the claim-time EV even on a fresh deploy.
Output: A pass/fail verdict on the 11 ladder thresholds, the 3 floor-clamp paths, the 11 inverse band-floors, and the delegation wiring. No contract commit. Apply any in-tree gap-fix (autonomous) if a threshold/inverse/wiring deviates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PLAN-ACTIVITY-CURVE-RESHAPE.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- The forward ladder and its inverse under review (locked §3). -->
ActivityCurveLib.decBucket(score, minBucket) -> uint8:
  thresholds (score >= T -> bucket): 1000->2, 500->3, 300->4, 250->5, 180->6, 120->7, 85->8, 55->9, 30->10, 10->11, else 12
  then: if bucket < minBucket -> bucket = minBucket
ActivityCurveLib.minScoreForBucket(bucket) -> uint16 (pre-floor inverse, band-floor T(b)):
  12->0, 11->10, 10->30, 9->55, 8->85, 7->120, 6->180, 5->250, 4->300, 3->500, 2->1000
Floor-clamp paths:
  FLIP.sol normal decimator: minBucket = (lvl%100==0) ? 2 : 5   (DECIMATOR_MIN_BUCKET_NORMAL=5 / _100=2)
  DecimatorModule terminal-dec: minBucket = TERMINAL_DEC_MIN_BUCKET = 2 (fixed)
Claim-time EV wiring: DecimatorModule _minScoreForBucket(winBucket) feeds the lootbox EV multiplier at decimator CLAIM (frozen score sealed at the winning burn's bucketing).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the forward ladder + floor clamps + the exact inverse in ActivityCurveLib against locked §3.1/§3.2</name>
  <read_first>
    - contracts/libraries/ActivityCurveLib.sol (BUCKET_BASE=12, BUCKET_T11..T2 constants, `decBucket`, `minScoreForBucket`)
    - .planning/PLAN-ACTIVITY-CURVE-RESHAPE.md §3.1 (forward ladder + floor clamp) and §3.2 (the band-floor inverse table; USER decision #5 = inverse uses each band's floor T(b))
  </read_first>
  <action>
    Read the bucket constants and the two functions. Confirm the threshold constants exactly: BUCKET_BASE==12, BUCKET_T11==10, BUCKET_T10==30, BUCKET_T9==55, BUCKET_T8==85, BUCKET_T7==120, BUCKET_T6==180, BUCKET_T5==250, BUCKET_T4==300, BUCKET_T3==500, BUCKET_T2==1000. Trace decBucket and confirm each of the 11 ladder outcomes at the band boundary: score 0->12, 10->11, 30->10, 55->9, 85->8, 120->7, 180->6, 250->5, 300->4, 500->3, 1000->2 (and verify the just-below-boundary score lands in the lower band, e.g. 999->3, 299->5, 249->6). Confirm the floor clamp is applied AFTER the ladder (`if bucket < minBucket bucket = minBucket`). Then verify minScoreForBucket is the EXACT band-floor inverse of decBucket: 12->0, 11->10, 10->30, 9->55, 8->85, 7->120, 6->180, 5->250, 4->300, 3->500, and bucket<=2 -> 1000. Cross-check consistency: for each bucket b in [2..11], decBucket(minScoreForBucket(b), 2) must equal b (the inverse round-trips through the forward ladder with the lowest floor) — confirm this holds for all 10 boundary scores and that bucket 12 maps to score 0. Confirm minScoreForBucket takes a single uint8 argument (NO lvl param — the band-floor inverse is level-independent per decision #5). If any threshold or inverse entry deviates, apply the minimal in-tree fix to ActivityCurveLib.sol (autonomous; no commit) and re-state. Do not paste full implementations; report the threshold name, expected, observed.
  </action>
  <acceptance_criteria>
    - Forward thresholds BUCKET_BASE=12, T11=10, T10=30, T9=55, T8=85, T7=120, T6=180, T5=250, T4=300, T3=500, T2=1000 confirmed exact.
    - decBucket boundary outcomes: 0->12, 10->11, 30->10, 55->9, 85->8, 120->7, 180->6, 250->5, 300->4, 500->3, 1000->2 confirmed; just-below boundaries land in the lower band (e.g. 999->3, 249->6).
    - Floor clamp applied after the ladder (bucket = max(ladderBucket, minBucket)).
    - minScoreForBucket exact band-floor inverse: 12->0, 11->10, 10->30, 9->55, 8->85, 7->120, 6->180, 5->250, 4->300, 3->500, 2->1000; single uint8 arg (no lvl).
    - Round-trip decBucket(minScoreForBucket(b), 2)==b holds for b in [2..11].
    - Any deviation corrected in-tree (file in <files>); clean review applies NO edit.
  </acceptance_criteria>
  <files>contracts/libraries/ActivityCurveLib.sol</files>
  <verify>
    <automated>forge test --match-contract ConsumerPointEquivalence -vv</automated>
  </verify>
  <done>All 11 forward thresholds, the floor-clamp ordering, and all 11 inverse band-floors confirmed against locked §3.1/§3.2; inverse round-trips through the forward ladder; minScoreForBucket is level-independent.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the no-drift delegation wiring (FLIP + Decimator) and the per-path floor + claim-time EV feed</name>
  <read_first>
    - contracts/FLIP.sol (the decimator burn path ~:657-670: minBucket selection + ActivityCurveLib.decBucket call; confirm NO local _adjustDecimatorBucket / _decimatorBurnMultiplier body remains)
    - contracts/modules/DegenerusGameDecimatorModule.sol (`_terminalDecBucket` ~:1114 delegating to the lib; `_minScoreForBucket` ~:684 delegating to the lib; the claim-time call site ~:409-412 passing _minScoreForBucket(winBucket); TERMINAL_DEC_MIN_BUCKET=2)
    - .planning/PLAN-ACTIVITY-CURVE-RESHAPE.md §3.1 (floor paths), §3.2 (claim-time EV reads the inverse), §4 (anti-drift: one shared lib, no duplicated body)
  </read_first>
  <action>
    Confirm the delegation is genuine, not copy-paste. In FLIP.sol: confirm the decimator burn computes minBucket = (lvl%100==0) ? DECIMATOR_MIN_BUCKET_100(2) : DECIMATOR_MIN_BUCKET_NORMAL(5) and calls ActivityCurveLib.decBucket(bonusPoints, minBucket) and ActivityCurveLib.decMultBps(bonusPoints); confirm the old private helpers `_adjustDecimatorBucket` and `_decimatorBurnMultiplier` are GONE (no residual body that could drift) and the constants DECIMATOR_BUCKET_BASE / DECIMATOR_ACTIVITY_CAP_POINTS are removed. In DecimatorModule.sol: confirm `_terminalDecBucket(bonusPoints)` returns ActivityCurveLib.decBucket(bonusPoints, TERMINAL_DEC_MIN_BUCKET) with TERMINAL_DEC_MIN_BUCKET==2 (fixed floor), and that the old arithmetic terminal-bucket body + TERMINAL_DEC_BUCKET_BASE + TERMINAL_DEC_ACTIVITY_CAP_POINTS constants are removed. Confirm `_minScoreForBucket(uint8)` delegates to ActivityCurveLib.minScoreForBucket and that the OLD lvl-aware arithmetic inverse body is gone; confirm the claim-time call site passes `_minScoreForBucket(winBucket)` (single arg — the lvl param was dropped) into the lootbox EV computation so the decimator-claim EV reads the sealed/frozen band-floor score, not a live score. Use `git grep` to prove no second copy of the ladder or multiplier arithmetic survives in either file. If a duplicated body or a wrong floor remains, apply the minimal in-tree fix (autonomous; no commit). Do not paste full implementations.
  </action>
  <acceptance_criteria>
    - `git grep -n "ActivityCurveLib.decBucket" contracts/FLIP.sol` and `... contracts/modules/DegenerusGameDecimatorModule.sol` each match; `git grep -n "_adjustDecimatorBucket\|_decimatorBurnMultiplier" contracts/FLIP.sol` returns nothing.
    - FLIP normal floor = (lvl%100==0)?2:5 via DECIMATOR_MIN_BUCKET_100/_NORMAL; terminal-dec floor = TERMINAL_DEC_MIN_BUCKET==2 (fixed).
    - DecimatorModule._minScoreForBucket(uint8) delegates to ActivityCurveLib.minScoreForBucket; the claim-time call passes _minScoreForBucket(winBucket) (single arg) into the lootbox EV computation; old lvl-aware arithmetic inverse removed.
    - No surviving duplicated ladder/multiplier body in either file; stale constants DECIMATOR_BUCKET_BASE, DECIMATOR_ACTIVITY_CAP_POINTS, TERMINAL_DEC_BUCKET_BASE, TERMINAL_DEC_ACTIVITY_CAP_POINTS removed.
    - Any deviation corrected in-tree (files in <files>); clean review applies NO edit.
  </acceptance_criteria>
  <files>contracts/FLIP.sol, contracts/modules/DegenerusGameDecimatorModule.sol</files>
  <verify>
    <automated>git grep -nE 'ActivityCurveLib\.(decBucket|decMultBps|minScoreForBucket)' -- contracts/FLIP.sol contracts/modules/DegenerusGameDecimatorModule.sol</automated>
  </verify>
  <done>FLIP and DecimatorModule both delegate to the single lib with no duplicated body; floor paths (5/2 normal, 2 terminal) correct; claim-time EV reads the band-floor inverse via the single-arg _minScoreForBucket; stale constants gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| activity score -> bucket -> claim-time lootbox EV | The player's burn score selects a bucket (better odds); the SEALED band-floor inverse re-derives the EV-input score at claim time. A wrong threshold or a divergent FLIP/Decimator copy mis-prices odds or EV. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-440-04 | Tampering | FLIP/Decimator ladder bodies drift apart | mitigate | This plan proves both delegate to the single ActivityCurveLib with NO duplicated body (git grep gate); one source of truth = no drift. |
| T-440-05 | Spoofing | claim-time EV reads a wrong/inflated band score via a bad inverse | mitigate | minScoreForBucket is asserted to be the exact band-floor inverse round-tripping through the forward ladder; the claim reads the sealed band-floor, not a live score (freeze re-attested in 443 REAUDIT-02). |
| T-440-06 | Elevation of Privilege | floor clamp lets a low score reach a too-good bucket | accept | Floor clamp is a CEILING on quality (raises bucket number toward the worse-odds floor when score is low); confirmed applied after the ladder. No path lowers the bucket below the per-path floor. |
</threat_model>

<verification>
- `forge test --match-contract ConsumerPointEquivalence -vv` is green (exercises decBucket + minScoreForBucket).
- All 11 thresholds + 11 inverse band-floors match locked §3; the inverse round-trips through the forward ladder.
- FLIP + DecimatorModule delegate to the single lib (git grep gate); no duplicated body; stale constants removed.
- Any in-tree gap-fix applied to the named .sol only; NO `git commit` of any contracts/*.sol.
</verification>

<success_criteria>
Bucket ladder, exact band-floor inverse, per-path floor clamps, and the FLIP↔Decimator single-lib delegation all confirmed against locked §3/§4, with the claim-time EV feed reading the sealed band-floor score. No contract commit. Gap-fixes (if any) applied in-tree and re-verified.
</success_criteria>

<output>
Create `.planning/phases/440-verify-review-the-working-tree-reshape-against-the-locked-de/440-02-SUMMARY.md` when done
</output>
