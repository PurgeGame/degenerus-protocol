---
phase: 440-verify
plan: 04
type: execute
wave: 2
depends_on: [440-01, 440-02, 440-03]
files_modified: []          # measurement only; no edits expected (any curve/ladder/migration gap-fix lands in 01-03)
autonomous: true
requirements: [VERIFY-04]
must_haves:
  truths:
    - "forge build exits 0 on the final working tree (after a forge clean to avoid the 1.6-nightly stale-storageLayout artifact)"
    - "ActivityCurveLib carries NO storage slots (pure library, all internal pure functions)"
    - "DegenerusGame runtime deployed bytecode < 24576 bytes (EIP-170); FLIP runtime < 24576"
    - "No read-side gas regression on decimatorBurn / placeBet / lootbox-open / century paths"
    - "advanceGame 16.7M ceiling confirmed NOT implicated (the reshape is read-side, not in the advance loop)"
  artifacts:
    - path: "contracts/libraries/ActivityCurveLib.sol"
      provides: "storageless pure lib"
      contains: "internal pure"
  key_links:
    - from: "forge build --sizes"
      to: "DegenerusGame / FLIP runtime size"
      via: "EIP-170 ceiling check"
      pattern: "24576"
---

<objective>
Verify the reshaped working tree BUILDS and stays within bounds against the locked design (§8 of PLAN-ACTIVITY-CURVE-RESHAPE.md). Confirm: `forge build` is clean; the new ActivityCurveLib carries no storage; DegenerusGame and FLIP stay under the EIP-170 24,576-byte deployed-bytecode ceiling with the added piecewise branches; there is no read-side gas regression on the consumer paths; and the advanceGame 16.7M ceiling is confirmed not implicated.

Purpose: VERIFY-04 is the build/bounds gate, and it runs in WAVE 2 — AFTER any curve/ladder/migration gap-fixes from plans 01-03 have landed in the working tree — so the build, EIP-170, and gas measurements reflect the FINAL state that phase 441 will freeze.
Output: A green build, a storageless-lib confirmation, the EIP-170 numbers for DegenerusGame + FLIP (with headroom), a read-side gas verdict, and the advanceGame-not-implicated confirmation. No contract commit; no edits expected (gap-fixes belong to 01-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PLAN-ACTIVITY-CURVE-RESHAPE.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

<interfaces>
<!-- Bounds context. v69 closure baseline (subject 8a633d1d): DegenerusGame 20,388 B / 4,188 B headroom.
     The reshape adds piecewise branches -> re-measure; expect a small growth, must stay < 24,576. -->
EIP-170 ceiling: 24,576 bytes deployed (runtime) bytecode.
v69 baseline DegenerusGame: 20,388 B (headroom 4,188).
forge 1.6.0-nightly gotcha: storageLayout can be missing from the artifact unless `forge clean` runs first (a known
  caching artifact noted in v69 REAUDIT-01) — clean before the build/size/layout reads.
Solvency bounds to re-confirm hold structurally (design §8.3): ROI strictly <10000, lootbox EV <=14500 still gated by
  LOOTBOX_EV_BENEFIT_CAP, century <=100% still ETH-capped (20-ETH maxBonus), multiplier <=17833. (The numeric proof
  is plan 01; this plan confirms the gating constants/caps still wrap the curves and nothing widened them.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean build + storageless-lib + EIP-170 ceiling on the final working tree</name>
  <read_first>
    - contracts/libraries/ActivityCurveLib.sol (confirm EVERY function is `internal pure` and there are NO state variables — only `constant`s, which carry no storage slot)
    - .planning/STATE.md (the v69 EIP-170 baseline line: DegenerusGame 20,388 / 4,188 headroom; the forge-clean storageLayout gotcha)
    - .planning/PLAN-ACTIVITY-CURVE-RESHAPE.md §8.4 (EIP-170 / gas plan)
  </read_first>
  <action>
    Run `forge clean` first (avoids the forge-1.6-nightly stale-storageLayout artifact that produced a false all-removed layout diff in v69). Then `forge build` and confirm it exits 0 with no errors. Confirm ActivityCurveLib is storageless: read the file and verify it declares only `library` with `internal pure` functions and `constant` values (constants live in code, not storage) — there are NO non-constant state variables, so it occupies zero storage slots and cannot move the layout of any consumer. Then run `forge build --sizes` and read the runtime (deployed) bytecode size for `DegenerusGame` and `FLIP`; confirm BOTH are strictly < 24576 bytes and record the actual size + headroom for each. Compare DegenerusGame against the v69 baseline 20,388 B — the added piecewise branches will grow it modestly; confirm the growth still leaves positive headroom (the locked expectation is the reshape stays under the ceiling). If the build fails or either contract exceeds 24576, that is a hard gap — but note: a build failure most likely indicates a gap-fix from plans 01-03 introduced a syntax error, so re-run after confirming 01-03 landed cleanly; do NOT silently edit a curve here (curve/ladder fixes belong to 01-03). Report the two sizes with headroom.
  </action>
  <acceptance_criteria>
    - `forge clean` then `forge build` exits 0 with no compiler errors.
    - ActivityCurveLib.sol declares only `internal pure` functions and `constant`s — zero non-constant state variables (storageless); confirmed by reading the file.
    - `forge build --sizes` shows DegenerusGame runtime size < 24576 and FLIP runtime size < 24576; both actual sizes + headroom recorded; DegenerusGame growth vs the 20,388 B v69 baseline noted and still under ceiling.
  </acceptance_criteria>
  <verify>
    <automated>forge clean && forge build --sizes 2>&1 | grep -E 'DegenerusGame|FLIP' | head -20</automated>
  </verify>
  <done>Build clean on the final tree; ActivityCurveLib storageless; DegenerusGame + FLIP both under the EIP-170 24,576 ceiling with recorded headroom.</done>
</task>

<task type="auto">
  <name>Task 2: Read-side gas non-regression + advanceGame-not-implicated + solvency-gating confirmation</name>
  <read_first>
    - .planning/PLAN-ACTIVITY-CURVE-RESHAPE.md §8.3 (solvency re-derivation: ROI<10000, lootbox EV<=14500 gated by LOOTBOX_EV_BENEFIT_CAP, century ETH-capped, multiplier<=17833) and §8.4 (gas: read-side, not in advanceGame loop, so 16.7M not implicated)
    - The consumer call paths (read-only, to confirm the curves are read-side): contracts/FLIP.sol decimator burn (~:640-700), contracts/modules/DegenerusGameDegeneretteModule.sol placeBet/settle, contracts/modules/DegenerusGameStorage.sol lootbox EV, the century paths in Mint/Afking; and confirm NONE of the reshaped curves is called inside the advanceGame loop
    - .claude/skills (economic-analyst is the relevant lens if present) — but keep this a targeted read-side check, NOT a full council audit (that is phase 443)
  </read_first>
  <action>
    Confirm the reshape is read-side and does not regress gas or touch the advance loop. First, confirm the curve functions are pure/inlined helpers called on user-action read paths (decimatorBurn, placeBet/settle, lootbox-open, century mint/afking) and NOT inside advanceGame: grep the advanceGame implementation path for any call to decMultBps / decBucket / centuryBps / _roiBpsFromScore / _wwxrpHighValueRoi / _lootboxEvMultiplierFromScore and confirm there are none in the advance loop (the curves are consumed at burn/bet/open/mint time, not during the per-level advance) — so the 16.7M advanceGame ceiling is structurally not implicated. Second, assess read-side gas: the old consumers were small arithmetic; the new curves are a handful of comparison + mul/div branches that the compiler inlines (the lib functions are `internal pure`). Confirm the branch count per curve is bounded (each is at most 4 segments) so the marginal gas on decimatorBurn / placeBet / lootbox-open / century is a small constant, not a loop — i.e. no algorithmic regression. If a `forge test --gas-report` or an existing gas snapshot for these paths is readily available, run the targeted one for the consumer test contract and note any delta; otherwise confirm-by-inspection that the curves add only bounded straight-line branches (no new loops, no new external calls, no new storage reads beyond the single playerActivityScore read that already existed). Third, re-confirm the solvency gating still wraps the curves: ROI output strictly <10000 (max 9990, from plan 01), lootbox EV still bounded by LOOTBOX_EV_BENEFIT_CAP (the per-account-per-level cap is unchanged), century bonus still bounded by the 20-ETH maxBonus ETH cap (Mint:1715 / Afking:838 unchanged), multiplier <=17833 — confirm none of these caps/gates were removed or widened by the reshape diff. Report the read-side gas verdict + the advanceGame-not-implicated confirmation + the solvency-gates-intact confirmation. No edits.
  </action>
  <acceptance_criteria>
    - No call to any reshaped curve (decMultBps/decBucket/centuryBps/_roiBpsFromScore/_wwxrpHighValueRoi/_lootboxEvMultiplierFromScore) inside the advanceGame loop; the 16.7M ceiling confirmed structurally not implicated.
    - Read-side gas: each curve adds only bounded straight-line branches (<=4 segments, inlined `internal pure`), no new loops/external calls/extra storage reads; no algorithmic regression on decimatorBurn / placeBet / lootbox-open / century. Any available targeted gas-report delta recorded.
    - Solvency gates intact and not widened: ROI<10000, lootbox EV gated by LOOTBOX_EV_BENEFIT_CAP, century gated by the 20-ETH maxBonus, multiplier<=17833 — confirmed unchanged by the diff.
  </acceptance_criteria>
  <verify>
    <automated>git grep -nE 'decMultBps|decBucket|centuryBps|_roiBpsFromScore|_wwxrpHighValueRoi|_lootboxEvMultiplierFromScore' -- contracts/ | grep -i advance || echo "NONE-IN-ADVANCE"</automated>
  </verify>
  <done>Reshape confirmed read-side: no curve in the advanceGame loop (16.7M not implicated); no read-side gas regression; the solvency gates (ROI<10000, EV benefit cap, century ETH cap, multiplier<=17833) intact and not widened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reshape diff -> deployed-bytecode + gas envelope | The added piecewise branches grow bytecode and per-call gas; the contracts must stay deployable (EIP-170) and the advanceGame loop must stay under its block-gas ceiling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-440-10 | Denial of Service | DegenerusGame/FLIP exceeds EIP-170 -> undeployable | mitigate | This plan measures both via `forge build --sizes` and asserts < 24576 with recorded headroom; a breach is a hard gap surfaced before FREEZE. |
| T-440-11 | Denial of Service | HIGH-weight: a curve lands in the advanceGame loop and inflates it past 16.7M | mitigate | This plan greps the advance loop for any curve call and confirms none — the reshape is read-side (burn/bet/open/mint paths), structurally outside the advance loop. |
| T-440-12 | Tampering | the reshape silently widened a solvency cap (EV benefit cap / century ETH cap) | mitigate | This plan confirms the gating caps are unchanged by the diff; the curves remain wrapped by the same bounds (re-derived design §8.3). |
</threat_model>

<verification>
- `forge clean && forge build` exits 0; `forge build --sizes` shows DegenerusGame + FLIP < 24576 with recorded headroom.
- ActivityCurveLib storageless (internal pure + constants only).
- No reshaped curve in the advanceGame loop (16.7M not implicated); read-side gas bounded; solvency gates intact.
- No `git commit` of any contracts/*.sol; no edits expected in this plan.
</verification>

<success_criteria>
The final working tree builds clean, the new lib is storageless, DegenerusGame + FLIP stay under EIP-170 with recorded headroom, the read-side consumer paths show no gas regression, the advanceGame 16.7M ceiling is confirmed not implicated, and the solvency gates are intact — measured on the post-01/02/03 working tree that phase 441 will freeze. No contract commit.
</success_criteria>

<output>
Create `.planning/phases/440-verify-review-the-working-tree-reshape-against-the-locked-de/440-04-SUMMARY.md` when done
</output>
