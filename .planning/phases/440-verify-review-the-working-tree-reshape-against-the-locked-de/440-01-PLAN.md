---
phase: 440-verify
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []          # review-only; conditional gap-fix files listed in task <files> if a gap is found
autonomous: true
requirements: [VERIFY-01]
must_haves:
  truths:
    - "decMultBps(0)=10000 no-op, decMultBps(235)=17049, decMultBps(500)=17676, decMultBps(30000)=17833 (byte-identical to prior MULT MAX), flat beyond cap"
    - "_roiBpsFromScore: 0->9000, 305->9891, 500->9970, 30000->9990, strictly <10000 everywhere"
    - "_wwxrpHighValueRoi: 0->9000, 305->10791, 500->10950, 30000->10990"
    - "centuryBps: 0->0, 305->9000, 500->9800, 30000->10000"
    - "_lootboxEvMultiplierFromScore: 0->9000, 60->10000, 400->13950, 500->14390, 30000->14500, flat beyond"
    - "Each curve is monotonic non-decreasing over [0,30000] and continuous at both knees (K and 500)"
  artifacts:
    - path: "contracts/libraries/ActivityCurveLib.sol"
      provides: "decMultBps + centuryBps value curves"
      contains: "function decMultBps"
    - path: "contracts/modules/DegenerusGameDegeneretteModule.sol"
      provides: "_roiBpsFromScore + _wwxrpHighValueRoi inline curves"
      contains: "_roiBpsFromScore"
    - path: "contracts/storage/DegenerusGameStorage.sol"
      provides: "_lootboxEvMultiplierFromScore inline curve"
      contains: "_lootboxEvMultiplierFromScore"
  key_links:
    - from: "test/fuzz/ConsumerPointEquivalence.t.sol"
      to: "contracts/libraries/ActivityCurveLib.sol"
      via: "import + waypoint asserts"
      pattern: "ActivityCurveLib\\.decMultBps"
---

<objective>
Adversarially verify the FIVE 3-segment piecewise value curves in the already-written working tree against the locked design (§2 of PLAN-ACTIVITY-CURVE-RESHAPE.md). Confirm every constant, knee, and waypoint matches the locked numbers; each curve is byte-identical to its prior named MAX at the cap; each is monotonic non-decreasing over [0,30000] and continuous at both knees; and the decimator s==0 -> 1.0x no-op is preserved.

Purpose: VERIFY-01 is the numeric correctness gate. The implementation already exists; this plan proves it equals the locked spec, it does NOT re-derive or re-implement the curves.
Output: A pass/fail verdict per curve with the actual working-tree constants checked against the locked waypoints, plus the core equivalence test run. No contract commit. If a constant/branch deviates from the locked spec, apply the minimal in-tree gap-fix to the named .sol (autonomous; the commit is phase 441).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PLAN-ACTIVITY-CURVE-RESHAPE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

<interfaces>
<!-- The five value curves under review and their locked endpoints. Verify code == these numbers. -->
ActivityCurveLib.sol:
  decMultBps(score)  K=235 MIN=10000 vA=17049 vB=17676 MAX=17833  (s==0 -> 10000 no-op)
  centuryBps(score)  K=305 MIN=0     vA=9000  vB=9800  MAX=10000
  shared knees: ACTIVITY_SEG_B_KNEE_POINTS=500, ACTIVITY_EFFECTIVE_CAP_POINTS=30000 (span 29500)
DegenerusGameDegeneretteModule.sol:
  _roiBpsFromScore(score)     K=305 MIN=9000 vA=9891  vB=9970  MAX=9990   (strictly <10000)
  _wwxrpHighValueRoi(score)   K=305 MIN=9000 vA=10791 vB=10950 MAX=10990
DegenerusGameStorage.sol:
  _lootboxEvMultiplierFromScore(score)  low anchor 0->9000, 60->10000 (verbatim);
    then K=400 vA=13950 vB=14390 MAX=14500 over 60..MAX
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the dec/term multiplier + century curves in ActivityCurveLib against locked §2.1/§2.4</name>
  <read_first>
    - contracts/libraries/ActivityCurveLib.sol (the full file: decMultBps, centuryBps, the MULT_* and CENTURY_* constants, ACTIVITY_SEG_B_KNEE_POINTS=500, ACTIVITY_EFFECTIVE_CAP_POINTS=30000)
    - .planning/PLAN-ACTIVITY-CURVE-RESHAPE.md §2.1 (multiplier waypoints) and §2.4 (century waypoints), plus §2 template line for the 3-segment shape
  </read_first>
  <action>
    Read ActivityCurveLib.sol and compare each named constant to the locked spec: MULT_MIN_BPS==10000, MULT_K_POINTS==235, MULT_VA_BPS==17049, MULT_VB_BPS==17676, MULT_MAX_BPS==17833; CENTURY_K_POINTS==305, CENTURY_VA_BPS==9000, CENTURY_VB_BPS==9800, CENTURY_MAX_BPS==10000. Confirm the segment structure matches the §2 template: for decMultBps the order is `if s==0 return 10000; if s>=30000 return 17833; if s<=235 ...; if s<=500 ...; else seg-C`. Compute the four multiplier waypoints by hand from the code's arithmetic and confirm: decMultBps(0)=10000, decMultBps(235)=17049, decMultBps(500)=17676, decMultBps(30000)=17833, and decMultBps(30000+anything)=17833 (flat). Confirm the s==0 short-circuit returns exactly MULT_MIN_BPS (1.0x no-op) — this is the gate that keeps a zero-score burner at 1x. For centuryBps confirm waypoints: centuryBps(0)=0, centuryBps(305)=9000, centuryBps(500)=9800, centuryBps(30000)=10000; note centuryBps has NO s==0 early return (s<=305 branch yields 0 naturally) — confirm that path returns 0. Check continuity at each knee: the seg-A value at s=235 (for mult) / s=305 (for century) equals vA, and the seg-B value at s=500 equals vB, with no off-by-one gap. If ANY constant or branch deviates from the locked number, apply the minimal correction to ActivityCurveLib.sol in the working tree (autonomous edit; do NOT commit) and re-state the corrected waypoint. Do NOT paste full implementations into the summary; report the constant name, the expected value, and the observed value.
  </action>
  <acceptance_criteria>
    - decMultBps: constants MULT_{MIN=10000,K=235,VA=17049,VB=17676,MAX=17833} confirmed present and exact; waypoints decMultBps(0)=10000, decMultBps(235)=17049, decMultBps(500)=17676, decMultBps(30000)=17833 confirmed; flat-beyond-cap confirmed (>=30000 returns 17833); s==0 no-op returns exactly 10000.
    - centuryBps: constants CENTURY_{K=305,VA=9000,VB=9800,MAX=10000} confirmed; waypoints centuryBps(0)=0, centuryBps(305)=9000, centuryBps(500)=9800, centuryBps(30000)=10000 confirmed.
    - Both curves continuous at their two knees (no off-by-one gap at K and at 500); shared knees ACTIVITY_SEG_B_KNEE_POINTS=500, ACTIVITY_EFFECTIVE_CAP_POINTS=30000.
    - Any deviation is corrected in-tree (file listed in <files>) and re-verified; a clean review applies NO edit.
  </acceptance_criteria>
  <files>contracts/libraries/ActivityCurveLib.sol</files>
  <verify>
    <automated>forge test --match-contract ConsumerPointEquivalence -vv</automated>
  </verify>
  <done>decMultBps and centuryBps constants + the four waypoints each + continuity confirmed against locked §2.1/§2.4; s==0 no-op preserved; equivalence test green or any gap fixed in-tree.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the Degenerette ROI, WWXRP, and lootbox EV curves against locked §2.2/§2.3/§2.5</name>
  <read_first>
    - contracts/modules/DegenerusGameDegeneretteModule.sol (the constants block ~:185-220 and `_roiBpsFromScore` ~:1141 and `_wwxrpHighValueRoi` ~:1178)
    - contracts/storage/DegenerusGameStorage.sol (the LOOTBOX_EV_* constants ~:1552-1565 and `_lootboxEvMultiplierFromScore` ~:1631)
    - .planning/PLAN-ACTIVITY-CURVE-RESHAPE.md §2.2 (ROI), §2.3 (WWXRP), §2.5 (lootbox EV — note the 0..60 anchor is verbatim, reshape governs 60..MAX, vA/vB from the FULL (9000,14500) range per USER decision #4)
  </read_first>
  <action>
    Read the three inline curves and confirm their constants against the locked spec: ROI uses ROI_MIN_BPS==9000, ROI_VA_BPS==9891, ROI_VB_BPS==9970, ROI_MAX_BPS==9990, knee ACTIVITY_SCORE_MAX_POINTS==305; WWXRP uses WWXRP_HIGH_ROI_BASE_BPS==9000, WWXRP_HIGH_ROI_VA_BPS==10791, WWXRP_HIGH_ROI_VB_BPS==10950, WWXRP_HIGH_ROI_MAX_BPS==10990, knee 305; lootbox uses LOOTBOX_EV_MIN_BPS==9000, LOOTBOX_EV_NEUTRAL_BPS==10000 at LOOTBOX_EV_ACTIVITY_NEUTRAL_POINTS==60, LOOTBOX_EV_VA_BPS==13950 at LOOTBOX_EV_ACTIVITY_MAX_POINTS==400, LOOTBOX_EV_VB_BPS==14390, LOOTBOX_EV_MAX_BPS==14500. Hand-compute and confirm waypoints: ROI 0->9000, 305->9891, 500->9970, 30000->9990 and that ROI is STRICTLY <10000 at every score (the solvency floor — 9990 max); WWXRP 0->9000, 305->10791, 500->10950, 30000->10990; lootbox EV 0->9000, 60->10000, 400->13950, 500->14390, 30000->14500. Confirm each function's >=30000 branch returns the MAX byte-identically and is flat beyond. Confirm continuity at each knee (ROI/WWXRP at 305 and 500; lootbox at 60, 400, and 500 — note the 0..60 neutral anchor segment must be unchanged from baseline). Confirm each curve is monotonic non-decreasing by checking the seg slopes are all positive (vA>MIN, vB>vA, MAX>vB for every curve). For the lootbox curve specifically, confirm vA=13950 was derived from the full (9000,14500) range (90% of the 5500 gain above 9000 = 13950), consistent with USER decision #4, NOT from the 10000 neutral anchor. If any constant/branch deviates, apply the minimal in-tree fix to the named .sol (autonomous; no commit) and re-state the corrected waypoint. Do not paste full implementations.
  </action>
  <acceptance_criteria>
    - ROI: constants {MIN=9000,VA=9891,VB=9970,MAX=9990,K=305} confirmed; waypoints 0->9000, 305->9891, 500->9970, 30000->9990 confirmed; ROI strictly <10000 at all scores.
    - WWXRP: constants {BASE=9000,VA=10791,VB=10950,MAX=10990,K=305} confirmed; waypoints 0->9000, 305->10791, 500->10950, 30000->10990 confirmed.
    - Lootbox EV: constants {MIN=9000,NEUTRAL=10000@60,VA=13950@400,VB=14390,MAX=14500} confirmed; waypoints 0->9000, 60->10000, 400->13950, 500->14390, 30000->14500 confirmed; the 0..60 neutral anchor segment unchanged; vA=13950 derived from the full (9000,14500) range (decision #4).
    - All three curves monotonic non-decreasing (positive seg slopes) and continuous at all knees; each >=30000 returns its MAX flat.
    - Any deviation corrected in-tree (file in <files>); clean review applies NO edit.
  </acceptance_criteria>
  <files>contracts/modules/DegenerusGameDegeneretteModule.sol, contracts/storage/DegenerusGameStorage.sol</files>
  <verify>
    <automated>forge test --match-contract ConsumerPointEquivalence -vv</automated>
  </verify>
  <done>ROI, WWXRP, and lootbox EV constants + waypoints + monotonicity + continuity + flat-beyond-cap confirmed against locked §2.2/§2.3/§2.5; ROI<10000 solvency floor confirmed; equivalence test green or any gap fixed in-tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player activity score -> value curve | Untrusted player score (already bounded by ACTIVITY_SCORE_HARD_CAP_POINTS=65534 upstream) crosses into the reward-curve math; the curve output sizes a payout/EV/bonus. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-440-01 | Tampering | value-curve output exceeds its solvency bound | mitigate | This plan asserts each curve's MAX byte-identical to its prior named cap (mult<=17833, ROI strictly <10000, WWXRP<=10990, century<=10000, lootbox EV<=14500); the >=30000 branch saturates flat so no input drives output past MAX. ROI<10000 is the solvency floor (re-derived per design §8.3). |
| T-440-02 | Elevation of Privilege | curve non-monotonic lets a player pick a lower score for a higher reward | accept | Monotonic non-decreasing over [0,30000] is asserted here; a clean review confirms no score yields a strictly higher output than a greater score. No gaming surface if monotonic. |
| T-440-03 | Information Disclosure | DOMINANT: activity score freeze (the anti-gaming knob) | accept | The reshape is read-side only; the snapshot-at-deposit freeze that pins the score is unchanged and is re-attested in phase 443 REAUDIT-02. Out of scope for this curve-numeric plan beyond confirming no curve reads a live mutable score. |
</threat_model>

<verification>
- `forge test --match-contract ConsumerPointEquivalence -vv` is green (the core equivalence oracle already imports ActivityCurveLib and asserts the waypoints).
- Every locked waypoint in §2.1-§2.5 matches the working-tree constants/branches; ROI strictly <10000; each curve monotonic + continuous + flat-beyond-cap.
- Any in-tree gap-fix is applied to the named .sol only; NO `git commit` of any contracts/*.sol.
</verification>

<success_criteria>
All five value curves confirmed equal to the locked §2 spec (constants, knees, waypoints, MAX byte-identity, monotonicity, continuity, flat-beyond-cap, s==0 no-op), with the core equivalence test green. No contract commit. Gap-fixes (if any) applied in-tree and re-verified.
</success_criteria>

<output>
Create `.planning/phases/440-verify-review-the-working-tree-reshape-against-the-locked-de/440-01-SUMMARY.md` when done
</output>
