---
phase: 440-verify
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: []          # review-only; conditional gap-fix files listed in task <files> if a gap is found
autonomous: true
requirements: [VERIFY-03]
must_haves:
  truths:
    - "All six §1 pre-clamp sites are removed: FLIP:667-668, Decimator:796-797, Decimator:918-920, Degenerette:1144-1146 (folded), Degenerette:1181-1183 (folded), Mint:1712, Afking:835"
    - "Removed constants ACTIVITY_SCORE_MID_POINTS, ACTIVITY_SCORE_HIGH_POINTS, ROI_MID_BPS, ROI_HIGH_BPS appear nowhere in contracts/"
    - "Every playerActivityScore / _playerActivityScore read-site either stores the raw score or feeds it through a NEW curve — no consumer reads the score through old saturated arithmetic"
    - "centuryBps is the single shared helper used by both Mint:1714 and Afking:837 (no divergent inline copy)"
  artifacts:
    - path: "contracts/FLIP.sol"
      provides: "decimator score read with no pre-clamp"
      contains: "playerActivityScore"
    - path: "contracts/modules/DegenerusGameDecimatorModule.sol"
      provides: "terminal-dec score reads with no pre-clamp"
      contains: "playerActivityScore"
    - path: "contracts/modules/DegenerusGameDegeneretteModule.sol"
      provides: "ROI/WWXRP entry pre-clamps folded into the >=30000 branch"
      contains: "ACTIVITY_EFFECTIVE_CAP_POINTS"
  key_links:
    - from: "contracts/modules/DegenerusGameMintModule.sol"
      to: "ActivityCurveLib.centuryBps"
      via: "century bonus"
      pattern: "ActivityCurveLib\\.centuryBps"
    - from: "contracts/modules/GameAfkingModule.sol"
      to: "ActivityCurveLib.centuryBps"
      via: "century bonus"
      pattern: "ActivityCurveLib\\.centuryBps"
---

<objective>
Adversarially verify the pre-clamp removal AND the FULL consumer migration against the locked design (§1, §5 of PLAN-ACTIVITY-CURVE-RESHAPE.md). Confirm all six 235/305 per-site pre-clamps are GONE so the high end is reachable; no consumer still reads the score through the old saturated arithmetic; and there is no residual stale constant or un-migrated consumer. The v69 incomplete-migration failure class (a consumer left on the old arithmetic) is the EXPLICITLY NAMED risk — sweep ALL activity-score read-sites, do not assume the diff is complete.

Purpose: VERIFY-03 is the reachability + completeness gate. v69 shipped an INCOMPLETE migration (6 bps-domain consumers left stale, caught only at re-audit); this plan exists to prove v70 did not repeat that.
Output: A complete read-site inventory with each consumer classified (raw-store vs new-curve), proof the six pre-clamps are removed, proof the four stale constants are gone, and confirmation centuryBps is a single shared helper. No contract commit. Apply any in-tree gap-fix (autonomous) if a stale consumer/clamp/constant is found.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PLAN-ACTIVITY-CURVE-RESHAPE.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- The six pre-clamp sites (locked §1) that MUST be removed, and the constants (locked §5) that MUST be gone. -->
Pre-clamp sites to confirm REMOVED:
  FLIP.sol ~:667-668                          if (bonusPoints > 235) bonusPoints = 235   (feeds mult + bucket)
  DegenerusGameDecimatorModule.sol ~:796-797  if (bonusPoints > 235) = 235               (terminal burn)
  DegenerusGameDecimatorModule.sol ~:918-920  same                                       (terminal boost)
  DegenerusGameDegeneretteModule.sol ~:1144   if (score > 305) score = 305               (ROI; FOLDED into s>=30000 branch)
  DegenerusGameDegeneretteModule.sol ~:1181   same                                       (WWXRP; FOLDED)
  DegenerusGameMintModule.sol ~:1712          _score = cachedScore > 305 ? 305 : ...     (century)
  GameAfkingModule.sol ~:835                   same                                       (afking century)
Stale constants to confirm GONE (no ref outside removal):
  ACTIVITY_SCORE_MID_POINTS(75), ACTIVITY_SCORE_HIGH_POINTS(255), ROI_MID_BPS(9500), ROI_HIGH_BPS(9950)
Constants to confirm KEPT: ACTIVITY_SCORE_MAX_POINTS(305 shared K), LOOTBOX_EV_ACTIVITY_NEUTRAL_POINTS(60),
  all MIN/MAX named curve constants, the bucket base/floor constants.
All activity-score read-sites to classify (already inventoried):
  FLIP:661 (->decMultBps/decBucket), Decimator:782/:898 (terminal mult/bucket), Degenerette:541 (packs raw into bet),
  Mint:1709 (->centuryBps), Afking:829 (->centuryBps), Afking:877 (->lootbox EV), Whale:865 (->lootbox EV),
  sDGNRS:1140 (stores uint16(score)+1 raw sentinel), Game:2210/IDegenerusGame:65 (getter passthrough).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove all six pre-clamps removed + the four stale constants gone (locked §1, §5)</name>
  <read_first>
    - .planning/PLAN-ACTIVITY-CURVE-RESHAPE.md §1 (the pre-clamp removal table) and §5 (constants to remove vs keep)
    - The six diff hunks: `git diff -- contracts/FLIP.sol contracts/modules/DegenerusGameDecimatorModule.sol contracts/modules/DegenerusGameDegeneretteModule.sol contracts/modules/DegenerusGameMintModule.sol contracts/modules/GameAfkingModule.sol`
  </read_first>
  <action>
    For each of the seven §1 pre-clamp sites (FLIP feeds both mult+bucket = one removal; Decimator x2 terminal; Degenerette x2 folded; Mint; Afking) read the corresponding diff hunk and confirm the `if (x > 235/305) x = 235/305` clamp is REMOVED (FLIP/Decimator/Mint/Afking) or FOLDED into the curve's own `>=30000` saturation branch (Degenerette ROI + WWXRP). For Mint and Afking specifically confirm the old `cachedScore > 305 ? 305 : cachedScore` line is replaced by the `ActivityCurveLib.centuryBps(cachedScore)` call (the raw uncapped score now flows into the self-saturating curve). Then run the sweep gates: `git grep -nE '> *(235|305)' -- 'contracts/*.sol'` must return zero ACTIVITY pre-clamp sites (if a `> 235`/`> 305` match exists, confirm it is unrelated to the activity score — e.g. a different domain — and document it; otherwise it is a residual clamp = gap). `git grep -nE 'ACTIVITY_SCORE_MID_POINTS|ACTIVITY_SCORE_HIGH_POINTS|ROI_MID_BPS|ROI_HIGH_BPS' -- 'contracts/*.sol'` must return zero (these four are removed). Confirm the KEPT constants survive: `git grep -n 'ACTIVITY_SCORE_MAX_POINTS' contracts/modules/DegenerusGameDegeneretteModule.sol` (305, the shared ROI/WWXRP K) and `LOOTBOX_EV_ACTIVITY_NEUTRAL_POINTS` (60). If any clamp remains or any stale constant survives, apply the minimal in-tree fix to the named .sol (autonomous; no commit). Report site-by-site.
  </action>
  <acceptance_criteria>
    - All seven §1 pre-clamp lines confirmed removed/folded (FLIP, Decimator x2, Degenerette x2 folded into >=30000, Mint, Afking).
    - `git grep -nE '> *(235|305)' -- 'contracts/*.sol'` finds zero residual activity-score pre-clamp sites (any match documented as a different domain).
    - `git grep -nE 'ACTIVITY_SCORE_MID_POINTS|ACTIVITY_SCORE_HIGH_POINTS|ROI_MID_BPS|ROI_HIGH_BPS' -- 'contracts/*.sol'` returns nothing.
    - KEPT constants confirmed present: ACTIVITY_SCORE_MAX_POINTS(305), LOOTBOX_EV_ACTIVITY_NEUTRAL_POINTS(60).
    - Any residual clamp/constant corrected in-tree (file in <files>); clean review applies NO edit.
  </acceptance_criteria>
  <files>contracts/FLIP.sol, contracts/modules/DegenerusGameDecimatorModule.sol, contracts/modules/DegenerusGameDegeneretteModule.sol, contracts/modules/DegenerusGameMintModule.sol, contracts/modules/GameAfkingModule.sol</files>
  <verify>
    <automated>test -z "$(git grep -nE 'ACTIVITY_SCORE_MID_POINTS|ACTIVITY_SCORE_HIGH_POINTS|ROI_MID_BPS|ROI_HIGH_BPS' -- 'contracts/*.sol')" && echo CLEAN || echo RESIDUAL</automated>
  </verify>
  <done>All six (seven-line) pre-clamps removed/folded; the four stale ROI/score constants gone; the shared K (305) and neutral (60) constants kept. High end is now reachable.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep ALL activity-score read-sites + confirm the single shared centuryBps helper (v69 incomplete-migration class)</name>
  <read_first>
    - The full read-site inventory via `git grep -nE 'playerActivityScore|_playerActivityScore' -- 'contracts/*.sol'`
    - The non-curve/secondary consumers: contracts/modules/DegenerusGameWhaleModule.sol ~:865 (->_lootboxEvMultiplierFromScore), GameAfkingModule.sol ~:877 (->_lootboxEvMultiplierFromScore), DegenerusGameDegeneretteModule.sol ~:541 (packs raw score into the bet), contracts/sDGNRS.sol ~:1140 (stores uint16(score)+1 sentinel)
    - .planning/PLAN-ACTIVITY-CURVE-RESHAPE.md §7 (storage-safe: LB_SCORE/Sub.score/PendingRedemption.activityScore/degenerette packed score all store the RAW score unchanged; only the formula applied changes) and §2.4 (the SHARED century helper requirement: Mint and Afking must use one helper or diverge)
  </read_first>
  <action>
    Enumerate every activity-score read-site and classify each as either (a) RAW-STORE (the site stores the unmodified score; the curve is applied later at settle/claim) or (b) NEW-CURVE (the site feeds the raw score directly into a reshaped curve). Confirm NO site applies the OLD saturated arithmetic. Specifically confirm: FLIP:661 feeds decMultBps/decBucket (new); Decimator:782 + :898 feed decMultBps/decBucket via _terminalDecBucket (new); Mint:1709 + Afking:829 feed centuryBps (new); Afking:877 + Whale:865 snapshot the raw score then call _lootboxEvMultiplierFromScore (the reshaped lootbox curve — raw in, new curve, OK); Degenerette:541 packs the raw uint16 score into the bet via _packFullTicketBet and the ROI/WWXRP curve is applied at settle (raw-store, new curve at settle — OK); sDGNRS:1140 stores uint16(score)+1 as a frozen sentinel (raw-store, no curve — OK); Game:2210 / IDegenerusGame:65 / sDGNRS:47 are getter/interface passthroughs (no curve). For each (a)-classified site, confirm the consumer that LATER reads the stored score applies a NEW curve, not stale arithmetic — i.e. trace Degenerette's settle path (_roiBpsFromScore/_wwxrpHighValueRoi, already reshaped) and the lootbox open path (_lootboxEvMultiplierFromScore, already reshaped). Then verify the shared century helper: confirm BOTH Mint:1714 and Afking:837 call `ActivityCurveLib.centuryBps(cachedScore)` divided by `ActivityCurveLib.CENTURY_MAX_BPS` — a SINGLE shared helper, no divergent inline copy (gate: `git grep -nE 'centuryBps|centuryBonusBps' -- 'contracts/*.sol'` shows exactly the two call-sites, both into the lib; note the implementation placed the helper in ActivityCurveLib rather than DegenerusGameStorage as design §4 suggested — this is an EQUIVALENT-OR-BETTER anti-drift outcome under USER decision #2, confirm both sites resolve to the one lib function). If any consumer is found still on old arithmetic, or the two century sites diverge, apply the minimal in-tree fix (autonomous; no commit). Produce the final read-site table (site -> classification -> curve applied) in the summary.
  </action>
  <acceptance_criteria>
    - Every playerActivityScore/_playerActivityScore read-site is classified raw-store or new-curve; the inventory is exhaustive (matches the git grep output, no site unaccounted-for).
    - No consumer applies the OLD saturated arithmetic; each raw-store site's downstream consumer uses a reshaped curve (Degenerette settle ROI/WWXRP, lootbox-open EV).
    - `git grep -nE 'centuryBps|centuryBonusBps' -- 'contracts/*.sol'` shows exactly two call-sites (Mint + Afking), both resolving to the single ActivityCurveLib.centuryBps helper; no divergent inline century copy.
    - The summary contains the read-site classification table.
    - Any un-migrated consumer or divergent century copy corrected in-tree (files in <files>); clean review applies NO edit.
  </acceptance_criteria>
  <files>contracts/modules/DegenerusGameMintModule.sol, contracts/modules/GameAfkingModule.sol, contracts/modules/DegenerusGameWhaleModule.sol, contracts/modules/DegenerusGameDegeneretteModule.sol, contracts/sDGNRS.sol</files>
  <verify>
    <automated>git grep -nE 'playerActivityScore|_playerActivityScore' -- 'contracts/*.sol'</automated>
  </verify>
  <done>Complete read-site inventory classified; no consumer on old arithmetic; centuryBps is a single shared helper across Mint+Afking. The v69 incomplete-migration failure class is swept and cleared.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored raw score -> later consumer curve | Several sites store the raw score (bet packing, sDGNRS redemption sentinel, lootbox EV snapshot) and apply the curve at settle/claim/open. A consumer left on the old saturated arithmetic mis-prices the payout invisibly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-440-07 | Tampering | NAMED RISK: an un-migrated consumer still reads the old saturated score (the v69 failure class) | mitigate | This plan exhaustively sweeps every playerActivityScore read-site and traces each raw-store site's downstream consumer to a reshaped curve; a residual stale consumer is a gap fixed in-tree before FREEZE. |
| T-440-08 | Tampering | a residual 235/305 pre-clamp re-saturates the input, making the tail unreachable | mitigate | git grep gate proves zero residual activity pre-clamp; the high end (score 30000 -> MAX) is reachable. |
| T-440-09 | Repudiation | Mint and Afking century bonuses diverge (two inline copies drift) | mitigate | Confirmed both call the single ActivityCurveLib.centuryBps; identical bonus for identical score, asserted in TST-03 (442). |
</threat_model>

<verification>
- `git grep` gates confirm zero residual pre-clamps and zero stale-constant references.
- Every activity-score read-site classified; no consumer on old arithmetic; raw-store sites trace to reshaped curves.
- centuryBps is a single shared helper (exactly two call-sites into the lib).
- Any in-tree gap-fix applied to the named .sol only; NO `git commit` of any contracts/*.sol.
</verification>

<success_criteria>
All six pre-clamps removed/folded, the four stale constants gone, the complete consumer migration confirmed (no consumer on old arithmetic), and centuryBps confirmed a single shared helper — the v69 incomplete-migration failure class swept and cleared. No contract commit. Gap-fixes (if any) applied in-tree and re-verified.
</success_criteria>

<output>
Create `.planning/phases/440-verify-review-the-working-tree-reshape-against-the-locked-de/440-03-SUMMARY.md` when done
</output>
