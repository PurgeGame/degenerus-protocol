---
phase: 182-regression-check
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/182-regression-check/182-02-TEST-RESULTS.md
autonomous: true
requirements: [REG-02]
must_haves:
  truths:
    - "Hardhat test suite runs to completion with all expected tests passing"
    - "Foundry test suite runs to completion with all expected tests passing"
    - "Every failure is classified as either EXPECTED (matches known baseline) or UNEXPECTED (regression)"
    - "Zero unexpected failures across both frameworks"
  artifacts:
    - path: ".planning/phases/182-regression-check/182-02-TEST-RESULTS.md"
      provides: "Complete test results from both Foundry and Hardhat with failure classification"
      contains: "Combined Verdict"
  key_links:
    - from: "npm test (Hardhat)"
      to: "182-02-TEST-RESULTS.md"
      via: "Hardhat results captured and analyzed"
      pattern: "Hardhat.*passing"
    - from: "forge test (Foundry)"
      to: "182-02-TEST-RESULTS.md"
      via: "Foundry results captured and analyzed"
      pattern: "Foundry.*passing"
---

<objective>
Run both Foundry and Hardhat test suites against the current codebase and verify zero unexpected failures. Compare results against the v15.0 baseline (Phase 167-02-TEST-BASELINE.md) to classify every failure as EXPECTED or UNEXPECTED.

Purpose: Test suites are the last line of defense against regressions. The v15.0 baseline had 124 known expected failures (13 Hardhat, 111 Foundry). This plan runs both suites fresh and verifies no NEW failures have appeared from v16.0-v17.1 refactors.

Output: A test results document with per-framework results, failure classification, and combined verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# v15.0 test baseline (known expected failures)
@.planning/milestones/v15.0-phases/167-integration-test-baseline/167-02-TEST-BASELINE.md
</context>

<interfaces>
<!-- Test infrastructure -->

Hardhat test command: npm test
  - Runs: test/unit/*.test.js test/integration/*.test.js test/deploy/*.test.js test/access/*.test.js test/edge/*.test.js test/gas/*.test.js test/adversarial/*.test.js
  - v15.0 baseline: 1188 passing, 13 expected failures (12 taper formula + 1 CoinPurchaseCutoff error)

Foundry test command: forge test
  - Runs all test/foundry/**/*.t.sol files
  - v15.0 baseline: 267 passing, 111 expected failures (73 NotTimeYet, 32 level advancement, 3 interface changes, 1 invariant cache, 2 degenerette freeze)

Known blocker from STATE.md: ContractAddresses.sol has unstaged changes (different deploy addresses) -- may need stashing before test runs.

Config files: foundry.toml, hardhat.config.js
Solidity: 0.8.34, viaIR: true, optimizer: 200 runs
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run Hardhat test suite and classify results against v15.0 baseline</name>
  <files>.planning/phases/182-regression-check/182-02-TEST-RESULTS.md</files>
  <read_first>
    - .planning/milestones/v15.0-phases/167-integration-test-baseline/167-02-TEST-BASELINE.md (known expected failures baseline)
    - hardhat.config.js (compilation settings)
    - package.json (test scripts)
  </read_first>
  <action>
**Step 1: Pre-flight check.**
Check git status for ContractAddresses.sol changes. If unstaged changes exist, note them but do NOT stash/modify (per user memory: NEVER checkout/restore/modify ContractAddresses.sol). The tests should use whatever addresses are currently in the file.

**Step 2: Run Hardhat tests.**
Execute `npm test` and capture full output. The command runs Hardhat tests across unit, integration, deploy, access, edge, gas, and adversarial directories.

Note: Compilation may take several minutes with viaIR enabled. The test run itself may take 2-5 minutes.

**Step 3: Record results.**
For each test directory (access, deploy, unit, integration, edge, gas, adversarial):
- Count passing and failing tests
- For any failures, record the test name, file, and error message

**Step 4: Classify failures against v15.0 baseline.**
The v15.0 baseline (167-02-TEST-BASELINE.md) documents 13 expected failures:
- 9 AffiliateHardening taper tests (AFF-05 through AFF-09)
- 3 DegenerusAffiliate taper tests
- 1 SecurityEconHardening CoinPurchaseCutoff test

For each failure:
- If it matches a v15.0 expected failure -> EXPECTED
- If it is a new failure not in the v15.0 baseline -> UNEXPECTED (regression)
- If a v15.0 expected failure now PASSES -> NOTE (test was fixed or behavior changed)

**Step 5: Begin writing 182-02-TEST-RESULTS.md with Hardhat section.**
  </action>
  <verify>
    <automated>grep -c "Hardhat" .planning/phases/182-regression-check/182-02-TEST-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Hardhat test suite was executed (npm test)
    - Every passing/failing count is recorded per test directory
    - Every failure has a classification (EXPECTED or UNEXPECTED)
    - The total test count is documented (should be near the v15.0 baseline of 1201)
  </acceptance_criteria>
  <done>Hardhat test results recorded with all failures classified. Zero unexpected failures, or any unexpected failure fully documented.</done>
</task>

<task type="auto">
  <name>Task 2: Run Foundry test suite and produce combined results document</name>
  <files>.planning/phases/182-regression-check/182-02-TEST-RESULTS.md</files>
  <read_first>
    - .planning/milestones/v15.0-phases/167-integration-test-baseline/167-02-TEST-BASELINE.md (Foundry baseline section)
    - foundry.toml (compilation and fuzz settings)
    - .planning/phases/182-regression-check/182-02-TEST-RESULTS.md (append to Hardhat results from Task 1)
  </read_first>
  <action>
**Step 1: Run Foundry tests.**
Execute `forge test --no-match-test "invariant" -v` first for standard tests (invariant tests are slower and should run separately). Capture output.

Then run `forge test --match-test "invariant" -v` for invariant tests. Capture output.

Note: If forge test fails to compile, check for missing dependencies (lib/forge-std). Run `forge install` if needed.

**Step 2: Record results.**
For each test suite:
- Count passing and failing tests
- For any failures, record the suite name, test name, and error message

**Step 3: Classify failures against v15.0 baseline.**
The v15.0 baseline (167-02-TEST-BASELINE.md) documents 111 expected Foundry failures across 4 categories:
- Category 1: NotTimeYet() -- 73 tests across VRFCore, LootboxRngLifecycle, VRFStallEdgeCases, VRFPathCoverage, LootboxBoonCoexistence, StallResilience
- Category 2: Level advancement -- 32 tests across TicketLifecycle, FarFutureIntegration, BafRebuyReconciliation, VRFLifecycle
- Category 3: Contract interface changes -- 3 tests (DeployCanary, DegeneretteFreezeResolution)
- Category 4: Replayed invariant cache -- 1 test (VRFPathInvariants)

For each failure:
- If it matches a v15.0 expected failure -> EXPECTED
- If it is a new failure not in the v15.0 baseline -> UNEXPECTED (regression)
- If a v15.0 expected failure now PASSES -> NOTE (test was fixed)

**Step 4: Complete 182-02-TEST-RESULTS.md with Foundry section and combined verdict.**

The output document should have this structure:

**Section 1: HARDHAT TEST RESULTS** (from Task 1)
- Compilation summary
- Per-Directory Results table (directory, passing, failing, total, notes)
- Failure Classification (per failure: test name, file, error, classification)
- Hardhat Verdict (PASS/FAIL)

**Section 2: FOUNDRY TEST RESULTS**
- Compilation summary
- Per-Suite Results table (suite, passing, failing, total, classification)
- Failure Classification (per category: tests, root cause, classification)
- Foundry Verdict (PASS/FAIL)

**Section 3: COMBINED RESULTS**
- Summary table (framework, passing, expected failures, unexpected failures, total)
- Comparison to v15.0 Baseline table (metric, v15.0, current, delta)
- Combined Verdict (PASS: zero unexpected / FAIL: list unexpected)
- New Expected Failures section (any failures not in v15.0 baseline caused by v16.0-v17.1 intentional changes)

Use horizontal rules (not triple-dash separators) between sections.
  </action>
  <verify>
    <automated>grep -c "Combined Verdict" .planning/phases/182-regression-check/182-02-TEST-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Foundry test suite was executed (forge test)
    - Every passing/failing count is recorded per suite
    - Every failure has a classification (EXPECTED or UNEXPECTED)
    - The combined document has both Hardhat and Foundry sections
    - A combined verdict is present
    - The total test count is documented and compared to v15.0 baseline
  </acceptance_criteria>
  <done>Both Hardhat and Foundry test results recorded in a single document. Combined verdict issued. Zero unexpected failures across both frameworks, or any unexpected failure fully documented.</done>
</task>

</tasks>

<verification>
- 182-02-TEST-RESULTS.md exists with both Hardhat and Foundry sections
- Hardhat section has per-directory pass/fail counts
- Foundry section has per-suite pass/fail counts
- Every failure is classified as EXPECTED or UNEXPECTED
- A Combined Verdict section exists
- Comparison to v15.0 baseline is documented
</verification>

<success_criteria>
Both Foundry and Hardhat test suites pass with zero unexpected failures. All failures are classified against the v15.0 baseline. The combined verdict confirms no test regressions from v16.0-v17.1 refactors.
</success_criteria>

<output>
After completion, create `.planning/phases/182-regression-check/182-02-SUMMARY.md`
</output>
