---
phase: 293-hrroll-regression-fixture-tst-hrroll
plan: 02
type: execute
wave: 2
depends_on: [293-01]
files_modified:
  - test/edge/HeroOverrideWeightedRoll.test.js
autonomous: false
requirements: [TST-HRROLL-01, TST-HRROLL-02, TST-HRROLL-03, TST-HRROLL-04, TST-HRROLL-05, TST-HRROLL-06]
requirements_addressed: [TST-HRROLL-01, TST-HRROLL-02, TST-HRROLL-03, TST-HRROLL-04, TST-HRROLL-05, TST-HRROLL-06]

must_haves:
  truths:
    - "test/edge/HeroOverrideWeightedRoll.test.js (new file) covers TST-HRROLL-01..06 in 6 describe blocks under a single top-level describe — TST-HRROLL-01 (chi² weighted distribution N=10000 df=3 crit 7.815), TST-HRROLL-02 (×1.5 leader bonus binomial N=10000 df=1 crit 3.841 seed [500,200,200,100] expected 0.60 LOCKED), TST-HRROLL-03 (commitment-window direct-storage-read), TST-HRROLL-04 (single-bettor probability 1.0 over 100 entropy variations), TST-HRROLL-05 (zero-wager (false,0,0) over 100 entropy variations), TST-HRROLL-06 (production-path gas-delta against +431 ± 100 soft / 750 hard with [BLOCKING_ESCALATION] on noise)"
    - "TST-HRROLL-02 asserts against the LOCKED seed [500, 200, 200, 100] (4 slots, total=1000, leaderBonus=250, effectiveTotal=1250, leader effective weight=750, expected pick-rate exactly 0.60) — NOT the algebraically inconsistent ROADMAP example [500, 100, 100, 100] which yields 71.4%"
    - "TST-HRROLL-01 chi² uses the inline Wilson-Hilferty + CHI2_CRIT_05 table pattern from test/stat/PerPullLevelDistribution.test.js:90-103 — inline copy, no test/helpers/chiSquare.mjs extraction"
    - "TST-HRROLL-01 expected proportions are BONUS-ADJUSTED (leader slot 0: 600/1100 = 0.5455; slots 1..3: 300/1100, 200/1100, 100/1100) — chi² is computed against the bonus-adjusted expectation, not the raw weights [400/1000, 300/1000, 200/1000, 100/1000]"
    - "TST-HRROLL-03 captures dailyHeroWagers[D][0..3] slot bytes via ethers.provider.getStorageAt pre-advance, advances day D→D+1 via advanceGame chain, captures slot bytes post-advance, asserts byte-identical; replays rollHeroSymbolRef(D, entropy, capturedSlots) against both captures and asserts identical (q, s); asserts dailyIdx is the controlling key per v41 Phase 288 D-288-FIX-SHAPE-01"
    - "TST-HRROLL-06 measures production-path gas-delta — worst-case-seeded payDailyJackpot tx.gasUsed vs all-zero-seeded baseline tx.gasUsed; computes stddev across 5+ measurements; asserts delta within +431 ± 100 soft (331..531) and ≤ 750 hard; if stddev > 100 gas, emit [BLOCKING_ESCALATION] checkpoint surfacing two fallback options to the user (RELAX bound to +1000 + cite 292-01-MEASUREMENT.md §3.c, OR INVOKE D-293-INVOKE-01 visibility-flip Phase-292 amendment) and STOP — do not auto-proceed"
    - "Cross-attestation small-N: 16-64 production-path replays through payDailyJackpot parsing the DailyWinningTraits event main-traits packed-byte for the hero quadrant; assert JS oracle output for the same (dailyHeroWagers, randWord) matches the on-chain trait byte"
    - "JSDoc test-file header (5 bullets per 293-CONTEXT.md decisions): (i) v42 HRROLL mechanic the fixture covers, (ii) JS-replay oracle + cross-attestation strategy per D-293-INVOKE-01, (iii) D-293-GAS-01 empirical methodology + ±100 soft tolerance vs +431 theoretical anchor, (iv) D-293-STALE-VIEW-01 reason getDailyHeroWinner is NOT used as an assertion vehicle, (v) LOCKED TST-HRROLL-02 seed [500, 200, 200, 100] / 0.60 disposition"
    - "Zero contracts/ mutations by default; the D-293-INVOKE-01 escalation path is ONLY invoked at the TST-HRROLL-06 [BLOCKING_ESCALATION] checkpoint and ONLY after explicit user approval — not pre-approved per feedback_never_preapprove_contracts.md"
    - "Single USER-APPROVED batched commit at phase close carrying both Plan 01 (test/helpers/rollHeroSymbolRef.mjs) and Plan 02 (test/edge/HeroOverrideWeightedRoll.test.js) files — per feedback_batch_contract_approval.md + feedback_manual_review_before_push.md (executor presents diff, waits for explicit user approval, then commits)"
    - "test/edge/HeroOverrideDayIndex.test.js (frozen v41 TST-HOFIX/TST-JPSURF artifact) and test/edge/MintBatchDeterminism.test.js (Phase 282 frozen v41-closure artifact) are NOT modified"
    - "contracts/DegenerusGame.sol:2545-2563 getDailyHeroWinner stale v41-leftover view is NOT touched and NOT used as an assertion vehicle per D-293-STALE-VIEW-01"
  artifacts:
    - path: "test/edge/HeroOverrideWeightedRoll.test.js"
      provides: "TST-HRROLL-01..06 regression assertions over the post-HRROLL audit subject"
      contains: "describe('HeroOverrideWeightedRoll"
    - path: "test/helpers/rollHeroSymbolRef.mjs"
      provides: "JS-replay oracle (from Plan 01)"
      contains: "export function rollHeroSymbolRef"
  key_links:
    - from: "test/edge/HeroOverrideWeightedRoll.test.js (TST-HRROLL-01 + 02 + 04 + 05)"
      to: "test/helpers/rollHeroSymbolRef.mjs::rollHeroSymbolRef"
      via: "ES module import — drives 10000-iter chi² + small-N edge cases"
      pattern: "import.*rollHeroSymbolRef.*from.*rollHeroSymbolRef"
    - from: "test/edge/HeroOverrideWeightedRoll.test.js (TST-HRROLL-03)"
      to: "contracts/storage/DegenerusGameStorage.sol:1475 dailyHeroWagers mapping"
      via: "ethers.provider.getStorageAt(addr, keccak256(abi.encode(q, keccak256(abi.encode(D, BASE_SLOT))))) — Solidity nested-mapping derivation (outer mapping value is uint256[4] fixed array; slot index for array element q is parentSlot + q)"
      pattern: "getStorageAt.*keccak256"
    - from: "test/edge/HeroOverrideWeightedRoll.test.js (TST-HRROLL-06 cross-attestation)"
      to: "contracts/modules/DegenerusGameJackpotModule.sol:106-111 event DailyWinningTraits + L523/L541 emit sites"
      via: "tx.receipt log parse + unpackWinningTraits → trait byte (quadrant << 6) | (color << 3) | symbol decode"
      pattern: "DailyWinningTraits|unpackWinningTraits"
    - from: "test/edge/HeroOverrideWeightedRoll.test.js (TST-HRROLL-06)"
      to: "contracts/modules/DegenerusGameJackpotModule.sol:1988 _applyHeroOverride callsite (production path through payDailyJackpot → _runJackpotEthFlow → _rollWinningTraits)"
      via: "tx.receipt.gasUsed delta between worst-case-seeded and all-zero-seeded states"
      pattern: "gasUsed"
---

<objective>
Ship `test/edge/HeroOverrideWeightedRoll.test.js` — the single regression fixture covering all 6 TST-HRROLL-01..06 assertions against the post-HRROLL audit subject (Phase 292 commit `a0218952`). Drives the JS-replay oracle from Plan 01 at N=10,000 iterations for the chi² assertions, captures live `dailyHeroWagers[D][q]` storage bytes for the commitment-window proof, and measures production-path gas-delta against the D-42N-GAS-01 +431 theoretical anchor. Phase close = ONE USER-APPROVED batched commit containing the Plan 01 helper + the Plan 02 test file.

Purpose: Cover the 5 ROADMAP success criteria — (1) weighted-distribution chi² at N=10000 under the JS-replay oracle (TST-HRROLL-01); (2) ×1.5 leader-bonus binomial under the LOCKED seed `[500, 200, 200, 100]` with expected leader rate exactly 0.60 (TST-HRROLL-02); (3) RNG commitment-window proof via direct-storage-read invariance across day-D→D+1 advance with `dailyIdx` as the controlling day key (TST-HRROLL-03); (4) edge cases — single-bettor probability 1.0 + zero-wager (false,0,0) over 100 entropy variations each (TST-HRROLL-04 + TST-HRROLL-05); (5) production-path gas-delta against the +431 theoretical anchor from `292-01-MEASUREMENT.md` §3.c with a `[BLOCKING_ESCALATION]` checkpoint when measurement noise > ±100 gas (TST-HRROLL-06).

Output: One new test file + one USER-APPROVED batched commit covering both Plan 01 + Plan 02 deliverables. Zero `contracts/` mutations by default. The D-293-INVOKE-01 escalation path is NOT pre-approved and is only surfaced at the TST-HRROLL-06 `[BLOCKING_ESCALATION]` checkpoint if empirical noise demands it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/293-hrroll-regression-fixture-tst-hrroll/293-CONTEXT.md
@.planning/phases/293-hrroll-regression-fixture-tst-hrroll/293-01-PLAN.md
@.planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md
@.planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md
@.planning/phases/292-hero-override-weighted-roll-hrroll/292-02-SUMMARY.md
@.planning/phases/291-mintcln-regression-fixture-tst-mintcln/291-02-PLAN.md
@test/helpers/rollHeroSymbolRef.mjs
@test/helpers/deployFixture.js
@test/helpers/testUtils.js
@test/edge/MintBatchDeterminism.test.js
@test/edge/MintCleanupRegression.test.js
@test/stat/PerPullLevelDistribution.test.js
@contracts/modules/DegenerusGameJackpotModule.sol
@contracts/modules/DegenerusGameDegeneretteModule.sol
@contracts/storage/DegenerusGameStorage.sol

<interfaces>
JS-replay oracle from Plan 01 (test/helpers/rollHeroSymbolRef.mjs):
  - `export function rollHeroSymbolRef({ day, entropy, dailyHeroWagers })` → `{ hasWinner, winQuadrant, winSymbol }`
  - `export function packDailyHeroWagers(rawAmounts)` — pack a 32-length flat-idx uint32 array into `BigInt[4]`
  - `export const ROLL_HERO_SYMBOL_CONSTANTS = { U64_MASK, U32_MASK }`

Live `_rollHeroSymbol` body (contracts/modules/DegenerusGameJackpotModule.sol:1639-1700) — the audit-subject the oracle bit-mirrors. Pass-1 cache via flat `uint32[32]` indexed `(q << 3) | s`; pass-2 cumulative cursor walk with leader-bonus add at `idx == leaderIdx`; keccak input `abi.encode(entropy, day)`.

Live `_applyHeroOverride` callsite (contracts/modules/DegenerusGameJackpotModule.sol:1988):
  - `_applyHeroOverride(traits, r, randWord)` — invoked from `_rollWinningTraits` for the symbol-roll path
  - Color path (L1612-1621) consumes `r` bits `quadrant*3`; symbol path consumes `keccak256(abi.encode(randWord, day))` per D-42N-BONUS-ENTROPY-01

Live `dailyHeroWagers` storage layout (contracts/storage/DegenerusGameStorage.sol:1475):
  - `mapping(uint32 => uint256[4]) internal dailyHeroWagers`
  - Wagers written at `placeDegeneretteBet` (DegenerusGameDegeneretteModule.sol:484-501) with saturation cap at `0xFFFFFFFF` per L495

Storage-slot derivation for `dailyHeroWagers[D][q]` (Solidity rules — mapping of fixed array):
  - Outer mapping: `parentSlot = keccak256(abi.encode(uint256(D), uint256(BASE_SLOT)))` where BASE_SLOT is the storage index of `dailyHeroWagers`
  - Inner fixed array (length 4): element at index q lives at `parentSlot + q` (no further keccak; fixed arrays are stored contiguously per Solidity slot rules)
  - `BASE_SLOT` pinned literal sourced at execute time via `FOUNDRY_DISABLE_NIGHTLY_WARNING=1 forge inspect contracts/storage/DegenerusGameStorage.sol:DegenerusGameStorage storageLayout` and grepping for `name: dailyHeroWagers`. The Phase 292 §2 attested EMPTY diff vs v41 close guarantees the index is unchanged from v41. The exact literal MUST be re-validated at Task 4 (TST-HRROLL-03) via a runtime forge-inspect-and-grep gate.

Live `DailyWinningTraits` event (contracts/modules/DegenerusGameJackpotModule.sol:106-111 + emit sites at L523 + L541):
  - `event DailyWinningTraits(uint32 indexed day, uint32 mainTraitsPacked, uint32 bonusTraitsPacked, uint24 bonusTargetLevel)`
  - `mainTraitsPacked` is a 4-byte packed trait register (one byte per quadrant); each byte decodes to `(quadrant << 6) | (color << 3) | symbol` per `_applyHeroOverride` L1623-1627
  - For cross-attestation: deploy worst-case `dailyHeroWagers[D]`, fire `payDailyJackpot`, parse `DailyWinningTraits` log, decode `mainTraitsPacked` to extract `(heroQuadrant, heroSymbol)` and compare against `rollHeroSymbolRef(D, randWord, dailyHeroWagers)` output

Chi² inline pattern from test/stat/PerPullLevelDistribution.test.js:90-103:
  - `const CHI2_CRIT_05 = { 1: 3.841, 2: 5.991, 3: 7.815, 4: 9.488, 5: 11.070, 6: 12.592, 7: 14.067 };`
  - `function wilsonHilfertyZ(chi2, df) { const term = Math.cbrt(chi2 / df) - (1 - 2 / (9 * df)); return term / Math.sqrt(2 / (9 * df)); }`
  - Per-bucket chi² accumulation: `for (let k = 0; k < df+1; k++) { const diff = observed[k] - expected[k]; chi2 += (diff*diff) / expected[k]; }`
  - Assert `chi2 < CHI2_CRIT_05[df]`; log via `console.log` per Phase 262 STAT-04 traceability pattern

Phase 282 reusable scaffolding patterns (test/edge/MintBatchDeterminism.test.js):
  - `loadFixture(deployFullProtocol)` boot
  - Pinned daily entropy: `0x2f02_3456_789a_bcde_f012_3456_789a_bcde_f012_3456_789a_bcde_f012_3456_789a_bcden` (reuse for invariant continuity across phases)
  - `pinDailyEntropy(game, deployer, mockVRF, word)` helper
  - `advanceGame()` drain chain pattern

Existing testUtils.js exports (test/helpers/testUtils.js):
  - `eth(n)` — wei conversion
  - `advanceToNextDay()` — time-warp 24h+1
  - `getLastVRFRequestId(mockVRF)` — VRF request ID extraction
  - `ZERO_ADDRESS`, `ZERO_BYTES32`

LOCKED TST-HRROLL-02 disposition (from <locked_user_dispositions>):
  - Seed: `[500, 200, 200, 100]` (4 entries placed at flat idx 0..3 ⇒ quadrant 0, symbols 0..3 — all in q=0)
  - Algebra: total = 1000; leader = 500; leaderBonus = 250; effectiveTotal = 1250; leader effective weight = 750
  - Expected leader pick-rate: `750 / 1250 = 0.60` exactly
  - Binomial chi² df=1, `CHI2_CRIT_05[1] = 3.841`, N = 10000
  - The ROADMAP success-criterion-2 example `(500 + 250) / 1250 = 60%` was algebraically inconsistent with seed `[500, 100, 100, 100]` (which gives 71.4%); the JSDoc header MAY note the typo but the test asserts the LOCKED disposition

D-293-STALE-VIEW-01 (from 293-CONTEXT.md): `contracts/DegenerusGame.sol:2545-2563 getDailyHeroWinner` is the v41-leftover deterministic-leader view; body unchanged from the deleted `_topHeroSymbol`. NOT used as a TST-HRROLL assertion vehicle (returns v41-deterministic output, not weighted-roll output). The JSDoc header MUST note this fact as a path-of-investigation clarification.

D-293-GAS-01 [BLOCKING_ESCALATION] checkpoint (from 293-CONTEXT.md + <locked_user_dispositions>):
  - Soft target: +431 gas (theoretical anchor from `292-01-MEASUREMENT.md` §3.c) ± 100 gas → acceptance window 331..531 gas absolute delta
  - Hard upper bound: ≤ 750 gas absolute delta
  - Sampling: 5+ measurements; compute stddev
  - If stddev > 100 gas → STOP and emit `[BLOCKING_ESCALATION]` checkpoint surfacing two fallback options:
    (i) RELAX bound to "soft +1000 gas" + theoretical-attestation cite to `292-01-MEASUREMENT.md` §3.c (mirrors D-291-GAS-01 graceful fallback)
    (ii) INVOKE D-293-INVOKE-01 visibility-flip Phase-292 amendment (one-line `private` → `internal` flip on `_rollHeroSymbol` + new `contracts/test/HeroRollTester.sol` harness + paired-call `noOp` companion — SECOND USER-APPROVED contract commit per `feedback_never_preapprove_contracts.md` + `feedback_batch_contract_approval.md`)
  - NEITHER option is pre-approved; the executor surfaces both and waits for explicit user disposition.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold test/edge/HeroOverrideWeightedRoll.test.js with JSDoc header + shared fixture helpers</name>
  <files>test/edge/HeroOverrideWeightedRoll.test.js</files>
  <read_first>
    - test/edge/HeroOverrideWeightedRoll.test.js (target file; may not exist yet)
    - test/edge/MintBatchDeterminism.test.js (Phase 282 sister fixture; reference for fixture boot, DAILY_ENTROPY pin, advanceGame drain pattern)
    - test/edge/MintCleanupRegression.test.js (Phase 291 sister fixture; reference for single-file-all-N-assertions shape + JSDoc header path-of-investigation note)
    - test/helpers/rollHeroSymbolRef.mjs (Plan 01 output — primary JS-replay oracle)
    - test/helpers/deployFixture.js (`deployFullProtocol`, `restoreAddresses`)
    - test/helpers/testUtils.js (`eth`, `advanceToNextDay`, `getLastVRFRequestId`, `ZERO_ADDRESS`, `ZERO_BYTES32`, `fulfillVRF`)
    - test/stat/PerPullLevelDistribution.test.js L88-L103 + L266-L283 (inline chi² pattern — Wilson-Hilferty Z + CHI2_CRIT_05 + per-bucket accumulation; lift the pattern verbatim)
    - contracts/modules/DegenerusGameJackpotModule.sol L100-L111 (DailyWinningTraits event declaration) + L1600-L1701 (_applyHeroOverride + _rollHeroSymbol) + L1988 (callsite)
    - contracts/modules/DegenerusGameDegeneretteModule.sol L484-L501 (placeDegeneretteBet wager-time write to dailyHeroWagers[dailyIdx][q])
    - contracts/storage/DegenerusGameStorage.sol L1470-L1475 (dailyHeroWagers mapping declaration)
    - .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md §3.c D-42N-GAS-01 (+431 theoretical anchor; soft +500 / hard +750 thresholds) + §3.e D-42N-COLOR-ENTROPY-01 (orthogonal entropy domains)
    - .planning/phases/293-hrroll-regression-fixture-tst-hrroll/293-CONTEXT.md <decisions> "JS-reference impl shape" + "Chi² implementation pattern" + "Test fixture deployment shape" + "JSDoc test-file header" + D-293-STALE-VIEW-01 + D-293-INVOKE-01 + D-293-GAS-01
  </read_first>
  <action>
    Create `test/edge/HeroOverrideWeightedRoll.test.js` from scratch. Do NOT extend `test/edge/HeroOverrideDayIndex.test.js` (frozen v41 closure artifact) or `test/edge/MintBatchDeterminism.test.js` (frozen v41 closure artifact). File structure:

    1. SPDX header `// SPDX-License-Identifier: AGPL-3.0-only`.

    2. JSDoc-style block-comment header (5 bullets per 293-CONTEXT.md `<code_context>` "JSDoc test-file header" pattern). Header content MUST include in this order:

       - One-line title: `HeroOverrideWeightedRoll.test.js — Phase 293 v42.0 HRROLL regression fixture (TST-HRROLL-01..06).`

       - Audit subject: cite Phase 292 audit-subject commit `a0218952` and the v42 cleanup that replaced deterministic `_topHeroSymbol(uint32 day)` with `_rollHeroSymbol(uint32 day, uint256 entropy) private view returns (bool, uint8, uint8)` — weighted random roll across the 32 `(quadrant, symbol)` slots in `dailyHeroWagers[day]` with ×1.5 leader-weight bonus (D-42N-LEADER-BONUS-01) and no min-wager floor (D-42N-FLOOR-01).

       - Bullet (i) — Mechanic covered: the v42 HRROLL weighted-roll selector. Cite contract source line range `contracts/modules/DegenerusGameJackpotModule.sol:1630-1700` and the `_applyHeroOverride` callsite at L1988 driven from `_rollWinningTraits`.

       - Bullet (ii) — JS-replay oracle + cross-attestation strategy per D-293-INVOKE-01: `_rollHeroSymbol` is `private view` so no inheritance harness can reach it; ALGORITHM_VERIFIED via JS-replay oracle at `test/helpers/rollHeroSymbolRef.mjs` (Plan 01 deliverable); cross-attestation via small-N (16-64 iter) production-path replay through `payDailyJackpot` parsing the `DailyWinningTraits` event main-traits packed byte.

       - Bullet (iii) — D-293-GAS-01 empirical methodology: production-path delta measurement between worst-case-seeded `dailyHeroWagers[D]` (all 32 slots populated) and all-zero-seeded baseline; assert delta within +431 ± 100 gas soft (acceptance window 331..531) and ≤ 750 gas hard; theoretical anchor from `292-01-MEASUREMENT.md` §3.c. If stddev across 5+ measurements > 100 gas, the test STOPS at a `[BLOCKING_ESCALATION]` checkpoint per `feedback_never_preapprove_contracts.md` — does NOT auto-proceed to either fallback.

       - Bullet (iv) — D-293-STALE-VIEW-01 disposition: `contracts/DegenerusGame.sol:2545-2563 getDailyHeroWinner` is the v41-leftover deterministic-leader view (body unchanged from the deleted `_topHeroSymbol`); semantically misleading post-HRROLL since the actual mechanism is now a weighted random roll requiring per-call entropy. NOT used as a TST-HRROLL assertion vehicle (would return v41-deterministic output, not weighted-roll output). Deferred to v43+ explicit cleanup phase per the 293-CONTEXT.md `<deferred>` register.

       - Bullet (v) — LOCKED TST-HRROLL-02 disposition: seed `[500, 200, 200, 100]` (total=1000; leaderBonus=250; effectiveTotal=1250; leader effective weight=750; expected pick-rate `750 / 1250 = 0.60` exactly). The ROADMAP success-criterion-2 example `(500 + 250) / 1250 = 60%` with seed `[500, 100, 100, 100]` was algebraically inconsistent under the actual contract arithmetic (yields 71.4%, not 60%); this fixture asserts the LOCKED seed disposition. Per `feedback_no_history_in_comments.md`: describe what IS — the v42 mechanic; the typo-note is acceptable as path-of-investigation prose because it documents the algebraic basis for the chosen seed, not a comparison to a prior implementation.

       - Per-test mapping (6 lines, one per TST-HRROLL-NN, mapping to the `describe` block / test name).

    3. Imports:
       - `import { expect } from "chai";`
       - `import hre from "hardhat";`
       - `import { loadFixture } from "@nomicfoundation/hardhat-toolbox/network-helpers.js";`
       - `import { execSync } from "node:child_process";`
       - `import { deployFullProtocol, restoreAddresses } from "../helpers/deployFixture.js";`
       - `import { eth, advanceToNextDay, getLastVRFRequestId, fulfillVRF, ZERO_ADDRESS, ZERO_BYTES32 } from "../helpers/testUtils.js";`
       - `import { rollHeroSymbolRef, packDailyHeroWagers, ROLL_HERO_SYMBOL_CONSTANTS } from "../helpers/rollHeroSymbolRef.mjs";`

    4. Module-level constants:
       - `const CHI2_CRIT_05 = Object.freeze({ 1: 3.841, 2: 5.991, 3: 7.815, 4: 9.488, 5: 11.070, 6: 12.592, 7: 14.067 });` — inline copy from `test/stat/PerPullLevelDistribution.test.js` L88-L97 (no helper extraction per 293-CONTEXT.md `<decisions>` "Chi² implementation pattern" — deferred to v43+ test-maintenance bundle)
       - `const DAILY_ENTROPY = 0x2f02_3456_789a_bcde_f012_3456_789a_bcde_f012_3456_789a_bcde_f012_3456_789a_bcden;` — reuse the Phase 282 / Phase 291 pin for invariant continuity
       - `const N_CHI2 = 10000;` — TST-HRROLL-01 + TST-HRROLL-02 iteration count
       - `const N_EDGE = 100;` — TST-HRROLL-04 + TST-HRROLL-05 entropy-variation count
       - `const N_GAS_SAMPLES = 5;` — TST-HRROLL-06 measurement repetitions for stddev computation
       - `const TST_HRROLL_02_SEED = Object.freeze([500, 200, 200, 100]);` — LOCKED disposition; 4 entries placed at flat idx 0..3 (quadrant 0, symbols 0..3)
       - `const TST_HRROLL_02_EXPECTED_LEADER_RATE = 750 / 1250;` — exact algebraic anchor; 0.60
       - `const TST_HRROLL_01_SEED = Object.freeze([400, 300, 200, 100]);` — at flat idx 0..3; leader at idx 0; total=1000; leaderBonus=200; effectiveTotal=1200; bonus-adjusted expected rates `[600/1200, 300/1200, 200/1200, 100/1200] = [0.5, 0.25, 0.1667, 0.0833]`
       - `const GAS_DELTA_THEORETICAL = 431;` — anchor from `292-01-MEASUREMENT.md` §3.c
       - `const GAS_DELTA_SOFT_TOLERANCE = 100;` — soft acceptance window ±100 around theoretical
       - `const GAS_DELTA_HARD_BOUND = 750;` — hard upper bound per D-42N-GAS-01

    5. Module-level helper functions:

       - `function wilsonHilfertyZ(chi2, df)` — verbatim copy from `test/stat/PerPullLevelDistribution.test.js` L99-L103: `const term = Math.cbrt(chi2 / df) - (1 - 2 / (9 * df)); return term / Math.sqrt(2 / (9 * df));`

       - `function computeChi2Multinomial(observed, expected)` — accumulate per-bucket `(observed[k] - expected[k])^2 / expected[k]` across all bucket indices in `observed`. Both arrays are equal length; `expected[k]` MUST be non-zero for each k. Returns the chi² statistic (Number).

       - `function deriveDailyHeroWagersBaseSlot()` — runs `FOUNDRY_DISABLE_NIGHTLY_WARNING=1 forge inspect contracts/storage/DegenerusGameStorage.sol:DegenerusGameStorage storageLayout` via `execSync`, parses the output text, and extracts the slot index for the `dailyHeroWagers` mapping. Returns the slot index as a `BigInt`. The function asserts the result is a non-negative BigInt; on parse failure it throws with a clear message including the forge output snippet for diagnosis. This re-validates the storage-layout EMPTY-diff attestation from `292-01-MEASUREMENT.md` §2 at test runtime — if v41 close pins it at some index N, the post-HRROLL tree MUST return the same N (else Phase 292's HRROLL-06 attestation regressed and the test fails loudly before any storage-read).

       - `function derivedailyHeroWagersSlot(D, q, baseSlot)` — Solidity nested-mapping-with-fixed-array derivation. Body: `const parentSlot = BigInt(hre.ethers.keccak256(hre.ethers.AbiCoder.defaultAbiCoder().encode(["uint256","uint256"], [BigInt(D), baseSlot]))); return "0x" + (parentSlot + BigInt(q)).toString(16).padStart(64, "0");`. Returns the storage slot for `dailyHeroWagers[D][q]` as a 0x-prefixed 64-hex-char string suitable for `ethers.provider.getStorageAt`. The "+q" addition (NOT another keccak) is correct because Solidity stores fixed-length arrays contiguously starting at the parent slot.

       - `async function readDailyHeroWagersSlots(addr, D, baseSlot)` — calls `derivedailyHeroWagersSlot(D, q, baseSlot)` for q=0..3 and returns a `BigInt[4]` of the slot contents via `BigInt(await hre.ethers.provider.getStorageAt(addr, slot))`. Output shape matches the `dailyHeroWagers` input signature of `rollHeroSymbolRef`.

       - `async function pinDailyEntropy(game, deployer, mockVRF, word)` — port from Phase 282/291 pattern (test/edge/MintBatchDeterminism.test.js L254-L268): drive `game.connect(deployer).advanceGame()` to issue a VRF request, then `fulfillVRF(mockVRF, requestId, word)` to seed the random word for the next jackpot resolution.

       - `function unpackHeroFromTraitsPacked(mainTraitsPacked, heroQuadrant)` — decode the trait byte at `mainTraitsPacked[heroQuadrant]` per `_applyHeroOverride` L1623-1627. Body: `const byte = Number((BigInt(mainTraitsPacked) >> BigInt(heroQuadrant * 8)) & 0xFFn); return { quadrant: (byte >> 6) & 3, color: (byte >> 3) & 7, symbol: byte & 7 };`. Returns the decoded `{ quadrant, color, symbol }` tuple. NB: The hero override writes ALL 4 trait bytes to the same `(heroQuadrant, heroSymbol)` per `_applyHeroOverride` L1623, so all 4 byte slots decode to the same `(quadrant, symbol)`; reading byte 0 suffices for cross-attestation.

       - `function seedDailyHeroWagersDirect(addr, D, baseSlot, rawAmounts)` — synthetic state seeding via `hardhat_setStorageAt`. Pack `rawAmounts` (32-length uint32 array, flat idx layout `(q<<3)|s`) via `packDailyHeroWagers` from Plan 01, then for q=0..3 issue `await hre.network.provider.send("hardhat_setStorageAt", [addr, slot, valueHex])` where `slot = derivedailyHeroWagersSlot(D, q, baseSlot)` and `valueHex = "0x" + packed[q].toString(16).padStart(64, "0")`. The chi² fixtures (TST-HRROLL-01 + TST-HRROLL-02) use this for synthetic seeding; TST-HRROLL-03 uses the production path (`placeDegeneretteBet`) instead.

    6. Top-level `describe("HeroOverrideWeightedRoll — Phase 293 v42.0 HRROLL regression fixture", function () { this.timeout(900_000); after(() => restoreAddresses()); ... })` block with six nested `describe` blocks (one per TST-HRROLL-NN). For this task, only emit the scaffold + the first describe block as an anchor that exercises the JS-replay oracle wiring:

       `describe("TST-HRROLL setup-and-sanity — JS-replay oracle wiring + forge-inspect storage-layout baseSlot derivation", function () { ... })`

       Inside:
       - `it("derives dailyHeroWagers base slot from forge inspect storageLayout and asserts the slot index is a non-negative BigInt", async function () { const baseSlot = deriveDailyHeroWagersBaseSlot(); expect(typeof baseSlot).to.equal("bigint"); expect(baseSlot >= 0n).to.be.true; console.log("      [TST-HRROLL setup] dailyHeroWagers BASE_SLOT = " + baseSlot.toString()); });`
       - `it("the JS-replay oracle returns (false,0,0) on a zero-wager input (HRROLL-01 early-bail)", function () { const out = rollHeroSymbolRef({ day: 1, entropy: 0n, dailyHeroWagers: [0n, 0n, 0n, 0n] }); expect(out.hasWinner).to.equal(false); expect(out.winQuadrant).to.equal(0); expect(out.winSymbol).to.equal(0); });`
       - `it("packDailyHeroWagers round-trips a single-bettor raw-amount array into a 4-element BigInt array with the expected slot byte layout", function () { const raw = new Array(32).fill(0); raw[0] = 1000; const packed = packDailyHeroWagers(raw); expect(packed.length).to.equal(4); expect(Number(packed[0] & ROLL_HERO_SYMBOL_CONSTANTS.U32_MASK)).to.equal(1000); for (let q = 1; q < 4; ++q) expect(packed[q]).to.equal(0n); });`

       The remaining 5 describe blocks (TST-HRROLL-01 + 02 + 03 + 04 + 05 + 06) are emitted in Tasks 2-7 below. This task ships the file skeleton + JSDoc header + 5 module-level helpers + the setup-and-sanity anchor describe block.

       Restrictions on Task 1 emission:
       - Do NOT yet implement TST-HRROLL-01/02/03/04/05/06 — those come in Tasks 2-7.
       - Do NOT touch `contracts/` (default zero-mutation posture per `feedback_no_contract_commits.md`).
       - Do NOT touch sister test files (`test/edge/HeroOverrideDayIndex.test.js`, `test/edge/MintBatchDeterminism.test.js`, `test/edge/MintCleanupRegression.test.js`).
       - Do NOT issue any `git commit` (single USER-APPROVED batched commit at Task 8).
       - JSDoc bullet (v) MAY note the ROADMAP `[500, 100, 100, 100]` typo as path-of-investigation prose; the test ASSERTS against the LOCKED seed `[500, 200, 200, 100]`. The two are mutually exclusive in the asserting code.
  </action>
  <verify>
    <automated>
test -f test/edge/HeroOverrideWeightedRoll.test.js
    </automated>
    <automated>
grep -c "TST-HRROLL-01" test/edge/HeroOverrideWeightedRoll.test.js
    </automated>
    <automated>
grep -c "TST-HRROLL-06" test/edge/HeroOverrideWeightedRoll.test.js
    </automated>
    <automated>
grep -c "a0218952" test/edge/HeroOverrideWeightedRoll.test.js
    </automated>
    <automated>
grep -c "D-293-STALE-VIEW-01\|getDailyHeroWinner" test/edge/HeroOverrideWeightedRoll.test.js
    </automated>
    <automated>
grep -nE "500.*200.*200.*100|TST_HRROLL_02_SEED" test/edge/HeroOverrideWeightedRoll.test.js
    </automated>
    <automated>
grep -c "rollHeroSymbolRef\|packDailyHeroWagers" test/edge/HeroOverrideWeightedRoll.test.js
    </automated>
    <automated>
npx hardhat test test/edge/HeroOverrideWeightedRoll.test.js --grep "setup-and-sanity"
    </automated>
    <automated>
git diff --name-only contracts/ | grep -v "^$" | head -1 | { read line; if [ -n "$line" ]; then echo "FAIL: contracts/ touched: $line"; exit 1; else echo "contracts/ clean"; fi; }
    </automated>
  </verify>
  <done>
    - `test/edge/HeroOverrideWeightedRoll.test.js` exists with the SPDX header, JSDoc 5-bullet block, the 11 module-level constants/helpers, and the setup-and-sanity describe block (3 tests).
    - JSDoc cites Phase 292 audit-subject commit `a0218952` and bullets (i)..(v) per the 293-CONTEXT.md spec.
    - The setup-and-sanity describe block passes — `forge inspect` BASE_SLOT derivation succeeds, oracle zero-wager smoke matches, `packDailyHeroWagers` round-trips.
    - Zero `contracts/` modifications; sister frozen test files (HeroOverrideDayIndex.test.js, MintBatchDeterminism.test.js) byte-identical.
    - No `git commit` issued.
  </done>
</task>

<task type="auto">
  <name>Task 2: TST-HRROLL-01 — weighted-distribution chi² at N=10000 against bonus-adjusted expectation</name>
  <files>test/edge/HeroOverrideWeightedRoll.test.js</files>
  <read_first>
    - test/edge/HeroOverrideWeightedRoll.test.js (the file from Task 1)
    - test/stat/PerPullLevelDistribution.test.js L260-L283 (chi² per-bucket accumulation + critical-value lookup + `console.log` traceability pattern)
    - test/helpers/rollHeroSymbolRef.mjs (Plan 01 — the JS-replay oracle)
    - .planning/phases/293-hrroll-regression-fixture-tst-hrroll/293-CONTEXT.md `<specifics>` "TST-HRROLL-01 fixture weights" entry
    - contracts/modules/DegenerusGameJackpotModule.sol L1681-L1699 (pass-2 cursor walk + leaderBonus add — the bit pattern the oracle mirrors)
  </read_first>
  <action>
    Add a new nested describe block to `test/edge/HeroOverrideWeightedRoll.test.js`:

    `describe("TST-HRROLL-01 — weighted-distribution chi² uniformity at N=" + N_CHI2 + " under seed " + JSON.stringify(TST_HRROLL_01_SEED) + " (df=3, crit=" + CHI2_CRIT_05[3] + ")", function () { ... })`

    Inside, ONE `it` test: `it("chi² statistic over " + N_CHI2 + " JS-oracle iterations is < " + CHI2_CRIT_05[3] + " against bonus-adjusted expected rates", function () { ... })`.

    Body:

    1. Build the raw 32-length flat amounts array: `const raw = new Array(32).fill(0); raw[0] = 400; raw[1] = 300; raw[2] = 200; raw[3] = 100;` — places the chi² seed at quadrant 0, symbols 0..3.

    2. Compute the bonus-adjusted expectation manually for the 4 non-zero buckets: `const total = 1000; const maxAmount = 400; const leaderBonus = 200 /* maxAmount/2 */; const effectiveTotal = total + leaderBonus; // = 1200`. Per-bucket effective weights: `[400+200, 300, 200, 100] = [600, 300, 200, 100]`. Expected pick rates: `[600/1200, 300/1200, 200/1200, 100/1200] = [0.5, 0.25, 0.1667, 0.0833]`.

    3. Pack the raw array via `const packed = packDailyHeroWagers(raw);` and pin a fixed `day = 1` for the run.

    4. Initialize `const observed = [0, 0, 0, 0];` — bucket counts for flat idx 0..3 only (other 28 buckets MUST have count 0 by construction; assert this in step 6).

    5. Drive N=10000 iterations. Use deterministic-but-distinct entropy inputs: `for (let i = 0; i < N_CHI2; ++i) { const entropy = (BigInt(i) << 192n) | (BigInt(i + 1) << 128n) | (BigInt(i + 2) << 64n) | BigInt(i + 3); const out = rollHeroSymbolRef({ day: 1, entropy, dailyHeroWagers: packed }); if (!out.hasWinner) throw new Error("hasWinner=false at i=" + i + " on non-zero seed"); const flatIdx = (out.winQuadrant << 3) | out.winSymbol; if (flatIdx >= 4) throw new Error("oracle returned non-seeded slot at i=" + i + " flatIdx=" + flatIdx); observed[flatIdx]++; }`.

    6. Assert the 28 zero-amount buckets received 0 hits (implicit by step 5 throw on `flatIdx >= 4`); assert `observed.reduce((a,b) => a+b, 0) === N_CHI2`.

    7. Compute the chi² statistic against bonus-adjusted expectation: `const expected = [N_CHI2 * 0.5, N_CHI2 * 0.25, N_CHI2 * (200/1200), N_CHI2 * (100/1200)];` then `const chi2 = computeChi2Multinomial(observed, expected);`.

    8. Critical-value lookup: `const crit = CHI2_CRIT_05[3]; // 7.815, df = 4 buckets - 1`.

    9. Assert + log per Phase 262 STAT-04 pattern: `expect(chi2, "chi² = " + chi2.toFixed(3) + " >= " + crit + " (df=3); observed=[" + observed.join(",") + "], expected=[" + expected.map(e => e.toFixed(1)).join(",") + "]").to.be.lt(crit); console.log("      [TST-HRROLL-01] chi² = " + chi2.toFixed(3) + " < " + crit + " (df=3); observed=[" + observed.join(",") + "]; expected=[" + expected.map(e => e.toFixed(1)).join(",") + "]; N=" + N_CHI2);`.

    10. (Optional sanity log) Compute and `console.log` the Wilson-Hilferty Z for the chi² statistic via `wilsonHilfertyZ(chi2, 3)` — purely traceability; no assertion.

    Restrictions:
    - Do NOT use the Solidity contract for this test — JS-replay oracle only. Cross-attestation against the contract lives in Task 7.
    - Do NOT replace the BONUS-ADJUSTED expectation with raw-weight expectation `[0.4, 0.3, 0.2, 0.1]` — the test EXERCISES the leader-bonus and asserts the algorithm produces the bonus-adjusted distribution, not the raw-weight distribution. A test that asserted against raw weights would falsely fail (chi² would be ~hundreds vs crit 7.815).
    - Do NOT extract `chi²` to a helper file (293-CONTEXT.md deferred decision).
  </action>
  <verify>
    <automated>
grep -c "TST-HRROLL-01 — weighted-distribution chi²" test/edge/HeroOverrideWeightedRoll.test.js
    </automated>
    <automated>
grep -nE "expected.*600.*1200|600.*/.*1200|0\\.5\\b" test/edge/HeroOverrideWeightedRoll.test.js | head -5
    </automated>
    <automated>
npx hardhat test test/edge/HeroOverrideWeightedRoll.test.js --grep "TST-HRROLL-01"
    </automated>
  </verify>
  <done>
    - The TST-HRROLL-01 describe block exists and passes under `npx hardhat test --grep "TST-HRROLL-01"`.
    - Chi² is computed against BONUS-ADJUSTED expected rates `[600/1200, 300/1200, 200/1200, 100/1200]`, not raw-weight rates.
    - Test logs the chi² statistic + observed counts via `console.log` per Phase 262 STAT-04 traceability pattern.
    - Zero `contracts/` modifications.
  </done>
</task>

<task type="auto">
  <name>Task 3: TST-HRROLL-02 — ×1.5 leader-bonus binomial at N=10000 under LOCKED seed [500, 200, 200, 100] expected leader rate exactly 0.60</name>
  <files>test/edge/HeroOverrideWeightedRoll.test.js</files>
  <read_first>
    - test/edge/HeroOverrideWeightedRoll.test.js (file from Task 2)
    - test/helpers/rollHeroSymbolRef.mjs (Plan 01)
    - .planning/phases/293-hrroll-regression-fixture-tst-hrroll/293-CONTEXT.md `<specifics>` "TST-HRROLL-02 fixture weights" entry (notes ROADMAP math inconsistency)
    - .planning/phases/293-hrroll-regression-fixture-tst-hrroll/293-CONTEXT.md `<deferred>` "TST-HRROLL-02 roadmap-math reconciliation" entry
    - The LOCKED disposition from <locked_user_dispositions>: seed [500, 200, 200, 100], expected leader rate exactly 0.60 = 750/1250
  </read_first>
  <action>
    Add a new nested describe block to `test/edge/HeroOverrideWeightedRoll.test.js`:

    `describe("TST-HRROLL-02 — ×1.5 leader-bonus binomial sanity at N=" + N_CHI2 + " under LOCKED seed " + JSON.stringify(TST_HRROLL_02_SEED) + " (df=1, crit=" + CHI2_CRIT_05[1] + ")", function () { ... })`

    Inside, ONE `it` test: `it("leader pick-rate matches expected 0.60 = 750/1250 (= (maxAmount + leaderBonus) / effectiveTotal) within chi² df=1 tolerance", function () { ... })`.

    Body:

    1. Build the LOCKED seed: `const raw = new Array(32).fill(0); raw[0] = 500; raw[1] = 200; raw[2] = 200; raw[3] = 100;` — quadrant 0, symbols 0..3; leader at flat idx 0 (strict-`>` first-seen).

    2. Document the algebra inline as a JS comment block per `feedback_no_history_in_comments.md` "describe what IS" — describe the v42 mechanics, not the ROADMAP example. Comment content:
       - `total = 500 + 200 + 200 + 100 = 1000`
       - `maxAmount = 500 (leader at flat idx 0 by strict-> first-seen tie-break)`
       - `leaderBonus = maxAmount / 2 = 250`
       - `effectiveTotal = total + leaderBonus = 1250`
       - `leader effective weight = maxAmount + leaderBonus = 750`
       - `expected leader pick-rate = 750 / 1250 = 0.60 exactly`
       - `LOCKED disposition (user 2026-05-17): the seed [500, 200, 200, 100] is the unique-up-to-permutation 4-slot vector satisfying leader = 0.5 * total which is the algebraic constraint for (L + L/2)/(T + L/2) = 0.60 under D-42N-LEADER-BONUS-01`

    3. Assert the algebra at runtime to lock-in the constants:
       - `expect(TST_HRROLL_02_SEED.reduce((a,b) => a+b, 0)).to.equal(1000);`
       - `expect(Math.max(...TST_HRROLL_02_SEED)).to.equal(500);`
       - `expect(TST_HRROLL_02_EXPECTED_LEADER_RATE).to.equal(0.6);`

    4. Pack via `const packed = packDailyHeroWagers(raw);` and pin `day = 2` (distinct from TST-HRROLL-01 day=1 for trace clarity).

    5. Drive N=10000 iterations with deterministic-but-distinct entropy. Reuse the same entropy-construction shape from Task 2 but offset by a constant to ensure independence: `for (let i = 0; i < N_CHI2; ++i) { const entropy = ((BigInt(i) + 0xDEADBEEFn) << 192n) | (BigInt(i + 1) << 128n) | (BigInt(i + 2) << 64n) | BigInt(i + 3); const out = rollHeroSymbolRef({ day: 2, entropy, dailyHeroWagers: packed }); if (!out.hasWinner) throw new Error("hasWinner=false at i=" + i); const flatIdx = (out.winQuadrant << 3) | out.winSymbol; if (flatIdx >= 4) throw new Error("oracle returned non-seeded slot at i=" + i + " flatIdx=" + flatIdx); if (flatIdx === 0) leaderHits++; else otherHits++; }`.

    6. Compute the empirical leader rate: `const empiricalLeaderRate = leaderHits / N_CHI2;`. Log: `console.log("      [TST-HRROLL-02] empirical leader pick-rate = " + empiricalLeaderRate.toFixed(4) + " (target = " + TST_HRROLL_02_EXPECTED_LEADER_RATE + " = 750/1250); leaderHits=" + leaderHits + ", otherHits=" + otherHits + ", N=" + N_CHI2);`.

    7. Compute the binomial chi² (collapsed to "leader vs others", df=1): `const expectedLeader = N_CHI2 * TST_HRROLL_02_EXPECTED_LEADER_RATE; // 6000` and `const expectedOther = N_CHI2 * (1 - TST_HRROLL_02_EXPECTED_LEADER_RATE); // 4000`. Then `const chi2 = computeChi2Multinomial([leaderHits, otherHits], [expectedLeader, expectedOther]);`.

    8. Assert + log: `const crit = CHI2_CRIT_05[1]; // 3.841; expect(chi2, "chi² = " + chi2.toFixed(3) + " >= " + crit + " (df=1); leaderHits=" + leaderHits + ", otherHits=" + otherHits).to.be.lt(crit); console.log("      [TST-HRROLL-02] chi² = " + chi2.toFixed(3) + " < " + crit + " (df=1, binomial)");`.

    9. (Optional sanity) `console.log` the Wilson-Hilferty Z for the chi² stat — traceability only, no assertion.

    Restrictions:
    - DO NOT change the seed from `[500, 200, 200, 100]` — this is the LOCKED user disposition per `<locked_user_dispositions>`. The seed is unique-up-to-permutation for the 4-slot 0.60-target constraint under the contract algorithm.
    - DO NOT assert against the ROADMAP example seed `[500, 100, 100, 100]` (which yields 71.4% not 60%).
    - DO NOT replace the binomial collapse with a 4-way chi² df=3 — the LOCKED disposition is the binomial collapse to "leader vs others", df=1, crit=3.841.
    - Per `feedback_no_history_in_comments.md`: the JSDoc may include a path-of-investigation note that the ROADMAP example was algebraically inconsistent (because that prose documents the basis for the chosen seed); but the test code itself describes only v42 mechanics, NOT what changed.
  </action>
  <verify>
    <automated>
grep -c "TST-HRROLL-02 — ×1.5 leader-bonus binomial" test/edge/HeroOverrideWeightedRoll.test.js
    </automated>
    <automated>
grep -nE "TST_HRROLL_02_SEED|500.*200.*200.*100|750.*1250|0\\.6\\b" test/edge/HeroOverrideWeightedRoll.test.js | head -8
    </automated>
    <automated>
grep -cE "\\bCHI2_CRIT_05\\[1\\]" test/edge/HeroOverrideWeightedRoll.test.js
    </automated>
    <automated>
npx hardhat test test/edge/HeroOverrideWeightedRoll.test.js --grep "TST-HRROLL-02"
    </automated>
  </verify>
  <done>
    - The TST-HRROLL-02 describe block exists and passes under `npx hardhat test --grep "TST-HRROLL-02"`.
    - The seed asserted is `[500, 200, 200, 100]` (LOCKED).
    - The expected leader rate asserted is `0.60` exactly = `750 / 1250`.
    - The chi² df=1 critical value `3.841` is used.
    - Test logs the empirical leader rate + chi² statistic via `console.log` per Phase 262 STAT-04 traceability pattern.
    - Zero `contracts/` modifications.
  </done>
</task>

<task type="auto">
  <name>Task 4: TST-HRROLL-03 — RNG commitment-window proof via direct-storage-read across day-D→D+1 advance</name>
  <files>test/edge/HeroOverrideWeightedRoll.test.js</files>
  <read_first>
    - test/edge/HeroOverrideWeightedRoll.test.js (file from Task 3)
    - contracts/modules/DegenerusGameDegeneretteModule.sol L484-L501 (placeDegeneretteBet wager-time write to dailyHeroWagers[dailyIdx][q])
    - contracts/storage/DegenerusGameStorage.sol L1470-L1475 (dailyHeroWagers declaration; index pinned via Task 1 deriveDailyHeroWagersBaseSlot)
    - .planning/phases/293-hrroll-regression-fixture-tst-hrroll/293-CONTEXT.md `<decisions>` "TST-HRROLL-03 RNG commitment-window proof technique" entry — direct-storage-read disposition (NOT getDailyHeroWinner)
    - .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md "Carry-forward anchors" → D-288-FIX-SHAPE-01 dailyIdx single-writer invariant
    - test/edge/MintBatchDeterminism.test.js (`loadFixture(deployFullProtocol)` + `pinDailyEntropy` + `advanceGame` drain pattern)
    - test/helpers/testUtils.js exports (`advanceToNextDay`, `getLastVRFRequestId`, `fulfillVRF`, `eth`)
    - test/helpers/rollHeroSymbolRef.mjs (Plan 01 — the oracle replays against captured storage bytes)
  </read_first>
  <action>
    Add a new nested describe block to `test/edge/HeroOverrideWeightedRoll.test.js`:

    `describe("TST-HRROLL-03 — RNG commitment-window proof: dailyHeroWagers[D][q] frozen across day-D→D+1 advance", function () { ... })`

    Inside, ONE `it` test: `it("captures dailyHeroWagers[D][0..3] slot bytes pre-advance, advances day, asserts post-advance bytes are byte-identical, and the JS-oracle reproduces the same (q, s) for both captures", async function () { ... })`.

    Body:

    1. Boot the fixture: `const { game, deployer, alice, bob, mockVRF } = await loadFixture(deployFullProtocol); const gameAddr = await game.getAddress();`.

    2. Derive the storage base slot: `const baseSlot = deriveDailyHeroWagersBaseSlot();`.

    3. Capture day-D before any bet placement. Read `dailyIdx` directly from the contract storage via the helper view exposed by the DegenerusGame interface, OR call the public `currentDay()` / `dailyIdx()` view if one exists. (Discovery path: at execute time the executor inspects `contracts/DegenerusGame.sol` and `contracts/interfaces/IDegenerusGame.sol` to find the public-view accessor; if none exists, read `dailyIdx` from slot 0 byte offset 4..7 per the v41 Phase 288 `D-288-FIX-SHAPE-01` storage layout. Bind the result as `const dayD = Number(await game.dailyIdx()); // or storage-slot read fallback` — see the verify gate for the discovery confirmation.)

    4. Place hero wagers on day D for at least 2 quadrants via `placeDegeneretteBet`. Concrete plan:
       - Call `placeDegeneretteBet` on `game.connect(alice)` with parameters that drive a hero-wager write to a specific `(quadrant, symbol)` slot. The exact ABI for `placeDegeneretteBet` is discovered at execute time from `contracts/modules/DegenerusGameDegeneretteModule.sol:placeDegeneretteBet` and the interface declaration; the relevant inputs include `heroQuadrant` and `customTicket` (which the contract uses to extract `heroSymbol = uint8(customTicket >> (heroQuadrant * 8)) & 7;` per L487-L488).
       - At least 2 bets: e.g., `(heroQuadrant=0, heroSymbol=0, betAmount=eth(1))` and `(heroQuadrant=2, heroSymbol=5, betAmount=eth(0.5))`. The exact bet amounts MUST be ≥ 1e12 wei (per the L489-L490 `wagerUnit = totalBet / 1e12; if (wagerUnit > 0) {...}` guard).
       - Verify post-bet that `dailyHeroWagers[D][0]` and `dailyHeroWagers[D][2]` slot bytes have non-zero amounts at the expected (symbol) bit offsets.

    5. Pre-advance storage capture:
       - `const slotsBeforeAdvance = await readDailyHeroWagersSlots(gameAddr, dayD, baseSlot); // BigInt[4]`
       - Assert at least one of `slotsBeforeAdvance` is non-zero (proves the bets landed at the slots being read).
       - Compute a JS-oracle output to lock against: `const refEntropy = DAILY_ENTROPY; const refOut = rollHeroSymbolRef({ day: dayD, entropy: refEntropy, dailyHeroWagers: slotsBeforeAdvance });` — captures the (q, s) outcome under a fixed entropy.

    6. Advance day-D→D+1 via the production path. Use the established `pinDailyEntropy` + advance chain pattern from Phase 282 (test/edge/MintBatchDeterminism.test.js L254-L268 + L260): drive `advanceGame()` once to issue the VRF request for day D's daily resolution; `fulfillVRF(mockVRF, requestId, DAILY_ENTROPY)`; drive `advanceGame()` repeatedly until `dailyIdx` increments to `dayD + 1` OR a hard cap of 30 iterations is reached (the cap prevents infinite loops if the advance chain gets stuck). After the advance, assert `Number(await game.dailyIdx()) === dayD + 1` (or the storage-slot fallback).

    7. Post-advance storage capture:
       - `const slotsAfterAdvance = await readDailyHeroWagersSlots(gameAddr, dayD, baseSlot); // BigInt[4]`
       - Assert byte-identical to `slotsBeforeAdvance`: `for (let q = 0; q < 4; ++q) { expect(slotsAfterAdvance[q]).to.equal(slotsBeforeAdvance[q], "dailyHeroWagers[" + dayD + "][" + q + "] mutated across day advance (pre=" + slotsBeforeAdvance[q].toString(16) + " post=" + slotsAfterAdvance[q].toString(16) + ")"); }`.

    8. JS-oracle replay against post-advance bytes: `const replayOut = rollHeroSymbolRef({ day: dayD, entropy: refEntropy, dailyHeroWagers: slotsAfterAdvance }); expect(replayOut).to.deep.equal(refOut);` — proves the oracle produces the same (q, s) before and after the advance (commitment-window invariant at fixture level).

    9. `console.log` the captured slot bytes for traceability: `console.log("      [TST-HRROLL-03] dayD=" + dayD + "; slots=[" + slotsBeforeAdvance.map(s => "0x" + s.toString(16)).join(", ") + "]; oracle output={hasWinner:" + refOut.hasWinner + ", q:" + refOut.winQuadrant + ", s:" + refOut.winSymbol + "}; dailyIdx invariant: dailyIdx==D as controlling day key (D-288-FIX-SHAPE-01)");`.

    10. Assert the `dailyIdx`-as-controlling-day-key invariant inline: `expect(Number(await game.dailyIdx()), "dailyIdx must equal dayD + 1 = " + (dayD + 1) + " after advance (D-288-FIX-SHAPE-01 single-writer invariant)").to.equal(dayD + 1);`. (If `dailyIdx()` accessor doesn't exist as a public view, use the slot-0 byte-4..7 read fallback established in step 3.)

    Restrictions:
    - DO NOT use `getDailyHeroWinner` as an assertion vehicle (per D-293-STALE-VIEW-01) — that view returns v41-deterministic output, not the weighted-roll oracle output.
    - DO NOT call `_rollHeroSymbol` directly from the test — it's `private view` (per D-293-INVOKE-01); the JS-oracle is the load-bearing assertion vehicle.
    - DO NOT modify `contracts/` (no visibility flip in this task).
  </action>
  <verify>
    <automated>
grep -c "TST-HRROLL-03 — RNG commitment-window proof" test/edge/HeroOverrideWeightedRoll.test.js
    </automated>
    <automated>
grep -nE "readDailyHeroWagersSlots|getStorageAt|dailyIdx" test/edge/HeroOverrideWeightedRoll.test.js | head -8
    </automated>
    <automated>
grep -cE "getDailyHeroWinner" test/edge/HeroOverrideWeightedRoll.test.js | { read count; if [ "$count" -le 2 ]; then echo "getDailyHeroWinner cited at most twice (JSDoc note only — OK)"; else echo "FAIL: getDailyHeroWinner cited >2 times — possible assertion-vehicle misuse"; exit 1; fi; }
    </automated>
    <automated>
npx hardhat test test/edge/HeroOverrideWeightedRoll.test.js --grep "TST-HRROLL-03"
    </automated>
  </verify>
  <done>
    - The TST-HRROLL-03 describe block exists and passes under `npx hardhat test --grep "TST-HRROLL-03"`.
    - Pre-advance and post-advance slot bytes captured via `readDailyHeroWagersSlots` and asserted byte-identical for all 4 quadrants of `dailyHeroWagers[dayD]`.
    - JS-oracle replay produces identical (hasWinner, q, s) for both captures under a fixed entropy.
    - `dailyIdx` asserted to equal `dayD + 1` after advance — D-288-FIX-SHAPE-01 single-writer invariant verified.
    - `getDailyHeroWinner` appears at most twice (JSDoc + path-of-investigation note); NOT used as an assertion vehicle.
    - Zero `contracts/` modifications.
  </done>
</task>

<task type="auto">
  <name>Task 5: TST-HRROLL-04 + TST-HRROLL-05 — single-bettor and zero-wager edge cases over 100 entropy variations each</name>
  <files>test/edge/HeroOverrideWeightedRoll.test.js</files>
  <read_first>
    - test/edge/HeroOverrideWeightedRoll.test.js (file from Task 4)
    - test/helpers/rollHeroSymbolRef.mjs (Plan 01)
    - .planning/phases/293-hrroll-regression-fixture-tst-hrroll/293-CONTEXT.md `<specifics>` "TST-HRROLL-04 single-bettor fixture" + "TST-HRROLL-05 zero-wager fixture" entries
    - contracts/modules/DegenerusGameJackpotModule.sol L1677-L1679 (total==0 early-bail)
  </read_first>
  <action>
    Add two new nested describe blocks to `test/edge/HeroOverrideWeightedRoll.test.js`:

    **Block 1 — TST-HRROLL-04:**
    `describe("TST-HRROLL-04 — single-bettor edge case: deterministic (q, s) return with probability 1.0 across " + N_EDGE + " entropy variations", function () { ... })`

    Inside, ONE `it` test: `it("seed[flatIdx=0] = 1000; assert rollHeroSymbolRef returns (true, 0, 0) for all " + N_EDGE + " distinct entropy values", function () { ... })`.

    Body:
    - `const raw = new Array(32).fill(0); raw[0] = 1000; const packed = packDailyHeroWagers(raw);` — seed at quadrant 0, symbol 0; sole non-zero slot.
    - For 100 iterations with distinct entropy: `for (let i = 0; i < N_EDGE; ++i) { const entropy = (BigInt(i) << 200n) ^ (BigInt(i * 0x12345678) << 64n) ^ BigInt(i + 0xCAFEBABE); const out = rollHeroSymbolRef({ day: 3, entropy, dailyHeroWagers: packed }); expect(out.hasWinner, "i=" + i + " hasWinner should be true").to.equal(true); expect(out.winQuadrant, "i=" + i + " winQuadrant should be 0").to.equal(0); expect(out.winSymbol, "i=" + i + " winSymbol should be 0").to.equal(0); }`.
    - `console.log("      [TST-HRROLL-04] single-bettor probability 1.0 confirmed across " + N_EDGE + " distinct entropy values (HRROLL-03 single-bettor disposition)");`.
    - Repeat the test with the seed at a different flat idx to exercise non-zero leaderIdx in pass-2: place 1000 at flat idx 17 (quadrant 2, symbol 1) instead of 0, drive 100 iterations, assert `(true, 2, 1)`. Combine into one `it` or split into two — the executor's choice; both seeds must be exercised.

    **Block 2 — TST-HRROLL-05:**
    `describe("TST-HRROLL-05 — zero-wager edge case: HRROLL-01 early-bail returns (false, 0, 0) across " + N_EDGE + " entropy variations", function () { ... })`

    Inside, ONE `it` test: `it("all-zero dailyHeroWagers slots; assert rollHeroSymbolRef returns (false, 0, 0) for all " + N_EDGE + " distinct entropy values", function () { ... })`.

    Body:
    - `const packed = [0n, 0n, 0n, 0n];` — all 4 quadrants zero.
    - For 100 iterations with distinct entropy: `for (let i = 0; i < N_EDGE; ++i) { const entropy = (BigInt(i) << 200n) ^ (BigInt(i * 0x87654321) << 64n) ^ BigInt(i + 0xDEADBEEF); const out = rollHeroSymbolRef({ day: 4, entropy, dailyHeroWagers: packed }); expect(out.hasWinner, "i=" + i + " hasWinner should be false (early-bail)").to.equal(false); expect(out.winQuadrant, "i=" + i + " winQuadrant should be 0").to.equal(0); expect(out.winSymbol, "i=" + i + " winSymbol should be 0").to.equal(0); }`.
    - `console.log("      [TST-HRROLL-05] zero-wager (false, 0, 0) confirmed across " + N_EDGE + " distinct entropy values (HRROLL-01 early-bail at total == 0)");`.

    Restrictions:
    - JS-replay oracle only (no contract invocation) — these are pure-function edge cases.
    - No assertions against `getDailyHeroWinner`.
  </action>
  <verify>
    <automated>
grep -c "TST-HRROLL-04 — single-bettor" test/edge/HeroOverrideWeightedRoll.test.js
    </automated>
    <automated>
grep -c "TST-HRROLL-05 — zero-wager" test/edge/HeroOverrideWeightedRoll.test.js
    </automated>
    <automated>
npx hardhat test test/edge/HeroOverrideWeightedRoll.test.js --grep "TST-HRROLL-04"
    </automated>
    <automated>
npx hardhat test test/edge/HeroOverrideWeightedRoll.test.js --grep "TST-HRROLL-05"
    </automated>
  </verify>
  <done>
    - TST-HRROLL-04 + TST-HRROLL-05 describe blocks exist and pass.
    - Single-bettor exercises at least 2 distinct leader-idx positions (flat idx 0 in quadrant 0 + flat idx 17 in quadrant 2) over 100 entropy variations each.
    - Zero-wager exercises 100 entropy variations and asserts (false, 0, 0) in every case.
    - Zero `contracts/` modifications.
  </done>
</task>

<task type="auto">
  <name>Task 6: TST-HRROLL-06 — production-path gas-delta measurement against the +431 theoretical anchor + [BLOCKING_ESCALATION] checkpoint on noise floor</name>
  <files>test/edge/HeroOverrideWeightedRoll.test.js</files>
  <read_first>
    - test/edge/HeroOverrideWeightedRoll.test.js (file from Task 5)
    - .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md §3 (full gas-delta breakdown; §3.a v41 baseline; §3.c D-42N-GAS-01 thresholds; §3.d ESCALATION-CHECKPOINT branch — NOT TRIGGERED at Phase 292)
    - .planning/phases/293-hrroll-regression-fixture-tst-hrroll/293-CONTEXT.md `<decisions>` "D-293-GAS-01 — TST-HRROLL-06 empirical gas methodology" entry + `<deferred>` "D-293-INVOKE-01 visibility-flip Phase 292 amendment" + "D-293-GAS-01 noise-floor relaxation alternative" entries
    - <locked_user_dispositions> "LOCKED — Zero contracts/ mutations (default)" entry (NEITHER fallback option is pre-approved; both surface to user at the [BLOCKING_ESCALATION] checkpoint)
    - contracts/modules/DegenerusGameJackpotModule.sol L490-L545 (payDailyJackpot path through _runJackpotEthFlow → _rollWinningTraits → _applyHeroOverride → _rollHeroSymbol) + L1988 callsite + L1639-L1700 _rollHeroSymbol body
    - contracts/modules/DegenerusGameDegeneretteModule.sol L484-L501 (placeDegeneretteBet for worst-case state seeding via production path)
  </read_first>
  <action>
    Add a new nested describe block to `test/edge/HeroOverrideWeightedRoll.test.js`:

    `describe("TST-HRROLL-06 — production-path gas regression against D-42N-GAS-01 (theoretical +431 ± " + GAS_DELTA_SOFT_TOLERANCE + " soft / ≤ " + GAS_DELTA_HARD_BOUND + " hard) with [BLOCKING_ESCALATION] on noise floor", function () { ... })`

    Inside, ONE `it` test: `it("worst-case-seeded payDailyJackpot tx gasUsed minus all-zero-seeded baseline gasUsed lands within +431 ± " + GAS_DELTA_SOFT_TOLERANCE + " gas across " + N_GAS_SAMPLES + " measurements; [BLOCKING_ESCALATION] if stddev > " + GAS_DELTA_SOFT_TOLERANCE + " gas", async function () { ... })`.

    Body:

    1. Boot one fresh fixture per measurement to avoid cross-measurement state contamination. Use a loop `for (let sampleIdx = 0; sampleIdx < N_GAS_SAMPLES; ++sampleIdx) { ... }` that accumulates two gas readings per iteration:

       (a) **Worst-case-seeded measurement:** Boot fixture, derive `baseSlot`, seed `dailyHeroWagers[dailyIdx][q]` so all 32 slots are populated with non-zero `uint32` amounts spread so the leader sits at flat idx 31 (quadrant 3, symbol 7) — this exercises the maximal pass-2 cursor walk + the leader-bonus add at the last cursor position. Concrete seeding via the helper `seedDailyHeroWagersDirect(addr, D, baseSlot, rawAmounts)` from Task 1: `const worstRaw = new Array(32); for (let i = 0; i < 31; ++i) worstRaw[i] = 100 + i; worstRaw[31] = 10000; // leader at idx 31`. Then drive the jackpot resolution: `await pinDailyEntropy(game, deployer, mockVRF, DAILY_ENTROPY);` then issue `const tx = await game.connect(deployer).payDailyJackpot(...);` with whatever arguments the public selector expects (executor discovers the ABI from the interface; if the public `payDailyJackpot` is not directly callable due to access modifiers, use `payDailyJackpotCoinAndTickets` instead; if neither is reachable, the natural advance chain through `advanceGame()` will eventually trigger jackpot resolution). Capture `const receipt = await tx.wait(); const gasWorst = Number(receipt.gasUsed);`. Verify the `DailyWinningTraits` event fired so the path through `_applyHeroOverride` → `_rollHeroSymbol` actually executed (if no `DailyWinningTraits` log appears in `receipt.logs`, the measurement is invalid and the test FAILS with a clear message).

       (b) **All-zero-seeded baseline measurement:** Boot a fresh fixture (do not reuse the worst-case fixture). Skip the synthetic seeding step — `dailyHeroWagers[dailyIdx]` is naturally all-zero at fixture boot, so the production-path `_rollHeroSymbol` invocation hits the `total == 0` early-bail at L1677-L1679 and skips the pass-2 cursor walk entirely. Drive the same path: `await pinDailyEntropy(game, deployer, mockVRF, DAILY_ENTROPY); const tx = await game.connect(deployer).payDailyJackpot(...); const receipt = await tx.wait(); const gasBaseline = Number(receipt.gasUsed);`. Verify the `DailyWinningTraits` event fired.

       (c) Compute the per-sample delta: `const delta = gasWorst - gasBaseline; samples.push({ gasWorst, gasBaseline, delta });`.

    2. After the N_GAS_SAMPLES loop, compute statistics:
       - `const deltas = samples.map(s => s.delta);`
       - `const meanDelta = deltas.reduce((a,b) => a+b, 0) / deltas.length;`
       - `const variance = deltas.reduce((a,b) => a + (b - meanDelta) ** 2, 0) / deltas.length;`
       - `const stddev = Math.sqrt(variance);`
       - `console.log("      [TST-HRROLL-06] gas-delta samples = [" + deltas.join(", ") + "]; mean = " + meanDelta.toFixed(1) + " gas; stddev = " + stddev.toFixed(1) + " gas; theoretical anchor = +" + GAS_DELTA_THEORETICAL + " gas (from 292-01-MEASUREMENT.md §3.c)");`.

    3. **Noise-floor check (BLOCKING_ESCALATION gate):** `if (stddev > GAS_DELTA_SOFT_TOLERANCE) { ... }`. Inside, throw an error with a clear `[BLOCKING_ESCALATION]` marker that lists both fallback options and STOPS without auto-proceeding. Error message structure:

       ```
       [BLOCKING_ESCALATION] TST-HRROLL-06 gas-delta stddev = ${stddev.toFixed(1)} gas across ${N_GAS_SAMPLES} samples (samples=[${deltas.join(",")}]), exceeds the ±${GAS_DELTA_SOFT_TOLERANCE} gas noise threshold. Production-path delta measurement cannot reliably resolve the ~+${GAS_DELTA_THEORETICAL} gas theoretical contribution from _rollHeroSymbol. Per `feedback_never_preapprove_contracts.md`, this test does NOT auto-proceed to either fallback. The executor MUST surface this checkpoint to the user with the following two options:

         (i) RELAX D-42N-GAS-01 bound to "within soft +1000 gas" + theoretical-attestation-cite to 292-01-MEASUREMENT.md §3.c (graceful fallback resembling D-291-GAS-01 SKIP-GAS posture).

         (ii) INVOKE D-293-INVOKE-01 visibility-flip Phase 292 amendment: one-line `private` → `internal` on _rollHeroSymbol at contracts/modules/DegenerusGameJackpotModule.sol:1643 + new contracts/test/HeroRollTester.sol harness exposing _rollHeroSymbol via an `internal` proxy + paired-call `noOp` companion + paired-call gas-delta isolation per the SURF-05 pattern. This is a SECOND USER-APPROVED contract commit at a phase that was supposed to be test-only; not pre-approved per feedback_never_preapprove_contracts.md + feedback_batch_contract_approval.md + feedback_manual_review_before_push.md.

       Neither option is pre-approved. The user types "approve (i)" or "approve (ii)" or provides alternative disposition before the executor proceeds.
       ```

       The throw uses `throw new Error("[BLOCKING_ESCALATION] ...");` — Hardhat's mocha reporter will surface this as a test failure with the full message inline. The executor's plan-phase orchestrator MUST recognize the `[BLOCKING_ESCALATION]` token in the error message and PAUSE execution rather than auto-failing the phase.

    4. **Noise-floor passes — soft + hard threshold assertions:** If `stddev <= GAS_DELTA_SOFT_TOLERANCE`, proceed to assert the mean delta against the soft and hard windows:
       - Soft: `const softLow = GAS_DELTA_THEORETICAL - GAS_DELTA_SOFT_TOLERANCE; // 331 const softHigh = GAS_DELTA_THEORETICAL + GAS_DELTA_SOFT_TOLERANCE; // 531 expect(meanDelta, "mean gas-delta = " + meanDelta.toFixed(1) + " outside soft window [" + softLow + ", " + softHigh + "]; stddev = " + stddev.toFixed(1)).to.be.within(softLow, softHigh);`. NB: `chai.expect.within(min, max)` is inclusive.
       - Hard: `expect(meanDelta, "mean gas-delta = " + meanDelta.toFixed(1) + " exceeds hard upper bound " + GAS_DELTA_HARD_BOUND + " (likely _rollHeroSymbol gas regression)").to.be.at.most(GAS_DELTA_HARD_BOUND);`.

    5. `console.log` the final disposition: `console.log("      [TST-HRROLL-06] PASS — mean delta = " + meanDelta.toFixed(1) + " gas within soft window [" + softLow + ", " + softHigh + "]; theoretical +" + GAS_DELTA_THEORETICAL + " anchor confirmed empirically");`.

    Restrictions:
    - DO NOT modify `contracts/` in this task. The `[BLOCKING_ESCALATION]` checkpoint surfaces the visibility-flip option as a USER decision, not an auto-action.
    - DO NOT pre-approve either fallback option. The throw message lists both options as user-facing dispositions.
    - If `payDailyJackpot` (or the equivalent external entry point) doesn't exist as a direct external selector, the executor uses the natural `advanceGame()` chain that reaches the jackpot-resolution path. The exact production path is discovered at execute time from the contract source; the measurement methodology (worst-case-seeded `gasUsed` − all-zero-seeded `gasUsed`) is the load-bearing invariant, not any specific external selector.
    - DO NOT measure `_rollHeroSymbol` directly (would require visibility flip — D-293-INVOKE-01 path; not invoked).
  </action>
  <verify>
    <automated>
grep -c "TST-HRROLL-06 — production-path gas regression" test/edge/HeroOverrideWeightedRoll.test.js
    </automated>
    <automated>
grep -c "BLOCKING_ESCALATION" test/edge/HeroOverrideWeightedRoll.test.js
    </automated>
    <automated>
grep -nE "GAS_DELTA_THEORETICAL|GAS_DELTA_SOFT_TOLERANCE|GAS_DELTA_HARD_BOUND|stddev" test/edge/HeroOverrideWeightedRoll.test.js | head -10
    </automated>
    <automated>
grep -cE "feedback_never_preapprove_contracts" test/edge/HeroOverrideWeightedRoll.test.js
    </automated>
    <automated>
git diff --name-only contracts/ | grep -v "^$" | head -1 | { read line; if [ -n "$line" ]; then echo "FAIL: contracts/ touched at TST-HRROLL-06 implementation: $line"; exit 1; else echo "contracts/ clean — escalation path NOT auto-invoked"; fi; }
    </automated>
  </verify>
  <done>
    - The TST-HRROLL-06 describe block exists with the worst-case-seeded vs all-zero-seeded production-path delta measurement methodology.
    - N_GAS_SAMPLES (≥5) measurements drive stddev computation.
    - `[BLOCKING_ESCALATION]` checkpoint fires (and stops the test) when `stddev > GAS_DELTA_SOFT_TOLERANCE`, listing both user-facing fallback options (relax bound vs invoke D-293-INVOKE-01 visibility flip); neither is auto-approved.
    - When noise floor passes, mean delta asserted within soft window [331, 531] (= +431 ± 100) and ≤ 750 hard upper bound.
    - Zero `contracts/` modifications by default.
    - The test runs to either (a) PASS (mean delta within soft window + stddev OK), or (b) [BLOCKING_ESCALATION] (stops execution, surfaces to user), or (c) FAIL with a clear delta-out-of-window message. No silent fall-through.
  </done>
</task>

<task type="auto">
  <name>Task 7: Cross-attestation — small-N production-path replay matches JS oracle output via DailyWinningTraits event decode</name>
  <files>test/edge/HeroOverrideWeightedRoll.test.js</files>
  <read_first>
    - test/edge/HeroOverrideWeightedRoll.test.js (file from Task 6)
    - contracts/modules/DegenerusGameJackpotModule.sol L100-L111 (DailyWinningTraits event) + L520-L523 + L538-L541 (emit sites) + L1623-L1627 (trait byte layout `(quadrant << 6) | (color << 3) | symbol`) + L1980-L1990 (_rollWinningTraits callsite)
    - .planning/phases/293-hrroll-regression-fixture-tst-hrroll/293-CONTEXT.md `<decisions>` "D-293-INVOKE-01" entry — JS-replay oracle ALGORITHM_VERIFIED via small-N production-path cross-attestation (16-64 iter)
    - test/helpers/rollHeroSymbolRef.mjs (Plan 01)
    - test/helpers/testUtils.js (fulfillVRF, getLastVRFRequestId)
  </read_first>
  <action>
    Add a new nested describe block to `test/edge/HeroOverrideWeightedRoll.test.js`:

    `describe("TST-HRROLL-01..05 cross-attestation — small-N production-path replay matches JS oracle output via DailyWinningTraits event decode", function () { ... })`

    Inside, ONE `it` test: `it("for N_CROSS distinct (dailyHeroWagers, randWord) pairs, the on-chain DailyWinningTraits hero (q, s) matches rollHeroSymbolRef output exactly", async function () { ... })`.

    Body:

    1. Constant: `const N_CROSS = 16; // small-N cross-attestation per D-293-INVOKE-01`.

    2. For N_CROSS iterations:

       (a) Boot a fresh fixture per iteration (cleanest cross-attestation — no state contamination).

       (b) Derive baseSlot via `deriveDailyHeroWagersBaseSlot()`.

       (c) Choose a per-iteration distinct seed pattern. E.g., `const raw = new Array(32).fill(0); raw[i % 32] = 1000 + i * 100; raw[(i + 7) % 32] = 500 + i * 50; raw[(i + 13) % 32] = 200 + i * 25;` — places at least 3 non-zero amounts at varying flat idx positions; ensures different leader positions across iterations.

       (d) Seed via `seedDailyHeroWagersDirect(gameAddr, dailyIdx, baseSlot, raw)`.

       (e) Pin a distinct VRF entropy per iteration: `const entropy = (BigInt(0xC0DE0000 + i) << 192n) | (BigInt(0xCAFE0000 + i) << 128n) | (BigInt(0xBEEF0000 + i) << 64n) | BigInt(0xDEAD0000 + i);`. Call `pinDailyEntropy(game, deployer, mockVRF, entropy);` so the next jackpot resolution consumes this exact word.

       (f) Drive the jackpot resolution path that fires `DailyWinningTraits` (per L520-L523 / L538-L541). The exact entry-point is whichever public selector the executor identifies at execute time (`payDailyJackpot`, `payDailyJackpotCoinAndTickets`, or the natural `advanceGame()` chain). Capture `const receipt = await tx.wait();`.

       (g) Parse the `DailyWinningTraits` log from `receipt.logs`. Use `const iface = game.interface; for (const log of receipt.logs) { try { const parsed = iface.parseLog(log); if (parsed && parsed.name === "DailyWinningTraits") { mainTraitsPacked = parsed.args.mainTraitsPacked; break; } } catch {} }`. If `mainTraitsPacked` is null, the test FAILS with a clear message.

       (h) Decode the hero `(q, s)` from `mainTraitsPacked`. Per `_applyHeroOverride` L1623-L1627, the hero override writes ALL 4 quadrant bytes with `(heroQuadrant << 6) | (heroColor << 3) | heroSymbol`. Read byte 0: `const { quadrant: onChainQ, symbol: onChainS } = unpackHeroFromTraitsPacked(mainTraitsPacked, 0);`. NB: For the cross-attestation to work, the synthetic `dailyHeroWagers` MUST be non-zero (otherwise the hero override skips the trait-byte overwrite and `mainTraitsPacked` carries the raw `JackpotBucketLib.getRandomTraits(r)` output instead — invalidating the comparison). The seed in step (c) guarantees at least 3 non-zero slots.

       (i) Drive the JS oracle with the same inputs. Capture `dailyHeroWagers[D][0..3]` via `readDailyHeroWagersSlots(gameAddr, dailyIdx, baseSlot)` AFTER the seeding (verify the synthetic seed actually landed). Then `const oracleOut = rollHeroSymbolRef({ day: Number(dailyIdx), entropy, dailyHeroWagers: capturedSlots });`.

       (j) Assert match: `expect(oracleOut.hasWinner, "i=" + i + " oracleOut.hasWinner must be true on non-zero seed").to.equal(true); expect(oracleOut.winQuadrant, "i=" + i + " quadrant mismatch: oracle=" + oracleOut.winQuadrant + " onChain=" + onChainQ).to.equal(onChainQ); expect(oracleOut.winSymbol, "i=" + i + " symbol mismatch: oracle=" + oracleOut.winSymbol + " onChain=" + onChainS).to.equal(onChainS);`.

    3. `console.log` the cross-attestation summary: `console.log("      [TST-HRROLL cross-attest] " + N_CROSS + " production-path replays matched JS oracle output exactly — D-293-INVOKE-01 ALGORITHM_VERIFIED established");`.

    Restrictions:
    - DO NOT call `_rollHeroSymbol` directly (private; would require D-293-INVOKE-01 visibility flip — not invoked).
    - Use the `DailyWinningTraits` event main-traits packed byte ONLY — do not use `getDailyHeroWinner` (D-293-STALE-VIEW-01).
    - The cross-attestation is small-N (16) by design — large-N is handled by the JS-replay oracle in TST-HRROLL-01/02.
  </action>
  <verify>
    <automated>
grep -c "cross-attestation" test/edge/HeroOverrideWeightedRoll.test.js
    </automated>
    <automated>
grep -c "DailyWinningTraits" test/edge/HeroOverrideWeightedRoll.test.js
    </automated>
    <automated>
grep -c "unpackHeroFromTraitsPacked" test/edge/HeroOverrideWeightedRoll.test.js
    </automated>
    <automated>
npx hardhat test test/edge/HeroOverrideWeightedRoll.test.js --grep "cross-attestation"
    </automated>
  </verify>
  <done>
    - The cross-attestation describe block exists and passes under `npx hardhat test --grep "cross-attestation"`.
    - At least 16 production-path replays exercise distinct synthetic seeds + distinct VRF entropy values.
    - For each replay, the on-chain `DailyWinningTraits` mainTraitsPacked byte 0 decodes to `(quadrant, symbol)` matching the JS oracle's `(winQuadrant, winSymbol)` output.
    - D-293-INVOKE-01 ALGORITHM_VERIFIED established at the cross-attestation level — the JS oracle is the trusted load-bearing assertion vehicle for TST-HRROLL-01/02/04/05.
    - Zero `contracts/` modifications.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: USER-APPROVED batched commit of Plan 01 helper + Plan 02 test file</name>
  <what-built>
    - `test/helpers/rollHeroSymbolRef.mjs` (Plan 01 — JS-replay oracle of `_rollHeroSymbol`)
    - `test/edge/HeroOverrideWeightedRoll.test.js` (Plan 02 — 7 describe blocks covering TST-HRROLL-01..06 + cross-attestation; 6+ tests total, all passing including the noise-floor-OK gas-delta path)
    - Zero `contracts/` mutations (the D-293-INVOKE-01 escalation path was NOT triggered; if Task 6's `[BLOCKING_ESCALATION]` checkpoint fired, this Task 8 commit step would not be reached without first resolving the noise-floor disposition)
  </what-built>
  <how-to-verify>
    1. Run the full Phase 293 suite: `npx hardhat test test/edge/HeroOverrideWeightedRoll.test.js`. Expect all 7 describe blocks (setup-and-sanity + TST-HRROLL-01..06 + cross-attestation) to pass with `console.log` traceability output for chi² statistics, leader pick-rate, captured storage slots, gas-delta samples, and cross-attestation summary.
    2. Confirm zero `contracts/` mutations: `git diff --name-only contracts/` should return empty.
    3. Confirm sister test files unchanged: `git diff --name-only test/edge/HeroOverrideDayIndex.test.js test/edge/MintBatchDeterminism.test.js test/edge/MintCleanupRegression.test.js test/helpers/raritySymbolBatchRef.mjs` should return empty.
    4. Review the unified diff: `git diff --stat -- test/helpers/rollHeroSymbolRef.mjs test/edge/HeroOverrideWeightedRoll.test.js`.
    5. Review the file contents one final time. Look for: (a) the LOCKED TST-HRROLL-02 seed `[500, 200, 200, 100]` in TST-HRROLL-02; (b) the LOCKED expected rate `0.60 = 750/1250`; (c) zero `getDailyHeroWinner` assertions (only path-of-investigation prose in JSDoc / inline comments); (d) the `[BLOCKING_ESCALATION]` checkpoint code in TST-HRROLL-06 with both fallback options listed; (e) zero gas-related tokens anywhere outside the TST-HRROLL-06 block (gas measurement is scoped to that test only).
    6. If everything looks correct, type `approved` to authorize the executor to issue ONE batched commit. If anything is off, describe the issue and the executor reverts to the relevant task.
  </how-to-verify>
  <on-approval>
    Stage both files and commit:

    `git add test/helpers/rollHeroSymbolRef.mjs test/edge/HeroOverrideWeightedRoll.test.js`

    Then commit with the following message body (HEREDOC; one batched commit per `feedback_batch_contract_approval.md`):

    ```
    test(293): HRROLL regression fixture TST-HRROLL-01..06 + JS-replay oracle [TST-HRROLL-01..06]

    Phase 293 v42.0 HRROLL regression fixture; covers post-HRROLL audit subject
    (Phase 292 commit a0218952) via JS-replay oracle at test/helpers/rollHeroSymbolRef.mjs +
    six regression tests at test/edge/HeroOverrideWeightedRoll.test.js.

    - TST-HRROLL-01: chi² weighted distribution at N=10000 under seed [400, 300, 200, 100];
      df=3 crit=7.815; bonus-adjusted expected rates [0.5, 0.25, 0.1667, 0.0833].
    - TST-HRROLL-02: ×1.5 leader-bonus binomial at N=10000 under LOCKED seed
      [500, 200, 200, 100]; df=1 crit=3.841; expected leader pick-rate exactly
      0.60 = 750/1250 = (maxAmount + leaderBonus) / effectiveTotal per
      D-42N-LEADER-BONUS-01.
    - TST-HRROLL-03: RNG commitment-window proof — dailyHeroWagers[D][q] slot bytes
      byte-identical across day-D→D+1 advance; D-288-FIX-SHAPE-01 dailyIdx
      single-writer invariant preserved.
    - TST-HRROLL-04: single-bettor probability 1.0 across 100 entropy variations.
    - TST-HRROLL-05: zero-wager (false, 0, 0) across 100 entropy variations
      (HRROLL-01 early-bail at total == 0).
    - TST-HRROLL-06: production-path gas-delta within +431 ± 100 soft / ≤ 750 hard
      vs the theoretical anchor from 292-01-MEASUREMENT.md §3.c.

    JS-replay oracle (D-293-INVOKE-01 ALGORITHM_VERIFIED) cross-attested against 16
    production-path replays via DailyWinningTraits event decode. Zero contracts/
    mutations (D-293-INVOKE-01 visibility-flip escalation NOT invoked). Single
    USER-APPROVED batched commit per feedback_batch_contract_approval.md.

    contracts/DegenerusGame.sol:2545-2563 getDailyHeroWinner stale v41-leftover
    public view NOT touched (D-293-STALE-VIEW-01 — deferred to v43+).
    ```

    Do NOT `git push`.
  </on-approval>
  <resume-signal>Type "approved" to authorize the batched commit, OR describe issues found during diff review (executor reverts to the relevant task).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS-replay oracle ↔ EVM contract semantics | Numeric drift risk between Number/BigInt/uint64/uint32 boundaries; mitigated structurally by Plan 01 mask/divide-by-shift discipline + this plan's small-N cross-attestation via `DailyWinningTraits` event decode (Task 7). |
| `hardhat_setStorageAt` synthetic state seeding | The fixture writes directly to mapping-derived storage slots; risk is slot-derivation error producing silent test pass against wrong storage. Mitigation: Task 1's `deriveDailyHeroWagersBaseSlot()` runs `forge inspect` at runtime and asserts the slot index matches the Phase 292 §2 attested EMPTY diff (storage-layout regression caught immediately if the index drifted). |
| Production-path gas-delta measurement noise | Risk: jackpot-path total gas (~hundreds-of-thousands) drowns the ~500-gas `_rollHeroSymbol` contribution. Mitigation: paired-call delta methodology + 5-sample stddev + `[BLOCKING_ESCALATION]` checkpoint at noise floor > ±100 gas. Never silently passes a noisy measurement. |

(This is a test-only phase by default; no production attack surface introduced. The D-293-INVOKE-01 escalation path — if invoked — would re-open the Phase 292 audit-subject boundary, but it requires explicit USER approval at the Task 6 `[BLOCKING_ESCALATION]` checkpoint per `feedback_never_preapprove_contracts.md`.)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-293-02-01 | Tampering | TST-HRROLL-02 seed disposition | mitigate | LOCKED seed `[500, 200, 200, 100]` (user disposition 2026-05-17) per `<locked_user_dispositions>`; expected leader rate exactly `0.60 = 750/1250`; binomial chi² df=1 crit=3.841 N=10000. Verify gate in Task 3 greps for the LOCKED seed + the exact expected rate. The ROADMAP example `[500, 100, 100, 100]` (71.4%) is NOT used. |
| T-293-02-02 | Information disclosure / accidental scope reopen | D-293-INVOKE-01 visibility-flip auto-invocation | mitigate | `[BLOCKING_ESCALATION]` checkpoint at TST-HRROLL-06 surfaces both fallback options to user and STOPS — does NOT auto-proceed per `feedback_never_preapprove_contracts.md`. Both options require explicit user approval; the executor cannot bypass the checkpoint. |
| T-293-02-03 | Repudiation | Storage-layout regression undetected at fixture-runtime | mitigate | Task 1 `deriveDailyHeroWagersBaseSlot()` re-runs `forge inspect storageLayout` at every test boot and asserts the slot index is parseable + non-negative. If the post-HRROLL tree returned a different slot index than the v41-close-attested value (per 292-01-MEASUREMENT.md §2 EMPTY diff), TST-HRROLL-03 would fail loudly at the slot-read step rather than silently asserting against a wrong slot. |
| T-293-02-04 | Tampering | D-293-STALE-VIEW-01 stale view used as assertion vehicle by mistake | mitigate | Verify gate in Task 4 greps for `getDailyHeroWinner` and asserts ≤ 2 occurrences (JSDoc note + path-of-investigation comment only; no assertion vehicle). If the executor accidentally wires `getDailyHeroWinner` as a test assertion, the gate FAILS and the executor must revert. |
| T-293-02-05 | Denial of service | Production-path measurement noise drowns the signal | mitigate | 5-sample stddev computation + `[BLOCKING_ESCALATION]` checkpoint at stddev > ±100 gas; no silent passing of noisy measurements. User chooses between (i) relax-bound + theoretical-cite or (ii) D-293-INVOKE-01 visibility flip — both require explicit approval per `feedback_never_preapprove_contracts.md`. |
| T-293-02-SC | Tampering | npm install / `ethers` / `chai` / `hardhat` package legitimacy | accept | No `npm install` performed in this plan — all packages are existing project dependencies. Package-legitimacy gate NOT APPLICABLE per `feedback_skip_research_test_phases.md` (no new package adds). |

**Escalation threat (D-293-INVOKE-01 path):** If user approves the visibility-flip amendment at the Task 6 `[BLOCKING_ESCALATION]` checkpoint, the threat is "Phase 292 audit-subject boundary reopened — `_rollHeroSymbol` becomes `internal view`, callable from `contracts/test/HeroRollTester.sol`." Mitigation under the escalation path follows the same byte-identity attestation pattern Phase 292 used: `forge inspect storageLayout` (EMPTY diff vs post-292 tree — visibility flip is a private-symbol delta, doesn't touch storage layout) + `forge inspect methodIdentifiers` (EMPTY diff vs post-292 tree — visibility flip on private symbol doesn't change the public ABI; selector unchanged). Per Phase 297 §3.A delta-surface inclusion convention, the escalation amendment would surface as a NEW row in `audit/FINDINGS-v42.0.md` §3.A.

**Severity:** Low (test-only default); Medium (escalation path if invoked — second contract commit at a phase that was supposed to be test-only).
</threat_model>

<verification>
- `test/edge/HeroOverrideWeightedRoll.test.js` exists with 7 describe blocks (setup-and-sanity + TST-HRROLL-01..06 + cross-attestation).
- `npx hardhat test test/edge/HeroOverrideWeightedRoll.test.js` exits 0 with all blocks passing (or PAUSES at the `[BLOCKING_ESCALATION]` checkpoint in TST-HRROLL-06 if noise floor demands).
- TST-HRROLL-02 asserts against the LOCKED seed `[500, 200, 200, 100]` and expected leader rate `0.60 = 750/1250`.
- TST-HRROLL-03 captures `dailyHeroWagers[D][0..3]` slot bytes pre + post day advance and asserts byte-identical; JS oracle reproduces the same (q, s) for both captures; `dailyIdx` asserted as controlling day key.
- TST-HRROLL-04 + TST-HRROLL-05 each drive 100 entropy variations.
- TST-HRROLL-06 production-path gas-delta methodology measures worst-case vs all-zero baseline across 5+ samples; `[BLOCKING_ESCALATION]` checkpoint fires on stddev > 100; soft window [331, 531] / hard ≤ 750.
- Cross-attestation describe block: 16 production-path replays match JS oracle output via `DailyWinningTraits` event decode.
- Zero `contracts/` mutations; sister frozen test files byte-identical.
- ONE USER-APPROVED batched commit at Task 8 carries Plan 01 helper + Plan 02 test file.
</verification>

<success_criteria>
- All 6 TST-HRROLL-NN requirement IDs covered by passing tests under `npx hardhat test test/edge/HeroOverrideWeightedRoll.test.js`.
- LOCKED TST-HRROLL-02 disposition (seed `[500, 200, 200, 100]`, expected rate 0.60) implemented exactly.
- D-293-GAS-01 [BLOCKING_ESCALATION] checkpoint surfaces both fallback options to user and stops; neither is pre-approved.
- D-293-STALE-VIEW-01 honored: `getDailyHeroWinner` NOT used as an assertion vehicle.
- D-293-INVOKE-01 default JS-replay oracle disposition implemented; ALGORITHM_VERIFIED via 16-sample production-path cross-attestation.
- ONE USER-APPROVED batched commit at phase close carries both Plan 01 + Plan 02 files.
- Zero `contracts/` mutations by default (unless user explicitly approves the visibility-flip escalation at the Task 6 checkpoint — orthogonal to this plan's default path).
</success_criteria>

<output>
Create `.planning/phases/293-hrroll-regression-fixture-tst-hrroll/293-02-SUMMARY.md` when done. The summary references both PLAN deliverables (the JS-replay oracle helper + the test fixture) and the USER-APPROVED batched commit SHA from Task 8.

Phase 293 close-out: emit the structured completion register to the orchestrator including (a) TST-HRROLL-01..06 PASS/FAIL/ESCALATED state per test, (b) the gas-delta sample register for TST-HRROLL-06, (c) the cross-attestation match count, (d) the USER-APPROVED batched commit SHA, (e) any D-293-INVOKE-01 / D-293-GAS-01 escalation outcomes (if the BLOCKING_ESCALATION checkpoint fired, the orchestrator carries the user disposition forward into the resolution path).
</output>
</content>
