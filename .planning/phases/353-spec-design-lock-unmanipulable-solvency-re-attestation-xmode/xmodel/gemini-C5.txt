The v56 afking design has already extracted the vast majority of available gas savings through structural compression (packing the accumulator into the existing 256-bit `Sub` slot to avoid new cold touches) and control-flow flattening (deferring the cross-contract storm to a ~10-day batched settle). The remaining viable optimizations are strictly localized memory-caching and loop-hoisting improvements that reduce redundant warm SLOADs without touching the frozen solvency debits or security checks.

```
VERDICT: OPTIMIZATIONS-FOUND
RATIONALE: The locked v56 aggregator design is already rigorously compressed, extracting the maximum structural savings by folding the accumulator into the existing 256-bit `Sub` slot (zero new cold slots) and eliminating the per-buy cross-contract storm. The core loops are lean; the only safe optimizations left are marginal loop-hoisting and local caching improvements that eliminate redundant warm SLOADs without altering the exact byte-level execution of solvency debits or the timing of state-freeze markers.
GAS-SUGGESTIONS: 
1. Hoist `_goRead(swept)` outside the per-buy chunk loop: The `swept` game-over state is a global flag that cannot be triggered mid-execution by afking buys (which add funds to the pool). Reading it once at the top of the `processSubscriberStage` chunk (alongside `_activeTicketLevel`) and passing it down saves ~100 gas (one warm SLOAD) per subscriber iteration. Invariant note: Safe; the global swept state cannot mutate during the buy walk.
2. Memoize `rngWordByDay[lastAutoBoughtDay]` during the `OPEN_BATCH` walk: Since subscriptions and opens often cluster around the same days, keep a 1-deep local cache (`uint24 cachedDay`, `uint256 cachedWord`) during the loop. If the next box shares the same stamp day, reuse the cached word instead of hitting storage. Saves ~100 gas (warm SLOAD) per clustered box. Invariant note: Preserves the RNG-freeze exactly; it merely avoids redundant loads of the identical immutable frozen word.
REJECTED-AS-INVARIANT-WEAKENING: 
1. Batching the `claimablePool` and `afkingFunding` debits: Accumulating the `ethValue` in a local memory variable to perform a single aggregate SSTORE at the end of the chunk would save ~100 gas per buy. REJECTED: Violates the SOLVENCY-01 invariant, which strictly mandates that the core ETH debit lines remain byte-unchanged to ensure continuous accounting safety.
2. Deferring the EV-cap RMW to the ~10-day settle leg: Delaying the `lootboxEvBenefitUsedByLevel` write out of the open path and into the batched settle to save open gas. REJECTED: Breaks the exactly-once EV-cap invariant. The budget must be checked and debited synchronously at materialization (the open) to prevent a race condition where a player manual-opens and afk-opens in the same window, double-drawing the EV budget.
3. Using live block data for sequential opens: Bypassing the `rngWordByDay` read entirely for subsequent opens in a batch by substituting live chain state (e.g., `block.prevrandao`). REJECTED: Violates the strict RNG-freeze invariant. The open MUST use the frozen seed from the stamp day to prevent manipulation by timing the open call.
```
