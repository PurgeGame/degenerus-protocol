---
phase: 318-tst-subscription-crank-correctness-removal-proofs-tst
plan: 02
type: execute
wave: 2
depends_on: ["318-01"]
files_modified:
  - test/fuzz/CrankFaucetResistance.t.sol
autonomous: true
requirements: [SAFE-01]

must_haves:
  truths:
    - "A self-crank / Sybil round-trip earns net-zero-or-negative: the BURNIE coinflip credit from cranking your own item is pegged below the gas cost of the crank tx (fixed gasUnits * 0.5 gwei peg, NOT measured gas), and the credit is illiquid coinflip stake — it cannot be round-tripped to a profit"
    - "WWXRP work (currency == 3) earns exactly zero reward when cranked"
    - "An item rewards at most once — re-cranking an already-resolved bet or already-opened box (or the same item twice in one batch) adds zero further reward, enforced by the existing delete/zero one-reward state"
    - "No crank can resolve a bet or open a box before its RNG word lands — resolution stays behind the RngNotReady / lootboxRngWordByIndex != 0 guard"
  artifacts:
    - path: "test/fuzz/CrankFaucetResistance.t.sol"
      provides: "Foundry coverage of SAFE-01 faucet-resistance + CRANK-04 WWXRP-zero + REW reward-model acceptance"
      contains: "creditFlip"
  key_links:
    - from: "crankBets / crankBoxes reward path"
      to: "BurnieCoinflip.creditFlip (one per tx)"
      via: "_ethToBurnieValue(gasUnits * CRANK_GAS_PRICE_REF)"
      pattern: "creditFlip"
    - from: "crank reward formula"
      to: "fixed gasUnits constants (no gasleft/tx.gasprice)"
      via: "CRANK_RESOLVE_BET_GAS_UNITS / CRANK_OPEN_BOX_GAS_UNITS * 0.5 gwei"
      pattern: "CRANK_.*GAS"
---

<objective>
Prove SAFE-01: the do-work crank is faucet-bounded by three caller-independent locks — the purchase-gate, the fixed gas-peg reward, and the coinflip-credit illiquidity — so a self-crank / Sybil round-trip is net-zero-or-negative. Plus the testable acceptance of CRANK-04 (WWXRP earns zero) and the REW reward model (one creditFlip per tx, fixed gasUnits constants never measured gas, reward pegged below gas).

Purpose: SAFE-01 is the economic floor on the new permissionless crank. If a caller could profit from cranking (especially their own items), the crank becomes a BURNIE faucet. This plan proves they cannot.

Output: `test/fuzz/CrankFaucetResistance.t.sol` — a Foundry suite asserting the round-trip-≤-0 property, WWXRP-zero, one-reward-per-item, and the pre-RNG-word resolution block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# The crank/reward surface under test (live file:line)
@.planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-03-SUMMARY.md

<interfaces>
<!-- The crank surface this suite exercises (live DegenerusGame.sol). Use directly. -->

DegenerusGame.sol crank entries (permissionless, no caller restriction):
  function crankBets(address[] calldata players, uint64[] calldata betIds) external;   // :1543
    // CRANK-02: if degeneretteBets[players[0]][betIds[0]] == 0 revert BatchAlreadyTaken();  // :1552 (loser-gas cap)
    // items 1..N isolated via try this._crankResolveBet(...) catch {}; reward only successes
    // WWXRP: if (currency == 3) { /* zero reward */ } else reward += ...   // :1564
    // ONE creditFlip(msg.sender, reward) at end if reward != 0             // :1578
  function crankBoxes(uint256 maxCount) external;                                       // :1592
    // box cursor; if (lootboxRngWordByIndex[index] == 0) return;  // :1603 orphan/not-ready gate
    // if (lootboxEthBase[index][player] == 0) continue;           // :1618 first-deposit dequeue
    // ONE creditFlip at end                                       // :1632
  function _crankResolveBet(address player, uint64 betId) external;  // :1640 onlySelf (reuses resolveBets, RngNotReady preserved)
  function _crankOpenBox(uint48 index, address player) external;     // :1661 onlySelf (reuses openLootBox, RngNotReady preserved)

Reward constants (REW — fixed, never gasleft()/tx.gasprice):
  CRANK_GAS_PRICE_REF = 0.5 gwei              // :1495
  CRANK_RESOLVE_BET_GAS_UNITS = 120_000       // :1501 (Phase-319 placeholder)
  CRANK_OPEN_BOX_GAS_UNITS = 120_000          // :1502 (Phase-319 placeholder)
  reward = _ethToBurnieValue(GAS_UNITS * CRANK_GAS_PRICE_REF, ...)   // :1567 / :1621
  // _ethToBurnieValue is a local zero-guarded private (OPEN-B: price-unavailable -> reward 0, never revert)

BurnieCoinflip.creditFlip(address player, uint256 amount) external;  // :849 — deferred-mint coinflip stake
  // gated onlyFlipCreditors (extended to AF_KING + GAME); credit is ILLIQUID stake, not liquid BURNIE.

RngNotReady guards preserved:
  DegenerusGameLootboxModule.sol:485 / :567  if (rngWord == 0) revert RngNotReady();
  DegenerusGameDegeneretteModule.sol:578     if (rngWord == 0) revert RngNotReady();
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Faucet round-trip ≤ 0 + one-reward-per-item + pre-RNG-word block</name>
  <files>test/fuzz/CrankFaucetResistance.t.sol</files>
  <behavior>
    - A caller who cranks their OWN resolvable bet/box receives BURNIE coinflip credit equal to gasUnits * 0.5 gwei -> BURNIE at the current level price; assert that credit, valued at the same peg, does NOT exceed the modeled gas cost of the crank tx (the reward is pegged at 0.5 gwei against a fixed gasUnits, while real submission gas is higher) — net round-trip ≤ 0.
    - The reward is paid as coinflip stake credit (creditFlip), NOT liquid BURNIE — assert the caller's liquid BURNIE balance is unchanged by the crank (credit lands in coinflip pending stake), so it cannot be immediately round-tripped.
    - Re-cranking the SAME already-resolved bet (or re-opening an already-opened box) yields zero additional creditFlip: the second crankBets reverts BatchAlreadyTaken at list[0], and the box second-pass adds zero (item already dequeued / box already opened). One reward per item.
    - A crank that targets a bet/box whose RNG word has NOT landed does not resolve it and does not reward it — the onlySelf sub-call hits the RngNotReady guard (caught by try/catch), the item is skipped, reward stays zero for that item.
  </behavior>
  <read_first>
    - test/fuzz/helpers/DeployProtocol.sol (the repaired fixture from 318-01 — afKing handle now available)
    - test/fuzz/LootboxRngLifecycle.t.sol AND test/fuzz/DegeneretteFreezeResolution.t.sol (the established setUp + vm.store/vm.load slot-helper patterns + how an existing suite drives a bet/box to the resolvable state with a VRF word)
    - contracts/DegenerusGame.sol:1543-1640 (crankBets/crankBoxes + the reward accumulation + WWXRP branch + the creditFlip call)
    - contracts/BurnieCoinflip.sol:849 (creditFlip — pending stake, not liquid balance)
  </read_first>
  <action>
    Create `test/fuzz/CrankFaucetResistance.t.sol` inheriting the repaired DeployProtocol fixture. Drive a player to place a degenerette bet and a lootbox purchase, advance to where the VRF word lands (reuse the slot-write helpers from LootboxRngLifecycle / DegeneretteFreezeResolution — lootboxRngWordByIndex=36, lootboxRngPacked=35 per the 317-08 authoritative map). Assert the four behaviors above. For the round-trip-≤-0 assertion, compute the reward in BURNIE from the contract's own constants (CRANK_RESOLVE_BET_GAS_UNITS/CRANK_OPEN_BOX_GAS_UNITS * CRANK_GAS_PRICE_REF via the level price) and assert it does not exceed the gas the crank tx would cost at any realistic gas price > 0.5 gwei — i.e. the peg is structurally sub-gas (REW-03: the reward uses fixed gasUnits constants at a 0.5 gwei reference, never gasleft()/tx.gasprice, so it cannot scale up to chase a higher submission price). Use the coinflip pending-stake getter to assert the reward is illiquid credit, not liquid BURNIE. Where a behavior is most naturally a fuzz property (e.g. round-trip over fuzzed reward amounts / player counts), use `forge` fuzz. Test-only; do not modify any contracts/*.sol.
  </action>
  <verify>
    <automated>node scripts/lib/patchForFoundry.js && FOUNDRY_DISABLE_NIGHTLY_WARNING=1 forge test --match-contract CrankFaucetResistance 2>&1 | tee /tmp/318-02.log | grep -qE "Suite result: ok" && echo SAFE01_PASS; node -e "import('./scripts/lib/patchContractAddresses.js').then(m=>m.restoreContractAddresses())"</automated>
  </verify>
  <done>CrankFaucetResistance suite is green: round-trip ≤ 0, reward is illiquid coinflip credit, one-reward-per-item enforced, pre-RNG-word crank is skipped not resolved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WWXRP currency==3 zero-reward + REW one-creditFlip-per-tx</name>
  <files>test/fuzz/CrankFaucetResistance.t.sol</files>
  <behavior>
    - Cranking a WWXRP-denominated work item (currency == 3) resolves the work but credits exactly zero reward (CRANK-04): the WWXRP branch at DegenerusGame.sol:1564 takes the zero-reward fork.
    - A crankBets / crankBoxes call over N successful items emits exactly ONE creditFlip for the caller (REW-02: never per-item), with amount == the in-memory accumulated sum of per-item rewards, not N separate credits.
    - A crank that resolves zero items (all skipped/reverting) emits NO creditFlip (reward == 0 guard at :1578 / :1632).
  </behavior>
  <read_first>
    - contracts/DegenerusGame.sol:1563-1578 (currency==3 zero branch + the single post-loop creditFlip with reward != 0 guard)
    - test/fuzz/CrankFaucetResistance.t.sol (the Task-1 setUp/helpers to extend)
    - contracts/BurnieCoinflip.sol:849 (creditFlip — count emissions via vm.expectEmit or pending-stake delta)
  </read_first>
  <action>
    Extend CrankFaucetResistance.t.sol. Set up a WWXRP (currency==3) work item and assert cranking it adds zero to the caller's coinflip credit (CRANK-04). Set up a batch of N>1 resolvable items and assert exactly ONE creditFlip fires for the batch with the summed amount (REW-02 — use vm.recordLogs / vm.expectEmit to count creditFlip emissions == 1, or assert a single pending-stake delta equal to the sum). Assert a batch where every item is skipped (e.g. all not-ready) emits zero creditFlip. Test-only.
  </action>
  <verify>
    <automated>node scripts/lib/patchForFoundry.js && FOUNDRY_DISABLE_NIGHTLY_WARNING=1 forge test --match-contract CrankFaucetResistance 2>&1 | grep -qE "Suite result: ok" && echo REW_WWXRP_PASS; node -e "import('./scripts/lib/patchContractAddresses.js').then(m=>m.restoreContractAddresses())"</automated>
  </verify>
  <done>WWXRP earns zero; exactly one creditFlip per crank tx with the summed amount; zero-success batch emits no creditFlip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crank caller → reward path | Untrusted caller drives reward issuance; the peg + illiquidity + one-reward state are the only faucet locks |
| WWXRP currency → reward branch | currency==3 must hit the zero fork or WWXRP work becomes a free faucet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-318-02-01 | Elevation of Privilege | self-crank profit (faucet) | mitigate | Test asserts round-trip ≤ 0 via the sub-gas fixed peg + illiquid coinflip credit |
| T-318-02-02 | Spoofing | WWXRP work earning reward | mitigate | Test asserts currency==3 zero-reward fork |
| T-318-02-03 | Tampering | double-reward via re-crank | mitigate | Test asserts one-reward-per-item via BatchAlreadyTaken + delete/zero state |
| T-318-02-SC | Tampering | no package installs | mitigate | No npm/pip/cargo installs in this plan |
</threat_model>

<verification>
- CrankFaucetResistance suite green under the default profile.
- Round-trip ≤ 0, WWXRP zero, one-reward-per-item, pre-RNG-word block, one-creditFlip-per-tx all asserted.
- `git diff --name-only -- contracts/` empty (no production-contract mutation).
</verification>

<success_criteria>
- SAFE-01 faucet-resistance proven: self-crank/Sybil round-trip ≤ 0; WWXRP 0; one-reward-per-item; no pre-RNG-word resolution.
- AGENT-COMMITTED test commit (test/ commits autonomously per project policy).
</success_criteria>

<output>
Create `.planning/phases/318-tst-subscription-crank-correctness-removal-proofs-tst/318-02-SUMMARY.md` when done.
</output>
