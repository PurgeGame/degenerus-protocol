---
phase: 318-tst-subscription-crank-correctness-removal-proofs-tst
plan: 06
type: execute
wave: 2
depends_on: ["318-01"]
files_modified:
  - test/fuzz/JackpotSingleCallCorrectness.t.sol
autonomous: true
requirements: [JGAS-03]

must_haves:
  truths:
    - "The daily ETH jackpot pays all 305 winners (buckets 159/95/50/1 at max scale DAILY_JACKPOT_SCALE_MAX_BPS=63_600) correctly in ONE call after the split removal — every bucket is paid, every winner credited the exact per-winner amount, none missed and none double-paid"
    - "Total ETH credited across all winners == the pool amount distributed (conservation: sum of per-winner credits + remainder/solo handling == the jackpot pool, no leak, no overpay)"
    - "The 305-winner single call completes under the block gas limit (theoretical worst case derived FIRST, then the single call shown to fit; full calibration is Phase 319)"
    - "The old two-call split path is behaviorally gone: no STAGE_JACKPOT_ETH_RESUME stage is entered, no resumeEthPool carry is read/written, and the symbols are grep-clean"
  artifacts:
    - path: "test/fuzz/JackpotSingleCallCorrectness.t.sol"
      provides: "JGAS-03 single-call 305-winner correctness + conservation + gas-fits + split-gone proofs"
      contains: "DAILY_ETH_MAX_WINNERS"
  key_links:
    - from: "payDailyJackpot single call"
      to: "_processDailyEth -> _processBucket per bucket"
      via: "all 4 buckets in one call at the 305 ceiling"
      pattern: "_processDailyEth"
    - from: "per-winner payout"
      to: "claimable (_addClaimableEth)"
      via: "exact per-winner amount, conservation holds"
      pattern: "_addClaimableEth"
---

<objective>
Prove JGAS-03: after the JGAS-02 two-call-split removal, the daily ETH jackpot pays all 305 winners (buckets 159/95/50/1 at max scale) correctly in ONE call — every bucket paid, exact per-winner amounts, none missed/double-paid, total credited == pool, and the single call fits under the block gas limit. The old split path (resumeEthPool / SPLIT_CALL1/2 / STAGE_JACKPOT_ETH_RESUME) is behaviorally gone — no resume stage entered. Gas analysis derives the theoretical worst case FIRST, then shows the single call fits (full numbers calibrated at Phase 319).

Purpose: JGAS removed the gas-split MECHANISM at the SAME 305-winner ceiling — no winner-count/EV change. This plan is the correctness floor: the single call must pay exactly what the two-call split paid, with nothing stranded by the dropped resumeEthPool carry.

Output: `test/fuzz/JackpotSingleCallCorrectness.t.sol` — the 305-winner single-call correctness + conservation + gas-fits + split-behaviorally-gone proofs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# The single-call jackpot surface + the split-removal footprint (live file:line)
@.planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-05-SUMMARY.md

<interfaces>
<!-- The single-call daily-ETH jackpot surface under test (live JackpotModule). Use directly. -->

DegenerusGameJackpotModule.sol:
  uint16 DAILY_ETH_MAX_WINNERS = 305;                 // :210  (159+95+50+1, preserved ceiling)
  uint32 DAILY_JACKPOT_SCALE_MAX_BPS = 63_600;        // :230  (6.36x max scale -> bucket counts 159/95/50/1)
  function payDailyJackpot(uint256 ... ) external;    // :320  single-call daily-ETH path (no split, no resume)
  function _processDailyEth(...) ... ;                // :1088 single call, full DAILY_ETH_MAX_WINNERS ceiling
  function _processBucket(lvl, traitId, traitIdx, count, share, entropy, isFinalDay, isSolo) ...; // :1154 per-bucket
  function _handleSoloBucketWinner(...);              // :1305 the 1-winner solo bucket
  function _addClaimableEth(address beneficiary, uint256 weiAmount) returns (uint256 claimableDelta); // :738 (2-arg, always claimable)
  event JackpotEthWin(address indexed winner, uint8 indexed level, uint16 indexed traitId, uint256 amount, uint256 ticketIndex); // :69 (rebuyLevel/rebuyTickets dropped)

DegenerusGameAdvanceModule.sol:
  // STAGE_JACKPOT_ETH_RESUME deleted; the resume-check block (if resumeEthPool != 0) deleted;
  // the daily ETH jackpot now completes in ONE advanceGame stage / one call.

Split-removal grep-clean set (ZERO matches outside contracts/test + mocks):
  resumeEthPool, SPLIT_CALL1, SPLIT_CALL2, SPLIT_NONE, _resumeDailyEth, STAGE_JACKPOT_ETH_RESUME, call1Bucket, splitMode

JackpotBucketLib.bucketCountsForPoolCap(...) -> uint16[4]   // bucket counts; at max scale == [159,95,50,1]
foundry.toml: block_gas_limit = 30_000_000_000 (test config); the REAL block gas limit for the JGAS-03
  "under block limit" assertion is the mainnet ~30M — derive the worst case FIRST, then assert the single
  call's measured gas < 30_000_000 (full peg calibration is Phase 319 / JGAS-04).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 305-winner single-call correctness + conservation</name>
  <files>test/fuzz/JackpotSingleCallCorrectness.t.sol</files>
  <behavior>
    - Drive the daily ETH jackpot at MAX scale (pool large enough that bucketCountsForPoolCap returns [159,95,50,1] = 305 winners) and assert the single payDailyJackpot call pays all four buckets: 159 + 95 + 50 winners in the normal buckets + 1 solo-bucket winner, all in ONE call (no second stage / resume needed).
    - Every winner is credited the exact per-winner amount for its bucket (per-bucket share / count), and the solo winner the solo amount; assert no winner is missed (count of JackpotEthWin emissions == 305) and none double-paid (each winner address credited once per its bucket assignment).
    - Conservation: the sum of all per-winner claimable credits (+ any remainder/solo handling) == the jackpot pool distributed for the day — no ETH leaked, no overpay. Assert via claimable deltas summed across winners == the pool input.
  </behavior>
  <read_first>
    - test/fuzz/helpers/DeployProtocol.sol (repaired fixture from 318-01)
    - test/fuzz/JackpotCombinedPool.t.sol (the established pattern for driving a daily jackpot payout, seeding winners, and reading per-winner claimable)
    - contracts/modules/DegenerusGameJackpotModule.sol:1088-1370 (_processDailyEth + _processBucket + _handleSoloBucketWinner + the per-winner _addClaimableEth + JackpotEthWin emit)
    - contracts/modules/DegenerusGameJackpotModule.sol:265-290 (the bucketCountsForPoolCap call producing 159/95/50/1 at max scale)
  </read_first>
  <action>
    Create `test/fuzz/JackpotSingleCallCorrectness.t.sol` on the repaired fixture. Seed enough qualifying ticket/winner state and a large-enough pool that the daily-ETH bucket counts hit the max-scale [159,95,50,1]=305. Run the single payDailyJackpot call. Capture JackpotEthWin logs (vm.recordLogs) and assert: 305 winner emissions total, each bucket's winner count matches (159/95/50/1), each per-winner amount equals the bucket share / count (and the solo amount for the 1-winner bucket), no winner address double-credited within its bucket. Sum the claimable deltas across all winners and assert conservation == the distributed pool (account for the documented remainder/solo handling). Use fuzz over the pool size / qualifying-winner distribution where the 305-ceiling is still reachable. Test-only; do not modify contracts/*.sol.
  </action>
  <verify>
    <automated>node scripts/lib/patchForFoundry.js && FOUNDRY_DISABLE_NIGHTLY_WARNING=1 forge test --match-contract JackpotSingleCallCorrectness 2>&1 | grep -qE "Suite result: ok" && echo JGAS03_CORRECTNESS_PASS; node -e "import('./scripts/lib/patchContractAddresses.js').then(m=>m.restoreContractAddresses())"</automated>
  </verify>
  <done>All 305 winners paid in one call across the 4 buckets with exact per-winner amounts; none missed/double-paid; total credited == pool (conservation holds).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Single-call fits the block gas limit (worst-case-first) + split behaviorally gone</name>
  <files>test/fuzz/JackpotSingleCallCorrectness.t.sol</files>
  <behavior>
    - Worst-case-FIRST: the theoretical worst case is the 305-winner max-scale single call (all 4 buckets, 159/95/50/1). Measure that single call's gas (forge gas accounting / gasleft delta around the call) and assert it is < 30_000_000 (the mainnet block gas limit) — i.e. the single call fits with margin. (Full peg calibration + the precise margin attribution to the removed per-winner autoRebuyState SLOAD is Phase 319 / JGAS-04; this plan proves it FITS.)
    - The split path is behaviorally gone: running the daily ETH jackpot at the 305 ceiling completes in ONE call — no STAGE_JACKPOT_ETH_RESUME stage is entered and no resumeEthPool carry is read or written (assert via state: no resume-stage transition observed, the jackpot fully distributed after the single call with no pending second-call remainder).
    - Grep-clean: the split symbols (resumeEthPool, SPLIT_CALL1/2, SPLIT_NONE, _resumeDailyEth, STAGE_JACKPOT_ETH_RESUME, call1Bucket, splitMode) return ZERO matches outside contracts/test + mocks (structural attestation via vm.readFile / ffi grep, comment-filtered).
  </behavior>
  <read_first>
    - .planning/phases/317-impl-.../317-05-SUMMARY.md (JGAS-02: the split fully removed, single-call _processDailyEth, the J5 freeze-SAFE verdict, the 305 ceiling preserved)
    - contracts/modules/DegenerusGameAdvanceModule.sol (STAGE_JACKPOT_ETH_RESUME + resume-check deleted — confirm no resume stage exists)
    - foundry.toml (block_gas_limit is 30e9 for tests; the JGAS-03 assertion uses the mainnet 30_000_000 limit, not the test config)
    - feedback_gas_worst_case (derive theoretical worst case FIRST, then measure)
  </read_first>
  <action>
    Extend JackpotSingleCallCorrectness.t.sol. Around the max-scale 305-winner payDailyJackpot call, capture gasleft() before/after (or use forge's gas snapshot) and assert the consumed gas < 30_000_000. Document in the test (and SUMMARY) the worst-case derivation: 305 winners = 159+95+50+1 at DAILY_JACKPOT_SCALE_MAX_BPS, all 4 buckets, one call — this IS the worst case for the daily-ETH path. Assert the jackpot is fully distributed after the single call (no pending remainder, no resume stage transition). Add a structural-attestation test asserting the split-symbol set is grep-clean outside test/mocks (comment-filtered, following the established VrfWireOneShot-style attestation). Note clearly that the empirical worst-case calibration + the margin attribution to the removed autoRebuyState SLOAD is Phase 319 (JGAS-04); this plan's bar is "fits under the limit." Test-only.
  </action>
  <verify>
    <automated>node scripts/lib/patchForFoundry.js && FOUNDRY_DISABLE_NIGHTLY_WARNING=1 forge test --match-contract JackpotSingleCallCorrectness 2>&1 | grep -qE "Suite result: ok" && echo JGAS03_GASFIT_SPLITGONE_PASS; node -e "import('./scripts/lib/patchContractAddresses.js').then(m=>m.restoreContractAddresses())"</automated>
  </verify>
  <done>The worst-case 305-winner single call measures < 30M gas (fits); no resume stage entered; the split symbols are grep-clean outside test/mocks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| jackpot pool → per-winner credits | Conservation must hold: no winner stranded by the dropped resumeEthPool carry, no over/under-pay |
| single-call gas → block gas limit | The 305-winner single call must fit; a too-heavy call would brick the daily jackpot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-318-06-01 | Tampering | a winner stranded by the dropped resumeEthPool carry | mitigate | Test asserts 305 winners paid in one call + conservation (sum credits == pool) |
| T-318-06-02 | Tampering | a bucket under/over-paid at the single-call ceiling | mitigate | Test asserts exact per-winner amounts per bucket, none double-paid |
| T-318-06-03 | Denial of Service | single call exceeding block gas limit (jackpot brick) | mitigate | Worst-case-first 305-winner gas measured < 30M; empirical calibration deferred to 319/JGAS-04 |
| T-318-06-04 | Tampering | a lingering resume stage / resumeEthPool path | mitigate | Test asserts no resume stage entered + grep-clean split symbols |
| T-318-06-SC | Tampering | no package installs | mitigate | No npm/pip/cargo installs in this plan |
</threat_model>

<verification>
- JackpotSingleCallCorrectness suite green under the default profile.
- 305-winner single-call correctness, conservation, gas-fits-under-30M, no-resume-stage, grep-clean split symbols all asserted.
- `git diff --name-only -- contracts/` empty (no production-contract mutation).
</verification>

<success_criteria>
- JGAS-03 proven: daily ETH jackpot pays all 305 winners correctly in ONE call (every bucket, exact amounts, none missed/double-paid, total == pool), single call fits the block limit, split behaviorally gone + grep-clean.
- AGENT-COMMITTED test commit.
</success_criteria>

<output>
Create `.planning/phases/318-tst-subscription-crank-correctness-removal-proofs-tst/318-06-SUMMARY.md` when done.
</output>
