---
phase: 318-tst-subscription-crank-correctness-removal-proofs-tst
plan: 05
type: execute
wave: 2
depends_on: ["318-01"]
files_modified:
  - test/fuzz/RngFreezeAndRemovalProofs.t.sol
autonomous: true
requirements: [SAFE-04]

must_haves:
  truths:
    - "RNG-freeze intact (SAFE-04): a crank-driven resolution stays post-unlock behind the RngNotReady guard — no resolve succeeds before the RNG word lands; the placement guard is untouched (placement still gated as before)"
    - "The ETH-auto-rebuy removal RETIRED freeze obligations rather than weakening them — the jackpot ETH credit path no longer consumes a VRF word, and no remaining VRF-window player-mutable input feeds the removed path"
    - "REMOVE grep-clean: the legacy afKing-mode + free-ETH-auto-rebuy symbol set returns ZERO matches outside contracts/test+mocks (afKing/AFKING_/setAutoRebuy/autoRebuyState/AutoRebuyState/_processAutoRebuy/_calcAutoRebuy/settleFlipModeChange/resumeEthPool/SPLIT_CALL1/SPLIT_CALL2/STAGE_JACKPOT_ETH_RESUME)"
    - "REMOVE behavioral: ETH jackpot winnings ALWAYS land in claimable (no ticket-conversion / auto-rebuy interception path); BURNIE flip recycle = flat 75bps applied unconditionally (no deity scaling, no under/over-credit)"
    - "UNMODIFIED invariants: the BURNIE win/loss RNG path (processCoinflipPayouts, rngWord & 1) and KNOWN_ISSUES are byte-unchanged"
  artifacts:
    - path: "test/fuzz/RngFreezeAndRemovalProofs.t.sol"
      provides: "SAFE-04 freeze-intact + the RM grep-clean + behavioral removal proofs + UNMODIFIED-invariant assertions"
      contains: "RngNotReady"
  key_links:
    - from: "crank resolution"
      to: "RngNotReady / lootboxRngWordByIndex guard"
      via: "resolution stays post-unlock"
      pattern: "RngNotReady"
    - from: "ETH jackpot winnings"
      to: "claimable (always)"
      via: "_addClaimableEth (no auto-rebuy branch)"
      pattern: "claimable"
---

<objective>
Prove SAFE-04: the RNG-freeze invariant is intact under the new permissionless crank — resolution stays post-unlock behind the RngNotReady guard, the placement guard is untouched, and the ETH-auto-rebuy removal RETIRED freeze obligations (one fewer VRF consumer, three fewer player-mutable in-window inputs) rather than weakening any. Plus the REMOVE proofs: the legacy afKing / ETH-auto-rebuy / jackpot-split symbols are grep-clean AND behaviorally gone (ETH always lands in claimable, BURNIE recycle is flat 75bps unconditional), and the BURNIE win/loss RNG path + KNOWN_ISSUES are byte-unmodified.

Purpose: SAFE-04 is the security hard-floor of this milestone (the v45 north-star: every VRF-interacting variable frozen across the rng window). The crank relaxes WHO can resolve, not WHEN — this plan proves the freeze guard still blocks pre-word resolution, and that the removal made the freeze surface strictly smaller.

Output: `test/fuzz/RngFreezeAndRemovalProofs.t.sol` — freeze-intact behavioral proofs + the grep-clean/behavioral removal proofs + the UNMODIFIED-invariant assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# The removal surface + freeze re-confirmation (live file:line)
@.planning/phases/317-impl-batched-add-remove-contract-diff-paired-keeper-rework-i/317-05-SUMMARY.md

<interfaces>
<!-- The freeze guards + removal surface under test (live). Use directly. -->

RngNotReady / freeze guards (must stay post-unlock):
  DegenerusGameLootboxModule.sol:485 / :567   if (rngWord == 0) revert RngNotReady();   (box resolution)
  DegenerusGameDegeneretteModule.sol:578       if (rngWord == 0) revert RngNotReady();   (bet resolution)
  DegenerusGameDegeneretteModule.sol:452       if (lootboxRngWordByIndex[index] != 0) revert RngNotReady();  (placement guard — untouched)
  DegenerusGame.sol crankBoxes :1603           if (lootboxRngWordByIndex[index] == 0) return;  (cursor orphan/not-ready gate)

ETH-always-to-claimable (RM-02): _addClaimableEth is now 2-arg (no entropy, no auto-rebuy branch):
  DegenerusGameJackpotModule.sol:738  function _addClaimableEth(address beneficiary, uint256 weiAmount) ... returns (uint256 claimableDelta)
  // every daily-ETH winner is credited to claimable; there is NO ticket-conversion / auto-rebuy interception.

BURNIE flat 75bps recycle (RM-03):
  BurnieCoinflip.sol:130  RECYCLE_BONUS_BPS = 75
  BurnieCoinflip.sol:1006 bonus = (amount * RECYCLE_BONUS_BPS) / BPS_DENOMINATOR;   (no deity tier, no scaling)

UNMODIFIED (byte-identical) invariants:
  BurnieCoinflip.sol:756  function processCoinflipPayouts(...)   (win/loss RNG path)
  BurnieCoinflip.sol:788  bool win = (rngWord & 1) == 1;
  KNOWN_ISSUES.md / KNOWN-ISSUES.md — byte-unmodified across the milestone

RM grep-clean symbol set (ZERO matches outside contracts/test + contracts/mocks):
  afKing, AFKING_, setAutoRebuy, autoRebuyState, AutoRebuyState, _processAutoRebuy, _calcAutoRebuy,
  settleFlipModeChange, _afKingRecyclingBonus, _afKingDeityBonusHalfBpsWithLevel,
  resumeEthPool, SPLIT_CALL1, SPLIT_CALL2, SPLIT_NONE, _resumeDailyEth, STAGE_JACKPOT_ETH_RESUME, call1Bucket
  (NOTE: hasAnyLazyPass / _hasAnyLazyPass are KEPT (RM-04/PROTO-01) — they are NOT in the grep-clean kill set;
   exclude "afKing" matches that are the kept hasAnyLazyPass identifier or AfKing.sol the keeper file.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RNG-freeze intact under the crank — resolution post-unlock, placement guard untouched</name>
  <files>test/fuzz/RngFreezeAndRemovalProofs.t.sol</files>
  <behavior>
    - A crank-driven bet resolution (crankBets -> _crankResolveBet) before the RNG word lands does NOT succeed — the onlySelf sub-call hits RngNotReady and is skipped (caught by try/catch); the bet stays unresolved until its word lands.
    - A crank-driven box open (crankBoxes -> _crankOpenBox) before the word lands is skipped at the cursor orphan gate (lootboxRngWordByIndex == 0 -> return) and the openLootBox RngNotReady guard; no pre-word open.
    - The placement guard is untouched: a degenerette PLACEMENT for an index that already has a word still reverts RngNotReady (DegeneretteModule:452) — the crank relaxed resolve, not placement.
    - After the word lands, the same crank resolves/opens successfully (proving the relaxation is WHO-can-call, not WHEN — the freeze window is unchanged).
  </behavior>
  <read_first>
    - test/fuzz/helpers/DeployProtocol.sol (repaired fixture from 318-01)
    - test/fuzz/DegeneretteFreezeResolution.t.sol + test/fuzz/RngLockDeterminism.t.sol (the established freeze-window test patterns: how to set/clear an RNG word via slot writes and assert RngNotReady before vs success after)
    - contracts/DegenerusGame.sol:1592-1680 (crankBoxes/_crankOpenBox/_crankResolveBet reuse the RngNotReady-guarded resolution machinery)
    - contracts/modules/DegenerusGameDegeneretteModule.sol:452 (placement guard) + :578 (resolution guard)
  </read_first>
  <action>
    Create `test/fuzz/RngFreezeAndRemovalProofs.t.sol` on the repaired fixture. Drive a bet + box to the pre-word state (RNG word slot == 0 per the 317-08 map: lootboxRngWordByIndex=36). Assert a crank attempt does NOT resolve/open (the item is skipped via the RngNotReady guard inside the onlySelf sub-call). Set the word (slot write) and assert the same crank now resolves/opens. Assert the placement guard at DegeneretteModule:452 still reverts RngNotReady for an index that has a word (placement untouched). Use fuzz over the word-set timing where natural. Test-only; do not modify contracts/*.sol.
  </action>
  <verify>
    <automated>node scripts/lib/patchForFoundry.js && FOUNDRY_DISABLE_NIGHTLY_WARNING=1 forge test --match-contract RngFreezeAndRemovalProofs 2>&1 | grep -qE "Suite result: ok" && echo SAFE04_FREEZE_PASS; node -e "import('./scripts/lib/patchContractAddresses.js').then(m=>m.restoreContractAddresses())"</automated>
  </verify>
  <done>No pre-word crank resolution; post-word crank succeeds; placement guard untouched — the freeze window is unchanged under the crank.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: REMOVE behavioral — ETH always to claimable + flat 75bps recycle + freeze obligation retirement</name>
  <files>test/fuzz/RngFreezeAndRemovalProofs.t.sol</files>
  <behavior>
    - Every daily-ETH jackpot winner's payout lands in claimable (claimableWinningsOf delta == payout) — there is NO auto-rebuy / ticket-conversion interception of winnings (RM-02 behavioral).
    - The jackpot ETH credit path consumes NO VRF word for the credit step: removing the auto-rebuy roll retired that VRF consumer (assert the credit is deterministic given the bucket assignment — _addClaimableEth is the 2-arg deterministic form, no entropy threaded into the credit).
    - BURNIE flip recycle bonus = flat 75bps applied unconditionally across player tiers: a deity-pass holder and a normal player both get exactly (amount * 75 / 10_000) recycle — no deity scaling, no under/over-credit (RM-03 behavioral).
  </behavior>
  <read_first>
    - .planning/phases/317-impl-.../317-05-SUMMARY.md (RM-02 ETH-always-to-claimable + the freeze-invariant re-confirmation: no VRF word in the credit path; the J5 verdict)
    - contracts/modules/DegenerusGameJackpotModule.sol:738 (_addClaimableEth 2-arg) + :1270-1370 (per-winner credit emitting JackpotEthWin)
    - contracts/BurnieCoinflip.sol:1006 (flat RECYCLE_BONUS_BPS recycle, no tier branch)
    - test/fuzz/JackpotCombinedPool.t.sol or test/fuzz/BafRebuyReconciliation.t.sol (existing patterns for driving a jackpot payout + reading claimable)
  </read_first>
  <action>
    Extend RngFreezeAndRemovalProofs.t.sol. Drive a daily-ETH jackpot payout and assert each winner's claimable increases by exactly the payout (no rebuy interception). Assert the credit path is deterministic (re-running with the same bucket assignment yields the same claimable, no entropy dependence in the credit). For BURNIE recycle: exercise the flip recycle for a deity-pass holder and a normal player and assert both receive exactly amount*75/10_000 (flat, unconditional). Test-only.
  </action>
  <verify>
    <automated>node scripts/lib/patchForFoundry.js && FOUNDRY_DISABLE_NIGHTLY_WARNING=1 forge test --match-contract RngFreezeAndRemovalProofs 2>&1 | grep -qE "Suite result: ok" && echo REMOVE_BEHAVIORAL_PASS; node -e "import('./scripts/lib/patchContractAddresses.js').then(m=>m.restoreContractAddresses())"</automated>
  </verify>
  <done>ETH winnings always land in claimable; credit path consumes no VRF word; BURNIE recycle is flat 75bps unconditional (no deity scaling, no under/over-credit).</done>
</task>

<task type="auto">
  <name>Task 3: REMOVE grep-clean + UNMODIFIED-invariant attestation</name>
  <files>test/fuzz/RngFreezeAndRemovalProofs.t.sol</files>
  <read_first>
    - .planning/phases/317-impl-.../317-VERIFICATION.md (the 24-symbol legacy grep set + the 7-symbol JGAS set + the BURNIE win/loss path UNMODIFIED spot-check)
    - contracts/BurnieCoinflip.sol:756 + :788 (processCoinflipPayouts + rngWord & 1 — the byte-unmodified win/loss path)
    - foundry.toml fs_permissions (read access to ./contracts is granted — vm.readFile works on contracts/*.sol)
  </read_first>
  <action>
    Add a structural-attestation test to RngFreezeAndRemovalProofs.t.sol that, using vm.readFile over the production contract sources (fs_permissions grants read on ./contracts) OR a forge ffi grep helper following the established structural-attestation pattern (e.g. VrfWireOneShot), asserts ZERO non-comment matches outside contracts/test + contracts/mocks for the legacy kill set: setAutoRebuy, autoRebuyState, AutoRebuyState, _processAutoRebuy, _calcAutoRebuy, settleFlipModeChange, _afKingRecyclingBonus, _afKingDeityBonusHalfBpsWithLevel, resumeEthPool, SPLIT_CALL1, SPLIT_CALL2, SPLIT_NONE, _resumeDailyEth, STAGE_JACKPOT_ETH_RESUME, call1Bucket, AFKING_ . Explicitly EXCLUDE the KEPT identifiers (hasAnyLazyPass / _hasAnyLazyPass per RM-04) and the keeper file AfKing.sol from the "afKing" search so the kept symbols do not produce false positives. Then assert the UNMODIFIED invariants: processCoinflipPayouts + the `(rngWord & 1) == 1` win/loss line are present byte-for-byte in BurnieCoinflip.sol (read the exact line), and the KNOWN-ISSUES doc is unmodified (assert its content/hash matches the milestone-baseline — read the file and compare against the recorded baseline). Filter comments using `grep -v '^[[:space:]]*//'` style filtering so header prose does not self-invalidate the gate. Test-only; do not modify contracts/*.sol.
  </action>
  <verify>
    <automated>node scripts/lib/patchForFoundry.js && FOUNDRY_DISABLE_NIGHTLY_WARNING=1 forge test --match-contract RngFreezeAndRemovalProofs 2>&1 | grep -qE "Suite result: ok" && echo REMOVE_GREPCLEAN_PASS; node -e "import('./scripts/lib/patchContractAddresses.js').then(m=>m.restoreContractAddresses())"</automated>
  </verify>
  <done>Legacy kill set is grep-clean outside test/mocks (kept hasAnyLazyPass/AfKing.sol excluded); processCoinflipPayouts + rngWord & 1 byte-unmodified; KNOWN-ISSUES unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crank caller → RNG-window | A relaxed resolver must not resolve before the word lands; the freeze guard is the floor |
| removed ETH-auto-rebuy path → winnings | Winnings must always reach claimable; no removed branch may strand or convert them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-318-05-01 | Information Disclosure | pre-word resolution leaking/biasing RNG outcome | mitigate | Test asserts RngNotReady blocks pre-word crank resolution; placement guard untouched |
| T-318-05-02 | Tampering | a removed branch stranding/converting ETH winnings | mitigate | Test asserts ETH always lands in claimable, deterministic credit, no VRF word |
| T-318-05-03 | Tampering | BURNIE recycle under/over-credit after the deity-tier collapse | mitigate | Test asserts flat 75bps unconditional for all tiers |
| T-318-05-04 | Repudiation | KNOWN_ISSUES / win-loss RNG path silently modified | mitigate | Structural attestation asserts byte-unmodified |
| T-318-05-SC | Tampering | no package installs | mitigate | No npm/pip/cargo installs in this plan |
</threat_model>

<verification>
- RngFreezeAndRemovalProofs suite green under the default profile.
- Freeze-intact (post-unlock resolve, placement untouched), ETH-always-to-claimable, flat 75bps, grep-clean kill set, byte-unmodified win/loss path + KNOWN_ISSUES all asserted.
- `git diff --name-only -- contracts/` empty (no production-contract mutation).
</verification>

<success_criteria>
- SAFE-04 RNG-freeze intact + freeze obligations retired by the removal proven.
- REMOVE grep-clean + behavioral (ETH→claimable, flat 75bps) proven; win/loss RNG path + KNOWN_ISSUES unmodified.
- AGENT-COMMITTED test commit(s).
</success_criteria>

<output>
Create `.planning/phases/318-tst-subscription-crank-correctness-removal-proofs-tst/318-05-SUMMARY.md` when done.
</output>
