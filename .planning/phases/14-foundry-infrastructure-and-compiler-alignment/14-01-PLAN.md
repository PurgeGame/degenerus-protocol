---
phase: 14-foundry-infrastructure-and-compiler-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - foundry.toml
  - test/fuzz/BurnieCoinInvariants.t.sol
  - test/fuzz/PriceLookupInvariants.t.sol
  - test/fuzz/ShareMathInvariants.t.sol
autonomous: true
requirements: [INFRA-01]

must_haves:
  truths:
    - "forge build compiles all production contracts and existing fuzz tests without errors or warnings using solc 0.8.34"
    - "Existing 3 fuzz invariant tests still pass under solc 0.8.34"
  artifacts:
    - path: "foundry.toml"
      provides: "Foundry config with solc 0.8.34 binary path, auto_detect_solc=false, invariant config"
      contains: "auto_detect_solc = false"
    - path: "test/fuzz/BurnieCoinInvariants.t.sol"
      provides: "Caret pragma for 0.8.34 compat"
      contains: "^0.8.26"
    - path: "test/fuzz/PriceLookupInvariants.t.sol"
      provides: "Caret pragma for 0.8.34 compat"
      contains: "^0.8.26"
    - path: "test/fuzz/ShareMathInvariants.t.sol"
      provides: "Caret pragma for 0.8.34 compat"
      contains: "^0.8.26"
  key_links:
    - from: "foundry.toml"
      to: "/home/zak/.cache/hardhat-nodejs/compilers-v2/linux-amd64/solc-linux-amd64-v0.8.34+commit.80d5c536"
      via: "solc binary path"
      pattern: "solc.*solc-linux-amd64-v0\\.8\\.34"
---

<objective>
Align Foundry's compiler with the production solc 0.8.34 and confirm all contracts compile cleanly.

Purpose: Nothing else in Phase 14 works until forge build succeeds with the correct compiler version. This is the foundation for all subsequent Foundry-based testing infrastructure.
Output: Updated foundry.toml, fixed pragma in 3 fuzz test files, clean forge build.
</objective>

<execution_context>
@/home/zak/.claude/agents/gsd-executor.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-foundry-infrastructure-and-compiler-alignment/14-RESEARCH.md
@foundry.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update foundry.toml for solc 0.8.34 and enhanced invariant config</name>
  <files>foundry.toml</files>
  <action>
Replace the current foundry.toml with the following changes:

1. Replace `solc_version = "0.8.26"` with `solc = "/home/zak/.cache/hardhat-nodejs/compilers-v2/linux-amd64/solc-linux-amd64-v0.8.34+commit.80d5c536"` (explicit binary path, NOT solc_version)
2. Add `auto_detect_solc = false` to prevent Foundry from trying to download
3. Add forge-std remapping: `"forge-std/=lib/forge-std/src/"` to the existing remappings array
4. Expand the `[invariant]` section with: `depth = 128`, `shrink_run_limit = 5000`, `show_metrics = true`, `dictionary_weight = 80`, `include_storage = true`, `include_push_bytes = true`

Keep all other settings unchanged: `src = "contracts"`, `test = "test/fuzz"`, `out = "forge-out"`, `via_ir = true`, `optimizer = true`, `optimizer_runs = 2`, `evm_version = "paris"`, `libs`, `[fuzz]` section.
  </action>
  <verify>
    <automated>forge build --force 2>&1 | tail -5</automated>
  </verify>
  <done>forge build completes with zero errors and zero warnings using solc 0.8.34</done>
</task>

<task type="auto">
  <name>Task 2: Fix fuzz test pragmas and verify existing tests pass</name>
  <files>test/fuzz/BurnieCoinInvariants.t.sol, test/fuzz/PriceLookupInvariants.t.sol, test/fuzz/ShareMathInvariants.t.sol</files>
  <action>
In each of the 3 files, change line 2 from `pragma solidity 0.8.26;` to `pragma solidity ^0.8.26;` (add caret prefix). This allows compilation under solc 0.8.34 while maintaining the minimum version requirement.

Do NOT change any other code in these files.

After the pragma fix, run the existing forge tests to confirm they still pass under the new compiler.
  </action>
  <verify>
    <automated>forge test --match-path "test/fuzz/**" -vvv 2>&1 | tail -20</automated>
  </verify>
  <done>All 3 existing fuzz invariant tests pass with solc 0.8.34, zero regressions</done>
</task>

</tasks>

<verification>
1. `forge build --force` exits 0 with no errors or warnings
2. `forge test --match-path "test/fuzz/**"` exits 0 with all tests passing
3. `grep "solc.*0.8.34" foundry.toml` returns the binary path line
4. `grep "pragma solidity" test/fuzz/*.t.sol` shows `^0.8.26` in all 3 files
</verification>

<success_criteria>
- forge build compiles all 22 production contracts + 5 mocks + 3 fuzz tests with solc 0.8.34
- All 3 existing fuzz invariant tests pass
- No Hardhat tests broken (they use their own compiler)
</success_criteria>

<output>
After completion, create `.planning/phases/14-foundry-infrastructure-and-compiler-alignment/14-01-SUMMARY.md`
</output>
