---
phase: 14-foundry-infrastructure-and-compiler-alignment
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - scripts/lib/patchForFoundry.js
autonomous: true
requirements: [INFRA-02]

must_haves:
  truths:
    - "patchForFoundry.js predicts correct Foundry deployer nonces and patches ContractAddresses.sol with non-zero addresses"
    - "Predicted addresses match CREATE address math for DEFAULT_TEST_CONTRACT deployer"
  artifacts:
    - path: "scripts/lib/patchForFoundry.js"
      provides: "Foundry address prediction and ContractAddresses patching"
      min_lines: 40
  key_links:
    - from: "scripts/lib/patchForFoundry.js"
      to: "scripts/lib/predictAddresses.js"
      via: "import predictAddresses, computeDeployDayBoundary"
      pattern: "import.*predictAddresses"
    - from: "scripts/lib/patchForFoundry.js"
      to: "scripts/lib/patchContractAddresses.js"
      via: "import patchContractAddresses"
      pattern: "import.*patchContractAddresses"
    - from: "scripts/lib/patchForFoundry.js"
      to: "contracts/ContractAddresses.sol"
      via: "patches compile-time constants"
      pattern: "patchContractAddresses\\("
---

<objective>
Create the address prediction script that patches ContractAddresses.sol for Foundry's test EVM deployer identity.

Purpose: ContractAddresses.sol uses compile-time constants (all address(0) in source). Without patching with Foundry-correct addresses, every cross-contract call in the deployed protocol hits address(0) and silently fails. This script is the bridge between Foundry's deterministic deployer and the production address constants.
Output: `scripts/lib/patchForFoundry.js` that correctly predicts all 22 protocol + 5 mock addresses for Foundry's DEFAULT_TEST_CONTRACT deployer.
</objective>

<execution_context>
@/home/zak/.claude/agents/gsd-executor.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-foundry-infrastructure-and-compiler-alignment/14-RESEARCH.md
@scripts/lib/predictAddresses.js
@scripts/lib/patchContractAddresses.js

<interfaces>
From scripts/lib/predictAddresses.js:
```javascript
export function predictAddresses(deployerAddress, startingNonce) -> Map<string, string>
export function computeDeployDayBoundary(timestamp) -> number
export const DEPLOY_ORDER: string[]  // 22 entries
export const KEY_TO_CONTRACT: Record<string, string>
```

From scripts/lib/patchContractAddresses.js:
```javascript
export function patchContractAddresses(addressMap, external, deployDayBoundary, vrfKeyHash, contractFilePath?)
export function restoreContractAddresses(contractFilePath?)
```

From forge-std StdConstants.sol:
```
DEFAULT_SENDER = 0x1804c8AB1F12E6bbf3894d4083f33e07309d1f38
DEFAULT_TEST_CONTRACT = 0x5615dEB798BB3E4dFa0139dFa1b3D433Cc23b72f
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create patchForFoundry.js with address prediction and patching</name>
  <files>scripts/lib/patchForFoundry.js</files>
  <action>
Create `scripts/lib/patchForFoundry.js` that:

1. Imports `ethers` from "ethers", `predictAddresses` and `computeDeployDayBoundary` from "./predictAddresses.js", `patchContractAddresses` and `restoreContractAddresses` from "./patchContractAddresses.js"

2. Defines constants:
   - `FOUNDRY_TEST_CONTRACT = "0x5615dEB798BB3E4dFa0139dFa1b3D433Cc23b72f"` (from forge-std StdConstants)
   - `MOCK_COUNT = 5` (5 mocks deployed before protocol contracts)
   - `PROTOCOL_START_NONCE = MOCK_COUNT + 1` (= 6)

3. Exports `patchForFoundry()` function that:
   a. Computes mock addresses at nonces 1-5 using `ethers.getCreateAddress({from: FOUNDRY_TEST_CONTRACT, nonce: 1..5})`
   b. Calls `predictAddresses(FOUNDRY_TEST_CONTRACT, PROTOCOL_START_NONCE)` for 22 protocol addresses
   c. Builds `external` object: `{ STETH_TOKEN: mockStETH, LINK_TOKEN: mockLINK, VRF_COORDINATOR: mockVRF, WXRP: mockWXRP, CREATOR: FOUNDRY_TEST_CONTRACT }`
   d. Computes `deployTimestamp = Math.floor(Date.now() / 1000)` and `dayBoundary = computeDeployDayBoundary(deployTimestamp)`
   e. Sets `vrfKeyHash = "0xabababababababababababababababababababababababababababababababab"` (matches Hardhat fixture)
   f. Calls `patchContractAddresses(predicted, external, dayBoundary, vrfKeyHash)`
   g. Returns `{ predicted, external, dayBoundary, deployTimestamp, FOUNDRY_TEST_CONTRACT, PROTOCOL_START_NONCE }`

4. Re-exports `restoreContractAddresses` for convenience

5. CLI entry point: if `process.argv[1]` ends with "patchForFoundry.js", call `patchForFoundry()` and log deployer, protocol start nonce, day boundary, and first/last predicted addresses

After creating the file, run it to verify it patches ContractAddresses.sol successfully, then restore.
  </action>
  <verify>
    <automated>node scripts/lib/patchForFoundry.js && grep -c "address(0)" contracts/ContractAddresses.sol | grep "^0$" && node -e "import('./scripts/lib/patchContractAddresses.js').then(m => m.restoreContractAddresses())" && echo "PASS: patch and restore cycle works"</automated>
  </verify>
  <done>patchForFoundry.js patches all 22 protocol + 5 external addresses in ContractAddresses.sol with non-zero addresses derived from Foundry's DEFAULT_TEST_CONTRACT deployer, and restoreContractAddresses successfully reverts the file</done>
</task>

<task type="auto">
  <name>Task 2: Empirically validate nonce starting value with a minimal Foundry test</name>
  <files>test/fuzz/helpers/NonceCheck.t.sol</files>
  <action>
Create a minimal Foundry test `test/fuzz/helpers/NonceCheck.t.sol` (pragma `^0.8.26`) that:

1. Imports `forge-std/Test.sol`
2. In setUp(), calls `vm.getNonce(address(this))` and stores it
3. Deploys a trivial contract via `new` and checks nonce increments by 1
4. Has `test_startingNonce()` that asserts the initial nonce is 1 (per EIP-161)
5. Has `test_deployerIsDefaultTestContract()` that asserts `address(this)` matches `0x5615dEB798BB3E4dFa0139dFa1b3D433Cc23b72f`

Run: `node scripts/lib/patchForFoundry.js && forge build --force && forge test --match-contract NonceCheck -vvv`
Then restore ContractAddresses.sol.

If the starting nonce is NOT 1, update `PROTOCOL_START_NONCE` in patchForFoundry.js accordingly. The canary test in Plan 14-03 provides the definitive validation, but this gives early signal.

Note: This test file may be deleted after Phase 14 if no longer needed, but it is useful for debugging nonce issues.
  </action>
  <verify>
    <automated>node scripts/lib/patchForFoundry.js && forge build --force && forge test --match-contract NonceCheck -vvv 2>&1 | tail -15; node -e "import('./scripts/lib/patchContractAddresses.js').then(m => m.restoreContractAddresses())"</automated>
  </verify>
  <done>Test confirms starting nonce value and deployer address identity. PROTOCOL_START_NONCE is confirmed correct or adjusted.</done>
</task>

</tasks>

<verification>
1. `node scripts/lib/patchForFoundry.js` exits 0 and logs deployer + nonce info
2. After patching, `grep "address(0)" contracts/ContractAddresses.sol` returns 0 lines (all addresses patched)
3. After restore, `grep "address(0)" contracts/ContractAddresses.sol` returns 22+ lines (all addresses back to zero)
4. NonceCheck test passes confirming nonce=1 and deployer=DEFAULT_TEST_CONTRACT
</verification>

<success_criteria>
- patchForFoundry.js correctly predicts Foundry deployer addresses using CREATE address math
- The patch-restore cycle is non-destructive (ContractAddresses.sol returns to original state)
- Nonce starting value empirically confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/14-foundry-infrastructure-and-compiler-alignment/14-02-SUMMARY.md`
</output>
