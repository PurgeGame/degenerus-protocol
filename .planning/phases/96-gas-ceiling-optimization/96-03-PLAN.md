---
phase: 96-gas-ceiling-optimization
plan: 03
type: execute
wave: 2
depends_on: [96-02]
files_modified:
  - .planning/phases/96-gas-ceiling-optimization/96-OPTIMIZATION-AUDIT.md
autonomous: true
requirements: [GOPT-03]

must_haves:
  truths:
    - "Every optimization candidate from Plan 02 has a final disposition: implemented, deferred, or rejected with justification"
    - "If optimizations were implemented, all Foundry and Hardhat tests still pass"
    - "If no optimizations are worth implementing, the rationale is documented"
  artifacts:
    - path: ".planning/phases/96-gas-ceiling-optimization/96-OPTIMIZATION-AUDIT.md"
      provides: "Final optimization disposition section appended to audit document"
      contains: "Optimization Disposition"
  key_links:
    - from: "96-OPTIMIZATION-AUDIT.md"
      to: "contracts/modules/DegenerusGameJackpotModule.sol"
      via: "Optimization implementation (if any)"
      pattern: "implement|deferred|rejected"
---

<objective>
Implement any gas optimizations identified in Plan 02's SLOAD and loop hoisting audit, or document why no optimizations are worth implementing.

Purpose: GOPT-03 requires that identified optimizations are either implemented and verified, or explicitly justified as not worth the complexity. This plan acts on Plan 02's findings.

Output: Updated `96-OPTIMIZATION-AUDIT.md` with final disposition section. If code changes are warranted, the contract modifications (subject to user approval per project rules).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-gas-ceiling-optimization/96-RESEARCH.md
@.planning/phases/96-gas-ceiling-optimization/96-02-SUMMARY.md

<interfaces>
<!-- From RESEARCH.md key finding: -->
<!-- "The hot path's computations that precede the loops are already hoisted above the bucket iteration. -->
<!--  The remaining per-iteration costs are dominated by unavoidable per-address cold SLOADs, -->
<!--  not by redundant computation." -->

<!-- Project constraint from CLAUDE.md memory: -->
<!-- "NEVER commit contracts/ or test/ changes without explicit user approval" -->
<!-- "Present fix and wait for explicit approval before editing code" -->

<!-- If optimizations are found worth implementing: -->
<!-- 1. Present the proposed change as a diff -->
<!-- 2. Wait for user approval before modifying contracts/ -->
<!-- 3. Run full test suite after any code change: forge test -vv && npx hardhat test -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Optimization disposition and implementation</name>
  <files>.planning/phases/96-gas-ceiling-optimization/96-OPTIMIZATION-AUDIT.md</files>
  <read_first>
    - .planning/phases/96-gas-ceiling-optimization/96-OPTIMIZATION-AUDIT.md (Plan 02's complete audit with SLOAD verdicts and loop hoisting verdicts)
    - .planning/phases/96-gas-ceiling-optimization/96-02-SUMMARY.md (Plan 02's summary with optimization recommendations)
    - .planning/phases/96-gas-ceiling-optimization/96-RESEARCH.md (key finding about unavoidable cold SLOADs)
    - contracts/modules/DegenerusGameJackpotModule.sol (current code state for any modifications)
  </read_first>
  <action>
Read Plan 02's optimization audit and act on its findings. There are three possible paths:

**Path A: No optimizations worth implementing (LIKELY based on research)**

If Plan 02's audit confirms the RESEARCH.md finding that "remaining per-iteration costs are dominated by unavoidable per-address cold SLOADs, not by redundant computation":

Append an "Optimization Disposition" section to `96-OPTIMIZATION-AUDIT.md` with:

1. **Disposition Table:**

| # | Candidate | Plan 02 Verdict | Savings | Disposition | Justification |
|---|-----------|-----------------|---------|-------------|---------------|

For each OPTIMIZE/HOIST/MARGINAL candidate from Plan 02, record:
- The candidate description
- Plan 02's verdict and gas savings estimate
- Final disposition: IMPLEMENT, DEFER, or REJECT
- Justification for the disposition

2. **Rejection Rationale (for each rejected optimization):**
- Exact gas savings vs added code complexity
- Whether the savings meaningfully change the risk classification (e.g., does saving 32K gas on a 13M stage matter?)
- Whether the change could introduce behavioral risk

3. **Summary Statement:** One-paragraph conclusion for GOPT-03. Example:
"No actionable optimizations identified. The daily jackpot hot path is dominated by unavoidable per-address cold SLOADs (autoRebuyState, claimableWinnings, ticketsOwedPacked) at 2,100 gas each. The only hoistable reads (gameOver, level) are warm SLOADs at 100 gas each, saving a combined ~64,200 gas (0.46% of the ~14M ceiling) at the cost of adding parameters to _addClaimableEth and _processAutoRebuy. This marginal savings does not change any risk classification."

**Path B: Optimizations worth implementing**

If Plan 02 identifies optimizations with total savings > 100,000 gas AND the savings would change a risk classification:

1. Draft the exact code changes (Solidity diff)
2. Present the changes to the user and WAIT for approval (per project memory: "Present fix and wait for explicit approval before editing code")
3. If approved: make the code changes
4. Run regression tests:
   - `git stash` (if ContractAddresses.sol has changes)
   - `forge test -vv`
   - `npx hardhat test`
   - `git stash pop` (if stashed)
5. Run the gas test to measure improvement:
   - `npx hardhat test test/gas/AdvanceGameGas.test.js`
6. Append results to the audit document:
   - Before/after gas comparison
   - All tests passing confirmation
   - Updated risk classification if headroom changed

**Path C: Marginal optimizations deferred**

If savings are real but < 100,000 gas total and don't change risk classifications:

1. Document each as DEFER with rationale
2. Note: "Can be revisited if gas ceiling becomes tighter in future milestones"
3. No code changes

**CRITICAL:** Do NOT commit any changes to contracts/ or test/ without explicit user approval. If Path B applies, present the diff and pause.
  </action>
  <verify>
    <automated>grep -c "Disposition\|IMPLEMENT\|DEFER\|REJECT" .planning/phases/96-gas-ceiling-optimization/96-OPTIMIZATION-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - 96-OPTIMIZATION-AUDIT.md contains "Optimization Disposition" section
    - Every optimization candidate from Plan 02 has a disposition (IMPLEMENT, DEFER, or REJECT)
    - Each disposition has a justification
    - Contains a summary statement for GOPT-03
    - If Path B (implementation): contracts/ changes have user approval, all tests pass
    - If Path A or C (no implementation): explicit rationale why no code changes are needed
    - GOPT-03 fully addressed: all identified optimizations have a final disposition
  </acceptance_criteria>
  <done>Every optimization candidate from the SLOAD and loop hoisting audit has a final disposition (implement, defer, or reject) with justification. If code changes were made, all tests pass. GOPT-03 is complete.</done>
</task>

</tasks>

<verification>
- `96-OPTIMIZATION-AUDIT.md` has an "Optimization Disposition" section
- Every candidate has IMPLEMENT, DEFER, or REJECT disposition
- If code changes: `forge test -vv` and `npx hardhat test` both pass
- If no code changes: rationale documented
</verification>

<success_criteria>
- GOPT-03: All identified optimizations have a documented disposition with justification
- If implemented: regression tests pass, gas improvement measured
- If not implemented: clear rationale that savings are marginal or don't change risk classifications
</success_criteria>

<output>
After completion, create `.planning/phases/96-gas-ceiling-optimization/96-03-SUMMARY.md`
</output>
