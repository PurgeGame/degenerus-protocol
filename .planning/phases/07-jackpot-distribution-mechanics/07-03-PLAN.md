---
phase: 07-jackpot-distribution-mechanics
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v1.1-transition-jackpots.md
autonomous: true
requirements:
  - JACK-08
  - JACK-09

must_haves:
  truths:
    - "A game theory agent can determine exactly which levels trigger BAF and Decimator and the pool percentage for each"
    - "BAF payout split mechanics (large vs small winners, lootbox tiers) are fully specified with exact thresholds"
    - "Decimator burn tracking, bucket/subbucket system, multiplier cap, and resolution are documented with exact formulas"
    - "Decimator claim mechanics (pro-rata, 50/50 split, expiry) are documented with exact expressions"
  artifacts:
    - path: "audit/v1.1-transition-jackpots.md"
      provides: "Complete BAF and Decimator transition jackpot reference document"
      contains: "DECIMATOR_MULTIPLIER_CAP"
  key_links:
    - from: "audit/v1.1-transition-jackpots.md"
      to: "contracts/modules/DegenerusGameEndgameModule.sol"
      via: "Verified BAF formulas reference exact line numbers"
      pattern: "EndgameModule.*line"
    - from: "audit/v1.1-transition-jackpots.md"
      to: "contracts/modules/DegenerusGameDecimatorModule.sol"
      via: "Verified decimator formulas reference exact line numbers"
      pattern: "DecimatorModule.*line"
    - from: "audit/v1.1-transition-jackpots.md"
      to: "audit/v1.1-pool-architecture.md"
      via: "References futurePrizePool as source for both BAF and Decimator"
      pattern: "futurePrizePool|baseFuturePool"
---

<objective>
Document the two transition jackpot mechanisms that fire at level milestones: BAF (Big-Ass Flip) and Decimator.

Purpose: Enable game theory agents to compute exact BAF and Decimator payouts at any level, including trigger schedules, pool percentages, payout splits, burn tracking mechanics, and claim windows.

Output: `audit/v1.1-transition-jackpots.md`
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-jackpot-distribution-mechanics/07-RESEARCH.md

@audit/v1.1-eth-inflows.md (style reference)
@audit/v1.1-pool-architecture.md (pool lifecycle context, futurePrizePool)

Primary source contracts:
@contracts/modules/DegenerusGameEndgameModule.sol (lines 138+ for BAF, lines 405-488 for lootbox tiers)
@contracts/modules/DegenerusGameDecimatorModule.sol (lines 221+ for burn tracking, lines 297+ for resolution)
@contracts/modules/DegenerusGameAdvanceModule.sol (decimator window open/close, lines 282-285)
@contracts/modules/DegenerusGamePayoutUtils.sol (HALF_WHALE_PASS_PRICE, LOOTBOX_CLAIM_THRESHOLD)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document BAF and Decimator transition jackpot mechanics</name>
  <files>audit/v1.1-transition-jackpots.md</files>
  <action>
Create a reference document following the style of audit/v1.1-eth-inflows.md. Structure:

1. **Overview** — Transition jackpots fire during `_runRewardJackpots()` (EndgameModule:138) at the end of jackpot phase day 5, before the new level begins. Two independent mechanisms: BAF (every 10 levels) and Decimator (every 5 levels, offset). Both draw from futurePrizePool. Per Research pitfall 3: document that BAF uses `baseFuturePool` (snapshot before deductions) and normal Decimator uses `futurePoolLocal` (after BAF deduction), while x00 Decimator uses `baseFuturePool`.

2. **BAF Trigger Schedule (JACK-08)** — Document exact level triggers:
   - Every 10 levels (lvl % 10 == 0) via `runRewardJackpots()` (EndgameModule:138)
   - Pool percentage table:
     - Levels 10, 20, 30, 40, 60, 70, 80, 90: 10% of baseFuturePool
     - Level 50: 25% of baseFuturePool
     - x00 levels (100, 200, 300...): 20% of baseFuturePool
   - Exact Solidity: `bafPct = prevMod100 == 0 ? 20 : (lvl == 50 ? 25 : 10)`
   - Include a level schedule table showing the first 100+ levels with BAF/Decimator triggers

3. **BAF Payout Distribution (JACK-08)** — Document the split mechanics:
   - External call to `jackpots.runBafJackpot(poolWei, lvl, rngWord)` returns winners and amounts
   - Large winners (>= 5% of BAF pool): 50% claimable ETH, 50% lootbox
   - Small winners (< 5% of pool): alternating (even index: 100% ETH, odd index: 100% lootbox)
   - Refund: unused BAF pool returns to futurePrizePool

4. **BAF Lootbox Ticket Tiers (JACK-08)** — Document EndgameModule:405-488:
   - Very small (<=0.5 ETH): single probabilistic roll
   - Medium (0.5-5 ETH): split in half, 2 probabilistic rolls
   - Large (>5 ETH): deferred whale pass claim (via LOOTBOX_CLAIM_THRESHOLD=5 ETH)
   - Each roll probability: 30% current level, 65% +1-4 levels, 5% +5-50 levels
   - Lootbox ETH stays in futurePrizePool (source pool)

5. **Decimator Trigger Schedule (JACK-09)** — Document exact level triggers:
   - Window opens at jackpot phase start (AdvanceModule:282-285):
     `(lvl % 10 == 4 && mod100 != 94) || mod100 == 99`
   - Pattern: levels 4, 14, 24, 34, 44, 54, 64, 74, 84, 99, 104, 114... (NOT 94)
   - Resolution fires during `_runRewardJackpots()` after day 5
   - Pool percentages:
     - Normal (levels 5, 15, 25...85): 10% of futurePoolLocal (after BAF deduction)
     - x00 levels (100, 200...): 30% of baseFuturePool (before deduction)

6. **Decimator Burn Tracking (JACK-09)** — Document `recordDecBurn()` (DecimatorModule:221):
   - Players choose bucket denominator 2-12 (DECIMATOR_MAX_DENOM=12). Lower denom = fewer winners, higher payout.
   - Subbucket: `keccak256(player, lvl, bucket) % bucket` (deterministic)
   - Burns accumulate with multiplier, capped at DECIMATOR_MULTIPLIER_CAP = 200,000 BURNIE
   - Effective amount formula via `_decEffectiveAmount()`: if multBps <= 10000 or prevBurn >= cap, no multiplier; otherwise multiplier applies up to cap, then 1x for remainder
   - Migration: can move to lower bucket (removes from old, adds to new)

7. **Decimator Resolution (JACK-09)** — Document `runDecimatorJackpot()` (DecimatorModule:297):
   - For each denominator 2-12: select winning subbucket from VRF
   - Sum burn totals from all winning subbuckets
   - Snapshot in `lastDecClaimRound` (overwrites previous -- old claims EXPIRE per Research pitfall 6)
   - Returns 0 if winners found (pool held for claims), returns poolWei if no qualifying burns

8. **Decimator Claim Mechanics (JACK-09)** — Document claim flow:
   - Pro-rata: `amountWei = (poolWei * playerBurn) / totalBurn`
   - Normal play: 50% ETH / 50% lootbox (via `_creditDecJackpotClaimCore`)
   - Game over mode: 100% ETH
   - Claims blocked during pool freeze (jackpot phase days 1-4)
   - Lootbox portion: <=LOOTBOX_CLAIM_THRESHOLD (5 ETH): immediate resolve; >5 ETH: deferred whale pass
   - Claim window: between resolution and next decimator run (old claims overwritten)

9. **Decimator Window Timing** — Document the full lifecycle:
   - Opens: at jackpot phase start for eligible levels
   - Burns: can occur during jackpot phase while window is open
   - Resolution: end of day 5 during _runRewardJackpots()
   - Claims: after resolution, before next decimator (blocked during freeze)

10. **Combined Level Schedule Table** — Table showing levels 1-110 with columns: Level, BAF trigger, BAF pool %, Decimator window, Decimator pool %, Decimator resolution level. This is the key quick-reference for agents.

11. **Constants Reference Table** — All constants with values, source file, and line number.

IMPORTANT: Per Research pitfall 3, always specify the exact pool variable used (baseFuturePool vs futurePoolLocal). Per Research pitfall 6, document claim expiry explicitly. Every formula must include exact Solidity expressions with line references.
  </action>
  <verify>
    <automated>grep -c "DECIMATOR_MULTIPLIER_CAP\|DECIMATOR_MAX_DENOM\|runBafJackpot\|runDecimatorJackpot\|baseFuturePool\|futurePoolLocal\|LOOTBOX_CLAIM_THRESHOLD\|lastDecClaimRound" audit/v1.1-transition-jackpots.md</automated>
  </verify>
  <done>Document exists with all 11 sections. Combined level schedule covers at least levels 1-110. BAF and Decimator trigger conditions have exact Solidity expressions with line references. Decimator burn tracking includes multiplier cap formula. Claim mechanics document expiry. Constants table has at least 10 entries. JACK-08 and JACK-09 are fully addressed.</done>
</task>

</tasks>

<verification>
- BAF pool percentages match EndgameModule:150 (10/25/20%)
- BAF payout split thresholds (5% of pool, 0.5 ETH, 5 ETH) match EndgameModule:405-488
- Decimator trigger levels match AdvanceModule:282-285 condition
- Decimator pool percentages: 10% futurePoolLocal (normal) and 30% baseFuturePool (x00) verified
- Decimator multiplier cap matches DECIMATOR_MULTIPLIER_CAP=200*1000 ether at DecimatorModule:100
- Claim pro-rata formula matches DecimatorModule source
- baseFuturePool vs futurePoolLocal distinction is explicit throughout (Research pitfall 3)
</verification>

<success_criteria>
A game theory agent reading only this document can determine at any level: whether BAF/Decimator fire, exact pool amounts, payout mechanics, and claim windows -- without reading contract source.
</success_criteria>

<output>
After completion, create `.planning/phases/07-jackpot-distribution-mechanics/07-03-SUMMARY.md`
</output>
