---
phase: 07-jackpot-distribution-mechanics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v1.1-jackpot-phase-draws.md
autonomous: true
requirements:
  - JACK-03
  - JACK-04
  - JACK-05
  - JACK-06
  - JACK-07

must_haves:
  truths:
    - "A game theory agent can compute the exact ETH payout per jackpot-phase day given currentPrizePool and a random seed"
    - "A game theory agent can determine which trait buckets are eligible each day and their share percentages"
    - "Carryover mechanics (days 2-4) and early-bird (day 1) are documented as distinct flows with exact formulas"
    - "Compressed jackpot conditions and effects (counterStep=2, BPS doubling) are fully specified"
    - "BURNIE parallel distribution during jackpot phase is documented with budget, split, and winner selection"
  artifacts:
    - path: "audit/v1.1-jackpot-phase-draws.md"
      provides: "Complete 5-day jackpot-phase draw reference document"
      contains: "_dailyCurrentPoolBps"
  key_links:
    - from: "audit/v1.1-jackpot-phase-draws.md"
      to: "contracts/modules/DegenerusGameJackpotModule.sol"
      via: "Verified formulas reference exact line numbers"
      pattern: "JackpotModule.*line"
    - from: "audit/v1.1-jackpot-phase-draws.md"
      to: "audit/v1.1-pool-architecture.md"
      via: "References currentPrizePool lifecycle and freeze mechanics"
      pattern: "currentPrizePool|freeze"
---

<objective>
Document the complete 5-day jackpot-phase draw sequence: daily pool slicing, trait bucket distribution, winner selection, carryover mechanics, compressed jackpots, lootbox conversion, and BURNIE parallel distribution.

Purpose: Enable game theory agents to simulate the full jackpot phase day-by-day, computing expected payouts per trait bucket, carryover flows, and BURNIE distributions.

Output: `audit/v1.1-jackpot-phase-draws.md`
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-jackpot-distribution-mechanics/07-RESEARCH.md

@audit/v1.1-eth-inflows.md (style reference)
@audit/v1.1-pool-architecture.md (pool lifecycle context, freeze mechanics)

Primary source contracts:
@contracts/modules/DegenerusGameJackpotModule.sol (lines 333-611 for daily jackpot, line 2687 for pool BPS, lines 1530+ for distribution, lines 2264+ for winner selection, line 685 for coin+tickets)
@contracts/modules/DegenerusGameAdvanceModule.sol (compressed jackpot trigger, phase orchestration)
@contracts/libraries/JackpotBucketLib.sol (bucket sizing, winner count scaling)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document 5-day jackpot-phase draw sequence with trait buckets and carryover</name>
  <files>audit/v1.1-jackpot-phase-draws.md</files>
  <action>
Create a reference document following the style of audit/v1.1-eth-inflows.md. Structure:

1. **Overview** — The jackpot phase is a 5-day draw sequence distributing currentPrizePool. Reference v1.1-pool-architecture.md for how currentPrizePool gets funded at consolidation. Include the high-level daily flow diagram from the research (Day 1: early-bird + draw, Days 2-4: carryover + draw, Day 5: 100% + final rewards).

2. **Daily Pool Slice Formula (JACK-03)** — Document `_dailyCurrentPoolBps()` from JackpotModule line 2687:
   - Days 1-4: random 6-14% via `DAILY_CURRENT_BPS_MIN=600` to `DAILY_CURRENT_BPS_MAX=1400`, seed = keccak256(randWord, DAILY_CURRENT_BPS_TAG, counter)
   - Day 5 (counter >= JACKPOT_LEVEL_CAP-1): 100% (10000 BPS)
   - Budget split: `dailyLootboxBudget = budget / 5` (20% to lootbox tickets), remaining 80% to ETH distribution
   - Include the exact Solidity expression with line numbers

3. **Trait Bucket Distribution (JACK-04)** — Document trait bucket system:
   - Trait ID encoding: `(quadrant << 6) | (color << 3) | symbol`, 4 quadrants x 8 colors x 8 symbols
   - Q0/Q1/Q2 derivation: fixed symbol-0, random color. Q3: fully random. Hero override: top daily hero symbol auto-wins quadrant.
   - Days 1-4 shares: DAILY_JACKPOT_SHARES_PACKED = 2000/2000/2000/2000 (20% each, solo bucket gets remainder)
   - Day 5 shares: FINAL_DAY_SHARES_PACKED = 6000/1333/1333/1334 (solo=60%, others ~13% each). Solo bucket index = `entropy & 3`
   - Winner selection via `_randTraitTicket()` (line 2264): duplicates allowed, virtual deity entries = floor(2% of bucket, min 2)
   - Solo bucket special: 1 winner gets 75% ETH / 25% whale passes if 25% >= HALF_WHALE_PASS_PRICE (2.175 ETH), else 100% ETH (per Research pitfall 4)
   - Bucket winner count scaling via JackpotBucketLib: JACKPOT_SCALE_MAX_BPS=40000 (4x at 200+ ETH), DAILY_JACKPOT_SCALE_MAX_BPS=66667 (6.67x for daily)
   - DGNRS day-5 reward: FINAL_DAY_DGNRS_BPS=100 (1% of reward pool) to solo bucket winner

4. **Day 1: Early-Bird Lootbox Jackpot** — Document `_runEarlyBirdLootboxJackpot()`:
   - 3% of futurePrizePool (`reserveContribution = (_getFuturePrizePool() * 300) / 10_000`)
   - Up to 100 winners (LOOTBOX_MAX_WINNERS), per-winner = totalBudget / 100
   - Each winner: random level offset 0-4, tickets at that level's price
   - Full budget added to nextPrizePool
   - This replaces carryover on day 1

5. **Days 2-4: Carryover Mechanics (JACK-05)** — Document carryover flow:
   - Source: 1% of futurePrizePool (`reserveSlice = _getFuturePrizePool() / 100`)
   - 50% lootbox (DAILY_REWARD_JACKPOT_LOOTBOX_BPS=5000), 50% ETH to trait winners
   - Source level selection: random eligible offset in [lvl+1, lvl+DAILY_CARRYOVER_MAX_OFFSET] (up to lvl+5)
   - Winner cap: remaining after daily distribution (min DAILY_CARRYOVER_MIN_WINNERS=20)
   - No carryover on day 5

6. **Compressed Jackpot (JACK-05)** — Document compressed mode:
   - Trigger: `compressedJackpotFlag = (day - purchaseStartDay <= 2)` in advanceGame when lastPurchaseDay
   - Effect: `counterStep=2`, `dailyBps *= 2`
   - Result: 5 logical days in 3 physical days
   - Per Research pitfall 1: explicitly distinguish "logical days" vs "physical days"

7. **Lootbox Conversion Reference (JACK-06)** — Consolidate jackpot-phase-specific lootbox conversion:
   - Daily budget: 20% (`budget / 5`) to lootbox tickets
   - Carryover (days 2-4): 50% (DAILY_REWARD_JACKPOT_LOOTBOX_BPS=5000) to lootbox
   - Early-bird (day 1): 100% of 3% future pool becomes tickets
   - Cross-reference with purchase-phase ratios documented in 07-01

8. **BURNIE Parallel Distribution (JACK-07)** — Document `payDailyJackpotCoinAndTickets()` (line 685):
   - Budget: same formula as purchase phase: 0.5% of prize pool target in BURNIE
   - Split: 25% far-future (FAR_FUTURE_COIN_BPS=2500, lvl+5 to lvl+99, up to FAR_FUTURE_COIN_SAMPLES=10 levels), 75% near-future (lvl to lvl+4, trait-matched, up to DAILY_COIN_MAX_WINNERS=50)
   - Far-future: `candidate = lvl + 5 + uint24(entropy % 95)`, 1 winner per level from ticketQueue
   - Payment via coin.creditFlipBatch()

9. **Constants Reference Table** — All constants with values, source file, and line number.

IMPORTANT: Document the day-by-day flow clearly so an agent can simulate each day independently. Every formula must include exact Solidity expressions with line references.
  </action>
  <verify>
    <automated>grep -c "_dailyCurrentPoolBps\|DAILY_CURRENT_BPS_MIN\|FINAL_DAY_SHARES_PACKED\|DAILY_JACKPOT_SHARES_PACKED\|compressedJackpotFlag\|DAILY_REWARD_JACKPOT_LOOTBOX_BPS\|FAR_FUTURE_COIN_BPS\|_runEarlyBirdLootboxJackpot" audit/v1.1-jackpot-phase-draws.md</automated>
  </verify>
  <done>Document exists with all 9 sections. Day-by-day flow is simulatable. Every formula includes Solidity expression with line reference. Constants table has at least 15 entries. JACK-03, JACK-04, JACK-05, JACK-06 (jackpot-phase portion), and JACK-07 are fully addressed.</done>
</task>

</tasks>

<verification>
- Daily pool slice formula matches JackpotModule:2687
- Trait bucket shares match packed constants at JackpotModule:113-122
- Carryover BPS matches DAILY_REWARD_JACKPOT_LOOTBOX_BPS at JackpotModule:179
- Compressed jackpot trigger matches AdvanceModule source
- BURNIE budget formula matches _calcDailyCoinBudget at JackpotModule:2678
- Day 1 early-bird 3% matches _runEarlyBirdLootboxJackpot source
- Winner selection documents virtual deity entries correctly
</verification>

<success_criteria>
A game theory agent reading only this document can simulate each of the 5 jackpot-phase days, computing: ETH budget per day, trait bucket allocation, winner counts, carryover flows, BURNIE distributions, and compressed mode effects -- without reading contract source.
</success_criteria>

<output>
After completion, create `.planning/phases/07-jackpot-distribution-mechanics/07-02-SUMMARY.md`
</output>
