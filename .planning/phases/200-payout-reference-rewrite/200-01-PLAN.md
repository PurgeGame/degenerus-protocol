---
phase: 200-payout-reference-rewrite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/JACKPOT-PAYOUT-REFERENCE.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03

must_haves:
  truths:
    - "JACKPOT-PAYOUT-REFERENCE.md documents all 8 jackpot types with correct function names"
    - "Skip-split (SPLIT_NONE) and two-call split (SPLIT_CALL1+SPLIT_CALL2) are both documented as conditional paths"
    - "No references to deleted _distributeJackpotEth, _processOneBucket, or JackpotEthCtx exist"
    - "Every caller path references _processDailyEth with correct splitMode and isJackpotPhase parameters"
  artifacts:
    - path: "docs/JACKPOT-PAYOUT-REFERENCE.md"
      provides: "Complete payout reference for all jackpot types"
      contains: "SPLIT_NONE"
  key_links:
    - from: "docs/JACKPOT-PAYOUT-REFERENCE.md"
      to: "contracts/modules/DegenerusGameJackpotModule.sol"
      via: "function name references"
      pattern: "_processDailyEth"
---

<objective>
Rewrite JACKPOT-PAYOUT-REFERENCE.md from scratch to reflect the unified `_processDailyEth` with skip-split architecture from Phase 198. The current document describes three-call split as the only daily ETH path, but skip-split (SPLIT_NONE for <= 160 winners) now exists. All references to deleted `_distributeJackpotEth` must be replaced.

Purpose: Eliminate stale documentation that references deleted functions, and accurately document the current skip-split conditional architecture.
Output: Fully rewritten `docs/JACKPOT-PAYOUT-REFERENCE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/200-payout-reference-rewrite/200-CONTEXT.md
@.planning/phases/199-delta-audit-skip-split-gas/199-02-AUDIT.md
@.planning/phases/199-delta-audit-skip-split-gas/199-01-GAS-DERIVATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite JACKPOT-PAYOUT-REFERENCE.md from scratch</name>
  <files>docs/JACKPOT-PAYOUT-REFERENCE.md</files>
  <read_first>
    - docs/JACKPOT-PAYOUT-REFERENCE.md (current stale version -- understand existing structure and coverage)
    - contracts/modules/DegenerusGameJackpotModule.sol (ground truth for all payout mechanics, lines 167-171 for SPLIT constants, lines 307-550 for payDailyJackpot, lines 1087-1119 for _runJackpotEthFlow, lines 1159-1290 for _processDailyEth, lines 1401-1478 for _handleSoloBucketWinner and _payNormalBucket, lines 1486-1540 for _processSoloBucketWinner, lines 1700-1780 for coin jackpot, lines 1840-1870 for far-future coin, lines 1960-2074 for BAF jackpot)
    - contracts/modules/DegenerusGameAdvanceModule.sol (stage machine -- lines 65-72 for STAGE constants, lines 340-440 for jackpot flow routing including line 414 resumeEthPool check)
    - contracts/modules/DegenerusGameGameOverModule.sol (terminal jackpot caller -- lines 165-178)
    - .planning/phases/199-delta-audit-skip-split-gas/199-02-AUDIT.md (verified caller paths, parity analysis)
    - .planning/phases/199-delta-audit-skip-split-gas/199-01-GAS-DERIVATION.md (gas figures for each path)
  </read_first>
  <action>
Clear and rewrite `docs/JACKPOT-PAYOUT-REFERENCE.md` entirely. Per D-01, keep the organization by jackpot type (the current structure is correct). Per D-02, document both SPLIT_NONE (single call, totalWinners <= 160) and SPLIT_CALL1+SPLIT_CALL2 (two calls, totalWinners > 160) as conditional paths within the daily jackpot sections.

**Document structure (preserve existing section numbering to avoid breaking cross-references from JACKPOT-EVENT-CATALOG.md):**

1. **Overview** -- Update to describe unified `_processDailyEth` with conditional skip-split. Replace the "Three-call split" bullet with: "Conditional two-call split for daily ETH. When totalWinners <= 160 (JACKPOT_MAX_WINNERS), all 4 buckets process in a single call via SPLIT_NONE. When totalWinners > 160, the largest + solo buckets process in Call 1 (SPLIT_CALL1), mid buckets in Call 2 (SPLIT_CALL2)." Keep the stage machine bullet but update values: `STAGE_JACKPOT_DAILY_STARTED` (11) triggers Call 1 or single-call, `STAGE_JACKPOT_ETH_RESUME` (8) triggers Call 2 (only when resumeEthPool != 0), `STAGE_JACKPOT_COIN_TICKETS` (9) triggers Call 3 (coin + tickets).

2. **Key Constants** -- Keep existing table. Add `SPLIT_NONE = 0`, `SPLIT_CALL1 = 1`, `SPLIT_CALL2 = 2` (JackpotModule lines 167-171). Update "Max scaled counts" to show both daily (305 at 6.36x) and early-burn (160 cap at 4x).

3. **Daily Normal Jackpot (Days 1-4)** -- Per D-02, replace "Three-Call Split" subsection with "Split Behavior" subsection documenting both paths:
   - SPLIT_NONE path: `totalWinners <= 160` -> `splitMode = SPLIT_NONE` -> `_processDailyEth` processes all 4 buckets in one call -> no `resumeEthPool` write -> Call 3 follows immediately
   - SPLIT_CALL1+CALL2 path: `totalWinners > 160` -> `splitMode = SPLIT_CALL1` -> Call 1 processes largest+solo (160) -> writes `resumeEthPool` -> Call 2 reads/clears `resumeEthPool`, processes 2 mid buckets (145) -> Call 3
   - Note: both paths use `bucketOrderLargestFirst` iteration. Each call uses its own VRF word (entropy independence).
   
   Update "Payout" to reference `_processDailyEth` (not `_processDailyEth(isResume=...)` -- the old isResume parameter is gone, replaced by splitMode).

4. **Daily x10 / x100 Multiplied Jackpots** -- No substantive changes needed. Same payout mechanics, same split behavior.

5. **Daily Final Day (Day 5)** -- Same split behavior as days 1-4. Note that `isFinalDay` is set based on `jackpotCounter + counterStep >= JACKPOT_LEVEL_CAP`. Update "Three-Call Split" reference to "Split Behavior" matching section 3.

6. **Trait Jackpot (Early-Burn Path)** -- Per D-03, replace `_distributeJackpotEth` with: "`_runJackpotEthFlow` -> `_processDailyEth(splitMode=SPLIT_NONE, isJackpotPhase=false)`. All 4 buckets processed in a single call. Winner counts capped at `JACKPOT_MAX_WINNERS` = 160 via `scaleTraitBucketCountsWithCap`. No whale pass, no DGNRS."

7. **Early-Bird Lootbox Jackpot** -- No changes needed (not affected by split architecture).

8. **Terminal Jackpot** -- Per D-03, replace `_distributeJackpotEth` with: "`runTerminalJackpot` -> `_processDailyEth(splitMode=SPLIT_NONE, isJackpotPhase=false)`. All 4 buckets processed in a single call. Uses `DAILY_ETH_MAX_WINNERS` = 305 cap and `bucketCountsForPoolCap` (not scaleTraitBucketCountsWithCap). No whale pass, no DGNRS. No autorebuy (`gameOver=true` prevents it)."

9. **Decimator Jackpot** -- No changes needed.

10. **BAF Jackpot** -- No changes needed.

11. **Daily BURNIE Coin Jackpot** -- No changes needed.

12. **Pool Flow Summary** -- No substantive changes. Verify table accuracy against current code.

13. **Split Details** -- Retitle from "Three-Call Split Details" to "Conditional Split Details". Rewrite to document:
    - Why the split exists (305 winners * ~82K gas/winner = 25M > 16M block limit)
    - Skip-split optimization: when `totalWinners <= JACKPOT_MAX_WINNERS` (160), all 4 buckets fit in one call -> `SPLIT_NONE` avoids a second VRF request + advanceGame call
    - Two-call split architecture: when `totalWinners > 160`, `SPLIT_CALL1` processes largest+solo, stores `resumeEthPool`, `SPLIT_CALL2` reads/clears `resumeEthPool`, processes mid buckets
    - Bucket assignment via `bucketOrderLargestFirst` and `call1Bucket` mask
    - Inter-call state via `resumeEthPool` (uint128 in storage)
    - Entropy independence (each call uses fresh VRF word)
    - Gas safety margins table (update with figures from 199-01-GAS-DERIVATION.md):
      | Path | Max Winners | Worst-Case Gas | Block Limit | Margin |
      | Skip-split (SPLIT_NONE, daily) | 160 | ~13.36M | 16M | 16.5% |
      | Call 1 (SPLIT_CALL1) | 160 | ~13.36M | 16M | 16.5% |
      | Call 2 (SPLIT_CALL2) | 145 | ~12.12M | 16M | 24.3% |
      | Early-burn (SPLIT_NONE) | 160 | ~13.36M | 16M | 16.5% |
      | Terminal (SPLIT_NONE) | 305 | ~8.28M* | 16M | 48.3% |
      *Terminal has no autorebuy (gameOver=true), so per-winner cost is ~27K not ~82K.
    - Note: Non-daily callers (early-burn, terminal) ALWAYS use SPLIT_NONE because their winner counts are capped at single-call-safe levels.

**Header line:** `Last verified against: \`fa2b9c39\` (2026-04-08)`

**Critical per D-03:** grep the final file for `_distributeJackpotEth`, `_processOneBucket`, `JackpotEthCtx` -- zero matches required.
  </action>
  <verify>
    <automated>grep -c "_distributeJackpotEth\|_processOneBucket\|JackpotEthCtx" docs/JACKPOT-PAYOUT-REFERENCE.md; test $? -eq 1 && echo "PASS: no stale references" || echo "FAIL: stale references found"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SPLIT_NONE" docs/JACKPOT-PAYOUT-REFERENCE.md` returns >= 5 (overview, section 3, section 6, section 8, section 13)
    - `grep -c "SPLIT_CALL1" docs/JACKPOT-PAYOUT-REFERENCE.md` returns >= 3 (overview, section 3, section 13)
    - `grep -c "_processDailyEth" docs/JACKPOT-PAYOUT-REFERENCE.md` returns >= 5 (replaces all old _distributeJackpotEth refs)
    - `grep -c "_distributeJackpotEth" docs/JACKPOT-PAYOUT-REFERENCE.md` returns 0
    - `grep -c "_processOneBucket" docs/JACKPOT-PAYOUT-REFERENCE.md` returns 0
    - `grep -c "JackpotEthCtx" docs/JACKPOT-PAYOUT-REFERENCE.md` returns 0
    - `grep -c "isJackpotPhase" docs/JACKPOT-PAYOUT-REFERENCE.md` returns >= 3 (section 6, section 8, section 13)
    - `grep "Last verified against" docs/JACKPOT-PAYOUT-REFERENCE.md` contains `fa2b9c39`
    - Document contains sections numbered 1 through 13
    - Section 3 contains subsection "Split Behavior" (not "Three-Call Split")
    - Section 13 is titled "Conditional Split Details" (not "Three-Call Split Details")
    - Section 6 references `_runJackpotEthFlow` and `_processDailyEth(splitMode=SPLIT_NONE, isJackpotPhase=false)`
    - Section 8 references `runTerminalJackpot` and `_processDailyEth(splitMode=SPLIT_NONE, isJackpotPhase=false)`
    - Gas safety margins table includes skip-split, call 1, call 2, early-burn, and terminal rows
  </acceptance_criteria>
  <done>
    JACKPOT-PAYOUT-REFERENCE.md is fully rewritten with:
    - All 8 jackpot types documented with current function names
    - Skip-split (SPLIT_NONE) and two-call split (SPLIT_CALL1+SPLIT_CALL2) as conditional paths
    - Zero references to deleted _distributeJackpotEth, _processOneBucket, or JackpotEthCtx
    - Correct splitMode and isJackpotPhase parameters for every caller path
    - Updated gas safety margins from 199-01-GAS-DERIVATION.md
    - Verified-against commit hash set to current HEAD (fa2b9c39)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- this plan modifies documentation only, no contract or test code.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200-01 | Information Disclosure | docs/JACKPOT-PAYOUT-REFERENCE.md | accept | Documentation is public. No secrets, keys, or addresses exposed. |
</threat_model>

<verification>
1. `grep -c "_distributeJackpotEth" docs/JACKPOT-PAYOUT-REFERENCE.md` returns 0
2. `grep -c "SPLIT_NONE" docs/JACKPOT-PAYOUT-REFERENCE.md` returns >= 5
3. `grep -c "SPLIT_CALL1" docs/JACKPOT-PAYOUT-REFERENCE.md` returns >= 3
4. `grep "fa2b9c39" docs/JACKPOT-PAYOUT-REFERENCE.md` matches the verified-against line
5. Document has sections 1-13 with correct titles
</verification>

<success_criteria>
JACKPOT-PAYOUT-REFERENCE.md accurately reflects the current contract architecture with unified _processDailyEth, conditional skip-split, and correct function names throughout. Zero stale references to deleted code.
</success_criteria>

<output>
After completion, create `.planning/phases/200-payout-reference-rewrite/200-01-SUMMARY.md`
</output>
