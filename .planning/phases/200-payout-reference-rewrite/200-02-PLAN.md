---
phase: 200-payout-reference-rewrite
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/JACKPOT-EVENT-CATALOG.md
autonomous: true
requirements:
  - DOC-04

must_haves:
  truths:
    - "Event catalog commit hash matches current HEAD"
    - "Every event name in the catalog matches the exact Solidity event name in the contract"
    - "Every emitting-path line number is correct for the current commit"
    - "Dead code paths (_resolveTraitWinners) are not listed as emitting paths"
    - "All 6 JackpotModule events are cataloged"
  artifacts:
    - path: "docs/JACKPOT-EVENT-CATALOG.md"
      provides: "Accurate event catalog for all jackpot operations"
      contains: "fa2b9c39"
  key_links:
    - from: "docs/JACKPOT-EVENT-CATALOG.md"
      to: "contracts/modules/DegenerusGameJackpotModule.sol"
      via: "event declaration and emit line references"
      pattern: "JackpotEthWin"
---

<objective>
Update JACKPOT-EVENT-CATALOG.md to accurately reflect the current contract state at commit fa2b9c39. The catalog was last verified at f0dc4c99 and has significant drift: wrong event names, stale line numbers, dead code references, and missing events.

Purpose: Ensure the event catalog is a reliable reference for indexing and debugging jackpot events.
Output: Corrected `docs/JACKPOT-EVENT-CATALOG.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/200-payout-reference-rewrite/200-CONTEXT.md
@.planning/phases/199-delta-audit-skip-split-gas/199-02-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify stale reference findings F-01, F-04, F-05 are already fixed</name>
  <files>(verification only -- no files modified)</files>
  <read_first>
    - contracts/modules/DegenerusGameGameOverModule.sol (line 170 -- verify comment says _processDailyEth not _distributeJackpotEth)
    - test/gas/AdvanceGameGas.test.js (lines 1053-1054 -- verify comments say _processDailyEth(SPLIT_NONE) not _distributeJackpotEth)
    - .planning/phases/199-delta-audit-skip-split-gas/199-02-AUDIT.md (Section F -- the audit findings)
  </read_first>
  <action>
Verify that audit findings F-01, F-04, F-05 from 199-02-AUDIT.md Section F are already resolved in the current working tree. Per D-04, these are contract/test file changes that require user approval. However, based on current file state, they appear to have already been fixed:

- F-01: GameOverModule.sol line 170 should read `// claimablePool already updated inside JackpotModule._processDailyEth` (not `_distributeJackpotEth`)
- F-04: AdvanceGameGas.test.js line 1053 should read `// For early-burn path: single call, 160 winners, _processDailyEth(SPLIT_NONE).` (not `_distributeJackpotEth`)
- F-05: AdvanceGameGas.test.js line 1054 should read `// For terminal path: single call, 305 winners, _processDailyEth(SPLIT_NONE).` (not `_distributeJackpotEth`)

Run grep to confirm zero remaining `_distributeJackpotEth` references outside of docs/ and .planning/ directories. Report the verification result in the summary.

If any of F-01, F-04, F-05 are NOT fixed, do NOT edit the files. Instead, note the exact fix needed in the summary for user approval per project rules (NEVER modify contract or test files without explicit user approval).
  </action>
  <verify>
    <automated>grep -rn "_distributeJackpotEth" contracts/ test/ --include="*.sol" --include="*.js" --include="*.ts" | grep -v node_modules; test $? -eq 1 && echo "PASS: no stale refs in contracts/test" || echo "FAIL: stale refs found"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_distributeJackpotEth" contracts/modules/DegenerusGameGameOverModule.sol` returns 0
    - `grep -c "_distributeJackpotEth" test/gas/AdvanceGameGas.test.js` returns 0
    - `grep "_processDailyEth" contracts/modules/DegenerusGameGameOverModule.sol` includes line 170
    - `grep "_processDailyEth" test/gas/AdvanceGameGas.test.js` includes lines 1053-1054
  </acceptance_criteria>
  <done>
    All 3 stale reference findings (F-01, F-04, F-05) confirmed as already fixed in the working tree. Zero remaining `_distributeJackpotEth` references in contracts/ or test/ directories.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite JACKPOT-EVENT-CATALOG.md with correct event names, signatures, and line numbers</name>
  <files>docs/JACKPOT-EVENT-CATALOG.md</files>
  <read_first>
    - docs/JACKPOT-EVENT-CATALOG.md (current stale catalog -- understand existing structure)
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 59-105 for all event declarations, then grep all `emit Jackpot` and `emit FarFuture` lines for emitting paths)
    - contracts/modules/DegenerusGameAdvanceModule.sol (lines 51-52 for Advance and RewardJackpotsSettled declarations, grep `emit Advance` and `emit RewardJackpotsSettled` for paths)
    - contracts/modules/DegenerusGameDecimatorModule.sol (lines 29-50 for event declarations, grep `emit` for paths)
  </read_first>
  <action>
Rewrite JACKPOT-EVENT-CATALOG.md to fix the following known issues and bring it to current commit fa2b9c39.

**Known issues to fix:**

1. **Section A title and signature wrong:** The catalog says "JackpotTicketWinner" but the contract event is `JackpotTicketWin` (line 78). The signature fields are also wrong. Correct to:
   ```solidity
   event JackpotTicketWin(
       address indexed winner,
       uint24 indexed ticketLevel,
       uint8 indexed traitId,
       uint32 ticketCount,
       uint24 sourceLevel,
       uint256 ticketIndex
   );
   ```
   Update field descriptions table accordingly: `ticketLevel` is the level tickets are for (not `level`), `ticketCount` is ticket count (not `amount`), `sourceLevel` is the jackpot level, `ticketIndex` is the burn ticket index.

2. **Section A emitting paths stale:** Remove paths 2-4 which reference `_resolveTraitWinners` -- this function is dead code (declared at line 1312 but never called). The actual emitting paths for `JackpotTicketWin` are:
   - `_runEarlyBirdLootboxJackpot` (line 689, lootbox ticket winners)
   - `_awardJackpotTickets` (line 990, BAF lootbox and daily lootbox)
   Update line numbers by grep.

3. **Section C (AutoRebuyProcessed in JackpotModule) must be removed:** `AutoRebuyProcessed` no longer exists in JackpotModule. It was removed when `_processAutoRebuy` stopped emitting its own event (the auto-rebuy info is now embedded in `JackpotEthWin` via `rebuyLevel` and `rebuyTickets` fields). Only DecimatorModule still has it.

4. **Missing events -- add new sections:**
   - `JackpotBurnieWin` (declared line 88): emitted at line 1771 in `_awardDailyCoinToTraitWinners`. Fields: `winner`, `level`, `traitId`, `amount`, `ticketIndex`.
   - `JackpotDgnrsWin` (declared line 97): emitted at line 1450 in `_handleSoloBucketWinner`. Fields: `winner`, `amount`.
   - `JackpotWhalePassWin` (declared line 100): emitted at line 1436 in `_handleSoloBucketWinner` and line 2044 in BAF jackpot. Fields: `winner`, `level`, `halfPassCount`.

5. **Section A path 1 line number update:** `_processDailyEth` emit at line 1244 is likely stale. Grep `emit JackpotTicketWinner` -- wait, the event is `JackpotTicketWin` not `JackpotTicketWinner`. The path 1 in the old catalog says line 1244 for `_processDailyEth` but this is actually `JackpotEthWin` emits. The `JackpotTicketWin` emitting paths need to be found from scratch by grepping `emit JackpotTicketWin`.

6. **Update all line numbers** by grepping each `emit EventName` in the contracts. Every line number in the catalog must match the current code.

7. **Update Event-to-Path Matrix** at the bottom:
   - Remove `AutoRebuyProcessed (JackpotMod)` row
   - Rename `JackpotTicketWinner` to `JackpotTicketWin`
   - Add `JackpotBurnieWin` row (BURNIE Coin column: X)
   - Add `JackpotDgnrsWin` row (Daily (5) column: X)
   - Add `JackpotWhalePassWin` row (Daily (1-4): X, Daily (5): X, BAF: X)
   - Add `JackpotEthWin` row if not present (Daily (1-4): X, Daily (5): X, Early-Burn: X, Terminal: X, BAF: X)

8. **Update Cross-Consistency Check** table to reference correct event names and section numbers. The section numbers in JACKPOT-PAYOUT-REFERENCE.md will be the same (1-13) since Plan 01 preserves numbering.

9. **Header line:** `Last verified against: commit \`fa2b9c39\``

10. **Verify `_resolveTraitWinners` is NOT referenced** as an emitting path anywhere in the final catalog. It is dead code.

**Section lettering:** Preserve the existing catalog structure (Section A, B, C, ...) but reassign letters as needed when adding/removing sections. Keep the logical grouping: JackpotModule events first, then AdvanceModule, then DecimatorModule.
  </action>
  <verify>
    <automated>grep -c "JackpotTicketWinner" docs/JACKPOT-EVENT-CATALOG.md; test $? -eq 1 && echo "PASS: no stale JackpotTicketWinner" || echo "FAIL: stale event name found"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Last verified against" docs/JACKPOT-EVENT-CATALOG.md` contains `fa2b9c39`
    - `grep -c "JackpotTicketWinner" docs/JACKPOT-EVENT-CATALOG.md` returns 0 (stale name gone)
    - `grep -c "JackpotTicketWin" docs/JACKPOT-EVENT-CATALOG.md` returns >= 3 (correct name present)
    - `grep -c "JackpotBurnieWin" docs/JACKPOT-EVENT-CATALOG.md` returns >= 2 (new section added)
    - `grep -c "JackpotDgnrsWin" docs/JACKPOT-EVENT-CATALOG.md` returns >= 2 (new section added)
    - `grep -c "JackpotWhalePassWin" docs/JACKPOT-EVENT-CATALOG.md` returns >= 2 (section present)
    - `grep -c "JackpotEthWin" docs/JACKPOT-EVENT-CATALOG.md` returns >= 3 (section present with paths)
    - `grep -c "_resolveTraitWinners" docs/JACKPOT-EVENT-CATALOG.md` returns 0 (dead code not referenced)
    - `grep -c "AutoRebuyProcessed" docs/JACKPOT-EVENT-CATALOG.md` returns >= 1 (DecimatorModule still has it) but the JackpotModule section is removed
    - The Event-to-Path Matrix includes rows for: JackpotEthWin, JackpotTicketWin, JackpotBurnieWin, JackpotDgnrsWin, JackpotWhalePassWin, FarFutureCoinJackpotWinner, AutoRebuyProcessed (DecMod), Advance, RewardJackpotsSettled, DecBurnRecorded, TerminalDecBurnRecorded
  </acceptance_criteria>
  <done>
    JACKPOT-EVENT-CATALOG.md fully updated with:
    - Correct event names matching contract declarations (JackpotTicketWin not JackpotTicketWinner)
    - All 6 JackpotModule events cataloged (JackpotEthWin, JackpotTicketWin, JackpotBurnieWin, JackpotDgnrsWin, JackpotWhalePassWin, FarFutureCoinJackpotWinner)
    - AutoRebuyProcessed removed from JackpotModule (only DecimatorModule retains it)
    - Dead code _resolveTraitWinners removed from all emitting paths
    - All line numbers verified against current commit
    - Commit hash updated to fa2b9c39
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- this plan modifies documentation only, no contract or test code.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200-02 | Information Disclosure | docs/JACKPOT-EVENT-CATALOG.md | accept | Documentation is public. No secrets exposed. |
</threat_model>

<verification>
1. `grep -c "JackpotTicketWinner" docs/JACKPOT-EVENT-CATALOG.md` returns 0 (stale name removed)
2. `grep "fa2b9c39" docs/JACKPOT-EVENT-CATALOG.md` matches the verified-against line
3. `grep -c "_resolveTraitWinners" docs/JACKPOT-EVENT-CATALOG.md` returns 0 (dead code purged)
4. Zero `_distributeJackpotEth` references remain in contracts/ or test/ directories
5. All 6 JackpotModule events have dedicated catalog sections
</verification>

<success_criteria>
JACKPOT-EVENT-CATALOG.md accurately catalogs every jackpot event with correct Solidity signatures, field descriptions, emitting paths with verified line numbers, and cross-references to the updated payout reference. Stale reference findings F-01, F-04, F-05 confirmed as already resolved.
</success_criteria>

<output>
After completion, create `.planning/phases/200-payout-reference-rewrite/200-02-SUMMARY.md`
</output>
