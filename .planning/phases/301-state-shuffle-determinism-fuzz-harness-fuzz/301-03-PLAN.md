---
phase: 301-state-shuffle-determinism-fuzz-harness-fuzz
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-03-LOOTBOX-CLUSTER-contribution.sol
  - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-03-SUMMARY.md
autonomous: true
requirements:
  - FUZZ-03
  - FUZZ-04

must_haves:
  truths:
    - "Lootbox-cluster contribution authors 4 of the remaining 9 per-consumer fuzz functions (ResolveRedemptionLootbox + ResolveLootboxCommon + DegeneretteLootboxDirect + DecimatorAwardLootbox)."
    - "Each function uses the LOCKED 6-phase template from plan 01 verbatim — only per-consumer setUp + VRF-output capture lines differ."
    - "Each function cites the catalog §N entry for its consumer surface (§6 / §7 / §8 / §13)."
  artifacts:
    - path: ".planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-03-LOOTBOX-CLUSTER-contribution.sol"
      provides: "Lootbox-family per-consumer fuzz functions"
      contains: "testFuzz_RngLockDeterminism_ResolveRedemptionLootbox"
  key_links:
    - from: ".planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-03-LOOTBOX-CLUSTER-contribution.sol"
      to: "test/fuzz/RngLockDeterminism.t.sol"
      via: "Wave 2 aggregator concatenation"
      pattern: "testFuzz_RngLockDeterminism_(ResolveRedemptionLootbox|ResolveLootboxCommon|DegeneretteLootboxDirect|DecimatorAwardLootbox)"
---

<objective>
Author the lootbox-family cluster contribution. Covers 4 per-consumer fuzz functions:

- `testFuzz_RngLockDeterminism_ResolveRedemptionLootbox` — catalog §6 — `LootboxModule.resolveRedemptionLootbox` at file:line 707 — VRF-derived outputs: per-index lootbox ticket allocation, ticket-queue write position, BURNIE floor outcome
- `testFuzz_RngLockDeterminism_ResolveLootboxCommon` — catalog §7 — `LootboxModule._resolveLootboxCommon / _resolveLootboxRoll` at file:line 960 / 1623 — VRF-derived outputs: per-index lootbox outcome (ETH amount, BURNIE amount, ticket-bucket selection)
- `testFuzz_RngLockDeterminism_DegeneretteLootboxDirect` — catalog §8 — `DegeneretteModule._resolveLootboxDirect + inline consumer` at file:line 797 / 594 — VRF-derived outputs: degenerette-direct lootbox payout
- `testFuzz_RngLockDeterminism_DecimatorAwardLootbox` — catalog §13 — `DecimatorModule._awardDecimatorLootbox` cluster at file:line 573 + cross-call re-read at :338 — VRF-derived outputs: decimator-tier lootbox award (recipient, amount, tier)

Output: `.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-03-LOOTBOX-CLUSTER-contribution.sol` (paste source for Wave 2). Zero `contracts/` + `test/` mutations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-CONTEXT.md
@.planning/RNGLOCK-CATALOG.md
@test/fuzz/LootboxRngLifecycle.t.sol
@test/fuzz/helpers/DeployProtocol.sol

<interfaces>
Catalog §6/§7/§8/§13 consumer surfaces are documented at `.planning/RNGLOCK-CATALOG.md`:
- §6 (`resolveRedemptionLootbox`): grep `^## §6 — ` for line range
- §7 (`_resolveLootboxCommon / _resolveLootboxRoll`): grep `^## §7 — ` for line range
- §8 (`DegeneretteModule._resolveLootboxDirect`): grep `^## §8 — ` for line range
- §13 (`DecimatorModule._awardDecimatorLootbox`): grep `^## §13 — ` for line range

Each §N entry has CAT-01 traced function set + CAT-02 SLOAD table + CAT-03 writer enumeration + CAT-04 verdict matrix. The SLOAD table identifies the participating slots; the VRF-derived outputs are computed from these slots SLOAD'd inside the resolution loop.

LootboxRngLifecycle.t.sol contains relevant precedent setUp patterns: `_setupForMidDayRng()` (lines 81..101) arranges to the lootbox VRF-request boundary; `_readLootboxRngIndex()` (line 104) reads the lootbox index; `_lootboxRngWord(uint48 index)` (line 109) reads the per-index VRF word from the mapping at slot 39.

Scaffold helpers from plan 01: `_perturb`, `_advanceToVrfRequestBoundary`, `_deliverMockVrf`, `_snapshotPreLock`, `_revertToPreLock`, `_assertVrfOutputByteIdentity`, `_completeDay`. Available via concatenation at Wave 2 aggregation time.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ResolveRedemptionLootbox + ResolveLootboxCommon functions</name>
  <files>.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-03-LOOTBOX-CLUSTER-contribution.sol</files>
  <read_first>
    - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-CONTEXT.md (full — D-301-HARNESS-ARCH-01 6-phase template; D-301-COVERAGE-01 13-consumer enumeration)
    - .planning/RNGLOCK-CATALOG.md §6 (ResolveRedemptionLootbox — locate via `grep -n "^## §6 — "`)
    - .planning/RNGLOCK-CATALOG.md §7 (ResolveLootboxCommon — locate via `grep -n "^## §7 — "`)
    - test/fuzz/LootboxRngLifecycle.t.sol (full — load-bearing for lootbox setUp + per-index storage readers)
    - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-PLAN.md (locked 6-phase template; helper signatures)
  </read_first>
  <action>
    Create `.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-03-LOOTBOX-CLUSTER-contribution.sol`. Paste-source file; no contract header; no closing `}`.

    Open: `// ANCHOR: CLUSTER_LOOTBOX_OPEN` + NatSpec citing D-301-COVERAGE-01 enumeration.

    Author `// ANCHOR: FUNC_ResolveRedemptionLootbox` followed by `function testFuzz_RngLockDeterminism_ResolveRedemptionLootbox(uint256 vrfWord, uint256 perturbSeed) public` per 6-phase template:
    - Setup: arrange to the lootbox-RNG VRF-request boundary using a pattern derived from `LootboxRngLifecycle.t.sol::_setupForMidDayRng()` — complete day 1, warp +1 day, complete day 2, then issue a redemption-eligible lootbox purchase via `game.purchase{value: 1.01 ether}(buyer, 400, 1 ether, bytes32(0), MintPaymentKind.DirectEth)` to create pending lootbox ETH, fund VRF subscription with LINK (`mockVRF.fundSubscription(1, 100e18)`), then call `game.requestLootboxRng()` to fire the redemption-lootbox VRF request. Reference scaffold pattern.
    - Lock: `uint256 reqId = mockVRF.lastRequestId(); assertTrue(game.rngLocked()); uint256 lockSnap = vm.snapshot();`
    - Perturbation: `_perturb(perturbSeed); assertTrue(game.rngLocked());`
    - Resolution: `_deliverMockVrf(reqId, vrfWord);` After fulfillment, capture per-index lootbox ticket allocation snapshot. Per catalog §6 SLOAD table, the consumer SLOADs include `lootboxRngWordByIndex[index]` (slot 39 — see LootboxRngLifecycle.t.sol::_lootboxRngWord helper), `ticketWriteSlot`, `currentPrizePool`. The VRF-derived outputs are: per-index ticket count allocation, ticket queue write position post-resolution, BURNIE floor outcome. Read these via direct storage SLOAD (mirror `_readLootboxRngIndex` / `_lootboxRngWord` patterns). Pack: `bytes32 perturbedOutputs = keccak256(abi.encode(ticketsAllocated, ticketWriteSlotAfter, burnieAwarded));`
    - Baseline: `_revertToPreLock(preLockSnap);` re-execute setup + lock + resolution without `_perturb`. Capture baseline.
    - Assert: `_assertVrfOutputByteIdentity(perturbedOutputs, baselineOutputs, "ResolveRedemptionLootbox VRF outputs must be byte-identical under perturbation");`

    Per-line NatSpec cites `RNGLOCK-CATALOG.md §6` + relevant SLOAD-table entries per `feedback_verify_call_graph_against_source.md` + `feedback_rng_window_storage_read_freshness.md` (enumerate ALL SLOADs inside rng-window).

    Trailing `// ANCHOR: FUNC_ResolveRedemptionLootbox_END`.

    Author `// ANCHOR: FUNC_ResolveLootboxCommon` followed by `function testFuzz_RngLockDeterminism_ResolveLootboxCommon(uint256 vrfWord, uint256 perturbSeed, uint256 lootboxIndexSeed) public` per 6-phase template:
    - Setup: arrange to the manual-open lootbox boundary — purchase lootbox, advance to VRF-request, fulfill the lootbox-RNG VRF (so `lootboxRngWordByIndex[index] != 0`), then arrange the manual `openLootBox(index)` call boundary (where `_resolveLootboxCommon` / `_resolveLootboxRoll` consume the per-index VRF word). The fuzzed `lootboxIndexSeed` selects which pending lootbox index to open. **NOTE**: Per catalog §7, this consumer's VRF input is the per-index `lootboxRngWordByIndex[index]` slot which is set at VRF callback time — the "rngLock window" for §7 is the (purchase-time → openLootBox-time) commitment window, not the (advanceGame → VRF callback) lock. The fuzz function asserts that mutations between purchase and open do not alter the open-time outcome. Adjust the 6-phase template to use the `lootboxRngLocked` window or equivalent flag if applicable; consult catalog §7 SLOAD table for the actual gating slot.
    - Lock: identify the commitment-window boundary per catalog §7 (likely the per-index commitment slots set at purchase time).
    - Perturbation: `_perturb(perturbSeed);` (some perturbations may not be effective for this consumer if its window is purchase-time-bounded; the assert is still valid).
    - Resolution: `game.openLootBox(index);` Capture VRF-derived outputs: ETH-payout amount, BURNIE-payout amount, ticket-bucket selection. Read via post-call balance deltas or storage SLOAD per catalog §7.
    - Baseline + Assert as template.

    Trailing `// ANCHOR: FUNC_ResolveLootboxCommon_END`.

    Zero `contracts/` mutations.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-03-LOOTBOX-CLUSTER-contribution.sol && \
      grep -q "testFuzz_RngLockDeterminism_ResolveRedemptionLootbox" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-03-LOOTBOX-CLUSTER-contribution.sol && \
      grep -q "testFuzz_RngLockDeterminism_ResolveLootboxCommon" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-03-LOOTBOX-CLUSTER-contribution.sol && \
      grep -q "RNGLOCK-CATALOG.md §6" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-03-LOOTBOX-CLUSTER-contribution.sol && \
      grep -q "RNGLOCK-CATALOG.md §7" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-03-LOOTBOX-CLUSTER-contribution.sol && \
      [ -z "$(git status --porcelain contracts/)" ] && [ -z "$(git status --porcelain test/)" ] && echo OK
    </automated>
  </verify>
  <done>Both lootbox-redemption + lootbox-common functions authored using 6-phase template; catalog §6/§7 cited; zero contracts/+test/ mutations.</done>
</task>

<task type="auto">
  <name>Task 2: Author DegeneretteLootboxDirect + DecimatorAwardLootbox functions + SUMMARY</name>
  <files>
    .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-03-LOOTBOX-CLUSTER-contribution.sol
    .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-03-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-03-LOOTBOX-CLUSTER-contribution.sol (Task 1 output — append)
    - .planning/RNGLOCK-CATALOG.md §8 (DegeneretteLootboxDirect — locate via `grep -n "^## §8 — "`)
    - .planning/RNGLOCK-CATALOG.md §13 (DecimatorAwardLootbox — locate via `grep -n "^## §13 — "`)
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    APPEND to `.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-03-LOOTBOX-CLUSTER-contribution.sol`.

    Add `// ANCHOR: FUNC_DegeneretteLootboxDirect` followed by `function testFuzz_RngLockDeterminism_DegeneretteLootboxDirect(uint256 vrfWord, uint256 perturbSeed) public` per 6-phase template. Per catalog §8, the consumer fires when a degenerette bet wins and triggers a direct lootbox award. Setup: place a degenerette bet with parameters that maximize win probability for the bootstrap fuzz iteration (or use `vm.assume` to filter losing iterations). VRF-derived outputs per §8: degenerette-direct lootbox payout (ETH + BURNIE + ticket allocation). NatSpec cites RNGLOCK-CATALOG.md §8.

    Trailing `// ANCHOR: FUNC_DegeneretteLootboxDirect_END`.

    Add `// ANCHOR: FUNC_DecimatorAwardLootbox` followed by `function testFuzz_RngLockDeterminism_DecimatorAwardLootbox(uint256 vrfWord, uint256 perturbSeed) public` per 6-phase template. Per catalog §13, the consumer is `_awardDecimatorLootbox` at DecimatorModule.sol:573 with cross-call re-read at :338 — fires during decimator-tier resolution. Setup: advance game state into the decimator window (consult catalog §13 SLOAD table + DecimatorModule source for the trigger conditions). VRF-derived outputs: decimator-tier lootbox award (recipient, amount, tier). Watch for the cross-call re-read concern (`feedback_rng_window_storage_read_freshness.md`) — the consumer reads the VRF-word slot twice across two function calls; both reads must produce identical results. NatSpec cites RNGLOCK-CATALOG.md §13.

    Trailing `// ANCHOR: FUNC_DecimatorAwardLootbox_END` and cluster-close `// ANCHOR: CLUSTER_LOOTBOX_END`.

    Author `.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-03-SUMMARY.md` per GSD summary template — document the 4 functions authored, their consumer surfaces, anchor list, zero contracts/+test/ mutations.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-03-LOOTBOX-CLUSTER-contribution.sol && \
      grep -q "testFuzz_RngLockDeterminism_DegeneretteLootboxDirect" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-03-LOOTBOX-CLUSTER-contribution.sol && \
      grep -q "testFuzz_RngLockDeterminism_DecimatorAwardLootbox" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-03-LOOTBOX-CLUSTER-contribution.sol && \
      grep -q "// ANCHOR: CLUSTER_LOOTBOX_END" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-03-LOOTBOX-CLUSTER-contribution.sol && \
      grep -q "RNGLOCK-CATALOG.md §8" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-03-LOOTBOX-CLUSTER-contribution.sol && \
      grep -q "RNGLOCK-CATALOG.md §13" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-03-LOOTBOX-CLUSTER-contribution.sol && \
      test -f .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-03-SUMMARY.md && \
      [ -z "$(git status --porcelain contracts/)" ] && [ -z "$(git status --porcelain test/)" ] && echo OK
    </automated>
  </verify>
  <done>All 4 lootbox-cluster functions authored; SUMMARY emitted; zero contracts/+test/ mutations.</done>
</task>

</tasks>

<verification>
- 301-03-LOOTBOX-CLUSTER-contribution.sol contains 4 testFuzz functions
- Each function cites catalog §N
- 301-03-SUMMARY.md exists
- git status --porcelain contracts/ + test/ returns empty
</verification>

<success_criteria>
Lootbox-family cluster contribution exists ready for Wave 2 aggregation. All 4 functions follow the locked 6-phase template.
</success_criteria>

<output>
Create `.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-03-SUMMARY.md` when done.
</output>
