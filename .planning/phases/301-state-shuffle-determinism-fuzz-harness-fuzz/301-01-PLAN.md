---
phase: 301-state-shuffle-determinism-fuzz-harness-fuzz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-SCAFFOLD-contribution.sol
  - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-SUMMARY.md
autonomous: true
requirements:
  - FUZZ-01
  - FUZZ-03
  - FUZZ-04

must_haves:
  truths:
    - "Scaffold contribution declares the canonical contract header (SPDX + pragma + imports + `contract RngLockDeterminism is DeployProtocol`) that Wave 2 aggregator pastes verbatim into `test/fuzz/RngLockDeterminism.t.sol`."
    - "Scaffold contribution defines all shared helper functions (`_advanceToVrfRequestBoundary`, `_deliverMockVrf`, `_snapshotPreLock`, `_revertToPreLock`, `_assertVrfOutputByteIdentity`, storage-slot constants) that all 18 fuzz functions invoke — no function re-implements scaffolding logic."
    - "Scaffold contribution authors the first 2 of 13 per-consumer fuzz functions — `testFuzz_RngLockDeterminism_PayDailyJackpot` (consumer §1) and `testFuzz_RngLockDeterminism_RunTerminalJackpot` (consumer §3) — as the LOCKED REFERENCE TEMPLATE for the 6-phase per-function structure (setup → lock → perturb → resolve → baseline → assert). All sibling cluster plans (02, 03, 04) author their per-consumer functions by copying this template's structure verbatim and substituting only the per-consumer setUp + assertion-target lines."
    - "Scaffold contribution compiles standalone via local syntax check (executor uses a sentinel temp file `forge build` check; final file lives only at aggregation time in Wave 2)."
  artifacts:
    - path: ".planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-SCAFFOLD-contribution.sol"
      provides: "Canonical contract header + shared helpers + 2 reference fuzz functions (PayDailyJackpot + RunTerminalJackpot) — Wave 2 aggregator paste source"
      contains: "contract RngLockDeterminism is DeployProtocol"
  key_links:
    - from: ".planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-SCAFFOLD-contribution.sol"
      to: "test/fuzz/RngLockDeterminism.t.sol"
      via: "Wave 2 aggregator (301-06-PLAN.md) concatenation"
      pattern: "contract RngLockDeterminism is DeployProtocol"
---

<objective>
Author the canonical scaffold contribution for the Phase 301 Foundry harness `test/fuzz/RngLockDeterminism.t.sol`. The scaffold contribution is the source-of-truth for: (1) the contract header (SPDX + pragma + imports + DeployProtocol inheritance), (2) shared helper functions used by all 18 sibling fuzz functions, (3) shared storage-slot constants for state inspection, (4) two LOCKED REFERENCE TEMPLATE fuzz functions (`testFuzz_RngLockDeterminism_PayDailyJackpot` + `testFuzz_RngLockDeterminism_RunTerminalJackpot`) demonstrating the 6-phase per-function structure that cluster plans 02/03/04 will replicate verbatim.

Purpose: Lock the template ONCE so cluster plans 02-04 do not re-derive the 6-phase structure. Eliminates inconsistency across the 18 functions. Mirrors Phase 299 Wave-1 cluster-contribution shape — this file is a CONTRIBUTION not the final harness file; Wave 2 plan 06 aggregates this scaffold + cluster contributions 02/03/04/05 into the canonical `test/fuzz/RngLockDeterminism.t.sol`.

Output: Single contribution file `.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-SCAFFOLD-contribution.sol` containing the contract header + shared helpers + 2 reference fuzz functions. Zero `contracts/` mutations. Zero writes to `test/` tree at this plan; final harness file written by Wave 2 aggregator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-CONTEXT.md
@.planning/RNGLOCK-CATALOG.md
@test/fuzz/LootboxRngLifecycle.t.sol
@test/fuzz/helpers/DeployProtocol.sol
@test/fuzz/helpers/VRFHandler.sol
@foundry.toml

<interfaces>
<!-- Key contracts the scaffold uses. Extracted from existing test/fuzz/ helpers. -->

From test/fuzz/helpers/DeployProtocol.sol:
- `abstract contract DeployProtocol is Test`
- public state: `mockVRF`, `game`, `admin`, `jackpotModule`, `decimatorModule`, `lootboxModule`, `degeneretteModule`, `mintModule`, `whaleModule`, `gameOverModule`, `coin`, `coinflip`, `sdgnrs`, `affiliate`, `gnrus`, `vault`
- `function _deployProtocol() internal` — call from `setUp()`

From test/fuzz/helpers/VRFHandler.sol:
- `contract VRFHandler is Test`
- `constructor(MockVRFCoordinator vrf_, DegenerusGame game_)`
- `function fulfillVrf(uint256 randomWord) external`

From test/fuzz/LootboxRngLifecycle.t.sol (precedent storage-inspection patterns):
- `SLOT_PACKED_0 = 0` / `SLOT_RNG_WORD_CURRENT = 3` / `SLOT_VRF_REQUEST_ID = 4` constants
- `_readRngWordCurrent()` / `_readVrfRequestId()` direct-storage readers
- `_completeDay(uint256 vrfWord)` pattern: `game.advanceGame()` → `mockVRF.fulfillRandomWords(reqId, vrfWord)` → loop on `game.rngLocked()`

Catalog §1 consumer surface (`PayDailyJackpot`): `JackpotModule.payDailyJackpot(false, ...)` at `contracts/modules/DegenerusGameJackpotModule.sol:339`. VRF-derived outputs: jackpot recipient address, ethJackpotAmount, hero-symbol override, trait-burn-ticket selection.

Catalog §3 consumer surface (`RunTerminalJackpot`): `JackpotModule.runTerminalJackpot` at `contracts/modules/DegenerusGameJackpotModule.sol:278`. VRF-derived outputs: terminal jackpot recipient set, per-bucket payout split, hero-symbol override.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author scaffold contribution file with canonical contract header + shared helpers + 2 reference fuzz functions</name>
  <files>.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-SCAFFOLD-contribution.sol</files>
  <read_first>
    - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-CONTEXT.md (D-301-HARNESS-ARCH-01 6-phase template; D-301-COVERAGE-01 13-consumer name list; D-301-VMSKIP-MECHANISM-01 Option C skip-block format)
    - .planning/RNGLOCK-CATALOG.md §1 (PayDailyJackpot consumer surface; SLOAD table; verdict matrix) — lines 685..1018
    - .planning/RNGLOCK-CATALOG.md §3 (RunTerminalJackpot consumer surface) — lines 1277..1500 (read enough to identify VRF-derived outputs)
    - test/fuzz/LootboxRngLifecycle.t.sol lines 1..230 (canonical Foundry fuzz pattern: DeployProtocol inheritance + setUp + storage-slot readers + VRFHandler usage + day-boundary helpers)
    - test/fuzz/helpers/DeployProtocol.sol (full file)
    - test/fuzz/helpers/VRFHandler.sol (full file)
    - foundry.toml (confirm `[profile.deep.fuzz] runs = 10000` already exists — D-301-FORGE-CONFIG-01 satisfied by `FOUNDRY_PROFILE=deep` invocation at verification time; no project-default mutation needed)
  </read_first>
  <action>
    Create `.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-SCAFFOLD-contribution.sol` as a self-contained Solidity contribution file. The file is NOT compiled in place — it is a paste source for Wave 2. Structure the file with explicit ANCHOR comments so the aggregator can locate paste regions:

    1. **Header anchor** (`// ANCHOR: HEADER`): SPDX-License-Identifier `AGPL-3.0-only`; `pragma solidity ^0.8.26;`; imports — `DeployProtocol` from `./helpers/DeployProtocol.sol`, `VRFHandler` from `./helpers/VRFHandler.sol`, `MockVRFCoordinator` from `../../contracts/mocks/MockVRFCoordinator.sol`, and any concrete module/contract identifier the reference functions need (`MintPaymentKind` from `../../contracts/interfaces/IDegenerusGame.sol` for purchase calls; concrete types per cluster needs).

    2. **Contract declaration anchor** (`// ANCHOR: CONTRACT_OPEN`): `contract RngLockDeterminism is DeployProtocol {` — note Wave 2 aggregator will paste the closing `}` after the last cluster function. Add NatSpec `/// @title RngLockDeterminism — Foundry fuzz harness asserting byte-identical VRF-derived outputs under mid-rngLock-window state perturbations.` and a reference to `D-301-HARNESS-ARCH-01` + `D-301-COVERAGE-01` per `feedback_no_history_in_comments.md` (the comment describes WHAT IS, not what changed).

    3. **State anchor** (`// ANCHOR: STATE`): declare `VRFHandler public vrfHandler;` + storage-slot constants `SLOT_PACKED_0 = 0`, `SLOT_RNG_WORD_CURRENT = 3`, `SLOT_VRF_REQUEST_ID = 4` (mirror LootboxRngLifecycle.t.sol pattern). Add any additional slot constants required by reference functions; document slot derivation via NatSpec citing `forge inspect DegenerusGame storage-layout`.

    4. **setUp anchor** (`// ANCHOR: SETUP`): `function setUp() public { _deployProtocol(); vm.warp(block.timestamp + 1 days); vrfHandler = new VRFHandler(mockVRF, game); mockVRF.fundSubscription(1, 100e18); }` — fund the VRF subscription with LINK so VRF requests fire mid-test without dedicated per-test setUp. Match LootboxRngLifecycle.t.sol convention.

    5. **Shared helpers anchor** (`// ANCHOR: SHARED_HELPERS`): author the following helper functions (no per-consumer specialization — all 18 fuzz functions call these verbatim):
       - `_advanceToVrfRequestBoundary()` → advances state until `game.rngLocked() == true` AND `mockVRF.lastRequestId() != 0` AND the request is unfulfilled. Returns the pending requestId. Uses `_completeDay` to bootstrap one cycle if needed, then `vm.warp(block.timestamp + 1 days)` + `game.advanceGame()` to arm the next request.
       - `_deliverMockVrf(uint256 reqId, uint256 word)` → `mockVRF.fulfillRandomWords(reqId, word)` then loops `game.advanceGame()` up to N times to drain resolution phases (mirror `_completeDay` loop in LootboxRngLifecycle.t.sol).
       - `_snapshotPreLock()` → returns `uint256 snapshotId = vm.snapshot()`. Conventional wrapper for symmetry with `_revertToPreLock`.
       - `_revertToPreLock(uint256 snapshotId)` → `vm.revertTo(snapshotId)`.
       - `_assertVrfOutputByteIdentity(bytes32 perturbed, bytes32 baseline, string memory label)` → `assertEq(perturbed, baseline, label)`. Per-function specializations hash the VRF-derived outputs into a single bytes32 before calling this helper (e.g., `keccak256(abi.encode(recipient, amount, heroByte))`).
       - `_completeDay(uint256 vrfWord)` → ported from LootboxRngLifecycle.t.sol; advances game, fulfills VRF, drains lock loop. **DO NOT re-author** — copy verbatim from the precedent file.
       - Storage-slot readers: `_readRngWordCurrent()`, `_readVrfRequestId()` — port verbatim from LootboxRngLifecycle.t.sol.

    6. **Perturbation action library anchor** (`// ANCHOR: ACTION_LIBRARY`): author a single `_perturb(uint256 seed)` internal helper that the per-consumer fuzz functions call inside their perturbation phase. The helper draws an action class from `seed % N_ACTIONS` and executes ONE action from the FUZZ-02 action set. Action classes (one switch case each; cite FUZZ-02 + Phase 300 ADMA-01 §1 for admin function enumeration):
       - `0`: bet via `degeneretteModule._placeDegeneretteBetCore` route — `vm.deal(actor, 1 ether); vm.prank(actor); game.placeDegeneretteBet(...)` with bounded `(quadrant, symbol, amount)` derived from seed bits.
       - `1`: mint via `game.purchase{value: ...}` with `MintPaymentKind.DirectEth` (bounded numCoins).
       - `2`: claim via `game.claimWinnings()` from a pre-funded actor.
       - `3`: ERC20 transfer via `coin.transfer(...)` between two actors (BURNIE).
       - `4`: ERC721 transfer via `dgnrs.transferFrom(from, to, tokenId)` (only if tokenId exists; else `vm.assume(false)` to reject the fuzz iteration).
       - `5`: ERC20 approval via `coin.approve(spender, amount)`.
       - `6`: affiliate registration via `affiliate.register(...)` (signature per actual function; consult `contracts/DegenerusAffiliate.sol` if needed — only if straightforward).
       - `7-N`: admin/owner function calls — pick one from ADMA-01 R-01..R-22 enumeration; use `vm.prank(address(admin))` for admin-gated calls. Cite `.planning/ADMIN-AUDIT.md §3.NN` in the case comment.
       - `N+1`: `retryLootboxRng()` invocation — `vm.warp(block.timestamp + 6 hours + 1); game.retryLootboxRng();` per the failsafe cooldown gate.

       For each action class, if the action class requires state preconditions that aren't trivially satisfiable in the fuzz iteration, use `try ... catch { return; }` so the perturbation silently no-ops without failing the iteration. Document this pattern in a NatSpec block on `_perturb`.

       Action class count: total ≥ 9 (the 7 base classes + admin + retry). Specific count is executor's discretion within the action-set FUZZ-02 envelope. Pick a count that fits in `seed % N_ACTIONS` cleanly.

    7. **Reference fuzz function 1 anchor** (`// ANCHOR: FUNC_PayDailyJackpot`): `function testFuzz_RngLockDeterminism_PayDailyJackpot(uint256 vrfWord, uint256 perturbSeed) public` with the canonical 6-phase template per `D-301-HARNESS-ARCH-01`:
       - Phase 1 (Setup): `vm.assume(vrfWord != 0); uint256 preLockSnap = _snapshotPreLock();` — arrange state so the next `advanceGame()` will request VRF for the daily-jackpot consumer surface. Per catalog §1, this means: complete day 1 to seed `dailyIdx`, then warp 1 day, then bootstrap a single ETH bet/mint via `degeneretteModule.placeDegeneretteBet` or `game.purchase` so the daily-jackpot pool is non-zero. Document each line with NatSpec citing catalog §1.
       - Phase 2 (Lock): `game.advanceGame(); uint256 reqId = mockVRF.lastRequestId(); assertTrue(game.rngLocked(), "rngLock must engage"); assertTrue(reqId != 0, "VRF request must be pending");` Capture `uint256 lockSnap = vm.snapshot();`.
       - Phase 3 (Perturbation): execute `_perturb(perturbSeed)`; assert `game.rngLocked()` is still true post-perturbation (lock did not lift).
       - Phase 4 (Resolution under perturbation): `_deliverMockVrf(reqId, vrfWord);` capture VRF-derived outputs into local variables — for §1 these are: (a) `address jackpotRecipient` read from `game.lastJackpotRecipient()` or equivalent public getter (executor: verify the actual getter name via `grep -rn "jackpotRecipient\|lastJackpotWinner" contracts/` if needed), (b) `uint256 jackpotAmount` from public getter, (c) `uint8 heroByte` derived from the most recent hero-override event or storage slot. Pack into `bytes32 perturbedOutputs = keccak256(abi.encode(jackpotRecipient, jackpotAmount, heroByte));`.
       - Phase 5 (Baseline): `_revertToPreLock(preLockSnap);` re-execute Phase 1 + Phase 2 WITHOUT calling `_perturb` (lock + VRF delivery only). Capture baseline outputs into `bytes32 baselineOutputs`.
       - Phase 6 (Assert): `_assertVrfOutputByteIdentity(perturbedOutputs, baselineOutputs, "PayDailyJackpot VRF outputs must be byte-identical under perturbation");` — **WITHOUT** a `vm.skip` block at this plan. Skip blocks land at Wave 2 aggregator per `D-301-VMSKIP-MECHANISM-01`. If the assertion fails at Wave 2 verification, the aggregator adds the appropriate skip block.

       Per-line NatSpec comments cite specific decisions/catalog sections per `feedback_no_history_in_comments.md` discipline.

    8. **Reference fuzz function 2 anchor** (`// ANCHOR: FUNC_RunTerminalJackpot`): `function testFuzz_RngLockDeterminism_RunTerminalJackpot(uint256 vrfWord, uint256 perturbSeed) public` — same 6-phase template; Phase 1 setup advances state to the terminal-jackpot trigger (catalog §3 — requires `_livenessTriggered() && !gameOver` precondition; executor: read catalog §3 SLOAD table to identify the trigger conditions and arrange them). VRF-derived outputs per §3 are recipient-set + per-bucket payout split + hero-byte. If the terminal-trigger preconditions cannot be arranged in a fuzz iteration, the function may use `vm.assume(<precondition>)` to filter unsuitable iterations.

    9. **Trailing marker** (`// ANCHOR: SCAFFOLD_END`): trailing comment `// END Plan 01 scaffold contribution; cluster plans 02/03/04 append fuzz functions; edge-case plan 05 appends edge-case functions; Wave 2 plan 06 aggregates + appends closing `}` + adds vm.skip blocks per `D-301-VMSKIP-MECHANISM-01`.`

    The contribution file does NOT have a closing `}` for the contract — Wave 2 closes it. The file is INTENTIONALLY non-compilable in isolation; this is a paste source.

    Verification approach: Author the file. THEN temporarily concatenate it into a sandbox copy `/tmp/sandbox_RngLockDeterminism.t.sol` with a closing `}` appended, run `forge build` against that sandbox file to confirm syntax + import resolution. Remove the sandbox afterward. **DO NOT** write the sandbox file into `test/`.

    Zero `contracts/` mutations per `D-43N-AUDIT-ONLY-01`. Zero writes to `test/` tree at this plan.
  </action>
  <verify>
    <automated>
      # Verify contribution file exists and contains the required anchors
      test -f .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-SCAFFOLD-contribution.sol && \
      grep -q "// ANCHOR: HEADER" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-SCAFFOLD-contribution.sol && \
      grep -q "// ANCHOR: CONTRACT_OPEN" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-SCAFFOLD-contribution.sol && \
      grep -q "// ANCHOR: SETUP" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-SCAFFOLD-contribution.sol && \
      grep -q "// ANCHOR: SHARED_HELPERS" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-SCAFFOLD-contribution.sol && \
      grep -q "// ANCHOR: ACTION_LIBRARY" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-SCAFFOLD-contribution.sol && \
      grep -q "// ANCHOR: FUNC_PayDailyJackpot" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-SCAFFOLD-contribution.sol && \
      grep -q "// ANCHOR: FUNC_RunTerminalJackpot" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-SCAFFOLD-contribution.sol && \
      grep -q "testFuzz_RngLockDeterminism_PayDailyJackpot" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-SCAFFOLD-contribution.sol && \
      grep -q "testFuzz_RngLockDeterminism_RunTerminalJackpot" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-SCAFFOLD-contribution.sol && \
      echo "OK: scaffold contribution complete"

      # Confirm zero contracts/ + test/ tree mutations
      [ -z "$(git status --porcelain contracts/)" ] && echo "OK: contracts/ untouched" || (echo "FAIL: contracts/ mutated"; exit 1)
      [ -z "$(git status --porcelain test/)" ] && echo "OK: test/ untouched" || (echo "FAIL: test/ mutated"; exit 1)
    </automated>
  </verify>
  <done>
    Contribution file `301-01-SCAFFOLD-contribution.sol` exists in the phase directory with all 8 anchors + 2 reference functions; sandbox `forge build` (executor-internal, file deleted after) confirmed syntax validity; git status shows zero `contracts/` + `test/` mutations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write SUMMARY</name>
  <files>.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-SUMMARY.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md
    - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-PLAN.md
  </read_first>
  <action>
    Author `.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-SUMMARY.md` per the GSD summary template. Record: scaffold contribution file produced; 8 anchors authored; 2 reference fuzz functions (PayDailyJackpot + RunTerminalJackpot); shared helper inventory; action-library class count; locked 6-phase template that cluster plans 02/03/04 will replicate.
  </action>
  <verify>
    <automated>test -f .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-SUMMARY.md && echo OK</automated>
  </verify>
  <done>SUMMARY.md exists and documents the scaffold contribution's contents.</done>
</task>

</tasks>

<verification>
- 301-01-SCAFFOLD-contribution.sol exists with all 8 anchors
- 2 reference fuzz functions present (PayDailyJackpot + RunTerminalJackpot)
- Sandbox `forge build` (executor-internal) confirmed syntactic validity
- git status --porcelain contracts/ returns empty
- git status --porcelain test/ returns empty
- 301-01-SUMMARY.md exists
</verification>

<success_criteria>
Wave 2 aggregator can paste the scaffold contribution + cluster contributions into `test/fuzz/RngLockDeterminism.t.sol` with mechanical concatenation (no semantic re-derivation). Cluster plans 02/03/04 have a locked 6-phase template to replicate. Zero `contracts/` mutations.
</success_criteria>

<output>
Create `.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-SUMMARY.md` when done.
</output>
