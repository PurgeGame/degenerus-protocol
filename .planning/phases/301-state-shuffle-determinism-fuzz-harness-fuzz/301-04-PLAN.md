---
phase: 301-state-shuffle-determinism-fuzz-harness-fuzz
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-MIXED-CLUSTER-contribution.sol
  - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-SUMMARY.md
autonomous: true
requirements:
  - FUZZ-03
  - FUZZ-04

must_haves:
  truths:
    - "Mixed-cluster contribution authors the remaining 5 per-consumer fuzz functions (MintTraitGeneration + BurnieCoinflipResolve + StakedStonkRedemption + GameOverRngSubstitution + RetryLootboxRng)."
    - "After plans 01+02+03+04, all 13 CAT-01 consumer surfaces are covered by ≥1 fuzz function (FUZZ-04 satisfied)."
    - "The RetryLootboxRng function uses OPPOSITE-DIRECTION assertion semantics per D-301-COVERAGE-01 (the failsafe SHOULD change VRF-derived output because it supplies a fresh VRF word)."
    - "Each function cites the catalog §N entry for its consumer surface (§10, §11, §12, §5, §9)."
  artifacts:
    - path: ".planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-MIXED-CLUSTER-contribution.sol"
      provides: "Mint/Coinflip/Stonk/GameOver/Retry per-consumer fuzz functions"
      contains: "testFuzz_RngLockDeterminism_RetryLootboxRng"
  key_links:
    - from: ".planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-MIXED-CLUSTER-contribution.sol"
      to: "test/fuzz/RngLockDeterminism.t.sol"
      via: "Wave 2 aggregator concatenation"
      pattern: "testFuzz_RngLockDeterminism_(MintTraitGeneration|BurnieCoinflipResolve|StakedStonkRedemption|GameOverRngSubstitution|RetryLootboxRng)"
---

<objective>
Author the mixed-cluster contribution covering the 5 remaining per-consumer fuzz functions after plans 01+02+03:

- `testFuzz_RngLockDeterminism_MintTraitGeneration` — catalog §10 — `MintModule trait-generation consumer (Phase 290 MINTCLN audit-subject surface)` — VRF-derived outputs: per-minted-token trait bytes (color + symbol + quadrant + boosters)
- `testFuzz_RngLockDeterminism_BurnieCoinflipResolve` — catalog §11 — `BurnieCoinflip._resolveFlip + win-decode` at file:line 807 / 837 — VRF-derived outputs: coinflip resolution (win/loss + payout)
- `testFuzz_RngLockDeterminism_StakedStonkRedemption` — catalog §12 — `StakedDegenerusStonk.resolveRedemptionPeriod + rngWordForDay re-read` at file:line 585 / 670 — VRF-derived outputs: redemption-period roll, per-player redemption allocation (TIER-1 / CATASTROPHE — V-184 anchor; FIXREC §103)
- `testFuzz_RngLockDeterminism_GameOverRngSubstitution` — catalog §5 — `GameOverModule rngWordByDay substitution` at file:line 100 — VRF-derived outputs: game-over substitution outcome
- `testFuzz_RngLockDeterminism_RetryLootboxRng` — catalog §9 — `AdvanceModule.retryLootboxRng` at file:line 1132 — **OPPOSITE-DIRECTION assertion** per D-301-COVERAGE-01: the failsafe supplies a FRESH VRF word so VRF-derived outputs SHOULD differ from baseline; the test asserts that the difference is bounded to the VRF-word-substitution change (and does NOT include any state-perturbation drift). This is the exempt-path coverage entry.

Output: `.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-MIXED-CLUSTER-contribution.sol`. Zero `contracts/` + `test/` mutations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-CONTEXT.md
@.planning/RNGLOCK-CATALOG.md
@.planning/RNGLOCK-FIXREC.md
@test/fuzz/LootboxRngLifecycle.t.sol
@test/fuzz/helpers/DeployProtocol.sol

<interfaces>
Catalog §5/§9/§10/§11/§12 consumer surfaces at `.planning/RNGLOCK-CATALOG.md` (locate via `grep -n "^## §N — "`).

FIXREC §103 (V-184 CATASTROPHE-tier, sStonk cross-day re-roll) at `.planning/RNGLOCK-FIXREC.md` is the load-bearing anchor for the StakedStonkRedemption fuzz function — this function MUST exercise the redemption-period roll consumer, which is the single confirmed CATASTROPHE in the FIXREC tier register. The fuzz function asserts byte-identity (vm.skip will be added at Wave 2 since V-184 reproduces a VIOLATION at current contract state).

Retry-failsafe semantics per Phase 296 retryLootboxRng commit `123f2dac` and `D-42N-RETRY-RNG-DOMAIN-SEP-01` Option A: the failsafe replaces the stalled VRF request with a fresh request; the per-index commitment slots are preserved; only the VRF word changes. The fuzz function for RetryLootboxRng asserts:
1. POST-retry VRF-derived outputs DIFFER from pre-retry outputs (failsafe supplies fresh word — outputs MUST differ)
2. POST-retry VRF-derived outputs are byte-identical between (with-perturbation) and (without-perturbation) baselines (state-perturbations during the failsafe DO NOT additionally drift outputs)

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author MintTraitGeneration + BurnieCoinflipResolve functions</name>
  <files>.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-MIXED-CLUSTER-contribution.sol</files>
  <read_first>
    - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-CONTEXT.md
    - .planning/RNGLOCK-CATALOG.md §10 (`grep -n "^## §10 — "`) — MintTraitGeneration; this is the Phase 290 MINTCLN audit-subject surface; large §
    - .planning/RNGLOCK-CATALOG.md §11 (`grep -n "^## §11 — "`) — BurnieCoinflip._resolveFlip + win-decode
    - test/fuzz/LootboxRngLifecycle.t.sol (pattern reference; the `_makePurchase` helper at lines 120..127 is the trait-mint trigger pattern)
    - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-PLAN.md (locked 6-phase template; helper signatures)
  </read_first>
  <action>
    Create `.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-MIXED-CLUSTER-contribution.sol`. Paste-source; no contract header; no closing `}`.

    Open: `// ANCHOR: CLUSTER_MIXED_OPEN` + NatSpec citing D-301-COVERAGE-01.

    Author `// ANCHOR: FUNC_MintTraitGeneration` followed by `function testFuzz_RngLockDeterminism_MintTraitGeneration(uint256 vrfWord, uint256 perturbSeed, uint16 numCoinsSeed) public` per 6-phase template:
    - Setup: complete day 1 to seed `dailyIdx`, warp +1 day, issue a multi-token mint via `game.purchase{value: numCoins * unitPrice + 0.01 ether}(buyer, numCoins, 0, bytes32(0), MintPaymentKind.DirectEth)` with `numCoins = bound(numCoinsSeed, 400, 800)` (or whatever the actual min/max range is per `contracts/modules/DegenerusGameMintModule.sol` — executor: grep for the `MIN_PURCHASE_COINS` / `MAX_PURCHASE_COINS` constants to determine the valid range). The mint queues per-token VRF-trait-generation work; the next `advanceGame()` fires the VRF request for trait generation.
    - Lock: `game.advanceGame(); uint256 reqId = mockVRF.lastRequestId(); assertTrue(game.rngLocked());`
    - Perturbation: `_perturb(perturbSeed);`
    - Resolution: `_deliverMockVrf(reqId, vrfWord);` After fulfillment, capture per-minted-token trait bytes via `dgnrs.tokenURI(tokenId)` or equivalent trait getter, OR by reading the on-chain trait storage slot (per Phase 290 MINTCLN audit, traits are derived inline from the VRF word + per-token salt). Pack: `bytes32 perturbedOutputs = keccak256(allTraits);` where `allTraits` is the concatenation of trait bytes across all minted tokens.
    - Baseline + Assert per template.

    Per-line NatSpec cites `RNGLOCK-CATALOG.md §10` + Phase 290 MINTCLN audit precedent. NatSpec block notes this is the Phase 290 audit-subject surface (3-input keccak + owed-in-baseKey collapse; cite `D-42N-EVT-BREAK-01`).

    Trailing `// ANCHOR: FUNC_MintTraitGeneration_END`.

    Author `// ANCHOR: FUNC_BurnieCoinflipResolve` followed by `function testFuzz_RngLockDeterminism_BurnieCoinflipResolve(uint256 vrfWord, uint256 perturbSeed) public` per 6-phase template:
    - Setup: deposit BURNIE into the coinflip pool via `coinflip.depositCoinflip(...)` (executor: grep `contracts/BurnieCoinflip.sol` for the deposit entry point's actual signature); arrange to the coinflip VRF-request boundary.
    - Lock: same template.
    - Perturbation: `_perturb(perturbSeed);` Specifically include a `coinflip.depositCoinflip` perturbation iteration in the action library if not already present — the coinflip-deposit-mid-lock attack is a relevant vector per Phase 296 (xiv) discussion.
    - Resolution: `_deliverMockVrf(reqId, vrfWord);` capture win/loss outcome + payout via `coinflip` public getters or storage SLOAD per catalog §11.
    - Baseline + Assert.

    NatSpec cites RNGLOCK-CATALOG.md §11.

    Trailing `// ANCHOR: FUNC_BurnieCoinflipResolve_END`.

    Zero `contracts/` mutations.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-MIXED-CLUSTER-contribution.sol && \
      grep -q "testFuzz_RngLockDeterminism_MintTraitGeneration" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-MIXED-CLUSTER-contribution.sol && \
      grep -q "testFuzz_RngLockDeterminism_BurnieCoinflipResolve" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-MIXED-CLUSTER-contribution.sol && \
      grep -q "RNGLOCK-CATALOG.md §10" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-MIXED-CLUSTER-contribution.sol && \
      grep -q "RNGLOCK-CATALOG.md §11" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-MIXED-CLUSTER-contribution.sol && \
      [ -z "$(git status --porcelain contracts/)" ] && [ -z "$(git status --porcelain test/)" ] && echo OK
    </automated>
  </verify>
  <done>Mint + Coinflip functions authored; catalog §10/§11 cited; zero contracts/+test/ mutations.</done>
</task>

<task type="auto">
  <name>Task 2: Author StakedStonkRedemption + GameOverRngSubstitution + RetryLootboxRng functions + SUMMARY</name>
  <files>
    .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-MIXED-CLUSTER-contribution.sol
    .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-MIXED-CLUSTER-contribution.sol (Task 1 output — append)
    - .planning/RNGLOCK-CATALOG.md §12 (`grep -n "^## §12 — "`) — StakedDegenerusStonk.resolveRedemptionPeriod
    - .planning/RNGLOCK-CATALOG.md §5 (`grep -n "^## §5 — "`) — GameOverModule rngWordByDay substitution
    - .planning/RNGLOCK-CATALOG.md §9 (`grep -n "^## §9 — "`) — AdvanceModule.retryLootboxRng
    - .planning/RNGLOCK-FIXREC.md §103 (`grep -n "^## §103 — "`) — V-184 CATASTROPHE
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    APPEND to `.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-MIXED-CLUSTER-contribution.sol`.

    Add `// ANCHOR: FUNC_StakedStonkRedemption` followed by `function testFuzz_RngLockDeterminism_StakedStonkRedemption(uint256 vrfWord, uint256 perturbSeed, uint256 burnAmountSeed) public` per 6-phase template. Per catalog §12 + FIXREC §103 (V-184 CATASTROPHE), this consumer reads `redemptionPeriodIndex` + `pendingRedemptionEthBase` + `pendingRedemptionBurnieBase` + `pendingRedemptions[player]` slots; the V-184 attack vector is a 1-wei `burn` mid-period that re-arms `pendingRedemptionEthBase` and forces re-roll. The fuzz function arranges:
    - Setup: arrange to the sStonk-redemption resolution boundary. Stake sDGNRS, advance time across redemption-period boundaries, submit a redemption claim via `sdgnrs.burn(amount)` or equivalent burn-to-claim entry (executor: grep `contracts/StakedDegenerusStonk.sol` for the redemption entry point).
    - Lock: per 6-phase template.
    - Perturbation: `_perturb(perturbSeed);` This function's perturbation library SHOULD include a 1-wei `sdgnrs.burn(1)` action that targets the V-184 exploit class. If `_perturb` does not already include this, the function can locally invoke an additional `try sdgnrs.burn(1) catch {}` perturbation step inside Phase 3, guarded by `perturbSeed % 7 == 0` (or similar mod gate).
    - Resolution: `_deliverMockVrf(reqId, vrfWord);` capture redemption-period roll + per-player redemption allocation via storage SLOAD per catalog §12.
    - Baseline + Assert. The assertion will FAIL at current contract state per V-184; the Wave 2 aggregator adds a `vm.skip` block citing `RNGLOCK-FIXREC.md §103` + `D-43N-V44-HANDOFF-111`.

    NatSpec cites RNGLOCK-CATALOG.md §12 + RNGLOCK-FIXREC.md §103 (CATASTROPHE-tier — load-bearing for Wave 2 vm.skip).

    Trailing `// ANCHOR: FUNC_StakedStonkRedemption_END`.

    Add `// ANCHOR: FUNC_GameOverRngSubstitution` followed by `function testFuzz_RngLockDeterminism_GameOverRngSubstitution(uint256 vrfWord, uint256 perturbSeed) public` per 6-phase template. Per catalog §5, the consumer is `GameOverModule rngWordByDay substitution` at file:line 100 — fires when `gameOver` triggers and substitutes the canonical rng word for game-over-resolution. Setup: advance game state into the game-over window (consult catalog §5 + GameOverModule source for the trigger conditions). VRF-derived outputs: game-over substitution outcome (per-player payout per game-over rules).

    NatSpec cites RNGLOCK-CATALOG.md §5.

    Trailing `// ANCHOR: FUNC_GameOverRngSubstitution_END`.

    Add `// ANCHOR: FUNC_RetryLootboxRng` followed by `function testFuzz_RngLockDeterminism_RetryLootboxRng(uint256 vrfWord1, uint256 vrfWord2, uint256 perturbSeed) public` per the OPPOSITE-DIRECTION template (D-301-COVERAGE-01 line 9 explicit specification):
    - Setup: arrange to a lootbox-RNG VRF-request boundary; the request will be intentionally stalled (do not deliver VRF for ≥6h).
    - Lock: `uint256 reqId1 = mockVRF.lastRequestId(); assertTrue(game.rngLocked());`
    - Perturbation: `_perturb(perturbSeed);`
    - Stall window: `vm.warp(block.timestamp + 6 hours + 1);` to clear the retry cooldown gate.
    - Failsafe invocation: `game.retryLootboxRng();` — this issues a fresh VRF request replacing the original.
    - Resolution-A: `uint256 reqId2 = mockVRF.lastRequestId(); assertTrue(reqId2 != reqId1, "retry must replace original reqId"); _deliverMockVrf(reqId2, vrfWord1);` Capture VRF-derived output (per-index lootbox allocation) as `bytes32 retryOutputs`.
    - Baseline-A (NO retry): `_revertToPreLock(preLockSnap);` re-execute setup + lock + perturb + DELIVER the original VRF request (no retry) with `vrfWord2 != vrfWord1`. Capture `bytes32 originalOutputs`.
    - Assert-A (OPPOSITE-DIRECTION): `assertNotEq(retryOutputs, originalOutputs, "retryLootboxRng MUST change VRF-derived outputs — failsafe supplies fresh word");`
    - Baseline-B (retry without perturbation): `_revertToPreLock(preLockSnap);` re-execute setup + lock + stall + retry + deliver `vrfWord1` WITHOUT calling `_perturb`. Capture `bytes32 retryNoPerturbOutputs`.
    - Assert-B (BYTE-IDENTITY across retry path): `_assertVrfOutputByteIdentity(retryOutputs, retryNoPerturbOutputs, "retryLootboxRng VRF outputs must be byte-identical between perturbation and baseline retry paths");`

    NatSpec cites RNGLOCK-CATALOG.md §9 + Phase 296 retryLootboxRng commit `123f2dac` + `D-42N-RETRY-RNG-DOMAIN-SEP-01` Option A.

    Trailing `// ANCHOR: FUNC_RetryLootboxRng_END` + cluster-close `// ANCHOR: CLUSTER_MIXED_END`.

    Author `.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-SUMMARY.md` per GSD summary template. Document the 5 functions authored, their catalog §N references, the OPPOSITE-DIRECTION semantics for RetryLootboxRng, V-184 CATASTROPHE flag on StakedStonkRedemption, anchor list, zero contracts/+test/ mutations.

    **Coverage attestation in SUMMARY:** confirm all 13 CAT-01 consumer surfaces now have ≥1 fuzz function after plans 01+02+03+04 (FUZZ-04 satisfied):
    1. PayDailyJackpot (§1, plan 01)
    2. PayDailyJackpotCoinAndTickets (§2, plan 02)
    3. RunTerminalJackpot (§3, plan 01)
    4. RunTerminalDecimatorJackpot (§4, plan 02)
    5. GameOverRngSubstitution (§5, plan 04)
    6. ResolveRedemptionLootbox (§6, plan 03)
    7. ResolveLootboxCommon (§7, plan 03)
    8. DegeneretteLootboxDirect (§8, plan 03)
    9. RetryLootboxRng (§9, plan 04 — exempt-path opposite-direction)
    10. MintTraitGeneration (§10, plan 04)
    11. BurnieCoinflipResolve (§11, plan 04)
    12. StakedStonkRedemption (§12, plan 04)
    13. DecimatorAwardLootbox (§13, plan 03)
  </action>
  <verify>
    <automated>
      test -f .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-MIXED-CLUSTER-contribution.sol && \
      grep -q "testFuzz_RngLockDeterminism_StakedStonkRedemption" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-MIXED-CLUSTER-contribution.sol && \
      grep -q "testFuzz_RngLockDeterminism_GameOverRngSubstitution" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-MIXED-CLUSTER-contribution.sol && \
      grep -q "testFuzz_RngLockDeterminism_RetryLootboxRng" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-MIXED-CLUSTER-contribution.sol && \
      grep -q "// ANCHOR: CLUSTER_MIXED_END" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-MIXED-CLUSTER-contribution.sol && \
      grep -q "assertNotEq" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-MIXED-CLUSTER-contribution.sol && \
      grep -q "RNGLOCK-FIXREC.md §103" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-MIXED-CLUSTER-contribution.sol && \
      test -f .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-SUMMARY.md && \
      [ -z "$(git status --porcelain contracts/)" ] && [ -z "$(git status --porcelain test/)" ] && echo OK
    </automated>
  </verify>
  <done>All 5 mixed-cluster functions authored (with OPPOSITE-DIRECTION semantics on RetryLootboxRng); SUMMARY emitted with FUZZ-04 13-consumer coverage attestation; zero contracts/+test/ mutations.</done>
</task>

</tasks>

<verification>
- 301-04-MIXED-CLUSTER-contribution.sol contains 5 testFuzz functions
- RetryLootboxRng uses assertNotEq + assertEq dual-direction
- StakedStonkRedemption cites RNGLOCK-FIXREC.md §103 (V-184)
- 301-04-SUMMARY.md documents 13/13 consumer coverage
- git status --porcelain contracts/ + test/ returns empty
</verification>

<success_criteria>
After plans 01+02+03+04, all 13 CAT-01 consumer surfaces are covered (FUZZ-04 satisfied). Mixed-cluster contribution ready for Wave 2 aggregation.
</success_criteria>

<output>
Create `.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-SUMMARY.md` when done.
</output>
