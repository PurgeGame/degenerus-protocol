---
phase: 301-state-shuffle-determinism-fuzz-harness-fuzz
plan: 06
type: execute
wave: 2
depends_on:
  - 301-01
  - 301-02
  - 301-03
  - 301-04
  - 301-05
files_modified:
  - test/fuzz/RngLockDeterminism.t.sol
  - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-06-SUMMARY.md
autonomous: true
requirements:
  - FUZZ-01
  - FUZZ-02
  - FUZZ-03
  - FUZZ-04
  - FUZZ-05

must_haves:
  truths:
    - "Canonical harness file `test/fuzz/RngLockDeterminism.t.sol` exists with the contract header from plan 01 scaffold + all 13 per-consumer fuzz functions from plans 01/02/03/04 + all 5 edge-case fuzz functions from plan 05 + closing `}`."
    - "Total function count in the file: 18 (13 per-consumer + 5 edge-case) per D-301-COVERAGE-01 + D-301-EDGE-CASES-01."
    - "`vm.skip` blocks added per `D-301-VMSKIP-MECHANISM-01` Option C — each skip block has cross-reference comment `// SKIP: RNGLOCK-FIXREC.md §{N} — {brief VIOLATION summary} — v44.0 D-43N-V44-HANDOFF-{NN} flips this to strict assertion`."
    - "`forge test --match-path test/fuzz/RngLockDeterminism.t.sol` PASSES with `FOUNDRY_PROFILE=deep` (10k runs per D-301-RUNS-01)."
    - "AGENT-COMMITTED batched test-tree commit lands per D-301-WAVE-SHAPE-01 — single commit including test/fuzz/RngLockDeterminism.t.sol + ROADMAP plan-checklist updates + SUMMARY files. Zero `contracts/` mutations."
  artifacts:
    - path: "test/fuzz/RngLockDeterminism.t.sol"
      provides: "Canonical Phase 301 Foundry fuzz harness — 18 fuzz functions; ships as regression oracle for v44.0 FIX-MILESTONE consumption"
      contains: "contract RngLockDeterminism is DeployProtocol"
    - path: ".planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-06-SUMMARY.md"
      provides: "Aggregator summary documenting canonical-file assembly, vm.skip block inventory with FIXREC §N + HANDOFF-NN cross-references, forge test result, commit SHA"
      contains: "MILESTONE_V43_PHASE_301"
  key_links:
    - from: "test/fuzz/RngLockDeterminism.t.sol vm.skip blocks"
      to: ".planning/RNGLOCK-FIXREC.md §N entries + D-43N-V44-HANDOFF-NN anchors"
      via: "Cross-reference comment per D-301-VMSKIP-MECHANISM-01 Option C"
      pattern: "SKIP: RNGLOCK-FIXREC.md §"
    - from: "test/fuzz/RngLockDeterminism.t.sol"
      to: "v44.0 FIX-MILESTONE plan-phase"
      via: "Regression oracle — v44.0 flips each vm.skip to strict assertion as FIX-NN sub-phases land"
      pattern: "v44.0 D-43N-V44-HANDOFF-"
---

<objective>
Aggregate the 5 Wave-1 contributions (`301-01-SCAFFOLD-contribution.sol` + `301-02-JACKPOT-CLUSTER-contribution.sol` + `301-03-LOOTBOX-CLUSTER-contribution.sol` + `301-04-MIXED-CLUSTER-contribution.sol` + `301-05-EDGECASE-CLUSTER-contribution.sol`) into the canonical single-file Foundry harness `test/fuzz/RngLockDeterminism.t.sol` per `D-301-HARNESS-ARCH-01`. Add `vm.skip` blocks per `D-301-VMSKIP-MECHANISM-01` Option C for fuzz cases that reproduce a CATALOG VIOLATION at current contract state. Verify the harness via `forge test --match-path test/fuzz/RngLockDeterminism.t.sol` with `FOUNDRY_PROFILE=deep` (10k runs per D-301-RUNS-01). Commit per `D-301-WAVE-SHAPE-01` (AGENT-COMMITTED batched test commit; test-tree only; zero `contracts/` mutations).

Purpose: Materialize the canonical harness file. Mirror Phase 299's Wave-2 aggregator pattern. The harness ships as v43.0's sole `test/` mutation and serves as regression oracle for v44.0 FIX-MILESTONE consumption — each vm.skip cross-references the FIXREC §N entry + v44.0 HANDOFF-NN anchor so v44.0 plan-phase consumes the harness as load-bearing acceptance-test input.

Output: `test/fuzz/RngLockDeterminism.t.sol` (canonical harness) + `301-06-SUMMARY.md` (aggregation summary + vm.skip inventory + forge test result + commit SHA). AGENT-COMMITTED batched test-tree commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-CONTEXT.md
@.planning/RNGLOCK-CATALOG.md
@.planning/RNGLOCK-FIXREC.md
@.planning/ADMIN-AUDIT.md
@.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-SCAFFOLD-contribution.sol
@.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-02-JACKPOT-CLUSTER-contribution.sol
@.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-03-LOOTBOX-CLUSTER-contribution.sol
@.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-MIXED-CLUSTER-contribution.sol
@.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-05-EDGECASE-CLUSTER-contribution.sol
@foundry.toml

<interfaces>
Anchor inventory across the 5 contribution files (the aggregator concatenates in this order):

From 301-01-SCAFFOLD-contribution.sol:
- HEADER, CONTRACT_OPEN, STATE, SETUP, SHARED_HELPERS, ACTION_LIBRARY, FUNC_PayDailyJackpot, FUNC_RunTerminalJackpot, SCAFFOLD_END

From 301-02-JACKPOT-CLUSTER-contribution.sol:
- CLUSTER_JACKPOT_OPEN, FUNC_PayDailyJackpotCoinAndTickets, FUNC_RunTerminalDecimatorJackpot, CLUSTER_JACKPOT_END

From 301-03-LOOTBOX-CLUSTER-contribution.sol:
- CLUSTER_LOOTBOX_OPEN, FUNC_ResolveRedemptionLootbox, FUNC_ResolveLootboxCommon, FUNC_DegeneretteLootboxDirect, FUNC_DecimatorAwardLootbox, CLUSTER_LOOTBOX_END

From 301-04-MIXED-CLUSTER-contribution.sol:
- CLUSTER_MIXED_OPEN, FUNC_MintTraitGeneration, FUNC_BurnieCoinflipResolve, FUNC_StakedStonkRedemption, FUNC_GameOverRngSubstitution, FUNC_RetryLootboxRng, CLUSTER_MIXED_END

From 301-05-EDGECASE-CLUSTER-contribution.sol:
- CLUSTER_EDGECASE_OPEN, HELPER_perturbAdminOnly, FUNC_EdgeCase_AdminDuringLock, FUNC_EdgeCase_NearEndOfWindow, FUNC_EdgeCase_MultiTxBatch, FUNC_EdgeCase_MultiBlock, FUNC_EdgeCase_RetryLootboxRngDuringLock, CLUSTER_EDGECASE_END

vm.skip target inventory (from RNGLOCK-FIXREC.md §0.4 + §0.5 EV-tier breakdown — fuzz cases that reproduce CATALOG VIOLATIONs at current contract state):

| Fuzz function | Reproduces VIOLATION? | FIXREC §N | HANDOFF anchor | Skip required? |
|---|---|---|---|---|
| testFuzz_RngLockDeterminism_PayDailyJackpot | Yes — §1/§2/§3 V-003..V-005 hero-override wagers (MEDIUM) | §1/§2/§3 | HANDOFF-01/02/03 | Yes |
| testFuzz_RngLockDeterminism_PayDailyJackpotCoinAndTickets | Yes — same V-003..V-005 cluster | §1/§2/§3 | HANDOFF-01/02/03 | Yes |
| testFuzz_RngLockDeterminism_RunTerminalJackpot | Yes — V-024/V-025/V-027/V-031 prizePoolsPacked inflation (MEDIUM-HIGH) | §13/§14/§16/§17 (etc) | HANDOFF-13/14/16/17 | Yes |
| testFuzz_RngLockDeterminism_RunTerminalDecimatorJackpot | Yes — similar inflation cluster | §13..§17 | HANDOFF-13..17 | Yes |
| testFuzz_RngLockDeterminism_GameOverRngSubstitution | Yes — V-054/V-057/V-063/V-065 claimablePool gameover writers (HIGH/MEDIUM) | §27..§33 | HANDOFF-31, etc | Yes |
| testFuzz_RngLockDeterminism_ResolveRedemptionLootbox | Yes — Cluster G G-cluster commitment-window slots | §43..§62 | HANDOFF-23..62 (subsumption) | Yes |
| testFuzz_RngLockDeterminism_ResolveLootboxCommon | Yes — same Cluster G | §43..§62 | HANDOFF-23..62 | Yes |
| testFuzz_RngLockDeterminism_DegeneretteLootboxDirect | Maybe — depends on slot writers; verify via forge test outcome | §8 region | TBD | Verify-at-run |
| testFuzz_RngLockDeterminism_RetryLootboxRng | NO — opposite-direction assertion; the failsafe legitimately changes outputs | n/a | n/a | NO skip (test is opposite-direction) |
| testFuzz_RngLockDeterminism_MintTraitGeneration | Yes — V-127 lastPurchaseDay (RESOLVED-AS-PHANTOM per §0.7) + Cluster H mintPacked writers | §0.7 marker | HANDOFF-77 (phantom) | Verify-at-run; if phantom marker holds, NO skip |
| testFuzz_RngLockDeterminism_BurnieCoinflipResolve | Yes — V-182 bountyOwedTo + Phase 296 (xiv) entropy-correlation Tier-1 ACCEPT_AS_DOCUMENTED | §102 | HANDOFF-110 | Yes |
| testFuzz_RngLockDeterminism_StakedStonkRedemption | Yes — V-184 CATASTROPHE (the headline finding) | §103 | HANDOFF-111 | Yes |
| testFuzz_RngLockDeterminism_DecimatorAwardLootbox | Yes — V-175/V-201/V-202 decimator writers | §98/§110/§111 | HANDOFF-99..102 | Yes |
| testFuzz_EdgeCase_AdminDuringLock | Yes — multiple admin writers per ADMA-03 R-01..R-22 | Multiple §N | Multiple HANDOFF anchors | Yes |
| testFuzz_EdgeCase_NearEndOfWindow | Yes — same VIOLATION classes as base PayDailyJackpot | inherited | inherited | Yes |
| testFuzz_EdgeCase_MultiTxBatch | Yes — same | inherited | inherited | Yes |
| testFuzz_EdgeCase_MultiBlock | Yes — same | inherited | inherited | Yes |
| testFuzz_EdgeCase_RetryLootboxRngDuringLock | Maybe — depends on whether perturbation alters retry-replaced-VRF-derived outputs at current state; verify at run | tbd | tbd | Verify-at-run |

The aggregator's job at run-time is to: (1) attempt all 18 functions WITHOUT vm.skip first, (2) for each function that FAILS, add a `vm.skip(true)` block at the top of the function body with the cross-reference comment, (3) re-run `forge test`, (4) iterate until all PASS or SKIP. Functions marked "Verify-at-run" may or may not need skip blocks depending on actual contract behavior — the aggregator decides empirically.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Concatenate contributions into canonical test/fuzz/RngLockDeterminism.t.sol</name>
  <files>test/fuzz/RngLockDeterminism.t.sol</files>
  <read_first>
    - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-01-SCAFFOLD-contribution.sol
    - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-02-JACKPOT-CLUSTER-contribution.sol
    - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-03-LOOTBOX-CLUSTER-contribution.sol
    - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-04-MIXED-CLUSTER-contribution.sol
    - .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-05-EDGECASE-CLUSTER-contribution.sol
    - test/fuzz/LootboxRngLifecycle.t.sol (precedent header style)
  </read_first>
  <action>
    Create `test/fuzz/RngLockDeterminism.t.sol`. Use the Write tool, NOT heredoc.

    Concatenation order (mechanical):
    1. Paste 301-01-SCAFFOLD-contribution.sol content from `// ANCHOR: HEADER` through `// ANCHOR: SCAFFOLD_END` (excludes the trailing scaffold-end marker line itself — keep as a comment for traceability).
    2. Paste 301-02-JACKPOT-CLUSTER-contribution.sol content from `// ANCHOR: CLUSTER_JACKPOT_OPEN` through `// ANCHOR: CLUSTER_JACKPOT_END`.
    3. Paste 301-03-LOOTBOX-CLUSTER-contribution.sol content from `// ANCHOR: CLUSTER_LOOTBOX_OPEN` through `// ANCHOR: CLUSTER_LOOTBOX_END`.
    4. Paste 301-04-MIXED-CLUSTER-contribution.sol content from `// ANCHOR: CLUSTER_MIXED_OPEN` through `// ANCHOR: CLUSTER_MIXED_END`.
    5. Paste 301-05-EDGECASE-CLUSTER-contribution.sol content from `// ANCHOR: CLUSTER_EDGECASE_OPEN` through `// ANCHOR: CLUSTER_EDGECASE_END`.
    6. Append closing `}` (the contract-level close that plan 01 deliberately omitted).

    Resolve import-merge issues: if any cluster's anchor block references an import that the scaffold didn't include, hoist that import to the top of the file. Deduplicate imports.

    Resolve helper-merge issues: if any cluster declared a helper inside its anchor block that overlaps with the scaffold's `// ANCHOR: SHARED_HELPERS` set, keep ONLY the scaffold's version. The cluster contributions were authored expecting the scaffold helpers to be present at concatenation time; no duplication should arise. If duplication DOES arise (e.g., plan 05's `_perturbAdminOnly` is unique to plan 05 and SHOULD persist), keep both.

    Verify the assembled file:
    ```
    forge build --skip test 2>&1 | tee /tmp/forge-build.log
    grep -q "Compiler run successful" /tmp/forge-build.log
    ```

    If `forge build` fails due to syntax/import errors, fix in `test/fuzz/RngLockDeterminism.t.sol` directly (the contribution files are static paste sources; fixes land in the canonical file). Iterate until `forge build` PASSES.

    **Function count gate**: confirm exactly 18 fuzz functions in the canonical file:
    ```
    PER_CONSUMER=$(grep -cE "function testFuzz_RngLockDeterminism_" test/fuzz/RngLockDeterminism.t.sol)
    EDGE_CASE=$(grep -cE "function testFuzz_EdgeCase_" test/fuzz/RngLockDeterminism.t.sol)
    [ "$PER_CONSUMER" -eq 13 ] && [ "$EDGE_CASE" -eq 5 ] && echo "OK: 13 + 5 = 18 functions"
    ```

    Zero `contracts/` mutations per `D-43N-AUDIT-ONLY-01`.
  </action>
  <verify>
    <automated>
      test -f test/fuzz/RngLockDeterminism.t.sol && \
      grep -q "contract RngLockDeterminism is DeployProtocol" test/fuzz/RngLockDeterminism.t.sol && \
      [ "$(grep -cE 'function testFuzz_RngLockDeterminism_' test/fuzz/RngLockDeterminism.t.sol)" -eq 13 ] && \
      [ "$(grep -cE 'function testFuzz_EdgeCase_' test/fuzz/RngLockDeterminism.t.sol)" -eq 5 ] && \
      forge build 2>&1 | grep -q "Compiler run successful" && \
      [ -z "$(git status --porcelain contracts/)" ] && echo "OK: contracts/ untouched"
    </automated>
  </verify>
  <done>Canonical harness file exists with 18 fuzz functions; forge build PASSES; zero contracts/ mutations.</done>
</task>

<task type="auto">
  <name>Task 2: Add vm.skip blocks per D-301-VMSKIP-MECHANISM-01 Option C, run forge test, iterate</name>
  <files>test/fuzz/RngLockDeterminism.t.sol</files>
  <read_first>
    - .planning/RNGLOCK-FIXREC.md §0.4 + §0.5 (tier register; identifies which fuzz cases reproduce VIOLATIONs)
    - .planning/RNGLOCK-FIXREC.md §0.6 (subsumption map; identifies HANDOFF anchors that close multiple catalog rows)
    - .planning/RNGLOCK-FIXREC.md §103 (V-184 CATASTROPHE — the headline VIOLATION on StakedStonkRedemption)
    - test/fuzz/RngLockDeterminism.t.sol (the canonical file produced in Task 1)
  </read_first>
  <action>
    Run the harness with the deep profile (10k runs per D-301-RUNS-01):
    ```
    FOUNDRY_PROFILE=deep forge test --match-path test/fuzz/RngLockDeterminism.t.sol 2>&1 | tee /tmp/forge-test-initial.log
    ```

    For each FAILING fuzz function, add a `vm.skip(true)` block at the top of the function body per `D-301-VMSKIP-MECHANISM-01` Option C. The skip block MUST include a cross-reference comment in the exact format:

    ```
    // SKIP: RNGLOCK-FIXREC.md §{N} — {brief VIOLATION summary} — v44.0 D-43N-V44-HANDOFF-{NN} flips this to strict assertion
    vm.skip(true);
    ```

    Source the §N + HANDOFF-NN values from `.planning/RNGLOCK-FIXREC.md`. Examples (resolve actual mapping at run time):
    - `testFuzz_RngLockDeterminism_StakedStonkRedemption` → `// SKIP: RNGLOCK-FIXREC.md §103 — V-184 sStonk cross-day re-roll CATASTROPHE — v44.0 D-43N-V44-HANDOFF-111 flips this to strict assertion`
    - `testFuzz_RngLockDeterminism_PayDailyJackpot` → `// SKIP: RNGLOCK-FIXREC.md §1 — V-003 dailyHeroWagers writer race — v44.0 D-43N-V44-HANDOFF-01 flips this to strict assertion`

    For fuzz cases that PASS without a skip, leave them unskipped — these are the "VERIFICATION-ONLY" gates already present in source per FIXREC §0.7 (HANDOFF-04/05/06/28/32/34/39/41/81/94/103 etc).

    For the `testFuzz_RngLockDeterminism_RetryLootboxRng` opposite-direction function: this MUST NOT be skipped (D-301-COVERAGE-01 line 9 specification). If it fails for an unexpected reason, debug — do NOT skip-mask.

    After adding skip blocks, re-run:
    ```
    FOUNDRY_PROFILE=deep forge test --match-path test/fuzz/RngLockDeterminism.t.sol 2>&1 | tee /tmp/forge-test-final.log
    grep -E "Test result|passing|failing|skipped" /tmp/forge-test-final.log
    ```

    Iterate until: (a) `forge test` reports ALL PASS or SKIP (no FAIL), (b) the PASS count + SKIP count = 18.

    Document the final skip inventory: for each skipped function, record §N + HANDOFF-NN + brief VIOLATION summary. This inventory will appear in 301-06-SUMMARY.md (Task 3).

    **forge test PASS gate** (D-301-VERIFICATION-01):
    ```
    grep -E "Suite result: ok\." /tmp/forge-test-final.log
    ```
    Must succeed before the task is `done`.

    Zero `contracts/` mutations per D-43N-AUDIT-ONLY-01.

    NOTE on test timing: 10k runs × 18 functions can take 30-90+ minutes. The aggregator may use `FOUNDRY_PROFILE=default` (1k runs) for iteration speed during development, then `FOUNDRY_PROFILE=deep` for the final attestation run. The COMMIT-time verification MUST use `FOUNDRY_PROFILE=deep` per `D-43N-FUZZ-RUNS-01` 10k requirement.
  </action>
  <verify>
    <automated>
      # Verify forge test PASSES with deep profile
      FOUNDRY_PROFILE=deep forge test --match-path test/fuzz/RngLockDeterminism.t.sol 2>&1 | tee /tmp/forge-test-verify.log | grep -E "Suite result: ok\."
      # Count skip blocks
      SKIP_COUNT=$(grep -cE "^\s*//\s*SKIP: RNGLOCK-FIXREC\.md §" test/fuzz/RngLockDeterminism.t.sol)
      echo "vm.skip blocks: $SKIP_COUNT"
      # Confirm each skip block has v44.0 handoff anchor cross-reference
      grep -cE "v44\.0 D-43N-V44-HANDOFF-" test/fuzz/RngLockDeterminism.t.sol
      # Confirm RetryLootboxRng is NOT skipped (opposite-direction assertion must run)
      ! sed -n '/testFuzz_RngLockDeterminism_RetryLootboxRng/,/^    }/p' test/fuzz/RngLockDeterminism.t.sol | grep -q "vm.skip(true)" && echo "OK: RetryLootboxRng not skipped"
      # Zero contracts/ mutations
      [ -z "$(git status --porcelain contracts/)" ] && echo "OK: contracts/ untouched"
    </automated>
  </verify>
  <done>`FOUNDRY_PROFILE=deep forge test --match-path test/fuzz/RngLockDeterminism.t.sol` reports `Suite result: ok.` (all 18 functions PASS or SKIP, no FAIL); every skip block has cross-reference comment to FIXREC §N + HANDOFF-NN; RetryLootboxRng is NOT skipped; zero contracts/ mutations.</done>
</task>

<task type="auto">
  <name>Task 3: Author SUMMARY + update ROADMAP plan checklist + AGENT-COMMITTED batched commit</name>
  <files>
    .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-06-SUMMARY.md
    .planning/ROADMAP.md
    .planning/STATE.md
  </files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md
    - .planning/ROADMAP.md (Phase 301 entry — locate plan-checklist if present; if absent, the SUMMARY is the entry-point)
    - .planning/STATE.md (current position; update Phase 301 status to complete)
    - test/fuzz/RngLockDeterminism.t.sol (the canonical file produced in Tasks 1-2)
  </read_first>
  <action>
    Author `.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-06-SUMMARY.md` per the GSD summary template. Sections:

    1. **Aggregation result**: canonical-file path; total fuzz function count (18 = 13 per-consumer + 5 edge-case); aggregation order (scaffold → jackpot → lootbox → mixed → edgecase → closing `}`).
    2. **forge test attestation**: `FOUNDRY_PROFILE=deep forge test --match-path test/fuzz/RngLockDeterminism.t.sol` PASS verbatim output (last 30 lines) showing `Suite result: ok` + per-function PASS/SKIP status.
    3. **vm.skip inventory**: numbered table — for each skipped function, list `(function name, FIXREC §N, HANDOFF-NN, brief VIOLATION summary)`. Total skipped count + total unskipped count. Cite `D-301-VMSKIP-MECHANISM-01` Option C compliance.
    4. **Coverage attestation**: confirm all 13 CAT-01 consumer surfaces have ≥1 fuzz function (FUZZ-04 satisfied); confirm all 5 D-301-EDGE-CASES-01 edge cases authored (FUZZ-05 satisfied); confirm FUZZ-02 action set covered via `_perturb` + `_perturbAdminOnly` helpers (ADMIN-AUDIT.md §3 R-01..R-22 referenced).
    5. **v44.0 forward-handoff**: enumerate the HANDOFF-NN anchors that the harness's vm.skip blocks reference. v44.0 plan-phase consumes this list as load-bearing input for FIX-NN sub-phase planning per `D-43N-V44-HANDOFF-NN` lineage.
    6. **Commit SHA**: filled in after `git commit` lands (placeholder `<commit-sha-pending>` at draft time; resolved post-commit).
    7. **AUDIT-ONLY attestation**: cite `git diff HEAD~1 HEAD -- contracts/` returns empty; the sole `test/` mutation is `test/fuzz/RngLockDeterminism.t.sol`; `.planning/` mutations are the 6 PLAN.md + 6 SUMMARY.md files + 5 contribution `.sol` files + ROADMAP plan-checklist + STATE update. Zero `contracts/` mutations per `D-43N-AUDIT-ONLY-01`.

    Update `.planning/ROADMAP.md` Phase 301 entry — add the Plans section:

    ```
    Plans:
    - [x] 301-01-PLAN.md — Wave 1: Author scaffold contribution (contract header + shared helpers + 2 reference fuzz functions PayDailyJackpot + RunTerminalJackpot)
    - [x] 301-02-PLAN.md — Wave 1: Author jackpot-cluster contribution (PayDailyJackpotCoinAndTickets + RunTerminalDecimatorJackpot)
    - [x] 301-03-PLAN.md — Wave 1: Author lootbox-cluster contribution (4 functions: ResolveRedemptionLootbox + ResolveLootboxCommon + DegeneretteLootboxDirect + DecimatorAwardLootbox)
    - [x] 301-04-PLAN.md — Wave 1: Author mixed-cluster contribution (5 functions: MintTraitGeneration + BurnieCoinflipResolve + StakedStonkRedemption + GameOverRngSubstitution + RetryLootboxRng [opposite-direction])
    - [x] 301-05-PLAN.md — Wave 1: Author edge-case cluster contribution (5 functions: AdminDuringLock + NearEndOfWindow + MultiTxBatch + MultiBlock + RetryLootboxRngDuringLock)
    - [x] 301-06-PLAN.md — Wave 2: Aggregate contributions into canonical test/fuzz/RngLockDeterminism.t.sol; add vm.skip blocks; forge test PASS; AGENT-COMMITTED batched commit
    ```

    Update `.planning/STATE.md`:
    - `last_activity: 2026-05-18 -- Phase 301 marked complete`
    - `progress.completed_phases: 4` (up from 3)
    - `progress.completed_plans: 32` (up from 26 by 6)
    - Current Position: `Phase: 301 — COMPLETE`

    AGENT-COMMITTED batched test-tree commit per `D-301-WAVE-SHAPE-01`:

    ```
    git add test/fuzz/RngLockDeterminism.t.sol \
            .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/ \
            .planning/ROADMAP.md \
            .planning/STATE.md
    git commit -m "$(cat <<'EOF'
    test(301): ship RngLockDeterminism.t.sol Foundry fuzz harness — 18 functions (13 consumer + 5 edge-case)

    Phase 301 FUZZ — state-shuffle determinism harness. Asserts byte-identical
    VRF-derived outputs across mid-rngLock-window perturbations. 10k runs per case
    (FOUNDRY_PROFILE=deep). vm.skip blocks per D-301-VMSKIP-MECHANISM-01 Option C
    cross-reference RNGLOCK-FIXREC.md §N + v44.0 D-43N-V44-HANDOFF-NN anchors.

    Coverage:
    - All 13 CAT-01 consumer surfaces (FUZZ-04 satisfied)
    - All 5 D-301-EDGE-CASES-01 edge cases (FUZZ-05 satisfied)
    - FUZZ-02 action set via _perturb + _perturbAdminOnly (ADMIN-AUDIT.md §3 R-01..R-22)

    AGENT-COMMITTED per D-43N-TEST-COMMITS-AUTO-01 (test-tree only;
    zero contracts/ mutations per D-43N-AUDIT-ONLY-01).

    Requirements: FUZZ-01..05 PASS.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    ```

    After the commit lands, capture the SHA and update `301-06-SUMMARY.md` §6 (Commit SHA) — REPLACE the `<commit-sha-pending>` placeholder with the actual SHA. This is the final amendment to the SUMMARY.

    Alternative if the SUMMARY-SHA-update would force an empty extra commit: include the placeholder, then run `git commit --amend --no-edit` after the SUMMARY edit lands so the commit message + SHA reference settle in a single commit. Use `--amend` here since the harness commit has not been pushed; this is the standard end-of-phase pattern.

    Final attestation: `git status --porcelain contracts/` returns empty.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-06-SUMMARY.md && \
      grep -q "vm.skip inventory" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-06-SUMMARY.md && \
      grep -q "MILESTONE_V43_PHASE_301" .planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-06-SUMMARY.md && \
      grep -q "301-06-PLAN.md" .planning/ROADMAP.md && \
      # Verify last commit touches only test/ + .planning/
      git diff --name-only HEAD~1 HEAD | grep -qE "^(test/fuzz/RngLockDeterminism\.t\.sol|\.planning/)" && \
      ! git diff --name-only HEAD~1 HEAD | grep -qE "^contracts/" && \
      [ -z "$(git status --porcelain contracts/)" ] && echo OK
    </automated>
  </verify>
  <done>SUMMARY emitted with vm.skip inventory + forge test attestation + commit SHA; ROADMAP plan-checklist updated; STATE.md updated; AGENT-COMMITTED batched test commit landed; zero contracts/ mutations.</done>
</task>

</tasks>

<verification>
- test/fuzz/RngLockDeterminism.t.sol exists with 18 fuzz functions (13 + 5)
- `FOUNDRY_PROFILE=deep forge test --match-path test/fuzz/RngLockDeterminism.t.sol` reports `Suite result: ok.`
- vm.skip blocks reference RNGLOCK-FIXREC.md §N + v44.0 D-43N-V44-HANDOFF-NN
- testFuzz_RngLockDeterminism_RetryLootboxRng is NOT skipped (opposite-direction semantics)
- 301-06-SUMMARY.md exists with vm.skip inventory + forge test attestation + commit SHA
- ROADMAP plan-checklist updated for Phase 301
- STATE.md updated (Phase 301 → COMPLETE; progress count incremented)
- AGENT-COMMITTED batched commit landed (test-tree + .planning only)
- git diff HEAD~1 HEAD shows zero contracts/ mutations
</verification>

<success_criteria>
- D-301-HARNESS-ARCH-01: single `test/fuzz/RngLockDeterminism.t.sol` file with 18 fuzz functions using 6-phase template
- D-301-COVERAGE-01: all 13 CAT-01 consumer surfaces covered (FUZZ-04)
- D-301-EDGE-CASES-01: all 5 edge cases authored (FUZZ-05)
- D-301-RUNS-01: 10k runs per case via FOUNDRY_PROFILE=deep
- D-301-VMSKIP-MECHANISM-01: Option C vm.skip blocks with FIXREC §N + HANDOFF-NN cross-references
- D-301-WAVE-SHAPE-01: 1 AGENT-COMMITTED batched test-tree commit
- D-301-VERIFICATION-01: forge test PASSES
- D-43N-AUDIT-ONLY-01: zero contracts/ mutations
- FUZZ-01..05 requirements satisfied
</success_criteria>

<output>
Create `.planning/phases/301-state-shuffle-determinism-fuzz-harness-fuzz/301-06-SUMMARY.md` when done.
</output>
