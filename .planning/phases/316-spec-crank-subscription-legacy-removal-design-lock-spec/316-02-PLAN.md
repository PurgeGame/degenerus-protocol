---
phase: 316-spec-crank-subscription-legacy-removal-design-lock-spec
plan: 02
type: execute
wave: 2
depends_on: ["316-01"]
files_modified:
  - .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md
autonomous: true
requirements: [RM-04]
must_haves:
  truths:
    - "316-SPEC.md locks PROTO-01/RM-04 = KEEP+EXPOSE _hasAnyLazyPass and delete the rest of afKing, with the verified 2-reader finding (readers at :1580/:1660, decl :1610) proving the deletion is dependency-safe IFF PROTO-01 ships the same diff."
    - "316-SPEC.md enumerates the full RM-01..06 deletion footprint using the RESEARCH §1 RE-VERIFIED line numbers (NOT the PLAN-V47 claimed lines where they drift), across DegenerusGame / storage / JackpotModule / PayoutUtils / BurnieCoinflip / IDegenerusGame / IBurnieCoinflip / DegenerusVault / StakedDegenerusStonk."
    - "316-SPEC.md locks the storage-layout slot-shift plan as a COMPOUNDED two-deletion shift (autoRebuyState = slot 19 AND resumeEthPool = slot 33, both removed in the same batched diff): vars in [20,33) shift −1, vars at slot ≥34 shift −2; ~28 test-side SLOT_* constants across ~15 files; contract source has ZERO numeric slot literals; re-derivation is ONE combined forge inspect pass on the POST-(RM-02+JGAS) contract — never patch-by-arithmetic — plus capturing the pre-deletion baseline-failure ledger first. (The JGAS deletion-footprint enumeration itself lives in Plan 316-05; 316-02 carries only the slot-compounding note, not a duplicate footprint.)"
    - "316-SPEC.md notes the VRF-freeze-obligation retirement: removing _processAutoRebuy/_calcAutoRebuy drops a VRF entropy consumer + 3 player-mutable in-window inputs; the entropy param is dropped from the 3-arg _addClaimableEth and the JackpotEthWin event signature changes (ABI break noted)."
    - "316-SPEC.md confirms the keeper does NOT depend on anything RM-* deletes (zero-match grep over the full RM-symbol set; only coupling = hasAnyLazyPass at keeper :671/:974)."
  artifacts:
    - path: ".planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md"
      provides: "REMOVE-footprint + reconciliation + storage-slot-shift sections (## REMOVE Footprint, ## Storage Slot-Shift Plan, ## VRF-Freeze Obligation Retirement)"
      contains: "AutoRebuyState"
  key_links:
    - from: "316-SPEC.md REMOVE footprint"
      to: "RESEARCH §1.1–§1.9 verified line numbers"
      via: "deletion-footprint enumeration grep-verified against HEAD"
      pattern: "_processAutoRebuy"
    - from: "316-SPEC.md storage slot-shift plan"
      to: "forge inspect storage-layout (autoRebuyState slot 19 + resumeEthPool slot 33, combined −2)"
      via: "post-deletion combined re-derivation mandate"
      pattern: "slot 33"
---

<objective>
Author the REMOVE-half design-lock sections of `316-SPEC.md`: the PROTO-01/RM-04 KEEP+EXPOSE reconciliation, the RM-01..06 deletion footprint (using RESEARCH §1's re-verified line numbers), the storage-layout slot-shift re-derivation plan (the COMPOUNDED two-deletion `autoRebuyState`@19 + `resumeEthPool`@33 shift), and the VRF-freeze-obligation retirement note.

Purpose: Phase 317 IMPL deletes exactly what this section enumerates. A wrong footprint (a missed symbol, a stale line number, a naive "decrement slots by 1" plan that misses the JGAS −2 compounding) becomes a broken removal — orphaned references, a non-compiling tree, or a test suite that blames the diff for a pre-existing stale-baseline failure or for the JGAS slot drift. The KEEP+EXPOSE reconciliation must be locked verbatim so the dead-code-deletion instinct does not strip the keeper's sole pass gate.

Output: The REMOVE-design sections of `316-SPEC.md`. Zero `contracts/` and zero `test/` mutations.

Note on scope split with Plan 316-05 (JGAS): the daily-ETH two-call-split DELETION FOOTPRINT (the `SPLIT_*`/`resumeEthPool`/`_resumeDailyEth`/`splitMode`/`call1Bucket`/`STAGE_JACKPOT_ETH_RESUME` symbols) is enumerated in the dedicated JGAS plan 316-05, NOT here. This plan owns ONLY the slot-shift COMPOUNDING note for `resumeEthPool` (slot 33) so the storage re-derivation is a single combined pass; do NOT duplicate the JGAS footprint enumeration in this plan's `## REMOVE Footprint` section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PLAN-V47-REMOVE-AFKING-ETH-AUTOREBUY.md
@.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-RESEARCH.md

<interfaces>
<!-- The RM deletion footprint with RESEARCH §1 RE-VERIFIED line numbers. Where RESEARCH marks ✗ DRIFT, USE THE RESEARCH LIVE LINE, NOT the PLAN-V47 claimed line. Re-grep each before writing (SC#5). -->

DegenerusGame.sol (RESEARCH §1.1 — all ✓ MATCH):
- DELETE fns: setAutoRebuy :1495, setAutoRebuyTakeProfit :1504, _setAutoRebuy :1512, _setAutoRebuyTakeProfit :1524, autoRebuyTakeProfitFor :1543, setAfKingMode :1559, _setAfKingMode :1569, afKingModeFor :1624, afKingActivatedLevelFor :1631, deactivateAfKingFromCoin :1641, syncAfKingLazyPassFromCoin :1654, _deactivateAfKing :1670.
- KEEP+EXPOSE: _hasAnyLazyPass :1610 (readers :1580 inside _setAfKingMode, :1660 inside syncAfKingLazyPassFromCoin — both deleted; the keeper needs the external view).
- DELETE events: AutoRebuyToggled :1476, AutoRebuyTakeProfitSet :1479, AfKingModeToggled :1482. DELETE error AfKingLockActive :92 (used :1676). DELETE consts AFKING_KEEP_MIN_ETH :151, AFKING_KEEP_MIN_COIN :154, AFKING_LOCK_LEVELS :157.
- REMOVE cross-calls: coinflip.settleFlipModeChange at :1603, :1678.

storage/DegenerusGameStorage.sol (RESEARCH §1.2):
- DELETE struct AutoRebuyState :910 (body 910–919), mapping autoRebuyState :926. forge: autoRebuyState = slot 19.
- (JGAS, owned by Plan 316-05) `resumeEthPool` uint128 own-slot decl :994, forge slot 33 — its DELETION is enumerated in 316-05; here it appears ONLY as the second deleted var that compounds the slot shift to −2 for the slot-≥34 region.

modules/DegenerusGameJackpotModule.sol (RESEARCH §1.3):
- _addClaimableEth decl :788 (3-arg `(beneficiary, weiAmount, entropy)`); auto-rebuy block at 800–808 (PLAN-V47 said 798–806, ✗ DRIFT +2 — use 800–808). _processAutoRebuy :822 DELETE. _budgetToTicketUnits :861 (verify-orphaned). _addClaimableEth callers passing entropy: 1430, 1530, 1571, 1583, 2132, 2165. JackpotEthWin event decl :69 carries rebuyLevel/rebuyTickets, emitted 1431-1438, 1531 → event signature CHANGES on removal (ABI break).

modules/DegenerusGamePayoutUtils.sol (RESEARCH §1.4):
- _calcAutoRebuy :51 DELETE (afKingMode bonus selector at :83). struct AutoRebuyCalc :19 (verify-orphaned). entropy roll `keccak256(abi.encode(entropy, beneficiary, weiAmount)) & 3` ~:70.

BurnieCoinflip.sol (RESEARCH §1.5 — surgery, keep core):
- settleFlipModeChange :217 DELETE. rebet-bonus afKing branch body 294–308 (afKingModeFor :300, hasDeityPass :302, _afKingDeityBonus :304, _afKingRecyclingBonus :305) collapse to _recyclingBonus. _claimCoinflipsInternal :416; sync call :422; afKingActive/hasDeityPass/deityBonusHalfBps block 434–443; recycle branch 540–548 collapse. _setCoinflipAutoRebuy :722 / _setCoinflipAutoRebuyTakeProfit :776: remove deactivateAfKingFromCoin calls :754/:766/:793 + AFKING_KEEP_MIN_COIN floor checks :753/:792. DELETE _afKingRecyclingBonus :1062, _afKingDeityBonusHalfBpsWithLevel :1078. DELETE consts AFKING_RECYCLE_BONUS_BPS :130, AFKING_DEITY_BONUS_PER_LEVEL_HALF_BPS :131, AFKING_DEITY_BONUS_MAX_HALF_BPS :132, DEITY_RECYCLE_CAP :133, AFKING_KEEP_MIN_COIN :140.
- KEEP: RECYCLE_BONUS_BPS :129 (=75), _recyclingBonus :1051 (`amount * RECYCLE_BONUS_BPS / BPS_DENOMINATOR` :1055), processCoinflipPayouts :805 win/loss `(rngWord & 1)==1` :837 (MUST NOT MODIFY).

interfaces/IDegenerusGame.sol (RESEARCH §1.6):
- REMOVE afKingModeFor :274, afKingActivatedLevelFor :279, deactivateAfKingFromCoin :283, syncAfKingLazyPassFromCoin :288. RESOLVED open: setAutoRebuy/setAutoRebuyTakeProfit/setAfKingMode are NOT declared in IDegenerusGame (they ARE in Vault's local interface). KEEP hasDeityPass :376.

interfaces/IBurnieCoinflip.sol (RESEARCH §1.7): REMOVE settleFlipModeChange :85.

DegenerusVault.sol (RESEARCH §1.8): REMOVE local decls setAutoRebuy :47 / setAutoRebuyTakeProfit :49 / setAfKingMode :51; wrappers gameSetAutoRebuy (:627/:628), gameSetAutoRebuyTakeProfit (:634/:635), gameSetAfKingMode (:643/:648). KEEP coinSetAutoRebuy :685 / coinSetAutoRebuyTakeProfit :692.

StakedDegenerusStonk.sol (RESEARCH §1.9): REMOVE local decl setAfKingMode :13; init call `game.setAfKingMode(address(0), true, 10 ether, 0)` :361 (preceded by `game.claimWhalePass(address(0))` :360) — REPLACED by self-subscribe (SUB-09, locked in Plan 316-03).

Storage slot-shift — COMPOUNDED two-deletion (RESEARCH §4 + §J3 — forge inspect authoritative):
- Both `autoRebuyState` (slot 19, RM-02) AND `resumeEthPool` (slot 33, JGAS-02) are deleted in the SAME batched Phase-317 diff.
- `autoRebuyState`@19 deletion → every var at slot ≥20 shifts −1.
- `resumeEthPool`@33 is its OWN slot (uint128 at offset 0; `vrfCoordinator` starts fresh at slot 34) → its deletion → an ADDITIONAL −1 for every var at slot ≥34.
- COMBINED: vars < 19 unchanged; vars in [20, 33) shift −1; vars at slot ≥34 shift −2. Key shifts under the COMBINED deletion: `lootboxEthBase` 20→19, `vrfCoordinator` 34→32, `lootboxRngPacked` 37→35, `lootboxRngWordByIndex` 38→36, `lootboxDay` 39→37, `degeneretteBets` 45→43, `boonPacked` 61→59. (The `vrf*`/`lootboxRng*` family the v45 VRF work depends on lands at −2.)
- Contract source has ZERO numeric slot literals (`grep -rnE '\.slot\s*:?=\s*[0-9]+|sload\([0-9]+\)|SLOT_[A-Z_]+\s*=\s*[0-9]+' contracts/` excl test returns only `QUEST_SLOT_COUNT=2` + `TICKET_SLOT_BIT=1<<23`, neither a storage slot). NO contract code breaks on either shift — both RM-06 and JGAS slot work are entirely test-side.
- ~28 test-side SLOT_* constants across ~15 files (BafRebuyReconciliation, BafFarFutureTickets, RngIndexDrainBinding, DegeneretteFreezeResolution, AdvanceGameRewrite, AffiliateDgnrsClaim, QueueDoubleBuffer, VRFCore, StorageFoundation, LootboxBoonCoexistence, LootboxRngLifecycle, VrfRotationOrphanIndex, StakedStonkRedemption, RngLockRotationDeterminism, RedemptionEdgeCases, VrfRotationLiveness, JackpotCombinedPool, TicketLifecycle, RngLockDeterminism, VRFStallEdgeCases, RedemptionInvariants.inv, RedemptionHandler).
- ⚠ HAZARD: LootboxBoonCoexistence.t.sol SLOT_* constants are ALREADY +1 stale vs current layout AND test_lootboxBoonAppliedDespiteExistingCoinflipBoon FAILS at baseline. With the JGAS −2 compounding, `lootboxRngWordByIndex` lands at slot 36 / `lootboxRngPacked` at 35 — so the re-derivation CANNOT be a blind decrement (some constants are already off in the wrong direction). RM-06 + JGAS must be re-derived TOGETHER in ONE combined `forge inspect` pass on the POST-(RM-02+JGAS) contract; capture the pre-deletion baseline-failure ledger first.

Keeper dependency (RESEARCH §3): StreakKeeperV2 zero-match grep over the full RM-symbol set; only game coupling = hasAnyLazyPass at keeper :671 (subscribe gate) / :974 (renewal sweep gate). Deletion dependency-safe IFF PROTO-01 ships same diff.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the PROTO-01/RM-04 reconciliation + the RM-01..06 deletion footprint section</name>
  <files>.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md</files>
  <read_first>
    - `316-RESEARCH.md` §1.1–§1.9 (the verified deletion-footprint table — USE these line numbers, note the ✗ DRIFT rows), §2 (`_hasAnyLazyPass` reader-set finding), §3 (keeper dependency clean), §8 Pitfall 4 (entropy-param drop — check all consumers).
    - `PLAN-V47-REMOVE-AFKING-ETH-AUTOREBUY.md` §1.5 (reconciliation), §3 (KEEP vs REMOVE table), §5.1–§5.9 (footprint), §8 (verify-before-IMPL checklist), §9 (success criteria).
    - `REQUIREMENTS.md` RM-01..06, PROTO-01.
    - Re-grep each footprint file:line in the `<interfaces>` block against HEAD before writing (SC#5; where RESEARCH marks ✗ DRIFT, use the RESEARCH live line).
  </read_first>
  <action>
    Append a `## REMOVE Footprint` section to `316-SPEC.md`. Lock:
    (1) The PROTO-01/RM-04 reconciliation statement (RM-04): KEEP+EXPOSE `_hasAnyLazyPass` (rename private→external `hasAnyLazyPass`), delete the rest of afKing — this OVERRIDES the dead-code-deletion instinct. Cite the verified reader-set (3 grep matches total: decl `DegenerusGame.sol:1610`, readers :1580 and :1660 both inside deleted afKing-mode machinery), and state the deletion is dependency-safe IFF PROTO-01 ships in the SAME diff (cite the keeper-dependency finding §3: keeper's sole game coupling is `hasAnyLazyPass`).
    (2) The RM-01..06 deletion footprint, organized by file, using the RESEARCH §1 RE-VERIFIED line numbers (where RESEARCH marks ✗ DRIFT — the JackpotModule auto-rebuy block 800–808 and the `_distributePayout` solvency check — use the RESEARCH live line, NOT the PLAN-V47 claimed line; record the drift inline). Cover: RM-01 afKing mode surface (the 13 DegenerusGame fns minus the kept `_hasAnyLazyPass`, 3 events, AfKingLockActive error, 3 consts, 2 settleFlipModeChange cross-calls); RM-02 free ETH auto-rebuy (`AutoRebuyState` struct/mapping, jackpot `_processAutoRebuy`/`_calcAutoRebuy` + entropy-param drop, ETH winnings always credit to claimable); RM-03 BURNIE recycle collapse to flat 75bps (drop `_afKingRecyclingBonus`/`_afKingDeityBonusHalfBpsWithLevel` + the 5 afKing consts, KEEP `RECYCLE_BONUS_BPS`=75 + `_recyclingBonus`); RM-04 the kept `_hasAnyLazyPass`; RM-05 cross-contract cascade (IDegenerusGame decls, IBurnieCoinflip `settleFlipModeChange`, Vault `gameSet*` wrappers + local decls, sStonk `setAfKingMode` init → self-subscribe; note the RESOLVED open that `setAutoRebuy`/`setAfKingMode` are NOT in IDegenerusGame but ARE in Vault's local interface).
    State the verify-before-IMPL orphan checks (RM-05/RM-06 hygiene): confirm `AutoRebuyCalc` (PayoutUtils:19), `_budgetToTicketUnits` (JackpotModule:861), and any `AutoRebuyState` import have no surviving caller after the cuts (grep post-edit at IMPL); state explicitly that the DegeneretteModule 2-arg `_addClaimableEth` overload (:1117) is SEPARATE and untouched (Pitfall 4). State that `KNOWN_ISSUES` + the BURNIE win/loss RNG path (`processCoinflipPayouts` :805, `(rngWord & 1)` :837) MUST stay byte-unmodified (RM-06).
    Do NOT enumerate the JGAS daily-ETH-split deletion footprint here — that footprint (the `SPLIT_*`/`resumeEthPool`/`_resumeDailyEth`/`splitMode`/`call1Bucket`/`STAGE_JACKPOT_ETH_RESUME` symbols) is owned by Plan 316-05's `## JGAS-01 Decision Gate` section. Add at most a one-line cross-reference pointing the reader to 316-05's JGAS section + noting that `resumeEthPool`'s slot deletion compounds the RM-06 shift (handled in this plan's Task 2).
  </action>
  <verify>
    <automated>grep -q '## REMOVE Footprint' .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md && grep -q 'AutoRebuyState' .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md && grep -q '_processAutoRebuy' .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md && grep -q 'KEEP' .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md && grep -q '1610' .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md && grep -q '1580' .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md && grep -q '1660' .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - `316-SPEC.md` contains a `## REMOVE Footprint` section.
    - The section locks PROTO-01/RM-04 = KEEP+EXPOSE `_hasAnyLazyPass`, delete the rest of afKing, citing the 3-match reader-set (decl :1610, readers :1580/:1660) and stating the deletion is dependency-safe IFF PROTO-01 ships the same diff.
    - The section enumerates the full RM-01..06 footprint by file using the RESEARCH §1 re-verified line numbers, with the JackpotModule auto-rebuy block recorded as 800–808 (drift +2 from PLAN-V47's 798–806) and the `_distributePayout` solvency check recorded as decl :705 / check ~738.
    - The section marks `RECYCLE_BONUS_BPS`=75 (:129) and `_recyclingBonus` (:1051) as KEPT and the BURNIE win/loss RNG path (`processCoinflipPayouts` :805, `rngWord & 1` :837) + `KNOWN_ISSUES` as byte-unmodified.
    - The section states the DegeneretteModule 2-arg `_addClaimableEth` overload (:1117) is separate and untouched (Pitfall 4).
    - The section does NOT enumerate the JGAS daily-ETH-split deletion footprint (only a one-line cross-reference to Plan 316-05's JGAS section is permitted) — the JGAS footprint is owned by 316-05, not duplicated here.
    - NO fenced solidity implementation bodies (symbols/line numbers named as prose/table only).
    - Zero `contracts/`/`test/` files modified by this plan.
  </acceptance_criteria>
  <done>`316-SPEC.md` has the `## REMOVE Footprint` section locking the KEEP+EXPOSE reconciliation and the verified RM-01..06 deletion footprint with corrected line numbers; the JGAS footprint is left to Plan 316-05 (cross-referenced only).</done>
</task>

<task type="auto">
  <name>Task 2: Author the storage slot-shift re-derivation plan (COMPOUNDED −2) + the VRF-freeze-obligation retirement note</name>
  <files>.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md</files>
  <read_first>
    - `316-RESEARCH.md` §4 (storage-layout slot-shift map — forge inspect authoritative; the ALREADY-stale LootboxBoonCoexistence hazard; the test-side blast radius list), §J3 (the COMBINED RM-02+JGAS −2 slot re-derivation — `resumeEthPool`@33 own-slot, the slot-≥34 −2 region, `lootboxRngWordByIndex` 38→36), §6 entropy cascade (the SAFE-04 retirement), §8 Pitfall 2 (RM-06 naive decrement), §8 Pitfall 4 (entropy-param consumer check).
    - `PLAN-V47-REMOVE-AFKING-ETH-AUTOREBUY.md` §6 (VRF/freeze-invariant angle), §7 (storage-layout impact + ordering).
    - `REQUIREMENTS.md` RM-02 (entropy param dropped), RM-06 (slot re-derivation), SAFE-04 (freeze-obligation retirement), JGAS-02 (the compounding `resumeEthPool` deletion).
    - Re-grep the §4 + §J3 slot facts (confirm `autoRebuyState` region at storage :926 and `resumeEthPool` :994/slot 33) and the entropy-cascade anchors against HEAD before writing.
  </read_first>
  <action>
    Append a `## Storage Slot-Shift Plan` section and a `## VRF-Freeze Obligation Retirement` section to `316-SPEC.md`. Lock:
    (1) Storage slot-shift (RM-06 + JGAS-02 COMPOUNDED): state that the SAME batched Phase-317 diff deletes TWO storage vars — `autoRebuyState` at forge-confirmed slot 19 (RM-02) AND `resumeEthPool` at forge-confirmed own-slot 33 (JGAS-02). Lock the COMBINED shift: vars < 19 unchanged; vars in [20, 33) shift −1; vars at slot ≥34 shift −2 (give the key COMBINED shifts: `lootboxEthBase` 20→19, `vrfCoordinator` 34→32, `lootboxRngPacked` 37→35, `lootboxRngWordByIndex` 38→36, `lootboxDay` 39→37, `degeneretteBets` 45→43, `boonPacked` 61→59). State explicitly that the `vrf*`/`lootboxRng*` family the v45 VRF work depends on lands at −2 (NOT −1) and that this is the JGAS-deepened shift — anyone treating the shift as −1 would mis-derive the slot-≥34 region. State the contract source contains ZERO numeric slot literals → NO contract code breaks; RM-06 (now incl. the JGAS `resumeEthPool` deletion) is ENTIRELY a test-side problem (~28 `SLOT_*` constants across ~15 test files — list the files). Lock the re-derivation MANDATE: re-run `forge inspect ... storage-layout` ONCE on the POST-(RM-02+JGAS) contract (both vars deleted in the same diff) and rewrite each test `SLOT_*` constant from that authoritative output, file-by-file — NEVER patch-by-arithmetic (Pitfall 2), and NEVER as a blind −1 (the −2 region would be wrong). Lock the compounding hazard: `LootboxBoonCoexistence.t.sol` constants are ALREADY +1 stale vs current AND `test_lootboxBoonAppliedDespiteExistingCoinflipBoon` FAILS at baseline; with the JGAS −2 those slots land at 35/36 — so RM-06 must (a) capture the pre-deletion baseline-failure ledger FIRST, (b) re-derive from the single combined forge inspect, (c) ensure the delta is attributable so the re-derivation is not blamed for the pre-existing failure (the TST phase 318 owns "no NEW failures vs baseline"). State that the JGAS deletion FOOTPRINT itself (the symbols being removed) is enumerated in Plan 316-05 — this section locks only the slot-derivation consequence.
    (2) VRF-freeze obligation retirement (SAFE-04, RM-02): document the entropy cascade — the VRF word (`rngWord`/`randWord`) is mixed via `EntropyLib.hash2` and threaded as `entropy`/`entropyState` through the jackpot resolution loop into the 3-arg `_addClaimableEth(beneficiary, weiAmount, entropy)` → `_processAutoRebuy` → `_calcAutoRebuy` where `keccak256(abi.encode(entropy, beneficiary, weiAmount)) & 3` picks the rebuy level. Removing `_processAutoRebuy`/`_calcAutoRebuy` makes `entropy` UNCONSUMED on the claimable path → drop it from the 3-arg `_addClaimableEth` signature; the `JackpotEthWin` event's `rebuyLevel`/`rebuyTickets` fields become dead → event signature changes (note the ABI break in a delta note; off-chain indexer is a separate frontend track per the out-of-scope list). State this is the concrete "one fewer VRF consumer + three fewer player-mutable in-window inputs (`autoRebuyEnabled`/`takeProfit`/`afKingMode`)" retirement SAFE-04 claims. Lock the IMPL obligation: verify no OTHER reader of the threaded `entropyState` survives before dropping (the 3-arg form is JackpotModule-only; the DegeneretteModule 2-arg overload :1117 is untouched). Route the "does dropping entropy change any other consumer" check to the `zero-day-hunter` skill at AUDIT (name the skill, do NOT run it here).
  </action>
  <verify>
    <automated>grep -q '## Storage Slot-Shift Plan' .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md && grep -q '## VRF-Freeze Obligation Retirement' .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md && grep -q 'slot 19' .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md && grep -q 'slot 33' .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md && grep -qE '[−-]2' .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md && grep -q 'forge inspect' .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md && grep -q 'LootboxBoonCoexistence' .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md && grep -qi 'JackpotEthWin' .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - `316-SPEC.md` contains a `## Storage Slot-Shift Plan` section and a `## VRF-Freeze Obligation Retirement` section.
    - The slot-shift section states BOTH `autoRebuyState`=slot 19 AND `resumeEthPool`=own-slot 33 are deleted in the same batched diff, and locks the COMBINED shift: vars in [20,33) shift −1, vars at slot ≥34 shift −2 (with the key combined shifts listed, incl. `lootboxRngWordByIndex` 38→36 and `vrfCoordinator` 34→32).
    - The slot-shift section explicitly flags that the `vrf*`/`lootboxRng*` family lands at −2 (NOT −1) — anyone treating the shift as a uniform −1 mis-derives the slot-≥34 region; contract source has ZERO numeric slot literals; RM-06 (incl. the JGAS `resumeEthPool` deletion) is entirely test-side (~28 `SLOT_*` constants across ~15 files, listed).
    - The slot-shift section mandates ONE combined `forge inspect` re-derivation on the POST-(RM-02+JGAS) contract file-by-file (never patch-by-arithmetic, never a blind −1) and records the `LootboxBoonCoexistence.t.sol` already-stale + baseline-failing hazard (now landing at 35/36 under the −2) with the capture-baseline-ledger-first instruction.
    - The slot-shift section states the JGAS deletion FOOTPRINT itself is owned by Plan 316-05 (this section locks only the slot-derivation consequence — no duplicate footprint enumeration).
    - The retirement section documents the entropy cascade, locks dropping the `entropy` param from the 3-arg `_addClaimableEth`, notes the `JackpotEthWin` event signature change (ABI break), and frames this as the SAFE-04 "one fewer VRF consumer + three fewer player-mutable inputs" retirement.
    - The retirement section states the IMPL must verify no other `entropyState` consumer survives (the 2-arg DegeneretteModule overload :1117 is untouched) and routes the consumer-check to the `zero-day-hunter` skill at AUDIT.
    - NO fenced solidity implementation bodies.
    - Zero `contracts/`/`test/` files modified by this plan.
  </acceptance_criteria>
  <done>`316-SPEC.md` has the storage slot-shift re-derivation plan locking the COMPOUNDED RM-02+JGAS −2 shift (one combined forge-inspect mandate + stale-baseline hazard) and the VRF-freeze-obligation retirement section (entropy-param drop + JackpotEthWin ABI break).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (cost of a wrong REMOVE design lock)

| Boundary | Description |
|----------|-------------|
| SPEC REMOVE footprint → Phase 317 deletion | A missed symbol or stale line number makes the deletion leave dangling references / a non-compiling tree. |
| storage slot-shift plan → Phase 318 test suite | A naive "decrement by 1" plan that misses the JGAS `resumeEthPool` deletion mis-derives the slot-≥34 region (the v45 VRF slot family) by a full slot, applied over already-stale constants → the suite drifts and the VRF slot work breaks. |
| entropy-param drop → VRF freeze invariant | Dropping `entropy` without checking all consumers could silently break a non-rebuy consumer. |

## STRIDE Threat Register (cost of a wrong design lock)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-316-05 | Denial of Service | A SPEC that locks a too-shallow RM-06 ("re-derive a few constants") or treats the shift as a uniform −1 (missing the JGAS −2 compounding) | mitigate | Task 2 locks `autoRebuyState`=slot 19 + `resumeEthPool`=slot 33, the COMBINED −2 region for slot ≥34 (incl. the v45 VRF `lootboxRng*`/`vrf*` family), ~28 test-side constants across ~15 files, ONE combined forge-inspect re-derivation on the post-(RM-02+JGAS) contract, and the already-stale `LootboxBoonCoexistence` hazard with a baseline-ledger-first instruction. |
| T-316-06 | Elevation | A SPEC that deletes `_hasAnyLazyPass` (dead-code instinct) and strands the keeper's pass gate | mitigate | Task 1 locks KEEP+EXPOSE with the verified reader-set + keeper-dependency finding; deletion dependency-safe IFF PROTO-01 ships same diff. |
| T-316-07 | Tampering | A SPEC that drops the `entropy` param without an all-consumer check | mitigate | Task 2 locks the IMPL obligation to verify no other `entropyState` consumer survives (2-arg overload :1117 untouched) + routes the check to `zero-day-hunter` at AUDIT. |
| T-316-08 | Tampering | A SPEC that locks a drifted/unverified RM line number | mitigate | Footprint uses RESEARCH §1 re-verified lines; ✗ DRIFT rows corrected inline; the dedicated attestation table is Plan 316-04. |
| T-316-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero packages installed (read-only doc authoring). |
</threat_model>

<verification>
- `316-SPEC.md` contains the `## REMOVE Footprint`, `## Storage Slot-Shift Plan`, and `## VRF-Freeze Obligation Retirement` section headers.
- Every cited RM file:line is the RESEARCH §1 re-verified line (drift rows corrected); no PLAN-V47 stale line is locked uncorrected.
- The slot-shift section locks the COMPOUNDED RM-02+JGAS −2 shift (slot 19 + slot 33), not a uniform −1; the JGAS deletion footprint is left to Plan 316-05.
- No `.sol`/`.t.sol` file appears in this plan's git diff.
</verification>

<success_criteria>
- ROADMAP SC#4 (REMOVE design + reconciliation locked) is satisfied: PROTO-01/RM-04 KEEP+EXPOSE, the RM-01..06 footprint, the COMPOUNDED storage-slot re-derivation plan (RM-02 + JGAS-02), and the VRF-freeze-obligation retirement are all locked.
- RM-04 design (KEEP+EXPOSE) is locked with the dependency-safety proof.
- The slot re-derivation accounts for the JGAS `resumeEthPool` deletion (−2 for slot ≥34) so the JGAS plan 316-05 need only enumerate the footprint, not re-derive slots.
</success_criteria>

<output>
Create `.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-02-SUMMARY.md` when done.
</output>
</output>
