---
phase: 316-spec-crank-subscription-legacy-removal-design-lock-spec
plan: 04
type: execute
wave: 4
depends_on: ["316-01", "316-02", "316-03"]
files_modified:
  - .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md
autonomous: true
requirements: [PROTO-01, RM-04]
must_haves:
  truths:
    - "316-SPEC.md contains a ## Call-Graph Attestation table where every cited file:line across DegenerusGame/modules/BurnieCoin/BurnieCoinflip/DegenerusVault/StakedDegenerusStonk/ContractAddresses + the StreakKeeperV2/AfKing keeper is grep-verified against source HEAD (MATCH / DRIFT / MISSING per RESEARCH §1)."
    - "316-SPEC.md contains ZERO unverified 'by construction' / 'single fn reaches all paths' claims — the attestation records the keeper-dependency clean result (zero-match grep over the RM-symbol set) and the drift items (≤+4-line drifts, the keeper transitional-state caveat, the IDegenerusGame setAutoRebuy-not-present resolution)."
    - "316-SPEC.md confirms the keeper does NOT depend on anything RM-* deletes (only coupling = hasAnyLazyPass at keeper :671/:974) and records the box-cursor VRF-rotation-orphan-index coupling landmine (Pitfall 3)."
    - "316-SPEC.md is assembled into a single coherent design-lock document: a top section maps all 38 v46.0 requirement designs to their locked SPEC sections; the 5 ROADMAP success criteria are each shown COVERED."
    - "316-SPEC.md is self-consistent: every section header referenced by the requirement-coverage map exists in the document; zero contracts/ and zero test/ mutations across the whole phase."
  artifacts:
    - path: ".planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md"
      provides: "Call-graph attestation table + 38-requirement coverage map + assembled final SPEC (## Call-Graph Attestation, ## Requirement Design Coverage)"
      contains: "Call-Graph Attestation"
  key_links:
    - from: "316-SPEC.md attestation table"
      to: "316-RESEARCH.md §1 verification table"
      via: "embed/reference as the SC#5 attestation substrate"
      pattern: "MATCH"
    - from: "316-SPEC.md requirement-coverage map"
      to: "the 38 v46.0 requirement IDs (PROTO/CRANK/REW/SUB/RM/SAFE/GAS)"
      via: "each design mapped to its locked SPEC section"
      pattern: "PROTO-01"
---

<objective>
Author the `## Call-Graph Attestation` table and the `## Requirement Design Coverage` map of `316-SPEC.md`, then assemble the document into a single coherent design-lock spec. The attestation is the SC#5 deliverable: every cited file:line grep-verified against HEAD, zero unverified "by construction" claims, the keeper-dependency clean result and the drift items recorded.

Purpose: SC#5 is the phase's verification backbone — the entire reason this is a "grep-verify every call-graph claim" phase. The attestation table is what the plan-checker and downstream phases trust. The requirement-coverage map proves all 38 v46.0 designs are locked somewhere in the SPEC (not silently dropped) and that the 5 ROADMAP success criteria are each covered. This plan runs LAST because it attests and assembles the content the three design plans authored.

Output: The attestation + coverage sections of `316-SPEC.md` and a self-consistency pass over the whole document. Zero `contracts/` and zero `test/` mutations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-RESEARCH.md
@.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md

<interfaces>
<!-- The attestation substrate. RESEARCH §1 IS the verified table — embed/reference it. The drift items the SPEC must record so it contains zero unverified claims: -->

Verified-clean (record as MATCH): all of RESEARCH §1.1 (DegenerusGame), §1.2 (storage), §1.5 (BurnieCoinflip interiors), §1.6 (IDegenerusGame), §1.7 (IBurnieCoinflip), §1.8 (Vault), §1.9 (sStonk).

Drift items the SPEC MUST record (so it has zero unverified "by construction" claims):
- DRIFT (cosmetic ≤+4): JackpotModule `_addClaimableEth` auto-rebuy block 798–806 (doc) → 800–808 (live, +2); DegeneretteModule `_distributePayout` solvency check — decl :705, check ~738 (doc said 742, inside body).
- MISSING (resolves an open): `setAutoRebuy`/`setAutoRebuyTakeProfit`/`setAfKingMode` are NOT declared in IDegenerusGame (they ARE in Vault's local interface) — record as the resolution to PLAN-V47 §5.6's "verify whether present".
- Keeper transitional-state caveat (RESEARCH §1.12): live keeper = MIXED state (19× `pullForKeeper`, 5× `mintForKeeper`, 2× `creditFlip`, OLD `sweep(startIdx,count)`, no `sweepCursor`/`reinvestPct`/`windowPaid`); PLAN-CRANK §9 "done this session" is FALSE vs live source → the SPEC locks against the INTENDED end-state. Record this so the plan-checker does not treat §9 as ground truth.
- PROTO-side keeper interface obligation: keeper still calls `IBurnie.pullForKeeper`/`mintForKeeper` (BurnieCoin has neither yet, deferred-selector by design); PROTO-02 adds `burnForKeeper`, the keeper IMPL switches its calls.

Keeper-dependency clean result (RESEARCH §3): zero-match grep over the full RM-symbol set (`syncAfKingLazyPassFromCoin`/`afKingModeFor`/`afKingActivatedLevelFor`/`setAfKingMode`/`deactivateAfKingFromCoin`/`setAutoRebuy`/`setAutoRebuyTakeProfit`/`autoRebuyState`/`AutoRebuyState`/`_processAutoRebuy`/`_calcAutoRebuy`/`settleFlipModeChange`/`_afKingRecyclingBonus`/`_afKingDeityBonus`/`gameSetAutoRebuy`/`gameSetAfKingMode`); only game coupling = `hasAnyLazyPass` at keeper :671 (subscribe gate) / :974 (renewal sweep gate). Deletion dependency-safe IFF PROTO-01 ships same diff.

Box-cursor VRF-rotation landmine (RESEARCH §8 Pitfall 3 / memory project_vrf_rotation_midday_orphan_index): the OPEN-D box cursor re-couples to the v45 orphan-index keyspace → must follow the `a303ae18` detect-preserve-re-issue path; the AUDIT phase re-verifies the freeze invariant under rotation WITH the new cursor.

The 38 v46.0 requirement IDs to map (REQUIREMENTS.md): PROTO-01..05, CRANK-01..04, REW-01..04, SUB-01..09, RM-01..06, SAFE-01..04, GAS-01..06. SPEC primary owners = PROTO-01/SUB-09/RM-04; the rest have downstream primary owners but their DESIGNS are locked at this SPEC. (SAFE-* and GAS-* designs are locked as the safety/gas properties the ADD/REMOVE sections must hold — map them to the relevant sections, noting their primary verification owners are Phase 318/319.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Call-Graph Attestation table (SC#5) with the drift items + keeper-dependency clean result</name>
  <files>.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md</files>
  <read_first>
    - `316-RESEARCH.md` §1 (the FULL verification table — embed/reference it), §1.12 (keeper transitional-state), §2 (`_hasAnyLazyPass` reader-set), §3 (keeper dependency clean), §8 Pitfall 1 + Pitfall 3, §9 (open questions), §10 "Plan 316-04" scope.
    - `316-SPEC.md` (the already-authored ADD/REMOVE/open-item sections — confirm every file:line they cite appears in the attestation).
    - `REQUIREMENTS.md` (the 38 IDs).
    - Re-grep a representative spread of the §1 anchors against HEAD to confirm the table still holds (the milestone has not mutated source yet; this is the last verification before the SPEC is consumed).
  </read_first>
  <action>
    Append a `## Call-Graph Attestation` section to `316-SPEC.md`. Author/embed the verification table covering every cited file:line across `DegenerusGame`/modules/`BurnieCoin`/`BurnieCoinflip`/`DegenerusVault`/`StakedDegenerusStonk`/`ContractAddresses` + the `StreakKeeperV2`/`AfKing` keeper, with a MATCH / DRIFT / MISSING verdict per row (reference RESEARCH §1 as the substrate; the SPEC's table need not duplicate every row verbatim but MUST cover every file:line the SPEC's design sections actually cite). Record explicitly, so the SPEC contains ZERO unverified "by construction" / "single fn reaches all paths" claims:
    (1) The drift items: the JackpotModule auto-rebuy block 800–808 (+2 drift), the `_distributePayout` solvency check decl :705/check ~738, and the IDegenerusGame "setAutoRebuy/setAutoRebuyTakeProfit/setAfKingMode NOT present here, present in Vault's local interface" resolution.
    (2) The keeper-dependency CLEAN result: zero-match grep over the full RM-symbol set; only game coupling = `hasAnyLazyPass` at keeper :671/:974; the deletion is dependency-safe IFF PROTO-01 ships in the same diff. State the PROTO-side keeper interface obligation (keeper still calls `pullForKeeper`/`mintForKeeper`; PROTO-02 adds `burnForKeeper`; the keeper IMPL switches).
    (3) The keeper transitional-state caveat (Pitfall 1): the live keeper is a MIXED state that does NOT match PLAN-CRANK §9's "done this session" claim; the SPEC locks against the intended end-state — cite the §1.12 drift so the plan-checker does not treat §9 as ground truth.
    (4) The box-cursor VRF-rotation-orphan-index coupling landmine (Pitfall 3): note it as the single biggest design risk; the box cursor follows the v45 `a303ae18` re-issue path; AUDIT re-verifies the freeze invariant under rotation.
    Add a one-line attestation statement that this table satisfies SC#5 (every cited file:line grep-verified against HEAD; the SPEC carries no unverified call-graph claim). When writing the prose, state the SPEC contains no unverified "by construction" claim only inside that explicit negation sentence (and inside any quoted feedback-rule reference) — do NOT use the bare phrase elsewhere.
  </action>
  <verify>
    <automated>F=.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md; grep -q '## Call-Graph Attestation' "$F" && grep -q 'hasAnyLazyPass' "$F" && grep -Eq 'DRIFT|✗' "$F" && grep -q '671' "$F" && grep -q '974' "$F"</automated>
  </verify>
  <acceptance_criteria>
    - `316-SPEC.md` contains a `## Call-Graph Attestation` section with a verification table covering every file:line the SPEC's design sections cite, each with a MATCH/DRIFT/MISSING verdict.
    - The table records the drift items: JackpotModule auto-rebuy block 800–808 (+2), `_distributePayout` decl :705/check ~738, and the IDegenerusGame setAutoRebuy-not-present resolution.
    - The section records the keeper-dependency clean result (zero-match grep over the RM-symbol set; only coupling = `hasAnyLazyPass` at keeper :671/:974; dependency-safe IFF PROTO-01 ships same diff) and the PROTO-side `pullForKeeper`→`burnForKeeper` interface obligation.
    - The section records the keeper transitional-state caveat (PLAN-CRANK §9 "done this session" false vs live source; lock against intended end-state) and the box-cursor VRF-rotation landmine (Pitfall 3, follows `a303ae18`).
    - The SPEC contains no unverified "by construction" / "single fn reaches all paths" claim (the phrase appears only in its explicit negation / a quoted feedback-rule reference).
    - NO fenced solidity implementation bodies.
    - Zero `contracts/`/`test/` files modified by this plan.
  </acceptance_criteria>
  <done>`316-SPEC.md` has the `## Call-Graph Attestation` section satisfying SC#5: every cited file:line verified, drift items + keeper-dependency clean result + transitional-state caveat + box-cursor landmine recorded, zero unverified call-graph claims.</done>
</task>

<task type="auto">
  <name>Task 2: Author the Requirement Design Coverage map + assemble and self-consistency-check the final SPEC</name>
  <files>.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md</files>
  <read_first>
    - `316-SPEC.md` (ALL sections authored by plans 316-01/02/03 + the attestation from Task 1 — read the whole assembled document).
    - `REQUIREMENTS.md` (the 38 IDs + the per-phase primary-owner table), `ROADMAP.md` (the Phase 316 goal + the 5 success criteria).
    - `316-RESEARCH.md` §10 (the 4-plan section ownership map — confirms which SPEC section owns which design).
  </read_first>
  <action>
    Prepend (near the top of `316-SPEC.md`, after any title/intro) a `## Requirement Design Coverage` section: a table mapping each of the 38 v46.0 requirement IDs (PROTO-01..05, CRANK-01..04, REW-01..04, SUB-01..09, RM-01..06, SAFE-01..04, GAS-01..06) to the SPEC section that LOCKS its design, plus its primary verification owner phase (Phase 316 for PROTO-01/SUB-09/RM-04; 317 for the IMPL set; 318 for SAFE-*; 319 for GAS-*). For SAFE-* and GAS-*, map to the ADD/REMOVE sections that establish the safety/gas properties (e.g. SAFE-01 faucet → the three-locks reward design; SAFE-04 freeze → the VRF-freeze-obligation-retirement section; GAS-* → the gas levers noted in the ADD section, with numeric calibration deferred to Phase 319/OPEN-A). Add a `## Success Criteria Coverage` mini-table showing each of the 5 ROADMAP success criteria mapped to the SPEC section(s) that satisfy it, marked COVERED.
    Then do a self-consistency pass over the WHOLE document: verify every section header referenced by the coverage map actually exists; verify the document reads as one coherent design-lock spec (a brief intro paragraph stating this SPEC locks the FULL v46.0 add+remove design and is the load-bearing input for Phases 317/318/319/320; the design sections; the attestation; the coverage maps). Fix any header-name mismatches between the coverage map and the actual section headers (edit the map to match — do NOT rename the design sections the other plans depend on). Confirm no `.sol`/`.t.sol` was touched anywhere in the phase.
  </action>
  <verify>
    <automated>F=.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md; grep -q '## Requirement Design Coverage' "$F" && grep -q '## Success Criteria Coverage' "$F" && grep -q 'PROTO-01' "$F" && grep -q 'GAS-06' "$F" && grep -q 'SUB-09' "$F" && grep -q 'SAFE-04' "$F" && [ "$(grep -c 'COVERED' "$F")" -ge 5 ] && [ -z "$(git -C /home/zak/Dev/PurgeGame/degenerus-audit diff --name-only -- contracts/ test/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `316-SPEC.md` contains a `## Requirement Design Coverage` table mapping ALL 38 v46.0 requirement IDs (PROTO-01..05, CRANK-01..04, REW-01..04, SUB-01..09, RM-01..06, SAFE-01..04, GAS-01..06) to the locking SPEC section + the primary verification owner phase.
    - The map shows PROTO-01/SUB-09/RM-04 owned at Phase 316, the IMPL set at 317, SAFE-* at 318, GAS-* at 319; SAFE-*/GAS-* designs are mapped to the ADD/REMOVE sections that establish their properties.
    - `316-SPEC.md` contains a `## Success Criteria Coverage` table mapping each of the 5 ROADMAP success criteria to its satisfying SPEC section(s), each marked COVERED (≥5 COVERED rows).
    - Every section header referenced by the coverage map exists in the document (self-consistent); the document reads as one coherent design-lock spec with an intro stating it is the load-bearing input for Phases 317/318/319/320.
    - No `.sol`/`.t.sol` file appears in the phase git diff (`git diff --name-only -- contracts/ test/` is empty).
    - NO fenced solidity implementation bodies.
  </acceptance_criteria>
  <done>`316-SPEC.md` has the `## Requirement Design Coverage` (38/38) and `## Success Criteria Coverage` (5/5 COVERED) maps, is self-consistent, and the phase touched zero `contracts/`/`test/` files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (cost of a wrong attestation/assembly)

| Boundary | Description |
|----------|-------------|
| attestation table → plan-checker / downstream phases | If the attestation locks an unverified or stale claim, the IMPL deletes/wires the wrong thing. |
| requirement-coverage map → milestone completeness | If a requirement design is silently unmapped, it ships unbuilt or under-built. |
| keeper-dependency claim → RM-* deletion safety | A wrong "keeper is clean" attestation could greenlight a deletion that strands the keeper. |

## STRIDE Threat Register (cost of a wrong design lock)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-316-13 | Tampering | A SPEC that locks an unverified "by construction" call-graph claim | mitigate | Task 1 grep-verifies every cited file:line against HEAD, records drift items, and states the SPEC carries no unverified call-graph claim (SC#5). |
| T-316-14 | Information Disclosure | A SPEC that silently drops a requirement design | mitigate | Task 2 maps all 38 requirement IDs to locking sections; the coverage table makes any gap visible. |
| T-316-15 | Elevation | A wrong keeper-dependency attestation greenlighting an unsafe deletion | mitigate | Task 1 records the zero-match grep over the full RM-symbol set + the only-coupling-is-hasAnyLazyPass finding (dependency-safe IFF PROTO-01 ships same diff). |
| T-316-16 | Repudiation | A SPEC that omits the box-cursor VRF-rotation landmine | mitigate | Task 1 records Pitfall 3 (box cursor follows `a303ae18`; AUDIT re-verifies freeze under rotation). |
| T-316-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero packages installed (read-only doc authoring). |
</threat_model>

<verification>
- `316-SPEC.md` contains the `## Call-Graph Attestation`, `## Requirement Design Coverage`, and `## Success Criteria Coverage` section headers.
- All 38 requirement IDs appear in the coverage map; all 5 success criteria are marked COVERED.
- The keeper-dependency clean result and the box-cursor VRF-rotation landmine are recorded.
- `git diff --name-only -- contracts/ test/` is empty across the whole phase (zero source-tree mutation).
</verification>

<success_criteria>
- ROADMAP SC#5 (every cited file:line grep-verified; zero "by construction" claims; keeper does NOT depend on anything RM-* deletes; zero contracts/test mutations) is satisfied by the attestation table.
- The assembled `316-SPEC.md` locks the FULL v46.0 add+remove design for all 38 requirements and is the load-bearing input for Phases 317/318/319/320.
- PROTO-01 and RM-04 reader-set attestations are re-confirmed in the attestation table.
</success_criteria>

<output>
Create `.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-04-SUMMARY.md` when done.
</output>
