---
phase: 316-spec-crank-subscription-legacy-removal-design-lock-spec
plan: 04
type: execute
wave: 5
depends_on: ["316-01", "316-02", "316-03", "316-05"]
files_modified:
  - .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md
autonomous: true
requirements: [PROTO-01, RM-04, JGAS-01]
must_haves:
  truths:
    - "316-SPEC.md contains a ## Call-Graph Attestation table where every cited file:line across DegenerusGame/modules/BurnieCoin/BurnieCoinflip/DegenerusVault/StakedDegenerusStonk/ContractAddresses + the StreakKeeperV2/AfKing keeper is grep-verified against source HEAD (MATCH / DRIFT / MISSING per RESEARCH §1 + §J1)."
    - "316-SPEC.md contains ZERO unverified 'by construction' / 'single fn reaches all paths' claims — the attestation records the keeper-dependency clean result (zero-match grep over the RM-symbol set), the RM drift items, AND the JGAS J1 footprint + J5 VRF verdict + the two +1 JGAS resume-check drifts (jackpot :348→349, advance :452-455→453-456), the keeper transitional-state caveat, and the IDegenerusGame setAutoRebuy-not-present resolution."
    - "316-SPEC.md confirms the keeper does NOT depend on anything RM-* deletes (only coupling = hasAnyLazyPass at keeper :671/:974) and records the box-cursor VRF-rotation-orphan-index coupling landmine (Pitfall 3) AND the JGAS J5 freeze-SAFE verdict (resume branch never calls _unlockRng; single-call removes a cross-tx resumeEthPool carry — a rotation-robustness improvement)."
    - "316-SPEC.md is assembled into a single coherent design-lock document: a top section maps all 42 v46.0 requirement designs (PROTO/CRANK/REW/SUB/RM/SAFE/GAS/JGAS) to their locked SPEC sections; the SPEC-owned set is the FOUR Phase-316 reqs (PROTO-01/SUB-09/RM-04/JGAS-01); the 5 ROADMAP success criteria are each shown COVERED."
    - "316-SPEC.md is self-consistent: every section header referenced by the requirement-coverage map exists in the document (incl. the new ## JGAS-01 Decision Gate); zero contracts/ and zero test/ mutations across the whole phase."
  artifacts:
    - path: ".planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md"
      provides: "Call-graph attestation table + 42-requirement coverage map + assembled final SPEC (## Call-Graph Attestation, ## Requirement Design Coverage)"
      contains: "Call-Graph Attestation"
  key_links:
    - from: "316-SPEC.md attestation table"
      to: "316-RESEARCH.md §1 + §J1 verification tables"
      via: "embed/reference as the SC#5 attestation substrate (incl. the JGAS footprint)"
      pattern: "MATCH"
    - from: "316-SPEC.md requirement-coverage map"
      to: "the 42 v46.0 requirement IDs (PROTO/CRANK/REW/SUB/RM/SAFE/GAS/JGAS)"
      via: "each design mapped to its locked SPEC section"
      pattern: "JGAS-01"
---

<objective>
Author the `## Call-Graph Attestation` table and the `## Requirement Design Coverage` map of `316-SPEC.md`, then assemble the document into a single coherent design-lock spec. The attestation is the SC#5 deliverable: every cited file:line grep-verified against HEAD (now including the JGAS J1 footprint across both modules), zero unverified "by construction" claims, the keeper-dependency clean result, the RM + JGAS drift items, and the JGAS J5 VRF verdict recorded.

Purpose: SC#5 is the phase's verification backbone — the entire reason this is a "grep-verify every call-graph claim" phase. The attestation table is what the plan-checker and downstream phases trust. The requirement-coverage map proves all 42 v46.0 designs are locked somewhere in the SPEC (not silently dropped) — including the JGAS-01..04 sub-thread folded in after the original plans — and that the 5 ROADMAP success criteria are each covered. This plan runs LAST because it attests and assembles the content the four design plans (316-01/02/03/05) authored.

Output: The attestation + coverage sections of `316-SPEC.md` and a self-consistency pass over the whole document. Zero `contracts/` and zero `test/` mutations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-RESEARCH.md
@.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md

<interfaces>
<!-- The attestation substrate. RESEARCH §1 + §J1 ARE the verified tables — embed/reference them. The drift items the SPEC must record so it contains zero unverified claims: -->

Verified-clean (record as MATCH): all of RESEARCH §1.1 (DegenerusGame), §1.2 (storage), §1.5 (BurnieCoinflip interiors), §1.6 (IDegenerusGame), §1.7 (IBurnieCoinflip), §1.8 (Vault), §1.9 (sStonk), AND §J1 (the JGAS two-call-split footprint — all constant values exact-match by value).

Drift items the SPEC MUST record (so it has zero unverified "by construction" claims):
- DRIFT (cosmetic ≤+4): JackpotModule `_addClaimableEth` auto-rebuy block 798–806 (doc) → 800–808 (live, +2); DegeneretteModule `_distributePayout` solvency check — decl :705, check ~738 (doc said 742, inside body).
- DRIFT (cosmetic +1, JGAS): JackpotModule resume-check req `:348` → live `:349` (comment vs `if`); AdvanceModule resume-check req `:452-455` → live `:453-456` (comment vs `if` block). Both are the requirement citing the leading comment line, not the guard.
- MISSING (resolves an open): `setAutoRebuy`/`setAutoRebuyTakeProfit`/`setAfKingMode` are NOT declared in IDegenerusGame (they ARE in Vault's local interface) — record as the resolution to PLAN-V47 §5.6's "verify whether present".
- Keeper transitional-state caveat (RESEARCH §1.12): live keeper = MIXED state (19× `pullForKeeper`, 5× `mintForKeeper`, 2× `creditFlip`, OLD `sweep(startIdx,count)`, no `sweepCursor`/`reinvestPct`/`windowPaid`); PLAN-CRANK §9 "done this session" is FALSE vs live source → the SPEC locks against the INTENDED end-state. Record this so the plan-checker does not treat §9 as ground truth.
- PROTO-side keeper interface obligation: keeper still calls `IBurnie.pullForKeeper`/`mintForKeeper` (BurnieCoin has neither yet, deferred-selector by design); PROTO-02 adds `burnForKeeper`, the keeper IMPL switches its calls.

JGAS attestation substrate (RESEARCH §J1 footprint + §J5 VRF verdict — embed both):
- J1 footprint (all ✓ MATCH by value): JackpotModule `SPLIT_NONE/CALL1/CALL2` :197/199/201, `JACKPOT_MAX_WINNERS=160` :219 (dead on removal), `DAILY_ETH_MAX_WINNERS=305` :227 (PRESERVED), `DAILY_JACKPOT_SCALE_MAX_BPS=63_600` :248 (PRESERVED), buckets 159/95/50/1 (PRESERVED), `resumeEthPool` storage :994 slot 33 + reads :349/1201/1252-1253/~1347-1348, `_resumeDailyEth` :1186, `splitMode` :1248, `call1Bucket` :1270-1278/1287-1288, split-threshold :476-483; AdvanceModule `STAGE_JACKPOT_ETH_RESUME=8` :70 + assignment :455 + resume-check :453-456. Stage numbers NOT load-bearing (§J2: function-local `stage`, assign+emit only, `Advance` event not consumed on-chain).
- J5 VRF verdict (HEADLINE): the ETH-resume branch (:453-456) never calls `_unlockRng`; the rng lock is held across the split + the same `randWord` re-consumed in call2; single-call collapses two same-word consumptions into one, does NOT move `_unlockRng` (still coin-tickets stage :467), adds NO new in-window player-mutable input, and removes a cross-tx `resumeEthPool` carry → a VRF-rotation-robustness IMPROVEMENT (consistent with the Phase 312 `a303ae18` work). Only residual risk = gas-fits (liveness), gated on JGAS-04. AUDIT-320 re-attests freeze under single-call + under VRF rotation.

Keeper-dependency clean result (RESEARCH §3): zero-match grep over the full RM-symbol set (`syncAfKingLazyPassFromCoin`/`afKingModeFor`/`afKingActivatedLevelFor`/`setAfKingMode`/`deactivateAfKingFromCoin`/`setAutoRebuy`/`setAutoRebuyTakeProfit`/`autoRebuyState`/`AutoRebuyState`/`_processAutoRebuy`/`_calcAutoRebuy`/`settleFlipModeChange`/`_afKingRecyclingBonus`/`_afKingDeityBonus`/`gameSetAutoRebuy`/`gameSetAfKingMode`); only game coupling = `hasAnyLazyPass` at keeper :671 (subscribe gate) / :974 (renewal sweep gate). Deletion dependency-safe IFF PROTO-01 ships same diff. (The JGAS `SPLIT_*`/`resumeEthPool`/`STAGE_JACKPOT_ETH_RESUME` symbols are internal to the jackpot/advance modules — the keeper does not reference them either; confirm zero-match for the JGAS symbol set too.)

Box-cursor VRF-rotation landmine (RESEARCH §8 Pitfall 3 / memory project_vrf_rotation_midday_orphan_index): the OPEN-D box cursor re-couples to the v45 orphan-index keyspace → must follow the `a303ae18` detect-preserve-re-issue path; the AUDIT phase re-verifies the freeze invariant under rotation WITH the new cursor.

The 42 v46.0 requirement IDs to map (REQUIREMENTS.md): PROTO-01..05, CRANK-01..04, REW-01..04, SUB-01..09, RM-01..06, SAFE-01..04, GAS-01..06, JGAS-01..04. SPEC primary owners = PROTO-01/SUB-09/RM-04/**JGAS-01** (FOUR, not three). The rest have downstream primary owners but their DESIGNS are locked at this SPEC. Owning-phase map for the JGAS sub-thread: JGAS-01 → Phase 316 (SPEC, the decision gate), JGAS-02 → Phase 317 (IMPL, the deletion in the batched diff), JGAS-03 → Phase 318 (TST, 305-winner single-call correctness), JGAS-04 → Phase 319 (GAS, the empirical 305-winner single-call measurement / finality gate). (SAFE-* / GAS-* designs are locked as the safety/gas properties the ADD/REMOVE/JGAS sections must hold — map them to the relevant sections; their primary verification owners are Phase 318/319. Phase 320 TERMINAL re-attests all 42 and owns 0 primarily.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Call-Graph Attestation table (SC#5) with the RM + JGAS drift items + keeper-dependency clean result + the J5 VRF verdict</name>
  <files>.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md</files>
  <read_first>
    - `316-RESEARCH.md` §1 (the FULL RM verification table — embed/reference it), §J1 (the JGAS two-module footprint table — embed/reference it), §J2 (stage-machine not load-bearing), §J5 (the JGAS VRF verdict), §1.12 (keeper transitional-state), §2 (`_hasAnyLazyPass` reader-set), §3 (keeper dependency clean), §8 Pitfall 1 + Pitfall 3, §9 (open questions), §J6 (the planning recommendation to embed the J1 table + J5 verdict here + go 38→42).
    - `316-SPEC.md` (the already-authored ADD/REMOVE/open-item/JGAS sections — confirm every file:line they cite, incl. the JGAS section's footprint, appears in the attestation).
    - `REQUIREMENTS.md` (the 42 IDs + the per-phase primary-owner table; the JGAS owning-phase map).
    - Re-grep a representative spread of the §1 + §J1 anchors against HEAD to confirm the tables still hold (the milestone has not mutated source yet; this is the last verification before the SPEC is consumed). Confirm at minimum the JGAS anchors `SPLIT_NONE` :197, `STAGE_JACKPOT_ETH_RESUME=8` :70, `resumeEthPool` storage :994, the jackpot resume-check :349, the advance resume-check :453-456.
  </read_first>
  <action>
    Append a `## Call-Graph Attestation` section to `316-SPEC.md`. Author/embed the verification table covering every cited file:line across `DegenerusGame`/modules/`BurnieCoin`/`BurnieCoinflip`/`DegenerusVault`/`StakedDegenerusStonk`/`ContractAddresses` + the `StreakKeeperV2`/`AfKing` keeper, with a MATCH / DRIFT / MISSING verdict per row (reference RESEARCH §1 + §J1 as the substrate; the SPEC's table need not duplicate every row verbatim but MUST cover every file:line the SPEC's design sections actually cite — INCLUDING the JGAS footprint the new `## JGAS-01 Decision Gate` section cites). Record explicitly, so the SPEC contains ZERO unverified "by construction" / "single fn reaches all paths" claims:
    (1) The RM drift items: the JackpotModule auto-rebuy block 800–808 (+2 drift), the `_distributePayout` solvency check decl :705/check ~738, and the IDegenerusGame "setAutoRebuy/setAutoRebuyTakeProfit/setAfKingMode NOT present here, present in Vault's local interface" resolution.
    (2) The JGAS footprint attestation (embed RESEARCH §J1): a sub-table of the JGAS two-module deletion surface — JackpotModule `SPLIT_*` :197/199/201, `JACKPOT_MAX_WINNERS=160` :219 (dead on removal), `resumeEthPool` storage :994 slot 33 + reads :349/1201/1252-1253/~1347-1348, `_resumeDailyEth` :1186, `splitMode` :1248, `call1Bucket` :1270-1278/1287-1288, split-threshold :476-483; AdvanceModule `STAGE_JACKPOT_ETH_RESUME=8` :70 + assignment :455 + resume-check :453-456 — each MATCH-by-value, with the TWO cosmetic +1 JGAS resume-check drifts recorded (jackpot :348→349, advance :452-455→453-456). State that `DAILY_ETH_MAX_WINNERS=305` :227 / `DAILY_JACKPOT_SCALE_MAX_BPS=63_600` :248 / the 159/95/50/1 buckets are PRESERVED (NOT in the deletion set) and that stage numbers are NOT load-bearing (§J2).
    (3) The J5 VRF/freeze-SAFE verdict (HEADLINE, re-stated here as attestation): the ETH-resume branch (:453-456) never calls `_unlockRng`; the lock is held across the split + the same `randWord` re-consumed in call2; single-call collapses two consumptions to one, `_unlockRng` placement unmoved (:467), no new in-window player-mutable input, removes a cross-tx `resumeEthPool` carry (rotation-robustness improvement). The only residual risk is gas-fits (liveness), gated on JGAS-04. AUDIT-320 re-attests freeze under single-call + under VRF rotation.
    (4) The keeper-dependency CLEAN result: zero-match grep over the full RM-symbol set; only game coupling = `hasAnyLazyPass` at keeper :671/:974; the deletion is dependency-safe IFF PROTO-01 ships in the same diff. State the keeper also does not reference the JGAS `SPLIT_*`/`resumeEthPool`/`STAGE_JACKPOT_ETH_RESUME` symbols (jackpot/advance-internal). State the PROTO-side keeper interface obligation (keeper still calls `pullForKeeper`/`mintForKeeper`; PROTO-02 adds `burnForKeeper`; the keeper IMPL switches).
    (5) The keeper transitional-state caveat (Pitfall 1): the live keeper is a MIXED state that does NOT match PLAN-CRANK §9's "done this session" claim; the SPEC locks against the intended end-state — cite the §1.12 drift so the plan-checker does not treat §9 as ground truth.
    (6) The box-cursor VRF-rotation-orphan-index coupling landmine (Pitfall 3): note it as the single biggest ADD-side design risk; the box cursor follows the v45 `a303ae18` re-issue path; AUDIT re-verifies the freeze invariant under rotation.
    Add a one-line attestation statement that this table satisfies SC#5 (every cited file:line grep-verified against HEAD; the SPEC carries no unverified call-graph claim). When writing the prose, state the SPEC contains no unverified "by construction" claim only inside that explicit negation sentence (and inside any quoted feedback-rule reference) — do NOT use the bare phrase elsewhere.
  </action>
  <verify>
    <automated>F=.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md; grep -q '## Call-Graph Attestation' "$F" && grep -q 'hasAnyLazyPass' "$F" && grep -Eq 'DRIFT|✗' "$F" && grep -q '671' "$F" && grep -q '974' "$F" && grep -q 'STAGE_JACKPOT_ETH_RESUME' "$F" && grep -q '_unlockRng' "$F" && grep -q 'resumeEthPool' "$F"</automated>
  </verify>
  <acceptance_criteria>
    - `316-SPEC.md` contains a `## Call-Graph Attestation` section with a verification table covering every file:line the SPEC's design sections cite (incl. the JGAS footprint), each with a MATCH/DRIFT/MISSING verdict.
    - The table records the RM drift items: JackpotModule auto-rebuy block 800–808 (+2), `_distributePayout` decl :705/check ~738, and the IDegenerusGame setAutoRebuy-not-present resolution.
    - The table embeds the JGAS J1 footprint (JackpotModule `SPLIT_*` :197/199/201, `JACKPOT_MAX_WINNERS=160` :219 dead-on-removal, `resumeEthPool` :994 slot 33, `_resumeDailyEth` :1186, `splitMode` :1248, `call1Bucket` :1270-1278, threshold :476-483; AdvanceModule `STAGE_JACKPOT_ETH_RESUME=8` :70 + :455 + :453-456) with the TWO cosmetic +1 JGAS resume-check drifts (jackpot :348→349, advance :452-455→453-456), states the 305-ceiling/scale/buckets PRESERVED, and that stage numbers are not load-bearing.
    - The section records the JGAS J5 VRF/freeze-SAFE verdict (resume branch never calls `_unlockRng`; single-call removes a cross-tx `resumeEthPool` carry = rotation-robustness improvement; `_unlockRng` unmoved; only residual risk = gas-fits gated on JGAS-04; AUDIT-320 re-attests under rotation).
    - The section records the keeper-dependency clean result (zero-match over the RM-symbol set AND the JGAS symbols; only coupling = `hasAnyLazyPass` at keeper :671/:974; dependency-safe IFF PROTO-01 ships same diff) and the PROTO-side `pullForKeeper`→`burnForKeeper` interface obligation.
    - The section records the keeper transitional-state caveat (PLAN-CRANK §9 "done this session" false vs live source) and the box-cursor VRF-rotation landmine (Pitfall 3, follows `a303ae18`).
    - The SPEC contains no unverified "by construction" / "single fn reaches all paths" claim (the phrase appears only in its explicit negation / a quoted feedback-rule reference).
    - NO fenced solidity implementation bodies.
    - Zero `contracts/`/`test/` files modified by this plan.
  </acceptance_criteria>
  <done>`316-SPEC.md` has the `## Call-Graph Attestation` section satisfying SC#5: every cited file:line verified (incl. the JGAS footprint), RM + JGAS drift items + the J5 VRF verdict + keeper-dependency clean result + transitional-state caveat + box-cursor landmine recorded, zero unverified call-graph claims.</done>
</task>

<task type="auto">
  <name>Task 2: Author the 42-Requirement Design Coverage map + assemble and self-consistency-check the final SPEC</name>
  <files>.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md</files>
  <read_first>
    - `316-SPEC.md` (ALL sections authored by plans 316-01/02/03/05 + the attestation from Task 1 — read the whole assembled document, INCLUDING the new `## JGAS-01 Decision Gate`).
    - `REQUIREMENTS.md` (the 42 IDs + the per-phase primary-owner table — note the JGAS-01..04 rows with owners 316/317/318/319; the 42/42 coverage block), `ROADMAP.md` (the Phase 316 goal + the 5 success criteria; SC#4 now carries the JGAS-01 clause).
    - `316-RESEARCH.md` §10 + §J6 (the plan section-ownership map — confirms which SPEC section owns which design; §J6 confirms 316-05 owns JGAS-01 + the 38→42 coverage update).
  </read_first>
  <action>
    Prepend (near the top of `316-SPEC.md`, after any title/intro) a `## Requirement Design Coverage` section: a table mapping each of the 42 v46.0 requirement IDs (PROTO-01..05, CRANK-01..04, REW-01..04, SUB-01..09, RM-01..06, SAFE-01..04, GAS-01..06, JGAS-01..04) to the SPEC section that LOCKS its design, plus its primary verification owner phase (Phase 316 for the FOUR SPEC-owned reqs PROTO-01/SUB-09/RM-04/JGAS-01; 317 for the IMPL set incl. JGAS-02; 318 for SAFE-* + JGAS-03; 319 for GAS-* + JGAS-04). Map the JGAS sub-thread: JGAS-01 → `## JGAS-01 Decision Gate` (owned Phase 316), JGAS-02 → `## JGAS-01 Decision Gate` (footprint) + 316-02's `## Storage Slot-Shift Plan` (the −2 slot consequence), owner Phase 317; JGAS-03 → `## JGAS-01 Decision Gate` (correctness criteria), owner Phase 318; JGAS-04 → `## JGAS-01 Decision Gate` (worst-case derivation), owner Phase 319. For SAFE-* and GAS-*, map to the ADD/REMOVE/JGAS sections that establish the safety/gas properties (e.g. SAFE-01 faucet → the three-locks reward design; SAFE-04 freeze → the `## VRF-Freeze Obligation Retirement` + the JGAS J5 verdict; GAS-* → the gas levers noted in the ADD section + the JGAS worst-case derivation, with numeric calibration deferred to Phase 319/OPEN-A). Add a `## Success Criteria Coverage` mini-table showing each of the 5 ROADMAP success criteria mapped to the SPEC section(s) that satisfy it, marked COVERED (SC#4 maps to BOTH the `## REMOVE Footprint`/`## Storage Slot-Shift Plan` AND the `## JGAS-01 Decision Gate`).
    Then do a self-consistency pass over the WHOLE document: verify every section header referenced by the coverage map actually exists (incl. `## JGAS-01 Decision Gate`); verify the document reads as one coherent design-lock spec (a brief intro paragraph stating this SPEC locks the FULL v46.0 add+remove+JGAS-split-removal design across all 42 requirements and is the load-bearing input for Phases 317/318/319/320). Fix any header-name mismatches between the coverage map and the actual section headers (edit the map to match — do NOT rename the design sections the other plans depend on). Confirm the SPEC-owned set reads as FOUR (PROTO-01/SUB-09/RM-04/JGAS-01), not three. Confirm no `.sol`/`.t.sol` was touched anywhere in the phase.
  </action>
  <verify>
    <automated>F=.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md; grep -q '## Requirement Design Coverage' "$F" && grep -q '## Success Criteria Coverage' "$F" && grep -q 'PROTO-01' "$F" && grep -q 'GAS-06' "$F" && grep -q 'SUB-09' "$F" && grep -q 'SAFE-04' "$F" && grep -q 'JGAS-01' "$F" && grep -q 'JGAS-04' "$F" && [ "$(grep -c 'COVERED' "$F")" -ge 5 ] && [ -z "$(git -C /home/zak/Dev/PurgeGame/degenerus-audit diff --name-only -- contracts/ test/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `316-SPEC.md` contains a `## Requirement Design Coverage` table mapping ALL 42 v46.0 requirement IDs (PROTO-01..05, CRANK-01..04, REW-01..04, SUB-01..09, RM-01..06, SAFE-01..04, GAS-01..06, JGAS-01..04) to the locking SPEC section + the primary verification owner phase.
    - The map shows the FOUR SPEC-owned reqs (PROTO-01/SUB-09/RM-04/JGAS-01) owned at Phase 316, the IMPL set incl. JGAS-02 at 317, SAFE-* + JGAS-03 at 318, GAS-* + JGAS-04 at 319; the JGAS-01..04 rows map to `## JGAS-01 Decision Gate` (+ 316-02's slot plan for JGAS-02's slot consequence); SAFE-*/GAS-* designs are mapped to the ADD/REMOVE/JGAS sections that establish their properties.
    - `316-SPEC.md` contains a `## Success Criteria Coverage` table mapping each of the 5 ROADMAP success criteria to its satisfying SPEC section(s), each marked COVERED (≥5 COVERED rows); SC#4 maps to the REMOVE + JGAS sections.
    - Every section header referenced by the coverage map exists in the document (self-consistent), incl. `## JGAS-01 Decision Gate`; the document reads as one coherent design-lock spec with an intro stating it locks the full v46.0 add+remove+JGAS design for all 42 reqs and is the load-bearing input for Phases 317/318/319/320; the SPEC-owned set reads as FOUR.
    - No `.sol`/`.t.sol` file appears in the phase git diff (`git diff --name-only -- contracts/ test/` is empty).
    - NO fenced solidity implementation bodies.
  </acceptance_criteria>
  <done>`316-SPEC.md` has the `## Requirement Design Coverage` (42/42, four SPEC-owned) and `## Success Criteria Coverage` (5/5 COVERED) maps, is self-consistent (incl. the JGAS section), and the phase touched zero `contracts/`/`test/` files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (cost of a wrong attestation/assembly)

| Boundary | Description |
|----------|-------------|
| attestation table → plan-checker / downstream phases | If the attestation locks an unverified or stale claim (RM or JGAS), the IMPL deletes/wires the wrong thing. |
| requirement-coverage map → milestone completeness | If a requirement design is silently unmapped (esp. the late-folded JGAS-01..04), it ships unbuilt or under-built. |
| keeper-dependency claim → RM-* / JGAS deletion safety | A wrong "keeper is clean" attestation could greenlight a deletion that strands the keeper. |

## STRIDE Threat Register (cost of a wrong design lock)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-316-13 | Tampering | A SPEC that locks an unverified "by construction" call-graph claim (RM or JGAS) | mitigate | Task 1 grep-verifies every cited file:line against HEAD (incl. the JGAS J1 footprint + the two +1 resume-check drifts), records drift items, and states the SPEC carries no unverified call-graph claim (SC#5). |
| T-316-14 | Information Disclosure | A SPEC that silently drops a requirement design (esp. the late-folded JGAS sub-thread) | mitigate | Task 2 maps all 42 requirement IDs (incl. JGAS-01..04) to locking sections; the coverage table makes any gap visible. |
| T-316-15 | Elevation | A wrong keeper-dependency attestation greenlighting an unsafe deletion | mitigate | Task 1 records the zero-match grep over the full RM-symbol set + the JGAS symbols + the only-coupling-is-hasAnyLazyPass finding (dependency-safe IFF PROTO-01 ships same diff). |
| T-316-16 | Repudiation | A SPEC that omits the box-cursor VRF-rotation landmine or the JGAS J5 freeze verdict | mitigate | Task 1 records Pitfall 3 (box cursor follows `a303ae18`) AND the JGAS J5 verdict (resume branch never calls `_unlockRng`; single-call removes a cross-tx carry; AUDIT-320 re-attests under rotation). |
| T-316-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero packages installed (read-only doc authoring). |
</threat_model>

<verification>
- `316-SPEC.md` contains the `## Call-Graph Attestation`, `## Requirement Design Coverage`, and `## Success Criteria Coverage` section headers.
- All 42 requirement IDs appear in the coverage map (incl. JGAS-01..04); all 5 success criteria are marked COVERED; the SPEC-owned set reads as FOUR.
- The keeper-dependency clean result, the JGAS J1 footprint + the two +1 drifts, the J5 VRF verdict, and the box-cursor VRF-rotation landmine are recorded.
- `git diff --name-only -- contracts/ test/` is empty across the whole phase (zero source-tree mutation).
</verification>

<success_criteria>
- ROADMAP SC#5 (every cited file:line grep-verified; zero "by construction" claims; keeper does NOT depend on anything RM-* deletes; zero contracts/test mutations) is satisfied by the attestation table, now extended to the JGAS footprint.
- The assembled `316-SPEC.md` locks the FULL v46.0 add+remove+JGAS-split-removal design for all 42 requirements and is the load-bearing input for Phases 317/318/319/320.
- PROTO-01, RM-04, and JGAS-01 reader-set / footprint attestations are re-confirmed in the attestation table.
</success_criteria>

<output>
Create `.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-04-SUMMARY.md` when done.
</output>
</output>
