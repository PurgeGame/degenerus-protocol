---
phase: 316-spec-crank-subscription-legacy-removal-design-lock-spec
plan: 03
type: execute
wave: 4
depends_on: ["316-01", "316-02", "316-05"]
files_modified:
  - .planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md
autonomous: false
requirements: [SUB-09]
must_haves:
  truths:
    - "316-SPEC.md locks the quantity model = flat dailyQuantity (uint8, min 1) COEXIST with optional reinvestPct (uint8), effective = max(flat, floor(claimable×pct/price)), packed into one flags byte + reinvestPct (no new slot), denominated in price-units (1 unit = 1 mintPrice = one 1-price lootbox, TICKET_SCALE=400)."
    - "316-SPEC.md locks the funding waterfall = the existing drainGameCreditFirst=true model (Claimable / Combined / DirectEth → InsufficientPool skip); claimable-only = empty _poolOf with no new flag."
    - "316-SPEC.md locks the two-tier skip-kill: NORMAL subs cancel on a funding skip via in-sweep swap-pop; Vault + sDGNRS EXEMPT keyed on the un-spoofable pinned ContractAddresses.VAULT/SDGNRS identity, NEVER a settable flag; renewal-lapse still cancels both."
    - "316-SPEC.md specifies the protocol-owned subs at init (SUB-09): sDGNRS self-subscribe claimable-only flat-1 + 2% reinvest + setCoinflipAutoRebuy(self,true,0); Vault self-subscribe claimable-only flat-1 no-reinvest; both free-renew via Whale pass."
    - "316-SPEC.md RESOLVES the whale-pass-expiry renewal funding for protocol subs (claimWhalePass no-ops post-expiry) as an explicit USER-RATIFIED decision recorded in the SPEC, OR escalates it as a flagged decision; and resolves OPEN-B (price-unavailable→0) and OPEN-C (CEI lean) with source citations."
  artifacts:
    - path: ".planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md"
      provides: "Open-item resolution sections (## Quantity & Funding Model, ## Protocol-Owned Subs (SUB-09), ## SPEC-Open Resolutions)"
      contains: "reinvestPct"
  key_links:
    - from: "316-SPEC.md two-tier skip-kill"
      to: "ContractAddresses.sol VAULT (:37) / SDGNRS (:47) pinned identity"
      via: "un-spoofable address-identity exemption (never a settable flag)"
      pattern: "SDGNRS"
    - from: "316-SPEC.md protocol-sub init (SUB-09)"
      to: "StakedDegenerusStonk.sol:360-361 + DegenerusVault constructor"
      via: "self-subscribe replaces setAfKingMode init"
      pattern: "SUB-09"
---

<objective>
Author the open-item-resolution sections of `316-SPEC.md`: the subscription quantity model, the funding waterfall, the two-tier skip-kill by pinned identity, the SUB-09 protocol-owned subs at init, and the SPEC-open resolutions (OPEN-B price-unavailable, OPEN-C reentrancy lean, "1 price lootbox" denomination, and the whale-pass-expiry renewal funding for protocol subs).

Purpose: These are the decisions that, if locked wrong, produce free-money or trivially-dodgeable cancellation downstream. The two-tier skip-kill MUST be by pinned-address identity (a settable flag = trivial cancellation dodge). The whale-pass-expiry renewal is a GENUINE user-OPEN (RESEARCH §5/Q1: `claimWhalePass` no-ops when `whalePassClaims==0`) that cannot be resolved from source alone — it must be closed with a user decision IN the SPEC, never defaulted as fact.

Output: The open-item-resolution sections of `316-SPEC.md`. Zero `contracts/` and zero `test/` mutations.

This plan is `autonomous: false` — it contains a `checkpoint:decision` for the whale-pass-expiry renewal funding (a user ratification the SPEC must record).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PLAN-CRANK-DO-WORK-INCENTIVE.md
@.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-RESEARCH.md

<interfaces>
<!-- Source facts grounding each open-item resolution. ALL grep-verified in 316-RESEARCH.md §5 against HEAD. Re-grep before writing (SC#5). -->

Quantity model + denomination (RESEARCH §5 "1 price lootbox", PLAN-CRANK §12.6):
- Keeper `TICKET_SCALE = 400` at keeper :387; `ticketQuantity = TICKET_SCALE * dailyQuantity`; lootbox mode `lootBoxAmt = cost`, `cost = (SUB_COST_ETH_TARGET * PRICE_COIN_UNIT) / mintPrice()`. So `dailyQuantity` is price-denominated: 1 unit = 1 mintPrice worth = one 1-price lootbox. `max(dailyQuantity, floor(claimable × reinvestPct / price))` is unit-consistent (both in price-units).
- Current keeper `subscribe(bool drainGameCreditFirst, uint8 dailyQuantity)` at :632 — NO reinvestPct yet (DRIFT vs intended; keeper rework adds it at Phase 317).
- Lootbox-floor skip path: `if (!useTickets && cost < LOOTBOX_MIN) → PlayerSkipped(player, 4)` (keeper :449/:289) — a TRANSIENT lootbox-floor skip, distinct from a funding skip.

Funding waterfall (RESEARCH §1.12, §5; PLAN-CRANK §12.6):
- `drainGameCreditFirst` 3-case at keeper 1076-1108: Claimable (`cred > cost`) / Combined (`1 < cred ≤ cost`) / DirectEth (`cred ≤ 1`); then `if (_poolOf[player] < msgValue)` skip at :1100. CEI holds: `_poolOf[player] -= msgValue` (:1104) → `purchase{value}` (:1110) → `lastSweptDay = today` (:1115).
- Claimable-only = sDGNRS/Vault never deposit to `_poolOf` → empty pool degrades to claimable-or-skip. No new flag.

Two-tier skip-kill identity (RESEARCH §5 "Two-tier skip-kill identity", SUB-06):
- Pinned constants in `ContractAddresses.sol`: `VAULT` :37, `SDGNRS` :47 already exist (un-spoofable). NO `AF_KING`/`STREAK_KEEPER` constant yet (PROTO-05 adds it). Exemption = keeper-side branch comparing the swept player to `ContractAddresses.VAULT`/`SDGNRS`. Existing swap-pop removal: `_removeFromSet` keeper :707/:1013.

Protocol-sub init (SUB-09) (RESEARCH §1.9, §5; PLAN-CRANK §12.6; PLAN-V47 §4):
- sStonk init: `game.claimWhalePass(address(0))` :360 then `game.setAfKingMode(address(0), true, 10 ether, 0)` :361 — the :361 call is REPLACED by self-subscribe (claimable-only, lootbox, dailyQuantity=1 + reinvestPct=2% + `setCoinflipAutoRebuy(self,true,0)`). sStonk also has public re-claim `gameClaimWhalePass()` :404.
- Vault: `gameClaimWhalePass()` :580 (owner-gated) → `claimWhalePass(address(this))` :582. Vault self-subscribes at init (claimable-only, dailyQuantity=1, reinvestPct=0, no BURNIE rebuy).

OPEN-B (RESEARCH §5): `_ethToBurnieValue` (MintModule:1412) already guards `if (amountWei==0 || priceWei==0) return 0`; `priceForLevel` (PriceLookupLib:21) never returns 0. Reward → 0, never revert.

OPEN-C (RESEARCH §5): game uses CEI throughout, no ReentrancyGuard; keeper loop already CEI (debit before call, day-stamp after). Lean CEI-proof; route the full-chain trace to `contract-auditor` at IMPL.

Whale-pass-expiry renewal (RESEARCH §5, §9 Q1 — GENUINE USER-OPEN):
- `claimWhalePass(player)` (WhaleModule:1004): if `whalePassClaims[player]==0` → early-return NO-OP; else clears the queue + `_applyWhalePassStats(player, level+1)` (advances FROZEN_UNTIL_LEVEL, capped currentLevel+7, WhaleModule 213-231/:428) + `_queueTicketRange(...100 levels...)`. Reverts on `_livenessTriggered()`.
- Problem: `claimWhalePass` only EXTENDS the freeze if `whalePassClaims>0`; it does NOT self-renew indefinitely. WHERE sDGNRS/Vault `whalePassClaims` accrue post-initial-expiry is NOT derivable from source. Options: (a) BURNIE-to-burn `burnForKeeper` fallback once the pass lapses; (b) a permanent Deity bit on the protocol addresses; (c) accept lapse. RESEARCH: flag for USER decision; do NOT default. Route the funding-model-closure check to `economic-analyst` at AUDIT.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the quantity model + funding waterfall + two-tier skip-kill section</name>
  <files>.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md</files>
  <read_first>
    - `316-RESEARCH.md` §5 ("1 price lootbox" denomination, funding waterfall, two-tier skip-kill identity), §1.12 (current keeper subscribe sig drift), §7 Don't-Hand-Roll (pinned-identity over settable flag).
    - `PLAN-CRANK-DO-WORK-INCENTIVE.md` §12.6 (OPEN-F COEXIST max-semantics RESOLVED, funding waterfall RESOLVED, two-tier skip-kill RESOLVED), §11 SUB-04..06.
    - `REQUIREMENTS.md` SUB-04, SUB-05, SUB-06.
    - Re-grep the quantity/funding/skip-kill anchors in `<interfaces>` against HEAD before writing.
  </read_first>
  <action>
    Append a `## Quantity & Funding Model` section to `316-SPEC.md`. Lock:
    (1) Quantity model (SUB-04): flat `dailyQuantity` (uint8, MINIMUM 1, flat=0 disallowed) COEXIST with optional `reinvestPct` (uint8); effective daily buy = `max(dailyQuantity, floor(claimable × reinvestPct / price))` — reinvest only triggers when its computed amount exceeds the flat schedule; both pack into one flags byte + `reinvestPct uint8` (NO new slot). Lock the denomination: `dailyQuantity` is price-denominated — 1 unit = 1 `mintPrice` worth = one "1-price lootbox" (cite `TICKET_SCALE=400`, `ticketQuantity = TICKET_SCALE × dailyQuantity` calibrated so 400·qty·price/400 == price·qty; lootbox mode `lootBoxAmt = cost`); the max-comparison is unit-consistent (both sides price-units). State that because flat ≥ 1 there is ALWAYS a ≥1-lootbox target, so a "skip" is unambiguously "can't afford the scheduled buy" (no reinvest-rounds-to-0 no-op case).
    (2) Funding waterfall (SUB-05): the EXISTING `drainGameCreditFirst=true` model — per day `cred = claimableWinningsOf(player)`, `cost` = scheduled spend → Claimable (`cred > cost`) / Combined (`1 < cred ≤ cost`) / DirectEth (`cred ≤ 1`) → `if (_poolOf[player] < msgValue)` InsufficientPool skip (cite keeper 1076-1108). Lock claimable-only = empty `_poolOf` (sDGNRS/Vault never deposit) → degrades to claimable-or-skip, NO new flag. Distinguish the transient lootbox-floor skip (`cost < LOOTBOX_MIN` → `PlayerSkipped(player,4)`) from a funding skip.
    (3) Two-tier skip-kill (SUB-06): NORMAL subs cancelled on a funding skip (`claimable+pool < cost`) via the existing in-sweep swap-pop/auto-pause removal (no `++i`; pool dust stays withdrawable; re-subscribe to resume). `Vault` + `sDGNRS` EXEMPT — funding skip = transient (buy nothing, retry, persist) because their empty-pool claimable-only design makes dry days normal. The exemption MUST key on the un-spoofable pinned `ContractAddresses.VAULT` (:37) + `SDGNRS` (:47) address constants — NEVER a player-settable flag (a settable flag = trivial cancellation dodge). Monthly pass-OR-pay renewal lapse still cancels BOTH. Record the audit notes: (a) griefable only at the funding margin — a pool-funded normal sub with balance ≥ cost is never skipped regardless of sweep timing; (b) a claimable-only NORMAL sub (no pool) is fragile by design (one dry day cancels it) → surface to players, consider a min-pool-at-subscribe; (c) the kill reuses the swap-pop path, distinct from the protocol-exempt transient skip.
  </action>
  <verify>
    <automated>F=.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md; grep -q '## Quantity & Funding Model' "$F" && grep -q 'reinvestPct' "$F" && grep -q 'TICKET_SCALE' "$F" && grep -q 'drainGameCreditFirst' "$F" && grep -q 'SDGNRS' "$F" && grep -qi 'settable flag' "$F"</automated>
  </verify>
  <acceptance_criteria>
    - `316-SPEC.md` contains a `## Quantity & Funding Model` section.
    - The section locks the COEXIST max-semantics model (`max(dailyQuantity≥1, floor(claimable×reinvestPct/price))`, one flags byte + `reinvestPct`, no new slot) and the price-unit denomination citing `TICKET_SCALE=400` (SUB-04).
    - The section locks the existing `drainGameCreditFirst=true` waterfall (Claimable/Combined/DirectEth → InsufficientPool skip, cite keeper 1076-1108) and claimable-only = empty `_poolOf` no new flag (SUB-05); distinguishes the transient lootbox-floor skip from a funding skip.
    - The section locks the two-tier skip-kill (SUB-06): NORMAL cancel via swap-pop, Vault/sDGNRS EXEMPT keyed on pinned `ContractAddresses.VAULT`/`SDGNRS` identity, explicitly NEVER a settable flag; renewal-lapse cancels both; the three audit notes recorded.
    - NO fenced solidity implementation bodies.
    - Zero `contracts/`/`test/` files modified by this plan.
  </acceptance_criteria>
  <done>`316-SPEC.md` has the `## Quantity & Funding Model` section locking the COEXIST quantity model, the existing funding waterfall, and the two-tier skip-kill by pinned-address identity.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: USER decision — whale-pass-expiry renewal funding for protocol subs (sDGNRS / Vault)</name>
  <files>.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md</files>
  <action>Pause and present the whale-pass-expiry renewal-funding decision below to the user. Do NOT default an answer — RESEARCH §5/Q1 establishes this is a genuine user-OPEN not derivable from source. Capture the user's selection + any caveats; Task 3 records it verbatim in the SPEC's `## SPEC-Open Resolutions` and `## Protocol-Owned Subs (SUB-09)` sections.</action>
  <decision>How do the protocol-owned subs (sDGNRS + Vault) keep renewing for free after their initial Whale pass expires?</decision>
  <context>
    RESEARCH §5/§9-Q1 (HIGH confidence on the mechanics, this is a GENUINE user-OPEN, not derivable from source): the protocol subs are designed to free-renew via their Whale pass (`claimWhalePass`). But `claimWhalePass(player)` (WhaleModule:1004) early-returns a NO-OP when `whalePassClaims[player] == 0` — it only EXTENDS the freeze when there are queued half-passes. It does NOT self-renew indefinitely. WHERE sDGNRS/Vault's `whalePassClaims` accrue after the initial pass expires is NOT determinable from contract source. If nothing accrues, the protocol subs LAPSE at whale-pass expiry — exactly the caveat PLAN-CRANK §12.6 flagged. This must be closed with a user decision recorded IN the SPEC; the planner must NOT default one as fact.
  </context>
  <options>
    <option id="burn-fallback">
      <name>BURNIE-to-burn fallback once the pass lapses</name>
      <pros>Reuses PROTO-02 `burnForKeeper` (already in the diff); the protocol subs degrade to the same pass-OR-PAY path as normal subs; no new mechanic.</pros>
      <cons>Requires the protocol addresses to hold/source burnable BURNIE post-expiry; `economic-analyst` must confirm the funding model closes (does the protocol address accrue enough BURNIE?).</cons>
    </option>
    <option id="permanent-deity">
      <name>Permanent Deity bit on the protocol addresses</name>
      <pros>`hasAnyLazyPass` returns true permanently (Deity = bit 184, never expires) → free renewal forever, zero per-renewal cost; simplest invariant.</pros>
      <cons>Requires setting the Deity bit on VAULT/SDGNRS at init (a contract change in the IMPL diff); grants those addresses the full Deity pass utility (trait/gold) as a side effect — confirm that is acceptable.</cons>
    </option>
    <option id="accept-lapse">
      <name>Accept lapse — protocol subs stop at whale-pass expiry</name>
      <pros>Zero added mechanism; matches "pre-launch, redeploy-fresh" posture if the protocol intends to re-init.</pros>
      <cons>The protocol-owned auto-rebuy quietly stops working after N levels; likely NOT the intent (SUB-09 says "free-renew via Whale pass").</cons>
    </option>
  </options>
  <verify>
    <automated>echo "checkpoint:decision — no automated verification; resolution recorded by Task 3"</automated>
  </verify>
  <done>The user has selected one of {burn-fallback, permanent-deity, accept-lapse} (with any caveats); the selection is available for Task 3 to record verbatim in the SPEC.</done>
  <resume-signal>Select: burn-fallback, permanent-deity, or accept-lapse (with any caveats to record in the SPEC).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Author the SUB-09 protocol-sub init section + the SPEC-open resolutions (record the whale-expiry decision)</name>
  <files>.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md</files>
  <read_first>
    - `316-RESEARCH.md` §5 (protocol-sub init configs, whale-pass-expiry mechanics, OPEN-B, OPEN-C, denomination), §1.9 (sStonk/Vault init + re-claim sites), §6 Component Responsibility Map (protocol-sub init owner), §9 Q1/Q2/Q3 (whale-expiry, CEI, JackpotEthWin ABI).
    - `PLAN-CRANK-DO-WORK-INCENTIVE.md` §12.6 (SUB-09 protocol-sub configs, pass-expiry caveat, quantity unit), §12 OPEN-B/OPEN-C.
    - `PLAN-V47-REMOVE-AFKING-ETH-AUTOREBUY.md` §4 (sStonk init successor RESOLVED).
    - `REQUIREMENTS.md` SUB-09.
    - The Task-2 user decision (the resume-signal selection) — record it verbatim in the SPEC.
    - Re-grep the SUB-09 init anchors + OPEN-B/OPEN-C anchors against HEAD before writing.
  </read_first>
  <action>
    Append a `## Protocol-Owned Subs (SUB-09)` section and a `## SPEC-Open Resolutions` section to `316-SPEC.md`. Lock:
    (1) SUB-09 protocol-sub init: `StakedDegenerusStonk` — replace the `game.setAfKingMode(address(0), true, 10 ether, 0)` init (sStonk:361) with a keeper self-subscribe (claimable-only funding, lootbox mode, `dailyQuantity=1`, `reinvestPct=2%`) PLUS `setCoinflipAutoRebuy(self, true, 0)` (BURNIE flip auto-rebuy, takeProfit=0 → full recycle, the v47-kept path at flat 75bps); cite the existing `game.claimWhalePass(address(0))` at :360 preceding it + the re-claim `gameClaimWhalePass()` at :404. `DegenerusVault` — self-subscribe at init (claimable-only, `dailyQuantity=1`, `reinvestPct=0`, no BURNIE rebuy); cite the Vault `gameClaimWhalePass()` :580 → `claimWhalePass(address(this))` :582. Both self-subscribe via `player == msg.sender` (no operator check).
    (2) `## SPEC-Open Resolutions`: lock OPEN-B = reward→0-never-revert by reusing the `_ethToBurnieValue` zero-guard (cite MintModule:1412 `if (amountWei==0||priceWei==0) return 0`) and the non-zero `priceForLevel` invariant (PriceLookupLib:21); lock OPEN-C = CEI-proof lean (game uses CEI throughout, no ReentrancyGuard; keeper loop already CEI debit-before-call) WITH the IMPL/AUDIT trace requirement routed to `contract-auditor`; lock the "1 price lootbox" denomination (`TICKET_SCALE=400`, point to the Task-1 quantity lock); record the `JackpotEthWin` ABI-break note (off-chain indexer is a separate frontend track, out of scope).
    (3) Record the whale-pass-expiry renewal funding decision from Task 2 VERBATIM (the option the user selected + any caveats), framed as the SUB-09 free-renew mechanism for the protocol subs. State the funding-model-closure check is routed to `economic-analyst` at AUDIT (name the skill, do NOT run it here). If the user selected `accept-lapse`, state the lapse behavior explicitly so it is a recorded, ratified decision — not a silent gap.
  </action>
  <verify>
    <automated>F=.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-SPEC.md; grep -q '## Protocol-Owned Subs (SUB-09)' "$F" && grep -q '## SPEC-Open Resolutions' "$F" && grep -q 'setCoinflipAutoRebuy' "$F" && grep -q 'claimWhalePass' "$F" && grep -qi 'whale-pass' "$F" && grep -q '_ethToBurnieValue' "$F"</automated>
  </verify>
  <acceptance_criteria>
    - `316-SPEC.md` contains a `## Protocol-Owned Subs (SUB-09)` section and a `## SPEC-Open Resolutions` section.
    - The SUB-09 section specifies sDGNRS self-subscribe (claimable-only, lootbox, flat-1 + 2% reinvest + `setCoinflipAutoRebuy(self,true,0)`) replacing the sStonk:361 `setAfKingMode` init, and Vault self-subscribe (claimable-only, flat-1, no reinvest, no BURNIE rebuy); both cite their `claimWhalePass` sites.
    - The SPEC-Open Resolutions section locks OPEN-B (reward→0 via `_ethToBurnieValue` guard + non-zero `priceForLevel`), OPEN-C (CEI lean + `contract-auditor` trace at IMPL), the "1 price lootbox" denomination (`TICKET_SCALE=400`), and the `JackpotEthWin` ABI-break note.
    - The whale-pass-expiry renewal funding decision from Task 2 is recorded verbatim (selected option + caveats), with the funding-model-closure check routed to `economic-analyst` at AUDIT; if `accept-lapse`, the lapse behavior is stated as a ratified decision.
    - NO fenced solidity implementation bodies.
    - Zero `contracts/`/`test/` files modified by this plan.
  </acceptance_criteria>
  <done>`316-SPEC.md` has the `## Protocol-Owned Subs (SUB-09)` section and the `## SPEC-Open Resolutions` section with OPEN-B/OPEN-C/denomination/JackpotEthWin resolved and the user-ratified whale-pass-expiry renewal funding recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (cost of a wrong open-item lock)

| Boundary | Description |
|----------|-------------|
| skip-kill design → cancellation | If exemption is a settable flag instead of pinned identity, any NORMAL sub spoofs the Vault/sDGNRS exemption → trivial cancellation dodge. |
| protocol-sub funding → liveness | If whale-pass-expiry renewal is under-specified/defaulted, the protocol subs silently lapse → SUB-09 broken at IMPL. |
| OPEN-B price edge → settlement | An unguarded price read could revert the whole crank settlement on a bad price → faucet/non-brick failure. |

## STRIDE Threat Register (cost of a wrong design lock)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-316-09 | Spoofing | A SPEC that exempts Vault/sDGNRS via a settable flag | mitigate | Task 1 locks the exemption on the un-spoofable pinned `ContractAddresses.VAULT`/`SDGNRS` identity, explicitly never a settable flag. |
| T-316-10 | Denial of Service | A SPEC that under-specifies whale-pass-expiry renewal | mitigate | Task 2 is a blocking user-decision checkpoint; Task 3 records the ratified decision verbatim + routes closure to `economic-analyst`. |
| T-316-11 | Tampering | A SPEC that locks an unguarded price read (OPEN-B) | mitigate | Task 3 locks reward→0 via the `_ethToBurnieValue` zero-guard + non-zero `priceForLevel` invariant. |
| T-316-12 | Elevation | A SPEC that grants protocol addresses unintended Deity utility (if permanent-deity chosen) | accept-with-condition | Task 2 surfaces the side-effect in the option cons; the user ratifies with eyes open; recorded in the SPEC. |
| T-316-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero packages installed (read-only doc authoring). |
</threat_model>

<verification>
- `316-SPEC.md` contains `## Quantity & Funding Model`, `## Protocol-Owned Subs (SUB-09)`, and `## SPEC-Open Resolutions` section headers.
- The two-tier skip-kill is locked on pinned-address identity (grep for "settable flag" confirms the prohibition is stated).
- The whale-pass-expiry decision is recorded as a user-ratified choice (not defaulted).
- No `.sol`/`.t.sol` file appears in this plan's git diff.
</verification>

<success_criteria>
- ROADMAP SC#2 (quantity + funding model) and SC#3 (protocol-owned subs at init + whale-expiry + denomination) are satisfied.
- SUB-09 design (protocol-owned subs at init) is locked with the whale-pass-expiry renewal funding ratified.
</success_criteria>

<output>
Create `.planning/phases/316-spec-crank-subscription-legacy-removal-design-lock-spec/316-03-SUMMARY.md` when done.
</output>
</output>
