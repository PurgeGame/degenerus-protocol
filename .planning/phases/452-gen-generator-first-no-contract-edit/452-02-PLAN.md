---
phase: 452-gen-generator-first-no-contract-edit
plan: 02
type: execute
wave: 2
depends_on: ["452-01"]
files_modified:
  - .planning/notes/degenerette-recalibration/derive_5_tables.py
autonomous: true
requirements: [GEN-02, GEN-03, EVEQ-01]

must_haves:
  truths:
    - "The full constant family regenerates from the rewritten distributions: honest QUICK_PLAY_PAYOUTS_N{0..4}_PACKED/_S8, WWXRP_FACTORS_N{0..4}, and the full _RIG_ family (QUICK_PLAY_PAYOUTS_RIG_N{0..4}_PACKED/_S8, WWXRP_FACTORS_RIG_N{0..4})"
    - "The S9_PIN pins and the WWXRP_ROI_* / RTP curve pass through UNTOUCHED — confirmed, not recomputed"
    - "Per-N self-asserts pass for honest AND rigged: basePayoutEV in (99,100] centi-x; ETH/WWXRP bonus-EV = 5.000%; all WWXRP factors < 2^64; rigged P(S=9) == honest P(S=9)"
    - "EVEQ-01 / DEC-02 Option A: the generator measures and reports the worst-case EV drift between the hero-gold and hero-common sub-cases per N (P(hero gold | N) = N/4 averaging documented); Option A kept (5 per-N tables) unless drift is grossly outside the ~0.5 centi-x tolerance"
    - "Numeric pre-proof: the generator confirms, before any contract edit, that P(S=9) and the WWXRP RTP curve (70->115->118->120%) are unchanged vs HEAD under Variant-2 + the R2 rig"
    - "stdout contains the FINAL PASTE-READY CONSTANTS block (the 453 IMPL paste source + the 454 TST byte-reproduce diff target)"
  artifacts:
    - path: ".planning/notes/degenerette-recalibration/derive_5_tables.py"
      provides: "Regenerated Variant-2 constant family + EV-drift measurement + P(S=9)/RTP pre-proof"
      contains: "FINAL PASTE-READY CONSTANTS"
  key_links:
    - from: "regenerated constant blocks"
      to: "p_score_distribution / p_score_distribution_rigged (Variant-2, Plan 01)"
      via: "_solve_table / SHAPE / S9_PIN / wwxrp_factors fed by the two rewritten distributions"
      pattern: "_solve_table"
    - from: "EV-drift measurement (EVEQ-01)"
      to: "Option A 5 per-N tables"
      via: "worst-case hero-gold vs hero-common sub-case EV per N, averaged at P(hero gold|N)=N/4"
      pattern: "drift"
    - from: "numeric pre-proof"
      to: "HEAD P(S=9) + WWXRP_ROI_* RTP curve"
      via: "generator confirms unchanged under Variant-2 + R2 before any .sol edit"
      pattern: "P\\(S=9\\)"
---

<objective>
Regenerate the full constant family from the Variant-2 distributions rewritten in Plan 01, add the
EVEQ-01 / DEC-02 Option-A EV-drift measurement (hero-gold vs hero-common per N), and add the
numeric pre-proof that P(S=9) and the WWXRP RTP curve are unchanged vs HEAD under Variant-2 + the
R2 rig — all printed before any contract edit. This is the deliverable phase 453 IMPL pastes and
phase 454 TST re-derives.

Purpose: GEN-03 (regenerate the constant family), GEN-02 (the self-asserts that make the regen
trustworthy), and EVEQ-01 (measure Option-A drift, resolve DEC-02 on measurement) all land here.
The `S9_PIN` pins and the `WWXRP_ROI_*` curve are NOT recomputed — the generator confirms they
pass through untouched. The numeric pre-proof (ROADMAP success criterion 5) front-loads the
"nothing that matters about WWXRP / the jackpot moved" guarantee before the gated `.sol` diff.

Output: A `derive_5_tables.py` that, on `python3` run, exits 0 with all honest + rigged
self-asserts passing, prints the FINAL PASTE-READY CONSTANTS block (honest + `_RIG_` families),
prints a per-N hero-placement EV-drift report (with the DEC-02 Option-A verdict), and prints a
P(S=9) + WWXRP RTP-curve "unchanged vs HEAD" pre-proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/452-gen-generator-first-no-contract-edit/452-CONTEXT.md
@.planning/phases/452-gen-generator-first-no-contract-edit/452-01-SUMMARY.md

<interfaces>
<!-- The machinery this plan drives + the asserts it must keep + the new outputs it adds. -->
<!-- Read derive_5_tables.py for exact bodies; these are the contracts. -->

Reused unchanged (currency-agnostic; fed by the Plan-01 distributions):
- `_solve_table(pS, N)` (~L404-419): SHAPE + S9_PIN solve with residual absorption (S=4/5/6) and
  the neutral-or-just-under nudge (`while total_ev > 100: payouts[6] -= 1`).
- `SHAPE` (~L163), `S9_PIN` (~L127), `wwxrp_factors` / `wwxrp_factors_rig`, `WWXRP_SCALE`,
  `SPLITS`, `ETH_BONUS_BPS`.

Existing self-asserts that MUST keep passing (GEN-02):
- honest: per-N `assert ev_frac <= 100` and `> 99` (basePayoutEV in (99,100] centi-x);
  `assert abs(bonus - 0.05) < 0.05*0.01` (5.000%); `assert factors[B] < (1<<64)`.
- rigged: the same three asserts in the rigged-verification block; `assert rig[9] == P_N_TABLE[N][9]`.

Constant blocks regenerated (GEN-03) — names the 453 IMPL diff pastes verbatim:
- honest: QUICK_PLAY_PAYOUTS_N{0..4}_PACKED, QUICK_PLAY_PAYOUT_N{0..4}_S8, WWXRP_FACTORS_N{0..4}_PACKED.
- rig: QUICK_PLAY_PAYOUTS_RIG_N{0..4}_PACKED, QUICK_PLAY_PAYOUT_RIG_N{0..4}_S8, WWXRP_FACTORS_RIG_N{0..4}_PACKED.
- UNTOUCHED (printed but never recomputed): QUICK_PLAY_PAYOUT_N{0..4}_S9 (the S9_PIN), the WWXRP_ROI_* curve.

EVEQ-01 / DEC-02 (LOCKED Option A): under Variant-2 the hero quadrant's gold-ness shifts P_N(S),
so within a fixed N a hero-gold ticket vs a hero-common ticket have slightly different score
distributions. The single per-N honest table averages over hero placement at P(hero gold | N) =
N/4 (N of the 4 quadrants are gold; the hero is equally likely any quadrant). The generator must
MEASURE the worst-case EV drift between the two sub-cases per N. Keep Option A (5 tables); only
flag Option-B escalation if drift is grossly outside ~0.5 centi-x (NOT expected; WWXRP gold-payout
drift is an explicit don't-care per USER). Every per-N table still asserts EV <= 100 -> Option A
is solvency-safe regardless.

Contract surface (read-only): `_getBasePayoutBps` ~L1175, `_wwxrpRoi` ~L1259, `_roiBpsFromScore`
~L1225 — the dispatch + ROI/RTP curves held fixed (Option A keeps the dispatch shape; INV-01/02).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the full constant family + confirm S9 pins / WWXRP_ROI_* untouched (GEN-03 + GEN-02)</name>
  <files>.planning/notes/degenerette-recalibration/derive_5_tables.py</files>
  <read_first>
    - .planning/notes/degenerette-recalibration/derive_5_tables.py (FULL — the regen drives the existing FINAL PASTE-READY CONSTANTS print blocks off the Plan-01 distributions; confirm `_solve_table`, `wwxrp_factors`, `wwxrp_factors_rig`, the packing loops, and every self-assert are intact)
    - .planning/phases/452-gen-generator-first-no-contract-edit/452-01-SUMMARY.md (what Plan 01 changed in the two distributions)
    - .planning/REQUIREMENTS.md (GEN-02, GEN-03 — the self-asserts + the regenerated constant set; S9 pins + WWXRP_ROI_* untouched)
    - contracts/modules/DegenerusGameDegeneretteModule.sol `_getBasePayoutBps` ~L1175 + constant blocks ~L283-360 (read-only: the constant names + dispatch the regen must match)
  </read_first>
  <action>
    With the Variant-2 distributions from Plan 01 in place, ensure the generator regenerates the
    FULL constant family off them and that every self-assert still holds. Confirm the honest solve
    path (`tables = [_solve_table(P_N_TABLE[N], N) ...]` equivalent) and the rigged solve path
    (`tables_rig = [_solve_table(P_N_RIG[N], N) ...]`) both feed `_solve_table` / `wwxrp_factors`
    unchanged, so the FINAL PASTE-READY CONSTANTS block emits: honest QUICK_PLAY_PAYOUTS_N{0..4}_PACKED,
    QUICK_PLAY_PAYOUT_N{0..4}_S8, WWXRP_FACTORS_N{0..4}_PACKED; and the _RIG_ family
    QUICK_PLAY_PAYOUTS_RIG_N{0..4}_PACKED, QUICK_PLAY_PAYOUT_RIG_N{0..4}_S8,
    WWXRP_FACTORS_RIG_N{0..4}_PACKED. Keep the GEN-02 self-asserts EXACTLY: per-N honest+rigged
    `basePayoutEV in (99,100]` (`assert ev_frac <= 100` and `> 99`); ETH/WWXRP bonus-EV = 5.000%
    (`assert abs(bonus - 0.05) < 0.05*0.01`); all WWXRP factors < 2^64; rigged P(S=9) == honest
    P(S=9). Confirm `S9_PIN` and the per-N `QUICK_PLAY_PAYOUT_N{N}_S9` lines are printed but NEVER
    recomputed under Variant-2 (they remain the relabel pins; the S9-reproduction loop from Plan 01
    Task 1 is the guard). Confirm the WWXRP_ROI_* / RTP-curve constants are not regenerated by this
    script (they live in the contract's ROI machinery, held fixed INV-02) — if the script prints
    any RTP/ROI reference, it must pass through HEAD values, not recompute them. Refresh the
    module-level docstring + the FINAL-block comments to describe Variant-2 (the doc-comment refresh
    deferred from Plan 01). Do NOT change SHAPE, S9_PIN, the split ratios, ETH_BONUS_BPS, or the
    packing/bit-layout — only the distributions feeding the solve changed (Plan 01).
  </action>
  <verify>
    <automated>python3 .planning/notes/degenerette-recalibration/derive_5_tables.py >/tmp/v452_p2t1.txt 2>&1; echo "exit=$?"; grep -c "FINAL PASTE-READY CONSTANTS" /tmp/v452_p2t1.txt; echo "--- honest packed ---"; grep -c "QUICK_PLAY_PAYOUTS_N[0-4]_PACKED" /tmp/v452_p2t1.txt; echo "--- rig packed ---"; grep -c "QUICK_PLAY_PAYOUTS_RIG_N[0-4]_PACKED" /tmp/v452_p2t1.txt; echo "--- wwxrp factors ---"; grep -c "WWXRP_FACTORS_N[0-4]_PACKED\|WWXRP_FACTORS_RIG_N[0-4]_PACKED" /tmp/v452_p2t1.txt; echo "--- s9 pins ---"; grep -c "QUICK_PLAY_PAYOUT_N[0-4]_S9" /tmp/v452_p2t1.txt</automated>
  </verify>
  <acceptance_criteria>
    - `python3 .planning/notes/degenerette-recalibration/derive_5_tables.py` exits 0 (all honest + rigged GEN-02 self-asserts pass).
    - stdout contains "FINAL PASTE-READY CONSTANTS" at least twice (honest block + WWXRP RIG FAMILY block) — `grep -c` returns >= 2.
    - All 5 honest `QUICK_PLAY_PAYOUTS_N{0..4}_PACKED` AND all 5 `QUICK_PLAY_PAYOUTS_RIG_N{0..4}_PACKED` lines are emitted (grep counts return 5 each).
    - All 10 WWXRP factor packed lines emitted (5 honest `WWXRP_FACTORS_N{0..4}_PACKED` + 5 `WWXRP_FACTORS_RIG_N{0..4}_PACKED`).
    - All 5 `QUICK_PLAY_PAYOUT_N{0..4}_S9` pin lines emitted and equal the HEAD S9_PIN values [10_756_411, 12_583_037, 14_792_939, 17_512_324, 20_916_435] (the S9-reproduction assert from Plan 01 passed; the pins were not recomputed).
    - Every honest + rigged per-N EV line reports a value in (99,100] centi-x and bonus EV = 5.00000% (the `assert ev_frac <= 100`, `> 99`, and bonus-EV asserts all passed without traceback).
  </acceptance_criteria>
  <done>
    The generator regenerates the full Variant-2 constant family (honest + _RIG_) off the Plan-01
    distributions, all GEN-02 self-asserts pass, the S9 pins + WWXRP_ROI_* pass through untouched,
    and the FINAL PASTE-READY CONSTANTS block prints the complete paste source for 453 IMPL.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add EVEQ-01 Option-A EV-drift measurement + P(S=9)/WWXRP-RTP numeric pre-proof</name>
  <files>.planning/notes/degenerette-recalibration/derive_5_tables.py</files>
  <read_first>
    - .planning/notes/degenerette-recalibration/derive_5_tables.py (FULL — add new measurement/pre-proof print sections; reuse `p_score_distribution`, `total_ev`, `tables`, `P_N_TABLE`, `P_N_RIG`, `S9_PIN`, `wwxrp_factors`)
    - .planning/phases/452-gen-generator-first-no-contract-edit/452-CONTEXT.md (DEC-02 Option A — LOCKED: 5 per-N tables, measure drift, escalate to B only if grossly out of ~0.5 centi-x; WWXRP gold drift is don't-care)
    - .planning/REQUIREMENTS.md (EVEQ-01; INV-02/INV-03 — the WWXRP RTP curve + P(S=9) numeric proof; INV-04 S=9 whale-pass bracket)
    - contracts/modules/DegenerusGameDegeneretteModule.sol `_wwxrpRoi` ~L1259 + `_roiBpsFromScore` ~L1225 (read-only: the WWXRP RTP curve 70->115->118->120% held fixed)
  </read_first>
  <action>
    Add two new print/verify sections to the generator (after the FINAL constant blocks; do not
    disturb the existing asserts).
    (1) EVEQ-01 Option-A EV-drift measurement: for each N, build the hero-gold sub-case
    distribution (the hero quadrant's color is gold) and the hero-common sub-case distribution
    (the hero quadrant's color is common) under Variant-2, using the same per-quadrant convolution
    as `p_score_distribution`; the single per-N table averages them at P(hero gold | N) = N/4.
    Compute `basePayoutEV` of the SAME solved per-N honest table against each sub-case distribution,
    and report the worst-case |EV_gold - EV_common| drift per N (in centi-x) plus the max across N.
    Print the DEC-02 verdict line: "Option A kept" if max drift is within ~0.5 centi-x of neutral
    (expected), else "ESCALATE to Option B" (grossly outside). Assert max drift is finite and the
    per-N tables remain EV <= 100 (solvency-safe regardless of drift — the existing EV-<=100 assert
    already covers this; reference it). Do NOT add per-(N, hero-is-gold) tables — Option A is locked.
    (2) Numeric pre-proof (ROADMAP success criterion 5 / INV-03): print an explicit line proving
    P(S=9) is unchanged vs HEAD under Variant-2 + the R2 rig — show honest P_N(S=9), rigged
    P_N(S=9), and the HEAD all-8-match probability side by side per N, and assert all three are
    EQUAL (Fraction-exact) for every N. Also confirm the WWXRP RTP curve is unchanged: state that
    `WWXRP_ROI_*` (70->115->118->120%) and the S=9 whale-pass bracket are NOT recomputed by this
    script (held fixed in the contract ROI machinery, INV-02/INV-04) and that the rig leaves P(S=9)
    and its pinned payout intact, so the realized WWXRP RTP curve is byte-identical to HEAD. Print a
    clear marker line (e.g. "PRE-PROOF: P(S=9) and WWXRP RTP curve unchanged vs HEAD under Variant-2
    + R2 rig") so the executor and the 454 TST gate can grep for it.
  </action>
  <verify>
    <automated>python3 .planning/notes/degenerette-recalibration/derive_5_tables.py >/tmp/v452_p2t2.txt 2>&1; echo "exit=$?"; echo "--- drift report ---"; grep -ci "drift\|Option A\|hero-gold\|hero placement" /tmp/v452_p2t2.txt; echo "--- preproof marker ---"; grep -c "PRE-PROOF" /tmp/v452_p2t2.txt; echo "--- ps9 line ---"; grep -ci "P(S=9)" /tmp/v452_p2t2.txt</automated>
  </verify>
  <acceptance_criteria>
    - `python3 .planning/notes/degenerette-recalibration/derive_5_tables.py` exits 0 (the new drift + pre-proof asserts pass alongside all existing self-asserts).
    - stdout contains a per-N EV-drift report measuring hero-gold vs hero-common sub-case EV, plus a max-across-N line and a DEC-02 verdict (`grep -i "drift"` and `grep -i "Option A"` both match).
    - The measured max hero-placement EV drift is reported as a number in centi-x; the verdict line states "Option A kept" (drift within ~0.5 centi-x — expected) OR explicitly flags "ESCALATE to Option B" if grossly outside.
    - stdout contains the marker "PRE-PROOF: P(S=9) and WWXRP RTP curve unchanged vs HEAD under Variant-2 + R2 rig" (or equivalent grep-able marker) — `grep -c "PRE-PROOF"` returns >= 1.
    - The pre-proof asserts honest P_N(S=9) == rigged P_N(S=9) == HEAD all-8-match probability (Fraction-exact) for all 5 N — no assertion traceback.
  </acceptance_criteria>
  <done>
    The generator measures and reports the Option-A hero-placement EV drift per N (with the DEC-02
    verdict), and prints + asserts the numeric pre-proof that P(S=9) and the WWXRP RTP curve are
    unchanged vs HEAD under Variant-2 + the R2 rig — all before any contract edit. The script exits
    0 with every assert (existing + new) passing.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — offline generator) | This plan edits ONLY the offline Python generator `derive_5_tables.py`. No runtime, no `.sol`, no contract surface; no untrusted input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-452-02 | Tampering | The regenerated constant family / drift report / pre-proof silently encode wrong values | mitigate | Every GEN-02 self-assert (per-N EV in (99,100] honest+rigged, bonus-EV 5.000%, factors<2^64, rigged P(S=9) == honest P(S=9)) plus the new pre-proof assert (honest == rigged == HEAD P(S=9)) runs on every invocation; the 454 TST byte-reproduce gate re-derives the FINAL block and diffs against the committed `.sol`; 455 REAUDIT re-attests. A wrong constant either trips an assert (exit != 0) or is caught by the TST diff before any contract ships. |

Contract-level threats (solvency, RNG-freeze, brick, the actual `_score`/`_rigWwxrpResult` runtime
behavior) are OUT OF SCOPE for Phase 452 — no contract is touched here; they are deferred to
Phases 453 (IMPL, the sole approval gate) / 455 (REAUDIT). No contract threats are invented for a
phase that produces no runtime surface.
</threat_model>

<verification>
- `python3 .planning/notes/degenerette-recalibration/derive_5_tables.py` exits 0 with the full
  Variant-2 regen + the new drift report + the pre-proof, all asserts passing.
- The FINAL PASTE-READY CONSTANTS block emits the complete honest + _RIG_ constant family
  (10 packed payout lines + 10 S8 lines + 10 WWXRP factor packed lines + 5 S9 pins).
- A per-N EV-drift report + DEC-02 Option-A verdict is printed.
- A grep-able "PRE-PROOF" marker confirms P(S=9) + WWXRP RTP curve unchanged vs HEAD.
- `git diff --stat` shows only `derive_5_tables.py` modified.
</verification>

<success_criteria>
- GEN-03: the full constant family (honest + _RIG_) regenerated from the Variant-2 distributions;
  S9 pins + WWXRP_ROI_* confirmed untouched.
- GEN-02: per-N basePayoutEV in (99,100] centi-x (honest AND rigged); bonus-EV = 5.000%; all WWXRP
  factors < 2^64; rigged P(S=9) == honest P(S=9) — all asserted, exit 0.
- EVEQ-01 / DEC-02: worst-case hero-placement EV drift measured + reported per N; Option A kept
  (5 per-N tables) unless grossly out of tolerance.
- Numeric pre-proof: P(S=9) and the WWXRP RTP curve confirmed unchanged vs HEAD under Variant-2 +
  the R2 rig, before any contract edit.
</success_criteria>

<output>
Create `.planning/phases/452-gen-generator-first-no-contract-edit/452-02-SUMMARY.md` when done.
</output>
