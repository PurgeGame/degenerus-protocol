---
phase: 452-gen-generator-first-no-contract-edit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/notes/degenerette-recalibration/derive_5_tables.py
autonomous: true
requirements: [GEN-01]

must_haves:
  truths:
    - "p_score_distribution implements Variant-2 (per-quadrant symbol +1 / hero +2; color +1 only if that quadrant's symbol matched), S in {0..9}, max 9 = hero-quad 3 + three ordinary quads x2"
    - "DEC-03 floor S>=2 holds under Variant-2: a lone ordinary (non-hero) symbol = S=1 (SHAPE pays 0); hero symbol alone = S=2 and a full color+symbol double = S=2 (both pay). The honest distribution produces the S=1 / S=2 mass consistent with the unchanged SHAPE[0]=SHAPE[1]=0 pay floor."
    - "Each per-N honest Variant-2 distribution sums to exactly 1 (Fraction-exact)"
    - "S=9 stays exactly the all-8-axes event: honest P_N(S=9) is byte-identical to HEAD's all-8-match probability, so the S9_PIN relabel asserts still hold"
    - "p_score_distribution_rigged implements DEC-01 R2: the rig forces only score-bearing cells (unmatched non-hero symbol always +1; unmatched color on a quadrant whose symbol already matched +1; hero symbol and no-op colors excluded), with an explicit empty-eligible-pool case (M<=6 but only excluded cells remain -> no lift)"
    - "The m>=7 cap is preserved in the rigged distribution so rig[9] == honest[9] (P(S=9) invariant); the existing assert rig[9] == P_N_TABLE[N][9] still passes for all N"
    - "The script still runs to exit 0 with the existing assert harness after both distributions are rewritten"
  artifacts:
    - path: ".planning/notes/degenerette-recalibration/derive_5_tables.py"
      provides: "Variant-2 honest p_score_distribution + R2 score-bearing p_score_distribution_rigged"
      contains: "def p_score_distribution"
  key_links:
    - from: "p_score_distribution_rigged"
      to: "p_score_distribution"
      via: "rigged eligible-cell pool defined relative to the honest Variant-2 color-gated-by-symbol rule"
      pattern: "p_score_distribution"
    - from: "p_score_distribution_rigged (Variant-2 R2)"
      to: "rig[9] == P_N_TABLE[N][9]"
      via: "m>=7 cap preserving P(S=9)"
      pattern: "rig\\[9\\] == P_N_TABLE"
---

<objective>
Rewrite the two probability-distribution functions in `derive_5_tables.py` so the generator
models Variant-2 (color-gated-by-symbol) scoring instead of today's independent `S = A + 2*H`,
and so the WWXRP rig (`p_score_distribution_rigged`) forces only **score-bearing** cells per
DEC-01 R2. This is the core logic change of Phase 452 — everything else (constant regen, EV-drift
measurement, pre-proof) consumes these two distributions and lands in Plan 02.

Purpose: The two distributions are the only place the scoring rule lives in the generator; the
currency-agnostic `_solve_table` / `SHAPE` / `S9_PIN` machinery below them is reused unchanged.
Getting the distributions right (and keeping P(S=9) byte-identical) is what makes the whole regen
trustworthy before any `.sol` is touched.

Output: A `derive_5_tables.py` whose `p_score_distribution` and `p_score_distribution_rigged`
encode Variant-2 + R2, that still runs to exit 0 under the existing assert harness (per-N EV
asserts, `rig[9] == honest[9]`, bonus-EV, factors<2^64). Constant regen + new pre-proof printing
is Plan 02's job — do NOT add new output sections here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/452-gen-generator-first-no-contract-edit/452-CONTEXT.md

<interfaces>
<!-- Current shape of the two functions being rewritten + the contract rule they mirror. -->
<!-- Read derive_5_tables.py for the exact current bodies; these are the contracts to preserve. -->

The honest distribution today (derive_5_tables.py ~L88-106) — `p_score_distribution(n_gold)`:
- Convolves N gold color axes (Bernoulli 1/15) + (4-N) common color axes (Bernoulli 2/15)
  + 3 non-hero symbol axes (P_SYM, +0/+1) + 1 hero symbol axis (P_HERO, +0/+2), independently.
- Returns a length-10 list summing to 1. THIS independence is what Variant-2 replaces.

The rigged distribution today (derive_5_tables.py ~L365-393) — `p_score_distribution_rigged(n_gold)`:
- "variant-B": enumerates (color-count c, non-hero-symbol-count y, hero h); base S = c+y+2h;
  if M=c+y+h <= 6, flips one unmatched ORDINARY cell w.p. 3/5 (+1); M>=7 untouched.
- THIS pool (any unmatched ordinary cell) is what DEC-01 R2 narrows to score-bearing cells.

The contract rule being mirrored (DegenerusGameDegeneretteModule.sol):
- `_score` ~L1053-1080 (HEAD, independent rule): per quadrant, color match +1; symbol match
  +1 (hero +2). Variant-2 will gate the color +1 behind that quadrant's symbol match (453 IMPL).
- `_rigWwxrpResult` ~L1299-1358 (HEAD): pass-1 builds `u` = unmatched-ordinary-cell count
  (color any quadrant + non-hero symbol). R2 narrows this pool to score-bearing cells.
- Foil precedent (already shipped + audited, commit 16225de6): the same color-gated-by-symbol
  rule on the foil match — the proven shape being ported.

Invariants the assert harness already enforces (must keep passing):
- `assert sum(dist) == 1` per honest N; `assert sum(out) == 1` per rigged N.
- `assert rig[9] == P_N_TABLE[N][9]` (P(S=9) invariant under the rig).
- `S9_PIN` reproduction loop (~L147-155) asserts honest P_N(S=9) equals the old all-8-match event.
- DEC-03 pay floor S>=2 is enforced by SHAPE[0]=SHAPE[1]=0 (unchanged) — Variant-2 must produce
  the correct S=1 (lone ordinary symbol, pays 0) and S=2 (hero alone, or a full quadrant double)
  mass so the floor behaves as DEC-03 specifies.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite p_score_distribution for Variant-2 (color-gated-by-symbol)</name>
  <files>.planning/notes/degenerette-recalibration/derive_5_tables.py</files>
  <read_first>
    - .planning/notes/degenerette-recalibration/derive_5_tables.py (FULL — edit the current body, not assumptions; note `convolve`, `P_COLOR_GOLD`/`P_COLOR_COMMON`/`P_SYM`/`P_HERO`, and the `P_N_TABLE`/`S9_PIN` reproduction loop that consume `p_score_distribution`'s output)
    - .planning/phases/452-gen-generator-first-no-contract-edit/452-CONTEXT.md (<domain> Variant-2 rule + DEC-03 floor S>=2 — LOCKED)
    - .planning/REQUIREMENTS.md (SCORE-01, SCORE-03 — the rule + the S=9 pin invariant; DEC-03 floor)
    - contracts/modules/DegenerusGameDegeneretteModule.sol `_score` ~L1053-1080 (read-only reference for the per-quadrant rule shape)
  </read_first>
  <action>
    Replace `p_score_distribution(n_gold)` so it models Variant-2 instead of the independent
    `S = A + 2*H`. The new rule is PER QUADRANT: a symbol match scores +1 (the hero quadrant's
    symbol scores +2), and that quadrant's COLOR scores +1 ONLY IF that quadrant's symbol also
    matched. Max stays 9 = hero quad (color 1 + symbol 2 = 3) + three ordinary quads (color 1 +
    symbol 1 = 2 each). Because color is now gated behind the same quadrant's symbol, the four
    quadrants are no longer independent axes — enumerate per-quadrant joint (symbol-match,
    color-match) outcomes and convolve the four quadrant contributions. Each ordinary quadrant
    contributes a per-quadrant distribution over {0, +1 (symbol only), +2 (symbol+color)} built
    from P_SYM (1/8 symbol) x its color Bernoulli (gold P_COLOR_GOLD 1/15, common P_COLOR_COMMON
    2/15), with the +color leg conditioned on the symbol matching; a color match with no symbol
    match contributes 0. The hero quadrant contributes over {0, +2 (hero symbol only), +3
    (hero symbol + color)} using P_HERO's 1/8 and the hero quadrant's color Bernoulli. For a
    ticket with n_gold gold quadrants, decide which quadrants are gold-colored vs common-colored;
    average over hero placement is handled in Plan 02 — for THIS function keep returning ONE
    length-10 distribution per N that matches the HEAD convention (the per-N table the existing
    `P_N_TABLE` / `S9_PIN` loop expects). Confirm DEC-03 behavior: a lone ordinary symbol = S=1
    (the unchanged SHAPE pays 0 there); the hero symbol alone = S=2 and a full quadrant double =
    S=2 (both pay). Preserve the function signature `p_score_distribution(n_gold)`, the length-10
    return, and the `assert sum(dist) == 1` / `assert len(dist) == 10`.
    CRITICAL: S=9 must remain exactly the all-8-axes event — every quadrant double — so honest
    P_N(S=9) is the product of all eight per-axis match probabilities, byte-identical to HEAD;
    the `S9_PIN` reproduction loop (~L147-155) MUST still pass unchanged. Do NOT touch `SHAPE`,
    `S9_PIN`, `_solve_table`, the WWXRP factor code, or any print/assert section other than this
    function's body. Refresh ONLY the function's own docstring/comments to describe Variant-2;
    leave the module-level docstring's broader prose for Plan 02's doc-comment refresh.
  </action>
  <verify>
    <automated>python3 .planning/notes/degenerette-recalibration/derive_5_tables.py >/tmp/v452_t1.txt 2>&1; echo "exit=$?"; grep -c "S=9 relabel pin reproduced" /tmp/v452_t1.txt</automated>
  </verify>
  <acceptance_criteria>
    - `python3 .planning/notes/degenerette-recalibration/derive_5_tables.py` exits 0 (the `S9_PIN` reproduction loop still asserts honest P_N(S=9) == old all-8-match event for all N).
    - stdout contains the line "S=9 relabel pin reproduced from the old M=8 scaling for all N (consistent)." (the S=9 invariant assert passed under Variant-2).
    - The per-N `assert sum(dist) == 1` passes (no assertion traceback for the honest distribution).
    - `p_score_distribution` body references the color-gated-by-symbol rule (color +1 only when that quadrant's symbol matched), not four independent color axes; `grep -n "only if\|gated\|symbol matched" derive_5_tables.py` shows the Variant-2 intent in the function's comments.
  </acceptance_criteria>
  <done>
    `p_score_distribution` encodes Variant-2 (color +1 only on a symbol-matched quadrant; hero
    symbol +2; max S=9 = hero double + three ordinary doubles; floor S>=2 consistent with DEC-03),
    the script exits 0, and the existing S=9-relabel assert + per-N sum-to-1 assert both still pass
    — i.e. P(S=9) is byte-identical to HEAD.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-derive p_score_distribution_rigged for DEC-01 R2 (score-bearing pool + empty-pool case)</name>
  <files>.planning/notes/degenerette-recalibration/derive_5_tables.py</files>
  <read_first>
    - .planning/notes/degenerette-recalibration/derive_5_tables.py (FULL — edit the current `p_score_distribution_rigged` body; note it must stay consistent with the Variant-2 `p_score_distribution` from Task 1 and feed `_solve_table` + `wwxrp_factors_rig` below it)
    - .planning/phases/452-gen-generator-first-no-contract-edit/452-CONTEXT.md (DEC-01 R2 — the eligible-cell pool definition + the explicit empty-eligible-pool case; LOCKED)
    - .planning/REQUIREMENTS.md (RIG-01, RIG-02 — score-bearing rig + m>=7 cap keeps P(S=9) invariant)
    - contracts/modules/DegenerusGameDegeneretteModule.sol `_rigWwxrpResult` ~L1299-1358 (read-only: current pass-1 `u` pool that R2 narrows)
    - .planning/notes/degenerette-recalibration/model_wwxrp_rig.py (scoping model — `rigged_score_dist` variant B; the R2 narrowing supersedes "any unmatched ordinary cell")
  </read_first>
  <action>
    Re-derive `p_score_distribution_rigged(n_gold)` so it is consistent with the Variant-2 honest
    rule from Task 1 AND forces only SCORE-BEARING cells per DEC-01 R2. Enumerate per-quadrant
    (symbol-matched, color-matched) joint states under Variant-2 (same per-quadrant structure as
    Task 1), compute the base Variant-2 score S and the total matched-axis count M = sum of all
    eight per-axis matches. R2 eligible-cell pool = the cells whose forced match RAISES S under
    Variant-2: (a) an unmatched NON-HERO symbol cell (forcing it +1, and it also unlocks that
    quadrant's color only if the color happens to already match — but per R2 the forced cell
    itself is the symbol, scoring +1); (b) an unmatched COLOR on a quadrant whose symbol ALREADY
    matched (color "unlocks" -> +1). EXCLUDE: the hero symbol cell (never rigged) and no-op colors
    (a color on a quadrant whose symbol is still unmatched buys nothing under Variant-2). When the
    rig fires (probability 3/5, matching the existing `pf = Fraction(3,5)` gate) on a roll with
    M <= 6, it lifts S by exactly +1 (one score-bearing cell forced). Add the EXPLICIT
    empty-eligible-pool case: when M <= 6 but the only unmatched cells are the excluded hero symbol
    and no-op colors, the score-bearing pool is empty -> NO lift that round (the probability mass
    stays at baseS even though the gate would otherwise fire). Preserve the m>=7 cap exactly
    (M >= 7 rolls untouched) so the rig can never manufacture S=9. Keep the function signature,
    the length-10 return, `assert sum(out) == 1`, and `assert len(out) == 10`. The downstream
    `P_N_RIG` loop's `assert rig[9] == P_N_TABLE[N][9]` (P(S=9) invariant) MUST still pass for
    all N. Do NOT touch `_solve_table`, `wwxrp_factors_rig`, the WWXRP RIG FAMILY print block, or
    any other section. Refresh ONLY this function's docstring to describe the R2 score-bearing
    pool + the empty-pool case.
  </action>
  <verify>
    <automated>python3 .planning/notes/degenerette-recalibration/derive_5_tables.py >/tmp/v452_t2.txt 2>&1; echo "exit=$?"; grep -c "factors<2^64 OK" /tmp/v452_t2.txt</automated>
  </verify>
  <acceptance_criteria>
    - `python3 .planning/notes/degenerette-recalibration/derive_5_tables.py` exits 0 (no assertion traceback) with both Variant-2 distributions in place.
    - The `P_N_RIG` loop's `assert rig[9] == P_N_TABLE[N][9]` passes for all 5 N — i.e. the rigged P(S=9) equals the honest P(S=9) (m>=7 cap held, RIG-02).
    - The rigged-table verification block prints 5 lines each ending "factors<2^64 OK" (one per N) — `grep -c "factors<2^64 OK" /tmp/v452_t2.txt` returns 5.
    - Each rigged per-N line reports `bonus EV=5.00000%` and a rigged EV in (99,100] centi-x (the existing `assert ev_frac <= 100` and `> 99` both pass under the R2 distribution).
    - The `p_score_distribution_rigged` body/docstring documents the R2 score-bearing pool (non-hero symbol; color on an already-symbol-matched quadrant) and the explicit empty-eligible-pool no-lift case; `grep -n "empty\|no-op\|score-bearing" derive_5_tables.py` shows it.
  </acceptance_criteria>
  <done>
    `p_score_distribution_rigged` forces only score-bearing cells under Variant-2 (DEC-01 R2),
    handles the empty-eligible-pool case explicitly, preserves the m>=7 cap so rigged P(S=9) ==
    honest P(S=9), and the script exits 0 with all existing rigged asserts (sum-to-1, S=9
    invariance, EV in (99,100], bonus EV 5.000%, factors<2^64) passing.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — offline generator) | This plan edits ONLY an offline Python generator (`derive_5_tables.py`). No runtime, no `.sol`, no contract surface, no untrusted input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-452-01 | Tampering | `p_score_distribution` / `p_score_distribution_rigged` silently produce a wrong distribution | mitigate | The self-assert harness (sum-to-1, `rig[9] == honest[9]`, EV in (99,100], bonus EV 5.000%, factors<2^64, S=9-relabel reproduction) runs on every script invocation; the 454 TST byte-reproduce gate and 455 REAUDIT re-derive and diff. A wrong distribution either trips an assert (exit != 0) or is caught downstream before any contract edit. |

Contract-level threats (solvency, RNG-freeze, brick) are OUT OF SCOPE for Phase 452 — no
contract is touched here; they are deferred to Phases 453/455. No contract threats are invented
for a phase that produces no runtime surface.
</threat_model>

<verification>
- `python3 .planning/notes/degenerette-recalibration/derive_5_tables.py` exits 0 with both
  distributions rewritten (the full existing assert harness still passes).
- stdout still contains "S=9 relabel pin reproduced from the old M=8 scaling for all N (consistent)."
  (honest P(S=9) byte-identical to HEAD).
- The rigged `assert rig[9] == P_N_TABLE[N][9]` passes for all N (rigged P(S=9) == honest P(S=9)).
- 5 rigged-verification lines end "factors<2^64 OK".
- No section other than the two distribution functions (and their own docstrings) is modified
  (`git diff --stat` shows only `derive_5_tables.py`; the SHAPE/S9_PIN/_solve_table/factor code
  is byte-unchanged except where the two functions live).
</verification>

<success_criteria>
- `p_score_distribution` implements Variant-2 (color +1 only on a symbol-matched quadrant; hero
  symbol +2; S in {0..9}; max 9 = all eight axes; floor S>=2 per DEC-03), with honest P(S=9)
  byte-identical to HEAD.
- `p_score_distribution_rigged` implements DEC-01 R2 (score-bearing pool only; hero symbol + no-op
  colors excluded; explicit empty-eligible-pool no-lift case) with the m>=7 cap preserving P(S=9).
- The script runs to exit 0 under the unchanged self-assert harness.
</success_criteria>

<output>
Create `.planning/phases/452-gen-generator-first-no-contract-edit/452-01-SUMMARY.md` when done.
</output>
