---
phase: 452-gen-generator-first-no-contract-edit
plan: 03
type: execute
wave: 3
depends_on: ["452-02"]
files_modified:
  - .planning/notes/degenerette-recalibration/derive_5_tables.py
autonomous: true
requirements: [EVEQ-01, GEN-02, GEN-03]

# Added 2026-06-21 AFTER the 452-02 EVEQ-01 measurement resolved DEC-02 to **Option B**
# (USER decision). The 452-01/02 generator produced Option-A (5 averaged honest tables) and
# MEASURED a 2.99 centi-x hero-placement drift on the honest ETH/FLIP lane with the hero-common
# sub-case EV-positive (player-selectable via the custom-ticket heroQuadrant param). This plan
# regenerates the generator for Option B: exact EV-equality on the honest lane.

must_haves:
  truths:
    - "The HONEST family is split per (N, hero-is-gold): for each (N, heroGold) sub-case that is structurally possible, a payout table is solved against THAT sub-case's own Variant-2 distribution (not the N/4-averaged one). Valid sub-cases: N0 hero-common; N1 hero-gold/hero-common; N2 hero-gold/hero-common; N3 hero-gold/hero-common; N4 hero-gold => ~8 honest base tables."
    - "After the split, the residual hero-placement EV drift on the honest lane is ~0 (each sub-case table is EV-exact for its own sub-case): the EVEQ-01 drift report now shows max honest drift effectively 0 / within rounding, and every honest sub-case basePayoutEV is in (99,100] centi-x (solvency-safe AND exactly EV-equal across picks)."
    - "The honest ETH-bonus factor family (WWXRP_FACTORS_N*, isWwxrp=false) is likewise split per (N, hero-is-gold) so the honest +5% ETH bonus EV is exactly 5.000% within each sub-case (not just on the average) — exact EV-equality covers base + bonus on the honest lane."
    - "The WWXRP _RIG_ family stays AVERAGED at 5 per-N tables (QUICK_PLAY_PAYOUTS_RIG_N{0..4} + WWXRP_FACTORS_RIG_N{0..4}) — WWXRP hero-placement drift is accepted by-design per the USER don't-care ruling; the rig keeps its m>=7 cap so rigged P(S=9)==honest P(S=9)."
    - "The numeric pre-proof still holds and is recomputed per sub-case: honest P_N(S=9) == rigged P_N(S=9) == HEAD all-8-match probability (Fraction-exact) for all N — P(S=9), the S9 pins, and the WWXRP RTP curve are unchanged vs HEAD."
    - "The FINAL PASTE-READY CONSTANTS block emits the new honest naming (per-(N,hero-gold)), the unchanged WWXRP _RIG_ family, and the S9 pins; and the generator PRINTS the exact `_getBasePayoutBps` dispatch shape 453 must implement (a heroIsGold selector used only when !isWwxrp)."
    - "The script runs to exit 0 with all self-asserts (per-sub-case EV in (99,100] honest, bonus EV 5.000%, factors<2^64, rigged P(S=9)==honest P(S=9), pre-proof equality) passing."
  artifacts:
    - path: ".planning/notes/degenerette-recalibration/derive_5_tables.py"
      provides: "Option-B honest per-(N,hero-gold) tables + averaged WWXRP _RIG_ family + dispatch-shape print + pre-proof"
      contains: "hero"
  key_links:
    - from: "honest per-(N,hero-gold) tables"
      to: "exact EV-equality (drift ~0)"
      via: "each sub-case table solved against its own Variant-2 sub-case distribution"
      pattern: "hero.*gold"
    - from: "WWXRP _RIG_ family"
      to: "5 averaged per-N tables (by-design WWXRP drift)"
      via: "WWXRP don't-care ruling; rig keeps m>=7 cap"
      pattern: "_RIG_"
---

<objective>
Regenerate `derive_5_tables.py` for **DEC-02 Option B** (exact EV-equality on the honest lane), the
resolution the 452-02 EVEQ-01 measurement forced and the USER selected. Split the HONEST payout +
ETH-bonus family per `(N, hero-is-gold)` so each pick is exactly EV-equal (no player-selectable
edge), keep the WWXRP `_RIG_` family averaged at 5 tables (WWXRP drift is by-design don't-care),
re-verify the P(S=9)/RTP pre-proof per sub-case, and PRINT the exact `_getBasePayoutBps` dispatch
shape that 453 IMPL must implement. NO `.sol` is touched — this is still the no-contract-risk
generator-first phase; the contract diff is 453 (the sole approval gate).

Output: a `derive_5_tables.py` that, on `python3` run, exits 0 with all self-asserts passing,
emits the Option-B honest per-(N,hero-gold) constant family + the unchanged WWXRP _RIG_ family +
the S9 pins, reports a hero-placement EV-drift of ~0 on the honest lane (exact equality), prints
the P(S=9)/WWXRP-RTP "unchanged vs HEAD" pre-proof, and prints the `_getBasePayoutBps` dispatch
shape (heroIsGold selector, !isWwxrp only) for the 453 paste.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/452-gen-generator-first-no-contract-edit/452-CONTEXT.md
@.planning/phases/452-gen-generator-first-no-contract-edit/452-02-SUMMARY.md
@.planning/REQUIREMENTS.md

<interfaces>
The current generator (after 452-01/02) already encodes:
- Variant-2 honest `p_score_distribution(n_gold)` (averaged over hero placement at P(hero gold|N)=N/4).
- DEC-01 R2 `p_score_distribution_rigged(n_gold)` (averaged), with the m>=7 cap.
- `_solve_table(pS, N)` (SHAPE + S9_PIN solve, residual absorption, neutral-or-just-under nudge) —
  currency-/sub-case-agnostic; REUSE UNCHANGED, just feed it the per-sub-case distribution.
- `wwxrp_factors(N)` / `wwxrp_factors_rig(N)` — the bonus-factor solve; honest one must now be
  computed per sub-case (built off the per-sub-case distribution), rigged one stays per-N averaged.
- The EVEQ-01 drift report + the NUMERIC PRE-PROOF sections from 452-02.

Option B target shape (this run's reading; see 452-CONTEXT.md DEC-02 RESOLUTION):
- HONEST family split per (N, heroGold): structurally-valid sub-cases are
  N0(common), N1(gold|common), N2(gold|common), N3(gold|common), N4(gold) = 8 base tables.
  Build the per-sub-case Variant-2 distribution by fixing whether the HERO quadrant's color is
  gold (1/15) or common (2/15), and distributing the remaining gold quadrants among the other 3
  ordinary quadrants. Solve each with `_solve_table`. The honest ETH-bonus factors split the same way.
- WWXRP _RIG_ family UNCHANGED: 5 averaged per-N tables (QUICK_PLAY_PAYOUTS_RIG_N{0..4} +
  WWXRP_FACTORS_RIG_N{0..4}); the rig dist stays averaged; WWXRP drift accepted by-design.
- S9 pins (S9_PIN / QUICK_PLAY_PAYOUT_N{N}_S9) and WWXRP_ROI_* RTP curve: untouched (printed, never recomputed).

Contract dispatch (453 will implement; this run only PRINTS the shape, edits no .sol):
- `_getBasePayoutBps(N, isWwxrp, heroIsGold)`: when `!isWwxrp`, index the honest table by (N, heroIsGold);
  when `isWwxrp`, index the _RIG_ table by N only (heroIsGold ignored). heroIsGold is derived in-contract
  from whether the player's hero quadrant carries a gold color (read-only fact about the existing ticket).
  Confirm N0 (always hero-common) and N4 (always hero-gold) collapse to a single table each.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split the honest family per (N, hero-is-gold) and solve each sub-case exactly</name>
  <files>.planning/notes/degenerette-recalibration/derive_5_tables.py</files>
  <read_first>
    - .planning/notes/degenerette-recalibration/derive_5_tables.py (FULL — the current Variant-2 generator; note p_score_distribution, _solve_table, wwxrp_factors, the honest FINAL block, and the EVEQ-01 drift report added in 452-02)
    - .planning/phases/452-gen-generator-first-no-contract-edit/452-CONTEXT.md (DEC-02 RESOLUTION — Option B, honest-only split, WWXRP averaged)
    - .planning/phases/452-gen-generator-first-no-contract-edit/452-02-SUMMARY.md (the measured drift numbers this closes)
    - .planning/REQUIREMENTS.md (EVEQ-01, GEN-02, GEN-03)
  </read_first>
  <action>
    Refactor the honest generation path so it produces one payout table per structurally-valid
    (N, heroGold) sub-case instead of one averaged table per N. Add a helper that builds the
    Variant-2 honest distribution for a fixed hero-color (gold vs common) and a given count of gold
    quadrants among the 3 non-hero ordinary quadrants; reuse the SAME per-quadrant joint
    (symbol,color) convolution `p_score_distribution` already uses (do not change the Variant-2
    rule). Enumerate the valid sub-cases: N0->{common}, N1->{gold,common}, N2->{gold,common},
    N3->{gold,common}, N4->{gold} (a sub-case "hero gold" means the hero quadrant's color is gold
    and N-1 of the other 3 are gold; "hero common" means hero color common and all N gold quads are
    among the other 3, requiring N<=3). Solve each sub-case table with `_solve_table` UNCHANGED so
    every honest sub-case basePayoutEV lands in (99,100] centi-x (keep `assert ev_frac <= 100` and
    `> 99` per sub-case). Split the honest ETH-bonus factor solve (`wwxrp_factors`, isWwxrp=false)
    the same way so the honest +5% ETH bonus EV is exactly 5.000% within EACH sub-case (keep the
    bonus-EV assert per sub-case). Keep the WWXRP _RIG_ family EXACTLY as-is (5 averaged per-N
    tables, the rig dist + m>=7 cap unchanged) — do NOT split it. Do NOT touch SHAPE, S9_PIN, the
    split ratios, ETH_BONUS_BPS, or the packing/bit-layout. Update the honest FINAL PASTE-READY
    CONSTANTS block to emit the new per-(N,hero-gold) honest names (choose a clear, greppable naming,
    e.g. QUICK_PLAY_PAYOUTS_N{N}_HEROGOLD_PACKED / _HEROCOMMON_PACKED and the matching _S8 + honest
    WWXRP_FACTORS), N0/N4 collapsing to one table each; the WWXRP _RIG_ block and the S9 pins print
    unchanged.
  </action>
  <verify>
    <automated>python3 .planning/notes/degenerette-recalibration/derive_5_tables.py >/tmp/v452_p3t1.txt 2>&1; echo "exit=$?"; echo "--- honest hero-split tables ---"; grep -ciE "HEROGOLD|HEROCOMMON|hero.gold|hero.common" /tmp/v452_p3t1.txt; echo "--- rig family still 5 ---"; grep -c "QUICK_PLAY_PAYOUTS_RIG_N[0-4]_PACKED" /tmp/v452_p3t1.txt; echo "--- s9 pins ---"; grep -c "QUICK_PLAY_PAYOUT_N[0-4]_S9" /tmp/v452_p3t1.txt</automated>
  </verify>
  <acceptance_criteria>
    - `python3 derive_5_tables.py` exits 0 (every honest sub-case `assert ev_frac <= 100` / `> 99` and bonus-EV assert passes; rigged asserts + pre-proof unchanged).
    - The honest FINAL block emits ~8 per-(N,hero-gold) base tables (N0 + N1g/N1c + N2g/N2c + N3g/N3c + N4) — greppable hero-gold / hero-common names present.
    - The WWXRP `_RIG_` family is still exactly 5 per-N tables (`grep -c QUICK_PLAY_PAYOUTS_RIG_N[0-4]_PACKED` returns 5) — unchanged from 452-02.
    - All 5 `QUICK_PLAY_PAYOUT_N{0..4}_S9` pin lines still equal HEAD [10_756_411, 12_583_037, 14_792_939, 17_512_324, 20_916_435].
  </acceptance_criteria>
  <done>
    The honest family is split per (N, hero-is-gold), each sub-case solved exactly (EV in (99,100]
    and exactly EV-equal across picks), the WWXRP `_RIG_` family stays averaged at 5, the S9 pins
    are untouched, and the script exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-verify exact EV-equality (drift ~0), per-sub-case pre-proof, and print the 453 dispatch shape</name>
  <files>.planning/notes/degenerette-recalibration/derive_5_tables.py</files>
  <read_first>
    - .planning/notes/degenerette-recalibration/derive_5_tables.py (FULL — the EVEQ-01 drift report + NUMERIC PRE-PROOF sections from 452-02; update them for the split honest family)
    - .planning/phases/452-gen-generator-first-no-contract-edit/452-CONTEXT.md (DEC-02 RESOLUTION — dispatch shape: heroIsGold selector, !isWwxrp only)
    - .planning/REQUIREMENTS.md (EVEQ-01, INV-02/03/04)
  </read_first>
  <action>
    Update the EVEQ-01 drift report so it now confirms EXACT EV-equality on the honest lane: for
    each (N, heroGold) sub-case, report basePayoutEV of THAT sub-case's own solved table against
    its own distribution; the max |EV_gold - EV_common| residual must now be ~0 (within the
    `_solve_table` rounding granularity, << the old 2.99 centi-x). Print a clear "Option B: honest
    lane exactly EV-equal across hero placement (residual <= X centi-x)" line and a note that the
    WWXRP _RIG_ lane retains its averaged-table drift by-design (USER don't-care). Keep the
    NUMERIC PRE-PROOF: honest P_N(S=9) == rigged P_N(S=9) == HEAD all-8-match probability
    (Fraction-exact) for all N — recompute per the new honest distributions; the S9 pins +
    WWXRP_ROI_* curve are still untouched (printed, never recomputed). Finally, PRINT the exact
    `_getBasePayoutBps` dispatch shape 453 IMPL must implement: a `heroIsGold` selector that, when
    `!isWwxrp`, indexes the honest table by (N, heroIsGold), and when `isWwxrp` indexes the _RIG_
    table by N only; note N0 always-hero-common and N4 always-hero-gold collapse to one table each;
    state heroIsGold is a read-only fact derived in-contract from whether the player's hero
    quadrant carries a gold color. Do NOT edit any `.sol` — only print the shape for the 453 paste.
  </action>
  <verify>
    <automated>python3 .planning/notes/degenerette-recalibration/derive_5_tables.py >/tmp/v452_p3t2.txt 2>&1; echo "exit=$?"; echo "--- option B equality ---"; grep -ciE "Option B|EV-equal|residual" /tmp/v452_p3t2.txt; echo "--- preproof ---"; grep -c "PRE-PROOF" /tmp/v452_p3t2.txt; echo "--- dispatch shape ---"; grep -ciE "_getBasePayoutBps|heroIsGold|dispatch" /tmp/v452_p3t2.txt</automated>
  </verify>
  <acceptance_criteria>
    - `python3 derive_5_tables.py` exits 0.
    - The drift report shows the honest-lane residual EV drift is ~0 (orders of magnitude below the old 2.99 centi-x) and states "Option B" exact EV-equality; `grep -i "Option B"` matches.
    - The "PRE-PROOF" marker still prints; honest==rigged==HEAD P(S=9) asserted Fraction-exact for all N (no traceback).
    - The generator prints the `_getBasePayoutBps` dispatch shape (heroIsGold selector, !isWwxrp only); `grep -i "heroIsGold"` matches.
  </acceptance_criteria>
  <done>
    The honest lane is proven exactly EV-equal across hero placement (residual ~0), the per-sub-case
    P(S=9)/RTP pre-proof passes, and the generator prints the exact 453 `_getBasePayoutBps` dispatch
    shape — all with no `.sol` touched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — offline generator) | Edits ONLY the offline Python generator `derive_5_tables.py`. No runtime, no `.sol`, no contract surface; no untrusted input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-452-03 | Tampering | The split honest tables silently encode wrong values or fail to close the drift | mitigate | Per-sub-case self-asserts (EV in (99,100], bonus EV 5.000%, factors<2^64) + the recomputed pre-proof (honest==rigged==HEAD P(S=9)) + the residual-drift check (must be ~0) all run on every invocation; the 454 TST byte-reproduce gate re-derives and diffs; 455 REAUDIT re-attests solvency on the new tables. A wrong table trips an assert (exit != 0) or is caught downstream before any contract ships. |

Contract-level threats (solvency, RNG-freeze, brick, the actual dispatch runtime behavior) are
OUT OF SCOPE for 452 — no contract is touched; deferred to 453 (IMPL, the sole approval gate) / 455 (REAUDIT).
</threat_model>

<verification>
- `python3 derive_5_tables.py` exits 0 with the split honest family + averaged _RIG_ family, all asserts passing.
- Honest lane proven exactly EV-equal across hero placement (residual ~0); WWXRP _RIG_ drift retained by-design.
- FINAL block emits ~8 honest per-(N,hero-gold) tables + 5 _RIG_ tables + 5 S9 pins.
- "PRE-PROOF" marker confirms P(S=9) + WWXRP RTP curve unchanged vs HEAD.
- The `_getBasePayoutBps` dispatch shape (heroIsGold, !isWwxrp only) is printed for 453.
- `git diff --stat` shows only `derive_5_tables.py`.
</verification>

<success_criteria>
- EVEQ-01 (Option B): honest lane exactly EV-equal across hero placement — the 2.99 centi-x selectable edge is closed.
- GEN-02/GEN-03: full Option-B honest constant family + averaged _RIG_ family regenerated; per-sub-case self-asserts green; S9 pins + WWXRP_ROI_* untouched.
- Numeric pre-proof: P(S=9) + WWXRP RTP curve confirmed unchanged vs HEAD under Variant-2 + R2 rig, per sub-case.
- The 453 `_getBasePayoutBps` dispatch shape is printed for the gated contract diff.
</success_criteria>

<output>
Create `.planning/phases/452-gen-generator-first-no-contract-edit/452-03-SUMMARY.md` when done.
Report the residual honest-lane EV drift (should be ~0) and the table counts in the SUMMARY.
</output>