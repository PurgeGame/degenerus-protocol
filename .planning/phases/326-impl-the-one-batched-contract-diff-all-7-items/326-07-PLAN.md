---
phase: 326-impl-the-one-batched-contract-diff-all-7-items
plan: 07
type: execute
wave: 5
depends_on: ["326-05", "326-06"]
files_modified:
  - contracts/DegenerusGame.sol
  - contracts/DegenerusVault.sol
autonomous: true
requirements: [SWAP-01, SWAP-02, SWAP-03, SWAP-04, SWAP-05, SWAP-06, SWAP-07]
must_haves:
  truths:
    - "A new sellFarFutureTickets(address player, uint32[] levels, uint256[] quantities, uint256[] queueIndices) game entrypoint resolves the player via _resolvePlayer (operator-honor), is rngLocked()-gated, and mass-sells many far levels into ONE aggregated current-level ticket mint + ONE cash credit."
    - "Each line requires 6 <= d = L - currentLevel <= 100, values at priceForLevel(targetLevel) with the two-line fractionBps(d) curve (15%@d6→5%@d100) and a daily per-player jitter (fraction ×∈[70%,110%], cash share ∈[20%,60%]) seeded from the SETTLED rngWordByDay[currentDay-1] (freeze-safe, no new mutable SLOAD in the rng window)."
    - "Ticket leg floored first: oneTicketWei = priceForLevel(currentLevel) (NOT /4); revert if totalBudget < oneTicketWei (always >=1 whole current ticket); funded fail-closed from claimableWinnings[SDGNRS] down to a >=1 ETH floor, NO pendingRedemptionEthValue term, NO daily cap, debited INLINE."
    - "_removeFarFutureTickets does an O(1) caller-verified swap-pop (q[idx]==player) only on full sell-out (packed==0), maintaining membership ⟺ packed != 0; duplicate input lines per level are aggregated so a level pops at most once; the swap-pop does NOT reproduce H-CANCEL-SWAP-MISS."
    - "VAULT gets a gameSellFarFutureTickets onlyVaultOwner wrapper + the IDegenerusGamePlayerActions interface entry; the cross-repo DroneManager re-sync is flagged (out of this diff)."
  artifacts:
    - path: "contracts/DegenerusGame.sol"
      provides: "sellFarFutureTickets entrypoint + _removeFarFutureTickets swap-pop primitive + inline claimableWinnings[SDGNRS] debit (R3)"
      contains: "function sellFarFutureTickets"
    - path: "contracts/DegenerusVault.sol"
      provides: "gameSellFarFutureTickets onlyVaultOwner wrapper + IDegenerusGamePlayerActions interface entry (R5 item-7)"
      contains: "gameSellFarFutureTickets"
  key_links:
    - from: "contracts/DegenerusGame.sol sellFarFutureTickets"
      to: "claimableWinnings[SDGNRS] (inline debit) + ticketQueue swap-pop"
      via: "CHECKED inline debit to a >=1 ETH floor + _removeFarFutureTickets on full sell-out"
      pattern: "_removeFarFutureTickets"
    - from: "contracts/DegenerusVault.sol gameSellFarFutureTickets"
      to: "contracts/DegenerusGame.sol sellFarFutureTickets"
      via: "gamePlayer.sellFarFutureTickets(address(this), levels, quantities, queueIndices)"
      pattern: "gameSellFarFutureTickets"
---

<objective>
Item 7 (SWAP) — ship the sDGNRS far-future salvage swap: a new `sellFarFutureTickets(...)` game entrypoint that lets a player (or VAULT via its operator) mass-sell far-future ticket entries (`6 ≤ d = L - currentLevel ≤ 100`) to sDGNRS for ~60% current-level tickets + ≤40% withdrawable ETH, -EV by design ("sDGNRS is the house"). The swap mints ONE aggregated current-level ticket batch + credits ONE cash amount, valued per-line at `priceForLevel(targetLevel)` with the two-line `fractionBps(d)` curve + a daily per-player jitter seeded from a SETTLED past VRF word, funded fail-closed from `claimableWinnings[SDGNRS]` to a ≥1 ETH floor (no `pendingRedemptionEthValue` term, no daily cap). The fully-liquidated seller is removed from `ticketQueue` via an O(1) caller-verified swap-pop that maintains `membership ⟺ packed != 0` (NOT the H-CANCEL-SWAP-MISS operation class). VAULT gets an `onlyVaultOwner` wrapper.

Purpose: ROADMAP SC6 (SWAP-01/02/03/04/05/06/07) — the new entrypoint (`_resolvePlayer` operator-honor, `rngLocked`-gated, mass-sell → ONE mint + ONE credit), the `d`-curve valuation + jitter, the ticket floor + inline fail-closed claimable debit, the swap-pop, and the VAULT wrapper. This is the LAST `DegenerusGame.sol` edit (R3 apply-order: after R1/RFALL + R2/KEEP) — hence Wave 5, serialized behind 326-06 — and the item-7 portion of the VAULT co-edit (R5), serialized behind 326-05's VAULT edits.
Output: The `sellFarFutureTickets` entrypoint + `_removeFarFutureTickets` primitive + inline claimable debit (R3); the VAULT `gameSellFarFutureTickets` wrapper + interface entry (R5 item-7); the R6 `DroneManager`/OPEN-E flags recorded.

NON-NEGOTIABLE: This plan EDITS `contracts/*.sol` but MUST NOT commit anything. ONE batched diff, committed only after 326-08. NOTHING pre-approved. FREEZE-INVARIANT (v45, hard floor): the SWAP-03 jitter seed MUST be the already-SETTLED `rngWordByDay[currentDay-1]` (immutable-once-written, public) — NOT the in-flight `rngWordCurrent` — and the swap MUST stay `rngLocked()`-gated so it cannot interleave with a VRF-consuming `processFutureTicketBatch` drain. NO-ARB FLOOR (SWAP-08, re-derived +4.5pp at the d6 ceiling): do NOT widen the `fractionBps` curve (15%@d6→5%@d100), the fraction band [70%,110%], or the cash band [20%,60%] — if implementation reveals the margin is violated at any `d`, emit `## STOP — NO-ARB MARGIN VIOLATED` and surface to the user instead of manufacturing margin. APPLY-ORDER: R3 is appended AFTER R1 (RFALL) + R2 (KEEP); R5 item-7 wrappers go after 326-05's VAULT interface adds + recovery wrapper. The cross-repo `DroneManager` re-sync is OUT of this diff (flag only, R6).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-SWAP.md
@.planning/PLAN-SDGNRS-FAR-FUTURE-SALVAGE-SWAP.md

<interfaces>
<!-- Key constructs for the SWAP entrypoint. Extracted from live source at da5c9d50. -->

contracts/DegenerusGame.sol:
  :449  function _resolvePlayer(...)                          // operator-honor resolution (NOT bare msg.sender)
  :2327 rngWordForDay(uint32 day) → rngWordByDay[day]         // public reader of the SETTLED past word
  :2333 function rngLocked() external view returns (bool)     // the gate (rngLockedFlag)
  :2592 function sampleFarFutureTickets(...)                  // far sampler (queue read :2604) — MUST gain NO hot-path read
  :2210 ticketsOwedView(...)                                  // near-future view (returns 0 for far levels)
  :217/:218 _queueTickets(SDGNRS/VAULT, i, 16, false)         // ctor far-seed (16 entries = 4 tickets/level)

contracts/storage/DegenerusGameStorage.sol:
  :166  TICKET_SCALE = 100                                    // owed in entries: 4 entries = 1 whole ticket, 400 quantity = 1 ticket
  :732  function _tqFarFutureKey(uint24 lvl) internal pure    // ffk = L | TICKET_FAR_FUTURE_BIT
  :744  if (ticketQueue[rk].length != 0) revert E();          // enrol guard (re-enrol on a drained key)
  :560  _queueTickets(...)                                    // the far-future PUSH path (membership establish)

contracts/libraries/PriceLookupLib.sol:
  :21   function priceForLevel(uint24 targetLevel) internal pure returns (uint256)  // the non-manipulable face anchor

contracts/modules/DegenerusGameJackpotModule.sol:
  :1754 function _awardFarFutureCoinJackpot(...)              // far sampler — MUST gain NO hot-path read
  :639  function _runEarlyBirdLootboxJackpot(...)             // NOTE C7: in JackpotModule, NOT AdvanceModule — does NOT read far membership

UNIT BASIS (SWAP-02, §E): oneTicketWei = priceForLevel(currentLevel)  (NOT /4); faceWei = priceForLevel(L) × wholeTickets.
JITTER SEED (SWAP-03, §C): rngWordByDay[currentDay-1]  (SETTLED, immutable-once-written, public; NOT rngWordCurrent).
</interfaces>

<grep_protocol>
Baseline `da5c9d50` (live tree byte-identical). Plans 326-04 (RFALL :1888) + 326-06 (KEEP rename :1570-1778 + affiliate :1778) already edited `DegenerusGame.sol`; plan 326-05 already edited `DegenerusVault.sol` (interface adds + `recoverAfKingPool()`). RE-GREP every anchor (`_resolvePlayer`, `rngLocked`, `rngWordByDay`, `_tqFarFutureKey`, `sampleFarFutureTickets`, `priceForLevel`, the VAULT `IDegenerusGamePlayerActions` interface + the `gameXxx onlyVaultOwner` wrappers) before editing — lines have shifted. The 325-ATTEST-SWAP §A-E (no-arb, BURNIE-can't-mint, jitter pin, swap-pop enumeration, unit basis) + carried corrections C7 (the JackpotModule:639 citation) + C8 (units) are source-of-truth.
</grep_protocol>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sellFarFutureTickets + _removeFarFutureTickets + the inline claimable debit on DegenerusGame (R3)</name>
  <read_first>
    - contracts/DegenerusGame.sol (`_resolvePlayer` :449; `rngLocked`/`rngWordByDay`/`rngWordForDay` :2333/:2326/:2327; `sampleFarFutureTickets` :2592 + queue read :2604; `ticketsOwedView` :2210; the ctor far-seed :217/:218; the `claimableWinnings[SDGNRS]` ledger + `claimablePool` how they are debited (mirror the RFALL `pullRedemptionReserve` CHECKED pattern from 326-04); the current-level mint path the swap reuses for the aggregated ticket mint)
    - contracts/storage/DegenerusGameStorage.sol (`TICKET_SCALE=100` :166; `_tqFarFutureKey` :732; the far-future PUSH/`_queueTickets` :560/:581/:613/:667; the enrol guard :744; `ticketsOwedPacked` shape for the far key)
    - contracts/libraries/PriceLookupLib.sol (`priceForLevel` :21 — the face anchor)
    - 325-ATTEST-SWAP.md §A (no-arb + the STOP rule), §C (jitter pin rngWordByDay[currentDay-1], freeze-safe), §D (swap-pop consumer enumeration + the membership⟺packed invariant + H-CANCEL-SWAP-MISS absence), §E (units) + 325-SPEC.md §1 R3 (the entrypoint signature + inline debit + swap-pop spec)
    - PLAN-SDGNRS-FAR-FUTURE-SALVAGE-SWAP.md §5 (entrypoint + _removeFarFutureTickets) + §3 (the d-curve + jitter bands) + §6.3 (rngLocked gate)
  </read_first>
  <action>
    Per SWAP-01..06 + SPEC §1 R3 + §2 (no new daily cap / no pendingRedemptionEthValue term). Append a new entrypoint `function sellFarFutureTickets(address player, uint32[] calldata levels, uint256[] calldata quantities, uint256[] calldata queueIndices) external` to `DegenerusGame.sol` (R3 — AFTER the RFALL + KEEP edits). It MUST:
    (1) Resolve the actor via `_resolvePlayer` (operator-honor — NOT bare `msg.sender`); revert while `rngLocked()` (the gate — freeze-safety + cursor mutual-exclusion).
    (2) For each input line: require `6 ≤ d = L - currentLevel ≤ 100`; value at `priceForLevel(targetLevel)`; apply the two-line `fractionBps(d)` curve (15%@d6 → 5%@d100, the EXACT curve from plan §3 — do NOT widen); apply the daily per-player jitter (fraction multiplier ∈[70%,110%], cash share ∈[20%,60%]) seeded from `rngWordByDay[currentDay-1]` (the SETTLED prior-day word — NOT `rngWordCurrent`; no new mutable SLOAD in the rng window).
    (3) Aggregate across all lines → ONE current-level ticket mint + ONE cash credit. UNITS (§E/C8): `oneTicketWei = priceForLevel(currentLevel)` (NOT `/4`); `faceWei = priceForLevel(L) × wholeTickets`; the ledger stores `owed` in entries (4/ticket). TICKET FLOOR FIRST: `require totalBudget ≥ oneTicketWei` (always ≥1 whole current ticket; revert if `totalBudget < oneTicketWei`).
    (4) Fund the cash leg fail-closed via a CHECKED INLINE `claimableWinnings[SDGNRS]` debit (mirror RFALL's CHECKED pattern; the ledger lives on the game — NO cross-contract `pullRedemptionReserve` call) down to a ≥1 ETH `claimableWinnings[SDGNRS]` floor; NO `pendingRedemptionEthValue` term; NO daily cap; standing/by-construction authorization via `_resolvePlayer`.
    (5) Remove the fully-liquidated seller via a NEW internal `function _removeFarFutureTickets(address player, uint24 L, uint32 n, uint256 idx) internal`: decrement `owed` (keep `rem` dust with the seller); on full sell-out (`newOwed == 0 && rem == 0` → `packed == 0`) VERIFY `q[idx] == player` then O(1) swap-pop the seller out of `ticketQueue[_tqFarFutureKey(L)]`; partial sells / sells leaving `rem` do NOT pop. Maintain `membership ⟺ packed != 0`. AGGREGATE duplicate input lines per level so a level pops at most once with a valid index (a stale duplicate index → the `q[idx] != player` verify reverts the second; fail-safe). Do NOT add any hot-path read to `sampleFarFutureTickets` (:2592) or `_awardFarFutureCoinJackpot` (JackpotModule:1754) — they stay random-access samplers over a correctly-shrunk array.
    NO-ARB STOP RULE: if at any `d ∈ [6,100]` the implementation would let `110% × fractionBps(d) × face` meet/exceed the cheapest realistic acquisition (~21% at d6 binding), emit `## STOP — NO-ARB MARGIN VIOLATED` and surface — do NOT widen any band to fix it. RE-GREP every anchor before editing (lines shifted by 326-04/05/06).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -n "function sellFarFutureTickets" contracts/DegenerusGame.sol && grep -n "function _removeFarFutureTickets" contracts/DegenerusGame.sol && grep -n "rngWordByDay\[" contracts/DegenerusGame.sol | head && grep -n "_resolvePlayer\|rngLocked" contracts/DegenerusGame.sol | head && timeout 400 forge build 2>&1 | grep -ciE "^error|Error \(" | tr -d "\n"; echo " build errors (MUST be 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `sellFarFutureTickets(address,uint32[],uint256[],uint256[])` exists, resolves via `_resolvePlayer`, and reverts while `rngLocked()` (grep both present in/around the function).
    - Each line requires `6 ≤ d ≤ 100`; the `fractionBps(d)` curve is the EXACT two-line 15%@d6→5%@d100 (no widened band); the jitter seed reads `rngWordByDay[currentDay-1]` (the settled prior-day word, NOT `rngWordCurrent`).
    - `oneTicketWei = priceForLevel(currentLevel)` (NOT `/4`); a `require totalBudget >= oneTicketWei` ticket floor is present (revert if below).
    - The cash leg debits `claimableWinnings[SDGNRS]` INLINE, CHECKED, to a ≥1 ETH floor; NO `pendingRedemptionEthValue` reference in the swap; NO daily-cap state.
    - `_removeFarFutureTickets` verifies `q[idx] == player` and swap-pops ONLY on full sell-out (`packed == 0`); partial/rem sells do NOT pop; duplicate lines per level aggregated.
    - `sampleFarFutureTickets` (:2592) and `_awardFarFutureCoinJackpot` (JackpotModule) gain NO new hot-path read (`git diff` shows no edit to those sampler bodies).
    - The RFALL (`pullRedemptionReserve`) + KEEP (renamed entrypoints + `bytes32("DGNRS")`) edits from 326-04/06 are preserved.
    - `forge build` reports 0 errors.
  </acceptance_criteria>
  <done>`sellFarFutureTickets` (operator-honor, rngLocked-gated, d-curve + settled-word jitter, ticket floor, inline fail-closed claimable debit, no cap) + `_removeFarFutureTickets` (caller-verified swap-pop maintaining membership⟺packed) land on DegenerusGame; samplers + prior edits untouched; compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Add the VAULT gameSellFarFutureTickets onlyVaultOwner wrapper + IDegenerusGamePlayerActions interface entry (R5 item-7); flag DroneManager/OPEN-E</name>
  <read_first>
    - contracts/DegenerusVault.sol (the `IDegenerusGamePlayerActions` interface :10/:395 — where game-player actions are declared; the `gameXxx onlyVaultOwner` wrapper pattern, e.g. `gameOpenLootBox` :550, `gameClaimWinnings` :574; the `gamePlayer` reference; the `onlyVaultOwner` modifier :433; the `recoverAfKingPool()` added in 326-05 — place the new wrapper near the other `gameXxx` wrappers)
    - contracts/DegenerusGame.sol (the `sellFarFutureTickets(address,uint32[],uint256[],uint256[])` signature added in Task 1 — the interface entry + the wrapper call must match it VERBATIM)
    - 325-ATTEST-SWAP.md §D (#7 — `ticketsOwedView` is near-future only; the far read the wrapper relies on) + 325-SPEC.md §1 R5 (item-7 VAULT portion) + R6 (DroneManager flag + OPEN-E disposition)
    - PLAN-SDGNRS-FAR-FUTURE-SALVAGE-SWAP.md (the VAULT wrapper + the DroneManager pass-through)
  </read_first>
  <action>
    Per SWAP-05/07 + SPEC §1 R5 (item-7) + R6. (A) Add `sellFarFutureTickets(address,uint32[],uint256[],uint256[])` to VAULT's `IDegenerusGamePlayerActions` interface (matching the Task-1 game signature VERBATIM). (B) Add `function gameSellFarFutureTickets(uint32[] calldata levels, uint256[] calldata quantities, uint256[] calldata queueIndices) external onlyVaultOwner { gamePlayer.sellFarFutureTickets(address(this), levels, quantities, queueIndices); }` near the other `gameXxx onlyVaultOwner` wrappers (the >50.1% DGVE holder can salvage VAULT's far inventory; VAULT self-calls, no operator). No reentrancy concern (output is internal ledger only; cash is pulled later via `gameClaimWinnings`). Place it AFTER the 326-05 interface adds + `recoverAfKingPool()` (apply-order R5). (C) FLAG (do NOT edit — out of this diff): record in the SUMMARY that the cross-repo `DroneManager` (degenerus-utilities) needs a +1 typed `onlyChainOwner sellFarFutureTickets(uint8 idx, uint32[], uint256[], uint256[])` pass-through matching the R3 signature, folded into its pending v47-interface re-sync — NOT in this `degenerus-audit` diff (R6). ALSO record that the OPEN-E operator-trust disposition is CONFIRMED to cover this first value-destructive operator-gated action (the SPEC explicitly confirms it; T-325-S* mitigated). RE-GREP the VAULT interface + wrapper region before editing (326-05 shifted lines).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -n "function gameSellFarFutureTickets" contracts/DegenerusVault.sol && grep -n "sellFarFutureTickets" contracts/DegenerusVault.sol | head && grep -n "onlyVaultOwner" contracts/DegenerusVault.sol | grep -i "sellfar" && timeout 400 forge build 2>&1 | grep -ciE "^error|Error \(" | tr -d "\n"; echo " build errors (MUST be 0)"</automated>
  </verify>
  <acceptance_criteria>
    - VAULT's `IDegenerusGamePlayerActions` declares `sellFarFutureTickets(address,uint32[],uint256[],uint256[])` matching the game signature VERBATIM.
    - `gameSellFarFutureTickets(uint32[],uint256[],uint256[]) external onlyVaultOwner` exists and calls `gamePlayer.sellFarFutureTickets(address(this), levels, quantities, queueIndices)`.
    - The wrapper carries `onlyVaultOwner` (VAULT self-calls; the >50.1% DGVE holder gate).
    - The 326-05 VAULT edits (interface `withdraw`/`poolOf` adds + `recoverAfKingPool()`) are preserved.
    - The SUMMARY records the `DroneManager` cross-repo flag (out of this diff) + the OPEN-E disposition confirmation (R6).
    - `forge build` reports 0 errors over both files.
  </acceptance_criteria>
  <done>VAULT has the verbatim interface entry + a `gameSellFarFutureTickets onlyVaultOwner` wrapper calling the game entrypoint; 326-05 VAULT edits preserved; DroneManager/OPEN-E flags recorded; compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| player/operator → sellFarFutureTickets | the entrypoint torches 85-95% of the resolved player's far-inventory value — a value-destructive operator-gated action; the trust is the locked OPEN-E disposition. |
| jitter seed ↔ VRF window | the daily quote derives from a VRF word; using an in-flight word would break the v45 freeze invariant. |
| salvage payout ↔ acquisition cost | if the salvage payout ceiling meets the cheapest acquisition cost, a money pump opens (no-arb break). |
| swap-pop ↔ far-future jackpot samplers | a queue mutation behind a live cursor reproduces H-CANCEL-SWAP-MISS. |
| claimableWinnings[SDGNRS] ↔ redemption desk | the inline debit must not drain the redemption-desk floor. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-326-07-01 | Information Disclosure / Integrity | salvage-swap money pump (no-arb break, SWAP-08) | mitigate | The `fractionBps` curve (15%@d6→5%@d100), the fraction band [70%,110%], and the cash band [20%,60%] are FIXED per the SPEC (re-derived margin +4.5pp at the d6 ceiling: salvage ceiling 16.5% < cheapest acquisition ~21%). The plan FORBIDS widening any band; if implementation reveals a violation it emits `## STOP — NO-ARB MARGIN VIOLATED`. BURNIE (the only token cheap enough to threaten 16.5%) CANNOT mint a far entry (grep-confirmed). The empirical no-arb proof is SWAP-08 at TST. |
| T-326-07-02 | Tampering | freeze-invariant break via the jitter seed (v45) | mitigate | The jitter seed is `rngWordByDay[currentDay-1]` — the SETTLED prior-day word (immutable-once-written, public), NOT the in-flight `rngWordCurrent`; reading it adds NO new mutable SLOAD to the rng window. The swap stays `rngLocked()`-gated. Freeze-safe by construction (grep-verified at §C). Acceptance gate asserts the seed reads `rngWordByDay[currentDay-1]`. |
| T-326-07-03 | Tampering | swap-pop reproduces H-CANCEL-SWAP-MISS | mitigate | The only persistent cursor over a far-future queue is `processFutureTicketBatch`, which runs EXCLUSIVELY inside `rngLockedFlag=true`; the swap reverts while `rngLocked()`, so cursor and swap-pop are mutually exclusive in time. The two far-future samplers are random-access/stateless. The O(1) swap-pop verifies `q[idx]==player` and maintains `membership ⟺ packed != 0`. H-CANCEL-SWAP-MISS proven absent (§D). |
| T-326-07-04 | Repudiation | inline debit drains the redemption-desk floor | mitigate | The `claimableWinnings[SDGNRS]` debit is CHECKED and floored at ≥1 ETH; NO `pendingRedemptionEthValue` term (the redemption desk is protected STRUCTURALLY post-v47). The ticket leg adds pool slack; the cash leg is a claimant-to-claimant relabel. Solvency (`claimablePool ≤ ETH + stETH` never violated) is the SWAP-09 TST proof. |
| T-326-07-05 | Elevation of Privilege | operator torches a player's far inventory (value-destructive) | accept | LOCKED OPEN-E disposition: operator = the same person or a fixed contract the player chose (for a drone, the chain owner's own `onlyChainOwner` manager acting on the owner's own position). Do NOT model a tricked-into-approving actor. The SPEC confirms the disposition EXPLICITLY covers a value-destructive operator call (T-325-S* mitigated, R6). |
| T-326-07-06 | Tampering | stale duplicate `queueIndices` corrupts the queue | mitigate | Duplicate input lines per level are AGGREGATED so a level pops at most once with a valid index; a stale duplicate index hits the `q[idx] != player` verify and reverts (fail-safe, no corruption). |
| T-326-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs. No `## Package Legitimacy Audit` required. |
</threat_model>

<verification>
- `forge build` 0 errors over both files.
- `sellFarFutureTickets` (operator-honor, rngLocked-gated, d∈[6,100], exact fractionBps curve, jitter from `rngWordByDay[currentDay-1]`, `oneTicketWei = priceForLevel(currentLevel)`, ticket floor, inline CHECKED claimable debit to ≥1 ETH, no cap) present.
- `_removeFarFutureTickets` caller-verified swap-pop on full sell-out only; membership⟺packed maintained; samplers untouched.
- VAULT `gameSellFarFutureTickets onlyVaultOwner` wrapper + interface entry; 326-05 VAULT edits preserved.
- The RFALL + KEEP DegenerusGame edits preserved; no band widened; STOP rule honored.
- DroneManager/OPEN-E flags recorded; no git commit performed.
</verification>

<success_criteria>
- ROADMAP SC6 (SWAP-01/02/03/04/05/06/07) satisfied: the `sellFarFutureTickets(...)` entrypoint (`_resolvePlayer` operator-honor, `rngLocked`-gated, mass-sell → ONE current-level ticket mint + ONE cash credit) values each line by `d = L - currentLevel` (require `6 ≤ d ≤ 100`) at `priceForLevel(targetLevel)` with the two-line `fractionBps(d)` curve + daily jitter seeded from the SETTLED past VRF word; ticket floor first (`oneTicketWei = priceForLevel(currentLevel)`, revert if below); funded fail-closed from `claimableWinnings[SDGNRS]` to a ≥1 ETH floor (no `pendingRedemptionEthValue` term, no daily cap, inline); the fully-liquidated seller swap-popped maintaining `membership ⟺ packed != 0`; the VAULT `gameSellFarFutureTickets onlyVaultOwner` wrapper added (DroneManager re-sync flagged out-of-diff).
</success_criteria>

<output>
Create `.planning/phases/326-impl-the-one-batched-contract-diff-all-7-items/326-07-SUMMARY.md` when done. Record the entrypoint signature, the d-curve + jitter-seed source, the unit basis, the inline-debit floor, the swap-pop verify/aggregate logic, the VAULT wrapper, AND the R6 flags (DroneManager cross-repo re-sync out-of-diff + OPEN-E disposition confirmed). If the no-arb margin is violated at any `d`, emit the STOP block instead of a SUMMARY. Do NOT git commit.
</output>
