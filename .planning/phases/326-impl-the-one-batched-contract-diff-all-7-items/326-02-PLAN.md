---
phase: 326-impl-the-one-batched-contract-diff-all-7-items
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/BurnieCoin.sol
  - contracts/modules/DegenerusGameGameOverModule.sol
autonomous: true
requirements: [BTOMB-01, BTOMB-02]
must_haves:
  truths:
    - "gameOver() one-shot bumps BURNIE's virtual VAULT mint allowance (_supply.vaultAllowance) by 1e36 wei via the GAME-gated vaultEscrow allowance-increase path — circulating totalSupply() is untouched."
    - "The flood is guarded by an explicit checked-add / cap so existing vaultAllowance + 1e36 cannot overflow uint128 (1e36 << uint128 max ~3.4e38, ~340x headroom)."
    - "The flood fires exactly once, gated on the gameover-drain (burnAtGameOver invocation in GameOverModule); it cannot be re-triggered."
    - "The 1e36 signal lands only in supplyIncUncirculated() / vaultMintAllowance() / balanceOf(VAULT), never in totalSupply() — a worthless-token tombstone overhang signal."
  artifacts:
    - path: "contracts/BurnieCoin.sol"
      provides: "a GAME-gated one-shot tombstone flood path that bumps vaultAllowance by 1e36 wei with a checked add/cap and a one-shot latch"
      contains: "vaultAllowance"
    - path: "contracts/modules/DegenerusGameGameOverModule.sol"
      provides: "the one-shot call into the BURNIE tombstone flood at the gameover-drain (adjacent to dgnrs.burnAtGameOver())"
      contains: "burnAtGameOver"
  key_links:
    - from: "contracts/modules/DegenerusGameGameOverModule.sol gameover-drain"
      to: "contracts/BurnieCoin.sol tombstone flood path"
      via: "one-shot GAME-gated call at gameOver, alongside dgnrs.burnAtGameOver()"
      pattern: "burnAtGameOver"
---

<objective>
Item 5 (BTOMB) — at `gameOver()`, one-shot flood BURNIE's virtual VAULT mint allowance by 1e36 wei (1 quintillion BURNIE) as a worthless-token tombstone overhang signal. ISOLATED files (`BurnieCoin.sol` + `DegenerusGameGameOverModule.sol`), but it co-hooks the same gameover-drain that item 4 (POOL, plan 326-05) also folds into — a different contract, so a Wave-1 edit here cannot conflict with POOL's sStonk edit. The flood bumps `_supply.vaultAllowance` (NOT circulating `totalSupply()`), so nothing keyed off circulating supply is distorted; the overhang shows up only in the uncirculated views and `balanceOf(VAULT)`.

Purpose: ROADMAP SC5 (BTOMB-01/02) — `gameOver()` one-shot bumps `_supply.vaultAllowance` by 1e36 wei via the existing GAME-gated escrow/allowance path with a checked add/cap (no `uint128` overflow, strictly one-shot).
Output: A GAME-gated one-shot tombstone flood path in `BurnieCoin.sol` (checked add, one-shot latch) + the one-shot call at the GameOverModule gameover-drain.

NON-NEGOTIABLE: This plan EDITS `contracts/*.sol` but MUST NOT commit anything. The whole milestone is ONE batched diff committed only after the final USER-APPROVAL gate (plan 326-08). NOTHING here is pre-approved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-BTOMB-HERO.md
@.planning/PLAN-V48-GAMEOVER-BURNIE-TOMBSTONE.md

<grep_protocol>
Baseline `da5c9d50` (live tree byte-identical). Re-grepped at plan time: `BurnieCoin.sol` Supply struct `:172-175`, ctor seed `:179-180`, `totalSupply()` `:256-258`, `supplyIncUncirculated()` `:263-265`, `vaultMintAllowance()` `:270-272`, `_toUint128` `:350-353`, `vaultEscrow` `:557-567` (the GAME-gated clean allowance-increase, `unchecked` add at :564-565); `GameOverModule.sol` `dgnrs.burnAtGameOver()` `:142`, `handleFinalSweep` `:192`. These files are disjoint from every other item's edits except the gameover-drain coordination with POOL (326-05, a DIFFERENT contract). Re-grep `vaultEscrow` / `vaultAllowance` / `burnAtGameOver` before editing. The 325-ATTEST-BTOMB-HERO §A table (B1-B7) + carried correction C4 are source-of-truth.
</grep_protocol>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the GAME-gated one-shot tombstone flood path in BurnieCoin.sol (checked add/cap + one-shot latch)</name>
  <read_first>
    - contracts/BurnieCoin.sol (Supply struct :172-175; ctor seed :179-180; `totalSupply()` :256-258; `supplyIncUncirculated()` :263-265; `vaultMintAllowance()` :270-272; `_toUint128` :350-353; the GAME/VAULT-gated `vaultEscrow` :557-567 with its `unchecked` `_supply.vaultAllowance += amount128` at :564-565; the `:370` reclassification site — read to confirm it is NOT the reuse target per C4)
    - 325-ATTEST-BTOMB-HERO.md §A (B1-B7 anchor table + the exact `vaultEscrow` body + the BTOMB finding) and §2 "BTOMB packing" in 325-SPEC.md
    - PLAN-V48-GAMEOVER-BURNIE-TOMBSTONE.md (the 1e36-wei flood + checked-add/cap + one-shot design)
  </read_first>
  <action>
    Per BTOMB-01/02 + SPEC §2 "BTOMB packing" + carried correction C4 + B7. Add a GAME-gated one-shot entrypoint to `BurnieCoin.sol` that floods `_supply.vaultAllowance` by exactly `1e36` wei (define a named constant, e.g. `BURNIE_TOMBSTONE_WEI = 1e36`, with a NatSpec describing what it IS — a gameover worthless-token overhang signal). The flood MUST: (1) be gated to `msg.sender == ContractAddresses.GAME` (the gameover-drain caller); (2) use a CHECKED add or an explicit cap so `existing vaultAllowance + 1e36` cannot wrap `uint128` — do NOT route through `vaultEscrow`'s `unchecked` block; either add a fresh checked statement (`_supply.vaultAllowance = uint128(uint256(_supply.vaultAllowance) + BURNIE_TOMBSTONE_WEI)` with the sum proven ≤ `type(uint128).max`, or reuse `_toUint128` on the sum) or cap at `type(uint128).max`; (3) be STRICTLY ONE-SHOT — add a `bool` latch (e.g. `tombstoneFlooded`) that is set on first call and reverts/early-returns on any subsequent call. Do NOT touch `totalSupply()` / circulating supply — only `vaultAllowance` moves (so the signal lands in `supplyIncUncirculated()` / `vaultMintAllowance()` / `balanceOf(VAULT)`). Do NOT reuse the `:370` reclassification path (it pairs `+= vaultAllowance` with `-= totalSupply`, which would shrink circulating — wrong). You MAY model the new path on `vaultEscrow`'s gating shape but with the checked add + one-shot latch added. The 1e36 constant is well below 1e38, ~340x headroom under `uint128` max.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -nE "1e36|10\*\*36|1_000_000_000_000_000_000_000_000_000_000_000_000" contracts/BurnieCoin.sol && grep -n "ContractAddresses.GAME" contracts/BurnieCoin.sol | head && timeout 400 forge build 2>&1 | grep -ciE "^error|Error \(" | tr -d "\n"; echo " build errors (MUST be 0)"</automated>
  </verify>
  <acceptance_criteria>
    - A `1e36`-wei tombstone constant exists in `BurnieCoin.sol` (grep finds the literal, named).
    - The flood path is gated to `ContractAddresses.GAME` (grep confirms the gate inside the new function).
    - The add is CHECKED or capped — NOT inside an `unchecked` block; `git diff` shows the new statement is outside any `unchecked {}`.
    - A one-shot latch (`bool`) is set on first call and prevents re-entry (a second call reverts or no-ops).
    - `git diff contracts/BurnieCoin.sol` shows NO change to `totalSupply()` and NO `_supply.totalSupply -=` introduced (circulating untouched).
    - `forge build` reports 0 errors.
  </acceptance_criteria>
  <done>`BurnieCoin.sol` has a GAME-gated, checked-add, one-shot-latched path that bumps `vaultAllowance` by 1e36 wei without touching circulating `totalSupply()`; compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the one-shot flood call at the GameOverModule gameover-drain</name>
  <read_first>
    - contracts/modules/DegenerusGameGameOverModule.sol (`dgnrs.burnAtGameOver();` :142, preceded by `charityGameOver.burnAtGameOver();` :141; `handleFinalSweep()` :192 with the `+30 days` gate :194; the module's existing interface/import block for how it references BURNIE / external contracts)
    - contracts/BurnieCoin.sol (the new tombstone flood entrypoint added in Task 1 — match its exact signature/selector)
    - 325-ATTEST-BTOMB-HERO.md §A (B6 — the `:142` gameover-drain hook) and 325-SPEC.md §3 "GameOverModule coordination"
  </read_first>
  <action>
    Per BTOMB-01/02 + SPEC §3 (GameOverModule coordination). At the gameover-drain in `DegenerusGameGameOverModule.sol` — adjacent to the existing `dgnrs.burnAtGameOver();` (:142) one-shot drain — add ONE call to the BURNIE tombstone flood entrypoint added in Task 1. Reference BURNIE through the module's existing pattern for calling external/sibling contracts (re-grep how the module reaches `dgnrs` / `charityGameOver` and follow that convention — add an interface decl or use the existing `ContractAddresses` reference as the module already does; do NOT invent a new global import style). This site is reached exactly once at `gameOver()`, so the call is structurally one-shot from this caller; the BurnieCoin-side latch (Task 1) is the belt-and-suspenders guarantee. COORDINATION NOTE: item 4 (POOL, plan 326-05) folds its sStonk `burnAtGameOver` pool-recovery into the SAME gameover-drain region — but that edit is in `StakedDegenerusStonk.sol` / `DegenerusVault.sol`, a DIFFERENT contract, and runs in a later wave; the only shared coupling is the gameover-drain ORDERING, not a shared signature, so this edit and POOL's compose cleanly. Place the BURNIE flood call as a discrete statement adjacent to `dgnrs.burnAtGameOver()` so the final batched diff reads as two adjacent gameover-drain hooks.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && git -C /home/zak/Dev/PurgeGame/degenerus-audit diff contracts/modules/DegenerusGameGameOverModule.sol | grep -E "^\+" | grep -iE "burnie|tombstone|vaultAllowance|escrow" && timeout 400 forge build 2>&1 | grep -ciE "^error|Error \(" | tr -d "\n"; echo " build errors (MUST be 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `DegenerusGameGameOverModule.sol` calls the BURNIE tombstone flood entrypoint exactly once, near the `dgnrs.burnAtGameOver();` line.
    - The call uses the module's existing external-contract-reference convention (no new global import pattern invented; grep the module's `dgnrs`/`charityGameOver` access to confirm).
    - `forge build` reports 0 errors over both touched files.
    - `handleFinalSweep` (:192) is byte-unchanged (no second flood hook added there).
  </acceptance_criteria>
  <done>The gameover-drain calls the BURNIE tombstone flood once, adjacent to the existing `burnAtGameOver()` drain, via the module's existing contract-reference convention; both files compile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| GameOverModule → BurnieCoin | only GAME may trigger the flood; the gameover-drain is the single caller. |
| circulating vs uncirculated supply | DGVB pro-rata BURNIE claims key off circulating `totalSupply()` — the flood must NOT touch it, or it would dilute real claims. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-326-02-01 | Elevation of Privilege | unauthorized flood trigger | mitigate | The flood entrypoint is gated `msg.sender == ContractAddresses.GAME`; a non-GAME caller reverts. Only the gameover-drain reaches it. |
| T-326-02-02 | Tampering | uint128 overflow on `existing + 1e36` | mitigate | The add is CHECKED (or capped at `type(uint128).max`), NOT inside `unchecked`. 1e36 wei « `uint128` max (~3.4e38, ~340x headroom); at gameover the existing allowance is bounded (2e24 seed + escrows) so the sum is far below 1e38. An attacker cannot inflate the existing allowance enough to threaten the cap (escrow is itself GAME/VAULT-gated). |
| T-326-02-03 | Tampering | re-trigger / multiple floods | mitigate | A `bool` one-shot latch set on first call reverts/no-ops every subsequent call; the gameover-drain is also structurally one-shot. Defense in depth. |
| T-326-02-04 | Information Disclosure / Integrity | circulating-supply distortion | mitigate | Grep-asserted: the flood touches `vaultAllowance` only; `totalSupply()` and `_supply.totalSupply` are byte-unchanged, so DGVB pro-rata circulating-supply claims are not diluted. The 1e36 signal is confined to `supplyIncUncirculated()` / `vaultMintAllowance()` / `balanceOf(VAULT)`. (The DGVB pro-rata-of-1e36-allowance overflow check is the TST-phase BTOMB-03 proof.) |
| T-326-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs. No `## Package Legitimacy Audit` required. |
</threat_model>

<verification>
- `forge build` 0 errors over both files.
- A named 1e36-wei tombstone constant + a GAME gate + a checked add + a one-shot latch all present in `BurnieCoin.sol`.
- `git diff` shows `totalSupply()` and `_supply.totalSupply` unchanged.
- The GameOverModule calls the flood once at the gameover-drain; `handleFinalSweep` unchanged.
- No git commit performed.
</verification>

<success_criteria>
- ROADMAP SC5 (BTOMB-01/02) satisfied: `gameOver()` one-shot bumps `_supply.vaultAllowance` by 1e36 wei via the GAME-gated escrow/allowance path with a checked add/cap (no `uint128` overflow, strictly one-shot); `totalSupply()` untouched; the signal lands in the uncirculated views.
</success_criteria>

<output>
Create `.planning/phases/326-impl-the-one-batched-contract-diff-all-7-items/326-02-SUMMARY.md` when done. Record the flood entrypoint signature, the checked-add/cap mechanism, the one-shot latch, and the GameOverModule call site. Note that POOL (326-05) co-hooks the gameover-drain in a different contract. Do NOT git commit.
</output>
