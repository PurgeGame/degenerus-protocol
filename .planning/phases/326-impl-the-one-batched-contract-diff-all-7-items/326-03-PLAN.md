---
phase: 326-impl-the-one-batched-contract-diff-all-7-items
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/modules/DegenerusGameDegeneretteModule.sol
autonomous: true
requirements: [HERO-01, HERO-02, HERO-03, HERO-05]
must_haves:
  truths:
    - "Degenerette scoring is S = A + 2*H (7 ordinary axes + 2 if the hero symbol matches), max 9, pay floor S >= 2 — a hero-alone match guarantees S >= 2 (a win)."
    - "_countMatches is replaced by _score(playerTicket, resultTicket, heroQuadrant) returning {0..9}; the standalone EV-neutral hero multiplier apparatus (_applyHeroMultiplier, HERO_BOOST_N*_PACKED, HERO_PENALTY, HERO_SCALE, the matches>=2 && matches<8 carve-out) is net-deleted."
    - "FT_HERO_SHIFT decode + the heroQuadrant >= 4 revert are KEPT (hero stays mandatory + stored; no bet-layout change); S=9 == old M=8 is a jackpot relabel (identical odds)."
    - "_awardDegeneretteDgnrs thresholds re-map to S>=7 and the WWXRP buckets re-map onto the 10-point scale; the payout SHAPE is locked (continuity, S=2 partial refund) and the byte-exact per-N constants are emitted by derive_5_tables.py at TST (Phase 327) — NOT hand-typed here."
    - "The recalibration is payout-SHAPE only and stays write-batch byte-identical to v47's resolveBets (DGAS); HERO-06 daily-hero-symbol jackpot is unaffected (different state, different path)."
  artifacts:
    - path: "contracts/modules/DegenerusGameDegeneretteModule.sol"
      provides: "_score(...) in {0..9} replacing _countMatches; the standalone hero multiplier net-deleted; S=0..7 packed + S=8/S=9 separate per-N constants; thresholds re-mapped to S>=7"
      contains: "_score"
  key_links:
    - from: "contracts/modules/DegenerusGameDegeneretteModule.sol _resolveFullTicketBet"
      to: "_score → _getBasePayoutBps(N, S) → _fullTicketPayout"
      via: "score-on-S dispatch (drop the multiplier branch)"
      pattern: "_getBasePayoutBps"
---

<objective>
Item 6 (HERO) — rescale the Degenerette hero from a standalone EV-neutral multiplier into a 2-point scoring element. ISOLATED to `DegeneretteModule.sol`. Scoring becomes `S = A + 2*H` (max 9, pay floor `S >= 2`): `_countMatches` (8 axes → {0..8}) becomes `_score(playerTicket, resultTicket, heroQuadrant)` = 7 ordinary axes (4 color + 3 non-hero symbol) + `2 if the hero symbol matches` → {0..9}. The standalone hero multiplier apparatus is net-deleted; per-N tables widen to 10 buckets at fixed EV (RTP unchanged); the jackpot is preserved exactly (`S=9 ≡ old M=8` relabel).

Purpose: ROADMAP SC5 (HERO-01/02/03/05) — `_countMatches → _score(...) ∈ {0..9}`, standalone multiplier removed (`_applyHeroMultiplier` / `HERO_BOOST_*` / `HERO_PENALTY` / `HERO_SCALE` + the `M<2`/`M=8` carve-out deleted; `heroQuadrant >= 4` revert + `FT_HERO_SHIFT` decode kept), per-N tables recalibrated to `basePayoutEV = 100 centi-x` across 10 buckets, jackpot preserved (`S=9 ≡ old M=8`; `_awardDegeneretteDgnrs` thresholds re-mapped to `S≥7`). This file is touched by no other item, so it runs in Wave 1.
Output: The `_score`-based scoring/dispatch with the multiplier apparatus removed, the S=8/S=9 separate-`uint256` packing scheme in place, and the threshold/bucket re-maps — with the byte-exact table VALUES left for the `derive_5_tables.py` byte-reproduce gate at TST.

NON-NEGOTIABLE: This plan EDITS `contracts/*.sol` but MUST NOT commit anything. ONE batched diff, committed only after 326-08. NOTHING pre-approved. CRITICAL TABLE-VALUE BOUNDARY: this plan locks the SHAPE + the on-chain layout (S-dispatch, separate S=8/S=9 constants, re-mapped thresholds). The byte-exact 10-bucket per-N payout CONSTANTS are SOLVED by `derive_5_tables.py` at TST (Phase 327) under the Phase-267-style PASS_ALL byte-reproduce gate — do NOT hand-type final constant values here. Use clearly-marked placeholder constants (or the existing v47 values widened mechanically) that the TST gate will overwrite byte-identically; record this handoff explicitly in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-SPEC.md
@.planning/phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-BTOMB-HERO.md
@.planning/PLAN-V48-DEGENERETTE-HERO-2PT-RESCALE.md

<grep_protocol>
Baseline `da5c9d50` (live tree byte-identical). Re-grepped at plan time: `_countMatches` `:932-962` (8 axes: 4 color :943 + 4 symbol :952, called :682), `_applyHeroMultiplier` `:1070-1095`, `HERO_BOOST_N0..N4_PACKED` `:339-343` (N4 at :343 — C5), `HERO_PENALTY` `:344`, `HERO_SCALE` `:345` (the `:331` anchor is its NatSpec, not the decl — C5), the `matches >= 2 && matches < 8` carve-out `:1047-1059`, `FT_HERO_SHIFT` `:323` (decode :637), `heroQuadrant >= 4` revert `:503`, `_fullTicketPayout` `:1008-1059`, `_getBasePayoutBps` `:1104-1119`, the packed M=0..7 tables `:256-260` + separate M=8 `:264-268`, `_awardDegeneretteDgnrs` `:1196` (thresholds `DEGEN_DGNRS_6/7/8_BPS` :203-205, gate `matches >= 6` :723), `_wwxrpBonusBucket` `:966-970` / `_wwxrpFactor` / `WWXRP_FACTORS_N*_PACKED` :283-287, `resolveBets` `:415` (write-batch shape, DGAS). Re-grep each construct before editing. The 325-ATTEST-BTOMB-HERO §B table (H1-H19) + §2 HERO-04 + "S=8/S=9 packing" + carried corrections C5/C6 are source-of-truth.
</grep_protocol>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace _countMatches with _score(...) ∈ {0..9} and rewire the resolve/payout dispatch onto S</name>
  <read_first>
    - contracts/modules/DegenerusGameDegeneretteModule.sol (`_countMatches` :932-962 — 4 color axes :943 + 4 symbol axes :952; `_resolveFullTicketBet` :622, heroQuadrant decode :637, `matches = _countMatches(...)` :682, payout dispatch :685-695, event emit :696-701, award gate :723-724; `_fullTicketPayout` :1008-1059; `_getBasePayoutBps` :1104-1119; `FT_HERO_SHIFT` :323/:637; `heroQuadrant >= 4` revert :503)
    - 325-ATTEST-BTOMB-HERO.md §B (H6-H12, H17, H18) + §2 HERO-04 IMPL instruction in 325-SPEC.md
    - PLAN-V48-DEGENERETTE-HERO-2PT-RESCALE.md §"On-chain shape" + §Decisions 1-3
  </read_first>
  <action>
    Per HERO-01/02 + SPEC §2 HERO-04. Convert `_countMatches(playerTicket, resultTicket)` into `_score(playerTicket, resultTicket, heroQuadrant)` returning a `uint8` in `{0..9}`: count the 7 ORDINARY axes (4 color + 3 non-hero symbol — the heroQuadrant's COLOR stays an ordinary axis; only its SYMBOL axis becomes the hero axis) plus add `2` if the hero symbol matches (so `S = A + 2*H`, max 9, where `A` is the ordinary-axis count 0..7 and `H` is 0 or 1). Update the resolve path `_resolveFullTicketBet` to call `_score(playerTicket, resultTicket, heroQuadrant)` (the heroQuadrant is already decoded at :637 via `FT_HERO_SHIFT` — KEEP that decode). Rename `matches` → the score variable name throughout the resolve/payout/award call edges so the new value flows to `_getBasePayoutBps(N, S)`, `_fullTicketPayout`, the `FullTicketResult` event, and the `_awardDegeneretteDgnrs` gate. KEEP the `heroQuadrant >= 4` revert (:503) — hero stays mandatory. Do NOT change the bet layout / `FT_HERO_SHIFT` encode. Do NOT touch `_countGoldQuadrants` (the N selector, :919) or `_roiBpsFromScore` (the activity curve, :1130) — both UNTOUCHED per D-02/H17/H18. Pay floor `S >= 2`: a score below 2 pays nothing (preserve the existing zero-payout-below-floor behavior, shifted onto the new floor). Update the NatSpec `(0-8)` ranges on the touched functions/event to `(0-9)` (H15 — the off-chain indexer widening is FLAGGED out-of-scope per SPEC §"Out-of-scope flag", not fixed here).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -n "function _score" contracts/modules/DegenerusGameDegeneretteModule.sol && ! grep -q "function _countMatches" contracts/modules/DegenerusGameDegeneretteModule.sol && grep -n "heroQuadrant >= 4" contracts/modules/DegenerusGameDegeneretteModule.sol && grep -n "FT_HERO_SHIFT" contracts/modules/DegenerusGameDegeneretteModule.sol && echo "checks-present"</automated>
  </verify>
  <acceptance_criteria>
    - `_score(...)` exists and `_countMatches` is gone (grep: no `function _countMatches`).
    - `_score` adds `2` for a hero-symbol match (the `+2` / `2 *` term is present in the new function body) and the ordinary-axis count is 7 axes, not 8.
    - The `heroQuadrant >= 4` revert (:503) and the `FT_HERO_SHIFT` decode (:637) are KEPT (grep both present).
    - `_countGoldQuadrants` and `_roiBpsFromScore` are byte-unchanged (`git diff` shows no hunk inside either).
    - The resolve/payout/award/event call edges all consume the `_score` result (no dangling `matches = _countMatches`).
    - NatSpec ranges on the touched constructs read `(0-9)` not `(0-8)`.
  </acceptance_criteria>
  <done>`_score(...) ∈ {0..9}` replaces `_countMatches`; the resolve/payout/award/event edges dispatch on `S`; the hero-mandatory revert + bet-layout decode are kept; N selector + activity curve untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Net-delete the standalone hero multiplier apparatus + lay the S=0..9 packing scheme + re-map thresholds (placeholder values; byte-exact at TST)</name>
  <read_first>
    - contracts/modules/DegenerusGameDegeneretteModule.sol (`_applyHeroMultiplier` :1070-1095; `HERO_BOOST_N0..N4_PACKED` :339-343; `HERO_PENALTY` :344; `HERO_SCALE` :345; the `matches >= 2 && matches < 8` carve-out :1047-1059; the packed M=0..7 tables `QUICK_PLAY_PAYOUTS_N0..N4_PACKED` :256-260 + separate M=8 `QUICK_PLAY_PAYOUT_N0..N4_M8` :264-268; dispatch `_getBasePayoutBps` :1104-1118; `_awardDegeneretteDgnrs` :1196 + `DEGEN_DGNRS_6/7/8_BPS` :203-205; `_wwxrpBonusBucket` :966-970 + `_wwxrpFactor`/`WWXRP_FACTORS_N*_PACKED` :283-287)
    - 325-ATTEST-BTOMB-HERO.md §B (H1-H5, H10-H14, H16) + §2 "S=8/S=9 packing scheme" + HERO-04 D-01/02/03 + carried correction C6 (HERO-06 no-leak) in 325-SPEC.md
    - PLAN-V48-DEGENERETTE-HERO-2PT-RESCALE.md §Decisions 1-3 + §"On-chain shape"
  </read_first>
  <action>
    Per HERO-03/05 + SPEC §2 (HERO-04 + "S=8/S=9 packing"). (A) NET-DELETE the standalone hero multiplier apparatus: `_applyHeroMultiplier` (:1070-1095), the 5 `HERO_BOOST_N*_PACKED` tables (:339-343), `HERO_PENALTY` (:344), `HERO_SCALE` (:345), and the `matches >= 2 && matches < 8` carve-out branch in `_fullTicketPayout` (:1047-1059) — grep-confirm each has no surviving reference after deletion. `_fullTicketPayout` now dispatches purely on `S` (no multiplier branch). (B) PACKING LAYOUT (settled, SPEC §2): keep the `QUICK_PLAY_PAYOUTS_N{N}_PACKED` `uint256` for S=0..7 (8 × 32-bit, dispatch `(packed >> (S*32)) & 0xFFFFFFFF` for `S<8`); hold S=8 AND S=9 as SEPARATE per-N constants (`QUICK_PLAY_PAYOUT_N{N}_S8` + `QUICK_PLAY_PAYOUT_N{N}_S9`, mirroring the existing separate-M=8 pattern), dispatched `if (S >= 9) return ..._S9; if (S == 8) return ..._S8;` ahead of the packed `<8` path. `S=9 ≡ old M=8` reuses the existing jackpot constant's physical value (identical odds — a relabel). (C) RE-MAP thresholds: `_awardDegeneretteDgnrs` keys on `S >= 7` (shift the `DEGEN_DGNRS_6/7/8_BPS` thresholds onto the new scale per D-03 — the bonus-currency rarity preserved at `S≥7`), and the award gate at the resolve call site changes from `matches >= 6` to the new `S >= 7` threshold; re-map `_wwxrpBonusBucket` buckets onto the 10-point scale. (D) TABLE VALUES: do NOT hand-type the byte-exact 10-bucket per-N payout constants — use clearly-marked placeholders (mechanically widen the v47 values into the new layout, or `0`-filled new buckets) that the `derive_5_tables.py` byte-reproduce gate at TST (Phase 327) will overwrite byte-identically. Mark every placeholder constant with a comment that it is TST-derived. (E) DGAS constraint: the recalibration is payout-SHAPE only — keep the `resolveBets` write-batch shape byte-identical (the cross-bet accumulator flushed once); do NOT change the write-batch structure. HERO-06: do NOT touch `dailyHeroWagers` / `_rollHeroSymbol` (the daily-hero-symbol jackpot reads WAGERED symbols, disjoint from per-bet `S` — C6 no-leak).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && ! grep -qE "_applyHeroMultiplier|HERO_BOOST_N|HERO_PENALTY|HERO_SCALE" contracts/modules/DegenerusGameDegeneretteModule.sol && grep -nE "_S8|_S9" contracts/modules/DegenerusGameDegeneretteModule.sol | head && grep -n "S >= 7\|S >= 9\|S == 8" contracts/modules/DegenerusGameDegeneretteModule.sol | head && timeout 400 forge build 2>&1 | grep -ciE "^error|Error \(" | tr -d "\n"; echo " build errors (MUST be 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "_applyHeroMultiplier|HERO_BOOST_N|HERO_PENALTY|HERO_SCALE"` returns NOTHING (the standalone multiplier apparatus is fully net-deleted, in code AND comments).
    - The `matches >= 2 && matches < 8` carve-out branch is gone from `_fullTicketPayout`.
    - Separate `QUICK_PLAY_PAYOUT_N{N}_S8` + `QUICK_PLAY_PAYOUT_N{N}_S9` per-N constants exist, and `_getBasePayoutBps` dispatches `if (S >= 9) ...; if (S == 8) ...;` ahead of the packed `S<8` path.
    - `_awardDegeneretteDgnrs` / its gate keys on `S >= 7` (no surviving `matches >= 6` gate on the award path).
    - Every byte-exact payout table value carries a comment flagging it as TST-derived (placeholder, to be byte-reproduced by `derive_5_tables.py`); the SUMMARY records this handoff.
    - `dailyHeroWagers` / `_rollHeroSymbol` are byte-unchanged (`git diff` shows no hunk; HERO-06 no-leak).
    - The `resolveBets` write-batch structure is unchanged (DGAS) — `git diff` shows no change to the cross-bet accumulator/flush shape.
    - `forge build` reports 0 errors.
  </acceptance_criteria>
  <done>The standalone multiplier apparatus is net-deleted; the S=0..7-packed + S=8/S=9-separate layout dispatches on `S`; thresholds re-map to `S≥7`; table VALUES are TST-handoff placeholders; the write-batch shape + daily-hero state are untouched; compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| per-bet score → payout table | the rescale must hold RTP exactly (fixed EV budget = 100 centi-x per pick) — a mis-calibrated table is a house-edge / player-windfall vector. |
| per-bet `S` vs daily-hero jackpot | `dailyHeroWagers` reads wagered symbols, not scores — the `matches` 0-8→0-9 widening must not leak into the daily-hero jackpot. |
| on-chain table values vs derive script | the byte-exact constants are the security-load-bearing surface; they are gated by the TST byte-reproduce PASS_ALL, never hand-typed. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-326-03-01 | Information Disclosure / Integrity | mis-calibrated payout table inflates RTP (player windfall) or deflates it (house over-edge) | mitigate | This plan locks SHAPE + layout only; the byte-exact per-N constants are emitted by `derive_5_tables.py` under the Phase-267-style PASS_ALL byte-reproduce gate at TST (HERO-04). Placeholder values here are explicitly flagged TST-derived; the IMPL build need not be RTP-correct, the TST gate enforces it. |
| T-326-03-02 | Tampering | `matches`-range widening leaks into the daily-hero-symbol jackpot | mitigate | `dailyHeroWagers`/`_rollHeroSymbol` read the WAGERED hero symbol pool (written from the bet's chosen symbol), disjoint from per-bet `_score` — grep-confirmed unchanged (C6/H19 no-leak). |
| T-326-03-03 | Tampering | jackpot odds drift on the S=9 relabel | mitigate | `S=9 ≡ old M=8` reuses the existing jackpot constant's physical value — identical odds (a relabel, not a re-weight). The byte-reproduce gate confirms the jackpot constant is byte-identical at TST. |
| T-326-03-04 | Denial of Service | gas regression from the write-batch shape change | mitigate | The recalibration is payout-SHAPE only; the `resolveBets` write-batch (cross-bet accumulator flushed once) is byte-identical to v47 (DGAS) — grep-confirmed unchanged. |
| T-326-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs. No `## Package Legitimacy Audit` required. |
</threat_model>

<verification>
- `forge build` 0 errors.
- `_score(...) ∈ {0..9}` replaces `_countMatches`; the multiplier apparatus is fully net-deleted (grep clean).
- Separate S=8/S=9 per-N constants + the S-dispatch present; thresholds re-map to `S≥7`.
- Table values flagged TST-derived (byte-reproduce-gate handoff recorded).
- `dailyHeroWagers`/`_rollHeroSymbol`, `_countGoldQuadrants`, `_roiBpsFromScore`, and the `resolveBets` write-batch shape are byte-unchanged.
- No git commit performed.
</verification>

<success_criteria>
- ROADMAP SC5 (HERO-01/02/03/05) satisfied: scoring `S = A + 2*H` (max 9, floor `S≥2`); `_countMatches → _score(...) ∈ {0..9}`; the standalone EV-neutral hero multiplier removed; per-N tables widened to 10 buckets (values byte-reproduced at TST); jackpot preserved (`S=9 ≡ old M=8`; `_awardDegeneretteDgnrs` thresholds re-mapped to `S≥7`).
</success_criteria>

<output>
Create `.planning/phases/326-impl-the-one-batched-contract-diff-all-7-items/326-03-SUMMARY.md` when done. Record: the `_score` formula, the deleted multiplier symbols, the S=8/S=9 packing layout, the re-mapped thresholds, AND an EXPLICIT note that the byte-exact 10-bucket per-N payout constants are placeholders to be byte-reproduced by `derive_5_tables.py` under the PASS_ALL gate at Phase 327 (HERO-04). Confirm the daily-hero state + write-batch shape are untouched. Do NOT git commit.
</output>
