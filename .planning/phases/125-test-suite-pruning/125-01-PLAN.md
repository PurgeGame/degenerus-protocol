---
phase: 125-test-suite-pruning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/125-test-suite-pruning/REDUNDANCY-AUDIT.md
  - test/poc/Coercion.test.js
  - test/poc/NationState.test.js
  - test/poc/Phase24_FormalMethods.test.js
  - test/poc/Phase25_DependencyIntegration.test.js
  - test/poc/Phase26_GasGriefing.test.js
  - test/poc/Phase27_WhiteHat.test.js
  - test/poc/Phase28_GameTheory.test.js
  - test/adversarial/EconomicAdversarial.test.js
  - test/adversarial/SepoliaActorAdversarial.test.js
  - test/adversarial/TechnicalAdversarial.test.js
  - test/simulation/simulation-2-levels.test.js
  - test/simulation/simulation-5-levels.test.js
  - test/validation/PaperParity.test.js
  - test/validation/SimContractParity.test.js
autonomous: true
requirements: [PRUNE-01, PRUNE-02]

must_haves:
  truths:
    - "Every test file across both suites has been analyzed for redundancy"
    - "Each deletion candidate has a documented justification referencing what covers the same ground"
    - "Redundant tests are deleted from disk"
    - "No test covering a unique contract line or behavior is deleted"
  artifacts:
    - path: ".planning/phases/125-test-suite-pruning/REDUNDANCY-AUDIT.md"
      provides: "Complete redundancy analysis with per-file verdicts and justifications"
      contains: "Verdict"
    - path: "test/poc/"
      provides: "Directory should be empty or deleted (all 7 files are ghost tests excluded from runner)"
  key_links:
    - from: "REDUNDANCY-AUDIT.md"
      to: "git rm commands"
      via: "deletion justifications drive which files are removed"
      pattern: "DELETE"
---

<objective>
Perform full redundancy audit across all 90 test files (47 Hardhat + 43 Foundry) and delete redundant tests.

Purpose: Identify and remove tests that duplicate coverage already provided by other tests, reducing maintenance burden without losing unique coverage. The poc/ directory (7 files) is already excluded from the Hardhat test runner and is the highest-confidence deletion target. The adversarial/, simulation/, and validation/ categories from v2.0-v3.x milestones likely overlap with the more thorough unit/ tests and Foundry invariant/ tests written in later milestones.
Output: REDUNDANCY-AUDIT.md document with per-file verdicts, and all DELETE-verdicted files removed from disk.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-test-suite-pruning/125-CONTEXT.md
@.planning/phases/120-test-suite-cleanup/COVERAGE-BASELINE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redundancy audit across all 90 test files</name>
  <read_first>
    - .planning/phases/120-test-suite-cleanup/COVERAGE-BASELINE.md (baseline counts: 369 Foundry, 1242 Hardhat)
    - .planning/phases/125-test-suite-pruning/125-CONTEXT.md (D-01 full sweep, D-02 same coverage = delete less thorough, D-03 Claude discretion on preservation)
    - hardhat.config.js (TEST_DIR_ORDER -- confirms poc/ is excluded from runner)
    - test/poc/Coercion.test.js (sample poc test to understand what they cover)
    - test/poc/Phase24_FormalMethods.test.js (sample poc test)
    - test/poc/Phase28_GameTheory.test.js (sample poc test -- largest at 382 lines)
    - test/adversarial/EconomicAdversarial.test.js (adversarial test -- likely overlaps unit/)
    - test/adversarial/SepoliaActorAdversarial.test.js (adversarial test -- largest at 498 lines)
    - test/adversarial/TechnicalAdversarial.test.js (adversarial test)
    - test/simulation/simulation-2-levels.test.js (simulation test)
    - test/simulation/simulation-5-levels.test.js (simulation test)
    - test/validation/PaperParity.test.js (validation test -- largest at 1102 lines)
    - test/validation/SimContractParity.test.js (validation test)
    - test/unit/SecurityEconHardening.test.js (unit test that likely supersedes adversarial/ coverage)
    - test/unit/EthInvariant.test.js (unit test that likely supersedes some poc/ coverage)
    - test/unit/BurnieCoinflip.test.js (unit test to compare with adversarial coinflip tests)
    - test/unit/DegenerusGame.test.js (core game unit test)
    - test/unit/AffiliateHardening.test.js (unit test to compare with adversarial affiliate tests)
    - test/edge/WhaleBundle.test.js (edge test to compare with adversarial whale tests)
    - test/edge/GameOver.test.js (edge test to compare with poc gameover tests)
    - test/integration/GameLifecycle.test.js (integration test to compare with simulation/)
    - test/fuzz/invariant/EthSolvency.inv.t.sol (Foundry invariant to compare with EthInvariant.test.js)
    - test/fuzz/invariant/WhaleSybil.inv.t.sol (Foundry invariant to compare with adversarial whale tests)
    - test/fuzz/invariant/DegeneretteBet.inv.t.sol (Foundry invariant to compare with adversarial bet tests)
    - test/fuzz/BurnieCoinInvariants.t.sol (Foundry fuzz to compare with BurnieCoin unit tests)
  </read_first>
  <files>.planning/phases/125-test-suite-pruning/REDUNDANCY-AUDIT.md</files>
  <action>
    Analyze all 90 test files to produce REDUNDANCY-AUDIT.md. The audit must cover three dimensions per D-01:

    **Dimension 1: Ghost tests (already excluded from runner)**
    The 7 poc/ test files are NOT listed in hardhat.config.js TEST_DIR_ORDER (which lists: access, deploy, unit, integration, edge, validation, gas, adversarial, simulation). These files exist on disk but never run. Verdict: DELETE all 7 with justification "ghost test -- excluded from Hardhat runner, never executed."

    **Dimension 2: Cross-suite duplicates (Hardhat vs Foundry)**
    For each Hardhat test file, check if a Foundry file covers the same contract behaviors with fuzz/invariant testing. Key comparison pairs:
    - test/unit/EthInvariant.test.js vs test/fuzz/invariant/EthSolvency.inv.t.sol
    - test/unit/BurnieCoin.test.js vs test/fuzz/BurnieCoinInvariants.t.sol
    - test/unit/DGNRSLiquid.test.js vs test/fuzz/ShareMathInvariants.t.sol
    - test/unit/DegenerusVault.test.js vs test/fuzz/invariant/VaultShare.inv.t.sol + VaultShareMath.inv.t.sol
    - test/edge/RngStall.test.js vs test/fuzz/StallResilience.t.sol + VRFStallEdgeCases.t.sol
    - test/edge/WhaleBundle.test.js vs test/fuzz/invariant/WhaleSybil.inv.t.sol
    - test/edge/CompressedJackpot.test.js vs test/fuzz/JackpotCombinedPool.t.sol
    - test/integration/VRFIntegration.test.js vs test/fuzz/VRFCore.t.sol + VRFLifecycle.t.sol + VRFPathCoverage.t.sol
    When the Foundry fuzz/invariant version covers the same behaviors with 1000+ input runs, the Hardhat single-path test is redundant per D-02.

    **Dimension 3: Within-suite overlaps (Hardhat internal)**
    Check adversarial/ vs unit/:
    - EconomicAdversarial.test.js -- do its scenarios (whale attacks, price manipulation, front-running) overlap with SecurityEconHardening.test.js and edge/ tests?
    - SepoliaActorAdversarial.test.js -- does it test Sepolia-specific behaviors that unit tests already cover?
    - TechnicalAdversarial.test.js -- does it test reentrancy/overflow scenarios already covered by unit tests?

    Check simulation/ vs integration/:
    - simulation-2-levels.test.js vs GameLifecycle.test.js -- do both simulate multi-level game progression?
    - simulation-5-levels.test.js -- does it add unique coverage beyond simulation-2-levels?

    Check validation/ content:
    - PaperParity.test.js (1102 lines) -- what does it validate? If it checks that contract outputs match paper/spec values, are those values also tested in unit tests?
    - SimContractParity.test.js -- does it compare simulation vs contract results? Is this superseded by unit tests?

    **Verdict Format:**
    For each of the 90 files, assign exactly one verdict:
    - KEEP -- unique coverage, no equivalent test elsewhere
    - DELETE -- coverage fully duplicated by [specific file(s)]
    - KEEP (borderline) -- some overlap but retains unique edge cases worth keeping

    **Output document structure for REDUNDANCY-AUDIT.md:**

    ```
    # Redundancy Audit -- Phase 125

    Generated: [date]
    Scope: 90 test files (47 Hardhat, 43 Foundry)

    ## Summary

    | Verdict | Count | Files |
    |---------|-------|-------|
    | DELETE | N | [list] |
    | KEEP | N | [list] |
    | KEEP (borderline) | N | [list] |

    ## Ghost Tests (poc/ -- excluded from runner)

    | File | Lines | Verdict | Justification |
    |------|-------|---------|---------------|
    [7 rows, all DELETE]

    ## Cross-Suite Duplicates

    | Hardhat File | Foundry Equivalent | Verdict | Justification |
    |---|---|---|---|
    [rows for each Hardhat file with a Foundry equivalent]

    ## Within-Suite Overlaps

    | File | Overlaps With | Verdict | Justification |
    |---|---|---|---|
    [rows for adversarial/, simulation/, validation/]

    ## All Files -- Final Verdicts

    ### Foundry (test/fuzz/)
    [One row per .t.sol file]

    ### Hardhat (test/)
    [One row per .test.js file]

    ## Deletion Manifest

    Files to delete:
    - [path] -- [one-line reason]
    ```

    IMPORTANT: When two tests cover the same ground, always keep the one that is more thorough per D-02. Foundry fuzz tests with 1000 runs beat Hardhat single-path tests. Unit tests that test more edge cases beat adversarial tests that test fewer. Do NOT merge assertions between tests -- keep the better test, delete the other (D-02).

    IMPORTANT: Do NOT delete deploy/ tests (DeployScript.test.js), access/ tests (AccessControl.test.js), gas/ tests (AdvanceGameGas.test.js), or integration/ tests (CharityGameHooks.test.js, GameLifecycle.test.js) unless you have overwhelming evidence they are fully redundant. These categories serve distinct purposes (deploy verification, access control matrix, gas benchmarks, integration flows) that are unlikely to be covered elsewhere.

    IMPORTANT: Foundry invariant/ tests (12 files) and halmos/ tests (4 files) are the highest-value tests. Do NOT consider them for deletion.
  </action>
  <verify>
    <automated>test -f /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/125-test-suite-pruning/REDUNDANCY-AUDIT.md && grep -c "DELETE" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/125-test-suite-pruning/REDUNDANCY-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/125-test-suite-pruning/REDUNDANCY-AUDIT.md exists
    - REDUNDANCY-AUDIT.md contains "## Ghost Tests" section with 7 poc/ files all marked DELETE
    - REDUNDANCY-AUDIT.md contains "## Cross-Suite Duplicates" section
    - REDUNDANCY-AUDIT.md contains "## Within-Suite Overlaps" section
    - REDUNDANCY-AUDIT.md contains "## All Files -- Final Verdicts" section with a row for every .t.sol and .test.js file
    - REDUNDANCY-AUDIT.md contains "## Deletion Manifest" section listing all DELETE files with reasons
    - Every DELETE verdict has a justification naming the specific file(s) that provide equivalent coverage
    - The 12 Foundry invariant/ files and 4 halmos/ files all have KEEP verdicts
    - DeployScript.test.js, AccessControl.test.js, AdvanceGameGas.test.js have KEEP verdicts (unless overwhelming evidence of redundancy is documented)
  </acceptance_criteria>
  <done>REDUNDANCY-AUDIT.md exists with per-file verdicts for all 90 test files, each DELETE has a named justification (PRUNE-01).</done>
</task>

<task type="auto">
  <name>Task 2: Delete redundant test files per audit verdicts</name>
  <read_first>
    - .planning/phases/125-test-suite-pruning/REDUNDANCY-AUDIT.md (the Deletion Manifest section -- this is the authoritative list of what to delete)
    - hardhat.config.js (TEST_DIR_ORDER -- confirm poc/ is not listed, verify if any other deleted dirs need removal from ordering)
  </read_first>
  <files>
    All files listed in the Deletion Manifest of REDUNDANCY-AUDIT.md
  </files>
  <action>
    Execute deletions from the REDUNDANCY-AUDIT.md Deletion Manifest using `git rm` for each file.

    **Step 1: Delete all files from the Deletion Manifest**
    Run `git rm {filepath}` for each file listed in the Deletion Manifest section of REDUNDANCY-AUDIT.md.

    **Step 2: Clean up empty directories**
    If all files in a directory have been deleted (e.g., test/poc/), remove the empty directory with `rmdir`.

    **Step 3: Update hardhat.config.js TEST_DIR_ORDER if needed**
    If an entire directory category is deleted AND it appears in TEST_DIR_ORDER (lines 23-33 of hardhat.config.js), remove it from the array. Currently TEST_DIR_ORDER contains: access, deploy, unit, integration, edge, validation, gas, adversarial, simulation.
    - If poc/ is deleted: no config change needed (poc/ is NOT in TEST_DIR_ORDER already)
    - If adversarial/ is fully deleted: remove "adversarial" from TEST_DIR_ORDER
    - If simulation/ is fully deleted: remove "simulation" from TEST_DIR_ORDER
    - If validation/ is fully deleted: remove "validation" from TEST_DIR_ORDER

    **Step 4: Quick smoke test**
    Run `npx hardhat test 2>&1 | tail -5` to verify the remaining tests still run. This does NOT need to pass 100% (that is Plan 02's job) -- just needs to not crash from missing imports or broken test ordering.

    IMPORTANT: Only delete files that appear in the Deletion Manifest. Do NOT delete any file that received a KEEP or KEEP (borderline) verdict.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && for f in test/poc/Coercion.test.js test/poc/NationState.test.js test/poc/Phase24_FormalMethods.test.js test/poc/Phase25_DependencyIntegration.test.js test/poc/Phase26_GasGriefing.test.js test/poc/Phase27_WhiteHat.test.js test/poc/Phase28_GameTheory.test.js; do test ! -f "$f" && echo "DELETED: $f" || echo "STILL EXISTS: $f"; done</automated>
  </verify>
  <acceptance_criteria>
    - All 7 poc/ test files no longer exist on disk
    - Every file listed in REDUNDANCY-AUDIT.md Deletion Manifest no longer exists on disk
    - No file with a KEEP verdict has been deleted (spot check: test/unit/DegenerusGame.test.js exists, test/deploy/DeployScript.test.js exists, test/access/AccessControl.test.js exists)
    - If test/poc/ directory is empty, it no longer exists
    - hardhat.config.js TEST_DIR_ORDER does not reference any fully-deleted directory
    - `npx hardhat test` does not crash with "file not found" or import errors (exit code may be non-zero for other reasons, but no missing-file crashes)
  </acceptance_criteria>
  <done>All DELETE-verdicted test files removed from disk, empty directories cleaned up, hardhat.config.js updated if needed (PRUNE-02).</done>
</task>

</tasks>

<verification>
1. REDUNDANCY-AUDIT.md exists with verdicts for all 90 files
2. All DELETE-verdicted files removed from disk
3. No KEEP-verdicted file removed
4. poc/ directory cleaned up (ghost tests deleted)
5. hardhat.config.js TEST_DIR_ORDER consistent with remaining directories
</verification>

<success_criteria>
- PRUNE-01: REDUNDANCY-AUDIT.md identifies every duplicate-coverage test with justification
- PRUNE-02: All redundant tests deleted per the audit verdicts
- No test covering unique behavior has been deleted
</success_criteria>

<output>
After completion, create `.planning/phases/125-test-suite-pruning/125-01-SUMMARY.md`
</output>
