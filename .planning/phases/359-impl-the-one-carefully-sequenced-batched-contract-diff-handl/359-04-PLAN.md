---
phase: 359-impl-the-one-carefully-sequenced-batched-contract-diff-handl
plan: 04
type: execute
wave: 4
depends_on: ["359-01", "359-02", "359-03"]
files_modified:
  - contracts/storage/DegenerusGameStorage.sol
  - contracts/DegenerusGame.sol
  - contracts/DegenerusQuests.sol
  - contracts/modules/DegenerusGameAdvanceModule.sol
  - contracts/modules/GameAfkingModule.sol
  - contracts/modules/DegenerusGameLootboxModule.sol
  - contracts/modules/DegenerusGameBoonModule.sol
  - contracts/modules/DegenerusGameDecimatorModule.sol
  - contracts/modules/DegenerusGameDegeneretteModule.sol
  - contracts/modules/DegenerusGameMintModule.sol
  - contracts/modules/DegenerusGameMintStreakUtils.sol
  - contracts/modules/DegenerusGameGameOverModule.sol
  - contracts/BurnieCoinflip.sol
  - test/fuzz
autonomous: false
requirements: [UDVT-01, UDVT-02, UDVT-03]
must_haves:
  truths:
    - "A type Day is uint24 UDVT with operator overloads replaces the raw day-counter integers across the ~649-line/27-contract day-bearing surface (incl. the just-added feature code — no raw-int islands)"
    - "The 3 abi.encodePacked day RNG-entropy sites cast Day to uint32 via uint32(Day.unwrap(d)) so the keccak preimage byte-image is preserved bit-for-bit"
    - "Packed Sub/struct day fields stay uint24-backed (no cold-slot spill — the v56 gas win); standalone day slots + indexed day event topics stay raw uint32; the rngWordByDay mapping KEY stays uint32"
    - "The post-UDVT forge build is green (CHECKPOINT 2) with the forge .t.sol day-signature churn included"
    - "The full single batched contracts diff is reviewed by the USER and committed only after explicit approval (the HARD STOP)"
  artifacts:
    - path: "contracts/storage/DegenerusGameStorage.sol"
      provides: "the type Day is uint24 declaration + global operator overloads + the day-field retypes (packed stay uint24-backed)"
      contains: "type Day is uint24"
    - path: "contracts/modules/DegenerusGameAdvanceModule.sol"
      provides: "the 2 RNG encodePacked sites cast Day to uint32 (:1405, :1828)"
      contains: "uint32(Day.unwrap"
    - path: "contracts/DegenerusGame.sol"
      provides: "the 3rd RNG encodePacked site cast Day to uint32 (:1011)"
      contains: "uint32(Day.unwrap"
  key_links:
    - from: "the day-bearing surface"
      to: "type Day is uint24 + using for Day global"
      via: "wrap/unwrap at boundaries; operator overloads for day comparisons"
      pattern: "type Day is uint24"
    - from: "the 3 abi.encodePacked day entropy sites"
      to: "uint32(Day.unwrap(d)) in the keccak preimage"
      via: "explicit uint32 cast preserves the 4-byte preimage image"
      pattern: "uint32\\(Day.unwrap"
---

<objective>
THE HEAVY ITEM + THE FINAL CONTRACT-COMMIT GATE. After all 7 behavior features are authored + the post-features build is green (plans 01–03), run ONE mechanical type Day is uint24 UDVT sweep over the whole day-bearing surface (SPEC D-01 features-first/UDVT-last; D-04 the sweep retypes the new feature code too — the tree ends consistent type Day with NO raw-int islands), preserving the RNG byte-image + the storage layout/gas BYTE-FOR-BYTE per the SPEC's locked D-19 discipline:

- UDVT-01: introduce type Day is uint24 + global operator overloads (the final set grepped from real day comparisons, from the locked set lt/le/eq/mod/add/sub); replace the raw day-counter integers across the ~649-line / 27-contract day-bearing surface (heaviest: DegenerusQuests / GameAfkingModule / DegenerusGameStorage / DegenerusGameAdvanceModule / DegenerusGameBoonModule / DegenerusGameLootboxModule / BurnieCoinflip).
- UDVT-02 (LOAD-BEARING): the 3 abi.encodePacked day RNG-entropy sites cast Day to uint32 via uint32(Day.unwrap(d)) — DegenerusGameAdvanceModule.sol:1405 + :1828, DegenerusGame.sol:1011 — so the keccak preimage stays 4 bytes (an unwrapped uint24-backed Day shortens it to 3 and changes the derived word). The rngWordByDay mapping KEY stays uint32 (DegenerusGameStorage.sol:454).
- UDVT-03: packed Sub/struct day fields become Day (uint24-backed) on the SAME slots (no cold-slot spill — the v56 gas win); standalone day slots + indexed day event topics stay raw uint32 (cast at the Day boundaries).

Then: CHECKPOINT 2 (post-UDVT forge build green — the forge .t.sol day-signature churn lands HERE per SPEC D-05), and the FINAL autonomous:false contract-commit hand-review gate — the USER reviews the whole single batched git diff -- contracts/ and approves the ONE contract commit (the milestone's one HARD STOP).

Purpose: complete the type-safety refactor byte-preservingly; reach the clean compiled batched diff; HOLD for USER hand-review.
Output: the consistent type Day tree, a green post-UDVT forge build, and the single batched contract commit (only after USER approval). The ~143 Hardhat JS test updates land as SEPARATE agent-committable commits (SPEC D-20).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

## Execution mode (HARD constraints)
- Sequential-on-main, worktrees OFF. Author directly on the working tree, strictly AFTER plans 01–03 (this sweep RE-TOUCHES every feature file from plans 01–03 + DegenerusGameStorage + DegenerusGame — it depends on all of them; SPEC D-04).
- CONTRACT-COMMIT BOUNDARY = THE HARD STOP. This plan reaches the green post-UDVT build, then the FINAL autonomous:false gate: the USER reviews git diff -- contracts/ (the ONE batched diff = plans 01–04) and approves the SINGLE contract commit. NEVER auto-commit contracts/*.sol. (Project rule: only contract commits need approval.) ContractAddresses.sol is freely modifiable (regenerated by build).
- Code-comment style (USER rule): comments explain HOW THE CODE WORKS + real invariants in plain English. CUT all procedural meta (plan IDs, req tags, spec-line citations, milestone history, USER-accepted/LOCKED). The type Day introduction needs a tight functional comment on WHY the 3 encodePacked sites cast to uint32 (the byte-image invariant) — in plain English, NO spec citations. Never change logic when trimming.
- Frozen-subject guard was clean at phase start. The UDVT is a PURE annotation/explicit-cast pass with ZERO intended behavior change — if a day comparison RESULT would change, that is a bug, not a refactor.
- Two SPEC D-03 checkpoints: checkpoint 1 (post-features) already landed at end of plan 03; checkpoint 2 (post-UDVT) is Task 5 here. Do NOT add per-file compiles between.
- solc 0.8.34 / paris — UDVT + global using for Day global operator overloads supported (since 0.8.19). GREENFIELD: there is ZERO existing UDVT in contracts/ — build from the SPEC D-19 matrix + the solc language feature, NOT an in-repo analog.
</execution_context>

<context>
@.planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md
@.planning/phases/359-impl-the-one-carefully-sequenced-batched-contract-diff-handl/359-CONTEXT.md
@.planning/phases/359-impl-the-one-carefully-sequenced-batched-contract-diff-handl/359-PATTERNS.md

<interfaces>
The load-bearing UDVT byte-preservation matrix (SPEC D-19), greenfield — no in-repo analog. Extracted at 1e7a646d.

The 3 freeze-critical abi.encodePacked day entropy sites — cast Day to uint32 (verbatim at 1e7a646d):
  DegenerusGameAdvanceModule.sol:1404-1406:  word = uint256(keccak256(abi.encodePacked(combined, currentDay, block.prevrandao)));  currentDay to uint32
  DegenerusGameAdvanceModule.sol:1827-1829:  uint256 derivedWord = uint256(keccak256(abi.encodePacked(vrfWord, gapDay)));        gapDay to uint32
  DegenerusGame.sol:1011:                    rngWord = uint256(keccak256(abi.encodePacked(day, address(this))));                  day to uint32
  At each site use uint32(Day.unwrap(d)) so the preimage stays 4 bytes (a uint24-backed Day would shorten it 4 to 3 and change the derived word). LOAD-BEARING (T-358-15).

The rngWordByDay mapping KEY — STAYS uint32 (DegenerusGameStorage.sol:454): mapping(uint32 => uint256) internal rngWordByDay;
  (a uint24-backed Day and a uint32 cast both zero-pad to the same 32-byte slot — leave the key uint32, cast at the access boundary; same for the _quoteFarFutureSwap key read + _backfillGapDays writes.)

Packed uint24 day fields -> become Day (uint24-backed), SAME slots (the v56 gas win):
  Sub.afkingStartDay:1944 (uint24); the boon-day bitfield (DegenerusGameStorage.sol:1615-1632): coinflipDay/deityCoinflipDay/lootboxBoostDay/deityLootboxDay/purchaseDay/deityPurchaseDay/decimatorDay/deityDecimatorDay/whaleDay/deityWhaleDay/activityDay (all packed uint24).
  NOTE: frozenUntilLevel / validThroughLevel are LEVELS not days — do NOT retype. The WWXRP bracket key (level/10) is a LEVEL — do NOT retype. The Sub.affiliateBase/pendingBurnie are uint32 ACCUMULATORS — not days, do NOT retype.

Standalone uint32 day slots + indexed event topics -> STAY raw uint32, cast at Day boundaries:
  purchaseStartDay:223, dailyIdx, deityBoonDay:1441, deityBoonRecipientDay:1447.

Heaviest day-bearing surfaces (by day mention count, exact per-site count produced at IMPL):
  DegenerusQuests.sol (252), DegenerusGameAdvanceModule.sol (197), DegenerusGameStorage.sol (165), GameAfkingModule.sol (138), DegenerusGameLootboxModule.sol (132), BurnieCoinflip.sol (105), DegenerusGameBoonModule.sol (61). Design estimate ~649 lines / 27 contracts.

Operator overloads: grep the actual day comparisons to finalize the set from lt/le/eq/mod/add/sub. solc 0.8.34 supports using fn as op for Day global.

Forge-test churn (D-05): 45 of 68 test/fuzz/*.t.sol files reference day-bearing calls — their day-typed signatures MUST be updated to reach the green post-UDVT forge build (they land WITH this sweep). The ~143 Hardhat JS test updates do NOT affect forge build (separate agent-committable commits, SPEC D-20).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: UDVT-01 — declare type Day is uint24 + finalize the operator-overload set</name>
  <files>contracts/storage/DegenerusGameStorage.sol</files>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol — the top-of-file type/using declarations region + :454 (the rngWordByDay mapping KEY, stays uint32) + :1615-1632 (the packed boon-day bitfield) + the Sub struct (afkingStartDay:1944).
    - .planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md — UDVT Width/Byte-Preservation Discipline section, items 5 (operator overloads, finalize from real day comparisons) and 6 (solc 0.8.34 confirmed).
    - .planning/phases/359-impl-the-one-carefully-sequenced-batched-contract-diff-handl/359-PATTERNS.md No Analog Found (GREENFIELD — build from the SPEC matrix + solc language feature) + the Operator overloads note.
    - Run a grep of the real day comparisons across the day-bearing surface to FINALIZE the overload set (Claude's discretion within the locked list).
  </read_first>
  <action>
    In contracts/storage/DegenerusGameStorage.sol (or the shared types location it pulls from): declare type Day is uint24; and the free wrap/unwrap plus the operator-overload functions, then bind them with using for Day global (governing SPEC UDVT-01 / D-19 item 5). FIRST grep the actual day comparisons across the day-bearing surface to determine the EXACT operator set needed (from the locked set lt/le/eq/mod/add/sub — include only what real comparisons use; YOUR discretion within that list). Author the overload functions (a pure function that unwraps both operands and applies the native operator) and bind each as the corresponding operator for Day globally. Keep the rngWordByDay mapping KEY as mapping(uint32 to uint256) (:454) — do NOT change it (a Day and a uint32 zero-pad to the same slot; cast at the access boundary). Add a tight functional comment explaining the type + the byte-preservation invariant (the 3 RNG sites cast to uint32) in plain English — no spec citations. This task ESTABLISHES the type; the per-surface retypes are Tasks 2–4.
  </action>
  <verify>
    <automated>grep -c "type Day is uint24" contracts/storage/DegenerusGameStorage.sol; grep -c "for Day global" contracts/storage/DegenerusGameStorage.sol</automated>
  </verify>
  <acceptance_criteria>
    - type Day is uint24 is declared with wrap/unwrap and the operator overloads bound via using for Day global.
    - The operator set is the one actually used by real day comparisons (a subset of the locked list), grepped at IMPL.
    - The rngWordByDay mapping KEY remains mapping(uint32 to uint256) (:454) — unchanged.
    - A plain-English comment explains the type + the uint32-cast byte-preservation invariant (no spec/plan citations).
  </acceptance_criteria>
  <done>type Day is uint24 + the grep-finalized operator overloads are declared globally; the rngWordByDay key stays uint32.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: UDVT-02 (LOAD-BEARING) — cast Day to uint32 at the 3 RNG encodePacked entropy sites</name>
  <files>contracts/modules/DegenerusGameAdvanceModule.sol, contracts/DegenerusGame.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameAdvanceModule.sol:1404-1406 (abi.encodePacked combined/currentDay/prevrandao) + :1827-1829 (abi.encodePacked vrfWord/gapDay) + the freeze spine context (_applyDailyRng:1879, _backfillGapDays:1817).
    - contracts/DegenerusGame.sol:1011 (abi.encodePacked day/address(this)).
    - .planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md — UDVT Discipline item 1 (the 3 sites cast Day to uint32; the pure annotation premise is FALSE here) + Cross-Cutting RNG-Freeze item 5 (the load-bearing per-site byte-diff gate, owned empirically at TST 361) + TDEC-03 (the freeze spine — these are the entropy sites the whole milestone protects).
    - .planning/phases/359-impl-the-one-carefully-sequenced-batched-contract-diff-handl/359-PATTERNS.md No Analog Found, The 3 freeze-critical abi.encodePacked day entropy sites.
  </read_first>
  <action>
    At EACH of the 3 RNG entropy sites (governing SPEC UDVT-02 / D-19 item 1 — the milestone's load-bearing freeze gate), wherever the day operand is now a Day, cast it uint32(Day.unwrap(d)) INSIDE the abi.encodePacked(...) so the keccak preimage stays exactly 4 bytes for that operand — byte-identical to the current raw-uint32 encoding: (1) DegenerusGameAdvanceModule.sol:1405 — currentDay to uint32(Day.unwrap(currentDay)); (2) DegenerusGameAdvanceModule.sol:1828 — gapDay to uint32(Day.unwrap(gapDay)); (3) DegenerusGame.sol:1011 — day to uint32(Day.unwrap(day)). Do NOT unwrap to a narrower or wider type; do NOT use the bare Day value in the preimage (a uint24-backed Day would shorten the preimage 4 to 3 bytes and change the derived word). If a given operand is still a raw uint32 (not yet retyped to Day), leave it raw uint32 (already correct). Add a tight one-line functional comment at each site: the explicit uint32 cast preserves the keccak preimage byte-image (plain English, no spec citation). Change NOTHING else at these sites — the surrounding entropy logic + the rngWordByDay writes stay byte-identical.
  </action>
  <verify>
    <automated>grep -c "uint32(Day.unwrap" contracts/modules/DegenerusGameAdvanceModule.sol; grep -c "uint32(Day.unwrap" contracts/DegenerusGame.sol</automated>
  </verify>
  <acceptance_criteria>
    - All 3 RNG encodePacked sites encode the day operand as uint32(Day.unwrap(...)) (or a raw uint32 if that operand was not retyped) — the preimage stays 4 bytes per operand.
    - DegenerusGameAdvanceModule.sol has at least 2 uint32(Day.unwrap sites (:1405, :1828); DegenerusGame.sol has the third (:1011).
    - No bare Day value appears inside any RNG abi.encodePacked preimage.
    - Each site has a plain-English byte-image-preservation comment; no other change at the sites.
  </acceptance_criteria>
  <done>The 3 RNG entropy sites cast Day to uint32 so the keccak preimage byte-image is preserved bit-for-bit.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: UDVT-01/03 — retype the heavy storage + advance/quests/afking/gameover surface (packed stay uint24, standalone stay uint32)</name>
  <files>contracts/storage/DegenerusGameStorage.sol, contracts/modules/DegenerusGameAdvanceModule.sol, contracts/DegenerusQuests.sol, contracts/modules/GameAfkingModule.sol, contracts/modules/DegenerusGameGameOverModule.sol</files>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol:223 (purchaseStartDay — standalone uint32, STAYS uint32), :454 (rngWordByDay key STAYS uint32), :1441/:1447 (deityBoonDay/deityBoonRecipientDay — standalone uint32, STAY uint32), :1615-1632 (the packed boon-day bitfield — uint24, becomes Day uint24-backed), the Sub struct (afkingStartDay:1944 uint24 to Day; affiliateBase/pendingBurnie are NOT days), _livenessTriggered:1231-1240, _simulatedDayIndex.
    - contracts/modules/DegenerusGameAdvanceModule.sol (the 197 day mentions; the freeze spine helpers — but NOT the encodePacked operands, done in Task 2).
    - contracts/DegenerusQuests.sol (the 252 day mentions; getPlayerQuestView day fields; the lastProgressDay/streak-day logic).
    - contracts/modules/GameAfkingModule.sol (the 138 day mentions; Sub.afkingStartDay reads, _finalizeAfking day args).
    - contracts/modules/DegenerusGameGameOverModule.sol (the gameOverDay / rngWordByDay[day] read at :106 — the key access boundary, cast Day to uint32 if day is now Day).
    - .planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md — UDVT Discipline items 2 (packed stay uint24-backed, same slots), 3 (standalone + indexed stay raw uint32), 4 (rngWordByDay KEY unchanged).
    - .planning/phases/359-impl-the-one-carefully-sequenced-batched-contract-diff-handl/359-PATTERNS.md No Analog Found — the packed/standalone/indexed matrix + the do-NOT-retype list (levels, accumulators).
  </read_first>
  <action>
    Mechanically retype the day-counter integers to Day across this heavy surface (governing SPEC UDVT-01 + UDVT-03 / D-19 items 2/3/4), preserving layout BYTE-FOR-BYTE:
    - PACKED day fields become Day (uint24-backed) on the SAME slots — Sub.afkingStartDay:1944 + the packed boon-day bitfield :1615-1632 (coinflipDay/deityCoinflipDay/lootboxBoostDay/deityLootboxDay/purchaseDay/deityPurchaseDay/decimatorDay/deityDecimatorDay/whaleDay/deityWhaleDay/activityDay). A type Day is uint24 occupies the same 3 bytes — NO field is pushed to a new cold slot (verify the struct/packing layout is unchanged).
    - STANDALONE uint32 day slots (purchaseStartDay:223, deityBoonDay:1441, deityBoonRecipientDay:1447, dailyIdx) STAY raw uint32; cast at the Day boundaries (wrap/unwrap) where they meet Day-typed code.
    - The rngWordByDay mapping KEY (:454) STAYS uint32; cast Day to uint32 at every key-access boundary (e.g. the GameOverModule:106 read, the AdvanceModule writes, the _quoteFarFutureSwap read). This is read/write access only — the encodePacked PREIMAGE casts were done in Task 2.
    - DO NOT retype: frozenUntilLevel / validThroughLevel (LEVELS), the WWXRP level/10 bracket key (a LEVEL), Sub.affiliateBase / pendingBurnie (uint32 ACCUMULATORS).
    - Wrap/unwrap at the boundaries with external/raw-int code so the tree compiles; use the operator overloads for day comparisons. ZERO behavior change — every day comparison RESULT must be identical. Keep comments lean. Use the operator overloads declared in Task 1; if a needed operator is missing from the set, add it in Task 1's declaration (keep the set minimal — only what real comparisons use).
  </action>
  <verify>
    <automated>grep -c "mapping(uint32 => uint256) internal rngWordByDay" contracts/storage/DegenerusGameStorage.sol; grep -c "validThroughLevel\|frozenUntilLevel" contracts/storage/DegenerusGameStorage.sol</automated>
  </verify>
  <acceptance_criteria>
    - The packed Sub/boon-day fields are Day (uint24-backed) on the SAME slots — no struct/packing layout change (no new cold slot).
    - Standalone day slots (purchaseStartDay, deityBoonDay, deityBoonRecipientDay, dailyIdx) stay raw uint32; the rngWordByDay KEY stays mapping(uint32 to uint256).
    - frozenUntilLevel/validThroughLevel + the level/10 bracket key + the Sub accumulators are NOT retyped to Day.
    - Day comparisons use the operator overloads; wrap/unwrap appears only at boundaries; zero behavior change.
  </acceptance_criteria>
  <done>The heavy storage + advance/quests/afking/gameover surface is consistently Day-typed with packed fields uint24-backed on the same slots and standalone/key as uint32.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: UDVT-01 — retype the remaining day surface (incl. the new feature code) + the forge .t.sol day-signature churn</name>
  <files>contracts/modules/DegenerusGameLootboxModule.sol, contracts/modules/DegenerusGameBoonModule.sol, contracts/modules/DegenerusGameDecimatorModule.sol, contracts/modules/DegenerusGameDegeneretteModule.sol, contracts/modules/DegenerusGameMintModule.sol, contracts/modules/DegenerusGameMintStreakUtils.sol, contracts/BurnieCoinflip.sol, test/fuzz</files>
  <read_first>
    - contracts/modules/DegenerusGameLootboxModule.sol (132 day mentions) + contracts/modules/DegenerusGameBoonModule.sol (61) + contracts/BurnieCoinflip.sol (105) — the remaining heavy day surfaces.
    - The just-added feature code from plans 01–03 that touches days: DegenerusGameMintStreakUtils._quoteFarFutureSwap (the rngWordByDay[_simulatedDayIndex()-1] read), DegenerusGameMintModule (salvage day reads), DegenerusGameDecimatorModule.boostTerminalDecimator (the daysRemaining / day helpers), GameAfkingModule (cancel day fields), DegenerusGameDegeneretteModule (any day reads — note level/10 is a LEVEL, not a day).
    - test/fuzz/*.t.sol — the 45 of 68 files that call day-bearing contract signatures (grep for the day-typed calls that will now fail to compile against the Day-typed signatures).
    - .planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md — UDVT Discipline (the full matrix) + D-20 (test-file handling: forge .t.sol churn lands WITH the contract sweep to reach the green build; the ~143 Hardhat JS updates are SEPARATE agent-committable commits).
    - .planning/phases/359-impl-the-one-carefully-sequenced-batched-contract-diff-handl/359-PATTERNS.md Forge-test churn (D-05) note (45/68 files).
  </read_first>
  <action>
    Finish the UDVT sweep (governing SPEC UDVT-01 / D-04 — the new feature code is retyped too, NO raw-int islands):
    - Retype the remaining day surface in LootboxModule / BoonModule / BurnieCoinflip + any day-typed code in the plans-01–03 feature hunks (MintStreakUtils salvage day read, MintModule salvage day reads, DecimatorModule.boostTerminalDecimator day helpers, GameAfkingModule cancel day fields). Apply the SAME matrix: packed uint24 day fields become Day on the same slots; standalone uint32 + indexed day topics stay uint32; the rngWordByDay key access casts Day to uint32. Do NOT retype the WWXRP level/10 bracket key (a LEVEL). The new boostTerminalDecimator daysRemaining logic uses the day helpers + overloads.
    - FORGE TEST CHURN (D-05): update the day-typed signatures in the affected test/fuzz/*.t.sol files (the ~45 that call day-bearing contract functions) so they compile against the now-Day-typed contract signatures — wrap raw-int test literals to Day at the call boundaries (Day.wrap(uint24(n))) or pass uint32 where a key/standalone day is expected. These are AGENT-committable (separate from the contract commit) but MUST be present to reach the green post-UDVT forge build. Do NOT change any test ASSERTION or behavior — only the day-typed signature/literal plumbing.
    - Do NOT touch the ~143 Hardhat JS test files here (they do not affect forge build; SPEC D-20 — separate agent-committable commits, deferred to after the contract commit).
    Keep all comments lean + functional. ZERO behavior change anywhere.
  </action>
  <verify>
    <automated>grep -rl "Day.wrap" test/fuzz 2>/dev/null | wc -l; grep -rc "Day" contracts/modules/DegenerusGameLootboxModule.sol contracts/BurnieCoinflip.sol 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - The remaining day surface (LootboxModule, BoonModule, BurnieCoinflip) + the day-typed code in the plans-01–03 feature hunks are retyped to Day per the matrix — NO raw-int day islands remain.
    - The WWXRP level/10 bracket key is NOT retyped to Day.
    - The affected forge .t.sol files compile against the Day-typed signatures (raw-int literals wrapped at the call boundaries); no test assertion/behavior changed.
    - The ~143 Hardhat JS test files are untouched in this task.
  </acceptance_criteria>
  <done>The whole day surface (incl. new feature code) is consistently Day-typed; the forge .t.sol day-signature churn is in place so the post-UDVT build can be green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: CHECKPOINT 2 — post-UDVT forge build green [BLOCKING]</name>
  <action>Run the post-UDVT `forge build` (SPEC D-03 checkpoint 2) and confirm it exits 0 with the full UDVT sweep + the forge .t.sol day-signature churn present; confirm ContractAddresses.sol/the forge fixture is intact and that the rngWordByDay KEY type, levels, and the level/10 bracket key are NOT day-retyped. This is a verification gate, not a code-authoring task — see how-to-verify for the steps.</action>
  <what-built>
    The full UDVT sweep is complete (type Day is uint24 + the 3 uint32-cast RNG sites + the packed/standalone/key matrix + the new feature code retyped + the forge .t.sol day-signature churn). This is the SECOND of the two SPEC D-03 forge build checkpoints — the whole batched contracts/ diff (plans 01–04) now compiles.
  </what-built>
  <how-to-verify>
    1. Run the post-UDVT compile: forge build. It MUST exit 0 (green). Foundry compiles test/*.t.sol in the same build — the forge .t.sol day-signature churn (Task 4) must already be present, so the build passes WITH the test churn included.
    2. WARNING on the build artifact: if forge regenerates ContractAddresses.sol or breaks the forge fixture, restore it (a known landmine — hardhat compile --force regenerates ContractAddresses.sol; do not let that mask a real failure).
    3. Confirm the diff scope vs the frozen subject: git diff --stat 1e7a646d -- contracts/ shows the full feature set + the day-type churn across the ~27-contract day surface, and NO change to the rngWordByDay mapping KEY type, frozenUntilLevel/validThroughLevel, or the level/10 bracket key.
    4. Confirm NO commit yet — git status shows the contract files modified but unstaged/uncommitted (the commit is the next task, gated on USER approval).
  </how-to-verify>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -8; echo "EXIT=${PIPESTATUS[0]}"</automated>
  </verify>
  <acceptance_criteria>
    - forge build exits 0 (green) with the full UDVT sweep + the forge .t.sol day-signature churn present.
    - ContractAddresses.sol / the forge fixture is intact (the regenerate landmine did not mask a failure).
    - The rngWordByDay KEY type, frozenUntilLevel/validThroughLevel, and the level/10 bracket key are NOT day-retyped in the diff.
    - No contracts/*.sol commit has been made yet.
  </acceptance_criteria>
  <resume-signal>Type "approved" once forge build is green post-UDVT, or describe the compile error so the sweep/test-churn can be fixed.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 6: THE HARD STOP — USER hand-review + single batched contract commit [BLOCKING]</name>
  <action>Present the full `git diff 1e7a646d -- contracts/` to the USER (behavior hunks first, then the UDVT churn), re-attest the 5 load-bearing invariants, and WAIT for explicit approval. Only on approval: make exactly ONE batched contracts/*.sol commit; commit the ~143 Hardhat JS test updates separately afterward. If changes are requested, do not commit — route back to the relevant plan's hunk. This is the contract-commit boundary HARD STOP — see how-to-verify for the steps.</action>
  <what-built>
    The ONE carefully-sequenced batched contracts/*.sol diff for v57.0 is complete and compiling (plans 01–04): BATCH-01/02 + BURNIE-01/02 + SALVAGE-01 + WWXRP-01 + TDEC-01 + CANCEL-01 + the wide UDVT refactor (UDVT-01/02/03). This is the milestone's ONE contract-commit boundary HARD STOP — per the project rule "only contract commits need approval", the USER hand-reviews the whole diff before any contracts/*.sol is committed.
  </what-built>
  <how-to-verify>
    1. Produce the review artifact: git diff 1e7a646d -- contracts/ (the single batched diff). The by-severity/by-file authoring order (BATCH -> BURNIE -> SALVAGE -> WWXRP/TDEC/CANCEL -> UDVT) gives a natural review grouping — surface the behavior hunks (the 7 features) SEPARATELY from the mechanical UDVT churn so the USER can review the highest-risk behavior (the Critical BURNIE fix + the 2 functional-solvency exceptions) first.
    2. Re-attest the load-bearing invariants in the diff for the reviewer: (a) the 3 abi.encodePacked day sites encode uint32(Day.unwrap(...)) — RNG byte-image preserved; (b) the ETH/claimablePool DEBIT code is byte-unchanged across all items (the BURNIE fix restores ticket claims; SALVAGE relabels only the ETH part out of SDGNRS); (c) the packed Sub/struct day fields stay uint24-backed (no cold-slot spill); (d) the TDEC boost gate is exactly require(!_livenessTriggered()); (e) the rngWordByDay KEY + levels + accumulators are not day-retyped.
    3. WAIT for explicit USER approval. Do NOT commit contracts/*.sol without it.
    4. On approval ONLY: stage and commit the contracts/*.sol diff (the single batched contract commit). ContractAddresses.sol may be included if the build regenerated it. Use a lean commit message describing the bundle (no plan IDs in the Solidity, but the commit message may summarize the 11 REQ-IDs). The ~143 Hardhat JS test updates (SPEC D-20) are committed SEPARATELY as agent-committable commits AFTER the contract commit — they do not need approval.
    5. If the USER requests changes: do NOT commit; record the requested changes and route back to the relevant plan's hunk (the by-severity grouping makes the target obvious), then re-reach the green build and re-present.
  </how-to-verify>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && git diff --stat 1e7a646d -- contracts/ | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The full git diff 1e7a646d -- contracts/ is presented, grouped behavior-hunks-first then UDVT churn.
    - The 5 load-bearing invariants are re-attested in the review (RNG uint32 casts, ETH/pool DEBIT byte-unchanged, packed uint24, TDEC !liveness gate, no day-retype of key/levels/accumulators).
    - NO contracts/*.sol commit is made before explicit USER approval.
    - On approval: exactly ONE batched contracts/*.sol commit is made; the Hardhat JS test updates are committed separately afterward (or noted as deferred to a follow-up agent commit).
  </acceptance_criteria>
  <resume-signal>USER reviews git diff 1e7a646d -- contracts/ and types "approved" to authorize the single batched contract commit, or requests changes (which route back to the relevant plan's hunk).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| day-counter values -> the 3 abi.encodePacked RNG entropy sites | the freeze spine: the keccak preimage byte-image MUST be preserved (the load-bearing UDVT-02 gate) |
| Day-typed code <-> rngWordByDay mapping KEY (uint32) | the key-access boundary: a Day and a uint32 must map to the same slot (cast at the boundary) |
| Day-typed contract signatures <-> forge .t.sol callers | the test-compile boundary (D-05): test day literals wrapped to Day to reach the green build |
| authored contracts/*.sol -> the contract commit | the HARD STOP — only contract commits need USER approval |

## STRIDE Threat Register (mapped to the SPEC's discharged register)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-358-15 | Tampering (RNG/freeze) — LOAD-BEARING | the 3 abi.encodePacked day entropy sites | mitigate | Cast Day to uint32 via uint32(Day.unwrap(d)) so the keccak preimage stays 4 bytes (a bare uint24-backed Day shortens it 4->3 and changes the derived word). SPEC UDVT-02 / RNG-Freeze item 5. Per-site byte-diff gate proven EMPIRICALLY at TST 361 SEC-01. |
| T-358-16 | Tampering (gas/storage layout) | packed Sub/struct day fields | mitigate | type Day is uint24 occupies the same 3 bytes -> no cold-slot spill -> the v56 packed-Sub gas win intact. SPEC UDVT-03 item 2. Proven GAS-NEUTRAL at 360. |
| T-358-UDVT-KEY | Tampering (storage) | rngWordByDay mapping KEY | accept | KEY stays mapping(uint32 to uint256); a uint24-backed Day and a uint32 cast zero-pad to the same 32-byte slot -> every stored daily word's address is byte-preserved. SPEC UDVT-02 item 4. |
| T-358-UDVT-INDEXED | Information disclosure (off-chain) | indexed day event topics | accept | indexed day topics stay raw uint32 so the topic byte-image is unchanged for off-chain consumers. SPEC UDVT-03 item 3. |
| T-359-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (Solidity + forge-test edits + a forge build only); slopcheck N/A. |

Defer EMPIRICAL proof to TST 361 (SEC-01 the per-site byte-diff gate — the derived rngWord byte-identical at every entropy site after the uint32 casts; UDVT-03 the NON-WIDENING regression) and the GAS-neutrality measurement at 360 (packed Sub fields stay uint24, no cold-slot spill) + the delta-audit at TERMINAL 362. Do not invent new mitigations — cite the SPEC's.
</threat_model>

<verification>
- type Day is uint24 + the grepped operator overloads declared globally; rngWordByDay KEY stays uint32.
- The 3 RNG encodePacked sites cast Day to uint32 (uint32(Day.unwrap(...))) — preimage byte-image preserved.
- Packed Sub/struct day fields uint24-backed on the same slots; standalone + indexed day topics stay uint32; levels/accumulators/level-10-bracket not retyped.
- The new feature code is retyped too (no raw-int islands); the forge .t.sol day-signature churn is present.
- CHECKPOINT 2: forge build exits 0 post-UDVT (with the test churn); ContractAddresses.sol intact.
- THE HARD STOP: the full git diff 1e7a646d -- contracts/ is reviewed by the USER; exactly ONE batched contract commit is made ONLY after explicit approval; the Hardhat JS updates are committed separately.
</verification>

<success_criteria>
- UDVT-01: the type + overloads replace the raw day-counter integers across the day-bearing surface, including the new feature code (no raw-int islands).
- UDVT-02: the 3 RNG encodePacked sites cast Day to uint32 (byte-image preserved); the rngWordByDay KEY stays uint32.
- UDVT-03: packed day fields uint24-backed (no cold-slot spill); standalone + indexed day topics stay uint32.
- CHECKPOINT 2: the post-UDVT forge build is green (SPEC D-03 second checkpoint), forge .t.sol churn included.
- THE HARD STOP: the single batched contracts/*.sol diff is committed ONLY after explicit USER hand-review approval; the Hardhat JS test updates land as separate agent-committable commits.
</success_criteria>

<output>
Create `.planning/phases/359-impl-the-one-carefully-sequenced-batched-contract-diff-handl/359-04-SUMMARY.md` when done. Record: the final operator-overload set chosen; the exact 3 encodePacked uint32-cast sites; the packed-vs-standalone-vs-key retype decisions (incl. the do-NOT-retype list applied); the forge .t.sol files touched for the day-signature churn; the post-UDVT forge build result (CHECKPOINT 2 green); the USER hand-review outcome + the single batched contract commit SHA (or "held — awaiting approval" / "changes requested" if not yet committed); and the status of the deferred ~143 Hardhat JS test updates (separate agent commits).
</output>
