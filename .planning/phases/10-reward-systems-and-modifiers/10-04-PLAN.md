---
phase: 10-reward-systems-and-modifiers
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v1.1-steth-yield.md
autonomous: true
requirements:
  - STETH-01
  - STETH-02

must_haves:
  truths:
    - "Agent can compute yield surplus using the exact formula: (ETH balance + stETH balance) - (current + next + claimable + future pools)"
    - "Agent understands stETH yield is passive surplus with no automatic distribution mechanism"
    - "Agent knows the payout ordering: player claims use ETH-first, vault/DGNRS claims use stETH-first"
    - "Agent understands admin staking constraint: cannot stake below claimablePool reserve"
  artifacts:
    - path: "audit/v1.1-steth-yield.md"
      provides: "stETH integration mechanics, yield surplus formula, admin operations, payout ordering"
      contains: "yieldPoolView"
  key_links:
    - from: "audit/v1.1-steth-yield.md"
      to: "contracts/DegenerusGame.sol"
      via: "stETH admin functions and yieldPoolView formula"
      pattern: "adminStakeEthForStEth|adminSwapEthForStEth|yieldPoolView"
    - from: "audit/v1.1-steth-yield.md"
      to: "contracts/DegenerusStonk.sol"
      via: "DGNRS burn includes stETH in proportional payout"
      pattern: "depositSteth|steth"
---

<objective>
Document stETH (Lido staked ETH) integration: how the game contract holds and stakes ETH via Lido, how yield surplus is computed, admin staking/rebalancing operations, payout ordering between ETH and stETH, and how stETH affects DGNRS burn payouts.

Purpose: Enable game theory agents to account for stETH yield in economic models and understand that yield is a passive surplus without explicit distribution.

Output: `audit/v1.1-steth-yield.md`
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-reward-systems-and-modifiers/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stETH yield integration reference document</name>
  <files>audit/v1.1-steth-yield.md</files>
  <action>
Create `audit/v1.1-steth-yield.md` following the established document pattern.

**Document structure:**

1. **Overview** -- The game contract holds both ETH and stETH (Lido rebasing staked ETH). Admin can stake excess ETH via Lido for yield. stETH balance increases over time through Lido staking rewards (rebasing). The yield surplus is implicit -- no explicit pool or distribution mechanism exists.

2. **stETH Integration Mechanics (STETH-01)** -- How stETH enters the system:
   - adminStakeEthForStEth: ADMIN-only function, stakes ETH via Lido submit()
   - Constraint: cannot stake below claimablePool reserve (must preserve player claim obligations)
   - adminSwapEthForStEth: ADMIN-only, value-neutral rebalancing (msg.value == amount)
   - Include exact Solidity from DegenerusGame.sol:1775-1817 for both functions
   - Document the Lido submit() interface and how stETH is received

3. **Yield Surplus Formula (STETH-02)** -- How yield accrual is measured:
   - yieldPoolView formula: (address(this).balance + steth.balanceOf(this)) - (currentPrizePool + nextPrizePool + claimablePool + futurePrizePool)
   - Include exact Solidity from DegenerusGame.sol:2129-2141
   - Yield is the difference between total holdings and total obligations
   - No automatic distribution: admin can rebalance, but yield just accumulates

   > **CRITICAL PITFALL: Yield is Passive Surplus.** There is no "yield pool" that gets distributed. Treating stETH yield as an explicit distribution mechanism is wrong. Yield simply increases total balance. Any surplus above obligations can be rebalanced by admin but there is no automated yield routing.

4. **Payout Ordering (STETH-02 continued)** -- Two payout strategies:
   - Player claims (claimWinnings): ETH-first, stETH fallback for remainder
   - Vault/DGNRS claims (claimWinningsStethFirst): stETH-first, ETH fallback
   - Include exact Solidity for both payout paths
   - Document the retry/fallback logic

5. **stETH in DGNRS Burns (STETH-02 continued)** -- How burn payouts include stETH:
   - Game calls dgnrs.depositSteth(amount) via _transferSteth
   - Uses steth.approve + depositSteth pattern
   - Burn returns proportional ETH + stETH + BURNIE
   - ETH preferred over stETH in ordering

6. **stETH Rounding Pitfall** -- Lido known issue:
   - 1-2 wei rounding on stETH transfers due to shares-to-balance conversion
   - Game handles implicitly through fallback/retry payout logic
   - Agents should not expect exact wei precision in stETH amounts

7. **Constants Reference Table** -- Every constant and function reference with source file and line number.

**Source contracts to reference:**
- contracts/DegenerusGame.sol lines 1775-1817 (admin staking functions)
- contracts/DegenerusGame.sol lines 2129-2141 (yieldPoolView)
- contracts/DegenerusStonk.sol (depositSteth, burn with stETH)

**Style:** Follow exact formatting pattern of audit/v1.1-burnie-coinflip.md. This will be a shorter document given the simpler mechanics, but maintain the same structural rigor.
  </action>
  <verify>
    <automated>grep -cE "STETH-0[1-2]|yieldPoolView|adminStakeEthForStEth|claimablePool|stETH.*first|passive.*surplus" audit/v1.1-steth-yield.md</automated>
  </verify>
  <done>Document exists with: (1) admin staking functions with claimablePool constraint, (2) exact yieldPoolView formula, (3) passive surplus pitfall prominently documented, (4) dual payout ordering (ETH-first vs stETH-first), (5) DGNRS burn stETH inclusion, (6) rounding pitfall, (7) constants reference table</done>
</task>

</tasks>

<verification>
- yieldPoolView formula matches DegenerusGame.sol:2129-2141 exactly
- Admin functions match DegenerusGame.sol:1775-1817 exactly
- Payout ordering correctly distinguishes player vs vault/DGNRS paths
- Every function reference has source file and line number
</verification>

<success_criteria>
A game theory agent reading only this document can: (1) compute yield surplus at any point given pool balances, (2) understand that yield has no automatic distribution, (3) predict payout composition (ETH vs stETH) for different claim types, (4) account for stETH in DGNRS burn value calculations.
</success_criteria>

<output>
After completion, create `.planning/phases/10-reward-systems-and-modifiers/10-04-SUMMARY.md`
</output>
