---
phase: 279-whole-burnie-floor-bur
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/modules/DegenerusGameLootboxModule.sol
  - contracts/modules/DegenerusGameJackpotModule.sol
autonomous: false
requirements:
  - BUR-01
  - BUR-02
  - BUR-03
  - BUR-04
  - BUR-05

must_haves:
  truths:
    - "`_resolveLootboxCommon` floors the post-bonus `burnieAmount` accumulator to a whole-BURNIE multiple via `(burnieAmount / 1 ether) * 1 ether` before the `if (burnieAmount != 0)` guard; the floored value reaches `coinflip.creditFlip`, the `LootBoxOpened.burnie` event field, and the function return tuple."
    - "`_awardDailyCoinToTraitWinners` computes `baseAmount` as `((coinBudget / cap) / 1 ether) * 1 ether`; the `extra` and `cursor` locals and both `++cursor`/wrap blocks and the `amount += 1` cursor-rotation line are fully deleted; `amount` is assigned only `baseAmount`; `randomWord` is still consumed by the keccak level/winner draws."
    - "`_awardFarFutureCoinJackpot` computes `perWinner` as `((farBudget / found) / 1 ether) * 1 ether` and the existing `if (perWinner == 0) return` early-bail is unchanged and sits immediately after the floored divide."
    - "The OUT-OF-SCOPE ticket-award cursor-rotation in `DegenerusGameJackpotModule.sol` near `:1003` (the `extra`/`cursor` block with `++cursor`/wrap at `:1020`-`:1021` and the adjacent `JackpotTicketWin` emit) is NOT touched."
    - "Storage layout for both `DegenerusGameLootboxModule.sol` and `DegenerusGameJackpotModule.sol` is byte-identical to v39 baseline `6a7455d1`; zero new state variables, events, emit sites, modifiers, admin entry points, or external mutation entry points."
    - "The contract compiles and the batched 2-file diff is committed in exactly one user-approved commit carrying a storage-layout byte-identity proof and a gas worst-case + bytecode delta in the message body."
  artifacts:
    - path: "contracts/modules/DegenerusGameLootboxModule.sol"
      provides: "BUR-01 whole-BURNIE floor on the _resolveLootboxCommon burnieAmount accumulator"
      contains: "burnieAmount = (burnieAmount / 1 ether) * 1 ether"
    - path: "contracts/modules/DegenerusGameJackpotModule.sol"
      provides: "BUR-02 baseAmount floor + extra/cursor dead-var full removal in _awardDailyCoinToTraitWinners; BUR-03 perWinner floor in _awardFarFutureCoinJackpot"
      contains: "((coinBudget / cap) / 1 ether) * 1 ether"
    - path: ".planning/phases/279-whole-burnie-floor-bur/279-01-STORAGE-LAYOUT-DIFF.md"
      provides: "BUR-04 storage-layout byte-identity proof vs 6a7455d1 for both modified modules"
      contains: "6a7455d1"
    - path: ".planning/phases/279-whole-burnie-floor-bur/279-01-GAS-WORSTCASE.md"
      provides: "BUR-05 theoretical worst-case gas derivation + measured bytecode delta"
      contains: "worst-case"
  key_links:
    - from: "_resolveLootboxCommon burnieAmount floor"
      to: "coinflip.creditFlip + LootBoxOpened.burnie + return tuple"
      via: "in-place reassignment of burnieAmount after the bonus block, before the != 0 guard"
      pattern: "burnieAmount = \\(burnieAmount / 1 ether\\) \\* 1 ether"
    - from: "_awardDailyCoinToTraitWinners baseAmount"
      to: "coinflip.creditFlip(winner, amount) inside the existing winner != address(0) && amount != 0 guard"
      via: "amount = baseAmount with no cursor-rotation +1"
      pattern: "\\(\\(coinBudget / cap\\) / 1 ether\\) \\* 1 ether"
    - from: "_awardFarFutureCoinJackpot perWinner floor"
      to: "if (perWinner == 0) return early-bail"
      via: "floor applied before the existing == 0 check so post-floor-zero bails for free"
      pattern: "\\(\\(farBudget / found\\) / 1 ether\\) \\* 1 ether"
---

<objective>
Land the full Phase 279 whole-BURNIE-floor contract surface in one batched, user-approved commit: apply the inline `(x / 1 ether) * 1 ether` integer-division floor at the 3 RNG-amount-driven BURNIE-award sites — `_resolveLootboxCommon`'s `burnieAmount` accumulator (BUR-01), `_awardDailyCoinToTraitWinners`'s `baseAmount` (BUR-02), `_awardFarFutureCoinJackpot`'s `perWinner` (BUR-03) — and fully remove the now-dead `extra`/`cursor` cursor-rotation machinery in `_awardDailyCoinToTraitWinners` (BUR-02 dead-var removal). Prove storage-layout byte-identity vs v39 baseline `6a7455d1` for both modified modules (BUR-04) and report the theoretical worst-case gas path + measured bytecode delta in the commit message (BUR-05).

Purpose: Realize the A1 floor-per-winner mechanic (`D-40N-BUR-FLOOR-01`) so every RNG-driven BURNIE award is a whole-BURNIE multiple (1 BURNIE = 1 ether), with sub-1-BURNIE residues evaporating silently (`D-40N-BUR-DUST-01`, `D-40N-BUR-SILENT-01`) — a pure-amount transformation upstream of the existing `coinflip.creditFlip` / `coinflip.creditFlipBatch` calls that adds zero events, state, modifiers, or entry points.
Output: Two modified contract files, two `.planning/` proof artifacts, one batched diff presented to the user, one user-approved commit `feat(279): whole-BURNIE floor at 3 RNG-amount sites + cursor-rotation dead-var removal [BUR-01..05]` carrying the storage-layout byte-identity proof + gas worst-case + bytecode delta in the message.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/279-whole-burnie-floor-bur/279-CONTEXT.md
@.planning/phases/279-whole-burnie-floor-bur/279-PATTERNS.md

<authority_note>
`279-CONTEXT.md` `<decisions>` is the ONLY authoritative source for implementation decisions (D-279-INLINE-01, D-279-BUR01-SITE-01, D-279-BUR02-DEADVAR-01, D-279-BUR03-ORDER-01, D-279-DISAMBIG-01) plus the Claude's Discretion items. `279-PATTERNS.md` carries the concrete before/after code excerpts — copy them, do not re-derive. ROADMAP.md / REQUIREMENTS.md cite approximate line numbers (`:1080`, `:1785`/`:1842`, `:1900`/`:1922`); the verified-at-discussion line numbers below and in CONTEXT.md `<code_context>` are authoritative. Where roadmap/requirements text conflicts with CONTEXT.md, CONTEXT.md wins.
</authority_note>

<interfaces>
<!-- Current contract state extracted from contracts/. Executor uses these directly — no exploration needed. -->

`_resolveLootboxCommon` (`contracts/modules/DegenerusGameLootboxModule.sol`, body around `:960`-`:1091`) — current state of the BUR-01 region:
- `:1068`: `burnieAmount = burnieNoMultiplier + burniePresale;` — `burnieAmount` is a named/outer-scope `uint256` return local.
- `:1069`-`:1072`: the `if (presale && burniePresale != 0)` bonus block, with `uint256 bonusBurnie` block-scoped inside it; `burnieAmount += bonusBurnie;` at `:1071`.
- `:1074`-`:1076`: `if (burnieAmount != 0) { coinflip.creditFlip(player, burnieAmount); }` — the existing zero-guard.
- `:1078`-`:1089`: `if (emitLootboxEvent) emit LootBoxOpened(player, index, day, amount, targetLevel, futureTickets, burnieAmount, roundedUp);` — the `burnieAmount` arg is the 7th positional field.
- `:1090`: `return (futureTickets, burnieAmount, roundedUp);` — `burnieAmount` is the 2nd return value.
All 3 consumers (`creditFlip` arg, `LootBoxOpened.burnie` field, return tuple) read `burnieAmount` directly — a single in-place floor reassignment makes all 3 see the floored value.

`_awardDailyCoinToTraitWinners` (`contracts/modules/DegenerusGameJackpotModule.sol`, `:1761`-`:1854`) — current state of the BUR-02 region:
- `:1753`-`:1760`: function NatSpec; line `:1756`-`:1757` describes "the cursor still advances and the corresponding +1 extra slot is structurally lost (accepted underspend)" — references the retiring mechanic, must be rewritten.
- `:1788`: `uint256 baseAmount = coinBudget / cap;`
- `:1789`: `uint256 extra = coinBudget % cap;` — only read by the `:1833` check. DELETE.
- `:1790`: `uint256 cursor = randomWord % cap;` — only read by the `:1833` check. DELETE.
- `:1810`-`:1817`: empty-bucket `continue` branch; the `unchecked` block at `:1811`-`:1815` contains `++i;` (KEEP) and `++cursor; if (cursor == cap) cursor = 0;` at `:1813`-`:1814` (DELETE).
- `:1832`: `uint256 amount = baseAmount;`
- `:1833`-`:1835`: `if (extra != 0 && cursor < extra) { amount += 1; }` — DELETE the whole block; `amount` becomes simply `uint256 amount = baseAmount;`.
- `:1837`-`:1846`: `if (winner != address(0) && amount != 0) { emit JackpotBurnieWin(...); coinflip.creditFlip(winner, amount); }` — the existing emit-guard; UNCHANGED, naturally absorbs post-floor-zero.
- `:1848`-`:1852`: loop-tail `unchecked` block contains `++i;` (KEEP) and `++cursor; if (cursor == cap) cursor = 0;` at `:1850`-`:1851` (DELETE).
- `randomWord` is still consumed by the keccak draws at `:1797`-`:1799` (`keccak256(abi.encode(randomWord, COIN_LEVEL_TAG, i))`) and `:1819`-`:1821` (`keccak256(abi.encode(randomWord, trait_i, lvlPrime, i))`). KEEP `randomWord`.

`_awardFarFutureCoinJackpot` (`contracts/modules/DegenerusGameJackpotModule.sol`, `:1859`-`:1926`) — current state of the BUR-03 region:
- `:1900`: `if (found == 0) return;`
- `:1902`: comment `// Distribute evenly among found winners`
- `:1903`: `uint256 perWinner = farBudget / found;`
- `:1904`: `if (perWinner == 0) return;` — the existing early-bail.
- `:1909`-`:1923`: the `FarFutureCoinJackpotWinner` emit loop; `batchAmounts[i] = perWinner;` at `:1918`.
- `:1925`: `coinflip.creditFlipBatch(batchPlayers, batchAmounts);` — UNCHANGED.

`coinflip.creditFlip(...)` / `coinflip.creditFlipBatch(...)` — the unchanged downstream sinks; the floor is purely upstream. No interface change.

OUT OF SCOPE — DO NOT TOUCH (D-279-DISAMBIG-01): `DegenerusGameJackpotModule.sol` contains a SECOND `extra`/`cursor` cursor-rotation: `uint256 extra = distParams & type(uint128).max;` at `:996`, `uint256 cursor = (startIdx + offset) % cap;` at `:999`, the `if (extra != 0 && cursor < extra)` block at `:1003`, the `++cursor; if (cursor == cap) cursor = 0;` at `:1020`-`:1021`, near a `JackpotTicketWin` emit. This is a TICKET-award rotation, NOT a BURNIE-amount site. It is OUT OF SCOPE for Phase 279 and MUST NOT be modified.
</interfaces>

<discipline>
Per project memory feedback:
- Only read/edit contracts from `contracts/` — stale copies exist elsewhere (`feedback_contract_locations.md`).
- `feedback_no_history_in_comments.md` — every NatSpec/comment rewrite describes what IS post-floor. NEVER write "previously fractional", "was +1-wei distribution", "cursor-rotation removed", "no longer". The `extra`/`cursor` cleanup must leave NO comment referencing the retired mechanic.
- `feedback_no_dead_guards.md` + `feedback_frozen_contracts_no_future_proofing.md` — drive the `extra`/`cursor` full removal at BUR-02; contracts are frozen at deploy, no leftover unreachable code, no future-proofing retention. The full dead-var removal is intended scope, not creep (user-confirmed 2026-05-14).
- `feedback_gas_worst_case.md` — the gas task derives the theoretical worst case FIRST, then benchmarks.
- `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md` — all contract edits batched into ONE diff, presented to the user, committed only after explicit user approval. Contract changes are NEVER pre-approved.
</discipline>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: BUR-01 — floor the burnieAmount accumulator in _resolveLootboxCommon</name>
  <files>contracts/modules/DegenerusGameLootboxModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameLootboxModule.sol (full file — _resolveLootboxCommon body around :960-:1091, especially the burnieAmount accumulator at :1068, the bonus block :1069-:1072, the if (burnieAmount != 0) guard :1074-:1076, the LootBoxOpened emit block :1078-:1089, the return tuple :1090; plus any NatSpec on _resolveLootboxCommon describing the BURNIE accumulator)
    - .planning/phases/279-whole-burnie-floor-bur/279-CONTEXT.md (D-279-INLINE-01, D-279-BUR01-SITE-01, Claude's Discretion on floor placement/wording + NatSpec)
    - .planning/phases/279-whole-burnie-floor-bur/279-PATTERNS.md (the "BUR-01 — lootbox spin BURNIE floor" section — concrete current code + apply instructions + local-variable scoping note)
  </read_first>
  <action>
    Apply D-279-INLINE-01 + D-279-BUR01-SITE-01 (BUR-01):

    (a) Insert the inline whole-BURNIE floor as a new statement immediately AFTER the bonus block closes at `:1072` and BEFORE the `if (burnieAmount != 0)` guard at `:1074`:
    `burnieAmount = (burnieAmount / 1 ether) * 1 ether;`
    Reassign `burnieAmount` in place (NOT a local intermediate) so all 3 downstream consumers see the floored value with zero further edits: the `coinflip.creditFlip(player, burnieAmount)` arg at `:1075`, the `LootBoxOpened` 7th positional arg (`burnieAmount`) at `:1086`, and the `return (futureTickets, burnieAmount, roundedUp)` at `:1090`.

    (b) Placement / scoping: the floor statement goes at the function-body brace depth where `burnieAmount` (the named `uint256` return local) is live — OUTSIDE the `if (presale && burniePresale != 0)` block (the `bonusBurnie` local at `:1070` is block-scoped inside that `if` and is irrelevant to the floor). `burnieAmount` is already `uint256` and `1 ether` is a `uint256` literal (`10**18`), so the division/multiply needs no cast. Do NOT floor the per-component values (`burnieNoMultiplier` / `burniePresale` / `bonusBurnie`) separately — D-279-BUR01-SITE-01 mandates flooring the final post-bonus accumulator ONCE.

    (c) Do NOT add any new branch — the existing `if (burnieAmount != 0)` guard at `:1074` naturally absorbs the post-floor-zero case (e.g. 0.99 BURNIE floors to 0, the guard skips the `creditFlip`). The `LootBoxOpened` emit still fires with `burnieAmount = 0` in that case, which is correct (the event field carries the post-floor amount per D-40N-BUR-SILENT-01).

    (d) NatSpec / comment: if `_resolveLootboxCommon` carries a comment describing the `burnieAmount` accumulator, add a one-line note describing the current invariant — "floored to whole-BURNIE (1 BURNIE = 1 ether); sub-1-BURNIE residue evaporates". Describe what IS — no "previously fractional" history (`feedback_no_history_in_comments.md`). Do NOT add a comment if none exists in that region and one is not warranted.
  </action>
  <verify>
    <automated>grep -n "burnieAmount = (burnieAmount / 1 ether) \* 1 ether" contracts/modules/DegenerusGameLootboxModule.sol ; npx hardhat compile 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `_resolveLootboxCommon` body contains exactly one statement `burnieAmount = (burnieAmount / 1 ether) * 1 ether;` (regex tolerant of whitespace), positioned after the `burnieAmount += bonusBurnie;` bonus block and before the `if (burnieAmount != 0)` guard — confirmed by line-index ordering (floor index < guard index < `coinflip.creditFlip` index).
    - The `coinflip.creditFlip(player, burnieAmount)` call, the `LootBoxOpened` emit, and the `return (futureTickets, burnieAmount, roundedUp)` all still read the bare `burnieAmount` local (no separate pre-floor snapshot variable introduced).
    - The per-component values `burnieNoMultiplier`, `burniePresale`, `bonusBurnie` are NOT individually floored.
    - No new event, state variable, modifier, or branch added; the `if (burnieAmount != 0)` guard is unchanged.
    - `npx hardhat compile` succeeds with no errors.
    - No comment in the edited region contains change-history language ("was", "previously", "used to", "no longer", "changed from").
  </acceptance_criteria>
  <done>
    `_resolveLootboxCommon` floors the post-bonus `burnieAmount` accumulator in place to a whole-BURNIE multiple before the existing zero-guard; the floored value flows to `creditFlip`, the `LootBoxOpened.burnie` field, and the return tuple; the contract compiles.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: BUR-02 + BUR-03 — floor baseAmount + full cursor-rotation dead-var removal in _awardDailyCoinToTraitWinners; floor perWinner in _awardFarFutureCoinJackpot</name>
  <files>contracts/modules/DegenerusGameJackpotModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameJackpotModule.sol (full file — _awardDailyCoinToTraitWinners at :1761-:1854 including the NatSpec :1753-:1760, baseAmount/extra/cursor at :1788-:1790, empty-bucket cursor-rotation :1813-:1814, the extra/cursor block + amount += 1 at :1832-:1835, the emit-guard :1837-:1846, the loop-tail cursor-rotation :1850-:1851; _awardFarFutureCoinJackpot at :1859-:1926 including perWinner at :1903 and the early-bail :1904; AND the OUT-OF-SCOPE second cursor-rotation at :996/:999/:1003/:1020-:1021 — read it ONLY to confirm you do not touch it)
    - .planning/phases/279-whole-burnie-floor-bur/279-CONTEXT.md (D-279-INLINE-01, D-279-BUR02-DEADVAR-01 — the full deletion list, D-279-BUR03-ORDER-01, D-279-DISAMBIG-01)
    - .planning/phases/279-whole-burnie-floor-bur/279-PATTERNS.md (the "BUR-02 — near-future coin jackpot floor + dead-variable cleanup" and "BUR-03 — far-future coin jackpot floor" sections — concrete current code + apply instructions + the DISAMBIGUATION warning)
  </read_first>
  <action>
    Apply D-279-INLINE-01 + D-279-BUR02-DEADVAR-01 + D-279-BUR03-ORDER-01 (BUR-02, BUR-03):

    BUR-02 — `_awardDailyCoinToTraitWinners`:
    (a) Change `:1788` from `uint256 baseAmount = coinBudget / cap;` to `uint256 baseAmount = ((coinBudget / cap) / 1 ether) * 1 ether;` — the inline whole-BURNIE floor on the per-winner base amount.
    (b) Delete the `uint256 extra = coinBudget % cap;` declaration at `:1789` — it is only ever read by the retired `:1833` check.
    (c) Delete the `uint256 cursor = randomWord % cap;` declaration at `:1790` — it is only ever read by the retired `:1833` check. KEEP `randomWord` itself — it is still consumed by the `keccak256(abi.encode(randomWord, COIN_LEVEL_TAG, i))` draw at `:1797`-`:1799` and the `keccak256(abi.encode(randomWord, trait_i, lvlPrime, i))` draw at `:1819`-`:1821`.
    (d) In the empty-bucket `continue` branch, delete `++cursor;` and `if (cursor == cap) cursor = 0;` at `:1813`-`:1814` — KEEP `++i;` inside the same `unchecked` block.
    (e) Delete the entire `if (extra != 0 && cursor < extra) { amount += 1; }` block at `:1832`-`:1835` — `amount` becomes simply `uint256 amount = baseAmount;` (the floored base value, nothing added).
    (f) In the loop-tail `unchecked` block, delete `++cursor;` and `if (cursor == cap) cursor = 0;` at `:1850`-`:1851` — KEEP `++i;`.
    (g) The existing `if (winner != address(0) && amount != 0)` emit-guard at `:1837` is UNCHANGED — it naturally skips both the `JackpotBurnieWin` emit and `coinflip.creditFlip` when `baseAmount < 1 ether` floors `amount` to 0 (full daily near-future budget evaporates silently per D-40N-BUR-DUST-01 + D-40N-BUR-SILENT-01). Do NOT add a new branch.
    (h) Rewrite the `_awardDailyCoinToTraitWinners` NatSpec at `:1753`-`:1760` — the lines `:1756`-`:1757` ("Empty (lvl', trait_i) buckets silently skip with no carry-forward and no top-up; the cursor still advances and the corresponding +1 extra slot is structurally lost (accepted underspend).") reference the retired cursor-rotation mechanic and MUST be removed/rewritten to describe the current invariant: each pull awards the floored whole-BURNIE `baseAmount`; empty buckets silently skip; sub-1-BURNIE residues (the `coinBudget % cap` remainder and any sub-1-ether base) evaporate. Describe what IS — no "previously", no "+1 extra slot was", no "cursor-rotation removed" (`feedback_no_history_in_comments.md`).

    BUR-03 — `_awardFarFutureCoinJackpot`:
    (i) Change `:1903` from `uint256 perWinner = farBudget / found;` to `uint256 perWinner = ((farBudget / found) / 1 ether) * 1 ether;` — the inline whole-BURNIE floor applied BEFORE the existing `if (perWinner == 0) return;` at `:1904`. Leave `:1904` exactly as-is — flooring before the `== 0` check lets the existing early-bail absorb the post-floor-zero case for free (when `perWinner < 1 ether` the floor yields 0, the function returns, `creditFlipBatch` is never reached, the full 25% far-future allocation evaporates). Do NOT add a new branch. The downstream `FarFutureCoinJackpotWinner` emit loop and `batchAmounts[i] = perWinner;` at `:1909`-`:1925` are UNCHANGED — they naturally carry the floored value.

    DISAMBIGUATION — DO NOT TOUCH (D-279-DISAMBIG-01): the SECOND `extra`/`cursor` cursor-rotation at `:996` (`uint256 extra = distParams & type(uint128).max;`), `:999` (`uint256 cursor = (startIdx + offset) % cap;`), the `if (extra != 0 && cursor < extra)` block at `:1003`, and the `++cursor; if (cursor == cap) cursor = 0;` at `:1020`-`:1021` is a TICKET-award rotation, NOT a BURNIE-amount site — it is OUT OF SCOPE for Phase 279. Make ZERO edits to that region.
  </action>
  <verify>
    <automated>grep -n "((coinBudget / cap) / 1 ether) \* 1 ether\|((farBudget / found) / 1 ether) \* 1 ether" contracts/modules/DegenerusGameJackpotModule.sol ; awk 'NR>=1761 && NR<=1860' contracts/modules/DegenerusGameJackpotModule.sol | grep -c -E '\b(extra|cursor)\b' ; grep -c -E '\b(extra|cursor)\b' contracts/modules/DegenerusGameJackpotModule.sol ; npx hardhat compile 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `_awardDailyCoinToTraitWinners` body contains `uint256 baseAmount = ((coinBudget / cap) / 1 ether) * 1 ether;` (regex tolerant of whitespace).
    - `_awardDailyCoinToTraitWinners` body (the function span, roughly `:1761` to the new closing brace) contains ZERO occurrences of the identifiers `extra` or `cursor` — the `coinBudget % cap` declaration, the `randomWord % cap` declaration, both `++cursor`/wrap blocks, and the `if (extra != 0 && cursor < extra)` + `amount += 1` block are all deleted.
    - `amount` in `_awardDailyCoinToTraitWinners` is assigned only `baseAmount` (no `amount += 1`).
    - `randomWord` still appears in `_awardDailyCoinToTraitWinners` — the two `keccak256(abi.encode(randomWord, ...))` draws are intact; both `++i;` increments are intact.
    - `_awardFarFutureCoinJackpot` body contains `uint256 perWinner = ((farBudget / found) / 1 ether) * 1 ether;` and the line immediately after it is the unchanged `if (perWinner == 0) return;`.
    - The OUT-OF-SCOPE `extra`/`cursor` region near `:996`-`:1021` is unchanged — the file still contains the `distParams & type(uint128).max` declaration and the `JackpotTicketWin`-adjacent cursor-rotation (so a whole-file `grep -c -E '\b(extra|cursor)\b'` is non-zero, but the `_awardDailyCoinToTraitWinners` span count is zero).
    - No new event, state variable, modifier, emit site, or branch added in either function.
    - `npx hardhat compile` succeeds with no errors.
    - No comment in the edited regions contains change-history language; the `_awardDailyCoinToTraitWinners` NatSpec no longer references "cursor", "+1 extra slot", or "accepted underspend" as a live mechanic.
  </acceptance_criteria>
  <done>
    `_awardDailyCoinToTraitWinners` floors `baseAmount` to a whole-BURNIE multiple and the `extra`/`cursor` cursor-rotation machinery is fully removed with `randomWord` and the `++i` increments preserved; `_awardFarFutureCoinJackpot` floors `perWinner` before the existing `== 0` early-bail; the OUT-OF-SCOPE ticket-award cursor-rotation is untouched; the contract compiles.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Prove storage-layout byte-identity, derive gas worst-case, present batched diff, await user approval, commit</name>
  <read_first>
    - feedback_gas_worst_case.md
    - feedback_no_contract_commits.md
    - feedback_batch_contract_approval.md
    - feedback_never_preapprove_contracts.md
    - feedback_manual_review_before_push.md
    - .planning/phases/278-jackpotmodule-cleanup-ent-05-baf-xorshift-refactor-wrapper-retirement-jpt-clean/278-01-STORAGE-LAYOUT-DIFF.md (the Phase 278 storage-layout byte-identity proof artifact — mirror its mechanic)
    - .planning/phases/278-jackpotmodule-cleanup-ent-05-baf-xorshift-refactor-wrapper-retirement-jpt-clean/278-01-GAS-WORSTCASE.md (the Phase 278 gas-worst-case derivation artifact — mirror its shape)
    - .planning/phases/275-auto-resolve-lootboxmodule-bernoulli-lbx-ar/275-A-STORAGE-LAYOUT-DIFF.md (the Phase 275 storage-layout proof — a second mechanic reference, covers LootboxModule)
  </read_first>
  <what-built>
    All Phase 279 contract changes from Tasks 1-2: BUR-01 floors the `burnieAmount` accumulator in `_resolveLootboxCommon`; BUR-02 floors `baseAmount` and fully removes the `extra`/`cursor` cursor-rotation machinery in `_awardDailyCoinToTraitWinners`; BUR-03 floors `perWinner` before the early-bail in `_awardFarFutureCoinJackpot`.

    Before presenting the diff, produce two artifacts mirroring the Phase 275/278 precedent:

    1. STORAGE-LAYOUT BYTE-IDENTITY PROOF (BUR-04) — Run the project's storage-layout inspection (`forge inspect contracts/modules/DegenerusGameLootboxModule.sol:DegenerusGameLootboxModule storage-layout` and `forge inspect contracts/modules/DegenerusGameJackpotModule.sol:DegenerusGameJackpotModule storage-layout`, or the project's equivalent — mirror exactly the mechanic in `278-01-STORAGE-LAYOUT-DIFF.md` and `275-A-STORAGE-LAYOUT-DIFF.md`) at the current working-tree HEAD AND against `git show 6a7455d1` (the `MILESTONE_V39_AT_HEAD_6a7455d1` baseline) for BOTH `DegenerusGameLootboxModule.sol` and `DegenerusGameJackpotModule.sol`. Diff each layout. Both MUST be byte-identical — Phase 279 touches only function bodies; no state variables, no event signatures, no storage slots, no modifiers, no admin entry points. Write the proof to `.planning/phases/279-whole-burnie-floor-bur/279-01-STORAGE-LAYOUT-DIFF.md` mirroring the Phase 278 artifact. If either layout is NOT byte-identical, STOP and surface the discrepancy to the user — do not commit.

    2. GAS WORST-CASE DERIVATION (`feedback_gas_worst_case.md`, BUR-05) — Derive the THEORETICAL worst-case gas path FIRST, THEN benchmark. Worst-case derivation: BUR-02's `_awardDailyCoinToTraitWinners` is the candidate worst-case site — the floor sits ONCE before a `for (i < cap)` loop over up to `DAILY_COIN_MAX_WINNERS` winners; the floor itself is outside the loop (one-time `DIV` + `MUL`), and the `extra`/`cursor` deletions REMOVE per-iteration `++cursor` + compare + conditional-zero ops from every loop iteration (net per-iteration gas-NEGATIVE). BUR-01 and BUR-03 each add one `DIV` + one `MUL` + one `PUSH` (~10-15 gas one-time per site). Derive the per-op delta for each site, identify which path is the theoretical worst case, THEN run `npx hardhat compile` and capture the measured bytecode-size delta for `DegenerusGameLootboxModule` + `DegenerusGameJackpotModule` vs current HEAD (the `extra`/`cursor` deletions should remove more bytecode than the 3 inline floors add → NET-NEGATIVE expected). Write the derivation + measured deltas to `.planning/phases/279-whole-burnie-floor-bur/279-01-GAS-WORSTCASE.md` mirroring the Phase 278 artifact. The derivation + deltas also go in the commit message body.
  </what-built>
  <how-to-verify>
    1. Run `git --no-pager diff --stat contracts/` and confirm exactly 2 files changed: `contracts/modules/DegenerusGameLootboxModule.sol` and `contracts/modules/DegenerusGameJackpotModule.sol`.
    2. Run `git --no-pager diff contracts/` and review the full batched diff.
    3. Confirm: `_resolveLootboxCommon` floors `burnieAmount` in place after the bonus block; `_awardDailyCoinToTraitWinners` floors `baseAmount` and the `extra`/`cursor` machinery is fully gone with `randomWord` + both `++i` preserved; `_awardFarFutureCoinJackpot` floors `perWinner` before the unchanged `if (perWinner == 0) return`; the OUT-OF-SCOPE ticket-award cursor-rotation near `:1003` is untouched; no new state variables, events, emit sites, modifiers, or entry points; no change-history language in comments.
    4. Review the storage-layout byte-identity proof (`279-01-STORAGE-LAYOUT-DIFF.md`) — must show byte-identical vs `6a7455d1` for BOTH modules.
    5. Review the gas worst-case derivation (`279-01-GAS-WORSTCASE.md`) — worst-case path derived BEFORE benchmarking; bytecode delta NET-NEGATIVE.
    6. Approve the diff explicitly, or describe required changes.
  </how-to-verify>
  <action>
    After explicit user approval ONLY: stage exactly the two contract files and commit with:
    `feat(279): whole-BURNIE floor at 3 RNG-amount sites + cursor-rotation dead-var removal [BUR-01..05]`
    The commit body MUST include: the storage-layout byte-identity result vs `6a7455d1` for both modules (BUR-04), the gas worst-case derivation, and the measured bytecode-size delta for `DegenerusGameLootboxModule` + `DegenerusGameJackpotModule` (BUR-05). Do NOT push. Do NOT stage test files, the two `.planning/` artifacts, or any other file. If the user requests changes, apply them and re-present the diff — do NOT commit until approved. The two `.planning/` artifacts (`279-01-STORAGE-LAYOUT-DIFF.md`, `279-01-GAS-WORSTCASE.md`) are committed separately as docs alongside the SUMMARY, not in the contract commit.
  </action>
  <resume-signal>Type "approved" to commit the batched contract diff, or describe the changes you want.</resume-signal>
  <acceptance_criteria>
    - `git diff --stat contracts/` shows exactly the two named files and no others.
    - `.planning/phases/279-whole-burnie-floor-bur/279-01-STORAGE-LAYOUT-DIFF.md` exists and shows the storage layout for BOTH `DegenerusGameLootboxModule.sol` and `DegenerusGameJackpotModule.sol` is byte-identical to `git show 6a7455d1`.
    - `.planning/phases/279-whole-burnie-floor-bur/279-01-GAS-WORSTCASE.md` exists; the worst-case path was derived BEFORE benchmarking; the measured bytecode delta is recorded and is NET-NEGATIVE.
    - The commit is created ONLY after explicit user approval, with message `feat(279): whole-BURNIE floor at 3 RNG-amount sites + cursor-rotation dead-var removal [BUR-01..05]` and a body containing the storage-layout result + gas/bytecode deltas.
    - Nothing is pushed; no test files or `.planning/` artifacts are staged in the contract commit.
  </acceptance_criteria>
  <done>
    The user has reviewed and approved the batched 2-file contract diff; one commit `feat(279): whole-BURNIE floor at 3 RNG-amount sites + cursor-rotation dead-var removal [BUR-01..05]` exists carrying the storage-layout byte-identity proof + gas + bytecode delta report; the two `.planning/` proof artifacts exist; nothing pushed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VRF/RNG word → BURNIE-amount compute | The pre-floor `burnieAmount` / `coinBudget` / `farBudget` values derive from RNG-influenced variance rolls and daily budgets; the floor is a deterministic integer-division transform applied at the amount-compute point upstream of `coinflip.creditFlip` / `coinflip.creditFlipBatch`. |
| amount-compute → `coinflip` credit sink | The floored amount crosses into the unchanged `coinflip.creditFlip(...)` / `creditFlipBatch(...)` interface; only the value shape changes (always a 1-ether multiple), the call interface is untouched. |
| contract storage layout → v39 baseline `6a7455d1` | Phase 279 must not perturb storage slots — every off-chain consumer and the audit baseline depend on byte-identical layout. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-279-01 | Tampering | integer-division floor correctness at the 3 sites (`(x / 1 ether) * 1 ether`) | mitigate | The floor is the canonical whole-unit floor: `x / 1 ether` truncates toward zero (Solidity unsigned integer division — no negative operands possible, all 3 target locals are `uint256` BURNIE-wei amounts ≥ 0), then `* 1 ether` rescales. The result is always `<= x` (floor direction) and always a multiple of `1 ether`. No truncation-induced underflow is possible because the operation never subtracts — the worst case is `x < 1 ether` yielding 0, which the existing zero-guards (`if (burnieAmount != 0)` at LootboxModule, `if (winner != address(0) && amount != 0)` at near-future, `if (perWinner == 0) return` at far-future) all already handle. BUR-03's floor is explicitly ordered BEFORE the `== 0` check (D-279-BUR03-ORDER-01) so a sub-1-ether `perWinner` cannot reach `creditFlipBatch` as a 0-amount entry. The TST-BUR test wave (Plan 02) asserts the floor direction + the `mod(amount, 1 ether) == 0` invariant across a representative sweep. |
| T-279-02 | Elevation of Privilege | storage layout / new mutation paths | accept | Phase 279 touches ONLY function bodies — no state variables, no event signatures, no storage slots, no modifiers, no admin or external mutation entry points added. Storage layout is proven byte-identical to v39 baseline `6a7455d1` for both modified modules (Task 3 artifact, BUR-04). The `extra`/`cursor` removal only DELETES local-variable machinery (stack locals, never storage) — it cannot alter the storage layout. The floor introduces no new external surface; it is a pure-amount transform inside existing private functions. |
| T-279-03 | Denial of Service | attacker amplification of dust evaporation / daily-budget griefing | accept | The dust-evaporation is bounded and not attacker-amplifiable beyond the intended design. At BUR-01 the per-spin per-player loss is bounded < 1 BURNIE (the fractional part of one variance-roll-derived `burnieAmount`) — an attacker spinning more lootboxes loses at most < 1 BURNIE per spin to evaporation, identical to any honest player, with no cross-spin accumulation (each spin floors independently). At BUR-02/BUR-03 the daily near-future / 25% far-future budget evaporates ONLY when `baseAmount < 1 ether` (i.e. `coinBudget < cap` ether) or `perWinner < 1 ether` (i.e. `farBudget < found` ether) — a low-pool-day condition driven by the daily budget and winner count, neither of which an attacker controls at the BURNIE-amount-compute point. There is no path for an attacker to inflate `cap` or `found` to force evaporation: `cap` is `DAILY_COIN_MAX_WINNERS` capped at `coinBudget`, and `found` is the count of sampled far-future ticket-queue winners. Evaporation is accepted by explicit user disposition (D-40N-BUR-DUST-01) — "sub 1 burnie amounts are economically negligible". The removed `extra`/`cursor` cursor-rotation was a sub-1-wei `+1` distribution mechanic, not a griefing-sensitive surface — its removal narrows attack surface, it does not widen it. |
| T-279-04 | Information Disclosure | event-field value semantics shift (`LootBoxOpened.burnie`, `JackpotBurnieWin.amount`, `FarFutureCoinJackpotWinner.perWinner` now carry floored values) | accept | The 3 event SIGNATURES and topic-hashes are UNCHANGED — only the emitted value is now always a 1-ether multiple. This is consistent with D-40N-BUR-SILENT-01: existing event fields emit the post-floor amount, no separate scaled-pre snapshot, no new field, no new event. Off-chain consumers see a value-shape change (always whole BURNIE) but no schema change; the emitted value now matches exactly what `coinflip.creditFlip` receives — the emit becomes self-consistent with the credit, not divergent. No sensitive information is exposed; the floored amount is strictly less information than the pre-floor fractional amount. |
| T-279-05 | Tampering | accidental modification of the OUT-OF-SCOPE ticket-award cursor-rotation near `:1003` | mitigate | `DegenerusGameJackpotModule.sol` contains a SECOND `extra`/`cursor` cursor-rotation (`:996`/`:999`/`:1003`/`:1020`-`:1021`) that is a TICKET-award rotation, NOT a BURNIE-amount site (D-279-DISAMBIG-01). Task 2's `read_first` requires reading that region explicitly to confirm non-modification; Task 2's acceptance criteria assert the `_awardDailyCoinToTraitWinners` span has zero `extra`/`cursor` occurrences WHILE the whole-file count stays non-zero (the OUT-OF-SCOPE region survives); Task 3's diff review explicitly confirms the `:1003` region is untouched. The storage-layout byte-identity proof (BUR-04) would also catch any structural perturbation of that region. |
</threat_model>

<verification>
- `npx hardhat compile` succeeds.
- `_resolveLootboxCommon` contains `burnieAmount = (burnieAmount / 1 ether) * 1 ether;` positioned after the bonus block, before the `if (burnieAmount != 0)` guard.
- `_awardDailyCoinToTraitWinners` contains `uint256 baseAmount = ((coinBudget / cap) / 1 ether) * 1 ether;` and ZERO `extra`/`cursor` identifier occurrences within its function span; `randomWord` and both `++i` increments intact.
- `_awardFarFutureCoinJackpot` contains `uint256 perWinner = ((farBudget / found) / 1 ether) * 1 ether;` immediately before the unchanged `if (perWinner == 0) return;`.
- The OUT-OF-SCOPE `extra`/`cursor` ticket-award cursor-rotation near `:996`-`:1021` is unchanged.
- Storage layout for both `DegenerusGameLootboxModule.sol` and `DegenerusGameJackpotModule.sol` byte-identical to `6a7455d1` (artifact proof, BUR-04).
- Exactly 2 contract files changed; one user-approved commit created; nothing pushed.
- Bytecode delta NET-NEGATIVE, reported in commit message + `279-01-GAS-WORSTCASE.md` (BUR-05).
</verification>

<success_criteria>
- BUR-01: `_resolveLootboxCommon` floors the post-bonus `burnieAmount` accumulator once to a whole-BURNIE multiple via `(burnieAmount / 1 ether) * 1 ether` before the existing zero-guard; the floored value reaches `creditFlip`, the `LootBoxOpened.burnie` field, and the return tuple.
- BUR-02: `_awardDailyCoinToTraitWinners` floors `baseAmount` via `((coinBudget / cap) / 1 ether) * 1 ether`; the `extra`/`cursor` declarations, both `++cursor`/wrap blocks, and the `amount += 1` cursor-rotation block are fully removed; `randomWord` and both `++i` increments preserved; NatSpec rewritten to describe the current invariant.
- BUR-03: `_awardFarFutureCoinJackpot` floors `perWinner` via `((farBudget / found) / 1 ether) * 1 ether` before the unchanged `if (perWinner == 0) return` early-bail.
- BUR-04: storage layout for both modified modules proven byte-identical to v39 baseline `6a7455d1`; zero new state/events/emit-sites/modifiers/entry-points; recorded in `279-01-STORAGE-LAYOUT-DIFF.md` + the commit message.
- BUR-05: theoretical worst-case gas path derived BEFORE benchmarking; measured bytecode delta NET-NEGATIVE; recorded in `279-01-GAS-WORSTCASE.md` + the commit message.
- The OUT-OF-SCOPE ticket-award cursor-rotation near `:1003` is untouched (D-279-DISAMBIG-01).
- Contract compiles; exactly one batched user-approved commit `feat(279): ... [BUR-01..05]`; nothing pushed.
</success_criteria>

<output>
After completion, create `.planning/phases/279-whole-burnie-floor-bur/279-01-SUMMARY.md`
</output>
