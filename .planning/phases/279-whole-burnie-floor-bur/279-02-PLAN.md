---
phase: 279-whole-burnie-floor-bur
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - test/unit/LootboxWholeBurnieFloor.test.js
  - test/unit/JackpotNearFutureCoinFloor.test.js
  - test/unit/JackpotFarFutureCoinFloor.test.js
  - test/stat/WholeBurnieFloorInvariant.test.js
  - test/stat/SurfaceRegression.test.js
autonomous: false
requirements:
  - TST-BUR-01
  - TST-BUR-02
  - TST-BUR-03
  - TST-BUR-04

must_haves:
  truths:
    - "A new test asserts (source-structural) that `_resolveLootboxCommon` contains the `burnieAmount = (burnieAmount / 1 ether) * 1 ether` floor positioned before the `if (burnieAmount != 0)` guard and before the `coinflip.creditFlip(player, burnieAmount)` call, plus JS boundary math: 0.99→0, 1.99→1, 2.00→2, 0→0."
    - "A new test asserts (source-structural) that `_awardDailyCoinToTraitWinners` floors `baseAmount`, contains zero `extra`/`cursor` identifiers, assigns `amount` only `baseAmount`, still references `randomWord`, and proves the `JackpotBurnieWin` emit + `creditFlip` sit inside the `winner != address(0) && amount != 0` guard so a sub-1-ether `baseAmount` evaporates the daily budget."
    - "A new test asserts (source-structural) that `_awardFarFutureCoinJackpot` floors `perWinner` and the floor expression appears BEFORE `if (perWinner == 0) return` so a sub-1-ether `perWinner` early-bails before `creditFlipBatch`."
    - "A new invariant-sweep test asserts every BURNIE-mint amount producible from the 3 BUR sites is a multiple of 1 ether, AND a negative cross-site assertion proves the mint-boost path at `DegenerusGameMintModule.sol:1199` still has NO `/ 1 ether) * 1 ether` floor (status-quo fractional per D-40N-BUR-MINTBOOST-OUT-01)."
    - "`test/stat/SurfaceRegression.test.js`'s `SURF_01_PROTECTED_RANGES_V40` array is re-cut to exclude the Phase 279 BUR-02/BUR-03 OLD-side delta lines in `DegenerusGameJackpotModule.sol`; the walk algorithm and `it` bodies are unchanged."
    - "The test suite passes; the batched test diff is committed in exactly one user-approved commit."
  artifacts:
    - path: "test/unit/LootboxWholeBurnieFloor.test.js"
      provides: "TST-BUR-01 LootboxModule:1080 floor regression — source-structural proof + JS boundary math + LootBoxOpened.burnie field-consistency"
      min_lines: 60
    - path: "test/unit/JackpotNearFutureCoinFloor.test.js"
      provides: "TST-BUR-02 near-future coin jackpot floor + dead-var removal + budget-evaporation regression"
      min_lines: 60
    - path: "test/unit/JackpotFarFutureCoinFloor.test.js"
      provides: "TST-BUR-03 far-future coin jackpot floor + early-bail-before-creditFlipBatch regression"
      min_lines: 50
    - path: "test/stat/WholeBurnieFloorInvariant.test.js"
      provides: "TST-BUR-04 whole-BURNIE invariant sweep across all 3 sites + mint-boost negative cross-site assertion"
      min_lines: 60
    - path: "test/stat/SurfaceRegression.test.js"
      provides: "SURF_01_PROTECTED_RANGES_V40 re-cut to exclude Phase 279 BUR-02/BUR-03 delta lines"
      contains: "SURF_01_PROTECTED_RANGES_V40"
  key_links:
    - from: "test/unit/LootboxWholeBurnieFloor.test.js"
      to: "_resolveLootboxCommon burnieAmount floor in DegenerusGameLootboxModule.sol"
      via: "extractBody + stripLineComments + regex + index-ordering assertion"
      pattern: "burnieAmount = \\(burnieAmount / 1 ether\\)"
    - from: "test/unit/JackpotNearFutureCoinFloor.test.js"
      to: "_awardDailyCoinToTraitWinners body in DegenerusGameJackpotModule.sol"
      via: "extractBody + /\\bextra\\b/ and /\\bcursor\\b/ absence assertions + emit-guard ordering proof"
      pattern: "\\(\\(coinBudget / cap\\) / 1 ether\\)"
    - from: "test/stat/WholeBurnieFloorInvariant.test.js"
      to: "DegenerusGameMintModule.sol:1199 mint-boost creditFlip"
      via: "extractBody on the MintModule function — assert NO floor expression present"
      pattern: "creditFlip\\(buyer, lootboxFlipCredit\\)"
    - from: "test/stat/SurfaceRegression.test.js SURF_01_PROTECTED_RANGES_V40"
      to: "git diff 6a7455d1 HEAD OLD-side delta lines in DegenerusGameJackpotModule.sol"
      via: "re-cut protected ranges as the exact complement of the Phase 279 OLD-side modified-line set"
      pattern: "SURF_01_PROTECTED_RANGES_V40"
---

<objective>
Land the Phase 279 test wave in one batched, user-approved commit: three new `test/unit/` regression tests proving the 3 BUR-site floors landed correctly (TST-BUR-01 LootboxModule:1080, TST-BUR-02 near-future coin jackpot + dead-var removal, TST-BUR-03 far-future coin jackpot + early-bail), one new `test/stat/` invariant-sweep test asserting every BUR-site BURNIE-mint amount is a 1-ether multiple plus the mint-boost negative cross-site assertion (TST-BUR-04), and the required `SurfaceRegression.test.js` `SURF_01_PROTECTED_RANGES_V40` re-cut so the byte-identity drift gate excludes the Phase 279 BUR-02/BUR-03 delta lines.

Purpose: Prove the whole-BURNIE floor landed at all 3 RNG-amount sites with the `mod(amount, 1 ether) == 0` invariant holding, prove the `extra`/`cursor` dead-var removal left zero residue, prove the budget-evaporation paths behave correctly (sub-1-ether → silent skip / early-bail), prove the mint-boost path stayed status-quo fractional, and keep the v40.0 surface-regression byte-identity gate green.
Output: Four new test files, one modified test file (`SurfaceRegression.test.js`), one batched diff presented to the user, one user-approved commit `test(279): whole-BURNIE floor regression + invariant sweep + surface-regression re-cut [TST-BUR-01..04]`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/279-whole-burnie-floor-bur/279-CONTEXT.md
@.planning/phases/279-whole-burnie-floor-bur/279-PATTERNS.md
@.planning/phases/279-whole-burnie-floor-bur/279-01-PLAN.md

<authority_note>
`279-CONTEXT.md` `<decisions>` is the ONLY authoritative source for implementation decisions; `279-PATTERNS.md` carries the concrete test analogs + reusable infra excerpts — copy them, do not re-derive. REQUIREMENTS.md §TST-BUR suggests `test/lootbox/` and `test/jackpot/` placement — those directories do NOT exist on disk. The established on-disk layout (confirmed by the `test/` directory listing) is: `test/unit/` (the live home of `LootboxWholeTicket.test.js`, `JackpotTicketRollSilentColdBust.test.js`, `LootboxAutoResolveRemByte.test.js` — the direct analogs), `test/stat/` (heavy Monte-Carlo / invariant-sweep tier wired into `test:stat`, the home of `LootboxBernoulliEv.test.js` + `SurfaceRegression.test.js`). PLACEMENT DECISION (planner, per CONTEXT.md Claude's Discretion "exact test filenames + placement"): TST-BUR-01/02/03 → `test/unit/`; TST-BUR-04 → `test/stat/` (the invariant sweep across N representative budgets/spins is the heavy-MC shape). The Phase 279 floor is trivially deterministic — per `279-PATTERNS.md` "No Analog Found", do NOT add an on-chain `BurnieFloorTester.sol`; the load-bearing evidence is source-level structural proof (`extractBody` + `stripLineComments` + regex + index-ordering), with JS-side boundary math as confirmation — the same fixture-coverage-gap precedent `JackpotTicketRollSilentColdBust.test.js` establishes for fixture-gapped JackpotModule functions.
</authority_note>

<test_precedent>
- `test/unit/LootboxWholeTicket.test.js` — same `_resolveLootboxCommon` function under test (Phase 274). Copy its imports (`chai`, `hardhat`, `node:fs`, `node:path`) and `MODULE_SOURCE_PATH` resolution to `contracts/modules/DegenerusGameLootboxModule.sol`. The G17/G18/G19 grep-precedent at its head is the source-structural assertion idiom for TST-BUR-01.
- `test/unit/JackpotTicketRollSilentColdBust.test.js` — the authoritative source-structural-proof infra for fixture-gapped JackpotModule functions. Copy VERBATIM: the `extractBody(source, signature)` brace-match function-body extractor, the `stripLineComments(body)` helper (strips `//` so structural greps don't self-invalidate on NatSpec prose), and the `MODULE_SOURCE_PATH` resolution to `contracts/modules/DegenerusGameJackpotModule.sol`. The `[02a]`/`[02b]` "body contains exactly / body has NO" assertion idiom and the `[02c]` early-return-before-emit ordering proof are the direct analogs for TST-BUR-02 and TST-BUR-03.
- `test/unit/LootboxAutoResolveRemByte.test.js` — the `extractBody` brace-matcher pattern and the `firstIdx < elseIdx < autoIdx` index-ordering idiom for "expression X appears before expression Y" structural proofs.
- `test/stat/LootboxBernoulliEv.test.js` — the `makeRng(seedHex)` deterministic seeded keccak-counter PRNG (a counter-incrementing `keccak256` generator) for generating representative budget/spin inputs in the TST-BUR-04 sweep. NOTE: TST-BUR-04 is a FLOOR invariant (`amount % (10n ** 18n) === 0n`), NOT EV-neutrality — no chi-square / Wilson-Hilferty needed; `LootboxBernoulliEv.test.js` is the STRUCTURAL analog for "sweep N inputs, assert a per-amount property", not the assertion itself.
- `test/stat/SurfaceRegression.test.js` — the v40.0 `SURF-01..05` describe block. `SURF_01_PROTECTED_RANGES_V40` (the protected-range array) currently asserts `DegenerusGameJackpotModule.sol` is byte-identical vs `6a7455d1` EXCEPT the documented Phase 276-278 delta sites. The currently-protected ranges `JackpotModule L711-1005 (gold-solo + daily-coin bodies)` and `JackpotModule L1014-2177 (BAF jackpot + ticket-distribution bodies)` CONTAIN the Phase 279 BUR-02 sites (`_awardDailyCoinToTraitWinners` body) and BUR-03 site (`_awardFarFutureCoinJackpot` `perWinner`). The walk algorithm (`walkAndAssertV40`) and the `it` bodies are UNCHANGED — only the range array + the block-header comment change.
</test_precedent>

<interfaces>
<!-- Post-Wave-1 contract state the tests assert against. Wave 2 depends_on Wave 1 so the source on disk is post-floor. -->

`_resolveLootboxCommon` post-Wave-1 (`contracts/modules/DegenerusGameLootboxModule.sol`): the body contains `burnieAmount = (burnieAmount / 1 ether) * 1 ether;` positioned after the `burnieAmount += bonusBurnie;` bonus block and before the `if (burnieAmount != 0)` guard. The `coinflip.creditFlip(player, burnieAmount)` call, the `LootBoxOpened` emit (7th positional arg `burnieAmount`), and the `return (futureTickets, burnieAmount, roundedUp)` all read the floored `burnieAmount`.

`_awardDailyCoinToTraitWinners` post-Wave-1 (`contracts/modules/DegenerusGameJackpotModule.sol`): `uint256 baseAmount = ((coinBudget / cap) / 1 ether) * 1 ether;`; ZERO `extra`/`cursor` identifiers in the function span; `uint256 amount = baseAmount;` with no `amount += 1`; `randomWord` still consumed by the two `keccak256(abi.encode(randomWord, ...))` draws; the `if (winner != address(0) && amount != 0)` guard wraps both the `JackpotBurnieWin` emit and `coinflip.creditFlip(winner, amount)`; both `++i` increments intact.

`_awardFarFutureCoinJackpot` post-Wave-1 (`contracts/modules/DegenerusGameJackpotModule.sol`): `uint256 perWinner = ((farBudget / found) / 1 ether) * 1 ether;` immediately followed by the unchanged `if (perWinner == 0) return;`. The `FarFutureCoinJackpotWinner` emit loop and `coinflip.creditFlipBatch(batchPlayers, batchAmounts)` are unchanged.

`DegenerusGameMintModule.sol:1199` (UNCHANGED by Phase 279 — the negative-assertion target): the mint-boost flip-credit `coinflip.creditFlip(buyer, lootboxFlipCredit)` — `lootboxFlipCredit` is a deterministic mint-amount-derived value with NO whole-BURNIE floor applied (D-40N-BUR-MINTBOOST-OUT-01 — explicitly out of v40.0 BUR scope; stays status-quo fractional). The executor must locate the enclosing function signature for the `extractBody` call.
</interfaces>

<discipline>
Per project memory feedback:
- Only read contracts from `contracts/` — stale copies exist elsewhere (`feedback_contract_locations.md`).
- `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md` — `test/` changes are NOT committed without explicit user approval; all test edits batched into ONE diff, presented to the user, committed only after explicit approval. Test changes are NEVER pre-approved.
- `feedback_no_history_in_comments.md` — test comments describe what the test asserts, not "this used to test fractional amounts".
- The Wave 1 contract commit must already be landed (this plan `depends_on: ["01"]`) — the tests assert against the post-floor source on disk. The `SurfaceRegression.test.js` re-cut requires running `git diff 6a7455d1 HEAD -- contracts/modules/DegenerusGameJackpotModule.sol` AFTER the Wave 1 commit to capture the exact OLD-side delta line set.
</discipline>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: TST-BUR-01 + TST-BUR-02 + TST-BUR-03 — three source-structural floor regression tests in test/unit/</name>
  <files>test/unit/LootboxWholeBurnieFloor.test.js, test/unit/JackpotNearFutureCoinFloor.test.js, test/unit/JackpotFarFutureCoinFloor.test.js</files>
  <read_first>
    - test/unit/LootboxWholeTicket.test.js (the imports + MODULE_SOURCE_PATH idiom + G17/G18/G19 source-structural grep precedent — the TST-BUR-01 analog, same function under test)
    - test/unit/JackpotTicketRollSilentColdBust.test.js (the extractBody + stripLineComments infra to copy verbatim; the [02a]/[02b] "body contains / body has NO" idiom; the [02c] early-return-before-emit ordering proof — the TST-BUR-02 and TST-BUR-03 analogs)
    - test/unit/LootboxAutoResolveRemByte.test.js (the extractBody brace-matcher + the firstIdx < elseIdx < autoIdx index-ordering idiom)
    - contracts/modules/DegenerusGameLootboxModule.sol (post-Wave-1 — the _resolveLootboxCommon body with the burnieAmount floor; confirm the exact floor expression + its position relative to the guard and the creditFlip call)
    - contracts/modules/DegenerusGameJackpotModule.sol (post-Wave-1 — the _awardDailyCoinToTraitWinners body with the baseAmount floor + extra/cursor removal + the winner-guard wrapping the emit/creditFlip; the _awardFarFutureCoinJackpot body with the perWinner floor before the == 0 early-bail)
    - .planning/phases/279-whole-burnie-floor-bur/279-PATTERNS.md (the "TST-BUR-01", "TST-BUR-02", "TST-BUR-03" sections — exact assertion shapes + reusable infra excerpts)
    - .planning/phases/279-whole-burnie-floor-bur/279-CONTEXT.md (D-40N-BUR-DUST-01, D-40N-BUR-SILENT-01 — the budget-evaporation behavior the tests assert)
  </read_first>
  <action>
    Create three new `test/unit/` test files. Each uses the source-structural-proof technique as the load-bearing evidence (the 3 BUR sites have a documented fixture-coverage gap — no deterministic full-state fixture exists at the required granularity; `JackpotTicketRollSilentColdBust.test.js` is the precedent for structural-proof-as-primary-evidence). Copy the `extractBody` brace-matcher and `stripLineComments` helper VERBATIM from `JackpotTicketRollSilentColdBust.test.js` into each file (or a shared local copy per file — match the existing per-file-copy precedent).

    (a) `test/unit/LootboxWholeBurnieFloor.test.js` (TST-BUR-01):
    - Resolve `MODULE_SOURCE_PATH` to `contracts/modules/DegenerusGameLootboxModule.sol`; `extractBody` the `_resolveLootboxCommon` body; `stripLineComments` it.
    - Assert the body contains the floor expression `burnieAmount = (burnieAmount / 1 ether) * 1 ether` (regex tolerant of whitespace between tokens).
    - Assert index-ordering: the floor-expression index < the `if (burnieAmount != 0)` guard index < the `coinflip.creditFlip(player, burnieAmount)` call index (the `firstIdx < elseIdx < autoIdx` idiom).
    - Negative/field-consistency assertion: prove the `LootBoxOpened` emit reads the bare `burnieAmount` local (not a separate pre-floor snapshot) — assert the emit block contains `burnieAmount` as the 7th positional arg and there is no other floored/unfloored `burnie*` snapshot variable introduced between the floor and the emit.
    - JS-side boundary math (deterministic, no on-chain tester): a small table asserting the floor function `(x / 1e18) * 1e18` applied to `0.99e18 → 0`, `1.99e18 → 1e18`, `2.00e18 → 2e18`, `0 → 0` — implement the floor in JS with BigInt and assert each case. This is confirmation evidence; the source-structural proof is load-bearing.

    (b) `test/unit/JackpotNearFutureCoinFloor.test.js` (TST-BUR-02):
    - Resolve `MODULE_SOURCE_PATH` to `contracts/modules/DegenerusGameJackpotModule.sol`; `extractBody` the `_awardDailyCoinToTraitWinners` body; `stripLineComments` it.
    - Assert the body contains `baseAmount = ((coinBudget / cap) / 1 ether) * 1 ether` (regex tolerant of whitespace).
    - Assert `/\bextra\b/.test(strippedBody) === false` AND `/\bcursor\b/.test(strippedBody) === false` — the dead vars are fully gone from this function's body (use the `\b` word-boundary so substrings like "extra" inside a longer identifier are not matched; both identifiers must be wholly absent).
    - Assert `amount` is assigned only `baseAmount` — the body does NOT contain `amount += 1`.
    - Assert `/\brandomWord\b/.test(strippedBody) === true` — `randomWord` survives (the keccak draws are intact).
    - Budget-evaporation structural proof: assert the `JackpotBurnieWin` emit AND the `coinflip.creditFlip(winner, amount)` call both appear AFTER the `if (winner != address(0) && amount != 0)` guard opening (index-ordering — guard index < emit index < creditFlip index), proving a sub-1-ether `baseAmount` that floors `amount` to 0 silently skips all winners. Optionally include JS boundary math: `coinBudget = 50e18, cap = 100 → baseAmount = 0.5e18 → floor → 0`; `coinBudget = 150e18, cap = 100 → baseAmount = 1.5e18 → floor → 1e18`.

    (c) `test/unit/JackpotFarFutureCoinFloor.test.js` (TST-BUR-03):
    - Resolve `MODULE_SOURCE_PATH` to `contracts/modules/DegenerusGameJackpotModule.sol`; `extractBody` the `_awardFarFutureCoinJackpot` body; `stripLineComments` it.
    - Assert the body contains `perWinner = ((farBudget / found) / 1 ether) * 1 ether` (regex tolerant of whitespace).
    - Assert index-ordering: the floor-expression index < the `if (perWinner == 0) return` index (the floor MUST precede the early-bail so a sub-1-ether `perWinner` cannot reach `creditFlipBatch` — the `returnIdx` ordering idiom). Also assert the `coinflip.creditFlipBatch` call index > the `if (perWinner == 0) return` index (the bail is between the floor and the batch call).
    - JS boundary math: `farBudget = 5e18, found = 10 → perWinner = 0.5e18 → floor → 0` (early-bail fires); `farBudget = 25e18, found = 10 → perWinner = 2e18` (positive case).
  </action>
  <verify>
    <automated>npx hardhat test test/unit/LootboxWholeBurnieFloor.test.js test/unit/JackpotNearFutureCoinFloor.test.js test/unit/JackpotFarFutureCoinFloor.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test/unit/LootboxWholeBurnieFloor.test.js` exists, asserts the `_resolveLootboxCommon` floor expression is present and ordered before the `!= 0` guard and the `creditFlip` call, asserts `LootBoxOpened` reads the floored `burnieAmount`, and includes the 4 JS boundary cases (0.99→0, 1.99→1, 2.00→2, 0→0).
    - `test/unit/JackpotNearFutureCoinFloor.test.js` exists, asserts the `_awardDailyCoinToTraitWinners` `baseAmount` floor expression is present, asserts `\bextra\b` and `\bcursor\b` are both absent from the function body, asserts no `amount += 1`, asserts `randomWord` survives, and proves the emit + `creditFlip` sit inside the `winner != address(0) && amount != 0` guard.
    - `test/unit/JackpotFarFutureCoinFloor.test.js` exists, asserts the `_awardFarFutureCoinJackpot` `perWinner` floor expression is present and ordered BEFORE `if (perWinner == 0) return`, and before `creditFlipBatch`.
    - All three test files copy the `extractBody` + `stripLineComments` infra from `JackpotTicketRollSilentColdBust.test.js`; no on-chain `BurnieFloorTester.sol` is added.
    - `npx hardhat test` on the three files passes with zero failures.
  </acceptance_criteria>
  <done>
    Three new `test/unit/` regression tests prove — via source-structural `extractBody` proof plus JS boundary math — that the BUR-01/BUR-02/BUR-03 floors landed at the correct sites with correct ordering, the `extra`/`cursor` dead vars are gone, and the budget-evaporation paths route through the existing guards; all three pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: TST-BUR-04 invariant sweep + mint-boost negative assertion + SurfaceRegression SURF_01 re-cut</name>
  <files>test/stat/WholeBurnieFloorInvariant.test.js, test/stat/SurfaceRegression.test.js</files>
  <read_first>
    - test/stat/LootboxBernoulliEv.test.js (the makeRng deterministic seeded keccak-counter PRNG to copy for the sweep input generation; the test:stat-tier file shape)
    - test/unit/JackpotTicketRollSilentColdBust.test.js (the extractBody + stripLineComments infra — reused for the 3-site combined gate + the mint-boost negative assertion)
    - test/stat/SurfaceRegression.test.js (the v40.0 SURF-01..05 describe block; SURF_01_PROTECTED_RANGES_V40 array and its header comment block; the walkAndAssertV40 algorithm and the it bodies — to confirm they stay UNCHANGED; the existing 276-278 delta-exclusion pattern as the model for the re-cut)
    - contracts/modules/DegenerusGameJackpotModule.sol (post-Wave-1)
    - contracts/modules/DegenerusGameLootboxModule.sol (post-Wave-1)
    - contracts/modules/DegenerusGameMintModule.sol (the mint-boost function around :1199 containing coinflip.creditFlip(buyer, lootboxFlipCredit) — locate the enclosing function signature for the extractBody negative assertion)
    - .planning/phases/279-whole-burnie-floor-bur/279-PATTERNS.md (the "TST-BUR-04" section + the "Byte-identity regression carry-forward (SurfaceRegression.test.js)" shared-pattern section — the exact re-cut instructions)
    - .planning/phases/279-whole-burnie-floor-bur/279-CONTEXT.md (D-40N-BUR-MINTBOOST-OUT-01 — the mint-boost out-of-scope basis for the negative assertion)
  </read_first>
  <action>
    (a) Create `test/stat/WholeBurnieFloorInvariant.test.js` (TST-BUR-04):
    - Copy the `makeRng(seedHex)` deterministic keccak-counter PRNG from `LootboxBernoulliEv.test.js` for generating representative budget/spin inputs.
    - The invariant: every observable BURNIE-mint amount producible from the 3 BUR sites is a multiple of `1 ether`. Implement the floor `(x / 10n**18n) * 10n**18n` in JS, sweep N representative inputs (per `279-PATTERNS.md`: representative variance-roll BURNIE amounts for BUR-01; representative `coinBudget`/`cap` pairs for BUR-02; representative `farBudget`/`found` pairs for BUR-03 — use `makeRng` to generate the sweep), and assert `flooredAmount % (10n ** 18n) === 0n` for every swept value. This is a deterministic FLOOR invariant — NO chi-square / Wilson-Hilferty (the BUR floor is not a probabilistic primitive).
    - 3-site source-structural combined gate: `extractBody` each of the 3 functions (`_resolveLootboxCommon`, `_awardDailyCoinToTraitWinners`, `_awardFarFutureCoinJackpot`) and assert each contains its floor expression — reuse the assertions from Task 1 as a combined cross-site gate (a single test that proves all 3 sites floored, complementing the per-site Task 1 files).
    - NEGATIVE cross-site assertion (D-40N-BUR-MINTBOOST-OUT-01): `extractBody` the `DegenerusGameMintModule.sol` function enclosing the `coinflip.creditFlip(buyer, lootboxFlipCredit)` call at `:1199`; `stripLineComments` it; assert the body does NOT contain any `/ 1 ether) * 1 ether` floor expression applied to `lootboxFlipCredit` — proving the mint-boost flip-credit path stayed status-quo fractional (out of v40.0 BUR scope). Assert the body still contains `creditFlip(buyer, lootboxFlipCredit)` so the test is pinned to the right call site.

    (b) Re-cut `SURF_01_PROTECTED_RANGES_V40` in `test/stat/SurfaceRegression.test.js`:
    - This step REQUIRES the Wave 1 contract commit to be landed. Run `git --no-pager diff 6a7455d1 HEAD -- contracts/modules/DegenerusGameJackpotModule.sol` and record the exact OLD-side (`-` prefixed) line numbers — the Phase 279 BUR-02 sites (the `baseAmount`/`extra`/`cursor` deletions + the `amount += 1` block deletion + the `++cursor`/wrap deletions + the NatSpec rewrite in `_awardDailyCoinToTraitWinners`) and the BUR-03 site (the `perWinner` line in `_awardFarFutureCoinJackpot`).
    - The two currently-protected ranges that CONTAIN these delta lines are `JackpotModule L711-1005 (gold-solo + daily-coin bodies)` and `JackpotModule L1014-2177 (BAF jackpot + ticket-distribution bodies)`. Re-cut `SURF_01_PROTECTED_RANGES_V40` so the Phase 279 BUR-02/BUR-03 OLD-side delta lines are EXCLUDED — split each affected range around those delta lines, matching the existing 276-278 delta-exclusion pattern (the protected ranges are "the exact complement of the OLD-side modified-line set" per the existing header-comment discipline).
    - Update the `SURF-01` block-header comment to add a sub-bullet documenting the Phase 279 BUR-02/BUR-03 floor + dead-var-removal delta exclusion. Describe what IS — `feedback_no_history_in_comments.md`.
    - The `walkAndAssertV40` algorithm and the `it` bodies MUST stay byte-identical — only the `SURF_01_PROTECTED_RANGES_V40` array and the header comment change. Confirm `DegenerusGameLootboxModule.sol` is NOT in the SURF-01 protected file set (it is covered at the milestone level by SURF-05, whose `expectedProductionFiles` array already lists both modified modules — no SURF-05 edit needed). Verify no other SURF block trips on the BUR-01 LootboxModule floor line; if one does, surface it to the user in Task 3's diff review rather than silently editing it.
  </action>
  <verify>
    <automated>npx hardhat test test/stat/WholeBurnieFloorInvariant.test.js test/stat/SurfaceRegression.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test/stat/WholeBurnieFloorInvariant.test.js` exists, sweeps N representative inputs and asserts `flooredAmount % (10n ** 18n) === 0n` for every value, includes the 3-site combined source-structural gate, and includes the mint-boost negative assertion proving `DegenerusGameMintModule.sol`'s `lootboxFlipCredit` path has NO whole-BURNIE floor.
    - `test/stat/WholeBurnieFloorInvariant.test.js` uses NO chi-square / Wilson-Hilferty (deterministic floor invariant, not a probabilistic primitive).
    - `SURF_01_PROTECTED_RANGES_V40` in `SurfaceRegression.test.js` is re-cut to exclude the Phase 279 BUR-02/BUR-03 OLD-side delta lines (derived from `git diff 6a7455d1 HEAD`); the `SURF-01` header comment has a Phase 279 sub-bullet.
    - The `walkAndAssertV40` algorithm and the `it` bodies in `SurfaceRegression.test.js` are byte-identical to before this plan; only the range array + header comment changed.
    - `npx hardhat test test/stat/WholeBurnieFloorInvariant.test.js test/stat/SurfaceRegression.test.js` passes with zero failures (the SURF-01 byte-identity gate is green against the post-Wave-1 source).
  </acceptance_criteria>
  <done>
    `test/stat/WholeBurnieFloorInvariant.test.js` proves the whole-BURNIE invariant holds across a representative sweep of all 3 BUR sites and proves the mint-boost path stayed status-quo fractional; `SurfaceRegression.test.js`'s `SURF_01_PROTECTED_RANGES_V40` is re-cut so the v40.0 byte-identity drift gate passes against the post-Wave-1 source with the walk algorithm untouched; both pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the full BUR test wave, present batched diff, await user approval, commit</name>
  <read_first>
    - feedback_no_contract_commits.md
    - feedback_batch_contract_approval.md
    - feedback_never_preapprove_contracts.md
    - feedback_manual_review_before_push.md
    - .planning/phases/278-jackpotmodule-cleanup-ent-05-baf-xorshift-refactor-wrapper-retirement-jpt-clean/278-02-PLAN.md (the Phase 278 test-wave commit checkpoint — mirror its shape)
  </read_first>
  <what-built>
    All Phase 279 test-wave changes from Tasks 1-2: four new test files (`test/unit/LootboxWholeBurnieFloor.test.js`, `test/unit/JackpotNearFutureCoinFloor.test.js`, `test/unit/JackpotFarFutureCoinFloor.test.js`, `test/stat/WholeBurnieFloorInvariant.test.js`) and one modified test file (`test/stat/SurfaceRegression.test.js` — `SURF_01_PROTECTED_RANGES_V40` re-cut + header comment).

    Before presenting the diff: run the full Phase 279 test set plus the surface-regression gate — `npx hardhat test test/unit/LootboxWholeBurnieFloor.test.js test/unit/JackpotNearFutureCoinFloor.test.js test/unit/JackpotFarFutureCoinFloor.test.js test/stat/WholeBurnieFloorInvariant.test.js test/stat/SurfaceRegression.test.js` — and confirm zero failures. Also run the broader `test:stat` tier if `WholeBurnieFloorInvariant.test.js` needs `package.json` `test:stat` wiring: check whether `test/stat/` is directory-globbed by the `test:stat` script (mirror what Phase 278's `278-02-PLAN.md` determined for `test/stat/` file-list vs glob) — if `test/stat/` is an explicit file list, `WholeBurnieFloorInvariant.test.js` must be ADDED to it and `package.json` joins `files_modified`; if it is directory-globbed, no `package.json` edit is needed. Surface which case applies in the diff review.
  </what-built>
  <how-to-verify>
    1. Run `git --no-pager diff --stat test/` (and `package.json` if it was touched) and confirm the changed file set: 4 new test files + `test/stat/SurfaceRegression.test.js` (+ optionally `package.json` for `test:stat` wiring).
    2. Run `git --no-pager diff test/ package.json` and review the full batched diff.
    3. Confirm: the 3 `test/unit/` tests use source-structural `extractBody` proof + JS boundary math (no on-chain `BurnieFloorTester.sol`); `WholeBurnieFloorInvariant.test.js` asserts the `mod(amount, 1 ether) == 0` invariant + the mint-boost negative assertion; `SurfaceRegression.test.js` changes are confined to `SURF_01_PROTECTED_RANGES_V40` + the header comment (walk algorithm + `it` bodies untouched).
    4. Run `npx hardhat test test/unit/LootboxWholeBurnieFloor.test.js test/unit/JackpotNearFutureCoinFloor.test.js test/unit/JackpotFarFutureCoinFloor.test.js test/stat/WholeBurnieFloorInvariant.test.js test/stat/SurfaceRegression.test.js` and confirm zero failures.
    5. Approve the diff explicitly, or describe required changes.
  </how-to-verify>
  <action>
    After explicit user approval ONLY: stage exactly the four new test files + `test/stat/SurfaceRegression.test.js` (+ `package.json` if `test:stat` wiring was required) and commit with:
    `test(279): whole-BURNIE floor regression + invariant sweep + surface-regression re-cut [TST-BUR-01..04]`
    Do NOT push. Do NOT stage contract files or `.planning/` artifacts. If the user requests changes, apply them and re-present the diff — do NOT commit until approved.
  </action>
  <resume-signal>Type "approved" to commit the batched test diff, or describe the changes you want.</resume-signal>
  <acceptance_criteria>
    - `git diff --stat` shows exactly the 4 new test files + `test/stat/SurfaceRegression.test.js` (+ optionally `package.json`) and no other files.
    - `npx hardhat test` on the 5 Phase 279 test files passes with zero failures.
    - The commit is created ONLY after explicit user approval, with message `test(279): whole-BURNIE floor regression + invariant sweep + surface-regression re-cut [TST-BUR-01..04]`.
    - Nothing is pushed; no contract files or `.planning/` artifacts are staged in the test commit.
  </acceptance_criteria>
  <done>
    The user has reviewed and approved the batched test diff; one commit `test(279): whole-BURNIE floor regression + invariant sweep + surface-regression re-cut [TST-BUR-01..04]` exists; the full Phase 279 test wave passes; nothing pushed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract source on disk → test source-structural assertions | The TST-BUR tests read the post-Wave-1 contract source and assert structural properties (floor expression present, ordered correctly, dead vars absent); a stale or wrong-version source read would silently pass against the wrong contract. |
| v39 baseline `6a7455d1` → `SURF_01_PROTECTED_RANGES_V40` re-cut | The surface-regression gate diffs current source against the baseline; an over-broad or under-broad re-cut either masks a real drift or fails on the intended Phase 279 delta. |
| mint-boost path `MintModule:1199` → negative cross-site assertion | The negative assertion must be pinned to the exact `lootboxFlipCredit` call site — a mis-located `extractBody` would assert against the wrong function and give false confidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-279T-01 | Tampering | source-structural proof reads a stale / wrong contract file | mitigate | All `extractBody` calls resolve `MODULE_SOURCE_PATH` to `contracts/modules/...` explicitly (`feedback_contract_locations.md` — never the stale copies elsewhere). The tests `depends_on: ["01"]` so they run against the post-Wave-1 source. Each structural assertion is paired with a positive pin (e.g. the mint-boost test asserts `creditFlip(buyer, lootboxFlipCredit)` IS present before asserting the floor is NOT) so a mis-located `extractBody` fails loudly rather than passing vacuously. |
| T-279T-02 | Repudiation | `SURF_01_PROTECTED_RANGES_V40` re-cut is over-broad (masks unintended drift) or under-broad (fails on the intended delta) | mitigate | The re-cut is derived mechanically from `git diff 6a7455d1 HEAD -- contracts/modules/DegenerusGameJackpotModule.sol` AFTER the Wave 1 commit lands — the excluded lines are exactly the OLD-side delta set, not a hand-estimated range. The walk algorithm + `it` bodies stay byte-identical (only the range array + header comment change), so the gate's logic is unperturbed. Task 3's diff review explicitly confirms the `SurfaceRegression.test.js` change is confined to `SURF_01_PROTECTED_RANGES_V40` + the header comment. The gate is then run (Task 2 + Task 3 verify) and must pass green — an over-broad cut that masked a real drift would not be caught here, but Phase 279 touches only the 3 known function bodies, and the byte-identity proof artifact from Plan 01 (BUR-04) independently confirms storage layout is untouched. |
| T-279T-03 | Information Disclosure | the mint-boost negative assertion gives false confidence (asserts against wrong function) | mitigate | The negative assertion is pinned: it asserts the `extractBody`'d function body CONTAINS `creditFlip(buyer, lootboxFlipCredit)` (the positive pin to the right call site) AND does NOT contain a `/ 1 ether) * 1 ether` floor on `lootboxFlipCredit`. The executor's `read_first` requires locating the enclosing function signature in `DegenerusGameMintModule.sol` around `:1199` before writing the assertion. If the function cannot be located or the positive pin fails, the test fails loudly. |
| T-279T-04 | Denial of Service | the invariant sweep is non-deterministic / flaky | accept | `WholeBurnieFloorInvariant.test.js` uses the `makeRng` deterministic seeded keccak-counter PRNG (fixed seed → reproducible input sequence) and asserts a deterministic FLOOR invariant (`amount % 1 ether == 0`) — there is no probabilistic component, no statistical tolerance, no chi-square. The sweep is fully reproducible; a failure is a real failure, not flake. No timing or external dependency. |
| T-279T-05 | Elevation of Privilege | test wave commits contract or `.planning/` files | mitigate | Task 3's commit step stages EXACTLY the 4 new test files + `SurfaceRegression.test.js` (+ optionally `package.json`); the acceptance criteria assert `git diff --stat` shows no contract files or `.planning/` artifacts. The commit happens ONLY after explicit user approval (`feedback_no_contract_commits.md` + `feedback_never_preapprove_contracts.md`) — test changes are never pre-approved. Nothing is pushed (`feedback_manual_review_before_push.md`). |
</threat_model>

<verification>
- `npx hardhat test` on all 5 Phase 279 test files passes with zero failures.
- `test/unit/LootboxWholeBurnieFloor.test.js` asserts the BUR-01 floor expression is present + ordered before the guard + the `creditFlip` call + reads-floored-value into `LootBoxOpened`, with the 4 JS boundary cases.
- `test/unit/JackpotNearFutureCoinFloor.test.js` asserts the BUR-02 `baseAmount` floor + `\bextra\b`/`\bcursor\b` absence + no `amount += 1` + `randomWord` survival + emit-guard ordering.
- `test/unit/JackpotFarFutureCoinFloor.test.js` asserts the BUR-03 `perWinner` floor ordered before the `== 0` early-bail.
- `test/stat/WholeBurnieFloorInvariant.test.js` asserts `mod(amount, 1 ether) == 0` across the sweep + the mint-boost negative cross-site assertion.
- `SURF_01_PROTECTED_RANGES_V40` re-cut excludes the Phase 279 BUR-02/BUR-03 delta lines; the SURF-01 gate passes; the walk algorithm + `it` bodies are byte-identical.
- Exactly 4 new test files + `SurfaceRegression.test.js` (+ optionally `package.json`) changed; one user-approved commit created; nothing pushed.
</verification>

<success_criteria>
- TST-BUR-01: `test/unit/LootboxWholeBurnieFloor.test.js` proves the `_resolveLootboxCommon` `burnieAmount` floor landed at the correct site with correct ordering and the `LootBoxOpened.burnie` field carries the floored value; 4 boundary cases covered.
- TST-BUR-02: `test/unit/JackpotNearFutureCoinFloor.test.js` proves the `_awardDailyCoinToTraitWinners` `baseAmount` floor landed, the `extra`/`cursor` dead vars are fully gone, `randomWord` survives, and the budget-evaporation path routes through the existing `winner != address(0) && amount != 0` guard.
- TST-BUR-03: `test/unit/JackpotFarFutureCoinFloor.test.js` proves the `_awardFarFutureCoinJackpot` `perWinner` floor landed before the `if (perWinner == 0) return` early-bail so a sub-1-ether `perWinner` cannot reach `creditFlipBatch`.
- TST-BUR-04: `test/stat/WholeBurnieFloorInvariant.test.js` proves every BUR-site BURNIE-mint amount is a 1-ether multiple across a representative sweep, AND proves the mint-boost path at `DegenerusGameMintModule.sol:1199` stayed status-quo fractional (no floor) per D-40N-BUR-MINTBOOST-OUT-01.
- `SurfaceRegression.test.js`'s `SURF_01_PROTECTED_RANGES_V40` is re-cut so the v40.0 byte-identity drift gate passes against the post-Wave-1 source; the walk algorithm + `it` bodies are untouched.
- Full Phase 279 test wave passes; exactly one batched user-approved commit `test(279): ... [TST-BUR-01..04]`; nothing pushed.
</success_criteria>

<output>
After completion, create `.planning/phases/279-whole-burnie-floor-bur/279-02-SUMMARY.md`
</output>
