---
phase: 267-degenerette-producer-5-table-payout-rewrite
phase_number: 267
plan: 267-01
plan_id: 267-01
plan_number: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-CONSTANTS-VERIFY.md
  - contracts/DegenerusTraitUtils.sol
  - contracts/modules/DegenerusGameDegeneretteModule.sol
  - .planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-SUMMARY.md
  - .planning/STATE.md
autonomous: false
milestone: v37.0
milestone_name: Degenerette Recalibration + Maintenance Bundle
phase_status: open

write_policy: "Mixed-shape phase per CONTEXT.md (1 batched USER-APPROVED contract commit + 3 AGENT-COMMITTED chore/planning commits = 4 atomic commits total). Task 3 carries the single batched contract diff (TraitUtils additive + DegeneretteModule rewrite + _distributePayout 3-tier ETH split + betAmount threading) and is `checkpoint:human-verify` non-autonomous per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_wait_for_approval.md`. Tasks 1, 2, 4 are AGENT-COMMITTED autonomous (upstream doc edits, constants verification, phase-close summary). Per `feedback_never_preapprove_contracts.md`, the orchestrator/agent does NOT pre-approve any contract change — Task 3 awaits the user's explicit `approved` string before commit. Per `feedback_manual_review_before_push.md`, the agent does NOT `git push`."

requirements:
  - DGN-01
  - DGN-02
  - DGN-03
  - DGN-04
  - DGN-05
  - DGN-06
  - DGN-07
  - DGN-08
  - DGN-09
  - DGN-10
  - DGN-11
  - DGN-12
  - DGN-13
  - DGN-14
  - DGN-15
  - PAY-SPLIT-01
  - PAY-SPLIT-02
  - PAY-SPLIT-03

# Audit anchors (per CONTEXT.md <domain> + REQUIREMENTS.md baseline)
baseline: 1c0f09132d7439af9881c56fe197f81757f8164a
baseline_short: 1c0f0913
baseline_signal: MILESTONE_V36_AT_HEAD_1c0f09132d7439af9881c56fe197f81757f8164a
v34_baseline: 6b63f6d4daf346a53a1d463790f637308ea8d555
v34_baseline_signal: MILESTONE_V34_AT_HEAD_6b63f6d4daf346a53a1d463790f637308ea8d555

# Locked decisions consumed by this plan (per 267-CONTEXT.md <decisions>)
locked_decisions:
  - id: D-267-SCOPE-01
    summary: "New `packedTraitsDegenerette` helper in TraitUtils. Existing `weightedColorBucket` / `traitFromWord` / `packedTraitsFromSeed` byte-identical (Mint + Jackpot + v34 gold-solo paths UNTOUCHED)."
  - id: D-267-DIST-01
    summary: "Color widths `[16,16,16,16,16,16,16,8]/120`. Producer: base-15 scaling `scaled = uint32((uint64(uint32(rnd)) * 15) >> 32)`; `scaled == 14 ? 7 : scaled >> 1`. Symbol uniform 1/8 from high 32 bits per lane."
  - id: D-267-NORM-01
    summary: "DELETE `_evNormalizationRatio` (L808-851) + its single call site (L965-969). EV-equality enforced by 5-table calibration."
  - id: D-267-EV-TARGET-01
    summary: "Each per-N payout table satisfies `Σ P_N(M) × payout(M) = exactly 100 centi-x`; drift 0.00 bps per N."
  - id: D-267-HERO-01
    summary: "Hero match becomes symbol-only. P(hero match) = 1/8 uniform per quadrant. HERO_PENALTY (9500) + HERO_SCALE (10000) UNCHANGED."
  - id: D-267-HERO-02
    summary: "5 separate `HERO_BOOST_N{0..4}_PACKED` tables; M=2..7 packed 16 bits each (96 bits)."
  - id: D-267-WWXRP-SPLIT-01
    summary: "10/30/30/30 split across buckets 5/6/7/8 preserved. 5 separate `WWXRP_FACTORS_N{0..4}_PACKED` tables (4 × 64-bit factors per uint256). Total ETH bonus EV = exactly 5.000% per N."
  - id: D-267-PRODUCER-API-01
    summary: "Existing 3 TraitUtils functions byte-identical (DGN-14). New `packedTraitsDegenerette(uint256) internal pure returns (uint32)` added alongside (additive only)."
  - id: D-267-COUNTGOLD-01
    summary: "`function _countGoldQuadrants(uint32 ticket) private pure returns (uint8 count)` operating directly on the packed `uint32` ticket via `((ticket >> (q*8 + 3)) & 7) == 7` for q∈{0,1,2,3}. REQUIREMENTS.md DGN-03 wording fixed in Task 1."
  - id: D-267-VISIBILITY-01
    summary: "`packedTraitsDegenerette` visibility = `internal pure` (mirrors `packedTraitsFromSeed`). Library `internal pure` inlined into consumer; zero new public selector. REQUIREMENTS.md DGN-01 + ROADMAP success criteria 1+5 + Phase 271 AUDIT-04 attestation language fixed in Task 1."
  - id: D-267-PAYSPLIT-01
    summary: "ETH-currency 3-tier split rule. Tier 1 (`payout <= 3 * betAmount`) → 100% ETH (no lootbox conversion). Threshold inclusive at exactly 3.0× bet."
  - id: D-267-PAYSPLIT-02
    summary: "3-tier split rule. Tier 2 (`3*bet < payout <= 10*bet`) → ethShare = 2.5*bet (flat floor); Tier 3 (`payout > 10*bet`) → ethShare = payout/4 (existing 25% standard). Implementation: `ethShare = max(2.5*betAmount, payout/4)` with the two bands meeting cleanly at payout = 10*bet."
  - id: D-267-PAYSPLIT-03
    summary: "Pool-cap (`ETH_WIN_CAP_BPS = 1_000` = 10% of futurePool) takes PRECEDENCE over Tier 1 + Tier 2 rules (applied AFTER the split). NatSpec on `_distributePayout` documents precedence in language of CURRENT design only."
  - id: D-267-PAYSPLIT-04
    summary: "Scope = ETH-currency Degenerette quickPlay only. `CURRENCY_BURNIE` (L735-736) + `CURRENCY_WWXRP` (L737-739) branches UNCHANGED."
  - id: D-267-PAYSPLIT-05
    summary: "`_distributePayout` signature extended from `(player, currency, payout, rngWord)` to `(player, currency, betAmount, payout, rngWord)`; `betAmount` is uint128 = per-ticket bet. L656 call site passes `amountPerTicket` (already in scope)."
  - id: D-267-PLAN-01
    summary: "Single multi-task plan (this file). 4 atomic-commit tasks: Task 1 chore (upstream docs); Task 2 chore (constants verification); Task 3 USER-APPROVED contracts; Task 4 chore (phase-close summary)."
  - id: D-267-CONSTVERIFY-01
    summary: "Three-part verification: (1) re-run derive_5_tables.py and capture stdout into 267-01-CONSTANTS-VERIFY.md; (2) grep-assert every script-emitted hex byte-string matches the .sol pasted hex byte-for-byte (5 × QUICK_PLAY_PAYOUTS_N + 5 × QUICK_PLAY_PAYOUT_N_M8 + 5 × HERO_BOOST_N + 5 × WWXRP_FACTORS_N); (3) Phase 268 STAT-05 empirically verifies. Any Task 2 mismatch BLOCKS Task 3."
  - id: D-267-COMMENTS-01
    summary: "NatSpec-first + surgical inline rewrite. (a) NatSpec on every new/rewritten function describing per-N table dispatch in language of CURRENT design only (per `feedback_no_history_in_comments.md` — never references the old normalizer). (b) DELETE §EXACT EV NORMALIZATION prose at L287-298 entirely. (c) Surgically rewrite L239 RTP claim, L262 weights=10, L316 HERO_BOOST_PACKED reference. (d) Block headers L235-251 / L253-268 / L314-322 get fresh §-headers."
  - id: D-267-APPROVAL-02
    summary: "Per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md`. Task 3 presents the single batched contract diff and waits for the user's explicit `approved` string before committing. NEVER pre-approve."

# Goal-backward must-haves (per quality_gate)
must_haves:
  truths:
    - "Phase 267 goal: `contracts/DegenerusTraitUtils.sol` ships a NEW additive `packedTraitsDegenerette(uint256) internal pure returns (uint32)` helper using the per-quadrant near-uniform color distribution `[16,16,16,16,16,16,16,8]/120` (commons 13.33%, gold 6.67%) + uniform 1/8 symbol; the existing `weightedColorBucket` / `traitFromWord` / `packedTraitsFromSeed` bodies stay byte-identical at the v36.0 closure HEAD `1c0f0913`. (DGN-01, DGN-14)"
    - "`contracts/modules/DegenerusGameDegeneretteModule.sol` deletes `_evNormalizationRatio` body (L808-851) + its single call site (L965-969); adds `_countGoldQuadrants(uint32 ticket) private pure returns (uint8 count)` per D-267-COUNTGOLD-01 returning N ∈ {0..4} from the color==7 quadrant count. (DGN-02, DGN-03)"
    - "`_getBasePayoutBps` rewritten to take `(uint8 N, uint8 matches)` and dispatch across 5 per-N payout tables; `_applyHeroMultiplier` rewritten to fire on symbol-only match (color of hero quadrant ignored) and dispatch across 5 per-N hero tables; `_wwxrpBonusRoiForBucket` (or new `_wwxrpFactor`) rewritten to dispatch across 5 per-N WWXRP factor tables. (DGN-04, DGN-07, DGN-09)"
    - "25 packed constants present and grep-citable in DegeneretteModule.sol: 5 × `QUICK_PLAY_PAYOUTS_N{0..4}_PACKED` + 5 × `QUICK_PLAY_PAYOUT_N{0..4}_M8` + 5 × `HERO_BOOST_N{0..4}_PACKED` + 5 × `WWXRP_FACTORS_N{0..4}_PACKED`; values byte-identical to `derive_5_tables.py` Fraction-exact output (verified in 267-01-CONSTANTS-VERIFY.md). (DGN-05, DGN-06, DGN-08, DGN-10)"
    - "4 old single-table constants DELETED (`QUICK_PLAY_BASE_PAYOUTS_PACKED` L240-248, `QUICK_PLAY_BASE_PAYOUT_8_MATCHES` L251, `WWXRP_BONUS_FACTOR_BUCKET5..8` block L265-268, `HERO_BOOST_PACKED` L319) + 2 normalizer constants removed; net constant count 11 → 24. (DGN-11)"
    - "L607 producer callsite swapped from `DegenerusTraitUtils.packedTraitsFromSeed(resultSeed)` to `DegenerusTraitUtils.packedTraitsDegenerette(resultSeed)`. (DGN-12)"
    - "4 stale comment surfaces rewritten in language of CURRENT design only per D-267-COMMENTS-01: L239 RTP claim → '`basePayoutEV = 100 centi-x exact per N; player RTP = roiBps/10000`'; L262 'all weights=10' → 'per-N factors derived from each N's basePayout schedule + binomial-convolution P_N(M) + 10/30/30/30 split'; §EXACT EV NORMALIZATION prose at L287-298 DELETED entirely; L316 `HERO_BOOST_PACKED` reference → per-N `HERO_BOOST_N{0..4}_PACKED` reference. NO 'was/used to/previously/formerly/changed from/removed/deleted' prose anywhere in the rewritten regions per `feedback_no_history_in_comments.md`. (DGN-13)"
    - "Storage layout byte-identical at v37.0 phase-close vs v36.0 baseline `1c0f0913`; zero new storage slots; zero new public/external mutation entry points; zero new external pure entry points (`packedTraitsDegenerette` is `internal pure` per D-267-VISIBILITY-01); zero new admin functions; zero new modifiers; existing `packedTraitsFromSeed` consumer paths in MintModule + JackpotModule UNCHANGED at the byte level. (DGN-15)"
    - "`_distributePayout` signature extended to 5 args `(player, currency, betAmount, payout, rngWord)` with `betAmount` (uint128) inserted between `currency` and `payout` per D-267-PAYSPLIT-05; the L656 call site updated to pass `amountPerTicket` (already in scope). (PAY-SPLIT-05 threading)"
    - "`_distributePayout` ETH-currency branch (replaces L684-740) implements the 3-tier split rule: (a) `payout <= 3 * betAmount` → ethShare = payout, lootboxShare = 0 (PAY-SPLIT-01); (b) `payout > 3 * betAmount` → `ethShare = max(2.5 * betAmount, payout / 4)`, `lootboxShare = payout - ethShare` (PAY-SPLIT-02 floor + standard); (c) pool-cap (`ETH_WIN_CAP_BPS`) applied AFTER the split rule and takes precedence (PAY-SPLIT-03); frozen-pool branch retains its existing solvency-check posture; `CURRENCY_BURNIE` + `CURRENCY_WWXRP` branches BYTE-UNCHANGED. NatSpec documents the rule + precedence in language of CURRENT design only."
    - "Upstream-doc fixes batched in single Task 1 commit: REQUIREMENTS.md DGN-01 visibility + DGN-03 signature + ADD PAY-SPLIT-01..03 section + STAT-07 + AUDIT-02 surface (h) + Coverage line 47→51 + traceability rows; ROADMAP.md Phase 267 inline goal + success criterion 6 (verify present from prior commits `da93a8bf` + `4c6d2326`) + Phase 268 STAT-07 wording + Phase 271 AUDIT-02 surface (h) wording + Phase 271 AUDIT-04 attestation language drop 'ALLOWED-NEW-STATELESS-ENTRY'."
    - "267-01-CONSTANTS-VERIFY.md AGENT-COMMITTED at Task 2 close with reproducible re-derivation evidence: `python3 .planning/notes/degenerette-recalibration/derive_5_tables.py` stdout captured + 25 grep-assertions confirming each emitted hex byte-string matches the planning-note pasted .sol hex byte-for-byte. Any Task 2 mismatch BLOCKS Task 3."
    - "Task 3 single batched contract diff (`contracts/DegenerusTraitUtils.sol` + `contracts/modules/DegenerusGameDegeneretteModule.sol`) presented to user via the `checkpoint:human-verify` gate; agent waits for explicit `approved` string before committing per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_wait_for_approval.md`. Single batched commit lands with subject `feat(267): degenerette producer + 5-table payout rewrite + 3-tier ETH split [DGN-01..15, PAY-SPLIT-01..03]`. NO `git push` per `feedback_manual_review_before_push.md`."
    - "267-01-SUMMARY.md AGENT-COMMITTED at Task 4 close with TWO-subsection commit-readiness register per D-267-CLOSURE-02 carry: §i USER-APPROVED contracts (1 commit at Task 3); §ii USER-APPROVED tests (0 commits — Phase 268 owns tests); §iii AGENT-COMMITTED planning artifacts (4 commits: Task 1 doc fixes, Task 2 constants verify, Task 4 summary, plus the Task 1 PLAN.md authoring commit if not already landed). NO §iv awaiting-approval subsection. 18 of 18 Phase 267 requirements (DGN-01..15 + PAY-SPLIT-01..03) flipped to PASS; ROADMAP Progress flipped 0/0 → 1/1."
    - "Producer chi² + per-N EV exactness empirical proof DEFERRED to Phase 268 (STAT-01..07); cross-surface byte-identity DEFERRED to Phase 268 (SURF-01..06); audit deliverable + closure signal DEFERRED to Phase 271. Phase 267 ships contracts only."

  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: "MODIFIED at Task 1 (AGENT-COMMITTED). Targeted edits: (a) DGN-01 visibility `external pure` → `internal pure` per D-267-VISIBILITY-01; (b) DGN-03 signature `(uint8[4]) internal pure returns (uint8)` → `(uint32 ticket) private pure returns (uint8)` per D-267-COUNTGOLD-01; (c) ADD `### PAY-SPLIT — ETH Payout Currency Split (Phase 267)` section with PAY-SPLIT-01..03 (already verified present at lines 32-36 — re-confirm + correct any drift); (d) ADD STAT-07 row in STAT section (line 46 — verify present); (e) ADD AUDIT-02 surface (h) wording in AUDIT section (line 79 — verify present); (f) Coverage line `47 total` → `51 total` (line 167 — verify present); (g) Traceability rows for PAY-SPLIT-01..03 + STAT-07 (lines 129-138 — verify present). Many of these were authored in commits `da93a8bf` + `4c6d2326` + `84b556c9`; Task 1's role is to ASSERT presence + apply any remaining drift fixes."
      contains: "DGN-03"
      contains_alt: "PAY-SPLIT-01"
    - path: .planning/ROADMAP.md
      provides: "MODIFIED at Task 1 (AGENT-COMMITTED). Targeted edits: (a) Phase 267 inline goal mentions `_distributePayout` 3-tier split + `betAmount` threading per D-267-PAYSPLIT-01..05 (verify present at line 34); (b) Phase 267 success criterion 6 covering `_distributePayout` ETH branch rewrite (verify present at line 43); (c) Phase 268 STAT-07 wording (verify present at line 49); (d) Phase 271 AUDIT-02 surface (h) wording (verify present at line 91); (e) Phase 271 AUDIT-04 attestation drops 'ALLOWED-NEW-STATELESS-ENTRY' since `packedTraitsDegenerette` is `internal pure` (line 91 + 97 — apply if not already corrected); (f) Phase 267 visibility wording in success criteria 1 + 5 corrected to `internal pure` (line 38 + line 42 — apply if not already corrected)."
      contains: "internal pure"
      contains_alt: "3-tier split"
    - path: .planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-CONSTANTS-VERIFY.md
      provides: "NEW at Task 2 (AGENT-COMMITTED). Captures full `python3 .planning/notes/degenerette-recalibration/derive_5_tables.py` stdout + 25 grep assertions confirming byte-identity between script-emitted hex byte-strings and the planning-note pasted .sol hex constants. ANY mismatch flagged with file+line cite + BLOCKS Task 3 commit. Includes a closing summary table: 25 PASS / 0 MISMATCH at HEAD (or X PASS / Y MISMATCH if Task 2 surfaces drift — in which case Task 2 ALSO patches the planning note + re-runs assertions)."
      min_lines: 80
      contains: "QUICK_PLAY_PAYOUTS_N0_PACKED"
      contains_alt: "WWXRP_FACTORS_N4_PACKED"
    - path: contracts/DegenerusTraitUtils.sol
      provides: "MODIFIED at Task 3 (USER-APPROVED batched commit). Additive only: NEW `packedTraitsDegenerette(uint256 rand) internal pure returns (uint32)` helper + NEW `_degTrait(uint64 rnd) private pure returns (uint8)` helper inserted after the existing `packedTraitsFromSeed` body (post-line-178). Existing 3 functions (`weightedColorBucket`, `traitFromWord`, `packedTraitsFromSeed`) bodies BYTE-IDENTICAL to v36.0 closure HEAD `1c0f0913` per DGN-14. Library declaration unchanged."
      contains: "function packedTraitsDegenerette"
      contains_alt: "function _degTrait"
    - path: contracts/modules/DegenerusGameDegeneretteModule.sol
      provides: "MODIFIED at Task 3 (USER-APPROVED batched commit). Primary mutation surface. Specific edits: (1) DELETE constants `QUICK_PLAY_BASE_PAYOUTS_PACKED` (L240-248) + `QUICK_PLAY_BASE_PAYOUT_8_MATCHES` (L251) + `WWXRP_BONUS_FACTOR_BUCKET5..8` block (L265-268) + `HERO_BOOST_PACKED` (L319). (2) ADD 25 per-N packed constants in the 235-322 region: 5 × `QUICK_PLAY_PAYOUTS_N{0..4}_PACKED` + 5 × `QUICK_PLAY_PAYOUT_N{0..4}_M8` + 5 × `HERO_BOOST_N{0..4}_PACKED` + 5 × `WWXRP_FACTORS_N{0..4}_PACKED`. (3) DELETE `_evNormalizationRatio` body (L808-851) + its call site at L965-969. (4) ADD `_countGoldQuadrants(uint32 ticket) private pure returns (uint8 count)` per D-267-COUNTGOLD-01. (5) REWRITE `_getBasePayoutBps` to `(uint8 N, uint8 matches)` per-N dispatch. (6) REWRITE `_applyHeroMultiplier` for symbol-only match + per-N dispatch. (7) REWRITE `_wwxrpBonusRoiForBucket` (or replace with `_wwxrpFactor(uint8 N, uint8 bucket)`) for per-N dispatch. (8) REWRITE `_fullTicketPayout` body to thread N through hero + WWXRP factor lookups + drop `_evNormalizationRatio` invocation. (9) SWAP L607 producer call from `packedTraitsFromSeed` to `packedTraitsDegenerette`. (10) UPDATE L656 call site to pass `amountPerTicket` as new 3rd arg. (11) REWRITE `_distributePayout` signature to `(address player, uint8 currency, uint128 betAmount, uint256 payout, uint256 rngWord)` + REWRITE its body's CURRENCY_ETH branch (L684-740) for 3-tier split rule + new NatSpec; CURRENCY_BURNIE (L735-736) + CURRENCY_WWXRP (L737-739) branches BYTE-UNCHANGED. (12) REWRITE block headers + comments at L235-251 / L253-268 / L287-298 / L314-322 + L239 + L262 + L316 per D-267-COMMENTS-01."
      contains: "_countGoldQuadrants"
      contains_alt: "uint128 betAmount"
    - path: .planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-SUMMARY.md
      provides: "NEW at Task 4 (AGENT-COMMITTED). Phase-closure summary mirroring v36 `266-01-SUMMARY.md` format: frontmatter (`requirements-completed: [DGN-01..15, PAY-SPLIT-01..03]` + closure_signal placeholder pointing to Phase 271) + per-task atomic-commit log (Task 1 `chore(267): docs upstream fixes for v37.0 phase 267 [DGN-01, DGN-03, PAY-SPLIT, STAT-07]` + Task 2 `chore(267): constants verification — derive_5_tables.py byte-identity proof` + Task 3 `feat(267): degenerette producer + 5-table payout rewrite + 3-tier ETH split [DGN-01..15, PAY-SPLIT-01..03]` + Task 4 `docs(267): phase 267 summary + commit-readiness register`) + per-REQ tally for 18 Phase 267 requirements (each REQ ID → file+line evidence + verifying grep recipe) + cross-phase cross-cite density (forward to Phase 268 STAT-01..07 + SURF-01..06; forward to Phase 271 AUDIT-01..06 + REG-01..04) + project-feedback-rules-honored table + TWO-subsection commit-readiness register per D-267-CLOSURE-02 carry."
      min_lines: 100
      contains: "DGN-01..15"
      contains_alt: "PAY-SPLIT-01..03"
    - path: .planning/STATE.md
      provides: "MODIFIED at Task 4 (AGENT-COMMITTED). Update Current Position (Phase 267 SHIPPED; Phase 268 next), last_activity bullet, progress.completed_phases (0 → 1), progress.completed_plans (0 → 1), progress.percent (0 → 20). NO milestone-level closure flips (those land in Phase 271 — terminal phase)."
      contains: "Phase 267 SHIPPED"

  key_links:
    - from: ".planning/REQUIREMENTS.md DGN-03 (Task 1 fix)"
      to: "contracts/modules/DegenerusGameDegeneretteModule.sol _countGoldQuadrants signature (Task 3)"
      via: "Task 1 corrects DGN-03 wording (`(uint8[4]) internal pure` → `(uint32 ticket) private pure returns (uint8)`) BEFORE Task 3 implements; doc-vs-impl coherence enforced at the requirement-line level per D-267-COUNTGOLD-01"
      pattern: "_countGoldQuadrants\\(uint32 ticket\\) private pure"
    - from: ".planning/REQUIREMENTS.md DGN-01 (Task 1 fix)"
      to: "contracts/DegenerusTraitUtils.sol packedTraitsDegenerette visibility (Task 3)"
      via: "Task 1 corrects DGN-01 visibility `external pure` → `internal pure` BEFORE Task 3 implements; doc-vs-impl coherence enforced per D-267-VISIBILITY-01; library `internal pure` is inlined into consumer with zero new public selector"
      pattern: "function packedTraitsDegenerette\\(uint256 [a-z]+\\) internal pure returns \\(uint32\\)"
    - from: ".planning/notes/degenerette-recalibration/derive_5_tables.py (Task 2 re-run)"
      to: ".planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-CONSTANTS-VERIFY.md (Task 2 output)"
      via: "Task 2 captures stdout via `python3 derive_5_tables.py > 267-01-CONSTANTS-VERIFY.md` (or appended); 25 grep assertions confirm script-emitted hex byte-string matches the planning-note .sol pasted hex byte-for-byte"
      pattern: "QUICK_PLAY_PAYOUTS_N[0-4]_PACKED|QUICK_PLAY_PAYOUT_N[0-4]_M8|HERO_BOOST_N[0-4]_PACKED|WWXRP_FACTORS_N[0-4]_PACKED"
    - from: ".planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-CONSTANTS-VERIFY.md (Task 2)"
      to: "contracts/modules/DegenerusGameDegeneretteModule.sol 25 per-N packed constants (Task 3)"
      via: "Task 2 BLOCKS Task 3 if any of 25 grep assertions fails. Task 3 pastes constants from `.planning/notes/2026-05-10-degenerette-payout-recalibration.md` §'Concrete file changes' (already verified byte-identical to script output by Task 2). Phase 268 STAT-05 empirically verifies on-chain dispatch."
      pattern: "QUICK_PLAY_PAYOUTS_N0_PACKED = 0x0001a42c000051f1000011da00000654000001ff000000cc0000000000000000"
    - from: "contracts/modules/DegenerusGameDegeneretteModule.sol L656 (Task 3 update)"
      to: "contracts/modules/DegenerusGameDegeneretteModule.sol _distributePayout signature (Task 3 rewrite)"
      via: "L656 callsite already has `amountPerTicket` in scope (uint128, decoded from packed bet at line 581-ish). Task 3 inserts it as new 3rd arg `betAmount`: `_distributePayout(player, currency, amountPerTicket, payout, lootboxWord)` — the 5-arg signature carries `betAmount` between `currency` and `payout` per D-267-PAYSPLIT-05"
      pattern: "_distributePayout\\(player, currency, amountPerTicket, payout, lootboxWord\\)"
    - from: "contracts/modules/DegenerusGameDegeneretteModule.sol L607 (Task 3 swap)"
      to: "contracts/DegenerusTraitUtils.sol packedTraitsDegenerette (Task 3 add)"
      via: "DegeneretteModule consumes the new producer at L607: `uint32 resultTicket = DegenerusTraitUtils.packedTraitsDegenerette(resultSeed)` — the per-quadrant near-uniform color distribution that the 5 per-N payout tables are calibrated against per D-267-DIST-01 + D-267-EV-TARGET-01"
      pattern: "DegenerusTraitUtils\\.packedTraitsDegenerette\\(resultSeed\\)"

# Canonical grep recipes (executors copy verbatim — do NOT re-derive)
canonical_grep_recipes:
  task1_doc_drift_check:
    - 'grep -n "external pure returns (uint32)" .planning/REQUIREMENTS.md  # Expected: ZERO hits in DGN-01 row after Task 1.'
    - 'grep -n "uint8\\[4\\]) internal pure returns (uint8)" .planning/REQUIREMENTS.md  # Expected: ZERO hits in DGN-03 row after Task 1.'
    - 'grep -cE "^- \\[ \\] \\*\\*PAY-SPLIT-0[1-3]\\*\\*" .planning/REQUIREMENTS.md  # Expected: 3 (PAY-SPLIT-01 + 02 + 03 rows present).'
    - 'grep -c "STAT-07" .planning/REQUIREMENTS.md  # Expected: >= 2 (definition row + traceability row).'
    - 'grep -c "ALLOWED-NEW-STATELESS-ENTRY" .planning/ROADMAP.md  # Expected: 0 after Task 1 (Phase 271 AUDIT-04 attestation language drops the phrase per D-267-VISIBILITY-01).'
    - 'grep -c "51 total" .planning/REQUIREMENTS.md  # Expected: 1 (Coverage line bumped from 47 → 51).'
  task2_constants_verify:
    - 'cd $(pwd) && python3 .planning/notes/degenerette-recalibration/derive_5_tables.py > /tmp/267-derive-stdout.txt 2>&1 && wc -l /tmp/267-derive-stdout.txt  # Expected: > 50 lines, no Python error.'
    - 'for K in QUICK_PLAY_PAYOUTS_N0_PACKED QUICK_PLAY_PAYOUTS_N1_PACKED QUICK_PLAY_PAYOUTS_N2_PACKED QUICK_PLAY_PAYOUTS_N3_PACKED QUICK_PLAY_PAYOUTS_N4_PACKED; do grep -F "$K" /tmp/267-derive-stdout.txt | grep -oE "0x[0-9a-f]{64}"; grep -F "$K" .planning/notes/2026-05-10-degenerette-payout-recalibration.md | grep -oE "0x[0-9a-f]{64}"; done  # Expected: each pair of hex strings IDENTICAL.'
    - 'for K in QUICK_PLAY_PAYOUT_N0_M8 QUICK_PLAY_PAYOUT_N1_M8 QUICK_PLAY_PAYOUT_N2_M8 QUICK_PLAY_PAYOUT_N3_M8 QUICK_PLAY_PAYOUT_N4_M8; do grep -F "$K" /tmp/267-derive-stdout.txt; grep -F "$K" .planning/notes/2026-05-10-degenerette-payout-recalibration.md; done  # Expected: each pair has identical decimal value.'
    - 'for K in HERO_BOOST_N0_PACKED HERO_BOOST_N1_PACKED HERO_BOOST_N2_PACKED HERO_BOOST_N3_PACKED HERO_BOOST_N4_PACKED; do grep -F "$K" /tmp/267-derive-stdout.txt | grep -oE "0x[0-9a-f]{24}"; grep -F "$K" .planning/notes/2026-05-10-degenerette-payout-recalibration.md | grep -oE "0x[0-9a-f]{24}"; done  # Expected: each pair IDENTICAL.'
    - 'for K in WWXRP_FACTORS_N0_PACKED WWXRP_FACTORS_N1_PACKED WWXRP_FACTORS_N2_PACKED WWXRP_FACTORS_N3_PACKED WWXRP_FACTORS_N4_PACKED; do grep -F "$K" /tmp/267-derive-stdout.txt | grep -oE "0x[0-9a-f]{64}"; grep -F "$K" .planning/notes/2026-05-10-degenerette-payout-recalibration.md | grep -oE "0x[0-9a-f]{64}"; done  # Expected: each pair IDENTICAL.'
  task3_contract_invariants:
    # New helpers + visibility lock
    - 'grep -nE "function packedTraitsDegenerette\\(uint256 [a-z]+\\) internal pure returns \\(uint32\\)" contracts/DegenerusTraitUtils.sol  # Expected: 1 hit per D-267-VISIBILITY-01.'
    - 'grep -nE "function _degTrait\\(uint64 [a-z]+\\) private pure returns \\(uint8\\)" contracts/DegenerusTraitUtils.sol  # Expected: 1 hit per D-267-DIST-01.'
    - 'grep -nE "function _countGoldQuadrants\\(uint32 ticket\\) private pure returns \\(uint8 count\\)" contracts/modules/DegenerusGameDegeneretteModule.sol  # Expected: 1 hit per D-267-COUNTGOLD-01.'
    # Existing TraitUtils functions byte-identical (DGN-14, SURF-01)
    - 'git diff 1c0f09132d7439af9881c56fe197f81757f8164a..HEAD -- contracts/DegenerusTraitUtils.sol | grep -E "^[+-].*function (weightedColorBucket|traitFromWord|packedTraitsFromSeed)"  # Expected: ZERO output (no +/- lines touching the existing 3 functions; only additive changes).'
    # Mint + Jackpot + Lootbox + EntropyLib byte-identical (SURF-02..04 carry)
    - 'git diff 1c0f09132d7439af9881c56fe197f81757f8164a..HEAD -- contracts/modules/DegenerusGameMintModule.sol contracts/modules/DegenerusGameJackpotModule.sol contracts/modules/DegenerusGameLootboxModule.sol contracts/libraries/EntropyLib.sol contracts/libraries/JackpotBucketLib.sol contracts/storage/GameStorage.sol  # Expected: empty output.'
    # Normalizer fully removed (DGN-02)
    - 'grep -nE "_evNormalizationRatio" contracts/modules/DegenerusGameDegeneretteModule.sol  # Expected: 0 hits.'
    # 25 per-N packed constants present (DGN-05/06/08/10)
    - 'grep -cE "QUICK_PLAY_PAYOUTS_N[0-4]_PACKED" contracts/modules/DegenerusGameDegeneretteModule.sol  # Expected: >= 5 (each constant declared once + may be referenced in dispatch).'
    - 'grep -cE "QUICK_PLAY_PAYOUT_N[0-4]_M8" contracts/modules/DegenerusGameDegeneretteModule.sol  # Expected: >= 5.'
    - 'grep -cE "HERO_BOOST_N[0-4]_PACKED" contracts/modules/DegenerusGameDegeneretteModule.sol  # Expected: >= 5.'
    - 'grep -cE "WWXRP_FACTORS_N[0-4]_PACKED" contracts/modules/DegenerusGameDegeneretteModule.sol  # Expected: >= 5.'
    # Old single-table constants removed (DGN-11)
    - 'grep -cE "QUICK_PLAY_BASE_PAYOUTS_PACKED|QUICK_PLAY_BASE_PAYOUT_8_MATCHES|WWXRP_BONUS_FACTOR_BUCKET[5-8]|HERO_BOOST_PACKED" contracts/modules/DegenerusGameDegeneretteModule.sol  # Expected: 0.'
    # Producer callsite swap (DGN-12)
    - 'grep -nE "DegenerusTraitUtils\\.packedTraitsDegenerette\\(resultSeed\\)" contracts/modules/DegenerusGameDegeneretteModule.sol  # Expected: 1 hit at L607-ish.'
    - 'grep -nE "DegenerusTraitUtils\\.packedTraitsFromSeed\\(resultSeed\\)" contracts/modules/DegenerusGameDegeneretteModule.sol  # Expected: 0 (callsite swapped).'
    # _distributePayout 5-arg signature + threading (PAY-SPLIT-05)
    - 'grep -nE "function _distributePayout\\(\\s*$" contracts/modules/DegenerusGameDegeneretteModule.sol; grep -nE "uint128 betAmount,?" contracts/modules/DegenerusGameDegeneretteModule.sol  # Expected: signature contains `uint128 betAmount,` between currency and payout.'
    - 'grep -nE "_distributePayout\\(player, currency, amountPerTicket, payout, lootboxWord\\)" contracts/modules/DegenerusGameDegeneretteModule.sol  # Expected: 1 hit at L656-ish (call site updated).'
    # 3-tier ETH split rule (PAY-SPLIT-01..03) — accept either explicit if/else or max() form
    - 'grep -nE "betAmount.*\\* 3|3 \\* (uint256\\()?betAmount|threeBet" contracts/modules/DegenerusGameDegeneretteModule.sol  # Expected: >= 1 hit inside _distributePayout for the 3× threshold.'
    - 'grep -nE "betAmount.*\\* 5\\) / 2|5 \\* betAmount\\) / 2|2 \\* betAmount \\+ \\(?betAmount >> 1|minEth" contracts/modules/DegenerusGameDegeneretteModule.sol  # Expected: >= 1 hit for the 2.5× floor.'
    - 'grep -nE "ETH_WIN_CAP_BPS" contracts/modules/DegenerusGameDegeneretteModule.sol  # Expected: still present (pool-cap precedence per PAY-SPLIT-03).'
    - 'grep -nE "PayoutCapped" contracts/modules/DegenerusGameDegeneretteModule.sol  # Expected: still emitted (existing event reused).'
    # CURRENCY_BURNIE + CURRENCY_WWXRP branches byte-unchanged (PAY-SPLIT-04)
    - 'grep -nE "coin\\.mintForGame\\(player, payout\\)" contracts/modules/DegenerusGameDegeneretteModule.sol  # Expected: 1 hit (existing).'
    - 'grep -nE "wwxrp\\.mintPrize\\(player, payout\\)" contracts/modules/DegenerusGameDegeneretteModule.sol  # Expected: 1 hit (existing).'
    # Stale-history prose absent in rewritten regions (D-267-COMMENTS-01 + feedback_no_history_in_comments.md)
    - 'grep -nE "(was |used to|previously|formerly|changed from|removed |deleted )" contracts/modules/DegenerusGameDegeneretteModule.sol contracts/DegenerusTraitUtils.sol | grep -vE "^[^:]+:[0-9]+:\\s*//.*(SPDX|@author|@notice|@dev|pragma)" | head -10  # Expected: ZERO hits in NatSpec / inline comments touched by this phase. (Non-comment matches in code identifiers like `wRemoved` are acceptable; the grep is a tripwire — manually triage any hit.)'
    # Stale RTP / weights=10 / EXACT EV NORMALIZATION prose absent (DGN-13)
    - 'grep -cE "99\\.99% RTP|all weights=10|EXACT EV NORMALIZATION" contracts/modules/DegenerusGameDegeneretteModule.sol  # Expected: 0.'
    # Storage layout byte-identical (DGN-15)
    - 'git diff 1c0f09132d7439af9881c56fe197f81757f8164a..HEAD -- contracts/storage/GameStorage.sol  # Expected: empty.'
    # Zero new public/external mutation entry points + zero new external pure entries (DGN-15 + D-267-VISIBILITY-01)
    - 'git diff 1c0f09132d7439af9881c56fe197f81757f8164a..HEAD -- contracts/ | grep -E "^\\+.*function .* (public|external)" | grep -vE "internal|private"  # Expected: ZERO new public/external function declarations.'
  task4_phase_close:
    - 'test -f .planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-SUMMARY.md && grep -c "DGN-01" .planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-SUMMARY.md  # Expected: >= 1.'
    - 'grep -cE "PAY-SPLIT-0[1-3]" .planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-SUMMARY.md  # Expected: >= 3.'
    - 'grep -c "Phase 267 SHIPPED" .planning/STATE.md  # Expected: 1.'

tags:
  - degenerette
  - payout-rewrite
  - 5-table-dispatch
  - producer-swap
  - eth-split-rule
  - betAmount-threading
  - additive-traitutils
  - DGN-01
  - DGN-02
  - DGN-03
  - DGN-04
  - DGN-05
  - DGN-06
  - DGN-07
  - DGN-08
  - DGN-09
  - DGN-10
  - DGN-11
  - DGN-12
  - DGN-13
  - DGN-14
  - DGN-15
  - PAY-SPLIT-01
  - PAY-SPLIT-02
  - PAY-SPLIT-03
---

<objective>

Produce the v37.0 Phase 267 contract+constant rewrite for the Degenerette payout recalibration: (1) add a NEW additive `packedTraitsDegenerette(uint256) internal pure returns (uint32)` producer + private `_degTrait` helper to `contracts/DegenerusTraitUtils.sol` per D-267-DIST-01 + D-267-VISIBILITY-01 + D-267-PRODUCER-API-01 (existing 3 functions byte-identical per DGN-14); (2) rewrite `contracts/modules/DegenerusGameDegeneretteModule.sol` for 5 per-N (gold-quadrant-count) payout/hero/WWXRP table dispatch indexed by a NEW `_countGoldQuadrants(uint32 ticket) private pure returns (uint8 count)` helper per D-267-COUNTGOLD-01; DELETE `_evNormalizationRatio` (L808-851) + its single call site (L965-969) per D-267-NORM-01; replace 4 single-table + 2 normalizer constants with 25 per-N packed constants (net 11 → 24); make hero match SYMBOL-ONLY per D-267-HERO-01; (3) rewrite `_distributePayout` to a 5-arg signature `(player, currency, betAmount, payout, rngWord)` per D-267-PAYSPLIT-05 + implement the 3-tier ETH split rule per D-267-PAYSPLIT-01..03 (≤3× bet → 100% ETH; 3-10× bet → 2.5× bet ETH floor + remainder lootbox; >10× bet → existing 25/75 split; pool-cap takes precedence on top); (4) batch all upstream-doc fixes (REQUIREMENTS.md DGN-01 visibility + DGN-03 signature + PAY-SPLIT/STAT-07/AUDIT-02(h) section presence + Coverage 47→51; ROADMAP.md Phase 267 inline goal + success criterion 6 + Phase 268 STAT-07 + Phase 271 AUDIT-02(h) + AUDIT-04 attestation drop "ALLOWED-NEW-STATELESS-ENTRY") into a single Task 1 chore commit per D-267-PLAN-01; (5) reproduce + verify all 25 packed constants byte-identical via `derive_5_tables.py` re-run captured into `267-01-CONSTANTS-VERIFY.md` per D-267-CONSTVERIFY-01 (Task 2 BLOCKS Task 3 on mismatch); (6) present the single batched contract diff at the Task 3 `checkpoint:human-verify` gate per D-267-APPROVAL-02 + `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_wait_for_approval.md` and wait for the user's explicit `approved` string before committing; (7) close the phase via Task 4 with `267-01-SUMMARY.md` + STATE.md flips per D-267-CLOSURE-02 carry (TWO-subsection commit-readiness register; NO awaiting-approval subsection).

**Per CONTEXT.md locked decisions (load-bearing):**

- D-267-COUNTGOLD-01: `_countGoldQuadrants(uint32 ticket) private pure returns (uint8 count)` (operates on packed ticket; NO unpack-then-repack).
- D-267-VISIBILITY-01: `packedTraitsDegenerette` is `internal pure` (mirrors `packedTraitsFromSeed`); ZERO new public selector.
- D-267-PAYSPLIT-01..05: ETH 3-tier split rule + 2.5× floor + pool-cap precedence + ETH-only scope + 5-arg signature.
- D-267-CONSTVERIFY-01: Task 2 BLOCKS Task 3 on any of 25 grep-mismatches.
- D-267-COMMENTS-01: NatSpec-first + surgical inline rewrite + ZERO history prose per `feedback_no_history_in_comments.md`.
- D-267-APPROVAL-02: Task 3 awaits explicit `approved` string per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_wait_for_approval.md`.

**Out of scope (per CONTEXT.md `<deferred>` + REQUIREMENTS.md Out of Scope):**

- Producer chi² + per-N EV exactness empirical proof → Phase 268 (STAT-01..07).
- Cross-surface byte-identity tests → Phase 268 (SURF-01..06).
- Lootbox dead-branch cleanup + SURF-05 gas-pin re-pinning → Phase 269.
- Post-v32.0 deferred-commit adversarial sub-audit → Phase 270.
- Audit deliverable + closure signal `MILESTONE_V37_AT_HEAD_<sha>` + KNOWN-ISSUES.md walkthrough + ROADMAP/STATE/MILESTONES milestone-level closure flips → Phase 271 (terminal).

**Output:** 4 atomic commits (1 batched USER-APPROVED contract commit at Task 3 + 3 AGENT-COMMITTED chore/planning commits at Tasks 1, 2, 4) + 18 of 18 Phase 267 requirements (DGN-01..15 + PAY-SPLIT-01..03) flipped to PASS at phase close.

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-CONTEXT.md
@.planning/notes/2026-05-10-degenerette-payout-recalibration.md
@.planning/notes/degenerette-recalibration/derive_5_tables.py
@contracts/DegenerusTraitUtils.sol
@contracts/modules/DegenerusGameDegeneretteModule.sol
@KNOWN-ISSUES.md
@./CLAUDE.md

<!-- Project feedback rules — load-bearing for Phase 267 per CONTEXT.md citations -->
@$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_contract_commits.md
@$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_batch_contract_approval.md
@$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_never_preapprove_contracts.md
@$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_wait_for_approval.md
@$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_manual_review_before_push.md
@$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md
@$HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_contract_locations.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from contracts/DegenerusTraitUtils.sol HEAD `1c0f0913` + contracts/modules/DegenerusGameDegeneretteModule.sol HEAD `1c0f0913`. Executor uses these directly — no codebase exploration needed. -->

From contracts/DegenerusTraitUtils.sol (HEAD `1c0f0913`, lines 115-178):
```solidity
library DegenerusTraitUtils {
    function weightedColorBucket(uint32 rnd) internal pure returns (uint8) {
        // BYTE-IDENTICAL — DO NOT TOUCH (DGN-14, Phase 268 SURF-01)
    }

    function traitFromWord(uint64 rnd) internal pure returns (uint8) {
        // BYTE-IDENTICAL — DO NOT TOUCH (DGN-14, Phase 268 SURF-01)
    }

    function packedTraitsFromSeed(uint256 rand) internal pure returns (uint32) {
        // BYTE-IDENTICAL — DO NOT TOUCH (DGN-14, Phase 268 SURF-01).
        // Layout reference for the new sibling helper:
        //   uint8 traitA = traitFromWord(uint64(rand));
        //   uint8 traitB = traitFromWord(uint64(rand >> 64))  | 64;
        //   uint8 traitC = traitFromWord(uint64(rand >> 128)) | 128;
        //   uint8 traitD = traitFromWord(uint64(rand >> 192)) | 192;
        //   return uint32(traitA) | (uint32(traitB) << 8) | (uint32(traitC) << 16) | (uint32(traitD) << 24);
    }

    // ── ADD AFTER LINE 178 (Task 3) per CONTEXT.md <specifics> ──
    function packedTraitsDegenerette(uint256 rand) internal pure returns (uint32);
    function _degTrait(uint64 rnd) private pure returns (uint8);
}
```

From contracts/modules/DegenerusGameDegeneretteModule.sol (HEAD `1c0f0913`, key constants + signatures):
```solidity
// Constants present at L185-321 (excerpt):
uint16 private constant ETH_ROI_BONUS_BPS = 500;            // L185 — UNCHANGED
uint16 private constant ETH_WIN_CAP_BPS = 1_000;            // L196 — UNCHANGED (used by PAY-SPLIT-03)
uint8  private constant CURRENCY_ETH = 0;                   // L208 — UNCHANGED
uint8  private constant CURRENCY_BURNIE = 1;                // L211 — UNCHANGED
uint8  private constant CURRENCY_WWXRP = 3;                 // L214 — UNCHANGED
uint16 private constant HERO_PENALTY = 9500;                // L320 — UNCHANGED
uint16 private constant HERO_SCALE = 10_000;                // L321 — UNCHANGED
uint256 private constant WWXRP_BONUS_FACTOR_SCALE = 1_000_000;  // L264 — UNCHANGED

// To DELETE (Task 3):
uint256 private constant QUICK_PLAY_BASE_PAYOUTS_PACKED = ...;       // L240-248
uint256 private constant QUICK_PLAY_BASE_PAYOUT_8_MATCHES = ...;     // L251
uint256 private constant WWXRP_BONUS_FACTOR_BUCKET5 = 1_531_388;     // L265
uint256 private constant WWXRP_BONUS_FACTOR_BUCKET6 = 13_016_797;    // L266
uint256 private constant WWXRP_BONUS_FACTOR_BUCKET7 = 57_745_766;    // L267
uint256 private constant WWXRP_BONUS_FACTOR_BUCKET8 = 30_027_799;    // L268
uint256 private constant HERO_BOOST_PACKED = 0x27b628c12a942e3937565bcc;  // L319

// Existing function signatures (Task 3 modifies):
function _evNormalizationRatio(uint32, uint32) private pure returns (uint256, uint256);  // L808-851 — DELETE
function _countMatches(uint32, uint32) private pure returns (uint8);                     // L857-887 — UNCHANGED
function _wwxrpBonusBucket(uint8) private pure returns (uint8);                          // L891-896 — UNCHANGED
function _wwxrpBonusRoiForBucket(uint8, uint256) private pure returns (uint256);         // L899-911 — REWRITE for per-N or REPLACE with _wwxrpFactor(uint8 N, uint8 bucket)
function _fullTicketPayout(uint32, uint32, uint8, uint8, uint128, uint256, uint256, bool, uint8) private pure returns (uint256);  // L923-983 — REWRITE body for N threading + drop _evNormalizationRatio call
function _applyHeroMultiplier(uint256, uint32, uint32, uint8, uint8) private pure returns (uint256);  // L988-1011 — REWRITE for symbol-only + per-N
function _getBasePayoutBps(uint8) private pure returns (uint256);                        // L1016-1021 — REWRITE to (uint8 N, uint8 matches)

// _distributePayout signature TRANSFORM (D-267-PAYSPLIT-05):
// BEFORE (L684-689):
function _distributePayout(address player, uint8 currency, uint256 payout, uint256 rngWord) private;
// AFTER (Task 3):
function _distributePayout(address player, uint8 currency, uint128 betAmount, uint256 payout, uint256 rngWord) private;

// CURRENCY_BURNIE + CURRENCY_WWXRP branches BYTE-UNCHANGED (D-267-PAYSPLIT-04):
//   } else if (currency == CURRENCY_BURNIE) { coin.mintForGame(player, payout); }
//   else if (currency == CURRENCY_WWXRP)   { wwxrp.mintPrize(player, payout); }

// Frozen-pool branch retains existing solvency-check posture (revert-on-insufficient).

// Producer call site (DGN-12):
// L607: uint32 resultTicket = DegenerusTraitUtils.packedTraitsFromSeed(resultSeed);
//    →  uint32 resultTicket = DegenerusTraitUtils.packedTraitsDegenerette(resultSeed);

// _distributePayout call site (D-267-PAYSPLIT-05 threading):
// L656: _distributePayout(player, currency, payout, lootboxWord);
//    →  _distributePayout(player, currency, amountPerTicket, payout, lootboxWord);
//    (`amountPerTicket` is uint128, decoded earlier in the spin loop and already in scope)
```

From .planning/notes/2026-05-10-degenerette-payout-recalibration.md §"Concrete file changes" (paste-ready Solidity bodies for ALL Task 3 edits — source of truth) and §"CONSTANT REWRITE" block (paste-ready 25 hex byte-strings for the new packed constants).

</interfaces>
</context>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Player → packed bet entry (`placeFullTicketBet`-equivalent) | Player commits `(customTicket, ticketCount, currency, amountPerTicket, hero)` at request time; `playerTicket` is the chosen pick (4 quadrants of `[QQ][CCC][SSS]`); RNG (`rngWord`) is post-commit Chainlink VRF; player has zero post-commit influence over `rngWord` (DGN-12 producer reads VRF-derived bits per spin). |
| VRF fulfillment → `_resolveFullTicket` per-spin keccak → `packedTraitsDegenerette(resultSeed)` → result-ticket trait | Per-spin seed = `keccak256(abi.encodePacked(rngWord, index, spinIdx, QUICK_PLAY_SALT))`; `packedTraitsDegenerette` consumes 4 × 64-bit lanes for color (low 32) + symbol (high 32). Color via base-15 scaling `scaled = uint32((uint64(uint32(rnd)) * 15) >> 32)` → bias < 2.3e-10. Backward-trace per `feedback_rng_backward_trace.md`: every consumer reads VRF-derived high-entropy bits unknown at input commitment time. |
| Result-ticket → match count → `_countGoldQuadrants(playerTicket)` → 5-table dispatch | `_countGoldQuadrants` operates on PLAYER ticket (not result ticket) — N is a function of the player's pick, computed deterministically from the customTicket they committed at request. No RNG dependency in the table-index calculation. |
| Match dispatch + per-N tables + 3-tier ETH split (`_distributePayout`) | Payout = `bet × basePayout_N(M) × roiBps / 1_000_000`; ETH-currency 3-tier split rule selects ethShare per `payout` vs `betAmount` thresholds (3× and 10× bet); pool-cap (`ETH_WIN_CAP_BPS`) takes precedence after the split. `betAmount` (uint128) is the per-ticket bet committed at request; not player-controllable post-commit. |
| Frozen-pool branch ↔ unfrozen branch | Frozen path debits pending future pool with revert-on-insufficient; unfrozen path debits live future pool with `ETH_WIN_CAP_BPS` cap. Boundary preserved at the byte level except the ethShare/lootboxShare values feeding into both branches change per the new 3-tier rule. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267-01 | Information Disclosure / Bias | RNG bias drift in NEW `packedTraitsDegenerette` color distribution `[16,16,16,16,16,16,16,8]/120` | mitigate | Per D-267-DIST-01: base-15 scaling `scaled = uint32((uint64(uint32(rnd)) * 15) >> 32)` from 32 bits gives `floor(2^32 / 15) = 286331153` per-bucket count vs ideal 286331153.066… — max bias ~2.3e-10 per slot. Implementation matches CONTEXT.md `<specifics>` paste-ready code byte-for-byte. Backward-trace per `feedback_rng_backward_trace.md`: `rnd` is per-spin keccak-derived from VRF rngWord; unknown at input commitment time. Phase 268 STAT-02 chi² empirical verification at α=0.05 over ≥1M samples (Wilson-Hilferty Z<1.645 / `CHI2_CRIT_05[7]=14.067`). Severity: medium. |
| T-267-02 | Tampering / EV Drift | Per-N table EV drift across 5 dispatch tables | mitigate | Per D-267-EV-TARGET-01: each of 5 payout tables satisfies `Σ P_N(M) × payout(M) = exactly 100 centi-x` (verified Fraction-exact by `derive_5_tables.py`; final per-N drift ±0.0003 bps via M=4/M=5/M=6 cascade). Task 2 grep-asserts script-emitted hex matches planning-note .sol hex byte-for-byte across all 25 packed constants — mismatch BLOCKS Task 3 (D-267-CONSTVERIFY-01). Phase 268 STAT-01 + STAT-05 empirically verifies on-chain dispatch (≥1M draws per N; basePayoutEV = 100.00 ± 0.50 centi-x; per-N match-count histogram within ±0.5% bin). Severity: high. |
| T-267-03 | Tampering / Boundary-Gaming | 3-tier ETH split rule discontinuities at exactly 3.0× and 10.0× bet | mitigate | Per D-267-PAYSPLIT-01..02: documented design accepts the 3.0× → 2.5× ETH drop at 3.01× (smaller than naive 25%-split alternative which would drop to 0.7525× ETH). The two bands meet exactly at `payout = 10 * bet` where 0.25 × payout = 2.5 × bet (no discontinuity at the upper boundary). NatSpec on `_distributePayout` documents the 3-tier rule + boundary behavior in language of CURRENT design only. Phase 268 STAT-07 covers boundary correctness across per-N basePayout × roiBps × hero × WWXRP-bonus combinations; Phase 271 §4 surface (h) audits monotonicity + no-payout-multiple-gaming exploit at 3.0× bet. Severity: medium. |
| T-267-04 | Tampering / Composition Break | Pool-cap precedence break (PAY-SPLIT-03) — split rule applied AFTER pool-cap could violate cap | mitigate | Per D-267-PAYSPLIT-03: pool-cap (`ETH_WIN_CAP_BPS`) is applied AFTER the 3-tier split in the unfrozen branch. Computed `ethShare` from PAY-SPLIT-01..02 then compared against `maxEth = pool * ETH_WIN_CAP_BPS / 10_000`; if `ethShare > maxEth`, excess flips to lootbox. Same-direction logic as the existing L716-723 cap. NatSpec documents precedence: "pool cap takes precedence over small-payout passthrough and 2.5× floor". Frozen-pool branch retains its existing solvency-check (revert-on-insufficient) per D-267-PAYSPLIT-03. Phase 268 STAT-07 covers thin-pool cap-flip path; Phase 271 §4(h) audits composition. Severity: high. |
| T-267-05 | Tampering / Argument Threading | `betAmount` mis-thread into `_distributePayout` 5-arg signature (PAY-SPLIT-05) | mitigate | Per D-267-PAYSPLIT-05: argument insertion is between `currency` and `payout` (currency-bet-payout group, related parameters adjacent). The L656 call site already has `amountPerTicket` (uint128) in scope (decoded earlier in the spin loop). Executor `<read_first>` directive includes both the spin-loop variable inventory at L580-660 and the `_distributePayout` body at L678-740. Acceptance criteria includes a grep-assertion that the call site reads exactly `_distributePayout(player, currency, amountPerTicket, payout, lootboxWord)`. Severity: medium. |
| T-267-06 | Tampering / Storage Drift | Storage-slot drift introduced by 25-constant addition + normalizer deletion | mitigate | Per DGN-15 + D-267-VISIBILITY-01: ALL changes are inline literals + new immutable constants + new internal/private functions. `contracts/storage/GameStorage.sol` UNTOUCHED (Task 3 acceptance criteria includes `git diff 1c0f0913..HEAD -- contracts/storage/GameStorage.sol` returning empty). Phase 268 SURF-03 storage-slot grep-proof at v37.0 phase-close vs v36.0 baseline. Severity: high. |
| T-267-07 | Elevation / ABI Widening | NEW `packedTraitsDegenerette` widens public ABI | mitigate | Per D-267-VISIBILITY-01: visibility = `internal pure` (mirrors `packedTraitsFromSeed`). On a `library` declaration, `internal pure` functions are inlined into the consumer at compile time — ZERO new function selector in the public ABI. Task 1 corrects upstream-doc wording (REQUIREMENTS.md DGN-01 + ROADMAP success criteria 1+5 + Phase 271 AUDIT-04 attestation drops "ALLOWED-NEW-STATELESS-ENTRY"). Task 3 acceptance criteria includes `git diff 1c0f0913..HEAD -- contracts/ \| grep -E "^\\+.*function .* (public\|external)"` returning ZERO new public/external declarations. Phase 268 SURF-01 byte-identity for existing externals; Phase 271 AUDIT-04 attestation. Severity: high. |

**Block-on-high test:** ZERO unmitigated High threats at Task 3 commit time. T-267-02 (EV drift), T-267-04 (pool-cap precedence), T-267-06 (storage drift), T-267-07 (ABI widening) are the load-bearing High threats; each has explicit mitigation paths backed by Task 2 byte-identity proof (T-267-02), NatSpec + Phase 268 STAT-07 (T-267-04), Task 3 storage-slot grep + Phase 268 SURF-03 (T-267-06), and Task 1 doc-fix + Task 3 ABI-widening grep + Phase 271 AUDIT-04 (T-267-07).

</threat_model>

<tasks>

<!-- ================================================================ -->
<!-- WAVE 1 — UPSTREAM DOC FIXES (AGENT-COMMITTED, AUTONOMOUS)         -->
<!-- ================================================================ -->

<task type="auto">
  <name>Task 1 [Wave 1]: Upstream-doc fixes batched — REQUIREMENTS.md DGN-01/DGN-03/PAY-SPLIT/STAT-07/AUDIT-02(h)/Coverage + ROADMAP.md visibility/3-tier wording/AUDIT-04 attestation</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-CONTEXT.md (`<decisions>` section — D-267-COUNTGOLD-01 + D-267-VISIBILITY-01 + D-267-PAYSPLIT-01..05 + D-267-PLAN-01 step 1)
    - .planning/REQUIREMENTS.md (full file — verify current state of DGN-01 line 16, DGN-03 line 18, PAY-SPLIT-01..03 lines 32-36, STAT-07 line 46, AUDIT-02 line 79, Coverage line 167, traceability rows lines 129-138)
    - .planning/ROADMAP.md (full file — verify current state of Phase 267 inline goal line 34 + success criteria lines 38-43, Phase 268 STAT-07 line 49, Phase 271 AUDIT-02(h) line 91, Phase 271 AUDIT-04 attestation line 91 + line 97)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md (apply same discipline to doc edits — describe what IS at v37.0 design, never "this used to be...")
  </read_first>
  <action>
**Goal:** Apply 6 targeted edits to `.planning/REQUIREMENTS.md` + 4 targeted edits to `.planning/ROADMAP.md` to make doc wording exactly match the locked Phase 267 implementation signatures + visibility + payout-split rule. Many of these edits were authored by recent commits `da93a8bf` + `4c6d2326` + `84b556c9`; this task ASSERTS presence and applies any remaining drift.

**`.planning/REQUIREMENTS.md` edits (apply if not already present):**

1. **DGN-01 visibility (line 16):** wording must read `external pure returns (uint32)` → `internal pure returns (uint32)`. Per D-267-VISIBILITY-01. Use `Edit` tool on the exact existing string.

2. **DGN-03 signature (line 18):** wording must read `_countGoldQuadrants(uint8[4]) internal pure returns (uint8)` → `_countGoldQuadrants(uint32 ticket) private pure returns (uint8)`. Per D-267-COUNTGOLD-01. Use `Edit` tool.

3. **PAY-SPLIT section (lines 32-36):** verify present. Three rows:
   - `**PAY-SPLIT-01**`: ETH ≤ 3× bet → 100% ETH (D-267-PAYSPLIT-01).
   - `**PAY-SPLIT-02**`: ETH > 3× bet → `ethShare = max(2.5*bet, payout/4)` (D-267-PAYSPLIT-02).
   - `**PAY-SPLIT-03**`: pool cap (`ETH_WIN_CAP_BPS = 1_000`) takes precedence (D-267-PAYSPLIT-03).
   If absent, ADD them under a new `### PAY-SPLIT — ETH Payout Currency Split (Phase 267)` heading after the DGN section.

4. **STAT-07 row (line 46):** verify present in `### STAT — Statistical Validation` section: ETH payout-split distribution validation under per-N basePayout × roiBps + thin-pool cap-flip + per-band frequency match (D-267-PAYSPLIT-01..03 empirical verification in Phase 268).

5. **AUDIT-02 surface (h) wording (line 79):** verify present — describes ETH split-rule monotonicity + boundary-gaming check at 3.0× threshold (PAY-SPLIT-01) + 2.5× ETH floor (PAY-SPLIT-02) + 10%-pool-cap precedence (PAY-SPLIT-03).

6. **Coverage line (line 167):** wording must read `47 total` → `51 total` and break down should be `15 DGN + 3 PAY-SPLIT + 7 STAT + 6 SURF + 3 LBX + 3 GASPIN + 4 DELTA + 6 AUDIT + 4 REG`. Mapped per phase: `18 → Phase 267, 13 → Phase 268, 6 → Phase 269, 4 → Phase 270, 10 → Phase 271`.

7. **Traceability rows (lines 129-138):** verify rows for `PAY-SPLIT-01..03` (Phase 267) + `STAT-07` (Phase 268) present. ADD if missing using the existing table format `| PAY-SPLIT-01 | Phase 267 | Pending |` etc.

**`.planning/ROADMAP.md` edits (apply if not already present):**

1. **Phase 267 inline goal (line 34):** verify mentions both the Degenerette producer + 5-table dispatch AND the `_distributePayout` 3-tier ETH split rule + `betAmount` threading per D-267-PAYSPLIT-01..05. Per recent commit `da93a8bf` + `4c6d2326` this should be present; apply drift fix if observed.

2. **Phase 267 success criteria (lines 38-43):** ensure success criterion 1 reads `external pure helper` → `internal pure helper` per D-267-VISIBILITY-01 (line 38). Ensure success criterion 5 mentions `internal pure library helper, inlined into consumer — does NOT widen the public ABI` (line 42). Ensure success criterion 6 covers the `_distributePayout` ETH-branch rewrite with 3-tier rule (PAY-SPLIT-01..03) (line 43). Per recent commits this should be present; apply drift fix if observed.

3. **Phase 268 STAT-07 wording (line 49):** verify Phase 268 goal mentions STAT-07 ETH payout-split distribution validation. ADD if absent.

4. **Phase 271 AUDIT-02 surface (h) (line 91) + AUDIT-04 attestation (line 91 + 97):** ensure AUDIT-02 goal mentions surface (h) ETH split-rule monotonicity + boundary-gaming + thin-pool cap-flip. **CRITICAL:** Drop the phrase `ALLOWED-NEW-STATELESS-ENTRY` from the AUDIT-04 attestation language (line 91 + line 97) — `packedTraitsDegenerette` is `internal pure`, not a NEW external entry. Replace with attestation that ZERO new external mutation entry points + ZERO new external pure entry points are added (D-267-VISIBILITY-01). Use `Edit` tool to replace the exact existing phrase.

**Discipline:**

- Per `feedback_no_history_in_comments.md` applied to docs: each edit describes the v37.0 design as it IS — never reference "this was changed from" or "the old normalizer" or "previous design". The doc must read as a fresh, coherent v37.0 spec post-edit.
- Use `Read` to verify current state of each line BEFORE editing, then `Edit` to apply each exact-string replacement. NEVER use `Bash(cat << 'EOF')` heredoc for edits.
- After all 10 edits applied: run the canonical_grep_recipes `task1_doc_drift_check` (5 grep assertions). All must pass.
- AGENT-COMMITTED at end with subject `chore(267): docs upstream fixes for v37.0 phase 267 [DGN-01, DGN-03, PAY-SPLIT, STAT-07, AUDIT-04]`.

This is autonomous (planning-doc edits, not contract changes — no `feedback_no_contract_commits.md` constraint applies).
  </action>
  <verify>
    <automated>grep -E "internal pure returns \(uint32\)" .planning/REQUIREMENTS.md | grep -c "DGN-01\|packedTraitsDegenerette" | head -1; grep -cE "_countGoldQuadrants\(uint32 ticket\) private pure" .planning/REQUIREMENTS.md; grep -cE "^- \[ \] \*\*PAY-SPLIT-0[1-3]\*\*" .planning/REQUIREMENTS.md; grep -c "STAT-07" .planning/REQUIREMENTS.md; grep -c "ALLOWED-NEW-STATELESS-ENTRY" .planning/ROADMAP.md; grep -c "51 total" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "DGN-01.*internal pure returns \(uint32\)" .planning/REQUIREMENTS.md` returns ≥ 1 hit (DGN-01 row visibility corrected per D-267-VISIBILITY-01).
    - `grep -nE "DGN-01.*external pure" .planning/REQUIREMENTS.md` returns 0 hits.
    - `grep -nE "_countGoldQuadrants\(uint32 ticket\) private pure" .planning/REQUIREMENTS.md` returns ≥ 1 hit (DGN-03 signature corrected per D-267-COUNTGOLD-01).
    - `grep -nE "_countGoldQuadrants\(uint8\[4\]\)" .planning/REQUIREMENTS.md` returns 0 hits.
    - `grep -cE "^- \[ \] \*\*PAY-SPLIT-0[1-3]\*\*" .planning/REQUIREMENTS.md` returns 3 (rows for PAY-SPLIT-01, PAY-SPLIT-02, PAY-SPLIT-03 present).
    - `grep -cE "^- \[ \] \*\*STAT-07\*\*" .planning/REQUIREMENTS.md` returns 1 (STAT-07 row present in STAT section).
    - `grep -c "AUDIT-02" .planning/REQUIREMENTS.md` and `grep -c "split-rule monotonicity\|boundary-gaming\|3-tier" .planning/REQUIREMENTS.md` both return ≥ 1 (AUDIT-02 surface (h) wording present).
    - `grep -c "51 total" .planning/REQUIREMENTS.md` returns 1 (Coverage bumped 47 → 51).
    - `grep -cE "^\| PAY-SPLIT-0[1-3] \| Phase 267 \| Pending \|" .planning/REQUIREMENTS.md` returns 3 (traceability rows present).
    - `grep -cE "^\| STAT-07 \| Phase 268 \| Pending \|" .planning/REQUIREMENTS.md` returns 1.
    - `grep -c "ALLOWED-NEW-STATELESS-ENTRY" .planning/ROADMAP.md` returns 0 (Phase 271 AUDIT-04 attestation language drop per D-267-VISIBILITY-01).
    - `grep -E "internal pure library helper" .planning/ROADMAP.md` returns ≥ 1 hit (Phase 267 success criterion 5 visibility wording corrected).
    - `grep -E "3-tier" .planning/ROADMAP.md` returns ≥ 1 hit (Phase 267 success criterion 6 + Phase 271 AUDIT-02 surface (h) wording present).
    - `grep -E "STAT-07" .planning/ROADMAP.md` returns ≥ 1 hit (Phase 268 STAT-07 wording present).
    - Single AGENT-COMMITTED commit landed: `git log --oneline -1 .planning/REQUIREMENTS.md .planning/ROADMAP.md` shows subject `chore(267): docs upstream fixes for v37.0 phase 267 [DGN-01, DGN-03, PAY-SPLIT, STAT-07, AUDIT-04]` (or close variant).
    - ZERO history-prose strings introduced: `grep -cE "(was changed|used to be|previously|formerly|the old normalizer)" .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns 0 in any rewritten line.
  </acceptance_criteria>
  <done>10 targeted upstream-doc edits applied; all 13 task1_doc_drift_check + acceptance grep assertions pass; single AGENT-COMMITTED chore commit landed; doc-vs-impl coherence enforced for DGN-01 visibility, DGN-03 signature, PAY-SPLIT-01..03 + STAT-07 + AUDIT-02(h) presence, Coverage 47→51, traceability rows, ROADMAP success-criteria visibility wording, ROADMAP AUDIT-04 attestation language drop "ALLOWED-NEW-STATELESS-ENTRY".</done>
</task>

<!-- ================================================================ -->
<!-- WAVE 2 — CONSTANTS VERIFICATION (AGENT-COMMITTED, AUTONOMOUS)     -->
<!-- BLOCKS WAVE 3 ON ANY MISMATCH per D-267-CONSTVERIFY-01            -->
<!-- ================================================================ -->

<task type="auto">
  <name>Task 2 [Wave 2]: Re-run derive_5_tables.py + grep-assert byte-identity for all 25 packed constants → 267-01-CONSTANTS-VERIFY.md</name>
  <files>.planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-CONSTANTS-VERIFY.md</files>
  <read_first>
    - .planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-CONTEXT.md (`<decisions>` D-267-CONSTVERIFY-01 + D-267-EV-TARGET-01 + D-267-EV-PRECISION-01)
    - .planning/notes/degenerette-recalibration/derive_5_tables.py (full script — Fraction-exact derivation; understand the FINAL PASTE-READY CONSTANTS section at L218-294)
    - .planning/notes/2026-05-10-degenerette-payout-recalibration.md (§"CONSTANT REWRITE" block at lines ~186-223 — pasted .sol hex byte-strings; these are the assertion targets)
  </read_first>
  <action>
**Goal:** Reproduce the 25 packed constants from the Fraction-exact Python derivation and grep-assert each emitted hex byte-string matches the planning-note pasted .sol hex byte-for-byte. Any mismatch BLOCKS Task 3 per D-267-CONSTVERIFY-01.

**Step 1 — Re-run script:**

```bash
cd /home/zak/Dev/PurgeGame/degenerus-audit
python3 .planning/notes/degenerette-recalibration/derive_5_tables.py > /tmp/267-derive-stdout.txt 2>&1
echo "Exit: $?"
wc -l /tmp/267-derive-stdout.txt
```

Expected: exit 0, output > 50 lines including the FINAL PASTE-READY CONSTANTS block. If Python error, STOP — surface the error to user (script may have drifted from CONTEXT.md spec).

**Step 2 — Author `.planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-CONSTANTS-VERIFY.md`:**

Use the `Write` tool. Structure:

```
# Phase 267 — Constants Verification

**Generated:** <today YYYY-MM-DD>
**Script:** `.planning/notes/degenerette-recalibration/derive_5_tables.py`
**Source-of-truth note:** `.planning/notes/2026-05-10-degenerette-payout-recalibration.md` §"CONSTANT REWRITE"
**Decision:** D-267-CONSTVERIFY-01 (Task 2 BLOCKS Task 3 on any mismatch)

## Script stdout (verbatim)

```
<paste the full /tmp/267-derive-stdout.txt content here, including:
 - the per-N P_N(M) table
 - the 5 EV-equal payout tables block
 - the per-pick EV verification block (drift bps per N)
 - the ETH-bonus EV verification (target 5.0000%)
 - the FINAL PASTE-READY CONSTANTS block with all 25 constant declarations>
```

## Byte-identity assertions (25 grep pairs)

| Constant | Script hex | Planning-note hex | Verdict |
|---|---|---|---|
| `QUICK_PLAY_PAYOUTS_N0_PACKED` | `0x...` | `0x...` | PASS / MISMATCH |
| `QUICK_PLAY_PAYOUTS_N1_PACKED` | `0x...` | `0x...` | PASS / MISMATCH |
| ... (25 rows total) |

## Summary

| Family | Constants | PASS | MISMATCH |
|---|---|---|---|
| Per-N payouts (M=0..7 packed) | 5 × `QUICK_PLAY_PAYOUTS_N{0..4}_PACKED` | <count> | <count> |
| Per-N M=8 jackpot | 5 × `QUICK_PLAY_PAYOUT_N{0..4}_M8` | <count> | <count> |
| Per-N hero boost (M=2..7 packed) | 5 × `HERO_BOOST_N{0..4}_PACKED` | <count> | <count> |
| Per-N WWXRP factors (B=5..8 packed) | 5 × `WWXRP_FACTORS_N{0..4}_PACKED` | <count> | <count> |
| **TOTAL** | 25 | <count> | <count> |

**Verdict:** <PASS_ALL_25 / MISMATCH_FOUND_TASK_3_BLOCKED>

## Re-run protocol (if Task 2 surfaces drift)

If any row is MISMATCH, the source-of-truth is the script (Fraction-exact derivation). The mismatch implies the planning-note .sol pasted hex was hand-edited at some point and drifted from script output. Resolution path:

1. Surface mismatch to user with row-level evidence (script hex vs note hex).
2. Confirm script is unchanged from intended spec (per CONTEXT.md `<decisions>` D-267-DIST-01 + D-267-EV-TARGET-01 + D-267-EV-PRECISION-01 + D-267-WWXRP-SPLIT-01).
3. Patch the planning note's pasted hex to match script output.
4. Re-run Task 2 from Step 1.
5. Only proceed to Task 3 when ALL 25 grep pairs are PASS.
```

**Step 3 — Run the canonical_grep_recipes `task2_constants_verify` block:**

For each of the 25 constants, extract the hex byte-string from `/tmp/267-derive-stdout.txt` and compare against the byte-string in `.planning/notes/2026-05-10-degenerette-payout-recalibration.md`. Use the recipes in the YAML frontmatter `canonical_grep_recipes.task2_constants_verify` field verbatim. Document each pair (script hex / note hex) in the table. PASS if identical (case-insensitive), MISMATCH otherwise.

**Step 4 — AGENT-COMMITTED:**

After ALL 25 grep pairs PASS, commit with subject `chore(267): constants verification — derive_5_tables.py byte-identity proof [D-267-CONSTVERIFY-01]`. If any MISMATCH found, STOP and surface to user — DO NOT commit a partial verification, do NOT proceed to Task 3.

This is autonomous (chore commit, planning-doc artifact only — no contract changes).
  </action>
  <verify>
    <automated>test -f .planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-CONSTANTS-VERIFY.md && grep -c "PASS" .planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-CONSTANTS-VERIFY.md && grep -cE "QUICK_PLAY_PAYOUTS_N[0-4]_PACKED|QUICK_PLAY_PAYOUT_N[0-4]_M8|HERO_BOOST_N[0-4]_PACKED|WWXRP_FACTORS_N[0-4]_PACKED" .planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-CONSTANTS-VERIFY.md | head -1; grep -c "MISMATCH" .planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-CONSTANTS-VERIFY.md</automated>
  </verify>
  <acceptance_criteria>
    - `python3 .planning/notes/degenerette-recalibration/derive_5_tables.py` runs to completion (exit 0, > 50 lines stdout).
    - `267-01-CONSTANTS-VERIFY.md` exists with full script stdout + 25-row assertion table + summary table + verdict line.
    - 25 of 25 rows show PASS verdict (`grep -c "PASS" 267-01-CONSTANTS-VERIFY.md` returns ≥ 25 + however many "PASS_ALL_25" / table-header occurrences).
    - 0 of 25 rows show MISMATCH verdict (`grep -c "MISMATCH" 267-01-CONSTANTS-VERIFY.md` returns 0 in row-level cells; the protocol section may mention "MISMATCH" as a label — count outside row cells does not count toward failure).
    - Verdict line reads `**Verdict:** PASS_ALL_25` (or equivalent) — explicit PASS_ALL signal. If MISMATCH found: task FAILS; user surfaced with row-level evidence; Task 3 BLOCKED until resolved.
    - All 5 grep pairs from `canonical_grep_recipes.task2_constants_verify` evaluated and recorded with row-level hex string evidence.
    - Single AGENT-COMMITTED commit landed: `git log --oneline -1` shows subject `chore(267): constants verification — derive_5_tables.py byte-identity proof [D-267-CONSTVERIFY-01]` (or close variant).
    - `git status` clean for `contracts/` (Task 2 emits ZERO contract-tree mutations — confirms autonomous chore-only posture).
  </acceptance_criteria>
  <done>derive_5_tables.py re-run captured into 267-01-CONSTANTS-VERIFY.md; all 25 packed-constant byte-strings grep-asserted byte-identical between script stdout and planning-note pasted .sol hex; 25 PASS / 0 MISMATCH at HEAD; AGENT-COMMITTED chore commit landed; Task 3 UNBLOCKED for execution.</done>
</task>

<!-- ================================================================ -->
<!-- WAVE 3 — CONTRACT IMPLEMENTATION (USER-APPROVED BATCHED COMMIT)   -->
<!-- non-autonomous; checkpoint:human-verify gate per D-267-APPROVAL-02 -->
<!-- ================================================================ -->

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 [Wave 3 GATE]: Implement single batched contract diff (TraitUtils additive + DegeneretteModule rewrite + 3-tier ETH split + betAmount threading); present diff; commit ONLY after explicit "approved"</name>
  <files>contracts/DegenerusTraitUtils.sol, contracts/modules/DegenerusGameDegeneretteModule.sol</files>
  <read_first>
    - .planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-CONTEXT.md (`<decisions>` ALL D-267-* + `<specifics>` paste-ready Solidity bodies for `_countGoldQuadrants`, `packedTraitsDegenerette`, `_degTrait`, `_getBasePayoutBps`, `_applyHeroMultiplier`, `_distributePayout` ETH-branch sketch)
    - .planning/notes/2026-05-10-degenerette-payout-recalibration.md (§"Concrete file changes" + §"CONSTANT REWRITE" — paste-ready bodies for `_countGoldQuadrants`, `_getBasePayoutBps`, `_wwxrpFactor`, `_applyHeroMultiplier`, `_fullTicketPayout`; SOURCE OF TRUTH for the Solidity diff)
    - .planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-CONSTANTS-VERIFY.md (Task 2 output — ALL 25 grep pairs MUST be PASS_ALL_25 before this task starts; if any MISMATCH, STOP and re-run Task 2)
    - contracts/DegenerusTraitUtils.sol (full file — verify `weightedColorBucket` L115-127 + `traitFromWord` L143-147 + `packedTraitsFromSeed` L169-178 will stay byte-identical; insertion point for new helpers is post-L178)
    - contracts/modules/DegenerusGameDegeneretteModule.sol L185-220 (currency + ETH_ROI_BONUS_BPS + ETH_WIN_CAP_BPS + PayoutCapped event — UNCHANGED; reference for _distributePayout)
    - contracts/modules/DegenerusGameDegeneretteModule.sol L235-322 (constant region — DELETE 4 single-table + ADD 25 per-N packed + REWRITE block headers + 4 stale comment surfaces)
    - contracts/modules/DegenerusGameDegeneretteModule.sol L580-676 (spin loop — verify `amountPerTicket` (uint128) is in scope at L656 callsite; verify L607 producer call site)
    - contracts/modules/DegenerusGameDegeneretteModule.sol L678-740 (`_distributePayout` body — REWRITE per D-267-PAYSPLIT-01..05; CURRENCY_BURNIE L735-736 + CURRENCY_WWXRP L737-739 BYTE-UNCHANGED; frozen-pool branch L695-711 retains existing solvency-check posture)
    - contracts/modules/DegenerusGameDegeneretteModule.sol L808-851 (`_evNormalizationRatio` body — DELETE entirely)
    - contracts/modules/DegenerusGameDegeneretteModule.sol L857-911 (`_countMatches` UNCHANGED; `_wwxrpBonusBucket` UNCHANGED; `_wwxrpBonusRoiForBucket` REWRITE for per-N or REPLACE with `_wwxrpFactor(N, bucket)`)
    - contracts/modules/DegenerusGameDegeneretteModule.sol L923-1011 (`_fullTicketPayout` REWRITE for N threading + `_applyHeroMultiplier` REWRITE for symbol-only + per-N)
    - contracts/modules/DegenerusGameDegeneretteModule.sol L1013-1021 (`_getBasePayoutBps` REWRITE to `(N, matches)`)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_contract_commits.md (governs this task — wait for explicit user `approved` string before commit)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_batch_contract_approval.md (single batched diff for the entire phase)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_never_preapprove_contracts.md (NEVER pre-approve; ALWAYS wait for explicit `approved`)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_wait_for_approval.md (acceptance criterion: user explicitly typed `approved` before commit landed)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md (ZERO "was/used to/previously/formerly/changed from/removed/deleted" prose anywhere in rewritten regions)
    - $HOME/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_manual_review_before_push.md (NO `git push` after commit — user pushes when ready)
  </read_first>
  <action>
This is a `checkpoint:human-verify` task — the SINGLE USER-APPROVAL GATE for the entire Phase 267 contract diff per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + D-267-APPROVAL-02. The agent IMPLEMENTS the diff, PRESENTS it via the `<what-built>` block below, and WAITS for the user's explicit `approved` string before committing per `feedback_wait_for_approval.md`.

**Pre-flight check (BLOCKS execution):** verify `.planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-CONSTANTS-VERIFY.md` exists and contains `Verdict: PASS_ALL_25` (or equivalent explicit PASS_ALL signal). If MISMATCH or file absent — STOP, surface to user, re-run Task 2.

**Implementation steps (apply via `Edit` and `Write` tools — NEVER `Bash(cat << 'EOF')`):**

### `contracts/DegenerusTraitUtils.sol` (additive only):

INSERT after L178 (after the existing `packedTraitsFromSeed` body, before the closing `}` of the library):

```solidity
    /*+======================================================================+
      |                  DEGENERETTE TRAIT PACKING (NEAR-UNIFORM)            |
      +======================================================================+
      |  Sibling helper to packedTraitsFromSeed using a per-quadrant         |
      |  near-uniform color distribution (7 commons at 2/15 each, gold at    |
      |  1/15) for the Degenerette quickPlay payout schedule.                |
      +======================================================================+*/

    /// @notice Packs 4 quadrant traits using the Degenerette near-uniform color
    ///         distribution: 7 commons at 2/15 each (13.333%), gold at 1/15 (6.667%).
    /// @dev Per-quadrant: color via base-15 scaling (gold = scaled==14, common =
    ///      scaled >> 1), symbol uniform 1/8 from the high 32 bits of each lane.
    ///      Output format mirrors packedTraitsFromSeed: [QQ][CCC][SSS] per byte,
    ///      4 bytes packed into uint32. Existing packedTraitsFromSeed (heavy-tail
    ///      color) preserved for MintModule + JackpotModule consumers.
    /// @param rand 256-bit random seed (typically from per-spin keccak256)
    /// @return 32-bit packed traits value with near-uniform color + uniform symbol
    function packedTraitsDegenerette(uint256 rand) internal pure returns (uint32) {
        uint8 traitA = _degTrait(uint64(rand));                      // Quadrant 0: bits 7-6 = 00
        uint8 traitB = _degTrait(uint64(rand >> 64))  | 64;          // Quadrant 1: bits 7-6 = 01
        uint8 traitC = _degTrait(uint64(rand >> 128)) | 128;         // Quadrant 2: bits 7-6 = 10
        uint8 traitD = _degTrait(uint64(rand >> 192)) | 192;         // Quadrant 3: bits 7-6 = 11
        return uint32(traitA)
             | (uint32(traitB) << 8)
             | (uint32(traitC) << 16)
             | (uint32(traitD) << 24);
    }

    /// @dev Per-quadrant Degenerette trait derivation. Color via base-15 scaling
    ///      with bias < 2.3e-10 per slot; symbol uniform 1/8 from high 32 bits.
    /// @param rnd 64-bit per-quadrant random word
    /// @return 6-bit trait ID [CCC][SSS] (quadrant bits added by caller)
    function _degTrait(uint64 rnd) private pure returns (uint8) {
        uint32 scaled = uint32((uint64(uint32(rnd)) * 15) >> 32);
        uint8 color = scaled == 14 ? 7 : uint8(scaled >> 1);
        uint8 symbol = uint8(rnd >> 32) & 7;
        return (color << 3) | symbol;
    }
```

Use the EXACT body from CONTEXT.md `<specifics>` and the planning note. ZERO history prose. Existing 3 functions UNTOUCHED (DGN-14, Phase 268 SURF-01).

### `contracts/modules/DegenerusGameDegeneretteModule.sol` (rewrite):

#### (a) Constant block L235-322 — DELETE 4 + ADD 25 + REWRITE 4 stale comment surfaces

Replace L235-322 region as follows:

1. DELETE `QUICK_PLAY_BASE_PAYOUTS_PACKED` (L240-248) + `QUICK_PLAY_BASE_PAYOUT_8_MATCHES` (L251).
2. DELETE `WWXRP_BONUS_FACTOR_BUCKET5` / `_BUCKET6` / `_BUCKET7` / `_BUCKET8` (L265-268). KEEP `WWXRP_BONUS_FACTOR_SCALE = 1_000_000` (L264).
3. DELETE `HERO_BOOST_PACKED` (L319). KEEP `HERO_PENALTY = 9500` (L320) + `HERO_SCALE = 10_000` (L321).
4. DELETE the §"EXACT EV NORMALIZATION" prose at L287-298 entirely.
5. ADD 25 packed constants (paste-ready hex from `.planning/notes/2026-05-10-degenerette-payout-recalibration.md` §"CONSTANT REWRITE" block — verified byte-identical by Task 2):

```solidity
    // ─── Per-N base payout tables (M=0..7 packed, M=8 separate) ────────────
    /// @dev Each table satisfies basePayoutEV = exactly 100 centi-x against P_N(M).
    ///      Indexed by N = _countGoldQuadrants(playerTicket) ∈ {0..4}.
    ///      M=4/M=5/M=6 cascade absorbs rounding residual; M=8 stays at uniform-scale (monotonic in N).
    ///      Packed 32 bits per match index: [M*32 .. M*32+31]
    uint256 private constant QUICK_PLAY_PAYOUTS_N0_PACKED = 0x0001a42c000051f1000011da00000654000001ff000000cc0000000000000000;
    uint256 private constant QUICK_PLAY_PAYOUTS_N1_PACKED = 0x0001eb8600005fd7000014e70000075f00000256000000ef0000000000000000;
    uint256 private constant QUICK_PLAY_PAYOUTS_N2_PACKED = 0x000241d9000070ac00001894000008aa000002bf000001190000000000000000;
    uint256 private constant QUICK_PLAY_PAYOUTS_N3_PACKED = 0x0002ac130000856900001d1700000a39000003400000014d0000000000000000;
    uint256 private constant QUICK_PLAY_PAYOUTS_N4_PACKED = 0x0003310c00009f5a000022be00000c4d000003e20000018d0000000000000000;

    /// @dev Per-N M=8 jackpot tier (stays at uniform-scale, monotonic in N).
    uint256 private constant QUICK_PLAY_PAYOUT_N0_M8 = 10_756_411; // 107,564.11x bet
    uint256 private constant QUICK_PLAY_PAYOUT_N1_M8 = 12_583_037; // 125,830.37x bet
    uint256 private constant QUICK_PLAY_PAYOUT_N2_M8 = 14_792_939; // 147,929.39x bet
    uint256 private constant QUICK_PLAY_PAYOUT_N3_M8 = 17_512_324; // 175,123.24x bet
    uint256 private constant QUICK_PLAY_PAYOUT_N4_M8 = 20_916_435; // 209,164.35x bet

    // ─── Per-N hero boost tables (M=2..7 packed, 16 bits each) ─────────────
    /// @dev Symbol-only hero match: P(hero|M, N) × boost(M, N) + (1 - P) × HERO_PENALTY = HERO_SCALE.
    ///      Hero fires on uniform 1/8 symbol match per quadrant; color of hero quadrant ignored.
    ///      Packed 16 bits per match index, M=2 lowest: [M=2 | M=3 | M=4 | M=5 | M=6 | M=7]
    uint256 private constant HERO_BOOST_N0_PACKED = 0x275a27be2849291a2a762d2e;
    uint256 private constant HERO_BOOST_N1_PACKED = 0x275027a9282728e52a262ca9;
    uint256 private constant HERO_BOOST_N2_PACKED = 0x27482797280828b529d92c26;
    uint256 private constant HERO_BOOST_N3_PACKED = 0x2742278827ed288829902ba6;
    uint256 private constant HERO_BOOST_N4_PACKED = 0x273d277c27d62860294b2b2a;

    // ─── Per-N WWXRP factors (B=5..8 packed, 64 bits each) ─────────────────
    /// @dev Per-N factors derived from each N's basePayout schedule + binomial-convolution P_N(M)
    ///      + 10/30/30/30 split across buckets 5/6/7/8. Total ETH bonus EV = exactly 5.000% per N.
    ///      Packed 64 bits per bucket index, B=5 lowest: [B=5 | B=6 | B=7 | B=8]
    uint256 private constant WWXRP_FACTORS_N0_PACKED = 0x0000000002278add0000000003fd603d0000000000ddba9f00000000001923d6;
    uint256 private constant WWXRP_FACTORS_N1_PACKED = 0x0000000003aef46a0000000005fd43a60000000001285f2400000000001e36c9;
    uint256 private constant WWXRP_FACTORS_N2_PACKED = 0x0000000006442ce7000000000914e5e4000000000192745c000000000024f43d;
    uint256 private constant WWXRP_FACTORS_N3_PACKED = 0x000000000a96251f000000000dd6ad96000000000228fcb000000000002de0ce;
    uint256 private constant WWXRP_FACTORS_N4_PACKED = 0x0000000011ba25db00000000151a90e70000000002fdeaff0000000000399efe;
```

6. REWRITE block headers + 4 stale comment surfaces per D-267-COMMENTS-01 — describe per-N tables in language of CURRENT design only.
7. The replacement for L235 RTP claim, L262 weights=10, L287-298 EXACT EV NORMALIZATION, L316 HERO_BOOST_PACKED reference per CONTEXT.md `<decisions>` D-267-COMMENTS-01.

#### (b) Add `_countGoldQuadrants(uint32 ticket) private pure returns (uint8 count)` per D-267-COUNTGOLD-01

INSERT (placement: near the other private helpers in the payout-math region, e.g., before `_countMatches` at L853 or alongside `_getBasePayoutBps` — exact placement is a `Claude's Discretion` slot per CONTEXT.md):

```solidity
    /// @dev Counts gold (color == 7) quadrants in a packed ticket.
    ///      Returns N ∈ {0, 1, 2, 3, 4} — the index for per-N payout/hero/WWXRP tables.
    function _countGoldQuadrants(uint32 ticket) private pure returns (uint8 count) {
        unchecked {
            for (uint8 q = 0; q < 4; ++q) {
                uint8 color = uint8((ticket >> (q * 8 + 3)) & 7);
                if (color == 7) ++count;
            }
        }
    }
```

#### (c) DELETE `_evNormalizationRatio` (L808-851) entirely + DELETE its call site (L965-969)

Use `Edit` to remove the function body in full + remove the 2 lines that call it (L965-969 region) inside `_fullTicketPayout`. Do NOT leave a comment trace per `feedback_no_history_in_comments.md`.

#### (d) REWRITE `_getBasePayoutBps` to `(uint8 N, uint8 matches)` (L1013-1021)

Replace with the body from CONTEXT.md `<specifics>` (paste-ready Solidity):

```solidity
    /// @dev Dispatches to the per-N base payout table.
    function _getBasePayoutBps(uint8 N, uint8 matches) private pure returns (uint256) {
        if (matches >= 8) {
            if (N == 0) return QUICK_PLAY_PAYOUT_N0_M8;
            if (N == 1) return QUICK_PLAY_PAYOUT_N1_M8;
            if (N == 2) return QUICK_PLAY_PAYOUT_N2_M8;
            if (N == 3) return QUICK_PLAY_PAYOUT_N3_M8;
            return QUICK_PLAY_PAYOUT_N4_M8;
        }
        uint256 packed;
        if (N == 0) packed = QUICK_PLAY_PAYOUTS_N0_PACKED;
        else if (N == 1) packed = QUICK_PLAY_PAYOUTS_N1_PACKED;
        else if (N == 2) packed = QUICK_PLAY_PAYOUTS_N2_PACKED;
        else if (N == 3) packed = QUICK_PLAY_PAYOUTS_N3_PACKED;
        else packed = QUICK_PLAY_PAYOUTS_N4_PACKED;
        return (packed >> (uint256(matches) * 32)) & 0xFFFFFFFF;
    }
```

#### (e) REWRITE `_wwxrpBonusRoiForBucket` (L899-911) — option: REPLACE with `_wwxrpFactor(uint8 N, uint8 bucket)`

Per `Claude's Discretion` slot in CONTEXT.md, the planner picks between (i) keeping `_wwxrpBonusRoiForBucket(bucket, bonusRoiBps)` signature with N threaded as new arg vs (ii) replacing with `_wwxrpFactor(N, bucket) → uint256` that returns just the factor (caller does the multiplication). Prefer (ii) per planning-note style. Body from planning note:

```solidity
    /// @dev Dispatches to the per-N WWXRP factor table for bucket B ∈ {5,6,7,8}.
    function _wwxrpFactor(uint8 N, uint8 bucket) private pure returns (uint256 factor) {
        uint256 packed;
        if (N == 0) packed = WWXRP_FACTORS_N0_PACKED;
        else if (N == 1) packed = WWXRP_FACTORS_N1_PACKED;
        else if (N == 2) packed = WWXRP_FACTORS_N2_PACKED;
        else if (N == 3) packed = WWXRP_FACTORS_N3_PACKED;
        else packed = WWXRP_FACTORS_N4_PACKED;
        factor = (packed >> (uint256(bucket - 5) * 64)) & 0xFFFFFFFFFFFFFFFF;
    }
```

If picking (i) instead, ensure caller in `_fullTicketPayout` updates accordingly + N is threaded.

#### (f) REWRITE `_applyHeroMultiplier` (L988-1011) for symbol-only + per-N

Replace with body from CONTEXT.md `<specifics>`:

```solidity
    /// @dev Symbol-only hero match (color of hero quadrant ignored).
    ///      Per-N hero boost dispatch: P(hero|M, N) × boost(M, N) + (1-P) × HERO_PENALTY = HERO_SCALE.
    function _applyHeroMultiplier(
        uint256 payout,
        uint32 playerTicket,
        uint32 resultTicket,
        uint8 matches,
        uint8 heroQuadrant,
        uint8 N
    ) private pure returns (uint256) {
        uint256 shift = uint256(heroQuadrant) * 8;
        bool symbolMatch = ((playerTicket >> shift) & 7) == ((resultTicket >> shift) & 7);

        uint256 multiplier;
        if (symbolMatch) {
            uint256 packed;
            if (N == 0) packed = HERO_BOOST_N0_PACKED;
            else if (N == 1) packed = HERO_BOOST_N1_PACKED;
            else if (N == 2) packed = HERO_BOOST_N2_PACKED;
            else if (N == 3) packed = HERO_BOOST_N3_PACKED;
            else packed = HERO_BOOST_N4_PACKED;
            multiplier = (packed >> (uint256(matches - 2) * 16)) & 0xFFFF;
        } else {
            multiplier = HERO_PENALTY;
        }
        return (payout * multiplier) / HERO_SCALE;
    }
```

Note the new 6th parameter `uint8 N`. Update the call site in `_fullTicketPayout` to pass N.

#### (g) REWRITE `_fullTicketPayout` body (L923-983) for N threading + drop `_evNormalizationRatio`

Replace body with the planning-note paste-ready form. Specifically:
- Compute `uint8 N = _countGoldQuadrants(playerTicket);` early.
- Pass N into `_getBasePayoutBps(N, matches)`.
- Use `_wwxrpFactor(N, bucket)` for ETH + WWXRP bonus computation.
- DROP the `(uint256 evNum, uint256 evDen) = _evNormalizationRatio(...)` lines + the `payout = (payout * evNum) / evDen` line (D-267-NORM-01).
- Pass N as 6th arg into `_applyHeroMultiplier(payout, playerTicket, resultTicket, matches, heroQuadrant, N)`.

The function signature `_fullTicketPayout(uint32 playerTicket, uint32 resultTicket, uint8 matches, uint8 currency, uint128 betAmount, uint256 roiBps, uint256 wwxrpHighRoi, bool heroEnabled, uint8 heroQuadrant) private pure returns (uint256 payout)` STAYS THE SAME (no signature change at this level).

#### (h) SWAP producer call site at L607 (DGN-12)

```solidity
// L607 BEFORE: uint32 resultTicket = DegenerusTraitUtils.packedTraitsFromSeed(resultSeed);
// L607 AFTER:  uint32 resultTicket = DegenerusTraitUtils.packedTraitsDegenerette(resultSeed);
```

#### (i) THREAD `betAmount` into `_distributePayout` (D-267-PAYSPLIT-05)

Update the call site at L656:

```solidity
// L656 BEFORE: _distributePayout(player, currency, payout, lootboxWord);
// L656 AFTER:  _distributePayout(player, currency, amountPerTicket, payout, lootboxWord);
```

`amountPerTicket` is uint128 and already in scope at L656 (decoded earlier in the spin loop ~L580).

#### (j) REWRITE `_distributePayout` signature + body (L678-740) for 3-tier ETH split rule

Replace L678-740 with (use the CONTEXT.md `<specifics>` sketch as the source of truth + adapt to existing variable names + preserve frozen-pool revert-on-insufficient + preserve PayoutCapped event signature):

```solidity
    /// @dev Distributes payout to player. ETH-currency 3-tier split rule:
    ///        - payout <= 3 * betAmount       → 100% ETH (no lootbox conversion)
    ///        - 3 * bet < payout <= 10 * bet  → 2.5 * betAmount ETH (flat floor) + remainder lootbox
    ///        - payout > 10 * betAmount       → payout / 4 ETH (25% standard) + remainder lootbox
    ///      Pool cap (ETH_WIN_CAP_BPS = 10% of futurePool) takes precedence over all three tiers:
    ///      if computed ethShare exceeds 10% of pool, excess flips to lootbox.
    ///      Frozen-pool branch retains its solvency-check posture (revert-on-insufficient pending future).
    /// @param player The player to receive the payout.
    /// @param currency The currency type (0=ETH, 1=BURNIE, 3=WWXRP).
    /// @param betAmount The per-ticket bet amount (uint128).
    /// @param payout The total payout amount (uint256).
    /// @param rngWord The RNG word for lootbox conversion (only used for ETH).
    function _distributePayout(
        address player,
        uint8 currency,
        uint128 betAmount,
        uint256 payout,
        uint256 rngWord
    ) private {
        if (currency == CURRENCY_ETH) {
            // 3-tier split: tier 1 (≤3× bet) all-ETH; tier 2 (3-10×) 2.5× bet floor; tier 3 (>10×) 25% standard.
            uint256 ethShare;
            uint256 lootboxShare;
            uint256 threeBet = uint256(betAmount) * 3;
            if (payout <= threeBet) {
                ethShare = payout;
                lootboxShare = 0;
            } else {
                uint256 minEth = (uint256(betAmount) * 5) / 2;   // 2.5 * bet (PAY-SPLIT-02 floor)
                uint256 stdEth = payout / 4;                      // 25% (PAY-SPLIT-02 standard)
                ethShare = stdEth > minEth ? stdEth : minEth;     // max(2.5*bet, payout/4)
                lootboxShare = payout - ethShare;
            }

            if (prizePoolFrozen) {
                // Frozen path: pending future debit + claimable ETH credit (unchanged solvency posture).
                (uint128 pNext, uint128 pFuture) = _getPendingPools();
                if (uint256(pFuture) < ethShare) revert E();
                _setPendingPools(pNext, pFuture - uint128(ethShare));
                _addClaimableEth(player, ethShare);
            } else {
                // Unfrozen path: pool cap (ETH_WIN_CAP_BPS) takes precedence over the 3-tier split (PAY-SPLIT-03).
                uint256 pool = _getFuturePrizePool();
                uint256 maxEth = (pool * ETH_WIN_CAP_BPS) / 10_000;
                if (ethShare > maxEth) {
                    lootboxShare += ethShare - maxEth;
                    ethShare = maxEth;
                    emit PayoutCapped(player, ethShare, lootboxShare);
                }
                unchecked { pool -= ethShare; }
                _setFuturePrizePool(pool);
                _addClaimableEth(player, ethShare);
            }

            if (lootboxShare > 0) {
                _resolveLootboxDirect(player, lootboxShare, rngWord);
            }
        } else if (currency == CURRENCY_BURNIE) {
            coin.mintForGame(player, payout);
        } else if (currency == CURRENCY_WWXRP) {
            wwxrp.mintPrize(player, payout);
        }
    }
```

CURRENCY_BURNIE + CURRENCY_WWXRP branches MUST be byte-identical to L735-739 (D-267-PAYSPLIT-04). Frozen-pool branch retains the existing solvency-check + side-channel posture.

### Validation steps before presenting diff:

1. Run `npx hardhat compile` — must succeed.
2. Run all `task3_contract_invariants` canonical_grep_recipes (24 grep assertions) — all must pass.
3. Cross-check against the planning note byte-by-byte for the 25 packed constants (Task 2 already verified script vs note; this step verifies note vs implementation).
4. `git diff` — review full diff yourself before presenting.

### Discipline (load-bearing):

- Per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md`: agent does NOT commit until user types `approved`. Agent does NOT pre-approve. Agent does NOT claim the diff is "already approved" or "tentatively approved".
- Per `feedback_no_history_in_comments.md`: ZERO occurrences of "was/used to/previously/formerly/changed from/removed/deleted" in any new or rewritten NatSpec / inline comment. The comments must read as the CURRENT v37.0 design.
- Per `feedback_manual_review_before_push.md`: agent does NOT `git push`.
- Per `feedback_wait_for_approval.md`: acceptance criterion is the user's explicit `approved` string. If the user requests changes, return to the relevant edit + re-present.

After approval: commit with subject `feat(267): degenerette producer + 5-table payout rewrite + 3-tier ETH split [DGN-01..15, PAY-SPLIT-01..03]`.
  </action>
  <verify>
    <automated>npx hardhat compile 2>&1 | tail -3 && grep -nE "function packedTraitsDegenerette\(uint256 [a-z]+\) internal pure returns \(uint32\)" contracts/DegenerusTraitUtils.sol | wc -l && grep -nE "function _countGoldQuadrants\(uint32 ticket\) private pure returns \(uint8 count\)" contracts/modules/DegenerusGameDegeneretteModule.sol | wc -l && grep -cE "_evNormalizationRatio" contracts/modules/DegenerusGameDegeneretteModule.sol && grep -cE "QUICK_PLAY_PAYOUTS_N[0-4]_PACKED" contracts/modules/DegenerusGameDegeneretteModule.sol && grep -cE "uint128 betAmount" contracts/modules/DegenerusGameDegeneretteModule.sol</automated>
  </verify>
  <what-built>
**Phase 267 single batched contract diff:**

- `contracts/DegenerusTraitUtils.sol` — ADDITIVE only:
  - NEW `packedTraitsDegenerette(uint256 rand) internal pure returns (uint32)` helper (post-L178).
  - NEW `_degTrait(uint64 rnd) private pure returns (uint8)` helper (immediately after).
  - Existing 3 functions (`weightedColorBucket`, `traitFromWord`, `packedTraitsFromSeed`) BYTE-IDENTICAL to v36.0 closure HEAD `1c0f0913` (DGN-14, Phase 268 SURF-01 carry).

- `contracts/modules/DegenerusGameDegeneretteModule.sol` — REWRITE:
  - **Constants:** DELETE `QUICK_PLAY_BASE_PAYOUTS_PACKED` + `QUICK_PLAY_BASE_PAYOUT_8_MATCHES` + `WWXRP_BONUS_FACTOR_BUCKET5..8` block + `HERO_BOOST_PACKED`. ADD 25 per-N packed constants (5 × payouts + 5 × M8 + 5 × hero + 5 × WWXRP factors). Net 11 → 24 (DGN-05/06/08/10/11).
  - **Stale prose rewrites:** L239 RTP claim → "basePayoutEV = 100 centi-x exact per N; player RTP = roiBps/10000". L262 "all weights=10" → "per-N factors derived from each N's basePayout schedule + binomial-convolution P_N(M) + 10/30/30/30 split". §EXACT EV NORMALIZATION prose at L287-298 DELETED entirely. L316 HERO_BOOST_PACKED reference → per-N HERO_BOOST_N{0..4}_PACKED reference. ZERO "was/used to/previously" prose anywhere (DGN-13, `feedback_no_history_in_comments.md`).
  - **Helpers:** ADD `_countGoldQuadrants(uint32 ticket) private pure returns (uint8 count)` per D-267-COUNTGOLD-01 (DGN-03).
  - **Function rewrites:** `_getBasePayoutBps` → `(uint8 N, uint8 matches)` per-N dispatch (DGN-04). `_wwxrpBonusRoiForBucket` REPLACED with `_wwxrpFactor(uint8 N, uint8 bucket)` per-N dispatch (DGN-09). `_applyHeroMultiplier` rewritten for symbol-only match + per-N dispatch (now 6-arg: adds `uint8 N`) (DGN-07). `_fullTicketPayout` body rewritten to thread N through hero + WWXRP factor lookups + drop `_evNormalizationRatio` invocation.
  - **Deletions:** `_evNormalizationRatio` body (L808-851) DELETED + its call site at L965-969 DELETED (DGN-02).
  - **Producer swap:** L607 `packedTraitsFromSeed(resultSeed)` → `packedTraitsDegenerette(resultSeed)` (DGN-12).
  - **`_distributePayout` rewrite:** signature extended to `(address player, uint8 currency, uint128 betAmount, uint256 payout, uint256 rngWord)` per D-267-PAYSPLIT-05. ETH-currency branch implements 3-tier split: tier 1 (≤3× bet) → 100% ETH; tier 2 (3-10× bet) → 2.5× bet ETH floor + remainder lootbox; tier 3 (>10× bet) → 25% ETH + 75% lootbox (PAY-SPLIT-01..02). Pool-cap (`ETH_WIN_CAP_BPS`) applied AFTER the split + takes precedence (PAY-SPLIT-03). Frozen-pool branch retains existing solvency-check posture. CURRENCY_BURNIE + CURRENCY_WWXRP branches BYTE-UNCHANGED (PAY-SPLIT-04).
  - **Call site update:** L656 `_distributePayout(player, currency, payout, lootboxWord)` → `_distributePayout(player, currency, amountPerTicket, payout, lootboxWord)` (PAY-SPLIT-05 threading).

**Cross-surface preservation (DGN-15 + Phase 268 SURF-02..04 carry):**

- MintModule + JackpotModule + LootboxModule + EntropyLib + JackpotBucketLib + GameStorage UNTOUCHED (Task 3 acceptance criteria includes `git diff 1c0f0913..HEAD -- <those files>` returning empty).
- Zero new storage slots; zero new public/external mutation entry points; zero new external pure entry points (`packedTraitsDegenerette` is `internal pure`, inlined into consumer); zero new admin functions; zero new modifiers.

**Constants verified at Task 2:** all 25 packed constants byte-identical between `derive_5_tables.py` Fraction-exact stdout and the .sol pasted hex (267-01-CONSTANTS-VERIFY.md PASS_ALL_25).

**18 of 18 Phase 267 requirements pending PASS:** DGN-01..15 + PAY-SPLIT-01..03.
  </what-built>
  <how-to-verify>
**Per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_wait_for_approval.md`:** the user reviews the COMPLETE diff before commit. Agent does NOT commit until explicit `approved` string is typed.

1. Run `git diff -- contracts/DegenerusTraitUtils.sol contracts/modules/DegenerusGameDegeneretteModule.sol | less` — review the full diff.
2. Run `git diff 1c0f09132d7439af9881c56fe197f81757f8164a..HEAD -- contracts/modules/DegenerusGameMintModule.sol contracts/modules/DegenerusGameJackpotModule.sol contracts/modules/DegenerusGameLootboxModule.sol contracts/libraries/EntropyLib.sol contracts/libraries/JackpotBucketLib.sol contracts/storage/GameStorage.sol` — expect empty (cross-surface byte-identity preserved per DGN-15 + Phase 268 SURF-02..04 carry).
3. Run `npx hardhat compile` — expect clean compile (no warnings beyond pre-existing).
4. Run the 24 `task3_contract_invariants` grep assertions from the YAML frontmatter `canonical_grep_recipes` block. ALL must pass.
5. Cross-check the 25 packed constant hex strings in `contracts/modules/DegenerusGameDegeneretteModule.sol` against the verified-PASS_ALL_25 entries in `267-01-CONSTANTS-VERIFY.md`. (Task 2 already proved script-vs-note byte-identity; this step verifies note-vs-implementation byte-identity.)
6. Run `grep -cE "(was |used to|previously|formerly|changed from|removed |deleted )" contracts/DegenerusTraitUtils.sol contracts/modules/DegenerusGameDegeneretteModule.sol | grep -vE "^[^:]+:[0-9]+:\\s*//.*(SPDX|@author|pragma)"` — expect ZERO history-prose hits in the new/rewritten regions (`feedback_no_history_in_comments.md` discipline).
7. Visually inspect `_distributePayout` 3-tier branching: confirm the worked examples from CONTEXT.md `<specifics>` table hold:
   - bet=1.0, payout=2.0 → ethShare=2.0, lootboxShare=0
   - bet=1.0, payout=3.0 → ethShare=3.0, lootboxShare=0 (boundary inclusive)
   - bet=1.0, payout=3.01 → ethShare=2.5, lootboxShare=0.51 (tier 2 floor)
   - bet=1.0, payout=10.0 → ethShare=2.5, lootboxShare=7.5 (tier 2-3 boundary)
   - bet=1.0, payout=100.0 → ethShare=25.0, lootboxShare=75.0 (tier 3 standard)
8. Confirm CURRENCY_BURNIE + CURRENCY_WWXRP branches are BYTE-IDENTICAL to L735-739 of the v36.0 baseline (existing `coin.mintForGame(player, payout)` and `wwxrp.mintPrize(player, payout)` lines unchanged).

**If user requests changes:** return to the relevant edit + re-present at this gate.

**Per `feedback_wait_for_approval.md`:** wait for the user's explicit `approved` string.

**Then commit (single atomic commit):**

```bash
git add contracts/DegenerusTraitUtils.sol contracts/modules/DegenerusGameDegeneretteModule.sol
git commit -m "$(cat <<'EOF'
feat(267): degenerette producer + 5-table payout rewrite + 3-tier ETH split [DGN-01..15, PAY-SPLIT-01..03]

- TraitUtils: ADD packedTraitsDegenerette + _degTrait (internal pure / private pure;
  DGN-01 + DGN-14). Existing 3 functions byte-identical.
- DegeneretteModule: DELETE _evNormalizationRatio + its call site (DGN-02);
  ADD _countGoldQuadrants (DGN-03); REWRITE _getBasePayoutBps for per-N dispatch (DGN-04);
  ADD 25 packed per-N constants (5 × payouts/M8/hero/WWXRP); DELETE 4 single-table
  + 2 normalizer constants; net 11 → 24 (DGN-05/06/08/10/11). _applyHeroMultiplier
  rewritten symbol-only + per-N (DGN-07). _wwxrpBonusRoiForBucket replaced with
  _wwxrpFactor(N, bucket) per-N dispatch (DGN-09). L607 producer swap (DGN-12).
  4 stale comments rewritten in language of CURRENT design (DGN-13). Zero new
  storage slots / public-external entries / admin functions (DGN-15).
- _distributePayout: signature extended to 5-arg (player, currency, betAmount,
  payout, rngWord) per PAY-SPLIT-05. 3-tier ETH split rule: ≤3× bet all-ETH
  (PAY-SPLIT-01); 3-10× bet 2.5× bet ETH floor + remainder lootbox (PAY-SPLIT-02);
  >10× bet 25%/75% standard (PAY-SPLIT-02). Pool-cap takes precedence on top
  (PAY-SPLIT-03). CURRENCY_BURNIE + CURRENCY_WWXRP branches byte-unchanged
  (PAY-SPLIT-04). Frozen-pool branch retains existing solvency-check posture.

Constants verification: 25 of 25 packed constants byte-identical between
derive_5_tables.py Fraction-exact stdout and pasted .sol hex per
267-01-CONSTANTS-VERIFY.md (D-267-CONSTVERIFY-01).

Empirical verification (per-N EV exactness, producer chi², bonus EV) deferred
to Phase 268 STAT-01..07. Cross-surface byte-identity tests deferred to
Phase 268 SURF-01..06. Audit deliverable + closure signal deferred to
Phase 271 (terminal).

Per feedback_no_contract_commits.md + feedback_batch_contract_approval.md +
feedback_never_preapprove_contracts.md + feedback_wait_for_approval.md:
single batched diff approved by user before commit.

Refs: D-267-DIST-01 / D-267-NORM-01 / D-267-EV-TARGET-01 / D-267-HERO-01..02 /
D-267-WWXRP-SPLIT-01 / D-267-PRODUCER-API-01 / D-267-COUNTGOLD-01 /
D-267-VISIBILITY-01 / D-267-PAYSPLIT-01..05 / D-267-CONSTVERIFY-01 /
D-267-COMMENTS-01 / D-267-APPROVAL-02
EOF
)"
```

**Per `feedback_manual_review_before_push.md`:** agent does NOT `git push`. User pushes when ready.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the single batched contract commit, or describe required changes.</resume-signal>
  <acceptance_criteria>
    - **Pre-flight:** `267-01-CONSTANTS-VERIFY.md` exists and `grep -c "PASS_ALL_25" 267-01-CONSTANTS-VERIFY.md` returns ≥ 1 (Task 2 PASS_ALL signal). If missing or MISMATCH, this task is BLOCKED until Task 2 re-runs cleanly.
    - **User approval gate:** user responds with explicit `approved` string before `git commit` runs. Agent does NOT pre-approve, does NOT commit on conditional approval ("looks good but..."), does NOT skip to commit on silence. Per `feedback_wait_for_approval.md` + `feedback_never_preapprove_contracts.md`.
    - **Compile:** `npx hardhat compile` exits 0 with no errors.
    - **TraitUtils additive only:** `git diff 1c0f09132d7439af9881c56fe197f81757f8164a..HEAD -- contracts/DegenerusTraitUtils.sol | grep -E "^[+-].*function (weightedColorBucket|traitFromWord|packedTraitsFromSeed)"` returns empty (existing 3 functions BYTE-IDENTICAL — DGN-14).
    - **Cross-surface byte-identity:** `git diff 1c0f09132d7439af9881c56fe197f81757f8164a..HEAD -- contracts/modules/DegenerusGameMintModule.sol contracts/modules/DegenerusGameJackpotModule.sol contracts/modules/DegenerusGameLootboxModule.sol contracts/libraries/EntropyLib.sol contracts/libraries/JackpotBucketLib.sol contracts/storage/GameStorage.sol` returns empty (DGN-15 + SURF-02..04 carry).
    - **New helpers present (DGN-01 + DGN-03):** `grep -cE "function packedTraitsDegenerette\(uint256 [a-z]+\) internal pure returns \(uint32\)" contracts/DegenerusTraitUtils.sol` returns 1; `grep -cE "function _degTrait\(uint64 [a-z]+\) private pure returns \(uint8\)" contracts/DegenerusTraitUtils.sol` returns 1; `grep -cE "function _countGoldQuadrants\(uint32 ticket\) private pure returns \(uint8 count\)" contracts/modules/DegenerusGameDegeneretteModule.sol` returns 1.
    - **Normalizer fully removed (DGN-02):** `grep -cE "_evNormalizationRatio" contracts/modules/DegenerusGameDegeneretteModule.sol` returns 0.
    - **25 per-N packed constants present (DGN-05/06/08/10):** `grep -cE "QUICK_PLAY_PAYOUTS_N[0-4]_PACKED" contracts/modules/DegenerusGameDegeneretteModule.sol` ≥ 5 (declarations); `grep -cE "QUICK_PLAY_PAYOUT_N[0-4]_M8" ...` ≥ 5; `grep -cE "HERO_BOOST_N[0-4]_PACKED" ...` ≥ 5; `grep -cE "WWXRP_FACTORS_N[0-4]_PACKED" ...` ≥ 5.
    - **Old single-table constants removed (DGN-11):** `grep -cE "QUICK_PLAY_BASE_PAYOUTS_PACKED|QUICK_PLAY_BASE_PAYOUT_8_MATCHES|WWXRP_BONUS_FACTOR_BUCKET[5-8]|HERO_BOOST_PACKED" contracts/modules/DegenerusGameDegeneretteModule.sol` returns 0.
    - **Producer swap (DGN-12):** `grep -cE "DegenerusTraitUtils\\.packedTraitsDegenerette\\(resultSeed\\)" contracts/modules/DegenerusGameDegeneretteModule.sol` returns 1; `grep -cE "DegenerusTraitUtils\\.packedTraitsFromSeed\\(resultSeed\\)" ...` returns 0 in DegeneretteModule (Mint/Jackpot still use packedTraitsFromSeed for their own paths but those files are UNTOUCHED).
    - **`_distributePayout` 5-arg signature (PAY-SPLIT-05):** `grep -cE "uint128 betAmount,?" contracts/modules/DegenerusGameDegeneretteModule.sol` returns ≥ 1 (declaration in `_distributePayout` parameter list); call site `grep -cE "_distributePayout\\(player, currency, amountPerTicket, payout, lootboxWord\\)"` returns 1.
    - **3-tier rule indicators present (PAY-SPLIT-01..02):** `grep -cE "uint256\\(betAmount\\) \\* 3|3 \\* uint256\\(betAmount\\)|threeBet" contracts/modules/DegenerusGameDegeneretteModule.sol` returns ≥ 1 (3× threshold present); `grep -cE "\\* 5\\) / 2|5\\) / 2|2 \\* uint256\\(betAmount\\) \\+|minEth" contracts/modules/DegenerusGameDegeneretteModule.sol` returns ≥ 1 (2.5× floor present).
    - **Pool-cap precedence preserved (PAY-SPLIT-03):** `grep -cE "ETH_WIN_CAP_BPS" contracts/modules/DegenerusGameDegeneretteModule.sol` returns ≥ 2 (constant declaration + usage in `_distributePayout`); `grep -cE "PayoutCapped" contracts/modules/DegenerusGameDegeneretteModule.sol` returns ≥ 2 (event declaration + emit).
    - **CURRENCY_BURNIE + CURRENCY_WWXRP byte-unchanged (PAY-SPLIT-04):** `grep -nE "coin\\.mintForGame\\(player, payout\\)" contracts/modules/DegenerusGameDegeneretteModule.sol` returns 1 hit; `grep -nE "wwxrp\\.mintPrize\\(player, payout\\)" ...` returns 1 hit.
    - **Stale prose absent (DGN-13):** `grep -cE "99\\.99% RTP|all weights=10|EXACT EV NORMALIZATION" contracts/modules/DegenerusGameDegeneretteModule.sol` returns 0.
    - **No history prose (`feedback_no_history_in_comments.md`):** `grep -cE "(was |used to|previously|formerly|changed from)" contracts/DegenerusTraitUtils.sol contracts/modules/DegenerusGameDegeneretteModule.sol` returns 0 in the new/rewritten regions (manual triage acceptable for legacy unchanged comments).
    - **Zero new public/external entries (DGN-15 + D-267-VISIBILITY-01):** `git diff 1c0f09132d7439af9881c56fe197f81757f8164a..HEAD -- contracts/ | grep -E "^\\+.*function .* (public|external)" | grep -vE "internal|private"` returns 0 lines.
    - **Single batched commit landed:** `git log --oneline -1` shows subject containing `feat(267)` + `degenerette producer + 5-table payout rewrite + 3-tier ETH split` + `[DGN-01..15, PAY-SPLIT-01..03]`. Commit touches EXACTLY the 2 files `contracts/DegenerusTraitUtils.sol` + `contracts/modules/DegenerusGameDegeneretteModule.sol`.
    - **No push:** `git status` shows commits ahead of remote are NOT pushed (per `feedback_manual_review_before_push.md`).
    - **18 of 18 Phase 267 requirements:** all 18 require IDs grep-citable to file+line evidence as documented in the must_haves artifacts table.
  </acceptance_criteria>
  <done>User typed `approved`; single batched contract commit landed with subject `feat(267): degenerette producer + 5-table payout rewrite + 3-tier ETH split [DGN-01..15, PAY-SPLIT-01..03]`; all 24 task3_contract_invariants grep assertions PASS; npx hardhat compile clean; cross-surface byte-identity preserved (Mint/Jackpot/Lootbox/EntropyLib/JackpotBucketLib/GameStorage `git diff` empty); 25 per-N packed constants byte-identical to Task 2 verification + planning-note source-of-truth; ZERO new public/external entries; ZERO history prose; agent did NOT push.</done>
</task>

<!-- ================================================================ -->
<!-- WAVE 4 — PHASE-CLOSE SUMMARY (AGENT-COMMITTED, AUTONOMOUS)        -->
<!-- ================================================================ -->

<task type="auto">
  <name>Task 4 [Wave 4]: Author 267-01-SUMMARY.md + STATE.md flips → AGENT-COMMITTED phase-close</name>
  <files>.planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-SUMMARY.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-CONTEXT.md (`<decisions>` D-267-PLAN-01 step 4 + D-267-CLOSURE-02 carry)
    - .planning/milestones/v36.0-phases/266-lootbox-entropy-refactor/266-01-SUMMARY.md (format precedent — frontmatter + per-task atomic-commit log + per-REQ tally + cross-phase cross-cite density + project-feedback-rules-honored table + commit-readiness register)
    - $HOME/.claude/get-shit-done/templates/summary.md (canonical SUMMARY template)
    - .planning/STATE.md (current state — verify Current Position + last_activity + progress fields; will need updating)
    - .planning/ROADMAP.md (verify Phase 267 line in `## Phases` section + Progress table at lines 105-111 — will tick the checkbox + update Plans Complete + Status + Completed)
  </read_first>
  <action>
**Goal:** Author the Phase 267 SUMMARY + flip STATE.md + ROADMAP.md Progress table for the phase-close (NOT milestone-close — that lives in Phase 271 terminal).

### Step 1 — Author `267-01-SUMMARY.md` (use `Write` tool):

Mirror the v36 `266-01-SUMMARY.md` format. Required structure:

**Frontmatter:**
```yaml
---
phase: 267-degenerette-producer-5-table-payout-rewrite
phase_number: 267
plan: 267-01
plan_id: 267-01
plan_number: 01
type: summary
status: complete
milestone: v37.0
completed: <today YYYY-MM-DD>
requirements-completed:
  - DGN-01
  - DGN-02
  - DGN-03
  - DGN-04
  - DGN-05
  - DGN-06
  - DGN-07
  - DGN-08
  - DGN-09
  - DGN-10
  - DGN-11
  - DGN-12
  - DGN-13
  - DGN-14
  - DGN-15
  - PAY-SPLIT-01
  - PAY-SPLIT-02
  - PAY-SPLIT-03
baseline: 1c0f09132d7439af9881c56fe197f81757f8164a
baseline_signal: MILESTONE_V36_AT_HEAD_1c0f09132d7439af9881c56fe197f81757f8164a
phase_close_sha: <result of `git rev-parse HEAD` after Task 3 commits + this commit; resolve at commit-time>
milestone_closure_signal: "deferred to Phase 271"
---
```

**Sections:**

1. `# Phase 267 — Degenerette Producer + 5-Table Payout Rewrite (SUMMARY)`
2. `## Overview` — 1-paragraph summary of what shipped (TraitUtils additive + DegeneretteModule rewrite + 3-tier ETH split + betAmount threading; 18 of 18 requirements PASS; tests/audit deferred to Phase 268/271).
3. `## Per-Task Atomic-Commit Log` — table with 4 rows:
   - Task 1: `chore(267): docs upstream fixes for v37.0 phase 267 [...]` — REQUIREMENTS.md DGN-01 + DGN-03 wording fix + PAY-SPLIT/STAT-07/AUDIT-02(h) presence; ROADMAP success-criteria visibility wording + AUDIT-04 attestation drop.
   - Task 2: `chore(267): constants verification — derive_5_tables.py byte-identity proof [D-267-CONSTVERIFY-01]` — 25/25 PASS; 267-01-CONSTANTS-VERIFY.md authored.
   - Task 3: `feat(267): degenerette producer + 5-table payout rewrite + 3-tier ETH split [DGN-01..15, PAY-SPLIT-01..03]` — single batched USER-APPROVED contract commit.
   - Task 4: `docs(267): phase 267 summary + commit-readiness register` — this commit.
4. `## Per-REQ Tally (18 of 18)` — table with 18 rows; for each REQ ID: file+line evidence + verifying grep recipe.
5. `## Cross-Phase Forward Cross-Cites` — Phase 268 STAT-01..07 + SURF-01..06 (empirical verification of Task 3 implementation); Phase 271 AUDIT-01..06 + REG-01..04 (audit deliverable + closure signal).
6. `## Project-Feedback-Rules Honored` — table:
   - `feedback_no_contract_commits.md` — Task 3 awaited explicit `approved` before commit.
   - `feedback_batch_contract_approval.md` — single batched diff for the entire phase contract change.
   - `feedback_never_preapprove_contracts.md` — agent did NOT pre-approve.
   - `feedback_wait_for_approval.md` — explicit `approved` string captured.
   - `feedback_no_history_in_comments.md` — ZERO "was/used to/previously" prose in rewritten regions (verified via grep).
   - `feedback_manual_review_before_push.md` — agent did NOT push.
   - `feedback_skip_research_test_phases.md` — research skipped per CONTEXT.md as source of truth; jumped straight to plan.
7. `## Commit-Readiness Register` (per D-267-CLOSURE-02 carry — TWO-subsection per CONTEXT.md):
   - **§i USER-APPROVED contracts:** 1 commit (Task 3 — `feat(267): ...` with SHA + short hash).
   - **§ii USER-APPROVED tests:** 0 commits (Phase 268 owns tests).
   - **§iii AGENT-COMMITTED planning artifacts:** 4 commits — Task 1 (docs upstream fixes) + Task 2 (constants verify) + Task 4 (this summary) + the PLAN.md authoring commit if not already landed at orchestrator level. Cite each by SHA short hash.
   - NO §iv awaiting-approval subsection.
8. `## Open Items / Deferrals` — explicit cross-cite to Phase 268 (STAT + SURF) + Phase 269 (LBX cleanup + GASPIN re-pin) + Phase 270 (post-v32 sub-audit) + Phase 271 (audit deliverable + closure signal `MILESTONE_V37_AT_HEAD_<sha>` + KNOWN-ISSUES.md walkthrough + ROADMAP/STATE/MILESTONES milestone-level closure flips).

### Step 2 — Update `.planning/STATE.md`:

Apply targeted edits:

- `last_updated:` → today's UTC timestamp.
- `last_activity:` → "Phase 267 SHIPPED — Degenerette producer + 5-table payout rewrite + 3-tier ETH split [DGN-01..15, PAY-SPLIT-01..03] satisfied; 18/18 requirements PASS; single batched USER-APPROVED contract commit `feat(267): ...` landed; 4 atomic commits total (1 contract + 3 chore/planning). Phase 268 (statistical validation + cross-surface preservation) next."
- `progress.completed_phases:` 0 → 1.
- `progress.completed_plans:` 0 → 1.
- `progress.percent:` 0 → 20.
- `## Current Position`: Phase: 268 (next); Plan: —; Status: "Phase 267 SHIPPED; ready for `/gsd-discuss-phase 268` or `/gsd-plan-phase 268` to draft 268-01-PLAN.md (Degenerette Statistical Validation + Cross-Surface Preservation)"; Resume file: appropriate Phase 268 anchor.
- DO NOT modify the `## Last Shipped Milestone` section (v36.0 stays; milestone-level demotion happens at Phase 271 close).
- DO NOT modify `## Active Milestone` v37.0 status block (still active; only Phase 267 ticked).

### Step 3 — Update `.planning/ROADMAP.md`:

- `## Phases` section: tick the Phase 267 checkbox `- [ ]` → `- [x]` at line ~24.
- `## Progress` table at lines 105-111: Phase 267 row update — `Plans Complete: 0/0` → `1/1`; `Status: Not started` → `Complete`; `Completed: -` → `<today YYYY-MM-DD>`.
- Phase 267 `Plans:` field at line 45 (`Plans: TBD`): replace with `Plans: 1` and add the line `- [x] 267-01-PLAN.md — Degenerette producer + 5-table payout rewrite + 3-tier ETH split (1 plan, 4 tasks: docs fixes + constants verify + USER-APPROVED contract commit + phase-close summary)`.

### Step 4 — Run validation grep recipes:

Run all canonical_grep_recipes `task4_phase_close` recipes — all must pass.

### Step 5 — AGENT-COMMITTED:

Commit with subject `docs(267): phase 267 summary + commit-readiness register`. Include all 3 modified files in the same atomic commit. NO `git push` per `feedback_manual_review_before_push.md`.

This is autonomous (planning artifact + STATE/ROADMAP flips — no contract changes).
  </action>
  <verify>
    <automated>test -f .planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-SUMMARY.md && grep -c "Phase 267 SHIPPED" .planning/STATE.md && grep -cE "^- \[x\] \*\*Phase 267:" .planning/ROADMAP.md && grep -cE "DGN-01..15" .planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-SUMMARY.md && grep -cE "PAY-SPLIT-0[1-3]" .planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `267-01-SUMMARY.md` exists with the 8 required sections + frontmatter listing all 18 `requirements-completed`.
    - `grep -cE "DGN-0[0-9]|DGN-1[0-5]" .planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-SUMMARY.md` returns ≥ 15.
    - `grep -cE "PAY-SPLIT-0[1-3]" .planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-SUMMARY.md` returns ≥ 3.
    - Per-task atomic-commit log table cites 4 commits with their actual SHA short hashes.
    - Per-REQ tally cites file+line evidence + grep recipe for each of 18 IDs.
    - Commit-readiness register has §i + §ii + §iii subsections; ZERO §iv awaiting-approval subsection (D-267-CLOSURE-02 carry).
    - `.planning/STATE.md` Current Position updated to Phase 268; progress.completed_phases = 1; progress.completed_plans = 1; progress.percent = 20; last_activity reflects Phase 267 SHIPPED. Active milestone v37.0 block UNCHANGED (still active). Last Shipped Milestone v36.0 block UNCHANGED.
    - `.planning/ROADMAP.md` Phase 267 checkbox ticked `- [x]`; Progress table row updated to `1/1` + Complete + completion date; `Plans:` field shows `1` + the one PLAN file with checkbox.
    - `task4_phase_close` grep recipes all pass.
    - Single AGENT-COMMITTED commit landed: `git log --oneline -1` shows subject `docs(267): phase 267 summary + commit-readiness register` (or close variant). Touches `267-01-SUMMARY.md` + `.planning/STATE.md` + `.planning/ROADMAP.md`.
    - `git status` clean for `contracts/` (Task 4 emits ZERO contract-tree mutations).
    - NO `git push` invoked.
  </acceptance_criteria>
  <done>267-01-SUMMARY.md authored with 8 sections + 18-row per-REQ tally + 4-row atomic-commit log + project-feedback-rules-honored table + commit-readiness register (TWO subsection per D-267-CLOSURE-02); STATE.md Current Position flipped to Phase 268; ROADMAP.md Phase 267 checkbox ticked + Progress table updated; AGENT-COMMITTED docs commit landed; Phase 267 CLOSED at the plan level; v37.0 milestone still ACTIVE (terminal closure deferred to Phase 271).</done>
</task>

</tasks>

<verification>

## Phase 267 Wave-Gate Verification (executor runs after each wave)

**After Wave 1 (Task 1 commit):**
```bash
grep -cE "DGN-01.*internal pure returns \(uint32\)" .planning/REQUIREMENTS.md  # expect >= 1
grep -cE "_countGoldQuadrants\(uint32 ticket\) private pure" .planning/REQUIREMENTS.md  # expect >= 1
grep -cE "^- \[ \] \*\*PAY-SPLIT-0[1-3]\*\*" .planning/REQUIREMENTS.md  # expect 3
grep -c "STAT-07" .planning/REQUIREMENTS.md  # expect >= 2
grep -c "ALLOWED-NEW-STATELESS-ENTRY" .planning/ROADMAP.md  # expect 0
grep -c "51 total" .planning/REQUIREMENTS.md  # expect 1
git log --oneline -1 .planning/REQUIREMENTS.md .planning/ROADMAP.md  # expect chore(267) docs commit
```

**After Wave 2 (Task 2 commit) — BLOCKS Wave 3 on any MISMATCH:**
```bash
test -f .planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-CONSTANTS-VERIFY.md
grep -c "PASS_ALL_25" .planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-CONSTANTS-VERIFY.md  # expect >= 1
grep -c "MISMATCH" .planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-CONSTANTS-VERIFY.md  # expect 0 in row cells (verdict label allowed elsewhere)
git log --oneline -1 .planning/phases/267-degenerette-producer-5-table-payout-rewrite/  # expect chore(267) constants verify commit
```

**After Wave 3 (Task 3 USER-APPROVED commit):**
```bash
npx hardhat compile  # expect success
grep -nE "function packedTraitsDegenerette\(uint256 [a-z]+\) internal pure returns \(uint32\)" contracts/DegenerusTraitUtils.sol  # expect 1 hit
grep -nE "function _degTrait\(uint64 [a-z]+\) private pure returns \(uint8\)" contracts/DegenerusTraitUtils.sol  # expect 1 hit
grep -nE "function _countGoldQuadrants\(uint32 ticket\) private pure returns \(uint8 count\)" contracts/modules/DegenerusGameDegeneretteModule.sol  # expect 1 hit
grep -cE "_evNormalizationRatio" contracts/modules/DegenerusGameDegeneretteModule.sol  # expect 0
grep -cE "QUICK_PLAY_PAYOUTS_N[0-4]_PACKED" contracts/modules/DegenerusGameDegeneretteModule.sol  # expect >= 5
grep -cE "QUICK_PLAY_PAYOUT_N[0-4]_M8" contracts/modules/DegenerusGameDegeneretteModule.sol  # expect >= 5
grep -cE "HERO_BOOST_N[0-4]_PACKED" contracts/modules/DegenerusGameDegeneretteModule.sol  # expect >= 5
grep -cE "WWXRP_FACTORS_N[0-4]_PACKED" contracts/modules/DegenerusGameDegeneretteModule.sol  # expect >= 5
grep -cE "QUICK_PLAY_BASE_PAYOUTS_PACKED|QUICK_PLAY_BASE_PAYOUT_8_MATCHES|WWXRP_BONUS_FACTOR_BUCKET[5-8]|HERO_BOOST_PACKED" contracts/modules/DegenerusGameDegeneretteModule.sol  # expect 0
grep -nE "DegenerusTraitUtils\\.packedTraitsDegenerette\\(resultSeed\\)" contracts/modules/DegenerusGameDegeneretteModule.sol  # expect 1 hit
grep -nE "uint128 betAmount" contracts/modules/DegenerusGameDegeneretteModule.sol  # expect >= 1 hit (in _distributePayout signature)
grep -nE "_distributePayout\\(player, currency, amountPerTicket, payout, lootboxWord\\)" contracts/modules/DegenerusGameDegeneretteModule.sol  # expect 1 hit
grep -cE "ETH_WIN_CAP_BPS" contracts/modules/DegenerusGameDegeneretteModule.sol  # expect >= 2
grep -cE "PayoutCapped" contracts/modules/DegenerusGameDegeneretteModule.sol  # expect >= 2
grep -cE "99\\.99% RTP|all weights=10|EXACT EV NORMALIZATION" contracts/modules/DegenerusGameDegeneretteModule.sol  # expect 0
git diff 1c0f09132d7439af9881c56fe197f81757f8164a..HEAD -- contracts/DegenerusTraitUtils.sol | grep -E "^[+-].*function (weightedColorBucket|traitFromWord|packedTraitsFromSeed)"  # expect empty (DGN-14)
git diff 1c0f09132d7439af9881c56fe197f81757f8164a..HEAD -- contracts/modules/DegenerusGameMintModule.sol contracts/modules/DegenerusGameJackpotModule.sol contracts/modules/DegenerusGameLootboxModule.sol contracts/libraries/EntropyLib.sol contracts/libraries/JackpotBucketLib.sol contracts/storage/GameStorage.sol  # expect empty (DGN-15 + SURF-02..04 carry)
git diff 1c0f09132d7439af9881c56fe197f81757f8164a..HEAD -- contracts/ | grep -E "^\\+.*function .* (public|external)" | grep -vE "internal|private"  # expect empty (zero new public/external entries)
git log --oneline -1  # expect feat(267) ... [DGN-01..15, PAY-SPLIT-01..03]
```

**After Wave 4 (Task 4 commit):**
```bash
test -f .planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-SUMMARY.md
grep -c "Phase 267 SHIPPED" .planning/STATE.md  # expect 1
grep -cE "^- \[x\] \*\*Phase 267:" .planning/ROADMAP.md  # expect 1
git log --oneline -4  # expect 4 commits: docs(267) summary, feat(267) contracts, chore(267) constants verify, chore(267) docs upstream fixes
```

**Cross-wave invariant (entire phase):** zero `git push` invocations by agent (per `feedback_manual_review_before_push.md`).

</verification>

<success_criteria>

Phase 267 SHIPPED when ALL of the following hold:

1. **Upstream-doc fixes (Wave 1, Task 1):** REQUIREMENTS.md DGN-01 visibility (`internal pure`) + DGN-03 signature (`(uint32 ticket) private pure returns (uint8)`) corrected; PAY-SPLIT-01..03 + STAT-07 + AUDIT-02 surface (h) rows present; Coverage line `51 total`; traceability rows present. ROADMAP.md Phase 267 success criteria 1+5 visibility wording + criterion 6 3-tier rule + Phase 268 STAT-07 + Phase 271 AUDIT-02 surface (h) + Phase 271 AUDIT-04 attestation drops "ALLOWED-NEW-STATELESS-ENTRY". Single AGENT-COMMITTED chore commit landed. [Doc-vs-impl coherence enforced for all locked decisions]

2. **Constants verification (Wave 2, Task 2) — BLOCKS Wave 3:** `267-01-CONSTANTS-VERIFY.md` AGENT-COMMITTED with full `derive_5_tables.py` stdout + 25-row byte-identity assertion table + verdict `PASS_ALL_25`. Any MISMATCH BLOCKS Task 3 until resolved per D-267-CONSTVERIFY-01. [25 of 25 packed constants byte-identical between Fraction-exact Python derivation and planning-note pasted .sol hex]

3. **Contract diff (Wave 3, Task 3) — USER-APPROVED single batched commit:**
   - **TraitUtils additive only (DGN-01 + DGN-14):** new `packedTraitsDegenerette(uint256) internal pure returns (uint32)` + `_degTrait(uint64) private pure returns (uint8)` helpers; existing 3 functions byte-identical.
   - **DegeneretteModule rewrite (DGN-02..15):** `_evNormalizationRatio` deleted (DGN-02); `_countGoldQuadrants(uint32) private pure` added (DGN-03); `_getBasePayoutBps(uint8 N, uint8 matches)` per-N dispatch (DGN-04); 25 packed per-N constants added (DGN-05/06/08/10); 4 single-table + 2 normalizer constants deleted; net 11 → 24 (DGN-11); symbol-only hero match + per-N dispatch (DGN-07); per-N WWXRP factor dispatch (DGN-09); L607 producer swap (DGN-12); 4 stale comments rewritten in language of CURRENT design (DGN-13); zero new storage / public-external entries / admin functions / modifiers / external pure entries (DGN-15 + D-267-VISIBILITY-01).
   - **`_distributePayout` rewrite (PAY-SPLIT-01..05):** 5-arg signature with `betAmount` (uint128) inserted between currency and payout; ETH-currency 3-tier split (≤3× bet all-ETH; 3-10× bet 2.5× bet ETH floor + remainder lootbox; >10× bet 25%/75% standard); pool-cap precedence; CURRENCY_BURNIE + CURRENCY_WWXRP byte-unchanged; frozen-pool branch retains existing solvency-check posture; L656 call site threads `amountPerTicket`.
   - **Compile:** `npx hardhat compile` clean.
   - **Cross-surface byte-identity:** Mint/Jackpot/Lootbox/EntropyLib/JackpotBucketLib/GameStorage `git diff 1c0f0913..HEAD` empty.
   - **Discipline:** USER explicitly typed `approved` before commit; agent did NOT pre-approve, did NOT push. Subject `feat(267): degenerette producer + 5-table payout rewrite + 3-tier ETH split [DGN-01..15, PAY-SPLIT-01..03]`. [`feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_wait_for_approval.md` + `feedback_manual_review_before_push.md` honored]

4. **Phase-close summary (Wave 4, Task 4):** `267-01-SUMMARY.md` AGENT-COMMITTED with frontmatter listing 18 `requirements-completed` (DGN-01..15 + PAY-SPLIT-01..03) + per-task atomic-commit log (4 rows) + per-REQ tally (18 rows; file+line evidence + grep recipe) + cross-phase forward cross-cites (Phase 268 STAT/SURF, Phase 269 LBX/GASPIN, Phase 270 DELTA, Phase 271 AUDIT/REG) + project-feedback-rules-honored table + TWO-subsection commit-readiness register per D-267-CLOSURE-02 carry (NO §iv awaiting-approval subsection). STATE.md Current Position flipped to Phase 268; progress 0/0 → 1/1; percent 0 → 20. ROADMAP.md Phase 267 checkbox ticked + Progress table updated.

5. **Discipline (entire phase):**
   - 4 atomic commits (1 USER-APPROVED contract + 3 AGENT-COMMITTED chore/planning).
   - ZERO `git push` invocations by agent.
   - ZERO history-prose strings ("was/used to/previously/formerly/changed from/removed/deleted") in any new/rewritten contract NatSpec or inline comment.
   - ZERO contract changes in Tasks 1, 2, 4 (those are autonomous chore/planning-only).
   - User explicit `approved` string captured before Task 3 commit (verified via Task 3 `<resume-signal>` + commit-time witness).

6. **18 of 18 Phase 267 requirements PASS:** DGN-01..15 + PAY-SPLIT-01..03 — each REQ ID maps to ≥ 1 task in this plan + ≥ 1 evidence artifact at v37.0 phase-close HEAD.

7. **Phase 267 ≠ terminal:** v37.0 milestone-level closure (closure signal `MILESTONE_V37_AT_HEAD_<sha>` + KNOWN-ISSUES.md walkthrough + audit deliverable + ROADMAP/STATE/MILESTONES milestone-level demotion + final user-review gate) DEFERRED to Phase 271. Phase 267 SUMMARY records this deferral explicitly in `## Open Items / Deferrals`.

8. **Empirical / cross-surface verification DEFERRED to Phase 268:** STAT-01..07 (per-N EV exactness + producer chi² + bonus EV + match-count histogram + ETH split-rule distribution validation) + SURF-01..06 (TraitUtils existing functions byte-identical + JackpotModule + LootboxModule + EntropyLib byte-identical + extended SurfaceRegression test + advanceGame gas envelope). Phase 267 ships contracts only.

</success_criteria>

<output>
After completion, the phase artifacts are:

- `.planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-PLAN.md` (this file)
- `.planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-CONSTANTS-VERIFY.md` (Task 2)
- `.planning/phases/267-degenerette-producer-5-table-payout-rewrite/267-01-SUMMARY.md` (Task 4)

Per Task 4, the SUMMARY mirrors the v36 `266-01-SUMMARY.md` format (frontmatter with closure_signal placeholder pointing to Phase 271 + per-task atomic-commit log + per-REQ tally + cross-phase cross-cite density + project-feedback-rules-honored table + TWO-subsection commit-readiness register per D-267-CLOSURE-02).
</output>
