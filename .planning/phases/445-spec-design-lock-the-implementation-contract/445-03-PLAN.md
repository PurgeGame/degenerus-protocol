---
phase: 445-spec-design-lock-the-implementation-contract
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-E-entrypoints.md
autonomous: true
requirements: [FOIL-02, FOIL-03, FOIL-04, MATCH-03, MATCH-04, MATCH-06, MATCH-07, MATCH-08, MATCH-09, MATCH-10, SEC-03]
must_haves:
  truths:
    - "An IMPL author can write buyFoilPack + claimFoilMatch with no further signature decision — params, ordering, the corrected 400-scale ticket queue, and the 75/25 pool fork are all pinned"
    - "The LIVE-vs-HERO-FREE winning-set re-derivation is byte-faithful (4-of-4 gated on heroFreeCount → steer-proof) and the dailyHeroWagers[day-1] anchor is captured"
    - "The isolated payout table + 40/40/20 lane + ETH 10%-cap reuse + whale-pass grant are pinned, and the ≈2-faces/pack/30d calibration is confirmed and reported (flag if materially off)"
    - "Module placement (new GAME_FOILPACK_MODULE) + EIP-170 headroom estimate is stated with the re-measure-at-IMPL caveat"
  artifacts:
    - path: ".planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-E-entrypoints.md"
      provides: "The locked entrypoint signatures, match algorithm, payout lanes, calibration, and placement"
      contains: "_queueTicketsScaled(buyer, _activeTicketLevel(), 400, false)"
  key_links:
    - from: "445-SPEC-E-entrypoints.md (claimFoilMatch 4-of-4 gate)"
      to: "heroFreeCount==4 (steer-proof, SEC-01 basis)"
      via: "HERO-FREE pure-VRF re-derivation"
      pattern: "heroFreeCount|HERO-FREE|hero-free"
    - from: "445-SPEC-E-entrypoints.md (ETH lane)"
      to: "ETH_WIN_CAP_BPS=1000 (SEC-02 basis)"
      via: "cloned cap+spill from DegenerusGameDegeneretteModule.sol:877-915"
      pattern: "ETH_WIN_CAP_BPS|877-915|10%"
---

<objective>
Author the **entrypoints + match + payout + calibration + placement** section of the 445 design-lock SPEC:
the `buyFoilPack()` / `claimFoilMatch(day, ticketIndex, drawKind)` signatures and ordered bodies, the
LIVE-vs-HERO-FREE winning-set re-derivation, the isolated 40/40/20 payout schedule, the ≈2-faces/30d
calibration confirm, and the `GAME_FOILPACK_MODULE` placement under EIP-170.

Purpose: lock FOIL-02/03/04, MATCH-03/04/06/07/08/09/10, the SEC-01/SEC-02 design basis, and SEC-03
placement. RESEARCH §B/§E/§F/§G are adversarially verified (V2/V3 PASS) with corrected anchors; this plan
transcribes the corrected numbers (400-scale queue, `dailyHeroWagers[day-1]`, ETH-cap clone at `:877-915`,
unified `foilMatchClaimed`) verbatim into the SPEC.

Output: `445-SPEC-E-entrypoints.md` (a SPEC section file consumed by Plan 04's consolidation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/445-spec-design-lock-the-implementation-contract/445-RESEARCH.md
@.planning/phases/445-spec-design-lock-the-implementation-contract/445-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/V71-FOILPACK-FINAL-SPEC.md
@.planning/SPEC-V62-LOOTBOX-REPACK.md

<scope_guard>
HARD CONSTRAINT — READ-ONLY on `contracts/*.sol`. Do NOT Edit or Write ANY file under `contracts/`.
This plan writes ONLY the SPEC section under `.planning/`. Read contracts to ground line:offsets; the
identifiers and corrected numbers are already in RESEARCH.md — transcribe them, never edit `.sol`.
</scope_guard>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock buyFoilPack + claimFoilMatch signatures and ordered bodies (FOIL-02/03/04, MATCH-03)</name>
  <files>.planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-E-entrypoints.md</files>
  <read_first>
    - .planning/phases/445-spec-design-lock-the-implementation-contract/445-RESEARCH.md §E.1 (buyFoilPack facade stub + ordered module body, lines ~380-414) and §E.2 (claimFoilMatch signature + ordered body, lines ~416-435)
    - .planning/phases/445-spec-design-lock-the-implementation-contract/445-RESEARCH.md "Open decisions" items 2/6 (the 400-scale correction + the foilMatchClaimed name unification)
    - contracts/DegenerusGame.sol:614-629 (buyPresaleBox — the thin facade stub pattern), contracts/modules/DegenerusGameMintModule.sol:236-299 (_processMintPayment shape), :201-217 (_recordMintPayment 75/25 routing branch), :1709 (cachedScore freeze)
    - contracts/storage/DegenerusGameStorage.sol:157/:663 (TICKET_SCALE=100 → 4 tickets = 400), :442 (traitBurnTicket eligibility), :462 (rngWordByDay)
  </read_first>
  <action>
    Write the entrypoint-signatures section. Pin the facade stub `function buyFoilPack() external payable` (no params —
    the pack is `(msg.sender, level, msg.value)`-determined; pattern = `buyPresaleBox` `DegenerusGame.sol:614-629`)
    and the module body `_buyFoilPack(address buyer, uint256 ethSent)` IN ORDER: (1) liveness gate
    `if (_livenessTriggered()) revert E()`; (2) one-per-RAW-level cap `_foilBoughtThisLevel(buyer, level)` revert
    (stamp written AFTER price settles); (3) price = `10 * PriceLookupLib.priceForLevel(lvl)` (FOIL-02) + payment
    classification reusing the `_processMintPayment` accounting shape BUT REJECTING the afking leg
    (`ethUsed = min(ethSent,cost)`, `remaining = cost-ethUsed`, require `remaining ≤ claimable-1`, the residual `revert E()`
    IS the afking-rejection guard; `_debitClaimable`/`claimablePool -=` only, never `_debitAfking`) (FOIL-03);
    (4) pool split forked to 75/25 via the NEW `FOIL_TO_FUTURE_BPS = 2500` constant (NOT the shared
    `PURCHASE_TO_FUTURE_BPS = 1000`), routed through the frozen/unfrozen `_recordMintPayment` branch verbatim (FOIL-04);
    (5) boost freeze `multBps = uint16(ActivityCurveLib.foilBoostBps(score))` from the same `_playerActivityScore` the
    mint freezes as `cachedScore` (`:1709`); (6) roll 4 signatures `packedTraitsFoil(keccak(seed,i), multBps)` with a
    deterministic frozen `seed = keccak(buyer, lvl, FOIL_SEED_TAG)` (no live RNG) and the single SSTORE of `foilRecord`;
    (7) **`_queueTicketsScaled(buyer, _activeTicketLevel(), 400, false)`** — STATE THE CORRECTION explicitly: pass `400`
    (= 4 × `TICKET_SCALE=100`), NOT `4`; (8) no FLIP/WWXRP/whale-pass mint at buy. Pin the claim signature
    `function claimFoilMatch(uint256 day, uint256 ticketIndex, uint8 drawKind) external` and its ordered body: bounds
    (`ticketIndex<4`, `drawKind<2`) + `_foilRecordFor` load + present-require; MATCH-02 window require; `rw =
    rngWordByDay[day]` require non-zero; the double-claim guard set BEFORE payout (CEI) using the unified
    `foilMatchClaimed` name; re-derive (Task 2); positional count (MATCH-03 — full 6-bit `[CCC][SSS]`, color-only does
    NOT count, wrong-quadrant does NOT count); tier select (Task 2); pay (Task 3). Cite FOIL-02/03/04 + MATCH-03
    inline. No fenced Solidity bodies — name the functions, params, ordered steps, and the corrected `400` as directive
    prose.
  </action>
  <verify>
    <automated>grep -q '_queueTicketsScaled(buyer, _activeTicketLevel(), 400, false)' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-E-entrypoints.md && grep -Eq 'FOIL_TO_FUTURE_BPS *= *2500|75/25|75%' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-E-entrypoints.md && grep -Eq 'claimFoilMatch\(uint256 day, uint256 ticketIndex, uint8 drawKind\)' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-E-entrypoints.md && grep -Eq '10 ?\* ?PriceLookupLib.priceForLevel|10 ?. ?priceForLevel' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-E-entrypoints.md && grep -Eiq 'afking.*reject|reject.*afking' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-E-entrypoints.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    445-SPEC-E-entrypoints.md contains: the `buyFoilPack() external payable` facade stub; the ordered `_buyFoilPack`
    body with the afking-rejection guard, the `10 * priceForLevel` price (FOIL-02), and `FOIL_TO_FUTURE_BPS=2500`
    75/25 fork (FOIL-04); the EXACT corrected string `_queueTicketsScaled(buyer, _activeTicketLevel(), 400, false)`;
    and the `claimFoilMatch(uint256 day, uint256 ticketIndex, uint8 drawKind)` signature with its ordered body using
    `foilMatchClaimed` (not `foilClaimed`). Inline REQ tags FOIL-02/FOIL-03/FOIL-04/MATCH-03.
  </acceptance_criteria>
  <done>Both entrypoint signatures and ordered bodies are pinned with the corrected 400-scale queue and 75/25 fork; an IMPL author can write them with no signature decision.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the LIVE-vs-HERO-FREE re-derivation + steer-proof tier gate (MATCH-09, SEC-01 basis)</name>
  <files>.planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-E-entrypoints.md</files>
  <read_first>
    - .planning/phases/445-spec-design-lock-the-implementation-contract/445-RESEARCH.md §B (winning-trait distribution LIVE vs HERO-FREE, lines ~148-200) and §E.3 (the crux re-derivation, lines ~437-462) and §E.2 step 7 (tier resolution)
    - .planning/phases/445-spec-design-lock-the-implementation-contract/445-RESEARCH.md "Open decisions" item 12 (the dailyHeroWagers[day-1] IMPL anchor)
    - contracts/libraries/JackpotBucketLib.sol:281-286 (getRandomTraits — uniform 6-bit slice), contracts/modules/DegenerusGameJackpotModule.sol:1316-1341 (_applyHeroResult single-quadrant override), :1760-1795 (_rollWinningTraits/_rollWinningTraitsPair main+bonus), :1290-1291 (dailyIdx == day-1)
    - .planning/V71-FOILPACK-FINAL-SPEC.md:50-53 (§4 manipulation policy)
  </read_first>
  <action>
    Append the winning-set re-derivation section. Pin the producer substrate: the daily winning set is
    `JackpotBucketLib.getRandomTraits(rw)` (`:281-286`) — a flat uniform 6-bit slice per quadrant `[QQ][CCC][SSS]`,
    BOTH color and symbol uniform 1/8 (NOT `weightedColorBucket`, NOT `_degTrait`), so per-quadrant exact match ≈ 1/64
    and the foil rarity boost CANCELS in the match channel (key MATCH-10/calibration fact). Pin the per-drawKind base
    word: `r = drawKind==1 ? EntropyLib.hash2(rw, BONUS_TRAITS_TAG) : rw`. Pin the two re-derived sets mirroring
    `_rollWinningTraits` (`:1760-1769`): (1) HERO-FREE pure-VRF = `getRandomTraits(r)` + `packWinningTraits` (no
    `_applyHeroResult`); (2) LIVE = HERO-FREE with `_applyHeroResult(liveW, r, hasHero, hQ, hSym)` overriding ONLY
    `liveW[heroQuadrant]` (color from `r`'s low bits, symbol = steered `heroSymbol`); every non-hero quadrant byte-identical.
    State the CRITICAL IMPL anchor: `_rollHeroSymbol` reads `dailyHeroWagers[dailyIdx]` where `dailyIdx == day-1`
    (`:1290-1291`) — so `claimFoilMatch(D)` MUST read `dailyHeroWagers[D-1]`; the pool (`:1841`) is retained → fully
    reconstructible. Pin the tier gate (MATCH-09, steer-proof): 2-of-4 / 3-of-4 off `liveCount` (bounded hero edge
    KEPT); 4-of-4 ONLY if `heroFreeCount==4` (a steered hero shifts at most one quadrant → reaches at most 3-of-4 on
    LIVE, never `heroFreeCount==4`); if `liveCount==4` only via the hero override but `heroFreeCount==3`, it pays the
    3-of-4 tier; `tier==0 ⇒ revert E()`. State the SEC-01 consequence: a steerer controls only `heroSymbol` of
    `liveW[hQ]`; the 4-of-4 whale-pass moonshot is un-steerable and non-stackable. Cite MATCH-09 + SEC-01 (design basis)
    inline. No fenced Solidity — name the producers, the override, the `day-1` anchor, and the tier gate as directive
    prose.
  </action>
  <verify>
    <automated>grep -Eq 'heroFreeCount ?== ?4|heroFreeCount==4' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-E-entrypoints.md && grep -Eq 'dailyHeroWagers\[day-1\]|dailyHeroWagers\[D-1\]|day-1|day − 1' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-E-entrypoints.md && grep -q 'getRandomTraits' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-E-entrypoints.md && grep -Eq '_applyHeroResult' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-E-entrypoints.md && grep -Eiq '1/64|boost.*cancel|cancels' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-E-entrypoints.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    445-SPEC-E-entrypoints.md contains: the `getRandomTraits` uniform substrate with the per-quadrant ≈1/64 + boost-cancels
    statement; the HERO-FREE (no `_applyHeroResult`) vs LIVE (`_applyHeroResult` single-quadrant) split; the
    `dailyHeroWagers[day-1]` IMPL anchor citing `:1290-1291`; the 4-of-4 gate `heroFreeCount==4` with the
    at-most-3-of-4-via-steer bound (SEC-01 basis). Inline REQ tags MATCH-09 + SEC-01.
  </acceptance_criteria>
  <done>The LIVE/HERO-FREE re-derivation is byte-faithful, the day-1 anchor is captured, and the steer-proof 4-of-4 gate is pinned.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the isolated payout lanes + calibration confirm + module placement (MATCH-04/06/07/08/10, SEC-02 basis, SEC-03)</name>
  <files>.planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-E-entrypoints.md</files>
  <read_first>
    - .planning/phases/445-spec-design-lock-the-implementation-contract/445-RESEARCH.md §E.5 (isolated payout schedule, 40/40/20 lane, ETH-cap reuse, lines ~476-506) and "Open decisions" items 3/4 (facade count + ETH-cap clone source :877-915)
    - .planning/phases/445-spec-design-lock-the-implementation-contract/445-RESEARCH.md §G "Calibration" (lines ~575-651) — the closed-form binomials, the 1.9376 faces/pack/30d, the 87.9%/12.1% split, the M≈2.4854 gold crossover, the 4-of-4 ≈1-in-69,906
    - .planning/phases/445-spec-design-lock-the-implementation-contract/445-RESEARCH.md §F "Module placement + EIP-170" (lines ~517-571) — the measured sizes, the new GAME_FOILPACK_MODULE recommendation, the re-measure-at-IMPL caveat
    - contracts/modules/DegenerusGameDegeneretteModule.sol:877-915 (ETH cap+spill clone source; maxEth :889, lootbox :915), :221 (ETH_WIN_CAP_BPS=1000); contracts/ContractAddresses.sol:13-35 (GAME_*_MODULE constants)
    - .planning/phases/445-spec-design-lock-the-implementation-contract/445-CONTEXT.md D-04 (placement = engineering call) + D-05 (calibration policy: CONFIRM and report; FLAG if materially off, never silently retune)
  </read_first>
  <action>
    Append three blocks. (A) Isolated payout schedule (MATCH-04/06/07): the foil claim owns its tier→faces table —
    2-of-4 = 5 faces, 3-of-4 = 65 faces, 4-of-4 = `whalePassClaims[player] += 1` (the EXISTING `:1122` slot) PLUS a
    bonus spin — and MUST NOT route through the EV-flat Degenerette `quickPlay` tables. `1 face = 1,000 FLIP =
    priceForLevel(recLevel) ETH`. Pin the disjoint entropy lanes (MATCH-08): `magnitudeLane = keccak(rw, day, drawKind,
    ticketIndex, FOIL_MAG_TAG)`, `currencyLane = keccak(rw, day, drawKind, ticketIndex, FOIL_CCY_TAG)`, distinct keccak
    domains (`FOIL_MAG_TAG ≠ FOIL_CCY_TAG ≠ BONUS_TRAITS_TAG ≠ FLIP_JACKPOT_TAG`) → provably disjoint; magnitude-first /
    currency-second reveal is UI-only. Pin the 40/40/20 currency split (`c = currencyLane % 100`; <40 FLIP, <80 ETH,
    else WWXRP): FLIP via `coin.mintForGame` (free mint), WWXRP via `wwxrp.mintPrize` (free mint), ETH via the existing
    capped-spin path — clamp to `ETH_WIN_CAP_BPS = 1000` (10% of `futurePrizePool`), over-cap spills to the lootbox,
    credit via `_creditClaimable`, **clone source `DegenerusGameDegeneretteModule.sol:877-915`** (maxEth `:889`, lootbox
    `:915`) — STATE the corrected anchor explicitly. State the SEC-02 basis: `ethShare ≤ 10%·pool`, FLIP/WWXRP mints,
    whale pass pool-neutral. All payout effects AFTER `foilMatchClaimed[mk]=true` (CEI). (B) Calibration confirm
    (MATCH-10, D-05): carry the closed-form binomial results — q=1/64 M-invariant, P(2-of-4)=0.00141943,
    P(3-of-4)=0.00001502, P(4-of-4)=5.960e-08; **E[faces/pack/30d] = 1.9376** over 240 ticket-draws, FLAT across all
    scores; tier split **87.9% / 12.1%** (build-comment cites 87.9%, not the illustrative ~85%); gold-odds crossover
    M≈2.4854 (multBps≈24,854); 4-of-4 ≈1-in-69,906/pack. State the D-05 policy verdict: 1.94 ≈ the ~2-face target
    (3.1% low) → NOT materially off → no recalibration flag; the table stays LOCKED. (Note: the per-quadrant match
    collapses to a constant, so the §G figures are closed-form exact — a Monte-Carlo is optional confirmation, not a
    gating recompute; if 447's empirical run lands materially off ≈2, flag to USER per D-05, never silently retune.)
    (C) Module placement (SEC-03, D-04): pin the recommendation — a NEW `GAME_FOILPACK_MODULE` (body ≈8–11 KB →
    ~13.5–16.5 KB EIP-170 headroom) + two thin facade stubs (`buyFoilPack` + `claimFoilMatch`, facade retains
    ~3.3–3.7 KB of its measured 4,188 B free) + one `GAME_FOILPACK_MODULE` constant in `ContractAddresses.sol`; NOT
    `MintModule` (~1,116 B free, SEC-03-excluded); storage in `DegenerusGameStorage` only. State the
    re-measure-at-IMPL caveat (HARD-REQ §6.7). Cite MATCH-04/06/07/08/10 + SEC-02 (basis) + SEC-03 inline. No fenced
    Solidity — tables + directive prose.
  </action>
  <verify>
    <automated>grep -Eq 'ETH_WIN_CAP_BPS ?= ?1000|ETH_WIN_CAP_BPS' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-E-entrypoints.md && grep -q '877-915' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-E-entrypoints.md && grep -Eq '40.?/.?40.?/.?20|40% FLIP' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-E-entrypoints.md && grep -Eq '1\.9376|1\.94|2-face|faces/pack' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-E-entrypoints.md && grep -q 'GAME_FOILPACK_MODULE' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-E-entrypoints.md && grep -Eq 'FOIL_MAG_TAG' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-E-entrypoints.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    445-SPEC-E-entrypoints.md contains: the isolated tier→faces table (5/65 faces + `whalePassClaims += 1` + bonus
    spin) with the no-Degenerette-routing statement (MATCH-04); the disjoint `FOIL_MAG_TAG`/`FOIL_CCY_TAG` lanes
    (MATCH-08); the 40/40/20 split with `coin.mintForGame` / `wwxrp.mintPrize` / the ETH cap `ETH_WIN_CAP_BPS=1000`
    cloned from `:877-915` (corrected anchor) and the SEC-02 ≤10%-pool basis (MATCH-06/07); the calibration result
    `E[faces/pack/30d]=1.9376` + 87.9%/12.1% + M≈2.4854 crossover + the D-05 "not materially off / no recalibration"
    verdict (MATCH-10); and the new `GAME_FOILPACK_MODULE` placement + ~13.5–16.5 KB headroom + re-measure-at-IMPL
    caveat (SEC-03). Inline REQ tags MATCH-04/06/07/08/10 + SEC-03.
  </acceptance_criteria>
  <done>The payout lanes, the calibration confirm, and the module placement are pinned; an IMPL author can write the spin + placement with no further decision, and the calibration is confirmed ≈2 faces/pack/30d (reported, not retuned).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (design basis for the entrypoint/payout slice)

| Boundary | Description |
|----------|-------------|
| top wagerer → LIVE hero symbol | a steerer controls ONLY `heroSymbol` of one quadrant on LIVE — never the HERO-FREE set |
| claimant → ETH prize pool | the ETH lane crosses into `futurePrizePool`; must be cap-clamped |

## STRIDE Threat Register (entrypoint/payout slice)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-445-E1 | Elevation of Privilege | 4-of-4 whale-pass moonshot via hero steering | mitigate | 4-of-4 gated on `heroFreeCount==4` (pure VRF); a steered hero reaches at most 3-of-4 (SEC-01); proven at 448 |
| T-445-E2 | Denial of Service / solvency | ETH lane drains `futurePrizePool` | mitigate | `ETH_WIN_CAP_BPS=1000` (10%) clamp + lootbox spill, cloned from `DegenerusGameDegeneretteModule.sol:877-915`; FLIP/WWXRP are mints; whale pass pool-neutral (SEC-02); proven at 448 |
| T-445-E3 | Tampering | afking principal spent on a foil buy | mitigate | the foil payment path REJECTS the afking leg (the `remaining > avail` `revert E()`); only fresh-ETH/claimable accepted (FOIL-03) |
| T-445-E4 | Information disclosure / grind | magnitude/currency steered via the match lane | mitigate | disjoint keccak domains (`FOIL_MAG_TAG`/`FOIL_CCY_TAG` ≠ the match lane); derived from retained `rw` at claim, never live-read (MATCH-08) |
</threat_model>

<verification>
- 445-SPEC-E-entrypoints.md exists; the corrected `400` queue, the `heroFreeCount==4` gate, the `dailyHeroWagers[day-1]`
  anchor, the `ETH_WIN_CAP_BPS=1000` clone at `:877-915`, the 1.9376 calibration, and `GAME_FOILPACK_MODULE` are all string-present.
- No file under `contracts/` modified: `git diff --quiet -- contracts/` after the plan.
</verification>

<success_criteria>
An IMPL-446 author can write both entrypoints, the LIVE/HERO-FREE predicate, the 40/40/20 payout, and stand up
`GAME_FOILPACK_MODULE` with ZERO further decision; the calibration is confirmed ≈2 faces/pack/30d and reported.
</success_criteria>

<output>
Create `.planning/phases/445-spec-design-lock-the-implementation-contract/445-03-SUMMARY.md` when done.
</output>
