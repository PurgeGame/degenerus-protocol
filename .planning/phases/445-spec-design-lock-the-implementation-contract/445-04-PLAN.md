---
phase: 445-spec-design-lock-the-implementation-contract
plan: 04
type: execute
wave: 2
depends_on: ["445-01", "445-02", "445-03"]
files_modified:
  - .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC.md
autonomous: false
requirements: [FOIL-01, FOIL-02, FOIL-03, FOIL-04, FOIL-05, RARE-01, RARE-02, RARE-03, RARE-04, MATCH-01, MATCH-02, MATCH-03, MATCH-04, MATCH-05, MATCH-06, MATCH-07, MATCH-08, MATCH-09, MATCH-10, SEC-03]
must_haves:
  truths:
    - "445-SPEC.md is a single house-format design-lock SPEC that an IMPL-446 author can build from mechanically"
    - "Every one of the 20 phase REQ-IDs appears in the SPEC's REQ-coverage map with a section pointer"
    - "The two pinned layout decisions and the single-slot player-loss edge are surfaced in a top-level USER-decisions callout for sign-off before 446"
    - "The §6 hard-floor (steer-proof, ETH ≤10%, isolated table, frozen producers untouched, no slot move, EIP-170) is mapped to SPEC sections + the downstream attest phase"
  artifacts:
    - path: ".planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC.md"
      provides: "The consolidated canonical v71 Foil Pack design-lock SPEC"
      contains: "MILESTONE_V70_AT_HEAD"
  key_links:
    - from: "445-SPEC.md (REQ-coverage map)"
      to: "the three section files A/D/E"
      via: "section pointers for all 20 REQ-IDs"
      pattern: "RARE-0|MATCH-0|FOIL-0|SEC-03"
---

<objective>
Consolidate the three SPEC section files (economics, storage, entrypoints) into the single canonical
house-format `445-SPEC.md` (mirror `SPEC-V61-DESIGN-LOCK.md` / `SPEC-V62-LOOTBOX-REPACK.md`): add the front
matter (baseline/role/scope/inputs), the REQ-coverage map for all 20 phase REQ-IDs, the consolidated
threat-model + §6 hard-floor map, and the top-level USER-decisions callout (the two pins + the player-loss
edge). This is the phase's single build-ready deliverable.

Purpose: produce the one document Phase 446 builds from mechanically, and surface the genuine SPEC decisions
to the USER before the 446 IMPL contract-commit gate.

Output: `445-SPEC.md` (the canonical consolidated SPEC).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-A-economics.md
@.planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-D-storage.md
@.planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-E-entrypoints.md
@.planning/phases/445-spec-design-lock-the-implementation-contract/445-RESEARCH.md
@.planning/phases/445-spec-design-lock-the-implementation-contract/445-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/V71-FOILPACK-FINAL-SPEC.md
@.planning/SPEC-V61-DESIGN-LOCK.md
@.planning/SPEC-V62-LOOTBOX-REPACK.md

<scope_guard>
HARD CONSTRAINT — READ-ONLY on `contracts/*.sol`. Do NOT Edit or Write ANY file under `contracts/`.
This plan writes ONLY `445-SPEC.md` (and may leave the section files in place as appendices or inline them).
</scope_guard>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble the canonical 445-SPEC.md (front matter + 3 sections + REQ-coverage map + §6 hard-floor map)</name>
  <files>.planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC.md</files>
  <read_first>
    - .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-A-economics.md, 445-SPEC-D-storage.md, 445-SPEC-E-entrypoints.md (the three section files to consolidate)
    - .planning/SPEC-V61-DESIGN-LOCK.md (lines 1-75 — the house front-matter + "Locked Knobs" + "Hard Floor" prose structure to mirror)
    - .planning/V71-FOILPACK-FINAL-SPEC.md:59-68 (§6 hard requirements floor — the 8 floor items)
    - .planning/REQUIREMENTS.md:48-58 (the traceability table; the 20 phase-445 REQ-IDs)
    - .planning/ROADMAP.md (the v71 baseline: contracts tree 99f2e53f @ ffbd7796, closure MILESTONE_V70_AT_HEAD_25ff6aae…)
  </read_first>
  <action>
    Write `445-SPEC.md` in the house design-lock format. Front matter: baseline = the v70 closure subject (contracts
    tree `99f2e53f` @ `ffbd7796`, closure `MILESTONE_V70_AT_HEAD_25ff6aaed0e9209e2003f467a3607056bfac9c03`); role = the
    single SPEC deliverable for Phase 445 (translates the locked FINAL-SPEC into the exact build surface so 446 is
    mechanical); scope = PAPER-ONLY, edits ZERO `contracts/*.sol`; inputs = `445-RESEARCH.md` (authoritative —
    adversarially V1/V2/V3-verified), `445-CONTEXT.md` (D-01..D-05), `V71-FOILPACK-FINAL-SPEC.md` ("this file wins"),
    `REQUIREMENTS.md` (the 20 REQ-IDs). Consolidate the three section files into ordered SPEC sections (Economics =
    rarity PMF + foilBoostBps; Storage = foilRecord + cap + claimed marker + the two pins; Entrypoints = buyFoilPack +
    claimFoilMatch + LIVE/HERO-FREE + payout + calibration + placement) — inline them or reference them as numbered
    sections, but the consolidated doc must be self-contained for an IMPL author. Add a "REQ-Coverage Map" table mapping
    EACH of the 20 phase-445 REQ-IDs (FOIL-01..05, RARE-01..04, MATCH-01..10, SEC-03) to its locking section, and note
    that SEC-01/SEC-02/SEC-04 design BASIS is locked here but ATTESTED downstream at 447/448/449. Add a "§6 Hard Floor"
    map: each of the 8 FINAL-SPEC §6 floor items (no exploit/farm beyond the bounded hero edge + steer-proof 4-of-4;
    ETH ≤10%-pool cap; isolated payout table; frozen producers untouched; buy-time score+signature freeze /
    claim-re-derive never-live-read; pull/claim flat advanceGame; EIP-170 fits; forge green + goldens/RNG-freeze
    re-pass) → the SPEC section(s) that satisfy it + the phase that attests it. State the four V3 corrected anchors as
    a "Corrected Anchors (carry verbatim into 446)" callout: `400`-scale queue (not `4`); `whalePassClaims` already at
    `:1122`; ETH-cap clone `:877-915`; unified `foilMatchClaimed`. No fenced Solidity bodies anywhere.
  </action>
  <verify>
    <automated>test -f .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC.md && for id in FOIL-01 FOIL-02 FOIL-03 FOIL-04 FOIL-05 RARE-01 RARE-02 RARE-03 RARE-04 MATCH-01 MATCH-02 MATCH-03 MATCH-04 MATCH-05 MATCH-06 MATCH-07 MATCH-08 MATCH-09 MATCH-10 SEC-03; do grep -q "$id" .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC.md || { echo "MISSING $id"; exit 1; }; done && grep -q 'MILESTONE_V70_AT_HEAD' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC.md && grep -q '400' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC.md && grep -q '877-915' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    445-SPEC.md exists in house design-lock format and contains: the baseline `99f2e53f`/`ffbd7796`/`MILESTONE_V70_AT_HEAD…`
    front matter; the paper-only scope statement; a REQ-Coverage Map naming ALL 20 phase REQ-IDs (FOIL-01..05,
    RARE-01..04, MATCH-01..10, SEC-03); the §6 hard-floor map (8 items → section + attest phase); and the "Corrected
    Anchors" callout (the `400` queue, `:1122`, `:877-915`, `foilMatchClaimed`). It does NOT contain any fenced
    Solidity function body.
  </acceptance_criteria>
  <done>445-SPEC.md is a single self-contained house-format SPEC covering all 20 REQ-IDs with the §6 hard-floor mapped; an IMPL-446 author can build from it mechanically.</done>
</task>

<task type="auto">
  <name>Task 2: Add the consolidated threat model + the top-level USER-decisions callout</name>
  <files>.planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC.md</files>
  <read_first>
    - .planning/phases/445-spec-design-lock-the-implementation-contract/445-02-PLAN.md and 445-03-PLAN.md (the per-plan <threat_model> blocks T-445-D1..D3 and T-445-E1..E4 to consolidate)
    - .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-D-storage.md (PIN 1 / PIN 2 and the USER-sign-off flag)
    - .planning/V71-FOILPACK-FINAL-SPEC.md:50-53 (§4 manipulation policy) and :59-68 (§6 floor — SEC-01/SEC-02 design basis)
  </read_first>
  <action>
    Append two blocks to 445-SPEC.md. (1) A consolidated `<threat_model>` section: a Trust-Boundaries table
    (client→`buyFoilPack`, top-wagerer→LIVE hero symbol, claimant→ETH pool, player→own `foilRecord` slot,
    claimant→`foilMatchClaimed`) + a single STRIDE register merging T-445-D1..D3 (storage) and T-445-E1..E4
    (entrypoint/payout), each with a disposition (mitigate/accept) and a specific mitigation referencing the SPEC
    section. Add the package-supply-chain row `T-445-SC` (Tampering / npm-pip-cargo installs) with disposition `accept`
    and rationale "no new package installs in this paper-only phase; the v71 IMPL adds NO new dependency — all reuse is
    in-repo audited rails." State the two headline floor items as design-locked-here / attested-downstream: SEC-01
    (4-of-4 steer-proof — gated HERO-FREE; a steered hero reaches at most 3-of-4) and SEC-02 (ETH ≤10% of
    `futurePrizePool` via `ETH_WIN_CAP_BPS=1000` + lootbox spill; FLIP/WWXRP mints; whale pass pool-neutral). (2) A
    top-level "USER Decisions to Confirm Before the 446 IMPL Gate" callout pulling forward PIN 1's player-loss edge
    (single-slot `foilRecord` level-keying: an L+1 re-buy before claiming L's matches loses L's unclaimed signatures —
    confirm acceptable OR switch to the `mapping(level => mapping(address => uint256))` variant) and PIN 2 (the locked
    [144-167]-stamp/[0-143]-payload bit-offset, vs the documented bit-224 alternative). Present these as a concise
    decision list the USER can sign off in one read. No fenced Solidity.
  </action>
  <verify>
    <automated>grep -Eiq 'USER Decisions to Confirm|USER decisions|sign off|sign-off' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC.md && grep -Eq 'T-445-SC|supply.?chain' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC.md && grep -Eiq 'SEC-01' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC.md && grep -Eiq 'SEC-02' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC.md && grep -Eq 'level => mapping|mapping\(level|level=>' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC.md && grep -Eiq 'STRIDE|threat' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    445-SPEC.md contains a consolidated `<threat_model>` (Trust Boundaries + STRIDE register merging T-445-D*/E* + the
    `T-445-SC` supply-chain row + the SEC-01/SEC-02 design-basis statement) AND a top-level "USER Decisions to Confirm"
    callout surfacing PIN 1 (single-slot vs `mapping(level => …)` player-loss edge) and PIN 2 (the locked bit-offset).
    Every STRIDE threat has a disposition.
  </acceptance_criteria>
  <done>The SPEC carries a consolidated threat model with all dispositions and a one-read USER-decisions callout surfacing the two pins + the player-loss edge.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: USER sign-off on the two pinned layout decisions before the 446 IMPL gate</name>
  <files>.planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC.md</files>
  <what-built>
    The consolidated `445-SPEC.md` — a build-ready, coefficient/signature/layout-exact design-lock SPEC for the v71.0
    Foil Pack — covering all 20 phase REQ-IDs, with the two genuine SPEC decisions PINNED and a USER-decisions callout.
  </what-built>
  <action>
    PAUSE for USER review. Present the "USER Decisions to Confirm" callout from 445-SPEC.md and collect the two
    sign-offs (PIN 1 keying, PIN 2 bit-offset) before any Phase 446 work begins. Do NOT edit any `contracts/*.sol`.
    If the USER requests a change to either pin, update the corresponding SPEC section (and 445-SPEC-D-storage.md) to
    the chosen variant, then re-present. This task is informational/gating only — it produces no new artifact beyond
    the USER's recorded decision.
  </action>
  <how-to-verify>
    1. Open `.planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC.md` and read the
       "USER Decisions to Confirm Before the 446 IMPL Gate" callout.
    2. PIN 1 — `foilRecord` level-keying. Confirm the LOCKED single-slot `mapping(address => uint256)` is acceptable:
       a player who RE-BUYS a foil pack at level L+1 BEFORE claiming level-L matches LOSES level-L's unclaimed
       signatures (a level advance alone never strands a match — only the player's OWN next buy does). The documented
       alternative is `mapping(level => mapping(address => uint256))` (survives the re-buy at +1 storage slot/level).
       Reply "single-slot OK" to keep the lock, or "switch to level=>player" to change it.
    3. PIN 2 — packed bit-offset. Confirm the LOCKED `[144-167]` stamp / `[0-143]` payload layout (or request the
       documented bit-224 mirror). This is a pure-engineering pin; "bit-offset OK" accepts it.
    4. Spot-check the REQ-Coverage Map lists all 20 REQ-IDs and the calibration reports ≈1.94 faces/pack/30d.
  </how-to-verify>
  <verify>
    <human-check>USER has replied with both sign-offs (PIN 1 keying + PIN 2 bit-offset); any requested change is reflected in 445-SPEC.md.</human-check>
  </verify>
  <resume-signal>Reply "single-slot OK, bit-offset OK" (or specify changes) to approve the SPEC and unblock Phase 446.</resume-signal>
  <done>Both pins are USER-confirmed (or amended), the SPEC reflects the final choice, and Phase 446 is unblocked.</done>
</task>

</tasks>

<verification>
- 445-SPEC.md exists, is self-contained, and all 20 phase REQ-IDs grep-match.
- The threat model has dispositions for every threat; the USER-decisions callout surfaces both pins.
- No file under `contracts/` modified across the whole phase: `git diff --quiet -- contracts/` (the v70 freeze tree `99f2e53f` is intact).
</verification>

<success_criteria>
A single canonical `445-SPEC.md` exists that an IMPL-446 author builds from mechanically; all 20 REQ-IDs are
covered; the §6 hard-floor is mapped; the two pins + the player-loss edge are surfaced and USER-signed-off.
</success_criteria>

<output>
Create `.planning/phases/445-spec-design-lock-the-implementation-contract/445-04-SUMMARY.md` when done.
</output>
