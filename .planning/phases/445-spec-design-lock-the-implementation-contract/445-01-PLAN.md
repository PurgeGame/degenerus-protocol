---
phase: 445-spec-design-lock-the-implementation-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-A-economics.md
autonomous: true
requirements: [FOIL-05, RARE-01, RARE-02, RARE-03, RARE-04]
must_haves:
  truths:
    - "An IMPL author can write traitFromWordFoil/packedTraitsFoil with no further math — the /15360 ladder, the w weights, and the commons-funding rule are all pinned"
    - "An IMPL author can write foilBoostBps with no further math — the 4 anchors, 3 segments, and per-segment closed forms are all pinned"
    - "p_gold = (2/256)·M is stated as the locked invariant, and the PMF is attested valid (Σ=1, all≥0) across multBps ∈ [20000,60000]"
    - "Decisions traced — D-02: ×2→×6 monotone foilBoostBps on the existing 500/30000 knees (~×5 @ 350 illustrative, NOT pinned); D-03: tapered mix-to-rare-tail PMF (gold full ×M, tiers 6→3 taper, commons 0/1/2 funding sink)"
  artifacts:
    - path: ".planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-A-economics.md"
      provides: "The locked rarity-PMF + foilBoostBps coefficient sections of the SPEC"
      contains: "/15360"
  key_links:
    - from: "445-SPEC-A-economics.md (foilBoostBps coefficients)"
      to: "frozen multBps stored in foilRecord"
      via: "RARE-03 freeze-at-buy / apply-at-resolve"
      pattern: "frozen.*multBps|foilBoostBps"
---

<objective>
Author the **economics** half of the 445 design-lock SPEC: the sibling-producer rarity PMF
(`traitFromWordFoil`/`packedTraitsFoil`) and the `foilBoostBps(score)` activity curve — both
coefficient-exact so Phase 446 IMPL is mechanical.

Purpose: lock RARE-01/02/03/04 + the FOIL-05 boosted-gold participation so no IMPL author re-derives
any number. RESEARCH.md §A and §C are already adversarially verified (V1 PASS); this task transcribes
their **reconciled/corrected** numbers verbatim into a build-ready SPEC section.

Output: `445-SPEC-A-economics.md` (a SPEC section file consumed by Plan 04's consolidation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/445-spec-design-lock-the-implementation-contract/445-RESEARCH.md
@.planning/phases/445-spec-design-lock-the-implementation-contract/445-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/V71-FOILPACK-FINAL-SPEC.md
@.planning/SPEC-V61-DESIGN-LOCK.md

<scope_guard>
HARD CONSTRAINT — READ-ONLY on `contracts/*.sol`. Do NOT Edit or Write ANY file under `contracts/`.
The only file this plan writes is the SPEC section under `.planning/`. You MAY read contracts to ground
the prose, but every identifier/coefficient is already in RESEARCH.md — prefer transcribing it.
</scope_guard>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock the sibling-producer rarity PMF (RARE-01/02/04, FOIL-05)</name>
  <files>.planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-A-economics.md</files>
  <read_first>
    - .planning/phases/445-spec-design-lock-the-implementation-contract/445-RESEARCH.md §A "Foil rarity PMF (tapered)" (lines ~51-144) — the taper rule, the per-tier probability table at M=2.0/2.5/5.0/6.0, the non-negativity proof, and the /15360 cutoff ladder
    - .planning/phases/445-spec-design-lock-the-implementation-contract/445-CONTEXT.md D-03 (the tapered mix-to-rare-tail decision + acceptance constraints)
    - contracts/DegenerusTraitUtils.sol:115-126 (weightedColorBucket baseline color widths 64/64/64/32/16/8/6/2; gold=2/256) and :201-223 (packedTraitsDegenerette/_degTrait — the SIBLING template to clone)
    - .planning/SPEC-V61-DESIGN-LOCK.md (house "Locked Knobs" prose style to mirror)
  </read_first>
  <action>
    Write the SPEC's rarity-PMF section. State the sibling producers `traitFromWordFoil(uint64 rnd, uint256 multBps)`
    and `packedTraitsFoil(uint256 rand, uint256 multBps)` as clones of `packedTraitsDegenerette`/`_degTrait`
    (`DegenerusTraitUtils.sol:201-223`), per-quadrant `[QQ][CCC][SSS]`, 4 bytes packed into a `uint32`, symbol
    uniform 1/8 from `rnd>>32 & 7` — ONLY the color stage changes; the v70-frozen `weightedColorBucket`/`traitFromWord`/`packedTraitsFromSeed`
    are NOT edited (RARE-01). Pin the taper rule: `boost(c) = 1 + (M−1)·w_c` with rare-rank weights
    w={7:5/5, 6:4/5, 5:3/5, 4:2/5, 3:1/5}, the three 25% commons (colors 0/1/2, each 64/256 baseline) the sole
    funding sink, gold (color 7) taking the full multiplier so `p_gold = (2/256)·M` exactly (×6 ⇒ 12/256 = 4.6875% ≈
    the spec's 4.7%). Carry the per-tier probability table at M=2.0/2.5/5.0/6.0 verbatim from RESEARCH §A. Pin the
    resolve mechanism as the `/15360 = 256×60` integer cutoff ladder (the ×60 clears the /5 taper denominator and the
    3-way common split): `width15360[c] = base[c]·60·(50000 + (multBps−10000)·w5[c])/50000` with w5={3:1,4:2,5:3,6:4,7:5},
    `rare = Σ width15360[c]`, `rem = 15360 − rare`, commons split `rem/3` with the `rem mod 3` residual to the first
    common, gold = `120·M`. State the attestation: the ladder sums to exactly 15360 with all 8 widths ≥0 for every
    integer `multBps ∈ [20000,60000]` (V1: 40,001 grid points, 0 mismatches/0 negatives) and the PMF stays valid
    (Σ=1, all≥0) to M≈8.8689 — so "worst case within the locked [2,6] range" (NOT "binding case M=6" — carry the V1
    wording correction). Note the gold-odds-vs-10-tickets anchors (≈22.3% @ ×2, ≈tie @ ×2.485, 53.6% @ ×6) follow from
    `p_gold` alone and are unaffected by the taper weights. Cite RARE-01/02/04 and FOIL-05 inline. No fenced Solidity
    bodies — name identifiers, signatures, and the cutoff formula as directive prose/tables.
  </action>
  <verify>
    <automated>grep -q '/15360' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-A-economics.md && grep -Eq '5/5|7:5/5|w_7' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-A-economics.md && grep -Eq 'p_gold|2/256|120' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-A-economics.md && grep -q 'packedTraitsFoil' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-A-economics.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    445-SPEC-A-economics.md contains: the `/15360` ladder formula with `(multBps−10000)`; the rare-rank weights
    w={7:5/5, 6:4/5, 5:3/5, 4:2/5, 3:1/5}; `p_gold = (2/256)·M` stated as the locked invariant with gold = `120·M`
    on the ladder; the sibling signatures `traitFromWordFoil`/`packedTraitsFoil`; an explicit statement that the
    frozen producers (weightedColorBucket/traitFromWord/packedTraitsFromSeed) are NOT edited; the validity
    attestation across `multBps ∈ [20000,60000]`; and inline REQ tags RARE-01/RARE-02/RARE-04/FOIL-05.
  </acceptance_criteria>
  <done>The rarity-PMF SPEC section is coefficient-exact: an IMPL author can write the sibling producer + ladder with zero further math.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the foilBoostBps(score) activity curve (RARE-02/03, FOIL-05)</name>
  <files>.planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-A-economics.md</files>
  <read_first>
    - .planning/phases/445-spec-design-lock-the-implementation-contract/445-RESEARCH.md §C "foilBoostBps(score) curve" (lines ~204-273) — the 5 constants, the segment closed forms, the value table, and the invariant verification
    - .planning/phases/445-spec-design-lock-the-implementation-contract/445-CONTEXT.md D-02 (×2→×6 monotone on the existing knees; "~×5 @ 350" ILLUSTRATIVE, NOT pinned)
    - contracts/libraries/ActivityCurveLib.sol:26 (ACTIVITY_SEG_B_KNEE_POINTS=500), :29 (ACTIVITY_EFFECTIVE_CAP_POINTS=30000), :42-61 (decMultBps idiom), :74-90 (centuryBps idiom)
  </read_first>
  <action>
    Append the `foilBoostBps(score)` section to 445-SPEC-A-economics.md. Pin the 5 constants to add in
    `ActivityCurveLib`: `FOIL_MIN_BPS=20000` (×2.0 @ score 0), `FOIL_K_POINTS=300` (seg-A knee, in the established
    235/305 K band), `FOIL_VA_BPS=50000` (×5.0 @ K), `FOIL_VB_BPS=55000` (×5.5 @ the existing
    ACTIVITY_SEG_B_KNEE_POINTS=500), `FOIL_MAX_BPS=60000` (×6.0 @ ACTIVITY_EFFECTIVE_CAP_POINTS=30000, saturates flat).
    Pin the 4-anchor/3-segment piecewise-linear shape modeled on `decMultBps`/`centuryBps`, reusing the two existing
    shared knees (500, 30000). Carry the per-segment closed forms verbatim: Seg A (0<score≤300) `20000+100·score`;
    Seg B (300<score≤500) `50000+25·(score−300)`; Seg C (500<score<30000) `55000+(score−500)·5000/29500`; saturation
    (score≥30000) `60000`; with the explicit `score==0 ⇒ 20000` and `score≥30000 ⇒ 60000` guard branches (no
    interpolation rounding at the endpoints). Carry the value table (0→20000, 50→25000, 100→30000, 300→50000,
    350→51250, 500→55000, 5000→55762, 30000→60000). State the invariants from V1: monotone non-decreasing over
    [0,30001], saturates flat at ×6, "~×5 @ 350" honored BY SHAPE (boundaries are {0,300,500,30000}; 350 is NOT a
    pinned breakpoint), `foilBoostBps(50)=25000` is the ~×2.5 tie anchor. State RARE-03: the bps is frozen at buy from
    `cachedScore` (the same `_playerActivityScore` source the mint path freezes at `DegenerusGameMintModule.sol:1709`)
    and applied at resolve — NEVER live-read. Cite RARE-02/RARE-03/FOIL-05 inline. No fenced Solidity function body —
    state constants, segment formulas, and the value table as directive prose/tables.
  </action>
  <verify>
    <automated>grep -q 'FOIL_MIN_BPS' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-A-economics.md && grep -q 'FOIL_MAX_BPS' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-A-economics.md && grep -Eq '20000\+100|100.{0,3}score' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-A-economics.md && grep -Eq 'frozen.*buy|frozen-at-buy|cachedScore' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-A-economics.md && grep -Eq '350.*NOT|NOT pinned|not pinned|illustrative' .planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC-A-economics.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    445-SPEC-A-economics.md contains: all 5 constants (FOIL_MIN_BPS=20000, FOIL_K_POINTS=300, FOIL_VA_BPS=50000,
    FOIL_VB_BPS=55000, FOIL_MAX_BPS=60000); the 4 segment closed forms including `20000+100·score` and the saturation
    branch; the value table with the 350→51250 row flagged ILLUSTRATIVE/NOT-pinned; the frozen-at-buy /
    apply-at-resolve / never-live-read statement (RARE-03) citing `cachedScore` / `DegenerusGameMintModule.sol:1709`;
    and inline REQ tags RARE-02/RARE-03/FOIL-05.
  </acceptance_criteria>
  <done>The foilBoostBps SPEC section is coefficient-exact and the RARE-03 freeze semantics are stated; an IMPL author can write the curve with zero further math.</done>
</task>

</tasks>

<verification>
- 445-SPEC-A-economics.md exists and contains BOTH the rarity-PMF and the foilBoostBps sections.
- The /15360 ladder, the w weights, `p_gold=(2/256)·M`, and the 5 curve constants are all string-present.
- No file under `contracts/` was modified (this plan is paper-only): `git diff --quiet -- contracts/` after the plan.
</verification>

<success_criteria>
An IMPL-446 author reading only this section can write `traitFromWordFoil`/`packedTraitsFoil` and `foilBoostBps`
with no further derivation. Every coefficient is the RESEARCH.md reconciled/corrected value.
</success_criteria>

<output>
Create `.planning/phases/445-spec-design-lock-the-implementation-contract/445-01-SUMMARY.md` when done.
</output>
