---
phase: 392-entropy-and-econ
plan: 04
type: execute
wave: 2
depends_on: ["392-02", "392-03"]
files_modified:
  - .planning/phases/392-entropy-and-econ/392-04-CLAUDE-NET.md
  - .planning/phases/392-entropy-and-econ/392-FINDINGS-BURNIE.md
  - .planning/phases/392-entropy-and-econ/392-FINDINGS.md
autonomous: true
requirements: [BURNIE-01, BURNIE-02, BURNIE-03, BURNIE-04, BURNIE-05, BURNIE-06]
user_setup: []

must_haves:
  truths:
    - "NET 2 (the Claude adversarial net) is on record for the full BURNIE/coinflip-rework surface, independent of the council"
    - "Every BURNIE-01..06 requirement has an explicit ADJUDICATED verdict (CONFIRMED / REFUTED / BY-DESIGN / MONITOR) with the conservation-identity / monotone-latch / backing-trace / round-trip-bound that settles it"
    - "Every FC-392-16..20 owned coinflip-burnie lead AND the cross-ref backing-dynamics leads (FC-392-11, FC-392-12, FC-392-13) have an explicit ADJUDICATED verdict folded with the matching council lead"
    - "BURNIE-04 / FC-392-16 (auto-rebuy carry excluded from sDGNRS redemption backing) is settled by a dedicated EXHAUSTIVE backing trace from burnieOwed through previewClaimCoinflips + the redeemBurnieShare waterfall to the autoRebuyCarry — CONFIRMED (under-credit/strand, with the conservative bound) or REFUTED or BY-DESIGN, NOT hand-waved"
    - "BURNIE-05 / FC-392-17 (VAULT seed-stake 30-day window-aging forfeiture) is settled by a dedicated determination — is the VAULT guaranteed to claim within 30 days or be on auto-rebuy at deploy (BY-DESIGN), or is ~half the seeded emission at silent forfeiture risk (CONFIRMED → routed fix, the 'most likely to need a contract change' lead)"
    - "BURNIE-02 emission conservation is re-verified (200k×20×2 seed sums; seed→arm handoff no off-by-one) anchored on the green BurnieEmissionSeeds invariant (5/5); BURNIE-01 survive-before-mint is enumerated across every BURNIE source; BURNIE-03 latch monotonicity proven; BURNIE-06 packed-lane round-trip + off-spine confirmed"
    - "Both nets are on record before the verdict; the skeptic gate is run before any CATASTROPHE/HIGH (a confirmed backing under-credit or lost-emission window is value-bearing — the spine concern of this phase)"
    - "Any CONFIRMED finding is DOCUMENTED and ROUTED to a gated fix (USER hand-review, batched) — NOT fixed in this phase; the subject stays byte-frozen"
    - "The consolidated 392-FINDINGS.md index ties the ECON slice (392-FINDINGS-ECON.md) + the BURNIE slice (392-FINDINGS-BURNIE.md) together — all 12 reqs (ECON-01..06 + BURNIE-01..06) re-attested-or-finding, both nets on record for both slices, the phase-392 verdict rollup"
  artifacts:
    - path: ".planning/phases/392-entropy-and-econ/392-04-CLAUDE-NET.md"
      provides: "NET 2 adversarial analysis over the BURNIE/coinflip-rework surface, independent of the council — the dedicated carry-backing trace, the VAULT-window determination, the emission-conservation re-verification, the survive-before-mint enumeration, the latch-monotonicity proof, the packed-lane round-trip, the loss-sequence backing model"
      min_lines: 90
    - path: ".planning/phases/392-entropy-and-econ/392-FINDINGS-BURNIE.md"
      provides: "The BURNIE per-item adjudication table: every BURNIE req + every coinflip-burnie owned lead + the cross-ref backing-dynamics leads with a CONFIRMED/REFUTED/BY-DESIGN/MONITOR verdict, both-nets-on-record, skeptic gate, routing of any CONFIRMED finding (esp. the two prime backing leads)"
      min_lines: 90
    - path: ".planning/phases/392-entropy-and-econ/392-FINDINGS.md"
      provides: "The consolidated phase-392 index tying both slices: all 12 reqs re-attested, both-nets-on-record for both slices, the phase verdict rollup, the routed-findings consolidation"
      min_lines: 30
  key_links:
    - from: "392-FINDINGS-BURNIE.md"
      to: "392-02 council outputs (council/burnie.*.txt)"
      via: "both-nets-on-record fold"
      pattern: "council"
    - from: "392-FINDINGS-BURNIE.md"
      to: "BURNIE-01..06 + FC-392-16..20 + FC-392-11/-12/-13"
      via: "per-item adjudicated verdict rows"
      pattern: "BURNIE-04|FC-392-16|FC-392-17"
    - from: "392-FINDINGS.md"
      to: "392-FINDINGS-ECON.md + 392-FINDINGS-BURNIE.md"
      via: "consolidated phase index"
      pattern: "FINDINGS-ECON|FINDINGS-BURNIE"
---

<objective>
Run NET 2 (the deep Claude adversarial net) over the SAME BURNIE/coinflip-rework surface the council swept
(392-02), then ADJUDICATE every BURNIE-01..06 requirement, every FC-392-16..20 owned coinflip-burnie lead,
and the cross-ref backing-dynamics leads (FC-392-11, FC-392-12, FC-392-13) against the byte-frozen subject
`a8b702a7`, folding in the NET-1 council leads. Give the two prime backing leads dedicated rigorous
treatment — the auto-rebuy carry excluded from sDGNRS redemption backing (FC-392-16) traced exhaustively
through the backing accounting, and the VAULT seed-stake 30-day window-aging forfeiture (FC-392-17, "most
likely to need a contract change") determined BY-DESIGN-or-CONFIRMED. Re-verify emission conservation,
enumerate survive-before-mint across every BURNIE source, prove the latch monotone, confirm the packed-lane
round-trip + off-spine, model the loss-sequence backing dynamics, run the skeptic gate before presenting
anything as CATASTROPHE/HIGH, and write `392-FINDINGS-BURNIE.md` with a per-item verdict table. Finally
write the consolidated `392-FINDINGS.md` index tying the ECON + BURNIE slices into the phase-392 verdict.

Purpose: a no-finding verdict for any sweep slice requires BOTH nets on record (AUDIT-V63-PLAN §2). This
plan puts NET 2 on record and synthesizes both nets for the BURNIE slice, then consolidates the whole phase.
The two prime backing leads are solvency-ADJACENT (the spine concern per the threat weighting): a confirmed
carry/seed accounting gap that progressively under-credits redeemers or silently forfeits ~half the seeded
initial emission is value-bearing. The BURNIE rework's defining intent (survive-before-mint) and the
stochastic backing variance trade are by-design — VERIFY conservation + completeness + monotonicity +
round-trip-losslessness, do not re-litigate the variance trade. Any CONFIRMED finding (especially either
prime backing lead) is DOCUMENTED + ROUTED to a gated USER-hand-review fix, never fixed here.

Output: 392-04-CLAUDE-NET.md (the independent adversarial analysis), 392-FINDINGS-BURNIE.md (the BURNIE
slice adjudication), and 392-FINDINGS.md (the consolidated phase-392 index). NO contract source is touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/AUDIT-V63-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/PAPER-REWARD-CHANGES-BRIEF.md
@.planning/phases/388-foundation-subject-freeze-green-baseline/388-02-FINDING-CANDIDATES.md
@.planning/v63-surface-map/coinflip-burnie.md
@.planning/phases/392-entropy-and-econ/392-02-COUNCIL-NET.md
@.planning/phases/392-entropy-and-econ/council/burnie.gemini.txt
@.planning/phases/392-entropy-and-econ/council/burnie.codex.txt
@.planning/phases/392-entropy-and-econ/392-FINDINGS-ECON.md
@test/REGRESSION-BASELINE-v63.md

<interfaces>
<!-- The 6 BURNIE reqs to adjudicate (REQUIREMENTS.md BURNIE-01..06 — see 392-02 interfaces for the exact -->
<!-- text + the frozen line-cites). The owned leads adjudicated here: FC-392-16..20 + the cross-ref -->
<!-- backing-dynamics leads FC-392-11 (the RNG-lock half attested at 391; the backing/EV-dynamics half -->
<!-- owned here), FC-392-12 (seed-stake leaderboard/bounty exclusion), FC-392-13 (claimCoinflipCarry -->
<!-- settle-ordering double-count). -->
<!-- Authoritative frozen BURNIE line-cites (verified at a8b702a7 — re-read via git show, do not trust blind): -->
<!--   BurnieCoinflip.sol: constructor seed stakes 200k days 1-20 to VAULT+SDGNRS @140-143/189-197; -->
<!--     SEED_FLIP_DAYS=20; sdgnrsAutoRebuyArmed @174; processCoinflipPayouts @870-893; autoRebuyCarry u128 -->
<!--     @154; claimCoinflipCarry @754-778 (settle first, RNG-lock @759, mint up to amount @770); -->
<!--     COIN_CLAIM_DAYS=365 / FIRST_DAYS=30 / AUTO_REBUY_OFF_MAX=1460 @136-138; claim min-skip @420-457/ -->
<!--     431-440 (uint16/uint32); redeemBurnieShare waterfall @940-975 (held balance + claimableStored, -->
<!--     never reads carry); previewClaimCoinflips @971-975 = _viewClaimableCoin + claimableStored; -->
<!--     coinflipDayResultPacked 8-bit 3-state @1092-1106; coinflipStakePacked 2/slot 128-bit @1072-1085; -->
<!--     setCoinflipAutoRebuy fromGame @662-668. -->
<!--   StakedDegenerusStonk.sol: burnieOwed = (burnieBal + previewClaimCoinflips(sDGNRS))*amount/supply -->
<!--     @1029-1031; no claimCoinflipCarry(sDGNRS,…) caller; auto-rebuy not enabled in constructor. -->
<!--   DegenerusVault.sol: coinClaimCoinflips → claimCoinflips(VAULT,…) @630-647; no VAULT auto-claim/rebuy. -->
<!--   DegenerusGameDegeneretteModule.sol: survival flip @763-780 (acc.burnieMint +=/-=, _distributePayout -->
<!--     @906, flush @447); resolveBurnieSpinsFromBox @1347-1394 (3 spins/1 flip, mint-only). -->
<!--   BurnieCoin.sol: _supply zero-start @174; _transfer inline shortfall top-up. -->
<!--   GameAfkingModule.sol: _settlePendingBurnie @1085-1096 (CEI flip credit). -->
<!--   Green oracle: REGRESSION-BASELINE-v63.md = forge 854/0/110; BurnieEmissionSeeds invariant 5/5 (the -->
<!--     emission-conservation anchor); CoinflipCarryClaim.t.sol (partial/cap/loss-zeroing/compounding). -->
<!--   Frozen-source read convention: git show a8b702a7:contracts/<File>.sol (ignore working tree). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NET 2 adversarial BURNIE analysis — carry-backing trace + VAULT-window determination + emission conservation + survive-before-mint + latch monotonicity + packed-lane round-trip vs frozen source</name>
  <files>.planning/phases/392-entropy-and-econ/392-04-CLAUDE-NET.md</files>
  <read_first>
    - .planning/v63-surface-map/coinflip-burnie.md (FULL §1-§4 — the prime leads FA-1 carry-backing-strand + FA-2 VAULT-window-aging; the §2 correctness review to attack)
    - .planning/phases/388-foundation-subject-freeze-green-baseline/388-02-FINDING-CANDIDATES.md (section F rows FC-392-16..20 verbatim + FC-392-11/-12/-13 — the leads to adjudicate; FC-392-11's RNG-lock half was attested at 391, the backing/EV-dynamics half is owned here)
    - .planning/phases/391-rng-spine/391-FINDINGS.md (the FC-392-11 RNG-lock-half attestation to fold — confirm the cross-ref so the backing half builds on the airtight-lock finding)
    - .planning/PAPER-REWARD-CHANGES-BRIEF.md (§2b/§5 the survival flip; the BURNIE:tickets 0.59:1 peg-vs-illiquid framing — context for the "BURNIE worthless except whale pass" bound on real impact)
    - .planning/AUDIT-V63-PLAN.md §4 (solvency-adjacent backing = the spine concern) + §5 (survive-before-mint defining intent; the stochastic backing trade is intended variance — verify conservation/completeness, not desirability)
    - the frozen source via git show a8b702a7:contracts/BurnieCoinflip.sol (the constructor seeds @140-143/189-197, processCoinflipPayouts @870-893, claimCoinflipCarry @754-778, the claim min-skip @420-457/431-440, redeemBurnieShare waterfall @940-975, previewClaimCoinflips @971-975, the packing lanes @1072-1106, sdgnrsAutoRebuyArmed @174, autoRebuyCarry @154), contracts/StakedDegenerusStonk.sol (burnieOwed @1029-1031), contracts/DegenerusVault.sol (coinClaimCoinflips @630-647), contracts/modules/DegenerusGameDegeneretteModule.sol (survival flip @763-780, resolveBurnieSpinsFromBox @1347-1394), contracts/BurnieCoin.sol (zero-start @174), contracts/modules/GameAfkingModule.sol (_settlePendingBurnie @1085-1096)
    - .planning/phases/392-entropy-and-econ/392-02-COUNCIL-NET.md + council/burnie.*.txt (NET 1 leads to consider — but attack INDEPENDENTLY first, then compare)
  </read_first>
  <action>
    First confirm `git diff a8b702a7 -- contracts/` is empty (read-only over the frozen subject). Read all
    source via `git show a8b702a7:contracts/<File>.sol` — ignore the working tree.

    Run the Claude adversarial net INDEPENDENTLY of the council (do not read the council outputs as the
    starting point — attack the conservation/backing/latch/round-trip claims first, then fold the council
    leads in at the end of this task). For each of the 6 reqs, 5 owned coinflip-burnie leads (FC-392-16..20),
    and 3 cross-ref backing-dynamics leads (FC-392-11/-12/-13), attempt to BREAK the claim with a concrete
    reachable call sequence (the multi-day settle/claim sequence; the loss-sequence backing model; the
    window-aging skip), then record the result.

    BURNIE adjudication (BURNIE-01..06 + the owned + cross-ref leads):

    - BURNIE-04 / FC-392-16 (the TOP carry-backing lead — DEDICATED EXHAUSTIVE backing trace): trace
      burnieOwed = (burnieBal + previewClaimCoinflips(sDGNRS)) * amount / supply (StakedStonk:1029-1031) →
      previewClaimCoinflips (BurnieCoinflip:971-975) = _viewClaimableCoin + claimableStored → the
      redeemBurnieShare consume waterfall (940-975) covers base from sDGNRS's held balance + claimableStored.
      After day 20 sDGNRS is on perpetual auto-rebuy, so its ongoing BURNIE accrual rolls into
      autoRebuyCarry. SETTLE EXHAUSTIVELY: (i) does NEITHER previewClaimCoinflips NOR the consume waterfall
      ever read autoRebuyCarry, and is there NO liquidation path (nothing calls claimCoinflipCarry(sDGNRS,…),
      sDGNRS holds no such code) — so carry-resident BURNIE is invisible to the only consumer of sDGNRS's
      BURNIE backing, progressively under-crediting redeemers? (ii) is this CONSERVATIVE — base <= burnieBal
      + claimableBurnie, the waterfall never reverts → no over-credit, no insolvency, only under-credit/
      strand? (iii) in steady state does _viewClaimableCoin(sDGNRS) return 0 between resolutions
      (cursor==latest after each daily auto-claim) so burnieOwed reflects only the HELD balance? Determine
      whether this matches design intent (an intended loss of the BURNIE-share economics the old 2M reserve
      provided, BURNIE being "worthless except the whale pass") or is an accidental backing gap. Give the
      verdict: CONFIRMED (under-credit/strand, with the conservative bound + the proposed routed fix shape)
      / REFUTED / BY-DESIGN — with the full backing trace, NOT a hand-wave.

    - BURNIE-05 / FC-392-17 (the TOP VAULT-window lead, "most likely to need a contract change" — DEDICATED
      determination): the constructor seeds the VAULT identically (days 1-20) but ONLY sDGNRS is auto-claimed
      by processCoinflipPayouts; the VAULT must claim via DegenerusVault.coinClaimCoinflips →
      claimCoinflips(VAULT,…). For lastClaim==0 the window is COIN_CLAIM_FIRST_DAYS=30 (BurnieCoinflip:423),
      and _claimCoinflipsInternal sets start=minClaimableDay=latest-30 when start<minClaimableDay, SILENTLY
      SKIPPING days below it (431-440). If the VAULT does not claim until flipsClaimableDay>=51, then
      minClaimableDay=21>20 and EVERY VAULT seed day (1-20) is skipped — the VAULT's entire ~half-of-4M
      expected seed emission never mints. DETERMINE: (i) is the VAULT expected/guaranteed to claim within 30
      days, or to be on auto-rebuy at deploy? (trace DegenerusVault for any auto-claim hook, any
      construction-time arming, any operational claim path); (ii) is there ANY auto-claim or auto-rebuy
      safety net for the VAULT seed (there is for sDGNRS, none for the VAULT — confirm); (iii) under the
      realistic deploy/operations timeline, is the VAULT seed at silent forfeiture risk? Give the verdict:
      BY-DESIGN (the VAULT is operationally claimed/armed within the window — cite the path) or CONFIRMED
      (silent forfeiture risk → routed fix, the proposed fix shape). This is the lead most likely to be a
      real contract change — give it the most rigorous treatment.

    - BURNIE-02 (emission conservation): confirm the constructor seeds days 1-20 inclusive to BOTH addresses
      (200k×20×2 = 8M stake), processCoinflipPayouts resolves exactly one epoch per advanceGame call with
      sDGNRS auto-claimed every resolved day (cursor lastClaim tracks flipsClaimableDay 1:1 through the seed
      window), and the seed→arm handoff at epoch>=20 has no off-by-one (arming fires AFTER day-20's win
      mints; autoRebuyStartDay=lastClaim=20; days 21+ roll to carry). Anchor on the green BurnieEmissionSeeds
      invariant (5/5). Confirm the conservation identity (totalSupply + vaultAllowance = supplyIncUncirculated
      holds; both start at 0; ~half of 4M expected to win through flip survival). Find any over/under-emission,
      or settle REFUTED.

    - BURNIE-01 (survive-before-mint completeness): enumerate EVERY BURNIE mint source and confirm each gates
      on a survived coinflip — the seed stakes (mint only if the day's flip survives), the per-bet survival
      double-or-nothing (DegeneretteModule:763-780, acc.burnieMint +=/-= nets to zero per losing bet, flushed
      once @447, no underflow within the bet since the same totalPayout was added immediately before), the box
      BURNIE spins ×3 under one survival flip (resolveBurnieSpinsFromBox:1347-1394, mint-only), the normal
      mint, and the afking BURNIE settlement (_settlePendingBurnie:1085-1096, a flip credit not a direct
      mint). FC-392-19 folds in: the survival flip seed reuses the box seed hash hash2(rngWord,betId) on the
      bet path — for BURNIE bets betLootboxShare==0 so no box opens; confirm no path mixes a BURNIE survival
      outcome with a box draw a player could bias via sequential betIds. Find any BURNIE that mints without a
      survived flip, or settle REFUTED.

    - BURNIE-03 (latch monotonicity): prove sdgnrsAutoRebuyArmed is set exactly once (epoch>=20), never
      cleared, gated behind the else of if(sdgnrsAutoRebuyArmed), no disarm/re-run path; the seed-window
      branch mints via _claimCoinflipsAmount(...,true), the armed branch never mints (no double-mint);
      setCoinflipAutoRebuy NEVER called for sDGNRS post-genesis (the enabled flag can't be toggled off to
      extract carry). FC-392-18 folds in: the setCoinflipAutoRebuy fromGame branch (662-668) skips
      operator-approval for a non-zero player but no in-protocol GAME module calls it (grep clean) →
      presently unreachable + matches baseline; confirm it stays unreachable (latent permissive branch, not a
      regression — note per [[open-e-operator-approval-trust-boundary]] operator-approval IS the trust
      boundary). Find any latch re-entry/double-claim/strand, or settle REFUTED.

    - BURNIE-06 (packed-lane round-trip + off-spine): confirm the stake lane (wei in 128-bit halves, bounded
      by uint128 supply, masked read/write preserves the sibling day) and the 8-bit 3-state day-result lane
      (0=unresolved, 1=loss, 50..156=win; b>=50 separates wins; NO win lands in [2,49] — the only sub-50
      fixed branch is unlucky=50) round-trip losslessly; and BURNIE never credits claimableWinnings or the
      prize pools (mintForGame/burnForCoinflip/burnCoin only; the sDGNRS redemption BURNIE leg burn+consume
      nets new BURNIE = 0; no new BURNIE→ETH path). Find any lossy lane/sibling-corruption/BURNIE→ETH path,
      or settle REFUTED.

    - FC-392-11 (cross-ref backing-dynamics half — the RNG-lock half attested at 391): MODEL whether a
      sequence of losses (or a single large-carry loss) can drop sDGNRS BURNIE backing below outstanding
      redemption obligations, and whether claimCoinflipCarry (player-pull from the carry) interacts with the
      sDGNRS settle to let a redeemer extract during a mid-roll backing window. Build on the 391 finding that
      the RNG-lock window over the carry roll is airtight (cross-ref); here adjudicate the BACKING DYNAMICS
      (the path-dependent backing vs obligations — note this couples to FC-392-16's finding that the carry is
      invisible to backing anyway). FC-392-13 folds in: claimCoinflipCarry settles resolved days (wins→carry,
      pending loss→zero) BEFORE withdrawing — confirm the take-profit chunks the settle banks into
      claimableStored + the carry withdrawal cannot double-count a single win across the two channels (the
      CoinflipCarryClaim.t.sol coverage anchor). Settle the loss-sequence backing + the double-count, or
      REFUTE.

    - FC-392-12 / FC-392-20 (LOW-INFO): confirm the seed stakes (direct _setFlipStake writes) stay off the
      top-bettor leaderboard, the bounty, and biggest-flip records — no path reads the seed stake for days
      1-20 into a leaderboard/bounty/BAF/biggestFlipEver computation that mis-credits the protocol addresses;
      and the claim-window widening to 365 (+1460) raises the per-claim resolution-walk ceiling (deep uint32
      safe, shallow uint16 safe for 365; per-call + caller-paid, NOT in the advanceGame chain) — confirm no
      realistic actor can force a many-hundred-day cold-SLOAD walk into a gas-sensitive caller (cross-ref the
      permissionless-access gas lead FC-393-04 → 393).

    For each item: attempt the break, record PROPERTY · attack/sequence/model tried · STATE VAR + file:line
    at a8b702a7 · the conservation-identity / monotone-latch / backing-trace / round-trip-bound / design-
    intent that settles it · a provisional verdict (CONFIRMED / REFUTED / BY-DESIGN / MONITOR). Note where
    the green baseline already proves a property (the BurnieEmissionSeeds 5/5 invariant for conservation; the
    CoinflipCarryClaim.t.sol coverage for the carry settle). At the END of the task, read the council outputs
    (392-02 council/burnie.*.txt) and note for each item whether the council raised a convergent/divergent
    lead — fold any council-only lead in as an additional row to adjudicate. Write all of this to
    392-04-CLAUDE-NET.md.

    Re-verify `git diff a8b702a7 -- contracts/` empty at the end (read-only). Do NOT run hardhat. Use neutral
    defensive-engineering vocabulary throughout.
  </action>
  <verify>
    <automated>test -s .planning/phases/392-entropy-and-econ/392-04-CLAUDE-NET.md && for id in BURNIE-01 BURNIE-02 BURNIE-03 BURNIE-04 BURNIE-05 BURNIE-06 FC-392-11 FC-392-12 FC-392-13 FC-392-16 FC-392-17 FC-392-18 FC-392-19 FC-392-20; do grep -q "$id" .planning/phases/392-entropy-and-econ/392-04-CLAUDE-NET.md || { echo "MISSING $id"; exit 1; }; done && grep -Eqi "autoRebuyCarry|carry.*backing|under-credit" .planning/phases/392-entropy-and-econ/392-04-CLAUDE-NET.md && grep -Eqi "VAULT.*window|window-aging|aged? out|minClaimableDay" .planning/phases/392-entropy-and-econ/392-04-CLAUDE-NET.md && grep -Eqi "conservation|survive|round-trip|lossless" .planning/phases/392-entropy-and-econ/392-04-CLAUDE-NET.md && [ -z "$(git -C /home/zak/Dev/PurgeGame/degenerus-audit diff a8b702a7 -- contracts/)" ]</automated>
  </verify>
  <acceptance_criteria>
    392-04-CLAUDE-NET.md contains an independent adversarial analysis with an attack attempt + a provisional
    verdict for EACH of BURNIE-01..06, FC-392-16..20, and the cross-ref leads FC-392-11/-12/-13; BURNIE-04/
    FC-392-16 carries a dedicated EXHAUSTIVE backing trace from burnieOwed through previewClaimCoinflips +
    the waterfall to autoRebuyCarry (CONFIRMED/REFUTED/BY-DESIGN, not a hand-wave); BURNIE-05/FC-392-17
    carries a dedicated VAULT-window determination (BY-DESIGN with the claim/arm path cited, or CONFIRMED
    with the forfeiture-risk + routed fix); BURNIE-02 carries the emission-conservation re-verification
    anchored on BurnieEmissionSeeds 5/5; BURNIE-01 enumerates survive-before-mint per source; BURNIE-03
    proves latch monotonicity; BURNIE-06 confirms the packed-lane round-trip + off-spine; the council leads
    are folded in at the end; `git diff a8b702a7 -- contracts/` is empty.
  </acceptance_criteria>
  <done>
    NET 2 (the Claude adversarial net) is on record for the BURNIE slice, independent of the council, with a
    per-item attack + provisional verdict, the two prime backing leads given dedicated rigorous treatment
    (the carry-backing trace + the VAULT-window determination), emission conservation re-verified,
    survive-before-mint enumerated, the latch proven monotone, the packed-lane round-trip confirmed, the
    loss-sequence backing modeled, and the council leads folded in.
  </done>
</task>

<task type="auto">
  <name>Task 2: Synthesize both nets, run the skeptic gate, write 392-FINDINGS-BURNIE.md + the consolidated 392-FINDINGS.md index</name>
  <files>.planning/phases/392-entropy-and-econ/392-FINDINGS-BURNIE.md, .planning/phases/392-entropy-and-econ/392-FINDINGS.md</files>
  <read_first>
    - .planning/phases/392-entropy-and-econ/392-04-CLAUDE-NET.md (Task 1 — NET 2 provisional verdicts)
    - .planning/phases/392-entropy-and-econ/392-02-COUNCIL-NET.md + council/burnie.*.txt (NET 1 leads)
    - .planning/phases/392-entropy-and-econ/392-FINDINGS-ECON.md (the ECON slice verdicts — for the consolidated index + the FC-392-08 cross-ref consistency)
    - .planning/phases/390-solvency-spine/390-FINDINGS.md AND .planning/phases/391-rng-spine/391-FINDINGS.md (the just-approved FINDINGS structure to match — header, both-nets table, per-item verdict table, skeptic gate, routing block; 391-FINDINGS for the FC-392-11 RNG-lock-half cross-ref + the FC-392-08 redemption ETH-spin pool RMW adjudication already done at 390 to confirm consistency)
    - .planning/AUDIT-V63-PLAN.md §2 (both-nets-on-record rule) + §4 (threat weighting: solvency-adjacent backing = the spine concern here) + §5 (design-intent anchor) + §8 (process rules: audit-only, routing)
    - .planning/REQUIREMENTS.md (the exact BURNIE-01..06 + ECON-01..06 text to attest)
    - the skeptic-gate posture: [[feedback_skeptic_pass_before_catastrophe]] (structural-protection check + 3-condition EV lens before any CATASTROPHE/HIGH)
  </read_first>
  <action>
    PART A — Synthesize NET 1 (council) and NET 2 (Claude) into 392-FINDINGS-BURNIE.md (the BURNIE-slice
    adjudication). Structure:

    (1) Header: subject `a8b702a7` byte-frozen (contracts tree 2934d3d8…); baseline =
    REGRESSION-BASELINE-v63 (854/0/110; BurnieEmissionSeeds invariant 5/5 = the emission-conservation
    anchor; CoinflipCarryClaim.t.sol = the carry-settle coverage anchor); method = COUNCIL + CLAUDE both;
    posture = AUDIT-ONLY (a CONFIRMED finding routes to a gated USER-hand-review fix, never fixed here); the
    design-intent anchor (survive-before-mint is the defining intent; the stochastic backing variance trade
    is intended — VERIFY conservation/completeness/monotonicity/round-trip, do not re-litigate the trade;
    BURNIE off the ETH/claimablePool spine; BURNIE "worthless except the whale pass" bounds real impact);
    the threat weighting (solvency-adjacent backing = the spine concern here — a confirmed backing
    under-credit or lost-emission window is value-bearing; an emission over-mint or latch double-claim is
    value-bearing; a survive-before-mint violation breaks the defining invariant).

    (2) Both-nets-on-record attestation: a short table confirming NET 1 (council — available/skipped per
    392-02-COUNCIL-NET.md) and NET 2 (Claude — 392-04-CLAUDE-NET) are both on record for the BURNIE slice. A
    no-finding (REFUTED/BY-DESIGN) verdict for any item MUST cite both nets.

    (3) The per-item adjudication table — ONE row per item, covering ALL 6 reqs (BURNIE-01..06), ALL 5 owned
    coinflip-burnie leads (FC-392-16..20), AND the 3 cross-ref backing-dynamics leads (FC-392-11/-12/-13).
    Columns: ITEM · what it claims · NET-1 result · NET-2 result · ADJUDICATED VERDICT (CONFIRMED / REFUTED /
    BY-DESIGN / MONITOR) · the settling conservation-identity / monotone-latch / backing-trace /
    round-trip-bound / design-intent / source-cite (file:line at a8b702a7 or the green-baseline anchor).
    BURNIE-04/FC-392-16 MUST cite the exhaustive backing trace verdict (the under-credit/strand conservative
    bound, or REFUTED, or BY-DESIGN); BURNIE-05/FC-392-17 MUST cite the VAULT-window determination (BY-DESIGN
    with the claim/arm path, or CONFIRMED with the routed fix shape); BURNIE-02 MUST cite the conservation
    re-verification + BurnieEmissionSeeds 5/5; BURNIE-01 MUST cite the per-source survive-before-mint
    enumeration; BURNIE-03 MUST cite the monotone-latch proof; BURNIE-06 MUST cite the round-trip bound +
    off-spine confirmation. Weight each finding per §4. Apply the §5 design-intent anchor.

    (4) Skeptic gate: before tagging ANY item CATASTROPHE/HIGH, run the dual-gate filter (structural-
    protection check + the 3-condition EV lens) and record the gate result for each elevated item. If
    nothing reaches HIGH, state that explicitly with the gate applied to the two prime backing leads
    (FC-392-16 carry-backing, FC-392-17 VAULT-window) and FC-392-11 (loss-sequence backing). If either prime
    lead CONFIRMS as MED+, run the full skeptic gate and weight it (a backing under-credit / lost-emission is
    value-bearing but bounded by BURNIE's "worthless except whale pass" rating — record the bound).

    (5) Routing: for any CONFIRMED finding (especially either prime backing lead), write a
    CONTRACT-CHANGE-NEEDED / ROUTED block (the finding, its weight, the proposed fix shape — e.g. a carry
    liquidation path for sDGNRS backing, or a VAULT auto-claim/auto-rebuy-at-deploy for the seed window — and
    the note that it goes to a SEPARATE gated USER-hand-review boundary, batched, never pre-approved, never
    auto-committed; the subject re-freezes after). If there are zero CONFIRMED findings, state "0 CONFIRMED —
    document-only; BURNIE-01..06 attested at a8b702a7" and note any INFO/MONITOR items carried forward. Any
    test-only gap is routed, NOT a contract finding.

    (6) Re-attestation line: each of BURNIE-01..06 marked attested-or-finding.

    PART B — Write the consolidated 392-FINDINGS.md index tying both slices into the phase-392 verdict:
    (i) a header naming the two slice deliverables (392-FINDINGS-ECON.md, 392-FINDINGS-BURNIE.md), the
    subject `a8b702a7` byte-frozen, the method (COUNCIL + CLAUDE both, dual-net per slice), the posture
    (audit-only); (ii) a both-nets-on-record rollup table — NET 1 + NET 2 on record for BOTH the ECON slice
    (392-01/392-03) AND the BURNIE slice (392-02/392-04); (iii) a phase-verdict rollup table — all 12 reqs
    (ECON-01..06 + BURNIE-01..06) with their adjudicated verdict + the slice deliverable that carries the
    detail; (iv) a consolidated routed-findings list (any CONFIRMED finding across both slices, with its
    weight + routed-to-gated-fix note) OR "0 CONFIRMED across both slices — document-only; all 12 reqs
    attested at a8b702a7"; (v) the cross-ref consistency notes (FC-392-08 redemption ETH-spin — the ECON
    cap-RMW half here, the solvency-CEI half at 390, the permissionless half at 393, confirm consistent;
    FC-392-11 — the RNG-lock half at 391, the backing-dynamics half in the BURNIE slice, confirm consistent);
    (vi) the phase-392 byte-freeze attestation.

    Re-confirm `git diff a8b702a7 -- contracts/` empty at the end. Neutral vocabulary throughout. No contract
    source is modified — these are documentation deliverables.
  </action>
  <verify>
    <automated>test -s .planning/phases/392-entropy-and-econ/392-FINDINGS-BURNIE.md && test -s .planning/phases/392-entropy-and-econ/392-FINDINGS.md && for id in BURNIE-01 BURNIE-02 BURNIE-03 BURNIE-04 BURNIE-05 BURNIE-06 FC-392-11 FC-392-12 FC-392-13 FC-392-16 FC-392-17 FC-392-18 FC-392-19 FC-392-20; do grep -q "$id" .planning/phases/392-entropy-and-econ/392-FINDINGS-BURNIE.md || { echo "MISSING $id in BURNIE"; exit 1; }; done && grep -Eqi "both.nets|NET 1.*NET 2|both nets on record" .planning/phases/392-entropy-and-econ/392-FINDINGS-BURNIE.md && grep -Eqi "skeptic" .planning/phases/392-entropy-and-econ/392-FINDINGS-BURNIE.md && grep -Eqi "autoRebuyCarry|backing" .planning/phases/392-entropy-and-econ/392-FINDINGS-BURNIE.md && for id in ECON-01 ECON-06 BURNIE-01 BURNIE-06; do grep -q "$id" .planning/phases/392-entropy-and-econ/392-FINDINGS.md || { echo "MISSING $id in index"; exit 1; }; done && grep -q "FINDINGS-ECON" .planning/phases/392-entropy-and-econ/392-FINDINGS.md && grep -q "FINDINGS-BURNIE" .planning/phases/392-entropy-and-econ/392-FINDINGS.md && [ -z "$(git -C /home/zak/Dev/PurgeGame/degenerus-audit diff a8b702a7 -- contracts/)" ]</automated>
  </verify>
  <acceptance_criteria>
    392-FINDINGS-BURNIE.md contains: a both-nets-on-record attestation; a per-item verdict row for EACH of
    BURNIE-01..06, FC-392-16..20, and the 3 cross-ref leads with a CONFIRMED/REFUTED/BY-DESIGN/MONITOR
    verdict + settling cite; BURNIE-04/FC-392-16 cites the exhaustive backing trace; BURNIE-05/FC-392-17
    cites the VAULT-window determination; BURNIE-02 cites the conservation re-verification + BurnieEmission
    Seeds 5/5; the skeptic gate is run and recorded; any CONFIRMED finding is documented + ROUTED (not
    fixed). 392-FINDINGS.md ties both slices: the both-nets rollup for both slices, all 12 reqs in the
    phase-verdict rollup, the consolidated routed-findings list, the FC-392-08/-11 cross-ref consistency
    notes. `git diff a8b702a7 -- contracts/` is empty.
  </acceptance_criteria>
  <done>
    The BURNIE slice is adjudicated with both nets on record, the skeptic gate applied, every BURNIE req +
    owned lead + cross-ref backing-dynamics lead carrying an explicit verdict, the two prime backing leads
    settled (CONFIRM/REFUTE/BY-DESIGN with the backing trace + the VAULT-window determination), and the
    consolidated 392-FINDINGS.md index ties the ECON + BURNIE slices into the phase-392 verdict — all 12 reqs
    re-attested-or-finding, both nets on record for both slices, any CONFIRMED finding routed to a gated fix,
    the subject byte-frozen throughout.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit-process → byte-frozen subject | the risk is a MISSED backing under-credit / lost-emission window / emission over-mint, or a FALSE-GREEN adjudication (a real backing gap waved through as "BURNIE is worthless"), or the audit mutating the frozen subject — not a new feature threat |
| single-net verdict | the risk is issuing a no-finding verdict with only one net on record (the v60/v62 lesson) |
| protocol-address seed/backing | the VAULT/SDGNRS seed/carry is protocol-held but BACKS real redeemers — a silent forfeiture/strand is NOT a genesis self-break non-finding, it is a value-bearing backing concern |

## STRIDE Threat Register (audit-only posture)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-392-11 | Tampering | byte-frozen subject contracts/ | mitigate | git-status-verify `git diff a8b702a7 -- contracts/` empty at the end of each task; all source read via `git show a8b702a7:` |
| T-392-12 | Repudiation / false-green | a REFUTED/BY-DESIGN BURNIE verdict issued without both nets, or the carry-backing lead waved as "BURNIE worthless" without the exhaustive backing trace, or the VAULT-window waved without the claim/arm-path determination | mitigate | the both-nets-on-record table; every no-finding verdict cites NET 1 + NET 2; FC-392-16 requires the exhaustive backing trace; FC-392-17 requires the BY-DESIGN-claim/arm-path or the CONFIRMED-routed-fix determination |
| T-392-13 | Elevation / under-weighting | a CONFIRMED backing under-credit / lost-emission window under-weighted as "doesn't matter, BURNIE worthless", or a HIGH tagged without the skeptic filter | mitigate | the skeptic gate runs before any CATASTROPHE/HIGH; a backing under-credit/lost-emission weighted value-bearing per §4, bounded by the BURNIE-worthless rating (the bound recorded, not used to dismiss) |
| T-392-14 | Tampering | a CONFIRMED finding silently fixed in-phase | mitigate | audit-only posture: CONFIRMED findings (esp. either prime backing lead) are DOCUMENTED + ROUTED to a gated USER-hand-review boundary, never fixed here |
</threat_model>

<verification>
- NET 2 (Claude) on record, independent, per-item attack + verdict over all 6 BURNIE reqs + 5 owned leads +
  3 cross-ref leads.
- Both nets on record before the verdict; the both-nets table present in 392-FINDINGS-BURNIE.md.
- BURNIE-04/FC-392-16 settled by the exhaustive backing trace; BURNIE-05/FC-392-17 settled by the
  VAULT-window determination; BURNIE-02 settled by the conservation re-verification + BurnieEmissionSeeds
  5/5; BURNIE-01/-03/-06 settled.
- Skeptic gate run before any CATASTROPHE/HIGH; result recorded; backing under-credit/lost-emission weighted
  value-bearing.
- The consolidated 392-FINDINGS.md ties both slices: both-nets rollup for both, all 12 reqs, the routed-
  findings list, the FC-392-08/-11 cross-ref consistency notes.
- Any CONFIRMED finding documented + ROUTED (not fixed); subject byte-frozen.
- `git diff a8b702a7 -- contracts/` empty.
</verification>

<success_criteria>
The BURNIE slice (BURNIE-01..06 + the 5 coinflip-burnie leads + the 3 cross-ref backing-dynamics leads) is
adjudicated against the byte-frozen subject `a8b702a7` with BOTH nets on record, the skeptic gate applied,
and every item carrying an explicit CONFIRMED/REFUTED/BY-DESIGN/MONITOR verdict backed by a conservation
identity, monotone-latch proof, backing trace, round-trip bound, design intent, or source-cite. The two
prime backing leads are settled by dedicated rigorous treatment (the carry-backing exhaustive trace + the
VAULT-window determination). The consolidated 392-FINDINGS.md index ties the ECON + BURNIE slices into the
phase-392 verdict — all 12 reqs re-attested-or-finding, both nets on record for both slices, any CONFIRMED
finding routed to a gated fix. The subject stays byte-frozen.
</success_criteria>

<output>
Create `.planning/phases/392-entropy-and-econ/392-04-SUMMARY.md` when done
</output>
</content>
