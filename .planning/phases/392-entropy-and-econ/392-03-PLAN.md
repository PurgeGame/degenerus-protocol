---
phase: 392-entropy-and-econ
plan: 03
type: execute
wave: 2
depends_on: ["392-01"]
files_modified:
  - .planning/phases/392-entropy-and-econ/392-03-CLAUDE-NET.md
  - .planning/phases/392-entropy-and-econ/392-FINDINGS-ECON.md
autonomous: true
requirements: [ECON-01, ECON-02, ECON-03, ECON-04, ECON-05, ECON-06]
user_setup: []

must_haves:
  truths:
    - "NET 2 (the Claude adversarial net) is on record for the full ECON surface, independent of the council"
    - "Every ECON-01..06 requirement has an explicit ADJUDICATED verdict (CONFIRMED / REFUTED / BY-DESIGN / MONITOR) with the binding cap / EV-arithmetic / saturation ceiling / supply-flag that settles it"
    - "Every FC-392-01..10 + FC-392-14/-15 owned reward-economics lead has an explicit ADJUDICATED verdict folded with the matching council lead"
    - "ECON-02 EV-neutrality is re-verified IN CODE against the documented PAPER-brief claims (the split 40/15/15/15/10/5; the ticket budget ×11/9 = 19,678 bps; the far/near 0.2×1.5+0.8×0.875=1.0; the variance ranges centered on the old per-tier value with EV 0.786× preserved) — each redistribution's coded value matched to its claimed EV-neutral target, NOT hand-waved"
    - "ECON-03 the two genuine EV changes are confirmed to match documented intent IN CODE (the EV band IS 9000-14500 bps with score-to-ceiling 40,000; the recycle gate IS ≥3-whole-ticket with drain-detection removed)"
    - "ECON-04 the money-pump search is settled by a dedicated composition analysis across recycle/spin/recirc/carry/affiliate — either a closed positive-EV cycle is CONFIRMED (value-out > value-in repeatable) or REFUTED with the concrete reason each composition's value-out is illiquid/sub-100%/capped vs the won-claimable value-in"
    - "ECON-05 / FC-392-07 (whale-half-pass channel) is settled by a quantified P(S==9) × boxes-per-pass acquisition cost + a per-bracket-supply-cap confirmation (one half-pass per bracket regardless of route, no supply break)"
    - "Both nets are on record before the verdict; the skeptic gate is run before any CATASTROPHE/HIGH (a money pump = HIGH; a scarce-asset supply break = value-bearing)"
    - "Any CONFIRMED finding is DOCUMENTED and ROUTED to a gated fix (USER hand-review, batched) — NOT fixed in this phase; the subject stays byte-frozen"
  artifacts:
    - path: ".planning/phases/392-entropy-and-econ/392-03-CLAUDE-NET.md"
      provides: "NET 2 adversarial analysis over the ECON surface, independent of the council — the EV-neutrality in-code re-verification, the money-pump composition search, the whale-pass acquisition-cost quantification, the bounded-accrual sweep, the streak-gaming + affiliate-composition attacks"
      min_lines: 90
    - path: ".planning/phases/392-entropy-and-econ/392-FINDINGS-ECON.md"
      provides: "The ECON per-item adjudication table: every ECON req + every reward-economics owned lead with a CONFIRMED/REFUTED/BY-DESIGN/MONITOR verdict, both-nets-on-record, skeptic gate, routing of any CONFIRMED finding"
      min_lines: 90
  key_links:
    - from: "392-FINDINGS-ECON.md"
      to: "392-01 council outputs (council/econ.*.txt)"
      via: "both-nets-on-record fold"
      pattern: "council"
    - from: "392-FINDINGS-ECON.md"
      to: "ECON-01..06 + FC-392-01..10 + FC-392-14/-15"
      via: "per-item adjudicated verdict rows"
      pattern: "ECON-04|FC-392-07"
---

<objective>
Run NET 2 (the deep Claude adversarial net) over the SAME ECON reward-economics surface the council swept
(392-01), then ADJUDICATE every ECON-01..06 requirement and every FC-392-01..10 + FC-392-14/-15 owned
reward-economics lead against the byte-frozen subject `a8b702a7`, folding in the NET-1 council leads.
Re-verify the EV-neutrality of each documented redistribution IN CODE against the PAPER brief, confirm the
two genuine EV changes match documented intent in code, run the money-pump composition search across
recycle/spin/recirc/carry/affiliate, quantify the box WWXRP-spin whale-half-pass acquisition cost +
per-bracket supply cap, sweep bounded-accrual (every consumer saturates below its hard ceiling), run the
skeptic gate before presenting anything as CATASTROPHE/HIGH, and write `392-FINDINGS-ECON.md` with a
per-item verdict table (CONFIRMED / REFUTED / BY-DESIGN / MONITOR) each carrying the binding cap /
EV-arithmetic / saturation / supply-flag that settles it.

Purpose: a no-finding verdict for any sweep slice requires BOTH nets on record (AUDIT-V63-PLAN §2). This
plan puts NET 2 on record and synthesizes both nets for the ECON slice. A closed positive-EV money pump
(ECON-04) is HIGH; a scarce-asset supply break (an extra whale half-pass beyond the per-bracket cap) is
value-bearing; an unbounded accrual grind is value-bearing — so the money-pump search, the whale-pass
quantification, and the bounded-accrual sweep get dedicated rigorous treatment. The ECON sweep VERIFIES
the documented EV-neutrality / two-EV-changes claims hold in code (§5 design-intent anchor); it does NOT
re-litigate the documented intent. Any CONFIRMED finding is DOCUMENTED + ROUTED to a gated USER-hand-review
fix, never fixed here.

Output: 392-03-CLAUDE-NET.md (the independent adversarial analysis) and 392-FINDINGS-ECON.md (the
adjudication of the ECON slice). NO contract source is touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/AUDIT-V63-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/PAPER-REWARD-CHANGES-BRIEF.md
@.planning/phases/388-foundation-subject-freeze-green-baseline/388-02-FINDING-CANDIDATES.md
@.planning/v63-surface-map/reward-economics.md
@.planning/phases/392-entropy-and-econ/392-01-COUNCIL-NET.md
@.planning/phases/392-entropy-and-econ/council/econ.gemini.txt
@.planning/phases/392-entropy-and-econ/council/econ.codex.txt
@test/REGRESSION-BASELINE-v63.md

<interfaces>
<!-- The 6 ECON reqs to adjudicate (REQUIREMENTS.md ECON-01..06 — see 392-01 interfaces for the exact -->
<!-- text + the frozen line-cites). The 12 owned reward-economics leads adjudicated here: FC-392-01..10 + -->
<!-- FC-392-14/-15 (FC-392-11/-12/-13 backing-dynamics leads are owned by the BURNIE slice 392-04). -->
<!-- Authoritative frozen ECON line-cites (verified at a8b702a7 — re-read via git show, do not trust blind): -->
<!--   DegenerusGameStorage.sol: EV band 9000-14500 + LOOTBOX_EV_ACTIVITY_MAX_BPS 40_000 @~1537-1548; -->
<!--     ACTIVITY_SCORE_HARD_CAP_BPS 65_534 @~140; ACTIVITY_SCORE_MAX_BPS 30_500; _effectiveQuestStreak @2284. -->
<!--   DegenerusGameMintStreakUtils.sol: questStreak*50 @313-315 (100-cap removed); hard-cap clamp @349-351. -->
<!--   DegenerusGameLootboxModule.sol: _applyEvMultiplierWithCap @474 (10-ether per-(player,level) cap); -->
<!--     _resolveLootboxRoll @~1950 (roll%20 split 40/15/15/15/10/5 + allowEthSpin gate); stale band comment -->
<!--     @472-473 (says 8000-13500, band is 9000-14500). -->
<!--   DegenerusGameDegeneretteModule.sol: resolveWwxrpSpinFromBox @1292 (S==9 whale-half-pass @1323-1330; -->
<!--     per-bracket global flag wwxrpJackpotWhalePassBracketAwarded[bracket]); resolveEthSpinFromBox @~1400 -->
<!--     (3-tier _distributePayout + recirc box allowEthSpin=false → recursion depth 1); _roiBpsFromScore. -->
<!--   DegenerusMintModule.sol: recycle kicker @1740-1744 (only totalClaimableUsed>=priceWei*3); presale -->
<!--     25% box-credit @1726. -->
<!--   DegenerusQuests.sol: _handleLevelQuestProgress @2063-2068 (level-quest +1 not primary-gated off-run); -->
<!--     _questCompleteWithPair @~1700-1760; _secondaryLocked @1529; decay anchor on slot 0 only. -->
<!--   GameAfkingModule.sol: recordAfkingSecondary @1714; _afkingStreak @2257. -->
<!--   DegenerusAffiliate.sol: payAffiliate GAME-only @414-419; affiliateBonusPointsBest 25-ether early-break -->
<!--     @725-726; VAULT-terminating chains + 75/20/5 winner-takes-all via creditFlip. -->
<!--   PAPER-REWARD-CHANGES-BRIEF documented EV-neutral claims to re-verify in code: split 40/15/15/15/10/5; -->
<!--     ticket budget ×11/9 = 16,100→19,678 bps; far/near 0.2×1.5+0.8×0.875=1.0; variance tiers symmetric -->
<!--     ranges centered on old value, EV 0.786× preserved; aggregate ticket value 0.55×16100=8855 == -->
<!--     0.45×19678=8855. The two EV changes: band 9000-14500 / score-to-ceiling 40,000; recycle ≥3-ticket. -->
<!--   Green oracle: test/REGRESSION-BASELINE-v63.md = forge 854/0/110. -->
<!--   Frozen-source read convention: git show a8b702a7:contracts/<File>.sol (ignore working tree). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NET 2 adversarial ECON analysis — EV-neutrality in-code re-verification + money-pump composition search + whale-pass quantification + bounded-accrual sweep vs frozen source</name>
  <files>.planning/phases/392-entropy-and-econ/392-03-CLAUDE-NET.md</files>
  <read_first>
    - .planning/PAPER-REWARD-CHANGES-BRIEF.md (FULL — the documented EV-neutral claims + the two EV changes; the arithmetic to confirm in code)
    - .planning/v63-surface-map/reward-economics.md (FULL §1-§6 + the bounded-accrual table + FA-1..FA-15; the math-verified EV-neutrality figures to re-confirm at source)
    - .planning/phases/388-foundation-subject-freeze-green-baseline/388-02-FINDING-CANDIDATES.md (section E rows FC-392-01..15 verbatim — adjudicate FC-392-01..10 + FC-392-14/-15 here; FC-392-11/-12/-13 belong to the BURNIE slice)
    - .planning/AUDIT-V63-PLAN.md §4 threat weighting (money-pump = HIGH; solvency = SPINE; RNG = DOMINANT) + §5 design-intent anchor (VERIFY the documented claims, do not re-litigate)
    - the frozen source via git show a8b702a7:contracts/modules/DegenerusGameLootboxModule.sol (_applyEvMultiplierWithCap @474, _resolveLootboxRoll @~1950, the EV band + stale comment @472-473), contracts/DegenerusGameStorage.sol (the EV band 9000-14500 + 40000 cap @~1537-1548, hard cap 65534 @~140, _effectiveQuestStreak @2284), contracts/modules/DegenerusGameMintStreakUtils.sol (questStreak*50 @313-315, hard-cap clamp @349-351), contracts/modules/DegenerusGameDegeneretteModule.sol (resolveWwxrpSpinFromBox @1292/1323-1330, resolveEthSpinFromBox @~1400, _roiBpsFromScore), contracts/modules/DegenerusMintModule.sol (recycle kicker @1740-1744, presale box-credit @1726), contracts/DegenerusQuests.sol (_handleLevelQuestProgress @2063-2068, _questCompleteWithPair @~1700-1760), contracts/DegenerusAffiliate.sol (payAffiliate @414-419, affiliateBonusPointsBest @725-726)
    - .planning/phases/392-entropy-and-econ/392-01-COUNCIL-NET.md + council/econ.*.txt (NET 1 leads to consider — but attack INDEPENDENTLY first, then compare)
  </read_first>
  <action>
    First confirm `git diff a8b702a7 -- contracts/` is empty (read-only over the frozen subject). Read all
    source via `git show a8b702a7:contracts/<File>.sol` — ignore the working tree.

    Run the Claude adversarial net INDEPENDENTLY of the council (do not read the council outputs as the
    starting point — attack the EV/accrual/money-pump claims first, then fold the council leads in at the
    end of this task). For each of the 6 reqs and 12 owned reward-economics leads (FC-392-01..10 +
    FC-392-14/-15), attempt to BREAK the bounded-accrual / EV-neutrality / no-money-pump / scarce-supply
    claim with a concrete reachable call sequence (the repeatable cycle for a money pump; the acquisition
    cost for the whale pass; the grind for unbounded accrual), then record the result.

    ECON adjudication (ECON-01..06 + the 12 owned leads):

    - ECON-01 (bounded accrual — DEDICATED sweep over the bounded-accrual table): for EACH reward surface
      (activity score, lootbox EV multiplier, Degenerette/WWXRP ROI, terminal-decimator boost, BURNIE
      survival flip, lootbox spins, recycle bonus, whale half-pass via box spin, BURNIE seed emission)
      confirm the named hard bound BINDS — the consumer saturates BELOW the cap. Specifically attack the
      now-uncapped quest-streak input (uint16, rate-bounded ≤3/day): trace whether it can push ANY
      downstream reward past its prior saturation ceiling. Confirm every consumer saturates below the 65,534
      hard cap (EV multiplier at LOOTBOX_EV_ACTIVITY_MAX_BPS=40,000; ROI/WWXRP at ACTIVITY_SCORE_MAX_BPS=
      30,500; terminal-decimator re-clamps streak to 100), so the uncapped streak widens no reward ceiling.
      Find any unbounded grind, or settle REFUTED with the binding bound per surface.

    - ECON-02 (EV-neutrality re-verified IN CODE — DEDICATED arithmetic check): for EACH documented
      EV-neutral redistribution, match the CODED value to the claimed EV-neutral target at source. Confirm
      (i) the reward split is 40% tickets / 15% DGNRS / 15% WWXRP-spin / 15% BURNIE-flat / 10% BURNIE-spins
      / 5% ETH-spin (the roll%20 buckets in _resolveLootboxRoll); (ii) the ticket-roll budget is ×11/9 =
      19,678 bps so aggregate ticket value is preserved (0.55×16,100 = 8,855 == 0.45×19,678 = 8,855); (iii)
      the far/near budget weighting is 1.5×/0.875× so 0.2×1.5 + 0.8×0.875 = 1.000 exactly EV-neutral; (iv)
      the variance tiers are symmetric BPS ranges centered on the old per-tier value with overall variance
      EV 0.786× preserved (drawn from the SAME varianceRoll, no extra entropy). Find any redistribution
      where the coded value diverges from the claimed EV-neutral target (a fat-finger constant, an
      asymmetric range, a budget that does not net to 1.0), or settle REFUTED with the coded arithmetic
      matched to the claim. This is VERIFICATION of the stated claim, not re-litigation.

    - ECON-03 (the two genuine EV changes match documented intent IN CODE): confirm (i) the EV-multiplier
      band IS 9000-14500 bps (floor 90% / ceiling 145%) with the max-activity threshold 40,000 (not the old
      8000-13500 / 25,500), at DegenerusGameStorage:~1537-1548; (ii) the recycle gate IS only
      totalClaimableUsed >= priceWei*3 with the spentAllClaimable drain-detection block deleted, at
      MintModule:1740-1744. Find any divergence from the documented numbers, or settle REFUTED. Note the
      stale EV-band comment (LootboxModule:472-473 still says 8000-13500) as a comment-only staleness
      (FC-392-04) — a MONITOR/INFO note, not a logic finding.

    - ECON-04 (money-pump search — DEDICATED composition analysis): hunt for ANY closed positive-EV loop
      where realized value-out exceeds value-in across a REPEATABLE cycle, composing the surfaces each map
      rated EV-neutral in isolation: the recycle kicker (10% illiquid BURNIE flip-credit on ≥3-ticket
      recycled spend) STACKED with the presale 25% box-credit on the SAME recycled spend; the spin recirc
      (ETH-spin → 1 recirc box) feeding a fresh box open; the auto-rebuy carry; the affiliate flip-credit
      (75/20/5 winner-takes-all). Either CONSTRUCT a concrete repeatable cycle whose value-out > value-in
      (accounting for BURNIE flip-credit illiquidity + flip-survival, the sub-100% direct lootbox EV, the
      10-ETH benefit cap, and that claimable must first be WON), or settle REFUTED with the concrete reason
      no composition closes a positive loop (each leg's output is illiquid/sub-100%/capped; the cycle's
      value-in is real won claimable that exceeds the illiquid value-out). FC-392-06 / FC-392-09 fold in
      (the recycle-kicker-stacked-with-presale-credit local view; the ETH-spin "EV-equal to the tickets it
      replaces" runs through the >100%-RTP Degenerette payout + recirc — confirm the aggregate box EV uplift
      is bounded by the 10-ETH cap).

    - ECON-05 / FC-392-07 (whale-half-pass channel — DEDICATED quantification): the box WWXRP-spin (15% of
      opens) can hit S==9 and award the bracket's one whale half-pass (resolveWwxrpSpinFromBox:1323-1330).
      Quantify the new acquisition cost: P(S==9) for a 1-WWXRP-staked box spin × the expected
      boxes-per-pass, and confirm (i) the cost still exceeds the by-design "near-unfarmable" bar, AND (ii)
      no path mints a half-pass BEYOND the per-bracket cap — the flag wwxrpJackpotWhalePassBracketAwarded
      [bracket] is GLOBAL/SHARED across the box route and the deliberate-bet route, so attack any race
      (a box route and a bet route writing the flag in one window; a recirc box re-rolling the WWXRP-spin
      within one open) that could mint >1 half-pass per bracket. A real SUPPLY break is value-bearing
      (CONFIRMED → routed); a mere cost-curve change with supply intact is the documented channel change
      (BY-DESIGN). Settle which, with the quantified cost + the supply-cap proof.

    - ECON-06 (quest-streak rate-bound + decay-gate) + FC-392-01 / FC-392-02: confirm the now-uncapped/
      halved streak is rate-bounded (≤3/day: primary slot 0 + secondary slot 1 + level-quest, each once/day)
      and decay-gated (off-run anchor = the daily primary; lastActiveDay/lastCompletedDay updated only on
      slot 0; miss a day beyond shields → _effectiveBaseStreak reads 0). Attack: FC-392-01 — the level-quest
      +1 (_handleLevelQuestProgress:2063-2068) is NOT gated by the daily primary off-run and does NOT update
      the decay anchor; trace whether the next-day decay zeroes it when the primary is skipped, or whether a
      persistent off-day streak bump bypasses the primary gate. FC-392-02 — toggling afking on/off across a
      day boundary: confirm a player cannot harvest BOTH the funded-day streak AND a manual +1 for the same
      primary (the afking branch in _questCompleteWithPair skips the manual +1 for slot 0). Settle whether
      the ceiling is reachable only by intended sustained effort, or surface a finding.

    - FC-392-05 (VERIFY-claim, the EV-cap re-earn): confirm the per-(player,level) 10-ETH usedBenefit cap
      (_applyEvMultiplierWithCap:474) CANNOT be reset within a level to re-earn the uplift across the paths
      that funnel into it (redemption + direct-open + Degenerette-recirc). REFUTE or surface.

    - FC-392-10 (INFO, BoxSpin sentinel): confirm BOX_BETID_SENTINEL=1<<63 can never collide with a real
      bet nonce (nonces increment from 1; bit 63 unreachable over the game lifetime) so the BoxSpin
      event-decode stays correct.

    - FC-392-03 (VERIFY-claim, INFO): the faster decimator-max ramp (20× boost in ~33 active days vs ~100)
      matches the documented intent — confirm the coded ramp matches the PAPER brief, not re-litigate.

    - FC-392-14 / FC-392-15 (LOW-INFO, affiliate composition): confirm a self-referral / circular-code
      attempt cannot route the upline1/upline2 (20/5%) slices back to the sender; the no-referrer 50/50
      VAULT/DGNRS path cannot be steered by a chosen code to capture the affiliate 75% slice for an
      attacker-controlled address; re-examine the carried v62 affiliate-score asymmetry against the
      GAME-only payAffiliate access (@414-419) + the affiliateBonusPointsBest 25-ether early-break
      (@725-726) to confirm the asymmetry did not change. These feed the ECON-04 affiliate-leg composition.

    For each item: attempt the break, record PROPERTY · attack/cycle/cost tried · STATE VAR + file:line at
    a8b702a7 · the binding cap / EV-arithmetic / saturation ceiling / supply-flag / decay-gate that settles
    it · a provisional verdict (CONFIRMED / REFUTED / BY-DESIGN / MONITOR). Note where the green baseline
    already proves a property (the math-verified EV-neutrality figures in the surface map; any ECON oracle
    in REGRESSION-BASELINE-v63). At the END of the task, read the council outputs (392-01 council/econ.*.txt)
    and note for each item whether the council raised a convergent/divergent lead — fold any council-only
    lead in as an additional row to adjudicate. Write all of this to 392-03-CLAUDE-NET.md.

    Re-verify `git diff a8b702a7 -- contracts/` empty at the end (read-only). Do NOT run hardhat. Use
    neutral defensive-engineering vocabulary throughout.
  </action>
  <verify>
    <automated>test -s .planning/phases/392-entropy-and-econ/392-03-CLAUDE-NET.md && for id in ECON-01 ECON-02 ECON-03 ECON-04 ECON-05 ECON-06 FC-392-01 FC-392-02 FC-392-03 FC-392-04 FC-392-05 FC-392-06 FC-392-07 FC-392-08 FC-392-09 FC-392-10 FC-392-14 FC-392-15; do grep -q "$id" .planning/phases/392-entropy-and-econ/392-03-CLAUDE-NET.md || { echo "MISSING $id"; exit 1; }; done && grep -Eqi "money.?pump|positive-EV|value-out" .planning/phases/392-entropy-and-econ/392-03-CLAUDE-NET.md && grep -Eqi "EV-neutral|19,?678|0\\.786|8855" .planning/phases/392-entropy-and-econ/392-03-CLAUDE-NET.md && grep -Eqi "S==9|whale.?half.?pass|whale halfpass|boxes-per-pass" .planning/phases/392-entropy-and-econ/392-03-CLAUDE-NET.md && grep -Eqi "saturat|bounded|hard cap|40,?000" .planning/phases/392-entropy-and-econ/392-03-CLAUDE-NET.md && [ -z "$(git -C /home/zak/Dev/PurgeGame/degenerus-audit diff a8b702a7 -- contracts/)" ]</automated>
  </verify>
  <acceptance_criteria>
    392-03-CLAUDE-NET.md contains an independent adversarial analysis with an attack attempt + a provisional
    verdict for EACH of ECON-01..06 and FC-392-01..10 + FC-392-14/-15; ECON-01 carries the bounded-accrual
    sweep (every consumer saturates below its hard cap; the uncapped streak widens no ceiling); ECON-02
    carries the in-code EV-neutrality arithmetic matched to the documented claims (split, ×11/9, far/near
    1.0, variance 0.786×); ECON-03 confirms the two EV changes in code (band 9000-14500 / 40,000; recycle
    ≥3-ticket); ECON-04 carries the money-pump composition analysis (cycle constructed or refuted with the
    value-in>value-out reason); ECON-05/FC-392-07 carries the quantified P(S==9) × boxes-per-pass + the
    per-bracket supply-cap proof; the council leads are folded in at the end; `git diff a8b702a7 --
    contracts/` is empty.
  </acceptance_criteria>
  <done>
    NET 2 (the Claude adversarial net) is on record for the ECON slice, independent of the council, with a
    per-item attack + provisional verdict, the EV-neutrality re-verified in code, the money-pump composition
    searched, the whale-pass acquisition cost quantified, the bounded-accrual swept, and the council leads
    folded in.
  </done>
</task>

<task type="auto">
  <name>Task 2: Synthesize both nets, run the skeptic gate, write 392-FINDINGS-ECON.md with the per-item verdict table</name>
  <files>.planning/phases/392-entropy-and-econ/392-FINDINGS-ECON.md</files>
  <read_first>
    - .planning/phases/392-entropy-and-econ/392-03-CLAUDE-NET.md (Task 1 — NET 2 provisional verdicts)
    - .planning/phases/392-entropy-and-econ/392-01-COUNCIL-NET.md + council/econ.*.txt (NET 1 leads)
    - .planning/phases/390-solvency-spine/390-FINDINGS.md AND .planning/phases/391-rng-spine/391-FINDINGS.md (the just-approved FINDINGS structure to match — header, both-nets table, per-item verdict table, skeptic gate, routing block)
    - .planning/AUDIT-V63-PLAN.md §2 (both-nets-on-record rule) + §4 (threat weighting: money-pump = HIGH) + §5 (design-intent anchor) + §8 (process rules: audit-only, routing)
    - .planning/REQUIREMENTS.md (the exact ECON-01..06 text to attest)
    - the skeptic-gate posture: [[feedback_skeptic_pass_before_catastrophe]] (structural-protection check + 3-condition EV lens before any CATASTROPHE/HIGH)
  </read_first>
  <action>
    Synthesize NET 1 (council) and NET 2 (Claude) into 392-FINDINGS-ECON.md — the ECON-slice adjudication
    deliverable. Structure:

    (1) Header: subject `a8b702a7` byte-frozen (contracts tree 2934d3d8…); baseline =
    REGRESSION-BASELINE-v63 (854/0/110); method = COUNCIL + CLAUDE both; posture = AUDIT-ONLY (a CONFIRMED
    finding routes to a gated USER-hand-review fix, never fixed here); the design-intent anchor (the reward
    rebalances are documented — VERIFY the EV-neutrality / two-EV-changes claims in code, do not re-litigate
    intent; the by-design RTP/WWXRP-worthless/lootbox-timing rulings apply); the threat weighting (a closed
    positive-EV money pump = HIGH; a scarce-asset supply break = value-bearing; an unbounded accrual grind =
    value-bearing; a documented-change desirability complaint is NOT a finding).

    (2) Both-nets-on-record attestation: a short table confirming NET 1 (council — available/skipped per
    392-01-COUNCIL-NET.md) and NET 2 (Claude — 392-03-CLAUDE-NET) are both on record for the ECON slice. A
    no-finding (REFUTED/BY-DESIGN) verdict for any item MUST cite both nets.

    (3) The per-item adjudication table — ONE row per item, covering ALL 6 reqs (ECON-01..06) AND all 12
    owned reward-economics leads (FC-392-01..10 + FC-392-14/-15). Columns: ITEM · what it claims · NET-1
    result · NET-2 result · ADJUDICATED VERDICT (CONFIRMED / REFUTED / BY-DESIGN / MONITOR) · the settling
    binding-cap / EV-arithmetic / saturation / supply-flag / source-cite (file:line at a8b702a7 or the
    green-baseline anchor). ECON-02 MUST cite the in-code EV-neutrality arithmetic matched to each
    documented claim; ECON-03 MUST cite the two EV-change numbers confirmed in code; ECON-04 MUST cite the
    money-pump composition result (cycle or refutation); ECON-05/FC-392-07 MUST cite the quantified P(S==9)
    × boxes-per-pass + the per-bracket supply-cap proof; ECON-01 MUST cite the bounded-accrual sweep
    (per-surface binding bound). Weight each finding per AUDIT-V63-PLAN §4. Apply the §5 design-intent anchor
    (the documented EV changes are by-design — verify; the RTP/WWXRP-worthless/lootbox-timing rulings apply).

    (4) Skeptic gate: before tagging ANY item CATASTROPHE/HIGH, run the dual-gate filter (structural-
    protection check + the 3-condition EV lens) and record the gate result for each elevated item. If
    nothing reaches HIGH (the surface-maps found 0 HIGH on inspection — no unbounded accrual or obvious
    closed positive-EV loop), state that explicitly with the gate applied to the MED-attention items
    (FC-392-07 whale-pass channel, FC-392-08 redemption ETH-spin — note FC-392-08's solvency-CEI half is
    owned by 390 and its permissionless half by 393, the ECON half is the cap-RMW / EV-uplift). If the
    money-pump search or the whale-pass quantification surfaces a real break (a closed positive-EV cycle or
    a >1-half-pass-per-bracket supply break), run the full skeptic gate and weight it (money pump = HIGH;
    supply break = value-bearing).

    (5) Routing: for any CONFIRMED finding, write a CONTRACT-CHANGE-NEEDED / ROUTED block (the finding, its
    weight, the proposed fix shape, and the note that it goes to a SEPARATE gated USER-hand-review boundary —
    batched, never pre-approved, never auto-committed; the subject re-freezes after). If there are zero
    CONFIRMED findings, state "0 CONFIRMED — document-only; ECON-01..06 attested at a8b702a7" and note any
    INFO/MONITOR items carried forward (e.g. the FC-392-04 stale EV-band comment, the FC-392-03 documented-
    ramp confirmation). Cross-ref FC-392-08's solvency half → 390 and permissionless half → 393. Any
    test-only (oracle-integrity / missing-property) gap is routed, NOT a contract finding.

    (6) Re-attestation line: each of ECON-01..06 marked attested-or-finding.

    Re-confirm `git diff a8b702a7 -- contracts/` empty at the end. Neutral vocabulary throughout. No
    contract source is modified — this is a documentation deliverable.
  </action>
  <verify>
    <automated>test -s .planning/phases/392-entropy-and-econ/392-FINDINGS-ECON.md && for id in ECON-01 ECON-02 ECON-03 ECON-04 ECON-05 ECON-06 FC-392-01 FC-392-02 FC-392-03 FC-392-04 FC-392-05 FC-392-06 FC-392-07 FC-392-08 FC-392-09 FC-392-10 FC-392-14 FC-392-15; do grep -q "$id" .planning/phases/392-entropy-and-econ/392-FINDINGS-ECON.md || { echo "MISSING $id"; exit 1; }; done && grep -Eqi "both.nets|NET 1.*NET 2|both nets on record" .planning/phases/392-entropy-and-econ/392-FINDINGS-ECON.md && grep -Eqi "skeptic" .planning/phases/392-entropy-and-econ/392-FINDINGS-ECON.md && grep -Eqi "money.?pump|positive-EV" .planning/phases/392-entropy-and-econ/392-FINDINGS-ECON.md && grep -Eqi "EV-neutral" .planning/phases/392-entropy-and-econ/392-FINDINGS-ECON.md && [ -z "$(git -C /home/zak/Dev/PurgeGame/degenerus-audit diff a8b702a7 -- contracts/)" ]</automated>
  </verify>
  <acceptance_criteria>
    392-FINDINGS-ECON.md contains: a both-nets-on-record attestation; a per-item verdict row for EACH of
    ECON-01..06 and FC-392-01..10 + FC-392-14/-15 with a CONFIRMED/REFUTED/BY-DESIGN/MONITOR verdict +
    settling cite; ECON-02 cites the in-code EV-neutrality arithmetic; ECON-03 cites the two EV-change
    numbers confirmed in code; ECON-04 cites the money-pump composition result; ECON-05/FC-392-07 cites the
    quantified acquisition cost + supply-cap proof; ECON-01 cites the bounded-accrual sweep; the skeptic gate
    is run and recorded; any CONFIRMED finding is documented + ROUTED (not fixed); FC-392-08's solvency/
    permissionless halves cross-ref'd to 390/393; `git diff a8b702a7 -- contracts/` is empty.
  </acceptance_criteria>
  <done>
    The ECON slice is adjudicated with both nets on record, the skeptic gate applied, every ECON req + owned
    reward-economics lead carrying an explicit verdict, the EV-neutrality re-verified in code, the
    money-pump search settled, the whale-pass channel quantified, the bounded-accrual swept, and any
    CONFIRMED finding documented + routed to a gated fix — the subject byte-frozen throughout.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit-process → byte-frozen subject | the risk is a MISSED money-pump / scarce-supply break / unbounded grind, an EV-claim-vs-code mismatch waved through, or a FALSE-GREEN adjudication; or the audit mutating the frozen subject — not a new feature threat |
| single-net verdict | the risk is issuing a no-finding verdict with only one net on record (the v60/v62 lesson: Claude-only missed council findings) |

## STRIDE Threat Register (audit-only posture)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-392-07 | Tampering | byte-frozen subject contracts/ | mitigate | git-status-verify `git diff a8b702a7 -- contracts/` empty at the end of each task; all source read via `git show a8b702a7:` |
| T-392-08 | Repudiation / false-green | a REFUTED/BY-DESIGN ECON verdict issued without both nets, or an EV-neutrality claim attested without the in-code arithmetic, or the money-pump search waved without a composition analysis | mitigate | the both-nets-on-record table; every no-finding verdict cites NET 1 + NET 2; ECON-02 requires the coded arithmetic matched to the claim; ECON-04 requires the composition cycle-or-refutation |
| T-392-09 | Elevation / under-weighting | a CONFIRMED money pump or scarce-supply break under-weighted, or a HIGH tagged without the skeptic filter | mitigate | the skeptic gate runs before any CATASTROPHE/HIGH; a money pump weighted HIGH, a supply break value-bearing per §4 |
| T-392-10 | Tampering | a CONFIRMED finding silently fixed in-phase | mitigate | audit-only posture: CONFIRMED findings are DOCUMENTED + ROUTED to a gated USER-hand-review boundary, never fixed here |
</threat_model>

<verification>
- NET 2 (Claude) on record, independent, per-item attack + verdict over all 6 ECON reqs + 12 owned leads.
- Both nets on record before the verdict; the both-nets table present.
- ECON-02 cites the in-code EV-neutrality arithmetic; ECON-03 cites the two EV-change numbers in code;
  ECON-04 cites the money-pump composition result; ECON-05/FC-392-07 cites the quantified cost + supply-cap
  proof; ECON-01 cites the bounded-accrual sweep.
- Skeptic gate run before any CATASTROPHE/HIGH; result recorded; money-pump weighted HIGH.
- Any CONFIRMED finding documented + ROUTED (not fixed); subject byte-frozen.
- `git diff a8b702a7 -- contracts/` empty.
</verification>

<success_criteria>
The ECON slice (ECON-01..06 + the 12 owned reward-economics leads) is adjudicated against the byte-frozen
subject `a8b702a7` with BOTH nets on record, the skeptic gate applied, and every item carrying an explicit
CONFIRMED/REFUTED/BY-DESIGN/MONITOR verdict backed by a binding cap, EV-arithmetic, saturation ceiling,
supply-flag, or source-cite. The EV-neutrality is re-verified in code against the documented claims; the
two genuine EV changes match documented intent in code; the money-pump search is settled; the whale-pass
channel is quantified; the bounded-accrual is swept. Any CONFIRMED finding is documented + routed to a
gated fix; the subject stays byte-frozen.
</success_criteria>

<output>
Create `.planning/phases/392-entropy-and-econ/392-03-SUMMARY.md` when done
</output>
</content>
