---
phase: 348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p
plan: 04
type: execute
wave: 2
depends_on: ["348-01"]
files_modified:
  - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-PLACEMENT-DECISION.md
autonomous: true
requirements: [PLACE-01]

must_haves:
  truths:
    - "The §4 placement is DECIDED = REQUIRED-PATH (D-348-01, the HEADLINE) and recorded as a DELIBERATE USER OVERRIDE of the doc's separate-legs recommendation — not a contradiction to silently reconcile"
    - "PLAN-V55 §4 + §9 are explicitly marked SUPERSEDED on the placement point (the doc recommends separate-legs; the USER chose required-path on guaranteed-every-day grounds)"
    - "The decision basis is recorded: chosen on guaranteed-every-day grounds, NOT forced by revert-safety (the REVERT-FREE-CHAIN proof made required-path VIABLE — a funded well-formed sub can't revert → can't freeze the day)"
    - "The mechanism is specified: a new chunked STAGE inserted in advanceGame immediately before rngGate (new-day path only), guarded by a subsFullyProcessed flag + a _subCursor draining a per-call gas budget — cited at the re-pinned STAGE insertion point (~:273, before rngGate :1152) and the index advance"
    - "The two proof obligations the override creates are CARRIED: D-348-02 (the uniform-index-epoch no-interleave guard — cross-referenced to 348-FREEZE-PROOF.md) and D-348-04 (obligation-1 as the SOLE no-brick guarantor — cross-referenced to 348-INVARIANT-CARRY.md)"
    - "The mint-gate standing dependency (D-348-03) is recorded as ACCEPTED: the STAGE inherits advanceGame's existing _enforceDailyMintGate (:973, called :191) — mint standing OR the 15-30min time-laddered bypass — with ZERO new gate code; decoupling was rejected"
    - "The leg chunking is specified: process-leg pre-RNG cursor-chunked (BUY_BATCH-style); the open-leg stays a normal post-RNG cursor-chunked leg (OPEN_BATCH-style) — the open is NOT folded into advance"
    - "The bounty fold is NOTED (not decided — PLACE-02 is 349-owned): the buy/process bounty folds into the advance bounty (2×·mult) rather than a flat-per-tx leg; farm-by-splitting watched via OPEN_KNEE pro-rate"
  artifacts:
    - path: ".planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-PLACEMENT-DECISION.md"
      provides: "PLACE-01 — the §4 placement DECISION (required-path, USER override) + the superseded-recommendation record + the two carried proof obligations + the mint-gate-standing acceptance"
      contains: "required-path"
  key_links:
    - from: "348-PLACEMENT-DECISION.md"
      to: "PLAN-V55-AFKING-IN-GAME-REDESIGN.md §4 + §9"
      via: "explicit SUPERSEDED marking on the placement point (separate-legs rec → required-path)"
      pattern: "supersede|SUPERSEDED"
    - from: "348-PLACEMENT-DECISION.md"
      to: "AdvanceModule the STAGE insertion point (~:273, before rngGate :1152) + _enforceDailyMintGate :973"
      via: "the chunked-STAGE mechanism spec + the inherited mint-gate standing dependency"
      pattern: "subsFullyProcessed|_enforceDailyMintGate"
---

<objective>
Author `348-PLACEMENT-DECISION.md` (PLACE-01) — the §4 placement DECISION. This is the HEADLINE divergence of the milestone: PLAN-V55 §4/§9 RECOMMEND separate permissionless legs; the USER chose REQUIRED-PATH (D-348-01) on guaranteed-every-day grounds. The SPEC must record this as a DELIBERATE override (not a contradiction to silently reconcile), document the superseded recommendation, specify the chunked-STAGE mechanism, and carry the two proof obligations the override creates (D-348-02 the uniform-index-epoch no-interleave guard; D-348-04 obligation-1-as-sole-no-brick-guarantor).

This is the analog of the placement-decision content that 343 folded into its edit-order map, here promoted to its own doc because it is the headline USER override. It consumes the re-pinned anchors from 348-01 (the STAGE insertion point, the mint gate, the index advance) and cross-references the proof obligations to 348-03 (FREEZE-PROOF + INVARIANT-CARRY).

Purpose: 349 IMPL builds the process-pass as a required-path `advanceGame` STAGE (not separate legs), with the mint-gate standing dependency accepted and the two proof obligations bound to their proofs — no re-litigation of the placement.
Output: One markdown artifact in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-CONTEXT.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-GREP-ATTESTATION.md

<interfaces>
<!-- CONSUMES the re-pinned anchors from 348-GREP-ATTESTATION.md (348-01). Cross-references the proofs in 348-FREEZE-PROOF.md + 348-INVARIANT-CARRY.md (348-03). -->
<!-- D-348-01 (HEADLINE override): REQUIRED-PATH — a chunked STAGE in advanceGame immediately before rngGate (new-day path only), guarded by subsFullyProcessed + a _subCursor gas-budget drain. OVERRIDES PLAN-V55 §4/§9 (separate-legs rec). Chosen on guaranteed-every-day grounds, NOT revert-safety. -->
<!-- D-348-02: the uniform-index-epoch no-interleave guard (FREEZE-02 proof obligation) — cross-ref 348-FREEZE-PROOF.md. -->
<!-- D-348-03: INHERIT the mint gate — the STAGE rides advanceGame's existing _enforceDailyMintGate (:973, called :191): mint standing OR the 15-30min time-laddered bypass; ZERO new gate code; decoupling rejected. -->
<!-- D-348-04: obligation-1 as the SOLE no-brick guarantor (no try/catch) — cross-ref 348-INVARIANT-CARRY.md. -->
<!-- STAGE insertion point: AdvanceModule ~:273, immediately before the rngGate(...) call (rngGate def :1152). The open stays a normal post-RNG leg (OPEN_BATCH-style). -->
<!-- PLACE-02 (bounty, 349-owned) — the SPEC only NOTES the fold (advance bounty 2×·mult; OPEN_KNEE pro-rate for farm-by-splitting); it does not decide the mechanics. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 348-PLACEMENT-DECISION.md — required-path DECIDED (USER override) + superseded record + the two carried proof obligations + mint-gate acceptance</name>
  <files>.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-PLACEMENT-DECISION.md</files>
  <read_first>
    - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-CONTEXT.md (D-348-01 the required-path HEADLINE override + "note PLAN-V55 §4/§9 superseded" + the two proof obligations; D-348-02 uniform-index-epoch; D-348-03 inherit-the-mint-gate; D-348-04 obligation-1-sole-guarantor; <specifics> "The required-path divergence is the headline")
    - .planning/PLAN-V55-AFKING-IN-GAME-REDESIGN.md (§4 "Process/open placement — RECOMMENDED vs ALTERNATIVE" [the separate-legs recommendation being overridden] + §9 "Next step" [the §4-resolved-by-proof note] + §5 "Bounty reconciliation" [the advance-bounty fold + OPEN_KNEE pro-rate])
    - .planning/PLAN-V55-REVERT-FREE-CHAIN-PROOF.md (§8 "§4 placement decision (resolved by this proof)" — required-path is VIABLE/clean because the funded buy is revert-free by construction; the choice rests on guaranteed-every-day vs minimal-surface, the mint-gate standing, bounty-farm-by-splitting)
    - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-GREP-ATTESTATION.md (the re-pinned STAGE insertion point ~:273, rngGate :1152, _enforceDailyMintGate :973 called :191, the index advance :1089/:1629)
    - contracts/modules/DegenerusGameAdvanceModule.sol (re-read the advanceGame flow around the STAGE insertion point ~:273 + the _enforceDailyMintGate call :191 + the body :973 — to specify the STAGE placement + the inherited mint-gate standing accurately)
  </read_first>
  <action>
    Author `348-PLACEMENT-DECISION.md` recording the §4 placement as DECIDED = REQUIRED-PATH. Lead with the DECISION and frame it explicitly as a DELIBERATE USER OVERRIDE of the doc's recommendation (D-348-01): state that PLAN-V55 §4 recommends separate permissionless legs and §9 leans the same way, but the USER chose required-path on GUARANTEED-EVERY-DAY grounds — and mark PLAN-V55 §4 + §9 as SUPERSEDED on the placement point (a labeled "Superseded recommendation" note, so a future reader does not treat the doc's separate-legs rec as live). Record the decision basis precisely: required-path was chosen NOT because revert-safety forced it but because the REVERT-FREE-CHAIN proof made it VIABLE/clean (a funded well-formed sub can't revert in a healthy game → can't freeze the day → the §0-Correction-2 liveness fear is discharged for the healthy path). Specify the mechanism: a new chunked STAGE inserted in `advanceGame` immediately before `rngGate` (new-day path only), guarded by a `subsFullyProcessed` flag + a `_subCursor` draining a per-call gas budget — cite the re-pinned STAGE insertion point (~:273) + `rngGate` (:1152). Carry the TWO proof obligations the override creates, each cross-referenced to its proof: (1) D-348-02 — the uniform-index-epoch no-interleave guard (every sub in a day's STAGE stamped to the SAME current `LR_INDEX`; `requestLootboxRng` cannot interleave while `subsFullyProcessed == false`) → cross-reference `348-FREEZE-PROOF.md` (FREEZE-02) as the doc that PROVES it; (2) D-348-04 — obligation-1 (slice-builder fidelity) is the SOLE no-brick guarantor under no-try/catch → cross-reference `348-INVARIANT-CARRY.md`. Record the mint-gate standing dependency (D-348-03) as ACCEPTED: the required-path STAGE rides `advanceGame`'s existing `_enforceDailyMintGate` (:973, called :191) — mint standing OR the 15-30min time-laddered bypass (which opens to anyone daily) — so afking processing is guaranteed-every-day in practice with ZERO new gate code; note decoupling was REJECTED (it would re-introduce a separate-leg surface inside advance). Specify the leg chunking: the process-leg runs pre-RNG cursor-chunked (BUY_BATCH-style) inside the STAGE; the OPEN-leg stays a NORMAL post-RNG cursor-chunked leg (OPEN_BATCH-style) — the open is NOT folded into advance (this also resolves the PLACE-02 "protocol-early-sequenced" drift; the early-slot defense is dropped). NOTE (do not decide — PLACE-02 is 349-owned) the buy/process bounty folds into the advance bounty (2×·mult) rather than a flat-per-tx leg, with farm-by-splitting watched via the OPEN_KNEE pro-rate. Do NOT paste contract bodies — cite by re-pinned `file:line`.
  </action>
  <acceptance_criteria>
    - 348-PLACEMENT-DECISION.md exists and states the placement is DECIDED = REQUIRED-PATH (the string "required-path" appears) and frames it as a deliberate USER override of the doc's separate-legs recommendation
    - PLAN-V55 §4 + §9 are explicitly marked SUPERSEDED on the placement point (the string "supersede"/"SUPERSEDED" appears with a §4/§9 reference)
    - The decision basis is recorded as guaranteed-every-day (NOT revert-safety), with the proof-made-it-viable rationale
    - The chunked-STAGE mechanism is specified (subsFullyProcessed + _subCursor, inserted before rngGate) citing the re-pinned STAGE insertion point + rngGate :1152
    - Both proof obligations are carried and cross-referenced: D-348-02 → 348-FREEZE-PROOF.md (FREEZE-02), D-348-04 → 348-INVARIANT-CARRY.md (the strings "D-348-02" and "D-348-04" both appear)
    - The mint-gate standing dependency is recorded as accepted with ZERO new gate code, citing _enforceDailyMintGate :973 (the string "_enforceDailyMintGate" appears)
    - No contracts/*.sol modified: `git diff --name-only -- contracts/` is empty
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-PLACEMENT-DECISION.md; test -f "$F" && grep -qi "required-path" "$F" && grep -qiE "supersede" "$F" && grep -qi "guaranteed-every-day\|guaranteed every day" "$F" && grep -q "subsFullyProcessed" "$F" && grep -q "_enforceDailyMintGate" "$F" && grep -q "D-348-02" "$F" && grep -q "D-348-04" "$F" && [ -z "$(git diff --name-only -- contracts/)" ]</automated>
  </verify>
  <done>348-PLACEMENT-DECISION.md records the §4 placement DECISION = required-path (USER override of the doc's separate-legs rec, marked superseded), specifies the chunked-STAGE mechanism, carries the two proof obligations cross-referenced to 348-03, and accepts the inherited mint-gate standing with zero new gate code. Zero contracts/ edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Paper-only SPEC phase; no code ships. The threat surface is that the headline placement override is recorded faithfully — a future reader (or the 349 author) must not treat PLAN-V55 §4/§9's separate-legs recommendation as live, and the two proof obligations the override creates must not be lost. The documentation-integrity threat: an un-recorded override silently reconciles to the doc's (now wrong) recommendation, OR a proof obligation (the no-interleave guard / obligation-1-sole-guarantor) survives un-bound to its proof into IMPL.

| Boundary | Description |
|----------|-------------|
| doc recommendation (separate-legs) → USER decision (required-path) | PLAN-V55 §4/§9 recommend separate-legs; the USER overrode to required-path. If the override is not marked SUPERSEDED, a future reader follows the stale recommendation. |
| placement override → its two proof obligations | Required-path creates the no-interleave guard obligation (D-348-02) + the obligation-1-sole-guarantor obligation (D-348-04); both must be bound to their proofs (348-03), not asserted loose. |
| required-path STAGE → game liveness (mint-gate standing) | The STAGE rides `advanceGame`; the inherited `_enforceDailyMintGate` standing dependency must be recorded as accepted, not silently assumed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-348-08 | Repudiation | the headline placement override | mitigate | The decision is recorded as a DELIBERATE USER override with the guaranteed-every-day rationale, and PLAN-V55 §4/§9 are explicitly marked SUPERSEDED on the placement point — no silent reconciliation; the audit trail is unambiguous. |
| T-348-09 | Denial of service | a sub bricking the day via the required-path STAGE | mitigate | The placement carries D-348-02 (the no-interleave guard, proven in 348-FREEZE-PROOF.md) + D-348-04 (obligation-1 the sole no-brick guarantor, in 348-INVARIANT-CARRY.md) cross-referenced; the §0-Correction-2 liveness fear is discharged for the healthy path by the revert-free-by-construction proof. |
| T-348-SC | Tampering | npm/pip/cargo installs | accept | No package installs — paper-only Markdown authoring; the only CLI is `git diff` + `grep`/read (read-only). No Package Legitimacy Audit needed; zero installs. |
</threat_model>

<verification>
- 348-PLACEMENT-DECISION.md exists in the phase directory.
- The placement is DECIDED = required-path, framed as a USER override, with PLAN-V55 §4/§9 marked superseded.
- The chunked-STAGE mechanism is specified; the two proof obligations are carried + cross-referenced; the mint-gate standing is accepted with zero new gate code.
- No contracts/*.sol modified: `git diff --name-only -- contracts/` is empty.
</verification>

<success_criteria>
- 348-PLACEMENT-DECISION.md satisfies all Task 1 acceptance criteria (ROADMAP Phase 348 Success Criterion 3).
- The §4 placement is decided on non-revert grounds (guaranteed-every-day), recorded as a deliberate override with the rationale and the two proof obligations.
- Zero contracts/*.sol edits.
</success_criteria>

<output>
Create `.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-04-SUMMARY.md` when done
</output>
