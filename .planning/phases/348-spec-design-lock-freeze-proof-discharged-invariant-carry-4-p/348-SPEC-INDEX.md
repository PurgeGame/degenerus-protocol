# Phase 348 — SPEC Index (the D-08 multi-doc set) + Traceability + Verdict + 349 Hand-Off

**Authored:** 2026-05-30
**Plan:** 348-06 (SPEC — Wave-4 integration; lands LAST)
**Milestone:** v55.0 — AfKing-in-Game Redesign
**Requirements (this phase):** FREEZE-01, FREEZE-02, FREEZE-03, PLACE-01, ARCH-04
**Audit baseline (`contracts/`):** byte-identical to the v54 de-custody HEAD **`20ca1f79`** (`git diff --numstat 20ca1f79 HEAD -- contracts/` → EMPTY; the working tree's `contracts/` IS the v54 baseline, so every grep in the set IS a valid attestation against `20ca1f79`).
**SPEC verdict:** **PASS — the FREEZE spine PROVEN (FREEZE-01 now PROVEN per D-348-07, not split); the §4 placement DECIDED [required-path, USER override]; the discharged REVERT-FREE-CHAIN + EV-cap invariants CARRIED with the D-348-04 try/catch DROP; the code-size reclaim MEASURED + sequenced < 24,576; the GAS inventory produced [advisory, 350-gated]; the OPEN-E/AFSUB/set-mutation CONSENT carry-over CONFIRMED against source; every cited anchor grep-attested vs `20ca1f79`; ZERO `contracts/*.sol` mutation.**

> This is the navigation + closure index for the Phase-348 SPEC. **This index IS the realization of the D-08 multi-doc SPEC pattern** — seven discrete, hand-off-able docs + this index, NOT one monolithic `348-SPEC.md`, per the v50 / Phase-334 / Phase-343 precedent. It maps every Phase-348 requirement and every ROADMAP Phase-348 success criterion to the doc(s) that satisfy it, CONFIRMS the OPEN-E/AFSUB/set-mutation carry-over at the index level (the CONSENT inputs feeding 349), records the SPEC verdict, and states the single 349 IMPL hand-off. **This is the analog of `343-SPEC-INDEX.md` (343-05).**

> **⚠ READ FIRST — the freeze spine was AMENDED after the other docs by D-348-07 (commit `97333f90`).** Score + baseLevel moved from **live-read → STAMPED-FROZEN** in the afking Sub stamp, which grew from `(index, amount, day)` to **`(index, amount, day, scorePlus1, baseLevelPlus1)`**, superseding D-348-05 for those fields. **FREEZE-01 is therefore PROVEN (not split into a proven half + a live-read tradeoff half), and the only residual live-read is the EV-cap RMW** (`lootboxEvBenefitUsedByLevel[player][level+1]`) — a benign monotonic down-clamp with a hard ≤10 ETH cap and no profitable timing. This index reflects that FINAL state throughout. **The "live score/base-level/EV-cap reads admitted because in-window manipulation is −EV" framing in ROADMAP SC1 + REQUIREMENTS.md FREEZE-01 is SUPERSEDED** by D-348-07 (recorded at §5.6 below, the 343-05 discipline of surfacing upstream corrections at the index level). Three docs carry the dated D-348-07 amendment note (`348-FREEZE-PROOF.md`, `348-INVARIANT-CARRY.md`, `348-IMPL-EDIT-ORDER-MAP.md`); two (`348-GAS-INVENTORY.md`, `348-PLACEMENT-DECISION.md`) describe the live-read window under the pre-amendment framing — that residual narrows to the EV-cap clamp only, noted at §5.6.

---

## 1. Phase summary

Phase 348 is a **paper-only SPEC design-lock** for the v55 AfKing-in-Game redesign (relocate the AfKing subscriber set + the box freeze into Game-resident state: a `GameAfkingModule` delegatecall module + a `DegenerusGameStorage` append + a per-sub box stamp, collapsing the v54 cross-contract value plumbing into in-context reads). It writes ZERO `contracts/*.sol` — the entire phase only READS/greps the contracts and WRITES Markdown. Against the v54 de-custody HEAD `20ca1f79`, this phase:

- **PROVED** the FREEZE spine, not assumed (`348-FREEZE-PROOF.md`):
  - **FREEZE-02 — PROVEN** (the load-bearing D-348-02 index-binding obligation): the required-path process STAGE reads `LR_INDEX` once and binds the stamp to the **pre-RNG** index; the `subsFullyProcessed` no-interleave guard is SPECIFIED against source (re-pinned `:1016`/`:1089`/`:1629`/`:274`).
  - **FREEZE-03 — PROVEN** (stamped-day determinism): the afking open seeds `keccak256(abi.encode(rngWord, player, day, amount))` from the **STAMPED** buy-day (`LB:534`, `abi.encode` — NOT the PRESALE `abi.encodePacked` at `:644`); ZERO `block.*` entropy in the draw (grep-verified).
  - **FREEZE-01 — PROVEN (D-348-07):** the stamp now carries `(index, amount, day, scorePlus1, baseLevelPlus1)` — the **full seed-AND-multiplier-input set is proven-frozen**; score/baseLevel are stamped at process and read from the stamp at open (the analog of the human `lootboxPurchasePacked` deposit-time freeze, `LB:529-530`). The residual live-read narrows to the **EV-cap RMW only** (shared per-(player,level) accumulator) — a benign monotonic down-clamp, hard ≤10 ETH, no profitable timing.
- **CARRIED** the discharged REVERT-FREE-CHAIN + EV-cap invariants as the v55 locked invariant set (`348-INVARIANT-CARRY.md`): obligations 1–3 (slice-builder fidelity verbatim · EV-cap-at-open via `_applyEvMultiplierWithCap` · stamp + stamped-day seed), with **obligation 4 (the try/catch valve) DROPPED by D-348-04** (REVERT-02 rewritten to the no-valve form: revert-free-by-construction + fail-loud-on-solvency + terminal-routing-unblocked — the parallel to 343's D-01 `AUTOBUY-02` correction). The 3 §7 SPEC follow-ups (cost-unit reconciliation, stamp field widths, double-draw guard) are DISCHARGED; the light `/contract-auditor` obligation-1 pass returned **PASS** (5/5 invariants stated correctly for the fold; D-348-06).
- **DECIDED** the §4 placement = **REQUIRED-PATH** (`348-PLACEMENT-DECISION.md`, D-348-01 — the headline USER override): a chunked STAGE inserted in `advanceGame` immediately before `rngGate`, guarded by `subsFullyProcessed` + `_subCursor`, inheriting `_enforceDailyMintGate` (ZERO new gate code, D-348-03); the open stays a NORMAL post-RNG `OPEN_BATCH`-style leg. PLAN-V55 §4/§9 SUPERSEDED on the placement point; decision basis = **guaranteed-every-day** (the REVERT-FREE-CHAIN proof made required-path VIABLE, then the choice rested on guaranteed-every-day vs minimal-surface), NOT revert-safety.
- **MEASURED + sequenced** the ARCH-04 code-size reclaim (`348-CODE-SIZE-PLAN.md`): `forge build --sizes` measured **24,358 B / 218 B headroom** (the doc's headroom figure is EXACT, not stale); the reclaim is corrected to **~1.4–1.7 KB clean** (R1 + the two thin wrappers; NOT the v55 PLAN doc's overstated ~2.8 KB, which bundles R3's 953 B requiring a 5-caller retarget). R1 (`claimAffiliateDgnrs`→`BingoModule`, ~1.2–1.35 KB void) lands FIRST; the running-total stays **< 24,576** at every row (worst case 24,418). The producer-before-consumer edit-order for the SINGLE 349 diff is fixed in `348-IMPL-EDIT-ORDER-MAP.md`.
- **PRODUCED** the GAS-opportunity inventory (`348-GAS-INVENTORY.md`, ADVISORY / UNVALIDATED — the 350 `/gas-skeptic` is the only gate): 7 candidates (SCAV-348-01..07), with **GAS-03 SAFE-WITH-CONDITIONS carried verbatim** (bucket affiliate by roll-winner is SAFE; `quests.handleAffiliate`/`handlePurchase` MUST NOT be batched — non-linear completion logic) under the `feedback_security_over_gas` floor.
- **ATTESTED** every cited `file:line` by grep against the live tree (`348-GREP-ATTESTATION.md`, the load-bearing UPSTREAM PRODUCER), **resolving the box-seed pattern-drift** (the seed at `:534` is `abi.encode`, NOT `abi.encodePacked` — the latter is the PRESALE box at `:644`) plus the OPEN-E subscribe-gate drift (`:343-352`, not `:400-409`), the funder-resolution drift (`:624`, not `:682`), and the `_resolveBuy` body extent (`:727-863`, not `:727-795`) — the `feedback_verify_call_graph_against_source` floor in action.
- **CONFIRMED** the OPEN-E / AFSUB / set-mutation CONSENT carry-over against the re-pinned source (§6 below — the CONSENT-01/02 inputs feeding 349).

---

## 2. Document set (the D-08 multi-doc SPEC — seven discrete docs + this index)

The seven sibling docs below are the discrete, hand-off-able SPEC deliverables (the D-08 decision: keep the concerns discrete rather than one monolith, per the v50 / Phase-334 / Phase-343 precedent). Each carries its own grep-attested anchors against `20ca1f79`; this index ties them together.

| # | Doc (filename) | Plan | One-line purpose | Requirement(s) | ROADMAP SC |
|---|----------------|------|------------------|----------------|------------|
| D1 | `348-GREP-ATTESTATION.md` | 348-01 | The load-bearing UPSTREAM PRODUCER — call-graph attestation + per-file Drift-Correction Table re-pinning EVERY cited `file:line` to its ACTUAL line vs `20ca1f79`; RESOLVES the box-seed pattern-drift (`abi.encode` at `:534`, NOT `abi.encodePacked`; the packed form is the PRESALE box `:644`) + the OPEN-E subscribe gate (`:343-352`) + the funder resolution (`:624`) + the `_resolveBuy` body extent (`:727-863`); empty-diff baseline recorded; `subsFullyProcessed` CONFIRMED-NEW (ZERO matches in source). | FREEZE-02/03 (attestation slice) · ARCH-04 (anchor re-pin) | SC5 |
| D2 | `348-CODE-SIZE-PLAN.md` | 348-02 | ARCH-04 — `forge build --sizes` MEASURED baseline (24,358 B / 218 B headroom, EXACT) + the re-derived reclaim (~1.4–1.7 KB clean, NOT the overstated ~2.8 KB — R3's 953 B needs a 5-caller retarget) + the SEQUENCED running-total proving < 24,576 at every step (R1 FIRST; worst case 24,418). | ARCH-04 | SC4 |
| D3 | `348-GAS-INVENTORY.md` | 348-02 | The `/gas-scavenger` ADVISORY / UNVALIDATED inventory (7 candidates SCAV-348-01..07 — the 350 `/gas-skeptic` is the only gate) + the GAS-03 SAFE-WITH-CONDITIONS carve-out verbatim (no `quests.handleAffiliate` batching) under the `feedback_security_over_gas` floor. | ARCH-04 (gas slice; the GAS-01/02/03 wins LAND at 350) | SC5 (gas slice) |
| D4 | `348-FREEZE-PROOF.md` | 348-03 | The SECURITY SPINE — FREEZE-02 pre-RNG index-binding (the `subsFullyProcessed` no-interleave guard SPECIFIED) + FREEZE-03 stamped-day determinism (`abi.encode` `:534`, ZERO `block.*`) + **FREEZE-01 PROVEN (D-348-07): the full `(index, amount, day, scorePlus1, baseLevelPlus1)` set frozen; the EV-cap RMW is the SOLE residual live-read — a benign monotonic clamp.** *(AMENDED — D-348-07.)* | FREEZE-01 · FREEZE-02 · FREEZE-03 | SC1 |
| D5 | `348-INVARIANT-CARRY.md` | 348-03 | The discharged REVERT-FREE-CHAIN + EV-cap invariants carried as the v55 locked invariant set: obligations 1–3 + **D-348-04 (try/catch valve DROPPED → no-valve REVERT-02 rewrite)** + the 3 §7 follow-ups discharged + the light `/contract-auditor` obligation-1 pass (PASS, 5/5). *(AMENDED — D-348-07: the `evMultiplierBps` fed to the cap helper now derives from the FROZEN `scorePlus1`; the cap RMW stays live.)* | (REVERT/EVCAP invariants — owned at 349; carried as PLACE-01 input) | SC2 |
| D6 | `348-PLACEMENT-DECISION.md` | 348-04 | PLACE-01 — the §4 placement DECIDED = REQUIRED-PATH (D-348-01 USER override; PLAN-V55 §4/§9 SUPERSEDED); the chunked-STAGE mechanism (`subsFullyProcessed` + `_subCursor` before `rngGate`); the two carried proof obligations (D-348-02 → FREEZE-PROOF, D-348-04 → INVARIANT-CARRY); the mint-gate standing ACCEPTED (ZERO new gate code, D-348-03); the bounty fold NOTED (PLACE-02, 349-owned). | PLACE-01 | SC3 |
| D7 | `348-IMPL-EDIT-ORDER-MAP.md` | 348-05 | ARCH-04 — the DOWNSTREAM CONSUMER of all five upstream deliverables: the FINAL reconciled shapes + the producer-before-consumer edit-order for the SINGLE 349 IMPL diff (reclaim FIRST → storage append → `GameAfkingModule` → AdvanceModule STAGE → interfaces → AfKing stubs) + the four carried corrections threaded + the re-pin-before-authoring caution. *(AMENDED — D-348-07: the 5-field stamp threaded through §1.1/§1.2/§1.3 + Steps 2a/3a + §4.4.)* | ARCH-04 | SC5 (edit-order slice) |
| — | `348-SPEC-INDEX.md` (this doc) | 348-06 | The index for the D-08 set — requirement/success-criterion traceability + the OPEN-E/set-mutation carry-over confirmation + the SPEC verdict + the 349 IMPL hand-off. | (cross-cutting; re-attests all 5) | (all) |

> **D-08 note:** the discrete-doc-set shape (seven docs + this index) is the deliberate v50 / Phase-334 / Phase-343 multi-doc precedent — each concern is independently hand-off-able to 349/350/351 and independently re-greppable when the subject HEAD moves. There is no monolithic `348-SPEC.md`.

---

## 3. Requirement → doc traceability

The five Phase-348 requirements (REQUIREMENTS.md / ROADMAP Phase 348), each mapped to the doc(s) that satisfy it:

| Requirement | Description (abbrev.) | Covering doc(s) | Plan(s) | Status |
|-------------|------------------------|-----------------|---------|--------|
| **FREEZE-01** | Freeze-completeness — the stamp captures ALL outcome-determining state at process; the open re-derives nothing manipulable. **PROVEN by D-348-07** (the full `(index, amount, day, scorePlus1, baseLevelPlus1)` set is frozen; the EV-cap RMW is the SOLE residual live-read — a benign monotonic down-clamp, hard ≤10 ETH, no profitable timing). | `348-FREEZE-PROOF.md` (D4, §0 + FREEZE-01) + `348-GREP-ATTESTATION.md` (D1, the `abi.encode` `:534` + EV-cap `:459`/`:1326`/`:1468-1469` + `lootboxDay :514` anchors) | 348-03, 348-01 | COVERED |
| **FREEZE-02** | Index-binding — the stamp binds to the pre-RNG `LR_INDEX` (read once at pass start); the process-pass MUST NOT straddle a mid-day `requestLootboxRng` index advance (`AdvanceModule:1016`). **PROVEN** (the load-bearing D-348-02 obligation; the `subsFullyProcessed` no-interleave guard SPECIFIED against source). | `348-FREEZE-PROOF.md` (D4, FREEZE-02 a/b/c/d) + `348-GREP-ATTESTATION.md` (D1, §2.4 — `:1016`/`:1089`/`:1629`/`:274`) | 348-03, 348-01 | COVERED |
| **FREEZE-03** | Determinism — the box seed `keccak256(abi.encode(rngWord, player, day, amount))` uses the STAMPED buy-day (never open-time `_simulatedDayIndex()`) and carries no `block.*` entropy in the draw. **PROVEN** (ZERO `block.*` grep-verified; `abi.encode` `:534`, NOT the PRESALE `abi.encodePacked` `:644`). | `348-FREEZE-PROOF.md` (D4, FREEZE-03 a/b/c/d) + `348-GREP-ATTESTATION.md` (D1, §1a + §2.1 entropy-side guard) | 348-03, 348-01 | COVERED |
| **PLACE-01** | The §4 placement (required-path `advanceGame` STAGE vs separate permissionless legs) DECIDED at SPEC on non-revert grounds (guaranteed-every-day; the `_enforceDailyMintGate` standing interaction; bounty farm-by-splitting). **DECIDED = required-path** (D-348-01 USER override). | `348-PLACEMENT-DECISION.md` (D6) + the carried invariants in `348-INVARIANT-CARRY.md` (D5, the D-348-04 no-valve REVERT-02 the override creates) | 348-04, 348-03 | COVERED |
| **ARCH-04** | Game runtime code-size stays < 24,576 bytes at every intermediate step — reclaim FIRST before adding the afking stubs (sequenced so the Game never breaches the ceiling mid-flight). | `348-CODE-SIZE-PLAN.md` (D2, the MEASURED reclaim + running-total) + `348-IMPL-EDIT-ORDER-MAP.md` (D7, the producer-before-consumer edit-order) + `348-GREP-ATTESTATION.md` (D1, §2.5 the reclaim-target sites) + `348-GAS-INVENTORY.md` (D3, the gas slice) | 348-02, 348-05, 348-01 | COVERED |

All five requirements are COVERED by a delivered doc.

---

## 4. ROADMAP success-criterion → doc traceability

The five ROADMAP Phase-348 success criteria, each mapped to the doc(s) that satisfy it:

| Success Criterion (ROADMAP) | Covering doc(s) | Status |
|------------------------------|-----------------|--------|
| **SC1** — the FREEZE spine is PROVEN, not assumed (FREEZE-01/02/03): freeze-completeness (the stamp captures ALL outcome-determining state; the open re-derives nothing manipulable) · index-binding (the stamp binds to the pre-RNG `LR_INDEX` read once; the process-pass MUST NOT straddle a mid-day `requestLootboxRng` advance) · determinism (the seed uses the STAMPED buy-day, no `block.*` entropy). **Per D-348-07 the SC1 "live score/base-level/EV-cap reads admitted because −EV" clause is SUPERSEDED — score+baseLevel are now stamped-frozen, FREEZE-01 is PROVEN, and the residual narrows to the benign EV-cap clamp (§5.6).** | `348-FREEZE-PROOF.md` (D4 — FREEZE-01 PROVEN [D-348-07] + FREEZE-02 + FREEZE-03) + `348-GREP-ATTESTATION.md` (D1 — the anchor attestation it reasons over) | COVERED |
| **SC2** — the discharged REVERT-FREE-CHAIN + EV-cap invariants are carried as locked SPEC invariants: obligation 1 (preserve `_resolveBuy` verbatim) · obligation 2 (EV-cap-at-open via `_applyEvMultiplierWithCap` keyed `[player][level+1]`, ≤10 ETH no-revert, buy-time write bypassed) · obligation 3 (stamp + stamped-day seed) · obligation 4 (~~the try/catch valve~~ → **DROPPED by D-348-04**); the 3 §7 follow-ups (cost-units, stamp widths, double-draw guard) discharged. | `348-INVARIANT-CARRY.md` (D5 — obligations 1–3 + the D-348-04 no-valve rewrite + the §7 discharges + the obligation-1 `/contract-auditor` PASS) | COVERED |
| **SC3** — the §4 placement is DECIDED on non-revert grounds: required-path is VIABLE (the proof showed a funded well-formed sub can't revert → won't freeze the day), so the choice rests on guaranteed-every-day vs minimal-surface, the `_enforceDailyMintGate` standing interaction, and bounty farm-by-splitting; process-leg pre-RNG cursor-chunked, open-leg post-RNG cursor-chunked; the decision + rationale recorded for IMPL. **DECIDED = required-path (D-348-01 USER override).** | `348-PLACEMENT-DECISION.md` (D6 — the decision + basis + mechanism + carried obligations + mint-gate dependency + bounty fold) | COVERED |
| **SC4** — the code-size reclaim plan is produced + sequenced: a measured, agent-verified reclaim keeping the Game runtime < 24,576 bytes at EVERY intermediate step (`claimAffiliateDgnrs`→`BingoModule` FIRST; read-aggregators drop-`view`/→lens; the reserve set); the edit-order sequenced so the ceiling is never breached mid-flight. | `348-CODE-SIZE-PLAN.md` (D2 — MEASURED 24,358/218 + the re-derived reclaim + the running-total) + `348-IMPL-EDIT-ORDER-MAP.md` (D7 — §2 the running-total carry + §3 the edit-order) | COVERED |
| **SC5** — the OPEN-E/set-mutation carry-over is confirmed + every cited `file:line` is grep-attested vs `20ca1f79` (CONSENT / FREEZE / ARCH): the subscribe-time `isOperatorApproved` (OPEN-E) gate, the pass-gating (`validThroughLevel`), the VAULT/SDGNRS exemption-on-`player`, the funder=src accounting, and the "no cursor advance after swap-pop" set-mutation invariant confirmed to carry over verbatim with the OPEN-E 4-protection re-attested; the GameAfkingModule + storage-append + two-path-open shapes reconciled producer-before-consumer; every cited anchor grep-verified vs `20ca1f79` with any drift corrected (no "by construction" survives un-checked). | `348-GREP-ATTESTATION.md` (D1 — the anchor attestation + the four drift corrections) + `348-IMPL-EDIT-ORDER-MAP.md` (D7 — the producer-before-consumer reconciliation) + **§6 of this index** (the CONSENT carry-over CONFIRMED against the re-pinned source) | COVERED |

All five success criteria are COVERED by a delivered doc.

---

## 5. SPEC verdict

**PASS — the Phase-348 SPEC is design-gating-complete.** No design amendment is required before 349 IMPL.

### 5.1 — The FREEZE spine PROVEN (SC1 / FREEZE-01/02/03)

- **FREEZE-02 (the load-bearing D-348-02 obligation) — PROVEN.** `LR_INDEX` advances at exactly two sites, both AFTER/OUTSIDE the STAGE (Site B daily request `:1629` downstream of `rngGate(:274)`; Site A mid-day `:1089` blocked by the guard); no third advance site exists (grep returns exactly `:1089`/`:1629`). The STAGE reads `LR_INDEX` once at pass start (the uniform index epoch); the `subsFullyProcessed` no-interleave guard is SPECIFIED against source (block `requestLootboxRng` while `!subsFullyProcessed`, mirroring the existing `LR_MID_DAY` reroll-block at `:1020`). **`subsFullyProcessed` is CONFIRMED-NEW** (ZERO matches in the AdvanceModule — 349 MUST author it; it is a SPECIFICATION, not an attestation of existing code). 351 TST-01 owns the empirical freeze-fuzz.
- **FREEZE-03 — PROVEN.** The seed at the re-pinned `LB:534` is `keccak256(abi.encode(rngWord, player, day, amount))` — `abi.encode` (the afking open MUST mirror this; the PRESALE box at `:644` is the only `abi.encodePacked` seed, a different path). ZERO `block.timestamp/number/prevrandao/coinbase/blockhash` anywhere in the lootbox module (grep-verified). The open seeds from the STAMPED buy-day (mirroring `lootboxDay[index][player]` `:514`), never open-time `_simulatedDayIndex()` (`:513`/`:766`/`:799`/`:836`/`:868`). The determinism lever (open-time day choice) is closed by the boundary-pinned stamp.
- **FREEZE-01 — PROVEN (D-348-07).** The stamp now carries `(index, amount, day, scorePlus1, baseLevelPlus1)`: `index`/`day`/`amount` frozen by FREEZE-02 + FREEZE-03; `scorePlus1` (uint16) + `baseLevelPlus1` (uint24) stamped at process and read from the stamp at open — the **direct analog of the human deposit-time freeze** of `lootboxPurchasePacked` (`LB:529-530`). The full seed-AND-multiplier-input set is frozen the instant the box is processed. **Freezing score/baseLevel does NOT move them into the seed** (they still enter AFTER the seed — FREEZE-03 unchanged); it completes FREEZE-01's frozen set. The **SOLE residual live-read is the EV-cap RMW** (`lootboxEvBenefitUsedByLevel[player][level+1]`), a shared per-(player,level) cumulative accumulator that cannot be per-box-stamped — but it is a **benign monotonic DOWN-CLAMP** (hard `LOOTBOX_EV_BENEFIT_CAP = 10 ether`, short-circuits to a no-write 100%-EV neutral once exhausted, `LB:478-481`): no profitable timing. **NOT findings-grade** (carried into `audit/FINDINGS-v55.0.md` + the v52 cumulative sweep as a benign noted item; no `/economic-analyst` red-team — a monotonic down-clamp with a hard cap has no profitable timing, USER-established no-credible-vector, precedent 339-01 D-03).

### 5.2 — The discharged invariants CARRIED, with the try/catch DROP (SC2)

The proof's obligations 1–3 are the v55 locked invariant set; **obligation 4 (the per-sub try/catch skip valve) is DROPPED by D-348-04** — a REWRITE of REQUIREMENTS.md REVERT-02 + proof §5 obligation 4 to the no-valve form: **(a) healthy path revert-free-by-construction** (obligation 1 — nothing to catch); **(b) Class B solvency-violation FAILS LOUD** (the `claimablePool -= uint128(...)` subtractions at `Game:1010`/`Game:1912`/`Storage:847` revert only if SOLVENCY-01 is already violated → catching would MASK a catastrophic bug → it MUST propagate); **(c) Class C liveness-timeout is TERMINAL — routing-unblocked** (fires only ≥120-day-dead; the SPEC requires 349 VERIFY the STAGE cannot block the game-over routing, `_handleGameOverPath` runs + returns at `:182` BEFORE the STAGE block). The proof burden CONCENTRATES on obligation 1 (the SOLE no-brick guarantor) — which is exactly where the light `/contract-auditor` pass sits. The two §10 knock-ons reconciled: §10 rule (2) "mint slice fails → SKIP" becomes a **pre-emptive** skip (the only pre-emptive skip is the `LOOTBOX_MIN` transient `:772-774`; rule (1) unfunded eviction unaffected); the optional per-cycle eviction cap is recommended **DROPPED** (lost its revert-driven rationale). The 3 §7 follow-ups DISCHARGED: cost-units equivalent (mind the dual `TICKET_SCALE` = 400 [AfKing `:238`] vs 4×100 [Storage `:166`]); stamp widths = 5 fields, still **2-slot-feasible** (the 40 D-348-07 bits drop into slot 2's ~90 spare bits — same single SSTORE); double-draw guarded (stamp at process, single RMW at open). **Obligation-1 light `/contract-auditor` pass: PASS** — 5/5 invariants stated correctly for the fold; zero mis-statements; zero findings (D-348-06).

### 5.3 — The §4 placement DECIDED = required-path (SC3 / PLACE-01)

The placement is a DELIBERATE USER OVERRIDE (D-348-01): the process-pass is a chunked STAGE inserted in `advanceGame` immediately before `rngGate(:274)` (new-day path only), guarded by `subsFullyProcessed` + `_subCursor`, inheriting `_enforceDailyMintGate` (`:191`/`:973`) with **ZERO new gate code** (D-348-03, ACCEPTED — decoupling rejected); the open stays a NORMAL post-RNG `OPEN_BATCH`-style leg. PLAN-V55 §4 + §9 are explicitly marked **SUPERSEDED** on the placement point (recorded as an override, not a silent reconciliation — the documentation-integrity discipline, the 343 D-01 precedent). Decision basis = **guaranteed-every-day, NOT revert-safety**: the REVERT-FREE-CHAIN proof made required-path VIABLE (a funded well-formed sub can't revert → can't freeze the day → the §0-Correction-2 liveness fear discharged for the healthy path), and given viability the USER weighed guaranteed-every-day higher than minimal-surface. The bounty fold (PLACE-02) is NOTED only (349-owned).

### 5.4 — The code-size reclaim MEASURED + sequenced < 24,576 (SC4 / ARCH-04)

`forge build --sizes` MEASURED the live `20ca1f79` Game at **24,358 B / 218 B headroom** — the v55 PLAN doc's most load-bearing figure (218 B) is **EXACT, not stale**, and genuinely thin (0.9%). The doc's "~2.8 KB clean reclaim" is **OVERSTATED** — corrected to **~1.4–1.7 KB clean** (R1 `claimAffiliateDgnrs`→`BingoModule` ~1.2–1.35 KB void + the R2/R3 thin wrappers ~120–220 B each; R3's full 953 B requires a 5-caller retarget — `WhaleModule:875`/`DecimatorModule:704`/`BurnieCoin:620`/`StakedDegenerusStonk:913` — a 349 cost, NOT free). The running-total proves **R1 FIRST is mandatory** (it must precede the ~1.0–1.5 KB stub additions, or the total spikes through 24,576); on the simultaneous worst case (R1 low 1,200 + stubs high 1,500) R1 alone breaches by 82 B → 349 ALSO lands R2 + the R3 wrapper to restore a ≥158 B margin (worst-case landing **24,418 < 24,576**). The central case lands at 24,275 (margin 301) with R2/R3 pure insurance. The **349 `forge build --sizes` after the diff is the FINAL authoritative verification.**

### 5.5 — The GAS inventory produced (advisory, 350-gated) + ZERO `contracts/*.sol` mutation

The `/gas-scavenger` inventory (`348-GAS-INVENTORY.md`) enumerates 7 candidates (SCAV-348-01..07), **all ADVISORY / UNVALIDATED** — the 350 `/gas-skeptic` is the sole validation-and-application gate, under the `feedback_security_over_gas` floor. GAS-01/02 (the ~120k box-buy collapse + the ~3–5k/sub staticcall→SLOAD) are STRUCTURAL to the relocation (confirmed-measured at 350/351, NOT standalone removals); GAS-03 is the residual same-slot batching change, **SAFE-WITH-CONDITIONS** (bucket affiliate by roll-winner SAFE; `quests.handleAffiliate`/`handlePurchase` MUST NOT be batched — non-linear completion logic, pre-marked for 350 REJECTION). NOTHING is applied at 348. **Paper-only invariant honored across the entire phase: `git diff --name-only -- contracts/` is EMPTY** — zero contract edits in 348-06 and in every sibling plan. The CLEANUP/reclaim/GAS lists ENUMERATE; they do not apply. The first `contracts/` mutation is the 349 IMPL diff.

### 5.6 — The upstream corrections surfaced (the 343-05 discipline)

Recorded at the index level so no correction is silently lost into 349 (the documentation-integrity threat T-348-13/15):

1. **D-348-07 — score+baseLevel STAMPED-FROZEN (supersedes D-348-05 + the SC1/REQUIREMENTS FREEZE-01 "−EV live-read" framing).** Score + baseLevel moved from live-read → stamped `scorePlus1`/`baseLevelPlus1` in the Sub stamp (now 5 fields). **ROADMAP SC1's clause** *"the §10 live score/base-level/EV-cap reads are admitted ONLY because in-window manipulation is −EV"* **and REQUIREMENTS.md FREEZE-01's parenthetical** are both **SUPERSEDED on the score/baseLevel axis** — those fields are no longer admitted-as-live, they are PROVEN-frozen. The residual narrows to the **EV-cap RMW only** (a benign monotonic down-clamp; §5.1b). **Two sibling docs (`348-GAS-INVENTORY.md` SCAV-348-01/-06; `348-PLACEMENT-DECISION.md` §3 "PLACE-02 protocol-early-sequenced drift")** describe the live-read window under the pre-D-348-07 framing (score/baseLevel/EV-cap read LIVE; the live-read window ACCEPTED as a known issue) — that framing is superseded for score+baseLevel and survives ONLY as the EV-cap clamp; the substance (the open stays a normal post-RNG leg; the box re-derives from the stamp + word) is unchanged. (`348-FREEZE-PROOF.md`, `348-INVARIANT-CARRY.md`, `348-IMPL-EDIT-ORDER-MAP.md` all carry the dated D-348-07 amendment note.)
2. **Box-seed `abi.encode` re-pin (348-01 §1a).** The afking open seed is `keccak256(abi.encode(rngWord, player, day, amount))` at `LB:534` — `abi.encode`, NOT `abi.encodePacked`. `abi.encodePacked` appears at exactly ONE site, `:644`, the PRESALE box — a distinct path the afking open does NOT copy. The encoding is load-bearing (`abi.encode` 32-byte left-padded vs `abi.encodePacked` tight-packed → different hash preimages → copying the packed form breaks box-outcome equivalence). The v55 PLAN doc's cited grep-pattern (`abi.encodePacked` at `:534`) was wrong; the line number was correct.
3. **D-348-04 — the try/catch valve DROPPED (no-valve REVERT-02).** REQUIREMENTS.md REVERT-02 + proof §5 obligation 4 both literally say "thin per-sub try/catch skip valve" — a stale carried invariant. Rewritten to the no-valve form (§5.2). The parallel to 343's D-01 `AUTOBUY-02`→`b.funder` correction.
4. **Required-path override (348-04 D-348-01).** PLAN-V55 §4/§9 (separate-legs recommendation) SUPERSEDED on the placement point (§5.3). A recorded override of a carried artifact, not a silent edit.
5. **The three additional 348-01 attestation drifts:** the OPEN-E subscribe gate is `:343-352` (NOT the doc-cited `:400-409`); the funder resolution is `:624` (NOT `:682`); `_resolveBuy`'s body extends to `:863` (NOT `:795`). 349 cites the ACTUAL re-pinned lines, never the drifted doc-cited lines.

### 5.7 — ZERO `contracts/*.sol` mutation (re-asserted)

Paper-only invariant honored across the entire phase. `git diff --name-only -- contracts/` is **EMPTY**; `git diff --numstat 20ca1f79 HEAD -- contracts/` is **EMPTY** (the working tree's `contracts/` is byte-identical to `20ca1f79`). The pre-existing unrelated `scope.txt` working-tree change is untouched by this plan and every sibling plan. The first `contracts/` mutation is the 349 IMPL diff.

---

## 6. CONSENT carry-over confirmation (OPEN-E / AFSUB / set-mutation) — CONFIRMED against the re-pinned source

The OPEN-E / AFSUB / set-mutation surface is the **CONSENT-01/02** input set, **349-owned (BUILT at IMPL)** — but it is **CONFIRMED HERE against the re-pinned `AfKing.sol` source** (the SC5 carry-over slice), not assumed, before 349 builds it. Each element carries over **VERBATIM** into `GameAfkingModule.subscribe` + the process/open passes (the relocation moves the logic into the module; it does NOT change the consent semantics):

| Carry-over element (CONSENT-01/02) | Re-pinned source (vs `20ca1f79`) | Carries over verbatim? |
|---|---|---|
| **Subscribe-time `isOperatorApproved` (OPEN-E) gate** — SUB-02 self-consent + OPENE-04 non-zero/non-self `fundingSource` gate | `isOperatorApproved` iface decl `AfKing.sol:43`; SUB-02 self-consent read `:338` (`if (!GAME.isOperatorApproved(subscriber, msg.sender))`); OPENE-04 gate `:343-352` (`fundingSource != address(0) && fundingSource != subscriber && !GAME.isOperatorApproved(fundingSource, subscriber)` → `revert NotApproved()`) | ✅ YES — both reads fold into `GameAfkingModule.subscribe` unchanged (the in-context `GAME.isOperatorApproved` becomes a same-contract read; same gate, same semantics) |
| **Pass-gating (`validThroughLevel`)** — the AFSUB cheaper-validity encoding | `validThroughLevel` Sub field `:103`; the subscribe-time write `:371` (`s.validThroughLevel = uint32(GAME.lazyPassHorizon(subscriber))`); the per-iteration `currentLevel > validThroughLevel` crossing `:571` | ✅ YES — the field relocates into the Game-resident `Sub` record; the crossing check rides the process STAGE |
| **VAULT/SDGNRS exemption-on-`player`** — the exemption stays keyed on the un-spoofable `player`, never `src` | the funder resolution `:624` (`address src = sub.fundingSource == address(0) ? player : sub.fundingSource;`); comment `:620-623` confirms the exemption stays on `player` | ✅ YES — the exemption stays on `player`; only the read becomes an in-context `afkingFunding[src]` SLOAD (no boundary) |
| **funder=src accounting** — debit the resolved funding source, not `player` | the `src` resolution `:624` (the OPENE-02 once-per-iter funder resolution; the v54 343-D-01 `b.funder` correction's source) | ✅ YES — the process-pass debits `afkingFunding[src]`; the VAULT/SDGNRS exemption stays on `player` (the 343 D-01 funder-keyed identity carried into v55) |
| **"No cursor advance after swap-pop" set-mutation invariant** — the H-CANCEL-SWAP-MISS / cancel-tombstone-streak class | the subscriber set + swap-pop bookkeeping relocates to `_subOf`/`_subscribers`/`_subscriberIndex` (`348-IMPL-EDIT-ORDER-MAP.md` §1.1); the eviction must preserve "no cursor advance after swap-pop" so a relocated tail is not skipped behind the cursor | ✅ YES — CONSENT-02 carries the tombstone-then-reclaim shape verbatim; 351 TST-04 proves the streak-preserved swap-pop regression NEGATIVE-VERIFIED |

**The OPEN-E 4-protection structure is RE-ATTESTED** (`[[open-e-operator-approval-trust-boundary]]`): **(1) consent-gate-at-subscribe** (the `:338` + `:343-352` reads — a non-zero/non-self `fundingSource` MUST be operator-approved at subscribe); **(2) default-self** (a zero `fundingSource` resolves to `player` at `:624` — byte-identical to self-funding, no gate needed); **(3) no-escalation** (the gate is checked at subscribe, the funding source cannot be changed to escalate later); **(4) trust-the-sub temporal bound** (the operator-approval is the trust boundary — grantee = the same person or a fixed contract; the BURNIE-funding overload is ACCEPTED-BY-DESIGN, the `allowBurnieFunding` flag DROPPED per the USER-locked Phase-320 disposition). These are **CONSENT-01/02 inputs, 349-owned** (BUILT at IMPL) — confirmed here against the re-pinned source so the 349 build inherits a CONFIRMED carry-over, not an assumption. (Note: the funder=src accounting + the OPEN-E gate are the carry-over of the v54 343-SPEC SOLVENCY/OPEN-E disposition into the game-resident model — the same trust boundary, now in-context.)

---

## 7. 349 IMPL hand-off

**349 IMPL is sequencing-complete from this SPEC set.** The single batched diff hand-off:

- **Authoring source:** the 349 author writes ONE fully-reconciled, code-size-safe `contracts/*.sol` diff **against `348-IMPL-EDIT-ORDER-MAP.md`** — the FINAL reconciled shapes (Section 1: the `DegenerusGameStorage` 2-slot append with the 5-field stamp + the reused `afkingFunding` ledger; the `GameAfkingModule` contents; the TWO open routes; the `AfKing.sol` thin-stub collapse), the code-size running-total (Section 2: reclaim FIRST), and the **producer-before-consumer edit order** (Section 3: reclaim FIRST → storage append → `GameAfkingModule` → AdvanceModule STAGE → interfaces → AfKing stubs), with **zero "by construction" assumptions** and **zero intermediate broken state**. The Game never breaches **24,576** mid-flight.
- **FREEZE gate:** the **`348-FREEZE-PROOF.md` spine (FREEZE-01 PROVEN [D-348-07] + FREEZE-02 + FREEZE-03)** is the freeze gate the 349 diff inherits — but FREEZE-02's `subsFullyProcessed` no-interleave guard is a **SPECIFICATION** the 349 IMPL MUST author (it does not exist in source — CONFIRMED-NEW), and FREEZE-01's frozen set requires 349 to **stamp `scorePlus1`/`baseLevelPlus1` at process + read them from the stamp at open** (D-348-07). 351 TST-01 proves the freeze empirically.
- **The carried corrections 349 MUST honor** (the four threaded in `348-IMPL-EDIT-ORDER-MAP.md` §4 + the D-348-07 amendment):
  1. **Box-seed `abi.encode` re-pin** → the open-pass: the afking open seed = `keccak256(abi.encode(rngWord, player, day, amount))` (`LB:534`); the `abi.encodePacked` form (`LB:644`) is the PRESALE box, the WRONG pattern to copy. Seed from the STAMPED day (FREEZE-03), not open-time.
  2. **No-try/catch REVERT-02 correction (D-348-04)** → the process/open STAGE: NO valve (revert-free-by-construction [obl 1] + fail-loud-on-solvency [class B, MUST propagate] + terminal-routing-unblocked [class C, verify the STAGE cannot block game-over routing]); obligation 1 is the SOLE no-brick guarantor; the eviction cap is DROPPED.
  3. **Required-path override (D-348-01)** → the AdvanceModule STAGE: the process-pass is a chunked STAGE before `rngGate(:274)` (new-day path only), inheriting `_enforceDailyMintGate` (ZERO new gate code); the open stays a separate post-RNG leg.
  4. **EV-cap-at-open, buy-time write BYPASSED (D-348-07-amended)** → the open-pass: one `_applyEvMultiplierWithCap(player, level+1, amount, mult)` RMW at open keyed `[player][level+1]`, ≤10 ETH no-revert, fed the **FROZEN** `evMultiplierBps` (from the stamped `scorePlus1`); the buy-time write at `MintModule:1303/1327` is BYPASSED (no double-draw). The cap RMW is the SOLE residual live-read (a benign monotonic clamp).
  - **The 5-field stamp (D-348-07):** stamp `(index, amount, day, scorePlus1, baseLevelPlus1)` at process (40 bits into the slot-2 word, same single SSTORE, still 2-slot-feasible); read score+baseLevel FROM the stamp at open (mirroring the human `lootboxPurchasePacked` read-from-snapshot path `LB:529-530`).
- **CONSENT carry-over (§6):** 349 inherits the CONFIRMED OPEN-E gate + `validThroughLevel` + VAULT/SDGNRS exemption-on-`player` + funder=src + "no cursor advance after swap-pop" — built into `GameAfkingModule.subscribe` + the process/open passes verbatim (CONSENT-01/02), with the OPEN-E 4-protection structure re-attested.
- **Re-pin before authoring (CRITICAL):** every grep across the D-08 set is a **point-in-time snapshot of `20ca1f79`**. As of `f353a50b` the working tree's `contracts/` is byte-identical to `20ca1f79` (`git diff --numstat 20ca1f79 HEAD -- contracts/` EMPTY), so the set is current. **If the subject HEAD's `contracts/` moves off `20ca1f79` before 349 — i.e. if ANY `contracts/*.sol` commit lands between now and the 349 diff — the 349 author MUST re-run `348-GREP-ATTESTATION.md`** (`git diff --numstat 20ca1f79 <new HEAD> -- contracts/` + re-grep every anchor) and re-derive the code-size running-total (`forge build --sizes --skip "test/**" --skip "*.t.sol"`), then cite the re-pinned successor lines — **NEVER trust the lines transcribed here once the tree has drifted.** (The exact 343→344 hand-off discipline.)

---

## 8. Validity + paper-only assertion

**Valid until** the next `contracts/` mutation (the 349 IMPL diff). This index, like the seven docs it indexes, is a point-in-time snapshot of `20ca1f79`.

**Paper-only assertion:** `git diff --name-only -- contracts/` is **EMPTY** — zero `contracts/*.sol` edits in this plan (348-06) or anywhere in Phase 348. The pre-existing unrelated `scope.txt` working-tree change is untouched. The only CLI used across the phase was `git diff` / `forge build --sizes` / `forge inspect` (read-only, no ffi, no package install) + `grep`/read.

*Phase: 348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p · Plan: 348-06 · Requirements: FREEZE-01, FREEZE-02, FREEZE-03, PLACE-01, ARCH-04.*
