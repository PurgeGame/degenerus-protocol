---
phase: 348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-CODE-SIZE-PLAN.md
  - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-GAS-INVENTORY.md
autonomous: true
requirements: [ARCH-04]

must_haves:
  truths:
    - "The CURRENT DegenerusGame runtime code-size is MEASURED via `forge build --sizes` (NOT trusted from the doc's stale 218B-headroom figure) and the real headroom vs 24,576 is recorded"
    - "Each EXISTING reclaim target (claimAffiliateDgnrs, playerActivityScore, previewSellFarFutureTickets) has a measured/estimated byte size — the doc's ~2.8KB-reclaim figure is re-derived, not copied"
    - "The new GameAfkingModule dispatch-stub additions to the Game are ESTIMATED (code not written) with the estimation basis stated"
    - "The edit-order map has a running-total column proving < 24,576 at EVERY intermediate step (reclaim FIRST → add stubs → lens/drop-view as needed)"
    - "/gas-scavenger has been RUN at SPEC as an advisory candidate list (no validation — 350 runs /gas-skeptic); each candidate tagged ADVISORY/UNVALIDATED"
    - "The §6 scorecard levers are enumerated (box-ledger→warm-stamp ~120k; afkingSnapshot/afkingFundingOf staticcall→SLOAD ~3-5k; same-slot aggregate flushes) with GAS-01/02 flagged structural-to-the-IMPL-relocation"
    - "The GAS-03 SAFE-WITH-CONDITIONS list is recorded: bucket affiliate by roll-winner is safe; do NOT batch quests.handleAffiliate (non-linear completion logic)"
    - "forge build --sizes runs without ffi (the foundry.toml ffi-off only blocks in-test vm.ffi)"
  artifacts:
    - path: ".planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-CODE-SIZE-PLAN.md"
      provides: "ARCH-04 — the MEASURED + sequenced code-size reclaim plan with a running-total edit-order proving < 24,576 at every step"
      contains: "24576"
    - path: ".planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-GAS-INVENTORY.md"
      provides: "The /gas-scavenger advisory candidate list + the §6 scorecard levers + the GAS-03 SAFE-WITH-CONDITIONS carve-out (all UNVALIDATED — 350 gates)"
      contains: "gas-skeptic"
  key_links:
    - from: "348-CODE-SIZE-PLAN.md"
      to: "forge build --sizes (the live DegenerusGame runtime size)"
      via: "measured headroom vs 24576 + per-target reclaim bytes + running-total edit-order"
      pattern: "forge build --sizes"
    - from: "348-GAS-INVENTORY.md"
      to: "the 350 GAS phase (/gas-skeptic validation gate)"
      via: "advisory candidate list, each tagged ADVISORY/UNVALIDATED"
      pattern: "ADVISORY|UNVALIDATED"
---

<objective>
Author the two MEASUREMENT-PRODUCER deliverables for Phase 348, both of which read the live tree directly (independent of the attestation doc — they grep/measure the same byte-identical `20ca1f79` working tree):

1. `348-CODE-SIZE-PLAN.md` (ARCH-04) — a MEASURED, agent-verified reclaim plan that keeps the Game runtime code-size < 24,576 bytes at EVERY intermediate step. Per D-348-08, do NOT trust the v55 PLAN doc's figures (218B headroom, the per-symbol bytes, ~2.8KB reclaim) — they may be stale vs `20ca1f79` and 218B is too thin to take on faith. `forge build --sizes` the CURRENT Game runtime size + the real headroom; measure each EXISTING reclaim target; ESTIMATE the new stub additions; produce an edit-order map with running-total arithmetic.

2. `348-GAS-INVENTORY.md` — per D-348-09, RUN `/gas-scavenger` at SPEC as an advisory candidate list (no validation here — 350 runs `/gas-skeptic`) so 349 IMPL can build the wins in from the start, PLUS enumerate the §6 scorecard levers + the GAS-03 SAFE-WITH-CONDITIONS list. This mirrors Phase 343's D-02 (gas-scavenger-at-SPEC → gas-skeptic-at-GAS).

These are the analog of `343-GAS-INVENTORY.md` (343-03) + the measurement portion of the 343 code-size work. They are Wave-1 producers because measurement reads the live tree, not the attestation doc.
Output: Two markdown artifacts in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-CONTEXT.md

<interfaces>
<!-- D-348-08: CLI `forge build --sizes` / `forge inspect` need NO ffi (foundry.toml ffi=false only blocks in-test vm.ffi — per the 319-04 note). src="contracts", out="forge-out". -->
<!-- The CURRENT contract under the ceiling: DegenerusGame (the runtime size to keep < 24,576). -->
<!-- ARCH-04 reclaim targets (re-measure each; doc figures are STALE): -->
- claimAffiliateDgnrs — contracts/DegenerusGame.sol:1553 (doc: 1,283B; → BingoModule, zero-risk void, no `view` issue — the clean big win, FIRST)
- playerActivityScore — contracts/DegenerusGame.sol:2676 (doc: 953B; → lens/drop-`view`; a delegatecall stub can't be `view`; precedent DeityBoonViewer.sol)
- previewSellFarFutureTickets — contracts/DegenerusGame.sol:2113 (doc: 383B; → lens/drop-`view`)
- reserve: decClaimable / getTickets / getDailyHeroWinner (doc: ~650B)
<!-- The stubs need ~1.5KB (doc); the doc's "realistic clean reclaim ≈ 2.8KB" must be re-derived. -->
<!-- §6 gas scorecard levers (PLAN-V55 §6, vs v54 baseline): -->
- Box-ledger → warm Sub-stamp + no boxPlayers.push: ~120k / afking box-buy (GAS-01, structural to IMPL relocation)
- afkingSnapshot/afkingFundingOf cross-contract staticcalls → SLOAD: ~3-5k / sub (GAS-02, structural)
- Same-slot affiliate/pool aggregate flushes across a process batch: ~0.6-1.2M / 50-sub batch (GAS-03 SAFE-WITH-CONDITIONS)
- claimablePool/prizePoolsPacked accumulate-and-flush per batch: ~2.9k × (N−1) (GAS-03)
<!-- GAS-03 SAFE-WITH-CONDITIONS: bucket affiliate payout by roll-winner is safe; do NOT batch quests.handleAffiliate (non-linear completion logic). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure code-size + author 348-CODE-SIZE-PLAN.md with a running-total edit-order proving < 24576 at every step</name>
  <files>.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-CODE-SIZE-PLAN.md</files>
  <read_first>
    - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-CONTEXT.md (D-348-08 measure-and-verify mandate; <canonical_refs> the reclaim-target list + the "CLI forge needs no ffi" note)
    - .planning/PLAN-V55-AFKING-IN-GAME-REDESIGN.md (§1 "Code-size: the Game has 218B of headroom" + §7 "Code-size plan (sequenced)" — the STALE figures to re-derive; the GameAfkingModule split — its bytecode is its OWN budget, not the Game's; the dispatch-stub additions to the Game proper ≈1-1.5KB)
    - contracts/DegenerusGame.sol (the three reclaim-target function bodies at :1553 / :2113 / :2676 — read each to size it; note that `claimAffiliateDgnrs` is a candidate to MOVE to BingoModule, and the two read-aggregators must drop `view` or move to a lens because a delegatecall stub can't be `view`)
    - foundry.toml (confirm ffi=false, src="contracts", out="forge-out" — and that this does NOT block CLI `forge build --sizes`)
  </read_first>
  <action>
    Run `forge build --sizes` (it needs no ffi). Record the CURRENT DegenerusGame runtime (deployed) code-size in bytes and the real headroom vs the 24,576 EIP-170 ceiling — flag explicitly whether the doc's "218B headroom" is accurate or stale. Then measure/estimate each EXISTING reclaim target's contribution: use `forge inspect DegenerusGame methodIdentifiers`/the function-body read to estimate `claimAffiliateDgnrs` (:1553), `playerActivityScore` (:2676), `previewSellFarFutureTickets` (:2113), and the reserve set (decClaimable/getTickets/getDailyHeroWinner) — re-derive the doc's per-symbol bytes + the "~2.8KB realistic clean reclaim" total rather than copying. ESTIMATE the new dispatch-stub additions to the Game proper (the code is not written — state the estimation basis: ~N stubs × ~M bytes/stub, citing the doc's ≈1-1.5KB and the existing stub-shaped functions as the per-stub reference). Author `348-CODE-SIZE-PLAN.md` with: (a) a measured baseline section (current size, headroom, the doc-figure delta); (b) a per-target reclaim table (Target | Site | Measured/Estimated bytes | Reclaim mechanism [move-to-BingoModule / drop-`view` / →lens] | Risk); (c) the SEQUENCED edit-order map as a table with a RUNNING-TOTAL column — start from the measured current size, apply reclaim FIRST (claimAffiliateDgnrs→BingoModule, the zero-risk void), then add the stubs, then drop-`view`/→lens as needed — with EVERY row's running total shown to be < 24,576, so the ceiling is never breached mid-flight. State clearly that the 349 build is the final verification and that a delegatecall stub cannot be `view` (precedent `DeityBoonViewer.sol`). If `forge build --sizes` surfaces that the real headroom differs materially from 218B, make the edit-order reflect the MEASURED number, not the doc's.
  </action>
  <acceptance_criteria>
    - 348-CODE-SIZE-PLAN.md exists and records the CURRENT measured DegenerusGame runtime code-size + the real headroom vs 24,576 (from `forge build --sizes`), and states whether the doc's 218B figure is accurate or stale
    - A per-target reclaim table is present with measured/estimated bytes for claimAffiliateDgnrs, playerActivityScore, previewSellFarFutureTickets (the doc's ~2.8KB re-derived, not copied)
    - The edit-order map is a table with a RUNNING-TOTAL column, and EVERY row's running total is < 24576 (reclaim FIRST → add stubs → lens/drop-view); the literal string "24576" or "24,576" appears
    - The doc states the new stub additions are ESTIMATED (code not written) with the estimation basis, and notes a delegatecall stub cannot be `view`
    - No contracts/*.sol modified: `git diff --name-only -- contracts/` is empty
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-CODE-SIZE-PLAN.md; test -f "$F" && grep -qE "24576|24,576" "$F" && grep -qiE "forge build --sizes|forge --sizes|--sizes" "$F" && grep -qiE "running.total|running total" "$F" && grep -qE "claimAffiliateDgnrs" "$F" && grep -qiE "estimate|estimated" "$F" && [ -z "$(git diff --name-only -- contracts/)" ]</automated>
  </verify>
  <done>348-CODE-SIZE-PLAN.md records the measured current Game size + real headroom, a per-target reclaim table with re-derived bytes, and a sequenced edit-order map with a running-total column proving < 24,576 at every intermediate step (reclaim FIRST). Zero contracts/ edits.</done>
</task>

<task type="auto">
  <name>Task 2: Run /gas-scavenger + author 348-GAS-INVENTORY.md (advisory candidate list + §6 levers + GAS-03 SAFE-WITH-CONDITIONS)</name>
  <files>.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-GAS-INVENTORY.md</files>
  <read_first>
    - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-CONTEXT.md (D-348-09 — run /gas-scavenger at SPEC as advisory; the §6 scorecard levers; the GAS-03 SAFE-WITH-CONDITIONS list; "GAS-01/02 structural to the IMPL relocation")
    - .planning/PLAN-V55-AFKING-IN-GAME-REDESIGN.md (§6 "Gas scorecard (cumulative, vs v54 baseline)" — the lever table with the savings figures; §2 "What this saves per afking box-buy"; §8 "Affiliate aggregate-flush conditions")
    - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-GAS-INVENTORY.md (the precedent format: an advisory candidate list, each tagged ADVISORY/UNVALIDATED, the /gas-skeptic-is-the-only-gate framing, the feedback_security_over_gas note)
    - contracts/AfKing.sol (the current afkingSnapshot/afkingFundingOf cross-contract read shape the relocation removes; the per-sub process loop that the same-slot flushes target)
    - contracts/modules/DegenerusGameLootboxModule.sol (the cold box-ledger SSTOREs + boxPlayers.push + enqueueBoxForAutoOpen that the warm Sub-stamp collapses — GAS-01)
  </read_first>
  <action>
    Run `/gas-scavenger` scoped to the v55 keeper/funding blast radius (the AfKing process/open path + the box-buy ledger + the affiliate/pool flushes) as an ADVISORY candidate list — no validation here (350 runs `/gas-skeptic`; this is the front-load so 349 builds wins in from the start, the Phase-343 D-02 pattern). Author `348-GAS-INVENTORY.md` with: (a) the /gas-scavenger advisory candidates, each in a table row tagged ADVISORY / UNVALIDATED with its estimated saving + the site + a one-line mechanism; (b) the §6 scorecard levers transcribed from PLAN-V55 §6 (box-ledger → warm Sub-stamp ~120k/afking box-buy; afkingSnapshot/afkingFundingOf staticcall → SLOAD ~3-5k/sub; same-slot affiliate/pool aggregate flushes ~0.6-1.2M/50-sub batch; claimablePool/prizePoolsPacked accumulate-and-flush ~2.9k×(N−1)) — and FLAG GAS-01 + GAS-02 as "structural to the IMPL relocation" (the warm Sub-stamp write + the in-context subscriber set ARE the saving; confirmed-measured at 350/351), distinct from GAS-03 which lands a residual same-slot batching change; (c) a prominently-labeled "GAS-03 SAFE-WITH-CONDITIONS" subsection recording that bucketing affiliate payout by roll-winner is SAFE, but `quests.handleAffiliate` MUST NOT be batched (non-linear completion logic) — carry this carve-out verbatim into the 350 gas phase. State at the top that every candidate is UNVALIDATED, the 350 `/gas-skeptic` is the only gate, and the security/freeze/solvency floor (`feedback_security_over_gas`) governs: any win that trades an invariant is REJECTED at 350, not here. Do NOT paste contract bodies — cite sites by `file:line` (use the 348-GREP-ATTESTATION re-pinned lines once available, or re-grep here).
  </action>
  <acceptance_criteria>
    - 348-GAS-INVENTORY.md exists and states all candidates are ADVISORY / UNVALIDATED with the 350 /gas-skeptic as the only validation gate (contains "gas-skeptic" and "ADVISORY" or "UNVALIDATED")
    - The /gas-scavenger advisory candidate list is present as a table (Candidate | Site | Est. saving | Mechanism | Status=ADVISORY)
    - The §6 scorecard levers are transcribed with GAS-01 + GAS-02 flagged "structural to the IMPL relocation" and the ~120k box-buy + ~3-5k staticcall→SLOAD figures present
    - A "GAS-03 SAFE-WITH-CONDITIONS" subsection records: bucket affiliate by roll-winner = SAFE; do NOT batch quests.handleAffiliate (non-linear)
    - No contracts/*.sol modified: `git diff --name-only -- contracts/` is empty
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-GAS-INVENTORY.md; test -f "$F" && grep -q "gas-skeptic" "$F" && grep -qiE "ADVISORY|UNVALIDATED" "$F" && grep -qiE "SAFE-WITH-CONDITIONS|SAFE WITH CONDITIONS" "$F" && grep -qi "handleAffiliate" "$F" && grep -qiE "structural" "$F" && [ -z "$(git diff --name-only -- contracts/)" ]</automated>
  </verify>
  <done>348-GAS-INVENTORY.md presents the /gas-scavenger advisory candidate list (all UNVALIDATED, 350 gates) + the §6 scorecard levers (GAS-01/02 flagged structural) + the GAS-03 SAFE-WITH-CONDITIONS carve-out (no batching of quests.handleAffiliate). Zero contracts/ edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Paper-only SPEC phase; no code ships. The threat surface is the correctness of the MEASURED code-size budget (a wrong measurement → a 349 edit-order that breaches 24,576 mid-flight → a build that won't deploy) and the discipline that gas candidates are ADVISORY-only (a candidate treated as validated here → an un-vetted gas change ships at 349/350 trading an invariant). The genuine adversarial content for a doc phase: a code-size edit-order that breaches the ceiling mid-flight is the documentation-integrity threat D-348-08 closes by MEASURING rather than trusting stale figures.

| Boundary | Description |
|----------|-------------|
| Doc-stated bytes → measured bytes | The v55 PLAN doc's 218B-headroom / per-symbol / ~2.8KB figures may be stale vs `20ca1f79`; trusting them risks a 349 build that breaches 24,576. `forge build --sizes` is the trust anchor. |
| SPEC gas candidate → IMPL/GAS application | A gas candidate recorded as if validated could ship at 349/350 trading a freeze/solvency invariant; the ADVISORY/UNVALIDATED tag + the 350 /gas-skeptic gate is the boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-348-03 | Tampering | the code-size edit-order budget | mitigate | `forge build --sizes` MEASURES the current Game runtime size + real headroom (not the doc's stale 218B); the edit-order's running-total column proves < 24,576 at every intermediate step (reclaim FIRST), so 349 never breaches the ceiling mid-flight. |
| T-348-04 | Tampering | gas candidate treated as validated | mitigate | Every gas candidate tagged ADVISORY / UNVALIDATED; the 350 `/gas-skeptic` is the only validation gate; the GAS-03 SAFE-WITH-CONDITIONS carve-out (no `quests.handleAffiliate` batching) is recorded verbatim under the `feedback_security_over_gas` floor. |
| T-348-SC | Tampering | npm/pip/cargo installs | accept | No package installs — the only CLI is `forge build --sizes` / `forge inspect` (read-only, no install) + `/gas-scavenger` (an analysis agent, no install). Paper-only Markdown output. No Package Legitimacy Audit needed. |
</threat_model>

<verification>
- 348-CODE-SIZE-PLAN.md + 348-GAS-INVENTORY.md exist in the phase directory.
- The code-size plan records the MEASURED current Game size + a running-total edit-order < 24,576 at every step.
- The gas inventory presents an ADVISORY-only candidate list (350 gates) + the §6 levers + the GAS-03 SAFE-WITH-CONDITIONS carve-out.
- No contracts/*.sol modified: `git diff --name-only -- contracts/` is empty.
</verification>

<success_criteria>
- Both docs satisfy their Task acceptance criteria (ROADMAP Phase 348 Success Criterion 4 [code-size] + the gas-inventory slice of Criterion 5).
- ARCH-04 reclaim plan is MEASURED + sequenced; the gas inventory is produced as advisory.
- Zero contracts/*.sol edits.
</success_criteria>

<output>
Create `.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-02-SUMMARY.md` when done
</output>
