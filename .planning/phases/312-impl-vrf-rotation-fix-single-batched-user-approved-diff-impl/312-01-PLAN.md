---
phase: 312-impl-vrf-rotation-fix-single-batched-user-approved-diff-impl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/modules/DegenerusGameAdvanceModule.sol
autonomous: false
requirements: [VRF-01, VRF-02, VRF-03, VRF-04, VRF-05]

must_haves:
  truths:
    - "forge build exits 0 with no new errors against the patched tree (ROADMAP SC-1)"
    - "After an emergency rotation while a mid-day request is in flight, lootboxRngWordByIndex[N] is guaranteed a real VRF-derived word via the re-issued mid-day request landing at rawFulfillRandomWords:1772 into the same preserved LR_INDEX slot — no same-day entropy-0 path (VRF-01, ROADMAP SC-2)"
    - "Post-rotation the daily-drain advance gate, requestLootboxRng, and retryLootboxRng all stay reachable — re-issue fills [N] (so :269 is false) or rngWordCurrent (so :271 is false); no permanent revert / freeze (VRF-02, ROADMAP SC-3)"
    - "No VRF-participating slot is mutated mid-window in a freeze-breaking way (the old word is abandoned by the :1761 requestId guard, the new word is unpredictable, admin rotation is EXEMPT-class); wireVrf reverts on a second wire via the init-only address(0) guard; the vault/admin-routed dispatch terminates at the AdvanceModule impls where the guards sit (VRF-03/04/05, ROADMAP SC-4)"
    - "Exactly ONE unified diff touching ONLY contracts/modules/DegenerusGameAdvanceModule.sol is presented for USER approval; the SPEC anchors are re-grepped before patching; no partial contract commit occurs (ROADMAP SC-5)"
  artifacts:
    - path: "contracts/modules/DegenerusGameAdvanceModule.sol"
      provides: "_setVrfConfig (internal) + _requestVrfWord (private) helpers; wireVrf init-only lock routed through _setVrfConfig; updateVrfCoordinatorAndSub 3-case preserve+re-issue rework"
      contains: "function _setVrfConfig(address"
  key_links:
    - from: "updateVrfCoordinatorAndSub mid-day branch"
      to: "rawFulfillRandomWords:1772 (lootboxRngWordByIndex[index])"
      via: "_requestVrfWord(VRF_MIDDAY_CONFIRMATIONS) sets fresh vrfRequestId; LR_INDEX preserved so the new word lands in the same slot N"
      pattern: "_requestVrfWord\\(VRF_MIDDAY_CONFIRMATIONS\\)"
    - from: "updateVrfCoordinatorAndSub daily branch"
      to: "rawFulfillRandomWords:1768 (rngWordCurrent)"
      via: "_requestVrfWord(VRF_REQUEST_CONFIRMATIONS) when rngWordCurrent==0 (VER-02 short-circuit)"
      pattern: "_requestVrfWord\\(VRF_REQUEST_CONFIRMATIONS\\)"
    - from: "wireVrf"
      to: "init-only seal"
      via: "address(vrfCoordinator) != address(0) reverts E()"
      pattern: "address\\(vrfCoordinator\\) != address\\(0\\)"
---

<objective>
Apply the LOCKED Phase 311 SPEC + Phase 312 RESEARCH section-3 patch manifest as a single batched USER-APPROVED diff to `contracts/modules/DegenerusGameAdvanceModule.sol` — and ONLY that file. Four edits, all spec-locked and source-quoted in RESEARCH section 3, close VRF-01..05:

1. `_requestVrfWord(uint16 confirmations) private returns (uint256 id)` — re-issue helper for the NEW rotation branches only (RESEARCH 3.1, D-06).
2. `_setVrfConfig(address coord, uint256 sub, bytes32 key) internal` — shared 3-SSTORE config write (RESEARCH 3.2, D-04/D-07).
3. `wireVrf` init-only lock + route its config write through `_setVrfConfig` (RESEARCH 3.3, D-03, closes VRF-04).
4. `updateVrfCoordinatorAndSub` rework: config write to `_setVrfConfig`; blanket reset + LR_MID_DAY=0 clear to the verbatim 3-case preserve+re-issue branch (RESEARCH 3.4, D-01/D-02, closes VRF-01/02/03/05).

This is a SPEC-LOCKED EXECUTION phase. DO NOT redesign. RESEARCH section 3 already resolved every open question (VER-01..04, zero drift, no scope expansion). Read RESEARCH 3.1/3.2/3.3/3.4 for the literal target code; quote its line numbers and signatures — do not re-derive them.

Purpose: Close the CATASTROPHE-class VRF-rotation orphan-index liveness defect (Scenario A entropy-0 traits + Scenario B ~120-day freeze) while preserving the rngLock freeze invariant under rotation, in one atomic reviewable diff.
Output: A patched `DegenerusGameAdvanceModule.sol` that builds green, presented as ONE unified diff for a single USER approval that gates the lone contract commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/312-impl-vrf-rotation-fix-single-batched-user-approved-diff-impl/312-CONTEXT.md

# THE patch manifest — section 3 holds the verbatim target code for all 4 edits; section 1 the re-grepped anchor table; section 2 the VER resolutions. CONSUME section 3 DIRECTLY.
@.planning/phases/312-impl-vrf-rotation-fix-single-batched-user-approved-diff-impl/312-RESEARCH.md

# THE locked design — section 2 fix shape (D-01/D-02), section 3 freeze-invariant disposition (VRF-03; validator-entropy REJECTED), section 4 wireVrf lock + _setVrfConfig dedup + vault reach (D-03/D-04/VRF-04/VRF-05).
@.planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md

<interfaces>
<!-- Confirmed live at HEAD bce6e243 (re-grepped during planning; matches RESEARCH section 1 — zero drift). The executor MUST re-grep before patching per VER-04 and feedback_verify_call_graph_against_source, but these are the contracts to build against — no codebase exploration needed. -->

Constants (DegenerusGameAdvanceModule.sol):
  :122  uint16 private constant VRF_REQUEST_CONFIRMATIONS = 10;
  :123  uint16 private constant VRF_MIDDAY_CONFIRMATIONS = 4;
  (also fixed in every VRFRandomWordsRequest: callbackGasLimit VRF_CALLBACK_GAS_LIMIT, numWords 1, extraArgs hex"")

Inherited from DegenerusGameStorage.sol (call directly — already in scope via inheritance):
  :210  error E();                          // the generic admin/guard revert — do NOT invent a new error
  :1352 function _lrRead(uint256 shift, uint256 mask) internal view returns (uint256)
  :1357 function _lrWrite(uint256 shift, uint256 mask, uint256 value) internal
  LR_MID_DAY_SHIFT = 224, LR_MID_DAY_MASK = 0xFF   // _lrRead(LR_MID_DAY_SHIFT, LR_MID_DAY_MASK) != 0 means mid-day in flight
  bool internal rngLockedFlag;                       // read directly: if (rngLockedFlag)
  uint256 internal rngWordCurrent;                   // VER-02 short-circuit read
  uint256 internal vrfRequestId;                     // set fresh on re-issue
  uint48 internal rngRequestTime;                    // set fresh = uint48(block.timestamp)
  IVRFCoordinator internal vrfCoordinator; uint256 internal vrfSubscriptionId; bytes32 internal vrfKeyHash;

wireVrf (:498) current body — config writes :506-508, lastVrfProcessedTimestamp :509 STAYS inline, emit :510 STAYS inline (uses local current from :505):
  address current = address(vrfCoordinator);     // :505 PRESERVE
  vrfCoordinator = IVRFCoordinator(coordinator_); // :506 to _setVrfConfig(coordinator_, subId, keyHash_)
  vrfSubscriptionId = subId;                      // :507
  vrfKeyHash = keyHash_;                          // :508
  lastVrfProcessedTimestamp = uint48(block.timestamp); // :509 PRESERVE inline (NOT absorbed)
  emit VrfCoordinatorUpdated(current, coordinator_);   // :510 PRESERVE inline (NOT absorbed)

updateVrfCoordinatorAndSub (:1688) current body — see RESEARCH 3.4 for the verbatim :1693-1716 quote and the REPLACE/PRESERVE table.

rawFulfillRandomWords (:1756) — REUSED UNCHANGED by re-issue:
  :1761 if (requestId != vrfRequestId || rngWordCurrent != 0) return;  // abandons the dead old coordinator's callback
  :1766 if (rngLockedFlag) { rngWordCurrent = word; }                  // :1768 daily write target
  :1769 } else { ... lootboxRngWordByIndex[index] = word; }            // :1772 mid-day write target (index = LR_INDEX-1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add _requestVrfWord + _setVrfConfig helpers (NEW code, no existing call sites changed)</name>
  <files>contracts/modules/DegenerusGameAdvanceModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameAdvanceModule.sol — the file being edited. Read the four inline VRFRandomWordsRequest struct sites (:1102-1111, :1143 onward, :1587-1596, :1605 onward) to confirm they are byte-identical modulo requestConfirmations, and the wireVrf/updateVrf config-write blocks (:506-508 and :1696-1698).
    - .planning/phases/312-impl-vrf-rotation-fix-single-batched-user-approved-diff-impl/312-RESEARCH.md section 3.1 (the verbatim _requestVrfWord signature + body) and 3.2 (the verbatim _setVrfConfig signature + body, plus the explicit MUST-stay-outside list).
  </read_first>
  <action>
    FIRST, discharge VER-04: re-grep the SPEC section 0 / RESEARCH section 1 anchors against current HEAD (run git rev-parse HEAD, expect bce6e243 or a docs-only descendant where git log of the base to HEAD over contracts/ is empty). Confirm the 4 requestRandomWords sites at :1102/:1143/:1587/:1605, the 2 config-write blocks, and that _setVrfConfig and _requestVrfWord are absent. If any contract line drifted, STOP and surface it — do not patch against stale lines.

    Add _requestVrfWord(uint16 confirmations) private returns (uint256 id) exactly per RESEARCH 3.1 — it builds the single VRFRandomWordsRequest with keyHash vrfKeyHash, subId vrfSubscriptionId, requestConfirmations confirmations, callbackGasLimit VRF_CALLBACK_GAS_LIMIT, numWords 1, extraArgs hex"", and returns vrfCoordinator.requestRandomWords(...). Visibility private (matches the existing _requestRng/_tryRequestRng at :1585/:1600 per D-06 Discretion + RESEARCH 3.1). The confirmations param is uint16 (matches both constants + the struct field).

    Add _setVrfConfig(address coord, uint256 sub, bytes32 key) internal exactly per RESEARCH 3.2 — the three SSTOREs vrfCoordinator = IVRFCoordinator(coord); vrfSubscriptionId = sub; vrfKeyHash = key; in that order. Visibility internal (D-07, SPEC 4.2 verbatim).

    DO NOT touch the 4 existing inline requestRandomWords sites — they stay inline (D-06; full retrofit DEFERRED). DO NOT route any existing call site through either helper in this task (wireVrf/updateVrf wiring is Tasks 2-3). DO NOT add a LINK-balance precheck inside _requestVrfWord (VER-01: the rotation re-issue fires pre-funding and is fulfilled at fund-time in the same _executeSwap tx; a precheck would brick rotation).

    Comments describe what IS, never what changed (feedback_no_history_in_comments) — no "previously", "was", or "now uses".
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | grep -iE 'error\[|^Error' | grep -v unsafe-typecast | head; echo EXIT $?</automated>
  </verify>
  <acceptance_criteria>
    - forge build exits 0 with no new error lines (lint warning unsafe-typecast notes are pre-existing and acceptable, not errors).
    - grep -c "function _setVrfConfig(address coord, uint256 sub, bytes32 key) internal" on the file equals 1.
    - grep -c "function _requestVrfWord(uint16 confirmations) private returns (uint256 id)" on the file equals 1.
    - grep -c "VRFRandomWordsRequest({" on the file equals 5 (the 4 untouched inline sites + the 1 new helper).
    - grep -c "requestRandomWords(" on the file equals 5 (4 inline + 1 in _requestVrfWord).
    - git diff --stat shows ONLY contracts/modules/DegenerusGameAdvanceModule.sol changed.
    - No "linkBal < MIN_LINK_FOR_LOOTBOX_RNG" line appears inside the new _requestVrfWord body.
  </acceptance_criteria>
  <done>Both helpers exist with the exact RESEARCH 3.1/3.2 signatures; build green; no existing call site rewired; only the one file changed.</done>
</task>

<task type="auto">
  <name>Task 2: wireVrf init-only lock + route config write through _setVrfConfig (VRF-04)</name>
  <files>contracts/modules/DegenerusGameAdvanceModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameAdvanceModule.sol — wireVrf at :498 (admin guard :503, local current :505, config writes :506-508, lastVrfProcessedTimestamp :509, emit :510).
    - .planning/phases/312-impl-vrf-rotation-fix-single-batched-user-approved-diff-impl/312-RESEARCH.md section 3.3 (the verbatim init-only lock line + the E() inheritance note + correctness rationale).
    - .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md section 4 (D-03/D-04/VRF-04 + the 0.Y dispatch trace establishing wireVrf is structurally a single constructor call).
  </read_first>
  <action>
    Add the init-only lock as the first guard in wireVrf (top of function, before or immediately after the existing :503 admin guard — RESEARCH 3.3 implies top-of-function): if (address(vrfCoordinator) != address(0)) revert E();. Use the inherited E() from DegenerusGameStorage:210 — do NOT invent a new error. vrfCoordinator is address(0) until the single constructor-driven wire (DegenerusAdmin:458 to DegenerusGame:308 to impl :498), so this permits exactly one call and reverts every subsequent wire. No new storage slot.

    Route wireVrf's 3-SSTORE config write (the current :506-508) through _setVrfConfig(coordinator_, subId, keyHash_).

    PRESERVE inline, NOT absorbed into the helper: the address current = address(vrfCoordinator); read (:505 — must read the OLD coordinator before _setVrfConfig overwrites it; here it is address(0) on the only legal call, but keep the structure), lastVrfProcessedTimestamp = uint48(block.timestamp); (:509), and emit VrfCoordinatorUpdated(current, coordinator_); (:510). Per RESEARCH 3.2 the emit + timestamp stay in the caller.

    Comments describe what IS (feedback_no_history_in_comments).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | grep -iE 'error\[|^Error' | grep -v unsafe-typecast | head; echo EXIT $?</automated>
  </verify>
  <acceptance_criteria>
    - forge build exits 0 with no new error lines.
    - grep -c "if (address(vrfCoordinator) != address(0)) revert E();" on the file equals 1, and that line is inside wireVrf.
    - wireVrf no longer contains the inline vrfCoordinator = IVRFCoordinator(coordinator_); 3-SSTORE block; it calls _setVrfConfig(coordinator_, subId, keyHash_) instead (grep -c on that call equals 1).
    - wireVrf still contains lastVrfProcessedTimestamp = uint48(block.timestamp); and emit VrfCoordinatorUpdated(current, coordinator_); inline.
    - No new error declaration was added to the contract.
    - git diff --stat shows ONLY contracts/modules/DegenerusGameAdvanceModule.sol changed.
  </acceptance_criteria>
  <done>wireVrf reverts with E() on a second wire and routes its config write through _setVrfConfig; timestamp + emit stay inline; build green; one file.</done>
</task>

<task type="auto">
  <name>Task 3: updateVrfCoordinatorAndSub 3-case preserve+re-issue rework (VRF-01/02/03/05)</name>
  <files>contracts/modules/DegenerusGameAdvanceModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameAdvanceModule.sol — updateVrfCoordinatorAndSub :1688 (admin guard :1693, current read :1695, config writes :1696-1698, blanket reset :1701-1704, LR_MID_DAY=0 clear + comment :1706-1709, totalFlipReversals comment :1711-1714, emit :1716) AND rawFulfillRandomWords :1756-1776 (the :1761 guard + :1768 daily / :1772 mid-day write targets the re-issue reuses unchanged).
    - .planning/phases/312-impl-vrf-rotation-fix-single-batched-user-approved-diff-impl/312-RESEARCH.md section 3.4 (the verbatim REPLACE/PRESERVE table + the literal 3-case branch) AND 3.5 (flag-read mechanics) AND the VER-01/VER-02/VER-03 resolutions in section 2 (pre-funding re-issue ok, rngWordCurrent==0 daily short-circuit, mid-day-first precedence).
    - .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md section 2 (D-01/D-02 fix shape) + section 3 (freeze-invariant disposition — old word abandoned, new word unpredictable, NO validator-influenceable entropy).
  </read_first>
  <action>
    Rework updateVrfCoordinatorAndSub per the RESEARCH 3.4 REPLACE/PRESERVE table:

    PRESERVE UNTOUCHED: the :1693 admin guard; the :1695 address current = address(vrfCoordinator); read (reads OLD coordinator before the config write); the :1711-1714 totalFlipReversals "Intentional: ... NOT reset here" comment block; the :1716 emit VrfCoordinatorUpdated(current, newCoordinator);.

    REPLACE the :1696-1698 config write with _setVrfConfig(newCoordinator, newSubId, newKeyHash);.

    REPLACE the :1700-1704 blanket reset (rngLockedFlag = false; vrfRequestId = 0; rngRequestTime = 0; rngWordCurrent = 0;) AND REMOVE the :1706-1709 mid-day-clear comment + _lrWrite(LR_MID_DAY_SHIFT, LR_MID_DAY_MASK, 0); — with the verbatim 3-case preserve+re-issue branch from RESEARCH 3.4:
      - Case mid-day FIRST: if (_lrRead(LR_MID_DAY_SHIFT, LR_MID_DAY_MASK) != 0) then KEEP LR_MID_DAY=1 (no write), set vrfRequestId = _requestVrfWord(VRF_MIDDAY_CONFIRMATIONS); rngRequestTime = uint48(block.timestamp);. LR_INDEX is untouched so the new word lands in the same orphaned slot N via rawFulfillRandomWords:1772. (This is the VRF-01 close.)
      - Case daily: else if (rngLockedFlag) then KEEP rngLockedFlag=true (no write); VER-02 short-circuit: if (rngWordCurrent == 0) { vrfRequestId = _requestVrfWord(VRF_REQUEST_CONFIRMATIONS); rngRequestTime = uint48(block.timestamp); } — else preserve the already-delivered word and do NOT re-issue / do NOT reset vrfRequestId or rngRequestTime (a fresh callback would be rejected by the :1761 rngWordCurrent!=0 guard).
      - Case nothing in flight: implicit else then config repoint only; no re-issue, no flag change.

    Mid-day-first precedence per VER-03 (defensive ordering; the flags are mutually exclusive in observed cross-tx state, proven by the :1045 + :1056 requestLootboxRng gates). rngRequestTime is uint48 (Storage:244) so cast uint48(block.timestamp).

    Comments describe what IS, never what changed (feedback_no_history_in_comments) — describe the pre-funding re-issue + retryLootboxRng failsafe posture as a present-tense fact per RESEARCH 3.4's comment guidance; no "previously cleared" or "used to reset".

    Net result: NO LINK precheck on re-issue (VER-01); NO blanket reset; NO LR_MID_DAY=0 clear; the totalFlipReversals block and emit intact.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | grep -iE 'error\[|^Error' | grep -v unsafe-typecast | head; echo EXIT $?</automated>
  </verify>
  <acceptance_criteria>
    - forge build exits 0 with no new error lines.
    - updateVrfCoordinatorAndSub routes config through _setVrfConfig(newCoordinator, newSubId, newKeyHash) (grep -c on that call equals 1).
    - The blanket reset is gone: the "// Reset RNG state to allow immediate advancement" comment is removed (grep -c on that string equals 0); the four-line rngLockedFlag=false / vrfRequestId=0 / rngRequestTime=0 / rngWordCurrent=0 block no longer appears inside the function.
    - The rotation mid-day clear is removed: the _lrWrite(LR_MID_DAY_SHIFT, LR_MID_DAY_MASK, 0) previously at :1709 no longer appears inside updateVrfCoordinatorAndSub. (The SET-to-1 at :1097 and the same-day CLEAR at :225 elsewhere are untouched.)
    - Both re-issue calls present: grep -c "_requestVrfWord(VRF_MIDDAY_CONFIRMATIONS)" equals 1 and grep -c "_requestVrfWord(VRF_REQUEST_CONFIRMATIONS)" equals 1.
    - The VER-02 short-circuit guard present inside the daily branch: grep -c "if (rngWordCurrent == 0)" is at least 1.
    - The totalFlipReversals comment block is PRESERVED: grep -c "Intentional: totalFlipReversals is NOT reset here" equals 1.
    - The emit is PRESERVED: grep -c "emit VrfCoordinatorUpdated(current, newCoordinator);" equals 1.
    - No LINK precheck added to the rotation path: grep -c "linkBal < MIN_LINK_FOR_LOOTBOX_RNG" equals 1 (only the pre-existing one in retryLootboxRng).
    - No history-narrating comment words: grep -niE "previously|used to|was reset|formerly" on the file returns 0 matches.
    - git diff --stat shows ONLY contracts/modules/DegenerusGameAdvanceModule.sol changed.
  </acceptance_criteria>
  <done>updateVrfCoordinatorAndSub repoints config via _setVrfConfig and detect-preserve-re-issues per the 3-case branch (mid-day-first, daily VER-02 short-circuit, nothing-in-flight no-op); totalFlipReversals + emit preserved; no blanket reset, no LR_MID_DAY clear, no LINK precheck; build green; one file.</done>
</task>

<task type="auto">
  <name>Task 4: Build-green gate + full-diff self-review against RESEARCH section 3 + scope/anchor re-grep</name>
  <files>contracts/modules/DegenerusGameAdvanceModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameAdvanceModule.sol — read the full unified diff (git diff on the file) and the patched wireVrf + updateVrfCoordinatorAndSub regions end-to-end.
    - .planning/phases/312-impl-vrf-rotation-fix-single-batched-user-approved-diff-impl/312-RESEARCH.md section 3 (compare the diff line-by-line to the 3.1/3.2/3.3/3.4 target shapes) + section 4 (Scope-Expansion Flags: NONE — confirm the diff touches only AdvanceModule.sol).
  </read_first>
  <action>
    This is a verification-only task — make NO functional edits unless the self-review surfaces a deviation from RESEARCH section 3 (in which case fix it and re-run, staying inside AdvanceModule.sol).

    1. Run forge build and assert exit 0 / no new error lines.
    2. Run the full anchor + scope re-grep battery (the verify block below) and confirm every value.
    3. Self-review the unified diff against RESEARCH 3.1/3.2/3.3/3.4: confirm helper signatures verbatim, the wireVrf lock + route-through, the updateVrf 3-case branch (mid-day-first, daily rngWordCurrent==0 short-circuit, nothing-in-flight no-op), and the PRESERVE set (admin guards, current reads, lastVrfProcessedTimestamp, totalFlipReversals comment, both emits, the 4 inline VRFRandomWordsRequest sites).
    4. Confirm NO second file is touched. If any non-AdvanceModule.sol edit appears necessary, STOP and surface it as a SCOPE EXPANSION requiring explicit USER approval — do not make it.

    Do NOT commit. The single contract commit is gated by the Task 5 USER-APPROVAL checkpoint.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && F=contracts/modules/DegenerusGameAdvanceModule.sol; forge build >/dev/null 2>&1 && echo build=PASS || echo build=FAIL; echo setVrfConfig=$(grep -c 'function _setVrfConfig(address coord, uint256 sub, bytes32 key) internal' $F); echo requestVrfWord=$(grep -c 'function _requestVrfWord(uint16 confirmations) private returns (uint256 id)' $F); echo wireLock=$(grep -c 'address(vrfCoordinator) != address(0)) revert E()' $F); echo structs=$(grep -c 'VRFRandomWordsRequest({' $F); echo midReissue=$(grep -c '_requestVrfWord(VRF_MIDDAY_CONFIRMATIONS)' $F); echo dailyReissue=$(grep -c '_requestVrfWord(VRF_REQUEST_CONFIRMATIONS)' $F); echo blanketComment=$(grep -c 'Reset RNG state to allow immediate advancement' $F); echo flipReversals=$(grep -c 'Intentional: totalFlipReversals is NOT reset here' $F); echo historyWords=$(grep -niE 'previously|used to|was reset|formerly' $F | wc -l); echo filesChanged=$(git diff --name-only | tr '\n' ' ')</automated>
  </verify>
  <acceptance_criteria>
    - build=PASS.
    - setVrfConfig=1, requestVrfWord=1, wireLock=1.
    - structs=5 (4 inline + 1 helper), midReissue=1, dailyReissue=1.
    - blanketComment=0 (blanket reset comment removed), flipReversals=1 (totalFlipReversals comment preserved), historyWords=0.
    - filesChanged is exactly "contracts/modules/DegenerusGameAdvanceModule.sol" — NO other file.
    - The diff matches RESEARCH 3.1/3.2/3.3/3.4 shape (self-review attested in the SUMMARY).
  </acceptance_criteria>
  <done>Build green, every anchor/scope assertion passes, the diff matches RESEARCH section 3 exactly, and only AdvanceModule.sol is touched — ready to present for approval.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 5: USER-APPROVAL checkpoint — present the ONE complete diff, build-green, HALT for explicit approval before the single commit</name>
  <files>contracts/modules/DegenerusGameAdvanceModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameAdvanceModule.sol — the patched file (read the full diff to present it accurately).
    - .planning/phases/312-impl-vrf-rotation-fix-single-batched-user-approved-diff-impl/312-CONTEXT.md (the batched-diff posture: ONE approval, no partial commits, no pre-approval — feedback_batch_contract_approval + feedback_never_preapprove_contracts + feedback_no_contract_commits + feedback_manual_review_before_push).
  </read_first>
  <action>
    Present the patched diff and HALT. Make NO code edits and NO commit in this task. Procedure:
    1. Run git diff on contracts/modules/DegenerusGameAdvanceModule.sol and present the COMPLETE unified diff to the user. State that git diff --stat lists ONLY that one file (no DegenerusAdmin.sol / MintModule.sol / Storage.sol / test/ change; no new storage slot).
    2. Run forge build and report the exit status — assert it exits 0 with no new error lines (the pre-existing unsafe-typecast lint warnings are not errors and are unchanged).
    3. Summarize each of the 4 edits against its RESEARCH anchor (3.1 _requestVrfWord, 3.2 _setVrfConfig, 3.3 wireVrf lock, 3.4 updateVrf 3-case) and confirm the PRESERVE set is intact (admin guards, current reads, lastVrfProcessedTimestamp, totalFlipReversals comment, both emits, the 4 untouched inline VRFRandomWordsRequest sites).
    4. State that NO commit has been made and that the single commit will use the CONTRACTS_COMMIT_APPROVED=1 bypass ONLY after explicit approval (the PreToolUse hook blocks all commits while contracts/*.sol is dirty).
    5. HALT for explicit USER approval. Do NOT pre-approve, do NOT assume approval, do NOT advance (feedback_never_preapprove_contracts). Only after the user types approval: make the single contract commit with CONTRACTS_COMMIT_APPROVED=1, then write the SUMMARY.
  </action>
  <verify>
    <human-check>User has seen the complete one-file diff plus forge build exit-0 evidence and explicitly approved before any commit is made.</human-check>
  </verify>
  <what-built>
    All four RESEARCH section-3 edits applied to contracts/modules/DegenerusGameAdvanceModule.sol and ONLY that file: the _requestVrfWord(uint16) private helper, the _setVrfConfig internal helper, the wireVrf init-only revert E() lock routed through _setVrfConfig, and the updateVrfCoordinatorAndSub 3-case preserve+re-issue rework (mid-day-first, daily rngWordCurrent==0 short-circuit, nothing-in-flight no-op). Closes VRF-01 (orphaned [N] gets a real word), VRF-02 (post-rotation liveness), VRF-03 (freeze invariant intact — old word abandoned, new word unpredictable, validator-entropy rejected), VRF-04 (wireVrf one-shot lock), VRF-05 (vault/admin-routed reach covered at the impl).
  </what-built>
  <how-to-verify>
    1. Present the COMPLETE unified diff: run `git diff contracts/modules/DegenerusGameAdvanceModule.sol` and show it in full. State that `git diff --stat` lists ONLY that one file (no DegenerusAdmin.sol, no MintModule.sol, no Storage.sol, no test/ change, no new storage slot).
    2. Run `forge build` in the repo root and report the exit status — assert it exits 0 with no new error lines (the pre-existing unsafe-typecast lint warnings on DegeneretteModule are not errors and are unchanged).
    3. Summarize each of the 4 edits against its RESEARCH anchor (3.1 helper, 3.2 helper, 3.3 wireVrf lock, 3.4 updateVrf 3-case) and confirm the PRESERVE set is intact (admin guards, current reads, lastVrfProcessedTimestamp, totalFlipReversals comment, both emits, the 4 untouched inline VRFRandomWordsRequest sites).
    4. State that NO commit has been made yet and that the single commit will use the CONTRACTS_COMMIT_APPROVED=1 bypass ONLY after explicit approval (the PreToolUse hook blocks all commits while contracts/*.sol is dirty).
    5. HALT. Do not commit, do not advance, do not assume approval. Wait for the user's explicit word.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the single batched commit of contracts/modules/DegenerusGameAdvanceModule.sol, or describe the changes you want before approval.</resume-signal>
  <done>The user has reviewed the complete one-file diff with build-green evidence and explicitly approved (or requested changes). Only after explicit approval does the executor make the single contract commit (CONTRACTS_COMMIT_APPROVED=1 bypass), then write the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chainlink VRF coordinator to AdvanceModule (`rawFulfillRandomWords:1756`) | Untrusted external callback delivers the random word; gated by `msg.sender == vrfCoordinator` (:1760) and the `requestId == vrfRequestId` match (:1761). |
| Admin governance (DegenerusAdmin vault-owner-gated) to AdvanceModule VRF admin fns | The emergency-rotation entry (`updateVrfCoordinatorAndSub`) and the one-shot wire (`wireVrf`); admin-gated at the impl (:503 / :1693), downstream of every wrapper. Admin rotation is an EXEMPT-class operation vs PLAYERS per v45-vrf-freeze-invariant. |
| Players to the VRF-derived outputs (`lootboxRngWordByIndex[N]`, `rngWordCurrent`) | Players consume traits/words derived from VRF; they MUST NOT be able to influence an in-flight word or read a predictable (entropy-0) one. This is the rngLock freeze surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-312-01 | Tampering / Information Disclosure (freeze invariant) | The re-issued in-flight VRF word during rotation (`updateVrfCoordinatorAndSub` to `rawFulfillRandomWords`) | mitigate | The OLD in-flight word is structurally abandoned: the dead old coordinator's late callback carries the OLD requestId and is rejected by the `:1761` guard (`requestId != vrfRequestId`), and `rngWordCurrent != 0` blocks a double-write. The NEW word is requested from the new coordinator and is unpredictable at re-issue time (does not exist yet). The slots feeding its consumption (LR_INDEX, the pre-swapped ticket buffer) are frozen across the window — rotation never touches LR_INDEX. Admin rotation is EXEMPT-class vs PLAYERS (v45-vrf-freeze-invariant); no player-discretionary write enters any VRF-participating slot. Consumed-this-cycle is the fresh word, not a buffered-for-next value. Closes **VRF-03** (SPEC section 3, VER-03). |
| T-312-02 | Information Disclosure (entropy-0 / predictable traits) | `MintModule:686` reads `lootboxRngWordByIndex[N]` unguarded (Scenario A consumer; NOT modified per D-08) | mitigate | The mid-day re-issue branch lands a real VRF word into the SAME slot [N] (LR_INDEX preserved) via `rawFulfillRandomWords:1772` BEFORE any same-day advance can drain the read slot. The orphan-at-zero is eliminated STRUCTURALLY (the slot is filled), not by adding a runtime zero-guard at the consumer. Closes **VRF-01** (SPEC section 2.3 / 3.4). |
| T-312-03 | Denial of Service (permanent revert / ~120-day freeze) | Post-rotation advance-flow drain gate (`:213`/`:271`), `requestLootboxRng`, `retryLootboxRng` | mitigate | Re-issue keeps the in-flight flag and supplies the word: in the mid-day case `:1772` fills [N] so the `:269` `lootboxRngWordByIndex[preIdx]==0` test is false; in the daily case `:1768` fills rngWordCurrent so the `:271` `cw==0` test is false. No LINK precheck on the re-issue (VER-01) — the request is accepted pre-funding and fulfilled at fund-time in the same `_executeSwap` tx; a precheck would have reverted the whole rotation. `retryLootboxRng` (`:1133`, >= MIDDAY_RNG_RETRY_TIMEOUT) remains the standing failsafe if the NEW coordinator also stalls (recoverable, never permanently stuck). Closes **VRF-02** (SPEC section 2.3, VER-01). |
| T-312-04 | Elevation of Privilege / Tampering (re-wire of live VRF config) | A second `wireVrf` call after deployment repointing the coordinator/sub/keyHash | mitigate | `wireVrf` gains an init-only lock `if (address(vrfCoordinator) != address(0)) revert E();`. vrfCoordinator is address(0) only until the single constructor-driven wire; every subsequent wire reverts. `updateVrfCoordinatorAndSub` (admin-gated, governance-stall-gated) becomes the SOLE post-init VRF-config mutator. Closes **VRF-04** (SPEC section 4.1, D-03). |
| T-312-05 | Elevation of Privilege (wrapper-bypass of the guards) | The vault/admin-routed dispatch to both VRF admin fns (DegenerusAdmin to DegenerusGame delegatecall to AdvanceModule impl) | mitigate | Both routed reaches (DegenerusAdmin vault-owner-gated wrapper + DegenerusGame selector delegatecall) terminate at the SAME two AdvanceModule implementations (`:498` / `:1688`) where the admin guard + init-only lock + safe-rotation sit — downstream of every wrapper, so no wrapper can bypass them. There is no DegenerusVault-routed path (SPEC section 0.Y reconciled the naming drift). Verified by backward-trace. Closes **VRF-05** (SPEC section 4, ADMA-02). |
| T-312-06 | Tampering (validator/proposer-influenceable entropy) | Any temptation to backfill [N] from block.timestamp / newKeyHash / blockhash / caller-supplied value | accept-by-rejection | EXPLICITLY REJECTED per feedback_security_over_gas / SPEC section 3.3. The fix introduces NO entropy backfill — it re-issues to get a genuine VRF word into [N] directly. No validator-influenceable source enters any VRF-derived path. (No code; the threat is closed by the design choosing re-issue over any synthetic fill.) |
| T-312-SC | Tampering | npm/pip/cargo installs | n/a | NO package installs in this phase. The diff is a pure Solidity edit to one existing file; no dependency is added, no package manager runs. RESEARCH section 4 confirms no new import. Slopcheck not applicable. |

**No HIGH/CATASTROPHE threat is left unmitigated.** The CATASTROPHE this phase exists to close (the orphan-index liveness defect — Scenario A entropy-0 + Scenario B freeze) is mitigated by T-312-01/02/03; the one-shot-lock and vault-reach hardening by T-312-04/05; validator-entropy is rejected by T-312-06.
</threat_model>

<verification>
Phase-level checks (the executor runs these across the plan; the Task 5 checkpoint presents them to the user):

- `forge build` exits 0 with no new error lines (pre-existing unsafe-typecast lint warnings unchanged).
- `git diff --stat` shows ONLY `contracts/modules/DegenerusGameAdvanceModule.sol` changed — no second contract, no test/, no new storage slot.
- The 4 edits match RESEARCH 3.1/3.2/3.3/3.4 verbatim (helper signatures, wireVrf lock + route-through, updateVrf 3-case branch).
- The PRESERVE set is intact: admin guards (:503/:1693), `current` reads (:505/:1695), `lastVrfProcessedTimestamp` (:509), the totalFlipReversals comment (:1711-1714), both `VrfCoordinatorUpdated` emits, and the 4 untouched inline VRFRandomWordsRequest sites.
- No history-narrating comments (`previously|used to|was reset|formerly` = 0).
- No commit made before explicit USER approval (Task 5 gate); the single commit uses CONTRACTS_COMMIT_APPROVED=1 only after approval.
</verification>

<success_criteria>
Maps to the 5 ROADMAP Phase 312 success criteria:

1. `forge build` PASS against the patched contract tree.
2. After an emergency rotation while a mid-day request is in flight, `lootboxRngWordByIndex[N]` is guaranteed a real VRF-derived word (mid-day re-issue lands it at :1772 into the preserved LR_INDEX slot); no path leaves it at 0 for a same-day advance (VRF-01).
3. Post-rotation, `requestLootboxRng` / `retryLootboxRng` / the daily-drain advance gate remain reachable; no permanent revert / freeze (VRF-02).
4. No VRF-participating slot is mutated mid-rngLock in a freeze-breaking way (old word abandoned, new word unpredictable, admin EXEMPT); `wireVrf` reverts on a second wire (VRF-04); vault-routed reach covered at the impl (VRF-05) — VRF-03/04/05.
5. The USER-APPROVED diff is committed exactly once (Task 5 gate, CONTRACTS_COMMIT_APPROVED=1 bypass post-approval); every cited file:line was re-grepped pre-patch (VER-04, Task 1).
</success_criteria>

<output>
Create `.planning/phases/312-impl-vrf-rotation-fix-single-batched-user-approved-diff-impl/312-01-SUMMARY.md` when done. The SUMMARY MUST record: the 4 edits applied (with the RESEARCH section-3 anchor each maps to), the VER-04 re-grep result at the patch HEAD, the build-green attestation, the single-file scope attestation, the self-review-against-RESEARCH attestation, the USER-approval evidence, and the single commit SHA.
</output>
