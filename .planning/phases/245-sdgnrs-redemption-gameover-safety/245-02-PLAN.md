---
phase: 245-sdgnrs-redemption-gameover-safety
phase_number: 245
plan: 245-02
plan_number: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v31-245-GOE.md
  - audit/v31-245-SDR-GOE.md
autonomous: true
requirements: [GOE-01, GOE-02, GOE-03, GOE-04, GOE-05, GOE-06]
requirements_addressed: [GOE-01, GOE-02, GOE-03, GOE-04, GOE-05, GOE-06]
baseline: 7ab515fe
baseline_sha: 7ab515fe
head: cc68bfc7
head_sha: cc68bfc7
head_anchor: cc68bfc7
in_scope_commits:
  - sha: 771893d1
    summary: "feat(gameover): shift purchase/claim gates to liveness and protect sDGNRS redemptions — primary GOE driver"
    drives_reqs: [GOE-01, GOE-02, GOE-03, GOE-04, GOE-05, GOE-06]
  - sha: cc68bfc7
    summary: "feat(baf): gate BAF jackpot on daily flip win — BAF-coupling addendum; GOE-06 candidate 1 (skipped-BAF-pool × drain interaction)"
    drives_reqs: [GOE-06]
in_scope_243_rows:
  changelog:    [D-243-C012, D-243-C015, D-243-C016, D-243-C017, D-243-C018, D-243-C019, D-243-C020, D-243-C021, D-243-C022, D-243-C023, D-243-C024, D-243-C025, D-243-C026, D-243-C028, D-243-C031, D-243-C032, D-243-C035, D-243-C036, D-243-C037, D-243-C038, D-243-C039]
  classification: [D-243-F010, D-243-F013, D-243-F014, D-243-F015, D-243-F016, D-243-F017, D-243-F018, D-243-F019, D-243-F020, D-243-F021, D-243-F022, D-243-F023, D-243-F024, D-243-F025, D-243-F026]
  call_sites:   [D-243-X017, D-243-X018, D-243-X019, D-243-X026, D-243-X027, D-243-X028, D-243-X029, D-243-X030, D-243-X031, D-243-X032, D-243-X033, D-243-X034, D-243-X035, D-243-X036, D-243-X037, D-243-X038, D-243-X039, D-243-X040, D-243-X041, D-243-X042, D-243-X043, D-243-X044, D-243-X045, D-243-X046, D-243-X047, D-243-X048, D-243-X049, D-243-X050, D-243-X051, D-243-X052, D-243-X053, D-243-X054, D-243-X056, D-243-X057, D-243-X058, D-243-X059, D-243-X060]
  consumer_index: [D-243-I016, D-243-I017, D-243-I018, D-243-I019, D-243-I020, D-243-I021]
  source_244_v_rows: ["GOX-01-V01..V08 (8-path entry-gate shift — primary for GOE-03 foundation + GOE-05 BURNIE-gate ordering)", "GOX-03-V01..V03 (handleGameOverDrain subtraction — primary for GOE-02 33/33/34 split)", "GOX-04-V01/V02 (_livenessTriggered + EXC-02 RE_VERIFIED_AT_HEAD — shared for GOE-04 deeper stall-tail + KI re-verify)", "GOX-06-V02 (_handleGameOverPath ordering — shared for GOE-04 stall-tail)", "GOX-06-V03 (cc68bfc7 jackpots direct-handle reentrancy parity — shared for GOE-06 candidate 1)", "RNG-01-V11 (EXC-03 envelope non-widening — shared for GOE-01 F-29-04 RE_VERIFIED_AT_HEAD)"]
ki_envelope_re_verify:
  - exception: "EXC-02 (Gameover prevrandao fallback via _getHistoricalRngFallback)"
    re_verify_in_req: GOE-04
    rationale: "Phase 244 GOX-04-V02 already RE_VERIFIED_AT_HEAD cc68bfc7 that the 14-day grace adds a TRIGGER for liveness (NOT a new prevrandao-consumption path); Phase 245 GOE-04 DEEPER re-verify covers the multi-level day-range × VRF-state × rngLockedFlag matrix and confirms EXC-02 envelope still unchanged under that deeper enumeration"
  - exception: "EXC-03 (Gameover RNG substitution for mid-cycle write-buffer tickets — F-29-04 class)"
    re_verify_in_req: GOE-01
    rationale: "Phase 244 RNG-01-V11 RE_VERIFIED EXC-03 envelope at the _unlockRng removal scope; Phase 245 GOE-01 owns the DEEPER 14-day-grace × F-29-04 interaction check (does Tier-1 grace gate at Storage:1242 introduce a NEW way for mid-cycle ticket-buffer swap to trigger _gameOverEntropy consumption?). Cross-cite SDR-08 for the new redemption-resolve-consumption site within the same envelope."
source_preflag_bullets: "audit/v31-244-PER-COMMIT-AUDIT.md L2470-2521 §Phase-245-Pre-Flag bullets grouped under GOE-01 (1 bullet L2503), GOE-02 (1 bullet L2506), GOE-03 (1 bullet L2509), GOE-04 (1 bullet L2512), GOE-05 (1 bullet L2515), GOE-06 (2 bullets L2518-L2519). 7 bullets total for GOE bucket. Consumed as ADVISORY input per CONTEXT.md D-25 — each bullet gets a one-line closure note in the relevant §GOE-NN cross-walk."
consolidation_responsibility: "245-02 owns final consolidation per CONTEXT.md D-04 + D-05 — Task 4 assembles audit/v31-245-SDR-GOE.md from 2 working files (v31-245-SDR.md + v31-245-GOE.md) + §Consumer Index + §Reproduction Recipe Appendix + §Phase-246-Input subsection + flips it FINAL READ-only at SUMMARY commit"
phase_246_input_responsibility: "245-02 Task 4 emits §Phase-246-Input subsection per CONTEXT.md D-18 — bullet-list of finding candidates (any verdict ≠ SAFE/RE_VERIFIED_AT_HEAD) from BOTH bucket files with format: `- FIND-01 candidate | SEVERITY: <bucket> | <one-line observation> | <file:line> | <source REQ> | <suggested Phase 246 action>`. If zero candidates surface, subsection explicitly states the zero state."
must_haves:
  truths:
    - "audit/v31-245-GOE.md exists with frontmatter `Audit baseline: 7ab515fe`, `Audit head: cc68bfc7`, `Owning commits: 771893d1 + cc68bfc7 (BAF-coupling addendum per GOE-06 candidate 1)`, `Scope per: audit/v31-243-DELTA-SURFACE.md §6 D-243-I016..D-243-I021 + audit/v31-244-PER-COMMIT-AUDIT.md §4 GOX shared-context V-rows + §Phase-245-Pre-Flag L2502-2519 (GOE-grouped bullets)`."
    - "Section §GOE-01 RE_VERIFIES F-29-04 RNG-consumer determinism envelope at HEAD cc68bfc7: (a) enumerates day-14-to-day-120 scenarios where Tier-1 14-day grace fires liveness BUT day-math threshold unmet; (b) confirms no NEW way for mid-cycle ticket-buffer swap to trigger `_gameOverEntropy` consumption; (c) cross-cites Phase 244 RNG-01-V11 as PRIMARY non-widening proof; (d) cross-cites SDR-08 for the new redemption-resolve-consumption site; (e) annotation `RE_VERIFIED_AT_HEAD cc68bfc7` per CONTEXT.md D-24."
    - "Section §GOE-02 RE_VERIFIES v24.0 claimablePool 33/33/34 split + 30-day sweep at HEAD cc68bfc7: (a) confirms _sendToVault split math at GameOverModule:225-233 operates on L158 `available` AFTER L86-90 + L94 + L157 pendingRedemptionEthValue subtractions; (b) handleFinalSweep at L196-216 30-day window preserves reserved wei when claimRedemption fires mid-window; (c) cross-cites Phase 244 GOX-03-V01/V02 primary closure + v24.0 claimablePool spec + Phase 239/240 GO-240 rows."
    - "Section §GOE-03 RE_VERIFIES purchase-blocking entry-point coverage at current surface — DEEPER sweep beyond Phase 244 GOX-01 (which closed the 8-path gate-shift claim): (a) full sweep of externally-callable functions in Game contracts + 5 modules (DegenerusGame.sol + MintModule + WhaleModule + AdvanceModule + GameOverModule + JackpotModule); (b) each function classified as (i) has _livenessTriggered / gameOver gate, OR (ii) state-read-only, OR (iii) admin-only with safe state mutation; (c) cross-cites GOX-01-V01..V08 primary closure of the 8-path claim + extends to any non-8-path entry-point."
    - "Section §GOE-04 RE_VERIFIES VRF-available vs prevrandao-fallback gameover-jackpot branches under new 14-day VRF-dead grace: (a) 4-dimensional matrix {day range (1-14, 14-120, 120-365, 365+), level (0, 1-9, 10+), VRF state (healthy, stalled < grace, stalled >= grace, intermittent), rngLockedFlag state} with every cell verdict; (b) VRF-available vs prevrandao-fallback branch disjointness at the new 14-day grace; (c) cross-cites Phase 244 GOX-04-V02 EXC-02 RE_VERIFIED_AT_HEAD primary; 245 GOE-04 owns the DEEPER stall-tail enumeration per Pre-Flag L2512; (d) annotation `RE_VERIFIED_AT_HEAD cc68bfc7` for EXC-02 envelope under the deeper matrix."
    - "Section §GOE-05 RE_VERIFIES `gameOverPossible` BURNIE endgame gate (v11.0) across all new liveness paths: (a) L890 livenessTriggered gate fires BEFORE L894 gameOverPossible gate at _purchaseCoinFor MintModule:885-911; (b) enumerates all paths to a BURNIE ticket purchase to ensure the gate remains effective; (c) cross-cites v11.0 BURNIE gate spec + Phase 244 GOX-01-V01 entry-gate enumeration."
    - "Section §GOE-06 closes the 2 Phase-244-Pre-Flag candidates per CONTEXT.md D-12: (1) cc68bfc7 BAF skipped-pool preservation in futurePool × handleGameOverDrain subtraction interaction — does the skipped-BAF pool get correctly swept by drain, or stranded?; (2) burnWrapped divergence (livenessTriggered() && !gameOver() at sDGNRS:507) — DGNRS wrapper ↔ sDGNRS wrapper-held backing conservation across State-0/1/2 transitions. If either candidate escalates >= MEDIUM, sweep expands in-place per CONTEXT.md D-13 (bounded to the specific cross-feature triple touched by the escalated candidate)."
    - "Every verdict row carries 8 columns per CONTEXT.md D-06: `Verdict Row ID | REQ-ID | Source Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA`."
    - "Verdict bucket per row drawn from {SAFE, INFO, LOW, MEDIUM, HIGH, CRITICAL} per CONTEXT.md D-08; KI envelope rows use `RE_VERIFIED_AT_HEAD cc68bfc7` annotation NOT a verdict bucket per CONTEXT.md D-24."
    - "Verdict Row IDs follow the `GOE-NN-V##` per-REQ-monotonic scheme per CONTEXT.md D-06."
    - "REQ-summary table aggregates per-REQ closure: every REQ in {GOE-01..GOE-06} appears at least once as a Verdict Row ID prefix; per-REQ severity floor stated."
    - "All D-243 row subsets from D-243-I016..D-243-I021 are cited at least once in verdict-table Source Row(s) cells."
    - "Phase 244 §Phase-245-Pre-Flag bullets 11-17 (GOE-grouped at v31-244-PER-COMMIT-AUDIT.md L2502-2519 — 7 bullets across GOE-01..GOE-06 including 2 bullets for GOE-06) each receive a one-line closure note in the relevant §GOE-NN cross-walk per CONTEXT.md D-25."
    - "audit/v31-245-SDR-GOE.md exists at SUMMARY-commit time with 4 sections per CONTEXT.md D-04: §1 SDR (verbatim embed of audit/v31-245-SDR.md from 245-01), §2 GOE (verbatim embed of audit/v31-245-GOE.md from this plan), §3 Consumer Index (REQ-ID → Phase 245 verdict-row mapping + cross-ref to D-243-X/F/C/S rows from v31-243-DELTA-SURFACE.md §6 + Phase 244 V-row cross-ref + prior-milestone artifacts per CONTEXT.md D-17), §4 Reproduction Recipe Appendix (all git show -L / grep / reach-path commands POSIX-portable per CONTEXT.md D-22)."
    - "audit/v31-245-SDR-GOE.md is flipped FINAL READ-only at SUMMARY commit per CONTEXT.md D-05 (frontmatter `status: FINAL — READ-ONLY`); the 2 working files (v31-245-SDR.md + v31-245-GOE.md) remain as appendices on disk."
    - "audit/v31-245-SDR-GOE.md includes a §Phase-246-Input subsection per CONTEXT.md D-18 — bullet-list of finding candidates with required fields (FIND-01 placeholder + SEVERITY + one-line observation + file:line + source REQ + suggested Phase 246 action); if zero candidates surfaced the subsection explicitly states the zero state."
    - "Reproduction Recipe sub-sections in audit/v31-245-GOE.md AND in §4 of audit/v31-245-SDR-GOE.md list POSIX-portable commands used."
    - "Zero F-31-NN finding-IDs emitted (CONTEXT.md D-23 — Phase 246 FIND-01 owns ID assignment); finding candidates use prose blocks with `SEVERITY: <bucket>` annotation only."
    - "Zero contracts/ or test/ writes per CONTEXT.md D-20 + feedback_no_contract_commits.md + feedback_never_preapprove_contracts.md."
    - "Zero edits to audit/v31-243-DELTA-SURFACE.md or audit/v31-244-PER-COMMIT-AUDIT.md per CONTEXT.md D-22."
  artifacts:
    - path: "audit/v31-245-GOE.md"
      provides: "GOE bucket (REQ GOE-01..GOE-06) per-REQ verdict tables + 2 GOE-06 Phase-244-Pre-Flag candidate closures + finding-candidate prose blocks. Working file consumed by this plan's Task 4 consolidation."
      contains: "## §GOE-01"
    - path: "audit/v31-245-SDR-GOE.md"
      provides: "Final consolidated Phase 245 deliverable per CONTEXT.md D-04 + D-05; assembled from 2 working files at SUMMARY commit + Consumer Index + Reproduction Recipe + Phase 246 Input subsection + flipped FINAL READ-only."
      contains: "## §1 — SDR Bucket"
  key_links:
    - from: "audit/v31-243-DELTA-SURFACE.md §6 D-243-I016..D-243-I021"
      to: "audit/v31-245-GOE.md §GOE-01..§GOE-06 verdict-table cells"
      via: "Source Row(s) column citation per CONTEXT.md D-06"
      pattern: "D-243-(C|F|X|S)[0-9]{3}"
    - from: "audit/v31-244-PER-COMMIT-AUDIT.md §4 GOX V-rows (GOX-01-V01..V08, GOX-03-V01..V03, GOX-04-V01/V02, GOX-06-V02/V03) + §2 RNG-01-V11"
      to: "audit/v31-245-GOE.md verdict-row Source Row(s) cells for shared-context cross-cites"
      via: "Phase 244 V-row cross-cite per CONTEXT.md D-17"
      pattern: "GOX-0[1-6]-V[0-9]+|RNG-01-V11"
    - from: "contracts/storage/DegenerusGameStorage.sol at HEAD cc68bfc7 (_livenessTriggered L1235-1243 + _VRF_GRACE_PERIOD L200-203) + contracts/modules/DegenerusGameAdvanceModule.sol at HEAD cc68bfc7 (_handleGameOverPath L519-630 + _gameOverEntropy L1228-1306 + cc68bfc7 BAF gate L826-839) + contracts/modules/DegenerusGameGameOverModule.sol at HEAD cc68bfc7 (handleGameOverDrain L79-189 + _sendToVault L225-233 + handleFinalSweep L196-216) + contracts/modules/DegenerusGameMintModule.sol at HEAD cc68bfc7 (_purchaseCoinFor L885-911 + gameOverPossible L894) + contracts/StakedDegenerusStonk.sol at HEAD cc68bfc7 (burnWrapped L506-516) + contracts/DegenerusStonk.sol at HEAD cc68bfc7 (wrapper-backing) + contracts/DegenerusJackpots.sol at HEAD cc68bfc7 (markBafSkipped + futurePool preservation)"
      to: "audit/v31-245-GOE.md verdict Evidence cells"
      via: "F-29-04 envelope RE_VERIFY + 33/33/34 split + entry-point sweep + VRF × prevrandao branch matrix + BURNIE gate enumeration + 2 Pre-Flag candidate closures"
      pattern: "_livenessTriggered|_VRF_GRACE_PERIOD|_gameOverEntropy|handleGameOverDrain|_sendToVault|handleFinalSweep|gameOverPossible|markBafSkipped|burnWrapped"
    - from: "audit/v31-245-SDR.md (from 245-01) + audit/v31-245-GOE.md (from this plan)"
      to: "audit/v31-245-SDR-GOE.md (final consolidated deliverable)"
      via: "Task 4 consolidation per CONTEXT.md D-04 + D-05 (245-02 owns)"
      pattern: "## §[0-9]+ — (SDR|GOE) Bucket"
---

<objective>
Execute the adversarial RE_VERIFICATION of every pre-existing gameover invariant (v24.0 / v29.0 / v11.0 / F-29-04) against the `771893d1` delta + `cc68bfc7` BAF-coupling addendum. Close all 6 GOE requirements (GOE-01..GOE-06) with per-REQ RE_VERIFIED_AT_HEAD verdict tables + 2 GOE-06 Phase-244-Pre-Flag candidate closures + finding-candidate prose blocks in working file `audit/v31-245-GOE.md`.

Then perform the FINAL CONSOLIDATION step per CONTEXT.md D-04 + D-05: assemble `audit/v31-245-SDR-GOE.md` from the 2 bucket working files (audit/v31-245-SDR.md from 245-01 + audit/v31-245-GOE.md from this plan), append §Consumer Index + §Reproduction Recipe Appendix + §Phase-246-Input subsection, and flip the file FINAL READ-only at SUMMARY commit time.

Scope sources (READ-only per CONTEXT.md D-20 + D-22):
- `audit/v31-243-DELTA-SURFACE.md` §6 Consumer Index rows D-243-I016..D-243-I021
- `audit/v31-244-PER-COMMIT-AUDIT.md` §4 GOX V-rows (GOX-01, GOX-03, GOX-04, GOX-06) + §2 RNG-01-V11 as shared-context
- `audit/v31-244-PER-COMMIT-AUDIT.md` §Phase-245-Pre-Flag L2502-2519 bullets 11-17 (GOE-grouped, 7 bullets) as ADVISORY input per CONTEXT.md D-25
- `audit/KNOWN-ISSUES.md` EXC-02 + EXC-03 entries for GOE-01 + GOE-04 envelope RE_VERIFIED_AT_HEAD annotations per CONTEXT.md D-24

Per CONTEXT.md D-12 (user auto-decided): GOE-06 depth = close the 2 Phase-244-Pre-Flag candidates only (skipped-BAF-pool × drain; DGNRS wrapper ↔ sDGNRS wrapper-held-backing conservation). Exhaustive sweep is deferred unless either candidate escalates >= MEDIUM per CONTEXT.md D-13.

Per CONTEXT.md D-24: KI EXC-02 envelope RE_VERIFIED in §GOE-04; KI EXC-03 envelope RE_VERIFIED in §GOE-01 (DEEPER 14-day grace × F-29-04 interaction) + cross-cite to 245-01 §SDR-08 (NEW redemption-resolve-consumption site).

Per CONTEXT.md D-23: Zero F-31-NN finding-IDs emitted — Phase 246 FIND-01 owns ID assignment.

Per CONTEXT.md D-21: HEAD anchor `cc68bfc7` locked. Verify `git diff cc68bfc7..HEAD -- contracts/` returns empty before each task.

Per CONTEXT.md D-18: Task 4 emits §Phase-246-Input subsection with finding-candidate aggregation across both bucket files.

Output: `audit/v31-245-GOE.md` is the GOE working file. `audit/v31-245-SDR-GOE.md` is the final consolidated deliverable assembled at Task 4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/245-sdgnrs-redemption-gameover-safety/245-CONTEXT.md
@.planning/phases/244-per-commit-adversarial-audit-evt-rng-qst-gox/244-04-SUMMARY.md
@audit/v31-243-DELTA-SURFACE.md
@audit/v31-244-PER-COMMIT-AUDIT.md
@KNOWN-ISSUES.md
</context>

<threat_model>
Per CONTEXT.md D-16 (GOE adversarial vectors — fixed, planner MUST cover at minimum). Actor taxonomy carries from Phase 238 D-07 (player / admin / validator / VRF oracle). This plan is READ-only — zero new contract attack surface introduced; the threat model below enumerates the vectors the AUDIT must close on, NOT new code to ship.

**Trust boundaries (within GOE scope):**

| Boundary | Description |
|---|---|
| `_livenessTriggered (Storage:1235-1243) Tier-1 14-day VRF-dead grace × F-29-04 envelope` | new grace-gate trigger — must NOT widen EXC-03 mid-cycle-substitution envelope under any day-14-to-day-120 scenario |
| `handleGameOverDrain → _sendToVault 33/33/34 split at GameOverModule:225-233` | v24.0 split invariant must be preserved after NEW L94 + L157 pendingRedemptionEthValue subtractions |
| `handleFinalSweep 30-day post-gameOver window at GameOverModule:196-216` | sweeps totalFunds WITHOUT re-subtracting pendingRedemptionEthValue — relies on claimRedemption having fired before window closes |
| `Every externally-callable function in Game contracts + 5 modules → _livenessTriggered / gameOver gate` | updated from v24.0's "10 entry points" — current surface must be enumerated and verified |
| `_gameOverEntropy VRF-available branch (L1237-1259) vs prevrandao-fallback branch (L1263-1293) disjointness under 14-day grace` | branches must be mutually exclusive per gameover tick; EXC-02 envelope must not widen |
| `_purchaseCoinFor L890 livenessTriggered BEFORE L894 gameOverPossible — BURNIE ticket ordering` | ordering prevents State-1 caller from bypassing gameOverPossible gate |
| `cc68bfc7 BAF skipped-pool preservation in DegenerusJackpots.futurePool × handleGameOverDrain subtraction` | GOE-06 candidate 1 — does the skipped-BAF pool get swept, or stranded? |
| `burnWrapped livenessTriggered() && !gameOver() divergence × DGNRS wrapper supply ↔ sDGNRS wrapper-held backing` | GOE-06 candidate 2 — wrapper conservation across State-0/1/2 transitions |

**STRIDE-mapped per-REQ vectors (transcribed from CONTEXT.md D-16):**

| Threat ID | REQ | Category | Component | Disposition | Mitigation Plan / Adversarial Vector |
|---|---|---|---|---|---|
| T-245-GOE-01a | GOE-01 | KI envelope (EXC-03) | F-29-04 RNG-consumer determinism envelope at HEAD cc68bfc7 | RE_VERIFY only per D-24 | Read KI EXC-03 acceptance rationale; verify 4-criterion envelope (terminal-state + no-player-exploit + bounded-tx + VRF-derived-entropy) still holds at HEAD. |
| T-245-GOE-01b | GOE-01 | Tampering | 14-day Tier-1 grace gate at Storage:1242 × mid-cycle ticket-buffer swap | mitigate-by-audit | Enumerate day-14-to-day-120 scenarios where Tier-1 gate fires liveness but day-math unmet — does this introduce a NEW way for mid-cycle swap to trigger `_gameOverEntropy` consumption? Cross-cite Phase 244 RNG-01-V11 (PRIMARY) + 245-01 SDR-08 (new redemption-resolve-consumption site in same envelope). |
| T-245-GOE-02a | GOE-02 | Tampering | 33/33/34 split at _sendToVault GameOverModule:225-233 operates on L158 `available` AFTER L86-90 + L94 + L157 subtractions | mitigate-by-audit | Read _sendToVault body; verify split input is post-subtraction `available`, not pre-subtraction `totalFunds`. Cross-cite Phase 244 GOX-03-V01/V02 primary. |
| T-245-GOE-02b | GOE-02 | Tampering (starvation) | handleFinalSweep 30-day window at L196-216 sufficiency for all pending claimants | mitigate-by-audit | Verify window is CLOCK-based (not tamperable); realistic-actor model (30-day UX recovery standard); no scenario leaves reserved wei stranded after window closes except user-abandon-wallet. |
| T-245-GOE-03a | GOE-03 | Tampering / Elevation-of-Privilege | Full sweep of externally-callable functions in Game + 5 modules | mitigate-by-audit | Enumerate every external function in DegenerusGame.sol + MintModule + WhaleModule + AdvanceModule + GameOverModule + JackpotModule; classify (i) has _livenessTriggered/gameOver gate, (ii) state-read-only, (iii) admin-only safe. Cross-cite Phase 244 GOX-01-V01..V08 primary for the 8 player-facing paths. |
| T-245-GOE-03b | GOE-03 | Tampering | Entry-points NOT in 8 — internal callees (_purchaseBurnieLootboxFor, _claim*), admin entries | mitigate-by-audit | Extend GOX-01 coverage to all externally-reachable code paths; verify no path allows ETH injection after liveness gate fires. |
| T-245-GOE-04a | GOE-04 | KI envelope (EXC-02) | Matrix of {day range × level × VRF state × rngLockedFlag state} with every cell verdict | RE_VERIFY only per D-24 | 4-dim matrix enumeration; each cell closes with disjointness-of-branches verdict. Cross-cite Phase 244 GOX-04-V02 primary (14-day grace adds trigger, not new prevrandao path). |
| T-245-GOE-04b | GOE-04 | Tampering | Multi-level transitions where VRF comes back partially | mitigate-by-audit | Per Pre-Flag L2512: enumerate VRF-breaks-at-day-14 scenarios + multi-level transitions; verify no stall-tail configuration bypasses EXC-02 envelope. |
| T-245-GOE-05a | GOE-05 | Tampering / Elevation-of-Privilege | gameOverPossible BURNIE gate at _purchaseCoinFor MintModule:894 | mitigate-by-audit | Verify L890 livenessTriggered gate fires BEFORE L894 gameOverPossible; State-1 callers rejected at L890 before reaching L894; enumerate all paths to BURNIE ticket purchase to confirm gate remains effective. |
| T-245-GOE-06a | GOE-06 | Tampering | cc68bfc7 BAF skipped-pool preservation in futurePool × handleGameOverDrain subtraction interaction | mitigate-by-audit | GOE-06 candidate 1: does the skipped-BAF pool get correctly swept by drain, or stranded? Read DegenerusJackpots.sol futurePool write pattern + GameOverModule handleGameOverDrain sweep pattern. |
| T-245-GOE-06b | GOE-06 | Tampering | burnWrapped livenessTriggered() && !gameOver() divergence × DGNRS wrapper ↔ sDGNRS wrapper-held backing conservation across State-0/1/2 | mitigate-by-audit | GOE-06 candidate 2: verify State-1 player retains DGNRS wrapper tokens for post-gameOver deterministic-burn; no wrapper supply mismatches sDGNRS wrapper-held backing. |

**Acceptance disposition rule:** Each row above MUST close with one of {SAFE, INFO, LOW, MEDIUM, HIGH, CRITICAL} per CONTEXT.md D-08, OR `RE_VERIFIED_AT_HEAD cc68bfc7` for KI envelope rows (GOE-01a + GOE-04a). GOE-06 candidate escalation rule per CONTEXT.md D-13: if either 06a/06b verdict is >= MEDIUM, sweep expands in-place bounded to the specific cross-feature triple touched.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: GOE-01 F-29-04 envelope under 14-day grace (RE_VERIFIED_AT_HEAD EXC-03) + GOE-02 claimablePool 33/33/34 split + 30-day sweep (RE_VERIFIED v24.0) — close GOE-01 + GOE-02</name>
  <files>audit/v31-245-GOE.md</files>
  <read_first>
    - .planning/phases/245-sdgnrs-redemption-gameover-safety/245-CONTEXT.md — D-01 (2-plan split), D-02 (single-wave parallel), D-04 (single consolidated deliverable), D-05 (per-plan working file + consolidation in 245-02), D-06 (8-column verdict format), D-08 (6-bucket severity), D-16 GOE-01 + GOE-02 adversarial vectors, D-17 (RE_VERIFIED_AT_HEAD methodology + corroborating prior-milestone artifacts), D-24 (KI exception RE_VERIFIED_AT_HEAD only), D-25 (Pre-Flag advisory)
    - audit/v31-243-DELTA-SURFACE.md — §1.4 commit-771893d1 changelog rows D-243-C016 (_gameOverEntropy at AdvanceModule:1216-1294), D-243-C017 (handleGameOverDrain at GameOverModule:79-189), D-243-C026 (_livenessTriggered at Storage:1235-1243), D-243-C028 (_VRF_GRACE_PERIOD = 14 days at Storage:200-203), D-243-C032 (IStakedDegenerusStonk.pendingRedemptionEthValue interface)
    - audit/v31-243-DELTA-SURFACE.md — §6 Consumer Index rows D-243-I018 (GOE-02 / GOX-03 handleGameOverDrain subtraction coverage), D-243-I019 (GOE-04 / GOX-04 liveness-trigger coverage — also relevant to GOE-01 F-29-04 envelope), D-243-I021 (GOE-01 / GOE-04 / SDR-08 gameover-entropy coverage)
    - audit/v31-244-PER-COMMIT-AUDIT.md — §4 GOX-03 V-rows V01/V02/V03 (PRIMARY closure for handleGameOverDrain subtraction — GOE-02 cross-cites); §4 GOX-04 V-rows V01/V02 (PRIMARY closure for _livenessTriggered + EXC-02 envelope — GOE-01 shares the _livenessTriggered body read); §2 RNG-01-V11 (PRIMARY non-widening proof for EXC-03 envelope at _unlockRng scope — GOE-01 cross-cites); §Phase-245-Pre-Flag bullets at L2503 (GOE-01), L2506 (GOE-02)
    - KNOWN-ISSUES.md — EXC-03 entry "Gameover RNG substitution for mid-cycle write-buffer tickets" — 4-criterion acceptance (terminal-state only; no player-reachable exploit; bounded-tx; VRF-derived-or-VRF+prevrandao)
    - contracts/storage/DegenerusGameStorage.sol at HEAD cc68bfc7:
      - L200-203 — `uint48 internal constant _VRF_GRACE_PERIOD = 14 days;`
      - L1235-1243 — `_livenessTriggered` body: L1241 `uint48 rngStart = rngRequestTime;`; L1242 `return rngStart != 0 && block.timestamp - rngStart >= _VRF_GRACE_PERIOD;`
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD cc68bfc7:
      - L1228-1306 — _gameOverEntropy body (VRF-available at L1237-1259 + prevrandao-fallback at L1263-1293); L1286 sdgnrs.resolveRedemptionPeriod call
      - L1301 — `_getHistoricalRngFallback` — the prevrandao-admixture function; verify it is the SOLE prevrandao consumption site at HEAD cc68bfc7
    - contracts/modules/DegenerusGameGameOverModule.sol at HEAD cc68bfc7:
      - L79-189 — handleGameOverDrain body: L80 GO_JACKPOT_PAID idempotency; L86-90 reserved-subtraction; L94 + L157 pendingRedemptionEthValue calls; L148 idempotency set; L186 _sendToVault call; L225-233 _sendToVault 33/33/34 split math
      - L196-216 — handleFinalSweep body: day+30 gate; L215 _sendToVault totalFunds sweep (no re-subtraction of pendingRedemptionEthValue)
    - audit/v30-GAMEOVER-JACKPOT-SAFETY.md — Phase 240 GO-240-NNN rows for GOE-04 VRF-available branch corroborating evidence (GOE-02 adjacent for 33/33/34 split)
    - .planning/milestones/v29.0-phases/232.1-rng-consumer-audit/ — F-29-04 commitment-window trace for GOE-01 envelope corroboration
    - .planning/milestones/v29.0-phases/235-trnx-rng/ — `rngLocked` 4-path walk for GOE-01 adjacent evidence
    - audit/v30-FREEZE-PROOF.md — Phase 238 19-row Gameover-Flow Freeze-Proof Subset for GOE-01 corroboration
    - v24.0 claimablePool 33/33/34 split + 30-day sweep spec (cited in PROJECT.md milestone history)
  </read_first>
  <action>
Execute GOE-01 + GOE-02 RE_VERIFICATION at HEAD cc68bfc7.

**Step A — Sanity gate (mandatory before any write):**

Run:
```
git rev-parse 7ab515fe
git rev-parse cc68bfc7
git rev-parse HEAD
git diff --stat cc68bfc7..HEAD -- contracts/
git status --porcelain contracts/ test/
```

Locked expectations: `cc68bfc7` resolves to `cc68bfc70e76fb75ac6effbc2135aae978f96ff3`; `git diff cc68bfc7..HEAD -- contracts/` reports zero changes; `git status --porcelain contracts/ test/` returns empty.

If diff non-empty, STOP and record scope-guard deferral in plan SUMMARY per CONTEXT.md D-21.

**Step B — Create file shell:**

Write `audit/v31-245-GOE.md` with:

```
# v31.0 Phase 245 — GOE Bucket Audit (commit 771893d1 + cc68bfc7 BAF-coupling addendum)

Audit baseline: 7ab515fe
Audit head:     cc68bfc7
Owning commits: 771893d1 (gameover liveness + sDGNRS redemption protection — primary GOE driver) + cc68bfc7 (BAF-coupling addendum — GOE-06 candidate 1)
Scope per:      audit/v31-243-DELTA-SURFACE.md §6 D-243-I016..D-243-I021 + audit/v31-244-PER-COMMIT-AUDIT.md §4 GOX shared-context V-rows + §Phase-245-Pre-Flag L2502-2519 (GOE-grouped bullets)
Plan:           245-02-PLAN.md
Phase:          245-sdgnrs-redemption-gameover-safety

## §0 — Per-Bucket Verdict Count Card

| REQ-ID | Verdict Rows | Finding Candidates | KI Envelope Status | Floor Severity |
| --- | --- | --- | --- | --- |
| GOE-01 | (filled at task close) | (filled) | EXC-03 RE_VERIFIED_AT_HEAD cc68bfc7 | (filled) |
| GOE-02 | (filled at task close) | (filled) | n/a | (filled) |
| GOE-03 | (filled at Task 2) | (filled) | n/a | (filled) |
| GOE-04 | (filled at Task 2) | (filled) | EXC-02 RE_VERIFIED_AT_HEAD cc68bfc7 | (filled) |
| GOE-05 | (filled at Task 2) | (filled) | n/a | (filled) |
| GOE-06 | (filled at Task 3) | (filled) | n/a | (filled per D-13) |

## §GOE-01 — F-29-04 RNG-consumer determinism envelope RE_VERIFIED_AT_HEAD cc68bfc7

## §GOE-02 — claimablePool 33/33/34 split + 30-day sweep against new drain flow RE_VERIFIED_AT_HEAD cc68bfc7

(§GOE-03..GOE-06 appended by Tasks 2 + 3.)

## §KI Envelope Re-Verify — EXC-03 (F-29-04 class) under new 14-day Tier-1 VRF-dead grace

## §Phase-244-Pre-Flag Cross-Walk — GOE bucket (7 bullets at v31-244-PER-COMMIT-AUDIT.md L2502-2519 per CONTEXT.md D-25)

(One-line closure note per bullet — appended incrementally.)

## §Reproduction Recipe — GOE bucket

(Commands appended at task close — Tasks 2 + 3 append their own too.)
```

**Step C — §GOE-01 F-29-04 envelope RE_VERIFIED_AT_HEAD (vectors GOE-01a + GOE-01b per CONTEXT.md D-16):**

**Sub-vector GOE-01a — EXC-03 acceptance RE_VERIFIED per CONTEXT.md D-24:**

Read KNOWN-ISSUES.md EXC-03 entry verbatim. 4-criterion envelope:
- (a) only reachable at gameover — terminal state; no further gameplay after 30-day window
- (b) no player-reachable exploit — gameover triggered by 120-day liveness stall OR pool deficit; neither player-timeable
- (c) gameover must drain within bounded transactions; cannot wait for deferred VRF
- (d) all substitute entropy is VRF-derived or VRF-plus-prevrandao

Verify each criterion at HEAD cc68bfc7:
- (a) `_gameOverEntropy` is reachable ONLY via `_handleGameOverPath` at AdvanceModule:560 (per D-243-X027 call-site row); `_handleGameOverPath` is gated on `_livenessTriggered()` at L551 per Phase 244 GOX-06-V02 — terminal-state gate preserved
- (b) new 14-day Tier-1 grace at Storage:1242 does NOT allow player to time gameover against mid-cycle swap state — the grace fires on VRF stall (external infrastructure), not on player action
- (c) _gameOverEntropy branches (L1237-1259 VRF-available / L1263-1293 prevrandao-fallback) both resolve within single tx; no deferred fulfillment
- (d) L1286 redemption-resolve uses `redemptionRoll` derived from VRF word (L1237 branch) OR VRF+prevrandao admixture (L1263 branch via `_getHistoricalRngFallback`); cross-cite `_getHistoricalRngFallback` body at AdvanceModule:1301

Verdict row:

| Verdict Row ID | REQ-ID | Source Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| GOE-01-V01 | GOE-01 | D-243-C016, D-243-C026, D-243-C028, EXC-03, RNG-01-V11 (primary) | contracts/modules/DegenerusGameAdvanceModule.sol:1228-1306 + contracts/storage/DegenerusGameStorage.sol:1235-1243 | GOE-01a EXC-03 4-criterion envelope RE_VERIFIED_AT_HEAD | RE_VERIFIED_AT_HEAD cc68bfc7 | 4 criteria (a)-(d) each re-verified at HEAD; cross-cite RNG-01-V11 for PRIMARY non-widening proof | 771893d1 |

**Sub-vector GOE-01b — DEEPER 14-day grace × mid-cycle-swap interaction check per CONTEXT.md D-14 GOE-01:**

Enumerate day-14-to-day-120 scenarios where Tier-1 grace fires liveness BUT day-math threshold unmet:

For each (day ∈ [14, 120), level ∈ [0, 10+], VRF state ∈ {stalled>=14 days}):
- Does Tier-1 grace fire? L1242 returns `rngStart != 0 && block.timestamp - rngStart >= 14 days` — YES when VRF stalled >=14 days regardless of day-math
- Does day-math fire? Depends on `purchaseStartDay + 120 * level` threshold — NO when day < 120 (level 0) OR day < 120 + 120*level
- When Tier-1 fires + day-math unmet: liveness fires via grace ONLY; advanceGame reaches `_handleGameOverPath`; gameOver latches; `_gameOverEntropy` fires prevrandao-fallback branch (L1263) because rngRequestTime != 0 still

Does this introduce a NEW mid-cycle ticket-buffer swap trigger?
- A mid-cycle swap is triggered by `_swapAndFreeze(purchaseLevel)` at AdvanceModule:292 (daily RNG request) OR `_swapTicketSlot(purchaseLevel_)` at AdvanceModule:1082 (mid-day lootbox RNG)
- Both swap sites are unchanged by 771893d1 — the swap triggers are identical at baseline and HEAD
- The NEW 14-day Tier-1 grace adds a new LIVENESS-FIRE trigger (VRF stall) but does NOT add a new swap trigger
- Therefore EXC-03 envelope is NOT widened by the 14-day grace; mid-cycle swap can still only be triggered by normal daily or lootbox RNG request paths

Cross-cite Phase 244 RNG-01-V11 as PRIMARY non-widening proof; 245 GOE-01 confirms the deeper enumeration preserves the envelope.

Cross-cite 245-01 SDR-08 for the NEW redemption-resolve-consumption site (L1286) within the same envelope.

Verdict row:

| Verdict Row ID | REQ-ID | Source Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| GOE-01-V02 | GOE-01 | D-243-C026, D-243-C028, Pre-Flag bullet L2503, RNG-01-V11, SDR-08-V01 (cross-file from 245-01) | contracts/storage/DegenerusGameStorage.sol:1242 + contracts/modules/DegenerusGameAdvanceModule.sol:1228-1306 | GOE-01b 14-day grace × mid-cycle-swap interaction (DEEPER enumeration) | <SAFE — RE_VERIFIED> | Day-14-to-120 × level × VRF-state enumeration shows Tier-1 grace adds liveness-fire trigger but not swap trigger; EXC-03 envelope preserved | 771893d1 |

Append `## §KI Envelope Re-Verify — EXC-03 under new 14-day Tier-1 VRF-dead grace` header (GOE-01-V01 row content above is sufficient; dedicated section header allows grep-by-reviewer across Phase 244 and Phase 245 files).

Append Pre-Flag closure note under §Phase-244-Pre-Flag Cross-Walk:
- Bullet L2503 (GOE-01 14-day grace × F-29-04 interaction) — CLOSED via GOE-01-V02 (DEEPER enumeration confirms envelope preserved).

**Step D — §GOE-02 claimablePool 33/33/34 split + 30-day sweep RE_VERIFIED_AT_HEAD (vectors GOE-02a + GOE-02b per CONTEXT.md D-16):**

**Sub-vector GOE-02a — 33/33/34 split operates on post-subtraction `available` per D-14 GOE-02:**

Read `_sendToVault` body GameOverModule:225-233:
- Identify the split math: 3 shares (33% / 33% / 34%)
- Verify the input parameter is `available` (post-L86-90 + L94 + L157 subtractions), NOT the raw `totalFunds`

Re-trace from handleGameOverDrain:79-189:
- L86-90: `reserved = uint256(claimablePool) + IStakedDegenerusStonk(ContractAddresses.SDGNRS).pendingRedemptionEthValue();`
- L<calc>: `available = totalFunds - reserved;` (confirm exact line)
- L94: `reserved` re-includes (for safety) pendingRedemptionEthValue (per Phase 244 GOX-03-V01 primary)
- L148: GO_JACKPOT_PAID bit set
- L157: post-refund subtract again
- L186: `_sendToVault(remaining, stBal);` — `remaining` is the post-subtraction available

Cross-cite Phase 244 GOX-03-V01/V02 as PRIMARY closure; GOE-02 extends to the v24.0 33/33/34 split invariant preservation proof.

Verify v24.0 33/33/34 split: the 33% goes to BURNIE buyback, 33% to stETH vault, 34% to claimable-pool refund (or equivalent — verify exact semantics at HEAD). The split proportions are preserved at HEAD (no 771893d1 change to _sendToVault body other than the INPUT shrinking by the subtraction).

Verdict row:

| Verdict Row ID | REQ-ID | Source Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| GOE-02-V01 | GOE-02 | D-243-C017, D-243-F015, GOX-03-V01/V02 (primary), v24.0 split spec | contracts/modules/DegenerusGameGameOverModule.sol:225-233 + handleGameOverDrain L186 | GOE-02a 33/33/34 split operates on post-subtraction `available` | <SAFE> | _sendToVault input is `remaining` (post-L86-90 + L94 + L157); split proportions preserved v24.0 | 771893d1 |

**Sub-vector GOE-02b — 30-day handleFinalSweep window sufficiency + no re-subtraction per D-14 GOE-02:**

Read handleFinalSweep body GameOverModule:196-216:
- Identify day+30 gate (e.g., `if (currentDay < gameOverDay + 30) revert;` — verify exact line)
- L215: `_sendToVault(totalFunds, stBal);` — `totalFunds` is the FULL ETH + stETH at sweep time; no re-subtraction of pendingRedemptionEthValue
- This means: any pendingRedemptionEthValue NOT CLAIMED by day+30 is swept to VAULT (forfeited by the claimant who missed the window)

Verify 30-day window sufficiency:
- Clock-based gate (not tamperable by admin/player/validator)
- 30 days is standard UX recovery period (user who loses wallet access, or who doesn't notice gameover, has 30 days to claim)
- Realistic-actor model: no scenario leaves funds stranded except user-abandonment (which is the intended forfeiture behavior)

Cross-cite v24.0 30-day sweep spec + Phase 239/240 GO-240-NNN rows as corroborating evidence.

Verdict rows:

| Verdict Row ID | REQ-ID | Source Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| GOE-02-V02 | GOE-02 | D-243-C017, Pre-Flag bullet L2506, v24.0 30-day sweep spec | contracts/modules/DegenerusGameGameOverModule.sol:196-216 | GOE-02b 30-day window sufficiency + no re-subtraction | <SAFE> | Clock-based gate; 30-day standard UX recovery; user-abandonment = intended forfeiture | 771893d1 |
| GOE-02-V03 | GOE-02 | Phase 239/240 GO-240-NNN rows (corroborating) | Cross-cite to prior-milestone artifacts | GOE-02a/b Phase 239/240 corroborating evidence | <SAFE via corroboration> | Prior-milestone artifacts re-derive same 33/33/34 + 30-day invariants at their HEADs; 245 re-derives at cc68bfc7 and confirms equivalence | 771893d1 |

Append Pre-Flag closure note:
- Bullet L2506 (GOE-02 claimablePool 33/33/34 + 30-day sweep against new drain flow) — CLOSED via GOE-02-V01..V03.

**Step E — Update §0 Verdict Count Card. Append §Reproduction Recipe with commands used.**

Constraints: READ-only (no contracts/, test/, upstream-audit-file writes); no F-31-NN.
  </action>
  <verify>
    <automated>test -f audit/v31-245-GOE.md && grep -q '^## §GOE-01' audit/v31-245-GOE.md && grep -q '^## §GOE-02' audit/v31-245-GOE.md && grep -q '^## §KI Envelope Re-Verify' audit/v31-245-GOE.md && grep -qE '^\| GOE-01-V[0-9]+ ' audit/v31-245-GOE.md && grep -qE '^\| GOE-02-V[0-9]+ ' audit/v31-245-GOE.md && grep -q 'RE_VERIFIED_AT_HEAD cc68bfc7' audit/v31-245-GOE.md && grep -q 'EXC-03' audit/v31-245-GOE.md && grep -q '_VRF_GRACE_PERIOD' audit/v31-245-GOE.md && grep -q 'RNG-01-V11' audit/v31-245-GOE.md && grep -q 'GOX-03-V01' audit/v31-245-GOE.md && grep -q '33/33/34' audit/v31-245-GOE.md && grep -q 'handleFinalSweep' audit/v31-245-GOE.md && [ "$(git status --porcelain contracts/ test/)" = "" ] && ! grep -qE 'F-31-[0-9]' audit/v31-245-GOE.md && [ "$(git status --porcelain audit/v31-243-DELTA-SURFACE.md audit/v31-244-PER-COMMIT-AUDIT.md)" = "" ]</automated>
  </verify>
  <done>
    - §GOE-01 contains at least 2 GOE-01-V## rows including V01 with `RE_VERIFIED_AT_HEAD cc68bfc7` for EXC-03 envelope
    - §GOE-02 contains at least 3 GOE-02-V## rows (split + window + corroborating)
    - §KI Envelope Re-Verify EXC-03 section header exists
    - Cross-cites: RNG-01-V11 (primary for GOE-01), GOX-03-V01/V02 (primary for GOE-02), SDR-08-V01 (cross-file for GOE-01b)
    - Pre-Flag closure notes for L2503, L2506 appended
    - Zero F-31-NN finding-IDs
    - Zero contracts/, test/, upstream-audit-file writes
  </done>
</task>

<task type="auto">
  <name>Task 2: GOE-03 full entry-point sweep + GOE-04 VRF × prevrandao branch matrix (RE_VERIFIED_AT_HEAD EXC-02) + GOE-05 gameOverPossible BURNIE gate ordering — close GOE-03 + GOE-04 + GOE-05</name>
  <files>audit/v31-245-GOE.md</files>
  <read_first>
    - .planning/phases/245-sdgnrs-redemption-gameover-safety/245-CONTEXT.md — D-16 GOE-03 + GOE-04 + GOE-05 adversarial vectors, D-17 (RE_VERIFIED methodology + prior-milestone artifacts), D-24 (KI EXC-02 envelope RE_VERIFIED)
    - audit/v31-243-DELTA-SURFACE.md — §1.4 D-243-C018..C025 (8 purchase/claim paths gate-shift), §3.1.16..§3.1.21 D-243-X029..X039 (4 MintModule + 4 WhaleModule callers), §3.1.22 D-243-X040..X052 (13 _livenessTriggered call sites)
    - audit/v31-243-DELTA-SURFACE.md §6 Consumer Index rows D-243-I016 (GOE-03 / GOX-01 coverage), D-243-I019 (GOE-04 / GOX-04 coverage), D-243-I020 (GOE-05 / GOX-05 coverage)
    - audit/v31-244-PER-COMMIT-AUDIT.md — §4 GOX-01 V-rows V01..V08 (PRIMARY closure for 8-path gate-shift — GOE-03 extends), §4 GOX-04 V-rows V01/V02 (PRIMARY closure for _livenessTriggered body + EXC-02 RE_VERIFIED — GOE-04 extends), §4 GOX-05 V-row V01 (day-math-first ordering — GOE-04 shared-context), §4 GOX-06 V-row V02 (_handleGameOverPath ordering — GOE-04 shared-context); §Phase-245-Pre-Flag bullets at L2509 (GOE-03), L2512 (GOE-04), L2515 (GOE-05)
    - KNOWN-ISSUES.md — EXC-02 entry "Gameover prevrandao fallback" — GAMEOVER_RNG_FALLBACK_DELAY = 14 days at AdvanceModule:109; _getHistoricalRngFallback at AdvanceModule:1301
    - contracts/DegenerusGame.sol at HEAD cc68bfc7 — full external-function inventory (L2133-2135 livenessTriggered external view at D-243-C012; plus all other external functions)
    - contracts/modules/DegenerusGameMintModule.sol at HEAD cc68bfc7:
      - L870 — purchaseCoin (entry-point; calls _purchaseCoinFor at L885)
      - L885-911 _purchaseCoinFor: L890 `if (_livenessTriggered()) revert E();`; L893-894 `// ENF-01: Block BURNIE tickets when drip projection cannot cover nextPool deficit. / if (gameOverPossible) revert GameOverPossible();`
      - L913-1198 _purchaseFor (L920 livenessTriggered gate)
      - L1206-1373 _callTicketPurchase (L1226 livenessTriggered gate)
      - L1388-1423 _purchaseBurnieLootboxFor (L1392 livenessTriggered gate)
      - All other external entry-points (verify full list)
    - contracts/modules/DegenerusGameWhaleModule.sol at HEAD cc68bfc7 — 4 whale-module purchase paths (L194/384/542/957)
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD cc68bfc7:
      - L109 — `GAMEOVER_RNG_FALLBACK_DELAY` (or equivalent 14-day constant)
      - L1228-1306 — _gameOverEntropy body (VRF-available + prevrandao-fallback branches)
      - L1301 — `_getHistoricalRngFallback` body (prevrandao admixture)
      - L1579-1596 — Tier-1 grace gate (or equivalent per the grace constants)
      - All other external entry-points (verify)
    - contracts/modules/DegenerusGameGameOverModule.sol at HEAD cc68bfc7 — external function inventory (handleGameOverDrain, handleFinalSweep, any admin-only entries)
    - contracts/modules/DegenerusGameJackpotModule.sol at HEAD cc68bfc7 — external function inventory (runBafJackpot, _runEarlyBirdLootboxJackpot, etc.)
    - contracts/storage/DegenerusGameStorage.sol at HEAD cc68bfc7 — L1235-1243 _livenessTriggered body (GOE-04 matrix foundation); plus other internal/external helpers
  </read_first>
  <action>
Append §GOE-03 + §GOE-04 + §GOE-05 to `audit/v31-245-GOE.md`.

**Step A — Sanity gate per Task 1.**

**Step B — §GOE-03 full entry-point sweep (vectors GOE-03a + GOE-03b per CONTEXT.md D-16):**

Append `## §GOE-03 — Purchase-blocking entry-point coverage at current surface (full sweep beyond 244 GOX-01 8-path claim)`.

**Sub-vector GOE-03a — full external-function sweep:**

Grep for every externally-callable function across Game + 5 modules:
```
grep -nE '^\s*function [a-zA-Z_]+\([^)]*\)\s+(external|public)' contracts/DegenerusGame.sol contracts/modules/DegenerusGame*.sol
```

For each external function, classify:
1. **Has _livenessTriggered / gameOver gate** — Phase 244 GOX-01-V01..V08 already covered 8 player-facing purchase/claim paths. Cross-cite V01..V08 as primary closure.
2. **State-read-only** — external view functions (no state mutation; cannot inject ETH)
3. **Admin-only with safe state mutation** — onlyOwner / onlyAdmin / onlyGame-modified functions that can mutate state but cannot inject player ETH for tickets

Build a table (NOT the verdict-row table — this is an inventory table):

| External Function | File:Line | Gate Class (i/ii/iii) | Cross-cite |
| purchaseCoin | MintModule:870 | (i) via _purchaseCoinFor L890 | GOX-01-V01 |
| purchaseTickets | (verify signature) | (i) | GOX-01-V02 |
| ... (all external functions) | ... | ... | ... |

For every function NOT in the 8-path GOX-01 coverage:
- If state-read-only, add row with class (ii) verdict
- If admin-only, add row with class (iii) verdict; verify the admin path does not inject ETH into game state that would represent a ticket purchase
- If none of the above AND the function is player-facing AND can inject ETH post-liveness, that's a finding candidate

Verdict rows:

| Verdict Row ID | REQ-ID | Source Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| GOE-03-V01 | GOE-03 | D-243-C018..C025, D-243-F016..F023, GOX-01-V01..V08 (primary) | contracts/modules/DegenerusGameMintModule.sol + DegenerusGameWhaleModule.sol | GOE-03a 8-path player-facing purchase/claim paths (primary closure via 244) | <SAFE via 244 cross-cite> | Phase 244 GOX-01-V01..V08 closed the 8-path gate-shift; GOE-03-V01 derives — each path has _livenessTriggered gate at entry | 771893d1 |
| GOE-03-V02 | GOE-03 | Extended sweep beyond 8-path, Pre-Flag bullet L2509 | contracts/modules/* + contracts/DegenerusGame.sol | GOE-03b non-8-path external entry-points (state-read-only + admin-only coverage) | <bucket> | Per-function classification table (class i/ii/iii); every external function accounted for; no entry-point injects ETH post-liveness | 771893d1 |

If the sweep surfaces ANY external function that is player-facing AND does NOT have a gate AND CAN inject ETH, emit a finding candidate:

```
### Finding Candidate GOE-03-FC01

SEVERITY: <bucket per D-08>
File:Line: contracts/<file>:<line>
Symbol: <function name>
Observation: External entry-point <name> lacks _livenessTriggered / gameOver gate AND can inject player ETH
Evidence: <trace>
Verdict-Row Cross-Ref: GOE-03-V02
Phase 246 Routing Hint: FIND-01 entry-point-coverage-gap
```

Append Pre-Flag closure note:
- Bullet L2509 (GOE-03 entry-point sweep beyond 8 paths) — CLOSED via GOE-03-V01..V02 (primary 244 cross-cite + extended per-function sweep).

**Step C — §GOE-04 VRF × prevrandao branch matrix under 14-day grace (vectors GOE-04a + GOE-04b per CONTEXT.md D-16 + KI EXC-02 RE_VERIFIED per D-24):**

Append `## §GOE-04 — VRF-available vs prevrandao-fallback gameover-jackpot branches under new 14-day VRF-dead grace`.

**4-dimensional matrix per D-14 GOE-04:**

Build the matrix across:
- Day range: {1-14, 14-120, 120-365, 365+}
- Level: {0, 1-9, 10+}
- VRF state: {healthy, stalled < grace, stalled >= grace, intermittent}
- rngLockedFlag state: {locked, unlocked}

For each cell (4 × 3 × 4 × 2 = 96 cells), identify:
- Does _livenessTriggered fire? (per Storage:1242 grace logic + day-math threshold)
- If liveness fires, does _handleGameOverPath execute? (per GOX-06-V02)
- If _handleGameOverPath executes, does gameOver latch? (check the gameOver-before-liveness ordering per GOX-06-V02)
- If gameOver latches, does _gameOverEntropy fire VRF-available (L1237) or prevrandao-fallback (L1263)?

Planner's discretion per CONTEXT.md (D-14 GOE-04 specifies the matrix but not the table shape): the 96-cell matrix may be compressed into grouped rows per (day-range × VRF-state) — since level and rngLockedFlag state are orthogonal to branch-selection in most cases, the effective matrix is 4 × 4 = 16 rows, each noting level × rngLockedFlag consistency as a sub-note.

**Key matrix cells:**

1. Day 1-14, any level, VRF healthy → no gameOver (day-math unmet + VRF alive); no branch fires
2. Day 1-14, any level, VRF stalled < 14 days → no grace (rngStart < 14 days); no liveness; no branch fires
3. Day 1-14, any level, VRF stalled >= 14 days → grace fires liveness; gameOver latches per GOX-06-V02; prevrandao-fallback branch L1263 fires (rngRequestTime != 0 still) — THE VRF-breaks-at-day-14 scenario
4. Day 14-120, level 0, VRF healthy → no gameOver (day-math unmet for level 0 threshold assumed 120)
5. Day 14-120, level 0, VRF stalled >= 14 days → grace fires liveness; gameOver latches; prevrandao-fallback branch fires
6. Day 120-365, level 0, VRF healthy → gameOver latches via day-math; VRF-available branch L1237 fires (VRF word exists)
7. Day 120-365, level 0, VRF stalled >= 14 days → gameOver latches (day-math + grace both fire); prevrandao-fallback branch L1263 fires (rngRequestTime != 0)
8. Day 365+, any level, any VRF → gameOver latches via day-math; branch depends on VRF state (VRF-available if healthy; prevrandao-fallback if stalled >= grace)

**Branch disjointness:** at most one branch fires per gameover tick. Verify:
- L1237-1259 gate: `currentWord != 0 && rngRequestTime != 0`
- L1263-1293 gate: `rngRequestTime != 0` (outer) with elapsed >= grace (inner)
- If currentWord != 0 (VRF arrived), L1237 branch fires; no fallback
- If currentWord == 0 AND rngRequestTime != 0 AND elapsed >= grace, L1263 fires
- Gates are mutually exclusive at runtime (verify at HEAD cc68bfc7 — read the exact conditions)

**EXC-02 envelope RE_VERIFIED per CONTEXT.md D-24:**

Phase 244 GOX-04-V02 primary closure: the 14-day grace adds a TRIGGER for liveness (not a new prevrandao-consumption path); sole prevrandao site remains AdvanceModule:1301 inside `_getHistoricalRngFallback`. GOE-04 DEEPER matrix confirms the envelope unchanged under multi-dim enumeration.

Verdict rows:

| Verdict Row ID | REQ-ID | Source Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| GOE-04-V01 | GOE-04 | D-243-C026, D-243-C028, GOX-04-V01 (primary) | contracts/storage/DegenerusGameStorage.sol:1241-1242 | GOE-04a 4-dim matrix: Tier-1 grace fires liveness across all day×VRF-state cells | <SAFE — RE_VERIFIED> | Per-cell classification shows grace-fire consistent with commit-message intent; liveness fires when VRF stalled >= 14 days regardless of day-math | 771893d1 |
| GOE-04-V02 | GOE-04 | D-243-C016, GOX-04-V02 (primary EXC-02 RE_VERIFIED), EXC-02 | contracts/modules/DegenerusGameAdvanceModule.sol:1237-1293 + :1301 | GOE-04a VRF-available vs prevrandao-fallback branch disjointness | RE_VERIFIED_AT_HEAD cc68bfc7 | Branches mutually exclusive at runtime; prevrandao site unchanged (sole site at :1301); EXC-02 envelope RE_VERIFIED under 4-dim matrix | 771893d1 |
| GOE-04-V03 | GOE-04 | Pre-Flag bullet L2512 | contracts/modules/DegenerusGameAdvanceModule.sol:1228-1306 + contracts/storage/DegenerusGameStorage.sol:1235-1243 | GOE-04b multi-level transitions + VRF-partial-recovery stall-tail | <bucket> | VRF-breaks-at-day-14 scenario + level-transition scenarios enumerated; no stall-tail bypasses EXC-02 envelope | 771893d1 |

Append Pre-Flag closure note:
- Bullet L2512 (GOE-04 deeper stall-tail enumeration) — CLOSED via GOE-04-V01..V03 (matrix + branch disjointness + stall-tail scenarios).

**Step D — §GOE-05 gameOverPossible BURNIE endgame gate ordering (vector GOE-05a per CONTEXT.md D-16):**

Append `## §GOE-05 — gameOverPossible BURNIE endgame gate (v11.0) across all new liveness paths`.

Read `_purchaseCoinFor` body at MintModule:885-911:
- L885 function header
- L890 `if (_livenessTriggered()) revert E();` — liveness gate (771893d1 NEW; was `if (gameOver) revert` at baseline per GOX-01-V01)
- L893-894 `// ENF-01: Block BURNIE tickets when drip projection cannot cover nextPool deficit. / if (gameOverPossible) revert GameOverPossible();` — BURNIE-gate (v11.0 invariant; UNCHANGED at 771893d1)

Ordering verification:
- State-0 caller (not liveness, not gameOverPossible): both gates pass
- State-0 caller with gameOverPossible true: L890 passes, L894 reverts
- State-1 caller (liveness fired, not gameOver): L890 reverts
- State-2 caller (gameOver latched): L890 reverts (liveness implies gameOver OR gameOver implies liveness per GOX-06-V02)

Question: can a State-1 caller bypass L894 gameOverPossible check by reaching _purchaseCoinFor through a different path?

Enumerate all paths to _purchaseCoinFor:
- `purchaseCoin` at MintModule:870 → calls _purchaseCoinFor(buyer, ticketQuantity, lootBoxBurnieAmount)
- Any internal callers? Grep for `_purchaseCoinFor(` in contracts/modules/

If the ONLY caller is `purchaseCoin` at L870, the gate at L890 is sufficient. If internal callers exist, verify each one either (a) has its own liveness gate before calling, OR (b) is guaranteed to be State-0 by construction.

Cross-cite v11.0 BURNIE endgame gate spec (in PROJECT.md milestone history) + Phase 244 GOX-01-V01 entry-gate enumeration.

Verdict rows:

| Verdict Row ID | REQ-ID | Source Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| GOE-05-V01 | GOE-05 | D-243-C018, D-243-F016, GOX-01-V01 (primary), Pre-Flag bullet L2515, v11.0 BURNIE gate spec | contracts/modules/DegenerusGameMintModule.sol:885-911 | GOE-05a L890 livenessTriggered BEFORE L894 gameOverPossible ordering | <SAFE> | State-1 caller rejected at L890 before reaching L894; BURNIE gate effectiveness preserved | 771893d1 |
| GOE-05-V02 | GOE-05 | Path-sweep for _purchaseCoinFor callers | contracts/modules/DegenerusGameMintModule.sol | GOE-05a no bypass path to _purchaseCoinFor that skips L890 | <bucket> | All callers of _purchaseCoinFor route through `purchaseCoin` at :870 with L890 gate; no internal caller bypasses | 771893d1 |

Append Pre-Flag closure note:
- Bullet L2515 (GOE-05 gameOverPossible BURNIE gate) — CLOSED via GOE-05-V01..V02 (ordering correctness + path-sweep).

**Step E — Update §0 Verdict Count Card. Append §Reproduction Recipe with new commands.**

Constraints: READ-only; no F-31-NN; EXC-02 RE_VERIFIED only (no re-litigation of acceptance).
  </action>
  <verify>
    <automated>test -f audit/v31-245-GOE.md && grep -q '^## §GOE-03' audit/v31-245-GOE.md && grep -q '^## §GOE-04' audit/v31-245-GOE.md && grep -q '^## §GOE-05' audit/v31-245-GOE.md && grep -qE '^\| GOE-03-V[0-9]+ ' audit/v31-245-GOE.md && grep -qE '^\| GOE-04-V[0-9]+ ' audit/v31-245-GOE.md && grep -qE '^\| GOE-05-V[0-9]+ ' audit/v31-245-GOE.md && grep -q 'EXC-02' audit/v31-245-GOE.md && grep -q 'gameOverPossible' audit/v31-245-GOE.md && grep -q 'GOX-01-V01' audit/v31-245-GOE.md && grep -q 'GOX-04-V02' audit/v31-245-GOE.md && [ "$(git status --porcelain contracts/ test/)" = "" ] && ! grep -qE 'F-31-[0-9]' audit/v31-245-GOE.md && [ "$(git status --porcelain audit/v31-243-DELTA-SURFACE.md audit/v31-244-PER-COMMIT-AUDIT.md)" = "" ]</automated>
  </verify>
  <done>
    - §GOE-03 contains at least 2 GOE-03-V## rows (8-path primary cross-cite + extended sweep)
    - §GOE-04 contains at least 3 GOE-04-V## rows including V02 with `RE_VERIFIED_AT_HEAD cc68bfc7` for EXC-02 envelope
    - §GOE-05 contains at least 2 GOE-05-V## rows (ordering + path-sweep)
    - 4-dimensional matrix (day × level × VRF × rngLockedFlag) addressed in GOE-04 cells
    - Cross-cites: GOX-01-V01..V08 (primary for GOE-03), GOX-04-V02 (primary for GOE-04 EXC-02), GOX-01-V01 (primary for GOE-05)
    - Pre-Flag closure notes for L2509, L2512, L2515 appended
    - Zero F-31-NN finding-IDs
    - Zero contracts/, test/, upstream-audit-file writes
  </done>
</task>

<task type="auto">
  <name>Task 3: GOE-06 close the 2 Phase-244-Pre-Flag candidates (skipped-BAF-pool × drain; DGNRS wrapper ↔ sDGNRS wrapper-held-backing conservation) — close GOE-06 with sweep-expansion-in-place guard per D-13</name>
  <files>audit/v31-245-GOE.md</files>
  <read_first>
    - .planning/phases/245-sdgnrs-redemption-gameover-safety/245-CONTEXT.md — D-12 (close the 2 Pre-Flag candidates only, no exhaustive sweep), D-13 (sweep-expansion-in-place if either escalates >= MEDIUM), D-16 GOE-06 adversarial vectors, D-25 (Pre-Flag advisory)
    - audit/v31-243-DELTA-SURFACE.md — §1.6 commit-cc68bfc7 changelog rows D-243-C035 (BafSkipped event at DegenerusJackpots.sol:71-74), D-243-C036 (markBafSkipped function at :498-510), D-243-C037 (IDegenerusJackpots.markBafSkipped interface), D-243-C038 (jackpots constant at AdvanceModule:105-106), D-243-C039 (`_consolidatePoolsAndRewardJackpots` MODIFIED at AdvanceModule:728-909); §2.3 classification rows D-243-F025 (markBafSkipped NEW), D-243-F026 (`_consolidatePoolsAndRewardJackpots` MODIFIED_LOGIC); §3.1.23 D-243-X053 (markBafSkipped caller at AdvanceModule:839)
    - audit/v31-244-PER-COMMIT-AUDIT.md — §4 GOX-06-V03 (cc68bfc7 jackpots direct-handle vs runBafJackpot reentrancy parity — PRIMARY closure for GOE-06 candidate 1 adjacent context); §1 EVT-02/EVT-03 bit-0 BAF-coupling verdicts (shared-context for GOE-06 candidate 1); §Phase-245-Pre-Flag bullets at L2518 (GOE-06 candidate 1 — skipped-BAF-pool × drain) + L2519 (GOE-06 candidate 2 — DGNRS wrapper conservation)
    - contracts/DegenerusJackpots.sol at HEAD cc68bfc7:
      - L71-74 — BafSkipped event
      - L498-510 — markBafSkipped function body (sets `lastBafResolvedDay` + emits BafSkipped; verify the `futurePool` treatment — is the skipped-BAF pool left intact in futurePool, or moved to claimablePool, or...?)
      - futurePool variable declaration + all write sites (grep `futurePool`)
      - bafBrackets + winningBafCredit consumer sweep (per §1.7 bullet 7 in Phase 243)
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD cc68bfc7:
      - L105-106 — jackpots constant declaration
      - L728-909 — `_consolidatePoolsAndRewardJackpots` body: L826-839 BAF gate `if ((rngWord & 1) == 1) { ... runBafJackpot ... } else { jackpots.markBafSkipped(lvl); }`
    - contracts/modules/DegenerusGameGameOverModule.sol at HEAD cc68bfc7:
      - L79-189 — handleGameOverDrain: verify how futurePool ETH is treated by drain (is it part of totalFunds? is it part of reserved? is it stranded?)
      - L196-216 — handleFinalSweep: same question
    - contracts/DegenerusStonk.sol at HEAD cc68bfc7:
      - DGNRS wrapper supply mechanics: identify `_mint`, `_burn`, `balanceOf` tracking, transfer functions; especially the wrapper-held-backing invariant (sDGNRS backing for every DGNRS wrapper token)
      - `burnForSdgnrs(player, amount)` function (called from sDGNRS.burnWrapped at L509) — does this decrement DGNRS wrapper supply?
    - contracts/StakedDegenerusStonk.sol at HEAD cc68bfc7:
      - L506-516 — burnWrapped: L507 `if (livenessTriggered() && !gameOver()) revert`; L509 `dgnrsWrapper.burnForSdgnrs(msg.sender, amount);`; L510 gameOver short-circuit; L514 _submitGamblingClaimFrom
      - The wrapper-backing accounting: every DGNRS wrapper token is backed by a sDGNRS token held by DegenerusStonk contract (verify)
    - .planning/phases/244-per-commit-adversarial-audit-evt-rng-qst-gox/244-01-SUMMARY.md — Phase 244 EVT plan closure for cc68bfc7 BAF-coupling context
  </read_first>
  <action>
Append §GOE-06 to `audit/v31-245-GOE.md`. Close the 2 Pre-Flag candidates per CONTEXT.md D-12. If either candidate escalates >= MEDIUM, sweep expands in-place per CONTEXT.md D-13.

**Step A — Sanity gate per Tasks 1 + 2.**

**Step B — §GOE-06 candidate 1: skipped-BAF-pool × handleGameOverDrain subtraction interaction (vector GOE-06a per CONTEXT.md D-16):**

Append `## §GOE-06 — NEW cross-feature emergent behavior from liveness × sDGNRS × drain interaction (2 Pre-Flag candidate closures per CONTEXT.md D-12)`.

Append `### §GOE-06 Candidate 1 — cc68bfc7 BAF skipped-pool preservation in futurePool × handleGameOverDrain subtraction`.

Read `markBafSkipped` body at DegenerusJackpots.sol:498-510:
- Identify what state markBafSkipped mutates: does it move futurePool ETH back to claimablePool? Does it zero futurePool? Does it leave futurePool intact (preserved for next BAF cycle)?
- Read the function body verbatim at HEAD cc68bfc7

Key investigative questions:
1. If markBafSkipped preserves the skipped-BAF pool in futurePool (does NOT return it to claimablePool), is this pool included in `totalFunds` that handleGameOverDrain reads?
2. When gameOver latches, does handleGameOverDrain sweep futurePool correctly?
3. Does handleFinalSweep at day+30 sweep any futurePool that wasn't resolved during handleGameOverDrain?

Read handleGameOverDrain body GameOverModule:79-189:
- Identify every variable it reads/sweeps: claimablePool (L86), pendingRedemptionEthValue (L94 + L157), possibly futurePool or equivalent
- Look for any explicit futurePool reference (grep `futurePool` in GameOverModule + Jackpots)

Possible outcomes:
- **Outcome A (SAFE):** markBafSkipped returns futurePool ETH to claimablePool; handleGameOverDrain sweeps claimablePool correctly via the L86-90 reserved-subtraction; no stranding
- **Outcome B (SAFE):** markBafSkipped preserves futurePool; handleGameOverDrain explicitly handles futurePool in its sweep logic; no stranding
- **Outcome C (INFO / LOW):** markBafSkipped preserves futurePool; handleGameOverDrain does NOT sweep futurePool; futurePool is stranded in DegenerusJackpots contract — forfeited wei
- **Outcome D (MEDIUM+):** stranded futurePool represents material wei lost at gameover; triggers sweep-expansion-in-place per CONTEXT.md D-13

Verdict row:

| Verdict Row ID | REQ-ID | Source Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| GOE-06-V01 | GOE-06 | D-243-C035, D-243-C036, D-243-F025, Pre-Flag bullet L2518, GOX-06-V03 (adjacent primary for reentrancy parity) | contracts/DegenerusJackpots.sol:498-510 + contracts/modules/DegenerusGameGameOverModule.sol:79-189 | GOE-06a cc68bfc7 BAF skipped-pool × handleGameOverDrain interaction | <bucket per actual reading — SAFE expected per 244 Pre-Flag framing, verify at HEAD> | markBafSkipped body at :498-510 analyzed; handleGameOverDrain coverage of futurePool analyzed; outcome per actual reading | cc68bfc7 |

If outcome is MEDIUM+, expand sweep in-place per D-13 to cover adjacent cross-feature triple (e.g., the INTERACTION between skipped-BAF-pool + rngLockedFlag + gameOver-latch). Bounded scope — do not balloon to full negative-space sweep.

Append Pre-Flag closure note:
- Bullet L2518 (GOE-06 candidate 1 — skipped-BAF-pool × drain) — CLOSED via GOE-06-V01 (verdict: <bucket>).

**Step C — §GOE-06 candidate 2: DGNRS wrapper ↔ sDGNRS wrapper-held backing conservation (vector GOE-06b per CONTEXT.md D-16):**

Append `### §GOE-06 Candidate 2 — burnWrapped divergence × DGNRS wrapper ↔ sDGNRS wrapper-held backing conservation across State-0/1/2`.

Read sDGNRS:506-516 burnWrapped body:
- L507 `if (livenessTriggered() && !gameOver()) revert BurnsBlockedDuringLiveness();` — State-1 block (liveness fired, gameOver not yet)
- L509 `dgnrsWrapper.burnForSdgnrs(msg.sender, amount);` — then-burn pattern: decrements player's DGNRS wrapper balance + mints sDGNRS to player (or equivalent)
- L510-512 gameOver short-circuit with `_deterministicBurnFrom(msg.sender, ContractAddresses.DGNRS, amount)` — deterministic burn path
- L513 `if (rngLocked()) revert BurnsBlockedDuringRng();` — rngLocked gate
- L514 `_submitGamblingClaimFrom(msg.sender, ContractAddresses.DGNRS, amount);` — gambling-burn path

Conservation question:
- In State-0: player calls burnWrapped → L509 burnForSdgnrs burns DGNRS wrapper → L514 _submitGamblingClaimFrom burns sDGNRS held by DegenerusStonk (the wrapper's backing). DGNRS wrapper total supply DECREASES; sDGNRS held by DegenerusStonk contract DECREASES (matched).
- In State-1: L507 reverts — burnWrapped blocked. No mutation. DGNRS wrapper state unchanged; sDGNRS state unchanged. Conservation: `DGNRS.totalSupply == sum(sDGNRS held by DegenerusStonk)` invariant preserved.
- In State-2 (gameOver): L509 burnForSdgnrs runs → L510-512 gameOver short-circuit → `_deterministicBurnFrom(msg.sender, ContractAddresses.DGNRS, amount)` burns the DegenerusStonk-held sDGNRS (not player's). The result: DGNRS.totalSupply DECREASES (via L509 burnForSdgnrs) AND sDGNRS held by DegenerusStonk DECREASES (via L510-512 _deterministicBurnFrom with burnFrom = DGNRS wrapper address). Matched.

CRITICAL question: in State-1 (liveness fired, gameOver not yet), a player holding DGNRS wrapper tokens retains them. When gameOver latches, the player's DGNRS wrapper tokens are still backed by sDGNRS held by DegenerusStonk. Does handleGameOverDrain preserve this backing relationship?

- handleGameOverDrain sweeps ETH from sDGNRS, but does NOT sweep sDGNRS tokens themselves (only ETH payout).
- After gameOver latches + handleFinalSweep runs at day+30, the sDGNRS contract may have zero ETH balance (swept to VAULT), but the sDGNRS tokens held by DegenerusStonk contract STILL EXIST.
- A player holding DGNRS wrapper tokens can still call burnWrapped in State-2 → gameOver short-circuit at L510 → _deterministicBurnFrom burns the DegenerusStonk-held sDGNRS. But _deterministicBurnFrom computes totalMoney = ethBal + stethBal + claimableEth - pendingRedemptionEthValue at sDGNRS:535. After handleFinalSweep, ethBal + stethBal ≈ 0 (swept). So the burn pays ZERO to the player.

Is this an issue?
- The DGNRS wrapper holder at State-1 has an economic expectation that their wrapper is redeemable for sDGNRS backing. After handleFinalSweep, that backing is worth zero ETH (swept to VAULT). Is this intended?
- Cross-cite v24.0 spec: gameOver handling is "terminal state — no further gameplay". Player economic expectation at gameOver is captured by handleGameOverDrain's 33/33/34 split (some ETH goes back to claimablePool for deterministic-burn payouts).
- Does _deterministicBurnFrom at State-2 (post-drain, pre-handleFinalSweep) pay out fairly? Read the formula at sDGNRS:535: totalMoney = ethBal + stethBal + claimableEth - pendingRedemptionEthValue. Post-drain, claimableEth should include the 33/33/34 split refund. So the player gets proportional backing.
- Post-handleFinalSweep (day+30): everything is swept. Player gets zero. This IS the intended forfeiture for missing the 30-day window (same as SDR-04b).

Verdict: SAFE — the conservation invariant holds (DGNRS wrapper supply tracks sDGNRS-wrapper-held backing across all 3 states); the economic expectation is preserved until handleFinalSweep, at which point forfeiture is intended behavior.

Verdict row:

| Verdict Row ID | REQ-ID | Source Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| GOE-06-V02 | GOE-06 | D-243-C014, D-243-F012, GOX-02-V02 (adjacent shared-context), Pre-Flag bullet L2519 | contracts/StakedDegenerusStonk.sol:506-516 + contracts/DegenerusStonk.sol:227-245 + contracts/modules/DegenerusGameGameOverModule.sol:79-216 | GOE-06b DGNRS wrapper ↔ sDGNRS wrapper-held backing conservation across State-0/1/2 | <SAFE expected> | Per-state conservation analysis: State-0 matched burn; State-1 revert preserves state; State-2 gameOver short-circuit + _deterministicBurnFrom preserves backing until handleFinalSweep; post-sweep forfeiture is intended behavior | 771893d1 |

If outcome is MEDIUM+, expand sweep in-place per D-13 to cover adjacent cross-feature triple (e.g., burnAtGameOver at sDGNRS:462 × DegenerusStonk-held sDGNRS supply — does burnAtGameOver zero the backing, breaking the invariant after sweep?). Bounded scope.

Append Pre-Flag closure note:
- Bullet L2519 (GOE-06 candidate 2 — DGNRS wrapper backing conservation) — CLOSED via GOE-06-V02 (per-state conservation proof).

**Step D — GOE-06 REQ-level aggregate verdict per CONTEXT.md D-13:**

`GOE-06 floor severity = worst of {GOE-06-V01 verdict, GOE-06-V02 verdict}` per D-13 framing (GOE-06 is the aggregate "NEW cross-feature emergent behavior" REQ).

If both candidates close SAFE: GOE-06 SAFE floor. Exhaustive sweep deferred to Phase 246 / future milestone per D-12 Deferred Ideas carry.
If one escalates to INFO/LOW: GOE-06 INFO/LOW floor; sweep-expansion-in-place documents adjacent-triple verdicts.
If one escalates to MEDIUM+: GOE-06 MEDIUM+ floor; sweep-expansion documents bounded-triple; finding candidate emitted for Phase 246.

**Step E — Update §0 Verdict Count Card with GOE-06 row counts + floor severity. Append §Reproduction Recipe with commands.**

Constraints: READ-only; no F-31-NN.
  </action>
  <verify>
    <automated>test -f audit/v31-245-GOE.md && grep -q '^## §GOE-06' audit/v31-245-GOE.md && grep -q '^### §GOE-06 Candidate 1' audit/v31-245-GOE.md && grep -q '^### §GOE-06 Candidate 2' audit/v31-245-GOE.md && grep -qE '^\| GOE-06-V[0-9]+ ' audit/v31-245-GOE.md && grep -q 'markBafSkipped' audit/v31-245-GOE.md && grep -q 'burnWrapped' audit/v31-245-GOE.md && grep -q 'futurePool' audit/v31-245-GOE.md && grep -q 'wrapper.*backing\|backing.*wrapper' audit/v31-245-GOE.md && for req in GOE-01 GOE-02 GOE-03 GOE-04 GOE-05 GOE-06; do grep -qE "^\| $req-V[0-9]+ " audit/v31-245-GOE.md || { echo "FAIL: $req absent"; exit 1; }; done && [ "$(git status --porcelain contracts/ test/)" = "" ] && ! grep -qE 'F-31-[0-9]' audit/v31-245-GOE.md && [ "$(git status --porcelain audit/v31-243-DELTA-SURFACE.md audit/v31-244-PER-COMMIT-AUDIT.md)" = "" ]</automated>
  </verify>
  <done>
    - §GOE-06 contains 2 sub-sections (Candidate 1 + Candidate 2) per CONTEXT.md D-12
    - At least 2 GOE-06-V## rows (one per candidate)
    - Every REQ in {GOE-01..GOE-06} appears at least once as Verdict Row ID prefix
    - GOE-06 aggregate floor severity stated per D-13
    - Pre-Flag closure notes for L2518, L2519 appended
    - Sweep-expansion-in-place applied IF either candidate escalates >= MEDIUM (bounded to adjacent triple)
    - Zero F-31-NN finding-IDs
    - Zero contracts/, test/, upstream-audit-file writes
  </done>
</task>

<task type="auto">
  <name>Task 4: CONSOLIDATION — assemble audit/v31-245-SDR-GOE.md from 2 working files + §Consumer Index + §Reproduction Recipe Appendix + §Phase-246-Input subsection + flip FINAL READ-only (per CONTEXT.md D-04 + D-05 + D-18)</name>
  <files>audit/v31-245-SDR-GOE.md</files>
  <read_first>
    - .planning/phases/245-sdgnrs-redemption-gameover-safety/245-CONTEXT.md — D-04 (single consolidated audit/v31-245-SDR-GOE.md with 4 sections), D-05 (per-plan working file pattern with consolidation in 245-02), D-06 (verdict-table columns carry), D-17 (corroborating prior-milestone artifacts per REQ), D-18 (245-02 pre-flags Phase 246 FIND-01..FIND-03 candidates), D-23 (no F-31-NN)
    - audit/v31-245-SDR.md (from 245-01) — SDR bucket complete content (§SDR-01..§SDR-08 + §KI Envelope Re-Verify EXC-03 + §Phase-244-Pre-Flag Cross-Walk + §Reproduction Recipe)
    - audit/v31-245-GOE.md (from 245-02 Tasks 1-3 above) — GOE bucket complete content (§GOE-01..§GOE-06 + §KI Envelope Re-Verify EXC-02 + EXC-03 + §Phase-244-Pre-Flag Cross-Walk + §Reproduction Recipe)
    - audit/v31-243-DELTA-SURFACE.md §6 Consumer Index — for cross-ref source D-243-X/F/C/S row IDs
    - audit/v31-244-PER-COMMIT-AUDIT.md §5 Consumer Index — for Phase 244 V-row cross-ref template (245 §3 Consumer Index format)
    - audit/v30-*.md prior-milestone artifacts per CONTEXT.md D-17 — cited in GOE-02 (v24.0 33/33/34 split + Phase 239/240), GOE-05 (v11.0 BURNIE gate), SDR-07 (v29.0 Phase 235 + v24.0 supply conservation)
  </read_first>
  <action>
**Step A — Sanity gate. Confirm both working files exist and are non-empty:**

```
test -f audit/v31-245-SDR.md
test -f audit/v31-245-GOE.md
```

If either is missing, STOP — Task 4 depends on BOTH 245-01 completion (v31-245-SDR.md) AND this plan's own Tasks 1-3 (v31-245-GOE.md). Per the wave topology in CONTEXT.md D-02, both plans are wave 1 parallel; Task 4 of 245-02 is the consolidation step that depends on 245-01 completing. The orchestrator schedules Task 4 to run AFTER 245-01 lands (per the SUMMARY-commit timing in CONTEXT.md D-05).

**Step B — Create `audit/v31-245-SDR-GOE.md` with the 4-section structure per CONTEXT.md D-04:**

Write the file with this skeleton:

```
# v31.0 Phase 245 — sDGNRS Redemption Gameover Safety + Pre-Existing Gameover Invariant Re-Verification (consolidated deliverable)

Status: FINAL — READ-ONLY (locked at SUMMARY commit per CONTEXT.md D-05)
Audit baseline: 7ab515fe
Audit head:     cc68bfc7
In-scope commits: 771893d1 (gameover liveness + sDGNRS redemption protection — primary driver) + cc68bfc7 (BAF-coupling addendum for GOE-06 candidate 1)
Phase:          245-sdgnrs-redemption-gameover-safety
Owning plans:   245-01 (SDR — 8 REQs) + 245-02 (GOE — 6 REQs + consolidation)
Severity bar:   {SAFE, INFO, LOW, MEDIUM, HIGH, CRITICAL} per CONTEXT.md D-08; KI envelope rows use `RE_VERIFIED_AT_HEAD cc68bfc7` annotation per CONTEXT.md D-24
Finding-IDs:    NOT emitted in this phase per CONTEXT.md D-23 (Phase 246 FIND-01 owns assignment); §Phase-246-Input subsection aggregates candidates

## §0 — Per-Phase Verdict Heatmap (planner-discretion readability aid per CONTEXT.md Discretion)

| REQ-ID | Verdict Rows | Floor Severity | KI Envelope | Owning Plan |
| --- | --- | --- | --- | --- |
| SDR-01 | (count from §1) | (filled) | n/a | 245-01 |
| SDR-02 | (count) | (filled) | n/a | 245-01 |
| SDR-03 | (count) | (filled) | n/a | 245-01 |
| SDR-04 | (count) | (filled) | n/a | 245-01 |
| SDR-05 | (count) | (filled) | n/a | 245-01 |
| SDR-06 | (count) | (filled) | n/a | 245-01 |
| SDR-07 | (count) | (filled) | n/a | 245-01 |
| SDR-08 | (count) | (filled) | EXC-03 RE_VERIFIED_AT_HEAD cc68bfc7 | 245-01 |
| GOE-01 | (count) | (filled) | EXC-03 RE_VERIFIED_AT_HEAD cc68bfc7 | 245-02 |
| GOE-02 | (count) | (filled) | n/a | 245-02 |
| GOE-03 | (count) | (filled) | n/a | 245-02 |
| GOE-04 | (count) | (filled) | EXC-02 RE_VERIFIED_AT_HEAD cc68bfc7 | 245-02 |
| GOE-05 | (count) | (filled) | n/a | 245-02 |
| GOE-06 | (count) | (filled per D-13) | n/a | 245-02 |

## §1 — SDR Bucket (commit 771893d1 — sDGNRS redemption deep sub-audit; plan 245-01)

(Embed contents of audit/v31-245-SDR.md verbatim, dropping the file-level header — the consolidated header above replaces it.)

## §2 — GOE Bucket (commit 771893d1 + cc68bfc7 BAF-coupling addendum — pre-existing gameover invariant RE_VERIFIED_AT_HEAD; plan 245-02)

(Embed contents of audit/v31-245-GOE.md verbatim.)

## §3 — Consumer Index (REQ-ID → Phase 245 verdict-row mapping + cross-ref to Phase 243 D-243 rows + Phase 244 V-rows + prior-milestone artifacts per CONTEXT.md D-17)

| REQ-ID | Phase 245 Verdict Rows | Source 243 Consumer Index Row | Phase 244 V-row Cross-Cite | Prior-Milestone Corroborating Artifacts | Owning Plan |
| --- | --- | --- | --- | --- | --- |
| SDR-01 | SDR-01-T{a-f} + SDR-01-V01..V<n> | D-243-I017 (burn/burnWrapped), D-243-I019 (liveness-trigger) | GOX-01-V01..V08 (entry-gate shift, shared-context) + GOX-04-V01/V02 (liveness predicates, shared-context) + GOX-06-V02 (_handleGameOverPath ordering, shared-context) | Phase 238 Freeze-Proof Subset (timing-matrix corroboration) | 245-01 |
| SDR-02 | SDR-02-V01..V<n> | D-243-I017, D-243-I018 | GOX-03-V01 (handleGameOverDrain subtraction primary) | v24.0 claimablePool spec | 245-01 |
| SDR-03 | SDR-03-V01..V<n> | D-243-I018 | GOX-03-V01/V02/V03 (primary closure) | v24.0 33/33/34 split spec | 245-01 |
| SDR-04 | SDR-04-V01..V<n> | D-243-I017, D-243-I019 | GOX-01-V01..V08 (entry-point coverage), GOX-03-V03 (reentrancy-safety) | Phase 238 4-actor taxonomy (D-07) | 245-01 |
| SDR-05 | SDR-05-V01..V06 (one per timing) | D-243-I018 | GOX-03-V01/V02 (subtraction arithmetic) | v24.0 claimablePool spec | 245-01 |
| SDR-06 | SDR-06-V01..V<n> | D-243-I017 | GOX-02-V01/V02/V03 (PRIMARY State-1 revert coverage) | v29.0 Phase 235 burn-path walk | 245-01 |
| SDR-07 | SDR-07-V01..V<n> | D-243-I017 (sDGNRS supply mutation coverage) | (none direct — 245 re-derives) | v29.0 Phase 235 + v24.0 supply-conservation (corroborating) | 245-01 |
| SDR-08 | SDR-08-V01..V<n> | D-243-I021 (gameover-entropy coverage) | RNG-01-V11 (PRIMARY EXC-03 envelope non-widening), GOX-06-V01 (rngRequestTime clearing adjacent) | v29.0 Phase 232.1 F-29-04 commitment-window trace + KI EXC-03 | 245-01 |
| GOE-01 | GOE-01-V01..V<n> | D-243-I019, D-243-I021 | RNG-01-V11 (PRIMARY EXC-03 envelope) | v29.0 Phase 232.1 F-29-04 + KI EXC-03 | 245-02 |
| GOE-02 | GOE-02-V01..V<n> | D-243-I018 | GOX-03-V01/V02 (PRIMARY handleGameOverDrain subtraction) | v24.0 33/33/34 split + 30-day sweep; Phase 239/240 GO-240 rows | 245-02 |
| GOE-03 | GOE-03-V01..V<n> | D-243-I016 (GOX-01 coverage) | GOX-01-V01..V08 (PRIMARY 8-path entry-gate) | v24.0 10-entry-point claim (updated to current surface) | 245-02 |
| GOE-04 | GOE-04-V01..V<n> | D-243-I019 | GOX-04-V01/V02 (PRIMARY EXC-02 RE_VERIFIED_AT_HEAD + _livenessTriggered body) + GOX-05-V01 (day-math-first) + GOX-06-V02 (_handleGameOverPath ordering) | Phase 239 rngLockedFlag + Phase 240 GO-240 rows + KI EXC-02 | 245-02 |
| GOE-05 | GOE-05-V01..V<n> | D-243-I020 (GOX-05 coverage) | GOX-01-V01 (PRIMARY _purchaseCoinFor gate) | v11.0 BURNIE endgame gate spec | 245-02 |
| GOE-06 | GOE-06-V01..V<n> | D-243-I016..I021 (cross-cutting) | GOX-06-V03 (cc68bfc7 BAF reentrancy parity adjacent) + GOX-02-V02 (burnWrapped divergence adjacent) + EVT-03-V<n> (bit-0 BAF-coupling adjacent) | v24.0 + v29.0 wrapper-backing conservation spec | 245-02 |

## §4 — Reproduction Recipe Appendix

(Concatenate §Reproduction Recipe sub-sections from audit/v31-245-SDR.md + audit/v31-245-GOE.md. Group by bucket: SDR commands first, then GOE commands. POSIX-portable per CONTEXT.md D-22 carry from Phase 243/244.)

## §5 — Phase 246 Input Subsection (per CONTEXT.md D-18)

(Aggregation of finding candidates across both bucket files. Format: `- FIND-01 candidate | SEVERITY: <bucket> | <one-line observation> | <file:line> | <source REQ> | <suggested Phase 246 action>`. If zero candidates surfaced, subsection explicitly states the zero state per D-18 second sentence.)
```

**Step C — Populate §1 and §2 bucket content:**

Read `audit/v31-245-SDR.md` and `audit/v31-245-GOE.md` in full. For each, drop the file-level header (Audit baseline / head / Owning commits / Scope per / Plan / Phase lines — replaced by consolidated header) and embed the remaining content verbatim under §1 (SDR) and §2 (GOE) respectively.

**Step D — Populate §0 Verdict Heatmap with actual row counts:**

For each REQ in {SDR-01..SDR-08, GOE-01..GOE-06}, count the number of `<REQ>-V##` rows (and SDR-01-T{a-f} foundation rows for SDR-01) in the consolidated §1 + §2 body. Fill the heatmap.

Populate Floor Severity per REQ from the individual §0 Verdict Count Cards in each working file.

**Step E — Aggregate §Phase-246-Input subsection per CONTEXT.md D-18:**

Grep both working files for `SEVERITY:` prose blocks (finding candidates from SDR bucket + GOE bucket). For each finding candidate:

```
- FIND-01 candidate | SEVERITY: <bucket from prose block> | <one-line observation copy-paste> | <file:line> | <source REQ> | <suggested Phase 246 action — e.g., FIND-01 ID assignment / FIND-02 severity reclassification / FIND-03 KI promotion / REG-01 regression cross-cite>
```

If zero finding candidates surfaced (Phase 245 all-SAFE / RE_VERIFIED_AT_HEAD outcome), populate subsection with the zero-state text per CONTEXT.md D-18 second sentence:
```
Zero finding candidates emitted during Phase 245 (all 14 REQs closed SAFE floor severity OR RE_VERIFIED_AT_HEAD). Phase 246 FIND-01 pool from Phase 245 is empty; FIND-02 has no candidates to reclassify; FIND-03 KI delta is zero. REG-01 regression coverage for Phase 245 is limited to the 6-timing SDR-01 matrix + GOE-06 2-candidate closure as spot-check anchors.
```

**Step F — Flip the file FINAL READ-only at SUMMARY commit time:**

Per CONTEXT.md D-05: the SUMMARY commit is the moment `audit/v31-245-SDR-GOE.md` flips FINAL READ-only. The frontmatter `Status: FINAL — READ-ONLY` annotation is set BEFORE the commit. Downstream Phase 246 plans CONSUME the file but do NOT EDIT it.

**Step G — Preserve 2 working files as appendices:**

Per CONTEXT.md D-05: `audit/v31-245-SDR.md` + `audit/v31-245-GOE.md` remain on disk as cross-ref appendices — do NOT delete them. The consolidated file is the PRIMARY deliverable; working files support grep-by-bucket.

**Step H — Run the finding-ID emission gate (D-23 enforcement):**

```
TOKEN="F-31""-"
! grep -q "$TOKEN" audit/v31-245-SDR-GOE.md
```

(Token assembled at runtime so the gate command itself doesn't match the pattern. Must return exit 1.)

Constraints:
- Zero edits to audit/v31-243-DELTA-SURFACE.md or audit/v31-244-PER-COMMIT-AUDIT.md per CONTEXT.md D-22
- Zero F-31-NN finding-IDs per D-23
- Zero contracts/ or test/ writes per D-20
- The 2 working files (audit/v31-245-SDR.md + audit/v31-245-GOE.md) remain as APPENDICES — do NOT delete them
- Per CONTEXT.md Discretion: planner may inline this consolidation into the 245-02 SUMMARY commit OR a separate consolidation commit; recommend single SUMMARY commit per Phase 243/244 precedent
  </action>
  <verify>
    <automated>test -f audit/v31-245-SDR-GOE.md && grep -q '^# v31.0 Phase 245' audit/v31-245-SDR-GOE.md && grep -q 'Status: FINAL — READ-ONLY' audit/v31-245-SDR-GOE.md && grep -q '^## §1 — SDR Bucket' audit/v31-245-SDR-GOE.md && grep -q '^## §2 — GOE Bucket' audit/v31-245-SDR-GOE.md && grep -q '^## §3 — Consumer Index' audit/v31-245-SDR-GOE.md && grep -q '^## §4 — Reproduction Recipe Appendix' audit/v31-245-SDR-GOE.md && grep -q '^## §5 — Phase 246 Input' audit/v31-245-SDR-GOE.md && for req in SDR-01 SDR-02 SDR-03 SDR-04 SDR-05 SDR-06 SDR-07 SDR-08 GOE-01 GOE-02 GOE-03 GOE-04 GOE-05 GOE-06; do grep -qE "^\| $req-V[0-9]+ |^\| $req-T[a-f] |^\| $req " audit/v31-245-SDR-GOE.md || { echo "FAIL: $req absent"; exit 1; }; done && test -f audit/v31-245-SDR.md && test -f audit/v31-245-GOE.md && [ "$(git status --porcelain contracts/ test/)" = "" ] && ! grep -qE 'F-31-[0-9]' audit/v31-245-SDR-GOE.md && [ "$(git status --porcelain audit/v31-243-DELTA-SURFACE.md audit/v31-244-PER-COMMIT-AUDIT.md)" = "" ]</automated>
  </verify>
  <done>
    - `audit/v31-245-SDR-GOE.md` exists with §0 heatmap + §1 SDR + §2 GOE + §3 Consumer Index + §4 Reproduction Recipe Appendix + §5 Phase 246 Input subsection
    - `Status: FINAL — READ-ONLY` annotation in header
    - All 14 REQ-IDs (SDR-01..SDR-08, GOE-01..GOE-06) appear at least once as verdict-row prefix in the consolidated body
    - §3 Consumer Index maps every REQ to D-243-I### + Phase 244 V-row cross-cite + prior-milestone corroborating artifacts per CONTEXT.md D-17
    - §4 Reproduction Recipe Appendix concatenates both working files' §Reproduction Recipe sub-sections
    - §5 Phase 246 Input subsection contains finding-candidate aggregation OR explicit zero-state text per CONTEXT.md D-18
    - 2 working files (v31-245-SDR.md + v31-245-GOE.md) remain as appendices (NOT deleted)
    - Zero F-31-NN finding-IDs
    - Zero contracts/, test/, or v31-243-DELTA-SURFACE.md or v31-244-PER-COMMIT-AUDIT.md writes
  </done>
</task>

</tasks>

<verification>
**End-of-plan verification gate (manual, before commit):**

1. **Coverage gate (GOE bucket):** Every REQ in {GOE-01..GOE-06} appears at least once as Verdict Row ID prefix in `audit/v31-245-GOE.md`. Run:
   ```
   for req in GOE-01 GOE-02 GOE-03 GOE-04 GOE-05 GOE-06; do
     count=$(grep -cE "^\| $req-V[0-9]+ " audit/v31-245-GOE.md)
     echo "$req verdict rows: $count"
     [ "$count" -ge 1 ] || { echo "FAIL: $req has zero verdict rows"; exit 1; }
   done
   ```
2. **Coverage gate (consolidated):** Every REQ in {SDR-01..SDR-08, GOE-01..GOE-06} (14 total) appears at least once as Verdict Row ID prefix in `audit/v31-245-SDR-GOE.md` (allowing for SDR-01-T{a-f} foundation rows).
3. **Source 243 row citation gate:** Every D-243 row in D-243-I016..D-243-I021 appears at least once in §GOE verdict-row Source Row(s) cells.
4. **Phase 244 V-row cross-cite gate:** Every Phase 244 V-row listed in frontmatter `source_244_v_rows` appears at least once as cross-cite. Explicit check for GOX-01-V01..V08 (primary for GOE-03), GOX-03-V01/V02 (primary for GOE-02), GOX-04-V02 (primary for GOE-04 EXC-02), GOX-06-V02/V03 (shared for GOE-04 + GOE-06), RNG-01-V11 (primary for GOE-01).
5. **Pre-Flag closure gate:** Every GOE-grouped Pre-Flag bullet (L2503, L2506, L2509, L2512, L2515, L2518, L2519 — 7 bullets total per CONTEXT.md D-25) has a one-line closure note in the §Phase-244-Pre-Flag Cross-Walk section. Run:
   ```
   for line in 2503 2506 2509 2512 2515 2518 2519; do
     grep -q "L$line" audit/v31-245-GOE.md || { echo "FAIL: Pre-Flag L$line closure note missing"; exit 1; }
   done
   ```
6. **KI envelope gate:** §KI Envelope Re-Verify sections exist with `EXC-02` (GOE-04) + `EXC-03` (GOE-01) + `RE_VERIFIED_AT_HEAD cc68bfc7` annotations.
7. **GOE-06 candidate closure gate:** 2 sub-sections (Candidate 1 + Candidate 2) exist per CONTEXT.md D-12; each has at least one GOE-06-V## verdict row.
8. **Severity gate:** Every verdict row has a severity from {SAFE, INFO, LOW, MEDIUM, HIGH, CRITICAL} OR `RE_VERIFIED_AT_HEAD cc68bfc7` for KI envelope rows. Zero TBD/blank verdicts.
9. **READ-only gate:** `git status --porcelain contracts/ test/` empty; `git status --porcelain audit/v31-243-DELTA-SURFACE.md audit/v31-244-PER-COMMIT-AUDIT.md` empty.
10. **F-31-NN absence gate:** `! grep -qE 'F-31-[0-9]' audit/v31-245-GOE.md && ! grep -qE 'F-31-[0-9]' audit/v31-245-SDR-GOE.md`.
11. **Consolidation FINAL-flip gate:** `Status: FINAL — READ-ONLY` annotation present in `audit/v31-245-SDR-GOE.md` header.
12. **Both working files preserved:** `test -f audit/v31-245-SDR.md && test -f audit/v31-245-GOE.md` both return success.
13. **Phase 246 Input subsection gate:** §5 in consolidated file contains either finding-candidate bullets OR explicit zero-state text per CONTEXT.md D-18.
</verification>

<success_criteria>
- `audit/v31-245-GOE.md` exists with §GOE-01..§GOE-06 sections + §KI Envelope Re-Verify EXC-02 + EXC-03 + §Phase-244-Pre-Flag Cross-Walk + §Reproduction Recipe
- `audit/v31-245-SDR-GOE.md` exists with §0 heatmap + §1 SDR + §2 GOE + §3 Consumer Index + §4 Reproduction Recipe Appendix + §5 Phase 246 Input subsection; Status: FINAL — READ-ONLY annotation
- Both working files (v31-245-SDR.md + v31-245-GOE.md) preserved as appendices
- §0 Verdict Count Card (in v31-245-GOE.md) + §0 Heatmap (in SDR-GOE.md) filled per REQ
- §Reproduction Recipe Appendix POSIX-portable per CONTEXT.md D-22 carry from Phase 243/244
- Plan SUMMARY records: per-REQ verdict-row count + floor severity (GOE-01..GOE-06 + aggregate for SDR-01..SDR-08 from 245-01), KI envelope status (EXC-02 in GOE-04, EXC-03 in GOE-01 + cross-cite to SDR-08), Phase 244 Pre-Flag bullet-closure status (7 GOE-grouped bullets L2502-2519), Phase 246 Input finding-candidate count, working-file paths, consolidation-output path, FINAL-flip confirmation, HEAD anchor confirmation, GOE-06 sweep-expansion decision per D-13
</success_criteria>

<output>
After completion, create `.planning/phases/245-sdgnrs-redemption-gameover-safety/245-02-SUMMARY.md` per standard template + per-bucket-plan additions:
- Per-REQ verdict-row count + floor severity (GOE-01..GOE-06)
- KI envelope status:
  - EXC-02 RE_VERIFIED_AT_HEAD cc68bfc7 in §GOE-04 (deeper matrix)
  - EXC-03 RE_VERIFIED_AT_HEAD cc68bfc7 in §GOE-01 (14-day grace × F-29-04 deeper) + cross-cite to §SDR-08 (new redemption-resolve consumption from 245-01)
- Phase 244 Pre-Flag bullet-closure status (7 GOE-grouped bullets L2503, L2506, L2509, L2512, L2515, L2518, L2519 — each CLOSED with closure note)
- Phase 244 V-row cross-cites made (GOX-01-V01..V08 primary for GOE-03; GOX-03-V01/V02 primary for GOE-02; GOX-04-V02 primary for GOE-04 EXC-02; GOX-06-V02/V03 shared for GOE-04 + GOE-06; RNG-01-V11 primary for GOE-01 EXC-03)
- GOE-06 sweep-expansion decision per D-13 (SAFE floor → sweep deferred; INFO/LOW → minimal expansion; MEDIUM+ → bounded sweep documented)
- Phase 246 Input subsection finding-candidate count (total across both buckets; zero-state if all-SAFE)
- Path to working files: `audit/v31-245-SDR.md` (from 245-01) + `audit/v31-245-GOE.md` (from this plan)
- Path to consolidated final deliverable: `audit/v31-245-SDR-GOE.md` (FINAL READ-only)
- Consolidation FINAL-flip confirmation
- Any scope-guard deferrals per CONTEXT.md D-22
- HEAD anchor confirmation (`cc68bfc7` matched at each task start)
</output>
</content>
</invoke>