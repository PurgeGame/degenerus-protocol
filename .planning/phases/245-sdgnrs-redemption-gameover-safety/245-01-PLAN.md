---
phase: 245-sdgnrs-redemption-gameover-safety
phase_number: 245
plan: 245-01
plan_number: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v31-245-SDR.md
autonomous: true
requirements: [SDR-01, SDR-02, SDR-03, SDR-04, SDR-05, SDR-06, SDR-07, SDR-08]
requirements_addressed: [SDR-01, SDR-02, SDR-03, SDR-04, SDR-05, SDR-06, SDR-07, SDR-08]
baseline: 7ab515fe
baseline_sha: 7ab515fe
head: cc68bfc7
head_sha: cc68bfc7
head_anchor: cc68bfc7
in_scope_commits:
  - sha: 771893d1
    summary: "feat(gameover): shift purchase/claim gates to liveness and protect sDGNRS redemptions — primary SDR driver (StakedDegenerusStonk.sol + GameOverModule.sol + AdvanceModule.sol + Storage.sol)"
    drives_reqs: [SDR-01, SDR-02, SDR-03, SDR-04, SDR-05, SDR-06, SDR-07, SDR-08]
in_scope_243_rows:
  changelog:    [D-243-C012, D-243-C013, D-243-C014, D-243-C015, D-243-C016, D-243-C017, D-243-C026, D-243-C028, D-243-C031, D-243-C032, D-243-C033, D-243-C034]
  classification: [D-243-F010, D-243-F011, D-243-F012, D-243-F013, D-243-F014, D-243-F015, D-243-F024]
  call_sites:   [D-243-X020, D-243-X021, D-243-X022, D-243-X023, D-243-X024, D-243-X025, D-243-X026, D-243-X027, D-243-X028, D-243-X040, D-243-X041, D-243-X042, D-243-X043, D-243-X044, D-243-X045, D-243-X046, D-243-X047, D-243-X048, D-243-X049, D-243-X050, D-243-X051, D-243-X052, D-243-X056, D-243-X057, D-243-X058, D-243-X059]
  consumer_index: [D-243-I017, D-243-I018, D-243-I019, D-243-I020, D-243-I021]
  source_244_v_rows: ["GOX-01-V01..V08 (entry-gate shift — shared-scope for SDR-06)", "GOX-02-V01..V03 (State-1 revert coverage — primary closure for SDR-06 reach-path enumeration)", "GOX-03-V01..V03 (handleGameOverDrain subtraction — primary closure for SDR-03 foundation)", "GOX-04-V01..V02 (_livenessTriggered + EXC-02 envelope — adjacency for SDR-01 timing-f)", "GOX-06-V01 (_gameOverEntropy rngRequestTime clearing — adjacency for SDR-08)", "RNG-01-V11 (EXC-03 envelope non-widening at _unlockRng scope — primary cross-cite for SDR-08)"]
ki_envelope_re_verify:
  - exception: "EXC-03 (Gameover RNG substitution for mid-cycle write-buffer tickets — F-29-04 class)"
    re_verify_in_req: SDR-08
    rationale: "_gameOverEntropy at AdvanceModule:1286 introduces new `sdgnrs.resolveRedemptionPeriod` consumption of fallback entropy — verify EXC-03 envelope stays within mid-cycle-substitution acceptance boundary (no widening). Cross-cite Phase 244 RNG-01-V11 for the primary non-widening proof at the _unlockRng scope; SDR-08 owns the NEW redemption-resolve-consumption site within the same envelope."
source_preflag_bullets: "audit/v31-244-PER-COMMIT-AUDIT.md L2470-2521 §Phase-245-Pre-Flag bullets grouped under SDR-01 (2 bullets L2477-2478), SDR-02 (2 bullets L2481-2482), SDR-03 (1 bullet L2485), SDR-04 (1 bullet L2488), SDR-05 (1 bullet L2491), SDR-06 (1 bullet L2494), SDR-07 (1 bullet L2497), SDR-08 (1 bullet L2500). 10 bullets total for SDR bucket. Consumed as ADVISORY input per CONTEXT.md D-25 — each bullet gets a one-line closure note in the relevant §SDR-NN cross-walk."
must_haves:
  truths:
    - "audit/v31-245-SDR.md exists with frontmatter `Audit baseline: 7ab515fe`, `Audit head: cc68bfc7`, `Owning commits: 771893d1`, `Scope per: audit/v31-243-DELTA-SURFACE.md §6 D-243-I017..D-243-I021 + audit/v31-244-PER-COMMIT-AUDIT.md §4 GOX shared-context V-rows`."
    - "Section §SDR-01 enumerates ALL 6 redemption-state-transition timings (a)-(f) per REQUIREMENTS.md L72-78 with a foundation row per timing using the `SDR-01-T{a-f}` sub-scheme per CONTEXT.md D-15; each foundation row identifies reachable code paths + gameover-latch point per timing + which SDR-02..08 REQs cite it back. Also emits standard `SDR-01-V##` rows for the adversarial vectors (a)+(b)+(c)+(d) per CONTEXT.md D-14 and absorbs the claimRedemption ungated-by-design property per CONTEXT.md D-09 (NO standalone INFO candidate emitted for claimRedemption)."
    - "Section §SDR-02 enumerates per-entry-site + per-exit-site exact wei accounting for pendingRedemptionEthValue (entries: `_submitGamblingClaimFrom` sDGNRS:752-817 adds wei into ledger via pendingRedemptionEthBase→Value roll at :593; exits: claimRedemption sDGNRS:618-700 payout subtraction at :657, handleGameOverDrain GameOverModule:94 + :157 subtract-on-drain, _deterministicBurnFrom sDGNRS:535 subtract-from-payout-base) with one worked-example wei ledger per timing per CONTEXT.md D-10 + D-11."
    - "Section §SDR-03 verifies handleGameOverDrain reads IStakedDegenerusStonk.pendingRedemptionEthValue() at GameOverModule:94 (pre-refund) AND at :157 (post-refund) — NEITHER is a snapshot of a cached value; confirms 33/33/34 split at _sendToVault GameOverModule:225-233 operates on L158 `available` AFTER the subtractions; enumerates multi-tx drain edges (STAGE_TICKETS_WORKING partial drain, L80 `GO_JACKPOT_PAID` idempotency bit at :148) — cross-cites Phase 244 GOX-03-V01..V03 as primary closure and proves the DEEPER multi-tx edges SDR-03 owns."
    - "Section §SDR-04 verifies claimRedemption post-gameOver DOS-free / starvation-free / underflow-free / race-free across the 4-actor taxonomy {player, admin, validator, VRF oracle} per Phase 238 D-07; specifically: (DOS) no actor can force claimRedemption to revert, stall, or miscompute; (starvation) the 30-day `handleFinalSweep` window at GameOverModule:196-216 cannot close before a claimer can call; (underflow) no path decrements `pendingRedemptionEthValue` below the claimer's entitlement; (race) no ordering between drain + claim + sweep leaves reserved wei stranded."
    - "Section §SDR-05 closes per-wei ETH conservation across all 6 gameover timings from SDR-01 with one worked example per timing showing `sum(ENTRIES) == sum(EXITS)` invariant; cross-cites SDR-02 entry/exit primitives for the wei-level accounting."
    - "Section §SDR-06 enumerates every reach-path to _submitGamblingClaim* (via `burn` at sDGNRS:486-495 + `burnWrapped` at :506-516); proves each path is blocked in State-1 by `BurnsBlockedDuringLiveness` revert at sDGNRS:102-105; cross-cites Phase 244 GOX-02-V01/V02/V03 as PRIMARY closure; SDR-06 owns the DEEPER negative-space sweep (admin-initiated `purchaseStartDay` manipulation, level transitions mid-window per Pre-Flag bullet L2494, constructor path reachability — constructor claim-whale-pass is unreachable because game is not yet constructed)."
    - "Section §SDR-07 enumerates sDGNRS supply mutations (_mint constructor sDGNRS:307-308 + :874-, transferFromPool pool-to-recipient distributions sDGNRS:412-442, burn via _deterministicBurnFrom sDGNRS:539-541 equivalent, burnAtGameOver sDGNRS:462-476 zeros contract's own balance); proves every sDGNRS token has exactly one mint + at most one burn (no ghost tokens, no dust mint from transferFromPool rounding, no over-burn at burnAtGameOver); cross-cites v29.0 / v24.0 supply-conservation proofs as corroborating evidence per CONTEXT.md D-17 (RE_VERIFIED_AT_HEAD discipline — prior-milestone artifacts are corroborating only, never sole warrant)."
    - "Section §SDR-08 verifies `_gameOverEntropy` at AdvanceModule:1286 `sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay)` is a NEW consumption site of fallback entropy within the F-29-04 EXC-03 envelope; proves no VRF-pending redemption can hang in pending limbo post-gameOver (fallback resolve reaches every VRF-pending redemption), no over-substitution (fallback fires multiple times for same redemption), no under-resolution (fallback skips a pending redemption); cross-cites Phase 244 RNG-01-V11 as PRIMARY non-widening proof at the _unlockRng scope — SDR-08 owns the NEW redemption-resolve-consumption site within the same envelope. Annotation: `RE_VERIFIED_AT_HEAD cc68bfc7` for the EXC-03 envelope unchanged verdict per CONTEXT.md D-24."
    - "Every verdict row carries 8 columns per CONTEXT.md D-06: `Verdict Row ID | REQ-ID | Source Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA`."
    - "Verdict bucket per row drawn from {SAFE, INFO, LOW, MEDIUM, HIGH, CRITICAL} per CONTEXT.md D-08; KI envelope rows use `RE_VERIFIED_AT_HEAD cc68bfc7` annotation NOT a verdict bucket per CONTEXT.md D-24."
    - "Verdict Row IDs follow the `SDR-NN-V##` per-REQ-monotonic scheme per CONTEXT.md D-06; SDR-01 foundation rows use `SDR-01-T{a-f}` sub-scheme per CONTEXT.md D-15."
    - "REQ-summary table aggregates per-REQ closure: every REQ in {SDR-01..SDR-08} appears at least once as a Verdict Row ID prefix; per-REQ severity floor stated."
    - "All D-243 row subsets from D-243-I017..D-243-I021 are cited at least once in verdict-table Source Row(s) cells — Consumer Index integrity preserved per CONTEXT.md D-17."
    - "Phase 244 §Phase-245-Pre-Flag bullets 1-10 (SDR-grouped at v31-244-PER-COMMIT-AUDIT.md L2477-2500) each receive a one-line closure note in the relevant §SDR-NN cross-walk per CONTEXT.md D-25 (ADVISORY consumption — closure verdict may differ from the Pre-Flag's suggested vector)."
    - "Reproduction Recipe sub-section at end of file lists the exact `git show -L`, `grep -rn`, and reach-path commands used for reviewer replay (POSIX-portable per CONTEXT.md D-04 reproduction-recipe carry)."
    - "Zero F-31-NN finding-IDs emitted (CONTEXT.md D-23 — Phase 246 FIND-01 owns ID assignment); finding candidates use prose blocks with `SEVERITY: <bucket>` annotation only per CONTEXT.md D-08."
    - "Zero contracts/ or test/ writes (CONTEXT.md D-20 + feedback_no_contract_commits.md + feedback_never_preapprove_contracts.md); `git status --porcelain contracts/ test/` returns empty."
    - "Zero edits to audit/v31-243-DELTA-SURFACE.md or audit/v31-244-PER-COMMIT-AUDIT.md (CONTEXT.md D-22 — record any scope-guard deferral in plan SUMMARY only)."
    - "claimRedemption ungated-by-design property absorbed into SDR-01/04/05 per CONTEXT.md D-09 — NO standalone INFO finding-candidate is emitted (user locked this as property-to-prove SAFE, not convention-drift)."
  artifacts:
    - path: "audit/v31-245-SDR.md"
      provides: "SDR bucket (REQ SDR-01..SDR-08) per-REQ verdict tables + SDR-01 timing-matrix foundation rows (SDR-01-T{a-f}) + finding-candidate prose blocks. Working file consumed by 245-02 consolidation step into audit/v31-245-SDR-GOE.md."
      contains: "## §SDR-01"
  key_links:
    - from: "audit/v31-243-DELTA-SURFACE.md §6 D-243-I017..D-243-I021"
      to: "audit/v31-245-SDR.md §SDR-01..§SDR-08 verdict-table cells"
      via: "Source Row(s) column citation per CONTEXT.md D-06"
      pattern: "D-243-(C|F|X)[0-9]{3}"
    - from: "audit/v31-244-PER-COMMIT-AUDIT.md §4 GOX V-rows (GOX-01-V01..V08, GOX-02-V01..V03, GOX-03-V01..V03, GOX-06-V01) + §2 RNG V-rows (RNG-01-V11)"
      to: "audit/v31-245-SDR.md verdict-row Source Row(s) cells for shared-context cross-cites"
      via: "Phase 244 V-row cross-cite per CONTEXT.md D-17 (primary closure in 244; 245 extends to DEEPER negative-space per REQ)"
      pattern: "GOX-0[1-6]-V[0-9]+|RNG-01-V11"
    - from: "contracts/StakedDegenerusStonk.sol at HEAD cc68bfc7 (burn L486-495 / burnWrapped L506-516 / _deterministicBurnFrom L527-569 / resolveRedemptionPeriod L585-610 / claimRedemption L618-670 / _submitGamblingClaimFrom L752-817 / burnAtGameOver L462-476 / _mint L874 / transferFromPool L412-442) + contracts/modules/DegenerusGameGameOverModule.sol at HEAD cc68bfc7 (handleGameOverDrain L79-189 / _sendToVault L225-233 / handleFinalSweep L196-216) + contracts/modules/DegenerusGameAdvanceModule.sol at HEAD cc68bfc7 (_gameOverEntropy L1228-1306 + :1286 resolveRedemptionPeriod call)"
      to: "audit/v31-245-SDR.md verdict Evidence cells"
      via: "per-wei ledger argument-trace + reach-path enumeration + State-1 revert proof + supply-mutation enumeration + fallback-entropy consumption proof"
      pattern: "pendingRedemptionEthValue|_submitGamblingClaim|BurnsBlockedDuringLiveness|_deterministicBurnFrom|handleGameOverDrain|resolveRedemptionPeriod|_gameOverEntropy|claimRedemption|burnAtGameOver"
---

<objective>
Execute the adversarial audit of the sDGNRS redemption lifecycle × gameover-timing matrix against the `771893d1` delta (liveness-gate shift + `pendingRedemptionEthValue` drain-subtraction + `_gameOverEntropy` redemption-resolve fallback + 14-day VRF-dead grace). Close all 8 SDR requirements (SDR-01..SDR-08) with per-REQ verdict tables + SDR-01 timing-matrix foundation rows (SDR-01-T{a-f}) + finding-candidate prose blocks in working file `audit/v31-245-SDR.md`.

Scope sources (READ-only per CONTEXT.md D-20 + D-22):
- `audit/v31-243-DELTA-SURFACE.md` §6 Consumer Index rows D-243-I017..D-243-I021
- `audit/v31-244-PER-COMMIT-AUDIT.md` §4 GOX V-rows (GOX-01..GOX-06) as shared-context + §2 RNG-01-V11 for the EXC-03 envelope non-widening proof
- `audit/v31-244-PER-COMMIT-AUDIT.md` §Phase-245-Pre-Flag L2470-2521 bullets 1-10 (SDR-grouped) as ADVISORY input per CONTEXT.md D-25
- `audit/KNOWN-ISSUES.md` "Gameover RNG substitution for mid-cycle write-buffer tickets" entry (EXC-03) for SDR-08 envelope RE_VERIFIED_AT_HEAD annotation per CONTEXT.md D-24

Per CONTEXT.md D-03: SDR-01 uses per-REQ re-walk representation (foundation rows `SDR-01-T{a-f}` + standard `SDR-01-V##` adversarial-vector rows). NO mega-matrix; each of SDR-02..08 cites foundation rows back but produces its own verdicts.

Per CONTEXT.md D-09 (user-locked gray area): `claimRedemption` at sDGNRS:618 is property-to-prove SAFE absorbed into SDR-01/04/05. NO standalone INFO finding-candidate emitted for the ungated entry point.

Per CONTEXT.md D-10 + D-11 (user-locked gray area): SDR-02 + SDR-05 use prose + spot-check format (one worked wei example per timing). NOT formal invariant-lemma algebra.

Per CONTEXT.md D-23: Zero F-31-NN finding-IDs emitted — Phase 246 FIND-01 owns ID assignment. This plan emits finding-candidate prose blocks with `SEVERITY: <bucket>` annotations per CONTEXT.md D-08 6-bucket taxonomy.

Per CONTEXT.md D-21: HEAD anchor `cc68bfc7` locked. Verify `git diff cc68bfc7..HEAD -- contracts/` returns empty before each task (current HEAD is 36f68c1d at plan-start; contracts/ deltas = zero, verified at planning time).

Output: `audit/v31-245-SDR.md` is the working file. Per CONTEXT.md D-05, 245-02 consolidates this working file + `audit/v31-245-GOE.md` into `audit/v31-245-SDR-GOE.md` (FINAL READ-only) at SUMMARY-commit time.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/245-sdgnrs-redemption-gameover-safety/245-CONTEXT.md
@.planning/phases/244-per-commit-adversarial-audit-evt-rng-qst-gox/244-04-SUMMARY.md
@audit/v31-243-DELTA-SURFACE.md
@audit/v31-244-PER-COMMIT-AUDIT.md
@KNOWN-ISSUES.md
</context>

<threat_model>
Per CONTEXT.md D-14 (SDR adversarial vectors — fixed, planner MUST cover at minimum). Actor taxonomy carries from Phase 238 D-07 (player / admin / validator / VRF oracle). This plan is READ-only — zero new contract attack surface introduced; the threat model below enumerates the vectors the AUDIT must close on, NOT new code to ship.

**Trust boundaries (within SDR scope):**

| Boundary | Description |
|---|---|
| `sDGNRS.burn / burnWrapped (State-0) → _submitGamblingClaimFrom → pendingRedemptionEthValue ledger` | player-facing entry; wei enters the reserved ledger at State-0 (liveness not yet fired) — must exit via exactly one path (claimRedemption payout OR handleGameOverDrain subtract-as-reserve OR roll-back to pool via resolveRedemptionPeriod roll-below-100) |
| `resolveRedemptionPeriod (State-0 normal-tick OR State-2 gameover-entropy)` | single function called from TWO distinct contexts — normal `rngGate` advanceGame (State-0/1) AND `_gameOverEntropy` L1286 (State-2); must resolve correctly in both contexts without double-resolve or missed-resolve |
| `claimRedemption (any state, gated by redemptionPeriods[].roll != 0)` | back-half of 2-step flow; ungated-by-state-by-design per CONTEXT.md D-09 (implicit `roll != 0` gate is algorithmically load-bearing — cannot claim unresolved) |
| `handleGameOverDrain (State-2) → IStakedDegenerusStonk.pendingRedemptionEthValue() external call at GameOverModule:94 + :157` | new external read-only call; subtraction must precede 33/33/34 split at _sendToVault:225-233 AND must occur at BOTH pre-refund (L94) AND post-refund (L157) sites |
| `handleFinalSweep (State-2, day+30) → _sendToVault total-funds sweep at GameOverModule:196-216` | 30-day window MUST be sufficient for all pending claimants; does NOT re-subtract pendingRedemptionEthValue — relies on claimRedemption being called first |
| `_gameOverEntropy (State-2, VRF-dead 14-day grace) → sdgnrs.resolveRedemptionPeriod at AdvanceModule:1286` | new consumption of fallback entropy within EXC-03 envelope — must stay within mid-cycle-substitution acceptance boundary per KI EXC-03 |
| `_deterministicBurnFrom subtraction at sDGNRS:535 (total = ethBal + stethBal + claimableEth - pendingRedemptionEthValue)` | NEW subtraction introduced alongside 771893d1 — excludes reserved gambling-burn ETH from deterministic-burn payout base to prevent double-counting |

**STRIDE-mapped per-REQ vectors (transcribed from CONTEXT.md D-14):**

| Threat ID | REQ | Category | Component | Disposition | Mitigation Plan / Adversarial Vector |
|---|---|---|---|---|---|
| T-245-SDR-01a | SDR-01 | Tampering / Information-Disclosure | 6 gameover-timing transitions (a)-(f) per REQUIREMENTS.md L72-78 | mitigate-by-audit | Enumerate transitions (a) pre-liveness all 3 steps; (b) request pre-liveness, resolve/claim in State-1; (c) request pre-liveness, resolve State-1, claim post-gameOver; (d) resolved pre-gameOver, claim post-gameOver; (e) request post-gameOver blocked; (f) VRF-pending at liveness, resolves via _gameOverEntropy fallback. Foundation rows SDR-01-T{a-f} identify reachable code paths per timing. |
| T-245-SDR-01b | SDR-01 | Information-Disclosure | per-timing gameover-latch point identification | mitigate-by-audit | For each of 6 timings, identify which STATE transition latches gameOver (or confirms gameOver does NOT latch in the timing). Cross-cite Phase 244 GOX-04-V01/V02 for the liveness-firing predicates + GOX-06-V02 for _handleGameOverPath ordering. |
| T-245-SDR-01c | SDR-01 | Information-Disclosure (claimRedemption ungated property) | claimRedemption sDGNRS:618 ungated-by-state per CONTEXT.md D-09 | mitigate-by-audit (absorb into SDR-01/04/05) | Prove the implicit `redemptionPeriods[claim.periodIndex].roll != 0` gate is algorithmically load-bearing under ALL 6 timings AND all 4 actor classes (player/admin/validator/VRF oracle). No standalone INFO finding-candidate emitted per D-09. |
| T-245-SDR-02a | SDR-02 | Tampering / Information-Disclosure | per-entry-site exact accounting — `_submitGamblingClaimFrom` sDGNRS:752-817 + period-resolve roll-adjust at :593 | mitigate-by-audit | Trace every wei entering pendingRedemptionEthValue via the `pendingRedemptionEthBase += segregatedEth` then `pendingRedemptionEthValue = pendingRedemptionEthValue - pendingRedemptionEthBase + rolledEth` (sDGNRS:593) period-close formula. Verify no wei double-enters or skips entry. |
| T-245-SDR-02b | SDR-02 | Tampering | per-exit-site exact accounting — claimRedemption sDGNRS:619-670 payout subtract at :657, handleGameOverDrain subtract at GameOverModule:94 + :157, _deterministicBurnFrom subtract at sDGNRS:535 | mitigate-by-audit | Trace every exit; one worked wei example per timing showing ledger closes (no dust, no overshoot). |
| T-245-SDR-03a | SDR-03 | Tampering | handleGameOverDrain reads pendingRedemptionEthValue() at GameOverModule:94 (pre-refund) AND :157 (post-refund), NOT a cached snapshot | mitigate-by-audit | Confirm both read sites use direct IStakedDegenerusStonk interface call; confirm subtraction precedes 33/33/34 split at _sendToVault:225-233; cross-cite Phase 244 GOX-03-V01..V03 as primary closure. |
| T-245-SDR-03b | SDR-03 | Tampering | multi-tx drain edges — STAGE_TICKETS_WORKING partial drain, L80 GO_JACKPOT_PAID idempotency bit at :148 | mitigate-by-audit | Enumerate every way handleGameOverDrain can re-enter; prove no path re-enters drain that double-subtracts OR skips subtraction. Pre-Flag L2485 explicitly calls for deeper drain-iteration edge analysis. |
| T-245-SDR-04a | SDR-04 | DOS / Elevation-of-Privilege | claimRedemption post-gameOver across 4-actor taxonomy | mitigate-by-audit | (player) — verify no calldata can revert claimRedemption; (admin) — verify admin cannot disable or stall claims; (validator) — verify tx ordering cannot DOS claims; (VRF oracle) — verify VRF state does not block claimRedemption (only roll != 0 gate fires). |
| T-245-SDR-04b | SDR-04 | DoS (starvation) | 30-day handleFinalSweep window at GameOverModule:196-216 cannot close before claimer can call | mitigate-by-audit | Verify handleFinalSweep is NOT admin-callable before day+30 (or document if it is); verify no realistic actor scenario leaves pendingRedemptionEthValue stranded when the 30-day window closes. |
| T-245-SDR-04c | SDR-04 | Tampering (underflow) | claimRedemption decrement at sDGNRS:657 cannot underflow pendingRedemptionEthValue below the claimer's entitlement | mitigate-by-audit | Trace `pendingRedemptionEthValue -= totalRolledEth` at sDGNRS:657; verify totalRolledEth is always <= pendingRedemptionEthValue by construction (period-close invariant). |
| T-245-SDR-04d | SDR-04 | Tampering (race) | ordering between handleGameOverDrain + claimRedemption + handleFinalSweep cannot leave wei stranded | mitigate-by-audit | Enumerate all permutations of drain/claim/sweep tx ordering across State-2 and day+30; prove no wei stranded. |
| T-245-SDR-05a | SDR-05 | Tampering | per-wei ETH conservation across all 6 gameover timings | mitigate-by-audit | Apply SDR-02 entry/exit primitives per timing; one worked wei example per timing showing `sum(ENTRIES) == sum(EXITS)` invariant (wei reaches claimer OR returns to pool, never both, never neither). |
| T-245-SDR-06a | SDR-06 | Tampering / Elevation-of-Privilege | every reach-path to _submitGamblingClaim* blocked in State-1 by BurnsBlockedDuringLiveness revert | mitigate-by-audit | Enumerate every reach-path: burn at sDGNRS:486-495 (callers D-243-X022 DegenerusStonk wrapper + D-243-X023 DegenerusStonk.yearSweep + D-243-X024 DegenerusVault wrapper), burnWrapped at :506-516 (no programmatic callers per D-243-X025 — player-facing external only). Each path has the revert at State-1. Cross-cite Phase 244 GOX-02-V01/V02/V03 as PRIMARY closure. |
| T-245-SDR-06b | SDR-06 | Tampering (negative-space) | admin-initiated paths, constructor paths, cross-chain forward-imported state — DEEPER sweep beyond 244 GOX-02 depth | mitigate-by-audit | Per Pre-Flag L2494: verify admin-triggered `purchaseStartDay` manipulation cannot create a new redemption in State-1; level transitions mid-window; sDGNRS constructor claim-whale-pass is unreachable because game is not yet constructed. |
| T-245-SDR-07a | SDR-07 | Tampering / Information-Disclosure | sDGNRS supply conservation across full lifecycle including gameover interception | mitigate-by-audit | Enumerate supply mutations: _mint constructor sDGNRS:307-308 + :874+ (pool + creator); transferFromPool sDGNRS:412-442 (pool distributions — per-call `transferred <= amount` invariant); _deterministicBurnFrom sDGNRS:527-569 burn; burnAtGameOver sDGNRS:462-476 contract-self-burn. Prove exactly-one-mint + at-most-one-burn per token. Cross-cite v29.0 / v24.0 supply-conservation proofs as corroborating. |
| T-245-SDR-07b | SDR-07 | Tampering (dust) | transferFromPool rounding edge at sDGNRS:412-442 | mitigate-by-audit | Verify no dust mint from `transferred < amount` path — the function RETURNS transferred, and the BPS-rounded-down residual stays in the pool (never leaves the supply). |
| T-245-SDR-08a | SDR-08 | Tampering / KI envelope (EXC-03) | _gameOverEntropy at AdvanceModule:1286 sdgnrs.resolveRedemptionPeriod new fallback-entropy consumption | RE_VERIFY only per D-24 | Verify the new redemption-resolve-consumption stays within the EXC-03 envelope (mid-cycle write-buffer-tickets acceptance — terminal-state only, VRF-derived or VRF+prevrandao admixture, no player-reachable exploit). Cross-cite Phase 244 RNG-01-V11 as PRIMARY non-widening proof at the _unlockRng scope. Annotation: `RE_VERIFIED_AT_HEAD cc68bfc7`. |
| T-245-SDR-08b | SDR-08 | Tampering (fairness) | VRF-pending redemption reaches resolve via fallback — no pending-limbo post-gameOver | mitigate-by-audit | Verify _gameOverEntropy branches (VRF-available at :1237-1259 vs prevrandao-fallback at :1264-1293) each call sdgnrs.resolveRedemptionPeriod exactly once per gameover tick; no over-substitution (fallback fires multiple times for same redemption); no under-resolution (fallback skips a pending redemption). |

**Acceptance disposition rule:** Each row above MUST close with one of {SAFE, INFO, LOW, MEDIUM, HIGH, CRITICAL} per CONTEXT.md D-08, OR `RE_VERIFIED_AT_HEAD cc68bfc7` for KI envelope rows (SDR-08a). Severity pre-classification per CONTEXT.md Discretion — planner may pre-classify or leave `SEVERITY: TBD-246` for ambiguous candidates.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: SDR-01 timing-matrix foundation (6 rows SDR-01-T{a-f} + standard SDR-01-V## rows) + SDR-02 pendingRedemptionEthValue accounting exactness + SDR-03 handleGameOverDrain subtraction depth — close SDR-01 + SDR-02 + SDR-03</name>
  <files>audit/v31-245-SDR.md</files>
  <read_first>
    - .planning/phases/245-sdgnrs-redemption-gameover-safety/245-CONTEXT.md — D-01 (2-plan split), D-02 (single-wave parallel), D-03 (per-REQ re-walk, no mega-matrix), D-04 (single consolidated audit/v31-245-SDR-GOE.md output shape), D-05 (per-plan working file pattern with consolidation in 245-02), D-06 (8-column verdict-table format), D-07 (per-REQ closure with cross-cite), D-08 (6-bucket severity), D-09 (claimRedemption ungated property-to-prove SAFE absorbed into SDR-01/04/05), D-10 + D-11 (per-wei accounting rigor — prose + spot-check + one worked example per timing), D-14 SDR-01/02/03 adversarial vectors, D-15 (SDR-01-T{a-f} foundation row scheme), D-17 (RE_VERIFIED_AT_HEAD methodology), D-20 (READ-only), D-21 (HEAD anchor cc68bfc7), D-22 (no edits to v31-243-DELTA-SURFACE.md or v31-244-PER-COMMIT-AUDIT.md), D-23 (no F-31-NN), D-25 (Phase 244 Pre-Flag advisory — each bullet gets one-line closure note)
    - audit/v31-243-DELTA-SURFACE.md — §1.4 commit-771893d1 changelog rows D-243-C013 (burn at sDGNRS:486-495), D-243-C014 (burnWrapped at sDGNRS:506-516), D-243-C017 (handleGameOverDrain at GameOverModule:79-189 — reserved-subtraction hunk at L86-90 + L94 + L157), D-243-C032 (IStakedDegenerusStonk.pendingRedemptionEthValue interface ADDED), D-243-C034 (BurnsBlockedDuringLiveness error ADDED at sDGNRS:102-105)
    - audit/v31-243-DELTA-SURFACE.md — §2.3 classification rows D-243-F011 (burn MODIFIED_LOGIC), D-243-F012 (burnWrapped MODIFIED_LOGIC), D-243-F015 (handleGameOverDrain MODIFIED_LOGIC)
    - audit/v31-243-DELTA-SURFACE.md — §3.1.11 D-243-X022/X023/X024 (burn callers — DegenerusStonk wrapper at :227 + DegenerusStonk.yearSweep at :304 + DegenerusVault wrapper at :740); §3.1.12 D-243-X025 (burnWrapped — zero programmatic callers, player-facing only); §3.1.15 D-243-X028 (handleGameOverDrain delegatecall selector); §3.2.3 D-243-X058 (pendingRedemptionEthValue interface call site at handleGameOverDrain:94); §3.2.3 D-243-X059 (pendingRedemptionEthValue at handleGameOverDrain:157)
    - audit/v31-243-DELTA-SURFACE.md — §6 Consumer Index rows D-243-I017 (SDR-06/GOX-02 coverage — burn/burnWrapped State-1 block), D-243-I018 (SDR-03/GOX-03 coverage — handleGameOverDrain subtraction)
    - audit/v31-244-PER-COMMIT-AUDIT.md — §4 GOX-02 V-rows V01/V02/V03 (burn + burnWrapped State-1 block — shared-context for SDR-01 reach-path enumeration); §4 GOX-03 V-rows V01/V02/V03 (handleGameOverDrain subtraction primary closure — shared-context for SDR-03); §4 GOX-04 V-rows V01/V02 (liveness-firing predicates — shared-context for SDR-01 timing-latch identification); §Phase-245-Pre-Flag bullets at L2477-2478 (SDR-01), L2481-2482 (SDR-02), L2485 (SDR-03)
    - contracts/StakedDegenerusStonk.sol at HEAD cc68bfc7:
      - L102-105 — BurnsBlockedDuringLiveness error declaration (D-243-C034)
      - L221-227 — pendingRedemptions mapping + pendingRedemptionEthValue storage + pendingRedemptionBurnie/Base storage (NatSpec at L104-: "reserved ETH is swept by handleGameOverDrain before claimRedemption can run")
      - L486-495 — burn body (D-243-C013): L487 gameOver short-circuit → L491 if(livenessTriggered()) revert → L492 if(rngLocked()) revert → L493 _submitGamblingClaim
      - L506-516 — burnWrapped body (D-243-C014): L507 `livenessTriggered() && !gameOver()` revert → L509 burnForSdgnrs → L510-: gameOver short-circuit → L513 rngLocked revert → L514 _submitGamblingClaimFrom
      - L527-569 — _deterministicBurnFrom body: L535 `totalMoney = ethBal + stethBal + claimableEth - pendingRedemptionEthValue` (NEW 771893d1 subtraction)
      - L577-583 — hasPendingRedemptions view
      - L585-610 — resolveRedemptionPeriod body: L592 `rolledEth = pendingRedemptionEthBase * roll / 100`; L593 `pendingRedemptionEthValue = pendingRedemptionEthValue - pendingRedemptionEthBase + rolledEth` (period-close wei-conservation formula); L594 `pendingRedemptionEthBase = 0`
      - L618-670 — claimRedemption body: L620 `claim = pendingRedemptions[player]`; implicit gate via roll != 0 check (loops over resolved periods); L657 `pendingRedemptionEthValue -= totalRolledEth` (payout decrement); L661 delete pendingRedemptions[player]
      - L744-817 — _submitGamblingClaimFrom body: burn operation + segregatedEth update to pendingRedemptionEthBase (then accumulated to pendingRedemptionEthValue on resolve at :593)
    - contracts/modules/DegenerusGameGameOverModule.sol at HEAD cc68bfc7:
      - L79-189 — handleGameOverDrain body: L80 GO_JACKPOT_PAID idempotency bit; L86-90 reserved-subtraction hunk (claimablePool + pendingRedemptionEthValue); L94 first pendingRedemptionEthValue() call (pre-refund); L148 `_goWrite(GO_JACKPOT_PAID_SHIFT, GO_JACKPOT_PAID_MASK, 1)` (idempotency set); L157 second pendingRedemptionEthValue() call (post-refund); L186 `_sendToVault(remaining, stBal)` (33/33/34 split)
      - L196-216 — handleFinalSweep body: 30-day post-gameOver window; L215 `_sendToVault(totalFunds, stBal)` (no re-subtraction of pendingRedemptionEthValue)
      - L225-233 — _sendToVault body (33/33/34 split math)
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD cc68bfc7:
      - L1193 — normal-tick `sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay)` (rngGate path — State-0/1 resolve)
      - L1286 — gameover-path `sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay)` (_gameOverEntropy — State-2 fallback resolve)
    - contracts/interfaces/IStakedDegenerusStonk.sol at HEAD cc68bfc7 — L88-90 pendingRedemptionEthValue interface declaration (external view returns uint256)
  </read_first>
  <action>
Execute the SDR-01 + SDR-02 + SDR-03 audit pass. This task owns the foundation (SDR-01 timing matrix + the per-wei accounting frame + the drain-subtraction depth analysis).

**Step A — Sanity gate (mandatory before any write):**

Run exactly:
```
git rev-parse 7ab515fe
git rev-parse cc68bfc7
git rev-parse HEAD
git diff --stat cc68bfc7..HEAD -- contracts/
git status --porcelain contracts/ test/
```

Locked expectations:
- `git rev-parse cc68bfc7` resolves to `cc68bfc70e76fb75ac6effbc2135aae978f96ff3`
- `git diff --stat cc68bfc7..HEAD -- contracts/` reports zero contracts/ changes (only .planning/ + audit/ appends have landed since CONTEXT-lock)
- `git status --porcelain contracts/ test/` returns empty

If `git diff cc68bfc7..HEAD -- contracts/` is non-empty, STOP. Record a scope-guard deferral in the plan SUMMARY and abort — the contract surface has drifted past the CONTEXT-locked anchor per CONTEXT.md D-21.

**Step B — Create file shell with literal sections:**

Write `audit/v31-245-SDR.md` with:

```
# v31.0 Phase 245 — SDR Bucket Audit (commit 771893d1 — sDGNRS redemption deep sub-audit)

Audit baseline: 7ab515fe
Audit head:     cc68bfc7
Owning commits: 771893d1 (gameover liveness + sDGNRS redemption protection — primary SDR driver)
Scope per:      audit/v31-243-DELTA-SURFACE.md §6 D-243-I017..D-243-I021 + audit/v31-244-PER-COMMIT-AUDIT.md §4 GOX shared-context V-rows + §Phase-245-Pre-Flag L2477-2500 (SDR-grouped bullets)
Plan:           245-01-PLAN.md
Phase:          245-sdgnrs-redemption-gameover-safety

## §0 — Per-Bucket Verdict Count Card

| REQ-ID | Verdict Rows | Finding Candidates | KI Envelope Status | Floor Severity |
| --- | --- | --- | --- | --- |
| SDR-01 | (filled at task close — includes 6 SDR-01-T{a-f} foundation rows + N standard SDR-01-V## rows) | (filled) | n/a | (filled) |
| SDR-02 | (filled) | (filled) | n/a | (filled) |
| SDR-03 | (filled) | (filled) | n/a | (filled) |
| SDR-04 | (filled at Task 2) | (filled) | n/a | (filled) |
| SDR-05 | (filled at Task 2) | (filled) | n/a | (filled) |
| SDR-06 | (filled at Task 2) | (filled) | n/a | (filled) |
| SDR-07 | (filled at Task 3) | (filled) | n/a | (filled) |
| SDR-08 | (filled at Task 3) | (filled) | EXC-03 RE_VERIFIED_AT_HEAD cc68bfc7 | (filled at Task 3) |

## §SDR-01 — Redemption state-transition × gameover-timing matrix (6 timings + claimRedemption ungated-by-design property)

(Foundation rows SDR-01-T{a-f} + standard SDR-01-V## adversarial-vector rows)

## §SDR-02 — pendingRedemptionEthValue accounting exactness (per-entry + per-exit wei ledger)

(Verdict table + prose + one worked wei example per timing per CONTEXT.md D-10/D-11)

## §SDR-03 — handleGameOverDrain subtracts full pendingRedemptionEthValue BEFORE 33/33/34 split (multi-tx drain depth)

(Verdict table + multi-tx drain edge enumeration + GO_JACKPOT_PAID idempotency proof)

(§SDR-04..SDR-08 appended by Tasks 2 + 3.)

## §Phase-244-Pre-Flag Cross-Walk — SDR bucket (10 bullets at v31-244-PER-COMMIT-AUDIT.md L2477-2500 per CONTEXT.md D-25)

(One-line closure note per bullet — appended incrementally as each REQ closes.)

## §Reproduction Recipe — SDR bucket

(Commands appended at task close — Tasks 2 + 3 append their own commands here too.)
```

**Step C — SDR-01 timing-matrix foundation (6 rows SDR-01-T{a-f} per CONTEXT.md D-15):**

For each of the 6 timings from REQUIREMENTS.md L72-78 and CONTEXT.md D-14 SDR-01 vector (a), produce one foundation row. Format:

| Timing | Label | Reachable code paths | Gameover-latch point | Referenced by SDR-NN V-rows |
| SDR-01-Ta | (a) pre-liveness all 3 steps | burn/burnWrapped (State-0) → _submitGamblingClaimFrom (sDGNRS:744/752) → pendingRedemptionEthBase accumulates → advanceGame normal-tick → resolveRedemptionPeriod (sDGNRS:585 via AdvanceModule:1193 rngGate path) → claimRedemption (sDGNRS:618) | N/A — timing completes before liveness or gameover fires | SDR-02, SDR-05, SDR-07 |
| SDR-01-Tb | (b) request pre-liveness, resolve+claim in State-1 | burn (State-0) → _submitGamblingClaimFrom → advanceGame tick that fires livenessTriggered (day-math threshold crossed at DegenerusGameStorage:1235-1243 per GOX-04-V01) → resolveRedemptionPeriod in State-1 normal-tick path OR resolve defers to _gameOverEntropy if gameOver latches → claimRedemption (ungated) | Liveness fires at advanceGame tick T_N; State-1 persists until State-2 latches at handleGameOverDrain invocation | SDR-02, SDR-04, SDR-05, SDR-06 |
| SDR-01-Tc | (c) request pre-liveness, resolve State-1, claim post-gameOver | burn (State-0) → _submitGamblingClaim → resolve in State-1 (period-close at :593) → handleGameOverDrain fires (day+X) → claimRedemption (State-2, any time before handleFinalSweep day+30) | handleGameOverDrain invocation sets GO_JACKPOT_PAID bit at :148 | SDR-03, SDR-04, SDR-05 |
| SDR-01-Td | (d) resolved pre-gameOver, claim post-gameOver | burn (State-0) → _submitGamblingClaim → resolve (normal-tick or State-1) → handleGameOverDrain subtracts pendingRedemptionEthValue at :94 + :157 → claimRedemption (State-2) | handleGameOverDrain invocation | SDR-03, SDR-04 |
| SDR-01-Te | (e) request post-gameOver — BLOCKED | burn (State-2) → L487 `if (game.gameOver()) { return ...; }` gameOver short-circuit → deterministic-burn path via _deterministicBurnFrom → NO redemption created; burnWrapped (State-2) → L509 `if (game.gameOver()) { return ...; }` gameOver short-circuit path | gameOver already latched | SDR-06, SDR-07 |
| SDR-01-Tf | (f) VRF-pending at liveness, resolves via _gameOverEntropy fallback | burn (State-0) → _submitGamblingClaim → advanceGame fires liveness (day-math OR Tier-1 14-day VRF-dead grace per GOX-04-V01) → VRF request pending → _handleGameOverPath at AdvanceModule:523-630 → _gameOverEntropy at :1228-1306 → L1286 sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay) → claimRedemption (State-2) | _handleGameOverPath gameOver-branch set | SDR-08, SDR-05 |

**SDR-01 standard verdict rows** (vectors SDR-01a + SDR-01b + SDR-01c per D-14):

| Verdict Row ID | REQ-ID | Source Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| SDR-01-V01 | SDR-01 | D-243-C013, D-243-C014, D-243-F011, D-243-F012 | contracts/StakedDegenerusStonk.sol:486-516 | SDR-01a all 6 timings reachable-path enumeration (foundation rows SDR-01-T{a-f}) | <bucket — SAFE expected> | Per-timing reach-path per foundation rows above; no extra path surfaces beyond the 6 | 771893d1 |
| SDR-01-V02 | SDR-01 | D-243-C013/C014/C015/C017/C026, GOX-04-V01/V02, GOX-06-V02 | contracts/modules/DegenerusGameStorage.sol:1235-1243 + contracts/modules/DegenerusGameAdvanceModule.sol:519-630 | SDR-01b per-timing gameover-latch point identification | <bucket> | cross-cite Phase 244 GOX-04-V01 (liveness predicate at Storage:1241-1242) + GOX-06-V02 (_handleGameOverPath ordering at AdvanceModule:519-630) — State-1→State-2 latch on first handleGameOverDrain invocation; State-0→State-1 latch at day-math or 14-day VRF-dead grace | 771893d1 |
| SDR-01-V03 | SDR-01 | Pre-Flag bullet L2477 (claimRedemption ungated) | contracts/StakedDegenerusStonk.sol:618-670 | SDR-01c claimRedemption ungated-by-design property-to-prove SAFE per CONTEXT.md D-09 (absorbed into SDR-01) | <SAFE expected> | Implicit `claim.roll != 0` gate: loop over redemptionPeriods[claim.periodIndex] — unresolved periods (roll == 0) contribute zero to totalRolledEth; no state-gate reverts; player cannot pre-claim unresolved redemptions; all 4 actor classes (player/admin/validator/VRF oracle) cannot bypass the roll != 0 gate. NO standalone INFO finding-candidate emitted per D-09. | 771893d1 |

Append a one-line Pre-Flag closure note block under §SDR-01:
- Bullet L2477 (claimRedemption ungated) — CLOSED via SDR-01-V03 (property-to-prove SAFE per CONTEXT.md D-09).
- Bullet L2478 (resolveRedemptionPeriod two callers) — CLOSED via SDR-01-Ta foundation row (normal-tick path at AdvanceModule:1193) + SDR-01-Tf (gameover-path at AdvanceModule:1286); both timings covered by foundation enumeration.

**Step D — SDR-02 per-entry + per-exit wei accounting (vectors SDR-02a + SDR-02b per D-14 + CONTEXT.md D-10/D-11 methodology):**

**Per-entry sites** (wei enters pendingRedemptionEthValue):

Read `_submitGamblingClaimFrom` body sDGNRS:752-817 verbatim. Identify:
- `segregatedEth` calculation based on burn amount
- `pendingRedemptionEthBase += segregatedEth` (period-local accumulator)
- `pendingRedemptions[player]` struct write (burn amount + flip day)

Then on period-close (resolveRedemptionPeriod sDGNRS:585-610):
- L592 `rolledEth = (pendingRedemptionEthBase * roll) / 100`
- L593 `pendingRedemptionEthValue = pendingRedemptionEthValue - pendingRedemptionEthBase + rolledEth` (the exact formula — `pendingRedemptionEthBase` LEAVES the ledger; `rolledEth` (possibly less) ENTERS the running total)
- L594 `pendingRedemptionEthBase = 0` (reset for next period)

Wei semantic (CRITICAL): the ENTRY is NOT at `_submitGamblingClaimFrom` time; it is at `resolveRedemptionPeriod` time when `rolledEth` is added to `pendingRedemptionEthValue`. Between burn-time and resolve-time, wei sits in `pendingRedemptionEthBase`. This is a 2-stage entry.

**Per-exit sites** (wei exits pendingRedemptionEthValue):

1. **Exit via claimRedemption** at sDGNRS:618-670:
   - L657 `pendingRedemptionEthValue -= totalRolledEth;`
   - totalRolledEth is the sum of `rolledEth` across all claimed periods for msg.sender
   - L661 `delete pendingRedemptions[player];` clears the struct
2. **Exit via handleGameOverDrain** at GameOverModule:94 + :157:
   - L94 `IStakedDegenerusStonk(ContractAddresses.SDGNRS).pendingRedemptionEthValue()` — READ + subtracted from `reserved = uint256(claimablePool) + pendingRedemptionEthValue()` (reserved ETH stays in sDGNRS contract, does NOT leave the sDGNRS balance — only excluded from the 33/33/34 drain split; wei still sits in sDGNRS for later claimRedemption payout)
   - L157 `IStakedDegenerusStonk(ContractAddresses.SDGNRS).pendingRedemptionEthValue()` — READ (post-refund) + subtracted from `available` for the remaining drain split
3. **Exit via _deterministicBurnFrom** at sDGNRS:535:
   - `totalMoney = ethBal + stethBal + claimableEth - pendingRedemptionEthValue;` — wei STAYS in the ledger (not a real exit — the subtraction just excludes reserved wei from the deterministic-burn payout base)

**Rationale for "exit" framing:** handleGameOverDrain L94/L157 and _deterministicBurnFrom L535 do NOT decrement pendingRedemptionEthValue — they READ-SUBTRACT from an OUT-going payout. The only actual decrement sites are claimRedemption :657 (payout) and resolveRedemptionPeriod :593 (roll-below-100 return to pool). Wei-conservation invariant closes accordingly: for every wei entering pendingRedemptionEthValue via :593, exactly one wei exits via EITHER :657 (paid out to claimer) OR via future :593 roll-below-100 (returned to pool as the `pendingRedemptionEthBase LEAVES - rolledEth RE-ENTERS` delta when roll < 100).

Wait — CRITICAL re-read: at :593, the formula is `pendingRedemptionEthValue - pendingRedemptionEthBase + rolledEth`. If roll < 100, rolledEth < pendingRedemptionEthBase — so `pendingRedemptionEthValue` DECREASES by (pendingRedemptionEthBase - rolledEth). This is the "roll-back-to-pool" exit (though more precisely, the wei stays in the sDGNRS balance; it's just no longer RESERVED for any claimer).

**Per-timing wei-ledger worked examples** (one per timing per CONTEXT.md D-11):

For each of SDR-01-T{a-f}, produce a worked example:

Example (Timing Ta — pre-liveness all 3 steps):
```
t=0   _submitGamblingClaimFrom: player burns 100 sDGNRS
      segregatedEth = 10 wei (hypothetical)
      pendingRedemptionEthBase += 10 → 10
      pendingRedemptionEthValue unchanged (still 0)
t=1   advanceGame normal-tick: resolveRedemptionPeriod(roll=80, flipDay)
      rolledEth = (10 * 80) / 100 = 8
      pendingRedemptionEthValue = 0 - 10 + 8 = -2 ← WAIT
```

(The formula requires pendingRedemptionEthValue >= pendingRedemptionEthBase for no underflow. Since Solidity unsigned arithmetic reverts on underflow at the EVM level if we're in a checked block, this relies on an invariant. VERIFY: is the subtraction in an `unchecked` block at sDGNRS:593? Read the source, confirm.)

Handle the underflow question as part of the worked-example derivation. If the subtraction is in `unchecked`, verify the invariant `pendingRedemptionEthValue >= pendingRedemptionEthBase` is preserved by construction (every `pendingRedemptionEthBase += X` is matched by a later `pendingRedemptionEthValue = ... - pendingRedemptionEthBase + rolledEth` that adds the same flow).

Correct the worked example based on the actual invariant (likely the first period starts with `pendingRedemptionEthBase` seeded, `pendingRedemptionEthValue` starts at 0, and the first resolve adds `rolledEth` to pendingRedemptionEthValue — the subtraction of `pendingRedemptionEthBase` at :593 is conceptually "remove the UNRESOLVED stake from the RESOLVED-ledger-total", which is zero for the first resolve).

Re-read sDGNRS:585-610 body carefully at HEAD cc68bfc7 to confirm the actual invariant. The RE-read is expected to clarify: either pendingRedemptionEthValue was already accumulated at _submitGamblingClaim time (which would make :593 a roll-adjust), OR pendingRedemptionEthValue starts at zero and :593 is the accumulator. One of these is right — verify and document.

Write verdict rows:

| Verdict Row ID | REQ-ID | Source Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| SDR-02-V01 | SDR-02 | D-243-C013, D-243-C014, D-243-F011, D-243-F012, Pre-Flag bullet L2481 | contracts/StakedDegenerusStonk.sol:593 | SDR-02a per-entry accounting — roll-adjust period-close formula | <bucket — SAFE expected> | `pendingRedemptionEthValue = pendingRedemptionEthValue - pendingRedemptionEthBase + rolledEth` preserves `sum(pendingRedemptionEthValue across active periods) == sum(rolledEth)` invariant | 771893d1 |
| SDR-02-V02 | SDR-02 | D-243-C013/C014, Pre-Flag bullet L2482 | contracts/StakedDegenerusStonk.sol:535 | SDR-02b _deterministicBurnFrom subtracts pendingRedemptionEthValue from payout base | <bucket> | `totalMoney = ethBal + stethBal + claimableEth - pendingRedemptionEthValue;` excludes reserved wei from deterministic-burn payout; no double-counting | 771893d1 |
| SDR-02-V03..V08 | SDR-02 | (per timing) | (per timing) | SDR-02b per-timing worked wei ledger | <bucket per timing — SAFE expected> | One row per timing SDR-01-T{a-f}; each shows worked wei example with ledger closing | 771893d1 |

Append Pre-Flag closure notes:
- Bullet L2481 (pendingRedemptionEthValue roll-adjust formula) — CLOSED via SDR-02-V01 (invariant preservation proof).
- Bullet L2482 (_deterministicBurnFrom subtraction at :535) — CLOSED via SDR-02-V02 (exclusion from payout base proof).

**Step E — SDR-03 handleGameOverDrain subtraction depth (vectors SDR-03a + SDR-03b per D-14):**

Read handleGameOverDrain body at GameOverModule.sol:79-189 verbatim. Identify:
1. L80 GO_JACKPOT_PAID_MASK idempotency read — function returns early if already paid
2. L86-90 reserved-subtraction hunk — `reserved = uint256(claimablePool) + IStakedDegenerusStonk(ContractAddresses.SDGNRS).pendingRedemptionEthValue();` (per Phase 244 GOX-03-V01 shared-context)
3. L94 first IStakedDegenerusStonk.pendingRedemptionEthValue() external call (pre-refund)
4. L148 `_goWrite(GO_JACKPOT_PAID_SHIFT, GO_JACKPOT_PAID_MASK, 1)` — idempotency write (prevents double-drain)
5. L157 second IStakedDegenerusStonk.pendingRedemptionEthValue() external call (post-refund)
6. L186 `_sendToVault(remaining, stBal)` — 33/33/34 split (code at L225-233)

Verify:
- Both calls at :94 + :157 use the external interface declaration `function pendingRedemptionEthValue() external view returns (uint256)` (IStakedDegenerusStonk.sol:88-90 per D-243-C032) — `view` modifier enforces no state writes, safe from re-entrancy
- Subtraction precedes split (both L86-90 and L157 subtract BEFORE L186 _sendToVault call)
- L80 + L148 idempotency bit prevents double-drain: if handleGameOverDrain re-enters (via direct external call OR delegatecall path), L80 check returns early; L148 sets the bit IRREVERSIBLY (no path clears it)
- STAGE_TICKETS_WORKING + multi-tx drain: verify handleGameOverDrain is self-contained (no partial-drain state where subtraction is skipped mid-iteration)

Write verdict rows:

| Verdict Row ID | REQ-ID | Source Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| SDR-03-V01 | SDR-03 | D-243-C017, D-243-C032, D-243-X058, GOX-03-V01 (shared-context primary) | contracts/modules/DegenerusGameGameOverModule.sol:79-94 | SDR-03a pre-refund pendingRedemptionEthValue() read at L94 | <bucket — SAFE expected via 244 shared-context> | cross-cite GOX-03-V01 for the L86-90 reserved-subtraction hunk + L94 direct read; 245 confirms the same at HEAD cc68bfc7 (no re-derivation — 244 primary) | 771893d1 |
| SDR-03-V02 | SDR-03 | D-243-C017, D-243-X059, GOX-03-V02 (shared-context) | contracts/modules/DegenerusGameGameOverModule.sol:151-157 | SDR-03a post-refund subtraction at L157 | <bucket> | cross-cite GOX-03-V02 | 771893d1 |
| SDR-03-V03 | SDR-03 | D-243-C017, GOX-03-V03, Pre-Flag bullet L2485 | contracts/modules/DegenerusGameGameOverModule.sol:80 + :148 + contracts/interfaces/IStakedDegenerusStonk.sol:88-90 | SDR-03b multi-tx drain edges — STAGE_TICKETS_WORKING partial drain + L80 GO_JACKPOT_PAID idempotency + external-call staticcall-safety | <bucket> | L80 early-return reads GO_JACKPOT_PAID bit; L148 set-irreversibly; pendingRedemptionEthValue() is `external view` (no state write); re-entrancy cannot bypass the bit; multi-tx drain NEVER runs `_sendToVault` twice | 771893d1 |

Append Pre-Flag closure note:
- Bullet L2485 (multi-tx drain + STAGE_TICKETS_WORKING deeper analysis) — CLOSED via SDR-03-V03 (GO_JACKPOT_PAID idempotency proof + staticcall-safety proof).

**Step F — Update §0 Verdict Count Card for SDR-01 + SDR-02 + SDR-03 rows. Append §Reproduction Recipe with grep + git-show + sed commands actually used.**

Constraints:
- Zero edits to audit/v31-243-DELTA-SURFACE.md or audit/v31-244-PER-COMMIT-AUDIT.md per CONTEXT.md D-22 (record any scope-guard deferral in plan SUMMARY only)
- Zero F-31-NN finding-IDs per CONTEXT.md D-23 (use `Finding Candidate SDR-NN-FC##` IDs scoped per-REQ if any surface)
- Zero contracts/ or test/ writes per CONTEXT.md D-20 (only write target is audit/v31-245-SDR.md)
- Severity pre-classification per row REQUIRED per CONTEXT.md D-08 6-bucket bar
- claimRedemption ungated property absorbed into SDR-01 per CONTEXT.md D-09 — NO standalone INFO finding-candidate emitted
  </action>
  <verify>
    <automated>test -f audit/v31-245-SDR.md && grep -q '^## §SDR-01' audit/v31-245-SDR.md && grep -q '^## §SDR-02' audit/v31-245-SDR.md && grep -q '^## §SDR-03' audit/v31-245-SDR.md && grep -q 'Audit baseline: 7ab515fe' audit/v31-245-SDR.md && grep -q 'Audit head:     cc68bfc7' audit/v31-245-SDR.md && grep -qE '^\| SDR-01-T[a-f] ' audit/v31-245-SDR.md && grep -qE '^\| SDR-01-V[0-9]+ ' audit/v31-245-SDR.md && grep -qE '^\| SDR-02-V[0-9]+ ' audit/v31-245-SDR.md && grep -qE '^\| SDR-03-V[0-9]+ ' audit/v31-245-SDR.md && grep -q 'pendingRedemptionEthValue' audit/v31-245-SDR.md && grep -q 'GO_JACKPOT_PAID' audit/v31-245-SDR.md && grep -q 'handleGameOverDrain' audit/v31-245-SDR.md && grep -q 'resolveRedemptionPeriod' audit/v31-245-SDR.md && grep -q 'claimRedemption' audit/v31-245-SDR.md && [ "$(git status --porcelain contracts/ test/)" = "" ] && ! grep -qE 'F-31-[0-9]' audit/v31-245-SDR.md && [ "$(git status --porcelain audit/v31-243-DELTA-SURFACE.md audit/v31-244-PER-COMMIT-AUDIT.md)" = "" ]</automated>
  </verify>
  <done>
    - `audit/v31-245-SDR.md` exists with §SDR-01, §SDR-02, §SDR-03 sections, §Phase-244-Pre-Flag Cross-Walk section, §Reproduction Recipe section
    - §SDR-01 contains 6 foundation rows `SDR-01-T{a-f}` (one per timing) + at least 3 standard SDR-01-V## rows (timing-reach + latch-point + claimRedemption-ungated-property)
    - §SDR-02 contains at least 3 SDR-02-V## rows (entry-formula + _deterministicBurnFrom subtraction + per-timing worked examples)
    - §SDR-03 contains at least 3 SDR-03-V## rows (pre-refund + post-refund + multi-tx edges)
    - Pre-Flag closure notes for bullets L2477, L2478, L2481, L2482, L2485 appended
    - Zero F-31-NN finding-IDs
    - Zero contracts/, test/, audit/v31-243-DELTA-SURFACE.md, or audit/v31-244-PER-COMMIT-AUDIT.md writes
    - All cited D-243 rows from D-243-I017 + D-243-I018 subsets appear in verdict-row Source Row(s) cells at least once
  </done>
</task>

<task type="auto">
  <name>Task 2: SDR-04 claimRedemption DOS/starvation/underflow/race + SDR-05 per-wei conservation across 6 timings + SDR-06 State-1 orphan-redemption deeper negative-space sweep — close SDR-04 + SDR-05 + SDR-06</name>
  <files>audit/v31-245-SDR.md</files>
  <read_first>
    - .planning/phases/245-sdgnrs-redemption-gameover-safety/245-CONTEXT.md — D-09 (claimRedemption ungated absorbed into SDR-04), D-10/D-11 (per-wei accounting rigor — one worked example per timing), D-14 SDR-04/05/06 adversarial vectors, D-25 (Pre-Flag advisory — closure notes per bullet)
    - audit/v31-243-DELTA-SURFACE.md §1.4 D-243-C013/C014/C017 (burn/burnWrapped/handleGameOverDrain — from Task 1's read_first); §3.1.11 D-243-X022..X024 (burn callers); §3.1.12 D-243-X025 (burnWrapped zero programmatic callers)
    - audit/v31-243-DELTA-SURFACE.md §6 Consumer Index rows D-243-I017 (SDR-06 coverage), D-243-I019 (SDR-04 claim-redemption gate coverage), D-243-I020 (SDR-05 per-wei conservation coverage)
    - audit/v31-244-PER-COMMIT-AUDIT.md §4 GOX-01 V-rows V01..V08 (8-path entry-gate shift — shared-context for SDR-06 State-1 block); §4 GOX-02 V-rows V01/V02/V03 (PRIMARY closure for State-1 burn/burnWrapped reach-path enumeration); §Phase-245-Pre-Flag bullets at L2488 (SDR-04), L2491 (SDR-05), L2494 (SDR-06)
    - contracts/StakedDegenerusStonk.sol at HEAD cc68bfc7:
      - L462-476 — burnAtGameOver body (only callable from DegenerusGame onlyGame modifier — admin/game-only)
      - L486-495 — burn body: verify flow ordering at cc68bfc7 (L487 gameOver short-circuit → L491 livenessTriggered revert → L492 rngLocked revert → L493 _submitGamblingClaim)
      - L506-516 — burnWrapped body: L507 `if (livenessTriggered() && !gameOver()) revert` → L509 burnForSdgnrs call → L510-: gameOver short-circuit → L513 rngLocked revert → L514 _submitGamblingClaimFrom
      - L618-670 — claimRedemption body in detail: verify the loop over redemptionPeriods, the totalRolledEth accumulator, the L657 decrement, the L661 delete
      - L744-817 — _submitGamblingClaimFrom body for State-1 block proof (the burn entry-points redirect here via _submitGamblingClaim or directly)
    - contracts/DegenerusStonk.sol at HEAD cc68bfc7 — DGNRS wrapper file; identify calls to sDGNRS.burn (D-243-X022 at L227 area — wrapper self-burn) + sDGNRS.yearSweep (D-243-X023 at L304 area)
    - contracts/DegenerusVault.sol at HEAD cc68bfc7 — Vault wrapper; identify call to sDGNRS.burn (D-243-X024 at L740 area)
    - contracts/modules/DegenerusGameGameOverModule.sol at HEAD cc68bfc7:
      - L79-189 — handleGameOverDrain (for SDR-04 race-condition proof)
      - L196-216 — handleFinalSweep (for SDR-04 starvation proof — 30-day window)
      - Gate check at L196: verify handleFinalSweep is only callable after gameOver + 30-day delay
    - contracts/modules/DegenerusGameMintModule.sol at HEAD cc68bfc7 (for SDR-06 entry-point enumeration — any path that could create a redemption in State-1):
      - L885-911 _purchaseCoinFor (L890 livenessTriggered gate per GOX-01-V01)
      - L913-1198 _purchaseFor (L920 livenessTriggered gate per GOX-01-V02)
    - contracts/modules/DegenerusGameWhaleModule.sol at HEAD cc68bfc7 — all 4 whale-module purchase paths (L194/384/542/957) per GOX-01-V05..V08
    - audit/v31-244-PER-COMMIT-AUDIT.md §4 GOX-01-V01..V08 — 8 purchase/claim paths shared-context for SDR-06b negative-space sweep (any NEW entry-point path that could bypass the State-1 block)
  </read_first>
  <action>
Append §SDR-04 + §SDR-05 + §SDR-06 to `audit/v31-245-SDR.md` (Task 1 created the file shell + §SDR-01/02/03).

**Step A — Sanity gate per Task 1 Step A. Re-confirm `cc68bfc7` HEAD anchor.**

**Step B — §SDR-04 claimRedemption DOS-free / starvation-free / underflow-free / race-free (vectors SDR-04a + SDR-04b + SDR-04c + SDR-04d per CONTEXT.md D-14):**

Append `## §SDR-04 — claimRedemption post-gameOver DOS / starvation / underflow / race-freeness (4-actor taxonomy)` to `audit/v31-245-SDR.md`.

**Vector SDR-04a (DOS) — 4-actor taxonomy per Phase 238 D-07:**

Read claimRedemption body sDGNRS:618-670 verbatim. For each actor:

1. **Player (msg.sender):**
   - Can player craft calldata that reverts claimRedemption? Function takes no args (per :618 signature). player cannot pass malicious calldata.
   - Can player call claimRedemption repeatedly to drain? L661 `delete pendingRedemptions[player]` clears the struct after full claim; subsequent calls hit the implicit `claim.roll != 0` gate (no unresolved periods remain) and return early.
   - Can player front-run drain? The L94/L157 pendingRedemptionEthValue() READ in handleGameOverDrain is taken at drain-invocation time; if player claims BEFORE drain fires, pendingRedemptionEthValue decreases by the paid amount (via L657) BEFORE drain's read — drain subtracts the NEW lower value correctly.
   - Can player call claimRedemption AFTER handleFinalSweep? Verify: after handleFinalSweep sweeps ETH to VAULT, does claimRedemption still have the balance to pay? Read sDGNRS contract balance at HEAD for the claimRedemption payout path — the L657 decrement of pendingRedemptionEthValue is a bookkeeping subtraction; the actual ETH payout uses `_payEth` (sDGNRS:817-) which requires on-chain ETH balance. If handleFinalSweep at L215 sweeps `totalFunds` (ETH + stETH) without re-subtracting pendingRedemptionEthValue, the ETH balance could fall below claimRedemption's entitlement. CRITICAL: verify handleFinalSweep preserves the pendingRedemptionEthValue-reserved balance (or document if it does NOT and emit a finding candidate).
2. **Admin:**
   - No admin path touches claimRedemption directly. burnAtGameOver (sDGNRS:462) is admin/game-only but only zeros the contract's own balance — does NOT decrement pendingRedemptionEthValue. Admin cannot disable claims.
3. **Validator:**
   - Validator can reorder txs within a block. Can validator-ordering cause DOS? Verify claimRedemption does not depend on a specific block ordering (e.g., cannot be starved by forcing all handleGameOverDrain calls before any claimRedemption).
4. **VRF oracle:**
   - VRF oracle responds to requestRandomWords. Can VRF delay block claimRedemption? No — claimRedemption is independent of VRF state (does not read rngWord, rngRequestTime, or VRF-dead grace).

**Vector SDR-04b (starvation) — 30-day handleFinalSweep window:**

Read handleFinalSweep body GameOverModule:196-216:
- Identify the gate (e.g., `if (currentDay < gameOverDay + 30) revert;`)
- Verify the gate is CLOCK-based, NOT state-based (cannot be tampered by admin or player)
- Verify handleFinalSweep is NOT admin-callable before day+30 (or document if it is and that's the finding)
- Verify 30-day window is SUFFICIENT for all pending claimants to react (realistic-actor model: a user who loses wallet access has 30 days to recover + claim — standard web3 UX)

**Vector SDR-04c (underflow) — pendingRedemptionEthValue decrement at :657:**

Read sDGNRS:657 `pendingRedemptionEthValue -= totalRolledEth;` in context:
- totalRolledEth is summed across `redemptionPeriods[claim.periodIndex].roll != 0` periods for msg.sender
- Invariant: totalRolledEth <= sum of pending rolledEth for player <= pendingRedemptionEthValue
- The invariant is established by construction: pendingRedemptionEthValue is the sum of rolledEth across ALL players' resolved periods; subtracting a subset (player's totalRolledEth) can never exceed the sum.
- Verify the subtraction is in an `unchecked` block or a checked block — either is safe given the invariant; checked reverts on violation, unchecked would be safe per invariant.

**Vector SDR-04d (race) — drain + claim + sweep ordering:**

Enumerate all tx-ordering permutations across State-2:

- **Ordering 1:** handleGameOverDrain first → claimRedemption second → handleFinalSweep third (day+30)
  - Drain subtracts pendingRedemptionEthValue from the 33/33/34 split; sDGNRS retains reserved ETH balance
  - claimRedemption pays out totalRolledEth; pendingRedemptionEthValue decreases; sDGNRS ETH balance decreases
  - handleFinalSweep sweeps remaining balance — no reserved wei stranded (player already claimed)
- **Ordering 2:** claimRedemption first (should be impossible since claimRedemption requires resolved periods; pre-gameOver resolves complete in normal-tick) → handleGameOverDrain second → handleFinalSweep third
  - Pre-gameOver claimRedemption: claim succeeds; pendingRedemptionEthValue decreases
  - handleGameOverDrain reads the LOWER pendingRedemptionEthValue — no double-subtraction
  - handleFinalSweep clean
- **Ordering 3:** handleGameOverDrain → claimRedemption (player delays claim) → day+30 → handleFinalSweep (any remaining sDGNRS balance swept)
  - Player who claims before day+30 gets full entitlement
  - Player who doesn't claim by day+30: their rolledEth is stranded in sDGNRS balance; handleFinalSweep sweeps it to VAULT
  - This IS the starvation-by-delay case — user failure to claim within 30 days results in forfeiture. Verify this is the intended behavior (the 30-day window exists precisely for this).

Verdict rows:

| Verdict Row ID | REQ-ID | Source Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| SDR-04-V01 | SDR-04 | D-243-C013/C014, Pre-Flag bullet L2488 | contracts/StakedDegenerusStonk.sol:618-670 | SDR-04a DOS across 4-actor taxonomy | <bucket> | 4-actor analysis: no calldata-revert; no double-claim (L661 delete); no admin disable; no validator-ordering starvation; no VRF-state block | 771893d1 |
| SDR-04-V02 | SDR-04 | D-243-C017, Pre-Flag bullet L2488 | contracts/modules/DegenerusGameGameOverModule.sol:196-216 | SDR-04b 30-day handleFinalSweep window sufficiency | <bucket> | Clock-based gate; 30-day window is standard UX recovery period | 771893d1 |
| SDR-04-V03 | SDR-04 | D-243-C013/C014 | contracts/StakedDegenerusStonk.sol:657 | SDR-04c underflow-freeness via construction invariant | <SAFE expected> | totalRolledEth <= pendingRedemptionEthValue by construction (sum of player's resolved periods ⊆ sum of all players' resolved periods) | 771893d1 |
| SDR-04-V04 | SDR-04 | D-243-C013/C014/C017 | Multi-file — contracts/StakedDegenerusStonk.sol:657 + contracts/modules/DegenerusGameGameOverModule.sol:79-216 | SDR-04d drain+claim+sweep race-freeness | <bucket> | 3 tx-ordering permutations analyzed; reserved wei preserved through drain; 30-day window handles claim-delay | 771893d1 |

Append Pre-Flag closure note:
- Bullet L2488 (claimRedemption DOS + 30-day window sufficiency) — CLOSED via SDR-04-V01..V04 (4-actor DOS analysis + starvation-window analysis + underflow-invariant + race-ordering permutations).

**Step C — §SDR-05 per-wei ETH conservation across all 6 gameover timings (vector SDR-05a per D-14):**

Append `## §SDR-05 — Per-wei ETH conservation across all 6 gameover timings`.

For each timing SDR-01-T{a-f}, produce a worked wei-ledger example closing `sum(ENTRIES) == sum(EXITS)`:

Template (Timing Ta — pre-liveness all 3 steps — no gameover interference):
```
Timing Ta: burn (State-0) → resolve (State-0) → claim (State-0)
  Setup: player burns 1000 sDGNRS; assume segregatedEth = 100 wei
  t=0 (burn): _submitGamblingClaimFrom
    pendingRedemptionEthBase: 0 → 100
    pendingRedemptionEthValue: 0 → 0 (unchanged; entry at resolve time)
  t=1 (resolve, roll=80): resolveRedemptionPeriod at sDGNRS:593
    rolledEth = (100 * 80) / 100 = 80
    pendingRedemptionEthValue: 0 - 100 + 80 = ??? 
    (CRITICAL: requires pendingRedemptionEthValue >= 100 before subtraction. If first period, how is this possible?)
    (RE-READ sDGNRS:585-610 carefully; verify the actual invariant at HEAD cc68bfc7.)
  
  Upon re-read, the formula treats pendingRedemptionEthValue as the RUNNING TOTAL of resolved rolledEth minus any already-claimed-out flows. For the first period, pendingRedemptionEthBase is zero at function start (because sDGNRS-storage initial state has pendingRedemptionEthBase = 0 from constructor), and _submitGamblingClaim accumulates it DURING the period. At period-close (first resolve), pendingRedemptionEthBase = 100, pendingRedemptionEthValue (running total so far = 0), subtract-then-add: 0 - 100 + 80 = still requires >= 100.
  
  LIKELY RESOLUTION: re-read the actual storage to confirm the flow. Possibilities:
    - (a) _submitGamblingClaim ALSO updates pendingRedemptionEthValue += segregatedEth at burn time, making :593 a pure roll-adjust
    - (b) The :593 subtraction is inside `unchecked` and relies on a different invariant (e.g., pendingRedemptionEthValue -= pendingRedemptionEthBase is always zero on first resolve because both start at 0)
  
  RESOLUTION FOR THIS AUDIT: read _submitGamblingClaimFrom sDGNRS:752-817 carefully. Identify every pendingRedemption* state mutation. Document the exact per-wei flow.
  
  t=2 (claim): claimRedemption at :618-670
    totalRolledEth for player = 80
    pendingRedemptionEthValue: <resolved-value> - 80 = 0 (if only player)
    player receives 80 wei via _payEth
    pendingRedemptions[player] deleted
  
  Ledger: IN = 100 wei (burn segregated) = OUT = 80 wei to player + 20 wei roll-back-to-pool (stayed in sDGNRS balance as non-reserved) ✓
  SAFE verdict.
```

Produce similar worked examples for timings Tb, Tc, Td, Te, Tf.

Timing Te (request post-gameOver — BLOCKED) has a special case: the burn path short-circuits via gameOver at L487 → _deterministicBurnFrom path (no redemption created); NO entry into pendingRedemptionEthValue. Ledger: IN = 0, OUT = 0. SAFE.

Timing Tf (VRF-pending → _gameOverEntropy fallback): resolve fires at AdvanceModule:1286 using fallback-derived redemptionRoll; ledger closes via same roll-adjust formula as Ta/Tb. Conservation holds because the ROLL value is the sole input to :593.

Verdict rows:

| Verdict Row ID | REQ-ID | Source Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| SDR-05-V01..V06 | SDR-05 | Cross-cite SDR-01-T{a-f} + SDR-02-V01/V02 | contracts/StakedDegenerusStonk.sol:585-670 | SDR-05a per-timing ledger close | <bucket per timing — SAFE expected> | One row per timing with worked wei example; ledger closes | 771893d1 |

Append Pre-Flag closure note:
- Bullet L2491 (per-wei conservation across gameover timings) — CLOSED via SDR-05-V01..V06 (6 per-timing worked examples).

**Step D — §SDR-06 State-1 orphan-redemption deeper negative-space sweep (vectors SDR-06a + SDR-06b per D-14):**

Append `## §SDR-06 — State-1 orphan-redemption window closed (deeper negative-space sweep)`.

**Vector SDR-06a (primary closure via 244 shared-context):**

Enumerate every reach-path to _submitGamblingClaim* as set in CONTEXT.md D-14:
1. `burn(uint256)` at sDGNRS:486-495 — callers:
   - DegenerusStonk wrapper self-burn at DegenerusStonk.sol:227 (D-243-X022)
   - DegenerusStonk.yearSweep at DegenerusStonk.sol:304 (D-243-X023)
   - DegenerusVault wrapper at DegenerusVault.sol:740 (D-243-X024)
2. `burnWrapped(uint256)` at sDGNRS:506-516 — player-facing external, NO programmatic callers per D-243-X025
3. (any cross-chain / admin / constructor paths — verify EXHAUSTIVELY)

Verdict rows cross-citing Phase 244 GOX-02-V01/V02/V03 as PRIMARY closure:

| Verdict Row ID | REQ-ID | Source Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| SDR-06-V01 | SDR-06 | D-243-C013/C034, GOX-02-V01 (primary) | contracts/StakedDegenerusStonk.sol:486-495 | SDR-06a burn State-1 block | <SAFE via 244 cross-cite> | Phase 244 GOX-02-V01 closed the burn State-1 block at HEAD cc68bfc7; SDR-06-V01 derives via cross-cite — L491 `if (livenessTriggered()) revert BurnsBlockedDuringLiveness` covers callers D-243-X022/X023/X024 | 771893d1 |
| SDR-06-V02 | SDR-06 | D-243-C014/C034, GOX-02-V02 (primary) | contracts/StakedDegenerusStonk.sol:506-516 | SDR-06a burnWrapped State-1 block | <SAFE via 244 cross-cite> | Phase 244 GOX-02-V02 closed the burnWrapped divergence (`livenessTriggered() && !gameOver()` pattern is load-bearing for then-burn order); SDR-06-V02 cross-cites | 771893d1 |

**Vector SDR-06b (DEEPER negative-space sweep per D-14 + Pre-Flag bullet L2494):**

Enumerate NON-OBVIOUS paths that could theoretically create a redemption in State-1:
1. **Admin-triggered `purchaseStartDay` manipulation** — does any admin function allow resetting day-math such that liveness un-fires mid-window? Read all admin-callable functions in contracts/DegenerusAdmin.sol + DegenerusGame.sol + modules; verify none can manipulate day-math to un-fire liveness.
2. **Level transitions mid-window** — if `level` changes during State-1, does the liveness predicate at Storage:1235-1243 still fire correctly? Verify the day-math at L1239 uses `purchaseStartDay + 120 * level` (or equivalent) — if level increases mid-window, threshold increases too; liveness could potentially un-fire. VERIFY: read the exact predicate at cc68bfc7.
3. **Constructor paths** — sDGNRS constructor claims a whale pass (if applicable). Verify State-1 is unreachable at construction time (game is not yet constructed, so `game.livenessTriggered()` would revert on non-contract OR return false on zero-state).
4. **Cross-chain forward-imported state** — are there any L2/cross-chain bridges that could pre-seed redemption state? Read all import/export paths in contracts/ for cross-chain interactions; verify none inject pendingRedemptionEthValue state.
5. **Reentrancy from external call during burn** — does any external call during burn (e.g., during _submitGamblingClaimFrom) allow reentering burn in State-1? Read _submitGamblingClaimFrom + _payEth for external-call patterns.

Verdict rows:

| Verdict Row ID | REQ-ID | Source Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| SDR-06-V03 | SDR-06 | Pre-Flag bullet L2494, admin-file sweep | contracts/DegenerusAdmin.sol + DegenerusGame.sol + modules/* | SDR-06b admin-triggered day-math manipulation | <bucket> | No admin function manipulates purchaseStartDay or day-math predicate; liveness cannot un-fire via admin action | 771893d1 |
| SDR-06-V04 | SDR-06 | Pre-Flag bullet L2494 | contracts/storage/DegenerusGameStorage.sol:1235-1243 | SDR-06b level-transition mid-window liveness consistency | <bucket> | Re-read day-math predicate at cc68bfc7; if level-dependent threshold, document whether level increase could un-fire liveness. If un-fireable, emit finding candidate. | 771893d1 |
| SDR-06-V05 | SDR-06 | Constructor sweep | contracts/StakedDegenerusStonk.sol constructor area | SDR-06b constructor-path State-1 unreachability | <SAFE expected> | State-1 unreachable because game is not yet constructed at sDGNRS construction time | 771893d1 |
| SDR-06-V06 | SDR-06 | Cross-chain import sweep | contracts/ (full tree) | SDR-06b cross-chain state-import sweep | <SAFE expected> | No cross-chain bridge injects pendingRedemptionEthValue state | 771893d1 |
| SDR-06-V07 | SDR-06 | Reentrancy surface sweep | contracts/StakedDegenerusStonk.sol:744-817 + :817+ _payEth | SDR-06b reentrancy during burn | <bucket> | _submitGamblingClaimFrom + _payEth pattern analysis; no reentrant path allows re-entering burn in State-1 | 771893d1 |

Append Pre-Flag closure note:
- Bullet L2494 (SDR-06 negative-space sweep) — CLOSED via SDR-06-V03..V07 (admin + level-transition + constructor + cross-chain + reentrancy sweep).

**Step E — Update §0 Verdict Count Card for SDR-04 + SDR-05 + SDR-06 rows. Append §Reproduction Recipe with grep + git-show commands used.**

Constraints: carry from Task 1 (READ-only, no F-31-NN, no upstream edits).
  </action>
  <verify>
    <automated>test -f audit/v31-245-SDR.md && grep -q '^## §SDR-04' audit/v31-245-SDR.md && grep -q '^## §SDR-05' audit/v31-245-SDR.md && grep -q '^## §SDR-06' audit/v31-245-SDR.md && grep -qE '^\| SDR-04-V[0-9]+ ' audit/v31-245-SDR.md && grep -qE '^\| SDR-05-V[0-9]+ ' audit/v31-245-SDR.md && grep -qE '^\| SDR-06-V[0-9]+ ' audit/v31-245-SDR.md && grep -q 'handleFinalSweep' audit/v31-245-SDR.md && grep -q '4-actor' audit/v31-245-SDR.md && grep -q 'BurnsBlockedDuringLiveness' audit/v31-245-SDR.md && grep -q 'GOX-02-V01' audit/v31-245-SDR.md && grep -q 'GOX-02-V02' audit/v31-245-SDR.md && [ "$(git status --porcelain contracts/ test/)" = "" ] && ! grep -qE 'F-31-[0-9]' audit/v31-245-SDR.md && [ "$(git status --porcelain audit/v31-243-DELTA-SURFACE.md audit/v31-244-PER-COMMIT-AUDIT.md)" = "" ]</automated>
  </verify>
  <done>
    - §SDR-04 contains at least 4 SDR-04-V## rows (DOS / starvation / underflow / race)
    - §SDR-05 contains at least 6 SDR-05-V## rows (one per timing SDR-01-T{a-f}) with worked wei-ledger examples
    - §SDR-06 contains at least 7 SDR-06-V## rows (V01/V02 cross-cite 244 primary + V03..V07 deeper negative-space sweep)
    - Pre-Flag closure notes for L2488, L2491, L2494 appended
    - Zero F-31-NN finding-IDs
    - Zero contracts/, test/, upstream-audit-file writes
    - 4-actor taxonomy explicitly applied in SDR-04 per CONTEXT.md D-14 + Phase 238 D-07
  </done>
</task>

<task type="auto">
  <name>Task 3: SDR-07 sDGNRS supply conservation + SDR-08 _gameOverEntropy fallback-entropy consumption at AdvanceModule:1286 within EXC-03 envelope — close SDR-07 + SDR-08 + KI EXC-03 RE_VERIFIED_AT_HEAD</name>
  <files>audit/v31-245-SDR.md</files>
  <read_first>
    - .planning/phases/245-sdgnrs-redemption-gameover-safety/245-CONTEXT.md — D-14 SDR-07 / SDR-08 adversarial vectors, D-17 (RE_VERIFIED_AT_HEAD methodology + corroborating prior-milestone artifacts), D-24 (KI exception RE_VERIFIED_AT_HEAD only — no re-litigation), D-25 (Pre-Flag advisory)
    - audit/v31-243-DELTA-SURFACE.md §1.4 D-243-C016 (_gameOverEntropy MODIFIED at AdvanceModule:1216-1294 — new rngRequestTime clearing; redemption-resolve call at :1286); §3.1.14 D-243-X027 (_gameOverEntropy caller at _handleGameOverPath:560)
    - audit/v31-243-DELTA-SURFACE.md §6 Consumer Index rows D-243-I021 (SDR-07/SDR-08 cross-cite to gameover-entropy subset)
    - audit/v31-244-PER-COMMIT-AUDIT.md §2 RNG-01-V11 (EXC-03 envelope non-widening proof at the _unlockRng scope — PRIMARY closure that SDR-08 cross-cites); §4 GOX-06-V01 (rngRequestTime clearing reentry — shared-context for SDR-08 surface check); §Phase-245-Pre-Flag bullets at L2497 (SDR-07), L2500 (SDR-08)
    - KNOWN-ISSUES.md "Gameover RNG substitution for mid-cycle write-buffer tickets" entry — EXC-03 envelope: acceptance rationale (a) terminal-state-only; (b) no player-reachable exploit; (c) gameover must drain within bounded transactions; (d) all substitute entropy is VRF-derived or VRF-plus-prevrandao
    - contracts/StakedDegenerusStonk.sol at HEAD cc68bfc7:
      - L221-227 — pendingRedemptions mapping + pendingRedemptionEthValue + pendingRedemptionBurnie/Base storage
      - L307-308 — constructor _mint (DGNRS wrapper creator amount + pool total)
      - L412-442 — transferFromPool body (pool-to-recipient distributions — BPS-rounded; verify no dust mint)
      - L462-476 — burnAtGameOver body (zeros contract's own balance; only-game modifier)
      - L519-569 — _deterministicBurn + _deterministicBurnFrom bodies (L539-541 equivalent burn site where supply decreases)
      - L874 — _mint private function body
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD cc68bfc7:
      - L1228-1306 — _gameOverEntropy body with BOTH branches:
        - L1237-1259 — VRF-available branch (currentWord != 0 AND rngRequestTime != 0): at L1256 `sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay)` — uses VRF-derived word
        - L1263-1293 — prevrandao-fallback branch (rngRequestTime != 0 AND elapsed >= grace): at L1286 `sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay)` — uses fallback (VRF + prevrandao admixture per EXC-02)
        - L1292 `rngRequestTime = 0` SSTORE at branch exit
      - L1263 — the outer `if (rngRequestTime != 0)` gate (only prevrandao-fallback branch requires stall + grace; the VRF-available branch does not)
    - audit/v30-CONSUMER-INVENTORY.md — Phase 237 consumer index for corroborating supply-conservation cross-cites
    - audit/v30-FREEZE-PROOF.md — Phase 238 19-row Gameover-Flow Freeze-Proof Subset for SDR-08 consumer-determinism envelope corroboration
    - .planning/milestones/v29.0-phases/232.1-rng-consumer-audit/ — F-29-04 commitment-window trace
  </read_first>
  <action>
Append §SDR-07 + §SDR-08 + §KI Envelope Re-Verify EXC-03 to `audit/v31-245-SDR.md` (Tasks 1 + 2 created §SDR-01..§SDR-06).

**Step A — Sanity gate. Re-confirm `cc68bfc7` HEAD anchor.**

**Step B — §SDR-07 sDGNRS supply conservation across full lifecycle (vectors SDR-07a + SDR-07b per CONTEXT.md D-14):**

Append `## §SDR-07 — sDGNRS supply conservation across full redemption lifecycle (including gameover interception)`.

Enumerate supply mutations at HEAD cc68bfc7:

1. **`_mint(address to, uint256 amount)` at sDGNRS:874** — private function; callers:
   - Constructor at sDGNRS:307-308: `_mint(ContractAddresses.DGNRS, creatorAmount)` + `_mint(address(this), poolTotal)` (genesis supply)
   - NO other internal callers (verify by grepping `_mint(` in contracts/StakedDegenerusStonk.sol)
   - Verdict: supply increases ONCE per token at genesis; no dust mint from non-constructor paths
2. **`transferFromPool(Pool pool, address to, uint256 amount)` at sDGNRS:412-442** — external onlyGame; BPS-rounded distribution; returns `transferred <= amount`
   - Does this MINT new supply? Read the body: verify it moves tokens from pool to recipient (balance-of change) WITHOUT creating new supply (no `totalSupply += ...`). The L426 self-win comment says "burn instead of no-op transfer, increasing value per remaining token" — verify the burn is a totalSupply DECREASE, not an increase, and that the "transfer" path is balance-moving only.
   - Verdict: no dust mint from transferFromPool; rounding residual stays in pool (no supply creation)
3. **`burnAtGameOver()` at sDGNRS:462-476** — external onlyGame; zeros contract's own balance via `_burn(address(this), balanceOf(address(this)))` pattern (verify the exact call)
   - Supply decreases by `balanceOf(address(this))` at gameover
   - Verdict: over-burn impossible — can only burn tokens the contract holds; no cross-account burn
4. **`_deterministicBurnFrom` at sDGNRS:527-569** — private burn path; at L539-541 equivalent, burns sDGNRS from `burnFrom` address
   - Supply decreases by `amount` on each burn call
   - Verdict: exactly-one-burn per token; no double-burn (the sDGNRS balance decrement + totalSupply decrement are atomic)
5. **`_submitGamblingClaimFrom` at sDGNRS:752-817** — also burns sDGNRS (for gambling-burn redemption path); cross-cite with SDR-01 burn flow
   - Supply decreases by `amount` at burn time; the SAME token is not burned twice (the burn path leads to _deterministicBurnFrom in gameOver short-circuit, OR to pendingRedemptionEthBase segregation in State-0)
   - Verdict: gambling-burn vs deterministic-burn paths are mutually exclusive (L487 short-circuit gates)

Worked invariant proof:

```
Invariant: for every sDGNRS token T ever minted, T is burned at most once.

Proof sketch:
  - T is minted at constructor via _mint at :307-308 (creator amount) or :308 (pool total)
  - T leaves the pool via transferFromPool (:412-442) which is a balance move, not a mint
  - T is burned via exactly one of:
    (a) _deterministicBurnFrom (:539-541 equivalent) during player-initiated burn or burnWrapped
    (b) burnAtGameOver (:462-476) zeroing the contract's own balance at gameover
  - The two burn paths are mutually exclusive per (L487 gameOver short-circuit for burn) and (onlyGame modifier for burnAtGameOver)
  - No path burns the same T twice: (a) decrements msg.sender's balance; (b) decrements contract's own balance; these are different addresses.
```

Verdict rows:

| Verdict Row ID | REQ-ID | Source Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| SDR-07-V01 | SDR-07 | Constructor + _mint | contracts/StakedDegenerusStonk.sol:307-308 + :874 | SDR-07a genesis mint exactly once | <SAFE expected> | Constructor calls _mint exactly twice (creator + pool); no other internal callers of _mint | 771893d1 |
| SDR-07-V02 | SDR-07 | transferFromPool | contracts/StakedDegenerusStonk.sol:412-442 | SDR-07b no dust mint from BPS rounding | <SAFE expected> | transferred <= amount invariant; rounding residual stays in pool (no supply change) | 771893d1 |
| SDR-07-V03 | SDR-07 | burnAtGameOver | contracts/StakedDegenerusStonk.sol:462-476 | SDR-07a contract-self-burn at gameover | <SAFE expected> | Burns balanceOf(address(this)); no cross-account burn; onlyGame modifier | 771893d1 |
| SDR-07-V04 | SDR-07 | _deterministicBurnFrom | contracts/StakedDegenerusStonk.sol:527-569 | SDR-07a player-initiated burn atomicity | <SAFE expected> | Balance decrement + totalSupply decrement atomic; exactly-one-burn per token | 771893d1 |
| SDR-07-V05 | SDR-07 | Burn-path mutual-exclusion | L487 gameOver short-circuit + onlyGame burnAtGameOver | SDR-07a gambling-burn vs deterministic-burn disjointness | <SAFE expected> | Two burn paths mutually exclusive by L487 + onlyGame gates | 771893d1 |
| SDR-07-V06 | SDR-07 | v29.0 Phase 235 / v24.0 supply-conservation RE_VERIFIED | Cross-cite to prior-milestone artifacts per CONTEXT.md D-17 | SDR-07b corroborating prior-proof RE_VERIFIED_AT_HEAD | <SAFE via corroboration> | v29.0 Phase 235 + v24.0 supply proofs re-derive same invariant at their respective HEADs; 245 re-derives at cc68bfc7 and confirms equivalence | 771893d1 |

Append Pre-Flag closure note:
- Bullet L2497 (sDGNRS supply conservation across lifecycle) — CLOSED via SDR-07-V01..V06 (genesis + pool + gameover + player-burn + mutual-exclusion + prior-milestone corroboration).

**Step C — §SDR-08 _gameOverEntropy fallback-entropy consumption + EXC-03 envelope RE_VERIFIED_AT_HEAD (vectors SDR-08a + SDR-08b per CONTEXT.md D-14 + D-24):**

Append `## §SDR-08 — _gameOverEntropy VRF-pending-redemption fallback fairness (F-29-04/EXC-03 class envelope)`.

Read `_gameOverEntropy` body at AdvanceModule:1228-1306 verbatim. Identify:

1. **VRF-available branch** (L1237-1259):
   - Condition: `currentWord != 0 && rngRequestTime != 0` (VRF fulfillment arrived before gameOver latched)
   - Redemption-resolve call at L1256: `sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay)` with `redemptionRoll` derived from VRF word
   - No prevrandao consumption in this branch
2. **Prevrandao-fallback branch** (L1263-1293):
   - Outer gate L1263: `if (rngRequestTime != 0)` — only fires when VRF request still pending
   - Inner gate L1264: `uint48 elapsed = ts - rngRequestTime;` — measures stall duration
   - L1287-: `if (elapsed >= _VRF_GRACE_PERIOD)` (14-day grace; verify exact line in body)
   - Redemption-resolve call at L1286: `sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay)` — `redemptionRoll` derived from VRF+prevrandao admixture per `_getHistoricalRngFallback`
   - L1292 `rngRequestTime = 0` SSTORE (clears the stall lock)
3. **Branch disjointness:** at most one branch fires per gameover tick. Verify by reading the branch-gate logic carefully.

**Vector SDR-08a — EXC-03 envelope RE_VERIFIED_AT_HEAD per CONTEXT.md D-24:**

Read EXC-03 entry in KNOWN-ISSUES.md (the "Gameover RNG substitution for mid-cycle write-buffer tickets" entry). Acceptance rationale:
- (a) only reachable at gameover — terminal state with no further gameplay after 30-day window
- (b) no player-reachable exploit — gameover triggered by 120-day liveness stall or pool deficit, neither player-timeable
- (c) gameover must drain within bounded transactions
- (d) all substitute entropy is VRF-derived or VRF-plus-prevrandao

The NEW L1286 `sdgnrs.resolveRedemptionPeriod` consumption MUST stay within the envelope:
- Terminal-state: YES — `_gameOverEntropy` is only reached via `_handleGameOverPath` post-liveness-fire
- No player-reachable exploit: YES — the redemption-roll is derived from VRF/fallback word, not player-controllable
- Bounded transactions: YES — single call per gameover tick
- VRF-derived or VRF+prevrandao: YES — `redemptionRoll` is a hash of the VRF/fallback word (read the derivation at AdvanceModule:1254-1256 and :1284-1286 to confirm)

Cross-cite Phase 244 RNG-01-V11 as PRIMARY non-widening proof: Phase 244 proved EXC-03 envelope did not widen at the `_unlockRng` removal scope. SDR-08 proves the NEW redemption-resolve-consumption site stays within the same envelope.

**Vector SDR-08b — no pending-limbo, no over-substitute, no under-resolve:**

- **No pending-limbo:** for every VRF-pending redemption at gameover-latch time, `_gameOverEntropy` fires exactly once per period (verify the period-close formula at sDGNRS:593 is called with the fallback-derived roll)
- **No over-substitute:** `_gameOverEntropy` is called by `_handleGameOverPath` at :560 exactly once per gameover tick (verify `_handleGameOverPath` does not recurse or re-call)
- **No under-resolve:** if rngRequestTime != 0 at gameover, branch L1263-1293 fires; if rngRequestTime == 0 (already clean), no stall; redemption-resolve in L1237-1259 branch covers the VRF-available path

Verdict rows:

| Verdict Row ID | REQ-ID | Source Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| SDR-08-V01 | SDR-08 | D-243-C016, D-243-F014, D-243-X027, EXC-03, RNG-01-V11 (primary) | contracts/modules/DegenerusGameAdvanceModule.sol:1286 | SDR-08a EXC-03 envelope RE_VERIFIED_AT_HEAD — new redemption-resolve-consumption site | RE_VERIFIED_AT_HEAD cc68bfc7 | 4 envelope criteria (a-d) re-verified at HEAD; NEW L1286 call stays within EXC-03 acceptance; cross-cite RNG-01-V11 for primary non-widening proof | 771893d1 |
| SDR-08-V02 | SDR-08 | D-243-C016 | contracts/modules/DegenerusGameAdvanceModule.sol:1237-1259 | SDR-08b VRF-available branch redemption-resolve fairness | <SAFE expected> | redemptionRoll derived from VRF word; branch disjoint with fallback branch; exactly-one-call per tick | 771893d1 |
| SDR-08-V03 | SDR-08 | D-243-C016, D-243-C028, EXC-03 | contracts/modules/DegenerusGameAdvanceModule.sol:1263-1293 | SDR-08b prevrandao-fallback branch redemption-resolve fairness under 14-day grace | <SAFE expected> | redemptionRoll from `_getHistoricalRngFallback` (VRF + prevrandao admixture); L1292 rngRequestTime = 0 SSTORE clears stall; branch disjoint with VRF-available | 771893d1 |
| SDR-08-V04 | SDR-08 | D-243-C015, D-243-X027, GOX-06-V02 (shared-context) | contracts/modules/DegenerusGameAdvanceModule.sol:560 + :1228-1306 | SDR-08b no-over-substitute / no-under-resolve via call-graph | <SAFE expected> | `_handleGameOverPath` calls `_gameOverEntropy` exactly once per gameover tick per Phase 244 GOX-06-V02 ordering analysis; fallback branch disjoint with VRF-available; every VRF-pending redemption reaches resolve via one of the two branches | 771893d1 |

Append `## §KI Envelope Re-Verify — EXC-03 (Gameover RNG substitution / F-29-04 class) under new 14-day VRF-dead grace + new L1286 redemption-resolve-consumption` header — the SDR-08-V01 row is the content; the dedicated section header allows reviewers to grep for "KI Envelope Re-Verify" consistently across Phase 244 and Phase 245 working files.

Append Pre-Flag closure note:
- Bullet L2500 (SDR-08 _gameOverEntropy fallback-resolve within EXC-03 envelope) — CLOSED via SDR-08-V01..V04 (envelope RE_VERIFIED_AT_HEAD + branch fairness + no-pending-limbo).

**Step D — Finalize §0 Verdict Count Card with all 8 SDR REQs' row counts + floor severities + EXC-03 KI envelope status for SDR-08. Append final §Reproduction Recipe commands.**

Required §0 values after Task 3:
- SDR-01: 6 T-rows + ≥3 V-rows; KI n/a; SAFE floor
- SDR-02: ≥3 V-rows (V01 entry + V02 deterministic + V03..V08 per-timing worked examples, though V03-V08 can be consolidated into §SDR-05 if Task 1 made them there); KI n/a
- SDR-03: ≥3 V-rows; KI n/a
- SDR-04: ≥4 V-rows; KI n/a
- SDR-05: 6 V-rows (per-timing worked examples — or reference SDR-02 if Task 1 placed them in SDR-02); KI n/a
- SDR-06: ≥7 V-rows; KI n/a
- SDR-07: ≥6 V-rows; KI n/a
- SDR-08: ≥4 V-rows including V01 `RE_VERIFIED_AT_HEAD cc68bfc7`; KI EXC-03 status stated

**Step E — Plan-close discretion (per CONTEXT.md Claude's Discretion):** Optionally append a per-REQ closure heatmap at the TOP of the file (REQ × verdict matrix) for reviewer convenience. Phase 244 did NOT include one; 245-01 discretion is to include or omit. If omitted, the §0 Verdict Count Card suffices.

**Step F — §Reproduction Recipe completeness:** ensure all grep / git-show / sed / git rev-parse commands used across Tasks 1 + 2 + 3 are listed in the §Reproduction Recipe sub-section, POSIX-portable (no bash-specific syntax).

Constraints:
- Zero contracts/, test/, upstream-audit-file writes
- Zero F-31-NN finding-IDs
- EXC-03 envelope RE_VERIFIED_AT_HEAD cc68bfc7 annotation — NO re-litigation of acceptance
- Cross-cite RNG-01-V11 explicitly for SDR-08 primary non-widening proof
  </action>
  <verify>
    <automated>test -f audit/v31-245-SDR.md && grep -q '^## §SDR-07' audit/v31-245-SDR.md && grep -q '^## §SDR-08' audit/v31-245-SDR.md && grep -q '^## §KI Envelope Re-Verify' audit/v31-245-SDR.md && grep -qE '^\| SDR-07-V[0-9]+ ' audit/v31-245-SDR.md && grep -qE '^\| SDR-08-V[0-9]+ ' audit/v31-245-SDR.md && grep -q 'EXC-03' audit/v31-245-SDR.md && grep -q 'RE_VERIFIED_AT_HEAD cc68bfc7' audit/v31-245-SDR.md && grep -q '_gameOverEntropy' audit/v31-245-SDR.md && grep -q 'RNG-01-V11' audit/v31-245-SDR.md && grep -q 'burnAtGameOver' audit/v31-245-SDR.md && grep -q 'transferFromPool' audit/v31-245-SDR.md && for req in SDR-01 SDR-02 SDR-03 SDR-04 SDR-05 SDR-06 SDR-07 SDR-08; do grep -qE "^\| $req-V[0-9]+ |^\| $req-T[a-f] " audit/v31-245-SDR.md || { echo "FAIL: $req absent"; exit 1; }; done && [ "$(git status --porcelain contracts/ test/)" = "" ] && ! grep -qE 'F-31-[0-9]' audit/v31-245-SDR.md && [ "$(git status --porcelain audit/v31-243-DELTA-SURFACE.md audit/v31-244-PER-COMMIT-AUDIT.md)" = "" ]</automated>
  </verify>
  <done>
    - §SDR-07 contains at least 6 SDR-07-V## rows (genesis + transferFromPool + burnAtGameOver + _deterministicBurnFrom + mutual-exclusion + prior-milestone corroboration)
    - §SDR-08 contains at least 4 SDR-08-V## rows including V01 with `RE_VERIFIED_AT_HEAD cc68bfc7` annotation
    - §KI Envelope Re-Verify section header exists with EXC-03 annotation
    - SDR-08 V01 cross-cites RNG-01-V11 as PRIMARY non-widening proof
    - Every REQ in {SDR-01..SDR-08} appears at least once as Verdict Row ID prefix
    - Pre-Flag closure notes for L2497, L2500 appended
    - §0 Verdict Count Card finalized for all 8 REQs with Floor Severity + KI status per row
    - Zero F-31-NN finding-IDs
    - Zero contracts/, test/, upstream-audit-file writes
  </done>
</task>

</tasks>

<verification>
**End-of-plan verification gate (manual, before commit):**

1. **Coverage gate:** Every REQ in {SDR-01, SDR-02, SDR-03, SDR-04, SDR-05, SDR-06, SDR-07, SDR-08} appears at least once as the prefix of a Verdict Row ID in `audit/v31-245-SDR.md`. SDR-01 specifically carries 6 `SDR-01-T{a-f}` foundation rows + standard `SDR-01-V##` rows. Run:
   ```
   for req in SDR-01 SDR-02 SDR-03 SDR-04 SDR-05 SDR-06 SDR-07 SDR-08; do
     count=$(grep -cE "^\| $req-V[0-9]+ |^\| $req-T[a-f] " audit/v31-245-SDR.md)
     echo "$req verdict rows: $count"
     [ "$count" -ge 1 ] || { echo "FAIL: $req has zero verdict rows"; exit 1; }
   done
   # SDR-01 foundation requirement: at least 6 SDR-01-T{a-f}
   tcount=$(grep -cE '^\| SDR-01-T[a-f] ' audit/v31-245-SDR.md)
   [ "$tcount" -ge 6 ] || { echo "FAIL: SDR-01 has fewer than 6 foundation timing rows"; exit 1; }
   ```
2. **Source Row(s) citation gate:** Every D-243 row in D-243-I017..D-243-I021 appears at least once in verdict-row Source Row(s) cells.
3. **Phase 244 shared-context cross-cite gate:** Every Phase 244 V-row listed in frontmatter `source_244_v_rows` appears at least once as cross-cite in verdict-row Source Row(s) cells. Explicit check for GOX-02-V01/V02/V03 (SDR-06 primary), GOX-03-V01/V02/V03 (SDR-03 primary), GOX-04-V01/V02 (SDR-01 shared), GOX-06-V01 (SDR-08 shared), RNG-01-V11 (SDR-08 primary non-widening).
4. **Pre-Flag closure gate:** Every SDR-grouped Pre-Flag bullet (L2477, L2478, L2481, L2482, L2485, L2488, L2491, L2494, L2497, L2500 — 10 bullets total per CONTEXT.md D-25) has a one-line closure note in the §Phase-244-Pre-Flag Cross-Walk section. Run:
   ```
   for line in 2477 2478 2481 2482 2485 2488 2491 2494 2497 2500; do
     grep -q "L$line" audit/v31-245-SDR.md || { echo "FAIL: Pre-Flag L$line closure note missing"; exit 1; }
   done
   ```
5. **Severity gate:** Every verdict row has a severity from {SAFE, INFO, LOW, MEDIUM, HIGH, CRITICAL} OR `RE_VERIFIED_AT_HEAD cc68bfc7` (SDR-08 V01 EXC-03 envelope row). Zero TBD/blank verdicts.
6. **KI envelope gate:** §KI Envelope Re-Verify section exists with `EXC-03` + `RE_VERIFIED_AT_HEAD cc68bfc7` annotation; SDR-08 V01 carries the same annotation.
7. **READ-only gate:** `git status --porcelain contracts/ test/` empty; `git status --porcelain audit/v31-243-DELTA-SURFACE.md audit/v31-244-PER-COMMIT-AUDIT.md` empty.
8. **F-31-NN absence gate:** `! grep -qE 'F-31-[0-9]' audit/v31-245-SDR.md` (zero finding-IDs emitted per CONTEXT.md D-23).
9. **Reproduction recipe completeness:** §Reproduction Recipe sub-section contains every `git show -L` / `grep -rn` / `sed -n` command actually used across Tasks 1-3 (POSIX-portable per CONTEXT.md D-04).
10. **claimRedemption standalone-INFO absence gate:** No standalone INFO finding-candidate emitted for claimRedemption ungated-by-state property per CONTEXT.md D-09 (property absorbed into SDR-01/04/05). Run:
    ```
    ! grep -qE '^### Finding Candidate.*[Cc]laim[Rr]edemption.*ungated' audit/v31-245-SDR.md
    ```
</verification>

<success_criteria>
- `audit/v31-245-SDR.md` exists with valid frontmatter header (Audit baseline / head / Owning commits / Scope per / Plan / Phase fields populated)
- §SDR-01..§SDR-08 sections each contain at least one verdict row with all 8 columns populated per CONTEXT.md D-06
- SDR-01 carries 6 foundation timing rows `SDR-01-T{a-f}` per CONTEXT.md D-15
- §KI Envelope Re-Verify EXC-03 section exists with `RE_VERIFIED_AT_HEAD cc68bfc7` annotation in SDR-08
- §Phase-244-Pre-Flag Cross-Walk section contains a one-line closure note for each of the 10 SDR-grouped Pre-Flag bullets at v31-244-PER-COMMIT-AUDIT.md L2477-2500
- §0 Verdict Count Card filled per REQ with Verdict Rows count + Finding Candidates count + KI Envelope Status + Floor Severity
- §Reproduction Recipe sub-section appended with all grep + git-show + sed commands used
- Plan SUMMARY records: total verdict-row count per REQ, total finding-candidate count per REQ, EXC-03 KI envelope status (RE_VERIFIED_AT_HEAD cc68bfc7), any scope-guard deferrals (gaps in Phase 243 catalog OR Phase 244 shared-context — recorded here, NOT edited into upstream files), Pre-Flag bullet-closure status for L2477-2500, working-file path `audit/v31-245-SDR.md` (consumed by 245-02 Task 4 consolidation step per CONTEXT.md D-05), HEAD anchor confirmation (`git rev-parse cc68bfc7` matched at task-start)
</success_criteria>

<output>
After completion, create `.planning/phases/245-sdgnrs-redemption-gameover-safety/245-01-SUMMARY.md` per the standard SUMMARY template + the per-bucket-plan additions:
- Per-REQ verdict-row count + floor severity (SDR-01..SDR-08)
- KI envelope status (EXC-03 RE_VERIFIED_AT_HEAD cc68bfc7 in §SDR-08)
- Phase 244 Pre-Flag bullet-closure status (10 SDR-grouped bullets L2477-2500 each CLOSED with closure note)
- Phase 244 V-row cross-cites made (GOX-02-V01/V02/V03 primary for SDR-06; GOX-03-V01..V03 primary for SDR-03; GOX-04-V01/V02 shared for SDR-01; GOX-06-V01 shared for SDR-08; RNG-01-V11 primary for SDR-08 EXC-03 non-widening)
- Path to working file: `audit/v31-245-SDR.md` (consumed by 245-02 consolidation step per CONTEXT.md D-05)
- Any scope-guard deferrals per CONTEXT.md D-22 (gaps in Phase 243 catalog OR Phase 244 shared-context)
- HEAD anchor confirmation (`cc68bfc7` matched at each task start)
- claimRedemption ungated-property absorption confirmation per CONTEXT.md D-09 (property-to-prove SAFE in SDR-01-V03, SDR-04-V01, SDR-05-V01..V06; zero standalone INFO finding-candidates)
</output>
</content>
</invoke>