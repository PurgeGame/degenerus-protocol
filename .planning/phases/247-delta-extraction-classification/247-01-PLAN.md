---
phase: 247-delta-extraction-classification
phase_number: 247
plan: 247-01
plan_number: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v32-247-DELTA-SURFACE.md
autonomous: true
milestone: v32.0
milestone_name: Backfill Idempotency + purchaseLevel Underflow Audit
deliverable: audit/v32-247-DELTA-SURFACE.md
phase_status: scope-anchor
write_policy: "Pure-catalog phase; zero contracts/ or test/ writes; writes confined to .planning/phases/247-*/ and audit/v32-247-*. v32.0 milestone READ-only-LIFTED but Phase 247 itself has no contract or test writes regardless per D-247-05."

# Requirement traceability — every Phase 247 REQ-ID MUST appear in this list
requirements:
  - DELTA-01
  - DELTA-02
  - DELTA-03
requirements_addressed:
  - DELTA-01
  - DELTA-02
  - DELTA-03

# HEAD anchor + audit baseline (per CONTEXT.md D-247-01 / D-247-03)
baseline: cc68bfc7
baseline_sha: cc68bfc7
head: acd88512
head_sha: acd88512
head_anchor: acd88512
head_anchor_history:
  - sha: acd88512
    scope: "Phase 247 single anchor: 4 in-scope contract-touching SHAs (8bdeabc2 / 6a63705b / 48554f8f / acd88512) + 1 out-of-scope test-only SHA (ad41973c). Verified at planning time 2026-04-30: `git diff --stat cc68bfc7..acd88512 -- contracts/` reports 4 files / +47 / -77."
working_tree_excluded:
  - "contracts/ContractAddresses.sol — deploy regen, ignored per D-247-03 (not part of audit-target commit history)"
  - "test/edge/LastPurchaseDayRace.test.js — Phase 251 scope per D-247-17 (untracked, plan-251 owns)"

# In-scope commits (chronological order; per-commit file attribution VERIFIED via `git show {sha} --stat`)
in_scope_commits:
  - sha: 8bdeabc2
    short_sha: 8bdeabc2
    subject: "fix(liveness): pause death clock during productive multi-call window"
    files: [contracts/storage/DegenerusGameStorage.sol]
    stat: "+12/-0 (GameStorage.sol only — _livenessTriggered productive-pause guard)"
    note: "Function name is _livenessTriggered which lives inside DegenerusGameStorage.sol (abstract base). SOURCE FILE is GameStorage, not AdvanceModule."
  - sha: 6a63705b
    short_sha: 6a63705b
    subject: "fix(mint): charge buyer not operator for purchaseCoin tickets"
    files: [contracts/modules/DegenerusGameMintModule.sol]
    stat: "+3/-6 (MintModule.sol only — _callTicketPurchase payer parameter dropped; both call sites pass buyer)"
  - sha: 48554f8f
    short_sha: 48554f8f
    subject: "refactor(vault): decouple share redemption from game operator approval"
    files: [contracts/DegenerusVault.sol]
    stat: "+17/-66 (DegenerusVault.sol only — burnCoin/burnEth signature change + NotApproved error / _requireApproved helper / IDegenerusGame import / game state constant removal + _burnCoinFor/_burnEthFor inline)"
    note: "GameStorage.sol +12 attribution in CONTEXT.md sample frontmatter is INCORRECT — git ground truth says GameStorage +12 belongs to 8bdeabc2 (liveness pause), not 48554f8f (Vault refactor). Plan uses git ground truth."
  - sha: acd88512
    short_sha: acd88512
    subject: "fix(advance): guard turbo block + make _backfillGapDays idempotent"
    files: [contracts/modules/DegenerusGameAdvanceModule.sol]
    stat: "+15/-5 (AdvanceModule.sol only — turbo block at L167 gains !rngLockedFlag conjunct + backfill branch at L1167 gains rngWordByDay[idx+1]==0 conjunct + NatSpec)"

# Out-of-scope commit (test-only per D-247-02)
out_of_scope_commits:
  - sha: ad41973c
    short_sha: ad41973c
    subject: "test(liveness): cover productive-phase pause regression at unit + integration"
    rationale: "test/ files only — out of catalog scope per D-247-02; Phase 251 owns"

# In-scope file aggregate (4 files at HEAD acd88512 vs baseline cc68bfc7)
in_scope_files:
  - path: contracts/DegenerusVault.sol
    touched_by: [48554f8f]
    stat: "+17/-66"
  - path: contracts/storage/DegenerusGameStorage.sol
    touched_by: [8bdeabc2]
    stat: "+12/-0"
  - path: contracts/modules/DegenerusGameMintModule.sol
    touched_by: [6a63705b]
    stat: "+3/-6"
  - path: contracts/modules/DegenerusGameAdvanceModule.sol
    touched_by: [acd88512]
    stat: "+15/-5"

# Aggregate sanity gate (locked from `git diff --stat cc68bfc7..acd88512 -- contracts/` at planning time)
aggregate_diff_stat:
  files_changed: 4
  insertions: 47
  deletions: 77

# Pre-locked classifications floor (per D-247-07)
prelocked_classifications:
  - sha: acd88512
    function: "advanceGame turbo block guard at AdvanceModule:167 (head-side line)"
    verdict: MODIFIED_LOGIC
    rationale: "&& !rngLockedFlag is a new conjunctive guard — control-flow branch added per D-247-06 MODIFIED_LOGIC definition"
  - sha: acd88512
    function: "rngGate / _backfillGapDays entry guard at AdvanceModule:1167 (head-side line)"
    verdict: MODIFIED_LOGIC
    rationale: "&& rngWordByDay[idx + 1] == 0 is a new conjunctive guard — control-flow branch added"
  - sha: 8bdeabc2
    function: "_livenessTriggered (in DegenerusGameStorage.sol around L1245-1247)"
    verdict: MODIFIED_LOGIC
    rationale: "early-return short-circuit (`if (lastPurchaseDay || jackpotPhaseFlag) return false`) is a new control-flow branch — death-clock state change"
  - sha: 6a63705b
    function: "purchaseCoin / _callTicketPurchase / consumePurchaseBoost / recordMint charge swap (MintModule)"
    verdict: MODIFIED_LOGIC
    rationale: "external-call target change — payer arg dropped; _coinReceive/recordMint/consumePurchaseBoost now charge buyer instead of msg.sender — observable on-chain effect change"
  - sha: 48554f8f
    function: "Vault burnCoin / burnEth / _burnCoinFor / _burnEthFor"
    verdict: MODIFIED_LOGIC_OR_REFACTOR_ONLY_PER_HUNK
    rationale: "per-function classification — operator-approval gate removal in burnCoin/burnEth = MODIFIED_LOGIC; pure-rename/inline relocation hunks = REFACTOR_ONLY; D-247-06 burden of proof — any doubt → MODIFIED_LOGIC"
  - sha: 48554f8f
    function: "Vault NotApproved error / _requireApproved helper / IDegenerusGame import / game state constant"
    verdict: DELETED
    rationale: "removed at HEAD; absent from acd88512; classification per-row in Section 2"

# must_haves block for goal-backward verification
must_haves:
  truths:
    - "audit/v32-247-DELTA-SURFACE.md exists at HEAD acd88512 and is the single canonical Phase 247 deliverable"
    - "All 7 sections present in fixed order per D-247-09 with literal headers (Section 0 — Overview & Row-ID Legend, Section 1 — Per-Source Changelog, Section 2 — Aggregate Function Classification, Section 3 — Downstream Call-Site Catalog, Section 4 — State Variable / Event / Interface Inventory, Section 5 — Storage Slot Layout Diff, Section 6 — Consumer Index, Section 7 — Reproduction Recipe Appendix)"
    - "Per CONTEXT.md D-247-08: every classification row in Section 2 cites a real diff hunk (file:line-range with +/- markers OR `git show {sha} -L <start>,<end>:<file>` reference) plus a one-line semantic-execution-changing element rationale"
    - "Per CONTEXT.md D-247-19: every Section 3 call-site row carries the exact `grep` command used to find it; aggregate reproduction recipe appendix lists all commands so a reviewer can replay Phase 247 from shell using portable POSIX syntax"
    - "Per CONTEXT.md D-247-21: zero F-32-NN finding IDs anywhere in the file (Phase 253 owns); Finding Candidates fresh-eyes subsection collects anomalies with file:line + rationale + suggested severity"
    - "Section 6 Consumer Index maps row IDs to Phase 248 BFL-01..06 / Phase 249 PLV-01..06 / Phase 250 SIB-01..05 / Phase 251 TST-01..04 / Phase 252 POST31-01..02 / Phase 253 FIND-01..04 + REG-01..02 entries (REQ-IDs sourced from REQUIREMENTS.md)"
    - "Section 5 storage slot diff embeds literal `forge inspect contracts/storage/DegenerusGameStorage.sol:DegenerusGameStorage storage-layout` output at both SHAs (cc68bfc7 and acd88512), with one row per slot whose label/offset/type/size changed; expected outcome is zero slot-layout-affecting rows because 8bdeabc2 +12 is NatSpec + a single non-storage early-return statement (no new state-variable declarations)"
    - "Per D-247-22: file frontmatter (or end-of-file metadata block) flips to FINAL READ-only on Task 5 plan-close commit (literal `status: FINAL — READ-ONLY`)"
    - "Per D-247-05: zero contracts/ or test/ writes; `git status --porcelain contracts/ test/` returns only the pre-existing `M contracts/ContractAddresses.sol` and `?? test/edge/LastPurchaseDayRace.test.js` lines (untouched by this plan)"
    - "Per D-247-14: 5 atomic per-task commits land (1 per task); each commit message follows the precedent format `audit(247-01): Task N — <task summary>` from Phase 246 246-01"
    - "Light reconciliation block per D-247-18 cross-checks any v31 Phase 243 row whose underlying function is touched by v32 deltas — especially AdvanceModule (acd88512 turbo+backfill on top of 16597cac/771893d1), MintModule (6a63705b on top of 6b3f4f3c/771893d1), Vault (48554f8f redemption flow vs v31 771893d1 sDGNRS landing), GameStorage (8bdeabc2 _livenessTriggered on top of 771893d1 14-day-grace fallback)"
    - "Section 0.3 Source Inventory table lists exactly 4 in-scope SHAs with the per-commit file attribution VERIFIED via `git show {sha} --stat`, plus 1 out-of-scope SHA `ad41973c` enumerated for completeness"
  artifacts:
    - path: "audit/v32-247-DELTA-SURFACE.md"
      provides: "DELTA-01 + DELTA-02 + DELTA-03 consolidated catalog — sole scope input for Phases 248-253 per ROADMAP Phase 247 Success Criterion 4"
      contains: "## Section 0 — Overview & Row-ID Legend, ## Section 1 — Per-Source Changelog, ## Section 2 — Aggregate Function Classification, ## Section 3 — Downstream Call-Site Catalog, ## Section 4 — State Variable / Event / Interface Inventory, ## Section 5 — Storage Slot Layout Diff, ## Section 6 — Consumer Index, ## Section 7 — Reproduction Recipe Appendix"
      min_lines: 350
  key_links:
    - from: "git diff cc68bfc7..acd88512 -- contracts/"
      to: "audit/v32-247-DELTA-SURFACE.md Section 1 per-source changelog rows"
      via: "per-commit `git show {sha} -- contracts/` hunk enumeration per D-247-18"
      pattern: "D-247-C[0-9]{3}"
    - from: "Section 1 changelog `func` rows"
      to: "Section 2 aggregate classification verdicts"
      via: "D-247-06 5-bucket rubric + D-247-08 hunk citation"
      pattern: "D-247-F[0-9]{3}"
    - from: "Section 1 + Section 4 changed function/interface symbols"
      to: "Section 3 call-site catalog rows"
      via: "D-247-15 grep-rn across contracts/ + D-247-19 grep-reproducibility mandate"
      pattern: "D-247-X[0-9]{3}"
    - from: "contracts/storage/DegenerusGameStorage.sol at cc68bfc7 AND acd88512"
      to: "Section 5 storage slot diff rows"
      via: "forge inspect storage-layout at both SHAs per D-247-16"
      pattern: "D-247-S[0-9]{3}"
    - from: "REQUIREMENTS.md BFL/PLV/SIB/TST/POST31/FIND/REG sections"
      to: "Section 6 Consumer Index"
      via: "v32.0 requirement → Phase 247 row-ID mapping"
      pattern: "D-247-I[0-9]{3}"
---

<objective>
Produce the authoritative v32.0 audit-surface catalog `audit/v32-247-DELTA-SURFACE.md` covering the 4 post-v31.0 contract-touching commits between v31.0 HEAD `cc68bfc7` and current HEAD `acd88512`. The deliverable is the SOLE scope input for Phases 248-253 per ROADMAP Phase 247 Success Criterion 4.

Three requirements addressed in one plan (single-plan multi-task atomic-commit topology per CONTEXT.md D-247-12, mirroring v30 Phase 242 + v31 Phase 246):

- **DELTA-01** — per-source function/state/event/interface inventory across the 4 in-scope SHAs (`8bdeabc2`, `6a63705b`, `48554f8f`, `acd88512`) + 1 out-of-scope SHA (`ad41973c` test-only) + storage slot-layout diff via `forge inspect` at both SHAs.
- **DELTA-02** — every changed function classified under the D-247-06 5-bucket rubric {NEW / MODIFIED_LOGIC / REFACTOR_ONLY / DELETED / RENAMED} with hunk citation + one-line rationale per D-247-08 / D-247-20. Pre-locked classification floors per D-247-07 honored.
- **DELTA-03** — every changed function + changed interface method's downstream call sites enumerated across `contracts/` via reproducible `grep -rn` per D-247-15 / D-247-19.

Plus: Section 6 Consumer Index mapping Phase 248..253 REQ-IDs (BFL/PLV/SIB/TST/POST31/FIND/REG) to Phase 247 row IDs; Section 7 reproduction recipe appendix; final READ-only flip per D-247-22.

5-task ordering per CONTEXT.md D-247-13:
1. **Task 1 (DELTA-01 enumeration)** — `git diff cc68bfc7..acd88512 -- contracts/` + per-commit `git show {sha} -- contracts/` for the 4 in-scope SHAs. Storage slot diff via `forge inspect contracts/storage/DegenerusGameStorage.sol:DegenerusGameStorage storage-layout` at both SHAs (head + worktree-mounted baseline). Light reconciliation against `audit/v31-243-DELTA-SURFACE.md` per D-247-18. Writes Sections 0/1/4/5 + reserved stubs for Sections 2/3/6 + first-pass Section 7.1.
2. **Task 2 (DELTA-02 classification)** — Iterate Task 1's row list; assign 5-bucket verdict per D-247-06 with hunk citation + one-line rationale per D-247-08 / D-247-20. Pre-locked verdicts from D-247-07 honored as floor. Writes Section 2 in place.
3. **Task 3 (DELTA-03 call-site catalog)** — `grep -rn` for every changed function + interface method across `contracts/` per D-247-15. One row per call site with caller file:line, caller function, call type (direct / delegatecall / self-call), exact `grep` command used per D-247-19. Writes Section 3 in place + appends §7.3.
4. **Task 4 (Consumer Index + reproduction recipe + final assembly)** — Map Phase 248..253 REQ-IDs to row IDs from Tasks 1-3. Concatenate all reproduction commands into Section 7 final consolidated form. Writes Section 6 + final Section 7 in place.
5. **Task 5 (READ-only flip + plan-close)** — Mark `audit/v32-247-DELTA-SURFACE.md` FINAL READ-only per D-247-22. Final atomic commit with plan-close metadata.

5 atomic per-task commits per D-247-14, mirroring Phase 246 246-01-PLAN.md commit-message precedent (`audit(247-01): Task N — <summary>`).

Row ID scheme per D-247-11 (5-prefix scheme, matches Phase 243):
- `D-247-C###` — Section 1 per-source changelog rows + Section 4 state/event/interface rows (contiguous numbering across §1 → §4)
- `D-247-F###` — Section 2 aggregate function classification rows
- `D-247-S###` — Section 5 storage slot layout diff rows
- `D-247-X###` — Section 3 downstream call-site catalog rows
- `D-247-I###` — Section 6 Consumer Index rows

Finding-ID emission: NO `F-32-NN` IDs anywhere (CONTEXT.md D-247-21 — Phase 253 FIND-01 owns). A `Finding Candidates (fresh-eyes)` subsection collects anomalies with file:line + rationale + suggested severity for Phase 253 routing.

Scope boundary per D-247-04 / D-247-05: 4 contract files in scope (DegenerusVault.sol, DegenerusGameStorage.sol, DegenerusGameMintModule.sol, DegenerusGameAdvanceModule.sol). Working-tree state untouched (M ContractAddresses.sol ignored per D-247-03; ?? LastPurchaseDayRace.test.js Phase 251 scope per D-247-17). Zero contracts/ or test/ writes throughout — pure-catalog phase.

Important per-commit attribution (verified via `git show {sha} --stat -- contracts/` at planning time):
- `8bdeabc2` touches **`contracts/storage/DegenerusGameStorage.sol`** only (+12/-0; `_livenessTriggered` lives in GameStorage abstract base — NOT AdvanceModule)
- `6a63705b` touches **`contracts/modules/DegenerusGameMintModule.sol`** only (+3/-6)
- `48554f8f` touches **`contracts/DegenerusVault.sol`** only (+17/-66; GameStorage NOT touched by this commit)
- `acd88512` touches **`contracts/modules/DegenerusGameAdvanceModule.sol`** only (+15/-5)
This per-commit attribution is what the plan-context's narrative description hints at (function-name basis) but where the in_scope_commits sample frontmatter in the planning context lists e.g. `8bdeabc2: [contracts/modules/DegenerusGameAdvanceModule.sol]` based on the function's behavioral impact rather than the diff target. Plan honors **git ground truth** for per-commit file attribution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/247-delta-extraction-classification/247-CONTEXT.md

<!-- Direct format precedent (FINAL READ-only — cite shape, never edit) -->
@audit/v31-243-DELTA-SURFACE.md

<!-- Plan-shape precedent (single-plan multi-task atomic-commit pattern per D-247-12 / D-247-14) -->
@.planning/milestones/v31.0-phases/246-findings-consolidation-lean-regression-appendix/246-01-PLAN.md

<!-- Per-task plan-shape precedents (DELTA-01 / DELTA-02 / DELTA-03 task patterns) -->
@.planning/milestones/v31.0-phases/243-delta-extraction-per-commit-classification/243-01-PLAN.md
@.planning/milestones/v31.0-phases/243-delta-extraction-per-commit-classification/243-02-PLAN.md
@.planning/milestones/v31.0-phases/243-delta-extraction-per-commit-classification/243-03-PLAN.md

<!-- Light reconciliation target (D-247-18) -->
@audit/v31-243-DELTA-SURFACE.md
@audit/FINDINGS-v31.0.md
@audit/KNOWN-ISSUES.md
</context>

<threat_model>
**Scope:** READ-only audit catalog production. Zero runtime code modified, zero test modified, zero deployment scripts touched. No attack surface added.

**Writes confined to:** `audit/v32-247-DELTA-SURFACE.md` (single file; inline 7-section deliverable per D-247-09 — no companion files expected given the +47/-77 4-file delta is well below any verbose-companion threshold).

**Threat surface introduced:** NONE (no contract code, no runtime behavior change). No STRIDE category applies to documentation-only output; threat register empty by construction.

**Regression / accuracy risk:** a miscounted or missed changed symbol becomes a wrong input to Phases 248-252 — mitigated by D-247-08 evidence burden (every Section 2 row cites hunk at file:line) + D-247-19 grep-reproducibility mandate (every Section 3 row carries exact grep command) + D-247-22 scope-guard deferral rule (Phases 248-252 record gaps in their own SUMMARYs rather than re-edit Phase 247 in place). Baseline sanity gate (`git diff --stat cc68bfc7..acd88512 -- contracts/` reports exactly `4 files changed, 47 insertions(+), 77 deletions(-)`) catches silent baseline drift inside Task 1.

Inherits parent phase threat model (READ-only-LIFTED milestone but Phase 247 itself is pure-catalog, zero contract-and-test surface).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: DELTA-01 enumeration — Sections 0 + 1 + 4 + 5 + 7.1 of audit/v32-247-DELTA-SURFACE.md</name>
  <files>audit/v32-247-DELTA-SURFACE.md</files>
  <read_first>
    - .planning/phases/247-delta-extraction-classification/247-CONTEXT.md — specifically D-247-01 (anchors cc68bfc7→acd88512), D-247-02 (contracts/ only), D-247-03 (ContractAddresses.sol ignored), D-247-04 (4-file scope), D-247-05 (no contracts/ test/ writes), D-247-08 (hunk-citation evidence burden), D-247-09 (7-section single deliverable), D-247-10 (column shapes), D-247-11 (Row ID format), D-247-15 (interface drift), D-247-16 (storage layout in Phase 247), D-247-17 (test/ deferred to Phase 251), D-247-18 (fresh + light reconciliation against v31-243), D-247-19 (grep-reproducibility), D-247-21 (no F-32-NN IDs)
    - audit/v31-243-DELTA-SURFACE.md — DIRECT format precedent for the 7-section shape; especially Section 0 legend block, Section 1 per-source changelog table format, Section 4 state/event/interface inventory shape, Section 5 storage slot diff format, Section 7.1 reproduction recipe shape. Cite shape — do NOT edit.
    - .planning/REQUIREMENTS.md DELTA section (DELTA-01 / DELTA-02 / DELTA-03 wordings)
    - .planning/ROADMAP.md Phase 247 success criteria block (4 items)

    HEAD-side reads for symbol-set enumeration at HEAD acd88512:
    - contracts/DegenerusVault.sol
    - contracts/storage/DegenerusGameStorage.sol
    - contracts/modules/DegenerusGameMintModule.sol
    - contracts/modules/DegenerusGameAdvanceModule.sol
    - contracts/interfaces/IDegenerusGame.sol (check if 48554f8f / 8bdeabc2 / acd88512 / 6a63705b touched it)
    - contracts/interfaces/IDegenerusVault.sol (check if 48554f8f changed it)
    - contracts/interfaces/IDegenerusGameStorage.sol (check if 8bdeabc2 changed it)
    - contracts/interfaces/IDegenerusGameMintModule.sol (check if 6a63705b changed it)
    - contracts/interfaces/IDegenerusGameAdvanceModule.sol (check if acd88512 changed it)

    Baseline-side reads (via `git show cc68bfc7:<path>`) — required to compute MODIFIED/DELETED symbol sets without working-tree contamination:
    - git show cc68bfc7:contracts/DegenerusVault.sol
    - git show cc68bfc7:contracts/storage/DegenerusGameStorage.sol
    - git show cc68bfc7:contracts/modules/DegenerusGameMintModule.sol
    - git show cc68bfc7:contracts/modules/DegenerusGameAdvanceModule.sol
  </read_first>
  <action>
Execute the DELTA-01 enumeration pass and write the first-pass consolidated deliverable.

**Step A — Verify baseline/HEAD sanity (mandatory gate):**

Run exactly these commands and check the output against the locked expectations from frontmatter:
```
git rev-parse cc68bfc7
git rev-parse acd88512
git rev-parse HEAD
git diff --stat cc68bfc7..acd88512 -- contracts/
git log --format='%H %s' cc68bfc7..acd88512
git diff --name-status cc68bfc7..acd88512 -- contracts/
git status --porcelain contracts/ test/
```

Locked expectations (from frontmatter `aggregate_diff_stat` + `in_scope_commits` + working_tree_excluded):
- `git rev-parse cc68bfc7` resolves to `cc68bfc70e76fb75ac6effbc2135aae978f96ff3`
- `git rev-parse acd88512` resolves to `acd88512c516bef51981d8b6f49de9878aba9159`
- `git rev-parse HEAD` may equal `acd88512...` directly OR a docs-only commit ABOVE acd88512 (e.g. `8185f94f` at planning time). If HEAD is above acd88512, verify `git diff cc68bfc7..acd88512 -- contracts/` and `git diff acd88512..HEAD -- contracts/ test/` (the latter MUST be empty — no contract/test deltas above the anchor).
- `git diff --stat cc68bfc7..acd88512 -- contracts/` last line reports exactly `4 files changed, 47 insertions(+), 77 deletions(-)`
- `git log --format='%H %s' cc68bfc7..acd88512` lists 5 commits in reverse-chronological order containing (in some order) `acd88512` (turbo+backfill), `48554f8f` (vault refactor), `6a63705b` (mint buyer-charge), `ad41973c` (test-only — out of scope), `8bdeabc2` (liveness pause). Note: `b02078d8` / `1ef9d846` (v31.0 milestone-archive docs commits) sit between `cc68bfc7` and `8bdeabc2` but touch only `.planning/` — they should NOT appear in the contracts/-filtered diff and may or may not appear in the unfiltered `git log cc68bfc7..acd88512` depending on which docs commits landed; either is acceptable as long as `--name-only -- contracts/` shows only the 4 in-scope files.
- `git diff --name-status cc68bfc7..acd88512 -- contracts/` lists exactly the 4 M-status files: `contracts/DegenerusVault.sol`, `contracts/modules/DegenerusGameAdvanceModule.sol`, `contracts/modules/DegenerusGameMintModule.sol`, `contracts/storage/DegenerusGameStorage.sol`. Zero `A`/`D`/`R` rows expected.
- `git status --porcelain contracts/ test/` returns EXACTLY two lines:
  - ` M contracts/ContractAddresses.sol` (deploy regen, ignored per D-247-03)
  - `?? test/edge/LastPurchaseDayRace.test.js` (Phase 251 scope per D-247-17)
  Any other line means the working tree drifted — STOP.

If ANY of these gates fails (especially the `4 files / 47 insertions / 77 deletions` aggregate), STOP. Do NOT write `audit/v32-247-DELTA-SURFACE.md`. Instead, log the mismatch inline as a scope-guard deferral for the executor-orchestrator to surface to the user — the baseline has drifted and the plan cannot proceed until CONTEXT.md is amended.

**Step B — For each of the 4 in-scope contract-touching commits, run per-commit `git show` to derive the symbol delta:**

```
git show 8bdeabc2 -- contracts/                         # expected: contracts/storage/DegenerusGameStorage.sol only (+12/-0)
git show 6a63705b -- contracts/                         # expected: contracts/modules/DegenerusGameMintModule.sol only (+3/-6)
git show 48554f8f -- contracts/                         # expected: contracts/DegenerusVault.sol only (+17/-66)
git show acd88512 -- contracts/                         # expected: contracts/modules/DegenerusGameAdvanceModule.sol only (+15/-5)
git show ad41973c --stat                                # test-only confirmation — verify contracts/ touch count is zero (expected: only test/ files)
```

For each in-scope commit, walk the hunk headers (`@@ -<baseline-range> +<head-range> @@`) and collect:
- Every added line starting with `+function ` / `+modifier ` / `+event ` / `+error ` → NEW symbol row
- Every removed line starting with `-function ` / `-modifier ` / `-event ` / `-error ` → DELETED row (cross-check via `git show cc68bfc7:<path>` whether the signature exists at HEAD at a different location — if yes, RENAMED candidate for Section 2; this Task only emits the per-commit row, the RENAMED/MODIFIED_LOGIC/NEW/DELETED final verdict is Task 2's D-247-06 job)
- Every added or removed line inside an EXISTING function body (file:line falls between a function header and its closing brace at both baseline and head) → MODIFIED row on that function (one row per function, regardless of how many lines inside it changed — per-line hunk detail goes in the One-Line Semantic Note column)
- Every added / removed line inside the contract-level storage declaration block → state-var row in Section 4
- Every added / removed `event Xxx(...);` → event row in Section 4
- Every added / removed interface method signature (in interface files) → interface row in Section 4
- Every added / removed `error Xxx(...);` → error row in Section 4 (treated as event row for symbol-kind purposes)

For MODIFIED functions, determine File:Line-Range at HEAD by finding the function header line + closing brace line at HEAD `acd88512`. Format: `contracts/<path>:<start>-<end>`.

For DELETED functions/errors/helpers (specifically: 48554f8f drops `NotApproved` error + `_requireApproved` helper + `IDegenerusGame` import + game state constant), use the BASELINE range via `git show cc68bfc7:<path>` with line-numbering.

**Step C — Capture storage slot layout via `forge inspect` at both SHAs (Section 5 per D-247-16):**

Head-side layout:
```
forge inspect contracts/storage/DegenerusGameStorage.sol:DegenerusGameStorage storage-layout > /tmp/v32-247-storage-layout-head.txt 2>&1
cat /tmp/v32-247-storage-layout-head.txt
```

Baseline-side layout via temporary `git worktree` (avoids touching main working tree):
```
WORKTREE_DIR=$(mktemp -d -t v32-247-baseline-XXXXXX)
git worktree add --detach "$WORKTREE_DIR" cc68bfc7
( cd "$WORKTREE_DIR" && forge inspect contracts/storage/DegenerusGameStorage.sol:DegenerusGameStorage storage-layout ) > /tmp/v32-247-storage-layout-baseline.txt 2>&1
git worktree remove --force "$WORKTREE_DIR"
cat /tmp/v32-247-storage-layout-baseline.txt
```

If `forge` is not available OR returns a compilation error at either SHA, STOP and record a scope-guard deferral inline — the storage diff is required scope input for Phase 250 SIB-04 / Phase 252 POST31-01. Do NOT fabricate the layout.

Compute slot-by-slot diff between baseline and head. Expected outcome (from frontmatter must_haves): zero slot-layout-changing rows because `8bdeabc2`'s +12 lines are 11 lines of NatSpec + 1 line of executable code (`if (lastPurchaseDay || jackpotPhaseFlag) return false;`) inside an existing `internal view` function (`_livenessTriggered`) — no new state variable declarations. If the diff turns up unexpected slot changes, that itself is a Finding Candidate for Section 1.6.

Change Type vocabulary for Section 5 rows: `APPENDED` (backwards-compatible end-append), `MOVED` (label moved to different slot — LAYOUT-BREAK candidate), `TYPE-CHANGED` (same slot, different type — SEMANTIC-BREAK candidate), `INSERTED` (mid-layout insertion displacing subsequent slots — LAYOUT-BREAK candidate), `REMOVED` (label gone — LAYOUT-BREAK candidate), `OFFSET-CHANGED` (packed-slot neighbor changed size — usually benign), `UNCHANGED` (omit unless needed for completeness).

**Step D — Light reconciliation against `audit/v31-243-DELTA-SURFACE.md` per D-247-18:**

For each v32 in-scope file, grep `audit/v31-243-DELTA-SURFACE.md` for any v31 row whose touched function is also touched by a v32 delta. Most likely overlap candidates (predicted from v31 catalog):
- AdvanceModule rows in v31 §1.2 (`16597cac` `advanceGame` modify) + v31 §1.4 (`771893d1` AdvanceModule rows D-243-C015 / C016 / C026) cross v32 `acd88512` AdvanceModule turbo+backfill
- MintModule rows in v31 §1.3 (`6b3f4f3c` D-243-C010 / C011 — `_purchaseFor` / `_callTicketPurchase`) + v31 §1.4 (`771893d1` D-243-C018..C021 — purchase-coin/etc liveness gate swap) cross v32 `6a63705b` MintModule charge swap
- Vault: v31 §1.4 `771893d1` touched `StakedDegenerusStonk.sol` + interfaces, but NOT `DegenerusVault.sol` directly. Reconciliation expectation: `48554f8f` is the FIRST v32 touch of Vault.sol since baseline cc68bfc7 → minimal v31 overlap; flag for reviewer awareness only.
- GameStorage: v31 §1.4 `771893d1` touched `DegenerusGameStorage.sol` (D-243-C026 — `_livenessTriggered` 14-day grace) cross v32 `8bdeabc2` `_livenessTriggered` productive-pause. **Direct overlap on the SAME function** — reconciliation MUST flag this and Phase 252 POST31-02 inherits the cross-cite.

Reconciliation verdict vocabulary per Phase 243 §1.7 precedent:
- `confirmed-delta-touches-v31-row` — v31-243-DELTA-SURFACE.md row's function is in Section 1 of this catalog; downstream Phase 252 POST31-02 must compose the v31 + v32 verdicts.
- `no-overlap` — v31 row is not in any of the 4 v32 delta files.

If zero overlap, write literally `Zero v31-243 row overlap with v32 deltas; v31-243 catalog requires no re-scoping.` (highly unlikely given the GameStorage `_livenessTriggered` direct overlap).

**Step E — Write `audit/v32-247-DELTA-SURFACE.md` with the 7-section skeleton:**

Use the literal headers below in this exact order. Replace every `<…>` placeholder with concrete data derived from Steps A-D — no `<placeholder>` tokens may remain in the committed file.

```markdown
---
status: IN_PROGRESS — Plan 247-01 single-plan multi-task
phase: 247 — Delta Extraction & Classification
milestone: v32.0 Backfill Idempotency + purchaseLevel Underflow Audit
baseline: cc68bfc7
head: acd88512
plan: 247-01
---

# v32.0 Phase 247 — Delta Surface Catalog

**Audit baseline:** `cc68bfc7` (v31.0 milestone HEAD; tag `v31.0`).
**Audit head:** `acd88512` (current contract-tree HEAD; latest contract-touching commit at planning time 2026-04-30).
**Phase:** 247 — Delta Extraction & Classification (DELTA-01 / DELTA-02 / DELTA-03).
**Scope:** READ-only-LIFTED at milestone level but Phase 247 is pure-catalog per D-247-05; zero `contracts/` or `test/` writes.
**Status:** IN PROGRESS — Plan 247-01 Task 1 populates Sections 0 / 1 / 4 / 5 / 7.1. Sections 2 / 3 / 6 are placeholder stubs reserved for Tasks 2 / 3 / 4 of the same plan. Task 5 flips this file to FINAL READ-only.

## Section 0 — Overview & Row-ID Legend

### 0.1 Overview

This file is the authoritative v32.0 audit-surface catalog covering the 4 post-v31.0 contract-touching commits between baseline `cc68bfc7` and head `acd88512`, plus 1 enumerated-but-out-of-scope test-only commit `ad41973c`. It is the SOLE scope input for Phases 248 (BFL Backfill Idempotency Proof), 249 (PLV purchaseLevel Correctness Proof), 250 (SIB Sibling-Pattern Sweep), 251 (TST Reproduction Tests — owns test/ but consumes Phase 247 contract classification), 252 (POST31 Landed-Commit Sanity), and 253 (FIND Findings Consolidation + REG Lean Regression), per ROADMAP Phase 247 Success Criterion 4.

**4-commit aggregate (from `git diff --stat cc68bfc7..acd88512 -- contracts/`):** 4 files changed, 47 insertions(+), 77 deletions(-). Net trim of 30 lines driven by the `48554f8f` Vault refactor.

**Reproducibility:** Every row in this file is derived from a documented `git diff` / `git show` / `forge inspect` / `grep` command. All commands are consolidated in Section 7 for reviewer replay using portable POSIX syntax.

### 0.2 Row-ID Legend (per CONTEXT.md D-247-11)

| Prefix | Section | Purpose | Owning Task |
|---|---|---|---|
| `D-247-C###` | Section 1 (per-source changelog) + Section 4 (state/event/interface/error inventory) | One row per changed symbol (function / state-var / event / error / interface method) across the 4 in-scope SHAs | Task 1 |
| `D-247-F###` | Section 2 (aggregate function classification) | One row per changed function with D-247-06 5-bucket classification + hunk citation + rationale | Task 2 |
| `D-247-S###` | Section 5 (storage slot layout diff) | One row per `DegenerusGameStorage.sol` slot that changed label / offset / type / size between baseline and head | Task 1 |
| `D-247-X###` | Section 3 (downstream call-site catalog) | One row per call site of each changed function / changed interface method discovered via `grep` across `contracts/` | Task 3 |
| `D-247-I###` | Section 6 (Consumer Index) | One row per Phase 248..253 REQ-ID mapping to the subset of 247 Row IDs scoped under it | Task 4 |

Row IDs are monotonic zero-padded three-digit integers within each prefix. `D-247-C###` numbering crosses Sections 1 → 4 contiguously (Section 1 first, then Section 4 continues the sequence).

### 0.3 Source Inventory

| Chrono Order | SHA (full) | Subject | Files Touched in contracts/ | Insertions | Deletions | In-Scope? | Role |
|---|---|---|---|---|---|---|---|
| 1 | `8bdeabc2599c889c849c0fbd3e816cd42f08763e` | fix(liveness): pause death clock during productive multi-call window | 1 (DegenerusGameStorage.sol) | 12 | 0 | YES | POST31-02 RE_VERIFY anchor |
| 2 | `ad41973ca6ffc7447aeb1cf44e8098145dfb78ae` | test(liveness): cover productive-phase pause regression at unit + integration | 0 (test/ only) | 0 | 0 | NO (D-247-02; Phase 251) | Phase 251 TST scope handoff |
| 3 | `6a63705b196cb94967c05a2b67b12f57584ed99c` | fix(mint): charge buyer not operator for purchaseCoin tickets | 1 (DegenerusGameMintModule.sol) | 3 | 6 | YES | POST31-01 sanity-anchor |
| 4 | `48554f8f350a5c10cbb4954a41eda652dff9253c` | refactor(vault): decouple share redemption from game operator approval | 1 (DegenerusVault.sol) | 17 | 66 | YES | POST31-01 sanity-anchor |
| 5 | `acd88512c516bef51981d8b6f49de9878aba9159` | fix(advance): guard turbo block + make _backfillGapDays idempotent | 1 (DegenerusGameAdvanceModule.sol) | 15 | 5 | YES | BFL-01..06 + PLV-01..06 + SIB-01..05 anchor |
| **In-scope total** | — | 4 commits | **4 distinct files (union)** | **47** | **77** | — | — |

Note: `b02078d8` (`chore: remove REQUIREMENTS.md for v31.0 milestone`) and `1ef9d846` (`chore: archive v31.0 milestone files`) sit between `cc68bfc7` and the next code commit but touch only `.planning/` — invisible to `git diff cc68bfc7..acd88512 -- contracts/`. NOT enumerated here per D-247-02 contracts/-only scope.

## Section 1 — Per-Source Changelog

Per CONTEXT.md D-247-10 columns: `Row ID | Commit SHA | File:Line-Range | Symbol Kind | Symbol Name | Change Type | One-Line Semantic Note`.

- **Row ID:** `D-247-C###` zero-padded, monotonic across Section 1 → Section 4.
- **Commit SHA:** short 8-char prefix.
- **File:Line-Range:** `contracts/<path>:<start>-<end>` at HEAD for MODIFIED/NEW rows; baseline range for DELETED rows.
- **Symbol Kind:** one of `func`, `modifier`, `state`, `event`, `error`, `interface-method`, `constant`, `import`, `none (test-only)`.
- **Symbol Name:** function/state-var/event/interface-method identifier (NOT full signature — full signature goes in Section 2 / Section 4 columns).
- **Change Type (per-source scope — coarser than D-247-06 classification):** one of `ADDED`, `REMOVED`, `MODIFIED`, `SIGNATURE-CHANGED`, `NATSPEC-ONLY`, `NO_CHANGE (test-only)`. The fine-grained D-247-06 5-bucket verdict (NEW / MODIFIED_LOGIC / REFACTOR_ONLY / DELETED / RENAMED) is Task 2's job in Section 2.
- **One-Line Semantic Note:** ≤ 12 words — names the specific element that drives the row.

### 1.1 Commit 8bdeabc2 — fix(liveness): pause death clock during productive multi-call window

**Change count card:** functions: REPLACE WITH actual count from fresh `git show` read / state-vars: 0 / events: 0 / interfaces: REPLACE / errors: 0 / call-sites-changed: TBD-Task-3.

Source command: `git show 8bdeabc2 -- contracts/storage/DegenerusGameStorage.sol`.

| Row ID | Commit SHA | File:Line-Range | Symbol Kind | Symbol Name | Change Type | One-Line Semantic Note |
|---|---|---|---|---|---|---|
| D-247-C001 | 8bdeabc2 | contracts/storage/DegenerusGameStorage.sol:REPLACE-WITH-actual-line-range-of-_livenessTriggered-at-acd88512 | func | `_livenessTriggered` | MODIFIED | early-return short-circuit when lastPurchaseDay or jackpotPhaseFlag set |
| D-247-C00N | 8bdeabc2 | REPLACE-WITH-actual-natspec-line-range | REPLACE-WITH-actual-symbol-kind | REPLACE-WITH-actual-symbol-name | NATSPEC-ONLY | productive-phase pause NatSpec block added above _livenessTriggered |

[Re-derive every changed symbol via fresh `git show 8bdeabc2 -- contracts/storage/DegenerusGameStorage.sol` read. Expected: 1 MODIFIED func row + 1 NATSPEC-ONLY row. Every `<…>` and "REPLACE WITH …" placeholder above MUST be replaced with a concrete value.]

### 1.2 Commit 6a63705b — fix(mint): charge buyer not operator for purchaseCoin tickets

**Change count card:** functions: REPLACE / state-vars: 0 / events: 0 / interfaces: 0 / errors: 0 / call-sites-changed: TBD-Task-3.

Source command: `git show 6a63705b -- contracts/modules/DegenerusGameMintModule.sol`.

| Row ID | Commit SHA | File:Line-Range | Symbol Kind | Symbol Name | Change Type | One-Line Semantic Note |
|---|---|---|---|---|---|---|
| D-247-C00N | 6a63705b | contracts/modules/DegenerusGameMintModule.sol:REPLACE | func | `_callTicketPurchase` | SIGNATURE-CHANGED | payer parameter dropped; both callers now pass buyer in both positions |
| D-247-C00N+1 | 6a63705b | contracts/modules/DegenerusGameMintModule.sol:REPLACE | func | REPLACE-WITH-actual-purchaseCoin-or-call-site-fn | MODIFIED | call-site update — msg.sender argument removed (single-arg buyer) |

[Re-derive every changed symbol via fresh `git show 6a63705b` read. Expected: at least 2 MODIFIED func rows (`_callTicketPurchase` signature change + the two call-sites that drop the `msg.sender` arg) plus the 4 internal call-sites within `_callTicketPurchase` itself (`consumePurchaseBoost(payer→buyer)`, `_coinReceive(payer→buyer)`, `recordMint(payer→buyer)` x2). Each call-site change is a hunk-line inside an EXISTING function body so it goes onto the SAME function row's One-Line Semantic Note — do NOT emit a separate row per hunk-line within the same function.]

### 1.3 Commit 48554f8f — refactor(vault): decouple share redemption from game operator approval

**Change count card:** functions: REPLACE / state-vars: 0 / events: 0 / interfaces: REPLACE-WITH-IDegenerusVault-touch-or-zero / errors: 1 (REMOVED — `NotApproved`) / constants: REPLACE-WITH-game-state-constant-removed-count / imports: 1 (REMOVED — `IDegenerusGame`) / call-sites-changed: TBD-Task-3.

Source command: `git show 48554f8f -- contracts/DegenerusVault.sol`.

| Row ID | Commit SHA | File:Line-Range | Symbol Kind | Symbol Name | Change Type | One-Line Semantic Note |
|---|---|---|---|---|---|---|
| D-247-C00N | 48554f8f | contracts/DegenerusVault.sol:REPLACE | func | `burnCoin` | SIGNATURE-CHANGED | address parameter dropped; only redeems msg.sender shares |
| D-247-C00N+1 | 48554f8f | contracts/DegenerusVault.sol:REPLACE | func | `burnEth` | SIGNATURE-CHANGED | address parameter dropped; only redeems msg.sender shares |
| D-247-C00N+2 | 48554f8f | contracts/DegenerusVault.sol:REPLACE-baseline-line-range | func | `_burnCoinFor` | REMOVED | inlined into burnCoin since approval-check no longer needed |
| D-247-C00N+3 | 48554f8f | contracts/DegenerusVault.sol:REPLACE-baseline-line-range | func | `_burnEthFor` | REMOVED | inlined into burnEth since approval-check no longer needed |
| D-247-C00N+4 | 48554f8f | contracts/DegenerusVault.sol:REPLACE-baseline-line-range | func | `_requireApproved` | REMOVED | game-operator approval helper no longer used |
| D-247-C00N+5 | 48554f8f | contracts/DegenerusVault.sol:REPLACE-baseline-line-range | error | `NotApproved` | REMOVED | game-operator approval-check error no longer raised |
| D-247-C00N+6 | 48554f8f | contracts/DegenerusVault.sol:REPLACE-baseline-line-range | import | `IDegenerusGame` | REMOVED | no longer needed since approval check decoupled |
| D-247-C00N+7 | 48554f8f | contracts/DegenerusVault.sol:REPLACE-baseline-line-range | constant | REPLACE-WITH-actual-game-state-constant-name | REMOVED | game state constant referenced only by removed approval helper |

[Re-derive every changed symbol via fresh `git show 48554f8f` read. Expected: 2 SIGNATURE-CHANGED + 4-6 REMOVED rows. If `IDegenerusVault` interface signature also changed (likely — burnCoin/burnEth signatures are part of the interface), check `contracts/interfaces/IDegenerusVault.sol` via `git show 48554f8f -- contracts/interfaces/` and emit corresponding interface rows in Section 4.3 — this is interface drift per D-247-15. If `git show 48554f8f -- contracts/interfaces/` returns empty, write "Interface IDegenerusVault NOT touched by 48554f8f" inline and DO NOT emit interface rows.]

### 1.4 Commit acd88512 — fix(advance): guard turbo block + make _backfillGapDays idempotent

**Change count card:** functions: REPLACE / state-vars: 0 / events: 0 / interfaces: 0 / errors: 0 / call-sites-changed: TBD-Task-3.

Source command: `git show acd88512 -- contracts/modules/DegenerusGameAdvanceModule.sol`.

| Row ID | Commit SHA | File:Line-Range | Symbol Kind | Symbol Name | Change Type | One-Line Semantic Note |
|---|---|---|---|---|---|---|
| D-247-C00N | acd88512 | contracts/modules/DegenerusGameAdvanceModule.sol:REPLACE-with-advanceGame-line-range-at-acd88512 | func | `advanceGame` | MODIFIED | turbo block at L167 gains !rngLockedFlag conjunctive guard + NatSpec |
| D-247-C00N+1 | acd88512 | contracts/modules/DegenerusGameAdvanceModule.sol:REPLACE-with-rngGate-or-_backfillGapDays-line-range | func | REPLACE-WITH-actual-fn-name (likely `rngGate` containing the L1167 hunk OR `_backfillGapDays` callsite if it's the same function) | MODIFIED | backfill branch at L1167 gains rngWordByDay[idx+1]==0 conjunctive guard + NatSpec |

[Re-derive every changed symbol via fresh `git show acd88512` read. The turbo guard hunk is at L164-172 head-side; the backfill guard hunk is at L1163-1175 head-side. Both hunks live INSIDE existing functions — the turbo hunk inside `advanceGame` and the backfill hunk inside whichever function owns L1167 (likely `rngGate` based on commit message reference to "rngGate's fresh-word path"). Confirm by reading function-header line above each hunk via `git show acd88512:contracts/modules/DegenerusGameAdvanceModule.sol | head -<line>`. Expected: 2 MODIFIED func rows. If both hunks are inside the SAME function, collapse to 1 row with combined One-Line Semantic Note.]

### 1.5 Commit ad41973c — test(liveness): cover productive-phase pause regression (test/ only — out of scope per D-247-02)

**Change count card:** functions: 0 / state-vars: 0 / events: 0 / interfaces: 0 / errors: 0 / call-sites-changed: 0.

Source command: `git show ad41973c --stat` (touches `test/` only; zero files in `contracts/`).

| Row ID | Commit SHA | File:Line-Range | Symbol Kind | Symbol Name | Change Type | One-Line Semantic Note |
|---|---|---|---|---|---|---|
| D-247-C00N | ad41973c | (none — test/ only) | none (test-only) | (none) | NO_CHANGE (test-only) | Phase 251 TST scope; enumerated for completeness per D-247-02 |

### 1.6 Finding Candidates (fresh-eyes)

Per CONTEXT.md D-247-21: any symbol discovered during the fresh enumeration pass that looks potentially worth a Phase 253 finding gets flagged here. Zero `F-32-NN` IDs — Phase 253 owns ID assignment.

Format: bullet list — `- contracts/<path>:<line> — <symbol> — <rationale> — suggested severity: <INFO|LOW|MEDIUM|HIGH>`.

[If the fresh pass surfaces anything, list it here. Likely candidates worth surfacing for Phase 253 routing:
- Any v31-243-DELTA row for AdvanceModule whose function is touched a SECOND time by `acd88512` (cumulative delta across two milestones)
- Any v31-243-DELTA row for `_livenessTriggered` (D-243-C026 14-day-grace) whose function is touched AGAIN by `8bdeabc2` (productive-pause early-return) — Phase 252 POST31-02 inherits this composition cross-cite
- Any 48554f8f Vault hunk that LOOKS like a refactor but quietly changes external-call ordering (D-247-06 burden of proof)
- Any unexpected slot-layout change from Section 5 (predicted to be zero — flag if non-zero)
If nothing surfaces, write literally `None surfaced during fresh enumeration.`]

### 1.7 Light Reconciliation Against audit/v31-243-DELTA-SURFACE.md

Per CONTEXT.md D-247-18: cross-check any v31 row whose underlying function is touched by v32 deltas. Narrower than Phase 237's full prior-artifact cross-check because v31.0 was already a complete delta audit at `cc68bfc7`.

| v31 Row ID (D-243-C###) | v31 Function | v32 Touched-By Commit | v32 Section 1 Row(s) | Reconciliation Verdict |
|---|---|---|---|---|
| D-243-C026 | `_livenessTriggered` (in DegenerusGameStorage.sol) | 8bdeabc2 | D-247-C001 | confirmed-delta-touches-v31-row — productive-pause early-return added on top of v31 14-day-grace fallback; Phase 252 POST31-02 composes both verdicts |
| REPLACE | REPLACE | REPLACE | REPLACE | REPLACE |

[Walk every v31 row whose function name matches one in v32 Sections 1.1-1.4. Expected non-empty overlap on `_livenessTriggered` (8bdeabc2 ↔ D-243-C026), and possibly on AdvanceModule advanceGame / rngGate (acd88512 ↔ D-243-C007 from 16597cac). Possibly minimal/zero overlap for MintModule (6a63705b vs v31 6b3f4f3c/771893d1 MintModule rows — different functions/hunks) and Vault (48554f8f vs v31 — Vault was untouched by v31, so no v31 row exists). If any column would say "no-overlap", omit those rows; only enumerate `confirmed-delta-touches-v31-row` rows.]

If the reconciliation table has zero `confirmed-delta-touches-v31-row` rows, write literally `Zero v31-243 row overlap with v32 deltas; v31-243 catalog requires no re-scoping.` (highly unlikely given the GameStorage `_livenessTriggered` direct overlap — predicted minimum 1 row).

## Section 2 — Aggregate Function Classification (RESERVED FOR TASK 2)

This section is reserved for Task 2 of this plan to populate the D-247-06 5-bucket classification table for every `func` row in Section 1. Row IDs `D-247-F###`. DO NOT edit this section in Task 1 — Task 2 replaces this stub in place.

Pre-locked classification floors per CONTEXT.md D-247-07 (Task 2 MUST honor these as floor; any contradiction requires explicit override rationale):

- `acd88512` AdvanceModule turbo block guard at L167 (`&& !rngLockedFlag`): MODIFIED_LOGIC (control-flow branch added)
- `acd88512` AdvanceModule rngGate / _backfillGapDays entry guard at L1167 (`&& rngWordByDay[idx + 1] == 0`): MODIFIED_LOGIC (control-flow branch added)
- `8bdeabc2` `_livenessTriggered` (in GameStorage.sol): MODIFIED_LOGIC (early-return short-circuit — death-clock state change)
- `6a63705b` MintModule purchaseCoin / `_callTicketPurchase` charge swap: MODIFIED_LOGIC (external-call target change — buyer-vs-operator)
- `48554f8f` Vault burnCoin / burnEth: MODIFIED_LOGIC (operator-approval gate removal — control-flow change)
- `48554f8f` Vault `_burnCoinFor` / `_burnEthFor`: REFACTOR_ONLY for pure inline relocation IF baseline body byte-identically appears at head's burnCoin/burnEth body (Task 2 verifies); else MODIFIED_LOGIC
- `48554f8f` Vault `_requireApproved` / `NotApproved` error / `IDegenerusGame` import / game state constant: DELETED (all removed)

## Section 3 — Downstream Call-Site Catalog (RESERVED FOR TASK 3)

This section is reserved for Task 3 of this plan to populate the grep-reproducible call-site inventory for every changed function and interface method. Row IDs `D-247-X###`. DO NOT edit this section in Tasks 1-2 — Task 3 replaces this stub in place.

## Section 4 — State Variable / Event / Interface / Error / Constant Inventory

Scope per CONTEXT.md D-247-09 item 4: all added/removed/signature-modified state variables (including any `DegenerusGameStorage.sol` deltas), every new `event` declaration, every changed interface method signature, every added/removed custom `error`, every added/removed top-level constant.

Row IDs continue `D-247-C###` sequence from Section 1 (do not restart numbering).

### 4.1 State Variables

| Row ID | Commit SHA | File:Line-Range | Symbol Name | Type Signature | Change Type | One-Line Semantic Note |
|---|---|---|---|---|---|---|
| REPLACE | REPLACE | REPLACE | REPLACE | REPLACE | REPLACE | REPLACE |

[Expected: 0 rows — `8bdeabc2`'s +12 lines are NatSpec + a single `if` statement inside an existing function body, NO new state variables. If grep on the diff confirms zero state-var deltas, write literally `None — no state variables added, removed, or signature-changed across the 4 in-scope SHAs.`]

### 4.2 Events

| Row ID | Commit SHA | File:Line-Range | Symbol Name | Event Signature | Change Type | One-Line Semantic Note |
|---|---|---|---|---|---|---|
| REPLACE | REPLACE | REPLACE | REPLACE | REPLACE | REPLACE | REPLACE |

[Expected: 0 rows — none of the 4 commits add or remove events. If confirmed, write `None.`]

### 4.3 Interface Methods

| Row ID | Commit SHA | File:Line-Range | Symbol Name | Method Signature | Change Type | One-Line Semantic Note |
|---|---|---|---|---|---|---|
| REPLACE | REPLACE | REPLACE | REPLACE | REPLACE | REPLACE | REPLACE |

[Expected: at most 2 rows for `IDegenerusVault.burnCoin` / `IDegenerusVault.burnEth` if `48554f8f` propagated the address-param drop into the interface (likely). Verify via `git show 48554f8f -- contracts/interfaces/IDegenerusVault.sol`. Also check `IDegenerusGameMintModule._callTicketPurchase` if it exists in any interface — `6a63705b` would have to drop the payer param there too. If neither interface was touched, write `None.`]

### 4.4 Errors / Custom Reverts

| Row ID | Commit SHA | File:Line-Range | Symbol Name | Error Signature | Change Type | One-Line Semantic Note |
|---|---|---|---|---|---|---|
| D-247-C00N | 48554f8f | contracts/DegenerusVault.sol:REPLACE-baseline-line-range | `NotApproved` | `error NotApproved()` (verify exact signature at baseline) | REMOVED | game-operator approval-check error no longer raised after Vault redemption decoupling |

### 4.5 Constants / Imports (REMOVED via 48554f8f)

| Row ID | Commit SHA | File:Line-Range | Symbol Name | Type/Source | Change Type | One-Line Semantic Note |
|---|---|---|---|---|---|---|
| D-247-C00N | 48554f8f | contracts/DegenerusVault.sol:REPLACE-baseline-line-range | `IDegenerusGame` (import) | import statement | REMOVED | unused after approval-check helper removed |
| D-247-C00N+1 | 48554f8f | contracts/DegenerusVault.sol:REPLACE-baseline-line-range | REPLACE-WITH-actual-game-state-constant-name | constant declaration | REMOVED | referenced only by _requireApproved |

If §4.1 / §4.2 / §4.3 are empty, write literally `None.` inside that subsection rather than leaving a blank table.

## Section 5 — Storage Slot Layout Diff (DegenerusGameStorage.sol)

Sole scope input for Phase 250 SIB-04 + Phase 252 POST31-01 per CONTEXT.md D-247-16. Verifies every slot that changed label / offset / type / size between baseline `cc68bfc7` and head `acd88512`. Among the 4 in-scope commits, only `8bdeabc2` touches `contracts/storage/DegenerusGameStorage.sol` (+12/-0); rows in this section, if any, are 100% attributed to `8bdeabc2`.

**Source command (reproducible):** `forge inspect contracts/storage/DegenerusGameStorage.sol:DegenerusGameStorage storage-layout` run at both SHAs. Baseline captured via `git worktree add --detach <tmp> cc68bfc7` to avoid touching the main working tree.

### 5.1 Baseline storage layout (at cc68bfc7)

<details><summary>Raw `forge inspect` output (baseline cc68bfc7)</summary>

```
REPLACE-WITH-literal-content-of-/tmp/v32-247-storage-layout-baseline.txt-pasted-verbatim
```

</details>

### 5.2 Head storage layout (at acd88512)

<details><summary>Raw `forge inspect` output (head acd88512)</summary>

```
REPLACE-WITH-literal-content-of-/tmp/v32-247-storage-layout-head.txt-pasted-verbatim
```

</details>

### 5.3 Slot-by-slot diff

| Row ID | Slot | Baseline (label / type / offset / bytes) | Head (label / type / offset / bytes) | Change Type | One-Line Note |
|---|---|---|---|---|---|
| D-247-S001 | REPLACE OR (no rows) | REPLACE | REPLACE | REPLACE | REPLACE |

Change Type vocabulary:
- `APPENDED` — new slot at end; no prior slot displaced (backwards-compatible)
- `MOVED` — existing label moved to a different slot number (LAYOUT-BREAK candidate)
- `TYPE-CHANGED` — same slot number, same label (or renamed), different solidity type/size (SEMANTIC-BREAK candidate)
- `INSERTED` — new slot inserted mid-layout displacing subsequent slots (LAYOUT-BREAK candidate)
- `REMOVED` — baseline slot absent at head (LAYOUT-BREAK candidate)
- `OFFSET-CHANGED` — packed slot where a neighbor changed size (within-slot layout shift; usually benign)

**Expected outcome:** zero rows — `8bdeabc2`'s +12 lines are NatSpec (11 lines) + 1 line of executable code inside an existing internal function body. No new state-variable declarations, so storage layout is byte-identical at both SHAs. If `diff /tmp/v32-247-storage-layout-baseline.txt /tmp/v32-247-storage-layout-head.txt` returns empty, write a single row `D-247-S001 | (n/a) | (identical) | (identical) | UNCHANGED | storage layout byte-identical at both SHAs (only NatSpec + non-storage early-return added by 8bdeabc2)` OR replace the table with the literal sentence "Storage layout byte-identical between baseline cc68bfc7 and head acd88512 — `diff /tmp/v32-247-storage-layout-baseline.txt /tmp/v32-247-storage-layout-head.txt` produces empty output. Confirms 8bdeabc2 introduces no new state slots." Either form is acceptable; pick one and document the choice in a one-line note.

If the diff DOES produce slot changes, that is itself a Finding Candidate worth a Section 1.6 entry (suggested severity HIGH if storage-layout-break detected, else INFO if a benign packing change).

### 5.4 Backwards-compatibility verdict (per-row)

| Row ID | Phase 250 SIB-04 / Phase 252 POST31-01 Expected Verdict | Rationale |
|---|---|---|
| D-247-S001 | REPLACE OR (n/a — no rows) | REPLACE OR "no slot changes; backwards-compatible by construction" |

## Section 6 — Consumer Index (RESERVED FOR TASK 4)

This section is reserved for Task 4 of this plan to populate the v32.0 requirement (BFL/PLV/SIB/TST/POST31/FIND/REG) → 247 Row-ID mapping. Row IDs `D-247-I###`. DO NOT edit this section in Tasks 1-3 — Task 4 replaces this stub in place.

## Section 7 — Reproduction Recipe Appendix

Per CONTEXT.md D-247-19: every command used by Phase 247 Plan 247-01 preserved here so a reviewer can replay the entire DELTA-01 / DELTA-02 / DELTA-03 enumeration from shell. Portable POSIX syntax only.

### 7.1 Task 1 commands (DELTA-01 enumeration + storage layout)

**Baseline sanity gate:**

```bash
git rev-parse cc68bfc7
git rev-parse acd88512
git rev-parse HEAD
git diff --stat cc68bfc7..acd88512 -- contracts/
git log --format='%H %s' cc68bfc7..acd88512
git diff --name-status cc68bfc7..acd88512 -- contracts/
git status --porcelain contracts/ test/
```

Expected: `git diff --stat` reports `4 files changed, 47 insertions(+), 77 deletions(-)`; `git status --porcelain contracts/ test/` returns exactly 2 lines (` M contracts/ContractAddresses.sol` + `?? test/edge/LastPurchaseDayRace.test.js`) — both untouched by this plan per D-247-03 / D-247-17.

**Per-source diff enumeration (Section 1):**

```bash
# In-scope commits (4 contract-touching SHAs)
git show 8bdeabc2 -- contracts/storage/DegenerusGameStorage.sol
git show 6a63705b -- contracts/modules/DegenerusGameMintModule.sol
git show 48554f8f -- contracts/DegenerusVault.sol
git show acd88512 -- contracts/modules/DegenerusGameAdvanceModule.sol

# Out-of-scope commit (test-only confirmation)
git show ad41973c --stat
```

**Per-function line-range read (for File:Line-Range column resolution):**

```bash
# Example for _livenessTriggered at HEAD
git show acd88512:contracts/storage/DegenerusGameStorage.sol | grep -n '^\s*function _livenessTriggered'
# → returns head-side header line; closing brace counted via awk or manual inspection

# Example for advanceGame at HEAD
git show acd88512:contracts/modules/DegenerusGameAdvanceModule.sol | grep -n '^\s*function advanceGame'
```

**Baseline-side source reads (for DELETED detection — 48554f8f Vault helpers):**

```bash
git show cc68bfc7:contracts/DegenerusVault.sol > /tmp/v32-247-baseline-DegenerusVault.sol
git show cc68bfc7:contracts/storage/DegenerusGameStorage.sol > /tmp/v32-247-baseline-DegenerusGameStorage.sol
git show cc68bfc7:contracts/modules/DegenerusGameMintModule.sol > /tmp/v32-247-baseline-DegenerusGameMintModule.sol
git show cc68bfc7:contracts/modules/DegenerusGameAdvanceModule.sol > /tmp/v32-247-baseline-DegenerusGameAdvanceModule.sol
```

**Storage slot layout diff (Section 5):**

```bash
# Head-side layout
forge inspect contracts/storage/DegenerusGameStorage.sol:DegenerusGameStorage storage-layout > /tmp/v32-247-storage-layout-head.txt 2>&1

# Baseline-side layout via temporary worktree (avoids touching main working tree)
WORKTREE_DIR=$(mktemp -d -t v32-247-baseline-XXXXXX)
git worktree add --detach "$WORKTREE_DIR" cc68bfc7
( cd "$WORKTREE_DIR" && forge inspect contracts/storage/DegenerusGameStorage.sol:DegenerusGameStorage storage-layout ) > /tmp/v32-247-storage-layout-baseline.txt 2>&1
git worktree remove --force "$WORKTREE_DIR"

# Diff (visual)
diff /tmp/v32-247-storage-layout-baseline.txt /tmp/v32-247-storage-layout-head.txt
```

**Light v31-243 reconciliation (Section 1.7):**

```bash
# Look for v31 rows whose function name matches a v32 in-scope function
for fn in _livenessTriggered advanceGame rngGate _backfillGapDays _callTicketPurchase _purchaseCoinFor purchaseCoin burnCoin burnEth _burnCoinFor _burnEthFor _requireApproved; do
  echo "=== $fn ==="
  grep -n "$fn" audit/v31-243-DELTA-SURFACE.md || true
done
```

### 7.2 Task 2 commands (DELTA-02 classification) — RESERVED FOR TASK 2

This subsection is appended by Task 2 of this plan during its execution per CONTEXT.md D-247-08 / D-247-20 (every classification row cites a hunk + one-line rationale, reproducible via `git show -L`).

### 7.3 Task 3 commands (DELTA-03 call-site catalog) — RESERVED FOR TASK 3

This subsection is appended by Task 3 of this plan during its execution per CONTEXT.md D-247-19 (every call-site row carries the exact `grep` command that found it).
```

Replace every `<…>` placeholder AND every `REPLACE`, `REPLACE WITH …`, `REPLACE-WITH-…` token in the template above with concrete data derived from the fresh `git show` / `forge inspect` reads. No placeholder tokens may remain in the committed file. Numeric placeholders `C00N` / `C00N+1` / `S001` MUST be replaced with actual monotonic zero-padded three-digit IDs (C001, C002, … through Cnnn / S001, S002, …) during writing.

After writing the file, run verification:
```
test -f audit/v32-247-DELTA-SURFACE.md
grep -q '^## Section 0 — Overview' audit/v32-247-DELTA-SURFACE.md
grep -q '^## Section 1 — Per-Source Changelog' audit/v32-247-DELTA-SURFACE.md
grep -q '^## Section 4 — State Variable / Event / Interface / Error / Constant Inventory' audit/v32-247-DELTA-SURFACE.md
grep -q '^## Section 5 — Storage Slot Layout Diff' audit/v32-247-DELTA-SURFACE.md
grep -q '^## Section 7 — Reproduction Recipe Appendix' audit/v32-247-DELTA-SURFACE.md
grep -q '^## Section 2 — Aggregate Function Classification (RESERVED FOR TASK 2)' audit/v32-247-DELTA-SURFACE.md
grep -q '^## Section 3 — Downstream Call-Site Catalog (RESERVED FOR TASK 3)' audit/v32-247-DELTA-SURFACE.md
grep -q '^## Section 6 — Consumer Index (RESERVED FOR TASK 4)' audit/v32-247-DELTA-SURFACE.md
grep -q '^### 1.1 Commit 8bdeabc2' audit/v32-247-DELTA-SURFACE.md
grep -q '^### 1.2 Commit 6a63705b' audit/v32-247-DELTA-SURFACE.md
grep -q '^### 1.3 Commit 48554f8f' audit/v32-247-DELTA-SURFACE.md
grep -q '^### 1.4 Commit acd88512' audit/v32-247-DELTA-SURFACE.md
grep -q '^### 1.5 Commit ad41973c' audit/v32-247-DELTA-SURFACE.md
grep -q '^### 1.6 Finding Candidates' audit/v32-247-DELTA-SURFACE.md
grep -q '^### 1.7 Light Reconciliation' audit/v32-247-DELTA-SURFACE.md
grep -q 'cc68bfc7' audit/v32-247-DELTA-SURFACE.md
grep -q 'acd88512' audit/v32-247-DELTA-SURFACE.md
[ "$(grep -c '^| D-247-C' audit/v32-247-DELTA-SURFACE.md)" -ge 8 ]
! grep -q 'F-32-' audit/v32-247-DELTA-SURFACE.md
! grep -qE 'REPLACE-WITH|REPLACE WITH|^REPLACE$|<placeholder>|<path>|<line-range>|<fn>|<fn-name>|<sha>|<kind>|<note>|<signature>|<method-name>|<error-name>|<state-var-name>|<solidity type>|<purpose>|<slot#>|<baseline label>|<head label>|<MOVED/TYPE-CHANGED|<pending-' audit/v32-247-DELTA-SURFACE.md
test -z "$(git status --porcelain audit/ contracts/ test/ | grep -v 'ContractAddresses.sol' | grep -v 'LastPurchaseDayRace.test.js' | grep -v 'audit/v32-247-DELTA-SURFACE.md')"
```

The last test confirms the only allowed working-tree changes are the pre-existing `ContractAddresses.sol`/`LastPurchaseDayRace.test.js` lines + this plan's `audit/v32-247-DELTA-SURFACE.md` write.

**Step F — Atomic per-task commit (D-247-14):**

```
git add audit/v32-247-DELTA-SURFACE.md
git commit -m "$(cat <<'EOF'
audit(247-01): Task 1 — DELTA-01 enumeration + Section 5 storage layout at HEAD acd88512

Plan 247-01 single-plan first-pass deliverable. Covers the 4 post-v31.0
contract-touching commits between baseline cc68bfc7 and head acd88512 per
CONTEXT.md D-247-01..D-247-22.

Sections populated:
  - Section 0: Overview + Row-ID Legend (C/F/S/X/I prefixes per D-247-11)
  - Section 1: Per-Source Changelog (4 in-scope commit subsections + 1
    out-of-scope test-only commit ad41973c per D-247-02; Finding Candidates
    + Light v31-243 reconciliation subsections present)
  - Section 4: State Variable / Event / Interface / Error / Constant inventory
  - Section 5: DegenerusGameStorage.sol slot-layout diff via forge inspect
    at both SHAs (sole scope input for Phase 250 SIB-04 + Phase 252 POST31-01
    per D-247-16)
  - Section 7.1: Reproduction Recipe Appendix (all git diff / git show /
    forge inspect / worktree commands used by this Task per D-247-19)

Sections reserved for downstream tasks:
  - Section 2: Aggregate Function Classification (RESERVED FOR TASK 2)
  - Section 3: Downstream Call-Site Catalog (RESERVED FOR TASK 3)
  - Section 6: Consumer Index (RESERVED FOR TASK 4)
  - Section 7.2 / 7.3: reproduction recipe stubs for Task 2 / Task 3

NO F-32-NN IDs (D-247-21 — Phase 253 FIND-01 owns).
Pure-catalog phase per D-247-05 — zero contracts/ or test/ writes.
File NOT yet marked READ-only — Task 5 performs the FINAL flip per D-247-22.

Baseline anchor: cc68bfc7 (D-247-01 frontmatter locked).
Head anchor: acd88512 (D-247-01 frontmatter locked).
EOF
)"
```

Do NOT push.
  </action>
  <verify>
    <automated>test -f audit/v32-247-DELTA-SURFACE.md && grep -q '^## Section 0 — Overview & Row-ID Legend' audit/v32-247-DELTA-SURFACE.md && grep -q '^## Section 1 — Per-Source Changelog' audit/v32-247-DELTA-SURFACE.md && grep -q '^## Section 4 — State Variable / Event / Interface / Error / Constant Inventory' audit/v32-247-DELTA-SURFACE.md && grep -q '^## Section 5 — Storage Slot Layout Diff (DegenerusGameStorage.sol)' audit/v32-247-DELTA-SURFACE.md && grep -q '^## Section 7 — Reproduction Recipe Appendix' audit/v32-247-DELTA-SURFACE.md && grep -q '^## Section 2 — Aggregate Function Classification (RESERVED FOR TASK 2)' audit/v32-247-DELTA-SURFACE.md && grep -q '^## Section 3 — Downstream Call-Site Catalog (RESERVED FOR TASK 3)' audit/v32-247-DELTA-SURFACE.md && grep -q '^## Section 6 — Consumer Index (RESERVED FOR TASK 4)' audit/v32-247-DELTA-SURFACE.md && grep -q '^### 1.1 Commit 8bdeabc2' audit/v32-247-DELTA-SURFACE.md && grep -q '^### 1.2 Commit 6a63705b' audit/v32-247-DELTA-SURFACE.md && grep -q '^### 1.3 Commit 48554f8f' audit/v32-247-DELTA-SURFACE.md && grep -q '^### 1.4 Commit acd88512' audit/v32-247-DELTA-SURFACE.md && grep -q '^### 1.5 Commit ad41973c' audit/v32-247-DELTA-SURFACE.md && grep -q '^### 1.6 Finding Candidates' audit/v32-247-DELTA-SURFACE.md && grep -q '^### 1.7 Light Reconciliation' audit/v32-247-DELTA-SURFACE.md && grep -q 'cc68bfc7' audit/v32-247-DELTA-SURFACE.md && grep -q 'acd88512' audit/v32-247-DELTA-SURFACE.md && [ "$(grep -c '^| D-247-C' audit/v32-247-DELTA-SURFACE.md)" -ge 8 ] && ! grep -q 'F-32-' audit/v32-247-DELTA-SURFACE.md && ! grep -qE 'REPLACE-WITH|REPLACE WITH|^REPLACE$|<line-range>|<fn>|<fn-name>|<sha>|<state-var-name>|<method-name>|<error-name>|<solidity type>|<purpose>|<slot#>' audit/v32-247-DELTA-SURFACE.md && [ "$(git log -1 --format='%s')" != "" ] && git log -1 --format='%s' | grep -qE 'audit\(247-01\): Task 1' && [ "$(git status --porcelain contracts/ test/ | grep -vE '^(M  |  M | M )?(contracts/ContractAddresses\.sol|\?\? test/edge/LastPurchaseDayRace\.test\.js)' | wc -l)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `audit/v32-247-DELTA-SURFACE.md` exists
    - File contains literal headers for Sections 0, 1, 2-stub, 3-stub, 4, 5, 6-stub, 7 in fixed order
    - Section 1 contains all 5 commit-subsection headers (1.1-1.5) + Finding Candidates (1.6) + Light Reconciliation (1.7)
    - Section 0.2 Row-ID Legend lists all 5 prefixes (`D-247-C`, `D-247-F`, `D-247-S`, `D-247-X`, `D-247-I`) in a table
    - Section 0.3 Source Inventory contains a table with 4 in-scope SHA rows + 1 out-of-scope test-only SHA row + 1 In-scope total row (5+1=6 data rows minimum)
    - Section 1 changelog rows ≥ 8 (covering at minimum: 1 row for 8bdeabc2 _livenessTriggered + 2 rows for 6a63705b MintModule + 4-6 rows for 48554f8f Vault REMOVED helpers + 2 rows for acd88512 + 1 row for ad41973c test-only — actual count expected higher)
    - Every Section 1 row has all 7 columns populated (no empty cells, no `<placeholder>` tokens, no `REPLACE WITH` tokens)
    - File contains literal `cc68bfc7` (baseline anchor) AND literal `acd88512` (HEAD anchor)
    - Section 1.5 row for `ad41973c` has Change Type = `NO_CHANGE (test-only)` per D-247-02
    - Section 1.7 has at minimum 1 reconciliation row for the `_livenessTriggered` direct overlap (D-243-C026 ↔ D-247-C001) — this is the ONE predicted non-empty overlap
    - Section 5 has subsections 5.1 / 5.2 / 5.3 / 5.4; the two `<details>` blocks contain VERBATIM `forge inspect` output OR a deferral note if forge unavailable; expected: zero slot-layout-changing rows in 5.3
    - Section 7.1 commands include all 5 per-commit `git show` calls (one per in-scope SHA + one for the test-only SHA), the baseline sanity gate, the storage-layout `forge inspect` + worktree pattern, and the v31-243 reconciliation grep loop
    - Section 7.2 / 7.3 reserved-stubs present (NOT populated by this Task)
    - File does NOT contain `F-32-` (any form) — D-247-21 enforcement
    - File does NOT contain placeholder tokens like `<path>`, `<line-range>`, `<fn>`, `<fn-name>`, `<state-var-name>`, `<method-name>`, `<error-name>`, `<solidity type>`, `<purpose>`, `<slot#>`, `REPLACE WITH`, `REPLACE-WITH-`
    - `git status --porcelain contracts/ test/` returns ONLY the pre-existing 2 lines (ContractAddresses.sol M + LastPurchaseDayRace.test.js ??) — no other changes
    - Git log shows 1 new commit with subject matching `audit\(247-01\): Task 1`
    - Commit body references CONTEXT.md decisions (D-247-01..D-247-22 range)
    - `git diff --name-only HEAD~1..HEAD` returns ONLY `audit/v32-247-DELTA-SURFACE.md`
    - Not pushed
  </acceptance_criteria>
  <deliverable>
`audit/v32-247-DELTA-SURFACE.md` first-pass file: Sections 0 + 1 + 2-stub + 3-stub + 4 + 5 + 6-stub + 7.1 populated with real data; fresh per-source changelog derived from `git show` output for the 4 in-scope SHAs; `forge inspect` storage layout at both SHAs (expected zero slot delta); light v31-243 reconciliation; finding candidates subsection. Sections 2 / 3 / 6 / 7.2 / 7.3 reserved as placeholder stubs for Tasks 2 / 3 / 4. One atomic commit per D-247-14.
  </deliverable>
  <done>Sections 0 + 1 + 4 + 5 + 7.1 populated from fresh git diff/show + forge inspect; all 5 source subsections present (4 in-scope + 1 out-of-scope test-only); ad41973c NO_CHANGE (test-only) row emitted per D-247-02; D-247-18 reconciliation table populated with at least 1 confirmed-overlap row; D-247-21 finding candidates subsection present; Section 2 / 3 / 6 / 7.2 / 7.3 stubs present for downstream tasks; zero F-32-NN IDs; zero contracts/ or test/ writes; one atomic commit landed per D-247-14.</done>
</task>

<task type="auto">
  <name>Task 2: DELTA-02 classification — populate Section 2 + append §7.2 of audit/v32-247-DELTA-SURFACE.md</name>
  <files>audit/v32-247-DELTA-SURFACE.md</files>
  <read_first>
    - audit/v32-247-DELTA-SURFACE.md (must exist from Task 1 with Section 1 populated and Section 2 RESERVED stub) — read every Section 1 row to build the Function Universe for classification
    - .planning/phases/247-delta-extraction-classification/247-CONTEXT.md — specifically D-247-06 (5-bucket rubric definitions: NEW / MODIFIED_LOGIC / REFACTOR_ONLY / DELETED / RENAMED), D-247-07 (pre-locked classification floors — non-negotiable), D-247-08 (hunk-citation evidence burden), D-247-19 (grep-reproducibility — Section 7.2 receives Task 2's `git show -L` commands), D-247-20 (classification evidence burden — every MODIFIED_LOGIC / REFACTOR_ONLY verdict cites a specific execution-trace-changing element OR specific non-execution-changing element)
    - .planning/milestones/v31.0-phases/243-delta-extraction-per-commit-classification/243-02-PLAN.md — DIRECT shape precedent for DELTA-02 classification task (table format, hunk-cite format, classification rubric application). Cite shape — do NOT edit.
    - audit/v31-243-DELTA-SURFACE.md Section 2 — direct format precedent for the populated Section 2 table

    Per-commit hunk reads at HEAD acd88512 (one fresh read per row in Section 1):
    - git show 8bdeabc2 -- contracts/storage/DegenerusGameStorage.sol  (re-read for hunk citation)
    - git show 6a63705b -- contracts/modules/DegenerusGameMintModule.sol  (re-read)
    - git show 48554f8f -- contracts/DegenerusVault.sol  (re-read; CRITICAL for REFACTOR_ONLY-vs-MODIFIED_LOGIC per-function decisions)
    - git show acd88512 -- contracts/modules/DegenerusGameAdvanceModule.sol  (re-read)

    Baseline-side reads (already cached at /tmp from Task 1 if Step E completed; otherwise re-fetch):
    - /tmp/v32-247-baseline-DegenerusVault.sol (or `git show cc68bfc7:contracts/DegenerusVault.sol`)
    - /tmp/v32-247-baseline-DegenerusGameStorage.sol
    - /tmp/v32-247-baseline-DegenerusGameMintModule.sol
    - /tmp/v32-247-baseline-DegenerusGameAdvanceModule.sol
  </read_first>
  <action>
Iterate the Section 1 row list from Task 1 and assign a D-247-06 5-bucket verdict to every `func` / `modifier` row. Pre-locked verdicts from CONTEXT.md D-247-07 are FLOOR — Task 2 may NOT contradict them; if any contradiction surfaces, halt and surface a scope-guard deferral with rationale.

**Step A — Build the Function Universe from Section 1:**

For each Section 1 row whose Symbol Kind ∈ {`func`, `modifier`}, that function is a Section 2 row. Section 1 rows with Symbol Kind ∈ {`state`, `event`, `error`, `interface-method`, `constant`, `import`, `none (test-only)`} are NOT classified in Section 2 (those live in Section 4 and don't carry a 5-bucket verdict). Expected universe size: ~6-12 functions across the 4 in-scope commits.

**Step B — Apply the D-247-06 5-bucket rubric per row:**

For each function in the Universe:
- **NEW** — Function did not exist at baseline `cc68bfc7`; appears at HEAD `acd88512` with a body. Verify: `grep -n "function <fn-name>" /tmp/v32-247-baseline-<file>.sol` returns nothing AND `grep -n "function <fn-name>" <head-version>` returns a header.
- **MODIFIED_LOGIC** — Function existed at baseline AND head; any state write (SSTORE), external call, control-flow branch, emitted event, return-path evaluation, or arithmetic operand changed. Cite the specific element in the rationale (e.g., "new conjunctive guard `&& !rngLockedFlag` adds a control-flow branch at L167").
- **REFACTOR_ONLY** — Function existed at baseline AND head; source-level shape changed (whitespace, parens, local variable names, multi-line decomposition, tuple destructuring, NatSpec, parameter rename) but execution trace is byte-equivalent. Burden of proof on REFACTOR_ONLY — any doubt escalates to MODIFIED_LOGIC per D-247-06.
- **DELETED** — Function existed at baseline; absent at head. Confirms via baseline-side grep returning a header AND head-side grep returning nothing.
- **RENAMED** — Same signature body at HEAD under a different name. Confirms via baseline grep finding NameA, head grep finding NameB, AND the bodies (extracted via `git show <sha>:<file> | sed -n '<start>,<end>p'`) being byte-equivalent except for the function header line.

**Step C — Honor pre-locked classifications per CONTEXT.md D-247-07 (non-negotiable floors):**

These verdicts MUST appear with these classifications. Any contradiction halts the task with an explicit `OVERRIDE RATIONALE` block explaining why the floor is wrong.

| Pre-Locked Function | Pre-Locked Verdict | Rationale (Floor) |
|---|---|---|
| `acd88512` AdvanceModule turbo guard at L167 (inside `advanceGame`) | MODIFIED_LOGIC | `&& !rngLockedFlag` is a new conjunctive guard — control-flow branch added |
| `acd88512` AdvanceModule backfill guard at L1167 (inside `rngGate` or whichever fn owns L1167) | MODIFIED_LOGIC | `&& rngWordByDay[idx + 1] == 0` is a new conjunctive guard — control-flow branch added |
| `8bdeabc2` `_livenessTriggered` (in GameStorage.sol) | MODIFIED_LOGIC | Early-return short-circuit `if (lastPurchaseDay || jackpotPhaseFlag) return false` — death-clock state change, control-flow branch added |
| `6a63705b` MintModule `_callTicketPurchase` | MODIFIED_LOGIC | External-call target change — `consumePurchaseBoost` / `_coinReceive` / `recordMint` now charge `buyer` instead of `payer`; observable on-chain effect change |
| `6a63705b` MintModule callers of `_callTicketPurchase` (the 2 sites that drop `msg.sender`) | MODIFIED_LOGIC | Argument list change at call site — observable effect (charge target swaps from operator to buyer) |
| `48554f8f` Vault `burnCoin` / `burnEth` | MODIFIED_LOGIC | Operator-approval gate removal — control-flow branch removed (NotApproved revert path eliminated); also signature change drops the address param |
| `48554f8f` Vault `_burnCoinFor` / `_burnEthFor` | DELETED (or REFACTOR_ONLY for inline-relocation IF body preserved byte-equivalent in caller) | Per-row: if baseline body byte-equivalently appears at head's burnCoin/burnEth body, REFACTOR_ONLY (inline relocation); else DELETED. D-247-06 burden of proof — any doubt → MODIFIED_LOGIC. |
| `48554f8f` Vault `_requireApproved` | DELETED | Helper removed; no caller at HEAD |

**Step D — Replace Section 2 placeholder with the populated table:**

Find the literal line `## Section 2 — Aggregate Function Classification (RESERVED FOR TASK 2)` and the entire RESERVED-stub block beneath it; replace with:

```markdown
## Section 2 — Aggregate Function Classification

Per CONTEXT.md D-247-06 5-bucket rubric: every `func` row in Section 1 receives one of {NEW / MODIFIED_LOGIC / REFACTOR_ONLY / DELETED / RENAMED}. Per D-247-08, every row cites a real diff hunk via `file:line-range` AND a one-line rationale per D-247-20 naming the specific execution-trace-changing element (SSTORE / external call / branch / emit / return-path / arithmetic operand) OR the specific non-execution-changing element (whitespace / rename / multi-line split). Pre-locked verdicts from D-247-07 are honored as floor.

**Universe size:** REPLACE-WITH-actual-count func rows from Section 1.

Columns per CONTEXT.md D-247-10: `Row ID | Function Signature | File:Line | Classification | Hunk Ref | One-Line Rationale`.

| Row ID | Function Signature | File:Line | Classification | Hunk Ref | One-Line Rationale |
|---|---|---|---|---|---|
| D-247-F001 | REPLACE-WITH-fn-signature-from-Section-1-row-D-247-C001 | contracts/storage/DegenerusGameStorage.sol:REPLACE | MODIFIED_LOGIC | `git show 8bdeabc2 -L <start>,<end>:contracts/storage/DegenerusGameStorage.sol` | early-return `if (lastPurchaseDay \|\| jackpotPhaseFlag) return false` short-circuits death clock during productive phase |
| D-247-F00N | REPLACE | REPLACE | REPLACE | REPLACE | REPLACE |

[Continue for every func/modifier row from Section 1. Every row's Hunk Ref column MUST cite either the file:line-range with +/- markers OR the exact `git show {sha} -L <start>,<end>:<file>` command that surfaces the hunk. Every Classification cell MUST be exactly one of {NEW, MODIFIED_LOGIC, REFACTOR_ONLY, DELETED, RENAMED} — no other vocabulary. Every Rationale cell MUST be ≤ 20 words and MUST name a specific element per D-247-20.]

### 2.1 Classification distribution (count card)

| Verdict | Count | Notes |
|---|---|---|
| NEW | REPLACE | REPLACE-with-fn-list-or-zero |
| MODIFIED_LOGIC | REPLACE | REPLACE-with-fn-list |
| REFACTOR_ONLY | REPLACE | REPLACE-with-fn-list-or-zero |
| DELETED | REPLACE | REPLACE-with-fn-list (expected: 48554f8f Vault helpers `_burnCoinFor`, `_burnEthFor`, `_requireApproved` if they are not REFACTOR_ONLY-via-inline) |
| RENAMED | REPLACE | REPLACE-with-fn-list-or-zero |
| **Total** | REPLACE | matches universe size from Section 1 func/modifier rows |
```

**Step E — Append §7.2 reproduction recipe:**

Find the literal line `### 7.2 Task 2 commands (DELTA-02 classification) — RESERVED FOR TASK 2` and replace its body with:

```markdown
### 7.2 Task 2 commands (DELTA-02 classification)

Per CONTEXT.md D-247-08 / D-247-20: every classification verdict cites a hunk reproducible via `git show -L`. Commands are grouped by source SHA.

**Per-row hunk extraction (sample — replace with actual line ranges populated):**

```bash
# 8bdeabc2 — _livenessTriggered productive-pause early-return
git show 8bdeabc2 -L <head-line-start>,<head-line-end>:contracts/storage/DegenerusGameStorage.sol

# 6a63705b — _callTicketPurchase signature change + 4 internal call-site charge swaps
git show 6a63705b -L <head-line-start>,<head-line-end>:contracts/modules/DegenerusGameMintModule.sol

# 48554f8f — Vault burnCoin / burnEth signature change
git show 48554f8f -L <head-line-start>,<head-line-end>:contracts/DegenerusVault.sol
# 48554f8f — DELETED helpers (read at baseline)
git show cc68bfc7 -L <baseline-line-start>,<baseline-line-end>:contracts/DegenerusVault.sol  # baseline-side read for _requireApproved / _burnCoinFor / _burnEthFor / NotApproved error

# acd88512 — turbo guard at L167 + backfill guard at L1167
git show acd88512 -L 160,180:contracts/modules/DegenerusGameAdvanceModule.sol
git show acd88512 -L 1160,1180:contracts/modules/DegenerusGameAdvanceModule.sol
```

**REFACTOR_ONLY verification (byte-equivalence proof for any REFACTOR_ONLY-classified row):**

```bash
# Pattern: extract baseline body + head body via sed and diff them
git show cc68bfc7:contracts/<path> | sed -n '<baseline-start>,<baseline-end>p' > /tmp/v32-247-baseline-fn.sol
git show acd88512:contracts/<path> | sed -n '<head-start>,<head-end>p' > /tmp/v32-247-head-fn.sol
diff /tmp/v32-247-baseline-fn.sol /tmp/v32-247-head-fn.sol
# Expected for REFACTOR_ONLY: only whitespace / NatSpec / parameter-rename diffs; no operand / branch / external-call changes.
```
```

Replace every `<…>` placeholder with the actual line ranges derived from the per-row hunk extraction.

**Step F — Verify:**

```
grep -q '^## Section 2 — Aggregate Function Classification$' audit/v32-247-DELTA-SURFACE.md
! grep -q 'RESERVED FOR TASK 2' audit/v32-247-DELTA-SURFACE.md
grep -q '^| D-247-F001' audit/v32-247-DELTA-SURFACE.md
grep -q 'MODIFIED_LOGIC' audit/v32-247-DELTA-SURFACE.md
grep -q '^### 2.1 Classification distribution' audit/v32-247-DELTA-SURFACE.md
grep -q '^### 7.2 Task 2 commands (DELTA-02 classification)$' audit/v32-247-DELTA-SURFACE.md
! grep -q '^### 7.2 Task 2 commands (DELTA-02 classification) — RESERVED FOR TASK 2' audit/v32-247-DELTA-SURFACE.md
git status --porcelain contracts/ test/ | grep -vE '(ContractAddresses\.sol|LastPurchaseDayRace\.test\.js)$' | wc -l   # MUST be 0
```

**Step G — Atomic commit per D-247-14:**

```
git add audit/v32-247-DELTA-SURFACE.md
git commit -m "$(cat <<'EOF'
audit(247-01): Task 2 — DELTA-02 aggregate function classification at HEAD acd88512

Plan 247-01 Task 2 deliverable. Iterates Section 1 func/modifier rows from
Task 1 and assigns a CONTEXT.md D-247-06 5-bucket verdict {NEW /
MODIFIED_LOGIC / REFACTOR_ONLY / DELETED / RENAMED} to each.

D-247-07 pre-locked classification floors honored:
  - acd88512 turbo guard L167 + backfill guard L1167 → MODIFIED_LOGIC
  - 8bdeabc2 _livenessTriggered productive-pause → MODIFIED_LOGIC
  - 6a63705b _callTicketPurchase + callers → MODIFIED_LOGIC (external-call
    target change)
  - 48554f8f Vault burnCoin/burnEth → MODIFIED_LOGIC (operator-approval gate
    removal)
  - 48554f8f Vault DELETED helpers (_burnCoinFor / _burnEthFor /
    _requireApproved + NotApproved error if classified at function-level)

Sections updated:
  - Section 2: Aggregate Function Classification populated; classification
    distribution count-card subsection 2.1 emits per-verdict counts
  - Section 7.2: Task 2 reproduction recipes appended (per-row git show -L
    + REFACTOR_ONLY byte-equivalence verification pattern)

Pure-catalog phase per D-247-05 — zero contracts/ or test/ writes.
File NOT yet marked READ-only — Task 5 performs the FINAL flip per D-247-22.

Baseline anchor: cc68bfc7. Head anchor: acd88512.
EOF
)"
```

Do NOT push.
  </action>
  <verify>
    <automated>grep -q '^## Section 2 — Aggregate Function Classification$' audit/v32-247-DELTA-SURFACE.md && ! grep -q '^## Section 2 — Aggregate Function Classification (RESERVED FOR TASK 2)' audit/v32-247-DELTA-SURFACE.md && grep -q '^| D-247-F001' audit/v32-247-DELTA-SURFACE.md && grep -q 'MODIFIED_LOGIC' audit/v32-247-DELTA-SURFACE.md && grep -q '^### 2.1 Classification distribution' audit/v32-247-DELTA-SURFACE.md && grep -q '^### 7.2 Task 2 commands (DELTA-02 classification)$' audit/v32-247-DELTA-SURFACE.md && ! grep -q '^### 7.2 Task 2 commands (DELTA-02 classification) — RESERVED FOR TASK 2' audit/v32-247-DELTA-SURFACE.md && [ "$(grep -c '^| D-247-F' audit/v32-247-DELTA-SURFACE.md)" -ge 5 ] && ! grep -q 'F-32-' audit/v32-247-DELTA-SURFACE.md && ! grep -qE 'REPLACE-WITH|REPLACE WITH|^REPLACE$|<head-line-start>|<baseline-line-start>|<fn-list>' audit/v32-247-DELTA-SURFACE.md && git log -1 --format='%s' | grep -qE 'audit\(247-01\): Task 2' && [ "$(git status --porcelain contracts/ test/ | grep -vE '(ContractAddresses\.sol|LastPurchaseDayRace\.test\.js)$' | wc -l)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - File contains literal header `## Section 2 — Aggregate Function Classification` (WITHOUT ` (RESERVED FOR TASK 2)` suffix)
    - Section 2 has at minimum 5 rows with Row IDs `D-247-F001..F00N` (one per func/modifier row from Section 1)
    - Every Section 2 Classification cell is exactly one of {NEW, MODIFIED_LOGIC, REFACTOR_ONLY, DELETED, RENAMED} (no other vocabulary)
    - Every Section 2 Hunk Ref cell cites either a `file:line-range` with +/- markers OR a `git show {sha} -L <start>,<end>:<file>` command
    - Every Section 2 Rationale cell ≤ 20 words and names a specific element (control-flow branch / SSTORE / external call / emit / return-path / arithmetic operand / whitespace / rename / multi-line split)
    - Pre-locked classifications from D-247-07 are present with the locked verdicts (any deviation must carry an `OVERRIDE RATIONALE` block — none expected)
    - Section 2.1 Classification distribution count-card has all 5 verdict rows + Total
    - Section 7.2 populated (no longer carries `RESERVED FOR TASK 2` suffix)
    - File still does NOT contain `F-32-` (D-247-21 enforcement)
    - File does NOT contain placeholder tokens like `REPLACE WITH`, `<head-line-start>`, `<baseline-line-start>`, `<fn-list>`
    - Git log shows 1 new commit with subject matching `audit\(247-01\): Task 2`
    - `git diff --name-only HEAD~1..HEAD` returns ONLY `audit/v32-247-DELTA-SURFACE.md`
    - `git status --porcelain contracts/ test/` excludes the 2 pre-existing lines + any audit/ writes; remainder is empty
    - Not pushed
  </acceptance_criteria>
  <deliverable>
Section 2 populated with the D-247-06 5-bucket classification table for every changed function, hunk-cited and rationaled per D-247-08 / D-247-20 with D-247-07 pre-locked floors honored. Section 7.2 carries the per-row git-show-L reproduction recipes + REFACTOR_ONLY byte-equivalence verification pattern. One atomic commit per D-247-14.
  </deliverable>
  <done>Section 2 populated; classification distribution count-card present; pre-locked floors honored; Section 7.2 reproduction recipes appended; zero F-32-NN IDs; zero contracts/ or test/ writes; one atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 3: DELTA-03 call-site catalog — populate Section 3 + append §7.3 of audit/v32-247-DELTA-SURFACE.md</name>
  <files>audit/v32-247-DELTA-SURFACE.md</files>
  <read_first>
    - audit/v32-247-DELTA-SURFACE.md (must exist from Tasks 1 + 2 with Sections 0 / 1 / 2 / 4 / 5 / 7.1 / 7.2 populated and Section 3 RESERVED stub) — read every Section 1 func/modifier row + every Section 4.3 changed-interface-method row to build the Caller-Search Universe
    - .planning/phases/247-delta-extraction-classification/247-CONTEXT.md — specifically D-247-15 (interface drift scope — changed interface methods get full call-site enumeration including self-calls via `IDegenerusGame(address(this))`), D-247-19 (grep-reproducibility mandate — every call-site row carries the exact `grep` command used to find it; portable POSIX syntax; truncate >50 lines with ellipsis annotation), D-247-02 (contracts/ tree only — NO test/ files)
    - .planning/milestones/v31.0-phases/243-delta-extraction-per-commit-classification/243-03-PLAN.md — DIRECT shape precedent for DELTA-03 call-site catalog task (grep methodology, call-type taxonomy, Consumer Index pattern). Cite shape — do NOT edit.
    - audit/v31-243-DELTA-SURFACE.md Section 3 — direct format precedent for the populated Section 3 table

    Live source files to grep against:
    - contracts/ tree (full recursive grep target)
  </read_first>
  <action>
Iterate the Section 1 + Section 4.3 row list and enumerate every downstream call site of each changed function and changed interface method across the `contracts/` tree per D-247-15. Every call-site row carries the exact `grep` command used to find it per D-247-19.

**Step A — Build the Caller-Search Universe:**

Universe membership:
- Every Section 1 row with Symbol Kind ∈ {`func`, `modifier`} where the function is publicly visible from outside its file (i.e., NOT `private` and NOT a `_helper` only called from one site within the same file UNLESS it's part of a delegatecall'd module)
- Every Section 4.3 row with Symbol Kind = `interface-method` (changed interface methods get full caller enumeration including self-calls per D-247-15)

For DELETED functions (e.g., `48554f8f` `_burnCoinFor` / `_burnEthFor` / `_requireApproved`): grep `contracts/` for the BASELINE name to confirm zero callers exist at HEAD (expected — they're deleted because there were no external callers, only internal-to-Vault.sol use). If grep finds callers at HEAD, that's a critical Finding Candidate.

**Step B — For each Universe member, run a documented `grep -rn`:**

```bash
# Example pattern — one block per Universe member
grep -rn '\b<fn-name>\s*(' contracts/ | grep -v 'function <fn-name>'   # exclude declaration line; portable POSIX (no Perl regex)
```

For each grep hit:
- **Caller File:Line:** the file and line number where the call appears
- **Caller Function:** the enclosing function name (find via reverse grep for `function ` from the hit line upward, or by `awk` walking braces)
- **Call Type:**
  - `direct` — `<fn-name>(args)` invoked directly within the same contract or via inheritance
  - `delegatecall` — `IDegenerusGameModules(...).<fn-name>(args)` or `address(...).delegatecall(abi.encodeCall(...))` patterns
  - `self-call` — `IDegenerusGame(address(this)).<fn-name>(args)` self-interface call (delegated module → host re-entry pattern)
  - `external` — call from a different contract (`game.<fn-name>(args)` where `game` is `IDegenerusGame`-typed and points outward, or any cross-contract call)
- **Grep Command Used:** the literal `grep -rn` command that surfaced this hit (per D-247-19)

If a single `grep -rn` produces > 50 lines of output, truncate to the first 50 hits with an ellipsis annotation `... (NN additional hits truncated; full output reproducible via the shown command)` — per D-247-19.

**Step C — Replace Section 3 placeholder with the populated catalog:**

Find the literal line `## Section 3 — Downstream Call-Site Catalog (RESERVED FOR TASK 3)` and the entire RESERVED-stub block beneath it; replace with:

```markdown
## Section 3 — Downstream Call-Site Catalog

Per CONTEXT.md D-247-15 + D-247-19: every changed function (Section 1) and changed interface method (Section 4.3) gets every call site across `contracts/` enumerated via `grep -rn`. Every row carries the exact grep command used to find it. Self-calls via `IDegenerusGame(address(this)).<fn>` and delegatecall selectors via `IDegenerusGameModules` are enumerated. test/ tree out of scope per D-247-02.

Columns per CONTEXT.md D-247-10: `Changed Function/Interface Method | Caller File:Line | Caller Function | Call Type | Grep Command Used`. Row IDs `D-247-X###`.

| Row ID | Changed Function/Interface Method | Caller File:Line | Caller Function | Call Type | Grep Command Used |
|---|---|---|---|---|---|
| D-247-X001 | REPLACE-WITH-changed-fn-from-Section-1 | contracts/REPLACE:REPLACE | REPLACE-with-enclosing-fn | direct | `grep -rn '\b<fn>\s*(' contracts/` |
| D-247-X00N | REPLACE | REPLACE | REPLACE | REPLACE | REPLACE |

[Continue for every caller of every Universe member. Group rows by Changed Function/Interface Method for readability — within each group, sort by Caller File:Line ascending. If any Universe member has zero callers at HEAD (expected for deleted helpers `_burnCoinFor` / `_burnEthFor` / `_requireApproved`), emit a single row with `Caller File:Line = (none — confirmed via grep -rn returning no contract-tree hits)` and the grep command, and flag in §3.1 if unexpected.]

### 3.1 Per-Universe-Member call-site count card

| Changed Function / Interface Method | Touched-By Commit | Caller Count (contracts/ only) | Notes |
|---|---|---|---|
| `_livenessTriggered` (in GameStorage.sol) | 8bdeabc2 | REPLACE | expected high — gates many purchase / burn paths per v31 D-243-X catalog |
| `_callTicketPurchase` | 6a63705b | REPLACE | expected ≥ 2 (the 2 sites that drop msg.sender per commit message) |
| `burnCoin` | 48554f8f | REPLACE | external — called from sDGNRS / game-side redemption flow |
| `burnEth` | 48554f8f | REPLACE | external — same shape as burnCoin |
| `_burnCoinFor` (DELETED) | 48554f8f | 0 (expected — confirmed by grep) | Finding Candidate if non-zero |
| `_burnEthFor` (DELETED) | 48554f8f | 0 (expected — confirmed by grep) | Finding Candidate if non-zero |
| `_requireApproved` (DELETED) | 48554f8f | 0 (expected — confirmed by grep) | Finding Candidate if non-zero |
| `advanceGame` (turbo guard hunk) | acd88512 | REPLACE | external — primary entry point |
| `rngGate` or whichever fn owns L1167 (backfill guard hunk) | acd88512 | REPLACE | likely internal-only |
| REPLACE-WITH-other-Universe-members | REPLACE | REPLACE | REPLACE |

### 3.2 Self-call / delegatecall enumeration (per D-247-15)

For every Universe member that is invokable via `IDegenerusGame(address(this)).<fn>` (self-interface call) OR via `IDegenerusGameModules` delegatecall selector, enumerate the indirect call sites:

| Universe Member | Indirect Call Site | Pattern | Grep Command |
|---|---|---|---|
| REPLACE | REPLACE | self-interface OR delegatecall | REPLACE |

If no Universe member is reachable via `IDegenerusGame(address(this)).<fn>` or delegatecall (likely — these are mostly internal utility functions), write `None — no Universe member reachable via self-interface or delegatecall pattern.` per D-247-15.
```

**Step D — Append §7.3 reproduction recipe:**

Find the literal line `### 7.3 Task 3 commands (DELTA-03 call-site catalog) — RESERVED FOR TASK 3` and replace its body with:

```markdown
### 7.3 Task 3 commands (DELTA-03 call-site catalog)

Per CONTEXT.md D-247-19: every call-site row carries the exact `grep -rn` command that found it. Portable POSIX syntax (no GNU-only `grep -P`).

**Per-Universe-Member grep enumeration:**

```bash
# 8bdeabc2 — _livenessTriggered callers (high-density expected)
grep -rn '\b_livenessTriggered\s*(' contracts/ | grep -v 'function _livenessTriggered'

# 6a63705b — _callTicketPurchase callers
grep -rn '\b_callTicketPurchase\s*(' contracts/ | grep -v 'function _callTicketPurchase'

# 48554f8f — burnCoin / burnEth callers (external + self)
grep -rn '\bburnCoin\s*(' contracts/ | grep -v 'function burnCoin'
grep -rn '\bburnEth\s*(' contracts/ | grep -v 'function burnEth'
# DELETED helpers — confirm zero callers at HEAD
grep -rn '\b_burnCoinFor\s*(' contracts/   # expected: empty
grep -rn '\b_burnEthFor\s*(' contracts/    # expected: empty
grep -rn '\b_requireApproved\s*(' contracts/  # expected: empty

# acd88512 — advanceGame + whichever-fn-owns-L1167 callers
grep -rn '\badvanceGame\s*(' contracts/ | grep -v 'function advanceGame'
grep -rn '\b<L1167-fn-name>\s*(' contracts/ | grep -v 'function <L1167-fn-name>'

# Self-interface / delegatecall enumeration
grep -rn 'IDegenerusGame(address(this))\.' contracts/ | grep -E '_livenessTriggered|_callTicketPurchase|advanceGame|burnCoin|burnEth'
grep -rn 'IDegenerusGameModules' contracts/ | head -20   # delegatecall selector base; sample only
```
```

Replace `<L1167-fn-name>` with the actual function name resolved during Task 1 Step B.

**Step E — Verify:**

```
grep -q '^## Section 3 — Downstream Call-Site Catalog$' audit/v32-247-DELTA-SURFACE.md
! grep -q 'RESERVED FOR TASK 3' audit/v32-247-DELTA-SURFACE.md
grep -q '^| D-247-X001' audit/v32-247-DELTA-SURFACE.md
grep -q '^### 3.1 Per-Universe-Member call-site count card' audit/v32-247-DELTA-SURFACE.md
grep -q '^### 3.2 Self-call / delegatecall enumeration' audit/v32-247-DELTA-SURFACE.md
grep -q '^### 7.3 Task 3 commands (DELTA-03 call-site catalog)$' audit/v32-247-DELTA-SURFACE.md
! grep -q '^### 7.3 Task 3 commands (DELTA-03 call-site catalog) — RESERVED FOR TASK 3' audit/v32-247-DELTA-SURFACE.md
grep -q 'grep -rn' audit/v32-247-DELTA-SURFACE.md
```

**Step F — Atomic commit per D-247-14:**

```
git add audit/v32-247-DELTA-SURFACE.md
git commit -m "$(cat <<'EOF'
audit(247-01): Task 3 — DELTA-03 downstream call-site catalog at HEAD acd88512

Plan 247-01 Task 3 deliverable. Enumerates every downstream call site of
each changed function (Section 1) and changed interface method (Section
4.3) across contracts/ tree per CONTEXT.md D-247-15.

Sections updated:
  - Section 3: Downstream Call-Site Catalog populated (one row per call
    site with caller file:line + enclosing function + call type +
    exact grep command per D-247-19)
  - Section 3.1: per-Universe-Member call-site count card (caller-density
    summary)
  - Section 3.2: self-interface / delegatecall enumeration per D-247-15
    self-call carve-out
  - Section 7.3: Task 3 reproduction recipes appended (every grep -rn
    command preserved for reviewer replay; portable POSIX syntax)

48554f8f DELETED-helper sanity gate: _burnCoinFor / _burnEthFor /
_requireApproved confirmed zero callers at HEAD via grep -rn (any
non-zero is a Finding Candidate routed to Phase 253 FIND-01).

contracts/ tree only — test/ out of scope per D-247-02 (Phase 251 owns).

Pure-catalog phase per D-247-05 — zero contracts/ or test/ writes.
File NOT yet marked READ-only — Task 5 performs the FINAL flip per D-247-22.

Baseline anchor: cc68bfc7. Head anchor: acd88512.
EOF
)"
```

Do NOT push.
  </action>
  <verify>
    <automated>grep -q '^## Section 3 — Downstream Call-Site Catalog$' audit/v32-247-DELTA-SURFACE.md && ! grep -q '^## Section 3 — Downstream Call-Site Catalog (RESERVED FOR TASK 3)' audit/v32-247-DELTA-SURFACE.md && grep -q '^| D-247-X001' audit/v32-247-DELTA-SURFACE.md && grep -q '^### 3.1 Per-Universe-Member call-site count card' audit/v32-247-DELTA-SURFACE.md && grep -q '^### 3.2 Self-call / delegatecall enumeration' audit/v32-247-DELTA-SURFACE.md && grep -q '^### 7.3 Task 3 commands (DELTA-03 call-site catalog)$' audit/v32-247-DELTA-SURFACE.md && ! grep -q '^### 7.3 Task 3 commands (DELTA-03 call-site catalog) — RESERVED FOR TASK 3' audit/v32-247-DELTA-SURFACE.md && [ "$(grep -c '^| D-247-X' audit/v32-247-DELTA-SURFACE.md)" -ge 5 ] && grep -q 'grep -rn' audit/v32-247-DELTA-SURFACE.md && ! grep -q 'F-32-' audit/v32-247-DELTA-SURFACE.md && ! grep -qE 'REPLACE-WITH|REPLACE WITH|^REPLACE$|<L1167-fn-name>|<fn>|<fn-name>' audit/v32-247-DELTA-SURFACE.md && git log -1 --format='%s' | grep -qE 'audit\(247-01\): Task 3' && [ "$(git status --porcelain contracts/ test/ | grep -vE '(ContractAddresses\.sol|LastPurchaseDayRace\.test\.js)$' | wc -l)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - File contains literal header `## Section 3 — Downstream Call-Site Catalog` (WITHOUT ` (RESERVED FOR TASK 3)` suffix)
    - Section 3 has at minimum 5 rows with Row IDs `D-247-X001..X00N` (one per call site of every Universe member)
    - Every Section 3 Call Type cell is exactly one of {direct, delegatecall, self-call, external} (no other vocabulary)
    - Every Section 3 Grep Command Used cell contains a literal `grep -rn` command (D-247-19 reproducibility mandate)
    - Section 3.1 count card has one row per Universe member (≥ 8 rows expected: _livenessTriggered, _callTicketPurchase, burnCoin, burnEth, _burnCoinFor, _burnEthFor, _requireApproved, advanceGame, +1 for L1167 fn)
    - Section 3.2 self-call / delegatecall enumeration present (may be `None.` if no Universe member is reachable via self-interface)
    - 48554f8f DELETED-helper sanity: _burnCoinFor / _burnEthFor / _requireApproved rows in Section 3.1 count card show 0 callers (else Finding Candidate)
    - Section 7.3 populated (no longer carries `RESERVED FOR TASK 3` suffix)
    - File still does NOT contain `F-32-` (D-247-21 enforcement)
    - File does NOT contain placeholder tokens like `REPLACE WITH`, `<L1167-fn-name>`, `<fn-name>`
    - Git log shows 1 new commit with subject matching `audit\(247-01\): Task 3`
    - `git diff --name-only HEAD~1..HEAD` returns ONLY `audit/v32-247-DELTA-SURFACE.md`
    - `git status --porcelain contracts/ test/` excludes only the 2 pre-existing lines; remainder empty
    - Not pushed
  </acceptance_criteria>
  <deliverable>
Section 3 populated with every downstream call site of every changed function + interface method enumerated from contracts/ via grep -rn. Section 3.1 count card surfaces caller density per Universe member. Section 3.2 enumerates self-interface / delegatecall indirect callers per D-247-15 carve-out. Section 7.3 carries every grep -rn command for reviewer replay. One atomic commit per D-247-14.
  </deliverable>
  <done>Section 3 populated; Section 3.1 + 3.2 subsections present; 48554f8f DELETED-helper zero-caller sanity confirmed; Section 7.3 reproduction recipes appended; zero F-32-NN IDs; zero contracts/ or test/ writes; one atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 4: Consumer Index + final Section 7 reproduction recipe assembly + Section 6 of audit/v32-247-DELTA-SURFACE.md</name>
  <files>audit/v32-247-DELTA-SURFACE.md</files>
  <read_first>
    - audit/v32-247-DELTA-SURFACE.md (must exist from Tasks 1-3 with Sections 0 / 1 / 2 / 3 / 4 / 5 / 7.1 / 7.2 / 7.3 populated and Section 6 RESERVED stub)
    - .planning/phases/247-delta-extraction-classification/247-CONTEXT.md — specifically D-247-09 item 6 (Consumer Index — v32.0 requirement → Phase 247 row-ID mapping), D-247-21 (no F-32-NN IDs)
    - .planning/REQUIREMENTS.md sections BFL / PLV / SIB / TST / POST31 / FIND / REG (read every requirement bullet to populate Section 6 row text)
    - .planning/ROADMAP.md Phase 247-253 entries (verify REQ-IDs match: BFL-01..06, PLV-01..06, SIB-01..05, TST-01..04, POST31-01..02, FIND-01..04, REG-01..02 = 29 REQ-IDs total across 6 downstream phases)
    - audit/v31-243-DELTA-SURFACE.md Section 6 — DIRECT format precedent for the populated Consumer Index table
  </read_first>
  <action>
Build Section 6 Consumer Index mapping every Phase 248..253 REQ-ID to the subset of Phase 247 row IDs (D-247-C### / D-247-F### / D-247-S### / D-247-X###) that scope it. Then assemble final Section 7 with all reproduction commands consolidated.

**Step A — Build the Consumer Index table:**

For each REQ-ID across Phase 248..253, identify which Phase 247 rows are scope-relevant:

| Phase | REQ-ID | Phase 247 Row Scope (predicted) | Rationale |
|---|---|---|---|
| 248 | BFL-01..06 | Section 1.4 + 2 + 3 rows for `acd88512` AdvanceModule (turbo guard + backfill guard + advanceGame + L1167-fn + _backfillGapDays callsites from §3) | Backfill idempotency proof needs full classification + caller catalog of acd88512 changes |
| 249 | PLV-01..06 | Section 1.4 + 2 + 3 rows for `acd88512` AdvanceModule (advanceGame + purchaseLevel reads via §3 grep results) + Section 1.1 + 2 + 3 rows for `8bdeabc2` `_livenessTriggered` (interaction with rngLockedFlag/lastPurchaseDay/jackpotPhaseFlag state) | purchaseLevel correctness over 4-state cross-product needs both turbo guard scope AND liveness-pause scope |
| 250 | SIB-01..05 | All Section 2 rows (full classification table) + all Section 3 rows (full call-site catalog) + Section 5 (storage layout — SIB-04 specific) | Sibling-pattern sweep is cross-module → needs full surface |
| 251 | TST-01..04 | All Section 2 rows (test exercises classified code) + Section 1.5 ad41973c row (test-only commit Phase 251 owns) + Section 1.4 acd88512 (LastPurchaseDayRace.test.js owner) | Tests exercise the classified contract surface |
| 252 | POST31-01 | Section 1 + 2 + 4 + 5 rows for all 4 in-scope SHAs (delta-sanity verify) | POST31-01 verifies all 4 commits don't widen bug envelopes |
| 252 | POST31-02 | Section 1.1 + 2 rows for `8bdeabc2` `_livenessTriggered` AND Section 1.4 + 2 rows for `acd88512` turbo guard | POST31-02 verifies productive-pause composes with !rngLockedFlag turbo guard |
| 253 | FIND-01..04 | All Section 1 + 2 + 3 + 4 + 5 + Section 1.6 Finding Candidates | FIND-01 publishes consolidated findings; Section 1.6 candidates flow as F-32-NN candidates |
| 253 | REG-01..02 | Section 1.7 v31-243 reconciliation rows + any Section 1 row whose function appears in audit/FINDINGS-v29/v30/v31 | REG-01 spot-checks prior findings; REG-02 documents superseded findings |

**Step B — Replace Section 6 placeholder:**

Find the literal line `## Section 6 — Consumer Index (RESERVED FOR TASK 4)` and replace its body with:

```markdown
## Section 6 — Consumer Index

Per CONTEXT.md D-247-09 item 6: every Phase 248..253 REQ-ID maps to the subset of Phase 247 row IDs that scope it. Phase 248-252 plans use this index as their sole scope input — they do NOT re-derive the universe from git diffs.

Row IDs `D-247-I###`, zero-padded three-digit, monotonic.

| Row ID | Consumer Phase | REQ-ID | Phase 247 Row Scope | Rationale |
|---|---|---|---|---|
| D-247-I001 | Phase 248 | BFL-01 | REPLACE-WITH-comma-separated-row-IDs (e.g. `D-247-C00N` (acd88512 advanceGame), `D-247-F00N` (MODIFIED_LOGIC verdict), `D-247-X001..X00N` (advanceGame callers)) | enumerate every code path reaching `_backfillGapDays` per BFL-01 wording |
| D-247-I002 | Phase 248 | BFL-02 | REPLACE | enumerate state writes inside `_backfillGapDays` |
| D-247-I003 | Phase 248 | BFL-03 | REPLACE | (continue per BFL-03..06 wording) |
| D-247-I004 | Phase 248 | BFL-04 | REPLACE | REPLACE |
| D-247-I005 | Phase 248 | BFL-05 | REPLACE | REPLACE |
| D-247-I006 | Phase 248 | BFL-06 | REPLACE | REPLACE |
| D-247-I007 | Phase 249 | PLV-01 | REPLACE | enumerate every read-site of purchaseLevel; correlates with §3 advanceGame caller list |
| D-247-I008 | Phase 249 | PLV-02 | REPLACE | 4-D state-space sweep — needs §1 + §2 acd88512 turbo + 8bdeabc2 _livenessTriggered |
| D-247-I009 | Phase 249 | PLV-03 | REPLACE | REPLACE |
| D-247-I010 | Phase 249 | PLV-04 | REPLACE | REPLACE |
| D-247-I011 | Phase 249 | PLV-05 | REPLACE | REPLACE |
| D-247-I012 | Phase 249 | PLV-06 | REPLACE | REPLACE |
| D-247-I013 | Phase 250 | SIB-01 | REPLACE | sibling-pattern sweep — needs full §2 classification + §3 call-site catalog |
| D-247-I014 | Phase 250 | SIB-02 | REPLACE | REPLACE |
| D-247-I015 | Phase 250 | SIB-03 | REPLACE | REPLACE |
| D-247-I016 | Phase 250 | SIB-04 | REPLACE | needs §5 storage layout diff |
| D-247-I017 | Phase 250 | SIB-05 | REPLACE | REPLACE |
| D-247-I018 | Phase 251 | TST-01 | REPLACE | LastPurchaseDayRace.test.js triggers panic 0x11 pre-fix → exercises acd88512 turbo guard pre-state |
| D-247-I019 | Phase 251 | TST-02 | REPLACE | LastPurchaseDayRace.test.js passes post-fix → exercises acd88512 turbo+backfill post-state |
| D-247-I020 | Phase 251 | TST-03 | REPLACE | productive-pause regression test — exercises 8bdeabc2 + ad41973c |
| D-247-I021 | Phase 251 | TST-04 | REPLACE | backfill double-execution reproduction — exercises acd88512 backfill guard |
| D-247-I022 | Phase 252 | POST31-01 | REPLACE-with-all-4-commit-row-IDs | delta-sanity all 4 in-scope commits |
| D-247-I023 | Phase 252 | POST31-02 | REPLACE — D-247-C001 (8bdeabc2 _livenessTriggered) + D-247-F001 + D-247-C00N (acd88512 turbo guard) + corresponding D-247-F### + D-247-X### | RE_VERIFY 8bdeabc2 productive-pause composes with acd88512 !rngLockedFlag turbo guard |
| D-247-I024 | Phase 253 | FIND-01 | REPLACE — entire surface (Sections 1-5) | publish FINDINGS-v32.0.md with executive summary |
| D-247-I025 | Phase 253 | FIND-02 | REPLACE | severity rubric — F-32-NN candidates from §1.6 |
| D-247-I026 | Phase 253 | FIND-03 | REPLACE | KI gating walk — needs §1.7 v31-243 reconciliation |
| D-247-I027 | Phase 253 | FIND-04 | REPLACE | finding-block formatting precedent |
| D-247-I028 | Phase 253 | REG-01 | REPLACE — §1.7 v31-243 reconciliation rows + any prior-finding domain match | spot-check regression — F-3X-NN matches |
| D-247-I029 | Phase 253 | REG-02 | REPLACE | document any superseded prior finding |

**Coverage check:** every Phase 248..253 REQ-ID per `.planning/REQUIREMENTS.md` MUST appear at least once. Total expected: 6 (BFL) + 6 (PLV) + 5 (SIB) + 4 (TST) + 2 (POST31) + 4 (FIND) + 2 (REG) = 29 REQ-ID rows. If REQUIREMENTS.md has fewer/more, adjust.
```

**Step C — Final Section 7 assembly (no edits to §7.1 / §7.2 / §7.3 from prior tasks):**

Section 7 was already assembled in pieces (§7.1 from Task 1, §7.2 from Task 2, §7.3 from Task 3). This step is a pure read-back verification — confirm all three subsections are present and that the section header `## Section 7 — Reproduction Recipe Appendix` (no RESERVED suffix) is intact. No edits required to Section 7 in Task 4 unless drift detected.

If any drift detected (e.g., a `RESERVED FOR TASK X` suffix slipped through), correct it inline with a one-line fix.

**Step D — Verify:**

```
grep -q '^## Section 6 — Consumer Index$' audit/v32-247-DELTA-SURFACE.md
! grep -q 'RESERVED FOR TASK 4' audit/v32-247-DELTA-SURFACE.md
grep -q '^| D-247-I001' audit/v32-247-DELTA-SURFACE.md
[ "$(grep -c '^| D-247-I' audit/v32-247-DELTA-SURFACE.md)" -ge 25 ]   # ~29 expected; floor 25
grep -q '^## Section 7 — Reproduction Recipe Appendix$' audit/v32-247-DELTA-SURFACE.md
! grep -q 'RESERVED FOR TASK' audit/v32-247-DELTA-SURFACE.md   # all 5 task-stubs replaced
grep -q '^### 7.1 Task 1 commands' audit/v32-247-DELTA-SURFACE.md
grep -q '^### 7.2 Task 2 commands (DELTA-02 classification)$' audit/v32-247-DELTA-SURFACE.md
grep -q '^### 7.3 Task 3 commands (DELTA-03 call-site catalog)$' audit/v32-247-DELTA-SURFACE.md
grep -q 'BFL-01' audit/v32-247-DELTA-SURFACE.md
grep -q 'PLV-01' audit/v32-247-DELTA-SURFACE.md
grep -q 'SIB-01' audit/v32-247-DELTA-SURFACE.md
grep -q 'TST-01' audit/v32-247-DELTA-SURFACE.md
grep -q 'POST31-01' audit/v32-247-DELTA-SURFACE.md
grep -q 'FIND-01' audit/v32-247-DELTA-SURFACE.md
grep -q 'REG-01' audit/v32-247-DELTA-SURFACE.md
! grep -q 'F-32-' audit/v32-247-DELTA-SURFACE.md
```

**Step E — Atomic commit per D-247-14:**

```
git add audit/v32-247-DELTA-SURFACE.md
git commit -m "$(cat <<'EOF'
audit(247-01): Task 4 — Consumer Index Section 6 + final Section 7 assembly

Plan 247-01 Task 4 deliverable. Builds Phase 247 → Phase 248..253 REQ-ID
mapping per CONTEXT.md D-247-09 item 6.

Sections updated:
  - Section 6: Consumer Index populated with 29 D-247-I### rows mapping
    every Phase 248..253 REQ-ID (BFL-01..06 / PLV-01..06 / SIB-01..05 /
    TST-01..04 / POST31-01..02 / FIND-01..04 / REG-01..02) to the subset
    of Phase 247 row IDs (D-247-C### / F### / S### / X###) that scopes it
  - Section 7: final read-back verification — §7.1 / §7.2 / §7.3 all
    present with no RESERVED FOR TASK suffixes; final assembled form
    serves as the single replayable shell recipe per D-247-19

Phase 247 catalog now COMPLETE — Sections 0-7 fully populated. Task 5
performs the FINAL READ-only flip per D-247-22.

Pure-catalog phase per D-247-05 — zero contracts/ or test/ writes.

Baseline anchor: cc68bfc7. Head anchor: acd88512.
EOF
)"
```

Do NOT push.
  </action>
  <verify>
    <automated>grep -q '^## Section 6 — Consumer Index$' audit/v32-247-DELTA-SURFACE.md && ! grep -q '^## Section 6 — Consumer Index (RESERVED FOR TASK 4)' audit/v32-247-DELTA-SURFACE.md && grep -q '^| D-247-I001' audit/v32-247-DELTA-SURFACE.md && [ "$(grep -c '^| D-247-I' audit/v32-247-DELTA-SURFACE.md)" -ge 25 ] && grep -q 'BFL-01' audit/v32-247-DELTA-SURFACE.md && grep -q 'PLV-01' audit/v32-247-DELTA-SURFACE.md && grep -q 'SIB-01' audit/v32-247-DELTA-SURFACE.md && grep -q 'TST-01' audit/v32-247-DELTA-SURFACE.md && grep -q 'POST31-01' audit/v32-247-DELTA-SURFACE.md && grep -q 'FIND-01' audit/v32-247-DELTA-SURFACE.md && grep -q 'REG-01' audit/v32-247-DELTA-SURFACE.md && grep -q '^## Section 7 — Reproduction Recipe Appendix$' audit/v32-247-DELTA-SURFACE.md && ! grep -q 'RESERVED FOR TASK' audit/v32-247-DELTA-SURFACE.md && ! grep -q 'F-32-' audit/v32-247-DELTA-SURFACE.md && ! grep -qE 'REPLACE-WITH|REPLACE WITH|^REPLACE$' audit/v32-247-DELTA-SURFACE.md && git log -1 --format='%s' | grep -qE 'audit\(247-01\): Task 4' && [ "$(git status --porcelain contracts/ test/ | grep -vE '(ContractAddresses\.sol|LastPurchaseDayRace\.test\.js)$' | wc -l)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - File contains literal header `## Section 6 — Consumer Index` (WITHOUT ` (RESERVED FOR TASK 4)` suffix)
    - Section 6 has at minimum 25 rows with Row IDs `D-247-I001..I029` (target: 29 — one per Phase 248..253 REQ-ID)
    - Every Phase 248..253 REQ-ID prefix appears at least once in Section 6: `BFL-01`, `PLV-01`, `SIB-01`, `TST-01`, `POST31-01`, `FIND-01`, `REG-01` (7 prefix anchors required)
    - Every Section 6 row has Phase 247 Row Scope cell populated (no `<placeholder>` / `REPLACE` tokens)
    - Section 7 carries no `RESERVED FOR TASK` suffix in any subsection (§7.1 / §7.2 / §7.3 all assembled)
    - File still does NOT contain `F-32-` (D-247-21 enforcement)
    - File does NOT contain placeholder tokens like `REPLACE WITH`, `REPLACE-WITH`
    - Git log shows 1 new commit with subject matching `audit\(247-01\): Task 4`
    - `git diff --name-only HEAD~1..HEAD` returns ONLY `audit/v32-247-DELTA-SURFACE.md`
    - `git status --porcelain contracts/ test/` excludes only the 2 pre-existing lines; remainder empty
    - Not pushed
  </acceptance_criteria>
  <deliverable>
Section 6 Consumer Index populated with Phase 247 → Phase 248..253 REQ-ID mapping covering all 29 REQ-IDs (BFL-01..06 / PLV-01..06 / SIB-01..05 / TST-01..04 / POST31-01..02 / FIND-01..04 / REG-01..02). Section 7 final-form verified — all three subsections present with reproduction commands fully consolidated. Phase 247 catalog complete in content. One atomic commit per D-247-14.
  </deliverable>
  <done>Section 6 populated with ≥ 25 D-247-I### rows mapping every Phase 248..253 REQ-ID; Section 7 final assembly verified; zero F-32-NN IDs; zero contracts/ or test/ writes; one atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 5: READ-only flip + plan-close per D-247-22</name>
  <files>audit/v32-247-DELTA-SURFACE.md</files>
  <read_first>
    - audit/v32-247-DELTA-SURFACE.md (must exist with all 7 sections populated from Tasks 1-4 — reads it to perform the FINAL READ-only flip)
    - .planning/phases/247-delta-extraction-classification/247-CONTEXT.md — specifically D-247-22 (Phase 247 output READ-only after plan-close — scope-guard deferral rule), D-247-14 (atomic per-task commits)
    - audit/v31-243-DELTA-SURFACE.md (READ the top-of-file frontmatter status line — direct precedent for the FINAL READ-only marker format)
  </read_first>
  <action>
Mark `audit/v32-247-DELTA-SURFACE.md` FINAL READ-only per CONTEXT.md D-247-22, then commit + plan-close.

**Step A — Update top-of-file frontmatter status:**

Find the existing frontmatter block at the top of `audit/v32-247-DELTA-SURFACE.md`:
```yaml
---
status: IN_PROGRESS — Plan 247-01 single-plan multi-task
phase: 247 — Delta Extraction & Classification
milestone: v32.0 Backfill Idempotency + purchaseLevel Underflow Audit
baseline: cc68bfc7
head: acd88512
plan: 247-01
---
```

Replace the `status:` line with:
```yaml
status: FINAL — READ-ONLY (Phase 247 closed at HEAD acd88512 per CONTEXT.md D-247-22 — scope-guard deferral rule applies; gaps recorded in Phase 248-252 plan SUMMARYs, not re-edited here)
```

**Step B — Update file-level Status header line:**

Find the existing prose line below the H1 title:
```
**Status:** IN PROGRESS — Plan 247-01 Task 1 populates Sections 0 / 1 / 4 / 5 / 7.1. Sections 2 / 3 / 6 are placeholder stubs reserved for Tasks 2 / 3 / 4 of the same plan. Task 5 flips this file to FINAL READ-only.
```

Replace with:
```
**Status:** FINAL — READ-ONLY per CONTEXT.md D-247-22. Plan 247-01 Tasks 1-5 complete. Phase 247 COMPLETE: DELTA-01 + DELTA-02 + DELTA-03 all closed at HEAD `acd88512` against baseline `cc68bfc7`. Sections 0-7 finalized. Any Phase 248-252 finding of a changed function / state-var / event / interface method / call site NOT in this catalog is recorded as a scope-guard deferral in the discovering plan's own SUMMARY.md — this file is NOT re-edited per D-247-22.
```

**Step C — Final sanity gate (must all pass before commit):**

```
test -f audit/v32-247-DELTA-SURFACE.md
grep -q '^status: FINAL — READ-ONLY' audit/v32-247-DELTA-SURFACE.md
grep -q '\*\*Status:\*\* FINAL — READ-ONLY' audit/v32-247-DELTA-SURFACE.md
! grep -q 'RESERVED FOR TASK' audit/v32-247-DELTA-SURFACE.md
! grep -q 'F-32-' audit/v32-247-DELTA-SURFACE.md
! grep -qE 'REPLACE-WITH|REPLACE WITH' audit/v32-247-DELTA-SURFACE.md
grep -q '^## Section 0 — Overview & Row-ID Legend' audit/v32-247-DELTA-SURFACE.md
grep -q '^## Section 1 — Per-Source Changelog' audit/v32-247-DELTA-SURFACE.md
grep -q '^## Section 2 — Aggregate Function Classification$' audit/v32-247-DELTA-SURFACE.md
grep -q '^## Section 3 — Downstream Call-Site Catalog$' audit/v32-247-DELTA-SURFACE.md
grep -q '^## Section 4 — State Variable / Event / Interface / Error / Constant Inventory' audit/v32-247-DELTA-SURFACE.md
grep -q '^## Section 5 — Storage Slot Layout Diff (DegenerusGameStorage.sol)' audit/v32-247-DELTA-SURFACE.md
grep -q '^## Section 6 — Consumer Index$' audit/v32-247-DELTA-SURFACE.md
grep -q '^## Section 7 — Reproduction Recipe Appendix$' audit/v32-247-DELTA-SURFACE.md
[ "$(grep -c '^| D-247-C' audit/v32-247-DELTA-SURFACE.md)" -ge 8 ]
[ "$(grep -c '^| D-247-F' audit/v32-247-DELTA-SURFACE.md)" -ge 5 ]
[ "$(grep -c '^| D-247-X' audit/v32-247-DELTA-SURFACE.md)" -ge 5 ]
[ "$(grep -c '^| D-247-I' audit/v32-247-DELTA-SURFACE.md)" -ge 25 ]
test -z "$(git status --porcelain contracts/ test/ | grep -vE '(ContractAddresses\.sol|LastPurchaseDayRace\.test\.js)$')"
git diff --name-only cc68bfc7..HEAD -- contracts/ | sort > /tmp/v32-247-final-contract-files.txt
diff /tmp/v32-247-final-contract-files.txt <(printf 'contracts/DegenerusVault.sol\ncontracts/modules/DegenerusGameAdvanceModule.sol\ncontracts/modules/DegenerusGameMintModule.sol\ncontracts/storage/DegenerusGameStorage.sol\n')   # MUST be empty (4 in-scope files, no extras)
```

If ANY check fails, STOP and diagnose before committing.

**Step D — Final atomic commit + plan-close (per D-247-14):**

```
git add audit/v32-247-DELTA-SURFACE.md
git commit -m "$(cat <<'EOF'
audit(247-01): Task 5 — FINAL READ-only flip + plan-close at HEAD acd88512

Plan 247-01 final task — flips audit/v32-247-DELTA-SURFACE.md to FINAL
READ-only per CONTEXT.md D-247-22. Phase 247 catalog complete.

Sections 0-7 all populated:
  - Section 0: Overview + Row-ID Legend (5-prefix scheme C/F/S/X/I)
  - Section 1: Per-Source Changelog (4 in-scope SHAs + 1 out-of-scope
    test-only SHA + Finding Candidates + v31-243 light reconciliation)
  - Section 2: Aggregate Function Classification (D-247-06 5-bucket
    rubric; D-247-07 pre-locked floors honored)
  - Section 3: Downstream Call-Site Catalog (grep-reproducible per
    D-247-19; 48554f8f DELETED-helper zero-caller sanity confirmed)
  - Section 4: State Variable / Event / Interface / Error / Constant
    Inventory
  - Section 5: Storage Slot Layout Diff (forge inspect at both SHAs;
    expected zero slot delta given 8bdeabc2 +12 is NatSpec + non-storage
    early-return)
  - Section 6: Consumer Index (29 D-247-I### rows mapping every Phase
    248..253 REQ-ID: BFL-01..06 / PLV-01..06 / SIB-01..05 / TST-01..04 /
    POST31-01..02 / FIND-01..04 / REG-01..02)
  - Section 7: Reproduction Recipe Appendix (§7.1 + §7.2 + §7.3
    consolidated; portable POSIX shell recipes for full Phase 247 replay)

Status flipped: IN_PROGRESS → FINAL — READ-ONLY per D-247-22.

Phase 247 deliverable signal: PHASE_247_CATALOG_FINAL_AT_HEAD_acd88512

Plan 247-01 closure summary:
  - 5 atomic per-task commits per D-247-14
  - 0 F-32-NN finding IDs (Phase 253 owns per D-247-21)
  - 0 contracts/ or test/ writes per D-247-05
  - 1 deliverable file: audit/v32-247-DELTA-SURFACE.md
  - 4 in-scope SHAs catalogued (8bdeabc2 / 6a63705b / 48554f8f / acd88512)
  - 1 out-of-scope SHA enumerated (ad41973c — Phase 251 scope per D-247-02)

Working-tree state at plan-close: M contracts/ContractAddresses.sol
(deploy regen ignored per D-247-03) + ?? test/edge/LastPurchaseDayRace.test.js
(Phase 251 scope per D-247-17) — both UNTOUCHED throughout Plan 247-01.

Baseline anchor: cc68bfc7. Head anchor: acd88512.

Phase 247 → Phase 248 handoff ready: BFL-01..06 plan can read Section 6
D-247-I001..I006 directly without re-deriving from git.
EOF
)"
```

Do NOT push.

**Step E — Plan-close summary creation:**

After the final commit, create `.planning/phases/247-delta-extraction-classification/247-01-SUMMARY.md` with:
- Plan metadata (phase, plan, baseline, head, commit count)
- 5 atomic commit SHAs (extract via `git log -5 --format='%h %s'`)
- Section deliverable count card (rows per section)
- Pre-locked classification floor compliance attestation
- Scope-guard deferrals (none expected)
- Phase 248 handoff signal (`PHASE_247_CATALOG_FINAL_AT_HEAD_acd88512`)

Use the standard summary template at `$HOME/.claude/get-shit-done/templates/summary.md` (referenced in `<execution_context>` block at top of plan).

The SUMMARY.md is committed in a separate commit (NOT counted toward the 5-task atomic-commit total per D-247-14):

```
git add .planning/phases/247-delta-extraction-classification/247-01-SUMMARY.md
git commit -m "docs(247-01): plan-close SUMMARY — Phase 247 catalog FINAL READ-only at HEAD acd88512"
```

Do NOT push.
  </action>
  <verify>
    <automated>test -f audit/v32-247-DELTA-SURFACE.md && grep -q '^status: FINAL — READ-ONLY' audit/v32-247-DELTA-SURFACE.md && grep -q '\*\*Status:\*\* FINAL — READ-ONLY' audit/v32-247-DELTA-SURFACE.md && ! grep -q 'RESERVED FOR TASK' audit/v32-247-DELTA-SURFACE.md && ! grep -q 'F-32-' audit/v32-247-DELTA-SURFACE.md && ! grep -qE 'REPLACE-WITH|REPLACE WITH' audit/v32-247-DELTA-SURFACE.md && grep -q '^## Section 0 — Overview & Row-ID Legend' audit/v32-247-DELTA-SURFACE.md && grep -q '^## Section 1 — Per-Source Changelog' audit/v32-247-DELTA-SURFACE.md && grep -q '^## Section 2 — Aggregate Function Classification$' audit/v32-247-DELTA-SURFACE.md && grep -q '^## Section 3 — Downstream Call-Site Catalog$' audit/v32-247-DELTA-SURFACE.md && grep -q '^## Section 4 — State Variable / Event / Interface / Error / Constant Inventory' audit/v32-247-DELTA-SURFACE.md && grep -q '^## Section 5 — Storage Slot Layout Diff (DegenerusGameStorage.sol)' audit/v32-247-DELTA-SURFACE.md && grep -q '^## Section 6 — Consumer Index$' audit/v32-247-DELTA-SURFACE.md && grep -q '^## Section 7 — Reproduction Recipe Appendix$' audit/v32-247-DELTA-SURFACE.md && [ "$(grep -c '^| D-247-C' audit/v32-247-DELTA-SURFACE.md)" -ge 8 ] && [ "$(grep -c '^| D-247-F' audit/v32-247-DELTA-SURFACE.md)" -ge 5 ] && [ "$(grep -c '^| D-247-X' audit/v32-247-DELTA-SURFACE.md)" -ge 5 ] && [ "$(grep -c '^| D-247-I' audit/v32-247-DELTA-SURFACE.md)" -ge 25 ] && grep -q 'PHASE_247_CATALOG_FINAL_AT_HEAD_acd88512' audit/v32-247-DELTA-SURFACE.md && [ "$(git log --format='%s' cc68bfc7..HEAD | grep -cE '^audit\(247-01\): Task [1-5]')" = "5" ] && test -f .planning/phases/247-delta-extraction-classification/247-01-SUMMARY.md && [ "$(git status --porcelain contracts/ test/ | grep -vE '(ContractAddresses\.sol|LastPurchaseDayRace\.test\.js)$' | wc -l)" = "0" ] && [ "$(git diff --name-only cc68bfc7..HEAD -- contracts/ | sort | tr '\n' '|')" = "contracts/DegenerusVault.sol|contracts/modules/DegenerusGameAdvanceModule.sol|contracts/modules/DegenerusGameMintModule.sol|contracts/storage/DegenerusGameStorage.sol|" ]</automated>
  </verify>
  <acceptance_criteria>
    - Top-of-file frontmatter `status:` line begins with `FINAL — READ-ONLY` (literal match)
    - Body Status line begins with `**Status:** FINAL — READ-ONLY per CONTEXT.md D-247-22`
    - File contains literal closure signal `PHASE_247_CATALOG_FINAL_AT_HEAD_acd88512`
    - File does NOT contain ANY `RESERVED FOR TASK` markers
    - File does NOT contain ANY `F-32-` references (D-247-21 enforcement)
    - File does NOT contain ANY `REPLACE WITH` / `REPLACE-WITH` placeholder tokens
    - Sections 0-7 all present with literal headers in fixed order (no RESERVED suffix on any of 2/3/6/7.1/7.2/7.3)
    - Row count floors satisfied: ≥8 D-247-C / ≥5 D-247-F / ≥1 D-247-S OR storage-byte-identical sentence / ≥5 D-247-X / ≥25 D-247-I
    - 5 atomic per-task commits exist between cc68bfc7 and HEAD with subject `audit(247-01): Task N — <summary>` for N=1..5 (D-247-14 enforcement)
    - 1 SUMMARY commit exists with subject `docs(247-01): plan-close SUMMARY...`
    - `.planning/phases/247-delta-extraction-classification/247-01-SUMMARY.md` exists
    - `git diff --name-only cc68bfc7..HEAD -- contracts/` returns the SAME 4 files as planning-time `--name-status` (DegenerusVault / AdvanceModule / MintModule / GameStorage) — no contracts/ drift
    - `git status --porcelain contracts/ test/` returns ONLY the 2 pre-existing lines (D-247-03 + D-247-17) — no new working-tree changes
    - Not pushed
  </acceptance_criteria>
  <deliverable>
audit/v32-247-DELTA-SURFACE.md FINAL — READ-ONLY at HEAD acd88512 per D-247-22. All 7 sections populated. PHASE_247_CATALOG_FINAL_AT_HEAD_acd88512 closure signal emitted. 5 atomic per-task commits + 1 SUMMARY commit landed per D-247-14. Phase 247 → Phase 248..253 handoff ready: Section 6 Consumer Index serves as the sole scope input for downstream plans.
  </deliverable>
  <done>File frontmatter + body Status both flipped to FINAL — READ-ONLY; closure signal present; 5 atomic per-task commits + 1 SUMMARY commit; 247-01-SUMMARY.md created; zero F-32-NN IDs; zero RESERVED suffixes; zero placeholder tokens; zero contracts/ or test/ writes; 4-file in-scope set unchanged from planning-time; not pushed.</done>
</task>

</tasks>

<verification>
- `audit/v32-247-DELTA-SURFACE.md` exists, FINAL — READ-ONLY at HEAD `acd88512`, and emits `PHASE_247_CATALOG_FINAL_AT_HEAD_acd88512` closure signal
- All 7 sections populated with real data derived from fresh `git diff` / `git show` / `forge inspect` / `grep -rn` output:
  - §0 Overview + Row-ID Legend (5-prefix C/F/S/X/I scheme per D-247-11) + Source Inventory table (4 in-scope + 1 out-of-scope SHA)
  - §1 Per-Source Changelog (1.1 8bdeabc2 / 1.2 6a63705b / 1.3 48554f8f / 1.4 acd88512 / 1.5 ad41973c NO_CHANGE test-only / 1.6 Finding Candidates fresh-eyes / 1.7 v31-243 Light Reconciliation)
  - §2 Aggregate Function Classification with D-247-07 pre-locked floors honored + 2.1 distribution count card
  - §3 Downstream Call-Site Catalog grep-reproducible per D-247-19 + 3.1 per-fn count card + 3.2 self-call/delegatecall enumeration
  - §4 State / Event / Interface / Error / Constant Inventory (4.1-4.5)
  - §5 Storage Slot Layout Diff via forge inspect at both SHAs (5.1 baseline / 5.2 head / 5.3 slot-by-slot diff / 5.4 backwards-compat verdict; expected zero slot-layout-changing rows)
  - §6 Consumer Index ≥ 25 D-247-I### rows mapping every Phase 248..253 REQ-ID (BFL/PLV/SIB/TST/POST31/FIND/REG)
  - §7 Reproduction Recipe Appendix: 7.1 (Task 1) + 7.2 (Task 2) + 7.3 (Task 3) all populated, no RESERVED suffixes
- Row ID integrity: every `D-247-C###` / `D-247-F###` / `D-247-S###` / `D-247-X###` / `D-247-I###` is zero-padded 3-digit, monotonic within prefix, no duplicates
- Zero `F-32-NN` references anywhere (D-247-21)
- Zero `<placeholder>` / `REPLACE-WITH` / `REPLACE WITH` tokens remain
- Zero `RESERVED FOR TASK X` markers remain
- Zero `contracts/` or `test/` writes (D-247-05) — `git status --porcelain contracts/ test/` returns only the 2 pre-existing lines (`M contracts/ContractAddresses.sol` per D-247-03 + `?? test/edge/LastPurchaseDayRace.test.js` per D-247-17)
- Baseline anchor `cc68bfc7` + HEAD anchor `acd88512` appear in frontmatter, body Status line, Section 0.1 Overview, and Section 7.1 sanity-gate command
- 5 atomic per-task commits land per D-247-14 with subject pattern `audit(247-01): Task N — <summary>` for N ∈ {1..5}
- 1 SUMMARY commit lands with subject `docs(247-01): plan-close SUMMARY...`
- `.planning/phases/247-delta-extraction-classification/247-01-SUMMARY.md` exists with plan-close metadata
- D-247-07 pre-locked classification floors all honored (5 commits' core function verdicts ∈ {MODIFIED_LOGIC | DELETED} as floored)
- D-247-18 light reconciliation against `audit/v31-243-DELTA-SURFACE.md` produces ≥ 1 confirmed-overlap row (`_livenessTriggered` D-243-C026 ↔ D-247-C001 minimum)
- Committed locally; not pushed
- `git diff --name-only cc68bfc7..HEAD -- contracts/` returns exactly the 4 in-scope files (no contracts/ drift introduced by the plan)
</verification>

<success_criteria>
DELTA-01 + DELTA-02 + DELTA-03 all satisfied at HEAD `acd88512` against baseline `cc68bfc7`. The Phase 247 catalog `audit/v32-247-DELTA-SURFACE.md` is FINAL — READ-ONLY and serves as the SOLE scope input for Phase 248 (BFL Backfill Idempotency Proof), Phase 249 (PLV purchaseLevel Correctness Proof), Phase 250 (SIB Sibling-Pattern Sweep), Phase 251 (TST Reproduction Tests — owns test/ but consumes Phase 247 contract classification for "what code is the test exercising"), Phase 252 (POST31 Landed-Commit Sanity), and Phase 253 (FIND Findings Consolidation + REG Lean Regression). Every row carries a hunk-level file:line anchor + ≤ 20-word semantic note per D-247-08 / D-247-20. Every call-site row carries the exact `grep -rn` command per D-247-19. Pre-locked classification floors per D-247-07 honored throughout. Storage slot layout diff committed as Phase 250 SIB-04 + Phase 252 POST31-01 sole scope input per D-247-16 — expected zero slot-layout-changing rows given 8bdeabc2 +12 is NatSpec + non-storage early-return only. v31-243 light reconciliation per D-247-18 surfaces the `_livenessTriggered` direct overlap (D-243-C026 ↔ D-247-C001) as Phase 252 POST31-02 cross-cite. Plan emits `PHASE_247_CATALOG_FINAL_AT_HEAD_acd88512` closure signal. Pure-catalog discipline preserved (D-247-05 — zero contracts/ or test/ writes throughout 5 atomic per-task commits + 1 SUMMARY commit). Phase 248..253 plans inherit a complete, grep-reproducible, hunk-cited target list — no further enumeration required.
</success_criteria>

<output>
After completion, create `.planning/phases/247-delta-extraction-classification/247-01-SUMMARY.md` (Task 5 Step E handles this).
</output>
</content>
</invoke>