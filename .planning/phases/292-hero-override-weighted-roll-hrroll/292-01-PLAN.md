---
phase: 292-hero-override-weighted-roll-hrroll
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md
  - .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md
autonomous: true
requirements: [HRROLL-05, HRROLL-09, HRROLL-10]
tags: [audit, hero-override, weighted-roll, design-intent-trace, gas-attestation, rng-backward-trace, v42.0]
must_haves:
  truths:
    # HRROLL-10 — pre-patch design-intent trace anchor recording
    - "Design-intent trace doc `292-01-DESIGN-INTENT-TRACE.md` lands in the phase artifact directory and explicitly records D-42N-LEADER-BONUS-01 (×1.5 leader bonus locked) + D-42N-FLOOR-01 (no min-wager floor) + D-42N-COLOR-ENTROPY-01 (color/symbol bit-source non-collision attested by construction) + D-42N-DETERMINISM-01 (exact roll algorithm) + D-42N-GAS-01 (gas acceptance threshold) + D-42N-BONUS-ENTROPY-01 (raw `randWord` feeds `_rollHeroSymbol`; cross-bonus invariance preserved) + D-42N-CACHE-01 (pass-2 cache shape) per `feedback_design_intent_before_deletion.md` (per HRROLL-10)"
    - "Design-intent trace covers section (i): original deterministic `_topHeroSymbol(uint32 day)` single-leader / winner-takes-all design intent — why the v41 form selected the max-amount `(quadrant, symbol)` slot via deterministic scan rather than a weighted random roll (per HRROLL-10(i))"
    - "Design-intent trace covers section (ii): leader-bonus magnitude trade-offs — ×2 monopolization risk vs ×1.5 balanced bias (locked) vs no-bonus pure-proportional roll; rationale for ×1.5 against the alternatives per D-42N-LEADER-BONUS-01 (per HRROLL-10(ii))"
    - "Design-intent trace covers section (iii): sybil exposure trade-offs — no-floor simplicity (locked per D-42N-FLOOR-01; smallest bettor wins with proportional probability) vs floor anti-spam; rationale for the no-floor disposition (per HRROLL-10(iii))"
    - "Design-intent trace covers section (iv): RNG commitment-window backward-trace per `feedback_rng_commitment_window.md` + `feedback_rng_backward_trace.md` — traces from `_rollHeroSymbol(dailyIdx, heroEntropy)` consumer at `_applyHeroOverride` (`contracts/modules/DegenerusGameJackpotModule.sol:1594-1621`) BACK to the wager-time write at `placeDegeneretteBet` (`contracts/modules/DegenerusGameDegeneretteModule.sol:484-501`); verifies (a) `dailyHeroWagers[dailyIdx][q]` writes happen at bet placement, (b) `dailyIdx` is set ONLY at `_unlockRng` and inherits the v41 Phase 288 D-288-FIX-SHAPE-01 single-writer-day-key invariant, (c) wager amounts for day D are LOCKED before day D+1's VRF request fires, (d) `randWord` is unknown at wager time, (e) player-controllable input committed before randomness available → RNG commitment-window invariant SAFE (per HRROLL-05 + HRROLL-10(iv))"
    - "Design-intent trace covers section (v): gas budget headroom + D-42N-CACHE-01 three-shape comparison decision (flat `uint32[32]` indexed `q*8 + s` vs `uint64[32]` weights array vs packed `uint256[4]` cache with SHR+AND extracts); records the chosen shape with rationale; records the locked exact algorithm per D-42N-DETERMINISM-01 (`abi.encode(heroEntropy, day)` — NOT `abi.encodePacked`; `pick = uint64(uint256(keccak256(...)) % effectiveTotal)`; flat idx ascending = q ascending → s ascending; leader-bonus added to cumulative at `idx == leaderIdx`; first-seen wins on ties via strict `>` in pass 1) (per HRROLL-10(v) + D-42N-DETERMINISM-01)"
    - "Design-intent trace explicitly enumerates the OUT-OF-SCOPE items NOT touched by Phase 292 per REQUIREMENTS.md `## Out of Scope` register: (a) HRROLL min-wager floor for eligibility — out per D-42N-FLOOR-01; (b) HRROLL leader-bonus magnitude alternatives (×2, ×3, flat floor) — out per D-42N-LEADER-BONUS-01; (c) HRROLL storage layout changes — `dailyHeroWagers[uint32 => uint256[4]]` + `dailyIdx` UNCHANGED; (d) HRROLL public ABI changes — `payDailyJackpot` + `payDailyJackpotCoinAndTickets` + view function signatures byte-identical; (e) per-bet hero-quadrant multiplier `_applyHeroMultiplier` UNCHANGED per v37 Phase 267 design; (f) divergent-entropy alternative for bonus rolls — REJECTED at D-42N-BONUS-ENTROPY-01; (g) cache-shape A/B benchmark in production — only theoretical-at-292 + empirical-at-293; (h) adversarial pass on HRROLL in isolation — deferred to Phase 296 SWEEP per D-271-ADVERSARIAL-01 carry"
    # Measurement scaffold — load-bearing for Plan 02's batched commit message body
    - "Measurement scaffold doc `292-01-MEASUREMENT.md` lands in the phase artifact directory with 6 section headers (§1 audit baseline / §2 storage byte-identity / §3 theoretical worst-case gas / §4 selector attestations / §5 events — degenerate single-callsite case for HRROLL / §6 callsite diff against `_applyHeroOverride` signature change) anchored to `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4`; numerical fields are placeholders Plan 02 fills in post-patch (per HRROLL-06 + HRROLL-07 + HRROLL-08)"
    - "Measurement scaffold §3 carries the locked D-42N-CACHE-01 three-shape gas-comparison table (flat `uint32[32]` vs `uint64[32]` weights vs packed `uint256[4]` with SHR+AND) per `feedback_gas_worst_case.md` theoretical-first methodology; selects the lowest-worst-case shape; locks it as the implementation reference for Plan 02; records the D-42N-GAS-01 acceptance threshold value derived from the chosen shape's theoretical worst case; if the chosen shape's worst case exceeds ~+10K vs v41 `_topHeroSymbol` baseline, scaffold carries an explicit ESCALATION-CHECKPOINT marker the executor must surface to the user BEFORE Plan 02 begins"
    - "Measurement scaffold §3 includes the D-42N-COLOR-ENTROPY-01 non-collision attestation as a one-liner: color path consumes bits `quadrant*3` of `r` (post-bonus-tag for `isBonus=true`; raw `randWord` for `isBonus=false`); symbol-roll path consumes `uint64(uint256(keccak256(abi.encode(randWord, day))) % effectiveTotal)` — sourced from orthogonal entropy domains by construction (keccak output is independent of the raw `randWord` bits used by the color path); non-collision is structural, not probabilistic"
    - "Measurement scaffold §6 documents that HRROLL `_applyHeroOverride` has exactly ONE caller in the codebase (`_rollWinningTraits` at L1941); the v40 B2-symmetric callsite pattern degenerates to a single-site verification for this phase (recorded as a documented structural distinction vs the Phase 290 B2-symmetric pattern) — the executor verifies the single callsite update post-patch"
    # Scope discipline — pre-patch gate satisfaction
    - "Trace + scaffold are AGENT-COMMITTED artifacts (no user approval needed — zero contract edits per `feedback_no_contract_commits.md`); Plan 02 cannot start its contract-edit task until both artifacts exist at the paths in `files_modified`"
    # Carry-forward locks (recorded explicitly in trace; do NOT re-derive)
    - "Carry-forward locks recorded in trace: v41 Phase 288 D-288-FIX-SHAPE-01 `dailyIdx` single-writer-day-key invariant (HRROLL-05 inherits); v41 Phase 281 owed-salt seed-separation pattern (referenced in HRROLL audit-story for D-42N-COLOR-ENTROPY-01 cross-RNG-consumer non-collision attestation); v40 D-40N-MINTBOOST-OUT-01 UNCHANGED (HRROLL does not touch mint-boost path); v34 D-271-ADVERSARIAL-01 + D-271-ADVERSARIAL-02 + D-271-ADVERSARIAL-03 (Phase 296 SWEEP combined adversarial pass over MINTCLN + HRROLL + DPNERF; `/degen-skeptic` OUT OF SCOPE)"
    # SWEEP-02(ii) baseline — pre-emptive answers for Phase 296
    - "Trace pre-emptively answers the 4 SWEEP-02(ii) HRROLL adversarial hypotheses so Phase 296 has a baseline disposition record to test against: (1) does ×1.5 leader bonus open whale-coordination or wash-trading MEV? (2) does no-floor design open sybil dilution attack (e.g., attacker spam-bets 1 wei on all 32 slots to dilute organic winners)? (3) does the new RNG-consumer (symbol roll consuming VRF bits via keccak) collide with existing consumers (jackpot path-select bits[0..12] / lootbox-Bernoulli bits[152..167] / jackpot-Bernoulli bits[200..215] / color-sample bits `quadrant*3`)? (4) does the gas regression open a DOS surface?"
  artifacts:
    - path: ".planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md"
      provides: "HRROLL-10 design-intent trace covering (i) original deterministic `_topHeroSymbol` rationale, (ii) leader-bonus magnitude trade-offs, (iii) sybil-exposure / no-floor trade-offs, (iv) HRROLL-05 RNG commitment-window backward-trace from `_rollHeroSymbol` consumer to `placeDegeneretteBet` wager-write at `contracts/modules/DegenerusGameDegeneretteModule.sol:484-501`, (v) gas headroom + D-42N-CACHE-01 cache-shape decision + D-42N-DETERMINISM-01 exact algorithm lock; decision anchors D-42N-LEADER-BONUS-01 + D-42N-FLOOR-01 + D-42N-COLOR-ENTROPY-01 + D-42N-DETERMINISM-01 + D-42N-GAS-01 + D-42N-BONUS-ENTROPY-01 + D-42N-CACHE-01 recorded; out-of-scope register enumerated; SWEEP-02(ii) adversarial hypotheses pre-emptively answered; carry-forward anchors recorded"
      min_lines: 110
    - path: ".planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md"
      provides: "Scaffold for Plan 02's 6 attestation sections (audit baseline / storage byte-identity via `forge inspect storageLayout`; theoretical worst-case gas with D-42N-CACHE-01 three-shape comparison table per `feedback_gas_worst_case.md` and D-42N-GAS-01 acceptance threshold + ESCALATION-CHECKPOINT branch + D-42N-COLOR-ENTROPY-01 non-collision attestation; selector attestations for `payDailyJackpot` + `payDailyJackpotCoinAndTickets` + view functions UNCHANGED; events — NONE touched (HRROLL has no event surface); callsite diff — single-site verification at `_rollWinningTraits` L1941 plus `_applyHeroOverride` signature delta documentation) anchored to `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4`"
      min_lines: 60
  key_links:
    - from: ".planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md"
      to: ".planning/phases/292-hero-override-weighted-roll-hrroll/292-02-PLAN.md (contract patch task)"
      via: "design-intent-before-deletion gate per `feedback_design_intent_before_deletion.md` (`_topHeroSymbol` deletion + `_applyHeroOverride` signature change + new `_rollHeroSymbol` are all preceded by rationale-record)"
      pattern: "D-42N-LEADER-BONUS-01"
    - from: ".planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md"
      to: "Plan 02 batched commit message body"
      via: "copy-forward source for storage byte-identity attestation + worst-case gas + selector attestations + callsite diff + D-42N-COLOR-ENTROPY-01 non-collision attestation; per `feedback_no_history_in_comments.md` (numerical attestations live in commit body, NOT in NatSpec)"
      pattern: "MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4"
    - from: ".planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md"
      to: "audit/FINDINGS-v42.0.md §9 `Deferred to Future Milestones` register at Phase 297"
      via: "D-42N-CACHE-01 + D-42N-GAS-01 + D-42N-BONUS-ENTROPY-01 anchor handoffs (any rejected alternatives explicitly cited as REJECTED rather than DEFERRED so the forward-cite zero-emission discipline per D-42N-FCITE-01 is preserved)"
      pattern: "D-42N-BONUS-ENTROPY-01"
    - from: ".planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md"
      to: ".planning/phases/296-*/296-01-ADVERSARIAL-LOG.md (Phase 296 SWEEP)"
      via: "SWEEP-02(ii) HRROLL adversarial-hypothesis pre-emptive answers (4 hypotheses: leader-bonus MEV, no-floor sybil, RNG-consumer bit-slice collision, gas DOS)"
      pattern: "SWEEP-02"
---

<objective>
Land the HRROLL-10 design-intent-before-deletion gate as two AGENT-COMMITTED planning artifacts in `.planning/phases/292-hero-override-weighted-roll-hrroll/`: (1) `292-01-DESIGN-INTENT-TRACE.md` recording the 7 decision anchors (D-42N-LEADER-BONUS-01 + D-42N-FLOOR-01 + D-42N-COLOR-ENTROPY-01 + D-42N-DETERMINISM-01 + D-42N-GAS-01 + D-42N-BONUS-ENTROPY-01 + D-42N-CACHE-01) and the 5-section trace HRROLL-10 requires (including the HRROLL-05 backward-trace per `feedback_rng_commitment_window.md` + `feedback_rng_backward_trace.md`); (2) `292-01-MEASUREMENT.md` scaffold with 6 attestation section headers Plan 02 fills in post-patch, plus the locked D-42N-CACHE-01 three-shape gas-comparison table that picks the implementation shape Plan 02 ships and the D-42N-GAS-01 acceptance threshold derived from that shape's theoretical worst case.

Purpose: Satisfy the `feedback_design_intent_before_deletion.md` discipline — the user requires the original-design rationale for what is about to be deleted (the deterministic `_topHeroSymbol(uint32 day)` selector at `contracts/modules/DegenerusGameJackpotModule.sol:1625-1653`) and restructured (the `_applyHeroOverride` signature at L1594-1621 gaining a second entropy parameter per D-42N-BONUS-ENTROPY-01) to be traced BEFORE the contract patch lands. Plan 02 cannot begin its contract-edit task until both artifacts exist. The trace doc also pre-emptively answers the 4 SWEEP-02(ii) HRROLL adversarial hypotheses so Phase 296 has a baseline disposition record to test against. The measurement scaffold seeds the verbatim copy-forward source for Plan 02's batched commit message body — per `feedback_gas_worst_case.md` theoretical-first + `feedback_no_history_in_comments.md` no-history-in-NatSpec, the gas / bytecode / storage-diff / selector attestations land in the commit body, NOT in NatSpec. The D-42N-CACHE-01 three-shape comparison is the load-bearing planner-discretion artifact that picks the pass-2 cache shape Plan 02 implements verbatim.

Output:
- `292-01-DESIGN-INTENT-TRACE.md` — 5-section trace per HRROLL-10 + 7 decision anchors + out-of-scope register + carry-forward anchors + SWEEP-02(ii) adversarial-hypothesis pre-emptive answers + HRROLL-05 backward-trace; AGENT-COMMITTED.
- `292-01-MEASUREMENT.md` — 6-section scaffold with `<FILL-IN-Plan-02>` placeholders + the locked D-42N-CACHE-01 three-shape comparison + D-42N-GAS-01 acceptance threshold + D-42N-COLOR-ENTROPY-01 non-collision one-liner + ESCALATION-CHECKPOINT branch if worst case > +10K; AGENT-COMMITTED; numerical fields filled by Plan 02 post-patch.

This plan makes ZERO contract edits and triggers ZERO user approval gates (per `feedback_no_contract_commits.md` — only contract / test edits need explicit approval).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/292-hero-override-weighted-roll-hrroll/292-CONTEXT.md

@contracts/modules/DegenerusGameJackpotModule.sol
@contracts/modules/DegenerusGameDegeneretteModule.sol
@contracts/storage/DegenerusGameStorage.sol

<interfaces>
<!-- Load-bearing identifiers / signatures / decision-anchor-locked spec snippets the planner-authoring agent needs. Extracted from live v42.0 HEAD = audit baseline v41.0 closure HEAD `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4`. -->
<!-- Plan 01 author uses these directly to seed the trace + scaffold; no codebase exploration needed beyond the @-references above. -->

Current `_applyHeroOverride` signature + NatSpec (`contracts/modules/DegenerusGameJackpotModule.sol:1584-1621` — REWRITTEN by Plan 02 to add a second entropy parameter per D-42N-BONUS-ENTROPY-01; NatSpec describes what IS post-patch with NO "previously" / "v41 form" / "(CALL 1 / CALL 2)" wording):
```
/// @dev If a top hero symbol exists for the prior day's settled wager pool,
///      that symbol auto-wins its own quadrant (with random color), replacing
///      only that quadrant. Applied to all jackpot paths (purchase phase +
///      jackpot phase). Reads `dailyHeroWagers[dailyIdx]`: `dailyIdx` is
///      written only at `_unlockRng` (AdvanceModule), so during jackpot
///      processing it is frozen at the previous day's index. Both CALL 1 and
///      CALL 2 of the daily ETH split therefore read the same slot regardless
///      of any physical-day boundary crossed between calls. Bets placed on
///      day D write to `dailyHeroWagers[D]`; day D+1's jackpot reads slot[D]
///      via `dailyIdx == D` (set by day D's `_unlockRng`).
function _applyHeroOverride(
    uint8[4] memory w,
    uint256 randomWord
) private view {
    (
        bool hasHeroWinner,
        uint8 heroQuadrant,
        uint8 heroSymbol
    ) = _topHeroSymbol(dailyIdx);
    if (!hasHeroWinner) return;

    uint8 heroColor;
    if (heroQuadrant == 0) {
        heroColor = uint8(randomWord & 7);
    } else if (heroQuadrant == 1) {
        heroColor = uint8((randomWord >> 3) & 7);
    } else if (heroQuadrant == 2) {
        heroColor = uint8((randomWord >> 6) & 7);
    } else {
        heroColor = uint8((randomWord >> 9) & 7);
    }

    w[heroQuadrant] = uint8(
        (uint256(heroQuadrant) << 6) |
            (uint256(heroColor) << 3) |
            uint256(heroSymbol)
    );
}
```

Current `_topHeroSymbol` (`contracts/modules/DegenerusGameJackpotModule.sol:1623-1653` — DELETED ENTIRELY by Plan 02 per `feedback_no_dead_guards.md` + `feedback_frozen_contracts_no_future_proofing.md`; both the function body L1625-1653 and the NatSpec block L1623-1624 go away):
```
/// @dev Returns the top hero symbol for a day across all quadrants.
///      Tie-breaker is deterministic first-seen order (q asc, symbol asc).
function _topHeroSymbol(
    uint32 day
)
    private
    view
    returns (bool hasWinner, uint8 winQuadrant, uint8 winSymbol)
{
    uint32 topAmount;
    for (uint8 q; q < 4; ) {
        uint256 packed = dailyHeroWagers[day][q];
        for (uint8 s; s < 8; ) {
            uint32 amount = uint32(
                (packed >> (uint256(s) * 32)) & 0xFFFFFFFF
            );
            if (amount > topAmount) {
                topAmount = amount;
                winQuadrant = q;
                winSymbol = s;
            }
            unchecked {
                ++s;
            }
        }
        unchecked {
            ++q;
        }
    }
    hasWinner = topAmount != 0;
}
```

Current `_rollWinningTraits` (`contracts/modules/DegenerusGameJackpotModule.sol:1928-1943` — single-site callsite of `_applyHeroOverride` at L1941; UPDATED by Plan 02 to pass raw `randWord` as a third argument so the new `_applyHeroOverride(traits, r, randWord)` 3-arg form gets both the color entropy `r` and the symbol-roll raw VRF entropy `randWord` per D-42N-BONUS-ENTROPY-01):
```
/// @dev Roll winning traits with hero symbol override.
///      All paths use fully random traits (6 bits per quadrant).
///      Hero override replaces the winning quadrant's trait if a top hero symbol exists.
/// @param randWord VRF entropy.
/// @param isBonus When true, applies keccak256 domain separation for independent bonus traits.
function _rollWinningTraits(
    uint256 randWord,
    bool isBonus
) private view returns (uint32 packed) {
    uint256 r = isBonus
        ? uint256(keccak256(abi.encodePacked(randWord, BONUS_TRAITS_TAG)))
        : randWord;
    uint8[4] memory traits = JackpotBucketLib.getRandomTraits(r);
    _applyHeroOverride(traits, r);
    packed = JackpotBucketLib.packWinningTraits(traits);
}
```

Storage declaration of the hero-wager packed slot (`contracts/storage/DegenerusGameStorage.sol:1470-1475` — UNCHANGED at v42 per HRROLL-06; planner records this as the byte-identity attestation target):
```
/// @dev Daily hero symbol wagers (ETH only), indexed by day.
///      Key: day index (from GameTimeLib). Value: 4 packed uint256s.
///      Each uint256 packs 8 × 32-bit amounts (one per symbol in that quadrant).
///      Amounts stored in units of 1e12 wei (0.000001 ETH) to fit 32 bits
///      (max ~4,295 ETH per symbol per day).
mapping(uint32 => uint256[4]) internal dailyHeroWagers;
```

Wager-time write site for HRROLL-05 backward-trace (`contracts/modules/DegenerusGameDegeneretteModule.sol:482-501` — UNCHANGED by Phase 292; this is the trace DESTINATION):
```
// Track hero wagers and player ETH stats (ETH bets only)
if (currency == CURRENCY_ETH) {
    // 1. Daily hero symbol tracking (heroQuadrant validated to {0..3} above)
    {
        uint32 day = _simulatedDayIndex();
        uint8 heroSymbol = uint8(customTicket >> (heroQuadrant * 8)) & 7;
        uint256 wagerUnit = totalBet / 1e12;
        if (wagerUnit > 0) {
            uint256 wPacked = dailyHeroWagers[day][heroQuadrant];
            uint256 shift = uint256(heroSymbol) * 32;
            uint256 current = (wPacked >> shift) & 0xFFFFFFFF;
            uint256 updated = current + wagerUnit;
            if (updated > 0xFFFFFFFF) updated = 0xFFFFFFFF;
            wPacked = (wPacked & ~(uint256(0xFFFFFFFF) << shift)) | (updated << shift);
            dailyHeroWagers[day][heroQuadrant] = wPacked;
        }
    }
    // 2. Per-player per-level ETH wagered (unrelated; NOT in HRROLL scope)
    ...
}
```

Locked D-42N-DETERMINISM-01 exact algorithm spec (recorded verbatim in `292-01-DESIGN-INTENT-TRACE.md` §(v); Plan 02 implements this verbatim):
- Pass 1: scan idx = 0..31 (q = idx >> 3, s = idx & 7) over `dailyHeroWagers[day][q]`; SLOAD each of the 4 packed uint256 once; extract via `uint32((packed >> (uint256(s) * 32)) & 0xFFFFFFFF)`; accumulate `total` as uint64; track `maxAmount` + `leaderIdx` via strict `>` (first-seen wins on ties matches v41 `_topHeroSymbol` tie-break).
- If `total == 0`: early-bail return `(false, 0, 0)` per HRROLL-01.
- `leaderBonus = maxAmount / 2` (×1.5 effective weight on leader per D-42N-LEADER-BONUS-01).
- `effectiveTotal = total + leaderBonus` (uint64; max ≈ 2.06e11 << 2^64; bias-free modulo).
- `pick = uint64(uint256(keccak256(abi.encode(heroEntropy, day))) % effectiveTotal)` — `abi.encode` (NOT `abi.encodePacked`) per D-42N-DETERMINISM-01 to avoid type-coercion ambiguity; `heroEntropy` is the new second parameter on `_applyHeroOverride` (raw `randWord` per D-42N-BONUS-ENTROPY-01).
- Pass 2: walk idx = 0..31 over cached weights; `cumulative += weight; if (idx == leaderIdx) cumulative += leaderBonus; if (cumulative > pick) return (true, uint8(idx >> 3), uint8(idx & 7));`. Cursor direction: flat idx ascending = q ascending → s ascending (matches v41 `_topHeroSymbol` scan order even though weighted picks shouldn't care).
- D-42N-CACHE-01 chosen shape (recorded in `292-01-MEASUREMENT.md` §3 three-shape comparison): one of {flat `uint32[32]`, `uint64[32]` weights, packed `uint256[4]`}; Plan 01 selects; Plan 02 implements verbatim.

Audit baseline reference: `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` (v41.0 closure HEAD; source of truth for "byte-identical to v41 close" assertions in `292-01-MEASUREMENT.md` scaffold + Plan 02's populated values).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author `292-01-DESIGN-INTENT-TRACE.md` covering the 5 HRROLL-10 trace sections + 7 decision anchors + out-of-scope register + SWEEP-02(ii) adversarial-hypothesis pre-emptive answers</name>
  <files>
    .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 58-69 (HRROLL-01..10 verbatim spec; locked requirement set, do NOT expand scope) + lines 15-18 (HRROLL-scope Out-of-Scope register entries)
    - .planning/ROADMAP.md (Phase 292 entry — success criteria 1-5; HRROLL-01..10 algorithm spec; ×1.5 leader bonus + no-floor + scan-order tie-break locks; storage/ABI byte-identity invariants; decision anchors named)
    - .planning/phases/292-hero-override-weighted-roll-hrroll/292-CONTEXT.md (LOCKED USER DECISIONS — D-42N-BONUS-ENTROPY-01 + D-42N-CACHE-01; Claude's-Discretion register; sister-phase 290 pattern lock; deletion posture; pass-2 cursor-walk spec; weight-arithmetic widening note)
    - .planning/phases/290-mint-batch-event-sig-cleanup-mintcln/290-01-DESIGN-INTENT-TRACE.md (verbatim pattern reference for the 3-section trace shape — Phase 292 uses 5 sections per HRROLL-10 instead of 3; structural shape mirrored: Audit Baseline + Anchors / Section headers / Decision Anchors restate / Out-of-Scope Register / SWEEP-02 adversarial pre-emptive answers / Plan-02 Pre-Patch Gate / Sister-Plan Coverage Map / Source Citations)
    - contracts/modules/DegenerusGameJackpotModule.sol L1584-L1653 (`_applyHeroOverride` + `_topHeroSymbol` — load-bearing for trace section (i) original-design rationale + section (v) deletion posture + section (ii) leader-bonus magnitude trade-offs)
    - contracts/modules/DegenerusGameJackpotModule.sol L1928-L1943 (`_rollWinningTraits` — load-bearing for the D-42N-BONUS-ENTROPY-01 callsite-scope discussion + the cross-bonus invariance rationale)
    - contracts/modules/DegenerusGameDegeneretteModule.sol L482-L501 (`placeDegeneretteBet` wager-time write to `dailyHeroWagers[dailyIdx][q]` — load-bearing for trace section (iv) HRROLL-05 backward-trace destination)
    - contracts/storage/DegenerusGameStorage.sol L1470-L1475 (`dailyHeroWagers` declaration — load-bearing for storage-byte-identity context)
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_design_intent_before_deletion.md (the discipline this task satisfies)
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_commitment_window.md (HRROLL-05 backward-trace methodology — wager-time commitment vs randomness-availability time-ordering)
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_rng_backward_trace.md (HRROLL-05 backward-trace methodology — every RNG audit traces BACKWARD from each consumer)
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_dead_guards.md (`_topHeroSymbol` deletion posture — no deprecated stub, no `// @dev removed` marker)
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md (the discipline applies to NatSpec/contract comments, NOT to planning artifacts — the trace DOES contain historical context by design; record this exemption at the top of the trace doc per the Phase 290 trace pattern)
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_frozen_contracts_no_future_proofing.md (no extensibility hooks, no flag-gated alternative algorithms — ship the locked one shape)
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_never_preapprove_contracts.md (trace MUST NOT tell Plan 02's executor that the contract patch is pre-approved)
  </read_first>
  <action>
    Create `292-01-DESIGN-INTENT-TRACE.md` (markdown; agent-side planning artifact; ≥ 110 lines). Mirror the structural shape of `290-01-DESIGN-INTENT-TRACE.md` (planning-doc exemption note at top → Audit Baseline + Anchors → section headers → Decision Anchors restate → Out-of-Scope Register → SWEEP-02 pre-emptive answers → Plan-02 Pre-Patch Gate → Sister-Plan Coverage Map → Source Citations) but with HRROLL-10's five trace sections instead of MINTCLN-10's three.

    Top header: `# Phase 292 HRROLL — Design-Intent Trace (HRROLL-10)`.

    Planning-doc exemption note (mirror Phase 290 wording at top): the trace IS a planning artifact whose purpose is to record historical rationale for v41 → v42 changes; `feedback_no_history_in_comments.md` applies to NatSpec/contract source comments only — it does NOT apply to planning docs.

    `## Audit Baseline + Anchors` — anchor `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4`; list ALL 7 Phase 292-scope anchors (D-42N-LEADER-BONUS-01, D-42N-FLOOR-01, D-42N-COLOR-ENTROPY-01, D-42N-DETERMINISM-01, D-42N-GAS-01, D-42N-BONUS-ENTROPY-01, D-42N-CACHE-01) with their one-line dispositions + the user-decision date 2026-05-17 where applicable (LEADER-BONUS / FLOOR / BONUS-ENTROPY are user-locked; COLOR-ENTROPY / DETERMINISM / GAS / CACHE are planner-discretion per CONTEXT.md). Then list carry-forward anchors: v41 Phase 288 D-288-FIX-SHAPE-01 (`dailyIdx` single-writer-day-key); v41 Phase 281 owed-salt seed-separation pattern (cross-RNG-consumer non-collision audit-story reference); v40 D-40N-MINTBOOST-OUT-01 (UNCHANGED — HRROLL doesn't touch mint-boost); v34 D-271-ADVERSARIAL-01 + D-271-ADVERSARIAL-02 + D-271-ADVERSARIAL-03 (Phase 296 SWEEP combined adversarial pass; `/degen-skeptic` OUT).

    `## Section (i) — Original Deterministic `_topHeroSymbol` Single-Leader Rationale` — trace WHY the v41 form selected the deterministic max-amount `(quadrant, symbol)` slot via the nested `for` scan at L1633-L1651 rather than a weighted random roll. Cover: (a) the deterministic-first design intent — at the v41 design moment, the simplest defensible mechanic was "the single largest hero-symbol bet wins"; (b) the deterministic-pick property meant zero RNG consumption for hero override (the existing color path consumed `randomWord` bits but symbol-pick was entropy-free); (c) the deterministic scan order (q asc → s asc; strict `>` tie-break = first-seen wins) was the auditable tie-break form; (d) the v41 form's failure mode that motivates the v42 weighted-roll cleanup — under organic play, the single-largest bettor wins 100% of the time; small organic bettors with non-trivial wagers see 0% pick probability; the player-game-theory outcome is concentration-incentive (whales benefit disproportionately because their bets convert directly into deterministic wins; the small-bettor's incentive to participate erodes). The v41 form was NOT a defect; it was a constraint-satisfying design at that phase; the v42 weighted-roll is a player-experience rebalance enabled by accumulated audit-story room at v42's audit-subject delta.

    `## Section (ii) — Leader-Bonus Magnitude Trade-Offs (×2 vs ×1.5 vs none)` — trace the trade-off space. Cover: (a) ×2 alternative — strongest leader bias; rewards large bettors most; risks recreating the deterministic-pick monopolization in probabilistic clothing (largest bettor wins ~67% of the time at typical bet distributions); (b) ×1.5 locked per D-42N-LEADER-BONUS-01 — moderate leader bias; preserves the "larger bets win more often" property without monopolization (largest bettor wins ~50-60% at typical distributions, varying with the spread); leader retains meaningful EV advantage but no single-bettor lockout; (c) no-bonus (pure-proportional) alternative — flat weighted roll; every wei has equal pick probability; arguably the most-fair mechanic but discards the "size matters" signal entirely and dilutes the hero-override mechanic to "first to bet anywhere wins proportionally." The ×1.5 disposition was the user's explicit balance between game-theoretic accessibility (small organic bettors retain meaningful pick probability) and incentive preservation (large hero-symbol bets still earn disproportionate win frequency). Record this rationale in source-prose; document that ×1.5 was selected at 2026-05-17 user disposition; out-of-scope alternatives are REJECTED, not DEFERRED — they would re-enter scope only as a fresh decision under a new anchor at some future milestone if the player game-theory diverges from intent post-launch.

    `## Section (iii) — Sybil Exposure / No-Floor Trade-Offs` — trace the trade-off space. Cover: (a) no-floor (locked per D-42N-FLOOR-01) — smallest organic bettor wins with proportional probability `amount / effectiveTotal`; arithmetic simplicity (no eligibility predicate); aligned with the "every wei counts" mechanic that the wagerUnit / 1e12 scaling already implies (a single 1e12 wei bet creates a slot entry of 1); (b) floor alternative (rejected) — would require an anti-spam threshold (e.g., `if (amount > minWagerFloor)`); imposes a discontinuity at the floor boundary (a bettor at floor − 1 has 0% pick probability; at floor + 1 has proportional probability); creates a perverse "bet exactly at the floor for max marginal EV" incentive; floor-value tuning would require its own decision anchor. Sybil dilution concern (attacker spam-bets 1 wei on all 32 slots to dilute organic winners): structurally bounded by the `wagerUnit = totalBet / 1e12` scaling — to deposit 32 × 1 wei of wager weight the attacker must place 32 × 1e12 wei = 3.2e13 wei = 0.032 ETH in actual stake (because the bet amount is the ETH-denominated value, scaled by 1e12 to fit uint32). At that capital cost, the attacker's spam dilutes the organic-winner pool by at most 32 / `effectiveTotal` ≈ negligible at any realistic daily wager volume. The no-floor disposition is therefore SAFE against sybil-dilution; recorded as a SWEEP-02(ii) pre-emptive answer below.

    `## Section (iv) — RNG Commitment-Window Backward-Trace (HRROLL-05)` — per `feedback_rng_commitment_window.md` + `feedback_rng_backward_trace.md`, BACKWARD-trace from `_rollHeroSymbol(dailyIdx, heroEntropy)` consumer at `_applyHeroOverride` (`contracts/modules/DegenerusGameJackpotModule.sol:1594-1621`) to the wager-time write at `placeDegeneretteBet` (`contracts/modules/DegenerusGameDegeneretteModule.sol:484-501`). Trace structure: (1) consumer site — `_rollHeroSymbol` reads `dailyHeroWagers[dailyIdx][q]` for q = 0..3 (4 SLOADs) and consumes `heroEntropy` (the new raw `randWord` plumbed through `_applyHeroOverride` per D-42N-BONUS-ENTROPY-01); (2) write site — `placeDegeneretteBet` writes `dailyHeroWagers[dailyIdx][heroQuadrant]` at L499 during ETH-currency bets, gated on `wagerUnit > 0` (wagerUnit = totalBet / 1e12); (3) `dailyIdx` write site — `dailyIdx` is mutated EXCLUSIVELY at `_unlockRng` (AdvanceModule) at the prior-day jackpot resolution boundary; verify by grepping `contracts/` for `dailyIdx =` assignments and confirming the only write site is `_unlockRng`; (4) commitment ordering — wager amounts for day D are LOCKED before day D+1's `_unlockRng` runs and therefore before day D+1's VRF request for that day's jackpot fires; (5) randomness availability — `randWord` is the Chainlink VRF callback payload delivered at day D+1's jackpot resolution; it is unknowable at day D's bet-placement time; (6) verification — player-controllable input (wager amounts) committed before randomness available → RNG commitment-window invariant SAFE; the attacker cannot adjust their wager-distribution to bias the symbol-roll because the symbol-roll entropy is unknown at wager-time. Document the v41 Phase 288 D-288-FIX-SHAPE-01 inheritance: the structural `dailyIdx` single-writer-day-key invariant established at v41 Phase 288 (frozen across the rng-lock window) is the load-bearing invariant HRROLL-05 inherits — without it, mid-jackpot `dailyIdx` mutation could cause the symbol-roll to consume wagers from a different day than intended. Plan 02 makes ZERO modifications to `dailyIdx` writers, so the v41 invariant remains load-bearing at v42 close HEAD.

    `## Section (v) — Gas Budget Headroom + D-42N-CACHE-01 Cache-Shape Decision + D-42N-DETERMINISM-01 Exact Algorithm` — record (a) the gas headroom analysis: v41 `_topHeroSymbol` does 4 SLOADs (one per quadrant) + 32 bit-extracts + 32 strict-`>` comparisons; v42 `_rollHeroSymbol` adds a second 32-extract pass (cursor walk) over a memory cache + one `keccak256(abi.encode(heroEntropy, day))` + one modulo against `effectiveTotal` + one `maxAmount / 2` division for the leader bonus; (b) D-42N-CACHE-01 — list the three candidate cache shapes (flat `uint32[32]` indexed `q*8 + s`; `uint64[32]` weights pre-bonus-applied; packed `uint256[4]` cache with SHR+AND extracts in the hot loop) and reference `292-01-MEASUREMENT.md` §3 where the three-shape comparison table is populated; the chosen shape is what Plan 02 implements verbatim; the rejected shapes are out-of-scope and not deferred (re-SLOAD-without-cache is explicitly REJECTED for wasting ~8.4K gas per call); (c) D-42N-DETERMINISM-01 — the exact algorithm spec is locked verbatim: `pass 1` = scan idx = 0..31 over `dailyHeroWagers[day][q]` (q = idx >> 3, s = idx & 7), SLOAD each of the 4 packed slots ONCE, extract via `uint32((packed >> (uint256(s) * 32)) & 0xFFFFFFFF)`, accumulate `total` as uint64, track `maxAmount` + `leaderIdx` via strict `>` (first-seen wins on ties — matches v41 scan order); early-bail `(false, 0, 0)` if `total == 0`; `leaderBonus = maxAmount / 2`; `effectiveTotal = total + leaderBonus`; `pick = uint64(uint256(keccak256(abi.encode(heroEntropy, day))) % effectiveTotal)` — `abi.encode` (NOT `abi.encodePacked`) per D-42N-DETERMINISM-01 to avoid type-coercion ambiguity; `pass 2` = walk idx = 0..31 over cached weights, `cumulative += weight; if (idx == leaderIdx) cumulative += leaderBonus; if (cumulative > pick) return (true, uint8(idx >> 3), uint8(idx & 7));` — flat idx ascending = q ascending → s ascending direction; (d) D-42N-BONUS-ENTROPY-01 rationale — explain the cross-bonus invariance: on jackpot days that trigger both regular AND bonus trait rolls, BOTH rolls land the SAME hero `(q, s)` per day because both consume the same raw `randWord` for the symbol pick; only colors differ (via the existing `r`-vs-`randWord` distinction at `_rollWinningTraits` L1937-L1939); record that the rejected divergent-entropy alternative (using post-bonus-tag `r` for the symbol-roll on bonus path) would have meant bonus and regular rolls land in DIFFERENT quadrants on the same jackpot day — a noticeable mechanic change that dilutes hero-symbol winner EV; the per-jackpot-day lock-in mechanic is intentional and preserved.

    `## Decision Anchors` — restate all 7 anchors verbatim with their one-line dispositions: D-42N-LEADER-BONUS-01 (×1.5 user-locked 2026-05-17); D-42N-FLOOR-01 (no floor user-locked 2026-05-17); D-42N-BONUS-ENTROPY-01 (raw `randWord` feeds `_rollHeroSymbol`; cross-bonus invariance preserved; user-locked 2026-05-17); D-42N-CACHE-01 (most-gas-efficient cache shape per user disposition 2026-05-17; planner picks at plan-phase from the three-shape comparison in `292-01-MEASUREMENT.md` §3); D-42N-COLOR-ENTROPY-01 (color bits from `r` vs symbol bits from `keccak(randWord, day)` — orthogonal entropy domains by construction; planner-locked); D-42N-DETERMINISM-01 (exact algorithm per §(v); planner-locked); D-42N-GAS-01 (acceptance threshold derived from D-42N-CACHE-01 chosen shape's theoretical worst case; if > +10K vs v41 baseline → ESCALATION-CHECKPOINT to user before Plan 02; planner-locked).

    `## Out-of-Scope Register (NOT touched by Phase 292 per REQUIREMENTS.md `## Out of Scope`)` — enumerate the 8 items: (a) HRROLL min-wager floor for eligibility — out per D-42N-FLOOR-01; (b) HRROLL leader-bonus magnitude alternatives (×2, ×3, flat floor) — REJECTED per D-42N-LEADER-BONUS-01 (not DEFERRED); (c) HRROLL storage layout changes — `dailyHeroWagers[uint32 => uint256[4]]` + `dailyIdx` UNCHANGED per the note's "no storage changes" constraint; (d) HRROLL public ABI changes — `payDailyJackpot` + `payDailyJackpotCoinAndTickets` + view function signatures byte-identical post-fix; (e) per-bet hero-quadrant multiplier `_applyHeroMultiplier` in `DegenerusGameDegeneretteModule.sol` — explicitly unrelated mechanic; stays as-is per v37 Phase 267 design; (f) divergent-entropy alternative for bonus rolls — REJECTED at D-42N-BONUS-ENTROPY-01 (not DEFERRED); (g) cache-shape A/B benchmark in production — only theoretical-at-292 + empirical-at-293 in scope; (h) adversarial pass on HRROLL in isolation — deferred to Phase 296 SWEEP per D-271-ADVERSARIAL-01 carry; HRROLL is NOT red-teamed at Phase 292. For each item state "out-of-scope per [register reason]; not addressed by Plan 02."

    `## SWEEP-02(ii) HRROLL Adversarial-Hypothesis Pre-Emptive Answers` — pre-emptively answer the 4 hypotheses listed in REQUIREMENTS.md SWEEP-02(ii) so Phase 296 has a baseline disposition record to test against: (1) **Does ×1.5 leader bonus open whale-coordination or wash-trading MEV?** Answer: NO — the leader bonus is computed ON CHAIN from the on-chain wager state at `_rollHeroSymbol` invocation time; the attacker cannot wash-trade between bet-placement and roll-execution because the symbol-roll entropy is unknown at bet time per HRROLL-05; coordinating whales can certainly push a single (q, s) to leader status, but that is the mechanic working as designed (×1.5 rewards capital commitment) — not an MEV exploit. Expected Phase 296 finding: SAFE_BY_DESIGN. (2) **Does no-floor design open sybil dilution attack?** Answer: NO — see Section (iii) capital-cost calculation; 32 × 1 wei of wager weight requires 32 × 1e12 wei = 0.032 ETH of actual stake; at realistic daily wager volumes the dilution is negligible; expected Phase 296 finding: SAFE_BY_STRUCTURAL_CLOSURE. (3) **Does the new RNG-consumer (symbol roll consuming VRF bits via keccak) collide with existing consumers?** Answer: NO — the symbol-roll consumes `keccak256(abi.encode(heroEntropy, day))` output, which is structurally orthogonal to the raw `randWord` bit-slices used by jackpot-path-select (bits[0..12]) / lootbox-Bernoulli (bits[152..167]) / jackpot-Bernoulli (bits[200..215]) / color-sample (bits `quadrant*3`); keccak's hash-output is independent of any specific bit-slice of its input; non-collision is by construction, not probabilistic; expected Phase 296 finding: SAFE_BY_DESIGN. Attest in `292-01-MEASUREMENT.md` §3 as the D-42N-COLOR-ENTROPY-01 one-liner. (4) **Does the gas regression open a DOS surface?** Answer: NO — the D-42N-GAS-01 acceptance threshold (derived from D-42N-CACHE-01 chosen shape's theoretical worst case in `292-01-MEASUREMENT.md` §3) bounds the regression; the worst case is bounded by 4 SLOAD + 2 × 32-extract loops + 1 keccak + 1 modulo + 1 division — no unbounded computation; if the worst case > +10K, Plan 01 escalates to user via ESCALATION-CHECKPOINT in `292-01-MEASUREMENT.md` §3 before Plan 02 proceeds. Expected Phase 296 finding: SAFE_BY_BOUNDED_COMPUTATION.

    `## Plan-02 Pre-Patch Gate` — explicit statement: "Plan 02 (`292-02-PLAN.md`) cannot begin its contract-edit task until `292-01-DESIGN-INTENT-TRACE.md` AND `292-01-MEASUREMENT.md` exist at the paths in Plan 01 `files_modified`, AND `292-01-MEASUREMENT.md` §3 carries no ESCALATION-CHECKPOINT marker (or the user has resolved any escalation that arose). This is the design-intent-before-deletion gate per `feedback_design_intent_before_deletion.md`. Plan 02's first task reads both artifacts and copies forward the decision anchors + measurement framework into the batched commit message body. Plan 02 is the user-approval gate for the contract changes; Plan 01 is AGENT-COMMITTED (planning artifacts only; zero contract / test edits per `feedback_no_contract_commits.md`). The planner has NOT pre-approved the contract diff per `feedback_never_preapprove_contracts.md` — the user reviews the full diff at Plan 02 Task 5 before any commit."

    `## Sister-Plan Coverage Map` — table mapping HRROLL-NN → which Phase 292 plan covers it: HRROLL-10 → Plan 01 (this trace doc) + `292-01-MEASUREMENT.md`; HRROLL-01..04 → Plan 02 contract patch; HRROLL-05 → Plan 01 trace §(iv) backward-trace + Plan 02 contract patch preserving the invariant; HRROLL-06 → Plan 02 storage byte-identity attestation (recorded in `292-01-MEASUREMENT.md` §2); HRROLL-07 → Plan 02 public ABI byte-identity attestation (recorded in `292-01-MEASUREMENT.md` §4); HRROLL-08 → Plan 01 theoretical worst-case gas in `292-01-MEASUREMENT.md` §3 (per `feedback_gas_worst_case.md` theoretical-first); empirical regression DEFERRED to Phase 293 TST-HRROLL-06 per the Phase 291 D-291-GAS-01 mirror pattern; HRROLL-09 → Plan 01 trace §(v) algorithm lock + Plan 02 contract patch implementing verbatim.

    `## Source Citations` — list line ranges with anchors per the Phase 290 pattern: `contracts/modules/DegenerusGameJackpotModule.sol` L183 (`BONUS_TRAITS_TAG` declaration; reused at L1755 + L1938 — domain-separation idiom for HRROLL keccak); L1584-L1593 (`_applyHeroOverride` NatSpec — REWRITTEN by Plan 02); L1594-L1621 (`_applyHeroOverride` body — signature gains second entropy parameter); L1623-L1624 (`_topHeroSymbol` NatSpec — DELETED by Plan 02); L1625-L1653 (`_topHeroSymbol` body — DELETED by Plan 02); L1928-L1943 (`_rollWinningTraits` — single callsite of `_applyHeroOverride` at L1941; updated to pass raw `randWord`); `contracts/modules/DegenerusGameDegeneretteModule.sol` L482-L501 (`placeDegeneretteBet` wager-time write — HRROLL-05 backward-trace destination); `contracts/storage/DegenerusGameStorage.sol` L1470-L1475 (`dailyHeroWagers` declaration — UNCHANGED).

    Discipline: write in prose, not bulleted-only; include line-number citations to the source files for every claim about the current code shape; the trace is the PLANNING artifact (history is allowed here — this is NOT a NatSpec comment, so `feedback_no_history_in_comments.md` does NOT apply); the trace DOES anticipate and pre-emptively answer the 4 SWEEP-02(ii) HRROLL adversarial hypotheses so Phase 296 has a baseline to test against; the trace tells Plan 02's executor that the diff is NOT pre-approved per `feedback_never_preapprove_contracts.md`.

    Do NOT make any contract edits in this task. Do NOT modify any file under `contracts/`. Do NOT modify KNOWN-ISSUES.md. Do NOT touch test files.
  </action>
  <verify>
    <automated>test -f .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md && grep -q "D-42N-LEADER-BONUS-01" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md && grep -q "D-42N-FLOOR-01" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md && grep -q "D-42N-COLOR-ENTROPY-01" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md && grep -q "D-42N-DETERMINISM-01" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md && grep -q "D-42N-GAS-01" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md && grep -q "D-42N-BONUS-ENTROPY-01" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md && grep -q "D-42N-CACHE-01" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md && grep -q "D-288-FIX-SHAPE-01" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md && grep -q "D-271-ADVERSARIAL-01" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md && grep -q "MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md && grep -qE "Section \(i\)" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md && grep -qE "Section \(ii\)" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md && grep -qE "Section \(iii\)" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md && grep -qE "Section \(iv\)" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md && grep -qE "Section \(v\)" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md && grep -q "Out-of-Scope" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md && grep -q "SWEEP-02" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md && grep -q "placeDegeneretteBet" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md && grep -q "DegenerusGameDegeneretteModule.sol" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md && grep -qE "484-501|L484|L484-L501" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md && [ $(grep -v '^#' .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md | wc -l) -ge 110 ] && [ -z "$(git diff --name-only HEAD -- contracts/ test/ KNOWN-ISSUES.md 2>/dev/null)" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `292-01-DESIGN-INTENT-TRACE.md` exists, ≥ 110 non-comment lines.
    - All 5 required HRROLL-10 trace sections present and named: `Section (i)` original `_topHeroSymbol` single-leader rationale; `Section (ii)` leader-bonus magnitude trade-offs; `Section (iii)` sybil exposure / no-floor trade-offs; `Section (iv)` HRROLL-05 RNG commitment-window backward-trace; `Section (v)` gas headroom + D-42N-CACHE-01 cache-shape + D-42N-DETERMINISM-01 algorithm lock.
    - All 7 Phase 292-scope decision anchors recorded verbatim: D-42N-LEADER-BONUS-01, D-42N-FLOOR-01, D-42N-COLOR-ENTROPY-01, D-42N-DETERMINISM-01, D-42N-GAS-01, D-42N-BONUS-ENTROPY-01, D-42N-CACHE-01.
    - Carry-forward anchors recorded: D-288-FIX-SHAPE-01 (dailyIdx single-writer-day-key); v41 Phase 281 owed-salt pattern reference; D-40N-MINTBOOST-OUT-01 (unchanged); D-271-ADVERSARIAL-01 + D-271-ADVERSARIAL-02 + D-271-ADVERSARIAL-03 (Phase 296 SWEEP combined pass).
    - Audit baseline `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` cited explicitly.
    - Out-of-Scope register section enumerates the 8 items per REQUIREMENTS.md `## Out of Scope`.
    - SWEEP-02(ii) section pre-emptively answers ALL 4 HRROLL adversarial hypotheses (leader-bonus MEV; no-floor sybil; RNG-consumer bit-slice collision; gas DOS).
    - HRROLL-05 backward-trace explicitly names `placeDegeneretteBet` at `contracts/modules/DegenerusGameDegeneretteModule.sol:484-501` (or `L484-L501`) as the trace destination.
    - D-42N-BONUS-ENTROPY-01 rationale explicitly records the cross-bonus invariance — bonus + regular rolls land same `(q, s)` per day because both consume raw `randWord` for symbol pick; rejected divergent-entropy alternative explained.
    - D-42N-DETERMINISM-01 records the EXACT algorithm: `abi.encode(heroEntropy, day)` (NOT `abi.encodePacked`); `pick = uint64(uint256(keccak256(...)) % effectiveTotal)`; flat idx ascending; leader-bonus added at `idx == leaderIdx`; first-seen tie-break via strict `>`.
    - Plan-02 Pre-Patch Gate statement present + explicitly cites `feedback_never_preapprove_contracts.md` (planner has NOT pre-approved the contract diff).
    - Sister-Plan Coverage Map maps each HRROLL-NN to its Phase 292 plan with HRROLL-08 explicitly noting the theoretical-at-292 / empirical-at-293 split per the Phase 291 D-291-GAS-01 mirror pattern.
    - Planning-doc exemption note at top of file (mirror Phase 290 pattern) — `feedback_no_history_in_comments.md` does NOT apply to planning docs.
    - ZERO files touched under `contracts/` or `test/` or `KNOWN-ISSUES.md` (`git diff` shows zero modifications outside the trace + scaffold paths in `files_modified`).
    - Trace describes the v41 `_topHeroSymbol` form as constraint-satisfying-at-its-design-phase, NOT as a defect (correct framing — the v42 patch is a player-experience rebalance, not a defect remediation).
  </acceptance_criteria>
  <done>
    `292-01-DESIGN-INTENT-TRACE.md` exists with the 5-section HRROLL-10 trace, all 7 decision anchors, out-of-scope register, SWEEP-02(ii) adversarial-hypothesis pre-emptive answers, HRROLL-05 backward-trace from `_rollHeroSymbol` consumer back to `placeDegeneretteBet` wager-write, Plan-02 pre-patch-gate statement, sister-plan coverage map, and source citations. The artifact is the load-bearing rationale-record Plan 02 cites in its commit message body. HRROLL-10's "BEFORE the contract patch lands" gate is partially satisfied (the second half is `292-01-MEASUREMENT.md` produced by Task 2).
  </done>
</task>

<task type="auto">
  <name>Task 2: Author `292-01-MEASUREMENT.md` scaffold with 6 attestation sections + D-42N-CACHE-01 three-shape gas comparison + D-42N-GAS-01 acceptance threshold + D-42N-COLOR-ENTROPY-01 non-collision one-liner + ESCALATION-CHECKPOINT branch</name>
  <files>
    .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md HRROLL-06 + HRROLL-07 + HRROLL-08 (lines 65-67: storage byte-identity + public ABI byte-identity + gas-regression requirements)
    - .planning/ROADMAP.md (Phase 292 success criterion 4: storage byte-identical + public ABI byte-identical + zero new entry points specs)
    - .planning/phases/292-hero-override-weighted-roll-hrroll/292-CONTEXT.md (D-42N-CACHE-01 + D-42N-GAS-01 + D-42N-COLOR-ENTROPY-01 + D-42N-DETERMINISM-01 spec for the three-shape comparison + non-collision attestation + theoretical-first methodology)
    - .planning/phases/290-mint-batch-event-sig-cleanup-mintcln/290-01-MEASUREMENT.md (verbatim pattern reference for the 6-section scaffold shape; planner mirrors the structural pattern with HRROLL-specific adjustments — §5 events degenerates to "none touched" for HRROLL; §6 callsite diff degenerates to single-site verification because `_applyHeroOverride` has only ONE caller at `_rollWinningTraits` L1941)
    - .planning/phases/290-mint-batch-event-sig-cleanup-mintcln/290-01-MEASUREMENT.md §3 (Phase 290's theoretical-first worst-case gas derivation; structural template for the D-42N-CACHE-01 three-shape comparison Plan 01 produces here)
    - contracts/modules/DegenerusGameJackpotModule.sol L1594-L1653 (`_applyHeroOverride` + `_topHeroSymbol` — load-bearing for §6 callsite diff documentation + §3 v41 baseline gas reference)
    - contracts/modules/DegenerusGameJackpotModule.sol L1928-L1943 (`_rollWinningTraits` — single-site callsite for §6 callsite-diff documentation)
    - contracts/storage/DegenerusGameStorage.sol L1470-L1475 (`dailyHeroWagers` declaration — §2 storage byte-identity attestation target)
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_gas_worst_case.md (theoretical-first discipline; the D-42N-CACHE-01 three-shape comparison + D-42N-GAS-01 threshold both follow this)
    - /home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md (the measurement doc IS the copy-forward source for the commit message body — keeps numerical attestations OUT of NatSpec)
  </read_first>
  <action>
    Create `292-01-MEASUREMENT.md` (markdown; agent-side planning artifact; ≥ 60 lines). The doc has TWO purposes: (1) load-bearing D-42N-CACHE-01 three-shape comparison table + D-42N-GAS-01 acceptance threshold + D-42N-COLOR-ENTROPY-01 non-collision attestation — all DERIVED at Plan 01 time (not placeholders); (2) scaffold for Plan 02's 6 attestations with `<FILL-IN-Plan-02>` placeholders for the post-patch numerical fields. The doc is the verbatim copy-forward source for Plan 02's batched commit message body per `feedback_no_history_in_comments.md`.

    Top header: `# Phase 292 HRROLL — Measurement Attestations (HRROLL-06 + HRROLL-07 + HRROLL-08)`.

    Mirror-pattern note at top: this scaffold mirrors `290-01-MEASUREMENT.md`'s 6-section shape with two HRROLL-specific adjustments — §5 events degenerates to "NONE touched — HRROLL has no event surface"; §6 callsite-diff degenerates to single-site verification because `_applyHeroOverride` has only one caller in the codebase. Plan 02 re-validates every populated value against the post-patch tree before the user approves the commit.

    Section structure:

    `## (1) Audit Baseline` — anchor: `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` (v41.0 closure HEAD; source of truth for "byte-identical to v41 close" assertions in this scaffold and for every "delta vs v41 close" measurement Plan 02 records below). All comparisons resolve against this SHA via `git worktree add` or `git show <sha>:<path>` techniques per the Phase 290 measurement-pattern precedent.

    `## (2) Storage-Slot Grep Proof (HRROLL-06)` — placeholder: `forge inspect contracts/storage/DegenerusGameStorage.sol:DegenerusGameStorage storageLayout diff vs v41 close = <EMPTY | <recorded value>>. <FILL-IN-Plan-02>`. Note: `dailyHeroWagers[uint32 => uint256[4]]` mapping at slot UNCHANGED; `dailyIdx` UNCHANGED; zero new storage slots; zero new mappings; zero new SSTORE callsites; zero new SLOAD callsites in HRROLL scope (the new `_rollHeroSymbol` reads the SAME 4 `dailyHeroWagers[day][q]` slots that v41 `_topHeroSymbol` read — same slot count, same access pattern). Escalation rule: if diff is non-empty → record exact diff text + flag `🚨 STORAGE LAYOUT REGRESSION — STOP — escalate to user before Plan 02 Task 5` (mirror Phase 290 escalation pattern).

    `## (3) Worst-Case Gas (theoretical FIRST per feedback_gas_worst_case.md) + D-42N-CACHE-01 + D-42N-GAS-01 + D-42N-COLOR-ENTROPY-01` — this section is PRODUCED AT PLAN 01 TIME (not placeholder). Structure:

    `### (3.a) v41 baseline — `_topHeroSymbol(dailyIdx)` gas reference` — derive analytically: 4 × SLOAD (one per quadrant; 2100 gas cold or 100 warm; in jackpot context the slots are cold per call ⇒ ~8400 gas SLOAD baseline) + 32 × bit-extract (`packed >> (s*32) & 0xFFFFFFFF` ≈ 3 SHL/SHR + 3 AND ≈ 24 gas each ⇒ ~768 gas) + 32 × strict-`>` comparison (~3 gas each ⇒ ~96 gas) + 32 × loop-counter `unchecked { ++s }` (~5 gas each ⇒ ~160 gas) + 4 × outer-loop `unchecked { ++q }` (~5 gas each ⇒ ~20 gas) + function entry/exit (~50 gas). v41 baseline ~9494 gas worst-case.

    `### (3.b) D-42N-CACHE-01 Three-Shape Comparison` — produce the three-shape comparison table as required by the locked decision. For each candidate shape, derive theoretical pass-1 + pass-2 + total gas using the same accounting basis as §(3.a):

    | Shape | Pass-1 cost (SLOAD + cache build) | Pass-2 cost (cursor walk + early-exit avg) | Keccak + modulo + division | Total (worst-case) | Delta vs v41 baseline |
    |---|---|---|---|---|---|
    | Flat `uint32[32]` indexed `q*8 + s` | 4 SLOAD (~8400) + 32 bit-extract (~768) + 32 MSTORE (~96) + leader tracking (~256) | 32 × MLOAD (~96) + 32 cumulative add + comparison (~256) + 1 leaderBonus-add branch (~10) | keccak256(abi.encode(uint256,uint32)) over 64 bytes input ≈ 30 + 6×2 = 42 gas + memory expansion (~32 gas) + MOD (~8 gas) + DIV (~5 gas) ≈ 87 gas | ~9969 gas | +475 gas |
    | `uint64[32]` weights array (pre-bonus-applied) | Same pass-1 as flat `uint32[32]` PLUS pre-applied `leaderBonus` add at `idx == leaderIdx` during cache build (~24 gas) — eliminates pass-2 conditional branch | Same pass-2 minus the conditional `if (idx == leaderIdx)` (~−10 gas) | Same (~87) | ~9969 + 24 − 10 ≈ ~9983 gas | +489 gas |
    | Packed `uint256[4]` cache with SHR+AND extracts in hot loop | 4 SLOAD (~8400) + 4 MSTORE (~12) + pass-1 leader tracking via same extract idiom (~768 + 256 ≈ 1024) | 32 × in-loop SHR+AND extract from cached packed slot (~768) + cursor walk (~256) + leaderBonus branch (~10) | Same (~87) | ~10545 gas | +1051 gas |

    Recommended shape: **flat `uint32[32]`** (~475 gas regression — lowest worst-case among the three; aligns with the v41 baseline structure most closely + clearest audit story). LOCK the flat `uint32[32]` shape as the implementation reference for Plan 02. Pass-2 retains the conditional leader-bonus add (variant 2's pre-apply saves ~10 gas in pass-2 but adds ~24 gas in pass-1 — net negative). Pre-bonus-apply variant (`uint64[32]`) is REJECTED as net-negative. Packed `uint256[4]` (re-extract in pass-2) is REJECTED as the worst case among the three (re-extract burns gas on every pass-2 iteration). Re-SLOAD-without-cache (the explicit anti-pattern per CONTEXT.md) is REJECTED per `feedback_no_dead_guards.md` — burns ~8.4K gas per call for no benefit.

    `### (3.c) D-42N-GAS-01 Acceptance Threshold` — derived from the locked D-42N-CACHE-01 chosen shape (flat `uint32[32]`): **+500 gas vs v41 `_topHeroSymbol` baseline** as the soft acceptance threshold; **+750 gas as the hard upper-bound** (10% headroom over the calculated ~+475 figure). If Plan 02's empirical-at-test-phase confirms the figure within ±100 gas, the attestation is PASSED. The Phase 293 TST-HRROLL-06 fixture asserts against this threshold per the D-291-GAS-01 mirror pattern (theoretical-at-contract-phase + empirical-at-test-phase). NOTE: the calculated ~+475 figure is well under the +10K ESCALATION-CHECKPOINT threshold from CONTEXT.md → NO escalation required → Plan 02 proceeds without user-checkpoint.

    `### (3.d) ESCALATION-CHECKPOINT branch` — IF (counterfactual) the chosen shape's worst case exceeded +10K vs v41 baseline (per CONTEXT.md's >+10K → user-checkpoint rule), this section would carry an explicit `🚨 ESCALATION-CHECKPOINT — STOP — surface to user before Plan 02 proceeds` marker. Current state: NOT TRIGGERED — calculated worst case +475 gas is well within the +10K bound.

    `### (3.e) D-42N-COLOR-ENTROPY-01 Non-Collision Attestation` — one-line: "Color path consumes bits `quadrant*3` of `r` (where `r = isBonus ? keccak256(abi.encodePacked(randWord, BONUS_TRAITS_TAG)) : randWord` per `_rollWinningTraits` L1937-L1939); symbol-roll path consumes `uint64(uint256(keccak256(abi.encode(randWord, day))) % effectiveTotal)`. The two entropy sources are structurally orthogonal: the color path reads specific bit-slices of an existing entropy variable (`r`); the symbol-roll path reads a keccak hash of the raw `randWord` plus day — keccak's output is independent of any specific bit-slice of its input by hash-function design. Non-collision is structural, NOT probabilistic." Cite the existing jackpot RNG-consumer bit-slice register from REQUIREMENTS.md SWEEP-02(ii)(iii) for completeness: jackpot-path-select bits[0..12]; lootbox-Bernoulli bits[152..167]; jackpot-Bernoulli bits[200..215]; color-sample bits `quadrant*3`. The symbol-roll keccak output does NOT live in the raw `randWord` bit register at all — it lives in the keccak output domain — so collision is structurally impossible.

    `## (4) Selector Attestations (HRROLL-07)` — placeholder: `payDailyJackpot(<signature>) selector = <0x........> (UNCHANGED vs v41 close). payDailyJackpotCoinAndTickets(<signature>) selector = <0x........> (UNCHANGED vs v41 close). View function selectors: <enumerate via grep at Plan 02 time>. _applyHeroOverride is private; signature CHANGES (gains 3rd parameter uint256 heroEntropy) — record canonical Solidity signature for completeness; private-function signature changes do NOT affect public ABI. _rollHeroSymbol is the new private function; record canonical Solidity signature for completeness. _topHeroSymbol is REMOVED at v42 close HEAD — record the pre-removal canonical signature for the audit-story symmetry. <FILL-IN-Plan-02>`. Note: HRROLL-07 lock = public ABI byte-identical; the private-function signature changes are documented but do not count against the public-ABI invariant.

    `## (5) Events — NONE Touched (HRROLL has no event surface)` — explicit one-line: "HRROLL scope contains zero event declarations and zero event emissions. No event topic hash changes. No event-signature attestations required. This section is structurally empty for Phase 292; recorded as a distinction vs the Phase 290 pattern (Phase 290 carried the breaking `TraitsGenerated` topic-hash attestation at §5)." NO placeholder; this section is final at Plan 01 time.

    `## (6) Callsite Diff (Single-Site — HRROLL `_applyHeroOverride` Caller Set Degenerates to ONE)` — produce the structural documentation at Plan 01 time + placeholder for Plan 02's post-patch verification. Pre-patch state documentation: `_applyHeroOverride` has EXACTLY ONE caller at `contracts/modules/DegenerusGameJackpotModule.sol:1941` (inside `_rollWinningTraits`); verified by grep at Plan 01 authorship time. Post-patch state Plan 02 verifies: the L1941 callsite reads `_applyHeroOverride(traits, r, randWord)` (3 args; the third arg is the raw `randWord` per D-42N-BONUS-ENTROPY-01). The B2-symmetric Phase 290 pattern degenerates to a single-site verification for HRROLL — recorded as a documented structural distinction. Plan 02 fills in the post-patch single-site grep-verification result here: `<FILL-IN-Plan-02 — confirm L1941 callsite shape post-patch + confirm grep "_applyHeroOverride" returns exactly 2 occurrences: 1 declaration + 1 callsite>`. Note also: `_rollWinningTraits` itself has 12 upstream callers (L285, 354, 520, 531, 538, 609, 610, 689, 1180, 1734, 1754, 1756) — verified at context-gathering scout per CONTEXT.md — `randWord` is already in scope at every caller; no caller-path edit required.

    Footer: `## Source-Doc Cross-Cite` — link back to `292-01-DESIGN-INTENT-TRACE.md` and forward to `292-02-PLAN.md`. Note: "Plan 02 fills the `<FILL-IN-Plan-02>` placeholders in §2 + §4 + §6 post-patch. §1 + §3 + §5 are final at Plan 01 time (audit baseline, gas comparison + decision-anchor recordings, and no-events distinction). The populated values are the verbatim copy-forward source for the batched commit message body in Plan 02 Task 5 (checkpoint:human-verify). The doc MUST be re-validated against the post-patch tree before the user approves the commit."

    Do NOT make any contract edits in this task. Do NOT modify any file under `contracts/`. Do NOT modify KNOWN-ISSUES.md. Do NOT touch test files.
  </action>
  <verify>
    <automated>test -f .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && grep -q "MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && grep -qE "^## \(1\) Audit Baseline" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && grep -qE "^## \(2\) Storage-Slot Grep Proof" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && grep -qE "^## \(3\) Worst-Case Gas" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && grep -qE "^## \(4\) Selector Attestations" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && grep -qE "^## \(5\) Events" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && grep -qE "^## \(6\) Callsite Diff" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && grep -q "D-42N-CACHE-01" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && grep -q "D-42N-GAS-01" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && grep -q "D-42N-COLOR-ENTROPY-01" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && grep -q "flat .uint32.32." .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && grep -q "uint64.32." .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && grep -q "packed .uint256.4." .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && grep -q "ESCALATION-CHECKPOINT" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && grep -q "FILL-IN-Plan-02" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && grep -qE "dailyHeroWagers" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && grep -qE "keccak256\(abi\.encode\(randWord, day\)\)|keccak256\(abi\.encode\(heroEntropy, day\)\)" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && grep -qE "L1941|1941" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md && [ $(grep -v '^$' .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md | wc -l) -ge 60 ] && [ -z "$(git diff --name-only HEAD -- contracts/ test/ KNOWN-ISSUES.md 2>/dev/null)" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `292-01-MEASUREMENT.md` exists, ≥ 60 non-blank lines.
    - All 6 section headers present: §1 Audit Baseline; §2 Storage-Slot Grep Proof; §3 Worst-Case Gas (theoretical FIRST + D-42N-CACHE-01 + D-42N-GAS-01 + D-42N-COLOR-ENTROPY-01); §4 Selector Attestations; §5 Events — NONE Touched; §6 Callsite Diff (Single-Site).
    - Audit baseline `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` cited.
    - §3.b carries the D-42N-CACHE-01 three-shape comparison table with ALL THREE shapes named verbatim (flat `uint32[32]`, `uint64[32]`, packed `uint256[4]`) + per-shape pass-1 + pass-2 + keccak + total + delta-vs-v41 columns populated with theoretical figures.
    - §3.b explicitly LOCKS one shape as the implementation reference for Plan 02 (the chosen shape is named + reason recorded).
    - §3.b explicitly REJECTS the rejected shapes including the re-SLOAD-without-cache anti-pattern per `feedback_no_dead_guards.md`.
    - §3.c records the D-42N-GAS-01 acceptance threshold value derived from the chosen shape (numeric figure + soft/hard split).
    - §3.d carries the ESCALATION-CHECKPOINT branch — either NOT TRIGGERED (with the calculated worst case under +10K) or TRIGGERED with the explicit `🚨` flag if a counterfactual chosen-shape worst case exceeded +10K.
    - §3.e contains the D-42N-COLOR-ENTROPY-01 non-collision attestation as a one-liner explicitly naming BOTH bit sources: `r` for color (existing) and `keccak256(abi.encode(randWord, day))` (or `heroEntropy` in lieu of `randWord` — equivalent post-D-42N-BONUS-ENTROPY-01) for symbol-roll (new); the orthogonality argument is structural-not-probabilistic.
    - §5 explicitly states HRROLL has no event surface (NONE touched).
    - §6 documents the single-site degeneration of the B2-symmetric Phase 290 pattern; explicitly names the single callsite at `_rollWinningTraits` L1941.
    - `<FILL-IN-Plan-02>` placeholders present in §2 + §4 + §6 for the post-patch numerical fields; §1 + §3 + §5 are final at Plan 01 time (no placeholders).
    - Source-Doc Cross-Cite footer present linking back to `292-01-DESIGN-INTENT-TRACE.md` and forward to `292-02-PLAN.md`.
    - ZERO files touched under `contracts/` or `test/` or `KNOWN-ISSUES.md`.
  </acceptance_criteria>
  <done>
    `292-01-MEASUREMENT.md` exists with the 6-section scaffold. §1 + §3 + §5 are FINAL at Plan 01 time: §1 anchors the audit baseline; §3 carries the load-bearing D-42N-CACHE-01 three-shape comparison + D-42N-GAS-01 acceptance threshold + D-42N-COLOR-ENTROPY-01 non-collision attestation + ESCALATION-CHECKPOINT branch (not triggered); §5 documents the no-events distinction. §2 + §4 + §6 carry `<FILL-IN-Plan-02>` placeholders Plan 02 fills in post-patch. The HRROLL-06 + HRROLL-07 + HRROLL-08 attestation framework is now in place; only the numerical post-patch fields remain to fill. The Plan-02 pre-patch gate per HRROLL-10 is now FULLY satisfied (both sidecar artifacts exist).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planner output → Plan 02 executor | Trace + scaffold are the load-bearing inputs Plan 02 cites; corruption / omission of an anchor or section would let Plan 02 land the contract patch without satisfying HRROLL-10's "BEFORE the patch" gate. Mitigated by Task 1 + Task 2 acceptance criteria + grep gates on the trace anchors + scaffold section headers + D-42N-CACHE-01 three-shape table verbatim presence. |
| Trace doc → audit/FINDINGS-v42.0.md §9 (Phase 297) | The decision anchors (D-42N-LEADER-BONUS-01 + D-42N-FLOOR-01 + D-42N-COLOR-ENTROPY-01 + D-42N-DETERMINISM-01 + D-42N-GAS-01 + D-42N-BONUS-ENTROPY-01 + D-42N-CACHE-01) propagate forward to the v42.0 audit deliverable. Mitigated by explicit anchor recording in Task 1 + the Sister-Plan Coverage Map. |
| Trace doc → Phase 296 ADVERSARIAL-LOG.md (SWEEP-02(ii)) | The 4 pre-emptive HRROLL adversarial-hypothesis answers are the baseline disposition record Phase 296's 3-skill PARALLEL adversarial pass tests against. Corruption would force Phase 296 to re-derive the dispositions without context. Mitigated by Task 1 acceptance criterion enforcing all 4 hypotheses get answered. |
| Measurement scaffold → Plan 02 batched commit message body | The scaffold IS the verbatim copy-forward source for the commit body (per `feedback_no_history_in_comments.md` — numerical attestations go in commit body, not NatSpec). Corruption of the scaffold framework would force Plan 02 to invent its own attestation structure ad-hoc. Mitigated by Task 2 acceptance criteria + the 6 fixed section headers + the locked D-42N-CACHE-01 three-shape table. |
| D-42N-CACHE-01 chosen-shape decision → Plan 02 implementation | Plan 02's executor implements verbatim against the chosen cache shape; if Plan 01's three-shape comparison is malformed or the chosen shape is mis-locked, Plan 02 may implement a non-optimal or non-intended shape. Mitigated by Task 2 acceptance criterion requiring all three shapes named, per-shape gas figures derived, and exactly one shape explicitly LOCKED with reason. |

## STRIDE Threat Register — Phase 292 Plan 01 (Planning-Artifact Scope)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-292-01-01 | Tampering | `292-01-DESIGN-INTENT-TRACE.md` anchor recording | mitigate | Task 1 acceptance criteria enforce explicit grep-gates on all 7 Phase 292-scope anchors (D-42N-LEADER-BONUS-01, D-42N-FLOOR-01, D-42N-COLOR-ENTROPY-01, D-42N-DETERMINISM-01, D-42N-GAS-01, D-42N-BONUS-ENTROPY-01, D-42N-CACHE-01) + carry-forward anchors (D-288-FIX-SHAPE-01, D-271-ADVERSARIAL-01); missing anchor fails the automated verify and blocks plan completion. |
| T-292-01-02 | Repudiation | Out-of-scope register completeness | mitigate | Task 1 acceptance criterion requires enumeration of the 8 out-of-scope items per REQUIREMENTS.md `## Out of Scope`; trace explicitly states each item is "out-of-scope per [register reason]"; rejected alternatives (×2 leader bonus, divergent-entropy bonus rolls) are explicitly REJECTED-not-DEFERRED so the forward-cite zero-emission discipline per D-42N-FCITE-01 is preserved. |
| T-292-01-03 | Information Disclosure | Pre-emptive answers to SWEEP-02(ii) HRROLL adversarial hypotheses | mitigate | Task 1 trace pre-emptively answers ALL 4 SWEEP-02(ii) HRROLL adversarial questions (leader-bonus MEV, no-floor sybil, RNG-consumer bit-slice collision, gas DOS) so Phase 296 has a baseline disposition record to test against. Acceptance criterion enforces this content. |
| T-292-01-04 | Tampering | HRROLL-05 backward-trace completeness | mitigate | Task 1 acceptance criterion requires explicit naming of `placeDegeneretteBet` at `contracts/modules/DegenerusGameDegeneretteModule.sol:484-501` as the trace destination + the 6-step verification structure (consumer site / write site / dailyIdx write site / commitment ordering / randomness availability / verification conclusion) per `feedback_rng_commitment_window.md` + `feedback_rng_backward_trace.md`. |
| T-292-01-05 | Tampering | `292-01-MEASUREMENT.md` D-42N-CACHE-01 three-shape comparison | mitigate | Task 2 acceptance criteria enforce all three candidate shapes named verbatim (flat `uint32[32]`, `uint64[32]`, packed `uint256[4]`) + per-shape gas figures derived + one shape explicitly LOCKED as implementation reference + rejected shapes explicitly REJECTED including the re-SLOAD anti-pattern. |
| T-292-01-06 | Tampering | D-42N-GAS-01 acceptance threshold + ESCALATION-CHECKPOINT branch | mitigate | Task 2 acceptance criteria enforce a numeric threshold derived from the chosen shape's theoretical worst case + an explicit ESCALATION-CHECKPOINT branch (TRIGGERED if > +10K, NOT TRIGGERED otherwise). Plan 02 reads the marker before proceeding. |
| T-292-01-07 | Spoofing | D-42N-COLOR-ENTROPY-01 non-collision attestation | mitigate | Task 2 acceptance criterion requires BOTH bit sources explicitly named (`r` for color; `keccak256(abi.encode(randWord, day))` for symbol-roll) + the structural-orthogonality argument recorded (keccak output is independent of input bit-slices by hash-function design); attestation is NOT probabilistic. |
| T-292-01-08 | Denial of Service | Frozen-contract discipline drift | accept | Task 1 + Task 2 acceptance criteria each include zero-modification gates on `contracts/`, `test/`, `KNOWN-ISSUES.md`. Plan 01 is incapable of touching contract source by design — its mutation surface is bounded to two paths under `.planning/phases/292-*/`. |
| T-292-01-SC | Tampering | Package install (npm/pip/cargo) | accept (rationale: not applicable) | Phase 292 Plan 01 makes ZERO package installs; this is a planning-artifact-authoring plan. The package-legitimacy gate does not fire. |

(Severity bound for this plan: `low`. No `high` severity threat is in scope — Plan 01 cannot mutate contract bytecode, cannot affect on-chain state, cannot break any selector. The actual `_topHeroSymbol` deletion + `_applyHeroOverride` signature change + new `_rollHeroSymbol` addition land in Plan 02 under `autonomous: false` + USER-APPROVAL gate.)
</threat_model>

<verification>
After Task 1 + Task 2 complete:

1. `test -f .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md` returns 0.
2. `test -f .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md` returns 0.
3. `grep -cE "D-42N-LEADER-BONUS-01|D-42N-FLOOR-01|D-42N-COLOR-ENTROPY-01|D-42N-DETERMINISM-01|D-42N-GAS-01|D-42N-BONUS-ENTROPY-01|D-42N-CACHE-01" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md` returns ≥ 7 (all 7 anchors present, possibly multiple times).
4. `grep -cE "Section \(i\)|Section \(ii\)|Section \(iii\)|Section \(iv\)|Section \(v\)" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-DESIGN-INTENT-TRACE.md` returns ≥ 5 (all five HRROLL-10 trace sections present).
5. `grep -cE "^## \(1\)|^## \(2\)|^## \(3\)|^## \(4\)|^## \(5\)|^## \(6\)" .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md` returns ≥ 6 (all six attestation headers present).
6. `grep -cE "flat .uint32.32.|uint64.32.|packed .uint256.4." .planning/phases/292-hero-override-weighted-roll-hrroll/292-01-MEASUREMENT.md` returns ≥ 3 (all three D-42N-CACHE-01 candidate shapes named).
7. `git diff --name-only HEAD -- contracts/ test/ KNOWN-ISSUES.md` is EMPTY (Plan 01 made zero contract / test / KI edits).
8. `git status .planning/phases/292-hero-override-weighted-roll-hrroll/` shows exactly the two new files (`292-01-DESIGN-INTENT-TRACE.md` + `292-01-MEASUREMENT.md`) as untracked or modified.
9. Both files committed AGENT-COMMITTED (no user approval needed — zero contract edits per `feedback_no_contract_commits.md`).
10. `292-01-MEASUREMENT.md` §3 explicitly LOCKS one cache shape as the Plan 02 implementation reference.
11. `292-01-DESIGN-INTENT-TRACE.md` §(iv) explicitly names `placeDegeneretteBet` at `contracts/modules/DegenerusGameDegeneretteModule.sol:484-501` (or L484-L501).

If verification fails: re-author the failing artifact section before declaring the plan complete.
</verification>

<success_criteria>
- `292-01-DESIGN-INTENT-TRACE.md` lands with the 5-section HRROLL-10 trace, all 7 Phase 292-scope decision anchors, carry-forward anchors, out-of-scope register (8 items), SWEEP-02(ii) adversarial-hypothesis pre-emptive answers (4 hypotheses), HRROLL-05 backward-trace from `_rollHeroSymbol` consumer back to `placeDegeneretteBet` at `DegenerusGameDegeneretteModule.sol:484-501`, Plan-02 pre-patch-gate statement, sister-plan coverage map, and source citations (per HRROLL-10).
- `292-01-MEASUREMENT.md` lands with the 6-section scaffold anchored to `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4`; §1 + §3 + §5 FINAL at Plan 01 time (audit baseline + load-bearing D-42N-CACHE-01 three-shape comparison table with locked chosen shape + D-42N-GAS-01 acceptance threshold + D-42N-COLOR-ENTROPY-01 non-collision attestation + ESCALATION-CHECKPOINT branch + HRROLL-no-events distinction); §2 + §4 + §6 carry `<FILL-IN-Plan-02>` placeholders Plan 02 populates post-patch (HRROLL-06 + HRROLL-07 byte-identity attestation framework + single-site callsite verification framework locked).
- ZERO files touched under `contracts/` or `test/` or `KNOWN-ISSUES.md` (Plan 01 is planning-artifact-authoring only).
- The "BEFORE the contract patch lands" gate per `feedback_design_intent_before_deletion.md` is satisfied — Plan 02 cannot begin its contract-edit task until both Plan 01 artifacts exist + the ESCALATION-CHECKPOINT in §3 is NOT TRIGGERED.
- Both artifacts are AGENT-COMMITTED (no user approval needed; no contract or test edits made by this plan).
- D-42N-CACHE-01 chosen cache shape is explicitly LOCKED in `292-01-MEASUREMENT.md` §3.b for Plan 02 to implement verbatim.
- D-42N-GAS-01 acceptance threshold is explicitly recorded in `292-01-MEASUREMENT.md` §3.c with a numeric value Phase 293 TST-HRROLL-06 will assert against per the D-291-GAS-01 mirror pattern.
</success_criteria>

<output>
Create `.planning/phases/292-hero-override-weighted-roll-hrroll/292-01-SUMMARY.md` when this plan completes. Record: (a) trace doc anchor coverage (all 7 Phase 292-scope anchors + carry-forward anchors present); (b) scaffold doc section coverage (all 6 attestation headers present); (c) D-42N-CACHE-01 chosen shape name + reason; (d) D-42N-GAS-01 numeric acceptance threshold; (e) D-42N-COLOR-ENTROPY-01 non-collision attestation recorded; (f) ESCALATION-CHECKPOINT state (TRIGGERED / NOT TRIGGERED); (g) line counts for both files; (h) confirmation that `git diff --name-only HEAD -- contracts/ test/ KNOWN-ISSUES.md` is EMPTY post-plan; (i) explicit hand-off statement: "Plan 02 may now begin its contract-patch task per the design-intent-before-deletion gate per `feedback_design_intent_before_deletion.md` and the no-escalation state of `292-01-MEASUREMENT.md` §3.d."
</output>
