---
phase: 357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw
plan: 01
type: execute
wave: 2
depends_on: ["357-00", "357-00b"]
files_modified:
  - .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-01-DELTA-AUDIT.md
autonomous: true
requirements: [AUDIT-01]

must_haves:
  truths:
    - "Every contract file changed vs the v55 baseline 453f8073 is enumerated from git diff 453f8073 HEAD' -- contracts/ (the ~14-file v56 delta + the 357-00 hardening hunks) and attested NON-WIDENING with zero orphan hunks; each hunk maps to exactly one v56 work item (AGG / TKT / AFF / QST / OPEN / GAS / LIVE / GAS-06 / the 357-00 F-356-01 stub + D-11/D-12/D-13 gates / the 5cb707f2 advance-gate)"
    - "SOLVENCY-01 is byte-unchanged: the ETH/claimablePool debit two-liner is byte-identical to 453f8073 (the GameAfkingModule leg-1 byte-diff anchor 709-710 ↔ 663-664); the 357-00 changes are BURNIE-only (drainAffiliateBase) + revert-only (D-11/D-12) → no debit change"
    - "RNG-freeze is attested intact (no in-window SLOAD a player can manipulate between rng-request and unlock); the affiliate flat-7% deterministic-split PULL non-gameability is re-attested on the corrected anchors: claim() :629, buyer-never-wins :633-634, 75/20/5 split :678-695, drain loop :654 — NO roll/seed/flush, buyer-never-wins (A != sub guaranteed + the rare U1/U2==sub cycle skip)"
    - "The open-end two-path/no-double-open + the LIVE-01 openBoxes valve + the GAS-06 gap/jackpot decouple + the shared-DegenerusQuests-core non-perturbation are re-attested; the 5cb707f2 advance-gate hunk (D-04) is attributed as a v56 surface (now provably-backed post-hardening)"
    - "The F-356-01 stub + the D-11/D-12/D-13 hardening hunks (the 357-00 changes) are attributed as v56-work-item hunks (RESOLVED-AT-357), NOT orphan hunks; every cited file:line is re-grep-verified at HEAD'"
    - "The NON-WIDENING regression is attested from the reconciled test/REGRESSION-BASELINE-v56.md at HEAD' (live − union == ∅ BY NAME); git diff HEAD' HEAD -- contracts/ is empty (read-only; subject frozen at HEAD')"
  artifacts:
    - path: ".planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-01-DELTA-AUDIT.md"
      provides: "The AUDIT-01 delta-audit log — per-surface NON-WIDENING table + Composition Attestation Matrix (SOLVENCY-01 byte-unchanged + RNG-freeze + affiliate PULL non-gameability + open two-path + LIVE-01 + GAS-06 + shared-quest non-perturbation + the 357-00 hardening attribution + the 5cb707f2 attribution) + regression-baseline attestation"
      contains: "NON-WIDENING"
  key_links:
    - from: "357-01-DELTA-AUDIT.md"
      to: "git diff 453f8073 HEAD' -- contracts/"
      via: "per-file delta enumeration mapped to v56 work items + the 357-00 hardening + the 5cb707f2 hunk"
      pattern: "NON-WIDENING"
    - from: "357-01-DELTA-AUDIT.md"
      to: "test/REGRESSION-BASELINE-v56.md (reconciled at HEAD')"
      via: "the live − union == ∅ NON-WIDENING subset attestation BY NAME"
      pattern: "NON-WIDENING|453f8073"
---

<objective>
Author the AUDIT-01 DELTA AUDIT (the SC1 delta-audit half): enumerate every contract surface changed by the v56.0 subject relative to the v55 baseline `453f8073` (the ~14-file v56 IMPL+GAS delta PLUS the 357-00 hardening hunks), attest each NON-WIDENING with zero orphan hunks, map every `contracts/` hunk to exactly one v56 work item, AND re-attest the structural spine — SOLVENCY-01 byte-unchanged (the ETH/claimablePool debit untouched), RNG-freeze intact, the affiliate flat-7% deterministic-split-PULL non-gameability (NO roll/seed/flush, buyer-never-wins), the open-end two-path/no-double-open + the LIVE-01 `openBoxes` valve + the GAS-06 gap/jackpot decouple, and the shared-`DegenerusQuests`-core non-perturbation. Attribute the `5cb707f2` advance-gate hunk (D-04) AND the 357-00 F-356-01 stub + D-11/D-12/D-13 gates (the in-scope contract changes). Read-only — produces ONE markdown log (`357-01-DELTA-AUDIT.md`), ZERO `contracts/*.sol` edits. The subject is FROZEN at HEAD'.

Purpose: the SC1 delta-audit half of the FULL-CLOSE terminal phase (AUDIT-01). Mirrors the v55 Phase 352 `352-01-DELTA-AUDIT.md` §-structure (the 5th repetition) so the findings deliverable (357-03) folds this log into §3/§5. Runs ∥ 357-02 (the adversarial sweep) — both READ-ONLY @ HEAD'.
Output: `357-01-DELTA-AUDIT.md` — a per-surface NON-WIDENING disposition table + a Composition Attestation Matrix + the regression-baseline attestation (`live − union == ∅` BY NAME at HEAD', cite the reconciled ledger).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@audit/FINDINGS-v55.0.md
@.planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-CONTEXT.md
@.planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-PATTERNS.md
@.planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-00-SUMMARY.md
@test/REGRESSION-BASELINE-v56.md

<interfaces>
<!-- The frozen audit subject is HEAD' (the 357-00 commit; read the SHA from 357-00-SUMMARY.md). The v55 baseline is 453f8073. Executor reads the subject via git — re-grep every cite at HEAD'. -->
- Audit subject HEAD' = the 357-00 .sol commit (the F-356-01 stub + D-11/D-12/D-13 gates), the post-fix HEAD. The v55 baseline = the v55.0 closure-frozen subject `453f8073` (closure signal MILESTONE_V55_AT_HEAD_ca3bbd3220de763298ef2e742111f6e6ef90d583). `git diff HEAD' HEAD -- contracts/` MUST be empty (this plan is read-only; subject frozen at HEAD').
- The delta-surface command: `git diff --stat 453f8073 HEAD' -- contracts/` then `git diff 453f8073 HEAD' -- contracts/`. The v56 contract delta (the v56 IMPL+GAS landing, VERIFIED at HEAD pre-357-00, +1453/−733, 14 files): ContractAddresses.sol, DegenerusAffiliate.sol (+108), DegenerusGame.sol (+92, NOW ALSO carries the 357-00 drainAffiliateBase stub), DegenerusQuests.sol (+477), DegenerusVault.sol (+6), StakedDegenerusStonk.sol (+6), interfaces/IDegenerusAffiliate.sol (+8), interfaces/IDegenerusGameModules.sol (+37), interfaces/IDegenerusQuests.sol (+44), modules/DegenerusGameAdvanceModule.sol (+59, carries the 5cb707f2 hunk + GAS-06 decouple + LIVE-01 valve), modules/DegenerusGameLootboxModule.sol (+351), modules/DegenerusGameWhaleModule.sol (+6), modules/GameAfkingModule.sol (+792, NOW ALSO carries the D-11/D-12/D-13 gates), storage/DegenerusGameStorage.sol (+170). ENUMERATE the ACTUAL per-file delta at execution time at HEAD' (re-run `git diff --stat 453f8073 HEAD' -- contracts/`; the 357-00 hardening adds hunks to DegenerusGame.sol + GameAfkingModule.sol on top of the v56 IMPL/GAS delta — reconcile, do not trust counts blindly).
- The v56 work-item families (map every hunk to exactly ONE; canonical = .planning/REQUIREMENTS.md + the ROADMAP Phase 354/355 detail):
  | Work item | Surface(s) | Requirements |
  |-----------|------------|--------------|
  | per-sub accumulator re-pack (affiliateBase/questProgress/pendingBurnie/hasEverSubscribed/validThroughLevel; amount→milli-ETH; window/settled markers dropped) | storage/DegenerusGameStorage.sol | AGG-05 / GAS-02 |
  | mode-agnostic aggregator accrue + inline _settleQuest (rides the STAGE) + claimQuest fallback + unsub-settle + first-sub head-start | modules/GameAfkingModule.sol | AGG-01..05 / QST-01/02/03 |
  | DegenerusQuests batched-settle entrypoint (non-perturbing) + O1/QST-05 single-credit fix | DegenerusQuests.sol, interfaces/IDegenerusQuests.sol | QST-01..05 |
  | DegenerusAffiliate flat-7% deterministic-split PULL — claim(subs[]) 75/20/5 + CEI + pendingClaim; no roll/seed | DegenerusAffiliate.sol, interfaces/IDegenerusAffiliate.sol | AFF-01/02 / AGG-01/04/05 |
  | ticket minimal-write primitive + buyerOwedBurnie 10%/20% accrual (folded into pendingBurnie) + century parity + open-end re-verification | modules/DegenerusGameLootboxModule.sol, modules/DegenerusGameMintModule.sol (if touched) | TKT-01/02 / OPEN-01/02 |
  | GAS-05 deferred pendingBurnie payout + weighted SUB_STAGE budget + OPEN_BATCH→130 + single-roll open | modules/GameAfkingModule.sol, modules/DegenerusGameAdvanceModule.sol | GAS-01..05 |
  | LIVE-01 openBoxes unified valve (86a2d6c8) + GAS-06 gap/jackpot decouple (3d969621) | modules/DegenerusGameAdvanceModule.sol, modules/DegenerusGameLootboxModule.sol | LIVE-01 / GAS-06 |
  | 5cb707f2 advance-gate active-sub mustMintToday bypass (D-04 — now provably-backed post-hardening) | modules/DegenerusGameAdvanceModule.sol (_enforceDailyMintGate :1124) | (v56 surface; D-04) |
  | 357-00 F-356-01 fix + D-11/D-12/D-13 hardening (RESOLVED-AT-357 — in audit scope) | DegenerusGame.sol (drainAffiliateBase stub), modules/GameAfkingModule.sol (NoPass + MustPurchaseToBeginAfking gates + VAULT/SDGNRS exemption) | (F-356-01 RESOLVED; D-11/D-12/D-13 = SEC-01 spine) |
  | dispatch/interface wiring (whale module, contract addresses, game wiring) | modules/DegenerusGameWhaleModule.sol, ContractAddresses.sol, DegenerusGame.sol, interfaces/IDegenerusGameModules.sol, DegenerusVault.sol, StakedDegenerusStonk.sol | (wiring — behavior-attributed to the owning item) |
- The structural spine to re-attest (each cross-ref'd to its 356 proof — the empirical proofs are GREEN; the delta-audit re-attests them STRUCTURALLY at HEAD'):
  - SOLVENCY-01 byte-unchanged (SEC-02): the GameAfkingModule ETH/claimablePool debit two-liner is byte-identical to 453f8073 — the leg-1 byte-diff anchor (453f8073:709-710 ↔ HEAD:663-664, anchored in REGRESSION-BASELINE-v56.md §7a). The 357-00 changes are BURNIE-only (drainAffiliateBase) + revert-only (D-11/D-12) → the debit is STILL byte-unchanged at HEAD'. Re-grep + re-confirm. Proof: V56FreezeSolvency 7/7 (356-04).
  - RNG-freeze intact (SEC-02): no in-window SLOAD a player can manipulate between rng-request and unlock (the v45 north-star). The accrue/settle touches no frozen RNG-window slot; the open-end materialization keeps the live-level parity + stamped-seed determinism the v55 close proved. Proof: V56FreezeSolvency RNG-freeze determinism fuzz (356-04).
  - The affiliate flat-7% deterministic-split PULL non-gameability (AFF-01/02): re-anchor on the CORRECTED DegenerusAffiliate.sol lines (the pattern map DRIFT NOTE — CONTEXT cited :579 which DRIFTED): claim() entry @ :629, buyer-never-wins comment @ :633-634 ("self-referral resolves to VAULT, so the 75% leg never skips to a buyer"), the 75/20/5 split arithmetic @ :678-695, the per-sub drain loop @ :654 (`uint256 b = afkingDrain.drainAffiliateBase(sub)`). NO roll, NO seed, NO scheduled/mutation flush — exactly ONE deterministic distribution path → no favorable-seed selection AND no two-distribution free option. Buyer never receives the base (A != sub guaranteed + the rare U1/U2==sub cycle skip). Proof: V56SecUnmanipulable 11/11 churn-fuzz (356-03).
  - The open-end two-path/no-double-open (OPEN-02 / LIVE-01): the afking open vs human openLootBox coexist; lastOpenedDay monotone no-double-open; no EV-cap double-draw on the shared (player,level) budget; LIVE-01 openBoxes valve (afking-first then human, both cursors drain, drainAfkingBoxes selector isolation, individual/mintBurnie open byte-unchanged). Proof: V56AfkingGasMarginal LIVE-01 cases (356-06, test/gas/V56AfkingGasMarginal.t.sol).
  - The GAS-06 gap/jackpot decouple: each advanceGame tx < 16,777,216 under a multi-day VRF-stall resume (gap-backfill advance N ~6.85M + deferred-jackpot N+1 SEPARATE tx); the idempotent-resume invariants (D-07). Proof: V56AfkingGasMarginal gap-resume (356-06).
  - The shared-DegenerusQuests-core non-perturbation (QST-04): the batched-settle entrypoint is non-perturbing to manual/bingo/degenerette/boon callers (afkingActive gates the streak bump; byte-identity with afking siblings present vs absent; O1/QST-05 single-credit). Proof: V56QuestNonPerturb 7/7 (356-05).
  - The 5cb707f2 advance-gate hunk (D-04): attribute the active-AfKing-sub mustMintToday bypass (DegenerusGameAdvanceModule._enforceDailyMintGate :1124) as a v56 surface — already folded into the NON-WIDENING ledger via the 356-07 HEAD reconciliation (commit d6badcb3). POST-HARDENING it is now SOUND: the D-11/D-12 gates guarantee every active sub is a pass-holding purchase-grounded participant, so the bypass is no longer claimable by an unfunded free-rider. Attest the bypass gives no advance-timing / daily-mint-gate edge given the hardened sub invariant; the D-13 VAULT/sDGNRS exemption doesn't reopen one (they advance via their own existing paths).
  - The 357-00 hardening (RESOLVED-AT-357): the F-356-01 drainAffiliateBase stub RESOLVES the carried finding (the fix + the 357-00b re-verification); the D-11/D-12/D-13 gates are the SEC-01 strategic-sub/unsub spine. These are IN audit scope (D-01) — attribute them as v56-work-item hunks, NOT orphan hunks.
- The regression ledger (NON-WIDENING gate — AUTHORITATIVE, reconciled at HEAD' by 357-00b): test/REGRESSION-BASELINE-v56.md. NON-WIDENING = `live − union == ∅` BY NAME (the live failing-NAME set minus the empirical 453f8073 baseline union is EMPTY). The baseline union was established empirically via the byte-identical-contracts commit 83a6a9ca. CITE the reconciled ledger at HEAD' (357-00b re-ran it post-357-00); do NOT re-run forge here unless the ledger is stale — the ledger is authoritative.
</interfaces>
</context>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan introduces NO new code or attack surface — it is a read-only delta audit producing one markdown log. The audit SUBJECT's security is audited, NOT modified: the v56.0 contract surfaces (the aggregator + ticket-mode primitive + open-end + DegenerusQuests batched-settle + the 357-00 hardening), frozen at HEAD'. ZERO contracts/ edits. The adversarial probing is 357-02; this plan is the structural delta + spine re-attestation. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-357-01-RO | Tampering | this plan's deliverable | accept | Read-only analysis; the executor never edits contracts/*.sol — verified by git diff HEAD' HEAD -- contracts/ empty. No new attack surface. |
| T-357-01-WIDEN | Elevation | the v56 delta surfaces vs v55 (the aggregator + ticket primitive + open-end + the 357-00 hardening) | mitigate | The audit IS the mitigation: every changed surface (~14 files + the 357-00 hunks) is attested NON-WIDENING with grep/diff evidence; each hunk maps to exactly one v56 work item; regression NON-WIDENING is `live − union == ∅` BY NAME at HEAD'. |
| T-357-01-SOLVENCY | Tampering | the SOLVENCY-01 ETH/claimablePool debit (the solvency-spine anchor) | mitigate | SOLVENCY-01 is re-attested byte-unchanged at HEAD' (the leg-1 byte-diff anchor 709-710 ↔ 663-664); the 357-00 changes are BURNIE-only + revert-only → no debit change. |
| T-357-01-AFFGAME | Elevation | the affiliate flat-7% deterministic-split PULL non-gameability | mitigate | Re-attested on the CORRECTED anchors (claim() :629 / buyer-never-wins :633-634 / 75/20/5 :678-695 / drain loop :654): NO roll/seed/flush, exactly ONE deterministic path, buyer-never-wins (A != sub + the U1/U2==sub cycle skip) → no settle-timing profit. The F-356-01 stub now makes claim() REACHABLE (the fix), the CEI unchanged. |
| T-357-01-BYPASS | Elevation | the 5cb707f2 advance-gate active-sub bypass (D-04) | mitigate | Attributed as a v56 surface; POST-HARDENING re-attested SOUND — the D-11/D-12 gates make every active sub a pass-holding purchase-grounded participant, so the bypass gives no free-rider edge; the D-13 exemption doesn't reopen one. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate the v56 delta surface (453f8073 → HEAD'), attest each NON-WIDENING, build the Composition Attestation Matrix (SOLVENCY-01 + RNG-freeze + affiliate PULL non-gameability + open two-path + LIVE-01 + GAS-06 + shared-quest non-perturbation + the 357-00 hardening + the 5cb707f2 attribution)</name>
  <read_first>
    - audit/FINDINGS-v55.0.md (the §3.A Delta-Surface Table + §3.B Composition Attestation Matrix format to mirror — read the §3 region ~`:212`/`:243`)
    - .planning/milestones/v55.0-phases/352-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/352-01-DELTA-AUDIT.md (the directly-reusable delta-audit log shape to mirror — the per-surface NON-WIDENING table + the Composition Attestation Matrix structure)
    - .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-00-SUMMARY.md (HEAD' — the subject SHA)
    - .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-PATTERNS.md (the CORRECTED DegenerusAffiliate.sol anchors — the :579→:629/:654/:678-695/:633-634 DRIFT NOTE; the level()-auto-getter note)
    - .planning/REQUIREMENTS.md (the 27-row traceability table — each delta hunk maps to a v56 REQ; AGG/TKT/AFF/QST/OPEN/GAS/SEC/LIVE/XMODEL/AUDIT)
    - the frozen subject via git show HEAD':contracts/<file>, git diff 453f8073 HEAD' -- contracts/<file>, and git diff --stat 453f8073 HEAD' -- contracts/ for the actual per-file delta
  </read_first>
  <files>.planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-01-DELTA-AUDIT.md</files>
  <action>
    Run `git diff --stat 453f8073 HEAD' -- contracts/` to ENUMERATE the actual changed-file set (reconcile against the interfaces 14-file list + the 357-00 hardening hunks on DegenerusGame.sol + GameAfkingModule.sol — re-derive, do not trust blindly), then `git diff 453f8073 HEAD' -- contracts/` for the full delta. Build a per-surface NON-WIDENING disposition table grouped by the v56 work-item families (the interfaces table). For each changed file, attest NON-WIDENING with concrete grep/diff evidence drawn from the frozen subject HEAD' (read via git show / git diff / grep). Mirror the v55 §3.A columns: Surface (file) | Requirements | Re-grepped anchors @ HEAD' | Disposition (NON-WIDENING). Specifically attest:
    - storage/DegenerusGameStorage.sol (AGG-05/GAS-02): the re-packed Sub slot (affiliateBase/questProgress/pendingBurnie/hasEverSubscribed/validThroughLevel; amount→milli-ETH; window/settled markers dropped) — layout-safe (pre-launch redeploy-fresh).
    - modules/GameAfkingModule.sol (AGG-01..05/QST-01/02/03/GAS-05 + the 357-00 D-11/D-12/D-13 gates): the mode-agnostic accrue + inline _settleQuest + claimQuest fallback + unsub-settle + first-sub head-start + the pendingBurnie deferred payout; PLUS the 357-00 NoPass + MustPurchaseToBeginAfking gates + the VAULT/SDGNRS exemption — attribute the 357-00 hunks as RESOLVED-AT-357 / SEC-01-spine, NOT orphan.
    - DegenerusQuests.sol + interfaces/IDegenerusQuests.sol (QST-01..05): the batched-settle entrypoint + the O1/QST-05 single-credit fix; non-perturbing.
    - DegenerusAffiliate.sol + interfaces/IDegenerusAffiliate.sol (AFF-01/02): the flat-7% deterministic-split PULL claim(subs[]) 75/20/5 + CEI + pendingClaim; re-anchor on the CORRECTED lines (:629/:633-634/:654/:678-695).
    - modules/DegenerusGameLootboxModule.sol (TKT-01/02/OPEN-01/02/LIVE-01): the ticket minimal-write primitive + buyerOwedBurnie accrual (folded into pendingBurnie) + the open-end + the openBoxes valve leg.
    - modules/DegenerusGameAdvanceModule.sol (GAS-05/LIVE-01/GAS-06 + the 5cb707f2 hunk D-04): the weighted SUB_STAGE budget + the openBoxes valve + the gap/jackpot decouple + the active-sub mustMintToday bypass.
    - DegenerusGame.sol (the 357-00 drainAffiliateBase stub + wiring): attribute the F-356-01 stub as RESOLVED-AT-357.
    - modules/DegenerusGameWhaleModule.sol + ContractAddresses.sol + DegenerusVault.sol + StakedDegenerusStonk.sol + interfaces/IDegenerusGameModules.sol: wiring — behavior-attributed to the owning item, NON-WIDENING.
    Then build the Composition Attestation Matrix (mirror v55 §3.B): (1) every contracts/ delta hunk maps to exactly ONE v56 work item (no orphan hunks) — explicitly attribute the 5cb707f2 advance-gate hunk (D-04, via the 356-07 d6badcb3 reconciliation) AND the 357-00 F-356-01 stub + D-11/D-12/D-13 gates (RESOLVED-AT-357 / SEC-01 spine). (2) SOLVENCY-01 byte-unchanged (the leg-1 byte-diff anchor 709-710 ↔ 663-664 byte-identical; the 357-00 changes BURNIE-only + revert-only), cross-ref V56FreezeSolvency 7/7. (3) RNG-freeze intact (no in-window manipulable SLOAD; the v45 north-star), cross-ref V56FreezeSolvency RNG-freeze fuzz. (4) the affiliate flat-7% deterministic-split-PULL non-gameability (the corrected anchors; NO roll/seed/flush; buyer-never-wins), cross-ref V56SecUnmanipulable 11/11. (5) the open-end two-path/no-double-open + LIVE-01 openBoxes valve + GAS-06 gap/jackpot decouple, cross-ref V56AfkingGasMarginal LIVE-01 + gap-resume. (6) the shared-DegenerusQuests-core non-perturbation + O1/QST-05 single-credit, cross-ref V56QuestNonPerturb 7/7. (7) the 5cb707f2 advance-gate bypass POST-HARDENING SOUND (every active sub pass-holding + purchase-grounded → no free-rider edge; D-13 exemption doesn't reopen one). DO NOT edit any contracts/*.sol — read-only via git show / git diff / grep. Every cited file:line re-grep-verified at HEAD'.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && HEADP=$(grep -oE "HEAD'? *= *[0-9a-f]{7,40}|[0-9a-f]{40}" .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-00-SUMMARY.md | grep -oE "[0-9a-f]{7,40}" | head -1); git diff --quiet "$HEADP" HEAD -- contracts/ && grep -q NON-WIDENING .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-01-DELTA-AUDIT.md && grep -q SOLVENCY-01 .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-01-DELTA-AUDIT.md && grep -qE "RNG-freeze|RNG_FREEZE" .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-01-DELTA-AUDIT.md && grep -q "5cb707f2" .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-01-DELTA-AUDIT.md && grep -q "F-356-01" .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-01-DELTA-AUDIT.md && grep -qE "LIVE-01|GAS-06" .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-01-DELTA-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - 357-01-DELTA-AUDIT.md enumerates all changed contract files (re-derived from git diff --stat 453f8073 HEAD' -- contracts/), each carrying a NON-WIDENING verdict backed by a concrete grep/diff anchor @ HEAD', mapped to its owning v56 work item(s).
    - Each delta hunk maps to exactly one v56 work item (the composition matrix attests no orphan hunks); the 5cb707f2 advance-gate hunk (D-04) AND the 357-00 F-356-01 stub + D-11/D-12/D-13 gates are attributed (RESOLVED-AT-357 / SEC-01 spine).
    - SOLVENCY-01 is attested byte-unchanged at HEAD' (the leg-1 byte-diff anchor 709-710 ↔ 663-664; the 357-00 changes BURNIE-only + revert-only); RNG-freeze attested intact.
    - The affiliate flat-7% deterministic-split-PULL non-gameability is re-attested on the CORRECTED anchors (:629/:633-634/:654/:678-695; NO roll/seed/flush; buyer-never-wins).
    - The open-end two-path/no-double-open + LIVE-01 openBoxes valve + GAS-06 gap/jackpot decouple + the shared-DegenerusQuests-core non-perturbation are each re-attested, cross-ref'd to their 356 proofs.
    - The 5cb707f2 bypass is re-attested SOUND post-hardening (every active sub pass-holding + purchase-grounded).
    - git diff HEAD' HEAD -- contracts/ is empty (zero contract mutation; read-only).
  </acceptance_criteria>
  <done>All changed surfaces (the v56 delta + the 357-00 hardening) attested NON-WIDENING with grep/diff evidence @ HEAD'; the delta-surface table + the Composition Attestation Matrix (SOLVENCY-01 byte-unchanged + RNG-freeze + affiliate PULL non-gameability + open two-path + LIVE-01 + GAS-06 + shared-quest non-perturbation + the 5cb707f2 + the 357-00 hardening attribution) are written; contracts/ byte-identical to HEAD'.</done>
</task>

<task type="auto">
  <name>Task 2: Regression-baseline attestation (live − union == ∅ BY NAME at HEAD') + the 357-00b reconciliation attribution</name>
  <read_first>
    - test/REGRESSION-BASELINE-v56.md (the AUTHORITATIVE ledger reconciled at HEAD' by 357-00b — the live − union == ∅ subset proof, the 83a6a9ca empirical-baseline method, the 14 migration-unmasked drops, the D-11/D-12 supersession drops + the V56SubHardening re-proofs, the SOLVENCY-01 leg-1 byte anchor §7a; cite, do NOT re-derive)
    - .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-00b-SUMMARY.md (the 357-00b reconciliation outcome — the drops + the new V56SubHardening proofs)
    - audit/FINDINGS-v55.0.md §5 (the LEAN Regression Appendix format to mirror — read the §5 region ~`:586`)
    - .planning/milestones/v55.0-phases/352-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/352-01-DELTA-AUDIT.md (the Regression-Baseline Attestation section shape to mirror — the subset-by-NAME framing)
    - .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-01-DELTA-AUDIT.md (the file being appended to)
  </read_first>
  <files>.planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-01-DELTA-AUDIT.md</files>
  <action>
    Append a Regression-Baseline Attestation section to 357-01-DELTA-AUDIT.md mirroring v55 §5. Attest the v56 baseline NON-WIDENING BY NAME at HEAD' as a strict subset: cite test/REGRESSION-BASELINE-v56.md (reconciled at HEAD' by 357-00b) as authoritative — the live failing-NAME set minus the empirical 453f8073 baseline union is EMPTY (`live − union == ∅`). State the binding gate explicitly as a SUBSET relation (live ⊆ union, BY NAME), NOT a count delta. Record that the 453f8073 baseline union was established EMPIRICALLY via the byte-identical-contracts commit 83a6a9ca (the raw 453f8073 corpus is UNCOMPILABLE — AfKing.sol deleted but DeployProtocol+5 files still deploy it). Then attribute the test-surface churn via the ledger (NOT counted as regression): the 14 migration-unmasked v56-behavior reds (vm.skip-dropped at 356-07 f23b010e, each re-proven GREEN by V56Sec*/V56AfkingGasMarginal) + the D-10 offset migration + the NEW 357-00b reconciliation (the D-11/D-12 supersession reds vm.skip-dropped, naming NoPass/MustPurchaseToBeginAfking, each re-proven GREEN by V56SubHardening; the F-356-01 narrowing). State the SWEEP NON-WIDENING claim: every git diff 453f8073 HEAD' -- contracts/ test/ hunk is attributable to a known v56-scope commit (the 354 IMPL e18af451 + the 355 GAS net diff + the liveness adds 86a2d6c8/3d969621 + the 5cb707f2 advance-gate + the AGENT-committed 356 TST work + the 357-00 hardening + the 357-00b reconciliation). Re-confirm the SOLVENCY-01 leg-1 byte anchor (709-710 ↔ 663-664 byte-identical) holds at HEAD'. Read-only — no contracts/ or test/ edits.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -qiE "live . union|live-union|subset|⊆" .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-01-DELTA-AUDIT.md && grep -q "453f8073" .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-01-DELTA-AUDIT.md && grep -q "V56SubHardening" .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-01-DELTA-AUDIT.md && grep -q "83a6a9ca" .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-01-DELTA-AUDIT.md && HEADP=$(grep -oE "[0-9a-f]{40}" .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-00-SUMMARY.md | head -1); git diff --quiet "$HEADP" HEAD -- contracts/</automated>
  </verify>
  <acceptance_criteria>
    - The Regression-Baseline Attestation records NON-WIDENING BY NAME with the live failing-NAME set a strict SUBSET of the empirical 453f8073 union (`live − union == ∅`, a subset relation not a count) cited from the reconciled test/REGRESSION-BASELINE-v56.md.
    - The empirical-baseline method (83a6a9ca byte-identical-contracts) is recorded; the 14 migration drops + the D-10 offset migration + the NEW 357-00b D-11/D-12 supersession drops (naming NoPass/MustPurchaseToBeginAfking, re-proven by V56SubHardening) + the F-356-01 narrowing are attributed via the ledger, NOT counted as regression.
    - The SWEEP NON-WIDENING attestation states every changed test/source hunk vs 453f8073 is attributable to a known v56-scope commit (incl. the 357-00 hardening + 357-00b reconciliation).
    - The SOLVENCY-01 leg-1 byte anchor is re-confirmed at HEAD'.
    - The frozen-subject check (HEAD' to HEAD) stays empty.
  </acceptance_criteria>
  <done>The regression baseline is attested NON-WIDENING BY NAME (live − union == ∅) at HEAD' with the ledger cited; the 357-00b reconciliation (the D-11/D-12 drops + the V56SubHardening re-proofs + the F-356-01 narrowing) is attributed; the SWEEP NON-WIDENING claim recorded — appended to 357-01-DELTA-AUDIT.md; the frozen subject is byte-identical to HEAD'.</done>
</task>

</tasks>

<verification>
- The frozen-subject check (HEAD' to HEAD) is empty (zero source mutation throughout).
- 357-01-DELTA-AUDIT.md contains the per-surface NON-WIDENING table (the v56 delta + the 357-00 hardening) + the Composition Attestation Matrix (SOLVENCY-01 byte-unchanged + RNG-freeze + affiliate PULL non-gameability + open two-path + LIVE-01 + GAS-06 + shared-quest non-perturbation + the 5cb707f2 + the 357-00 hardening attribution) + the Regression-Baseline Attestation (live − union == ∅ BY NAME at HEAD').
- Every v56 delta hunk maps to exactly one v56 work item (no orphan hunks); the corrected DegenerusAffiliate anchors are used (no stale :579 / :558 citation).
</verification>

<success_criteria>
The SC1 delta-audit half of AUDIT-01 satisfied: every source surface changed vs the v55 baseline 453f8073 is enumerated and attested NON-WIDENING with each hunk mapped to exactly one v56 work item; SOLVENCY-01 byte-unchanged + RNG-freeze intact + the affiliate flat-7% deterministic-split-PULL non-gameability + the open two-path/no-double-open + LIVE-01 + GAS-06 + the shared-quest non-perturbation are re-attested; the 5cb707f2 advance-gate (D-04) + the 357-00 F-356-01 stub + D-11/D-12/D-13 hardening are attributed; the regression baseline is attested NON-WIDENING BY NAME at HEAD'; the frozen subject is byte-identical to HEAD'.
</success_criteria>

<output>
Create `.planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-01-SUMMARY.md` when done
</output>
