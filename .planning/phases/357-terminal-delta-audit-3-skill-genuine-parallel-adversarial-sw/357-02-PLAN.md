---
phase: 357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw
plan: 02
type: execute
wave: 2
depends_on: ["357-00", "357-00b"]
files_modified:
  - .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-02-ADVERSARIAL-LOG.md
autonomous: true
requirements: [AUDIT-01]

must_haves:
  truths:
    - "The 3-skill sweep runs the FIXED set /contract-auditor + /economic-analyst + /zero-day-hunter (/degen-skeptic OUT as a probing skill per D-271-ADVERSARIAL-02 — used ONLY as the elevation dual-gate filter)"
    - "The sweep runs as GENUINE PARALLEL_SUBAGENT — launched INLINE from the ORCHESTRATOR context (which holds the Task tool) as 3 concurrent background Task spawns, NOT nested inside a gsd-executor (which lacks the Task tool → forces the SEQUENTIAL fallback); the actual execution path used (PARALLEL_SUBAGENT / HYBRID / SEQUENTIAL_MAIN_CONTEXT) is recorded in §A"
    - "Every charged probe is recorded across the v56 surfaces: the strategic sub/unsub edge (PRIMARY); the settle-timing non-exploitability (trivial — no seed); pre-credit-EV inflation; the two-path open; the D-11/D-12 hardened gates + the now-sound 5cb707f2 bypass (D-08b — confirm the passless cap-occupancy + unfunded free-rider vectors are CLOSED); the new drainAffiliateBase stub reachability + the affiliate claim CEI"
    - "The best-effort XMODEL (Codex + Gemini) close AUGMENTS the Claude sweep — its dispositions fold into the log; a CLI being unavailable does NOT block (record attempted/partial per D-03)"
    - "Every elevation passes the /degen-skeptic dual-gate (structural-protection lens + 3-condition EV lens) BEFORE being recorded as a FINDING_CANDIDATE; each disposition row carries NEGATIVE-VERIFIED / SAFE_BY_DESIGN / FINDING_CANDIDATE + the skeptic outcome + the probe tier"
    - "Each subagent probes the FROZEN subject via git show HEAD':contracts/... (READ-ONLY, NOT from memory); every cited file:line re-grep-verified at HEAD'; git diff HEAD' HEAD -- contracts/ is empty (read-only sweep)"
  artifacts:
    - path: ".planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-02-ADVERSARIAL-LOG.md"
      provides: "The AUDIT-01 adversarial disposition log — §A CHARGE (3-skill set + subject SHA HEAD' + execution path + the XMODEL augmentation note) / §B raw per-skill output / §C per-probe disposition table / §D skeptic dual-gate attestation / §E read-only invariant"
      contains: "FINDING_CANDIDATE"
  key_links:
    - from: "357-02-ADVERSARIAL-LOG.md"
      to: "the 3 skill SKILL.md personas at $HOME/.claude/skills/ + the Codex + Gemini CLIs"
      via: "per-skill + per-model charged-probe disposition against the frozen subject HEAD'"
      pattern: "NEGATIVE-VERIFIED|SAFE_BY_DESIGN|FINDING_CANDIDATE"
---

<objective>
Author the AUDIT-01 ADVERSARIAL SWEEP (the SC1 adversarial-sweep half): run the fixed 3-skill pass (`/contract-auditor` + `/economic-analyst` + `/zero-day-hunter`) as GENUINE concurrent background Task spawns against the frozen v56.0 audit subject (HEAD', the 357-00 commit), charged with the v56 surfaces (the strategic sub/unsub edge PRIMARY + settle-timing non-exploitability + pre-credit-EV inflation + the two-path open + the D-11/D-12 hardened gates + the now-sound 5cb707f2 bypass + the new drainAffiliateBase stub reachability + the affiliate claim CEI), AUGMENTED by the best-effort XMODEL (Codex + Gemini) close, and record every disposition in `357-02-ADVERSARIAL-LOG.md` mirroring the v55 Phase 352 `352-02-ADVERSARIAL-LOG.md` §A/§B/§C/§D/§E structure. Every elevation passes the `/degen-skeptic` dual-gate BEFORE being recorded. Read-only — ZERO `contracts/*.sol` edits.

Purpose: the SC1 adversarial-sweep half of the FULL-CLOSE terminal phase (AUDIT-01). The log feeds §4 of the findings deliverable (357-03). The genuinely-new v56 attack surface the sweep spends deepest effort on: the strategic sub/unsub edge (PRIMARY — the milestone's SEC-01 spine), the D-11/D-12 hardened gates + the now-sound 5cb707f2 bypass, and the new drainAffiliateBase stub reachability + the affiliate claim CEI. Runs ∥ 357-01 (the delta-audit) — both READ-ONLY @ HEAD'.
Output: `357-02-ADVERSARIAL-LOG.md` — §A CHARGE + §B raw per-skill output + §C per-probe disposition table + §D skeptic dual-gate attestation + §E read-only invariant.

## EXECUTION NOTE — genuine-parallel topology (D-06, the hard-won 314/324/328/333/352 lesson — LOAD-BEARING)
The 3-skill sweep MUST run GENUINE PARALLEL_SUBAGENT — all 3 skills as concurrent background Task spawns launched INLINE FROM THE ORCHESTRATOR CONTEXT (which holds the Task tool). It MUST NOT be nested inside a `gsd-executor` (the executor lacks the Task tool → forces the SEQUENTIAL fallback, functional + persona-fidelity-preserved via the dedicated SKILL.md files, but slower). During execute-phase the ORCHESTRATOR runs this plan INLINE in the main context (mirror how 352-02 was handled) rather than delegating it to a single executor subagent. If the running context lacks the Task tool, fall back to SEQUENTIAL_MAIN_CONTEXT with per-skill persona fidelity via the SKILL.md files. RECORD which path was actually used in §A. (The skills live at `$HOME/.claude/skills/...`, NOT `.claude/skills/` — confirmed pitfall: `.claude/skills/` does not exist in this repo.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@audit/FINDINGS-v55.0.md
@.planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-CONTEXT.md
@.planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-PATTERNS.md
@.planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-00-SUMMARY.md
@$HOME/.claude/skills/contract-auditor/SKILL.md
@$HOME/.claude/skills/economic-analyst/SKILL.md
@$HOME/.claude/skills/zero-day-hunter/SKILL.md
@$HOME/.claude/skills/degen-skeptic/SKILL.md

<interfaces>
<!-- The frozen audit subject is HEAD' (the 357-00 commit; read the SHA from 357-00-SUMMARY.md). The v55 baseline is 453f8073. Each skill probes HEAD' via git show; every cite re-grep-verified at HEAD'. -->
- Audit subject HEAD' = the 357-00 .sol commit (the F-356-01 stub + D-11/D-12/D-13 gates), the post-fix HEAD. Baseline: v55 HEAD `453f8073`. Each skill probes the FROZEN subject via `git show HEAD':contracts/...` (READ-ONLY, NOT from memory); every cited file:line re-grep-verified against HEAD'. `git diff HEAD' HEAD -- contracts/` MUST be empty.
- The 3-skill set is FIXED: /contract-auditor + /economic-analyst + /zero-day-hunter. /degen-skeptic is OUT as a probing skill (carried decision D-271-ADVERSARIAL-02, held v44..v55) — used ONLY as the elevation dual-gate filter (the skeptic FUNCTION), NOT a 4th hunt.
- The CHARGE (the v56-novel surfaces; weighted by novelty, NOT a uniform re-sweep). Probe targets from ROADMAP Phase 357 SC1 + CONTEXT D-06/D-08b:
  - **(a) STRATEGIC SUB/UNSUB EDGE (PRIMARY — /economic-analyst + /zero-day-hunter lead, the SEC-01 spine).** A churn loop (subscribe → accrue → unsub → re-subscribe) MUST NOT extract more affiliate/quest value than a steady sub. The accrued `affiliateBase` is the uplines' money that PERSISTS on unsub (forfeit-nothing-gain-nothing); the quest `pendingBurnie` is the sub's own per-delivered-day balance (no settle-timing/claim-timing EV); the self-marking running balances make double-settle impossible (re-claim sees affiliateBase==0; double quest flush finds questProgress==0). Probes: can churn re-claim the affiliate / dodge a streak penalty / harvest or duplicate a settlement? Structural defense = the flat-7% deterministic-split PULL (no roll/seed) + the self-marking balances + the D-11/D-12 hardening; PROVEN empirically by V56SecUnmanipulable 11/11 (356-03). The sweep re-attests it ADVERSARIALLY at HEAD'.
  - **(b) SETTLE-TIMING NON-EXPLOITABILITY (trivial — /economic-analyst).** There is NO seed and NO roll under the flat-7% deterministic-split pull (the C1/C2 free-option finding is MOOT — the roll is REMOVED). Probe: confirm there is no favorable-seed selection AND no two-distribution free option (exactly ONE deterministic path). Expected NEGATIVE-VERIFIED.
  - **(c) PRE-CREDIT-EV INFLATION (/economic-analyst + /contract-auditor).** The first-sub-only +daysToNextSettle head-start is bounded +0..+9 once/account (USER-ACCEPTED-BY-DESIGN — the bound replaces the escrow); the activity-score reads the actual streak; the per-window streak advances only on debit-DELIVERED days (the C3-a streak-dodge fix stays in force). Probe: can a sub inflate EV pre-credit beyond the accepted bound? Expected NEGATIVE-VERIFIED / SAFE_BY_DESIGN (the bound is accepted).
  - **(d) TWO-PATH OPEN (/contract-auditor lead).** The afking open (resolveAfkingBox / mintBurnie) vs human openLootBox + the LIVE-01 openBoxes valve. Probes: shared mutable-state hazard / double-open (lastOpenedDay monotone) / EV-cap double-draw on the shared (player,level) budget / openBoxes valve ordering + drainAfkingBoxes selector isolation. Structural defense = OPEN-02 + LIVE-01, PROVEN by V56AfkingGasMarginal LIVE-01 (356-06).
  - **(e) THE D-11/D-12 HARDENED GATES + THE NOW-SOUND 5cb707f2 BYPASS (D-08b — /contract-auditor + /zero-day-hunter).** Probes: can the D-11 pass-gate be forged/flash-bypassed (subscribe reads the live horizon; the :942 crossing re-reads)? Can the D-12 grounding be faked (the immediate cover-buy must be real funded delivery)? Can the D-13 protocol-sub exemption be abused beyond the two hardcoded addresses (VAULT/SDGNRS, keyed on the un-spoofable resolved subscriber identity)? CONFIRM the passless cap-occupancy + unfunded free-rider vectors are CLOSED (subscribe now reverts both). CONFIRM the 5cb707f2 advance-gate bypass is now SOUND (every active sub is a pass-holding purchase-grounded participant → no free-rider edge; the D-13 exemption doesn't reopen one). This materially STRENGTHENS the SEC-01 story.
  - **(f) THE NEW drainAffiliateBase STUB REACHABILITY + THE AFFILIATE claim CEI (/contract-auditor lead, the F-356-01 fix surface).** Probes: is the new Game dispatch stub correctly AFFILIATE-only (the module guard at :1301 runs under delegatecall, msg.sender = affiliate)? Is the affiliate claim() CEI sound (drainAffiliateBase zeroes affiliateBase, recordAmount=0 → no reentrancy edge)? Is there any permissionless-pays-3rd-party hazard (claim mints to A/U1/U2, fine by design)? Expected NEGATIVE-VERIFIED.
- THE BEST-EFFORT XMODEL CLOSE (D-03 — AUGMENTATION, not a hard dependency): feed Codex + Gemini crafted prompts on the FULL afking system (buy STAGE/accrual/settle AND open-pass/materialize) + the F-356-01 fix surface + the D-11/D-12/D-13 hardening. Their dispositions FOLD into §B/§C as an XMODEL augmentation subsection. The Claude 3-skill sweep is the PRIMARY gate. A CLI being unavailable does NOT block closure — record it as attempted/partial in §A/§B (the augmentation is additive assurance). Detect availability via `command -v codex` / `command -v gemini` (or the project's configured CLI names); if unavailable, record "XMODEL attempted — <model> CLI unavailable, partial" and proceed.
- The skeptic dual-gate (MANDATORY — the /degen-skeptic FUNCTION applied to every elevation): (1) structural-protection lens — does a structural mechanism already prevent the elevation? (2) 3-condition EV lens — (a) manifests without an attacker / is positive-EV, (b) magnitude is material, (c) severity survives the re-read. A FINDING_CANDIDATE only if it survives BOTH gates. Applied per-skill self-arm AND orchestrator integration-time re-application. Read $HOME/.claude/skills/degen-skeptic/SKILL.md for the exact lens definitions.
- Disposition vocabulary (mirror v55 §4): NEGATIVE-VERIFIED (probe investigated, no issue) / SAFE_BY_DESIGN (intended, informational) / FINDING_CANDIDATE (survives the skeptic filter → route per the elevation rule). Tier-1 (single-skill) vs Tier-2 (multi-skill consensus) noted.
- Elevation routing (D-07): the working target is the v55-style clean closure where the ONLY new finding is F-356-01 (RESOLVED in-phase at 357-00, NOT deferred). The O1/QST-05 lootbox-quest double-credit is RESOLVED (single-credit proven at 356-05), NOT a finding. A FINDING_CANDIDATE that survives the dual-gate is recorded in the log and surfaced to 357-03/357-04 for USER adjudication at the closure gate (do NOT auto-fix; the subject is FROZEN at HEAD' — any further fix is a NEW contract phase requiring USER approval; default leaning DEFER→v57 with the fix design locked). The terminal NEVER silently halt-and-fixes beyond the locked F-356-01.
- Known Non-Issues to NOT re-flag (honor each skill's list + the MEMORY notes): the affiliate is a flat-7% deterministic-split PULL with NO roll/seed (the C1/C2 free-option is MOOT — do NOT re-raise the removed roll); affiliate/quest are BURNIE flip-credit OFF the ETH/claimablePool path (SOLVENCY-01 untouched); the first-sub +0..+9 head-start is USER-ACCEPTED-BY-DESIGN (the bound replaces the escrow — NOT a missed control); the O1/QST-05 double-credit is RESOLVED (single-credit at 356-05); the operator-approval trust boundary is the consent unit (do NOT model a tricked-into-approving actor); reentrancy is the USER-locked SAFE_BY_DESIGN stance (trusted contracts, CEI'd, [[threat-model-reentrancy-mev-nonissues]]); MEV is LOW/confirmatory-only (gated, timing-independent); the v44 §9d 135-anchor maximalist register is NOT live vectors. The USER-LOCKED weighting: DOMINANT = RNG/freeze + solvency; HIGH = gas-DoS ONLY in the advanceGame chain (16.7M = gg); LOW/confirmatory = access-control + reentrancy + MEV.
- The frozen subject surfaces to read (via git show HEAD':contracts/...): modules/GameAfkingModule.sol (subscribe + the D-11/D-12/D-13 gates + the accrue + _settleQuest + claimQuest + unsub-settle + the crossing eviction + drainAffiliateBase impl), DegenerusAffiliate.sol (claim() :629 + the drain loop :654 + the 75/20/5 split :678-695 + buyer-never-wins :633-634 + withdraw CEI + pendingClaim), DegenerusGame.sol (the new drainAffiliateBase dispatch stub), modules/DegenerusGameAdvanceModule.sol (the 5cb707f2 :1124 bypass + the GAS-06 decouple + the LIVE-01 valve), modules/DegenerusGameLootboxModule.sol (the open-end + the openBoxes valve), DegenerusQuests.sol (the batched-settle entrypoint + the O1/QST-05 single-credit), storage/DegenerusGameStorage.sol (the re-packed Sub slot + pendingBurnie).
</interfaces>
</context>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan introduces NO new code or attack surface — it IS the adversarial security examination of the v56.0 surfaces. The threats under examination are exactly the SC1 charged probe set against the frozen subject HEAD' (the strategic sub/unsub edge + the D-11/D-12 hardened gates + the 5cb707f2 bypass + the two-path open + the drainAffiliateBase stub + the affiliate claim CEI). No contracts/*.sol is edited. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-357-02-CHURN | Tampering | the strategic sub/unsub edge (PRIMARY) | mitigate | /economic-analyst + /zero-day-hunter lead against HEAD'; structural defense = the flat-7% deterministic-split PULL (no roll/seed) + the self-marking running balances (re-claim sees affiliateBase==0; double quest flush finds questProgress==0) + the D-11/D-12 hardening, proven by V56SecUnmanipulable 11/11; any elevation passes the skeptic dual-gate before recording. |
| T-357-02-PASSFORGE | Spoofing | the D-11/D-12 hardened gates (D-08b) | mitigate | /contract-auditor + /zero-day-hunter probe pass-forge / flash-bypass / grounding-fake / D-13 exemption-abuse; structural defense = the live-horizon read + the :942 crossing re-read + the real-funded-delivery grounding + the un-spoofable pinned VAULT/SDGNRS exemption. CONFIRM the passless cap-occupancy + unfunded free-rider vectors CLOSED. |
| T-357-02-BYPASS | Elevation | the now-sound 5cb707f2 advance-gate bypass | mitigate | Re-attested SOUND post-hardening — every active sub is pass-holding + purchase-grounded → the :1124 bypass gives no free-rider edge; the D-13 exemption doesn't reopen one (VAULT/sDGNRS advance via their own paths). |
| T-357-02-STUBCEI | Elevation | the new drainAffiliateBase stub + the affiliate claim CEI | mitigate | /contract-auditor probes the stub's AFFILIATE-only guard (module :1301 under delegatecall, msg.sender=affiliate) + the claim() CEI (drainAffiliateBase zeroes affiliateBase, recordAmount=0 → no reentrancy edge). |
| T-357-02-XMODEL | Spoofing | the best-effort XMODEL augmentation | accept | XMODEL (Codex + Gemini) is best-effort augmentation (D-03) — a CLI being unavailable is recorded attempted/partial, NOT a block. The Claude 3-skill sweep is the primary gate. |
| T-357-02-RO | Tampering | this plan's deliverable | accept | Read-only sweep; the contracts diff vs HEAD' stays empty. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Write §A CHARGE, run the 3-skill GENUINE-PARALLEL sweep + the best-effort XMODEL close across the v56 charge set (record the execution path)</name>
  <read_first>
    - $HOME/.claude/skills/contract-auditor/SKILL.md (persona + output format + Known Non-Issues — the two-path-open + the drainAffiliateBase-stub + the D-11/D-12-gate lead)
    - $HOME/.claude/skills/economic-analyst/SKILL.md (persona + Known Non-Issues — the strategic-sub/unsub-EV + the settle-timing + the pre-credit-EV lead)
    - $HOME/.claude/skills/zero-day-hunter/SKILL.md (persona + the reveal-window / pass-forge / grounding-fake pitfalls + Known Non-Issues — the churn + the hardened-gate co-lead)
    - $HOME/.claude/skills/degen-skeptic/SKILL.md (the dual-gate lens definitions applied to every elevation)
    - audit/FINDINGS-v55.0.md §4 (the v55 Phase 352 adversarial-log structure: §4.1 Outcome / §4.2 FINDING_CANDIDATE write-up / §4.3 SAFE_BY_DESIGN rows / §4.4 Skeptic-Reviewer Filter Attestation — read the §4 region ~`:453`)
    - .planning/milestones/v55.0-phases/352-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/352-02-ADVERSARIAL-LOG.md (the directly-reusable §A/§B/§C/§D/§E log shape + the A.2 GENUINE PARALLEL_SUBAGENT topology note to mirror)
    - .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-00-SUMMARY.md (HEAD' — the subject SHA each skill probes)
    - the frozen subject surfaces via git show HEAD':contracts/<file> (the interfaces list — read the ACTUAL game-resident code at HEAD', NOT from memory)
  </read_first>
  <files>.planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-02-ADVERSARIAL-LOG.md</files>
  <action>
    First write the §A CHARGE section to 357-02-ADVERSARIAL-LOG.md: the FIXED 3-skill set (/contract-auditor + /economic-analyst + /zero-day-hunter; /degen-skeptic OUT as a probing skill per D-271-ADVERSARIAL-02, used ONLY as the dual-gate filter), the audit subject SHA (HEAD') + baseline (453f8073), the per-skill probe assignment mapped to the persona (the (a) strategic sub/unsub edge → economic-analyst + zero-day-hunter lead; (b) settle-timing → economic-analyst; (c) pre-credit-EV → economic-analyst + contract-auditor; (d) two-path open → contract-auditor lead; (e) the D-11/D-12 hardened gates + the now-sound 5cb707f2 bypass → contract-auditor + zero-day-hunter; (f) the drainAffiliateBase stub + the affiliate claim CEI → contract-auditor lead), the §A.2 GENUINE PARALLEL_SUBAGENT topology note (ran INLINE in the orchestrator context which holds the Task tool; 3 concurrent background Task spawns, each read-only; NOT the executor-nested SEQUENTIAL fallback), and the recorded execution path actually used (PARALLEL_SUBAGENT if the running context holds the Task tool, else HYBRID or SEQUENTIAL_MAIN_CONTEXT — note WHICH).

    Then run the sweep: invoke each skill (concurrent Task spawns when available) against the frozen subject HEAD', each charged with its probe subset; honor its Known Non-Issues list (do NOT re-flag the removed affiliate roll / the off-the-ETH-path BURNIE credit / the accepted +0..+9 head-start / the resolved O1/QST-05 / the operator-approval trust boundary / the SAFE_BY_DESIGN reentrancy / MEV-as-confirmatory — see the interfaces Known-Non-Issues list + the USER-LOCKED weighting). Collect the raw per-skill outputs into §B under a per-skill subsection. THEN run the best-effort XMODEL close (D-03): detect codex + gemini availability (command -v); for each available CLI, feed a crafted prompt on the FULL afking buy+open system + the F-356-01 fix + the D-11/D-12/D-13 hardening; fold its disposition into §B as an XMODEL augmentation subsection; if a CLI is unavailable, record "XMODEL attempted — <model> unavailable, partial" and proceed (NOT a block). Probe against the actual frozen source (git show HEAD':contracts/...), NOT from memory — every cited file:line re-grep-verified at HEAD'. DO NOT edit any contracts/*.sol file.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -qi degen-skeptic .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-02-ADVERSARIAL-LOG.md && grep -qiE "PARALLEL_SUBAGENT|SEQUENTIAL_MAIN_CONTEXT|HYBRID" .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-02-ADVERSARIAL-LOG.md && grep -qiE "XMODEL|Codex|Gemini" .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-02-ADVERSARIAL-LOG.md && grep -qiE "sub/unsub|strategic sub|churn" .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-02-ADVERSARIAL-LOG.md && HEADP=$(grep -oE "[0-9a-f]{40}" .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-00-SUMMARY.md | head -1); git diff --quiet "$HEADP" HEAD -- contracts/</automated>
  </verify>
  <acceptance_criteria>
    - §A CHARGE names the FIXED 3-skill set (/contract-auditor + /economic-analyst + /zero-day-hunter) with /degen-skeptic explicitly OUT as a probing skill per D-271-ADVERSARIAL-02 (dual-gate filter only), plus the subject SHA HEAD' + baseline 453f8073.
    - The 6-surface charge split (a strategic sub/unsub PRIMARY / b settle-timing / c pre-credit-EV / d two-path open / e D-11/D-12 gates + 5cb707f2 bypass / f drainAffiliateBase stub + claim CEI) is recorded with the per-skill probe assignment.
    - The §A.2 GENUINE PARALLEL_SUBAGENT topology note is present AND the execution path actually used (PARALLEL_SUBAGENT / HYBRID / SEQUENTIAL_MAIN_CONTEXT) is recorded.
    - Each charged probe has raw per-skill output in §B probing the actual frozen subject (git show HEAD':contracts/...); the best-effort XMODEL (Codex + Gemini) augmentation is folded in (or recorded attempted/partial if a CLI is unavailable — NOT a block).
    - The frozen-subject check (HEAD' to HEAD) stays empty (read-only).
  </acceptance_criteria>
  <done>§A CHARGE written (3-skill set, subject SHA HEAD', the 6-surface split, the topology note + execution path); the 3-skill sweep run; the best-effort XMODEL augmentation folded in (or recorded attempted/partial); raw per-skill + per-model outputs collected in §B against the frozen subject; the frozen subject is byte-identical to HEAD'.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the skeptic dual-gate, build the §C disposition table, write the §D attestation + §E read-only invariant, route any FINDING_CANDIDATE</name>
  <read_first>
    - $HOME/.claude/skills/degen-skeptic/SKILL.md (the dual-gate definition: structural-protection lens + 3-condition EV lens — applied to every elevation)
    - audit/FINDINGS-v55.0.md §4.2/§4.3/§4.4 (the FINDING_CANDIDATE write-up + the SAFE_BY_DESIGN rows + the dual-gate Skeptic-Reviewer Filter Attestation to mirror)
    - .planning/milestones/v55.0-phases/352-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/352-02-ADVERSARIAL-LOG.md (the §C disposition-table columns + the §C Outcome summary + the §D attestation + the §E read-only invariant shape)
    - .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-02-ADVERSARIAL-LOG.md (the §B raw per-skill + XMODEL outputs from Task 1)
    - the MEMORY notes [[threat-model-reentrancy-mev-nonissues]] (the USER-locked weighting + the SAFE_BY_DESIGN reentrancy/MEV stance) + [[v45-vrf-freeze-invariant]] (the freeze north-star) + [[v56-affiliate-drain-missing-game-stub-bug]] (the F-356-01 fix surface — now RESOLVED-AT-357)
  </read_first>
  <files>.planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-02-ADVERSARIAL-LOG.md</files>
  <action>
    For every elevation in the §B raw per-skill + XMODEL outputs, apply the dual-gate skeptic filter (per-skill self-arm + orchestrator integration-time re-application): the structural-protection lens first, then the 3-condition EV lens. Only elevations surviving BOTH gates become a FINDING_CANDIDATE; everything else is NEGATIVE-VERIFIED or SAFE_BY_DESIGN. Build the §C disposition table — one row per probe per skill (+ the XMODEL rows) — with columns: Probe ID, Skill/Model, Surface (strategic-sub/unsub / settle-timing / pre-credit-EV / two-path-open / D-11-D-12-gates+5cb707f2-bypass / drainAffiliateBase-stub+claim-CEI), Disposition (NEGATIVE-VERIFIED / SAFE_BY_DESIGN / FINDING_CANDIDATE), Skeptic-filter outcome, Tier (1 single-skill / 2 multi-skill consensus / n-a). Then mirror the v55 §4 structure: a §C Outcome summary (count of rows by disposition), the FINDING_CANDIDATE write-up(s) if any (severity + the exact skeptic-filter trace), the SAFE_BY_DESIGN informational rows, a §D Skeptic-Reviewer Filter Attestation (the dual-gate applied + any self-discards), and a §E read-only invariant (git diff HEAD' HEAD -- contracts/ empty; every cite re-grep-verified at HEAD').

    Record explicitly the expected dispositions (honestly, regardless of outcome): the strategic-sub/unsub-churn probe (expected NEGATIVE-VERIFIED — forfeit-nothing-gain-nothing: affiliateBase persists for the uplines on unsub; pendingBurnie is the sub's own per-delivered-day balance; the self-marking balances make double-settle impossible; PROVEN by V56SecUnmanipulable 11/11); the settle-timing probe (expected NEGATIVE-VERIFIED — no seed/roll, exactly ONE deterministic path); the pre-credit-EV probe (expected SAFE_BY_DESIGN — the +0..+9 head-start is USER-ACCEPTED-BY-DESIGN, the bound replaces the escrow); the two-path-open probe (expected NEGATIVE-VERIFIED — lastOpenedDay monotone + the shared EV-cap budget + LIVE-01 openBoxes valve isolation, PROVEN by V56AfkingGasMarginal); the D-11/D-12 hardened-gate probes (expected NEGATIVE-VERIFIED — the live-horizon read + the :942 crossing re-read + the real-funded grounding + the un-spoofable VAULT/SDGNRS exemption; CONFIRM the passless cap-occupancy + unfunded free-rider vectors are CLOSED — this STRENGTHENS SEC-01); the 5cb707f2-bypass probe (expected NEGATIVE-VERIFIED — now SOUND: every active sub is pass-holding + purchase-grounded); the drainAffiliateBase-stub + claim-CEI probe (expected NEGATIVE-VERIFIED — AFFILIATE-only guard under delegatecall + the CEI sound). F-356-01 itself is RESOLVED-AT-357 (the fix landed at 357-00), recorded as RESOLVED, NOT a live FINDING_CANDIDATE.

    If ZERO FINDING_CANDIDATEs survive, record the clean-closure outcome explicitly (the working target — the ONLY new finding is F-356-01, RESOLVED in-phase). If a FINDING_CANDIDATE DOES survive, record it WITHOUT fixing it — surface it to 357-03 (findings deliverable) and 357-04 (closure gate) for USER adjudication (the subject is FROZEN at HEAD'; any further fix is a NEW contract phase requiring USER approval; default leaning DEFER→v57 with the fix design locked). An advisory/sub-finding is RECORDED but is NOT a finding and does NOT amend the verdict. Read-only — no source edits.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -qE "NEGATIVE-VERIFIED|SAFE_BY_DESIGN|FINDING_CANDIDATE" .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-02-ADVERSARIAL-LOG.md && grep -qi skeptic .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-02-ADVERSARIAL-LOG.md && grep -q "F-356-01" .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-02-ADVERSARIAL-LOG.md && HEADP=$(grep -oE "[0-9a-f]{40}" .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-00-SUMMARY.md | head -1); git diff --quiet "$HEADP" HEAD -- contracts/</automated>
  </verify>
  <acceptance_criteria>
    - The §C disposition table has one row per charged probe per skill (+ the XMODEL rows), each carrying NEGATIVE-VERIFIED / SAFE_BY_DESIGN / FINDING_CANDIDATE + the skeptic-filter outcome + the Tier.
    - The /degen-skeptic dual-gate (structural-protection + 3-condition EV) is applied and attested in §D.
    - A §C Outcome summary records the count of rows by disposition; a §E read-only invariant records git diff HEAD' HEAD -- contracts/ empty.
    - Each of the 6 probe families records its disposition with the expected structural-defense cross-ref (V56Sec*/V56AfkingGasMarginal/V56QuestNonPerturb); the passless cap-occupancy + unfunded free-rider vectors are recorded CLOSED (SEC-01 strengthened); F-356-01 is recorded RESOLVED-AT-357 (not a live candidate).
    - If a FINDING_CANDIDATE survives, it is recorded WITHOUT a fix and surfaced to 357-03/357-04 (default leaning DEFER→v57); if zero survive, the clean-closure outcome is recorded explicitly.
    - The frozen-subject check (HEAD' to HEAD) stays empty (read-only).
  </acceptance_criteria>
  <done>§C disposition table built with per-probe rows (+ XMODEL) + skeptic-filter outcomes; §C Outcome summary + §D Skeptic-Reviewer Filter Attestation + §E read-only invariant written; the passless cap-occupancy + unfunded free-rider vectors recorded CLOSED; F-356-01 recorded RESOLVED-AT-357; any FINDING_CANDIDATE routed (not fixed) to the closure gate; the frozen subject is byte-identical to HEAD'.</done>
</task>

</tasks>

<verification>
- The frozen-subject check (HEAD' to HEAD) stays empty (read-only sweep throughout).
- 357-02-ADVERSARIAL-LOG.md contains: §A CHARGE (FIXED 3-skill set, /degen-skeptic OUT as a probing skill, subject SHA HEAD', the 6-surface split, the GENUINE PARALLEL_SUBAGENT topology note + execution path, the best-effort XMODEL note), §B raw per-skill + XMODEL outputs, the §C per-probe disposition table + Outcome summary, the §D Skeptic-Reviewer Filter Attestation, and the §E read-only invariant.
- Every charged probe (the 6 v56 surfaces) maps to at least one disposition row; the passless cap-occupancy + unfunded free-rider vectors are recorded CLOSED; F-356-01 is RESOLVED-AT-357.
</verification>

<success_criteria>
The SC1 adversarial-sweep half of AUDIT-01 satisfied: the fixed 3-skill sweep runs as GENUINE PARALLEL_SUBAGENT inline from the orchestrator context (else the recorded fallback) against the frozen subject HEAD', AUGMENTED by the best-effort XMODEL (Codex + Gemini) close; every charged probe across the strategic sub/unsub edge + settle-timing + pre-credit-EV + two-path open + the D-11/D-12 hardened gates + the now-sound 5cb707f2 bypass + the drainAffiliateBase stub + the affiliate claim CEI has a disposition row; every elevation passes the /degen-skeptic dual-gate before being recorded; the passless cap-occupancy + unfunded free-rider vectors are confirmed CLOSED; F-356-01 is RESOLVED-AT-357; any FINDING_CANDIDATE is routed to the closure gate (not auto-fixed); the frozen subject is byte-identical to HEAD'.
</success_criteria>

<output>
Create `.planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-02-SUMMARY.md` when done
</output>
