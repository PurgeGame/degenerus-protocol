---
phase: 357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/DegenerusGame.sol
  - contracts/modules/GameAfkingModule.sol
autonomous: false
requirements: [AUDIT-01]

must_haves:
  truths:
    - "DegenerusGame.sol gains a `drainAffiliateBase(address sub) external returns (uint256)` dispatch stub that delegatecalls GAME_AFKING_MODULE (guard-less at the dispatch layer, mirroring claimAfkingBurnie:413), _revertDelegate on failure, data.length==0 guard, returns abi.decode(data,(uint256)) — fixing F-356-01 (DegenerusAffiliate.claim() at :629 → drainAffiliateBase loop at :654 no longer reverts)"
    - "GameAfkingModule.subscribe() UPSERT branch (dailyQuantity >= 1) REVERTS with a new NoPass-style custom error unless _passHorizonOf(subscriber) >= the storage level (deity sentinel type(uint24).max always covers) — D-11; the cancel branch (dailyQuantity == 0, returns at :328) is NOT gated; the :942 crossing eviction is KEPT"
    - "GameAfkingModule.subscribe() UPSERT branch REVERTS with a new MustPurchaseToBeginAfking-style custom error unless the subscriber is grounded on a real purchase (already-bought-today OR a funded in-tx cover-buy executes) — the unfunded-forfeit leg at :456 (_setStreakBase(s, 0)) flips to the revert; genuinely-grounded re-subscribes (already-bought-today / pending-box) MUST NOT revert — D-12"
    - "BOTH new gates EXEMPT the hardcoded set {ContractAddresses.VAULT, ContractAddresses.SDGNRS} keyed on the un-spoofable resolved subscriber identity (the :270 player==address(0)?msg.sender:player), mirroring the :996-997 exemption idiom — D-13; the 5cb707f2 AdvanceModule:1124 bypass is LEFT AS-IS; no level() stub is added (Solidity auto-getter collision)"
    - "forge build is clean (exit 0) after the three changes"
    - "the three changes are bundled FIX-FIRST at ONE autonomous:false boundary and HELD at the contract-commit boundary for explicit USER hand-review (pre-commit hook moved aside + CONTRACTS_COMMIT_APPROVED context), then committed as the SINGLE .sol commit of the phase (HEAD') so the closure HEAD = the actually-shippable hardened subject (audited == shipped) — D-01"
  artifacts:
    - path: "contracts/DegenerusGame.sol"
      provides: "the F-356-01 drainAffiliateBase dispatch stub"
      contains: "function drainAffiliateBase(address"
    - path: "contracts/modules/GameAfkingModule.sol"
      provides: "the D-11 pass-required + D-12 purchase-grounded subscribe gates with the D-13 VAULT/SDGNRS exemption"
      contains: "MustPurchaseToBeginAfking"
  key_links:
    - from: "contracts/DegenerusAffiliate.sol:654"
      to: "contracts/DegenerusGame.sol drainAffiliateBase stub"
      via: "afkingDrain.drainAffiliateBase(sub) external call → Game dispatch → GAME_AFKING_MODULE delegatecall"
      pattern: "drainAffiliateBase"
    - from: "contracts/modules/GameAfkingModule.sol subscribe UPSERT branch"
      to: "_passHorizonOf + the cover-buy block + the VAULT/SDGNRS exemption"
      via: "the D-11/D-12 revert gates wrapped by the D-13 exempt-set short-circuit"
      pattern: "NoPass|MustPurchaseToBeginAfking"
---

<objective>
The leading 357-00 CONTRACT GATE: author the THREE bundled contract changes at ONE `autonomous: false` USER-approved hand-review boundary, then `forge build` clean and HELD for explicit USER hand-review (pre-commit hook moved aside), committed as the SINGLE `.sol` commit of the entire phase (the post-fix HEAD = HEAD'):

1. **F-356-01 fix** — ADD the `drainAffiliateBase(address sub) external returns (uint256)` dispatch stub in `contracts/DegenerusGame.sol`, composing the `claimAfkingBurnie:413` afking-module-target / guard-less shape with the `runDecimatorJackpot:1202` decode-and-return tail. Fixes the LIVE-public-contract bug where `DegenerusAffiliate.claim()` (`:629`) reverts at the drain loop (`:654`) → afking-affiliate rewards permanently unreachable.
2. **D-11 pass-required subscribe** — `subscribe()` UPSERT branch REVERTS (new `NoPass`-style error) unless `_passHorizonOf(subscriber) >= level`.
3. **D-12 purchase-grounded subscribe** — `subscribe()` UPSERT branch REVERTS (new `MustPurchaseToBeginAfking`-style error) unless grounded on a real purchase; the `:456` unfunded-forfeit leg flips to the revert.

BOTH gates EXEMPT `{VAULT, SDGNRS}` (D-13, load-bearing for bootstrap). The D-14 test reconciliation + the new positive proofs + the re-freeze are 357-00b (the sibling Wave-1 plan).

Purpose: FIX-FIRST (D-01) — the closure HEAD = the actually-shippable hardened subject (audited == shipped). This is the v56-specific contract footprint that breaks the prior "zero contract mutation" TERMINAL mold. It is the FIRST of TWO `autonomous: false` gates in this phase (the other is the 357-04 closure gate).
Output: the post-fix HEAD' — `contracts/DegenerusGame.sol` + `contracts/modules/GameAfkingModule.sol` mutated, committed as the sole `.sol` commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-CONTEXT.md
@.planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-PATTERNS.md

<interfaces>
<!-- The frozen subject pre-fix is HEAD f16bd048. The executor reads the ACTUAL anchors via grep — do not trust line numbers blindly; the pattern map re-grepped them at live HEAD (verified 2026-06-02). -->

THE THREE WRITE TARGETS (the ONLY contracts/ writes in the entire phase 357):

=== CHANGE 1: contracts/DegenerusGame.sol — ADD the drainAffiliateBase dispatch stub (F-356-01) ===
- The ANALOG to compose (read both, author the stub between them in shape):
  - `claimAfkingBurnie` @ `:413-423` — the GAME_AFKING_MODULE delegatecall, GUARD-LESS at the dispatch layer (no `msg.sender != address(this)` check — the module impl owns access control), `_revertDelegate(data)` on `!ok`.
  - `runDecimatorJackpot` @ `:1202-1221` — the single-arg → delegatecall → `_revertDelegate` on fail → `data.length == 0` guard → `return abi.decode(data, (uint256));` return tail. (Its `msg.sender != address(this)` self-call guard at `:1204` is for a self-call selector — DO NOT copy that guard; drainAffiliateBase is an EXTERNAL call from DegenerusAffiliate, not a self-call.)
- `_revertDelegate` helper @ `:1130-1135` (UNCHANGED — reuse it; it propagates the module's NotApproved() selector verbatim).
- The stub to author: `function drainAffiliateBase(address sub) external returns (uint256)` → delegatecall `ContractAddresses.GAME_AFKING_MODULE` with `abi.encodeWithSelector(IGameAfkingModule.drainAffiliateBase.selector, sub)` → `if (!ok) _revertDelegate(data);` → `if (data.length == 0) revert E();` → `return abi.decode(data, (uint256));`. Guard-less (mirror claimAfkingBurnie). Place it adjacent to the other GAME_AFKING_MODULE dispatch stubs (the `:352-423` block).
- DO NOT add a `level()` stub: `DegenerusGameStorage.sol:245` declares `uint24 public level` → Solidity auto-generates the `level()` getter (resolved at affiliate `:684`). Adding an explicit `function level()` would COLLIDE.
- The module impl is ALREADY correct (READ-ONLY reference) — `GameAfkingModule.sol:1300-1305`: `function drainAffiliateBase(address sub) external returns (uint256 base)` with the AFFILIATE-only guard `if (msg.sender != ContractAddresses.AFFILIATE) revert NotApproved();` at `:1301` (runs in Game storage context under delegatecall → msg.sender is the affiliate).
- The interface decl is ALREADY correct: `IDegenerusGameModules.sol:503` (`function drainAffiliateBase(address sub) external returns (uint256 base);`) — the selector the stub encodes.
- Reachability (the bug): `DegenerusAffiliate.claim()` (`:629`) loops `afkingDrain.drainAffiliateBase(sub)` (`:654`) against `ContractAddresses.GAME`; with no Game dispatch stub AND no `fallback()` (`DegenerusGame.sol` has only `receive()` @ `:2959`), that call reverts → `claim()` reverts → affiliate rewards unreachable.

=== CHANGE 2 + 3 + exemption: contracts/modules/GameAfkingModule.sol — the D-11 / D-12 gates + D-13 exemption ===
- `subscribe()` @ `:255`; the resolved subscriber identity @ `:270` (`player == address(0) ? msg.sender : player`) — KEY the D-13 exemption on THIS un-spoofable identity, never on fundingSource/src.
- The cancel/unsub branch @ `:311-328` (the `if (dailyQuantity == 0)` path) returns at `:328` BEFORE the UPSERT branch — leave it UNGATED.
- The UPSERT branch begins after `:328` (the `Sub storage s = _subOf[subscriber]` at `:334`); the pass-horizon read `s.validThroughLevel = _passHorizonOf(subscriber)` @ `:350`.
- The cover-buy block @ `:362-460` (the D-12 grounding source): the funded paths (`_setStreakBase(s, snap)` funded-day-0 @ `:424` / `:443`) ARE grounded; the unfunded-forfeit `else { _setStreakBase(s, 0); }` @ `:456` is the leg that flips to the D-12 revert.
- `_passHorizonOf` @ `:482` — the D-11 read: returns `type(uint24).max` if the deity bit is set, else the `FROZEN_UNTIL_LEVEL_SHIFT` horizon. It is ALREADY read at `:350` — reuse that read for the D-11 compare, do NOT double-SLOAD.
- The crossing eviction @ `:942` (`if (currentLevel > sub.validThroughLevel)`) — KEEP it (a pass valid at subscribe can be outgrown).
- The custom-error idiom: decls @ `:82-95` (`error InvalidReinvestPct(); error NotApproved(); error NotSubscribed(); error SubscriberCapReached(); error NoWork();`); bare call sites `revert Foo();` @ `:266`/`:273`/`:312`/`:507`. ADD `error NoPass();` (D-11) + `error MustPurchaseToBeginAfking();` (D-12) in the `:82-95` block with NatSpec.
- The un-spoofable pinned exemption idiom (the EXACT pattern to mirror) @ `:996-997`: `player == ContractAddresses.VAULT || player == ContractAddresses.SDGNRS`. Wrap BOTH new gates so the exempt set short-circuits PAST them.
- VAULT/SDGNRS already hold a deity pass (Game constructor `HAS_DEITY_PASS_SHIFT=1` @ `DegenerusGame.sol:209-211`) → they pass D-11 naturally; the hardcoded exemption is STILL load-bearing for D-12 (neither buys at construction) and is robust against deploy ordering. Gate BOTH on the SAME exempt set.
- The two self-subscribe call sites that MUST keep working post-gate: `DegenerusVault.sol:483` (`gamePlayer.subscribe(address(this), true, false, 1, 0, address(0))`) + `StakedDegenerusStonk.sol:388` (`game.subscribe(address(this), true, false, 1, 2, address(0))`) — both no-pass + unfunded at construction.
- The D-12 "purchased today" predicate is an IMPL decision (CONTEXT D-12 candidate signals: manual ETH mint today `mintPacked_` DAY_SHIFT==today / manual slot-0 `done[0]` / afking `lastAutoBoughtDay == today` / pending-unopened-box `lastOpenedDay < lastAutoBoughtDay`). Reuse the existing `wasActive` re-sub path's bought-today/pending-box recognition (the cover-buy block already skips re-buy in those states) — the grounding predicate MUST be satisfied by exactly those genuinely-grounded states + a funded in-tx cover-buy that executes.

=== THE COMMIT BOUNDARY (the autonomous:false gate) ===
- The pre-commit hook BLOCKS all `contracts/` commits (`.git/hooks/pre-commit`). To commit the .sol: `mv .git/hooks/pre-commit .git/hooks/pre-commit.bak`, commit, `mv .git/hooks/pre-commit.bak .git/hooks/pre-commit`.
- This commit is the SOLE `.sol` commit of phase 357. Everything downstream (357-00b D-14 + 357-01/02/03/04) re-freezes at HEAD' and is READ-ONLY against `contracts/`.
- `ContractAddresses.sol` is freely modifiable per policy, but this change touches NEITHER it nor the interface (already correct) — only DegenerusGame.sol + GameAfkingModule.sol.
- SOLVENCY-01 stays byte-unchanged: drainAffiliateBase is BURNIE-only (off the ETH/claimablePool path); the D-11/D-12 gates only add reverts, no debit change.
</interfaces>
</context>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| DegenerusAffiliate.claim() → DegenerusGame.drainAffiliateBase stub → GAME_AFKING_MODULE delegatecall | The new dispatch stub is permissionless at the Game layer; the module impl's `msg.sender != ContractAddresses.AFFILIATE` guard (`:1301`) is the access boundary, enforced in Game storage context under delegatecall. |
| EOA → subscribe() UPSERT branch | Untrusted player input crosses the D-11 pass-gate + D-12 grounding gate; the D-13 exemption must carve out ONLY the two pinned protocol addresses, keyed on the un-spoofable resolved subscriber identity. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-357-00-STUB | Elevation | the new drainAffiliateBase dispatch stub | mitigate | Guard-less dispatch is CORRECT (mirror claimAfkingBurnie) — the module impl owns the AFFILIATE-only access gate at `:1301` (runs under delegatecall in Game storage context, msg.sender = affiliate). The stub adds NO new access surface; it only routes a selector that was already callable on the module. The affiliate `claim()` CEI is unchanged (drainAffiliateBase zeroes affiliateBase, recordAmount=0 → reentrancy non-issue). |
| T-357-00-PASSFORGE | Spoofing | the D-11 pass-gate | mitigate | The gate reads the LIVE `_passHorizonOf(subscriber)` horizon (deity bit or FROZEN_UNTIL_LEVEL); a player cannot forge a pass horizon they do not hold. The `:942` crossing eviction re-reads on level advance so an outgrown pass cannot persist. No flash-bypass: the read is in-tx live state. |
| T-357-00-GROUNDFAKE | Spoofing | the D-12 grounding gate | mitigate | Grounding requires EITHER a real already-bought-today signal (the existing day-stamped buy markers) OR a FUNDED in-tx cover-buy that actually executes `_deliverAfkingBuy` (the funds must be real; the unfunded `:456` leg now reverts). A player cannot fake delivery — the buy debits real funding. |
| T-357-00-EXEMPTABUSE | Elevation | the D-13 VAULT/SDGNRS exemption | mitigate | Keyed on the un-spoofable resolved subscriber identity (`:270`), hardcoded to exactly the two `ContractAddresses` constants — not on fundingSource/src. Only the two protocol self-subscribers bypass; no third party can spoof `subscriber == VAULT`. |
| T-357-00-BOOTSTRAP | Denial of Service | VAULT/sDGNRS construction-time self-subscribe | mitigate | The D-13 exemption is LOAD-BEARING: WITHOUT it, the naive gates revert `DegenerusVault.sol:483` / `StakedDegenerusStonk.sol:388` (no-pass + unfunded) → deploy bricks. The exemption is verified by the D-14 positive proof (357-00b) that VAULT/sDGNRS still subscribe. |
| T-357-00-SC | Tampering | no package installs in this plan | accept | This plan installs NO npm/pip/cargo packages — it edits two existing .sol files + runs forge build. No supply-chain surface. |
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ADD the drainAffiliateBase dispatch stub in DegenerusGame.sol (F-356-01)</name>
  <read_first>
    - contracts/DegenerusGame.sol — read the GAME_AFKING_MODULE dispatch block (grep "GAME_AFKING_MODULE" + read claimAfkingBurnie at the `:413` region) AND the runDecimatorJackpot decode-return tail (grep "runDecimatorJackpot" + read its `:1202` region) AND _revertDelegate (grep "_revertDelegate" — the helper decl ~`:1130`); confirm receive() exists at `:2959` and NO fallback() (grep "fallback")
    - contracts/interfaces/IDegenerusGameModules.sol — grep "drainAffiliateBase" (the selector decl, ~`:503` — confirm the signature matches `(address sub) external returns (uint256 base)`)
    - contracts/modules/GameAfkingModule.sol — grep "function drainAffiliateBase" (the impl + the AFFILIATE-only guard, ~`:1300` — READ-ONLY, already correct)
    - contracts/DegenerusAffiliate.sol — grep "drainAffiliateBase" + read claim() at the `:629` region + the drain loop at `:654` (the reachability source — confirm it calls against ContractAddresses.GAME)
    - .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-PATTERNS.md (the "Resulting stub to author" + the ACCESS NOTE — guard-less, no level() stub)
  </read_first>
  <files>contracts/DegenerusGame.sol</files>
  <action>
    ADD a `function drainAffiliateBase(address sub) external returns (uint256)` dispatch stub to DegenerusGame.sol, placed adjacent to the other GAME_AFKING_MODULE dispatch stubs (the block around claimAfkingBurnie). Compose: target `ContractAddresses.GAME_AFKING_MODULE` via `delegatecall(abi.encodeWithSelector(IGameAfkingModule.drainAffiliateBase.selector, sub))` (mirror claimAfkingBurnie's guard-less afking dispatch shape — DO NOT add a `msg.sender != address(this)` guard), then `if (!ok) _revertDelegate(data);` then the runDecimatorJackpot-style return tail `if (data.length == 0) revert E(); return abi.decode(data, (uint256));`. Reuse the existing `_revertDelegate` helper unchanged. Do NOT add a `level()` stub (it would collide with the `uint24 public level` auto-getter). Do NOT edit the module impl or the interface (both already correct). Touch ONLY DegenerusGame.sol. NO fenced implementations beyond mirroring the named analogs — follow the analog shapes exactly.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -q "function drainAffiliateBase(address" contracts/DegenerusGame.sol && grep -A8 "function drainAffiliateBase(address" contracts/DegenerusGame.sol | grep -q "GAME_AFKING_MODULE" && grep -A10 "function drainAffiliateBase(address" contracts/DegenerusGame.sol | grep -q "abi.decode(data" && ! grep -A6 "function drainAffiliateBase(address" contracts/DegenerusGame.sol | grep -q "msg.sender != address(this)" && ! grep -qE "function level\(\)" contracts/DegenerusGame.sol</automated>
  </verify>
  <acceptance_criteria>
    - DegenerusGame.sol contains `function drainAffiliateBase(address sub) external returns (uint256)` delegatecalling GAME_AFKING_MODULE with IGameAfkingModule.drainAffiliateBase.selector.
    - The stub is GUARD-LESS at the dispatch layer (no `msg.sender != address(this)` check) — the module's AFFILIATE-only guard is the access boundary.
    - The stub uses `_revertDelegate(data)` on `!ok` and returns `abi.decode(data, (uint256))` with the `data.length == 0` guard.
    - NO `function level()` stub was added (the auto-getter is preserved).
    - The module impl + the interface decl are byte-unchanged (only DegenerusGame.sol edited in this task).
  </acceptance_criteria>
  <done>The drainAffiliateBase dispatch stub is authored in DegenerusGame.sol, guard-less, decode-and-return, mirroring claimAfkingBurnie + runDecimatorJackpot; no level() stub; F-356-01 fixed at the dispatch layer.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ADD the D-11 pass-required + D-12 purchase-grounded subscribe gates with the D-13 VAULT/SDGNRS exemption in GameAfkingModule.sol</name>
  <read_first>
    - contracts/modules/GameAfkingModule.sol — read: the custom-error decl block (grep "error " — the `:82-95` region); the subscribe() entry + the resolved subscriber identity (grep "function subscribe" + the `player == address(0)` resolution ~`:270`); the cancel branch (grep "dailyQuantity == 0" — the `:311-328` ungated path); the UPSERT branch + the pass-horizon write (grep "validThroughLevel = _passHorizonOf" ~`:350`); the cover-buy block + the unfunded-forfeit (grep "_setStreakBase(s, 0)" — the `:456` leg); _passHorizonOf (grep "function _passHorizonOf" ~`:482`); the crossing eviction (grep "currentLevel > sub.validThroughLevel" ~`:942`); the un-spoofable exemption idiom (grep "ContractAddresses.VAULT" — the `:996-997` pattern)
    - contracts/DegenerusVault.sol — grep "subscribe(address(this)" (the `:483` self-subscribe that must keep working)
    - contracts/StakedDegenerusStonk.sol — grep "subscribe(address(this)" (the `:388` self-subscribe that must keep working)
    - contracts/libraries/BitPackingLib.sol — grep "HAS_DEITY_PASS_SHIFT\|FROZEN_UNTIL_LEVEL_SHIFT\|DAY_SHIFT" (the pass-horizon + last-mint-day fields)
    - .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-PATTERNS.md (the D-11/D-12/D-13 gate shapes + the "purchased today" predicate candidates + the un-spoofable-pinned-exemption shared pattern)
  </read_first>
  <files>contracts/modules/GameAfkingModule.sol</files>
  <action>
    Make three coordinated edits to GameAfkingModule.sol, all in the subscribe() UPSERT branch (after the `:328` cancel-branch return), each wrapped by the D-13 exemption:
    (1) ADD two NatSpec'd custom errors to the `:82-95` decl block: `error NoPass();` (D-11) + `error MustPurchaseToBeginAfking();` (D-12), mirroring the existing `error Foo();` idiom.
    (2) D-11: in the UPSERT branch, REVERT `NoPass()` unless `_passHorizonOf(subscriber) >= level` (the storage `level`; the deity sentinel type(uint24).max always covers). REUSE the existing `_passHorizonOf(subscriber)` read at `:350` (the one feeding `s.validThroughLevel`) — do NOT add a second SLOAD; gate on that same value. KEEP the `:942` crossing eviction unchanged.
    (3) D-12: flip the unfunded-forfeit `else { _setStreakBase(s, 0); }` at `:456` to `revert MustPurchaseToBeginAfking();` so an unfunded subscribe reverts instead of silently starting an inert run. The funded paths (`_setStreakBase(s, snap)` at `:424`/`:443`) and the genuinely-grounded re-sub states (already-bought-today / pending-unopened-box — the cover-buy block already recognizes these and skips re-buy) MUST NOT revert. Leave the 5cb707f2 AdvanceModule:1124 bypass AS-IS (out of this file).
    (4) D-13: wrap BOTH new gates (D-11 + D-12) so `subscriber == ContractAddresses.VAULT || subscriber == ContractAddresses.SDGNRS` short-circuits PAST them, keyed on the un-spoofable resolved subscriber identity (`:270`), mirroring the `:996-997` idiom. Both VAULT/sDGNRS construction-time self-subscribes (no-pass + unfunded) MUST still succeed.
    Touch ONLY GameAfkingModule.sol. Keep comments lean per the project style (explain how the gate works + the bootstrap-exemption invariant in plain English; no plan IDs / requirement tags / spec-line citations in the code comments). NO fenced implementations — follow the named in-file analogs.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -q "error NoPass()" contracts/modules/GameAfkingModule.sol && grep -q "error MustPurchaseToBeginAfking()" contracts/modules/GameAfkingModule.sol && grep -q "revert NoPass()" contracts/modules/GameAfkingModule.sol && grep -q "revert MustPurchaseToBeginAfking()" contracts/modules/GameAfkingModule.sol && grep -c "ContractAddresses.VAULT" contracts/modules/GameAfkingModule.sol | awk '{ if ($1 >= 2) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - Two new custom errors `NoPass()` (D-11) + `MustPurchaseToBeginAfking()` (D-12) are declared and reverted in the subscribe UPSERT branch.
    - The D-11 gate compares `_passHorizonOf(subscriber) >= level` reusing the existing `:350` read (no double-SLOAD); the deity sentinel covers; the `:942` crossing eviction is unchanged.
    - The `:456` unfunded-forfeit `_setStreakBase(s, 0)` leg is replaced by `revert MustPurchaseToBeginAfking()`; the funded + already-grounded re-sub paths do NOT revert.
    - BOTH gates short-circuit for `subscriber == ContractAddresses.VAULT || subscriber == ContractAddresses.SDGNRS` (a second VAULT/SDGNRS exemption appears in subscribe, distinct from the existing `:996-997` one), keyed on the resolved subscriber identity.
    - The cancel branch (`dailyQuantity == 0`) remains UNGATED; the AdvanceModule:1124 bypass is untouched (only GameAfkingModule.sol edited).
  </acceptance_criteria>
  <done>The D-11 pass-gate + D-12 grounding-gate + D-13 VAULT/SDGNRS exemption are authored in GameAfkingModule.sol's subscribe UPSERT branch; the cancel branch + the crossing eviction + the advance bypass are untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: forge build clean</name>
  <read_first>
    - contracts/DegenerusGame.sol (Task 1 result)
    - contracts/modules/GameAfkingModule.sol (Task 2 result)
  </read_first>
  <files>(none — build verification only)</files>
  <action>
    Run `forge build` and confirm it exits 0 (the three changes compile clean). If it fails, fix the compile error in DegenerusGame.sol or GameAfkingModule.sol ONLY (do not widen scope). Do NOT run `hardhat compile --force` (it regenerates ContractAddresses.sol and breaks the forge fixture per the project landmine note).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -5; forge build --sizes >/dev/null 2>&1 && echo "BUILD OK" || (forge build && echo "BUILD OK")</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` exits 0 with the three changes applied.
    - No `hardhat compile --force` was run (ContractAddresses.sol not regenerated).
  </acceptance_criteria>
  <done>forge build is clean with all three contract changes applied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: USER hand-review-and-commit gate — the ONE phase-357 .sol commit (HEAD')</name>
  <files>(none — blocking gate; the .sol commit happens here after explicit USER approval)</files>
  <what-built>
    The THREE bundled contract changes are applied + `forge build` clean, held at the contract-commit boundary:
    1. F-356-01 fix — the `drainAffiliateBase` dispatch stub in DegenerusGame.sol (mirrors claimAfkingBurnie:413 + runDecimatorJackpot return tail; guard-less; no level() stub). The LIVE-public-contract affiliate-claim bug is now fixed at the dispatch layer.
    2. D-11 pass-required subscribe — `subscribe()` UPSERT reverts NoPass() unless `_passHorizonOf(subscriber) >= level`.
    3. D-12 purchase-grounded subscribe — the `:456` unfunded-forfeit leg flips to `revert MustPurchaseToBeginAfking()`.
    BOTH gates EXEMPT {VAULT, SDGNRS} (D-13, load-bearing for bootstrap). The cancel branch + the `:942` crossing eviction + the AdvanceModule:1124 bypass are untouched. SOLVENCY-01 byte-unchanged (BURNIE-only / revert-only changes).
  </what-built>
  <how-to-verify>
    1. Review the diff: `git diff -- contracts/DegenerusGame.sol contracts/modules/GameAfkingModule.sol`.
    2. Confirm CHANGE 1 (DegenerusGame.sol): the drainAffiliateBase stub is guard-less, delegatecalls GAME_AFKING_MODULE, decode-returns uint256; no level() stub added.
    3. Confirm CHANGE 2/3 (GameAfkingModule.sol): NoPass() + MustPurchaseToBeginAfking() declared + reverted in the subscribe UPSERT branch; BOTH gates exempt VAULT/SDGNRS; the cancel branch + crossing eviction unchanged.
    4. Confirm `forge build` clean (Task 3).
    5. Approve the diff for commit, OR describe required changes.
  </how-to-verify>
  <read_first>
    - contracts/DegenerusGame.sol (the Task-1 stub)
    - contracts/modules/GameAfkingModule.sol (the Task-2 gates)
    - .planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-CONTEXT.md (D-01 fix-first + D-11/D-12/D-13)
  </read_first>
  <action>
    Present the full `git diff` of the two contract files to the USER, BLOCK for explicit approval (the contract-commit boundary — the ONLY action needing approval per the project rule; auto-advance HELD). After approval: move the pre-commit hook aside (`mv .git/hooks/pre-commit .git/hooks/pre-commit.bak`), `git add contracts/DegenerusGame.sol contracts/modules/GameAfkingModule.sol`, commit with a message naming the THREE changes (F-356-01 stub + D-11 pass-gate + D-12 grounding + D-13 exemption) and the Co-Authored-By trailer, then restore the hook (`mv .git/hooks/pre-commit.bak .git/hooks/pre-commit`). This commit is HEAD' (the post-fix HEAD; the SOLE .sol commit of the phase). Record HEAD' in the SUMMARY. DO NOT push. DO NOT commit any other file in this commit.
  </action>
  <verify>
    <human-check>The USER reviews the git diff of DegenerusGame.sol + GameAfkingModule.sol and explicitly approves the .sol commit; after the commit, `git log -1 --name-only` shows ONLY the two contract files and `forge build` is clean.</human-check>
  </verify>
  <acceptance_criteria>
    - The full contract diff (both files) is presented to the USER; execution BLOCKS until explicit approval.
    - After approval: the pre-commit hook is moved aside, the two .sol files are committed (ONLY those two), and the hook is restored.
    - The commit message names the three changes + carries the Co-Authored-By trailer.
    - HEAD' (the post-fix HEAD SHA) is recorded in the SUMMARY for the downstream re-freeze (357-00b + 357-01/02).
    - Nothing is pushed; no other file is in the commit.
  </acceptance_criteria>
  <done>The USER approved the diff; the three contract changes are committed as HEAD' (the sole .sol commit of phase 357); the pre-commit hook is restored; nothing pushed; HEAD' recorded.</done>
  <resume-signal>Type "approved" to commit the .sol changes as HEAD', or describe required changes to the diff.</resume-signal>
</task>

</tasks>

<verification>
- forge build is clean (exit 0) with the three changes.
- DegenerusGame.sol contains the guard-less drainAffiliateBase dispatch stub (decode-and-return); no level() stub.
- GameAfkingModule.sol contains the D-11 NoPass + D-12 MustPurchaseToBeginAfking gates in the subscribe UPSERT branch, both exempting VAULT/SDGNRS; the cancel branch + the crossing eviction untouched.
- The three changes are committed as the SOLE .sol commit (HEAD') after explicit USER hand-review; the pre-commit hook is restored; nothing pushed.
</verification>

<success_criteria>
The 357-00 contract gate satisfied: F-356-01 fixed (drainAffiliateBase dispatch stub), D-11 pass-required subscribe + D-12 purchase-grounded subscribe added with the D-13 VAULT/SDGNRS exemption, forge build clean, committed at the autonomous:false USER hand-review boundary as HEAD' (the sole .sol commit). The D-14 test reconciliation + new positive proofs + re-freeze are 357-00b.
</success_criteria>

<output>
Create `.planning/phases/357-terminal-delta-audit-3-skill-genuine-parallel-adversarial-sw/357-00-SUMMARY.md` when done — RECORD the resolved HEAD' SHA (the downstream plans freeze against it).
</output>
