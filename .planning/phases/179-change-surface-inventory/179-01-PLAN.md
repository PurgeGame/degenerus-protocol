---
phase: 179-change-surface-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/179-change-surface-inventory/179-01-DIFF-INVENTORY.md
autonomous: true
requirements:
  - DELTA-05
must_haves:
  truths:
    - "Every changed line in contracts/ since v15.0 is visible in one document, organized by contract file"
    - "Every change is attributed to its originating milestone (v16.0-repack, v16.0-endgame-delete, v17.0-affiliate-cache, v17.1-comments, rngBypass-refactor, or pre-v16.0-manual)"
    - "No contract file with changes is missing from the inventory"
  artifacts:
    - path: ".planning/phases/179-change-surface-inventory/179-01-DIFF-INVENTORY.md"
      provides: "Complete attributed diff inventory of all contract changes since v15.0"
      contains: "## Per-Contract Diff Inventory"
  key_links:
    - from: "git diff e2cd1b2b..HEAD -- contracts/"
      to: "179-01-DIFF-INVENTORY.md"
      via: "diff extraction + commit attribution"
      pattern: "Attribution:"
---

<objective>
Generate a complete, exhaustive inventory of every line changed in contracts/ between the v15.0 audit baseline (commit e2cd1b2b) and HEAD. Organize by contract file. Attribute each change block to its originating milestone or manual edit.

Purpose: DELTA-05 requires every changed line accounted for. This inventory is the foundation for Plan 02's function-level verdicts.
Output: 179-01-DIFF-INVENTORY.md -- per-contract diff with milestone attribution
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/179-change-surface-inventory/179-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract complete per-contract diff and attribute each change block to its originating milestone</name>
  <files>.planning/phases/179-change-surface-inventory/179-01-DIFF-INVENTORY.md</files>
  <read_first>
    - .planning/phases/179-change-surface-inventory/179-CONTEXT.md
  </read_first>
  <action>
Generate the full change inventory. The v15.0 baseline commit is e2cd1b2b.

**Step 1 — Extract the full diff:**
Run `git diff e2cd1b2b..HEAD -- contracts/` and capture the output. This covers ALL 33 changed contract files (766 added, 1002 deleted lines).

**Step 2 — Identify commit attribution for each file:**
For each changed contract file, run `git log --oneline e2cd1b2b..HEAD -- {file}` to see which commits touched it. Use these commit messages to attribute changes to milestones:

Commit-to-milestone mapping (use commit message prefixes):
- `ed057810`, `622cc1ae` = v16.0-repack (storage slot 0-2 repack, currentPrizePool helpers)
- `4f13ab83`, `0009d207` = v16.0-endgame-delete (EndgameModule elimination + consolidation into JackpotModule)
- `f730bc0c`, `f76473d6`, `32b36e4f`, `fc703475`, `76ea45d1` = v17.0-affiliate-cache (affiliate bonus cache + decimator rescale + gameover RNG fix)
- `3c3245d5`, `08e80cef` = v17.0-affiliate-cache (tiered affiliate bonus rate, terminal decimator rescale)
- `9c3e31bd` = v17.1-comments (comment correctness sweep)
- `f750be67`, `56869a24`, `d8fa9f33` = rngBypass-refactor (far-future ticket fix + rngBypass parameter)
- `cdac1bed`, `3ff2977c`, `e2f5f30f` = pre-v16.0-manual (vestigial removal, carryover gas, gameover revert)

**Step 3 — Write the inventory document:**
Create 179-01-DIFF-INVENTORY.md with this structure:

```
# Phase 179: Change Surface Inventory — Full Diff

**Baseline:** e2cd1b2b (v15.0 Phase 167 final)
**HEAD:** {current HEAD hash}
**Scope:** contracts/ directory only (33 files, 766 added / 1002 deleted)

## Summary

| Category | Files | Lines Changed |
|----------|-------|---------------|
| HEAVY (>50 lines) | 4 | {sum} |
| MEDIUM (10-50 lines) | 14 | {sum} |
| LIGHT (<10 lines) | 15 | {sum} |
| DELETED | 1 (EndgameModule) | 571 |

## Milestone Attribution Key

| Tag | Description | Commits |
|-----|-------------|---------|
| v16.0-repack | Storage slots 0-2 repack, currentPrizePool helpers | ed057810, 622cc1ae |
| v16.0-endgame-delete | EndgameModule elimination, functions moved to JackpotModule | 4f13ab83, 0009d207 |
| v17.0-affiliate-cache | Affiliate bonus cache, decimator rescale, gameover RNG | f730bc0c...76ea45d1 |
| v17.1-comments | Comment correctness sweep (56 fixes, 29 contracts) | 9c3e31bd |
| rngBypass-refactor | rngBypass parameter replaces phaseTransitionActive guard | f750be67, 56869a24, d8fa9f33 |
| pre-v16.0-manual | Vestigial removal, carryover gas, gameover revert fix | cdac1bed, 3ff2977c, e2f5f30f |

## Per-Contract Diff Inventory
```

For each of the 33 files, create a section:

```
### {ContractName.sol}
**Path:** contracts/{path}
**Lines:** +{added} / -{deleted}
**Attribution:** {milestone-tag(s)}

**Changes:**
{For each changed hunk, show the diff context with +/- lines}
{Tag each hunk with its attribution}
```

**HEAVY files (>50 lines changed) — show full function-level detail:**
1. `DegenerusGameEndgameModule.sol` — 571 lines deleted (v16.0-endgame-delete). Just note "ENTIRE FILE DELETED" with list of functions that were removed vs migrated.
2. `DegenerusGameJackpotModule.sol` — 503 lines changed. Show each function touched. Major additions are the migrated runRewardJackpots, rewardTopAffiliate functions from EndgameModule.
3. `DegenerusGameAdvanceModule.sol` — 141 lines changed. Show each function touched. Major change is rngBypass parameter addition.
4. `DegenerusGameStorage.sol` — 138 lines changed. Show storage slot layout changes.

**MEDIUM files (10-50 lines) — show hunk-level detail:**
List each changed section with attribution.

**LIGHT files (<10 lines) — show line-level detail:**
Show exact changed lines with attribution. Most are v17.1 comment-only fixes.

**Step 4 — Completeness check:**
At the end of the document, add a verification section:

```
## Completeness Verification

Total files in `git diff --stat`: 33
Files documented above: 33
Missing: 0

Total lines added: 766
Total lines deleted: 1002
```

Run `git diff e2cd1b2b..HEAD --stat -- contracts/ | grep '|' | wc -l` and confirm it equals the number of sections in the document.

**CRITICAL — per D-01:** Use git diff from e2cd1b2b to HEAD, NOT per-milestone diffs. The single diff catches any manual edits between milestones.
**CRITICAL — per D-02:** Organize by contract file, not by milestone. Attribution is a column/tag, not the grouping axis.
  </action>
  <verify>
    <automated>grep -c "^### " .planning/phases/179-change-surface-inventory/179-01-DIFF-INVENTORY.md</automated>
  </verify>
  <acceptance_criteria>
    - Document has exactly 33 contract file sections (one per changed file), verifiable by counting `### ` headings
    - Every section has an Attribution tag from the milestone key
    - Completeness Verification section at bottom confirms 33 files / 0 missing
    - HEAVY files (JackpotModule, AdvanceModule, Storage, EndgameModule) have function-level detail
    - LIGHT files show exact changed lines
    - No contract file from `git diff --stat e2cd1b2b..HEAD -- contracts/` is absent
  </acceptance_criteria>
  <done>179-01-DIFF-INVENTORY.md exists with 33 contract sections, every change attributed to a milestone, completeness verification confirms exhaustive coverage</done>
</task>

</tasks>

<verification>
1. `grep -c "^### " .planning/phases/179-change-surface-inventory/179-01-DIFF-INVENTORY.md` equals 33
2. `grep -c "Attribution:" .planning/phases/179-change-surface-inventory/179-01-DIFF-INVENTORY.md` equals 33
3. Every file from `git diff --stat e2cd1b2b..HEAD -- contracts/` has a matching section header
4. Document contains "Missing: 0" in completeness verification
</verification>

<success_criteria>
- Complete diff inventory exists with all 33 changed contract files documented
- Every change block attributed to its originating milestone
- HEAVY files have function-level detail showing which functions were added/modified/deleted
- Completeness verification at bottom confirms exhaustive coverage
</success_criteria>

<output>
After completion, create `.planning/phases/179-change-surface-inventory/179-01-SUMMARY.md`
</output>
