---
phase: 480-rename-sweep-gated-contract-diff-2-no-behavior-change
plan: 02
type: execute
wave: 2
depends_on: [480-01]
files_modified:
  - test/edge/DeityPassGoldNerfRegression.test.js
  - test/edge/LootboxAutoResolveRegression.test.js
  - test/edge/MintCleanupRegression.test.js
  - test/
autonomous: true
requirements: [RN-10]
tags: [tickets, entries, rename, harness, by-name, regression]

must_haves:
  truths:
    - "No renamed literal remains as a stale string anywhere under test/ (as a quoted string, .includes(...), === \"...\", or regex literal); the DeityPassGoldNerfRegression deriveStorageSlot string reads \"lvlTraitEntry\" and the LootboxAutoResolveRegression / MintCleanupRegression by-name reads read \"entriesOwedPacked\""
    - "The 479-FIX-05-reconciled .test.js source-string assertions that pin the _queueTickets(..., wholeTicketsToEntries(whole), ...) call form are updated to the _queueEntries(...) form (RN-03 renames the function, breaking those assertions only at JS runtime)"
    - "The full Hardhat suite is green at or above the 479-close floor (the forge-invisible runtime by-name class passes)"
    - "The full forge suite is green at or above the 479-close floor (1003/0/107); the Bernoulli/EV testers pass UNCHANGED (zero stat-test edit)"
    - "No event field-name assertion (ticketCount / futureTickets / TicketsQueued.quantity) is changed — those are 481 scope; only renamed internal-identifier literals are updated"
  artifacts:
    - path: "test/edge/DeityPassGoldNerfRegression.test.js"
      provides: "deriveStorageSlot string updated to the renamed storage label"
      contains: "deriveStorageSlot(\"lvlTraitEntry\")"
    - path: "test/edge/LootboxAutoResolveRegression.test.js"
      provides: ".includes by-name read updated to the renamed storage label"
      contains: "entriesOwedPacked"
    - path: "test/edge/MintCleanupRegression.test.js"
      provides: "cells[k] === by-name read updated to the renamed storage label"
      contains: "entriesOwedPacked"
  key_links:
    - from: "test/edge/DeityPassGoldNerfRegression.test.js deriveStorageSlot(\"lvlTraitEntry\")"
      to: "the renamed contracts/ storage label lvlTraitEntry"
      via: "the by-name slot derivation must match the new label for the slot test to pass"
      pattern: "deriveStorageSlot\\(\"lvlTraitEntry\"\\)"
    - from: "npm test (full Hardhat suite)"
      to: "the runtime by-name string-assertion class"
      via: "the ONLY signal that surfaces forge-invisible source-string breaks"
      pattern: "lvlTraitEntry|entriesOwedPacked|_queueEntries"
---

<objective>
Close the forge-invisible runtime by-name break that an identifier rename creates (the 479-FIX-05 trap): exhaustively sweep `test/` for EVERY renamed literal as a quoted string / `.includes(` / `=== "..."` / regex, update every hit to the new name, then run the FULL Hardhat suite + full forge suite to prove the rename is behavior-preserving end-to-end.

Purpose: `forge build` green ≠ done. Phase 479's FIX-05 gap-closure proved that a contract symbol-shape change breaks `indexOf`/`includes`/`===`/`match` source-string assertions across many `*.test.js` — which forge CANNOT see; they only fail when the Hardhat suite runs. A rename is the same hazard, larger: RN-03 renames `_queueTickets`->`_queueEntries`, and 479 itself reconciled several `.test.js` suites to pin the `_queueTickets(..., wholeTicketsToEntries(whole), ...)` source-string form — those exact assertions break again at JS runtime under this rename.

Output: every stale renamed literal in `test/` updated; full Hardhat + forge green at or above the 479-close floor; the Bernoulli/EV testers green UNCHANGED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/480-rename-sweep-gated-contract-diff-2-no-behavior-change/480-RESEARCH.md
@.planning/phases/480-rename-sweep-gated-contract-diff-2-no-behavior-change/480-VALIDATION.md
@.planning/phases/479-conv-value-fix-gated-contract-diff-1-behavior-change/479-02-SUMMARY.md
@.planning/phases/480-rename-sweep-gated-contract-diff-2-no-behavior-change/480-01-SUMMARY.md

<interfaces>
<!-- The renamed-literal sweep set + the named by-name files. RN-03 also renames the function names that 479 pinned as .test.js source strings. -->

Renamed literals to sweep `test/` for (each as a quoted string / .includes( / === "..." / regex / source-string assertion). Full target set = 480-RESEARCH §1A-1D (the ledger §10 binding scope — this is LARGER than the pre-§10 set: RN-06 Decimator-BET, RN-07 degenerette, RN-08 ticketQuantity all added):
  traitBurnTicket -> lvlTraitEntry
  ticketsOwedPacked -> entriesOwedPacked
  _queueTickets / _queueTicketsScaled / _queueTicketRange -> _queueEntries / _queueEntriesScaled / _queueEntryRange
  _budgetToTicketUnits -> _budgetToEntries
  WHALE_BONUS_TICKETS_PER_LEVEL / WHALE_STANDARD_TICKETS_PER_LEVEL / LAZY_PASS_TICKETS_PER_LEVEL / VAULT_PERPETUAL_TICKETS -> *_ENTRIES_*
  DecEntry / TerminalDecEntry / _decClaimableFromEntry / terminalDecEntries -> DecBet / TerminalDecBet / _decClaimableFromBet / terminalDecBets   (§10.7 — BET, dec-prefixed; NOT "record")
  _budgetToEntries / _degenerettePayout — if a JS test asserts these as a contract SOURCE STRING (`.includes("_fullTicketPayout")` etc.), update to the new name.
  TICKET_SCALE -> QTY_SCALE (RN-04 scale rename) — sweep `test/` for `TICKET_SCALE` as a contract SOURCE-STRING assertion and update. CAVEAT: `test/stat/*BernoulliEv*` (the 37 EV assertions) may hold their OWN JS `const TICKET_SCALE = 100` mirror — that is the test's independent value, NOT bound to the contract symbol; leave its EV math/values UNCHANGED (rename the JS const only if it is a contract source-string assertion, never alter an EV expected value). `\bTICKET_SCALE\b` excludes `AFKING_TICKET_SCALE` (KEPT).

SCOPE NOTE — param-only renames do NOT surface in JS (positional calls): the degenerette `amountPerTicket`/`ticketCount`/`customTicket` and `ticketQuantity` (RN-07/RN-08) are renamed in `.sol` CODE in Plan 480-01 (forge-side, the compile-break harness). Their JS-visible forms are EVENT fields / ABI args / the agent bet-input (`event.args.ticketCount`, `event.args.ticketQuantity`) which are **481 scope — do NOT sweep them here.** Only sweep them in JS if a test asserts them as a contract SOURCE STRING (rare). The Decimator-BET / storage-label / `_queueTickets*` / `_budgetToTicketUnits` sets below ARE the JS by-name targets.

Named runtime by-name files (480-RESEARCH §6(b) — forge-invisible, only the JS run catches them):
  test/edge/DeityPassGoldNerfRegression.test.js — deriveStorageSlot("traitBurnTicket") at :441,:552,:666,:790,:1109,:1244 -> "lvlTraitEntry"
  test/edge/LootboxAutoResolveRegression.test.js — .includes("ticketsOwedPacked") at :402,:403 -> "entriesOwedPacked"
  test/edge/MintCleanupRegression.test.js — cells[k] === "ticketsOwedPacked" at :550,:553 -> "entriesOwedPacked"

479-reconciled source-string assertions that RN-03 breaks again (the _queueTickets(...) call form 479-FIX-05 pinned — see 479-02-SUMMARY): re-check at minimum CrossSurfaceTicketMixing, JackpotTicketRollSilentColdBust, LootboxAutoResolveRegression, LootboxConsolation, LootboxAutoResolveSilentColdBust, LootboxAutoResolveRemByte, EventSurfaceUnification, LootboxWholeTicket — any that assert `_queueTickets(...)` / `_queueTicketsScaled(...)` as a literal must move to `_queueEntries(...)` / `_queueEntriesScaled(...)`. Do NOT trust this list as complete; trust the exhaustive `rg` sweep + the FULL suite run.

Comment-only refs (480-RESEARCH §6(c) — no break; refresh for consistency, comment-only): test/fuzz/MintModuleDivergenceAcrossSplit.t.sol (slot-8 hardcoder comment — the slot NUMBER survives, layout is stable), test/randTraitTicketRef.mjs, test/.../Phase264GasRegression.test.js, MintBatchDeterminism.test.js, DegenerusJackpots.test.js, PerPullEmptyBucketSkip.test.js, etc.

DO NOT TOUCH (481 scope / KEEP): event field-name assertions — `ticketCount`, `futureTickets`, `TicketsQueued.quantity`, `TicketsQueuedRange.ticketsPerLevel`; the agent's Degenerette `ticketCount` bet-input param (unrelated). The sweep set above is specific to the renamed INTERNAL identifiers and does not match these event field names.

479-close floor (do not regress): forge test 1003 passed / 0 failed / 107 skipped; the 37 Bernoulli EV-tester assertions (JackpotTicketRollBernoulliEv 16 + LootboxBernoulliEv 8 + LootboxAutoResolveBernoulliEv 13) green UNCHANGED (no stat-test edit). The pre-existing SurfaceRegression.test.js 5 failures are unrelated (guard byte-identical trait files) — not a 480 regression.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exhaustive runtime by-name test/ literal sweep (the forge-invisible class)</name>
  <files>test/edge/DeityPassGoldNerfRegression.test.js, test/edge/LootboxAutoResolveRegression.test.js, test/edge/MintCleanupRegression.test.js, test/</files>
  <read_first>
    - .planning/phases/480-rename-sweep-gated-contract-diff-2-no-behavior-change/480-RESEARCH.md (§6(b) the named runtime by-name files; §6(c) comment-only refs; §6 the FIX-05 lesson that forge does NOT catch this class)
    - .planning/phases/479-conv-value-fix-gated-contract-diff-1-behavior-change/479-02-SUMMARY.md (the Gap-closure section — the 7 suites whose _queueTickets(...) source-string assertions 479 reconciled; RN-03 breaks them again)
    - test/edge/DeityPassGoldNerfRegression.test.js (deriveStorageSlot("traitBurnTicket") ×6) ; test/edge/LootboxAutoResolveRegression.test.js (:402,:403 .includes) ; test/edge/MintCleanupRegression.test.js (:550,:553 ===)
    - .planning/phases/480-rename-sweep-gated-contract-diff-2-no-behavior-change/480-01-SUMMARY.md (the exact OLD->NEW set Plan 480-01 applied — the sweep targets must match it precisely)
  </read_first>
  <action>
    Run an exhaustive sweep of `test/` for EVERY renamed literal from the `<interfaces>` set, in all forms a JS/regex assertion can take: a bare quoted string, `.includes("...")`, `=== "..."`, `.match(/.../)`, `indexOf("...")`, and any source-string assertion that embeds a renamed `_queueTickets*` call form. For each hit, update the literal to the new name from the §1 / 480-01 mapping, preserving the assertion's strength and intent (rename the literal only — do not weaken or delete the check).

    Specifically: update the 3 named by-name files (DeityPassGoldNerfRegression.test.js `deriveStorageSlot("traitBurnTicket")` ×6 -> `"lvlTraitEntry"`; LootboxAutoResolveRegression.test.js `.includes("ticketsOwedPacked")` -> `"entriesOwedPacked"`; MintCleanupRegression.test.js `cells[k] === "ticketsOwedPacked"` -> `"entriesOwedPacked"`). Then re-check the 479-FIX-05-reconciled suites that pin the `_queueTickets(..., wholeTicketsToEntries(whole), ...)` source-string form (CrossSurfaceTicketMixing + the 7 gap-closure suites listed in 479-02-SUMMARY) and move every `_queueTickets(...)` / `_queueTicketsScaled(...)` literal to the `_queueEntries(...)` / `_queueEntriesScaled(...)` form — keep `wholeTicketsToEntries(whole)` exactly (it is a KEEP identifier).

    Refresh the comment-only refs (§6(c)) for consistency (slot-8 hardcoder comment in MintModuleDivergenceAcrossSplit.t.sol — the slot NUMBER survives since layout is stable; randTraitTicketRef.mjs; Phase264GasRegression.test.js; MintBatchDeterminism.test.js; DegenerusJackpots.test.js; PerPullEmptyBucketSkip.test.js; etc.) — comment text only, no logic change.

    Do NOT touch event field-name assertions (`ticketCount`, `futureTickets`, `TicketsQueued.quantity`, `TicketsQueuedRange.ticketsPerLevel`) — those are 481 scope. Do NOT edit any `test/stat/*BernoulliEv*` tester (EV tripwire). The sweep target set is the renamed INTERNAL identifiers only.
  </action>
  <verify>
    <automated>! rg -n "traitBurnTicket|ticketsOwedPacked|_budgetToTicketUnits|TicketUnits|DecEntry|TerminalDecEntry|_decClaimableFromEntry|terminalDecEntries" test/ && ! rg -n "_queueTickets\b|_queueTicketsScaled\b|_queueTicketRange\b" test/ && ! rg -n "WHALE_BONUS_TICKETS_PER_LEVEL|WHALE_STANDARD_TICKETS_PER_LEVEL|LAZY_PASS_TICKETS_PER_LEVEL|VAULT_PERPETUAL_TICKETS" test/ && rg -q 'deriveStorageSlot\("lvlTraitEntry"\)' test/edge/DeityPassGoldNerfRegression.test.js && rg -q "entriesOwedPacked" test/edge/LootboxAutoResolveRegression.test.js test/edge/MintCleanupRegression.test.js</automated>
  </verify>
  <acceptance_criteria>
    - No old renamed literal remains anywhere under `test/`: the no-stale greps for the storage labels, sink function names, constants, helper, and Decimator record names all return empty.
    - `test/edge/DeityPassGoldNerfRegression.test.js` reads `deriveStorageSlot("lvlTraitEntry")` (×6); `LootboxAutoResolveRegression.test.js` and `MintCleanupRegression.test.js` read `entriesOwedPacked`.
    - The 479-reconciled source-string assertions now read the `_queueEntries(...)` / `_queueEntriesScaled(...)` form with `wholeTicketsToEntries(whole)` preserved; no assertion was weakened or deleted.
    - Event field-name assertions (`ticketCount`/`futureTickets`/`TicketsQueued.quantity`) are UNCHANGED; no `test/stat/*BernoulliEv*` file was edited.
  </acceptance_criteria>
  <done>Every renamed literal in test/ is updated to the new name (the forge-invisible class), the 3 named by-name files + the 479-reconciled source-string suites are corrected, and event-field assertions + EV testers are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Full Hardhat + forge + stat green floor (no-behavior-change proof)</name>
  <files>test/</files>
  <read_first>
    - .planning/phases/480-rename-sweep-gated-contract-diff-2-no-behavior-change/480-VALIDATION.md (Full suite command + the green floor; Bernoulli/EV testers green UNCHANGED tripwire)
    - .planning/phases/479-conv-value-fix-gated-contract-diff-1-behavior-change/479-02-SUMMARY.md (the 1003/0/107 forge floor; the 37 Bernoulli EV assertions; the pre-existing SurfaceRegression 5 failures noted as unrelated)
  </read_first>
  <action>
    Run the FULL test suite to prove the rename is behavior-preserving end-to-end — this is the ONLY signal that surfaces the forge-invisible runtime by-name class. Run, in order: `npm test` (full Hardhat — the by-name class), `npm run test:stat` (the Bernoulli/EV testers), and `forge test` (the full forge floor). All must be green at or above the 479-close floor.

    If a suite fails on a stale renamed literal the Task-1 sweep missed, return to Task 1, update that literal, and re-run (the sweep + full suite iterate until green). If a suite fails for a NON-rename reason (e.g. the pre-existing SurfaceRegression.test.js 5 failures, which guard byte-identical trait files untouched by 480), confirm it is the SAME pre-existing failure set as the 479 close (not a new regression) and record it as carried, not a 480 break.

    The Bernoulli/EV testers (`test/stat/JackpotTicketRollBernoulliEv`, `LootboxBernoulliEv`, `LootboxAutoResolveBernoulliEv` = 37 assertions) MUST pass UNCHANGED — if any required an edit to pass, STOP: a rename disturbed a Bernoulli expression (a behavior change, out of scope).
  </action>
  <verify>
    <automated>npm test && npm run test:stat && forge test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` (full Hardhat) is green — the forge-invisible runtime by-name class passes; no stale renamed literal remains.
    - `forge test` is green at or above the 479-close floor (1003 passed / 0 failed / 107 skipped); any non-green is confirmed to be the SAME pre-existing failure set as the 479 close (e.g. SurfaceRegression.test.js), not a new 480 regression.
    - `npm run test:stat` — the 37 Bernoulli/EV assertions pass UNCHANGED (no `test/stat/*BernoulliEv*` edit in the diff).
    - The full forge + Hardhat green floor is met (no behavior change proven end-to-end).
  </acceptance_criteria>
  <done>The full Hardhat + forge + stat suites are green at or above the 479-close floor, the Bernoulli/EV testers pass UNCHANGED, and any residual red is a confirmed pre-existing non-rename failure — proving the rename sweep is behavior-preserving.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| forge-visible build ↔ forge-invisible JS runtime assertions | `forge build`/`--check` (Plan 480-01) cannot see `.test.js` source-string assertions; only `npm test` surfaces them — this plan owns that surface |
| renamed contract identifiers ↔ by-name test harnesses | `deriveStorageSlot("...")` / `.includes("...")` / source-string assertions hard-bind the old name; a missed update is a silent runtime failure, not a compile error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-480-03 | Tampering (forge-invisible runtime by-name break) | `.test.js` quoted-string / `.includes` / `=== "..."` / regex / `_queueTickets(...)` source-string assertions | mitigate | Exhaustive `rg test/` sweep for every renamed literal (Task 1) + the FULL Hardhat suite as the sole catching signal (Task 2); iterate sweep+suite until green. |
| T-480-06 | Tampering (assertion-strength erosion) | a by-name update that weakens or deletes a check instead of renaming the literal | mitigate | Acceptance asserts every updated assertion preserves its original strength/intent; the 479-reconciled source-string suites keep `wholeTicketsToEntries(whole)` and the positional emit==queue checks intact. |
| T-480-07 | Tampering (EV/Bernoulli drift) | a rename forcing an edit to a `test/stat/*BernoulliEv*` tester | mitigate | The 37 Bernoulli/EV assertions must pass UNCHANGED (no stat-test edit in the diff); STOP if an edit is required (a behavior change, out of scope). |
| T-480-SC | Tampering (supply chain) | npm/forge package installs | accept | No package installs in this phase — only `.test.js` literal updates + suite runs. The Package Legitimacy Gate does not apply. |
</threat_model>

<verification>
- No-stale-literal in `test/`: `rg` for each renamed identifier (storage labels, sinks, constants, helper, Decimator record names) returns empty.
- The 3 named by-name files updated: `deriveStorageSlot("lvlTraitEntry")`, `.includes("entriesOwedPacked")`, `cells[k] === "entriesOwedPacked"`.
- Full Hardhat green (`npm test`); full forge green at/above 1003/0/107 (`forge test`); Bernoulli/EV testers green UNCHANGED (`npm run test:stat`).
- Any residual red is a confirmed pre-existing non-rename failure (e.g. SurfaceRegression.test.js 5 failures) — not a 480 regression.
</verification>

<success_criteria>
- Maps to roadmap success criterion 5 (runtime half): by-name test harnesses updated in lockstep — the `DeityPassGoldNerfRegression` name string + the `.test.js` runtime by-name assertions + the slot-8 hardcoder comments refreshed; full Hardhat/forge/stat green.
- Closes RN-10 (runtime half): every renamed internal-identifier literal swept from `test/` JS; the full Hardhat + forge + stat suite green at/above the 479-close floor; the Bernoulli/EV testers green UNCHANGED — the rename is proven behavior-preserving end-to-end (the forge-invisible class included). The 481-scope event-field assertions (`ticketCount`/`futureTickets`/`ticketQuantity`/`TicketsQueued.quantity`) are left UNTOUCHED.
</success_criteria>

<output>
Create `.planning/phases/480-rename-sweep-gated-contract-diff-2-no-behavior-change/480-02-SUMMARY.md` when done. Record: the full set of `test/` files swept + the literals updated (especially any beyond the 3 named files that the exhaustive `rg` surfaced), the 479-reconciled source-string suites re-updated to the `_queueEntries(...)` form, the full Hardhat/forge/stat green results vs the 479-close floor, confirmation the Bernoulli/EV testers passed UNCHANGED, and any carried pre-existing non-rename failures (e.g. SurfaceRegression.test.js).
</output>
