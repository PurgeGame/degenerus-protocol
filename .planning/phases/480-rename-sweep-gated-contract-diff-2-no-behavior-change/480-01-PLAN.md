---
phase: 480-rename-sweep-gated-contract-diff-2-no-behavior-change
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/storage/DegenerusGameStorage.sol
  - contracts/DegenerusGame.sol
  - contracts/modules/DegenerusGameJackpotModule.sol
  - contracts/modules/DegenerusGameLootboxModule.sol
  - contracts/modules/DegenerusGameMintModule.sol
  - contracts/modules/DegenerusGameFoilPackModule.sol
  - contracts/modules/DegenerusGameBingoModule.sol
  - contracts/modules/DegenerusGameWhaleModule.sol
  - contracts/modules/DegenerusGameAdvanceModule.sol
  - contracts/modules/DegenerusGameDecimatorModule.sol
  - contracts/modules/GameAfkingModule.sol
  - contracts/interfaces/IDegenerusGameModules.sol
  - contracts/interfaces/IDegenerusGame.sol
  - contracts/test/LootboxBernoulliTester.sol
  - test/fuzz/JackpotSingleCallCorrectness.t.sol
  - test/gas/AdvanceGasCeiling.sol
  - test/gas/AdvanceStageWorstCaseGas.t.sol
  - test/gas/GameOverCompositionAdvanceGas.t.sol
  - scripts/layout/golden/
autonomous: false
requirements: [RN-01, RN-02, RN-03, RN-04, RN-05, RN-06, RN-07]

must_haves:
  truths:
    - "Every entries-denominated storage mapping, sink function (+ params), constant, helper, and misleading local carries an entry/entries name; no traitBurnTicket / ticketsOwedPacked / _queueTickets* / _budgetToTicketUnits / *TicketUnits / *_TICKETS_PER_LEVEL / VAULT_PERPETUAL_TICKETS / DecEntry / TerminalDecEntry / _decClaimableFromEntry / terminalDecEntries literal remains anywhere under contracts/"
    - "Storage layout is byte-stable: the recaptured goldens differ from baseline ONLY in label and typeLabel name strings; slot/offset/bytes/encoding byte-identical; storage_layout_oracle.sh --check exits 0"
    - "The 2 inline-asm .slot reads bind to lvlTraitEntry.slot at the unchanged slot number (FoilPackModule:782, MintModule:569)"
    - "The KEEP-set survives unchanged: AFKING_TICKET_SCALE, wholeTicketsToEntries, TICKET_SCALE, ticketQueue, the whole local, the Decimator 'External Entry Points' delegatecall comments, the event names/fields (JackpotTicketWin / LootBoxOpened / TicketsQueued / TicketsQueuedRange.ticketsPerLevel), and every external view selector"
    - "The RN-05 unit collision is resolved by scope: the sink scaled param is entriesScaled; the bug-site scaled-whole locals are wholeTicketsScaled/scaledWholeTickets; no logic line changes"
    - "forge build is green (contracts + the 4 compile-break harness files renamed in lockstep); npx hardhat compile is green"
    - "All contracts/*.sol renames ship as ONE batched USER-approved diff; no contracts/*.sol comment names a phase/plan ID or narrates history"
  artifacts:
    - path: "contracts/storage/DegenerusGameStorage.sol"
      provides: "renamed storage labels + sinks + struct types; entries-basis NatSpec"
      contains: "lvlTraitEntry"
    - path: "contracts/modules/DegenerusGameJackpotModule.sol"
      provides: "_budgetToEntries + *Entries locals + lvlTraitEntry_ param; no *TicketUnits"
      contains: "_budgetToEntries"
    - path: "contracts/modules/DegenerusGameWhaleModule.sol"
      provides: "WHALE_BONUS_ENTRIES_PER_LEVEL / WHALE_STANDARD_ENTRIES_PER_LEVEL / LAZY_PASS_ENTRIES_PER_LEVEL (values 40/2/4 unchanged)"
      contains: "WHALE_BONUS_ENTRIES_PER_LEVEL = 40"
    - path: "contracts/modules/DegenerusGameAdvanceModule.sol"
      provides: "VAULT_PERPETUAL_ENTRIES (value 16 unchanged)"
      contains: "VAULT_PERPETUAL_ENTRIES = 16"
    - path: "contracts/modules/DegenerusGameDecimatorModule.sol"
      provides: "burn-record renames (record/burnRecord); 'External Entry Points' comment intact"
      contains: "External Entry Points"
    - path: "scripts/layout/golden/DegenerusGame.json"
      provides: "recaptured golden — label/typeLabel-only change, no slot/offset/bytes/encoding move"
      contains: "lvlTraitEntry"
  key_links:
    - from: "DegenerusGameFoilPackModule.sol:782 / DegenerusGameMintModule.sol:569 (inline-asm)"
      to: "lvlTraitEntry storage slot"
      via: ".slot binds the renamed variable by name; slot number unchanged"
      pattern: "lvlTraitEntry\\.slot"
    - from: "scripts/layout/golden/ (recaptured)"
      to: "storage_layout_oracle.sh --check"
      via: "--check green after --capture; diff is label/typeLabel string only"
      pattern: "lvlTraitEntry"
    - from: "test/fuzz/JackpotSingleCallCorrectness.t.sol + test/gas/{AdvanceGasCeiling.sol,AdvanceStageWorstCaseGas.t.sol,GameOverCompositionAdvanceGas.t.sol}"
      to: "forge build green"
      via: "compile-break harness renamed in lockstep with the contract rename"
      pattern: "lvlTraitEntry"
---

<objective>
Make the ticket/entry distinction unmistakable in identifiers across all `contracts/*.sol`: rename the entries-denominated storage mappings, sink functions (+ params), constants (values unchanged), the canonical budget->entries helper, the misleading whole-into-entries locals, and resolve the Decimator "entry" (= burn record) terminology collision — WITHOUT any behavior change. Storage layout stays byte-stable (label/typeLabel name strings only; no slot/offset/bytes/encoding move). The 4 compile-break Solidity test files are renamed in lockstep so `forge build` is green, and all layout goldens are recaptured (`--capture`) with `--check` green.

Purpose: `ticketsOwedPacked.owed` is denominated in ENTRIES (1 whole ticket = 4 entries; 1 entry = price/4 = 1 NFT). Phase 479 fixed the ¼ under-delivery; this phase renames the misleadingly-named symbols so the unit is unmistakable at every call site and the bug class cannot recur. This is a pure identifier rename — no value, no logic, no event field, no selector, no `.slot` offset changes.

Output: ONE batched `contracts/*.sol` rename diff presented for USER approval before commit (contract-commit gate, success criterion 5), PLUS autonomous commits of the recaptured layout goldens and the lockstep-renamed compile-break harness files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/480-rename-sweep-gated-contract-diff-2-no-behavior-change/480-RESEARCH.md
@.planning/phases/480-rename-sweep-gated-contract-diff-2-no-behavior-change/480-VALIDATION.md
@.planning/v75-grounding/v75.0-ticket-entry-map.md
@.planning/phases/479-conv-value-fix-gated-contract-diff-1-behavior-change/479-02-SUMMARY.md

<interfaces>
<!-- The LOCKED canonical rename table (480-RESEARCH §1). Use these EXACT target names; do not invent or re-decide. -->
<!-- Exact file:line anchors are in 480-RESEARCH §2; the executor reads source for current state. Rename by IDENTIFIER, never by substring/sed. -->

LOCKED rename table (OLD -> NEW), per 480-RESEARCH §1:
  traitBurnTicket -> lvlTraitEntry                         (storage mapping label; RN-01)
  traitBurnTicket_ -> lvlTraitEntry_                       (Jackpot helper param, trailing _; RN-01)
  ticketsOwedPacked -> entriesOwedPacked                   (storage mapping label; RN-02)
  _queueTickets (param quantity) -> _queueEntries (param entries)                 (RN-03)
  _queueTicketsScaled (param quantityScaled = scaled-ENTRIES) -> _queueEntriesScaled (param entriesScaled)   (RN-03)
  _queueTicketRange (param ticketsPerLevel) -> _queueEntryRange (param entriesPerLevel)                       (RN-03)
  WHALE_BONUS_TICKETS_PER_LEVEL (=40) -> WHALE_BONUS_ENTRIES_PER_LEVEL            (value UNCHANGED; RN-04)
  WHALE_STANDARD_TICKETS_PER_LEVEL (=2) -> WHALE_STANDARD_ENTRIES_PER_LEVEL       (value UNCHANGED; RN-04)
  LAZY_PASS_TICKETS_PER_LEVEL (=4) -> LAZY_PASS_ENTRIES_PER_LEVEL                 (value UNCHANGED; RN-04)
  VAULT_PERPETUAL_TICKETS (=16) -> VAULT_PERPETUAL_ENTRIES                        (value UNCHANGED; RN-04)
  _budgetToTicketUnits -> _budgetToEntries                (canonical budget->entries helper; RN-05)
  *TicketUnits / ticketUnits locals (hold ENTRIES) -> *Entries / entries          (RN-05)
  bug-site quantityScaled / scaledTickets / countScaled (hold scaled-WHOLE-tickets) -> wholeTicketsScaled / scaledWholeTickets   (RN-05; see §3 collision)
  Decimator DecEntry -> DecRecord ; TerminalDecEntry -> TerminalDecRecord (struct TYPES)             (RN-06)
  Decimator terminalDecEntries -> terminalDecRecords (mapping label)                                 (RN-06)
  Decimator levelEntries -> levelRecords ; entryBurn -> recordBurn ; entrySub -> recordSub ; entryBucket -> recordBucket ; _decClaimableFromEntry -> _decClaimableFromRecord   (RN-06)

KEEP — do NOT rename (480-RESEARCH §1 KEEP-set):
  ticketQueue (holds addresses — fix comment only, not the name)
  AFKING_TICKET_SCALE (=400; documented dual-scale)
  wholeTicketsToEntries (the 479 canonical post-Bernoulli whole->entries converter)
  TICKET_SCALE (=100; a scaling factor, not a unit name)
  whole (bug-site local — after 479 it genuinely holds whole tickets; NOT misleading)
  decBurn (mapping name already says "burn"; only its DecEntry VALUE TYPE is the collision)
  The Decimator "External Entry Points" / "entry points" delegatecall-dispatch comments (§4 — NOT the price/4 unit)
  All event names/fields: JackpotTicketWin.ticketCount, LootBoxOpened.futureTickets, TicketsQueued.quantity, TicketsQueuedRange.ticketsPerLevel (481 scope)
  All external view selectors (no churn in any phase)

Layout-golden mechanics (verified — scripts/layout/normalize_layout.py emits {slot, offset, label, typeLabel, bytes, encoding}):
  - Renaming a storage MAPPING VARIABLE (traitBurnTicket / ticketsOwedPacked / terminalDecEntries) changes its `label` string only.
  - Renaming a STRUCT TYPE (DecEntry / TerminalDecEntry) changes the `typeLabel` STRING of the mappings whose value type references it (decBurn, terminalDecRecords) — e.g. "mapping(uint24 => struct DegenerusGameStorage.DecEntry...)" -> "...DecRecord...". This is layout-NEUTRAL (slot/offset/bytes/encoding identical) but it IS a typeLabel string change, so the older "label-only" phrasing is incomplete: the gate is "ONLY `label` and `typeLabel` name strings may move; slot/offset/bytes/encoding byte-identical".
  - Constants (RN-04) are `private constant` — NOT in storage layout — so they do not touch the goldens.
  - `--capture` rewrites all 25 goldens; only DegenerusGame.json + the 12 module goldens (which inherit DegenerusGameStorage) change. The standalone-contract goldens (Coinflip, DGNRS, sDGNRS, FLIP, GNRUS, WrappedWrappedXRP, DegenerusAffiliate, DegenerusQuests, DegenerusVaultShare, DegenerusDeityPass, DegenerusJackpots, DegenerusAdmin) must be byte-identical (no git diff).

Verification surfaces (two distinct):
  - `npx hardhat compile` compiles contracts/ only -> GREEN after the contract rename alone (foundry .t.sol test files are NOT compiled by hardhat).
  - `forge build` compiles contracts/ AND test/*.t.sol/*.sol -> RED until the 4 compile-break harness files are renamed in lockstep (Task 2).

Repo comment rules (contracts are frozen-at-deploy): comments describe what the code IS — no "renamed from"/history narration, no phase/plan IDs in contracts/*.sol, no future-proofing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the full contract identifier rename (RN-01 … RN-06) across all contracts/*.sol</name>
  <files>contracts/storage/DegenerusGameStorage.sol, contracts/DegenerusGame.sol, contracts/modules/DegenerusGameJackpotModule.sol, contracts/modules/DegenerusGameLootboxModule.sol, contracts/modules/DegenerusGameMintModule.sol, contracts/modules/DegenerusGameFoilPackModule.sol, contracts/modules/DegenerusGameBingoModule.sol, contracts/modules/DegenerusGameWhaleModule.sol, contracts/modules/DegenerusGameAdvanceModule.sol, contracts/modules/DegenerusGameDecimatorModule.sol, contracts/modules/GameAfkingModule.sol, contracts/interfaces/IDegenerusGameModules.sol, contracts/interfaces/IDegenerusGame.sol, contracts/test/LootboxBernoulliTester.sol</files>
  <read_first>
    - .planning/phases/480-rename-sweep-gated-contract-diff-2-no-behavior-change/480-RESEARCH.md (§1 LOCKED name table; §2 exact file:line reference inventory for every RN; §3 the RN-05 unit-collision landmine; §4 the Decimator "entry point" KEEP landmine; §9 condensed landmines)
    - contracts/storage/DegenerusGameStorage.sol (decls: traitBurnTicket :472, ticketsOwedPacked :524, sinks _queueTickets :641 / _queueTicketsScaled :690 / _queueTicketRange :742, wholeTicketsToEntries :680, DecEntry :1833 / decBurn :1866, TerminalDecEntry :1939 / terminalDecEntries :1947; bitfield comment labels :437-438)
    - contracts/modules/DegenerusGameJackpotModule.sol (lvlTraitEntry_ param refs; _budgetToTicketUnits :705 + *TicketUnits locals; bug-site quantityScaled/scaledTickets :2127-2135; bitfield comment :403-404)
    - contracts/modules/DegenerusGameLootboxModule.sol (bug-site scaledTickets/countScaled :1356-1402,:2176-2186 + NatSpec :2176)
    - contracts/modules/DegenerusGameFoilPackModule.sol (traitBurnTicket.slot :782) ; contracts/modules/DegenerusGameMintModule.sol (traitBurnTicket.slot :569; ticketsOwedPacked :336/:640/:1252-1265)
    - contracts/modules/DegenerusGameWhaleModule.sol (RN-04 const decls :126/:141/:144 + refs; sink callers) ; contracts/modules/DegenerusGameAdvanceModule.sol (VAULT_PERPETUAL_TICKETS :131 + refs :1623/:1629)
    - contracts/modules/DegenerusGameDecimatorModule.sol (RN-06 record renames :306/:342/:551/:806/:847 etc.; KEEP "External Entry Points" :122 + "entry points" :382)
    - contracts/test/LootboxBernoulliTester.sol (:71 stale NatSpec `///` mirror of the old _queueTickets call form — comment-only; Bernoulli expression byte-identical)
  </read_first>
  <action>
    Apply the LOCKED §1 rename table (mirrored in `<interfaces>`) across every file listed, using the §2 file:line inventory to find every reference. Rename by IDENTIFIER (precise whole-token match), never by blind substring/sed.

    RN-01 (`traitBurnTicket` -> `lvlTraitEntry`, ~30 refs / 8 files): the storage decl in Storage; all code reads in DegenerusGame.sol, BingoModule, JackpotModule (+ the helper param `traitBurnTicket_` -> `lvlTraitEntry_`); the 2 inline-asm `.slot` reads `mstore(0x20, traitBurnTicket.slot)` at FoilPackModule:782 and MintModule:569 (the assembler binds `.slot` by name -> they become `lvlTraitEntry.slot`; slot NUMBER unchanged); and the NatSpec/comment refs in DegenerusGame.sol, IDegenerusGameModules.sol, IDegenerusGame.sol, BingoModule, FoilPackModule, JackpotModule, MintModule, Storage.

    RN-02 (`ticketsOwedPacked` -> `entriesOwedPacked`, ~15 refs / 3 files): the storage decl + the sink reads/writes + NatSpec in Storage; DegenerusGame.sol; MintModule. State in NatSpec that the field is denominated in entries.

    RN-03 (sinks + params): `_queueTickets`(param `quantity` -> `entries`) -> `_queueEntries`; `_queueTicketsScaled`(param `quantityScaled` = scaled-ENTRIES -> `entriesScaled`) -> `_queueEntriesScaled`; `_queueTicketRange`(param `ticketsPerLevel` -> `entriesPerLevel`) -> `_queueEntryRange`. Rename the decls in Storage AND every caller (DegenerusGame.sol, AdvanceModule, JackpotModule, LootboxModule, MintModule, WhaleModule, DecimatorModule, GameAfkingModule). Correct the misleading "Queues whole tickets" / "Number of tickets" NatSpec to the entries basis. LANDMINE: `_queueTicketRange` emits `TicketsQueuedRange(..., entriesPerLevel)` — rename ONLY the local/param feeding the event; do NOT rename the `TicketsQueuedRange` EVENT or its field (481 scope). Also update the comment-only `///` mirror at contracts/test/LootboxBernoulliTester.sol:71 to the `_queueEntries(...)` form (comment text only; the Bernoulli collapse expression stays byte-identical — EV-tester tripwire).

    RN-04 (constants, VALUES UNCHANGED): `WHALE_BONUS_TICKETS_PER_LEVEL`(=40) -> `WHALE_BONUS_ENTRIES_PER_LEVEL`; `WHALE_STANDARD_TICKETS_PER_LEVEL`(=2) -> `WHALE_STANDARD_ENTRIES_PER_LEVEL`; `LAZY_PASS_TICKETS_PER_LEVEL`(=4) -> `LAZY_PASS_ENTRIES_PER_LEVEL` (WhaleModule); `VAULT_PERPETUAL_TICKETS`(=16) -> `VAULT_PERPETUAL_ENTRIES` (AdvanceModule). Keep every literal value identical. Correct the genesis literal comment at DegenerusGame.sol:225 to "16 entries (= 4 whole tickets) per level". KEEP `AFKING_TICKET_SCALE`(=400) untouched.

    RN-05 (canonical helper + entries locals + unit-collision disambiguation): `_budgetToTicketUnits` -> `_budgetToEntries` (decl :705 + calls :358/:396/:638/:748 + NatSpec :626/:889 in JackpotModule). Rename the entries-holding locals to carry `Entries`: `dailyTicketUnits` -> `dailyEntries`, `carryoverTicketUnits` -> `carryoverEntries`, `ticketUnits` -> `entries`/`*Entries`, `baseUnits` -> `baseEntries` (all in JackpotModule per §2). Update the packed-bitfield COMMENT LABELS at Storage:437-438 and Jackpot:403-404 (`dailyTicketUnits`/`carryoverTicketUnits` -> the new names) — comment labels only, NO packing/offset change. COLLISION (§3): the SAME token in a different scope holds the opposite unit — the bug-site locals `quantityScaled`/`scaledTickets`/`countScaled` (JackpotModule:2127/2133, LootboxModule:1356-1402/2181-2186) hold scaled-WHOLE-tickets, NOT scaled-entries; rename THOSE to `wholeTicketsScaled`/`scaledWholeTickets`. Keep the `whole` local exactly as `whole` (after 479 it correctly holds whole tickets). Fix the LootboxModule:2176 NatSpec ("Number of tickets x TICKET_SCALE") to "scaled whole-ticket count (whole x TICKET_SCALE), collapsed to entries at queue via wholeTicketsToEntries". Do NOT touch any Bernoulli collapse expression.

    RN-06 (Decimator "entry" = burn RECORD): rename the struct TYPES `DecEntry` -> `DecRecord` and `TerminalDecEntry` -> `TerminalDecRecord` (decls in Storage :1833/:1939 + every `DecEntry storage`/`DecEntry memory` local in DecimatorModule :153/:154/:306/:390/:546/:578); the mapping label `terminalDecEntries` -> `terminalDecRecords` (Storage :1947); the locals/fn `levelEntries` -> `levelRecords`, `entryBurn` -> `recordBurn`, `entrySub` -> `recordSub`, `entryBucket` -> `recordBucket`, `_decClaimableFromEntry` -> `_decClaimableFromRecord` (+ its NatSpec :535/:540). Update the generic-prose burn-record comments (DecimatorModule :116/:135/:385-386) from "entry" to "record". LANDMINE (§4): do NOT rename the function-dispatch comments "External Entry Points (delegatecall targets)" (:122) and "the single and batch entry points." (:382) — these describe delegatecall dispatch, NOT the unit. KEEP the `decBurn` mapping NAME (already "burn"; only its value type changes to `DecRecord`).

    Comment discipline (frozen-at-deploy contracts): every touched comment describes what the code IS — no "renamed from"/history narration, no phase/plan IDs, no future-proofing.

    Do NOT touch: any event/emit field name or emitted value; any external view selector; any `.slot` numeric offset; any Bernoulli/EV expression. If a rename appears to require editing logic, a value, an event, or a Bernoulli expression — STOP; that is out of scope (a rename hit behavior).
  </action>
  <verify>
    <automated>npx hardhat compile && ! rg -n "traitBurnTicket|ticketsOwedPacked|_budgetToTicketUnits|TicketUnits|DecEntry|TerminalDecEntry|_decClaimableFromEntry|terminalDecEntries" contracts/ && ! rg -n "_queueTickets\b|_queueTicketsScaled\b|_queueTicketRange\b" contracts/ && ! rg -n "WHALE_BONUS_TICKETS_PER_LEVEL|WHALE_STANDARD_TICKETS_PER_LEVEL|LAZY_PASS_TICKETS_PER_LEVEL|VAULT_PERPETUAL_TICKETS" contracts/ && rg -q "WHALE_BONUS_ENTRIES_PER_LEVEL = 40" contracts/modules/DegenerusGameWhaleModule.sol && rg -q "VAULT_PERPETUAL_ENTRIES = 16" contracts/modules/DegenerusGameAdvanceModule.sol && rg -q "lvlTraitEntry\.slot" contracts/modules/DegenerusGameFoilPackModule.sol contracts/modules/DegenerusGameMintModule.sol && rg -q "External Entry Points" contracts/modules/DegenerusGameDecimatorModule.sol && rg -q "AFKING_TICKET_SCALE = 400" contracts/modules/GameAfkingModule.sol && rg -q "function wholeTicketsToEntries" contracts/storage/DegenerusGameStorage.sol</automated>
  </verify>
  <acceptance_criteria>
    - `npx hardhat compile` exits 0.
    - No old renamed literal remains anywhere under contracts/ (code OR comment): the no-stale greps above for RN-01/02/05/06 storage+helper+struct names, RN-03 sink names, and RN-04 constants all return empty.
    - New names present at their decl sites: `lvlTraitEntry`, `entriesOwedPacked`, `_queueEntries`/`_queueEntriesScaled`/`_queueEntryRange`, `_budgetToEntries`, `WHALE_BONUS_ENTRIES_PER_LEVEL = 40`, `WHALE_STANDARD_ENTRIES_PER_LEVEL = 2`, `LAZY_PASS_ENTRIES_PER_LEVEL = 4`, `VAULT_PERPETUAL_ENTRIES = 16`, `DecRecord`/`TerminalDecRecord`/`terminalDecRecords`/`_decClaimableFromRecord`.
    - The 2 inline-asm reads are `lvlTraitEntry.slot` (FoilPackModule:782, MintModule:569).
    - KEEP-set intact: `AFKING_TICKET_SCALE = 400`, `wholeTicketsToEntries`, `TICKET_SCALE`, `ticketQueue`, the `whole` local, the Decimator "External Entry Points" comment, and no event/selector identifier changed (`JackpotTicketWin`/`LootBoxOpened`/`TicketsQueued`/`TicketsQueuedRange` still present with the same field names).
    - RN-05 collision resolved: the `_queueEntriesScaled` param is `entriesScaled`; the bug-site scaled-whole locals are `wholeTicketsScaled`/`scaledWholeTickets`; the `whole` local is unchanged.
    - No `contracts/*.sol` comment names a phase/plan ID or narrates history (manual scan of the diff; confirmed at the Task 3 gate).
  </acceptance_criteria>
  <done>The full §1 rename table is applied across all contracts/*.sol, the project compiles under hardhat, no stale literal remains, the KEEP-set survives, and no comment narrates history or a phase ID.</done>
</task>

<task type="auto">
  <name>Task 2: Lockstep compile-break harness rename (forge build green) + layout golden recapture (label/typeLabel-only)</name>
  <files>test/fuzz/JackpotSingleCallCorrectness.t.sol, test/gas/AdvanceGasCeiling.sol, test/gas/AdvanceStageWorstCaseGas.t.sol, test/gas/GameOverCompositionAdvanceGas.t.sol, scripts/layout/golden/</files>
  <read_first>
    - .planning/phases/480-rename-sweep-gated-contract-diff-2-no-behavior-change/480-RESEARCH.md (§5 layout-stability proof + golden sequence; §6(a) the compile-break Solidity test set — note 2 files the grounding map MISSED: AdvanceStageWorstCaseGas.t.sol, GameOverCompositionAdvanceGas.t.sol)
    - test/fuzz/JackpotSingleCallCorrectness.t.sol (:27,:52 traitBurnTicket code refs) ; test/gas/AdvanceGasCeiling.sol (:101) ; test/gas/AdvanceStageWorstCaseGas.t.sol (:45) ; test/gas/GameOverCompositionAdvanceGas.t.sol (:75)
    - scripts/layout/storage_layout_oracle.sh (--capture rewrites all goldens, default --check diffs live vs golden) ; scripts/layout/normalize_layout.py (emits slot/offset/label/typeLabel/bytes/encoding)
  </read_first>
  <action>
    Rename every renamed-production-identifier CODE reference in the 4 compile-break Solidity test files so `forge build` is green (these reference `traitBurnTicket[...]` as real code, so the build is RED until they are updated in lockstep with Task 1): JackpotSingleCallCorrectness.t.sol (:27,:52), AdvanceGasCeiling.sol (:101), AdvanceStageWorstCaseGas.t.sol (:45), GameOverCompositionAdvanceGas.t.sol (:75) — and any other code-level renamed identifier these files reference (run `forge build` and fix every compile error that names an old identifier; the build IS the oracle for this class). Identifier rename only — no test logic or assertion-strength change.

    Then recapture the layout goldens: run `scripts/layout/storage_layout_oracle.sh --capture`. Inspect `git diff scripts/layout/golden/`: the ONLY changed fields must be `label` strings (traitBurnTicket->lvlTraitEntry, ticketsOwedPacked->entriesOwedPacked, terminalDecEntries->terminalDecRecords) and `typeLabel` strings (the decBurn / terminalDecRecords mappings now reference DecRecord/TerminalDecRecord). NO `slot`, `offset`, `bytes`, or `encoding` field may move. Only DegenerusGame.json + the 12 module goldens (all inherit DegenerusGameStorage) may change; the standalone-contract goldens must be byte-identical (no diff). If ANY slot/offset/bytes/encoding field moved, or a standalone-contract golden changed — STOP: a rename touched real layout (do not proceed to the gate). Finally run `scripts/layout/storage_layout_oracle.sh` (default --check) and confirm it exits 0.

    Commit the autonomous artifacts (the 4 renamed harness files + the recaptured goldens) — these are NOT contracts/*.sol and are not gated. The contracts/*.sol diff is gated by Task 3.
  </action>
  <verify>
    <automated>forge build && scripts/layout/storage_layout_oracle.sh --capture >/dev/null 2>&1; test -z "$(git diff scripts/layout/golden/ | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -E '\"(slot|offset|bytes|encoding)\":')" && rg -q "lvlTraitEntry" scripts/layout/golden/DegenerusGame.json && scripts/layout/storage_layout_oracle.sh</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` exits 0 (the 4 compile-break harness files reference only the new identifiers).
    - `scripts/layout/storage_layout_oracle.sh --capture` rewrites the goldens; `git diff scripts/layout/golden/` shows changes ONLY in `label` and `typeLabel` string fields — zero `slot`/`offset`/`bytes`/`encoding` field changes (the grep for those fields in the diff returns empty).
    - Only DegenerusGame.json + the 12 Storage-inheriting module goldens changed; the 12 standalone-contract goldens are byte-identical (no diff).
    - `scripts/layout/storage_layout_oracle.sh` (default --check) exits 0.
    - `scripts/layout/golden/DegenerusGame.json` contains `lvlTraitEntry` (the recapture landed).
  </acceptance_criteria>
  <done>forge build is green with the harness renamed in lockstep, the layout goldens are recaptured with only label/typeLabel name-string changes, and --check is green — proving the rename did not move a single storage slot.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: USER approval of the batched contracts/*.sol rename diff before commit</name>
  <files>contracts/storage/DegenerusGameStorage.sol, contracts/DegenerusGame.sol, contracts/modules/DegenerusGameJackpotModule.sol, contracts/modules/DegenerusGameLootboxModule.sol, contracts/modules/DegenerusGameMintModule.sol, contracts/modules/DegenerusGameFoilPackModule.sol, contracts/modules/DegenerusGameBingoModule.sol, contracts/modules/DegenerusGameWhaleModule.sol, contracts/modules/DegenerusGameAdvanceModule.sol, contracts/modules/DegenerusGameDecimatorModule.sol, contracts/modules/GameAfkingModule.sol, contracts/interfaces/IDegenerusGameModules.sol, contracts/interfaces/IDegenerusGame.sol, contracts/test/LootboxBernoulliTester.sol</files>
  <read_first>
    - .planning/phases/480-rename-sweep-gated-contract-diff-2-no-behavior-change/480-VALIDATION.md (Manual-Only Verifications — the rename-only by-eye diff gate)
  </read_first>
  <action>Present the single batched contracts/*.sol rename diff (Task 1) to the USER for review and approval BEFORE committing. The repo gates ONLY the commit of contracts/*.sol; do not run the commit until the USER approves the diff (success criterion 5). The autonomous artifacts from Task 2 (the 4 harness files + recaptured goldens) commit separately and are NOT part of this gate. On approval, commit the contract files as ONE diff. Do not commit if the diff includes any unexpected file, any value/event/selector change, any Bernoulli-expression change, or any `.slot` numeric-offset change.</action>
  <what-built>
    A single batched `contracts/*.sol` IDENTIFIER-rename diff across 14 files (13 mainnet modules/interfaces + the contracts/test/LootboxBernoulliTester.sol comment-only NatSpec refresh): `traitBurnTicket`->`lvlTraitEntry` (incl. the 2 inline-asm `.slot` reads), `ticketsOwedPacked`->`entriesOwedPacked`, the three sinks `_queueTickets*`->`_queueEntries*` (+ params), the four `*_TICKETS_PER_LEVEL`/`VAULT_PERPETUAL_TICKETS` constants -> `*_ENTRIES_*` (values 40/2/4/16 UNCHANGED), `_budgetToTicketUnits`->`_budgetToEntries` + the `*TicketUnits` entries locals + the bug-site scaled-whole disambiguation (`wholeTicketsScaled`), and the Decimator burn-record collision (`DecEntry`/`TerminalDecEntry`/`_decClaimableFromEntry`/`entry*` locals -> record). NO behavior change: storage layout proven byte-stable (Task 2 golden recapture is label/typeLabel name-string only, --check green), the RNG/Bernoulli entropy path is untouched, every event field/value and external view selector is unchanged.
  </what-built>
  <how-to-verify>
    1. Run `git diff --stat contracts/` — confirm ONLY the 14 expected files changed (13 modules/interfaces + contracts/test/LootboxBernoulliTester.sol).
    2. Run `git diff contracts/` and confirm EVERY hunk is an identifier rename or a comment correction — there is NO value change (constants still = 40/2/4/16), NO `event`/`emit` field-name or emitted-value change, NO external view selector change, NO Bernoulli-collapse expression change, NO `.slot` numeric-offset change.
    3. Confirm the KEEP-set survives in the diff: `AFKING_TICKET_SCALE`, `wholeTicketsToEntries`, `TICKET_SCALE`, `ticketQueue`, the `whole` local, the Decimator "External Entry Points" comment, and the event names (`JackpotTicketWin`/`LootBoxOpened`/`TicketsQueued`/`TicketsQueuedRange`).
    4. Confirm no `contracts/*.sol` comment names a phase/plan ID (479/480/481/"Phase N") or narrates "renamed from"/history.
    5. Confirm the layout golden recapture (Task 2) diff was label/typeLabel-only and `storage_layout_oracle.sh --check` is green.
    6. Approve to commit the batched `.sol` rename diff (success criterion 5), or describe required changes.
  </how-to-verify>
  <resume-signal>Type "approved" to commit the batched contract rename diff, or describe required adjustments.</resume-signal>
  <verify>
    <human-check>USER has reviewed `git diff contracts/` and explicitly approved the batched .sol rename diff for commit; `git diff --stat contracts/` shows only the 14 expected files; the diff is identifier/comment-only with no value/event/selector/Bernoulli/.slot change.</human-check>
  </verify>
  <done>The batched contracts/*.sol rename diff is USER-approved and committed as one diff (or adjustments captured and re-presented).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract storage layout ↔ 155 delegatecall sites | a single slot/offset move on the shared DegenerusGameStorage layout is a whole-protocol corruption; the rename MUST be label/typeLabel-only |
| the gated contracts/*.sol commit ↔ USER approval | the sole gate; the USER confirms the diff is rename-only (no value/logic/event/selector change) before commit |
| forge-visible build ↔ forge-invisible JS runtime assertions | two distinct verification surfaces; `forge build`/`--check` cannot see the JS by-name string assertions (covered by Plan 480-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-480-01 | Tampering (storage layout) | a rename moving a slot/offset/type/size on the shared layout | mitigate | After `--capture`, `git diff scripts/layout/golden/` must change ONLY `label` and `typeLabel` name strings; zero `slot`/`offset`/`bytes`/`encoding` move; standalone-contract goldens byte-identical; `--check` exits 0. STOP if any non-name field moves. |
| T-480-02 | Tampering (semantic mis-rename / KEEP-set corruption) | a blind substring/sed rename hitting "Entry Points" comments, AFKING_TICKET_SCALE, the event fields (JackpotTicketWin/LootBoxOpened/TicketsQueued/TicketsQueuedRange.ticketsPerLevel), the `whole` local, or a Bernoulli expression | mitigate | Rename by IDENTIFIER only; the RN-05 unit-collision resolved by scope (sink param -> entriesScaled, bug-site -> wholeTicketsScaled); acceptance greps assert each KEEP item survives and each event identifier is unchanged. |
| T-480-03 | Tampering (forge-invisible runtime by-name break) | `.test.js` source-string / `deriveStorageSlot` / `.includes` / `=== "..."` assertions referencing the old names (the 479-FIX-05 trap) | mitigate | Out of scope for this plan's `forge build`/`--check` signals — covered by Plan 480-02 (exhaustive `rg test/` sweep + FULL Hardhat suite). Flagged here so the gate does NOT treat forge-green as done. |
| T-480-04 | Tampering (inline-asm slot binding) | the 2 `.slot` reads (FoilPackModule:782, MintModule:569) bind the variable by name | mitigate | The reads become `lvlTraitEntry.slot`; the layout golden proves the slot NUMBER is unchanged; acceptance grep confirms the asm reads the new name. |
| T-480-05 | Spoofing/Tampering (unauthorized contract change) | the gated contracts/*.sol commit could carry an unintended logic/value/event change | mitigate | The Task 3 blocking human-verify gate: USER reviews `git diff contracts/` (every hunk an identifier/comment change; no value/event/selector/Bernoulli/.slot change) before commit. |
| T-480-SC | Tampering (supply chain) | npm/forge package installs | accept | No package installs in this phase — only existing-dep identifier renames + golden recapture. The Package Legitimacy Gate does not apply. |
</threat_model>

<verification>
- `npx hardhat compile` green (contracts rename, Task 1); `forge build` green (contracts + 4 compile-break harness, Task 2).
- No-stale-literal: `rg "traitBurnTicket|ticketsOwedPacked|_budgetToTicketUnits|TicketUnits|DecEntry|TerminalDecEntry|_decClaimableFromEntry|terminalDecEntries" contracts/` and `rg "_queueTickets\b|_queueTicketsScaled\b|_queueTicketRange\b" contracts/` and `rg "WHALE_BONUS_TICKETS_PER_LEVEL|WHALE_STANDARD_TICKETS_PER_LEVEL|LAZY_PASS_TICKETS_PER_LEVEL|VAULT_PERPETUAL_TICKETS" contracts/` all return empty.
- KEEP-set: `AFKING_TICKET_SCALE = 400`, `wholeTicketsToEntries`, `TICKET_SCALE`, `ticketQueue`, the `whole` local, the Decimator "External Entry Points" comment, and every event name/field present and unchanged.
- Layout: `storage_layout_oracle.sh --capture` then `git diff scripts/layout/golden/` is label/typeLabel-only (no slot/offset/bytes/encoding move; standalone goldens byte-identical); `storage_layout_oracle.sh` (--check) exits 0.
- The definitive forge-invisible runtime by-name proof + the full-suite green floor land in Plan 480-02 (wave 2). This plan's signals (hardhat compile + forge build + no-stale greps + layout --check) are the necessary wave-1 guards.
- Bernoulli/EV testers are NOT edited here (NatSpec mirror at LootboxBernoulliTester.sol:71 only); they re-run green UNCHANGED in 480-02.
</verification>

<success_criteria>
- Maps to roadmap success criterion 1: `traitBurnTicket`->`lvlTraitEntry`, `ticketsOwedPacked`->`entriesOwedPacked`, `_queueTickets*`->`_queueEntries*` (+params), `_budgetToTicketUnits`->`_budgetToEntries`, `*_TICKETS_PER_LEVEL`/`VAULT_PERPETUAL_TICKETS`->`*_ENTRIES_*` (values unchanged), the misleading whole-into-entries locals renamed, and the 2 inline-asm `.slot` reads updated.
- Maps to criterion 2: Decimator "entry" (burn record) -> `record`/`burnRecord`; "entry" now means only the price/4 unit protocol-wide; the "External Entry Points" delegatecall comments survive.
- Maps to criterion 3: `forge inspect storageLayout` proves NO slot/offset/type move; all layout goldens recaptured (label/typeLabel-only) and `--check` green.
- Maps to criterion 4 (compile-break half): the 4 compile-break `.t.sol`/`.sol` harness files renamed in lockstep; `forge build` green. (The DeityPassGoldNerf name-string + runtime by-name half is Plan 480-02.)
- Maps to criterion 5: ONE batched `.sol` rename diff presented and USER-approved before commit (autonomous:false blocking checkpoint).
</success_criteria>

<output>
Create `.planning/phases/480-rename-sweep-gated-contract-diff-2-no-behavior-change/480-01-SUMMARY.md` when done. Record: the full OLD->NEW rename set applied (referencing §1), confirmation the 2 `.slot` reads + slot numbers are unchanged, the layout golden recapture result (which goldens changed; label/typeLabel-only proof; `--check` green), the `forge build` green confirmation after the lockstep harness rename, the KEEP-set survival, and the USER approval + commit SHA of the batched contract rename diff. Note that the forge-invisible runtime by-name sweep + full Hardhat suite is carried to Plan 480-02.
</output>
