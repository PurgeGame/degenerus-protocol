---
phase: 480-rename-sweep-gated-contract-diff-2-no-behavior-change
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/storage/DegenerusGameStorage.sol
  - contracts/DegenerusGame.sol
  - contracts/modules/DegenerusGameJackpotModule.sol
  - contracts/modules/DegenerusGameLootboxModule.sol
  - contracts/modules/DegenerusGameMintModule.sol
  - contracts/modules/DegenerusGameFoilPackModule.sol
  - contracts/modules/DegenerusGameBingoModule.sol
  - contracts/modules/DegenerusGameWhaleModule.sol
  - contracts/modules/DegenerusGameAdvanceModule.sol
  - contracts/modules/DegenerusGameDecimatorModule.sol
  - contracts/modules/DegenerusGameDegeneretteModule.sol
  - contracts/modules/GameAfkingModule.sol
  - contracts/DegenerusVault.sol
  - contracts/interfaces/IDegenerusGameModules.sol
  - contracts/interfaces/IDegenerusGame.sol
  - contracts/test/LootboxBernoulliTester.sol
  - contracts/test/JackpotBernoulliTester.sol  # TICKET_SCALE->QTY_SCALE mirror (Bernoulli math byte-identical)
  - test/  # the compile-break Solidity harness set (forge build oracle; re-derived by grep)
  - scripts/layout/golden/
autonomous: false
requirements: [RN-01, RN-02, RN-03, RN-04, RN-05, RN-06, RN-07, RN-08, RN-09, RN-10]

must_haves:
  truths:
    - "Every entry-COUNT/VALUE holder carries an entry/spin/traits/bet name per ledger §10 §1A-1D: no traitBurnTicket / ticketsOwedPacked / _queueTickets* / _budgetToTicketUnits / *TicketUnits / *_TICKETS_PER_LEVEL / VAULT_PERPETUAL_TICKETS / DecEntry / TerminalDecEntry / _decClaimableFromEntry / terminalDecEntries / amountPerTicket / customTicket / _fullTicketPayout / ticketQuantity literal remains as a code identifier under contracts/ (excluding the KEEP-set in §2 of 480-RESEARCH)"
    - "Decimator de-ticket uses BET not Record (§10.7): DecBet / TerminalDecBet / terminalDecBets / _decClaimableFromBet / decLevelBets / decBetBurn / decBetBucket / decBetSubBucket"
    - "ticketQuantity (=scaled ENTRIES ×100, §10.6) is renamed to entryQuantityScaled across all ~67 param/local refs EXCEPT the TicketsBought event field (481)"
    - "Storage layout is byte-stable: recaptured goldens differ from baseline ONLY in label and typeLabel name strings; slot/offset/bytes/encoding byte-identical; storage_layout_oracle.sh --check exits 0"
    - "The 2 inline-asm .slot reads bind to lvlTraitEntry.slot at the unchanged slot number (FoilPackModule:782, MintModule:569)"
    - "The KEEP-set survives unchanged: the activation-queue cluster (ticketQueue/processTicketBatch/...), the Jackpot Ticket-Jackpot mechanism names, AFKING_TICKET_SCALE, wholeTicketsToEntries, the whole local, the whole-ticket-leg wei/level labels (ticketCost/oneTicketWei/_activeTicketLevel/_callTicketPurchase), useTickets/FLAG_USE_TICKETS, the Decimator External Entry Points comments, every event name/field (481), and every external view selector (481)"
    - "TICKET_SCALE (=100) is renamed to QTY_SCALE across production + the two contracts/test/*BernoulliTester.sol mirrors (their own TICKET_SCALE=100 decls); the Bernoulli collapse expressions are byte-identical and the EV testers stay green UNCHANGED; AFKING_TICKET_SCALE (=400) is KEPT (its dual-scale comment reference to the inherited TICKET_SCALE=100 updates to QTY_SCALE=100)"
    - "The F1 unit collision is resolved by scope: the sink scaled param is entriesScaled; the two-bug-site scaled-whole locals (Jackpot:2127/2133, Lootbox:1356/2177) are wholeTicketsScaled; ZERO blanket quantityScaled→wholeTicketsScaled; no logic line changes"
    - "Comment-only doc fixes land (no behavior): F2 (JackpotTicketWin NatSpec), F3 (ticketsOwedView/getPlayerPurchases/getTickets @return), F5 (whalePassClaims), and the NFT scrub of the 4 Storage CONV-02 NatSpec refs"
    - "Degenerette renames are identifiers/params ONLY (amountPerSpin/spinCount/customTraits/*Traits/_degenerettePayout + Vault/interface lockstep); the Degenerette EVENTS, the FT_*_SHIFT constants, and the _packFullTicketBet body are UNTOUCHED (481/482)"
    - "npx hardhat compile green; forge build green (the compile-break Solidity harness set renamed in lockstep, re-derived by grep — forge build is the oracle, NOT a hardcoded file list)"
    - "All contracts/*.sol renames ship as ONE batched USER-approved diff; no contracts/*.sol comment names a phase/plan ID or narrates history"
  artifacts:
    - path: "contracts/storage/DegenerusGameStorage.sol"
      provides: "lvlTraitEntry + entriesOwedPacked + DecBet/TerminalDecBet + entries-basis NatSpec; NFT scrub"
      contains: "lvlTraitEntry"
    - path: "contracts/modules/DegenerusGameJackpotModule.sol"
      provides: "_budgetToEntries + *Entries locals + lvlTraitEntry_ param; bug-site wholeTicketsScaled; no *TicketUnits"
      contains: "_budgetToEntries"
    - path: "contracts/modules/DegenerusGameDecimatorModule.sol"
      provides: "DecBet/decLevelBets/decBetBurn/decBetBucket/decBetSubBucket; External Entry Points comment intact"
      contains: "External Entry Points"
    - path: "contracts/modules/DegenerusGameDegeneretteModule.sol"
      provides: "amountPerSpin/spinCount/customTraits/*Traits/_degenerettePayout; FT_*_SHIFT + _packFullTicketBet body UNTOUCHED"
      contains: "amountPerSpin"
    - path: "contracts/DegenerusVault.sol"
      provides: "entryQuantityScaled + degenerette forwarder params (amountPerSpin/spinCount/customTraits) in lockstep"
      contains: "entryQuantityScaled"
    - path: "contracts/modules/DegenerusGameWhaleModule.sol"
      provides: "WHALE_*_ENTRIES_PER_LEVEL (40/2/4 unchanged) + bonusEntries/standardEntries"
      contains: "WHALE_BONUS_ENTRIES_PER_LEVEL = 40"
    - path: "scripts/layout/golden/DegenerusGame.json"
      provides: "recaptured golden — label/typeLabel-only, no slot/offset/bytes/encoding move"
      contains: "lvlTraitEntry"
  key_links:
    - from: "DegenerusGameFoilPackModule.sol:782 / DegenerusGameMintModule.sol:569 (inline-asm)"
      to: "lvlTraitEntry storage slot"
      via: ".slot binds the renamed variable by name; slot number unchanged"
      pattern: "lvlTraitEntry\\.slot"
    - from: "scripts/layout/golden/ (recaptured)"
      to: "storage_layout_oracle.sh --check"
      via: "--check green after --capture; diff is label/typeLabel string only"
      pattern: "lvlTraitEntry"
    - from: "the compile-break Solidity harness set (test/**/*.sol — re-derived by grep)"
      to: "forge build green"
      via: "renamed code refs (traitBurnTicket[ / _queueTickets*( / ticketsOwedPacked[ / DecEntry / amountPerTicket / customTicket / ticketQuantity) updated in lockstep"
      pattern: "lvlTraitEntry|_queueEntries|entriesOwedPacked|DecBet|amountPerSpin|entryQuantityScaled"
---

<objective>
Make the ticket/entry distinction unmistakable in identifiers across all `contracts/*.sol` per the disambiguation ledger **§10** binding scope — WITHOUT any behavior change. Rename every entry-COUNT/VALUE holder (storage, sink functions + params, the four entry constants, the canonical budget→entries helper, the misleading whole-into-entries locals, the Decimator burn-BET set, the Degenerette ticket-relics, and `ticketQuantity`=scaled-entries); land the comment-only F2/F3/F5 + NFT-scrub doc fixes; KEEP every mechanism/subsystem/leg name (ledger §10.1). Storage layout stays byte-stable (label/typeLabel name strings only). The compile-break Solidity test files are renamed in lockstep so `forge build` is green, and all layout goldens are recaptured (`--capture`) with `--check` green.

Purpose: `ticketsOwedPacked.owed` is denominated in ENTRIES (1 whole ticket = 4 entries; 1 entry = price/4). Phase 479 fixed the ¼ under-delivery; this phase renames the misleadingly-named symbols so the unit is unmistakable and the bug class cannot recur. This is a pure identifier/comment rename — no value, no logic, no event field/name, no selector, no `.slot` offset, no Bernoulli expression.

Output: ONE batched `contracts/*.sol` rename diff presented for USER approval before commit (contract-commit gate, success criterion 6), PLUS autonomous commits of the recaptured layout goldens, the lockstep-renamed compile-break harness files, and the grounding-doc NFT scrub. (The runtime by-name `.test.js` sweep + the full-suite green floor is Plan 480-02.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/480-rename-sweep-gated-contract-diff-2-no-behavior-change/480-RESEARCH.md
@.planning/phases/480-rename-sweep-gated-contract-diff-2-no-behavior-change/480-VALIDATION.md
@.planning/v75-grounding/v75.0-ticket-entry-disambiguation-ledger.md
@.planning/phases/479-conv-value-fix-gated-contract-diff-1-behavior-change/479-02-SUMMARY.md

<interfaces>
<!-- The LOCKED canonical rename table = 480-RESEARCH §1A-1D (distilled from ledger §10). Use these EXACT target names; do not invent or re-decide. -->
<!-- Exact file:line anchors = 480-RESEARCH §5 (the ledger §10.4 grep-run sets). Rename by IDENTIFIER, never by substring/sed. -->

Rename groups (full table in 480-RESEARCH §1A-1D):
  §1A entries plumbing (RN-01..05):
    traitBurnTicket -> lvlTraitEntry (+ param traitBurnTicket_ -> lvlTraitEntry_; +2 inline-asm .slot)
    ticketsOwedPacked -> entriesOwedPacked
    _queueTickets(quantity) -> _queueEntries(entries) ; _queueTicketsScaled(quantityScaled=scaled-ENTRIES) -> _queueEntriesScaled(entriesScaled) ; _queueTicketRange(ticketsPerLevel) -> _queueEntryRange(entriesPerLevel) ; _activate10LevelPass(ticketsPerLevel) -> (entriesPerLevel)
    WHALE_BONUS/STANDARD/LAZY_PASS_TICKETS_PER_LEVEL + VAULT_PERPETUAL_TICKETS -> *_ENTRIES_* (values 40/2/4/16 UNCHANGED)
    _budgetToTicketUnits -> _budgetToEntries ; ticketUnits/dailyTicketUnits/carryoverTicketUnits/baseUnits -> entries/dailyEntries/carryoverEntries/baseEntries ; bonusTickets/standardTickets -> bonusEntries/standardEntries
    bug-site (Jackpot:2127/2133, Lootbox:1356/2177 ONLY) quantityScaled/scaledTickets/countScaled -> wholeTicketsScaled/scaledWholeTickets   [F1 — §3]
  §1B Decimator burn-BET (RN-06):
    DecEntry -> DecBet ; TerminalDecEntry -> TerminalDecBet ; terminalDecEntries -> terminalDecBets ; _decClaimableFromEntry -> _decClaimableFromBet
    levelEntries -> decLevelBets ; entryBurn -> decBetBurn ; entryBucket -> decBetBucket ; entrySub -> decBetSubBucket
  §1C Degenerette ticket-relics (RN-07):
    amountPerTicket -> amountPerSpin ; ticketCount -> spinCount ; customTicket -> customTraits ; _fullTicketPayout -> _degenerettePayout
    playerTicket -> playerTraits ; resultTicket -> resultTraits ; firstResultTicket -> firstResultTraits ; _countGoldQuadrants(ticket) -> (traits)
    Vault inline IGamePlayer (:50-52) + forwarder (:597-599) + IDegenerusGameModules:422 lockstep
  §1D ticketQuantity -> entryQuantityScaled (RN-08, ~67 refs Mint/Game/Vault/interfaces; the TicketsBought event FIELD stays for 481)

KEEP (do NOT rename — full list 480-RESEARCH §2):
  Activation-queue cluster (ticketQueue/ticketCursor/ticketLevel/ticketsFullyProcessed/ticketWriteSlot/ticketRedemptionOpen/_tq*Key/processTicketBatch/processFutureTicketBatch + Advance wrappers)
  Jackpot Ticket-Jackpot mechanism (_distributeTicketJackpot/_awardJackpotTickets/_jackpotTicketRoll/_randTraitTicket/dailyTicketBudgetsPacked/PURCHASE_PHASE_TICKET_MAX_WINNERS/isTicketJackpotDay/ticketIndex/ticketBasis/ticketConversionBps)
  Scale factors KEPT: AFKING_TICKET_SCALE (=400; comment ref to inherited scale → QTY_SCALE), TICKET_LCG_MULT, TICKET_MIN_BUYIN_WEI, TICKET_SLOT_BIT, TICKET_FAR_FUTURE_BIT. (TICKET_SCALE itself RENAMES → QTY_SCALE, see RN-04.)
  wholeTicketsToEntries ; the whole local ; FOIL_PACK_TICKETS(=10) ; ticketPrice/whole/frac/roundedUp (Bernoulli) ; the Lootbox whole-ticket reward path (LOOTBOX_TICKET_*_BPS/_ticketBudget/_lootboxTicketCount)
  Whole-ticket-leg labels: ticketCost/ticketWei/oneTicketWei/ticketFreshFlip/ticketRecycledFlip/ticketNextShare/ticketFutureShare/_activeTicketLevel/_callTicketPurchase/_removeFarFutureTickets/sellFarFutureTickets
  useTickets/FLAG_USE_TICKETS/isTicket (buy-mode flag) ; FOIL_PACK_ENTRIES(=16) ; decBurn mapping ; e.subBucket field
  Decimator "External Entry Points"/"entry point" delegatecall comments ; EntropyLib ; deity "virtual entry" raffle-slot comments
  Unpromoted GAP rows (KEEP): ticketsOut, _processOneTicketEntry
  ALL event names/fields (481) ; ALL external view selectors (481) ; the Degenerette FT_*_SHIFT constants + _packFullTicketBet body + dead mode bits (482)

Layout-golden mechanics (scripts/layout/normalize_layout.py emits {slot, offset, label, typeLabel, bytes, encoding}):
  - Renaming a storage MAPPING VARIABLE (traitBurnTicket / ticketsOwedPacked / terminalDecEntries) changes its `label` string only.
  - Renaming a STRUCT TYPE (DecEntry / TerminalDecEntry) changes the `typeLabel` STRING of the mappings whose value type references it (decBurn, terminalDecBets) — layout-NEUTRAL but a typeLabel string move. The gate: ONLY `label` and `typeLabel` name strings may move; slot/offset/bytes/encoding byte-identical.
  - Constants (RN-04) are `private constant` — NOT in storage layout — they do not touch goldens.
  - `--capture` rewrites all goldens; only DegenerusGame.json + the 12 Storage-inheriting module goldens may change. The 12 standalone-contract goldens must be byte-identical (no git diff).

Verification surfaces (two distinct):
  - `npx hardhat compile` compiles contracts/ only -> GREEN after the contract rename alone (foundry .t.sol/.sol test files are NOT compiled by hardhat).
  - `forge build` compiles contracts/ AND test/*.sol -> RED until the compile-break Solidity harness set is renamed in lockstep. The set is LARGER than the pre-§10 7-file list (RN-07 degenerette params + RN-08 ticketQuantity pull in ~20 .sol test files) — do NOT hardcode a list; re-derive by grep (480-RESEARCH §7(a)) and let `forge build` be the oracle.

Repo comment rules (frozen-at-deploy): comments describe what the code IS — no "renamed from"/history, no phase/plan IDs in contracts/*.sol, no future-proofing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the full contract identifier rename (RN-01 … RN-08) + the comment-only doc fixes (RN-09) across all contracts/*.sol</name>
  <files>contracts/storage/DegenerusGameStorage.sol, contracts/DegenerusGame.sol, contracts/modules/DegenerusGameJackpotModule.sol, contracts/modules/DegenerusGameLootboxModule.sol, contracts/modules/DegenerusGameMintModule.sol, contracts/modules/DegenerusGameFoilPackModule.sol, contracts/modules/DegenerusGameBingoModule.sol, contracts/modules/DegenerusGameWhaleModule.sol, contracts/modules/DegenerusGameAdvanceModule.sol, contracts/modules/DegenerusGameDecimatorModule.sol, contracts/modules/DegenerusGameDegeneretteModule.sol, contracts/modules/GameAfkingModule.sol, contracts/DegenerusVault.sol, contracts/interfaces/IDegenerusGameModules.sol, contracts/interfaces/IDegenerusGame.sol, contracts/test/LootboxBernoulliTester.sol</files>
  <read_first>
    - .planning/phases/480-rename-sweep-gated-contract-diff-2-no-behavior-change/480-RESEARCH.md (§1A-1D LOCKED name table; §2 KEEP-set; §3 the F1 unit-collision landmine; §4 the Decimator "entry point" KEEP landmine; §5 the ledger §10.4 grep-run reference inventory; §8 comment fixes; §9 condensed landmines)
    - .planning/v75-grounding/v75.0-ticket-entry-disambiguation-ledger.md §10 (the binding owner-decision scope — §10.1 governing principle, §10.2 reclassifications, §10.4 ref sets, §10.6 ticketQuantity, §10.7 Decimator-as-BET + 3 new locals + NFT scrub + TICKET_SCALE)
    - contracts/storage/DegenerusGameStorage.sol (decls: traitBurnTicket:472, ticketsOwedPacked:524, sinks :641/:690/:742, _activate10LevelPass param :1298, wholeTicketsToEntries:680, DecEntry:1833/decBurn:1866, TerminalDecEntry:1939/terminalDecEntries:1947; bitfield comment labels :437-438; CONV-02 NFT NatSpec :522/:634/:674/:679)
    - contracts/modules/DegenerusGameJackpotModule.sol (lvlTraitEntry_ param refs; _budgetToTicketUnits:705 + *TicketUnits locals; bug-site quantityScaled/scaledTickets :2127-2135; F2 NatSpec :72-77; bitfield comment :403-404)
    - contracts/modules/DegenerusGameLootboxModule.sol (bug-site scaledTickets/countScaled :1356-1402,:2176-2186 + NatSpec :2176)
    - contracts/modules/DegenerusGameDecimatorModule.sol (RN-06 bet renames :153/:306/:342/:346/:551/:806/:847 etc.; KEEP "External Entry Points" :122 + "entry points" :382)
    - contracts/modules/DegenerusGameDegeneretteModule.sol (RN-07: amountPerTicket/customTicket/ticketCount params + playerTicket:745/resultTicket:775/firstResultTicket:754/_countGoldQuadrants:748 + _fullTicketPayout; KEEP FT_*_SHIFT + _packFullTicketBet body)
    - contracts/DegenerusVault.sol (RN-08 ticketQuantity :551 + RN-07 inline IGamePlayer :50-52, forwarder :597-599)
    - contracts/modules/DegenerusGameWhaleModule.sol (RN-04 consts :126/:141/:144 + refs; bonusTickets/standardTickets :320-506; sink callers) ; DegenerusGameAdvanceModule.sol (VAULT_PERPETUAL_TICKETS :131 + :1623/:1629)
    - contracts/modules/DegenerusGameFoilPackModule.sol (traitBurnTicket.slot :782) ; DegenerusGameMintModule.sol (traitBurnTicket.slot :569; ticketsOwedPacked :336/:640/:1252-1265; ticketQuantity 29 refs; F3 view docs)
    - contracts/DegenerusGame.sol (traitBurnTicket :2434/:2517; ticketsOwedPacked :2092/:2539; ticketQuantity 15 refs; F3 @return ticketsOwedView:2087/getPlayerPurchases:2533/getTickets:2510; genesis 16 :225)
    - contracts/test/LootboxBernoulliTester.sol (:71 stale NatSpec `///` mirror — comment-only; Bernoulli expression byte-identical)
  </read_first>
  <action>
    Apply the LOCKED §1A-1D rename table (480-RESEARCH) across every file, using the §5 ledger §10.4 reference inventory to find every reference. **Rename by IDENTIFIER (precise whole-token match), never by blind substring/sed** — the §3/§4/§9 landmines (Decimator "entry point" comments, AFKING_TICKET_SCALE, the 481 event fields, the KEEP mechanism cluster) prove a blanket sed corrupts unrelated symbols.

    RN-01 `traitBurnTicket`→`lvlTraitEntry` (§5 set): the storage decl; all code reads (Game:2434,2517; Bingo:140; Jackpot:867,931,1130,1432,1442,1454,1467,1479,1626 + param `traitBurnTicket_`→`lvlTraitEntry_`); the 2 inline-asm `.slot` reads (FoilPack:782, Mint:569 → `lvlTraitEntry.slot`, slot NUMBER unchanged); NatSpec across Game/Bingo/FoilPack/Jackpot/Mint/Storage/interfaces.

    RN-02 `ticketsOwedPacked`→`entriesOwedPacked` (§5 set: Storage:524,660,669,704,732,763,772; Mint:336,640,1252,1263,1265; Game:2092,2539 — Advance has ZERO). NatSpec: field denominated in entries.

    RN-03 sinks + params: `_queueTickets`(quantity→entries)→`_queueEntries`; `_queueTicketsScaled`(quantityScaled=scaled-ENTRIES→**entriesScaled**)→`_queueEntriesScaled`; `_queueTicketRange`(ticketsPerLevel→entriesPerLevel)→`_queueEntryRange`; AND the second `ticketsPerLevel` param on `_activate10LevelPass`(Storage:1298)→`entriesPerLevel`. Decls in Storage + every caller (§5). Correct the misleading "Queues whole tickets"/"Number of tickets" NatSpec to entries. LANDMINE: `_queueEntryRange` emits `TicketsQueuedRange(...,entriesPerLevel)` — rename ONLY the local/param; do NOT rename the EVENT or its field (481). Update the comment-only `///` mirror at LootboxBernoulliTester.sol:71 to `_queueEntries(...)` (comment text only; Bernoulli collapse expression byte-identical — EV tripwire).

    RN-04 constants (VALUES UNCHANGED): `WHALE_BONUS_TICKETS_PER_LEVEL`(40)→`WHALE_BONUS_ENTRIES_PER_LEVEL`; `WHALE_STANDARD_TICKETS_PER_LEVEL`(2)→`WHALE_STANDARD_ENTRIES_PER_LEVEL`; `LAZY_PASS_TICKETS_PER_LEVEL`(4)→`LAZY_PASS_ENTRIES_PER_LEVEL` (Whale); `WHALE_PASS_TICKETS_PER_LEVEL`(2)→`WHALE_PASS_ENTRIES_PER_LEVEL` (Lootbox:210 decl, :1919 emit — the lootbox mirror of WHALE_STANDARD; ledger §4 puts the private constant in 480 with its twins, only the `LootBoxWhalePassJackpot.tickets` event FIELD is 481); `VAULT_PERPETUAL_TICKETS`(16)→`VAULT_PERPETUAL_ENTRIES` (Advance). Every literal identical. Genesis literal comment Game:225 → "16 entries (= 4 whole tickets) per level".

    RN-04 scale factor (owner 2026-06-29): rename `TICKET_SCALE`(=100)→`QTY_SCALE` — the canonical decl `DegenerusGameStorage.sol:157` + all ~47 production refs (Game/Lootbox/FoilPack/Jackpot/GameAfking/Mint) AND the two `contracts/test/*BernoulliTester.sol` local mirrors (each declares its own `TICKET_SCALE = 100` — Lootbox:21, Jackpot:29 — and uses it in the Bernoulli collapse). The rename is pure-identifier: the Bernoulli `/ QTY_SCALE` math is byte-identical to `/ TICKET_SCALE` (same value 100), so the EV testers stay green UNCHANGED — do NOT alter any rounding/collapse expression. `\bTICKET_SCALE\b` does NOT match `AFKING_TICKET_SCALE` (the `_` blocks the word boundary), so KEEP `AFKING_TICKET_SCALE`(=400); only update its dual-scale comment (GameAfking:164-167,663-664) where it references the inherited scale "TICKET_SCALE = 100" → "QTY_SCALE = 100" (and clarify :164 refers to AFKING_TICKET_SCALE = 400).

    RN-05 helper + entries locals + F1 disambiguation: `_budgetToTicketUnits`→`_budgetToEntries` (decl:705 + calls :358/:396/:638/:748 + NatSpec :626/:889). Entries locals `dailyTicketUnits`→`dailyEntries`, `carryoverTicketUnits`→`carryoverEntries`, `ticketUnits`→`entries`, `baseUnits`→`baseEntries` (Jackpot); `bonusTickets`→`bonusEntries`, `standardTickets`→`standardEntries` (Whale §10.7). Bitfield COMMENT LABELS at Storage:437-438 + Jackpot:403-404 (comment labels only, NO packing change). **F1 COLLISION (§3):** the bug-site locals `quantityScaled`/`scaledTickets`/`countScaled` at **Jackpot:2127/2133 + Lootbox:1356/2177 ONLY** (scaled-WHOLE) → `wholeTicketsScaled`/`scaledWholeTickets`. Keep `whole` as `whole`. **ZERO blanket `quantityScaled`→`wholeTicketsScaled`** (the sink param is `entriesScaled`). Fix Lootbox:2176 NatSpec to "scaled whole-ticket count (whole × QTY_SCALE), collapsed to entries at queue via wholeTicketsToEntries" (note the `× QTY_SCALE` reflects the RN-04 scale rename).

    RN-06 Decimator burn-BET (§10.7 — BET not Record, `dec` prefix): struct TYPES `DecEntry`→`DecBet`, `TerminalDecEntry`→`TerminalDecBet` (decls Storage:1833/:1939 + every `DecEntry storage`/`memory` local in Decimator); mapping `terminalDecEntries`→`terminalDecBets` (Storage:1947); `_decClaimableFromEntry`→`_decClaimableFromBet` (+ NatSpec); locals `levelEntries`→`decLevelBets`, `entryBurn`→`decBetBurn`, `entryBucket`→`decBetBucket`, `entrySub`→`decBetSubBucket` (= `e.subBucket`). Update burn-record prose comments (:116/:135/:385-386) "entry"→"bet". LANDMINE (§4): do NOT rename "External Entry Points (delegatecall targets)" (:122) / "the single and batch entry points." (:382). KEEP `decBurn` (only its value type → `DecBet`); KEEP the `e.subBucket` struct field name.

    RN-07 Degenerette ticket-relics (identifiers/params ONLY): `amountPerTicket`→`amountPerSpin`, `ticketCount`→`spinCount`, `customTicket`→`customTraits`, `_fullTicketPayout`→`_degenerettePayout`; the `*Traits` set `playerTicket`→`playerTraits`, `resultTicket`→`resultTraits`, `firstResultTicket`→`firstResultTraits`, `_countGoldQuadrants(ticket)`→`(traits)`. Lockstep: Vault inline `IGamePlayer` (:50-52), forwarder (:597-599), `IDegenerusGameModules:422`. **OUT (do NOT touch): the Degenerette EVENTS `FullTicketResolved`/`FullTicketResult` + fields (481); the `FT_*_SHIFT` constants + the `_packFullTicketBet` body + the dead mode/isRandom/hasCustom bits (482).** In 480 the `customTicket` LOCAL renames to `customTraits` but the read `(packed >> FT_TICKET_SHIFT)` keeps `FT_TICKET_SHIFT`.

    RN-08 `ticketQuantity`→`entryQuantityScaled` (§10.6 — it is scaled ENTRIES ×100): all ~67 PARAM/local refs across Mint(29)/Game(15)/Vault(10 incl. `gamePurchaseTicketsFlip`:551 + inline IGamePlayer)/IDegenerusGameModules(7)/IDegenerusGame(6). **OUT: the `TicketsBought.ticketQuantity` event FIELD (481).** KEEP the whole-ticket-leg wei labels `ticketCost`/`ticketWei`/`oneTicketWei`/`ticketFreshFlip`/`ticketRecycledFlip`/`ticketNextShare`/`ticketFutureShare`, `_activeTicketLevel`, `_callTicketPurchase`. (sDGNRS holds zero `ticketQuantity` refs — confirm by grep, nothing to do there.)

    RN-09 comment-only doc fixes (no behavior): **F2** Jackpot:72-77 "whole-ticket count"→"entries count"; **F3** Game:2087(ticketsOwedView)/:2533(getPlayerPurchases)/:2510(getTickets) @return "whole tickets owed"→"entries"; **F5** Storage:1155 whalePassClaims doc → "100 entries (100 levels × 1 entry per half-pass)"; **NFT scrub** the 4 CONV-02 NatSpec refs Storage:522/634/674/679 "1 entry = 1 minted NFT" → "price/4 = ¼ of a whole ticket (4 entries per whole ticket)". (The view-selector RENAMES + event RENAMES stay 481 — F3 is the @return text only.)

    Comment discipline: every touched comment describes what the code IS — no "renamed from"/history, no phase/plan IDs, no future-proofing.

    Do NOT touch: any event/emit field name or emitted value; any external view selector; any `.slot` numeric offset; any Bernoulli/EV expression; the FT_*_SHIFT constants / _packFullTicketBet body. If a rename appears to require editing logic, a value, an event, a selector, the FT_* packing, or a Bernoulli expression — STOP; that is out of 480.
  </action>
  <verify>
    <automated>npx hardhat compile && ! rg -n "traitBurnTicket|ticketsOwedPacked|_budgetToTicketUnits|DecEntry|TerminalDecEntry|_decClaimableFromEntry|terminalDecEntries|_fullTicketPayout" contracts/ && ! rg -n "\b(ticketUnits|baseUnits|dailyTicketUnits|carryoverTicketUnits|amountPerTicket|customTicket|levelEntries|entryBurn|entryBucket|entrySub)\b" contracts/ && ! rg -n "_queueTickets\b|_queueTicketsScaled\b|_queueTicketRange\b" contracts/ && ! rg -n "WHALE_BONUS_TICKETS_PER_LEVEL|WHALE_STANDARD_TICKETS_PER_LEVEL|WHALE_PASS_TICKETS_PER_LEVEL|LAZY_PASS_TICKETS_PER_LEVEL|VAULT_PERPETUAL_TICKETS" contracts/ && [ -z "$(rg -n '\bticketQuantity\b' contracts/ | grep -v 'event TicketsBought')" ] && ! rg -n "\bTICKET_SCALE\b" contracts/ && rg -q "QTY_SCALE = 100" contracts/storage/DegenerusGameStorage.sol && rg -q "WHALE_BONUS_ENTRIES_PER_LEVEL = 40" contracts/modules/DegenerusGameWhaleModule.sol && rg -q "WHALE_PASS_ENTRIES_PER_LEVEL = 2" contracts/modules/DegenerusGameLootboxModule.sol && rg -q "VAULT_PERPETUAL_ENTRIES = 16" contracts/modules/DegenerusGameAdvanceModule.sol && rg -q "lvlTraitEntry\.slot" contracts/modules/DegenerusGameFoilPackModule.sol contracts/modules/DegenerusGameMintModule.sol && rg -q "struct DecBet" contracts/storage/DegenerusGameStorage.sol && rg -q "amountPerSpin" contracts/modules/DegenerusGameDegeneretteModule.sol && rg -q "entryQuantityScaled" contracts/DegenerusGame.sol && rg -q "External Entry Points" contracts/modules/DegenerusGameDecimatorModule.sol && rg -q "AFKING_TICKET_SCALE = 400" contracts/modules/GameAfkingModule.sol && rg -q "function wholeTicketsToEntries" contracts/storage/DegenerusGameStorage.sol && rg -q "_packFullTicketBet" contracts/modules/DegenerusGameDegeneretteModule.sol</automated>
  </verify>
  <acceptance_criteria>
    - `npx hardhat compile` exits 0.
    - No old renamed code identifier remains under contracts/: the no-stale greps for RN-01/02/05/06/07 names, RN-03 sinks, and the four `*_TICKETS_PER_LEVEL` + `VAULT_PERPETUAL_TICKETS` constants all return empty (case-exact `\b…\b` for the lowercase locals `ticketUnits`/`baseUnits`/`amountPerTicket`/`customTicket`/`levelEntries`/`entryBurn`/`entryBucket`/`entrySub`).
    - RN-08 (`ticketQuantity`): EXACTLY ONE `ticketQuantity` survives — the `TicketsBought` event-field decl at `MintModule:165` (481 scope). The gate `[ -z "$(rg '\bticketQuantity\b' contracts/ | grep -v 'event TicketsBought')" ]` is empty; do NOT rename the event field (that would bleed a 481 ABI change into 480). All 67 param/local refs (incl. the `@param` NatSpec + the emit arg) are `entryQuantityScaled`.
    - New names present at decl sites: `lvlTraitEntry`, `entriesOwedPacked`, `_queueEntries`/`_queueEntriesScaled`/`_queueEntryRange`, `_budgetToEntries`, `WHALE_BONUS_ENTRIES_PER_LEVEL = 40`/`= 2`/`= 4`, `VAULT_PERPETUAL_ENTRIES = 16`, `struct DecBet`/`TerminalDecBet`/`terminalDecBets`/`_decClaimableFromBet`/`decLevelBets`, `amountPerSpin`/`spinCount`/`customTraits`/`_degenerettePayout`, `entryQuantityScaled`.
    - The 2 inline-asm reads are `lvlTraitEntry.slot` (FoilPack:782, Mint:569).
    - KEEP-set intact: `AFKING_TICKET_SCALE = 400`, `wholeTicketsToEntries`, the activation-queue cluster, the Jackpot mechanism names, `_packFullTicketBet` + the `FT_*_SHIFT` constants, the `whole` local, the Decimator "External Entry Points" comment, and no event/selector identifier changed. `TICKET_SCALE`→`QTY_SCALE` landed (`! rg "\bTICKET_SCALE\b" contracts/` empty; `QTY_SCALE = 100` present; the Bernoulli EV testers green UNCHANGED).
    - F1 resolved: the `_queueEntriesScaled` param is `entriesScaled`; the bug-site scaled-whole locals are `wholeTicketsScaled`/`scaledWholeTickets`; ZERO blanket `quantityScaled`→`wholeTicketsScaled`.
    - RN-09 doc fixes present (F2 "entries count", F3 @return "entries", F5 whalePassClaims, the 4 Storage NatSpec refs no longer say "1 entry = 1 minted NFT").
    - No `contracts/*.sol` comment names a phase/plan ID or narrates history (confirmed at the Task 3 gate).
  </acceptance_criteria>
  <done>The full §1A-1D rename table + the RN-09 comment fixes are applied across all contracts/*.sol, the project compiles under hardhat, no stale code identifier remains, the KEEP-set (incl. the 481 events/selectors and the 482 FT_* packing) survives, and no comment narrates history or a phase ID.</done>
</task>

<task type="auto">
  <name>Task 2: Lockstep compile-break Solidity harness rename (forge build green) + layout golden recapture (label/typeLabel-only) + grounding-doc NFT scrub</name>
  <files>test/ (the compile-break Solidity harness set — re-derived by grep), scripts/layout/golden/, .planning/v75-grounding/</files>
  <read_first>
    - .planning/phases/480-rename-sweep-gated-contract-diff-2-no-behavior-change/480-RESEARCH.md (§6 layout-stability proof + golden sequence; §7(a) the compile-break re-derivation greps — the class is now LARGER than 7 files because RN-07/RN-08 are in scope; §8 the NFT scrub)
    - scripts/layout/storage_layout_oracle.sh (--capture rewrites all goldens, default --check diffs live vs golden) ; scripts/layout/normalize_layout.py (emits slot/offset/label/typeLabel/bytes/encoding)
  </read_first>
  <action>
    **(a) Compile-break Solidity harness lockstep (forge build green).** Re-derive the set deterministically (do NOT trust any static list — RN-07 degenerette params + RN-08 ticketQuantity made it ~20 .sol files, not 7):
    - `rg -n '\b_queue(Tickets|TicketsScaled|TicketRange)\s*\(' test/ --glob '*.sol'`
    - `rg -n 'traitBurnTicket\[|ticketsOwedPacked\[' test/ --glob '*.sol'`
    - `rg -n '\b(DecEntry|TerminalDecEntry)\b|_decClaimableFromEntry\s*\(|terminalDecEntries\[|_budgetToTicketUnits|_fullTicketPayout' test/ --glob '*.sol'`
    - `rg -n '\bamountPerTicket\b|\bcustomTicket\b|\bticketQuantity\b' test/ --glob '*.sol'`
    Rename every CODE hit (indexing, calls, named-argument keys, locals mirroring the module) to the new name. `forge build` IS the final oracle: fix every `Undeclared identifier` / `member not found` until green. Known seed set (grep-verified at HEAD; re-confirm): fuzz/{JackpotSingleCallCorrectness,QueueDoubleBuffer,TicketEdgeCases,TicketRouting,TicketLifecycle,MintModuleDivergenceAcrossSplit,KeeperRewardRoutingSameResults,PrizeLegEntriesDelivery,DecimatorBountyRegression,RngFreezeAndRemovalProofs,RngLockDeterminism,DegeneretteFreezeResolution,DegeneretteV73SolvencyFuzz,DegeneretteHeroScore,KeeperFaucetResistance}.t.sol, fuzz/handlers/DegeneretteHandler.sol, gas/{AdvanceGasCeiling.sol,AdvanceStageWorstCaseGas,GameOverCompositionAdvanceGas,KeeperResolveBetWorstCaseGas}.t.sol, mutation/DegeneretteV73MutationKills.t.sol, halmos/Arithmetic.t.sol, fuzz/{EthInEvents,FarFutureSalvageSwap,PriceLookupInvariants}.t.sol. (Comment-only `.t.sol` refs do NOT break the build — they are the §7(c) comment-sweep owned by 480-02.) Identifier rename only — no test logic/assertion-strength change.

    **(b) Layout golden recapture.** Run `scripts/layout/storage_layout_oracle.sh --capture`. Inspect `git diff scripts/layout/golden/`: the ONLY changed fields must be `label` strings (traitBurnTicket→lvlTraitEntry, ticketsOwedPacked→entriesOwedPacked, terminalDecEntries→terminalDecBets) and `typeLabel` strings (the decBurn / terminalDecBets mappings now reference DecBet/TerminalDecBet). NO `slot`/`offset`/`bytes`/`encoding` may move. Only DegenerusGame.json + the 12 Storage-inheriting module goldens may change; the 12 standalone-contract goldens must be byte-identical. If ANY non-name field moved, or a standalone golden changed — STOP (a rename touched layout). Then run `scripts/layout/storage_layout_oracle.sh` (default --check) and confirm exit 0.

    **(c) Grounding-doc NFT scrub (§8 / §10.7).** Remove "NFT" from the v75 grounding docs (`.planning/v75-grounding/v75.0-ticket-entry-disambiguation-ledger.md`, `v75.0-ticket-entry-map.md`, `v75.0-naming-changes.html`, this phase's 480-RESEARCH/480-VALIDATION) where it describes an entry — an entry is a "jackpot entry", not an NFT. (No deity-pass cosmetic-NFT reference exists in these files to preserve.) Doc-only.

    Commit the autonomous artifacts (the renamed compile-break harness files + the recaptured goldens + the grounding-doc scrub) — NOT contracts/*.sol; not gated. The contracts/*.sol diff is gated by Task 3.
  </action>
  <verify>
    <automated>forge build && scripts/layout/storage_layout_oracle.sh --capture >/dev/null 2>&1; test -z "$(git diff scripts/layout/golden/ | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -E '\"(slot|offset|bytes|encoding)\":')" && rg -q "lvlTraitEntry" scripts/layout/golden/DegenerusGame.json && rg -q "DecBet" scripts/layout/golden/DegenerusGame.json && scripts/layout/storage_layout_oracle.sh</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` exits 0 (every compile-break `.sol` test file referencing a renamed code identifier updated; the §7(a) re-derivation greps over `test/ --glob '*.sol'` return no code hits).
    - `scripts/layout/storage_layout_oracle.sh --capture` rewrites the goldens; `git diff scripts/layout/golden/` shows changes ONLY in `label` and `typeLabel` string fields — zero `slot`/`offset`/`bytes`/`encoding` changes.
    - Only DegenerusGame.json + the 12 Storage-inheriting module goldens changed; the 12 standalone goldens byte-identical.
    - `scripts/layout/storage_layout_oracle.sh` (--check) exits 0; `DegenerusGame.json` contains `lvlTraitEntry` and `DecBet`.
    - The v75 grounding docs no longer describe an entry as an "NFT".
  </acceptance_criteria>
  <done>forge build is green with the (expanded) compile-break harness renamed in lockstep, the layout goldens are recaptured with only label/typeLabel name-string changes, --check is green (proving no slot moved), and the grounding-doc NFT scrub is done.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: USER approval of the batched contracts/*.sol rename diff before commit</name>
  <files>contracts/storage/DegenerusGameStorage.sol, contracts/DegenerusGame.sol, contracts/modules/DegenerusGameJackpotModule.sol, contracts/modules/DegenerusGameLootboxModule.sol, contracts/modules/DegenerusGameMintModule.sol, contracts/modules/DegenerusGameFoilPackModule.sol, contracts/modules/DegenerusGameBingoModule.sol, contracts/modules/DegenerusGameWhaleModule.sol, contracts/modules/DegenerusGameAdvanceModule.sol, contracts/modules/DegenerusGameDecimatorModule.sol, contracts/modules/DegenerusGameDegeneretteModule.sol, contracts/modules/GameAfkingModule.sol, contracts/DegenerusVault.sol, contracts/interfaces/IDegenerusGameModules.sol, contracts/interfaces/IDegenerusGame.sol, contracts/test/LootboxBernoulliTester.sol</files>
  <read_first>
    - .planning/phases/480-rename-sweep-gated-contract-diff-2-no-behavior-change/480-VALIDATION.md (Manual-Only Verifications — the rename-only by-eye diff gate)
  </read_first>
  <action>Present the single batched contracts/*.sol rename diff (Task 1) to the USER for review and approval BEFORE committing. The repo gates ONLY the commit of contracts/*.sol; do not run the commit until the USER approves the diff (success criterion 6). The autonomous artifacts from Task 2 commit separately and are NOT part of this gate. On approval, commit the contract files as ONE diff. Do not commit if the diff includes any unexpected file, any value/event/selector change, any FT_*-packing / Bernoulli-expression change, or any `.slot` numeric-offset change.</action>
  <what-built>
    A single batched `contracts/*.sol` IDENTIFIER+comment rename diff across 17 files (15 mainnet modules/interfaces/facade + the 2 `contracts/test/*BernoulliTester.sol` mirror refreshes — `TICKET_SCALE`→`QTY_SCALE` + the Lootbox `:71` NatSpec, Bernoulli math byte-identical): the entries plumbing (`traitBurnTicket`→`lvlTraitEntry` incl. 2 `.slot`, `ticketsOwedPacked`→`entriesOwedPacked`, `_queueTickets*`→`_queueEntries*` +params, the four `*_TICKETS_PER_LEVEL` (incl. `WHALE_PASS`)/`VAULT_PERPETUAL_TICKETS`→`*_ENTRIES_*` values 40/2/2/4/16 UNCHANGED, the dual-use scale factor `TICKET_SCALE`→`QTY_SCALE`, `_budgetToEntries` + entries locals + the F1 bug-site `wholeTicketsScaled` disambiguation), the Decimator burn-BET set (`DecBet`/`dec`-prefixed locals), the Degenerette ticket-relics (`amountPerSpin`/`spinCount`/`customTraits`/`*Traits`/`_degenerettePayout` + Vault/interface lockstep), `ticketQuantity`→`entryQuantityScaled` (67 refs), and the comment-only F2/F3/F5 + NFT-scrub NatSpec fixes. NO behavior change: storage layout proven byte-stable (Task 2 golden recapture label/typeLabel-only, --check green), the RNG/Bernoulli entropy path + the Degenerette FT_* packing untouched, every event field/name + external view selector unchanged, `AFKING_TICKET_SCALE` KEPT.
  </what-built>
  <how-to-verify>
    1. `git diff --stat contracts/` — confirm ONLY the 17 expected files changed (15 mainnet + the 2 BernoulliTester mirrors).
    2. `git diff contracts/` — confirm EVERY hunk is an identifier rename or a comment correction; NO value change (constants still 40/2/4/16), NO `event`/`emit` field-name or emitted-value change, NO external view selector change, NO `FT_*_SHIFT`/`_packFullTicketBet`-body change, NO Bernoulli-collapse expression change, NO `.slot` numeric-offset change.
    3. KEEP-set survives in the diff: the activation-queue cluster, the Jackpot mechanism names, `AFKING_TICKET_SCALE`, `wholeTicketsToEntries`, the `whole` local, the Decimator "External Entry Points" comment, the whole-ticket-leg wei labels, and the event names (`JackpotTicketWin`/`LootBoxOpened`/`TicketsQueued`/`FullTicketResolved`). `TICKET_SCALE`→`QTY_SCALE` is the only scale-factor rename (Bernoulli math byte-identical).
    4. No `contracts/*.sol` comment names a phase/plan ID or narrates "renamed from"/history.
    5. The layout golden recapture (Task 2) diff was label/typeLabel-only and `--check` is green.
    6. Approve to commit the batched `.sol` rename diff (success criterion 6), or describe required changes.
  </how-to-verify>
  <resume-signal>Type "approved" to commit the batched contract rename diff, or describe required adjustments.</resume-signal>
  <verify>
    <human-check>USER has reviewed `git diff contracts/` and explicitly approved the batched .sol rename diff for commit; `git diff --stat contracts/` shows only the 17 expected files; the diff is identifier/comment-only with no value/event/selector/FT_*-packing/Bernoulli/.slot change.</human-check>
  </verify>
  <done>The batched contracts/*.sol rename diff is USER-approved and committed as one diff (or adjustments captured and re-presented).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract storage layout ↔ 155 delegatecall sites | a single slot/offset move on the shared DegenerusGameStorage layout is a whole-protocol corruption; the rename MUST be label/typeLabel-only |
| the gated contracts/*.sol commit ↔ USER approval | the sole gate; the USER confirms the diff is rename-only (no value/logic/event/selector/FT_*-packing change) before commit |
| forge-visible build ↔ forge-invisible JS runtime assertions | two distinct verification surfaces; `forge build`/`--check` cannot see the JS by-name string assertions (covered by Plan 480-02) |
| 480 identifier scope ↔ 481 event/selector scope ↔ 482 FT_* repack scope | the rename must NOT bleed into the event fields, the view selectors, or the Degenerette packed-bet constants/body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-480-01 | Tampering (storage layout) | a rename moving a slot/offset/type/size on the shared layout | mitigate | After `--capture`, `git diff scripts/layout/golden/` changes ONLY `label`/`typeLabel` name strings; zero `slot`/`offset`/`bytes`/`encoding` move; standalone goldens byte-identical; `--check` exits 0. STOP if any non-name field moves. |
| T-480-02 | Tampering (semantic mis-rename / KEEP-set or scope corruption) | a blind substring/sed hitting "Entry Points" comments, AFKING_TICKET_SCALE/TICKET_SCALE, the 481 event fields, the 482 FT_*-packing, the activation-queue / Jackpot mechanism cluster, the `whole` local, or a Bernoulli expression | mitigate | Rename by IDENTIFIER only; F1 resolved by scope (sink param `entriesScaled`, two bug-sites `wholeTicketsScaled`); acceptance greps assert each KEEP item survives and each event/selector/FT_* identifier is unchanged. |
| T-480-03 | Tampering (forge-invisible runtime by-name break) | `.test.js` source-string / `deriveStorageSlot` / `.includes` / `=== "..."` assertions referencing the old names (the 479-FIX-05 trap) | mitigate | Out of scope for this plan's `forge build`/`--check` signals — covered by Plan 480-02 (exhaustive `rg test/` sweep + FULL Hardhat suite). Flagged so the gate does NOT treat forge-green as done. |
| T-480-04 | Tampering (inline-asm slot binding) | the 2 `.slot` reads bind the variable by name | mitigate | The reads become `lvlTraitEntry.slot`; the layout golden proves the slot NUMBER unchanged; acceptance grep confirms the asm reads the new name. |
| T-480-05 | Spoofing/Tampering (unauthorized contract change) | the gated contracts/*.sol commit could carry an unintended logic/value/event/FT_*-packing change | mitigate | The Task 3 blocking human-verify gate: USER reviews `git diff contracts/` before commit. |
| T-480-SC | Tampering (supply chain) | npm/forge package installs | accept | No package installs — only existing-dep identifier renames + golden recapture. |
</threat_model>

<verification>
- `npx hardhat compile` green (contracts rename + comment fixes, Task 1); `forge build` green (contracts + the re-derived compile-break Solidity harness set, Task 2).
- No-stale-code-identifier under contracts/: the §1A-1D names (storage/sinks/constants/helper/Decimator-BET/Degenerette/ticketQuantity) all return empty; the new names present at their decls.
- KEEP-set + scope-boundary: `AFKING_TICKET_SCALE = 400`, `wholeTicketsToEntries`, the activation-queue + Jackpot mechanism names, the whole-ticket-leg labels, the Decimator "External Entry Points" comment, every event name/field, every external view selector, and the `FT_*_SHIFT`/`_packFullTicketBet` body — all present and unchanged. `TICKET_SCALE`→`QTY_SCALE` landed (`! rg "\bTICKET_SCALE\b" contracts/`; Bernoulli EV testers green UNCHANGED).
- F1: `_queueEntriesScaled` param `entriesScaled`; the two bug-sites `wholeTicketsScaled`; ZERO blanket rename.
- Layout: `--capture` then `git diff scripts/layout/golden/` label/typeLabel-only (no slot/offset/bytes/encoding move; standalone goldens byte-identical); `--check` exits 0.
- Comment fixes F2/F3/F5 + NFT scrub present; the grounding-doc NFT scrub done.
- The definitive forge-invisible runtime by-name proof + the full-suite green floor land in Plan 480-02 (wave 2). This plan's signals are the necessary wave-1 guards.
- Bernoulli/EV testers NOT edited here (NatSpec mirror at LootboxBernoulliTester.sol:71 only); they re-run green UNCHANGED in 480-02.
</verification>

<success_criteria>
- Maps to roadmap success criterion 1: the full §10 rename (entries plumbing + Decimator BET + Degenerette relics + ticketQuantity) lands across all contracts/*.sol incl. the 2 inline-asm `.slot` reads.
- Maps to criterion 2: the KEEP-set survives (activation-queue, Jackpot mechanism, AFKING_TICKET_SCALE + the other scale factors, whole-ticket-leg labels, "External Entry Points", events/selectors/FT_*-packing); `TICKET_SCALE`→`QTY_SCALE` (the one owner-chosen scale rename).
- Maps to criterion 3: comment-only F2/F3/F5 + NFT-scrub fixes land; no behavior change.
- Maps to criterion 4: `forge inspect storageLayout` proves NO slot/offset/type move; all goldens recaptured (label/typeLabel-only) and `--check` green.
- Maps to criterion 5 (compile-break half): the re-derived compile-break Solidity harness set renamed in lockstep; `forge build` green. (The runtime by-name + full-suite half is Plan 480-02.)
- Maps to criterion 6: ONE batched `.sol` rename diff presented and USER-approved before commit (blocking checkpoint).
</success_criteria>

<output>
Create `.planning/phases/480-rename-sweep-gated-contract-diff-2-no-behavior-change/480-01-SUMMARY.md` when done. Record: the full OLD→NEW rename set applied (referencing §1A-1D), confirmation the 2 `.slot` reads + slot numbers are unchanged, the layout golden recapture result (which goldens changed; label/typeLabel-only proof; `--check` green), the `forge build` green confirmation after the (expanded) lockstep harness rename + the full re-derived compile-break file list, the KEEP-set + scope-boundary survival (events/selectors/FT_* untouched), the F2/F3/F5 + NFT-scrub doc fixes, and the USER approval + commit SHA of the batched contract rename diff. Note that the forge-invisible runtime by-name sweep + full Hardhat suite is carried to Plan 480-02.
</output>
