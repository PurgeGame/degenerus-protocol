---
phase: 180-storage-layout-configuration-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/180-storage-layout-configuration-verification/180-01-STORAGE-ADDRESSES.md
autonomous: true
requirements: [DELTA-02, DELTA-04]

must_haves:
  truths:
    - "All 13 DegenerusGameStorage inheritors have byte-identical storage layout"
    - "GAME_ENDGAME_MODULE has zero live consumers after EndgameModule deletion"
    - "Every ContractAddresses label has at least one live consumer contract"
  artifacts:
    - path: ".planning/phases/180-storage-layout-configuration-verification/180-01-STORAGE-ADDRESSES.md"
      provides: "Storage layout verification results and ContractAddresses alignment audit"
      contains: "VERDICT"
  key_links:
    - from: "forge inspect output"
      to: "180-01-STORAGE-ADDRESSES.md"
      via: "slot-by-slot comparison of all 13 contracts"
      pattern: "IDENTICAL|DIVERGENT"
---

<objective>
Verify storage layout identity across all 13 DegenerusGameStorage inheritors and audit ContractAddresses alignment after GAME_ENDGAME_MODULE removal.

Purpose: DELTA-02 requires proving no storage slot drift exists after v16.0 repack and v17.0/v17.1 changes. DELTA-04 requires proving every ContractAddresses label maps to a correct, live contract.
Output: 180-01-STORAGE-ADDRESSES.md with verdicts for both requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/180-storage-layout-configuration-verification/180-CONTEXT.md
@contracts/ContractAddresses.sol
</context>

<tasks>

<task type="auto">
  <name>Task 1: Storage layout verification across all 13 inheritors (DELTA-02)</name>
  <files>.planning/phases/180-storage-layout-configuration-verification/180-01-STORAGE-ADDRESSES.md</files>
  <read_first>
    contracts/storage/DegenerusGameStorage.sol
    contracts/ContractAddresses.sol
    .planning/phases/180-storage-layout-configuration-verification/180-CONTEXT.md
  </read_first>
  <action>
Run `forge inspect <Contract> storage-layout --json` on all 13 contracts that share the DegenerusGameStorage inheritance tree. The 13 contracts are:

**Base:**
1. DegenerusGameStorage

**Abstract intermediaries (inherit DegenerusGameStorage directly):**
2. DegenerusGamePayoutUtils
3. DegenerusGameMintStreakUtils

**Concrete modules (inherit DegenerusGameStorage directly):**
4. DegenerusGameAdvanceModule
5. DegenerusGameBoonModule
6. DegenerusGameGameOverModule
7. DegenerusGameLootboxModule

**Concrete modules (inherit via PayoutUtils):**
8. DegenerusGameJackpotModule
9. DegenerusGameDecimatorModule

**Concrete modules (inherit via MintStreakUtils):**
10. DegenerusGameWhaleModule
11. DegenerusGameMintModule
12. DegenerusGame

**Concrete module (inherits both PayoutUtils and MintStreakUtils):**
13. DegenerusGameDegeneretteModule

For each contract, extract the storage layout JSON. Compare every slot against DegenerusGameStorage as the baseline. For each contract, verify:
- Same number of storage slots (expected: 95)
- Every slot has identical: label, offset, slot number, type

Write the results into a new file `180-01-STORAGE-ADDRESSES.md` with:
- A summary table: Contract | Slots | Match (IDENTICAL/DIVERGENT) | Notes
- If ALL 13 are IDENTICAL: overall verdict "DELTA-02: VERIFIED -- all 13 inheritors byte-identical"
- If ANY diverge: mark as CRITICAL finding with full slot diff

Per D-01, D-02, D-03 from CONTEXT.md -- this re-confirms v16.0 Phase 172 verification still holds after v17.0/v17.1 changes.

Efficient approach: pipe each `forge inspect` JSON through python/jq to extract a hash or sorted slot list, then compare. If all hashes match, the diff is trivially empty.
  </action>
  <verify>
    <automated>grep -c "IDENTICAL" .planning/phases/180-storage-layout-configuration-verification/180-01-STORAGE-ADDRESSES.md</automated>
  </verify>
  <acceptance_criteria>
    - File 180-01-STORAGE-ADDRESSES.md exists
    - Contains exactly 13 contract rows in the storage layout table
    - Contains string "DELTA-02:" followed by "VERIFIED" or a finding
    - Each of the 13 contracts listed by name with slot count and match status
    - grep -c "DegenerusGameStorage\|DegenerusGamePayoutUtils\|DegenerusGameMintStreakUtils\|DegenerusGameAdvanceModule\|DegenerusGameBoonModule\|DegenerusGameGameOverModule\|DegenerusGameLootboxModule\|DegenerusGameJackpotModule\|DegenerusGameDecimatorModule\|DegenerusGameWhaleModule\|DegenerusGameMintModule\|DegenerusGame\b\|DegenerusGameDegeneretteModule" returns at least 13
  </acceptance_criteria>
  <done>All 13 inheritors verified with slot count and match status; DELTA-02 verdict written</done>
</task>

<task type="auto">
  <name>Task 2: ContractAddresses alignment audit (DELTA-04)</name>
  <files>.planning/phases/180-storage-layout-configuration-verification/180-01-STORAGE-ADDRESSES.md</files>
  <read_first>
    contracts/ContractAddresses.sol
    .planning/phases/180-storage-layout-configuration-verification/180-01-STORAGE-ADDRESSES.md
  </read_first>
  <action>
Append a ContractAddresses alignment section to 180-01-STORAGE-ADDRESSES.md. Read-only audit of ContractAddresses.sol -- NEVER modify the file (per user memory: user manages deploy addresses manually).

ContractAddresses.sol has these labels (lines 7-38):
- DEPLOY_DAY_BOUNDARY (uint48, not an address)
- VRF_KEY_HASH (bytes32, not an address)
- ICONS_32, GAME_MINT_MODULE, GAME_ADVANCE_MODULE, GAME_WHALE_MODULE, GAME_JACKPOT_MODULE, GAME_DECIMATOR_MODULE, GAME_ENDGAME_MODULE, GAME_GAMEOVER_MODULE, GAME_LOOTBOX_MODULE, GAME_BOON_MODULE, GAME_DEGENERETTE_MODULE
- COIN, COINFLIP, VAULT, AFFILIATE, JACKPOTS, QUESTS, GAME, SDGNRS, DGNRS, ADMIN, DEITY_PASS, WWXRP, STETH_TOKEN, LINK_TOKEN, GNRUS, CREATOR, VRF_COORDINATOR, WXRP

For each address label, run `grep -rn "ContractAddresses.<LABEL>" contracts/ --include="*.sol"` (excluding ContractAddresses.sol itself and .bak files) to find all consumers.

Per D-07, D-08, D-09 from CONTEXT.md:

1. **GAME_ENDGAME_MODULE (line 16):** Verify ZERO live consumers exist. The EndgameModule was deleted in v16.0. If any contract still references this label, that is a finding. Expected result: zero consumers -- the label is dead but harmless (library constant costs no gas, no runtime impact).

2. **All other labels:** Each should have at least one live consumer. Build a table: Label | Consumer Count | Key Consumers (first 3 files)

3. Write verdict:
   - "DELTA-04: VERIFIED -- GAME_ENDGAME_MODULE has 0 live consumers (dead label, no runtime impact); all other labels have active consumers"
   - OR flag any label with zero consumers as a finding (dead reference)
   - OR flag GAME_ENDGAME_MODULE with nonzero consumers as CRITICAL (stale reference to deleted module)
  </action>
  <verify>
    <automated>grep "DELTA-04:" .planning/phases/180-storage-layout-configuration-verification/180-01-STORAGE-ADDRESSES.md</automated>
  </verify>
  <acceptance_criteria>
    - 180-01-STORAGE-ADDRESSES.md contains a "ContractAddresses Alignment" section
    - Contains string "DELTA-04:" followed by "VERIFIED" or a finding
    - Contains string "GAME_ENDGAME_MODULE" with a consumer count (expected: 0)
    - Contains a table with at least 28 address labels (all labels from ContractAddresses.sol minus the 2 non-address constants)
    - grep "GAME_ENDGAME_MODULE" returns a line showing consumer count
  </acceptance_criteria>
  <done>Every ContractAddresses label audited for consumer count; GAME_ENDGAME_MODULE confirmed dead; DELTA-04 verdict written</done>
</task>

</tasks>

<verification>
- 180-01-STORAGE-ADDRESSES.md exists with both sections
- Contains "DELTA-02:" verdict line
- Contains "DELTA-04:" verdict line
- All 13 contracts listed in storage layout table
- All 28+ address labels listed in ContractAddresses table
- GAME_ENDGAME_MODULE explicitly addressed
</verification>

<success_criteria>
- Storage layout verified identical (or divergences flagged) across all 13 DegenerusGameStorage inheritors
- Every ContractAddresses label has a consumer audit with count
- GAME_ENDGAME_MODULE confirmed dead (0 consumers) or finding raised
- Both DELTA-02 and DELTA-04 have explicit verdicts
</success_criteria>

<output>
After completion, create `.planning/phases/180-storage-layout-configuration-verification/180-01-SUMMARY.md`
</output>
