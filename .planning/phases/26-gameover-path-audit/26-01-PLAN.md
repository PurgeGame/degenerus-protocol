---
phase: 26-gameover-path-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v3.0-gameover-core-distribution.md
autonomous: true
requirements:
  - GO-08
  - GO-01

must_haves:
  truths:
    - "Terminal decimator (death bet) resolution has an explicit PASS or FINDING verdict with file:line references"
    - "handleGameOverDrain 10%/90% distribution has an explicit PASS or FINDING verdict with file:line references"
    - "claimablePool mutations through the drain sequence are traced exhaustively with invariant verification at each step"
    - "All 5 open questions from research that relate to terminal decimator and drain (Q1 decBucketOffsetPacked collision, Q2 stBal staleness, Q4 terminal dec claim expiry, Q5 _processAutoRebuy) are resolved with explicit verdicts"
  artifacts:
    - path: "audit/v3.0-gameover-core-distribution.md"
      provides: "GO-08 and GO-01 audit verdicts"
      contains: "GO-08"
    - path: "audit/v3.0-gameover-core-distribution.md"
      provides: "GO-01 audit verdict"
      contains: "GO-01"
  key_links:
    - from: "contracts/modules/DegenerusGameDecimatorModule.sol"
      to: "audit/v3.0-gameover-core-distribution.md"
      via: "terminal decimator code section audited"
      pattern: "GO-08.*PASS|GO-08.*FINDING"
    - from: "contracts/modules/DegenerusGameGameOverModule.sol"
      to: "audit/v3.0-gameover-core-distribution.md"
      via: "handleGameOverDrain code audited"
      pattern: "GO-01.*PASS|GO-01.*FINDING"
---

<objective>
Audit the core GAMEOVER distribution path: terminal decimator (death bet) resolution and handleGameOverDrain fund distribution sequence.

Purpose: These are the two highest-risk audit targets -- the terminal decimator is ~280 lines of brand-new code with zero prior audit coverage, and handleGameOverDrain is the central fund distribution function that allocates 10% to decimator, 90% to terminal jackpot, and sweeps remainder to vault. Together they control all fund movement during GAMEOVER.

Output: audit/v3.0-gameover-core-distribution.md with PASS/FINDING verdicts for GO-08 and GO-01.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-gameover-path-audit/26-RESEARCH.md
@audit/v1.1-endgame-and-activity.md
@audit/v1.1-ECONOMICS-PRIMER.md
@.planning/PLAN-TERMINAL-DECIMATOR.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit terminal decimator integration (GO-08)</name>
  <files>audit/v3.0-gameover-core-distribution.md</files>
  <read_first>
    - contracts/modules/DegenerusGameDecimatorModule.sol (lines 749-1027 -- terminal decimator section)
    - contracts/modules/DegenerusGameGameOverModule.sol (full file -- 233 lines, dispatch at line 140)
    - contracts/storage/DegenerusGameStorage.sol (search for: lastTerminalDecClaimRound, decBucketOffsetPacked, terminalDecBurn, weightedBurn)
    - .planning/PLAN-TERMINAL-DECIMATOR.md (design spec for terminal decimator)
    - .planning/phases/26-gameover-path-audit/26-RESEARCH.md (open questions 1, 4, 5)
  </read_first>
  <action>
Audit the terminal decimator code path through GAMEOVER. Read DecimatorModule.sol lines 749-1027 line by line. Produce an audit report section for GO-08 following the C4A verdict format from prior phases:

```
### GO-08: Terminal Decimator Integration
**Verdict:** PASS | FINDING-[severity]
**Files:** [file:line-range]
**Summary:** [1-2 sentences]
**Trace:** [step-by-step execution trace]
**Recommendation:** [fix if FINDING, or "None" if PASS]
```

The audit MUST cover these specific items:

1. **runTerminalDecimatorJackpot** (DecimatorModule ~line 880-922):
   - Verify the double-resolution guard: `if (lastTerminalDecClaimRound.lvl == lvl) return poolWei;` -- confirm it prevents re-running for same level
   - Trace the return value (refund amount when no winners) -- verify it flows back to handleGameOverDrain correctly
   - Verify decBucketOffsetPacked write at ~line 914 and whether it can collide with normal decimator writes at ~line 341 for the same level (Research Open Question 1)
   - Verify the weighted burn calculation using _terminalDecMultiplierBps and time-weighted pro-rata share

2. **recordTerminalDecBurn** (DecimatorModule ~line 804-867):
   - Verify the eligibility window and deadline enforcement
   - Trace the burn recording and weighted burn calculation
   - Verify the bucket offset packing is correct

3. **claimTerminalDecimatorJackpot** (DecimatorModule ~line 930-937):
   - Verify claim eligibility checks (round matching, non-zero weighted burn)
   - Verify payout calculation: `amountWei = (poolWei * playerWeightedBurn) / totalWeightedBurn`
   - Verify _addClaimableEth call -- confirm it routes to the correct version that skips auto-rebuy during GAMEOVER (Research Open Question 5)
   - Verify lastTerminalDecClaimRound cannot be overwritten after GAMEOVER (Research Open Question 4)

4. **_terminalDecMultiplierBps** (DecimatorModule ~line 1000-1006):
   - Verify the discontinuity at day 10 (2.75x -> 2x regime change)
   - Verify underflow protection at daysRemaining=0 (should be gated by TerminalDecDeadlinePassed)
   - Verify boundary values: daysRemaining=1 returns 10000 (1x), daysRemaining=10 returns 20000 (2x), daysRemaining=120 returns 300000 (30x)

5. **claimablePool accounting**: Trace every claimablePool mutation through the terminal decimator path. Verify `balance >= claimablePool` invariant holds after each mutation.

Create the file audit/v3.0-gameover-core-distribution.md with:
- Title: "# GAMEOVER Path Audit: Core Distribution (GO-01, GO-08)"
- Executive Summary section (placeholder for GO-01 from Task 2)
- Severity Definitions (Critical/High/Medium/Low/Informational per C4A standards)
- "## GO-08: Terminal Decimator Integration" section with full verdict
- Open Questions Resolved section covering Research Q1, Q4, Q5 with explicit answers
  </action>
  <verify>
    <automated>grep -c "GO-08" audit/v3.0-gameover-core-distribution.md | grep -v "^0$" && grep -E "GO-08.*(PASS|FINDING)" audit/v3.0-gameover-core-distribution.md && grep -c "decBucketOffsetPacked" audit/v3.0-gameover-core-distribution.md | grep -v "^0$" && grep -c "claimTerminalDecimatorJackpot" audit/v3.0-gameover-core-distribution.md | grep -v "^0$" && grep -c "_terminalDecMultiplierBps" audit/v3.0-gameover-core-distribution.md | grep -v "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.0-gameover-core-distribution.md exists and contains "GO-08"
    - File contains "GO-08" followed by "PASS" or "FINDING" (explicit verdict)
    - File contains "decBucketOffsetPacked" (collision analysis from Q1)
    - File contains "claimTerminalDecimatorJackpot" (claim path audited)
    - File contains "_terminalDecMultiplierBps" (multiplier audited)
    - File contains "runTerminalDecimatorJackpot" with file:line reference
    - File contains "recordTerminalDecBurn" with file:line reference
    - File contains "Open Question" or "Research Q" section resolving Q1, Q4, Q5
    - File contains "claimablePool" trace through terminal decimator path
  </acceptance_criteria>
  <done>GO-08 has an explicit PASS or FINDING verdict with file:line references for all 4 terminal decimator functions. Research open questions 1, 4, and 5 are resolved with explicit answers. claimablePool invariant is traced through the terminal decimator path.</done>
</task>

<task type="auto">
  <name>Task 2: Audit handleGameOverDrain distribution sequence (GO-01)</name>
  <files>audit/v3.0-gameover-core-distribution.md</files>
  <read_first>
    - contracts/modules/DegenerusGameGameOverModule.sol (full file -- lines 68-164 for handleGameOverDrain)
    - contracts/modules/DegenerusGameJackpotModule.sol (search for: runTerminalJackpot, _distributeJackpotEth -- lines ~288-324 and ~1537-1576)
    - contracts/DegenerusGame.sol (search for: claimWinnings, claimablePool -- lines ~1415-1447)
    - contracts/storage/DegenerusGameStorage.sol (search for: claimablePool, gameOver, gameOverTime, gameOverFinalJackpotPaid, _sendToVault)
    - audit/v3.0-gameover-core-distribution.md (read current state from Task 1)
    - .planning/phases/26-gameover-path-audit/26-RESEARCH.md (open question 2: stBal staleness)
  </read_first>
  <action>
Audit the handleGameOverDrain distribution sequence. Read GameOverModule.sol lines 68-164 line by line and trace every fund movement and claimablePool mutation. Append the GO-01 section to audit/v3.0-gameover-core-distribution.md.

The audit MUST cover these specific items:

1. **Level aliasing** (GameOverModule ~line 72):
   - `uint24 lvl = currentLevel == 0 ? 1 : currentLevel;`
   - Verify this causes terminal jackpot to target level 2 (lvl+1) when GAMEOVER fires at level 0
   - Confirm this is BY DESIGN per economics primer (Research Pitfall 4)
   - Document as known behavior, not a finding

2. **Deity refund section** (GameOverModule ~lines 78-107):
   - Verify budget = totalFunds - claimablePool is safe (no underflow)
   - Verify 20 ETH per pass, FIFO ordering by deityPassOwners array
   - Verify budget cap prevents over-allocation
   - Verify claimablePool += totalRefunded maintains invariant
   - Note: unchecked arithmetic in refund loop (Research Q3) -- verify claimableWinnings overflow impossible

3. **Terminal state setting** (GameOverModule ~lines 112-113):
   - Verify gameOver = true and gameOverTime set BEFORE external calls (CEI)
   - Verify prize pool zeroing

4. **Terminal decimator dispatch** (GameOverModule ~lines 138-146):
   - Verify 10% allocation: `decPool = remaining / 10`
   - Verify refund handling: `decSpend = decPool - decRefund; claimablePool += decSpend`
   - Verify remaining adjustment: `remaining -= decPool; remaining += decRefund`
   - Verify the delegatecall dispatch via `IDegenerusGame(address(this)).runTerminalDecimatorJackpot`

5. **Terminal jackpot dispatch** (GameOverModule ~lines 151-159):
   - Read JackpotModule `runTerminalJackpot` (~lines 288-324) and `_distributeJackpotEth` (~lines 1537-1576)
   - Verify 90% + refund allocation goes to terminal jackpot
   - Verify claimablePool is updated INSIDE _distributeJackpotEth (not double-counted)
   - Verify undistributed remainder -> _sendToVault
   - Verify stBal staleness concern (Research Q2): confirm no delegatecall module transfers stETH

6. **sDGNRS pool token burn** (GameOverModule ~line 162):
   - Verify dgnrs.burnRemainingPools() call
   - Verify it cannot revert and block distribution

7. **claimablePool invariant trace**: After each of the 5 mutation steps above, verify `address(this).balance + stETH >= claimablePool` still holds.

Write the "## GO-01: handleGameOverDrain Distribution" section with full verdict, file:line references, and step-by-step claimablePool trace. Also update the Executive Summary with both GO-01 and GO-08 results. Resolve Research Open Question 2 (stBal staleness) and Research Open Question 3 (unchecked deity refund arithmetic).
  </action>
  <verify>
    <automated>grep -E "GO-01.*(PASS|FINDING)" audit/v3.0-gameover-core-distribution.md && grep -c "handleGameOverDrain" audit/v3.0-gameover-core-distribution.md | grep -v "^0$" && grep -c "claimablePool" audit/v3.0-gameover-core-distribution.md | grep -v "^0$" && grep "Executive Summary" audit/v3.0-gameover-core-distribution.md && grep -c "_sendToVault" audit/v3.0-gameover-core-distribution.md | grep -v "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.0-gameover-core-distribution.md contains "GO-01" followed by "PASS" or "FINDING"
    - File contains "handleGameOverDrain" with file:line references (GameOverModule.sol:68-164)
    - File contains "claimablePool" at least 10 times (exhaustive trace)
    - File contains "Executive Summary" section with both GO-01 and GO-08 verdicts
    - File contains "_sendToVault" analysis
    - File contains "runTerminalJackpot" with file:line reference to JackpotModule
    - File contains "_distributeJackpotEth" with file:line reference
    - File contains "burnRemainingPools" analysis
    - File contains resolution of Research Q2 (stBal staleness) and Q3 (unchecked arithmetic)
    - File contains "level aliasing" or "lvl = currentLevel == 0 ? 1" documentation
  </acceptance_criteria>
  <done>GO-01 has an explicit PASS or FINDING verdict with file:line references for the entire handleGameOverDrain sequence. claimablePool invariant is traced through every mutation step. Research open questions 2 and 3 are resolved. Executive summary reflects both GO-01 and GO-08 verdicts.</done>
</task>

</tasks>

<verification>
- audit/v3.0-gameover-core-distribution.md exists with GO-08 and GO-01 sections
- Both verdicts are explicit PASS or FINDING-[severity] with file:line references
- All 5 research open questions relevant to this plan (Q1-Q5) are resolved
- claimablePool trace covers every mutation site in the drain sequence
- Executive summary provides overall assessment
</verification>

<success_criteria>
- GO-08 verdict delivered with coverage of all 4 terminal decimator functions
- GO-01 verdict delivered with step-by-step trace of the 7-step drain sequence
- claimablePool invariant verified at every mutation point
- Research open questions Q1, Q2, Q3, Q4, Q5 all resolved with explicit answers
</success_criteria>

<output>
After completion, create `.planning/phases/26-gameover-path-audit/26-01-SUMMARY.md`
</output>
