---
phase: 376-impl-the-one-batched-contract-diff-afpay-pack-curse-smite
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
requirements: [PACK-01, PACK-02, AFPAY-01, AFPAY-02, AFPAY-03, AFPAY-04, AFPAY-05, AFPAY-06, AFPAY-07]
files_modified:
  - contracts/modules/DegenerusGamePayoutUtils.sol
  - contracts/storage/DegenerusGameStorage.sol
  - contracts/DegenerusGame.sol
  - contracts/modules/GameAfkingModule.sol
  - contracts/modules/DegenerusGameGameOverModule.sol
  - contracts/modules/DegenerusGameMintModule.sol
  - contracts/modules/DegenerusGameWhaleModule.sol
  - contracts/modules/DegenerusGameDegeneretteModule.sol
  - contracts/modules/DegenerusGameJackpotModule.sol
  - contracts/modules/DegenerusGameAdvanceModule.sol

must_haves:
  truths:
    - "The balances mapping reads/writes route through the PACK accessor layer; no production .sol accesses claimableWinnings[...] or afkingFunding[...] as a raw mapping outside the accessor bodies"
    - "claimableWinnings and afkingFunding are folded into ONE uint256 mapping [afking:high128 | claimable:low128]; the two separate mappings no longer exist"
    - "Every afking debit and every claimable debit pairs an equal claimablePool -= inside the accessor; every credit pairs claimablePool += (SOLVENCY-01 in one home)"
    - "The afking-as-payment waterfall (msg.value -> claimable -> afking) is live on every non-mintBurnie ETH-in spend path: ticket mint, lootbox, presale box, whale bundle, lazy pass, deity pass, degenerette ETH bet"
    - "AfkingSpent(address indexed player, uint256 amount) is emitted at every afking debit on a spend path (both _processMintPayment and _settleShortfall)"
    - "The no-afking DirectEth/Claimable/Combined cases on every path stay behavior-identical to baseline 2bee6d6f"
  artifacts:
    - path: "contracts/modules/DegenerusGamePayoutUtils.sol"
      provides: "PACK accessor layer (_claimableOf/_afkingOf reads + _creditClaimable/_debitClaimable/_creditAfking/_debitAfking mutators, each paired with claimablePool)"
      contains: "function _afkingOf"
    - path: "contracts/storage/DegenerusGameStorage.sol"
      provides: "Repacked single uint256 balances mapping + generalized _settleShortfall + AfkingSpent event decl"
      contains: "function _settleShortfall"
    - path: "contracts/DegenerusGame.sol"
      provides: "_processMintPayment afking tier on all 3 pay-kinds; gameOver claim-merge via accessors"
      contains: "afkingUsed"
  key_links:
    - from: "contracts/modules/DegenerusGameMintModule.sol"
      to: "_settleShortfall"
      via: "lootbox + presale shortfall calls"
      pattern: "_settleShortfall"
    - from: "contracts/modules/DegenerusGameWhaleModule.sol"
      to: "_settleShortfall"
      via: "whale/lazy/deity pass shortfall calls (all 3 replaced)"
      pattern: "_settleShortfall"
    - from: "spend paths"
      to: "claimablePool"
      via: "paired debit inside accessor / settle helper"
      pattern: "claimablePool"
---

<objective>
Author Track A of the ONE batched v61.0 contract diff: the claimable/afking slot-packing accessor layer + repack (PACK-01/02), then the afking-as-payment waterfall (AFPAY-01..07) written ONCE against those accessors. Producer-before-consumer: PACK accessor -> repack -> generalized settle helper -> spend paths -> AfkingSpent event.

Purpose: afkingFunding becomes a fresh-ETH-equivalent fallback tier (msg.value -> claimable -> afking) across every non-mintBurnie spend path, treated identically to msg.value (saves a withdraw->re-pay round-trip), with the SOLVENCY-01 invariant (claimablePool == sum(claimable + afking)) centralized in ONE accessor home and full AfkingSpent observability.

Output: the applied Track A diff. The canonical full forge-build-clean + the SettleClaimableShortfallTester signature update + the boundary HOLD are Plan 03. Run forge build at the end of this plan as a sanity gate.

CONTRACT-BOUNDARY HARD STOP — autonomous: false. NEVER commit any contracts/*.sol; the executor applies + compiles, then the diff is HELD for USER hand-review.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/SPEC-V61-DESIGN-LOCK.md
@.planning/REQUIREMENTS.md
@.planning/phases/376-impl-the-one-batched-contract-diff-afpay-pack-curse-smite/376-CONTEXT.md

<interfaces>
<!-- Extracted from baseline 2bee6d6f. Executor uses these directly. -->

Balance mapping decls — contracts/storage/DegenerusGameStorage.sol:
  mapping(address => uint256) internal claimableWinnings;   // :412
  mapping(address => uint256) internal afkingFunding;       // :420
  uint128 internal claimablePool;                           // :365 (SOLVENCY invariant comment :358)
  IDegenerusCoin internal constant coin = ...;              // :137

Canonical settle helper — contracts/storage/DegenerusGameStorage.sol:851:
  function _settleClaimableShortfall(address buyer, uint256 basis, uint256 shortfall) internal
    // body: if (shortfall != 0) { if (basis <= shortfall) revert E(); claimableWinnings[buyer] = basis - shortfall; claimablePool -= uint128(shortfall); }
  // 5 production callers: MintModule:1143 (lootbox), MintModule:1489 (presale), WhaleModule:263/490/596 (whale/lazy/deity)
  // all 3 whale callers identical: _settleClaimableShortfall(buyer, claimableWinnings[buyer], totalPrice - msg.value)

Accessor precedents — contracts/modules/DegenerusGamePayoutUtils.sol:
  function _creditClaimable(address beneficiary, uint256 weiAmount) internal   // :22 (claimableWinnings[beneficiary] += weiAmount; emit PlayerCredited)
  function _creditBoxProceeds(uint256 boxEth) internal                          // :36 (claimablePool += uint128(boxEth); two _creditClaimable)
  // second claimable credit: claimableWinnings[winner] += remainder (:63, inside _queueWhalePassClaimCore)

afking credit/debit pairs (the standalone claimablePool line to FOLD into the accessor):
  GameAfkingModule.sol:337-338   afkingFunding[fundDest] += msg.value;  claimablePool += uint128(msg.value);
  GameAfkingModule.sol:791-792   afkingFunding[src] -= ethValue;        claimablePool -= uint128(ethValue);
  GameAfkingModule.sol:798-799   claimableWinnings[player] -= claimableUse; claimablePool -= uint128(claimableUse);
  DegenerusGame.sol:1606         afkingFunding[player] += msg.value;     (depositAfkingFunding; paired claimablePool += nearby)
  DegenerusGame.sol:1624         afkingFunding[msg.sender] = bal - amount; (withdrawAfkingFunding; paired claimablePool -= nearby)
  DegenerusGame.sol:1575/1584    gameOver claim-merge: afking = gameOver ? afkingFunding[player] : 0; afkingFunding[player] = 0; single claimablePool -= (payout) :1588

_processMintPayment — contracts/DegenerusGame.sol:1054 (private; sole call site :474):
  function _processMintPayment(address player, uint256 amount, MintPaymentKind payKind)
    private returns (uint256 prizeContribution, uint256 newClaimableBalance)
  // DirectEth (:1060): msg.value >= amount; prizeContribution = amount; NO claimable
  // Claimable (:1065): msg.value == 0; claimable->sentinel; claimableUsed = amount; prizeContribution = amount
  // Combined (:1077): msg.value first, claimable->sentinel for remainder; prizeContribution = msg.value + claimableUsed
  // claimablePool -= uint128(claimableUsed) at :1104; emit ClaimableSpent :1105

gameOver claimable-zeroing — contracts/modules/DegenerusGameGameOverModule.sol:215-217:
  claimableWinnings[VAULT] = 0; claimableWinnings[SDGNRS] = 0; claimableWinnings[GNRUS] = 0;  (refund credit :131; pool bookkeeping :212-231)

Affiliate split host — contracts/modules/DegenerusGameMintModule.sol _callTicketPurchase (private, def ~:1538):
  freshEth per pay-kind :1620-1632; payAffiliate branches :1655 (Combined fresh) / :1665 (Combined recycled) / :1675 (DirectEth) / :1684 (else recycled); coinCost :1600/1695; rebuy bonus coinCost/10 :1697

_purchaseForWith — contracts/modules/DegenerusGameMintModule.sol:1093 (private; body to :1419):
  lootbox shortfall block :1126-1146 (DirectEth->revert at :1135; settle call :1143; lootboxFreshEth/lootboxClaimableUsed bookkeeping); lootbox affiliate fresh leg :1311 / recycled leg :1321

_buyPresaleBoxFor — contracts/modules/DegenerusGameMintModule.sol:1456 (private): shortfall call :1489; _creditBoxProceeds(applied) :1494

_collectBetFunds — contracts/modules/DegenerusGameDegeneretteModule.sol:573 (private): CURRENCY_ETH branch :579-588 (claimable debit :586-587); InvalidBet() reverts :581/585
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PACK accessor layer (PACK-01) + storage repack (PACK-02)</name>
  <files>contracts/modules/DegenerusGamePayoutUtils.sol, contracts/storage/DegenerusGameStorage.sol, contracts/DegenerusGame.sol, contracts/modules/GameAfkingModule.sol, contracts/modules/DegenerusGameGameOverModule.sol, contracts/modules/DegenerusGameMintModule.sol, contracts/modules/DegenerusGameWhaleModule.sol, contracts/modules/DegenerusGameDegeneretteModule.sol, contracts/modules/DegenerusGameJackpotModule.sol, contracts/modules/DegenerusGameAdvanceModule.sol</files>
  <read_first>
    - .planning/SPEC-V61-DESIGN-LOCK.md §1 D-01 (accessor-first lock) + §3 (the SOLVENCY accessor-invariant home: Storage:358/365/851 + PayoutUtils:25/39/63 + the GameAfkingModule afking pair) + §4 Track A steps 1-2 + the Cross-check
    - .planning/PLAN-PLAYERQUESTSTATE-1SLOT-PACKING.md (the accessor-layer precedent)
    - contracts/modules/DegenerusGamePayoutUtils.sol (full file)
    - contracts/storage/DegenerusGameStorage.sol lines 355-365, 410-420, 840-859
    - contracts/modules/DegenerusGameGameOverModule.sol lines 131, 212-231
    - EVERY baseline ref site: run grep for claimableWinnings and afkingFunding across contracts/ at 2bee6d6f and read each match. External reads via the IDegenerusGame interface getters (Vault/sDGNRS/GNRUS/mocks) are NOT same-contract raw accesses and stay unchanged.
  </read_first>
  <action>
Two sub-steps in producer-before-consumer order (do STEP A fully, forge build clean, then STEP B):

STEP A (PACK-01) — accessor layer. Add to DegenerusGamePayoutUtils.sol (the shared base, is DegenerusGameStorage): _claimableOf(address) internal view returns (uint256); _afkingOf(address) internal view returns (uint256); and mutators _debitClaimable(address,uint256), _creditAfking(address,uint256), _debitAfking(address,uint256) (keep existing _creditClaimable as the canonical claimable credit). Each MUTATOR pairs its balance change with the matching claimablePool update INSIDE the accessor: credits do claimablePool += uint128(amount); debits do claimablePool -= uint128(amount). This is the ONE SOLVENCY-01 home.

Resolve the accessor physical home (planner-resolve item): natural home is DegenerusGamePayoutUtils. Grep-confirm the Game + every balance-touching module (GameAfkingModule, MintModule, WhaleModule, DegeneretteModule, GameOverModule, JackpotModule, AdvanceModule) reaches it via inheritance; if ANY does not, promote the accessors to DegenerusGameStorage (where claimablePool + the mappings live). Pick the single base every consumer inherits — do NOT duplicate accessors.

Route every production read claimableWinnings[x] to _claimableOf(x), afkingFunding[x] to _afkingOf(x); every credit to _creditClaimable/_creditAfking; every debit to _debitClaimable/_debitAfking. When converting a site that today does the mapping write AND an adjacent standalone claimablePool +=/-= (GameAfkingModule:337/791/798, _processMintPayment:1104, _creditBoxProceeds:39, _settleClaimableShortfall:855-857, the GameOver pool bookkeeping), REMOVE the redundant standalone claimablePool line — the accessor now performs it. Audit each converted site for exactly ONE claimablePool delta per balance delta. In STEP A the accessors still read/write the TWO existing mappings (pure routing, behavior-identical). forge build must be clean before STEP B.

STEP B (PACK-02) — storage repack. Replace the two mappings claimableWinnings(:412)/afkingFunding(:420) with ONE mapping(address => uint256) internal balancesPacked; layout [afking:high128 | claimable:low128] documented in the decl comment. Update the SOLVENCY invariant comment at :358 to name the packed mapping. Rewrite ONLY the accessor bodies to operate on the packed slot: _claimableOf(a) = uint128(balancesPacked[a]); _afkingOf(a) = uint128(balancesPacked[a] >> 128). Each mutator loads the slot once, splits uint128 afking = uint128(slot >> 128) and uint128 claimable = uint128(slot), does the add/sub on the relevant uint128 half (0.8 checked math on the uint128 op catches per-half overflow/underflow), and recombines balancesPacked[a] = (uint256(afking) << 128) | claimable. NEVER a naive full-word balancesPacked[a] += x (0.8's 256-bit check misses a 127->128 carry). No explicit overflow require needed (each half stays a clean uint128; per-player ETH <= supply ~1.2e26 << 2^128). gameOver claimable-zeroing (GameOverModule:215-217): zero VAULT/SDGNRS/GNRUS claimable via _debitClaimable(addr, _claimableOf(addr)) or a dedicated _zeroClaimablePreserveAfking(addr) accessor so ONLY the low half clears and the afking high-half survives — do NOT assign the whole slot to 0; confirm the GameOver pool bookkeeping at :212-231 still nets the same. The src != player operator-funded auto-buy (GameAfkingModule:791 debits src, :798 debits player) stays two-address (two separate slots, no collision).

Comments explain HOW the accessor maintains the invariant + the why-not-naive-+= rule; no plan IDs / REQ tags / change-history.
  </action>
  <verify>
    <automated>forge build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - forge build exits 0 (clean compile)
    - DegenerusGamePayoutUtils.sol (or DegenerusGameStorage.sol if promoted) contains function _afkingOf, function _debitAfking, function _creditAfking, function _debitClaimable (grep assertion)
    - The two separate mappings are gone: grep for "mapping(address => uint256) internal claimableWinnings" and "...afkingFunding" returns nothing; balancesPacked is declared once
    - Every raw slot access (claimableWinnings[/afkingFunding[/balancesPacked[) in production .sol sits inside an accessor function body in the accessor-home file — no stray raw access elsewhere
    - The recombine uses (uint256(afking) << 128) | claimable (grep assertion); no naive balancesPacked[...] += / -= anywhere
    - Each converted balance-delta site has exactly one paired claimablePool delta (source assertion at GameAfkingModule:337/791/798, _processMintPayment, _creditBoxProceeds, GameOver, the settle helper)
    - gameOver zeroing preserves the afking high-half (does not assign the whole slot to 0)
  </acceptance_criteria>
  <done>The single shared accessor base exposes the six paired accessors; the two mappings are folded into one packed slot with uint128-half recombine math; gameOver zeroing preserves the afking half; forge build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Generalize the settle helper (AFPAY-01) + AfkingSpent event (AFPAY-07)</name>
  <files>contracts/storage/DegenerusGameStorage.sol</files>
  <read_first>
    - .planning/SPEC-V61-DESIGN-LOCK.md §1 D-02 (AfkingSpent breadth) + §4 Track A steps 3 and 5 + the Verification Item 1 (purchaseWith DEAD — leave untouched)
    - .planning/REQUIREMENTS.md AFPAY-01 and AFPAY-07
    - contracts/storage/DegenerusGameStorage.sol lines 840-859 (current _settleClaimableShortfall) + the accessor layer authored in Task 1
    - the 5 production caller signatures (interfaces block above) — the new signature must serve all 5
  </read_first>
  <action>
Generalize _settleClaimableShortfall(buyer, basis, shortfall) into _settleShortfall(address buyer, uint256 shortfall, bool allowClaimable) returns (uint256 claimableUsed, uint256 afkingUsed). Behavior: draw claimable to the STRICT 1-wei sentinel FIRST but ONLY if allowClaimable (read via _claimableOf(buyer), debit via _debitClaimable; claimableUsed = the amount drawn, capped at _claimableOf(buyer) - 1); then draw the remainder from afking with NO sentinel, draining toward 0 (read _afkingOf(buyer), debit _debitAfking; afkingUsed = remainder covered); revert E() if the two tiers together cannot cover shortfall. Drop the stale basis param (the helper reads the live balance itself). The accessor debits already pair the claimablePool updates (Task 1) — do NOT add standalone claimablePool lines here. Emit AfkingSpent(buyer, afkingUsed) when afkingUsed != 0.

Declare event AfkingSpent(address indexed player, uint256 amount) in DegenerusGameStorage.sol (visible to game + all modules per D-02). Per D-02 the event is emitted at EACH afking debit on a spend path: here in _settleShortfall, and (Task 4) in _processMintPayment. (Deliberate asymmetry vs claimable, which stays silent outside _processMintPayment — by design, AFPAY's headline transparency signal.)

Leave purchaseWith (MintModule:858) untouched (Verification Item 1: DEAD, not reachable).

Comments explain HOW the waterfall draws (claimable-to-sentinel then afking-to-zero) + the revert-if-both-short rule; no history.
  </action>
  <verify>
    <automated>forge build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - forge build exits 0
    - DegenerusGameStorage.sol contains function _settleShortfall(address buyer, uint256 shortfall, bool allowClaimable) returning (uint256 claimableUsed, uint256 afkingUsed) (grep assertion)
    - The old _settleClaimableShortfall symbol no longer exists in production .sol (grep returns only the test consumer, which Plan 03 updates)
    - DegenerusGameStorage.sol declares event AfkingSpent(address indexed player, uint256 amount) (grep assertion)
    - _settleShortfall emits AfkingSpent only when afkingUsed != 0; draws claimable only when allowClaimable; reverts E() when both tiers are short (source assertion)
    - No standalone claimablePool +=/-= inside _settleShortfall (the accessor debits handle it) — source assertion
  </acceptance_criteria>
  <done>The generalized _settleShortfall returns (claimableUsed, afkingUsed), draws claimable-to-sentinel (gated by allowClaimable) then afking-to-zero, reverts when both short, emits AfkingSpent on afking draw; the event is declared in storage; forge build clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the afking tier into all spend paths (AFPAY-02/03/04/05/06)</name>
  <files>contracts/DegenerusGame.sol, contracts/modules/DegenerusGameMintModule.sol, contracts/modules/DegenerusGameWhaleModule.sol, contracts/modules/DegenerusGameDegeneretteModule.sol</files>
  <read_first>
    - .planning/SPEC-V61-DESIGN-LOCK.md §4 Track A step 4 (the per-path sketch for AFPAY-02..06) + §1 Hard Floor + §2 (the corrected _purchaseForWith :1093 host name — there is NO _purchaseWithFor)
    - .planning/REQUIREMENTS.md AFPAY-02, AFPAY-03, AFPAY-04, AFPAY-05, AFPAY-06 + the AFPAY-06 byte-identity obligation in 376-CONTEXT.md <specifics>
    - .planning/PLAN-V61-AFKING-AS-PAYMENT-SOURCE.md §2 funding table + §4 edit map (sketch detail)
    - contracts/DegenerusGame.sol:1054-1113 (_processMintPayment full body)
    - contracts/modules/DegenerusGameMintModule.sol:1093-1419 (_purchaseForWith — lootbox shortfall + affiliate legs), :1456-1499 (_buyPresaleBoxFor), :1538-1704 (_callTicketPurchase — the affiliate fresh/recycled split)
    - contracts/modules/DegenerusGameWhaleModule.sol:255-266, :485-495, :590-600 (the 3 shortfall sites)
    - contracts/modules/DegenerusGameDegeneretteModule.sol:573-607 (_collectBetFunds CURRENCY_ETH branch)
    - the generalized _settleShortfall signature from Task 2
  </read_first>
  <action>
AFPAY-02 — _processMintPayment (DegenerusGame.sol:1054): add the afking tier to all three pay-kinds. DirectEth: msg.value -> afking for any shortfall, SKIP claimable (afking-only fallback); Claimable: claimable->sentinel then afking; Combined: msg.value then claimable->sentinel then afking. Compute afkingUsed via the accessor (_afkingOf/_debitAfking) or by delegating the shortfall to _settleShortfall with the right allowClaimable flag; set prizeContribution = msg.value + claimableUsed + afkingUsed. Emit AfkingSpent(player, afkingUsed) when afkingUsed != 0 (D-02). Keep the existing ClaimableSpent emission + the 1-wei sentinel semantics for the claimable portion. Revert E() if msg.value + claimable + afking cannot cover amount.

AFPAY-03 — lootbox shortfall (MintModule:1126-1146, inside _purchaseForWith :1093): replace the settle call at :1143 with _settleShortfall(buyer, shortfall, payKind != MintPaymentKind.DirectEth). This LIFTS the DirectEth->revert at :1135 so a DirectEth lootbox can draw afking (claimable still skipped for DirectEth via allowClaimable=false). Set lootboxFreshEth += afkingUsed (afking is fresh-ETH-equivalent), lootboxClaimableUsed = claimableUsed. The fresh leg at :1311 (score-bearing affiliate) then covers the afking-drawn portion; the recycled leg at :1321 covers the claimable portion. Remove the now-obsolete DirectEth revert branch for the lootbox shortfall.

AFPAY-04 — presale box (MintModule:1489, inside _buyPresaleBoxFor) + whale bundle / lazy pass / deity pass (WhaleModule:263/490/596): replace each _settleClaimableShortfall(...) call with _settleShortfall(buyer, shortfall, true) (these paths allow claimable, so allowClaimable=true; afking covers the remainder). All 3 whale sites are identical, so the same edit applies to each. The presale _creditBoxProceeds(applied) at :1494 is unchanged (it routes the box proceeds; the shortfall funding source — claimable vs afking — does not change the 80/20 routing).

AFPAY-05 — Degenerette _collectBetFunds (DegeneretteModule:573, CURRENCY_ETH branch :579-588): after the claimable-to-sentinel draw, cover the remaining shortfall from afking; keep the InvalidBet() revert when still short after both tiers. Use the accessor (_afkingOf/_debitAfking) or _settleShortfall, but the revert error MUST stay InvalidBet() (not E()) on this path — preserve the existing error surface.

AFPAY-06 — affiliate fresh/recycled split (MintModule _callTicketPurchase, :1620-1692): set freshEth = costWei - claimableUsedTicket so the afking-drawn portion lands in fresh (afking is fresh-ETH-equivalent: fresh-rate affiliate including lootbox activity score). The existing per-pay-kind freshEth computation (:1620-1632) must yield byte-identical results for the existing no-afking DirectEth/Claimable/Combined cases — re-verify each branch. NO rebuy bonus on the afking portion: the bonus block reads claimableWinnings deltas (now _claimableOf deltas) only, so afking is excluded automatically — confirm the bonus computation at :1697-1702 keys off the claimable delta, not the total spend.

Across all paths: the afking-drawn portion is treated identically to msg.value (own deposited principal). The accessor debits already pair claimablePool (Task 1) — no standalone pool lines. Use the corrected host name _purchaseForWith everywhere (NOT _purchaseWithFor — that symbol does not exist). Reference D-02 for the AfkingSpent emission breadth.

Comments explain HOW each path draws the waterfall; no plan IDs / REQ tags / history.
  </action>
  <verify>
    <automated>forge build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - forge build exits 0
    - grep for _purchaseWithFor in contracts/ returns nothing (the corrected name _purchaseForWith is used)
    - _processMintPayment sets prizeContribution = msg.value + claimableUsed + afkingUsed and emits AfkingSpent when afkingUsed != 0 (source assertion)
    - The lootbox shortfall calls _settleShortfall(buyer, shortfall, payKind != MintPaymentKind.DirectEth) and the old DirectEth->revert for the lootbox shortfall is gone (source assertion at MintModule ~:1135-1143)
    - All 3 WhaleModule sites and the presale site call _settleShortfall (grep: 4 production _settleShortfall calls in MintModule+WhaleModule for AFPAY-03/04, plus the AFPAY-02 path)
    - The Degenerette ETH path still reverts InvalidBet() (not E()) when both tiers are short (source assertion)
    - AFPAY-06: freshEth = costWei - claimableUsedTicket (source assertion); the rebuy bonus block keys off the claimable delta only (afking excluded)
  </acceptance_criteria>
  <done>The afking tier is wired into _processMintPayment (all 3 pay-kinds), the lootbox/presale/whale/lazy/deity shortfalls (via _settleShortfall), and the Degenerette ETH bet; freshEth excludes the recycled claimable so afking gets fresh-rate affiliate; no rebuy bonus on afking; forge build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external caller -> spend path (purchaseTickets / buyLootbox / buyPresaleBox / whale buys / degenerette ETH bet) | untrusted msg.value + payKind + the player's own claimable/afking principal cross here |
| spend path -> claimablePool / balances slot | the SOLVENCY-01 ledger; every debit must net the pool |

## STRIDE Threat Register (grounded in the repo hard floor)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-376A-RNG | RNG-freeze (DOMINANT) | the AFPAY waterfall + the PACK repack | mitigate | AFPAY/PACK are LEDGER-ONLY: no rngWord read added, no non-VRF state read inside the rng-window. Acceptance: no new rngWord reference in any touched file (proven empirically at SEC-01/378). |
| T-376A-SOLV | Solvency (SPINE) | the accessor layer + _settleShortfall + every debit site | mitigate | Every afking/claimable debit pairs an equal claimablePool -= INSIDE the accessor (ONE home). The uint128 halves are width-safe (per-player ETH <= supply ~1.2e26 << 2^128); recombine via (uint256(afking) << 128) | claimable, never a naive full-word += (0.8 misses the 127->128 carry). gameOver zeroing preserves the afking high-half. Re-proven at SEC-02/378. |
| T-376A-DBLDRAW | Tampering | afking auto-buy (_deliverAfkingBuy) vs the manual spend chain | accept | OUT of scope by construction: _deliverAfkingBuy / _queueTicketsScaled never reach _processMintPayment (ref count = 0 in GameAfkingModule per SPEC §3) — no double-draw. Proven at TST-01/378. |
| T-376A-BYTEID | Tampering | AFPAY-06 affiliate split refactor | mitigate | The no-afking DirectEth/Claimable/Combined cases stay byte-for-byte behavior-identical (freshEth = costWei - claimableUsedTicket reduces to the baseline freshEth when claimableUsedTicket is the existing recycled amount). Re-verified at IMPL, proven at TST-01/378. |
| T-376A-DOS | Gas-DoS | the shared-helper afking draw | accept | No new unbounded loop; the extra AfkingSpent LOG + one accessor SLOAD/SSTORE ride shortfall-funded buys OFF the advanceGame hot path. Formal measurement at 377. |
| T-376A-SC | Tampering | npm/pip/cargo installs | mitigate | None — this plan adds NO package-manager installs (contract edits only). No package legitimacy gate needed. |
</threat_model>

<verification>
- forge build exits 0 after the full Track A diff is applied.
- RNG-freeze: no new rngWord reference in any touched file (AFPAY/PACK are ledger-only).
- SOLVENCY-01: every balance-delta site pairs exactly one claimablePool delta inside the accessor; the recombine never uses a naive full-word += ; gameOver zeroing preserves the afking half.
- The 5 former _settleClaimableShortfall production callers all now call _settleShortfall; the old symbol survives only in the test consumer (Plan 03 updates it).
- No _purchaseWithFor symbol anywhere (the corrected host name _purchaseForWith is used).
</verification>

<success_criteria>
- The PACK accessor layer + single packed balances slot exist; all production claimable/afking accesses route through the paired accessors.
- The afking-as-payment waterfall is live on every non-mintBurnie ETH-in spend path with AfkingSpent observability.
- SOLVENCY-01 is centralized in ONE accessor home; RNG-freeze intact (no rngWord read added).
- forge build clean; the diff is HELD at the contract-commit boundary (NOT committed) for USER hand-review.
</success_criteria>

<output>
Create `.planning/phases/376-impl-the-one-batched-contract-diff-afpay-pack-curse-smite/376-01-SUMMARY.md` when done. Record: the accessor-home decision (PayoutUtils vs promoted to Storage), the packed-mapping name, the per-path AFPAY edits, and any site where the byte-identity re-verification required care. Do NOT commit any contracts/*.sol — leave the Track A diff applied + compiled for Plan 02 to build on and Plan 03 to hold.
</output>
