---
phase: 376-impl-the-one-batched-contract-diff-afpay-pack-curse-smite
plan: 03
type: execute
wave: 3
depends_on: [376-01, 376-02]
autonomous: false
requirements: [AFPAY-01, PACK-01, PACK-02]
files_modified:
  - contracts/test/SettleClaimableShortfallTester.sol

must_haves:
  truths:
    - "The full contract set + all contracts/test/ helpers compile clean under the new _settleShortfall signature and the repacked balances mapping (forge build exits 0)"
    - "contracts/test/SettleClaimableShortfallTester.sol calls _settleShortfall(buyer, shortfall, allowClaimable) and uses the PACK accessors for its claimable getters/setters"
    - "No contracts/test/ consumer references the removed _settleClaimableShortfall symbol, the removed claimableWinnings/afkingFunding raw mappings, or any other broken signature"
    - "DegenerusGame stays under the 24576-byte EIP-170 ceiling (PLACE-03), confirmed via forge build --sizes"
    - "The complete batched diff (Track A + Track B) is applied and compiling, and is HELD at the contract-commit boundary — NOT committed"
  artifacts:
    - path: "contracts/test/SettleClaimableShortfallTester.sol"
      provides: "Test harness updated to the new _settleShortfall signature + PACK accessors (build-cleanliness; test-side, free to commit)"
      contains: "_settleShortfall"
  key_links:
    - from: "contracts/test/SettleClaimableShortfallTester.sol"
      to: "_settleShortfall"
      via: "the production settle helper under test"
      pattern: "_settleShortfall"
---

<objective>
Close the build under the v61.0 batched diff and HOLD it at the contract-commit boundary. Two responsibilities:
1. Build-cleanliness — update contracts/test/SettleClaimableShortfallTester.sol to the new _settleShortfall(buyer, shortfall, allowClaimable) -> (claimableUsed, afkingUsed) signature + the repacked balances accessors, and sweep every other contracts/test/ consumer that the new signatures or the folded balances mapping break, so forge build stays clean. (Test-side — these files ARE agent-committable.)
2. The contract-commit boundary HOLD — confirm the full Track A + Track B diff compiles (forge build) and that DegenerusGame stays under the 24576-byte EIP-170 ceiling (forge build --sizes), then HOLD the contracts/*.sol diff for explicit USER hand-review. NEVER commit any contracts/*.sol.

Purpose: the Phase-376 definition of done = the ONE batched diff applied + forge build clean + HELD at the boundary (SC5 literal). The proving tests (TST-01..06) + the empirical SEC floor (SEC-01/02) are Phase 378 — do NOT write them here. Gas-neutrality is Phase 377 — do NOT tune gas here.

CONTRACT-BOUNDARY HARD STOP — autonomous: false. This plan's only committable changes are under contracts/test/ (and planning/docs); the contracts/*.sol diff from Plans 01-02 stays HELD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/SPEC-V61-DESIGN-LOCK.md
@.planning/REQUIREMENTS.md
@.planning/phases/376-impl-the-one-batched-contract-diff-afpay-pack-curse-smite/376-CONTEXT.md

<interfaces>
<!-- Extracted from baseline 2bee6d6f. -->

contracts/test/SettleClaimableShortfallTester.sol (full, at baseline):
  is DegenerusGameStorage
  setClaimable(address,uint256)   // :18 — claimableWinnings[buyer] = amount  (NOW route via the accessor / packed slot)
  setClaimablePool(uint256)       // :23 — claimablePool = uint128(amount)
  getClaimable(address)           // :28 — return claimableWinnings[buyer]     (NOW _claimableOf(buyer))
  getClaimablePool()              // :33 — return claimablePool
  settle(address,uint256,uint256) // :38 — _settleClaimableShortfall(buyer, basis, shortfall)  (NOW the new signature)
  sentinelError()                 // :45 — return E.selector
  // The old helper signature: _settleClaimableShortfall(address buyer, uint256 basis, uint256 shortfall)
  // The new helper signature (Plan 01 Task 2): _settleShortfall(address buyer, uint256 shortfall, bool allowClaimable) returns (uint256 claimableUsed, uint256 afkingUsed)
  // The balances are now ONE packed mapping (Plan 01 Task 1 STEP B) — raw claimableWinnings[...] no longer exists.

Plan 01 produced (read its SUMMARY): the accessor-home file + the six accessors (_claimableOf/_afkingOf/_creditClaimable/_debitClaimable/_creditAfking/_debitAfking), the packed mapping name, the generalized _settleShortfall, the AfkingSpent event.
Plan 02 produced (read its SUMMARY): the curse field, the MintStreakUtils base helpers, the decurse/smite host module + the measured Game size.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SettleClaimableShortfallTester + sweep contracts/test/ for build breaks</name>
  <files>contracts/test/SettleClaimableShortfallTester.sol</files>
  <read_first>
    - .planning/phases/376-impl-the-one-batched-contract-diff-afpay-pack-curse-smite/376-CONTEXT.md <decisions> (the SettleClaimableShortfallTester signature-update item + the sweep-for-other-consumers instruction) + <specifics>
    - .planning/SPEC-V61-DESIGN-LOCK.md §3 (the test-consumer row: SettleClaimableShortfallTester :39) + §1 D-01 (the new _settleShortfall signature)
    - .planning/phases/376-impl-the-one-batched-contract-diff-afpay-pack-curse-smite/376-01-SUMMARY.md (the exact accessor names + packed mapping name + the _settleShortfall signature as actually authored)
    - contracts/test/SettleClaimableShortfallTester.sol (full file)
  </read_first>
  <action>
First, enumerate every contracts/test/ file that touches the changed surface: grep contracts/test/ for _settleClaimableShortfall, claimableWinnings, afkingFunding, and any helper whose signature changed. Read each match. The known consumer is SettleClaimableShortfallTester.sol; there may be others (the build will reveal them).

Update SettleClaimableShortfallTester.sol to the new world:
- settle(...) at :38: change the body to call the generalized _settleShortfall(buyer, shortfall, allowClaimable). Update the function signature to expose allowClaimable (e.g. settle(address buyer, uint256 shortfall, bool allowClaimable) returns (uint256 claimableUsed, uint256 afkingUsed)) so the 378 refinement suite can exercise both the claimable-only and the afking-fallback branches. Drop the stale basis param (the helper reads the live balance). Keep the exact production helper running (no re-implementation) — the contract still inherits DegenerusGameStorage so the real body executes.
- setClaimable / getClaimable: route through the accessor + packed slot (use _creditClaimable or directly seed the packed slot for setClaimable; return _claimableOf(buyer) for getClaimable) so the harness reads/writes the same packed balance the production code does. Add setAfking/getAfking (via _creditAfking/_afkingOf) so the suite can seed the afking tier for the fallback branch.
- setClaimablePool / getClaimablePool / sentinelError: unchanged (claimablePool is still its own uint128).
- Update the contract doc-comment to describe the new _settleShortfall + the packed balances — explain HOW it works (no plan IDs / change-history).

Fix any OTHER contracts/test/ consumer the sweep surfaces (e.g. a tester reading the raw claimableWinnings/afkingFunding mappings, or calling a changed helper) so forge build is clean. These are test-side files — free to edit and (per the project rule) free to commit.

Do NOT add proving tests, gas tests, or SEC tests here — only the minimal edits to keep the build clean (those belong to 377/378).
  </action>
  <verify>
    <automated>forge build</automated>
  </verify>
  <acceptance_criteria>
    - forge build exits 0 (the full contract set + all contracts/test/ helpers compile)
    - SettleClaimableShortfallTester.sol calls _settleShortfall and exposes allowClaimable + the (claimableUsed, afkingUsed) returns; it no longer references _settleClaimableShortfall or raw claimableWinnings[...] (grep assertion)
    - grep for _settleClaimableShortfall across contracts/ (incl. test) returns nothing (the old symbol is fully gone)
    - grep for raw claimableWinnings[ / afkingFunding[ across contracts/test/ returns nothing (testers use the accessors)
    - No proving/gas/SEC test was added (this task is build-cleanliness only)
  </acceptance_criteria>
  <done>SettleClaimableShortfallTester (and any other broken contracts/test/ consumer) is updated to the new _settleShortfall signature + the PACK accessors; forge build is clean; no new proving tests added.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Contract-commit boundary HOLD — full diff applied, compiling, under EIP-170, HELD for hand-review</name>
  <files>contracts/*.sol (the full batched Track A + Track B diff — reviewed at this gate, NOT committed by the executor)</files>
  <action>
Gather the hand-review evidence per how-to-verify below, write it into the SUMMARY, then PAUSE for USER hand-review. The executor MUST NOT commit any contracts/*.sol and MUST NOT set CONTRACTS_COMMIT_APPROVED.
  </action>
  <what-built>
The complete v61.0 batched contract diff (Track A: PACK accessor + repack + AFPAY waterfall + AfkingSpent; Track B: curse counter + APPLY + SET + CURE + lootbox stamp + decurse + view+cap + smite) is applied to contracts/*.sol and compiling clean, with the contracts/test/ consumers updated for build-cleanliness. This is the CONTRACT-BOUNDARY HARD STOP for v61.0 — the diff is HELD for explicit USER hand-review and must NOT be committed by the executor (only contract commits need approval; tests/planning/docs are agent-committable).
  </what-built>
  <how-to-verify>
Before pausing, the executor MUST produce (and put in the SUMMARY) the evidence the user will review:
1. Run `forge build` — confirm it exits 0 (clean compile of the full set). Paste the tail.
2. Run `forge build --sizes` — confirm DegenerusGame is reported UNDER 24576 bytes (PLACE-03 guardrail). Paste the DegenerusGame size line. If it is OVER, this is a BLOCKER: state it explicitly and do not present the diff as ready.
3. Produce the contract-only diff for the batched change: `git diff -- contracts/` (mainnet .sol only; exclude contracts/test/ which is committable). Summarize it per REQ-ID group (PACK-01/02, AFPAY-01..07, CURSE-01..07, SMITE-01) so the user can hand-review one batched diff.
4. RNG-freeze spot-check: confirm `git grep -n rngWord -- contracts/` shows no NEW rngWord read/write on any AFPAY/PACK/CURSE/SMITE surface (the changes are ledger/score-only). State the result.
5. SOLVENCY-01 spot-check: confirm every afking/claimable debit pairs a claimablePool delta inside the accessor (one home), and the recombine uses (uint256(afking) << 128) | claimable with no naive full-word +=. State the result.

Then PAUSE. The user hand-reviews the batched contracts/*.sol diff and either approves the commit (with CONTRACTS_COMMIT_APPROVED=1, the user's own action) or requests changes. The executor MUST NOT commit any contracts/*.sol. Tests + planning + docs may be committed (the commit-guard hook blocks commits while contracts/*.sol is dirty — force-add planning docs as usual, and only after the contract diff is approved or the contract files are clean).
  </how-to-verify>
  <verify>
    <human-check>USER hand-reviews the batched contracts/*.sol diff and either approves the commit (their own action with CONTRACTS_COMMIT_APPROVED=1) or requests changes. The executor never commits contracts/*.sol.</human-check>
  </verify>
  <resume-signal>USER types "approved" (and performs the contract commit themselves) or describes the changes needed. The executor does NOT auto-advance past this gate and does NOT commit contracts/*.sol.</resume-signal>
  <done>forge build clean + DegenerusGame under 24576 bytes + the batched contracts/*.sol diff HELD (not committed) with the full hand-review evidence (build tail, --sizes line, per-REQ-ID diff summary, RNG-freeze + SOLVENCY-01 spot-checks) recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor -> contract-commit | the HARD STOP: only the USER may approve a contracts/*.sol commit; the executor holds the diff |
| test harness -> production storage layout | the tester inherits DegenerusGameStorage and must match the repacked slot exactly |

## STRIDE Threat Register (grounded in the repo hard floor)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-376C-COMMIT | Elevation of Privilege | the contract-commit boundary | mitigate | The plan is autonomous:false and the final task is a blocking-human checkpoint; the executor never sets CONTRACTS_COMMIT_APPROVED and never commits contracts/*.sol. The commit-guard hook blocks commits while contracts/*.sol is dirty. |
| T-376C-SIZE | Denial of Service | DegenerusGame bytecode size | mitigate | forge build --sizes confirms DegenerusGame < 24576 bytes (EIP-170); an over-ceiling result is surfaced as a BLOCKER at the checkpoint, not worked around. |
| T-376C-TESTDRIFT | Tampering | the test harness vs the repacked slot | mitigate | The tester routes through the same PACK accessors + packed slot as production (no raw-mapping divergence), so its assertions reflect real production behavior. |
| T-376C-SC | Tampering | npm/pip/cargo installs | mitigate | None — no package-manager installs. No package legitimacy gate needed. |
</threat_model>

<verification>
- forge build exits 0 with the full Track A + Track B diff + the updated contracts/test/ consumers.
- forge build --sizes reports DegenerusGame under 24576 bytes.
- The old _settleClaimableShortfall symbol is fully gone; testers use the PACK accessors.
- The batched contracts/*.sol diff is HELD (not committed); the checkpoint evidence (build, sizes, diff summary, RNG/SOLVENCY spot-checks) is in the SUMMARY.
</verification>

<success_criteria>
- The full contract set + contracts/test/ helpers compile clean under the new signatures + the repacked mapping.
- DegenerusGame stays under the EIP-170 ceiling.
- The complete v61.0 batched diff is applied, compiling, and HELD at the contract-commit boundary for USER hand-review — never committed by the executor.
</success_criteria>

<output>
Create `.planning/phases/376-impl-the-one-batched-contract-diff-afpay-pack-curse-smite/376-03-SUMMARY.md` when done. Record: the forge build result, the DegenerusGame --sizes measurement vs 24576, the per-REQ-ID summary of the batched contracts/*.sol diff, the RNG-freeze + SOLVENCY-01 spot-check results, and the list of contracts/test/ files updated for build-cleanliness. State explicitly that the contracts/*.sol diff is HELD for USER hand-review and was NOT committed by the executor.
</output>
