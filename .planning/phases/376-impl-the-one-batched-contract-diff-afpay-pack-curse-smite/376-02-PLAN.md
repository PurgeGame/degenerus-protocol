---
phase: 376-impl-the-one-batched-contract-diff-afpay-pack-curse-smite
plan: 02
type: execute
wave: 2
depends_on: [376-01]
autonomous: false
requirements: [CURSE-01, CURSE-02, CURSE-03, CURSE-04, CURSE-05, CURSE-06, CURSE-07, SMITE-01]
files_modified:
  - contracts/libraries/BitPackingLib.sol
  - contracts/modules/DegenerusGameMintStreakUtils.sol
  - contracts/DegenerusGame.sol
  - contracts/modules/DegenerusGameMintModule.sol
  - contracts/modules/DegenerusGameWhaleModule.sol
  - contracts/modules/GameAfkingModule.sol
  - contracts/interfaces/IDegenerusGameModules.sol

must_haves:
  truths:
    - "A uint8 curse counter lives in mintPacked_ bits 215-222 (CURSE_COUNT_SHIFT=215, MASK_8), clobber-free against the 12 field-isolated mintPacked_ writers"
    - "The activity-score APPLY subtracts curse*100 bps (floored at 0) at the single _playerActivityScore chokepoint, propagating to every consumer + the public view + frozen snapshots"
    - "A stale (>=5d-idle) ghost-cashout via the public claimWinnings adds +2 to the counter (saturating at CURSE_COUNT_CAP=20), with infra/gameOver/non-stale/deity/whale/active-afker bails"
    - "Any >=1-ticket buy clears the counter to 0 (cure); the permissionless decurse(target) clears it for 100 BURNIE"
    - "A deity can smite(deityId, smitee) for 200 BURNIE adding +2 (saturating), gated by ownerOf(deityId)==msg.sender, with active-afker immunity + a 5-stack smite ceiling + the protocol-addr skip"
    - "decurse and smite are thin Game dispatch stubs delegatecalling into an existing module; the Game stays under the 24576-byte EIP-170 ceiling"
  artifacts:
    - path: "contracts/libraries/BitPackingLib.sol"
      provides: "CURSE_COUNT_SHIFT=215 + MASK_8 + updated layout doc"
      contains: "CURSE_COUNT_SHIFT"
    - path: "contracts/modules/DegenerusGameMintStreakUtils.sol"
      provides: "curse APPLY + saturating mutators + curseCountOf view + CURSE_COUNT_CAP=20 (base helpers for Game + all modules)"
      contains: "CURSE_COUNT_CAP"
    - path: "contracts/DegenerusGame.sol"
      provides: "_maybeCurse SET hook inline in claimWinnings; decurse/smite Game dispatch stubs"
      contains: "_maybeCurse"
  key_links:
    - from: "contracts/DegenerusGame.sol claimWinnings"
      to: "_maybeCurse"
      via: "inline base-helper call after _claimWinningsInternal"
      pattern: "_maybeCurse"
    - from: "contracts/modules/DegenerusGameMintStreakUtils.sol _playerActivityScore"
      to: "curse counter"
      via: "penalty subtract before scoreBps = bonusBps"
      pattern: "scoreBps = bonusBps"
    - from: "decurse/smite Game stubs"
      to: "the existing host module"
      via: "delegatecall abi.encodeWithSelector"
      pattern: "delegatecall"
---

<objective>
Author Track B of the ONE batched v61.0 contract diff: the cashout-curse counter infrastructure (CURSE-01..07) and deity-smite (SMITE-01), all riding the mintPacked_ slot — a DIFFERENT storage slot from Track A's balances mapping. Producer-before-consumer: layout (CURSE-01) -> APPLY (CURSE-02) -> SET (CURSE-03) -> CURE (CURSE-04) -> lootbox stamp (CURSE-05) -> decurse (CURSE-06) -> view+cap (CURSE-07) -> smite (SMITE-01). The whole CURSE chain precedes SMITE because smite shares the counter (CURSE-01), the cap (D-03/CURSE-07), and the APPLY (CURSE-02).

Purpose: a mild progressive nudge against cash-out-and-ghost — each ghost-cashout (and each deity smite) subtracts 100 bps/point off every consumer of the player's activity score (floored 0), cured by any >=1-ticket buy or the permissionless paid decurse. Pure ledger/score effect: no ETH, no RNG, no prize-pool touch.

Output: the applied Track B diff, layered on top of Plan 01's Track A diff (already applied). The canonical full forge-build-clean + the EIP-170 size guardrail + the boundary HOLD are Plan 03. Run forge build at the end of this plan as a sanity gate.

CONTRACT-BOUNDARY HARD STOP — autonomous: false. NEVER commit any contracts/*.sol; the executor applies + compiles, then the diff is HELD for USER hand-review.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/SPEC-V61-DESIGN-LOCK.md
@.planning/REQUIREMENTS.md
@.planning/phases/376-impl-the-one-batched-contract-diff-afpay-pack-curse-smite/376-CONTEXT.md

<interfaces>
<!-- Extracted from baseline 2bee6d6f. Executor uses these directly. -->

BitPackingLib — contracts/libraries/BitPackingLib.sol:
  layout doc :9-25 (line :22 documents [215-227] unused); AFFILIATE_BONUS_POINTS_SHIFT = 209 (:82, MASK_6, ends bit 214); LEVEL_UNITS_SHIFT = 228 (:85, MASK_16); existing masks MASK_1/2/6/16/24/32; setPacked keystone :101 returns (data & ~(mask<<shift)) | ((value & mask)<<shift)
  NOTE: a module-local MASK_8 already exists in DegeneretteModule:346 and BP_MASK_8 in storage:1777 — those are separate scopes; add a NEW library-scoped BitPackingLib.MASK_8 (no clash, referenced as BitPackingLib.MASK_8).

_playerActivityScore — contracts/modules/DegenerusGameMintStreakUtils.sol:241 (3-arg) + :327 (2-arg wrapper):
  uint256 packed = mintPacked_[player];  // :248 — the curse counter rides THIS existing SLOAD (zero new SLOAD)
  scoreBps = bonusBps;                    // :320 — CURSE-02 subtracts curse*100 bps just before this
  _bountyEligible :30 (does NOT read the curse counter — confirms self-smite harmless)
  active-afker idiom: _subOf[who].dailyQuantity != 0  // :59

claimWinnings (SET host) — contracts/DegenerusGame.sol:1556:
  function claimWinnings(address player) external { player = _resolvePlayer(player); _claimWinningsInternal(player, false); }
  _claimWinningsInternal :1568 (private; the successful-claim body; lastEthDay from mintPacked_ DAY_SHIFT)
  claimWinningsStethFirst :1563 (vault-only — curse SET must NOT fire here)
  public playerActivityScore view :2701 (CURSE-07 reads near here for the UI)

CURE host _purchaseForWith — contracts/modules/DegenerusGameMintModule.sol:1093 (CORRECTED name; NO _purchaseWithFor):
  totalCost computed :1116; priceWei :1106; the mintPacked_ leg stamps (recordMintData / _recordLootboxMintDay) fold into the same RMW; score calc _playerActivityScore at :1285 — CURE must reset curse BEFORE :1285 so the curing buy gets the un-penalized score; plain standalone lootbox leg lives inside _purchaseForWith ~:1170-1254

_recordLootboxMintDay — contracts/modules/DegenerusGameWhaleModule.sol:1000 (private; CORRECTED line; call site :858):
  function _recordLootboxMintDay(address player, uint256 cachedPacked) private stamps DAY_SHIFT via _simulatedDayIndex()
  relocate this private fn to the MintStreakUtils base so the plain lootbox leg + the pass flows share one copy.

day basis — contracts/storage/DegenerusGameStorage.sol:
  _currentMintDay() :1309 (D-05 staleness basis: lastEthDay + 5 > _currentMintDay()); _simulatedDayIndex() :1257; Sub struct dailyQuantity :1968-1971; _subOf :2063

dispatch-stub pattern to mirror — contracts/DegenerusGame.sol:413 (claimAfkingBurnie) + :428 (drainAffiliateBase):
  delegatecall abi.encodeWithSelector(IGameAfkingModule.<fn>.selector, <args>); if (!ok) _revertDelegate(data);
  IGameAfkingModule interface — contracts/interfaces/IDegenerusGameModules.sol:471 (subscribe :478, mintBurnie :489, claimAfkingBurnie :498, drainAffiliateBase :506)

smite gate — contracts/DegenerusDeityPass.sol:335: function ownerOf(uint256 tokenId) external view returns (address) (soulbound, tokenId = symbolId 0-31); IDegenerusDeityPass interface already referenced in DegenerusGameWhaleModule.sol; DEITY_PASS constant ContractAddresses.sol:57
burn sink — contracts/BurnieCoin.sol:572: function burnCoin(address target, uint256 amount) external onlyGame; PRICE_COIN_UNIT = 1000 ether (DegenerusGameStorage.sol:162); coin var IDegenerusCoin internal constant coin (Storage:137)
protocol-addr skip (D-04, constant compares, no SLOAD): ContractAddresses.VAULT / SDGNRS / GNRUS
sDGNRS redemption-snapshot activity read — contracts/StakedDegenerusStonk.sol:932 (the reason D-04 keeps the protocol skip)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Curse layout (CURSE-01) + APPLY (CURSE-02) + saturating mutators + view+cap (CURSE-07)</name>
  <files>contracts/libraries/BitPackingLib.sol, contracts/modules/DegenerusGameMintStreakUtils.sol</files>
  <read_first>
    - .planning/SPEC-V61-DESIGN-LOCK.md §1 D-03 (CURSE_COUNT_CAP=20 + the cap-as-wrap-guard double-duty + the 5-stack smite ceiling headroom) + §4 Track B steps 1,2,7 + §3 BitPackingLib row (clobber-free empirical confirmation)
    - .planning/REQUIREMENTS.md CURSE-01, CURSE-02, CURSE-07
    - .planning/phases/376-impl-the-one-batched-contract-diff-afpay-pack-curse-smite/376-CONTEXT.md PLACE-01 (the MintStreakUtils base hosts the curse mutators / APPLY / cap / curseCountOf)
    - contracts/libraries/BitPackingLib.sol (full file — the layout doc + the shift/mask convention)
    - contracts/modules/DegenerusGameMintStreakUtils.sol:241-333 (_playerActivityScore both arities — the packed load :248 + the scoreBps = bonusBps site :320)
    - contracts/DegenerusGame.sol:2701-2710 (the public playerActivityScore view CURSE-07 sits near)
  </read_first>
  <action>
CURSE-01 — BitPackingLib: add uint256 internal constant CURSE_COUNT_SHIFT = 215; and uint256 internal constant MASK_8 = (uint256(1) << 8) - 1; (library-scoped — reference everywhere as BitPackingLib.MASK_8; it does NOT collide with the module-local MASK_8 in DegeneretteModule). Update the layout doc comment at line ~:22 from [215-227] (unused) to [215-222] CURSE_COUNT_SHIFT - cashout/smite curse counter (8 bits) plus [223-227] (unused). The field is clobber-free (all 12 mintPacked_ writers are field-isolated RMW via setPacked, confirmed in SPEC §3) — do NOT widen any existing mask over bits 215-222.

CURSE-02 — APPLY in DegenerusGameMintStreakUtils._playerActivityScore (3-arg def :241): the packed word is already loaded at :248, so read the counter with ZERO new SLOAD: uint256 curse = (packed >> BitPackingLib.CURSE_COUNT_SHIFT) & BitPackingLib.MASK_8;. Just before scoreBps = bonusBps; at :320, subtract: uint256 penaltyBps = curse * 100; bonusBps = bonusBps > penaltyBps ? bonusBps - penaltyBps : 0;. Floors at 0; propagates to every consumer + the public playerActivityScore view (:2701) + all frozen snapshots automatically (single chokepoint). The 2-arg wrapper (:327) delegates to the 3-arg, so it inherits the penalty.

CURSE-07 + CURSE_COUNT_CAP — in the MintStreakUtils base (PLACE-01: visible to Game + every module, zero duplication): add uint8 internal constant CURSE_COUNT_CAP = 20; (D-03). Add the saturating mutators used by SET/CURE/SMITE: internal _applyCurseStack(address target) — load mintPacked_[target], read the current curse, and if curse < CURSE_COUNT_CAP write back min(curse + 2, CURSE_COUNT_CAP) via setPacked (the cap is the uint8-wrap guard — never wrap 254->0); internal _clearCurse(address target) — zero the curse field via setPacked. Add public view curseCountOf(address player) external view returns (uint8) returning the curse field cast to uint8 so the UI reads it directly. Both mutators use field-isolated setPacked RMW — no clobber of the leg stamps or any other field.

Comments explain HOW the field packs + WHY the cap doubles as the wrap guard; no plan IDs / REQ tags / history.
  </action>
  <verify>
    <automated>forge build</automated>
  </verify>
  <acceptance_criteria>
    - forge build exits 0
    - BitPackingLib.sol contains CURSE_COUNT_SHIFT = 215 and MASK_8 (grep assertion); the layout doc names [215-222] as the curse counter
    - DegenerusGameMintStreakUtils.sol contains CURSE_COUNT_CAP = 20, _applyCurseStack, _clearCurse, and curseCountOf (grep assertion)
    - The APPLY reads curse from the packed word already loaded at :248 (zero new SLOAD) and subtracts curse*100 bps floored at 0 immediately before scoreBps = bonusBps (source assertion)
    - _applyCurseStack skips the write when curse >= CURSE_COUNT_CAP (saturation; no uint8 wrap) — source assertion
    - No existing mintPacked_ field mask is widened over bits 215-222 (the curse field stays isolated)
  </acceptance_criteria>
  <done>The curse counter field is defined in BitPackingLib; the APPLY subtracts the bps penalty at the single chokepoint (zero new SLOAD); the saturating mutators + cap + public view live in the MintStreakUtils base; forge build clean.</done>
</task>

<task type="auto">
  <name>Task 2: SET hook (CURSE-03) + CURE (CURSE-04) + lootbox stamp relocate (CURSE-05)</name>
  <files>contracts/DegenerusGame.sol, contracts/modules/DegenerusGameMintModule.sol, contracts/modules/DegenerusGameWhaleModule.sol, contracts/modules/DegenerusGameMintStreakUtils.sol</files>
  <read_first>
    - .planning/SPEC-V61-DESIGN-LOCK.md §1 D-04 (protocol-addr skip + the redemption-snapshot reason) + D-05 (staleness basis _currentMintDay()) + §4 Track B steps 3,4,5 + the Cross-check (CURE vs PACK repack touch different slots — safe) + §2 corrected anchors (_purchaseForWith :1093, _recordLootboxMintDay :1000)
    - .planning/REQUIREMENTS.md CURSE-03, CURSE-04, CURSE-05 + the Out-of-scope note (curse does NOT fire on claimWinningsStethFirst)
    - .planning/PLAN-CASHOUT-CURSE.md §3 (the SET cheapest-first bail sketch) + §Accepted edges
    - contracts/DegenerusGame.sol:1556-1595 (claimWinnings + _claimWinningsInternal), :1563 (claimWinningsStethFirst — must NOT curse)
    - contracts/modules/DegenerusGameMintModule.sol:1093-1306 (_purchaseForWith — the cure point before the score calc at :1285 + the plain lootbox leg ~:1170-1254)
    - contracts/modules/DegenerusGameWhaleModule.sol:1000-1016 (_recordLootboxMintDay) + :858 (its call site)
    - contracts/storage/DegenerusGameStorage.sol:1309 (_currentMintDay), :1968 (Sub struct), :2063 (_subOf)
  </read_first>
  <action>
CURSE-03 — SET. Add internal _maybeCurse(address player) to the MintStreakUtils base (reachable from the direct Game claimWinnings as a base-internal call). Call it from the public claimWinnings (DegenerusGame.sol:1556) AFTER a successful _claimWinningsInternal (NOT from claimWinningsStethFirst :1563 — vault-only, out of scope). Cheapest-first bails (skip the SSTORE on any): (1) infra — player == VAULT || SDGNRS || GNRUS via constant compares, no SLOAD (D-04); (2) gameOver; (3) non-stale — bail if lastEthDay + 5 > _currentMintDay() (D-05; lastEthDay from mintPacked_ DAY_SHIFT); (4) deity-pass holder (mintPacked_ HAS_DEITY_PASS_SHIFT bit); (5) whale/lazy pass active (frozenUntilLevel >= level with bundleType 1 or 3); (6) active afker — _subOf[player].dailyQuantity != 0; (7) already at cap — curse >= CURSE_COUNT_CAP. Otherwise call _applyCurseStack(player). Net: +2 only on a stale cashout below the cap by a non-exempt player. Do NOT add a DAY_SHIFT stamp here.

CURSE-04 — CURE. In _purchaseForWith (MintModule:1093 — the CORRECTED host name; NO _purchaseWithFor), reset the curse to 0 when totalCost >= priceWei (>=1 ticket worth, funding-agnostic) BEFORE the score calc at :1285 so the curing buy gets the un-penalized score. Fold the clear into the existing mintPacked_ RMW: call _clearCurse(buyer), OR fold the curse-field clear into the same setPacked chain that recordMintData / _recordLootboxMintDay use — with NO write-after-write clobber against those leg stamps (read the slot once, clear the curse field AND apply the leg stamps in one consistent RMW, or order the clear so a later stamp does not re-introduce a stale curse). The CURE touches the mintPacked_ slot; the Track A PACK repack touches the balances slot — different slots, no cross-track conflict (SPEC §4 Cross-check).

CURSE-05 — lootbox stamp relocate. Move _recordLootboxMintDay (WhaleModule:1000, private) to the MintStreakUtils base (one shared copy). Update the WhaleModule call site at :858 to use the relocated base helper (same signature). Then wire the plain standalone lootbox leg inside _purchaseForWith (~:1170-1254) through _recordLootboxMintDay so manual lootbox buyers stamp DAY_SHIFT and gain bounty eligibility (closes the plain-vs-pass-bundled inconsistency). Keep _recordLootboxMintDay's existing _simulatedDayIndex() day basis (it is the day-STAMP helper, distinct from the CURSE-03 staleness compare which uses _currentMintDay()).

Comments explain HOW the bails short-circuit + WHY claimWinningsStethFirst is excluded; no plan IDs / REQ tags / history.
  </action>
  <verify>
    <automated>forge build</automated>
  </verify>
  <acceptance_criteria>
    - forge build exits 0
    - DegenerusGame.sol claimWinnings calls _maybeCurse after _claimWinningsInternal; claimWinningsStethFirst does NOT call _maybeCurse (source assertion)
    - _maybeCurse bails on infra (VAULT/SDGNRS/GNRUS constant compares, no SLOAD), gameOver, non-stale (lastEthDay + 5 > _currentMintDay()), deity, whale/lazy pass, active afker (_subOf[player].dailyQuantity != 0), and cap — then _applyCurseStack (source assertion)
    - grep for _purchaseWithFor in contracts/ returns nothing; the CURE resets curse to 0 when totalCost >= priceWei BEFORE the score calc at ~:1285 (source assertion)
    - _recordLootboxMintDay now lives in the MintStreakUtils base (its definition is in DegenerusGameMintStreakUtils.sol, not WhaleModule); the WhaleModule call site at :858 still resolves; the plain lootbox leg in _purchaseForWith calls it
    - The CURE fold does not clobber the recordMintData / _recordLootboxMintDay leg stamps (single consistent mintPacked_ RMW) — source assertion
  </acceptance_criteria>
  <done>The SET hook fires +2 only on a stale non-exempt cashout via the public claimWinnings; the CURE clears the counter on a >=1-ticket buy before the score calc; _recordLootboxMintDay is relocated to the base and wired into the plain lootbox leg; forge build clean.</done>
</task>

<task type="auto">
  <name>Task 3: decurse (CURSE-06) + smite (SMITE-01) Game stubs + module impls + EIP-170 guardrail</name>
  <files>contracts/DegenerusGame.sol, contracts/modules/GameAfkingModule.sol, contracts/interfaces/IDegenerusGameModules.sol</files>
  <read_first>
    - .planning/SPEC-V61-DESIGN-LOCK.md §1 D-03 (cap), D-04 (protocol skip), Verification Item 2 (self-smite HARMLESS — no guard) + §4 Track B steps 6,8
    - .planning/REQUIREMENTS.md CURSE-06, SMITE-01
    - .planning/phases/376-impl-the-one-batched-contract-diff-afpay-pack-curse-smite/376-CONTEXT.md PLACE-01/02/03 (the stub-into-EXISTING-module decision + host preference GameAfkingModule > MintModule + the 24576-byte guardrail) + the <decisions> Execution posture
    - .planning/PLAN-V61-DEITY-SMITE.md §1 confirmed knobs + §3 sketch
    - contracts/DegenerusGame.sol:405-439 (the claimAfkingBurnie / drainAffiliateBase dispatch-stub pattern to mirror)
    - contracts/interfaces/IDegenerusGameModules.sol:471-510 (IGameAfkingModule — where the new selectors are declared if GameAfkingModule is the host)
    - contracts/modules/GameAfkingModule.sol (confirm it inherits MintStreakUtils, has coin + _subOf access, and the claimAfkingBurnie precedent)
    - contracts/DegenerusDeityPass.sol:335 (ownerOf gate) + the IDegenerusDeityPass interface in WhaleModule + contracts/BurnieCoin.sol:572 (burnCoin) + contracts/storage/DegenerusGameStorage.sol:162 (PRICE_COIN_UNIT), :137 (coin)
  </read_first>
  <action>
Host selection (PLACE-02, planner pick = GameAfkingModule): place the decurse + smite IMPLEMENTATIONS in GameAfkingModule (afking-immunity cohesion: active afker = immunity is an afking concept; already inherits MintStreakUtils, already has the claimAfkingBurnie stub precedent, already references coin + _subOf). Add decurse + smite to the IGameAfkingModule interface (IDegenerusGameModules.sol:471). Add the two thin Game dispatch stubs in DegenerusGame.sol mirroring claimAfkingBurnie:413 exactly: a delegatecall to ContractAddresses.GAME_AFKING_MODULE with abi.encodeWithSelector(IGameAfkingModule.decurse.selector, target) and abi.encodeWithSelector(IGameAfkingModule.smite.selector, deityId, smitee); if (!ok) _revertDelegate(data). GUARDRAIL (PLACE-03): after the stubs land, the Game MUST stay under the 24576-byte EIP-170 ceiling. If forge build --sizes shows DegenerusGame over the ceiling, FALL BACK to hosting the impls in DegenerusGameMintModule (PLACE-02 preference 2 — cure-adjacency, already calls burnCoin) and re-point the stubs/interface; if still over, STOP and surface as a BLOCKER at hand-review (do NOT silently work around it). Record the chosen host + the measured Game size in the SUMMARY.

CURSE-06 — decurse(address target) impl: permissionless (NO _resolvePlayer — clearing another's curse is purely beneficial); revert E() if the target's curse is already 0 (no wasted burn); coin.burnCoin(msg.sender, PRICE_COIN_UNIT / 10) (100 BURNIE from the caller); _clearCurse(target); emit Decursed(msg.sender, target). NO DAY_SHIFT stamp. Declare event Decursed(address indexed curer, address indexed target).

SMITE-01 — smite(uint256 deityId, address smitee) impl: gate IDegenerusDeityPass(ContractAddresses.DEITY_PASS).ownerOf(deityId) == msg.sender (soulbound single-tier, tokenId = symbolId 0-31). Validate smitee BEFORE the burn: (1) active-afking immunity — revert if _subOf[smitee].dailyQuantity != 0 (the SOLE immunity); (2) smite ceiling — revert if the smitee's curse points >= 10 (the 5-stack ceiling; 1 stack = 2 points, so a deity cannot push past 10 points via smite while the cashout path reaches the 20-point cap, per D-03); (3) protocol-addr skip (D-04) — revert on VAULT/SDGNRS/GNRUS via constant compares. Self-smite is ALLOWED (Verification Item 2: harmless-by-design, no guard). Then coin.burnCoin(msg.sender, PRICE_COIN_UNIT / 5) (200 BURNIE); _applyCurseStack(smitee) (the same saturating +2 at the shared cap); emit Smited(deityId, smitee). 1 smite/tx. Pure ledger/score — no ETH, no rngWord, no prize-pool touch. Declare event Smited(uint256 indexed deityId, address indexed smitee). Reuse the IDegenerusDeityPass interface already referenced in WhaleModule (import it — do not redeclare a conflicting one).

Comments explain HOW the gate + validations + burn order work + WHY self-smite needs no guard; no plan IDs / REQ tags / history.
  </action>
  <verify>
    <automated>forge build --sizes</automated>
  </verify>
  <acceptance_criteria>
    - forge build --sizes exits 0 AND reports DegenerusGame under 24576 bytes (PLACE-03 guardrail; if over, the fallback host was used or it is surfaced as a BLOCKER in the SUMMARY)
    - DegenerusGame.sol contains decurse and smite dispatch stubs delegatecalling the host module (grep: delegatecall + the new selectors); the IGameAfkingModule interface (or the fallback module's interface) declares decurse and smite
    - decurse is permissionless (no _resolvePlayer), reverts when the target curse is already 0, burns PRICE_COIN_UNIT / 10, clears the curse, emits Decursed (source assertion)
    - smite gates ownerOf(deityId) == msg.sender, reverts on active-afker smitee + on >= 10 curse points + on protocol addrs BEFORE the burn, burns PRICE_COIN_UNIT / 5, applies the saturating +2, emits Smited (source assertion); self-smite has no guard
    - No rngWord reference is added in any touched file (RNG-freeze: smite/decurse are score/ledger-only)
  </acceptance_criteria>
  <done>decurse + smite are thin Game dispatch stubs into the chosen existing module with full impls (gate, validations, burns, saturating stack); the Game stays under the EIP-170 ceiling (or the fallback host is used / a BLOCKER surfaced); forge build clean; no RNG read added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external caller -> claimWinnings (curse SET) | untrusted ghost-cashout crosses here; the SET only LOWERS the caller's own score |
| deity caller -> smite | gated by ownerOf(deityId)==msg.sender; the smitee's score is LOWERED; deity burns own BURNIE |
| anyone -> decurse | permissionless beneficial clear; caller burns own BURNIE |
| the curse field (mintPacked_ bits 215-222) -> the activity-score APPLY | the only consumer of the counter |

## STRIDE Threat Register (grounded in the repo hard floor)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-376B-RNG | RNG-freeze (DOMINANT) | CURSE/SMITE on the activity-score path | mitigate | CURSE/SMITE touch ONLY the activity-score path: a view-only read of the counter inside _playerActivityScore + a score-LOWERING write on a successful access-controlled claim/smite. NO rngWord read or write added. Acceptance: no new rngWord reference in any touched file (proven empirically at SEC-01/378). |
| T-376B-SOLV | Solvency (SPINE) | the whole Track B | accept | Track B is OFF the ETH/claimablePool path entirely (no balance touch). The BURNIE burns are pure sinks via burnCoin (onlyGame). SOLVENCY-01 untouched (proven at SEC-02/378). |
| T-376B-WRAP | Tampering | the uint8 curse counter | mitigate | CURSE_COUNT_CAP = 20 is the mandatory uint8-wrap guard: _applyCurseStack skips the SSTORE when curse >= cap, so a +2 can never wrap 254->0. The 5-stack (10-point) smite ceiling sits cleanly below the 20-point cap (D-03). Proven at TST-03/TST-05/378. |
| T-376B-AC | Access control | smite gate + decurse + the SET source | mitigate | smite gated by IDegenerusDeityPass.ownerOf(deityId) == msg.sender (soulbound); active-afker = sole immunity; >=5-stack ceiling; D-04 protocol-addr skip (constant compares) on both curse sources — protects the sDGNRS redemption-snapshot score (StakedStonk:932). The SET fires only on the public claimWinnings, never the vault-only claimWinningsStethFirst. decurse is permissionless-by-design (beneficial; burns the caller's own 100 BURNIE). Self-smite is harmless-by-design (Verification Item 2 — counter only lowers score; _bountyEligible does not read it). |
| T-376B-DOS | Gas-DoS | the APPLY + the SET/CURE/smite writes | accept | CURSE-02 APPLY rides the packed word already loaded at :248 (zero new SLOAD on the hot activity-score read); the SET/CURE/smite writes are single field-isolated setPacked RMWs, none on the advanceGame hot path; no new unbounded loop. Formal measurement at 377. |
| T-376B-SC | Tampering | npm/pip/cargo installs | mitigate | None — this plan adds NO package-manager installs (contract edits only). No package legitimacy gate needed. |
</threat_model>

<verification>
- forge build --sizes exits 0; DegenerusGame stays under 24576 bytes (PLACE-03).
- RNG-freeze: no new rngWord reference in any touched file (CURSE/SMITE are score/ledger-only).
- The curse counter saturates at CURSE_COUNT_CAP=20 and never wraps the uint8 (the cap-guarded mutator).
- The SET fires only on the public claimWinnings (not claimWinningsStethFirst); the protocol-addr skip protects the sDGNRS redemption snapshot.
- No _purchaseWithFor symbol anywhere (the corrected host name _purchaseForWith is used for the CURE).
- decurse/smite are thin Game dispatch stubs into the chosen existing module (no new module, no inline-in-Game fat).
</verification>

<success_criteria>
- The curse counter (mintPacked_ bits 215-222), APPLY, SET, CURE, lootbox stamp, decurse, view+cap, and smite are all authored, sharing one saturating field.
- The activity-score penalty propagates from the single chokepoint to every consumer + the public view + frozen snapshots.
- RNG-freeze intact (no rngWord read added); SOLVENCY-01 untouched (Track B off the ETH path); the Game stays under the EIP-170 ceiling.
- forge build clean; the diff is HELD at the contract-commit boundary (NOT committed) for USER hand-review.
</success_criteria>

<output>
Create `.planning/phases/376-impl-the-one-batched-contract-diff-afpay-pack-curse-smite/376-02-SUMMARY.md` when done. Record: the chosen decurse/smite host module (GameAfkingModule or the MintModule fallback) + the measured DegenerusGame bytecode size vs 24576, the curse field layout, the SET bail order, and the CURE RMW-fold approach. Do NOT commit any contracts/*.sol — leave the Track B diff applied + compiled for Plan 03 to build clean and hold at the boundary.
</output>
