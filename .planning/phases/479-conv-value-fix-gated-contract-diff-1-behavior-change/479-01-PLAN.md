---
phase: 479-conv-value-fix-gated-contract-diff-1-behavior-change
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/storage/DegenerusGameStorage.sol
  - contracts/modules/DegenerusGameJackpotModule.sol
  - contracts/modules/DegenerusGameLootboxModule.sol
autonomous: false
requirements: [CONV-01, CONV-02, FIX-01, FIX-02, FIX-03, FIX-05]

must_haves:
  truths:
    - "A jackpot BAF ticket roll and a lootbox roll (incl. the decimator-recirc path) queue the entries basis (whole<<2), delivering ~4x the pre-fix value"
    - "Both prize legs route their queued count through ONE canonical converter wholeTicketsToEntries(w)=w<<2; no award leg open-codes amount/price into the entries sink"
    - "The Bernoulli round-up block is byte-identical (the <<2 is applied to the post-Bernoulli queued entries, never inside the Bernoulli expression); the EV testers pass unchanged"
    - "emit == queue holds on the Jackpot path: JackpotTicketWin emits the same wholeTicketsToEntries(whole) it queues (field still named ticketCount in 479)"
    - "The ticket/entry convention is documented in NatSpec at _queueTickets, ticketsOwedPacked, and the canonical helper; the misleading _queueTicketsScaled NatSpec is corrected"
    - "The two call-site comments the value change makes false are corrected to entries (JackpotModule :2145-2146 and :889); no contracts/*.sol comment names a phase/plan ID"
    - "All .sol edits ship as ONE batched diff approved by the USER before commit"
  artifacts:
    - path: "contracts/storage/DegenerusGameStorage.sol"
      provides: "wholeTicketsToEntries canonical helper + CONV-02 convention NatSpec"
      contains: "function wholeTicketsToEntries"
    - path: "contracts/modules/DegenerusGameJackpotModule.sol"
      provides: "_jackpotTicketRoll queue+emit routed through the canonical helper"
      contains: "wholeTicketsToEntries(whole)"
    - path: "contracts/modules/DegenerusGameLootboxModule.sol"
      provides: "lootbox roll resolution queue routed through the canonical helper"
      contains: "wholeTicketsToEntries(whole)"
  key_links:
    - from: "DegenerusGameJackpotModule._jackpotTicketRoll (:2143)"
      to: "Storage.wholeTicketsToEntries -> _queueTickets"
      via: "queue post-Bernoulli whole as entries"
      pattern: "_queueTickets\\(winner, targetLevel, wholeTicketsToEntries\\(whole\\), true\\)"
    - from: "DegenerusGameLootboxModule resolve path (:1383)"
      to: "Storage.wholeTicketsToEntries -> _queueTickets"
      via: "queue post-Bernoulli whole as entries (covers decimator resolveLootboxDirect transitively)"
      pattern: "_queueTickets\\(player, rollLevel, wholeTicketsToEntries\\(whole\\), false\\)"
    - from: "DegenerusGameJackpotModule emit JackpotTicketWin (:2147)"
      to: "the queued entries value"
      via: "emit==queue invariant (4th arg equals the _queueTickets 3rd arg)"
      pattern: "wholeTicketsToEntries\\(whole\\)"
---

<objective>
Fix the 1/4 under-delivery in both prize legs (Jackpot BAF roll + Lootbox roll, the latter also covering the decimator-recirc path) by routing each leg's post-Bernoulli whole-ticket count through ONE canonical whole-ticket->entries converter, and land the ticket/entry convention NatSpec so the bug class cannot recur — WITHOUT disturbing the Bernoulli round-up's EV-neutrality.

Purpose: `ticketsOwedPacked.owed` is denominated in ENTRIES (1 whole ticket = 4 entries; 1 entry = price/4 = 1 NFT). Two prize legs queue a whole-ticket count (`amount/price`, no `<<2`) into the entries sink, delivering 1/4 of warranted value (USER-confirmed against the live RTP sim; conservation-safe, winners under-paid). This plan restores full value.

Output: ONE batched `contracts/*.sol` diff — a small pure helper + 2 call-site arg changes + 1 forced emit-value change + convention NatSpec + 2 call-site comment corrections — presented for USER approval before commit (contract-commit gate, success criterion 5).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/479-conv-value-fix-gated-contract-diff-1-behavior-change/479-RESEARCH.md
@.planning/phases/479-conv-value-fix-gated-contract-diff-1-behavior-change/479-VALIDATION.md
@.planning/v75-grounding/v75.0-ticket-entry-map.md
@./CLAUDE.md

<interfaces>
<!-- Ground-truth unit convention + exact sites. Use directly — no codebase exploration needed. -->

Unit convention (verified 3 ways): ticketsOwedPacked[wk][buyer] packs [uint32 owed][uint8 rem].
`owed` is in ENTRIES. 1 whole ticket = 4 entries. 1 entry = priceForLevel(level)/4 = 1 minted NFT. TICKET_SCALE = 100.

Storage sink (contracts/storage/DegenerusGameStorage.sol):
  :522  mapping(uint24 => mapping(address => uint40)) internal ticketsOwedPacked;   // value = [uint32 owed (ENTRIES)][uint8 rem]
  :631-636  NatSpec for _queueTickets — currently says "Queues whole tickets"/"Number of tickets" (MISLEADING; arg is ENTRIES)
  :637  function _queueTickets(address buyer, uint24 targetLevel, uint32 quantity, bool rngBypass) internal  // owed += quantity (entries)
  :668-672  NatSpec for _queueTicketsScaled — says "multiply by 100 for whole tickets" (WRONG; quantityScaled is scaled-ENTRIES = entries*100)
  :673  function _queueTicketsScaled(...)

Jackpot bug site (contracts/modules/DegenerusGameJackpotModule.sol, _jackpotTicketRoll @ :2098):
  :2127  uint256 quantityScaled = (amount * TICKET_SCALE) / targetPrice;            // scaled-WHOLE-tickets
  :2133  uint32 scaledTickets = uint32(quantityScaled);
  :2134  uint32 whole = scaledTickets / uint32(TICKET_SCALE);                        // WHOLE tickets  <-- KEEP byte-identical
  :2135  uint32 frac  = scaledTickets % uint32(TICKET_SCALE);                        // <-- KEEP
  :2137  if (frac != 0 && (uint32(entropy >> 96) % uint32(TICKET_SCALE)) < frac) { whole += 1; roundedUp = true; }  // <-- KEEP byte-identical
  :2143  _queueTickets(winner, targetLevel, whole, true);                           // BUG: whole into ENTRIES sink -> 1/4
  :2147-2155  emit JackpotTicketWin(winner, targetLevel, BAF_TRAIT_SENTINEL, whole /*:2151 = 4th positional arg*/, minTargetLevel, 0, roundedUp);

Already-correct Jackpot leg (DO NOT TOUCH) — :888 _queueTickets(winner, queueLvl, uint32(units), true); :890 emits uint32(units) (entries via _budgetToTicketUnits).

Lootbox bug site (contracts/modules/DegenerusGameLootboxModule.sol):
  :1375  uint32 whole = scaledTickets / uint32(TICKET_SCALE);                        // WHOLE tickets  <-- KEEP
  :1376-1380  Bernoulli round-up on bits[224..255]  <-- KEEP byte-identical
  :1383  _queueTickets(player, rollLevel, whole, false);                            // BUG: whole into ENTRIES sink -> 1/4
  :1397-1405  emit LootBoxOpened(player, index, fullAmount, rollLevel, scaledTickets /*:1402*/, flipAmount, roundedUp);
  Also reached via DegenerusGameDecimatorModule.resolveLootboxDirect (:677 delegatecall) — the single :1383 fix covers it. DO NOT add a 2nd fix in the Decimator (double-apply hazard).

Inheritance: DegenerusGameLootboxModule is DegenerusGameStorage; DegenerusGameJackpotModule is DegenerusGamePayoutUtils (-> DegenerusGameStorage). Both call _queueTickets (in Storage), so an internal pure helper added to Storage is reachable from both. A pure function adds NO storage slot — layout is byte-stable.

Overflow bound: whole <= scaledTickets/100 <= 42,949,672 (scaledTickets is uint32-capped); whole<<2 <= ~171.8M < uint32 max (4.29e9). 25x margin — no truncation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonical wholeTicketsToEntries helper + CONV-02 convention NatSpec (Storage)</name>
  <files>contracts/storage/DegenerusGameStorage.sol</files>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol (:522 ticketsOwedPacked decl; :631-672 _queueTickets + _queueTicketsScaled NatSpec/bodies)
    - .planning/phases/479-conv-value-fix-gated-contract-diff-1-behavior-change/479-RESEARCH.md (section 3 the fix, section 5 CONV-02 NatSpec targets, section 9 landmines)
  </read_first>
  <action>
    Add an `internal pure` canonical converter `wholeTicketsToEntries(uint32 wholeTickets) returns (uint32)` to DegenerusGameStorage.sol, placed adjacent to `_queueTickets` (~:636). It returns `wholeTickets << 2` (CONV-01: the one canonical post-Bernoulli whole->entries conversion both prize modules route through; reachable from both because they inherit Storage). Keep the return type uint32 — `wholeTickets` is provably <= ~42.9M (scaledTickets/100, uint32-capped), so `<< 2` <= ~171.8M fits uint32 with a 25x margin; no overflow guard needed.

    Add CONV-02 convention NatSpec stating units (describe what the code IS, never history — per feedback_no_history_in_comments):
    - On the new helper: in = whole tickets (each = priceForLevel(level)); out = entries (each = price/4, 1 minted NFT); 4 entries per whole ticket.
    - On `ticketsOwedPacked` (:522): `owed` is denominated in ENTRIES (price/4 units), NOT whole tickets.
    - On `_queueTickets` (:631-636): correct the misleading "whole tickets"/"Number of tickets" wording — the `quantity` arg is ENTRIES; `owed += quantity` accumulates entries.
    - On `_queueTicketsScaled` (:668-672): correct "multiply by 100 for whole tickets" — `quantityScaled` is scaled-ENTRIES (entries x 100); promotion to a whole owed unit happens at the TICKET_SCALE remainder boundary.

    NatSpec / comment-only edits to existing functions plus one tiny pure helper — no change to any existing identifier name (renames are Phase 480) and no change to existing logic. Do NOT rename `_queueTickets`, `ticketsOwedPacked`, or any param.
  </action>
  <verify>
    <automated>grep -q "function wholeTicketsToEntries(uint32" contracts/storage/DegenerusGameStorage.sol && grep -q "wholeTickets << 2" contracts/storage/DegenerusGameStorage.sol && npx hardhat compile 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `wholeTicketsToEntries(uint32 wholeTickets) internal pure returns (uint32)` exists in DegenerusGameStorage.sol and returns `wholeTickets << 2`.
    - CONV-02 NatSpec present at the helper, `ticketsOwedPacked` decl, `_queueTickets`, and `_queueTicketsScaled`, each stating the entries unit (grep finds "entries" + "price/4" near these symbols).
    - No existing identifier renamed; no existing logic line changed (only added helper + comment text).
    - `npx hardhat compile` succeeds.
  </acceptance_criteria>
  <done>The canonical converter exists in Storage and the four convention NatSpec sites state the entries basis; project compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Route both prize legs through the helper + forced Jackpot emit==queue reconciliation</name>
  <files>contracts/modules/DegenerusGameJackpotModule.sol, contracts/modules/DegenerusGameLootboxModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameJackpotModule.sol (:2127-2155 — Bernoulli block KEEP, queue :2143, emit :2147-2155; :2145-2146 comment to correct; :884-892 already-correct leg whose :889 comment to correct)
    - contracts/modules/DegenerusGameLootboxModule.sol (:1375-1405 — Bernoulli block KEEP, queue :1383, emit :1397-1405)
    - .planning/phases/479-conv-value-fix-gated-contract-diff-1-behavior-change/479-RESEARCH.md (section 3 fix, section 4 emit==queue decision point, section 9 landmines)
    - test/integration/CrossSurfaceTicketMixing.test.js ([03c] asserts the Jackpot emit 4th arg equals the adjacent _queueTickets 3rd arg; [CROSS-01d] embeds the exact call-site strings this edit must produce)
  </read_first>
  <action>
    FIX-01 (Jackpot, :2143): change `_queueTickets(winner, targetLevel, whole, true)` to `_queueTickets(winner, targetLevel, wholeTicketsToEntries(whole), true)`.
    FIX-01 emit==queue (Jackpot, :2151): change the 4th positional `emit JackpotTicketWin(...)` arg from `whole` to `wholeTicketsToEntries(whole)` so the emitted value equals the queued entries (emit==queue on the entries basis — FIX-05 contract side; keeps integration assertion [03c] green). This is a VALUE normalization only — the field stays named `ticketCount` and the event signature is byte-identical (the field rename is Phase 481). Do NOT touch the already-correct :888/:890 `uint32(units)` LOGIC line.

    CONV-02 comment correction (Jackpot, :2145-2146 — REQUIRED, comment-only): once the queued/emitted value is entries, the existing comment ("ticketCount is the whole ticket count queued above; roundedUp is true iff the bits[96..127] Bernoulli sub-roll incremented that count") is false. Rewrite it to state the entries basis while keeping the roundedUp clause accurate — the Bernoulli sub-roll still increments the underlying WHOLE-ticket count, not entries. e.g. "ticketCount is the entries count (whole<<2, 4 per whole ticket) queued above; roundedUp is true iff the bits[96..127] Bernoulli sub-roll incremented the underlying whole-ticket count."

    CONV-02 comment correction (Jackpot, :889 — REQUIRED, comment-only, logic untouched): the existing "ticketCount carries the whole ticket count awarded." on the already-correct leg is wrong at baseline (that leg emits `uint32(units)` = entries via `_budgetToTicketUnits`). Correct it to state `ticketCount` carries ENTRIES. Leave the `uint32(units)` logic line unchanged.

    FIX-02 (Lootbox, :1383): change `_queueTickets(player, rollLevel, whole, false)` to `_queueTickets(player, rollLevel, wholeTicketsToEntries(whole), false)`. This single site also covers the decimator-recirc path (resolveLootboxDirect) — do NOT edit DegenerusGameDecimatorModule.sol.

    FIX-02 emit decision (Lootbox, :1402 — DECISION RESOLVED, deferral): LEAVE `emit LootBoxOpened(... scaledTickets ...)` as the scaled-whole value. The integration emit==queue assertion [03c] reads ONLY the Jackpot file and does NOT cover LootBoxOpened; normalizing the LootBoxOpened emitted unit to entries is the full event-surface job of the later event-surface phase (EVT-01). Do NOT add a new contract comment here — the existing :1374 comment ("scaledTickets stays at the scaled value for the event emit") already documents what the code IS, and a contracts/*.sol comment must NOT name a phase/plan ID (lean-code-comments-no-procedural-meta / feedback_no_history_in_comments). Record the deferral ONLY in 479-01-SUMMARY.md: LootBoxOpened still emits scaledTickets, its emit-unit normalization to entries is left to the event-surface phase, and no 479 test asserts emit==queue on this path.

    FIX-03 (hard floor): do NOT modify lines 2127-2141 / 1362-1380. The Bernoulli collapse (`whole = scaledTickets / TICKET_SCALE`, `frac`, the round-up) stays byte-identical; the `<<2` lives ONLY inside `wholeTicketsToEntries`, applied to the post-Bernoulli `whole`. If any change here requires editing a test/stat/*BernoulliEv* tester, STOP — the Bernoulli expression was disturbed (design error).

    Total contract hunk across Tasks 1+2: 1 pure helper + 4 Storage NatSpec edits + 2 call-site arg changes + 1 emit-value change + 2 call-site comment corrections (Jackpot :2145-2146 + :889). NO new procedural-meta comment. Minimal, no future-proofing (contracts are frozen-at-deploy).

    Out of scope (optional, NOT required): contracts/test/LootboxBernoulliTester.sol:71 has a NatSpec mirror of the old call form that goes stale; leaving it as-is is acceptable. If refreshed, it must be comment-only and the Bernoulli collapse expression must stay byte-identical (EV-tester tripwire).
  </action>
  <verify>
    <automated>grep -q "_queueTickets(winner, targetLevel, wholeTicketsToEntries(whole), true)" contracts/modules/DegenerusGameJackpotModule.sol && grep -q "_queueTickets(player, rollLevel, wholeTicketsToEntries(whole), false)" contracts/modules/DegenerusGameLootboxModule.sol && ! grep -nE "_queueTickets\([a-zA-Z]+, (targetLevel|rollLevel), whole," contracts/modules/DegenerusGameJackpotModule.sol contracts/modules/DegenerusGameLootboxModule.sol && npx hardhat compile && npx hardhat test test/stat/JackpotTicketRollBernoulliEv.test.js test/stat/LootboxBernoulliEv.test.js test/stat/LootboxAutoResolveBernoulliEv.test.js && forge test --match-contract JackpotSingleCallCorrectness</automated>
  </verify>
  <acceptance_criteria>
    - Jackpot :2143 reads `_queueTickets(winner, targetLevel, wholeTicketsToEntries(whole), true)`; Jackpot :2151 emit 4th arg reads `wholeTicketsToEntries(whole)` (emit value == queue value).
    - Jackpot :2145-2146 comment states the value is entries (`whole<<2`, 4 per whole ticket) and the roundedUp clause references the underlying WHOLE-ticket count; Jackpot :889 comment states `ticketCount` carries ENTRIES. Both comment-only — the `uint32(units)` logic line at :888 is unchanged.
    - Lootbox :1383 reads `_queueTickets(player, rollLevel, wholeTicketsToEntries(whole), false)`; LootBoxOpened still emits `scaledTickets`; NO new contract comment added and the existing :1374 comment is unchanged.
    - No `contracts/*.sol` comment in the diff names a phase/plan ID (no "479"/"480"/"481"/"Phase N").
    - No `_queueTickets(..., whole, ...)` bare-whole form remains in either module; the :888 `uint32(units)` logic line is unchanged; DegenerusGameDecimatorModule.sol is untouched.
    - Bernoulli collapse lines (2127-2141 / 1362-1380) byte-identical; all three `test/stat/*BernoulliEv*` testers pass UNCHANGED; `forge test --match-contract JackpotSingleCallCorrectness` green.
    - `npx hardhat compile` succeeds.
  </acceptance_criteria>
  <done>Both prize legs queue entries via the canonical helper, the Jackpot emit equals its queued entries, the Lootbox emit deferral is documented, and the Bernoulli EV testers + correct-leg forge test stay green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: USER approval of the batched contracts/*.sol diff before commit</name>
  <files>contracts/storage/DegenerusGameStorage.sol, contracts/modules/DegenerusGameJackpotModule.sol, contracts/modules/DegenerusGameLootboxModule.sol</files>
  <action>Present the single batched contracts/*.sol diff (Tasks 1+2) to the USER for review and approval BEFORE committing. The repo gates ONLY the commit of contracts/*.sol; do not run the commit until the USER approves the diff (success criterion 5). On approval, commit the three .sol files as one diff. Do not commit if the diff includes any unexpected file or any change to the Bernoulli collapse blocks.</action>
  <what-built>
    A single batched `contracts/*.sol` diff across three files: `DegenerusGameStorage.sol` (wholeTicketsToEntries pure helper + CONV-02 NatSpec at the helper, ticketsOwedPacked, _queueTickets, _queueTicketsScaled), `DegenerusGameJackpotModule.sol` (queue+emit routed through the helper at the BAF roll, plus :2145-2146 and :889 comment corrections to the entries basis), `DegenerusGameLootboxModule.sol` (queue routed through the helper; LootBoxOpened emit + its :1374 comment unchanged). Behavior change: both prize legs now deliver ~4x the pre-fix entries; conservation-safe (restores the already-collateralized payout — the undelivered 3/4 was pooled). RNG/Bernoulli entropy path untouched (RNG-freeze invariant preserved).
  </what-built>
  <how-to-verify>
    1. Run `git diff --stat contracts/` — confirm ONLY the three files above changed.
    2. Run `git diff contracts/` and review: the only LOGIC changes are (a) the new pure helper, (b) `whole` -> `wholeTicketsToEntries(whole)` at Jackpot :2143 and its emit 4th arg, (c) `whole` -> `wholeTicketsToEntries(whole)` at Lootbox :1383; everything else is NatSpec/comment text.
    3. Confirm the comment corrections landed: Jackpot :2145-2146 and :889 now state ENTRIES; no `contracts/*.sol` comment names a phase/plan ID; the existing Lootbox :1374 comment is unchanged.
    4. Confirm the Bernoulli collapse blocks are NOT in the diff and the :888 `uint32(units)` LOGIC line is unchanged (only its :889 comment is corrected).
    5. Approve to commit the batched `.sol` diff (success criterion 5), or describe required changes.
  </how-to-verify>
  <resume-signal>Type "approved" to commit the batched contract diff, or describe required adjustments.</resume-signal>
  <verify>
    <human-check>USER has reviewed `git diff contracts/` and explicitly approved the batched .sol diff for commit; `git diff --stat contracts/` shows only the three expected files.</human-check>
  </verify>
  <done>The batched contracts/*.sol diff is USER-approved and committed as one diff (or adjustments captured and re-presented).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RNG (VRF word) -> prize resolution | Bernoulli round-up consumes a fixed entropy slice; must stay frozen vs players (DOMINANT threat class) |
| prize award -> ticketsOwedPacked entries sink | value the contract owes a winner; under/over-issue is a solvency/backing (SPINE) concern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-479-01 | Tampering (RNG-freeze / EV) | Bernoulli collapse in _jackpotTicketRoll (:2127-2141) + lootbox resolve (:1362-1380) | mitigate | The `<<2` is confined to the post-Bernoulli queued value inside `wholeTicketsToEntries`; the Bernoulli expression + its entropy slice (`entropy>>96`, `rollSeed>>224`) are byte-identical. Tripwire: the three `test/stat/*BernoulliEv*` testers must pass UNCHANGED. |
| T-479-02 | Denial of Service (solvency / backing) | entries sink delivering ~4x | accept | Restores the intended, already-collateralized payout — the undelivered 3/4 was pooled (over-collateralized, RTP-suppressing). No attacker gain; conservation holds. RTP re-sim confirms the target band in Phase 482 (VER-02). |
| T-479-03 | Tampering (integer truncation) | `whole << 2` at uint32 width in wholeTicketsToEntries | mitigate | `whole <= scaledTickets/100 <= ~42.9M` (scaledTickets is uint32-capped); `<<2 <= ~171.8M < 4.29e9 (uint32 max)`. 25x margin, no truncation. Pinned for max-realistic `whole` by the 479-02 regression (FIX-04). |
| T-479-04 | Elevation / access | no new external entry points; no access-control surface | accept | Edits touch one private fn, one internal resolve path, and one internal pure helper. No new selectors, no new caller surface. |
| T-479-SC | Tampering (supply chain) | npm/forge package installs | accept | No package installs in this phase — only existing-dep `.sol`/test edits. The Package Legitimacy Gate does not apply. |
</threat_model>

<verification>
- `npx hardhat compile` green; `forge build` green.
- Bernoulli EV testers green UNCHANGED: `npx hardhat test test/stat/JackpotTicketRollBernoulliEv.test.js test/stat/LootboxBernoulliEv.test.js test/stat/LootboxAutoResolveBernoulliEv.test.js`.
- Correct-leg regression unbroken: `forge test --match-contract JackpotSingleCallCorrectness`.
- Source assertions: both prize legs route via `wholeTicketsToEntries(whole)`; no bare-`whole` `_queueTickets` form; CONV-02 NatSpec present at the four sites.
- The definitive 4x-delivery + emit==queue behavioral proof lands in Plan 479-02 (wave 2). This plan's automated signals (compile + EV testers + correct-leg forge + source greps) are the necessary wave-1 guards.
- Baseline floor: keep the broader suite at or above 893/0 (re-checked after 479-02 reconciles the 2 stale CrossSurfaceTicketMixing assertions).
</verification>

<success_criteria>
- `wholeTicketsToEntries(uint32)=w<<2` exists in Storage; both prize legs queue via it; no award leg open-codes `amount/price` into the entries sink (CONV-01).
- CONV-02 NatSpec at `_queueTickets`, `ticketsOwedPacked`, the helper; `_queueTicketsScaled` NatSpec corrected; the Jackpot call-site comments at :2145-2146 and :889 corrected to entries (comment-only).
- Jackpot emit==queue holds on the entries basis (4th arg == queued entries); Lootbox LootBoxOpened keeps its scaled emit (existing :1374 comment unchanged), deferral recorded in SUMMARY only; no `contracts/*.sol` comment names a phase/plan ID.
- Bernoulli collapse byte-identical; EV testers + JackpotSingleCallCorrectness green; compile green.
- ONE batched `.sol` diff approved by the USER before commit (autonomous:false checkpoint).
</success_criteria>

<output>
Create `.planning/phases/479-conv-value-fix-gated-contract-diff-1-behavior-change/479-01-SUMMARY.md` when done. Record: the canonical helper home + signature, the exact call-site/emit edits, the LootBoxOpened emit deferral to 481, confirmation the Bernoulli blocks + EV testers were untouched/green, and the USER approval + commit SHA of the batched contract diff.
</output>
