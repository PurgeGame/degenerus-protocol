---
phase: 479-conv-value-fix-gated-contract-diff-1-behavior-change
plan: 02
type: execute
wave: 2
depends_on: ["479-01"]
files_modified:
  - test/fuzz/PrizeLegEntriesDelivery.t.sol
  - test/integration/CrossSurfaceTicketMixing.test.js
autonomous: true
requirements: [FIX-04, FIX-05]

must_haves:
  truths:
    - "A deterministic regression proves the prize-leg entries basis equals (B<<2)/price and matches the _budgetToTicketUnits purchase/daily basis (uniform entries-per-ETH)"
    - "The canonical converter wholeTicketsToEntries(w)=w<<2 is proven for representative and max-realistic w with no uint32 truncation"
    - "Driving the real openBox full-stack delivers owed-entries == the entries basis (~4x pre-fix), proving the lootbox leg behaviorally"
    - "emit == queue holds on the touched Jackpot path; CrossSurfaceTicketMixing [03a]/[03b]/[03c]/[CROSS-01d] are reconciled to the current 2-emit entries reality and green"
    - "The three Bernoulli EV testers are re-confirmed green UNCHANGED"
  artifacts:
    - path: "test/fuzz/PrizeLegEntriesDelivery.t.sol"
      provides: "Deterministic forge proof of the canonical converter + entries-basis equivalence + no-overflow"
      contains: "wholeTicketsToEntries"
    - path: "test/integration/CrossSurfaceTicketMixing.test.js"
      provides: "Reconciled emit==queue + structural assertions to entries semantics + full-stack openBox owed-delta behavioral proof"
      contains: "wholeTicketsToEntries(whole)"
  key_links:
    - from: "test/fuzz/PrizeLegEntriesDelivery.t.sol"
      to: "Storage.wholeTicketsToEntries + Jackpot._budgetToTicketUnits"
      via: "harness subclass exposing the internal helpers"
      pattern: "wholeTicketsToEntries"
    - from: "test/integration/CrossSurfaceTicketMixing.test.js [03a/b/c]"
      to: "the post-fix 2-emit Jackpot reality (uint32(units), wholeTicketsToEntries(whole))"
      via: "reconcile stale assertions to entries"
      pattern: "wholeTicketsToEntries\\(whole\\)"
---

<objective>
Prove the Phase 479 value fix behaviorally and reconcile the stale event-surface regression: (1) a deterministic regression that the prize-leg entries basis equals `(B<<2)/price` and matches the already-correct `_budgetToTicketUnits` basis (FIX-04), and (2) `emit == queue` on the touched Jackpot path plus reconciliation of the pre-existing failing `CrossSurfaceTicketMixing` assertions to the entries semantics (FIX-05) — confirming the Bernoulli EV testers stay green unchanged.

Purpose: lock the 1/4 -> full correction so it cannot silently regress, and clear the two assertions that have been stale-failing since `58556895` merged 3 JackpotTicketWin emit sites to 2.

Output: a new deterministic forge regression + an updated integration test, both green. Autonomous (test-only; no contract changes — commits ungated).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/479-conv-value-fix-gated-contract-diff-1-behavior-change/479-RESEARCH.md
@.planning/phases/479-conv-value-fix-gated-contract-diff-1-behavior-change/479-VALIDATION.md
@.planning/phases/479-conv-value-fix-gated-contract-diff-1-behavior-change/479-01-SUMMARY.md
@test/integration/CrossSurfaceTicketMixing.test.js
@test/fuzz/JackpotSingleCallCorrectness.t.sol

<interfaces>
<!-- Post-fix contract shape this plan asserts against (from 479-01). -->

Canonical converter (DegenerusGameStorage.sol, internal pure): wholeTicketsToEntries(uint32 wholeTickets) returns (uint32) => wholeTickets << 2.
Reference entries basis (DegenerusGameJackpotModule.sol :705, internal): _budgetToTicketUnits(budget, level) => (budget << 2) / priceForLevel(level)  (entries).

Post-fix call/emit sites:
  Jackpot :888  _queueTickets(winner, queueLvl, uint32(units), true)    + :890 emit JackpotTicketWin 4th arg = uint32(units)    [UNCHANGED already-correct leg]
  Jackpot :2143 _queueTickets(winner, targetLevel, wholeTicketsToEntries(whole), true) + :2151 emit 4th arg = wholeTicketsToEntries(whole)  [FIXED]
  Lootbox :1383 _queueTickets(player, rollLevel, wholeTicketsToEntries(whole), false)  [FIXED]; LootBoxOpened still emits scaledTickets (481 defers the unit normalization)

CrossSurfaceTicketMixing.test.js (existing) assertions impacted:
  [03a] expects exactly 3 emit JackpotTicketWin sites -> STALE (reality is 2 since 58556895). Reconcile to 2.
  [03b] expects fourthArgs deep.equal ["ticketCount","uint32(units)","whole"] -> STALE. Reconcile to ["uint32(units)","wholeTicketsToEntries(whole)"] (source order: :890 then :2147).
  [03c] asserts each emit 4th arg == adjacent preceding _queueTickets 3rd arg (reads ONLY the Jackpot file) -> STAYS GREEN post-fix (both pairs match). Keep as the emit==queue assertion.
  [03d]/[03e] event-definition byte-identity / ABI fragment -> unaffected (479 changes values, not the signature). Leave as-is.
  [CROSS-01d] embeds two exact source strings that BREAK on the helper routing:
     line ~720: lootboxSrc.includes("_queueTickets(player, rollLevel, whole, false)")  -> update to "...wholeTicketsToEntries(whole), false)"
     line ~735: rollBody.includes("_queueTickets(winner, targetLevel, whole, true)")    -> update to "...wholeTicketsToEntries(whole), true)"
  [CROSS-01a/b/c] live-state rem-byte reads + slot math -> unaffected (still route through _queueTickets which carries rem untouched). Reusable helpers: readTicketsOwedSlot, resolveLiveTicketsOwed, reachOpenableLootbox, findOpenableEthIndex, buyLootboxes.

Overflow bound to pin: whole <= scaledTickets/100 <= 42,949,672; wholeTicketsToEntries(42_949_672) = 171,798,688 < uint32 max 4,294,967,295.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deterministic forge regression — canonical converter + entries-basis equivalence + no-overflow (FIX-04)</name>
  <files>test/fuzz/PrizeLegEntriesDelivery.t.sol</files>
  <read_first>
    - test/fuzz/JackpotSingleCallCorrectness.t.sol (harness-subclass pattern: extend the production module under test/ to reach internal surfaces; uses PriceLookupLib.priceForLevel(...) >> 2 as the entry unit)
    - contracts/storage/DegenerusGameStorage.sol (wholeTicketsToEntries — internal pure, reachable by a subclass)
    - contracts/modules/DegenerusGameJackpotModule.sol (:705 _budgetToTicketUnits — internal, reachable by a subclass)
    - .planning/phases/479-conv-value-fix-gated-contract-diff-1-behavior-change/479-RESEARCH.md (section 6 test plan, section 7 validation architecture)
  </read_first>
  <action>
    Create `test/fuzz/PrizeLegEntriesDelivery.t.sol` — a forge test with a `PrizeLegHarness is DegenerusGameJackpotModule` subclass that exposes the two internal helpers via external shims (`exposedWholeTicketsToEntries(uint32)` -> wholeTicketsToEntries; `exposedBudgetToEntries(uint256 budget, uint24 level)` -> _budgetToTicketUnits). The subclass overrides NO production logic — it only adds external pass-throughs (mirror the JackpotSingleCallHarness precedent; test-only, no contracts/*.sol mutated).

    Assertions (deterministic, no VRF needed):
    1. Canonical converter: for representative whole counts {0, 1, 7, 1000, 42_949_672 (max realistic)}, `exposedWholeTicketsToEntries(w) == w * 4`, and the max-realistic case does NOT truncate (assert == 171_798_688, i.e. equals the uint256 product) — pins T-479-03 no-overflow.
    2. Entries-basis equivalence (uniform entries-per-ETH): for representative (budget B, level) pairs, the prize-leg basis equals the purchase/daily basis. Concretely: let `price = priceForLevel(level)`; the prize leg yields `wholeTicketsToEntries(B/price)` entries while the purchase/daily reference yields `_budgetToTicketUnits(B, level) = (B<<2)/price`. For B an exact multiple of `price`, assert they are EQUAL; for the general case assert the prize leg differs from the reference by at most one sub-ticket (4 entries) — the Bernoulli granularity — i.e. `(B<<2)/price - exposedWholeTicketsToEntries(B/price) < 4`. This pins FIX-04 "entries-per-ETH uniform across purchase/daily/prize" and "delivered == (B<<2)/price minus <=1 sub-ticket".
    3. A fuzz case over `uint32 whole` bounded to <= 42_949_672 asserting `exposedWholeTicketsToEntries(whole) == uint256(whole) << 2` (no truncation) and `% 4 == 0` (entries always a whole number of sub-tickets).

    This deterministically covers BOTH prize legs' value correctness: both route their post-Bernoulli `whole` through the SAME proven converter (verified structurally in Task 2), so proving the converter + the basis equivalence proves the delivered entries for both FIX-01 and FIX-02.
  </action>
  <verify>
    <automated>forge test --match-contract PrizeLegEntriesDelivery -vv</automated>
  </verify>
  <acceptance_criteria>
    - `test/fuzz/PrizeLegEntriesDelivery.t.sol` exists; the harness subclass overrides no production logic (only external shims).
    - Converter asserted == w*4 for the representative set incl. max-realistic 42_949_672 -> 171_798_688 (no uint32 truncation).
    - Prize-leg basis proven equal to `(B<<2)/price` for exact-multiple budgets and within one sub-ticket (4 entries) otherwise.
    - `forge test --match-contract PrizeLegEntriesDelivery` is GREEN; no contracts/*.sol modified.
  </acceptance_criteria>
  <done>A deterministic forge regression proves the canonical converter value, no-overflow at max-realistic whole, and uniform entries-per-ETH across prize vs purchase/daily legs.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile CrossSurfaceTicketMixing to entries + full-stack openBox owed-delta + emit==queue (FIX-05)</name>
  <files>test/integration/CrossSurfaceTicketMixing.test.js</files>
  <read_first>
    - test/integration/CrossSurfaceTicketMixing.test.js (FULL — [03a] :243, [03b] :272, [03c] :288, [03d]/[03e], [CROSS-01a..d] incl. the embedded source strings at ~:720 and ~:735; reusable helpers readTicketsOwedSlot/resolveLiveTicketsOwed/reachOpenableLootbox/findOpenableEthIndex)
    - .planning/phases/479-conv-value-fix-gated-contract-diff-1-behavior-change/479-RESEARCH.md (section 4 emit==queue + reconciliation, section 6 test plan)
    - .planning/phases/479-conv-value-fix-gated-contract-diff-1-behavior-change/479-01-SUMMARY.md (confirm the exact call-site/emit strings landed in 479-01)
  </read_first>
  <action>
    Reconcile the stale/affected assertions to the current 2-emit entries reality (these were already failing at baseline since `58556895`; the fix shifts the values they should now pin). Per repo rule, comments describe what the code IS — refresh the file header / block comments that still describe the Phase-278 "whole-ticket" emit basis to the entries basis (no history narration).

    - [03a] (~:243): change the expected emit-site count from 3 to 2; keep the "no 4th arg references TICKET_SCALE" check (still true — `uint32(units)` and `wholeTicketsToEntries(whole)` neither reference TICKET_SCALE).
    - [03b] (~:272): change the expected `fourthArgs` deep-equal from `["ticketCount","uint32(units)","whole"]` to `["uint32(units)","wholeTicketsToEntries(whole)"]` (source order: :890 then :2147). Update the explanatory comment to name the two surviving sites (correct entries leg + BAF roll).
    - [03c] (~:288): KEEP — it asserts emit 4th arg == adjacent _queueTickets 3rd arg and stays green post-fix (`uint32(units)`==`uint32(units)`; `wholeTicketsToEntries(whole)`==`wholeTicketsToEntries(whole)`). This is the contract-side emit==queue assertion; confirm it passes, do not weaken it.
    - [03d]/[03e]: leave unchanged (event signature/ABI unchanged in 479).
    - [CROSS-01d] (~:720, ~:735): update the two embedded source strings to the helper-routed form — `_queueTickets(player, rollLevel, wholeTicketsToEntries(whole), false)` and `_queueTickets(winner, targetLevel, wholeTicketsToEntries(whole), true)` — and refresh the adjacent comment to say the legs route the post-Bernoulli whole through the canonical converter. Keep the rest of the structural rem-byte proof intact.

    Add an emit==queue + 4x behavioral block for the lootbox leg (FIX-05 + behavioral FIX-02) reusing the existing fixture helpers:
    - Drive the REAL `openBox` full-stack (reuse reachOpenableLootbox + findOpenableEthIndex; keep the documented soft-skip precedent if the simulator denies lootbox-RNG reachability).
    - Capture the emitted `LootBoxOpened` (read `scaledTickets`, `rollLevel`) and the `ticketsOwedPacked` owed-entries delta at `rollLevel` (reuse readTicketsOwedSlot/resolveLiveTicketsOwed). Assert the delivered owed-entries delta equals the entries basis: `delta == ((scaledTickets / 100) << 2)` OR `delta == (((scaledTickets / 100) + 1) << 2)` (the Bernoulli round-up branch). This pins the lootbox leg at ~4x the pre-fix `scaledTickets/100` whole count. Document that `_jackpotTicketRoll` (private, VRF-rigging-gated) is covered deterministically by the Task-1 forge converter+basis proof, not driven full-stack here (carry-forward FIXTURE_COVERAGE_GAP).

    Do NOT edit any `test/stat/*BernoulliEv*` file. Do NOT edit any contracts/*.sol.
  </action>
  <verify>
    <automated>npx hardhat test test/integration/CrossSurfaceTicketMixing.test.js && npx hardhat test test/stat/JackpotTicketRollBernoulliEv.test.js test/stat/LootboxBernoulliEv.test.js test/stat/LootboxAutoResolveBernoulliEv.test.js</automated>
  </verify>
  <acceptance_criteria>
    - [03a] expects 2 emit sites; [03b] expects `["uint32(units)","wholeTicketsToEntries(whole)"]`; [03c] passes unchanged as the emit==queue assertion; [03d]/[03e] unchanged.
    - [CROSS-01d] embedded strings updated to the `wholeTicketsToEntries(whole)` helper form; rem-byte structural proof otherwise intact.
    - A full-stack `openBox` block asserts owed-entries delta == the entries basis (`(scaledTickets/100)<<2` or +1 round-up), with the documented soft-skip preserved.
    - `npx hardhat test test/integration/CrossSurfaceTicketMixing.test.js` is GREEN (was red at baseline on [03a]/[03b]); the three Bernoulli EV testers GREEN UNCHANGED; no contracts/*.sol or test/stat/* edited.
  </acceptance_criteria>
  <done>CrossSurfaceTicketMixing is reconciled to entries semantics and green, the emit==queue assertion holds, the lootbox 4x delivery is proven full-stack, and the Bernoulli EV testers remain green unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> production internal surfaces | a test-only subclass exposes internal helpers; must not override production logic (would invalidate the proof) |
| RNG (VRF word) -> Bernoulli EV invariant | the regression must not perturb the entropy path; EV testers are the tripwire |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-479-T1 | Spoofing (false-green test) | PrizeLegHarness subclass | mitigate | Subclass adds ONLY external pass-through shims; overrides no production fn (acceptance criterion). The converter+basis are asserted against the production internal symbols, not re-implemented in JS. |
| T-479-T2 | Tampering (EV-neutrality regression) | Bernoulli EV testers | mitigate | This plan edits no test/stat/* file and re-runs all three EV testers as a verify gate — they must stay green UNCHANGED. |
| T-479-03 | Tampering (integer truncation) | wholeTicketsToEntries at uint32 | mitigate | Task 1 pins max-realistic `whole` (42_949_672 -> 171_798_688) and a fuzz no-truncation case. |
| T-479-SC | Tampering (supply chain) | npm/forge package installs | accept | No package installs — only test-file edits against existing deps. Package Legitimacy Gate N/A. |
</threat_model>

<verification>
- `forge test --match-contract PrizeLegEntriesDelivery` green (FIX-04 deterministic proof).
- `npx hardhat test test/integration/CrossSurfaceTicketMixing.test.js` green — [03a]/[03b] reconciled (were red at baseline), [03c] emit==queue holds, [CROSS-01d] strings updated, openBox owed-delta proven (FIX-05 + behavioral FIX-02).
- The three `test/stat/*BernoulliEv*` testers green UNCHANGED (FIX-03 tripwire re-confirmed).
- Full-suite spot-check after the wave: `npm test && forge test` at or above the 893/0 floor (reconciling the 2 stale assertions should raise the green count).
- No contracts/*.sol modified by this plan (git-verify after the test-write tasks).
</verification>

<success_criteria>
- FIX-04: a regression proves owed-entries delivered == `(B<<2)/price` (minus <=1 sub-ticket) per prize leg and uniform entries-per-ETH across purchase/daily/prize.
- FIX-05: emit==queue holds on the touched Jackpot path; CrossSurfaceTicketMixing [03a]/[03b]/[03c]/[CROSS-01d] reconciled to entries and green.
- Bernoulli EV testers green UNCHANGED; no contract or stat-test edits.
</success_criteria>

<output>
Create `.planning/phases/479-conv-value-fix-gated-contract-diff-1-behavior-change/479-02-SUMMARY.md` when done. Record: the new forge regression's assertions + the max-realistic no-overflow pin, the exact CrossSurfaceTicketMixing reconciliations ([03a] 3->2, [03b] array, [03c] kept, [CROSS-01d] strings), the openBox owed-delta result (or soft-skip reason), and confirmation the Bernoulli EV testers stayed green unchanged with zero contract edits.
</output>
