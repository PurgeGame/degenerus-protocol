---
phase: 95-delta-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [DELTA-01, DELTA-02]

must_haves:
  truths:
    - "All 33 Hardhat failures are pre-existing -- zero regressions from chunk removal"
    - "Zero remaining references to 6 removed symbols in any Solidity file under contracts/"
  artifacts:
    - path: "N/A"
      provides: "Verification output confirming Hardhat test results and symbol sweep"
  key_links: []
---

<objective>
Verify Hardhat test suite has zero regressions and confirm all 6 removed symbols have no remaining references in contract source.

Purpose: DELTA-01 requires proving the chunk removal introduced no Hardhat regressions. DELTA-02 requires proving no stale symbol references remain. These are read-only verification tasks that produce evidence, not code changes.
Output: Test output confirming 1209 pass / 33 fail (all pre-existing), and grep sweep confirming zero hits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-delta-verification/95-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hardhat regression triage (DELTA-01)</name>
  <files>N/A -- no files modified, verification only</files>
  <read_first>
    - .planning/phases/95-delta-verification/95-RESEARCH.md (section: Pre-existing Hardhat Failures)
  </read_first>
  <action>
Run the full Hardhat test suite and verify zero regressions from the chunk removal:

1. Run `npx hardhat test` and capture the output.
2. Confirm the results are: **1209 passing, 33 failing**.
3. Verify the 33 failures match the pre-existing failure list from RESEARCH.md:
   - DegenerusStonk burn tests (10) -- `GameNotOver()` and event mismatch
   - Lootbox pool split tests (8) -- `HH17: BigInt normalization` error
   - Distress lootbox tests (2) -- unrecognized custom error `0x92bbf6e8`
   - AdvanceGame mint gate tests (7) -- transaction reverted without reason
   - Compressed jackpot timing tests (3) -- assertion failures
   - Lootbox split test (1) -- assertion failure
   - Degenerette cap test (1) -- assertion failure
   - Deity affiliate test (1) -- assertion failure
4. If any NEW failures appear that are NOT in the pre-existing list, flag them as regressions.
5. If results exactly match (1209/33, same categories), DELTA-01 is confirmed.

NOTE: Do NOT fix any failing tests. The 33 failures are pre-existing and not in scope for this phase.
  </action>
  <verify>
    <automated>npx hardhat test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Hardhat output contains "1209 passing" (or same passing count as documented)
    - Hardhat output contains "33 failing" (or same failing count as documented)
    - Zero new test names appear in the failure list that are not documented in RESEARCH.md
  </acceptance_criteria>
  <done>Hardhat suite confirms identical results before and after chunk removal: same passing count, same failing count, same failure categories. Zero regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Removed symbol sweep (DELTA-02)</name>
  <files>N/A -- no files modified, verification only</files>
  <read_first>
    - .planning/phases/95-delta-verification/95-RESEARCH.md (section: What Was Removed)
  </read_first>
  <action>
Run a comprehensive grep sweep for all 6 removed symbols across ALL Solidity files under contracts/:

1. Run: `grep -rn 'dailyEthBucketCursor\|dailyEthWinnerCursor\|_skipEntropyToBucket\|_winnerUnits\|DAILY_JACKPOT_UNITS_SAFE\|DAILY_JACKPOT_UNITS_AUTOREBUY' contracts/`
2. Expected result: **zero hits** (empty output, exit code 1).
3. If any hits are found, they are stale references that must be flagged.

Also run a secondary sweep for partial matches (in case of commented-out or string references):
4. Run: `grep -rn 'BucketCursor\|WinnerCursor\|skipEntropy\|winnerUnits\|UNITS_SAFE\|UNITS_AUTOREBUY' contracts/`
5. Expected result: **zero hits**.

Note: References in `test/` and `audit/` directories are expected and acceptable per RESEARCH.md. Only `contracts/` matters for DELTA-02.
  </action>
  <verify>
    <automated>grep -rn 'dailyEthBucketCursor\|dailyEthWinnerCursor\|_skipEntropyToBucket\|_winnerUnits\|DAILY_JACKPOT_UNITS_SAFE\|DAILY_JACKPOT_UNITS_AUTOREBUY' contracts/ ; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - grep command returns exit code 1 (no matches)
    - Output is empty (zero lines printed before the EXIT:1 marker)
    - Secondary partial-match grep also returns zero hits in contracts/
  </acceptance_criteria>
  <done>All 6 removed symbols (dailyEthBucketCursor, dailyEthWinnerCursor, _skipEntropyToBucket, _winnerUnits, DAILY_JACKPOT_UNITS_SAFE, DAILY_JACKPOT_UNITS_AUTOREBUY) have zero remaining references in any .sol file under contracts/.</done>
</task>

</tasks>

<verification>
- `npx hardhat test` shows identical pass/fail counts to pre-removal baseline
- `grep -r` for all 6 removed symbols across contracts/ returns zero hits
</verification>

<success_criteria>
- DELTA-01: Hardhat test output proves 1209 pass / 33 fail with all 33 matching pre-existing list
- DELTA-02: grep sweep proves zero remaining references to removed symbols in contracts/
</success_criteria>

<output>
After completion, create `.planning/phases/95-delta-verification/95-01-SUMMARY.md`
</output>
