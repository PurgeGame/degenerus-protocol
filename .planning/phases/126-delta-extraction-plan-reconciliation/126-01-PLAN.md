---
phase: 126-delta-extraction-plan-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/126-delta-extraction-plan-reconciliation/DELTA-INVENTORY.md
  - .planning/phases/126-delta-extraction-plan-reconciliation/FUNCTION-CATALOG.md
autonomous: true
requirements: [DELTA-01, DELTA-02, DELTA-03]

must_haves:
  truths:
    - "Every contract file changed since v5.0 appears in the diff inventory with exact insertion/deletion counts"
    - "Every changed/new/deleted function is cataloged with its change type and originating v6.0 phase"
    - "The DegenerusAffiliate unplanned change is explicitly flagged as unplanned with function-level detail"
    - "Any other commit not traceable to phases 120-125 is identified"
  artifacts:
    - path: ".planning/phases/126-delta-extraction-plan-reconciliation/DELTA-INVENTORY.md"
      provides: "File-level diff inventory + commit-to-phase tracing + unplanned change documentation"
      contains: "## File-Level Diff Inventory"
    - path: ".planning/phases/126-delta-extraction-plan-reconciliation/FUNCTION-CATALOG.md"
      provides: "Per-contract function checklist with change types and originating phases"
      contains: "## Per-Contract Function Catalog"
  key_links:
    - from: "FUNCTION-CATALOG.md"
      to: "Phase 127/128 audit scope"
      via: "Function checklist becomes Taskmaster coverage target"
      pattern: "\\| .* \\| (new|modified|deleted) \\| (120|121|122|123|124|unplanned)"
---

<objective>
Extract the complete delta between v5.0 and HEAD for all contract files, producing two deliverables: (1) a file-level diff inventory with line counts and commit-to-phase tracing, and (2) a per-contract function catalog listing every changed/new/deleted function with its change type and originating v6.0 phase.

Purpose: Define the precise audit scope for Phases 127 and 128. The function catalog becomes the Taskmaster coverage target.
Output: DELTA-INVENTORY.md and FUNCTION-CATALOG.md in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-delta-extraction-plan-reconciliation/126-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build File-Level Diff Inventory + Commit-Phase Trace</name>
  <files>
    .planning/phases/126-delta-extraction-plan-reconciliation/DELTA-INVENTORY.md
  </files>
  <read_first>
    .planning/phases/126-delta-extraction-plan-reconciliation/126-CONTEXT.md
  </read_first>
  <action>
Create DELTA-INVENTORY.md with three sections:

**Section 1: File-Level Diff Inventory**
Run `git diff --stat v5.0..HEAD -- contracts/` and `git diff --numstat v5.0..HEAD -- contracts/`.
Create a table with columns: File | Insertions | Deletions | Net | Category.
Category values: "new contract" (DegenerusCharity.sol), "mock" (contracts/mocks/*), "config" (ContractAddresses.sol), "production" (everything else).
Exclude mocks and ContractAddresses.sol from the audit scope section but include them in the full inventory for completeness.
The table MUST contain exactly 17 rows (per `git diff --stat` output) — one per changed file.

**Section 2: Commit-to-Phase Trace**
Run `git log --oneline --format='%h %ai %s' v5.0..HEAD -- contracts/`.
Create a table with columns: Commit | Date | Message | Phase | Planned.
Map each commit to its v6.0 phase using the commit message prefix:
- `fix(120-*)` or `feat(120-*)` -> Phase 120
- `fix(121-*)` or `feat(121-*)` -> Phase 121
- `fix(122-*)` -> Phase 122
- `feat(123-*)` or `test(123-*)` -> Phase 123
- `feat(124-*)` or `docs(124-*)` -> Phase 124
- `feat(affiliate):` -> "unplanned" (per D-04)
- Merge commits -> "merge" (not a phase)
Planned column: "yes" for phase-prefixed commits, "no" for the affiliate commit (per D-04, D-05).
There are exactly 13 commits to trace.

**Section 3: Unplanned Changes (per D-04, D-05)**
Document commit a3e2341f in detail:
- Full commit message (from `git show a3e2341f --format='%B' --no-patch`)
- Files touched (from `git show --stat a3e2341f`)
- Classification: "unplanned but intentional" (per D-04)
- Note: "Adds default referral codes so every address is an affiliate without on-chain registration. Requires Phase 128 adversarial review."
- Flag: NEEDS_ADVERSARIAL_REVIEW = yes

Also run `git log --oneline v5.0..HEAD -- contracts/ | grep -v -E '(120|121|122|123|124|125|Merge)'` to find any other commits not traceable to phases 120-125. If only a3e2341f matches, state "No other unplanned commits found."

**Section 4: Merge/Branch Anomalies**
Run `git log --oneline --graph v5.0..HEAD -- contracts/` to identify merge topology.
Document the worktree merge (commit 8b9a7e22 "Merge branch 'worktree-agent-a660a579'") — explain that Phase 124 was developed on a worktree branch and merged back.
Note the timeline: the affiliate commit (a3e2341f) is the LAST commit and sits AFTER the merge, meaning it was done after all planned phases completed.
Verdict: "No anomalous reverts, force-pushes, or out-of-order commits detected" (or document any found).
  </action>
  <verify>
    <automated>grep -c "^|" .planning/phases/126-delta-extraction-plan-reconciliation/DELTA-INVENTORY.md | xargs test 20 -le</automated>
  </verify>
  <acceptance_criteria>
    - DELTA-INVENTORY.md exists in the phase directory
    - File-Level Diff Inventory table has rows for all 17 files from `git diff --stat v5.0..HEAD -- contracts/`
    - Commit-to-Phase Trace table has rows for all 13 commits from `git log v5.0..HEAD -- contracts/`
    - Commit a3e2341f is explicitly flagged as "unplanned but intentional" with NEEDS_ADVERSARIAL_REVIEW = yes
    - Merge anomaly section documents commit 8b9a7e22 worktree merge
    - Each commit in the trace table has a Phase column value (120, 121, 122, 123, 124, "unplanned", or "merge")
  </acceptance_criteria>
  <done>Every contract file change since v5.0 is inventoried with line counts, every commit is traced to its originating phase, and unplanned/anomalous commits are flagged</done>
</task>

<task type="auto">
  <name>Task 2: Build Per-Contract Function Catalog</name>
  <files>
    .planning/phases/126-delta-extraction-plan-reconciliation/FUNCTION-CATALOG.md
  </files>
  <read_first>
    .planning/phases/126-delta-extraction-plan-reconciliation/126-CONTEXT.md
    .planning/phases/126-delta-extraction-plan-reconciliation/DELTA-INVENTORY.md
  </read_first>
  <action>
Create FUNCTION-CATALOG.md as the primary deliverable (per D-06). This is the Taskmaster coverage target for Phases 127 and 128.

For each of the 12 production contract files (excluding mocks and ContractAddresses.sol), create a section with a function-level table.

**Method per contract:**
1. Run `git diff v5.0..HEAD -- contracts/{file}` to get the full diff
2. Extract every function signature that appears in the diff (added, removed, or modified lines within function bodies)
3. Classify each function:
   - "new" = function signature appears only in the + side of the diff (entire function added)
   - "modified" = function signature exists in both v5.0 and HEAD but the body changed
   - "deleted" = function signature appears only in the - side of the diff (entire function removed)
   - "natspec-only" = only NatSpec/comment lines changed, no logic change

**Table columns per contract:**
| Function | Visibility | Change Type | Phase | Review Flag |

- Function: full signature (e.g., `_resolveCodeOwner(bytes32)`)
- Visibility: external/public/internal/private
- Change Type: new/modified/deleted/natspec-only
- Phase: originating v6.0 phase number (120-124) or "unplanned" for the affiliate commit
- Review Flag: NEEDS_ADVERSARIAL_REVIEW for all new/modified/deleted functions; blank for natspec-only (per D-02)

**Contract sections to include (in order):**

1. **DegenerusCharity.sol** (538 new lines — Phase 123) — ALL functions are "new", Phase 123. Note: "Entire contract is new — full adversarial review in Phase 127"
2. **DegenerusGameDegeneretteModule.sol** (296 changes — Phase 122) — Extract changed functions from the degenerette freeze fix
3. **DegenerusAffiliate.sol** (76 changes — unplanned) — Extract changed/new functions from default affiliate codes
4. **DegenerusGameGameOverModule.sol** (74 changes — Phase 124) — Extract functions modified for charity game-over hooks
5. **DegenerusStonk.sol** (54 additions — Phase 123/124) — Extract new/modified functions for charity integration
6. **DegenerusGameAdvanceModule.sol** (35 changes — Phase 121/124) — Extract functions from storage/gas fixes + charity hooks
7. **DegenerusGameJackpotModule.sol** (30 changes — Phase 121) — Extract functions from jackpot fixes
8. **DegenerusGameLootboxModule.sol** (29 changes — Phase 121/124) — Extract functions from lootbox fixes + charity hooks
9. **DegenerusGame.sol** (13 changes — Phase 124) — Extract functions from game router changes
10. **DegenerusGameEndgameModule.sol** (5 changes — Phase 121) — Extract changed function(s)
11. **DegenerusGameStorage.sol** (5 deletions — Phase 121) — Document deleted variable `lastLootboxRngWord`
12. **BitPackingLib.sol** (2 changes — Phase 121) — Document NatSpec change (natspec-only)

**Summary section at the end:**
Total table: | Category | Count |
- New functions (Phase 123 Charity)
- Modified functions (Phases 121-124)
- Deleted functions/variables
- NatSpec-only changes
- Unplanned changes (affiliate)
- Total requiring adversarial review (NEEDS_ADVERSARIAL_REVIEW count)
  </action>
  <verify>
    <automated>grep -c "NEEDS_ADVERSARIAL_REVIEW" .planning/phases/126-delta-extraction-plan-reconciliation/FUNCTION-CATALOG.md | xargs test 5 -le</automated>
  </verify>
  <acceptance_criteria>
    - FUNCTION-CATALOG.md exists in the phase directory
    - Contains sections for all 12 production contract files
    - Each section has a table with columns: Function | Visibility | Change Type | Phase | Review Flag
    - DegenerusCharity.sol section notes "Entire contract is new"
    - DegenerusAffiliate.sol functions have Phase = "unplanned"
    - BitPackingLib.sol entry has Change Type = "natspec-only" and Review Flag is blank
    - Summary section at the end with category counts
    - Every function with Change Type new/modified/deleted has Review Flag = NEEDS_ADVERSARIAL_REVIEW
  </acceptance_criteria>
  <done>Every changed/new/deleted function across all 12 production contracts is cataloged with change type, originating phase, and adversarial review flag</done>
</task>

</tasks>

<verification>
- DELTA-INVENTORY.md covers all 17 files from git diff --stat and all 13 commits from git log
- FUNCTION-CATALOG.md covers all 12 production contracts with function-level detail
- Commit a3e2341f is flagged as unplanned in both files
- No production contract file is missing from the catalog
- Summary counts are internally consistent
</verification>

<success_criteria>
- File-level diff inventory exists with exact line counts for all 17 changed files
- All 13 commits are traced to their originating v6.0 phase (or flagged as unplanned)
- Per-contract function catalog covers all 12 production contracts
- Every changed function has a change type, originating phase, and review flag
- Unplanned DegenerusAffiliate change is documented per D-04
- Merge anomaly (worktree branch) is documented per PLAN-03
</success_criteria>

<output>
After completion, create `.planning/phases/126-delta-extraction-plan-reconciliation/126-01-SUMMARY.md`
</output>
