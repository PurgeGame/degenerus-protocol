---
phase: 193-gas-ceiling-test-regression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/193-gas-ceiling-test-regression/193-01-AUDIT.md
autonomous: true
requirements: [GAS-01, DELTA-03]

must_haves:
  truths:
    - "advanceGame worst-case gas with new jackpot paths is measured and documented"
    - "Safety margin against 30M block gas limit is >= 1.5x (worst-case <= 20M gas)"
    - "Foundry test suite shows zero new failures beyond 28 pre-existing"
    - "Hardhat test suite shows zero new failures beyond 19 pre-existing / 3 pending"
  artifacts:
    - path: ".planning/phases/193-gas-ceiling-test-regression/193-01-AUDIT.md"
      provides: "Gas ceiling analysis and test regression results"
      contains: "GAS-01"
  key_links:
    - from: "contracts/modules/DegenerusGameAdvanceModule.sol"
      to: "contracts/modules/DegenerusGameJackpotModule.sol"
      via: "delegatecall through runBafJackpot"
      pattern: "runBafJackpot"
---

<objective>
Measure advanceGame worst-case gas consumption with all new jackpot code paths (specialized events, whale pass daily path, DGNRS fold from commits 93c05869/520249a2), verify >= 1.5x safety margin against 30M block gas limit, and confirm zero new test regressions in both Foundry and Hardhat suites.

Purpose: Prove the post-v22.0 jackpot changes do not push advanceGame near the block gas limit and that the existing test suites remain green at their known baselines.
Output: 193-01-AUDIT.md with gas ceiling results and test regression verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/193-gas-ceiling-test-regression/193-CONTEXT.md
@.planning/phases/192-delta-extraction-behavioral-verification/192-01-SUMMARY.md
@.planning/phases/192-delta-extraction-behavioral-verification/192-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gas Ceiling Analysis + Test Regression Verification</name>
  <files>.planning/phases/193-gas-ceiling-test-regression/193-01-AUDIT.md</files>

  <read_first>
    - contracts/modules/DegenerusGameAdvanceModule.sol (advanceGame entry point, _consolidatePoolsAndRewardJackpots, stage constants)
    - contracts/modules/DegenerusGameJackpotModule.sol (runBafJackpot, payDailyJackpot, new specialized events, whale pass daily path, DGNRS fold)
    - test/gas/AdvanceGameGas.test.js (existing gas benchmark harness -- uses driveOneCycle + recordGas pattern)
    - .planning/phases/192-delta-extraction-behavioral-verification/192-01-AUDIT.md (function-level changelog to identify which code paths changed)
    - .planning/phases/193-gas-ceiling-test-regression/193-CONTEXT.md (decisions D-01 through D-06)
  </read_first>

  <action>
Run three test suites and produce a single audit document:

**Part A -- Gas Ceiling (GAS-01, per D-01/D-02/D-03):**

1. Run the Hardhat gas benchmark suite:
   ```
   npx hardhat test test/gas/AdvanceGameGas.test.js
   ```
   This test file drives advanceGame through every stage and prints a gas summary table at the end.
   Capture the full "ADVANCEGAME GAS BENCHMARK SUMMARY" output.

2. Identify the peak gas value from the summary table.

3. Calculate safety margin: `30_000_000 / peak_gas`. Must be >= 1.5x per D-02.

4. If the existing gas benchmark test does not cover the specific new jackpot paths (specialized events for daily normal/solo/x10/x100/BAF, whale pass daily-only path, DGNRS fold into daily ETH), note which new paths ARE exercised by the existing tests vs which paths are only reachable in specific game states. The key question is whether advanceGame's TOTAL worst-case has increased -- the existing benchmark already exercises the worst-case full-cycle path.

5. Also run `forge test --gas-report --match-contract AdvanceGame 2>&1` to get Foundry-side gas reporting on the advanceGame rewrite test if applicable. If no Foundry test specifically measures advanceGame gas, note this and rely on the Hardhat benchmark.

**Part B -- Foundry Test Regression (DELTA-03, per D-04):**

6. Run `forge test 2>&1` and capture the full output.
7. Count passing and failing tests.
8. Expected baseline: 150 passing / 28 pre-existing failures.
9. If counts differ from baseline, investigate each new failure:
   - Identify which test function failed
   - Determine if the failure is attributable to the v22.0->v23.0 changes or is a pre-existing issue
   - Document the attribution

**Part C -- Hardhat Test Regression (DELTA-03, per D-05):**

10. Run `npx hardhat test 2>&1` and capture the full output (this runs all test directories in order: access, deploy, unit, integration, edge, validation, gas).
11. Count passing, failing, and pending tests.
12. Expected baseline: 1225 passing / 19 failing / 3 pending.
13. If counts differ from baseline, investigate each new failure with the same attribution methodology as Foundry.

**Part D -- Write 193-01-AUDIT.md:**

Structure the audit document as:
```markdown
# Phase 193: Gas Ceiling & Test Regression Audit

## Gas Ceiling Analysis (GAS-01)

### advanceGame Gas Benchmark Results

[Paste the full ADVANCEGAME GAS BENCHMARK SUMMARY table from Hardhat]

### Peak Gas & Safety Margin

- **Peak gas:** [value] gas
- **Safety margin:** [30M / peak] = [X.Xx] (threshold: >= 1.5x)
- **Prior baseline (v15.0):** 7,023,530 gas / 1.99x margin
- **Delta from prior:** [+/- N gas, +/- X%]
- **Verdict:** SAFE / UNSAFE

### New Jackpot Path Coverage

[Which new code paths (specialized events, whale pass daily, DGNRS fold) are exercised by the benchmark, which are not, and why the unmeasured paths do not increase worst-case gas]

## Test Regression (DELTA-03)

### Foundry Results

- **Command:** `forge test`
- **Passing:** [N] (baseline: 150)
- **Failing:** [N] (baseline: 28 pre-existing)
- **New failures:** [0 or list]
- **Verdict:** PASS / FAIL

### Hardhat Results

- **Command:** `npx hardhat test`
- **Passing:** [N] (baseline: 1225)
- **Failing:** [N] (baseline: 19)
- **Pending:** [N] (baseline: 3)
- **New failures:** [0 or list]
- **Verdict:** PASS / FAIL

## Overall Verdict

- GAS-01: [PASS/FAIL]
- DELTA-03: [PASS/FAIL]
```
  </action>

  <verify>
    <automated>test -f .planning/phases/193-gas-ceiling-test-regression/193-01-AUDIT.md && grep -q "GAS-01" .planning/phases/193-gas-ceiling-test-regression/193-01-AUDIT.md && grep -q "DELTA-03" .planning/phases/193-gas-ceiling-test-regression/193-01-AUDIT.md && grep -q "Safety margin" .planning/phases/193-gas-ceiling-test-regression/193-01-AUDIT.md && grep -q "Foundry Results" .planning/phases/193-gas-ceiling-test-regression/193-01-AUDIT.md && grep -q "Hardhat Results" .planning/phases/193-gas-ceiling-test-regression/193-01-AUDIT.md && grep -q "Overall Verdict" .planning/phases/193-gas-ceiling-test-regression/193-01-AUDIT.md && echo "PASS" || echo "FAIL"</automated>
  </verify>

  <acceptance_criteria>
    - 193-01-AUDIT.md exists with all sections populated from actual test output (not placeholders)
    - "Gas Ceiling Analysis (GAS-01)" section contains the full ADVANCEGAME GAS BENCHMARK SUMMARY table
    - "Peak Gas & Safety Margin" section contains a numeric peak gas value, a calculated safety margin (30M/peak), and a SAFE/UNSAFE verdict
    - "Safety margin" line shows a value >= 1.5 (i.e., peak <= 20M gas) OR documents why it exceeds the threshold
    - "Prior baseline" line references 7,023,530 gas / 1.99x margin (from v15.0)
    - "Foundry Results" section contains actual pass/fail counts from `forge test` output
    - "Hardhat Results" section contains actual pass/fail/pending counts from `npx hardhat test` output
    - "New failures" line in each section is either "0" or lists specific test names with attribution
    - "Overall Verdict" section contains explicit PASS or FAIL for both GAS-01 and DELTA-03
  </acceptance_criteria>

  <done>
    Gas ceiling measured with safety margin documented. Both test suites run with results compared against baselines. 193-01-AUDIT.md committed with GAS-01 and DELTA-03 verdicts.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries relevant -- this is a read-only audit phase analyzing gas consumption and running existing test suites. No code changes, no external service integration, no user input handling.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193-01 | D (Denial of Service) | advanceGame | mitigate | Gas ceiling analysis IS the mitigation -- verify worst-case stays below 20M gas (1.5x margin against 30M block limit) |
</threat_model>

<verification>
1. 193-01-AUDIT.md exists with complete gas benchmark data and test results
2. advanceGame peak gas has >= 1.5x safety margin against 30M
3. Foundry: zero new failures beyond 28 pre-existing
4. Hardhat: zero new failures beyond 19 failing / 3 pending
</verification>

<success_criteria>
- GAS-01 verdict is documented (PASS if margin >= 1.5x, FAIL otherwise)
- DELTA-03 verdict is documented (PASS if zero new failures in both suites)
- All gas values are from actual test execution, not estimates
- All test counts are from actual test execution, not assumed baselines
</success_criteria>

<output>
After completion, create `.planning/phases/193-gas-ceiling-test-regression/193-01-SUMMARY.md`
</output>
