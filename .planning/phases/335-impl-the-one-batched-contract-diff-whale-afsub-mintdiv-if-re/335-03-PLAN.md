---
phase: 335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/modules/DegenerusGameMintModule.sol
autonomous: true
worktree: false
requirements: [MINTDIV-02]
tags: [contracts, mintmodule, mintdiv, one-liner, index-alignment]

must_haves:
  truths:
    - "The within-player `processed` advance in `processTicketBatch` at `:716` is `processed += take;` (matching the reference advance at `processFutureTicketBatch:502`)."
    - "The pre-edit `processed += writesUsed >> 1;` shape is GONE from the file."
    - "The two loops `processTicketBatch` and `processFutureTicketBatch` STAY separate (D-15 full-dedup REJECTED) — only the one-liner is changed."
    - "The change is independent of WHALE and AFSUB (no ordering dependency)."
  artifacts:
    - path: "contracts/modules/DegenerusGameMintModule.sol"
      provides: "MINTDIV-02 alignment — per-ticket trait indices are identical whether or not a player's owed splits across a `WRITES_BUDGET_SAFE` slice."
  key_links:
    - from: "contracts/modules/DegenerusGameMintModule.sol::processTicketBatch (within-player advance at :716)"
      to: "contracts/modules/DegenerusGameMintModule.sol::processFutureTicketBatch (reference advance at :502)"
      via: "literal copy of the `processed += take;` shape — same file, ~200 lines above"
      pattern: "processed \\+= take;"
---

<objective>
Execute Step 4 of the SPEC-locked edit-order map: the MINTDIV-02 one-liner at `contracts/modules/DegenerusGameMintModule.sol:716`.

Change `processed += writesUsed >> 1;` → `processed += take;` to match the reference-correct contiguous advance at `processFutureTicketBatch:502`. This makes `processTicketBatch` advance its within-player `startIndex` exactly like `processFutureTicketBatch`, so a player's owed tickets generate contiguous per-ticket LCG trait indices across a `WRITES_BUDGET_SAFE = 550` budget-slice split.

This delivers:
- **MINTDIV-02** — alignment-if-real. MINTDIV-01 is PROVEN REACHABLE per `334-MINTDIV01-REACHABILITY-VERDICT.md` (D-22). The D-16 NEGATIVE branch ("no change, documented NEGATIVE") does NOT apply — the one-liner SHIPS.

Purpose: this is the smallest contract change in the diff. Independent of WHALE and AFSUB; placed in Wave 1 with no `depends_on` because the edit commutes with the others within the single batched diff (334-IMPL-EDIT-ORDER-MAP §2).

Output: a one-line diff under `contracts/modules/DegenerusGameMintModule.sol`. The full-suite regression — including the budget-slice-split per-ticket same-traits regression — lives at Plan 335-05 (test migration) + 336/TST-03.

NOTE on scope discipline: D-15 explicitly REJECTS full dedup of the two near-duplicate loops. The fix is exactly this one line — no defensive change anywhere else (PATTERNS §2.4 last bullet).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-CONTEXT.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-PATTERNS.md
@.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-MINTDIV01-REACHABILITY-VERDICT.md
@.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-WHALE-MINTDIV.md
@.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-IMPL-EDIT-ORDER-MAP.md

<interfaces>
<!-- The LITERAL COPY target lives 200 lines above the suspect line in the same file. PATTERNS §2.4. -->

From contracts/modules/DegenerusGameMintModule.sol:497-512 (the reference-CORRECT advance — analog A3):
```
uint40 newPacked = (uint40(remainingOwed) << 8) | uint40(rem);
if (newPacked != packed) {
    owedMap[player] = newPacked;
}
unchecked {
    processed += take;          // :502 ← THE LITERAL COPY TARGET
    used += writesThis;
}
if (remainingOwed == 0) {
    unchecked { ++idx; }
    processed = 0;
}
```

From contracts/modules/DegenerusGameMintModule.sol:710-718 (the suspect advance — pre-edit):
```
unchecked {
    used += writesUsed;
    if (advance) {
        ++idx;
        processed = 0;
    } else {
        processed += writesUsed >> 1;   // :716 ← FIX TO `processed += take;`
    }
}
```

From contracts/modules/DegenerusGameMintModule.sol:93 (the budget binding the −17-warm trace):
- `uint256 internal constant WRITES_BUDGET_SAFE = 550;` (UNTOUCHED — this is the slice ceiling whose split the alignment reads back across).

**`take` scope verification (PATTERNS §2.4 last bullet):** the variable `take` MUST be in-scope at `:716` and represent the same per-iter ticket count as `_processOneTicketEntry`'s emit count. Read `:671-718` end-to-end before applying the edit to confirm. If `take` is wrapped/aliased, copy the assignment shape from `:502` verbatim using the equivalent local name.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: One-liner index-alignment fix at MintModule.sol:716</name>
  <files>contracts/modules/DegenerusGameMintModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameMintModule.sol (lines 393–520 — `processFutureTicketBatch` full body including the reference advance at `:502`)
    - contracts/modules/DegenerusGameMintModule.sol (lines 671–720 — `processTicketBatch` end-to-end to confirm `take` is the in-scope per-iter ticket count and that the surrounding `unchecked { used += writesUsed; if (advance) { ... } else { ... } }` block structure is intact)
    - .planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-PATTERNS.md (§2.4 Step 4 + §0 the convergence posture)
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-MINTDIV01-REACHABILITY-VERDICT.md (PROVEN REACHABLE — owed=300 scenario + −17-warm trace + 2 callers)
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-WHALE-MINTDIV.md (§7 — the one-liner shape)
  </read_first>
  <action>
    Change ONE line in `contracts/modules/DegenerusGameMintModule.sol`:

    AT `:716`:
    - PRE-EDIT: `processed += writesUsed >> 1;`
    - POST-EDIT: `processed += take;`

    Constraints:
    - DO NOT modify any other line in `processTicketBatch` (the `if (advance) { ++idx; processed = 0; } else { ... }` shell stays unchanged).
    - DO NOT modify `processFutureTicketBatch` (the reference advance at `:502` is untouched).
    - DO NOT modify `WRITES_BUDGET_SAFE:93` (the budget constant is the slice ceiling; `:716`'s `>>1` was masking the bug, not enforcing the budget).
    - DO NOT merge / dedup the two loops (D-15 — full dedup REJECTED).
    - DO NOT add comments justifying the change in-line (the docstring at the function header may be updated to record "post-MINTDIV-02 alignment with processFutureTicketBatch:502", but this is optional and not required by the SPEC).

    Pre-edit verification step (REQUIRED before writing):
    - Read `contracts/modules/DegenerusGameMintModule.sol:671-718` end-to-end.
    - Confirm `take` is a local variable in-scope at `:716` (declared earlier in the function, typically near the top of the per-player loop body — the same `take` variable that bounds the writes-this-slice).
    - If `take` is wrapped/aliased or has a different local name in this function's scope (e.g. `takeThis`, `thisTake`), use that name verbatim. The PATTERN is `processed += <the local representing this slice's ticket count>`, mirroring the `:502` shape; the name `take` is the standard one used at `:502` and is what the SUMMARY assumes unless the read step says otherwise.
  </action>
  <verify>
    <automated>grep -nE "^\s*processed \+= take;" contracts/modules/DegenerusGameMintModule.sol | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^\s*processed \+= take;" contracts/modules/DegenerusGameMintModule.sol` returns ≥ 2 lines (the reference at `:502` AND the new one at `:716`-ish).
    - `grep -nE "processed \+= writesUsed >> 1" contracts/modules/DegenerusGameMintModule.sol` returns 0 lines (the pre-edit shape is gone).
    - `grep -nE "uint256 internal constant WRITES_BUDGET_SAFE = 550;" contracts/modules/DegenerusGameMintModule.sol` returns exactly 1 line (constant unchanged).
    - `grep -cE "function processFutureTicketBatch" contracts/modules/DegenerusGameMintModule.sol` returns 1 (the reference function is untouched).
    - `grep -cE "function processTicketBatch" contracts/modules/DegenerusGameMintModule.sol` returns 1 (the suspect function still exists; only the one line within it changed).
    - `git diff contracts/modules/DegenerusGameMintModule.sol` shows exactly 1 changed line (or 2 if the docstring is also updated — both shapes accepted).
  </acceptance_criteria>
  <done>The MINTDIV-02 one-liner is applied at `:716`; the suspect `>>1` shape is gone; the reference function and budget constant are untouched; the two loops remain separate (no dedup).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keeper/caller → `processTicketBatch` (any path that reaches it) | Pre-existing. The function processes per-player ticket batches under a global writes budget. The fix corrects within-player `startIndex` advance arithmetic; no new untrusted entry point introduced. |
| `processTicketBatch` → `_raritySymbolBatch` (LCG consumer) | The downstream LCG consumes the per-ticket index. Pre-fix: a budget-slice split mis-advanced the index, causing the LCG to repeat indices and produce divergent per-ticket trait indices for a player whose owed crosses `WRITES_BUDGET_SAFE`. Post-fix: contiguous indices, same per-ticket trait derivation as the non-split case. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-335-12 | Tampering (RNG-derived output divergence) | `processTicketBatch` per-ticket LCG index advance | mitigate (the fix IS the mitigation) | The pre-edit `>>1` advance was the divergence source per 334-MINTDIV01-REACHABILITY-VERDICT.md (owed=300 scenario, 2 callers, −17 warm trace). The fix aligns the advance with the reference at `:502`, so per-ticket trait indices are identical across a slice split. The same-traits-across-split regression test lives at 336/TST-03 — empirical proof of the alignment. |
| T-335-13 | Tampering (RNG-freeze invariant regression) | `processTicketBatch`'s `_raritySymbolBatch` consumes a frozen-word entry | accept (the freeze invariant holds pre- AND post-fix) | The frozen word (the VRF-derived seed) is consumed at the SAME `startIndex` arithmetic post-fix — pre-fix had the WRONG arithmetic; post-fix matches `processFutureTicketBatch:502`. No new write into a frozen slot is introduced (the fix only changes a LOCAL accumulator's advance step). 334-WHALE04-FREEZE-PROOF §4 confirms the freeze invariant for `processTicketBatch` is read-side, not write-side; the fix is on the read-side advance. |
| T-335-14 | Repudiation | claim "MINTDIV-01 was never reachable; the fix introduces a regression" | mitigate (proven reachable + reference-alignment) | 334-MINTDIV01-REACHABILITY-VERDICT.md PROVEN REACHABLE with a concrete owed=300 scenario, 2 caller paths, and the −17-warm trace. The fix is a literal copy of the reference shape `:502`; if the reference is correct (and 332 TST evidence + the SC1 integration in 334-IMPL-EDIT-ORDER-MAP §2 establish it is), the fix cannot introduce a regression — it aligns the buggy path with the correct one. |
| T-335-15 | Tampering | merging the two loops as a "while we're here" optimization | mitigate (avoid) | **DO NOT MERGE / DEDUP** per D-15. The two loops stay separate. Any structural change beyond the one-liner deviates from the locked decision. |

## Invariants re-attested

- **v45 VRF-freeze invariant** — read-side only on this function; the fix changes a local-advance accumulator (no write to a frozen slot). Per 334-WHALE04-FREEZE-PROOF §4: the `_raritySymbolBatch` LCG consumes a frozen word at a player's level entry; pre- and post-fix the consumed word is the same. The fix corrects WHICH indices are derived from it, not WHETHER it is frozen.
- **OPEN-E operator-approval trust boundary** — N/A (AfKing-side).
- **AFKING cancel-tombstone + swap-pop invariant** — N/A (AfKing-side).
- **D-IMPL-01 gameOver-forfeit structural guard** — N/A (whale-side).
- **`_applyWhalePassStats` 3-caller invariant** — N/A (whale-side).
</threat_model>

<verification>
Plan-level checks (executor MUST run after the task):

1. `grep -nE "^\s*processed \+= take;" contracts/modules/DegenerusGameMintModule.sol` returns ≥ 2 lines (the `:502` reference + the new `:716`-area line).
2. `grep -nE "processed \+= writesUsed >> 1" contracts/modules/DegenerusGameMintModule.sol` returns 0 lines (pre-edit shape gone).
3. `git diff contracts/modules/DegenerusGameMintModule.sol | grep -cE "^[+-]"` returns ≤ 4 (one-line edit at minimum; docstring update is optional and adds at most 2-3 more lines).
4. `grep -nE "WRITES_BUDGET_SAFE = 550" contracts/modules/DegenerusGameMintModule.sol` returns 1 line (constant unchanged).
5. `grep -cE "function process(Ticket|FutureTicket)Batch" contracts/modules/DegenerusGameMintModule.sol` returns 2 (both functions exist; no dedup).
6. No other file in `contracts/modules/` is touched — `git diff --name-only contracts/modules/` should list only `DegenerusGameMintModule.sol` from this plan (unless concurrent with WHALE plans 335-01/02 within the batched diff — in that case, those files appear from their own plans, but no NEW file appears from THIS plan).
</verification>

<success_criteria>
- The one-line MINTDIV-02 fix is applied at `:716`.
- The reference advance at `:502` and the budget constant `WRITES_BUDGET_SAFE:93` are untouched.
- The two loops remain separate (D-15).
- No other modification to `MintModule.sol` outside the one-liner (and optional docstring).
- 334-MINTDIV01-REACHABILITY-VERDICT.md PROVEN REACHABLE is re-cited in the SUMMARY as the load-bearing justification.
</success_criteria>

<output>
Create `.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-03-SUMMARY.md` when done. The SUMMARY MUST record:
- The exact pre-edit and post-edit line text at `:716` (or wherever the bracketing function placed it post-edit).
- The grep output confirming `processed += take;` appears at ≥ 2 lines (the reference + the new one).
- A re-citation of 334-MINTDIV01-REACHABILITY-VERDICT.md (PROVEN REACHABLE — owed=300 scenario, 2 callers, −17-warm trace) as the load-bearing justification.
- A statement that the docstring at the function header was/was-not updated (and the exact text added if so).
- A grep-attestation block confirming all 6 plan-level verification gates pass.
- A short note that 336/TST-03 owns the same-traits-across-split empirical regression.
</output>
