---
phase: 335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/storage/DegenerusGameStorage.sol
  - contracts/DegenerusGame.sol
autonomous: true
worktree: false
requirements: [WHALE-03, AFSUB-01]
tags: [contracts, whale, autoopen, view, facade, storage-confirm]

must_haves:
  truths:
    - "The `whalePassClaims` mapping declaration in DegenerusGameStorage.sol is confirmed in place; no edit needed."
    - "DegenerusGame.sol exposes a new external view `lazyPassHorizon(address) → uint24` next to `hasAnyLazyPass:1520`, returning `type(uint24).max` for deity holders and `frozenUntilLevel` for lazy/whale holders, otherwise 0."
    - "The `OPEN_NORMAL_GAS_UNIT = 90_000` constant (`:1561`), the `gasleft()` weighting at `:1687`, and the `weighted += used / OPEN_NORMAL_GAS_UNIT` math at `:1728` are all removed from `DegenerusGame.sol`; the `autoOpen` loop guard becomes a flat opened-count check."
    - "The stale `~5.4M whale-pass box` comment at `:1558`/`:1681-1682` is updated or deleted to reflect uniform O(1) opens."
  artifacts:
    - path: "contracts/storage/DegenerusGameStorage.sol"
      provides: "Confirmed `whalePassClaims` slot for Plan 335-02 to write into (no edit)."
    - path: "contracts/DegenerusGame.sol"
      provides: "`lazyPassHorizon(address)` view (producer for Plan 335-04); flat autoOpen loop guard (retired weighting)."
  key_links:
    - from: "contracts/DegenerusGame.sol::lazyPassHorizon"
      to: "contracts/AfKing.sol IGame interface decl :18-43"
      via: "external view exposed via IGame for AfKing consumption (Plan 335-04)"
      pattern: "function lazyPassHorizon\\(address\\)"
    - from: "contracts/DegenerusGame.sol::autoOpen"
      to: "contracts/modules/DegenerusGameLootboxModule.sol::_autoOpenBox"
      via: "the retired gas-weight loop now calls `_autoOpenBox` under a flat opened-count guard"
      pattern: "while \\(cursor < qlen && (i|opened) < maxCount\\)"
---

<objective>
Execute Step 1 + Step 2 of the SPEC-locked 5-step producer-before-consumer edit-order map (`.planning/phases/334-.../334-IMPL-EDIT-ORDER-MAP.md`):

1. **Storage confirm-attestation (Step 1, no edit)** — confirm the `whalePassClaims` mapping declaration in `contracts/storage/DegenerusGameStorage.sol:955` is reachable from the LootboxModule write site that Plan 335-02 will author; confirm `_applyWhalePassStats:1111` and `_queueTicketRange:647` are untouched.

2. **Game facade edits (Step 2)**:
   - **Add `lazyPassHorizon` view (D-11)** — the producer view that Plan 335-04's AfKing subscribe + crossing consumes (`AFSUB-01`).
   - **Retire the WHALE-03 autoOpen carve-out** — delete `OPEN_NORMAL_GAS_UNIT = 90_000` at `:1561`, the `gasleft()` measurement at `:1687`, and the `weighted += used / OPEN_NORMAL_GAS_UNIT` math at `:1728`; replace the `weighted < maxCount` loop guard with a flat opened-count check (`WHALE-03` retirement piece — re-confirmation lives in Plan 335-06).

Purpose: this plan establishes the producer surfaces (storage slot existence + facade view + flat autoOpen) that downstream plans (335-02 LootboxModule writer, 335-04 AfKing consumer) depend on. Producer-before-consumer per D-18.

Output: a partial diff under `contracts/storage/DegenerusGameStorage.sol` (NO edit, just confirmed) and `contracts/DegenerusGame.sol` (view added + autoOpen weighting deleted) that compiles in isolation but is FUNCTIONLESS until Plans 335-02 / 335-04 land their consumers in the same batched diff.

NOTE: `claimWhalePass` entrypoint home (D-01 Claude's Discretion) — the existing `WhaleModule:1018` already exists. This plan adds NO facade-routing fn for it (we expose the module-direct path); this is the simpler of the two coherent choices recorded in Claude's Discretion of the SPEC. If a downstream verification reveals callers expect `DegenerusGame.claimWhalePass`, a thin delegatecall facade can be added in a follow-up — the SPEC explicitly accepts both shapes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-CONTEXT.md
@.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-PATTERNS.md
@.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-IMPL-EDIT-ORDER-MAP.md
@.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-AFKING.md
@.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-WHALE-MINTDIV.md
@.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-GREP-ATTESTATION.md

<interfaces>
<!-- Extracted from the audit baseline `b0511ca2` — analog A1 (PATTERNS §2.2). The new view MIRRORS hasAnyLazyPass:1520's packed-read shape. -->

From contracts/DegenerusGame.sol:1520 (the existing analog to mirror):
- `function hasAnyLazyPass(address player) external view returns (bool)` — reads `mintPacked_[player]`, checks `HAS_DEITY_PASS_SHIFT` bit, then compares unpacked `frozenUntilLevel` against `level`.

From contracts/DegenerusGame.sol:1561 (DELETE):
- `uint256 private constant OPEN_NORMAL_GAS_UNIT = 90_000;`

From contracts/DegenerusGame.sol:1687-1734 (the autoOpen body — DELETE the weighting, KEEP the cursor/skip preamble):
- `function autoOpen(uint256 maxCount) external returns (uint256 opened)` — pre-edit uses `uint256 weighted; while (cursor < qlen && weighted < maxCount) { uint256 g0 = gasleft(); _autoOpenBox(index, player); uint256 used = g0 - gasleft(); weighted += used / OPEN_NORMAL_GAS_UNIT; if (used % OPEN_NORMAL_GAS_UNIT != 0 || used == 0) ++weighted; }`. POST-edit: replace `weighted` with a plain opened-count i; loop guard becomes `i < maxCount`.

From contracts/storage/DegenerusGameStorage.sol:955 (CONFIRM-ONLY — no edit):
- `mapping(address => uint256) internal whalePassClaims;` — the slot Plan 335-02 will `+=` into.

From contracts/storage/DegenerusGameStorage.sol:1213 (CONFIRM-ONLY — load-bearing for D-IMPL-01):
- `function _livenessTriggered() internal view returns (bool)` — the structural guard for D-23 gameOver-forfeit (transitivity trace in 335-CONTEXT.md D-IMPL-01).

From contracts/modules/DegenerusGameWhaleModule.sol:1018 (CONFIRM-ONLY — the existing claim endpoint):
- `function claimWhalePass(address player) external` — already permissionless-w/-beneficiary, `_livenessTriggered`-gated, `level+1`-anchored, calls `_applyWhalePassStats` then `_queueTicketRange(player, startLevel, 100, halfPasses, false)`. UNTOUCHED.

From contracts/AfKing.sol:18-43 (CONFIRM target — the IGame iface AfKing reads):
- The `IGame` interface declaration; `lazyPassHorizon(address) external view returns (uint24)` MUST be added here too (in Plan 335-04's AfKing edits OR in this plan if the convention is interface-with-impl) — see Decision in Plan 335-04.

For this plan, ONLY add the impl on DegenerusGame.sol. The iface decl is owned by Plan 335-04 (it lives in AfKing.sol where the iface is declared).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Storage confirm-attestation — DegenerusGameStorage.sol whalePassClaims slot</name>
  <files>contracts/storage/DegenerusGameStorage.sol</files>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol (lines 945–965 for whalePassClaims; 1100–1140 for _applyWhalePassStats; 640–680 for _queueTicketRange; 1205–1230 for _livenessTriggered)
    - .planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-PATTERNS.md (§2.1 Step 1 — confirm-only)
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-IMPL-EDIT-ORDER-MAP.md (§1 Step 1)
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-GREP-ATTESTATION.md (§4 drift corrections — `_livenessTriggered` at :1213 not :571)
  </read_first>
  <action>
    NO edit to DegenerusGameStorage.sol. This task is a confirm-only step per D-18 producer-before-consumer Step 1. Verify (via grep + read) that:

    (a) `mapping(address => uint256) internal whalePassClaims;` is declared at line 955 (or in inherited state reachable from the module path).
    (b) `_applyWhalePassStats(address player, uint24 ticketStartLevel) internal` is declared at line 1111 — UNTOUCHED in this phase per D-04 / Pitfall P3.
    (c) `_queueTicketRange(address player, uint24 startLevel, uint24 count, ...)` is declared near line 647 — UNTOUCHED.
    (d) `_livenessTriggered() internal view returns (bool)` is declared at line 1213 (NOT :571 per 334-GREP-ATTESTATION drift correction #1) — the structural guard for D-IMPL-01 (gameOver-forfeit transitivity).

    Record these confirmations in the SUMMARY (Task 5 of the eventual SUMMARY doc). DO NOT edit this file.
  </action>
  <verify>
    <automated>grep -n "mapping(address =&gt; uint256) internal whalePassClaims;" contracts/storage/DegenerusGameStorage.sol</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "mapping(address =&gt; uint256) internal whalePassClaims" contracts/storage/DegenerusGameStorage.sol` returns exactly 1 line.
    - `grep -n "function _applyWhalePassStats" contracts/storage/DegenerusGameStorage.sol` returns exactly 1 line at or near :1111.
    - `grep -n "function _queueTicketRange" contracts/storage/DegenerusGameStorage.sol` returns exactly 1 line at or near :647.
    - `grep -n "function _livenessTriggered" contracts/storage/DegenerusGameStorage.sol` returns exactly 1 line at or near :1213.
    - `git diff contracts/storage/DegenerusGameStorage.sol` is empty after this task (NO edit).
  </acceptance_criteria>
  <done>The four anchors are confirmed at the recorded line numbers; the file is byte-identical to the baseline (no edit).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add lazyPassHorizon external view on DegenerusGame.sol</name>
  <files>contracts/DegenerusGame.sol</files>
  <read_first>
    - contracts/DegenerusGame.sol (lines 1515–1535 — the `hasAnyLazyPass` analog at :1520)
    - contracts/storage/DegenerusGameStorage.sol (lines 1108–1135 — `_applyWhalePassStats` to confirm `frozenUntilLevel` packing constants `BitPackingLib.FROZEN_UNTIL_LEVEL_SHIFT` + `BitPackingLib.MASK_24` are the right unpack ops; the same constants are used at :1520's body)
    - .planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-PATTERNS.md (§2.2 Step 2 + Pitfall P8: view returns `uint24` regardless of stored-field width in AFSUB)
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-AFKING.md (§3 — `lazyPassHorizon` placement + deity sentinel = `type(uint24).max`)
  </read_first>
  <action>
    Insert a new external view `function lazyPassHorizon(address player) external view returns (uint24)` as a SIBLING of `hasAnyLazyPass:1520` (place it immediately AFTER `hasAnyLazyPass`'s closing brace — they share the same packed-read shape).

    Body (mirrors PATTERNS analog A1, the new shape recorded in 335-PATTERNS.md §2.2):
    - Read `mintPacked_[player]` into a local `packed`.
    - If `(packed >> BitPackingLib.HAS_DEITY_PASS_SHIFT) & 1 != 0` → return `type(uint24).max` (deity sentinel per D-11 / Pitfall P8).
    - Otherwise return `uint24((packed >> BitPackingLib.FROZEN_UNTIL_LEVEL_SHIFT) & BitPackingLib.MASK_24)` (the `frozenUntilLevel` unpacked).

    Constraints:
    - The return type is `uint24` (Pitfall P8). The width of `Sub.validThroughLevel` is the planner's call in Plan 335-04 and is INDEPENDENT of this view's return type.
    - DO NOT delete or modify `hasAnyLazyPass` — other callers may reference it; AFSUB-01 removes only AfKing's two reads (`AfKing.sol:432`/`:631`) in Plan 335-04. Co-existence is fine.
    - The `IGame` interface that AfKing.sol declares (:18–43) is owned by Plan 335-04 — that plan adds the new view to the iface decl. This task only adds the impl on DegenerusGame.sol.
    - Solidity visibility/state-mutability MUST match `hasAnyLazyPass` (`external view`).
  </action>
  <verify>
    <automated>grep -n "function lazyPassHorizon" contracts/DegenerusGame.sol</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "function lazyPassHorizon" contracts/DegenerusGame.sol` returns exactly 1 line.
    - `grep -nE "function lazyPassHorizon\(address[^)]*\) external view returns \(uint24\)" contracts/DegenerusGame.sol` returns exactly 1 line.
    - `grep -n "function hasAnyLazyPass" contracts/DegenerusGame.sol` STILL returns the original line — `hasAnyLazyPass` is NOT deleted (co-existence per the action constraints).
    - `grep -c "type(uint24).max" contracts/DegenerusGame.sol` returns ≥ 1 (the deity sentinel is in the new body).
    - `grep -nE "BitPackingLib\.(FROZEN_UNTIL_LEVEL_SHIFT|MASK_24|HAS_DEITY_PASS_SHIFT)" contracts/DegenerusGame.sol` returns ≥ 3 lines (the packing constants are reused, not redefined).
    - The post-edit file still compiles as Solidity — verified by Plan 335-06's `forge build` (not asserted here; the syntactic grep gates above are sufficient at task-level).
  </acceptance_criteria>
  <done>The `lazyPassHorizon` view is present alongside `hasAnyLazyPass`; returns `uint24` (Pitfall P8); deity sentinel + frozen-until-level unpack semantics implemented per PATTERNS §2.2; `hasAnyLazyPass` untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Retire WHALE-03 autoOpen carve-out — delete OPEN_NORMAL_GAS_UNIT, gas-weighting, and ceil-divide math</name>
  <files>contracts/DegenerusGame.sol</files>
  <read_first>
    - contracts/DegenerusGame.sol (lines 1555–1565 — the `OPEN_NORMAL_GAS_UNIT` constant; lines 1680–1740 — the `autoOpen` body and weighted-budget loop)
    - .planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-PATTERNS.md (§2.2 Step 2 — the retirement pattern with the flat-count replacement)
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-DESIGN-LOCK-WHALE-MINTDIV.md (§6 — the WHALE-03 retirement, locked under D-07)
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-IMPL-EDIT-ORDER-MAP.md (§1 Step 2 — the autoOpen-weight retirement is the producer-side condition that lets Plan 335-06 measure flat OPEN_BATCH from `KeeperOpenBoxWorstCaseGas`)
  </read_first>
  <action>
    Three surgical deletions inside `contracts/DegenerusGame.sol`:

    (a) **Delete** `uint256 private constant OPEN_NORMAL_GAS_UNIT = 90_000;` at `:1561` (the constant decl; mark with full deletion of the line, no placeholder).

    (b) **Inside `autoOpen(uint256 maxCount) external returns (uint256 opened)` (header at `:1687`)** — replace the weighted-budget loop body per PATTERNS analog §2.2:
        - DELETE `uint256 weighted;` declaration (whichever line it is on within the autoOpen body).
        - DELETE the per-iter `uint256 g0 = gasleft();` and `uint256 used = g0 - gasleft();` measurements (the two `gasleft()` reads bracketing `_autoOpenBox(index, player);`).
        - DELETE the per-iter `weighted += used / OPEN_NORMAL_GAS_UNIT;` math at `:1728` AND the immediately-following ceil-divide compensation `if (used % OPEN_NORMAL_GAS_UNIT != 0 || used == 0) ++weighted;`.
        - REPLACE the loop guard `while (cursor < qlen && weighted < maxCount)` with `while (cursor < qlen && opened < maxCount)` — i.e. use the existing `opened` counter (already incremented per `_autoOpenBox` call) as the flat opened-count guard. NO new local variable needed.
        - The `_autoOpenBox(index, player);` call STAYS unchanged; the `++opened` increment STAYS in the surrounding `unchecked` block; the cursor walk preamble (skip-already-emptied + `++cursor`) STAYS unchanged.

    (c) **Update or delete** the stale `~5.4M whale-pass box` comment text at `:1558` (the constant's docstring) and `:1681-1682` (any comment in `autoOpen`'s prelude referring to whale-pass-weighted cost). After WHALE-01 (Plan 335-02), opens are uniform O(1); the comment must reflect that or be removed.

    Constraints:
    - DO NOT change the function signature of `autoOpen`. DO NOT add new modifiers. DO NOT touch the `_livenessTriggered() || rngLockedFlag` early-out.
    - DO NOT add a new pre-derived `OPEN_BATCH = 100` (or any constant) here — the flat OPEN_BATCH constant is PICKED FROM MEASUREMENT in Plan 335-06 (D-IMPL-04 / Pitfall P4). This task only RETIRES the weighting; it does not establish a new ceiling.
    - The `_autoOpenBox(index, player)` call site MUST remain. The per-iter `++opened` MUST remain inside its existing `unchecked` block.
  </action>
  <verify>
    <automated>grep -n "OPEN_NORMAL_GAS_UNIT" contracts/DegenerusGame.sol</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "OPEN_NORMAL_GAS_UNIT" contracts/DegenerusGame.sol` returns 0 lines (constant + all references removed).
    - `grep -cE "uint256 g0 = gasleft\(\)" contracts/DegenerusGame.sol` returns 0 (the gas-weight measurement is gone).
    - `grep -cE "used / OPEN_NORMAL_GAS_UNIT" contracts/DegenerusGame.sol` returns 0 (the ceil-divide math is gone).
    - `grep -cE "uint256 weighted" contracts/DegenerusGame.sol` returns 0 (the local `weighted` accumulator is gone — only `opened` survives as the loop counter).
    - `grep -nE "while \(cursor < qlen && opened < maxCount\)" contracts/DegenerusGame.sol` returns exactly 1 line (the new flat opened-count guard).
    - `grep -n "_autoOpenBox(index, player)" contracts/DegenerusGame.sol` STILL returns the original call site (the call is untouched — only the surrounding weighting math is removed).
    - `grep -n "rngLockedFlag" contracts/DegenerusGame.sol` STILL returns the autoOpen early-out (rngLock liveness is untouched).
  </acceptance_criteria>
  <done>The WHALE-03 retirement is mechanically complete on the facade: constant deleted, gas-weighting removed, flat opened-count guard installed; `_autoOpenBox` + the rngLockedFlag/liveness gate untouched; comments aligned with uniform O(1) opens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external caller → `autoOpen()` | Untrusted keeper (any address) crosses here, but `autoOpen` is permissionless and value-neutral (no funds move; only player-owned `_autoOpenBox` writes). The only effect of an unconstrained caller is gas spent. |
| any address → `lazyPassHorizon(player)` external view | Pure view; reads only `mintPacked_[player]`. No state mutation. No revert path. |
| Plan 335-04's AfKing consumer → `lazyPassHorizon` | In-contract delegatecall via `IGame` iface; bounded; no untrusted control flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-335-01 | Information disclosure | `lazyPassHorizon` view | accept | View returns a value already derivable from public `mintPacked_[player]` reads + the public `level()` getter; no new disclosure surface. |
| T-335-02 | Tampering | `autoOpen` flat-count loop guard | mitigate | The replacement guard is `opened < maxCount` (caller-bounded). The `_livenessTriggered() || rngLockedFlag` early-out at function entry is UNTOUCHED, so RNG-lock + liveness invariants survive verbatim. **No NEW write into a frozen slot is introduced** (v45 VRF-freeze invariant — re-attested by 334-WHALE04-FREEZE-PROOF; the retired weighting was a read-only `gasleft()` measurement, never an entropy input). |
| T-335-03 | Denial of service | `autoOpen` under whale-pass-cluster worst-case | mitigate | The flat opened-count guard means `maxCount` opens consume `≤ maxCount × measured_per_box` gas. The actual ceiling enforcement (`chosen × measured ≤ 16.7M − headroom`) is established empirically in Plan 335-06 via `KeeperOpenBoxWorstCaseGas`; if measurement reveals a ceiling overshoot under any reasonable `OPEN_BATCH`, the D-IMPL-04 STOP-and-re-spec gate fires. |
| T-335-04 | Elevation of privilege | adding a redundant `if (gameOver) revert` to `claimWhalePass` | mitigate (avoid) | **DO NOT ADD** per Pitfall P2 / D-IMPL-01. The existing `_livenessTriggered:1213` enforces gameOver-forfeit structurally. A redundant guard is wasted gas AND a freeze-floor signal that the structural argument is fragile. This plan does NOT touch `claimWhalePass`. |
| T-335-05 | Tampering | repackaging this plan into a `pendingWhalePasses` parallel map | mitigate (avoid) | **DO NOT INTRODUCE** per Pitfall P1 / D-02 / D-20. The storage IS `whalePassClaims` (already declared, confirmed Task 1). Any new parallel map is a deviation from the locked decision. |

## Invariants re-attested

- **v45 VRF-freeze invariant** — this plan introduces no new write into any `mintPacked_[*]` / `dailyWord_[*]` / VRF-derived slot during `rngLock` (`autoOpen`'s loop guard rewrite is read-only-on-gas; the new view is pure-read). Cite `334-WHALE04-FREEZE-PROOF.md §5` for the slot-by-slot freeze map.
- **OPEN-E operator-approval trust boundary** — NOT IN SCOPE of this plan (AfKing-side; Plan 335-04 owns the 4-protection re-attest).
- **AFKING cancel-tombstone + swap-pop invariant** — NOT IN SCOPE of this plan (Plan 335-04).
- **D-IMPL-01 gameOver-forfeit structural guard** — preserved by NOT touching `claimWhalePass` (T-335-04).
- **`_applyWhalePassStats` 3-caller invariant** — preserved by NOT touching `_applyWhalePassStats` (Storage decl untouched — Task 1).
</threat_model>

<verification>
Plan-level checks (executor MUST run after all 3 tasks):

1. `git diff --stat contracts/storage/DegenerusGameStorage.sol contracts/DegenerusGame.sol` shows changes ONLY in `DegenerusGame.sol` (Storage is confirm-only).
2. `grep -n "function lazyPassHorizon" contracts/DegenerusGame.sol` returns 1 line.
3. `grep -n "OPEN_NORMAL_GAS_UNIT" contracts/DegenerusGame.sol` returns 0 lines.
4. `grep -cE "uint256 g0 = gasleft\(\)" contracts/DegenerusGame.sol` returns 0.
5. `grep -nE "while \(cursor < qlen && opened < maxCount\)" contracts/DegenerusGame.sol` returns 1 line.
6. `grep -n "function hasAnyLazyPass" contracts/DegenerusGame.sol` STILL returns the original line (co-existence preserved).
7. NO `claimWhalePass`-named function is added or modified inside `contracts/DegenerusGame.sol` — `grep -n "function claimWhalePass" contracts/DegenerusGame.sol` returns 0 lines (per Claude's Discretion: expose the module-direct path at WhaleModule:1018).
8. The post-edit file MUST still parse; a partial `forge build` is acceptable to gate this (a full green build is asserted by Plan 335-06 after all upstream consumers land).
</verification>

<success_criteria>
- WHALE-03 retirement (facade-side) mechanically complete; constant + gas-weighting + ceil-divide math removed; flat opened-count guard installed.
- `lazyPassHorizon(address)` view present alongside `hasAnyLazyPass:1520`; returns `uint24`; deity sentinel + frozen-until-level unpack correct.
- `whalePassClaims` slot confirmed at Storage:955 (no edit); `_applyWhalePassStats` / `_queueTicketRange` / `_livenessTriggered` confirmed at their drift-corrected anchors.
- NO change to `claimWhalePass` / `_applyWhalePassStats` / `_queueTicketRange` / `_livenessTriggered` (Pitfall P2 / P3).
- NO new parallel `pendingWhalePasses` map (Pitfall P1).
</success_criteria>

<output>
Create `.planning/phases/335-impl-the-one-batched-contract-diff-whale-afsub-mintdiv-if-re/335-01-SUMMARY.md` when done. The SUMMARY MUST record:
- Confirmed anchors from Task 1 (the four `file:line` confirmations vs the working tree).
- Exact line ranges of the deletions in `DegenerusGame.sol` (constant + gas-weight measurement + ceil-divide math + comment updates).
- The new `lazyPassHorizon` view's final line range (insertion point relative to `hasAnyLazyPass`).
- A statement that `claimWhalePass` was NOT touched on the facade (Claude's Discretion path: expose `WhaleModule:1018` directly; if a follow-up reveals callers need a facade fn, it can be added in 336 as a non-storage-impacting wrapper).
- A grep-attestation block confirming all 8 plan-level verification gates above pass on the post-edit file.
</output>
