---
phase: 104-day-advancement-vrf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/unit-02/COVERAGE-CHECKLIST.md
autonomous: true
requirements: [COV-01, COV-02, COV-03]

must_haves:
  truths:
    - "Every state-changing function (external, public, internal, private) in DegenerusGameAdvanceModule.sol has a checklist entry"
    - "Functions are categorized as B (external/public state-changing), C (internal/private state-changing), or D (view/pure) -- no Category A (per D-01)"
    - "Category B functions have risk tiers assigned and all audit columns initialized as pending"
    - "Category C functions note their parent callers and flag MULTI-PARENT functions for extra scrutiny"
    - "The checklist totals match the research inventory (~35 functions: 6B + 21C + 8D) or documents why counts differ"
  artifacts:
    - path: "audit/unit-02/COVERAGE-CHECKLIST.md"
      provides: "Complete function checklist for Taskmaster coverage enforcement"
      contains: "Function Checklist"
  key_links:
    - from: "audit/unit-02/COVERAGE-CHECKLIST.md"
      to: "contracts/modules/DegenerusGameAdvanceModule.sol"
      via: "Every state-changing function listed with line numbers"
      pattern: "line \\d+"
---

<objective>
Taskmaster Agent: Build the coverage checklist for Unit 2 (DegenerusGameAdvanceModule).

Purpose: The Taskmaster ensures no function is skipped during the Mad Genius attack phase. Every state-changing function must appear on this checklist. The Mad Genius cannot sign off until every entry is analyzed.

Output: COVERAGE-CHECKLIST.md (function-by-function audit tracking table with categories B/C/D)
</objective>

<execution_context>
@.planning/ULTIMATE-AUDIT-DESIGN.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-day-advancement-vrf/104-CONTEXT.md
@.planning/phases/104-day-advancement-vrf/104-RESEARCH.md
@audit/KNOWN-ISSUES.md

<!-- Format reference from Phase 103 (methodology only, not findings) -->
@audit/unit-01/COVERAGE-CHECKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build complete function coverage checklist for DegenerusGameAdvanceModule</name>
  <files>audit/unit-02/COVERAGE-CHECKLIST.md</files>

  <read_first>
    - .planning/ULTIMATE-AUDIT-DESIGN.md (Agent 3: Taskmaster section -- adopt this persona)
    - .planning/phases/104-day-advancement-vrf/104-RESEARCH.md (complete function inventory with categories B/C/D)
    - .planning/phases/104-day-advancement-vrf/104-CONTEXT.md (decisions D-01 through D-10)
    - contracts/modules/DegenerusGameAdvanceModule.sol (1,570 lines -- read the ENTIRE file, line by line)
    - contracts/storage/DegenerusGameStorage.sol (1,613 lines -- read relevant sections for inherited helpers and storage variable names)
    - contracts/interfaces/IDegenerusGameModules.sol (module interface -- verify function signatures)
    - audit/unit-01/COVERAGE-CHECKLIST.md (format reference ONLY -- do not copy content, only replicate the table structure adapted for B/C/D categories)
    - audit/KNOWN-ISSUES.md (known issues context)
  </read_first>

  <action>
YOU ARE THE TASKMASTER. Read your identity and mandate from ULTIMATE-AUDIT-DESIGN.md, Agent 3.

Your job: Build the COVERAGE CHECKLIST for DegenerusGameAdvanceModule.sol. If a function is missing from your checklist, it will NOT be audited. Completeness is everything.

**Step 1: Read every line of DegenerusGameAdvanceModule.sol (1,570 lines).**
Do NOT rely solely on the research inventory. The research identifies ~35 functions (6B + 21C + 8D). You must independently verify every function exists and is correctly categorized. If you find a function the research missed, ADD IT. If the research miscategorized a function, FIX IT.

**Per D-01: Use Categories B/C/D ONLY. No Category A.** This is a module, not the router. Category A (delegatecall dispatchers) does not apply because AdvanceModule's functions are called via delegatecall FROM the router, not by dispatching delegatecalls themselves.

**Step 2: Categorize every function into one of three categories:**

Category B -- External/Public State-Changing (expected 6 from research):
Functions callable from outside or from the router via delegatecall. Per D-02, each gets full Mad Genius treatment. For each, record:
- Function name and exact line range (from your own source read, not estimates)
- Access control (exact modifier or require statement, or "any caller")
- Primary storage variables written (list the variable names)
- External/delegatecall calls made
- Risk tier: Tier 1 (CRITICAL: complex FSM, multiple code paths, BAF-class risk, VRF lifecycle), Tier 2 (MEDIUM: moderate complexity), Tier 3 (LOW: simple setters/admin)

The research identifies these 6 Category B functions:
| B1 | advanceGame() | L125-397 | external, any caller | Tier 1 (CRITICAL) |
| B2 | requestLootboxRng() | L689-759 | external, any caller | Tier 1 (HIGH) |
| B3 | reverseFlip() | L1438-1446 | external, any caller | Tier 3 (LOW) |
| B4 | rawFulfillRandomWords() | L1455-1476 | external, VRF coordinator only | Tier 1 (HIGH) |
| B5 | wireVrf() | L412-425 | external, ADMIN only | Tier 3 (LOW) |
| B6 | updateVrfCoordinatorAndSub() | L1390-1419 | external, ADMIN only | Tier 2 (MEDIUM) |

Verify these against the actual source. Fix line numbers if they differ.

Category C -- Private/Internal State-Changing Helpers (expected 21+ from research):
Per D-03, these are traced as part of their parent's call tree. They do NOT get standalone attack sections UNLESS they are called from multiple parents with different cached-local states (marked [MULTI-PARENT] in research). For each, record:
- Function name and exact line range
- Called by which parent function(s)
- Storage writes
- Whether it is MULTI-PARENT (called from 2+ parents with different cached-local contexts)

**MULTI-PARENT functions requiring extra scrutiny (from research):**
- C7: payDailyJackpot() -- called from purchase + jackpot paths with different state contexts
- C11: rngGate() -- major routing function, called from B1
- C17: _processFutureTicketBatch() -- called from C18 and B1 paths
- C19: _runProcessTicketBatch() -- called from mid-day, daily, purchase, jackpot paths
- C25: _unlockRng() -- called from B1 and C11
- C28: _applyDailyRng() -- called from C11 and C13

Category D -- View/Pure Functions (expected 8 from research):
Read-only functions. For each, record:
- Function name and line range
- What it reads/computes
- Security note if it exposes internal state dangerously

**Step 3: Create the audit/unit-02/ directory and produce COVERAGE-CHECKLIST.md with this exact structure:**

```
# Unit 2: Day Advancement + VRF -- Coverage Checklist

## Contract Under Audit
- contracts/modules/DegenerusGameAdvanceModule.sol (XXXX lines)
  - Inherits: DegenerusGameStorage (storage layout verified in Unit 1, PASS)
  - Executes via: delegatecall from DegenerusGame
  - Storage context: DegenerusGame's 102-variable layout (slots 0-78)

## Checklist Summary
| Category | Count | Analysis Depth |
|----------|-------|---------------|
| B: External/Public State-Changing | NN | Full Mad Genius (per D-02) |
| C: Internal State-Changing Helpers | NN | Via caller's call tree (per D-03); MULTI-PARENT get extra scrutiny |
| D: View/Pure | NN | Minimal |
| **TOTAL** | **NN** | |

## Category B: External/Public State-Changing Functions

| # | Function | Lines | Access Control | Storage Writes | External/Delegatecalls | Risk Tier | Analyzed? | Call Tree? | Storage Map? | Cache Check? |
|---|----------|-------|---------------|----------------|----------------------|-----------|-----------|------------|--------------|-------------|
| B1 | advanceGame() | X-Y | any | [list] | [list] | 1 | pending | pending | pending | pending |
[... all Category B functions ...]

## Category C: Internal State-Changing Helpers

| # | Function | Lines | Called By | Storage Writes | Multi-Parent? | Analyzed? | Call Tree? | Storage Map? | Cache Check? |
|---|----------|-------|----------|----------------|---------------|-----------|------------|--------------|-------------|
| C1 | _handleGameOverPath() | X-Y | B1 | [list] | NO | pending | pending | pending | pending |
[... all Category C functions ...]

## Category D: View/Pure Functions

| # | Function | Lines | Reads/Computes | Security Note | Reviewed? |
|---|----------|-------|---------------|---------------|-----------|
| D1 | _enforceDailyMintGate() | X-Y | [what] | [note] | pending |
[... all Category D functions ...]

## Priority Investigation Targets (D-04, D-05)
The following functions are PRIORITY for the Mad Genius ticket queue drain investigation:
- _prepareFutureTickets() (C18)
- _processFutureTicketBatch() (C17)
- _runProcessTicketBatch() (C19)
These must be traced end-to-end as part of advanceGame()'s call tree, with a standalone verdict section: CONFIRMED BUG or PROVEN SAFE.

## Cross-Module Delegatecall Targets (D-08, D-09)
advanceGame() delegates to 4 external modules. Per D-08, trace subordinate calls far enough to verify state coherence (cached-local-vs-storage). Per D-09, if a subordinate writes to storage the parent cached locally, that IS a finding.
- GAME_JACKPOT_MODULE: payDailyJackpot, payDailyJackpotCoinAndTickets, payDailyCoinJackpot, consolidatePrizePools, awardFinalDayDgnrsReward, processTicketBatch
- GAME_ENDGAME_MODULE: rewardTopAffiliate, runRewardJackpots
- GAME_MINT_MODULE: processFutureTicketBatch
- GAME_GAMEOVER_MODULE: handleGameOverDrain, handleFinalSweep
```

All "Analyzed?" columns start as "pending" -- the Mad Genius fills these in during Wave 2.
  </action>

  <verify>
    <automated>test -f audit/unit-02/COVERAGE-CHECKLIST.md && grep -c "pending" audit/unit-02/COVERAGE-CHECKLIST.md</automated>
  </verify>

  <acceptance_criteria>
    - File audit/unit-02/COVERAGE-CHECKLIST.md exists
    - Contains all three category tables (B, C, D) -- NO Category A table
    - Category B has exactly 6 external/public state-changing functions (or documents why count differs from research)
    - Category C has at least 21 internal state-changing helpers (or documents why count differs)
    - Category D lists all view/pure functions (~8 from research, may differ after independent verification)
    - Every entry has exact line numbers from the actual source (not the research estimates)
    - MULTI-PARENT functions are explicitly flagged: C7, C11, C17, C19, C25, C28
    - Priority Investigation Targets section lists _prepareFutureTickets, _processFutureTicketBatch, _runProcessTicketBatch
    - Cross-Module Delegatecall Targets section lists all 4 module targets
    - All "Analyzed?" columns show "pending"
    - No function from the source code is missing from the checklist
    - No Category A table exists (per D-01)
  </acceptance_criteria>

  <done>Complete coverage checklist with every state-changing function in DegenerusGameAdvanceModule.sol listed with line numbers, categorized into B/C/D, risk-tiered, multi-parent flagged, and ready for Mad Genius sign-off</done>
</task>

</tasks>

<verification>
- COVERAGE-CHECKLIST.md contains every state-changing function from DegenerusGameAdvanceModule.sol
- All checklist entries have line numbers from actual source
- Categories match D-01 decision (B/C/D only, no A)
- Multi-parent functions flagged for extra scrutiny
- Priority investigation targets explicitly listed
</verification>

<success_criteria>
- Taskmaster has built a complete function checklist that the Mad Genius can systematically work through
- No state-changing function in the audit scope is missing from the checklist
- The checklist is ready for Mad Genius attack phase (Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/104-day-advancement-vrf/104-01-SUMMARY.md`
</output>
