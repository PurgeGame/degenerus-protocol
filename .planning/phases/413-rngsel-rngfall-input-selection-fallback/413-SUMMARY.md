# Phase 413: RNGSEL + RNGFALL — Input-Selection Grinding & Gameover Fallback

**Status:** PASSED. **Verdict: 0 CATASTROPHE / 0 HIGH / 0 MEDIUM; 1 LOW (USER by-design) + 2 INFO (by-design).**
**Subject:** byte-frozen tree `0dd445a6`. Method: 5 adversarial input-selection/fallback agents + 3-lens verify on candidates.

## USER dispositions (domain rulings, take precedence)
- **RNGSEL-01 salvage address-selection → BY-DESIGN / WONTFIX.** Salvage = liquidity for otherwise-illiquid far-future tickets; the jitter band only moves payout within 70-110% of an already-discounted, sub-fair amount — even max-roll is −EV vs holding (max offer 16.5% of face @ d=6, never break-even), so address-selection picks the least-bad point on a below-break-even curve. Counterparty always +EV (never drained below cost); solvency intact. The agent's NEEDS-USER-RULING is resolved by this ruling.
- **`_deityBoonForSlot` → BY-DESIGN (USER + code + V62 precedent).** `deity != recipient` (can't self-boon), 3 fixed-type slots/day used-once, `deityBoonRecipientDay[recipient]==day` (≤1 boon/recipient/day), boon type a pure keccak with no re-roll. Foreknowing the word only chooses which 3 distinct OTHERS get the 3 fixed boons = the intended 'bless whom you choose' power. No quality-grind, no self-enrichment, no stacking.

## Target verdicts

### [FREEZE-HOLDS · INFO · conf=high] _deityBoonForSlot at DegenerusGameLootboxModule.sol:2319 seed keccak256(rngWord,deity,day,slot); issueDeityBoon :1132-11
**Requirement:** RNG-freeze input-selection: a player must not select a controllable input after the VRF word is public to grind boon type or quality beyond intended power.

**Mechanism:** Seed is keccak256(rngWord,deity,day,slot); roll equals seed mod total; boonType from _boonFromRoll. The daily word is public and the shipped DeityBoonViewer precomputes all three slot types pre-grant, so outcomes are knowable. But the boon type cannot be ground: every seed input to a fixed slot is immutable once the word is public. rngWord frozen; deity is the caller own address (a fresh deity needs a 24 ETH plus escalating pass, only 32 exist); day is wall-clock _simulatedDayIndex not caller-supplied; slot 0 to 2 is a fixed three-item menu granted in full, not a re-roll (usedMask one per slot per day :1146-1153, recipient one per day :1147 :1154). Reshape flags are not mutable mid-day for under-budget value: decimatorAllowed equals decWindowOpen written only in advanceGame (AdvanceModule:1733 :1737); deityPassAvailable flips only via 24 ETH pass buys and purchaseDeityPass reverts on a second pass per wallet (WhaleModule:551). So the three per-slot boon types are deterministic and not improvable by any player-selectable input; the freeze holds for the type. The deity only freedom is recipient routing: recipient is freely chosen and does not enter the seed (deity equals recipient reverts :1134, a sybil wallet works), so they route the most valuable of the three pre-revealed boons to a controlled wallet. That is self-dealing distribution, not an RNG break: it changes who gets the already-determined best boon, never its quality or EV.

**Refutation:** Not a real finding: the per-slot boon type is deterministic from frozen word, fixed deity address, wall-clock day, and a fixed slot menu, with no player-selectable input that improves quality (no re-roll, no day-pick, no cheap deity-address change, no affordable flag flip). The remaining freedom, recipient routing or sybil self-grant, is the deity intended power and was already ruled by design: AUDIT-V62-PLAN.md:79 deity sybil self-grant, audit/FINDINGS.md:217 one boon per recipient per day no economic profit, SKEPTIC-REVIEW.md:182-187 the deity grants not gets, AUDIT-V62-FINDINGS-LEDGER.md:56 deityEligible flip economically absurd. The shipped DeityBoonViewer is affirmative design intent. Word frozen plus no quality grind plus by-design routing means the MUTABLE-INPUT tag is a real degree of freedom that does not become a freeze break.

**Citations:** DegenerusGameLootboxModule.sol:1132-1162, 2311-2326, 1633-1695, 350-460, 1873-1936, 187, 1157, 207. DegenerusGame.sol:869-891. DeityBoonViewer.sol:95-122. DegenerusGameWhaleModule.sol:539-605, 551, 558-582, 140. DegenerusGameAdvanceModule.sol:1733, 1737. Prior: AUDIT-V62-PLAN.md:79, audit/FINDINGS.md:217, SKEPTIC-REVIEW.md:182-187, AUDIT-V62-FINDINGS-LEDGER.md:56, v66-RNGNET-CONSUMER-NET.md:49, 124.

### [NEEDS-USER-RULING · LOW · conf=high] RNGSEL-01 — far-future salvage seed address-selection grind. _farFutureSeed(player)=keccak256(abi.encodePacked(player, r
**Requirement:** RNG-derived player-facing outcomes must not be steerable to a precomputable favorable result via a player-controllable input (class A input-selection), and the salvage exit must remain -EV (below face) with solvency preserved.

**Mechanism:** The salvage seed's ONLY player-variable preimage is the seller address; the other preimage, rngWordByDay[currentDay-1], is the SETTLED prior-day VRF word — fully public and immutable when consumed. _resolvePlayer (Game.sol:509-515) only requires msg.sender==player (or approved), so an attacker holding far-future entries across N EOAs can off-chain compute keccak256(addr_i, knownWord) for each, then route sellFarFutureTickets through the address whose seed yields the highest jitterMult=7000+(seed%4001) (MintStreakUtils.sol:183). This is a genuine class-A input-selection grind — NOT a freeze break: the word is settled, the day is the prior day, no player-controllable state changes between request and consumption (the FALLBACK/class-B prevrandao path is not involved). levels[]/quantities[] do not enter the seed (only scale face linearly), and ticketShareBps + the ETH/FLIP split (MintStreakUtils.sol:184,203,223-247) only REDISTRIBUTE the same totalBudget (value-conserving per the code's conservation comments), so the ENTIRE extractable swing is the jitter band. BOUNDS: jitterMult is uniform on [0.70,1.10] (mean 0.90). Selecting max jitter gives 1.10/0.90 = 1.222x value vs. an honest random seller (~22% more), or 1.10/0.70 = 1.571x vs. the worst-case floor (the panel's '1.57x' is the max-over-floor framing, not the realistic max-over-mean). CRITICALLY, the offer is -EV at every parameter: absolute max offer across all distances and max jitter = 16.50% of face (at d=6); 11% at d=20; 5.5% at d=100. Max jitter NEVER reaches break-even, never arbitrages, never exceeds face. Solvency is structurally intact: the buyer (sDGNRS, or vault fallback) funds from its OWN claimable above a >=1 ETH floor, ethRelabel<=totalBudget, and the relabel leaves claimablePool unchanged (MintModule.sol:1305-1308,1339). So neither the DOMINANT (RNG-freeze) nor the SPINE (solvency) invariant is touched. The open question is purely intent: if the jitter band exists to make the discount non-gameable/unpredictable per-player, address-selection defeats that purpose; if it is flavor variance on an already-deep -EV discount, the grind is immaterial.

**PoC:** 1) Read the settled prior-day word W=rngWordByDay[currentDay-1] (public; e.g. via the day getter at DegenerusGame.sol:2090). 2) Off-chain, for each of ~40 candidate EOAs addr_i, compute seed_i=uint256(keccak256(abi.encodePacked(addr_i, W))) and jit_i=7000+(seed_i%4001); ~40 addresses suffices to find jit>=10900 (P=0.0252/address). 3) Acquire/hold the far-future ticket entries at the chosen high-jitter address (entries are per-address in ticketsOwedPacked[ffk][player]; an attacker mints/pre-positions there). 4) Call DegenerusGame.sellFarFutureTickets(addr_best, levels, quantities, queueIndices) from addr_best (or an approved operator). Result: totalBudget=faceWei*fractionBps(d)*~11000/1e8 vs. the ~9000 mean a non-grinder gets — ~22% more ETH/FLIP/tickets out of an already-deeply-discounted exit. Worked example: selling face-value F at d=20, mean seller gets 0.09*F; max-jitter grinder gets 0.11*F; the seed must be re-computed each day (word rotates) and re-pre-positioned, and the offer still returns only 11% of F (an 89% haircut), so the grind merely softens a large loss — it does not produce a profit or a solvency drain.

**User question (RESOLVED above):** Is the per-player/per-day jitter band (jitterMult in [70%,110%] of the discount-curve value) intended to make the salvage offer non-gameable/unpredictable to the seller, or is it merely flavor variance on an already-deep, capped -EV discount? If the former, this is a (LOW) defect: a multi-EOA seller can deterministically select the ~110% point off-chain (knownWord + chosen address), realizing ~1.22x the value an honest random seller gets, defeating the anti-gaming intent. If the latter, it is BY-DESIGN — the seller is merely selecting the least-bad point on a curve whose offer never exceeds ~16.5% of face, with zero freeze/solvency impact. If you want it non-grindable, the fix is to remove the player address from the seed preimage (key the jitter on a non-player-selectable input, e.g. the bundle/level set or a per-tx nonce, while keeping it freeze-safe).

**Citations:** contracts/modules/DegenerusGameMintStreakUtils.sol:252-258 (_farFutureSeed = keccak(player, rngWordByDay[day-1])); :183-184 (jitterMult 7000+(seed%4001), ticketShareBps 4000+((seed>>128)%4001)); :196-206 (totalBudget = faceWei*fractionBps(d)*jitterMult/1e8; ticket/cash split); :223-247 (_quoteFarFutureFlipSplit, value-conserving); :145-148 (_farFutureFractionBps two-line discount curve 15%->10%->5%); contracts/modules/DegenerusGameMintModule.sol:1241-1326 (sellFarFutureTickets exec path); :1305-1308 (total-preserving relabel, ethRelabel<=totalBudget); :1338-1354 (_resolveSalvageBuyer, sDGNRS >= totalBudget+1 ether floor); :1378-1400 (_removeFarFutureTickets ownership guard, per-address entries); contracts/DegenerusGame.sol:509-515 (_resolvePlayer: msg.sender==player or approved); :1610-1629 (sellFarFutureTickets entrypoint, _resolvePlayer then delegatecall); :2090 (rngWordByDay day getter — word is public); contracts/storage/DegenerusGameStorage.sol:463 (mapping(uint24=>uint256) rngWordByDay). .planning/v66-BLIND-SPOT-PANEL.md:75-77 (panel MED/likely + 1.57x estimate); .planning/v66-RNGNET-CONSUMER-NET.md:85,144 (consumer #64 NEEDS-PROOF — transitive salvage seed).

### [FREEZE-HOLDS · N/A · conf=high] RNGSEL-02 — Degenerette score-seed input-selection grind (DegenerusGameDegeneretteModule.sol _resolveFullTicketBet:635-6
**Requirement:** No player-controllable input (the per-bet index, which is the sole bet-selectable seed component) may bind to a lootboxRngWordByIndex slot whose VRF word is already known/revealed at placement time. Because resultTicket = packedTraitsDegenerette(keccak(rngWord, index[, spinIdx], QUICK_PLAY_SALT)) omits betId and player, it is a shared, precomputable "house" draw per (index, spinIdx); the player scores their committed customTicket/heroQuadrant against it. The freeze therefore requires: a bet can only bind to an index whose word is still 0, and that word must only be written AFTER the index is sealed against new placements.

**Mechanism:** The shared-draw observation is correct (resultTicket at DegeneretteModule:690 is pure in (rngWord, index, spinIdx); betId/player omitted, :676-689), and the entire freeze does rest solely on the placement guard at :531-533 (the placement path has NO rngLockedFlag gate — verified _placeDegeneretteBet:467-499 / _placeDegeneretteBetCore:501-584). The guard is sufficient because three independently-verified facts make it impossible for any write to land on the active placement index: (1) LR_INDEX is strictly monotonic — the ONLY mutator is _lrAdvanceIndexClearPending (AdvanceModule:1660-1669, always +1); the three other lootboxRngPacked writers (WhaleModule:916, MintModule:1611, AfkingModule:1041) field-mask ONLY LR_PENDING_ETH and preserve LR_INDEX; no decrement/reset exists anywhere. (2) Every lootboxRngWordByIndex write targets LR_INDEX-1 or lower: daily-drain pre-write preIdx=LR_INDEX-1 (:278-289), _finalizeLootboxRng LR_INDEX-1 (:1282-1284), mid-day rawFulfillRandomWords LR_INDEX-1 (:1843-1844), gap-backfill _backfillOrphanedLootboxIndices loop from idx-1 downward (:1890-1901). NONE writes the active LR_INDEX. (3) Both request sites advance LR_INDEX in the REQUEST tx, strictly before any word can land (fulfillment/finalize is a later tx): fresh daily _finalizeRngRequest:1686-1688, mid-day requestLootboxRng:1140; retryLootboxRng:1153 deliberately does NOT advance (preserves the reserved slot for the same index); updateVrfCoordinatorAndSub re-issues with LR_INDEX preserved (:1769-1772). Consequence: at the instant a word is written to index N, LR_INDEX is already >= N+1, so new bets bind to N+1 (word 0) and index N is sealed. Across all three demanded interleavings — gap-backfill (writes <= LR_INDEX-1, never active), mid-day retry (no advance, same sealed slot), daily request pre-increment (advance precedes word in time) — no interleaving leaves the active index worded while placement is accepted.

**Refutation:** This verdict (FREEZE-HOLDS) would flip to a HIGH REAL-FINDING if ANY of these were shown false: (a) a code path writes lootboxRngWordByIndex[X] with X == current LR_INDEX (the active placement target) — none found; all writes are LR_INDEX-1 or lower; (b) LR_INDEX is ever decremented or reset below a previously-worded index — none found; sole mutator is monotonic +1, other packed writers mask only LR_PENDING_ETH; (c) a word can be written to index N in the SAME tx that LR_INDEX is still N (i.e., a request that writes the word before advancing the index) — none; every request advances first, and word writes are in later fulfillment/finalize txs; (d) a placement path bypasses the :531-533 guard (alternate degeneretteBets writer) — none; the sole writer is :563 under the guard (Game.sol:1350/1373 are read-only views); (e) customTicket could be supplied at resolve rather than frozen at placement — it is frozen in the packed bet. All five were checked and refuted.

**Citations:** contracts/modules/DegenerusGameDegeneretteModule.sol:531-533 (placement freeze guard, sole gate); :547-555/:563 (frozen customTicket+index pack, sole bet writer); :642-653 (frozen decode + resolve re-check on frozen index); :676-690 (shared resultSeed/resultTicket, betId/player omitted); :698 (player pick scored against shared draw). contracts/modules/DegenerusGameAdvanceModule.sol:1660-1669 (_lrAdvanceIndexClearPending, sole +1 LR_INDEX mutator); :1686-1688 (fresh daily advance-before-word); :1140 (mid-day requestLootboxRng advance-before-word); :1153-1171 (retryLootboxRng no-advance, preserves reserved slot); :278-290 (daily pre-write to LR_INDEX-1); :1281-1286 (_finalizeLootboxRng to LR_INDEX-1); :1843-1844 (mid-day rawFulfillRandomWords to LR_INDEX-1); :1889-1908 (_backfillOrphanedLootboxIndices, loop <= LR_INDEX-1); :1769-1772 (updateVrfCoordinatorAndSub preserves LR_INDEX). contracts/modules/DegenerusGameWhaleModule.sol:916 / DegenerusGameMintModule.sol:1611 / GameAfkingModule.sol:1041 (lootboxRngPacked writers, LR_PENDING_ETH-mask only, LR_INDEX preserved). contracts/storage/DegenerusGameStorage.sol:1532-1534 (genesis LR_INDEX=1). .planning/v66-RNGNET-CONSUMER-NET.md row 29 (FROZEN-AT-COMMIT classification corroborated).

### [FREEZE-HOLDS · N/A · conf=high] RNGSEL-03 — redemption/lootbox elective-resolution / first-mover capture. Seed = keccak256(rngWordForDay(day+1), player,
**Requirement:** RNG-freeze / input-selection invariant: a player must not be able to SELECT a controllable input (address, claim-order, which-slot, timing) to land on a precomputed favorable derived outcome (e.g. a whale-pass jackpot tier), and must not capture order-dependent value a passive holder would not get. The frozen VRF word alone does not answer input-selection or first-mover capture.

**Mechanism:** The redemption-lootbox outcome IS publicly precomputable once the period resolves (redemptionPeriods[day]!=0), and a live-game claim is permissionless with no deadline — but none of the three grind vectors lands, because every seed input is bound to the (player,day) slot and is NOT a claim-time-selectable preimage:

1) ADDRESS/SLOT SELECTION: claimRedemption(player,day) (sDGNRS:782) takes player+day, but they only INDEX into pendingRedemptions[player][day] (sDGNRS:833) written at submit. The seed's address component is claim.player, the amount is claim.ethValueOwed*roll/100/2 (sDGNRS:842,851 → resolveRedemptionLootbox amount → _resolveRedemptionChunk seed:970), and rngWord=rngWordForDay(day+1). A caller cannot pass a different player to get a favorable hash — the slot for an arbitrary address is empty (returns false / NoClaim, sDGNRS:837). No menu of slots to cherry-pick: the single-pool sentinel _pendingResolveDay (sDGNRS:1041-1043) forces every burn of a day into ONE pool resolved by ONE roll, so a player holds at most one (player,day) slot per day and it has exactly one deterministic outcome.

2) ELECTIVE-RESOLUTION / NO-DEADLINE RE-ROLL: declining to claim an unfavorable resolved slot yields no benefit — the slot is immutable and re-submitting does NOT overwrite/re-roll it. A new burn stamps a FRESH (player,newDay) slot keyed to newDay+1's word; submit is gated by BurnsBlockedBeforeDailyRng (sDGNRS:1036) to the CURRENT drawn day, so newDay+1 is undrawn and unknowable at submit. Re-rolling therefore requires committing a fresh proportional sDGNRS burn BEFORE the deciding word exists — no free re-roll, no foreknowledge, real value at risk each iteration. This is the exact REMEDIATED v42/v43 §12 cross-day re-roll class (per .planning/v66-RNGNET-CONSUMER-NET.md RNGNET-03): the old re-armable pendingRedemptionEthBase is gone; resolveRedemptionPeriod (sDGNRS:742) writes a plain day-keyed roll once and deletes the pool.

3) WHALE-PASS FIRST-MOVER / ORDER-DEPENDENCE: the whale-pass award is NOT order-dependent and has NO global scarcity. _activateWhalePass (Lootbox:1486-1490) is unconditionally whalePassClaims[player]+=1 — no cap, no FCFS counter, no shared pool. Whether a box yields BOON_WHALE_PASS depends solely on that box's own frozen seed bits[120..151] vs its own weight table (_rollLootboxBoons:1456, _boonFromRoll:1687). Two claimants never compete for the same whale pass; claiming FIRST captures nothing a later claim wouldn't. (The only global-capped pass is the DEITY pass, DEITY_PASS_MAX_TOTAL=32 at Lootbox:1435, which merely toggles deity-discount-boon EV inclusion and is still per-box-seed-deterministic — and is a separate consumer, not this redemption path.) The boon weight space reads live game-state (decWindowOpen, deityPassOwners.length, lazyPassValue, currentDay) at Lootbox:1424-1444, but those reshape the roll→boon-type MAPPING identically for any caller, not the seed, and the player cannot steer them to their own benefit at claim time.

4) VALUE-CAPTURE BY CLAIMANT: a third-party keeper that triggers the claim receives only the small FLIP keeper bounty (sDGNRS:820-824); ALL lootbox/direct/whale-pass value routes to the slot's player (Lootbox lootbox+direct legs credit player; sDGNRS:920,928). Outcome is identical regardless of who claims or when. A passive holder who never gambling-burned simply has no slot — they chose the plain burn() path; that is opt-in, by-design, not asymmetric capture.

**Refutation:** Verdict could flip to a finding only if ANY of these were true (all checked false at HEAD): (i) claimRedemption let the caller pass an arbitrary player/amount/day that re-derived a fresh seed not bound to a pre-written slot — FALSE, slot is loaded and emptiness returns false (sDGNRS:833,837); (ii) re-submitting overwrote/re-rolled an already-resolved (player,day) slot or a player could hold multiple same-day slots with independent rolls — FALSE, single-pool sentinel + day-keyed plain roll (sDGNRS:759,1041-1043); (iii) submit could stamp a slot whose resolving day+1 word was already on-chain — FALSE, BurnsBlockedBeforeDailyRng pins to the current drawn day (sDGNRS:1036); (iv) the whale pass (or any awarded boon) had a global FCFS cap making it order-dependent — FALSE, _activateWhalePass is uncapped per-player (Lootbox:1486-1490); (v) a third party could capture the lootbox/pass value instead of the slot's player — FALSE, all value credits player (sDGNRS:920,928; bounty-only to caller 820-824). The freeze additionally inherits the CROSS-CONTRACT-SEAM residue that rngWordForDay(day+1) must be undrawn at submit (consumer #52 in v66-RNGNET); that seam is enforced by the submit-day pin and is adjudicated separately (RNGSEAM-01/02) — it is the submit-timing freeze, NOT this elective-resolution/first-mover question, which holds independently.

**Citations:** contracts/sDGNRS.sol:782 (claimRedemption), :833-837 (slot load + emptiness guard), :842,851 (amount derivation), :871 (slot delete CEI), :916-920 (seed entropy mint), :742-767 (resolveRedemptionPeriod day-keyed once), :1036 (BurnsBlockedBeforeDailyRng submit pin), :1041-1043 (_pendingResolveDay single-pool sentinel), :1129-1142 (slot write at submit), :820-824 (keeper bounty to caller only). contracts/modules/DegenerusGameLootboxModule.sol:926-956 (resolveRedemptionLootbox deterministic 5-ETH chunk loop), :965-992 (_resolveRedemptionChunk seed:970), :1276 + :1456-1465 (_rollLootboxBoons roll), :1633-1695 (_boonFromRoll), :1687-1688 (BOON_WHALE_PASS branch), :1899-1910 (_applyBoon whale-pass), :1486-1490 (_activateWhalePass uncapped), :1435 (DEITY_PASS_MAX_TOTAL=32, separate path). .planning/v66-RNGNET-CONSUMER-NET.md consumer #52 + RNGNET-03 (§12 re-roll class remediated). .planning/v66-BLIND-SPOT-PANEL.md tier-3 item #14.

### [FREEZE-HOLDS · INFO · conf=high] RNGFALL-01 — gameover prevrandao fallback (`_gameOverEntropy` AdvanceModule.sol:1327-1364 + `_getHistoricalRngFallback` 
**Requirement:** RNG-consumer freeze: no player-controllable input may be SELECTED (class A) or mutated in the same/adjacent block (class B) to bias a derived outcome under the VRF-dead prevrandao fallback while the rngLocked()-dependent consumer gates are open.

**Mechanism:** CONFIRMED MECHANICS: (1) The fallback fires inside `_gameOverEntropy` only when liveness triggered during a >=14-day VRF stall (Storage:1464-1472 _VRF_GRACE_PERIOD=14d; GAMEOVER_RNG_FALLBACK_DELAY=14d). fallbackWord = keccak(hash-of-up-to-5 committed historical VRF words, currentDay, block.prevrandao) (`_getHistoricalRngFallback`:1408-1410). The only non-immutable preimage input is block.prevrandao = the 1-bit propose-or-skip proposer lever. The single player-controllable input that could enter (reverseFlip nudge) is explicitly canceled: `fallbackWord -= totalFlipReversals` (:1337) before `_applyDailyRng` re-adds it (:1919). (2) LOCK-STATE: two VRF-dead sub-scenarios. B1 (request accepted, node never fulfills) keeps rngLockedFlag=TRUE through the fallback (`_finalizeRngRequest`:1696 set it; `_unlockRng` never ran), so ALL consumer gates stay CLOSED — no exposure. A1 (coordinator rejects every request) reaches `_gameOverEntropy`:1367; `_tryRequestRng` catch sets rngRequestTime=ts at :1371-1373 WITHOUT setting rngLockedFlag, so 14d later the fallback fires with rngLocked()==FALSE — confirming the prompt premise that the bounty-arm gate (Coinflip:686 `!game.rngLocked()`) and carry gates (:752/791/822) are OPEN. (3) CONSUMERS riding fallbackWord: Coinflip win=(fallbackWord & 1) and rewardPercent=keccak(fallbackWord,epoch)%20 (`processCoinflipPayouts`:864-886, called :1341 when lvl!=0); the bounty DGNRS payout (Game.payCoinflipBountyDgnrs:469 = poolBalance(Reward)*20/10000 = 0.2% of the real-equity Reward pool, paid to bountyOwedTo iff win); the sentinel redemption roll ((fallbackWord>>8)%151+25 at :1352-1355) scaling a pre-frozen pending pool (sDGNRS.resolveRedemptionPeriod:754); `_finalizeLootboxRng`:1358. WHY FREEZE HOLDS: (a) NO class-A input-selection grind — no player-chosen address/index/ordering/which-to-claim enters ANY fallback-consumer preimage; arming the bounty or extracting carry in the window is EV-neutral 50/50 to a non-proposer. (b) New gambling-burn submits are blocked in A1 (BurnsBlockedBeforeDailyRng sDGNRS:1036 — the stalled day's word is unrecorded by definition) and box buys/opens/ticket queues are blocked by `_livenessTriggered()` (LootboxModule:659; Storage:618/650/703), so redemption-magnitude and lootbox-binding pools cannot be freshly armed during the window — only a pre-existing pending pool sees a 1-bit-biased roll. (c) The bias is the SAME 1-bit lever already documented + design-accepted (KNOWN-ISSUES.md L29; EXC-02/EXC-03 re-verified v25/v29/v31 GOE-01/GOE-04) and is NOT widened: forcing bit-0 to win re-randomizes the whole keccak word, so a colluding proposer cannot hold the dominant terminal jackpot fixed while grabbing the 0.2% bounty — the lever is genuinely joint across all consumers. (d) One-shot: the fallback latches rngWordByDay[day], clears rngRequestTime, and gameOver latches in the same advanceGame flow (GameOverModule:135), so the grab is non-repeatable.

**Refutation:** Flips to REAL-FINDING (DOMINANT) if EITHER: (1) a NON-proposer player could deterministically bias a fallback consumer by selecting a controllable input (e.g. a caller-chosen address/index/day folded into any seed) — verified absent: every preimage is {immutable historical words, currentDay, prevrandao}; the lone player input (reverseFlip nudge) is canceled at :1337. (2) The bias were >1 bit, or the proposer could hold the terminal jackpot fixed while independently grabbing the bounty/roll — refuted: forcing bit-0 re-randomizes the entire keccak word (joint, not separable). (3) Fresh redemption/lootbox/ticket inputs could be armed DURING the window to load a freshly-predictable pool — refuted: BurnsBlockedBeforeDailyRng (sDGNRS:1036) and `_livenessTriggered()` (LootboxModule:659; Storage:618/650/703) block them. (4) The grab were repeatable — refuted: gameOver latches one-shot in the same flow. If the user deems a proposer-extractable 0.2% real-equity (DGNRS) payout unacceptable even under a 14-day double-failure, the targeted mitigation is to gate the bounty-arm + carry sites on `_livenessTriggered()` (not just `!rngLocked()`) so A1 closes them like every other consumer — a hardening of an accepted envelope, not a defect.

**Citations:** contracts/modules/DegenerusGameAdvanceModule.sol:1293-1375 (`_gameOverEntropy` fallback, reverseFlip-cancel :1337, redemption roll :1352-1355, rngRequestTime-without-lock :1371-1373); :1388-1411 (`_getHistoricalRngFallback`, prevrandao preimage :1408-1410); :1610-1629 (`_tryRequestRng` catch — no lock on failure); :1671-1696 (`_finalizeRngRequest` lock set); :1797-1819 (`_unlockRng` clear); :605-701 (`_handleGameOverPath`, liveness gate :630, unlock :699). contracts/storage/DegenerusGameStorage.sol:1464-1472 (`_livenessTriggered`), :190 (_VRF_GRACE_PERIOD=14d), :618/650/703 (liveness blocks buys). contracts/Coinflip.sol:684-707 (bounty-arm `!game.rngLocked()`), :853-961 (`processCoinflipPayouts` win=fallbackWord&1 :886, bounty payout :897-912), :744-839 (carry/rebuy gates :752/791/822), :1252-1254 (`_targetFlipDay`=+1 → resolved-day stakes frozen >=1 day prior). contracts/DegenerusGame.sol:453-476 (`payCoinflipBountyDgnrs` 0.2% Reward), :1817-1828 (`reverseFlip` rngLocked gate). contracts/sDGNRS.sol:1021-1057 (`_submitGamblingClaimFrom` BurnsBlockedBeforeDailyRng :1036), :742-768 (`resolveRedemptionPeriod` roll scaling :754). contracts/modules/DegenerusGameLootboxModule.sol:656-659 (open gated by `_livenessTriggered()`). contracts/modules/DegenerusGameGameOverModule.sol:135 (gameOver latch). KNOWN-ISSUES.md:29,36 (design-accepted prevrandao bias). audit/FINDINGS-v29.0.md:109-111,194 + audit/v31-245-GOE.md GOE-01/GOE-04 (EXC-02/03 re-verified). .planning/v66-RNGNET-CONSUMER-NET.md rows 9/10; .planning/v66-BLIND-SPOT-PANEL.md Tier-1 item 1.

