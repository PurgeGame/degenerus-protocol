---
phase: 09-level-progression-and-endgame
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v1.1-endgame-and-activity.md
autonomous: true
requirements:
  - LEVL-04
  - END-01
  - END-02

must_haves:
  truths:
    - "Agent can compute a player's activity score BPS given their streak, mint count, quest streak, pass status, and deity status"
    - "Agent knows the exact threshold BPS values where activity score affects lootbox tickets and degenerette ROI"
    - "Agent can determine when distress mode, game-over-imminent, and actual timeout trigger for any level"
    - "Agent can compute the terminal distribution: deity refund, 10% decimator, 90% terminal jackpot, 30-day final sweep"
    - "Agent understands the RNG fallback mechanism that ensures game can always terminate"
  artifacts:
    - path: "audit/v1.1-endgame-and-activity.md"
      provides: "Activity score system, death clock escalation, terminal distribution formulas"
      contains: "activityScore"
  key_links:
    - from: "audit/v1.1-endgame-and-activity.md"
      to: "contracts/DegenerusGame.sol"
      via: "Activity score component formulas"
      pattern: "_playerActivityScore"
    - from: "audit/v1.1-endgame-and-activity.md"
      to: "contracts/modules/DegenerusGameAdvanceModule.sol"
      via: "Death clock liveness guard and distress mode"
      pattern: "livenessTriggered|_isDistressMode|_isGameoverImminent"
    - from: "audit/v1.1-endgame-and-activity.md"
      to: "contracts/modules/DegenerusGameGameOverModule.sol"
      via: "Terminal distribution and final sweep formulas"
      pattern: "handleGameOverDrain|handleFinalSweep|runTerminalJackpot"
---

<objective>
Document the activity score system, death clock escalation timeline, and terminal game-over distribution for game theory agent consumption.

Purpose: Enable agents to compute player activity scores (which affect lootbox and degenerette ROI), model liveness timeout risk at any point in a level's lifecycle, and compute expected payouts from terminal distribution events.

Output: `audit/v1.1-endgame-and-activity.md`
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-level-progression-and-endgame/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create endgame and activity score reference document</name>
  <files>audit/v1.1-endgame-and-activity.md</files>
  <action>
Create `audit/v1.1-endgame-and-activity.md` following the established document pattern (title, purpose, date, source contracts, TOC, numbered sections, constants table).

**Document structure:**

1. **Overview** -- Brief description covering: activity scores modify reward curves, death clock creates terminal game conditions, terminal distribution pays out all remaining funds.

2. **Activity Score System (LEVL-04)** -- Full component breakdown:

   **Base components (non-deity, non-pass):**
   | Component | Formula | Max BPS |
   | Mint streak | 1% per consecutive level | 5000 (50 levels) |
   | Mint count | (mintCount * 25) / currLevel * 100 BPS | 2500 |
   | Quest streak | 1% per quest | 10000 (100 quests) |
   | Affiliate bonus | affiliateBonusPointsBest * 100 | variable |
   | Whale bundle 10-level (type 1) | +1000 BPS | 1000 |
   | Whale bundle 100-level (type 3) | +4000 BPS | 4000 |

   **Active pass holder floors (frozenUntilLevel > currentLevel, bundleType 1 or 3):**
   - Streak floor: PASS_STREAK_FLOOR_POINTS = 50 (5000 BPS minimum)
   - Mint count floor: PASS_MINT_COUNT_FLOOR_POINTS = 25 (2500 BPS minimum)

   **Deity pass holders:**
   - Fixed: 50 * 100 + 25 * 100 = 7500 BPS (replaces streak + count)
   - Plus: DEITY_PASS_ACTIVITY_BONUS_BPS = 8000
   - Plus: quest streak + affiliate (same formulas)
   - Deity minimum: 15500 BPS before quest/affiliate

   Include exact Solidity from DegenerusGame.sol:2387-2463.

3. **Mint Streak Mechanics (LEVL-04 continued)** -- Document MintStreakUtils.sol:
   - _recordMintStreakForLevel: idempotent per level
   - Streak increments if lastCompleted + 1 == mintLevel (consecutive)
   - Streak resets to 1 on gap
   - Storage: mintPacked_[player] bits [160:184] (lastCompleted) and LEVEL_STREAK_SHIFT (streak)
   - _mintStreakEffective: returns 0 if currentMintLevel > lastCompleted + 1 (gap detection)

4. **Activity Score Consumers** -- Where score BPS matters:
   | Consumer | Thresholds | Effect |
   | Lootbox tickets | NEUTRAL=6000, MAX=25500 | Scales future ticket multiplier |
   | Degenerette ROI | MID=7500, HIGH=25500, MAX=30500 | Three-tier piecewise ROI curve |
   | Degenerette WWXRP | Same tiers | Separate high-value ROI function |

   Include the threshold constants and explain the piecewise behavior at each tier boundary.

5. **Death Clock: Three-Stage Escalation (END-01)** -- Document as a timeline:
   - **Deploy timeout (level 0 only):** 365 days from levelStartTime
   - **Level timeout (level > 0):** 120 days from levelStartTime
   - **Safety valve:** if nextPool >= target when timeout fires, levelStartTime resets (AdvanceModule:401-403)

   The escalation stages (document in countdown order):
   - **Game Over Imminent:** 5 days before timeout (level > 0) or 10 days (level 0)
     - Effect: allows decimator burns near timeout
   - **Distress Mode:** 6 hours before timeout
     - Effect: lootbox purchases route 100% to nextPool, 25% ticket bonus (DISTRESS_TICKET_BONUS_BPS=2500)
   - **Game Over:** timeout reached and nextPool still < target

   Include exact Solidity for liveness guard (AdvanceModule:380-382), distress mode (Storage:169-178), and game-over-imminent check.

6. **RNG Fallback for Game Over (END-01 continued):**
   - Normal: VRF request
   - Fallback after 3 days (GAMEOVER_RNG_FALLBACK_DELAY): hashes up to 5 early historical VRF words + currentDay + block.prevrandao
   - Purpose: ensures game termination even if Chainlink VRF stalls

7. **Terminal Distribution (END-02)** -- Document handleGameOverDrain flow:
   Step 1: Early game over deity refund (levels 0-9): 20 ETH per deity pass, FIFO by deityPassOwners array, budget-capped at totalFunds - claimablePool
   Step 2: Set gameOver = true, gameOverTime = block.timestamp
   Step 3: Clear all pools to 0
   Step 4: Decimator 10%: remaining / 10 to runDecimatorJackpot, refunds return to remaining
   Step 5: Terminal jackpot 90% + decimator refund: runTerminalJackpot(remaining, lvl + 1, rngWord) -- NOTE: uses lvl + 1 (next-level ticketholders, NOT current level)
   Step 6: Undistributed remainder to vault via _sendToVault
   Step 7: DGNRS cleanup: burns undistributed pool tokens

   Include exact Solidity from GameOverModule.sol:136-158.

   > **PITFALL:** Terminal jackpot pays next-level ticketholders (lvl + 1), not current-level players. This is the single most important detail for agent game-over modeling.

8. **Final Sweep (END-02 continued)** -- Document handleFinalSweep:
   - Triggers 30 days after gameOverTime
   - Forfeits ALL unclaimed winnings (claimablePool = 0)
   - Splits entire remaining balance 50/50: vault and DGNRS contract
   - stETH prioritized over ETH for transfers
   - Shuts down VRF subscription

   **Vault split (_sendToVault):**
   - 50% to ContractAddresses.VAULT (stETH preferred, ETH fallback)
   - 50% to ContractAddresses.DGNRS (stETH via depositSteth, ETH via direct transfer)

9. **Constants Reference Table** -- Every constant with name, value, source file, line number.

10. **Agent Simulation Notes** -- Pseudocode for: computing activity score, modeling death clock timeline, computing terminal distribution shares.

**Source contracts to reference:**
- contracts/DegenerusGame.sol (_playerActivityScore, _isGameoverImminent, constructor)
- contracts/modules/DegenerusGameMintStreakUtils.sol (streak recording/evaluation)
- contracts/modules/DegenerusGameAdvanceModule.sol (liveness guard, levelStartTime)
- contracts/storage/DegenerusGameStorage.sol (_isDistressMode, DISTRESS_MODE_HOURS)
- contracts/modules/DegenerusGameGameOverModule.sol (handleGameOverDrain, handleFinalSweep)

**Style:** Follow audit/v1.1-burnie-coinflip.md pattern. Pitfall callout boxes in blockquote format. Constants reference table at end. Exact Solidity with file:line references.
  </action>
  <verify>
    <automated>grep -cE "LEVL-04|END-0[1-2]|activityScore|deathClock|distress|gameOver|terminal|handleFinalSweep" audit/v1.1-endgame-and-activity.md</automated>
  </verify>
  <done>Document exists with: (1) complete activity score component table with BPS values for all three player types, (2) mint streak mechanics with storage layout, (3) activity score consumer thresholds, (4) three-stage death clock timeline with exact Solidity, (5) RNG fallback mechanism, (6) full terminal distribution flow with lvl+1 pitfall callout, (7) final sweep mechanics, (8) constants reference table with source lines</done>
</task>

</tasks>

<verification>
- Activity score formulas match DegenerusGame.sol:2387-2463
- Death clock timing constants match AdvanceModule and Storage source
- Terminal distribution flow matches GameOverModule.sol:136-158
- Deity refund amount matches DEITY_PASS_EARLY_GAMEOVER_REFUND = 20 ether
- Final sweep 30-day delay and 50/50 split verified against source
- Every constant has source file and line number
</verification>

<success_criteria>
A game theory agent reading only this document can: (1) compute any player's activity score BPS and know what reward tier they fall into, (2) determine exactly when distress mode and game-over trigger for any level, (3) compute their expected payout from terminal distribution given their ticket holdings, (4) model the 30-day claim-or-forfeit window after game over.
</success_criteria>

<output>
After completion, create `.planning/phases/09-level-progression-and-endgame/09-02-SUMMARY.md`
</output>
