READ-ONLY defensive security review of the Degenerus smart-contract repo (do NOT modify any file). Independently verify ONE finding and recommend a fix. Cite file:line. Be terse and concrete.

FINDING TO VERIFY (the foil ETH lane bypasses its promised 10% pool cap during a frozen pool):
- `contracts/modules/DegenerusGameDegeneretteModule.sol` function `_distributePayout` (~lines 889-957) applies the ETH_WIN_CAP_BPS (10% of future pool) cap ONLY in the UNFROZEN branch (~938-953; excess spills to lootbox). The FROZEN branch (`prizePoolFrozen == true`, ~929-937) applies NO 10% cap — it only debits `pendingFuture` with a revert-on-insufficient check.
- The foil ETH match leg routes through this path: `contracts/modules/DegenerusGameFoilPackModule.sol` `_payFoilTier` ETH branch (~604-613) stakes `faces * priceForLevel(L)` into `resolveEthSpinFromBox`. The foil tier "faces" are large: {4→2, 5→6, 6→35, 7→400, 8→10000}, so the ETH stake can be FAR larger than a normal Degenerette bet.
- The design `.planning/phases/445-spec-design-lock-the-implementation-contract/445-SPEC.md` (§E.5 C and threat T-445-E2) PROMISED the foil ETH lane is 10%-capped with lootbox spill.

ANSWER THESE:
1. Confirm or refute: during `prizePoolFrozen`, a foil ETH match claim pays the full `ethShare` (= payout/4 for large wins) from `pendingFuture` with NO 10% cap and NO lootbox spill. Cite file:line.
2. Reachability: can `claimFoilMatch` be called while `prizePoolFrozen == true`? Trace whether claims are blocked or allowed during the frozen / terminal-jackpot / game-over window. If a foil ETH claim during freeze with insufficient `pendingFuture` reverts, does it brick the whole `claimFoilMatch` call (a claim-liveness wrinkle / does the match stay claimable later)?
3. Severity: true solvency hole (pool goes negative / unbacked pay)? Or a bounded cap-invariant violation + pending-future drain + claim-timing payout boost (claim during freeze = uncapped vs unfrozen = 10%-capped)? Rate CAT/HIGH/MED/LOW with reasoning. (Pre-launch, no live funds yet.)
4. Preferred fix, and why it least regresses the EXISTING (v70-audited) Degenerette game:
   (1) cap the FROZEN branch too (10% of pendingFuture + lootbox spill) — but this changes SHARED Degenerette frozen behavior;
   (2) a foil-specific cap so only the foil ETH lane is capped regardless of frozen state;
   (3) block/defer the foil ETH leg while prizePoolFrozen (claim waits until unfrozen) — does this create any new grief/liveness vector given MATCH-05 "records persist per-level"?
   Pick one (or propose better).

End with: VERDICT (real? severity?) + RECOMMENDED FIX (one line).
