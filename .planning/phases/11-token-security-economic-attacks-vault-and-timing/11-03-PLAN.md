---
phase: 11-token-security-economic-attacks-vault-and-timing
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11-token-security-economic-attacks-vault-and-timing/11-03-SUMMARY.md
autonomous: true
requirements: [TOKEN-07, TOKEN-08]

must_haves:
  truths:
    - "TOKEN-07 verdict confirmed: self-referral is blocked at registration, REF_CODE_LOCKED sentinel prevents re-registration after invalid attempt, circular ring EV is bounded below solo play with VAULT referral"
    - "TOKEN-08 verdict confirmed: lockForLevel/unlock cap reset cannot extract double EV at level transition — same-level unlock blocked by LockStillActive revert; inter-level cap reset is intended mechanics; level-transition timing exploit either ruled out or documented as finding"
  artifacts:
    - path: ".planning/phases/11-token-security-economic-attacks-vault-and-timing/11-03-SUMMARY.md"
      provides: "TOKEN-07 and TOKEN-08 verdicts with contract/line evidence and C4 severity for any findings"
      contains: "TOKEN-07 VERDICT:"
  key_links:
    - from: "DegenerusAffiliate.sol referPlayer()"
      to: "REF_CODE_LOCKED sentinel"
      via: "self-referral revert at registration time"
      pattern: "REF_CODE_LOCKED|Insufficient"
    - from: "DegenerusStonk.sol unlock()"
      to: "lockedLevel[msg.sender] == currentLevel check"
      via: "LockStillActive revert for same-level unlock"
      pattern: "LockStillActive|lockedLevel"
    - from: "DegenerusStonk.sol lockForLevel()"
      to: "auto-unlock branch lines 442-448"
      via: "level change detection resets ethSpentThisLevel/burnieSpentThisLevel"
      pattern: "ethSpentThisLevel|burnieSpentThisLevel"
---

<objective>
Deliver verdicts for TOKEN-07 (affiliate economic exploits) and TOKEN-08 (DGNRS lockForLevel/unlock cap reset) via source-trace and arithmetic analysis.

Purpose: Confirm that affiliate mechanics cannot be gamed for positive-EV extraction, and that the DGNRS spending cap reset requires a genuine level transition — ruling out same-level double-spend and level-transition timing exploits.
Output: 11-03-SUMMARY.md with two verdicts, evidence citations, and Code4rena severity for any findings.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-token-security-economic-attacks-vault-and-timing/11-RESEARCH.md

<!-- Key interfaces extracted from codebase for executor -->
<interfaces>
From contracts/DegenerusAffiliate.sol (TOKEN-07):
- referPlayer(address referrer, bytes32 code): line 390
    - if (referrer == address(0) || referrer == msg.sender) revert Insufficient()  — self-referral blocked
    - invalid code on active game → slot locked to REF_CODE_LOCKED sentinel (= VAULT address)
    - once locked to REF_CODE_LOCKED, player cannot re-register referral
- payAffiliate(player, amount): line 446
    - pays upline1: 20% of amount (rakeback to direct referrer)
    - pays upline2: 4% of amount (rakeback to referrer's referrer)
    - max total rakeback: 24% (not 25% — check exact values in source)
    - 2-hop limit: no deeper chain paid

From contracts/DegenerusStonk.sol (TOKEN-08):
- lockForLevel(uint256 amount): line 437
    - reads game.level() as currentLevel
    - if lockedLevel[msg.sender] != 0 AND lockedLevel[msg.sender] != currentLevel:
        auto-unlocks previous lock: clears lockedBalance, ethSpentThisLevel, burnieSpentThisLevel, lockedLevel
    - sets lockedBalance[msg.sender] = amount, lockedLevel[msg.sender] = currentLevel
- unlock(): line 464
    - if lockedBalance[msg.sender] == 0 → revert NoLockedTokens
    - if lockedLevel[msg.sender] == currentLevel → revert LockStillActive  ← same-level guard
    - clears lockedBalance, ethSpentThisLevel, burnieSpentThisLevel
- _checkAndRecordEthSpend(player, amount): enforces per-level ETH spend cap based on lockedBalance
- _lockedClaimableValues(player): computes claimable ETH bonus from locked DGNRS
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TOKEN-07 — Affiliate self-referral, wash trading, and circular ring EV model</name>
  <files>contracts/DegenerusAffiliate.sol</files>
  <action>
Read DegenerusAffiliate.sol completely. For TOKEN-07:

Self-referral check:
(a) Confirm `if (referrer == msg.sender) revert Insufficient()` is enforced at referPlayer() (line 390 area).
(b) Confirm REF_CODE_LOCKED sentinel behavior: what value is REF_CODE_LOCKED? When a player attempts invalid referral on an active game, is their referral slot permanently locked to the sentinel (VAULT address)? Once locked, can they ever re-register with a valid code?

Circular ring model (open question from RESEARCH.md — resolve explicitly):
A circular 3-wallet ring: A refers B, B refers C, C tries to refer A — but A is already referred by... check whether the referral system is one-way (A can only have one referrer) or bidirectional.
(a) Determine: once A refers B (B has upline = A), can B then register A as B's own referrer? This would create a cycle.
(b) The REF_CODE_LOCKED mechanism: if B tries to use A's referral code after A already registered B (or vice versa), does the re-registration lock apply?
(c) Model the arithmetic: 3-wallet ring A→B→C→A (if achievable) — total rakeback flow across all three wallets. Compare to each wallet playing solo with VAULT as referral (0 rakeback). Net EV of ring vs. solo: is the ring positive-EV for participants collectively? (It cannot create ETH from nothing — can only redistribute rakeback from VAULT to participants, so the question is whether this is a meaningful extraction vector.)

Wash trading model:
A single actor controlling 2 wallets (A and B) where A refers B. B spends ETH → A receives 20% rakeback. Net: actor spends 1 ETH, receives 0.20 ETH back to A. Is this positive EV? No — actor net spends 0.80 ETH (but gets tickets, which have expected value from prize pool). This is equivalent to a 20% discount — not an "extraction" vector, just normal rakeback. Confirm there is no amplification path where wash trading multiplies rakeback beyond the stated percentages.

Document: whether any affiliate path produces positive EV above normal play (net ETH out > net ETH in from non-prize-pool sources).
  </action>
  <verify>Analysis complete when: self-referral block confirmed at line level, REF_CODE_LOCKED sentinel behavior documented, circular ring EV arithmetic stated, wash trading discount characterized.</verify>
  <done>TOKEN-07 verdict: PASS (no exploitable affiliate path beyond designed rakeback) or FINDING (specific ring/wash EV extraction above design intent). Evidence at DegenerusAffiliate.sol line granularity.</done>
</task>

<task type="auto">
  <name>Task 2: TOKEN-08 — DGNRS lockForLevel/unlock level-transition timing exploit analysis</name>
  <files>contracts/DegenerusStonk.sol, contracts/DegenerusGame.sol</files>
  <action>
Read DegenerusStonk.sol lockForLevel() (line 437) and unlock() (line 464) completely. For TOKEN-08:

Same-level unlock guard:
(a) Confirm `if (lockedLevel[msg.sender] == currentLevel) revert LockStillActive()` is present in unlock().
(b) This means a player CANNOT call unlock() + lockForLevel() within the same level to reset counters. Confirm.

Inter-level cap reset (intended mechanics):
(c) A player locks at level N, spends cap, waits for level N+1, unlock(), lockForLevel() at N+1 → fresh cap. This is intended. Confirm that _checkAndRecordEthSpend uses ethSpentThisLevel which is reset by unlock(). Net: player gets one cap per level per lock — intended design.

Level-transition timing window (RESEARCH.md open question — resolve explicitly):
(d) Read the auto-unlock branch in lockForLevel() lines 442-448. If a player locks at level N (spending some cap), and the game transitions to level N+1 during the same block (or next block), does calling lockForLevel() again in the same tx-window trigger auto-unlock of the level-N lock AND immediately apply a new level-N+1 lock? If yes:
    - Does this give the player both the level-N partial cap (already spent) AND a full level-N+1 fresh cap in rapid succession?
    - Is this a double-spend? Or is it just the normal inter-level sequence compressed to one transaction?
    - The critical question: are there any cap-bypass benefits from executing the level transition and lockForLevel() in the same block vs. separate blocks?
(e) Read _lockedClaimableValues() — does the level-N claimable amount get credited BEFORE the auto-unlock resets it? If so, is it possible to claim level-N claimable value AND a fresh level-N+1 cap in the same transaction?

If the auto-unlock in lockForLevel() allows same-block double-cap exploitation: MEDIUM finding. If the reset is clean and sequential: PASS.

Write 11-03-SUMMARY.md with TOKEN-07 and TOKEN-08 verdicts.
  </action>
  <verify>11-03-SUMMARY.md exists with TOKEN-07 and TOKEN-08 verdicts. TOKEN-08 section explicitly addresses the level-transition timing window and same-block lockForLevel auto-unlock path. Any findings include C4 severity.</verify>
  <done>TOKEN-08 verdict: PASS (no double-cap exploit, level transition is clean) or FINDING (specific same-block double-cap with extraction amount). Both verdicts written to SUMMARY.md with evidence citations.</done>
</task>

</tasks>

<verification>
- TOKEN-07: Self-referral block confirmed, REF_CODE_LOCKED sentinel behavior documented, circular ring and wash trading EV characterized (bounded by design, no extraction beyond rakeback)
- TOKEN-08: Same-level unlock guard confirmed (LockStillActive), auto-unlock branch in lockForLevel() traced, level-transition timing window explicitly resolved
- 11-03-SUMMARY.md written with both verdicts
</verification>

<success_criteria>
TOKEN-07 and TOKEN-08 verdicts delivered. Circular ring EV arithmetic explicitly stated. Level-transition timing window resolved with specific code path citation. Any finding includes Code4rena severity. SUMMARY.md committed.
</success_criteria>

<output>
After completion, create `.planning/phases/11-token-security-economic-attacks-vault-and-timing/11-03-SUMMARY.md` following the standard summary template.
</output>
