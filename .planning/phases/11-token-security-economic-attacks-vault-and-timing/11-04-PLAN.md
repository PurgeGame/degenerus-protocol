---
phase: 11-token-security-economic-attacks-vault-and-timing
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11-token-security-economic-attacks-vault-and-timing/11-04-SUMMARY.md
autonomous: true
requirements: [VAULT-01, VAULT-02]

must_haves:
  truths:
    - "VAULT-01 verdict confirmed: DegenerusVault receive() open ETH donation cannot manipulate share redemption formula to benefit one shareholder class over another — proportional dilution only"
    - "VAULT-02 verdict confirmed: DegenerusStonk _burnFor() floor division consistently favors protocol over claimer — no partial-burn sequence extracts disproportionate value"
  artifacts:
    - path: ".planning/phases/11-token-security-economic-attacks-vault-and-timing/11-04-SUMMARY.md"
      provides: "VAULT-01 and VAULT-02 verdicts with contract/line evidence and C4 severity for any findings"
      contains: "VAULT-01 VERDICT:"
  key_links:
    - from: "DegenerusVault.sol receive()"
      to: "address(this).balance in redemption formula"
      via: "ETH donation joins reserve proportionally"
      pattern: "receive\\(\\)|totalMoney"
    - from: "DegenerusStonk.sol _burnFor()"
      to: "totalValueOwed = (totalMoney * amount) / supplyBefore"
      via: "integer floor division favors protocol"
      pattern: "_burnFor|totalValueOwed|supplyBefore"
---

<objective>
Deliver verdicts for VAULT-01 (DegenerusVault ETH donation safety) and VAULT-02 (DegenerusStonk burn-to-claim rounding) via source-trace and arithmetic analysis.

Purpose: Confirm that vault mechanics are safe against donation-based manipulation and that the Stonk redemption formula cannot be gamed via partial burns or supply manipulation.
Output: 11-04-SUMMARY.md with two verdicts, evidence citations, and Code4rena severity for any findings.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-token-security-economic-attacks-vault-and-timing/11-RESEARCH.md

<!-- Prior phase context -->
@.planning/phases/08-eth-accounting-invariant-and-cei-verification/08-03-SUMMARY.md

<!-- Key interfaces extracted from codebase for executor -->
<interfaces>
From contracts/DegenerusVault.sol (VAULT-01):
- receive() external payable: line 461 — emits Deposit event only; no share minting, no state write
- Share redemption formula uses address(this).balance (ETH) + steth balance + claimableEth
- DegenerusVault shares are pre-minted at construction: 1T DGVE shares to CREATOR — no first-deposit attack surface
- DegenerusStonk.receive() is onlyGame — no open donation path to Stonk (only Vault is open)

From contracts/DegenerusStonk.sol (VAULT-02):
- _burnFor(address from, uint256 amount) lines 766-778:
    uint256 supplyBefore = totalSupply;
    uint256 ethBal = address(this).balance;
    uint256 stethBal = steth.balanceOf(address(this));
    uint256 claimableEth = _claimableWinnings();
    uint256 totalMoney = ethBal + stethBal + claimableEth;
    uint256 totalValueOwed = (totalMoney * amount) / supplyBefore;  // floor division
- totalSupply is read BEFORE burn (supplyBefore), ensuring partial burns use full denominator

From Phase 8 ACCT-06 PASS:
- DegenerusVault share rounding confirmed floor-safe (Phase 8 verdict)
- "No partial-burn extraction" confirmed in Phase 8
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: VAULT-01 — DegenerusVault receive() donation attack surface analysis</name>
  <files>contracts/DegenerusVault.sol</files>
  <action>
Read DegenerusVault.sol completely. For VAULT-01:

receive() behavior:
(a) Confirm receive() only emits Deposit event — no share minting, no state update beyond the implicit balance increase.
(b) Identify the redemption formula used in _burnCoinFor() and _burnEthFor() (or their equivalents). Confirm that `address(this).balance` (or `totalMoney` equivalent) is used live in the formula, not a cached stored value. This matters: if the formula uses live balance, a donation increases the redemption value proportionally for ALL existing shareholders — no single shareholder class benefits more than another.

ERC4626 inflation attack analogy (RESEARCH.md Pitfall 5):
(c) Confirm that shares are pre-minted at construction (1T DGVE to CREATOR) — no first-deposit attack surface. The inflation attack works only when the first depositor mints shares against a non-zero reserve. Verify when (at construction or at first deposit) shares are minted and in what quantity.
(d) Confirm: is there any path where a donation via receive() is front-run against a pending share minting to inflate the per-share price before the mint? Since shares are pre-minted, this path should be closed.
(e) Determine: can a minority shareholder send ETH to vault via receive() to dilute majority shareholder's claim? (No — donation increases all shareholders' claims proportionally; a minority shareholder dilutes themselves proportionally. Net EV of donation is negative for the donor if they have < 100% of shares.)

Assess: any scenario where vault receive() creates an extractable advantage. If none: PASS.
  </action>
  <verify>Analysis complete when: receive() behavior confirmed (event-only), shares pre-minted at construction confirmed, redemption formula uses live balance confirmed, ERC4626 inflation vector ruled out with reasoning.</verify>
  <done>VAULT-01 verdict: PASS (donation proportional, no extraction path) or FINDING (specific attack sequence with extraction amount). Evidence at DegenerusVault.sol line granularity.</done>
</task>

<task type="auto">
  <name>Task 2: VAULT-02 — DegenerusStonk burn-to-claim rounding and partial burn analysis</name>
  <files>contracts/DegenerusStonk.sol</files>
  <action>
Read DegenerusStonk.sol completely. For VAULT-02:

Burn formula verification:
(a) Read _burnFor() (lines 766-778 area). Confirm:
    - supplyBefore = totalSupply reads BEFORE any burn
    - totalValueOwed = (totalMoney * amount) / supplyBefore uses integer floor division
    - Floor division means claimer receives slightly LESS than pro-rata — dust stays in contract
    - This is protocol-favorable: residual dust benefits remaining shareholders

Partial burn analysis:
(b) Can a sequence of partial burns extract more than one full burn? Example:
    - totalMoney = 1000 ETH, totalSupply = 1000 shares
    - Full burn of 1000 shares: totalValueOwed = (1000 * 1000) / 1000 = 1000 ETH (exact, no dust)
    - Two partial burns of 500 each:
        Burn 1: totalValueOwed = (1000 * 500) / 1000 = 500 ETH; remaining: 500 ETH, 500 shares
        Burn 2: totalValueOwed = (500 * 500) / 500 = 500 ETH; total: 1000 ETH
    - Result: same as full burn in this example. Check with non-round numbers:
        totalMoney = 1001, totalSupply = 1000
        Full burn: 1001 * 1000 / 1000 = 1001 ETH
        Two partial (500 each):
            Burn 1: 1001 * 500 / 1000 = 500 (floor) — claimer gets 500, 501 remaining
            Burn 2: 501 * 500 / 500 = 501 — claimer gets 501
            Total: 1001 ETH (same as full burn in this case)
    - Generalize: can floor division in partial burns ever SUM to MORE than a single full burn? Prove direction.
(c) Supply manipulation: can a user manipulate totalSupply (e.g., by burning a tiny amount to reduce denominator) before executing a larger burn? Note: each burn reduces supplyBefore atomically — the only manipulation is within a single transaction. Since supplyBefore is read at start of _burnFor(), a second burn in same tx reads the updated supplyBefore. No manipulation path via single-tx multi-burn.
(d) Confirm receive() of DegenerusStonk is onlyGame (not open) — external donation to Stonk is blocked.

Write 11-04-SUMMARY.md with VAULT-01 and VAULT-02 verdicts.
  </action>
  <verify>11-04-SUMMARY.md exists with VAULT-01 and VAULT-02 verdicts. VAULT-02 section includes partial burn arithmetic. Any findings include C4 severity and extraction path.</verify>
  <done>VAULT-02 verdict: PASS (floor division protocol-favorable, no partial-burn extraction) or FINDING (specific partial-burn sequence with disproportionate extraction). Both verdicts written to SUMMARY.md.</done>
</task>

</tasks>

<verification>
- VAULT-01: receive() event-only confirmed, shares pre-minted at construction confirmed, no ERC4626 inflation attack surface, donation is proportional to all shareholders
- VAULT-02: floor division confirmed protocol-favorable, partial burn arithmetic verified (no extraction), supply manipulation path ruled out
- 11-04-SUMMARY.md written with both verdicts
</verification>

<success_criteria>
VAULT-01 and VAULT-02 verdicts delivered. Partial burn arithmetic explicitly computed (even with non-round numbers). ERC4626 inflation attack ruled out with construction-time share minting confirmation. Any finding includes Code4rena severity. SUMMARY.md committed.
</success_criteria>

<output>
After completion, create `.planning/phases/11-token-security-economic-attacks-vault-and-timing/11-04-SUMMARY.md` following the standard summary template.
</output>
