---
phase: 11-token-security-economic-attacks-vault-and-timing
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11-token-security-economic-attacks-vault-and-timing/11-02-SUMMARY.md
autonomous: true
requirements: [TOKEN-04, TOKEN-05, TOKEN-06]

must_haves:
  truths:
    - "TOKEN-04 verdict confirmed: no whale + lootbox ticket combination produces EV > 1.0 for any player at any activity score — LOOTBOX_EV_BENEFIT_CAP=10 ETH cap is enforced in all code paths"
    - "TOKEN-05 verdict confirmed: no positive-return activity score inflation path exists — minimum ETH cost to reach max EV benefit exceeds maximum extractable surplus"
    - "TOKEN-06 verdict confirmed: BURNIE 30-day guard applies at all COIN purchase entry points including operator-proxied calls — whale/lazy/deity pass paths are confirmed ETH-ticket-only and exempt by design"
  artifacts:
    - path: ".planning/phases/11-token-security-economic-attacks-vault-and-timing/11-02-SUMMARY.md"
      provides: "TOKEN-04, TOKEN-05, TOKEN-06 verdicts with contract/line evidence and C4 severity for any findings"
      contains: "TOKEN-04 VERDICT:"
  key_links:
    - from: "DegenerusGameLootboxModule.sol _lootboxEvMultiplierFromScore()"
      to: "_applyEvMultiplierWithCap() LOOTBOX_EV_BENEFIT_CAP"
      via: "remainingCap = max(0, 10 ETH - usedBenefit)"
      pattern: "LOOTBOX_EV_BENEFIT_CAP"
    - from: "DegenerusGameDegeneretteModule.sol _playerActivityScoreInternal()"
      to: "activity score components and cost floor"
      via: "ACTIVITY_SCORE constants"
      pattern: "_playerActivityScoreInternal"
    - from: "DegenerusGameMintModule.sol purchaseCoin()"
      to: "COIN_PURCHASE_CUTOFF guard"
      via: "ticketQuantity != 0 branch in _processCoinPurchase()"
      pattern: "COIN_PURCHASE_CUTOFF|CoinPurchaseCutoff"
---

<objective>
Deliver verdicts for TOKEN-04 (whale + lootbox combined EV model), TOKEN-05 (activity score inflation cost), and TOKEN-06 (BURNIE 30-day guard completeness) via source-trace and arithmetic analysis.

Purpose: Confirm that no combination of whale activity, lootbox mechanics, and BURNIE purchases allows a player to extract positive expected value from the protocol's fixed-sum prize pool.
Output: 11-02-SUMMARY.md with three verdicts, evidence citations, and Code4rena severity for any findings.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-token-security-economic-attacks-vault-and-timing/11-RESEARCH.md

<!-- Key interfaces extracted from codebase for executor -->
<interfaces>
From contracts/modules/DegenerusGameLootboxModule.sol (TOKEN-04):
- _lootboxEvMultiplierFromScore(score):
    score <= 6000 bps: linear 8000-10000 bps (80-100% EV)
    score 6000-25500 bps: linear 10000-13500 bps (100-135% EV)
    score > 25500 bps: 13500 bps (135% max)
- _applyEvMultiplierWithCap(player, lvl, amount, evMultiplierBps):
    usedBenefit = lootboxEvBenefitUsedByLevel[player][lvl]
    remainingCap = max(0, LOOTBOX_EV_BENEFIT_CAP - usedBenefit)  // LOOTBOX_EV_BENEFIT_CAP = 10 ETH
    adjustedPortion = min(amount, remainingCap)
    neutralPortion = amount - adjustedPortion
    // Only adjustedPortion receives multiplier; rest receives 1x

From contracts/modules/DegenerusGameDegeneretteModule.sol (TOKEN-05):
- _playerActivityScoreInternal(): computes composite activity score from:
    degenerette bets, lootbox purchases, BURNIE ticket purchases, whale bundle purchases
- Score inputs have associated ETH/BURNIE costs that form the cost floor for score inflation

From contracts/modules/DegenerusGameMintModule.sol (TOKEN-06):
- COIN_PURCHASE_CUTOFF = 335 days (line 116)
- COIN_PURCHASE_CUTOFF_LVL0 = 882 days
- Guard location: purchaseCoin entry → _processCoinPurchase() → ticketQuantity != 0 branch
  `if (level == 0 ? elapsed > COIN_PURCHASE_CUTOFF_LVL0 : elapsed > COIN_PURCHASE_CUTOFF) revert CoinPurchaseCutoff()`
- Whale bundle (purchaseWhaleBundle), lazy pass (purchaseLazyPass), deity pass (purchaseDeityPass):
  these purchase ETH tickets only — not BURNIE tickets; they do NOT call _processCoinPurchase()

From MEMORY.md:
- Whale bundle: 2.4 ETH (levels 0-3), 4 ETH (x49/x99), qty 1-100
- Lazy pass: 0.24 ETH flat (levels 0-2), sum-of-10-level-prices (level 3+)
- Deity pass: 24 + T(n) ETH where T(n) = n*(n+1)/2, n = passes sold
- COIN ticket cost: costWei = (priceWei * ticketQuantity) / 400; 1 full ticket = qty 400 = priceWei
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TOKEN-04 + TOKEN-05 — Lootbox EV cap model and activity score inflation cost analysis</name>
  <files>contracts/modules/DegenerusGameLootboxModule.sol, contracts/modules/DegenerusGameDegeneretteModule.sol</files>
  <action>
TOKEN-04 — Whale + lootbox combined EV model:
Read DegenerusGameLootboxModule.sol completely. For _applyEvMultiplierWithCap():
(a) Confirm LOOTBOX_EV_BENEFIT_CAP constant value (expected: 10 ETH in wei).
(b) Confirm lootboxEvBenefitUsedByLevel[player][lvl] is updated on every lootbox redemption to track cumulative benefit received.
(c) Compute maximum possible EV surplus per player per level: (1.35 - 1.0) * 10 ETH = 3.5 ETH above neutral. This is the absolute cap — confirm no code path bypasses the cap by resetting lootboxEvBenefitUsedByLevel or applying the multiplier without the cap check.
(d) Check: can a whale buy multiple lootbox types (BURNIE lootbox, ETH lootbox, deity lootbox if applicable) and stack caps across types? Confirm whether lootboxEvBenefitUsedByLevel is per-player-per-level-per-type or a single cap covering all lootbox types.
(e) Determine: is there any ticket-level combination (whale bundle + max activity + max lootbox) where EV > 1.0 after accounting for purchase costs? The lootbox benefit is bounded at 3.5 ETH above purchase cost, while whale bundles cost 2.4–4 ETH. Model whether the 3.5 ETH benefit can exceed total purchase costs.

TOKEN-05 — Activity score inflation cost:
Read _playerActivityScoreInternal() completely. Enumerate all activity score components and their ETH/BURNIE costs. For each component:
(a) Identify the minimum cost to reach the maximum activity score tier (25500 bps).
(b) Compute the maximum EV benefit achievable at max score: 3.5 ETH above neutral (from TOKEN-04 cap).
(c) Compare minimum inflation cost vs. maximum benefit: if cost > benefit, no positive-return path exists.
(d) Check for any activity score component that is free or near-zero cost (e.g., quest completions, affiliate bonus) that could inflate score without proportional spending.
  </action>
  <verify>Analysis complete when: LOOTBOX_EV_BENEFIT_CAP confirmed at source, lootboxEvBenefitUsedByLevel update path confirmed, max surplus (3.5 ETH) stated, activity score cost floor computed and compared to max benefit.</verify>
  <done>TOKEN-04 verdict: PASS (no EV > 1.0 path) or FINDING (specific bypass with extraction amount). TOKEN-05 verdict: PASS (inflation cost > benefit) or FINDING (specific inflation path with positive return).</done>
</task>

<task type="auto">
  <name>Task 2: TOKEN-06 — BURNIE 30-day guard completeness audit across all purchase entry points</name>
  <files>contracts/modules/DegenerusGameMintModule.sol, contracts/DegenerusGame.sol</files>
  <action>
Read DegenerusGameMintModule.sol. Confirm the BURNIE purchase cutoff guard:
(a) Locate COIN_PURCHASE_CUTOFF (335 days) and COIN_PURCHASE_CUTOFF_LVL0 (882 days) constants.
(b) Confirm the guard fires at `ticketQuantity != 0` in _processCoinPurchase() with the exact timestamp comparison shown in RESEARCH.md.
(c) Confirm this is the only entry point for BURNIE ticket purchases — that purchaseWhaleBundle, purchaseLazyPass, and purchaseDeityPass do NOT call _processCoinPurchase() and use ETH tickets only.

Operator-proxied COIN purchase path (open question from RESEARCH.md):
Read DegenerusGame.sol to locate isOperatorApproved usage and any operator-delegation path. Trace: when an operator-approved proxy calls purchaseCoin on behalf of a player, does the call still route through MintModule._processCoinPurchase()? Confirm:
- The operator approval mechanism (if present) does not bypass the module entry point
- The guard fires in all cases where ticketQuantity != 0, regardless of msg.sender being the player or an approved operator

If an operator-proxied path bypasses the guard (calls _queueCoinTickets directly without the timestamp check): MEDIUM finding — operator-enabled guard bypass.
If all paths route through _processCoinPurchase(): PASS.

Write 11-02-SUMMARY.md with TOKEN-04, TOKEN-05, and TOKEN-06 verdicts.
  </action>
  <verify>11-02-SUMMARY.md exists with all three verdicts. TOKEN-06 section explicitly addresses the operator-proxied path. Any findings include C4 severity and specific line references.</verify>
  <done>TOKEN-06 verdict: PASS (guard applies to all COIN purchase paths) or FINDING (specific bypass path). All three TOKEN-04/05/06 verdicts written to SUMMARY.md with evidence citations.</done>
</task>

</tasks>

<verification>
- TOKEN-04: LOOTBOX_EV_BENEFIT_CAP confirmed, lootboxEvBenefitUsedByLevel update path confirmed, max 3.5 ETH surplus per player per level, no bypass
- TOKEN-05: Activity score components enumerated, cost floor >= EV benefit ceiling confirmed
- TOKEN-06: Guard at purchaseCoin entry confirmed, operator-proxied path traced, whale/lazy/deity pass exemption confirmed as ETH-ticket-only by design
- 11-02-SUMMARY.md written with all verdicts
</verification>

<success_criteria>
TOKEN-04, TOKEN-05, TOKEN-06 verdicts delivered. EV model arithmetic explicitly stated (max surplus = 3.5 ETH above neutral, bounded). Operator-proxied path explicitly resolved. Any finding includes Code4rena severity and specific extraction path. SUMMARY.md committed.
</success_criteria>

<output>
After completion, create `.planning/phases/11-token-security-economic-attacks-vault-and-timing/11-02-SUMMARY.md` following the standard summary template.
</output>
