---
phase: 11-token-security-economic-attacks-vault-and-timing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11-token-security-economic-attacks-vault-and-timing/11-01-SUMMARY.md
autonomous: true
requirements: [TOKEN-01, TOKEN-02, TOKEN-03]

must_haves:
  truths:
    - "TOKEN-01 verdict confirmed: no path to unbounded COIN minting via vaultMintAllowance — every vaultAllowance += site is traced to an authorized caller with corresponding value source"
    - "TOKEN-02 verdict confirmed: claimWhalePass is replay-safe and re-entry-safe — whalePassClaims[player] = 0 is confirmed before any state effects"
    - "TOKEN-03 verdict confirmed: BurnieCoinflip entropy is VRF-derived in all code paths including the historical fallback — no block-level data (timestamp, blockhash, prevrandao) used as entropy source"
  artifacts:
    - path: ".planning/phases/11-token-security-economic-attacks-vault-and-timing/11-01-SUMMARY.md"
      provides: "TOKEN-01, TOKEN-02, TOKEN-03 verdicts with contract/line evidence and C4 severity for any findings"
      contains: "TOKEN-01 VERDICT:"
  key_links:
    - from: "BurnieCoin.sol vaultEscrow()"
      to: "vaultAllowance += authorization gate"
      via: "sender == GAME || sender == VAULT check"
      pattern: "OnlyVault"
    - from: "DegenerusGameEndgameModule.sol claimWhalePass()"
      to: "whalePassClaims[player] = 0"
      via: "zero-check-and-clear CEI pattern"
      pattern: "whalePassClaims\\[player\\] = 0"
    - from: "DegenerusGameAdvanceModule.sol rngGate()"
      to: "BurnieCoinflip.processCoinflipPayouts()"
      via: "rngWordCurrent (VRF-derived)"
      pattern: "rngWordCurrent|processCoinflipPayouts"
---

<objective>
Deliver verdicts for TOKEN-01 (vaultMintAllowance bypass), TOKEN-02 (claimWhalePass double-mint), and TOKEN-03 (BurnieCoinflip entropy source) via source-trace analysis.

Purpose: Establish that COIN mint authorization is airtight, whale pass claims are replay-safe, and coinflip outcomes are VRF-sourced — all three are foundational to the reentrancy matrix in Phase 12.
Output: 11-01-SUMMARY.md with three verdicts, evidence citations, and Code4rena severity for any findings.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-token-security-economic-attacks-vault-and-timing/11-RESEARCH.md

<!-- Key interfaces extracted from codebase for executor -->
<interfaces>
From contracts/BurnieCoin.sol (TOKEN-01):
- vaultEscrow(uint256): only GAME or VAULT can call; increments vaultAllowance — line 677
- vaultMintTo(address, uint256): onlyVault (VAULT only); decrements vaultAllowance, mints to recipient — line 694
- onlyVault modifier: checks msg.sender == ContractAddresses.VAULT — line 654
- _supply struct: packed {totalSupply, vaultAllowance, ...}; vaultAllowance is the authorization ledger

From contracts/modules/DegenerusGameEndgameModule.sol (TOKEN-02):
- claimWhalePass(address player): lines 493-509
  - halfPasses = whalePassClaims[player]; if 0 → return immediately
  - whalePassClaims[player] = 0 BEFORE _applyWhalePassStats and _queueTicketRange
  - _queueTicketRange writes to ticket storage only; no ETH external call

From contracts/modules/DegenerusGameAdvanceModule.sol + BurnieCoinflip.sol (TOKEN-03):
- rngWordCurrent set by rawFulfillRandomWords(requestId, randomWords): randomWords[0]
- rngGate(): currentWord = _applyDailyRng(day, rngWordCurrent) → coinflip.processCoinflipPayouts(bonusFlip, currentWord, day)
- _getHistoricalRngFallback(): searches rngWordByDay[] for earliest non-zero VRF word, XORs with keccak256(word, currentDay) — still VRF-derived
- BurnieCoinflip.processCoinflipPayouts(): bool win = (rngWord & 1) == 1 — uses passed-in rngWord, not block.timestamp/blockhash
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TOKEN-01 — Trace all vaultAllowance increase sites and confirm authorization gates</name>
  <files>contracts/BurnieCoin.sol</files>
  <action>
Read BurnieCoin.sol in full. Enumerate every site where `vaultAllowance +=` or `_supply.vaultAllowance` is incremented. For each site confirm:
(a) the caller is either GAME or VAULT (not an arbitrary address),
(b) the corresponding value source — GAME credits come from real ETH purchase funds, VAULT receives BURNIE sent to it,
(c) the invariant `totalSupply + vaultAllowance = supplyIncUncirculated` holds across all paths.

Cross-reference the RESEARCH.md Pattern 1 (two-party authorization) and Pitfall 1 (confusion about vaultEscrow being a free-mint).

Check for any path that increments vaultAllowance WITHOUT a corresponding ETH/BURNIE consumption — this would be the bypass. If found, severity-rate as HIGH. If not found, verdict is PASS.

Also confirm: sending BURNIE to VAULT via _transfer() reclassifies totalSupply → vaultAllowance (not a new mint), and that this path cannot be exploited to create net-new supply.

Document: exact line references for every vaultAllowance site, the authorization check at each, and the corresponding value source.
  </action>
  <verify>Analysis complete when every `vaultAllowance +=` line is enumerated with caller constraints and value source documented. No unguarded free-mint path exists OR a specific bypass path is named with exact function and line.</verify>
  <done>TOKEN-01 verdict: PASS (no bypass) or FINDING (specific line and attacker sequence). Evidence citations at BurnieCoin.sol line granularity.</done>
</task>

<task type="auto">
  <name>Task 2: TOKEN-02 + TOKEN-03 — claimWhalePass CEI confirmation and coinflip entropy source audit</name>
  <files>contracts/modules/DegenerusGameEndgameModule.sol, contracts/modules/DegenerusGameAdvanceModule.sol, contracts/BurnieCoinflip.sol</files>
  <action>
TOKEN-02 — claimWhalePass double-mint check:
Read DegenerusGameEndgameModule.sol lines 493-509. Confirm:
(a) whalePassClaims[player] = 0 occurs BEFORE any state effects (_applyWhalePassStats, _queueTicketRange),
(b) there is no ETH external call in this function path (no re-entry vector),
(c) a second call with same player hits `halfPasses == 0` and returns immediately — no double-claim possible.
Check if there is any other function that credits whalePassClaims[player] without a corresponding clear, which would allow refilling after the clear.

TOKEN-03 — BurnieCoinflip entropy source:
Read the full entropy chain:
1. rawFulfillRandomWords sets rngWordCurrent = randomWords[0] (VRF output)
2. rngGate() in AdvanceModule: confirm rngWordCurrent flows through _applyDailyRng to become currentWord, then to coinflip.processCoinflipPayouts(bonusFlip, currentWord, day)
3. BurnieCoinflip.processCoinflipPayouts(): confirm `bool win = (rngWord & 1) == 1` uses the passed-in rngWord (VRF-derived), and that no block-level data (block.timestamp, blockhash, prevrandao) is used anywhere in the win/loss calculation
4. Read _getHistoricalRngFallback() — confirm it sources from rngWordByDay[] (historical VRF words), not blockhash. This is the game-over entropy path. Confirm it XORs with keccak256(abi.encodePacked(word, currentDay)) — deterministic from VRF history.

If any entropy source uses block.timestamp, blockhash, or prevrandao as a coinflip input: HIGH finding with specific line. Otherwise: PASS.

Write 11-01-SUMMARY.md with all three verdicts.
  </action>
  <verify>11-01-SUMMARY.md exists with TOKEN-01, TOKEN-02, TOKEN-03 verdicts, each with contract/line citations. Any findings include C4 severity and attacker sequence.</verify>
  <done>TOKEN-02 verdict: PASS (CEI confirmed, no re-entry path) or FINDING. TOKEN-03 verdict: PASS (VRF-only entropy) or HIGH FINDING (block-level data found). All three verdicts written to SUMMARY.md.</done>
</task>

</tasks>

<verification>
- TOKEN-01: Every vaultAllowance increment site enumerated and guarded — no free-mint path
- TOKEN-02: claimWhalePass state-clear before effects confirmed — zero-check-and-clear pattern safe
- TOKEN-03: processCoinflipPayouts uses only VRF-derived rngWord in all paths including historical fallback
- 11-01-SUMMARY.md written with verdicts, line evidence, and C4 severity for any findings
</verification>

<success_criteria>
All three verdicts (TOKEN-01, TOKEN-02, TOKEN-03) delivered with contract-level evidence. No requirement left at "pending". Any finding includes Code4rena severity, impact description, and attacker sequence. SUMMARY.md committed.
</success_criteria>

<output>
After completion, create `.planning/phases/11-token-security-economic-attacks-vault-and-timing/11-01-SUMMARY.md` following the standard summary template.
</output>
