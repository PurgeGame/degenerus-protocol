# Phase 400 — SOLVENCY · CARRY · REDEMPTION: dual-net findings (v64.0)

**Status:** ✅ COMPLETE — both nets on record, adjudicated vs frozen source.
**Requirements:** SOLV-01..05 · **Subject frozen** `402855e1` (contracts tree at HEAD `39b2f344`; working tree == frozen, verified clean).
**Threat-model weight:** solvency is the protocol SPINE — highest-priority invariant class.
**Nets:**
- **NET-1 cross-model council** — `gemini` (CLEAN, all 5 invariants hold) + `codex` (1 LOW on SOLV-05 first-claim window). Both on record (`council/solv.{gemini,codex}.txt`, 0 skipped).
- **NET-2 Claude Workflow** — 5 dimension finders + per-lead refute + completeness critic (`wm1506k5l`, 6 agents): **0 leads across all 5 dimensions + 5 cross-path compositions verified clean**.

## Outcome: 0 CATASTROPHE / 0 HIGH / 0 MED / 0 LOW. 1 INFO (SOLV-05, by-design window + VAULT operational note). SOLV-01..04 convergent CLEAN across all three nets.

| # | Site | Disposition | Detail |
|---|------|-------------|--------|
| SOLV-05-A | First-claim coinflip window strands wins older than 180d · `BurnieCoinflip.sol:151,484,493,1123` | **INFO — BY-DESIGN window + VAULT operational note (no contract change)** | **codex (council) LOW; gemini + NET-2 clean.** Mechanically correct: for a *first-time* claimant (`start==0`), `minClaimableDay = latest − COIN_CLAIM_FIRST_DAYS(180)` (`:493`) clamps the lookback, so a first claim made when `latest > 180` skips days before `latest−180`. **Adjudicated to INFO by the decisive NET-2 insight: a forgone out-of-window win is NOT stranded value** — BURNIE is *burned at deposit* (`burnForCoinflip`) and only *minted at in-window claim* (`mintForGame`), so an unclaimed old win simply leaves total supply lower (deflationary un-mint), never a locked/stuck asset or solvency liability. The redemption-backing entity **sDGNRS auto-settles unconditionally every resolution** (`:941-957`) so its `lastClaim` never lags `latest` — its backing can never strand. The window only ever bites a genuine first-time **human** or the **VAULT** (the one non-auto-settled holder). **The 30→180 change strictly REDUCED forfeiture (6× more time), never increased it.** Operational note for the vault owner: claim the seed coinflip wins (days 1-20) within 180d to realize the intended seed emission — lazily auto-claimed on any vault payout that taps claimable coinflips (`DegenerusVault.sol:739-760`) or directly via `coinClaimCoinflips` (`:665`). |

## Verification map — every invariant CONFIRMED (≥2 nets; SOLV-01..04 all three)

- **SOLV-01 — claimablePool identity** (`claimablePool == Σ claimable + Σ afking`): HOLDS on every changed path. The sDGNRS/vault **salvage ETH leg is a total-preserving buyer→seller relabel** (`MintModule:1307-1309` `_debitSalvageEth` + `_creditClaimable` — neither touches `claimablePool`; both halves already ride inside the pool); the **dust-drop forfeit** self-credits sDGNRS via `creditRedemptionDirect{value}` paired with `claimablePool += forfeitEth` backed by msg.value+stETH (`Lootbox:1013-1014`, reconciles with the `_pendingRedemptionEthValue` release `SDGNRS:868`); the **payable-delegatecall redemption** credits `claimablePool += amount` in tandem with arriving ETH+stETH, no double-count vs the submit-time `pullRedemptionReserve` debit. All free-ETH reservation sites reserve `claimablePool` (SOLVENCY-01 holds); keeper/box bounties pay **only** minted BURNIE flip-credit (no ETH keeper liability to reserve). ✅ all 3 nets.
- **SOLV-02 — BURNIE-04 carry-escrow** (`98c4f049` + win-multiplier `891f7a8f`): the escrowed sDGNRS auto-rebuy carry is removed from redemption backing at submit (`withdrawRedeemedBurnie` held→claimable→carry, `BurnieCoinflip:1025-1055`); paid OR forfeited exactly once at claim contingent on the D+1 flip; the slot is `delete`d before any credit (`SDGNRS:868`) → no double-pay; the day+1 read is ABSOLUTE/fail-closed. ✅ all 3 nets.
- **SOLV-03 — salvage carry + vault fallback** (`a8fa3afa`, byte-identical at frozen tree): the BURNIE leg taps the buyer's `autoRebuyCarry` (symmetric with redemption, capped at spendable held+claimable+carry — no mint-from-nothing); the vault-owner fallback is bounded by the owner toggle + `claimable + afking >= totalBudget + vaultFloorWei` (`MintModule:1338-1353`, `DegenerusVault:633`) — never drains the vault below its ETH floor, no underflow. ✅ all 3 nets.
- **SOLV-04 — payable redemption + dust forfeit + CEI**: live legs fully fund or revert atomically; unfunded claims revert (`resolveRedemptionLootbox` validates `msg.value <= amount`, pulls the stETH delta); **stETH-before-ETH CEI holds** in `_payoutWithStethFallback` (`Game:1888-1916`) and `SDGNRS._payEth` (`:1152-1169`) — `claimablePool` debited before the untrusted ETH `.call`, so a reentrant `advanceGame`→yield-surplus cannot read in-flight stETH as backing (V62-03/`53cd25cf` class intact); the two redemption payables make **no untrusted `.call`** before returning (no reentrancy window); nested boon dispatches are `payable` (callvalue guards preserved); the dust-forfeit runs only in the live branch and never injects value after the gameOver drain snapshot. ✅ all 3 nets.
- **SOLV-05 — keeper bounty**: not a faucet at real gas — the advance bounty peg-value caps at ~0.01062 ETH (`mult=6`, 2-hour stall) and is paid as **illiquid BURNIE flip-credit** (must survive a 50/50 flip, no biggest-flip compounding); a multi-million-gas advance costs ≥0.003–0.5 ETH at 5–50 gwei ⇒ net-negative-or-neutral. The DGNRS-record bounty is a 0.2% redistribution from the pre-funded Reward pool, not a free-ETH drain. ✅ (do NOT use the 0.5-gwei peg as the gas reference). ✅ all 3 nets.

## Cross-path compositions (NET-2 critic, all clean)

(1) salvage carry vs redemption carry-escrow on the same sDGNRS buyer — both drain LIVE storage sequentially (no stale-snapshot double-spend); the escrow slice is *destroyed* at submit, not a standing liability. (2) vault-owner fallback ETH leg vs `distributeYieldSurplus` — the salvage relabel is pool-neutral and the afking ETH physically stays in the Game, so obligations are unchanged and vault ETH is never misread as surplus. (3) dust-forfeit vs gameOver drain — forfeit runs only in `!isGameOver`; post-latch sDGNRS claims pay 100% from sDGNRS's own balance, never the Game payables. (4) reentrancy across the two redemption payables — no untrusted `.call` anywhere in the lootbox resolution chain. (5) 180-day window vs day-20 rebuy latch — sDGNRS settles every advance; the latch is BURNIE-side only, no ETH/stETH interaction.

## Verdict

The solvency SPINE is **clean across the carry-escrow fix, salvage, every redemption path, and the CEI ordering** — SOLV-01..04 convergent across all three nets, with five cross-path compositions independently verified. The lone lead (codex's SOLV-05 first-claim window) adjudicates to **INFO/by-design**: a forgone out-of-window coinflip win is a deflationary un-mint, not stranded value; sDGNRS (the backing entity) auto-settles so its backing never strands; the 30→180 change strictly improved forfeiture; only a vault operational note remains (claim seed wins within 180d). **Both nets on record; SOLV-01..05 attested. No contract change warranted.**
