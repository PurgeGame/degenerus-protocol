# Solvency · Carry · Redemption Correctness Review — Degenerus Protocol (cross-model council, NET-1)

Review the solvency-bearing accounting across the changed carry / salvage / redemption / coinflip-claim paths for value conservation, identity preservation, and CEI ordering. Frozen subject: the `contracts/` tree at this checkout (read-only; do not modify any file). Verify the code against the stated invariants below; report any divergence with `file:line`, the invariant vs the code, and the concrete effect. A clean result is a valid outcome.

## The load-bearing invariants to verify

- **SOLV-01 — claimablePool identity.** `claimablePool == Σ player claimable + Σ player afking` must hold across EVERY changed credit/debit path. Specifically re-derive it across: the sDGNRS salvage-swap legs (ETH/BURNIE pawn-shop payout), the dust-drop forfeit self-credit, and the payable-delegatecall redemption ETH leg. Every credit to a player must be matched by an equal `claimablePool` increment (and debits symmetric); no path may credit a player without backing the pool, or decrement the pool without paying.
- **SOLV-02 — BURNIE-04 carry-escrow fix** (`98c4f049`: submit-time carry escrow, flip-contingent D+1 payout, `CoinflipClaimState` event). Verify the escrowed auto-rebuy carry is (a) removed from sDGNRS redemption backing at submit, (b) paid or forfeited exactly once at claim contingent on the D+1 flip, (c) never double-counted or over-credited. Trace `withdrawRedeemedBurnie` → held → claimable → carry, and the slot delete.
- **SOLV-03 — salvage carry + vault-owner fallback** (`a8fa3afa`). The far-future salvage BURNIE leg taps the buyer's auto-rebuy carry (symmetric with redemption: buyer-keyed sDGNRS + vault), plus a vault-owner salvage-buyer fallback (toggle + ETH floor; ETH sourced from vault claimable + afking, stage reserves via `depositAfkingFunding`). Verify value conservation on both legs and that the toggle + ETH-floor bound the vault leg (no unbounded vault drain, no value minted from nothing).
- **SOLV-04 — permissionless / payable redemption + dust forfeit + CEI** (`403afc62` payable-chain fix, `4547b387`, `78b858ed`). Verify: every live (funded) claim pays out and reverts cleanly when unfunded; the **stETH-before-ETH CEI ordering** holds (the V62-03 yield-surplus reentrancy class — no in-flight stETH counted as backing by a reentrant `advanceGame`→yield-surplus); the dust-drop forfeit self-credits correctly; and no path strands value or double-credits across the gameover drain snapshot. Nested boon dispatches on the payable chain must preserve the callvalue guards.
- **SOLV-05 — coinflip claim windows + keeper bounty** (first-claim window 30→180 days; calibrated keeper bounty). Verify the window change strands no seed/winning value (a late claimant is still paid), and the keeper bounty is net-negative-or-neutral vs REAL prevailing gas (5–50+ gwei) and flip-credit illiquidity — not a faucet (do NOT use the 0.5-gwei AUTO_GAS_PRICE_REF peg as the gas reference).

## Focus questions (highest value)

1. **Identity break:** is there any changed path that credits a player (claimable or afking) without an equal `claimablePool` increment, or pays from the pool without crediting a player — i.e. a solvency drift? Re-derive the five reservation sites if relevant.
2. **Double-pay / strand:** can the carry-escrow or salvage carry be paid twice, or escrowed-then-stranded (removed from backing but never paid nor returned)? Can a coinflip win or seed be stranded by the window change?
3. **CEI / reentrancy:** does any ETH/stETH payout precede the state update it depends on, such that a reentrant `advanceGame` / yield-surplus / nested-boon path observes stale backing? Re-check the `_payoutWithStethFallback` ordering.
4. **Vault leg bound:** is the vault-owner salvage fallback strictly bounded by the toggle + ETH floor + available vault claimable/afking — no unbounded drain, no negative-balance underflow?
5. **Keeper faucet:** at real gas, does any keeper/permissionless bounty (redemption, coinflip, salvage) pay more than it costs to call?

PRIOR DISPOSITIONS (carried — do NOT re-litigate, but flag if the NEW delta breaks them): SOLVENCY-01 (all free-ETH reservation sites reserve `claimablePool` inclusive of keeper total) held through v63; V62-03 sDGNRS redemption reentrancy was FIXED (CEI reorder, `53cd25cf` yield-surplus class); the BURNIE-04 carry-stranding was the one v63 CONFIRMED MED, fixed `98c4f049`; the redemption-payable HIGH (every funded claim reverted) was fixed `403afc62`. These are re-verify targets, not open findings.

Report each invariant with `file:line` + a verdict (holds / diverges), and list any divergence as a finding with the concrete solvency effect.
