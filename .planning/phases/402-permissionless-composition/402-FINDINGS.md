# Phase 402 — PERMISSIONLESS-COMPOSITION: dual-net findings (v64.0)

**Status:** ✅ COMPLETE — both nets on record (codex deferred), adjudicated vs frozen source.
**Requirements:** PERM-01..04 · **Subject frozen** `402855e1` (working tree == frozen, verified clean).
**Nets:**
- **NET-1 council** — `gemini` (PERM-01/02/03 HOLDS; PERM-04 flagged the `MintStreakRecorded` front-load gap). **`codex` DEFERRED** — usage cap until ~02:33 (backfill: `phases/CODEX-BACKFILL-TODO.md`). **PERM-04 + PERM-CRIT-01 are tagged for codex's second council opinion.**
- **NET-2 Claude Workflow** — 4 dimension finders + per-lead refute + completeness critic (`wydep6v2v`, 8 agents): PERM-01/02 = 0 leads; PERM-03 = 1 lead → INFO; PERM-04 = 1 lead → **LOW (confirmed)**; critic = 1 lead → **LOW (confirmed)**.

## Outcome: 0 CATASTROPHE / 0 HIGH / 0 MED / 0 LOW. 3 INFO (all USER-adjudicated by-design / defense-in-depth). PERM-01..04 attested.

| # | Site | Disposition | Detail |
|---|------|-------------|--------|
| PERM-04-L1 | `MintStreakRecorded` doesn't cover pass/whale front-load streak advances · `MintStreakUtils.sol:21-25,106` + `DegenerusGameStorage.sol:1202` (`_withPassStreakFrontLoad`) | **INFO — USER-ADJUDICATED BY-DESIGN 2026-06-16 (pass front-load is event-derivable from the independent pass emits; no parity gap, no contract change)** | **Convergent finder (gemini + NET-2), downgraded by USER ruling.** `LEVEL_STREAK` has two write paths: `_recordMintStreakForLevel` (`MintStreakUtils:104`, **emits** `:106`) and `_withPassStreakFrontLoad` (`Storage:1232-1237`, the 3 pass/whale front-load sites — `_activate10LevelPass`/`_applyWhalePassStats`/lazy-pass — which do **not** emit `MintStreakRecorded`). **USER 2026-06-16: BY-DESIGN — the pass mint-streak effect is fully derivable from the passes' OWN independent emits.** `_withPassStreakFrontLoad` applies a *deterministic* contiguity rule, and the pass events that already fire (`DeityPassPurchased` / `WhalePassClaimed`) carry buyer + level/pass-type, so the full streak = manual advances (`MintStreakRecorded`) ∪ pass front-load (derived from the pass events + the known formula). The event-parity contract is satisfied by the union of the independent emits; `MintStreakRecorded` correctly scopes to manual-mint advances. **No gap.** (The production indexer additionally reconstructs streak from a packed-storage snapshot — `derive-components.ts:109` `mintStreakEffective` + explicit `passState`/`PASS_STREAK_FLOOR_POINTS` — and no handler even consumes `MintStreakRecorded`, so there was never a live de-sync regardless.) |
| PERM-CRIT-01 | Redemption claim deep-reverts during the RNG-lock freeze window on a seed-winning ETH-spin · `StakedDegenerusStonk.sol:782/798/910` + `DegeneretteModule.sol:884` + `LootboxModule.sol:2145` | **INFO — USER-ADJUDICATED BY-DESIGN 2026-06-16 (correct trade-off; an upfront guard would be a regression; no contract change)** | **NET-2 critic (mechanics confirmed), dispositioned by USER ruling.** `prizePoolFrozen` persists across the RNG-lock window (set in `_swapAndFreeze`, seed = `futurePrizePool/100`; cleared at `_unlockRng`). A redemption claim made *during* a freeze window whose seed-fixed outcome wins the rare 5% lootbox **ETH-spin** can revert the whole call when the 1%-seeded frozen pending pool can't cover the win's `ethShare` — then succeeds after RNG unlock (**transient, self-resolving, no fund loss**). **USER 2026-06-16: BY-DESIGN.** The decimator avoids this not by a copyable guard but because its payout is a **value sealed at bucketing** (`DecimatorModule:686`) plus a blanket **upfront** `prizePoolFrozen` revert (`:298,329`) — viable for it because a decimator claim always targets the sealed pool. The redemption claim **cannot** adopt a blanket upfront `prizePoolFrozen` guard: that would over-block the **95%+ of claims that don't hit the 5% ETH-spin** and never need the frozen pool. Failing only the rare seed-determined ETH-spin win that genuinely can't be satisfied — while letting every other claim through during freeze — is the **correct** trade-off; the "symmetric upfront guard" remediation would be a regression. Accepted transient. |
| PERM-03-L1 | `resolveRedemptionLootbox` lacks an in-leg `rngWord==0` revert · `LootboxModule.sol:926-957` | **INFO (defense-in-depth asymmetry; not reachable)** | NET-2 → refuted to INFO. Sibling lootbox paths carry a local `rngWord==0` revert (`:545,:631,:1140`); the redemption variant does not. **Not reachable at the frozen tree:** the caller (`_claimRedemptionFor:910-921`) invokes it only when `!gameOver` + `lootboxEth!=0`, gated by `redemptionPeriods[day]!=0` which is set only after `rngWordByDay[day+1]` is committed (burn-gate `BurnsBlockedBeforeDailyRng:1036` + the `rngGate` resolution ordering). A non-zero word is provably guaranteed. Optional hardening: add the symmetric in-leg `rngWord==0` revert. |

## Verification map — PERM-01..04 attested (gemini + NET-2; codex deferred)

- **PERM-01 — access + composition + offset-key isolation:** every permissionless claim entrypoint (`claimDecimatorJackpot[Many]`, `claimRedemption[Many]`) credits the **named beneficiary, never `msg.sender`** — the only caller-paid value is the illiquid BURNIE flip-credit keeper bounty (floored by a pure non-zero `priceForLevel`); real ETH leaves only via access-gated `claimWinnings`. CEI holds (slot-delete / `claimed=1` before credits); duplicate batch entries settle exactly once (idempotent). **Offset-key isolation holds:** `decBucketOffsetPacked` has exactly two writers — regular `[lvl]` (`:269`) and terminal `[lvl+1]` (`:1024`); a gameover at `level=K` latches `gameOver` and freezes `level`, so no regular round can ever resolve at `K+1` — the terminal's `[K+1]` slot is exclusive (DEC-ALIAS class holds). ✅ both nets.
- **PERM-02 — keeper bounty not a faucet:** decimator `1.5e13 wei`/box + redemption `2.4e13 wei`/box, fixed at the 0.5-gwei reference; at a realistic 10 gwei the ~30k/48k-gas settle costs ~20× the reward; paid as illiquid BURNIE flip-credit, no compounding/self-arming. Net-negative vs real gas. ✅ both nets.
- **PERM-03 — redemption RNG gates:** burn-side `BurnsBlockedBeforeDailyRng` (`sDGNRS:1036`) pins the stamped day to an already-drawn day; the day-`D` claim's lootbox/coinflip legs read `day+1` words resolved in the same `rngGate` tx that commits them, closing the zero-word grind window. `boostTerminalDecimator` is provably pre-liveness (gated `!_livenessTriggered`) so it cannot grind the not-yet-selected winning subbucket. ✅ both nets (residual = the PERM-03-L1 defense-in-depth INFO).
- **PERM-04 — indexer-parity events emission-only:** all 3 events (`AffiliateEarningsRecorded` reused in claim `:665`, `MintStreakRecorded`, `AfkingDelivered` `:948`) are emission-only — the introducing commit `78eb3dd2` is +18 lines = docstrings + 2 event decls + 3 emits (no state/flow/value change); the later `891f7a8f` touched a streak-basis arg well clear of the emit sites. `AffiliateEarningsRecorded` + `AfkingDelivered` fire at correct sites/args once-per-event. The `MintStreakRecorded` coverage gap = PERM-04-L1 LOW. ✅ both nets.

## Verdict

The permissionless/keeper surface is **access-correct, composition-safe, and not a faucet**, the decimator offset-key isolation and the redemption RNG gates **hold**, and the indexer events are **emission-only**. Three INFO, all USER-adjudicated as by-design or defense-in-depth: (1) `MintStreakRecorded` scopes to manual-mint advances — pass front-load is fully **event-derivable** from the independent `DeityPassPurchased`/`WhalePassClaimed` emits + the deterministic front-load rule (USER by-design); (2) the rare redemption ETH-spin **deep-revert during a freeze window** is the **correct trade-off** vs over-blocking the common case with an upfront guard — transient, self-resolving, no fund loss (USER by-design); (3) the `resolveRedemptionLootbox` missing in-leg `rngWord==0` revert is unreachable defense-in-depth. **No CATASTROPHE/HIGH/MED/LOW, no solvency/RNG-freeze break.** All three INFO + the gemini council are **tagged for the codex backfill** (second council opinion). **PERM-01..04 attested; no in-milestone contract change.**
