---
phase: 330-impl-the-one-batched-contract-diff-router-advance-rework-mic
plan: 03
type: execute
wave: 3
depends_on: ["330-02"]
files_modified:
  - contracts/DegenerusGame.sol
autonomous: true
requirements: [ROUTER-05]
must_haves:
  truths:
    - "autoResolve is renamed to degeneretteResolve; _autoResolveBet renamed to _degeneretteResolveBet; the internal self-call site renamed in lock-step"
    - "The per-item gas-pegged reward is replaced by a count: ≥3 successfully-resolved NON-WWXRP bets pays ONE flat RESOLVE_FLAT_BURNIE creditFlip; 0 resolved reverts NoWork(); 1-2 resolved commits unpaid (never strands the tail)"
    - "RESOLVE_FLAT_BURNIE is a clearly-named GAS-331 placeholder constant"
    - "AUTO-02 probe, per-item try/catch isolation, WWXRP exclusion, self-resolve, and one-creditFlip-CEI-last are all preserved"
  artifacts:
    - path: "contracts/DegenerusGame.sol"
      provides: "degeneretteResolve renamed + flat ≥3-gate re-peg + RESOLVE_FLAT_BURNIE placeholder + NoWork()"
      contains: "function degeneretteResolve"
  key_links:
    - from: "degeneretteResolve loop"
      to: "_degeneretteResolveBet"
      via: "this._degeneretteResolveBet(players[i], betIds[i]) in the per-item try"
      pattern: "this._degeneretteResolveBet"
---

<objective>
The `degeneretteResolve` rename + flat ~1-BURNIE "lose" re-peg (D-05, 329-SPEC §2 + 329-ATTEST-DEGENERETTE-RESOLVE §A/§B) — a SURVIVOR that rides the BATCH-02 diff. It stays a SEPARATE permissionless caller-supplied-arrays call (NOT a router leg — ROUTER-05; the router-fold is architecturally blocked, 329-ATTEST §D). Two changes on `DegenerusGame.sol`:

1. **Rename (D-05a):** `autoResolve` → `degeneretteResolve` (`:1587`), `_autoResolveBet` → `_degeneretteResolveBet` (`:1684`), and the internal self-call site (`:1606`) in lock-step. NO interface row (the symbols are NOT in any `contracts/interfaces/` file — confirmed ABSENT, 329-ATTEST §A.2).
2. **Flat ≥3 re-peg (D-05b):** replace the per-item gas-pegged `reward += _ethToBurnieValue(AUTO_RESOLVE_BET_GAS_UNITS * AUTO_GAS_PRICE_REF, …)` accumulation with a `++successCount` (non-WWXRP arm only); after the loop, `if (successCount >= 3) coinflip.creditFlip(msg.sender, RESOLVE_FLAT_BURNIE);` and `if (totalResolved == 0) revert NoWork();` (the 1-2 case commits the resolutions UNPAID — lean = do-not-revert, never strand the tail). `RESOLVE_FLAT_BURNIE` is a GAS-331 placeholder (~1e18; the exact value is calibrated at GAS / Phase 331, NOT pinned here).

Depends on 330-02 (same file — sequential edit; the router-surface edits land first).

Purpose: rename + re-peg the Degenerette resolve helper to a flat ≥3-gate "lose" while preserving all structural protections.
Output: `contracts/DegenerusGame.sol` with `degeneretteResolve` / `_degeneretteResolveBet` + the flat ≥3 re-peg + `RESOLVE_FLAT_BURNIE` placeholder + `NoWork()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-DEGENERETTE-RESOLVE.md

<interfaces>
<!-- Attested @ 0cc5d10f (329-ATTEST-DEGENERETTE-RESOLVE.md §A/§B). Re-grep at edit time. -->

RENAME TARGETS (§A.1):
- external `autoResolve(address[] calldata players, uint64[] calldata betIds) external` at `:1587` → `degeneretteResolve` (same params, permissionless, empty/length guard `if (len == 0 || betIds.length != len) revert E()` at `:1592` stays).
- `onlySelf` internal `_autoResolveBet(address player, uint64 betId) external` at `:1684` (self-guard `:1685`, `resolveBets` delegatecall `:1692`) → `_degeneretteResolveBet`.
- self-call site `try this._autoResolveBet(players[i], betIds[i])` at `:1606` → `this._degeneretteResolveBet(...)`.
- NO interface edit: `autoResolve`/`_autoResolveBet` appear in ZERO files under `contracts/interfaces/` (§A.2 ABSENT — do NOT add or chase an interface row).
- `_autoResolveBet` STAYS `external` onlySelf (per-item isolation needs the external `this.` call for the per-item try/catch — do NOT make it internal; unlike `_autoOpenBox`, the Degenerette per-item try/catch is PRESERVED, RD-5 applies only to autoOpen).

RE-PEG LOOP (§B.2, the current `:1587-1622` shape):
```
uint256 reward;
uint24 lvl = _activeTicketLevel();
for (...) {
  uint256 betPacked = degeneretteBets[players[i]][betIds[i]];
  uint8 currency = uint8((betPacked >> 42) & 0x3);
  try this._autoResolveBet(players[i], betIds[i]) {
    if (currency == 3) { /* zero reward */ } else { reward += _ethToBurnieValue(AUTO_RESOLVE_BET_GAS_UNITS * AUTO_GAS_PRICE_REF, PriceLookupLib.priceForLevel(lvl)); }
  } catch {}
  ++i;
}
if (reward != 0) coinflip.creditFlip(msg.sender, reward);
```
TARGET (§B.1):
- the non-WWXRP arm `reward += _ethToBurnieValue(...)` (`:1611-1614`) → `unchecked { ++successCount; }` (counts SUCCESSES of non-WWXRP only; WWXRP success does NOT increment — AUTO-04 / D-05d).
- track `totalResolved` (any resolution, WWXRP-inclusive) — increment it inside BOTH arms of the `try`-success body so a WWXRP-only batch that DID resolve does not falsely `NoWork()`.
- the post-loop `if (reward != 0) coinflip.creditFlip(msg.sender, reward);` (`:1622`) → `if (totalResolved == 0) revert NoWork(); if (successCount >= 3) coinflip.creditFlip(msg.sender, RESOLVE_FLAT_BURNIE);` (the 1-2-resolved case: totalResolved != 0 so no revert, successCount < 3 so no creditFlip → resolutions commit unpaid; the tail is never rolled back).
- `lvl`/`PriceLookupLib`: no longer needed for the reward (flat constant) — remove the now-unused `lvl` if it has no other use in the function.

PLACEHOLDER (§B.2): add `uint256 private constant RESOLVE_FLAT_BURNIE = 1e18; // GAS-331 PLACEHOLDER — flat ~1-BURNIE "lose"; exact value calibrated under the USER-gated GAS phase (331), NOT locked here.` Use a clearly-named marker comment.

NoWork(): add `error NoWork();` (reuse the same selector the AfKing router's ROUTER-06 will use — declare it once on DegenerusGame; AfKing declares its own; they are separate contracts with separate selectors, that is fine). Trivial error decl.

DEAD CONSTANT housekeeping (§B.2): after the per-item peg is removed, `AUTO_RESOLVE_BET_GAS_UNITS` (`:1545`) has no remaining consumer — delete it. Then `AUTO_GAS_PRICE_REF` (`:1539`) ALSO becomes dead (its only remaining consumer was the open-leg conversion 330-02 already deleted + this resolve peg) — confirm 0 references and delete it too. Grep before deleting.

PRESERVED (§A.4 — do NOT change): AUTO-02 probe `if (degeneretteBets[players[0]][betIds[0]] == 0) revert BatchAlreadyTaken();` (`:1596`); per-item `try this._degeneretteResolveBet(...) { … } catch {}` isolation; the currency decode `(betPacked >> 42) & 0x3` (`:1604`) + WWXRP `currency == 3` exclusion; self-resolve (no caller gate); ONE creditFlip CEI-last.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rename autoResolve→degeneretteResolve + _autoResolveBet→_degeneretteResolveBet + self-call site (D-05a)</name>
  <read_first>
    - contracts/DegenerusGame.sol (autoResolve :1587-1622, the self-call :1606, _autoResolveBet :1684-1700)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-DEGENERETTE-RESOLVE.md (§A.1 the 3 rename targets, §A.2 interface ABSENT)
  </read_first>
  <action>
    Rename the external `autoResolve(address[] calldata players, uint64[] calldata betIds)` (`:1587`) to `degeneretteResolve` — same params, same visibility (external), same permissionless behavior. Rename the `onlySelf` `_autoResolveBet(address player, uint64 betId) external` (`:1684`) to `_degeneretteResolveBet` — KEEP it `external` with the `if (msg.sender != address(this)) revert E();` self-guard (the per-item try/catch isolation requires the external `this.` call — do NOT internalize it). Rename the self-call site `try this._autoResolveBet(players[i], betIds[i])` (`:1606`) to `this._degeneretteResolveBet(...)`. Update the surrounding doc-comments to the new names. Do NOT add any interface edit (the symbols are absent from `contracts/interfaces/`).
  </action>
  <verify>
    <automated>grep -c "function degeneretteResolve(" contracts/DegenerusGame.sol | grep -qx 1 && grep -c "function _degeneretteResolveBet(address player, uint64 betId) external" contracts/DegenerusGame.sol | grep -qx 1 && grep -c "this._degeneretteResolveBet(" contracts/DegenerusGame.sol | grep -qx 1 && grep -c "autoResolve\|_autoResolveBet" contracts/DegenerusGame.sol | grep -qx 0 && echo PASS</automated>
  </verify>
  <done>`degeneretteResolve` + `_degeneretteResolveBet` (external onlySelf, self-guard kept) + the renamed self-call exist; zero `autoResolve`/`_autoResolveBet` references remain in DegenerusGame.sol.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Flat ≥3-gate re-peg + RESOLVE_FLAT_BURNIE placeholder + NoWork() + dead-constant cleanup (D-05b)</name>
  <read_first>
    - contracts/DegenerusGame.sol (the renamed degeneretteResolve loop :1601-1622; AUTO_RESOLVE_BET_GAS_UNITS :1545; AUTO_GAS_PRICE_REF :1539; the error decls region for BatchAlreadyTaken :95)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-DEGENERETTE-RESOLVE.md (§B.1/§B.2 the exact edit targets + the lean)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md (§2 D-05b/D-05c/D-05d/D-05f)
  </read_first>
  <action>
    In the renamed `degeneretteResolve` loop: replace the non-WWXRP reward accumulation `reward += _ethToBurnieValue(AUTO_RESOLVE_BET_GAS_UNITS * AUTO_GAS_PRICE_REF, PriceLookupLib.priceForLevel(lvl));` with `unchecked { ++successCount; }` (declare `uint256 successCount;` before the loop). In the WWXRP arm (`currency == 3`) keep zero reward but track resolution: declare `uint256 totalResolved;` before the loop and `unchecked { ++totalResolved; }` inside BOTH arms of the try-success body (so any resolution — WWXRP or not — counts toward the no-work gate). Remove the `uint256 reward;` declaration.

    After the loop, replace `if (reward != 0) coinflip.creditFlip(msg.sender, reward);` with:
    `if (totalResolved == 0) revert NoWork(); if (successCount >= 3) coinflip.creditFlip(msg.sender, RESOLVE_FLAT_BURNIE);`
    (the 1-2-resolved path: `totalResolved != 0` → no revert; `successCount < 3` → no creditFlip → resolutions commit unpaid; the trailing tail is never rolled back — the lean, D-05b(iv)).

    Add the placeholder constant near the other auto-reward constants: `uint256 private constant RESOLVE_FLAT_BURNIE = 1e18; // GAS-331 PLACEHOLDER — flat ~1-BURNIE "lose"; exact value calibrated under the USER-gated GAS phase (331), NOT locked here.`

    Add `error NoWork();` to the DegenerusGame error decls.

    DEAD-CONSTANT CLEANUP: after removing the per-item peg, grep `AUTO_RESOLVE_BET_GAS_UNITS` → if 0 references, delete its decl (`:1545`). Then grep `AUTO_GAS_PRICE_REF` → 330-02 already removed the open-leg consumer; if 0 references now, delete it (`:1539`). If either still has a consumer, leave it and note in the SUMMARY.

    Remove the now-unused `lvl`/`PriceLookupLib` usage in the function if the flat re-peg leaves them dead (only remove if truly unreferenced in `degeneretteResolve`). PRESERVE the AUTO-02 probe, the per-item try/catch, the currency decode + WWXRP exclusion, self-resolve, one-creditFlip-CEI-last.
  </action>
  <verify>
    <automated>grep -c "RESOLVE_FLAT_BURNIE" contracts/DegenerusGame.sol | grep -q "[2-9]" && grep -c "GAS-331 PLACEHOLDER" contracts/DegenerusGame.sol | grep -q "[1-9]" && grep -c "if (successCount >= 3) coinflip.creditFlip(msg.sender, RESOLVE_FLAT_BURNIE)" contracts/DegenerusGame.sol | grep -qx 1 && grep -c "if (totalResolved == 0) revert NoWork();" contracts/DegenerusGame.sol | grep -qx 1 && grep -c "error NoWork();" contracts/DegenerusGame.sol | grep -qx 1 && grep -v '^[[:space:]]*//' contracts/DegenerusGame.sol | grep -c "AUTO_RESOLVE_BET_GAS_UNITS" | grep -qx 0 && echo PASS</automated>
  </verify>
  <done>The flat ≥3 re-peg is in place: `++successCount` (non-WWXRP) + `++totalResolved` (any) + `if (totalResolved == 0) revert NoWork(); if (successCount >= 3) creditFlip(msg.sender, RESOLVE_FLAT_BURNIE);`; `RESOLVE_FLAT_BURNIE` is a clearly-marked GAS-331 placeholder; `NoWork()` declared; `AUTO_RESOLVE_BET_GAS_UNITS` (and `AUTO_GAS_PRICE_REF` if dead) removed; all structural protections preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller (any EOA) → degeneretteResolve(players[], betIds[]) | permissionless, caller-supplied arrays; pays a flat bounty only at ≥3 non-WWXRP resolutions |
| degeneretteResolve → coinflip.creditFlip | the flat ~1-BURNIE flip-credit (illiquid, ≤ mintPrice/1000 ETH) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-330-03-01 | Spoofing / faucet | the flat ~1-BURNIE reward | mitigate | The keeper pays REAL prevailing gas (≥220k for the ≥3 minimum × 5-50+ gwei = 0.0011-0.011+ ETH) vs ≤0.00024 ETH illiquid flip-credit → net loss at any realistic gas; the ≥3 gate widens the margin (329-ATTEST-DEGENERETTE-RESOLVE §C). Exact constant confirmed sub-real-gas at GAS-06 (Phase 331). |
| T-330-03-02 | Tampering | WWXRP counted toward the ≥3 reward gate | mitigate | `++successCount` fires ONLY in the non-WWXRP arm (the existing `currency == 3` fork is preserved); a WWXRP-only batch never reaches 3 → never pays (AUTO-04 / D-05d). |
| T-330-03-03 | Denial of Service | reverting at <3 would strand the trailing tail | mitigate | The lean: `totalResolved == 0 → revert NoWork()`, but 1-2 resolved → NO revert (resolutions commit unpaid). The per-item `delete` commits inside each `try` regardless of the pay-gate, so no resolution is rolled back (D-05b(iv)). |
| T-330-03-04 | Tampering | losing-bet liveness dependency | accept | 329-ATTEST-DEGENERETTE-RESOLVE §E re-confirmed INERT-SAFE: no invariant/accounting/RNG-slot/gameOver/sweep/jackpot path requires losing bets resolved (all 8 `degeneretteBets` consumers + 8 modules grep-clean). SURFACE-TO-USER: NONE. The flat reward mildly IMPROVES backlog liveness. |
| T-330-03-05 | Tampering | rename dangling the internal selector | mitigate | `_degeneretteResolveBet` stays `external` onlySelf; the self-call site renames in lock-step (verify grep proves 0 `autoResolve`/`_autoResolveBet` residue). The CrankLeversAndPacking literal source-string assertions are updated in the test plan (330-07). |
| T-330-03-SC | Tampering | npm/forge installs | mitigate | No package installs (source edit only). |
</threat_model>

<verification>
- The rename is atomic in DegenerusGame.sol (0 residual `autoResolve`/`_autoResolveBet`); the test-side literal-string assertions update in 330-07.
- The flat ≥3-gate / NoWork()-at-zero / unpaid-1-2 / WWXRP-excluded behavior is proven at TST-05 (Phase 332); the exact `RESOLVE_FLAT_BURNIE` value is confirmed sub-real-gas at GAS-06 (Phase 331).
- Resolution RESULTS are byte-identical vs the per-item path (rename + bounty-shape only, no payout/RNG change) — TST-05.
- Compiles within the BATCH-02 `forge build` (330-08).
</verification>

<success_criteria>
- ROUTER-05: `autoResolve`→`degeneretteResolve` renamed (+ `_degeneretteResolveBet` + self-call); stays a SEPARATE caller-supplied-arrays call (NOT a router leg); the KEEP-04 affiliate path is unaffected (it lives in `_batchPurchaseUnit`, untouched here). The flat ≥3-gate re-peg + `RESOLVE_FLAT_BURNIE` placeholder + `NoWork()` land; all structural protections preserved.
</success_criteria>

<output>
Create `.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-03-SUMMARY.md` when done.
</output>
