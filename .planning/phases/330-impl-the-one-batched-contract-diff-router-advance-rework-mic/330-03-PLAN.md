---
phase: 330-impl-the-one-batched-contract-diff-router-advance-rework-mic
plan: 03
type: execute
wave: 3
depends_on: ["330-02"]
files_modified:
  - contracts/interfaces/IDegenerusGameModules.sol
  - contracts/interfaces/IDegenerusGame.sol
autonomous: true
requirements: [ADV-02, ROUTER-04]
must_haves:
  truths:
    - "`IDegenerusGameAdvanceModule.advanceGame()` in `IDegenerusGameModules.sol` (`:10`) is updated from `external;` to `external returns (uint8 mult, bool rewardable);` so the `DegenerusGame.sol:280` `abi.encodeWithSelector(IDegenerusGameAdvanceModule.advanceGame.selector)` delegatecall + the wrapper decode (330-02) stay selector-coherent with the producer (330-01)."
    - "`IDegenerusGame` gains `advanceDue()` and `boxesPending()` external-view signatures matching the bodies added on `DegenerusGame.sol` in 330-02, so typed consumers (the off-chain keeper pre-call probe) can read them."
    - "NO `degeneretteResolve`/`_degeneretteResolveBet` row is added to ANY interface file (C6 — both symbols are defined directly on `DegenerusGame.sol`, never via an interface)."
  artifacts:
    - path: "contracts/interfaces/IDegenerusGameModules.sol"
      provides: "IDegenerusGameAdvanceModule.advanceGame() updated to the (uint8 mult, bool rewardable) return"
      contains: "returns (uint8 mult, bool rewardable)"
    - path: "contracts/interfaces/IDegenerusGame.sol"
      provides: "advanceDue() + boxesPending() external-view signatures"
      contains: "function advanceDue"
  key_links:
    - from: "contracts/interfaces/IDegenerusGameModules.sol advanceGame() tuple signature"
      to: "contracts/DegenerusGame.sol:280 delegatecall selector + the wrapper decode (330-02)"
      via: "the .selector must encode the same return shape the producer ABI-encodes"
      pattern: "advanceGame.selector"
---

<objective>
INTERFACE reconciliation (SPEC §3 edit-order step 3). Update the two `contracts/interfaces/*.sol` files so the downstream consumers (the `DegenerusGame.sol` wrapper's delegatecall selector + the AfKing router + the off-chain keeper) bind against the settled signatures: (1) `IDegenerusGameAdvanceModule.advanceGame()` gains the `(uint8 mult, bool rewardable)` return; (2) `IDegenerusGame` gains the `advanceDue()`/`boxesPending()` view signatures. NO `degeneretteResolve` interface row (C6).

Purpose: ROADMAP SC3 (ADV-02 — the `advanceGame` return shape is consistent across the delegatecall selector site and its callers) + SC1 (ROUTER-04 — the O(1) discovery views are exposed on the typed game interface for the keeper pre-call probe). This is the THIRD edit in the producer-before-consumer chain: it consumes 330-02's settled view bodies + the producer's tuple, and produces the interface signatures the AfKing router (330-04) and any typed off-chain consumer bind against.
Output: the updated `IDegenerusGameAdvanceModule.advanceGame()` signature + the two new view signatures on `IDegenerusGame`.

NON-NEGOTIABLE: This plan EDITS `contracts/*.sol` but MUST NOT commit anything. ONE batched diff, committed only after 330-07. NOTHING pre-approved. APPLY-ORDER: Wave 3, AFTER 330-02 (the view bodies + the wrapper tuple must exist so the interface signatures match exactly). NO `degeneretteResolve`/`_degeneretteResolveBet` row in ANY interface file (C6 — verified ABSENT at baseline: `grep -rln "autoResolve\|_autoResolveBet" contracts/interfaces/` → ZERO). Do NOT add the AfKing-local `IGame` consumer declarations here — those (`IGame.advanceGame`, `IGame.autoOpen`, `IGame.advanceDue`, `IGame.boxesPending`) live in `AfKing.sol` and are part of plan 330-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-DEGENERETTE-RESOLVE.md
@.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-02-PLAN.md

<interfaces>
<!-- Anchors from live source at 0cc5d10f. Re-grep before editing. -->

contracts/interfaces/IDegenerusGameModules.sol:
  :8   interface IDegenerusGameAdvanceModule {
  :10  function advanceGame() external;          // → external returns (uint8 mult, bool rewardable);
  // selector consumed at DegenerusGame.sol:280 abi.encodeWithSelector(IDegenerusGameAdvanceModule.advanceGame.selector)

contracts/interfaces/IDegenerusGame.sol:
  :15  interface IDegenerusGame {                 // the broad typed game interface (68 fn decls; consumed by Stonk/Jackpots/Affiliate/Burnie/Coinflip/Quests + IDegenerusGame(address(this)) self-calls)
  // ADD: function advanceDue() external view returns (bool);
  //      function boxesPending() external view returns (bool);
  // (match the exact bodies added on DegenerusGame.sol in 330-02)

C6 (329-ATTEST-DEGENERETTE-RESOLVE §A.2): autoResolve/_autoResolveBet are ABSENT from all contracts/interfaces/ files — the rename adds NO interface row. degeneretteResolve must NOT be added here.
</interfaces>

<grep_protocol>
Baseline `0cc5d10f` (live tree byte-identical). RE-GREP `function advanceGame` in `IDegenerusGameModules.sol`, the `interface IDegenerusGame {` decl + a couple of existing `external view returns (bool)` decls in `IDegenerusGame.sol` (to match the file's signature style), and `grep -rln "autoResolve\|degeneretteResolve\|_autoResolveBet" contracts/interfaces/` (MUST stay ZERO — C6). 330-02 edits `DegenerusGame.sol` (a DIFFERENT file) so these interface lines are at baseline. The view signatures MUST byte-match the 330-02 bodies (same name, same `external view returns (bool)`).
</grep_protocol>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update IDegenerusGameAdvanceModule.advanceGame() to the tuple return + add advanceDue()/boxesPending() to IDegenerusGame (C6: no degeneretteResolve row)</name>
  <read_first>
    - contracts/interfaces/IDegenerusGameModules.sol (`interface IDegenerusGameAdvanceModule` :8; `function advanceGame() external;` :10 — the ONLY edit here; confirm no other `advanceGame` decl in the file)
    - contracts/interfaces/IDegenerusGame.sol (`interface IDegenerusGame {` :15; read 2-3 existing `external view returns (...)` decls to match the file's brace/spacing style; pick a logical insertion point near other keeper/view functions)
    - contracts/DegenerusGame.sol (the 330-02-added `function advanceDue() external view returns (bool)` + `function boxesPending() external view returns (bool)` bodies — the interface signatures MUST byte-match these; and the wrapper `:275` + the delegatecall selector site `:280` to confirm the module-interface return shape now matches the producer)
    - 329-SPEC.md §1 R1 (the `advanceGame` interface signature update) + §1 R3 (the view signatures added to the interfaces) + §0 C6 (the degeneretteResolve no-interface-row correction)
    - 329-ATTEST-DEGENERETTE-RESOLVE.md §A.2 (C6 — the interface rename rows are ABSENT/no-op)
  </read_first>
  <action>
    Per ADV-02 + ROUTER-04 + SPEC §1 R1/R3 + §3 step 3 + C6. Re-grep the three anchors first.
    (1) In `IDegenerusGameModules.sol`: change `function advanceGame() external;` (`:10`, inside `interface IDegenerusGameAdvanceModule`) to `function advanceGame() external returns (uint8 mult, bool rewardable);`. This keeps the `.selector` used at `DegenerusGame.sol:280` selector-coherent with the producer's ABI-encoded `(uint8, bool)` return — a void-vs-tuple mismatch here would silently mis-decode. No other change to this file.
    (2) In `IDegenerusGame.sol` (inside `interface IDegenerusGame`): ADD `function advanceDue() external view returns (bool);` and `function boxesPending() external view returns (bool);` — the signatures MUST byte-match the bodies 330-02 added on `DegenerusGame.sol` (same names, same `external view returns (bool)`). Place them near the other keeper/view declarations; match the file's existing decl style.
    (3) Do NOT add ANY `degeneretteResolve`/`_degeneretteResolveBet` declaration to either interface file (C6). Do NOT add the AfKing-local `IGame` consumer decls here (those live in `AfKing.sol`, plan 330-04). Comments describe what IS.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && echo "--- module-interface tuple ---"; grep -c "function advanceGame() external returns (uint8 mult, bool rewardable);" contracts/interfaces/IDegenerusGameModules.sol | tr -d "\n"; echo " advanceGame tuple sig (MUST be 1)"; grep -c "function advanceGame() external;" contracts/interfaces/IDegenerusGameModules.sol | tr -d "\n"; echo " stale void sig (MUST be 0)"; echo "--- view sigs ---"; grep -c "function advanceDue() external view returns (bool);" contracts/interfaces/IDegenerusGame.sol | tr -d "\n"; echo " advanceDue sig (MUST be 1)"; grep -c "function boxesPending() external view returns (bool);" contracts/interfaces/IDegenerusGame.sol | tr -d "\n"; echo " boxesPending sig (MUST be 1)"; echo "--- C6: no degeneretteResolve interface row ---"; grep -rc "autoResolve\|degeneretteResolve\|_autoResolveBet" contracts/interfaces/ | tr -d "\n"; echo " (per-file counts; ALL MUST be 0)"; echo "--- build ---"; timeout 400 forge build 2>&1 | grep -ciE "^error|Error \(" | tr -d "\n"; echo " build errors (MUST be 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `IDegenerusGameModules.sol` has exactly one `function advanceGame() external returns (uint8 mult, bool rewardable);` and zero `function advanceGame() external;` (the void sig is gone).
    - `IDegenerusGame.sol` has exactly one `function advanceDue() external view returns (bool);` and one `function boxesPending() external view returns (bool);`, byte-matching the 330-02 bodies.
    - `grep -rc "autoResolve\|degeneretteResolve\|_autoResolveBet" contracts/interfaces/` is 0 for every interface file (C6 — no interface row for the rename).
    - `forge build` reports 0 errors (the selector at `DegenerusGame.sol:280` now binds to the tuple return; the view signatures resolve).
  </acceptance_criteria>
  <done>`IDegenerusGameAdvanceModule.advanceGame()` returns `(uint8 mult, bool rewardable)`; `IDegenerusGame` exposes `advanceDue()`/`boxesPending()`; no `degeneretteResolve` interface row exists (C6); the tree compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| delegatecall selector ↔ producer return shape | the `IDegenerusGameAdvanceModule.advanceGame.selector` at `DegenerusGame.sol:280` encodes the call; a void-vs-tuple interface mismatch would make the wrapper decode garbage. The interface return shape must equal the producer's ABI-encoded return. |
| typed game consumer → views | the `IDegenerusGame` view signatures are the typed surface the off-chain keeper (and any peripheral contract) binds to; they must match the concrete bodies. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-330-03-01 | Tampering | interface return-shape drift → wrapper decodes garbage | mitigate | The module-interface `advanceGame()` return is updated to the exact `(uint8 mult, bool rewardable)` shape the producer (330-01) ABI-encodes and the wrapper (330-02) decodes; `forge build` fails if the selector site and the return shape diverge. |
| T-330-03-02 | Tampering | a spurious `degeneretteResolve` interface row (false IMPL target) | mitigate | C6 enforced by grep gate (`grep -rc … contracts/interfaces/` MUST be 0) — both symbols are concrete `DegenerusGame.sol` definitions, never interface-bound; adding a row would be a no-op false target. |
| T-330-03-03 | Information Disclosure | view signatures diverge from the concrete bodies | mitigate | The interface signatures are required to byte-match the 330-02 bodies (same name + `external view returns (bool)`); `forge build` resolves them against the concrete contract. |
| T-330-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. No `## Package Legitimacy Audit` required. |
</threat_model>

<verification>
- `forge build` 0 errors over the whole tree (the selector binds to the tuple; the views resolve).
- `IDegenerusGameAdvanceModule.advanceGame()` returns `(uint8 mult, bool rewardable)`; no stale void sig.
- `IDegenerusGame` exposes `advanceDue()`/`boxesPending()` matching the 330-02 bodies.
- Zero `degeneretteResolve`/`autoResolve` rows in `contracts/interfaces/` (C6).
- No git commit performed (batched-diff discipline).
</verification>

<success_criteria>
- ROADMAP SC3 (ADV-02 — the `advanceGame` return shape is reconciled across the delegatecall selector + its consumers) + SC1 (ROUTER-04 — the O(1) views are exposed on the typed game interface) satisfied, with NO `degeneretteResolve` interface row (C6), so the AfKing router (330-04) binds against fully-settled signatures.
</success_criteria>

<output>
Create `.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-03-SUMMARY.md` when done. Record: the re-grepped lines, the module-interface `advanceGame` update, the two view signatures added + their match to the 330-02 bodies, and the C6 zero-interface-row confirmation. Do NOT git commit.
</output>
