---
phase: 330-impl-the-one-batched-contract-diff-router-advance-rework-mic
plan: 02
type: execute
wave: 2
depends_on: ["330-01"]
files_modified:
  - contracts/DegenerusGame.sol
autonomous: true
requirements: [ADV-02, ROUTER-04, ROUTER-05, BATCH-02]
must_haves:
  truths:
    - "The `advanceGame` wrapper (`:275`) decodes the delegatecall `data` into `(uint8 mult, bool rewardable)` on the SUCCESS branch (it currently captures `data` only for the revert path `:284`), updates its own external signature to `returns (uint8 mult, bool rewardable)`, and forwards the tuple to external callers — the AfKing router consumes it via `IGame.advanceGame()` (ADV-02 / R1)."
    - "Two O(1) discovery views exist on `DegenerusGame`: `advanceDue()` returns true when new-day (`currentDayView() != dailyIdx`) OR mid-day partial-drain (`LR_MID_DAY != 0`); `boxesPending()` returns true when `boxPlayers[activeIndex].length > boxCursor` AND `lootboxRngWordByIndex[activeIndex] != 0` — both two/three scalar SLOADs, NO iteration over any growable set (ROUTER-04, no unbounded scans)."
    - "`autoResolve` → `degeneretteResolve` (`:1587`), `_autoResolveBet` → `_degeneretteResolveBet` (`:1684`), and the self-call site `try this._autoResolveBet(...)` (`:1606`) → `this._degeneretteResolveBet(...)` are renamed in lock-step (BATCH-02 / D-05a); the `auto*` family (`autoOpen`) is untouched."
    - "The `degeneretteResolve` reward is re-pegged: the per-item peg at `:1611-1614` becomes a per-item `++successCount` in the NON-WWXRP arm; the post-loop `:1622` becomes `if (successCount >= 3) coinflip.creditFlip(msg.sender, RESOLVE_FLAT_BURNIE);` plus a `NoWork()` revert when ZERO bets resolve (1–2 resolved → resolved-and-pay-0, NO revert); WWXRP (currency == 3) stays EXCLUDED from the count (D-05b/d)."
    - "`RESOLVE_FLAT_BURNIE = 1e18` lands under a named, greppable D-01b flagged-marker block; AUTO-02 probe / per-item try-catch / self-resolve / one-creditFlip-CEI-last are all preserved (D-05d)."
  artifacts:
    - path: "contracts/DegenerusGame.sol"
      provides: "advanceGame wrapper tuple decode (ADV-02); advanceDue()/boxesPending() O(1) views (ROUTER-04); degeneretteResolve/_degeneretteResolveBet rename + flat ≥3-gate re-peg + RESOLVE_FLAT_BURNIE (BATCH-02)"
      contains: "function degeneretteResolve"
  key_links:
    - from: "contracts/DegenerusGame.sol advanceGame wrapper success branch"
      to: "contracts/modules/DegenerusGameAdvanceModule.sol advanceGame() return tuple (plan 330-01)"
      via: "abi.decode(data, (uint8, bool)) on the delegatecall success branch"
      pattern: "abi.decode\\(data"
    - from: "contracts/DegenerusGame.sol advanceDue()/boxesPending()"
      to: "contracts/AfKing.sol doWork router (plan 330-04) + the interface adds (plan 330-03)"
      via: "O(1) discovery predicates consumed by the router routing"
      pattern: "function advanceDue|function boxesPending"
---

<objective>
CONSUMER of the design-1 tuple + the discovery-view + rename/re-peg home (SPEC §3 edit-order step 2). On `DegenerusGame.sol`: (1) decode the delegatecall return into `(uint8 mult, bool rewardable)` on the wrapper success branch and forward it (ADV-02 / R1); (2) add the two O(1) discovery views `advanceDue()` and `boxesPending()` (ROUTER-04 / R3); (3) rename `autoResolve`→`degeneretteResolve` + `_autoResolveBet`→`_degeneretteResolveBet` and re-peg the reward to the flat ~1-BURNIE / ≥3-NON-WWXRP-gate / revert-on-zero shape, adding `RESOLVE_FLAT_BURNIE` under the D-01b marker block (BATCH-02 / D-05). The `_autoBuy` refactor + the router itself land on AfKing (plan 330-04); this plan settles the game-side signatures the router consumes.

Purpose: ROADMAP SC1 (ROUTER-04 views, no unbounded scans) + SC3 (ADV-02 the wrapper decode of the design-1 return) + SC2 (ROUTER-05 — `autoResolve` excluded from the router + RENAMED) + SC5 (BATCH-02 — the rename + flat re-peg rides the same diff). This is the SECOND edit in the producer-before-consumer chain: it consumes plan 330-01's tuple and produces the view signatures + the renamed entrypoint that plans 330-03 (interfaces) and 330-04 (AfKing router) consume.
Output: the wrapper tuple decode + forward; `advanceDue()`/`boxesPending()`; the `degeneretteResolve`/`_degeneretteResolveBet` rename + the flat ≥3-gate re-peg + `RESOLVE_FLAT_BURNIE` under the D-01b marker.

NON-NEGOTIABLE: This plan EDITS `contracts/*.sol` but MUST NOT commit anything. ONE batched diff, committed only after 330-07. NOTHING pre-approved. APPLY-ORDER DISCIPLINE: this plan runs in Wave 2, AFTER 330-01 (the AdvanceModule producer) — the wrapper decode needs the tuple return to exist. It must NOT touch the KEEP-04 affiliate wiring `bytes32("DGNRS")` (`:1781`) — that path is untouched by this milestone (C1); it must NOT add any `degeneretteResolve` interface row (C6 — the interfaces plan 330-03 handles the `advanceGame`/view signatures ONLY, the rename has NO interface edit). The router-fold of `degeneretteResolve` is OUT — it stays a SEPARATE permissionless call (ROUTER-05/D-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-DEGENERETTE-RESOLVE.md

<interfaces>
<!-- Anchors extracted from live source at 0cc5d10f. Re-grep before editing — lines shift only within this file + by 330-01's edits to a DIFFERENT file (no drift here). -->

contracts/DegenerusGame.sol — current state at 0cc5d10f:
  :275  function advanceGame() external {                       // wrapper — gains the tuple return + the decode
  :276-282  (bool ok, bytes memory data) = ContractAddresses.GAME_ADVANCE_MODULE.delegatecall(abi.encodeWithSelector(IDegenerusGameAdvanceModule.advanceGame.selector));
  :284  if (!ok) _revertDelegate(data);                          // today `data` is used ONLY for the revert path; decode on SUCCESS (:283-284 region)
  :1022 _revertDelegate(...)                                     // exists
  :462  function currentDayView() ... return _simulatedDayIndex(); // advanceDue() new-day input (vs dailyIdx)
  // dailyIdx — monotonic day counter, DegenerusGameStorage.sol:231
  // LR_MID_DAY packed flag — _lrRead(LR_MID_DAY_SHIFT, LR_MID_DAY_MASK) != 0 (set AdvanceModule:1085, cleared :222) → advanceDue() mid-day input
  :1551 boxCursor (uint48)                                       // boxesPending() input
  :1562 boxPlayers mapping(uint48 => address[])                  // boxesPending(): boxPlayers[activeIndex].length
  :1647 open gated on lootboxRngWordByIndex[index] != 0          // boxesPending() input
  :1636 function autoOpen(uint256 maxCount) external             // UNTOUCHED (auto* family); loop at :1656; bounty :1676
  :1587 function autoResolve(address[] calldata players, uint64[] calldata betIds)  // → degeneretteResolve
  :1596 if (degeneretteBets[players[0]][betIds[0]] == 0) revert BatchAlreadyTaken();  // AUTO-02 probe — PRESERVE
  :1601 betPacked = degeneretteBets[players[i]][betIds[i]];      // per-item read — PRESERVE
  :1604 currency = (betPacked >> 42) & 0x3;                       // currency decode — the ≥3 count reads this
  :1608-1610 if (currency == 3) { /* zero reward */ }            // WWXRP exclusion — PRESERVE, count NON-WWXRP only
  :1606 try this._autoResolveBet(players[i], betIds[i])           // self-call — rename to this._degeneretteResolveBet
  :1611-1614 reward += _ethToBurnieValue(AUTO_RESOLVE_BET_GAS_UNITS * AUTO_GAS_PRICE_REF, priceForLevel(lvl));  // REMOVE per-item peg → ++successCount
  :1616 catch {}                                                  // per-item isolation — PRESERVE
  :1622 if (reward != 0) coinflip.creditFlip(msg.sender, reward); // REPLACE with successCount>=3 flat + NoWork()-at-0
  :1684 function _autoResolveBet(address player, uint64 betId) external  // → _degeneretteResolveBet (onlySelf :1685; delegatecalls resolveBets :1692)
  :1539 AUTO_GAS_PRICE_REF = 0.5 gwei                             // D-01a — UNCHANGED
  :1545 AUTO_RESOLVE_BET_GAS_UNITS = 66_528                       // D-01a — likely DEAD post-re-peg; grep-decide, do NOT pre-delete
  :1546 AUTO_OPEN_BOX_GAS_UNITS                                   // D-01a — UNCHANGED (autoOpen uses it)
  :1781 _purchaseFor(player, 0, msg.value, bytes32("DGNRS"), payKind);  // KEEP-04 affiliate — DO NOT TOUCH (C1)
  :1790-1796 _ethToBurnieValue(amountWei, priceWei)              // peg conversion
  :2319 public view degeneretteBets[player][betId]               // getter — D-05f inert, leave as-is
</interfaces>

<grep_protocol>
Baseline `0cc5d10f` (live tree byte-identical). RE-GREP `function advanceGame`, `function autoResolve`, `function _autoResolveBet`, `this._autoResolveBet`, `AUTO_RESOLVE_BET_GAS_UNITS`, `bytes32("DGNRS")`, `boxCursor`/`boxPlayers`/`lootboxRngWordByIndex`, `currentDayView` BEFORE editing — plan 330-01 edits a DIFFERENT file so the `DegenerusGame.sol` lines are at baseline, but never trust a raw line number (`feedback_verify_call_graph_against_source`). 329-SPEC §1 R1/R3 + §2 D-05a-g + 329-ATTEST-ROUTER-ADVANCE §D1/§D4/§F1/§F2 + 329-ATTEST-DEGENERETTE-RESOLVE §A/§B are source-of-truth.
</grep_protocol>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decode the design-1 tuple in the advanceGame wrapper (ADV-02 / R1) + add advanceDue() and boxesPending() O(1) views (ROUTER-04 / R3)</name>
  <read_first>
    - contracts/DegenerusGame.sol (`advanceGame` wrapper :275-284 — the `(bool ok, bytes memory data) = …delegatecall(…)` capture and the `if (!ok) _revertDelegate(data)` :284; `_revertDelegate` :1022; `currentDayView()` :462; how the game reads `dailyIdx` (DegenerusGameStorage) and the `LR_MID_DAY` packed flag via `_lrRead(LR_MID_DAY_SHIFT, LR_MID_DAY_MASK)`; `boxCursor` :1551, `boxPlayers` :1562, `lootboxRngWordByIndex` open-gate :1647 and how `activeIndex` for boxes is named/derived — re-grep `boxCursorIndex`/the active box index)
    - 329-SPEC.md §1 R1 (the decode site + the forward) + §1 R3 (the two view predicates + the O(1) requirement) + §2 invariant (b) (the router/wrapper introduces no new in-window read — the wrapper decodes the RETURN, recomputes nothing)
    - 329-ATTEST-ROUTER-ADVANCE.md §D.1 D4 (the exact wrapper decode site) + §F.1 F1/F2 (the advanceDue/boxesPending predicates proven O(1))
  </read_first>
  <action>
    Per ADV-02 + ROUTER-04 + SPEC §1 R1/R3 + §3 step 2. Re-grep the wrapper + view-input anchors first.
    (1) WRAPPER DECODE: change `function advanceGame() external {` (`:275`) to `function advanceGame() external returns (uint8 mult, bool rewardable) {`. On the delegatecall SUCCESS branch (after `if (!ok) _revertDelegate(data);`), decode the captured `data` with `(mult, rewardable) = abi.decode(data, (uint8, bool));` and return the tuple (the wrapper forwards exactly what AdvanceModule returned — no recompute; SPEC R1). Keep the existing `abi.encodeWithSelector(IDegenerusGameAdvanceModule.advanceGame.selector)` delegatecall and the `_revertDelegate(data)` revert path unchanged. (The AdvanceModule producer 330-01 now ABI-encodes `(uint8, bool)` as its delegatecall return — the wrapper decodes it.)
    (2) `advanceDue()` VIEW: add `function advanceDue() external view returns (bool)` returning TRUE when EITHER new-day (`currentDayView() != dailyIdx`) OR mid-day partial-drain (`_lrRead(LR_MID_DAY_SHIFT, LR_MID_DAY_MASK) != 0`). Two scalar/packed SLOADs — NO iteration. Use the exact `dailyIdx` reference + the `LR_MID_DAY` read idiom already used elsewhere in the contract (re-grep).
    (3) `boxesPending()` VIEW: add `function boxesPending() external view returns (bool)` returning TRUE when `boxPlayers[activeIndex].length > boxCursor` AND `lootboxRngWordByIndex[activeIndex] != 0` — reading `.length` (one SLOAD of the dynamic-array length slot) + `boxCursor` + `lootboxRngWordByIndex[activeIndex]`, NO iteration over `boxPlayers`. Use the contract's existing active-box-index accessor (re-grep `boxCursorIndex` / how `autoOpen` selects the index at :1636-1656) so the predicate matches the index the open path consumes.
    Both views MUST be O(1) (ROUTER-04 — no unbounded scans). Comments describe what IS.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && echo "--- wrapper signature + decode ---"; grep -c "function advanceGame() external returns (uint8 mult, bool rewardable)" contracts/DegenerusGame.sol | tr -d "\n"; echo " wrapper tuple sig (MUST be 1)"; grep -c "abi.decode(data, (uint8, bool))" contracts/DegenerusGame.sol | tr -d "\n"; echo " decode on success branch (MUST be 1)"; echo "--- views ---"; grep -c "function advanceDue() external view returns (bool)" contracts/DegenerusGame.sol | tr -d "\n"; echo " advanceDue (MUST be 1)"; grep -c "function boxesPending() external view returns (bool)" contracts/DegenerusGame.sol | tr -d "\n"; echo " boxesPending (MUST be 1)"; echo "--- views are O(1) — no for/while inside them (manual read confirms) ---"; echo "--- build ---"; timeout 400 forge build 2>&1 | grep -ciE "^error|Error \(" | tr -d "\n"; echo " build errors (MUST be 0)"</automated>
  </verify>
  <acceptance_criteria>
    - The wrapper signature is `function advanceGame() external returns (uint8 mult, bool rewardable)` and it `abi.decode(data, (uint8, bool))`s the delegatecall return on the SUCCESS branch, returning the tuple; the `_revertDelegate(data)` revert path is unchanged.
    - `advanceDue()` exists, returns `(bool)`, and is TRUE on new-day (`currentDayView() != dailyIdx`) OR mid-day (`LR_MID_DAY != 0`) — covering BOTH; reading confirms NO loop/iteration.
    - `boxesPending()` exists, returns `(bool)`, reads `boxPlayers[activeIndex].length > boxCursor` AND `lootboxRngWordByIndex[activeIndex] != 0` — reading confirms NO loop over the array (only `.length` + two scalars).
    - The active-box index the predicate uses matches the index `autoOpen` consumes (re-grep evidence in the SUMMARY).
    - `forge build` reports 0 errors.
  </acceptance_criteria>
  <done>The wrapper decodes + forwards `(uint8 mult, bool rewardable)`; `advanceDue()` (new-day OR mid-day) and `boxesPending()` are O(1) views with no unbounded scans; the file compiles.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rename autoResolve→degeneretteResolve + re-peg to flat ≥3-NON-WWXRP-gate + add RESOLVE_FLAT_BURNIE under the D-01b marker block (BATCH-02 / D-05)</name>
  <read_first>
    - contracts/DegenerusGame.sol (`autoResolve` :1587 + its `(players[], betIds[])` args; AUTO-02 probe :1596; per-item read :1601; currency decode :1604; WWXRP fork :1608-1610; the self-call `try this._autoResolveBet(...)` :1606 + `catch {}` :1616; the per-item peg `reward += _ethToBurnieValue(AUTO_RESOLVE_BET_GAS_UNITS * AUTO_GAS_PRICE_REF, priceForLevel(lvl))` :1611-1614; the post-loop `if (reward != 0) coinflip.creditFlip(msg.sender, reward);` :1622; `_autoResolveBet` :1684 onlySelf :1685 delegatecalls resolveBets :1692; the constants `AUTO_GAS_PRICE_REF` :1539 / `AUTO_RESOLVE_BET_GAS_UNITS` :1545 / `AUTO_OPEN_BOX_GAS_UNITS` :1546; `_ethToBurnieValue` :1790-1796; the public getter :2319 — leave as-is)
    - 329-SPEC.md §2 D-05a (rename targets) + D-05b (the exact :1611-1614/:1622 edit + the resolve-always-pay-at-≥3-revert-only-at-0 lean) + D-05d (preserved invariants) + D-05e (RESOLVE_FLAT_BURNIE soft constant, AUTO_RESOLVE_BET_GAS_UNITS likely dead — grep-decide) + the Placeholder note (D-01b marker block)
    - 329-ATTEST-DEGENERETTE-RESOLVE.md §A.1 (the 3 rename targets) + §A.2 (NO interface edit — C6) + §A.4 (the 5 preserved invariants) + §B.1/§B.2 (the exact feasible edit + the targets)
    - 330-CONTEXT.md <decisions> D-01/D-01a/D-01b (the placeholder strategy + the marker-block comment form)
  </read_first>
  <action>
    Per BATCH-02 + D-05a/b/d/e + SPEC §3 step 2. Re-grep `function autoResolve`, `function _autoResolveBet`, `this._autoResolveBet`, `AUTO_RESOLVE_BET_GAS_UNITS`, `creditFlip(msg.sender, reward)` first.
    (1) RENAME (lock-step): `autoResolve` (`:1587`) → `degeneretteResolve` (same external signature `(address[] calldata players, uint64[] calldata betIds)`); `_autoResolveBet` (`:1684`) → `_degeneretteResolveBet` (same `onlySelf` body); the self-call `try this._autoResolveBet(players[i], betIds[i])` (`:1606`) → `this._degeneretteResolveBet(players[i], betIds[i])`. ALL three must rename together (missing the self-call dangles the selector). Do NOT add any interface-file edit (C6 — handled NOT-AT-ALL; both symbols are defined directly on this contract). Leave `autoOpen` and the rest of the `auto*` family UNTOUCHED.
    (2) RE-PEG (the localized arithmetic + boundary swap on the existing per-item loop):
      - REMOVE the per-item peg `reward += _ethToBurnieValue(AUTO_RESOLVE_BET_GAS_UNITS * AUTO_GAS_PRICE_REF, priceForLevel(lvl));` (`:1611-1614`) and replace it with a per-item `++successCount` placed in the NON-WWXRP success arm ONLY (so WWXRP `currency == 3` resolutions do NOT increment the count — AUTO-04 / D-05d preserved). The count increments inside the `try`-success path (counts SUCCESSES, not attempts).
      - Track total resolutions (any-arm, including WWXRP) so the revert-at-zero gate is "any resolution at all" — use a small running counter or a `bool resolvedAny` set in the success path; this distinguishes "ZERO resolved" (revert) from "1–2 non-WWXRP resolved" (resolve-and-pay-0, NO revert).
      - REPLACE the post-loop `if (reward != 0) coinflip.creditFlip(msg.sender, reward);` (`:1622`) with: `if (successCount >= 3) coinflip.creditFlip(msg.sender, RESOLVE_FLAT_BURNIE);` (ONE flat creditFlip, count-INDEPENDENT, fired LAST = CEI) AND a revert when ZERO bets resolved (`if (!resolvedAny) revert NoWork();` — reuse the same `NoWork()` selector the AfKing router declares in 330-04; this plan may need to declare the `error NoWork();` once on `DegenerusGame.sol` if it is not visible here — grep `error NoWork` and add the decl on this contract if absent, since the AfKing decl is a different contract's error and not in scope). The 1–2-resolved case falls through both branches: resolutions commit, NO creditFlip, NO revert (the lean — do NOT revert, which would roll back the resolutions and strand the tail).
      - Remove the now-unused `reward` accumulator if it has no other use after the swap (grep-confirm).
    (3) ADD `RESOLVE_FLAT_BURNIE` constant = `1e18` (~1 BURNIE, D-05b best-guess placeholder, D-01) under a NAMED, GREPPABLE D-01b flagged-marker block (e.g. a labelled comment block the GAS phase locates by name — the marker comment describes what IS: `// SPEC placeholder; calibrated at the GAS phase`, NO history). This is the home for the NEW placeholder constants on `DegenerusGame.sol` (if any other NEW placeholder belongs game-side it goes under the same marker; the GAS_BUDGET/DEFAULT_*_COUNT live AfKing-side in 330-04 under their own marker).
    (4) `AUTO_RESOLVE_BET_GAS_UNITS` (`:1545`): grep for surviving references after the per-item peg is removed. If ZERO references remain, remove it (dead, D-05e). If anything still references it, leave it. Do NOT touch `AUTO_GAS_PRICE_REF` (:1539) or `AUTO_OPEN_BOX_GAS_UNITS` (:1546) — D-01a, untouched (`autoOpen` still uses the latter).
    (5) PRESERVE byte-for-byte: the AUTO-02 probe (`:1596`), the per-item try/catch isolation (`:1606`/`:1616` other than the rename), the WWXRP exclusion fork (`:1608-1610`), self-resolve (no caller gate at `:1587`), the public getter (`:2319`), and the KEEP-04 affiliate wiring `bytes32("DGNRS")` (`:1781`, C1). Comments describe what IS.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && echo "--- rename ---"; grep -c "function degeneretteResolve" contracts/DegenerusGame.sol | tr -d "\n"; echo " degeneretteResolve (MUST be 1)"; grep -c "function _degeneretteResolveBet" contracts/DegenerusGame.sol | tr -d "\n"; echo " _degeneretteResolveBet (MUST be 1)"; grep -c "this._degeneretteResolveBet" contracts/DegenerusGame.sol | tr -d "\n"; echo " self-call renamed (MUST be 1)"; echo "--- old names gone ---"; grep -c "autoResolve\|_autoResolveBet" contracts/DegenerusGame.sol | tr -d "\n"; echo " stale autoResolve refs (MUST be 0)"; echo "--- re-peg ---"; grep -c "RESOLVE_FLAT_BURNIE" contracts/DegenerusGame.sol | tr -d "\n"; echo " RESOLVE_FLAT_BURNIE refs (MUST be ≥2: decl + creditFlip)"; grep -c "successCount >= 3" contracts/DegenerusGame.sol | tr -d "\n"; echo " ≥3 gate (MUST be ≥1)"; echo "--- affiliate untouched ---"; grep -c 'bytes32("DGNRS")' contracts/DegenerusGame.sol | tr -d "\n"; echo " DGNRS affiliate (MUST be ≥1, unchanged)"; echo "--- autoOpen family untouched ---"; grep -c "function autoOpen" contracts/DegenerusGame.sol | tr -d "\n"; echo " autoOpen (MUST be 1, unchanged)"; echo "--- build ---"; timeout 400 forge build 2>&1 | grep -ciE "^error|Error \(" | tr -d "\n"; echo " build errors (MUST be 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `degeneretteResolve`, `_degeneretteResolveBet`, and `this._degeneretteResolveBet` all present (count 1 each); NO surviving `autoResolve`/`_autoResolveBet` reference anywhere in the file (count 0).
    - NO interface-file edit was made for the rename (C6 — this plan touches only `DegenerusGame.sol`; `git diff` shows no `contracts/interfaces/` hunk from this plan).
    - The per-item peg at the former `:1611-1614` is gone; the NON-WWXRP arm does `++successCount`; the post-loop pays `coinflip.creditFlip(msg.sender, RESOLVE_FLAT_BURNIE)` only when `successCount >= 3`; ZERO resolutions → `revert NoWork()`; 1–2 resolved → no creditFlip AND no revert (reading the control flow confirms the resolve-always-pay-at-≥3-revert-only-at-0 lean).
    - WWXRP (`currency == 3`) does NOT increment `successCount` (the increment is inside the NON-WWXRP arm only) — AUTO-04 preserved.
    - `RESOLVE_FLAT_BURNIE = 1e18` is declared under a named, greppable D-01b flagged-marker block whose comment describes what IS (no history).
    - `AUTO_RESOLVE_BET_GAS_UNITS` removed ONLY if grep showed zero surviving references; `AUTO_GAS_PRICE_REF`/`AUTO_OPEN_BOX_GAS_UNITS` byte-unchanged (D-01a).
    - AUTO-02 probe, per-item try/catch, WWXRP fork, self-resolve, the public getter, and the `bytes32("DGNRS")` affiliate wiring are byte-unchanged (other than the mechanical rename); `autoOpen` is untouched.
    - `forge build` reports 0 errors.
  </acceptance_criteria>
  <done>`autoResolve`/`_autoResolveBet` are renamed in lock-step to `degeneretteResolve`/`_degeneretteResolveBet` with no stale refs and no interface edit; the reward is the flat `RESOLVE_FLAT_BURNIE` ≥3-NON-WWXRP-gate / revert-on-zero shape under the D-01b marker; the preserved invariants and the affiliate path are byte-intact; the file compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| AdvanceModule delegatecall → Game wrapper | the wrapper decodes the design-1 return; a mis-decode would feed the router a wrong `(mult, rewardable)` and mis-pay or mis-gate the re-homed bounty. |
| keeper → degeneretteResolve reward | the flat ~1-BURNIE ≥3-gate reward; the threat is a positive-EV farm (refuted by D-05c: real-gas net-loss) and a value/RNG-result change from the rename/re-peg (must be results-identical). |
| off-chain keeper → advanceDue()/boxesPending() | the O(1) discovery views are the pre-call probe; an unbounded scan would be a gas-DoS / mis-route surface (ROUTER-04). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-330-02-01 | Tampering | wrapper decodes the wrong tuple shape → mis-paid/mis-gated bounty | mitigate | `abi.decode(data, (uint8, bool))` matches the exact `(uint8 mult, bool rewardable)` AdvanceModule (330-01) now returns; the wrapper recomputes nothing (forwards the single-sourced value — GAS-03 / invariant d). Empirical reward-routing proof = TST-03 (Phase 332). |
| T-330-02-02 | Denial of Service | `advanceDue()`/`boxesPending()` do an unbounded scan (ROUTER-04 violation) | mitigate | Both views are O(1): `advanceDue()` = two scalar/packed SLOADs; `boxesPending()` = `.length` + two scalars, NO iteration over `boxPlayers`. Reading the view bodies confirms zero loops (329-ATTEST §F1/§F2). |
| T-330-02-03 | Elevation of Privilege / Information Disclosure | the rename/re-peg changes resolution RESULTS (payout/RNG) | mitigate | The re-peg is a localized arithmetic + boundary swap on the existing `:1587-1622` loop — the resolution path itself (`_degeneretteResolveBet`→`resolveBets` delegatecall, the per-bet payout/RNG, the `delete`) is UNCHANGED; only the keeper's reward arithmetic + the pay/revert boundary change. WWXRP stays excluded from the count. Byte-identical-results proof = TST-05 (Phase 332). |
| T-330-02-04 | Spoofing / Tampering | the flat ~1-BURNIE reward becomes a positive-EV farm | accept | D-05c: 1 BURNIE ≤ mintPrice/1000 ≤ 0.00024 ETH, illiquid (coinflip flip-credit); the keeper pays REAL gas ≥220k for the ≥3 minimum × 5–50+ gwei = ~4.6–46× the peg ceiling → net loss at any realistic gas; the ≥3 gate widens the margin. Constant is a placeholder; GAS-06 sanity-check confirms sub-real-gas at Phase 331. |
| T-330-02-05 | Denial of Service | a revert at <3 resolved rolls back the tail (strands losing bets) | mitigate | The lean = resolve-always, pay-at-≥3, revert-ONLY-at-0: 1–2-resolved commits its `delete`s unpaid (no revert); only a genuinely-empty call reverts `NoWork()`. D-05f confirmed losing-bet liveness is INERT-SAFE — no path requires losing bets resolved. |
| T-330-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. No `## Package Legitimacy Audit` required. |
</threat_model>

<verification>
- `forge build` 0 errors over the touched file.
- The wrapper decodes + forwards `(uint8 mult, bool rewardable)`; `advanceDue()` and `boxesPending()` are O(1) (no loops).
- `degeneretteResolve`/`_degeneretteResolveBet`/`this._degeneretteResolveBet` present; zero stale `autoResolve` refs; NO interface edit (C6).
- The flat ≥3-NON-WWXRP-gate / revert-on-zero re-peg + `RESOLVE_FLAT_BURNIE = 1e18` under the D-01b marker; AUTO-02/try-catch/WWXRP/self-resolve/affiliate byte-intact.
- No git commit performed (batched-diff discipline).
</verification>

<success_criteria>
- ROADMAP SC1 (ROUTER-04 — O(1) `advanceDue()`/`boxesPending()`, no unbounded scans) + SC3 (ADV-02 — the wrapper decodes the design-1 return on the success branch + forwards it) + SC2 (ROUTER-05 — `autoResolve` excluded from the router + RENAMED to `degeneretteResolve`) + SC5 (BATCH-02 — the rename + flat ≥3-gate re-peg rides the same diff, RESOLVE_FLAT_BURNIE under the D-01b marker) satisfied on the game-side surface the interfaces (330-03) and the AfKing router (330-04) consume.
</success_criteria>

<output>
Create `.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-02-SUMMARY.md` when done. Record: the re-grepped current lines, the wrapper decode + the two view bodies (confirm O(1)), the active-box-index match, the rename map + zero-stale confirmation, the re-peg control flow (≥3 gate / revert-on-0 / 1–2-pay-0 lean / WWXRP-excluded count), the `RESOLVE_FLAT_BURNIE`/`NoWork()` decl decisions, the `AUTO_RESOLVE_BET_GAS_UNITS` grep-decision, and confirmation the affiliate wiring + autoOpen are byte-untouched. Do NOT git commit.
</output>
