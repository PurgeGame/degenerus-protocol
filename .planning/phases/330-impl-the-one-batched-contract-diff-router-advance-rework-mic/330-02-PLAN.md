---
phase: 330-impl-the-one-batched-contract-diff-router-advance-rework-mic
plan: 02
type: execute
wave: 2
depends_on: ["330-01"]
files_modified:
  - contracts/DegenerusGame.sol
autonomous: true
requirements: [ADV-02, ROUTER-04, ROUTER-09, ROUTER-10, GASOPT-03]
must_haves:
  truths:
    - "The DegenerusGame.advanceGame wrapper decodes the delegatecall return into (uint8 mult, bool rewardable) and forwards it (no longer discards data on success)"
    - "advanceDue() / boxesPending() O(1) discovery views exist; boxesPending() is rngLock-aware (false during rngLock, true for mid-day-resolved rounds whose word landed)"
    - "autoOpen is rngLock-blocked via an entry-gate (rngLocked || _livenessTriggered → return), drops its try/catch for an internal _autoOpenBox call, returns the raw open count, and no longer pays an in-callee bounty"
    - "The game-side batchPurchase rngLock pre-check (if (rngLockedFlag) revert RngLocked(); ~:1737) is REMOVED; the immediately-following gameOver pre-check (~:1738) is KEPT (RD-2 / ROUTER-08 / Q5 — the game-side half of the buy-path freeze-safety; the AfKing :568 half is owned by 330-06)"
    - "A batched keeperSnapshot read collapses the keeper's two per-player claimableWinningsOf STATICCALLs into one call"
  artifacts:
    - path: "contracts/DegenerusGame.sol"
      provides: "wrapper tuple decode + advanceDue/boxesPending views + reworked autoOpen + batchPurchase :1737 rngLock pre-check removal + keeperSnapshot"
      contains: "function advanceDue"
  key_links:
    - from: "DegenerusGame.advanceGame wrapper"
      to: "(uint8 mult, bool rewardable)"
      via: "abi.decode of the delegatecall return data (was discarded)"
      pattern: "abi.decode\\(data"
    - from: "autoOpen"
      to: "rngLocked() || _livenessTriggered()"
      via: "pre-loop entry-gate return"
      pattern: "_livenessTriggered\\(\\)"
---

<objective>
DegenerusGame is the SECOND edit in the batched diff (329-SPEC §3 step 2): consume R1's advance return + produce R3's discovery views + R4's autoOpen pull-out + the game-side RD-2 buy-path freeze-safety half + GASOPT-03. Five router-surface changes on `DegenerusGame.sol`:

1. **Wrapper tuple decode (ADV-02):** the `advanceGame()` wrapper (`:275`) delegatecalls the module and currently DISCARDS `data` on success (`:283`). Add `returns (uint8 mult, bool rewardable)` and `abi.decode` the delegatecall return into the tuple, forwarding it.
2. **rngLock-aware O(1) discovery views (ROUTER-04):** add `advanceDue()` (new-day OR mid-day partial-drain pending; true regardless of rngLock) and `boxesPending()` (rngLock-aware — false during rngLock, true for mid-day-resolved rounds whose word landed). Both O(1), no unbounded scan.
3. **autoOpen RD-3/RD-5 rework (ROUTER-09/10):** add an entry-gate `if (rngLocked() || _livenessTriggered()) return …;`, DROP the per-item `try this._autoOpenBox` / `catch {}` for a direct internal `_autoOpenBox(index, player)` call (make `_autoOpenBox` internal), pull OUT the in-callee `creditFlip` (`:1676`, U4) so the leg returns the raw open count, and delete the open-leg gas-units bounty conversion.
4. **batchPurchase rngLock pre-check removal — game-side RD-2 half (ROUTER-08 / Q5):** DELETE the one-line rngLock pre-check `if (rngLockedFlag) revert RngLocked();` at `batchPurchase` (`~:1737`); KEEP the `if (gameOver) revert E();` (`~:1738`) immediately following it. This is the game-side half of the buy-path freeze-safety; the AfKing `_autoBuy` `:568` entry-guard half is owned by 330-06 (ROUTER-08 is attributed to 330-06 for requirement coverage, but the physical `:1737` line lives in `DegenerusGame.sol` so it lands HERE — single-editor-per-file). Removing only the AfKing `:568` guard without this one would just revert deeper at `:1737`; both must go together.
5. **GASOPT-03 batched keeper read:** add a game-side `keeperSnapshot` view collapsing the two per-player `claimableWinningsOf` STATICCALLs the keeper makes (`AfKing:691`/`:722`) into ONE batched call (SUBSUMES GASOPT-02).

The `degeneretteResolve` rename + flat ≥3 re-peg is a SEPARATE plan (330-03) on the same file — do NOT touch the `autoResolve` path here.

CROSS-PLAN NOTE (RD-2 / ROUTER-08): 330-06 owns the AfKing `_autoBuy` `:568` rngLock guard removal AND the ROUTER-08 requirement attribution; 330-06 Task 4 is a DOCUMENT-ONLY task that defers the game-side `:1737` removal to THIS plan. So: the `:1737` deletion is performed HERE (Task 2 below), recorded in this SUMMARY, and 330-06's Task 4 + the 330-09 BATCH-02 gate cross-check that it landed. Neither executor should assume the other did it — this plan DOES the edit, 330-06 VERIFIES it.

Purpose: settle the game-side surface the router (330-04 AfKing) consumes, including the game-side buy-path freeze-safety half.
Output: `contracts/DegenerusGame.sol` with the wrapper decode, two discovery views, reworked autoOpen, the batchPurchase `:1737` rngLock pre-check removed (`:1738` kept), and `keeperSnapshot`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md

<interfaces>
<!-- Attested @ 0cc5d10f (329-ATTEST-ROUTER-ADVANCE.md §D.1 D4, §B.1 B2/B3, §E, §F.2). Re-grep at edit time. -->

WRAPPER (D4, `:275`): currently `function advanceGame() external { (bool ok, bytes memory data) = ContractAddresses.GAME_ADVANCE_MODULE.delegatecall(abi.encodeWithSelector(IDegenerusGameAdvanceModule.advanceGame.selector)); if (!ok) _revertDelegate(data); }`. The success path discards `data`. → add `returns (uint8 mult, bool rewardable)`; on success `(mult, rewardable) = abi.decode(data, (uint8, bool));`.

AUTOOPEN current shape (§E.1, `:1636-1676`): `function autoOpen(uint256 maxCount) external { uint48 index = uint48(_lrRead(LR_INDEX_SHIFT, LR_INDEX_MASK)); if (boxCursorIndex != index) { boxCursorIndex = index; boxCursor = 0; } if (lootboxRngWordByIndex[index] == 0) return; … while (cursor < qlen && opened < maxCount) { … ++cursor; if (lootboxEthBase[index][player] == 0) continue; try this._autoOpenBox(index, player) { reward += _ethToBurnieValue(AUTO_OPEN_BOX_GAS_UNITS * AUTO_GAS_PRICE_REF, PriceLookupLib.priceForLevel(lvl)); ++opened; } catch {} } boxCursor = uint48(cursor); if (reward != 0) coinflip.creditFlip(msg.sender, reward); }`.

_autoOpenBox current (§E.1 E8, `:1705`): `function _autoOpenBox(uint48 index, address player) external { if (msg.sender != address(this)) revert E(); _openLootBoxFor(player, index); }`. → make INTERNAL: `function _autoOpenBox(uint48 index, address player) internal { _openLootBoxFor(player, index); }` (drop the self-guard — internal can't be called externally).

ENTRY-GATE BASIS (§E.2): exactly two open-path revert sources — rngLock (use the internal `rngLockedFlag` storage var, or the `rngLocked()` external) + `_livenessTriggered()` (internal view, defined in storage at `:1213`, inherited; the terminal-jackpot liveness control reached via `_openLootBoxFor → … → _queueTickets`). The entry-gate `if (rngLockedFlag || _livenessTriggered()) return …;` replicates both pre-loop → brick-proof (MANDATORY now that try/catch is dropped, §E.2 verdict).

BATCHPURCHASE GUARDS (§B.1 B1/B2/B3, the RD-2 game-side half): `batchPurchase(...)` is defined ONCE (`:1731`), AF_KING-only gated (`:1736` `if (msg.sender != ContractAddresses.AF_KING) revert E();`). The two pre-checks immediately inside:
- `:1737` `if (rngLockedFlag) revert RngLocked();` → **REMOVE** (RD-2; this is the BARE-predicate batchPurchase pre-check).
- `:1738` `if (gameOver) revert E();` → **KEEP** (terminal state, unrelated to the freeze).
§B.2 Q5 grep PERFORMED: `batchPurchase` has NO dependent besides the keeper (`AfKing:821`, the ONLY external caller); not declared in either interface; removing `:1737` affects ONLY the keeper buy path. DISTINCT from the FAR-FUTURE revert at `_queueTickets :573` `if (isFarFuture && rngLockedFlag && !rngBypass) revert RngLocked();` — that is a COMPOUND-predicate revert on a different (far-future ticket) path, fires on neither the open nor the batchPurchase path, and LEGITIMATELY REMAINS. So a grep of the BARE form `if (rngLockedFlag) revert RngLocked();` distinguishes the batchPurchase pre-check (must go to 0) from the `:573` far-future compound form (untouched).

BOX STATE for boxesPending (§F.2 / §E.3): `boxCursor` (`:1551`, uint48), `boxCursorIndex` (`:1554`, uint48), `boxPlayers[index]` (`mapping(uint48 => address[])`, `.length`), `lootboxRngWordByIndex[index]` (the word-gate, read at `:1647`), active index via `_lrRead(LR_INDEX_SHIFT, LR_INDEX_MASK)`. Form (§E.3): `boxPlayers[activeIndex].length > boxCursor AND lootboxRngWordByIndex[activeIndex] != 0 AND !rngLockedFlag` — but use `boxCursorIndex` reconciliation: if `boxCursorIndex != activeIndex` the cursor effectively resets to 0, so the pending check is `boxPlayers[activeIndex].length > (boxCursorIndex == activeIndex ? boxCursor : 0)`. O(1), no scan.

ADVANCEDUE basis (§F.2): `currentDayView() :462 (= _simulatedDayIndex() :463) != dailyIdx` (new-day) OR a mid-day partial-drain pending (`!ticketsFullyProcessed` with `ticketQueue[_tqReadKey(...)].length > 0`, OR `_lrRead(LR_MID_DAY_SHIFT, LR_MID_DAY_MASK) != 0`). True regardless of rngLock (advance is liveness-critical). Read `dailyIdx` (storage, `:231`), `ticketsFullyProcessed` (storage, `:304`). O(1).

GASOPT-03 (§C.2 C3): the keeper makes two per-player `claimableWinningsOf(player)` reads (`AfKing:691` the reinvest term, `:722` the drain-first waterfall). Add a game-side batched read `function keeperSnapshot(address[] calldata players) external view returns (uint256 mintPriceWei, bool rngLocked_, uint256[] memory claimables)` — one call returning mintPrice + rngLocked + per-player claimable, so the keeper does ONE STATICCALL for the chunk instead of N per-player calls. (The keeper's autoBuy already reads `mintPrice()` and `rngLocked()` per call; fold them in too so the keeper makes exactly one game read per chunk.) Behavior-identical values.

`coinflip` handle (used by the U4 site being removed): `coinflip.creditFlip(...)` — the handle stays (still used at `:1498` boon + the degeneretteResolve `:1622` site owned by 330-03). Removing the autoOpen U4 site does not remove the handle.

`AUTO_OPEN_BOX_GAS_UNITS` (`:1546`) + the open-leg `_ethToBurnieValue(AUTO_OPEN_BOX_GAS_UNITS * AUTO_GAS_PRICE_REF, …)` conversion: the open-leg conversion is DELETED (the reward moves to the router's D-07 flat-per-tx `1×` pro-rated model, 330-04). `AUTO_OPEN_BOX_GAS_UNITS` becomes dead after this — delete it. `AUTO_GAS_PRICE_REF` (`:1539`) is STILL used by the degeneretteResolve per-item peg until 330-03 re-pegs it — leave `AUTO_GAS_PRICE_REF` for 330-03 to retire.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wrapper tuple decode (ADV-02) + advanceDue()/boxesPending() O(1) views (ROUTER-04)</name>
  <read_first>
    - contracts/DegenerusGame.sol (the advanceGame wrapper :275-285; currentDayView :462; the box-state decls :1551/:1554; dailyIdx/ticketsFullyProcessed via the storage base; rngLocked :2413)
    - contracts/storage/DegenerusGameStorage.sol (dailyIdx :231, ticketsFullyProcessed :304, _simulatedDayIndex :1178, _livenessTriggered :1213, the LR_MID_DAY_SHIFT/LR_INDEX_SHIFT masks)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md (§D.1 D4 wrapper, §F.2 the view forms)
  </read_first>
  <action>
    WRAPPER: change `advanceGame()` (`:275`) to `function advanceGame() external returns (uint8 mult, bool rewardable)`. Keep the delegatecall + `if (!ok) _revertDelegate(data);`. On the success path, replace the discard with `(mult, rewardable) = abi.decode(data, (uint8, bool));` (the module now returns this tuple per 330-01). Do NOT change the selector or the delegatecall target.

    advanceDue(): add `function advanceDue() external view returns (bool)` returning true when EITHER (a) `_simulatedDayIndex() != dailyIdx` (new-day advance pending) OR (b) a mid-day partial-drain is pending — `!ticketsFullyProcessed && ticketQueue[_tqReadKey(_purchaseLevelForView())].length > 0`, where the level used matches the mid-day path's `purchaseLevel`/read-key (read the actual mid-day key derivation in advanceGame `:202-225` and mirror it in a view-safe form; if a simpler O(1) signal exists — e.g. `_lrRead(LR_MID_DAY_SHIFT, LR_MID_DAY_MASK) != 0` combined with `!ticketsFullyProcessed` — prefer it). True regardless of rngLock. Keep it O(1) — no unbounded scan; `.length` reads only.

    boxesPending(): add `function boxesPending() external view returns (bool)`. Compute the active index `idx = uint48(_lrRead(LR_INDEX_SHIFT, LR_INDEX_MASK))`; the effective cursor is `boxCursorIndex == idx ? boxCursor : 0`; return `boxPlayers[idx].length > effectiveCursor && lootboxRngWordByIndex[idx] != 0 && !rngLockedFlag` (RD-3 rngLock-aware: false during rngLock, true for a mid-day-resolved round whose word has landed). O(1).
  </action>
  <verify>
    <automated>grep -c "function advanceGame() external returns (uint8 mult, bool rewardable)" contracts/DegenerusGame.sol | grep -qx 1 && grep -c "abi.decode(data" contracts/DegenerusGame.sol | grep -q "[1-9]" && grep -c "function advanceDue() external view returns (bool)" contracts/DegenerusGame.sol | grep -qx 1 && grep -c "function boxesPending() external view returns (bool)" contracts/DegenerusGame.sol | grep -qx 1 && echo PASS</automated>
  </verify>
  <done>The wrapper returns and decodes `(uint8 mult, bool rewardable)`; `advanceDue()` and `boxesPending()` exist as O(1) views; `boxesPending()` reads `rngLockedFlag` (false during lock) + the word-gate; no unbounded scan.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: autoOpen RD-3/RD-5 rework (ROUTER-09/10) + game-side batchPurchase :1737 rngLock pre-check removal (RD-2 / ROUTER-08 / Q5)</name>
  <read_first>
    - contracts/DegenerusGame.sol (autoOpen :1636-1676; _autoOpenBox :1705-1708; the AUTO_OPEN_BOX_GAS_UNITS decl :1546; the coinflip handle usage; the batchPurchase pre-checks :1736-1738; the far-future _queueTickets revert :573)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md (§E.1 E1-E8 + §E.2 the entry-gate correctness proof + §E.3; §B.1 B2/B3 the batchPurchase :1737 remove / :1738 keep + §B.2 the Q5 dependent grep)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md (§2 RD-3/RD-5, ROUTER-09/10; RD-2/ROUTER-08; the "entry-gate is MANDATORY if try/catch goes" lock)
  </read_first>
  <action>
    Change `autoOpen(uint256 maxCount)` to `function autoOpen(uint256 maxCount) external returns (uint256 opened)`. Add the entry-gate as the FIRST statement: `if (rngLockedFlag || _livenessTriggered()) return 0;` (replicates BOTH open-path revert sources pre-loop — RD-5 brick-proof basis). KEEP the existing per-index word-gate `if (lootboxRngWordByIndex[index] == 0) return 0;` and the `boxCursorIndex != index` cursor-reset.

    In the loop: DROP the `try this._autoOpenBox(index, player) { … } catch {}` wrapper — call `_autoOpenBox(index, player);` directly (internal), then `unchecked { ++opened; }`. KEEP the `if (lootboxEthBase[index][player] == 0) continue;` skip and the `++cursor` BEFORE the body (since the body is now guaranteed-non-reverting under the entry-gate, the cursor-advance-first is brick-safe — no half-walk marooning). REMOVE the per-item `reward += _ethToBurnieValue(AUTO_OPEN_BOX_GAS_UNITS * AUTO_GAS_PRICE_REF, …)` accumulation and the post-loop `if (reward != 0) coinflip.creditFlip(msg.sender, reward);` (U4 pull-out, ROUTER-10 — the open bounty moves to the router's flat-per-tx model). KEEP `boxCursor = uint48(cursor);`. The function now `return opened;` (the raw open count the router uses to compute the D-07 `1× × min(opened, KNEE)/KNEE` reward).

    Make `_autoOpenBox` INTERNAL: `function _autoOpenBox(uint48 index, address player) internal { _openLootBoxFor(player, index); }` — drop the `if (msg.sender != address(this)) revert E();` self-guard (internal functions cannot be called externally, so the guard is dead). Grep-confirm `_autoOpenBox` has no remaining `this._autoOpenBox` external-call site after the autoOpen edit.

    DELETE the now-dead `AUTO_OPEN_BOX_GAS_UNITS` constant (`:1546`) once the open-leg conversion is removed (grep 0 remaining references). Leave `AUTO_GAS_PRICE_REF` (`:1539`) — 330-03's degeneretteResolve re-peg retires it.

    BATCHPURCHASE :1737 (RD-2 / ROUTER-08 / Q5 — the game-side half of the buy-path freeze-safety): DELETE the one-line rngLock pre-check `if (rngLockedFlag) revert RngLocked();` at `DegenerusGame.batchPurchase` (`~:1737`); KEEP the `if (gameOver) revert E();` (`~:1738`) immediately following it. Removing ONLY the AfKing `_autoBuy` `:568` guard (330-06) without this one would just revert deeper here, so both go together; `batchPurchase` is AF_KING-gated with NO dependent besides the keeper (§B.2 Q5), so this affects only the keeper buy path. This deletion targets the BARE-predicate form `if (rngLockedFlag) revert RngLocked();` — do NOT touch the FAR-FUTURE compound revert at `_queueTickets :573` `if (isFarFuture && rngLockedFlag && !rngBypass) revert RngLocked();` (different path, legitimately remains). After the edit, the bare batchPurchase pre-check count must be 0 and the far-future compound revert must still be present (1). CROSS-PLAN: ROUTER-08 is attributed to 330-06 for requirement coverage, but this physical line lives in `DegenerusGame.sol` so the edit lands HERE (single-editor-per-file); 330-06 Task 4 is a DOCUMENT-ONLY task that verifies this landed and flags the 330-09 BATCH-02 gate if it did not. Record the `:1737` removal + `:1738` keep in this plan's SUMMARY so 330-06's reconciliation finds it.

    NOTE: `autoOpen` becoming UNREWARDED here is correct — the standalone parametered `autoOpen(count)` stays a manual/emergency clear (unrewarded); only the router (`doWork`, 330-04) pays. The KEEP-04 affiliate / orphan-index re-issue coupling (the word-gate) is untouched.
  </action>
  <verify>
    <automated>grep -c "function autoOpen(uint256 maxCount) external returns (uint256 opened)" contracts/DegenerusGame.sol | grep -qx 1 && grep -c "if (rngLockedFlag || _livenessTriggered()) return 0;" contracts/DegenerusGame.sol | grep -qx 1 && grep -c "function _autoOpenBox(uint48 index, address player) internal" contracts/DegenerusGame.sol | grep -qx 1 && grep -c "this._autoOpenBox" contracts/DegenerusGame.sol | grep -qx 0 && grep -v '^[[:space:]]*//' contracts/DegenerusGame.sol | grep -c "AUTO_OPEN_BOX_GAS_UNITS" | grep -qx 0 && grep -cF "if (rngLockedFlag) revert RngLocked();" contracts/DegenerusGame.sol | grep -qx 0 && grep -cF "if (isFarFuture && rngLockedFlag && !rngBypass) revert RngLocked();" contracts/DegenerusGame.sol | grep -qx 1 && grep -cF "if (gameOver) revert E();" contracts/DegenerusGame.sol | grep -q "[1-9]" && echo PASS</automated>
  </verify>
  <done>autoOpen has the `rngLockedFlag || _livenessTriggered()` entry-gate, no try/catch, calls internal `_autoOpenBox`, returns the raw `opened` count, pays no in-callee bounty; `_autoOpenBox` is internal with no self-guard and no external call site; `AUTO_OPEN_BOX_GAS_UNITS` is gone. The bare `batchPurchase` rngLock pre-check `if (rngLockedFlag) revert RngLocked();` (`~:1737`) is REMOVED (count 0), the far-future compound revert (`:573`) is UNTOUCHED (count 1), and the `gameOver` pre-check (`~:1738`) is KEPT (count ≥1); the `:1737` removal + `:1738` keep are recorded in the SUMMARY for 330-06's cross-check.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: GASOPT-03 batched keeperSnapshot read (SUBSUMES GASOPT-02)</name>
  <read_first>
    - contracts/DegenerusGame.sol (mintPrice :2398, rngLocked :2413, the existing claimableWinningsOf view — grep its definition; the autoBuy keeper reads at AfKing:691/:722 for the value semantics)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md (§C.2 C3 — GASOPT-03 SUBSUMES GASOPT-02; new function + interface surface)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md (§3 step 2 GASOPT-03 keeperSnapshot)
  </read_first>
  <action>
    Add a game-side batched view `function keeperSnapshot(address[] calldata players) external view returns (uint256 mintPriceWei, bool rngLocked_, uint256[] memory claimables)` that returns `mintPrice()`'s value, `rngLockedFlag`, and a parallel array of `claimableWinningsOf(players[i])` for each supplied player — computed with the SAME accessors the keeper uses today (identical values; this is purely call-count reduction, the keeper does ONE STATICCALL per chunk instead of `mintPrice` + `rngLocked` + N×`claimableWinningsOf`). Use the existing internal claimable accessor (do not duplicate accounting logic — call the same internal getter `claimableWinningsOf` resolves to). Keep it a pure view (no state writes). The keeper (330-04) calls this once at the top of its chunk and reads per-player claimable from the returned array by index.

    Do NOT remove the existing `claimableWinningsOf(address)` / `mintPrice()` / `rngLocked()` single-value views — `keeperSnapshot` is additive; other callers (and the standalone autoBuy if it remains) still use them.
  </action>
  <verify>
    <automated>grep -c "function keeperSnapshot(address\[\] calldata players) external view returns (uint256 mintPriceWei, bool rngLocked_, uint256\[\] memory claimables)" contracts/DegenerusGame.sol | grep -qx 1 && echo PASS</automated>
  </verify>
  <done>`keeperSnapshot(address[])` returns `(mintPriceWei, rngLocked_, claimables[])` from the same accessors; additive (single-value views retained); pure view, no writes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keeper → DegenerusGame.autoOpen | permissionless open; the entry-gate replaces the per-box try/catch isolation |
| keeper → DegenerusGame.advanceGame wrapper | the decoded `(mult, rewardable)` becomes a router bounty |
| keeper → DegenerusGame.batchPurchase | AF_KING-gated buy path; the `:1737` rngLock pre-check is the game-side freeze-safety half being removed |
| open path → _queueTickets terminal-jackpot liveness control | the deliberate anti-manipulation revert that MUST stay for direct openLootBox callers |
| keeper → keeperSnapshot | a read-only batched accessor; no state edge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-330-02-01 | Denial of Service | dropping autoOpen try/catch (RD-5) | mitigate | The entry-gate `rngLockedFlag || _livenessTriggered()` excludes BOTH the exactly-two open-path revert sources PRE-LOOP (329-ATTEST §E.2 — exhaustively enumerated), so the loop body is guaranteed-non-reverting → the cursor-advance-first cannot maroon a tail. The terminal-jackpot guard (`storage:571`) stays intact for direct `openLootBox` callers. USER-accepted frozen-contract trade. |
| T-330-02-02 | Tampering | terminal-jackpot manipulation via autoOpen during liveness | mitigate | `_livenessTriggered()` in the entry-gate makes autoOpen decline exactly when `_queueTickets` would revert (it cannot queue tickets anyway); the deliberate liveness control is not weakened — only mirrored at entry. |
| T-330-02-03 | Information Disclosure / Tampering | boxesPending() leaking a stale "open" signal during rngLock | mitigate | `boxesPending()` ANDs `!rngLockedFlag` (RD-3) so it returns false during the freeze — the router never routes to the open leg during rngLock; it stays true only for mid-day-resolved rounds whose word landed and we are not locked. |
| T-330-02-04 | Tampering | keeperSnapshot returning values that diverge from the per-call accessors | mitigate | `keeperSnapshot` reuses the SAME internal accessors (`claimableWinningsOf` getter + `mintPrice()` + `rngLockedFlag`) — identical values, fewer calls. Same-results proven at TST-03 (Phase 332). |
| T-330-02-05 | Elevation of Privilege | _autoOpenBox made internal | accept | Internal visibility is strictly more restrictive than the external onlySelf it replaces (no external call surface at all); the self-guard becomes dead and is removed. |
| T-330-02-06 | Denial of Service | removing the batchPurchase :1737 rngLock pre-check (RD-2) | mitigate | Q1 RESOLVED SAFE (329-ATTEST §A8/§B): the guard was v46 batch-hygiene (Phase 317 `df4ef365`), NOT orphan-index defense — buys queue at the CURRENT LR_INDEX (pre-entropy by construction); the v45 orphan hazard is defended on the RESOLUTION side (autoOpen word-gate, untouched). §B.2 Q5 grep confirms `batchPurchase` is AF_KING-gated with the keeper (`AfKing:821`) as the ONLY external caller, so the removal affects only the keeper path. The `:1738` `gameOver` pre-check is KEPT; the far-future `_queueTickets :573` compound rngLock revert is untouched. Both the AfKing `:568` AND this game-side `:1737` guard are dropped together (330-06 owns `:568` + the ROUTER-08 attribution; this plan does the `:1737` edit, 330-06 Task 4 verifies). |
| T-330-02-SC | Tampering | npm/forge installs | mitigate | No package installs (source edit only). |
</threat_model>

<verification>
- The wrapper no longer discards the delegatecall `data` on success (decodes the tuple).
- autoOpen is rngLock-blocked (entry-gate) and brick-proof (no try/catch, guaranteed-non-reverting body) — proven empirically at TST-01 (autoOpen-blocked-during-rngLock, no marooned boxes) Phase 332.
- `boxesPending()` is rngLock-aware (false during lock). `advanceDue()` is true regardless of rngLock.
- The game-side `batchPurchase` rngLock pre-check (`:1737`) is removed and the `gameOver` pre-check (`:1738`) kept; the buy path is freeze-safe with BOTH this half AND the AfKing `:568` half gone (330-06) — proven at TST-01 (autoBuy-during-rngLock safe) Phase 332; cross-checked at the 330-09 BATCH-02 gate.
- Compiles within the BATCH-02 `forge build` (330-08).
</verification>

<success_criteria>
- ADV-02: wrapper decodes `(uint8 mult, bool rewardable)`.
- ROUTER-04: `advanceDue()` + `boxesPending()` (rngLock-aware) O(1) views exist.
- ROUTER-09: autoOpen entry-gate `rngLocked()||_livenessTriggered()`, try/catch dropped, `_autoOpenBox` internal.
- ROUTER-10: the autoOpen in-callee creditFlip is pulled out; autoOpen returns the raw open count.
- ROUTER-08 (game-side half): the `batchPurchase :1737` rngLock pre-check removed, `:1738` gameOver kept (RD-2 / Q5); ROUTER-08 requirement attribution remains on 330-06, this plan performs the physical edit and 330-06 Task 4 reconciles it.
- GASOPT-03: `keeperSnapshot` batched read added (SUBSUMES GASOPT-02).
</success_criteria>

<output>
Create `.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-02-SUMMARY.md` when done.
</output>
