---
phase: 330-impl-the-one-batched-contract-diff-router-advance-rework-mic
plan: 08
type: execute
wave: 7
depends_on: ["330-03", "330-07"]
files_modified:
  - test/fuzz/CrankFaucetResistance.t.sol
  - test/fuzz/RngFreezeAndRemovalProofs.t.sol
  - test/gas/CrankResolveBetWorstCaseGas.t.sol
  - test/fuzz/CrankNonBrick.t.sol
  - test/gas/CrankLeversAndPacking.t.sol
  - test/fuzz/AfKingConcurrency.t.sol
  - test/fuzz/AfKingSubscription.t.sol
  - test/fuzz/AfKingFundingWaterfall.t.sol
  - test/gas/SweepPerPlayerWorstCaseGas.t.sol
autonomous: true
requirements: [GASOPT-04]
must_haves:
  truths:
    - "Every autoResolve/_autoResolveBet reference across the 5 rename test files (57 refs) is updated to degeneretteResolve/_degeneretteResolveBet, INCLUDING the CrankLeversAndPacking literal source-string assertions"
    - "The GASOPT-04 AutoBought-event oracle is migrated to lastAutoBoughtDay / pool-balance-delta across the 4 AfKing-family files; the no-double-buy invariant _countAutoBoughtFor(sub)==1 is re-expressed in storage terms WITHOUT weakening SAFE-03 / H-CANCEL-SWAP"
    - "The 2 collision files (CrankNonBrick, CrankLeversAndPacking) receive BOTH the rename fixes AND the AutoBought oracle migration"
  artifacts:
    - path: "test/gas/CrankLeversAndPacking.t.sol"
      provides: "renamed literal source-string assertions + AutoBought oracle migration"
      contains: "degeneretteResolve"
  key_links:
    - from: "the test suite"
      to: "the renamed degeneretteResolve + the AutoBought-free keeper"
      via: "atomic test fixes landing in the same BATCH-02 diff as the contract edits"
      pattern: "degeneretteResolve"
---

<objective>
The test rename-fixes + GASOPT-04 oracle migration (329-SPEC §3 step 6; 329-ATTEST-DEGENERETTE-RESOLVE §A.3/§A.5). Two test-edit classes that MUST land together in the BATCH-02 diff (the suite breaks the moment the contract symbol is renamed OR the event is removed):

1. **Rename fixes (5 files / 57 refs):** update every `autoResolve`/`_autoResolveBet` reference to `degeneretteResolve`/`_degeneretteResolveBet` — call-sites AND comments AND the `CrankLeversAndPacking.t.sol` LITERAL SOURCE-STRING assertions (`_countOccurrences(game_, "function autoResolve(")` at `:277`/`:278`/`:279`/`:290`) which assert against the contract source as a string and BREAK without an atomic update.
2. **GASOPT-04 oracle migration (4 AfKing-family files):** migrate the `AutoBought` event oracle (`keccak256("AutoBought(address,uint32,uint256)")`, drained via `getRecordedLogs()`) to the `lastAutoBoughtDay` storage stamp / pool-balance-delta. Re-express the no-double-buy invariant `_countAutoBoughtFor(sub)==1` in `lastAutoBoughtDay` + pool/balance-delta terms WITHOUT weakening SAFE-03 / H-CANCEL-SWAP.

The 2 COLLISION files (`CrankNonBrick.t.sol` + `CrankLeversAndPacking.t.sol`) carry BOTH classes — both edits in the same diff.

Also: the contract now exposes a PARAMETERLESS `doWork()` and the standalone `autoBuy(count)`/`autoOpen(count)` are UNREWARDED escapes (330-07); `autoBuy` is no longer the rewarded entrypoint and returns no `bountyEarned`. Where tests call `game`/`afKing` keeper entrypoints by the old shape (e.g. `autoBuy(maxCount) returns (bountyEarned)`), update them to the new surface (parity-only — the deep behavioral router proofs are TST-02/03 at Phase 332; this plan keeps the suite COMPILING + the rename/oracle parity).

Purpose: keep the suite compiling + the rename/oracle assertions correct in the same diff.
Output: the 9 test files updated (5 rename + 4 AutoBought + the 2-file overlap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-DEGENERETTE-RESOLVE.md
@.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-03-PLAN.md
@.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-06-PLAN.md
@.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-07-PLAN.md

<interfaces>
<!-- Attested @ 0cc5d10f (329-ATTEST-DEGENERETTE-RESOLVE §A.3 / §A.5). Re-grep at edit time. -->

RENAME files (`autoResolve`/`_autoResolveBet` → `degeneretteResolve`/`_degeneretteResolveBet`):
| File | refs @ 0cc5d10f | also AutoBought? |
|------|-----------------|------------------|
| test/fuzz/CrankFaucetResistance.t.sol | 13 | no |
| test/fuzz/CrankNonBrick.t.sol | 7 | YES (10 AutoBought refs) |
| test/fuzz/RngFreezeAndRemovalProofs.t.sol | 7 | no |
| test/gas/CrankResolveBetWorstCaseGas.t.sol | 8 | no |
| test/gas/CrankLeversAndPacking.t.sol | 22 (incl. literal source-string assertions :277/:278/:279/:290) | YES (3 AutoBought refs) |

CrankLeversAndPacking literal assertions @ 0cc5d10f: `assertGt(_countOccurrences(game_, "function autoResolve("), 0, …)` (:277), `… "address[] calldata players" …` (:278), `… "uint64[] calldata betIds" …` (:279), `assertEq(_countOccurrences(game_, "function autoResolve("), 1, "GAS-03: single autoResolve …")` (:290). → update the literal strings to `"function degeneretteResolve("` (the param-shape literals :278/:279 stay the same param types; the count assertion :290 message updates).

AutoBought-ORACLE files (`keccak256("AutoBought(address,uint32,uint256)")` drained via `getRecordedLogs()`):
| File | AutoBought refs @ 0cc5d10f |
|------|----------------------------|
| test/fuzz/AfKingConcurrency.t.sol | 76 (incl. `_countAutoBoughtFor(sub)==1` at :129) |
| test/gas/SweepPerPlayerWorstCaseGas.t.sol | 29 (incl. :130/:268) |
| test/fuzz/AfKingFundingWaterfall.t.sol | 18 (the cost/msgValue stream :63) |
| test/fuzz/AfKingSubscription.t.sol | 16 (uses lastAutoBoughtDay offset + NoSubscribersAutoBought tail) |
| (+ collision) CrankNonBrick.t.sol | 10 |
| (+ collision) CrankLeversAndPacking.t.sol | 3 |

The no-double-buy oracle replacement: tests already read `lastAutoBoughtDay` via `_lastAutoBoughtDayOf` (the storage stamp is present at baseline). Re-express `_countAutoBoughtFor(sub)==1` (which counted AutoBought logs per sub) as: a sub's `lastAutoBoughtDay == today` after exactly one autoBuy + the pool/balance delta equals exactly one buy's cost (so a second autoBuy of the same sub that day is a no-op — the `:627` `lastAutoBoughtDay >= today` skip). Preserve the SAFE-03 (no-double-buy) + H-CANCEL-SWAP (cancel-tombstone streak) proofs — do NOT weaken them; if a proof genuinely cannot be re-expressed without the event, FLAG it (do not silently drop the assertion).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rename autoResolve→degeneretteResolve across the 5 rename test files (incl. CrankLeversAndPacking literal source-string assertions)</name>
  <read_first>
    - test/fuzz/CrankFaucetResistance.t.sol, test/fuzz/RngFreezeAndRemovalProofs.t.sol, test/gas/CrankResolveBetWorstCaseGas.t.sol (the 3 rename-only files)
    - test/fuzz/CrankNonBrick.t.sol, test/gas/CrankLeversAndPacking.t.sol (the 2 collision files — rename portion here, AutoBought portion in Task 2)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-DEGENERETTE-RESOLVE.md (§A.3 the per-file ref counts + the literal-assertion detail)
    - .planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-03-PLAN.md (the renamed contract symbols)
  </read_first>
  <action>
    In all 5 files, replace every `autoResolve` → `degeneretteResolve` and every `_autoResolveBet` → `_degeneretteResolveBet` (call-sites, comments, prose). In `CrankLeversAndPacking.t.sol`, update the literal source-string assertions: `_countOccurrences(game_, "function autoResolve(")` → `_countOccurrences(game_, "function degeneretteResolve(")` (at `:277` and `:290`), and update the `:290` assertion message string (`"GAS-03: single autoResolve …"` → reference `degeneretteResolve`). The param-shape literal assertions `"address[] calldata players"` (:278) / `"uint64[] calldata betIds"` (:279) stay (the param types are unchanged by the rename). Confirm the post-edit per-file count of the OLD symbols is 0 in each of the 5 files.
  </action>
  <verify>
    <automated>for f in test/fuzz/CrankFaucetResistance.t.sol test/fuzz/RngFreezeAndRemovalProofs.t.sol test/gas/CrankResolveBetWorstCaseGas.t.sol test/fuzz/CrankNonBrick.t.sol test/gas/CrankLeversAndPacking.t.sol; do c=$(grep -c "autoResolve\|_autoResolveBet" "$f"); [ "$c" = "0" ] || { echo "FAIL $f has $c old refs"; exit 1; }; done; grep -c "function degeneretteResolve(" test/gas/CrankLeversAndPacking.t.sol | grep -q "[1-9]" && echo PASS</automated>
  </verify>
  <done>All 5 rename files have zero `autoResolve`/`_autoResolveBet` references; the CrankLeversAndPacking literal source-string assertions reference `"function degeneretteResolve("`; param-shape literals preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate the AutoBought-event oracle to lastAutoBoughtDay/pool-balance-delta across the 4 AfKing-family files + the 2 collision files (GASOPT-04)</name>
  <read_first>
    - test/fuzz/AfKingConcurrency.t.sol (the `_countAutoBoughtFor` helper :129 + the AutoBought log drain :62), test/gas/SweepPerPlayerWorstCaseGas.t.sol (:73/:130/:268), test/fuzz/AfKingFundingWaterfall.t.sol (:63), test/fuzz/AfKingSubscription.t.sol (the lastAutoBoughtDay offset + NoSubscribersAutoBought tail)
    - test/fuzz/CrankNonBrick.t.sol + test/gas/CrankLeversAndPacking.t.sol (the AutoBought portion of the 2 collision files)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-DEGENERETTE-RESOLVE.md (§A.5 the collision-set + the no-double-buy re-expression constraint)
    - .planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-06-PLAN.md (the AutoBought event removal + the lastAutoBoughtDay oracle)
  </read_first>
  <action>
    In each file that drains the `AutoBought` event (the 4 AfKing-family + the 2 collision files), remove the `keccak256("AutoBought(address,uint32,uint256)")` topic-match + the `getRecordedLogs()` decode of `AutoBought` records, and re-express the assertions in terms of the `lastAutoBoughtDay` storage stamp (via the existing `_lastAutoBoughtDayOf` accessor) + pool/balance deltas:
    - "a sub was autoBought today exactly once" → `_lastAutoBoughtDayOf(sub) == today` AND the sub's `_poolOf`/balance dropped by exactly one buy's cost (or the claimable/combined equivalent for the funding mode).
    - the no-double-buy invariant `_countAutoBoughtFor(sub) == 1` (Concurrency :129, Sweep :130/:268) → assert that after two autoBuy passes in the same day the sub's pool moved by exactly ONE buy's cost (the `:627` `lastAutoBoughtDay >= today` skip makes the 2nd a no-op) and `lastAutoBoughtDay == today`.
    - the `cost`/`msgValue` stream (FundingWaterfall :63) → derive the per-player charged amount from the pool-balance delta instead of the event's `cost` field.
    Preserve the SAFE-03 (no-double-buy) and H-CANCEL-SWAP (cancel-tombstone streak) PROOFS — the storage/delta re-expression must assert the SAME property at the same or stronger strength. If any single assertion genuinely cannot be re-expressed without the event (e.g. it needed per-log ordering the storage stamp cannot reconstruct), FLAG it in the SUMMARY as a BATCH-02 reconciliation item rather than dropping it.
    In the 2 collision files, apply this AutoBought migration IN ADDITION TO the Task-1 rename (both land in the same file edit).
    Also update any test that called the old rewarded `autoBuy(maxCount) returns (bountyEarned)` to the new surface: the standalone `autoBuy(count)` is now UNREWARDED (no return), and `doWork()` is the parameterless rewarded entrypoint — adjust the call shape so the file COMPILES (parity-only; deep router proofs are Phase 332).
  </action>
  <verify>
    <automated>for f in test/fuzz/AfKingConcurrency.t.sol test/gas/SweepPerPlayerWorstCaseGas.t.sol test/fuzz/AfKingFundingWaterfall.t.sol test/fuzz/AfKingSubscription.t.sol test/fuzz/CrankNonBrick.t.sol test/gas/CrankLeversAndPacking.t.sol; do c=$(grep -c "AutoBought" "$f"); [ "$c" = "0" ] || { echo "FAIL $f still has $c AutoBought refs"; exit 1; }; done; echo PASS</automated>
  </verify>
  <done>Zero `AutoBought` references remain in the 6 files; the no-double-buy / SAFE-03 / H-CANCEL-SWAP proofs are re-expressed in `lastAutoBoughtDay`/pool-balance-delta terms (or any irreducible assertion is flagged in the SUMMARY); the 2 collision files carry both the rename + the oracle migration; old-shape `autoBuy` call-sites updated to compile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the test suite ↔ the contract source | the rename literal-string assertions read the contract as a string; the oracle reads storage/logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-330-08-01 | Tampering | the no-double-buy proof weakened by the oracle migration | mitigate | The `_countAutoBoughtFor(sub)==1` invariant is re-expressed in `lastAutoBoughtDay`+pool-delta terms asserting the SAME property at ≥ the same strength (the storage stamp is the authoritative oracle the contract itself uses at `:627`); SAFE-03 / H-CANCEL-SWAP proofs preserved; any irreducible assertion is FLAGGED, never silently dropped. Net-zero regression proven at TST-04 (Phase 332). |
| T-330-08-02 | Tampering | a dangling old symbol breaking compile mid-diff | mitigate | The 2 collision files get BOTH the rename + the AutoBought migration in the same edit (the suite breaks the moment either the symbol is renamed OR the event is gone); the verify greps assert 0 residual `autoResolve`/`AutoBought` per file. |
| T-330-08-03 | Repudiation | the literal source-string assertion silently passing on a stale string | mitigate | CrankLeversAndPacking's `_countOccurrences(game_, "function degeneretteResolve(")` asserts the renamed literal exists exactly once — it FAILS loudly if the contract rename (330-03) and the test literal diverge. |
| T-330-08-SC | Tampering | npm/forge installs | mitigate | No package installs (test edits only; `test/` is AGENT-committable). |
</threat_model>

<verification>
- The full suite compiles + runs as part of BATCH-02 (330-09 `forge build` + `forge test`); the net-zero-regression proof is TST-04 (Phase 332) — this plan's job is the rename/oracle parity so the suite is not RED for a renamed symbol or a removed event.
- The CrankLeversAndPacking literal source-string assertions are green against the renamed contract.
- `test/` is AGENT-committable (no contract-commit gate) — but the test edits ride the SAME BATCH-02 diff so the suite is coherent at the hand-review (330-09).
</verification>

<success_criteria>
- GASOPT-04: the AutoBought-event oracle is migrated to `lastAutoBoughtDay`/pool-balance-delta across the 4 AfKing-family + 2 collision files; the no-double-buy invariant re-expressed without weakening SAFE-03 / H-CANCEL-SWAP.
- The 5-file/57-ref `autoResolve`→`degeneretteResolve` rename (incl. the literal source-string assertions) is applied atomically; the 2 collision files carry both classes.
</success_criteria>

<output>
Create `.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-08-SUMMARY.md` when done.
</output>
