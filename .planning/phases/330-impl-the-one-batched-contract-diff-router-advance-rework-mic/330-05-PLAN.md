---
phase: 330-impl-the-one-batched-contract-diff-router-advance-rework-mic
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/modules/DegenerusGameMintModule.sol
autonomous: true
requirements: [GASOPT-01]
must_haves:
  truths:
    - "In `processFutureTicketBatch` (`:393`, `rk` set at `:398`) the repeated `ticketsOwedPacked[rk][player]` accesses (`:431`/`:441`/`:453`/`:463`/`:498`) read/write through a hoisted `mapping(address => uint40) storage owedMap = ticketsOwedPacked[rk]` computed ONCE before the player `while` loop — dropping the outer keccak per ticket; behavior-identical, gas-only."
    - "In `processTicketBatch` (`:670`, `rk` set at `:671`) the repeated `ticketsOwedPacked[rk][player]` accesses (`:741`/`:748`/`:754` + the helper sites `:769`/`:822`) read/write through the same hoisted `owedMap` pointer; `rk` is loop-invariant; behavior-identical, gas-only."
    - "No payout, RNG, ordering, or storage-layout change — `rk` is computed identically, the hoist is a pure storage-pointer alias for the same slot; the values written/read are byte-identical to the per-access form."
  artifacts:
    - path: "contracts/modules/DegenerusGameMintModule.sol"
      provides: "processFutureTicketBatch + processTicketBatch with the hoisted ticketsOwedPacked[rk] storage pointer"
      contains: "storage owedMap = ticketsOwedPacked[rk]"
  key_links:
    - from: "contracts/modules/DegenerusGameMintModule.sol owedMap pointer"
      to: "ticketsOwedPacked[rk] (the same nested-mapping slot)"
      via: "loop-invariant storage-pointer alias — rk computed once before the loop"
      pattern: "storage owedMap = ticketsOwedPacked\\[rk\\]"
---

<objective>
The GASOPT-01 no-cost micro-opt (SPEC §3 edit-order step 5). Hoist the loop-invariant `ticketsOwedPacked[rk]` nested-mapping pointer in both `processFutureTicketBatch` and `processTicketBatch` so the outer keccak is computed once per call instead of per ticket. INDEPENDENT, gas-only, behavior-identical — touches no other item's surface.

Purpose: ROADMAP SC4 (GASOPT-01) — `rk` is loop-invariant (computed once before the player loop); the repeated `ticketsOwedPacked[rk][player]` accesses go through a single hoisted `mapping(address => uint40) storage owedMap = ticketsOwedPacked[rk]`, dropping the outer-mapping keccak per ticket. Same-results, proven at TST (Phase 332). This file is fully disjoint from the router/advance/rename surface (AdvanceModule / DegenerusGame / interfaces / AfKing), so it runs in Wave 1 alongside 330-01 — but because worktrees are excluded for contract-touching plans, all `.sol` edits run sequentially in main; the wave assignment only records that this plan has NO producer/consumer dependency.
Output: the two hoisted `owedMap` pointers + the rewritten `ticketsOwedPacked[rk][player]` accesses through them.

NON-NEGOTIABLE: This plan EDITS `contracts/*.sol` but MUST NOT commit anything. ONE batched diff, committed only after 330-07. NOTHING pre-approved. This is a PURE gas-only refactor — NO behavior/payout/RNG/ordering/storage-layout change. Do NOT touch any other function in the file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md

<interfaces>
<!-- Anchors from live source at 0cc5d10f. Re-grep before editing. -->

contracts/modules/DegenerusGameMintModule.sol:
  :393  function processFutureTicketBatch(
  :398  uint24 rk = inFarFuture ? _tqFarFutureKey(lvl) : _tqReadKey(lvl);   // set ONCE before the :429 player while-loop
  :429  while (idx < total && used < writesBudget) { ... }                  // the player loop
  :431/:441/:453/:463/:498  ticketsOwedPacked[rk][player] reads/writes      // → owedMap[player]
  :670  function processTicketBatch(uint24 lvl) external returns (bool finished) {
  :671  uint24 rk = _tqReadKey(lvl);                                        // set ONCE before the :697 player while-loop
  :697  while (idx < total && used < writesBudget) { ... }                  // the player loop
  :741/:748/:754  ticketsOwedPacked[rk][player] reads/writes               // → owedMap[player]
  :769/:822  ticketsOwedPacked[rk][player] (the _zeroOwedRemainder helper region) // → owedMap[player] IF in-scope of the same rk
</interfaces>

<grep_protocol>
Baseline `0cc5d10f` (live tree byte-identical). RE-GREP `function processFutureTicketBatch`, `function processTicketBatch`, `uint24 rk =`, `ticketsOwedPacked\[rk\]` BEFORE editing — this is the only plan touching this file, so lines are at baseline, but enumerate ALL `ticketsOwedPacked[rk][...]` accesses per function (the grep above found :431/:441/:453/:463/:498 in the future batch and :741/:748/:754 + :769/:822 in the standard batch) so NONE is missed. 329-ATTEST-ROUTER-ADVANCE §C.2 (C3/C4) is source-of-truth: `rk` is loop-invariant at both sites; the hoist is behavior-identical, gas-only.
</grep_protocol>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hoist ticketsOwedPacked[rk] to a storage pointer in processFutureTicketBatch + processTicketBatch (GASOPT-01)</name>
  <read_first>
    - contracts/modules/DegenerusGameMintModule.sol (`processFutureTicketBatch` :393 — the `rk` set :398, the player loop :429, ALL `ticketsOwedPacked[rk][player]` accesses :431/:441/:453/:463/:498; `processTicketBatch` :670 — the `rk` set :671, the player loop :697, ALL accesses :741/:748/:754 + the `_zeroOwedRemainder` helper region :769/:822; confirm whether :769/:822 are inside the SAME function scope as the `rk` at :671 — if they are in a separate helper that does NOT see the local `rk`, hoist only within the scope that does, and leave the helper's own access form intact)
    - 329-SPEC.md §3 (the GASOPT-01 blueprint — `mapping(address=>uint40) storage owedMap = ticketsOwedPacked[rk]` once per call) + the Placeholder note (this is gas-only, no constant)
    - 329-ATTEST-ROUTER-ADVANCE.md §C.2 C3/C4 (the loop-invariant `rk` + the behavior-identical hoist verdict)
  </read_first>
  <action>
    Per GASOPT-01 + SPEC §3 + 329-ATTEST §C.2. Re-grep `uint24 rk =` and ALL `ticketsOwedPacked[rk][...]` accesses in BOTH functions first.
    (1) In `processFutureTicketBatch`: immediately AFTER the `uint24 rk = …;` set (`:398`) and BEFORE the player `while` loop (`:429`), add `mapping(address => uint40) storage owedMap = ticketsOwedPacked[rk];`. Rewrite every `ticketsOwedPacked[rk][player]` access (`:431`/`:441`/`:453`/`:463`/`:498`) to `owedMap[player]` — reads AND writes. The slot is identical (`owedMap` aliases `ticketsOwedPacked[rk]`); only the outer keccak is hoisted out of the loop.
    (2) In `processTicketBatch`: same — after `uint24 rk = _tqReadKey(lvl);` (`:671`) and before the loop (`:697`), add `mapping(address => uint40) storage owedMap = ticketsOwedPacked[rk];`. Rewrite the accesses at `:741`/`:748`/`:754`. For the `_zeroOwedRemainder` helper region (`:769`/`:822`): IF those accesses are within the same function body that holds the local `rk` (so `owedMap` is in scope), rewrite them to `owedMap[player]` too; IF they live in a SEPARATE helper function that takes its own `rk`/key argument and does not see this local, leave them as-is OR hoist a local pointer within that helper's own scope — do NOT pass a storage pointer across a function boundary in a way that changes behavior. Decide by reading the scope; record the decision.
    (3) NO other change — no payout/RNG/ordering/storage-layout edit. The `rk` derivation (`_tqFarFutureKey`/`_tqReadKey`) is UNCHANGED. Comments describe what IS.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && echo "--- hoisted pointers ---"; grep -c "storage owedMap = ticketsOwedPacked\[rk\]" contracts/modules/DegenerusGameMintModule.sol | tr -d "\n"; echo " owedMap hoists (MUST be 2 — one per function)"; echo "--- in-loop accesses now via owedMap (no surviving ticketsOwedPacked[rk][player] inside the loops) ---"; grep -c "owedMap\[player\]" contracts/modules/DegenerusGameMintModule.sol | tr -d "\n"; echo " owedMap[player] accesses (MUST be ≥8 — the rewritten reads/writes)"; echo "--- rk derivation unchanged ---"; grep -c "uint24 rk = inFarFuture ? _tqFarFutureKey(lvl) : _tqReadKey(lvl)\|uint24 rk = _tqReadKey(lvl)" contracts/modules/DegenerusGameMintModule.sol | tr -d "\n"; echo " rk sets (MUST be 2, unchanged)"; echo "--- build ---"; timeout 400 forge build 2>&1 | grep -ciE "^error|Error \(" | tr -d "\n"; echo " build errors (MUST be 0)"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly two `mapping(address => uint40) storage owedMap = ticketsOwedPacked[rk];` hoists (one in each function), each placed after the `rk` set and before the player loop.
    - Every in-loop `ticketsOwedPacked[rk][player]` access in both functions is rewritten to `owedMap[player]` (reads AND writes) — `git diff` shows no surviving `ticketsOwedPacked[rk][player]` inside either loop body.
    - The `_zeroOwedRemainder` helper-region accesses (`:769`/`:822`) are handled per the scope decision recorded in the SUMMARY (rewritten if in-scope; left/hoisted-locally if a separate helper) — no cross-boundary behavior change.
    - The `rk` derivation (`_tqFarFutureKey`/`_tqReadKey`) and all payout/RNG/ordering are byte-unchanged; the storage layout is unchanged (a storage pointer is not a state variable).
    - `forge build` reports 0 errors.
  </acceptance_criteria>
  <done>Both `processFutureTicketBatch` and `processTicketBatch` hoist `ticketsOwedPacked[rk]` to a single `owedMap` storage pointer per call; every in-loop access goes through it; the `rk` derivation and all behavior are byte-identical; the file compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| gas-only refactor → behavior | the hoist must be a pure storage-pointer alias for the same `ticketsOwedPacked[rk]` slot; a wrong `rk` or a mis-scoped pointer would change written/read values. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-330-05-01 | Tampering | the hoist changes behavior (non-gas-only) | mitigate | `owedMap` is `ticketsOwedPacked[rk]` for the same loop-invariant `rk`; `owedMap[player]` resolves to the identical slot as `ticketsOwedPacked[rk][player]`. The `rk` derivation is unchanged. Behavior-identical by construction (329-ATTEST §C.2). Empirical same-results proof = TST-03 (Phase 332). |
| T-330-05-02 | Tampering | a missed access site still uses the un-hoisted form (partial, inconsistent) | mitigate | The grep gate enumerates ALL `ticketsOwedPacked[rk][player]` sites per function before editing; the verify asserts no surviving in-loop `ticketsOwedPacked[rk][player]` and the `_zeroOwedRemainder` scope decision is recorded. |
| T-330-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. No `## Package Legitimacy Audit` required. |
</threat_model>

<verification>
- `forge build` 0 errors over the touched file.
- Two `owedMap = ticketsOwedPacked[rk]` hoists; all in-loop accesses via `owedMap[player]`; `rk` derivation unchanged.
- The `_zeroOwedRemainder` scope decision recorded; no cross-boundary behavior change.
- No git commit performed (batched-diff discipline).
</verification>

<success_criteria>
- ROADMAP SC4 (GASOPT-01) satisfied: both `processTicketBatch` and `processFutureTicketBatch` hoist `mapping(address=>uint40) storage owedMap = ticketsOwedPacked[rk]` (rk loop-invariant), dropping the outer keccak per ticket — behavior-identical, gas-only.
</success_criteria>

<output>
Create `.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-05-SUMMARY.md` when done. Record: the re-grepped current lines, the two hoist insertions, the full list of rewritten access sites per function, the `_zeroOwedRemainder` scope decision, and confirmation the `rk` derivation + all behavior + the storage layout are byte-identical. Do NOT git commit.
</output>
