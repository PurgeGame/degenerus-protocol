---
phase: 330-impl-the-one-batched-contract-diff-router-advance-rework-mic
plan: 04
type: execute
wave: 4
depends_on: ["330-03"]
files_modified:
  - contracts/AfKing.sol
autonomous: true
requirements: [ROUTER-01, ROUTER-02, ROUTER-03, ROUTER-04, ROUTER-05, ROUTER-06, GASOPT-02]
must_haves:
  truths:
    - "A `doWork(uint256 maxCount)` entrypoint on `AfKing.sol` routes by priority advance → autoOpen → autoBuy and RETURNS after the first rewarded category — a STRUCTURAL early-return (an explicit `return` after the first rewarded leg) so advance/open/buy bounties can NEVER stack in one tx (ROUTER-02/03, invariant a)."
    - "`doWork` discovers work via the 3 O(1) predicates — `IGame.advanceDue()`, `IGame.boxesPending()`, and the AfKing-local buys-pending cursor check — and reverts a dedicated `error NoWork()` when ALL THREE are empty; it never pays a bounty for no work (ROUTER-04/06)."
    - "`doWork` has NO `nonReentrant` guard on itself or any leg; strict CEI is preserved (all state effects before external calls, `creditFlip` LAST in each leg) — ROUTER-07/D-01: keeper-never-a-payee + no untrusted ETH send + one-category early-return + creditFlip-last."
    - "The advance leg pays ONLY the re-homed advance bounty — ONE `creditFlip(msg.sender, advanceBounty)` fired LAST, scaled by the design-1 `mult`, gated on `rewardable` from `IGame.advanceGame()`; the autoOpen/autoBuy legs keep their OWN in-callee bounty (no double-pay, R4)."
    - "`autoBuy(uint256)` is refactored to an internal `_autoBuy(uint256 maxCount)` with a thin behavior-identical external wrapper; `maxCount == 0` resolves to the FIXED `DEFAULT_AUTO_BUY_COUNT` default (plain `while (processed < count && cursor < len)`, NOT gasleft); `EmptyAutoBuy` is removed/repurposed so `0` = default everywhere; the KEEP-04 affiliate path survives untouched (C1)."
    - "GASOPT-02: the two `claimableWinningsOf(player)` reads in the buy loop (`:691`/`:722`) are hoisted to ONE call per iteration, preserving the existing laziness (both fire in ONE iteration only when `reinvestPct>0 && FLAG_DRAIN_FIRST`); behavior-identical, gas-only. GAS_BUDGET + DEFAULT_AUTO_OPEN_COUNT + DEFAULT_AUTO_BUY_COUNT land under a named D-01b flagged-marker block."
  artifacts:
    - path: "contracts/AfKing.sol"
      provides: "doWork(maxCount) one-category router (ROUTER-01/02/03/04/06); _autoBuy refactor + D-06 default-count (ROUTER-01/05); NoWork() error; GASOPT-02 claimable-hoist; the D-01b marker constants"
      contains: "function doWork(uint256 maxCount)"
  key_links:
    - from: "contracts/AfKing.sol doWork advance leg"
      to: "IGame.advanceGame() / IGame.advanceDue() (DegenerusGame.sol, plans 330-02/330-03)"
      via: "the router consumes the (mult, rewardable) tuple + the O(1) advance-due predicate"
      pattern: "advanceGame\\(\\)|advanceDue\\(\\)"
    - from: "contracts/AfKing.sol doWork autoOpen leg"
      to: "IGame.autoOpen(maxCount) / IGame.boxesPending() (DegenerusGame.sol)"
      via: "the router routes the boxes leg via the O(1) boxes-pending predicate"
      pattern: "autoOpen\\(|boxesPending\\(\\)"
---

<objective>
CONSUMER LAST (SPEC §3 edit-order step 4). Build the unified keeper router on `AfKing.sol`: the `doWork(uint256 maxCount)` entrypoint that routes to exactly ONE category per call (advance → autoOpen → autoBuy) with a structural early-return, refactor `autoBuy` to internal `_autoBuy(maxCount)` hosting the D-06 fixed default-count, declare `error NoWork()`, keep NO reentrancy guard (strict CEI), and fold in the GASOPT-02 claimable-hoist. This is the last contract edit in the chain — it consumes the producer's tuple (330-01), the wrapper decode + the views (330-02), and the interface signatures (330-03).

Purpose: ROADMAP SC1 (ROUTER-01/02/03/04/06 — the one-category router) + SC2 (ROUTER-05 — the `_autoBuy` internal refactor, KEEP-04 affiliate survives) + SC4 (GASOPT-02 — the claimable-hoist). The router pays ONLY the re-homed advance bounty (CEI, scaled by `mult`, gated on `rewardable`); the autoOpen/autoBuy legs keep their own in-callee bounty; the one-category early-return guarantees one `creditFlip` per tx (the PLAN-CRANK §7 faucet bound).
Output: `doWork(maxCount)`; the `_autoBuy` refactor + the thin external wrapper + the D-06 default; `error NoWork()`; the GASOPT-02 hoist; the GAS_BUDGET/DEFAULT_*_COUNT constants under the D-01b marker.

NON-NEGOTIABLE: This plan EDITS `contracts/*.sol` but MUST NOT commit anything. ONE batched diff, committed only after 330-07. NOTHING pre-approved. APPLY-ORDER: Wave 4, LAST — it binds against 330-01's tuple, 330-02's wrapper/views, and 330-03's interface signatures. Add the consumer declarations (`IGame.advanceGame()` returning `(uint8, bool)`, `IGame.autoOpen(uint256)`, `IGame.advanceDue()`, `IGame.boxesPending()`) to the `IGame` interface block in `AfKing.sol` (`:23`) — these AfKing-local consumer decls are part of THIS edit (the `contracts/interfaces/*` files were handled in 330-03). DO NOT add a `nonReentrant` guard anywhere (ROUTER-07/D-01). The KEEP-04 affiliate wiring is game-side (`DegenerusGame.sol:1781`); AfKing only forwards `totalValue` via `batchPurchase` — the `_autoBuy` refactor must NOT touch any affiliate path (C1).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md
@.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-02-PLAN.md
@.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-03-PLAN.md

<interfaces>
<!-- Anchors from live source at 0cc5d10f. Re-grep before editing — prior plans edit DIFFERENT files. -->

contracts/AfKing.sol — current state at 0cc5d10f:
  :23   interface IGame {                                  // ADD: advanceGame() returns (uint8,bool); autoOpen(uint256); advanceDue() view returns (bool); boxesPending() view returns (bool);
  :26-30 function batchPurchase(address[],uint256[],uint8[]) external payable;   // UNCHANGED — no affiliate arg (C1)
  :34   function claimableWinningsOf(address player) external view returns (uint256);
  :99-106 CEI invariant block ("No reentrancy guard — strict CEI everywhere; the keeper is never a payee…") — PRESERVE
  :143  error EmptyAutoBuy();                              // remove/repurpose (D-06e: 0 = default everywhere)
  :146  error NoSubscribersAutoBought();                   // the NoWork() idiom precedent
  :215  _autoBuyDay (uint32) / :216 _autoBuyCursor (uint224) — one slot; the buys-pending cursor (D-06 / R3 F3)
  :263  BOUNTY_ETH_TARGET immutable                        // re-pegged at GAS, untouched here
  :514  subscriberCount() == _subscribers.length           // buys-pending: cursor vs this
  :567  function autoBuy(uint256 maxCount) external returns (uint256 bountyEarned) {   // → internal _autoBuy; thin external wrapper
  :569  if (maxCount == 0) revert EmptyAutoBuy();           // REPLACE: 0 → DEFAULT_AUTO_BUY_COUNT
  :577  cursor = _autoBuyDay == today ? uint256(_autoBuyCursor) : 0;
  :597  while (processed < maxCount && cursor < _subscribers.length) { ... }   // the count-bounded loop
  :691  uint256 claimable = IGame(...).claimableWinningsOf(player);  // GASOPT-02 hoist site 1 (gated reinvestPct>0)
  :722  uint256 cred = IGame(...).claimableWinningsOf(player);       // GASOPT-02 hoist site 2 (gated FLAG_DRAIN_FIRST)
  :821  batchPurchase{value: totalValue}(...)               // the keeper buy; no affiliate arg (C1)
  :827-838 stall ladder (today*1days+82620; 2h→6×/1h→4×/20m→2×)   // autoBuy's OWN absolute-day epoch — UNTOUCHED (GAS-03/D-03a, invariant d)
  :845  bountyEarned = batchLen * ((BOUNTY_ETH_TARGET * PRICE_COIN_UNIT * bountyMultiplier) / mp);  // per-item MARGINAL — the autoBuy leg's own bounty
  :846  creditFlip(msg.sender, bountyEarned)               // ONE creditFlip, fired LAST (CEI)
  :886-888 _currentDay() (block.timestamp - 82620) / 1 days

contracts/DegenerusGame.sol (consumed, NOT edited here):
  autoOpen(maxCount) loop :1656; bounty `if (reward != 0) coinflip.creditFlip(msg.sender, reward);` :1676 (the autoOpen leg's OWN in-callee bounty)
  advanceGame() wrapper returns (uint8 mult, bool rewardable) (330-02); advanceDue()/boxesPending() O(1) views (330-02)
</interfaces>

<grep_protocol>
Baseline `0cc5d10f` (live tree byte-identical). RE-GREP `function autoBuy`, `EmptyAutoBuy`, `NoSubscribersAutoBought`, `claimableWinningsOf`, `interface IGame`, `_autoBuyCursor`/`_autoBuyDay`, `_subscribers.length`, the stall ladder before editing. Prior plans edit `AdvanceModule.sol`/`DegenerusGame.sol`/`interfaces/*` (DIFFERENT files) so `AfKing.sol` is at baseline — but never trust a raw line number (`feedback_verify_call_graph_against_source`). 329-SPEC §1 R2/R4 + §2 invariant (a)/(c)/(d) + ROUTER-07 + 329-ATTEST-ROUTER-ADVANCE §A/§B/§F (the per-leg no-untrusted-send rows + the O(1) predicates + the maxCount mapping + the D-06 baseline) are source-of-truth.
</grep_protocol>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor autoBuy → internal _autoBuy(maxCount) with the D-06 fixed default-count + the GASOPT-02 claimable-hoist + the D-01b marker constants</name>
  <read_first>
    - contracts/AfKing.sol (`autoBuy` :567-849 FULL body — the rngLocked pre-guard :568, the `EmptyAutoBuy` revert :569, the daily cursor :577-580, the `while (processed < maxCount && cursor < _subscribers.length)` loop :597, the two `claimableWinningsOf` reads :691 (gated `if (sub.reinvestPct > 0)` :690) + :722 (gated `if ((sub.flags & FLAG_DRAIN_FIRST) == 0) … else { … }` :719), the `batchPurchase` :821, the stall ladder :827-838, the bounty :845 + the `creditFlip` :846; the `didWork` no-buy tail revert :806 `NoSubscribersAutoBought`; the CEI invariant block :99-106; `EmptyAutoBuy` :143; `_autoBuyDay`/`_autoBuyCursor` :215-216)
    - 329-SPEC.md §1 R2 (the D-06 `maxCount==0` fixed default-count + `EmptyAutoBuy` reconciliation + the plain `while (processed < DEFAULT_COUNT && cursor < len)` NOT gasleft) + §1 R4 (KEEP-04 affiliate survives the refactor, C1) + §3 the GASOPT-02 blueprint
    - 329-ATTEST-ROUTER-ADVANCE.md §A7 (the two GASOPT-02 hoist sites + the laziness condition) + §B3 (the _autoBuy no-untrusted-send row) + §F.3 (the D-06 clean baseline — autoBuy(0) reverts today)
    - 330-CONTEXT.md <decisions> D-01/D-01b (the GAS_BUDGET ≈ 10_000_000 + DEFAULT_*_COUNT placeholder estimates + the marker-block comment form)
  </read_first>
  <action>
    Per ROUTER-01/05 + GASOPT-02 + SPEC §1 R2/R4 + the D-06 default. Re-grep `function autoBuy`, `EmptyAutoBuy`, `claimableWinningsOf` first.
    (1) REFACTOR: rename the current `autoBuy(uint256 maxCount) external returns (uint256 bountyEarned)` body into an INTERNAL `function _autoBuy(uint256 maxCount) internal returns (uint256 bountyEarned)`, and add a thin behavior-identical EXTERNAL wrapper `function autoBuy(uint256 maxCount) external returns (uint256 bountyEarned) { return _autoBuy(maxCount); }`. The money-moving path (the `batchPurchase` call, the per-player loop, the bounty `creditFlip`) stays in `_autoBuy` (no new cross-contract money edge — ROUTER-05). The KEEP-04 affiliate path is game-side; AfKing only forwards `totalValue` — the refactor MUST NOT touch any affiliate logic (C1).
    (2) D-06 DEFAULT-COUNT: replace the `if (maxCount == 0) revert EmptyAutoBuy();` (`:569`) with a resolution to the fixed default: at the top of `_autoBuy`, `uint256 count = maxCount == 0 ? DEFAULT_AUTO_BUY_COUNT : maxCount;` and use `count` as the loop bound (`while (processed < count && cursor < _subscribers.length)`). It is a PLAIN fixed-count loop — NOT a `gasleft()`-bounded loop (D-06a). `0` now means "default" everywhere (the external wrapper inherits it via `_autoBuy`). REMOVE the `EmptyAutoBuy` error decl (`:143`) if it has no other use after this (grep-decide; D-06e: it is removed/repurposed). Keep the `rngLocked()` pre-guard (`:568`) and the `NoSubscribersAutoBought` no-buy tail revert (`:806`) — those are unrelated to the count default.
    (3) GASOPT-02 HOIST: in the per-player loop, the two `claimableWinningsOf(player)` reads (`:691` gated `reinvestPct > 0`, `:722` gated `FLAG_DRAIN_FIRST`) fire in the SAME iteration ONLY when `reinvestPct > 0 && FLAG_DRAIN_FIRST`. Hoist them to ONE call per iteration: read `claimableWinningsOf(player)` once into a local (lazily — only when at least one of the two gates is true, preserving the existing laziness so a player needing NEITHER read still pays zero), reuse the local for both consumers. Behavior-identical, gas-only — the value read is the same within one iteration; do NOT change WHEN the read happens for the no-gate case (still skipped). Confirm by reading both consumer sites use the identical `claimableWinningsOf(player)` value.
    (4) D-01b MARKER CONSTANTS: add `GAS_BUDGET = 10_000_000`, `DEFAULT_AUTO_OPEN_COUNT = 100`, `DEFAULT_AUTO_BUY_COUNT = 100` (SPEC-derived best-guess placeholders, D-01) under a NAMED, GREPPABLE D-01b flagged-marker block (a labelled comment block the GAS phase locates by name; comments describe what IS: `// SPEC placeholder; calibrated at the GAS phase`, NO history). Do NOT touch `BOUNTY_ETH_TARGET` (re-pegged at GAS, D-01a). Do NOT touch the stall ladder (`:827-838`) — it is autoBuy's OWN absolute-day epoch, intentionally distinct from the AdvanceModule game-day epoch (GAS-03/D-03a, invariant d); leave a code comment at the ladder site noting the two epochs are intentionally distinct (referencing 329-SPEC invariant d) so a future auditor does not flag the divergence.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && echo "--- refactor ---"; grep -c "function _autoBuy(uint256 maxCount) internal" contracts/AfKing.sol | tr -d "\n"; echo " _autoBuy internal (MUST be 1)"; grep -c "function autoBuy(uint256 maxCount) external returns (uint256 bountyEarned)" contracts/AfKing.sol | tr -d "\n"; echo " external wrapper (MUST be 1)"; echo "--- D-06 default ---"; grep -c "DEFAULT_AUTO_BUY_COUNT" contracts/AfKing.sol | tr -d "\n"; echo " DEFAULT_AUTO_BUY_COUNT refs (MUST be ≥2: decl + use)"; grep -c "EmptyAutoBuy" contracts/AfKing.sol | tr -d "\n"; echo " EmptyAutoBuy refs (MUST be 0 — removed)"; echo "--- no gasleft loop ---"; grep -c "gasleft()" contracts/AfKing.sol | tr -d "\n"; echo " gasleft loops (MUST be 0 in _autoBuy)"; echo "--- GASOPT-02 ---"; grep -c "claimableWinningsOf" contracts/AfKing.sol | tr -d "\n"; echo " claimableWinningsOf calls (was 3 incl interface decl: 1 decl + 2 reads → now 1 decl + 1 hoisted read = 2)"; echo "--- marker constants ---"; grep -c "GAS_BUDGET\|DEFAULT_AUTO_OPEN_COUNT" contracts/AfKing.sol | tr -d "\n"; echo " marker constants present (MUST be ≥2)"; echo "--- no nonReentrant ---"; grep -c "nonReentrant" contracts/AfKing.sol | tr -d "\n"; echo " nonReentrant (MUST be 0)"; echo "--- build ---"; timeout 400 forge build 2>&1 | grep -ciE "^error|Error \(" | tr -d "\n"; echo " build errors (MUST be 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `_autoBuy(uint256 maxCount) internal` holds the full money-moving body; `autoBuy(uint256) external` is a thin `return _autoBuy(maxCount);` wrapper (behavior-identical).
    - `maxCount == 0` resolves to `DEFAULT_AUTO_BUY_COUNT` via a plain fixed-count loop (no `gasleft()`); `EmptyAutoBuy` is removed (zero refs); `0` no longer reverts.
    - The two `claimableWinningsOf(player)` reads are hoisted to ONE lazy call per iteration (the read still skipped when NEITHER gate is true); `git diff` confirms both consumers use the same hoisted local.
    - `GAS_BUDGET`/`DEFAULT_AUTO_OPEN_COUNT`/`DEFAULT_AUTO_BUY_COUNT` are declared under a named greppable D-01b marker (comments describe what IS); `BOUNTY_ETH_TARGET` + the stall ladder are byte-unchanged (a clarifying intentional-distinct-epoch comment may be added at the ladder).
    - NO `nonReentrant` anywhere; no affiliate path touched (`batchPurchase` still carries no affiliate arg — C1).
    - `forge build` reports 0 errors.
  </acceptance_criteria>
  <done>`autoBuy` is split into internal `_autoBuy(maxCount)` + a thin external wrapper; `0` resolves to the fixed `DEFAULT_AUTO_BUY_COUNT` (no gasleft, no EmptyAutoBuy); the two claimable reads are hoisted lazily; the marker constants land under D-01b; the affiliate path + the stall ladder + the no-guard invariant are intact; the file compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Add the doWork(maxCount) one-category router + error NoWork() + the IGame consumer decls (ROUTER-01/02/03/04/06, R4)</name>
  <read_first>
    - contracts/AfKing.sol (the `interface IGame {` block :23-34 — where to ADD the consumer decls; the CEI invariant block :99-106; `error NoSubscribersAutoBought()` :146 (the NoWork idiom); the now-internal `_autoBuy(maxCount)` from Task 1; `_autoBuyDay`/`_autoBuyCursor` :215-216 + `_currentDay()` :886 + `subscriberCount()`/`_subscribers.length` :514 (the buys-pending predicate); the `creditFlip` LAST-CEI pattern at :846)
    - contracts/DegenerusGame.sol (consumed: the `advanceGame()` wrapper returning `(uint8 mult, bool rewardable)` from 330-02; `advanceDue()`/`boxesPending()` from 330-02; `autoOpen(uint256)` :1636 + its OWN in-callee bounty :1676)
    - 329-SPEC.md §1 R2 (the doWork signature + the priority routing + the NoWork() revert + per-leg maxCount mapping) + §1 R4 (the router pays ONLY the re-homed advance bounty, ONE creditFlip, CEI-last, scaled by mult gated on rewardable; the autoOpen/autoBuy legs keep their own bounty; no double-pay) + §2 invariant (a) (the explicit early-return after the first rewarded category) + ROUTER-07 (NO guard, strict CEI) + §2 invariant (c) (advance is highest priority, reachable + rewarded; the unrewarded fallbacks are unaffected)
    - 329-ATTEST-ROUTER-ADVANCE.md §B (the per-leg no-untrusted-ETH-send rows — the no-guard basis) + §F.1 (the 3 O(1) predicates + their locations: advance/boxes on Game, buys AfKing-local) + §F.2 (the per-leg maxCount mapping: advance NO count, autoOpen(maxCount), _autoBuy(maxCount)) + §A10/§A12 (the one-creditFlip-last + the revert idiom)
  </read_first>
  <action>
    Per ROUTER-01/02/03/04/06 + R4 + invariant (a) + ROUTER-07 + SPEC §1 R2 + §3. Re-grep `interface IGame`, the now-internal `_autoBuy`, the buys-pending cursor anchors first.
    (1) IGame CONSUMER DECLS: in the `interface IGame {` block (`:23`), ADD `function advanceGame() external returns (uint8 mult, bool rewardable);`, `function autoOpen(uint256 maxCount) external;`, `function advanceDue() external view returns (bool);`, `function boxesPending() external view returns (bool);` (matching the concrete signatures from 330-02; `autoOpen` returns nothing, the leg's bounty is internal). Keep `batchPurchase` (`:26-30`, NO affiliate arg — C1) and `claimableWinningsOf` unchanged.
    (2) `error NoWork()`: add the error decl on `AfKing.sol` (the established AfKing anti-spam revert idiom — `EmptyAutoBuy`/`NoSubscribersAutoBought` precedent). This is AfKing's own `NoWork()` (a separate declaration from any `DegenerusGame.sol` `NoWork()` in 330-02; each contract declares its own).
    (3) `doWork(uint256 maxCount)`: add `function doWork(uint256 maxCount) external returns (...)` (choose a return that surfaces which category ran + the bounty, consistent with the existing `autoBuy` return idiom). Routing by priority with a STRUCTURAL early-return:
      - ADVANCE leg FIRST (highest priority): if `IGame(GAME).advanceDue()`, call `(uint8 mult, bool rewardable) = IGame(GAME).advanceGame();` (advance is self-bounded — NO `maxCount` arg, D-06c). If `rewardable`, pay the re-homed advance bounty as ONE `creditFlip(msg.sender, advanceBounty)` fired LAST (CEI), where `advanceBounty` is scaled by the design-1 `mult` (use the same ETH-pegged BURNIE bounty basis as the existing legs — `BOUNTY_ETH_TARGET`-derived, ×`mult`; the exact constant is a GAS-331 placeholder). Then `return` — the explicit one-category early-return (invariant a). NOTE: advancing always runs when due; if `rewardable == false` (e.g. an unrewarded-fallback-class advance), the leg did its work but pays nothing — still `return` after the advance leg (do NOT fall through to autoOpen/autoBuy in the same tx; the structural early-return is per-category-entered, and entering the advance category consumes the tx). [Confirm against SPEC R2/invariant a that "entered the advance category" is the early-return trigger, not "paid a bounty" — the lock is one CATEGORY per call.]
      - autoOpen leg SECOND: else if `IGame(GAME).boxesPending()`, call `IGame(GAME).autoOpen(maxCount)` (per-leg `maxCount`; `0` → the game-side default per 330-02/the loop), which pays its OWN in-callee bounty internally (`DegenerusGame.sol:1676`); then `return` (early-return — the router does NOT double-pay). For the `maxCount == 0` autoOpen default, resolve to `DEFAULT_AUTO_OPEN_COUNT` at the router boundary OR rely on the autoOpen loop's `0`-handling — match the SPEC R2 intent (the count-bounded autoOpen leg uses the fixed default when `0`); pass `maxCount == 0 ? DEFAULT_AUTO_OPEN_COUNT : maxCount`.
      - autoBuy leg THIRD: else if the AfKing-local buys-pending predicate is true (`cursor = _autoBuyDay == _currentDay() ? uint256(_autoBuyCursor) : 0; cursor < _subscribers.length`), call the internal `_autoBuy(maxCount)` (which already handles `0` → `DEFAULT_AUTO_BUY_COUNT` from Task 1), which pays its own bounty internally; then `return`.
      - else: `revert NoWork();` — all 3 O(1) predicates empty (ROUTER-06; never pays for no work).
    (4) NO `nonReentrant` guard on `doWork` or any leg (ROUTER-07/D-01). Strict CEI: in the advance leg, the only money-out is the bounty `creditFlip` fired LAST. The autoOpen/_autoBuy legs already preserve CEI internally. Confirm the router adds NO ETH send to any untrusted address (it only calls pinned `IGame(ContractAddresses.GAME)` + `creditFlip` to `msg.sender` as flip-credit). Comments describe what IS.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && echo "--- doWork + NoWork ---"; grep -c "function doWork(uint256 maxCount) external" contracts/AfKing.sol | tr -d "\n"; echo " doWork (MUST be 1)"; grep -c "error NoWork()" contracts/AfKing.sol | tr -d "\n"; echo " NoWork decl (MUST be 1)"; grep -c "revert NoWork()" contracts/AfKing.sol | tr -d "\n"; echo " NoWork revert (MUST be ≥1)"; echo "--- IGame consumer decls ---"; grep -c "function advanceDue() external view returns (bool)\|function boxesPending() external view returns (bool)\|function autoOpen(uint256 maxCount) external\|function advanceGame() external returns (uint8 mult, bool rewardable)" contracts/AfKing.sol | tr -d "\n"; echo " IGame router decls (MUST be 4)"; echo "--- structural early-return: count return statements in doWork (manual read confirms one per leg) ---"; echo "--- no guard ---"; grep -c "nonReentrant" contracts/AfKing.sol | tr -d "\n"; echo " nonReentrant (MUST be 0)"; echo "--- one creditFlip in the advance leg only (autoOpen/autoBuy pay internally) ---"; echo "--- build ---"; timeout 400 forge build 2>&1 | grep -ciE "^error|Error \(" | tr -d "\n"; echo " build errors (MUST be 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `doWork(uint256 maxCount)` exists and routes advance → autoOpen → autoBuy with an EXPLICIT `return` after EACH category is entered (reading the body confirms: entering the advance category returns before reaching autoOpen; autoOpen returns before autoBuy) — bounties can NEVER stack in one tx (invariant a / ROUTER-03).
    - The 3 discovery predicates are the O(1) `IGame.advanceDue()`, `IGame.boxesPending()`, and the AfKing-local `_autoBuyCursor < _subscribers.length` cursor check (ROUTER-04); `revert NoWork()` fires only when all 3 are empty (ROUTER-06).
    - The advance leg pays ONE `creditFlip(msg.sender, advanceBounty)` fired LAST, scaled by the design-1 `mult` from `IGame.advanceGame()`, gated on `rewardable`; the autoOpen/autoBuy legs do NOT receive a router-side creditFlip (they pay their own internal bounty) — no double-pay (R4).
    - The 4 `IGame` consumer decls (`advanceGame` tuple / `autoOpen(uint256)` / `advanceDue()` / `boxesPending()`) are present; `batchPurchase` carries no affiliate arg (C1).
    - NO `nonReentrant` guard on `doWork` or any leg (ROUTER-07); strict CEI (the router's only money-out is the advance-leg `creditFlip` last; no untrusted ETH send).
    - `forge build` reports 0 errors.
  </acceptance_criteria>
  <done>`doWork(maxCount)` routes one category per call with a structural early-return, discovers work via the 3 O(1) predicates, reverts `NoWork()` on no work, pays only the re-homed advance bounty (scaled by mult, gated on rewardable, CEI-last) with no double-pay and no reentrancy guard; the IGame consumer decls are added; the file compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| keeper → doWork router | the permissionless router entrypoint; the threats are bounty-stacking (>1 category per tx), reentrancy (router→game→creditFlip), and paying for no work. |
| router advance leg → re-homed bounty | the one re-homed `creditFlip`; it must fire only when `rewardable`, scaled by `mult`, LAST (CEI), and never alongside the open/buy bounties. |
| _autoBuy default-count → gas | `maxCount==0` does a fixed default batch; a pathological cluster can OOG (clean revert + manual retry — liveness, not security). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-330-04-01 | Spoofing / Tampering | bounty-stacking — one tx collects advance + open + buy bounties (invariant a / Pitfall 4) | mitigate | The router uses an EXPLICIT `return` after the FIRST category is entered (a code invariant, not a comment) — advance returns before autoOpen, autoOpen before autoBuy; exactly one category (one `creditFlip`) per tx. Empirical one-category proof = TST-02 (Phase 332). |
| T-330-04-02 | Elevation of Privilege | reentrancy on the no-guard `doWork` (router→game→creditFlip double-pay) | accept | ROUTER-07/D-01: NO guard, on the grep-checked per-leg no-untrusted-ETH-send basis (329-ATTEST §B) — keeper-never-a-payee (bounty is flip-credit, not an ETH push), no untrusted ETH send in any leg, one-category early-return, `creditFlip` LAST (CEI). There is no untrusted control-flow handoff to re-enter through. Empirical double-pay regression backstop = TST-02 (D-01b, Phase 332). |
| T-330-04-03 | Denial of Service | the router pays a bounty for no work | mitigate | Each leg is entered ONLY when its O(1) predicate (advanceDue / boxesPending / buys-pending cursor) has work; `revert NoWork()` fires when all 3 are empty — never pays for no work (ROUTER-06). The discovery views are O(1) (no unbounded scan, ROUTER-04). |
| T-330-04-04 | Denial of Service | `_autoBuy(0)` default-count OOGs on a pathological cluster | accept | D-06d: worst-case OOG is a CLEAN state-reverting revert (no fund loss, no invariant break); the keeper retries with a smaller explicit `maxCount` (the manual escape hatch). never-OOG is keeper-UX/liveness, NOT a security invariant — accepted under the security floor. Advance liveness has independent fallbacks (D-04). |
| T-330-04-05 | Tampering | the `_autoBuy` refactor breaks the KEEP-04 affiliate passthrough (C1) | mitigate | The affiliate wiring is game-side (`DegenerusGame.sol:1781`); AfKing only forwards `totalValue` via `batchPurchase` (no affiliate arg). The refactor moves the body to `_autoBuy` without touching the `batchPurchase` call or any affiliate logic — `git diff` confirms `batchPurchase` is unchanged. |
| T-330-04-06 | Tampering | the GASOPT-02 hoist changes behavior (non-gas-only) | mitigate | The two `claimableWinningsOf(player)` reads return the same value within one iteration; hoisting to one lazy call (still skipped when neither gate is true) is behavior-identical. Empirical same-results proof = TST-03 (Phase 332). |
| T-330-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. No `## Package Legitimacy Audit` required. |
</threat_model>

<verification>
- `forge build` 0 errors over the touched file.
- `doWork(maxCount)` routes one category per call with an explicit early-return; 3 O(1) predicates; `revert NoWork()` on no work; one re-homed advance `creditFlip` (mult-scaled, rewardable-gated, CEI-last); no double-pay.
- `autoBuy` → internal `_autoBuy` + thin wrapper; `0` → `DEFAULT_AUTO_BUY_COUNT` (no gasleft, no EmptyAutoBuy); GASOPT-02 hoisted lazily; marker constants under D-01b.
- NO `nonReentrant` guard anywhere; KEEP-04 affiliate path + the stall ladder untouched.
- No git commit performed (batched-diff discipline).
</verification>

<success_criteria>
- ROADMAP SC1 (ROUTER-01/02/03/04/06 — the one-category `doWork(maxCount)` router with the structural early-return, the 3 O(1) discovery predicates, the clean `NoWork()` no-work signal) + SC2 (ROUTER-05 — `autoBuy` → internal `_autoBuy`, no new cross-contract money edge, KEEP-04 affiliate survives) + SC4 (GASOPT-02 — the claimable-hoist, behavior-identical) satisfied, with NO reentrancy guard (ROUTER-07/D-01) and the re-homed advance bounty paid exactly once per tx (R4).
</success_criteria>

<output>
Create `.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-04-SUMMARY.md` when done. Record: the re-grepped lines, the `_autoBuy` refactor + the D-06 default + the EmptyAutoBuy removal, the GASOPT-02 hoist evidence (both consumers use the same local), the doWork routing structure (the per-category early-returns), the NoWork() decl + the 3 predicates, the advance-bounty payment (mult-scaled, rewardable-gated, CEI-last, no double-pay), the IGame consumer decls, the D-01b marker constants, and confirmation no guard + the affiliate path + the stall ladder are intact. Do NOT git commit.
</output>
