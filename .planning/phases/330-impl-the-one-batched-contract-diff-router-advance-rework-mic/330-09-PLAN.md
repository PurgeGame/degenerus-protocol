---
phase: 330-impl-the-one-batched-contract-diff-router-advance-rework-mic
plan: 09
type: execute
wave: 8
depends_on: ["330-01", "330-02", "330-03", "330-04", "330-05", "330-06", "330-07", "330-08"]
files_modified: []
autonomous: false
requirements: [BATCH-02]
must_haves:
  truths:
    - "The full reconciled diff (6 contracts/*.sol + the test files) compiles (forge build exits 0) and the suite runs (forge test) with no NEW regression vs the v48.0 baseline beyond the enumerated red set"
    - "Cross-item reconciliation joint-checks pass: the advance tuple flows producer→consumer; exactly ONE creditFlip in doWork; NO nonReentrant guard; both rngLock guards dropped (AfKing :568 + game-side :1737); gameOver :1738 kept; GAS-331 placeholders clearly marked; rename atomic (0 autoResolve/AutoBought residue)"
    - "The single batched contracts/*.sol diff is HELD at the contract-commit boundary for explicit USER hand-review — NEVER committed autonomously"
  artifacts: []
  key_links:
    - from: "the 8 producer/consumer plans"
      to: "the single reconciled batched diff held for hand-review"
      via: "the BATCH-02 gate compiles + tests + reconciles + HOLDS"
      pattern: "forge build"
---

<objective>
BATCH-02 — the contract-commit HARD STOP (329-SPEC §3; STATE.md contract-boundary policy). After 330-01..08 have applied the full reconciled keeper-router redesign diff to `contracts/` + `test/` (producer-before-consumer order), this gate:
1. Confirms the PRECONDITION that the SUPERSEDED held-330 working-tree diff was reset/discarded before the redesigned diff was applied (the user decides; do not assume it is gone — verify the applied diff is the REDESIGN, not the old design).
2. Compiles (`forge build`) + runs the suite (`forge test`) on the full reconciled diff.
3. Runs cross-item reconciliation joint-checks (the producer→consumer signatures, the single creditFlip, the dropped guards, the GAS-331 placeholders, the atomic rename).
4. HOLDS the single batched `contracts/*.sol` diff at the contract-commit boundary for EXPLICIT USER HAND-REVIEW — the contract commit is NEVER made autonomously.

This is the ONLY `autonomous: false` plan in the phase. Per the project policy (`feedback_batch_contract_approval` + `feedback_never_preapprove_contracts` + `feedback_manual_review_before_push` + `feedback_no_contract_commits` + `feedback_pause_at_contract_phase_boundaries`): editing `.sol` autonomously is fine; the COMMIT requires the user's hand-review of the one batched diff. Auto-advance is HELD here.

Purpose: the single hand-review gate for the whole v49.0 contract surface.
Output: a presented batched diff + the reconciliation report; NO commit until the user approves.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md
@.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-01-PLAN.md
@.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-02-PLAN.md
@.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-03-PLAN.md
@.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-06-PLAN.md
@.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-07-PLAN.md

<reconciliation>
The producer→consumer chain to joint-check (no intermediate broken-state ships):
- R1 advance tuple: AdvanceModule.advanceGame() returns (uint8 mult, bool rewardable) [330-01] → DegenerusGame.advanceGame wrapper decodes it [330-02] → IDegenerusGameAdvanceModule declares it [330-04] → AfKing.doWork consumes it [330-07].
- R4 unified creditFlip: 0 caller-creditFlip in AdvanceModule [330-01] + 0 in autoOpen [330-02] + 0 in _autoBuy [330-06] → exactly 1 in doWork [330-07]; the SDGNRS gameover creditFlip (U6) STAYS [330-01].
- RD-2 both guards: AfKing _autoBuy :568 dropped [330-06] + game-side batchPurchase :1737 dropped [330-02] + gameOver :1738 kept [330-02].
- RD-3/RD-5: autoOpen entry-gate (rngLocked||_livenessTriggered) + try/catch dropped + _autoOpenBox internal [330-02]; boxesPending rngLock-aware [330-02].
- D-05: degeneretteResolve rename + flat ≥3 re-peg + RESOLVE_FLAT_BURNIE placeholder + NoWork() [330-03]; tests renamed incl. literal assertions [330-08].
- GASOPT-01 [330-05] / GASOPT-03 keeperSnapshot [330-02 + 330-06] / GASOPT-04 AutoBought removal + oracle migration [330-06 + 330-08] / GASOPT-05 per-iter approval drop, subscribe-time keep [330-06].
- The 2 collision test files carry both the rename + the AutoBought oracle migration [330-08].
- GAS-331 placeholders (the D-07 flat-per-tx ratios + KNEE + RESOLVE_FLAT_BURNIE) are clearly marked, NOT locked.
- NO nonReentrant guard anywhere (ROUTER-07).
</reconciliation>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Compile + test + cross-item reconciliation joint-checks on the full reconciled diff</name>
  <read_first>
    - all 6 modified contracts: contracts/AfKing.sol, contracts/DegenerusGame.sol, contracts/modules/DegenerusGameAdvanceModule.sol, contracts/modules/DegenerusGameMintModule.sol, contracts/interfaces/IDegenerusGameModules.sol (+ confirm contracts/interfaces/IDegenerusGame.sol UNCHANGED)
    - the 9 modified test files
    - .planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-06-PLAN.md (Task 4 — the game-side :1737 removal reconciliation flag)
  </read_first>
  <action>
    PRECONDITION CHECK: confirm with the user (or verify via the applied diff content) that the SUPERSEDED held-330 working-tree diff was reset/discarded before the redesigned diff was applied — i.e. the current `contracts/*.sol` diff vs HEAD (= baseline 0cc5d10f) is the REDESIGN (parameterless doWork, autoBuy→advance→autoOpen, dropped rngLock guards, unified single creditFlip, GAS-331 placeholders), NOT the old `doWork(maxCount)`/dual-epoch/per-leg-bounty design. If the applied diff contains held-330 artifacts (e.g. a `doWork(uint256 maxCount)` signature, an autoBuy stall ladder, per-leg creditFlips), STOP and flag — the held diff was not properly reset.

    Run `forge build` — MUST exit 0. Run `forge test` and capture the pass/fail counts; compare the failing set to the v48.0 baseline enumerated red set (the suite has known pre-existing baseline failures; the requirement is NET-ZERO NEW regression beyond the enumerated set — the full non-widening proof is TST-04 at Phase 332, but this gate confirms the diff COMPILES + does not introduce gross new breakage).

    Run the cross-item reconciliation joint-checks (greps against the working tree):
    - `IDegenerusGameAdvanceModule.advanceGame() external returns (uint8 mult, bool rewardable)` declared; the wrapper decodes it; AfKing's IGame declares it.
    - exactly ONE `creditFlip(msg.sender` in AfKing.sol's doWork; 0 caller-creditFlips in AdvanceModule (only the SDGNRS one); 0 in autoOpen; 0 in _autoBuy.
    - 0 `nonReentrant` across all 6 contracts.
    - AfKing has no rngLock entry-guard on _autoBuy; DegenerusGame.batchPurchase has no `if (rngLockedFlag) revert RngLocked();` but KEEPS `if (gameOver) revert E();`.
    - 0 `autoResolve`/`_autoResolveBet` and 0 `AutoBought` residue in contracts + tests; `degeneretteResolve` + `RESOLVE_FLAT_BURNIE` (GAS-331 placeholder) present.
    - the GAS-331 placeholder markers present on the D-07 ratios + KNEE + RESOLVE_FLAT_BURNIE.
    - `_autoOpenBox` is internal; `boxesPending()`/`advanceDue()` exist; `keeperSnapshot` declared on both DegenerusGame + AfKing's IGame.

    Record the reconciliation results + the forge build/test outcome in the SUMMARY. DO NOT commit anything in this task.
  </action>
  <verify>
    <automated>forge build 2>&1 | tail -3; grep -c "function doWork() external" contracts/AfKing.sol | grep -qx 1 && grep -c "nonReentrant" contracts/AfKing.sol contracts/DegenerusGame.sol contracts/modules/DegenerusGameAdvanceModule.sol | grep -v ':0$' | grep -qv . && grep -rc "autoResolve\|AutoBought" contracts/AfKing.sol contracts/DegenerusGame.sol | grep -qv '[1-9]' && echo "RECON-CHECKS-RAN (full verdict in SUMMARY)"</automated>
  </verify>
  <done>The full diff is confirmed to be the REDESIGN (not held-330); `forge build` exits 0; `forge test` shows no NEW gross regression beyond the baseline red set; all cross-item reconciliation joint-checks pass (or any failure is recorded as a blocker); nothing committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: BATCH-02 hand-review/commit gate — HOLD the batched diff for explicit USER approval</name>
  <files>contracts/AfKing.sol, contracts/DegenerusGame.sol, contracts/modules/DegenerusGameAdvanceModule.sol, contracts/modules/DegenerusGameMintModule.sol, contracts/interfaces/IDegenerusGameModules.sol</files>
  <action>
    Present the full reconciled v49.0 keeper-router redesign batched diff + the Task-1 reconciliation report and HOLD at the contract-commit boundary. Do NOT commit any `contracts/*.sol` change without the user's explicit hand-review approval (the resume-signal below). Show `git diff --stat -- 'contracts/*.sol'` then the full `git diff -- 'contracts/*.sol'`, and surface the reconciliation verdict + the forge build/test outcome. The `<what-built>` / `<how-to-verify>` / `<resume-signal>` below drive the checkpoint presentation; planning docs + test files are AGENT-committable, but the `contracts/*.sol` commit waits for explicit approval.
  </action>
  <what-built>
    The single reconciled v49.0 keeper-router redesign batched diff across 6 `contracts/*.sol` (AfKing.sol parameterless doWork router + internal _autoBuy + dropped rngLock guard + deleted stall ladder + unified single creditFlip + D-07 GAS-331 placeholders + GASOPT-04/05 + the local IGame router surface; DegenerusGame.sol wrapper tuple decode + advanceDue/boxesPending views + autoOpen RD-3/RD-5 rework + degeneretteResolve rename + flat ≥3 re-peg + keeperSnapshot + batchPurchase :1737 drop; DegenerusGameAdvanceModule.sol 3 creditFlips removed + (mult,rewardable) return; DegenerusGameMintModule.sol GASOPT-01 hoists; IDegenerusGameModules.sol advance tuple) + the 9 reconciled test files (rename + AutoBought oracle migration). Applied + locally compiled (`forge build` exits 0) + tested.
  </what-built>
  <how-to-verify>
    1. Review the full batched contract diff: `git diff --stat -- 'contracts/*.sol'` then `git diff -- 'contracts/*.sol'` (the 6 files; confirm `contracts/interfaces/IDegenerusGame.sol` is UNCHANGED).
    2. Confirm the diff is the REDESIGN: parameterless `doWork()` (no maxCount); priority autoBuy→advance→autoOpen with a structural early-return; exactly ONE creditFlip in doWork (CEI-last); NO nonReentrant guard; both rngLock guards gone (AfKing _autoBuy + game-side batchPurchase :1737) with gameOver kept; the autoBuy stall ladder/epoch deleted (advance the sole stall epoch); the D-07 flat-per-tx reward + RESOLVE_FLAT_BURNIE wired as clearly-marked GAS-331 PLACEHOLDERS (calibrated at Phase 331 under a second gate); the autoResolve→degeneretteResolve rename + flat ≥3 re-peg; GASOPT-01/03/04/05.
    3. Review the reconciliation report in the 330-09 SUMMARY (producer→consumer signature flow; the forge build/test outcome; the baseline red-set comparison).
    4. Decide: APPROVE the diff for commit, or describe required changes.
  </how-to-verify>
  <resume-signal>Type "approved — commit the v49.0 keeper-router redesign batched diff" (then the contract commit is made with the user's explicit authorization), or describe the changes needed (a revision pass follows). The contract diff is NEVER committed without this explicit approval.</resume-signal>
  <verify>
    <automated>echo "Manual checkpoint — resumes on explicit USER hand-review approval of the batched contracts/*.sol diff"</automated>
  </verify>
  <done>The full batched contracts/*.sol diff + the reconciliation report are presented; the contract commit is made ONLY after the user types the explicit approval resume-signal; never committed autonomously.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| applied .sol working-tree diff → the committed mainnet contract surface | the contract-commit boundary — crossing it requires explicit USER hand-review |
| held-330 superseded diff → the redesigned diff | the precondition reset boundary (the old design must not leak into the redesign diff) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-330-09-01 | Tampering | the superseded held-330 design leaking into the applied diff | mitigate | The PRECONDITION check confirms the applied diff is the REDESIGN (parameterless doWork, dropped guards, unified creditFlip, GAS-331 placeholders), not the old doWork(maxCount)/dual-epoch/per-leg model; if held-330 artifacts are found the gate STOPS and flags. |
| T-330-09-02 | Elevation of Privilege | an autonomous contract commit | mitigate | This plan is `autonomous: false`; the contract commit is NEVER made without the blocking-human checkpoint approval (`feedback_no_contract_commits` + `feedback_never_preapprove_contracts`); the commit-guard hook blocks commits while `contracts/*.sol` is dirty (bypass only after explicit approval). |
| T-330-09-03 | Tampering | a cross-item reconciliation gap (e.g. a leg still self-credits, a guard left in) | mitigate | The joint-checks grep the producer→consumer chain (single creditFlip, 0 caller-creditFlip in legs, 0 nonReentrant, both guards dropped, atomic rename, GAS-331 markers); any gap is recorded as a blocker before the hand-review. |
| T-330-09-04 | Denial of Service | the diff not compiling / gross new regression | mitigate | `forge build` MUST exit 0; `forge test` is compared to the v48.0 baseline red set (net-zero NEW gross regression; the full non-widening proof is TST-04 Phase 332). |
| T-330-09-SC | Tampering | npm/forge/package installs in the diff | mitigate | No package installs in the v49.0 surface (source edits only); the keeper subsystem adds no new dependency. No package-legitimacy checkpoint needed (no install tasks). |
</threat_model>

<verification>
- The single batched `contracts/*.sol` diff compiles + the suite runs with no NEW gross regression.
- All cross-item reconciliation joint-checks pass (or blockers are recorded).
- The diff is HELD at the contract-commit boundary; the commit is made ONLY after the user's explicit hand-review approval.
- The GAS-calibrated peg constants (the GAS-331 placeholders) land under a SEPARATE second USER gate at Phase 331 GAS.
</verification>

<success_criteria>
- BATCH-02: the single reconciled producer-before-consumer batched diff is applied + locally compiled/tested + reconciled, and HELD at the contract-commit boundary for explicit USER hand-review — never committed autonomously; CEI with one creditFlip last in doWork; NO nonReentrant guard; the held-330 reset precondition confirmed.
</success_criteria>

<output>
Create `.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-09-SUMMARY.md` when done.
</output>
