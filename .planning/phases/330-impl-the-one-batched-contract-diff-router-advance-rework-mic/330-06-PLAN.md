---
phase: 330-impl-the-one-batched-contract-diff-router-advance-rework-mic
plan: 06
type: execute
wave: 4
depends_on: ["330-02"]
files_modified:
  - test/fuzz/CrankFaucetResistance.t.sol
  - test/fuzz/CrankNonBrick.t.sol
  - test/fuzz/RngFreezeAndRemovalProofs.t.sol
  - test/gas/CrankResolveBetWorstCaseGas.t.sol
  - test/gas/CrankLeversAndPacking.t.sol
autonomous: true
requirements: [BATCH-02]
must_haves:
  truths:
    - "All `game.autoResolve(...)` call-sites across the 5 test files flip to `game.degeneretteResolve(...)` so the suite compiles against the renamed contract entrypoint (D-05a / D-02)."
    - "The LITERAL source-string assertions in `CrankLeversAndPacking.t.sol` — `_countOccurrences(game_, \"function autoResolve(\")` (`:277`/`:290`/`:415`) and `\"function _autoResolveBet(\"` (`:381`) — flip to `\"function degeneretteResolve(\"` / `\"function _degeneretteResolveBet(\"` so they re-green against the renamed source (these BREAK without the literal update)."
    - "ONLY mechanical rename-fixes are made — NO new `doWork`/router behavioral test, NO re-peg behavioral assertion, NO freeze/one-category/same-results proof (those are Phase 332 TST). Comment/message strings referencing the old name are updated to match, with no logic change."
  artifacts:
    - path: "test/gas/CrankLeversAndPacking.t.sol"
      provides: "the literal source-string assertions flipped to the degeneretteResolve names"
      contains: "function degeneretteResolve("
  key_links:
    - from: "test/* autoResolve references (57 refs across 5 files)"
      to: "contracts/DegenerusGame.sol degeneretteResolve / _degeneretteResolveBet (renamed in plan 330-02)"
      via: "atomic rename of call-sites + literal source-string assertions + comment prose"
      pattern: "degeneretteResolve"
---

<objective>
The D-02 test rename-fixes — the ONLY test work riding the 330 diff. Flip the 5 test files / 57 references to `autoResolve` / `_autoResolveBet` (including the LITERAL source-string assertions in `CrankLeversAndPacking.t.sol`) to the renamed `degeneretteResolve` / `_degeneretteResolveBet` so the existing suite keeps compiling + green at v48-baseline parity after the 330-02 contract rename. NO behavioral test proofs — those are Phase 332 TST.

Purpose: BATCH-02 — the rename test-updates ride the same batched diff posture (the rename CHANGES test source, so parity is parity-after-rename, not byte-identical). Without these, the suite breaks at compile (call-sites) AND at run (the literal-string assertions that hard-code `"function autoResolve("`). `test/` is AGENT-committable, but this plan funnels into the same green-bar/commit gate (330-07) for a single coherent diff.
Output: the 5 test files with every `autoResolve`/`_autoResolveBet` reference renamed (call-sites + literal assertions + comments).

NON-NEGOTIABLE: This plan edits ONLY `test/` files (no `contracts/*.sol`) — so the commit-guard hook is irrelevant to it, BUT per the batched-diff posture it still funnels into the single green-bar verification at 330-07; do NOT git commit during execution (the whole milestone diff is committed once, after the 330-07 hand-review, to keep the contract + test diff coherent). APPLY-ORDER: Wave 4 — it depends on 330-02 (the contract rename must EXIST so the call-sites resolve and the literal-string assertions match the renamed source). D-02 SCOPE: rename-fixes + parity ONLY — NO `doWork` smoke test, NO re-peg behavioral assertion (D-02a defers ALL behavioral proofs to Phase 332). Do NOT touch the `auto*` family references (`autoOpen`, `autoBuy`) — only the two `autoResolve*` symbols rename.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-DEGENERETTE-RESOLVE.md
@.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-02-PLAN.md

<interfaces>
<!-- The 5 rename-surface test files (329-ATTEST-DEGENERETTE-RESOLVE §A.3, enumerated at 0cc5d10f). Re-grep before editing. -->

test/fuzz/CrankFaucetResistance.t.sol — 13 refs: game.autoResolve @ :166/:241/:278/:304/:315/:340/:369/:519/:548/:580 + comments :11/:101/:528
test/fuzz/CrankNonBrick.t.sol — 7 refs: game.autoResolve @ :158/:204 + comments :12/:20/:22/:125/:128 (incl. `try this._autoResolveBet catch {}` prose :22)
test/fuzz/RngFreezeAndRemovalProofs.t.sol — 7 refs: game.autoResolve @ :147/:157/:243/:252/:260/:308/:696
test/gas/CrankResolveBetWorstCaseGas.t.sol — 8 refs: game.autoResolve @ :172/:231/:304/:493 + comments :11/:150/:206/:361
test/gas/CrankLeversAndPacking.t.sol — 22 refs: game.autoResolve :143 + LITERAL source-string assertions
    _countOccurrences(game_, "function autoResolve(") @ :277/:290/:415
    _countOccurrences(game_, "function _autoResolveBet(") @ :381
    + message strings :146/:229/:277-279/:290 + comment prose
</interfaces>

<grep_protocol>
Baseline `0cc5d10f`. RE-GREP `autoResolve` and `_autoResolveBet` across `test/` BEFORE editing — this plan runs AFTER 330-02 renames the contract, so the contract source already reads `degeneretteResolve`/`_degeneretteResolveBet`; the test references must catch up. The CRITICAL sites are the literal source-string assertions in `CrankLeversAndPacking.t.sol` (`_countOccurrences(game_, "function autoResolve(")` etc.) — these compare the contract SOURCE as a literal string, so they FAIL at run unless the asserted literal is flipped to the renamed function name. 329-ATTEST-DEGENERETTE-RESOLVE §A.3 is the source-of-truth enumeration (5 files / 57 refs).
</grep_protocol>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename every test/ autoResolve/_autoResolveBet reference (call-sites + literal source-string assertions + comments) to the degeneretteResolve names</name>
  <read_first>
    - 329-ATTEST-DEGENERETTE-RESOLVE.md §A.3 (the full 5-file / 57-ref enumeration with line anchors) + §A.1 (the rename map: `autoResolve`→`degeneretteResolve`, `_autoResolveBet`→`_degeneretteResolveBet`)
    - 330-02-PLAN.md (confirm the contract-side renamed symbols + signatures the call-sites must match)
    - test/gas/CrankLeversAndPacking.t.sol (the literal source-string assertions :277/:290/:381/:415 — read how `_countOccurrences(game_, "...")` works so the flipped literal matches the renamed source EXACTLY, incl. the trailing `(`)
    - Each of the 5 files at its enumerated line anchors (re-grep, do not trust raw numbers)
  </read_first>
  <action>
    Per BATCH-02 + D-05a + D-02. Re-grep `autoResolve` and `_autoResolveBet` across `test/` first to find EVERY current reference (the 57-ref enumeration is the baseline map; re-grep confirms live).
    (1) CALL-SITES: flip every `game.autoResolve(...)` to `game.degeneretteResolve(...)` (same arguments). Any `this._autoResolveBet(...)` prose in comments → `this._degeneretteResolveBet(...)`.
    (2) LITERAL SOURCE-STRING ASSERTIONS (CrankLeversAndPacking.t.sol): flip `_countOccurrences(game_, "function autoResolve(")` (`:277`/`:290`/`:415`) to `_countOccurrences(game_, "function degeneretteResolve(")` and `_countOccurrences(game_, "function _autoResolveBet(")` (`:381`) to `_countOccurrences(game_, "function _degeneretteResolveBet(")` — the asserted literal MUST byte-match the renamed contract source (the same trailing `(` etc.). Any message/label strings or assertion-failure messages referencing the old name (`:146`/`:229`/`:277-279`/`:290`) flip to the new name so the messages stay accurate. Do NOT change the assertion LOGIC (the counts/expectations stay — only the searched-for literal + the message text rename).
    (3) COMMENT PROSE: flip comment references to the old name (e.g. CrankFaucetResistance `:11`/`:101`/`:528`, CrankNonBrick `:12`/`:20`/`:22`/`:125`/`:128`, CrankResolveBetWorstCaseGas `:11`/`:150`/`:206`/`:361`) to the new name. Comments describe what IS.
    (4) SCOPE GUARD: do NOT add any new test, do NOT change any behavioral assertion's expected VALUE (no re-peg / flat-bounty / ≥3-gate / freeze / one-category proof — those are Phase 332), do NOT touch `autoOpen`/`autoBuy` references. The bounty-VALUE expectations in these tests (if any assert a per-item peg) that would change under the re-peg are NOT in scope here — if a call-site test asserts a specific reward shape that the re-peg changes, that test's BEHAVIORAL re-validation is Phase 332 TST-05; for D-02 parity, only the rename is applied. If a test FAILS purely on the renamed function existing-vs-not (a compile/symbol failure), the rename fixes it; if a test fails on a CHANGED bounty value (a behavioral consequence of the re-peg), classify it as KNOWN-TST-DEFERRED (Phase 332) and record it in the SUMMARY — do NOT alter the expected value here.
    (5) After editing, run the affected suites to confirm they COMPILE; full green-bar parity is verified at 330-07.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && echo "--- zero stale autoResolve refs in test/ ---"; grep -rc "autoResolve\|_autoResolveBet" test/fuzz/CrankFaucetResistance.t.sol test/fuzz/CrankNonBrick.t.sol test/fuzz/RngFreezeAndRemovalProofs.t.sol test/gas/CrankResolveBetWorstCaseGas.t.sol test/gas/CrankLeversAndPacking.t.sol | tr -d "\n"; echo " (per-file stale counts; ALL MUST be 0)"; echo "--- renamed literal assertions present ---"; grep -c 'function degeneretteResolve(' test/gas/CrankLeversAndPacking.t.sol | tr -d "\n"; echo " degeneretteResolve literal (MUST be ≥3)"; grep -c 'function _degeneretteResolveBet(' test/gas/CrankLeversAndPacking.t.sol | tr -d "\n"; echo " _degeneretteResolveBet literal (MUST be ≥1)"; echo "--- compile the renamed suites ---"; timeout 500 forge build 2>&1 | grep -ciE "^error|Error \(" | tr -d "\n"; echo " build errors (MUST be 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "autoResolve\|_autoResolveBet" <the 5 files>` is 0 for every file (no stale reference survives).
    - `CrankLeversAndPacking.t.sol` asserts `"function degeneretteResolve("` (≥3 sites) and `"function _degeneretteResolveBet("` (≥1 site) — the literals byte-match the renamed contract source.
    - The assertion LOGIC (expected counts, expected reward shape) is UNCHANGED — only the searched-for literal + message text + comments rename; no new test was added.
    - `autoOpen`/`autoBuy` references are untouched.
    - `forge build` compiles the renamed suites with 0 errors; any test whose FAILURE is a behavioral consequence of the re-peg (changed bounty value) is recorded as KNOWN-TST-DEFERRED (Phase 332), not altered.
  </acceptance_criteria>
  <done>Every `autoResolve`/`_autoResolveBet` reference across the 5 test files (call-sites, literal source-string assertions, comments) is renamed to the `degeneretteResolve`/`_degeneretteResolveBet` names; the suites compile; no behavioral assertion value was changed; any re-peg-driven value failure is recorded as TST-deferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| renamed contract source → test literal assertions | the `CrankLeversAndPacking.t.sol` assertions read the contract source as a literal string; a missed literal flip silently fails the suite even though the contract is correct. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-330-06-01 | Tampering | a stale `autoResolve` reference breaks compile/run silently | mitigate | The grep gate asserts ZERO stale `autoResolve`/`_autoResolveBet` across all 5 files; the literal-assertion flip is explicitly verified (`"function degeneretteResolve("` count ≥3). The full enumeration (329-ATTEST §A.3) is the checklist. |
| T-330-06-02 | Tampering | a behavioral assertion value is changed under cover of the rename | mitigate | D-02 scope is rename-ONLY; the action forbids changing any expected VALUE — a re-peg-driven value failure is classified KNOWN-TST-DEFERRED (Phase 332 TST-05), not silently altered. The reviewer at 330-07 sees the test diff alongside the contract diff. |
| T-330-06-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. No `## Package Legitimacy Audit` required. |
</threat_model>

<verification>
- `forge build` compiles the renamed suites with 0 errors.
- Zero stale `autoResolve`/`_autoResolveBet` across the 5 files; the literal source-string assertions flipped to the renamed names.
- No behavioral assertion value changed; any re-peg value failure recorded as TST-deferred.
- No git commit performed (the test diff joins the single batched commit at 330-07).
</verification>

<success_criteria>
- BATCH-02 (the test-parity portion) satisfied: the 5 test files / 57 references to `autoResolve`/`_autoResolveBet` (call-sites + the CrankLeversAndPacking literal source-string assertions + comments) are renamed to `degeneretteResolve`/`_degeneretteResolveBet` so the suite compiles + stays at v48-baseline parity after the 330-02 rename — rename-fixes ONLY, all behavioral proofs deferred to Phase 332.
</success_criteria>

<output>
Create `.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-06-SUMMARY.md` when done. Record: the re-grepped reference count per file, the renamed call-sites + literal assertions + comments, confirmation no behavioral value changed, and any KNOWN-TST-DEFERRED re-peg value failures (classified for Phase 332). Do NOT git commit.
</output>
