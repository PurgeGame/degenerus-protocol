---
phase: 330-impl-the-one-batched-contract-diff-router-advance-rework-mic
plan: 06
type: execute
wave: 5
depends_on: ["330-02", "330-04"]
files_modified:
  - contracts/AfKing.sol
autonomous: true
requirements: [ROUTER-08, GASOPT-03, GASOPT-04, GASOPT-05]
must_haves:
  truths:
    - "autoBuy is refactored to an internal _autoBuy that returns the raw buy count and no longer pays its own bounty (no in-callee creditFlip)"
    - "Both rngLock guards are dropped on the buy path — the AfKing _autoBuy entry guard (:568); the in-callee creditFlip (:846) is pulled out"
    - "The autoBuy absolute-day stall ladder + its epoch (:823-838/:829) are DELETED — advance is the sole stall epoch"
    - "The per-player AutoBought event is dropped; the no-double-buy oracle is the lastAutoBoughtDay stamp"
    - "The per-iteration isOperatorApproved(player, this) check (:676) is removed; the subscribe-time isOperatorApproved(fundingSource, subscriber) gate (:401) is KEPT"
    - "The keeper reads game state once per chunk via keeperSnapshot (GASOPT-03), and AfKing's local IGame interface declares the router surface (advanceGame tuple, autoOpen return, advanceDue, boxesPending, keeperSnapshot)"
  artifacts:
    - path: "contracts/AfKing.sol"
      provides: "internal _autoBuy (rngLock guard + stall ladder + AutoBought + per-iter approval all removed; keeperSnapshot wired); the router-surface IGame interface"
      contains: "function _autoBuy"
  key_links:
    - from: "_autoBuy"
      to: "IGame(GAME).batchPurchase{value}(players, amounts, modes)"
      via: "the keeper buy call (carries no affiliate arg; KEEP-04 wired game-side)"
      pattern: "batchPurchase\\{value"
---

<objective>
AfKing CONSUMER, part A (329-SPEC §3 step 4 — the `_autoBuy` refactor + RD-2 + the D-07 deletions + GASOPT-03/04/05 + the local-interface additions). This sets up the buy leg the `doWork` router (330-07) sequences. Six changes on `AfKing.sol`:

1. **`autoBuy` → internal `_autoBuy` (ROUTER-05 mechanics):** rename `autoBuy(uint256 maxCount)` to internal `_autoBuy(uint256 maxCount) returns (uint256 boughtCount)`; it returns the raw count of successful buys and NO LONGER credits a bounty (the creditFlip is pulled into `doWork`, 330-07). The standalone parametered + UNREWARDED `autoBuy(uint256 count)` external escape is added in 330-07 alongside the router.
2. **RD-2 drop the rngLock guard (ROUTER-08):** delete the `_autoBuy` entry guard `if (IGame(GAME).rngLocked()) revert AutoBuyAborted(msg.sender, 1);` (`:568`). (The game-side `batchPurchase :1737` pre-check is dropped in the DegenerusGame edit — covered by 330-02; see Task 4 + note below.)
3. **Delete the autoBuy stall ladder + absolute-day epoch (`:823-838`/`:829`) — invariant (d)/GAS-03:** advance is the sole stall epoch; the buy leg's reward is flat (no `bountyMultiplier`).
4. **GASOPT-04:** drop the per-player `AutoBought` event (decl `:171`, emit `:785`); the no-double-buy oracle stays the existing `sub.lastAutoBoughtDay` stamp (`:81`/`:784`/`:627`).
5. **GASOPT-05:** remove the per-iteration `isOperatorApproved(player, address(this))` check (`:676`); KEEP the subscribe-time `isOperatorApproved(fundingSource, subscriber)` gate (`:401`).
6. **GASOPT-03 wiring + local IGame interface:** read game state once per chunk via `keeperSnapshot` (330-02 Task 3); add the router-surface rows to AfKing's local `IGame` interface (`advanceGame` tuple, `autoOpen` return, `advanceDue`, `boxesPending`, `keeperSnapshot`).

NO `nonReentrant` guard anywhere (ROUTER-07). The KEEP-04 `bytes32("DGNRS")` affiliate passthrough is GAME-side (`_batchPurchaseUnit :1781`) and survives — the keeper call (`:821`) carries no affiliate arg and is untouched except for the changes above.

IMPORTANT NOTE on the game-side `batchPurchase :1737` rngLock pre-check (RD-2/ROUTER-08): that guard is in `DegenerusGame.batchPurchase`, NOT AfKing. It is OWNED by THIS plan's ROUTER-08 requirement attribution but lives in DegenerusGame.sol — see Task 4. Since 330-02 already owns the DegenerusGame.sol edit and 330-06 owns AfKing.sol, the `batchPurchase :1737` removal must NOT create a second editor of DegenerusGame.sol. RESOLUTION: the `batchPurchase :1737` removal is folded into 330-02's DegenerusGame scope (330-02 Task 2 now PERFORMS the edit); Task 4 here is a DOCUMENTATION/verification task only. (The executor of 330-02 removes the bare `if (rngLockedFlag) revert RngLocked();` at batchPurchase `:1737` and KEEPS `if (gameOver) revert E();` at `:1738`; 330-06 Task 4 verifies it landed and flags the 330-09 BATCH-02 gate if it did not.) Re-read carefully: ROUTER-08 stays attributed to 330-06 for requirement coverage, but the physical game-side line is edited by 330-02 — neither executor should assume the other did it: 330-02 DOES the `:1737` edit, 330-06 VERIFIES it.

Purpose: prepare the buy leg + the keeper's game-read surface + the local interface for the router.
Output: `contracts/AfKing.sol` with internal `_autoBuy` (guards/ladder/event/per-iter-approval removed, keeperSnapshot wired) + the router-surface local `IGame` interface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md

<interfaces>
<!-- Attested @ 0cc5d10f (329-ATTEST-ROUTER-ADVANCE.md §A/§C). Re-grep at edit time. -->

AfKing local `IGame` interface (AfKing.sol :24-38 at baseline) currently declares: `isOperatorApproved`, `batchPurchase`, `rngLocked`, `mintPrice`, `claimableWinningsOf`, `hasAnyLazyPass`. → ADD the router-surface rows the `doWork` router (330-07) calls on GAME:
- `function advanceGame() external returns (uint8 mult, bool rewardable);`
- `function autoOpen(uint256 maxCount) external returns (uint256 opened);`
- `function advanceDue() external view returns (bool);`
- `function boxesPending() external view returns (bool);`
- `function keeperSnapshot(address[] calldata players) external view returns (uint256 mintPriceWei, bool rngLocked_, uint256[] memory claimables);`
(These match the DegenerusGame signatures from 330-02. `mintPrice()`/`rngLocked()`/`claimableWinningsOf()` stay for any path still using single reads, but the chunk should prefer `keeperSnapshot`. NOTE: the kept `claimableWinningsOf` interface DECL form is `function claimableWinningsOf(address player) external view returns (uint256);` — it contains `(address player)`, NOT the literal call form `claimableWinningsOf(player)`, so it does not collide with the Task 3 grep gate.)

`autoBuy(uint256 maxCount) external returns (uint256 bountyEarned)` at `:567`:
- `:568` `if (IGame(ContractAddresses.GAME).rngLocked()) revert AutoBuyAborted(msg.sender, 1);` → DELETE (RD-2; buys are freeze-safe by construction).
- `:676` `if (!IGame(...).isOperatorApproved(player, address(this))) { emit PlayerSkipped(player, 5); ++cursor; ++processed; continue; }` → DELETE (GASOPT-05; the sub is the consent unit, revoke = setDailyQuantity(0) → tombstone-skip at the `:612` `dailyQuantity == 0` branch).
- `:691` `uint256 claimable = IGame(...).claimableWinningsOf(player);` (the reinvest term) + `:722` `uint256 cred = IGame(...).claimableWinningsOf(player);` (the drain-first waterfall) → both feed from the `keeperSnapshot` array (GASOPT-03; one game read per chunk). Both call sites use the literal `claimableWinningsOf(player)` form (the Task 3 gate counts these → must reach 0).
- `:785` `emit AutoBought(player, today, msgValue);` → DELETE (GASOPT-04); the `sub.lastAutoBoughtDay = today;` write at `:784` STAYS (the no-double-buy oracle; the `:627` `if (sub.lastAutoBoughtDay >= today)` skip reads it).
- `:823-838` the SUB-03 stall-escalating `bountyMultiplier` block + `:829` `uint256 dayStart = uint256(today) * 1 days + 82_620;` → DELETE entirely (invariant d / D-07 — advance is the sole stall epoch).
- `:845` `bountyEarned = batchLen * ((BOUNTY_ETH_TARGET * PRICE_COIN_UNIT * bountyMultiplier) / mp);` + `:846` `ICoinflip(COINFLIP).creditFlip(msg.sender, bountyEarned);` → REMOVE the in-callee bounty math + creditFlip (RD-4 pull-out); `_autoBuy` returns `batchLen` (the raw buy count) to `doWork` (330-07), which computes the flat `1.5×` bounty.
- `AutoBuyAborted` error (`:140-141`): once the only firing site (`:568`) is removed, becomes dead → delete the error decl too (grep 0 references).
- `EmptyAutoBuy` (`:567`'s `if (maxCount == 0) revert EmptyAutoBuy();`): `_autoBuy` is now internal called with a fixed batch from `doWork`; the standalone `autoBuy(count)` escape (330-07) may still revert on 0 — keep `EmptyAutoBuy` for the escape, but `_autoBuy` internal need not re-check (doWork passes a fixed non-zero batch). Keep the error decl; let 330-07 decide the escape's guard.

KEEP (do NOT touch): `:401` `isOperatorApproved(fundingSource, subscriber)` subscribe-time gate (GASOPT-05 KEEPS it; the alt-account funding consent → `:403 revert NotApproved()`). The CEI invariant `:100-101`. The two-tier funding-skip exemption (VAULT/SDGNRS pinned). The cancel-tombstone reclaim. The `batchPurchase{value: totalValue}(players, amounts, modes)` keeper buy at `:821` (no affiliate arg — KEEP-04 is game-side).

DEAD-ANCHOR NOTE: the game-side `DegenerusGame.batchPurchase :1737` rngLock pre-check (RD-2) lives in DegenerusGame.sol — its removal is PERFORMED by 330-02 Task 2 (do NOT edit DegenerusGame.sol here; Task 4 documents/verifies only).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refactor autoBuy → internal _autoBuy (return raw count, drop rngLock guard :568, pull out the bounty/creditFlip) (ROUTER-08)</name>
  <read_first>
    - contracts/AfKing.sol (autoBuy :567-848 in full — the guard :568, the buy loop, the bounty block :840-847, the stall epoch :823-838, the AutoBought emit :785, the per-iter approval :676, the claimable reads :691/:722)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md (§A A7/A8/A11/A12 + §D.2 U5)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md (§2 RD-2/RD-4; §1 R4 the buy leg returns raw count, never self-credits)
  </read_first>
  <action>
    Change `function autoBuy(uint256 maxCount) external returns (uint256 bountyEarned)` (`:567`) to `function _autoBuy(uint256 maxCount) internal returns (uint256 boughtCount)`. DELETE the entry guard `if (IGame(ContractAddresses.GAME).rngLocked()) revert AutoBuyAborted(msg.sender, 1);` (`:568`, RD-2). Keep the `if (maxCount == 0) revert EmptyAutoBuy();` OR drop it for the internal form (doWork passes a fixed non-zero batch) — prefer keeping a cheap `if (maxCount == 0) return 0;` so an internal call with 0 is a safe no-op.

    At the bounty tail (`:840-847`): DELETE the `bountyMultiplier` stall block (`:823-838`, including the `:829` `dayStart = uint256(today) * 1 days + 82_620;` absolute-day epoch and the elapsed-bands), the bounty math `bountyEarned = batchLen * ((BOUNTY_ETH_TARGET * PRICE_COIN_UNIT * bountyMultiplier) / mp);` (`:845`), and the `ICoinflip(COINFLIP).creditFlip(msg.sender, bountyEarned);` (`:846`, U5 pull-out). Set the function to `return batchLen;` (the raw successful-buy count `doWork` uses for the flat `1.5×` reward). KEEP the `_autoBuyCursor = uint224(cursor);` persist, the `AutoBuyCompleted`/`NoSubscribersAutoBought` tail logic — but adjust: the `batchLen == 0` branch should still honor `didWork` (commit set-work, no revert) but return 0 instead of paying; for the internal form, return 0 when no buys (the router's NoWork() handles the no-work-across-all-categories case, but a buy-leg-only no-op that did set-work must still commit). Preserve the `didWork`/tombstone-reclaim semantics exactly — reverting would strand a tombstone.

    DELETE the now-dead `AutoBuyAborted` error decl (`:140-141`) after confirming 0 references. Keep `BOUNTY_ETH_TARGET` (still used by `doWork`'s flat formula in 330-07). Keep the keeper buy call `IGame(GAME).batchPurchase{value: totalValue}(players, amounts, modes);` (`:821`) unchanged (KEEP-04 is game-side).
  </action>
  <verify>
    <automated>grep -c "function _autoBuy(uint256 maxCount) internal returns (uint256 boughtCount)" contracts/AfKing.sol | grep -qx 1 && grep -c "AutoBuyAborted" contracts/AfKing.sol | grep -qx 0 && grep -v '^[[:space:]]*//' contracts/AfKing.sol | grep -c "bountyMultiplier" | grep -qx 0 && grep -c "creditFlip(msg.sender, bountyEarned)" contracts/AfKing.sol | grep -qx 0 && grep -c "batchPurchase{value: totalValue}" contracts/AfKing.sol | grep -qx 1 && echo PASS</automated>
  </verify>
  <done>`_autoBuy` is internal, returns the raw buy count, has no rngLock entry guard, no stall ladder/epoch, no in-callee bounty/creditFlip; `AutoBuyAborted` is gone; the keeper buy call + KEEP-04 game-side wiring are intact; tombstone/didWork commit semantics preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: GASOPT-04 (drop AutoBought event) + GASOPT-05 (drop per-iter isOperatorApproved :676, keep subscribe-time :401)</name>
  <read_first>
    - contracts/AfKing.sol (the AutoBought event decl :171; the emit :785 + the lastAutoBoughtDay write :784; the per-iter approval skip :676-680; the subscribe-time gate :401-403; the lastAutoBoughtDay skip :627)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md (§C.2 C4/C5 — GASOPT-04 oracle migration scope, GASOPT-05 keep/drop split + the 333-SWEEP blocking-condition)
  </read_first>
  <action>
    GASOPT-04: DELETE the `event AutoBought(address indexed player, uint32 day, uint256 cost);` decl (`:171`) and the `emit AutoBought(player, today, msgValue);` statement (`:785`). KEEP the `sub.lastAutoBoughtDay = today;` write (`:784`) immediately preceding the deleted emit — it is the no-double-buy oracle (read by the `:627` `if (sub.lastAutoBoughtDay >= today)` skip). The test-oracle migration (re-expressing `_countAutoBoughtFor(sub)==1` in `lastAutoBoughtDay`/pool-balance-delta terms) is the TEST plan's job (330-08) — this plan only removes the event; both land in the same BATCH-02 diff.

    GASOPT-05: DELETE the per-iteration check `if (!IGame(ContractAddresses.GAME).isOperatorApproved(player, address(this))) { emit PlayerSkipped(player, 5); unchecked { ++cursor; ++processed; } continue; }` (`:676-680`). KEEP the subscribe-time `isOperatorApproved(fundingSource, subscriber)` gate (`:401`) and its `revert NotApproved()` (`:403`) byte-unchanged — that is the alt-account funding-consent gate (the consent unit per OPEN-E). The `PlayerSkipped` reason-5 (NotApproved) becomes unused for the loop; leave the event decl + the other reason codes intact (the comment enumerating reasons should drop reason 5 or note it's no longer emitted). NOTE the 333-SWEEP blocking-condition (re-attest the 4 OPEN-E protections hold without `:676`) in the SUMMARY.
  </action>
  <verify>
    <automated>grep -c "AutoBought" contracts/AfKing.sol | grep -qx 0 && grep -c "sub.lastAutoBoughtDay = today;" contracts/AfKing.sol | grep -qx 1 && grep -c "isOperatorApproved(player, address(this))" contracts/AfKing.sol | grep -qx 0 && grep -c "isOperatorApproved(fundingSource, subscriber)" contracts/AfKing.sol | grep -qx 1 && echo PASS</automated>
  </verify>
  <done>The `AutoBought` event (decl + emit) is gone; `lastAutoBoughtDay` stamp + its `:627` skip-read survive; the per-iter `isOperatorApproved(player, this)` check is removed; the subscribe-time `isOperatorApproved(fundingSource, subscriber)` gate is kept; the 333-SWEEP blocking-condition recorded in the SUMMARY.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: GASOPT-03 wiring (one keeperSnapshot read per chunk) + add the router-surface rows to AfKing's local IGame interface</name>
  <read_first>
    - contracts/AfKing.sol (the local IGame interface :24-38; the two claimableWinningsOf(player) reads :691/:722; the mintPrice/rngLocked reads inside autoBuy)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md (§C.2 C3 GASOPT-03; §F.2 the discovery views)
    - .planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-02-PLAN.md (the keeperSnapshot signature authored in DegenerusGame)
  </read_first>
  <action>
    Add the router-surface rows to AfKing's local `IGame` interface (the file-scope `interface IGame { … }` at `:24`): `advanceGame() external returns (uint8 mult, bool rewardable);`, `autoOpen(uint256 maxCount) external returns (uint256 opened);`, `advanceDue() external view returns (bool);`, `boxesPending() external view returns (bool);`, and `keeperSnapshot(address[] calldata players) external view returns (uint256 mintPriceWei, bool rngLocked_, uint256[] memory claimables);`. These must MATCH the DegenerusGame signatures from 330-02. Keep the existing rows (`isOperatorApproved`, `batchPurchase`, `rngLocked`, `mintPrice`, `claimableWinningsOf`, `hasAnyLazyPass`) — the kept `claimableWinningsOf(address player)` DECL row is untouched and does NOT match the literal call-site form `claimableWinningsOf(player)`.

    GASOPT-03 wiring inside `_autoBuy`: the two per-player `claimableWinningsOf(player)` STATICCALLs (`:691` reinvest term, `:722` drain-first waterfall) currently fire per-iteration. Restructure so the chunk's claimable values come from a SINGLE `keeperSnapshot(chunkPlayers)` call. Given the cursor-walk processes a variable subset, the simplest correct wiring: before the loop, build the candidate player slice for this chunk and call `keeperSnapshot` once to get `(mp, rngLocked_, claimables[])`, then index `claimables[k]` per player instead of a fresh `claimableWinningsOf(player)` STATICCALL — BOTH `:691` and `:722` must be replaced by the indexed snapshot read so that NO `claimableWinningsOf(player)` STATICCALL remains in `_autoBuy`'s loop body (this is what GASOPT-03 BUYS — call-count reduction, not just GASOPT-02's per-iter hoist). If a clean one-shot batch is genuinely impractical because the processed set is data-dependent (skips/tombstones change membership) and the executor MUST fall back to a per-iteration path (e.g. a per-player single `keeperSnapshot([player])` call, collapsing the TWO reads to ONE per processed player — the GASOPT-02 subset), then the `claimableWinningsOf(player)` STATICCALLs are still ELIMINATED (replaced by the single per-player snapshot read), so the Task 3 grep gate still reaches 0; AND the executor MUST write an EXPLICIT note in the 330-06 SUMMARY stating (a) which path was taken (full chunk batch vs per-iteration fallback), (b) WHY the full batch was impractical if the fallback was used, so 330-09's BATCH-02 gate can RECORD it as a known implementation delta (GASOPT-03 superset vs GASOPT-02 subset). The VALUES must be identical to the per-call reads (same accessor). Do NOT change `mp` semantics (`mp = mintPriceWei` from the snapshot).
  </action>
  <verify>
    <automated>grep -c "function keeperSnapshot(address\[\] calldata players) external view returns (uint256 mintPriceWei, bool rngLocked_, uint256\[\] memory claimables);" contracts/AfKing.sol | grep -qx 1 && grep -c "function advanceGame() external returns (uint8 mult, bool rewardable);" contracts/AfKing.sol | grep -qx 1 && grep -c "function autoOpen(uint256 maxCount) external returns (uint256 opened);" contracts/AfKing.sol | grep -qx 1 && grep -c "function advanceDue() external view returns (bool);" contracts/AfKing.sol | grep -qx 1 && grep -c "function boxesPending() external view returns (bool);" contracts/AfKing.sol | grep -qx 1 && grep -cF "claimableWinningsOf(player)" contracts/AfKing.sol | grep -qx 0 && echo PASS</automated>
  </verify>
  <done>AfKing's local `IGame` declares the 5 router-surface rows matching DegenerusGame; the two per-iteration `claimableWinningsOf(player)` STATICCALLs (`:691`/`:722`) are GONE from `_autoBuy`'s loop body (grep count 0 — replaced by indexed `keeperSnapshot` reads, full-chunk batch OR documented per-player fallback), while the kept `claimableWinningsOf(address player)` interface decl is untouched; claimable VALUES unchanged; if the per-iteration fallback was taken, an explicit note records which path + why in the SUMMARY for the 330-09 BATCH-02 gate.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: DOCUMENT-ONLY — verify the game-side batchPurchase :1737 rngLock pre-check removal (performed by 330-02)</name>
  <read_first>
    - .planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-02-PLAN.md (the DegenerusGame.sol owner — Task 2 PERFORMS the :1737 removal)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md (§B.1 B2/B3 the :1737 remove / :1738 keep + §B.2 the Q5 dependent grep)
  </read_first>
  <action>
    Do NOT edit DegenerusGame.sol from this plan (330-02 is its sole editor — single-editor-per-file). In the 330-06 SUMMARY, record the BATCH-02 cross-item reconciliation requirement that the game-side `DegenerusGame.batchPurchase` rngLock pre-check `if (rngLockedFlag) revert RngLocked();` (`:1737`) MUST be removed (RD-2; removing only the AfKing `:568` guard would just revert deeper) and the `if (gameOver) revert E();` (`:1738`) MUST be KEPT. Verify (read-only grep against the working tree after 330-02 has run, if it has) that the bare `batchPurchase` rngLock pre-check is gone and `batchPurchase` still reverts on gameOver: `grep -cF "if (rngLockedFlag) revert RngLocked();" contracts/DegenerusGame.sol` should be 0 and the far-future compound revert `grep -cF "if (isFarFuture && rngLockedFlag && !rngBypass) revert RngLocked();" contracts/DegenerusGame.sol` should be 1 (untouched). If 330-02 did not include this line removal, FLAG it as a BATCH-02 blocker in the SUMMARY (the 330-09 gate must catch it). This is a verification/reconciliation task — the actual edit belongs to 330-02 (Task 2), but ROUTER-08's buy-path freeze-safety depends on BOTH guards being gone.
  </action>
  <verify>
    <automated>grep -c "batchPurchase" contracts/AfKing.sol | grep -q "[1-9]" && echo "PASS (document-only task; the :1737 removal is performed by 330-02 and the reconciliation grep is recorded in the SUMMARY + gated at 330-09)"</automated>
  </verify>
  <done>The SUMMARY records the game-side `batchPurchase :1737` removal + `:1738` keep requirement (RD-2), reconciles it against the working tree if 330-02 has run, and flags it as a BATCH-02 blocker if 330-02 missed it; no DegenerusGame.sol edit made from this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keeper (any EOA) → AfKing._autoBuy → IGame(GAME).batchPurchase | the buy leg; the only value transfer is to the pinned GAME |
| _autoBuy → subscriber _poolOf[src] + claimable ledger | per-sub funding waterfall; CEI debit before the batched call |
| AfKing → IGame (local interface) | the router-surface views/calls the keeper makes on GAME |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-330-06-01 | Tampering | dropping the _autoBuy rngLock guard (RD-2) | mitigate | Q1 RESOLVED SAFE (329-ATTEST §A8): the guard was v46 batch-hygiene, NOT orphan-index defense; a box queues at the CURRENT LR_INDEX, words land at LR_INDEX-1, LR_INDEX advances only in advanceGame → a fresh box is structurally pre-entropy; the v45 orphan hazard is defended on the RESOLUTION side (autoOpen word-gate, untouched). Both the AfKing `:568` AND the game-side `:1737` guard are dropped together (330-02 performs `:1737`, this plan's Task 4 verifies). |
| T-330-06-02 | Spoofing | removing per-iter isOperatorApproved (GASOPT-05) | mitigate | The SUB is the consent unit (revoke = setDailyQuantity(0) → tombstone-skip at the `:612` dailyQuantity==0 branch); the subscribe-time `isOperatorApproved(fundingSource, subscriber)` gate (`:401`) is KEPT. Residual (revoke-but-stay-subscribed keeps getting one's OWN buys from one's OWN pool) is low-harm. **333-SWEEP re-attests the 4 OPEN-E protections hold without `:676` as a BLOCKING-CONDITION before closure; if it fails the removal is REVERTED.** |
| T-330-06-03 | Repudiation | dropping the AutoBought event (GASOPT-04) | accept | No off-chain/frontend consumer (the USER owns the keeper); the no-double-buy oracle is the `lastAutoBoughtDay` storage stamp (read at `:627`), which is STRONGER (storage, not a log). The test-oracle migration (330-08) re-expresses `_countAutoBoughtFor==1` in storage terms without weakening SAFE-03 / H-CANCEL-SWAP. |
| T-330-06-04 | Denial of Service | _autoBuy returning 0 on a no-buy chunk that did set-work | mitigate | The `didWork`/tombstone-reclaim commit semantics are preserved byte-faithfully: a chunk that committed set work (cancel-reclaim, auto-pause, window renewal) commits it and returns 0 WITHOUT reverting — reverting would strand the tombstone for re-griefing (H-CANCEL-SWAP regression guard). |
| T-330-06-05 | Tampering | keeperSnapshot values diverging from per-call reads | mitigate | `keeperSnapshot` reuses the same accessors (330-02 T3); the claimable VALUES are identical, only the call count drops. The Task 3 gate proves the per-iteration `claimableWinningsOf(player)` STATICCALLs are GONE (count 0), so no fallback per-iteration path silently degrades GASOPT-03 to the GASOPT-02 subset without it being recorded; if the per-player fallback IS taken it is noted in the SUMMARY for the 330-09 gate. Same-results proven at TST-03 (Phase 332). |
| T-330-06-06 | Elevation of Privilege | a new nonReentrant guard sneaking in | mitigate | ROUTER-07: NO `nonReentrant` guard. The buy leg sends ETH only to the pinned GAME; player value flows through the pull ledger; the bounty (in doWork, 330-07) is the single CEI-last creditFlip. Keeper-never-a-payee (`:100-101`). |
| T-330-06-SC | Tampering | npm/forge installs | mitigate | No package installs (source edit only). |
</threat_model>

<verification>
- The buy path is freeze-safe with BOTH rngLock guards gone (AfKing `:568` here + game-side `:1737` via 330-02) — proven at TST-01 (autoBuy-during-rngLock safe) Phase 332.
- `_autoBuy` returns the raw buy count and credits nothing (the bounty is in doWork, 330-07).
- The two per-iteration `claimableWinningsOf(player)` STATICCALLs are eliminated (Task 3 grep gate count 0); GASOPT-03 (full chunk batch) or the documented GASOPT-02-subset fallback is recorded in the SUMMARY.
- The `AutoBought` event is gone; the suite's `AutoBought`-keyed oracle migrates to `lastAutoBoughtDay` in 330-08 (same BATCH-02 diff).
- Compiles within the BATCH-02 `forge build` (330-09).
</verification>

<success_criteria>
- ROUTER-08: `_autoBuy` internal with the rngLock guard dropped (+ the game-side `:1737` removal performed by 330-02, `:1738` kept; Task 4 verifies); the stall ladder/epoch deleted; the in-callee creditFlip pulled out (returns raw count).
- GASOPT-03: keeper reads game state via the batched `keeperSnapshot` (the two per-player `claimableWinningsOf(player)` STATICCALLs collapsed to 0 remaining — full-chunk batch or documented per-player fallback); the local IGame declares the router surface.
- GASOPT-04: the `AutoBought` event removed; `lastAutoBoughtDay` is the oracle.
- GASOPT-05: per-iter `isOperatorApproved(player, this)` removed; subscribe-time `isOperatorApproved(fundingSource, subscriber)` kept; 333-SWEEP blocking-condition recorded.
</success_criteria>

<output>
Create `.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-06-SUMMARY.md` when done.
</output>
