---
phase: 330-impl-the-one-batched-contract-diff-router-advance-rework-mic
plan: 07
type: execute
wave: 6
depends_on: ["330-06"]
files_modified:
  - contracts/AfKing.sol
autonomous: true
requirements: [ROUTER-01, ROUTER-02, ROUTER-03, ROUTER-06, ROUTER-10, ADV-01, ADV-02, ADV-03, ADV-05]
must_haves:
  truths:
    - "A PARAMETERLESS doWork() exists on AfKing — no maxCount arg; it determines its own fixed per-leg default batch internally"
    - "doWork routes by priority autoBuy → advance-leg → autoOpen and STRUCTURALLY early-returns after the first category that has work (one rewarded category per tx)"
    - "doWork uses the rngLock-aware O(1) predicates (buys-pending TRUE during rngLock, advanceDue() regardless, boxesPending() false during rngLock) to pick the category"
    - "Exactly ONE creditFlip fires in doWork, CEI-last after the early-return point; the legs return raw counts/mult and never self-credit"
    - "The D-07 flat-per-tx reward (advance 2×·mult / buy 1.5× / open 1× pro-rated below KNEE) is wired as clearly-named GAS-331 placeholder constants"
    - "doWork reverts NoWork() when all 3 discovery predicates are empty; standalone parametered + UNREWARDED autoOpen(count)/autoBuy(count) escapes exist; NO nonReentrant guard"
  artifacts:
    - path: "contracts/AfKing.sol"
      provides: "parameterless doWork() router + unified single creditFlip + D-07 flat-per-tx placeholders + unrewarded escapes + NoWork()"
      contains: "function doWork() external"
  key_links:
    - from: "doWork"
      to: "the single ICoinflip(COINFLIP).creditFlip(msg.sender, bountyEarned)"
      via: "CEI-last after the one-category early-return"
      pattern: "creditFlip\\(msg.sender"
---

<objective>
AfKing CONSUMER, part B (329-SPEC §3 step 4 — the router itself): author the PARAMETERLESS `doWork()` one-category router on top of the internal `_autoBuy` + the local IGame surface (330-06). This is the REWORKED heart of the redesign. On `AfKing.sol`:

1. **`doWork()` parameterless (ROUTER-01):** `function doWork() external` — NO `maxCount`; it determines its OWN fixed per-leg default batch internally (D-07 supersedes D-06; the `maxCount == 0` sentinel is gone).
2. **Priority routing + structural early-return (ROUTER-02/03):** `autoBuy → advance-leg → autoOpen` (RD-1); after the FIRST category that has work, do the work then fall through to the single creditFlip and RETURN — advance/open/buy bounties can never stack in one tx.
3. **rngLock-aware O(1) discovery (ROUTER-04):** route on buys-pending (AfKing-local cursor, TRUE during rngLock) → `IGame(GAME).advanceDue()` (regardless of rngLock) → `IGame(GAME).boxesPending()` (false during rngLock).
4. **Unified single creditFlip (ROUTER-10 / R4):** exactly ONE `ICoinflip(COINFLIP).creditFlip(msg.sender, bountyEarned)`, CEI-last, fed by the chosen leg's raw return (`_autoBuy` count / `advanceGame` `(mult, rewardable)` / `autoOpen` opened count). Legs never self-credit.
5. **D-07 flat-per-tx reward (R5, GAS-331 placeholders):** advance `2× × mult`, buy `1.5×`, open `1× × min(opened, KNEE)/KNEE` — all `× PRICE_COIN_UNIT / mp` off a `1×` base unit. The `1×` unit, the `1/1.5/2` ratios, and `KNEE (~5)` are clearly-named GAS-331 placeholder constants.
6. **NoWork() (ROUTER-06) + unrewarded escapes (ROUTER-01) + NO nonReentrant guard (ROUTER-07):** `revert NoWork()` when all 3 predicates empty; add standalone parametered + UNREWARDED `autoOpen(uint256 count)` / `autoBuy(uint256 count)` external escapes; NO guard. Add the D-01b TST-02 marker comment.

Depends on 330-06 (same file — `_autoBuy` internal + the local IGame router surface must exist first).

Purpose: the parameterless one-category router with the unified flat-per-tx bounty.
Output: `contracts/AfKing.sol` with `doWork()` + the unrewarded escapes + the D-07 placeholders + `NoWork()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md
@.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-06-PLAN.md

<interfaces>
<!-- Settled signatures from 329-SPEC §1 R2/R4/R5 + the 330-06 local IGame. -->

The router calls (via AfKing's local IGame, added in 330-06):
- buys-pending discovery: AfKing-LOCAL (no cross-call) — the existing `autoBuyProgress() :527` day/cursor pattern + the subscriber set: "active subscribers remain ahead of the keeper cursor for today." TRUE even during rngLock (RD-2). Compute a cheap O(1) "are there un-autoBought subscribers ahead of the cursor for today" predicate from `_autoBuyDay`/`_autoBuyCursor`/`_subscribers.length` (if `_autoBuyDay != today` OR `_autoBuyCursor < _subscribers.length` there is buy work). Keep it O(1).
- advance-leg discovery: `IGame(ContractAddresses.GAME).advanceDue()` (TRUE regardless of rngLock).
- open discovery: `IGame(ContractAddresses.GAME).boxesPending()` (FALSE during rngLock; RD-3).

The legs:
- `_autoBuy(maxCount)` internal → returns `boughtCount` (330-06).
- advance: `(uint8 mult, bool rewardable) = IGame(ContractAddresses.GAME).advanceGame();` — the wrapper decodes the module tuple (330-02). Reward only if `rewardable`.
- open: `uint256 opened = IGame(ContractAddresses.GAME).autoOpen(BATCH);` — returns the raw open count (330-02).

The single creditFlip (R4): `ICoinflip(ContractAddresses.COINFLIP).creditFlip(msg.sender, bountyEarned);` — CEI-LAST, ONCE, after the one-category early-return. `mp` (mintPrice) from `keeperSnapshot`/`mintPrice()`; `PRICE_COIN_UNIT` is the existing constant (`:` the `1000 ether` AfKing const). `BOUNTY_ETH_TARGET` immutable stays the `1×` base ETH-target.

D-07 flat-per-tx (R5) — GAS-331 PLACEHOLDERS (NOT locked; calibrated at Phase 331 under a USER gate):
- base unit `1×` = `(BOUNTY_ETH_TARGET * PRICE_COIN_UNIT) / mp` (the existing per-tx base conversion).
- advance reward = `2× × mult` where `mult ∈ {1,2,4,6}` from the advance tuple (new-day stall ladder; mid-day/gameover mult=1).
- buy reward = `1.5× × boughtCount`? NO — D-07 is FLAT PER TX, not per-item. Re-read 329-SPEC §1 R5: the buy leg reward is a flat `1.5×` per tx (bounded once/day/sub + the 2 standing subs; "no real faucet ability there"). Wire it FLAT per-tx (not × count). (The old per-item `batchLen × …` is the superseded model.) Use a named ratio constant.
- open reward = `1× × min(opened, KNEE) / KNEE` (pro-rate below the knee; flat 1× at/above KNEE≈5).
- Express the `1.5` and `2` ratios as named placeholder constants to avoid fractional literals (e.g. numerator/denominator pair or basis-points), each under a `// GAS-331 PLACEHOLDER` marker. `KNEE` is a named placeholder constant `~5`.

NoWork() (ROUTER-06): `error NoWork();` on AfKing (separate selector from DegenerusGame's — fine). Fires only when buys-pending == false AND `advanceDue()` == false AND `boxesPending()` == false.

The standalone escapes (ROUTER-01): `function autoBuy(uint256 count) external { _autoBuy(count); }` (UNREWARDED — no creditFlip) and `function autoOpen(uint256 count) external { IGame(ContractAddresses.GAME).autoOpen(count); }` (UNREWARDED). These are manual/emergency clears; only `doWork` credits. (Keep `EmptyAutoBuy` for the `autoBuy(0)` escape guard if desired.)

NO `nonReentrant` guard (ROUTER-07). D-01b marker: a comment near the single creditFlip noting "TST-02 (Phase 332) is the router→game→creditFlip double-pay backstop proving the no-guard disposition; legs structurally cannot credit — only doWork credits once after the early-return."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author the parameterless doWork() one-category router with the rngLock-aware predicates + structural early-return (ROUTER-01/02/03/04/06)</name>
  <read_first>
    - contracts/AfKing.sol (the post-330-06 file — the internal _autoBuy, the local IGame router surface, autoBuyProgress :527, _autoBuyDay/_autoBuyCursor/_subscribers, _currentDay)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md (§1 R2/R3, §2 invariant (a) one-category early-return, ROUTER-01/02/03/04/06)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md (§F.2 the 3 discovery views + their rngLock behaviors)
  </read_first>
  <action>
    Add `function doWork() external` (NO params — ROUTER-01). Define a fixed per-leg default batch constant (e.g. `uint256 internal constant DOWORK_BATCH = …;` a sane fixed batch like the prior caller-bounded default; name it clearly). Route in priority order (RD-1, ROUTER-02):
    1. Compute the O(1) buys-pending predicate from `_autoBuyDay`/`_autoBuyCursor`/`_subscribers.length` + `_currentDay()` (TRUE even during rngLock). If buys pending: `uint256 bought = _autoBuy(DOWORK_BATCH);` then GOTO the single creditFlip with the buy reward and RETURN (structural early-return — ROUTER-03).
    2. Else if `IGame(GAME).advanceDue()`: `(uint8 mult, bool rewardable) = IGame(GAME).advanceGame();` then GOTO the single creditFlip with the advance reward (only if `rewardable`; if `!rewardable`, advance ran but earns nothing — still RETURN, no NoWork) and RETURN.
    3. Else if `IGame(GAME).boxesPending()`: `uint256 opened = IGame(GAME).autoOpen(DOWORK_BATCH);` then GOTO the single creditFlip with the open reward and RETURN.
    4. Else `revert NoWork();` (ROUTER-06 — all 3 predicates empty; never pays for no work).

    Enforce the early-return STRUCTURALLY (each branch ends in `return` after the single creditFlip) so no two categories' bounties can stack in one tx (invariant a / ROUTER-03). The single creditFlip is authored in Task 2; here, structure each branch to converge on a single `bountyEarned` value + one creditFlip site (CEI-last). Do NOT add a `nonReentrant` guard.
  </action>
  <verify>
    <automated>grep -c "function doWork() external" contracts/AfKing.sol | grep -qx 1 && grep -c "revert NoWork();" contracts/AfKing.sol | grep -q "[1-9]" && grep -c "error NoWork();" contracts/AfKing.sol | grep -qx 1 && grep -c "advanceDue()" contracts/AfKing.sol | grep -q "[1-9]" && grep -c "boxesPending()" contracts/AfKing.sol | grep -q "[1-9]" && grep -c "nonReentrant" contracts/AfKing.sol | grep -qx 0 && echo PASS</automated>
  </verify>
  <done>`doWork()` is parameterless, routes autoBuy→advance→autoOpen on the rngLock-aware O(1) predicates, structurally early-returns after the first category with work, reverts `NoWork()` when all empty, and has NO nonReentrant guard.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Unified single creditFlip + the D-07 flat-per-tx reward as GAS-331 placeholders (ROUTER-10 / R4 / R5) + ADV-01..05 routing + D-01b marker</name>
  <read_first>
    - contracts/AfKing.sol (the doWork branches from Task 1; BOUNTY_ETH_TARGET, PRICE_COIN_UNIT, the mp source)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md (§1 R4/R5 — the flat-per-tx table + the placeholder posture + one creditFlip CEI-last)
  </read_first>
  <action>
    Define the D-07 GAS-331 placeholder constants with clear markers, e.g.:
    - `uint256 internal constant BOUNTY_BASE_NUM = 1; uint256 internal constant BOUNTY_BASE_DEN = 1; // GAS-331 PLACEHOLDER — the 1× base unit`
    - `uint256 internal constant ADVANCE_RATIO_NUM = 2; uint256 internal constant RATIO_DEN = 1; // GAS-331 PLACEHOLDER — advance 2×`
    - `uint256 internal constant BUY_RATIO_NUM = 3; uint256 internal constant BUY_RATIO_DEN = 2; // GAS-331 PLACEHOLDER — buy 1.5×` (use a num/den pair to express 1.5 without fractional literals)
    - `uint256 internal constant OPEN_KNEE = 5; // GAS-331 PLACEHOLDER — open pro-rate knee`
    (The exact names are the executor's discretion; the REQUIREMENT is each is clearly marked `// GAS-331 PLACEHOLDER` and the values are SPEC starting estimates, NOT locked — calibrated at Phase 331 under a USER gate.)

    Compute the base unit `unit = (BOUNTY_ETH_TARGET * PRICE_COIN_UNIT) / mp` (mp from `keeperSnapshot`/`mintPrice()`). Per branch set `bountyEarned`:
    - advance: `bountyEarned = rewardable ? (unit * ADVANCE_RATIO_NUM * mult) / RATIO_DEN : 0;` (the 2×·mult flat per-tx; mid-day/gameover mult=1, new-day mult from the tuple — ADV-02/05).
    - buy: `bountyEarned = (unit * BUY_RATIO_NUM) / BUY_RATIO_DEN;` (FLAT per-tx 1.5× — NOT × count; the buy leg is bounded once/day/sub).
    - open: `bountyEarned = (unit * (opened < OPEN_KNEE ? opened : OPEN_KNEE)) / OPEN_KNEE;` (1× pro-rated below the knee, flat 1× at/above).

    Author EXACTLY ONE `ICoinflip(ContractAddresses.COINFLIP).creditFlip(msg.sender, bountyEarned);` CEI-LAST, reached by all three branches after their work + before the `return` (e.g. a shared tail label or a single creditFlip after the if/else-if chain followed by `return`). Skip the creditFlip when `bountyEarned == 0` (the `!rewardable` advance case) — but still RETURN (do not fall to NoWork). The legs (`_autoBuy`/`advanceGame`/`autoOpen`) NEVER self-credit (verified in 330-01/02/06). Emit `AutoBuyCompleted`/an appropriate completion event if the existing event vocabulary fits, else leave events as-is.

    Add the standalone UNREWARDED escapes: `function autoBuy(uint256 count) external { _autoBuy(count); }` and `function autoOpen(uint256 count) external { IGame(ContractAddresses.GAME).autoOpen(count); }` — neither calls creditFlip (direct, non-router calls are unrewarded — ROUTER-01).

    Add the D-01b marker comment at the single creditFlip site: that TST-02 (Phase 332) is the router→game→creditFlip double-pay backstop proving the ROUTER-07 no-guard disposition (legs structurally cannot credit; only doWork credits once after the early-return).
  </action>
  <verify>
    <automated>grep -c "GAS-331 PLACEHOLDER" contracts/AfKing.sol | grep -q "[3-9]" && grep -c "ICoinflip(ContractAddresses.COINFLIP).creditFlip(msg.sender" contracts/AfKing.sol | grep -qx 1 && grep -c "OPEN_KNEE" contracts/AfKing.sol | grep -q "[2-9]" && grep -c "function autoBuy(uint256 count) external" contracts/AfKing.sol | grep -qx 1 && grep -c "function autoOpen(uint256 count) external" contracts/AfKing.sol | grep -qx 1 && echo PASS</automated>
  </verify>
  <done>Exactly ONE creditFlip in doWork (CEI-last, skipped at 0), fed by the chosen leg; the D-07 flat-per-tx reward (advance 2×·mult / buy flat 1.5× / open 1× pro-rated below OPEN_KNEE) wired as clearly-marked GAS-331 placeholders; the standalone UNREWARDED autoBuy(count)/autoOpen(count) escapes exist; the D-01b TST-02 marker present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keeper (any EOA) → doWork() → one of {_autoBuy, GAME.advanceGame, GAME.autoOpen} → the single creditFlip | the unified router; one rewarded category per tx |
| doWork → ICoinflip(COINFLIP).creditFlip | the single bounty (minted flip-credit, not an ETH push) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-330-07-01 | Spoofing / bounty-stacking | a tx earning more than one category's bounty | mitigate | The one-category STRUCTURAL early-return (ROUTER-03 / invariant a): each branch ends in `return` after the single creditFlip; advance/open/buy bounties can never stack in one tx. Proven at TST-02 (Phase 332, the one-rewarded-category assertion). |
| T-330-07-02 | Elevation of Privilege / reentrancy | router→game→creditFlip composed reentrancy double-pay | mitigate | NO nonReentrant guard (ROUTER-07), re-grounded on the unified single creditFlip: exactly ONE creditFlip in doWork, CEI-LAST, fed by legs that return raw counts/mult and NEVER self-credit; every external call is to a pinned ContractAddresses.* (GAME/COINFLIP); keeper-never-a-payee; the bounty is minted flip-credit, not an ETH push. D-01b TST-02 backstop proves no double-pay (Phase 332). |
| T-330-07-03 | Tampering / advance-timing MEV | same-tx bundling of advance-consume + buy/open | mitigate | RD-1 ordering: autoBuy runs FIRST at day-open (pre-entropy, rngLock false) BEFORE advance requests the word; advance consumes via the design-1 return (no new in-window SLOAD, ADV-04); autoOpen runs AFTER and is rngLock-blocked (boxesPending false during lock). The one-category early-return means a single doWork tx touches exactly one category — no same-tx advance+open bundling. Freeze proven at TST-01 (Phase 332). |
| T-330-07-04 | Denial of Service / faucet | the D-07 flat-per-tx reward farmed | mitigate | Placeholder peg calibrated at GAS (Phase 331, USER gate) to break-even at 0.5 gwei on the worst-case MARGINAL (the CR-01 rule); the open KNEE pro-rate kills the small-batch corner (per-box below the knee `1×/KNEE` ≤ a one-box 0.5-gwei gas → −EV); advance/buy have no gate (un-fakeable ticket batches / bounded once-per-day-per-sub). WR-01 round-trip guard at GAS-05. |
| T-330-07-05 | Denial of Service / liveness | autoBuy-highest-priority starving advance (D-04a) | accept | While buys pend the rewarded advance leg waits; buys drain monotonically (finite sub set, cursor advances each call) so advance runs once buys clear; first-30-min advance during a buy backlog relies on the SECONDARY bypass tiers (30-min permissionless + Vault/sStonk gameAdvance) + the death-clock — NOT the router bounty (329-ATTEST §G). Bounded delay, ACCEPTED. |
| T-330-07-06 | Tampering | a leg self-crediting (breaking the single-creditFlip premise) | mitigate | The legs are verified non-self-crediting: AdvanceModule's 3 creditFlips deleted (330-01), autoOpen's pulled out (330-02), _autoBuy's pulled out (330-06). The verify greps in those plans prove 0 leg creditFlips to the caller. |
| T-330-07-SC | Tampering | npm/forge installs | mitigate | No package installs (source edit only). |
</threat_model>

<verification>
- doWork() does its fixed per-leg default batch and does not OOG in the common case; a backlog larger than one batch leaves a remainder for the next call (proven at TST-02, Phase 332).
- Exactly one creditFlip per doWork tx (one-category early-return) — TST-02.
- The advance leg is rewarded via doWork (with mult honored) and unrewarded standalone — TST-03.
- The D-07 placeholders are calibrated to break-even at GAS (Phase 331) under a USER gate before TST exercises them.
- Compiles within the BATCH-02 `forge build` (330-09).
</verification>

<success_criteria>
- ROUTER-01: parameterless `doWork()` + standalone UNREWARDED `autoBuy(count)`/`autoOpen(count)` escapes.
- ROUTER-02/03: priority `autoBuy → advance → autoOpen` with a structural early-return.
- ROUTER-04: routes on the rngLock-aware O(1) predicates.
- ROUTER-06: `NoWork()` when all 3 predicates empty.
- ROUTER-10 / R4: exactly one creditFlip in doWork, CEI-last; legs return raw counts/mult, never self-credit.
- ADV-01/02/03/05: standalone advance unrewarded; the router pays from the `(mult, rewardable)` tuple; mid-day partial-drain rewarded with mult=1.
- D-07 flat-per-tx wired as GAS-331 placeholders; NO nonReentrant guard (ROUTER-07); D-01b marker present.
</success_criteria>

<output>
Create `.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-07-SUMMARY.md` when done.
</output>
