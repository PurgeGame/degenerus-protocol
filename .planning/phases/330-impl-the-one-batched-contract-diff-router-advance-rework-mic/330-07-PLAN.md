---
phase: 330-impl-the-one-batched-contract-diff-router-advance-rework-mic
plan: 07
type: execute
wave: 5
depends_on: ["330-01", "330-02", "330-03", "330-04", "330-05", "330-06"]
files_modified: []
autonomous: false
requirements: [BATCH-02]
must_haves:
  truths:
    - "The full batched diff compiles (`forge build` succeeds, 0 errors) with the reconciled shared signatures intact: `advanceGame()` returns `(uint8 mult, bool rewardable)` PRODUCED in AdvanceModule, DECLARED on the interface, and DECODED in the DegenerusGame wrapper; `doWork(maxCount)` routes one category per call with the structural early-return + `NoWork()`; `_autoBuy` internal refactor with the D-06 default; `degeneretteResolve`/`_degeneretteResolveBet` rename + the flat ≥3-gate re-peg; the GASOPT-01/02 hoists."
    - "The existing test suite shows NO NEW breakage vs the v48.0-closure baseline (last-known 632 passing / 42 failing forge + 21/0 Hardhat); 'net-zero' = equal pass-count AFTER the mandatory D-02 rename-fixes (parity-after-rename, not byte-identical); documented pre-existing failures are NOT regressions; the CrankLeversAndPacking literal-string assertions re-green to the renamed function names."
    - "The cross-item reconciliation joint-checks pass: the design-1 tuple produced/encoded/decoded coherently (no void-vs-tuple mismatch at the `:280` selector); ONE re-homed advance `creditFlip` in the router (mult-scaled, rewardable-gated, CEI-last) with the autoOpen/autoBuy legs paying their own bounty (no double-pay); NO `nonReentrant` guard anywhere (ROUTER-07); the KEEP-04 `bytes32(\"DGNRS\")` affiliate wiring is byte-unchanged; the two day-epochs are NOT merged (GAS-03/D-03a); the `totalFlipReversals` freeze is byte-intact (ADV-04); the placeholder constants live under the named D-01b marker; `degeneretteResolve` has NO interface row (C6)."
    - "The batched `contracts/*.sol` + `test/*.sol` diff is HELD at the contract-commit boundary and presented for ONE explicit USER hand-review; nothing is committed without that approval."
  artifacts: []
  key_links: []
---

<objective>
Verify the complete batched diff (plans 330-01..06) and HOLD it at the contract-commit boundary for the single USER hand-review. This plan compiles the whole tree, runs the existing test suite to confirm NO NEW breakage vs the v48.0-closure baseline (after the mandatory D-02 rename-fixes), runs the cross-item reconciliation joint-checks (the BATCH-01 surfaces settled in the 329 SPEC), and then presents the ONE batched diff for explicit user approval. It does NOT commit — the user reviews the diff first. This is the v48 Phase 326-08 analog.

Purpose: ROADMAP SC5 (BATCH-02) — the diff is reconciled per the SPEC's settled shared signatures (R1-R4 + the 4 invariants), compiles + passes the existing suite with no NEW breakage, and is HELD at the contract-commit boundary for ONE explicit USER hand-review. The single approval gate for the whole milestone's contract surface.
Output: a verified, compiling, non-regressing batched diff + the user-approval gate.

NON-NEGOTIABLE: This is the ONLY place the batched diff is presented for approval, and it is `autonomous: false`. NO task may state or imply the contracts are "pre-approved." The executor APPLIES nothing new here (all edits landed in 330-01..06); it VERIFIES + PRESENTS. Commit only AFTER the user explicitly approves the diff. Scope is IMPL-only: confirm it compiles + no NEW test breakage — do NOT author the behavioral/freeze/one-category/same-results/re-peg test proofs (Phase 332 TST) and do NOT run the formal RNG-freeze fuzz or calibrate any peg constant (Phase 331 GAS; may NOTE the freeze argument + the placeholder-marker hold).

COMMIT-ORDERING HAZARD ([[feedback_contract_commit_guard_hook]]): a PreToolUse hook blocks ALL commits while any `contracts/*.sol` is dirty. ALL planning/SUMMARY doc commits for plans 330-01..06 are BLOCKED once `.sol` is dirty (which it is throughout this phase). Sequence: (1) the user approves the diff at Task 3; (2) the SINGLE sanctioned contract+test commit lands using the approved bypass `CONTRACTS_COMMIT_APPROVED=1`; (3) ONLY THEN do the deferred `.planning/` SUMMARY doc commits land (`.planning/` is gitignored → force-add). Do NOT attempt any doc commit before the approved contract commit — it will be blocked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-01-SUMMARY.md
@.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-02-SUMMARY.md
@.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-03-SUMMARY.md
@.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-04-SUMMARY.md
@.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-05-SUMMARY.md
@.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-06-SUMMARY.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md

<baseline_note>
The v48.0-closure baseline is HEAD `0cc5d10f` (the live `contracts/` tree was byte-identical to it before this phase — `git diff --name-only 0cc5d10f HEAD -- 'contracts/*.sol'` was EMPTY). The documented suite baseline carried forward is the last-known v48 closure count: forge ~632 passing / 42 failing + Hardhat 21/0 (re-establish the EXACT current pre-330 baseline by reading the most recent prior-milestone TST/closure suite count before classifying — the v48.0 closure ran the suite). A NEW failure = a count above that baseline OR a previously-passing test now failing. "Net-zero" = equal pass-count AFTER the mandatory D-02 rename-fixes (the rename CHANGES test source, so this is parity-after-rename, NOT a byte-identical test tree). Do NOT chase the pre-existing baseline failures. Any test whose failure is a BEHAVIORAL consequence of the re-peg (a changed bounty value — flat-vs-per-item) is KNOWN-TST-DEFERRED (Phase 332 TST-05, against the calibrated constants), classified as such per 330-06 — NOT a 330 regression. The CrankLeversAndPacking literal source-string assertions MUST re-green to the renamed function names.
</baseline_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compile the full batched diff + run the cross-item reconciliation joint-checks (BATCH-01 / the 4 invariants + R1-R4)</name>
  <read_first>
    - All 6 SUMMARYs (330-01..06) — collect the per-plan dispositions: the AdvanceModule tuple return + 3 deleted creditFlip sites + the ADVANCE_BOUNTY_ETH grep-decision; the DegenerusGame wrapper decode + the two O(1) views + the rename/re-peg + the RESOLVE_FLAT_BURNIE/NoWork decls + the AUTO_RESOLVE_BET_GAS_UNITS grep-decision; the interface tuple + view sigs (no C6 row); the AfKing doWork router + the _autoBuy refactor + the D-06 default + the EmptyAutoBuy removal + GASOPT-02 + the marker constants + no-guard; the MintModule GASOPT-01 hoists; the test rename + any KNOWN-TST-DEFERRED value failures
    - 329-SPEC.md §1 R1-R4 (the shared-signature reconciliation surfaces) + §2 the 4 invariants + the dispositions + §3 the edit-order map + §0 corrections C1-C6
  </read_first>
  <action>
    Compile + reconcile. (1) `forge build` over the whole tree — it MUST succeed (0 errors). Any error means a cross-plan reconciliation gap (a void-vs-tuple selector mismatch, a dangling rename, a stale `EmptyAutoBuy`/`autoResolve` reference, a duplicated `NoWork()` decl collision, a missing IGame consumer decl) — fix it (this plan owns final cross-plan reconciliation for BATCH-01/BATCH-02). (2) Run the joint-checks (grep + targeted read), recording each result PASS/FAIL with evidence in the SUMMARY:
      - R1 (the design-1 tuple): `advanceGame()` returns `(uint8 mult, bool rewardable)` PRODUCED on `AdvanceModule.sol`, DECLARED on `IDegenerusGameAdvanceModule` (the `DegenerusGame.sol:280` selector binds the tuple), and DECODED via `abi.decode(data, (uint8, bool))` on the `DegenerusGame.sol` wrapper success branch — no void-vs-tuple mismatch anywhere. `rewardable` is a DISTINCT bool (not `mult > 0`).
      - R2 (the router): `doWork(uint256 maxCount)` exists, routes advance → autoOpen → autoBuy with an EXPLICIT per-category `return` (invariant a, no fall-through); `revert NoWork()` when all 3 O(1) predicates empty (ROUTER-06); per-leg maxCount (advance no count; autoOpen(maxCount); _autoBuy(maxCount)); `maxCount==0` → the fixed `DEFAULT_*_COUNT` (no `gasleft()` loop); `EmptyAutoBuy` removed (0 refs).
      - R3 (the views): `advanceDue()` (new-day OR mid-day) + `boxesPending()` are O(1) (no loop in either body); buys-pending is AfKing-local; the views exposed on the interfaces; ROUTER-04 (no unbounded scan).
      - R4 (the bounty composition): the router fires exactly ONE re-homed advance `creditFlip` (mult-scaled, rewardable-gated, CEI-last); the autoOpen (`DegenerusGame:1676`) + autoBuy (`AfKing` leg) bounties stay in-callee (no router-side double-pay); the one-category early-return preserves one-creditFlip-per-tx. KEEP-04 `bytes32("DGNRS")` is byte-unchanged at the game self-call (NOT in AfKing — C1); `batchPurchase` carries no affiliate arg.
      - Invariant (a): an EXPLICIT `return` follows each entered category in `doWork` (read the body).
      - Invariant (b) / ADV-04: `_applyDailyRng` + `totalFlipReversals` (`:1838`/`:1844`) byte-unchanged; the router/wrapper introduce NO new in-window SLOAD.
      - Invariant (c) / D-04: the 30-min universal bypass, the Vault/sStonk `gameAdvance()` wrappers, and the 120-day death-clock are byte-unchanged (the fallback hierarchy intact; only the PAYMENT re-homed). Standalone `advanceGame()` still drives the full tick unrewarded.
      - Invariant (d) / GAS-03: the two day-epochs (AfKing absolute-day stall ladder vs AdvanceModule game-day band) are NOT physically merged; the advance `mult` is single-sourced + returned + consumed without recompute.
      - ROUTER-07 / D-01: NO `nonReentrant` guard on `doWork` or any leg.
      - D-05: `degeneretteResolve`/`_degeneretteResolveBet`/`this._degeneretteResolveBet` present, zero stale `autoResolve`; the re-peg = per-item `++successCount` (NON-WWXRP) + `successCount >= 3` flat `RESOLVE_FLAT_BURNIE` + `NoWork()`-at-0 + resolve-and-pay-0 for 1–2; WWXRP excluded from the count; AUTO-02/try-catch/self-resolve/one-creditFlip preserved; NO `degeneretteResolve` interface row (C6).
      - D-01b: the NEW placeholder constants (`RESOLVE_FLAT_BURNIE`, `GAS_BUDGET`, `DEFAULT_AUTO_OPEN_COUNT`, `DEFAULT_AUTO_BUY_COUNT`) live under named, greppable marker blocks with what-IS comments (no history); the v46/v48 constants (`AUTO_GAS_PRICE_REF`, `AUTO_OPEN_BOX_GAS_UNITS`) are unchanged (D-01a); `AUTO_RESOLVE_BET_GAS_UNITS` removed-only-if-dead per the 330-02 grep-decision.
      - GASOPT-01/02: two `owedMap = ticketsOwedPacked[rk]` hoists in MintModule; the AfKing claimable read hoisted lazily — both gas-only, no behavior change.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && timeout 500 forge build 2>&1 | tail -3; echo "--- total build errors (MUST be 0) ---"; timeout 500 forge build 2>&1 | grep -ciE "^error|Error \(" | tr -d "\n"; echo " build errors"; echo "--- design-1 tuple coherence ---"; grep -c "abi.decode(data, (uint8, bool))" contracts/DegenerusGame.sol | tr -d "\n"; echo " wrapper decode (MUST be 1)"; grep -c "function advanceGame() external returns (uint8 mult, bool rewardable);" contracts/interfaces/IDegenerusGameModules.sol | tr -d "\n"; echo " interface tuple (MUST be 1)"; echo "--- router + no-guard + EmptyAutoBuy gone ---"; grep -c "function doWork(uint256 maxCount)" contracts/AfKing.sol | tr -d "\n"; echo " doWork (MUST be 1)"; grep -c "nonReentrant" contracts/AfKing.sol | tr -d "\n"; echo " nonReentrant (MUST be 0)"; grep -c "EmptyAutoBuy" contracts/AfKing.sol | tr -d "\n"; echo " EmptyAutoBuy (MUST be 0)"; echo "--- rename + affiliate (contracts/) ---"; grep -rc "autoResolve\|_autoResolveBet" contracts/ | grep -v ":0" || echo "zero stale autoResolve in contracts/ (GOOD)"; grep -c 'bytes32("DGNRS")' contracts/DegenerusGame.sol | tr -d "\n"; echo " DGNRS affiliate (MUST be ≥1, unchanged)"; echo "--- C6: no degeneretteResolve interface row ---"; grep -rc "degeneretteResolve" contracts/interfaces/ | grep -v ":0" || echo "zero degeneretteResolve interface rows (C6 GOOD)"; echo "--- ADV-04 freeze untouched ---"; grep -n "totalFlipReversals" contracts/modules/DegenerusGameAdvanceModule.sol; echo "--- GASOPT-01 hoists ---"; grep -c "storage owedMap = ticketsOwedPacked\[rk\]" contracts/modules/DegenerusGameMintModule.sol | tr -d "\n"; echo " owedMap hoists (MUST be 2)"</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` succeeds with ZERO errors over the whole tree.
    - All BATCH-01/02 joint-checks (R1-R4 + the 4 invariants + ROUTER-07 + D-05 + D-01a/b + GASOPT-01/02 + C1/C6) PASS with grep/read evidence recorded in the SUMMARY.
    - The design-1 tuple is coherent end-to-end (produced/declared/decoded; no void-vs-tuple mismatch); the router fires exactly ONE re-homed advance creditFlip with no double-pay; NO `nonReentrant` guard; `EmptyAutoBuy` gone; zero stale `autoResolve` in `contracts/`; `bytes32("DGNRS")` unchanged; zero `degeneretteResolve` interface rows (C6); `totalFlipReversals` byte-intact; two `owedMap` hoists.
  </acceptance_criteria>
  <done>The full batched diff compiles; every cross-item reconciliation joint-check (the 4 invariants + R1-R4 + the dispositions) passes with recorded evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Run the existing test suite — confirm NO NEW breakage vs the v48.0 baseline (parity-after-rename)</name>
  <read_first>
    - <baseline_note> above (the documented pre-existing failures + the parity-after-rename definition + the KNOWN-TST-DEFERRED re-peg-value class)
    - The most recent prior-milestone TST/closure suite count (the v48.0 closure ran the suite — re-establish the exact pre-330 baseline before classifying: forge ~632/42 + Hardhat 21/0)
    - 330-06-SUMMARY.md (the test rename-fixes + any pre-classified KNOWN-TST-DEFERRED re-peg value failures)
  </read_first>
  <action>
    Run the existing test suite (`forge test`, plus the Hardhat suite if the project's standard invocation includes it — check `foundry.toml`/`package.json`). Capture the pass/fail count. Re-establish the exact pre-330 baseline (the v48.0-closure suite count: forge ~632/42, Hardhat 21/0) and compare. "Net-zero" is parity-after-rename (the D-02 rename CHANGES test source). Classify EVERY failure: (a) a documented pre-existing baseline failure (unrelated to v49 — do NOT chase), (b) a KNOWN-TST-DEFERRED failure caused by the flat re-peg's CHANGED bounty value vs an old per-item-peg assertion (Phase 332 TST-05 re-validates against the calibrated constants; record as deferred, NOT a 330 regression), or (c) a NEW failure attributable to the 330 diff (a compile/symbol break from a missed rename, a router/refactor logic bug, a wrapper-decode mismatch). For any class-(c) NEW failure, root-cause it and fix the CONTRACT or the rename (NOT a new test — behavioral tests are Phase 332; only fix a contract/rename bug the test legitimately catches). Goal: ZERO class-(c) failures. Confirm the CrankLeversAndPacking literal source-string assertions re-green to the renamed names. Note (do NOT execute): the TST-01 freeze fuzz + TST-02 one-category/reentrancy + TST-03 reward-routing/GASOPT same-results + TST-05 re-peg proofs are Phase 332 — you MAY note the freeze argument (the router consumes the design-1 return, no new in-window SLOAD per ADV-04) and the no-guard / no-double-pay composition hold per the SPEC, but the formal proofs are out of scope here.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && timeout 600 forge test 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - The suite runs to completion; the pass/fail count is captured and compared to the re-established v48.0 baseline (forge ~632/42, Hardhat 21/0).
    - Every failure is classified (a) pre-existing-baseline, (b) KNOWN-TST-DEFERRED (re-peg changed-value), or (c) NEW-330; ZERO class-(c) failures (any is contract/rename-root-caused + fixed).
    - The CrankLeversAndPacking literal source-string assertions re-green to `"function degeneretteResolve("` / `"function _degeneretteResolveBet("`.
    - The KNOWN-TST-DEFERRED re-peg-value failures (if any) are recorded as deferred-to-Phase-332, not 330 regressions.
  </acceptance_criteria>
  <done>The suite shows no NEW (class-c) breakage vs the v48.0 baseline; all failures classified; the diff is correctness-clean at the IMPL level (the re-peg behavioral proofs pending Phase 332 against the calibrated constants).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: USER hand-review of the ONE batched v49.0 contract diff (the single approval gate)</name>
  <what-built>
    The complete v49.0 batched `contracts/*.sol` + `test/*.sol` diff — the unified keeper router + advance-bounty rework + the degeneretteResolve rename/re-peg + the two micro-opts, reconciled per the 329 SPEC's settled shared signatures (R1-R4) + the 4 structural invariants, in producer-before-consumer order:
    1. AdvanceModule (PRODUCER) — `advanceGame()` void → `(uint8 mult, bool rewardable)`; the 3 caller-reward `creditFlip` sites (`:189`/`:225`/`:468`) deleted (standalone `advanceGame()` pays no bounty); the `:876` SDGNRS merge-credit kept; `totalFlipReversals` freeze byte-intact. (ADV-01/02/03/05.)
    2. DegenerusGame — the wrapper decodes `(uint8 mult, bool rewardable)` on the delegatecall success branch + forwards it; the O(1) `advanceDue()` (new-day OR mid-day) + `boxesPending()` views; `autoResolve`→`degeneretteResolve` + `_autoResolveBet`→`_degeneretteResolveBet` rename; the flat re-peg (per-item `++successCount` NON-WWXRP, `successCount >= 3` → flat `RESOLVE_FLAT_BURNIE` creditFlip, `NoWork()` at 0, resolve-and-pay-0 for 1–2); `RESOLVE_FLAT_BURNIE = 1e18` under the D-01b marker; the `bytes32("DGNRS")` affiliate wiring untouched (C1). (ADV-02, ROUTER-04, ROUTER-05, BATCH-02.)
    3. Interfaces — `IDegenerusGameAdvanceModule.advanceGame()` → the tuple; `IDegenerusGame` gains `advanceDue()`/`boxesPending()`; NO `degeneretteResolve` interface row (C6). (ADV-02, ROUTER-04.)
    4. AfKing (CONSUMER) — `doWork(uint256 maxCount)` routes advance → autoOpen → autoBuy with a STRUCTURAL early-return; `error NoWork()` on no work; `autoBuy` → internal `_autoBuy(maxCount)` + a thin external wrapper; `maxCount==0` → the fixed `DEFAULT_*_COUNT` (no gasleft, no EmptyAutoBuy); the GASOPT-02 claimable-hoist; `GAS_BUDGET`/`DEFAULT_*_COUNT` under the D-01b marker; NO `nonReentrant` guard (strict CEI, keeper-never-a-payee); the re-homed advance bounty paid once (mult-scaled, rewardable-gated, CEI-last); the autoOpen/autoBuy legs keep their own bounty (no double-pay). (ROUTER-01/02/03/04/05/06, GASOPT-02.)
    5. MintModule — the GASOPT-01 `ticketsOwedPacked[rk]` storage-pointer hoist in `processFutureTicketBatch` + `processTicketBatch` (gas-only). (GASOPT-01.)
    6. Tests — the 5 test files / 57 references to `autoResolve`/`_autoResolveBet` renamed (call-sites + the CrankLeversAndPacking literal source-string assertions + comments), rename-fixes ONLY (D-02). (BATCH-02.)
    Verified: `forge build` succeeds; all BATCH-01/02 reconciliation joint-checks pass; the existing suite shows no NEW breakage vs the v48.0 baseline (forge ~632/42, Hardhat 21/0; any re-peg changed-value failures are deferred to the Phase-332 TST against the calibrated constants). The peg/count/BURNIE constants are SPEC placeholders under the D-01b marker, calibrated at Phase 331 GAS under a SECOND user-approved gate.
  </what-built>
  <how-to-verify>
    1. Review the full batched diff: `git diff -- contracts/ test/` (read every changed file; this is the single hand-review for the whole v49.0 contract surface).
    2. Confirm the reconciled surfaces match the SPEC: `advanceGame()` produces/declares/decodes `(uint8 mult, bool rewardable)` coherently (no void-vs-tuple mismatch); `doWork(maxCount)` routes ONE category per call with an explicit per-category `return` (no bounty-stacking) and reverts `NoWork()` on no work; the 3 discovery predicates are O(1) (no unbounded scan); the re-homed advance bounty is paid ONCE (mult-scaled, rewardable-gated, CEI-last) with autoOpen/autoBuy paying their own bounty (no double-pay); NO `nonReentrant` guard anywhere (ROUTER-07 — keeper-never-a-payee + no untrusted ETH send + CEI-last); `autoBuy`→`_autoBuy` with `maxCount==0`→fixed default (no gasleft, no EmptyAutoBuy); the `degeneretteResolve` rename + the flat ≥3-NON-WWXRP-gate/revert-on-0/pay-0-for-1–2 re-peg (WWXRP excluded from the count) with AUTO-02/try-catch/self-resolve/one-creditFlip preserved and NO interface row (C6); the GASOPT-01/02 hoists are gas-only; the placeholder constants (`RESOLVE_FLAT_BURNIE`/`GAS_BUDGET`/`DEFAULT_*_COUNT`) live under the named D-01b marker with what-IS comments; `bytes32("DGNRS")` + the two day-epochs + `totalFlipReversals` are byte-unchanged.
    3. Confirm the build/test evidence in the 330-07 SUMMARY (forge build clean; no NEW failures vs baseline; re-peg-value failures classified deferred).
    4. If approved, the diff may be committed (a single sanctioned contract+test commit using the approved bypass `CONTRACTS_COMMIT_APPROVED=1`); the `.planning/` SUMMARY docs (already authored, commits deferred while `.sol` was dirty) are force-added in the SAME or an immediately-following commit. If issues are found, describe them for a gap-closure pass.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the single batched v49.0 contract+test commit, or describe the issues to fix (gap-closure).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| applied .sol diff → committed source | the contract-commit boundary; ONLY crossed after explicit user hand-review (the v49.0 HARD STOP). |
| cross-item reconciliation | the 6 plans' edits to the shared design-1 tuple / discovery views / rename / router compose into one coherent diff (BATCH-01/02); a void-vs-tuple selector mismatch or a dangling rename would ship a broken intermediate. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-330-07-01 | Tampering | a cross-item reconciliation gap ships silently | mitigate | `forge build` MUST be zero-error + the R1-R4 / 4-invariant / disposition / GASOPT joint-checks run with grep+read evidence before the gate (the design-1 tuple coherence, the no-guard, the no-double-pay, the C6 no-interface-row, the freeze byte-intact). |
| T-330-07-02 | Repudiation | a contract committed without review | mitigate | Task 3 is `autonomous:false` blocking-human; NO pre-approval; commit only after explicit "approved"; the commit-guard hook blocks commits while `.sol` is dirty (the `CONTRACTS_COMMIT_APPROVED=1` bypass is used only post-approval). |
| T-330-07-03 | Tampering | a v49 regression masquerades as a baseline / TST-deferred failure | mitigate | Every test failure is classified (a) pre-existing-baseline, (b) KNOWN-TST-DEFERRED (re-peg changed-value), or (c) NEW-330; only (a)+(b) are tolerated; any (c) is contract/rename-root-caused + fixed. The re-peg-value class is explicitly bounded to the degeneretteResolve bounty assertions; the CrankLeversAndPacking literal assertions MUST re-green. |
| T-330-07-04 | Tampering | the no-guard / freeze / one-category composition breakage slips past IMPL | accept | The ROUTER-07 no-guard basis (per-leg no-untrusted-send, keeper-never-a-payee, CEI-last), the ADV-04 freeze (no new in-window SLOAD), and the one-category early-return are NOTED to hold per the SPEC; the FORMAL empirical proofs (TST-01 freeze fuzz, TST-02 one-category/double-pay reentrancy, TST-03 reward-routing/GASOPT same-results, TST-05 re-peg) are Phase 332 (out of IMPL scope). The IMPL joint-checks (Task 1) already assert the structural invariants by read+grep. |
| T-330-07-05 | Repudiation | a doc/SUMMARY commit attempted before the approved contract commit (blocked by the hook) | mitigate | The commit-ordering is explicit: approve → single sanctioned contract+test commit (`CONTRACTS_COMMIT_APPROVED=1`) → then the deferred `.planning/` SUMMARY commits (force-add, `.planning/` gitignored). No doc commit is attempted while `.sol` is dirty. |
| T-330-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this milestone (audit-remediation, no new deps). No `## Package Legitimacy Audit` required. |
</threat_model>

<verification>
- `forge build`: zero errors over the whole tree.
- All BATCH-01/02 joint-checks (R1-R4 + the 4 invariants + ROUTER-07 + D-05 + D-01a/b + GASOPT-01/02 + C1/C6) PASS with recorded evidence.
- `forge test` (+ Hardhat): no NEW (class-c) failures vs the v48.0 baseline (forge ~632/42, Hardhat 21/0); all failures classified (re-peg changed-value = TST-deferred); the CrankLeversAndPacking literal assertions re-green.
- Task 3 reached as a blocking-human gate; nothing committed until explicit user approval; doc commits deferred until after the approved contract commit.
</verification>

<success_criteria>
- ROADMAP SC5 (BATCH-02) satisfied: the diff is reconciled per the SPEC's settled shared signatures + the 4 invariants, authored producer-before-consumer (so no intermediate ships where advancing is unrewarded), compiles + passes the existing suite with no NEW breakage, CEI on every leg (`creditFlip` last) with no multiplier duplication and no reentrancy guard, and is HELD at the contract-commit boundary for the single explicit USER hand-review (no pre-approval).
</success_criteria>

<output>
Create `.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-07-SUMMARY.md` when done. Record: the forge build result, the full BATCH-01/02 joint-check table (with grep/read evidence for R1-R4 + the 4 invariants + the dispositions + GASOPT), the test pass/fail count vs the re-established v48.0 baseline + the failure classification (incl. the re-peg KNOWN-TST-DEFERRED), and the user-approval outcome. The single batched contract+test commit happens ONLY after explicit user approval (this plan does not pre-commit); the deferred `.planning/` SUMMARY doc commits land AFTER that approved contract commit.
</output>
