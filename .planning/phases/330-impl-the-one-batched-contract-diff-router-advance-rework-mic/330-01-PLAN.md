---
phase: 330-impl-the-one-batched-contract-diff-router-advance-rework-mic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/modules/DegenerusGameAdvanceModule.sol
autonomous: true
requirements: [ADV-01, ADV-02, ADV-03, ADV-05]
must_haves:
  truths:
    - "`advanceGame()` on `DegenerusGameAdvanceModule.sol` returns `(uint8 mult, bool rewardable)` instead of void — `mult` is the single-sourced stall multiplier from the game-day band (currently `:243-254`), `rewardable` is a DISTINCT bool (NOT `mult > 0`) that is TRUE on each of the 3 classified advance-leg work sites and FALSE otherwise."
    - "The 3 caller-reward `creditFlip(caller, …)` sites (new-day `:189`, mid-day partial-drain ADV-05 `:225`, main new-day `:468`) are DELETED — standalone `advanceGame()` pays NO bounty; the advance state machine (stage transitions, ticket batches, jackpot, daily-RNG) is byte-functionally unchanged and still drives the full tick unrewarded."
    - "The `:876` SDGNRS future→next merge-credit `creditFlip(ContractAddresses.SDGNRS, …)` STAYS — it is a protocol accounting credit, NOT a caller bounty, and is excluded from the deletion."
    - "`advanceGame()` reads only FROZEN VRF-window state — no new mutable in-window SLOAD is introduced; `totalFlipReversals` (`:1838`) consume+reset (`:1844`) inside `_applyDailyRng` (`:1834`) is byte-unchanged (ADV-04 / invariant b)."
  artifacts:
    - path: "contracts/modules/DegenerusGameAdvanceModule.sol"
      provides: "advanceGame() returning (uint8 mult, bool rewardable); the 3 caller-reward creditFlip sites removed; the SDGNRS merge-credit retained"
      contains: "returns (uint8"
  key_links:
    - from: "contracts/modules/DegenerusGameAdvanceModule.sol advanceGame() return tuple"
      to: "contracts/DegenerusGame.sol :275 wrapper abi.decode (consumed in plan 330-02)"
      via: "delegatecall return-data encoding of (uint8 mult, bool rewardable)"
      pattern: "returns \\(uint8"
---

<objective>
PRODUCER FIRST (SPEC §3 edit-order step 1). Re-home the advance bounty: delete the 3 caller-reward `creditFlip` sites in `DegenerusGameAdvanceModule.sol` and convert `advanceGame()` from void to `returns (uint8 mult, bool rewardable)` so the AfKing router (plan 330-04) can consume the design-1 tuple and pay the re-homed bounty. This is the keystone edit — nothing downstream can be wired until this return shape exists.

Purpose: ROADMAP SC3 (ADV-01/02/03/05). ADV-01 = the 3 `creditFlip(caller,…)` sites (`:189`/`:225`/`:468`) removed → standalone `advanceGame()` pays no bounty. ADV-02 = `advanceGame` returns the stall multiplier + a rewardable flag so the router pays the re-homed bounty from the multiplier's canonical day-epoch home (no recompute in a money path). ADV-03 = standalone `advanceGame()` stays a fully-functional unrewarded liveness fallback (the SPEC-identified free-fallback caller hierarchy is intact — invariant c). ADV-05 = mid-day partial-drain ticket processing (`:225`) is router-rewardable advance-leg work covered by `rewardable`.
Output: `advanceGame()` with the `(uint8 mult, bool rewardable)` return; the 3 caller-reward `creditFlip` sites deleted; the `:876` SDGNRS merge-credit retained.

NON-NEGOTIABLE: This plan EDITS `contracts/*.sol` but MUST NOT commit anything. The whole milestone is ONE batched diff committed only after the final USER-APPROVAL gate (plan 330-07). Do NOT git commit during execution (the commit-guard hook blocks commits while `.sol` is dirty anyway). NOTHING in this plan is pre-approved — the user reviews the diff at 330-07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md

<grep_protocol>
Every cited `file:line` is from the v48.0-closure baseline `0cc5d10f` (live tree byte-identical — confirmed: `git diff --name-only 0cc5d10f HEAD -- 'contracts/*.sol'` is EMPTY). This is the FIRST edit to this file and nothing else in the batched diff touches it, so lines barely drift — but RE-GREP `function advanceGame`, `creditFlip(` (to find all 4 sites + classify which 3 are caller-rewards), and the stall band at `bountyMultiplier`/`dayStart` before editing. The 329-ATTEST-ROUTER-ADVANCE §D.1/§D.2 table (D1-D8) is the source-of-truth for the site classifications; §E is the ADV-04 freeze attestation; §C.1 is the single-sourced multiplier.
</grep_protocol>

<interfaces>
<!-- Anchors extracted from live source at 0cc5d10f. Re-grep before editing — these shift only within this file. -->

contracts/modules/DegenerusGameAdvanceModule.sol — current state at 0cc5d10f:
  :155  function advanceGame() external {              // VOID today — gains the (uint8 mult, bool rewardable) return
  :242  uint256 bountyMultiplier = 1;                   // the single-sourced stall multiplier base
  :243-246  dayStart = (uint256(day - 1) + ContractAddresses.DEPLOY_DAY_BOUNDARY) * 1 days + 82_620;
  :247  uint256 elapsed = ts - dayStart;
  :248-254  bands: 2h→6× / 1h→4× / 20m→2×                // the `mult` value to return
  :147  uint256 private constant ADVANCE_BOUNTY_ETH = 0.005 ether;   // the standalone caller-reward peg (referenced by :191/:227/:470)
  :189  creditFlip(caller, …)  — NEW-DAY caller bounty (REWARDABLE advance-leg)        [DELETE; bounty math :191]
  :225  creditFlip(caller, …)  — MID-DAY partial-drain caller bounty (ADV-05, REWARDABLE) [DELETE; bounty math :227]
  :468  creditFlip(caller, …)  — MAIN NEW-DAY caller bounty (REWARDABLE, ×bountyMultiplier) [DELETE; bounty math :470]
  :876  creditFlip(ContractAddresses.SDGNRS, …)  — future→next merge-credit (NOT a caller bounty) [KEEP — bounty math :877-878]
  :1834 _applyDailyRng(uint32 day, uint256 rawWord) private returns (uint256 finalWord)
  :1838 uint256 nudges = totalFlipReversals;            // the player-controllable in-window nudge (ADV-04)
  :1844 totalFlipReversals = 0;                          // consume+reset — byte-UNCHANGED
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the 3 caller-reward creditFlip sites + add the (uint8 mult, bool rewardable) return to advanceGame()</name>
  <read_first>
    - contracts/modules/DegenerusGameAdvanceModule.sol (`advanceGame()` :155; the 4 `creditFlip(` sites :189/:225/:468/:876 — classify the 3 caller-rewards vs the :876 SDGNRS merge-credit by reading the FIRST argument: `caller` = delete, `ContractAddresses.SDGNRS` = keep; the bounty math at :191/:227/:470 deleted with their sites; `ADVANCE_BOUNTY_ETH` :147 — grep whether it has any remaining consumer after the 3 deletions, leave it if `:877-878` or anything else still references it, remove it ONLY if grep shows zero surviving references; the stall band `bountyMultiplier`/`dayStart`/`elapsed` :242-254 — the `mult` source; `_applyDailyRng` :1834 + `totalFlipReversals` :1838/:1844 — READ to confirm NOT touched)
    - 329-SPEC.md §1 R1 (the `(uint8 mult, bool rewardable)` final return shape + the DISTINCT-bool semantics) + §2 invariant (b) (ADV-04 — no new in-window read) + §2 invariant (c) (ADV-03 — the fallback hierarchy stays intact, delete the payment NOT the function) + §3 edit-order step 1
    - 329-ATTEST-ROUTER-ADVANCE.md §D.1 (D1 the advanceGame void→tuple site, D4 the wrapper decode that consumes it downstream) + §D.2 (D5/D6/D7 the 3 REWARDABLE caller-reward classifications, D8 the :876 NOT-router-rewardable SDGNRS merge-credit) + §E (the totalFlipReversals freeze — DO NOT touch)
  </read_first>
  <action>
    Per ADV-01/02/03/05 + SPEC §1 R1 + §3 step 1 + the 329-ATTEST §D classifications. Re-grep `function advanceGame`, `creditFlip(`, and the stall band first.
    (1) DELETE the 3 caller-reward `creditFlip` sites and their bounty-math expressions: the new-day site (`:189`, math `:191`), the mid-day partial-drain site (`:225`, math `:227`, ADV-05), and the main new-day site (`:468`, math `:470`, the `×bountyMultiplier`-scaled one). Identify them by the FIRST `creditFlip` argument being `caller` (the advance caller). Each is a pure caller-incentive payout — deleting it leaves the surrounding advance logic (stage transitions, ticket batches, jackpot, daily-RNG, the `emit Advance(...)`, the `return` control flow) byte-functionally intact (ADV-01/ADV-03). Do NOT alter any `emit`/`return`/state-mutation that shared the block — remove ONLY the `creditFlip` call + its argument expression.
    (2) KEEP the `:876` `creditFlip(ContractAddresses.SDGNRS, …)` future→next merge-credit (math `:877-878`) — its FIRST argument is the pinned `ContractAddresses.SDGNRS`, NOT the caller; it is a protocol accounting credit, NOT router-rewardable (D8).
    (3) Change `function advanceGame() external {` (`:155`) to `function advanceGame() external returns (uint8 mult, bool rewardable) {` and populate the return on every exit path: `mult` = the single-sourced stall multiplier from the `:242-254` band (return the computed `bountyMultiplier` cast to `uint8`; on paths that do not compute a stall multiplier, return `mult = 1` for the base / `0` where no advance-leg work occurred — match the SPEC R1 semantics: `mult` is the stall band value the router scales the re-homed bounty by); `rewardable` = a DISTINCT bool set TRUE on each of the 3 deleted-site work conditions (the new-day advance-leg, the mid-day partial-drain ADV-05 leg, the main new-day leg) and FALSE on all other exit paths (e.g. a no-work return, or a path whose only credit was the `:876` SDGNRS merge). `rewardable` MUST NOT be derived as `mult > 0` (a leg can have `mult ≥ 1` yet be non-rewardable — D-05/R1 DISTINCT-bool verdict). Ensure EVERY `return;` and every implicit fall-through end-of-function path now returns the tuple (Solidity requires all paths return when a return type is declared) — wire each to the correct `(mult, rewardable)` per the control-flow class it sits in.
    (4) `ADVANCE_BOUNTY_ETH` (`:147`): grep for surviving references after the 3 deletions. If ZERO references remain, remove the constant (dead). If anything still references it, leave it untouched. Do NOT pre-delete on assumption — grep-decide.
    (5) DO NOT touch `_applyDailyRng` (`:1834`), `totalFlipReversals` (`:1838`/`:1844`), or any rng-window read/write (ADV-04 / invariant b — the freeze window is byte-frozen; the router introduces no new in-window SLOAD by consuming the design-1 return). Comments describe what IS (no "was/changed-from" history).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && echo "--- return shape ---"; grep -c "function advanceGame() external returns (uint8 mult, bool rewardable)" contracts/modules/DegenerusGameAdvanceModule.sol | tr -d "\n"; echo " (MUST be 1)"; echo "--- creditFlip site count (caller-rewards gone, SDGNRS merge stays = MUST be 1) ---"; grep -c "creditFlip(" contracts/modules/DegenerusGameAdvanceModule.sol | tr -d "\n"; echo " creditFlip calls remaining"; echo "--- SDGNRS merge-credit retained ---"; grep -c "creditFlip(ContractAddresses.SDGNRS" contracts/modules/DegenerusGameAdvanceModule.sol | tr -d "\n"; echo " (MUST be ≥1)"; echo "--- totalFlipReversals freeze untouched ---"; grep -n "totalFlipReversals" contracts/modules/DegenerusGameAdvanceModule.sol; echo "--- build ---"; timeout 400 forge build 2>&1 | grep -ciE "^error|Error \(" | tr -d "\n"; echo " build errors (MUST be 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function advanceGame() external returns (uint8 mult, bool rewardable)"` returns exactly 1 — the void signature is gone and the tuple return is present.
    - The `creditFlip(` call count drops to exactly 1 (only the `:876` SDGNRS merge-credit survives); `grep -c "creditFlip(ContractAddresses.SDGNRS"` returns ≥1 (the merge-credit is the survivor).
    - `git diff` shows the 3 deleted sites were `creditFlip(caller, …)` (first arg `caller`), and that no `emit Advance`/`return`/state mutation that shared those blocks was altered — only the `creditFlip` call + its bounty-math expression were removed.
    - Every exit path of `advanceGame()` returns `(uint8 mult, bool rewardable)`; `rewardable` is set as a DISTINCT bool (NOT `mult > 0`) — confirm by reading the return assignments: the 3 advance-leg work paths set `rewardable = true`, all other paths set `rewardable = false`.
    - `git diff` shows NO hunk inside `_applyDailyRng` and NO edit to `totalFlipReversals` (ADV-04 freeze byte-intact).
    - `ADVANCE_BOUNTY_ETH` is removed ONLY if grep showed zero surviving references; otherwise byte-unchanged (the SUMMARY records which).
    - `forge build` reports 0 errors.
  </acceptance_criteria>
  <done>`advanceGame()` returns `(uint8 mult, bool rewardable)` on all paths (rewardable a distinct bool); the 3 caller-reward `creditFlip` sites are deleted; the `:876` SDGNRS merge-credit and the `totalFlipReversals` freeze are byte-untouched; the file compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| advance caller → re-homed bounty | the payment for advance work moves from the standalone `advanceGame()` caller-reward sites into the router (plan 330-04); this plan must DELETE the payment without breaking the advance state machine or the unrewarded-fallback liveness. |
| advance-consume → VRF window | `advanceGame()` consumes the daily VRF tick; the freeze invariant (ADV-04 / v45) requires no new mutable in-window SLOAD — the return-tuple addition reads only the already-computed stall multiplier, NOT rng-window state. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-330-01-01 | Denial of Service | standalone `advanceGame()` liveness after the bounty is deleted (invariant c / ADV-03) | accept | The 3 deleted sites are pure caller-incentive payouts; the advance state machine does not depend on them. The SPEC-attested free-fallback hierarchy (30-min universal bypass `AdvanceModule:1012`, Vault/sStonk `gameAdvance()` wrappers, the 120-day death-clock) is unchanged — re-homing reduces NO structural caller (329-ATTEST §G, D-04). No new fallback added (no future-proofing). |
| T-330-01-02 | Tampering | a non-rewardable path leaks the re-homed bounty (`rewardable` derived from `mult > 0`) | mitigate | `rewardable` is a DISTINCT bool set TRUE only on the 3 classified advance-leg work conditions and FALSE elsewhere (R1 verdict) — a fallback caller / the `:876`-only path returns `rewardable = false` so the router pays nothing. Verified by reading the return assignments; the empirical one-category/no-double-pay proof is TST-02/TST-03 (Phase 332). |
| T-330-01-03 | Tampering | a new in-window SLOAD breaks the VRF freeze (ADV-04 / invariant b) | mitigate | The tuple addition returns the already-computed `bountyMultiplier` from the `:242-254` band — it reads NO rng-window state and does NOT touch `_applyDailyRng`/`totalFlipReversals` (`git diff` shows no hunk there). The router (330-04) consumes the RETURN and recomputes nothing. Empirical freeze fuzz = TST-01 (Phase 332). |
| T-330-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. No `## Package Legitimacy Audit` required. |
</threat_model>

<verification>
- `forge build` compiles the touched file with 0 errors.
- `advanceGame()` returns `(uint8 mult, bool rewardable)` on every path; `rewardable` is a distinct bool (not `mult > 0`).
- The 3 caller-reward `creditFlip(caller, …)` sites are gone; the `:876` `creditFlip(ContractAddresses.SDGNRS, …)` merge-credit survives.
- `_applyDailyRng` + `totalFlipReversals` are byte-unchanged (ADV-04).
- No git commit performed (batched-diff discipline).
</verification>

<success_criteria>
- ROADMAP SC3 satisfied for the PRODUCER edit: the 3 advance-bounty `creditFlip(caller,…)` sites removed (ADV-01), `advanceGame` returns `(uint8 mult, bool rewardable)` from the single-sourced multiplier home with no money-path recompute (ADV-02), standalone `advanceGame()` stays a fully-functional unrewarded liveness fallback (ADV-03), and the mid-day partial-drain leg is covered by `rewardable` (ADV-05) — the keystone PRODUCER edit the wrapper decode (330-02) and the AfKing router (330-04) consume.
</success_criteria>

<output>
Create `.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-01-SUMMARY.md` when done. Record the re-grepped current line of each of the 4 `creditFlip` sites + which 3 were deleted, the final `advanceGame` signature, the `(mult, rewardable)` assignment per control-flow class, the `ADVANCE_BOUNTY_ETH` grep-decision (removed/kept), and confirmation `_applyDailyRng`/`totalFlipReversals` are byte-untouched. Do NOT git commit.
</output>
