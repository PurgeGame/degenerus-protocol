---
phase: 453-impl-the-sole-approval-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/modules/DegenerusGameDegeneretteModule.sol
autonomous: false
requirements: [SCORE-01, SCORE-02, SCORE-03, RIG-01, RIG-02, RIG-03, IMPL-01]

must_haves:
  truths:
    - "_score implements Variant-2: per quadrant symbol +1 (hero symbol +2), color +1 only when that quadrant's symbol matched; S in {0..9}; floor S>=2."
    - "_rigWwxrpResult forces only score-bearing cells (unmatched non-hero symbols + unmatched colors on symbol-matched quads), m>=7 cap kept, uniform pick, two-pass force; +2 unlock allowed; rig can NEVER produce S=9."
    - "Honest payout + ETH-bonus families are split per (N, heroIsGold) (8 base tables + 8 factor sets); WWXRP _RIG_ family stays 5 by-N tables."
    - "_getBasePayoutBps / _wwxrpFactor / _fullTicketPayout thread heroIsGold; honest lane indexed by (N, heroIsGold), rigged lane by N only; all 4 call sites compute heroIsGold from the player ticket + heroQuadrant."
    - "S9 pins (QUICK_PLAY_PAYOUT_N{0..4}_S9) and the WWXRP_ROI_* / WWXRP_FLOOR_BPS curve are byte-identical to HEAD."
    - "forge build is clean and the module fits under EIP-170."
    - "The diff is contracts/modules/DegenerusGameDegeneretteModule.sol ONLY, presented for USER hand-review and NOT committed without CONTRACTS_COMMIT_APPROVED=1."
  artifacts:
    - path: "contracts/modules/DegenerusGameDegeneretteModule.sol"
      provides: "Variant-2 _score, R2 _rigWwxrpResult, heroIsGold-threaded payout dispatch, regenerated Option-B constant family"
      contains: "heroIsGold"
  key_links:
    - from: "_fullTicketPayout"
      to: "_getBasePayoutBps / _wwxrpFactor"
      via: "heroIsGold forwarded"
      pattern: "heroIsGold"
    - from: "the 4 call sites of _fullTicketPayout"
      to: "_fullTicketPayout"
      via: "heroIsGold = ((playerTicket >> (heroQuadrant*8+3)) & 7) == 7"
      pattern: "heroQuadrant \\* 8 \\+ 3"
    - from: "generator FINAL PASTE-READY CONSTANTS"
      to: "contract constant blocks ~L283-360"
      via: "verbatim paste, byte-reproduce diff"
      pattern: "QUICK_PLAY_PAYOUTS_N1_HEROGOLD_PACKED"
---

<objective>
Implement v73.0 Variant-2 (color-gated-by-symbol scoring) + the DEC-01 R2 score-bearing WWXRP rig
+ the DEC-02 Option-B per-(N, heroIsGold) honest payout family in
`contracts/modules/DegenerusGameDegeneretteModule.sol` as ONE batched `.sol` diff. This is the
**sole contract-commit approval gate** of the milestone: apply every edit, `forge build`, verify
EIP-170 + the held-fixed invariant lines, write the SUMMARY, then STOP and present the diff for
USER hand-review. Do NOT `git commit` the `.sol`.

Purpose: ports the proven foil-match Variant-2 rule into the core Degenerette betting engine —
main slot moves from ~1-in-3 / 32% to ~1-in-5 at ~2x multipliers at IDENTICAL EV, with the full
WWXRP rig family recalibrated and P(S=9) + the WWXRP RTP curve held byte-identical to HEAD.

Output: the modified single contract file (uncommitted), validated by `forge build` + grep/diff
acceptance checks, plus a SUMMARY. The byte-reproduce + rig-parity TESTS are Phase 454 — 453's
verification is `forge build` + structural grep + diff-of-invariant-lines, NOT the full suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/453-impl-the-sole-approval-gate/453-CONTEXT.md
@.planning/phases/452-gen-generator-first-no-contract-edit/452-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/notes/degenerette-recalibration/derive_5_tables.py
@contracts/modules/DegenerusGameDegeneretteModule.sol

<interfaces>
<!-- Current (HEAD) signatures the executor changes. Extracted from the contract — no exploration needed. -->

_score(uint32 playerTicket, uint32 resultTicket, uint8 heroQuadrant) private pure returns (uint8 s)   // ~L1053
_rigWwxrpResult(uint32 playerTicket, uint32 resultTicket, uint8 heroQuadrant, uint256 rigSeed) private pure returns (uint32 rigged)   // ~L1299
_wwxrpFactor(uint8 N, uint8 bucket, bool isWwxrp) private pure returns (uint256 factor)   // ~L1101
_getBasePayoutBps(uint8 N, uint8 s, bool isWwxrp) private pure returns (uint256)   // ~L1175
_fullTicketPayout(uint8 N, uint8 s, uint8 currency, uint128 betAmount, uint256 roiBps, uint256 wwxrpHighRoi) private pure returns (uint256 payout)   // ~L1132

<!-- The 4 _fullTicketPayout call sites and the vars in scope at each: -->
<!-- ~L753  regular bet loop:        playerTicket (=customTicket, L704), heroQuadrant (L688), goldCount (L707) -->
<!-- ~L1489 resolveWwxrpSpinFromBox: playerTicket (L1473), heroQuadrant (L1472)  [N passed inline _countGoldQuadrants(playerTicket)] -->
<!-- ~L1551 resolveFlipSpinsFromBox: playerTicket (L1544, loop-local), heroQuadrant inline uint8(ss & MASK_2) -> capture to a local -->
<!-- ~L1604 resolveEthSpinFromBox:   playerTicket (L1597), heroQuadrant inline uint8(seed & MASK_2) -> capture to a local -->

<!-- heroIsGold derivation (CONTEXT D-5): the hero quadrant's color is bits 5-3 of its byte; gold == 7. -->
<!--   heroIsGold = ((playerTicket >> (heroQuadrant * 8 + 3)) & 7) == 7 -->

<!-- 453 IMPL DISPATCH SHAPE (printed verbatim by the generator; the ONLY dispatch change): -->
<!--   honest lane (!isWwxrp): N0 -> _N0 table; N4 -> _N4 table; N in {1,2,3} -> heroIsGold ? _HEROGOLD : _HEROCOMMON -->
<!--   WWXRP lane (isWwxrp):   _RIG_N{n} table by N only (heroIsGold IGNORED) -->
<!--   S=9 pin: by N only (placement-independent) -->
<!--   Same selector applies to _wwxrpFactor (WWXRP_FACTORS_*). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate + paste the Option-B constant family verbatim from the generator</name>
  <read_first>
    - .planning/notes/degenerette-recalibration/derive_5_tables.py (the byte-source; run it)
    - contracts/modules/DegenerusGameDegeneretteModule.sol constant blocks ~L283-360 (current HEAD blocks being replaced)
    - .planning/phases/453-CONTEXT.md "Constant blocks (~L300-360)" + 452-CONTEXT.md DEC-02 Option B
  </read_first>
  <files>contracts/modules/DegenerusGameDegeneretteModule.sol</files>
  <action>
    Run `python3 .planning/notes/degenerette-recalibration/derive_5_tables.py` and capture its
    stdout. From the two FINAL PASTE-READY CONSTANTS blocks, paste VERBATIM into the contract
    constant section (~L283-360):
    (a) HONEST base — REPLACE the 5 `QUICK_PLAY_PAYOUTS_N{0..4}_PACKED` with the 8 Option-B
    constants the generator emits: `QUICK_PLAY_PAYOUTS_N0_PACKED`,
    `QUICK_PLAY_PAYOUTS_N{1,2,3}_HEROGOLD_PACKED`, `QUICK_PLAY_PAYOUTS_N{1,2,3}_HEROCOMMON_PACKED`,
    `QUICK_PLAY_PAYOUTS_N4_PACKED`.
    (b) HONEST S8 — REPLACE the 5 `QUICK_PLAY_PAYOUT_N{0..4}_S8` with the matching 8 per-(N,heroGold)
    `_S8` constants (N0, N{1,2,3}_HEROGOLD/_HEROCOMMON, N4).
    (c) HONEST factors — REPLACE the 5 `WWXRP_FACTORS_N{0..4}_PACKED` with the 8 per-(N,heroGold)
    honest factor constants.
    (d) RIGGED family — REPLACE the 5 `QUICK_PLAY_PAYOUTS_RIG_N{0..4}_PACKED`, the 5
    `QUICK_PLAY_PAYOUT_RIG_N{0..4}_S8`, and the 5 `WWXRP_FACTORS_RIG_N{0..4}_PACKED` with the
    recalibrated values from the WWXRP RIG FAMILY block (still 5 by-N tables — NOT split).
    DO NOT TOUCH: the 5 `QUICK_PLAY_PAYOUT_N{0..4}_S9` pins, `WWXRP_BONUS_FACTOR_SCALE`,
    `WWXRP_RIG_SALT`, and anything in the `WWXRP_ROI_*` / `WWXRP_FLOOR_BPS` curve. Constants are
    NEVER hand-edited — paste exactly the generator's hex. Leave the dispatch functions for Task 4
    (this task only swaps the constant declarations; the contract will not compile until Task 4
    updates the dispatchers that reference the renamed constants — that is expected and fine).
  </action>
  <acceptance_criteria>
    - Re-run the generator and diff: `python3 .planning/notes/degenerette-recalibration/derive_5_tables.py | grep "private constant"` — every honest base/S8/factor + rigged base/S8/factor line that appears in stdout appears byte-identically in the contract (`grep -F` each constant value).
    - `grep -c "QUICK_PLAY_PAYOUTS_N1_HEROGOLD_PACKED\|QUICK_PLAY_PAYOUTS_N1_HEROCOMMON_PACKED" contracts/modules/DegenerusGameDegeneretteModule.sol` == 2 (the HEROGOLD/HEROCOMMON split exists).
    - `git diff contracts/modules/DegenerusGameDegeneretteModule.sol` shows the 5 `QUICK_PLAY_PAYOUT_N{0..4}_S9` lines are UNCHANGED (not in the diff hunks).
    - `git diff` shows NO change to any `WWXRP_ROI_` / `WWXRP_FLOOR_BPS` line.
  </acceptance_criteria>
  <done>The 8 honest base + 8 honest S8 + 8 honest factor constants and the 5 recalibrated rigged base/S8/factor constants are pasted verbatim from the generator; S9 pins + WWXRP_ROI_* untouched (confirmed by git diff).</done>
  <verify>
    <automated>diff <(python3 .planning/notes/degenerette-recalibration/derive_5_tables.py | grep -oE "0x[0-9a-f]{64}" | sort -u) <(grep -oE "0x[0-9a-f]{64}" contracts/modules/DegenerusGameDegeneretteModule.sol | sort -u) | grep "^<" ; test $? -ne 0 || echo "MISSING_CONSTANT"</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Rewrite _score to Variant-2 (color-gated-by-symbol)</name>
  <read_first>
    - contracts/modules/DegenerusGameDegeneretteModule.sol `_score` ~L1045-1080 (current S = A + 2H impl + doc-comment)
    - .planning/phases/453-CONTEXT.md edit #1; 452-CONTEXT.md Variant-2 rule + DEC-03 floor S>=2
    - derive_5_tables.py `_ordinary_quadrant_dist` / `_hero_quadrant_dist` / `p_score_distribution` (the canonical scoring model the contract must match)
  </read_first>
  <files>contracts/modules/DegenerusGameDegeneretteModule.sol</files>
  <action>
    Rewrite `_score(uint32 playerTicket, uint32 resultTicket, uint8 heroQuadrant)` to Variant-2.
    Per quadrant q (color = bits 5-3, symbol = bits 2-0): compute `symMatch` and `colorMatch`.
    A symbol match scores +1 for ordinary quadrants and +2 for the hero quadrant
    (`q == heroQuadrant`). The quadrant's COLOR scores +1 ONLY IF that quadrant's symbol ALSO
    matched (gate the color increment on `symMatch`). Drop the HEAD independent color +1. Net:
    ordinary quad contributes {0, +1 (sym only), +2 (sym+color)}; hero quad contributes
    {0, +2 (hero sym only), +3 (hero sym+color)}. Max S = 9 (hero 3 + three ordinary 2). Keep the
    `unchecked` blocks and the for-loop shape. Do NOT add a pay-floor branch here — floor S>=2 is
    enforced by the payout SHAPE (S=0,1 pay 0 in the constants), per DEC-03; `_score` returns the
    raw 0..9 score. Refresh the doc-comment to describe Variant-2 (color gated behind symbol; hero
    symbol +2; S=9 = all-8-axes; floor S>=2 lives in the payout table).
  </action>
  <acceptance_criteria>
    - `_score` no longer increments color independently of symbol: `grep -n "colorMatch" contracts/modules/DegenerusGameDegeneretteModule.sol` shows the color score gated by symMatch inside `_score` (the color `++s` is reachable only when symMatch is true).
    - The hero `+= 2` on symbol match is preserved; ordinary `+= 1`.
    - The doc-comment above `_score` no longer says "S = A + 2*H" / "color stays an ordinary axis" and instead describes color-gated-by-symbol.
    - `forge build` may still fail here only if Task 1's renamed constants are referenced by un-updated dispatchers — that is acceptable until Task 4. `_score` itself must be syntactically valid.
  </acceptance_criteria>
  <done>_score is Variant-2: hero symbol +2 / ordinary symbol +1; color +1 only when the same quadrant's symbol matched; max 9; doc-comment refreshed; floor S>=2 left to the payout table.</done>
  <verify>
    <automated>grep -A30 "function _score(" contracts/modules/DegenerusGameDegeneretteModule.sol | grep -q "symMatch" && grep -A30 "function _score(" contracts/modules/DegenerusGameDegeneretteModule.sol | grep -qv "S = A + 2" && echo SCORE_OK</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Adapt _rigWwxrpResult to the DEC-01 R2 score-bearing rig (+2 unlock, never S=9)</name>
  <read_first>
    - contracts/modules/DegenerusGameDegeneretteModule.sol `_rigWwxrpResult` ~L1287-1358 (current pass-1 `u` build + pass-2 force walk + doc-comment)
    - .planning/phases/453-CONTEXT.md edit #2; 452-CONTEXT.md DEC-01 (R2 + "allow +2, never 9")
    - derive_5_tables.py `p_score_distribution_rigged` (the canonical rigged model: eligible-pool definition, the empty-pool case, the +1/+2 lift split, the m>=7 cap)
  </read_first>
  <files>contracts/modules/DegenerusGameDegeneretteModule.sol</files>
  <action>
    Narrow the rig's eligible-cell pool to SCORE-BEARING cells only, matching the generator's
    `p_score_distribution_rigged` model. In pass 1, change the unmatched-cell count `u`: keep the
    non-hero unmatched-symbol contribution (`q != heroQuadrant && !symMatch` -> eligible), but
    REPLACE the HEAD blanket `if (!colorMatch) ++u;` with a GATED color count — an unmatched color
    is eligible ONLY when that quadrant's symbol ALREADY matched (`symMatch && !colorMatch`),
    including the hero quadrant's color (only the hero SYMBOL is excluded, never its color). EXCLUDE
    no-op colors (unmatched color on a symbol-unmatched quad) and the hero symbol. Keep the m>=7
    early return, the 3/5 gate (`rigSeed % 5 >= 3`), and the uniform pick
    `uint8((rigSeed >> 8) % u)`. In pass 2, walk the SAME fixed quadrant order with the SAME
    eligibility predicates as pass 1 (so the pick index lines up): for an eligible non-hero
    unmatched symbol, force the symbol (bits 2-0); for an eligible unmatched color on a
    symbol-matched quad, force the color (bits 5-3). The +2 unlock (forcing a symbol onto an
    already-color-matched quad lifts S by 2 under Variant-2) happens naturally and is ALLOWED; the
    m>=7 cap guarantees a fired roll has M<=6 -> post-force M<=7 -> S<=8, so the rig can NEVER make
    S=9. Handle the empty-eligible-pool case: if `u == 0` after the gated pass-1 (every unmatched
    cell is excluded), return `rigged` unchanged (no lift) BEFORE the `% u` pick — guard against a
    division-by-zero on the uniform pick. Refresh the doc-comment to describe the R2 pool
    (non-hero symbols + colors on symbol-matched quads), the empty-pool no-op, the +2 unlock, and
    the never-S=9 guarantee.
  </action>
  <acceptance_criteria>
    - The HEAD blanket `if (!colorMatch) ++u;` is gone; the color eligibility is gated on a prior symbol match (`symMatch && !colorMatch`) — `grep -A40 "function _rigWwxrpResult(" ...` shows no unconditional `if (!colorMatch) ++u;`.
    - Pass-2 eligibility predicates match pass-1 exactly (same order, same conditions) — the symbol-force branch is gated `q != heroQuadrant && !symMatch`, the color-force branch is gated on a symbol-matched-but-color-unmatched quad.
    - An explicit `u == 0` (empty-pool) guard returns `rigged` before the `% u` pick (no div-by-zero).
    - The `if (m >= 7) return rigged;` cap and the `rigSeed % 5 >= 3` gate are preserved verbatim.
    - The doc-comment describes the R2 pool, the +2 unlock, and the never-S=9 (m>=7 cap) guarantee.
  </acceptance_criteria>
  <done>_rigWwxrpResult forces only score-bearing cells (non-hero symbols + colors on symbol-matched quads, incl. hero color); m>=7 cap + 3/5 gate + uniform pick + two-pass force preserved; empty-pool no-op guards the pick; +2 unlock allowed; never S=9; doc-comment refreshed.</done>
  <verify>
    <automated>grep -A45 "function _rigWwxrpResult(" contracts/modules/DegenerusGameDegeneretteModule.sol | grep -q "m >= 7" && grep -A45 "function _rigWwxrpResult(" contracts/modules/DegenerusGameDegeneretteModule.sol | grep -qv "if (!colorMatch) ++u;" && echo RIG_OK</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 4: Thread heroIsGold through _getBasePayoutBps, _wwxrpFactor, _fullTicketPayout + 4 call sites</name>
  <read_first>
    - contracts/modules/DegenerusGameDegeneretteModule.sol `_wwxrpFactor` ~L1101, `_fullTicketPayout` ~L1132, `_getBasePayoutBps` ~L1175 (current dispatchers)
    - the 4 call sites: ~L753 (regular bet loop, vars L688/L704/L707), ~L1489 (resolveWwxrpSpinFromBox, L1472/L1473), ~L1551 (resolveFlipSpinsFromBox, L1544 + inline ss&MASK_2), ~L1604 (resolveEthSpinFromBox, L1597 + inline seed&MASK_2)
    - derive_5_tables.py "453 IMPL DISPATCH SHAPE" block (the exact branch layout) + .planning/phases/453-CONTEXT.md edits #3/#4/#5
  </read_first>
  <files>contracts/modules/DegenerusGameDegeneretteModule.sol</files>
  <action>
    Implement the generator's printed dispatch shape (Claude's discretion on exact branch layout /
    helper extraction, provided behavior matches and EIP-170 holds).
    (1) `_getBasePayoutBps(uint8 N, uint8 s, bool isWwxrp, bool heroIsGold)`: S=9 pin by N only
    (unchanged). isWwxrp (rigged) lane: by N only, heroIsGold ignored (RIG family unchanged shape).
    Honest lane (!isWwxrp, s in 0..8): N0 -> `QUICK_PLAY_PAYOUTS_N0_PACKED`/`_N0_S8`;
    N4 -> `_N4_PACKED`/`_N4_S8`; N in {1,2,3} -> `heroIsGold ? _N{n}_HEROGOLD_* : _N{n}_HEROCOMMON_*`.
    (2) `_wwxrpFactor(uint8 N, uint8 bucket, bool isWwxrp, bool heroIsGold)`: same selector — honest
    factors per-(N,heroGold) (N0/N4 collapse), rigged by N only.
    (3) `_fullTicketPayout(...)`: add a `bool heroIsGold` param; forward it to both
    `_getBasePayoutBps` and `_wwxrpFactor`.
    (4) At each of the 4 call sites compute
    `bool heroIsGold = ((playerTicket >> (heroQuadrant * 8 + 3)) & 7) == 7;` (inline or via a tiny
    private helper — discretion) and pass it as the new arg. For the two box paths that pass
    heroQuadrant inline (`uint8(ss & MASK_2)` at ~L1551 / `uint8(seed & MASK_2)` at ~L1604), hoist
    the heroQuadrant into a named local first so both `_score` and the heroIsGold derivation use the
    same value. The regular-bet site (~L753) and resolveWwxrpSpinFromBox (~L1489) already have a
    named `heroQuadrant` in scope. Refresh the doc-comments on the three functions to describe the
    (N, heroIsGold) honest split + the heroIsGold-ignored rigged lane.
  </action>
  <acceptance_criteria>
    - `forge build` exits 0 (this task completes the compile — the renamed constants from Task 1 now resolve).
    - `grep -c "heroIsGold" contracts/modules/DegenerusGameDegeneretteModule.sol` >= 8 (3 function signatures forward it + 4 call-site derivations + at least one dispatch use).
    - All 4 call sites derive heroIsGold from the hero quadrant's color bits: `grep -c "heroQuadrant \* 8 + 3" contracts/modules/DegenerusGameDegeneretteModule.sol` >= 4 (or, if a helper is used, the helper is called 4x and contains that expression once).
    - `_getBasePayoutBps` and `_wwxrpFactor` signatures both end with `, bool heroIsGold)`; the rigged branches ignore heroIsGold (no HEROGOLD/HEROCOMMON constant referenced under `if (isWwxrp)`).
    - Honest dispatch references `QUICK_PLAY_PAYOUTS_N{1,2,3}_HEROGOLD_PACKED` and `_HEROCOMMON_PACKED` (grep finds both infixes used in `_getBasePayoutBps`).
  </acceptance_criteria>
  <done>heroIsGold is threaded through _getBasePayoutBps/_wwxrpFactor/_fullTicketPayout and computed at all 4 call sites; honest lane indexed by (N, heroIsGold) per the printed dispatch shape, rigged lane by N only; forge build is clean.</done>
  <verify>
    <automated>forge build 2>&1 | tail -3 && test $(grep -c "heroIsGold" contracts/modules/DegenerusGameDegeneretteModule.sol) -ge 8 && echo THREAD_OK</automated>
  </verify>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 5: forge build + EIP-170 verify, present the single-file diff for USER approval — DO NOT COMMIT</name>
  <read_first>
    - contracts/modules/DegenerusGameDegeneretteModule.sol (the full applied diff)
    - .planning/phases/453-CONTEXT.md <verification> success criteria + the HARD RULE (apply, build, verify, STOP, present — do NOT commit .sol)
    - the MEMORY rules: only contract commits need approval; commit-guard CONTRACTS_COMMIT_APPROVED=1 + hook move-aside
  </read_first>
  <files>contracts/modules/DegenerusGameDegeneretteModule.sol</files>
  <action>
    Run `forge build` (must exit 0) and `forge build --sizes` (DegenerusGameDegeneretteModule must
    be under the 24,576-byte EIP-170 limit — report headroom; on overflow STOP and flag for
    optimization, e.g. helper extraction / branch consolidation, NEVER drop tables). Run the
    structural greps and the S9-pin / WWXRP_ROI_* unchanged `git diff` checks. Re-run the generator
    and confirm every emitted `private constant` hex matches the contract (byte-reproduce smoke).
    Write the SUMMARY recording all results + the EIP-170 headroom + an explicit UNCOMMITTED note.
    Then PRESENT the single-file diff to the USER for hand-review and STOP — do NOT `git commit` the
    `.sol`. The commit happens only after explicit USER approval (`CONTRACTS_COMMIT_APPROVED=1` +
    hook move-aside). Planning docs (PLAN/SUMMARY) may be committed; the `.sol` may NOT.
  </action>
  <what-built>
    The complete Variant-2 + R2-rig + Option-B contract diff is applied to the single file
    `contracts/modules/DegenerusGameDegeneretteModule.sol`. Before this checkpoint, the executor
    ran `forge build --sizes` (clean, EIP-170 confirmed), the structural greps, and the
    invariant-line diffs, and wrote the SUMMARY. Nothing is committed.
  </what-built>
  <how-to-verify>
    The executor MUST run and record (in the SUMMARY) all of:
    1. `forge build` exits 0 (no errors).
    2. `forge build --sizes 2>&1 | grep DegenerusGameDegeneretteModule` — capture the deployed
       runtime size; it MUST be under the EIP-170 24,576-byte limit (report the headroom). If it
       OVERFLOWS, do NOT silently drop tables — STOP and flag for optimization (e.g. helper
       extraction / branch consolidation) in the SUMMARY and escalate.
    3. `git diff --stat` shows ONLY `contracts/modules/DegenerusGameDegeneretteModule.sol` changed.
    4. `git diff contracts/modules/DegenerusGameDegeneretteModule.sol` — confirm the 5
       `QUICK_PLAY_PAYOUT_N{0..4}_S9` pin lines and every `WWXRP_ROI_*` / `WWXRP_FLOOR_BPS` line
       are NOT in the diff (byte-identical to HEAD).
    5. Re-run `python3 .planning/notes/degenerette-recalibration/derive_5_tables.py` and confirm
       every emitted `private constant` hex matches the contract (byte-reproduce smoke check; the
       full TST gate is Phase 454).
    Then present the diff to the USER for hand-review. The executor MUST NOT `git commit` the
    `.sol` — committing requires explicit USER approval (`CONTRACTS_COMMIT_APPROVED=1` + hook
    move-aside). Planning docs (PLAN/SUMMARY) may be committed; the `.sol` may NOT.
  </how-to-verify>
  <verify>
    <automated>forge build 2>&1 | tail -2 && forge build --sizes 2>&1 | grep DegenerusGameDegeneretteModule && git diff --stat 2>&1 | grep -c "DegenerusGameDegeneretteModule.sol"</automated>
    <human-check>USER hand-reviews the presented single-file diff and replies "approved" (or requests changes) before ANY .sol commit; the executor never commits contracts/*.sol itself.</human-check>
  </verify>
  <done>forge build clean + EIP-170 confirmed (headroom recorded); single-file diff with the 5 S9 pins + all WWXRP_ROI_* lines byte-identical to HEAD; byte-reproduce smoke passes; SUMMARY written; the diff is PRESENTED and the .sol is UNCOMMITTED pending USER approval.</done>
  <resume-signal>USER reviews the diff and replies "approved" (then the orchestrator commits the .sol under CONTRACTS_COMMIT_APPROVED=1) or requests changes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player ticket -> resolution engine | The player supplies the custom ticket + heroQuadrant (validated upstream); the score + payout are derived from it. heroIsGold is read-only from the ticket's hero-quadrant color bits. |
| frozen RNG seed -> _rigWwxrpResult | The WWXRP rig draws from a frozen, reel-independent hash of the spin seed; no live state enters. (Full RNG-freeze re-attestation is Phase 455 AUD-01.) |
| constant blocks -> payout math | The EV-equality + solvency of every payout depends on the pasted constants matching the generator's solved tables. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-453-01 | Elevation | _rigWwxrpResult manufacturing S=9 (unbacked jackpot) | mitigate | Keep the `if (m >= 7) return rigged;` cap (Task 3) — a fired roll has M<=6 -> post-force M<=7 -> S<=8; the generator's `rig[9]==honest[9]` assert is the proof. RIG-02. |
| T-453-02 | Information disclosure | Player-selectable hero-placement EV edge (hero-common EV-positive on honest lane) | mitigate | DEC-02 Option B: honest family split per (N, heroIsGold); each sub-case solved to EV<=100; the 2.99 centi-x selectable edge is closed (generator EVEQ-01 confirms residual <0.01 centi-x). Tasks 1+4. |
| T-453-03 | Tampering | Hand-edited / drifted payout constants -> unbacked payout or RTP drift | mitigate | Constants pasted VERBATIM from the generator (never hand-typed); Task 1 + Task 5 byte-reproduce smoke check; full TST gate Phase 454. |
| T-453-04 | Tampering | S9 pins / WWXRP_ROI_* curve silently changed -> jackpot / RTP drift | mitigate | Task 1 + Task 5 `git diff` assert those lines are byte-identical to HEAD (INV-02/INV-04 held fixed). SCORE-03. |
| T-453-05 | Denial of service | EIP-170 overflow from 5->8 tables + heroIsGold threading -> module brick / undeployable | mitigate | Task 5 `forge build --sizes` is a blocking criterion; on overflow, flag for optimization (helper extraction) — never drop tables. |
| T-453-06 | Denial of service | `% u` division-by-zero in the rig when the eligible pool is empty | mitigate | Task 3 adds an explicit `u == 0` empty-pool no-op guard before the uniform pick. |
| T-453-07 | Tampering | Unapproved `.sol` commit | mitigate | Task 5 is a blocking-human gate; executor presents the diff and does NOT commit; commit only under CONTRACTS_COMMIT_APPROVED=1 + hook move-aside. IMPL-01. |
| T-453-SC | Tampering | Supply-chain (npm/pip/cargo installs) | accept | No package installs in this phase — single-file `.sol` edit only; the package legitimacy gate is N/A. |
| T-453-08 | Spoofing | Deep solvency / RNG-freeze / liveness re-audit | transfer | Deferred to Phase 455 AUD-01 (the full adversarial re-audit on the new scoring). |
</threat_model>

<verification>
Phase-level checks (run by the executor; recorded in the SUMMARY):
1. `forge build` exits 0; `forge build --sizes | grep DegenerusGameDegeneretteModule` is under 24,576 bytes (report headroom).
2. `git diff --stat` shows ONLY `contracts/modules/DegenerusGameDegeneretteModule.sol`.
3. `git diff` confirms the 5 S9 pins + all `WWXRP_ROI_*` / `WWXRP_FLOOR_BPS` lines are byte-identical to HEAD.
4. Byte-reproduce smoke: every `private constant` hex the generator emits matches the contract.
5. `grep` confirms: `_score` is color-gated (no "S = A + 2"); `_rigWwxrpResult` has the m>=7 cap and no blanket `if (!colorMatch) ++u;`; heroIsGold threaded through the 3 dispatchers + 4 call sites.
6. The `.sol` is NOT committed (no `git commit` of `contracts/*.sol` before USER approval).

Deferred to Phase 454 (named for traceability, NOT run here): the byte-reproduce stat gate
(`test/stat/DegenerettePerNEvExactness.test.js`), the rig-parity behavioral test (run the actual
contract rig over many seeds vs `p_score_distribution_rigged`), the bonus-EV oracle, and full-suite
parity (forge + Hardhat).
</verification>

<success_criteria>
- `_score` is Variant-2 (color gated by symbol; hero symbol +2; max 9; floor S>=2 via the payout table). [SCORE-01, SCORE-02]
- S=9 stays the all-8-axes event; S9 pins + WWXRP S=9 whale-pass bracket byte-identical to HEAD. [SCORE-03]
- `_rigWwxrpResult` is R2: score-bearing pool (non-hero symbols + colors on symbol-matched quads), m>=7 cap, uniform pick, two-pass force, empty-pool no-op, +2 unlock allowed, never S=9. [RIG-01, RIG-02, RIG-03]
- The Option-B constant family is pasted verbatim (8 honest base + 8 S8 + 8 factor + 5 recalibrated rigged); S9 pins + WWXRP_ROI_* untouched. [SCORE-03]
- `_getBasePayoutBps` / `_wwxrpFactor` / `_fullTicketPayout` thread heroIsGold; honest lane by (N, heroIsGold), rigged by N only.
- `forge build` clean; module under EIP-170 (or overflow flagged for optimization, tables NOT dropped).
- The diff is single-file, presented for USER hand-review, NOT committed without CONTRACTS_COMMIT_APPROVED=1. [IMPL-01]
</success_criteria>

<output>
Create `.planning/phases/453-impl-the-sole-approval-gate/453-01-SUMMARY.md` when done, recording:
the applied edits, the `forge build --sizes` headroom for DegenerusGameDegeneretteModule, the
byte-reproduce smoke result, the S9-pin/WWXRP_ROI_* unchanged confirmation, and an explicit note
that the `.sol` is UNCOMMITTED pending USER approval (the sole approval gate).
</output>
