---
phase: 15
plan: 3
title: "Tuning, Metrics Verification, and Level Advancement"
status: draft
estimated_effort: medium
---

# Plan 15-03: Tuning, Metrics Verification, and Level Advancement

## Goal

Tune handler bound ranges and fuzzer configuration to achieve >60% non-reverting call rate, verify game advances past level 0 in at least some runs, and add call counters and a summary invariant for coverage analysis.

## Tasks

<tasks>
- Add call counters to GameHandler and WhaleHandler for coverage analysis
- Add `invariant_callSummary()` to EthSolvency.inv.t.sol that logs call distribution and game state (level, rngLocked, gameOver)
- Tune bound ranges and handler logic based on revert rate analysis from show_metrics output
- Verify: >60% non-reverting call rate confirmed via show_metrics, game advances past level 0 in at least some runs
- Run final `make invariant-test` with all three success criteria met
</tasks>

## Verification

- [ ] `show_metrics = true` confirms >60% non-reverting call rate
- [ ] Game level > 0 observed in at least some fuzzer runs (logged by invariant_callSummary)
- [ ] `make invariant-test` passes with all invariants holding
- [ ] Call distribution is reasonable across all three handlers

## Must Haves

- Call counters in all handler functions
- Summary logging in invariant test showing coverage metrics
- Non-reverting call rate > 60%
- Evidence of game advancing past level 0
- All ETH solvency and ghost accounting invariants still passing
