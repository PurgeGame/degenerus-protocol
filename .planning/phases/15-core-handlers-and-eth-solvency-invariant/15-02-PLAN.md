---
phase: 15
plan: 2
title: "WhaleHandler and Ghost Variable Reconciliation"
status: draft
estimated_effort: medium
---

# Plan 15-02: WhaleHandler and Ghost Variable Reconciliation

## Goal

Create WhaleHandler with whale bundle, lazy pass, and deity pass functions. Add ghost accounting invariant that reconciles total deposited vs total claimed across all handlers.

## Tasks

<tasks>
- Create `test/fuzz/handlers/WhaleHandler.sol` with purchaseWhaleBundle(), purchaseLazyPass(), purchaseDeityPass() and ghost variables (ghost_whaleBundleDeposited, ghost_lazyPassDeposited, ghost_deityPassDeposited)
- Update EthSolvency.inv.t.sol to register WhaleHandler as third targetContract and add ghost accounting invariant: totalDeposited >= totalClaimed
- Run `make invariant-test` and verify all invariants hold with zero violations
</tasks>

## Verification

- [ ] `make invariant-test` passes with zero assertion failures
- [ ] WhaleHandler ghost variables are non-zero in some runs
- [ ] Ghost accounting reconciliation invariant passes

## Must Haves

- WhaleHandler with bounded inputs (qty 1-5 for bundles, symbolId 0-31 for deity)
- Multi-actor support (5 actors, each funded with 200 ETH for expensive whale ops)
- Ghost variable tracking for all whale ETH deposits
- Graceful early return when cost exceeds actor balance
- All three handlers registered as targetContracts in invariant test
