---
phase: 244-per-commit-adversarial-audit-evt-rng-qst-gox
phase_number: 244
plan: 244-01
plan_number: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v31-244-EVT.md
autonomous: true
requirements: [EVT-01, EVT-02, EVT-03, EVT-04]
requirements_addressed: [EVT-01, EVT-02, EVT-03, EVT-04]
baseline: 7ab515fe
baseline_sha: 7ab515fe
head: cc68bfc7
head_sha: cc68bfc7
head_anchor: cc68bfc7
in_scope_commits:
  - sha: ced654df
    summary: "fix(jackpot): emit accurate scaled ticketCount on all JackpotTicketWin paths (DegenerusGameJackpotModule.sol +33/-6)"
    drives_reqs: [EVT-01, EVT-02, EVT-03, EVT-04]
  - sha: cc68bfc7
    summary: "feat(baf): gate BAF jackpot on daily flip win — BAF-coupling ADDENDUM (DegenerusJackpots.sol +19, IDegenerusJackpots.sol +6, DegenerusGameAdvanceModule.sol +22/-10)"
    drives_reqs: [EVT-02, EVT-03]
    routes_per: "CONTEXT.md D-03 (BAF/event-flow territory routes through EVT plan, NOT a 5th plan)"
in_scope_243_rows:
  changelog:    [D-243-C001, D-243-C002, D-243-C003, D-243-C004, D-243-C005, D-243-C006, D-243-C035, D-243-C036, D-243-C037, D-243-C038, D-243-C039, D-243-C040, D-243-C041, D-243-C042]
  classification: [D-243-F001, D-243-F002, D-243-F003, D-243-F004, D-243-F005, D-243-F025, D-243-F026]
  call_sites:   [D-243-X001, D-243-X002, D-243-X003, D-243-X004, D-243-X005, D-243-X006, D-243-X007, D-243-X008, D-243-X009, D-243-X010, D-243-X011, D-243-X053, D-243-X054, D-243-X060]
  consumer_index: [D-243-I004, D-243-I005, D-243-I006, D-243-I007]
  finding_candidates_137: ["bullet 6 (BAF expected-value re-verification under bit-0 coupling)", "bullet 7 (markBafSkipped consumer gating on cursor > lastBafResolvedDay)"]
must_haves:
  truths:
    - "audit/v31-244-EVT.md exists with frontmatter `Audit baseline: 7ab515fe`, `Audit head: cc68bfc7`, `Owning commits: ced654df + cc68bfc7 (BAF-coupling addendum per D-03)`."
    - "Section §EVT-01 verdict table contains a row per JackpotTicketWin emit site (minimum 5 rows: _runEarlyBirdLootboxJackpot D-243-C001, _distributeTicketsToBucket D-243-C002, runBafJackpot stub-removals D-243-C003, _jackpotTicketRoll D-243-C005, plus argument-trace verification rows). Every row carries a TICKET_SCALE-scaled non-zero verdict OR an explicit finding-candidate with severity."
    - "Section §EVT-02 verdict table contains a row per JackpotWhalePassWin emit site (D-243-C004 in _awardJackpotTickets at L2074-2117) PLUS a sub-section verifying the cc68bfc7 markBafSkipped consumer gating per §1.7 bullet 7 (every consumer of bafBrackets[lvl] / winningBafCredit gates on `cursor > lastBafResolvedDay`)."
    - "Section §EVT-03 verdict table proves uniform TICKET_SCALE scaling across BAF + trait-matched paths (UI consumer divisibility invariant: every emit value % TICKET_SCALE == 0) AND a cc68bfc7 BAF-coupling sub-section verifies the bit-0 of rngWord coupling per §1.7 bullet 6 (BAF fires ~50% under coupling vs always before — economic effect tracked)."
    - "Section §EVT-04 records the literal NatSpec text from contracts/modules/DegenerusGameJackpotModule.sol:86-93 at HEAD cc68bfc7 (event JackpotTicketWin declaration) and verifies per-claim accuracy against actual emit-site behavior (scaling described correctly + fractional-remainder resolution path described correctly)."
    - "Every verdict row carries columns: `Verdict Row ID | REQ-ID | Source 243 Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA` per CONTEXT.md D-06."
    - "Verdict bucket per row drawn from {SAFE, INFO, LOW, MEDIUM, HIGH, CRITICAL} per CONTEXT.md D-08 — zero TBD/blank verdicts."
    - "Verdict Row IDs follow the `EVT-NN-V##` per-REQ-monotonic scheme per CONTEXT.md Specifics (e.g., EVT-01-V01, EVT-02-V03)."
    - "REQ-summary table at top or bottom of file aggregates per-REQ closure: every REQ in {EVT-01, EVT-02, EVT-03, EVT-04} appears at least once as a Verdict Row ID prefix; per-REQ severity floor stated."
    - "All 8 D-243 row subsets from D-243-I004 through D-243-I007 are cited at least once in the verdict-table cells — Consumer Index integrity preserved."
    - "Phase 243 §1.7 bullets 6 and 7 each receive a closing verdict per CONTEXT.md D-09 (mapped to EVT-02 + EVT-03 per D-09 mapping); zero candidate left unaddressed."
    - "Reproduction recipe sub-section at end of file lists the exact `git show ced654df -- contracts/modules/DegenerusGameJackpotModule.sol` + `git show cc68bfc7 -- contracts/DegenerusJackpots.sol` + `grep -rn` commands the audit reproduces (POSIX-portable per CONTEXT.md D-04 reproduction-recipe carry)."
    - "Zero F-31-NN finding-IDs emitted (CONTEXT.md D-21 — Phase 246 owns ID assignment); finding candidates use prose blocks with `SEVERITY: <bucket>` annotation only."
    - "Zero contracts/ or test/ writes (CONTEXT.md D-18 + feedback_no_contract_commits.md); `git status --porcelain contracts/ test/` returns empty."
    - "Zero edits to audit/v31-243-DELTA-SURFACE.md (CONTEXT.md D-20 — record any new in-scope symbol gap as a deferral in this plan's SUMMARY only)."
  artifacts:
    - path: "audit/v31-244-EVT.md"
      provides: "EVT bucket (REQ EVT-01..EVT-04) per-REQ verdict tables + cc68bfc7 BAF-coupling sub-sections + finding-candidate prose blocks. Working file consumed by 244-04 consolidation step."
      contains: "## §EVT-01"
  key_links:
    - from: "audit/v31-243-DELTA-SURFACE.md §6 D-243-I004..D-243-I007"
      to: "audit/v31-244-EVT.md §EVT-01..§EVT-04 verdict-table cells"
      via: "Source 243 Row(s) column citation per CONTEXT.md D-06"
      pattern: "D-243-(C|F|X)[0-9]{3}"
    - from: "contracts/modules/DegenerusGameJackpotModule.sol at HEAD cc68bfc7 (emit-site bodies + event NatSpec)"
      to: "audit/v31-244-EVT.md verdict Evidence column"
      via: "argument-trace from emit-site back to scaling site (CONTEXT.md D-10 EVT-01 vector b)"
      pattern: "ticketCount.*TICKET_SCALE"
    - from: "contracts/DegenerusJackpots.sol:498-510 (markBafSkipped) + contracts/modules/DegenerusGameAdvanceModule.sol:728-909 (_consolidatePoolsAndRewardJackpots bit-0 coupling)"
      to: "audit/v31-244-EVT.md cc68bfc7 BAF-coupling sub-section (per D-03)"
      via: "consumer-gating grep for bafBrackets[lvl] / winningBafCredit per CONTEXT.md D-09 bullets 6+7"
      pattern: "lastBafResolvedDay|markBafSkipped|rngWord & 1"
---

<objective>
Adversarially audit the `ced654df` commit (JackpotTicketWin event correctness) PLUS the `cc68bfc7` BAF-coupling addendum (per CONTEXT.md D-03). Close all 4 EVT requirements (EVT-01 / EVT-02 / EVT-03 / EVT-04) with per-REQ verdict tables + finding-candidate prose blocks in working file `audit/v31-244-EVT.md`.

Scope source is `audit/v31-243-DELTA-SURFACE.md` §6 Consumer Index rows D-243-I004..D-243-I007 (per CONTEXT.md D-04 single-file precedent + D-05 per-plan working-file pattern). Scope is READ-only per CONTEXT.md D-18.

Per CONTEXT.md D-03: the cc68bfc7 BAF-flip-gate addendum (BAF gated on `rngWord & 1 == 1`, new `markBafSkipped` + `BafSkipped` event + new direct-handle `jackpots` constant) folds into the EVT plan as an EVT-02 / EVT-03 sub-scope expansion — NOT a 5th plan. Per CONTEXT.md D-09 the Phase 243 §1.7 bullets 6 (BAF expected-value re-verification under coupling) + 7 (markBafSkipped consumer gating) close in this plan.

Per CONTEXT.md D-21: zero F-31-NN finding-IDs emitted (Phase 246 owns assignment). This plan emits finding-candidate prose blocks with `SEVERITY: <bucket>` annotations per CONTEXT.md D-08 6-bucket taxonomy.

Per CONTEXT.md D-19: HEAD anchor `cc68bfc7` is locked in frontmatter. If `git rev-parse HEAD` reports a different commit AND `git diff cc68bfc7..HEAD -- contracts/` reports any non-zero change, STOP and record a scope-guard deferral in this plan's SUMMARY (the contract surface has drifted; CONTEXT.md must be re-locked first). The current HEAD `8500af0d` has zero `contracts/` deltas vs `cc68bfc7` (verified at planning time — only `.planning/` + `audit/v31-243-DELTA-SURFACE.md` appendix work landed since), so `cc68bfc7` is the live contract anchor.

Output: `audit/v31-244-EVT.md` is the working file. Per CONTEXT.md D-05, 244-04 consolidates this working file into `audit/v31-244-PER-COMMIT-AUDIT.md` at SUMMARY-commit time.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/244-per-commit-adversarial-audit-evt-rng-qst-gox/244-CONTEXT.md
@.planning/phases/243-delta-extraction-per-commit-classification/243-03-SUMMARY.md
@audit/v31-243-DELTA-SURFACE.md
@.planning/milestones/v29.0-phases/231-earlybird-jackpot-audit/231-01-PLAN.md
@.planning/milestones/v29.0-phases/233-jackpot-baf-entropy-audit/233-CONTEXT.md
</context>

<threat_model>
Per CONTEXT.md D-10 (EVT adversarial vectors — fixed, planner MUST cover at minimum). Actor taxonomy carries from Phase 238 D-07 (player / admin / validator / VRF oracle). This plan is READ-only — zero new contract attack surface introduced; the threat model below enumerates the vectors the AUDIT must close on, NOT new code to ship.

**Trust boundaries (within EVT scope):**

| Boundary | Description |
|---|---|
| `JackpotModule emit site → off-chain UI consumer` | UI parses ticketCount from JackpotTicketWin / JackpotWhalePassWin events; mis-scaled value or zero stub causes wrong winner-display / wrong leaderboard |
| `_consolidatePoolsAndRewardJackpots → DegenerusJackpots.markBafSkipped` (cc68bfc7) | new direct-handle external call; reentrancy-adjacency vs the existing `runBafJackpot` self-call path |
| `bafBrackets[lvl] / winningBafCredit consumers → lastBafResolvedDay` (cc68bfc7) | every consumer must filter on `cursor > lastBafResolvedDay` to ignore pre-skip credit |

**STRIDE-mapped per-REQ vectors (transcribed from CONTEXT.md D-10 + §1.7 bullets 6+7):**

| Threat ID | REQ | Category | Component | Disposition | Mitigation Plan / Adversarial Vector |
|---|---|---|---|---|---|
| T-244-EVT-01a | EVT-01 | Tampering / Information-Disclosure | every JackpotTicketWin emit path (D-243-C001/C002/C003/C005 → call sites D-243-X001/X002/X008..X011) | mitigate-by-audit | Enumerate every emit site via grep `emit JackpotTicketWin\b` in JackpotModule; for each site verify ticketCount argument is TICKET_SCALE-scaled non-zero by walking the argument expression back to its scaling site. Verdict per emit site. |
| T-244-EVT-01b | EVT-01 | Information-Disclosure | argument-trace from emit-site to scaling site | mitigate-by-audit | For every emit site: trace ticketCount arg from emit-site backward through local variable assignments to either a literal `* TICKET_SCALE` multiplication OR a parameter that itself was scaled at the call boundary. NO emit path passes raw unscaled count or zero stub. |
| T-244-EVT-02a | EVT-02 | Information-Disclosure | new JackpotWhalePassWin emit at _awardJackpotTickets D-243-C004 (D-243-X006/X007 callers) | mitigate-by-audit | Verify the new emit covers the previously-silent large-amount odd-index BAF path via per-amount-bucket dispatch trace; verify `amount` and `traitId` args correct; cover the small-lootbox branch (D-243-X006 at L2018) AND the odd-index branch (D-243-X007 at L2049). |
| T-244-EVT-02b | EVT-02 | Tampering | cc68bfc7 markBafSkipped consumer gating (§1.7 bullet 7) | mitigate-by-audit | Grep every consumer of `bafBrackets[lvl]` and `winningBafCredit` in `contracts/`; verify each consumer filters on `cursor > lastBafResolvedDay` so stale pre-skip leaderboard rows cannot be claimed. NatSpec at DegenerusJackpots.sol:498-510 names BurnieCoinflip as one consumer — verify it is the ONLY consumer (or enumerate all). |
| T-244-EVT-03a | EVT-03 | Tampering | uniform TICKET_SCALE across BAF + trait-matched paths (D-243-C001/C002/C005) | mitigate-by-audit | For each emit site, derive the scaled value path from the raw input; confirm uniform TICKET_SCALE factor across BAF + trait-matched paths; UI-consumer divisibility invariant `(every emit value) % TICKET_SCALE == 0` holds. |
| T-244-EVT-03b | EVT-03 | Information-Disclosure / Repudiation | cc68bfc7 BAF-coupling on bit-0 of rngWord (§1.7 bullet 6) | mitigate-by-audit | Re-verify jackpot expected value + fairness under the new `(rngWord & 1) == 1` coupling at `_consolidatePoolsAndRewardJackpots` (DegenerusGameAdvanceModule.sol L728-909). The same low-order bit feeds BurnieCoinflip._resolveDay daily-win/loss outcome → BAF resolution is now CORRELATED with the daily coinflip rather than independent. Track the economic effect (BAF fires ~50% of the time vs always) explicitly. Commit msg flags this as intentional ("BAF gated on daily flip win") — this audit confirms intentionality is preserved + measures economic impact. |
| T-244-EVT-04a | EVT-04 | Repudiation / Information-Disclosure | NatSpec at JackpotTicketWin event declaration L86-93 (D-243-C006/C029) | mitigate-by-audit | Read NatSpec at HEAD cc68bfc7 verbatim; per-claim accuracy check against actual emit-site behavior (TICKET_SCALE scaling described correctly + fractional-remainder resolution path described correctly: carry vs `_rollRemainder`). Verdict per NatSpec claim. |

**Acceptance disposition rule:** Each row above MUST close with one of {SAFE, INFO, LOW, MEDIUM, HIGH, CRITICAL} per CONTEXT.md D-08 6-bucket discrimination bar. INFO for downstream-coupling consequences worth recording (e.g., the `(rngWord & 1) == 1` coupling reduces BAF expected-value); HIGH/CRITICAL only if the audit surfaces a directly exploitable funds-siphon / state-lock / supply-break.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Audit ced654df JackpotTicketWin emit correctness — close EVT-01 + EVT-03 + EVT-04</name>
  <files>audit/v31-244-EVT.md</files>
  <read_first>
    - .planning/phases/244-per-commit-adversarial-audit-evt-rng-qst-gox/244-CONTEXT.md — D-01 (4-plan single-wave parallel), D-04 (single consolidated AUDIT.md output shape), D-05 (per-plan working file pattern), D-06 (verdict-table columns), D-08 (6-bucket severity bar), D-10 (EVT adversarial vectors), D-17 (REFACTOR_ONLY equivalence proof methodology), D-18 (READ-only), D-19 (HEAD anchor `cc68bfc7`), D-20 (no edits to audit/v31-243-DELTA-SURFACE.md), D-21 (no F-31-NN), D-22 (KI envelope re-verify only)
    - audit/v31-243-DELTA-SURFACE.md §1.1 (commit ced654df changelog rows D-243-C001..C006)
    - audit/v31-243-DELTA-SURFACE.md §2.3 classification rows D-243-F001..F005 (MODIFIED_LOGIC verdicts for the 5 emit-site functions)
    - audit/v31-243-DELTA-SURFACE.md §3.1.1..§3.1.5 call-site catalog rows D-243-X001 (`_runEarlyBirdLootboxJackpot` caller), D-243-X002 (`_distributeTicketsToBucket` caller), D-243-X003/X004/X005 (`runBafJackpot` callers — JackpotModule impl), D-243-X006/X007 (`_awardJackpotTickets` callers), D-243-X008/X009/X010 (`_jackpotTicketRoll` callers), D-243-X011 (`advanceGame` direct-level row)
    - audit/v31-243-DELTA-SURFACE.md §6 Consumer Index rows D-243-I004 (EVT-01 → 5 changelog + 4 classification + 8 call-site rows), D-243-I006 (EVT-03 → scaling sub-set), D-243-I007 (EVT-04 → NatSpec only)
    - contracts/modules/DegenerusGameJackpotModule.sol at HEAD cc68bfc7 — specifically the 5 emit-site functions:
      - L86-93 — `event JackpotTicketWin` declaration + NatSpec (D-243-C006 / D-243-C029)
      - L670-719 — `_runEarlyBirdLootboxJackpot` body with TICKET_SCALE-scaled emit (D-243-C001 / D-243-F001)
      - L966-1019 — `_distributeTicketsToBucket` body with `units * TICKET_SCALE` cast emit (D-243-C002 / D-243-F002)
      - L1974-2059 — `runBafJackpot` body with two stub emits removed (D-243-C003 / D-243-F003)
      - L2074-2117 — `_awardJackpotTickets` body with new JackpotWhalePassWin in whale-pass fallback (D-243-C004 / D-243-F004)
      - L2129-2173 — `_jackpotTicketRoll` body with new emit at ticket-roll site (D-243-C005 / D-243-F005)
    - audit/v31-243-DELTA-SURFACE.md §1.8 reconciliation row INV-237-124 (`_jackpotTicketRoll` FUNCTION-MODIFIED — emit added but RNG arithmetic unchanged) and INV-237-101 (DECOUPLED — naming-collision note for `_distributeTicketsToBuckets` plural vs singular)
  </read_first>
  <action>
Execute the EVT-01 + EVT-03 + EVT-04 audit pass for commit `ced654df`. This task does NOT touch the cc68bfc7 BAF-coupling addendum (Task 2 owns that).

**Step A — Sanity gate (mandatory before any write):**

Run exactly:
```
git rev-parse 7ab515fe
git rev-parse cc68bfc7
git rev-parse HEAD
git diff --stat cc68bfc7..HEAD -- contracts/
git status --porcelain contracts/ test/
```

Locked expectations:
- `git rev-parse 7ab515fe` resolves to a valid commit hash (baseline anchor)
- `git rev-parse cc68bfc7` resolves to `cc68bfc70e76fb75ac6effbc2135aae978f96ff3` (head anchor per CONTEXT.md D-19)
- `git diff --stat cc68bfc7..HEAD -- contracts/` reports zero file changes (the only commits since `cc68bfc7` are documentation appends to .planning/ + audit/ — verified at planning time)
- `git status --porcelain contracts/ test/` returns empty (no working-tree contamination)

If `git diff cc68bfc7..HEAD -- contracts/` is non-empty, STOP. Record a scope-guard deferral in the plan SUMMARY and abort — the contract surface has drifted past the CONTEXT-locked anchor and `cc68bfc7` is no longer authoritative.

**Step B — Create file shell:**

Create `audit/v31-244-EVT.md` with the literal sections (planner's discretion on within-section table-vs-prose ordering per CONTEXT.md Discretion):

```
# v31.0 Phase 244 — EVT Bucket Audit (commits ced654df + cc68bfc7 BAF-coupling addendum)

Audit baseline: 7ab515fe
Audit head:     cc68bfc7
Owning commits: ced654df (JackpotTicketWin event correctness) + cc68bfc7 (BAF-coupling per CONTEXT.md D-03)
Scope per:      audit/v31-243-DELTA-SURFACE.md §6 D-243-I004..D-243-I007
Plan:           244-01-PLAN.md
Phase:          244-per-commit-adversarial-audit-evt-rng-qst-gox

## §0 — Per-Bucket Verdict Count Card

| REQ-ID | Verdict Rows | Finding Candidates | Floor Severity |
| --- | --- | --- | --- |
| EVT-01 | (filled at task close) | (filled at task close) | (filled) |
| EVT-02 | (filled at Task 2) | (filled) | (filled) |
| EVT-03 | (filled) | (filled) | (filled) |
| EVT-04 | (filled) | (filled) | (filled) |

## §EVT-01 — Every JackpotTicketWin emit path emits non-zero TICKET_SCALE-scaled ticketCount

(verdict table + finding-candidate prose)

## §EVT-03 — Uniform TICKET_SCALE scaling across BAF + trait-matched paths

(verdict table + finding-candidate prose)

## §EVT-04 — JackpotTicketWin event NatSpec accuracy at HEAD cc68bfc7

(verdict table + finding-candidate prose; literal NatSpec block embedded)

(§EVT-02 + cc68bfc7 BAF-coupling sub-sections appended by Task 2)

## §Reproduction Recipe — EVT bucket

(commands appended at task close — Task 2 appends its own commands here too)
```

**Step C — Per-emit-site enumeration for EVT-01 (vector EVT-01a from CONTEXT.md D-10):**

Run grep:
```
grep -rn --include='*.sol' 'emit JackpotTicketWin\b' contracts/modules/DegenerusGameJackpotModule.sol
grep -rn --include='*.sol' 'emit JackpotTicketWin\b' contracts/ | grep -v contracts/mocks/ | grep -v contracts/test/
```

The second grep covers any cross-file emit (expected: zero — JackpotModule is the sole emitter). Record both grep commands in §Reproduction Recipe.

For each emit-site hit, classify under one of the 5 changed functions (D-243-C001..C005):
- `_runEarlyBirdLootboxJackpot` body L670-719 → emit row(s)
- `_distributeTicketsToBucket` body L966-1019 → emit row(s)
- `runBafJackpot` body L1974-2059 → previously had stub emits (now removed per ced654df) — verify emits at this layer are now sourced from the helper functions instead
- `_awardJackpotTickets` body L2074-2117 → JackpotWhalePassWin only (Task 2 owns); JackpotTicketWin must NOT be emitted directly here per ced654df shape
- `_jackpotTicketRoll` body L2129-2173 → new ticket-roll emit row

For each JackpotTicketWin emit site, write a verdict row:

| Verdict Row ID | REQ-ID | Source 243 Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| EVT-01-V01 | EVT-01 | D-243-C001, D-243-F001 | contracts/modules/DegenerusGameJackpotModule.sol:<emit-line> | EVT-01a (every emit path enumerated) | <SAFE/INFO/LOW/MED/HIGH/CRITICAL> | <ticketCount expression as it appears at emit + the local-variable scaling chain that produces it> | ced654df |
| EVT-01-V02 | EVT-01 | D-243-C002, D-243-F002 | … | … | … | … | ced654df |
| … one row per emit site … |

**Step D — Argument-trace per emit site for EVT-01 (vector EVT-01b):**

For each EVT-01-V## row, walk the ticketCount argument expression back to its scaling site:
- Read the emit statement (e.g., `emit JackpotTicketWin(winner, lvl, traitId, ticketCount, sourceLevel, idx);`)
- Identify the local variable name passed as ticketCount
- Walk back through the function body to the assignment site of that local
- Confirm the assignment either:
  - Multiplies a raw count by `TICKET_SCALE` (or by an equivalent literal), OR
  - Receives a parameter that was itself scaled at the caller boundary (cross-cite caller's scaling proof if so)
- Confirm NO emit path passes raw unscaled count, NO emit path passes zero stub, NO emit path passes a value that mathematically can be zero under non-trivial reachable inputs

Record the trace inline in the Evidence column (compact form: `ticketCount = scaledQty (L<source>); scaledQty = units * TICKET_SCALE (L<scaling-site>)`).

If any emit site fails the trace (raw count slipped through, stub zero remains, scaling bypassed under a reachable branch), emit a finding-candidate prose block:

```
### Finding Candidate EVT-01-FC01

SEVERITY: <bucket per D-08>
File:Line: contracts/modules/DegenerusGameJackpotModule.sol:<line>
Symbol: <function name> emit at <emit-line>
Observation: <plain-language description of the bypass / stub / unscaled value>
Evidence: <argument trace showing the bypass>
Verdict-Row Cross-Ref: EVT-01-V##
Phase 246 Routing Hint: <which FIND-NN bucket / KI promotion candidate / regression appendix>
```

**Step E — EVT-03 uniform-scaling verdicts (vector EVT-03a):**

Build a separate verdict table under §EVT-03 cross-citing the EVT-01 emit-site rows but reframing the vector as "uniform TICKET_SCALE across BAF + trait-matched paths" per CONTEXT.md D-07 (per-REQ closure with explicit shared-row cross-cite). Per CONTEXT.md D-07 second sentence, EVT-03 rows MAY cross-cite EVT-01 rows with a `(see Verdict Row ID EVT-01-V## — same vector)` reference INSTEAD OF full re-derivation, IFF the vectors are the SAME. For EVT-03, the vector is materially DIFFERENT (uniform-scaling vs scaling-non-zero), so each emit site STILL gets its own EVT-03-V## row, but the Evidence column may shorten by referencing EVT-01-V## for the back-trace step and adding only the divisibility check.

For each emit value across BAF + trait-matched paths, confirm `value % TICKET_SCALE == 0` mathematically (the value is a product of TICKET_SCALE × something, OR a sum of such products). If any path produces a value that is a fractional remainder (carry, partial scaling), that is a candidate INFO finding worth recording — the UI consumer cannot divide cleanly.

**Step F — EVT-04 NatSpec accuracy verdicts (vector EVT-04a):**

Read the literal NatSpec block at `contracts/modules/DegenerusGameJackpotModule.sol:86-93` (the JackpotTicketWin event declaration at HEAD cc68bfc7) and embed verbatim in §EVT-04. Then for each NatSpec sentence/clause, write a verdict row:

| Verdict Row ID | REQ-ID | Source 243 Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| EVT-04-V01 | EVT-04 | D-243-C006, D-243-C029 | contracts/modules/DegenerusGameJackpotModule.sol:86-93 | EVT-04a (per-claim NatSpec accuracy) | <SAFE/INFO/LOW/MED/HIGH/CRITICAL> | NatSpec claim "<verbatim>" → matches/contradicts actual emit-site behavior at <line> because <reasoning> | ced654df |

Two sub-claims to verify per CONTEXT.md D-10 EVT-04 (a) + (b):
1. Scaling described correctly — does the NatSpec say ticketCount is TICKET_SCALE-scaled? Does that match the emit-site code?
2. Fractional-remainder resolution path described correctly — does the NatSpec mention carry vs `_rollRemainder` (or whichever path the code actually uses)? Does that match?

If a NatSpec clause is silent on a load-bearing detail (e.g., NatSpec says "scaled" without specifying the scale factor name), that's an INFO candidate worth recording for Phase 246 docs work.

**Step G — Update §0 Verdict Count Card for EVT-01 + EVT-03 + EVT-04 rows. Append §Reproduction Recipe with the grep + git-show commands actually used.**

Constraints:
- Zero edits to `audit/v31-243-DELTA-SURFACE.md` per CONTEXT.md D-20 (record any new in-scope symbol gap as a deferral in this plan's SUMMARY only)
- Zero F-31-NN finding-IDs (CONTEXT.md D-21 — Phase 246 owns assignment); use `Finding Candidate EVT-NN-FC##` IDs scoped per-REQ
- Zero `contracts/` or `test/` writes (CONTEXT.md D-18); only write target is `audit/v31-244-EVT.md`
- Severity pre-classification per row is REQUIRED (CONTEXT.md D-08 6-bucket bar; D-22 KI envelope re-verify only — no re-litigation of the 4 accepted RNG exceptions)
  </action>
  <verify>
    <automated>test -f audit/v31-244-EVT.md && grep -q '^## §EVT-01' audit/v31-244-EVT.md && grep -q '^## §EVT-03' audit/v31-244-EVT.md && grep -q '^## §EVT-04' audit/v31-244-EVT.md && grep -qE '^\| EVT-01-V[0-9]+ ' audit/v31-244-EVT.md && grep -qE '^\| EVT-03-V[0-9]+ ' audit/v31-244-EVT.md && grep -qE '^\| EVT-04-V[0-9]+ ' audit/v31-244-EVT.md && [ "$(git status --porcelain contracts/ test/)" = "" ] && ! grep -qE 'F-31-[0-9]' audit/v31-244-EVT.md && grep -q 'Audit baseline: 7ab515fe' audit/v31-244-EVT.md && grep -q 'Audit head:     cc68bfc7' audit/v31-244-EVT.md</automated>
  </verify>
  <done>
    - `audit/v31-244-EVT.md` exists with §EVT-01, §EVT-03, §EVT-04 sections
    - At least one verdict row per REQ in {EVT-01, EVT-03, EVT-04} with the 8 required columns per CONTEXT.md D-06
    - Every JackpotTicketWin emit site (per the grep `emit JackpotTicketWin\b` in JackpotModule) has at least one EVT-01 verdict row whose argument-trace closes to a TICKET_SCALE-scaled non-zero source
    - JackpotTicketWin event NatSpec at L86-93 cc68bfc7 embedded verbatim in §EVT-04 with per-claim verdict
    - Zero F-31-NN finding-IDs emitted
    - Zero `contracts/` or `test/` writes (`git status --porcelain contracts/ test/` returns empty)
    - Zero edits to `audit/v31-243-DELTA-SURFACE.md`
    - All cited D-243 rows from D-243-I004 + D-243-I006 + D-243-I007 appear in verdict-row Source 243 Row(s) cells at least once
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit ced654df JackpotWhalePassWin emit (EVT-02) + cc68bfc7 BAF-coupling addendum sub-section (EVT-02 / EVT-03 expansion per D-03 + close §1.7 bullets 6+7 per D-09)</name>
  <files>audit/v31-244-EVT.md</files>
  <read_first>
    - .planning/phases/244-per-commit-adversarial-audit-evt-rng-qst-gox/244-CONTEXT.md — D-03 (cc68bfc7 BAF-coupling routes through EVT plan), D-09 (8 INFO Finding Candidates pre-loaded into 244 verdict pool — bullets 6 + 7 are Task 2's responsibility), D-10 EVT-02 vectors, D-22 KI envelope re-verify only
    - audit/v31-243-DELTA-SURFACE.md §1.4 commit-771893d1 row D-243-C004 (`_awardJackpotTickets` modified — new JackpotWhalePassWin emit in whale-pass fallback branch at L2074-2117)
    - audit/v31-243-DELTA-SURFACE.md §1.6 commit-cc68bfc7 changelog rows D-243-C035 (BafSkipped event ADDED at DegenerusJackpots.sol:71-74), D-243-C036 (markBafSkipped function ADDED at DegenerusJackpots.sol:498-510), D-243-C037 (IDegenerusJackpots.markBafSkipped interface method ADDED), D-243-C038 (jackpots constant ADDED at DegenerusGameAdvanceModule.sol:105-106), D-243-C039 (`_consolidatePoolsAndRewardJackpots` MODIFIED with bit-0 BAF gating at L728-909), D-243-C040 (jackpots constant inventory entry), D-243-C041 (BafSkipped event inventory entry), D-243-C042 (markBafSkipped interface method inventory entry)
    - audit/v31-243-DELTA-SURFACE.md §1.7 bullet 6 (BAF expected-value re-verification under bit-0 of rngWord coupling — same low-order bit BurnieCoinflip._resolveDay consumes), bullet 7 (markBafSkipped consumer gating on `cursor > lastBafResolvedDay`)
    - audit/v31-243-DELTA-SURFACE.md §2.3 classification rows D-243-F004 (`_awardJackpotTickets` MODIFIED_LOGIC), D-243-F025 (markBafSkipped NEW), D-243-F026 (`_consolidatePoolsAndRewardJackpots` MODIFIED_LOGIC at cc68bfc7)
    - audit/v31-243-DELTA-SURFACE.md §3.1.4 D-243-X006 (`_awardJackpotTickets` caller — small-lootbox branch L2018), D-243-X007 (odd-index branch L2049); §3.1.23 D-243-X053 (`markBafSkipped` caller at AdvanceModule L839); §3.1.24 D-243-X054 (`_consolidatePoolsAndRewardJackpots` caller at advanceGame L422); §3.2.4 D-243-X060 (interface-method-level row for markBafSkipped)
    - audit/v31-243-DELTA-SURFACE.md §6 Consumer Index rows D-243-I005 (EVT-02 → C004 + F004 + X006/X007)
    - contracts/modules/DegenerusGameJackpotModule.sol at HEAD cc68bfc7 — L2074-2117 `_awardJackpotTickets` body (whale-pass fallback emit; D-243-C004) + L86-120 area for the JackpotWhalePassWin event declaration (NOT new — existed at baseline per §4.2 row D-243-C029 note; only EMIT sites are new)
    - contracts/DegenerusJackpots.sol at HEAD cc68bfc7 — L71-74 `event BafSkipped` + L498-510 `markBafSkipped` (cc68bfc7 ADDITIONS); plus the per-NatSpec consumer hint mentioning BurnieCoinflip
    - contracts/interfaces/IDegenerusJackpots.sol at HEAD cc68bfc7 — L30-34 `markBafSkipped` interface declaration
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD cc68bfc7 — L105-106 `jackpots` constant + L728-909 `_consolidatePoolsAndRewardJackpots` body (cc68bfc7 BAF-coupling at the new `if ((rngWord & 1) == 1) { ... } else { jackpots.markBafSkipped(lvl); }` branch around L826-839)
    - contracts/BurnieCoinflip.sol at HEAD cc68bfc7 — `_resolveDay` body (cited per §1.7 bullet 6 as the other consumer of `rngWord & 1`); needed for the coupling-fairness verdict — find the line that consumes `rngWord & 1` (or equivalent low-order-bit check)
    - audit/KNOWN-ISSUES.md — KI exception envelope (no widening allowed per CONTEXT.md D-22)
  </read_first>
  <action>
Append §EVT-02 + a `## §cc68bfc7-BAF-Coupling Sub-Section` block to `audit/v31-244-EVT.md` (Task 1 created the file shell + §EVT-01/03/04 sections).

**Step A — Per CONTEXT.md D-19 / Task 1 Step A: re-confirm the HEAD anchor `cc68bfc7` matches `git rev-parse cc68bfc7` and that `git diff cc68bfc7..HEAD -- contracts/` is empty. If diff is non-empty, STOP and record a scope-guard deferral in plan SUMMARY.**

**Step B — EVT-02 verdict table — JackpotWhalePassWin emit (vector EVT-02a from CONTEXT.md D-10):**

Append `## §EVT-02 — JackpotWhalePassWin emit covers previously-silent large-amount odd-index BAF path` to `audit/v31-244-EVT.md`.

Run grep:
```
grep -rn --include='*.sol' 'emit JackpotWhalePassWin\b' contracts/ | grep -v contracts/mocks/ | grep -v contracts/test/
```

Expected: emit site(s) inside `_awardJackpotTickets` (D-243-C004 / D-243-F004) at the whale-pass fallback branch (large-amount path, `amount > LOOTBOX_CLAIM_THRESHOLD`). Cross-cite the two callers D-243-X006 (small-lootbox branch L2018) and D-243-X007 (odd-index branch L2049) — these are CALLER sites of `_awardJackpotTickets`, not emit sites of JackpotWhalePassWin itself. The whale-pass emit lives INSIDE `_awardJackpotTickets`, so the call from the odd-index branch (L2049) is the path that exercises the previously-silent odd-index BAF case.

Per-amount-bucket dispatch trace:
1. From the odd-index BAF branch in `runBafJackpot` (L1974-2059), walk into `_awardJackpotTickets` (L2074-2117) via the L2049 caller
2. Inside `_awardJackpotTickets`, identify the per-amount-bucket dispatch (small-lootbox vs whale-pass-fallback for `amount > LOOTBOX_CLAIM_THRESHOLD`)
3. Confirm the new emit covers the large-amount odd-index BAF path that was previously silent (no emit at all under ced654df baseline)
4. Confirm the `amount` and `traitId` arguments to JackpotWhalePassWin are correctly populated (no zero, no stub, no swapped indexer)

For each emit site, write a verdict row:

| Verdict Row ID | REQ-ID | Source 243 Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| EVT-02-V01 | EVT-02 | D-243-C004, D-243-F004, D-243-X007 | contracts/modules/DegenerusGameJackpotModule.sol:<emit-line> | EVT-02a (whale-pass fallback emit covers previously-silent odd-index BAF) | <bucket> | dispatch-trace `runBafJackpot:2049 → _awardJackpotTickets:<L> → emit JackpotWhalePassWin(amount=<expr>, traitId=<expr>)`; argument expressions verified against the L2049 call-site values | ced654df |

If the whale-pass fallback emit is reachable from BOTH the small-lootbox branch AND the odd-index BAF branch (depending on how `_awardJackpotTickets` dispatches internally), emit one EVT-02-V## row per reachable path.

**Step C — Append `## §cc68bfc7-BAF-Coupling Sub-Section` block (per CONTEXT.md D-03):**

Open the section with a one-paragraph framing:

```
This sub-section closes Phase 244 EVT-02 / EVT-03 sub-scope expansion per CONTEXT.md D-03 and Phase 243 §1.7 finding-candidate bullets 6 (BAF bit-0 coupling) + 7 (markBafSkipped consumer gating) per CONTEXT.md D-09. The cc68bfc7 commit (3 files / +47/-10) introduces:
- new event `BafSkipped` at DegenerusJackpots.sol:71-74 (D-243-C035)
- new external `markBafSkipped(uint24 lvl)` at DegenerusJackpots.sol:498-510 (D-243-C036; D-243-F025 NEW)
- new interface declaration `IDegenerusJackpots.markBafSkipped` at IDegenerusJackpots.sol:30-34 (D-243-C037 / D-243-C042)
- new file-scope constant `jackpots = IDegenerusJackpots(ContractAddresses.JACKPOTS)` at DegenerusGameAdvanceModule.sol:105-106 (D-243-C038 / D-243-C040)
- BAF-firing branch at `_consolidatePoolsAndRewardJackpots` (DegenerusGameAdvanceModule.sol:728-909, MODIFIED_LOGIC per D-243-F026) gated on `(rngWord & 1) == 1` — losing-flip branch invokes `jackpots.markBafSkipped(lvl)` in place of `runBafJackpot`
```

**Step D — Verdict table for EVT-02 sub-section — markBafSkipped consumer gating (vector EVT-02b — §1.7 bullet 7):**

Run greps:
```
grep -rn --include='*.sol' '\bbafBrackets\b' contracts/ | grep -v contracts/mocks/ | grep -v contracts/test/
grep -rn --include='*.sol' '\bwinningBafCredit\b' contracts/ | grep -v contracts/mocks/ | grep -v contracts/test/
grep -rn --include='*.sol' '\blastBafResolvedDay\b' contracts/ | grep -v contracts/mocks/ | grep -v contracts/test/
```

For each consumer hit of `bafBrackets[lvl]` and `winningBafCredit`:
- Identify the consuming function (function header line)
- Verify the consumer filters on `cursor > lastBafResolvedDay` (the gating predicate per markBafSkipped NatSpec)
- If it does, emit a SAFE verdict row
- If it does NOT, emit a finding-candidate (per §1.7 bullet 7 the NatSpec names BurnieCoinflip as one consumer; verify whether it is the SOLE consumer or one of several)

| Verdict Row ID | REQ-ID | Source 243 Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| EVT-02-V02 | EVT-02 | D-243-C036, D-243-F025, §1.7 bullet 7 | contracts/<consumer-file>:<line> | EVT-02b (consumer filters on `cursor > lastBafResolvedDay`) | <bucket> | grep cite + caller-function header + filter check | cc68bfc7 |

**Step E — Verdict table for EVT-03 sub-section — bit-0 BAF coupling fairness (vector EVT-03b — §1.7 bullet 6):**

Append a verdict block under §EVT-03 (file already has the §EVT-03 section from Task 1; this appends additional EVT-03-V## rows for the cc68bfc7 vector).

Adversarial vector EVT-03b: the same low-order bit `rngWord & 1` is consumed by BOTH `_consolidatePoolsAndRewardJackpots` BAF gate (cc68bfc7 ADDED) AND BurnieCoinflip._resolveDay daily-win/loss outcome. BAF resolution is now correlated with the daily coinflip rather than independent.

Required verdict checks:
1. Identify the BurnieCoinflip._resolveDay site that consumes `rngWord & 1` (or equivalent low-order-bit check) — grep `BurnieCoinflip.sol` for `& 1` or `rngWord` usage
2. Document the exact correlation: when daily flip wins (bit-0 = 1 OR 0 — verify which), BAF fires; when daily flip loses, BAF is skipped via markBafSkipped
3. Compute the new BAF expected-value: previously BAF fires every day; now BAF fires ~50% of the time (assuming bit-0 is uniformly distributed in the VRF word, which Phase 215/237 already proved holds for the VRF-derived rngWord)
4. Verdict: this is INTENTIONAL per commit-msg ("BAF gated on daily flip win"); SEVERITY:INFO is the expected verdict (downstream-coupling consequence worth recording for Phase 246 economic-impact ledger). Verify there is no ATTACKER-amplifiable consequence (e.g., a player cannot precompute `rngWord & 1` because rngWord is committed by VRF-then-callback; a validator cannot grind the flip because BurnieCoinflip is VRF-based not blockhash-based — RE_VERIFY KI EXC-02 envelope per CONTEXT.md D-22)

| Verdict Row ID | REQ-ID | Source 243 Row(s) | File:Line | Adversarial Vector | Verdict | Evidence | Owning Commit SHA |
| EVT-03-V<n> | EVT-03 | D-243-C039, D-243-F026, D-243-X053, §1.7 bullet 6 | contracts/modules/DegenerusGameAdvanceModule.sol:826 (BAF gate) + contracts/BurnieCoinflip.sol:<resolveDay-line> | EVT-03b (bit-0 of rngWord coupled across BAF + daily flip) | <bucket — INFO expected> | bit-0 consumption in both functions; expected-value calculus; RE_VERIFY KI EXC-02 envelope unchanged (no widening of accepted prevrandao-fallback exception) | cc68bfc7 |

**Step F — Verdict for jackpots direct-handle vs runBafJackpot self-call reentrancy parity (cross-cite §1.7 bullet 8 — primary REQ owner is RNG-01 / GOX-06 in 244-02 / 244-04):**

Per CONTEXT.md D-09 mapping, §1.7 bullet 8 (cc68bfc7 jackpots direct-handle vs runBafJackpot delegatecall reentrancy parity) is OWNED by 244-02 RNG-01 + 244-04 GOX-06 cross-cite, NOT by 244-01. This task does NOT close bullet 8 — it surfaces a NOTE in §EVT-03 (or a new §cc68bfc7-Cross-REQ-Notes block) that bullet 8 is owned by 244-02 + 244-04. No verdict row emitted in this plan for bullet 8.

**Step G — Update §0 Verdict Count Card with EVT-02 + EVT-03 cc68bfc7 row counts and floor severities. Append §Reproduction Recipe with the new grep + git-show commands actually used (BafSkipped grep, markBafSkipped grep, bafBrackets grep, winningBafCredit grep, lastBafResolvedDay grep, BurnieCoinflip.sol read).**

Constraints (carry from Task 1):
- Zero edits to `audit/v31-243-DELTA-SURFACE.md`
- Zero F-31-NN finding-IDs (Phase 246 owns)
- Zero `contracts/` or `test/` writes
- KI envelope re-verify only — no re-litigation of EXC-01..04 (CONTEXT.md D-22)
  </action>
  <verify>
    <automated>test -f audit/v31-244-EVT.md && grep -q '^## §EVT-02' audit/v31-244-EVT.md && grep -q '^## §cc68bfc7-BAF-Coupling Sub-Section' audit/v31-244-EVT.md && grep -qE '^\| EVT-02-V[0-9]+ ' audit/v31-244-EVT.md && grep -q 'markBafSkipped' audit/v31-244-EVT.md && grep -q 'rngWord & 1' audit/v31-244-EVT.md && grep -q 'lastBafResolvedDay' audit/v31-244-EVT.md && [ "$(git status --porcelain contracts/ test/)" = "" ] && ! grep -qE 'F-31-[0-9]' audit/v31-244-EVT.md && [ "$(git status --porcelain audit/v31-243-DELTA-SURFACE.md)" = "" ]</automated>
  </verify>
  <done>
    - `audit/v31-244-EVT.md` contains §EVT-02 with at least one EVT-02-V## row whose Source 243 Row(s) cites D-243-C004 + D-243-F004 + at least one of D-243-X006/X007
    - `audit/v31-244-EVT.md` contains a `## §cc68bfc7-BAF-Coupling Sub-Section` block citing D-243-C035..C042 + D-243-X053/X054/X060 + §1.7 bullets 6 + 7
    - At least one EVT-02-V## row covers markBafSkipped consumer-gating verdict (vector EVT-02b)
    - At least one EVT-03-V## row covers bit-0 BAF-coupling fairness verdict (vector EVT-03b) with KI EXC-02 envelope re-verify note
    - §1.7 bullet 6 + bullet 7 each receive a closing verdict (or are explicitly rolled forward to Phase 245 with hand-off note per CONTEXT.md D-09)
    - §1.7 bullet 8 surfaces a NOTE that 244-02 / 244-04 own its closure (no verdict emitted in 244-01)
    - Reproduction Recipe sub-section appended with the new grep commands actually run
    - Zero F-31-NN finding-IDs
    - Zero `contracts/`, `test/`, or `audit/v31-243-DELTA-SURFACE.md` writes
  </done>
</task>

</tasks>

<verification>
**End-of-plan verification gate (manual, before commit):**

1. **Coverage gate:** Every REQ in {EVT-01, EVT-02, EVT-03, EVT-04} appears at least once as the prefix of a Verdict Row ID in `audit/v31-244-EVT.md`. Run:
   ```
   for req in EVT-01 EVT-02 EVT-03 EVT-04; do
     count=$(grep -cE "^\| $req-V[0-9]+ " audit/v31-244-EVT.md)
     echo "$req verdict rows: $count"
     [ "$count" -ge 1 ] || { echo "FAIL: $req has zero verdict rows"; exit 1; }
   done
   ```
2. **Source 243 row citation gate:** Every D-243 row in the Consumer Index entries D-243-I004..D-243-I007 appears at least once in a verdict-row Source 243 Row(s) cell. Run a per-row grep loop.
3. **§1.7 candidate closure gate:** Bullets 6 + 7 each have a closing verdict OR a hand-off note. Bullet 8 has a NOTE deferring to 244-02 / 244-04. Run:
   ```
   grep -E '§1\.7 bullet (6|7|8)' audit/v31-244-EVT.md
   ```
4. **Severity gate:** Every verdict row has a severity from {SAFE, INFO, LOW, MEDIUM, HIGH, CRITICAL}. Zero TBD/blank verdicts. Grep for `\| (SAFE|INFO|LOW|MEDIUM|HIGH|CRITICAL) \|` and count vs total verdict rows.
5. **READ-only gate:** `git status --porcelain contracts/ test/` returns empty; `git status --porcelain audit/v31-243-DELTA-SURFACE.md` returns empty (no edits to upstream Phase 243 catalog).
6. **F-31-NN absence gate:** `! grep -qE 'F-31-[0-9]' audit/v31-244-EVT.md` (zero F-31-NN finding-IDs emitted; Phase 246 owns assignment per CONTEXT.md D-21).
7. **Reproduction recipe completeness:** §Reproduction Recipe sub-section contains every grep / git-show command actually used by Tasks 1 + 2 (POSIX-portable per CONTEXT.md D-04 reproduction-recipe carry).
</verification>

<success_criteria>
- `audit/v31-244-EVT.md` exists with valid frontmatter (Audit baseline / head / Owning commits / Scope per / Plan / Phase fields populated)
- §EVT-01, §EVT-02, §EVT-03, §EVT-04 sections each contain at least one verdict row with all 8 columns populated per CONTEXT.md D-06
- `## §cc68bfc7-BAF-Coupling Sub-Section` exists and closes §1.7 bullets 6 + 7 with verdicts (or hand-off notes)
- §0 Verdict Count Card filled out per REQ
- §Reproduction Recipe sub-section appended with all grep + git-show commands used
- Plan SUMMARY records: total verdict-row count per REQ, total finding-candidate count per REQ, any scope-guard deferrals (gaps in Phase 243 catalog), and the working-file path `audit/v31-244-EVT.md` for 244-04 consolidation
</success_criteria>

<output>
After completion, create `.planning/phases/244-per-commit-adversarial-audit-evt-rng-qst-gox/244-01-SUMMARY.md` per the standard SUMMARY template + the per-bucket-plan additions:
- Per-REQ verdict-row count + floor severity
- Per-REQ finding-candidate count + severity distribution
- Phase 243 §1.7 bullet-closure status (bullets 6, 7 closed; bullet 8 deferred to 244-02 / 244-04)
- Path to working file: `audit/v31-244-EVT.md` (consumed by 244-04 consolidation step per CONTEXT.md D-05)
- Any scope-guard deferrals per CONTEXT.md D-20 (in-scope symbols missing from Phase 243 catalog — recorded here, NOT edited into Phase 243)
- HEAD anchor confirmation (`git rev-parse cc68bfc7` matched at task-start)
</output>
