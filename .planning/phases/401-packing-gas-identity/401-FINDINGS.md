# Phase 401 — PACKING & GAS-IDENTITY: dual-net findings (v64.0)

**Status:** ✅ COMPLETE — both nets on record (codex deferred, see note), adjudicated vs frozen source.
**Requirements:** PACK-01..04 · **Subject frozen** `402855e1` (working tree == frozen, verified clean).
**Nets:**
- **NET-1 council** — `gemini` (CLEAN on all 4 PACK invariants). **`codex` DEFERRED** — ChatGPT usage cap until ~02:33 (see `phases/CODEX-BACKFILL-TODO.md`; backfill after reset).
- **NET-2 Claude Workflow** — 4 dimension finders + per-lead refute + completeness critic (`w1rh83t2x`, 7 agents): PACK-01/02/03 = 0 leads; PACK-04 = 2 leads **both REFUTED**; critic = 0 leads. Bounds derived against authoritative `forge inspect` layouts.
- **Deterministic scripts** — `check-raw-selectors` PASS, `check-interface-coverage` PASS, `check-delegatecall-alignment` FAIL = stale-checker naming (see PACK-C).

## Outcome: 0 CATASTROPHE / 0 HIGH / 0 MED / 0 LOW. PACK-01..04 attested clean. 2 PACK-04 leads REFUTED. 2 INFO doc-drift + 1 INFO checker-staleness + 1 dev-tooling note.

| # | Site | Disposition | Detail |
|---|------|-------------|--------|
| PACK-04-L1 | `hasAnyLazyPass`/`lazyPassHorizon` removed from Game proxy · `DegenerusGame.sol` (baseline `:1648,:1668`) | **REFUTED (no consumer break)** | NET-2 finder raised LOW; NET-2 refuter + my cross-check REFUTED: these `external view`s were **never in the vendored/published ABI** (baseline ABI snapshot + current forge-out both show 0 occurrences) — no production consumer (the indexer) could have called them; the indexer is event-driven (`validThroughLevel` from events). Removed for EIP-170 reclamation (flagged zero-on-chain-caller in `GAS-AUDIT-2026-06-10`); replacement `_passHorizonOf` is `internal`. **Dev-tooling note:** local `degenerus-sim` scripts declare these in inline ABI fragments and call them — would need updating if re-run against the new build (not a production consumer). |
| PACK-04-L2 | `sampleTraitTickets` → `sampleTraitTicketsAtLevel` · `DegenerusGame.sol:2314` | **REFUTED (dead-code removal)** | NET-2 refuter: not a return-shape reshape — both functions co-existed in baseline `77580320`; the sole on-chain caller (`DegenerusJackpots.sol:389`) already used `sampleTraitTicketsAtLevel` at baseline, and the indexer ABI already carries the new name. The removed `sampleTraitTickets(uint256)` was dead code → removing it broke no consumer. |
| PACK-C | `check-delegatecall-alignment.sh` FAIL on `GAME_AFKING_MODULE` | **INFO — stale checker naming (not a contract defect)** | The afking module IS correctly dispatched via raw `.delegatecall(msg.data)` thin stubs (`DegenerusGame.sol:366-441`) AND has an interface — named **`IGameAfkingModule`** (`IDegenerusGameModules.sol:505`), not the `IDegenerusGameAfkingModule` the checker's naming convention derives. Checker-universe staleness; fix is a `NAMING_EXCEPTIONS` registration in the test tooling (optional, non-contract). |
| PACK-D | Stale comments · `DegenerusGameStorage.sol:2320` + `GameAfkingModule.sol:619/625` (SUBSCRIBER_CAP "500" vs actual 1000) ; `StakedDegenerusStonk.sol:269` (ethValueOwed "~79B ETH" vs actual 160-ETH cap) | **INFO doc-drift (no width/logic impact)** | The bounds still hold (1000 << uint16 65535; 160 ETH << uint96). Inert comment text; deferrable to a comment-cleanup pass. |

## Verification map — PACK-01..04 attested (gemini + NET-2 + scripts; codex deferred)

- **PACK-01 — narrowing safety:** every narrowed field's width ≥ its reachable max, derived against `forge inspect` + the widest writer/cap. Highlights: **Admin `uint40` vote weight** safe under the fixed 1.0T sDGNRS supply (uint40 max 1.0995e12 ≥ 1e12, ~9.5% headroom); **`activityScore` `uint16(score)+1`** lands EXACTLY at 65535 because `ACTIVITY_SCORE_HARD_CAP_BPS=65534` is deliberately tuned to leave room for the +1 sentinel (tight, non-truncating); **BurnieCoinflip lossless-wei stake** stores raw wei in a 128-bit lane bound by BurnieCoin's `uint128` supply cap (`_toUint128` reverts) — no spill into the sibling-day lane; **8-bit 3-state day-result** covers 0=unresolved / 1=loss / 50..156=win with no collision (max 156 < 256); **Game slot-58 6-field merge** (`_subCursor`/`_subOpenCursor` u16 / `_afkingResetDay` u24 / `boxCursor`/`boxCursorIndex`/`presaleCloseIndex` u48) = 200 bits, cursors bound by `cursor < len ≤ SUBSCRIBER_CAP(1000)`. ✅ 0 leads.
- **PACK-02 — masked RMW + slot agreement:** every masked RMW helper clears exactly its own bits (canonical `(data & ~(mask<<shift)) | ((val&mask)<<shift)`); `balancesPacked` 128/128 split guarded against cross-half carry/borrow; cross-module delegatecall-shared slots agree on offset/width/mask by construction (Game struct defs cross-checked against every module). ✅ 0 leads (gemini + NET-2).
- **PACK-03 — dispatch + gas hot-path identity:** the raw `delegatecall(msg.data)` stubs resolve the same selector + ABI-decode + revert-bubble + return data; gas-round refactors change no externally-observable behavior (output/revert/event). The afking raw-dispatch is correct (PACK-C). ✅ 0 leads (gemini + NET-2 + scripts).
- **PACK-04 — ABI getter preservation:** every privatized/packed field still has its consumer-facing getter (privatized RNG fields `rngWordCurrent`/`vrfRequestId` exposed via `isRngFulfilled()`/`rngWordByDay`); the 2 raised removals REFUTED (never-vendored views + dead-code rename); the 3 indexer-parity events keep stable signatures. ✅ 0 surviving leads.

## Verdict

The storage-packing + gas-refactor delta is **behavior- and value-identical**: no silent truncation (every narrowing bounded ≥ its real max via `forge inspect`), no co-resident clobber, no cross-module slot disagreement, no dispatch divergence, and no production ABI/interface break (both PACK-04 removal leads REFUTED — never-vendored views + dead-code rename). Residue = 2 stale comments + 1 stale-checker naming + 1 local dev-script note, all INFO/non-contract. **PACK-01..04 attested.** NET-1 gemini + NET-2 Claude + the deterministic scripts on record; **codex deferred to post-reset backfill** (its lens is additive — the result is already convergent-clean across two independent nets + forge-inspect ground truth).
