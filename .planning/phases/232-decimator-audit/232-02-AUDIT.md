# DCM-02 Adversarial Audit: Decimator Event Emission

**Scope:** Commit `67031e7d` ("feat(decimator): emit `DecimatorClaimed` and `TerminalDecimatorClaimed`") — `DegenerusGameDecimatorModule.claimDecimatorJackpot` (ID-89) + `DegenerusGameDecimatorModule.claimTerminalDecimatorJackpot` (ID-93) body-level emit additions across §1.3 of `230-01-DELTA-MAP.md`, plus the two NEW event declarations `DecimatorClaimed` and `TerminalDecimatorClaimed`.
**Method:** Fresh read from HEAD source (per D-03) — no reuse of v25.0 Phase 214 or v27.0 Phase 223 verdicts as pre-approved; each of the 3 new `emit` sites audited for CEI position (D-08) and argument correctness (D-09) independently. Pre-fix vs post-fix comparison via `git show 67031e7d^:contracts/modules/DegenerusGameDecimatorModule.sol` (zero matches for `DecimatorClaimed` / `TerminalDecimatorClaimed` strings — confirms all 3 emit sites + 2 event declarations are wholly NEW with this commit).
**Scope source:** `230-01-DELTA-MAP.md` §4 Consumer Index row DCM-02 (per D-04)
**Verdict vocabulary:** `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (per D-02)
**Finding-ID policy:** No finding IDs emitted (per D-13) — `Finding Candidate: Y/N` column only; Phase 236 FIND-01 owns canonical ID assignment.
**Indexer-compat scope:** v28.0 Phase 227 cross-reference is a READ-only OBSERVATION (per D-10). No `database/` writes permitted.
**HEAD:** `af910648` (`git rev-parse HEAD` at audit start)
**Owning SHA verified:** `git show 67031e7d --stat` → 1 file changed, 37 insertions, 0 deletions in `contracts/modules/DegenerusGameDecimatorModule.sol`. Diff hunks: (1) `event DecimatorClaimed` declaration (lines 49-61 at HEAD); (2) `emit DecimatorClaimed(msg.sender, lvl, amountWei, amountWei, 0)` inside `claimDecimatorJackpot` gameOver branch (line 331 at HEAD); (3) multi-line `emit DecimatorClaimed(msg.sender, lvl, amountWei, amountWei - lootboxPortion, lootboxPortion)` after the normal split (lines 343-349 at HEAD); (4) `event TerminalDecimatorClaimed` declaration (lines 625-633 at HEAD); (5) multi-line `emit TerminalDecimatorClaimed(msg.sender, lastTerminalDecClaimRound.lvl, amountWei)` inside `claimTerminalDecimatorJackpot` (lines 815-819 at HEAD).

## Methodology

Six attack vectors applied to the 3 new emit sites and 2 new event declarations per `232-CONTEXT.md` D-08, D-09, and D-10 (plus 4-indexed-topic and gas notes from the Out-of-Scope Reminders block):

- **(a) D-08 CEI position — gameOver fast-path emit (`claimDecimatorJackpot` site 1, line 331):** Verify the emit fires AFTER `_creditClaimable(msg.sender, amountWei)` and BEFORE the `return` — no SSTORE or external call between the credit and the emit, no statement at all between them. The `_consumeDecClaim` call earlier in the function has already executed `e.claimed = 1` (line 297) so the one-shot mutation is committed.
- **(b) D-08 CEI position — normal split emit (`claimDecimatorJackpot` site 2, lines 343-349):** Verify the emit fires AFTER `_creditDecJackpotClaimCore` (which performs `_creditClaimable` + `claimablePool -=` + `_awardDecimatorLootbox`) AND AFTER the conditional `_setFuturePrizePool(_getFuturePrizePool() + lootboxPortion)` — no SSTORE or external call between the pool write and the emit.
- **(c) D-08 CEI position — terminal emit (`claimTerminalDecimatorJackpot` site 3, lines 815-819):** Verify the emit fires AFTER `_consumeTerminalDecClaim(msg.sender)` (state mutation `e.weightedBurn = 0` at line 880 is the consume operation that makes the claim one-shot) AND AFTER `_creditClaimable(msg.sender, amountWei)` — no SSTORE or external call between the credit and the emit.
- **(d) D-09 argument correctness — `DecimatorClaimed.ethPortion + lootboxPortion == amountWei`:** Verify algebraically at both emit sites. GameOver branch: `(amountWei, 0)` literal → `amountWei + 0 == amountWei` ✓. Normal split branch: `(amountWei - lootboxPortion, lootboxPortion)` → `(amountWei - lootboxPortion) + lootboxPortion == amountWei` ✓ by Solidity unchecked-arithmetic identity. Verify `lootboxPortion ≤ amountWei` so the subtraction does not underflow (Solidity ≥0.8 reverts on underflow — confirmed by `_creditDecJackpotClaimCore` line 383: `lootboxPortion = amount - ethPortion` where `ethPortion = amount >> 1` so `lootboxPortion = amount - (amount >> 1) ≤ amount`).
- **(e) D-09 argument correctness — `lvl == claimDecimatorJackpot(lvl)` input arg + `player == msg.sender`:** Verify both emit sites in `claimDecimatorJackpot` pass through the function's `uint24 lvl` parameter unchanged (no local re-derivation) and `msg.sender` literal (no local alias risk).
- **(f) D-09 argument correctness — `TerminalDecimatorClaimed.lvl == lastTerminalDecClaimRound.lvl` (storage-sourced, NOT caller-controlled):** Verify the `lvl` arg is read from storage at the emit site, and trace the canonical SSTORE that sets `lastTerminalDecClaimRound.lvl` (`runTerminalDecimatorJackpot` line 798 — set during jackpot resolution by Game-only entry-guarded path, not during claim).
- **(g) D-09 argument correctness — `TerminalDecimatorClaimed.amountWei == consumed claim`:** Verify the emit's `amountWei` is the return value of `_consumeTerminalDecClaim(msg.sender)` (line 812), which is computed at line 874-876 from `lastTerminalDecClaimRound.poolWei * weight / totalBurn` — the algebraically consumed amount.
- **(h) D-10 indexer-compat OBSERVATION:** Cross-reference the v28.0 Phase 227 event-coverage matrix to determine whether the two new event signatures are registered in the v28.0 indexer event-processor surface. Per `feedback_contract_locations.md` and the v29.0 PROJECT.md scoping rule, this is OBSERVATION ONLY — `database/` writes are out of scope for this milestone.
- **(i) Claude's Discretion: 4-indexed-topic EVM limit:** Confirm both events use 2 indexed topics (`player` + `lvl`) — well below the EVM's 4-topic limit (anonymous + 3 indexed, or 4 indexed for non-anonymous events).
- **(j) Claude's Discretion: gas implication:** Both emits fire once-per-claim (NOT loop-multiplied). Per CONTEXT Out-of-Scope Reminders, gas-ceiling re-profile not required; recorded as SAFE-INFO with Finding Candidate: N.

Verdict vocabulary locked to `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` per D-02. Columns locked to `Function | File:Line | Attack Vector | Verdict | Evidence | SHA | Finding Candidate` per D-02 + D-13.

**Pre-fix vs post-fix diff (verbatim from `git show 67031e7d -- contracts/modules/DegenerusGameDecimatorModule.sol`):** 1 file changed, 37 insertions(+), 0 deletions(-). All 5 hunks are pure additions (zero deletions, zero existing line modifications). The `_creditClaimable`, `_consumeDecClaim`, `_creditDecJackpotClaimCore`, `_setFuturePrizePool`, `_consumeTerminalDecClaim` call sites were already in the pre-fix bodies at the same line offsets — `67031e7d` only inserts emits AFTER pre-existing state-mutation hops. No CEI ordering modification of any pre-existing call sequence; the emits are tail-additions after all pre-existing mutations.

## Findings-Candidate Block

No FAIL/VULNERABLE candidate findings. All 3 emit sites are CEI-correct (no SSTORE or external call between the final state mutation and the emit at any site) and all event arguments correctly source from function inputs / `msg.sender` / `lastTerminalDecClaimRound.lvl` storage / consumed return values per the D-09 invariants. One SAFE-INFO observation is recorded as `Finding Candidate: Y` for Phase 236 FIND-01:

- **(SAFE-INFO #1) Two new event signatures absent from v28.0 Phase 227 indexer event-coverage matrix.** `DecimatorClaimed(address,uint24,uint256,uint256,uint256)` and `TerminalDecimatorClaimed(address,uint24,uint256)` were introduced by `67031e7d` (2026-04-16), which postdates the v28.0 milestone cutoff. Verified via grep against `.planning/milestones/v28.0-phases/227-indexer-event-processing-correctness/*.md` — zero matches for either event name. Contract-side emission is correct (verified across all rows below); the observation is about downstream `database/` indexer registration. Per D-10 this is recorded as OBSERVATION only and routes to either Phase 236 FIND-02 (KNOWN-ISSUES candidate) or a future `database/` milestone. Zero `database/` writes attempted this phase. Recorded as a candidate so Phase 236 can decide whether to surface as an INFO observation describing the indexer-side handler-registration gap that any post-deployment indexer must close before the new events are observable off-chain.

**No DEFERRED row-level verdicts.** Two scope-boundary hand-offs (Phase 236 FIND-01 ID assignment; Phase 236 FIND-02 indexer-compat KNOWN-ISSUES candidate routing) are documented in **Downstream Hand-offs** below — these are NOT findings, they are scope boundaries.

## Per-Function Verdict Table

| Function | File:Line | Attack Vector | Verdict | Evidence | SHA | Finding Candidate |
|---|---|---|---|---|---|---|
| `DegenerusGameDecimatorModule.claimDecimatorJackpot` (gameOver emit site) | contracts/modules/DegenerusGameDecimatorModule.sol:330-332 | (a) CEI position — emit AFTER `_creditClaimable` with no intermediate state mutation or external call before `return` | SAFE | Three consecutive lines inside the `if (gameOver)` block: line 330 `_creditClaimable(msg.sender, amountWei);` (state mutation: `claimableWinnings[beneficiary] += weiAmount` at PayoutUtils:35); line 331 `emit DecimatorClaimed(msg.sender, lvl, amountWei, amountWei, 0);`; line 332 `return;`. Zero statements between credit and emit. The earlier `_consumeDecClaim(msg.sender, lvl)` at line 327 already wrote `e.claimed = 1` (line 297) — one-shot guard committed before the gameOver branch is entered. No external call between credit and emit. | 67031e7d | N |
| `DegenerusGameDecimatorModule.claimDecimatorJackpot` (gameOver emit site) | contracts/modules/DegenerusGameDecimatorModule.sol:331 | (d) Argument correctness — `ethPortion + lootboxPortion == amountWei` in gameOver fast-path | SAFE | Emit verbatim: `emit DecimatorClaimed(msg.sender, lvl, amountWei, amountWei, 0);`. Args: `ethPortion = amountWei`, `lootboxPortion = 0`. Sum: `amountWei + 0 == amountWei` — algebraic identity, holds for all `amountWei`. NatSpec on the event (line 54) explicitly documents `lootboxPortion = 0` post-GAMEOVER ("Portion routed to whale passes / lootbox (0 post-GAMEOVER)"); literal `0` matches NatSpec contract. | 67031e7d | N |
| `DegenerusGameDecimatorModule.claimDecimatorJackpot` (gameOver emit site) | contracts/modules/DegenerusGameDecimatorModule.sol:331 | (e) Argument correctness — `lvl == claimDecimatorJackpot(lvl)` input + `player == msg.sender` | SAFE | Emit verbatim: `emit DecimatorClaimed(msg.sender, lvl, amountWei, amountWei, 0);`. `msg.sender` is the EVM literal — not a local alias overwritten anywhere in the function body (verified by reading lines 321-350; no `address sender = ...` declaration). `lvl` is the function's `uint24 lvl` parameter (line 321) — not re-derived in the body; passed through unchanged from caller. `amountWei` is the return value of `_consumeDecClaim(msg.sender, lvl)` at line 327, which computes pro-rata payout from `decClaimRounds[lvl].poolWei` and `decBurn[lvl][player].weightedBurn`. | 67031e7d | N |
| `DegenerusGameDecimatorModule.claimDecimatorJackpot` (normal split emit site) | contracts/modules/DegenerusGameDecimatorModule.sol:340-349 | (b) CEI position — emit AFTER `_setFuturePrizePool` (and `_creditDecJackpotClaimCore`) with no intermediate state mutation or external call | SAFE | Sequence at HEAD: line 335-339 `_creditDecJackpotClaimCore(msg.sender, amountWei, decClaimRounds[lvl].rngWord)` (which internally performs `_creditClaimable` line 385, `claimablePool -= uint128(lootboxPortion)` line 388, `_awardDecimatorLootbox` line 389 → may delegatecall to GAME_LOOTBOX_MODULE); line 340 `if (lootboxPortion != 0) {`; line 341 `_setFuturePrizePool(_getFuturePrizePool() + lootboxPortion);` (SSTORE via packed `prizePoolsPacked`); line 342 `}`; lines 343-349 the multi-line emit. Zero statements between line 341 (final SSTORE) and line 343 (emit). The earlier `_consumeDecClaim` at line 327 already wrote `e.claimed = 1` before reaching the lootbox branch. No external call between `_setFuturePrizePool` and emit. CEI strictly preserved: all SSTOREs (claimableWinnings via _creditClaimable, claimablePool decrement, futurePool update) and the lootbox delegatecall complete BEFORE the emit. | 67031e7d | N |
| `DegenerusGameDecimatorModule.claimDecimatorJackpot` (normal split emit site) | contracts/modules/DegenerusGameDecimatorModule.sol:343-349 | (d) Argument correctness — `ethPortion + lootboxPortion == amountWei` (algebraic sum at emit) | SAFE | Emit verbatim: `emit DecimatorClaimed(msg.sender, lvl, amountWei, amountWei - lootboxPortion, lootboxPortion);`. Args: `ethPortion = amountWei - lootboxPortion`, `lootboxPortion = lootboxPortion`. Sum: `(amountWei - lootboxPortion) + lootboxPortion == amountWei` — algebraic identity, exact. Underflow safety: `lootboxPortion` returned from `_creditDecJackpotClaimCore` line 383 = `amount - (amount >> 1)`, where `amount = amountWei`; therefore `lootboxPortion ≤ amountWei` always (specifically `lootboxPortion = ⌈amountWei/2⌉`), and Solidity ≥0.8 checked subtraction `amountWei - lootboxPortion` is always non-negative (yields `⌊amountWei/2⌋`, the `ethPortion` already credited at line 385). The emit's `ethPortion = amountWei - lootboxPortion` is therefore byte-identical to the `ethPortion = amount >> 1` already credited inside `_creditDecJackpotClaimCore` line 382 — algebraically same value, no double-counting. | 67031e7d | N |
| `DegenerusGameDecimatorModule.claimDecimatorJackpot` (normal split emit site) | contracts/modules/DegenerusGameDecimatorModule.sol:343-349 | (e) Argument correctness — `lvl == input arg` + `player == msg.sender` | SAFE | Emit verbatim args: `msg.sender, lvl, amountWei, amountWei - lootboxPortion, lootboxPortion`. `msg.sender` is the EVM literal (no local alias overwritten). `lvl` is the function parameter (line 321) — passed through unchanged. `amountWei` is the `_consumeDecClaim` return value computed from `decClaimRounds[lvl]` storage at line 279 — keyed by the same `lvl`. Per DCM-01 audit (commit a7d497e7), the writer key (`level()+1` at burn time) and reader key (post-bump `lvl` at claim time) are the same integer at every hop; the emit faithfully reflects the lvl the claimer asserted. | 67031e7d | N |
| `DegenerusGameDecimatorModule.claimTerminalDecimatorJackpot` (terminal emit site) | contracts/modules/DegenerusGameDecimatorModule.sol:812-819 | (c) CEI position — emit AFTER `_consumeTerminalDecClaim` AND `_creditClaimable` with no intermediate state mutation or external call | SAFE | Three-hop ordering: line 812 `uint256 amountWei = _consumeTerminalDecClaim(msg.sender);` (state mutation at line 880 `e.weightedBurn = 0` is the one-shot consume — the function reverts at line 858/862/869/872/877 BEFORE writing if any check fails, so by the time it returns, the consume is committed); line 814 `_creditClaimable(msg.sender, amountWei);` (state mutation at PayoutUtils:35 `claimableWinnings[beneficiary] += weiAmount`); lines 815-819 the multi-line emit. Zero statements between line 814 (final SSTORE) and line 815 (emit start). No external call anywhere in the function body — `_consumeTerminalDecClaim` is `private` (line 855) and `_creditClaimable` is `internal` (PayoutUtils:32) with no external calls; both execute in current storage context. CEI strictly preserved: state-mutation order is consume (`e.weightedBurn = 0`) → credit (`claimableWinnings += amountWei`) → emit. | 67031e7d | N |
| `DegenerusGameDecimatorModule.claimTerminalDecimatorJackpot` (terminal emit site) | contracts/modules/DegenerusGameDecimatorModule.sol:815-819 | (f) Argument correctness — `lvl == lastTerminalDecClaimRound.lvl` (storage-sourced, not caller-controlled) | SAFE | Emit verbatim: `emit TerminalDecimatorClaimed(msg.sender, lastTerminalDecClaimRound.lvl, amountWei);`. The `lvl` arg is a direct SLOAD of `lastTerminalDecClaimRound.lvl` — NOT a caller-supplied value (the function takes zero args at line 811: `function claimTerminalDecimatorJackpot() external`). Canonical SSTORE site: `runTerminalDecimatorJackpot` at line 798 (`lastTerminalDecClaimRound.lvl = lvl;`), called only from `GameOverModule.handleGameOverDrain` via the IDegenerusGame self-call chain — `runTerminalDecimatorJackpot` has Game-only entry-guard at line 760 (`if (msg.sender != ContractAddresses.GAME) revert OnlyGame();`), so player-controlled paths cannot poison this storage slot. The `_consumeTerminalDecClaim` helper at line 857 SLOADs the same field for its own validation (`uint24 lvl = lastTerminalDecClaimRound.lvl;`), so the emit's `lvl` is consistent with the lvl that was used to gate the consume. Caller-controlled key-space drift impossible. | 67031e7d | N |
| `DegenerusGameDecimatorModule.claimTerminalDecimatorJackpot` (terminal emit site) | contracts/modules/DegenerusGameDecimatorModule.sol:812,815-819 | (g) Argument correctness — `amountWei == consumed claim return value` + `player == msg.sender` | SAFE | The `amountWei` arg at line 818 is the local declared at line 812 `uint256 amountWei = _consumeTerminalDecClaim(msg.sender);` — not re-derived between consume and emit (line 813 is blank; line 814 reads `amountWei` for `_creditClaimable` but does not modify the local). `_consumeTerminalDecClaim` computes `amountWei = (lastTerminalDecClaimRound.poolWei * weight) / totalBurn` at lines 874-876 — the algebraically consumed amount, identical to the credit applied at line 814. `msg.sender` is the EVM literal (no local alias overwritten). The emit therefore faithfully reflects (player, resolved-round-lvl, consumed-payout). | 67031e7d | N |
| `DecimatorClaimed` event declaration | contracts/modules/DegenerusGameDecimatorModule.sol:55-61 | (i) 4-indexed-topic EVM limit — 2 indexed topics (player + lvl) well under limit | SAFE-INFO | Declaration verbatim: `event DecimatorClaimed(address indexed player, uint24 indexed lvl, uint256 amountWei, uint256 ethPortion, uint256 lootboxPortion);`. Indexed-topic count = 2 (`player`, `lvl`); non-indexed data fields = 3 (`amountWei`, `ethPortion`, `lootboxPortion`). EVM allows up to 4 indexed topics for non-anonymous events (topic0 = signature hash + up to 3 indexed). 2 < 4: well under limit. | 67031e7d | N |
| `DecimatorClaimed` event declaration | contracts/modules/DegenerusGameDecimatorModule.sol:55-61 | (j) Gas implication — once-per-claim emit, not loop-multiplied | SAFE-INFO | Both emit sites are inside `claimDecimatorJackpot` which is callable once per `(player, lvl)` pair — guarded by `e.claimed == 0` check at line 283 (sets to 1 at line 297). Function exits after `return` (line 332) in gameOver branch or after the single emit (line 349) in normal branch. Zero loops in the function body. Per CONTEXT Out-of-Scope Reminders, gas-ceiling re-profile not required. | 67031e7d | N |
| `TerminalDecimatorClaimed` event declaration | contracts/modules/DegenerusGameDecimatorModule.sol:629-633 | (i) 4-indexed-topic EVM limit — 2 indexed topics (player + lvl) well under limit | SAFE-INFO | Declaration verbatim: `event TerminalDecimatorClaimed(address indexed player, uint24 indexed lvl, uint256 amountWei);`. Indexed-topic count = 2 (`player`, `lvl`); non-indexed data fields = 1 (`amountWei`). 2 < 4: well under limit. | 67031e7d | N |
| `TerminalDecimatorClaimed` event declaration | contracts/modules/DegenerusGameDecimatorModule.sol:629-633 | (j) Gas implication — once-per-player emit on terminal claim, not loop-multiplied | SAFE-INFO | Sole emit site is inside `claimTerminalDecimatorJackpot` (line 811) which is callable once per player — guarded by `e.weightedBurn == 0` check at line 861 (the consume sets `e.weightedBurn = 0` at line 880 making subsequent calls revert at line 861/862 with `TerminalDecNotWinner`). Zero loops in the function body. Per CONTEXT Out-of-Scope Reminders, gas-ceiling re-profile not required. | 67031e7d | N |
| `DecimatorClaimed` and `TerminalDecimatorClaimed` event signatures (indexer-compat OBSERVATION) | contracts/modules/DegenerusGameDecimatorModule.sol:55-61, 629-633 | (h) D-10 indexer-compat — neither event signature in v28.0 Phase 227 event-processor scope (postdates v28.0 cutoff) | SAFE-INFO | Cross-reference via grep against `.planning/milestones/v28.0-phases/227-indexer-event-processing-correctness/*.md`: zero matches for `DecimatorClaimed` and zero matches for `TerminalDecimatorClaimed`. Commit `67031e7d` is dated 2026-04-16, which postdates v28.0. v28.0 Phase 227 audited the event-coverage matrix at the v28.0-baseline commit, so neither event was in scope for that audit. Contract-side emission is correct (every other row in this table is SAFE); the gap is in downstream `database/` indexer registration. Per D-10 + v29.0 PROJECT.md scoping, `database/` is OUT OF SCOPE this milestone — zero `database/` writes attempted. Routes to Phase 236 FIND-02 KNOWN-ISSUES candidate or a future `database/` milestone. NOT a contract-side finding. | 67031e7d | Y |

**Row count:** 14 distinct `(emit-site / declaration × attack-vector)` rows. **Verdict tally:** 9 SAFE + 5 SAFE-INFO. Zero VULNERABLE, zero DEFERRED. One SAFE-INFO row carries `Finding Candidate: Y` for Phase 236 FIND-01 / FIND-02 (indexer-compat OBSERVATION per D-10).

## CEI-Position Analysis (per D-08)

### Emit Site 1: `claimDecimatorJackpot` — gameOver fast-path

Function entry at line 321 (`function claimDecimatorJackpot(uint24 lvl) external`). Sequence through the gameOver branch:

1. **Line 325** — `if (prizePoolFrozen) revert E();` — VRF-window guard (read; no SSTORE).
2. **Line 327** — `uint256 amountWei = _consumeDecClaim(msg.sender, lvl);` — internal call. `_consumeDecClaim` (line 275) reads `decClaimRounds[lvl]` (line 279), reads `decBurn[lvl][player]` (line 282), reads `decBucketOffsetPacked[lvl]` (line 286), and writes `e.claimed = 1` (line 297) as the FINAL operation in its body. One-shot mutation committed before line 327 returns. No external call inside `_consumeDecClaim`.
3. **Line 329** — `if (gameOver) {` — branch guard (storage read).
4. **Line 330** — `_creditClaimable(msg.sender, amountWei);` — internal call to `DegenerusGamePayoutUtils._creditClaimable` (line 32). Writes `claimableWinnings[beneficiary] += weiAmount` (line 35). Emits `PlayerCredited` (line 37). No external call.
5. **Line 331** — `emit DecimatorClaimed(msg.sender, lvl, amountWei, amountWei, 0);` — the emit.
6. **Line 332** — `return;` — function exit.

**CEI verdict: SAFE.** All state mutations (`e.claimed = 1` at line 297; `claimableWinnings[msg.sender] += amountWei` at PayoutUtils:35) complete strictly BEFORE the emit at line 331. Zero statements between credit (line 330) and emit (line 331); the only thing after the emit is `return`. No external call anywhere in this branch — both helpers (`_consumeDecClaim`, `_creditClaimable`) execute in the current storage context with no `.call`/`.delegatecall`/`.transfer` operations. The `PlayerCredited` event emitted inside `_creditClaimable` fires BEFORE `DecimatorClaimed`, but both are post-mutation; the event ordering is informational, not CEI-relevant.

### Emit Site 2: `claimDecimatorJackpot` — normal ETH/lootbox split

Sequence through the non-gameOver branch (continuing from line 332 after the gameOver `return`):

1. **Line 335-339** — `uint256 lootboxPortion = _creditDecJackpotClaimCore(msg.sender, amountWei, decClaimRounds[lvl].rngWord);` — internal call. `_creditDecJackpotClaimCore` (line 376):
   - **Line 382** — `uint256 ethPortion = amount >> 1;` (local; no state mutation).
   - **Line 383** — `lootboxPortion = amount - ethPortion;` (local).
   - **Line 385** — `_creditClaimable(account, ethPortion);` — SSTORE: `claimableWinnings[account] += ethPortion`.
   - **Line 388** — `claimablePool -= uint128(lootboxPortion);` — SSTORE: `claimablePool` decremented.
   - **Line 389** — `_awardDecimatorLootbox(account, lootboxPortion, rngWord);` — internal call which may either (a) do `_queueTicketRange` + `_creditClaimable(remainder)` at lines 580-586 (SSTOREs), or (b) `delegatecall` to `GAME_LOOTBOX_MODULE` at lines 590-600 — this is the only EXTERNAL interaction in the entire `claimDecimatorJackpot` flow. After this call, `_creditDecJackpotClaimCore` returns.
2. **Line 340** — `if (lootboxPortion != 0) {` — branch guard.
3. **Line 341** — `_setFuturePrizePool(_getFuturePrizePool() + lootboxPortion);` — SSTORE: writes packed `prizePoolsPacked` future component (Storage:802-805). This is the FINAL state mutation in `claimDecimatorJackpot`.
4. **Line 342** — `}` (block close).
5. **Lines 343-349** — `emit DecimatorClaimed(msg.sender, lvl, amountWei, amountWei - lootboxPortion, lootboxPortion);` — the emit.
6. **Line 350** — `}` (function close; implicit return).

**CEI verdict: SAFE.** Zero statements between line 341 (final SSTORE) and line 343 (emit). No external call between `_setFuturePrizePool` and the emit — the only external interaction in the function is the lootbox `delegatecall` at line 590-600 inside `_awardDecimatorLootbox`, which executes BEFORE `_setFuturePrizePool`. CEI strictly preserved: all SSTOREs (`claimableWinnings`, `claimablePool`, packed prize pool, plus any whale-pass / lootbox state inside `_awardDecimatorLootbox`) and the only external interaction (lootbox delegatecall) complete BEFORE the emit. Reentrancy posture: even if the lootbox delegatecall could re-enter `claimDecimatorJackpot` (it is delegatecall, executes in the current contract's storage context — re-entrant call would land in the same `claimDecimatorJackpot` body), the `e.claimed = 1` set at line 297 inside the original `_consumeDecClaim` would cause the re-entrant `_consumeDecClaim` to revert at line 283 with `DecAlreadyClaimed`. No double-emit risk.

### Emit Site 3: `claimTerminalDecimatorJackpot` — terminal path

Function entry at line 811 (`function claimTerminalDecimatorJackpot() external` — zero args).

1. **Line 812** — `uint256 amountWei = _consumeTerminalDecClaim(msg.sender);` — internal call. `_consumeTerminalDecClaim` (line 854):
   - **Line 857** — `uint24 lvl = lastTerminalDecClaimRound.lvl;` (SLOAD).
   - **Line 858** — `if (lvl == 0) revert TerminalDecNotActive();`.
   - **Line 860** — `TerminalDecEntry storage e = terminalDecEntries[player];` (storage pointer).
   - **Line 861-862** — `if (e.burnLevel != uint48(lvl) || e.weightedBurn == 0) revert TerminalDecNotWinner();`.
   - **Line 865** — `uint88 weight = e.weightedBurn;` (SLOAD into local).
   - **Line 867** — `uint64 packedOffsets = decBucketOffsetPacked[lvl];` (SLOAD).
   - **Line 868** — `uint8 winningSub = _unpackDecWinningSubbucket(packedOffsets, e.bucket);` (pure).
   - **Line 869** — `if (e.subBucket != winningSub) revert TerminalDecNotWinner();`.
   - **Line 871** — `uint256 totalBurn = uint256(lastTerminalDecClaimRound.totalBurn);` (SLOAD).
   - **Line 872** — `if (totalBurn == 0) revert TerminalDecNotWinner();`.
   - **Lines 874-876** — `amountWei = (uint256(lastTerminalDecClaimRound.poolWei) * uint256(weight)) / totalBurn;` (compute).
   - **Line 877** — `if (amountWei == 0) revert TerminalDecNotWinner();`.
   - **Line 880** — `e.weightedBurn = 0;` — SSTORE: the one-shot consume (any subsequent `_consumeTerminalDecClaim` call will revert at line 861-862 because `e.weightedBurn == 0`). This is the FINAL operation; the function returns immediately after.
2. **Line 813** — blank line.
3. **Line 814** — `_creditClaimable(msg.sender, amountWei);` — SSTORE: `claimableWinnings[msg.sender] += amountWei`.
4. **Lines 815-819** — `emit TerminalDecimatorClaimed(msg.sender, lastTerminalDecClaimRound.lvl, amountWei);` — the emit (re-SLOADs `lastTerminalDecClaimRound.lvl` at the emit site; since the consume only zeroed `e.weightedBurn` and not `lastTerminalDecClaimRound.lvl`, the re-SLOAD returns the same value the consume validated against at line 857).
5. **Line 820** — `}` (function close).

**CEI verdict: SAFE.** Three-hop ordering: consume (`e.weightedBurn = 0` at line 880) → credit (`claimableWinnings += amountWei` at PayoutUtils:35) → emit (line 815). Zero statements between credit (line 814) and emit (line 815, opening of multi-line). No external call anywhere in the function body — both helpers (`_consumeTerminalDecClaim`, `_creditClaimable`) execute in the current storage context with no `.call`/`.delegatecall`/`.transfer`. CEI strictly preserved. Reentrancy posture: the only state mutation made claimable is `e.weightedBurn = 0`, which makes subsequent re-entrant `_consumeTerminalDecClaim` calls revert at line 861-862. The re-SLOAD of `lastTerminalDecClaimRound.lvl` at line 817 is gas-suboptimal vs. caching the line-857 read into a local, but it is not a correctness concern — no path between line 857 and line 817 can mutate `lastTerminalDecClaimRound.lvl` (only `runTerminalDecimatorJackpot` writes that field at line 798, and it has Game-only entry-guard at line 760). Recorded in Scope-guard Deferrals as informational.

## Event-Argument Correctness Analysis (per D-09)

### `DecimatorClaimed(player, lvl, amountWei, ethPortion, lootboxPortion)`

Per-argument source trace at both emit sites:

- **`player` (indexed) == `msg.sender`:** Both emit sites pass the EVM literal `msg.sender` (line 331 single-line emit; line 344 multi-line emit). No local alias `address sender = msg.sender` exists in the function body (verified by reading lines 321-350). Solidity's `msg.sender` is the immediate caller; for `external` functions reached via the Game wrapper's delegatecall (IM-07 chain `DegenerusGame.claimDecimatorJackpot` wrapper → module via `delegatecall`), `msg.sender` is preserved by `delegatecall` semantics — it remains the original EOA / contract that called the Game wrapper. `player == msg.sender` is the original claimer, NOT the Game module address. SAFE.

- **`lvl` (indexed) == `claimDecimatorJackpot(lvl)` input arg:** Both emit sites pass the function's `uint24 lvl` parameter (line 321) unchanged. No local re-derivation (no `lvl = ...` assignment in the body). The same `lvl` is forwarded to `_consumeDecClaim(msg.sender, lvl)` at line 327 and used for `decClaimRounds[lvl].rngWord` at line 338. The lvl asserted by the player IS the lvl used for the consume + payout + emit. Per DCM-01 audit (`a7d497e7`), this `lvl` must equal the resolution level (= writer's `level()+1` at burn time = post-bump `lvl` at jackpot resolution); the emit faithfully reports whichever lvl the consume succeeded against. SAFE.

- **`amountWei` (data) == `_consumeDecClaim` return value:** Both emit sites pass the local `amountWei` declared at line 327 (return of `_consumeDecClaim`). The local is not modified between line 327 and the emit. `_consumeDecClaim` computes `amountWei = _decClaimableFromEntry(round.poolWei, totalBurn, e, packedOffsets)` at line 288-293 — the pro-rata payout. Identical value used for `_creditClaimable` and for the emit. SAFE.

- **`ethPortion + lootboxPortion == amountWei` invariant (gameOver site):** Args at line 331 are `(amountWei, 0)`. Sum: `amountWei + 0 == amountWei`. Algebraic identity, holds for all `amountWei`. NatSpec on the event (line 54) explicitly declares `lootboxPortion = 0` post-GAMEOVER; literal matches NatSpec. SAFE.

- **`ethPortion + lootboxPortion == amountWei` invariant (normal split site):** Args at lines 343-349 are `(amountWei - lootboxPortion, lootboxPortion)`. Sum: `(amountWei - lootboxPortion) + lootboxPortion == amountWei`. Algebraic identity. Underflow check: `lootboxPortion = amount - (amount >> 1)` from `_creditDecJackpotClaimCore` line 383 where `amount = amountWei`. For any non-negative `amountWei`, `(amount >> 1) ≤ amount / 2`, so `lootboxPortion = amount - (amount >> 1) ≥ ⌈amount/2⌉ > 0` (when `amount > 0`); also `lootboxPortion ≤ amount`. Therefore `amountWei - lootboxPortion = amount - (amount - (amount >> 1)) = amount >> 1 ≥ 0` — never underflows under Solidity ≥0.8 checked subtraction. The emit's `ethPortion = amountWei - lootboxPortion` exactly equals the `ethPortion = amount >> 1` already credited at PayoutUtils:35 inside `_creditDecJackpotClaimCore` line 385 — NOT a double-credit, just a re-derivation of the same value for emit purposes. SAFE.

### `TerminalDecimatorClaimed(player, lvl, amountWei)`

Per-argument source trace at the single emit site (lines 815-819):

- **`player` (indexed) == `msg.sender`:** Emit passes the EVM literal `msg.sender` (line 816). No local alias in the function body (verified by reading lines 811-820). For external calls via the `DegenerusGame.claimTerminalDecimatorJackpot` wrapper's `delegatecall` (IM-08 chain introduced by `858d83e4`), `msg.sender` is preserved by delegatecall semantics — it remains the original claimer EOA. Same argument shape as `_creditClaimable(msg.sender, amountWei)` at line 814 — the player credited IS the player emitted. SAFE.

- **`lvl` (indexed) == `lastTerminalDecClaimRound.lvl` (storage-sourced, NOT caller-controlled):** Emit reads `lastTerminalDecClaimRound.lvl` directly at line 817 — a storage SLOAD. The function takes ZERO arguments at line 811 (`function claimTerminalDecimatorJackpot() external` — empty parameter list), so caller-controlled `lvl` injection is impossible. Canonical SSTORE site for `lastTerminalDecClaimRound.lvl`: `runTerminalDecimatorJackpot` at line 798 (`lastTerminalDecClaimRound.lvl = lvl;`). `runTerminalDecimatorJackpot` is gated by Game-only entry-guard at line 760 (`if (msg.sender != ContractAddresses.GAME) revert OnlyGame();`), so the only writer is the Game contract itself via the IM-08 / IM-07 self-call chain — specifically `GameOverModule.handleGameOverDrain` which passes `lvl = level` (terminal jackpot lvl is the game-level at gameover time). Player-controlled paths cannot poison this storage slot. The `_consumeTerminalDecClaim` helper at line 857 also SLOADs `lastTerminalDecClaimRound.lvl` for its own validation (`uint24 lvl = lastTerminalDecClaimRound.lvl;`), so the emit's lvl is consistent with the lvl that was used to gate the consume at line 861-862. Caller-controlled key-space drift impossible. SAFE.

- **`amountWei` (data) == consumed claim return value:** Emit passes the local `amountWei` declared at line 812 (`uint256 amountWei = _consumeTerminalDecClaim(msg.sender);`). The local is not modified between line 812 and line 818. `_consumeTerminalDecClaim` computes `amountWei = (lastTerminalDecClaimRound.poolWei * weight) / totalBurn` at lines 874-876 — the algebraically consumed amount. Identical value used for `_creditClaimable(msg.sender, amountWei)` at line 814 and for the emit. SAFE.

## Indexer-Compatibility Observation (per D-10 — READ-only OBSERVATION)

v28.0 Phase 227 audited the event-processor surface at the v28.0-baseline commit. The Phase 227 deliverables (`227-01-EVENT-COVERAGE-MATRIX.md`, `227-02-EVENT-ARG-MAPPING.md`, `227-03-INDEXER-COMMENT-AUDIT.md`) catalog every event in the v28.0-baseline contract surface and verify per-`(contractFile, eventName)` indexer handler registration. Commit `67031e7d` (2026-04-16) postdates that baseline — the two new event signatures `DecimatorClaimed(address,uint24,uint256,uint256,uint256)` (topic0 = `keccak256("DecimatorClaimed(address,uint24,uint256,uint256,uint256)")`) and `TerminalDecimatorClaimed(address,uint24,uint256)` (topic0 = `keccak256("TerminalDecimatorClaimed(address,uint24,uint256)")`) are absent from Phase 227's event-coverage matrix. Verified by grep against `.planning/milestones/v28.0-phases/227-indexer-event-processing-correctness/*.md` — zero matches for either event name. The implication: the `database/` indexer's `HANDLER_REGISTRY` (`database/src/handlers/index.ts:227` per Phase 227-01 record) must register both signatures before the v29.0 deployment is observable off-chain; otherwise both events will fire on-chain (correctly, per the Per-Function Verdict Table above) but be classified as UNHANDLED by the indexer and silently dropped. Per v29.0 PROJECT.md scoping, the `database/` repo is OUT OF SCOPE this milestone — this observation routes either (a) to Phase 236 FIND-02 as a KNOWN-ISSUES candidate, or (b) to a future `database/` milestone. Zero `database/` writes attempted this phase. This is NOT a contract-side finding — the contracts emit correctly with all D-08 (CEI) and D-09 (argument correctness) invariants satisfied; the observation is about downstream ABI-registration on the indexer side.

## Scope-guard Deferrals

One informational observation surfaced during this audit that is in-scope-anchored but worth recording for traceability:

- **Gas-suboptimal re-SLOAD at emit site 3 (`claimTerminalDecimatorJackpot` line 817).** The emit at lines 815-819 re-SLOADs `lastTerminalDecClaimRound.lvl` even though `_consumeTerminalDecClaim` SLOADed the same field at line 857 to gate the consume. Caching the line-857 SLOAD into a local and re-using it at the emit would save one warm SSLOAD (~100 gas). This is NOT a correctness concern — no path between line 857 and line 817 can mutate `lastTerminalDecClaimRound.lvl` (only `runTerminalDecimatorJackpot` writes that field at line 798, and it has Game-only entry-guard) — so the re-SLOAD is guaranteed to return the same value. Recorded as informational only; not a candidate finding (per CONTEXT Out-of-Scope Reminders, gas-ceiling re-profile is not required and a single warm SSLOAD savings is below any reasonable INFO threshold). This row stays in Scope-guard Deferrals because gas-optimization is not in DCM-02 scope per D-04.

No other surfaces encountered. All anchors map to `230-01-DELTA-MAP.md` §4 Consumer Index DCM-02 rows. Phase 230 catalog NOT edited.

## Downstream Hand-offs

- **Phase 236 FIND-01 hand-off (D-13):** Zero VULNERABLE / zero DEFERRED row-level verdicts to classify. One SAFE-INFO row carries `Finding Candidate: Y` (the indexer-compat OBSERVATION row at the bottom of the verdict table) — this becomes an anchor for Phase 236 severity classification. Recommended Phase 236 disposition: surface as INFO-grade observation describing the v28.0 Phase 227 indexer event-processor scope gap; remediation belongs to a future `database/` milestone, not the contracts. Phase 236 FIND-01 owns canonical finding-ID assignment.
- **Phase 236 FIND-02 hand-off (D-10):** The indexer-compat OBSERVATION (recorded above per D-10 as READ-only) routes to Phase 236 FIND-02 as a candidate KNOWN-ISSUES.md entry. Recommended classification: design-decision / cross-repo-dependency, cross-referenced by future finding-ID assignment back to this AUDIT row. Wording precedent: v28.0 Phase 229 KNOWN-ISSUES entries on indexer-side handler-registration gaps.
- **Future `database/` milestone hand-off:** Event-processor registration for `DecimatorClaimed` (5-arg signature) and `TerminalDecimatorClaimed` (3-arg signature) is a downstream indexer task — handler implementations to insert into `database/src/handlers/index.ts` `HANDLER_REGISTRY` map (per Phase 227-01 dispatch model). Database schema migrations may be needed to persist the new fields (`amountWei` / `ethPortion` / `lootboxPortion` for the in-flight event; `amountWei` for the terminal event). Recorded for traceability; no action this milestone, no contract change required.
- **Phase 232 DCM-01 cross-reference (intra-phase):** DCM-01 audit (`a7d497e7`) verified the `lvl` key-space alignment that the `DecimatorClaimed.lvl` indexed topic surfaces to indexers — writer key (`level()+1` at burn time, BurnieCoin:574) and reader key (`claimDecimatorJackpot(lvl)` input, DecimatorModule:321) are the same integer at every hop because `_finalizeRngRequest` synchronously bumps `level` (AdvanceModule:1514) at VRF request time. DCM-02 (this audit) confirms the emit faithfully reports that lvl. Combined: indexers can trust the `(player, lvl)` indexed topic pair as the canonical identifier for claim events.
- **Phase 232 DCM-03 cross-reference (intra-phase):** DCM-03 owns the new `DegenerusGame.claimTerminalDecimatorJackpot` external wrapper (`858d83e4`) audit. DCM-02 (this audit) confirmed the underlying `DegenerusGameDecimatorModule.claimTerminalDecimatorJackpot()` (line 811) reads `lastTerminalDecClaimRound.lvl` from storage at the emit site (line 817) and at the consume site (line 857) — eliminating any caller-passed `lvl` injection risk on the terminal claim event. DCM-03 audits the wrapper-side pass-through correctness; the module-side emit-event-arg correctness is fully owned by DCM-02 here.

---
*Phase: 232-decimator-audit*
*Plan: 232-02 (DCM-02)*
*Audit completed: 2026-04-18*
