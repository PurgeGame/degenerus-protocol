# DCM-03 Adversarial Audit: Terminal Decimator Claim Passthrough

**Scope:** Commit `858d83e4` ("feat(game): expose `claimTerminalDecimatorJackpot` passthrough") — `DegenerusGame.claimTerminalDecimatorJackpot` external wrapper (NEW, §1.6) + `IDegenerusGame.claimTerminalDecimatorJackpot` interface declaration (NEW, §1.10 / §3.1 ID-30) + IM-08 delegatecall chain (§2.2) audited end-to-end against the pre-existing `IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot` (§3.3.d ID-93) selector target
**Method:** Fresh read from HEAD source (per D-03) — no reuse of v25.0 Phase 214 or v27.0 Phase 220/223 verdicts as pre-approved; wrapper + interface + module passthrough audited independently. Pre-fix vs post-fix comparison via `git show 858d83e4^:...`: wrapper and `IDegenerusGame.claimTerminalDecimatorJackpot` declaration BOTH absent pre-fix; module body (`DecimatorModule:811`) and sub-interface (`IDegenerusGameModules:176`) BOTH already present pre-fix. The 858d83e4 diff is purely additive on the wrapper-side (15 insertions / 0 deletions on `DegenerusGame.sol`; 4 insertions / 0 deletions on `IDegenerusGame.sol`); the callee was already in place.
**Scope source:** `230-01-DELTA-MAP.md` §4 Consumer Index row DCM-03 (per D-04)
**Verdict vocabulary:** `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (per D-02)
**Finding-ID policy:** No finding IDs emitted (per D-13) — `Finding Candidate: Y/N` column only; Phase 236 FIND-01 owns canonical ID assignment.
**Delegatecall corroboration:** `make check-delegatecall` 44/44 PASS at HEAD (§3.5); IM-08 is the +1 site vs v27.0 Phase 220 baseline of 43 (Phase 230 Known Non-Issue #4). Cited as evidence per D-12, NOT as a finding.
**HEAD:** `7859b802` (`git rev-parse HEAD` at audit start)
**Owning SHA verified:** `git show 858d83e4 --stat` confirms 2-file diff: `contracts/DegenerusGame.sol` (+15 / -0) and `contracts/interfaces/IDegenerusGame.sol` (+4 / -0). Plan-level reference of "13 insertions / 9 deletions on `DegenerusGame.sol`" reflected an aggregated `git diff 14cb45e1..HEAD` view that mixes f20a2b5e's earlybird-block removal with 858d83e4's wrapper addition; the per-commit `git show 858d83e4` view is purely additive (+15 / +4 / 0 deletions). DCM-03 audit scope is the per-commit additive surface, not the aggregated dual-authored file delta.
**Module/sub-interface scope boundary:** The pre-existing `DegenerusGameDecimatorModule.claimTerminalDecimatorJackpot` body (`DecimatorModule:811-820`) and sub-interface (`IDegenerusGameModules:176`) are NOT touched by 858d83e4. They are nonetheless in DCM-03 scope per D-04 / §4 Consumer Index because the IM-08 delegatecall chain audit must verify lockstep between the new wrapper and the pre-existing callee. The 67031e7d emit insertion at `DecimatorModule:815-819` is DCM-02 territory; DCM-03 cites the emit only as evidence of post-mutation CEI position, not as a vector under audit.

## Methodology

Six attack vectors applied to the wrapper + interface + IM-08 chain per `232-CONTEXT.md` D-11 (caller restriction, reentrancy, parameter pass-through, privilege escalation) plus interface/implementer lockstep + delegatecall-site alignment corroboration per D-12:
- **(a) Caller restriction (D-11):** Confirm no `onlyX` modifier on the wrapper (intentional per ID-30 NatSpec — post-GAMEOVER player claim) and verify the module body internally gates on game-state (resolved terminal claim round; per-player one-shot consume). Missing wrapper-level guard is NOT a privilege escalation if the module-internal guards cover the privilege space.
- **(b) Reentrancy (D-11):** Trace CEI ordering inside the IM-08 chain. Verify the module body completes all state mutations (consume + credit) before any external interaction, and that the per-player claim is one-shot per terminal claim round (the consume invariant).
- **(c) Parameter pass-through (D-11):** Verify wrapper takes zero args, delegates with empty calldata; module also takes zero args; level is sourced from storage `lastTerminalDecClaimRound.lvl` (NOT from caller calldata). No caller-controlled parameter injection surface.
- **(d) Privilege escalation (D-11):** Verify delegatecall preserves `msg.sender` / `msg.value` / storage context; `_creditClaimable(msg.sender, ...)` credits the original caller; wrapper non-payable so `msg.value` injection is impossible at the wrapper boundary; no alternative credit-path inside the module body.
- **(e) Interface/implementer lockstep:** Verify `IDegenerusGame.claimTerminalDecimatorJackpot()` byte-matches `DegenerusGame.claimTerminalDecimatorJackpot()` (ID-30 PASS per §3.1) and `IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot()` byte-matches `DegenerusGameDecimatorModule.claimTerminalDecimatorJackpot()` (ID-93 PASS per §3.3.d). The wrapper's compile-time `IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot.selector` resolves to the parameterless ABI selector that the module function exposes.
- **(f) Delegatecall-site alignment corroboration (D-12):** `make check-delegatecall` PASS 44/44 at HEAD (§3.5); IM-08 is the +1 site vs v27.0 Phase 220 baseline of 43 (Phase 230 Known Non-Issue #4 already classifies this bump as legitimate new surface). Recorded as SAFE-INFO corroborating evidence, NOT a finding.

Verdict vocabulary locked to `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` per D-02. Columns locked to `Function | File:Line | Attack Vector | Verdict | Evidence | SHA | Finding Candidate` per D-02 + D-13.

**Pre-fix vs post-fix diff (verbatim from `git show 858d83e4 -- contracts/DegenerusGame.sol contracts/interfaces/IDegenerusGame.sol`):**

```
contracts/DegenerusGame.sol             | 15 +++++++++++++++
contracts/interfaces/IDegenerusGame.sol |  4 ++++
2 files changed, 19 insertions(+)
```

Two pure-addition hunks: (1) `DegenerusGame.sol:1266-1279` — the new external wrapper inserted directly after the pre-existing `claimDecimatorJackpot(uint24 lvl)` wrapper (sibling-positioned per §1.6); (2) `IDegenerusGame.sol:227-229` — the new interface declaration inserted directly after `claimDecimatorJackpot(uint24 lvl)` interface declaration (sibling-positioned per §1.10). Zero deletions, zero modifications of any pre-existing line. The module body (`DecimatorModule:811-820`) and sub-interface declaration (`IDegenerusGameModules:176`) were already present at the parent commit and remain unchanged by 858d83e4 — those side-rows in the verdict table are PASS by lockstep, not by addition.

**`grep -rn "claimTerminalDecimatorJackpot" contracts/` rollup:** 5 references in `contracts/`, exactly matching the plan's expectation:
- `contracts/DegenerusGame.sol:1268` — NEW wrapper (858d83e4)
- `contracts/DegenerusGame.sol:1274` — NEW selector reference inside the wrapper (858d83e4)
- `contracts/interfaces/IDegenerusGame.sol:229` — NEW interface declaration (858d83e4)
- `contracts/interfaces/IDegenerusGameModules.sol:176` — pre-existing sub-interface declaration
- `contracts/modules/DegenerusGameDecimatorModule.sol:811` — pre-existing module function body (modified body by 67031e7d adds emit; that is DCM-02 scope)

The Game wrapper is the SOLE caller of `IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot.selector` in the entire `contracts/` tree (expected per IM-08 being a NEW chain); no auxiliary caller exists.

## Findings-Candidate Block

No FAIL/VULNERABLE candidate findings. All 4 D-11 attack vectors verify SAFE on the 858d83e4 wrapper + IM-08 chain surface. Interface/implementer lockstep verifies SAFE for both ID-30 and ID-93 rows. Delegatecall-site alignment 44/44 corroborates IM-08 as the legitimate +1 site per D-12 (Phase 230 Known Non-Issue #4). Zero VULNERABLE, zero DEFERRED row-level verdicts. Zero SAFE-INFO rows carry `Finding Candidate: Y` — the delegatecall-site alignment corroboration row is `Finding Candidate: N` per D-12 (Phase 230 already classified the 43→44 bump as a known non-issue, so it is NOT a candidate finding).

**No candidate findings — all verdicts SAFE against 858d83e4 surface.**

Three scope-boundary hand-offs (Phase 236 FIND-01 ID assignment; Phase 232 DCM-02 event-side cross-reference; Phase 236 REG-01 sibling-wrapper precedent cross-reference) are documented in **Downstream Hand-offs** below — these are NOT findings, they are scope boundaries.

## Per-Function Verdict Table

| Function | File:Line | Attack Vector | Verdict | Evidence | SHA | Finding Candidate |
|---|---|---|---|---|---|---|
| `DegenerusGame.claimTerminalDecimatorJackpot` | contracts/DegenerusGame.sol:1268 | (a) Caller restriction — no `onlyX` modifier on wrapper (intentional per ID-30 NatSpec); module-internal game-state guards cover privilege-escalation space | SAFE | Wrapper at line 1268 has no modifier (signature: `function claimTerminalDecimatorJackpot() external {`) — verified by direct read; sibling `claimDecimatorJackpot(uint24 lvl)` at line 1252 also has no modifier (same wrapper-level pattern). NatSpec at lines 1266-1267 documents intent: "Only callable post-GAMEOVER. Level is read from the resolved claim round." Privilege space covered by module-internal guards: `_consumeTerminalDecClaim` at DecimatorModule:854 reverts (i) `TerminalDecNotActive` at line 858 if `lastTerminalDecClaimRound.lvl == 0` (no terminal round resolved — pre-GAMEOVER state OR gameover-without-terminal-burns); (ii) `TerminalDecNotWinner` at line 862 if `e.burnLevel != uint48(lvl)` or `e.weightedBurn == 0` (caller did not burn at the resolved terminal level OR has already claimed); (iii) `TerminalDecNotWinner` at line 869/872/877 if winning subbucket mismatch / no winners total / payout truncates to zero. The `lastTerminalDecClaimRound.lvl != 0` invariant gates the entire claim path: only `runTerminalDecimatorJackpot` at DecimatorModule:798 sets `lastTerminalDecClaimRound.lvl`, and that function has Game-only entry-guard at line 760 (`if (msg.sender != ContractAddresses.GAME) revert OnlyGame();`). The sole production caller of `runTerminalDecimatorJackpot` is `GameOverModule.handleGameOverDrain:162` (`grep -rn "runTerminalDecimatorJackpot" contracts/` returns exactly that one consumer-side caller). `gameOver = true` is set at GameOverModule:136 BEFORE the `runTerminalDecimatorJackpot` self-call at line 162, so `lastTerminalDecClaimRound.lvl != 0` is reachable ONLY in post-GAMEOVER state. Wrapper-level `onlyX` modifier would be redundant. | 858d83e4 | N |
| `DegenerusGame.claimTerminalDecimatorJackpot` | contracts/DegenerusGame.sol:1268-1279 | (b) Reentrancy — module body completes consume + credit before any external interaction; claim is one-shot per (player, terminal claim round) | SAFE | Wrapper executes one operation: `delegatecall` into `ContractAddresses.GAME_DECIMATOR_MODULE` (constant `0x15cF58144EF33af1e14b5208015d11F9143E27b9` per ContractAddresses:21-22); after delegatecall, `if (!ok) _revertDelegate(data)` (line 1278) is the only post-call code. Module body at DecimatorModule:811-820 executes in Game's storage context (delegatecall semantic): line 812 `uint256 amountWei = _consumeTerminalDecClaim(msg.sender)` — internal-private call (DecimatorModule:854 `private`) which performs all entry-guards then the FINAL state mutation `e.weightedBurn = 0` at line 880 BEFORE returning (the per-player one-shot consume — any re-entrant `_consumeTerminalDecClaim(msg.sender)` reverts at line 861-862 because `e.weightedBurn == 0`); line 814 `_creditClaimable(msg.sender, amountWei)` — internal-call to PayoutUtils:32 which performs `claimableWinnings[msg.sender] += amountWei` SSTORE (line 35) and emits `PlayerCredited` (line 37); lines 815-819 `emit TerminalDecimatorClaimed(...)` (DCM-02 scope — cited here only as evidence of post-mutation CEI position). NO `.call`/`.delegatecall`/`.transfer`/`.send` anywhere in `claimTerminalDecimatorJackpot`, `_consumeTerminalDecClaim`, or `_creditClaimable` — confirmed by direct read of all three function bodies. Therefore zero external-interaction surface inside the IM-08 chain post-mutation; reentrancy attack vectors (cross-function, single-function, read-only) all unreachable. The one-shot per (player, round) invariant is preserved by `e.weightedBurn = 0` at line 880; subsequent calls from the same `msg.sender` revert at the consume-side guard. | 858d83e4 | N |
| `DegenerusGame.claimTerminalDecimatorJackpot` | contracts/DegenerusGame.sol:1268-1278 | (c) Parameter pass-through — wrapper zero args; payload zero args; module zero args; `lvl` sourced from storage `lastTerminalDecClaimRound.lvl` set at terminal jackpot resolution | SAFE | Wrapper signature at line 1268: `function claimTerminalDecimatorJackpot() external {` — zero parameters. Payload construction at lines 1272-1276: `abi.encodeWithSelector(IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot.selector)` — zero args appended after the 4-byte selector; the encoded calldata is exactly `[selector(4 bytes)]`. Module signature at DecimatorModule:811: `function claimTerminalDecimatorJackpot() external {` — zero parameters. The `lvl` consumed by the module body is sourced from storage, NOT from caller calldata: `_consumeTerminalDecClaim` at DecimatorModule:857 reads `uint24 lvl = lastTerminalDecClaimRound.lvl;` (SLOAD); the emit at DecimatorModule:817 also re-SLOADs `lastTerminalDecClaimRound.lvl`. Canonical SSTORE site for `lastTerminalDecClaimRound.lvl`: `runTerminalDecimatorJackpot` at DecimatorModule:798 (`lastTerminalDecClaimRound.lvl = lvl;`), where the local `lvl` is the function parameter passed by the sole production caller `GameOverModule.handleGameOverDrain:162` as `lvl = level` (GameOverModule:82 SLOAD of game-level at gameover entry). `runTerminalDecimatorJackpot` is gated by Game-only entry-guard at DecimatorModule:760 (`if (msg.sender != ContractAddresses.GAME) revert OnlyGame();`), so player-controlled paths cannot poison this storage slot. Caller-controlled parameter injection surface size: ZERO bytes of calldata after the selector, ZERO storage slots reachable by the player to influence `lastTerminalDecClaimRound.lvl`. | 858d83e4 | N |
| `DegenerusGame.claimTerminalDecimatorJackpot` | contracts/DegenerusGame.sol:1268-1278 | (d) Privilege escalation — delegatecall preserves `msg.sender`, `msg.value`=0 (wrapper non-payable), storage context; `_creditClaimable(msg.sender, ...)` credits original caller | SAFE | (i) `msg.sender` preservation: Solidity `delegatecall` semantic preserves `msg.sender`; the EOA / contract that called `DegenerusGame.claimTerminalDecimatorJackpot()` is the `msg.sender` inside the module body. The module body uses `msg.sender` at three consumer sites: line 812 `_consumeTerminalDecClaim(msg.sender)` (the player whose entry is consumed), line 814 `_creditClaimable(msg.sender, amountWei)` (the player credited via `claimableWinnings[msg.sender] += amountWei` at PayoutUtils:35), line 816 `emit TerminalDecimatorClaimed(msg.sender, ...)` (DCM-02 emit; player faithfully reported). All three uses route to the original caller — never to the module's own address (`address(this)` would be the Game address under delegatecall semantics anyway, but the code never reads `address(this)` inside this path). (ii) `msg.value` injection: the wrapper at line 1268 has signature `function claimTerminalDecimatorJackpot() external {` — NOT `payable`. Confirmed by `grep -n "payable" contracts/DegenerusGame.sol | grep -i claim` returning zero hits inside `claimTerminalDecimatorJackpot`. Any attempt to send ETH at the wrapper boundary reverts with the EVM's automatic non-payable revert before reaching the delegatecall. Inside the module body `msg.value` is therefore guaranteed 0 (delegatecall preserves msg.value, but msg.value=0 at the wrapper entry); no code path reads `msg.value` in the IM-08 chain (verified by `grep -n msg.value` against `_consumeTerminalDecClaim`, `_creditClaimable`, `claimTerminalDecimatorJackpot` module body — zero hits). (iii) Storage-context preservation: delegatecall executes module bytecode against the Game contract's storage; the SSTORE at DecimatorModule:880 (`e.weightedBurn = 0`) and the SSTORE at PayoutUtils:35 (`claimableWinnings[beneficiary] += weiAmount`) both write to Game-storage slots — intent-correct (DegenerusGameDecimatorModule is a behavior-only contract, all decimator state lives in Game per the existing module-pattern design). (iv) No alternative credit path: `grep -nE "\.transfer\(|\.send\(|\.call\{|payable\(" contracts/modules/DegenerusGameDecimatorModule.sol` returns zero hits in `claimTerminalDecimatorJackpot` body or `_consumeTerminalDecClaim` body; the only credit operation is `_creditClaimable(msg.sender, amountWei)` at line 814; no `payable(addr).call`, no ETH transfer, no second `_creditClaimable` with a different recipient. | 858d83e4 | N |
| `IDegenerusGame.claimTerminalDecimatorJackpot` | contracts/interfaces/IDegenerusGame.sol:229 | (e) Interface/implementer lockstep (ID-30 PASS per §3.1) — signature byte-match with Game wrapper | SAFE | Interface declaration at line 229: `function claimTerminalDecimatorJackpot() external;` — zero parameters, no return values, no modifiers. Implementer at DegenerusGame.sol:1268: `function claimTerminalDecimatorJackpot() external {` — same signature byte-for-byte. Both introduced in lockstep by 858d83e4 per Phase 230 §3.1 ID-30 ("NEW in both interface and implementer (858d83e4) — introduced in lockstep. Signatures match."). NatSpec strings at IDegenerusGame.sol:227-228 and DegenerusGame.sol:1266-1267 are byte-identical: `/// @notice Claim terminal Decimator jackpot for caller.` and `/// @dev Only callable post-GAMEOVER. Level is read from the resolved claim round.` Automated `make check-interfaces` PASS at HEAD (§3.4 — exit 0, "all interface functions have matching implementations" across 17 interface/implementer pairs) corroborates mechanically. ABI selector consistency: both compile to selector `keccak256("claimTerminalDecimatorJackpot()")[0..4]` — identical 4-byte selector exposed on the Game contract's external ABI as ID-30. | 858d83e4 | N |
| `IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot` | contracts/interfaces/IDegenerusGameModules.sol:176 | Selector lockstep (ID-93 PASS per §3.3.d) — pre-existing sub-interface; wrapper's compile-time `.selector` reference resolves to the same 4-byte selector the module ABI exposes | SAFE | Sub-interface declaration at line 176: `function claimTerminalDecimatorJackpot() external;` — zero parameters; pre-existing per §3.3.d ID-93 (verified via `git show 858d83e4^:contracts/interfaces/IDegenerusGameModules.sol | grep -n claimTerminalDecimatorJackpot` returning the same line 176 — declaration was already present at the parent commit). Implementer at DecimatorModule:811: `function claimTerminalDecimatorJackpot() external {` — same signature byte-for-byte (also pre-existing per `git show 858d83e4^:contracts/modules/DegenerusGameDecimatorModule.sol`). Both functions compile to the same 4-byte selector `keccak256("claimTerminalDecimatorJackpot()")[0..4]`. The NEW wrapper at DegenerusGame.sol:1273-1276 references `IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot.selector` — Solidity compiler resolves this at compile-time to the parameterless selector, encoding it as the first 4 bytes of the delegatecall payload. Phase 230 §3.3.d ID-93 records this row PASS ("Body MODIFIED by 67031e7d (added `TerminalDecimatorClaimed` emission) but external signature unchanged. Paired with Game wrapper `claimTerminalDecimatorJackpot` (ID-30) introduced by 858d83e4 — interface/impl/wrapper all introduced or confirmed in lockstep."). Automated `make check-interfaces` PASS at HEAD (§3.4) corroborates the pre-existing module-side lockstep. | 858d83e4 | N |
| IM-08 delegatecall chain (§2.2) | contracts/DegenerusGame.sol:1268-1279 → contracts/modules/DegenerusGameDecimatorModule.sol:811-820 | End-to-end chain correctness — wrapper selector → standard `delegatecall` + `_revertDelegate` pattern → module body; `make check-delegatecall` 44/44 PASS at HEAD corroborates site alignment (Phase 230 Known Non-Issue #4) | SAFE-INFO | Chain anatomy at HEAD: Hop 1 — external caller invokes `DegenerusGame.claimTerminalDecimatorJackpot()` at the deployed Game address (selector `keccak256("claimTerminalDecimatorJackpot()")[0..4]`, ABI entry from ID-30). Hop 2 — wrapper at DegenerusGame.sol:1268-1279 builds payload `abi.encodeWithSelector(IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot.selector)` (zero args), invokes `ContractAddresses.GAME_DECIMATOR_MODULE.delegatecall(payload)`, captures `(bool ok, bytes memory data)`, forwards revert via `if (!ok) _revertDelegate(data)` (line 1278). Helper `_revertDelegate` at DegenerusGame.sol:987-992 is the standard memory-safe assembly revert-data forwarder used by every delegatecall wrapper in `DegenerusGame.sol` (15 sites confirmed via `grep -n "_revertDelegate" contracts/DegenerusGame.sol`); no raw `assembly { delegatecall ... }` at this site (uses high-level `.delegatecall(bytes)` builtin). Sibling pattern: `claimDecimatorJackpot(uint24 lvl)` wrapper at DegenerusGame.sol:1252-1264 uses identical structure (delegatecall to `GAME_DECIMATOR_MODULE` via `abi.encodeWithSelector(...selector, lvl)` + `if (!ok) _revertDelegate(data)`) — divergence: only the args (zero vs one). New wrapper mirrors established pattern; no novel construction. Hop 3 — module body at DecimatorModule:811-820 executes in Game storage context (consume → credit → emit). Return path — module returns no value; wrapper does not decode return data; any revert is forwarded by `_revertDelegate(data)` preserving the original revert reason (`TerminalDecNotActive` / `TerminalDecNotWinner` from the consume guards bubble up to the external caller). Corroboration: `make check-delegatecall` at HEAD returns exit 0 with "PASS 44/44 delegatecall sites aligned" (re-run during this audit; output tail captured: `[OK] contracts/modules/DegenerusGameLootboxModule.sol:960  IDegenerusGameBoonModule -> GAME_BOON_MODULE` followed by `PASS 44/44 delegatecall sites aligned`). Header observation: `interface <-> address map: 9 LIVE pair(s) validated, 1 known-dead constant(s) skipped` and `sites discovered: 44`. The +1 site (43→44) vs v27.0 Phase 220 baseline is attributable to IM-08 — Phase 230 Known Non-Issue #4 already classifies this as legitimate new-surface growth, NOT drift; SAFE-INFO classification carries Finding Candidate: N per D-12 (corroborating evidence, NOT a finding). | 858d83e4 | N |

**Row count:** 7 distinct `(target × attack vector)` rows. **Verdict tally:** 6 SAFE + 1 SAFE-INFO. Zero VULNERABLE, zero DEFERRED. Zero rows carry `Finding Candidate: Y` (the only SAFE-INFO row is the IM-08 chain corroboration, classified Finding Candidate: N per D-12).

## IM-08 Delegatecall Chain Analysis (end-to-end)

### Hop 1: External Caller → `DegenerusGame.claimTerminalDecimatorJackpot()`

Entry point is an EOA or contract calling `DegenerusGame`'s deployed address at the `claimTerminalDecimatorJackpot()` selector (`keccak256("claimTerminalDecimatorJackpot()")[0..4]`). Selector is exposed on the Game contract's external ABI through ID-30 (interface declaration at IDegenerusGame.sol:229 + implementer at DegenerusGame.sol:1268). Entry properties:
- **Non-payable.** Wrapper signature `function claimTerminalDecimatorJackpot() external {` at DegenerusGame.sol:1268 — no `payable` modifier. EVM rejects any ETH transfer with the automatic non-payable revert before the function body executes; `msg.value` is guaranteed 0 inside the wrapper.
- **No `onlyX` modifier.** Intentional per ID-30 NatSpec at DegenerusGame.sol:1266-1267 ("Only callable post-GAMEOVER. Level is read from the resolved claim round.") — callable by any address once the terminal claim round is resolved. Privilege space is fully covered by module-internal guards (see Hop 3); a wrapper-level modifier would be redundant.
- **No reentrancy guard.** Standard for delegatecall wrappers in this repo (sibling `claimDecimatorJackpot(uint24 lvl)` at DegenerusGame.sol:1252-1264 also has no guard); the IM-08 chain has no external-interaction surface (Hop 3 detail below) so a guard would be defending nothing.

### Hop 2: Wrapper Delegatecall Construction

Wrapper body at DegenerusGame.sol:1268-1279:

```solidity
function claimTerminalDecimatorJackpot() external {
    (bool ok, bytes memory data) = ContractAddresses
        .GAME_DECIMATOR_MODULE
        .delegatecall(
            abi.encodeWithSelector(
                IDegenerusGameDecimatorModule
                    .claimTerminalDecimatorJackpot
                    .selector
            )
        );
    if (!ok) _revertDelegate(data);
}
```

- **Target address.** `ContractAddresses.GAME_DECIMATOR_MODULE` resolves at compile time to the constant at ContractAddresses.sol:21-22 (`address(0x15cF58144EF33af1e14b5208015d11F9143E27b9)`) — the deployed `DegenerusGameDecimatorModule` contract. Same target used by 14 other delegatecall wrappers on Game (sibling `claimDecimatorJackpot` at line 1252-1264, `recordDecBurn` wrapper, `runDecimatorJackpot` wrapper, `consumeDecClaim` wrapper, `decClaimable` view wrapper, etc.). The `make check-delegatecall` 44/44 PASS at §3.5 verifies that the interface→address map for `IDegenerusGameDecimatorModule → GAME_DECIMATOR_MODULE` is consistently used across all 15 module-side wrappers (no mistargeted selectors).
- **Payload construction.** `abi.encodeWithSelector(IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot.selector)` — zero args appended after the 4-byte selector. Solidity compiler resolves the `.selector` member expression at compile-time to the parameterless selector matching the module function's signature (`claimTerminalDecimatorJackpot()`); the resulting calldata is exactly the 4-byte selector with no trailing data. Identical encoding shape to sibling `claimDecimatorJackpot(uint24 lvl)` wrapper at line 1256-1262 — only difference is the sibling wraps one `uint24 lvl` arg after the selector; the new wrapper has none (intentional — module function takes zero args).
- **Delegatecall invocation.** High-level `.delegatecall(bytes)` builtin (Solidity 0.8.34 — confirmed by pragma at top of `DegenerusGame.sol`); NOT raw `assembly { delegatecall ... }`. Captures `(bool ok, bytes memory data)`. Standard pattern across all 15 sites in `DegenerusGame.sol` (per `grep -n "_revertDelegate" contracts/DegenerusGame.sol` returning 15 hits, each preceded by a `delegatecall` site).
- **Revert forwarding.** `if (!ok) _revertDelegate(data)` at line 1278. Helper `_revertDelegate` at DegenerusGame.sol:987-992 is the canonical memory-safe revert-data forwarder:
  ```solidity
  function _revertDelegate(bytes memory reason) private pure {
      if (reason.length == 0) revert E();
      assembly ("memory-safe") {
          revert(add(32, reason), mload(reason))
      }
  }
  ```
  Forwards the module's revert data (e.g., `TerminalDecNotActive()` selector or `TerminalDecNotWinner()` selector — both 4-byte custom-error selectors at DecimatorModule:640-641) directly to the external caller without re-wrapping; preserves revert-reason traceability for off-chain debuggers and for try/catch consumers. If the module reverts with empty data (no revert reason), wrapper substitutes the default `E()` error to ensure the external caller always receives some revert payload.

The wrapper has NO post-call processing other than the conditional revert — no return value decoding, no state mutation, no event emission. Successful delegatecall returns are simply propagated as wrapper returns (the wrapper has no return values either, matching the module function's void return).

### Hop 3: `DegenerusGameDecimatorModule.claimTerminalDecimatorJackpot()` execution

Module body at DecimatorModule:811-820 (pre-existing function; body modified by 67031e7d to add the `emit TerminalDecimatorClaimed` — DCM-02 owns that side; DCM-03 cites it only as evidence of post-mutation CEI position):

```solidity
function claimTerminalDecimatorJackpot() external {
    uint256 amountWei = _consumeTerminalDecClaim(msg.sender);

    _creditClaimable(msg.sender, amountWei);
    emit TerminalDecimatorClaimed(
        msg.sender,
        lastTerminalDecClaimRound.lvl,
        amountWei
    );
}
```

Executes in Game's storage context (delegatecall semantic). Walk:

- **(i) Entry guards (inside `_consumeTerminalDecClaim`).** Function called as `_consumeTerminalDecClaim(msg.sender)` at line 812; private helper at DecimatorModule:854-881:
    - Line 857: `uint24 lvl = lastTerminalDecClaimRound.lvl;` (SLOAD of the resolved terminal claim round level).
    - Line 858: `if (lvl == 0) revert TerminalDecNotActive();` — gates entire claim path on a resolved terminal round. Covers (a) pre-GAMEOVER state (no `runTerminalDecimatorJackpot` ever ran, slot is default 0); (b) post-GAMEOVER without terminal burns (`runTerminalDecimatorJackpot` returned early at line 790-791 with `totalWinnerBurn == 0` without setting `lastTerminalDecClaimRound.lvl`).
    - Line 860-862: `TerminalDecEntry storage e = terminalDecEntries[player]; if (e.burnLevel != uint48(lvl) || e.weightedBurn == 0) revert TerminalDecNotWinner();` — gates per-player: caller must have a terminal-decimator burn entry stamped at the resolved level (`e.burnLevel == uint48(lvl)`) AND must not have already claimed (`e.weightedBurn != 0`; consume sets `e.weightedBurn = 0` at line 880 as the one-shot flag).
    - Line 865: `uint88 weight = e.weightedBurn;` (SLOAD into local for arithmetic).
    - Line 867-869: SLOAD `decBucketOffsetPacked[lvl]`, derive `winningSub = _unpackDecWinningSubbucket(packedOffsets, e.bucket)`, revert `TerminalDecNotWinner` if `e.subBucket != winningSub` — gates: caller's bucket/subbucket must match the winning subbucket selected by the VRF-derived seed at jackpot resolution time (`runTerminalDecimatorJackpot:771-788`).
    - Line 871-872: SLOAD `lastTerminalDecClaimRound.totalBurn`; revert `TerminalDecNotWinner` if zero (defense-in-depth — should be unreachable since `runTerminalDecimatorJackpot:790-791` early-returns when `totalWinnerBurn == 0` without setting `lvl`).
    - Lines 874-877: compute `amountWei = (lastTerminalDecClaimRound.poolWei * weight) / totalBurn`; revert `TerminalDecNotWinner` if zero (defense-in-depth — pro-rata truncation case).

- **(ii) State mutation — one-shot consume.** Line 880: `e.weightedBurn = 0;` — the ONE state mutation in `_consumeTerminalDecClaim`. SSTORE to `terminalDecEntries[msg.sender].weightedBurn`. From this point, any subsequent call to `claimTerminalDecimatorJackpot()` from the same `msg.sender` reverts at line 861-862 with `TerminalDecNotWinner` (because `e.weightedBurn == 0`). Per-(player, terminal claim round) one-shot invariant established BEFORE control returns to the caller.

- **(iii) Credit application.** Back in `claimTerminalDecimatorJackpot` body at DecimatorModule:814: `_creditClaimable(msg.sender, amountWei);` — internal call to PayoutUtils:32-38. Body:
    ```solidity
    function _creditClaimable(address beneficiary, uint256 weiAmount) internal {
        if (weiAmount == 0) return;
        unchecked {
            claimableWinnings[beneficiary] += weiAmount;
        }
        emit PlayerCredited(beneficiary, beneficiary, weiAmount);
    }
    ```
  SSTORE to `claimableWinnings[msg.sender]` (Game-storage slot under delegatecall semantic) plus `PlayerCredited(msg.sender, msg.sender, amountWei)` event. No external call — purely an SSTORE + event. The `unchecked` block is safe because `amountWei` is bounded by `lastTerminalDecClaimRound.poolWei * weight / totalBurn` where `poolWei ≤ uint96.max` and the running total over all winners equals `poolWei` (no over-credit possible across the full claim cohort). `_creditClaimable` is `internal` and called only after the consume's SSTORE — no reordering possible inside the function body.

- **(iv) Event emission.** Lines 815-819: `emit TerminalDecimatorClaimed(msg.sender, lastTerminalDecClaimRound.lvl, amountWei);` — DCM-02 territory (audited and verified SAFE in `232-02-AUDIT.md` lines 50, 116, 138-142). DCM-03 cites this as evidence of post-mutation CEI position: emit fires AFTER the consume's `e.weightedBurn = 0` SSTORE AND AFTER the credit's `claimableWinnings += amountWei` SSTORE. No state mutation between credit and emit; no external call between credit and emit.

- **(v) No external interaction.** Confirmed by direct read + grep:
    - `grep -nE "\.transfer\(|\.send\(|\.call\{|payable\(" contracts/modules/DegenerusGameDecimatorModule.sol` returns ZERO hits inside `claimTerminalDecimatorJackpot` (lines 811-820), `_consumeTerminalDecClaim` (lines 854-881), or any helper called transitively (`_unpackDecWinningSubbucket` is pure).
    - `grep -nE "delegatecall|staticcall" contracts/modules/DegenerusGameDecimatorModule.sol` returns one hit at line 594 (`IDegenerusGameLootboxModule -> GAME_LOOTBOX_MODULE` inside `_awardDecimatorLootbox`, which is in the in-flight `claimDecimatorJackpot` normal-split path — NOT in the terminal claim path; verified via `make check-delegatecall` output line `[OK] contracts/modules/DegenerusGameDecimatorModule.sol:594  IDegenerusGameLootboxModule -> GAME_LOOTBOX_MODULE`).
    - PayoutUtils:32-38 `_creditClaimable` makes no external call (verified — pure SSTORE + emit).

  Therefore the IM-08 chain has ZERO external-interaction surface post-mutation; reentrancy attack vectors are unreachable by construction.

### Return Path

Module function returns no value (signature: `function claimTerminalDecimatorJackpot() external {` — no return params). Wrapper at DegenerusGame.sol:1268-1279 does not decode return data (`(bool ok, bytes memory data) = ...delegatecall(...)`; `data` is read only inside the `if (!ok)` revert-forwarding branch). On success (`ok == true`), wrapper falls through to the implicit return — no post-call action.

On revert, the module raises one of:
- `TerminalDecNotActive()` (custom error selector at DecimatorModule:640) — when `lastTerminalDecClaimRound.lvl == 0` (no terminal round resolved yet).
- `TerminalDecNotWinner()` (custom error selector at DecimatorModule:641) — when caller is not a winner (wrong bucket, no burns, already claimed, or zero pro-rata payout).

The custom-error selector + ABI-encoded args (none for these 2 errors) lands in `data` from the delegatecall return; wrapper invokes `_revertDelegate(data)` at line 1278 which uses memory-safe assembly to `revert(add(32, reason), mload(reason))` — preserves the original revert payload byte-for-byte. External caller sees the same revert selector they would see if calling the module directly. Try/catch consumers can match on `TerminalDecNotActive` / `TerminalDecNotWinner` selectors for graceful UI fallback.

If the module reverts with empty revert data (which `claimTerminalDecimatorJackpot` body cannot produce — every revert site uses a custom error with a 4-byte selector), `_revertDelegate` at DegenerusGame.sol:988 falls back to `revert E()` (the wrapper's default error). This is defense-in-depth; not reachable via 858d83e4-introduced surface.

## Delegatecall-Site Alignment Corroboration (per D-12)

Per §3.5 of `230-01-DELTA-MAP.md`: `make check-delegatecall` returns exit 0 with output `PASS 44/44 delegatecall sites aligned`. Header observation: `interface <-> address map: 9 LIVE pair(s) validated, 1 known-dead constant(s) skipped` and `sites discovered: 44`. The site count at HEAD is 44, up from the v27.0 Phase 220 baseline of 43.

The +1 site delta is fully attributable to IM-08 — the NEW `DegenerusGame.claimTerminalDecimatorJackpot` wrapper introduced by 858d83e4. Re-running `make check-delegatecall` during this audit confirms 44/44 PASS at HEAD `7859b802` (output tail: `[OK] contracts/modules/DegenerusGameLootboxModule.sol:960  IDegenerusGameBoonModule -> GAME_BOON_MODULE` followed by `PASS 44/44 delegatecall sites aligned`). The interface→address map for `IDegenerusGameDecimatorModule → GAME_DECIMATOR_MODULE` is verified consistently across all 15 module-side wrappers on `DegenerusGame.sol` (sibling `claimDecimatorJackpot` wrapper at line 1252-1264 uses the same selector→address binding); no mistargeted selectors anywhere in the delegatecall surface.

Phase 230 Known Non-Issue #4 explicitly classifies the 43→44 bump as legitimate new-surface growth, NOT drift: "The plan `<interfaces>` block referenced 43 delegatecall sites (v27.0 Phase 220 count); the HEAD count is 44. Delta: +1 from `DegenerusGame.claimTerminalDecimatorJackpot` (858d83e4 = IM-08). Documented inline in §3.5. All 44 PASS the alignment gate." Per D-12, this is recorded in the Per-Function Verdict Table as SAFE-INFO with `Finding Candidate: N` — corroborating evidence for IM-08 chain correctness, NOT a standalone finding (Phase 230 already adjudicated the bump).

The check-delegatecall gate corroborates two independent IM-08 invariants:
1. **Selector resolution alignment.** The `IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot.selector` reference at DegenerusGame.sol:1273-1276 resolves to the same 4-byte selector exposed by `DegenerusGameDecimatorModule.claimTerminalDecimatorJackpot()` at DecimatorModule:811. If the wrapper had referenced a typo'd selector (e.g., `IDegenerusGameDecimatorModule.claimTerminalDecJackpot.selector`), check-delegatecall would have caught it as an unaligned site.
2. **Interface-to-address binding.** The `IDegenerusGameDecimatorModule` sub-interface is mapped to `GAME_DECIMATOR_MODULE` (the deployed `DegenerusGameDecimatorModule` address); the wrapper's `ContractAddresses.GAME_DECIMATOR_MODULE.delegatecall(...)` at line 1271 uses the consistent target. If the wrapper had referenced `GAME_JACKPOT_MODULE` or any other module address, the check-delegatecall gate would have flagged the interface↔address mismatch.

## Scope-guard Deferrals

None surfaced during this audit. All concerns encountered during the fresh read of `contracts/DegenerusGame.sol`, `contracts/interfaces/IDegenerusGame.sol`, `contracts/interfaces/IDegenerusGameModules.sol`, `contracts/modules/DegenerusGameDecimatorModule.sol`, and `contracts/modules/DegenerusGameGameOverModule.sol` map to `230-01-DELTA-MAP.md` §4 Consumer Index DCM-03 rows or to the Phase 236 / Phase 232 DCM-02 hand-offs documented below.

One observation about the audit narrative (recorded for traceability, NOT a deferral and NOT a finding):

- **Plan-level diff stat aggregation note.** The plan's `<read_first>` block describes the 858d83e4 diff as "13 insertions / 9 deletions on `DegenerusGame.sol` split between the wrapper addition and the f20a2b5e earlybird-block removal (dual-authored file)." This reflects the aggregated `git diff 14cb45e1..HEAD -- contracts/DegenerusGame.sol` view (which combines f20a2b5e's `recordMint` earlybird-block removal with 858d83e4's wrapper addition). The per-commit `git show 858d83e4 --stat` is purely additive: `+15 / -0` on `DegenerusGame.sol` (the wrapper) and `+4 / -0` on `IDegenerusGame.sol` (the interface declaration). DCM-03 audits the per-commit additive surface; the f20a2b5e half of the dual-authored aggregation is in EBD-01 scope (audited and shipped in Phase 231-01-AUDIT.md). No scope leakage; the two surfaces are cleanly separable per the per-commit diff.

## Downstream Hand-offs

- **Phase 236 FIND-01 hand-off (D-13):** Zero VULNERABLE / zero DEFERRED row-level verdicts to classify. Zero SAFE-INFO rows carry `Finding Candidate: Y` (the IM-08 chain corroboration row is `Finding Candidate: N` per D-12 — Phase 230 already classified the 43→44 bump as a known non-issue). DCM-03 contributes zero candidate findings to the Phase 236 FIND-01 pool. Recommended Phase 236 disposition: no per-row finding classification needed; cite this audit's clean SAFE verdict in the consolidated `audit/FINDINGS-v29.0.md` "no new findings" section for completeness.

- **Phase 232 DCM-02 cross-reference (intra-phase):** The `emit TerminalDecimatorClaimed(msg.sender, lastTerminalDecClaimRound.lvl, amountWei)` at DecimatorModule:815-819 is owned by DCM-02 (commit 67031e7d). DCM-03 cited it as evidence of post-mutation CEI position only — verifying that the IM-08 chain places the emit AFTER the consume + credit SSTOREs. DCM-02 (`232-02-AUDIT.md` lines 50, 116, 138-142) independently verified the emit's CEI position (SAFE) and event-argument correctness (`lvl` storage-sourced from `lastTerminalDecClaimRound.lvl`, `amountWei` from `_consumeTerminalDecClaim` return, `msg.sender` EVM literal). Combined: the IM-08 chain has CEI-correct ordering across both 858d83e4-side (wrapper + chain) and 67031e7d-side (emit). No cross-plan finding to consolidate; cross-reference exists for traceability only.

- **Phase 232 DCM-01 cross-reference (intra-phase):** DCM-01 audit (`232-01-AUDIT.md` lines 102-107, commit a7d497e7) verified the terminal decimator path's key-space alignment as INTENTIONALLY UNAFFECTED by the 3ad0f8d3 `+1` keying change — terminal path keys by `lvl = level` (no `+1`) because gameover has no level-bump between burn and resolution. DCM-03 inherits this conclusion: the `lastTerminalDecClaimRound.lvl` slot consumed by the IM-08 chain is set at `runTerminalDecimatorJackpot:798` from the gameover-time `level` SLOAD at `GameOverModule:82`, matching the writer-side terminal burn key. No cross-plan finding to consolidate.

- **Phase 235 RNG-01 hand-off:** N/A — the terminal CLAIM path consumes ZERO new RNG entropy. The terminal jackpot RNG (`rngWord` at `runTerminalDecimatorJackpot:758`) was committed upstream in `GameOverModule.handleGameOverDrain` at line 95-99 (`rngWord = rngWordByDay[day]`) and consumed at jackpot RESOLUTION time (DecimatorModule:771-788 winning-subbucket selection). By claim time the RNG has already been baked into `decBucketOffsetPacked[lvl]` storage at DecimatorModule:795. The wrapper introduces no new RNG consumer; `claimTerminalDecimatorJackpot()` reads only resolved state (`lastTerminalDecClaimRound.*` slots, `terminalDecEntries[player]`, `decBucketOffsetPacked[lvl]`). Recorded as N/A — no Phase 235 RNG-01 backward-trace surface introduced by 858d83e4.

- **Phase 236 REG-01 hand-off (sibling-wrapper precedent):** The pre-existing sibling `claimDecimatorJackpot(uint24 lvl)` wrapper at DegenerusGame.sol:1252-1264 is the precedent the new wrapper mirrors (selector encoding, `_revertDelegate` revert forwarding, no modifier, non-payable). If any v27.0 INFO finding or `audit/KNOWN-ISSUES.md` entry touched the sibling wrapper pattern, Phase 236 REG-01 should re-verify those conclusions still hold for the new IM-08 wrapper. Anchor: `230-01-DELTA-MAP.md` §4 Consumer Index REG-01 row + the v27.0 audit `audit/FINDINGS-v27.0.md` decimator-related rows. Recommended Phase 236 disposition: re-grep v27.0 findings for `claimDecimatorJackpot` / `decimator wrapper` / `delegatecall wrapper` references; cite identical SAFE conclusion for the new wrapper if the sibling pattern conclusions held.

- **Future indexer hand-off (cross-repo, OUT OF SCOPE this milestone):** Off-chain indexers tracking `TerminalDecimatorClaimed` events emitted via the IM-08 chain (DCM-02 emit, DCM-03 wrapper-side correctness) must register the `TerminalDecimatorClaimed(address,uint24,uint256)` event signature in the `database/` repo's indexer event-processor surface. DCM-02 (`232-02-AUDIT.md` Indexer-Compatibility Observation per D-10) already documented this as a cross-repo observation routed to Phase 236 FIND-02 KNOWN-ISSUES candidate. DCM-03 confirms wrapper-side selector ABI consistency (ID-30 PASS) so the indexer's signature-match logic will correctly bind the emitted topic0 to the registered handler once the handler is added. No `database/` writes attempted this audit (per v29.0 PROJECT.md scoping).

---
*Phase: 232-decimator-audit*
*Plan: 232-03 (DCM-03)*
*Audit completed: 2026-04-18*
