---
phase: 232-decimator-audit
plan: 232-03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/232-decimator-audit/232-03-AUDIT.md
autonomous: true
requirements: [DCM-03]
must_haves:
  truths:
    - "`DegenerusGame.claimTerminalDecimatorJackpot` (NEW by 858d83e4, §1.6) has at least one verdict row per D-11 attack vector"
    - "`IDegenerusGame.claimTerminalDecimatorJackpot` interface declaration (NEW by 858d83e4, §1.10 / §3.1 ID-30) has a verdict row confirming interface/implementer lockstep"
    - "`IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot` module-interface row (§3.3.d ID-93) is verified as PASS (pre-existing selector + lockstep with the new wrapper)"
    - "IM-08 delegatecall chain (§2.2) is audited end-to-end: `DegenerusGame.claimTerminalDecimatorJackpot` → `DegenerusGameDecimatorModule.claimTerminalDecimatorJackpot` via `_delegateToModule` with `IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot.selector`"
    - "Caller restriction verdict recorded per D-11: no `onlyX` modifier on the wrapper (intentional — post-GAMEOVER player claim per ID-30 NatSpec); missing wrapper-level guard is NOT a privilege escalation because the module body internally gates on game-state"
    - "Reentrancy verdict recorded per D-11: module body completes all state mutations (consume + credit) before any external interaction; claim is one-shot per terminal claim round"
    - "Parameter pass-through verdict recorded per D-11: wrapper takes zero args, delegates empty calldata; module takes zero args (level sourced from storage `lastTerminalDecClaimRound.lvl`); no caller-controlled parameter injection possible"
    - "Privilege-escalation verdict recorded per D-11: delegatecall preserves `msg.sender` / `msg.value` / storage context; `_creditClaimable(msg.sender, ...)` credits the original caller, not the module address"
    - "check-delegatecall 44/44 PASS at HEAD is cited as corroborating evidence per D-12 (NOT as a finding — Phase 230 already documented the 43→44 bump as a known non-issue)"
    - "Every verdict row cites the 7-char commit SHA `858d83e4` (per D-03)"
    - "Every verdict row cites a File:Line anchor pointing into contracts/DegenerusGame.sol, contracts/interfaces/IDegenerusGame.sol, contracts/interfaces/IDegenerusGameModules.sol, or contracts/modules/DegenerusGameDecimatorModule.sol at HEAD"
    - "Verdict vocabulary used is exactly `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (per D-02)"
    - "No F-29-NN finding IDs appear anywhere in 232-03-AUDIT.md (per D-13)"
    - "Every verdict row carries a `Finding Candidate: Y/N` column value (per D-13)"
    - "Zero contracts/ and zero test/ writes occur during plan execution (READ-only per v29.0 milestone rule)"
  artifacts:
    - path: ".planning/phases/232-decimator-audit/232-03-AUDIT.md"
      provides: "DCM-03 claimTerminalDecimatorJackpot passthrough adversarial audit"
      contains: "## Per-Function Verdict Table"
  key_links:
    - from: "230-01-DELTA-MAP.md §1.6 + §1.10 + §3.1 ID-30 + §3.3.d ID-93 + §2.2 IM-08 (858d83e4)"
      to: "232-03-AUDIT.md per-function verdict table rows"
      via: "Every 858d83e4 function + interface declaration has a verdict + IM-08 delegatecall chain audited end-to-end"
      pattern: "SAFE|SAFE-INFO|VULNERABLE|DEFERRED"
    - from: "230-01-DELTA-MAP.md §3.5 check-delegatecall 44/44 PASS"
      to: "232-03-AUDIT.md Delegatecall-Site Alignment Corroboration subsection"
      via: "IM-08 identified as the +1 site; alignment is corroborating evidence per D-12"
      pattern: "44/44"
    - from: "232-03-AUDIT.md VULNERABLE/DEFERRED verdicts"
      to: "Phase 236 FIND-01 finding-candidate pool"
      via: "Findings-Candidate Block (no F-29-NN IDs, Finding Candidate: Y/N column)"
      pattern: "## Findings-Candidate Block"
---

<objective>
DCM-03 adversarial audit of the terminal-decimator-claim passthrough owned by commit `858d83e4` ("feat(game): expose `claimTerminalDecimatorJackpot` passthrough"). Produces `232-03-AUDIT.md`: a per-function verdict table covering `DegenerusGame.claimTerminalDecimatorJackpot` (NEW external wrapper), `IDegenerusGame.claimTerminalDecimatorJackpot` (NEW interface declaration), the pre-existing `IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot` (ID-93, module interface that the wrapper delegatecalls into), and an end-to-end audit of IM-08 (the new cross-module delegatecall chain), plus Findings-Candidate / Scope-guard Deferrals / Downstream Hand-offs subsections.

Purpose: A new external wrapper on `DegenerusGame.sol` delegatecalls into the pre-existing `DegenerusGameDecimatorModule.claimTerminalDecimatorJackpot` function (itself modified by 67031e7d to emit `TerminalDecimatorClaimed` — DCM-02 owns that side). The wrapper selector-calls via `IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot.selector` and forwards revert data through `_revertDelegate(data)`. A C4A warden would probe: (a) Caller restriction — no `onlyX` modifier on the wrapper (intentional, per ID-30 NatSpec: post-GAMEOVER player claim); verify the module body internally gates on game-state (post-gameover, resolved terminal claim round exists) so the missing wrapper-level guard is NOT a privilege escalation, (b) Reentrancy — selector delegatecall executes in Game's storage context; verify module body completes all state mutations (consume + credit) before any external interaction, and that the claim is one-shot per terminal claim round (`_consumeTerminalDecClaim` prevents re-entry claiming twice), (c) Parameter pass-through — wrapper takes zero args and delegates with empty calldata; module function also takes zero args (level is read from storage `lastTerminalDecClaimRound.lvl`, not a caller argument); verify no caller-controlled parameter injection surface exists, (d) Privilege escalation — delegatecall preserves `msg.sender`, `msg.value`, storage context; verify `_creditClaimable(msg.sender, ...)` credits the original caller, not the module address or any elevated context (per D-11). The `make check-delegatecall` 44/44 PASS at HEAD is corroborating evidence per D-12 — the 43→44 site-count bump vs. v27.0 Phase 220 baseline is attributable to this NEW IM-08 site and was already documented in Phase 230 Known Non-Issue #4; recorded as evidence, NOT as a new finding. This plan surfaces adversarial verdicts against the D-02 column schema using the D-02 verdict vocabulary. Phase 236 FIND-01 owns canonical F-29-NN ID assignment (D-13).

Output: `232-03-AUDIT.md` with per-function verdict table + dedicated IM-08 Delegatecall Chain Analysis + Delegatecall-Site Alignment Corroboration subsection + Findings-Candidate / Scope-guard Deferrals / Downstream Hand-offs subsections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/232-decimator-audit/232-CONTEXT.md
@.planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md
@.planning/phases/230-delta-extraction-scope-map/230-01-SUMMARY.md
@.planning/milestones/v25.0-phases/214-adversarial-audit/214-01-REENTRANCY-CEI.md
@.planning/phases/231-earlybird-jackpot-audit/231-01-PLAN.md
@.planning/phases/231-earlybird-jackpot-audit/231-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract 858d83e4 diff + read Game + interfaces + module at HEAD to audit the IM-08 delegatecall chain end-to-end; write 232-03-AUDIT.md</name>
  <files>.planning/phases/232-decimator-audit/232-03-AUDIT.md</files>
  <read_first>
    - .planning/phases/232-decimator-audit/232-CONTEXT.md (D-01 through D-14 — locked column schema D-02, SHA-citation rule D-03, scope anchor D-04, scope-guard deferral rule D-05, passthrough audit attack vectors D-11, delegatecall alignment corroboration rule D-12, finding-ID discipline D-13)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.6 (lines 146-155 — DegenerusGame.sol function-level changelog):
        * `claimTerminalDecimatorJackpot() external` NEW by `858d83e4`: "New external wrapper — delegatecalls into `ContractAddresses.GAME_DECIMATOR_MODULE` via `IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot.selector` and forwards revert data through `_revertDelegate(data)`. Sibling to the pre-existing `claimDecimatorJackpot(uint24 lvl)` wrapper; placed directly after it in source order."
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.10 (lines 190-198 — IDegenerusGame.sol interface-file changelog):
        * `claimTerminalDecimatorJackpot() external` NEW by `858d83e4`: "Interface-level declaration matching the new `DegenerusGame.claimTerminalDecimatorJackpot` wrapper. NatSpec notes post-GAMEOVER callability and that the level is read from the resolved claim round (not a caller arg)."
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §3.1 row ID-30 (line 333 — `IDegenerusGame.claimTerminalDecimatorJackpot` interface drift verdict): "PASS — NEW in both interface and implementer (858d83e4) — introduced in lockstep. Signatures match."
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §3.3.d row ID-93 (line 431 — `IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot` module-interface drift): "PASS — Body MODIFIED by 67031e7d (added `TerminalDecimatorClaimed` emission) but external signature unchanged. Paired with Game wrapper `claimTerminalDecimatorJackpot` (ID-30) introduced by 858d83e4 — interface/impl/wrapper all introduced or confirmed in lockstep."
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §2.2 row IM-08 (line 252 — the cross-module delegatecall chain): "DegenerusGame.claimTerminalDecimatorJackpot → DegenerusGameDecimatorModule.claimTerminalDecimatorJackpot via delegatecall (selector-call) | `858d83e4`, `67031e7d` | NEW external wrapper (858d83e4) delegatecalls the module via `IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot.selector`. Module-side callee was further MODIFIED (67031e7d) to emit `TerminalDecimatorClaimed`."
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §3.5 (lines 517-526 — `make check-delegatecall` PASS 44/44; header observation: "sites discovered: 44"; note: "the plan's `<interfaces>` block mentions '43 delegatecall sites' as the v27.0 Phase 220 count; the HEAD count at 44 reflects the new wrapper added by 858d83e4 (`DegenerusGame.claimTerminalDecimatorJackpot`, recorded as ID-30 in §3.1 and IM-08 in §2.2). All 44 are verified aligned.")
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §4 Consumer Index DCM-03 row (line 554 — authoritative scope anchor per D-04: "§1.6 (`DegenerusGame.claimTerminalDecimatorJackpot` NEW by 858d83e4), §1.10 (`IDegenerusGame.claimTerminalDecimatorJackpot` NEW interface decl), §3.1 ID-30, §3.3.d ID-93, §2.2 IM-08, commit `858d83e4` rows")
    - .planning/phases/230-delta-extraction-scope-map/230-01-SUMMARY.md "Known Non-Issues" #4: "The plan `<interfaces>` block referenced 43 delegatecall sites (v27.0 Phase 220 count); the HEAD count is 44. Delta: +1 from `DegenerusGame.claimTerminalDecimatorJackpot` (858d83e4 = IM-08). Documented inline in §3.5. All 44 PASS the alignment gate." — this is the D-12 corroboration anchor.
    - Output of `git show 858d83e4 --stat` and `git show 858d83e4 -- contracts/DegenerusGame.sol contracts/interfaces/IDegenerusGame.sol` to inspect the authored diff — the delta IS the audit surface. 13 insertions / 9 deletions on `DegenerusGame.sol` split between the wrapper addition and the f20a2b5e earlybird-block removal (dual-authored file); 4 insertions / 0 deletions on `IDegenerusGame.sol` is the interface declaration.
    - contracts/DegenerusGame.sol at HEAD — locate:
        * The new `claimTerminalDecimatorJackpot()` external wrapper (sibling to `claimDecimatorJackpot(uint24 lvl)` per §1.6). Record exact File:Line of the function declaration, the delegatecall payload construction (`abi.encodeWithSelector(IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot.selector)` — note no args), the delegatecall target address (`ContractAddresses.GAME_DECIMATOR_MODULE` per §1.6), the `_revertDelegate(data)` call for revert forwarding, and any modifier on the wrapper (expected: none per D-11).
        * The pre-existing sibling `claimDecimatorJackpot(uint24 lvl)` wrapper at its File:Line — compare modifier-presence and delegatecall-construction style to the new wrapper. Any divergence from the established wrapper pattern is a finding candidate.
        * The `_delegateToModule` / `_revertDelegate` helper definitions (or whichever helper the wrapper uses) — cite File:Line to confirm the standard delegate pattern is used, not a raw `assembly { delegatecall ... }`.
    - contracts/interfaces/IDegenerusGame.sol at HEAD — locate the new `claimTerminalDecimatorJackpot()` external declaration. Record File:Line and the full NatSpec comment. Verify the declaration is parameterless and matches the wrapper signature byte-for-byte.
    - contracts/interfaces/IDegenerusGameModules.sol at HEAD — locate the pre-existing `claimTerminalDecimatorJackpot()` declaration inside `interface IDegenerusGameDecimatorModule` (§3.3.d ID-93). Record File:Line. Verify the selector computed via `IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot.selector` resolves to the parameterless signature.
    - contracts/modules/DegenerusGameDecimatorModule.sol at HEAD — locate `claimTerminalDecimatorJackpot()` at approximately line 811 (per §3.3.d row ID-93 File:Line note). For DCM-03 purposes — the module function body is audited ONLY from a passthrough-correctness perspective. Specifically record:
        * `msg.sender` consumers (every site that reads `msg.sender` — the IM-08 delegatecall preserves `msg.sender`, so the module sees the original claimer)
        * `_consumeTerminalDecClaim(...)` call site (state mutation that makes the claim one-shot; sets/reads `lastTerminalDecClaimRound.lvl`)
        * `_creditClaimable(msg.sender, amountWei)` call site (CEI state mutation; credits the original claimer via preserved msg.sender)
        * `emit TerminalDecimatorClaimed(...)` call site (DCM-02 owns this side — DCM-03 references only that the emit exists as evidence of post-mutation CEI)
        * Internal game-state guards: grep for `gameOver`, `terminalDec`, `lastTerminalDecClaimRound`, or equivalent — identify the branch that reverts if the caller attempts to claim (i) before gameOver, (ii) before a terminal decimator claim round has been resolved, (iii) after already claiming. Cite File:Line of each guard.
    - Also grep for any other caller of `IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot.selector` in the entire `contracts/` tree to confirm the Game wrapper is the ONLY caller site (expected per IM-08 being a new chain). Command: `grep -rn "claimTerminalDecimatorJackpot" contracts/ 2>/dev/null` — expected to find: (1) the new Game wrapper declaration, (2) the new Game wrapper body (delegatecall), (3) the NEW IDegenerusGame interface declaration, (4) the pre-existing IDegenerusGameDecimatorModule sub-interface declaration, (5) the pre-existing DegenerusGameDecimatorModule function body. Any additional reference is a finding candidate.
    - .planning/milestones/v25.0-phases/214-adversarial-audit/214-01-REENTRANCY-CEI.md (lines 1-60 — precedent CEI + delegatecall audit table shape; per D-03 do NOT carry forward v25.0 verdicts as pre-approved)
    - .planning/phases/231-earlybird-jackpot-audit/231-01-PLAN.md (precedent column schema + acceptance_criteria format)
    - .planning/phases/231-earlybird-jackpot-audit/231-02-PLAN.md (precedent — single-function focused audit structure analogous to 232-03's narrow wrapper-surface audit)
  </read_first>
  <action>
Execute a fresh-from-source adversarial audit of the `858d83e4` terminal-decimator passthrough per CONTEXT.md D-01 through D-14. Verdict vocabulary per D-02 is strictly `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`. Column schema per D-02 is strictly `Function | File:Line | Attack Vector | Verdict | Evidence | SHA` with an additional `Finding Candidate: Y/N` column per D-13.

**Step 1 — Build the scope list from `230-01-DELTA-MAP.md` §4 Consumer Index DCM-03 row (D-04).** The target set is:
  - **`DegenerusGame.claimTerminalDecimatorJackpot()`** (NEW by `858d83e4`, §1.6) — the external wrapper
  - **`IDegenerusGame.claimTerminalDecimatorJackpot()`** (NEW by `858d83e4`, §1.10 / §3.1 ID-30) — the interface declaration
  - **`IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot()`** (pre-existing, §3.3.d ID-93) — the module sub-interface selector target; included for lockstep verification
  - **IM-08 chain (§2.2)**: `DegenerusGame.claimTerminalDecimatorJackpot` → `DegenerusGameDecimatorModule.claimTerminalDecimatorJackpot` via delegatecall — audited end-to-end
  - **Corroborating evidence**: `make check-delegatecall` 44/44 PASS at HEAD (§3.5) — IM-08 is the +1 site; documented as evidence per D-12, NOT as a finding (Phase 230 Known Non-Issue #4 already classifies it).

**Step 2 — Run the D-11 attack vectors:**
  - **(a) Caller restriction (no `onlyX` modifier on the wrapper is intentional):** Read the wrapper at HEAD. Confirm no modifier gates access. Per ID-30 NatSpec, this is intentional (post-GAMEOVER player claim). Now audit the module body (`DegenerusGameDecimatorModule.claimTerminalDecimatorJackpot` at ~line 811) for internal game-state gating:
      - (i) Identify every early-revert / require guard that blocks calls before gameOver (grep for `gameOver`, `require` statements).
      - (ii) Identify every guard that requires a resolved terminal decimator claim round (grep for `lastTerminalDecClaimRound`, `terminalDec`, and the `_consumeTerminalDecClaim` invariant).
      - (iii) Identify the one-shot guard — the first call succeeds and consumes the claim; subsequent calls from the same msg.sender revert. Confirm `_consumeTerminalDecClaim` handles this (likely via per-player SSTORE or equivalent).
      - Verdict: SAFE iff the module-internal game-state guards cover all privilege-escalation vectors that a wrapper-level `onlyX` would have blocked. If any guard is missing — VULNERABLE. If a guard exists but is weaker than expected — DEFERRED (scope-guard to Phase 236 FIND-01 with evidence).
  - **(b) Reentrancy:** Audit the CEI ordering inside the module function body (DCM-02 handles the event side — DCM-03 focuses on claim-consumption ordering):
      - (i) Identify `_consumeTerminalDecClaim(...)` site — state mutation that makes the claim one-shot per round.
      - (ii) Identify `_creditClaimable(msg.sender, amountWei)` — the actual credit. Verify it follows `_consumeTerminalDecClaim` in source order.
      - (iii) Identify any external call inside the module body (excluding the emit, which doesn't re-enter).
      - (iv) Note: `_creditClaimable` itself may be an internal storage write OR may make an external transfer. Read its definition to confirm. If internal-only SSTORE → no reentrancy surface. If external transfer → CEI must be verified: is `_consumeTerminalDecClaim` done BEFORE `_creditClaimable`?
      - (v) Confirm the claim is one-shot per terminal claim round — i.e., once `_consumeTerminalDecClaim` runs for a (player, round) pair, the next call from the same player on the same round reverts. Cite the storage pattern.
      - Verdict: SAFE iff all state mutations (consume + credit) complete before any external interaction and claim is one-shot. Otherwise VULNERABLE or DEFERRED with File:Line.
  - **(c) Parameter pass-through:** Audit the wrapper payload construction:
      - (i) Wrapper signature: `function claimTerminalDecimatorJackpot() external` — zero args. Confirm at File:Line.
      - (ii) Payload: `abi.encodeWithSelector(IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot.selector)` — no additional args appended. Confirm at File:Line.
      - (iii) Module signature: `function claimTerminalDecimatorJackpot() external` — zero args. Confirm at File:Line.
      - (iv) The `lvl` used by the module body is sourced from storage `lastTerminalDecClaimRound.lvl` — NOT from calldata. Find the SSTORE that sets `lastTerminalDecClaimRound.lvl` (expected: in `runTerminalDecimatorJackpot` during jackpot resolution) to confirm the level is determined at resolution time, not claim time.
      - (v) Verify no caller-controlled parameter injection surface exists: neither msg.data (beyond the 4-byte selector, which the delegatecall payload re-constructs) nor any indirectly influential state.
      - Verdict: SAFE iff wrapper has zero args, payload has zero args, module has zero args, level sourced from storage. Otherwise VULNERABLE or DEFERRED with File:Line.
  - **(d) Privilege escalation:** Audit delegatecall context preservation:
      - (i) `delegatecall` preserves `msg.sender` — the original EOA that called `DegenerusGame.claimTerminalDecimatorJackpot()` is the msg.sender inside the module body. Confirm the module body uses `msg.sender` for crediting (`_creditClaimable(msg.sender, amountWei)` per DCM-02 evidence) — this credits the ORIGINAL CALLER, not the module's own address or any privileged context. Cite File:Line.
      - (ii) `delegatecall` preserves `msg.value` — but the wrapper is not `payable` (confirm at File:Line). So `msg.value` is guaranteed 0 at entry. Any attempt to send ETH reverts at the wrapper boundary.
      - (iii) `delegatecall` runs in the GAME's storage context — the module body SSTORE operations modify Game storage. Verify this is the intent (yes — module is a behavior-only contract, all state lives in Game).
      - (iv) No code path in the module body re-routes credit to any address other than `msg.sender` (grep for all `_creditClaimable` / `payable(...).call` / `.transfer` / `.send` sites — each must use `msg.sender` or a storage-read value that cannot be influenced by the caller).
      - Verdict: SAFE iff all delegatecall-context-preserved values are used correctly and no alternative credit path exists. Otherwise VULNERABLE or DEFERRED with File:Line.
  - **(e) Interface/implementer lockstep (DCM-03 uses this as a secondary D-11 invariant):** Verify:
      - (i) `IDegenerusGame.claimTerminalDecimatorJackpot()` signature byte-matches `DegenerusGame.claimTerminalDecimatorJackpot()` (both zero-arg external; ID-30 PASS per §3.1).
      - (ii) `IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot()` signature byte-matches `DegenerusGameDecimatorModule.claimTerminalDecimatorJackpot()` (both zero-arg external; ID-93 PASS per §3.3.d — pre-existing lockstep).
      - (iii) Wrapper's `IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot.selector` reference resolves at compile-time to the correct 4-byte selector (identical to what the module function exposes on its own ABI).
      - (iv) `make check-interfaces` PASS at HEAD (§3.4) corroborates all of the above mechanically — cite §3.4 output rollup.
      - Verdict: SAFE iff all three signature-lockstep points hold and the gate is green. No finding expected; this is mostly corroboration.
  - **(f) Delegatecall-site alignment corroboration per D-12:** `make check-delegatecall` 44/44 PASS at HEAD per §3.5. IM-08 is the +1 site vs. the v27.0 Phase 220 baseline of 43. Phase 230 Known Non-Issue #4 already classifies this bump as a legitimate IM-08 new surface, not a finding. Cite as SAFE-INFO row with Finding Candidate: N. Per D-12 this is corroborating evidence, NOT a standalone finding.

**Step 3 — Write `232-03-AUDIT.md` mirroring the v25.0 Phase 214 precedent shape AND the 231-01-PLAN / 231-02-PLAN precedent structure.** Required structure, in this exact order:

```markdown
# DCM-03 Adversarial Audit: Terminal Decimator Claim Passthrough

**Scope:** Commit `858d83e4` ("feat(game): expose `claimTerminalDecimatorJackpot` passthrough") — `DegenerusGame.claimTerminalDecimatorJackpot` external wrapper (NEW, §1.6) + `IDegenerusGame.claimTerminalDecimatorJackpot` interface declaration (NEW, §1.10 / §3.1 ID-30) + IM-08 delegatecall chain (§2.2) audited end-to-end against the pre-existing `IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot` (§3.3.d ID-93) selector target
**Method:** Fresh read from HEAD source (per D-03) — no reuse of v25.0 Phase 214 or v27.0 Phase 220/223 verdicts as pre-approved; wrapper + interface + module passthrough audited independently
**Scope source:** `230-01-DELTA-MAP.md` §4 Consumer Index row DCM-03 (per D-04)
**Verdict vocabulary:** `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (per D-02)
**Finding-ID policy:** No `F-29-NN` IDs emitted (per D-13) — `Finding Candidate: Y/N` column only; Phase 236 FIND-01 owns canonical ID assignment
**Delegatecall corroboration:** `make check-delegatecall` 44/44 PASS at HEAD (§3.5); IM-08 is the +1 site vs. v27.0 Phase 220 baseline of 43 (Phase 230 Known Non-Issue #4). Cited as evidence per D-12, NOT as a finding.

## Methodology

[One paragraph: attack vectors per CONTEXT.md D-11 (caller restriction, reentrancy, parameter pass-through, privilege escalation) plus interface/implementer lockstep + delegatecall-site alignment corroboration per D-12. Locked verdict vocabulary. Locked columns per D-02 plus Finding Candidate column per D-13.]

## Findings-Candidate Block

[Prose free-form (no F-29-NN IDs). One block per VULNERABLE or DEFERRED verdict identified in the table below, plus any SAFE-INFO row marked Finding Candidate: Y. Each block: title, verdict, File:Line, owning SHA, attack vector, evidence excerpt, recommended Phase 236 severity-classification note. If zero VULNERABLE/DEFERRED and zero SAFE-INFO Finding Candidates, state "No candidate findings — all verdicts SAFE against 858d83e4 surface."]

## Per-Function Verdict Table

| Function | File:Line | Attack Vector | Verdict | Evidence | SHA | Finding Candidate |
|---|---|---|---|---|---|---|
| `DegenerusGame.claimTerminalDecimatorJackpot` | contracts/DegenerusGame.sol:<line> | Caller restriction — no `onlyX` modifier on wrapper (intentional); module-internal game-state guards cover privilege-escalation space | <verdict> | [evidence: wrapper body lacks modifier; module body internal guards at <module File:Line(s)> enumerate gameOver + resolved-claim-round + one-shot] | 858d83e4 | <Y/N> |
| `DegenerusGame.claimTerminalDecimatorJackpot` | contracts/DegenerusGame.sol:<line> | Reentrancy — module body completes consume + credit before any external interaction; claim is one-shot per terminal claim round | <verdict> | [evidence: CEI ordering `_consumeTerminalDecClaim` → `_creditClaimable` → emit inside module body; `_creditClaimable` internals reviewed; no intermediate external call] | 858d83e4 | <Y/N> |
| `DegenerusGame.claimTerminalDecimatorJackpot` | contracts/DegenerusGame.sol:<line> | Parameter pass-through — wrapper zero args; payload zero args; module zero args; `lvl` sourced from storage `lastTerminalDecClaimRound.lvl` set at jackpot resolution | <verdict> | [evidence: wrapper sig + payload `abi.encodeWithSelector(selector)` + module sig + canonical SSTORE File:Line for `lastTerminalDecClaimRound.lvl`] | 858d83e4 | <Y/N> |
| `DegenerusGame.claimTerminalDecimatorJackpot` | contracts/DegenerusGame.sol:<line> | Privilege escalation — delegatecall preserves `msg.sender`, `msg.value`=0 (wrapper non-payable), storage context; `_creditClaimable(msg.sender, ...)` credits original caller | <verdict> | [evidence: wrapper non-payable attribute; module body grep for msg.sender usage; `_creditClaimable` credit target is msg.sender] | 858d83e4 | <Y/N> |
| `IDegenerusGame.claimTerminalDecimatorJackpot` | contracts/interfaces/IDegenerusGame.sol:<line> | Interface/implementer lockstep (ID-30 PASS per §3.1) — signature byte-match with Game wrapper | SAFE | [evidence: §3.1 ID-30 PASS; check-interfaces PASS per §3.4; both signatures parameterless external] | 858d83e4 | N |
| `IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot` | contracts/interfaces/IDegenerusGameModules.sol:<line> | Selector lockstep (ID-93 PASS per §3.3.d) — pre-existing; wrapper's selector reference resolves to the module ABI 4-byte selector | SAFE | [evidence: §3.3.d ID-93 PASS; selector derivation via compile-time `IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot.selector`] | 858d83e4 | N |
| IM-08 delegatecall chain (§2.2) | contracts/DegenerusGame.sol:<line> → contracts/modules/DegenerusGameDecimatorModule.sol:<line> | End-to-end chain correctness — wrapper selector → `_delegateToModule`/`_revertDelegate` pattern → module body; `make check-delegatecall` 44/44 PASS at HEAD corroborates site alignment (Phase 230 Known Non-Issue #4) | SAFE-INFO | [evidence: §3.5 check-delegatecall PASS 44/44; +1 site attributable to IM-08 per Known Non-Issue #4; wrapper uses standard delegate helper consistent with sibling `claimDecimatorJackpot` wrapper] | 858d83e4 | N |

[Rows above are the MINIMUM set (7 rows — 4 D-11 attack vectors on the wrapper + 2 interface-lockstep rows + 1 IM-08 chain corroboration). Replace `<line>` with actual File:Line at HEAD. Additional rows allowed when evidence warrants split.]

## IM-08 Delegatecall Chain Analysis (end-to-end)

### Hop 1: External Caller → `DegenerusGame.claimTerminalDecimatorJackpot()`
[Prose: entry point is an EOA or contract calling `DegenerusGame`'s deployed address at the `claimTerminalDecimatorJackpot()` selector. Cite File:Line of wrapper declaration. Confirm non-payable (no `msg.value` injection possible). Confirm no modifier (intentional per ID-30 NatSpec — callable by any address post-GAMEOVER once a terminal claim round is resolved).]

### Hop 2: Wrapper Delegatecall Construction
[Prose: wrapper constructs payload via `abi.encodeWithSelector(IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot.selector)` (zero additional args). Cite File:Line. Confirm the target address is `ContractAddresses.GAME_DECIMATOR_MODULE` (or whichever constant the established pattern uses — must match sibling `claimDecimatorJackpot` wrapper). Confirm `_delegateToModule` / `_revertDelegate` helpers are used (not raw assembly). The check-delegatecall gate at §3.5 corroborates the interface↔address pairing is aligned.]

### Hop 3: `DegenerusGameDecimatorModule.claimTerminalDecimatorJackpot()` execution
[Prose: module body executes in Game's storage context (delegatecall semantic). Walk the body:
- (i) Entry guards — gameOver check, terminal claim round check (cite File:Line of each)
- (ii) `_consumeTerminalDecClaim(...)` — state mutation making claim one-shot (cite File:Line)
- (iii) `_creditClaimable(msg.sender, amountWei)` — credit applied to original caller via preserved msg.sender (cite File:Line)
- (iv) `emit TerminalDecimatorClaimed(msg.sender, lastTerminalDecClaimRound.lvl, amountWei)` — DCM-02 owns event side; cited here as evidence of post-mutation CEI position (cite File:Line)
- (v) Return — no return data; any revert is forwarded by `_revertDelegate(data)` back through the wrapper.]

### Return Path
[Prose: module function returns without value; wrapper does not re-process the return. Any revert from the module body (e.g., not gameOver yet, no terminal round resolved, already claimed) is captured via the low-level delegatecall return and forwarded to the external caller via `_revertDelegate(data)`. This preserves revert-reason traceability from the module back to the caller.]

## Delegatecall-Site Alignment Corroboration (per D-12)

[One paragraph citing §3.5 of 230-01-DELTA-MAP.md: `make check-delegatecall` returns exit 0 with output "PASS 44/44 delegatecall sites aligned". Header observation: "sites discovered: 44" up from 43 at v27.0 Phase 220 baseline. Delta (+1) attributable to IM-08 — the NEW `DegenerusGame.claimTerminalDecimatorJackpot` wrapper's delegatecall site into `DegenerusGameDecimatorModule.claimTerminalDecimatorJackpot`. Phase 230 Known Non-Issue #4 already classifies this as legitimate new-surface growth, NOT drift. Per D-12, this is recorded as corroborating evidence (SAFE-INFO in the verdict table), NOT as a new finding — it confirms the interface↔address mapping and selector-resolution are aligned at HEAD with no unaligned selectors anywhere in the delegatecall surface.]

## Scope-guard Deferrals

[Any passthrough-adjacent concern surfaced during this audit that is NOT anchored in `230-01-DELTA-MAP.md` §4 DCM-03 rows is recorded here per D-05. Phase 230 is READ-only. Each entry: concern, why not in DCM-03 scope, recommended receiving phase. If none, state "None surfaced during this audit."]

## Downstream Hand-offs

[Concerns overlapping downstream-phase territory. Expected entries:
- **Phase 236 FIND-01 hand-off:** Every VULNERABLE / DEFERRED row + every SAFE-INFO row with `Finding Candidate: Y` becomes a finding-candidate anchor for Phase 236 severity classification.
- **Phase 232 DCM-02 cross-reference:** The `emit TerminalDecimatorClaimed` at the end of the module body belongs to DCM-02 scope — DCM-03 cited it only as evidence of post-mutation CEI position. Any event-side finding from DCM-02 that affects the IM-08 chain's correctness should be cross-referenced during Phase 236 consolidation.
- **Phase 235 RNG-01 hand-off (if applicable):** If the terminal decimator claim path consumes an RNG word (e.g., reading `lastTerminalDecClaimRound.rngWord` or similar), flag for Phase 235 RNG-01 backward-trace. Expected: N/A — terminal CLAIM is after terminal JACKPOT resolution; the RNG was committed upstream in `runTerminalDecimatorJackpot`. Confirm by inspection and record "N/A — terminal claim reads resolved state only, no new RNG consumer introduced by 858d83e4".
- **Phase 236 REG-01 hand-off:** The pre-existing sibling `claimDecimatorJackpot(uint24 lvl)` wrapper pattern is the precedent the new wrapper mirrors. If any v27.0 INFO finding touched that sibling pattern, cross-reference during regression sweep.
Any additional hand-offs surfaced during audit recorded here.]
```

Every verdict row MUST:
- Use exactly the locked column set per D-02 + Finding Candidate column per D-13: `Function | File:Line | Attack Vector | Verdict | Evidence | SHA | Finding Candidate`
- Cite the 7-char SHA `858d83e4`
- Use a real File:Line anchor from HEAD source (never a placeholder like `<line>`)
- Use verdict vocabulary strictly from `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (per D-02)
- Never reference F-29-NN IDs (per D-13)
- Carry a `Y` or `N` in Finding Candidate column (per D-13)

Read contract sources ONLY from `contracts/` directory at HEAD (per user memory `feedback_contract_locations.md` — never read stale copies). Use `git show 858d83e4^:contracts/...` for pre-fix comparisons (git-tree read of the same repo at a prior SHA — NOT a stale copy; expected: wrapper function and interface declaration both absent pre-fix).

READ-only constraint (v29.0 milestone rule): Zero writes to `contracts/` or `test/` permitted. Only writes allowed during task execution are `.planning/phases/232-decimator-audit/232-03-AUDIT.md` itself. `git status --porcelain contracts/ test/` MUST be empty before and after task execution.
  </action>
  <verify>
    <automated>test -f .planning/phases/232-decimator-audit/232-03-AUDIT.md && grep -c "858d83e4" .planning/phases/232-decimator-audit/232-03-AUDIT.md && test -z "$(git status --porcelain contracts/ test/)"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/232-decimator-audit/232-03-AUDIT.md` exists
    - File contains the literal header string `## Per-Function Verdict Table`
    - File contains the literal header string `## IM-08 Delegatecall Chain Analysis`
    - File contains the literal header string `## Delegatecall-Site Alignment Corroboration`
    - File contains the literal header string `## Findings-Candidate Block`
    - File contains the literal header string `## Scope-guard Deferrals`
    - File contains the literal header string `## Downstream Hand-offs`
    - Verdict table header row is exactly `| Function | File:Line | Attack Vector | Verdict | Evidence | SHA | Finding Candidate |`
    - Every row in the Per-Function Verdict Table cites the 7-char SHA `858d83e4`
    - Every row cites a File:Line anchor starting with `contracts/DegenerusGame.sol:`, `contracts/interfaces/IDegenerusGame.sol:`, `contracts/interfaces/IDegenerusGameModules.sol:`, or `contracts/modules/DegenerusGameDecimatorModule.sol:`
    - Every row's verdict is exactly one of the strings `SAFE`, `SAFE-INFO`, `VULNERABLE`, or `DEFERRED` (per D-02)
    - Every row's Finding Candidate cell is exactly `Y` or `N` (per D-13)
    - At least one row per target: `DegenerusGame.claimTerminalDecimatorJackpot`, `IDegenerusGame.claimTerminalDecimatorJackpot`, `IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot`, and IM-08 chain (4 distinct targets — expected minimum 7 verdict rows since the wrapper gets 4 D-11 attack-vector rows)
    - At least 4 verdict rows exist on the `DegenerusGame.claimTerminalDecimatorJackpot` wrapper covering the 4 D-11 attack vectors: caller restriction, reentrancy, parameter pass-through, privilege escalation
    - `grep -cE "SAFE|SAFE-INFO|VULNERABLE|DEFERRED" .planning/phases/232-decimator-audit/232-03-AUDIT.md` returns at least 7
    - `grep -c "858d83e4" .planning/phases/232-decimator-audit/232-03-AUDIT.md` returns at least 7 (one per verdict row minimum)
    - The string `F-29-NN` does NOT appear in the file (per D-13)
    - The string `F-29-` does NOT appear in any form in the file (Phase 236 owns ID assignment per D-13)
    - IM-08 Delegatecall Chain Analysis section contains at least three labeled hop subsections (Hop 1, Hop 2, Hop 3) plus a Return Path subsection
    - Delegatecall-Site Alignment Corroboration section references the literal string `44/44` (per D-12 + §3.5)
    - Delegatecall-Site Alignment Corroboration section explicitly states the 43→44 bump is corroborating evidence NOT a finding, and references `Phase 230 Known Non-Issue #4`
    - Downstream Hand-offs subsection explicitly names `Phase 236 FIND-01` (per D-13)
    - Downstream Hand-offs subsection explicitly names `Phase 232 DCM-02` (event-side cross-reference)
    - Caller-restriction analysis explicitly mentions `post-GAMEOVER` or `gameOver` as the internal gating invariant
    - Privilege-escalation analysis explicitly mentions `msg.sender` preservation through delegatecall
    - Parameter pass-through analysis explicitly mentions `lastTerminalDecClaimRound.lvl` as the storage-sourced level (not caller-controlled)
    - No placeholder line numbers like `:<line>` remain in the final file — every File:Line is a real integer or integer range
    - No contract source content read from outside `contracts/` directory (per user memory `feedback_contract_locations.md`)
    - `git status --porcelain contracts/ test/` returns empty output both before and after task execution (READ-only v29.0 milestone constraint; zero writes to production code)
  </acceptance_criteria>
  <done>`232-03-AUDIT.md` exists with per-function verdict table covering the new Game wrapper + both interface declarations + IM-08 delegatecall chain end-to-end. Every row cites SHA `858d83e4` and a real File:Line in `contracts/`. Verdicts use `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` vocabulary. D-11 attack vectors all covered (caller restriction, reentrancy, parameter pass-through, privilege escalation). IM-08 Delegatecall Chain Analysis walks all three hops + return path. Delegatecall-Site Alignment Corroboration cites `make check-delegatecall` 44/44 PASS at HEAD (§3.5) as evidence per D-12 (NOT as finding — Phase 230 Known Non-Issue #4 already classifies the +1 bump). Findings-Candidate / Scope-guard Deferrals / Downstream Hand-offs subsections present; Phase 236 FIND-01 and Phase 232 DCM-02 cross-references explicit. Zero F-29-NN emissions (per D-13). Zero contracts/ or test/ writes (READ-only milestone rule).</done>
</task>

</tasks>

<verification>
- `232-03-AUDIT.md` exists with per-function verdict table covering `DegenerusGame.claimTerminalDecimatorJackpot` (new wrapper), `IDegenerusGame.claimTerminalDecimatorJackpot` (new interface decl), `IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot` (pre-existing module sub-interface), and IM-08 delegatecall chain
- Locked column set per D-02 (`Function | File:Line | Attack Vector | Verdict | Evidence | SHA`) + `Finding Candidate` column per D-13
- Every verdict row cites `858d83e4`
- Every File:Line anchor resolves into `contracts/` at HEAD
- Verdicts exclusively use `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (per D-02)
- Every row carries `Y` or `N` in Finding Candidate column (per D-13)
- D-11 attack vectors all present: caller restriction, reentrancy, parameter pass-through, privilege escalation
- IM-08 Delegatecall Chain Analysis walks Hop 1 → Hop 2 → Hop 3 → Return Path
- Delegatecall-Site Alignment Corroboration cites `make check-delegatecall` 44/44 PASS per D-12 + §3.5 + Phase 230 Known Non-Issue #4, explicitly recorded as evidence NOT a finding
- Findings-Candidate / Scope-guard Deferrals / Downstream Hand-offs subsections present
- Hand-offs explicitly name Phase 236 FIND-01 (per D-13) and Phase 232 DCM-02 (event-side cross-reference)
- Zero `F-29-NN` references anywhere in the file (per D-13)
- Zero writes to `contracts/` or `test/` during plan execution (READ-only v29.0 milestone rule)
</verification>

<success_criteria>
The new `DegenerusGame.claimTerminalDecimatorJackpot` wrapper has `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` verdicts against all four D-11 attack vectors (caller restriction, reentrancy, parameter pass-through, privilege escalation). The IM-08 delegatecall chain is audited end-to-end from EOA → wrapper → module body → return path. Interface/implementer lockstep is confirmed per §3.1 ID-30 and §3.3.d ID-93. `make check-delegatecall` 44/44 PASS at HEAD is cited as corroborating evidence per D-12 (NOT as a new finding — Phase 230 Known Non-Issue #4 already classifies the 43→44 bump). Every verdict traceable via SHA + File:Line. Every row carries `Finding Candidate: Y/N` per D-13. Phase 230 delta-map is not edited; any out-of-scope discovery is recorded in Scope-guard Deferrals per D-05. READ-only milestone rule honored: zero writes to `contracts/` or `test/`.
</success_criteria>

<output>
After completion, create `.planning/phases/232-decimator-audit/232-03-SUMMARY.md`
</output>
</content>
</invoke>