---
phase: 232-decimator-audit
plan: 232-02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/232-decimator-audit/232-02-AUDIT.md
autonomous: true
requirements: [DCM-02]
must_haves:
  truths:
    - "Every function listed in 230-01-DELTA-MAP.md §1.3 owned by commit 67031e7d has a verdict row in the per-function table (minimum: `claimDecimatorJackpot` ID-89, `claimTerminalDecimatorJackpot` ID-93)"
    - "All three new `emit` sites are catalogued — `DecimatorClaimed` gameOver fast-path emission, `DecimatorClaimed` normal ETH/lootbox split emission, `TerminalDecimatorClaimed` terminal emission"
    - "Each `emit` site has a verdict row covering CEI position (per D-08) AND event-argument correctness (per D-09)"
    - "Argument invariants explicitly checked per D-09: `DecimatorClaimed.ethPortion + lootboxPortion == amountWei`, `DecimatorClaimed.lvl == claimDecimatorJackpot(lvl)` input arg, `DecimatorClaimed.player == msg.sender`, `TerminalDecimatorClaimed.lvl == lastTerminalDecClaimRound.lvl`, `TerminalDecimatorClaimed.amountWei == consumed claim`"
    - "v28.0 indexer-compat observation recorded per D-10 — neither DCM-02 event signature was in v28.0 Phase 227 event-processor scope; documented as OBSERVATION only, not a contract-side finding"
    - "Every verdict row cites the 7-char commit SHA `67031e7d` (per D-03)"
    - "Every verdict row cites a File:Line anchor pointing into contracts/modules/DegenerusGameDecimatorModule.sol at HEAD"
    - "Verdict vocabulary used is exactly `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (per D-02)"
    - "No F-29-NN finding IDs appear anywhere in 232-02-AUDIT.md (per D-13)"
    - "2-indexed-topic confirmation per Claude's Discretion block: both events well under 4-topic EVM limit (DecimatorClaimed: `player`+`lvl` indexed; TerminalDecimatorClaimed: `player`+`lvl` indexed)"
    - "Every verdict row carries a `Finding Candidate: Y/N` column value (per D-13)"
    - "Zero contracts/ and zero test/ writes occur during plan execution (READ-only per v29.0 milestone rule)"
  artifacts:
    - path: ".planning/phases/232-decimator-audit/232-02-AUDIT.md"
      provides: "DCM-02 decimator event emission adversarial audit"
      contains: "## Per-Function Verdict Table"
  key_links:
    - from: "230-01-DELTA-MAP.md §1.3 event declarations + §2.2 IM-08 callee-side rows (67031e7d)"
      to: "232-02-AUDIT.md per-function verdict table rows"
      via: "Every 67031e7d function + emit site has a verdict"
      pattern: "SAFE|SAFE-INFO|VULNERABLE|DEFERRED"
    - from: "232-02-AUDIT.md indexer-compat observation"
      to: "Phase 236 FIND-02 KNOWN-ISSUES candidate OR future `database/` milestone"
      via: "Indexer-Compatibility Observation subsection (READ-only observation per D-10)"
      pattern: "v28.0 Phase 227"
    - from: "232-02-AUDIT.md VULNERABLE/DEFERRED verdicts"
      to: "Phase 236 FIND-01 finding-candidate pool"
      via: "Findings-Candidate Block (no F-29-NN IDs, Finding Candidate: Y/N column)"
      pattern: "## Findings-Candidate Block"
---

<objective>
DCM-02 adversarial audit of the decimator event-emission addition owned by commit `67031e7d` ("feat(decimator): emit `DecimatorClaimed` and `TerminalDecimatorClaimed`"). Produces `232-02-AUDIT.md`: a per-function verdict table covering `DegenerusGameDecimatorModule.claimDecimatorJackpot` and `DegenerusGameDecimatorModule.claimTerminalDecimatorJackpot` (both bodies MODIFIED by `67031e7d`), dedicated CEI-position analysis for each of the 3 new `emit` sites, event-argument correctness verdicts per D-09, and a 1-paragraph v28.0 Phase 227 indexer-compat OBSERVATION (per D-10, not a contract-side finding).

Purpose: Two new events + three new `emit` sites are added to the Decimator module. A C4A warden would probe: (a) CEI position of each emit — `DecimatorClaimed` fires at two sites inside `claimDecimatorJackpot` (gameOver fast-path post-`_creditClaimable`; normal ETH/lootbox split post-`_setFuturePrizePool`) and both must be AFTER all state mutations + pool writes to satisfy CEI; `TerminalDecimatorClaimed` fires immediately after `_creditClaimable(msg.sender, amountWei)` following the `_consumeTerminalDecClaim` state mutation (per D-08), (b) argument correctness — `ethPortion + lootboxPortion == amountWei` at the `DecimatorClaimed` emission, `lvl` argument matches the input arg to `claimDecimatorJackpot(uint24 lvl)`, `player == msg.sender`, `TerminalDecimatorClaimed.lvl == lastTerminalDecClaimRound.lvl` (resolved claim round, not caller-controlled), `TerminalDecimatorClaimed.amountWei == consumed claim` (per D-09), (c) indexer compatibility — v28.0 Phase 227 audited the event-processor surface; the two new event signatures were added POST-v27 and are NOT in that audit's scope; record as OBSERVATION per D-10, contract-side verdict is neutral, `database/` milestone out of scope, (d) 4-indexed-topic EVM limit — both events use 2 indexed topics (player + lvl), well within limit (Claude's Discretion). This plan surfaces adversarial verdicts against the D-02 column schema using the D-02 verdict vocabulary. Phase 236 FIND-01 owns canonical F-29-NN ID assignment (D-13); Phase 236 FIND-02 owns any KNOWN-ISSUES updates surfaced by the indexer-compat observation (per CONTEXT Deferred section).

Output: `232-02-AUDIT.md` with per-function verdict table + dedicated CEI-position analysis block + event-argument correctness block + indexer-compat OBSERVATION + Findings-Candidate / Scope-guard Deferrals / Downstream Hand-offs subsections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/232-decimator-audit/232-CONTEXT.md
@.planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md
@.planning/phases/230-delta-extraction-scope-map/230-01-SUMMARY.md
@.planning/milestones/v25.0-phases/214-adversarial-audit/214-01-REENTRANCY-CEI.md
@.planning/phases/231-earlybird-jackpot-audit/231-01-PLAN.md
@.planning/phases/231-earlybird-jackpot-audit/231-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract 67031e7d diff + read DecimatorModule at HEAD to audit the two new events + three emit sites; write 232-02-AUDIT.md</name>
  <files>.planning/phases/232-decimator-audit/232-02-AUDIT.md</files>
  <read_first>
    - .planning/phases/232-decimator-audit/232-CONTEXT.md (D-01 through D-14 — locked column schema D-02, SHA-citation rule D-03, scope anchor D-04, scope-guard deferral rule D-05, CEI audit focus D-08, event-argument correctness rules D-09, indexer-compat READ-only observation rule D-10, finding-ID discipline D-13)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.3 (lines 107-120 — DegenerusGameDecimatorModule.sol function-level changelog; both target functions with specific body-hunk descriptions):
        * `claimDecimatorJackpot(uint24 lvl)` MODIFIED by `67031e7d`: "Added two new `emit DecimatorClaimed(...)` calls — one in the `gameOver` fast path (post `_creditClaimable`), one after the normal ETH/lootbox split (post `_setFuturePrizePool`). Arguments: claimer, lvl, `amountWei`, ETH portion, lootbox portion. No change to payout math or CEI ordering."
        * `claimTerminalDecimatorJackpot()` MODIFIED by `67031e7d`: "Added `emit TerminalDecimatorClaimed(msg.sender, lastTerminalDecClaimRound.lvl, amountWei)` immediately after `_creditClaimable(msg.sender, amountWei)` — at the correct CEI position (state mutation in `_consumeTerminalDecClaim` already complete, credit applied, event emitted last)."
        * Event declarations (non-function delta items, line 117-118):
          - `event DecimatorClaimed(address indexed player, uint24 indexed lvl, uint256 amountWei, uint256 ethPortion, uint256 lootboxPortion)` — NEW by `67031e7d`
          - `event TerminalDecimatorClaimed(address indexed player, uint24 indexed lvl, uint256 amountWei)` — NEW by `67031e7d`
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §2.2 row IM-08 (line 252 — "NEW external wrapper (858d83e4) delegatecalls the module via `IDegenerusGameDecimatorModule.claimTerminalDecimatorJackpot.selector`. Module-side callee was further MODIFIED (67031e7d) to emit `TerminalDecimatorClaimed`." — DCM-02 audits the CALLEE side of IM-08)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §4 Consumer Index DCM-02 row (line 553 — authoritative scope anchor per D-04: "§1.3 (`claimDecimatorJackpot`, `claimTerminalDecimatorJackpot` MODIFIED by 67031e7d) + event declarations `DecimatorClaimed`, `TerminalDecimatorClaimed`, §2.2 (IM-08 callee-side), commit `67031e7d` rows")
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §3.3.d rows ID-89 and ID-93 (lines 427, 431 — both methods signed identical between interface and implementer; body-level changes do not affect interface drift verdict)
    - Output of `git show 67031e7d --stat` and `git show 67031e7d -- contracts/modules/DegenerusGameDecimatorModule.sol` to inspect the authored diff — the delta IS the audit surface. 35 insertions / 2 deletions per §1.3. Pre-fix source (`67031e7d^`) shows the functions without the emits; HEAD shows them added.
    - contracts/modules/DegenerusGameDecimatorModule.sol at HEAD — locate:
        * Event declarations `DecimatorClaimed` and `TerminalDecimatorClaimed` — record exact File:Line of each declaration and confirm indexed-topic layout (`player indexed`, `lvl indexed`, remaining params non-indexed).
        * `claimDecimatorJackpot(uint24 lvl)` at approximately line 321 — enumerate:
          - Function entry + modifier chain
          - gameOver fast-path branch entry point (File:Line of the `if (gameOver)` / equivalent guard)
          - `_creditClaimable(msg.sender, amountWei)` call site inside gameOver branch (File:Line)
          - First `emit DecimatorClaimed(...)` immediately following `_creditClaimable` in gameOver branch (File:Line + full emit arg list verbatim)
          - Normal path branch entry point
          - `_creditClaimable` call (if any) in normal path
          - ETH / lootbox split computation (cite the expression producing `ethPortion` and `lootboxPortion`)
          - `_setFuturePrizePool(...)` call site (File:Line)
          - Second `emit DecimatorClaimed(...)` immediately following `_setFuturePrizePool` (File:Line + full emit arg list verbatim)
          - Any state mutation AFTER either emit (should be none for CEI — record if any exist)
        * `claimTerminalDecimatorJackpot()` at approximately line 811 — enumerate:
          - Function entry + modifier chain
          - `_consumeTerminalDecClaim(...)` call site (File:Line — this is the state mutation)
          - `_creditClaimable(msg.sender, amountWei)` call site (File:Line)
          - `emit TerminalDecimatorClaimed(msg.sender, lastTerminalDecClaimRound.lvl, amountWei)` (File:Line + full emit arg list verbatim)
          - Any state mutation AFTER the emit (should be none for CEI)
        * Record also `lastTerminalDecClaimRound.lvl` source — find the SSTORE that sets this (likely in `runTerminalDecimatorJackpot` or `_consumeTerminalDecClaim`) to verify `lastTerminalDecClaimRound.lvl` is set to the RESOLVED claim round, not a caller-controlled value. Cite File:Line of the SSTORE for the audit evidence.
    - contracts/modules/DegenerusGameDecimatorModule.sol at `67031e7d^` via `git show 67031e7d^:contracts/modules/DegenerusGameDecimatorModule.sol | head -900` — confirm the pre-fix bodies had no `emit DecimatorClaimed` or `emit TerminalDecimatorClaimed` anywhere (proves the 3 emit sites are all NEW with this commit).
    - .planning/milestones/v25.0-phases/214-adversarial-audit/214-01-REENTRANCY-CEI.md (lines 1-60 — precedent CEI audit table shape and methodology preamble; per D-03 do NOT carry forward v25.0 verdicts as pre-approved)
    - .planning/phases/231-earlybird-jackpot-audit/231-01-PLAN.md (precedent column schema + acceptance_criteria format)
    - .planning/phases/231-earlybird-jackpot-audit/231-02-PLAN.md (precedent — single-function focused audit structure analogous to 232-02's narrow surface)
    - If available, `.planning/milestones/v28.0-phases/227-indexer-event-processing-correctness/` directory listing — Phase 227 event-processor audit output for v28.0 baseline. The DCM-02 indexer-compat observation cites this phase as the surface where these new events would need to be registered on the `database/` side. Read enough to confirm that neither `DecimatorClaimed` nor `TerminalDecimatorClaimed` event signature appears in the v28.0 Phase 227 scope (expected — `67031e7d` postdates v28.0).
  </read_first>
  <action>
Execute a fresh-from-source adversarial audit of the `67031e7d` decimator event-emission addition per CONTEXT.md D-01 through D-14. Verdict vocabulary per D-02 is strictly `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`. Column schema per D-02 is strictly `Function | File:Line | Attack Vector | Verdict | Evidence | SHA` with an additional `Finding Candidate: Y/N` column per D-13.

**Step 1 — Build the scope list from `230-01-DELTA-MAP.md` §4 Consumer Index DCM-02 row (D-04).** The target set is:
  - **Function 1: `DegenerusGameDecimatorModule.claimDecimatorJackpot(uint24 lvl)`** — MODIFIED by `67031e7d` — TWO new emit sites (gameOver fast-path + normal ETH/lootbox split)
  - **Function 2: `DegenerusGameDecimatorModule.claimTerminalDecimatorJackpot()`** — MODIFIED by `67031e7d` — ONE new emit site (terminal path, post `_creditClaimable`)
  - **Event 1: `DecimatorClaimed(address indexed player, uint24 indexed lvl, uint256 amountWei, uint256 ethPortion, uint256 lootboxPortion)`** — NEW event declaration by `67031e7d`
  - **Event 2: `TerminalDecimatorClaimed(address indexed player, uint24 indexed lvl, uint256 amountWei)`** — NEW event declaration by `67031e7d`

Total emit sites audited: 3 (2 in `claimDecimatorJackpot`, 1 in `claimTerminalDecimatorJackpot`). Every emit site MUST produce AT LEAST two verdict rows — one for CEI position (D-08) and one for argument correctness (D-09).

**Step 2 — For each emit site, run the attack vectors from CONTEXT.md D-08 and D-09:**

  **D-08 CEI-position attack vectors:**
  - **(a) `DecimatorClaimed` gameOver fast-path emission (site 1):** Verify the emit is positioned AFTER `_creditClaimable(msg.sender, amountWei)`. Read the function body at HEAD. Check: (i) is `_creditClaimable` the final state-mutation call before the emit? (ii) are there any SSTORE operations between `_creditClaimable` and the emit? (iii) is there any external call between `_creditClaimable` and the emit? Record verdict + File:Line of each of: the `_creditClaimable` call, the emit, and any intermediate statements.
  - **(b) `DecimatorClaimed` normal ETH/lootbox split emission (site 2):** Verify the emit is positioned AFTER `_setFuturePrizePool(...)`. Check: (i) is `_setFuturePrizePool` the final pool-write state-mutation before the emit? (ii) is the ETH/lootbox split computation (`ethPortion`, `lootboxPortion` locals) completed BEFORE both the state mutation and the emit? (iii) any intermediate state mutations or external calls between `_setFuturePrizePool` and the emit? Record verdict + File:Line.
  - **(c) `TerminalDecimatorClaimed` terminal emission (site 3):** Verify the emit is positioned AFTER `_creditClaimable(msg.sender, amountWei)`. Per §1.3 note: "state mutation in `_consumeTerminalDecClaim` already complete, credit applied, event emitted last" — trace the full ordering: `_consumeTerminalDecClaim(...)` → `_creditClaimable(msg.sender, amountWei)` → `emit TerminalDecimatorClaimed(...)`. Check: (i) is `_consumeTerminalDecClaim` the state-consuming operation that makes the claim one-shot? (ii) is `_creditClaimable` the only state-mutation between consume and emit? (iii) any intermediate external call or SSTORE? Record verdict + File:Line of each hop.

  **D-09 argument-correctness attack vectors:**
  - **(d) `DecimatorClaimed.ethPortion + lootboxPortion == amountWei` invariant (both emit sites):** Trace the local variables at the emit site. For the gameOver fast-path: the emit may pass `amountWei` + zero-valued portions or split portions — cite the exact local values. For the normal split: cite the split computation that produces `ethPortion` and `lootboxPortion`, and verify algebraically that their sum equals `amountWei` at the emit site. Record verdict per emit site.
  - **(e) `DecimatorClaimed.lvl == claimDecimatorJackpot(lvl)` input arg:** Verify the `lvl` arg passed to the emit is the same `uint24 lvl` function parameter (not a locally-recomputed derivative). Record verdict + File:Line.
  - **(f) `DecimatorClaimed.player == msg.sender`:** Verify the first emit arg is `msg.sender` (or a local alias of `msg.sender` not overwritten). Record verdict + File:Line.
  - **(g) `TerminalDecimatorClaimed.lvl == lastTerminalDecClaimRound.lvl`:** Verify the `lvl` arg is sourced from storage state `lastTerminalDecClaimRound.lvl`, NOT from msg.sender or any caller-controlled value. Find the SSTORE site that sets `lastTerminalDecClaimRound.lvl` (likely in `runTerminalDecimatorJackpot` or `_consumeTerminalDecClaim`) to verify this value is set during jackpot resolution, not during claim. Record verdict + File:Line of both the read at the emit AND the canonical write site.
  - **(h) `TerminalDecimatorClaimed.amountWei == consumed claim`:** Verify the `amountWei` arg is the value returned from `_consumeTerminalDecClaim` (or equivalent) — the CONSUMED amount, not a pre-consumed or speculative amount. Record verdict + File:Line.

  **Claude's Discretion attack vectors (optional but recommended per CONTEXT Out-of-Scope Reminders):**
  - **(i) 4-indexed-topic EVM limit:** Confirm both events use 2 indexed topics (`player` + `lvl`), well below the 4-topic limit. Record as SAFE-INFO with Finding Candidate: N.
  - **(j) Gas implication of new emits:** Events fire once per claim (not loop-multiplied). Per CONTEXT Out-of-Scope Reminders, gas-ceiling re-profile is NOT required; record as SAFE-INFO with Finding Candidate: N.

**Step 3 — Indexer-compat OBSERVATION (per D-10).** Read `.planning/milestones/v28.0-phases/227-indexer-event-processing-correctness/` (if it exists) or cite the v28.0 event-processor registration surface by grep against the `database/` repo's event handlers IF accessible. Per v29.0 PROJECT.md scoping, `database/` is OUT OF SCOPE — do NOT modify `database/` files. Record a 1-paragraph OBSERVATION documenting:
  - Whether the two new event signatures (`DecimatorClaimed` topic0 = `keccak256("DecimatorClaimed(address,uint24,uint256,uint256,uint256)")`, `TerminalDecimatorClaimed` topic0 = `keccak256("TerminalDecimatorClaimed(address,uint24,uint256)")`) are registered in the v28.0 Phase 227-audited indexer event-processor surface.
  - Expected answer: NO — `67031e7d` postdates the v28.0 milestone cutoff.
  - Classification per D-10: OBSERVATION only, not a contract-side finding. Routed to either Phase 236 FIND-02 (KNOWN-ISSUES candidate) or a future `database/` milestone. Zero `database/` writes permitted this phase.

**Step 4 — Write `232-02-AUDIT.md` mirroring the v25.0 Phase 214 precedent shape AND the 231-01-PLAN precedent structure.** Required structure, in this exact order:

```markdown
# DCM-02 Adversarial Audit: Decimator Event Emission

**Scope:** Commit `67031e7d` ("feat(decimator): emit `DecimatorClaimed` and `TerminalDecimatorClaimed`") — `DegenerusGameDecimatorModule.claimDecimatorJackpot` + `DegenerusGameDecimatorModule.claimTerminalDecimatorJackpot` body-level emit additions across §1.3 of `230-01-DELTA-MAP.md` + event declarations
**Method:** Fresh read from HEAD source (per D-03) — no reuse of v25.0 Phase 214 or v27.0 Phase 223 verdicts as pre-approved; each of the 3 new `emit` sites audited for CEI position (D-08) and argument correctness (D-09) independently
**Scope source:** `230-01-DELTA-MAP.md` §4 Consumer Index row DCM-02 (per D-04)
**Verdict vocabulary:** `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (per D-02)
**Finding-ID policy:** No `F-29-NN` IDs emitted (per D-13) — `Finding Candidate: Y/N` column only; Phase 236 FIND-01 owns canonical ID assignment
**Indexer-compat scope:** v28.0 Phase 227 cross-reference is a READ-only OBSERVATION (per D-10). No `database/` writes permitted.

## Methodology

[One paragraph: attack vectors per CONTEXT.md D-08 (CEI position for each of 3 emit sites) and D-09 (argument correctness — ethPortion+lootboxPortion==amountWei, lvl matches input, player==msg.sender, terminal lvl from storage not caller, terminal amountWei matches consumed). Locked verdict vocabulary. Locked columns per D-02 plus Finding Candidate column per D-13. Indexer-compat observation method per D-10.]

## Findings-Candidate Block

[Prose free-form (no F-29-NN IDs). One block per VULNERABLE or DEFERRED verdict identified in the table below, plus any SAFE-INFO row marked Finding Candidate: Y. Each block: title, verdict, File:Line, owning SHA, attack vector, evidence excerpt, recommended Phase 236 severity-classification note. If zero VULNERABLE/DEFERRED and zero SAFE-INFO Finding Candidates, state "No candidate findings — all verdicts SAFE against 67031e7d surface."]

## Per-Function Verdict Table

| Function | File:Line | Attack Vector | Verdict | Evidence | SHA | Finding Candidate |
|---|---|---|---|---|---|---|
| `DegenerusGameDecimatorModule.claimDecimatorJackpot` (gameOver emit site) | contracts/modules/DegenerusGameDecimatorModule.sol:<line> | CEI position — emit AFTER `_creditClaimable` with no intermediate state mutation or external call | <verdict> | [evidence: ordering + File:Line of `_creditClaimable`, the emit, and any intermediate statements] | 67031e7d | <Y/N> |
| `DegenerusGameDecimatorModule.claimDecimatorJackpot` (gameOver emit site) | contracts/modules/DegenerusGameDecimatorModule.sol:<line> | Argument correctness — `ethPortion + lootboxPortion == amountWei` in gameOver fast-path | <verdict> | [evidence: local-variable derivation; may be `amountWei + 0` or split portions] | 67031e7d | <Y/N> |
| `DegenerusGameDecimatorModule.claimDecimatorJackpot` (gameOver emit site) | contracts/modules/DegenerusGameDecimatorModule.sol:<line> | Argument correctness — `lvl == claimDecimatorJackpot(lvl)` input + `player == msg.sender` | <verdict> | [evidence: emit arg list verbatim] | 67031e7d | <Y/N> |
| `DegenerusGameDecimatorModule.claimDecimatorJackpot` (normal split emit site) | contracts/modules/DegenerusGameDecimatorModule.sol:<line> | CEI position — emit AFTER `_setFuturePrizePool` with no intermediate state mutation or external call | <verdict> | [evidence: ordering + File:Line of split computation, `_setFuturePrizePool`, the emit] | 67031e7d | <Y/N> |
| `DegenerusGameDecimatorModule.claimDecimatorJackpot` (normal split emit site) | contracts/modules/DegenerusGameDecimatorModule.sol:<line> | Argument correctness — `ethPortion + lootboxPortion == amountWei` (algebraic sum at emit) | <verdict> | [evidence: split formula + sum proof] | 67031e7d | <Y/N> |
| `DegenerusGameDecimatorModule.claimDecimatorJackpot` (normal split emit site) | contracts/modules/DegenerusGameDecimatorModule.sol:<line> | Argument correctness — `lvl == input arg` + `player == msg.sender` | <verdict> | [evidence: emit arg list verbatim] | 67031e7d | <Y/N> |
| `DegenerusGameDecimatorModule.claimTerminalDecimatorJackpot` (terminal emit site) | contracts/modules/DegenerusGameDecimatorModule.sol:<line> | CEI position — emit AFTER `_consumeTerminalDecClaim` AND `_creditClaimable` with no intermediate state mutation or external call | <verdict> | [evidence: three-hop ordering consume → credit → emit, File:Line each] | 67031e7d | <Y/N> |
| `DegenerusGameDecimatorModule.claimTerminalDecimatorJackpot` (terminal emit site) | contracts/modules/DegenerusGameDecimatorModule.sol:<line> | Argument correctness — `lvl == lastTerminalDecClaimRound.lvl` (storage-sourced, not caller-controlled) | <verdict> | [evidence: emit reads storage; canonical SSTORE at <runTerminalDecimatorJackpot or _consumeTerminalDecClaim>:<line>] | 67031e7d | <Y/N> |
| `DegenerusGameDecimatorModule.claimTerminalDecimatorJackpot` (terminal emit site) | contracts/modules/DegenerusGameDecimatorModule.sol:<line> | Argument correctness — `amountWei == consumed claim return value` + `player == msg.sender` | <verdict> | [evidence: `_consumeTerminalDecClaim` return → local → emit; full chain] | 67031e7d | <Y/N> |
| `DecimatorClaimed` event declaration | contracts/modules/DegenerusGameDecimatorModule.sol:<line> | 4-indexed-topic EVM limit — 2 indexed topics (player + lvl) well under limit | SAFE-INFO | [evidence: declaration verbatim + topic count] | 67031e7d | N |
| `TerminalDecimatorClaimed` event declaration | contracts/modules/DegenerusGameDecimatorModule.sol:<line> | 4-indexed-topic EVM limit — 2 indexed topics (player + lvl) well under limit | SAFE-INFO | [evidence: declaration verbatim + topic count] | 67031e7d | N |

[Rows above are the MINIMUM set (11 rows — 3 emit sites × 3 vectors + 2 declaration SAFE-INFO rows). Replace `<line>` with actual File:Line at HEAD. Additional rows allowed when a single attack vector requires multiple File:Line citations to cover the chain.]

## CEI-Position Analysis (per D-08)

### Emit Site 1: `claimDecimatorJackpot` — gameOver fast-path
[Prose walk: enumerate every statement from function entry through the emit. Identify the guard that selects the gameOver branch. Cite `_creditClaimable(msg.sender, amountWei)` at File:Line. Cite the emit at File:Line. Quote any statement between them verbatim. Verdict: SAFE iff no state mutation or external call between.]

### Emit Site 2: `claimDecimatorJackpot` — normal ETH/lootbox split
[Prose walk: enumerate the normal path from branch entry through split computation to emit. Cite `ethPortion` / `lootboxPortion` derivation at File:Line. Cite `_setFuturePrizePool(...)` at File:Line. Cite the emit at File:Line. Quote any statement between `_setFuturePrizePool` and the emit verbatim. Verdict: SAFE iff no state mutation or external call between.]

### Emit Site 3: `claimTerminalDecimatorJackpot` — terminal path
[Prose walk: enumerate from function entry through `_consumeTerminalDecClaim` to `_creditClaimable` to emit. Cite each at File:Line. Verdict: SAFE iff the three-hop ordering is respected with no intermediate mutations.]

## Event-Argument Correctness Analysis (per D-09)

### `DecimatorClaimed(player, lvl, amountWei, ethPortion, lootboxPortion)`
[Prose per-argument walk. For each indexed + non-indexed parameter, cite the source: function arg, local variable, storage read, `msg.sender`. Prove algebraically `ethPortion + lootboxPortion == amountWei` at each of the two emit sites. For the gameOver fast-path, note whether the ETH/lootbox split is `(amountWei, 0)` or `(0, amountWei)` or true split — cite the exact local values.]

### `TerminalDecimatorClaimed(player, lvl, amountWei)`
[Prose per-argument walk. Confirm `player == msg.sender`, `lvl == lastTerminalDecClaimRound.lvl` (storage-sourced), `amountWei == return value of _consumeTerminalDecClaim`. Cite the SSTORE of `lastTerminalDecClaimRound.lvl` at its canonical write site (runTerminalDecimatorJackpot or equivalent) to prove it is set during jackpot resolution, not during claim — this is why the emit can trust storage over any caller-controlled value.]

## Indexer-Compatibility Observation (per D-10 — READ-only OBSERVATION)

[One paragraph: v28.0 Phase 227 audited the event-processor surface at the v27.0-baseline commit. Commit `67031e7d` postdates that baseline. Therefore neither `DecimatorClaimed` nor `TerminalDecimatorClaimed` event signature was in scope for Phase 227. State the implication: the `database/` indexer must register both signatures before the v29.0 deployment is observable off-chain. Per v29.0 PROJECT.md scoping, the `database/` repo is OUT OF SCOPE this milestone — this observation routes either (a) to Phase 236 FIND-02 as a KNOWN-ISSUES candidate, or (b) to a future `database/` milestone. Zero `database/` writes attempted this phase. This is NOT a contract-side finding — the contracts emit correctly; the observation is about downstream ABI-registration.]

## Scope-guard Deferrals

[Any event-emission-adjacent concern surfaced during this audit that is NOT anchored in `230-01-DELTA-MAP.md` §4 DCM-02 rows is recorded here per D-05. Phase 230 is READ-only. Each entry: concern, why not in DCM-02 scope, recommended receiving phase. If none, state "None surfaced during this audit."]

## Downstream Hand-offs

[Concerns overlapping downstream-phase territory. Expected entries:
- **Phase 236 FIND-01 hand-off:** Every VULNERABLE / DEFERRED row + every SAFE-INFO row with `Finding Candidate: Y` becomes a finding-candidate anchor for Phase 236 severity classification.
- **Phase 236 FIND-02 hand-off:** The indexer-compat observation may warrant a KNOWN-ISSUES entry during Phase 236 consolidation, cross-referenced by future F-29-NN ID assignment.
- **Future `database/` milestone hand-off:** Event-processor registration for `DecimatorClaimed` + `TerminalDecimatorClaimed` is a downstream indexer task, not a contract-side finding. Recorded for traceability; no action this milestone.
Any additional hand-offs surfaced during audit recorded here.]
```

Every verdict row MUST:
- Use exactly the locked column set per D-02 + Finding Candidate column per D-13: `Function | File:Line | Attack Vector | Verdict | Evidence | SHA | Finding Candidate`
- Cite the 7-char SHA `67031e7d`
- Use a real File:Line anchor from HEAD source (never a placeholder like `<line>`)
- Use verdict vocabulary strictly from `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (per D-02)
- Never reference F-29-NN IDs (per D-13)
- Carry a `Y` or `N` in Finding Candidate column (per D-13)

Read contract sources ONLY from `contracts/` directory at HEAD (per user memory `feedback_contract_locations.md` — never read stale copies). Use `git show 67031e7d^:contracts/modules/DegenerusGameDecimatorModule.sol` for pre-fix comparisons (git-tree read of the same repo at a prior SHA — NOT a stale copy).

READ-only constraint (v29.0 milestone rule): Zero writes to `contracts/`, `test/`, or `database/` permitted. Only writes allowed during task execution are `.planning/phases/232-decimator-audit/232-02-AUDIT.md` itself. `git status --porcelain contracts/ test/` MUST be empty before and after task execution.
  </action>
  <verify>
    <automated>test -f .planning/phases/232-decimator-audit/232-02-AUDIT.md && grep -c "67031e7d" .planning/phases/232-decimator-audit/232-02-AUDIT.md && test -z "$(git status --porcelain contracts/ test/)"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/232-decimator-audit/232-02-AUDIT.md` exists
    - File contains the literal header string `## Per-Function Verdict Table`
    - File contains the literal header string `## CEI-Position Analysis`
    - File contains the literal header string `## Event-Argument Correctness Analysis`
    - File contains the literal header string `## Indexer-Compatibility Observation`
    - File contains the literal header string `## Findings-Candidate Block`
    - File contains the literal header string `## Scope-guard Deferrals`
    - File contains the literal header string `## Downstream Hand-offs`
    - Verdict table header row is exactly `| Function | File:Line | Attack Vector | Verdict | Evidence | SHA | Finding Candidate |`
    - Every row in the Per-Function Verdict Table cites the 7-char SHA `67031e7d`
    - Every row cites a File:Line anchor starting with `contracts/modules/DegenerusGameDecimatorModule.sol:`
    - Every row's verdict is exactly one of the strings `SAFE`, `SAFE-INFO`, `VULNERABLE`, or `DEFERRED` (per D-02)
    - Every row's Finding Candidate cell is exactly `Y` or `N` (per D-13)
    - At least one row per target function + emit site: `claimDecimatorJackpot` (gameOver emit site), `claimDecimatorJackpot` (normal split emit site), `claimTerminalDecimatorJackpot` (terminal emit site), `DecimatorClaimed` event declaration, `TerminalDecimatorClaimed` event declaration (5 distinct emit/declaration anchors — expected minimum 11 verdict rows total since each emit site gets CEI + argument correctness rows, each event declaration gets 1 SAFE-INFO row)
    - `grep -cE "SAFE|SAFE-INFO|VULNERABLE|DEFERRED" .planning/phases/232-decimator-audit/232-02-AUDIT.md` returns at least 11
    - `grep -c "67031e7d" .planning/phases/232-decimator-audit/232-02-AUDIT.md` returns at least 11 (one per verdict row minimum)
    - The string `F-29-NN` does NOT appear in the file (per D-13)
    - The string `F-29-` does NOT appear in any form in the file (Phase 236 owns ID assignment per D-13)
    - CEI-Position Analysis section contains exactly three labeled emit-site subsections (gameOver fast-path, normal ETH/lootbox split, terminal path)
    - Event-Argument Correctness Analysis section contains exactly two labeled event subsections (`DecimatorClaimed`, `TerminalDecimatorClaimed`)
    - Indexer-Compatibility Observation section references `v28.0 Phase 227` verbatim and explicitly states it is a READ-only OBSERVATION (not a finding) per D-10
    - Downstream Hand-offs subsection explicitly names `Phase 236 FIND-01` (per D-13)
    - Downstream Hand-offs subsection explicitly names `Phase 236 FIND-02` (per D-10 indexer-compat KNOWN-ISSUES candidate)
    - Event-argument correctness analysis explicitly mentions `ethPortion + lootboxPortion == amountWei` as an invariant
    - Event-argument correctness analysis explicitly mentions `lastTerminalDecClaimRound.lvl` as the storage-sourced (not caller-controlled) source of `TerminalDecimatorClaimed.lvl`
    - No placeholder line numbers like `:<line>` remain in the final file — every File:Line is a real integer or integer range
    - No contract source content read from outside `contracts/` directory (per user memory `feedback_contract_locations.md`)
    - Zero `database/` repo files are modified or referenced with a write path — `database/` is READ-only context only per D-10 + v29.0 PROJECT.md scoping
    - `git status --porcelain contracts/ test/` returns empty output both before and after task execution (READ-only v29.0 milestone constraint; zero writes to production code)
  </acceptance_criteria>
  <done>`232-02-AUDIT.md` exists with per-function verdict table covering both `67031e7d` functions + the three new emit sites + two new event declarations. Every row cites SHA `67031e7d` and a real File:Line in `contracts/modules/DegenerusGameDecimatorModule.sol`. Verdicts use `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` vocabulary. D-08 (CEI position for each emit site) and D-09 (event-argument correctness invariants) attack vectors all covered. Dedicated CEI-Position Analysis block has three emit-site subsections; Event-Argument Correctness Analysis block has two event subsections; Indexer-Compatibility Observation records v28.0 Phase 227 gap as READ-only observation per D-10. Findings-Candidate / Scope-guard Deferrals / Downstream Hand-offs subsections present; Phase 236 FIND-01 and FIND-02 hand-offs explicit. Zero F-29-NN emissions (per D-13). Zero contracts/, test/, or database/ writes (READ-only milestone rule).</done>
</task>

</tasks>

<verification>
- `232-02-AUDIT.md` exists with per-function verdict table covering both functions in 230-01-DELTA-MAP.md §1.3 owned by `67031e7d` + the three new emit sites + two new event declarations
- Locked column set per D-02 (`Function | File:Line | Attack Vector | Verdict | Evidence | SHA`) + `Finding Candidate` column per D-13
- Every verdict row cites `67031e7d`
- Every File:Line anchor resolves into `contracts/modules/DegenerusGameDecimatorModule.sol` at HEAD
- Verdicts exclusively use `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (per D-02)
- Every row carries `Y` or `N` in Finding Candidate column (per D-13)
- D-08 CEI-position attack vectors covered for all 3 emit sites
- D-09 event-argument correctness attack vectors covered for both events (`ethPortion+lootboxPortion==amountWei`; `lvl` sourcing; `player==msg.sender`; terminal `lvl` from storage; terminal `amountWei` from consumed return value)
- Indexer-compat OBSERVATION present per D-10, references v28.0 Phase 227, explicitly NOT a contract-side finding
- Findings-Candidate / Scope-guard Deferrals / Downstream Hand-offs subsections present
- Hand-offs explicitly name Phase 236 FIND-01 (per D-13) and Phase 236 FIND-02 (per D-10 indexer-compat KNOWN-ISSUES candidate)
- Zero `F-29-NN` references anywhere in the file (per D-13)
- Zero writes to `contracts/`, `test/`, or `database/` during plan execution (READ-only v29.0 milestone rule)
</verification>

<success_criteria>
`DegenerusGameDecimatorModule.claimDecimatorJackpot` and `DegenerusGameDecimatorModule.claimTerminalDecimatorJackpot` have `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` verdicts against the D-08 (CEI position at each of 3 emit sites) and D-09 (event-argument correctness: `ethPortion + lootboxPortion == amountWei`, `lvl` sourcing, `player == msg.sender`, terminal `lvl` from storage, terminal `amountWei` from consumed) attack vectors. Every verdict is traceable via SHA + File:Line. Every row carries `Finding Candidate: Y/N` per D-13. Indexer-compat observation per D-10 routes to Phase 236 FIND-02 KNOWN-ISSUES candidate without any `database/` writes. Phase 230 delta-map is not edited; any out-of-scope discovery is recorded in Scope-guard Deferrals per D-05. READ-only milestone rule honored: zero writes to `contracts/`, `test/`, or `database/`.
</success_criteria>

<output>
After completion, create `.planning/phases/232-decimator-audit/232-02-SUMMARY.md`
</output>
</content>
</invoke>