---
phase: 211-test-suite-repair
plan: 07
type: execute
wave: 2
depends_on: [211-05, 211-06]
files_modified: []
autonomous: true
requirements: [VER-02]

must_haves:
  truths:
    - "Foundry test suite has zero new failures (all 358+ tests pass except FuturepoolSkim)"
    - "Hardhat test suite maintains zero failures (1233+ pass)"
  artifacts:
    - path: ".planning/phases/211-test-suite-repair/211-07-forge-test-results.txt"
      provides: "Full Foundry test output proving zero failures"
    - path: ".planning/phases/211-test-suite-repair/211-07-hardhat-test-results.txt"
      provides: "Full Hardhat test output proving zero failures"
  key_links:
    - from: "test/fuzz/*.t.sol"
      to: "contracts/**/*.sol"
      via: "Foundry test suite execution"
      pattern: "forge test"
---

<objective>
Re-run both test suites to confirm all 46 previously-failing Foundry tests are now fixed and no regressions were introduced. This is the verification gate for VER-02.

Purpose: Prove the test suite repair is complete — zero new failures in both Foundry and Hardhat.
Output: Test result artifacts showing green suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/211-test-suite-repair/211-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full Foundry test suite and verify zero failures</name>
  <files></files>
  <read_first>
    - .planning/phases/211-test-suite-repair/211-04-forge-test-results.txt (lines 1829-1892 — the 46 failing tests to verify are now fixed)
  </read_first>
  <action>
1. Run `node scripts/lib/patchForFoundry.js` to patch ContractAddresses.sol for Foundry.

2. Run the full Foundry test suite:
   ```bash
   forge test --skip FuturepoolSkim -v 2>&1 | tee .planning/phases/211-test-suite-repair/211-07-forge-test-results.txt
   ```

3. Verify the output:
   - Count total passing, failing, and skipped tests
   - Confirm zero FAIL entries (excluding FuturepoolSkim which is skipped)
   - Specifically verify these 46 previously-failing tests now show PASS:
     * 27 TicketLifecycle level-advancement tests
     * 8 AffiliateDgnrsClaim E() tests
     * 1 VRFLifecycle levelAdvancement test
     * 1 FarFutureIntegration FF tickets test
     * 2 VRFStallEdgeCases tests (midDay + backfill)
     * 1 StallResilience stallSwapResume test
     * 3 DegeneretteFreezeResolution tests
     * 2 LootboxBoonCoexistence tests
     * 1 RedemptionGas claimRedemption test

4. Restore ContractAddresses.sol: `git checkout contracts/ContractAddresses.sol`

5. If ANY test fails, report the failure details but do NOT attempt fixes (that would be a new plan).
  </action>
  <verify>
    <automated>grep -c "FAIL" .planning/phases/211-test-suite-repair/211-07-forge-test-results.txt | xargs -I{} test {} -eq 0 && echo "ZERO FAILURES" || echo "FAILURES FOUND"</automated>
  </verify>
  <acceptance_criteria>
    - Zero FAIL lines in Foundry output (FuturepoolSkim excluded via --skip)
    - Total passing count >= 358 (was 312 pass + 46 fail)
    - Test result file saved to 211-07-forge-test-results.txt
  </acceptance_criteria>
  <done>Foundry suite: 358+ passing, 0 failing. All 46 previously-failing tests now pass.</done>
</task>

<task type="auto">
  <name>Task 2: Run full Hardhat test suite and verify no regressions</name>
  <files></files>
  <read_first>
    - .planning/phases/211-test-suite-repair/211-04-hardhat-test-results.txt (last 10 lines — verify baseline: 1233 passing)
  </read_first>
  <action>
1. Run the full Hardhat test suite:
   ```bash
   npx hardhat test 2>&1 | tee .planning/phases/211-test-suite-repair/211-07-hardhat-test-results.txt
   ```

2. Verify the output:
   - Count total passing, failing, and pending tests
   - Confirm zero failures
   - Confirm passing count >= 1233 (baseline from 211-04)

3. If ANY test fails, report the failure details but do NOT attempt fixes.
  </action>
  <verify>
    <automated>grep -c "failing" .planning/phases/211-test-suite-repair/211-07-hardhat-test-results.txt | xargs -I{} test {} -eq 0 && echo "ZERO FAILURES" || echo "FAILURES FOUND"</automated>
  </verify>
  <acceptance_criteria>
    - Zero "failing" lines in Hardhat output
    - Total passing count >= 1233 (baseline)
    - Test result file saved to 211-07-hardhat-test-results.txt
  </acceptance_criteria>
  <done>Hardhat suite: 1233+ passing, 0 failing. No regressions from test file modifications.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — verification only, no code modifications.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | Verification-only plan, no threats |
</threat_model>

<verification>
Both test suites must show zero failures:

```bash
# Foundry
grep -E "passed|failed" .planning/phases/211-test-suite-repair/211-07-forge-test-results.txt | tail -5

# Hardhat
grep -E "passing|failing" .planning/phases/211-test-suite-repair/211-07-hardhat-test-results.txt | tail -5
```
</verification>

<success_criteria>
- Foundry: 358+ passing, 0 failing (up from 312/46)
- Hardhat: 1233+ passing, 0 failing (maintained)
- VER-02 requirement satisfied: both test suites pass with zero new regressions
</success_criteria>

<output>
After completion, create `.planning/phases/211-test-suite-repair/211-07-SUMMARY.md`
</output>
