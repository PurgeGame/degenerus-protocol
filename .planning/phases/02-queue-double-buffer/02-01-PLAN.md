---
phase: 02-queue-double-buffer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/storage/DegenerusGameStorage.sol
  - contracts/modules/DegenerusGameJackpotModule.sol
  - contracts/modules/DegenerusGameMintModule.sol
  - contracts/DegenerusGame.sol
  - test/fuzz/QueueDoubleBuffer.t.sol
autonomous: true
requirements: [QUEUE-01, QUEUE-02, QUEUE-03]

must_haves:
  truths:
    - "Every _queueTickets* function uses _tqWriteKey() for mapping keys, not raw targetLevel/lvl"
    - "Every processing function uses _tqReadKey() for mapping keys, not raw lvl"
    - "_swapTicketSlot() reverts when read slot is non-empty (confirmed from Phase 1)"
    - "Far-future sampling and view functions use _tqWriteKey() for correct buffer reads"
    - "Events still emit logical levels, not keyed levels"
  artifacts:
    - path: "contracts/storage/DegenerusGameStorage.sol"
      provides: "Write-path key substitution in _queueTickets, _queueTicketsScaled, _queueTicketRange"
      contains: "_tqWriteKey(targetLevel)"
    - path: "contracts/modules/DegenerusGameJackpotModule.sol"
      provides: "Read-path key substitution in processTicketBatch, _processOneTicketEntry, _resolveZeroOwedRemainder, far-future sampling"
      contains: "_tqReadKey(lvl)"
    - path: "contracts/modules/DegenerusGameMintModule.sol"
      provides: "Read-path key substitution in processFutureTicketBatch"
      contains: "_tqReadKey(lvl)"
    - path: "contracts/DegenerusGame.sol"
      provides: "Write-key substitution in _pickWinnersFromHistory, ticketsOwedView, getPlayerPurchases"
      contains: "_tqWriteKey"
    - path: "test/fuzz/QueueDoubleBuffer.t.sol"
      provides: "Unit tests proving write-key and read-key isolation"
      min_lines: 150
  key_links:
    - from: "contracts/storage/DegenerusGameStorage.sol (_queueTickets)"
      to: "_tqWriteKey"
      via: "local variable wk = _tqWriteKey(targetLevel)"
      pattern: "_tqWriteKey\\(targetLevel\\)"
    - from: "contracts/modules/DegenerusGameJackpotModule.sol (processTicketBatch)"
      to: "_tqReadKey"
      via: "local variable rk = _tqReadKey(lvl)"
      pattern: "_tqReadKey\\(lvl\\)"
    - from: "contracts/modules/DegenerusGameMintModule.sol (processFutureTicketBatch)"
      to: "_tqReadKey"
      via: "local variable rk = _tqReadKey(lvl)"
      pattern: "_tqReadKey\\(lvl\\)"
---

<objective>
Migrate all ticket queue mapping accesses to use double-buffer key helpers: write-path functions use _tqWriteKey(), processing functions use _tqReadKey(), and far-future/view functions use _tqWriteKey(). Create unit tests proving buffer isolation.

Purpose: Enables concurrent purchase writing and ticket processing by routing writes and reads to different buffer slots.
Output: Modified DegenerusGameStorage.sol (3 write functions), JackpotModule.sol (4 functions), MintModule.sol (1 function), DegenerusGame.sol (3 functions), QueueDoubleBuffer.t.sol test file.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-queue-double-buffer/02-RESEARCH.md
@.planning/phases/01-storage-foundation/01-01-SUMMARY.md
@.planning/phases/01-storage-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From contracts/storage/DegenerusGameStorage.sol:
```solidity
uint24 internal constant TICKET_SLOT_BIT = 1 << 23;

function _tqWriteKey(uint24 level) internal view returns (uint24) {
    return ticketWriteSlot != 0 ? level | TICKET_SLOT_BIT : level;
}

function _tqReadKey(uint24 level) internal view returns (uint24) {
    return ticketWriteSlot == 0 ? level | TICKET_SLOT_BIT : level;
}

function _swapTicketSlot(uint24 purchaseLevel) internal { ... }
// Reverts with E() if ticketQueue[_tqReadKey(purchaseLevel)].length > 0
// Toggles ticketWriteSlot ^= 1, sets ticketsFullyProcessed = false
```

From test/fuzz/StorageFoundation.t.sol:
```solidity
contract StorageHarness is DegenerusGameStorage {
    function exposed_tqWriteKey(uint24 lvl) external view returns (uint24);
    function exposed_tqReadKey(uint24 lvl) external view returns (uint24);
    function exposed_swapTicketSlot(uint24 purchaseLevel) external;
    function setTicketWriteSlot(uint8 val) external;
    function getTicketWriteSlot() external view returns (uint8);
    function getTicketsFullyProcessed() external view returns (bool);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write-path key migration in DegenerusGameStorage + read-path key migration in JackpotModule and MintModule</name>
  <files>
    contracts/storage/DegenerusGameStorage.sol,
    contracts/modules/DegenerusGameJackpotModule.sol,
    contracts/modules/DegenerusGameMintModule.sol,
    contracts/DegenerusGame.sol
  </files>
  <behavior>
    QUEUE-01 write-path:
    - _queueTickets: ticketsOwedPacked[wk] and ticketQueue[wk] where wk = _tqWriteKey(targetLevel); events keep logical targetLevel
    - _queueTicketsScaled: same pattern as _queueTickets
    - _queueTicketRange: wk = _tqWriteKey(lvl) inside loop; ticketsOwedPacked[wk] and ticketQueue[wk]

    QUEUE-02 read-path:
    - processTicketBatch (JackpotModule): rk = _tqReadKey(lvl) at top; ticketQueue[rk] x3, pass rk to _processOneTicketEntry and _resolveZeroOwedRemainder
    - _processOneTicketEntry (JackpotModule): add uint24 rk parameter; ticketsOwedPacked[rk] instead of ticketsOwedPacked[lvl]; keep lvl for roll salt
    - _resolveZeroOwedRemainder (JackpotModule): add uint24 rk parameter; ticketsOwedPacked[rk] x3
    - Any other JackpotModule private helper that accesses ticketsOwedPacked[lvl] in the batch processing chain (line ~2112): add rk parameter
    - processFutureTicketBatch (MintModule): rk = _tqReadKey(lvl) at top; ticketQueue[rk] x3, ticketsOwedPacked[rk] x7

    Far-future and view functions (DegenerusGame.sol + JackpotModule.sol):
    - _awardFarFutureCoinJackpot (JackpotModule:2552): ticketQueue[_tqWriteKey(candidate)] instead of ticketQueue[candidate]
    - _pickWinnersFromHistory (DegenerusGame.sol:2742 area): ticketQueue[_tqWriteKey(candidate)]
    - ticketsOwedView (DegenerusGame.sol:2061): ticketsOwedPacked[_tqWriteKey(lvl)]
    - getPlayerPurchases (DegenerusGame.sol:2742 area): ticketsOwedPacked[_tqWriteKey(level)]
  </behavior>
  <action>
    **Write-path (QUEUE-01) in DegenerusGameStorage.sol:**

    1. `_queueTickets` (line 539): After the early return and event emission, add `uint24 wk = _tqWriteKey(targetLevel);`. Replace line 546 `ticketsOwedPacked[targetLevel][buyer]` with `ticketsOwedPacked[wk][buyer]`. Replace line 550 `ticketQueue[targetLevel].push(buyer)` with `ticketQueue[wk].push(buyer)`. Replace line 560 `ticketsOwedPacked[targetLevel][buyer]` with `ticketsOwedPacked[wk][buyer]`. Total: 3 replacements.

    2. `_queueTicketsScaled` (line 571): After early return and event, add `uint24 wk = _tqWriteKey(targetLevel);`. Replace line 578 `ticketsOwedPacked[targetLevel]` with `ticketsOwedPacked[wk]`. Replace line 582 `ticketQueue[targetLevel]` with `ticketQueue[wk]`. Replace line 617 `ticketsOwedPacked[targetLevel]` with `ticketsOwedPacked[wk]`. Total: 3 replacements.

    3. `_queueTicketRange` (line 627): Inside the for-loop, add `uint24 wk = _tqWriteKey(lvl);` as the first line after `for`. Replace line 636 `ticketsOwedPacked[lvl]` with `ticketsOwedPacked[wk]`. Replace line 640 `ticketQueue[lvl]` with `ticketQueue[wk]`. Replace line 650 `ticketsOwedPacked[lvl]` with `ticketsOwedPacked[wk]`. Total: 3 replacements per iteration.

    **Read-path (QUEUE-02) in JackpotModule.sol:**

    4. `processTicketBatch` (line 1916): Add `uint24 rk = _tqReadKey(lvl);` after the function signature. Replace all `ticketQueue[lvl]` with `ticketQueue[rk]` (~3 accesses). Pass `rk` as additional parameter to `_processOneTicketEntry` and `_resolveZeroOwedRemainder` calls.

    5. `_processOneTicketEntry` (line 2010): Add `uint24 rk` parameter to function signature (alongside existing `lvl`). Replace `ticketsOwedPacked[lvl]` with `ticketsOwedPacked[rk]`. Keep `lvl` for roll salt computation unchanged.

    6. `_resolveZeroOwedRemainder` (line 1981): Add `uint24 rk` parameter. Replace all `ticketsOwedPacked[lvl]` with `ticketsOwedPacked[rk]` (~3 accesses).

    7. Check line ~2112 for any other private helper that accesses `ticketsOwedPacked[lvl]` in the processing chain -- if found, add `rk` parameter and substitute.

    **Read-path (QUEUE-02) in MintModule.sol:**

    8. `processFutureTicketBatch` (line 294): Add `uint24 rk = _tqReadKey(lvl);` near the top. Replace all `ticketQueue[lvl]` with `ticketQueue[rk]` (~3 accesses). Replace all `ticketsOwedPacked[lvl]` with `ticketsOwedPacked[rk]` (~7 accesses).

    **Far-future and view functions:**

    9. `_awardFarFutureCoinJackpot` (JackpotModule ~line 2552): Replace `ticketQueue[candidate]` with `ticketQueue[_tqWriteKey(candidate)]`. Use write key per research recommendation -- these are future levels where new purchases land.

    10. `_pickWinnersFromHistory` (DegenerusGame.sol ~line 2680): Replace `ticketQueue[candidate]` with `ticketQueue[_tqWriteKey(candidate)]`.

    11. `ticketsOwedView` (DegenerusGame.sol line 2061): Replace `ticketsOwedPacked[lvl]` with `ticketsOwedPacked[_tqWriteKey(lvl)]`.

    12. `getPlayerPurchases` (DegenerusGame.sol line 2742): Replace `ticketsOwedPacked[level]` with `ticketsOwedPacked[_tqWriteKey(level)]`.

    **Critical invariants to preserve:**
    - Events emit LOGICAL levels (targetLevel, lvl), NOT keyed levels
    - ticketCursor and ticketLevel track logical levels, NOT keyed levels
    - Roll salt in _processOneTicketEntry uses lvl (logical), NOT rk
    - Within any single function, ticketQueue and ticketsOwedPacked MUST use the SAME key
  </action>
  <verify>
    <automated>forge clean && forge build 2>&1 | tail -5</automated>
  </verify>
  <done>All 3 write-path functions use _tqWriteKey(), all processing functions use _tqReadKey(), far-future/view functions use _tqWriteKey(). grep -c 'ticketQueue\[targetLevel\]\|ticketsOwedPacked\[targetLevel\]' contracts/storage/DegenerusGameStorage.sol returns 0. grep -c 'ticketQueue\[lvl\]\|ticketsOwedPacked\[lvl\]' contracts/modules/DegenerusGameJackpotModule.sol returns 0. grep -c 'ticketQueue\[lvl\]\|ticketsOwedPacked\[lvl\]' contracts/modules/DegenerusGameMintModule.sol returns 0. forge build succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for write-key and read-key buffer isolation</name>
  <files>test/fuzz/QueueDoubleBuffer.t.sol</files>
  <behavior>
    - testQueueTicketsUsesWriteKey: queue tickets at level X, verify ticketQueue[_tqWriteKey(X)] has entry, ticketQueue[_tqReadKey(X)] is empty
    - testQueueTicketsScaledUsesWriteKey: same pattern with scaled tickets
    - testQueueTicketRangeUsesWriteKey: same pattern with range queue
    - testProcessTicketBatchUsesReadKey: queue tickets, swap buffers, verify processTicketBatch reads from old write key (now read key)
    - testProcessFutureTicketBatchUsesReadKey: similar to above but for MintModule's processFutureTicketBatch
    - testSwapTicketSlotRevertsNonEmpty: already exists in Phase 1 tests (QUEUE-03 confirmed)
    - testWriteReadIsolation: queue tickets in slot 0, swap, queue more (now slot 1), verify both buffers have separate entries
  </behavior>
  <action>
    Create `test/fuzz/QueueDoubleBuffer.t.sol` with a QueueHarness contract that extends DegenerusGameStorage and exposes:
    - `exposed_queueTickets(address, uint24, uint32)` wrapping `_queueTickets`
    - `exposed_queueTicketsScaled(address, uint24, uint32)` wrapping `_queueTicketsScaled`
    - `exposed_queueTicketRange(address, uint24, uint24, uint32)` wrapping `_queueTicketRange`
    - `exposed_swapTicketSlot(uint24)` wrapping `_swapTicketSlot`
    - `getQueueLength(uint24 key)` returning `ticketQueue[key].length`
    - `getTicketsOwed(uint24 key, address buyer)` returning `ticketsOwedPacked[key][buyer]`
    - `exposed_tqWriteKey(uint24)` and `exposed_tqReadKey(uint24)` for key computation

    Note: processTicketBatch and processFutureTicketBatch are in modules (JackpotModule, MintModule) which inherit DegenerusGameStorage via DegenerusGame. Testing them directly requires either: (a) a harness that inherits the module, or (b) grep verification that they use _tqReadKey. Given the complexity of module state setup (delegatecall, game state), use approach (b) for QUEUE-02: rely on grep verification + write-key tests that prove buffer isolation. The harness tests prove that queue writes land in the correct buffer and that after a swap the buffers are isolated.

    Write tests:
    1. `testQueueTicketsUsesWriteKey`: Default ticketWriteSlot=0. Queue tickets at level 5. Assert `getQueueLength(_tqWriteKey(5)) == 1` and `getQueueLength(_tqReadKey(5)) == 0`. Also check ticketsOwedPacked at the write key.
    2. `testQueueTicketsScaledUsesWriteKey`: Same pattern with _queueTicketsScaled.
    3. `testQueueTicketRangeUsesWriteKey`: Queue range at levels 5-7. Assert write-key queues populated, read-key queues empty.
    4. `testWriteReadIsolation`: Queue at level 5 (slot 0). Swap. Queue at level 5 again (now slot 1). Assert both `ticketQueue[5]` and `ticketQueue[5 | TICKET_SLOT_BIT]` have entries. Assert they are in different buffers.
    5. `testSwapResetsFullyProcessed`: Queue, swap (draining first), verify ticketsFullyProcessed is false after swap.
    6. `testQueueAfterSwapUsesNewWriteKey`: Swap slot. Queue tickets. Verify they land in the new write buffer (keyed slot), not the old one.

    Use `forge test --match-path "test/fuzz/QueueDoubleBuffer.t.sol" -vvv` to verify.
  </action>
  <verify>
    <automated>forge test --match-path "test/fuzz/QueueDoubleBuffer.t.sol" -vvv 2>&1 | tail -20</automated>
  </verify>
  <done>All 6+ tests pass. Tests prove: (1) write functions route to write buffer, (2) after swap write functions route to new buffer, (3) read and write buffers are isolated for the same logical level. QUEUE-03 already covered by Phase 1 test testSwapTicketSlotRevertsNonEmpty.</done>
</task>

</tasks>

<verification>
Phase-level verification after both tasks:

1. **QUEUE-01 grep check:** `grep -c 'ticketQueue\[targetLevel\]\|ticketsOwedPacked\[targetLevel\]' contracts/storage/DegenerusGameStorage.sol` returns 0
2. **QUEUE-02 grep check:** `grep -c 'ticketQueue\[lvl\]\|ticketsOwedPacked\[lvl\]' contracts/modules/DegenerusGameJackpotModule.sol contracts/modules/DegenerusGameMintModule.sol` returns 0
3. **QUEUE-03 existing test:** `forge test --match-test "testSwapTicketSlotRevertsNonEmpty" -vvv` passes (from Phase 1)
4. **Full suite:** `forge test` passes with zero failures
5. **Build:** `forge clean && forge build` succeeds
</verification>

<success_criteria>
- Zero direct `ticketQueue[targetLevel]` or `ticketsOwedPacked[targetLevel]` in DegenerusGameStorage.sol write functions
- Zero direct `ticketQueue[lvl]` or `ticketsOwedPacked[lvl]` in JackpotModule and MintModule processing functions
- All QueueDoubleBuffer tests pass proving buffer isolation
- Full forge test suite green
- Events still emit logical levels (not keyed)
</success_criteria>

<output>
After completion, create `.planning/phases/02-queue-double-buffer/02-01-SUMMARY.md`
</output>
