---
phase: 393-permissionless-composition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/393-permissionless-composition/393-01-COUNCIL-PROMPT-ACCESS.md
  - .planning/phases/393-permissionless-composition/council/access.gemini.txt
  - .planning/phases/393-permissionless-composition/council/access.codex.txt
  - .planning/phases/393-permissionless-composition/council/access.council.json
  - .planning/phases/393-permissionless-composition/393-01-COUNCIL-NET.md
autonomous: true
requirements: [ACCESS-01, ACCESS-02, ACCESS-03, ACCESS-04, ACCESS-05]
user_setup: []

must_haves:
  truths:
    - "The cross-model council (NET 1) is on record for the full PERMISSIONLESS-COMPOSITION slice (the new permissionless decimator/redemption/carry entrypoints + the two keeper bounties + the forced-claim-timing surface + the partial-balance burst-solvency leg accounting + the freeze/rngLocked/liveness/gameOver gate intactness + the ETH/stETH reentrancy across the widened entrypoints + the widened claim-loop gas)"
    - "The council prompt is charged neutrally against the frozen subject a8b702a7, with the by-design rulings (lootbox-resolution/permissionless-claim TIMING, operator-approval IS the trust boundary, Degenerette/WWXRP RTP, the genesis admin self-break) excluded so the council does not waste passes on settled intent, and with the exact frozen ACCESS line-cites supplied so the council reads the right code"
    - "ACCESS-02 (the keeper box-bounty) is charged HARD as a dedicated named break-target demanding a REAL economic argument against REAL prevailing gas (5-50+ gwei, NOT the 0.5-gwei AUTO_GAS_PRICE_REF peg) PLUS flip-credit illiquidity (the bounty is BURNIE flip-credit, x0.5 survive-flip x peg discount) AND an un-manufacturability proof (the bounty-eligible box exists only because a real burn created it) — not hand-waving"
    - "ACCESS-04 (partial-balance burst solvency) is charged HARD as a dedicated named break-target demanding a real same-block multi-claim ETH+stETH leg-accounting argument under the MAX(175%) reservation — does the segregation accounting (_pendingRedemptionEthValue release == total leg movement) ever strand ETH or under-pull stETH across many partial-balance live claims in one block?"
    - "The doctrine is encoded: a permissionless caller must credit ONLY the beneficiary (no third-party ETH push / forced-credit grief); a keeper bounty must be net-NEGATIVE vs real gas + un-manufacturable; forced claim-TIMING is by-design UNLESS adjacent-level reward-magnitude divergence makes a forced resolution materially harmful (verify the magnitude, not the timing model)"
    - "The inherited cross-refs are encoded with their already-adjudicated solvency/ECON halves so the council attacks the ACCESS half consistently: FC-390-03 (mid-batch gameOver/frozen boundary — solvency REFUTED at 390), FC-390-06 (keeper-bounty creditFlip BURNIE issuance bound — solvency REFUTED at 390, ACCESS-02 downstream-dilution half owned here), FC-392-08 (redemption ETH-spin pool RMW + recirc — solvency/CEI REFUTED at 390, ECON cap-RMW BY-DESIGN at 392, permissionless-race half owned here), FC-392-20 (widened claim-loop gas — INFO at 392, fresh worst-case half owned here as FC-393-04)"
    - "Either both gemini+codex outputs exist, OR the unavailable CLI is recorded in the council.json skipped[] (never fatal; codex has been usage-capped since 392 — record faithfully and flag a post-reset codex re-run to 396)"
    - "The subject is byte-frozen throughout (git diff a8b702a7 -- contracts/ empty after the council fan-out)"
  artifacts:
    - path: ".planning/phases/393-permissionless-composition/393-01-COUNCIL-PROMPT-ACCESS.md"
      provides: "Neutral council prompt for the ACCESS-01..05 slice + the FC-393-01..04 owned leads + the inherited cross-refs (FC-390-03, FC-390-06, FC-392-08, FC-392-20)"
      min_lines: 50
    - path: ".planning/phases/393-permissionless-composition/393-01-COUNCIL-NET.md"
      provides: "Council-net capture record: raw output paths, available/skipped, byte-freeze attestation, NET 1 ON RECORD line"
      min_lines: 20
  key_links:
    - from: "393-01-COUNCIL-PROMPT-ACCESS.md"
      to: "council.sh --label access"
      via: "council.sh fan-out to gemini+codex"
      pattern: "council\\.sh.*--label access"
    - from: "393-01-COUNCIL-NET.md"
      to: "council/access.council.json"
      via: "manifest available/skipped capture"
      pattern: "access\\.council\\.json"
---

<objective>
Run NET 1 (the cross-model council) over the PERMISSIONLESS-COMPOSITION slice — the new and widened
permissionless / keeper entrypoints of the post-v62 change set (the permissionless
`claimDecimatorJackpot(address,uint24)` / `claimDecimatorJackpotMany` / `claimRedemptionMany` / the new
`claimCoinflipCarry`), the two keeper box-bounties, the forced-claim-timing surface on the winner, the
partial-balance redemption-leg burst solvency, the freeze/rngLocked/liveness/gameOver gate intactness on
every new/widened entrypoint, the ETH/stETH reentrancy across the legs, and the widened claim-loop gas —
against the byte-frozen subject `a8b702a7`. Author ONE neutral council prompt for the ACCESS-01..05 slice
(plus the FC-393-01..04 owned leads and the inherited cross-refs FC-390-03, FC-390-06, FC-392-08,
FC-392-20), fan it to the gemini+codex CLIs via `council.sh`, capture the raw output under the phase
`council/` dir, and record the council-net capture (available/skipped, byte-freeze attestation).

Purpose: per AUDIT-V63-PLAN §2, a no-finding verdict for any sweep slice requires BOTH nets on record.
Access-control / reentrancy / MEV is the LOW/confirmatory threat class (§4) — but the SUBSTANTIVE items in
this slice are ACCESS-02 (keeper-bounty economics vs real gas) and ACCESS-04 (partial-balance burst
solvency), where a real grief/faucet/steer or a burst-solvency strand weighs higher. The council — the
PRIMARY finder ([[cross-model-led-audits-over-claude-only]], the council caught the V62-01/02/03 classes
Claude-only missed) — goes on record FIRST so the Wave-2 Claude-net + adjudication plan (393-02) can fold
the council leads in before any verdict. This phase CONSUMES the boundary results already adjudicated in
388-392 (it depends on 388-392); the inherited cross-refs reference those FINDINGS for consistency rather
than re-deriving the solvency/ECON halves.

Output: the COUNCIL-PROMPT-ACCESS doc, the raw gemini/codex output, the council.json manifest, and
393-01-COUNCIL-NET.md (the capture record). NO contract source is touched — this is read-only over the
subject; the council writes only to its out-dir.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/AUDIT-V63-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/phases/388-foundation-subject-freeze-green-baseline/388-02-FINDING-CANDIDATES.md
@.planning/v63-surface-map/permissionless-access.md

<interfaces>
<!-- council.sh contract (read from .planning/audit-v52/cross-model/bin/council.sh). -->
<!-- Usage: council.sh --out-dir DIR [--schema SCHEMA.json] --label NAME PROMPT_FILE -->
<!-- Produces in DIR: <label>.gemini.txt, <label>.codex.txt, <label>.council.json -->
<!-- (no --schema => .txt extension). council.json shape: -->
<!-- { "label", "schema", "models":[...], "skipped":[...], "skip_reasons":{...}, "outputs":{model:path} } -->
<!-- It runs gemini + codex in PARALLEL internally; a missing/unauthed/usage-capped CLI is recorded in -->
<!-- skipped[] (+ skip_reasons) and is NEVER fatal. NOTE: codex has been hard usage-capped since 392-01 -->
<!-- (it skipped 392-01..04); it will LIKELY skip again here — record faithfully, gemini + the Wave-2 -->
<!-- Claude net still satisfy the dual-NET, and flag a post-reset codex re-run → 396. -->

<!-- Council-prompt shape reference (the just-approved 390/391/392 slices): -->
<!--   .planning/phases/390-solvency-spine/390-01-COUNCIL-PROMPT-SOLV.md -->
<!--   .planning/phases/391-rng-spine/391-01-COUNCIL-PROMPT-RNG.md -->
<!--   .planning/phases/392-entropy-and-econ/392-02-COUNCIL-PROMPT-BURNIE.md -->
<!--   header role + "read EXACT frozen source at a8b702a7 via git show a8b702a7:contracts/<File>.sol" -->
<!--   + threat-priority line + trust-boundary framing + KNOWN-BY-DESIGN exclusion list + numbered -->
<!--   break-targets + per-finding output format (FINDING: PROPERTY · reachable CALL SEQUENCE · STATE -->
<!--   VAR + file:line at a8b702a7 · SEVERITY · why protections don't stop it | or VERIFIED SOUND + why). -->

<!-- The DegenerusGame.sol thin-dispatcher fact (permissionless-access.md preamble): every external fn is -->
<!-- a selector-matched (...).delegatecall(msg.data) stub into the owning module (e.g. -->
<!-- claimDecimatorJackpot(address,uint24) @DegenerusGame.sol:808; claimDecimatorJackpotMany @:832). -->
<!-- delegatecall preserves msg.sender + address(this), so module-side msg.sender is the EXTERNAL caller -->
<!-- and module-side external calls (coinflip.creditFlip) originate from the GAME address — the module -->
<!-- access gates (msg.sender != COIN/SDGNRS) + onlyFlipCreditors/onlyGame on callees stay correct. -->

<!-- Authoritative frozen ACCESS line-cites (verified via permissionless-access.md against a8b702a7 — -->
<!-- supply these so the council reads the right code; the council MUST re-read via git show, not trust blind): -->
<!--   DegenerusGameDecimatorModule.sol: permissionless claimDecimatorJackpot(address player,uint24 lvl) -->
<!--     @293 (credits player, never caller); claimDecimatorJackpotMany(address[],uint24) @325 (batch, -->
<!--     skips claimed/non-winner no-revert, caches over/frozen/offsets/totalBurn once @338-341); shared -->
<!--     core _claimDecimatorJackpotFor @385; both gate prizePoolFrozen @298; bounty BOX_BOUNTY_ETH_TARGET -->
<!--     = 15_000_000_000_000 wei per settled box paid !gameOver && settled!=0 as -->
<!--     (settled*TARGET*PRICE_COIN_UNIT)/_mintPriceInContext() BURNIE creditFlip to msg.sender @364-370; -->
<!--     _mintPriceInContext() @376 reproduces mintPrice() (priceForLevel(jackpotPhaseFlag?level:level+1)); -->
<!--     forced-timing: _rollTargetLevel(level+1, frozenSeed) + whale-pass _queueTicketRange(winner, -->
<!--     level+1,…) read the LIVE level @claim; frozen lootbox seed round.rngWord snapshot @277. -->
<!--   StakedDegenerusStonk.sol: claimRedemption(address player,uint24 day) @771 (permissionless LIVE, -->
<!--     self-only post-gameOver: isGameOver && player!=msg.sender reverts Unauthorized); -->
<!--     claimRedemptionMany(address[],uint24) @787 (batch, skips empty/non-self-post-game, bounty mirror -->
<!--     BOX_BOUNTY_ETH_TARGET creditFlip @803-814); dust-lootbox drop <MIN_REDEMPTION_LOOTBOX_ETH (0.01 -->
<!--     ETH) FORFEITED to sDGNRS own claimable @839-848/:897-901; partial-balance legs send -->
<!--     min(address(this).balance, legAmount) ETH + GAME pulls remainder as stETH @880/:888/:898; -->
<!--     _pendingRedemptionEthValue release; lootbox seed hash2(rngWord, player) rngWord=rngWordForDay(day+1) -->
<!--     @878; submit gate _submitGamblingClaimFrom rngWordForDay(currentPeriod)!=0 revert -->
<!--     BurnsBlockedBeforeDailyRng @991; post-gameover delete pendingRedemptions + lower -->
<!--     _pendingRedemptionEthValue BEFORE _payEth(.call) (CEI) @1098; Game callees resolveRedemptionLootbox -->
<!--     @926 (msg.sender!=SDGNRS gate) + creditRedemptionDirect @1004 (msg.sender!=SDGNRS gate). -->
<!--   BurnieCoinflip.sol: claimCoinflipCarry(address player,uint256 amount) @366 (player=_resolvePlayer -->
<!--     self/operator-approved, mints to player not caller; rngLocked gate @371; settles resolved days -->
<!--     first then pays from settled carry); COIN_CLAIM_DAYS 90→365; AUTO_REBUY_OFF_CLAIM_DAYS_MAX 1095→1460; -->
<!--     counters uint8→uint16 @136-138; deep walk uint32 remaining (safe 1460), shallow uint16 (safe 365); -->
<!--     onlyBurnieCoin tightened to COIN only (SDGNRS dropped); onlyFlipCreditors {GAME,QUESTS,AFFILIATE, -->
<!--     ADMIN,SDGNRS}; processCoinflipPayouts onlyDegenerusGameContract sDGNRS auto-settle deep=false -->
<!--     (bounded windowDays 365, ~1 day/advance). -->
<!--   DegenerusGame.sol: _payoutWithStethFallback @1888 reordered (53cd25cf) stETH-leg-out FIRST, ETH .call -->
<!--     LAST (closes V62-03 solvency-reentrancy); distributeYieldSurplus reachable only internally from -->
<!--     advanceGame (_distributeYieldSurplus AdvanceModule:529/783), no external stub, credits-only, only -->
<!--     when totalBal>obligations. -->
<!--   Already-adjudicated cross-ref halves (390/392 — supply so the council attacks the ACCESS half -->
<!--   consistently, NOT re-derive): FC-390-03 mid-batch gameOver/frozen boundary = REFUTED at 390 (batch -->
<!--   caches over/frozen once, resolution-into-claimable only, all value to winner); FC-390-06 keeper-bounty -->
<!--   issuance bound = REFUTED at 390 (per box ACTUALLY settled, (wallet,day) 160-ETH base cap + 50%/day -->
<!--   supply cap, decimator per real claimed entry — ACCESS-02 downstream BURNIE-dilution half owned HERE); -->
<!--   FC-392-08 redemption ETH-spin pool RMW + recirc = solvency/CEI REFUTED at 390 (flush-before-recirc, -->
<!--   stETH-remainder pulled in before credit, chunks sequential one frame) + ECON cap-RMW BY-DESIGN at 392 -->
<!--   (recirc depth 1 allowEthSpin=false, monotonic cap) — permissionless cross-chunk-race half owned HERE; -->
<!--   FC-392-20 widened claim-loop gas = INFO at 392 (bounded, caller-paid, off advanceGame chain) — fresh -->
<!--   worst-case under the new packed masked sub-word reads owned HERE as FC-393-04. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the neutral council prompt for the PERMISSIONLESS-COMPOSITION slice</name>
  <files>.planning/phases/393-permissionless-composition/393-01-COUNCIL-PROMPT-ACCESS.md</files>
  <read_first>
    - .planning/phases/390-solvency-spine/390-01-COUNCIL-PROMPT-SOLV.md AND .planning/phases/392-entropy-and-econ/392-02-COUNCIL-PROMPT-BURNIE.md (the just-approved prompt shape to match — header role, threat-priority line, trust-boundary framing, KNOWN-BY-DESIGN list, the dedicated-prime-target charge pattern, per-finding output format)
    - .planning/v63-surface-map/permissionless-access.md (the FULL §1-§5 + the net assessment — the "what we believe is safe about every new/widened permissionless entrypoint" thesis to break: §1 decimator claims + bounty + forced-timing residual lead, §2 redemption claims + bounty + reentrancy + the two residual leads (a forfeit-to-self / (b) partial-balance), §3 claimCoinflipCarry, §4 widened claim windows + gas, §5 incidental non-widening deltas)
    - .planning/phases/388-foundation-subject-freeze-green-baseline/388-02-FINDING-CANDIDATES.md (rows FC-393-01..04 verbatim + the inherited cross-refs FC-390-03, FC-390-06, FC-392-08, FC-392-20 — read the disposition/owner columns so the prompt charges the ACCESS half not the already-settled solvency/ECON half)
    - .planning/phases/390-solvency-spine/390-FINDINGS.md (§2b/§2c: FC-390-03 REFUTED + FC-390-06 REFUTED + FC-392-08 solvency/CEI REFUTED + FC-393-02 BY-DESIGN/REFUTED solvency-half + FC-393-03 REFUTED solvency-half — the already-adjudicated halves to state as the consistency anchor, with the ACCESS halves explicitly routed to 393)
    - .planning/phases/392-entropy-and-econ/392-FINDINGS-ECON.md + 392-FINDINGS-BURNIE.md (FC-392-08 ECON cap-RMW BY-DESIGN + permissionless-race → 393; FC-392-20 INFO + gas worst-case → 393 as FC-393-04 — the ECON/gas halves to state as the consistency anchor)
    - .planning/AUDIT-V63-PLAN.md §4 threat weighting (access/reentrancy/MEV = LOW/confirmatory; keeper-bounty uses REAL gas not the peg) + §5 design-intent anchor (permissionless/lootbox claim TIMING by-design; operator-approval IS the trust boundary)
    - the keeper-bounty doctrine [[feedback_bounty_exploit_uses_real_gas_not_peg_ref]] (compare reward to REAL prevailing gas 5-50+ gwei + flip-credit illiquidity, NOT the 0.5-gwei AUTO_GAS_PRICE_REF peg) + [[lootbox-resolution-timing-by-design]] + [[open-e-operator-approval-trust-boundary]] + [[genesis-admin-self-break-nonfinding]] + [[redemption-dust-lootbox-drop-bydesign]]
    - the frozen source for the prime sites via git show a8b702a7:contracts/modules/DegenerusGameDecimatorModule.sol (the permissionless claim @293, the batch @325-341, the shared core @385, the bounty @364-376, the forced-timing _rollTargetLevel/_queueTicketRange @283-322), contracts/StakedDegenerusStonk.sol (claimRedemption @771, claimRedemptionMany + bounty @787-814, the dust-forfeit @839-848/:897-901, the partial-balance legs @880/:888/:898, the CEI _payEth @1098, the day+1 read @878, the submit gate @991), contracts/BurnieCoinflip.sol (claimCoinflipCarry @366-371, the widened caps + counters @136-138, the auto-settle deep=false), contracts/modules/DegenerusGame.sol (the dispatcher stubs @808/:832, _payoutWithStethFallback @1888)
  </read_first>
  <action>
    Write 393-01-COUNCIL-PROMPT-ACCESS.md, charged NEUTRALLY ("here is what we believe is safe about every
    new and widened permissionless / keeper entrypoint in the post-v62 change set — find where a
    permissionless caller can grief, faucet, steer, or break a gate/reentrancy/burst-solvency property")
    and instructing the council to read the EXACT frozen source at `a8b702a7` via `git show
    a8b702a7:contracts/<File>.sol` (ignore the working tree). Use neutral defensive-engineering vocabulary
    throughout (beneficiary, caller, keeper, bounty, faucet, grief, steer, gate, reservation, leg, strand,
    reentrancy, manufacturability) per [[neutral-defensive-audit-vocabulary]].

    The prompt MUST include:
    (a) the threat-priority line from AUDIT-V63-PLAN §4 stated so the council knows access-control /
    reentrancy / MEV is the LOW/confirmatory class — BUT the two SUBSTANTIVE items are ACCESS-02
    (keeper-bounty economics vs real gas) and ACCESS-04 (partial-balance burst solvency), where a real
    grief/faucet/steer or a burst-solvency strand weighs higher (DOMINANT RNG/freeze audited at 391; HIGH
    gas-DoS only in the advanceGame chain — these claim loops are caller-paid + off that chain; SPINE =
    solvency, and the partial-balance leg accounting is solvency-adjacent here);
    (b) the trust-boundary / doctrine framing so the council does not waste passes: state the thin-dispatcher
    fact (every external fn is a selector-matched delegatecall stub; delegatecall preserves msg.sender +
    address(this), so module-side msg.sender is the EXTERNAL caller and the module access gates +
    onlyFlipCreditors/onlyGame on callees stay correct — confirm no stub mis-routes a permissionless
    entrypoint into a gated one or vice versa), state that a permissionless caller must credit ONLY the
    beneficiary (no third-party ETH push / forced-credit grief), that a keeper bounty must be net-NEGATIVE
    vs REAL prevailing gas AND un-manufacturable, and instruct the council to enumerate, for each new/widened
    external entrypoint: caller authorization (who gets the value), the freeze/rngLocked/liveness/gameOver
    gate, the ETH/stETH reentrancy ordering, and what a permissionless caller can grief/faucet/steer by
    choosing when/what to call;
    (c) a KNOWN-BY-DESIGN exclusion list so the council does not re-litigate settled intent
    ([[lootbox-resolution-timing-by-design]] permissionless economically-incentivized claim/open TIMING is
    NOT a player edge — the seed is frozen at request, so do NOT flag day/level/wait-to-open steering as a
    freshness or fairness bug PER SE; the council's forced-timing job is strictly the MAGNITUDE question of
    ACCESS-03/FC-393-01, below; [[open-e-operator-approval-trust-boundary]] operator-approval IS the trust
    boundary — do NOT model a "tricked into approving" actor; [[genesis-admin-self-break-nonfinding]] an
    admin breaking their OWN game at genesis with no engaged community is a NON-finding; [[redemption-dust-
    lootbox-drop-bydesign]] the dust-lootbox-drop-to-claimable <0.01 ETH is intended anti-dust-farm, NOT a
    bug — the forfeit-to-self job is strictly whether a keeper can EXTRACT per-victim value, not the drop
    itself; [[degenerette-wwxrp-rtp-by-design]] / [[intended-game-mechanics-not-findings]] RTP/EV are
    by-design — not the subject here);
    (d) the per-finding output format (FINDING: PROPERTY broken · reachable ordered CALL SEQUENCE [multi-tx
    / same-block burst / observe-then-retrigger where the ordering matters] · STATE VAR + file:line at
    a8b702a7 · SEVERITY per the threat priority · WHY the existing authorization / gate / CEI / reservation
    protections do not stop it | or VERIFIED SOUND/IDENTICAL: the property and the specific reason it holds
    — cite the beneficiary credit, the gate, the CEI ordering, the reservation bound, or the
    un-manufacturability — so the adjudicator can confirm).

    State the master thesis the slice rests on, mapped to ACCESS-01..05, as the claims to BREAK:
    "(ACCESS-01) every permissionless claim credits ONLY the beneficiary — claimDecimatorJackpot(player,…),
    claimRedemption(player,…), their Many batches, and claimCoinflipCarry(player,…) all forward value to
    `player` (or into the player's game-claimable / lootbox), never to msg.sender, so a third-party trigger
    pushes no ETH at the player and holds no exclusive timing edge — find any path that pushes a forced
    credit or ETH at a victim, or routes value to the caller; (ACCESS-02) both keeper box-bounties are
    net-NEGATIVE vs real prevailing gas AND un-manufacturable — the bounty is 0.000015 ETH of ILLIQUID
    BURNIE flip-credit per settled box (creditFlip → next-day stake that must survive a 50/50 flip ×0.5 ×
    peg discount), ~40x under-water at 20 gwei BEFORE the flip risk, and each settle-able box exists only
    because a real player BURNED (so no Sybil can manufacture boxes to farm) — find any way to fabricate
    bounty-eligible work or to make the bounty net-positive at any realistic gas; (ACCESS-03) forced
    claim-timing on the winner is INERT — frozen seeds bound the randomness, the timing class is by-design,
    so the ONLY question is whether any reward MAGNITUDE / target-level distribution diverges enough between
    ADJACENT levels (the live-level-dependent _rollTargetLevel(level+1) and whale-pass start level) to make
    a forced resolution materially harmful to the winner; (ACCESS-04) partial-balance redemption-leg
    solvency holds under same-block bursts — each leg sends min(balance, legAmount) ETH and pulls the rest
    as stETH, and the MAX(175%) reservation guarantees ETH+stETH ≥ rolled across many one-block claims
    without stranding ETH or under-pulling stETH; (ACCESS-05) the freeze/rngLocked/liveness/gameOver gates
    are intact on EVERY new/widened entrypoint and reentrancy is closed across the ETH/stETH legs — the
    decimator/redemption claims gate prizePoolFrozen, post-gameover redemption is self-claim-only, the CEI
    ordering (slot delete + ledger debit before untrusted .call; stETH-first/ETH-last; the 53cd25cf
    reorder) holds."

    Name the concrete break-targets. Charge ACCESS-02 (keeper-bounty economics) and ACCESS-04
    (partial-balance burst solvency) HARD as the two dedicated numbered prime items:

    1. ACCESS-02 (the keeper box-bounty — DEDICATED economic break-target, the substantive faucet item):
       both bounties are BOX_BOUNTY_ETH_TARGET = 15_000_000_000_000 wei (0.000015 ETH) of value per settled
       box, paid `!gameOver && settled != 0` as `(settled*TARGET*PRICE_COIN_UNIT)/_mintPriceInContext()`
       BURNIE flip-credit to msg.sender (DecimatorModule:364-370; StakedDegenerusStonk:803-814). DO NOT
       use the 0.5-gwei AUTO_GAS_PRICE_REF peg. Demand a REAL economic argument: (i) cost the per-box settle
       (the ~30k-gas settle + the lootbox-resolve delegatecall it does for a LIVE winner) at REAL prevailing
       gas (5/20/50+ gwei) and confirm the 0.000015-ETH reward is net-NEGATIVE BEFORE the 50% flip risk and
       the flip-credit illiquidity (creditFlip → _addDailyFlip next-day stake, must survive a 50/50 flip
       ×0.5, then the BURNIE→ETH peg discount ~0.59) — so the realized liquid value is a small fraction of
       0.000015 ETH while the gas is many multiples of it; (ii) prove the bounty is UN-MANUFACTURABLE — each
       settle-able box exists only because a real player BURNED (decimator: a real decimator-BURNIE burn;
       redemption: a real sDGNRS gambling-burn), so a Sybil cannot fabricate bounty-eligible work — couple
       this to FC-390-06 (the issuance is bounded: one box per real claim, the (wallet,day) 160-ETH base cap
       + 50%/day supply cap on the redemption side, decimator per real claimed entry — so a keeper cannot
       mint UNBOUNDED BURNIE claim-value that downstream dilutes the sDGNRS/vault ETH backing); (iii) confirm
       `_mintPriceInContext()` (@376) reproduces the Game's mintPrice() exactly so the caller cannot SKEW the
       bounty size, and the ETH-value of the bounty is price-independent. Find any way to fabricate
       bounty-eligible work, to make the bounty net-positive at any realistic gas, or to mint unbounded
       dilutive BURNIE — OR state VERIFIED SOUND with the concrete real-gas + illiquidity + un-manufacturability
       numbers. A hand-wave ("illiquid so fine") is NOT acceptable — require the real-gas accounting.

    2. ACCESS-04 / FC-393-03 (the partial-balance burst-solvency — DEDICATED solvency-adjacent break-target,
       the MED prime): each redemption leg sends `min(address(this).balance, legAmount)` ETH and pulls the
       rest as stETH (StakedDegenerusStonk @880/:888/:898); the comment asserts the MAX (175%) reservation
       guarantees ETH+stETH ≥ rolled. Demand a real same-block multi-claim leg-accounting argument: under an
       ADVERSARIAL sequence of MANY partial-balance LIVE claims in ONE block (the permissionless
       claimRedemptionMany / interleaved claimRedemption calls draining the contract ETH balance
       progressively), does the segregation accounting hold — i.e. is Σ(legs over the burst) == Σ(rolled) ==
       Σ(released `_pendingRedemptionEthValue`), and does the MAX(175%) reservation (ETH+stETH ≥ Σ MAX ≥ Σ
       rolled) actually cover each leg so that an ETH-balance drain merely SHIFTS the deficit to the stETH
       leg of the SAME held reservation, never stranding ETH (left unreleased) or under-pulling stETH (GAME
       pulls less than the remainder)? Spell out the interleavings to try: a claim that drains
       address(this).balance to near-zero so the next claim's ETH leg clamps to ~0 and the entire roll must
       come from stETH; a claim whose stETH transferFrom would exceed the held reservation. Couple to the
       390 FC-392-08 / FC-393-03 solvency-half adjudication (REFUTED at 390: each leg recomputes fresh
       `bal`, GAME pulls remainder fail-closed reverting if short, Σ legs == Σ rolled == Σ released, MAX
       reservation covers) — attack the ACCESS half (the cross-chunk / same-block race + the cap-RMW raced
       across chunks the FC-392-08 permissionless-race half routed here) for consistency, and either confirm
       the burst can't strand/under-pull OR surface a finding.

    Then name the remaining owned leads + the gate/reentrancy/authorization sweep as numbered break-targets
    at their map severity:

    3. ACCESS-03 / FC-393-01 (LOW — the forced-timing MAGNITUDE question, NOT the timing model): a third
       party can now FORCE a winner's live-game decimator claim to resolve at a `level` the winner did not
       choose — `_rollTargetLevel(level+1, frozenSeed)` and the whale-pass `_queueTicketRange(winner,
       level+1,…)` read the LIVE `level` at claim time (DecimatorModule:283-322). The frozen seed prevents
       win/loss steering (DO NOT re-litigate that — timing is by-design). The ONLY question: does any reward
       MAGNITUDE / target-level distribution diverge enough between ADJACENT levels to make a forced
       resolution MATERIALLY harmful to the winner (e.g. a griefer settling just before an advance that
       shifts the target-level distribution or the whale-pass start level)? Charge the council to quantify
       the adjacent-level magnitude divergence and rule it material or inert — with the concrete distribution
       reasoning, not a "timing is by-design so fine" dismissal.

    4. ACCESS-01 / FC-393-02 (INFO — forfeit-to-self timing, benign?): the dust lootbox (<0.01 ETH half)
       forfeits to sDGNRS's OWN claimable (StakedDegenerusStonk:839-848/:897-901), raising backing for ALL
       holders. A keeper choosing WHICH redemptions to settle could bias WHEN forfeits land vs when full
       lootboxes resolve. Charge the council to confirm the per-claim split is DETERMINISTIC from the fixed
       roll + the player's own owed value, so there is NO per-victim value extraction — only a benign timing
       of uniform backing accrual (the dust-drop itself is by-design per [[redemption-dust-lootbox-drop-
       bydesign]] — the question is strictly whether a keeper can extract per-victim value) — OR surface a
       finding.

    5. ACCESS-05 (gate + reentrancy intactness sweep across the new/widened entrypoints): charge the council
       to confirm on EACH of claimDecimatorJackpot/Many, claimRedemption/Many, claimCoinflipCarry: (i) the
       freeze/rngLocked/liveness/gameOver gate is present and correct (decimator + redemption claims revert
       on prizePoolFrozen; post-gameover redemption is self-claim-only — isGameOver && player!=msg.sender
       reverts Unauthorized; claimCoinflipCarry gates rngLocked); (ii) the ETH/stETH reentrancy follows CEI
       (post-gameover claimRedemption deletes the pendingRedemptions slot + lowers
       _pendingRedemptionEthValue BEFORE the untrusted _payEth .call; _payEth sends stETH first then ETH
       .call last; the Game's _payoutWithStethFallback @1888 reordered 53cd25cf stETH-out-first / ETH-call-
       last closing V62-03); (iii) the Game-side callees resolveRedemptionLootbox (@926) +
       creditRedemptionDirect (@1004) gate msg.sender != SDGNRS, and distributeYieldSurplus is reachable only
       internally from advanceGame (no external stub, credits-only) — confirm it is not an independently
       exploitable permissionless lever. Find any missing/widened gate or open reentrancy path across the
       legs, OR confirm intact. Fold in cross-ref FC-390-03 (LOW — the mid-batch gameOver/frozen boundary,
       solvency REFUTED at 390: the batch caches over/frozen/offsets/totalBurn ONCE @338-341 so claim
       effects can't flip the boundary mid-batch; confirm the ACCESS half — no permissionless batch can
       split inconsistently across the boundary).

    6. ACCESS-05 / FC-393-04 / cross-ref FC-392-20 (INFO — the widened claim-loop gas worst-case): the
       coinflip claim windows widened (COIN_CLAIM_DAYS 90→365, AUTO_REBUY_OFF_CLAIM_DAYS_MAX 1095→1460,
       counters uint8→uint16) over the NEW packed storage (each day now a masked sub-word read/write). The
       claims are user-paid + OUT of the advanceGame chain; the advanceGame-chain sDGNRS auto-settle walks
       deep=false (bounded windowDays 365, ~1 day/advance). FC-392-20 ruled this INFO at 392 (bounded,
       caller-paid, off the chain) and routed the FRESH worst-case here. Charge the council to confirm no
       realistic actor can force a many-hundred-day cold-SLOAD walk into a GAS-SENSITIVE caller (the
       advanceGame chain specifically) under the new packed masked sub-word layout — i.e. the worst-case
       365/1460 loop is reachable only by a caller paying their OWN gas, never bricking advanceGame. Confirm
       inert OR surface.

    Do NOT pre-state any verdict in the prompt — the council finds, Claude adjudicates at 393-02. No fenced
    solidity blocks beyond the minimal identifier/selector/line citations the council needs to locate the
    code.
  </action>
  <verify>
    <automated>test -s .planning/phases/393-permissionless-composition/393-01-COUNCIL-PROMPT-ACCESS.md && grep -q "a8b702a7" .planning/phases/393-permissionless-composition/393-01-COUNCIL-PROMPT-ACCESS.md && for id in ACCESS-01 ACCESS-02 ACCESS-03 ACCESS-04 ACCESS-05 FC-393-01 FC-393-02 FC-393-03 FC-393-04 FC-390-03 FC-390-06 FC-392-08 FC-392-20; do grep -q "$id" .planning/phases/393-permissionless-composition/393-01-COUNCIL-PROMPT-ACCESS.md || { echo "MISSING $id"; exit 1; }; done && grep -Eqi "prevailing gas|real gas|gwei" .planning/phases/393-permissionless-composition/393-01-COUNCIL-PROMPT-ACCESS.md && grep -Eqi "illiquid|flip-credit|un-manufacturab|manufactur" .planning/phases/393-permissionless-composition/393-01-COUNCIL-PROMPT-ACCESS.md && grep -Eqi "partial-balance|same-block|burst|reservation" .planning/phases/393-permissionless-composition/393-01-COUNCIL-PROMPT-ACCESS.md && grep -Eqi "beneficiary|credits only|msg.sender" .planning/phases/393-permissionless-composition/393-01-COUNCIL-PROMPT-ACCESS.md</automated>
  </verify>
  <acceptance_criteria>
    393-01-COUNCIL-PROMPT-ACCESS.md exists and is non-empty; instructs reading frozen source at `a8b702a7`
    via git show; states the beneficiary-only doctrine + the keeper-bounty-net-negative-vs-real-gas +
    un-manufacturability doctrine + the forced-timing-magnitude question as the thesis to break; names ALL
    of ACCESS-01..05 + FC-393-01..04 + the inherited cross-refs FC-390-03, FC-390-06, FC-392-08, FC-392-20;
    charges ACCESS-02 (keeper-bounty vs REAL gas 5-50+ gwei NOT the 0.5-gwei peg + flip-credit illiquidity +
    un-manufacturability) and ACCESS-04 (partial-balance burst solvency under same-block multi-claim leg
    accounting) as DEDICATED numbered prime break-targets demanding a real argument (not a hand-wave);
    carries the KNOWN-BY-DESIGN exclusion list (permissionless/lootbox claim timing, operator-approval trust
    boundary, genesis admin self-break, dust-lootbox-drop, RTP/EV); states the already-adjudicated
    solvency/ECON cross-ref halves as the consistency anchor; uses neutral vocabulary; no verdict is
    pre-stated.
  </acceptance_criteria>
  <done>
    The neutral PERMISSIONLESS-COMPOSITION council prompt is authored against the frozen subject, covering
    all 5 ACCESS reqs + the 4 FC-393 owned leads + the 4 inherited cross-refs, with the keeper-bounty
    real-gas economics and the partial-balance burst-solvency charged hard as prime targets and the
    beneficiary-only + un-manufacturability + forced-timing-magnitude doctrine encoded, ready to fan to the
    council.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the council fan-out and record the council-net capture</name>
  <files>.planning/phases/393-permissionless-composition/council/access.gemini.txt, .planning/phases/393-permissionless-composition/council/access.codex.txt, .planning/phases/393-permissionless-composition/council/access.council.json, .planning/phases/393-permissionless-composition/393-01-COUNCIL-NET.md</files>
  <read_first>
    - .planning/audit-v52/cross-model/bin/council.sh (the exact CLI contract + skipped[]/skip_reasons behavior)
    - .planning/phases/393-permissionless-composition/393-01-COUNCIL-PROMPT-ACCESS.md (Task 1 output)
    - .planning/phases/392-entropy-and-econ/392-01-COUNCIL-NET.md (the just-approved COUNCIL-NET capture record shape to match — including the codex-usage-cap skip handling)
  </read_first>
  <action>
    Run `.planning/audit-v52/cross-model/bin/council.sh --out-dir
    .planning/phases/393-permissionless-composition/council --label access
    .planning/phases/393-permissionless-composition/393-01-COUNCIL-PROMPT-ACCESS.md`. council.sh fans
    gemini+codex in parallel internally; this is ONE invocation (one slice) so no extra pacing of multiple
    invocations is needed beyond letting the single fan-out complete per [[pace-runs-to-survive-5h-cap]]. A
    skipped CLI is recorded in the council.json skipped[]/skip_reasons and is NOT fatal; do not treat a skip
    as a failure. EXPECT codex to skip (it has been hard usage-capped since 392-01); record it faithfully —
    gemini + the Wave-2 Claude net still satisfy the dual-NET, and flag a post-reset codex re-run → 396.

    IMMEDIATELY after the fan-out, git-status-verify the subject was not mutated:
    `git diff a8b702a7 -- contracts/` MUST be empty and `git status --porcelain contracts/` MUST be empty
    ([[feedback_verify_writecapable_agents]] — the council writes only to its out-dir, but verify; remove any
    stray file a council model writes outside the out-dir). Do NOT run `hardhat compile --force` (regenerates
    ContractAddresses source).

    Write 393-01-COUNCIL-NET.md recording: the access.council.json manifest's available[]/skipped[] (+
    skip_reasons), the raw output file paths, a one-line characterization of each model's output (raw, NOT
    adjudicated — adjudication is 393-02), the byte-freeze attestation (`git diff a8b702a7 -- contracts/`
    empty; `git status --porcelain contracts/` empty), and an explicit "NET 1 ON RECORD for
    PERMISSIONLESS-COMPOSITION" line so 393-02 can confirm both-nets-on-record before any verdict. If a CLI
    is in skipped[], record WHICH + WHY (the codex usage-cap) and note the slice still has the other model on
    record (a single available model satisfies "council on record" with the skip documented + the post-reset
    codex re-run flagged to 396; both-unavailable would require a re-run and must be surfaced, not silently
    passed).
  </action>
  <verify>
    <automated>test -f .planning/phases/393-permissionless-composition/council/access.council.json && test -s .planning/phases/393-permissionless-composition/393-01-COUNCIL-NET.md && grep -q "NET 1 ON RECORD" .planning/phases/393-permissionless-composition/393-01-COUNCIL-NET.md && [ -z "$(git -C /home/zak/Dev/PurgeGame/degenerus-audit diff a8b702a7 -- contracts/)" ]</automated>
  </verify>
  <acceptance_criteria>
    `access.council.json` exists; either both gemini+codex `.txt` outputs are non-empty OR the unavailable
    CLI is in the council.json skipped[] (+ skip_reasons); 393-01-COUNCIL-NET.md records the
    available/skipped + the codex-cap skip reason if applicable + raw output paths + the byte-freeze
    attestation + an explicit "NET 1 ON RECORD" line + the post-reset codex re-run flag to 396 if codex
    skipped; `git diff a8b702a7 -- contracts/` is empty.
  </acceptance_criteria>
  <done>
    NET 1 (the cross-model council) is on record for the PERMISSIONLESS-COMPOSITION slice with raw output
    captured under the phase council/ dir, the subject proven byte-frozen, the codex usage-cap skip recorded
    + a post-reset re-run flagged to 396, and the capture documented for the Wave-2 adjudication to fold in.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit-process → subject source | the risk is the AUDIT MUTATING the byte-frozen subject (a Write-capable fan-out edits contracts/), not a new feature threat |
| council CLI → out-dir | the council writes external-model output; risk = an unavailable/usage-capped CLI silently dropping the only net (handled by skipped[]/skip_reasons) or a CLI given write access beyond out-dir |

## STRIDE Threat Register (audit-only posture)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-393-01 | Tampering | byte-frozen subject contracts/ | mitigate | git-status-verify `git diff a8b702a7 -- contracts/` empty after the council fan-out (Task 2); council writes only to its out-dir; remove any stray file |
| T-393-02 | Information disclosure / false-green | the ACCESS slice with both CLIs unavailable silently treated as "on record" | mitigate | the council.json skipped[]/skip_reasons is recorded in 393-01-COUNCIL-NET.md; codex-cap skip documented + post-reset re-run flagged to 396; both-unavailable is surfaced for re-run, never passed silently |
| T-393-03 | Tampering | `hardhat compile --force` regenerating ContractAddresses source | accept/avoid | the plan forbids hardhat; only `git show`/read tools touch the subject |
</threat_model>

<verification>
- The council prompt exists, is neutral, charged against `a8b702a7`, covers ACCESS-01..05 + FC-393-01..04 +
  the inherited cross-refs FC-390-03, FC-390-06, FC-392-08, FC-392-20, with ACCESS-02 (keeper-bounty vs real
  gas + illiquidity + un-manufacturability) and ACCESS-04 (partial-balance burst solvency) charged hard as
  prime targets and the beneficiary-only + forced-timing-magnitude doctrine encoded.
- `council.sh` ran for `--label access`; the council.json manifest exists.
- Both model outputs non-empty OR the missing CLI in skipped[] (codex cap recorded).
- 393-01-COUNCIL-NET.md records the capture + byte-freeze attestation + "NET 1 ON RECORD" + the post-reset
  codex re-run flag if codex skipped.
- `git diff a8b702a7 -- contracts/` empty; `git status --porcelain contracts/` empty.
</verification>

<success_criteria>
NET 1 (the cross-model council) is on record for the full PERMISSIONLESS-COMPOSITION slice, with raw
gemini/codex output captured under the phase council/ dir and the capture documented, so the Wave-2
Claude-net + adjudication plan can fold the council leads in before issuing any per-item verdict. The
subject is proven byte-frozen throughout.
</success_criteria>

<output>
Create `.planning/phases/393-permissionless-composition/393-01-SUMMARY.md` when done
</output>
