---
phase: 86-daily-coin-ticket-jackpot
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v4.0-daily-ticket-jackpot.md
autonomous: true
requirements: [DCOIN-02, DCOIN-04]

must_haves:
  truths:
    - "_distributeTicketJackpot (JM:1105) traced end-to-end with file:line citations showing bucket sizing, winner selection from traitBurnTicket, and ticket queuing via _queueTickets"
    - "All 3 callers of _distributeTicketJackpot enumerated: daily (JM:733), carryover (JM:745), early-bird lootbox (JM:1093)"
    - "Winner selection mechanism documented: _randTraitTicket (JM:2237) selects from traitBurnTicket[lvl][traitId] with deity virtual entries"
    - "Ticket target level documented: winners receive tickets for lvl+1 (not lvl) at JM:1209"
    - "Budget computation chain documented: _budgetToTicketUnits (JM:1063) and _packDailyTicketBudgets/_unpackDailyTicketBudgets (JM:2753-2782)"
    - "Every discrepancy and new finding tagged with [DISCREPANCY] or [NEW FINDING]"
  artifacts:
    - path: "audit/v4.0-daily-ticket-jackpot.md"
      provides: "Complete ticket jackpot distribution audit"
      contains: "_distributeTicketJackpot"
  key_links:
    - from: "payDailyJackpotCoinAndTickets (JM:681)"
      to: "_distributeTicketJackpot (JM:1105)"
      via: "direct calls at JM:733 and JM:745"
      pattern: "_distributeTicketJackpot"
    - from: "_distributeTicketsToBucket (JM:1178)"
      to: "_queueTickets"
      via: "ticket queuing at JM:1209"
      pattern: "_queueTickets.*lvl \\+ 1"
    - from: "_distributeTicketsToBucket (JM:1178)"
      to: "_randTraitTicket (JM:2237)"
      via: "winner selection at JM:1190"
      pattern: "_randTraitTicket"
---

<objective>
Trace the daily ticket jackpot distribution path (_distributeTicketJackpot) with exhaustive file:line citations from the current Solidity codebase.

Purpose: Satisfy DCOIN-02 (ticket jackpot winner selection) and DCOIN-04 (discrepancy tagging) by producing an independent audit document that verifies every claim against current code.

Output: `audit/v4.0-daily-ticket-jackpot.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-daily-coin-ticket-jackpot/86-RESEARCH.md

Prior audit for cross-reference (not to trust -- to verify):
@audit/v4.0-ticket-creation-queue-mechanics.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace _distributeTicketJackpot end-to-end with all callers and budget computation</name>
  <files>audit/v4.0-daily-ticket-jackpot.md</files>
  <read_first>
    contracts/modules/DegenerusGameJackpotModule.sol (lines 1105-1135 for _distributeTicketJackpot; lines 1141-1175 for _distributeTicketsToBuckets; lines 1178-1219 for _distributeTicketsToBucket; lines 1222-1270 for _computeBucketCounts; lines 2237-2280 for _randTraitTicket; lines 730-753 for daily+carryover callers in payDailyJackpotCoinAndTickets; lines 1085-1102 for early-bird lootbox caller in _distributeLootboxAndTickets; lines 1063-1070 for _budgetToTicketUnits; lines 2753-2782 for _packDailyTicketBudgets/_unpackDailyTicketBudgets; lines 183 for MAX_BUCKET_WINNERS; lines 217 for LOOTBOX_MAX_WINNERS)
    audit/v4.0-ticket-creation-queue-mechanics.md (for cross-reference with Phase 81 _queueTickets documentation)
  </read_first>
  <action>
Create the audit document `audit/v4.0-daily-ticket-jackpot.md`. All file:line citations MUST be verified by reading current Solidity -- do NOT trust research line numbers without confirmation.

**Section 1: Overview**
- Document header matching v4.0 audit format (see audit/v4.0-ticket-creation-queue-mechanics.md for style)
- State scope: ticket jackpot distribution mechanics
- List requirement IDs covered: DCOIN-02, DCOIN-04

**Section 2: _distributeTicketJackpot (JM:1105) -- Core Function**
- Signature: `_distributeTicketJackpot(uint24 lvl, uint32 winningTraitsPacked, uint256 ticketUnits, uint256 entropy, uint16 maxWinners, uint8 saltBase)`
- Early exit: `if (ticketUnits == 0) return` at JM:1113
- Trait unpacking: `JackpotBucketLib.unpackWinningTraits(winningTraitsPacked)` at JM:1115 -> 4-element uint8 array
- Cap computation: `cap = maxWinners; if (ticketUnits < cap) cap = uint16(ticketUnits)` at JM:1118-1119
- Bucket sizing: `_computeBucketCounts(lvl, traitIds, cap, entropy)` at JM:1121 -> returns `(uint16[4] counts, uint8 activeCount)`
- Early exit: `if (activeCount == 0) return` at JM:1127
- Distribution: `_distributeTicketsToBuckets(lvl, traitIds, counts, ticketUnits, entropy, cap, saltBase)` at JM:1129

**Section 3: _distributeTicketsToBuckets (JM:1141) -- Bucket Loop**
- baseUnits computation: `ticketUnits / cap` at JM:1150
- distParams packing: `(ticketUnits % cap) | ((entropy % cap) << 128)` at JM:1151 -- remainder for +1 distribution + offset for fairness rotation
- Loop over 4 traits (JM:1154-1174): for each non-zero count, calls `_distributeTicketsToBucket` with per-trait entropy step and salt = `saltBase + traitIdx`

**Section 4: _distributeTicketsToBucket (JM:1178) -- Per-Bucket Distribution**
- Winner cap: `if (count > MAX_BUCKET_WINNERS) count = MAX_BUCKET_WINNERS` at JM:1189 (MAX_BUCKET_WINNERS = 250, JM:183)
- Winner selection: `_randTraitTicket(traitBurnTicket[lvl], entropy, traitId, uint8(count), salt)` at JM:1190
- Unit distribution per winner: baseUnits + 1 if cursor < extra (JM:1204-1206) -- round-robin remainder distribution
- CRITICAL: Ticket target level is `lvl + 1` NOT `lvl`: `_queueTickets(winner, lvl + 1, uint32(units))` at JM:1209
- Null winner skip: `if (winner != address(0) && units != 0)` at JM:1208

**Section 5: _randTraitTicket (JM:2237) -- Winner Selection Mechanism**
- Source: `traitBurnTicket_[trait]` storage array at JM:2244
- Deity virtual entries: for trait symbols < 32, adds `deityBySymbol[fullSymId]` with virtualCount = max(len/50, 2) at JM:2250-2258
- Effective length: `len + virtualCount` at JM:2261
- Empty check: returns empty array if effectiveLen == 0 or numWinners == 0 (JM:2262-2264)
- Selection: `idx = slice % effectiveLen` at JM:2272, winner = `holders[idx]` if idx < len, else `deity` (JM:2273)
- Entropy rotation: `slice = (slice >> 16) | (slice << 240)` at JM:2277 -- bit rotation for subsequent winners
- NOTE: May return duplicate winners (no dedup), may return address(0) entries from storage

**Section 6: All Callers of _distributeTicketJackpot**
Enumerate exactly 3 call sites:

6.1 Daily tickets (JM:733):
- Inside `payDailyJackpotCoinAndTickets` (JM:681)
- Args: `(lvl, winningTraitsPacked, dailyTicketUnits, entropyDaily, LOOTBOX_MAX_WINNERS, 241)`
- dailyTicketUnits unpacked from dailyTicketBudgetsPacked at JM:687
- entropyDaily = `randWord ^ (uint256(lvl) << 192)` at JM:695
- Level: lastDailyJackpotLevel (JM:693)
- Traits: lastDailyJackpotWinningTraits (JM:694)

6.2 Carryover tickets (JM:745):
- Inside `payDailyJackpotCoinAndTickets` (JM:681)
- Args: `(carryoverSourceLevel, winningTraitsPacked, carryoverTicketUnits, entropyNext, LOOTBOX_MAX_WINNERS, 240)`
- carryoverTicketUnits unpacked from dailyTicketBudgetsPacked at JM:688
- carryoverSourceLevel = `lvl + uint24(carryoverSourceOffset)` at JM:698 (default: lvl+1 at JM:696)
- entropyNext = `randWord ^ (uint256(carryoverSourceLevel) << 192)` at JM:700
- Conditional: only called when `carryoverTicketUnits != 0` (checked inside _distributeTicketJackpot)

6.3 Early-bird lootbox (JM:1093):
- Inside `_distributeLootboxAndTickets` (JM:1076)
- Args: `(lvl, winningTraitsPacked, ticketUnits, randWord ^ (uint256(lvl) << 192), LOOTBOX_MAX_WINNERS, 242)`
- ticketUnits from `_budgetToTicketUnits(ticketBasis, lvl + 1)` at JM:1091 where ticketBasis = `(lootboxBudget * ticketConversionBps) / 10_000` at JM:1090
- NOTE: _budgetToTicketUnits receives `lvl + 1` for pricing but _distributeTicketJackpot receives `lvl` as the trait source level. Winners then get tickets for `lvl + 1` (at JM:1209). Document this level arithmetic explicitly.

**Section 7: Budget Computation Chain**

7.1 _budgetToTicketUnits (JM:1063-1070):
- Formula: `(budget << 2) / ticketPrice` where ticketPrice = `PriceLookupLib.priceForLevel(lvl)` at JM:1068
- The `<< 2` means tickets cost ticketPrice/4 (quarter-price for jackpot rewards)
- Returns 0 if budget == 0 or ticketPrice == 0

7.2 _packDailyTicketBudgets (JM:2753-2764):
- Packing layout: `counterStep (8 bits @ 0) | dailyTicketUnits (64 bits @ 8) | carryoverTicketUnits (64 bits @ 72) | carryoverSourceOffset (8 bits @ 136)`
- Written at JM:459 inside payDailyJackpot Phase 1

7.3 _unpackDailyTicketBudgets (JM:2766-2782):
- Reverse of pack: uint8, uint64, uint64, uint8
- Read at JM:686-690 inside payDailyJackpotCoinAndTickets

**Section 8: Cross-Reference with Phase 81**
- Verify _queueTickets call at JM:1209 matches Phase 81's documentation of ticket creation path #11 (or equivalent)
- Note whether Phase 81 documented this as a ticket creation source
- Tag [CONFIRMED] if matches, [DISCREPANCY] if differs

**Section 9: Findings**
- Tag every discrepancy as [DISCREPANCY] with severity
- Tag every new finding as [NEW FINDING] with severity
- Document the lvl+1 target level behavior explicitly (tickets target NEXT level)
- Document the deity virtual entry mechanism and its implications for winner probability
- Assess: duplicate winner addresses from _randTraitTicket -- is this intentional? (likely yes for gas efficiency, document the tradeoff)
  </action>
  <verify>
    <automated>grep -c "file:line\|JM:\|AM:\|GS:" audit/v4.0-daily-ticket-jackpot.md | awk '{if ($1 >= 30) print "PASS: ",$1," citations"; else print "FAIL: only ",$1," citations"}'</automated>
  </verify>
  <acceptance_criteria>
    - audit/v4.0-daily-ticket-jackpot.md exists
    - File contains "_distributeTicketJackpot" (core function documented)
    - File contains "JM:1105" (core function line)
    - File contains "JM:733" AND "JM:745" AND "JM:1093" (all 3 callers)
    - File contains "_randTraitTicket" AND "JM:2237" (winner selection mechanism)
    - File contains "traitBurnTicket" (winner source documented)
    - File contains "lvl + 1" in context of _queueTickets (target level documented)
    - File contains "JM:1209" (ticket queuing line)
    - File contains "_budgetToTicketUnits" AND "JM:1063" (budget computation)
    - File contains "_packDailyTicketBudgets" AND "_unpackDailyTicketBudgets" (packing chain)
    - File contains "DCOIN-02" (requirement traceability)
    - File contains "deityBySymbol" OR "deity" (virtual entry mechanism documented)
    - File contains "LOOTBOX_MAX_WINNERS" (winner cap for all 3 callers)
    - Discrepancy summary table or findings section present
  </acceptance_criteria>
  <done>_distributeTicketJackpot traced end-to-end: all 3 callers enumerated (daily JM:733, carryover JM:745, early-bird JM:1093), bucket sizing via _computeBucketCounts, winner selection via _randTraitTicket from traitBurnTicket with deity virtual entries, ticket queuing via _queueTickets to lvl+1. Budget computation chain (_budgetToTicketUnits, pack/unpack) documented. Phase 81 cross-reference included. All discrepancies and new findings tagged.</done>
</task>

</tasks>

<verification>
After task completes:
1. Verify audit/v4.0-daily-ticket-jackpot.md exists and contains all required sections
2. Grep for _distributeTicketJackpot, _randTraitTicket, _queueTickets (core function chain)
3. Grep for all 3 caller lines: JM:733, JM:745, JM:1093
4. Grep for DCOIN-02 requirement ID
5. Verify budget computation chain documented (_budgetToTicketUnits, pack/unpack functions)
6. Verify lvl+1 target level explicitly documented
</verification>

<success_criteria>
- Complete audit document covering ticket jackpot distribution mechanics
- 30+ file:line citations verified against current Solidity
- All 3 callers of _distributeTicketJackpot enumerated with argument details
- Winner selection chain documented: _computeBucketCounts -> _distributeTicketsToBuckets -> _distributeTicketsToBucket -> _randTraitTicket -> _queueTickets
- Budget computation chain documented: _budgetToTicketUnits + pack/unpack pair
- Every discrepancy and new finding tagged per DCOIN-04
</success_criteria>

<output>
After completion, create `.planning/phases/86-daily-coin-ticket-jackpot/86-02-SUMMARY.md`
</output>
