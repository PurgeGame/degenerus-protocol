---
phase: 375-spec-design-lock-open-knobs-anchor-re-attestation-vs-2bee6d6
plan: 02
type: execute
wave: 2
depends_on: ["375-01"]
files_modified:
  - .planning/SPEC-V61-DESIGN-LOCK.md
autonomous: true
requirements: [SPEC-01]

must_haves:
  truths:
    - "The SPEC document locks all open knobs in writing: D-01 accessor-first sequencing, D-02 AfkingSpent-at-every-debit, D-03 CURSE_COUNT_CAP=20, D-04 protocol-addr skip, D-05 staleness basis _currentMintDay()"
    - "The SPEC cites re-attested anchors from 375-ANCHOR-REATTESTATION.md (the 2bee6d6f-grounded table), not the pre-attestation CONTEXT.md lines"
    - "The SPEC maps the producer-before-consumer edit order: Track A (PACK accessor → repack → AFPAY waterfall → event) and Track B (CURSE infra → SMITE), with the CURE-vs-PACK-repack no-write-after-write cross-check"
    - "The SPEC notes the reconciliation that supersedes PLAN-V61-MILESTONE-SCOPE.md §2 / REQUIREMENTS.md PACK-02 'feature-first' wording with the locked accessor-first order"
  artifacts:
    - path: ".planning/SPEC-V61-DESIGN-LOCK.md"
      provides: "The Phase 375 design-lock SPEC document: locked knobs + re-attested anchor table + producer-before-consumer edit-order map; the input Phase 376 IMPL authors the single batched diff from"
      contains: "accessor-first"
      min_lines: 80
  key_links:
    - from: ".planning/SPEC-V61-DESIGN-LOCK.md anchor citations"
      to: ".planning/phases/375-.../375-ANCHOR-REATTESTATION.md"
      via: "the re-attested table folded/cited into the SPEC"
      pattern: "2bee6d6f"
    - from: ".planning/SPEC-V61-DESIGN-LOCK.md edit-order map"
      to: "the 17 contract requirements (AFPAY/PACK/CURSE/SMITE)"
      via: "Track A + Track B ordered REQ-ID sequences"
      pattern: "PACK-01.*PACK-02.*AFPAY"
---

<objective>
Author the v61.0 design-lock SPEC document (`.planning/SPEC-V61-DESIGN-LOCK.md`) — the single SPEC-01 deliverable. It (1) locks the open knobs D-01..D-05 plus the discretion/verification items in writing, (2) cites the re-attested anchors produced by Plan 01 (`375-ANCHOR-REATTESTATION.md`) instead of the pre-attestation CONTEXT.md lines, and (3) maps the producer-before-consumer edit order for the single 376 IMPL diff. This document lets Phase 376 author ONE batched `contracts/*.sol` diff with zero "by construction" assumptions.

Purpose: Phase 376 is the contract-boundary HARD STOP — it edits real `contracts/*.sol` once, under hand-review. A vague or stale SPEC forces 376 to re-derive decisions or re-grep anchors mid-diff, reintroducing the mis-specification risk this phase exists to eliminate. A concrete design-lock doc with re-attested anchors + a settled edit order makes the 376 diff mechanical.
Output: `.planning/SPEC-V61-DESIGN-LOCK.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/375-spec-design-lock-open-knobs-anchor-re-attestation-vs-2bee6d6/375-CONTEXT.md
@.planning/phases/375-spec-design-lock-open-knobs-anchor-re-attestation-vs-2bee6d6/375-01-SUMMARY.md

<consumes_plan_01>
This plan CONSUMES Plan 01's output: `375-ANCHOR-REATTESTATION.md` (the `2bee6d6f`-grounded re-attested anchor table + the three verification-item verdicts). The SPEC MUST cite the re-attested lines from that artifact, NOT the pre-attestation `~:NNN` lines in CONTEXT.md. Read `375-ANCHOR-REATTESTATION.md` in full before writing the anchor-bearing SPEC sections.
</consumes_plan_01>

<output_path>
Write the SPEC to `.planning/SPEC-V61-DESIGN-LOCK.md` (repo convention: design-lock docs live in `.planning/`, sibling to `PLAN-V61-MILESTONE-SCOPE.md` / `PLAN-CASHOUT-CURSE.md`). This is a `.planning/` docs artifact — freely committable, NO contract-commit gate. ZERO `contracts/*.sol` in this plan.
</output_path>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock the open knobs (D-01..D-05 + discretion/verification) in the SPEC</name>
  <files>.planning/SPEC-V61-DESIGN-LOCK.md</files>
  <read_first>
    - .planning/phases/375-spec-design-lock-open-knobs-anchor-re-attestation-vs-2bee6d6/375-CONTEXT.md (`<decisions>` — the canonical locked values for D-01..D-05 + the SPEC-execution items; `<specifics>` — the accessor-first headline divergence + the hard floor)
    - .planning/phases/375-spec-design-lock-open-knobs-anchor-re-attestation-vs-2bee6d6/375-01-SUMMARY.md (Plan 01's verification-item verdicts for `purchaseWith`-dead + self-smite)
    - .planning/PLAN-V61-AFKING-AS-PAYMENT-SOURCE.md (§7 sequencing resolved by D-01; §8 open checks resolved by D-02)
    - .planning/PLAN-CASHOUT-CURSE.md (§Open decision = cap D-03; §Accepted edges = day-basis D-05; §Constants)
    - .planning/PLAN-V61-DEITY-SMITE.md (§4 open: protocol skip D-04 + self-smite)
  </read_first>
  <action>
    Create `.planning/SPEC-V61-DESIGN-LOCK.md` with a title + a baseline line (frozen subject = `2bee6d6f`) + a "Locked Knobs" section containing one clearly-headed subsection per decision, each restating the LOCKED value verbatim from CONTEXT.md with its rationale and the affected REQ-IDs:
    - **D-01 — Accessor-first PACK/AFPAY sequencing.** State the locked order: the PACK accessor layer (`_claimableOf/_afkingOf` + `_creditClaimable/_debitClaimable/_creditAfking/_debitAfking`, each paired with the `claimablePool` update) AND the slot repack (`[afking:high128 | claimable:low128]`) land FIRST, then the AFPAY waterfall is authored ONCE against those accessors. Edit order: PACK-01 → PACK-02 → AFPAY-01..07. Rationale: the `claimablePool == Σ(claimableWinnings + afkingFunding)` solvency invariant is centralized in the accessor layer BEFORE the new spend path exists; the waterfall is authored once with no raw-mapping churn. Explicitly note this SUPERSEDES the "feature-first" wording in `PLAN-V61-MILESTONE-SCOPE.md` §2 and `REQUIREMENTS.md` PACK-02 (both deferred the exact feature-first-vs-accessor-first choice to SPEC-01 → this is the intended lock, not a conflict).
    - **D-02 — `AfkingSpent` at every afking debit.** Lock: emit `AfkingSpent(address indexed player, uint256 amount)` at EACH afking draw — `_processMintPayment` (ticket mint) AND the shared `_settleShortfall` helper (whale / presale / lootbox). Not the narrower `_processMintPayment`-only option. Call out the deliberate departure from how claimable spends stay silent outside `_processMintPayment` (so it reads as intentional). REQ: AFPAY-07.
    - **D-03 — `CURSE_COUNT_CAP = 20` points.** Lock: the `uint8` curse counter saturates at 20 pts (10 stacks / 10 ghost-cashouts; −2000 bps max), doubling as the mandatory uint8-wrap guard (`+= 2` must never wrap 254→0); clean headroom above the 5-stack (10-pt) smite ceiling. REQ: CURSE-07 / CURSE-02 / SMITE-01.
    - **D-04 — Protocol-addr skip kept.** Lock: both `smite()` and the cashout-curse SET skip `VAULT` / `SDGNRS` / `GNRUS` (constant compares, no SLOAD). Rationale: protects the sDGNRS redemption-snapshot activity-score read from corruption, keeps the two curse sources consistent, prevents a deity wasting 200 BURNIE on a non-player address. REQ: CURSE-03 / SMITE-01.
    - **D-05 — Staleness day-basis = `_currentMintDay()`.** Lock: the `_maybeCurse` staleness compare (`lastEthDay + 5 > _currentMintDay()`) uses `_currentMintDay()` (not `_simulatedDayIndex()`); the ≤1-day skew is immaterial against a 5-day window. REQ: CURSE-03.
    - **Verification items (fold Plan 01's verdicts):** a `purchaseWith`-dead subsection stating the confirmed verdict (dead → leave untouched at IMPL; OR the live-site flag) and a self-smite subsection stating the harmless-by-design verdict — both citing `375-ANCHOR-REATTESTATION.md`.
    Restate the hard floor (from CONTEXT.md `<specifics>`): RNG-freeze intact (all 3 items read no `rngWord`) + SOLVENCY-01 (`claimablePool == Σ`) centralized in the PACK accessor (D-01 makes this cleaner) — proven at SEC-01/02 (378).
  </action>
  <verify>
    <automated>F=.planning/SPEC-V61-DESIGN-LOCK.md; test -f "$F" && for k in "D-01" "D-02" "D-03" "D-04" "D-05" "accessor-first" "CURSE_COUNT_CAP = 20" "AfkingSpent" "_currentMintDay" "2bee6d6f"; do grep -Fq "$k" "$F" || { echo "MISSING: $k"; exit 1; }; done && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/SPEC-V61-DESIGN-LOCK.md` exists with a baseline line naming `2bee6d6f`.
    - The SPEC contains a distinct, clearly-headed subsection for EACH of D-01, D-02, D-03, D-04, D-05.
    - D-01 states the order as PACK accessor layer + repack → AFPAY waterfall (PACK-01 → PACK-02 → AFPAY-01..07) and explicitly notes it supersedes the `PLAN-V61-MILESTONE-SCOPE.md` §2 / `REQUIREMENTS.md` PACK-02 "feature-first" wording.
    - D-02 locks `AfkingSpent` at every afking debit (both `_processMintPayment` AND `_settleShortfall`) and names the deliberate departure from silent claimable spends.
    - D-03 locks `CURSE_COUNT_CAP = 20` and notes the uint8-wrap-guard double-duty + the headroom above the 5-stack smite ceiling.
    - D-04 locks the VAULT/SDGNRS/GNRUS protocol-addr skip for both `smite()` and the curse SET with the sDGNRS-redemption-snapshot rationale.
    - D-05 locks the staleness basis to `_currentMintDay()` (not `_simulatedDayIndex()`).
    - The `purchaseWith`-dead and self-smite verdicts are recorded, citing `375-ANCHOR-REATTESTATION.md`.
    - The hard floor (RNG-freeze + SOLVENCY-01 in the accessor layer) is restated.
    - No subjective acceptance language; every assertion is a source statement on the SPEC file.
    - No `contracts/*.sol` modified.
  </acceptance_criteria>
  <done>
    All five locked decisions (D-01..D-05) plus the `purchaseWith`-dead and self-smite verdicts and the hard-floor restatement are written into `SPEC-V61-DESIGN-LOCK.md` with locked values and rationale; D-01 records the feature-first→accessor-first reconciliation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Map the producer-before-consumer edit order (Track A + Track B + cross-check) and fold the re-attested anchor table into the SPEC</name>
  <files>.planning/SPEC-V61-DESIGN-LOCK.md</files>
  <read_first>
    - .planning/phases/375-spec-design-lock-open-knobs-anchor-re-attestation-vs-2bee6d6/375-ANCHOR-REATTESTATION.md (Plan 01's re-attested table — the SPEC cites THESE lines, MANDATORY read)
    - .planning/phases/375-spec-design-lock-open-knobs-anchor-re-attestation-vs-2bee6d6/375-CONTEXT.md (`<decisions>` → "Edit-order map (producer-before-consumer) — two independent tracks" + the CURE-vs-PACK-repack cross-check; `<code_context>` → Integration Points / Reusable Assets)
    - .planning/PLAN-V61-AFKING-AS-PAYMENT-SOURCE.md (§4 6-edit map for Track A)
    - .planning/PLAN-CASHOUT-CURSE.md (§1–5 for the Track B CURSE ordering)
    - .planning/REQUIREMENTS.md (the 17 contract REQ-IDs the tracks sequence)
  </read_first>
  <action>
    Add two sections to `.planning/SPEC-V61-DESIGN-LOCK.md`:
    (A) **Re-attested anchor table** — fold in (copy / embed) the `2bee6d6f`-grounded table from `375-ANCHOR-REATTESTATION.md` so the SPEC is self-contained for 376; every anchor cited elsewhere in the SPEC MUST use the re-attested (CONFIRMED/CORRECTED) line, not the CONTEXT.md `~:NNN`. State explicitly that the table is grounded on `2bee6d6f`.
    (B) **Producer-before-consumer edit-order map** — two independent tracks:
    - **Track A (balances mapping):** PACK-01 accessor layer → PACK-02 repack `[afking:high128 | claimable:low128]` → AFPAY-01 `_settleShortfall` generalization → AFPAY-02..06 spend paths (`_processMintPayment`, lootbox shortfall, presale + whale/lazy/deity pass, Degenerette `_collectBetFunds`, the fresh/recycled affiliate split) → AFPAY-07 `AfkingSpent` event. State WHY accessor-first (D-01): the SOLVENCY invariant exists before the spend path.
    - **Track B (`mintPacked_` curse counter — a DIFFERENT storage slot from the balances mapping, hence independent of Track A):** CURSE-01 `BitPackingLib` `CURSE_COUNT_SHIFT = 215` + `MASK_8` → CURSE-02 APPLY (`MintStreakUtils._playerActivityScore`, zero-new-SLOAD) → CURSE-03 SET (`_maybeCurse` from public `claimWinnings`) → CURSE-04 CURE (`_purchaseWithFor`) → CURSE-05 lootbox bounty stamp (`_recordLootboxMintDay` relocate) → CURSE-06 `decurse` → CURSE-07 view → SMITE-01 (shares the counter / cap / APPLY → lands with/after the CURSE infra).
    - **Cross-check (write-after-write safety):** state that CURSE-04 CURE fires in `_purchaseWithFor` (a buy path AFPAY also touches) but mutates `mintPacked_` curse BITS, NOT the balances mapping → the PACK repack (Track A) and the CURE (Track B) touch DIFFERENT storage slots → no write-after-write conflict. Note the two tracks can be authored in either relative order (independent), but WITHIN each track the producer precedes the consumer.
  </action>
  <verify>
    <automated>F=.planning/SPEC-V61-DESIGN-LOCK.md; test -f "$F" && for k in "Track A" "Track B" "CURSE_COUNT_SHIFT = 215" "_settleShortfall" "write-after-write" "_recordLootboxMintDay"; do grep -Fq "$k" "$F" || { echo "MISSING: $k"; exit 1; }; done && grep -Eq "PACK-01.*" "$F" && grep -Eq "CURSE-01.*" "$F" && grep -Fq "2bee6d6f" "$F" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - The SPEC contains a re-attested anchor table grounded on `2bee6d6f`, folded from `375-ANCHOR-REATTESTATION.md`; the SPEC states the table's baseline explicitly.
    - The SPEC contains a Track A sequence in order: PACK-01 → PACK-02 → AFPAY-01 → AFPAY-02..06 → AFPAY-07, with the accessor-first (D-01) rationale.
    - The SPEC contains a Track B sequence in order: CURSE-01 → CURSE-02 → CURSE-03 → CURSE-04 → CURSE-05 → CURSE-06 → CURSE-07 → SMITE-01.
    - The SPEC states Track A and Track B touch different storage slots (balances mapping vs `mintPacked_`) → the two tracks are independent.
    - The SPEC records the CURE-vs-PACK-repack write-after-write cross-check: CURE mutates `mintPacked_` curse bits, the PACK repack mutates the balances mapping → no clobber.
    - Anchors cited in the SPEC use the re-attested lines (matching `375-ANCHOR-REATTESTATION.md`), not the pre-attestation CONTEXT.md `~:NNN` values where those drifted.
    - No `contracts/*.sol` modified.
  </acceptance_criteria>
  <done>
    The SPEC contains the `2bee6d6f`-grounded re-attested anchor table and a complete producer-before-consumer edit-order map (Track A balances + Track B curse counter) with the write-after-write cross-check, so 376 IMPL can author the single diff in a settled order against trusted lines.
  </done>
</task>

<task type="auto">
  <name>Task 3: Finalize the SPEC — coverage self-check + commit the design-lock artifact</name>
  <files>.planning/SPEC-V61-DESIGN-LOCK.md</files>
  <read_first>
    - .planning/SPEC-V61-DESIGN-LOCK.md (the document assembled in Tasks 1-2 — verify completeness before committing)
    - .planning/phases/375-spec-design-lock-open-knobs-anchor-re-attestation-vs-2bee6d6/375-CONTEXT.md (cross-check that no locked decision or verification item was omitted)
    - $HOME/.claude/get-shit-done/references/feedback_contract_commit_guard_hook.md (if present — the commit-guard / force-add `.planning/` rule; `.planning/` is gitignored, force-add planning docs)
  </read_first>
  <action>
    Run a coverage self-check over `.planning/SPEC-V61-DESIGN-LOCK.md` and record a short "Coverage" section at the end of the SPEC confirming each Phase 375 Success Criterion is satisfied:
    (1) all open knobs locked in writing (packing sequencing D-01 · `AfkingSpent` breadth D-02 · `purchaseWith`-dead · curse cap D-03 + staleness basis D-05 · smite protocol-skip D-04 + self-smite); (2) every cited anchor re-attested against `2bee6d6f` with drift corrected (point at the folded table); (3) the producer-before-consumer edit order mapped (CURSE before SMITE within Track B; PACK accessor before repack within Track A; the SOLVENCY accessor location). Add a one-line forward note that the 17 contract REQ-IDs (AFPAY-01..07 · PACK-01/02 · CURSE-01..07 · SMITE-01) are authored at 376 in the Track A / Track B order, SEC-01/02 + TST-01..06 proven at 378.
    Then commit the docs artifacts. Since `.planning/` is gitignored, force-add the SPEC + the Plan-01 artifact + the SUMMARYs. NO `contracts/*.sol` is staged (paper-only phase → the contract-commit guard is satisfied trivially; no `CONTRACTS_COMMIT_APPROVED` needed). Use a docs commit message scoped to phase 375.
  </action>
  <verify>
    <automated>F=.planning/SPEC-V61-DESIGN-LOCK.md; grep -Fqi "Coverage" "$F" && test -z "$(git status --porcelain contracts/ 2>/dev/null)" && git log -1 --name-only --pretty=format: | grep -q "SPEC-V61-DESIGN-LOCK.md" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/SPEC-V61-DESIGN-LOCK.md` contains a Coverage section mapping each of the three Phase 375 Success Criteria to where it is satisfied in the SPEC.
    - The coverage check confirms no locked decision (D-01..D-05) or verification item (`purchaseWith`-dead, self-smite, SOLVENCY location) from CONTEXT.md is omitted.
    - `git status --porcelain contracts/` is empty (ZERO `contracts/*.sol` touched).
    - The SPEC + `375-ANCHOR-REATTESTATION.md` + the SUMMARYs are committed (force-added past the `.planning/` gitignore) under a phase-375 docs commit; the latest commit includes `SPEC-V61-DESIGN-LOCK.md`.
  </acceptance_criteria>
  <done>
    The SPEC carries a Coverage section satisfying all three Phase 375 Success Criteria with no omitted decision/verification item, and the docs artifacts are committed with zero `contracts/*.sol` in the diff.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPEC document → Phase 376 IMPL | The SPEC is the authoritative input for the single batched `contracts/*.sol` diff; any wrong/stale anchor or unsettled knob propagates into a real contract edit under hand-review |
| Plan 01 artifact → SPEC citations | The re-attested table is the source of trusted lines; the SPEC must cite from it, not from the pre-attestation CONTEXT.md |

## STRIDE Threat Register (specification-integrity framing)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-375-05 | Tampering | SPEC anchor citations | mitigate | The SPEC folds and cites the `2bee6d6f`-re-attested table from Plan 01 (Task 2); citing a stale CONTEXT.md `~:NNN` that drifted could steer the 376 diff to mis-edit the `claimablePool -=` pairing or `_settleShortfall` → silent SOLVENCY-01 regression. The fold + the cite-the-re-attested-line acceptance criterion close this |
| T-375-06 | Repudiation | Unsettled knob → "by construction" assumption at 376 | mitigate | Every open knob (D-01..D-05) + verification item is locked in writing with a named value (Task 1); the Coverage self-check (Task 3) confirms none is omitted, so 376 cannot fall back to an unrecorded assumption |
| T-375-07 | Tampering | Edit-order write-after-write hazard (CURE vs PACK repack) | mitigate | The SPEC records the cross-check that CURE mutates `mintPacked_` curse bits while the PACK repack mutates the balances mapping (different slots) → no write-after-write clobber; mis-sequencing in the single 376 diff that assumed a shared slot is pre-empted by the documented track independence |
| T-375-08 | Tampering | This SPEC phase accidentally edits a contract | accept | Paper-only by mandate; every task's acceptance criterion asserts `git status --porcelain contracts/` is empty and the final commit stages no `contracts/*.sol` → the contract-commit guard is satisfied trivially. Recorded as accepted (the phase produces only `.planning/` docs) |
</threat_model>

<verification>
- `.planning/SPEC-V61-DESIGN-LOCK.md` exists and locks D-01..D-05 + the verification items.
- The SPEC cites the `2bee6d6f`-re-attested anchors folded from `375-ANCHOR-REATTESTATION.md`.
- The SPEC maps Track A + Track B with the CURE-vs-PACK-repack write-after-write cross-check.
- The SPEC carries a Coverage section satisfying the three Phase 375 Success Criteria.
- `git status --porcelain contracts/` is empty; the docs artifacts are committed.
</verification>

<success_criteria>
- The open knobs are LOCKED in writing (D-01 packing sequencing · D-02 `AfkingSpent` breadth · `purchaseWith`-dead · D-03 curse cap + D-05 staleness basis · D-04 smite protocol-skip + self-smite).
- Every cited anchor is re-attested against `2bee6d6f` (drift corrected), folded into the SPEC from Plan 01.
- The producer-before-consumer edit order is mapped (Track A: PACK accessor before repack before AFPAY; Track B: CURSE before SMITE; the SOLVENCY accessor location).
- ZERO `contracts/*.sol` modified; the SPEC committed.
</success_criteria>

<output>
Create `.planning/phases/375-spec-design-lock-open-knobs-anchor-re-attestation-vs-2bee6d6/375-02-SUMMARY.md` when done. State the SPEC path (`.planning/SPEC-V61-DESIGN-LOCK.md`), confirm all five locked decisions + the two verification verdicts are captured, confirm the edit-order map (Track A + Track B + cross-check) is present, and confirm ZERO `contracts/*.sol` touched.
</output>
