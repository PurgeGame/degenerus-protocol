---
phase: 375-spec-design-lock-open-knobs-anchor-re-attestation-vs-2bee6d6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/375-spec-design-lock-open-knobs-anchor-re-attestation-vs-2bee6d6/375-ANCHOR-REATTESTATION.md
autonomous: true
requirements: [SPEC-01]

must_haves:
  truths:
    - "Every contract anchor in CONTEXT.md <canonical_refs> is re-attested against 2bee6d6f with the line confirmed or corrected"
    - "purchaseWith is empirically confirmed dead (def + interface + comments only, no call site/selector) at 2bee6d6f"
    - "The SOLVENCY accessor-invariant target location (the claimablePool == Σ identity home) is named with a re-attested line"
    - "Self-smite is confirmed harmless (no score-floor / bounty exploit via the shared counter)"
  artifacts:
    - path: ".planning/phases/375-spec-design-lock-open-knobs-anchor-re-attestation-vs-2bee6d6/375-ANCHOR-REATTESTATION.md"
      provides: "Re-attested anchor table (every CONTEXT.md anchor with confirmed/corrected line vs 2bee6d6f) + the three verification-item results"
      contains: "2bee6d6f"
  key_links:
    - from: "375-ANCHOR-REATTESTATION.md anchor rows"
      to: "git objects at 2bee6d6f"
      via: "git grep -n / git show against the frozen baseline"
      pattern: "2bee6d6f:contracts/"
---

<objective>
Re-attest every cited `file:line` anchor in `375-CONTEXT.md` `<canonical_refs>` → "Contract anchors" against the frozen baseline `2bee6d6f`, correcting any line-number drift, and resolve the three SPEC verification items (`purchaseWith`-dead confirm, self-smite sanity, SOLVENCY accessor-invariant location). The plan docs were grep-verified against the 2026-06-06 working-tree HEAD; this plan re-grounds every anchor on the actual IMPL subject so the SPEC (Plan 02) cites lines IMPL can trust.

Purpose: A stale/wrong `file:line` in the SPEC would steer the single 376 IMPL diff to edit the wrong line → a security invariant (RNG-freeze, `claimablePool == Σ(claimable + afking)` solvency) could silently regress in the shipped diff. Re-attestation against `2bee6d6f` is the specification-integrity mitigation.
Output: `375-ANCHOR-REATTESTATION.md` — a re-attested anchor table (the artifact Plan 02 folds into the SPEC) + the three verification-item results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/375-spec-design-lock-open-knobs-anchor-re-attestation-vs-2bee6d6/375-CONTEXT.md

<baseline_note>
Frozen subject = `2bee6d6f` (v60.0 closure HEAD; confirmed `git merge-base --is-ancestor 2bee6d6f HEAD`). Read anchors FROM this baseline via git — NOT from the working-tree `.sol` files (the working tree may be ahead). The shorthand module names in the plan docs map to full paths under `contracts/modules/` and `contracts/storage/` (e.g. "MintModule" → `contracts/modules/DegenerusGameMintModule.sol`, "DegenerusGameStorage" → `contracts/storage/DegenerusGameStorage.sol`); cite full paths in the table.

Verification mechanism (per anchor): `git grep -n <symbol> 2bee6d6f -- <full/path.sol>` or `git show 2bee6d6f:<full/path.sol> | sed -n '<a>,<b>p'`. Record CONFIRMED (symbol at/near cited line) or CORRECTED (with the new line) for each.
</baseline_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-attest every contract anchor in CONTEXT.md against 2bee6d6f</name>
  <files>.planning/phases/375-spec-design-lock-open-knobs-anchor-re-attestation-vs-2bee6d6/375-ANCHOR-REATTESTATION.md</files>
  <read_first>
    - .planning/phases/375-spec-design-lock-open-knobs-anchor-re-attestation-vs-2bee6d6/375-CONTEXT.md (the FULL anchor inventory lives in `<canonical_refs>` → "Contract anchors"; read it in full — every bullet/symbol is an anchor to re-attest)
  </read_first>
  <action>
    For EACH anchor symbol listed under CONTEXT.md `<canonical_refs>` → "Contract anchors (re-attest vs `2bee6d6f` — full paths)", run a git lookup against `2bee6d6f` and record whether the symbol sits at (or within a few lines of) the cited line, or drifted. Enumerate at minimum these files + symbols (the full inventory from CONTEXT.md):
    - `contracts/storage/DegenerusGameStorage.sol`: `_settleClaimableShortfall`/`_settleShortfall` generalization target (cited ~:851), `AfkingSpent` event decl, `claimablePool` `uint128` decl (cited ~:838-839), `PRICE_COIN_UNIT` (cited ~:162).
    - `contracts/DegenerusGame.sol`: `_processMintPayment` (~:1054), `_resolvePlayer` (~:573), `claimWinnings` (~:1556), public `playerActivityScore` view (~:2701), post-gameOver claim-merge (~:1575-1585), the `decurse`/`smite` insertion neighborhood.
    - `contracts/modules/DegenerusGameMintModule.sol`: lootbox shortfall (~:1126-1146), presale box (~:1489), ticket affiliate split (~:1620-1692), cure site `_purchaseWithFor` (~:1285), plain lootbox leg (~:1170-1254), `purchaseWith` (~:858).
    - `contracts/modules/DegenerusGameWhaleModule.sol`: whale bundle / lazy pass / deity pass shortfalls (~:263/490/596), `_recordLootboxMintDay` (~:983).
    - `contracts/modules/DegenerusGameDegeneretteModule.sol`: `_collectBetFunds` (~:579-588).
    - `contracts/modules/DegenerusGameMintStreakUtils.sol`: `_playerActivityScore` (~:241), CURSE APPLY site `scoreBps = bonusBps` (~:320), `packed` load (~:248), `_bountyEligible` (~:30-63).
    - `contracts/modules/DegenerusGamePayoutUtils.sol`: the 2 centralized claimable credits.
    - `contracts/modules/GameAfkingModule.sol`: afking auto-buy own spend (~:791-799, OUT of scope — confirm the no-double-draw boundary).
    - `contracts/libraries/BitPackingLib.sol`: `AFFILIATE_BONUS_POINTS_SHIFT` (ends bit 214), `LEVEL_UNITS_SHIFT` (= 228), and the `[215-222]` free gap for the planned `CURSE_COUNT_SHIFT = 215`; grep that no full-slot `mintPacked_` writer clobbers bits 215-222.
    - `contracts/DegenerusDeityPass.sol`: the `ownerOf(deityId)` smite-gate surface (soulbound, `tokenId = symbolId` 0-31).
    - `contracts/BurnieCoin.sol`: `burnCoin` (~:572, `onlyGame`).
    - `contracts/StakedDegenerusStonk.sol`: the redemption-snapshot activity-score read (~:942).
    - `contracts/test/SettleClaimableShortfallTester.sol`: the `_settleShortfall`-signature consumer (test-side).
    Write `375-ANCHOR-REATTESTATION.md` with a table: columns = Anchor (file + symbol) | Cited line (CONTEXT.md) | Status (CONFIRMED / CORRECTED) | Actual line @ 2bee6d6f | Evidence (the git command + the matched source line). Group rows by file. For the `[215-222]` gap, record the confirmed neighbours (`AFFILIATE_BONUS_POINTS_SHIFT = 209` spanning to bit 214; `LEVEL_UNITS_SHIFT = 228`) and the grep result proving no `mintPacked_` full-word writer clobbers 215-222. Do NOT read the working-tree `.sol` files as source of truth — read each anchor from `2bee6d6f` via git.
  </action>
  <verify>
    <automated>test -f .planning/phases/375-spec-design-lock-open-knobs-anchor-re-attestation-vs-2bee6d6/375-ANCHOR-REATTESTATION.md && grep -q "2bee6d6f" .planning/phases/375-spec-design-lock-open-knobs-anchor-re-attestation-vs-2bee6d6/375-ANCHOR-REATTESTATION.md && grep -Eqi "CONFIRMED|CORRECTED" .planning/phases/375-spec-design-lock-open-knobs-anchor-re-attestation-vs-2bee6d6/375-ANCHOR-REATTESTATION.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `375-ANCHOR-REATTESTATION.md` exists and contains a re-attested anchor table grouped by file.
    - Every file + symbol enumerated in CONTEXT.md `<canonical_refs>` "Contract anchors" appears as a row (none dropped).
    - Each row records a Status of CONFIRMED or CORRECTED with the actual line at `2bee6d6f` and the git-command evidence.
    - Spot-check rows present and correct: `DegenerusGameMintModule.sol` `purchaseWith` resolves to line 858 at `2bee6d6f`; `DegenerusGameStorage.sol` `claimablePool` `uint128` decl is recorded at its ACTUAL baseline line (CONTEXT.md's ~:838-839 is the doc-comment region, not the `uint128 internal claimablePool;` decl — the row must distinguish them); `BitPackingLib.sol` confirms `AFFILIATE_BONUS_POINTS_SHIFT = 209` (ends bit 214) and `LEVEL_UNITS_SHIFT = 228`, leaving `[215-222]` free for `CURSE_COUNT_SHIFT = 215`.
    - A grep result is recorded proving no full-slot `mintPacked_` writer clobbers bits 215-222.
    - No `contracts/*.sol` file is modified (this task only reads via git and writes the `.planning/` artifact).
  </acceptance_criteria>
  <done>
    The re-attested anchor table covers every CONTEXT.md anchor with a CONFIRMED/CORRECTED status and baseline-line evidence; line drift (if any) is corrected against `2bee6d6f`; the `[215-222]` free-gap claim is empirically confirmed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resolve the three SPEC verification items (purchaseWith-dead · self-smite sanity · SOLVENCY accessor-invariant location)</name>
  <files>.planning/phases/375-spec-design-lock-open-knobs-anchor-re-attestation-vs-2bee6d6/375-ANCHOR-REATTESTATION.md</files>
  <read_first>
    - .planning/phases/375-spec-design-lock-open-knobs-anchor-re-attestation-vs-2bee6d6/375-CONTEXT.md (`<decisions>` → "SPEC-execution items": `purchaseWith` dead-confirm, self-smite sanity, SOLVENCY accessor-invariant location; `<canonical_refs>` for the anchor neighborhoods)
    - .planning/PLAN-V61-DEITY-SMITE.md (§4 open: self-smite is the item to verify harmless)
    - .planning/PLAN-V61-AFKING-AS-PAYMENT-SOURCE.md (§8 open checks: `purchaseWith` dead-confirm; §6 PACK sub-concept = the SOLVENCY accessor-invariant home)
  </read_first>
  <action>
    Append three resolved verification sections to `375-ANCHOR-REATTESTATION.md`:
    (1) `purchaseWith`-dead confirm — at `2bee6d6f`, grep for every reference to `purchaseWith` across `contracts/` and the `IDegenerusGameModules` interface; confirm it is dead (only the function def in `DegenerusGameMintModule.sol:858`, the `IDegenerusGameModules` interface entry ~:242, and stale comments — NO call site, NO selector/`.selector` use, NO dispatch). Record the grep output and the verdict (dead → leave untouched at IMPL; if any live call site is found, flag it so the SPEC wires the afking interaction instead).
    (2) Self-smite sanity — reason through (citing the shared-counter mechanics in `PLAN-V61-DEITY-SMITE.md` + the CURSE APPLY at `MintStreakUtils:~320`) whether a deity paying 200 BURNIE to `smite` their OWN address yields any score-floor or bounty exploit via the shared `uint8` counter. The counter only ever LOWERS the activity-score multiplier (floored 0) and `smite` burns BURNIE from the caller; confirm self-smite is harmless-by-design (a self-inflicted penalty + a self-paid BURNIE burn, no positive-EV path). Record the verdict with the reasoning chain.
    (3) SOLVENCY accessor-invariant location — name the ONE place the `claimablePool == Σ(claimable + afking)` identity is centralized: the PACK accessor layer (`_creditClaimable/_debitClaimable/_creditAfking/_debitAfking`, each paired with the `claimablePool` update). Record the target file/region at `2bee6d6f` where this accessor layer lands (`DegenerusGamePayoutUtils.sol` centralized credits + the `DegenerusGameStorage.sol` `claimablePool` decl + `_settleShortfall`), re-attested with baseline lines, so the SPEC and SEC-02 (378) can point at the single invariant home.
  </action>
  <verify>
    <automated>grep -Eqi "purchaseWith" .planning/phases/375-spec-design-lock-open-knobs-anchor-re-attestation-vs-2bee6d6/375-ANCHOR-REATTESTATION.md && grep -Eqi "self-smite|self smite" .planning/phases/375-spec-design-lock-open-knobs-anchor-re-attestation-vs-2bee6d6/375-ANCHOR-REATTESTATION.md && grep -Eqi "claimablePool == |claimablePool ==Σ|SOLVENCY" .planning/phases/375-spec-design-lock-open-knobs-anchor-re-attestation-vs-2bee6d6/375-ANCHOR-REATTESTATION.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `375-ANCHOR-REATTESTATION.md` contains a `purchaseWith`-dead section with the grep output across `contracts/` at `2bee6d6f` and an explicit verdict (dead / live).
    - The verdict names the surviving references (def at `:858` + the `IDegenerusGameModules` interface entry + comments) and confirms NO call site / selector / dispatch (or flags the live site if found).
    - A self-smite section records the reasoning chain (shared counter only lowers the score floored 0; caller burns their own BURNIE) and the harmless-by-design verdict — no subjective "looks fine" language.
    - A SOLVENCY accessor-invariant section names the single centralized home (the PACK accessor layer) with re-attested baseline line references (`DegenerusGamePayoutUtils.sol` credits + `DegenerusGameStorage.sol` `claimablePool` + `_settleShortfall`) so SEC-02 (378) can anchor on it.
    - No `contracts/*.sol` file is modified.
  </acceptance_criteria>
  <done>
    All three SPEC verification items are resolved in writing with baseline-grounded evidence: `purchaseWith` confirmed dead (or its live site flagged), self-smite confirmed harmless via the shared-counter reasoning, and the SOLVENCY accessor-invariant location pinned to the single accessor-layer home.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan-doc anchors → IMPL edit sites | The SPEC's cited `file:line` values cross into Phase 376 as the authoritative edit targets; a wrong line steers an edit to the wrong location |
| working-tree HEAD → frozen baseline `2bee6d6f` | The plan docs were grepped at HEAD; the IMPL subject is `2bee6d6f`; any line drift between the two is the integrity gap this plan closes |

## STRIDE Threat Register (specification-integrity framing)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-375-01 | Tampering | The re-attested anchor table → 376 edit targets | mitigate | Re-attest EVERY CONTEXT.md anchor via `git grep -n … 2bee6d6f` and record CONFIRMED/CORRECTED with baseline-line evidence; a wrong/stale line that mis-targets the `claimablePool -=` pairing or the `_settleShortfall` generalization would silently break SOLVENCY-01 in the 376 diff — re-grounding on `2bee6d6f` removes the drift |
| T-375-02 | Information disclosure | `purchaseWith` dead-confirm | mitigate | Empirically grep for any live call site/selector at `2bee6d6f`; if `purchaseWith` is wrongly assumed dead but is live, the AFPAY waterfall would skip a real claimable-spend path → an un-gated afking draw. The grep verdict closes the assumption |
| T-375-03 | Elevation of privilege | Self-smite via the shared curse counter | accept | Reasoned harmless-by-design: the shared `uint8` counter only LOWERS the score (floored 0) and `smite` burns the caller's own BURNIE → no positive-EV / score-floor / bounty path. Recorded as accepted with the reasoning chain, not just asserted |
| T-375-04 | Tampering | Working-tree `.sol` mistaken for the IMPL subject | mitigate | Anchors are read FROM `2bee6d6f` via git, never from the working tree (which is 10 commits ahead of origin and may be further mutated); the artifact records the git evidence so Plan 02 cites the baseline, not HEAD |
</threat_model>

<verification>
- `375-ANCHOR-REATTESTATION.md` exists under the phase directory.
- Every CONTEXT.md "Contract anchors" file + symbol appears as a re-attested row (CONFIRMED/CORRECTED + baseline line + git evidence).
- The three verification items (`purchaseWith`-dead, self-smite, SOLVENCY location) are resolved with baseline-grounded reasoning.
- `git status --porcelain contracts/` shows NO modified `contracts/*.sol` (paper-only).
</verification>

<success_criteria>
- A re-attested anchor table grounded on `2bee6d6f` exists, covering every CONTEXT.md anchor, with all line drift corrected.
- `purchaseWith` is empirically confirmed dead (or its live site flagged) at the baseline.
- Self-smite is confirmed harmless-by-design via the shared-counter reasoning chain.
- The SOLVENCY accessor-invariant location is pinned to the single accessor-layer home with re-attested lines.
- ZERO `contracts/*.sol` modified.
</success_criteria>

<output>
Create `.planning/phases/375-spec-design-lock-open-knobs-anchor-re-attestation-vs-2bee6d6/375-01-SUMMARY.md` when done. The SUMMARY MUST state the artifact path (`375-ANCHOR-REATTESTATION.md`), the count of anchors re-attested, the count CORRECTED (drift found), and the three verification-item verdicts — Plan 02 consumes these.
</output>
