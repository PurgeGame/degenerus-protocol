---
phase: 27-payout-claim-path-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v3.0-payout-scatter-decimator.md
autonomous: true
requirements:
  - PAY-03
  - PAY-04
  - PAY-05
  - PAY-06

must_haves:
  truths:
    - "BAF normal scatter payout has an explicit PASS or FINDING verdict with file:line references"
    - "BAF century scatter payout has an explicit PASS or FINDING verdict with file:line references"
    - "Decimator normal claims have an explicit PASS or FINDING verdict with file:line references"
    - "Decimator x00 claims have an explicit PASS or FINDING verdict with file:line references"
    - "Pool source distinction (baseFuturePool vs futurePoolLocal) is verified correct for each distribution"
    - "lastDecClaimRound expiry is verified as by-design behavior"
  artifacts:
    - path: "audit/v3.0-payout-scatter-decimator.md"
      provides: "PAY-03, PAY-04, PAY-05, PAY-06 audit verdicts"
      contains: "PAY-03"
    - path: "audit/v3.0-payout-scatter-decimator.md"
      provides: "PAY-05 and PAY-06 decimator verdicts"
      contains: "PAY-05"
  key_links:
    - from: "contracts/modules/DegenerusGameEndgameModule.sol"
      to: "audit/v3.0-payout-scatter-decimator.md"
      via: "BAF scatter code audited"
      pattern: "PAY-03.*PASS|PAY-03.*FINDING"
    - from: "contracts/modules/DegenerusGameDecimatorModule.sol"
      to: "audit/v3.0-payout-scatter-decimator.md"
      via: "decimator claim code audited"
      pattern: "PAY-05.*PASS|PAY-05.*FINDING"
---

<objective>
Audit the scatter and decimator distribution paths: BAF normal scatter (PAY-03), BAF century scatter (PAY-04), decimator normal claims (PAY-05), and decimator x00 claims (PAY-06).

Purpose: These four distribution systems share the critical pool source distinction between `baseFuturePool` (snapshot at level entry) and `futurePoolLocal` (running total after BAF deduction). Pool source confusion is identified in the research as the primary risk for this audit wave. The decimator claim mechanics include the `lastDecClaimRound` overwrite expiry pattern that must be verified as by-design.

Output: audit/v3.0-payout-scatter-decimator.md with PASS/FINDING verdicts for PAY-03, PAY-04, PAY-05, and PAY-06.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-payout-claim-path-audit/27-RESEARCH.md
@audit/v1.1-transition-jackpots.md
@audit/v1.1-ECONOMICS-PRIMER.md
@audit/v1.1-parameter-reference.md
@audit/v3.0-gameover-core-distribution.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit BAF normal and century scatter payouts (PAY-03, PAY-04)</name>
  <files>audit/v3.0-payout-scatter-decimator.md</files>
  <read_first>
    - contracts/modules/DegenerusGameEndgameModule.sol (full file -- 540 lines, lines 138-400 for BAF scatter)
    - contracts/DegenerusJackpots.sol (full file -- 689 lines, BAF winner selection logic)
    - contracts/modules/DegenerusGamePayoutUtils.sol (full file -- 94 lines, _creditClaimable, _queueWhalePassClaimCore)
    - contracts/storage/DegenerusGameStorage.sol (search for: baseFuturePool, futurePoolLocal, futurePrizePool, whalePassClaims)
    - audit/v1.1-transition-jackpots.md (BAF specification, Section 1 "Execution Order")
    - audit/v1.1-parameter-reference.md (BAF percentage constants)
  </read_first>
  <action>
Audit the BAF normal scatter payout (PAY-03) and BAF century scatter payout (PAY-04). Read EndgameModule.sol lines 138-400 line by line. Also read DegenerusJackpots.sol for the winner selection logic.

Create the file audit/v3.0-payout-scatter-decimator.md with:
- Title: "# Payout Path Audit: Scatter and Decimator (PAY-03, PAY-04, PAY-05, PAY-06)"
- Executive Summary section (placeholder for PAY-05, PAY-06 from Task 2)
- Severity Definitions (per C4A standards)

**PAY-03: BAF Normal Scatter Payout** (EndgameModule:138-400)

The audit MUST cover:

1. **Trigger conditions**: Verify BAF fires at level transitions (every level). Verify the trigger is inside the `advanceGame` flow via delegatecall.

2. **Pool source verification**: Verify BAF normal uses `futurePoolLocal` (the running total AFTER previous deductions). This is the critical pool confusion risk. Verify the percentage: 10% of `futurePoolLocal` for normal BAF. Cross-reference against v1.1-transition-jackpots.md Section 1.

3. **7-category prize split**: Verify the scatter distributes across 7 prize categories. Trace each category's recipient selection and payout formula. Verify the large/small winner classification threshold.

4. **Whale pass queueing**: Verify that large winners above the threshold get half as ETH and half queued as a whale pass claim via `_queueWhalePassClaimCore` (PayoutUtils:77-93). Verify the whale pass remainder correctly increments `claimablePool`. Verify `whalePassClaims[player]` has no expiry (CP-07 from research -- confirm intentional).

5. **claimablePool accounting**: Trace every `claimablePool` mutation through the BAF normal path. Verify `balance >= claimablePool` invariant holds after each step.

6. **CEI ordering**: Verify no reentrancy path. BAF runs inside advanceGame (delegatecall context) so external calls are restricted.

**PAY-04: BAF Century Scatter Payout** (EndgameModule:175-184)

The audit MUST cover:

1. **Century trigger**: Verify x00 BAF fires at levels 100, 200, 300, etc. Verify the trigger condition in code.

2. **Enhanced pool source**: Verify x00 BAF uses 20% of `baseFuturePool` (snapshot at level entry, BEFORE normal BAF deduction). This is distinct from normal BAF which uses `futurePoolLocal`. Cross-reference against v1.1-transition-jackpots.md.

3. **Different scatter sampling**: Verify the century scatter uses 4 rounds each at lvl+1/+2/+3 and 38 rounds randomly from past 99 levels. Verify this matches the v1.1 spec.

4. **Distribution mechanics**: Verify the payout calculation and recipient selection follow the same 7-category split as normal BAF or document differences.

Write PAY-03 and PAY-04 verdict sections:
```
### PAY-XX: [Title]
**Verdict:** PASS | FINDING-[severity]
**Files:** [file:line-range]
**Summary:** [1-2 sentences]
**Pool Source:** [which pool variable and percentage]
**claimablePool Impact:** [how this path affects claimablePool]
**CEI Status:** [correct/violation]
**Double-Claim Guard:** [mechanism]
**Auto-Rebuy Interaction:** [verified / N/A]
**Recommendation:** [fix if FINDING, or "None" if PASS]
```
  </action>
  <verify>
    <automated>grep -c "PAY-03" audit/v3.0-payout-scatter-decimator.md | grep -v "^0$" && grep -E "PAY-03.*(PASS|FINDING)" audit/v3.0-payout-scatter-decimator.md && grep -E "PAY-04.*(PASS|FINDING)" audit/v3.0-payout-scatter-decimator.md && grep "baseFuturePool\|futurePoolLocal" audit/v3.0-payout-scatter-decimator.md | head -1 && grep -c "whalePass" audit/v3.0-payout-scatter-decimator.md | grep -v "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.0-payout-scatter-decimator.md exists and contains "PAY-03" followed by "PASS" or "FINDING"
    - File contains "PAY-04" followed by "PASS" or "FINDING"
    - File contains "baseFuturePool" AND "futurePoolLocal" showing pool source distinction was verified
    - File contains "whalePass" analysis (whale pass queueing for large winners)
    - File contains "10%" for normal BAF and "20%" for century BAF pool percentages
    - File contains DegenerusJackpots.sol reference for winner selection
    - File contains "claimablePool" mutation trace for BAF path
    - File contains "7-category" or "7 prize" or "scatter" distribution analysis
    - File contains "x00" or "century" trigger condition verification for PAY-04
    - File contains CEI ordering verification
  </acceptance_criteria>
  <done>PAY-03 and PAY-04 each have explicit PASS or FINDING verdicts with file:line references. Pool source distinction (baseFuturePool vs futurePoolLocal) verified for both normal and century BAF. Whale pass queueing mechanics verified. 7-category prize split traced. claimablePool invariant verified through BAF paths.</done>
</task>

<task type="auto">
  <name>Task 2: Audit decimator normal and x00 claims (PAY-05, PAY-06)</name>
  <files>audit/v3.0-payout-scatter-decimator.md</files>
  <read_first>
    - contracts/modules/DegenerusGameDecimatorModule.sol (lines 297-547 for normal decimator claims, lines 1-296 for resolution)
    - contracts/DegenerusGame.sol (lines 1293-1305 for claimDecimatorJackpot dispatch)
    - contracts/modules/DegenerusGameEndgameModule.sol (lines 175-184 for x00 decimator pool allocation)
    - contracts/storage/DegenerusGameStorage.sol (search for: lastDecClaimRound, decBurn, prizePoolFrozen, DecEntry)
    - audit/v3.0-payout-scatter-decimator.md (read current state from Task 1)
    - audit/v1.1-transition-jackpots.md (decimator specification, Section 8 "Claims expire")
    - audit/v3.0-gameover-core-distribution.md (terminal decimator context from Phase 26 -- distinct from normal decimator)
  </read_first>
  <action>
Audit the decimator normal claims (PAY-05) and decimator x00 claims (PAY-06). Read DecimatorModule.sol lines 297-547 line by line. Append PAY-05 and PAY-06 sections to audit/v3.0-payout-scatter-decimator.md and update the Executive Summary.

**PAY-05: Decimator Normal Claims** (DecimatorModule:297-547)

The audit MUST cover:

1. **Pro-rata formula**: Verify `amountWei = (poolWei * playerBurn) / totalBurn` for the normal decimator claim. Verify `poolWei` is sourced from the correct pool (10% of `futurePoolLocal` for normal decimator). Verify rounding direction favors the protocol (truncation, not rounding up).

2. **50/50 ETH/lootbox split**: Verify normal decimator gives 50% as direct ETH credit and 50% as lootbox tickets (via `_creditDecJackpotClaimCore`). Verify this differs from GAMEOVER mode (which gives 100% ETH, already verified in Phase 26).

3. **lastDecClaimRound overwrite expiry**: Verify `lastDecClaimRound` is overwritten on each new decimator resolution. Verify this permanently expires claims from prior rounds. Confirm this is by-design per v1.1-transition-jackpots.md Section 8 (CP-02 from research). Classify as INFO if not documented in natspec, not as a vulnerability.

4. **Double-claim guard**: Verify `decBurn[lvl][player].claimed` flag is set to 1 on claim. Verify `claimed == 1` prevents re-claiming the same round. Verify the `claimed` flag is per-round (not global).

5. **prizePoolFrozen guard**: Verify `if (prizePoolFrozen) revert E();` prevents decimator claims during jackpot phase. Verify this prevents futurePool corruption from concurrent decimator and jackpot distributions.

6. **claimablePool accounting**: Trace `claimablePool` mutations through `_addClaimableEth` -> `_creditClaimable`. Verify the delta is correct. Verify `claimablePool` at DecimatorModule:478/490/519 (from Research mutation site table).

**PAY-06: Decimator x00 Claims** (EndgameModule:175-184, DecimatorModule)

The audit MUST cover:

1. **Pool source**: Verify x00 decimator uses 30% of `baseFuturePool` (snapshot BEFORE BAF deduction). This is distinct from normal decimator (10% of `futurePoolLocal`). Cross-reference against v1.1-transition-jackpots.md.

2. **Separate resolution**: Verify x00 decimator has its own resolution separate from normal decimator. Verify whether it uses the same `lastDecClaimRound` struct or a different one.

3. **Enhanced payout**: Verify the 30% pool allocation (vs 10% normal). Verify claim mechanism uses the same pro-rata formula and 50/50 ETH/lootbox split.

4. **Eligibility**: Verify x00 decimator is only available at century levels (100, 200, 300...). Verify the level check in the claim path.

Update the Executive Summary to cover all four requirements. Add a "Pool Source Summary" section documenting which pool each distribution uses:
- BAF normal: `futurePoolLocal` * 10%
- BAF century: `baseFuturePool` * 20%
- Decimator normal: `futurePoolLocal` * 10%
- Decimator x00: `baseFuturePool` * 30%
  </action>
  <verify>
    <automated>grep -E "PAY-05.*(PASS|FINDING)" audit/v3.0-payout-scatter-decimator.md && grep -E "PAY-06.*(PASS|FINDING)" audit/v3.0-payout-scatter-decimator.md && grep -c "lastDecClaimRound" audit/v3.0-payout-scatter-decimator.md | grep -v "^0$" && grep -c "prizePoolFrozen" audit/v3.0-payout-scatter-decimator.md | grep -v "^0$" && grep "Pool Source Summary\|pool source" audit/v3.0-payout-scatter-decimator.md | head -1 && grep "Executive Summary" audit/v3.0-payout-scatter-decimator.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.0-payout-scatter-decimator.md contains "PAY-05" followed by "PASS" or "FINDING"
    - File contains "PAY-06" followed by "PASS" or "FINDING"
    - File contains "lastDecClaimRound" with overwrite expiry analysis
    - File contains "prizePoolFrozen" guard analysis
    - File contains "decBurn" or "claimed" double-claim guard analysis
    - File contains "30%" for x00 decimator pool allocation
    - File contains "10%" for normal decimator pool allocation
    - File contains "Pool Source Summary" or equivalent section with all 4 distribution pool sources
    - File contains "Executive Summary" covering PAY-03, PAY-04, PAY-05, PAY-06
    - File contains pro-rata formula analysis with rounding direction verification
    - File contains "50/50" or "50%" ETH/lootbox split analysis for normal claims
    - File contains reference to Phase 26 terminal decimator (distinct from normal)
  </acceptance_criteria>
  <done>PAY-05 and PAY-06 each have explicit PASS or FINDING verdicts with file:line references. Pro-rata claim formula verified. lastDecClaimRound overwrite expiry confirmed as by-design. Double-claim guards verified. prizePoolFrozen guard verified. Pool source summary documents all 4 distributions correctly. Executive summary covers all four requirements.</done>
</task>

</tasks>

<verification>
- audit/v3.0-payout-scatter-decimator.md exists with PAY-03, PAY-04, PAY-05, PAY-06 sections
- All four verdicts are explicit PASS or FINDING-[severity] with file:line references
- Pool source distinction (baseFuturePool vs futurePoolLocal) verified for each distribution
- Decimator claim expiry verified as by-design with v1.1 spec reference
- claimablePool mutations traced through every scatter and decimator path
- Whale pass queueing mechanics verified
- No pool source confusion in any distribution formula
</verification>

<success_criteria>
- PAY-03 verdict: BAF normal scatter with 10% futurePoolLocal verified
- PAY-04 verdict: BAF century scatter with 20% baseFuturePool verified
- PAY-05 verdict: Decimator normal claims with pro-rata formula, 50/50 split, and expiry verified
- PAY-06 verdict: Decimator x00 claims with 30% baseFuturePool verified
- Pool source summary provides definitive reference for Phase 27 consolidation
</success_criteria>

<output>
After completion, create `.planning/phases/27-payout-claim-path-audit/27-02-SUMMARY.md`
</output>
