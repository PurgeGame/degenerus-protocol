---
phase: 351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat
plan: 09
type: execute
wave: 3
depends_on: ["351-02", "351-03", "351-04", "351-05", "351-06", "351-07", "351-08"]
files_modified:
  - test/REGRESSION-BASELINE-v55.md
autonomous: true
requirements: [TST-05]
must_haves:
  truths:
    - "The whole-tree forge test run AT the v55 TST HEAD is recorded (passed/failed/skipped) with the IMPL→TST delta"
    - "Every forge red is enumerated BY NAME and ∈ the v55.0 union (live − union == ∅, net-zero new regression vs 20ca1f79)"
    - "The D-351-01 rewrite map is reconciled: which v54 baseline test became which adapted test"
    - "Every D-351-02 removed-surface drop is listed BY NAME + reason (batchPurchase/BatchBuy; AfKing.depositFor/poolOf/withdraw custody; batchPurchase try/catch-isolation leg)"
    - "The v55 TST-01..06 green proofs are listed (additive green, contribute zero red)"
    - "The Hardhat .test.js suite is confirmed still compiling + passing as a sanity check"
    - "git diff 453f8073 HEAD -- contracts/ stays EMPTY (full-tree run, not --match-path)"
  artifacts:
    - path: "test/REGRESSION-BASELINE-v55.md"
      provides: "the BY-NAME NON-WIDENING ledger vs the v54 baseline 20ca1f79"
      contains: "net-zero new regression"
  key_links:
    - from: "test/REGRESSION-BASELINE-v55.md"
      to: "the whole-tree forge test failing set"
      via: "live − union == ∅ binding subset gate"
      pattern: "net-zero"
---

<objective>
Author `test/REGRESSION-BASELINE-v55.md` (the BY-NAME NON-WIDENING ledger, mirroring
`REGRESSION-BASELINE-v50.md`/`-v49.md` EXACTLY) and run the authoritative whole-tree `forge test` at the v55
TST HEAD to prove net-zero new regression vs the v54 baseline `20ca1f79`. This is the LAST wave — it depends on
the ENTIRE suite compiling (all corpus adaptations + new proofs from 351-02..08) and the D-351-02 drops being
finalized. Because the afking corpus was rewritten WHOLESALE (D-351-01, not extended), the ledger must
additionally reconcile the rewrite map (which v54 baseline test became which adapted test) + list every
D-351-02 removed-surface drop BY NAME + reason. The baseline carries against `20ca1f79` (v54) — there is no
`REGRESSION-BASELINE-v54.md`, so v55 is the first ledger off the v54 baseline.

Purpose: TST-05 — the suite compiles + passes against the game-resident model with net-zero new regression vs
`20ca1f79`, every pre-existing red enumerated BY NAME, the rewrites + drops reconciled, the v55 proofs additive
green, and the Hardhat suite confirmed as a sanity check.
Output: `test/REGRESSION-BASELINE-v55.md`. ZERO `contracts/*.sol` mutation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-CONTEXT.md
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-PATTERNS.md
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-02-SUMMARY.md
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-03-SUMMARY.md
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-04-SUMMARY.md
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-05-SUMMARY.md
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-06-SUMMARY.md
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-07-SUMMARY.md
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-08-SUMMARY.md
@test/REGRESSION-BASELINE-v50.md

<interfaces>
<!-- The ledger format (mirror v50 EXACTLY) + the v55-specific reconciliation. -->

The 7-section format to mirror (REGRESSION-BASELINE-v50.md):
  1. TST-HEAD arithmetic + reconciliation (passed/failed/skipped table; IMPL HEAD → TST HEAD delta).
  2. The carried-forward union BY NAME — bucketed (A = VRF/RNG-window reds, B = stale-harness/behavioral, C = HERO-deferred), every red (suite-basename, testName).
  3. NEW vs the prior baseline — the deltas with provenance (the union = prior − {OUT} + {IN} delta-math).
  4. Flaky-cluster non-determinism analysis (the unseeded [invariant] DegeneretteBet.inv cluster ⊆-gate rationale; foundry.toml [fuzz] seed=0xdeadbeef but NO [invariant] seed — confirmed).
  5. NEW green proof files (the v55 TST-01..06 additive-green table).
  6. Net-zero-new-regression PROOF (live − union == ∅ binding ⊆-gate + the FC1-FC5 false-confidence guards).
  7. Scope attestation (git diff 453f8073 HEAD -- contracts/ EMPTY; full-tree run, NOT --match-path).

The binding headline (mirror v50 :21-33): at the v55 TST HEAD, every forge test failing test ∈ the v55.0 §2
  enumerated union BY NAME — live failing set − the §2 union == ∅ — net-zero new regression.

v55-SPECIFIC reconciliation (CONTEXT D-351-01 + PATTERNS section 14):
  - The baseline carries against the v54 `20ca1f79` (NOT v49/v50; no REGRESSION-BASELINE-v54.md exists — v55 is the first off v54).
    The v54 baseline reds must be established: run forge test at 20ca1f79's contracts (the FROZEN subject) OR derive from the
    v54 close record. The carried-over reds that STILL exist (A7 vm.assume-exhaustion, the unseeded DegeneretteBet.inv cluster, etc.) enumerate BY NAME.
  - Because the corpus is REWRITTEN wholesale, §2/§3 must reconcile the REWRITE MAP: which v54 baseline test (suite, name) became which
    adapted test (the 351-02..08 SUMMARYs record the per-file adaptations). A renamed/relocated test is NOT a new red — it is a rewrite.
  - List every D-351-02 removed-surface DROP BY NAME + reason (the 351-05 + 351-06 SUMMARYs record them):
    the batchPurchase/BatchBuy/AF_KING-custody surface, the KeeperNonBrick batchPurchase try/catch-isolation leg, the RedemptionStethFallback AfKing-custody-recovery leg.
  - The v55 TST-01..06 proofs are ADDITIVE GREEN (V55FreezeDeterminism, V55RevertFreeEvCap, V55SetMutationOpenE, V55AfkingGasMarginal + the adapted corpus greens).

Foundry-focused (CONTEXT Discretion): the BY-NAME ledger is Foundry-centric. The Hardhat .test.js suite
  (test/unit/*.test.js — incl. DegenerusGame.test.js) is confirmed still compiling + passing as a SANITY CHECK
  (adapt any Hardhat test referencing a changed afking ABI method); it is NOT the primary BY-NAME ledger.

Run discipline: the AUTHORITATIVE run is the WHOLE TREE (`forge test`, NOT `--match-path` / `--match-contract`) —
  a scoped run would hide a NEW red in an un-adapted file. foundry.toml [fuzz] seed=0xdeadbeef (deterministic);
  [invariant] unseeded (the flaky cluster — §4). Placement: test/ (alongside v48/v49/v50 — confirmed by precedent).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the authoritative whole-tree forge test + establish the v54 20ca1f79 baseline red union BY NAME</name>
  <read_first>
    - test/REGRESSION-BASELINE-v50.md (FULL — the exact 7-section format + the bucket A/B/C structure + the ⊆-gate headline + the FC1-FC5 guards)
    - test/REGRESSION-BASELINE-v49.md (the 666/42/17 BY-NAME precedent — the bucket taxonomy)
    - .planning/STATE.md (the v54.0 close record — 343 SPEC + 344 IMPL shipped 20ca1f79; the baseline the v55 ledger carries against)
    - all of 351-02..08 SUMMARYs (the per-file adaptation records + the D-351-02 drops + the carried-forward reds noted by name, e.g. the RngLockDeterminism A7 red)
  </read_first>
  <action>
    Run the AUTHORITATIVE whole-tree `forge test` at the v55 TST HEAD (NOT `--match-path`/`--match-contract` — a
    scoped run hides a new red in an un-adapted file). Capture passed/failed/skipped + the EXACT failing test names
    (suite-basename + testName). Establish the v54 `20ca1f79` baseline red union BY NAME: either (a) check out the
    `20ca1f79` contracts into a scratch state and run forge test to capture its red union, OR (b) derive it from the
    v54.0 close record (STATE.md / the 344 IMPL verification) + the carried-forward reds the 351-02..08 SUMMARYs
    flagged by name (the RngLockDeterminism A7 vm.assume-exhaustion red, the unseeded DegeneretteBet.inv cluster, the
    VRF/RNG-window bucket-A reds). Classify every current red into bucket A (VRF/RNG-window), B (stale-harness/
    behavioral), or C (HERO-deferred). Confirm the foundry.toml [fuzz] seed (0xdeadbeef, deterministic) + the
    [invariant] unseeded flaky-cluster (the DegeneretteBet.inv 0-N subset). Do NOT edit any contracts/ or test/*.sol file.
  </action>
  <verify>
    <automated>forge test 2>&1 | tail -3 captures the passed/failed/skipped summary; the failing test names are enumerated (count matches the summary's failed N)</automated>
  </verify>
  <acceptance_criteria>
    - The whole-tree `forge test` summary (passed/failed/skipped) is captured at the v55 TST HEAD (a full-tree run, no scoping flags).
    - Every failing test name (suite + testName) is enumerated and classified into bucket A/B/C.
    - The v54 `20ca1f79` baseline red union is established BY NAME (via re-run or the documented derivation).
    - The carried-forward reds the 351-02..08 SUMMARYs flagged (e.g. the A7 red) appear in the union.
    - `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>The authoritative whole-tree forge test run is captured, every red enumerated BY NAME + bucketed, and the v54 20ca1f79 baseline red union is established.</done>
</task>

<task type="auto">
  <name>Task 2: Author REGRESSION-BASELINE-v55.md — the 7-section BY-NAME ledger with the rewrite map + the D-351-02 drops + the net-zero proof</name>
  <read_first>
    - test/REGRESSION-BASELINE-v50.md (the section-by-section template to mirror EXACTLY — §1 arithmetic, §2 union, §3 deltas, §4 flaky cluster, §5 green proofs, §6 net-zero proof + FC guards, §7 scope attestation)
    - Task 1's captured run (the live failing set + the v54 baseline union + the bucket classification)
    - all of 351-02..08 SUMMARYs (the rewrite map — which baseline test → which adapted test; the D-351-02 drops BY NAME + reason; the v55 green proof file names)
    - .planning/REQUIREMENTS.md (TST-01..06 — the green proofs map to these)
  </read_first>
  <action>
    Create `test/REGRESSION-BASELINE-v55.md` mirroring the v50 7-section format EXACTLY, carrying the baseline against
    the v54 `20ca1f79` (NOT v49/v50). Author:
    §1 — the TST-HEAD arithmetic table (IMPL HEAD `453f8073`/`20ca1f79` baseline → v55 TST HEAD delta: passed/failed/
    skipped) + the reconciliation prose.
    §2 — the carried-forward union BY NAME, bucketed A/B/C, every red (suite-basename, testName) with a This-run column.
    §3 — NEW vs the v54 baseline: the union = prior − {OUT} + {IN} delta-math. CRITICAL v55 addition — reconcile the
    D-351-01 REWRITE MAP (which v54 baseline test became which adapted test, from the SUMMARYs) and list every
    D-351-02 removed-surface DROP BY NAME + reason (batchPurchase/BatchBuy/AF_KING-custody; the KeeperNonBrick
    batchPurchase try/catch-isolation leg; the RedemptionStethFallback AfKing-custody-recovery leg). A renamed/relocated
    test is a rewrite (OUT-old + IN-new with the map entry), NOT a new red.
    §4 — the unseeded DegeneretteBet.inv flaky-cluster ⊆-gate rationale (foundry.toml [fuzz] seeded / [invariant]
    unseeded; the 0-N red subset; the strict-equality→⊆ relaxation).
    §5 — the v55 TST-01..06 green proof files (V55FreezeDeterminism, V55RevertFreeEvCap, V55SetMutationOpenE,
    V55AfkingGasMarginal + the adapted corpus greens) — additive green, contribute zero red.
    §6 — the net-zero-new-regression PROOF: the binding `live − union == ∅` ⊆-gate headline + the FC1-FC5
    false-confidence guards (mirror v50 §6).
    §7 — the scope attestation: `git diff 453f8073 HEAD -- contracts/` EMPTY; the run was WHOLE-TREE (not --match-path).
    Place the file in `test/` (alongside v48/v49/v50). Do NOT edit any contracts/ or test/*.sol file.
  </action>
  <verify>
    <automated>test -f test/REGRESSION-BASELINE-v55.md AND grep -c "net-zero new regression\|live − union == ∅\|live - union" test/REGRESSION-BASELINE-v55.md >= 1 AND grep -c "20ca1f79" test/REGRESSION-BASELINE-v55.md >= 1 (carries against v54)</automated>
  </verify>
  <acceptance_criteria>
    - `test/REGRESSION-BASELINE-v55.md` exists with all 7 sections (mirroring v50's structure).
    - §2 enumerates every red BY NAME (suite + testName), bucketed A/B/C.
    - §3 reconciles the D-351-01 rewrite map (baseline test → adapted test) AND lists every D-351-02 drop BY NAME + reason.
    - §5 lists the v55 TST-01..06 green proof files.
    - §6 states the binding `live − union == ∅` net-zero-new-regression gate; §7 attests `git diff 453f8073 HEAD -- contracts/` EMPTY + a whole-tree run.
    - The baseline carries against `20ca1f79` (v54), confirmed in the header + §2.
    - `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>REGRESSION-BASELINE-v55.md is authored in the v50 7-section format, carrying against 20ca1f79, with the rewrite map + the D-351-02 drops reconciled BY NAME and the net-zero-new-regression proof.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm the Hardhat .test.js sanity arm + the binding net-zero subset gate</name>
  <read_first>
    - test/REGRESSION-BASELINE-v55.md (Task 2 output — the union + the net-zero gate to validate against)
    - test/unit/DegenerusGame.test.js (the Hardhat suite that may reference a changed afking ABI method — the sanity-check arm)
    - package.json (the Hardhat test invocation — `npx hardhat test` or the configured script)
    - .planning/phases/351-CONTEXT.md §"Claude's Discretion" (the Foundry-focused / Hardhat-sanity split)
  </read_first>
  <action>
    Run the Hardhat `.test.js` suite as the SANITY-CHECK arm (CONTEXT Discretion — Foundry is the primary BY-NAME
    ledger; Hardhat confirms still-compiling + passing). If any Hardhat test references a changed afking ABI method
    (e.g. a removed `afKing.*` or a renamed `doWork`→`mintBurnie`), adapt it minimally so the suite compiles + passes
    (this is a sanity arm — do not author new Hardhat coverage). Record the Hardhat pass/fail summary in
    REGRESSION-BASELINE-v55.md §7 (or a §7 sub-note) as the sanity confirmation. Then VALIDATE the binding gate:
    confirm `live failing set − the §2 union == ∅` (every observed forge red ∈ the §2 union BY NAME — no new red
    outside the baseline). If a NEW red OUTSIDE the union appears, STOP and record it (it is a real regression to
    investigate, not to paper over). Do NOT edit any contracts/ file.
  </action>
  <verify>
    <automated>(Hardhat) npx hardhat test 2>&1 | tail -5 captures pass/fail; (gate) the forge live-red set is confirmed ⊆ the §2 union (no name outside the union)</automated>
  </verify>
  <acceptance_criteria>
    - The Hardhat `.test.js` suite is run and its pass/fail summary is recorded in REGRESSION-BASELINE-v55.md §7.
    - Any Hardhat test referencing a changed afking ABI is adapted so the suite compiles + passes (sanity arm).
    - The binding gate is validated: `live forge red set − §2 union == ∅` (no new red outside the baseline) — OR a new red is recorded with a STOP if one exists.
    - `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>The Hardhat sanity arm is confirmed compiling+passing (recorded in §7), and the net-zero subset gate (live − union == ∅) is validated against the FROZEN v54 baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only. Authors a markdown ledger + runs the test suites. ZERO contract mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-351-09-FG | Tampering | the NON-WIDENING ledger + the authoritative run | mitigate | False-GREEN risks: (1) a SCOPED run (`--match-path`) hiding a new red in an un-adapted file → mitigated by the WHOLE-TREE `forge test` discipline (§7 attestation); (2) a rewritten-corpus test silently dropped without a ledger entry (coverage loss masquerading as net-zero) → mitigated by the §3 rewrite-map + the D-351-02 BY-NAME drop ledger reconciling every OUT; (3) a bare-count gate hiding a name-swap (a baseline red gone + a new red in, same count) → mitigated by the BY-NAME `live − union == ∅` subset gate, never a count; (4) the unseeded invariant flaky cluster mis-read as a regression → mitigated by the §4 ⊆-gate rationale. ZERO `contracts/` mutation (`git diff 453f8073 HEAD -- contracts/` empty). |
</threat_model>

<verification>
- The whole-tree `forge test` run is captured; every red ∈ the §2 union BY NAME (`live − union == ∅`).
- The D-351-01 rewrite map + the D-351-02 drops are reconciled BY NAME in §3.
- The Hardhat sanity arm is confirmed (recorded in §7).
- `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY (full-tree run, not --match-path).
</verification>

<success_criteria>
TST-05 is proven: the suite compiles + passes against the game-resident model with net-zero new regression vs
the v54 baseline `20ca1f79`, every red enumerated BY NAME, the wholesale-rewrite map + the D-351-02 drops
reconciled, the v55 TST-01..06 proofs additive green, and the Hardhat suite confirmed as a sanity check. The
v55.0 regression baseline is restored. ZERO `contracts/*.sol` mutation.
</success_criteria>

<output>
Create `.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-09-SUMMARY.md` when done.
</output>
