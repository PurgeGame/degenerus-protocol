---
phase: 351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat
plan: 02
type: execute
wave: 2
depends_on: ["351-01"]
files_modified:
  - test/fuzz/AfKingSubscription.t.sol
  - test/fuzz/AfKingFundingWaterfall.t.sol
  - test/fuzz/AfKingConcurrency.t.sol
  - test/fuzz/V55SetMutationOpenE.t.sol
autonomous: true
requirements: [TST-04, TST-02]
must_haves:
  truths:
    - "The adapted AfKing subscription/funding/concurrency fuzz files compile + run against game.subscribe"
    - "Set-mutation: no cursor advance after swap-pop (H-CANCEL-SWAP-MISS NEGATIVE-VERIFIED) on the game-resident set"
    - "Eviction/tombstone/reclaim preserve the mint-streak (scorePlus1) across a process cycle"
    - "The NO-ORPHAN guard holds: a sub removed between stamp and open gets no free box"
    - "The OPEN-E 4-protection regresses (consent-gate-at-subscribe / default-self / no-escalation / trust-the-sub temporal bound)"
    - "git diff 453f8073 HEAD -- contracts/ stays EMPTY"
  artifacts:
    - path: "test/fuzz/V55SetMutationOpenE.t.sol"
      provides: "TST-04 set-mutation + OPEN-E 4-protection regression proof"
      contains: "swap"
    - path: "test/fuzz/AfKingConcurrency.t.sol"
      provides: "adapted swap-pop / tombstone concurrency corpus (game-resident set)"
  key_links:
    - from: "test/fuzz/V55SetMutationOpenE.t.sol"
      to: "GameAfkingModule subscribe / processSubscriberStage swap-pop"
      via: "game.subscribe(...,dailyQuantity=0) cancel + advanceGame STAGE"
      pattern: "subscribe"
---

<objective>
Adapt the three `AfKing*` fuzz files (`AfKingSubscription`, `AfKingFundingWaterfall`, `AfKingConcurrency`) to
the game-resident `GameAfkingModule` path per the five call-site deltas (D-351-01), preserving maximum
coverage by reframing each property onto its v55 successor mechanism, and author the dedicated TST-04
set-mutation + OPEN-E 4-protection proof. `AfKingConcurrency` is the PRIMARY set-mutation / swap-pop /
tombstone analog (it proves the H-CANCEL-SWAP-MISS resolution that TST-04 regresses).

Purpose: TST-04 — the afking open route + the human open route coexist with no shared mutable-state hazard;
eviction/tombstone/swap-pop preserve the streak with "no cursor advance after swap-pop"; the OPEN-E
4-protection holds. Plus the funding-waterfall corners feed TST-02's revert-free funded-slice fuzz.
Output: 3 adapted fuzz files + 1 new TST-04 proof file. ZERO `contracts/*.sol` mutation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-CONTEXT.md
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-PATTERNS.md
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-01-SUMMARY.md

<interfaces>
<!-- The 5 mechanical call-site deltas (PATTERNS §"five load-bearing call-site deltas") — apply the subset each file uses. -->
<!-- READ-ONLY contract anchors at 453f8073; NEVER edit sites. -->

Δ1 import: drop `import {…} from "../../contracts/AfKing.sol"` (deleted) → repoint to DegenerusGame type / IDegenerusGame* interface, or drop.
Δ2 subscribe: `afKing.subscribe(player,drainFirst,useTickets,qty,reinvest,src)` → `game.subscribe(...)` — IDENTICAL 6-arg sig, receiver-only swap. (GameAfkingModule.sol:234; stub DegenerusGame.sol:363)
Δ3 doWork: `afKing.doWork()` → `game.mintBurnie()`. (GameAfkingModule.sol:985; stub DegenerusGame.sol:390)
Δ4 autoBuy: `afKing.autoBuy(N)` has NO successor — the per-sub buy folded into advanceGame()'s required-path STAGE.
   Drive it via a new-day `game.advanceGame()` (settle VRF as KeeperRewardRoutingSameResults._settleGame does) so
   processSubscriberStage(50) runs the funded subs. (STAGE DegenerusGameAdvanceModule.sol:305-312 → _runSubscriberStage :754 → processSubscriberStage(50) :759-761)
Δ5 views/cancel: `afKing.poolOf(x)` → `game.afkingFundingOf(x)` (DegenerusGame.sol:1579); `afKing.withdraw(a)` → `game.withdrawAfkingFunding(a)` (:1562).
   `subscriberCount`/`subscriptionOf`/`autoBuyProgress`/`setDailyQuantity`/`depositFor`/`unsubscribe` have NO game-exposed external view →
   read `_subscribers.length` / `_subOf[x]` / cursors via `vm.load` PINNED slots, or re-`subscribe(...,dailyQuantity=0)` to cancel.

Set-mutation successor mechanism (the swap-pop now on the game-resident set):
  GameAfkingModule.sol:395-401  uint256 last = _subscribers.length - 1; address mover = _subscribers[last]; _subscribers[idx] = mover; _subscribers.pop();
  STAGE reclaim WITHOUT advancing cursor: GameAfkingModule.sol:582-587 (delete _subOf[player]); subsFullyProcessed DegenerusGameStorage.sol:343
  NO-ORPHAN guard (NEW property to assert): GameAfkingModule.sol comment ~:557 "removal from _subscribers between stamp and open ORPHANS the box"

Storage layout for vm.load/vm.store (RE-DERIVE every slot via `forge inspect storage DegenerusGame` — the
  AfKing-standalone-layout constants SUBOF_SLOT/SUBSCRIBER_INDEX_SLOT/AUTOBUY_SLOT in the old files are WRONG):
  _subOf DegenerusGameStorage.sol:1902 · _subscribers :1914 · Sub struct :1867 (scorePlus1(16)+amount(96)+lastAutoBoughtDay(32)+lastOpenedDay(32)) · cursors/subsFullyProcessed :343

OPEN-E 4-protection (TST-04, from [[open-e-operator-approval-trust-boundary]]):
  consent-gate-at-subscribe (isOperatorApproved checked in subscribe) · default-self (src=address(0) → funder=self, byte-identical) ·
  no-escalation (operator cannot widen the grant) · trust-the-sub temporal bound. Subscribe-time gate at GameAfkingModule.sol subscribe (:234).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adapt AfKingConcurrency.t.sol (swap-pop / tombstone) + AfKingSubscription.t.sol (subscribe/event/views)</name>
  <read_first>
    - test/fuzz/AfKingConcurrency.t.sol (FULL — 774 lines; the docstring :23-34 set-mutation property, the slot-constant block ~:46-60 [WRONG layout — re-derive], the 68 afKing. call sites)
    - test/fuzz/AfKingSubscription.t.sol (FULL — 577 lines; setUp :68-71, the subscribe sites :157/:167/:326/:332/:351, doWork :269, subscriptionOf reads :105/:110/:137/:160/:171/:334, poolOf/withdraw :355/:378/:380, event sigs :60-66)
    - contracts/modules/GameAfkingModule.sol :234 (subscribe), :395-401 (swap-pop), :539 (processSubscriberStage), :582-587 (reclaim), :985 (mintBurnie)
    - contracts/storage/DegenerusGameStorage.sol :343 (cursors/subsFullyProcessed), :1867 (Sub), :1902 (_subOf), :1914 (_subscribers)
    - .planning/phases/351-PATTERNS.md §2 + §3 (the per-file adaptation maps)
  </read_first>
  <behavior>
    - AfKingConcurrency: a `setDailyQuantity(0)` equivalent (re-`game.subscribe(player,…,dailyQuantity=0,…)`) tombstones in-place; the deferred swap-pop reclaim at the freed index re-reads the moved occupant THIS pass → no active sub skipped (no cursor advance after swap-pop). Reframe onto _subscribers swap-pop (:395-401) + the STAGE reclaim (:582-587).
    - AfKingSubscription: `game.subscribe(...)` with the identical 6-arg sig registers a sub; the SubscriptionExtendedFree/SubscriptionExpired/CoinflipStakeUpdated events still emit from the game-resident module (re-confirm sigs by grep; rename if changed); `game.afkingFundingOf(s)` returns the funded pool; `game.withdrawAfkingFunding(x)` returns it.
    - Per-day epoch-determinism asserts → stamped-day determinism (scorePlus1/amount/lastAutoBoughtDay frozen at stamp).
  </behavior>
  <action>
    Apply Δ1-Δ5 to both files. In AfKingConcurrency: rewrite all 68 `afKing.` sites; `afKing.autoBuy(N)`→STAGE-drive
    via a new-day `advanceGame()` (port the _settleGame VRF-drain helper from KeeperRewardRoutingSameResults:111-127);
    the 10 `afKing.setDailyQuantity(0)` → re-`game.subscribe(player,…,dailyQuantity=0,…)`; `subscriberCount()`→
    `_subscribers.length` via vm.load; `autoBuyProgress()`→ `_subCursor`/`subsFullyProcessed` via vm.load.
    RE-DERIVE every pinned slot with `forge inspect storage DegenerusGame` (the :46-60 AfKing-layout constants are WRONG).
    Preserve the docstring :23-34 property verbatim, reframed onto the game-resident swap-pop. In AfKingSubscription:
    rewrite the subscribe/doWork/poolOf/withdraw sites (Δ2/Δ3/Δ5); the subscriptionOf field reads (validThroughLevel/
    dailyQuantity/fundingSource) → `_subOf[pass]` via vm.load (re-derived offsets); keep the vm.recordLogs + topic-decode
    event idiom, re-confirming each event sig emits from GameAfkingModule. Reframe the `_afkingEpoch` determinism asserts
    → stamped-day asserts (FREEZE-03 — box seeds from sub.lastAutoBoughtDay's rngWordByDay[day]). Do NOT add new `vm.skip`s;
    preserve any existing ones. Do NOT edit any contracts/ file.
  </action>
  <verify>
    <automated>forge test --match-contract "AfKingConcurrency|AfKingSubscription" 2>&1 | tail -8 (compiles + runs; reds only if pre-existing baseline — record names for 351-09, not vacuous green)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' test/fuzz/AfKingConcurrency.t.sol | grep -c "afKing\."` == 0 AND same for AfKingSubscription.t.sol.
    - `grep -c "import.*AfKing.sol" test/fuzz/AfKingConcurrency.t.sol test/fuzz/AfKingSubscription.t.sol` == 0.
    - Both files compile under `forge test --match-contract` (no `AfKing.sol not found`, no unknown-symbol on game.subscribe/mintBurnie/afkingFundingOf).
    - The swap-pop "no cursor advance" property test exists by name in AfKingConcurrency and asserts a non-vacuous outcome (the moved sub is processed THIS pass).
    - Every `vm.load`/`vm.store` slot is re-derived (no `SUBOF_SLOT = 1` / `AUTOBUY_SLOT = 4` AfKing-layout literal survives — grep shows none).
    - `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>AfKingConcurrency + AfKingSubscription compile + run on the game-resident path with all call-sites adapted and slots re-derived; the swap-pop no-skip property is reframed and asserted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adapt AfKingFundingWaterfall.t.sol (funding corners → afkingFunding SLOADs, feeds TST-02 funded-slice)</name>
  <read_first>
    - test/fuzz/AfKingFundingWaterfall.t.sol (FULL — 678 lines; the 39 afKing. sites, the LANDMINE-A exemption-spoof corners, the claimable-first vs funding-source vs pool waterfall)
    - contracts/modules/GameAfkingModule.sol :464 (afkingFunding[player]), :662 (funding-skip), :709 (afkingFunding[src] -=), :710 (claimablePool -=), :463 (claimableWinnings[player])
    - contracts/storage/DegenerusGameStorage.sol :358 (SOLVENCY-01 invariant)
    - .planning/phases/351-PATTERNS.md §4 (the funding-waterfall reframe + the v50-green testFundingSourceVaultDoesNotInheritExemption note)
  </read_first>
  <behavior>
    - The funding waterfall (claimable-first vs funding-source src vs pool) debits in the same order as v54, now read via in-context `afkingFunding[...]` SLOADs not cross-contract staticcalls.
    - `testFundingSourceVaultDoesNotInheritExemption` (B10, v50-green) stays GREEN — the LANDMINE-A exemption-spoof is rejected.
    - A funded well-formed buy never reverts (feeds TST-02): exercise amount / claimable-mix corners across the waterfall.
  </behavior>
  <action>
    Apply Δ4/Δ5: `afKing.poolOf(src)`→`game.afkingFundingOf(src)`; `afKing.autoBuy(50)`→STAGE-drive via a new-day
    `advanceGame()`; `afKing.subscriberCount()`→`_subscribers.length` via vm.load (re-derived slot). Reframe the
    cross-contract `afkingFundingOf`/`poolOf` STATICCALL plumbing → in-context `afkingFunding[...]` SLOAD assertions
    (GameAfkingModule.sol:464/:662/:709, the claimablePool -= :710). Preserve the LANDMINE-A exemption-spoof corners +
    the v50-green `testFundingSourceVaultDoesNotInheritExemption` assertion. RE-DERIVE every pinned slot via
    `forge inspect storage DegenerusGame`. Keep this file's funded-slice corners intact — 351-05's TST-02 reuses them.
    Do NOT edit any contracts/ file.
  </action>
  <verify>
    <automated>forge test --match-contract "AfKingFundingWaterfall" 2>&1 | tail -6 (compiles + runs; testFundingSourceVaultDoesNotInheritExemption present + green)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' test/fuzz/AfKingFundingWaterfall.t.sol | grep -c "afKing\."` == 0.
    - The file compiles under `forge test --match-contract AfKingFundingWaterfall`.
    - `testFundingSourceVaultDoesNotInheritExemption` exists and passes (the LANDMINE-A spoof rejected).
    - At least one assertion reads `afkingFunding`/`afkingFundingOf` (the in-context SLOAD replacing the staticcall).
    - `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>AfKingFundingWaterfall compiles + runs on the game-resident afkingFunding SLOAD path, LANDMINE-A preserved green, funded-slice corners intact for TST-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author V55SetMutationOpenE.t.sol — TST-04 set-mutation + OPEN-E 4-protection regression</name>
  <read_first>
    - test/fuzz/AfKingConcurrency.t.sol (Task 1 output — the swap-pop/tombstone scaffolding to extend, not duplicate)
    - contracts/modules/GameAfkingModule.sol :234 (subscribe + the subscribe-time OPEN-E isOperatorApproved gate), :395-401 (swap-pop), :539 (processSubscriberStage), :557 (NO-ORPHAN guard comment), :582-587 (reclaim)
    - contracts/DegenerusVault.sol :481 (SUB-09 self-subscribe, src=address(0) default-self) + StakedDegenerusStonk.sol:382
    - .planning/phases/351-PATTERNS.md §3 (the set-mutation primary analog) + the OPEN-E note in 351-CONTEXT.md
    - [[open-e-operator-approval-trust-boundary]] memory disposition (consent-gate / default-self / no-escalation / trust-the-sub)
  </read_first>
  <behavior>
    - Two-path coexistence: an afking-stamp open and a human lootboxEth/boxPlayers open in the same fixture state do not corrupt each other's state (no shared mutable-state hazard).
    - NO-ORPHAN: a sub removed (evict / cancel-reclaim / re-stamp / funding-kill) between stamp and open gets NO free box (the :557 guard dominates all four orphan paths).
    - Streak preserved: across a tombstone-then-reclaim cycle, the surviving sub's scorePlus1 (mint-streak) is not reset by the swap-pop.
    - OPEN-E: subscribe with an unapproved operator REVERTS (consent-gate); src=address(0) yields funder==self byte-identically (default-self); an operator cannot escalate an existing grant (no-escalation); the grant is temporally bounded (trust-the-sub).
  </behavior>
  <action>
    Create `test/fuzz/V55SetMutationOpenE.t.sol` extending DeployProtocol. Author the TST-04 proofs:
    (a) two-path coexistence — stamp an afking box for one player + buy a human box for another, open both, assert
    neither path mutated the other's ledger/stamp (the two routes share no mutable-state hazard, BOX-05);
    (b) NO-ORPHAN — fuzz the four orphan paths (re-stamp / cancel-reclaim via re-subscribe(dailyQuantity=0) / evict /
    funding-kill) between stamp and open, assert `_afkingBoxReady` is false / no box materializes (the :557 guard);
    (c) streak-preserved swap-pop — subscribe ≥3 players, tombstone a middle one, run a STAGE cycle, assert the
    swap-pop occupant keeps its scorePlus1 and is processed without cursor-skip;
    (d) OPEN-E 4-protection — assert subscribe-with-unapproved-operator reverts (consent-gate), src=address(0)
    funder==self (default-self, byte-identical default path), no-escalation, trust-the-sub temporal bound.
    Use fuzz for the subscribe/evict/swap-pop ORDERINGS (D-351-04). Gate each assertion non-vacuously (the sub actually
    existed / the box actually would-have-materialized absent the guard). RE-DERIVE any pinned slot via
    `forge inspect storage DegenerusGame`. Do NOT edit any contracts/ file.
  </action>
  <verify>
    <automated>forge test --match-contract "V55SetMutationOpenE" 2>&1 | tail -8 (all functions present + pass; non-vacuous)</automated>
  </verify>
  <acceptance_criteria>
    - `test/fuzz/V55SetMutationOpenE.t.sol` exists with named tests covering: two-path coexistence, NO-ORPHAN (≥4 orphan paths), streak-preserved swap-pop, and the OPEN-E 4-protection (4 named sub-assertions).
    - `forge test --match-contract V55SetMutationOpenE` exits 0 (or any failure is a real assertion, not a compile error / vacuous pass).
    - The NO-ORPHAN test asserts `_afkingBoxReady`==false (no free box) for a sub removed between stamp and open — non-vacuous (a control case where the box WOULD materialize is also asserted).
    - The OPEN-E consent-gate test asserts subscribe REVERTS for an unapproved operator.
    - `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>V55SetMutationOpenE.t.sol proves TST-04: two-path coexistence + NO-ORPHAN + streak-preserved swap-pop + the OPEN-E 4-protection, all non-vacuous, fuzzed on orderings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only. Adapts 3 fuzz files + authors 1 proof file. ZERO contract mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-351-02-FG | Tampering | the adapted/new TST-04 fuzz tests | mitigate | The only threat is a false-GREEN: a test that passes vacuously, asserts nothing, or pokes a mis-derived storage slot (the AfKing-layout constants are WRONG for the game-resident set). Mitigation: re-derive every slot via `forge inspect storage DegenerusGame`; non-vacuity gates on every set-mutation/NO-ORPHAN assertion (the sub actually existed / the box would have materialized absent the guard); fuzz the orderings. ZERO `contracts/` mutation (`git diff 453f8073 HEAD -- contracts/` empty). |
</threat_model>

<verification>
- The 3 adapted files + the new proof compile and run under `forge test --match-contract`.
- No surviving `afKing.` call or `AfKing.sol` import; no AfKing-layout slot literal.
- The swap-pop no-skip + NO-ORPHAN + OPEN-E assertions are non-vacuous.
- `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
</verification>

<success_criteria>
TST-04 is empirically proven: the two open routes coexist with no shared mutable-state hazard, set-mutation
(eviction/tombstone/swap-pop) preserves the mint-streak with no cursor advance after swap-pop, the NO-ORPHAN
guard holds across all four orphan paths, and the OPEN-E 4-protection regresses. The `AfKing*` corpus is
adapted to the game-resident path with maximum coverage preserved. ZERO `contracts/*.sol` mutation.
</success_criteria>

<output>
Create `.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-02-SUMMARY.md` when done.
</output>
