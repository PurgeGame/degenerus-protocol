---
phase: 351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat
plan: 07
type: execute
wave: 2
depends_on: ["351-01"]
files_modified:
  - test/gas/RouterWorstCaseGas.t.sol
  - test/gas/SweepPerPlayerWorstCaseGas.t.sol
  - test/gas/KeeperLeversAndPacking.t.sol
  - test/gas/KeeperResolveBetWorstCaseGas.t.sol
autonomous: true
requirements: [TST-06, TST-05]
must_haves:
  truths:
    - "The 4 gas-corpus files compile + run against the game-resident STAGE / mintBurnie / Sub-layout"
    - "RouterWorstCaseGas asserts a 50-chunk processSubscriberStage(50) AND the open leg each stay under 16.7M"
    - "The vm.readFile AFKING_SRC in KeeperLeversAndPacking repoints to GameAfkingModule.sol; the packed-Sub layout asserts re-derive against the game-resident Sub"
    - "git diff 453f8073 HEAD -- contracts/ stays EMPTY"
  artifacts:
    - path: "test/gas/RouterWorstCaseGas.t.sol"
      provides: "the 16.7M-per-tx ceiling worst-case on the STAGE + open leg"
      contains: "16_700_000"
    - path: "test/gas/KeeperLeversAndPacking.t.sol"
      provides: "the packed-Sub layout asserts (re-derived) + AFKING_SRC repointed"
  key_links:
    - from: "test/gas/RouterWorstCaseGas.t.sol"
      to: "DegenerusGameAdvanceModule processSubscriberStage(50)"
      via: "advanceGame() STAGE 50-chunk under 16.7M"
      pattern: "16_700_000"
---

<objective>
Adapt the four `*Gas` worst-case files (`RouterWorstCaseGas`, `SweepPerPlayerWorstCaseGas`,
`KeeperLeversAndPacking`, `KeeperResolveBetWorstCaseGas`) to the game-resident STAGE / `mintBurnie` /
game-resident Sub layout (D-351-01). `RouterWorstCaseGas` is the PRIMARY 16.7M-per-tx-ceiling template (reframe
the `doWork()` router worst case onto the `processSubscriberStage(50)` STAGE + the open leg).
`KeeperOpenBoxWorstCaseGas` (the per-open marginal donor) is owned by the TST-06 harness plan 351-08, NOT here.

Purpose: TST-06 (the 16.7M ceiling worst-case confirmation) + TST-05 (these files must compile + run for the
non-widening reconciliation). The marginal idiom + the worst-case preconditions are preserved for 351-08 to consume.
Output: 4 adapted gas files. ZERO `contracts/*.sol` mutation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-CONTEXT.md
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-PATTERNS.md
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-01-SUMMARY.md
@.planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-TST06-MEASUREMENT-SPEC.md

<interfaces>
<!-- The gas-corpus anchors (READ-ONLY at 453f8073; never edit sites). -->

The 16.7M ceiling (350-SPEC section 5): SUB_STAGE_BATCH = 50 (DegenerusGameAdvanceModule.sol:149); a landed buy ~262k → 50 ~13.1M.
  processSubscriberStage(50) (via advanceGame() STAGE :305-312 → _runSubscriberStage :754-761) + the open leg each must stay under 16.7M.
  Partial-drain: break + return mult while !subsFullyProcessed (:313-317).

Δ3 doWork → mintBurnie; Δ4 autoBuy → STAGE-drive via new-day advanceGame(); Δ5 subscriberCount → _subscribers.length via vm.load.

RouterWorstCaseGas.t.sol (PATTERNS section 12): 21 afKing. + 9 doWork + 3 srcref. Reframe the AfKing.doWork() router worst case onto
  the STAGE + open leg under 16.7M. Repoint the AfKing.sol:868/850 mirrors → GameAfkingModule.sol.

SweepPerPlayerWorstCaseGas.t.sol (PATTERNS section 13): 15 afKing. + 5 srcref. Reframe the per-player sweep worst case → the per-sub STAGE
  marginal; subscriberCount()/autoBuy(total) → slot-read / STAGE-drive; BOUNTY_ETH_TARGET + SUB-04 reinvest comments (:50/:150/:172/:196) → GameAfkingModule.sol.

KeeperLeversAndPacking.t.sol (PATTERNS section 13): doWork=1 + srcref=5 (vm.readFile :88/:127/:222/:268). Repoint AFKING_SRC → GameAfkingModule.sol;
  RE-DERIVE the packed-Sub layout asserts against the game-resident Sub (DegenerusGameStorage.sol:1867 — scorePlus1(16)+amount(96)+lastAutoBoughtDay(32)+lastOpenedDay(32)).
  The :299 comment "Plan 335-04 deleted the keeper-burn function" anchors a removed surface → update to the v55 fold.

KeeperResolveBetWorstCaseGas.t.sol (PATTERNS section 13): the marginal-idiom DONOR :197-242 (loop-N-divide). Light touch — degenerette resolve-bet
  per-spin marginal; keep the idiom, confirm no afKing coupling (drop/repoint any stray ref).

The MARGINAL rule (350-SPEC section 0 / CR-01): (gas for N − gas for N−1) / 1, never a single-item total. Preserve wherever these files measure.
foundry.toml: ffi OFF; vm.readFile allowed under fs_permissions read ./contracts; forge inspect / forge build --sizes do NOT need ffi.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt RouterWorstCaseGas.t.sol — the 16.7M ceiling on processSubscriberStage(50) + the open leg</name>
  <read_first>
    - test/gas/RouterWorstCaseGas.t.sol (FULL — 694 lines; the 21 afKing. + 9 doWork + 3 srcref sites, the 16.7M-ceiling worst-case structure, the AfKing.sol:868/850 mirrors)
    - contracts/modules/DegenerusGameAdvanceModule.sol :149 (SUB_STAGE_BATCH=50), :305-312 (STAGE), :313-317 (partial-drain), :754-761 (_runSubscriberStage)
    - contracts/modules/GameAfkingModule.sol :985 (mintBurnie), :539 (processSubscriberStage), :1023 (autoOpen)
    - .planning/phases/350-TST06-MEASUREMENT-SPEC.md section 5 (the 16.7M ceiling) + .planning/phases/351-PATTERNS.md section 12
  </read_first>
  <action>
    Apply Δ3 (`afKing.doWork()`→`game.mintBurnie()`, 9 sites) + Δ4 (`afKing.autoBuy(total)`→STAGE-drive via a new-day
    `advanceGame()`; port `_settleGame`) + Δ5 (`subscriberCount()`→`_subscribers.length` via vm.load, re-derived
    slot) to the 21 `afKing.` sites. Reframe the `doWork()` router worst case onto: assert a 50-chunk
    `processSubscriberStage(50)` (via `advanceGame()`) AND the open leg (`autoOpen`/`mintBurnie` open leg) each stay
    UNDER 16.7M on the worst-case funded-lootbox-sub mix (post-349.2 — including the restored BURNIE quest/affiliate
    per-sub calls). Repoint the `AfKing.sol:868/850` source-grep/comment mirrors → `GameAfkingModule.sol`. RE-DERIVE
    any pinned slot via `forge inspect storage DegenerusGame`. Do NOT edit any contracts/ file.
  </action>
  <verify>
    <automated>forge test --match-contract RouterWorstCaseGas 2>&1 | tail -8 (the STAGE-50 + open-leg under-16.7M asserts run + pass)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' test/gas/RouterWorstCaseGas.t.sol | grep -c "afKing\.\|doWork()"` == 0.
    - `grep -c "16_700_000\|16700000" test/gas/RouterWorstCaseGas.t.sol` >= 1 (the ceiling assert).
    - The file runs `processSubscriberStage(50)` via `advanceGame()` and asserts < 16.7M; the open leg likewise.
    - `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>RouterWorstCaseGas asserts the 50-chunk STAGE + the open leg each stay under 16.7M on the worst-case funded-lootbox mix; all call-sites adapted.</done>
</task>

<task type="auto">
  <name>Task 2: Adapt KeeperLeversAndPacking.t.sol + KeeperResolveBetWorstCaseGas.t.sol — repoint AFKING_SRC, re-derive Sub layout, confirm donor idiom</name>
  <read_first>
    - test/gas/KeeperLeversAndPacking.t.sol (FULL — 545 lines; AFKING_SRC :88/:127/:222/:268, the packed-Sub layout asserts, the :299 removed-keeper-burn comment, doWork=1)
    - test/gas/KeeperResolveBetWorstCaseGas.t.sol (the marginal-idiom donor :197-242; confirm no afKing coupling)
    - contracts/storage/DegenerusGameStorage.sol :1867 (the game-resident Sub layout — scorePlus1(16)+amount(96)+lastAutoBoughtDay(32)+lastOpenedDay(32))
    - contracts/modules/GameAfkingModule.sol :985 (mintBurnie)
    - .planning/phases/351-PATTERNS.md section 13 (both files)
  </read_first>
  <action>
    In `KeeperLeversAndPacking.t.sol`: repoint `AFKING_SRC = "contracts/AfKing.sol"` → `"contracts/modules/GameAfkingModule.sol"`
    at :88/:127/:222/:268 (else `vm.readFile` THROWS). RE-DERIVE the packed-Sub layout asserts against the
    game-resident `Sub` (DegenerusGameStorage.sol:1867 — the 4-field layout: scorePlus1(16)+amount(96)+
    lastAutoBoughtDay(32)+lastOpenedDay(32); the old AfKing-standalone byte offsets are WRONG). Update the :299
    "Plan 335-04 deleted the keeper-burn function" comment to reference the v55 fold. Apply Δ3 to the 1 `doWork` site.
    In `KeeperResolveBetWorstCaseGas.t.sol`: confirm the marginal-idiom donor (:197-242, loop-N-divide) is intact and
    has NO `afKing` coupling; drop/repoint any stray ref. Light touch — it is the degenerette resolve-bet per-spin
    marginal, not afking. RE-DERIVE any pinned slot via `forge inspect storage DegenerusGame`. Do NOT edit any contracts/ file.
  </action>
  <verify>
    <automated>forge test --match-contract "KeeperLeversAndPacking|KeeperResolveBetWorstCaseGas" 2>&1 | tail -8 (no vm.readFile throw; the packed-Sub layout asserts pass against the re-derived offsets)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "contracts/AfKing.sol" test/gas/KeeperLeversAndPacking.t.sol` == 0 (AFKING_SRC repointed at all 4 sites).
    - The packed-Sub layout asserts use the re-derived game-resident offsets (no AfKing-standalone byte offset literal survives).
    - Both files compile + run under `forge test --match-contract` (no `vm.readFile` "file not found").
    - `KeeperResolveBetWorstCaseGas` has no `afKing.`/`doWork()` reference (the donor idiom is afking-decoupled).
    - `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>KeeperLeversAndPacking runs with AFKING_SRC repointed + the Sub layout re-derived; KeeperResolveBetWorstCaseGas's marginal donor idiom is confirmed afking-decoupled.</done>
</task>

<task type="auto">
  <name>Task 3: Adapt SweepPerPlayerWorstCaseGas.t.sol — per-player sweep worst case → per-sub STAGE marginal</name>
  <read_first>
    - test/gas/SweepPerPlayerWorstCaseGas.t.sol (FULL — 413 lines; the 15 afKing. + 5 srcref sites, the BOUNTY_ETH_TARGET + SUB-04 reinvest comments :50/:150/:172/:196)
    - contracts/modules/DegenerusGameAdvanceModule.sol :305-312 (STAGE), :149 (SUB_STAGE_BATCH=50)
    - contracts/modules/GameAfkingModule.sol :539 (processSubscriberStage), the bounty constants (BOUNTY_ETH_TARGET ~:987)
    - .planning/phases/351-PATTERNS.md section 13 (the sweep reframe)
  </read_first>
  <action>
    Apply Δ4 (`afKing.autoBuy(total)`→STAGE-drive via a new-day `advanceGame()`) + Δ5 (`subscriberCount()`→
    `_subscribers.length` via vm.load, re-derived slot) to the 15 `afKing.` sites. Reframe the per-player sweep
    worst case onto the per-sub STAGE marginal (the per-sub `processSubscriberStage` cost). Repoint the
    `BOUNTY_ETH_TARGET` immutable + SUB-04 reinvest comments (:50/:150/:172/:196) → `GameAfkingModule.sol`. Preserve
    the marginal idiom (loop-N-divide) wherever the file measures. RE-DERIVE any pinned slot via
    `forge inspect storage DegenerusGame`. Do NOT edit any contracts/ file.
  </action>
  <verify>
    <automated>forge test --match-contract SweepPerPlayerWorstCaseGas 2>&1 | tail -6 (compiles + runs; the per-sub STAGE marginal measured)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' test/gas/SweepPerPlayerWorstCaseGas.t.sol | grep -c "afKing\."` == 0.
    - The file compiles + runs under `forge test --match-contract SweepPerPlayerWorstCaseGas`.
    - The per-player sweep is reframed onto the per-sub STAGE marginal (the measurement drives `advanceGame()`'s STAGE).
    - `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>SweepPerPlayerWorstCaseGas runs on the per-sub STAGE marginal with all call-sites adapted and the bounty comments repointed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only. Adapts 4 gas-corpus files. ZERO contract mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-351-07-FG | Tampering | the adapted gas-corpus files | mitigate | False-GREEN risks: (1) a `vm.readFile` AFKING_SRC still pointing at the deleted AfKing.sol throws → mitigated by repointing all 4 sites to GameAfkingModule.sol; (2) a packed-Sub layout assert using the WRONG AfKing-standalone byte offsets → vacuous/wrong → mitigated by re-deriving against DegenerusGameStorage.sol:1867; (3) a single-item-total gas measurement reintroducing the CR-01 faucet framing → mitigated by preserving the loop-N-divide marginal idiom. ZERO `contracts/` mutation (`git diff 453f8073 HEAD -- contracts/` empty). |
</threat_model>

<verification>
- The 4 gas files compile + run under `forge test --match-contract`.
- RouterWorstCaseGas asserts < 16.7M on the STAGE-50 + open leg.
- KeeperLeversAndPacking's AFKING_SRC is repointed + the Sub layout re-derived.
- `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
</verification>

<success_criteria>
The `*Gas` worst-case corpus is adapted to the game-resident STAGE / `mintBurnie` / Sub layout: the 16.7M-per-tx
ceiling is asserted on the 50-chunk STAGE + open leg, the `vm.readFile` AFKING_SRC is repointed, the packed-Sub
layout asserts are re-derived, and the marginal idiom is preserved. ZERO `contracts/*.sol` mutation.
</success_criteria>

<output>
Create `.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-07-SUMMARY.md` when done.
</output>
