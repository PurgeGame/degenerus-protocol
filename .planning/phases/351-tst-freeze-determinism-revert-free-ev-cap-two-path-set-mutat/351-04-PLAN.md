---
phase: 351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat
plan: 04
type: execute
wave: 2
depends_on: ["351-01"]
files_modified:
  - test/fuzz/RngLockDeterminism.t.sol
  - test/fuzz/V55FreezeDeterminism.t.sol
autonomous: true
requirements: [TST-01]
must_haves:
  truths:
    - "Two opens of the same stamp at different blocks/days yield a byte-identical materialized box (seed uses the STAMPED day)"
    - "The box seed carries NO block.timestamp/number/prevrandao/coinbase/blockhash"
    - "Index-binding holds across a mid-day requestLootboxRng index advance (the box binds to the stamped day's word, no interleave)"
    - "DIFFERENTIAL: the afking stamp→open and a manual openLootBox yield byte-identical traits for the same (amount, level, rngWord, score)"
    - "RngLockDeterminism compiles + runs against the game-resident stamped-day freeze"
    - "git diff 453f8073 HEAD -- contracts/ stays EMPTY"
  artifacts:
    - path: "test/fuzz/V55FreezeDeterminism.t.sol"
      provides: "TST-01 freeze/determinism + the D-351-05 differential box oracle"
      contains: "resolveAfkingBox"
    - path: "test/fuzz/RngLockDeterminism.t.sol"
      provides: "adapted freeze/determinism corpus (epoch refs → stamped-day)"
  key_links:
    - from: "test/fuzz/V55FreezeDeterminism.t.sol"
      to: "DegenerusGameLootboxModule.resolveAfkingBox vs openLootBox"
      via: "byte-identical materialized traits for the same (amount, level, rngWord, score)"
      pattern: "resolveAfkingBox"
---

<objective>
Adapt the freeze/determinism corpus (`RngLockDeterminism.t.sol`, the largest fuzz file at 2,272 lines) to the
game-resident stamped-day freeze, and author the dedicated TST-01 proof with the D-351-05 DIFFERENTIAL box
oracle. This is the HEADLINE property and where fuzz is most valuable (D-351-04). The corrected freeze target
(PATTERNS §7 + CONTEXT ⚠): the committed Sub stamp is **4-field** (`scorePlus1`, `amount`, `lastAutoBoughtDay`,
`lastOpenedDay`) and the LEVEL resolves LIVE at open by design — so the freeze test proves the **seed** is
frozen (stamped `amount`/`scorePlus1`/`day`, day's committed word, NO `block.*`), NOT that level/baseLevel is
frozen.

Purpose: TST-01 — the stamp+open produces an identical box outcome independent of open timing/block; the seed
uses the STAMPED day; index-binding holds across a mid-day index advance; and the differential oracle proves
the afking-open ≡ human-open for the same tuple.
Output: 1 adapted fuzz file + 1 new TST-01 proof file. ZERO `contracts/*.sol` mutation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-CONTEXT.md
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-PATTERNS.md
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-01-SUMMARY.md

<interfaces>
<!-- The differential preimage — VERIFIED against the live tree at 453f8073 (READ-ONLY anchors; never edit sites). -->

resolveAfkingBox (the afking arm) — DegenerusGameLootboxModule.sol:877:
  seed = uint256(keccak256(abi.encode(rngWord, player, day, amount)))   // :889 — FROZEN stamped day + day's word
  uint24 currentLevel = level + 1;  uint24 targetLevel = _rollTargetLevel(currentLevel, seed);  // LIVE level by design
  evMultiplierBps = _lootboxEvMultiplierFromScore(activityScore); scaledAmount = _applyEvMultiplierWithCap(player, currentLevel, amount, evMultiplierBps);
  → _resolveLootboxCommon(player, day, 0, scaledAmount, targetLevel, currentLevel, …)
  Contract comment :887 "Byte-identical to openLootBox (:534) / resolveLootboxDirect (:768)".

openLootBox (the human arm) — DegenerusGameLootboxModule.sol:503: reads lootboxEth[index][player] :505, SAME abi.encode preimage.

The 4-field stamp (CORRECTED freeze target) — DegenerusGameStorage.sol:1850-1882:
  per-sub stamp (112b): scorePlus1(16) + amount(96); markers (64b): lastAutoBoughtDay(32) + lastOpenedDay(32).
  NO index, NO baseLevelPlus1. The box sources rngWordByDay[lastAutoBoughtDay]; level is LIVE (no roll floor).
  → DO NOT write a freeze test trying to prove level/baseLevel unmanipulable — score is stamped-frozen; baseLevel is intentionally live.

The seed-day freeze:
  _openAfkingBox GameAfkingModule.sol:888 → seed (sub.amount, day, rngWordByDay[day], sub.scorePlus1-1) :901-907; marker sub.lastOpenedDay=day :892.
  _afkingBoxReady :918 gates lastOpenedDay < lastAutoBoughtDay && rngWordByDay[lastAutoBoughtDay] != 0.

Index-binding (FREEZE-02 reconciled to DAY-keyed reality — CONTEXT §116):
  The box word is DAY-keyed (rngWordByDay[lastAutoBoughtDay]), NOT lootbox-index-keyed. The freeze is the PRE-RNG
  stamp (STAGE runs before rngGate commits rngWordByDay[day], DegenerusGameAdvanceModule.sol:305-312) + the open
  reads it AFTER it lands. The mid-day requestLootboxRng index advance (AdvanceModule.sol:1096 + index advance ~:1089/:1629)
  is fuzzed BETWEEN stamp and open; assert the box still binds to the stamped day's word (no interleave / no stale-index attach).

Δ3 doWork→mintBurnie (4 sites); preserve the existing vm.skip blocks (17 skips — orthogonal to the gate).
_settleGame VRF-drain helper: port from KeeperRewardRoutingSameResults:111-127 (351-03 preserves it).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author V55FreezeDeterminism.t.sol — TST-01 stamped-day freeze + the differential box oracle</name>
  <read_first>
    - contracts/modules/DegenerusGameLootboxModule.sol :877-915 (resolveAfkingBox — the seed preimage, LIVE level, single EV-cap RMW) + :503-560 (openLootBox — the human arm + its ledger zeroing)
    - contracts/modules/GameAfkingModule.sol :888-907 (_openAfkingBox seed), :918 (_afkingBoxReady), :938 (_autoOpen), :1023 (autoOpen)
    - contracts/storage/DegenerusGameStorage.sol :1850-1882 (the 4-field stamp — the corrected freeze target)
    - test/fuzz/KeeperRewardRoutingSameResults.t.sol (the _settleGame :111-127 + the topic-decode :62-70 to PORT)
    - .planning/phases/351-PATTERNS.md §5 (the differential oracle assertion shape) + §7 (the freeze target reconciliation)
  </read_first>
  <behavior>
    - Determinism: stamp a box, then open it at two different blocks/timestamps (vm.roll/vm.warp between opens of the SAME stamp), assert byte-identical materialized traits — the seed used the STAMPED day, not open-time.
    - No-entropy: the box outcome is invariant under vm.prevrandao/coinbase/blockhash changes between stamp and open (the draw carries no block.* entropy).
    - DIFFERENTIAL (D-351-05): for the same (amount, level, rngWord, score), `resolveAfkingBox` and `openLootBox` produce the same targetLevel + the same materialized traits (the two arms share one abi.encode preimage).
  </behavior>
  <action>
    Create `test/fuzz/V55FreezeDeterminism.t.sol` extending DeployProtocol. Port the `_settleGame` VRF-drain
    helper + the CoinflipStakeUpdated/log-decode idiom from KeeperRewardRoutingSameResults (351-03). Author:
    (a) the determinism fuzz — subscribe + fund a LOOTBOX-mode sub, run a new-day `advanceGame()` STAGE to stamp,
    settle the day's word, then `autoOpen` the box at block B1; in a parallel fixture state (or via snapshot/revert)
    open the same stamp at block B2 with vm.roll/vm.warp/vm.prevrandao perturbed — assert the materialized box is
    byte-identical (FREEZE-03);
    (b) the differential oracle (D-351-05) — capture the afking arm's (amount, level, rngWord, score) tuple, call
    `openLootBox`/`resolveAfkingBox` for the SAME tuple, assert byte-identical targetLevel + traits (robust to any
    future resolution refactor — NOT golden snapshots). Do NOT assert level/baseLevel is FROZEN (it is LIVE by
    design — the differential at the SAME live level is what proves equivalence).
    Fuzz open-timing/block (D-351-04). Gate non-vacuously: assert each box actually materialized (signal zeroed /
    `_afkingBoxReady` true→false). RE-DERIVE any pinned slot via `forge inspect storage DegenerusGame`. Do NOT edit any contracts/ file.
  </action>
  <verify>
    <automated>forge test --match-contract "V55FreezeDeterminism" 2>&1 | tail -8 (determinism + differential present + pass; non-vacuous)</automated>
  </verify>
  <acceptance_criteria>
    - `test/fuzz/V55FreezeDeterminism.t.sol` exists with named tests for: stamped-day determinism (open at two blocks → identical box), no-block-entropy, and the differential afking-vs-human oracle.
    - The differential test asserts byte-identical materialized traits for the same `(amount, level, rngWord, score)` across `resolveAfkingBox` and `openLootBox`.
    - The determinism test perturbs `vm.roll`/`vm.warp`/`vm.prevrandao` between the two opens and asserts equality (non-vacuous: the box materialized).
    - NO test asserts `targetLevel`/`baseLevel` is frozen across a LIVE level change (the differential is at a fixed live level).
    - `forge test --match-contract V55FreezeDeterminism` exits 0 (or any failure is a real assertion, not compile/vacuous).
    - `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>V55FreezeDeterminism.t.sol proves TST-01: stamped-day determinism + no-block-entropy + the differential afking-vs-human box oracle, fuzzed on open-timing, non-vacuous.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TST-01 index-binding — the pre-RNG stamp / mid-day requestLootboxRng advance fuzz (in V55FreezeDeterminism.t.sol)</name>
  <read_first>
    - contracts/modules/DegenerusGameAdvanceModule.sol :305-312 (the STAGE block, PRE-RNG before rngGate), :754-761 (_runSubscriberStage → processSubscriberStage), :1096 (requestLootboxRng) + the index advance ~:1089/:1629
    - contracts/modules/GameAfkingModule.sol :888-907 (_openAfkingBox reads rngWordByDay[day]), :918 (_afkingBoxReady gate)
    - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-FREEZE-PROOF.md (FREEZE-02 — read for the index-binding property the literal ROADMAP wording maps onto)
    - .planning/phases/351-PATTERNS.md §7 (the FREEZE-02 reconciliation to the DAY-keyed reality)
  </read_first>
  <behavior>
    - The process STAGE writes the stamp BEFORE the day's rngWordByDay[day] is committed (pre-RNG ordering); the open reads it AFTER it lands.
    - A mid-day `requestLootboxRng` index advance fired BETWEEN stamp and open does NOT change which word the box binds to — the box always uses rngWordByDay[lastAutoBoughtDay] (the stamped day), never a later/stale lootbox-index word (no interleave).
  </behavior>
  <action>
    Add to `test/fuzz/V55FreezeDeterminism.t.sol` the index-binding proof reconciled to the DAY-keyed reality
    (PATTERNS §7 — the box word is DAY-keyed, not lootbox-index-keyed; do NOT assert attachment to a stale lootbox
    index). Stamp a sub via the new-day STAGE (pre-RNG), then fuzz a mid-day `requestLootboxRng` index advance
    between stamp and open, settle, `autoOpen`, and assert the materialized box matches the box that binds to the
    STAMPED day's `rngWordByDay[lastAutoBoughtDay]` (the differential arm at the stamped tuple) — proving no
    interleave / no stale-index attach. Also assert the ordering invariant: `_afkingBoxReady` is FALSE before the
    stamped day's word lands and TRUE after (the pre-RNG/post-RNG boundary). Fuzz the advance timing (D-351-04). RE-DERIVE
    any pinned slot. Do NOT edit any contracts/ file.
  </action>
  <verify>
    <automated>forge test --match-test "IndexBinding\|MidDay\|PreRng" --match-contract V55FreezeDeterminism 2>&1 | tail -6 (pass; non-vacuous)</automated>
  </verify>
  <acceptance_criteria>
    - A named index-binding test exists asserting the box binds to `rngWordByDay[lastAutoBoughtDay]` even with a mid-day `requestLootboxRng` advance between stamp and open.
    - The pre-RNG/post-RNG ordering is asserted (`_afkingBoxReady` false → true across the word commit).
    - The test is non-vacuous (a control where the box DOES materialize at the stamped day is asserted).
    - `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>The index-binding / pre-RNG-stamp property is proven: a mid-day index advance does not re-bind the box; the stamped day's word is always used (no interleave).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Adapt RngLockDeterminism.t.sol — epoch refs → stamped-day, doWork→mintBurnie, preserve vm.skip + the v50-green</name>
  <read_first>
    - test/fuzz/RngLockDeterminism.t.sol (FULL — 2,272 lines; the 4 afKing. + 4 doWork sites, the 17 vm.skip blocks, the v50-green testFuzz_RngLockDeterminism_ClaimWhalePassDuringLockSafe, the A7 vm.assume-exhaustion baseline red)
    - contracts/modules/GameAfkingModule.sol :985 (mintBurnie), :888-907 (the stamped-day seed)
    - contracts/storage/DegenerusGameStorage.sol :1850-1882 (the 4-field stamp)
    - .planning/phases/351-PATTERNS.md §7 (the freeze-target reconciliation — do NOT prove score/baseLevel unmanipulable)
  </read_first>
  <behavior>
    - The existing RngLock determinism properties (the whale-pass-during-lock-safe green, the rngLocked-window invariants) hold on the game-resident path.
    - Any `_afkingEpoch`-style determinism reference reframes to stamped-day determinism (the box seeds from sub.lastAutoBoughtDay's rngWordByDay[day]).
  </behavior>
  <action>
    Apply Δ3 (`afKing.doWork()`→`game.mintBurnie()`, 4 sites). Reframe any per-day epoch-determinism reference →
    stamped-day determinism (FREEZE-03). PRESERVE the 17 `vm.skip` blocks verbatim (orthogonal to the gate — do NOT
    flip them) and the v50-green `testFuzz_RngLockDeterminism_ClaimWhalePassDuringLockSafe`. The A7
    `vm.assume`-exhaustion baseline red is carried forward (it is a pre-existing red — record its NAME for 351-09's
    ledger, do NOT try to fix it here). Do NOT write a freeze test trying to prove score/baseLevel unmanipulable
    (PATTERNS §7 — score is stamped-frozen, baseLevel is intentionally live). RE-DERIVE any pinned slot via
    `forge inspect storage DegenerusGame`. Do NOT edit any contracts/ file.
  </action>
  <verify>
    <automated>forge test --match-contract "RngLockDeterminism" 2>&1 | tail -8 (compiles + runs; the whale-pass-during-lock green passes; the A7 red is the only carried-forward failure, by name)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' test/fuzz/RngLockDeterminism.t.sol | grep -c "afKing\.\|doWork()"` == 0.
    - The file compiles + runs under `forge test --match-contract RngLockDeterminism`.
    - The 17 `vm.skip` blocks are preserved (`grep -c "vm.skip" == 17`, unchanged from baseline).
    - `testFuzz_RngLockDeterminism_ClaimWhalePassDuringLockSafe` is present and green.
    - No freeze test asserts score/baseLevel is frozen across a live level change.
    - `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>RngLockDeterminism compiles + runs on the game-resident stamped-day freeze with vm.skip + the v50-green preserved; epoch refs reframed; the A7 red carried by name.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only. Adapts the freeze corpus + authors the TST-01 proof. ZERO contract mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-351-04-FG | Tampering | the TST-01 freeze tests | mitigate | False-GREEN risks: (1) a determinism test that opens the SAME state twice (vacuously equal) instead of re-opening the same STAMP at different blocks — mitigated by perturbing vm.roll/warp/prevrandao + asserting non-vacuous materialization; (2) mis-targeting the freeze at the LIVE level (CONTEXT ⚠) — mitigated by proving the SEED frozen + the differential at a fixed live level, never asserting level frozen; (3) mis-derived storage slot — mitigated by `forge inspect storage DegenerusGame`. ZERO `contracts/` mutation (`git diff 453f8073 HEAD -- contracts/` empty). |
</threat_model>

<verification>
- V55FreezeDeterminism + RngLockDeterminism compile + run under `forge test --match-contract`.
- The differential afking-vs-human oracle + the determinism + index-binding proofs are non-vacuous.
- No freeze test asserts level/baseLevel frozen; the SEED is the freeze target.
- `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
</verification>

<success_criteria>
TST-01 is empirically proven: the stamp+open yields a byte-identical box independent of open timing/block (the
seed uses the STAMPED day, no block.* entropy), index-binding holds across a mid-day index advance, and the
differential oracle proves the afking-open ≡ human-open for the same tuple. The freeze corpus is adapted to the
game-resident stamped-day model. ZERO `contracts/*.sol` mutation.
</success_criteria>

<output>
Create `.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-04-SUMMARY.md` when done.
</output>
