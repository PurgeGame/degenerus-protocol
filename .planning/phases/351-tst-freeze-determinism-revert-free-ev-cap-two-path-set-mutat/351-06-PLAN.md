---
phase: 351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat
plan: 06
type: execute
wave: 2
depends_on: ["351-01"]
files_modified:
  - test/fuzz/KeeperBatchAffiliateDeltaAudit.t.sol
  - test/fuzz/RedemptionStethFallback.t.sol
autonomous: true
requirements: [TST-04, TST-05]
must_haves:
  truths:
    - "The D-351-02 removed-surface legs are dropped BY NAME with a recorded reason (batchPurchase/batchPurchaseForKeeper; AfKing.depositFor/poolOf/withdraw custody-recovery)"
    - "The affiliate-conservation delta-audit reframes onto the 349.2 per-buy affiliate path (or is dropped if batchPurchase is its sole subject — adjudicated)"
    - "The pure ETH-vs-stETH redemption-fallback core (POOL-04a/b ETH-segregation) STAYS and compiles"
    - "Both files compile (the AfKing.sol import is repointed/dropped)"
    - "git diff 453f8073 HEAD -- contracts/ stays EMPTY"
  artifacts:
    - path: "test/fuzz/RedemptionStethFallback.t.sol"
      provides: "the ETH-vs-stETH redemption core (custody-recovery leg dropped)"
      contains: "stETH"
  key_links:
    - from: "test/fuzz/KeeperBatchAffiliateDeltaAudit.t.sol"
      to: "GameAfkingModule affiliate.payAffiliate (349.2 per-buy) OR dropped"
      via: "affiliate-conservation reframe or D-351-02 removal"
      pattern: "affiliate"
---

<objective>
Handle the two D-351-02 removed-surface files: `KeeperBatchAffiliateDeltaAudit.t.sol` (subject = the
removed/never-landed `batchPurchase`/`batchPurchaseForKeeper`) and `RedemptionStethFallback.t.sol` (its
`AfKing.depositFor`/`poolOf`/`withdraw` custody-recovery leg = v54 de-custody machinery v55 replaced). For each:
adjudicate adapt-vs-drop per the D-351-02 rule (bias = adapt/preserve; drop ONLY a leg whose entire subject is a
fully-removed surface with NO behavioral successor), drop only those legs BY NAME with a reason, and reframe /
keep every other property.

Purpose: TST-04 (the affiliate-conservation + redemption properties that survive) + TST-05 (these files must
compile + their drops be finalized BY NAME for the non-widening ledger). This plan finalizes the D-351-02 drop
ledger that 351-09 consumes.
Output: 2 adapted/trimmed fuzz files + a recorded D-351-02 drop list (in the SUMMARY). ZERO `contracts/*.sol`
mutation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-CONTEXT.md
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-PATTERNS.md
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-01-SUMMARY.md

<interfaces>
<!-- The two removed-surface files + their adjudication (READ-ONLY anchors at 453f8073; never edit sites). -->

KeeperBatchAffiliateDeltaAudit.t.sol (PATTERNS section 9 + "No Analog Found"):
  0 afKing./doWork — its ENTIRE subject is `batchPurchase` / the proposed `batchPurchaseForKeeper` (:11-42), a v49/331 path
  that NEVER LANDED (:40 `TODO-331-05: flip to true once batchPurchaseForKeeper lands`) AND a surface v55 removed.
  The affiliate-conservation delta-audit it performs is RE-HOMED by 349.2's restored BURNIE affiliate (the per-buy
  affiliate.payAffiliate at GameAfkingModule.sol:806/:816).
  Adjudication: (a) reframe the affiliate-conservation delta-audit onto the 349.2 per-buy affiliate path, OR
  (b) if `batchPurchase` is its SOLE subject (no reusable affiliate-conservation property) then DROP with a BY-NAME + reason
  ledger entry. Bias = adapt; removal is the D-351-02 exception. The executor decides based on what the file actually asserts.

RedemptionStethFallback.t.sol (PATTERNS section 10):
  import=1 (AfKing.sol:7 — compile break) + 1 afKing. + uses AfKing(payable(AF_KING)).depositFor{value}(...)/.poolOf(...)/.withdraw(...)
  (:571-587) for the POOL-04 sDGNRS burnAtGameOver AfKing-prepaid-pool recovery (:564-589).
  The de-custody depositFor/poolOf/withdraw external API NO LONGER EXISTS (funding is game-resident afkingFunding).
  Adjudication: DROP the AfKing-custody-recovery leg (:564-589) BY NAME + reason (v54 de-custody machinery v55 replaced),
  OR reframe it onto a game-resident afkingFunding recovery IF one exists. The pure ETH-vs-stETH redemption-fallback
  CORE (the POOL-04a/b ETH-segregation asserts) STAYS + adapts. Repoint the :7 import regardless (drop it or repoint to
  the game type — the redemption core does not need the AfKing type).

349.2 per-buy affiliate (the reframe target): GameAfkingModule.sol :806 / :816 (affiliate.payAffiliate fresh + claimable branches) —
  BURNIE flip-credit (coinflip.creditFlip :831), so ETH/claimablePool UNCHANGED (no solvency surface).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adjudicate + handle KeeperBatchAffiliateDeltaAudit.t.sol (reframe onto 349.2 per-buy affiliate, or drop BY NAME)</name>
  <read_first>
    - test/fuzz/KeeperBatchAffiliateDeltaAudit.t.sol (FULL — 338 lines; the subject :11-42, the :40 TODO-331-05 never-landed flag, the affiliate-conservation delta-audit assertions)
    - contracts/modules/GameAfkingModule.sol :806/:816 (affiliate.payAffiliate fresh + claimable), :831 (coinflip.creditFlip)
    - .planning/phases/349.2-restore-quest-credit-affiliate-for-afking-lootbox-subs-reg/349.2-DESIGN.md (the restored affiliate behavior — read for what to assert if reframing)
    - .planning/phases/351-PATTERNS.md section 9 (the adjudication)
  </read_first>
  <action>
    Determine what `KeeperBatchAffiliateDeltaAudit.t.sol` actually asserts. If it carries a reusable
    affiliate-conservation property (total affiliate paid == sum of per-buy affiliate, no leakage), REFRAME it onto the
    349.2 per-buy `affiliate.payAffiliate` path (GameAfkingModule.sol:806/:816) — drive funded LOOTBOX-mode subs
    through a new-day STAGE and assert the affiliate BURNIE flip-credit is conserved (it is BURNIE via creditFlip, so
    ETH/claimablePool are unchanged — assert the credit, not an ETH cut). If the file's SOLE subject is the
    removed/never-landed `batchPurchase`/`batchPurchaseForKeeper` with no reusable property, DROP the file (or the
    batchPurchase-only test functions) per D-351-02 and record the dropped name(s) + reason
    ("batchPurchase/batchPurchaseForKeeper removed surface; affiliate-conservation re-homed by 349.2 per-buy
    affiliate") for 351-09's ledger. Bias = adapt; removal is the exception. Note the decision + the dropped/reframed
    names in the SUMMARY. Do NOT edit any contracts/ file.
  </action>
  <verify>
    <automated>If reframed: forge test --match-contract KeeperBatchAffiliateDeltaAudit exits cleanly. If dropped: the file (or the named functions) is gone and the SUMMARY records the BY-NAME + reason.</automated>
  </verify>
  <acceptance_criteria>
    - A decision is recorded in the SUMMARY: REFRAME (onto 349.2 per-buy affiliate) or DROP (BY NAME + reason).
    - If reframed: `grep -c "batchPurchase" test/fuzz/KeeperBatchAffiliateDeltaAudit.t.sol` == 0 (the removed-surface ref is gone) AND the affiliate-conservation property runs against the per-buy path.
    - If dropped: the file (or the batchPurchase-only functions) is removed AND the dropped test name(s) + reason are in the SUMMARY for the 351-09 ledger.
    - `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>KeeperBatchAffiliateDeltaAudit is adjudicated: either reframed onto the 349.2 per-buy affiliate path or dropped with a BY-NAME + reason ledger entry; the decision is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Adapt RedemptionStethFallback.t.sol — keep the ETH-vs-stETH core, drop the AfKing-custody-recovery leg BY NAME</name>
  <read_first>
    - test/fuzz/RedemptionStethFallback.t.sol (FULL — 619 lines; the :7 AfKing import, the POOL-04a/b ETH-segregation core, the custody-recovery leg :564-589 [depositFor/poolOf/withdraw :571-587])
    - contracts/modules/GameAfkingModule.sol (confirm there is NO game-resident depositFor/poolOf/withdraw external — funding is internal afkingFunding) — grep for any `withdrawAfkingFunding`/`afkingFundingOf` recovery the leg could reframe onto
    - contracts/storage/DegenerusGameStorage.sol :358 (SOLVENCY-01) — the redemption-core invariant context
    - .planning/phases/351-PATTERNS.md section 10 (the custody-recovery drop adjudication)
  </read_first>
  <action>
    Repoint/drop the `:7 import {…} from "../../contracts/AfKing.sol"` (the redemption core does not need the AfKing
    type — drop it, or repoint to the game type if a symbol is still referenced). DROP the AfKing-custody-recovery leg
    (the POOL-04 sDGNRS burnAtGameOver AfKing-prepaid-pool recovery, :564-589, using
    `depositFor`/`poolOf`/`withdraw` :571-587) per D-351-02 — the standalone de-custody external API no longer exists
    and there is no game-resident successor that recovers a PREPAID AfKing pool (confirm via the read_first grep; if a
    game-resident afkingFunding recovery DOES cover it, reframe instead). Record the dropped test name(s) + reason
    ("AfKing.depositFor/poolOf/withdraw custody-recovery = v54 de-custody machinery removed by v55; funding is
    game-resident afkingFunding, no prepaid-pool external recovery") for 351-09's ledger. KEEP + adapt the pure
    ETH-vs-stETH redemption-fallback CORE (the POOL-04a/b ETH-segregation asserts) — apply any call-site delta it needs
    (likely none beyond the import). Note the drop in the SUMMARY. Do NOT edit any contracts/ file.
  </action>
  <verify>
    <automated>forge test --match-contract RedemptionStethFallback 2>&1 | tail -6 (compiles + runs; the ETH-vs-stETH core present + green; the custody-recovery leg gone)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import.*AfKing.sol" test/fuzz/RedemptionStethFallback.t.sol` == 0 (the :7 import dropped/repointed).
    - `grep -c "depositFor\|\.poolOf(\|\.withdraw(" test/fuzz/RedemptionStethFallback.t.sol` for the AfKing-custody calls == 0 (the leg is dropped).
    - The file compiles + runs under `forge test --match-contract RedemptionStethFallback`; the POOL-04a/b ETH-vs-stETH core tests are present and green.
    - The dropped custody-recovery test name(s) + reason are recorded in the SUMMARY.
    - `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>RedemptionStethFallback compiles with the ETH-vs-stETH core preserved and the AfKing-custody-recovery leg dropped BY NAME (logged for the ledger); the import is repointed/dropped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only. Adjudicates + trims 2 removed-surface fuzz files. ZERO contract mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-351-06-FG | Tampering | the 2 removed-surface fuzz files | mitigate | False-GREEN / coverage-loss risks: (1) silently deleting a property that HAS a successor (violating the D-351-02 bias=adapt) — mitigated by the explicit adjudication (reframe onto 349.2 per-buy affiliate / game-resident recovery before dropping) + the BY-NAME + reason ledger entry 351-09 reconciles; (2) the surviving redemption core passing vacuously — mitigated by keeping the POOL-04a/b ETH-segregation asserts intact + green. ZERO `contracts/` mutation (`git diff 453f8073 HEAD -- contracts/` empty). |
</threat_model>

<verification>
- Both files compile + run under `forge test --match-contract`.
- The two D-351-02 drops are recorded BY NAME + reason in the SUMMARY (for the 351-09 ledger).
- The surviving redemption ETH-vs-stETH core + any reframed affiliate-conservation property are non-vacuous.
- `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
</verification>

<success_criteria>
The two D-351-02 removed-surface files are adjudicated and handled: the batchPurchase affiliate-delta-audit is
reframed onto the 349.2 per-buy affiliate path or dropped BY NAME; the redemption ETH-vs-stETH core stays while
the AfKing-custody-recovery leg is dropped BY NAME. Every drop is recorded with a reason for the 351-09
non-widening ledger. ZERO `contracts/*.sol` mutation.
</success_criteria>

<output>
Create `.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-06-SUMMARY.md` when done.
</output>
