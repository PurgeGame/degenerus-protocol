---
phase: 256-charity-allowlist-test-coverage
plan: 03b
type: execute
wave: 3
depends_on: [256-01, 256-03a]
files_modified:
  - test/governance/CharityAllowlist.test.js
autonomous: false
requirements: [TST-03]
must_haves:
  truths:
    - "test/governance/CharityAllowlist.test.js gains a Section 6 describe(\"vote(uint8 slot)\") covering all TST-03 surface."
    - "All four vote sad paths covered with reason-code asserts: InvalidSlot (slot >= 20), VoteRejected(0) REJECT_EMPTY_SLOT, VoteRejected(1) REJECT_ALREADY_VOTED, VoteRejected(2) REJECT_ZERO_WEIGHT (sub-1e18 sDGNRS exercises the integer-floor path)."
    - "Multi-slot vote independence: voter1 (100 sDGNRS) votes slots 3, 5, 7 — each succeeds with full 100 weight per slot (D-256-MULTI-VOTE-01)."
    - "Single-slot vote applies full sDGNRS weight; vote on locked filled slot 0 succeeds (D-256-LOCKED-SLOT-01 positive case for locked-slot voting)."
    - "hasVoted state is set per (level, voter, slot) tuple after vote."
    - "Vote revert order verified: InvalidSlot fires before EMPTY_SLOT (slot >= 20 with current == 0 → InvalidSlot, not VoteRejected)."
  artifacts:
    - path: "test/governance/CharityAllowlist.test.js"
      provides: "Appended Section 6 (vote describe) covering TST-03"
      contains: "describe(\"vote(uint8 slot)\""
      min_lines: 380
  key_links:
    - from: "test/governance/CharityAllowlist.test.js"
      to: "test/helpers/charityFixture.js"
      via: "uses pre-declared REJECT_EMPTY_SLOT / REJECT_ALREADY_VOTED / REJECT_ZERO_WEIGHT constants from Plan 03a"
      pattern: "REJECT_(EMPTY_SLOT\\|ALREADY_VOTED\\|ZERO_WEIGHT)"
---

<objective>
Append Section 6 (`describe("vote(uint8 slot)")`) to `test/governance/CharityAllowlist.test.js`. This plan extends the file scaffolding created by Plan 03a; the imports, constants (including pre-declared REJECT_* reason codes), top-level describe, and after-hook are already in place.

This is plan 2 of the 3-way split that resolves checker Warning #4. Plan 03b owns Section 6 only (TST-03 vote). Plan 03c will append sections 7-9 (pickCharity / TST-06 / gas).

Per `feedback_batch_contract_approval.md`, Plan 03b's commit is a SEPARATE commit from 03a's — single approval gate per commit. The append is mechanical (insert Section 6 between Section 5 and the top-level describe's closing brace).

Purpose: Phase 257 audit cites the full file as the structural-coverage evidence base for AUDIT-02 adversarial sweep. This plan lands TST-03 (vote weighting + 4 reject paths + multi-slot independence + locked-slot vote).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/256-charity-allowlist-test-coverage/256-CONTEXT.md
@.planning/phases/256-charity-allowlist-test-coverage/256-PATTERNS.md
@.planning/phases/256-charity-allowlist-test-coverage/256-01-SUMMARY.md
@.planning/phases/256-charity-allowlist-test-coverage/256-03a-SUMMARY.md
@test/governance/CharityAllowlist.test.js
@test/helpers/charityFixture.js
@test/helpers/deployFixture.js
@test/helpers/testUtils.js
@contracts/GNRUS.sol

<interfaces>
<!-- Vote-related contract surface this plan asserts against. -->

From contracts/GNRUS.sol (post-Phase-255 HEAD):
```solidity
// Errors
error InvalidSlot();                               // slot >= 20
error VoteRejected(uint8 reason);                  // 0=REJECT_EMPTY_SLOT, 1=REJECT_ALREADY_VOTED, 2=REJECT_ZERO_WEIGHT

// Events
event Voted(uint24 indexed level, uint8 indexed slot, address indexed voter, uint256 weight);

// Constants
uint8 private constant REJECT_EMPTY_SLOT = 0;
uint8 private constant REJECT_ALREADY_VOTED = 1;
uint8 private constant REJECT_ZERO_WEIGHT = 2;

// External entry points
function vote(uint8 slot) external;                // permissionless; revert order: InvalidSlot → REJECT_EMPTY_SLOT → REJECT_ALREADY_VOTED → REJECT_ZERO_WEIGHT
mapping(uint24 => mapping(address => mapping(uint8 => bool))) public hasVoted;
mapping(uint24 => mapping(uint8 => uint256)) public slotApproveWeight;
```

From test/helpers/charityFixture.js:
```js
export const POOL_REWARD;
export async function impersonate(address): Promise<Signer>;
export async function stopImpersonating(address): Promise<void>;
export async function giveSDGNRS(sdgnrs, gameAddress, recipient, amount): Promise<void>;
export async function deployGNRUSFixture(): Promise<{
  charity, charityAddress, sdgnrs, sdgnrsAddress, game, gameAddress,
  vault, vaultAddress, mockSteth, stethAddress,
  deployer, voter1, voter2, voter3,         // voter1: 100 sDGNRS, voter2: 100 sDGNRS, voter3: 200 sDGNRS
  recipient1, recipient2, recipient3, others
}>;
```

From test/governance/CharityAllowlist.test.js (Plan 03a output — ALREADY PRE-DECLARED at the top of the file):
```js
const REJECT_EMPTY_SLOT = 0;
const REJECT_ALREADY_VOTED = 1;
const REJECT_ZERO_WEIGHT = 2;
```
Plan 03b uses these constants directly — does NOT re-declare them.
</interfaces>

<decision_refs>
- D-256-LAYOUT-01 — NEW file `test/governance/CharityAllowlist.test.js` owns ALL v33 governance surface. Plan 03a created the scaffolding + sections 1-5; 03b appends section 6.
- D-256-VOTE-REJECT-01 — All 4 vote sad paths via reason codes (InvalidSlot, REJECT_EMPTY_SLOT, REJECT_ALREADY_VOTED, REJECT_ZERO_WEIGHT). Zero-weight uses sub-1e18 sDGNRS (e.g. 5e17) NOT zero balance — exercises the integer-floor `/ 1e18 == 0` path.
- D-256-MULTI-VOTE-01 — voter1 (100 sDGNRS) votes slots 3, 5, 7 — each succeeds with full 100 weight (sum = 300, NOT 100/3).
- D-256-LOCKED-SLOT-01 — Voters CAN vote on locked slots (positive case lands in this plan's Section 6 in addition to Plan 03a's Section 3 coverage; OR planner chooses to leave the locked-slot vote in 03a only and skips here. Acceptance criteria below tolerate either choice.)
- `feedback_no_contract_commits.md` + `feedback_never_preapprove_contracts.md` + `feedback_wait_for_approval.md` — test/ file additions require explicit per-commit user approval; this plan has `autonomous: false`.
- `feedback_no_history_in_comments.md` — no `// removed:`, `// was:`, `// migrated from v32` annotations.
</decision_refs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Section 6 (vote describe) to test/governance/CharityAllowlist.test.js</name>
  <files>test/governance/CharityAllowlist.test.js</files>
  <read_first>
    - test/governance/CharityAllowlist.test.js (Plan 03a output — confirms scaffolding + pre-declared constants + closing-brace location)
    - test/helpers/charityFixture.js (Plan 01 output)
    - contracts/GNRUS.sol lines 558-581 (vote 4-reject-path order)
    - .planning/phases/256-charity-allowlist-test-coverage/256-CONTEXT.md `<decisions>` D-256-VOTE-REJECT-01 + D-256-MULTI-VOTE-01 + D-256-LOCKED-SLOT-01 + `<specifics>` (zero-weight setup, multi-slot independence)
    - .planning/phases/256-charity-allowlist-test-coverage/256-PATTERNS.md "Custom-error revert pattern WITH `withArgs(reasonCode)`" + "Game-impersonation" sections
  </read_first>
  <action>
    Locate the closing brace of the top-level `describe("GNRUS Charity Allowlist (v33.0)", function () { ... });` in the file. Before that closing brace, AFTER Section 5's closing `});`, INSERT the following Section 6 describe.

    **Per-it-block convention:** Each `it()` body begins with `const { ... } = await loadFixture(deployGNRUSFixture);` to load the v33 fixture. The destructure list typically includes some subset of `{ charity, charityAddress, deployer, voter1, voter2, voter3, recipient1, recipient2, recipient3, others, gameAddress, sdgnrs }`. This preamble is OMITTED from per-it-block descriptions below for brevity (Warning #7 resolution).

    **Section 6 — `describe("vote(uint8 slot)", ...)` (TST-03):**
    - `it("single-slot vote applies full sDGNRS weight")` — instant-apply slot 5 with recipient1; voter1.vote(5) → emits `Voted(0n, 5n, voter1.address, 100n)`; `slotApproveWeight(0, 5) == 100n`.
    - `it("vote on slot 0 (locked, filled) succeeds")` — first-fill slot 0 with recipient1; voter1.vote(0) succeeds; `slotApproveWeight(0, 0) == 100n`. (D-256-LOCKED-SLOT-01 positive case; Section 3 also covers — keep here for vote-describe coherence; planner discretion at execution time on whether to drop one of the duplicates.)
    - `it("multi-slot vote independence: voter1 votes slots 3, 5, 7 — full weight each")` — D-256-MULTI-VOTE-01. Instant-apply slots 3, 5, 7 with three distinct recipients (recipient1, recipient2, recipient3). voter1.vote(3); voter1.vote(5); voter1.vote(7). Assert each emits Voted with weight=100; `slotApproveWeight(0, 3) == 100n`; `slotApproveWeight(0, 5) == 100n`; `slotApproveWeight(0, 7) == 100n`. Total approve sum = 300, NOT 100/3.
    - `it("hasVoted is set per (level, voter, slot) tuple after vote")` — assert `await charity.hasVoted(0, voter1.address, 5)` returns true after the vote; returns false for other slots/voters/levels (e.g., `hasVoted(0, voter1.address, 7)` false; `hasVoted(0, voter2.address, 5)` false; `hasVoted(1, voter1.address, 5)` false).
    - `it("InvalidSlot on slot >= 20")` — `voter1.vote(20)` → `revertedWithCustomError(charity, "InvalidSlot")`. Also test slot=255.
    - `it("VoteRejected(REJECT_EMPTY_SLOT) on empty slot")` — slot 7 never filled; `voter1.vote(7)` → `revertedWithCustomError(charity, "VoteRejected").withArgs(REJECT_EMPTY_SLOT)`.
    - `it("VoteRejected(REJECT_ALREADY_VOTED) on second vote for same (level, voter, slot)")` — instant-apply slot 5; voter1.vote(5); voter1.vote(5) again → `revertedWithCustomError(charity, "VoteRejected").withArgs(REJECT_ALREADY_VOTED)`.
    - `it("VoteRejected(REJECT_ZERO_WEIGHT) on sub-1e18 sDGNRS balance (integer-floor)")` — D-256-VOTE-REJECT-01. Use a fresh signer (e.g. `others[5]`) NOT pre-funded by the fixture. Fund with `giveSDGNRS(sdgnrs, gameAddress, others[5].address, eth("0.5"))` — 0.5e18 sDGNRS, divides to 0 weight. Instant-apply slot 5; `others[5].vote(5)` → `revertedWithCustomError(charity, "VoteRejected").withArgs(REJECT_ZERO_WEIGHT)`. (Exercises the `weight = sdgnrs.balanceOf(voter) / 1e18` integer-floor path explicitly.)
    - `it("vote revert order: InvalidSlot fires before EMPTY_SLOT")` — Verify revert order at L558-573 of contracts/GNRUS.sol: slot=20 with current==0 → InvalidSlot (not EMPTY_SLOT). voter1.vote(20) on a never-filled slate → must be InvalidSlot, not VoteRejected.

    **Append the Section 6 describe BETWEEN Section 5's closing `});` and the top-level describe's closing `});`. Do NOT modify the imports, constants, top-level describe declaration, or after-hook — those are owned by Plan 03a.

    **Section 6 estimated size: ~100-130 lines, ~9 it-blocks.** Use the pre-declared REJECT_* constants from Plan 03a directly; do not re-declare.

    **No history-in-comments.** No `// removed for v33`, `// was: vote(proposalId, true)`, etc.

    **No commented-out tests.** No `it.skip(...)` shells.
  </action>
  <verify>
    <automated>npx hardhat test test/governance/CharityAllowlist.test.js 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `npx hardhat test test/governance/CharityAllowlist.test.js` exits 0 with all it-blocks passing (sections 1-5 from 03a + section 6 from this plan).
    - `grep -c "describe(" test/governance/CharityAllowlist.test.js | grep -v '^#'` returns 7 (top + sections 1-6).
    - `grep -c "it(" test/governance/CharityAllowlist.test.js | grep -v '^#'` returns ≥ 28 (sections 1-5 from 03a ≥ 20 + section 6 ≥ 8).
    - `grep -c "VoteRejected.*withArgs(0)\\|VoteRejected.*withArgs(REJECT_EMPTY_SLOT)" test/governance/CharityAllowlist.test.js | grep -v '^#'` ≥ 1 (REJECT_EMPTY_SLOT covered).
    - `grep -c "VoteRejected.*withArgs(1)\\|VoteRejected.*withArgs(REJECT_ALREADY_VOTED)" test/governance/CharityAllowlist.test.js | grep -v '^#'` ≥ 1 (REJECT_ALREADY_VOTED covered).
    - `grep -c "VoteRejected.*withArgs(2)\\|VoteRejected.*withArgs(REJECT_ZERO_WEIGHT)" test/governance/CharityAllowlist.test.js | grep -v '^#'` ≥ 1 (REJECT_ZERO_WEIGHT covered).
    - `grep -c "InvalidSlot" test/governance/CharityAllowlist.test.js | grep -v '^#'` ≥ 2 (vote slot bounds + setCharity slot bounds from 03a).
    - `grep -c "hasVoted" test/governance/CharityAllowlist.test.js | grep -v '^#'` ≥ 2 (positive + negative assertions in the hasVoted it-block).
    - `grep -c "describe(\"vote(uint8 slot)\"" test/governance/CharityAllowlist.test.js | grep -v '^#'` returns 1 (Section 6 added).
    - `grep -c "describe(\"setCharity" test/governance/CharityAllowlist.test.js | grep -v '^#'` returns ≥ 4 (sections 1-4 from 03a; section 5 may use a different describe name like "edit-queue level-boundary semantics").
    - `grep -in "// removed\\|// was:\\|// migrated\\|// v32" test/governance/CharityAllowlist.test.js | grep -v '^#'` returns nothing (no history-in-comments).
    - `grep -c "it.skip(" test/governance/CharityAllowlist.test.js | grep -v '^#'` returns 0.
    - `wc -l test/governance/CharityAllowlist.test.js` returns ≥ 380 (250-400 from 03a + 100-130 from 03b).
    - Plan 03a's CapExceeded structural unreachability comment block is preserved (grep `grep -ic "CapExceeded.*structurally unreachable\\|popcount32(future).*<= 20" test/governance/CharityAllowlist.test.js` ≥ 1 — Plan 03b must not delete it).
  </acceptance_criteria>
  <done>
    `test/governance/CharityAllowlist.test.js` gains Section 6 (~9 it-blocks) covering TST-03 fully. All 4 vote reject paths asserted via reason codes. Multi-slot vote independence proved. hasVoted state asserted. Revert order verified. All tests pass. No history-in-comments. No skipped tests. Plan 03a's content (sections 1-5, scaffolding, verdicts) preserved unchanged.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Present test/governance/CharityAllowlist.test.js (Section 6 append) for user approval</name>
  <what-built>Appended Section 6 (`describe("vote(uint8 slot)")`) to `test/governance/CharityAllowlist.test.js` (~9 it-blocks, ~100-130 added lines). Covers: single-slot vote weight, vote on locked slot (positive), multi-slot independence (D-256-MULTI-VOTE-01), hasVoted state, all 4 vote reject paths via reason codes (InvalidSlot, REJECT_EMPTY_SLOT, REJECT_ALREADY_VOTED, REJECT_ZERO_WEIGHT via sub-1e18 sDGNRS), and revert-order verification.</what-built>
  <how-to-verify>
    1. Run `git diff --stat -- test/governance/CharityAllowlist.test.js` — expect ~100-130 lines added, 0 lines deleted (pure append between Section 5's `});` and the top-level `});`).
    2. Run `git diff -- test/governance/CharityAllowlist.test.js | head -200` to inspect the appended Section 6.
    3. Run `wc -l test/governance/CharityAllowlist.test.js` — expect 380-530 lines (cumulative with Plan 03a).
    4. Run `grep -c "describe(\\|it(" test/governance/CharityAllowlist.test.js` — expect 7 describes + ≥28 it-blocks.
    5. Run `npx hardhat test test/governance/CharityAllowlist.test.js` — confirm green.
    6. Run the reason-code coverage sweep (Section 6 portion):
       ```bash
       grep -E "VoteRejected.*withArgs|InvalidSlot" test/governance/CharityAllowlist.test.js | wc -l
       ```
       Expect ≥ 6 (3 reason codes for vote + 2 InvalidSlot calls + 1 wider sweep margin).
    7. Verify Plan 03a's verdicts are preserved:
       ```bash
       grep -ic "structurally unreachable\\|defensive guard" test/governance/CharityAllowlist.test.js
       ```
       Expect ≥ 2 (must be unchanged from 03a).
    8. Spot-check the multi-slot vote independence it-block: `grep -A 20 "multi-slot vote independence" test/governance/CharityAllowlist.test.js` — confirm voter1 votes 3 distinct slots and each ends up with weight 100.
    9. Spot-check the zero-weight test: `grep -A 15 "REJECT_ZERO_WEIGHT" test/governance/CharityAllowlist.test.js` — confirm `eth("0.5")` (sub-1e18) funding pattern, NOT zero balance.
    10. Run `npx hardhat test` (full suite) — confirm no regressions.
    11. Approve the diff via explicit "approved" reply per `feedback_wait_for_approval.md` + `feedback_manual_review_before_push.md` + `feedback_batch_contract_approval.md` (one approval per commit).
  </how-to-verify>
  <resume-signal>Type "approved" to commit `test/governance/CharityAllowlist.test.js` (Section 6 append), or describe issues to revise.</resume-signal>
  <files>test/governance/CharityAllowlist.test.js</files>
  <action>Present the diff produced by Task 1 to the user via the resume-signal protocol; user reviews and explicitly approves before commit. This is a checkpoint gate per `feedback_no_contract_commits.md` + `feedback_never_preapprove_contracts.md` + `feedback_wait_for_approval.md` + `feedback_batch_contract_approval.md` (one approval per commit). On approval: commit `test/governance/CharityAllowlist.test.js` with message `feat(256-03b): CharityAllowlist.test.js add vote() describe`. On revision request: cycle back to Task 1, address feedback, re-present the updated diff.</action>
  <verify>
    <automated>git log -1 --pretty=format:%s -- test/governance/CharityAllowlist.test.js | grep -q "256-03b" && echo "committed" || echo "awaiting approval"</automated>
  </verify>
  <done>User has typed "approved" (or equivalent), the diff for `test/governance/CharityAllowlist.test.js` (Section 6 append) is committed in a single commit referencing the Phase 256-03b plan number, and `git status` shows the file clean.</done>
</task>

</tasks>

<verification>
- `npx hardhat test test/governance/CharityAllowlist.test.js` exits 0 with all sections-1-6 it-blocks passing.
- `npx hardhat test` (full suite) exits 0.
- All 3 vote reason codes asserted with `withArgs(N)`; revert order verified; multi-slot independence + hasVoted + zero-weight (integer-floor) all green.
- Plan 03a's verdict comments (CapExceeded + D-256-CANCEL-QUEUED-01 structural unreachability) preserved.
</verification>

<success_criteria>
- `test/governance/CharityAllowlist.test.js` Section 6 added with ~9 it-blocks, all passing.
- TST-03 (vote 4-reject-paths + multi-slot + hasVoted + revert-order) is satisfied.
- D-256-VOTE-REJECT-01 + D-256-MULTI-VOTE-01 covered.
- User has explicitly approved the diff per `feedback_no_contract_commits.md` + `feedback_wait_for_approval.md` + `feedback_batch_contract_approval.md`.
- File extension is append-only (no modification to Plan 03a's imports / constants / top-level describe / sections 1-5 content).
</success_criteria>

<output>
After completion, create `.planning/phases/256-charity-allowlist-test-coverage/256-03b-SUMMARY.md` documenting:
- Final line count (cumulative with 03a) + Section 6 it-block count.
- Confirmation that all 4 vote reject paths (InvalidSlot + 3 VoteRejected reasons) are covered with reason-code asserts.
- Confirmation that the integer-floor path (sub-1e18 sDGNRS → REJECT_ZERO_WEIGHT) is exercised explicitly with `eth("0.5")` funding.
- Confirmation that multi-slot vote independence (D-256-MULTI-VOTE-01) totals weight = 300 across slots 3/5/7 (not 100/3).
- Confirmation that Plan 03a's CapExceeded + D-256-CANCEL-QUEUED-01 unreachability verdicts are preserved unchanged.
</output>
</output>
