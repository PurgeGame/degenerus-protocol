---
phase: 87-other-jackpots
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v4.0-other-jackpots-degenerette.md
autonomous: true
requirements:
  - OJCK-04
  - OJCK-06

must_haves:
  truths:
    - "The degenerette bet placement path (placeFullTicketBets) is documented with file:line including currency types, minimums, max spins, and packed bet storage"
    - "The ETH fund flow (futurePrizePool vs pending pools during frozen state) is traced with file:line"
    - "The bet resolution path (_resolveFullTicketBet) with lootbox RNG dependency, per-spin deterministic derivation, and 8-attribute match counting is documented with file:line"
    - "The payout distribution (25% ETH capped at 10% pool, 75% lootbox) is traced with file:line"
    - "The sDGNRS reward path (_awardDegeneretteDgnrs) for 6+ matches on ETH bets is documented with file:line including BPS rates and 1 ETH cap"
    - "The consolation prize (_maybeAwardConsolation) for qualifying losers is documented with file:line"
    - "The degenerette _addClaimableEth is confirmed as a DIFFERENT function from JackpotModule/EndgameModule versions (NO auto-rebuy)"
    - "topDegeneretteByLevel write/read analysis is documented -- whether consumed by any jackpot logic or view-only"
    - "Every claim about degenerette mechanics in prior audit docs is cross-referenced and confirmed or flagged as [DISCREPANCY]"
  artifacts:
    - path: "audit/v4.0-other-jackpots-degenerette.md"
      provides: "Degenerette jackpot audit with file:line citations covering bet, resolution, payout, sDGNRS, and consolation"
      contains: "## 1. Degenerette Overview"
  key_links:
    - from: "audit/v4.0-other-jackpots-degenerette.md"
      to: "contracts/modules/DegenerusGameDegeneretteModule.sol"
      via: "file:line citations for placeFullTicketBets (DDM:388), _resolveFullTicketBet (DDM:585), _distributePayout (DDM:680), _awardDegeneretteDgnrs (DDM:1164), _maybeAwardConsolation (DDM:722), _addClaimableEth (DDM:1153)"
      pattern: "DDM:\\d+"
    - from: "audit/v4.0-other-jackpots-degenerette.md"
      to: "contracts/storage/DegenerusGameStorage.sol"
      via: "file:line citations for degeneretteBets, playerDegeneretteEthWagered, topDegeneretteByLevel, dailyHeroWagers"
      pattern: "GS:\\d+"
---

<objective>
Audit the degenerette jackpot mechanics -- a symbol-match betting game operating independently from advanceGame (player-initiated bets resolved with lootbox RNG). The degenerette has unique characteristics: it uses a different _addClaimableEth without auto-rebuy, has sDGNRS rewards for high matches, a consolation prize system, and its own per-level tracking state.

Purpose: The degenerette is the only "other jackpot" that operates outside the level-transition flow, using lootbox RNG instead of advanceGame RNG. Its ETH payout mechanics (25/75 split with 10% pool cap) and separate _addClaimableEth function need careful comparison against the JackpotModule/EndgameModule versions to ensure no cross-contamination or missing auto-rebuy.

Output: `audit/v4.0-other-jackpots-degenerette.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-other-jackpots/87-RESEARCH.md

Key contract files to trace:
- contracts/modules/DegenerusGameDegeneretteModule.sol (ALL degenerette mechanics: DDM:1-1179)
- contracts/DegenerusGame.sol (delegatecall wrappers for placeFullTicketBets, resolveBets)
- contracts/storage/DegenerusGameStorage.sol (degeneretteBets packed storage, playerDegeneretteEthWagered, topDegeneretteByLevel, dailyHeroWagers, claimablePool, claimableWinnings)
- contracts/modules/DegenerusGameJackpotModule.sol (for _addClaimableEth COMPARISON -- the version WITH auto-rebuy)

Prior audit docs for cross-reference:
- audit/v4.0-ticket-creation-queue-mechanics.md (lootbox RNG flow context)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit degenerette bet placement, resolution, and match mechanics (OJCK-04 part 1)</name>
  <files>audit/v4.0-other-jackpots-degenerette.md</files>
  <read_first>
    contracts/modules/DegenerusGameDegeneretteModule.sol
    contracts/DegenerusGame.sol
    contracts/storage/DegenerusGameStorage.sol
  </read_first>
  <action>
Create the audit document `audit/v4.0-other-jackpots-degenerette.md` with the following structure. Begin with a header and phase/requirement metadata.

**Section 1: Degenerette Overview**

Note that degenerette operates INDEPENDENTLY from advanceGame. It is player-initiated (bet + resolve) using lootbox RNG. Document the call chain:
```
User -> DegenerusGame.placeFullTicketBets -> delegatecall -> DegeneretteModule._placeFullTicketBets
User -> DegenerusGame.resolveBets -> delegatecall -> DegeneretteModule._resolveFullTicketBet
```

**Section 2: Bet Placement**

Trace `placeFullTicketBets` at DDM:388-404 (and the internal `_placeFullTicketBets`):

2.1 **Currency types**: ETH (0), BURNIE (1), WWXRP (3) -- verify enum values and mapping
2.2 **Minimum bets**: 0.005 ETH, 100 BURNIE, 1 WWXRP -- verify constants and locations
2.3 **Max spins per bet**: 10 (`MAX_SPINS_PER_BET`) -- verify constant
2.4 **RNG binding**: `lootboxRngIndex` at DDM:473 -- how the bet is tied to a pending VRF word
2.5 **Packed bet storage**: `degeneretteBets[player][nonce]` -- 256-bit packed struct. Document all fields and bit positions.
2.6 **ETH fund flow** at DDM:547-565:
   - Normal: ETH goes to futurePrizePool
   - Frozen: ETH goes to pending pools
   - `lootboxRngPendingEth += totalBet` for RNG tracking
2.7 **Per-level tracking**:
   - `playerDegeneretteEthWagered[player][lvl]` at GS:1351
   - `topDegeneretteByLevel[lvl]` packed storage at GS:1355 -- trace the write logic (DDM:518-523)
   - `dailyHeroWagers[day][quadrant]` packed uint256 at GS:1343

**Section 3: Bet Resolution**

Trace `resolveBets` at DDM:411 / `_resolveFullTicketBet` at DDM:585-672:

3.1 **RNG requirement**: `lootboxRngWordByIndex[index] != 0` -- bet cannot resolve until VRF fulfillment
3.2 **Per-spin result derivation**: `keccak256(rngWord, index, [spinIdx,] QUICK_PLAY_SALT)` at DDM:617-618. Document exact hash inputs for each spin.
3.3 **Match counting**: 8 attributes (4 quadrants x color + symbol) at DDM:855-876. Trace the exact match counting algorithm:
   - How quadrant/attribute results are derived from entropy
   - How matches are counted (0-8 range)
   - Hero quadrant boost/penalty mechanism

3.4 **Payout calculation**: Base multiplier table scaled by ROI BPS:
   - Document full payout table: 0-1 match: 0x; 2: 1.90x; 3: 4.75x; 4: 15x; 5: 42.5x; 6: 195x; 7: 1000x; 8: 100,000x
   - Activity-score-based ROI BPS (90-99.9%) -- trace the ROI computation
   - ETH: +5% bonus ROI for 5+ matches
   - WWXRP: up to 109.9% high-value ROI target for 5+ matches
   - EV normalization -- how per-outcome EVs are equalized regardless of trait selection
  </action>
  <verify>
    <automated>grep -c "DDM:" audit/v4.0-other-jackpots-degenerette.md | xargs test 15 -le</automated>
  </verify>
  <acceptance_criteria>
    - File audit/v4.0-other-jackpots-degenerette.md exists
    - Contains "## 1. Degenerette Overview" section header
    - Contains "## 2. Bet Placement" section header
    - Contains "## 3. Bet Resolution" section header
    - Contains file:line citations matching pattern "DDM:\d+" at least 15 times
    - Contains "placeFullTicketBets" function trace
    - Contains "_resolveFullTicketBet" function trace
    - Contains "lootboxRngIndex" RNG binding documentation
    - Contains payout multiplier table (0x through 100,000x)
    - Contains "degeneretteBets" packed storage documentation
    - Contains "futurePrizePool" ETH fund flow trace
    - Contains match counting algorithm (8 attributes, 4 quadrants)
  </acceptance_criteria>
  <done>Degenerette bet placement and resolution fully traced: currency types, bet packing, lootbox RNG binding, per-spin derivation, 8-attribute match counting, and payout multiplier table -- all with file:line citations</done>
</task>

<task type="auto">
  <name>Task 2: Audit degenerette payout distribution, sDGNRS rewards, consolation, and _addClaimableEth comparison (OJCK-04 part 2)</name>
  <files>audit/v4.0-other-jackpots-degenerette.md</files>
  <read_first>
    contracts/modules/DegenerusGameDegeneretteModule.sol
    contracts/modules/DegenerusGameJackpotModule.sol
    contracts/storage/DegenerusGameStorage.sol
  </read_first>
  <action>
Append to the audit document created in Task 1.

**Section 4: ETH Payout Distribution**

Trace `_distributePayout` at DDM:680-715:

4.1 **prizePoolFrozen guard** at DDM:685 -- prevents futurePrizePool corruption during jackpot phase
4.2 **25/75 split**:
   - `ethPortion = payout / 4` (25%)
   - `lootboxPortion = payout - ethPortion` (75%)
4.3 **10% pool cap**: `maxEth = (pool * ETH_WIN_CAP_BPS) / 10_000` where `ETH_WIN_CAP_BPS = 1000`
   - If ethPortion > maxEth: excess added to lootboxPortion, ethPortion capped
4.4 **ETH credit**: `_addClaimableEth` call -- THIS IS THE DEGENERETTE VERSION (DDM:1153-1159)
4.5 **Lootbox conversion**: delegatecall to `resolveLootboxDirect` for lootbox portion
4.6 **Pool update**: `futurePrizePool -= ethPortion` at DDM:702-703

**Section 5: _addClaimableEth Comparison**

CRITICAL comparison -- three different _addClaimableEth implementations exist:

5.1 **Degenerette version** (DDM:1153-1159):
   - Credits `claimablePool` and `claimableWinnings[player]`
   - Does NOT include auto-rebuy logic
   - Simple ETH accounting only

5.2 **JackpotModule/EndgameModule version** (find exact file:line in JackpotModule):
   - Credits `claimablePool` and `claimableWinnings[player]`
   - INCLUDES auto-rebuy: if player has auto-rebuy enabled, converts ETH to tickets instead of crediting
   - Returns the claimable delta (amount actually credited vs amount auto-rebuyed)

5.3 **Document the difference**: The degenerette intentionally does NOT auto-rebuy. Verify this is by design (likely: degenerette payouts are player-resolved, not system-awarded, so auto-rebuy would be unexpected). Flag if this is undocumented in prior audits.

**Section 6: sDGNRS Rewards**

Trace `_awardDegeneretteDgnrs` at DDM:1164-1178:

6.1 **Eligibility**: ETH bets only, 6+ matches
6.2 **BPS rates**: 6 match = 4% (`DGRS_6_BPS`), 7 match = 8% (`DGRS_7_BPS`), 8 match = 15% (`DGRS_8_BPS`) -- verify constants and locations
6.3 **1 ETH cap**: bet amount capped at 1 ETH for reward calculation at DDM:1173
6.4 **Pool source**: `IStakedDegenerusStonk.Pool.Reward`
6.5 **Transfer**: `dgnrs.transferFromPool(...)` -- trace the exact call

**Section 7: Consolation Prize**

Trace `_maybeAwardConsolation` at DDM:722-737:

7.1 **Eligibility**: Total losing bets (totalPayout == 0)
7.2 **Minimum qualifiers**: 0.01 ETH, 500 BURNIE, 20 WWXRP -- verify constants
7.3 **Award**: 1 WWXRP -- trace the token transfer mechanism

**Section 8: topDegeneretteByLevel Usage Analysis**

8.1 **Write path**: DDM:518-523 during bet placement. Packed (amount << 160 | address).
8.2 **Read paths**: Search ALL contracts for `topDegeneretteByLevel` reads. Check DG:2817 (view function) and any other consumers.
8.3 **Verdict**: Is this consumed by any jackpot or reward mechanism, or is it view-only (frontend state)?
8.4 If view-only, tag as [INFO] -- storage that could be removed if gas optimization desired.

**Section 9: Findings Summary**

9.1 Summary table of all findings: ID, Description, Severity, Location
9.2 _addClaimableEth comparison verdict
9.3 topDegeneretteByLevel usage verdict
9.4 prizePoolFrozen guard completeness assessment

Tag all discrepancies with [DISCREPANCY] and new issues with [NEW FINDING] per OJCK-06.
  </action>
  <verify>
    <automated>grep -c "_addClaimableEth\|_awardDegeneretteDgnrs\|_maybeAwardConsolation\|_distributePayout\|topDegeneretteByLevel" audit/v4.0-other-jackpots-degenerette.md | xargs test 8 -le</automated>
  </verify>
  <acceptance_criteria>
    - Contains "## 4. ETH Payout Distribution" section header
    - Contains "## 5. _addClaimableEth Comparison" with explicit comparison of degenerette vs JM/EM versions
    - Contains "## 6. sDGNRS Rewards" section header
    - Contains "## 7. Consolation Prize" section header
    - Contains "## 8. topDegeneretteByLevel Usage Analysis" with verdict
    - Contains "## 9. Findings Summary" with severity table
    - Contains "_distributePayout" with 25/75 split and 10% cap trace
    - Contains "_addClaimableEth" explicitly noted as "does NOT include auto-rebuy"
    - Contains "_awardDegeneretteDgnrs" with BPS rates (4%, 8%, 15%)
    - Contains "_maybeAwardConsolation" with WWXRP consolation trace
    - Contains "ETH_WIN_CAP_BPS" constant reference
    - If any discrepancy found, contains "[DISCREPANCY]" or "[NEW FINDING]" tag with severity
  </acceptance_criteria>
  <done>Degenerette payout fully traced: 25/75 ETH/lootbox split with 10% cap, _addClaimableEth comparison confirming no auto-rebuy, sDGNRS rewards for 6+ matches, consolation WWXRP, topDegeneretteByLevel usage verdict. Complete findings summary with severity ratings.</done>
</task>

</tasks>

<verification>
- All file:line citations in the audit document are verifiable against the contracts/ directory
- Bet placement -> resolution -> payout lifecycle fully traced
- _addClaimableEth comparison between degenerette and JM/EM versions explicitly documented
- sDGNRS reward rates and 1 ETH cap documented
- Consolation prize mechanics documented
- topDegeneretteByLevel consumption determined (jackpot logic or view-only)
- All discrepancies tagged with [DISCREPANCY] or [NEW FINDING] and severity
</verification>

<success_criteria>
- audit/v4.0-other-jackpots-degenerette.md exists with 9 sections
- At least 25 file:line citations (DDM:, GS:, DG: patterns combined)
- Bet placement with 3 currencies and packed storage documented
- Per-spin RNG derivation documented
- Match counting (8 attributes) documented
- Payout multiplier table documented
- 25/75 split with 10% pool cap documented
- _addClaimableEth comparison: degenerette (no auto-rebuy) vs JM/EM (with auto-rebuy)
- sDGNRS rewards documented with BPS rates
- Consolation prize documented
- topDegeneretteByLevel verdict rendered
- All findings tagged per OJCK-06
</success_criteria>

<output>
After completion, create `.planning/phases/87-other-jackpots/87-04-SUMMARY.md`
</output>
