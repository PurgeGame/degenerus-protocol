---
phase: 83-ticket-consumption-winner-selection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v4.0-ticket-consumption-winner-selection.md
autonomous: true
requirements: [TCON-01, TCON-02]

must_haves:
  truths:
    - "Every function that reads ticketQueue for winner selection is identified with file:line citation"
    - "Every function that reads traitBurnTicket for winner selection is identified with file:line citation"
    - "Non-winner-selection reads (processing, length checks) are enumerated separately for completeness"
    - "Each read site documents: function name, file:line, key used, purpose, and which jackpot type it serves"
  artifacts:
    - path: "audit/v4.0-ticket-consumption-winner-selection.md"
      provides: "Sections 1-3: Executive summary, ticketQueue read enumeration, traitBurnTicket read enumeration"
      contains: "ticketQueue"
  key_links:
    - from: "audit/v4.0-ticket-consumption-winner-selection.md"
      to: "contracts/modules/DegenerusGameJackpotModule.sol"
      via: "file:line citations for all winner selection reads"
      pattern: "JM:\\d+"
    - from: "audit/v4.0-ticket-consumption-winner-selection.md"
      to: "contracts/DegenerusGame.sol"
      via: "file:line citations for view function reads"
      pattern: "DG:\\d+"
---

<objective>
Independently trace and document every function that reads from ticketQueue or traitBurnTicket for the purpose of winner selection, producing the first half of the Phase 83 audit document.

Purpose: TCON-01 and TCON-02 require exhaustive enumeration of all storage reads used for winner selection. This forms the foundation for Plan 02's winner index computation analysis.
Output: audit/v4.0-ticket-consumption-winner-selection.md (Sections 1-3)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-ticket-consumption-winner-selection/83-RESEARCH.md
@.planning/phases/81-ticket-creation-queue-mechanics/81-01-SUMMARY.md
@.planning/phases/81-ticket-creation-queue-mechanics/81-02-SUMMARY.md

Source contracts (read from contracts/ directory ONLY -- stale copies exist elsewhere):
@contracts/modules/DegenerusGameJackpotModule.sol
@contracts/DegenerusGame.sol
@contracts/DegenerusJackpots.sol
@contracts/storage/DegenerusGameStorage.sol
@contracts/modules/DegenerusGameAdvanceModule.sol

Prior audit documents for cross-reference:
@audit/v4.0-ticket-creation-queue-mechanics.md
@audit/v4.0-ticket-queue-double-buffer.md
@audit/v4.0-findings-consolidated.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate all ticketQueue reads for winner selection (TCON-01)</name>
  <read_first>
    - contracts/modules/DegenerusGameJackpotModule.sol (search for all ticketQueue references -- grep for "ticketQueue", "_tqFarFutureKey", "_tqReadKey", "_tqWriteKey")
    - contracts/DegenerusGame.sol (search for "sampleFarFutureTickets", "ticketQueue")
    - contracts/DegenerusJackpots.sol (search for "sampleFarFutureTickets", "ticketQueue")
    - contracts/modules/DegenerusGameAdvanceModule.sol (search for "ticketQueue" -- length checks, not winner selection)
    - contracts/storage/DegenerusGameStorage.sol (search for "ticketQueue" -- slot definition, key helpers)
    - audit/v4.0-ticket-queue-double-buffer.md (Phase 81 enumeration of all ticketQueue key consumers for cross-reference)
  </read_first>
  <files>audit/v4.0-ticket-consumption-winner-selection.md</files>
  <action>
Create the audit document with Sections 1-2. CRITICAL: Do NOT reproduce the research -- independently grep and read the Solidity source to build the enumeration from scratch. The research is a guide for what to look for, not a source to copy from.

**Section 1: Executive Summary**
- State the audit scope: ticket consumption (reads) for winner selection across all jackpot types
- Methodology: exhaustive grep of ticketQueue and traitBurnTicket reads across all contracts in contracts/ directory
- List finding counts (populated after both tasks complete -- leave placeholder)
- Use the established v4.0 audit document format from audit/v4.0-ticket-creation-queue-mechanics.md

**Section 2: ticketQueue Reads for Winner Selection (TCON-01)**

Step 1: Grep for ALL ticketQueue read sites across the entire contracts/ directory:
```
grep -rn 'ticketQueue\[' contracts/
grep -rn '_tqFarFutureKey\|_tqReadKey\|_tqWriteKey' contracts/
```

Step 2: For each result, classify as:
- **Winner selection read** (function uses the queue contents to determine a jackpot winner)
- **Processing read** (processTicketBatch/processFutureTicketBatch reading to assign traits)
- **Length check** (advanceGame checking queue length for drain/swap gate)
- **View function** (external sampling for off-chain or BAF jackpot)

Step 3: Document each winner selection read in a table with columns:
| # | Function | File:Line | Key Used | Purpose | Jackpot Type |

Research identifies 2 winner-selection reads:
1. _awardFarFutureCoinJackpot at JM:2543 using _tqFarFutureKey(candidate) -- FF Coin
2. sampleFarFutureTickets at DG:2681 using _tqWriteKey(candidate) -- BAF indirect

INDEPENDENTLY VERIFY these line numbers against the actual code. Line numbers may have drifted.

Step 4: Also enumerate non-winner-selection reads in a separate table for completeness (processing, length checks, view functions). This demonstrates exhaustive coverage.

Step 5: For each read site, document:
- The exact Solidity line with a short code quote
- Which key space it reads from (_tqFarFutureKey, _tqReadKey, or _tqWriteKey)
- Why that key space is correct (or incorrect -- flag as [DISCREPANCY] if wrong)
- Which jackpot type consumes the result

Use the 2-letter module abbreviation format established in Phase 81: JM = JackpotModule, DG = DegenerusGame, AM = AdvanceModule, GS = GameStorage, MM = MintModule, EM = EndgameModule, LM = LootboxModule.
  </action>
  <verify>
    <automated>grep -c 'ticketQueue' audit/v4.0-ticket-consumption-winner-selection.md | xargs test 5 -le</automated>
  </verify>
  <done>
- Section 2 contains a table of ALL ticketQueue reads for winner selection with file:line citations
- Every citation verified against actual Solidity source (not copied from research)
- Non-winner-selection reads documented in a separate completeness table
- Each read site has: function name, file:line, key used, purpose, jackpot type
  </done>
</task>

<task type="auto">
  <name>Task 2: Enumerate all traitBurnTicket reads for winner selection (TCON-02)</name>
  <read_first>
    - contracts/modules/DegenerusGameJackpotModule.sol (search for "traitBurnTicket" -- all references)
    - contracts/DegenerusGame.sol (search for "traitBurnTicket" -- view functions)
    - contracts/storage/DegenerusGameStorage.sol (search for "traitBurnTicket" -- storage slot definition)
    - audit/v4.0-ticket-consumption-winner-selection.md (current state from Task 1)
  </read_first>
  <files>audit/v4.0-ticket-consumption-winner-selection.md</files>
  <action>
Add Section 3 to the audit document. CRITICAL: Do NOT reproduce the research -- independently grep and read the Solidity source.

**Section 3: traitBurnTicket Reads for Winner Selection (TCON-02)**

Step 1: Grep for ALL traitBurnTicket read sites:
```
grep -rn 'traitBurnTicket' contracts/
```

Step 2: For each result, classify as:
- **Winner selection read** (function uses traitBurnTicket contents to pick jackpot winner)
- **Write** (processTicketBatch writing traits -- exclude from this section)
- **Length check** (eligibility/bucket sizing -- include, these gate winner selection)
- **View function** (external sampling for off-chain or BAF)

Step 3: Document each winner selection and length-check read in a table:
| # | Function | File:Line | Purpose | Jackpot Type |

Research identifies 14 read sites across JackpotModule and DegenerusGame. INDEPENDENTLY VERIFY each line number.

Key reads to verify:
- _randTraitTicket at JM:2244 (core helper -- `holders = traitBurnTicket_[trait]`)
- _randTraitTicketWithIndices at JM:2294 (core helper with indices)
- awardFinalDayDgnrsReward at JM:785 (DGNRS reward)
- _runEarlyBirdLootboxJackpot at JM:833 (earlybird)
- _hasTraitTickets at JM:1040 (eligibility check)
- _distributeTicketsToBucket at JM:1191 (ticket jackpot)
- _computeBucketCounts at JM:1231 (bucket sizing)
- _processDailyEthChunk at JM:1448 (daily ETH)
- _processJackpotBucket at JM:1641 (early-burn ETH)
- _awardDailyCoinToTraitWinners at JM:2459 (daily coin near-future)
- _hasActualTraitTickets at JM:2680 (carryover eligibility)
- sampleTraitTickets at DG:2618 (view function)
- sampleTraitTicketsAtLevel at DG:2647 (view function for BAF)
- getTickets at DG:2730 (view function for UI)

Step 4: For each read site, document:
- The exact Solidity line with a short code quote
- Whether it reads by trait index (traitBurnTicket[lvl][trait]) or iterates all traits
- Which helper function it delegates to (_randTraitTicket or _randTraitTicketWithIndices)
- Which jackpot type ultimately uses this read
- Whether deity virtual entries apply (only for _randTraitTicket and _randTraitTicketWithIndices calls)

Step 5: Document the two core winner selection helpers in detail:
- _randTraitTicket (JM:2237-2280): parameters, deity virtual entry logic, effectiveLen calculation, entropy derivation, bit rotation for multiple winners
- _randTraitTicketWithIndices (JM:2283-2337): same as above plus ticketIndex return value

Include the full Solidity signature and key lines for each helper.
  </action>
  <verify>
    <automated>grep -c 'traitBurnTicket' audit/v4.0-ticket-consumption-winner-selection.md | xargs test 10 -le</automated>
  </verify>
  <done>
- Section 3 contains a table of ALL traitBurnTicket reads for winner selection with file:line citations
- Every citation verified against actual Solidity source
- Two core winner selection helpers (_randTraitTicket, _randTraitTicketWithIndices) documented in detail
- Each read site has: function name, file:line, purpose, jackpot type, whether deity virtual entries apply
- Length checks and view functions included with appropriate classification
  </done>
</task>

</tasks>

<verification>
- `grep -c 'JM:' audit/v4.0-ticket-consumption-winner-selection.md` returns >= 15 (file:line citations to JackpotModule)
- `grep -c 'DG:' audit/v4.0-ticket-consumption-winner-selection.md` returns >= 3 (file:line citations to DegenerusGame)
- `grep -c 'ticketQueue' audit/v4.0-ticket-consumption-winner-selection.md` returns >= 5
- `grep -c 'traitBurnTicket' audit/v4.0-ticket-consumption-winner-selection.md` returns >= 10
- `grep -c '_randTraitTicket' audit/v4.0-ticket-consumption-winner-selection.md` returns >= 5
- Document has Section 1 (Executive Summary), Section 2 (ticketQueue), Section 3 (traitBurnTicket)
</verification>

<success_criteria>
- Every ticketQueue read for winner selection has a file:line citation verified against Solidity source
- Every traitBurnTicket read for winner selection has a file:line citation verified against Solidity source
- Non-winner-selection reads enumerated separately for completeness
- Core winner selection helpers documented with parameter signatures and key logic
- Document follows v4.0 audit format established in Phase 81
</success_criteria>

<output>
After completion, create `.planning/phases/83-ticket-consumption-winner-selection/83-01-SUMMARY.md`
</output>
