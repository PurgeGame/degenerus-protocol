---
phase: 349-impl-the-one-carefully-sequenced-batched-contract-diff-code-
plan: 03
type: execute
wave: 3
depends_on: ["349-02"]
files_modified:
  - contracts/modules/GameAfkingModule.sol
autonomous: false
requirements: [ARCH-02, BOX-02, BOX-03, REVERT-01, REVERT-02, CONSENT-01, CONSENT-02]
must_haves:
  truths:
    - "A new GameAfkingModule (delegatecall module on the GAME_*_MODULE pattern, inherits DegenerusGameStorage) is authored — its bytecode is its OWN budget, 0 B to the Game (ARCH-02)"
    - "subscribe + the 4 setters carry the CONSENT-01 gates VERBATIM: the SUB-02 self-consent isOperatorApproved(subscriber, msg.sender) read + the OPENE-04 non-zero/non-self fundingSource isOperatorApproved(fundingSource, subscriber) gate; the AFSUB validThroughLevel = lazyPassHorizon(subscriber) write; the VAULT/SDGNRS exemption stays keyed on player; the funder=src resolution (src = fundingSource == 0 ? player : fundingSource)"
    - "The process STAGE callee folds _resolveBuy's slice-builder validation invariants VERBATIM (REVERT-01): ev = cost - claimableUse + enum payKind, the 1-wei claimable sentinel, the LOOTBOX_MIN transient skip, quantity >= 1 — so the funded buy is revert-free by construction (the SOLE no-brick guarantor under the no-valve model)"
    - "The cost-unit fold preserves the dual TICKET_SCALE: amount = effectiveQty * 400 (AfKing TICKET_SCALE) AND the Game's / (4 * 100) divisor (Storage TICKET_SCALE = 100) so cost stays mp * effectiveQty — the two named constants (400 vs 100) are NOT collapsed into one symbol"
    - "The process pass STAMPS the 5-field (index, amount, day, scorePlus1, baseLevelPlus1) per process-day as one warm-dirty write, overwritten each cycle — with NO cold lootboxEth*/lootboxPurchasePacked/boxPlayers.push (BOX-02); boons OFF ⇒ amount = spend"
    - "The process pass debits afkingFunding and sets lastAutoBoughtDay == today ONLY AFTER a successful debit (a failed/skipped buy writes no marker → no free box; a wallet subscribing between process and open has no this-cycle marker → no free box) (BOX-03)"
    - "There is NO try/catch valve (D-348-04 / REVERT-02 no-valve form); the slice builder pre-emptively declines an unbuildable slice (the LOOTBOX_MIN transient skip — sub STAYS, retries); rule-(1) unfunded eviction is unaffected; the per-cycle eviction cap is DROPPED"
    - "Set-mutation preserves no cursor advance after swap-pop (the H-CANCEL-SWAP-MISS / cancel-tombstone-streak class) and the tombstone-then-reclaim shape (CONSENT-02)"
    - "The process pass NEVER routes through _callTicketPurchase's lootbox EV tally (MintModule:1303/1327) — it stamps only; the single EV RMW happens at open (the double-draw guard, EVCAP-01 producer)"
  artifacts:
    - path: "contracts/modules/GameAfkingModule.sol"
      provides: "subscribe + setters (CONSENT-01 gates) + the required-path process STAGE callee (REVERT-01 slice fold, BOX-02 stamp, BOX-03 debit+marker, CONSENT-02 set-mutation)"
      contains: "function subscribe"
  key_links:
    - from: "GameAfkingModule.subscribe"
      to: "GAME.isOperatorApproved + lazyPassHorizon (now in-context, same-contract reads)"
      via: "the CONSENT-01 OPEN-E gate + AFSUB validThroughLevel write carried verbatim"
      pattern: "isOperatorApproved"
    - from: "the process STAGE callee"
      to: "afkingFunding[src] debit then Sub.lastAutoBoughtDay = today"
      via: "BOX-03 success-marker only after a successful debit"
      pattern: "lastAutoBoughtDay"
    - from: "the process STAGE slice builder"
      to: "the Game-side revert primitives it must avoid (Game:976/985/1003/1006, Storage:843)"
      via: "REVERT-01 verbatim invariants (1-wei sentinel, ev=cost-claimableUse, enum payKind, LOOTBOX_MIN skip)"
      pattern: "claimableUse"
---

<objective>
Step 3 (part A) of the producer-before-consumer edit-order map (348-IMPL-EDIT-ORDER-MAP.md Section 3 Step 3):
author the new `GameAfkingModule` (delegatecall module, inherits `DegenerusGameStorage`) and land its
SUBSCRIBE + SETTERS + the REQUIRED-PATH PROCESS STAGE CALLEE. This is the logic producer the AdvanceModule STAGE
(349-05) calls and the interfaces (349-05) declare; it must exist before them. The OPEN-PASS + ROUTER + bounty
are part B (349-04, same file).

The process STAGE callee is the load-bearing fold: it folds `_resolveBuy`'s slice-builder validation invariants
VERBATIM (REVERT-01 — the SOLE no-brick guarantor under the D-348-04 no-valve model), stamps the D-348-07
5-field box stamp (BOX-02), debits `afkingFunding` + sets the success-marker only after a successful debit
(BOX-03), and carries the CONSENT-01/02 OPEN-E + set-mutation semantics verbatim. NO try/catch valve.

Purpose: ARCH-02 (the module owns the logic) + REVERT-01 (migration fidelity) + REVERT-02 (no-valve, the
process-leg half) + BOX-02/03 (the warm-dirty stamp + the debit-gated marker) + CONSENT-01/02. The module's
bytecode is its OWN budget (0 B to the Game), so the running-total is unaffected by this step.

Output: A new `contracts/modules/GameAfkingModule.sol` with subscribe/setters + the process STAGE callee (the
open-pass + router land in 349-04). Part of the single batched 349 diff. NO COMMIT.

⚠ CONTRACT-BOUNDARY HARD STOP. `autonomous: false`. The executor applies the edits but NEVER runs `git commit` /
`git add contracts/` (the only action needing approval is committing `contracts/*.sol`). The orchestrator owns
all commits, deferred past the single user-approval gate (349-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-IMPL-EDIT-ORDER-MAP.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-INVARIANT-CARRY.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-FREEZE-PROOF.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-SPEC-INDEX.md
@.planning/phases/349-impl-the-one-carefully-sequenced-batched-contract-diff-code-/349-01-SUMMARY.md

<interfaces>
<!-- Inherit 349-01 Task 1's re-pinned anchors; SPEC-attested refs @ 20ca1f79 below. The logic FOLDS from AfKing.sol into the module. -->

The delegatecall-module pattern to mirror (its bytecode is its own budget):
- the GAME_BINGO_MODULE / claimBingo lane `DegenerusGame.sol:328-343` (selector + abi.encodeWithSelector + delegatecall); GameAfkingModule is a peer GAME_*_MODULE that inherits DegenerusGameStorage

subscribe + setters (FOLD from AfKing.sol — CONSENT-01 carried verbatim):
- `subscribe(player, drainGameCreditFirst, useTickets, dailyQuantity, reinvestPct, fundingSource)` `:324-331`
- SUB-02 self-consent read `:338` — `if (!GAME.isOperatorApproved(subscriber, msg.sender)) { ... }`
- OPENE-04 gate `:343-352` — `fundingSource != address(0) && fundingSource != subscriber && !GAME.isOperatorApproved(fundingSource, subscriber) → revert NotApproved()`
- AFSUB validThroughLevel write `:371` — `s.validThroughLevel = uint32(GAME.lazyPassHorizon(subscriber));`
- `dailyQuantity == 0` revert (quantity >= 1) `:332`
- setters: `setDailyQuantity` `:392`, `setDrainGameCreditFirst` `:405`, `setMode` `:414`, `setReinvestPct` `:423`

The _resolveBuy slice builder (FOLD VERBATIM — REVERT-01; body :727-863):
- `effectiveQty = sub.dailyQuantity` `:756`; reinvest bump `:758-759` (effectiveQty = max(dailyQuantity, reinvestQty), >= 1)
- ticket `amount = effectiveQty * TICKET_SCALE` `:770` where AfKing TICKET_SCALE = 400 `:238`
- `cost = mp * effectiveQty` `:761`
- LOOTBOX_MIN transient skip `:772-774` (`if (cost < LOOTBOX_MIN) { lootboxSkip = true; return (...); }`; decl :269)
- 1-wei claimable sentinel `:790` (`if (claimable > 0 && claimableUse >= claimable) claimableUse = claimable - 1;`)
- `ev = cost - claimableUse` `:791` (`ethValue = cost - claimableUse;`)
- enum payKind `:792-794` (`MintPaymentKind` enum decl :9; ev==0 ? Claimable : claimableUse==0 ? DirectEth : Combined)

funder + set-mutation (CONSENT carried):
- funder resolution `:624` — `address src = sub.fundingSource == address(0) ? player : sub.fundingSource;` (VAULT/SDGNRS exemption stays on `player`, comment :620-623)
- the per-iteration `currentLevel > validThroughLevel` crossing `:571` (pass-gating eviction/refresh)
- the success-marker write `:676` — `sub.lastAutoBoughtDay = today;` (today's AFTER-debit shape — the BOX-03 model)
- the swap-pop tombstone bookkeeping (set membership ⟺ _subscriberIndex; "no cursor advance after swap-pop" — AFSUB-05 / SUB-07 cancel-tombstone, the H-CANCEL-SWAP-MISS class)

The Game-side revert set the slice MUST avoid (348-INVARIANT-CARRY §1 / §5): Game:976 (Claimable claimable<=amount), Game:985/1003/1006 (fund-checks / unknown payKind), Storage:843 (basis<=shortfall settle), MintModule:1057 (< LOOTBOX_MIN), batchPurchase:1922 (enum-cast Panic 0x21).

The dual-TICKET_SCALE subtlety (348-INVARIANT-CARRY §3-i, LOAD-BEARING): AfKing TICKET_SCALE = 400 (`:238`) == Game's 4 * (Storage TICKET_SCALE = 100, `:166`); amount = effectiveQty * 400 AND the Game divisor / (4 * 100) compose so cost = mp * effectiveQty. Different named constants — do NOT collapse into one symbol.

D-348-04 no-valve (348-INVARIANT-CARRY §2): NO try/catch. Healthy path revert-free-by-construction (obl 1); class B solvency FAILS LOUD (must propagate, never masked); class C liveness terminal. Rule-(2) becomes a PRE-EMPTIVE skip (LOOTBOX_MIN — sub STAYS); the per-cycle eviction cap is DROPPED; rule-(1) unfunded eviction unaffected.

Double-draw guard (348-INVARIANT-CARRY §3-iii): the process pass stamps only; it does NOT route through _callTicketPurchase (MintModule:1496) nor the buy-time EV writes (MintModule:1303/1327) — the single EV RMW is at open (349-04).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold GameAfkingModule + fold subscribe + the 4 setters with the CONSENT-01 OPEN-E gates verbatim</name>
  <files>contracts/modules/GameAfkingModule.sol</files>
  <read_first>
    - contracts/AfKing.sol (subscribe :324-352 incl. SUB-02 :338 + OPENE-04 :343-352 + dailyQuantity==0 :332 + validThroughLevel write :371; setters :392/:405/:414/:423)
    - contracts/modules/DegenerusGameBingoModule.sol (a peer GAME_*_MODULE that inherits DegenerusGameStorage — the module skeleton/imports/pragma to mirror)
    - 348-IMPL-EDIT-ORDER-MAP.md §1.2 (the module surface table: subscribe + setters folded from AfKing :324/:392/:405/:414/:423, OPEN-E gates carried) + §3 Step 3a
    - 348-SPEC-INDEX.md §6 (the CONSENT carry-over table + the OPEN-E 4-protection re-attestation — consent-gate-at-subscribe / default-self / no-escalation / trust-the-sub)
  </read_first>
  <action>
    Create `contracts/modules/GameAfkingModule.sol` as a new delegatecall module that inherits
    `DegenerusGameStorage` (so it reads/writes the 349-02 appended set + stamp in-context), mirroring the skeleton
    of an existing `GAME_*_MODULE` (pragma, SPDX, imports, the inheritance). Fold `subscribe` (the 6-param signature
    from AfKing `:324-331`) + the 4 setters (`setDailyQuantity` `:392`, `setDrainGameCreditFirst` `:405`, `setMode`
    `:414`, `setReinvestPct` `:423`) into the module, operating on `_subOf[...]` directly. Carry the CONSENT-01
    gates VERBATIM (the relocation moves the logic; it does NOT change the consent semantics): the SUB-02
    self-consent `isOperatorApproved(subscriber, msg.sender)` read (`:338`), the OPENE-04 non-zero/non-self
    `fundingSource` gate (`:343-352` — `fundingSource != address(0) && fundingSource != subscriber &&
    !isOperatorApproved(fundingSource, subscriber) → revert`), the `dailyQuantity == 0` revert (`:332`, the
    quantity >= 1 floor), and the AFSUB `validThroughLevel = uint32(lazyPassHorizon(subscriber))` write (`:371`).
    Because the module is in-context, `GAME.isOperatorApproved`/`GAME.lazyPassHorizon` become same-contract reads —
    keep the SAME gate, the SAME semantics (re-attest the OPEN-E 4-protection: consent-gate-at-subscribe /
    default-self [`fundingSource == 0` resolves to `player`] / no-escalation [gate at subscribe, source not
    changeable to escalate] / trust-the-sub temporal bound). Do NOT author the process STAGE / open / router yet
    (Tasks 2/3 here + 349-04). Comments describe current state only.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && M=contracts/modules/GameAfkingModule.sol; test -f $M && grep -qE "DegenerusGameStorage" $M && grep -c "function subscribe" $M | grep -qx 1 && grep -c "isOperatorApproved" $M | grep -qE "^[1-9]" && grep -c "lazyPassHorizon" $M | grep -qx 1 && grep -c "function setDailyQuantity\|function setDrainGameCreditFirst\|function setMode\|function setReinvestPct" $M | grep -qx 4 && echo "MODULE+SUBSCRIBE+SETTERS+OPENE"</automated>
  </verify>
  <acceptance_criteria>
    - `contracts/modules/GameAfkingModule.sol` exists, inherits `DegenerusGameStorage`, mirrors the GAME_*_MODULE pattern
    - `subscribe` (1 occurrence) carries the SUB-02 self-consent read + the OPENE-04 fundingSource gate + the `dailyQuantity == 0` revert + the `validThroughLevel = lazyPassHorizon(...)` write
    - All 4 setters (`setDailyQuantity`/`setDrainGameCreditFirst`/`setMode`/`setReinvestPct`) are present
    - The OPEN-E 4-protection is preserved (default-self resolves `fundingSource == 0` to `player`; the gate is at subscribe; no escalation path) — verbatim carry, not a rewrite
  </acceptance_criteria>
  <done>`GameAfkingModule.sol` is scaffolded (inherits DegenerusGameStorage), with subscribe + the 4 setters folded carrying the CONSENT-01 OPEN-E gates + the AFSUB validThroughLevel write verbatim; the OPEN-E 4-protection is re-attested.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fold the _resolveBuy slice-builder invariants VERBATIM into the process STAGE callee (REVERT-01) with the dual-TICKET_SCALE cost-unit preserved</name>
  <files>contracts/modules/GameAfkingModule.sol</files>
  <read_first>
    - contracts/AfKing.sol (_resolveBuy :727-863 — effectiveQty :756/:758-759, amount = effectiveQty*TICKET_SCALE :770, cost = mp*effectiveQty :761, LOOTBOX_MIN skip :772-774 + decl :269, 1-wei sentinel :790, ev = cost-claimableUse :791, enum payKind :792-794 + enum decl :9, TICKET_SCALE=400 :238)
    - contracts/storage/DegenerusGameStorage.sol (TICKET_SCALE = 100 :166 — the Game divisor base; the Game-side revert primitives the slice avoids)
    - 348-INVARIANT-CARRY.md §1 (the 6 obligation-1 invariants + the Game-side revert set each guards) + §3-i (the dual-TICKET_SCALE: 400 vs 4*100 — do NOT collapse) + §5 (the /contract-auditor PASS: all 5 invariants stated correctly for the fold)
    - 348-IMPL-EDIT-ORDER-MAP.md §4.2 (REVERT-02 no-valve; obligation 1 is the SOLE no-brick guarantor; the pre-emptive LOOTBOX_MIN skip)
  </read_first>
  <action>
    Author the REQUIRED-PATH PROCESS STAGE CALLEE in `GameAfkingModule.sol` (the per-funded-sub logic the
    AdvanceModule STAGE will call across the cursor, 349-05). FOLD the `_resolveBuy` slice-builder validation
    invariants VERBATIM (REVERT-01 — this is migration fidelity, the SOLE no-brick guarantor under the no-valve
    model): `effectiveQty = max(dailyQuantity, reinvestQty)` with the `dailyQuantity >= 1` subscribe-time floor and
    the reinvest bump (`:756`/`:758-759`); `cost = mp * effectiveQty` (`:761`); the `LOOTBOX_MIN` transient skip
    (`:772-774` — `if (cost < LOOTBOX_MIN) { mark skip; return; }`, sub STAYS and retries, NO Game call — carry
    `LOOTBOX_MIN`, decl `:269`); the 1-wei claimable sentinel (`:790` — `if (claimable > 0 && claimableUse >=
    claimable) claimableUse = claimable - 1;`, leaving `claimable > cost` / `basis > shortfall`); `ev = cost -
    claimableUse` (`:791`) with `claimableUse` capped in `[0, cost]`; and the enum-typed `payKind` (`:792-794`,
    `MintPaymentKind` enum, ev==0 ? Claimable : claimableUse==0 ? DirectEth : Combined) — keep payKind ENUM-TYPED
    end-to-end (never a raw out-of-range uint8). ⚠ Preserve the DUAL TICKET_SCALE (§3-i, LOAD-BEARING): ticket
    `amount = effectiveQty * 400` (AfKing's `TICKET_SCALE`, `:238`) AND the Game's `/ (4 * 100)` divisor (Storage
    `TICKET_SCALE = 100`, `:166`) so `cost` stays `mp * effectiveQty` — do NOT naively reuse one `TICKET_SCALE`
    symbol for both roles (a constant-collision a verbatim fold can silently break). These invariants make the
    funded buy revert-free by construction (they structurally avoid Game:976/985/1003/1006, Storage:843,
    MintModule:1057, batchPurchase:1922). ⚠ Author NO try/catch valve (REVERT-02 no-valve, D-348-04): the only
    pre-emptive skip is the `LOOTBOX_MIN` transient (rule (2) is pre-emptive, not a revert-catch); rule-(1)
    unfunded eviction is a separate pre-buy decision (unaffected); the per-cycle eviction cap is DROPPED.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && M=contracts/modules/GameAfkingModule.sol; grep -qE "MintPaymentKind" $M && grep -qE "LOOTBOX_MIN" $M && grep -qE "claimableUse" $M && grep -qE "\* 400|TICKET_SCALE" $M && PROSE=$(grep -ic "try {" $M); test "$PROSE" = "0" && grep -ic "catch" $M | grep -qx 0 && echo "SLICE-FOLD NO-TRYCATCH"</automated>
  </verify>
  <acceptance_criteria>
    - The process STAGE callee folds the 5 obligation-1 invariants (effectiveQty>=1, cost=mp*effectiveQty, LOOTBOX_MIN transient skip, 1-wei sentinel, ev=cost-claimableUse, enum payKind) — REVERT-01 verbatim
    - `payKind` is ENUM-TYPED (`MintPaymentKind`); the slice structurally avoids the Game-side revert set
    - The dual-TICKET_SCALE is preserved (amount uses 400; the Game divisor uses 4*100) — NOT collapsed into one symbol
    - NO `try {` / `catch` appears in the module (REVERT-02 no-valve, D-348-04); the LOOTBOX_MIN skip is the only pre-emptive skip; no per-cycle eviction cap
  </acceptance_criteria>
  <done>The process STAGE callee folds the _resolveBuy slice-builder invariants verbatim (REVERT-01), preserves the dual-TICKET_SCALE cost-unit, and contains NO try/catch (REVERT-02 no-valve); the funded buy is revert-free by construction.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Stamp the 5 fields warm-dirty (BOX-02), debit afkingFunding then set the success-marker after the debit (BOX-03), carry the set-mutation no-cursor-advance-after-swap-pop (CONSENT-02)</name>
  <files>contracts/modules/GameAfkingModule.sol</files>
  <read_first>
    - contracts/AfKing.sol (the funder resolution :624; the success-marker write today :676 `sub.lastAutoBoughtDay = today;`; the validThroughLevel crossing :571; the swap-pop tombstone bookkeeping + AFSUB-05/SUB-07 cancel-tombstone comments ~:538/:557; the lastAutoBoughtDay >= today idempotency check :557)
    - contracts/storage/DegenerusGameStorage.sol (the appended Sub stamp fields from 349-02; afkingFunding :410 inside claimablePool; the invariant :348)
    - 348-IMPL-EDIT-ORDER-MAP.md §1.1 (the stamp fields + markers) + §3 Step 3a (stamp (index, amount, day, scorePlus1, baseLevelPlus1) at process; debit afkingFunding; set lastAutoBoughtDay) + §4.4 (double-draw guard: stamp only, never MintModule:1303/1327)
    - 348-SPEC-INDEX.md §6 (CONSENT-02: no cursor advance after swap-pop — H-CANCEL-SWAP-MISS; tombstone-then-reclaim)
    - 348-FREEZE-PROOF.md FREEZE-02 (b) (read LR_INDEX ONCE at pass start — the uniform epoch; stamp every sub to that same value) — note the LR_INDEX read-once is the AdvanceModule STAGE's job (349-05); the callee receives/uses the epoch index
  </read_first>
  <action>
    In the process STAGE callee, after the slice resolves (Task 2), STAMP the D-348-07 5-field stamp into
    `_subOf[player]` (BOX-02): `index` = the pre-RNG `LR_INDEX` epoch value (read ONCE at pass start by the
    AdvanceModule STAGE, 349-05 — the callee uses the passed/shared epoch index, NOT a per-sub re-read), `amount` =
    the stamped spend (boons OFF ⇒ `amount` = spend exactly, no boosted-amount field), `day` = the boundary-pinned
    process day, `scorePlus1` = activityScore + 1 (computed + FROZEN at process), `baseLevelPlus1` = baseLevel + 1
    (computed + FROZEN at process) — ONE warm-dirty write per process-day, overwritten each cycle. ⚠ Write NO cold
    `lootboxEth*` / `lootboxPurchasePacked` / `boxPlayers.push` (BOX-02 — the warm Sub-stamp REPLACES the cold box
    ledger). Then DEBIT `afkingFunding[src]` (where `src = sub.fundingSource == address(0) ? player :
    sub.fundingSource`, the `:624` funder resolution; the VAULT/SDGNRS exemption stays keyed on `player`, never
    `src`) and set `sub.lastAutoBoughtDay = today` as the success-marker ONLY AFTER the debit succeeds (BOX-03 — a
    failed/skipped buy writes no marker → no free box; a wallet that subscribes between this process pass and the
    open has no this-cycle marker → no free box; preserve the `lastAutoBoughtDay >= today` idempotency check
    `:557`). ⚠ The debit moves `claimablePool` in tandem (SOLVENCY-01 — the `claimablePool -= uint128(...)` site
    FAILS LOUD on an underflow, never caught: that is class B, must propagate). ⚠ DOUBLE-DRAW GUARD (EVCAP-01
    producer): the process pass STAMPS only — it does NOT route through `_callTicketPurchase` (`MintModule:1496`)
    nor touch the buy-time EV writes (`MintModule:1303`/`:1327`); the single EV RMW is at open (349-04). For
    set-mutation (CONSENT-02): when the `validThroughLevel` crossing (`:571`) or a cancel evicts a sub, preserve
    "NO CURSOR ADVANCE AFTER SWAP-POP" (a relocated tail must not be skipped behind `_subCursor` — the
    H-CANCEL-SWAP-MISS / cancel-tombstone-streak class) and the tombstone-then-reclaim shape (`_subscriberIndex`
    membership bookkeeping). Comments describe current state only.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && M=contracts/modules/GameAfkingModule.sol; grep -qE "afkingFunding" $M && grep -qE "lastAutoBoughtDay" $M && grep -qE "scorePlus1" $M && grep -qE "baseLevelPlus1" $M && grep -c "boxPlayers.push" $M | grep -qx 0 && grep -c "lootboxPurchasePacked" $M | grep -qx 0 && grep -c "_callTicketPurchase" $M | grep -qx 0 && echo "STAMP+DEBIT+MARKER NO-COLD-LEDGER NO-DOUBLE-DRAW"</automated>
  </verify>
  <acceptance_criteria>
    - The process pass stamps `index/amount/day/scorePlus1/baseLevelPlus1` (5-field) as a warm-dirty write; boons OFF ⇒ amount = spend (BOX-02)
    - NO `boxPlayers.push` / `lootboxPurchasePacked` / `lootboxEth*` cold-ledger writes in the process pass (BOX-02)
    - `afkingFunding[src]` is debited (src = fundingSource==0 ? player : fundingSource; exemption on player) and `lastAutoBoughtDay = today` is set ONLY after the successful debit (BOX-03)
    - The process pass does NOT call `_callTicketPurchase` nor touch MintModule:1303/1327 (double-draw guard — single EV RMW deferred to open)
    - Set-mutation preserves "no cursor advance after swap-pop" + tombstone-then-reclaim (CONSENT-02)
  </acceptance_criteria>
  <done>The process pass stamps the 5-field warm-dirty (no cold ledger), debits afkingFunding[src] then sets lastAutoBoughtDay after the debit (BOX-03), routes no double-draw, and preserves the swap-pop streak invariant (CONSENT-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subscribe caller → operator-approval | A non-zero/non-self `fundingSource` must be operator-approved at subscribe (OPEN-E); default-self needs no gate. |
| process STAGE → Game-side revert set | The folded slice must be revert-free by construction; under the no-valve model a mis-stated invariant bricks the day. |
| afkingFunding debit → claimablePool | A debit underflow signals a SOLVENCY-01 violation — it MUST fail loud (never be caught/masked). |
| swap-pop eviction → mint-streak | A cursor advancing past a swap-popped tail re-introduces the H-CANCEL-SWAP-MISS missed-day streak reset. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-349-03-BRICK | Denial of Service (day-brick) | the process STAGE slice fold | mitigate | REVERT-01 folds the 5 obligation-1 invariants VERBATIM (the SOLE no-brick guarantor under no-valve); Task 2 verify asserts the invariants + NO try/catch; the dual-TICKET_SCALE is preserved so `cost` cannot drift into a Game-side revert. (The light /contract-auditor obligation-1 pass returned PASS, 348-INVARIANT-CARRY §5.) |
| T-349-03-VALVE | Repudiation (masked bug) | a stray try/catch | mitigate | D-348-04 DROPS the valve; Task 2 verify asserts 0 `try`/`catch`. Class B solvency underflow must propagate (fail loud), never be masked; rule-(2) is a pre-emptive LOOTBOX_MIN skip, not a revert-catch. |
| T-349-03-FREEBOX | Spoofing (free box) | the BOX-03 success-marker | mitigate | `lastAutoBoughtDay = today` is set ONLY after a successful `afkingFunding` debit; a failed/skipped buy or a between-pass subscribe leaves no marker → no free box. Task 3 verify asserts the marker + the debit. |
| T-349-03-OPENE | Elevation (fund someone's sub without consent) | the OPENE-04 subscribe gate | mitigate | CONSENT-01 carries the gate verbatim; the OPEN-E 4-protection (consent-gate-at-subscribe / default-self / no-escalation / trust-the-sub) is re-attested. Task 1 verify asserts the `isOperatorApproved` gate. |
| T-349-03-SWAP | Denial (missed-day streak reset) | swap-pop eviction | mitigate | CONSENT-02 preserves "no cursor advance after swap-pop" + tombstone-then-reclaim (the H-CANCEL-SWAP-MISS class); 351 TST-04 proves the streak-preserved swap-pop NEGATIVE-VERIFIED. |
| T-349-03-DBL | Spoofing (double EV draw) | the buy-time EV tally | mitigate | The double-draw guard: the process pass stamps only, never routing through `_callTicketPurchase`/MintModule:1303/1327; the single EV RMW is at open (349-04). Task 3 verify asserts no `_callTicketPurchase`. |
| T-349-03-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs (Solidity edits only). |
</threat_model>

<verification>
- GameAfkingModule exists, inherits DegenerusGameStorage; subscribe + 4 setters carry the CONSENT-01 OPEN-E gates verbatim
- The process STAGE callee folds the _resolveBuy invariants verbatim (REVERT-01); NO try/catch (REVERT-02 no-valve); dual-TICKET_SCALE preserved
- The process pass stamps the 5-field warm-dirty (no cold ledger, BOX-02), debits then sets the marker (BOX-03), no double-draw, swap-pop streak preserved (CONSENT-02)
- NO `git commit` / `git add contracts/` was run by the executor (orchestrator commits post-approval)
</verification>

<success_criteria>
- The GameAfkingModule owns subscribe/setters + the process STAGE callee (ARCH-02; its bytecode is its own budget)
- REVERT-01 verbatim fold + REVERT-02 no-valve (process leg) + BOX-02 warm-dirty stamp + BOX-03 debit-gated marker + CONSENT-01/02 are landed
- The double-draw guard holds (stamp only; single EV RMW deferred to open in 349-04)
- Edits held at the contract-commit boundary (no executor commit)
</success_criteria>

<output>
Create `.planning/phases/349-impl-the-one-carefully-sequenced-batched-contract-diff-code-/349-03-SUMMARY.md` when done.
</output>
