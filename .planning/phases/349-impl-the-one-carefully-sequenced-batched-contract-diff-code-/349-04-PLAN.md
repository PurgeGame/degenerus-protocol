---
phase: 349-impl-the-one-carefully-sequenced-batched-contract-diff-code-
plan: 04
type: execute
wave: 4
depends_on: ["349-03"]
files_modified:
  - contracts/modules/GameAfkingModule.sol
autonomous: false
requirements: [BOX-01, BOX-04, BOX-05, EVCAP-01, PLACE-02]
must_haves:
  truths:
    - "The open-pass (post-RNG) materializes the afking box from the Sub stamp + lootboxRngWordByIndex[stamp.index] with draw math BYTE-IDENTICAL to openLootBox — the seed is keccak256(abi.encode(rngWord, player, day, amount)) (abi.encode, NOT abi.encodePacked — the packed form is the PRESALE box LB:644, the WRONG pattern) seeded from the STAMPED day (FREEZE-03), never open-time _simulatedDayIndex()"
    - "The open reads score+baseLevel FROM the stamp (scorePlus1/baseLevelPlus1, FROZEN — D-348-07), NOT a live recompute; the benign open-time level/currentDay dependence of targetLevel is kept identical to openLootBox so ticket placement/value does not drift (BOX-04)"
    - "lastOpenedIndex is monotonic per sub — the open only materializes when stamp.index > lastOpenedIndex (no double-open); BOX-01: boons OFF ⇒ box amount = spend (no boosted-amount freeze field)"
    - "The afking open routes the box through _applyEvMultiplierWithCap(player, level+1, amount, mult) exactly ONCE at open (read+write at open, keyed [player][level+1], same map as MintModule's buy-time write), hard-clamped <=10 ETH → no revert, fed the FROZEN evMultiplierBps (from the stamped scorePlus1); the buy-time EV write is BYPASSED for afking boxes — no double-draw (EVCAP-01)"
    - "Humans (not in the sub set) keep the existing lootboxEth/boxPlayers openLootBox route unchanged; the two open routes share no mutable-state hazard — the EV-cap map is the one intended shared per-level 10-ETH budget (one RMW each), lootboxRngWordByIndex is read-only/write-once, and the afking route's stamp is producer→consumer ordered by lastAutoBoughtDay/lastOpenedIndex + post-RNG sequencing (BOX-05)"
    - "The router (doWork/autoBuy/autoOpen) dispatches the one-category early-return shape; the open is a NORMAL post-RNG OPEN_BATCH-style router category (NOT folded into advance); the buy/process bounty is work-scaled (OPEN_KNEE pro-rate, not once-per-advance) to resist farm-by-splitting; payment is the deferred BURNIE flip-credit mint (creditFlip) (PLACE-02)"
  artifacts:
    - path: "contracts/modules/GameAfkingModule.sol"
      provides: "the open-pass (BOX-01/04/05 + EVCAP-01 + FREEZE-03 seed) + the router/bounty (PLACE-02)"
      contains: "_applyEvMultiplierWithCap"
  key_links:
    - from: "the afking open-pass"
      to: "lootboxRngWordByIndex[stamp.index] + the abi.encode(rngWord, player, day, amount) seed (stamped day)"
      via: "byte-identical to openLootBox (LB:534, abi.encode, FREEZE-03)"
      pattern: "abi\\.encode\\("
    - from: "the afking open-pass"
      to: "_applyEvMultiplierWithCap(player, level+1, amount, mult) keyed [player][level+1]"
      via: "EVCAP-01 single RMW at open, buy-time write bypassed, frozen evMultiplierBps"
      pattern: "_applyEvMultiplierWithCap"
    - from: "the open router category + the buy/process bounty"
      to: "creditFlip (deferred BURNIE flip-credit) + OPEN_KNEE pro-rate"
      via: "PLACE-02 work-scaled bounty; open stays a post-RNG leg"
      pattern: "creditFlip"
---

<objective>
Step 3 (part B) of the producer-before-consumer edit-order map (348-IMPL-EDIT-ORDER-MAP.md Section 3 Step 3):
finish the `GameAfkingModule` by authoring the OPEN-PASS (the afking-stamp route, §1.3), the ROUTER
(`doWork`/`autoBuy`/`autoOpen`), and the bounty (PLACE-02). This is the consumer of the 349-03 process stamp and
the second open route alongside the human `openLootBox`.

The open-pass is the freeze-critical materialization: the seed is `keccak256(abi.encode(rngWord, player, day,
amount))` (the `abi.encode` form at `LB:534`, NOT the PRESALE `abi.encodePacked`) seeded from the STAMPED day
(FREEZE-03), it reads score+baseLevel FROM the stamp (D-348-07), it does the single EV-cap RMW at open with the
buy-time write bypassed (EVCAP-01), and `lastOpenedIndex` is monotonic (no double-open, BOX-04). Boons are OFF ⇒
box `amount` = spend (BOX-01). Humans keep their existing route (BOX-05, two hazard-free routes).

Purpose: BOX-01 (amount=spend) + BOX-04 (byte-identical materialization + monotonic no-double-open) + BOX-05
(two-route hazard-free) + EVCAP-01 (EV-cap-at-open, buy-time bypassed) + PLACE-02 (work-scaled bounty, post-RNG
open category, creditFlip). The module's bytecode is its OWN budget (0 B to the Game).

Output: The open-pass + router + bounty added to `contracts/modules/GameAfkingModule.sol` (continuing 349-03,
same file). Part of the single batched 349 diff. NO COMMIT.

⚠ CONTRACT-BOUNDARY HARD STOP. `autonomous: false`. The executor applies the edits but NEVER runs `git commit` /
`git add contracts/` (the only action needing approval is committing `contracts/*.sol`). The orchestrator owns
all commits, deferred past the single user-approval gate (349-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-IMPL-EDIT-ORDER-MAP.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-FREEZE-PROOF.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-INVARIANT-CARRY.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-PLACEMENT-DECISION.md
@.planning/phases/349-impl-the-one-carefully-sequenced-batched-contract-diff-code-/349-01-SUMMARY.md

<interfaces>
<!-- Inherit 349-01 Task 1's re-pinned anchors; SPEC-attested refs @ 20ca1f79 below. -->

The human openLootBox route (the byte-identical template the afking open MIRRORS — BOX-04/05):
- `openLootBox(address player, uint48 index)` `LB:503`
- `lootboxRngWordByIndex[index]` read `:510` (write-once by _finalizeLootboxRng :1231 — read-only at open)
- frozen buy-day `lootboxDay[index][player]` `:514` (the FREEZE-03 template; the afking open uses the STAMPED day instead)
- ⚠ the seed `LB:534` — `uint256 seed = uint256(keccak256(abi.encode(rngWord, player, day, amount)));` (abi.encode)
- ⚠ PRESALE box seed `:644` — `abi.encodePacked(rngWord, keccak256("PRESALE_BOX"), player, amount)` — the WRONG pattern; the afking open does NOT copy this
- `_rollTargetLevel(baseLevel, seed)` `:535`/def `:903` (targetLevel; keep the benign open-time level/currentDay dependence identical)
- the human frozen-snapshot read `lootboxPurchasePacked[index][player]` `:529-530` (unpacks scorePlus1/adj/baseLevelPlus1 — the analog the afking open mirrors by reading FROM the Sub stamp)
- open-time _simulatedDayIndex() sites the afking open must NOT copy for the seed day: `:513`/`:766`/`:799`/`:836`/`:868`

The EV-cap helper (EVCAP-01 — the single RMW at open):
- `_applyEvMultiplierWithCap(player, lvl, amount, evMultiplierBps)` `LB:459-495`; READ `:473`; the 10-ETH remaining clamp `:474-476`; the cap-exhausted no-write 100%-EV short-circuit `:478-481`; WRITE `:488`
- the shared map `lootboxEvBenefitUsedByLevel[player][level+1]` `Storage:1469`; cap `LOOTBOX_EV_BENEFIT_CAP = 10 ether` `Storage:1326`
- MintModule's buy-time writes (the ones BYPASSED for afking) `MintModule:1303`/`:1327` (both [buyer][cachedLevel+1])

The router + bounty (FOLD from AfKing + PLACE-02):
- `doWork()` `:864`, `autoBuy(uint256)` `:904`, `autoOpen(uint256)` `:910` (the one-category early-return dispatch)
- the advance bounty mult block `AdvanceModule:226-242` (the 2x/4x/6x day-epoch stall mult the process STAGE rides); OPEN_KNEE pro-rate (pay-for-work; the in-codebase farm-by-splitting answer); `creditFlip` deferred BURNIE flip-credit payment

FREEZE-03 (348-FREEZE-PROOF FREEZE-03 a/b): the afking open seeds the abi.encode form from the STAMPED day; ZERO block.* entropy in the draw (grep-verified ZERO in the module). BOX-05 two-route hazard-free (348-IMPL-EDIT-ORDER-MAP §1.3): shared EV-cap map = one intended per-level 10-ETH budget (one RMW each); lootboxRngWordByIndex read-only/write-once; afking stamp producer→consumer ordered by lastAutoBoughtDay/lastOpenedIndex + post-RNG sequencing.

PLACE-02 (348-PLACEMENT-DECISION §6, NOTED → 349 decides): the open stays a NORMAL post-RNG OPEN_BATCH-style router category (NOT folded into advance); the buy/process bounty work-scaled (OPEN_KNEE pro-rate, not once-per-advance — closes the middle-chunk-unpaid gap, resists farm-by-splitting); payment = deferred creditFlip.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author the afking open-pass — byte-identical materialization (BOX-04), abi.encode stamped-day seed (FREEZE-03), boons OFF amount=spend (BOX-01), monotonic lastOpenedIndex</name>
  <files>contracts/modules/GameAfkingModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameLootboxModule.sol (openLootBox :503-558 — the byte-identical template: rngWord read :510, the abi.encode seed :534, _rollTargetLevel :535/:903, the human frozen-snapshot read :529-530; the PRESALE abi.encodePacked :644 is the WRONG pattern)
    - contracts/modules/GameAfkingModule.sol (the process pass + the Sub stamp from 349-03)
    - 348-FREEZE-PROOF.md FREEZE-03 (a) (the abi.encode seed, ZERO block.* entropy) + (b) (seed from the STAMPED day, never open-time _simulatedDayIndex) + FREEZE-01 (a) (read scorePlus1/baseLevelPlus1 FROM the stamp)
    - 348-IMPL-EDIT-ORDER-MAP.md §1.3 (the afking-stamp open route) + §4.1 (the abi.encode re-pin: LB:534 abi.encode, NOT :644 abi.encodePacked — different hash preimages)
  </read_first>
  <action>
    Author the OPEN-PASS in `GameAfkingModule.sol` (the afking-stamp route). It materializes the box from the Sub
    stamp + `lootboxRngWordByIndex[stamp.index]` with draw math BYTE-IDENTICAL to `openLootBox` (`LB:503-558`).
    ⚠ The seed MUST be `keccak256(abi.encode(rngWord, player, day, amount))` — the `abi.encode` form at `LB:534`
    (32-byte left-padded), NOT `abi.encodePacked` (the PRESALE box `:644` is the only packed seed and is a DIFFERENT
    path; copying it would compute a different preimage → break box-outcome equivalence). Seed the `day` term from
    the STAMPED `day` in the Sub record (FREEZE-03), NEVER open-time `_simulatedDayIndex()` (the `:513`/`:766`/`:799`/
    `:836`/`:868` sites are the ones NOT to copy). Read `rngWord` from `lootboxRngWordByIndex[stamp.index]` (write-once,
    `:510`). Read score + baseLevel FROM the stamp (`scorePlus1`/`baseLevelPlus1`, FROZEN — D-348-07; mirroring the
    human read-from-snapshot of `lootboxPurchasePacked` `:529-530`), NOT a live recompute. Compute `targetLevel` via
    `_rollTargetLevel(baseLevel, seed)` (`:535`/`:903`) keeping the benign open-time `level`/`currentDay` dependence
    IDENTICAL to `openLootBox` so ticket placement/value does not drift (BOX-04). Boons are OFF for afking boxes ⇒
    box `amount` = spend exactly (BOX-01 — there is no boosted-amount freeze field; use the stamped `amount`).
    Enforce `lastOpenedIndex` MONOTONICITY: only materialize when `stamp.index > sub.lastOpenedIndex`, then advance
    `lastOpenedIndex = stamp.index` (no double-open, BOX-04). The EV-cap RMW is Task 2. Comments describe current
    state only.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && M=contracts/modules/GameAfkingModule.sol; grep -qE "keccak256\(abi\.encode\(" $M && grep -c "abi.encodePacked" $M | grep -qx 0 && grep -qE "lootboxRngWordByIndex" $M && grep -qE "lastOpenedIndex" $M && grep -qE "_rollTargetLevel" $M && grep -c "_simulatedDayIndex" $M | grep -qx 0 && echo "OPEN-PASS abi.encode STAMPED-DAY MONOTONIC"</automated>
  </verify>
  <acceptance_criteria>
    - The open seed uses `keccak256(abi.encode(...))` (NOT `abi.encodePacked`); 0 `abi.encodePacked` in the module (FREEZE-03 / §4.1)
    - The seed `day` is the STAMPED day; `_simulatedDayIndex()` is NOT used for the seed (0 occurrences) (FREEZE-03)
    - score+baseLevel are read FROM the stamp (`scorePlus1`/`baseLevelPlus1`), `targetLevel` via `_rollTargetLevel` with the open-time dependence kept identical to openLootBox (BOX-04)
    - boons OFF ⇒ box amount = the stamped spend (BOX-01); `lastOpenedIndex` monotonic (open only if stamp.index > lastOpenedIndex) (BOX-04)
  </acceptance_criteria>
  <done>The open-pass materializes byte-identically to openLootBox (abi.encode stamped-day seed, score/baseLevel read from the stamp, targetLevel dependence kept identical), boons OFF amount=spend, lastOpenedIndex monotonic.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: EV-cap-at-open via _applyEvMultiplierWithCap once, buy-time write bypassed, frozen evMultiplierBps (EVCAP-01); confirm the two open routes are hazard-free (BOX-05)</name>
  <files>contracts/modules/GameAfkingModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameLootboxModule.sol (_applyEvMultiplierWithCap :459-495 — READ :473, the 10-ETH clamp :474-476, the no-write 100%-EV short-circuit :478-481, WRITE :488; resolveLootboxDirect :763 / resolveRedemptionLootbox :796 for the same at-open RMW shape)
    - contracts/storage/DegenerusGameStorage.sol (lootboxEvBenefitUsedByLevel :1469; LOOTBOX_EV_BENEFIT_CAP = 10 ether :1326)
    - contracts/modules/DegenerusGameMintModule.sol (the buy-time EV writes :1303/:1327 — BYPASSED for afking; _callTicketPurchase :1496)
    - 348-INVARIANT-CARRY.md §1 obligation 2 (EV-cap at open, keyed [player][level+1], exactly once, <=10 ETH no-revert, buy-time write bypassed, evMultiplierBps from the FROZEN scorePlus1) + §3-iii (double-draw guard) + §6 (the cap RMW is the SOLE residual live-read — benign monotonic clamp)
    - 348-IMPL-EDIT-ORDER-MAP.md §4.4 (EV-cap-at-open buy-time-bypassed) + §1.3 (BOX-05 shared-no-mutable-state-hazard reconciliation)
    - 348-FREEZE-PROOF.md FREEZE-01 (b) (the EV-cap RMW stays live by structural necessity — a benign monotonic down-clamp, no profitable timing)
  </read_first>
  <action>
    In the open-pass, route the box through `_applyEvMultiplierWithCap(player, level+1, amount, evMultiplierBps)`
    (`LB:459-495`) EXACTLY ONCE at open — the same shape `resolveLootboxDirect`/`resolveRedemptionLootbox` use —
    doing the cap READ (`:473`) + WRITE (`:488`) atomically at open, keyed on the SAME
    `lootboxEvBenefitUsedByLevel[player][level+1]` map (`Storage:1469`) MintModule uses at buy. ⚠ Feed it the FROZEN
    `evMultiplierBps` derived from the stamped `scorePlus1` (D-348-07 — NOT a live score recompute). The cap is
    hard-clamped at `LOOTBOX_EV_BENEFIT_CAP = 10 ether` with a no-write 100%-EV short-circuit once exhausted
    (`:478-481`) ⇒ NO revert (it is NOT a class-B solvency site; the only residual live-read, a benign monotonic
    down-clamp — FREEZE-01b). ⚠ The buy-time EV write (`MintModule:1303`/`:1327`) is BYPASSED for afking boxes — the
    349-03 process pass already stamps instead of routing through `_callTicketPurchase` (`:1496`), so the SINGLE EV
    RMW is here at open (the double-draw guard, §3-iii): exactly one RMW per open, equivalent to the v54
    per-(sub,level) accumulator. Then VERIFY (in the SUMMARY) the two open routes share NO mutable-state hazard
    (BOX-05): the shared EV-cap map is the one intended per-level 10-ETH budget (each route does exactly one RMW via
    the same helper); `lootboxRngWordByIndex` is read-only at open / write-once by `_finalizeLootboxRng` (`:1231`);
    the afking route reads the stamp produced by the process STAGE — strict producer→consumer ordering enforced by
    `lastAutoBoughtDay`/`lastOpenedIndex` + the post-RNG sequencing; the human `openLootBox` route is untouched
    (disjoint per-index state). Comments describe current state only.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && M=contracts/modules/GameAfkingModule.sol; grep -c "_applyEvMultiplierWithCap" $M | grep -qE "^[1-9]" && grep -qE "level \+ 1|level\+1|lvl \+ 1|cachedLevel \+ 1" $M && grep -c "MintModule" $M | grep -qx 0 && grep -c "_callTicketPurchase" $M | grep -qx 0 && echo "EVCAP-AT-OPEN BUY-TIME-BYPASSED"</automated>
  </verify>
  <acceptance_criteria>
    - The open-pass calls `_applyEvMultiplierWithCap(player, level+1, amount, evMultiplierBps)` exactly once per open, keyed [player][level+1], fed the FROZEN evMultiplierBps (from the stamped scorePlus1)
    - The cap is hard-clamped <=10 ETH with the no-write short-circuit → no revert (not a class-B site)
    - The buy-time EV write is bypassed (the module does NOT call `_callTicketPurchase`; no MintModule:1303/1327 path) — single RMW at open, no double-draw
    - The two-route hazard-free reconciliation (shared 10-ETH budget / write-once word / producer→consumer stamp) is recorded in the SUMMARY (BOX-05)
  </acceptance_criteria>
  <done>The open-pass does the single EV-cap RMW at open keyed [player][level+1] with the frozen evMultiplierBps and the buy-time write bypassed (EVCAP-01, no double-draw); the two open routes are confirmed hazard-free (BOX-05).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author the router (doWork/autoBuy/autoOpen) + the PLACE-02 bounty (work-scaled, post-RNG open category, creditFlip)</name>
  <files>contracts/modules/GameAfkingModule.sol</files>
  <read_first>
    - contracts/AfKing.sol (the router: doWork :864, autoBuy :904, autoOpen :910 — the one-category early-return dispatch shape; the existing creditFlip bounty payment)
    - 348-PLACEMENT-DECISION.md §6 (PLACE-02 NOTED → 349 decides: buy/process bounty folds into the advance bounty 2x mult; OPEN_KNEE pro-rate is the farm-by-splitting answer; the open keeps its own separate post-RNG router category) + §3b (open-leg = NORMAL post-RNG OPEN_BATCH-style, NOT folded into advance)
    - 348-IMPL-EDIT-ORDER-MAP.md §1.2 (the router surface) + §5 (PLACE-02 = the bounty fold: 2x*mult advance bounty, OPEN_KNEE pro-rate, farm-by-splitting watch, creditFlip deferred payment)
    - contracts/modules/DegenerusGameAdvanceModule.sol (the advance bounty mult block :226-242 the process STAGE rides — read for the 2x/4x/6x mult shape; the open category cursor from 349-02)
  </read_first>
  <action>
    Author the ROUTER in `GameAfkingModule.sol`: `doWork()` (`:864`), `autoBuy(uint256)` (`:904`), `autoOpen(uint256)`
    (`:910`) — fold the one-category early-return dispatch shape from AfKing (route to the process/open work; revert
    with the no-work sentinel when there is nothing to do). Land the PLACE-02 bounty (NOTED at SPEC, DECIDED here):
    (a) the OPEN stays a NORMAL post-RNG `OPEN_BATCH`-style ROUTER CATEGORY (driven by the open-leg cursor from
    349-02) — it is NOT folded into `advanceGame`; (b) the buy/process bounty is WORK-SCALED (an `OPEN_KNEE`
    pro-rate — pay for work done, not once-per-advance) to close the middle-chunk-unpaid liveness gap and RESIST
    FARM-BY-SPLITTING (many minimal chunks each claiming a flat bounty); the process STAGE rides the advance bounty's
    day-epoch stall `mult` (the 2x/4x/6x block at `AdvanceModule:226-242`); (c) PAYMENT is the DEFERRED BURNIE
    flip-credit mint (`creditFlip`) — NOT a direct transfer, NOT a `mintForGame`. ⚠ Do NOT pull GAS-01/02/03 work in
    here (the aggregate-flush batching + the staticcall→SLOAD confirmations are 350's charge — the in-context SLOADs
    are a natural consequence of the relocation, but no dedicated gas pass here). Comments describe current state
    only.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && M=contracts/modules/GameAfkingModule.sol; grep -c "function doWork\|function autoBuy\|function autoOpen" $M | grep -qE "^[1-9]" && grep -qE "creditFlip" $M && echo "ROUTER+BOUNTY-CREDITFLIP"</automated>
  </verify>
  <acceptance_criteria>
    - The router (`doWork`/`autoBuy`/`autoOpen`) is present with the one-category early-return dispatch shape
    - The open is a post-RNG `OPEN_BATCH`-style router category (NOT folded into advanceGame); the open-leg cursor drives it
    - The buy/process bounty is work-scaled (OPEN_KNEE pro-rate, not once-per-advance) — farm-by-splitting resistant (PLACE-02)
    - Bounty payment is the deferred `creditFlip` BURNIE flip-credit (not a direct transfer / not mintForGame); no GAS-01/02/03 dedicated work pulled in
  </acceptance_criteria>
  <done>The router (doWork/autoBuy/autoOpen) + the PLACE-02 bounty are landed — open as a post-RNG category, work-scaled OPEN_KNEE pro-rate, creditFlip payment — with no GAS-phase work pulled forward.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| afking open seed → box outcome | The seed must mirror openLootBox's `abi.encode` exactly and use the STAMPED day; a packed-form copy or an open-time day = a different, player-steerable outcome. |
| afking open ↔ human openLootBox | The two routes share the EV-cap map + the word map; an unguarded shared write would be a double-draw / contention hazard. |
| EV-cap RMW → double-draw | The buy-time write must be bypassed for afking boxes or the per-level 10-ETH budget is drawn twice. |
| bounty → farm-by-splitting | A once-per-advance flat bounty invites many-minimal-chunk farming; the pay-for-work pro-rate is the structural answer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-349-04-SEED | Spoofing (outcome steer) | the open seed encoding/day | mitigate | The seed is `abi.encode` (NOT the PRESALE `abi.encodePacked`) seeded from the STAMPED day (FREEZE-03); Task 1 verify asserts `abi.encode` present, `abi.encodePacked` absent, `_simulatedDayIndex` absent. The day is pinned at process → no open-time day lever. |
| T-349-04-FRZ | Spoofing (score/baseLevel manipulation) | score/baseLevel read | mitigate | The open reads scorePlus1/baseLevelPlus1 FROM the stamp (D-348-07, FROZEN) — no live recompute; FREEZE-01 holds for those fields. The sole residual live-read is the EV-cap RMW (benign monotonic clamp). |
| T-349-04-DBL | Spoofing (double EV draw) | EV-cap RMW | mitigate | EVCAP-01: exactly one `_applyEvMultiplierWithCap` RMW at open keyed [player][level+1]; the buy-time write (MintModule:1303/1327) is bypassed (the process pass stamps only). Task 2 verify asserts no `_callTicketPurchase`/MintModule path. |
| T-349-04-DOPEN | Tampering (double-open) | lastOpenedIndex | mitigate | BOX-04: monotonic `lastOpenedIndex` — open only if `stamp.index > lastOpenedIndex`, then advance. Task 1 verify asserts the marker is referenced. |
| T-349-04-HAZ | Tampering (two-route contention) | shared EV-cap/word maps | mitigate | BOX-05: the EV-cap map is the one intended per-level 10-ETH budget (one RMW each); `lootboxRngWordByIndex` is read-only/write-once; the afking stamp is producer→consumer ordered. The human route is untouched (disjoint per-index state). |
| T-349-04-FARM | Economic (bounty farming) | the buy/process bounty | mitigate | PLACE-02: work-scaled OPEN_KNEE pro-rate (pay for work, not once-per-advance) resists farm-by-splitting; payment is the deferred creditFlip BURNIE flip-credit (uncapped-mint-safe — flip credit, not mintForGame). |
| T-349-04-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs (Solidity edits only). |
</threat_model>

<verification>
- The open-pass materializes byte-identically (abi.encode stamped-day seed, score/baseLevel from the stamp, targetLevel dependence identical), boons OFF amount=spend, monotonic lastOpenedIndex (BOX-01/04)
- The single EV-cap RMW is at open with the frozen evMultiplierBps and the buy-time write bypassed (EVCAP-01); the two routes are hazard-free (BOX-05)
- The router + the PLACE-02 work-scaled bounty (post-RNG open category, creditFlip) are landed; no GAS-phase work pulled in
- NO `git commit` / `git add contracts/` was run by the executor (orchestrator commits post-approval)
</verification>

<success_criteria>
- The afking open-pass is freeze-correct (FREEZE-03 abi.encode stamped-day seed; D-348-07 stamp reads) + byte-identical to openLootBox (BOX-04) + boons-off amount=spend (BOX-01) + two-route hazard-free (BOX-05)
- EVCAP-01 single RMW at open, buy-time bypassed, no double-draw; PLACE-02 work-scaled bounty + creditFlip
- The GameAfkingModule is complete (subscribe/setters/process from 349-03 + open/router/bounty here)
- Edits held at the contract-commit boundary (no executor commit)
</success_criteria>

<output>
Create `.planning/phases/349-impl-the-one-carefully-sequenced-batched-contract-diff-code-/349-04-SUMMARY.md` when done.
Record the BOX-05 two-route hazard-free reconciliation in the SUMMARY.
</output>
