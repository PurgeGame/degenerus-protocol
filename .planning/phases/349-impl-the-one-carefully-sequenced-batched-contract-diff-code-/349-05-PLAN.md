---
phase: 349-impl-the-one-carefully-sequenced-batched-contract-diff-code-
plan: 05
type: execute
wave: 5
depends_on: ["349-04"]
files_modified:
  - contracts/modules/DegenerusGameAdvanceModule.sol
  - contracts/interfaces/IDegenerusGameModules.sol
  - contracts/AfKing.sol
autonomous: false
requirements: [ARCH-03, REVERT-02]
must_haves:
  truths:
    - "The afking process STAGE is INSERTED in advanceGame immediately BEFORE the rngGate( call (the new-day path only), AFTER the daily ticket-drain gate + the inherited _enforceDailyMintGate (ZERO new gate code, D-348-03) — driving the GameAfkingModule process pass across the subscriber set via _subCursor, BUY_BATCH-style (D-348-01 required-path)"
    - "The subsFullyProcessed / _subCursor chunk gate is AUTHORED: read LR_INDEX ONCE at pass start into a uniform-epoch local (stamp every sub to that same value), mirror the ticketsFullyProcessed partial-drain discipline (break + return while !subsFullyProcessed, set true only at cursor end, then fall through to rngGate) — FREEZE-02b"
    - "The requestLootboxRng no-interleave guard is AUTHORED: while subsFullyProcessed == false the mid-day index advance is blocked (an early revert mirroring the LR_MID_DAY reroll-block at :1020) — FREEZE-02c, the load-bearing D-348-02 obligation; the two index-advance sites (:1089 mid-day, :1629 daily) then both sit OUTSIDE/AFTER the STAGE"
    - "The STAGE cannot block the game-over routing (REVERT-02 class C, no-valve): _handleGameOverPath runs and returns at :182 BEFORE the STAGE block at :272-273 is reached — VERIFIED (no edit unless needed)"
    - "The interfaces declare the relocated mutating ABI (subscribe, the 4 setters, doWork/autoBuy/autoOpen) so the AfKing dispatch stubs + the AdvanceModule STAGE call resolve against a real ABI"
    - "AfKing.sol collapses to ~8 thin delegatecall dispatch stubs (each shaped like claimBingo: selector + abi.encodeWithSelector + delegatecall + _revertDelegate tail), the relocated bodies (subscriber set, _resolveBuy :727-863, the open logic) DELETED — they now live in GameAfkingModule (ARCH-03)"
    - "forge build is CLEAN across the WHOLE batched 349 diff (349-01..05); the post-fold forge build --sizes shows DegenerusGame < 24,576 B (the FINAL authoritative ARCH-04 verification); then HOLD at the contract-commit boundary"
  artifacts:
    - path: "contracts/modules/DegenerusGameAdvanceModule.sol"
      provides: "the process STAGE before rngGate + the subsFullyProcessed/_subCursor gate + the requestLootboxRng no-interleave guard (FREEZE-02) + the game-over-routing-unblocked verification (REVERT-02 class C)"
      contains: "subsFullyProcessed"
    - path: "contracts/interfaces/IDegenerusGameModules.sol"
      provides: "the relocated mutating ABI (subscribe/setters/router)"
    - path: "contracts/AfKing.sol"
      provides: "the thin dispatch-stub collapse; relocated bodies deleted"
      contains: "delegatecall"
  key_links:
    - from: "AdvanceModule STAGE (before rngGate :274)"
      to: "the GameAfkingModule process pass + subsFullyProcessed/_subCursor"
      via: "the required-path chunked STAGE (D-348-01), uniform LR_INDEX epoch read once (FREEZE-02b)"
      pattern: "subsFullyProcessed"
    - from: "requestLootboxRng (mid-day index advance :1089)"
      to: "the no-interleave guard (revert while !subsFullyProcessed)"
      via: "FREEZE-02c — mirror the LR_MID_DAY reroll-block :1020"
      pattern: "subsFullyProcessed"
    - from: "AfKing.sol thin stubs"
      to: "GameAfkingModule via delegatecall (the new ABI)"
      via: "ARCH-03 — claimBingo-shaped dispatch; relocated bodies deleted"
      pattern: "_revertDelegate"
---

<objective>
Steps 4, 5, 6 (terminal) of the producer-before-consumer edit-order map (348-IMPL-EDIT-ORDER-MAP.md Section 3
Steps 4/5/6): INSERT the afking process STAGE in `advanceGame` before `rngGate` + AUTHOR the FREEZE-02
`subsFullyProcessed`/`_subCursor` chunk gate + the `requestLootboxRng` no-interleave guard + VERIFY the
game-over routing is unblocked (REVERT-02 class C); DECLARE the relocated ABI in the interfaces; COLLAPSE
`AfKing.sol` to thin dispatch stubs (ARCH-03); run the `forge build` clean gate across the WHOLE batched 349
diff (the FINAL ARCH-04 < 24,576 verification); then HOLD at the contract-commit boundary.

These are the consumers (the STAGE call consumes the 349-03/04 process pass; the interfaces declare it; the
stubs dispatch into it), authored AFTER their producers so no intermediate file holds a dangling reference. The
STAGE authoring is FREEZE-02-critical: the `subsFullyProcessed` guard does NOT exist in source (CONFIRMED-NEW) —
349 AUTHORS it here against the re-pinned `:1016`/`:1089`/`:1629`/`:274` lines.

Purpose: ARCH-03 (AfKing thin stubs) + REVERT-02 (the class-C terminal-routing-unblocked verification + the
no-valve form's AdvanceModule half) + the FREEZE-02 guard authoring + the ARCH-04-delivered build proof. The
single batched 349 diff compiles clean and is HELD for explicit user hand-review.

Output: Edits to `DegenerusGameAdvanceModule.sol` + the interfaces + `AfKing.sol`; the whole 349 diff compiles
clean (`forge build`) and DegenerusGame is measured < 24,576 B. Then HOLD — NO COMMIT.

⚠ CONTRACT-BOUNDARY HARD STOP. `autonomous: false`. The executor applies the edits but NEVER runs `git commit` /
`git add contracts/` (the ONLY action needing approval is committing `contracts/*.sol`). The orchestrator owns
ALL commits, deferred past the single user-approval gate, and splits the batched diff into atomic per-plan
commits AFTER approval.

⚠ `forge build`-clean ONLY, then HOLD. The ABI change (the relocated subscribe/setters/router, the deleted AfKing
bodies, the new module) WILL break existing TEST-FILE compilation — that is EXPECTED and is 351 TST's charge
(which also re-syncs the stale `poolOf` test files, 348-CODE-SIZE-PLAN §0). 349 does NOT run `forge test` and does
NOT touch `test/`. `forge build` compiles `contracts/` sources (the correct gate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-IMPL-EDIT-ORDER-MAP.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-FREEZE-PROOF.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-PLACEMENT-DECISION.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-CODE-SIZE-PLAN.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-INVARIANT-CARRY.md
@.planning/phases/349-impl-the-one-carefully-sequenced-batched-contract-diff-code-/349-01-SUMMARY.md

<interfaces>
<!-- Inherit 349-01 Task 1's re-pinned anchors; SPEC-attested refs @ 20ca1f79 below. These are the CONSUMERS of 349-02/03/04. -->

AdvanceModule STAGE insertion + guards (FREEZE-02; D-348-01 required-path):
- STAGE insertion point `:272-273` — `// RNG: use existing word or request new one` (:272) / `bool bonusFlip = ...` (:273); the STAGE inserts HERE, immediately before the rngGate( call
- `rngGate(...)` call `:274` — `(uint256 rngWord, uint32 gapDays) = rngGate(`; the STAGE runs strictly BEFORE this
- `rngGate` def `:1152`
- `_enforceDailyMintGate(...)` call `:191` (the gate the STAGE inherits — ZERO new gate code, D-348-03); def `:973`
- the daily ticket-drain gate `:247-270` (the STAGE sits after this, before rngGate) + the ticketsFullyProcessed partial-drain discipline `:196/:247/:269` (the shape to mirror for subsFullyProcessed) + the STAGE_TICKETS_WORKING partial-drain status `:216-218/:264-266`
- `requestLootboxRng()` def `:1016`; the LR_MID_DAY reroll-block to mirror `:1020` (`if (_lrRead(LR_MID_DAY_SHIFT, LR_MID_DAY_MASK) != 0) revert E();`)
- the two LR_INDEX advance sites `:1089` (mid-day, inside requestLootboxRng) + `:1629` (daily, inside _finalizeRngRequest, downstream of rngGate) — both then OUTSIDE/AFTER the STAGE
- `_handleGameOverPath` runs+returns `:182` (BEFORE the STAGE block :272-273 — REVERT-02 class C verify)
- subsFullyProcessed is CONFIRMED-NEW (grep returns 0; 349 AUTHORS it — it is a SPECIFICATION, not existing code)

The AfKing thin-stub collapse (ARCH-03; 348-IMPL-EDIT-ORDER-MAP §1.4):
- the claimBingo void-dispatch shape to mirror `DegenerusGame.sol:328-343` (selector + abi.encodeWithSelector(args) + delegatecall + `_revertDelegate(data)` tail); `_revertDelegate` `:1051`
- the ~8 external mutating stubs: subscribe `:324`, setDailyQuantity `:392`, setDrainGameCreditFirst `:405`, setMode `:414`, setReinvestPct `:423`, doWork `:864`, autoBuy `:904`, autoOpen `:910`
- DELETE the relocated bodies: the subscriber set (_subOf :210, _subscribers :214, _subscriberIndex :218), _resolveBuy :727-863, the open logic — now in GameAfkingModule
- ~1.0-1.5 KB of Game-runtime stub cost (the additions the 349-01 R1 reclaim cleared)

REVERT-02 class C (348-INVARIANT-CARRY §2 / §4): no valve; the game-over routing-unblocked verification — _handleGameOverPath (:182) runs+returns BEFORE the STAGE block; confirm the STAGE does not sit on the path that reaches game-over.

ARCH-04 final verification (348-CODE-SIZE-PLAN §1/§4): MEASURED baseline 24,358 B / 218 B; the post-fold `forge build --sizes --skip "test/**" --skip "*.t.sol"` is the FINAL check < 24,576 (worst case 24,418 with R1+R2+R3-wrapper). If it exceeds, pull the reserve set (decClaimable/getTickets/getDailyHeroWinner ~650 B) and/or complete the R3 caller-retarget (up to 953 B).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Insert the process STAGE before rngGate + AUTHOR the subsFullyProcessed/_subCursor chunk gate (FREEZE-02b, D-348-01 required-path, D-348-03 inherited mint-gate)</name>
  <files>contracts/modules/DegenerusGameAdvanceModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameAdvanceModule.sol (the new-day do/while block ~:245; the STAGE insertion point :272-273 before rngGate( :274; _enforceDailyMintGate :191; the daily ticket-drain gate :247-270; the ticketsFullyProcessed partial-drain discipline :196/:247/:269 + STAGE_TICKETS_WORKING :216-218/:264-266 — the shape to mirror)
    - contracts/modules/GameAfkingModule.sol (the process pass callee from 349-03 — what the STAGE drives across _subCursor)
    - 348-PLACEMENT-DECISION.md §3 (the chunked STAGE mechanism: subsFullyProcessed + _subCursor before rngGate; inherits _enforceDailyMintGate, ZERO new gate code, D-348-03) + §3a (the insertion point :272-273)
    - 348-FREEZE-PROOF.md FREEZE-02 (b) (read LR_INDEX ONCE at pass start — the uniform epoch; stamp every sub to that same value)
    - 348-IMPL-EDIT-ORDER-MAP.md §3 Step 4a + 4b
  </read_first>
  <action>
    INSERT the new chunked process STAGE in `DegenerusGameAdvanceModule.sol` immediately BEFORE the `rngGate(`
    call (`:274`), on the NEW-DAY path only (D-348-01 required-path). Place it inside the `do { ... } while` block,
    AFTER the daily ticket-drain gate (`:247-270`) and AFTER the inherited `_enforceDailyMintGate` (`:191` — ZERO
    new gate code, D-348-03; the STAGE rides the existing gate), BEFORE `rngGate(:274)`. The STAGE drives the
    `GameAfkingModule` process pass across the subscriber set via `_subCursor`, draining a `BUY_BATCH`-style
    per-call gas budget (so a large set spreads across several advance calls under the 16.7M advance-chain ceiling).
    AUTHOR the `subsFullyProcessed` / `_subCursor` chunk gate (FREEZE-02b): read `LR_INDEX` ONCE at pass start into
    a uniform-epoch local and stamp every sub in the day's STAGE to that SAME value (NOT a per-sub re-read — the
    uniform index epoch); mirror the existing `ticketsFullyProcessed` partial-drain discipline (`break` and return
    while `!subsFullyProcessed`, with the `STAGE_*_WORKING` partial-drain status shape of `:216-218/:264-266`), set
    `subsFullyProcessed = true` ONLY at cursor end, then fall through to `rngGate(:274)`. The mid-day same-day path
    (`:194-224`) returns before this block, so the STAGE never runs mid-day. Comments describe current state only.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && A=contracts/modules/DegenerusGameAdvanceModule.sol; grep -qE "subsFullyProcessed" $A && grep -qE "_subCursor" $A && awk '/subsFullyProcessed/{if(!s)s=NR} /= rngGate\(|rngGate\(/{if(!g)g=NR} END{ if(s>0 && g>0 && s<g) print "STAGE-BEFORE-RNGGATE"; else print "CHECK-ORDER" }' $A | grep -qx "STAGE-BEFORE-RNGGATE" && echo "STAGE-INSERTED-BEFORE-RNGGATE"</automated>
  </verify>
  <acceptance_criteria>
    - The process STAGE references `subsFullyProcessed` + `_subCursor` and is positioned BEFORE the first `rngGate(` call (new-day path), after the ticket-drain gate + the inherited `_enforceDailyMintGate`
    - The STAGE reads `LR_INDEX` ONCE at pass start (uniform epoch) and mirrors the `ticketsFullyProcessed` partial-drain discipline (break/return while !subsFullyProcessed; set true at cursor end; fall through to rngGate)
    - NO new mint-gate code is authored (the STAGE inherits `_enforceDailyMintGate` :191, D-348-03)
    - The STAGE drives the GameAfkingModule process pass across _subCursor (BUY_BATCH-style)
  </acceptance_criteria>
  <done>The chunked process STAGE is inserted before rngGate (new-day path, after the ticket gate + inherited mint-gate), authoring the subsFullyProcessed/_subCursor uniform-epoch chunk gate (FREEZE-02b) driving the GameAfkingModule process pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: AUTHOR the requestLootboxRng no-interleave guard (FREEZE-02c, D-348-02) + VERIFY the game-over routing is unblocked (REVERT-02 class C, no-valve)</name>
  <files>contracts/modules/DegenerusGameAdvanceModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameAdvanceModule.sol (requestLootboxRng :1016; the LR_MID_DAY reroll-block to mirror :1020; the mid-day index advance :1089; the daily advance :1629; rngGate :1152/:274; _handleGameOverPath runs+returns :182)
    - 348-FREEZE-PROOF.md FREEZE-02 (c) (the no-interleave guard SPEC: block requestLootboxRng while !subsFullyProcessed, mirror the :1020 reroll-block; subsFullyProcessed CONFIRMED-NEW) + (a) (the two advance sites :1089/:1629 both AFTER/OUTSIDE the STAGE) + (d) (PROVEN conditional on 349 authoring this guard)
    - 348-INVARIANT-CARRY.md §2 (REVERT-02 class C: terminal — verify the STAGE cannot block game-over routing; no valve) + §4 (class C accepted, terminal)
    - 348-IMPL-EDIT-ORDER-MAP.md §3 Step 4c (the no-interleave guard) + Step 4d (game-over routing verify) + §4.2 (REVERT-02 no-valve)
  </read_first>
  <action>
    (a) AUTHOR the `requestLootboxRng` NO-INTERLEAVE GUARD (FREEZE-02c — the load-bearing D-348-02 obligation):
    inside `requestLootboxRng` (`:1016`), while `subsFullyProcessed == false`, BLOCK the mid-day index advance —
    add an early `revert` exactly mirroring the existing reroll-block at `:1020` (`if (_lrRead(LR_MID_DAY_SHIFT,
    LR_MID_DAY_MASK) != 0) revert E();`) — i.e. `if (!subsFullyProcessed) revert E();` (or the equivalent named
    revert). This removes the only lever by which a separate-tx `requestLootboxRng` could advance the index
    mid-STAGE: with it blocked, the two index-advance sites (`:1089` mid-day, `:1629` daily) both sit OUTSIDE/AFTER
    the STAGE (the daily advance fires downstream of `rngGate(:274)`, after the STAGE; the mid-day advance is
    blocked for the pass duration). This guard does NOT exist in source (`subsFullyProcessed` CONFIRMED-NEW) — 349
    AUTHORS it. (b) VERIFY (no edit unless a real gap is found) that the STAGE CANNOT BLOCK THE GAME-OVER ROUTING
    (REVERT-02 class C, no-valve, D-348-04): game-over is a separate advance path — `_handleGameOverPath` runs and
    returns at `:182`, BEFORE the STAGE block at `:272-273` is reached. Confirm the STAGE does NOT sit on the path
    that reaches game-over routing (so a ≥120-day-dead terminal state still routes to game-over, never blocked by
    the afking STAGE). Record the verification in the SUMMARY. ⚠ Do NOT add a try/catch valve anywhere (D-348-04
    no-valve): a class-B solvency underflow must FAIL LOUD (propagate), never be masked; class C is absorbed by the
    game ending, not by catching. Comments describe current state only.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && A=contracts/modules/DegenerusGameAdvanceModule.sol; awk '/function requestLootboxRng/{f=1} f&&/subsFullyProcessed/{print "GUARD-IN-REQUESTRNG"; exit} f&&/^    }/{exit}' $A | grep -qx "GUARD-IN-REQUESTRNG" && grep -ic "try {" $A | grep -qx 0 && grep -ic "} catch" $A | grep -qx 0 && echo "NO-INTERLEAVE-GUARD NO-VALVE"</automated>
  </verify>
  <acceptance_criteria>
    - `requestLootboxRng` (`:1016`) contains an early revert gated on `!subsFullyProcessed` (mirroring the :1020 reroll-block) — FREEZE-02c authored
    - The game-over-routing-unblocked verification is recorded in the SUMMARY (_handleGameOverPath :182 runs+returns before the STAGE block; the STAGE is not on the game-over path) — REVERT-02 class C
    - NO try/catch valve anywhere in the AdvanceModule (D-348-04 no-valve); class B fails loud, class C terminal
  </acceptance_criteria>
  <done>The requestLootboxRng no-interleave guard (revert while !subsFullyProcessed, FREEZE-02c) is authored, the game-over routing is verified unblocked (REVERT-02 class C), and no try/catch valve exists.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Declare the relocated ABI in the interfaces (Step 5) + collapse AfKing.sol to thin dispatch stubs, deleting the relocated bodies (ARCH-03, Step 6)</name>
  <files>contracts/interfaces/IDegenerusGameModules.sol, contracts/AfKing.sol</files>
  <read_first>
    - contracts/interfaces/IDegenerusGameModules.sol (the existing module-interface pattern to mirror — add the relocated mutating surface)
    - contracts/AfKing.sol (the ~8 external mutating fns to stub: subscribe :324, setDailyQuantity :392, setDrainGameCreditFirst :405, setMode :414, setReinvestPct :423, doWork :864, autoBuy :904, autoOpen :910; the bodies to DELETE: _subOf :210/_subscribers :214/_subscriberIndex :218, _resolveBuy :727-863, the open logic)
    - contracts/DegenerusGame.sol (the claimBingo void-dispatch shape to mirror :328-343; _revertDelegate :1051)
    - 348-IMPL-EDIT-ORDER-MAP.md §1.4 (the thin-stub collapse, ~8 stubs, claimBingo-shaped, a stub CANNOT be view) + §3 Step 5 (interfaces) + Step 6 (AfKing stubs, delete relocated bodies) + the ARCH-03 AF_KING-address dissolution-vs-thin-shim note
  </read_first>
  <action>
    (a) INTERFACES (Step 5): ADD the relocated mutating surface to `contracts/interfaces/IDegenerusGameModules.sol`
    (and/or the GameAfkingModule selector interface) — `subscribe`, the 4 setters (`setDailyQuantity`/
    `setDrainGameCreditFirst`/`setMode`/`setReinvestPct`), `doWork`/`autoBuy`/`autoOpen` — mirroring the existing
    module-interface pattern, so the AfKing dispatch stubs (b) and the AdvanceModule STAGE call (Task 1) resolve
    against a real declared ABI. (b) AFKING STUBS (Step 6, ARCH-03): collapse the external mutating surface of
    `AfKing.sol` to ~8 THIN delegatecall dispatch stubs, each shaped EXACTLY like the `claimBingo` void-dispatch
    (`DegenerusGame.sol:328-343`): selector + `abi.encodeWithSelector(args)` + delegatecall to the GameAfkingModule
    address + the `_revertDelegate(data)` tail (resolve the ARCH-03 AF_KING-address question per the SPEC — keep
    AfKing as the thin external shim addressing the module). The stubs: `subscribe` (`:324`), `setDailyQuantity`
    (`:392`), `setDrainGameCreditFirst` (`:405`), `setMode` (`:414`), `setReinvestPct` (`:423`), `doWork` (`:864`),
    `autoBuy` (`:904`), `autoOpen` (`:910`). DELETE the relocated bodies entirely — the subscriber set (`_subOf`
    `:210`, `_subscribers` `:214`, `_subscriberIndex` `:218`), `_resolveBuy` (`:727-863`), and the open logic — they
    now live in `GameAfkingModule` + `DegenerusGameStorage`. ⚠ A delegatecall stub CANNOT be `view` (irrelevant
    here — all 8 are mutating). Authoring the stubs only AFTER the module + storage + interfaces exist means no
    intermediate file holds a dangling reference. Remove any now-dead imports/state in AfKing.sol (confirm via grep).
    Comments describe current state only.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && K=contracts/AfKing.sol; grep -c "_resolveBuy" $K | grep -qx 0 && grep -c "mapping(address => Sub) private _subOf" $K | grep -qx 0 && grep -qE "delegatecall" $K && grep -c "function subscribe\|function setDailyQuantity\|function doWork\|function autoBuy\|function autoOpen" $K | grep -qE "^[5-9]|^[1-9][0-9]" && echo "AFKING-STUBBED BODIES-DELETED"</automated>
  </verify>
  <acceptance_criteria>
    - The interfaces declare the relocated mutating ABI (subscribe, the 4 setters, doWork/autoBuy/autoOpen)
    - `AfKing.sol` external mutating surface is ~8 thin delegatecall dispatch stubs (claimBingo-shaped: selector + encode + delegatecall + `_revertDelegate`); AfKing keeps `delegatecall` (1+ occurrences)
    - The relocated bodies are DELETED from AfKing.sol: `_resolveBuy` (0), the `_subOf`/`_subscribers`/`_subscriberIndex` set (moved to storage), the open logic
    - Any now-dead imports/state in AfKing.sol are removed (grep-confirmed unused); the ARCH-03 AF_KING-address question is resolved per the SPEC
  </acceptance_criteria>
  <done>The interfaces declare the relocated ABI and AfKing.sol collapses to thin claimBingo-shaped dispatch stubs with the relocated bodies (_resolveBuy, the subscriber set, the open logic) deleted (ARCH-03).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: [BUILD GATE + HOLD] forge build clean across the whole 349 diff + ARCH-04 forge build --sizes < 24,576, then HOLD at the contract-commit boundary</name>
  <files>contracts/ (the whole batched 349 diff — build verification only; no further feature edits)</files>
  <read_first>
    - 348-CODE-SIZE-PLAN.md §1 (MEASURED 24,358 B / 218 B) + §4 (the running-total; worst case 24,418 with R1+R2+R3-wrapper; the post-fold build is the FINAL check) + §5 hand-off notes (re-measure; pull reserve / R3 retarget if it exceeds) + §0 (forge build --sizes --skip the stale test files; the stale poolOf failures are 351's charge)
    - 348-IMPL-EDIT-ORDER-MAP.md §3 (no file ships an intermediate broken state; the Game never breaches 24,576 mid-flight) + §6 (the 349 forge build --sizes is the FINAL authoritative verification)
    - the only-contract-commits-need-approval rule (the executor NEVER commits contracts/*.sol; HOLD at the boundary)
  </read_first>
  <action>
    (a) BUILD GATE: run `forge build` (compiles `contracts/` sources). It MUST exit 0 (clean). This is the FIRST and
    ONLY build of the whole batched 349 diff (349-01..05) and proves the producer-before-consumer authoring held —
    no intermediate broken state, no dangling reference (the storage append → module → STAGE → interfaces → stubs
    order). ⚠ Do NOT run `forge test`. The ABI change (relocated subscribe/setters/router, deleted AfKing bodies)
    WILL break existing TEST-FILE compilation — that is EXPECTED and is 351 TST's charge (which also re-syncs the
    stale `poolOf` test files, §0). `test/` is NOT touched in 349. If `forge build` surfaces a CONTRACT-source
    compile error, FIX the contract edit (it is a real bug in the diff). (b) ARCH-04 FINAL VERIFICATION: run
    `forge build --sizes --skip "test/**" --skip "*.t.sol"` and confirm `DegenerusGame` runtime is < 24,576 B (the
    measured baseline was 24,358; the worst-case running-total lands at 24,418 with R1 + the R2/R3 wrappers). If it
    EXCEEDS 24,576, pull the reserve set (`decClaimable`/`getTickets`/`getDailyHeroWinner`, ~650 B) and/or complete
    the R3 caller-retarget (up to ~953 B, retargeting `WhaleModule:875`/`DecimatorModule:704`/`BurnieCoin:620`/
    `StakedDegenerusStonk:913` to a lens) — record which lever was pulled in the SUMMARY. Record the measured size +
    margin in the SUMMARY. (c) HOLD: with `forge build` clean and the size < 24,576, STOP. Do NOT run `git commit` /
    `git add contracts/` (the only action needing approval is committing `contracts/*.sol`; the executor NEVER
    commits). The whole batched 349 diff is now applied + compiling + size-verified and HELD at the contract-commit
    boundary for explicit user hand-review. The orchestrator owns the single user-approval gate and the
    post-approval atomic per-plan commits.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -5 && forge build && forge build --sizes --skip "test/**" --skip "*.t.sol" 2>&1 | grep -i "DegenerusGame" | head -3 && echo "BUILD-CLEAN SIZE-MEASURED HOLD-AT-BOUNDARY"</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` exits 0 (clean compile of the whole batched 349 diff — the producer-before-consumer order held; no dangling reference)
    - `forge build --sizes` shows `DegenerusGame` runtime < 24,576 B (ARCH-04 FINAL verification); the measured size + margin recorded in the SUMMARY (and which reserve/retarget lever, if any, was pulled)
    - `forge test` was NOT run; `test/` was NOT touched (the ABI-break of test files is expected, 351's charge)
    - NO `git commit` / `git add contracts/` was run; the diff is HELD at the contract-commit boundary for user hand-review
  </acceptance_criteria>
  <done>`forge build` exits 0 across the whole batched 349 diff and `forge build --sizes` shows DegenerusGame < 24,576 B (ARCH-04 final); `forge test` NOT run and `test/` untouched; the diff is HELD at the contract-commit boundary with no executor commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| requestLootboxRng (separate tx) → the mid-STAGE index | A separate-tx index advance mid-STAGE would let a sub stamp to an index whose word is requested in the same flow (freeze broken) — the no-interleave guard removes the lever. |
| afking STAGE → game-over routing | The required-path STAGE must not block a ≥120-day-dead terminal state from routing to game-over (REVERT-02 class C, no-valve). |
| AfKing stubs → GameAfkingModule | The thin stubs delegatecall the module; a dangling reference (deleting bodies before the module exists) would break the build. |
| DegenerusGame deploy → EIP-170 | The post-fold runtime must be < 24,576 B (the stubs add ~1.0-1.5 KB, cleared by the 349-01 R1 reclaim). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-349-05-FRZ | Spoofing (reveal-then-steer) | requestLootboxRng mid-STAGE | mitigate | FREEZE-02c: the no-interleave guard blocks the mid-day index advance while `!subsFullyProcessed` (mirroring the :1020 reroll-block); the STAGE reads LR_INDEX once (uniform epoch, Task 1). Both advance sites (:1089/:1629) then sit AFTER the STAGE. Task 2 verify asserts the guard. (subsFullyProcessed CONFIRMED-NEW — authored here; 351 TST-01 proves it empirically.) |
| T-349-05-GO | Denial of Service (day-brick / no game-over) | the required-path STAGE | mitigate | REVERT-02 class C (no-valve): _handleGameOverPath runs+returns at :182 BEFORE the STAGE block :272-273; the STAGE is verified off the game-over path. No try/catch (class B fails loud; class C terminal). Task 2 verify asserts no try/catch + records the routing verification. |
| T-349-05-DANGLE | Denial of Service (broken build) | the stub/body deletion order | mitigate | Producer-before-consumer: the AfKing stubs + the interfaces are authored AFTER the module (349-03/04) + storage (349-02); the `forge build` gate (Task 4) is the FIRST + ONLY build and proves no dangling reference. |
| T-349-05-CEIL | Denial of Service (deploy fails) | DegenerusGame runtime size | mitigate | ARCH-04: the 349-01 R1 reclaim cleared the 218 B margin before the ~1.0-1.5 KB stubs; Task 4's `forge build --sizes` is the FINAL < 24,576 proof, with the reserve/R3-retarget levers if needed. |
| T-349-05-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs (Solidity edits + `forge build` only; forge is the existing toolchain, no ffi). |
</threat_model>

<verification>
- The process STAGE is inserted before rngGate (new-day path, after the inherited mint-gate); the subsFullyProcessed/_subCursor uniform-epoch chunk gate is authored (FREEZE-02b)
- The requestLootboxRng no-interleave guard is authored (FREEZE-02c); the game-over routing is verified unblocked (REVERT-02 class C); NO try/catch valve
- The interfaces declare the relocated ABI; AfKing.sol collapses to thin dispatch stubs with relocated bodies deleted (ARCH-03)
- `forge build` exits 0 across the whole 349 diff; `forge build --sizes` shows DegenerusGame < 24,576 B (ARCH-04 final); `forge test` NOT run; `test/` NOT touched
- NO executor commit; the diff is HELD at the contract-commit boundary for explicit user hand-review
</verification>

<success_criteria>
- The required-path STAGE + the FREEZE-02 guards (chunk gate + no-interleave) are authored; the game-over routing is verified unblocked (REVERT-02)
- The interfaces + the AfKing thin stubs land (ARCH-03); the relocated bodies are deleted with no dangling reference
- The whole batched 349 diff compiles clean and DegenerusGame is < 24,576 B (the FINAL ARCH-04 verification)
- The diff is HELD for explicit user hand-review (no executor commit)
</success_criteria>

<output>
Create `.planning/phases/349-impl-the-one-carefully-sequenced-batched-contract-diff-code-/349-05-SUMMARY.md` when done.
Record the FREEZE-02c no-interleave guard, the game-over-routing verification, and the measured DegenerusGame size + margin in the SUMMARY.
</output>
