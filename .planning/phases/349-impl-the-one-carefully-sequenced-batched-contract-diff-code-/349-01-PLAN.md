---
phase: 349-impl-the-one-carefully-sequenced-batched-contract-diff-code-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/DegenerusGame.sol
  - contracts/modules/DegenerusGameBingoModule.sol
autonomous: false
requirements: [ARCH-03]
must_haves:
  truths:
    - "The executor re-pins EVERY anchor from 348-GREP-ATTESTATION.md against the live tree BEFORE writing any edit (the 343/344 hand-off discipline); `git diff --numstat 20ca1f79 HEAD -- contracts/` is EMPTY (live tree IS the SPEC baseline) and the re-pinned lines are recorded for plans 349-02..05 to inherit"
    - "The code-size reclaim lands FIRST in the batched diff (R1 before any byte-adding Game edit) so the headroom for the ARCH-03 AfKing dispatch stubs exists — the Game runtime stays < 24,576 B at this step (24,358 → ~23,158 after R1); this executes Step 1 of the 348 ARCH-04 plan"
    - "claimAffiliateDgnrs is MOVED out of DegenerusGame.sol into DegenerusGameBingoModule.sol (callable directly on the module via the GAME_BINGO_MODULE delegatecall lane) — NO Game stub is left (a true void), so the full ~1.2–1.35 KB is reclaimed from the Game image"
    - "claimAffiliateDgnrs behavior is byte-preserved by the move (same 4 cross-contract calls, same nested-mapping R/W, same deity-bonus branch) — a relocation, not a rewrite"
    - "If the worst-case running-total needs more margin, R2 (previewSellFarFutureTickets) and the R3 wrapper (playerActivityScore) are pulled to lens/drop-`view` WITHOUT a caller-retarget (R3's 4 external callers keep the inherited body); the R3 full 953 B retarget is NOT done here unless the post-fold build demands it"
  artifacts:
    - path: "contracts/DegenerusGame.sol"
      provides: "claimAffiliateDgnrs removed from the Game image (no stub left) — headroom cleared for the ARCH-03 stubs"
      contains: "GAME_BINGO_MODULE"
    - path: "contracts/modules/DegenerusGameBingoModule.sol"
      provides: "claimAffiliateDgnrs relocated, callable directly on the module"
      contains: "function claimAffiliateDgnrs"
  key_links:
    - from: "external/UI callers of claimAffiliateDgnrs"
      to: "contracts/modules/DegenerusGameBingoModule.sol claimAffiliateDgnrs"
      via: "direct call on the GAME_BINGO_MODULE address (no Game stub indirection)"
      pattern: "function claimAffiliateDgnrs"
---

<objective>
Step 1 of the producer-before-consumer edit-order map (348-IMPL-EDIT-ORDER-MAP.md Section 3 Step 1):
land the CODE-SIZE RECLAIM FIRST so the Game runtime never breaches the 24,576-byte EIP-170 ceiling
mid-flight once the ARCH-03 AfKing dispatch stubs are added (349-05). The Game has only 218 B of measured
headroom (24,358 B at `20ca1f79`); the ~1.0–1.5 KB of stubs MUST be preceded by the reclaim or the deploy
breaks. R1 (`claimAffiliateDgnrs` → `BingoModule`, ~1.2–1.35 KB void) is the clean big win and lands first.

This plan ALSO carries the [BLOCKING-FIRST] re-pin task (the load-bearing 343→344 hand-off discipline): the
348 anchors were grep-attested against `20ca1f79`; the executor re-pins every anchor this whole 349 diff
touches BEFORE any edit and records the re-pinned lines for the downstream plans (349-02..05) to inherit.

Purpose: this reclaim is the PRODUCER STEP for ARCH-03 — clearing the 218 B margin before the AfKing stub
additions is what lets the Game image stay under 24,576 at every intermediate step of the single batched diff.
(It executes Step 1 of the ARCH-04 reclaim PLAN the SPEC produced at 348; ARCH-04 itself is 348-Complete — this
plan delivers its first sequenced edit, in service of the ARCH-01/02/03 fold.)

Output: Edits applied to `contracts/` (the FIRST contract edits of the single batched 349 diff). NO COMMIT —
held at the contract-commit boundary. Plus the executor's first-action re-grep attestation recorded.

⚠ CONTRACT-BOUNDARY HARD STOP. This plan is `autonomous: false`. The executor applies the edits to
`contracts/` but NEVER runs `git commit` / `git add contracts/` (the only action needing approval is committing
`contracts/*.sol`). The orchestrator owns all commits, deferred past the single user-approval gate (see 349-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-IMPL-EDIT-ORDER-MAP.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-CODE-SIZE-PLAN.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-GREP-ATTESTATION.md

<interfaces>
<!-- The current shapes the edits land on. Re-grep at author time (Task 1) — these are the SPEC-attested anchors @ 20ca1f79. -->

ARCH-04 reclaim-target sites (DegenerusGame.sol, attested @ 20ca1f79):
- `claimAffiliateDgnrs` def `:1553` — `function claimAffiliateDgnrs(address player) external {` (R1: MOVE → BingoModule)
- `previewSellFarFutureTickets` def `:2113` (R2 insurance: lens / drop-`view`; its sole-caller `_quoteFarFutureSwap` body drops too)
- `playerActivityScore` def `:2676` (R3 insurance: thin wrapper → lens; 4 external callers keep the inherited body)

The GAME_BINGO_MODULE delegatecall lane — the existing void-dispatch shape claimAffiliateDgnrs's MOVE uses
(the module is callable directly; no Game stub left):
- `claimBingo` dispatch stub `DegenerusGame.sol:328-343` (selector + abi.encodeWithSelector(args) + delegatecall + `_revertDelegate(data)` tail; `.GAME_BINGO_MODULE` at `:334`)
- `_revertDelegate` private helper `DegenerusGame.sol:1051`

R3's 4 external callers (the blast radius — do NOT retarget unless the post-fold build demands it):
- `WhaleModule.sol:875`, `DecimatorModule.sol:704` (delegatecall modules via `IDegenerusGame(address(this)).playerActivityScore`)
- `BurnieCoin.sol:620`, `StakedDegenerusStonk.sol:913` (cross-contract)
- the inherited `_playerActivityScore` body (`MintStreakUtils.sol:151/237`) is reached THROUGH the external selector → it STAYS regardless

Measured baseline (348-CODE-SIZE-PLAN.md §1): DegenerusGame = 24,358 B runtime / 218 B margin (EXACT). Reclaim
~1.4–1.7 KB clean (R1 + the two thin wrappers); stub budget ~1.0–1.5 KB (349-05). The 349 `forge build --sizes`
after the WHOLE diff (run in 349-05) is the FINAL authoritative verification.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: [BLOCKING-FIRST] Re-pin every anchor vs the live tree before any edit (the 343/344 hand-off discipline)</name>
  <files>.planning/phases/349-impl-the-one-carefully-sequenced-batched-contract-diff-code-/349-01-SUMMARY.md (attestation record); reads contracts/ only</files>
  <read_first>
    - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-GREP-ATTESTATION.md (THE attestation to re-run — §1 the four drift corrections, §2.1–§2.5 the per-file Drift-Correction Tables, §4 the call-graph attestation)
    - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-IMPL-EDIT-ORDER-MAP.md §5 (re-pin before authoring: line numbers WILL drift the instant the first edit lands; never trust the transcribed lines once the tree has moved)
  </read_first>
  <action>
    BEFORE writing any contract edit, confirm the SPEC baseline and re-pin every anchor this whole 349 diff
    touches. Run `git diff --numstat 20ca1f79 HEAD -- contracts/` and confirm it is EMPTY (the live tree IS the
    `20ca1f79` SPEC baseline → the 348 attestation is currently valid; this re-grep is the discipline floor, not a
    known-drift fix). Then re-grep and re-pin AT MINIMUM the anchors the 349 diff touches, citing the ACTUAL
    matched line for every subsequent edit (never the upstream doc-cited line): the reclaim sites
    (`claimAffiliateDgnrs` SPEC `:1553`, `previewSellFarFutureTickets` SPEC `:2113`, `playerActivityScore` SPEC
    `:2676`); the `claimBingo` delegatecall lane (`:328-343`, `.GAME_BINGO_MODULE` `:334`, `_revertDelegate`
    `:1051`); the storage append target (`Sub` struct + the `_subOf`/`_subscribers`/`_subscriberIndex` set
    currently in AfKing.sol, the `afkingFunding` mapping `Storage:410`, the invariant comment `Storage:348`); the
    AfKing surface (`subscribe` `:324`, setters `:392/:405/:414/:423`, `doWork` `:864`, `autoBuy` `:904`,
    `autoOpen` `:910`, `_resolveBuy` `:727-863`, OPEN-E gate `:343-352`, funder `:624`, `validThroughLevel` write
    `:371`, `LOOTBOX_MIN` `:269`, `TICKET_SCALE` `:238`); the AdvanceModule STAGE region (`rngGate` call `:274`,
    `rngGate` def `:1152`, `_enforceDailyMintGate` `:191/:973`, `_handleGameOverPath` `:182`, `requestLootboxRng`
    `:1016`, the reroll-block `:1020`, index advances `:1089`/`:1629`, `ticketsFullyProcessed` discipline
    `:196/:247/:269`); the LootboxModule open path (`openLootBox` `:503`, the `abi.encode` seed `:534`, the
    PRESALE `abi.encodePacked` `:644` — the WRONG pattern, `_applyEvMultiplierWithCap` `:459`, `lootboxDay` `:514`,
    `lootboxPurchasePacked` `:529`, `lootboxRngWordByIndex` `:510`, `_rollTargetLevel` `:903`); and the EV-cap map
    (`lootboxEvBenefitUsedByLevel` `Storage:1469`, cap `:1326`, the MintModule buy-time writes `MintModule:1303`/
    `:1327`). Confirm `subsFullyProcessed` is absent repo-wide (`grep -rn "subsFullyProcessed" contracts/` → 0;
    CONFIRMED-NEW, 349 authors it). Record the re-pinned anchors (matched line + text, drift noted) in the SUMMARY.
    If `git diff --numstat 20ca1f79 HEAD -- contracts/` is NON-empty, STOP and re-run the FULL 348-GREP-ATTESTATION
    (§5) + re-derive the code-size running-total before proceeding. The downstream plans (349-02..05) inherit your
    re-pinned anchors.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && git diff --numstat 20ca1f79 HEAD -- contracts/ | grep -c . | grep -qx 0 && grep -rn "subsFullyProcessed" contracts/ | grep -c . | grep -qx 0 && echo "BASELINE-CLEAN subsFullyProcessed-ABSENT"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --numstat 20ca1f79 HEAD -- contracts/` returns EMPTY (live tree == SPEC baseline) — attested in SUMMARY
    - `grep -rn "subsFullyProcessed" contracts/` returns 0 (CONFIRMED-NEW; 349 authors it) — attested in SUMMARY
    - The re-pinned anchors for every symbol this phase touches are recorded (matched line + text); any drift noted with the actual line
    - No contract edit was written before this attestation ran (the re-grep is the FIRST action; if baseline non-empty, the full attestation + size re-derivation is re-run)
  </acceptance_criteria>
  <done>The re-grep attestation ran first and is recorded in the SUMMARY: baseline clean vs `20ca1f79`, `subsFullyProcessed` absent, every touched anchor re-pinned (drift, if any, noted with the actual line) for 349-02..05 to inherit.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: [R1 — RECLAIM FIRST] MOVE claimAffiliateDgnrs → DegenerusGameBingoModule.sol, leaving NO Game stub</name>
  <files>contracts/DegenerusGame.sol, contracts/modules/DegenerusGameBingoModule.sol</files>
  <read_first>
    - contracts/DegenerusGame.sol (re-pinned `claimAffiliateDgnrs` `:1553` — the full body: 4 cross-contract calls `affiliate.affiliateScore` / `affiliate.totalAffiliateScore` / `dgnrs.transferFromPool` / `coinflip.creditFlip`, `PriceLookupLib.priceForLevel`, the nested-mapping R/W `affiliateDgnrsClaimedBy`/`levelDgnrsAllocation`/`levelDgnrsClaimed`/`mintPacked_`, the deity-bonus branch; the `claimBingo` dispatch lane `:328-343`; `_revertDelegate` `:1051`)
    - contracts/modules/DegenerusGameBingoModule.sol (the destination module — inherits DegenerusGameStorage; mirror an existing module entrypoint shape)
    - 348-CODE-SIZE-PLAN.md §2 R1 row + §5 hand-off note 1 (R1 is the FIRST edit; wire on GAME_BINGO_MODULE; callable directly → NO Game stub = true void) + §0 (the BingoModule already hosts claimBingo, same lane)
    - 348-IMPL-EDIT-ORDER-MAP.md §3 Step 1a (R1 zero-risk void, 0 external solidity callers)
  </read_first>
  <action>
    Relocate `claimAffiliateDgnrs` (re-pinned `DegenerusGame.sol:1553`) into
    `contracts/modules/DegenerusGameBingoModule.sol` as a public/external function on that module (which already
    inherits `DegenerusGameStorage`, so it operates on the same state in-context). MOVE it verbatim — same
    signature `claimAffiliateDgnrs(address player)`, same 4 cross-contract calls, same `PriceLookupLib.priceForLevel`
    use, same nested-mapping reads/writes, same deity-bonus branch. This is a RELOCATION, not a rewrite: byte-preserve
    the behavior. Because the BingoModule is reachable directly on the `GAME_BINGO_MODULE` address (the same lane the
    existing `claimBingo` uses, `:328-343`), leave **NO dispatch stub** in `DegenerusGame.sol` — DELETE the function
    body from the Game entirely (a true void; per 348-CODE-SIZE-PLAN §5 note 1 and §2 R1: 0 external solidity callers,
    so no Game-side indirection is needed). Remove any now-dead imports/`using` directives in `DegenerusGame.sol`
    that only `claimAffiliateDgnrs` referenced (confirm via grep before removing). If the BingoModule lacks an import
    the moved body needs (e.g. `PriceLookupLib`, an affiliate/coinflip/dgnrs interface), add it to the module. Do NOT
    change the function's logic, constants, or event emissions. This MUST be the first byte-affecting Game edit of the
    349 diff (running-total: 24,358 → ~23,158).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -c "function claimAffiliateDgnrs" contracts/DegenerusGame.sol | grep -qx 0 && grep -c "function claimAffiliateDgnrs" contracts/modules/DegenerusGameBingoModule.sol | grep -qx 1 && echo "R1-MOVED NO-GAME-STUB"</automated>
  </verify>
  <acceptance_criteria>
    - `function claimAffiliateDgnrs` appears 0 times in `contracts/DegenerusGame.sol` (moved out; NO stub left)
    - `function claimAffiliateDgnrs` appears exactly once in `contracts/modules/DegenerusGameBingoModule.sol`
    - The moved body is behavior-identical (same 4 cross-contract calls, nested-mapping R/W, deity-bonus branch) — a relocation, not a rewrite
    - Any now-dead import/`using` in DegenerusGame.sol that only this function referenced is removed (grep-confirmed unused)
    - The BingoModule has the imports the moved body needs (it compiles in 349-05's build gate)
  </acceptance_criteria>
  <done>`claimAffiliateDgnrs` is moved out of `DegenerusGame.sol` (0 occurrences, no stub) into `DegenerusGameBingoModule.sol` (1 occurrence) with behavior byte-preserved; dead Game imports removed; the reclaim is the first byte-affecting Game edit.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: [R2/R3 INSURANCE — conditional] Pull previewSellFarFutureTickets + the playerActivityScore wrapper to lens/drop-`view` ONLY if the running-total needs the margin</name>
  <files>contracts/DegenerusGame.sol</files>
  <read_first>
    - contracts/DegenerusGame.sol (re-pinned `previewSellFarFutureTickets` `:2113` + its sole-caller `_quoteFarFutureSwap` `MintStreakUtils.sol:97`; `playerActivityScore` `:2676` wrapper + the inherited `_playerActivityScore` `MintStreakUtils.sol:151/237`)
    - 348-CODE-SIZE-PLAN.md §2 R2/R3 rows + §4 Scenario A (the worst-case BREACH +82 and the R2+R3-wrapper restore to 24,418) + §5 notes 2/3/4 (a delegatecall stub CANNOT be `view`; R3 has a real 4-caller blast radius — keep the wrapper, do NOT retarget the 953 B here)
    - 348-IMPL-EDIT-ORDER-MAP.md §3 Step 1b (R2/R3 worst-case insurance; pull if Step 6's stub additions approach the ceiling)
  </read_first>
  <action>
    This task is CONDITIONAL insurance for the running-total — it MAY be deferred to 349-05 once the post-fold
    `forge build --sizes` is known. The recommended posture (per 348-CODE-SIZE-PLAN §4): in the simultaneous
    worst case (R1 reclaims only its low-end 1,200 B AND the stubs hit their high-end 1,500 B) R1 alone breaches
    by 82 B, so to guarantee a safe ≥158 B margin BEFORE the build is run, ALSO land here: (a) `previewSellFarFutureTickets`
    (re-pinned `:2113`) → drop its `view` mutability (off-chain `eth_call` ignores mutability) OR relocate it to a
    lens; because its SOLE caller is `_quoteFarFutureSwap` (`MintStreakUtils.sol:97`), relocating the wrapper also
    drops that inherited body from the Game image. (b) `playerActivityScore` (re-pinned `:2676`) → drop `view` /
    relocate the thin WRAPPER only (~120–200 B). ⚠ Do NOT retarget the 4 external callers (`WhaleModule:875`,
    `DecimatorModule:704`, `BurnieCoin:620`, `StakedDegenerusStonk:913`) and do NOT attempt the full 953 B reclaim
    here — the inherited `_playerActivityScore` body stays reachable through the external selector; the caller-retarget
    is a separate cost deferred unless 349-05's measured build still exceeds 24,576. A delegatecall stub CANNOT be
    `view`, so these read-aggregators MUST drop-`view`/→lens, NOT become dispatch stubs. If the executor instead
    chooses to defer ALL of R2/R3 to 349-05 (pending the real measurement), record that decision in the SUMMARY and
    leave `:2113`/`:2676` unchanged in this plan — either path is acceptable provided 349-05's build proves < 24,576.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -rc "IDegenerusGame(address(this)).playerActivityScore\|game.playerActivityScore\|degenerusGame.playerActivityScore" contracts/ | grep -v ":0" | wc -l | grep -qxE "[0-9]+" && echo "R3-CALLERS-INTACT (no retarget here)"</automated>
  </verify>
  <acceptance_criteria>
    - EITHER R2 + the R3 wrapper are dropped-`view`/relocated (the worst-case insurance landed), OR a recorded SUMMARY decision defers them to 349-05 pending the measured build — both acceptable
    - The R3 4 external callers (`WhaleModule`/`DecimatorModule`/`BurnieCoin`/`StakedDegenerusStonk`) are UNCHANGED (no caller-retarget in this plan; the 953 B retarget is not attempted here)
    - If R2/R3 are touched, they drop-`view`/→lens (NOT converted to delegatecall stubs — a stub cannot be `view`)
    - The decision (land-insurance vs defer-to-349-05) is recorded in the SUMMARY
  </acceptance_criteria>
  <done>The R2/R3 worst-case insurance is either landed (drop-`view`/lens on `:2113` + the `:2676` wrapper, callers untouched) or explicitly deferred to 349-05 pending the measured build, with the decision recorded in the SUMMARY; the R3 caller-retarget (953 B) is NOT done here.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DegenerusGame deploy → EIP-170 ceiling | The Game runtime must stay < 24,576 B at every intermediate step of the batched diff; only 218 B of measured headroom exists at `20ca1f79`. |
| claimAffiliateDgnrs callers → BingoModule | After R1, the function is reached directly on the GAME_BINGO_MODULE address (no Game stub); a behavior drift in the move would mis-account affiliate DGNRS. |
| SPEC anchors → live tree | Every 348 `file:line` is a `20ca1f79` snapshot; an edit citing a drifted line would patch the wrong region. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-349-01-CEIL | Denial of Service (deploy fails) | DegenerusGame runtime size | mitigate | R1 lands FIRST (Task 2), clearing the 218 B margin BEFORE any byte-adding edit; Task 3 lands R2/R3-wrapper insurance for the worst case; 349-05's `forge build --sizes` is the final proof < 24,576. |
| T-349-01-MOVE | Tampering (mis-account) | claimAffiliateDgnrs relocation | mitigate | The move is byte-preserving (same calls / mappings / deity branch); Task 2 verify asserts 0 occurrences in the Game + 1 in the module — a relocation, not a rewrite. |
| T-349-01-DRIFT | Repudiation (stale anchor) | the 348 SPEC anchors | mitigate | Task 1 re-pins every anchor vs the live tree before any edit; baseline confirmed EMPTY vs `20ca1f79`; drift (if any) is noted with the actual line for 349-02..05 to inherit. |
| T-349-01-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs (Solidity edits only; forge is the existing toolchain). |
</threat_model>

<verification>
- `git diff --numstat 20ca1f79 HEAD -- contracts/` was EMPTY before any edit (re-pin attestation, Task 1)
- `subsFullyProcessed` is absent repo-wide (CONFIRMED-NEW)
- `claimAffiliateDgnrs` is 0 in the Game / 1 in the BingoModule (R1 moved, no stub, behavior byte-preserved)
- R2/R3-wrapper insurance landed OR explicitly deferred to 349-05 (decision recorded); R3's 4 callers untouched
- NO `git commit` / `git add contracts/` was run by the executor (orchestrator commits post-approval)
</verification>

<success_criteria>
- The re-pin attestation ran FIRST and is recorded in the SUMMARY for 349-02..05 to inherit
- The code-size reclaim (R1) is the FIRST byte-affecting Game edit, clearing the headroom for the ARCH-03 stubs; the Game stays < 24,576 at this step
- claimAffiliateDgnrs relocated to BingoModule with no Game stub, behavior byte-preserved
- R2/R3-wrapper insurance landed or deferred-with-decision; the R3 953 B caller-retarget NOT attempted here
- Edits held at the contract-commit boundary (no executor commit)
</success_criteria>

<output>
Create `.planning/phases/349-impl-the-one-carefully-sequenced-batched-contract-diff-code-/349-01-SUMMARY.md` when done.
Record the re-pinned anchors (the 343/344 hand-off discipline) in the SUMMARY for downstream plans 349-02..05 to inherit.
</output>
