---
phase: 356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb
plan: 07
type: execute
wave: 2
depends_on: ["356-01", "356-02", "356-03", "356-04", "356-05", "356-06"]
files_modified:
  - test/REGRESSION-BASELINE-v56.md
autonomous: true
requirements: [SEC-01, SEC-02, LIVE-01, GAS-06]
must_haves:
  truths:
    - "At the v56 TST HEAD, the live forge test failing set minus the empirically-established 453f8073 baseline red union == empty (by NAME, never a bare count)"
    - "The 453f8073 baseline red union is established by an EMPIRICAL checkout + full-tree run (the v56 contract tree differs from 453f8073), not carried verbatim from the v55 ledger"
    - "The D-10 offset-migration red->green deltas are recorded as NARROWING (the 6555125 != 3774873600 garbage-read reds flipping green)"
    - "The SEC-02 leg-1 byte-diff anchor (the ETH/claimablePool debit unchanged vs 453f8073) is recorded"
    - "ZERO contract mutation during the phase; both the forge AND hardhat suites enumerated"
  artifacts:
    - path: "test/REGRESSION-BASELINE-v56.md"
      provides: "the BY-NAME NON-WIDENING ledger (the v55 structure cloned, baseline swapped to 453f8073)"
      contains: "453f8073"
      min_lines: 200
  key_links:
    - from: "the binding headline"
      to: "live forge test failing set - the 453f8073 baseline union"
      via: "set-diff BY NAME (the unseeded-[invariant] subset gate)"
      pattern: "live.*union.*=.*∅|live − union"
---

<objective>
Author `test/REGRESSION-BASELINE-v56.md` — the BY-NAME NON-WIDENING ledger (D-11). Clone the `test/REGRESSION-BASELINE-v55.md` structure verbatim (its 7 sections), swap the baseline anchor to `453f8073` (the v55 frozen subject), and establish the baseline red union EMPIRICALLY by checking out `453f8073`, patching, running the full `forge test --json` tree, and parsing the failing `(suite, test)` set BY NAME — because the v56 contract tree DIFFERS from `453f8073` (the IMPL diff + the 355 gas tune + `openBoxes` + the decouple are all post-`453f8073`), the v55 union is NOT carried verbatim. The binding headline: at the v56 TST HEAD, `live forge test failing set − the 453f8073 baseline red union == ∅` (the unseeded-`[invariant]` ⊆ relaxation, not strict equality). Record the D-10 offset-migration red->green deltas as NARROWING and the SEC-02 leg-1 byte-diff anchor.

Purpose: SC 5 — restore a clean v56.0 regression baseline against the FROZEN IMPL subject; prove the v56 redesign + the new proofs introduce no failing test outside the `453f8073` baseline. This is the gate for the 357 TERMINAL close.
Output: the doc-only NON-WIDENING ledger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-CONTEXT.md
@.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-RESEARCH.md
@.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-PATTERNS.md
@test/REGRESSION-BASELINE-v55.md

<interfaces>
<!-- The v55 ledger structure to clone (7 sections, verified this session): -->
  §1 TST-HEAD arithmetic + reconciliation
  §2 the baseline red union enumerated BY NAME (Buckets A VRF/RNG, B stale-harness, F flaky) — EMPIRICALLY established
  §3 NEW-vs-baseline deltas (rewrite map / DROPs / the NARROWING fixes) — here the §3 delta is the D-10 offset-migration red->green NARROWING
  §4 the unseeded DegeneretteBet.inv invariant cluster — the ⊆-gate rationale (NOT strict equality)
  §5 NEW vs the baseline — the v56 green proof files (V56SecUnmanipulable, V56FreezeSolvency, V56QuestNonPerturb, the extended V56AfkingGasMarginal)
  §6 net-zero-new-regression PROOF (the ⊆ gate + the FC1-FC6 false-confidence guards)
  §7 scope attestation + the Hardhat compile-sanity arm

The empirical-checkout method (Pattern 3 — the v56 contract tree DIFFERS from 453f8073):
  node scripts/lib/patchForFoundry.js              # predict CREATE addrs (no pretest hook)
  forge test --json                                # WHOLE tree, NOT --match-path
  git checkout -- contracts/ContractAddresses.sol  # restore frozen (sha256 80fe0dac…)
  # then: git worktree/stash to 453f8073, patch, forge test --json, parse the failing (suite,test) union BY NAME, restore, return to HEAD

The binding headline (clone the v55 wording, swap the baseline):
  "at the v56 TST HEAD, every forge test failing test ∈ the empirically-established 453f8073 baseline red union BY NAME —
   live failing set − union == ∅ — net-zero new regression."

The ⊆ relaxation (Pitfall 5): foundry.toml seeds [fuzz] (seed=0xdeadbeef) but the [invariant] block is UNSEEDED ->
  DegeneretteBet.inv::invariant_solvencyUnderDegenerette is flaky -> use the SUBSET gate (live − union == ∅), NOT strict equality.

Enumerate BOTH the forge AND hardhat suites (v55's ledger was 603/134/16 spanning both).
The SEC-02 leg-1 byte-diff anchor (from 356-04): git diff 453f8073 HEAD -- contracts/modules/GameAfkingModule.sol shows the
  SOLVENCY-01 debit site :744-745 unchanged (afkingFunding[src] -= ethValue; claimablePool -= uint128(ethValue)).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the v56 TST-HEAD whole-tree + empirically establish the 453f8073 baseline red union BY NAME</name>
  <read_first>
    - test/REGRESSION-BASELINE-v55.md (the full ledger — the §1-§7 structure, the empirical-checkout method §2/§6, the ⊆-gate rationale §4, the FC1-FC6 guards §6)
    - scripts/lib/patchForFoundry.js (the CREATE-address predictor; no pretest hook)
    - .planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-RESEARCH.md (Pattern 3 empirical-checkout, Pitfall 5/6, the toolchain availability)
  </read_first>
  <action>
    At the v56 TST HEAD (after 356-01..06 land): run `node scripts/lib/patchForFoundry.js`, then `forge test --json` over the WHOLE tree (NOT --match-path), capture the live failing (suite, test) set BY NAME, then `git checkout -- contracts/ContractAddresses.sol`. Then establish the baseline union: use a git worktree or stash to check out `453f8073` (the v55 frozen subject), patch, run `forge test --json` over its full tree, parse the failing (suite, test) union BY NAME, restore ContractAddresses.sol, and return to HEAD. Do NOT carry the v55 148-name union verbatim — the v56 contract tree differs (the IMPL diff + gas tune + openBoxes + decouple are post-453f8073), so the union MUST be re-derived empirically. Also capture the Hardhat compile-sanity arm: `npx hardhat compile` EXIT 0 (the sanity arm, NOT the primary ledger). Record the raw (suite,test) name sets for both HEADs (this task produces the data; Task 2 writes the ledger). Do NOT run `hardhat compile --force` (Pitfall 6 — regenerates ContractAddresses.sol). Verify sha256 of ContractAddresses.sol after each restore.
  </action>
  <verify>
    <automated>git diff --quiet HEAD -- contracts/ && echo "CONTRACTS_FROZEN_OK"; node scripts/lib/patchForFoundry.js >/dev/null 2>&1; forge test --json > /tmp/v56_head_run.json 2>/dev/null; git checkout -- contracts/ContractAddresses.sol; test -s /tmp/v56_head_run.json && echo "HEAD_RUN_CAPTURED"</automated>
  </verify>
  <acceptance_criteria>
    The v56 TST-HEAD whole-tree `forge test --json` run is captured (a non-empty JSON with the failing (suite,test) names); the `453f8073` baseline run is captured BY NAME (via worktree/stash checkout); `npx hardhat compile` exits 0 (sanity arm); `git diff --quiet HEAD -- contracts/` exits 0 after every restore (ZERO contract mutation, ContractAddresses.sol restored byte-identical sha256 80fe0dac…).
  </acceptance_criteria>
  <done>Both the v56-HEAD live failing set and the empirical 453f8073 baseline union are captured BY NAME; the Hardhat compile-sanity arm passed; ContractAddresses.sol restored at HEAD.</done>
</task>

<task type="auto">
  <name>Task 2: Author REGRESSION-BASELINE-v56.md — the BY-NAME ⊆ gate + the D-10 NARROWING + the SEC-02 byte-diff anchor</name>
  <read_first>
    - test/REGRESSION-BASELINE-v55.md (clone its 7-section structure verbatim; swap the baseline 20ca1f79 -> 453f8073)
    - the Task-1 captured name sets (/tmp/v56_head_run.json + the 453f8073 union)
    - .planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-PATTERNS.md (the ledger section map + the binding headline wording)
  </read_first>
  <action>
    Write `test/REGRESSION-BASELINE-v56.md` cloning the v55 7-section structure: §1 the v56 TST-HEAD arithmetic + reconciliation (the live forge counts spanning forge AND hardhat, like v55's 603/134/16); §2 the empirically-established 453f8073 baseline red union enumerated BY NAME (Buckets A VRF/RNG, B stale-harness, F flaky) — with the empirical-checkout method documented; §3 the NEW-vs-baseline deltas — here the §3 delta is the D-10 offset-migration red->green NARROWING (the ~13 `6555125 != 3774873600` garbage-read reds across the 10 migrated files flipping green, recorded as a NARROWING never a widening); §4 the unseeded DegeneretteBet.inv ⊆-gate rationale (carry the v55 ⊆ relaxation, NOT strict equality — Pitfall 5); §5 the v56 green proof files (V56SecUnmanipulable, V56FreezeSolvency, V56QuestNonPerturb, the extended V56AfkingGasMarginal — all GREEN, contribute zero red); §6 the net-zero-new-regression PROOF (the ⊆ gate `live − union == ∅` BY NAME + the FC1-FC6 false-confidence guards); §7 the scope attestation (ZERO contracts/*.sol mutation — `git diff 453f8073 HEAD -- contracts/` is the committed v56 IMPL diff, 356 added nothing) + the Hardhat compile-sanity arm + the SEC-02 leg-1 byte-diff anchor (`git diff 453f8073 HEAD -- contracts/modules/GameAfkingModule.sol` shows the debit site :744-745 unchanged). State the binding headline with the actual v56 counts. If the set-diff is NOT empty (a live red NOT in the baseline union), STOP — that is a genuine new regression to triage, NOT a ledger to finalize.
  </action>
  <verify>
    <automated>test -f test/REGRESSION-BASELINE-v56.md && grep -qE "453f8073" test/REGRESSION-BASELINE-v56.md && grep -qiE "live.*union.*∅|live − union == ∅|net-zero" test/REGRESSION-BASELINE-v56.md && echo "LEDGER_OK"; git diff --quiet HEAD -- contracts/ && echo "CONTRACTS_FROZEN_OK"</automated>
  </verify>
  <acceptance_criteria>
    `test/REGRESSION-BASELINE-v56.md` exists with all 7 sections, anchored on `453f8073`; the binding headline states `live failing set − the 453f8073 union == ∅` BY NAME with the actual v56 counts (spanning forge AND hardhat); §3 records the D-10 offset-migration red->green as NARROWING; §4 carries the ⊆ relaxation; §7 records ZERO contract mutation + the Hardhat compile-sanity arm + the SEC-02 leg-1 byte-diff anchor; `git diff --quiet HEAD -- contracts/` exits 0.
  </acceptance_criteria>
  <done>The NON-WIDENING ledger is authored BY NAME against the empirical 453f8073 union with the ⊆ gate; the D-10 narrowing + the SEC-02 byte-diff anchor are recorded; net-zero new regression proven; ZERO contract mutation attested.</done>
</task>

</tasks>

<threat_model>
## The vectors this test plan proves closed (the NON-WIDENING gate — SEC corollary across all 4 reqs)

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the v56 redesign + the new proofs -> the regression baseline | A silent new regression (a real failing test coinciding with a narrowing fix) would be masked by a bare-count gate; the v56 contract tree differs from 453f8073 so a carried-verbatim union would be wrong. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-356-07-WIDEN | Tampering (regression integrity) | the NON-WIDENING gate | mitigate | The gate is a NAME-set membership test (`live − union == ∅`), NOT a bare count (Pitfall: a bare count masks a new red coinciding with a narrowing). |
| T-356-07-BASE | Tampering | the baseline union | mitigate | The union is established EMPIRICALLY by checking out 453f8073 + full-tree run (the v56 tree differs); a carried-verbatim v55 union would be wrong (Pattern 3). |
| T-356-07-FLAKY | (accepted/relaxed) | the unseeded DegeneretteBet.inv cluster | accept | The [invariant] block is unseeded -> flaky run-to-run; carry the ⊆ relaxation (live ⊆ union), NOT strict equality (Pitfall 5). |
| T-356-07-FROZEN | Tampering | the audit subject byte-freeze | mitigate | §7 attests ZERO contracts/*.sol mutation (`git diff 453f8073 HEAD -- contracts/` is the committed v56 IMPL diff; 356 added nothing); ContractAddresses.sol restored each round-trip. |
| T-356-07-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (vacuously satisfied). |
</threat_model>

<verification>
- `test/REGRESSION-BASELINE-v56.md` exists with the 7-section structure anchored on 453f8073.
- The binding headline: `live forge test failing set − the empirical 453f8073 baseline red union == ∅` BY NAME (the ⊆ gate).
- The D-10 offset-migration red->green NARROWING is recorded; the SEC-02 leg-1 byte-diff anchor is recorded.
- Both forge AND hardhat suites enumerated; the Hardhat compile-sanity arm passed (EXIT 0).
- ZERO `contracts/*.sol` mutation (the §7 attestation + `git diff --quiet HEAD -- contracts/` exits 0).
</verification>

<success_criteria>
- The NON-WIDENING ledger is authored BY NAME with the empirical 453f8073 union and the ⊆ relaxation.
- Net-zero new regression proven (`live − union == ∅`); if not empty, the new red is triaged, not the ledger finalized.
- The D-10 narrowing + the SEC-02 byte-diff anchor + the ZERO-contract-mutation attestation are recorded.
- No contract file touched.
</success_criteria>

<output>
Create `.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-07-SUMMARY.md` when done
</output>
