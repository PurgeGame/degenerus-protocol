---
phase: 356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb
plan: 05
type: execute
wave: 1
depends_on: ["356-02"]
files_modified:
  - test/fuzz/V56QuestNonPerturb.t.sol
autonomous: true
requirements: [SEC-01]
must_haves:
  truths:
    - "Slot-1 (the player's own random/manual quest) stays fully accessible every day during afking"
    - "A slot-1 completion during afking is STREAK-NEUTRAL — it does NOT advance the afking compute-on-read streak (afkingActive gates it)"
    - "For a NON-afking player, slot-1 advances the streak normally"
    - "awardQuestStreakBonus and the manual/bingo/degenerette/boon callers produce byte-identical results with afking subs present vs absent"
  artifacts:
    - path: "test/fuzz/V56QuestNonPerturb.t.sol"
      provides: "QST-04 slot-1 streak-neutral + cross-caller byte-identity proof"
      contains: "contract V56QuestNonPerturb"
      min_lines: 150
  key_links:
    - from: "the slot-1 streak-neutral test"
      to: "the afkingActive flag gating the bump (DegenerusQuests:283/441/471/481)"
      via: "beginAfking sets afkingActive; slot-1 bump checks it"
      pattern: "afkingActive|beginAfking"
    - from: "the cross-caller byte-identity test"
      to: "awardQuestStreakBonus (DegenerusQuests:378)"
      via: "same inputs with/without afking subs present -> identical state"
      pattern: "awardQuestStreakBonus"
---

<objective>
Author `test/fuzz/V56QuestNonPerturb.t.sol` — the QST-04 (D-04) FULL empirical quest-core non-perturbation proof: (a) slot-1 (the player's own random/manual quest) stays fully accessible every day during afking AND is STREAK-NEUTRAL (a slot-1 completion during afking must NOT advance the afking compute-on-read streak — the `afkingActive` flag gates it; for a NON-afking player slot-1 advances normally); (b) the manual/bingo/degenerette/boon quest callers (`awardQuestStreakBonus` etc.) produce byte-identical results with afking subs present vs absent — the new `beginAfking` + finalize-write entrypoints added to the shared DegenerusQuests core do not perturb the other callers. Reuse the funded-sub fixture from the migrated `V55SetMutationOpenE` + direct DegenerusQuests state reads.

Purpose: D-04 mandates FULL empirical coverage in 356 (not deferred to 357). The shared-core boundary (the new afking entrypoints vs the pre-existing quest callers) is the risk; this proves the addition is non-perturbing and the slot-1 streak dodge stays closed.
Output: one new forge suite proving QST-04 empirically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-CONTEXT.md
@.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-RESEARCH.md
@.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-PATTERNS.md

<interfaces>
<!-- Shipped DegenerusQuests surface under test (read-only; verified anchors). -->

DegenerusQuests.sol:
  beginAfking(player, currentDay) :432 -- sets state.afkingActive = true (:441), returns the snapshot streak; does NOT touch slot-1.
  awardQuestStreakBonus(player, uint16 amount, uint32 currentDay) :378 onlyGame -- the shared manual/bingo/degenerette/boon caller.
  afkingActive flag :283 -- "While set: slot-0 completions are streak-neutral and pay no immediate reward".
  finalizeAfking(player, uint24 earnedStreak, uint32 afkingCoveredDay, uint32 currentDay) :463-483 onlyGame:
    - idempotent (`if (!state.afkingActive) return;`)
    - funding-kill guard :474 `lastValid = max(afkingCoveredDay, state.lastActiveDay); finalStreak = (currentDay==0 || lastValid+1 >= currentDay) ? earned : 0`
    - Pitfall 4: the guard is `lastValid + 1 >= currentDay` (NOT `<= currentDay-2`); a sub that lapsed afking but kept
      minting MANUALLY (bumps lastActiveDay) is NOT wrongly zeroed (D-02.4).
  the O1 lootbox-quest double-credit was FIXED in the v56 IMPL (region :880-900) -- its single-credit regression is
    covered here (the slot-1/streak-neutral + the cross-caller byte-identity should not re-introduce a double-credit).

The two D-04 properties:
  (a) slot-1 accessible-every-day + streak-neutral-during-afking (afkingActive gates the bump);
      NON-afking player: slot-1 advances normally.
  (b) awardQuestStreakBonus & the manual/bingo/degenerette/boon callers byte-identical with afking present vs absent.

The v56 Sub-slot offset block: copy from V56AfkingGasMarginal:68-89.
PlayerQuestState reads: probe state.streak / state.afkingActive / state.lastActiveDay via the questPlayerState mapping
  (confirm the slot + field offsets via `forge inspect DegenerusQuests storageLayout` against HEAD).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Slot-1 streak-neutrality during afking + slot-1 accessibility + NON-afking normal advance</name>
  <behavior>
    - During afking (afkingActive true), a slot-1 (own random/manual) quest completion does NOT advance the afking compute-on-read streak.
    - Slot-1 stays fully accessible every day during afking (the completion succeeds; only the streak bump is suppressed).
    - For a NON-afking player, the same slot-1 completion advances the streak normally (the gate is afkingActive, not a global suppression).
  </behavior>
  <read_first>
    - test/fuzz/V55SetMutationOpenE.t.sol (the funded-sub + deity-pass + new-day STAGE harness ~386-423, the Sub reads — NOW v56-migrated by 356-02)
    - test/gas/V56AfkingGasMarginal.t.sol:68-89,565,579-614 (the offset block, _grantDeityPass, the _settleGame harness)
    - contracts/DegenerusQuests.sol:283,432,441,471,481 (the afkingActive flag + beginAfking + the slot-1-streak-neutral gate)
    - test/fuzz/helpers/DeployProtocol.sol (the fixture)
  </read_first>
  <action>
    Create `test/fuzz/V56QuestNonPerturb.t.sol` (contract `V56QuestNonPerturb is DeployProtocol`). Copy the v56 offset block + the funded-sub/deity-pass/new-day-STAGE harness from the migrated V55SetMutationOpenE. Confirm the PlayerQuestState slot + field offsets via `forge inspect DegenerusQuests storageLayout`. Test (a): subscribe a player into afking (afkingActive true via beginAfking on the subscribe-day STAGE), complete a slot-1 quest, and assert `state.streak` is UNCHANGED (streak-neutral); assert the slot-1 completion itself succeeded (accessibility). Test the converse: a NON-afking player completes the same slot-1 quest and assert `state.streak` advanced normally. Drive the day/STAGE machinery via _settleGame. Probe state.streak / state.afkingActive directly via vm.load on questPlayerState. Seeded-fuzz determinism; lean comments only.
  </action>
  <verify>
    <automated>node scripts/lib/patchForFoundry.js >/dev/null 2>&1; forge test --match-contract V56QuestNonPerturb --match-test "Slot1|StreakNeutral|Accessible|NonAfking" -vv 2>&1 | grep -E "Suite result|PASS|FAIL"; git checkout -- contracts/ContractAddresses.sol</automated>
  </verify>
  <acceptance_criteria>
    `forge test --match-contract V56QuestNonPerturb --match-test "Slot1|StreakNeutral|Accessible|NonAfking"` exits 0; the during-afking slot-1 completion asserts `state.streak` unchanged (exact equality before/after); the slot-1 completion succeeds (accessible); the NON-afking player's slot-1 completion asserts `state.streak` advanced; `git diff --quiet HEAD -- contracts/` exits 0.
  </acceptance_criteria>
  <done>Slot-1 is streak-neutral during afking and fully accessible; a NON-afking player's slot-1 advances normally; the C3-a non-funded streak dodge stays closed; no contract touched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cross-caller byte-identity — awardQuestStreakBonus + manual/bingo/degenerette/boon with afking present vs absent</name>
  <behavior>
    - awardQuestStreakBonus produces byte-identical PlayerQuestState for a target player whether or not OTHER players hold afking subs (the new shared-core entrypoints do not perturb the existing callers).
    - The manual/bingo/degenerette/boon quest-reward paths produce byte-identical results in the two worlds (afking subs present vs absent).
    - The O1 fix holds: a completed lootbox quest credits its reward ONCE (no double-credit re-introduced by the shared-core changes).
  </behavior>
  <read_first>
    - contracts/DegenerusQuests.sol:378 (awardQuestStreakBonus onlyGame), :880-900 (the O1 region — single-credit), :463-483 (finalizeAfking idempotency)
    - test/fuzz/V55SetMutationOpenE.t.sol (the snapshot/revert + state-diff pattern; the funded-sub harness — NOW v56-migrated)
    - test/gas/V56AfkingGasMarginal.t.sol:181-208 (the vm.snapshotState/vm.revertToState idiom — the two-world comparison uses the same snapshot-baseline approach)
    - test/fuzz/helpers/DeployProtocol.sol
  </read_first>
  <action>
    Extend `V56QuestNonPerturb.t.sol`. Test (b) the two-world byte-identity: from one clean snapshot, run a target player's quest-reward path (awardQuestStreakBonus + a bingo/degenerette/boon caller) in WORLD-A (no afking subs present); vm.revertToState; set up WORLD-B (other players hold afking subs, beginAfking called); run the identical target path; assert the target player's resulting PlayerQuestState (streak, lastActiveDay, the reward credit) is byte-identical between the two worlds (read the full PlayerQuestState slots via vm.load and compare). Add the O1 single-credit assertion: a completed lootbox quest credits its BURNIE reward exactly once across the two worlds. Use vm.snapshotState/vm.revertToState (NOT a linear re-run — Pattern 1). Seeded-fuzz determinism; lean comments only.
  </action>
  <verify>
    <automated>node scripts/lib/patchForFoundry.js >/dev/null 2>&1; forge test --match-contract V56QuestNonPerturb --match-test "ByteIdent|NonPerturb|CrossCaller|Bingo|Degenerette|Boon|O1|SingleCredit" -vv 2>&1 | grep -E "Suite result|PASS|FAIL"; git checkout -- contracts/ContractAddresses.sol</automated>
  </verify>
  <acceptance_criteria>
    `forge test --match-contract V56QuestNonPerturb --match-test "ByteIdent|NonPerturb|CrossCaller|Bingo|Degenerette|Boon|O1|SingleCredit"` exits 0; the two-world test asserts the target player's PlayerQuestState is byte-identical (a slot-by-slot equality) with afking present vs absent; the O1 single-credit assertion confirms exactly one credit; `git diff --quiet HEAD -- contracts/` exits 0.
  </acceptance_criteria>
  <done>awardQuestStreakBonus + the manual/bingo/degenerette/boon callers are byte-identical with afking present vs absent; the O1 single-credit holds; the shared core is proven non-perturbing; no contract touched.</done>
</task>

</tasks>

<threat_model>
## The vectors this test plan proves closed (QST-04 / SEC-01 corollary — shared-core non-perturbation)

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| afking entrypoints -> shared DegenerusQuests core | The new beginAfking + finalize-write entrypoints share PlayerQuestState with the manual/bingo/degenerette/boon callers; a perturbation would silently alter the other callers' rewards. |
| slot-1 (own quest) -> the afking compute-on-read streak | A cheap slot-1 completion during afking that advanced the streak would re-open the C3-a non-funded streak dodge. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-356-05-SLOT1 | Elevation (econ) | the slot-1 streak-neutral gate | mitigate | Test (a): afkingActive gates the bump (:283/441) — slot-1 during afking is streak-neutral; NON-afking advances normally. Assert state.streak unchanged during afking. |
| T-356-05-PERTURB | Tampering | the shared DegenerusQuests core | mitigate | Test (b): two-world byte-identity (afking present vs absent) on awardQuestStreakBonus + the manual/bingo/degenerette/boon callers. |
| T-356-05-O1 | Tampering | the O1 lootbox-quest credit | mitigate | The O1 double-credit was FIXED in v56; assert single-credit holds across the two worlds (the shared-core changes do not re-introduce it). |
| T-356-05-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (vacuously satisfied). |
</threat_model>

<verification>
- `forge build` EXIT 0.
- `forge test --match-contract V56QuestNonPerturb` green (slot-1 streak-neutral + accessibility + NON-afking advance + cross-caller byte-identity + O1 single-credit).
- ZERO `contracts/*.sol` mutation (ContractAddresses.sol restored each round-trip).
</verification>

<success_criteria>
- Slot-1 is streak-neutral during afking and fully accessible; a NON-afking player's slot-1 advances normally.
- awardQuestStreakBonus + the manual/bingo/degenerette/boon callers are byte-identical with afking present vs absent.
- The O1 single-credit holds.
- No contract file touched.
</success_criteria>

<output>
Create `.planning/phases/356-tst-unmanipulable-strategic-sub-unsub-quest-core-non-perturb/356-05-SUMMARY.md` when done
</output>
