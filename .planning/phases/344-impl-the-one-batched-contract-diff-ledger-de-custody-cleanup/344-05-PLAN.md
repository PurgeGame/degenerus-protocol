---
phase: 344-impl-the-one-batched-contract-diff-ledger-de-custody-cleanup
plan: 05
type: execute
wave: 5
depends_on: ["344-04"]
files_modified:
  - contracts/DegenerusVault.sol
  - contracts/StakedDegenerusStonk.sol
autonomous: false
requirements: [DECUSTODY-04, CLEANUP-02, BATCH-02]
must_haves:
  truths:
    - "An actor-consequence trace (D-344-01) is documented BEFORE the v48-recovery deletions, confirming recoverability for BOTH VAULT and sDGNRS; ESCALATES on any gap"
    - "DegenerusVault.recoverAfKingPool() is removed (0 external callers)"
    - "The StakedStonk.burnAtGameOver AfKing-withdraw leg is removed; burnAtGameOver body otherwise stays"
    - "The sDGNRS receive() AF_KING relaxation is narrowed to GAME-only"
    - "The full CLEANUP-01 kill-set is grep-confirmed empty (no orphaned references)"
    - "forge build is clean across the whole batched 344 diff; then HOLD at the contract-commit boundary"
  artifacts:
    - path: "contracts/DegenerusVault.sol"
      provides: "recoverAfKingPool removed"
    - path: "contracts/StakedDegenerusStonk.sol"
      provides: "AfKing-withdraw leg removed + receive() narrowed to GAME-only"
    - path: ".planning/phases/344-impl-the-one-batched-contract-diff-ledger-de-custody-cleanup/344-ACTOR-CONSEQUENCE-TRACE.md"
      provides: "the D-344-01 recoverability trace for VAULT + sDGNRS"
  key_links:
    - from: "v48 recovery legs (DegenerusVault:517 + StakedStonk:539)"
      to: "AfKing.poolOf / AfKing.withdraw (deleted in 344-04)"
      via: "D-06 kill order — callers removed before/with the views, ONE diff"
      pattern: "afKing\\.withdraw\\(afKing\\.poolOf"
    - from: "game.withdrawKeeperFunding send-back"
      to: "VAULT.receive() + sDGNRS.receive() (GAME-allowed)"
      via: "the recoverability replacement the trace confirms"
      pattern: "ContractAddresses.GAME"
---

<objective>
Step 5 (terminal) of the producer-before-consumer edit-order map (343-IMPL-EDIT-ORDER-MAP.md Section 4 Step 5):
remove the now-moot v48 stuck-pool recovery legs (the D-06 kill order), narrow the sDGNRS `receive()` to
GAME-only, verify the full CLEANUP-01 kill-set is grep-confirmed empty, run the `forge build` gate across the
entire batched 344 diff, and HOLD at the contract-commit boundary for explicit user hand-review.

⚠ This plan contains the D-344-01 [BLOCKING] actor-consequence trace, which MUST run and document
recoverability for BOTH VAULT and sDGNRS BEFORE any deletion. If the trace finds a recoverability gap, ESCALATE
(do NOT delete the safety code on the grep-orphan proof alone). The 343 inventory already proved grep-orphan /
no-caller; D-344-01 adds the required actor-consequence layer.

⚠ D-06 KILL ORDER: these recovery legs are the SOLE callers of `AfKing.withdraw`/`AfKing.poolOf` (deleted in
344-04). In this SINGLE batched diff, authoring order satisfies the gate — the legs are removed before/with the
view deletions; no intermediate state holds a dangling reference. The `forge build` here is the FIRST and ONLY
build of the whole diff (344-01..05) and proves the D-06 gate held (no broken build).

⚠ D-344-07: `forge build`-clean ONLY, then HOLD. The ABI change (non-payable `batchPurchase`, `BatchBuy.funder`,
deleted AfKing `deposit`/`withdraw`/`poolOf`/`receive`) WILL break existing TEST-FILE compilation — that is
EXPECTED and is 346 TST's charge. 344 does NOT run `forge test` and does NOT touch `test/`. `forge build`
compiles `contracts/` (sources), not the test tree, so it is the correct gate.

Output: Edits applied to `contracts/DegenerusVault.sol` + `contracts/StakedDegenerusStonk.sol` + the
actor-consequence trace doc. The whole 344 diff compiles. Then HOLD — NO COMMIT.

⚠ CONTRACT-BOUNDARY HARD STOP. `autonomous: false`. The executor NEVER runs `git commit` / `git add contracts/`
(hook-enforced). The orchestrator owns ALL commits, deferred past the single user-approval gate, and splits the
batched diff into atomic per-plan commits AFTER approval (D-344-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-IMPL-EDIT-ORDER-MAP.md
@.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-CLEANUP-INVENTORY.md
@.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-SOLVENCY-PROOF.md
@.planning/phases/344-impl-the-one-batched-contract-diff-ledger-de-custody-cleanup/344-CONTEXT.md

<interfaces>
<!-- Re-pinned anchors inherited from 344-01 Task 1. SPEC-attested starting refs, confirmed against the live tree. -->

v48 recovery legs (attested @ 83a84431; re-pin before editing):
- DegenerusVault.sol `:516` — `function recoverAfKingPool() external {` body `:517` `afKing.withdraw(afKing.poolOf(address(this)));`
  (`grep -rn 'recoverAfKingPool' contracts/` → 1 hit, only its own def — 0 external callers)
- StakedDegenerusStonk.sol `:535` — `function burnAtGameOver() external onlyGame {`; AfKing leg `:539`
  `afKing.withdraw(afKing.poolOf(address(this)));` (the leg is ONE statement; the fn body otherwise STAYS — it burns sDGNRS)
- StakedDegenerusStonk.sol `receive()` `:439-445`:
    `receive() external payable {`
    `    if (`
    `        msg.sender != ContractAddresses.GAME &&`
    `        msg.sender != ContractAddresses.AF_KING`   ← `:442` the dead branch after the leg removal
    `    ) revert Unauthorized();`
    `    emit Deposit(msg.sender, msg.value, 0, 0);`
    `}`

Recoverability-trace anchors (the replacement the trace confirms):
- VAULT `receive()` `:508` — OPEN (no sender gate): `receive() external payable { emit Deposit(...); }` → accepts the Game's withdraw send-back
- sDGNRS `receive()` `:441` — allows `ContractAddresses.GAME` → accepts the Game's withdraw send-back (msg.sender == GAME)
- game.withdrawKeeperFunding (344-02) — ungated send-back to `msg.sender` (= VAULT or sDGNRS when they call it)
- _claimWinningsInternal Decision-B merge (344-02) — the post-gameOver alternative recovery path

The full CLEANUP-01 kill-set (14 items) to grep-confirm empty: see 343-CLEANUP-INVENTORY.md Section 2.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING — D-344-01] Actor-consequence trace BEFORE deleting the v48 recovery legs; ESCALATE on any gap</name>
  <files>.planning/phases/344-impl-the-one-batched-contract-diff-ledger-de-custody-cleanup/344-ACTOR-CONSEQUENCE-TRACE.md</files>
  <read_first>
    - 344-CONTEXT.md D-344-01 (the actor-consequence trace floor; the three replacement legs (a/b/c); the explicit VAULT-can-receive + sDGNRS-GAME-allowance confirmations; ESCALATE on gap)
    - 343-SOLVENCY-PROOF.md Section D (OPEN-E: funding-source ETH now keeperFunding[src], withdrawable by the source)
    - contracts/DegenerusVault.sol `:508` (VAULT receive() openness) + `:516-517` (recoverAfKingPool)
    - contracts/StakedDegenerusStonk.sol `:439-445` (receive() GAME/AF_KING) + `:535-539` (burnAtGameOver + AfKing leg)
    - [[v48-afking-pool-recovery]] — the ORIGINAL design intent of the v48 recovery legs (the trace input)
  </read_first>
  <action>
    BEFORE deleting any v48 recovery code, author
    `.planning/phases/344-impl-the-one-batched-contract-diff-ledger-de-custody-cleanup/344-ACTOR-CONSEQUENCE-TRACE.md`
    proving (per feedback_design_intent_before_deletion) that the v48 stuck-pool recovery is FULLY REPLACED for
    BOTH VAULT and sDGNRS by the de-custody. The trace MUST cover, for each of VAULT and sDGNRS:
    (a) ungated `game.withdrawKeeperFunding` is now the live recovery path (funding-source ETH lives in
        `keeperFunding[VAULT]` / `keeperFunding[SDGNRS]`, withdrawable by the source);
    (b) the Game's withdraw `.call` send-back is RECEIVABLE — confirm VAULT.receive() (`:508`, OPEN) accepts it,
        AND sDGNRS.receive() (`:441`, GAME-allowed) accepts it because the send-back has `msg.sender == GAME`;
    (c) the Decision-B `claimWinnings` keeper-merge (344-02) is the post-gameOver alternative.
    The trace MUST explicitly state the conclusion: recoverability is preserved for BOTH actors, OR a gap exists.
    ⚠ If the trace finds a recoverability GAP, ESCALATE (stop; do NOT delete the safety code — Task 2 is gated on
    this conclusion) — the grep-orphan proof alone is NOT sufficient to delete. This is a blocking human-verify
    checkpoint: pause for the user to confirm the trace before the deletions proceed.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && D=".planning/phases/344-impl-the-one-batched-contract-diff-ledger-de-custody-cleanup/344-ACTOR-CONSEQUENCE-TRACE.md"; test -f "$D" && grep -qi "VAULT" "$D" && grep -qi "SDGNRS\|sDGNRS" "$D" && grep -qi "recoverab" "$D" && echo "TRACE-PRESENT"</automated>
  </verify>
  <how-to-verify>
    1. Confirm the trace doc exists and covers VAULT + sDGNRS for legs (a), (b), (c) above.
    2. Confirm VAULT.receive() (`contracts/DegenerusVault.sol:508`) has no sender gate (accepts the Game send-back).
    3. Confirm sDGNRS.receive() (`contracts/StakedDegenerusStonk.sol:441`) allows `ContractAddresses.GAME`
       (the Game's withdraw `.call` is `msg.sender == GAME`).
    4. Confirm the trace's conclusion is "recoverability preserved for BOTH" (not "gap found").
    5. If the trace concludes a GAP, do NOT approve deletion — re-plan / escalate.
  </how-to-verify>
  <acceptance_criteria>
    - `344-ACTOR-CONSEQUENCE-TRACE.md` exists and documents legs (a), (b), (c) for BOTH VAULT and sDGNRS
    - The trace explicitly confirms VAULT.receive() accepts the Game send-back AND sDGNRS.receive()'s GAME-allowance permits it
    - The trace's conclusion is "recoverability preserved" (a "gap" conclusion blocks Task 2 and escalates)
    - This task is authored/verified BEFORE any v48-recovery deletion (Task 2)
  </acceptance_criteria>
  <done>The actor-consequence trace doc exists, covers VAULT + sDGNRS for legs (a)(b)(c), and concludes "recoverability preserved" (user-approved); OR a gap was found and escalated (Task 2 does not proceed).</done>
  <resume-signal>Type "approved" once the trace confirms recoverability for both VAULT and sDGNRS; or describe the gap to escalate.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove the v48 recovery legs (D-06 kill order) + narrow sDGNRS receive() to GAME-only</name>
  <files>contracts/DegenerusVault.sol, contracts/StakedDegenerusStonk.sol</files>
  <read_first>
    - contracts/DegenerusVault.sol (re-pinned `recoverAfKingPool` `:516-517`)
    - contracts/StakedDegenerusStonk.sol (re-pinned `burnAtGameOver` `:535` + AfKing leg `:539`; `receive()` `:439-445`, AF_KING branch `:442`)
    - 343-IMPL-EDIT-ORDER-MAP.md Section 4 Step 5 (5a Vault leg, 5b sDGNRS leg, 5c receive() narrow) + the D-06 INTEGRITY GATE note
    - 343-CLEANUP-INVENTORY.md Section 3 (the D-06 gate: legs are the sole callers of withdraw/poolOf) + items #9/#10/#11
  </read_first>
  <action>
    (gated on Task 1 concluding "recoverability preserved") Apply the D-06 kill order in the single batched diff:
    (a) DegenerusVault.sol (re-pinned `:516-517`): REMOVE `recoverAfKingPool()` and its body
        `afKing.withdraw(afKing.poolOf(address(this)));` entirely (0 external callers). Remove the function's
        doc-comment block too. If this leaves `afKing` unused in the Vault, remove the now-dead import/state
        reference as well (confirm via grep before removing).
    (b) StakedDegenerusStonk.sol (re-pinned `:539`): REMOVE the `burnAtGameOver()` AfKing-withdraw leg
        `afKing.withdraw(afKing.poolOf(address(this)));` (ONE statement). The `burnAtGameOver()` body (`:535`)
        otherwise STAYS — it also burns sDGNRS. If this leaves `afKing` unused in sDGNRS, remove the now-dead
        import/state reference (confirm via grep).
    (c) StakedDegenerusStonk.sol receive() (re-pinned `:439-445`): NARROW to GAME-only — after (b) the AfKing
        send-back path is gone, so the `msg.sender != ContractAddresses.AF_KING` allowance branch (`:442`) is
        dead. Keep `msg.sender != ContractAddresses.GAME`; drop the `&& msg.sender != ContractAddresses.AF_KING`
        clause and adjust the surrounding comment to current state only (D-344-02 — no change-history prose; the
        comment at `:435-438` mentioning the AF_KING send-back is removed/updated).
    These deletions, authored within the same diff as 344-04's `withdraw`/`poolOf` deletion, satisfy the D-06
    gate — no intermediate file ever deletes the views while a leg still calls them.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -rc "recoverAfKingPool" contracts/ | grep -v ":0" | grep -c . | grep -qx 0 && grep -rc "afKing.withdraw(afKing.poolOf" contracts/ | grep -v ":0" | grep -c . | grep -qx 0 && awk '/receive\(\) external payable/{f=1} f&&/AF_KING/{print "AFKING-IN-RECEIVE"; exit} f&&/}/{exit}' contracts/StakedDegenerusStonk.sol | grep -qxv "AFKING-IN-RECEIVE" && echo "LEGS-REMOVED RECEIVE-NARROWED"</automated>
  </verify>
  <acceptance_criteria>
    - `recoverAfKingPool` appears 0 times repo-wide (removed)
    - `afKing.withdraw(afKing.poolOf(...))` appears 0 times repo-wide (both legs removed)
    - The sDGNRS `receive()` no longer references `AF_KING` (narrowed to GAME-only); `burnAtGameOver` body otherwise intact
    - Any now-dead `afKing` import/state in Vault/sDGNRS is removed (grep-confirmed unused)
  </acceptance_criteria>
  <done>Both v48 recovery legs removed (recoverAfKingPool + the burnAtGameOver AfKing-withdraw leg, 0 repo-wide); sDGNRS receive() narrowed to GAME-only; any now-dead afKing import/state removed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: [BUILD GATE + HOLD] CLEANUP-01 kill-set grep-confirmed empty + forge build clean across the whole 344 diff, then HOLD</name>
  <files>contracts/ (the whole batched 344 diff — build verification only; no further edits)</files>
  <read_first>
    - 343-CLEANUP-INVENTORY.md Section 2 (the full 14-item kill-set + their grep targets — the grep-confirmed-empty checklist)
    - 344-CONTEXT.md D-344-07 (forge build-clean ONLY; do NOT run forge test; do NOT touch test/) + D-344-06 (HOLD at the boundary; executor never commits)
    - 343-IMPL-EDIT-ORDER-MAP.md Section 6 (the 344 hand-off — the diff is design-gating-complete from the map)
  </read_first>
  <action>
    (a) CLEANUP-02 grep-confirmed-empty: re-run each kill-set grep from 343-CLEANUP-INVENTORY.md Section 2 against
        the live tree and confirm every de-custody orphan is gone (no orphaned references). At minimum: `_poolOf`
        (0 in AfKing), `function poolOf(` (0 in AfKing), `function withdraw(` (0 in AfKing), `function depositFor(`
        / `function deposit(` (0 in AfKing), `event Deposited` (0 in AfKing), `recoverAfKingPool` (0 repo-wide),
        `afKing.withdraw(afKing.poolOf` (0 repo-wide), `sum(_poolOf)` (0), the AF_KING branch in sDGNRS receive()
        (gone), and `keeperFunding[b.player]` (0 in DegenerusGame — the D-01 trap stays defused). Record the
        checklist in the SUMMARY.
    (b) BUILD GATE: run `forge build` (compiles `contracts/` sources — NOT the test tree). It MUST exit 0 (clean).
        This is the FIRST and ONLY build of the whole batched 344 diff (344-01..05) and proves the producer-
        before-consumer authoring held (no intermediate broken state; the D-06 gate held). ⚠ Do NOT run
        `forge test`. The ABI change WILL break existing TEST-FILE compilation — that is EXPECTED and is 346 TST's
        charge (D-344-07). `test/` is NOT touched in 344. If `forge build` surfaces a CONTRACT-source compile
        error, fix the contract edit (it is a real bug in the diff); if it surfaces only a TEST-tree error,
        that is out of scope — `forge build` should not compile the test tree, but if your config does, scope the
        build to sources or note the test-only failures as expected-and-deferred.
    (c) HOLD: with `forge build` clean, STOP. Do NOT run `git commit` / `git add contracts/` (hook-enforced —
        executor subagents NEVER commit). The whole batched 344 diff is now applied + compiling and HELD at the
        contract-commit boundary for explicit user hand-review. The orchestrator owns the single user-approval
        gate and the post-approval atomic per-plan commits (D-344-06).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -rc "recoverAfKingPool" contracts/ | grep -v ":0" | grep -c . | grep -qx 0 && grep -c "_poolOf" contracts/AfKing.sol | grep -qx 0 && grep -c "keeperFunding\[b\.player\]" contracts/DegenerusGame.sol | grep -qx 0 && forge build 2>&1 | tail -5 && forge build && echo "BUILD-CLEAN HOLD-AT-BOUNDARY"</automated>
  </verify>
  <acceptance_criteria>
    - Every CLEANUP-01 kill-set grep returns empty (no orphaned de-custody references) — checklist recorded in SUMMARY
    - `keeperFunding[b.player]` appears 0 times in DegenerusGame.sol (the D-01 trap stays defused end-to-end)
    - `forge build` exits 0 (clean compile of the whole batched 344 diff — the producer-before-consumer + D-06 gates held)
    - `forge test` was NOT run; `test/` was NOT touched (D-344-07)
    - NO `git commit` / `git add contracts/` was run; the diff is HELD at the contract-commit boundary
  </acceptance_criteria>
  <done>Every CLEANUP-01 kill-set grep returns empty; `forge build` exits 0 across the whole batched 344 diff; `forge test` NOT run and `test/` untouched; the diff is HELD at the contract-commit boundary with no executor commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| v48 recovery legs → AfKing deleted views | D-06 — the legs are the sole callers of `withdraw`/`poolOf`; removal order must avoid a dangling reference. |
| VAULT/sDGNRS recoverability | After de-custody, their funding-source ETH must remain recoverable (the trace proves it). |
| Game withdraw send-back → VAULT/sDGNRS receive() | The send-back must be receivable (VAULT open; sDGNRS GAME-allowed). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-344-05-REC | Denial (stranded funds) | v48-recovery removal | mitigate | The D-344-01 [BLOCKING] actor-consequence trace (Task 1) proves recoverability is fully replaced for BOTH VAULT and sDGNRS by ungated `withdrawKeeperFunding` (send-back received by VAULT.receive() open + sDGNRS.receive() GAME-allowed) + the Decision-B claim-merge; ESCALATES on any gap before deletion. (feedback_design_intent_before_deletion / D-344-01) |
| T-344-05-D6 | DoS (broken build) | withdraw/poolOf callers | mitigate | D-06 kill order — the recovery-leg callers are removed before/with the 344-04 view deletions in ONE diff; the `forge build` gate (Task 3) proves no dangling reference survived. (343-CLEANUP-INVENTORY Section 3 / T-343-07) |
| T-344-05-DEAD | Tampering (dead branch) | sDGNRS receive() AF_KING | mitigate | The AF_KING allowance branch is dead after the leg removal; narrowed to GAME-only (the Game's reserve deposits + the withdraw send-back both pass as GAME). |
| T-344-05-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs (Solidity edits + forge build only; forge is the existing toolchain). |
</threat_model>

<verification>
- The D-344-01 actor-consequence trace exists, covers VAULT + sDGNRS, and concludes "recoverability preserved" (else escalate)
- `recoverAfKingPool` + `afKing.withdraw(afKing.poolOf(...))` are 0 repo-wide; sDGNRS receive() is GAME-only
- The full CLEANUP-01 kill-set is grep-confirmed empty; `keeperFunding[b.player]` is 0 (D-01 defused)
- `forge build` exits 0 across the whole batched 344 diff; `forge test` NOT run; `test/` NOT touched
- NO executor commit; the diff is HELD at the contract-commit boundary for user hand-review
</verification>

<success_criteria>
- The v48 recovery is removed only AFTER the trace proves recoverability (D-344-01)
- The D-06 kill order held (forge build clean — no dangling reference)
- The whole batched 344 diff compiles clean and is HELD for explicit user hand-review (BATCH-02)
</success_criteria>

<output>
Create `.planning/phases/344-impl-the-one-batched-contract-diff-ledger-de-custody-cleanup/344-05-SUMMARY.md` when done.
Also create `.planning/phases/344-impl-the-one-batched-contract-diff-ledger-de-custody-cleanup/344-ACTOR-CONSEQUENCE-TRACE.md` (Task 1, D-344-01).
</output>
