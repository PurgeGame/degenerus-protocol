---
phase: 344-impl-the-one-batched-contract-diff-ledger-de-custody-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/storage/DegenerusGameStorage.sol
  - contracts/DegenerusGame.sol
  - contracts/AfKing.sol
autonomous: false
requirements: [LEDGER-01, LEDGER-02]
must_haves:
  truths:
    - "The executor re-pins every anchor from 343-GREP-ATTESTATION.md against the live tree BEFORE writing any edit (D-344-05)"
    - "A new per-player mapping(address => uint256) keeperFunding exists on DegenerusGameStorage.sol with NO separate aggregate"
    - "Both BatchBuy structs (Game + AfKing) carry a leading `funder` field, ABI-identical"
    - "The storage invariant comment names the keeper component (claimablePool == Σ claimableWinnings + Σ keeperFunding)"
  artifacts:
    - path: "contracts/storage/DegenerusGameStorage.sol"
      provides: "keeperFunding mapping + updated invariant comment"
      contains: "mapping(address => uint256) internal keeperFunding"
    - path: "contracts/DegenerusGame.sol"
      provides: "BatchBuy struct with funder field"
      contains: "struct BatchBuy"
    - path: "contracts/AfKing.sol"
      provides: "BatchBuy struct with funder field (ABI-identical to Game's)"
      contains: "struct BatchBuy"
  key_links:
    - from: "contracts/DegenerusGame.sol BatchBuy"
      to: "contracts/AfKing.sol BatchBuy"
      via: "identical field order/types (ABI-compatible)"
      pattern: "address funder"
---

<objective>
Step 1 of the producer-before-consumer edit-order map (343-IMPL-EDIT-ORDER-MAP.md Section 4 Step 1):
land the STORAGE PRODUCERS for the v54 keeper-funding ledger — the per-player `keeperFunding` mapping
and the `funder` field on BOTH `BatchBuy` structs — and update the storage invariant comment to name the
keeper component. These must exist before any Game function reads/writes `keeperFunding[...]` or `b.funder`
(plans 344-02 / 344-04).

Purpose: Establishes the storage shape the entire ledger rides on (no new aggregate — D-CF-03; the keeper
total rides inside the existing `claimablePool`). The `funder` field is the structural fix for the D-01 trap
(the Game debits the FUNDER's bucket, not the beneficiary's).

Output: Edits applied to `contracts/` (part of the single batched 344 diff). NO COMMIT — held at the
contract-commit boundary. Plus the executor's first-action re-grep attestation recorded.

⚠ CONTRACT-BOUNDARY HARD STOP. This plan is `autonomous: false`. The executor applies the edits to
`contracts/` but NEVER runs `git commit` / `git add contracts/` (hook-enforced). The orchestrator owns all
commits, deferred past the single user-approval gate (see 344-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-IMPL-EDIT-ORDER-MAP.md
@.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-GREP-ATTESTATION.md
@.planning/phases/344-impl-the-one-batched-contract-diff-ledger-de-custody-cleanup/344-CONTEXT.md

<interfaces>
<!-- The current shapes the edits land on. Re-grep at author time (Task 1) — these are the SPEC-attested anchors. -->

Current storage anchors (DegenerusGameStorage.sol, attested @ 83a84431):
- invariant comment block `:345-354` (`:348` reads `///      INVARIANT: claimablePool >= sum(claimableWinnings[*])`)
- `:355` reads `uint128 internal claimablePool;`
- `:402` reads `mapping(address => uint256) internal claimableWinnings;` (the TEMPLATE to mirror)
- `keeperFunding` is CONFIRMED-NEW — `grep -rln "keeperFunding" contracts/` returns 0 files

Current BatchBuy struct shape (BOTH structs, ABI-identical, attested):
- DegenerusGame.sol `:1796` — `struct BatchBuy { address player; uint256 ethValue; uint256 amount; bool isTicket; uint8 mode; }`
- AfKing.sol `:20` — identical field order/types; "ABI-identical" doc note at AfKing.sol `:16` / `:30`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING-FIRST] Re-pin every anchor vs the live tree (D-344-05) before any edit</name>
  <files>.planning/phases/344-impl-the-one-batched-contract-diff-ledger-de-custody-cleanup/344-01-SUMMARY.md (attestation record); reads contracts/ only</files>
  <read_first>
    - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-GREP-ATTESTATION.md (THE attestation to re-run — Section 2 Drift-Correction Table per file, Section 4 kill-set anchors)
    - 344-CONTEXT.md D-344-05 (the re-grep IS the executor's first action; line numbers drift the instant the first edit lands)
  </read_first>
  <action>
    BEFORE writing any contract edit, re-run every grep from 343-GREP-ATTESTATION.md against the LIVE working
    tree and re-pin every anchor this 344 diff touches. Confirm `git diff --numstat 83a84431 HEAD -- contracts/`
    is EMPTY (the live tree IS the SPEC baseline; the SPEC anchors are currently valid — this re-grep is the
    discipline floor, not a known-drift fix). Re-pin AT MINIMUM the anchors this plan touches: the storage
    invariant block (SPEC `:345-354`), `claimablePool` decl (SPEC `:355`), `claimableWinnings` mapping
    (SPEC `:402`), and both `BatchBuy` structs (Game SPEC `:1796`, AfKing SPEC `:20`). Confirm `keeperFunding`
    is absent repo-wide (`grep -rln "keeperFunding" contracts/` → 0). Record the re-pinned anchors (matched line +
    text) in the plan SUMMARY (or a scratch attestation note per D-344-05 — executor's discretion). If ANY anchor
    has drifted from the SPEC citation, use the ACTUAL re-grepped line for every subsequent edit in THIS phase,
    never the upstream doc-cited line. The downstream plans (344-02..05) inherit your re-pinned anchors.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && git diff --numstat 83a84431 HEAD -- contracts/ | grep -c . | grep -qx 0 && grep -rln "keeperFunding" contracts/ | grep -c . | grep -qx 0 && echo "BASELINE-CLEAN keeperFunding-ABSENT"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --numstat 83a84431 HEAD -- contracts/` returns EMPTY (live tree == baseline) — attested in SUMMARY
    - `grep -rln "keeperFunding" contracts/` returns 0 files (CONFIRMED-NEW) — attested in SUMMARY
    - The re-pinned anchors for every symbol this phase touches are recorded (matched line + text); any drift is noted
    - No contract edit was written before this attestation ran (the re-grep is the FIRST action)
  </acceptance_criteria>
  <done>The re-grep attestation ran first and is recorded in the SUMMARY: baseline clean, keeperFunding absent, every touched anchor re-pinned (drift, if any, noted with the actual line).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add keeperFunding mapping + name the keeper component in the storage invariant comment</name>
  <files>contracts/storage/DegenerusGameStorage.sol</files>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol (the invariant comment block ~`:345-354` + `claimablePool` decl `:355` + `claimableWinnings` mapping `:402` — the per-player-mapping TEMPLATE to mirror)
    - 343-IMPL-EDIT-ORDER-MAP.md Section 2.1 (the mapping; NO new aggregate — D-CF-03) + Section 2.2 (the invariant comment edit, the table row for the storage block)
    - 343-SOLVENCY-PROOF.md Section 0 (the D-CF-03 framing: `claimablePool == Σ claimableWinnings + Σ keeperFunding`)
  </read_first>
  <action>
    ADD `mapping(address => uint256) internal keeperFunding;` to DegenerusGameStorage.sol (per-player prepaid
    keeper ETH; distinct from `claimableWinnings`), mirroring the existing `claimableWinnings` mapping shape.
    There is NO separate `keeperFundingPool` aggregate (D-CF-03) — the systemwide keeper total rides inside the
    existing `claimablePool` (`:355`). UPDATE the invariant comment block (re-pinned anchor, SPEC `:345-354`,
    currently `INVARIANT: claimablePool >= sum(claimableWinnings[*])`) to name the keeper component:
    `claimablePool == Σ claimableWinnings[*] + Σ keeperFunding[*]`. The comment must describe the current state
    only (no history of the change — feedback_no_history_in_comments). Do NOT add any other storage variable.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -c "mapping(address => uint256) internal keeperFunding;" contracts/storage/DegenerusGameStorage.sol | grep -qx 1 && grep -v '^[[:space:]]*//\|^[[:space:]]*///' contracts/storage/DegenerusGameStorage.sol | grep -c "keeperFundingPool" | grep -qx 0 && echo "MAPPING-ADDED NO-AGGREGATE"</automated>
  </verify>
  <acceptance_criteria>
    - `keeperFunding` mapping declared exactly once: `mapping(address => uint256) internal keeperFunding;`
    - NO `keeperFundingPool` (or any new aggregate) declared (grep on non-comment lines returns 0)
    - The invariant comment names the keeper component (`Σ keeperFunding` appears in the invariant block)
    - The `claimablePool` decl (`:355`) is UNCHANGED (still `uint128 internal claimablePool;`)
  </acceptance_criteria>
  <done>The `keeperFunding` mapping is declared once, no aggregate is introduced, and the invariant comment names the keeper component; `claimablePool` decl unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add the `funder` field to BOTH BatchBuy structs (Game + AfKing), ABI-identical</name>
  <files>contracts/DegenerusGame.sol, contracts/AfKing.sol</files>
  <read_first>
    - contracts/DegenerusGame.sol (BatchBuy struct, re-pinned anchor SPEC `:1796`)
    - contracts/AfKing.sol (BatchBuy struct, re-pinned anchor SPEC `:20`; "ABI-identical" doc note `:16` / `:30`)
    - 343-IMPL-EDIT-ORDER-MAP.md Section 1.2 (the funder field on both structs) + Section 3.1 (D-01: WHY funder, not player)
  </read_first>
  <action>
    ADD a leading `address funder;` field to BOTH `BatchBuy` structs — `DegenerusGame.sol:1796` AND
    `AfKing.sol:20` — so the final shape is `{ address funder; address player; uint256 ethValue; uint256 amount;
    bool isTicket; uint8 mode; }`. The two structs MUST stay field-for-field identical (ABI-compatible) — AfKing
    builds the struct, the Game's `batchPurchase` decodes it; a field-order mismatch would mis-decode. `funder`
    is the resolved funding source (`src`); the bucket the Game debits (D-01). `player` stays the beneficiary
    (the `purchaseWith` target; the VAULT/SDGNRS exemption keys on it). Refresh the "ABI-identical" doc note at
    AfKing.sol `:16` / `:30` to reflect the new field (current state only — no change-history prose). Do NOT wire
    `funder` into any function body in this plan — that is 344-02 (Game debit) and 344-04 (AfKing `funder: src`
    build). This plan only adds the field.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -A7 "struct BatchBuy" contracts/DegenerusGame.sol | grep -c "address funder;" | grep -qx 1 && grep -A7 "struct BatchBuy" contracts/AfKing.sol | grep -c "address funder;" | grep -qx 1 && echo "FUNDER-ON-BOTH-STRUCTS"</automated>
  </verify>
  <acceptance_criteria>
    - `DegenerusGame.sol` BatchBuy struct has `address funder;` as the FIRST field (before `player`)
    - `AfKing.sol` BatchBuy struct has `address funder;` as the FIRST field (before `player`), field-identical to Game's
    - The two structs are byte-equivalent in field order and types (ABI-compatible)
    - No function body wires `funder` in this plan (the debit/build come in 344-02 / 344-04)
  </acceptance_criteria>
  <done>Both BatchBuy structs carry `address funder;` as the leading field, field-identical (ABI-compatible); no function body wires it yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AfKing → Game (batchPurchase decode) | The `BatchBuy[]` calldata crosses contract boundary; AfKing encodes, Game decodes. Field-order mismatch = mis-decode (mis-account). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-344-01-01 | Tampering | BatchBuy struct ABI (both copies) | mitigate | Both structs add `funder` as the SAME leading field; Task 3 verify asserts identical field order/types — prevents the D-01 mis-decode that would debit the wrong bucket |
| T-344-01-02 | Information Disclosure / Solvency | `keeperFunding` mapping (no aggregate) | mitigate | D-CF-03: no new aggregate; the keeper total rides inside `claimablePool` which every reservation site already reserves (SOLVENCY-01 proven). Task 2 verify asserts no `keeperFundingPool` is introduced — the omittable-pool class stays structurally impossible. |
| T-344-01-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan (Solidity edits only). |
</threat_model>

<verification>
- `git diff --numstat 83a84431 HEAD -- contracts/` was EMPTY before any edit (re-grep attestation, Task 1)
- `keeperFunding` mapping declared once; no new aggregate variable
- `funder` field on both BatchBuy structs, identical order/types
- The storage invariant comment names the keeper component
- NO `git commit` / `git add contracts/` was run by the executor (hook-enforced; orchestrator commits post-approval)
</verification>

<success_criteria>
- The re-grep attestation (D-344-05) ran FIRST and is recorded in the SUMMARY
- The storage producers (`keeperFunding` mapping + `funder` on both structs + the invariant comment) are landed in `contracts/`
- No aggregate introduced; no function body wired yet
- Edits held at the contract-commit boundary (no executor commit)
</success_criteria>

<output>
Create `.planning/phases/344-impl-the-one-batched-contract-diff-ledger-de-custody-cleanup/344-01-SUMMARY.md` when done.
Record the re-pinned anchors (D-344-05) in the SUMMARY for downstream plans 344-02..05 to inherit.
</output>
