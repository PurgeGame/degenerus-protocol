---
phase: 344-impl-the-one-batched-contract-diff-ledger-de-custody-cleanup
plan: 03
type: execute
wave: 3
depends_on: ["344-02"]
files_modified:
  - contracts/AfKing.sol
  - contracts/interfaces/IDegenerusGameModules.sol
autonomous: false
requirements: [AUTOBUY-01, LEDGER-05]
must_haves:
  truths:
    - "The AfKing-local IGame block declares depositKeeperFunding / withdrawKeeperFunding / keeperFundingOf"
    - "IGame.batchPurchase is flipped from payable to non-payable (the ONLY interface decl in the repo)"
    - "IGame.keeperSnapshot is extended to the new return tuple matching the Game's extended impl"
    - "IDegenerusGameModules.sol:237 comment is refreshed (comment-only, no ABI weight)"
  artifacts:
    - path: "contracts/AfKing.sol"
      provides: "the IGame interface block consuming the new Game ABI"
      contains: "interface IGame"
  key_links:
    - from: "AfKing IGame.batchPurchase decl"
      to: "DegenerusGame.batchPurchase impl (344-02)"
      via: "non-payable ABI match"
      pattern: "function batchPurchase\\(BatchBuy\\[\\] calldata buys\\) external;"
    - from: "AfKing IGame.keeperSnapshot decl"
      to: "DegenerusGame.keeperSnapshot impl (344-02)"
      via: "extended return tuple match"
      pattern: "keeperFundings"
---

<objective>
Step 3 of the producer-before-consumer edit-order map (343-IMPL-EDIT-ORDER-MAP.md Section 4 Step 3):
declare the new/changed Game ABI in the interfaces so AfKing's calls (344-04) resolve against a real surface.
The AfKing-local `IGame` block (`interface IGame {` at `:40`) is the ONLY place `batchPurchase` is declared in
the entire repo — flip it to non-payable here and add the new ledger decls. The `contracts/interfaces/`
mention (`IDegenerusGameModules.sol:237`) is a COMMENT only — refresh it (no ABI weight).

Purpose: Interfaces must exist before AfKing (the consumer) rewires its call sites. This plan touches the
INTERFACE block of `AfKing.sol`; the de-custody BODY edits are 344-04 (sequential — same file, no concurrent
write).

Output: Edits applied to `contracts/AfKing.sol` (IGame block) + `contracts/interfaces/IDegenerusGameModules.sol`
(comment). Part of the single batched 344 diff. NO COMMIT.

⚠ CONTRACT-BOUNDARY HARD STOP. `autonomous: false`. The executor NEVER runs `git commit` / `git add
contracts/` (hook-enforced). Orchestrator commits post-approval (344-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-IMPL-EDIT-ORDER-MAP.md
@.planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-CLEANUP-INVENTORY.md
@.planning/phases/344-impl-the-one-batched-contract-diff-ledger-de-custody-cleanup/344-CONTEXT.md

<interfaces>
<!-- Re-pinned anchors inherited from 344-01 Task 1. SPEC-attested starting refs. -->

Current AfKing.sol IGame block (attested @ 83a84431; re-pin before editing):
- `interface IGame {` `:40`
- `:41` — `function isOperatorApproved(address owner, address operator) external view returns (bool);`
- `:43` — `function batchPurchase(BatchBuy[] calldata buys) external payable;`  ← the ONLY interface decl; flips to non-payable
- `:56` — `function keeperSnapshot(address[] calldata players) external view returns (uint256 mintPriceWei, bool rngLocked_, uint256[] memory claimables);`  ← extend the return tuple

The Game-side impl signatures these MUST match (from 344-02):
- `depositKeeperFunding(address) external payable`
- `withdrawKeeperFunding(uint256) external`
- `keeperFundingOf(address) external view returns (uint256)`
- `batchPurchase(BatchBuy[] calldata buys) external`  (non-payable)
- `keeperSnapshot(address[]) external view returns (uint256 mintPriceWei, bool rngLocked_, uint256[] memory claimables, uint256[] memory keeperFundings)`

Comment-only site (no ABI weight):
- IDegenerusGameModules.sol `:237` — `///         (not msg.value), so batchPurchase can run many subscriber buys inline in one frame.`
  (`batchPurchase` is NOT declared anywhere under `contracts/interfaces/` — this is the only mention, a comment)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Flip IGame.batchPurchase non-payable + extend IGame.keeperSnapshot + add the three ledger decls</name>
  <files>contracts/AfKing.sol</files>
  <read_first>
    - contracts/AfKing.sol (the IGame block `:40-57`; re-pinned `batchPurchase` decl `:43`, `keeperSnapshot` decl `:56`)
    - 343-IMPL-EDIT-ORDER-MAP.md Section 4 Step 3 (3c — the AfKing IGame block edits) + Section 1.3/1.4/1.5/1.6 (the impl signatures to mirror)
    - 343-CLEANUP-INVENTORY.md Section 5 (item #8 — payable→non-payable flip IN PLACE, NOT a deletion) + Section 6 (the new IGame ABI to ADD)
  </read_first>
  <action>
    In the AfKing-local `IGame` block (`interface IGame {` at `:40`):
    (a) FLIP `batchPurchase` (re-pinned `:43`) from `external payable` to non-payable:
        `function batchPurchase(BatchBuy[] calldata buys) external;` — this is a flip IN PLACE, NOT a deletion;
        it is the ONLY interface declaration of `batchPurchase` in the entire repo.
    (b) EXTEND `keeperSnapshot` (re-pinned `:56`) to the new return tuple matching the Game's extended impl:
        `function keeperSnapshot(address[] calldata players) external view returns (uint256 mintPriceWei, bool
        rngLocked_, uint256[] memory claimables, uint256[] memory keeperFundings);`
    (c) ADD the three new ledger decls to the SAME block:
        `function depositKeeperFunding(address player) external payable;`
        `function withdrawKeeperFunding(uint256 amount) external;`
        `function keeperFundingOf(address player) external view returns (uint256);`
    These must EXACTLY match the Game-side impl signatures from 344-02 (mismatched ABI = call resolution failure).
    Do NOT touch the de-custody body (`_poolOf`, receive/deposit/withdraw, the subscribe credit, the batch call) —
    that is 344-04.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -c "function batchPurchase(BatchBuy\[\] calldata buys) external;" contracts/AfKing.sol | grep -qx 1 && grep -c "function batchPurchase(BatchBuy\[\] calldata buys) external payable;" contracts/AfKing.sol | grep -qx 0 && grep -c "function depositKeeperFunding(address player) external payable;" contracts/AfKing.sol | grep -qx 1 && grep -c "function withdrawKeeperFunding(uint256 amount) external;" contracts/AfKing.sol | grep -qx 1 && grep -c "function keeperFundingOf(address player) external view returns (uint256);" contracts/AfKing.sol | grep -qx 1 && grep -c "uint256\[\] memory keeperFundings" contracts/AfKing.sol | grep -q "[1-9]" && echo "IGAME-ABI-UPDATED"</automated>
  </verify>
  <acceptance_criteria>
    - `IGame.batchPurchase` is `external;` (non-payable) — the `external payable;` form appears 0 times in AfKing.sol's IGame block
    - `IGame.keeperSnapshot` declares the 4-element return tuple including `uint256[] memory keeperFundings`
    - All three new decls present and signature-matched to the Game impl: `depositKeeperFunding(address) payable`, `withdrawKeeperFunding(uint256)`, `keeperFundingOf(address) view returns (uint256)`
    - No de-custody body edits in this plan (the `_poolOf`/receive/withdraw deletions + the call rewiring are 344-04)
  </acceptance_criteria>
  <done>The IGame block declares the non-payable batchPurchase, the extended 4-tuple keeperSnapshot, and the three new ledger decls — all signature-matched to the Game impl; no de-custody body edits.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Refresh the IDegenerusGameModules.sol:237 batchPurchase comment (comment-only)</name>
  <files>contracts/interfaces/IDegenerusGameModules.sol</files>
  <read_first>
    - contracts/interfaces/IDegenerusGameModules.sol (re-pinned `:237` — the comment-only `batchPurchase` mention)
    - 343-CLEANUP-INVENTORY.md Section 5 (this is a comment, NOT a payable decl; carries no ABI weight)
  </read_first>
  <action>
    Refresh the comment at IDegenerusGameModules.sol `:237` so it matches the v54 non-payable reality (keeper ETH
    is debited from `keeperFunding` inside the Game, NOT sent as `msg.value`; `batchPurchase` runs many subscriber
    buys inline in one frame with no value transfer). This is a COMMENT-ONLY refresh — it carries no ABI weight,
    and `batchPurchase` is NOT declared anywhere under `contracts/interfaces/`. Describe the current state only
    (no change-history prose). Do NOT add a payable/non-payable declaration here.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -c "function batchPurchase" contracts/interfaces/IDegenerusGameModules.sol | grep -qx 0 && grep -c "batchPurchase" contracts/interfaces/IDegenerusGameModules.sol | grep -q "[1-9]" && echo "COMMENT-ONLY-REFRESHED NO-DECL"</automated>
  </verify>
  <acceptance_criteria>
    - The `batchPurchase` mention at `:237` remains a comment (no `function batchPurchase` declaration introduced)
    - The comment text reflects the v54 non-payable / no-msg.value reality
  </acceptance_criteria>
  <done>The IDegenerusGameModules.sol:237 comment reflects the v54 non-payable / no-msg.value reality and remains comment-only (no declaration introduced).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AfKing IGame decl → Game impl | The interface ABI must byte-match the Game impl, else AfKing's external calls fail to resolve / mis-encode. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-344-03-ABI | Tampering | IGame ABI mismatch | mitigate | Each new/changed decl is signature-matched verbatim to the Game impl from 344-02; Task 1 verify asserts each. A non-payable flip in the interface is required to match the non-payable impl (a payable mismatch would revert the value-call). |
| T-344-03-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs (Solidity interface edits only). |
</threat_model>

<verification>
- `IGame.batchPurchase` is non-payable; the payable form appears 0 times
- `IGame.keeperSnapshot` declares the extended 4-tuple
- The three new ledger decls are present and signature-matched to the Game impl
- The `IDegenerusGameModules.sol:237` comment is refreshed (no decl introduced)
- NO `git commit` / `git add contracts/` was run by the executor
</verification>

<success_criteria>
- The interfaces declare the full new/changed Game ABI so 344-04's AfKing calls resolve
- Held at the contract-commit boundary (no executor commit)
</success_criteria>

<output>
Create `.planning/phases/344-impl-the-one-batched-contract-diff-ledger-de-custody-cleanup/344-03-SUMMARY.md` when done.
</output>
