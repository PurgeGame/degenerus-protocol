---
phase: 311-spec-vrf-rotation-liveness-fix-spec
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md
autonomous: true
requirements: [VRF-01, VRF-02, VRF-03, VRF-04, VRF-05]

must_haves:
  truths:
    - "311-SPEC.md exists with a §0 call-graph manifest grep-verified against contract HEAD"
    - "Every file:line cited in CONTEXT.md <canonical_refs> has a recorded VERIFIED-or-DRIFTED line in the manifest"
    - "Each §9d VRF-cluster anchor (HANDOFF-78/85/86/87/88/89/90/91 + ADMA-01/02) is mapped to the closing change in a §0 mapping table"
    - "The vault/admin-routed dispatch to wireVrf + updateVrfCoordinatorAndSub is traced to its actual source sites (ADMA-02 / VRF-05)"
    - "Zero contracts/ and zero test/ files are modified"
  artifacts:
    - path: ".planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md"
      provides: "SPEC skeleton + §0 grep-verified call-graph manifest + §9d-anchor→closing-change mapping"
      contains: "§0 Call-Graph Manifest"
  key_links:
    - from: "311-SPEC.md §0 manifest"
      to: "contracts/modules/DegenerusGameAdvanceModule.sol"
      via: "grep-verified line citations"
      pattern: "DegenerusGameAdvanceModule.sol:[0-9]"
    - from: "311-SPEC.md §0 mapping table"
      to: "audit/FINDINGS-v44.0.md §9d cluster"
      via: "anchor → closing-change rows"
      pattern: "HANDOFF-(78|85|86|87|88|89|90|91)|ADMA-0(1|2)"
---

<objective>
Author the `311-SPEC.md` skeleton and its load-bearing §0 evidence base: a grep-verified call-graph manifest for every file:line cited in `311-CONTEXT.md` `<canonical_refs>`, plus a §9d-anchor → closing-change mapping table. This is the verification foundation that the Plan 02 design narrative cites — no design section may assert a call path without a row in this manifest.

Purpose: Discharge the hard grep-verification obligation (`feedback_verify_call_graph_against_source`) as a discrete, checkable pass BEFORE any design prose is written, so the locked fix shape rests on confirmed source facts and zero "by construction" claims. Drift (e.g. the CONTEXT-named `DegenerusVault` dispatch actually living in `DegenerusAdmin.sol` / `DegenerusGame.sol`) is caught and recorded here.
Output: `311-SPEC.md` containing the section skeleton (placeholders for §1–§6 design sections) and a COMPLETE §0 with the verified manifest + mapping table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-CONTEXT.md

# Sibling SPEC format precedent — mirror its "§0 Call-Graph Evidence (grep-verified against HEAD)" structure
@.planning/phases/309-spec-locked-layout-bonus-only-cap-shared-cap-disposition-spe/309-SPEC.md

<manifest_targets>
<!-- Anchors to grep-verify, transcribed from 311-CONTEXT.md <canonical_refs>. -->
<!-- The executor MUST re-grep each against HEAD and record the ACTUAL verified line (anchors may drift). -->

contracts/modules/DegenerusGameAdvanceModule.sol:
  :498  wireVrf (D-03/D-04 init-only lock + shared write)
  :1044 requestLootboxRng · :1048 LR_MID_DAY single-in-flight gate · :1097 sets LR_MID_DAY=1
  :1133 retryLootboxRng · :1134 reverts if LR_MID_DAY==0 (failsafe)
  :1688 updateVrfCoordinatorAndSub (D-01/D-02) · :1701-1704 force-unlock+zero resets · :1709 LR_MID_DAY=0 · :1711-1714 totalFlipReversals carry-over comment
  :1756 rawFulfillRandomWords · :1761 requestId/word guard (abandons old word) · :1766-1768 daily branch → rngWordCurrent · mid-day branch (the else after :1769) → lootboxRngWordByIndex[index]
  :1208 existing _backfillOrphanedLootboxIndices CALL (D-05 reachability)
  :1817 _backfillOrphanedLootboxIndices DEFINITION (backward scan, VRF-derived keccak)
  :209-238,269 advance-flow drain gate (Scenario B revert site; D-05 must confirm re-issue un-blocks)
  requestRandomWords call sites (the re-issue mechanic reference for D-01/D-02)

contracts/modules/DegenerusGameMintModule.sol:
  :686 entropy = lootboxRngWordByIndex[...-1] with NO zero-guard (Scenario A consumer)

contracts/storage/DegenerusGameStorage.sol:
  :244 rngRequestTime · :373 rngWordCurrent · :1287/1291/1295 vrfCoordinator/vrfKeyHash/vrfSubscriptionId · :1328-1329 LR_MID_DAY shift/mask · :1431 lootboxRngWordByIndex

vault/admin-routed dispatch (ADMA-02 / VRF-05):
  CONTEXT names "DegenerusVault" — re-grep to find the ACTUAL dispatch sites for wireVrf + updateVrfCoordinatorAndSub across contracts/ and record them (do not assume the CONTEXT path is correct).

§9d cluster anchor source:
  audit/FINDINGS-v44.0.md §9d — HANDOFF-78/85/86/87/88/89/90/91 + ADMA-01/02 (the anchors this fix closes)
</manifest_targets>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skeleton 311-SPEC.md + §0 grep-verified call-graph manifest</name>
  <files>.planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md</files>
  <read_first>
    - .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-CONTEXT.md (source of truth — <canonical_refs> and the manifest_targets list above are transcribed from it)
    - .planning/phases/309-spec-locked-layout-bonus-only-cap-shared-cap-disposition-spe/309-SPEC.md (mirror its §0 "Call-Graph Evidence (grep-verified against HEAD)" table structure)
    - contracts/modules/DegenerusGameAdvanceModule.sol (the primary file every AdvanceModule anchor verifies against — read from contracts/ ONLY per feedback_contract_locations)
    - contracts/modules/DegenerusGameMintModule.sol (Scenario A consumer at :686)
    - contracts/storage/DegenerusGameStorage.sol (the VRF-participating storage slots)
  </read_first>
  <action>
    Create `.planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md`. Write the document header (title "Phase 311 SPEC — VRF-Rotation Liveness Fix", gathered-date, baseline `MILESTONE_V44_AT_HEAD_6f0ba2963a10654ba554a8c333c5ee80c54a8349`, current verified HEAD sha) and a section skeleton with these headings, leaving §1–§6 as one-line "[authored in Plan 02]" placeholders: `## §0 Call-Graph Manifest (grep-verified against HEAD)`, `## §1 Design-Intent Backward-Trace (Scenario A + Scenario B)`, `## §2 LOCKED Fix Shape — Re-Issue In-Flight (D-01/D-02)`, `## §3 Freeze-Invariant Disposition (VRF-03)`, `## §4 wireVrf One-Shot Lock + _setVrfConfig Dedup + Vault Reach (D-03/D-04/VRF-04/VRF-05)`, `## §5 Orphan-Recovery Breadth (D-05)`, `## §6 Rejected Options`.

    Then author §0 IN FULL. Run `grep`/`sed -n` against the three `contracts/` files for every anchor in the `<manifest_targets>` list and build a Markdown table with columns: `Symbol/Site | CONTEXT-claimed line | VERIFIED line (HEAD) | Status (VERIFIED / DRIFTED ±N) | One-line confirmation of what is at that line`. One row per anchor. For DRIFTED anchors, record the actual line and the drift delta — do NOT silently rewrite the claim. Record the verified HEAD sha used. Include a dedicated sub-row block for the `requestRandomWords` call sites (record every line) since they are the re-issue mechanic reference for D-01/D-02, and a sub-row for the `rawFulfillRandomWords` daily-vs-mid-day branch boundary (record the exact line of the daily `rngWordCurrent =` write and the mid-day `lootboxRngWordByIndex[index] =` write). Cover requirement context for VRF-01 (the `:686` consumer + `lootboxRngWordByIndex` slot + the `:1769`-area mid-day fulfillment write) and VRF-02 (`:1044`/`:1133` reachability + the `:209-238,269` drain gate). State explicitly in §0 prose that this manifest contains zero "by construction" claims and that every downstream design assertion in §1–§6 must cite a row here.

    Per feedback_contract_locations: every contract read is from `contracts/` only. Do NOT modify any `.sol` file or any `test/` file — this task only reads source and writes `311-SPEC.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md && grep -q "## §0 Call-Graph Manifest" .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md && grep -Eq "DegenerusGameAdvanceModule\.sol:(498|1044|1133|1688|1756|1817|1208)" .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md && grep -q "DegenerusGameMintModule.sol:686" .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md && git diff --quiet -- contracts/ test/ && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `311-SPEC.md` exists with a `## §0 Call-Graph Manifest (grep-verified against HEAD)` section and the §1–§6 design-section headings present as placeholders.
    - §0 contains one manifest row per anchor in `<manifest_targets>` (AdvanceModule :498/:1044/:1048/:1097/:1133/:1134/:1688/:1701-1704/:1709/:1711-1714/:1756/:1761/:1766-1768/mid-day-branch/:1208/:1817/:209-238/:269; MintModule :686; Storage :244/:373/:1287/:1291/:1295/:1328-1329/:1431), each row recording the VERIFIED line at HEAD and a VERIFIED/DRIFTED status.
    - The `requestRandomWords` call sites are recorded with their actual line numbers; the `rawFulfillRandomWords` daily vs mid-day write lines are each recorded.
    - §0 records the verified HEAD sha and states no "by construction" claims are used.
    - `git diff --quiet -- contracts/ test/` returns clean (zero source/test mutations).
  </acceptance_criteria>
  <done>§0 manifest is complete with a VERIFIED-or-DRIFTED status for every CONTEXT anchor; the SPEC skeleton headings for §1–§6 exist; no contract/test files touched.</done>
</task>

<task type="auto">
  <name>Task 2: §0 §9d-anchor → closing-change mapping table + vault/admin-routed reach trace</name>
  <files>.planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md</files>
  <read_first>
    - .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md (the §0 manifest authored in Task 1 — this task appends to §0)
    - .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-CONTEXT.md (the §9d cluster → decision mapping in <domain> and <decisions>: D-01..D-05, and the ADMA-02 vault-reach note in <code_context>)
    - audit/FINDINGS-v44.0.md (§9d cluster — HANDOFF-78/85/86/87/88/89/90/91 + ADMA-01/02 source rows; consult §9d.2 + §9d.4)
    - contracts/DegenerusAdmin.sol (re-grep the actual admin dispatch to wireVrf + updateVrfCoordinatorAndSub — read from contracts/ ONLY)
    - contracts/DegenerusGame.sol (the selector-routed dispatch to both VRF admin functions)
  </read_first>
  <action>
    Append to §0 of `311-SPEC.md` two further blocks.

    (1) A `### §0.X §9d-Anchor → Closing-Change Mapping` table with columns `§9d Anchor | V-NN | What it flags | Closing change (decision ID) | Requirement closed`. One row each for HANDOFF-78 (V-137, queue+apply tactic — closed by D-01/D-02 re-issue per VRF-03), HANDOFF-85/87/89/91 (the freeze-cluster siblings — VRF-03), HANDOFF-86/88/90 + ADMA-01 (wireVrf one-shot lock — D-03, VRF-04), and ADMA-02 (vault-routed reach — D-03+D-01/D-02, VRF-05). Cross-reference each anchor to the FINDINGS-v44.0.md §9d row it comes from. Per `project_rnglock_audit_disposition`, add a one-line note that the §9d rows are a maximalist catalog (not live player vectors) and the closing changes target the real liveness defect, not over-fixing governance rows.

    (2) A `### §0.Y Vault/Admin-Routed Reach Trace (ADMA-02 / VRF-05)` sub-section that records the ACTUAL dispatch path to both `wireVrf` and `updateVrfCoordinatorAndSub`, grep-verified across `contracts/`. CONTEXT.md names a `DegenerusVault` dispatch; the executor MUST record the real sites found (e.g. `DegenerusAdmin.sol` admin-wrapper functions + their `gameAdmin.wireVrf(...)` / `gameAdmin.updateVrfCoordinatorAndSub(...)` calls, and the `DegenerusGame.sol` selector-routed entry points) with verified line numbers, and note any naming drift between CONTEXT ("DegenerusVault") and the verified source. This trace is the evidence base for the §4 VRF-05 design assertion that the D-03 lock + D-01/D-02 safe-rotation cover the routed path.

    Per feedback_contract_locations: contract reads from `contracts/` only. Do NOT modify any `.sol` or `test/` file.
  </action>
  <verify>
    <automated>grep -q "§9d-Anchor" .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md && grep -Eq "HANDOFF-78" .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md && grep -Eq "HANDOFF-86" .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md && grep -Eq "ADMA-02" .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md && grep -Eq "DegenerusAdmin\.sol|DegenerusGame\.sol" .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md && git diff --quiet -- contracts/ test/ && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - §0 now contains a `### §0.X §9d-Anchor → Closing-Change Mapping` table with one row per cluster anchor (HANDOFF-78/85/86/87/88/89/90/91 + ADMA-01/02), each mapped to a decision ID (D-01..D-05) and the requirement it closes (VRF-01..05).
    - §0 contains a `### §0.Y Vault/Admin-Routed Reach Trace` recording the actual dispatch sites for both VRF admin functions with grep-verified line numbers, and an explicit note on the CONTEXT "DegenerusVault" vs verified-source naming drift.
    - Every §9d anchor row cross-references the FINDINGS-v44.0.md §9d source row.
    - `git diff --quiet -- contracts/ test/` returns clean.
  </acceptance_criteria>
  <done>§0 has a complete §9d-anchor→closing-change mapping covering all 10 cluster anchors and a grep-verified vault/admin-routed reach trace; the manifest is now the complete evidence base for Plan 02; no contract/test files touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a design (SPEC) phase — no runtime trust boundary is crossed. The relevant boundary is **design-correctness**: an incomplete or wrong §0 evidence base would let a false call-path assertion enter the locked design and re-open the defect at Phase 312 IMPL.

| Boundary | Description |
|----------|-------------|
| CONTEXT-claimed anchors → verified source HEAD | CONTEXT.md `<canonical_refs>` may have drifted vs HEAD; an unverified claim is untrusted input to the design |
| CONTEXT "DegenerusVault" name → actual dispatch site | a mis-named or unverified vault-reach path could leave a routed VRF-config mutation surface uncovered (ADMA-02 / VRF-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-311-01 | Tampering (design-integrity) | §0 manifest line citations | mitigate | Every anchor re-grepped against HEAD; VERIFIED/DRIFTED status recorded per row; drift recorded, never silently rewritten (feedback_verify_call_graph_against_source) |
| T-311-02 | Information disclosure (missed surface) | vault/admin-routed VRF dispatch (ADMA-02) | mitigate | §0.Y traces the ACTUAL dispatch sites across contracts/ and records the CONTEXT-vs-source naming drift, so §4 VRF-05 cannot assume an unverified path |
| T-311-03 | Repudiation (unfalsifiable claim) | "by construction" assertions | mitigate | §0 prose forbids "by construction"; every downstream §1–§6 claim must cite a §0 manifest row |
| T-311-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan (doc-only); nothing to verify |
</threat_model>

<verification>
- `311-SPEC.md` exists with §0 fully authored and §1–§6 headings present as placeholders.
- §0 manifest covers every CONTEXT `<canonical_refs>` anchor with a VERIFIED/DRIFTED status and a recorded HEAD sha.
- §0 §9d mapping covers all 10 cluster anchors; §0.Y vault-reach trace records actual dispatch sites.
- `git diff --quiet -- contracts/ test/` is clean.
</verification>

<success_criteria>
- The grep-verification obligation is fully discharged as a self-contained, checkable manifest (ROADMAP Success Criterion 5 evidence base).
- Zero "by construction" claims; every anchor has a recorded verified line.
- Zero contracts/ + zero test/ mutations.
</success_criteria>

<output>
Create `.planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-01-SUMMARY.md` when done.
</output>
