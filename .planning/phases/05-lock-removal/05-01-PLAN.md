---
phase: 05-lock-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/modules/DegenerusGameMintModule.sol
  - contracts/modules/DegenerusGameLootboxModule.sol
  - contracts/modules/DegenerusGameDegeneretteModule.sol
  - contracts/modules/DegenerusGameAdvanceModule.sol
  - test/fuzz/LockRemoval.t.sol
autonomous: true
requirements: [LOCK-01, LOCK-02, LOCK-03, LOCK-04, LOCK-05, LOCK-06]

must_haves:
  truths:
    - "A ticket purchase submitted while rngLockedFlag==true does not revert"
    - "A lootbox open submitted while rngLockedFlag==true does not revert"
    - "jackpotResolutionActive is true on lastPurchaseDay at jackpot levels regardless of rngLockedFlag"
    - "The full Foundry test suite passes with zero regressions"
    - "grep for rngLockedFlag in MintModule, LootboxModule, DegeneretteModule returns zero results"
  artifacts:
    - path: "test/fuzz/LockRemoval.t.sol"
      provides: "LockRemovalHarness + guard-logic unit tests for LOCK-01 through LOCK-06"
      min_lines: 80
    - path: "contracts/modules/DegenerusGameMintModule.sol"
      provides: "Purchase paths without rngLockedFlag guards"
      contains: "No rngLockedFlag references"
    - path: "contracts/modules/DegenerusGameLootboxModule.sol"
      provides: "Lootbox opens without rngLockedFlag guards"
      contains: "No rngLockedFlag references, no error RngLocked declaration"
    - path: "contracts/modules/DegenerusGameDegeneretteModule.sol"
      provides: "jackpotResolutionActive without rngLockedFlag"
      contains: "No rngLockedFlag references"
    - path: "contracts/modules/DegenerusGameAdvanceModule.sol"
      provides: "requestLootboxRng without redundant rngLockedFlag check"
      contains: "Exactly 6 rngLockedFlag references (lines 129, 1148, 1215, 1227, 1239, 1276)"
  key_links:
    - from: "test/fuzz/LockRemoval.t.sol"
      to: "contracts/storage/DegenerusGameStorage.sol"
      via: "LockRemovalHarness extends DegenerusGameStorage"
      pattern: "contract LockRemovalHarness is DegenerusGameStorage"
    - from: "contracts/modules/DegenerusGameMintModule.sol"
      to: "contracts/storage/DegenerusGameStorage.sol"
      via: "gameOver guard remains, rngLockedFlag guard removed"
      pattern: "if \\(gameOver\\) revert"
---

<objective>
Remove the six rngLockedFlag purchase-path guards and validate the full always-open-purchases system end-to-end.

Purpose: This is the final phase of the always-open-purchases milestone. Phases 1-4 built double-buffered ticket queues, prize pool freeze/unfreeze, and drain gates. Those infrastructure layers make the rngLockedFlag purchase guards redundant. Removing them means players can buy tickets at any time, even during daily RNG processing.

Output: Modified contract modules with guards removed, LockRemoval test suite, passing full Foundry suite.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-lock-removal/05-RESEARCH.md

<interfaces>
<!-- Key storage fields and helpers the executor needs (from DegenerusGameStorage) -->

From contracts/storage/DegenerusGameStorage.sol:
```solidity
bool internal rngLockedFlag;      // Slot 1 -- stays in storage, only guard sites removed
bool internal gameOver;
bool internal lastPurchaseDay;
uint24 internal level;
error E();
```

From contracts/modules/DegenerusGameLootboxModule.sol:
```solidity
error RngLocked();  // line 46 -- remove after LOCK-03/LOCK-04 (no remaining usage)
```

From contracts/modules/DegenerusGameAdvanceModule.sol:
```solidity
error RngLocked();  // line 37 -- KEEP (still used by reverseFlip at line 1239)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create LockRemovalHarness and guard-logic tests</name>
  <files>test/fuzz/LockRemoval.t.sol</files>
  <behavior>
    - LOCK-01: callTicketPurchaseGuard does NOT revert when rngLockedFlag==true (only reverts on quantity==0, quantity>uint32.max, or gameOver)
    - LOCK-02: purchaseForLootboxGuard returns blocked==true only when lootBoxAmount!=0 AND lastPurchaseDay AND purchaseLevel%5==0 -- rngLockedFlag has no effect
    - LOCK-03: openLootBoxGuard does NOT revert when rngLockedFlag==true
    - LOCK-04: openBurnieLootBoxGuard does NOT revert when rngLockedFlag==true
    - LOCK-05: jackpotResolutionActive returns true when lastPurchaseDay AND (level+1)%5==0 -- rngLockedFlag has no effect
    - LOCK-06: requestLootboxRngGuard does NOT revert on rngLockedFlag alone (only reverts when rngWordByDay==0 or rngRequestTime!=0)
    - Fuzz: randomize rngLockedFlag, level, lastPurchaseDay; confirm guards depend only on the correct variables
  </behavior>
  <action>
Create `test/fuzz/LockRemoval.t.sol` following the established harness pattern (see AdvanceGameRewrite.t.sol, PrizePoolFreeze.t.sol).

1. Create `LockRemovalHarness is DegenerusGameStorage` with:
   - Setters: `setRngLockedFlag(bool)`, `setGameOver(bool)`, `setLastPurchaseDay(bool)`, `setLevel(uint24)`, `setRngRequestTime(uint48)`, `setRngWordByDay(uint256 day, uint256 word)`
   - Guard functions reproducing the POST-REMOVAL logic:
     - `callTicketPurchaseGuard(uint256 quantity)` -- reproduces MintModule:838-840 WITHOUT rngLockedFlag check
     - `purchaseForLootboxGuard(uint256 lootBoxAmount) returns (bool blocked)` -- reproduces MintModule:627 WITHOUT rngLockedFlag in condition
     - `openLootBoxGuard()` -- empty (guard removed entirely)
     - `openBurnieLootBoxGuard()` -- empty (guard removed entirely)
     - `jackpotResolutionActive() returns (bool)` -- reproduces DegeneretteModule:503 WITHOUT rngLockedFlag
     - `requestLootboxRngGuard(uint256 currentDay)` -- reproduces AdvanceModule:641-644 WITHOUT line 643

2. Create `LockRemovalTest is Test` with:
   - `test_LOCK01_purchaseDuringRngLock`: set rngLockedFlag=true, call callTicketPurchaseGuard(1) -- must NOT revert
   - `test_LOCK01_purchaseStillRevertsOnGameOver`: set gameOver=true, expect revert E()
   - `test_LOCK01_purchaseStillRevertsOnZeroQuantity`: call with quantity=0, expect revert E()
   - `test_LOCK02_lootboxGuardStripped`: set rngLockedFlag=true, lastPurchaseDay=false, call purchaseForLootboxGuard(1) -- must return false
   - `test_LOCK02_lootboxGuardBlocksOnJackpotLevel`: set lastPurchaseDay=true, level=4, call purchaseForLootboxGuard(1) -- must return true (level+1=5, 5%5==0)
   - `test_LOCK02_lootboxGuardPassesNonJackpot`: set lastPurchaseDay=true, level=5, call purchaseForLootboxGuard(1) -- must return false (6%5!=0)
   - `test_LOCK03_openLootBoxDuringLock`: set rngLockedFlag=true, call openLootBoxGuard() -- must NOT revert
   - `test_LOCK04_openBurnieLootBoxDuringLock`: set rngLockedFlag=true, call openBurnieLootBoxGuard() -- must NOT revert
   - `test_LOCK05_degeneretteJackpotResolution`: set rngLockedFlag=true, lastPurchaseDay=true, level=4 -- jackpotResolutionActive() must return true
   - `test_LOCK05_degeneretteNotJackpotLevel`: set lastPurchaseDay=true, level=5 -- must return false
   - `test_LOCK06_lootboxRngRequestGate`: set rngLockedFlag=true but rngRequestTime=0 and rngWordByDay[day]!=0 -- must NOT revert
   - `test_LOCK06_lootboxRngStillBlocksOnActiveRequest`: set rngRequestTime=1 -- must revert E()
   - Fuzz test: `testFuzz_guardsIgnoreRngLockedFlag(bool rngLocked, uint24 lvl, bool lastDay)` -- set rngLockedFlag to rngLocked, verify guard behavior is identical regardless of rngLocked value

Run: `forge test --match-path test/fuzz/LockRemoval.t.sol -vvv` -- all tests must pass (tests validate the POST-REMOVAL guard logic, so they pass against the harness immediately).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge test --match-path test/fuzz/LockRemoval.t.sol -vvv</automated>
  </verify>
  <done>LockRemoval.t.sol exists with LockRemovalHarness and 13+ tests covering all six LOCK requirements plus fuzz. All tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Remove six rngLockedFlag guards, update NatSpec, clean up error declarations</name>
  <files>contracts/modules/DegenerusGameMintModule.sol, contracts/modules/DegenerusGameLootboxModule.sol, contracts/modules/DegenerusGameDegeneretteModule.sol, contracts/modules/DegenerusGameAdvanceModule.sol</files>
  <action>
Make exactly these six changes (reference 05-RESEARCH.md for full context on each site):

**LOCK-01 -- MintModule line 840:**
Delete the entire line `if (rngLockedFlag) revert E();`

**LOCK-02 -- MintModule line 627:**
Change `if (lootBoxAmount != 0 && rngLockedFlag && lastPurchaseDay && (purchaseLevel % 5 == 0)) revert E();`
to `if (lootBoxAmount != 0 && lastPurchaseDay && (purchaseLevel % 5 == 0)) revert E();`
Also update the NatSpec comment at line 626 -- change "Block lootbox purchases during BAF/Decimator resolution (jackpot levels)" to reflect that the block is based on lastPurchaseDay and jackpot level, not rngLockedFlag.

**LOCK-03 -- LootboxModule line 557:**
Delete the entire line `if (rngLockedFlag) revert RngLocked();`
Update NatSpec above `openLootBox` (lines 548-555): remove `@custom:reverts RngLocked When RNG is locked during jackpot resolution` and remove any `@dev` text referencing RNG lock blocking.

**LOCK-04 -- LootboxModule line 640:**
Delete the entire line `if (rngLockedFlag) revert RngLocked();`
Update NatSpec above `openBurnieLootBox` (lines 631-638): same NatSpec cleanup as LOCK-03.

**After LOCK-03 + LOCK-04:** Remove `error RngLocked();` declaration from LootboxModule (line 46) since it has zero remaining usage sites.

**LOCK-05 -- DegeneretteModule line 503:**
Change `jackpotResolutionActive = rngLockedFlag && lastPurchaseDay && ((level + 1) % 5 == 0);`
to `jackpotResolutionActive = lastPurchaseDay && ((level + 1) % 5 == 0);`

**LOCK-06 -- AdvanceModule line 643:**
Delete the entire line `if (rngLockedFlag) revert E();`

**DO NOT touch any other rngLockedFlag references.** The variable declaration, set/clear sites, reverseFlip guard, VRF callback routing, autorebuy/takeprofit/afking guards, and view functions all remain unchanged.

**Verification grep (run after all edits):**
- `grep -n rngLockedFlag contracts/modules/DegenerusGameMintModule.sol` -- expect ZERO results
- `grep -n rngLockedFlag contracts/modules/DegenerusGameLootboxModule.sol` -- expect ZERO results
- `grep -n rngLockedFlag contracts/modules/DegenerusGameDegeneretteModule.sol` -- expect ZERO results
- `grep -n rngLockedFlag contracts/modules/DegenerusGameAdvanceModule.sol` -- expect exactly 6 results (lines ~129, ~1148, ~1215, ~1227, ~1239, ~1276)
- `grep -n RngLocked contracts/modules/DegenerusGameLootboxModule.sol` -- expect ZERO results (error declaration removed)

Then run the full test suite: `forge test` -- must pass with zero failures.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -cn rngLockedFlag contracts/modules/DegenerusGameMintModule.sol contracts/modules/DegenerusGameLootboxModule.sol contracts/modules/DegenerusGameDegeneretteModule.sol && forge test</automated>
  </verify>
  <done>All six rngLockedFlag purchase-path guards removed. NatSpec updated. error RngLocked() removed from LootboxModule. grep confirms zero rngLockedFlag references in MintModule, LootboxModule, DegeneretteModule. AdvanceModule has exactly 6 remaining references (all out-of-scope). Full Foundry test suite passes.</done>
</task>

</tasks>

<verification>
1. `grep -n rngLockedFlag contracts/modules/DegenerusGameMintModule.sol` returns zero results
2. `grep -n rngLockedFlag contracts/modules/DegenerusGameLootboxModule.sol` returns zero results
3. `grep -n rngLockedFlag contracts/modules/DegenerusGameDegeneretteModule.sol` returns zero results
4. `grep -n rngLockedFlag contracts/modules/DegenerusGameAdvanceModule.sol` returns exactly 6 results (lines 129, 1148, 1215, 1227, 1239, 1276)
5. `grep -n RngLocked contracts/modules/DegenerusGameLootboxModule.sol` returns zero results
6. `forge test --match-path test/fuzz/LockRemoval.t.sol -vvv` -- all tests pass
7. `forge test` -- full suite passes with zero failures
</verification>

<success_criteria>
- Six rngLockedFlag purchase-path guards removed from four contract modules
- NatSpec updated at all modified function sites
- error RngLocked() removed from LootboxModule (zero remaining usage)
- LockRemoval.t.sol test suite covers all six LOCK requirements with unit + fuzz tests
- Full Foundry test suite green (zero regressions)
- Zero rngLockedFlag references in MintModule, LootboxModule, DegeneretteModule
- Exactly 6 rngLockedFlag references remain in AdvanceModule (all out-of-scope)
</success_criteria>

<output>
After completion, create `.planning/phases/05-lock-removal/05-01-SUMMARY.md`
</output>
