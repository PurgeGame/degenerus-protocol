---
phase: 142-documentation-submission-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - KNOWN-ISSUES.md
  - contracts/DegenerusGame.sol
  - audit/C4A-CONTEST-README.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, SUB-01, SUB-02, SUB-03]

must_haves:
  truths:
    - "KNOWN-ISSUES.md documents turbo-at-L0 unreachable as a design decision"
    - "KNOWN-ISSUES.md documents backfill cap >120 days as design decision (frozen stakes, not lost)"
    - "NatSpec on DegenerusGame constructor mentions dailyIdx initialization"
    - "C4A contest README references the v10.0 delta audit findings (not just v8.0)"
    - "No submission-facing document references stale behavior"
    - "Test suite passes with 0 failures"
  artifacts:
    - path: "KNOWN-ISSUES.md"
      provides: "Turbo-at-L0 + backfill cap design decision entries"
      contains: "turbo"
    - path: "contracts/DegenerusGame.sol"
      provides: "Updated constructor NatSpec"
      contains: "dailyIdx"
    - path: "audit/C4A-CONTEST-README.md"
      provides: "Updated Known Issues summary referencing v10.0 delta"
      contains: "v10.0"
  key_links:
    - from: "KNOWN-ISSUES.md"
      to: "audit/C4A-CONTEST-README.md"
      via: "C4A README references KNOWN-ISSUES.md Known Issues section"
      pattern: "KNOWN-ISSUES.md"
---

<objective>
Update all submission-facing documentation with Phase 141 delta audit findings, verify NatSpec accuracy, sweep for stale references, and confirm test suite passes.

Purpose: Wardens receive accurate documentation reflecting current contract behavior. Every finding from the delta audit is pre-disclosed to minimize payable submissions.
Output: Updated KNOWN-ISSUES.md, NatSpec, C4A README, green test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-delta-adversarial-audit/141-01-SUMMARY.md

@KNOWN-ISSUES.md
@audit/C4A-CONTEST-README.md
@contracts/DegenerusGame.sol (constructor NatSpec, lines 237-245)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update KNOWN-ISSUES.md and C4A README with delta audit findings</name>
  <files>KNOWN-ISSUES.md, audit/C4A-CONTEST-README.md</files>
  <read_first>
    - KNOWN-ISSUES.md (full file, ~127 lines)
    - audit/C4A-CONTEST-README.md (full file, ~102 lines)
    - .planning/phases/141-delta-adversarial-audit/141-01-SUMMARY.md (delta audit findings)
  </read_first>
  <action>
    **KNOWN-ISSUES.md updates (per DOC-01, SUB-03):**

    Add two new entries to the "Design Decisions" section (after the existing "Chainlink VRF V2.5 dependency" entry, since they relate to VRF/gap mechanics):

    1. **Turbo mode unreachable at level 0.** Entry text:
       "**Turbo mode unreachable at level 0.** Turbo (compressedJackpotFlag=2) requires `purchaseDays <= 1`, but at level 0 `purchaseStartDay` is the Solidity default (0), making `purchaseDays = currentDay` which exceeds 1 after deploy day. All 5 consumers of `compressedJackpotFlag` traced and verified safe with turbo inert at L0. Turbo remains reachable at levels >= 1 where `purchaseStartDay` is set to the transition day. This is an accepted design consequence of the `dailyIdx` initialization change."

    2. **Backfill cap at 120 gap days.** Entry text:
       "**Backfill cap at 120 gap days.** `_backfillGapDays` caps iteration at 120 days to stay within block gas limit (~9M gas). If a VRF stall exceeds 120 days, gap days beyond the cap are skipped -- coinflip stakes for those days are frozen (not lost or burned). The `skip-unresolved` handling in BurnieCoinflip (rewardPercent=0 && !win) silently advances past unresolved days. This scenario requires a sustained 4-month Chainlink VRF outage with no coordinator migration -- an unprecedented infrastructure failure affecting the entire ecosystem."

    **C4A README update (per DOC-03):**

    Update the Known Issues section (line 43) to replace:
    "Post-v8.0 delta adversarial audit (6 INFO findings, 0 actionable) also incorporated."
    with:
    "Post-v8.0 delta adversarial audit (6 INFO findings, 0 actionable) and v10.0 delta audit (0 vulnerabilities, 2 INFO — turbo unreachable at L0, backfill cap >120 days) also incorporated."

    **Stale reference sweep (per SUB-02):**
    After making changes, grep all submission-facing files (KNOWN-ISSUES.md, audit/C4A-CONTEST-README.md) for any references to "dailyIdx = 0", "dailyIdx defaults to 0", or "dailyIdx starts at zero" -- there should be none. Also verify no stale references to turbo being active at level 0 exist in these files.
  </action>
  <verify>
    <automated>grep -c "turbo" KNOWN-ISSUES.md && grep -c "Backfill cap" KNOWN-ISSUES.md && grep -c "v10.0" audit/C4A-CONTEST-README.md</automated>
  </verify>
  <acceptance_criteria>
    - KNOWN-ISSUES.md has a "Turbo mode unreachable at level 0" design decision entry
    - KNOWN-ISSUES.md has a "Backfill cap at 120 gap days" design decision entry
    - C4A README Known Issues section references v10.0 delta audit
    - No stale references to dailyIdx=0 or turbo-at-L0 in submission-facing docs
  </acceptance_criteria>
  <done>Both design decisions documented, C4A README updated, zero stale references in submission-facing docs</done>
</task>

<task type="auto">
  <name>Task 2: Update constructor NatSpec and verify no stale NatSpec on changed functions</name>
  <files>contracts/DegenerusGame.sol</files>
  <read_first>
    - contracts/DegenerusGame.sol (lines 237-250, constructor NatSpec and code)
    - contracts/modules/DegenerusGameAdvanceModule.sol (lines 1490-1522, _backfillGapDays NatSpec -- already accurate, verify only)
  </read_first>
  <action>
    **NatSpec update (per DOC-02):**

    Update the constructor NatSpec in DegenerusGame.sol (lines 237-242). The current @dev block mentions `levelStartTime` but not `dailyIdx`. Add `dailyIdx` to the @dev description.

    Change line 240-241 from:
    ```
     *      levelStartTime is initialized here to the deploy timestamp.
     *      Deploy day boundary determines which calendar day is "day 1" in the game.
    ```
    to:
    ```
     *      levelStartTime is initialized here to the deploy timestamp.
     *      dailyIdx is set to the current day index so gap detection starts from deploy day.
     *      Deploy day boundary determines which calendar day is "day 1" in the game.
    ```

    **Verify _backfillGapDays NatSpec (per DOC-02):**
    Read _backfillGapDays NatSpec (AdvanceModule lines 1490-1501). Confirm it already mentions the cap and gap day behavior. It does -- no changes needed there. Just verify and move on.

    **IMPORTANT:** Do NOT modify ContractAddresses.sol per project memory rules. Only touch DegenerusGame.sol constructor NatSpec.
  </action>
  <verify>
    <automated>grep -n "dailyIdx" contracts/DegenerusGame.sol | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Constructor NatSpec in DegenerusGame.sol mentions dailyIdx initialization
    - _backfillGapDays NatSpec in AdvanceModule confirmed accurate (no changes needed)
    - No other NatSpec on changed functions is stale
  </acceptance_criteria>
  <done>Constructor NatSpec describes dailyIdx initialization, all changed function NatSpec verified accurate</done>
</task>

<task type="auto">
  <name>Task 3: Run test suite and confirm 0 failures</name>
  <files></files>
  <read_first>
    - (none -- just run the test command)
  </read_first>
  <action>
    **Test verification (per SUB-01):**

    Run the Hardhat test suite:
    ```
    npx hardhat test
    ```

    The NatSpec change in Task 2 is a comment-only change to a .sol file, so contracts must recompile. Confirm:
    1. Compilation succeeds with no errors
    2. All tests pass (expect ~1351 passing based on recent runs)
    3. Zero failures

    If any test fails, investigate whether the failure is related to the NatSpec change (it should not be -- comment-only). If unrelated pre-existing failure, document it but do not attempt to fix contract code without user approval.
  </action>
  <verify>
    <automated>npx hardhat test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Hardhat compilation succeeds
    - All tests pass with 0 failures
    - No regressions from the NatSpec comment change
  </acceptance_criteria>
  <done>Test suite passes with 0 failures, compilation clean</done>
</task>

</tasks>

<verification>
1. `grep "turbo" KNOWN-ISSUES.md` returns matches (turbo design decision documented)
2. `grep "Backfill cap" KNOWN-ISSUES.md` returns matches (backfill cap documented)
3. `grep "v10.0" audit/C4A-CONTEST-README.md` returns matches (delta audit referenced)
4. `grep "dailyIdx" contracts/DegenerusGame.sol` includes NatSpec line in constructor block
5. `npx hardhat test` passes with 0 failures
6. `grep -r "dailyIdx.*=.*0\|dailyIdx defaults" KNOWN-ISSUES.md audit/C4A-CONTEST-README.md` returns nothing (no stale refs)
</verification>

<success_criteria>
- KNOWN-ISSUES.md has 2 new design decision entries (turbo-at-L0, backfill cap)
- C4A README references v10.0 delta audit
- Constructor NatSpec mentions dailyIdx
- Test suite: 0 failures
- Zero stale references in submission-facing documentation
</success_criteria>

<output>
After completion, create `.planning/phases/142-documentation-submission-readiness/142-01-SUMMARY.md`
</output>
