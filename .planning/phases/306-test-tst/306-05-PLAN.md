---
phase: 306-test-tst
plan: 05
type: execute
wave: 1
subsystem: Gas regression bench — RedemptionGas
tags: [TST, gas-regression, sStonk, theoretical-worst-case, burn-path, claim-path, v44.0]
depends_on: []
requirements: [TST-06]
files_modified:
  - test/fuzz/RedemptionGas.t.sol
autonomous: true
user_setup: []

must_haves:
  truths:
    - "`forge build` exits 0 against the modified gas-bench file"
    - "Theoretical worst-case gas derived FIRST per `feedback_gas_worst_case.md`, documented inline in NatSpec ABOVE each gas-assertion block; THEN tested against actual `gasleft()`-bracketed measurement"
    - "Burn-path gas regression assertion: actual gas for `sdgnrs.burn(amount)` at v44 ≤ v43 baseline gas × 1.05 (5% headroom per ROADMAP §306 Success Criterion 5)"
    - "Claim-path gas regression assertion: actual gas for `sdgnrs.claimRedemption(day)` at v44 ≤ v43 baseline gas × 1.00 (no regression — per-day keying simpler than period-index lookup per ROADMAP claim)"
    - "v43 baseline gas captured from `git show MILESTONE_V43_AT_HEAD_8111cfc5189f628b64b500c881f9995c3edf0ed2:test/fuzz/RedemptionGas.t.sol` + `forge snapshot` run against checked-out v43 source (executor performs the baseline capture step explicitly + records baseline numbers as `bytes32`-encoded constants in the bench file)"
    - "AGENT-COMMITTED test-tree commit per `D-43N-TEST-COMMITS-AUTO-01` lineage; zero `contracts/*.sol` mutations"
  artifacts:
    - path: "test/fuzz/RedemptionGas.t.sol"
      provides: "Gas regression bench with theoretical-worst-case derivation + actual measurement + assertion against v43 baseline (5% burn / 0% claim)"
      contains: "test_gas_regression_burn"
  key_links:
    - from: "test/fuzz/RedemptionGas.t.sol::test_gas_regression_burn"
      to: "GAS_BASELINE_V43_BURN_FIRST_OF_DAY constant"
      via: "actual gas / baseline gas ≤ 1.05 ratio assertion"
      pattern: "GAS_BASELINE_V43_BURN"
    - from: "test/fuzz/RedemptionGas.t.sol::test_gas_regression_claim"
      to: "GAS_BASELINE_V43_CLAIM constant"
      via: "actual gas / baseline gas ≤ 1.00 ratio assertion"
      pattern: "GAS_BASELINE_V43_CLAIM"
---

<objective>
Extend `test/fuzz/RedemptionGas.t.sol` with TWO gas-regression assertion tests per TST-06 + ROADMAP §306 Success Criterion 5 (burn-path gas ≤ v43 +5%, claim-path gas ≤ v43 +0%). Per `feedback_gas_worst_case.md`, the executor MUST derive the THEORETICAL worst-case gas FIRST (cold-SLOAD counts + storage-write counts + revert-path arithmetic + event emissions + external-call gas), then capture v43.0 baseline `forge snapshot` numbers from `git show MILESTONE_V43_AT_HEAD_8111cfc5189f628b64b500c881f9995c3edf0ed2:test/fuzz/RedemptionGas.t.sol`, then assert v44 measured gas against the baseline ratios. NO "just run forge snapshot and accept whatever" — the theoretical worst-case derivation comes FIRST per feedback.

Purpose: TST-06 closure for v44.0. The gas regression assertion is the load-bearing closure rule for Phase 305's gas-cost-budget claim — per Phase 305 SUMMARY, the 1-slot DayPending tightening saved ~44k gas per first-burn-of-day and ~10k per subsequent same-day burn vs the original SPEC-01 3-slot design; the regression bench verifies v44 ACTUAL gas relative to v43 BASELINE, NOT relative to the never-shipped SPEC-01 3-slot intermediate. Plan 05 has no contract dependencies and runs in Wave 1 fully parallel with Plans 01/02/03/04 — modifies only test/fuzz/RedemptionGas.t.sol.

Output: 2 new gas-regression test functions appended to `test/fuzz/RedemptionGas.t.sol` (test_gas_regression_burn + test_gas_regression_claim) + 2 baseline-constant declarations + theoretical-worst-case derivation NatSpec blocks. AGENT-COMMITTED test-tree commit envelope per `D-43N-TEST-COMMITS-AUTO-01`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/agent-contracts.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/304-spec-invariant-model-spec/304-SPEC.md
@.planning/phases/305-implementation-impl/305-01-SUMMARY.md
@contracts/StakedDegenerusStonk.sol
@test/fuzz/RedemptionGas.t.sol

<interfaces>
Existing gas-bench surface (test/fuzz/RedemptionGas.t.sol, 149 lines, already updated for v44 signatures by Phase 305 Mutation 13):

```
test_gas_burn_gambling         — gambling burn during active game (line 46)
test_gas_burnWrapped_gambling  — wrapped variant (line 54)
test_gas_resolveRedemptionPeriod — resolve via game prank with 3-arg signature (line 70)
test_gas_claimRedemption       — full lifecycle: burn + resolve + mock coinflip + claim (line 86)
test_gas_hasPendingRedemptions_true  — pending pool present (line 121)
test_gas_hasPendingRedemptions_false — no pending pool (line 132)
test_gas_previewBurn           — view fn (line 144)
```

These existing tests benchmark gas via `forge snapshot --match-path "test/fuzz/RedemptionGas.t.sol"`. The new gas-regression tests added by this plan ASSERT against captured baselines, not just generate snapshots.

v43 baseline capture method (in Task 1):

```
cd /home/zak/Dev/PurgeGame/degenerus-audit
# Stash current state
git stash -u || true
# Checkout v43 closure HEAD
git checkout 8111cfc5189f628b64b500c881f9995c3edf0ed2 -- test/fuzz/RedemptionGas.t.sol contracts/ test/fuzz/helpers/DeployProtocol.sol
# Take snapshot
FOUNDRY_PROFILE=default forge snapshot --match-path "test/fuzz/RedemptionGas.t.sol" --match-test "test_gas_burn_gambling|test_gas_claimRedemption" 2>&1 | tee /tmp/v43-gas-snapshot.txt
# Restore working tree
git checkout HEAD -- test/fuzz/RedemptionGas.t.sol contracts/ test/fuzz/helpers/DeployProtocol.sol
git stash pop || true
```

Capture the exact numbers from the snapshot output (e.g. `test_gas_burn_gambling() (gas: 123456)`) and encode as Solidity `uint256 constant` literals in the bench file.

The new gas-regression tests use Foundry's `gasleft()` bracketing pattern:

```
uint256 gasBefore = gasleft();
// target call
uint256 gasAfter = gasleft();
uint256 actualGas = gasBefore - gasAfter;
assertLe(actualGas, (GAS_BASELINE_V43_X * 105) / 100, "burn-path gas regression: > +5% over v43 baseline");
```

`assertLe(actualGas, (GAS_BASELINE_V43_BURN_FIRST_OF_DAY * 105) / 100, "...")` is the burn-path 5% headroom assertion.
`assertLe(actualGas, GAS_BASELINE_V43_CLAIM, "...")` is the claim-path 0% headroom assertion.
</interfaces>

<patterns>
1. **Theoretical worst-case derivation FIRST** (feedback_gas_worst_case.md). For burn-path:
   - Cold SLOADs: ContractAddresses.GAME (no SLOAD — immutable), game.currentDayView() (CALL ~2600), game.rngLocked() (CALL ~2600), game.livenessTriggered() (CALL ~2600), game.gameOver() (CALL ~2600), `pendingResolveDay` SLOAD (2100 cold + 100 warm)
   - 1-slot DayPending: 1 SLOAD (2100 cold), 1 SSTORE (~20k for non-zero from zero on lazy-init, ~5k for non-zero update on subsequent burn-of-day)
   - balanceOf[burnFrom] SLOAD (2100 cold) + SSTORE (~5k)
   - totalSupply SLOAD (2100 cold, sentinel is itself written) + SSTORE (~5k)
   - address(this).balance OPCODE (cheap), steth.balanceOf CALL (~2600), _claimableWinnings → game.claimableWinningsOf CALL (~2600)
   - pendingRedemptionEthValue SLOAD + SSTORE (~5k + 5k cold)
   - coin.balanceOf CALL (~2600), coinflip.previewClaimCoinflips CALL (~2600)
   - pendingRedemptionBurnie SLOAD + SSTORE (~5k + 5k cold)
   - pendingRedemptions[player][D] composite-keyed SLOAD (cold, 2100) + SSTORE (~20k non-zero from zero or ~5k update)
   - game.playerActivityScore CALL (~2600)
   - Emit Transfer + RedemptionSubmitted events (~1000 each)
   - Total theoretical worst-case (first burn of day): ~115k-135k gas

   For claim-path:
   - pendingRedemptions[player][D] composite SLOAD (2100 cold)
   - redemptionPeriods[D] SLOAD (2100 cold) — read roll + flipDay
   - game.gameOver() CALL (~2600)
   - coinflip.getCoinflipDayResult CALL (~2600)
   - pendingRedemptionEthValue SLOAD + SSTORE (~5k + 5k)
   - delete pendingRedemptions[player][D] SSTORE refund (~-4800)
   - game.resolveRedemptionLootbox CALL if lootboxEth != 0 (~30k-50k)
   - coin.transfer ERC20 call for BURNIE if payout != 0 (~30k-50k)
   - .call{value: ethDirect}("") raw ETH transfer (~6000-20k)
   - Emit RedemptionClaimed (~1000)
   - Total theoretical worst-case: ~80k-130k gas

2. **v43 baseline capture protocol** — git-stash + checkout-v43 + forge-snapshot + restore. Document the exact baseline numbers as inline NatSpec comments above the `GAS_BASELINE_V43_*` constants for reproducibility per `feedback_verify_call_graph_against_source.md`.

3. **gasleft() bracketing** — Foundry idiom: `uint256 gasBefore = gasleft(); target_call(); uint256 actualGas = gasBefore - gasleft();`. Compiles to ~50 gas overhead for the bracketing itself.

4. **Per-function FOUNDRY_PROFILE** — gas tests run at FOUNDRY_PROFILE=default (not =deep), no fuzz runs needed for the deterministic gas measurement. The bench measures a single deterministic call path.
</patterns>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture v43.0 baseline gas + derive v44 theoretical worst case</name>
  <files>.planning/phases/306-test-tst/306-05-GAS-BASELINE.md</files>
  <read_first>
    Read `contracts/StakedDegenerusStonk.sol` lines 530-560 (burn entry) + 675-740 (claimRedemption body) + 800-895 (_submitGamblingClaimFrom) + `feedback_gas_worst_case.md` + the existing `test/fuzz/RedemptionGas.t.sol`.
  </read_first>
  <action>
    Create the gas baseline + theoretical worst case artifact at `.planning/phases/306-test-tst/306-05-GAS-BASELINE.md`. The artifact:

    1. **v43 baseline capture protocol** — document the exact command sequence (`git stash` → `git checkout 8111cfc5 -- test/fuzz/RedemptionGas.t.sol contracts/ test/fuzz/helpers/DeployProtocol.sol` → `forge snapshot` → restore working tree). Execute the protocol; record the exact gas numbers from `forge snapshot` output for `test_gas_burn_gambling` + `test_gas_claimRedemption` at v43 HEAD. If the v43 source's `RedemptionGas.t.sol` uses different function signatures (it does — v43 had 2-arg `resolveRedemptionPeriod` + 0-arg `claimRedemption` + 0-arg `hasPendingRedemptions`), document the v43 snapshot numbers EXACTLY as captured. Do NOT compare apples to oranges — the v43 baseline is the v43-code-against-v43-tests result.

    2. **v44 theoretical worst-case derivation** — per `feedback_gas_worst_case.md`, derive cold-SLOAD count + SSTORE count + external CALL gas + event emission gas for each of the two paths (burn-first-of-day + claim full-lifecycle). Tabulate per-op gas cost (using EIP-2929 + EIP-3529 reference numbers) + sum to a theoretical upper bound. Document each line of the burn/claim function and its gas attribution. Express the theoretical-worst-case as a single uint256 each (e.g. `THEORY_BURN_WORST_CASE = 135_000; THEORY_CLAIM_WORST_CASE = 130_000;`).

    3. **Asserted regression ratio computation** — `BURN_LIMIT_V44 = (GAS_BASELINE_V43_BURN_FIRST_OF_DAY * 105) / 100`. `CLAIM_LIMIT_V44 = GAS_BASELINE_V43_CLAIM` (no headroom). Document both expected limits.

    4. **Comparison framework** — present a 3-column table per path: (a) v43 baseline measured gas, (b) v44 theoretical worst case, (c) v44 assertion limit. The actual v44 measured gas (Task 2 produces this) should fit between (b) and (c). If the actual exceeds (c), TST-06 FAILS and the regression is a real finding for Phase 308 disposition.

    Artifact length target: 200-300 lines. AGENT-COMMITTED in the same envelope as Task 2 + Task 3.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && test -s .planning/phases/306-test-tst/306-05-GAS-BASELINE.md && grep -cE "(THEORY_BURN_WORST_CASE|GAS_BASELINE_V43_BURN|BURN_LIMIT_V44)" .planning/phases/306-test-tst/306-05-GAS-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - Artifact exists + is non-empty
    - Contains exact captured v43 baseline gas numbers for the 2 paths
    - Contains theoretical worst-case derivation tables with per-op attribution
    - Contains computed assertion limits (BURN_LIMIT_V44 = baseline × 1.05, CLAIM_LIMIT_V44 = baseline × 1.00)
    - Comparison framework table present
  </acceptance_criteria>
  <done>
    The v43 baseline is captured (recorded numbers from `forge snapshot` against v43 HEAD), v44 theoretical worst case is derived from first principles per `feedback_gas_worst_case.md`, and assertion limits are computed. Phase 308 §3.A delta-surface can cite this artifact for the gas-budget portion of the closure attestation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append test_gas_regression_burn + test_gas_regression_claim to RedemptionGas.t.sol</name>
  <files>test/fuzz/RedemptionGas.t.sol</files>
  <read_first>
    Read `.planning/phases/306-test-tst/306-05-GAS-BASELINE.md` (Task 1 output) + `test/fuzz/RedemptionGas.t.sol` lines 1-149 (existing bench file).
  </read_first>
  <action>
    Edit `test/fuzz/RedemptionGas.t.sol` to add 2 gas-regression assertion tests + 4 baseline constants.

    1. **Add 4 uint256 constants** below the existing `PLAYER_SDGNRS` constant at line 15:
       - `uint256 internal constant GAS_BASELINE_V43_BURN_FIRST_OF_DAY = <value-from-Task-1>;` — exact v43 measured gas for the first-burn-of-day path (cold storage everywhere)
       - `uint256 internal constant GAS_BASELINE_V43_CLAIM = <value-from-Task-1>;` — exact v43 measured gas for the full-lifecycle claim path
       - `uint256 internal constant BURN_LIMIT_V44 = (GAS_BASELINE_V43_BURN_FIRST_OF_DAY * 105) / 100;` — 5% headroom
       - `uint256 internal constant CLAIM_LIMIT_V44 = GAS_BASELINE_V43_CLAIM;` — 0% headroom

       ABOVE each constant, NatSpec comment cites the source: `/// @dev v43 baseline gas captured 2026-05-19 at MILESTONE_V43_AT_HEAD_8111cfc5... per `.planning/phases/306-test-tst/306-05-GAS-BASELINE.md` §X`.

    2. **Add test_gas_regression_burn function** (after the existing test_gas_burnWrapped_gambling at line 64). NatSpec block above the function documents:
       - The theoretical worst case (from Task 1's table)
       - The v43 baseline number
       - The assertion ratio (≤ +5% per ROADMAP §306 Success Criterion 5)
       - The state setup required to hit the worst-case path (cold SLOADs everywhere → first burn of a fresh day after the contract has never been touched for that day)

       Body:
       - Mirror setUp's burn-prep (player has sDGNRS, game has claimable winnings)
       - Bracket the burn with `gasleft()`:
         - `vm.prank(player);`
         - `uint256 gasBefore = gasleft();`
         - `sdgnrs.burn(PLAYER_SDGNRS / 10);`
         - `uint256 actualGas = gasBefore - gasleft();`
       - Assertion: `assertLe(actualGas, BURN_LIMIT_V44, "TST-06: burn-path gas regression vs v43 baseline > +5%");`
       - Emit: `emit log_named_uint("actual_burn_gas", actualGas); emit log_named_uint("burn_limit_v44", BURN_LIMIT_V44);` for diagnostic visibility on test runs.

    3. **Add test_gas_regression_claim function** (after the existing test_gas_claimRedemption at line 114). Same pattern:
       - Mirror setUp + the burn + resolve + coinflip mock from test_gas_claimRedemption
       - Bracket the `sdgnrs.claimRedemption(currentDay);` call with `gasleft()`
       - Assertion: `assertLe(actualGas, CLAIM_LIMIT_V44, "TST-06: claim-path gas regression vs v43 baseline > +0% (no regression allowed)");`
       - Diagnostic log_named_uint for actualGas + CLAIM_LIMIT_V44

    Final file size target: ~230-260 lines (up from 149).

    DO NOT modify any existing test_gas_* function bodies — they remain as gas-snapshot generators. Only APPEND the 2 new regression-assertion tests.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -10 && forge test --match-path "test/fuzz/RedemptionGas.t.sol" --match-test "test_gas_regression" -vv 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` exits 0
    - Both `test_gas_regression_burn` and `test_gas_regression_claim` exist + PASS (assertion holds at v44 source)
    - 4 GAS_BASELINE_V43_* / BURN_LIMIT_V44 / CLAIM_LIMIT_V44 constants present with NatSpec citations
    - Each new test emits log_named_uint diagnostic for actual gas + limit values (visible in -vv output)
    - If burn-path gas exceeds BURN_LIMIT_V44 or claim-path exceeds CLAIM_LIMIT_V44, the test FAILS and surfaces a regression for Phase 308 disposition
  </acceptance_criteria>
  <done>
    2 gas-regression assertions in place + 4 baseline constants + theoretical-worst-case derivation cited via NatSpec. TST-06 closure-rule mechanized.
  </done>
</task>

<task type="auto">
  <name>Task 3: AGENT-COMMITTED test-tree commit envelope + SUMMARY</name>
  <files>.planning/phases/306-test-tst/306-05-SUMMARY.md</files>
  <read_first>
    Read the SUMMARY template + Phase 305 SUMMARY precedent + `.planning/phases/306-test-tst/306-05-GAS-BASELINE.md` (Task 1).
  </read_first>
  <action>
    Write `.planning/phases/306-test-tst/306-05-SUMMARY.md`. Frontmatter:
    - phase: 306-test-tst
    - plan: 05
    - subsystem: Gas regression bench — RedemptionGas
    - tags: [TST, gas-regression, sStonk, v44.0]
    - requirements-completed: [TST-06]

    Body documents:
    1. The theoretical-worst-case derivation methodology + result (from 306-05-GAS-BASELINE.md)
    2. The v43 baseline gas numbers (exact values from forge snapshot)
    3. The v44 measured gas numbers (from the test runs)
    4. Pass/fail verdict on burn-path ≤ +5% AND claim-path ≤ +0%
    5. If either fails, document the regression for Phase 308 §3.A delta-surface disposition

    Commit envelope:
    - `git add -f test/fuzz/RedemptionGas.t.sol .planning/phases/306-test-tst/306-05-GAS-BASELINE.md .planning/phases/306-test-tst/306-05-SUMMARY.md`
    - Use `gsd-sdk query commit` with message `test(306-05): gas regression bench — TST-06 closure (burn ≤ +5% v43, claim ≤ +0% v43)` and `--files test/fuzz/RedemptionGas.t.sol .planning/phases/306-test-tst/306-05-GAS-BASELINE.md .planning/phases/306-test-tst/306-05-SUMMARY.md`

    AGENT-COMMITTED per `D-43N-TEST-COMMITS-AUTO-01`. Zero `contracts/*.sol` mutations.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && git log --oneline -1 -- test/fuzz/RedemptionGas.t.sol 2>&1 | head -5 && git diff --stat HEAD~1..HEAD -- contracts/ 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Single git commit touches test/fuzz/RedemptionGas.t.sol + 306-05-GAS-BASELINE.md + 306-05-SUMMARY.md
    - Zero `contracts/*.sol` mutations
    - SUMMARY.md cites the exact v43 baseline + v44 measured numbers + pass/fail verdict
  </acceptance_criteria>
  <done>
    AGENT-COMMITTED atomic commit lands the gas-regression bench + baseline artifact + SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gas-bench test ↔ sStonk | gasleft()-bracketed external calls measure deterministic call-path gas |
| Gas-bench test ↔ v43 baseline | Baseline captured via git-checkout + forge-snapshot ONCE; encoded as constants |
| Test-tree ↔ contracts/ | Zero mutations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-306-05-01 | Tampering | v43 baseline gas captured against different test signature (v43 had 2-arg resolveRedemptionPeriod) | mitigate | Document the v43 test signature explicitly in 306-05-GAS-BASELINE.md; capture using v43 source-tree fully restored (test + contract); record the EXACT v43 baseline number that fed the assertion. The v44 test path differs in arg signature, but the underlying gas attribution (cold SLOADs + SSTOREs + external calls) is comparable per-op |
| T-306-05-02 | Repudiation | "Just trust forge snapshot" without theoretical derivation | mitigate | feedback_gas_worst_case.md REQUIRES theoretical worst-case FIRST; Task 1 enforces the derivation as a separate artifact (306-05-GAS-BASELINE.md) before the assertion is wired |
| T-306-05-03 | Information disclosure | Gas measurement varies with EIP-2929 hot/cold state | accept | The bench measures cold-state worst case explicitly (first call after deploy, no prior touch of relevant slots); the deterministic call path + Foundry's fresh-EVM-per-test isolation ensures reproducibility |
| T-306-05-04 | Denial of service | If actual gas exceeds limit, test fails — blocks milestone closure | mitigate | This is the INTENDED behavior — a failing gas regression surfaces a Phase 305 regression for Phase 308 disposition. Per `feedback_security_over_gas.md`, gas regression is acceptable if it preserves an INV — the regression test's failure routes to user disposition (accept-as-documented vs Phase 305 patch) |
| T-306-05-SC | Tampering | No package installs | accept | Zero npm/pip/cargo |
</threat_model>

<verification>
Phase-level success criteria mapped to this plan:
- ROADMAP Success Criterion 5 (gas regression assertions PASS: burn ≤ +5% v43, claim ≤ +0% v43) — DELIVERED via the 2 new test_gas_regression_* functions + 4 baseline constants

Test invocation:
```
cd /home/zak/Dev/PurgeGame/degenerus-audit
forge build
forge test --match-path "test/fuzz/RedemptionGas.t.sol" --match-test "test_gas_regression" -vv
```

Expected:
- `[PASS] test_gas_regression_burn` with `actual_burn_gas: <X>` + `burn_limit_v44: <Y>` where X ≤ Y
- `[PASS] test_gas_regression_claim` with `actual_claim_gas: <X>` + `claim_limit_v44: <Y>` where X ≤ Y
</verification>

<success_criteria>
- `.planning/phases/306-test-tst/306-05-GAS-BASELINE.md` created with v43 baseline numbers + theoretical worst-case derivation
- `test/fuzz/RedemptionGas.t.sol` extended with 4 baseline constants + 2 test_gas_regression_* functions
- Both regression tests PASS (burn ≤ +5% baseline, claim ≤ baseline)
- 306-05-SUMMARY.md written + AGENT-COMMITTED in single atomic commit with the modified test file + baseline artifact
- Zero `contracts/*.sol` mutations
- Theoretical-worst-case derived FIRST per `feedback_gas_worst_case.md` (NOT just `forge snapshot` and accept)
</success_criteria>

<output>
Write `.planning/phases/306-test-tst/306-05-SUMMARY.md` documenting the theoretical worst case + v43 baseline + v44 measured + pass/fail verdict + Phase 308 §3.A delta-surface citation. Single AGENT-COMMITTED atomic commit envelope covering the test file + baseline artifact + SUMMARY.
</output>
