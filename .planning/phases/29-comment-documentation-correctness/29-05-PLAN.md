---
phase: 29-comment-documentation-correctness
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v3.0-doc-storage-constants.md
autonomous: true
requirements:
  - DOC-03
  - DOC-04

must_haves:
  truths:
    - "The storage layout diagram in DegenerusGameStorage.sol (lines 31-105) is verified byte-accurate against actual variable declarations"
    - "Every per-variable NatSpec annotation in DegenerusGameStorage.sol matches the actual type and purpose"
    - "Every constant/immutable with a NatSpec comment across all 27 contracts has the comment value verified against the code value"
    - "BPS vs half-BPS vs PPM scale is correctly annotated on every percentage constant"
  artifacts:
    - path: "audit/v3.0-doc-storage-constants.md"
      provides: "Storage layout verification and constants comment verification"
      contains: "## DOC-03"
  key_links:
    - from: "audit/v3.0-doc-storage-constants.md"
      to: "contracts/storage/DegenerusGameStorage.sol"
      via: "Byte-offset arithmetic verification"
      pattern: "Slot \\d+"
---

<objective>
Verify DOC-03 (storage layout comments match actual positions) and DOC-04 (constants comments match actual values) across all protocol contracts. DegenerusGameStorage.sol has a 75-line packed-slot layout diagram (lines 31-105) plus per-variable annotations across ~1,631 lines. There are ~627 constant/immutable declarations across all 27 contracts.

Purpose: Incorrect storage layout comments mislead auditors about EVM slot packing. Incorrect constant comments (especially BPS/half-BPS/PPM scale confusion) can cause wardens to report false findings.

Output: audit/v3.0-doc-storage-constants.md
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-comment-documentation-correctness/29-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify storage layout diagram and per-variable NatSpec in DegenerusGameStorage.sol (DOC-03)</name>
  <files>audit/v3.0-doc-storage-constants.md</files>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol (1,631 lines -- primary verification target, contains slot diagram at lines 31-105 and variable declarations at lines 189-360+)
    - .planning/phases/29-comment-documentation-correctness/29-RESEARCH.md (contains the type-size reference table and Slot 0 verification example)
  </read_first>
  <action>
    DOC-03 Part 1: Storage Layout Diagram Verification (lines 31-105 of DegenerusGameStorage.sol)

    For each EVM slot claimed in the diagram, verify byte offsets by:
    1. Reading the claimed layout (e.g., [0:6] levelStartTime uint48)
    2. Confirming the type size: bool=1B, uint8=1B, uint24=3B, uint48=6B, uint128=16B, uint256=32B, address=20B
    3. Accumulating byte totals per slot (must total <= 32 bytes)
    4. Verifying the actual variable declaration order in the source code matches the diagram order

    Verify at minimum these slots from the diagram:
    - Slot 0: levelStartTime(uint48) + dailyIdx(uint48) + rngRequestTime(uint48) + level(uint24) + flags/counters -- must total 32 bytes
    - Slot 1: Verify byte ranges and types add up to 27 bytes + 5 padding (per research)
    - Slot 2+: Verify each subsequent slot's claims

    DOC-03 Part 2: Per-Variable NatSpec Verification (lines 189-360+)
    1. For each storage variable with a /// comment, verify the comment describes the correct type, purpose, and any noted byte offset
    2. Verify variable declaration order matches the packed-slot diagram order
    3. Flag any variable whose NatSpec description contradicts the diagram or actual usage per audit reports

    Record findings as:
    | Slot | Byte Range | Variable | Type | Diagram Claim | Actual | Verdict |

    Write report to audit/v3.0-doc-storage-constants.md with:
    - Header: Phase 29-05, date, scope
    - "## DOC-03: Storage Layout Verification -- DegenerusGameStorage.sol"
    - Subsection per slot with byte-offset arithmetic proof
    - Summary: N slots verified, M discrepancies found
    - Per-variable NatSpec verification table
  </action>
  <verify>
    <automated>grep -c "DOC-03" audit/v3.0-doc-storage-constants.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.0-doc-storage-constants.md exists
    - File contains "## DOC-03: Storage Layout Verification"
    - Slot 0 byte-offset arithmetic is shown explicitly (cumulative byte total = 32)
    - At least 3 slots verified with byte-offset proofs
    - Variable declaration order verification is present
    - Each slot has a PASS or DISCREPANCY verdict
  </acceptance_criteria>
  <done>Storage layout diagram byte offsets verified against actual variable declarations; per-variable NatSpec annotations verified against types and purposes</done>
</task>

<task type="auto">
  <name>Task 2: Verify constants comments across all contracts (DOC-04)</name>
  <files>audit/v3.0-doc-storage-constants.md</files>
  <read_first>
    - audit/v3.0-doc-storage-constants.md (append to existing report from Task 1)
    - audit/v3.0-cross-cutting-recent-changes.md (Phase 28 CHG-04 already verified 30 active constants -- build on this baseline, read the specific constants list)
    - audit/v1.1-parameter-reference.md (reference for scale conventions: BPS /10000, half-BPS /20000, PPM /1000000)
    - contracts/DegenerusGame.sol (constants -- largest file, most constants)
    - contracts/modules/DegenerusGameJackpotModule.sol (jackpot constants)
    - contracts/modules/DegenerusGameLootboxModule.sol (lootbox constants)
    - contracts/modules/DegenerusGameMintModule.sol (mint/pool split constants)
    - contracts/modules/DegenerusGameDecimatorModule.sol (decimator constants)
    - contracts/modules/DegenerusGameAdvanceModule.sol (advance constants)
    - contracts/modules/DegenerusGameWhaleModule.sol (whale pass constants)
    - contracts/BurnieCoin.sol (BURNIE supply constants)
    - contracts/BurnieCoinflip.sol (coinflip constants)
    - contracts/StakedDegenerusStonk.sol (sDGNRS constants)
    - contracts/DegenerusVault.sol (vault constants)
    - contracts/DegenerusAdmin.sol (governance constants)
    - contracts/DegenerusAffiliate.sol (affiliate constants)
    - contracts/DegenerusQuests.sol (quest constants)
    - contracts/WrappedWrappedXRP.sol (WWXRP constants)
  </read_first>
  <action>
    DOC-04: Constants Comments Verification

    Phase 28 CHG-04 already verified 30 active constant VALUES match. This task verifies the COMMENTS on constants are accurate.

    For every constant and immutable declaration with a /// comment across all contracts:
    1. Read the comment's claimed value/purpose
    2. Verify the actual code value matches
    3. Pay special attention to scale annotations:
       - BPS constants: verify comment says correct percentage (e.g., 1000 BPS = 10%, NOT 1%)
       - Half-BPS constants: verify comment correctly notes half-BPS scale (e.g., 300 half-BPS = 1.5%, NOT 3%)
       - PPM constants: verify comment says correct percentage (e.g., 10000 PPM = 1%, NOT 10%)
    4. Verify human-readable descriptions match the constant's actual usage per audit reports

    Organize by contract file. For each file, produce:
    | Constant | Value | Comment Claim | Actual | Scale | Verdict |

    Known items to verify:
    - The 30 constants already verified in Phase 28 CHG-04 -- confirm their COMMENTS are also correct (Phase 28 verified VALUES only)
    - Constants in files touched by commits f71b6382 (5 removed) and 9b0942af (bonusFlip moved, volume EV removed) -- verify no stale comments remain

    Append to audit/v3.0-doc-storage-constants.md:
    - "## DOC-04: Constants Comment Verification"
    - Per-contract subsections with constant tables
    - Special subsection: "Scale Convention Verification" showing BPS/half-BPS/PPM checks
    - Summary: N constants verified, M comment discrepancies found
  </action>
  <verify>
    <automated>grep -c "DOC-04" audit/v3.0-doc-storage-constants.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.0-doc-storage-constants.md contains "## DOC-04: Constants Comment Verification"
    - At least 10 contract files have constant comment verification tables
    - Scale convention section explicitly addresses BPS vs half-BPS vs PPM
    - Summary states total constants checked and discrepancies found
    - No stale comments from removed constants (f71b6382, 9b0942af) remain undocumented
  </acceptance_criteria>
  <done>All constants with NatSpec comments across all 27 contracts have their comment values verified against code values; BPS/half-BPS/PPM scale correctness confirmed</done>
</task>

</tasks>

<verification>
- audit/v3.0-doc-storage-constants.md exists with both DOC-03 and DOC-04 sections
- Storage layout byte-offset arithmetic proven for all claimed slots
- Variable declaration order matches diagram order
- Constants across all contracts have comment accuracy verified
- Scale confusion issues (BPS vs half-BPS vs PPM) flagged if present
</verification>

<success_criteria>
Storage layout diagram is byte-accurate against actual declarations. Every constant with a NatSpec comment has its value and scale verified. All discrepancies catalogued with exact line references and corrections.
</success_criteria>

<output>
After completion, create `.planning/phases/29-comment-documentation-correctness/29-05-SUMMARY.md`
</output>
