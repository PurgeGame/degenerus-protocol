---
phase: 336-tst-equivalence-freeze-safety-divergence-repro-non-widening-
plan: 05
type: execute
wave: 5
depends_on: [336-04]
files_modified:
  - test/fuzz/MintModuleDivergenceAcrossSplit.t.sol
autonomous: true
worktree: false
requirements: [TST-03]
must_haves:
  truths:
    - "A new dedicated test file test/fuzz/MintModuleDivergenceAcrossSplit.t.sol contains a deterministic anchor test + a boundary fuzz overlay proving byte-identical per-ticket trait derivation across a budget-slice split for an affected player"
    - "The deterministic anchor cites .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-MINTDIV01-REACHABILITY-VERDICT.md BY PATH in its docstring (D-TST03-03 audit-lineage)"
    - "The cross-path equality oracle (D-TST03-02) is across-N-narrow-slices == single-fat-budget-contiguous — NOT a reference-loop equality against processFutureTicketBatch:502"
    - "The boundary fuzz overlay scans owed ∈ [maxT+1, maxT+200] = [293, 492] (D-TST03-01)"
    - "Uses the LIVE 3-arg TraitsGenerated(address,uint256,uint32) event signature, NOT the v48-era 6-arg form (RESEARCH Pitfall 3)"
    - "contracts/*.sol shows ZERO byte diff vs e756a6f3 after this plan lands"
  artifacts:
    - path: "test/fuzz/MintModuleDivergenceAcrossSplit.t.sol"
      provides: "TST-03 deterministic anchor + boundary fuzz overlay — byte-identical trait derivation across budget-slice split"
      contains: "testMintDivCrossPathEquality_OwedSplitsAcrossSlices"
      min_lines: 100
  key_links:
    - from: "deterministic anchor test"
      to: ".planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-MINTDIV01-REACHABILITY-VERDICT.md"
      via: "docstring citation by PATH (D-TST03-03)"
      pattern: "334-MINTDIV01-REACHABILITY-VERDICT"
    - from: "cross-path equality oracle"
      to: "contracts/modules/DegenerusGameMintModule.sol:719 processed += take"
      via: "two-environment (vm.snapshot/revertTo) or before-after digest comparison; MINTDIV-02 fix makes A == B"
      pattern: "processTicketBatch"
---

<objective>
Empirically close the MINTDIV-01 reachability verdict's question (per `334-MINTDIV01-REACHABILITY-VERDICT.md`): prove byte-identical per-ticket trait derivation across a budget-slice split for an affected player (`owed=300` at level L, warm budget 550, maxT=292). The MINTDIV-02 one-liner fix (`processed += take` at MintModule:719) is what makes the across-split path equivalent to the contiguous path — this test empirically validates that invariant.

Purpose: TST-03 — the load-bearing MINTDIV regression. The fix landed at IMPL HEAD `e756a6f3`; 336 codifies the invariant the fix preserves.
Output: A NEW dedicated test file `test/fuzz/MintModuleDivergenceAcrossSplit.t.sol` (D-TST03-04 file home — pattern-mapper picked a new dedicated file per PATTERNS.md §"NEW test/fuzz/MintModuleDivergenceAcrossSplit.t.sol"; no MINTDIV-shaped analog exists in-tree).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/336-tst-equivalence-freeze-safety-divergence-repro-non-widening-/336-CONTEXT.md
@.planning/phases/336-tst-equivalence-freeze-safety-divergence-repro-non-widening-/336-RESEARCH.md
@.planning/phases/336-tst-equivalence-freeze-safety-divergence-repro-non-widening-/336-PATTERNS.md
@.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-MINTDIV01-REACHABILITY-VERDICT.md

@test/fuzz/RngLockDeterminism.t.sol
@test/fuzz/GameOverPathIsolation.t.sol
@test/fuzz/AfKingSubscription.t.sol
@test/fuzz/helpers/DeployProtocol.sol
@contracts/modules/DegenerusGameMintModule.sol
@contracts/storage/DegenerusGameStorage.sol

<interfaces>
Two existing analogs to COMPOSE (no MINTDIV-shaped analog exists in-tree):

1. test/fuzz/RngLockDeterminism.t.sol lines 130-144 — `vm.snapshot()` / `vm.revertTo()` two-env isolation primitives + helper shape.
2. test/fuzz/GameOverPathIsolation.t.sol lines 30-86 — `vm.recordLogs()` + TraitsGenerated event capture + advance-to-game-over driver that exercises processTicketBatch.

LIVE TraitsGenerated event signature (contracts/storage/DegenerusGameStorage.sol:485-489) — the 3-arg form:
```
event TraitsGenerated(
    address indexed player,
    uint256 baseKey,
    uint32 take
);
```
topic-0 hash: `keccak256("TraitsGenerated(address,uint256,uint32)")`. (Pitfall 3: do NOT use the v48-era 6-arg form `(address,uint24,uint32,uint32,uint32,uint256)` — that's why GameOverPathIsolation.t.sol:31-32 is a carried-forward red.)

MintModule surfaces (contracts/modules/DegenerusGameMintModule.sol):
- `processTicketBatch(uint24 lvl) external returns (bool finished)` — the subject the cross-path test exercises (around lines 671-733)
- `WRITES_BUDGET_SAFE = 550` at line ~93 — the warm budget that binds maxT=292
- `_processOneTicketEntry` — the inner per-ticket helper (line ~702 area)
- `_raritySymbolBatch` — the LCG consumer; baseKey + entropyWord drive deterministic output
- Line 719: `processed += take` — the MINTDIV-02 fix; the cross-path invariant this test empirically validates

DeployProtocol fixture members (every test inherits): game, coin, dgnrs, mockVRF, vault, affiliate, admin, afKing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test/fuzz/MintModuleDivergenceAcrossSplit.t.sol with the deterministic anchor test</name>
  <files>test/fuzz/MintModuleDivergenceAcrossSplit.t.sol</files>
  <read_first>
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-MINTDIV01-REACHABILITY-VERDICT.md (full file — to extract the exact scenario the docstring cites verbatim: owed=300 at level L, warm budget 550, maxT=292; the −17 warm trace)
    - .planning/phases/336-tst-equivalence-freeze-safety-divergence-repro-non-widening-/336-PATTERNS.md §"NEW test/fuzz/MintModuleDivergenceAcrossSplit.t.sol" (lines 418-541) — the exact file structure, imports, setup, cross-path mechanic options
    - .planning/phases/336-tst-equivalence-freeze-safety-divergence-repro-non-widening-/336-RESEARCH.md §"TST-03 cross-path equality" (lines 372-438) + Assumption A2 (the two-env vs before-after mechanic choice) + §"Pitfall 3" (the event signature drift) + §"Pitfall 5" (snapshot state pollution)
    - test/fuzz/RngLockDeterminism.t.sol lines 94-144 — the snapshot/revert helpers; copy the SHAPE (do NOT import — this is a new file with its own helpers)
    - test/fuzz/GameOverPathIsolation.t.sol full file — to study the TraitsGenerated event capture + the advance-to-game-over driver pattern (NOT the topic-hash — see Pitfall 3)
    - test/fuzz/helpers/DeployProtocol.sol — the fixture every test inherits; confirm it stands up game / mockVRF / etc.
    - contracts/modules/DegenerusGameMintModule.sol around lines 393-733 — confirm processTicketBatch signature, WRITES_BUDGET_SAFE constant, the per-ticket inner helper signature, line 719 MINTDIV-02 fix
    - contracts/storage/DegenerusGameStorage.sol lines 485-489 — the LIVE 3-arg TraitsGenerated event signature
  </read_first>
  <behavior>
    - NEW file `test/fuzz/MintModuleDivergenceAcrossSplit.t.sol` (D-TST03-04 home — dedicated file; PATTERNS.md leans this way because no MINTDIV-shaped analog exists)
    - SPDX header + pragma + imports (DeployProtocol, Vm, MintPaymentKind)
    - Inherits from DeployProtocol; setUp() calls _deployProtocol() + advance warp + funds mockVRF subscription
    - At least ONE deterministic anchor test: testMintDivCrossPathEquality_OwedSplitsAcrossSlices — exercises owed=300 at level L, warm budget 550, maxT=292 (per 334 verdict)
    - The deterministic test's docstring CITES `.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-MINTDIV01-REACHABILITY-VERDICT.md` by exact path (D-TST03-03 audit-lineage requirement)
    - The cross-path oracle is across-split == contiguous (D-TST03-02), NOT reference-loop equality vs processFutureTicketBatch:502
    - Uses the LIVE 3-arg TraitsGenerated signature (Pitfall 3)
    - Helper TOPIC constant: `bytes32 internal constant TOPIC_TRAITS_GENERATED = keccak256("TraitsGenerated(address,uint256,uint32)");`
  </behavior>
  <action>
    Create the NEW file test/fuzz/MintModuleDivergenceAcrossSplit.t.sol. Use the SPDX/pragma block matching the project (AGPL-3.0-only / ^0.8.26 — confirm by reading any sibling test file). Mirror PATTERNS.md §"NEW test/fuzz/MintModuleDivergenceAcrossSplit.t.sol" imports verbatim:

    Imports: DeployProtocol from helpers, Vm from forge-std, MintPaymentKind from the IDegenerusGame interface.

    Contract name: `MintModuleDivergenceAcrossSplitTest is DeployProtocol`.

    Top-level constant TOPIC_TRAITS_GENERATED defined as `keccak256("TraitsGenerated(address,uint256,uint32)")` — the LIVE 3-arg form per Storage:485-489 (Pitfall 3 mitigation; DO NOT copy the wrong 6-arg topic from GameOverPathIsolation.t.sol:31-32).

    setUp(): mirror GameOverPathIsolation.t.sol:40-46 — `_deployProtocol(); vm.warp(block.timestamp + 1 days); mockVRF.fundSubscription(1, 100e18);` plus any player allocation needed.

    Deterministic anchor test — function signature: `function testMintDivCrossPathEquality_OwedSplitsAcrossSlices() public`.

    Docstring above the signature MUST contain the verbatim sentence: *"per `.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-MINTDIV01-REACHABILITY-VERDICT.md`: owed=300 at level L, warm budget 550, maxT=292"* (the D-TST03-03 audit-lineage cite by path). Followed by a one-liner explaining the cross-path oracle: across-split == contiguous via byte-identical traitBurnTicket digest.

    Body structure (executor picks ONE of the two mechanic options per RESEARCH §A2 at first execution — start with the SIMPLER before-after, fall back to two-env if state-pollution surfaces):

    OPTION A (before-after on same state — the simpler mechanic; RESEARCH §"Alternative" note):
    1. Set lvl = 1, player = makeAddr("mintdiv-300-player"), entropy seed deterministic.
    2. Seed player with owed=300 at level L via test-side helper (write owedMap / ticketsOwedPacked[rk][player] = packed_owed_300 via vm.store; compute slot by reading MintModule.sol for the owedMap declaration).
    3. Drive processTicketBatch under NATURAL budget slicing (call game.processTicketBatch(lvl) in a loop until it returns true) — each call uses one WRITES_BUDGET_SAFE=550 slice, naturally splitting the 300-owed player at the 292-take boundary.
    4. Capture: bytes32 envADigest = _digestTraitBurnTicketForPlayer(player, lvl) — keccak256 over `vm.load(address(game), traitBurnTicket[lvl][traitId])` for traitId in 0..255.
    5. Re-stage by re-seeding owed=300 AND re-injecting the SAME entropy word (same per-index VRF-derived word) AND clearing traitBurnTicket[lvl] for the player — OR use vm.snapshot/revertTo for clean isolation.
    6. Drive processTicketBatch in a DIFFERENT slice shape (e.g. 3-fractured call sequence — confirm exact mechanic by reading processTicketBatch's budget logic).
    7. Capture envBDigest the same way.
    8. assertEq(envADigest, envBDigest, "TST-03 D-TST03-02: byte-identical trait derivation across budget-slice splits (MINTDIV-02 invariant)").

    OPTION B (two-env vm.snapshot/revertTo — the more defensive mechanic if Option A surfaces state pollution):
    Same shape, but wrap Env A in `uint256 snapId = vm.snapshot();` ... `vm.revertTo(snapId);` to fully isolate Env B from Env A. Mirror RngLockDeterminism.t.sol:130-136.

    Helpers the executor authors in the same file (no pre-existing analog beyond the storage-direct pattern at AfKingSubscription.t.sol:398-438):
    - `_seedPlayerWithOwed(address player, uint24 lvl, uint32 owed)` — write owedMap[player] = packed(owed, 0) via vm.store on the ticketsOwedPacked[rk] mapping. Compute slot by reading MintModule.sol for the ticketsOwedPacked declaration's storage slot.
    - `_driveProcessTicketBatchUntilDone(uint24 lvl)` — loop on `game.processTicketBatch(lvl)` until it returns true.
    - `_digestTraitBurnTicketForPlayer(address player, uint24 lvl) returns (bytes32)` — keccak256 the concatenation of `vm.load(address(game), traitBurnTicket[lvl][traitId])` for traitId in 0..255.

    The exact storage slot formulas (ticketsOwedPacked root slot, traitBurnTicket root slot) are derived by reading contracts/modules/DegenerusGameMintModule.sol + contracts/storage/DegenerusGameStorage.sol; executor confirms at write-time.

    Implements TST-03 D-TST03-01 (shape) + D-TST03-02 (oracle) + D-TST03-03 (anchor + audit-lineage cite) + D-TST03-04 (file home).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit &amp;&amp; forge test --match-path test/fuzz/MintModuleDivergenceAcrossSplit.t.sol --match-test testMintDivCrossPathEquality_OwedSplitsAcrossSlices -vv 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `test/fuzz/MintModuleDivergenceAcrossSplit.t.sol` exists; at least 80 lines (the deterministic anchor + helpers + imports)
    - `grep -c "testMintDivCrossPathEquality_OwedSplitsAcrossSlices" test/fuzz/MintModuleDivergenceAcrossSplit.t.sol` returns at least 1
    - `grep -c "334-MINTDIV01-REACHABILITY-VERDICT" test/fuzz/MintModuleDivergenceAcrossSplit.t.sol` returns at least 1 (the audit-lineage citation by path per D-TST03-03)
    - `grep -c "TraitsGenerated(address,uint256,uint32)" test/fuzz/MintModuleDivergenceAcrossSplit.t.sol` returns at least 1 (LIVE 3-arg signature; Pitfall 3 mitigation)
    - `grep -c "TraitsGenerated(address,uint24,uint32,uint32,uint32,uint256)" test/fuzz/MintModuleDivergenceAcrossSplit.t.sol` returns exactly 0 (the WRONG 6-arg form must NOT appear)
    - `forge test --match-test testMintDivCrossPathEquality_OwedSplitsAcrossSlices -vv` EXITS 0
    - `git diff e756a6f3 -- contracts/` returns empty (zero contracts mutation per D-TST04-04)
  </acceptance_criteria>
  <done>New dedicated file present + deterministic anchor test GREEN + audit-lineage citation present + LIVE event signature used + zero contracts diff.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the boundary fuzz overlay testFuzz_MintDiv_BoundaryOwedCrossPath</name>
  <files>test/fuzz/MintModuleDivergenceAcrossSplit.t.sol</files>
  <read_first>
    - test/fuzz/MintModuleDivergenceAcrossSplit.t.sol (the file just created in Task 1 — confirm the helpers it exposes)
    - .planning/phases/336-tst-equivalence-freeze-safety-divergence-repro-non-widening-/336-PATTERNS.md §"Boundary-fuzz overlay pattern" (around lines 495-502)
    - .planning/phases/336-tst-equivalence-freeze-safety-divergence-repro-non-widening-/336-CONTEXT.md D-TST03-01 — the boundary range owed ∈ [maxT+1, maxT+200] = [293, 492]
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-MINTDIV01-REACHABILITY-VERDICT.md — to confirm maxT=292 is the warm-budget boundary
  </read_first>
  <behavior>
    - New fuzz function: `function testFuzz_MintDiv_BoundaryOwedCrossPath(uint32 owed) public`
    - `vm.assume(owed >= 293 && owed <= 492);` — forces split mid-player (maxT=292 warm; range = [maxT+1, maxT+200] per D-TST03-01)
    - Reuses the helpers added in Task 1 (_seedPlayerWithOwed, _driveProcessTicketBatchUntilDone, _digestTraitBurnTicketForPlayer)
    - Same cross-path oracle (across-split == contiguous) but with the fuzzed `owed` parameter substituted for the deterministic 300
    - GREEN at default profile (runs=1000); the deep gate is not required for TST-03 (only TST-01 D-TST01-02 specifies it)
  </behavior>
  <action>
    Append to test/fuzz/MintModuleDivergenceAcrossSplit.t.sol (the file created in Task 1) a new boundary fuzz function.

    Inline docstring above the signature (one-liner): "D-TST03-01 boundary fuzz overlay: scans owed ∈ [maxT+1, maxT+200] = [293, 492] (per .../334-MINTDIV01-REACHABILITY-VERDICT.md, maxT=292 at WRITES_BUDGET_SAFE=550 warm). Catches LCG-boundary surprises around the budget-slice split."

    Body: mirror the deterministic anchor flow (testMintDivCrossPathEquality_OwedSplitsAcrossSlices from Task 1), substituting the fuzzed `owed` value for the hard-coded 300:

    1. `vm.assume(owed >= 293 && owed <= 492);`
    2. Allocate player = makeAddr(string.concat("mintdiv-fuzz-player-", vm.toString(owed)))  // OR a stable address; the fuzz seed independence is fine if the per-test setup is clean.
    3. _seedPlayerWithOwed(player, lvl, owed);
    4. envADigest = _driveProcessTicketBatchUntilDone + _digestTraitBurnTicketForPlayer (Env A — natural split).
    5. Re-stage (re-seed + clear traitBurnTicket entries — same mechanic as the deterministic test, OR vm.snapshot/revertTo).
    6. envBDigest in differently-split shape.
    7. assertEq(envADigest, envBDigest, "TST-03 D-TST03-01: byte-identical trait derivation across budget-slice splits (boundary fuzz)").

    Implements TST-03 D-TST03-01 (the boundary fuzz overlay).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit &amp;&amp; forge test --match-path test/fuzz/MintModuleDivergenceAcrossSplit.t.sol -vv 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "testFuzz_MintDiv_BoundaryOwedCrossPath" test/fuzz/MintModuleDivergenceAcrossSplit.t.sol` returns at least 1
    - `grep -c "vm.assume(owed >= 293" test/fuzz/MintModuleDivergenceAcrossSplit.t.sol` returns at least 1 (the boundary clamp; "owed >= 293" OR ">= uint32(293)" form acceptable)
    - `forge test --match-path test/fuzz/MintModuleDivergenceAcrossSplit.t.sol -vv` EXITS 0 with at least 1000 fuzz runs reported for the new boundary function AND the deterministic anchor still GREEN
    - `git diff e756a6f3 -- contracts/` returns empty (zero contracts mutation per D-TST04-04)
  </acceptance_criteria>
  <done>Boundary fuzz overlay present + GREEN at default profile; deterministic anchor still green; zero contracts diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → game contract | Test pranks player + forges contract storage (vm.store on owedMap / ticketsOwedPacked) to set up the affected-player scenario; reads traitBurnTicket via vm.load for the digest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-336-05-01 | Tampering | vm.store seeds owedMap state to short-circuit the natural ticket-purchase path | accept | TST phase commits ONLY to test/ + .planning/; contracts/*.sol byte-identical to e756a6f3 per D-TST04-04. The state-forging only avoids the multi-tx scaffolding around buying tickets to reach owed=300; the load-bearing assertion measures byte-identical trait derivation, which is independent of how the owed state was reached. |
| T-336-05-02 | Repudiation | Vacuous pass — both envs produce empty traitBurnTicket digests (zero work happened) | mitigate | The digest captures the FULL 256-trait-id span; if processTicketBatch never advanced (envA digest == envB digest == zero-hash), the test silently passes. Mitigation: assert envADigest != bytes32(0) before the equality check (proves work happened). Add this guard in the executor's write. |
| T-336-05-03 | Tampering | Wrong TraitsGenerated topic hash (v48-era 6-arg form) silently captures zero events (Pitfall 3) | mitigate | The acceptance criterion explicitly checks BOTH: the 3-arg signature is PRESENT in the file (>=1 grep match), AND the 6-arg signature is ABSENT (exactly 0 grep matches). |
</threat_model>

<verification>
- `forge test --match-path test/fuzz/MintModuleDivergenceAcrossSplit.t.sol -vv` EXITS 0 (deterministic anchor + boundary fuzz)
- `git diff e756a6f3 -- contracts/` empty (zero contracts mutation per D-TST04-04)
- `git diff e756a6f3 -- test/fuzz/MintModuleDivergenceAcrossSplit.t.sol` shows the file is NEW (full creation)
- Per-plan atomic commit per D-CC-01: a single commit scoped to `336-05` / TST-03 MINTDIV cross-path equality
</verification>

<success_criteria>
- TST-03 deterministic anchor GREEN + boundary fuzz overlay GREEN
- The 334-MINTDIV01-REACHABILITY-VERDICT empirically closed: the MINTDIV-02 fix preserves byte-identical traits across the budget-slice split
- D-TST03-01..04 satisfied: deterministic+fuzz shape, cross-path equality oracle (not reference-loop), 334 verdict cited by path, new dedicated file home
- LIVE 3-arg TraitsGenerated event signature used (Pitfall 3 mitigated)
- Zero `contracts/*.sol` mutation per D-TST04-04
- Sequential-on-main, no-worktrees per D-CC-02; per-plan atomic commit per D-CC-01
</success_criteria>

<output>
Create `.planning/phases/336-tst-equivalence-freeze-safety-divergence-repro-non-widening-/336-05-SUMMARY.md` when done.
</output>
