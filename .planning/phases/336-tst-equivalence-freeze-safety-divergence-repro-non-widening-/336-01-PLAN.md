---
phase: 336-tst-equivalence-freeze-safety-divergence-repro-non-widening-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fuzz/RngLockDeterminism.t.sol
autonomous: true
worktree: false
requirements: [TST-01]
must_haves:
  truths:
    - "A new freeze-fuzz function in test/fuzz/RngLockDeterminism.t.sol proves that calling claimWhalePass() inside the rngLock window does NOT alter the consumed per-index VRF-derived word — same word delivered → byte-identical consumed output, with vs. without the perturbation"
    - "The proof runs at the default profile (fuzz runs=1000) AND under FOUNDRY_PROFILE=deep (fuzz runs=10000) without behavioral change"
    - "contracts/*.sol shows ZERO byte diff vs the FROZEN audit subject e756a6f3 after this plan lands"
    - "Phase 336 sequential-on-main (D-CC-02) — no worktree used; commits land directly on main per D-CC-01"
  artifacts:
    - path: "test/fuzz/RngLockDeterminism.t.sol"
      provides: "TST-01 freeze leg: new testFuzz_RngLockDeterminism_ClaimWhalePassDuringLockSafe + new perturbation class (cls 11) for claimWhalePass"
      contains: "testFuzz_RngLockDeterminism_ClaimWhalePassDuringLockSafe"
  key_links:
    - from: "test/fuzz/RngLockDeterminism.t.sol::testFuzz_RngLockDeterminism_ClaimWhalePassDuringLockSafe"
      to: "_snapshotPreLock / _advanceToVrfRequestBoundary / _deliverMockVrf / _revertToPreLock / _assertVrfOutputByteIdentity"
      via: "reuse of existing snapshot/perturb/revert helpers verbatim per D-TST01-01 (no parallel-harness)"
      pattern: "_snapshotPreLock|_assertVrfOutputByteIdentity"
    - from: "perturbation library _perturb cls 11 (new)"
      to: "game.claimWhalePass(claimant)"
      via: "try/catch wrapper (mirrors existing cls 9 doWork / cls 10 autoBuy(0))"
      pattern: "claimWhalePass"
---

<objective>
Empirically prove WHALE-04 (the paper freeze-proof at `.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-WHALE04-FREEZE-PROOF.md`): calling `claimWhalePass()` during the rngLock window does NOT perturb the consumed per-index VRF-derived word.

Purpose: Closes the TST-01 freeze leg — the v45-vrf-freeze-invariant re-attested empirically for the deferred whale-pass claim path landed at WHALE-01/02 (IMPL `e756a6f3`).
Output: An extension of `test/fuzz/RngLockDeterminism.t.sol` (the ROADMAP-LOCKED freeze-fuzz home per D-TST01-01) with a new fuzz function + a new perturbation class — NO parallel harness, NO contract mutation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/336-tst-equivalence-freeze-safety-divergence-repro-non-widening-/336-CONTEXT.md
@.planning/phases/336-tst-equivalence-freeze-safety-divergence-repro-non-widening-/336-RESEARCH.md
@.planning/phases/336-tst-equivalence-freeze-safety-divergence-repro-non-widening-/336-PATTERNS.md
@.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-WHALE04-FREEZE-PROOF.md

@test/fuzz/RngLockDeterminism.t.sol
@contracts/modules/DegenerusGameWhaleModule.sol
@contracts/modules/DegenerusGameLootboxModule.sol
@contracts/storage/DegenerusGameStorage.sol
@foundry.toml

<interfaces>
<!-- Key surfaces the executor needs. Already in the test file or its imports. -->

From test/fuzz/RngLockDeterminism.t.sol (helpers to REUSE verbatim, lines 94-144):
```solidity
function _readLootboxRngIndex() internal view returns (uint48);
function _lootboxRngWord(uint48 index) internal view returns (uint256);
function _advanceToVrfRequestBoundary() internal returns (uint256 reqId);
function _deliverMockVrf(uint256 reqId, uint256 word) internal;
function _snapshotPreLock() internal returns (uint256 snapshotId);
function _revertToPreLock(uint256 snapshotId) internal;
function _assertVrfOutputByteIdentity(bytes32 perturbed, bytes32 baseline, string memory label) internal pure;
```

From test/fuzz/RngLockDeterminism.t.sol perturbation library (lines ~150-232, ending classes 9/10):
```solidity
} else if (cls == 9) {
    vm.prank(actor);
    try afKing.doWork() {} catch { return; }
} else if (cls == 10) {
    vm.prank(actor);
    try afKing.autoBuy(0) {} catch { return; }
}
// cls == 11 is the new claimWhalePass perturbation (this plan adds it).
```

From contracts/modules/DegenerusGameWhaleModule.sol (the materialization entrypoint, line ~1018):
```solidity
function claimWhalePass(address player) external { /* materializes whalePassClaims[player] queued amount */ }
```

From contracts/modules/DegenerusGameLootboxModule.sol:1253 — the O(1) accumulator write at box-open:
```solidity
whalePassClaims[player] += 1;  // WHALE-01 O(1) record (the writer this freeze test perturbs)
```

From foundry.toml (lines 45-58 area — deep profile auto-engages via env var; NO test annotation needed):
```toml
[profile.deep.fuzz]
runs = 10000

[profile.deep.invariant]
runs = 1000
depth = 256
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cls 11 perturbation (claimWhalePass) to the existing _perturb library</name>
  <files>test/fuzz/RngLockDeterminism.t.sol</files>
  <read_first>
    - test/fuzz/RngLockDeterminism.t.sol (full file — to see the existing cls 0..10 library, the N_PERTURB_ACTIONS constant, and the _perturb internal function signature)
    - .planning/phases/336-tst-equivalence-freeze-safety-divergence-repro-non-widening-/336-PATTERNS.md §"Perturbation library extension pattern" (lines 127-142) — the exact diff shape for adding a new class
    - contracts/modules/DegenerusGameWhaleModule.sol around line 1018 — claimWhalePass signature + the _livenessTriggered guard at line 1019
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-WHALE04-FREEZE-PROOF.md §2 — confirms far-future band REVERTS under rngLock at Storage:661 (try/catch is the correct wrapper)
  </read_first>
  <behavior>
    - The _perturb library MUST gain a new `else if (cls == N_PERTURB_ACTIONS - 1)` branch (where N_PERTURB_ACTIONS becomes 12 after the bump) wrapping `game.claimWhalePass(actor)` in try/catch (mirrors cls 9/10 exactly)
    - N_PERTURB_ACTIONS literal MUST be bumped from 11 to 12 (find the existing constant in the same file)
    - The new branch MUST NOT revert the test on a real revert (the WHALE-04 proof relies on the test surviving the structurally-expected `RngLocked()` / `_livenessTriggered()` reverts)
    - Zero changes to any cls 0..10 branch — they MUST remain byte-identical
  </behavior>
  <action>
    Locate the existing `_perturb(uint256 seed)` internal function in `test/fuzz/RngLockDeterminism.t.sol` (per PATTERNS.md §"Perturbation library extension pattern" the cls 9 / cls 10 branches end around lines 216-231; the executor confirms exact lines on read). Locate the `uint256 internal constant N_PERTURB_ACTIONS = 11;` (or `uint8` — confirm exact type) declaration and bump to 12. Append a new branch AFTER cls 10:

    ```
    } else if (cls == 11) {
        address claimant = actor;
        vm.prank(actor);
        try game.claimWhalePass(claimant) {} catch { return; }
    }
    ```

    The `actor` variable is already in scope from the function's existing parameter list — confirm by reading the function signature; if `actor` is named differently (e.g. `who`), use that name. The try/catch is REQUIRED: per `334-WHALE04-FREEZE-PROOF.md §2`, `claimWhalePass` may revert under rngLock (far-future band hits `Storage:661` revert; the whole call may revert under `_livenessTriggered()` at `WhaleModule:1019`). The freeze proof's load-bearing property is that the consumed VRF word is byte-identical with vs. without the attempted perturbation — including in the revert case.

    DO NOT introduce new imports, new state, or new helpers. DO NOT touch any cls 0..10 branch.

    Implements WHALE-04 freeze-safety per the paper proof in `.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-WHALE04-FREEZE-PROOF.md` (TST-01 freeze leg).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit &amp;&amp; forge test --match-path test/fuzz/RngLockDeterminism.t.sol -vv 2>&amp;1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "cls == 11" test/fuzz/RngLockDeterminism.t.sol` returns exactly 1 match (the new branch)
    - `grep -n "claimWhalePass" test/fuzz/RngLockDeterminism.t.sol` returns at least 1 match (the new branch + any reference in the new fuzz function added in Task 2)
    - `grep -n "N_PERTURB_ACTIONS = 12" test/fuzz/RngLockDeterminism.t.sol` returns exactly 1 match (the bumped literal)
    - `git diff e756a6f3 -- contracts/` returns empty (zero contracts mutation per D-TST04-04)
    - Pre-existing `testFuzz_RngLockDeterminism_AutoBuyDuringLockSafe` and other existing tests in this file still PASS (the cls 0..10 branches are untouched)
  </acceptance_criteria>
  <done>cls 11 branch present; N_PERTURB_ACTIONS bumped to 12; existing tests still green; zero contracts diff.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add testFuzz_RngLockDeterminism_ClaimWhalePassDuringLockSafe mirroring the AutoBuyDuringLockSafe template</name>
  <files>test/fuzz/RngLockDeterminism.t.sol</files>
  <read_first>
    - test/fuzz/RngLockDeterminism.t.sol lines 1839-1928 (testFuzz_RngLockDeterminism_AutoBuyDuringLockSafe — the EXACT template to mirror)
    - test/fuzz/RngLockDeterminism.t.sol lines 1914-1927 (the non-vacuity B-control pattern — mirror it for the new test)
    - .planning/phases/336-tst-equivalence-freeze-safety-divergence-repro-non-whidening-/336-PATTERNS.md §"Core template — snapshot/perturb/revert byte-identity" (lines 81-125) — the spine
    - .planning/phases/336-tst-equivalence-freeze-safety-divergence-repro-non-widening-/336-RESEARCH.md §"Existing TST-01 router-context freeze-byte-identity" (lines 232-277) — the reference implementation shape
    - .planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-WHALE04-FREEZE-PROOF.md (full file — the paper proof being empirically re-attested; specifically the "future-window-only writes" property per D-03 claim-time anchoring)
    - contracts/modules/DegenerusGameLootboxModule.sol:1253 — the `whalePassClaims[player] += 1` writer (the box-open path that pre-loads the claim)
  </read_first>
  <behavior>
    - New function signature: `function testFuzz_RngLockDeterminism_ClaimWhalePassDuringLockSafe(uint256 seed) public` — same shape as the AutoBuy template
    - The test MUST pre-load `whalePassClaims[claimant] > 0` via the box-open path so `claimWhalePass` has work to do (otherwise the perturbation is vacuous)
    - The test MUST snapshot pre-lock, perturb in-lock with the claim attempt (try/catch — revert is expected for far-future band under rngLock), deliver the VRF word, capture the consumed per-index word, revert, deliver the SAME word in a clean baseline, capture, and assert byte-identity
    - The test MUST include the non-vacuity control (mirroring lines 1914-1927) — a separate baseline run where the perturbation source is structurally absent must yield a DIFFERENT consumed word, proving the test is not vacuously passing
    - Pass at default profile (fuzz runs=1000) AND at `FOUNDRY_PROFILE=deep` (fuzz runs=10000) — same function, the profile env var is the gate
  </behavior>
  <action>
    Append a new fuzz function to `test/fuzz/RngLockDeterminism.t.sol` (place it adjacent to `testFuzz_RngLockDeterminism_AutoBuyDuringLockSafe` for discoverability — confirm exact insertion point by reading file around line 1928). Mirror the spine from PATTERNS.md §"Core template" verbatim with these substitutions:

    1. Function name: `testFuzz_RngLockDeterminism_ClaimWhalePassDuringLockSafe`
    2. Perturbation seed label: `"tst01-claim-word"`
    3. Pre-stage: a buyer purchases boxes via the existing `_buyBox`-equivalent or `game.purchase{value: ...}(...)` flow (mirror lines 1858-1867 of the AutoBuy template); deterministically trigger BOON_WHALE_PASS on box-open by pre-injecting a per-index RNG word whose boon bits include the whale-pass slot — uses `_injectLootboxRngWord` (already present in the file) OR a new helper if the existing one cannot target the boon bit. Alternative: stage the claim by directly writing `whalePassClaims[claimant]` via `vm.store` on the appropriate mapping slot (compute slot via `keccak256(abi.encode(claimant, ROOT_SLOT))`).
    4. Perturbation invocation: replace the `_perturb(perturbSeed)` call with `_perturb(11)` (or pass the cls 11 seed) — wraps `claimWhalePass(claimant)` per cls 11 added in Task 1
    5. Non-vacuity control: mirror lines 1914-1927 — a separate baseline run with `whalePassClaims[claimant] = 0` (zero the pre-load via `vm.store`) MUST yield a DIFFERENT consumed word, asserting `controlWord != baselineWord`

    Inline docstring at the top of the function (one-liner ABOVE the signature):
    ```
    /// Empirically re-attests WHALE-04 paper proof
    /// (.planning/phases/334-spec-design-lock-mintdiv-reachability-proof-rngaudit-structu/334-WHALE04-FREEZE-PROOF.md):
    /// claimWhalePass() during rngLock perturbs ZERO bytes of the consumed per-index VRF-derived word.
    /// Per D-TST01-01 the home is LOCKED to this file; per D-TST01-02 deep-fuzz gates via FOUNDRY_PROFILE=deep.
    ```

    Implements TST-01 freeze leg per D-TST01-01 (home) + D-TST01-02 (depth gate). Empirically validates WHALE-04 SC2 (RNG-freeze safety PROVEN, not assumed).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit &amp;&amp; forge test --match-path test/fuzz/RngLockDeterminism.t.sol --match-test ClaimWhalePassDuringLockSafe -vv 2>&amp;1 | tail -30 &amp;&amp; FOUNDRY_PROFILE=deep forge test --match-path test/fuzz/RngLockDeterminism.t.sol --match-test ClaimWhalePassDuringLockSafe 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "testFuzz_RngLockDeterminism_ClaimWhalePassDuringLockSafe" test/fuzz/RngLockDeterminism.t.sol` returns at least 1 (the function exists)
    - `forge test --match-test ClaimWhalePassDuringLockSafe` at the default profile EXITS 0 with at least 1000 fuzz runs reported
    - `FOUNDRY_PROFILE=deep forge test --match-test ClaimWhalePassDuringLockSafe` EXITS 0 with at least 10000 fuzz runs reported (the deep gate per D-TST01-02)
    - Non-vacuity assertion present: a `controlWord != baselineWord` assertion (or equivalent label) exists in the function body
    - `git diff e756a6f3 -- contracts/` returns empty (zero contracts mutation per D-TST04-04)
    - The pre-existing `testFuzz_RngLockDeterminism_AutoBuyDuringLockSafe` still passes (no regression in the surrounding file)
  </acceptance_criteria>
  <done>New fuzz function present + green at default + green at deep profile + non-vacuity control assertion present + zero contracts diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → game contract | Test pranks an actor and invokes claimWhalePass + autoBuy/advanceGame which traverse module boundaries; the test asserts no cross-boundary state perturbation alters the frozen VRF window |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-336-01-01 | Tampering | Test forges contract state via vm.store to satisfy a freeze proof that wouldn't otherwise hold | accept | TST phase commits ONLY to test/ + .planning/; contracts/*.sol byte-identical to e756a6f3 per D-TST04-04. The fuzz test asserts the consumed word equivalence — if a contract-side regression escapes the freeze, the test fails REGARDLESS of staging shape (state-forging only pre-loads the perturbation source). |
| T-336-01-02 | Repudiation | Vacuous-pass freeze test (the perturbation source is structurally absent or zero, so byte-identity holds trivially) | mitigate | Non-vacuity control (Task 2) — a separate baseline run with the perturbation source zeroed asserts `controlWord != baselineWord`, proving the test would have caught a real perturbation. Mirror lines 1914-1927 of the existing AutoBuy template. |
| T-336-01-03 | Tampering | Snapshot-state pollution across freeze-fuzz iterations — a second-environment leak from the first stage's writes corrupts the perturbation baseline | mitigate | Reuse `_revertToPreLock(snapshotId)` BEFORE re-staging the second env (helpers at `RngLockDeterminism.t.sol:130-144` enforce revert-before-re-stage by construction). RESEARCH §5 Pitfall 5 maps here per PATTERNS.md §Pitfall→Plan Surface Map. |
</threat_model>

<verification>
- `forge test --match-path test/fuzz/RngLockDeterminism.t.sol -vv` EXITS 0 (the new test + all existing tests in the file)
- `FOUNDRY_PROFILE=deep forge test --match-path test/fuzz/RngLockDeterminism.t.sol --match-test ClaimWhalePassDuringLockSafe` EXITS 0 at 10000 runs
- `git diff e756a6f3 -- contracts/` empty (zero contracts mutation — the D-TST04-04 hard constraint)
- `git diff e756a6f3 -- test/fuzz/RngLockDeterminism.t.sol` shows ONLY: the bumped `N_PERTURB_ACTIONS = 12` literal, the new cls 11 branch, the new fuzz function (and its docstring) — NO other test functions touched
- Per-plan atomic commit per D-CC-01: `git log -1 --format='%H %s' main` shows a single commit message scoped to `336-01` / TST-01 freeze leg
</verification>

<success_criteria>
- TST-01 freeze leg empirically PROVEN: `testFuzz_RngLockDeterminism_ClaimWhalePassDuringLockSafe` GREEN at default + deep profiles
- WHALE-04 paper proof (`.planning/phases/334-.../334-WHALE04-FREEZE-PROOF.md`) RE-ATTESTED empirically by the new fuzz test
- ROADMAP-LOCKED home (`test/fuzz/RngLockDeterminism.t.sol`) honored per D-TST01-01 — NO parallel harness file exists in this plan's diff
- Zero `contracts/*.sol` mutation per D-TST04-04
- Sequential-on-main, no-worktrees per D-CC-02; per-plan atomic commit per D-CC-01
</success_criteria>

<output>
Create `.planning/phases/336-tst-equivalence-freeze-safety-divergence-repro-non-widening-/336-01-SUMMARY.md` when done.
</output>
