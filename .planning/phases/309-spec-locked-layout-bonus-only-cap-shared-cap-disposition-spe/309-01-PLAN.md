---
phase: 309-spec-locked-layout-bonus-only-cap-shared-cap-disposition-spe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/309-spec-locked-layout-bonus-only-cap-shared-cap-disposition-spe/309-SPEC.md
autonomous: true
requirements: [SPEC-01, SPEC-02, SPEC-03]
must_haves:
  truths:
    - "Every file:line cited in CONTEXT.md <canonical_refs> is grep-verified against contract HEAD with the matched substring recorded in the SPEC; zero 'by construction' / 'single fn reaches all paths' claims (SC5)"
    - "The final packed uint256 layout is LOCKED in the SPEC with exact bit offsets, field widths, helper signatures, the baseLevel co-pack, the lootboxDay rejection, and the rename — plus an explicit no-new-slot (net -1 slot) attestation (SC1)"
    - "The bonus-only cap rule (D-08) is stated exactly as `<= LOOTBOX_EV_NEUTRAL_BPS` early return, with all-three-callers confirmation grounded in the verified call sites (SC2)"
    - "The allocation-time tally rule (D-09; first-deposit + subsequent) and the openLootBox frozen-apply formula with no cap SLOAD/SSTORE + whole-slot zero-at-open are LOCKED (SC3)"
  artifacts:
    - path: ".planning/phases/309-spec-locked-layout-bonus-only-cap-shared-cap-disposition-spe/309-SPEC.md"
      provides: "Locked v45.0 design spec — call-graph evidence section + SPEC-01 layout + SPEC-02 bonus-only cap + SPEC-03 allocation tally"
      contains: "lootboxPurchasePacked"
  key_links:
    - from: "309-SPEC.md call-graph evidence section"
      to: "contract HEAD MILESTONE_V44_AT_HEAD_6f0ba2963a10654ba554a8c333c5ee80c54a8349"
      via: "grep-verified file:line + matched substring per cited ref"
      pattern: "grep -n"
---

<objective>
Author the first four sections of `.planning/phases/309-*/309-SPEC.md`: (0) the call-graph
EVIDENCE section that grep-verifies every cited `file:line` against contract HEAD, then the
three carried/locked content sections SPEC-01 (packed-slot layout), SPEC-02 (bonus-only cap),
and SPEC-03 (allocation tally + open-apply). The SPEC-04 shared-cap proof is Plan 02.

Purpose: Lock the v45.0 design BEFORE any contract change so Phase 310 IMPL has an
unambiguous, source-grounded contract to implement. Grep-verification is the spine — no
section may rest on an unverified line citation or a "by construction" claim
(`feedback_verify_call_graph_against_source`).

Output: `309-SPEC.md` with §0 Evidence (grep matrix), §1 SPEC-01 Layout, §2 SPEC-02 Cap,
§3 SPEC-03 Allocation. (§4 SPEC-04 added by Plan 02.)

CRITICAL: This is a SPEC-ONLY phase. ZERO `contracts/` mutations, ZERO `test/` mutations.
The executor READS contract source to grep-verify HEAD line numbers and WRITES the SPEC
document — it never edits, creates, or deletes any `.sol`, test, or source file.
`files_modified` is exactly one path: the 309-SPEC.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/309-spec-locked-layout-bonus-only-cap-shared-cap-disposition-spe/309-CONTEXT.md
@.planning/v45-lootbox-evcap-fix-plan.md

<baseline>
Audit baseline HEAD: MILESTONE_V44_AT_HEAD_6f0ba2963a10654ba554a8c333c5ee80c54a8349.
All grep-verification runs against the contracts/ tree at this HEAD. Only read contracts
from the contracts/ directory (stale copies exist elsewhere — memory feedback_contract_locations).
</baseline>

<verified_head_facts>
<!-- Planner pre-verified these against HEAD during planning. Executor MUST re-grep and -->
<!-- record the matched substring; these are the expected results, NOT a substitute for -->
<!-- the executor's own grep. Two divergences are flagged for explicit SPEC treatment. -->

Storage.sol:
- 1370  `mapping(uint48 => mapping(address => uint32)) internal lootboxDay;`        (D-03 reject — seed input)
- 1374-1375  `mapping(uint48 => mapping(address => uint24)) internal lootboxBaseLevelPacked;`  (D-02 merge/remove)
- 1379  `mapping(uint48 => mapping(address => uint16)) internal lootboxEvScorePacked;`  (D-05 widen→uint256 + rename)
- 1427-1428  `mapping(address => mapping(uint24 => uint256)) internal lootboxEvBenefitUsedByLevel;`  (shared cap; key (player, uint24 level) — confirms D-04 wrong-key-shape)

LootboxModule.sol:
- 308-314  `LOOTBOX_EV_MIN_BPS=8000`, `LOOTBOX_EV_NEUTRAL_BPS=10000`, `LOOTBOX_EV_MAX_BPS=13500`, `LOOTBOX_EV_BENEFIT_CAP=` (10 ether)
- 444      `_lootboxEvMultiplierFromScore(` definition
- 475      `_applyEvMultiplierWithCap(` definition (SPEC-02 Change-1 site)
- 517      `openLootBox(` definition
- 528, 616 `uint32 day = lootboxDay[index][player];` (seed-input reads)
- 541      `uint24 baseLevelPacked = lootboxBaseLevelPacked[index][player];` (read)
- 545, 621, 671, 707  seed `keccak256(abi.encode(rngWord, player, day, amount))` (INV-04 invariant; raw amount)
- 557      `uint16 evScorePacked = lootboxEvScorePacked[index][player];`
- 558      `_lootboxEvMultiplierFromScore(uint256(evScorePacked - 1))`
- 559      `_applyEvMultiplierWithCap(` CALL #1 (open path)
- 570      `lootboxBaseLevelPacked[index][player] = 0;` (zero-at-open)
- 571      `lootboxEvScorePacked[index][player] = 0;` (zero-at-open)
- 666      `resolveLootboxDirect(address player, uint256 amount, uint256 rngWord, uint16 activityScore)`
- 674      `_lootboxEvMultiplierFromScore(uint256(activityScore))` (frozen-score source)
- 675      `_applyEvMultiplierWithCap(player, currentLevel, amount, evMultiplierBps)` CALL #2
- 702      `resolveRedemptionLootbox(address player, uint256 amount, uint256 rngWord, uint16 activityScore)`
- 710      `_lootboxEvMultiplierFromScore(uint256(activityScore))` (frozen-score source)
- 711      `_applyEvMultiplierWithCap(player, currentLevel, amount, evMultiplierBps)` CALL #3

MintModule.sol:
- 987      `uint32 storedDay = lootboxDay[lbIndex][buyer];`
- 991      `lootboxDay[lbIndex][buyer] = lbDay;` (first-deposit branch)
- 992      `lootboxBaseLevelPacked[lbIndex][buyer] = uint24(cachedLevel + 1);` (first deposit; sentinel = level + 1)
- 1013     `lootboxEth[lbIndex][buyer] = (uint256(cachedLevel + 1) << 232) | newAmount;` (subsequent deposit; lootboxEth untouched per IMPL-05)
- 1155     `if (lbFirstDeposit) { lootboxEvScorePacked[lbIndex][buyer] = uint16(cachedScore + 1); }`
- 1396-1397  SECOND lootboxDay writer in a simulation path — executor MUST enumerate it and confirm D-03 (lootboxDay stays its own slot) still holds for ALL writers.

WhaleModule.sol:
- 851      `uint32 storedDay = lootboxDay[index][buyer];`
- 854      `lootboxDay[index][buyer] = dayIndex;` (first-deposit branch)
- 855      `lootboxBaseLevelPacked[index][buyer] = uint24(level + 2);`  <-- DIVERGENCE: sentinel = level + 2, NOT level + 1 like Mint:992
- 856      `lootboxEvScorePacked[index][buyer] = uint16(... playerActivityScore(buyer) + 1)` (first-deposit, INLINE — not a separate lbFirstDeposit block like Mint:1155)
- 876      `lootboxEth[index][buyer] = (uint256(purchaseLevel) << 232) | newAmount;` (subsequent deposit)

FLAGGED DIVERGENCES (inline-duplicated business logic — Phase 294 BURNIE precedent):
1. baseLevel sentinel offset differs: Mint writes `cachedLevel + 1` (Mint:992), Whale writes `level + 2` (Whale:855). The SPEC's `baseLevelPlus1` pack field MUST document BOTH encodings as-is and NOT silently normalize — the IMPL phase must preserve each call site's existing semantics.
2. Score write structure differs: Mint gates behind `if (lbFirstDeposit)` (Mint:1155); Whale writes inline in the first-deposit branch (Whale:856). The SPEC tally rule must cover both write shapes.
</verified_head_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: §0 Evidence — grep-verify every cited file:line against HEAD</name>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol (declarations at 1370, 1374-1375, 1379, 1427-1428)
    - contracts/modules/DegenerusGameLootboxModule.sol (308-314, 444, 475, 517, 528, 541, 545, 557-559, 570-571, 616, 621, 666, 671, 674-675, 702, 707, 710-711)
    - contracts/modules/DegenerusGameMintModule.sol (987, 991-992, 1013, 1155, and ALL lootboxDay writers incl. 1396-1397)
    - contracts/modules/DegenerusGameWhaleModule.sol (851, 854-856, 876)
    - .planning/phases/309-*/309-CONTEXT.md <canonical_refs> (the authoritative cite list)
  </read_first>
  <action>
    Create `309-SPEC.md` and write its header (phase, baseline HEAD MILESTONE_V44_AT_HEAD_6f0ba2963a10654ba554a8c333c5ee80c54a8349, requirements SPEC-01..04) plus section `## §0 Call-Graph Evidence (grep-verified against HEAD)`.

    For EVERY ref in CONTEXT.md <canonical_refs> "Contract HEAD sites to grep-verify", run a name-anchored grep against the contracts/ tree and record a row: { symbol, file, line(s), matched substring }. Use line-anchored greps that print line numbers (grep -n) so the recorded line matches HEAD. Enumerate the ACTUAL SLOADs/sites — do not assume one function reaches all paths. Specifically:
      - All four Storage.sol declarations (lootboxDay, lootboxBaseLevelPacked, lootboxEvScorePacked, lootboxEvBenefitUsedByLevel) with their full key shape.
      - All three `_applyEvMultiplierWithCap` CALL sites (open / resolveLootboxDirect / resolveRedemptionLootbox) — record the line of each call, not just the definition.
      - All `lootboxDay` writers across Mint + Whale + the LootboxModule reads, INCLUDING the second Mint writer near 1396 — explicitly enumerate every writer to support D-03.
      - All `lootboxBaseLevelPacked` and `lootboxEvScorePacked` read/write/clear sites across the 4 files.
      - The seed-build `keccak256(abi.encode(rngWord, player, day, amount))` sites (LootboxModule 545/621/671/707) showing the RAW `amount` arg (INV-04 anchor).

    Record the two FLAGGED DIVERGENCES from <verified_head_facts> as explicit rows in §0: the baseLevel sentinel offset mismatch (Mint `+1` vs Whale `+2`) and the score-write structure mismatch (Mint gated vs Whale inline). Each divergence row states the IMPL implication. Do NOT normalize or "fix" them — this phase only documents.

    Add an explicit attestation line: "Zero 'by construction' / 'single fn reaches all paths' claims — every cited site is grep-verified above." Per memory feedback_verify_call_graph_against_source + feedback_no_history_in_comments, describe what IS at HEAD.

    This task READS contract source and WRITES only 309-SPEC.md. No contract/test edits.
  </action>
  <verify>
    <automated>test -f .planning/phases/309-spec-locked-layout-bonus-only-cap-shared-cap-disposition-spe/309-SPEC.md && grep -q "§0" .planning/phases/309-spec-locked-layout-bonus-only-cap-shared-cap-disposition-spe/309-SPEC.md && grep -q "6f0ba2963a10654ba554a8c333c5ee80c54a8349" .planning/phases/309-spec-locked-layout-bonus-only-cap-shared-cap-disposition-spe/309-SPEC.md && grep -Eq "level \+ 2|level\+2" .planning/phases/309-spec-locked-layout-bonus-only-cap-shared-cap-disposition-spe/309-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "lootboxEvScorePacked" contracts/storage/DegenerusGameStorage.sol` returns line 1379, and 309-SPEC.md §0 records that line + the matched declaration substring.
    - `grep -n "_applyEvMultiplierWithCap" contracts/modules/DegenerusGameLootboxModule.sol` returns the definition (475) AND the three call lines (559, 675, 711); 309-SPEC.md §0 lists all three call sites distinctly.
    - `grep -n "lootboxDay" contracts/modules/DegenerusGameMintModule.sol` returns BOTH writers (991 and ~1396); 309-SPEC.md §0 enumerates every lootboxDay writer (not just the first).
    - 309-SPEC.md §0 contains a row stating the baseLevel sentinel divergence: Mint writes `cachedLevel + 1`, Whale writes `level + 2`.
    - 309-SPEC.md §0 contains a row stating the score-write structure divergence: Mint gated behind `if (lbFirstDeposit)`, Whale inline.
    - 309-SPEC.md §0 contains the literal attestation string "Zero 'by construction'" (or equivalent) confirming no unverified claims.
    - `git status --porcelain contracts/ test/` returns no output (zero code mutations).
  </acceptance_criteria>
  <done>§0 Evidence section exists in 309-SPEC.md with a grep-verified row (symbol/file/line/matched-substring) for every CONTEXT.md canonical ref, both divergences flagged, the no-by-construction attestation present, and zero contract/test mutations.</done>
</task>

<task type="auto">
  <name>Task 2: §1 SPEC-01 — lock the packed uint256 layout</name>
  <read_first>
    - 309-SPEC.md §0 (the grep-verified line numbers this section cites)
    - .planning/phases/309-*/309-CONTEXT.md (D-01..D-07 decisions)
    - .planning/REQUIREMENTS.md (SPEC-01)
    - contracts/storage/DegenerusGameStorage.sol:1370,1374-1375,1379,1427-1428 (to cite the verified decls)
  </read_first>
  <action>
    Append `## §1 SPEC-01 — Packed-Slot Layout (LOCKED)` to 309-SPEC.md. Transcribe the locked decisions D-01..D-07 faithfully (no redesign, no alternatives):

      - State the final single `uint256` word layout with exact bit offsets per D-02: `[0:16]` `score+1` (uint16, 0=unset); `[16:80]` `adjustedPortion` (uint64, <= 10 ETH); `[80:104]` `baseLevel+1` (uint24, 0=unset); `[104:256]` free (152 bits).
      - State the width rationale per D-01: `adjustedPortion` is uint64 (NOT uint96 — the v45 fix-plan's uint96 is SUPERSEDED). Reason: `adjustedPortion <= LOOTBOX_EV_BENEFIT_CAP = 10 ETH = 1e19 wei`; ceil(log2(1e19)) = 64; uint64 max ~= 18.44 ETH (~84% headroom); a single box's accumulated adjustedPortion can never exceed the cap because each `add = min(deposit, remaining)` advances `used`.
      - State the baseLevel co-pack (D-02): merges `lootboxBaseLevelPacked` (Storage.sol:1374-1375) into the word, REMOVING that mapping → net -1 storage slot per (index, player). Justify: same `(uint48 index => address)` key, identical write-once/read+clear lifecycle, all sites within the 4 in-scope files, NOT a seed input, and the deposit-time adjustedPortion accumulation already read-modify-writes the word so folding level in adds zero deposit-path SLOAD.
      - State the lootboxDay REJECTION (D-03): `lootboxDay` (Storage.sol:1370) is EXCLUDED — it is read at LootboxModule.sol:528/616 and feeds the frozen roll seed `keccak256(abi.encode(rngWord, player, day, amount))` at :545; co-packing it would perturb a seed-input read path, forbidden by INV-04/IMPL-05. Record as evaluated-and-rejected with this reason. (This is the freeze-invariant hard line per v45-vrf-freeze-invariant + feedback_security_over_gas.)
      - State the no-other-co-pack negative finding (D-04): the only other cap-bounded field is `lootboxEvBenefitUsedByLevel`, keyed `(player, uint24 level)` (Storage.sol:1427-1428) — wrong key shape for an `(index, player)` slot, cannot co-pack. Record explicitly.
      - State the rename (D-05): `lootboxEvScorePacked → lootboxPurchasePacked`, type `mapping(uint48 => mapping(address => uint256))`. This single mapping REPLACES BOTH `lootboxEvScorePacked` and `lootboxBaseLevelPacked`. Rationale: word is no longer EV-only (carries baseLevel) so the neutral name describes what IS (feedback_no_history_in_comments).
      - State the helper signatures (D-06): `_packLootboxPurchase(uint16 scorePlus1, uint64 adj, uint24 baseLevelPlus1) -> uint256` and `_unpackLootboxPurchase(uint256) -> (uint16 scorePlus1, uint64 adj, uint24 baseLevelPlus1)`. Note in the SPEC that the IMPL must preserve the divergent baseLevel sentinel encodings from §0 (Mint `+1` vs Whale `+2`) at each call site — the pack helper takes the already-encoded `baseLevelPlus1`, it does not reconcile the offset.
      - State the no-new-slot attestation (D-07): mapping values never cross-pack and the old uint16 already occupied a full slot; net change is -1 slot via the baseLevel merge. Zero-at-open clears all three fields in a single SSTORE of the whole word.

    Reference §0 line numbers when citing storage decls. WRITES only 309-SPEC.md.
  </action>
  <verify>
    <automated>F=.planning/phases/309-spec-locked-layout-bonus-only-cap-shared-cap-disposition-spe/309-SPEC.md; grep -q "SPEC-01" "$F" && grep -q "lootboxPurchasePacked" "$F" && grep -q "_packLootboxPurchase" "$F" && grep -Eq "uint64" "$F" && grep -Eq "\[16:80\]" "$F"</automated>
  </verify>
  <acceptance_criteria>
    - 309-SPEC.md §1 states `adjustedPortion` is `uint64` and explicitly marks the fix-plan's `uint96` as SUPERSEDED.
    - 309-SPEC.md §1 contains the four-field bit layout with offsets `[0:16]`, `[16:80]`, `[80:104]`, `[104:256]`.
    - 309-SPEC.md §1 contains `_packLootboxPurchase(uint16 scorePlus1, uint64 adj, uint24 baseLevelPlus1)` and `_unpackLootboxPurchase` signatures verbatim from D-06.
    - 309-SPEC.md §1 contains the rename `lootboxEvScorePacked` → `lootboxPurchasePacked` and states it replaces BOTH that mapping and `lootboxBaseLevelPacked`.
    - 309-SPEC.md §1 records the lootboxDay REJECTION with the seed-input reason citing LootboxModule.sol:545 seed build.
    - 309-SPEC.md §1 records the D-04 negative finding (lootboxEvBenefitUsedByLevel wrong key shape) and the D-07 no-new-slot / net -1-slot attestation.
    - 309-SPEC.md §1 notes the IMPL must preserve the divergent baseLevel sentinel (Mint +1 / Whale +2) at each call site.
    - `git status --porcelain contracts/ test/` returns no output.
  </acceptance_criteria>
  <done>§1 SPEC-01 locks the full layout (offsets, uint64 width + supersession note, baseLevel co-pack, lootboxDay rejection, D-04 negative finding, rename, helper signatures, no-new-slot attestation, divergent-sentinel note) with §0 line citations; zero code mutations.</done>
</task>

<task type="auto">
  <name>Task 3: §2 SPEC-02 bonus-only cap + §3 SPEC-03 allocation tally + open-apply</name>
  <read_first>
    - 309-SPEC.md §0 (verified call sites 475/559/675/711 and the deposit sites)
    - .planning/phases/309-*/309-CONTEXT.md (D-08, D-09)
    - .planning/REQUIREMENTS.md (SPEC-02, SPEC-03, IMPL-05)
    - .planning/v45-lootbox-evcap-fix-plan.md (Change 1 + Change 2)
    - contracts/modules/DegenerusGameLootboxModule.sol:475-509,517-571,559,675,711 (the cap fn + open path + call sites)
    - contracts/modules/DegenerusGameMintModule.sol:987-1013,1155 + DegenerusGameWhaleModule.sol:851-876 (deposit tally sites)
  </read_first>
  <action>
    Append `## §2 SPEC-02 — Bonus-Only Cap (LOCKED)` and `## §3 SPEC-03 — Allocation Tally + Open-Apply (LOCKED)` to 309-SPEC.md.

    §2 (D-08, verbatim from REQUIREMENTS SPEC-02 + fix-plan Change 1): In `_applyEvMultiplierWithCap` (LootboxModule.sol:475), replace the existing `== LOOTBOX_EV_NEUTRAL_BPS` early return with `<= LOOTBOX_EV_NEUTRAL_BPS` → return `(amount * evMultiplierBps) / 10_000` (penalty or neutral applies in full, never consumes the cap). Only `> NEUTRAL` draws the cap. State explicitly it applies to ALL THREE callers and cite the three grep-verified call sites from §0: open path (LootboxModule.sol:559), resolveLootboxDirect (:675), resolveRedemptionLootbox (:711). Note the EV constants are at :308-314 (NEUTRAL=10000).

    §3 (D-09, verbatim from SPEC-03; width now uint64 per D-01): State the per-deposit tally rule using the box's frozen multiplier from the first-deposit score snapshot:
      - if `mult <= NEUTRAL`: store `score+1` only, no cap draw (adjustedPortion stays 0).
      - if `mult > NEUTRAL`: `remaining = CAP - lootboxEvBenefitUsedByLevel[player][lvl]` (0 if used >= CAP); `add = min(depositAmount, remaining)`; advance `lootboxEvBenefitUsedByLevel[player][lvl] += add`; accumulate `adjustedPortion += add` into the packed word via read-modify-write.
      - First deposit writes `score+1` (and `baseLevel+1` per D-02 — preserving the per-module sentinel offset from §0); later deposits accumulate `adjustedPortion` only (score + multiplier already frozen).
      - Cite the deposit sites from §0: Mint first-deposit (991/992) + subsequent + score write (1155); Whale first-deposit (854/855/856) + subsequent (876). Note the two structural divergences from §0 (Mint gated score write vs Whale inline; Mint baseLevel +1 vs Whale +2) so the tally rule covers both shapes.
      - State the openLootBox application: `scaled = mult <= NEUTRAL ? amount*mult/1e4 : adj*mult/1e4 + (amount - adj)` with NO cap SLOAD/SSTORE; the zero-at-open write clears the WHOLE packed slot in a single SSTORE (replaces the separate clears at LootboxModule.sol:570/571).
      - State the seed/roll preservation (IMPL-05): raw `amount` still feeds `keccak256(rngWord, player, day, amount)` and `lootboxEth` layout is untouched — only reward scaling uses `adjustedPortion`. Anchor to the §0 seed-build sites.

    Transcribe locked decisions; do not invent alternatives. WRITES only 309-SPEC.md.
  </action>
  <verify>
    <automated>F=.planning/phases/309-spec-locked-layout-bonus-only-cap-shared-cap-disposition-spe/309-SPEC.md; grep -q "SPEC-02" "$F" && grep -q "SPEC-03" "$F" && grep -Eq "<= ?LOOTBOX_EV_NEUTRAL_BPS|<= ?NEUTRAL" "$F" && grep -Eq "amount\*mult/1e4|amount \* mult|adj\*mult/1e4|adj \* mult" "$F" && grep -Eq "559" "$F" && grep -Eq "675" "$F" && grep -Eq "711" "$F"</automated>
  </verify>
  <acceptance_criteria>
    - 309-SPEC.md §2 states the early-return condition as `<= LOOTBOX_EV_NEUTRAL_BPS` returning `(amount * evMultiplierBps) / 10_000` and that only `> NEUTRAL` draws the cap.
    - 309-SPEC.md §2 cites all three grep-verified call sites by line: 559 (open), 675 (resolveLootboxDirect), 711 (resolveRedemptionLootbox), and confirms the rule applies to all three callers.
    - 309-SPEC.md §3 states the `mult <= NEUTRAL` (store score+1, no cap draw) vs `mult > NEUTRAL` (`add = min(deposit, CAP - used)`, advance used, accumulate adjustedPortion) tally branches.
    - 309-SPEC.md §3 distinguishes first-deposit (writes score+1 / baseLevel+1) from subsequent deposits (accumulate adjustedPortion only).
    - 309-SPEC.md §3 contains the openLootBox formula `scaled = mult <= NEUTRAL ? amount*mult/1e4 : adj*mult/1e4 + (amount - adj)` and states "no cap SLOAD/SSTORE" + whole-slot zero-at-open.
    - 309-SPEC.md §3 states raw `amount` preserved for `keccak(rngWord, player, day, amount)` and `lootboxEth` untouched (IMPL-05).
    - 309-SPEC.md §3 references the §0 deposit-site divergences (Mint gated vs Whale inline; Mint +1 vs Whale +2 sentinel).
    - `git status --porcelain contracts/ test/` returns no output.
  </acceptance_criteria>
  <done>§2 locks the bonus-only `<= NEUTRAL` rule with all-three-caller line citations; §3 locks the per-deposit tally (both branches, first vs subsequent, both module shapes), the openLootBox frozen-apply formula with no cap SLOAD/SSTORE + whole-slot zero-at-open, and the IMPL-05 seed/lootboxEth preservation; zero code mutations.</done>
</task>

</tasks>

<threat_model>
This is a SPEC-only phase: the executable attack surface this phase ships is ZERO (no code).
The threat model is about the SPEC's correctness as a security artifact — a mis-spec here
becomes a live roll-manipulation vector in Phase 310 IMPL.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPEC content → Phase 310 IMPL | Locked layout/semantics flow into a contract diff; an under-specified freeze rule becomes an implemented vulnerability |
| Cited file:line → contract HEAD | Stale/wrong line citations would let IMPL patch the wrong site (inline-duplication divergence, Phase 294 precedent) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-309-01 | Tampering | SPEC layout locks a seed-input field as co-packable | mitigate | §1 records D-03 lootboxDay REJECTION with the seed-build citation (LootboxModule.sol:545); §0 grep-verifies the seed sites use raw `amount`. The freeze hard line per v45-vrf-freeze-invariant. |
| T-309-02 | Information disclosure (mis-mapped sites) | Cited file:line drifts from HEAD → IMPL patches wrong/duplicated logic | mitigate | §0 grep-verifies EVERY cited site against HEAD with matched substring + flags the Mint/Whale baseLevel-sentinel and score-write divergences so IMPL preserves both (feedback_verify_call_graph_against_source). |
| T-309-03 | Repudiation ("by construction" claims) | SPEC asserts a path is safe without enumerating it | mitigate | §0 attestation forbids "by construction" / "single fn reaches all paths"; every claim grep-grounded. SPEC-04 proof (Plan 02) carries the backward-trace + full SLOAD enumeration. |
</threat_model>

<verification>
- `309-SPEC.md` exists with sections §0, §1, §2, §3.
- `git status --porcelain contracts/ test/` returns no output across all three tasks (zero code/test mutations — the phase invariant).
- Every CONTEXT.md canonical ref appears as a grep-verified §0 row; both Mint/Whale divergences flagged.
- SPEC-01 layout (offsets, uint64, baseLevel co-pack, lootboxDay rejection, rename, helpers, no-new-slot) fully transcribed.
- SPEC-02 `<= NEUTRAL` rule + three-caller citation present; SPEC-03 tally + open-apply formula + seed preservation present.
</verification>

<success_criteria>
- ROADMAP SC1 (SPEC-01 layout locked + no-new-slot attestation), SC2 (bonus-only cap rule + all-three-callers), SC3 (allocation tally + open formula + zero-at-open), and SC5 (every cited file:line grep-verified, zero "by construction") are satisfied by §0-§3. SC4 (SPEC-04 shared-cap proof) is delivered by Plan 02.
- Zero `contracts/` and zero `test/` mutations.
</success_criteria>

<output>
Create `.planning/phases/309-spec-locked-layout-bonus-only-cap-shared-cap-disposition-spe/309-01-SUMMARY.md` when done.
</output>
