---
phase: 309-spec-locked-layout-bonus-only-cap-shared-cap-disposition-spe
plan: 02
type: execute
wave: 2
depends_on: ["309-01"]
files_modified:
  - .planning/phases/309-spec-locked-layout-bonus-only-cap-shared-cap-disposition-spe/309-SPEC.md
autonomous: true
requirements: [SPEC-04]
must_haves:
  truths:
    - "The SPEC-04 disposition is ACCEPT, PROVEN by a backward-trace (not asserted): each consumer (resolveLootboxDirect, resolveRedemptionLootbox) is traced backward to show the relevant VRF word was unknown at input-commitment time and that the frozen activityScore parameter — not rngWord — drives the multiplier (SC4)"
    - "The SPEC enumerates ALL SLOADs inside the [rng request → unlock] window for all three callers (openLootBox, resolveLootboxDirect, resolveRedemptionLootbox) and confirms lootboxEvBenefitUsedByLevel[player][level] is the only shared mutable consumed alongside the word — confirmed, not assumed (SC4)"
    - "The residual resolution-order cap-steering freedom is classified as already-accepted self-MEV (REQUIREMENTS Out-of-Scope), with the argument that knowing rngWord yields no extra gain because multipliers are frozen"
  artifacts:
    - path: ".planning/phases/309-spec-locked-layout-bonus-only-cap-shared-cap-disposition-spe/309-SPEC.md"
      provides: "SPEC-04 shared-cap disposition — backward-trace proof + full in-window SLOAD enumeration + ACCEPT verdict"
      contains: "lootboxEvBenefitUsedByLevel"
  key_links:
    - from: "309-SPEC.md §4 SPEC-04 backward-trace"
      to: "DegenerusGameLootboxModule.sol resolver multiplier source (activityScore param at :674/:710)"
      via: "grep-verified frozen-score citation proving rngWord-independence"
      pattern: "_lootboxEvMultiplierFromScore.*activityScore"
---

<objective>
Author `## §4 SPEC-04 — Shared-Cap Disposition (LOCKED: ACCEPT)` in the existing
`309-SPEC.md`. Treat the ACCEPT verdict as a THEOREM the SPEC must PROVE, not state:
deliver (a) the rigorous word-independence backward-trace per feedback_rng_backward_trace
and (b) the complete enumeration of ALL SLOADs inside the [rng request → unlock] window
for all three callers per feedback_rng_window_storage_read_freshness.

Purpose: SPEC-04's "accept" rests on a freeze-invariant claim. If that claim is wrong or
under-proven, Phase 310 IMPL ships a known-word reorder vector through the shared
per-(player, level) cap accumulator. The proof closes that risk before any code moves.

Output: §4 appended to 309-SPEC.md (which §0-§3 from Plan 01 already established).

CRITICAL: SPEC-ONLY phase. ZERO contracts/ mutations, ZERO test/ mutations. The executor
READS contract source to ground the trace + SLOAD enumeration in grep-verified HEAD lines
and WRITES only the SPEC document.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/309-spec-locked-layout-bonus-only-cap-shared-cap-disposition-spe/309-CONTEXT.md
@.planning/phases/309-spec-locked-layout-bonus-only-cap-shared-cap-disposition-spe/309-SPEC.md

<baseline>
Audit baseline HEAD: MILESTONE_V44_AT_HEAD_6f0ba2963a10654ba554a8c333c5ee80c54a8349.
Grep against the contracts/ directory only (memory feedback_contract_locations).
</baseline>

<interfaces>
<!-- Planner pre-verified these against HEAD. The §4 proof must re-ground each claim with -->
<!-- the executor's own grep and record the matched line. -->

_applyEvMultiplierWithCap (LootboxModule.sol:475-509) — the ONLY function reading/writing
the shared cap. Inside it: SLOAD `lootboxEvBenefitUsedByLevel[player][lvl]` then, if
non-neutral and cap remaining, SSTORE `lootboxEvBenefitUsedByLevel[player][lvl] = used + adjustedPortion`.
Today the early return is `== LOOTBOX_EV_NEUTRAL_BPS`; Change 1 (SPEC-02) widens it to `<=`.
This is the single shared-mutable touchpoint for all three callers.

resolveLootboxDirect (LootboxModule.sol:666-693):
- `rngWord` is a PARAMETER (passed in, not SLOADed in this fn).
- `activityScore` is a PARAMETER → `_lootboxEvMultiplierFromScore(uint256(activityScore))` at :674.
  Frozen by caller: decimator = bucket-at-burn, degenerette = bet-time. NOT derived from rngWord.
- `level` (global) read for `currentLevel = level + 1` (:670) — not player-discretionary.
- seed `keccak256(abi.encode(rngWord, player, day, amount))` at :671 — raw `amount`, day from `_simulatedDayIndex()`.
- Shared-mutable SLOAD/SSTORE: only `lootboxEvBenefitUsedByLevel[player][currentLevel]` via `_applyEvMultiplierWithCap` at :675.

resolveRedemptionLootbox (LootboxModule.sol:702-729):
- Identical shape: `rngWord` + `activityScore` are PARAMETERS; multiplier from `activityScore` at :710
  (redemption = burn-submission snapshot). seed at :707 (raw amount). cap call at :711.
- Shared-mutable: only `lootboxEvBenefitUsedByLevel[player][currentLevel]`.

openLootBox (LootboxModule.sol:517-575) — in-window SLOADs to enumerate:
- :521 `lootboxEth[index][player]`         (per-(index,player); written at purchase, pre-word)
- :524 `lootboxRngWordByIndex[index]`      (the VRF word itself — the unlock gate; rngWord==0 reverts RngNotReady)
- :527 `_simulatedDayIndex()`              (global day)
- :528 `lootboxDay[index][player]`         (seed input; frozen at purchase; D-03 keeps it separate)
- :533 presale flag `_psRead(...)`         (global)
- :534 `lootboxEthBase[index][player]`     (per-(index,player); pre-word)
- :539 `lootboxBaseLevelPacked[index][player]`  (per-(index,player); pre-word; merges into packed word post-IMPL)
- :557 `lootboxEvScorePacked[index][player]`     (per-(index,player); frozen score; becomes lootboxPurchasePacked post-IMPL)
- :566 `lootboxDistressEth[index][player]` (per-(index,player); pre-word)
- :485 (inside cap fn, pre-IMPL) `lootboxEvBenefitUsedByLevel[player][currentLevel]` — REMOVED from open path post-IMPL (cap moves to allocation).
POST-IMPL openLootBox reads the FROZEN packed word and does NO cap SLOAD/SSTORE (SPEC-03).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: §4.A SPEC-04 backward-trace — prove word-independence (ACCEPT as theorem)</name>
  <read_first>
    - 309-SPEC.md §0 (the grep-verified resolver + cap-fn line numbers this proof cites)
    - .planning/phases/309-*/309-CONTEXT.md (D-10 — the 4-point backward-trace skeleton)
    - .planning/REQUIREMENTS.md (SPEC-04, INV-04/05/06, Out-of-Scope "Accepted self-MEV races")
    - contracts/modules/DegenerusGameLootboxModule.sol:666-693 (resolveLootboxDirect), :702-729 (resolveRedemptionLootbox), :444 (_lootboxEvMultiplierFromScore), :475-509 (_applyEvMultiplierWithCap)
    - Memory methodology: feedback_rng_backward_trace, feedback_rng_commitment_window
  </read_first>
  <action>
    Append `## §4 SPEC-04 — Shared-Cap Disposition (LOCKED: ACCEPT)` and its subsection
    `### §4.A Word-Independence Backward-Trace` to 309-SPEC.md. State the disposition is
    ACCEPT (D-10) and that the rest of §4 is the PROOF, not an assertion — open with an
    explicit "this is a theorem, proven below; no 'by construction' claim" line.

    Trace BACKWARD from each consumer to its input-commitment time (per feedback_rng_backward_trace),
    transcribing/grounding the D-10 four-point argument:
      1. Multiplier source is the FROZEN `activityScore` PARAMETER, not `rngWord`:
         `evMultiplierBps = _lootboxEvMultiplierFromScore(uint256(activityScore))` at
         LootboxModule.sol:674 (resolveLootboxDirect) and :710 (resolveRedemptionLootbox).
         State the commitment time per caller: decimator = bucket-at-burn, degenerette =
         bet-time, redemption = burn-submission snapshot. Confirm `activityScore` and
         `rngWord` are both function PARAMETERS (caller-supplied), so the multiplier was
         fixed before this fn sees the word.
      2. Seed uses raw `amount` (:671/:707) → cap allocation never changes any roll
         (IMPL-05 / INV-04). Cite the §0 seed-build rows.
      3. Purchased boxes now allocate the cap PRE-WORD at purchase (SPEC-03), so they do
         NOT compete at resolution — removing the open-path cap draw the old greedy design had.
      4. Residual freedom = resolution-ORDER cap-steering among on-the-fly bonus boxes.
         Prove it is word-INDEPENDENT: because multipliers are frozen, steering the scarce
         cap to the highest-multiplier box is optimal REGARDLESS of the word; knowing
         `rngWord` yields no extra gain. Therefore NOT a freeze violation → classify as
         already-accepted self-MEV (REQUIREMENTS Out-of-Scope "Accepted self-MEV races").

    For each consumer, write the backward chain explicitly: consumer → multiplier input
    (activityScore param, frozen at commitment) → confirm the word (rngWord param) was
    unknown/uncommitted at that commitment time and influences ONLY the seed/roll (not the
    multiplier or the cap allocation). State that this preserves INV-06 (no new
    player-discretionary writer of a slot consumed against the live word).

    READS contract source; WRITES only 309-SPEC.md. No code/test edits.
  </action>
  <verify>
    <automated>F=.planning/phases/309-spec-locked-layout-bonus-only-cap-shared-cap-disposition-spe/309-SPEC.md; grep -q "SPEC-04" "$F" && grep -Eq "§4.A|4.A" "$F" && grep -q "activityScore" "$F" && grep -Eq "674" "$F" && grep -Eq "710" "$F" && grep -Eiq "self-MEV|self MEV" "$F" && grep -Eiq "backward" "$F"</automated>
  </verify>
  <acceptance_criteria>
    - 309-SPEC.md §4 opens stating disposition = ACCEPT and that the section is a PROOF/theorem, with an explicit "no 'by construction'" disclaimer.
    - §4.A states the multiplier derives from the frozen `activityScore` PARAMETER (not `rngWord`) and cites both `_lootboxEvMultiplierFromScore(uint256(activityScore))` sites by line (674 + 710).
    - §4.A names the per-caller commitment time: decimator = bucket-at-burn, degenerette = bet-time, redemption = burn-submission snapshot.
    - §4.A states the seed uses raw `amount` (cites :671/:707) → cap allocation never changes a roll (INV-04).
    - §4.A states purchased boxes allocate the cap pre-word at purchase so they do not compete at resolution.
    - §4.A classifies the residual resolution-order cap-steering as word-INDEPENDENT (frozen multipliers ⇒ knowing rngWord yields no extra gain) and maps it to the REQUIREMENTS Out-of-Scope "Accepted self-MEV races".
    - §4.A states INV-06 is preserved (no new player-discretionary writer against the live word).
    - `git status --porcelain contracts/ test/` returns no output.
  </acceptance_criteria>
  <done>§4.A delivers the four-point word-independence backward-trace as a proof — frozen-activityScore multiplier source (line-cited), raw-amount seed, pre-word purchased allocation, and the residual order-steering classified as accepted self-MEV — grounded in §0 grep-verified lines; zero code mutations.</done>
</task>

<task type="auto">
  <name>Task 2: §4.B SPEC-04 in-window SLOAD enumeration + ACCEPT verdict</name>
  <read_first>
    - 309-SPEC.md §0 + §4.A (the verified sites and the backward-trace this enumeration completes)
    - .planning/phases/309-*/309-CONTEXT.md (D-11 — the SLOAD-enumeration requirement)
    - .planning/REQUIREMENTS.md (SPEC-04, INV-05/06)
    - contracts/modules/DegenerusGameLootboxModule.sol:517-575 (openLootBox in-window reads), :666-693, :702-729 (resolvers), :475-509 (cap fn SLOAD/SSTORE)
    - Memory methodology: feedback_rng_window_storage_read_freshness
  </read_first>
  <action>
    Append `### §4.B In-Window SLOAD Enumeration (all three callers)` to 309-SPEC.md. Per
    feedback_rng_window_storage_read_freshness, enumerate ALL storage reads inside the
    `[rng request → unlock]` window for EACH of the three callers — not just the
    VRF-derived seeds; non-VRF reads consumed alongside the word are a distinct bug class
    (F-41-02/03 precedent). For each caller, produce a table of every in-window SLOAD:
    { slot/mapping, file:line, key shape, mutable-or-frozen, shared-or-per-box, why-safe }.

      - openLootBox (LootboxModule.sol:517-575): enumerate lootboxEth (:521),
        lootboxRngWordByIndex (:524, the unlock gate), lootboxDay (:528, seed input — D-03),
        lootboxEthBase (:534), lootboxBaseLevelPacked (:539), lootboxEvScorePacked (:557),
        lootboxDistressEth (:566), and the cap SLOAD inside `_applyEvMultiplierWithCap`
        (:485, pre-IMPL). State that POST-IMPL the open path reads the FROZEN packed word
        (lootboxPurchasePacked) and performs NO cap SLOAD/SSTORE (SPEC-03) — so for
        purchased boxes there is NO shared mutable consumed alongside the word after the fix.
      - resolveLootboxDirect (:666-693): enumerate `level` (global, :670) and the cap
        SLOAD/SSTORE `lootboxEvBenefitUsedByLevel[player][currentLevel]` via the cap fn
        (:675 → :485/:503). Note rngWord + activityScore are parameters (no SLOAD).
      - resolveRedemptionLootbox (:702-729): identical enumeration; cap touchpoint at
        :711 → :485/:503.

    Conclude with the LOCKED finding (D-11): across all three callers,
    `lootboxEvBenefitUsedByLevel[player][currentLevel]` is the ONLY shared MUTABLE consumed
    alongside the word — every other in-window read is either a global, a frozen
    seed-input (lootboxDay), or a per-(index,player) value committed pre-word. Confirm this
    is enumerated-and-verified, not assumed.

    Close §4 with the ACCEPT VERDICT block tying §4.A + §4.B together: the shared
    accumulator cannot be reordered against a KNOWN word (purchased allocation fixed
    pre-word; on-the-fly multipliers frozen at commitment; only word-independent
    order-steering remains = accepted self-MEV). State disposition = ACCEPT + DOCUMENT,
    satisfying SPEC-04 / INV-05 with no fix required.

    READS contract source; WRITES only 309-SPEC.md. No code/test edits.
  </action>
  <verify>
    <automated>F=.planning/phases/309-spec-locked-layout-bonus-only-cap-shared-cap-disposition-spe/309-SPEC.md; grep -Eq "§4.B|4.B" "$F" && grep -q "lootboxEvBenefitUsedByLevel" "$F" && grep -q "lootboxRngWordByIndex" "$F" && grep -q "lootboxDistressEth" "$F" && grep -Eiq "only shared mutable|only shared|sole shared" "$F" && grep -Eiq "ACCEPT" "$F"</automated>
  </verify>
  <acceptance_criteria>
    - 309-SPEC.md §4.B contains a per-caller in-window SLOAD table for openLootBox, resolveLootboxDirect, and resolveRedemptionLootbox.
    - The openLootBox table enumerates at minimum: lootboxEth, lootboxRngWordByIndex, lootboxDay, lootboxEthBase, lootboxBaseLevelPacked, lootboxEvScorePacked, lootboxDistressEth, and the cap SLOAD — each with its file:line.
    - §4.B states that POST-IMPL the open path reads the frozen packed word and performs NO cap SLOAD/SSTORE (no shared mutable for purchased boxes after the fix).
    - §4.B states `lootboxEvBenefitUsedByLevel[player][currentLevel]` is the ONLY shared MUTABLE consumed alongside the word across all three callers, and that this is enumerated/verified (not assumed).
    - §4.B notes lootboxDay is a frozen seed input (D-03), distinguishing it from a shared mutable.
    - §4 closes with an ACCEPT + DOCUMENT verdict block referencing §4.A + §4.B and satisfying SPEC-04 / INV-05 with no fix.
    - `git status --porcelain contracts/ test/` returns no output.
  </acceptance_criteria>
  <done>§4.B enumerates every in-window SLOAD for all three callers in per-caller tables, identifies lootboxEvBenefitUsedByLevel as the sole shared mutable (verified, not assumed), notes the post-IMPL open path has none, and closes §4 with the ACCEPT verdict tying the backward-trace + enumeration together; zero code mutations.</done>
</task>

</tasks>

<threat_model>
SPEC-only phase: zero executable surface ships. The threat is that SPEC-04's ACCEPT rests
on an under-proven word-independence claim, letting Phase 310 IMPL keep a known-word
reorder vector through the shared cap accumulator.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPEC-04 verdict → Phase 310/312 | An unproven "accept" propagates to IMPL + the adversarial sweep as a false-clear |
| [rng request → unlock] window → resolver state | Any shared mutable read alongside the live word is a candidate freeze violation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-309-04 | Repudiation (unproven accept) | SPEC-04 ACCEPT stated without proof | mitigate | §4.A delivers the backward-trace (frozen-activityScore multiplier, raw-amount seed, pre-word purchased allocation) and §4.B the full in-window SLOAD enumeration — proof, not assertion (D-10/D-11). |
| T-309-05 | Tampering (known-word reorder) | shared lootboxEvBenefitUsedByLevel accumulator across purchased + on-the-fly callers | accept | §4 proves the residual order-steering is word-INDEPENDENT (frozen multipliers); classified as accepted self-MEV per REQUIREMENTS Out-of-Scope. INV-05/06 preserved. |
| T-309-06 | Information disclosure (missed in-window read) | a non-VRF SLOAD consumed alongside the word goes unenumerated (F-41-02/03 bug class) | mitigate | §4.B enumerates ALL in-window SLOADs per caller from grep-verified lines, not just VRF-derived seeds (feedback_rng_window_storage_read_freshness). |
</threat_model>

<verification>
- §4 (with §4.A and §4.B) appended to the existing 309-SPEC.md (which already has §0-§3 from Plan 01).
- `git status --porcelain contracts/ test/` returns no output across both tasks.
- §4.A: four-point backward-trace with line-cited frozen-activityScore multiplier source.
- §4.B: per-caller in-window SLOAD tables; lootboxEvBenefitUsedByLevel identified as the sole shared mutable; ACCEPT verdict.
</verification>

<success_criteria>
- ROADMAP SC4 satisfied: backward-trace shows resolveLootboxDirect + resolveRedemptionLootbox introduce no known-word ordering edge through the shared accumulator; fix-or-accept disposition documented as ACCEPT, proven per feedback_rng_backward_trace + feedback_rng_window_storage_read_freshness.
- Zero `contracts/` and zero `test/` mutations.
</success_criteria>

<output>
Create `.planning/phases/309-spec-locked-layout-bonus-only-cap-shared-cap-disposition-spe/309-02-SUMMARY.md` when done.
</output>
