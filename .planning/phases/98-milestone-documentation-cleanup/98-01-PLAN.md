---
phase: 98-milestone-documentation-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - contracts/storage/DegenerusGameStorage.sol
autonomous: true
requirements: [DOC-01, BANNER-01]

must_haves:
  truths:
    - "REQUIREMENTS.md checkboxes for DELTA-01, DELTA-02, DELTA-04, and GOPT-03 are checked"
    - "All REQUIREMENTS.md traceability rows show Complete (none show Partial, Pending, or checkpoint pending)"
    - "DegenerusGameStorage.sol EVM SLOT 1 banner appears after compressedJackpotFlag and before purchaseStartDay"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate requirement tracking for v4.2 milestone"
      contains: "[x] **DELTA-01**"
    - path: "contracts/storage/DegenerusGameStorage.sol"
      provides: "Correct section banner placement for Slot 0/Slot 1 boundary"
      contains: "EVM SLOT 1"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "v4.2-MILESTONE-AUDIT.md gap list"
      via: "checkbox and traceability state"
      pattern: "\\[x\\] \\*\\*DELTA-0[124]\\*\\*"
    - from: "contracts/storage/DegenerusGameStorage.sol"
      to: "EVM Slot 0/Slot 1 boundary"
      via: "section banner comment"
      pattern: "EVM SLOT 1"
---

<objective>
Close all v4.2 milestone documentation gaps identified in v4.2-MILESTONE-AUDIT.md.

Purpose: The milestone audit found two categories of gaps: (1) REQUIREMENTS.md checkboxes and traceability rows are stale — four requirements were verified during execution but never marked complete; (2) the "EVM SLOT 1" section banner in DegenerusGameStorage.sol is placed before two Slot 0 variables, making the slot boundary visually misleading.

Output:
- .planning/REQUIREMENTS.md with all checkboxes and traceability rows accurate
- contracts/storage/DegenerusGameStorage.sol with the section banner moved to the correct position
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v4.2-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix REQUIREMENTS.md checkboxes and traceability (DOC-01)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — must read before any edit)
    - .planning/v4.2-MILESTONE-AUDIT.md (gap evidence for each requirement)
  </read_first>
  <action>
    Make the following exact edits to .planning/REQUIREMENTS.md:

    CHECKBOX CHANGES (4 lines):
    1. Change `- [ ] **DELTA-01**` to `- [x] **DELTA-01**`
    2. Change `- [ ] **DELTA-02**` to `- [x] **DELTA-02**`
    3. Change `- [ ] **DELTA-04**` to `- [x] **DELTA-04**`
    4. Change `- [ ] **GOPT-03**` to `- [x] **GOPT-03**`

    TRACEABILITY TABLE CHANGES (4 rows):
    5. Change `| DELTA-01 | Phase 95 (tracking: Phase 98) | Verified — checkbox pending |`
       to     `| DELTA-01 | Phase 95 | Complete |`
    6. Change `| DELTA-02 | Phase 95 (tracking: Phase 98) | Verified — checkbox pending |`
       to     `| DELTA-02 | Phase 95 | Complete |`
    7. Change `| DELTA-04 | Phase 95 (tracking: Phase 98) | Verified — checkbox pending |`
       to     `| DELTA-04 | Phase 95 | Complete |`
    8. Change `| GOPT-03 | Phase 96 (tracking: Phase 98) | Complete — checkbox pending |`
       to     `| GOPT-03 | Phase 96 | Complete |`

    Also update the DOC-01 and BANNER-01 rows:
    9.  Change `| DOC-01  | Phase 98 | Pending |`
        to     `| DOC-01  | Phase 98 | Complete |`
    10. Change `| BANNER-01 | Phase 98 | Pending |`
        to     `| BANNER-01 | Phase 98 | Complete |`

    Update the footer timestamp:
    11. Change `*Last updated: 2026-03-24 after gap closure planning (audit reset partial requirements)*`
        to     `*Last updated: 2026-03-25 after Phase 98 gap closure (all v4.2 requirements complete)*`

    Do NOT change any other lines. Preserve all NatSpec, whitespace, and formatting.
  </action>
  <verify>
    <automated>grep -n "\[ \] \*\*DELTA\|Verified — checkbox\|checkbox pending\|Pending — Plan 03" /home/zak/Dev/PurgeGame/degenerus-audit/.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "- \[ \] \*\*DELTA-01" .planning/REQUIREMENTS.md` returns no output (checkbox is checked)
    - `grep "- \[ \] \*\*DELTA-02" .planning/REQUIREMENTS.md` returns no output
    - `grep "- \[ \] \*\*DELTA-04" .planning/REQUIREMENTS.md` returns no output
    - `grep "- \[ \] \*\*GOPT-03" .planning/REQUIREMENTS.md` returns no output
    - `grep "- \[x\] \*\*DELTA-01" .planning/REQUIREMENTS.md` returns a match
    - `grep "- \[x\] \*\*DELTA-02" .planning/REQUIREMENTS.md` returns a match
    - `grep "- \[x\] \*\*DELTA-04" .planning/REQUIREMENTS.md` returns a match
    - `grep "- \[x\] \*\*GOPT-03" .planning/REQUIREMENTS.md` returns a match
    - `grep "checkbox pending\|Verified — checkbox\|Pending — Plan 03\| Pending |" .planning/REQUIREMENTS.md` returns no output
    - `grep "| DELTA-01 | Phase 95 | Complete |" .planning/REQUIREMENTS.md` returns a match
    - `grep "| DELTA-02 | Phase 95 | Complete |" .planning/REQUIREMENTS.md` returns a match
    - `grep "| DELTA-04 | Phase 95 | Complete |" .planning/REQUIREMENTS.md` returns a match
    - `grep "| GOPT-03 | Phase 96 | Complete |" .planning/REQUIREMENTS.md` returns a match
  </acceptance_criteria>
  <done>All four previously-unchecked requirement checkboxes are checked. All traceability rows show "Complete" with no status words like "Pending", "Partial", or "pending".</done>
</task>

<task type="auto">
  <name>Task 2: Move EVM SLOT 1 banner to correct position in DegenerusGameStorage.sol (BANNER-01)</name>
  <files>contracts/storage/DegenerusGameStorage.sol</files>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol lines 270-320 (the region around the banner and the Slot 0/1 boundary)
  </read_first>
  <action>
    The current layout around lines 278-302 is:

    ```
    // Note: dailyJackpotCoinTicketsPending ends at Slot 0 byte 29.
    // dailyEthPhase (byte 30) and compressedJackpotFlag (byte 31) fill the remaining Slot 0 space. No padding.

    // =========================================================================
    // EVM SLOT 1: Price and Double-Buffer Fields
    // =========================================================================
    // Packs into EVM Slot 1: purchaseStartDay through prizePoolFrozen (25 bytes used, 7 bytes padding).

    /// @dev Daily jackpot ETH phase.
    ///      0 = current level, 1 = carryover.
    uint8 internal dailyEthPhase;

    /// @dev Jackpot compression tier: 0=normal (5d), 1=compressed (3d), 2=turbo (1d).
    ///      Set when purchase-phase target is met quickly, signaling high player interest.
    ///      Turbo (2): target met within 1 day — entire jackpot in 1 physical day.
    ///      Compressed (1): target met within 3 days — 5 logical days in 3 physical.
    ///      Cleared at phase end.
    uint8 internal compressedJackpotFlag;

    /// @dev Game day index when the current purchase phase opened.
    ...
    uint48 internal purchaseStartDay;
    ```

    The target layout must be:

    ```
    // Note: dailyJackpotCoinTicketsPending ends at Slot 0 byte 29.
    // dailyEthPhase (byte 30) and compressedJackpotFlag (byte 31) fill the remaining Slot 0 space. No padding.

    /// @dev Daily jackpot ETH phase.
    ///      0 = current level, 1 = carryover.
    uint8 internal dailyEthPhase;

    /// @dev Jackpot compression tier: 0=normal (5d), 1=compressed (3d), 2=turbo (1d).
    ///      Set when purchase-phase target is met quickly, signaling high player interest.
    ///      Turbo (2): target met within 1 day — entire jackpot in 1 physical day.
    ///      Compressed (1): target met within 3 days — 5 logical days in 3 physical.
    ///      Cleared at phase end.
    uint8 internal compressedJackpotFlag;

    // =========================================================================
    // EVM SLOT 1: Price and Double-Buffer Fields
    // =========================================================================
    // Packs into EVM Slot 1: purchaseStartDay through prizePoolFrozen (25 bytes used, 7 bytes padding).

    /// @dev Game day index when the current purchase phase opened.
    ...
    uint48 internal purchaseStartDay;
    ```

    Steps:
    1. Remove the 4-line banner block (the two separator lines, the title line, and the description line) from its current position between the Note comment and `dailyEthPhase`.
    2. Insert the same 4-line banner block between `compressedJackpotFlag` and the `/// @dev Game day index` comment for `purchaseStartDay`.

    The banner block is exactly:
    ```
    // =========================================================================
    // EVM SLOT 1: Price and Double-Buffer Fields
    // =========================================================================
    // Packs into EVM Slot 1: purchaseStartDay through prizePoolFrozen (25 bytes used, 7 bytes padding).
    ```

    There must be a blank line between the Note comment and `dailyEthPhase` after the banner is removed (preserve the existing blank line that was between the Note and the old banner, or ensure one blank line separates the Note from the `/// @dev Daily jackpot` comment).

    There must be a blank line before the banner in its new position (between `compressedJackpotFlag;` declaration and the banner opening `//===`).

    Do NOT change any variable declarations, NatSpec comments, or any other part of the file.

    This is a comment-only change. No logic, no variable order, no runtime behavior change.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "EVM SLOT 1" contracts/storage/DegenerusGameStorage.sol` returns a line number AFTER the line containing `compressedJackpotFlag`
    - `grep -n "EVM SLOT 1\|compressedJackpotFlag\|purchaseStartDay\|dailyEthPhase" contracts/storage/DegenerusGameStorage.sol` shows order: dailyEthPhase, compressedJackpotFlag, EVM SLOT 1, purchaseStartDay
    - `forge build` exits 0 (no compilation errors introduced)
    - `grep -c "EVM SLOT 1" contracts/storage/DegenerusGameStorage.sol` returns 2 (the title line + the description line; banner has exactly one title and one description)
  </acceptance_criteria>
  <done>The "EVM SLOT 1" section banner appears immediately before purchaseStartDay, after compressedJackpotFlag. The file compiles cleanly. No variable declarations were moved or altered.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `grep -c "- \[ \]" .planning/REQUIREMENTS.md` — count of unchecked boxes should be 0 (all were either already checked or checked in this task)
2. `grep "Pending\|Partial\|checkbox pending" .planning/REQUIREMENTS.md` — should return no output
3. `grep -n "EVM SLOT 1\|compressedJackpotFlag\|purchaseStartDay" contracts/storage/DegenerusGameStorage.sol` — line order must be: compressedJackpotFlag < EVM SLOT 1 < purchaseStartDay
4. `forge build` exits 0
</verification>

<success_criteria>
1. All REQUIREMENTS.md checkboxes for v4.2 requirements are checked (DELTA-01, DELTA-02, DELTA-04, GOPT-03 changed from `[ ]` to `[x]`)
2. All REQUIREMENTS.md traceability rows show "Complete" — no row shows "Pending", "Partial", or any variant of "pending"
3. DegenerusGameStorage.sol "EVM SLOT 1" banner appears after compressedJackpotFlag and before purchaseStartDay
4. forge build exits 0 (contract still compiles)
</success_criteria>

<output>
After completion, create `.planning/phases/98-milestone-documentation-cleanup/98-01-SUMMARY.md` using the summary template.
</output>
