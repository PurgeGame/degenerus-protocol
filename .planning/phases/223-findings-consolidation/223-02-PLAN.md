---
phase: 223-findings-consolidation
plan: 02
type: execute
wave: 2
depends_on: [223-01]
files_modified:
  - KNOWN-ISSUES.md
  - .planning/MILESTONES.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CSI-13, CSI-14]
must_haves:
  truths:
    - "KNOWN-ISSUES.md is updated with accepted INFO/LOW items from v27.0 that meet D-08 criteria (design decision / accepted trade-off / external-reader benefit)"
    - ".planning/MILESTONES.md has a v27.0 Call-Site Integrity Audit entry mirroring the v26.0 format (Shipped date, Phases completed line, Key accomplishments bullets)"
    - ".planning/PROJECT.md moves v27.0 from 'Current Milestone' to 'Completed Milestone' and lists v27.0 result summary"
    - ".planning/REQUIREMENTS.md flips CSI-12, CSI-13, CSI-14 checkboxes from '[ ]' to '[x]' and the traceability table rows to 'Complete' (CSI-08, CSI-09, CSI-10 are also flipped since 222-VERIFICATION documents them as satisfied — matches the STATE.md / 222-VERIFICATION note)"
  artifacts:
    - path: "KNOWN-ISSUES.md"
      provides: "Updated Design Decisions section referencing specific F-27-NN IDs from audit/FINDINGS-v27.0.md"
      contains: "At least one new entry introduced by v27.0"
    - path: ".planning/MILESTONES.md"
      provides: "v27.0 retrospective entry at the top of the file (most recent milestone first)"
      contains: "## v27.0 Call-Site Integrity Audit (Shipped: 2026-04-13)"
    - path: ".planning/PROJECT.md"
      provides: "Updated milestone status — v27.0 completed, next milestone TBD or current"
      contains: "## Completed Milestone: v27.0 Call-Site Integrity Audit"
    - path: ".planning/REQUIREMENTS.md"
      provides: "All v27.0 CSI-NN checkboxes marked complete"
      contains: "[x] CSI-12, [x] CSI-13, [x] CSI-14"
  key_links:
    - from: "KNOWN-ISSUES.md new v27.0 entries"
      to: "audit/FINDINGS-v27.0.md"
      via: "explicit F-27-NN reference in each new KNOWN-ISSUES entry"
      pattern: "F-27-0[1-9]|F-27-1[0-6]"
    - from: ".planning/MILESTONES.md v27.0 entry"
      to: ".planning/ROADMAP.md Phase 220/221/222/223 completion rows"
      via: "phase count + accomplishments bullets derived from ROADMAP completion table + phase SUMMARYs"
      pattern: "Phases completed: 4 phases"
    - from: ".planning/PROJECT.md 'Completed Milestone: v27.0' section"
      to: ".planning/MILESTONES.md v27.0 entry"
      via: "one-line result summary matching the MILESTONES entry's first accomplishment"
      pattern: "v27.0 Call-Site Integrity Audit"
    - from: ".planning/REQUIREMENTS.md Traceability table"
      to: "CSI-12/13/14 checkbox section"
      via: "status column flipped from Pending to Complete matching the checkbox state"
      pattern: "CSI-1[234].*Complete"
---

<objective>
Finalize the v27.0 milestone by promoting accepted design-decision findings to `KNOWN-ISSUES.md` per D-08, writing the v27.0 retrospective entry in `.planning/MILESTONES.md` per D-09, moving v27.0 from "Current Milestone" to "Completed Milestone" in `.planning/PROJECT.md` per D-10 (PRESERVING the v27.0 goal/target-scope/incident-context narrative by migrating it into the new Completed Milestone block), and flipping the CSI-12/CSI-13/CSI-14 requirement checkboxes (plus CSI-08/CSI-09/CSI-10 which are satisfied but still showing `[ ]` per 222-VERIFICATION's note) in `.planning/REQUIREMENTS.md`.

Purpose: Satisfies CSI-13 (KNOWN-ISSUES) and CSI-14 (MILESTONES + PROJECT). Depends on Plan 223-01 because the new KNOWN-ISSUES entries must reference specific F-27-NN IDs that only exist after FINDINGS-v27.0.md is written.

Output: Four files modified — `KNOWN-ISSUES.md`, `.planning/MILESTONES.md`, `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`. No new files. No contract or test edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/MILESTONES.md
@.planning/phases/223-findings-consolidation/223-CONTEXT.md

# Output of Plan 223-01 — authoritative finding IDs and dispositions
@audit/FINDINGS-v27.0.md
@.planning/phases/223-findings-consolidation/223-01-SUMMARY.md

# Structure templates to mirror
@KNOWN-ISSUES.md

# Phase SUMMARYs for MILESTONES accomplishments bullets
@.planning/phases/220-delegatecall-target-alignment/220-01-SUMMARY.md
@.planning/phases/220-delegatecall-target-alignment/220-02-SUMMARY.md
@.planning/phases/221-raw-selector-calldata-audit/221-01-SUMMARY.md
@.planning/phases/221-raw-selector-calldata-audit/221-02-SUMMARY.md
@.planning/phases/222-external-function-coverage-gap/222-01-SUMMARY.md
@.planning/phases/222-external-function-coverage-gap/222-02-SUMMARY.md
@.planning/phases/222-external-function-coverage-gap/222-03-SUMMARY.md
</context>

<interfaces>
<!-- .planning/MILESTONES.md v26.0 entry (lines 1-13) — structure to mirror for v27.0. -->

v26.0 format reference (MILESTONES.md lines 1-13):
```markdown
## v26.0 Bonus Jackpot Split (Shipped: 2026-04-12)

**Phases completed:** 2 phases, 4 plans, 9 tasks

**Key accomplishments:**

- keccak256 domain separation for independent bonus traits, BURNIE target range [lvl+1, lvl+4], DJT storage removed
- All 6 jackpot caller sites rewired with independent bonus trait routing and DailyWinningTraits event emission
- Complete delta audit of 7 Phase 218 commits across 4 contracts with 10 code path sections, event correctness at 3 sites, and entropy independence proof
- Fix 1 -- EVNT-01 naming (D-10):

---
```

<!-- KNOWN-ISSUES.md Design Decisions section (lines 9-36) — each entry is a bolded one-liner + paragraph + finding reference. New v27.0 entries should mirror this form. -->

KNOWN-ISSUES entry format reference (per existing entry at line 29-33 for F-25-08):
```markdown
**<Short descriptive title>.** <Paragraph describing the design decision, the trade-off, and why it's accepted.> (See <F-27-NN> in `audit/FINDINGS-v27.0.md`)
```

<!-- PROJECT.md structure (lines 11-27) — "Current Milestone" followed by "Completed Milestone" entries. -->

PROJECT.md pattern (lines 11-27) for completed-milestone entry:
```markdown
## Completed Milestone: <Vx.y Title>

**Status:** Complete (YYYY-MM-DD)

**Result:** <1-3 sentences summarizing phase count, plan count, core deliverables, key metric(s).>
```

<!-- REQUIREMENTS.md status section (lines 29-32 for Phase 222; 34-38 for Phase 223) and traceability table (lines 65-82) — edit both for consistency. -->
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Promote D-08-qualifying items to KNOWN-ISSUES.md referencing F-27-NN IDs from 223-01 output</name>
  <files>KNOWN-ISSUES.md</files>
  <read_first>
    - KNOWN-ISSUES.md (full — understand existing Design Decisions section structure; find the insertion point)
    - audit/FINDINGS-v27.0.md (produced by Plan 223-01; identify which F-27-NN IDs qualify per D-08 promotion criteria)
    - .planning/phases/223-findings-consolidation/223-01-SUMMARY.md (the handoff note identifying D-08 promotion candidates AND the F-27-NN → source-item mapping table)
    - .planning/phases/223-findings-consolidation/223-CONTEXT.md (D-08 criteria verbatim)
  </read_first>
  <action>
    Append new v27.0 entries to the `## Design Decisions` section of `KNOWN-ISSUES.md`. Entries MUST follow the existing pattern: a bolded one-liner title, a prose paragraph explaining the design decision and accepted trade-off, and a parenthetical `(See F-27-NN in audit/FINDINGS-v27.0.md)` reference at the end.

    **D-08 promotion criteria (all three must hold):**
    1. Severity is INFO or LOW.
    2. The item is a design decision or accepted trade-off (not a bug awaiting fix).
    3. An external user/auditor reading only KNOWN-ISSUES.md benefits from the context.

    **Expected promotion candidates from v27.0** (final list determined by Plan 223-01's actual F-27-NN assignments; use the 223-01-SUMMARY.md mapping table for concrete F-27-NN IDs):

    a) **WR-222-01 (patchContractAddresses.js VRF_KEY_HASH regex)** — Accepted as non-runtime deployment-tooling robustness note. An external reader evaluating deploy integrity might wonder why the deploy script does not defensively validate its patch operations; promoting this entry explains the accepted trade-off and points to the F-27-NN finding for detail.

    b) **Phase 221 WR-221-01/WR-221-02 resolutions (summary form)** — Brief one-entry summary that v27.0's Phase 221 gate had two robustness gaps identified in code review and both were closed in-cycle. External readers evaluating the enforcement gates benefit from seeing this history referenced; full detail is in FINDINGS-v27.0.md.

    c) **Phase 222 VERIFICATION Gap 1 + Gap 2 resolutions (summary form)** — Brief one-entry summary (may be combined into a single KNOWN-ISSUES entry) documenting that the test-quality and drift-scoping gaps identified in 222-VERIFICATION.md were closed by Plan 222-03 (commits ef83c5cd, e0a1aa3e). This gives external auditors confidence that in-cycle quality gates were actually exercised and produced closure, and points them to the F-27-NN entries for full detail.

    d) **IN-220-03 (parallel-make race on ContractAddresses.sol)** — Accepted as a pre-existing foot-gun deferred to future cleanup. Documents the accepted trade-off (default `make test` is serial; `-j` users must be aware). Promote only if the Plan 223-01 consolidator produced a standalone F-27-NN for this item.

    **Authoring rules:**
    - Each new entry starts with a bolded title sentence ending in a period. Example: `**Phase 222 VERIFICATION gap closures from Plan 222-03.**`
    - The paragraph body is 2-5 sentences describing what the design decision / accepted trade-off IS (not what changed), consistent with `feedback_no_history_in_comments` (note that in KNOWN-ISSUES we may reference v27.0 as a cycle context because KNOWN-ISSUES is explicitly audit-reader-facing prose, not source-comment prose — "Promoted from F-27-NN after 222-VERIFICATION re-check" is acceptable narrative).
    - Each entry ends with `(See F-27-NN in \`audit/FINDINGS-v27.0.md\`)` using backticks around the filename.
    - Insert new entries at the END of the `## Design Decisions` section, after the existing F-25-12 decimator-settlement entry (currently the last entry before the `## Automated Tool Findings (Pre-disclosed)` section divider).
    - Minimum: 1 new entry. Maximum: 4 new entries (consolidator may combine b/c into a single "v27.0 in-cycle resolutions" entry if desired). Final count depends on how Plan 223-01 assigned F-27-NN IDs and how many unique design-decision narratives emerge.
    - Do NOT modify existing entries in KNOWN-ISSUES.md. Do NOT reorder existing sections.
    - Do NOT add entries under `## Automated Tool Findings (Pre-disclosed)` or `## ERC-20 Deviations` or `## Event Design Decisions` — only under `## Design Decisions`.
    - Count new entries added: record in the 223-02-SUMMARY.md.

    **Grep verification target:** After editing, `grep -c 'See F-27-' KNOWN-ISSUES.md` should return a number equal to the new-entry count (minimum 1).
  </action>
  <verify>
    <automated>
      test -f KNOWN-ISSUES.md
      grep -c '^\*\*' KNOWN-ISSUES.md  # total bolded entries increased vs pre-edit (pre-edit: count of `^\*\*` in git show HEAD:KNOWN-ISSUES.md)
      grep -c 'F-27-' KNOWN-ISSUES.md  # expect >= 1
      grep -cE 'audit/FINDINGS-v27\.0\.md' KNOWN-ISSUES.md  # expect >= 1
      grep -c '^## Design Decisions' KNOWN-ISSUES.md  # expect 1 (unchanged)
      grep -c '^## Automated Tool Findings' KNOWN-ISSUES.md  # expect 1 (unchanged — section not duplicated)
      git diff --name-only contracts/ test/  # expect empty
    </automated>
  </verify>
  <acceptance_criteria>
    - `KNOWN-ISSUES.md` contains at least one new entry referencing an `F-27-NN` ID and `audit/FINDINGS-v27.0.md`.
    - All new entries are inside the `## Design Decisions` section (located between `## Design Decisions` heading and the `## Automated Tool Findings (Pre-disclosed)` heading).
    - No existing entries in KNOWN-ISSUES.md were modified (only appended at end of Design Decisions section); `git diff KNOWN-ISSUES.md` shows only additions (no deletions from existing entries).
    - Each new entry follows the format: bolded one-liner title + prose paragraph + `(See F-27-NN in \`audit/FINDINGS-v27.0.md\`)` reference.
    - No edits under `## ERC-20 Deviations` or `## Event Design Decisions` or `## Automated Tool Findings`.
    - No contract or test edits — `git diff --name-only contracts/ test/` returns empty.
  </acceptance_criteria>
  <done>
    KNOWN-ISSUES.md is updated with D-08-qualifying v27.0 design decisions referencing specific F-27-NN IDs. CSI-13 is satisfied.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write v27.0 MILESTONES.md entry + move v27.0 in PROJECT.md (preserving narrative) + flip CSI-12/13/14 (and confirm CSI-08/09/10) in REQUIREMENTS.md</name>
  <files>.planning/MILESTONES.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/MILESTONES.md (full — identify insertion point; the v26.0 entry at lines 1-13 is the mirror template; v27.0 entry goes ABOVE v26.0 as the most recent milestone)
    - .planning/PROJECT.md (full — identify the `## Current Milestone: v27.0 Call-Site Integrity Audit` section starting around line 11 and the `## Completed Milestone:` pattern starting around line 23; CAPTURE the exact text of lines 11-21 — the v27.0 Goal / Target scope / Incident context narrative — because Edit 2(a)/(b) below MIGRATES this narrative rather than deleting it)
    - .planning/REQUIREMENTS.md (full — CSI-12/13/14 checkboxes at lines 36-38; traceability table at lines 65-82; CSI-08/09/10 still `[ ]` per 222-VERIFICATION note line 106)
    - .planning/phases/220-delegatecall-target-alignment/220-01-SUMMARY.md, 220-02-SUMMARY.md (for accomplishments bullets)
    - .planning/phases/221-raw-selector-calldata-audit/221-01-SUMMARY.md, 221-02-SUMMARY.md
    - .planning/phases/222-external-function-coverage-gap/222-01-SUMMARY.md, 222-02-SUMMARY.md, 222-03-SUMMARY.md
    - .planning/phases/223-findings-consolidation/223-01-SUMMARY.md (produced by Plan 223-01 — the F-27-NN finding count is the data point for the final bullet)
    - audit/FINDINGS-v27.0.md (for the specific INFO count to cite in MILESTONES)
    - .planning/ROADMAP.md (phase completion table — confirm Phase 220/221/222/223 row counts: 2/2/3/2 plans)
  </read_first>
  <action>
    Three file edits, all mechanical, all derived from the read_first inputs. Order: MILESTONES first (creates the single source of truth for the retrospective narrative), then PROJECT (references the MILESTONES summary), then REQUIREMENTS (checkbox state mirrors the project state).

    **Note on CONTEXT D-09 stale counts:** CONTEXT D-09 states "Expected totals: 4 phases, 7 plans, ~15 tasks". These are PRE-execution estimates and are superseded by the actuals (4 phases, 9 plans, 23 tasks — Phase 222 produced 3 plans rather than the 2 estimated, and total tasks are higher after Plan 222-03 gap-closure). Use the ACTUAL counts from the phase SUMMARY performance blocks. This CONTEXT-vs-actual divergence is informational; do not block on it.

    **Count-wording convention:** For the CRITICAL_GAP total, use the expression `177+1 CRITICAL_GAP` consistently in BOTH MILESTONES Edit 1 and PROJECT Edit 2(b). This matches the original 222-01 matrix classification (177 non-admin + 1 admin-boundary function = 178 total). Do NOT use the bare `178 CRITICAL_GAP` form anywhere in this plan's edits.

    ---

    **Edit 1: `.planning/MILESTONES.md` — add v27.0 entry ABOVE the v26.0 entry (most-recent-first ordering).**

    Insert immediately after the opening `# Milestones` line (line 1) and before the current line 3 (`## v26.0 Bonus Jackpot Split (Shipped: 2026-04-12)`). The new block is:

    ```markdown
    ## v27.0 Call-Site Integrity Audit (Shipped: 2026-04-13)

    **Phases completed:** 4 phases, 9 plans, <TASK_COUNT> tasks

    **Key accomplishments:**

    - Interface-coverage gate extended with delegatecall target-alignment gate — `scripts/check-delegatecall-alignment.sh` codifies 1:1 interface↔address mapping across 43 call sites in `contracts/` with `validate_mapping` preflight catching universe-level drift before per-site enumeration
    - Raw-selector gate `scripts/check-raw-selectors.sh` installed with 5-pattern coverage (hex literal, string-derived keccak selector, abi.encodeCall, abi.encodeWithSignature, abi.encode*-feeding-low-level-call) — wired as `make test-foundry` / `make test-hardhat` prerequisite alongside `check-interfaces` and `check-delegatecall`
    - 308 external/public functions across 24 deployable contracts classified COVERED / CRITICAL_GAP / EXEMPT in `222-01-COVERAGE-MATRIX.md` — final post-Plan-222-02 disposition: 19 COVERED / 177+1 CRITICAL_GAP (all with Test Ref) / 112 EXEMPT
    - `scripts/coverage-check.sh` standalone gate shipped with three orthogonal failure modes (MATRIX_DRIFT / UNCURED_GAP / REGRESSED_COVERAGE) — contract-scoped drift detection catches same-name function masking after Plan 222-03 fix (commit `e0a1aa3e`)
    - 76 leverage-first integration tests in `test/fuzz/CoverageGap222.t.sol` closing every CRITICAL_GAP via natural caller chains — Plan 222-03 (commit `ef83c5cd`) strengthened all 62 reachability-only assertions to guard-rejection or observable-state-change checks for CSI-11 quality closure
    - `audit/FINDINGS-v27.0.md` consolidates <FINDING_COUNT> INFO findings across the three source phases with full v25.0 regression appendix verifying F-25-01 through F-25-13 against current code — zero exploitable vulnerabilities, all items resolved or documented as accepted trade-offs
    - KNOWN-ISSUES.md updated with <KI_COUNT> new v27.0 design-decision entries referencing specific F-27-NN IDs

    ---
    ```

    Placeholders:
    - `<TASK_COUNT>` — sum of task counts across Phase 220 (3+2=5), Phase 221 (3+1=4), Phase 222 (3+5+3=11), Phase 223 (1+2=3). Total = 5+4+11+3 = **23 tasks**. Use exact integer 23 (derived from the task counts in each SUMMARY's performance block: 220-01=3, 220-02=2, 221-01=3, 221-02=1, 222-01=3, 222-02=5, 222-03=3, 223-01=1, 223-02=2). Recount from the actual SUMMARY files at execution time; if the total diverges, use the re-counted value.
    - `<FINDING_COUNT>` — exact integer from `audit/FINDINGS-v27.0.md` (count of `#### F-27-NN:` headers). Read from 223-01-SUMMARY.md or grep the FINDINGS file directly.
    - `<KI_COUNT>` — exact integer count of new KNOWN-ISSUES entries added in Task 1 of this plan.

    All three placeholders MUST be concrete integers in the final file — no literal `<TASK_COUNT>` / `<FINDING_COUNT>` / `<KI_COUNT>` strings allowed.

    ---

    **Edit 2: `.planning/PROJECT.md` — move v27.0 from "Current Milestone" to "Completed Milestone", PRESERVING the goal/target-scope/incident-context narrative by migrating it into the new Completed Milestone block.**

    Current state: lines 11-21 hold `## Current Milestone: v27.0 Call-Site Integrity Audit` with goal / target-scope / incident-context narrative (~10 lines of substantive framing content). Lines 23-27 are `## Completed Milestone: v26.0 Bonus Jackpot Split`.

    **Preservation principle:** The goal / target-scope / incident-context narrative in the existing `## Current Milestone: v27.0` section is substantive framing content that belongs to the v27.0 audit cycle now that v27.0 is shipping. It MUST be migrated into the new `## Completed Milestone: v27.0` block — NOT deleted. Only the `## Current Milestone:` header and the "Started / Target" stub fields get replaced with a fresh TBD block.

    Execute this edit in three sub-steps:

    (a) **Capture the v27.0 narrative.** Before making any destructive edit, copy the goal / target-scope / incident-context narrative currently under `## Current Milestone: v27.0 Call-Site Integrity Audit` (lines ~11-21) into an in-memory buffer. This narrative includes the audit theme, the three-axis breakdown, the incident-response framing, and any other substantive framing content below the "Started / Target" stub fields. Preserve the exact wording — editorial changes are out of scope for this plan.

    (b) **Replace ONLY the header + stub fields (not the narrative) with a fresh TBD Current Milestone block.** Per D-10: "Move 'Current Milestone' from v27.0 to whatever the next roadmap cycle is (check ROADMAP.md next-milestone section; if none defined, leave 'Current Milestone' empty with a `TBD — pending roadmap planning` note)."

    Check `.planning/ROADMAP.md` for a next-milestone section. Based on the Milestones bullet list at ROADMAP.md lines 4-10, no v28.0 is defined — the next milestone is not yet planned. Therefore write:

    ```markdown
    ## Current Milestone: TBD — pending roadmap planning

    **Status:** v27.0 complete (2026-04-13); next milestone pending roadmap planning. See `.planning/ROADMAP.md` for prior milestone history and `audit/FINDINGS-v27.0.md` for the most recent consolidated findings report.
    ```

    (c) **Insert the new `## Completed Milestone: v27.0 Call-Site Integrity Audit` block AFTER the new "Current Milestone" section and BEFORE the existing `## Completed Milestone: v26.0 Bonus Jackpot Split` section** (keeps most-recent-first ordering consistent with MILESTONES.md). MIGRATE the captured narrative from step (a) into this new block so no substantive content is lost. Structure:

    ```markdown
    ## Completed Milestone: v27.0 Call-Site Integrity Audit

    **Status:** Complete (2026-04-13)

    **Goal / Target scope / Incident context:** <paste the captured narrative from step (a) here, verbatim from the prior Current Milestone section; preserve paragraph breaks and any sub-headers from the original>

    **Result:** 4 phases (220-223), 9 plans. Phase 220 wired `scripts/check-delegatecall-alignment.sh` with 1:1 interface↔address mapping preflight (43 delegatecall sites verified ALIGNED). Phase 221 wired `scripts/check-raw-selectors.sh` with 5-pattern coverage and produced the 221-01-AUDIT.md catalog (5 JUSTIFIED INFO sites). Phase 222 classified all 308 external/public functions (19 COVERED / 177+1 CRITICAL_GAP / 112 EXEMPT after matrix refresh), shipped `scripts/coverage-check.sh` with three failure modes, and closed every CRITICAL_GAP via `test/fuzz/CoverageGap222.t.sol` (76 integration tests); Plan 222-03 strengthened test assertions and scoped drift detection to contract sections (commits ef83c5cd + e0a1aa3e). Phase 223 consolidated <FINDING_COUNT> INFO findings into `audit/FINDINGS-v27.0.md` with a full v25.0 regression appendix (all 13 prior findings verified). Zero exploitable vulnerabilities. All 14/14 requirements satisfied.
    ```

    Replace `<FINDING_COUNT>` with the concrete integer from Plan 223-01's output.

    The `177+1 CRITICAL_GAP` expression must appear EXACTLY ONCE in this Result block and MUST NOT be rewritten as the bare `178 CRITICAL_GAP` form — consistency with MILESTONES Edit 1 is a hard requirement.

    Do NOT delete or modify the existing `## Completed Milestone: v26.0 Bonus Jackpot Split` section or any earlier completed-milestone entries.

    **Narrative-preservation self-check before committing:** Diff the "substantive content" (everything except headers and stub Started/Target fields) between the pre-edit Current Milestone v27.0 section and the post-edit Completed Milestone v27.0 section. Any text that appeared in the pre-edit narrative MUST appear in the post-edit block, or the consolidator must record an explicit reason in 223-02-SUMMARY.md for dropping it. Default expectation: zero substantive content dropped.

    ---

    **Edit 3: `.planning/REQUIREMENTS.md` — flip checkboxes and traceability table.**

    (a) Phase 223 section — flip three checkboxes from `[ ]` to `[x]`:
    - Line 36: `- [x] **CSI-12**: audit/FINDINGS-v27.0.md is produced ...`
    - Line 37: `- [x] **CSI-13**: KNOWN-ISSUES.md is updated ...`
    - Line 38: `- [x] **CSI-14**: MILESTONES.md retrospective entry is written ...; PROJECT.md moves v27.0 to "Completed Milestone"; v27.0 marked SHIPPED`

    (b) Phase 222 section — per 222-VERIFICATION.md line 106 note ("CSI-08, CSI-09, and CSI-10 checkboxes remain `[ ]` (Pending) and the traceability table shows them as 'Pending' despite Phase 222 satisfying all three. Phase 223 should update CSI-08/09/10 to `[x]` / 'Complete' during findings consolidation"), flip:
    - Line 29: `- [x] **CSI-08**: The test suite compile error in test/fuzz/FuturepoolSkim.t.sol ... is fixed ...`
    - Line 30: `- [x] **CSI-09**: forge coverage --report summary runs to completion ...`
    - Line 31: `- [x] **CSI-10**: Every external/public function on a deployed contract is classified ...`

    (c) Traceability table — update status column for rows CSI-08, CSI-09, CSI-10, CSI-12, CSI-13, CSI-14 from `Pending` to `Complete`. These are at lines 74, 75, 76, 78, 79, 80 of the current REQUIREMENTS.md. CSI-11 (line 77) is already `Complete`; verify no regression.

    (d) Coverage summary line at the bottom (line 82) — should already read `**Coverage:** 14/14 requirements mapped to exactly one phase. No orphans. No duplicates.` No edit needed unless the state was out of sync.

    **Grep verification after edit:**
    - `grep -c '\[x\] \*\*CSI-0[89]\|\[x\] \*\*CSI-1[01234]' .planning/REQUIREMENTS.md` should return 14 (all 14 CSI-NN items complete).
    - `grep -c '\[ \] \*\*CSI-' .planning/REQUIREMENTS.md` should return 0.
    - `grep -c '| Pending |' .planning/REQUIREMENTS.md` should return 0.
    - `grep -c '| Complete |' .planning/REQUIREMENTS.md` should return 14.
  </action>
  <verify>
    <automated>
      # MILESTONES
      grep -c '^## v27.0 Call-Site Integrity Audit (Shipped: 2026-04-13)$' .planning/MILESTONES.md  # expect 1
      grep -c '^\*\*Phases completed:\*\* 4 phases' .planning/MILESTONES.md  # expect >= 1
      grep -c '^\*\*Key accomplishments:\*\*$' .planning/MILESTONES.md  # expect >= 2 (v26.0 + new v27.0)
      # Ensure v27.0 entry appears BEFORE v26.0 in the file
      awk '/^## v27\.0 /{print NR; exit}' .planning/MILESTONES.md  # capture line
      awk '/^## v26\.0 /{print NR; exit}' .planning/MILESTONES.md  # capture line
      # v27 line number must be less than v26 line number — verify manually at execution time

      # Count-wording consistency (B-3(b) / W-7): both MILESTONES and PROJECT must use `177+1 CRITICAL_GAP`, never the bare `178 CRITICAL_GAP`
      grep -c '177+1 CRITICAL_GAP' .planning/MILESTONES.md  # expect >= 1
      grep -c '177+1 CRITICAL_GAP' .planning/PROJECT.md  # expect >= 1
      ! grep -qE '(^| )178 CRITICAL_GAP' .planning/MILESTONES.md  # expect no match (bare 178 form prohibited)
      ! grep -qE '(^| )178 CRITICAL_GAP' .planning/PROJECT.md  # expect no match (bare 178 form prohibited)

      # PROJECT.md
      grep -c '^## Current Milestone: TBD' .planning/PROJECT.md  # expect 1 (or matches next milestone if ROADMAP has one defined)
      grep -c '^## Completed Milestone: v27.0 Call-Site Integrity Audit' .planning/PROJECT.md  # expect 1
      grep -c '^## Completed Milestone: v26.0 Bonus Jackpot Split' .planning/PROJECT.md  # expect 1 (unchanged)
      ! grep -q '^## Current Milestone: v27.0' .planning/PROJECT.md  # expect no match (v27.0 no longer current)
      # Narrative preservation check: the "Goal / Target scope / Incident context" label must appear in the new Completed Milestone v27.0 block
      grep -c '\*\*Goal / Target scope / Incident context:\*\*' .planning/PROJECT.md  # expect 1

      # FINDING_COUNT cross-validation (W-5): MILESTONES and PROJECT must agree on the F-27-NN count
      FINDING_COUNT=$(grep -c '^#### F-27-' audit/FINDINGS-v27.0.md)
      grep -qF "${FINDING_COUNT} INFO findings" .planning/MILESTONES.md || echo "MILESTONES count mismatch"
      grep -qF "${FINDING_COUNT}" .planning/PROJECT.md || echo "PROJECT count mismatch"
      # Both should be silent (no mismatch output)

      # REQUIREMENTS
      grep -c '\[x\] \*\*CSI-12\*\*' .planning/REQUIREMENTS.md  # expect 1
      grep -c '\[x\] \*\*CSI-13\*\*' .planning/REQUIREMENTS.md  # expect 1
      grep -c '\[x\] \*\*CSI-14\*\*' .planning/REQUIREMENTS.md  # expect 1
      grep -c '\[x\] \*\*CSI-08\*\*' .planning/REQUIREMENTS.md  # expect 1
      grep -c '\[x\] \*\*CSI-09\*\*' .planning/REQUIREMENTS.md  # expect 1
      grep -c '\[x\] \*\*CSI-10\*\*' .planning/REQUIREMENTS.md  # expect 1
      grep -c '\[ \] \*\*CSI-' .planning/REQUIREMENTS.md  # expect 0
      grep -c '| Pending |' .planning/REQUIREMENTS.md  # expect 0
      grep -c '| Complete |' .planning/REQUIREMENTS.md  # expect 14

      # No contract or test edits
      git diff --name-only contracts/ test/  # expect empty
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/MILESTONES.md` starts with (after the `# Milestones` header on line 1):
      1. The new `## v27.0 Call-Site Integrity Audit (Shipped: 2026-04-13)` block (with concrete integer values substituted for TASK_COUNT/FINDING_COUNT/KI_COUNT — no placeholder strings).
      2. Then the separator `---`.
      3. Then the existing `## v26.0 Bonus Jackpot Split (Shipped: 2026-04-12)` block (unchanged).
    - The new v27.0 entry contains `**Phases completed:** 4 phases, 9 plans` with a concrete task count.
    - The new v27.0 entry contains at minimum 6 bullets under "Key accomplishments:" covering: delegatecall gate, raw-selector gate, coverage matrix, coverage-check gate, CoverageGap222.t.sol tests, and FINDINGS-v27.0.md consolidation.
    - The MILESTONES entry uses `177+1 CRITICAL_GAP` (not the bare `178 CRITICAL_GAP` form).
    - `.planning/PROJECT.md` no longer contains `## Current Milestone: v27.0 Call-Site Integrity Audit`.
    - `.planning/PROJECT.md` contains exactly one `## Completed Milestone: v27.0 Call-Site Integrity Audit` section appearing before the existing `## Completed Milestone: v26.0 Bonus Jackpot Split` section.
    - The new `## Completed Milestone: v27.0` section contains a `**Goal / Target scope / Incident context:**` label with the captured narrative from the pre-edit `## Current Milestone: v27.0` section — substantive content from that pre-edit section is PRESERVED (not deleted).
    - The PROJECT Result block uses `177+1 CRITICAL_GAP` (not the bare `178 CRITICAL_GAP` form).
    - Both MILESTONES and PROJECT agree on the F-27-NN finding count (FINDING_COUNT cross-validation grep is silent).
    - `.planning/PROJECT.md` contains a `## Current Milestone:` section pointing at the next cycle or TBD.
    - `.planning/REQUIREMENTS.md` shows `[x]` for all six CSI-NN items flipped by this plan (CSI-08, CSI-09, CSI-10, CSI-12, CSI-13, CSI-14), plus the already-complete CSI-01..07 and CSI-11 unchanged.
    - `.planning/REQUIREMENTS.md` traceability table rows for CSI-08 through CSI-14 all show `Complete` status (no `Pending` remains for any CSI-NN).
    - `grep -c '\[ \] \*\*CSI-' .planning/REQUIREMENTS.md` returns 0.
    - No contract or test edits — `git diff --name-only contracts/ test/` returns empty.
  </acceptance_criteria>
  <done>
    MILESTONES.md has the v27.0 retrospective entry mirroring v26.0 format using the `177+1 CRITICAL_GAP` wording. PROJECT.md correctly reflects v27.0 as Completed Milestone with the goal/target-scope/incident-context narrative preserved (migrated from the pre-edit Current Milestone section), and Current Milestone is set to TBD pending roadmap planning. REQUIREMENTS.md shows all 14 CSI-NN checkboxes complete with traceability table flipped to match. CSI-14 (and CSI-08/09/10 checkbox-flip side-task) satisfied. Combined with Task 1's CSI-13 satisfaction and Plan 223-01's CSI-12 satisfaction, the v27.0 milestone is fully closed.
  </done>
</task>

</tasks>

<threat_model>
**Documentation-only plan — no executable surface; threat model N/A.**

This plan modifies exactly four markdown/documentation files (`KNOWN-ISSUES.md`, `.planning/MILESTONES.md`, `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`). It touches no contracts, no scripts, no tests, no deploy pipeline, no CI configuration, no dependencies, and no runtime code. The edits are additive (KNOWN-ISSUES gets new entries appended, MILESTONES gets a new block prepended, PROJECT shuffles one section above another while preserving its narrative, REQUIREMENTS flips checkbox states and status column values). None of these files feed into automated verification or runtime execution — they are consumed by human readers and by orchestration tooling (gsd-state) that only validates checkbox parity. No trust boundaries introduced, no input processed, no output executed. STRIDE analysis produces no applicable threats.

Per the phase planning-context block: "documentation-only; no executable surface; threat model N/A."
</threat_model>

<verification>
Phase-plan-level checks after both tasks complete:

1. All four file edits are committed as a single logical change:
   - `git log --oneline -1 KNOWN-ISSUES.md .planning/MILESTONES.md .planning/PROJECT.md .planning/REQUIREMENTS.md` returns a single commit sha from this plan.
2. No contract or test regressions: `git diff --name-only contracts/ test/` returns empty.
3. Sibling gates still green (no-op for this plan but verifies no inadvertent dependency break):
   - `make check-interfaces` exits 0
   - `make check-delegatecall` exits 0
   - `make check-raw-selectors` exits 0
4. FINDINGS-v27.0.md (produced by Plan 223-01) is byte-unchanged by this plan:
   - `git diff audit/FINDINGS-v27.0.md` returns empty.
5. Cross-file consistency:
   - The `<FINDING_COUNT>` integer used in MILESTONES matches the `#### F-27-` count in FINDINGS-v27.0.md.
   - The `<FINDING_COUNT>` integer used in PROJECT's v27.0 Completed Milestone section matches the MILESTONES entry (W-5 cross-validation grep).
   - The `177+1 CRITICAL_GAP` wording appears in BOTH MILESTONES and PROJECT; the bare `178 CRITICAL_GAP` form appears in NEITHER.
   - Every KNOWN-ISSUES new-entry `F-27-NN` reference exists in FINDINGS-v27.0.md.
6. Narrative preservation:
   - The pre-edit `## Current Milestone: v27.0` goal/target-scope/incident-context narrative is present in the post-edit `## Completed Milestone: v27.0` block (under the `**Goal / Target scope / Incident context:**` label). The 223-02-SUMMARY.md records zero substantive-content drops; if any drop occurred, SUMMARY records the explicit reason.
7. CSI checkbox state:
   - `grep -c '\[x\] \*\*CSI-' .planning/REQUIREMENTS.md` returns 14.
   - `grep -c '\[ \] \*\*CSI-' .planning/REQUIREMENTS.md` returns 0.
</verification>

<success_criteria>
- `KNOWN-ISSUES.md` contains new Design Decisions entries referencing F-27-NN IDs from `audit/FINDINGS-v27.0.md`; D-08 promotion criteria honored.
- `.planning/MILESTONES.md` has a v27.0 entry as the most-recent milestone, mirroring the v26.0 format with `**Phases completed:** 4 phases, N plans, M tasks` header and 6+ accomplishments bullets, using the `177+1 CRITICAL_GAP` wording.
- `.planning/PROJECT.md` moves v27.0 from "Current Milestone" to a new "Completed Milestone" section (appearing above the v26.0 completed-milestone section) with the pre-edit goal/target-scope/incident-context narrative MIGRATED into the new block (not deleted) under a `**Goal / Target scope / Incident context:**` label, plus a one-line result summary matching the MILESTONES entry and using the `177+1 CRITICAL_GAP` wording.
- `.planning/PROJECT.md` "Current Milestone" points at next-cycle or TBD per D-10.
- MILESTONES and PROJECT agree on the F-27-NN finding count (W-5 cross-validation).
- `.planning/REQUIREMENTS.md` has `[x]` for all 14 CSI-NN items (CSI-01 through CSI-14), and the traceability table's Status column shows `Complete` for every row.
- No contract or test edits — `git diff --name-only contracts/ test/` returns empty.
- All three CSI-NN requirements for this plan (CSI-13 and CSI-14) are satisfied by this plan's edits; CSI-12 satisfied by Plan 223-01's output.
</success_criteria>

<output>
After completion, create `.planning/phases/223-findings-consolidation/223-02-SUMMARY.md` following the standard GSD summary template, capturing:
- Count of new KNOWN-ISSUES.md entries added (integer) and their F-27-NN reference list
- MILESTONES.md insertion point line number (for audit trail)
- PROJECT.md section moves executed (Current Milestone content before/after; Completed Milestone order)
- **Narrative-preservation record:** confirm the pre-edit v27.0 goal/target-scope/incident-context narrative was migrated into the new `## Completed Milestone: v27.0` block; if any substantive content was dropped, record it here with the explicit reason
- **CONTEXT D-09 reconciliation note:** record that CONTEXT D-09's "Expected totals: 4 phases, 7 plans, ~15 tasks" is superseded by the actuals (4 phases, 9 plans, 23 tasks); this note is informational only
- **Count-wording verification:** confirm `177+1 CRITICAL_GAP` appears in both MILESTONES and PROJECT; confirm the bare `178 CRITICAL_GAP` form does not appear anywhere
- **FINDING_COUNT cross-validation:** confirm MILESTONES and PROJECT both cite the same `<FINDING_COUNT>` integer matching the `#### F-27-` count in FINDINGS-v27.0.md
- REQUIREMENTS.md checkbox flips (list of CSI-NN IDs changed)
- Commit sha for the single commit combining all four file edits
- Phase 223 close-out confirmation: all 14 CSI-NN items complete, all 4 ROADMAP success criteria verified, v27.0 milestone SHIPPED
- Handoff note: future cycle planning (v28.0 or equivalent) is unblocked; `.planning/ROADMAP.md` is the intake point for the next milestone
</output>
</content>
</invoke>