---
phase: 223-findings-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/FINDINGS-v27.0.md
autonomous: true
requirements: [CSI-12]
must_haves:
  truths:
    - "audit/FINDINGS-v27.0.md exists with every warning and info item from phases 220/221/222 severity-classified"
    - "FINDINGS-v27.0.md follows the FINDINGS-v25.0.md structure (Executive Summary severity table, per-phase finding subsections with field tables, regression appendix)"
    - "Resolved-in-cycle items (WR-221-01, WR-221-02, WR-222-02, WR-222-03, WR-222-04, VERIFICATION Gap 1, VERIFICATION Gap 2) carry Status: Resolved in v27.0 with resolving commit sha"
    - "Regression Appendix verifies every v25.0 finding (F-25-01..F-25-13) against the current code with HOLDS / SUPERSEDED / FIXED / INVALIDATED tag and rationale"
    - "v26.0 regression gap is noted explicitly in the appendix preamble (no separate v26.0 FINDINGS doc exists)"
    - "The verbatim D-07 scope-framing sentence appears in the preamble"
  artifacts:
    - path: "audit/FINDINGS-v27.0.md"
      provides: "Severity-classified consolidated findings for v27.0 with full v25.0 regression appendix"
      contains: "F-27-01 through F-27-15 (target ~15 after de-dup); Regression Appendix F-25-01 through F-25-13"
  key_links:
    - from: "audit/FINDINGS-v27.0.md Phase 220 subsection"
      to: ".planning/phases/220-delegatecall-target-alignment/220-REVIEW.md items WR-220-01/02/03 + IN-220-01/02/03/04/05"
      via: "per-finding Source field citing file:line + finding type"
      pattern: "WR-220-0[1-3]|IN-220-0[1-5]"
    - from: "audit/FINDINGS-v27.0.md Phase 221 subsection"
      to: ".planning/phases/221-raw-selector-calldata-audit/221-REVIEW.md items WR-221-01/02 + IN-221-01/02/03"
      via: "per-finding Source field; resolved items cite commit sha"
      pattern: "WR-221-0[12]|IN-221-0[123]"
    - from: "audit/FINDINGS-v27.0.md Phase 222 subsection"
      to: ".planning/phases/222-external-function-coverage-gap/222-REVIEW.md items WR-222-01/02/03/04 + IN-222-01..06 + VERIFICATION.md Gap 1 + Gap 2"
      via: "per-finding Source field; resolved items cite commits ef83c5cd + e0a1aa3e"
      pattern: "WR-222-0[1-4]|IN-222-0[1-6]|Gap [12]"
    - from: "audit/FINDINGS-v27.0.md Regression Appendix"
      to: "audit/FINDINGS-v25.0.md F-25-01..F-25-13"
      via: "per-finding HOLDS/SUPERSEDED/FIXED/INVALIDATED tag + rationale referencing current code state"
      pattern: "F-25-0[1-9]|F-25-1[0-3]"
---

<objective>
Produce `audit/FINDINGS-v27.0.md` — the consolidated severity-classified findings document for the v27.0 Call-Site Integrity Audit milestone — by rolling up every warning and info item from the three source-phase REVIEW.md files (Phase 220, 221, 222) plus the two resolved Phase 222 VERIFICATION gaps, into ~15 F-27-NN findings following the v25.0 structure, and by regression-verifying all 13 v25.0 findings against the current codebase.

Purpose: Satisfies CSI-12. Downstream Plan 223-02 references specific F-27-NN IDs when promoting KNOWN-ISSUES entries and writing the MILESTONES retrospective, so this plan must exist first.

Output: Single new file `audit/FINDINGS-v27.0.md` (~400-550 lines, mirroring `audit/FINDINGS-v25.0.md` in structure). No other file touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/223-findings-consolidation/223-CONTEXT.md

# Structure template — mirror this exactly
@audit/FINDINGS-v25.0.md

# Source findings (primary inputs)
@.planning/phases/220-delegatecall-target-alignment/220-REVIEW.md
@.planning/phases/221-raw-selector-calldata-audit/221-REVIEW.md
@.planning/phases/222-external-function-coverage-gap/222-REVIEW.md
@.planning/phases/222-external-function-coverage-gap/222-VERIFICATION.md

# Source commit shas for resolved-in-cycle items
# WR-221-01 and WR-221-02 resolved in 221-01 gate work (Phase 221 commits: a1ed4ed2, 839e0f43, 8c7e0a79, 80e3b1c5, a2f855ff, f115a402)
# WR-222-02, WR-222-03, WR-222-04, VERIFICATION Gap 1, VERIFICATION Gap 2 resolved by Plan 222-03 at commits ef83c5cd (Gap 1) and e0a1aa3e (Gap 2)

# For regression appendix — spot-check that the modules referenced by F-25-NN still exist and the observed patterns still apply
@contracts/modules/DegenerusGameAdvanceModule.sol
@contracts/modules/DegenerusGameJackpotModule.sol
@contracts/modules/DegenerusGameMintModule.sol
@contracts/modules/DegenerusGameOverModule.sol
@contracts/modules/DegenerusGameDegeneretteModule.sol
@contracts/DegenerusGameStorage.sol
@contracts/StakedDegenerusStonk.sol
@contracts/DegenerusGame.sol
</context>

<interfaces>
<!-- FINDINGS-v25.0.md field-table schema (from audit/FINDINGS-v25.0.md lines 31-50, 122-144, 168-228). -->
<!-- Every F-27-NN finding MUST use this exact field set (case-matched to v25.0). -->

Per-finding field table:
```
| Field | Value |
|-------|-------|
| **Severity** | INFO | LOW | MEDIUM | HIGH | CRITICAL   (default INFO per D-04; LOW requires justification) |
| **Source** | Phase <N> (<plan-or-review>), originally <WR-220-01 / IN-221-03 / VERIFICATION Gap 1 / etc.> |
| **Contract** | <ContractOrScriptName> | Multiple | N/A (documentation) |
| **Function** | `functionName` | <file section / line range> | N/A |
```

After the field table, prose paragraph describing the observation.

Then:
```
**Severity justification:** <why the severity class applies; for LOW explain the escalation reason; for INFO explain why no exploitable risk exists>.
```

For resolved-in-cycle items, add:
```
**Status:** Resolved in v27.0 (commit <sha>).
```
Followed by a 1-3 sentence explanation of the fix and (for VERIFICATION gaps) the re-verification outcome (4/4 must-haves).

Regression Appendix entry format (per v25.0 precedent at lines 279-331):
```
| Finding | Contract | Status | Evidence |
|---------|----------|--------|----------|
| F-25-<NN> | <Contract>   | HOLDS | SUPERSEDED | FIXED | INVALIDATED | <one-line rationale with code pointer e.g. AdvanceModule L435 or "no longer exists"> |
```

Status tag semantics (from CONTEXT D-05):
- **HOLDS** — still applies as-is (underlying code unchanged or semantically equivalent)
- **SUPERSEDED** — code path restructured but the conclusion stands
- **FIXED** — code change made the finding moot
- **INVALIDATED** — changed circumstances invalidate the INFO-level observation
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author audit/FINDINGS-v27.0.md — preamble + executive summary + ~15 F-27-NN findings grouped by source phase</name>
  <files>audit/FINDINGS-v27.0.md</files>
  <read_first>
    - audit/FINDINGS-v25.0.md (FULL — this IS the structure template; mirror section headings, field-table schema, prose tone, Audit Trail table format)
    - .planning/phases/220-delegatecall-target-alignment/220-REVIEW.md (full; extract WR-220-01/02/03 + IN-220-01/02/03/04/05 — 8 items)
    - .planning/phases/221-raw-selector-calldata-audit/221-REVIEW.md (full; extract WR-221-01/02 + IN-221-01/02/03 — 5 items; WR-221-01/02 already have "RESOLVED" status blocks — preserve their resolution narrative)
    - .planning/phases/222-external-function-coverage-gap/222-REVIEW.md (full; extract WR-222-01/02/03/04 + IN-222-01/02/03/04/05/06 — 10 items)
    - .planning/phases/222-external-function-coverage-gap/222-VERIFICATION.md (full; extract Gap 1 + Gap 2 blocks — both have status: resolved + resolved_by: 222-03-PLAN + resolution_commits)
    - .planning/phases/223-findings-consolidation/223-CONTEXT.md (for locked decisions D-01..D-07 and the exact verbatim D-07 scope-framing sentence)
  </read_first>
  <action>
    Create `audit/FINDINGS-v27.0.md` (new file) with the following structure. Every section referenced against `audit/FINDINGS-v25.0.md` means "mirror that file's structure verbatim, substituting v27.0 content." Do NOT copy v25.0 finding text; only mirror the section scaffolding and field-table schema.

    **F-27-NN assignment rule (stable cross-plan IDs):**

    Phase 220 uses F-27-01..F-27-0N sequential (one per consolidated source item). Phase 221 continues from the next available F-27-NN. Phase 222 continues from the next available F-27-NN. Record the final F-27-NN → source-item mapping as a table in `223-01-SUMMARY.md` so Plan 223-02 can reference stable IDs. The "F-27-XX" placeholders in the expected-finding lists below MUST be replaced with concrete sequential integers at authoring time; the exact integers assigned to Phase 221 and Phase 222 depend on Phase 220's final count (after the Phase 220 consolidation-guidance fold), but the allocation is STRICTLY sequential — no gaps, no reservations, Phase 220 first, then 221, then 222.

    **1. Header (mirror v25.0 lines 1-8):**
    - Title: `# Degenerus Protocol -- Delta Findings Report (v27.0 Call-Site Integrity Audit)`
    - `**Audit Date:** 2026-04-13`
    - `**Methodology:**` sentence describing the three-phase audit: delegatecall target alignment (Phase 220), raw selector and hand-rolled calldata safety (Phase 221), external function classification coverage (Phase 222). Note that all three phases were executed by Claude (claude-opus-4-6) with structured reasoning, code review, and verification passes.
    - `**Scope:**` sentence — use the **verbatim D-07 scope-framing sentence** from 223-CONTEXT.md D-07:
      > "Call-site integrity audit covering three axes — delegatecall target alignment (Phase 220), raw selector and hand-rolled calldata safety (Phase 221), external function classification coverage (Phase 222). Scope: post-v26.0 delta; the v25.0 Master Delta Report (`audit/FINDINGS-v25.0.md`) and the v5.0 baseline (`audit/FINDINGS.md`) remain prior references."
      Emit this sentence byte-identical; a grep for the opening "Call-site integrity audit covering three axes" must match.
    - `**Contracts in scope:**` list — same as v25.0 scope (DegenerusGame, modules, BurnieCoin, BurnieCoinflip, DegenerusAffiliate, DegenerusJackpots, DegenerusQuests, StakedDegenerusStonk, DegenerusVault, DegenerusStonk, DeityBoonViewer, Icons32Data, GNRUS, WWXRP) PLUS the v27.0-specific tooling scope: `scripts/check-delegatecall-alignment.sh`, `scripts/check-raw-selectors.sh`, `scripts/coverage-check.sh`, `scripts/lib/patchContractAddresses.js`, `Makefile`, `test/fuzz/FuturepoolSkim.t.sol`, `test/fuzz/CoverageGap222.t.sol`, `contracts/ContractAddresses.sol`, `contracts/interfaces/IDegenerusGameModules.sol`.

    **2. Executive Summary (mirror v25.0 lines 10-23):**
    - Severity table with columns `Severity | Count` — rows CRITICAL (0), HIGH (0), MEDIUM (0), LOW (0 or 1 if an escalation is justified; default 0 per D-04), INFO (remainder; target ~14-15), **Total** (~15).
    - "Overall Assessment:" paragraph — note that zero exploitable vulnerabilities were found; all findings are informational observations on tooling robustness, test quality, or script style; several were resolved in-cycle. Mention the phase-level verdicts (220 passed 9/9, 221 passed 13/13, 222 re-verified 4/4 after Plan 222-03 gap closure).
    - One-sentence note that this report is a delta supplement to `audit/FINDINGS-v25.0.md`; the regression appendix re-verifies v25.0 findings. Note that v26.0 did not produce a separate FINDINGS doc (its 2 phases were design-focused; details in `.planning/MILESTONES.md`).

    **3. Findings section (mirror v25.0 line 27 heading, grouped by source phase per D-02):**

    Use numeric monotonic IDs `F-27-NN` starting at F-27-01. Grouping is by source phase (D-01 + D-02). Within each phase subsection, items are ordered by source severity-then-file-line (warnings first, then info items, within each group ordered by file:line).

    **De-duplication rule (from D-02):** Where one underlying issue appears in two REVIEW sources (e.g., WR-222-02 "reachability-only tests" and 222-VERIFICATION.md Gap 1 "CSI-11 test quality"), consolidate into one F-27-NN finding with both sources cited. Same for WR-222-03 + VERIFICATION Gap 2 (both are the drift-scoping issue). WR-222-04 (tautological ticketsOwedView) is a subordinate observation of the Gap 1 / WR-222-02 test-quality cluster but is a distinct line-level bug; either (a) fold it as a sub-point inside the Gap-1/WR-222-02 finding, OR (b) keep it as a standalone finding — preserve its explicit mention of the `uint32 >= 0` tautology and its resolution in commit ef83c5cd. Pick the option that makes the count land at 14-15 findings total. The final count MUST be a concrete integer; do not write "~14-15" in the produced file.

    **Folded-finding severity-justification handling (for IN-222-04/05/06 and similar multi-item folds):** For folded findings with multiple sub-items, the severity-justification paragraph must either address each sub-item's reasoning (paragraph form) OR each sub-item gets its own inline one-sentence severity-justification in the sub-list.

    **Expected finding IDs (final count in the range 14-16; consolidator picks the exact integer after de-dup):**

    ### Phase 220: Delegatecall Target Alignment (N findings)

    - F-27-01: Trailing slash on `CONTRACTS_DIR` silently disables interfaces/ and mocks/ filters
        - Source: Phase 220 (220-REVIEW.md WR-220-01)
        - Contract: `scripts/check-delegatecall-alignment.sh:163,166`
        - Severity: INFO (gate robustness gap; no impact on default `make check-delegatecall` or CI; affects only fixture-based self-tests with a trailing slash)
    - F-27-02: Mapping preflight scans only `IDegenerusGameModules.sol`
        - Source: Phase 220 (220-REVIEW.md WR-220-02)
        - Contract: `scripts/check-delegatecall-alignment.sh:90,95`
        - Severity: INFO (would silently stop seeing a module interface if one is moved to a new file; current 9 interfaces all live in the single file so no present impact)
    - F-27-03: 10-line preceding window for target-address detection is fragile
        - Source: Phase 220 (220-REVIEW.md WR-220-03)
        - Contract: `scripts/check-delegatecall-alignment.sh:212,219-220`
        - Severity: INFO (future refactors that split delegatecall args across >10 lines could mis-report as orphan-selector WARN; every current site fits within window)
    - F-27-04: `self_test_transform()` duplicates `validate_mapping()` with a hard-coded interface list
        - Source: Phase 220 (220-REVIEW.md IN-220-01)
        - Severity: INFO (redundant code; no behavioral risk since `validate_mapping` is the stronger check)
    - F-27-05: Parallel-make race on `ContractAddresses.sol` between `test-foundry` and `test-hardhat` branches
        - Source: Phase 220 (220-REVIEW.md IN-220-03)
        - Contract: `Makefile:44` (pre-existing; Phase 220 adds `check-delegatecall` as another prereq to both sub-targets)
        - Severity: INFO (deterministic only under `make -j`; default `make test` is serial; documented as foot-gun to flag before the dependency list grows further)

    **Consolidation guidance for Phase 220:**
    IN-220-02 (`grep -c . || true` subshell masking), IN-220-04 (bare FAIL error message wording), IN-220-05 (`GAME_*_MODULE` regex captures comment tokens) — fold into a single F-27-NN "Phase 220 gate script — minor robustness notes" finding listing all three as sub-items, OR keep them as separate rows if doing so keeps the total count at/below 16. Prefer fold (one F-27 ID with 3 sub-bullets) to keep the finding count focused on distinct classes of risk. If folded, apply the "folded-finding severity-justification handling" rule above (paragraph-form justification OR per-sub-item inline justification).

    ### Phase 221: Raw Selector & Calldata Audit (N findings)

    - F-27-XX: Non-existent `CONTRACTS_DIR` silently passes — **Status: Resolved in v27.0 (commit `8c7e0a79` — Phase 221 gate fix; one of the 6 Phase 221 commits; consolidator picks the exact sha from the Phase 221 git log or references 221-REVIEW.md's RESOLVED annotation which states the fix is at `scripts/check-raw-selectors.sh:29-32`)**
        - Source: Phase 221 (221-REVIEW.md WR-221-01 — already marked RESOLVED)
        - Contract: `scripts/check-raw-selectors.sh`
        - Severity: INFO (robustness gap; affected only fixture-based gate self-tests; did not affect real CI invocations before fix)
    - F-27-XX: `warn_total` declared and tested but never incremented — **Status: Resolved in v27.0 (commit as per 221-REVIEW.md WR-221-02 resolution — dead variable removed)**
        - Source: Phase 221 (221-REVIEW.md WR-221-02 — already marked RESOLVED)
        - Severity: INFO (dead variable; no current behavioral impact; inconsistent with sibling `check-delegatecall-alignment.sh`)
    - F-27-XX: Pattern D comment contains historical phase reference
        - Source: Phase 221 (221-REVIEW.md IN-221-01)
        - Contract: `scripts/check-raw-selectors.sh:122-124`
        - Severity: INFO (violates `feedback_no_history_in_comments` project convention; cosmetic)
    - F-27-XX: `grep --exclude-dir` basename-vs-full-path asymmetry between Patterns A-D and Pattern E
        - Source: Phase 221 (221-REVIEW.md IN-221-02)
        - Severity: INFO (latent — current repo layout puts `mocks`/`interfaces` only as direct children of `contracts/`; future nested directories with those names would be silently excluded by A-D but correctly retained by E)
    - F-27-XX: Pattern E awk emits opener line number not `abi.encode*` payload line
        - Source: Phase 221 (221-REVIEW.md IN-221-03)
        - Contract: `scripts/check-raw-selectors.sh:132-178`
        - Severity: INFO (cosmetic navigation friction; DegenerusAdmin.sol:911 reported instead of :914)

    **Consolidation guidance for Phase 221:** 5 items map to 5 findings (no dedup candidates). For the two RESOLVED items, use the commit sha from 221-REVIEW.md's RESOLVED annotations; if 221-REVIEW does not name a specific commit, reference the resolution verbatim ("guard added at scripts/check-raw-selectors.sh:29-32" for WR-221-01; "Option A: remove the tier — dead variable + warn-path line all removed" for WR-221-02) and cite "Phase 221 commits" without a single sha if a single sha is not extractable. The Resolved-in-cycle narrative is more important than a specific sha when the REVIEW block already documents the state change.

    ### Phase 222: External Function Coverage Gap (N findings)

    - F-27-XX: `patchContractAddresses.js` VRF_KEY_HASH regex fails on multi-line format
        - Source: Phase 222 (222-REVIEW.md WR-222-01)
        - Contract: `scripts/lib/patchContractAddresses.js:59-62`
        - Severity: INFO (candidate LOW — D-04 escalation justification: silent deploy-time failure leaves dummy 0xabab... key hash in place; deployment-pipeline impact, not runtime; consolidator decides INFO vs LOW based on whether a failed VRF subscription deploy counts as "real bug risk" per D-04. Default INFO; escalate to LOW only if the consolidator concludes the deployment-tooling impact qualifies as concrete production impact.)
    - F-27-XX: CoverageGap222.t.sol reachability-only assertions (62 of 76 tests) — **Status: Resolved in v27.0 (commit `ef83c5cd` — 222-03 gap closure; CSI-11 VERIFICATION Gap 1 re-verified 4/4 at 2026-04-12)**
        - Source: Phase 222 (222-REVIEW.md WR-222-02 + 222-VERIFICATION.md Gap 1 — consolidated per D-02 dedup rule since both record the same observation)
        - Contract: `test/fuzz/CoverageGap222.t.sol`
        - Severity: INFO (test quality — regression safety gap; Phase 222 phase-level goal still satisfied because forge coverage registered branch hits; resolved in-cycle via 62 assertion rewrites to `assertFalse(ok, ...)` / `assertTrue(oN, ...)` / `assertGt(after, before, ...)` patterns)
    - F-27-XX: `test_gap_lifecycle_purchase_then_advanceGame` uint32 >= 0 tautological assertion — **Status: Resolved in v27.0 (commit `ef83c5cd`)**
        - Source: Phase 222 (222-REVIEW.md WR-222-04)
        - Contract: `test/fuzz/CoverageGap222.t.sol:60-63`
        - Severity: INFO (vacuous assertion; resolved alongside WR-222-02 in the same commit with pre/post snapshot + assertGt pattern)
        - **Consolidation option:** fold into the preceding F-27-XX (WR-222-02 / Gap 1) as a sub-point if final count is too high. If kept standalone, link to the parent finding explicitly. If folded, apply the "folded-finding severity-justification handling" rule.
    - F-27-XX: `coverage-check.sh` drift mode not contract-scoped — **Status: Resolved in v27.0 (commit `e0a1aa3e` — 222-03 gap closure; CSI-10 VERIFICATION Gap 2 re-verified 4/4 at 2026-04-12; also surfaced a real matrix drift for `emitDailyWinningTraits` which was fixed in the same commit)**
        - Source: Phase 222 (222-REVIEW.md WR-222-03 + 222-VERIFICATION.md Gap 2 — consolidated per D-02 dedup rule)
        - Contract: `scripts/coverage-check.sh`
        - Severity: INFO (gate weakness — drift check could be masked by same-named function in another contract; resolved in-cycle via preflight parser populating per-section function sets + `;fn;` membership test)
    - F-27-XX: `payDailyCoinJackpot` direct delegatecall — gate-enhancement suggestion
        - Source: Phase 222 (222-REVIEW.md IN-222-01)
        - Contract: `contracts/modules/DegenerusGameAdvanceModule.sol:857-870`
        - Severity: INFO (no current bug; forward-looking enhancement to `check-delegatecall-alignment.sh` to flag any module function with `OnlyGame()` called via direct delegatecall rather than through a GAME wrapper)
    - F-27-XX: `_emitDailyWinningTraits` docstring conflates wrapper and module guard checks
        - Source: Phase 222 (222-REVIEW.md IN-222-02)
        - Contract: `contracts/modules/DegenerusGameAdvanceModule.sol:872-875`
        - Severity: INFO (clarity issue; both statements in the docstring are accurate but the flow is conflated; cosmetic)
    - F-27-XX: `FuturepoolSkim.t.sol` header comments contain historical context
        - Source: Phase 222 (222-REVIEW.md IN-222-03)
        - Contract: `test/fuzz/FuturepoolSkim.t.sol:7-27`
        - Severity: INFO (violates `feedback_no_history_in_comments` project convention; includes `v20.0`, `commit d8dbd9e3`, `D-01/D-02/D-03` references — D-01/D-02/D-03 retained as load-bearing matrix cross-reference per reviewer note, remainder are history)

    **Consolidation guidance for Phase 222:**
    IN-222-04 (coverage-check LCOV missing = WARN not FAIL), IN-222-05 (Makefile coverage-check no freshness check), IN-222-06 (bash sed/awk style — backtick portability) — fold into one F-27-NN "Phase 222 coverage-gate minor robustness notes" finding listing the three as sub-items. All are non-blocking, non-correctness observations on the gate script. Apply the "folded-finding severity-justification handling" rule (paragraph-form justification OR per-sub-item inline justification).

    **4. Summary Statistics section (mirror v25.0 lines 230-264):**
    - "By Severity" table
    - "By Source Phase" table — 3 rows (Phase 220, Phase 221, Phase 222) with finding-ID ranges
    - "By Contract/Script" table — group findings by their primary target file (e.g., `scripts/check-delegatecall-alignment.sh`, `scripts/check-raw-selectors.sh`, `scripts/coverage-check.sh`, `scripts/lib/patchContractAddresses.js`, `test/fuzz/CoverageGap222.t.sol`, `test/fuzz/FuturepoolSkim.t.sol`, `contracts/modules/DegenerusGameAdvanceModule.sol`, `Makefile`)

    **5. Audit Trail section (mirror v25.0 lines 266-276):**
    Table with columns `Phase | Scope | Plans | Findings | Verdict`. Rows:
    - Phase 220 | Delegatecall target alignment: 43-site per-call audit + static-analysis gate wiring + 1:1 interface/address mapping preflight | 2 | 8 raw (3 WR + 5 IN) / N consolidated INFO | SOUND
    - Phase 221 | Raw selector & calldata audit: 5-pattern static-analysis gate (CSI-04/05/06) + findings catalog with 5 JUSTIFIED INFO sites | 2 | 5 raw (2 WR + 3 IN) / N consolidated INFO (2 resolved in-cycle) | SOUND
    - Phase 222 | External-function coverage gap: FuturepoolSkim.t.sol compile fix + 308-function classification matrix + CoverageGap222.t.sol (76 tests) + coverage-check.sh gate + Plan 222-03 quality closure | 3 | 10 raw (4 WR + 6 IN) + 2 VERIFICATION gaps / N consolidated INFO (4 resolved in-cycle) | SOUND
    - **Total** | **3-phase call-site-integrity delta audit (v26.0-v27.0)** | **7** | **~15 INFO** | **SAFE**

    **6. Regression Appendix (mirror v25.0 lines 278-349 but adapted per D-05 and D-06):**

    Section heading: `## Regression Appendix — v25.0 Findings`

    Preamble (2-3 sentences):
    > "Regression verification of all 13 INFO findings from the v25.0 Master Delta Findings Report (`audit/FINDINGS-v25.0.md`). No v26.0 FINDINGS document exists (the v26.0 milestone was design-focused — bonus jackpot split — and did not produce a formal findings doc; accomplishments are captured in `.planning/MILESTONES.md`), so no v26.0 findings are included in this appendix. Each v25.0 finding is checked against the current contract source with code-level evidence."
    (This satisfies D-05 and explicitly addresses D-06.)

    Then a status-key block (mirror v25.0 lines 283-287 verbatim):
    ```
    **Status key:**
    - **HOLDS** — still applies as-is (underlying code unchanged or semantically equivalent)
    - **SUPERSEDED** — code path restructured but the conclusion stands
    - **FIXED** — code change made the finding moot
    - **INVALIDATED** — changed circumstances invalidate the INFO-level observation
    ```

    Then a table `| Finding | Contract | Status | Evidence |` with 13 rows for F-25-01 through F-25-13. For each F-25-NN:
    - Read the v25.0 entry (audit/FINDINGS-v25.0.md) for the contract + function + observation
    - Verify against current code in the @context files above
    - Assign HOLDS (default — most likely outcome since v26.0 was design-focused and v27.0 was call-site-integrity tooling, very few contract edits landed)
    - Write a one-line evidence cell with code pointer (file + line or function name)

    **Expected disposition distribution (based on 222-01-SUMMARY.md's note that only commit e4064d67 was a contract edit in v27.0, affecting `_emitDailyWinningTraits` self-call routing):**
    - Most F-25-NN → HOLDS (code unchanged)
    - F-25-06 (Auto-Rebuy Overwrite by Design in `_consolidatePoolsAndRewardJackpots`) — verify AdvanceModule still uses memory-batch pattern; expected HOLDS
    - F-25-07 (rngLockedFlag asymmetry) — expected HOLDS (documented design decision already in KNOWN-ISSUES.md)
    - F-25-08 (Gameover prevrandao fallback) — expected HOLDS
    - F-25-09 (Deity Boon deterministic fallback) — expected HOLDS
    - F-25-10..F-25-13 (pool/surplus/BPS/uint128 observations) — expected HOLDS (no v27.0 edits to these sites)

    If any F-25-NN verifies as SUPERSEDED, FIXED, or INVALIDATED, the consolidator must provide specific code evidence (e.g., "Function deleted in commit `<sha>`" or "`_consolidatePoolsAndRewardJackpots` restructured; memory-batch pattern now at L627-795").

    **No milestone-finding appendix.** v25.0's report included a separate "Regression Check: Milestone Findings" table for F-185-01 HIGH and F-187-01 INFO — the v27.0 cycle produced zero milestone-level findings (no HIGH/MEDIUM), so this sub-section is OMITTED (note the omission explicitly in one sentence after the F-25 table: "No milestone-level findings were produced during the v26.0-v27.0 cycle; the milestone-findings sub-section from v25.0 is therefore omitted.").

    **Regression Summary subsection (mirror v25.0 lines 334-349):**
    - "Total items checked:" count
    - Status breakdown table with HOLDS / SUPERSEDED / FIXED / INVALIDATED rows
    - Verdict sentence: "No regressions detected. All 13 prior v25.0 findings remain in their documented state."

    **FINAL FILE STRUCTURE CHECK before writing:** The completed file must contain, in order:
    1. Header (title + audit date + methodology + scope + contracts-in-scope)
    2. Executive Summary (severity table + overall-assessment paragraph + delta-supplement note)
    3. `## Findings` section with 3 phase subsections (`### Phase 220:`, `### Phase 221:`, `### Phase 222:`)
    4. `## Summary Statistics` section (by severity / by source phase / by contract)
    5. `## Audit Trail` section (phase-row verdict table)
    6. `## Regression Appendix — v25.0 Findings` section (preamble + status key + F-25 table + regression summary)

    File size expected: ~400-550 lines. No `(none)` placeholders. Every F-27-NN has all four field-table rows populated. Every resolved-in-cycle finding has the `**Status:** Resolved in v27.0 (commit <sha>)` line.

    **F-27-NN → source-item mapping table in SUMMARY:** Before committing, record in `223-01-SUMMARY.md` a table with one row per F-27-NN containing columns `F-27-NN | Title | Source Item(s) | Phase | Status`. Every source item from 220-REVIEW / 221-REVIEW / 222-REVIEW / 222-VERIFICATION MUST appear at least once in the "Source Item(s)" column. This table is the authoritative cross-reference Plan 223-02 uses for KNOWN-ISSUES and MILESTONES.
  </action>
  <verify>
    <automated>
      test -f audit/FINDINGS-v27.0.md
      grep -c '^# Degenerus Protocol -- Delta Findings Report (v27.0' audit/FINDINGS-v27.0.md  # expect 1
      grep -c '^## Executive Summary' audit/FINDINGS-v27.0.md  # expect 1
      grep -c '^## Findings' audit/FINDINGS-v27.0.md  # expect 1
      grep -c '^### Phase 220:' audit/FINDINGS-v27.0.md  # expect 1
      grep -c '^### Phase 221:' audit/FINDINGS-v27.0.md  # expect 1
      grep -c '^### Phase 222:' audit/FINDINGS-v27.0.md  # expect 1
      grep -c '^## Summary Statistics' audit/FINDINGS-v27.0.md  # expect 1
      grep -c '^## Audit Trail' audit/FINDINGS-v27.0.md  # expect 1
      grep -c '^## Regression Appendix' audit/FINDINGS-v27.0.md  # expect 1
      grep -cE '^#### F-27-0[1-9]:|^#### F-27-1[0-6]:' audit/FINDINGS-v27.0.md  # expect 14-16
      grep -c '\*\*Status:\*\* Resolved in v27.0' audit/FINDINGS-v27.0.md  # expect >= 5 (WR-221-01, WR-221-02, WR-222-02+Gap1, WR-222-03+Gap2, WR-222-04; WR-222-04 may appear as a nested sub-point if folded — see per-ID presence check below)
      # Each of the five Resolved source items MUST be present somewhere in the file (either as a standalone F-27-NN with Resolved status, or as an explicit sub-point with its own Resolved line):
      for id in WR-221-01 WR-221-02 WR-222-02 WR-222-03 WR-222-04; do grep -q "$id" audit/FINDINGS-v27.0.md || echo "MISSING: $id"; done  # expect empty output (silent pass)
      # And each of those IDs must co-occur with a Resolved marker somewhere in the file (loose proximity check):
      grep -E 'WR-221-01.*Resolved|Resolved.*WR-221-01' audit/FINDINGS-v27.0.md | wc -l  # expect >= 1
      grep -E 'WR-221-02.*Resolved|Resolved.*WR-221-02' audit/FINDINGS-v27.0.md | wc -l  # expect >= 1
      grep -E 'WR-222-02.*Resolved|Resolved.*WR-222-02' audit/FINDINGS-v27.0.md | wc -l  # expect >= 1
      grep -E 'WR-222-03.*Resolved|Resolved.*WR-222-03' audit/FINDINGS-v27.0.md | wc -l  # expect >= 1
      grep -E 'WR-222-04.*Resolved|Resolved.*WR-222-04' audit/FINDINGS-v27.0.md | wc -l  # expect >= 1
      grep -E 'ef83c5cd|e0a1aa3e' audit/FINDINGS-v27.0.md | wc -l  # expect >= 2 (both 222-03 resolution shas must appear)
      grep -c 'Call-site integrity audit covering three axes' audit/FINDINGS-v27.0.md  # expect 1 (verbatim D-07 scope-framing sentence)
      grep -c 'F-25-' audit/FINDINGS-v27.0.md  # expect >= 13 (all v25.0 findings in regression appendix)
      grep -cE 'F-25-0[1-9]|F-25-1[0-3]' audit/FINDINGS-v27.0.md  # expect 13 distinct v25.0 finding IDs
      grep -cE 'HOLDS|SUPERSEDED|FIXED|INVALIDATED' audit/FINDINGS-v27.0.md  # expect >= 13 (one tag per F-25 row + 4 in status key block)
      wc -l audit/FINDINGS-v27.0.md  # expect 400-600
      git diff --name-only contracts/ test/  # expect empty (no contract/test edits)
    </automated>
  </verify>
  <acceptance_criteria>
    - `audit/FINDINGS-v27.0.md` exists and is 400-600 lines.
    - Header section present with title exactly starting `# Degenerus Protocol -- Delta Findings Report (v27.0 Call-Site Integrity Audit)`.
    - Preamble contains the verbatim D-07 scope-framing sentence (grep for "Call-site integrity audit covering three axes" matches exactly once).
    - Executive Summary severity table contains rows for CRITICAL, HIGH, MEDIUM, LOW, INFO, and Total; CRITICAL/HIGH/MEDIUM counts are 0; LOW is 0 or 1; INFO is >= 13 and <= 16; Total matches INFO + LOW.
    - Three `### Phase 220:`, `### Phase 221:`, `### Phase 222:` subsections each contain >= 2 finding blocks.
    - Finding count `#### F-27-NN:` is >= 14 and <= 16.
    - F-27-NN IDs are sequentially assigned starting at F-27-01, grouped by phase (220 → 221 → 222); no gaps or out-of-order IDs.
    - Each finding has a `| Field | Value |` table with rows for `**Severity**`, `**Source**`, `**Contract**`, and `**Function**`.
    - Each finding has a `**Severity justification:**` paragraph (or, for folded findings, per-sub-item inline severity-justifications per the authoring rule).
    - At least 5 findings/sub-points carry `**Status:** Resolved in v27.0 (commit <sha>)` — specifically the two RESOLVED Phase 221 items (WR-221-01, WR-221-02) + the two Phase 222 VERIFICATION gaps (WR-222-02+Gap1, WR-222-03+Gap2) + WR-222-04 (may appear as its own block or as a nested sub-point with its own Resolved line).
    - Each of WR-221-01, WR-221-02, WR-222-02, WR-222-03, WR-222-04 is literally present in the file AND co-occurs with a "Resolved" marker (either on the same line or within the same finding block).
    - Both resolution shas `ef83c5cd` and `e0a1aa3e` appear verbatim in the file (grep for each returns >= 1 line).
    - `## Regression Appendix` section exists with preamble explicitly stating v26.0 has no separate FINDINGS doc.
    - Regression Appendix table has >= 13 rows with F-25-NN IDs covering F-25-01 through F-25-13.
    - Each F-25-NN row carries exactly one status tag from {HOLDS, SUPERSEDED, FIXED, INVALIDATED}.
    - `## Audit Trail` table has 3 phase rows (220, 221, 222) plus a Total row.
    - `223-01-SUMMARY.md` contains the F-27-NN → source-item mapping table covering every WR-* / IN-* / Gap item from the three REVIEW/VERIFICATION files.
    - No contract or test edits — `git diff --name-only contracts/ test/` returns empty output (filter out the pre-existing dirty ContractAddresses.sol and the Phase 222 pre-work test files already flagged in STATE.md blockers).
    - `git diff --name-only HEAD~1..HEAD audit/` contains `audit/FINDINGS-v27.0.md` after the plan's single commit.
  </acceptance_criteria>
  <done>
    `audit/FINDINGS-v27.0.md` is committed with the full v27.0 findings consolidation: ~15 F-27-NN items, every item with complete field-table + severity justification, resolved-in-cycle items tagged with commit shas (including both ef83c5cd and e0a1aa3e), regression appendix verifying all 13 v25.0 findings with HOLDS/SUPERSEDED/FIXED/INVALIDATED tags, and the verbatim D-07 scope-framing sentence in the preamble. The F-27-NN → source-item mapping table is recorded in 223-01-SUMMARY.md for downstream Plan 223-02 consumption. CSI-12 is satisfied (checkbox flip deferred to Plan 223-02).
  </done>
</task>

</tasks>

<threat_model>
**Documentation-only plan — no executable surface; threat model N/A.**

This plan writes exactly one markdown file (`audit/FINDINGS-v27.0.md`) in the `audit/` directory. It touches no contracts, no scripts, no tests, no deploy pipeline, no CI configuration, no dependencies, and no runtime code. The file is consumed by human readers (C4A wardens, external auditors) for audit-trail context; it does not feed into any automated verification, deployment, or runtime decision. No trust boundaries are introduced, no new input is processed, and no output is executed. STRIDE analysis produces no applicable threats for a pure-markdown additive file with no executable surface.

Per the phase planning-context block: "documentation-only; no executable surface; threat model N/A."
</threat_model>

<verification>
Phase-plan-level checks after the task completes:

1. File exists and is committed:
   - `test -f audit/FINDINGS-v27.0.md && echo EXISTS`
   - `git log --oneline -1 audit/FINDINGS-v27.0.md` returns a commit from this plan
2. No contract or test regressions:
   - `git diff --name-only contracts/ test/` returns empty (accounting for pre-existing dirty files already tracked in STATE.md)
3. Sibling gates still green:
   - `make check-interfaces` exits 0
   - `make check-delegatecall` exits 0
   - `make check-raw-selectors` exits 0
   - `bash scripts/coverage-check.sh` exits 0
4. All grep checks from Task 1's `<verify>` block pass.
5. Regression Appendix internal consistency:
   - Count of rows in the F-25 table equals 13
   - Every row has exactly one status tag
   - Status key block appears before the table
</verification>

<success_criteria>
- `audit/FINDINGS-v27.0.md` exists at the path and follows the FINDINGS-v25.0.md structure.
- ~15 F-27-NN findings (final integer count 14-16) cover every WR-* and IN-* item from 220-REVIEW, 221-REVIEW, 222-REVIEW plus both 222-VERIFICATION gaps, with de-duplication where items overlap.
- F-27-NN IDs are sequentially assigned (Phase 220 first, then 221, then 222) with no gaps.
- Every resolved-in-cycle item carries `**Status:** Resolved in v27.0 (commit <sha>)`, and both `ef83c5cd` and `e0a1aa3e` appear as specific shas.
- All five resolved source items (WR-221-01, WR-221-02, WR-222-02, WR-222-03, WR-222-04) appear in the file with a Resolved marker, either as standalone F-27-NN blocks or as sub-points with their own Resolved lines.
- Default severity INFO applied per D-04; any LOW escalation is justified in the severity-justification field.
- Regression Appendix verifies all 13 v25.0 findings with HOLDS/SUPERSEDED/FIXED/INVALIDATED tags and one-line evidence citations.
- v26.0 regression gap is explicitly noted in the appendix preamble.
- The verbatim D-07 scope-framing sentence appears in the preamble.
- `223-01-SUMMARY.md` contains the F-27-NN → source-item mapping table.
- Zero contract or test edits — `git diff --name-only contracts/ test/` is empty (modulo pre-existing dirty files from STATE.md).
- Phase-level CSI-12 requirement is satisfied (checkbox flip happens in Plan 223-02).
</success_criteria>

<output>
After completion, create `.planning/phases/223-findings-consolidation/223-01-SUMMARY.md` following the standard GSD summary template, capturing:
- Final count of F-27-NN findings produced (integer, not range)
- F-27-NN → source-item mapping table (one row per F-27-NN with columns `F-27-NN | Title | Source Item(s) | Phase | Status`); every WR-*/IN-*/Gap item from the three REVIEW/VERIFICATION files MUST appear at least once in the Source Item(s) column (this table is the authoritative cross-reference Plan 223-02 will consume)
- Distribution: CRITICAL/HIGH/MEDIUM/LOW/INFO (expected 0/0/0/0-1/14-15)
- Resolved-in-cycle count (expected >= 5 including WR-222-04 whether standalone or sub-point)
- v25.0 regression disposition counts (HOLDS/SUPERSEDED/FIXED/INVALIDATED)
- Commit sha for the single FINDINGS-v27.0.md commit
- Handoff note to Plan 223-02 identifying which F-27-NN IDs are D-08 promotion candidates for KNOWN-ISSUES.md (expected: the WR-222-01 patchContractAddresses finding, the two 222 VERIFICATION-gap resolved items for historical record, and any IN-* item documenting a deliberate script trade-off)
</output>
</content>
</invoke>