---
phase: 227
plan: 02
type: execute
wave: 2
depends_on: ["227-01"]
files_modified:
  - .planning/phases/227-indexer-event-processing-correctness/227-02-EVENT-ARG-MAPPING.md
  - .planning/phases/227-indexer-event-processing-correctness/227-02-SUMMARY.md
autonomous: true
requirements: [IDX-02]
requirements_addressed: [IDX-02]
finding_id_block: F-28-227-101+
must_haves:
  truths:
    - "Every PROCESSED or DELEGATED (contractFile, eventName) row from 227-01 has a full per-arg verdict in 227-02-EVENT-ARG-MAPPING.md"
    - "Every per-arg verdict covers the three dimensions (field-name, type, coercion) per D-227-09 — all three must PASS for an event to PASS"
    - "Arg→schema mapping uses Phase 226's locked schema model (226-01-SCHEMA-MIGRATION-DIFF.md) as the authoritative target-column source — no re-parsing of Drizzle schemas"
    - "Silent-truncation hits (Number(parseBigInt(...)) against uint48+ Solidity args) are flagged as LOW-severity findings in the F-28-227-101+ block"
    - "Address-case drift (address args read without parseAddress) are flagged as LOW-severity findings in the F-28-227-101+ block"
  artifacts:
    - path: ".planning/phases/227-indexer-event-processing-correctness/227-02-EVENT-ARG-MAPPING.md"
      provides: "Per-event arg-row verdict table + F-28-227-1NN finding stubs"
      contains: "| Event | Arg | Solidity type | Coercion wrapper | TS field | Schema column | Schema type | Name match? | Type match? | Coercion safe? | Verdict |"
    - path: ".planning/phases/227-indexer-event-processing-correctness/227-02-SUMMARY.md"
      provides: "Counts, finding IDs consumed, next available ID, handoff"
  key_links:
    - from: "handlers/*.ts ctx.args.<name> reads"
      to: "Drizzle .insert(table).values({...}) target columns"
      via: "parseBigInt / parseAddress / Number(parseBigInt(...)) coercion wrappers"
      pattern: "ctx\\.args\\.\\w+|\\.insert\\(\\w+\\)\\.values"
    - from: "227-02 arg-row verdicts"
      to: "226-01-SCHEMA-MIGRATION-DIFF.md target-column types"
      via: "direct lookup of schema table + column"
      pattern: "226-01-SCHEMA-MIGRATION-DIFF"
---

<objective>
IDX-02 — For every PROCESSED/DELEGATED `(contractFile, eventName)` row from 227-01, walk the handler body and produce a per-arg verdict covering (1) field-name, (2) type, (3) coercion correctness against the Phase 226 locked schema model. Emit F-28-227-101+ findings for name swaps, type mismatches, silent truncation (`Number(parseBigInt(uint48+))`), and address-case drift.

Purpose: surface silent data-corruption bugs between event args and schema columns.
Output: `227-02-EVENT-ARG-MAPPING.md` + `227-02-SUMMARY.md`.

Per D-227-02: catalog-only. Per D-227-11: finding IDs `F-28-227-101+` — reserved block to avoid parallel-collision with 227-03 (`F-28-227-201+`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/227-indexer-event-processing-correctness/227-CONTEXT.md
@.planning/phases/227-indexer-event-processing-correctness/227-RESEARCH.md
@.planning/phases/227-indexer-event-processing-correctness/227-VALIDATION.md
@.planning/phases/227-indexer-event-processing-correctness/227-01-EVENT-COVERAGE-MATRIX.md
@.planning/phases/227-indexer-event-processing-correctness/227-01-SUMMARY.md
@.planning/phases/226-schema-migration-orphan-audit/226-01-SCHEMA-MIGRATION-DIFF.md

<interfaces>
Handler shape (from RESEARCH.md §Handler Shape — every handler in `database/src/handlers/*.ts` follows this):

```typescript
export async function handle<Event>(ctx: HandlerContext): Promise<void> {
  const <fieldX> = parseAddress(ctx.args.<argX>);              // address coercion
  const <fieldY> = Number(parseBigInt(ctx.args.<argY>));       // narrowing coercion
  const <fieldZ> = parseBigInt(ctx.args.<argZ>).toString();    // string-preserving coercion
  await ctx.tx
    .insert(<schemaTable>)
    .values({ <fieldX>, <fieldY>, <fieldZ>, blockNumber: ctx.blockNumber, blockHash: ctx.blockHash })
    .onConflictDoUpdate({ target: [...], set: { ... } });
}
```

Coercion wrappers (from `database/src/utils/bigint-utils.ts`):

| Wrapper | Semantics |
|---------|-----------|
| `parseBigInt(v)` | Returns `bigint`. Decimal-string round-trip survivor. |
| `parseAddress(v)` | Lowercases address. EIP-55 checksum NOT preserved. Matches `event-processor.ts:101 rawLog.address.toLowerCase()`. |
| `Number(parseBigInt(...))` | **Narrowing** — unsafe past 2^53 (uint48+ risk). |
| `parseBigInt(...).toString()` | Decimal string. Safe for uint256. Target column must be `numeric` or `text`, never `integer`/`bigint`. |

### COERCION GOTCHAS TABLE (VERBATIM from RESEARCH.md §Type-Coercion Gotchas — copy into deliverable)

| Solidity type | Safe TS coercion | Unsafe (finding candidate) | Target column |
|---------------|-----------------|---------------------------|---------------|
| `uint8` / `uint16` / `uint24` / `uint32` | `Number(parseBigInt(x))` | direct `ctx.args.x as number` (works for small ints but loses provenance) | `smallint` / `integer` |
| `uint48` / `uint56` / `uint64` | `parseBigInt(x).toString()` or Drizzle `bigint({mode:'bigint'})` | `Number(parseBigInt(x))` — JS `Number` safe only to 2^53-1 | `bigint` or `numeric`/`text` |
| `uint96` / `uint128` / `uint256` | `parseBigInt(x).toString()` | `Number(parseBigInt(x))` — **silent truncation** past 2^53 | `numeric` or `text` |
| `address` | `parseAddress(x)` (lowercased) | raw `ctx.args.x` (mixed case from viem) — breaks FK joins against other lowercased columns | `text` |
| `bytes32` | `ctx.args.x as string` (viem returns `0x`-prefixed hex) | hex-decode to Buffer | `text` |
| `bool` | `Boolean(ctx.args.x)` or direct | `Number(ctx.args.x)` | `boolean` |
| enum / `uint8` | `Number(parseBigInt(x))` with named enum guard | raw `parseBigInt` (leaks bigint type surface) | `smallint` |
| `int256` (signed) | `parseBigInt(x).toString()` | `Number(...)` | `numeric` |

### PER-ARG VERDICT TABLE TEMPLATE (REQUIRED columns)

| Event | Arg | Solidity type | Coercion wrapper | TS field | Schema table | Schema column | Schema type | Name match? | Type match? | Coercion safe? | Verdict |
|---|---|---|---|---|---|---|---|---|---|---|---|

### SILENT-TRUNCATION HUNT PATTERN (verbatim from RESEARCH.md)

```
rg -nB2 -A1 "Number\(parseBigInt\(ctx\.args\.\w+\)\)" /home/zak/Dev/PurgeGame/database/src/handlers/*.ts
```

Verdict: LOW finding if the matched Solidity arg type is `uint48` or wider. PASS if `uint32` or narrower.

### ADDRESS-CASE DRIFT HUNT PATTERN (verbatim from RESEARCH.md)

```
rg -n "ctx\.args\.\w+" /home/zak/Dev/PurgeGame/database/src/handlers/*.ts | rg -v "parseAddress|parseBigInt|\.toString"
```

Verdict: LOW finding if the matched Solidity arg type is `address`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: For every PROCESSED/DELEGATED row, extract (arg, coercion, ts_field, target_column)</name>
  <files>.planning/phases/227-indexer-event-processing-correctness/227-02-EVENT-ARG-MAPPING.md</files>
  <read_first>
    - /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/227-indexer-event-processing-correctness/227-01-EVENT-COVERAGE-MATRIX.md (input set: PROCESSED ∪ DELEGATED rows)
    - /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/227-indexer-event-processing-correctness/227-RESEARCH.md (§Handler Shape, §Arg→Field Mapping Extraction, §Type-Coercion Gotchas — the coercion table is the single source of truth for this plan; copy verbatim into the deliverable)
    - /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/226-schema-migration-orphan-audit/226-01-SCHEMA-MIGRATION-DIFF.md (authoritative target-column types per D-227-09; do NOT re-parse Drizzle schemas)
    - /home/zak/Dev/PurgeGame/database/src/handlers/*.ts (all files referenced from HANDLER_REGISTRY — identified via 227-01)
    - /home/zak/Dev/PurgeGame/database/src/utils/bigint-utils.ts (parseBigInt / parseAddress definitions)
    - The specific contracts/*.sol file for each event (already cited in 227-01 universe table — for Solidity arg types)
  </read_first>
  <action>
Write the `227-02-EVENT-ARG-MAPPING.md` header block, then the Coercion Gotchas table (verbatim from RESEARCH.md §Type-Coercion Gotchas — the block above in `<interfaces>` is the copy-source), then one verdict table per PROCESSED/DELEGATED event from 227-01.

For each event:

1. From `contracts/<ContractFile>.sol`: extract `(argName, solidityType, indexed)` — reuse the Pass 2 regex `rg -nHU --multiline-dotall 'event\s+<EventName>\s*\(([^;]*?)\)\s*;'` and split on `,`.

2. From `database/src/handlers/index.ts`: find `<EventName>:` → handler symbol (e.g., `handleQuestSlotRolled`).

3. From the handler file (grep `^export async function <handleFn>` then read body until first `.onConflictDoUpdate` or function close):
   - For every `ctx.args.<name>` read, record (argName, coercion wrapper, ts variable).
   - Extract the object-literal keys of `.insert(<table>).values({...})` — these are the TS→column mapping.

4. For DELEGATED events (router handlers), the body branches on `ctx.contractAddress`. Produce ONE verdict row per `(contractFile, eventName, args)` tuple — each branch may write a different schema table, so per-emitter-contract rows are required.

5. Look up each target column in `226-01-SCHEMA-MIGRATION-DIFF.md` and record:
   - Schema table name
   - Column name
   - Column type (smallint / integer / bigint / numeric / text / boolean / etc.)

6. Apply the three verdict dimensions per D-227-09:
   - **Name match?** — does `<tsField>` camel↔snake-equivalent match the schema column name?
   - **Type match?** — does Solidity type → schema column type align per the Coercion Gotchas table?
   - **Coercion safe?** — is the wrapper appropriate (no `Number(parseBigInt(uint48+))`, no raw `ctx.args.<addr>` for address types)?

7. All three must PASS for the event to PASS. Any FAIL → event FAIL.

Output each event as:

```markdown
### <EventName> — <contracts/XYZ.sol> → handlers/<handler>.ts → <schemaTable>

| Event | Arg | Solidity type | Coercion wrapper | TS field | Schema table | Schema column | Schema type | Name match? | Type match? | Coercion safe? | Verdict |
|---|---|---|---|---|---|---|---|---|---|---|---|
| … | … | … | … | … | … | … | … | ✓/✗ | ✓/✗ | ✓/✗ | PASS/FAIL |

**Event verdict:** PASS | FAIL
**Handler file:line:** database/src/handlers/<file>.ts:NN
```
  </action>
  <verify>
    <automated>test -f .planning/phases/227-indexer-event-processing-correctness/227-02-EVENT-ARG-MAPPING.md && grep -qE 'Name match\?' .planning/phases/227-indexer-event-processing-correctness/227-02-EVENT-ARG-MAPPING.md && grep -qE 'Coercion safe\?' .planning/phases/227-indexer-event-processing-correctness/227-02-EVENT-ARG-MAPPING.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists with the Coercion Gotchas table copied verbatim from RESEARCH.md.
    - Every PROCESSED/DELEGATED row from 227-01 has a corresponding verdict block (cross-check: count of `### ` headings ≥ count of PROCESSED + DELEGATED rows in 227-01).
    - Every verdict row has the three verdict columns (Name match? / Type match? / Coercion safe?) populated.
    - Every handler citation is an absolute path under `/home/zak/Dev/PurgeGame/database/src/handlers/` with a line number.
    - Every schema target-column type comes from 226-01-SCHEMA-MIGRATION-DIFF.md (do not re-parse Drizzle).
  </acceptance_criteria>
  <done>Every PROCESSED/DELEGATED event has a verdict block with per-arg rows and an event-level PASS/FAIL.</done>
</task>

<task type="auto">
  <name>Task 2: Run silent-truncation + address-drift grep sweeps; emit F-28-227-101+ finding stubs</name>
  <files>.planning/phases/227-indexer-event-processing-correctness/227-02-EVENT-ARG-MAPPING.md</files>
  <read_first>
    - The 227-02-EVENT-ARG-MAPPING.md produced in Task 1
    - /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/227-indexer-event-processing-correctness/227-RESEARCH.md (§Type-Coercion Gotchas — silent-truncation + address-case grep patterns, verbatim)
    - /home/zak/Dev/PurgeGame/database/src/handlers/*.ts
  </read_first>
  <action>
Sweep 1 — silent-truncation hunt (RESEARCH.md verbatim):

```
rg -nB2 -A1 "Number\(parseBigInt\(ctx\.args\.\w+\)\)" /home/zak/Dev/PurgeGame/database/src/handlers/*.ts
```

For each hit:
- Resolve the Solidity arg type by cross-referencing the verdict table from Task 1.
- If arg type is `uint48` / `uint56` / `uint64` / `uint96` / `uint128` / `uint256` → emit a LOW finding.
- If arg type is `uint32` or narrower → PASS (no finding).

Sweep 2 — address-case drift (RESEARCH.md verbatim):

```
rg -n "ctx\.args\.\w+" /home/zak/Dev/PurgeGame/database/src/handlers/*.ts | rg -v "parseAddress|parseBigInt|\.toString"
```

For each hit where the Solidity arg type is `address` (cross-reference Task 1): emit a LOW finding for checksum-case drift.

Sweep 3 — field-swap / name-mismatch: scan Task 1 verdict rows; every row with `Name match? = ✗` → emit an INFO-or-LOW finding (severity promotes to LOW if the mismatched column is semantically load-bearing, e.g., `from` written to `to_address`).

Finding stub format (emit at bottom of `227-02-EVENT-ARG-MAPPING.md`, sequential IDs starting at `F-28-227-101`):

```markdown
#### F-28-227-101: <EventName>.<arg> silent truncation via Number(parseBigInt(...))

- **Severity:** LOW
- **Direction:** schema↔handler
- **Phase:** 227
- **Requirement:** IDX-02
- **File:** /home/zak/Dev/PurgeGame/database/src/handlers/<file>.ts:NN
- **Event source:** /home/zak/Dev/PurgeGame/degenerus-audit/contracts/<ContractFile>.sol:NN (arg type uint64)
- **Target column:** <table>.<column> (<schema type>)
- **Resolution:** RESOLVED-CODE — replace `Number(parseBigInt(x))` with `parseBigInt(x).toString()` and widen column if needed
- **Evidence:** <1-2 lines>
```

Count all findings. Confirm none collide with 227-01's `F-28-227-01..NN` block or 227-03's reserved `F-28-227-201+` block.
  </action>
  <verify>
    <automated>grep -cE 'F-28-227-1[0-9]{2}' .planning/phases/227-indexer-event-processing-correctness/227-02-EVENT-ARG-MAPPING.md >/dev/null && ! grep -qE 'F-28-227-20[0-9]' .planning/phases/227-indexer-event-processing-correctness/227-02-EVENT-ARG-MAPPING.md</automated>
  </verify>
  <acceptance_criteria>
    - Every silent-truncation hit (`Number(parseBigInt(ctx.args.X))` against a uint48+ Solidity arg) has a `F-28-227-1NN` LOW finding stub.
    - Every address-case drift hit (raw `ctx.args.<addr>` without parseAddress against an `address`-type Solidity arg) has a `F-28-227-1NN` LOW finding stub.
    - Every Name-match ✗ row has an INFO or LOW finding stub.
    - All finding IDs fall in `F-28-227-101..199` (reserved block per D-227-11) — NONE collide with 227-01's `F-28-227-01..` or 227-03's `F-28-227-201..`.
    - Each finding cites absolute paths: handler `File:line` under `/home/zak/Dev/PurgeGame/database/src/handlers/` AND event-source `File:line` under `/home/zak/Dev/PurgeGame/degenerus-audit/contracts/`.
    - Each finding cites the target schema column (table.column) and the schema type sourced from `226-01-SCHEMA-MIGRATION-DIFF.md`.
  </acceptance_criteria>
  <done>All three sweeps complete; findings in reserved block; no cross-plan ID collisions.</done>
</task>

<task type="auto">
  <name>Task 3: Write 227-02-SUMMARY.md with counts, finding IDs consumed, handoff</name>
  <files>.planning/phases/227-indexer-event-processing-correctness/227-02-SUMMARY.md</files>
  <read_first>
    - The 227-02-EVENT-ARG-MAPPING.md produced in Tasks 1-2
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
Write `227-02-SUMMARY.md`:

1. Event verdict counts: PASS / FAIL (per-event totals).
2. Per-arg-row verdict counts (grouped by dimension: Name match ✓/✗, Type match ✓/✗, Coercion safe ✓/✗).
3. Finding IDs consumed in the `F-28-227-101+` block; next available ID for Phase 229 consolidation (still in 1NN block).
4. Severity breakdown (INFO / LOW); no MEDIUM or HIGH per D-227-05 default (unless observed during audit — if so, justify inline).
5. Spot-recheck log: pick 3 random events per 227-VALIDATION.md §Per-Task Verification Map; re-walk each arg manually against Phase 226's schema model; confirm verdict holds.
6. Open questions (e.g., if any handler uses a coercion wrapper not in the RESEARCH.md Coercion Gotchas table — flag for Phase 229).
7. Handoff to Phase 229: list all F-28-227-1NN finding IDs with one-line titles for consolidation.
  </action>
  <verify>
    <automated>test -f .planning/phases/227-indexer-event-processing-correctness/227-02-SUMMARY.md && grep -qE 'F-28-227-1' .planning/phases/227-indexer-event-processing-correctness/227-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY exists with verdict counts matching the mapping document.
    - Finding-ID range cited; all IDs in `F-28-227-101..199`.
    - 3 spot-rechecks documented.
  </acceptance_criteria>
  <done>SUMMARY committed; Phase 229 has finding-ID inventory for 227-02 contribution.</done>
</task>

</tasks>

<threat_model>
N/A — read-only catalog audit; no runtime gate per D-227-02. No trust boundaries crossed; no code executed; no network surface changed. STRIDE register not applicable.
</threat_model>

<verification>
- `227-02-EVENT-ARG-MAPPING.md` covers every PROCESSED/DELEGATED row from 227-01 with per-arg verdicts on (name, type, coercion).
- Silent-truncation + address-drift sweeps complete.
- All finding IDs in reserved block `F-28-227-101..199`; no collision with 227-01 or 227-03.
- Schema-column types sourced from `226-01-SCHEMA-MIGRATION-DIFF.md`; no re-parsing of Drizzle schemas.
- Zero writes to `contracts/` or `database/`.
</verification>

<success_criteria>
- IDX-02 satisfied: every processed/delegated event has a per-arg verdict.
- Silent-truncation and address-case drift candidates surfaced as findings.
- Handoff to Phase 229 documented.
</success_criteria>

<output>
Create `.planning/phases/227-indexer-event-processing-correctness/227-02-SUMMARY.md`.
</output>
