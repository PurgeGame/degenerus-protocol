---
phase: 216-pool-eth-accounting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/216-pool-eth-accounting/216-01-ETH-CONSERVATION.md
autonomous: true
requirements: [POOL-01]

must_haves:
  truths:
    - "Every ETH inflow path (purchase, whale passes, degenerette bets) is algebraically accounted with code-level proof showing exact Solidity lines"
    - "Every ETH outflow path (jackpots, claims, redemption, sweep, drain) is algebraically accounted with code-level proof"
    - "Every internal ETH transfer (pool consolidation, yield distribution, resumeEthPool carry) is traced with conservation proven at each step"
    - "A global conservation equation proves SUM(inflows) = SUM(outflows) + SUM(held) with each term grounded in code"
    - "The two-call split (SPLIT_NONE/CALL1/CALL2) is proven to neither create nor destroy ETH across the split boundary"
    - "The batched-SSTORE pool consolidation is proven to neither create nor destroy ETH during memory-to-storage writeback"
  artifacts:
    - path: ".planning/phases/216-pool-eth-accounting/216-01-ETH-CONSERVATION.md"
      provides: "Algebraic ETH conservation proof with code-level flow traces for all 20 EF chains"
      contains: "POOL-01"
  key_links:
    - from: "MintModule._purchaseFor()"
      to: "futurePool / currentPrizePool"
      via: "pool allocation splits"
      pattern: "futurePool|currentPrizePool"
    - from: "AdvanceModule._consolidatePoolsAndRewardJackpots()"
      to: "prizePoolsPacked SSTORE"
      via: "memory batch writeback"
      pattern: "_setPrizePools|_setCurrentPrizePool"
    - from: "JackpotModule._addClaimableEth()"
      to: "claimableWinnings mapping"
      via: "pool deduction + claimable credit"
      pattern: "claimableWinnings|claimablePool"
    - from: "Game._claimWinningsInternal()"
      to: "ETH transfer to player"
      via: "claimableWinnings deduction + send"
      pattern: "_claimWinningsInternal"
---

<objective>
Produce an algebraic ETH conservation proof across the entire restructured pool architecture (POOL-01).

Purpose: Prove that no ETH can be created, destroyed, or misrouted through the consolidated pool system. Uses symbolic variables and equations to prove all ETH in = all ETH out + all ETH held, with each equation term grounded by code-level flow traces showing the exact Solidity lines that implement it. Per D-01, this is fresh from scratch with no reliance on prior pool audit artifacts. Per D-02, Phase 214 adversarial audit findings can be cited as supporting evidence.

Output: 216-01-ETH-CONSERVATION.md -- algebraic conservation proof with flow traces for all 20 EF chains (EF-01 through EF-20).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/216-pool-eth-accounting/216-CONTEXT.md

@.planning/phases/213-delta-extraction/213-DELTA-EXTRACTION.md (EF-01 through EF-20 chain definitions)

Phase 214 can be cited as supporting evidence per D-02:
@.planning/phases/214-adversarial-audit/214-03-STATE-COMPOSITION.md (pool packed field integrity verdicts)

@docs/JACKPOT-PAYOUT-REFERENCE.md (jackpot payout flow for EF-04/05/06 tracing)
</context>

<tasks>

<task type="auto">
  <name>Task 1: ETH inflow proof and pool allocation algebra</name>
  <files>.planning/phases/216-pool-eth-accounting/216-01-ETH-CONSERVATION.md</files>
  <read_first>
    contracts/storage/DegenerusGameStorage.sol (lines 337-396 for currentPrizePool, claimablePool, claimableWinnings, prizePoolsPacked; lines 693-821 for _setPrizePools/_getPrizePools/_setCurrentPrizePool etc; line 1020 for resumeEthPool)
    contracts/modules/DegenerusGameMintModule.sol (lines 903-950 for _purchaseFor -- msg.value split to pools; lines 875-900 for _purchaseCoinFor; lines 1396-1450 for _purchaseBurnieLootboxFor)
    contracts/modules/DegenerusGameWhaleModule.sol (lines 187-250 for purchaseWhaleBundle; lines 380-420 for purchaseLazyPass; lines 538-580 for purchaseDeityPass)
    contracts/modules/DegenerusGameDegeneretteModule.sol (lines 511-560 for _collectBetFunds)
    contracts/DegenerusGame.sol (lines 508-560 for purchase entry; lines 606-660 for purchaseWhaleBundle entry; lines 631-660 for purchaseLazyPass entry; lines 651-680 for purchaseDeityPass entry)
    contracts/modules/DegenerusGameAdvanceModule.sol (lines 620-800 for _consolidatePoolsAndRewardJackpots -- entire pool consolidation flow with memory batch; lines 789-800 for single SSTORE batch)
  </read_first>
  <action>
Read every ETH inflow path and the pool consolidation path from source. For each, extract: (1) exact Solidity code for the ETH-critical lines, (2) line numbers, (3) how msg.value is split across pools, (4) which storage variables are written.

Produce the first half of 216-01-ETH-CONSERVATION.md with these sections:

**Section 0: Pool Architecture Overview**
Summarize the v20.0 consolidated pool architecture:
- Storage slots: prizePoolsPacked (nextPool:uint128 | futurePool:uint128), currentPrizePool (uint128), claimablePool (uint128, slot 1 alongside currentPrizePool), claimableWinnings mapping, resumeEthPool (uint128), yieldAccumulator
- Memory-batch pattern: _consolidatePoolsAndRewardJackpots loads all pools into memory, computes, writes back in batched SSTOREs
- Two-call split: SPLIT_NONE/CALL1/CALL2 with resumeEthPool carrying ETH between calls
- Include DegenerusGameStorage.sol line numbers for each variable

**Section 1: ETH Inflow Accounting**
For each inflow chain, trace msg.value from entry to storage:

EF-01 (Purchase): Game.purchase() -> MintModule._purchaseFor()
- Show the exact percentage split of msg.value into futurePool, currentPrizePool, and any other recipients
- Show line numbers for each assignment
- Define symbolic variable: I_purchase = msg.value at purchase entry

EF-16 (Whale passes): WhaleModule.purchaseWhaleBundle/purchaseLazyPass/purchaseDeityPass
- Show how pass ETH is allocated (which pools receive what fraction)
- Define symbolic variable: I_whale = msg.value at whale entry

EF-17 (Degenerette bets): DegeneretteModule._collectBetFunds()
- Show how bet ETH is collected and where it goes
- Note the milli-ETH packing in lootboxRngPendingEth
- Define symbolic variable: I_bet = msg.value at bet entry

**Section 2: Internal Flow Accounting**
EF-02 (Pool consolidation): AdvanceModule._consolidatePoolsAndRewardJackpots()
- Trace the full memory-batch flow: load futurePool/currentPrizePool/nextPool/yieldAccumulator -> compute drawdown/drip/take -> write back
- Prove conservation: memFuture_before + memNext_before + memCurrent_before + memYieldAcc_before = memFuture_after + memNext_after + memCurrent_after + memYieldAcc_after
- Show every arithmetic operation that moves ETH between pools, with line numbers
- Prove the single-SSTORE batch (line ~789) writes back exactly the computed values

EF-03 (Yield surplus): AdvanceModule -> JackpotModule.distributeYieldSurplus()
- Show the 23/23/23/8 split and where the remaining percentage goes
- Trace from yieldAccumulator deduction to claimableWinnings[sDGNRS], claimableWinnings[VAULT], claimableWinnings[GNRUS]
- Prove: yieldDeducted = credited_sDGNRS + credited_VAULT + credited_GNRUS + retained_buffer

EF-14 (GNRUS charity): GNRUS.pickCharity() distribution
- Trace the 2% unallocated per level flow
- Show where charity ETH comes from and goes to

EF-20 (BURNIE flip credit): AdvanceModule coinflip.creditFlip() during consolidation
- Trace the creditFlip call: what ETH amount is passed to coinflip
- Show this is a cross-contract call, not an internal pool transfer

**Two-call split conservation:**
- Trace how resumeEthPool bridges ETH across CALL1 -> CALL2
- Prove: ETH entering CALL1 that isn't consumed = resumeEthPool = ETH available at CALL2 start
- Prove: after CALL2 completes, resumeEthPool = 0 (fully consumed)

For EVERY chain include:
- Contract name and exact line numbers
- The actual Solidity code snippet (key lines only)
- Symbolic equation showing ETH conservation
- Verdict: CONSERVED (ETH neither created nor destroyed) or LEAK (ETH created/destroyed/misrouted)

End section 2 with an inflow + internal summary table:
| Chain | Entry | Pool(s) Written | Symbolic Equation | Verdict |
  </action>
  <verify>
    <automated>grep -c "CONSERVED\|LEAK" .planning/phases/216-pool-eth-accounting/216-01-ETH-CONSERVATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/216-pool-eth-accounting/216-01-ETH-CONSERVATION.md exists
    - Contains "Section 0: Pool Architecture Overview" with prizePoolsPacked, currentPrizePool, claimablePool, resumeEthPool
    - Contains "Section 1: ETH Inflow Accounting" covering EF-01, EF-16, EF-17
    - Contains "Section 2: Internal Flow Accounting" covering EF-02, EF-03, EF-14, EF-20
    - Contains symbolic variables (I_purchase, I_whale, I_bet or equivalent)
    - Contains algebraic equations for pool consolidation conservation
    - Contains two-call split (resumeEthPool) conservation proof
    - Contains line number references for every code snippet
    - Contains "CONSERVED" or "LEAK" verdicts for each chain
    - Does NOT reference prior pool audit artifacts (phases 183-187, 199-200) per D-01
  </acceptance_criteria>
  <done>ETH inflow paths and internal flows algebraically proven with code-level traces, pool consolidation memory-batch proven conservative, two-call split proven conservative</done>
</task>

<task type="auto">
  <name>Task 2: ETH outflow proof and global conservation equation</name>
  <files>.planning/phases/216-pool-eth-accounting/216-01-ETH-CONSERVATION.md</files>
  <read_first>
    .planning/phases/216-pool-eth-accounting/216-01-ETH-CONSERVATION.md (sections 0-2 from Task 1 -- needed as foundation for outflow proof)
    contracts/modules/DegenerusGameJackpotModule.sol (lines 310-450 for payDailyJackpot; lines 764-850 for _addClaimableEth; lines 1182-1280 for _processDailyEth; lines 1404-1500 for _handleSoloBucketWinner; lines 1977-2050 for runBafJackpot; lines 716-760 for distributeYieldSurplus)
    contracts/modules/DegenerusGameDecimatorModule.sol (lines 307-400 for claimDecimatorJackpot; lines 779-860 for claimTerminalDecimatorJackpot)
    contracts/modules/DegenerusGameDegeneretteModule.sol (lines 684-750 for _distributePayout)
    contracts/modules/DegenerusGameGameOverModule.sol (lines 79-190 for handleGameOverDrain; lines 188-250 for handleFinalSweep; lines 217-250 for _sendToVault, _sendStethFirst)
    contracts/DegenerusGame.sol (lines 1366-1420 for _claimWinningsInternal; lines 1393-1430 for claimAffiliateDgnrs)
    contracts/GNRUS.sol (lines 282-340 for burn -- proportional ETH+stETH redemption)
    contracts/DegenerusStonk.sol (lines 304-360 for yearSweep)
    contracts/StakedDegenerusStonk.sol (lines 455-500 for burnAtGameOver)
    docs/JACKPOT-PAYOUT-REFERENCE.md (complete -- jackpot payout flow reference)
  </read_first>
  <action>
Read every ETH outflow path from source and append sections 3-5 to the existing 216-01-ETH-CONSERVATION.md.

**Section 3: ETH Outflow Accounting**
For each outflow chain, trace ETH from pool deduction to external transfer or claimable credit:

EF-04 (Daily jackpot): JackpotModule.payDailyJackpot() -> _processDailyEth() -> _addClaimableEth()
- Show the currentPrizePool -> daily slice calculation (6-14% on days 1-4, 100% on day 5)
- Trace through _processDailyEth: how the slice is divided among winners
- Trace _addClaimableEth: deduction from pool, credit to claimableWinnings[winner]
- Two-call split: how resumeEthPool carries daily ETH between CALL1/CALL2
- Define: O_daily = sum of _addClaimableEth credits from daily jackpot

EF-05 (Solo bucket winner): JackpotModule._handleSoloBucketWinner() -> _addClaimableEth()
- Show what triggers solo bucket payout and the amount calculation
- Trace whale pass + DGNRS reward side effects
- Define: O_solo = _addClaimableEth credit from solo bucket

EF-06 (BAF jackpot): JackpotModule.runBafJackpot() -> _addClaimableEth()
- Show the self-call guard, claimableDelta return
- Trace from pool amount calculation to winner credit
- Define: O_baf = _addClaimableEth credits from BAF

EF-07 (Decimator): DecimatorModule.claimDecimatorJackpot() -> _creditClaimable()
- Show claimablePool deduction and uint128 narrowing
- Define: O_decimator = _creditClaimable amount

EF-08 (Terminal decimator): DecimatorModule.claimTerminalDecimatorJackpot() -> _creditClaimable()
- Show <=7 days blocked, multiplier calculation
- Define: O_terminal = _creditClaimable amount

EF-09 (Degenerette winnings): DegeneretteModule._distributePayout() -> _creditClaimable()
- Show claimablePool narrowed uint128
- Define: O_degen = _creditClaimable amount

EF-10 (Gameover drain): GameOverModule.handleGameOverDrain()
- Show terminal jackpots + BAF + decimator + _creditClaimable chain
- Show RNG gate defense-in-depth, gameOverStatePacked
- Define: O_drain = total ETH moved from pools to claimable/external

EF-11 (Final sweep): GameOverModule.handleFinalSweep() -> _sendToVault() -> _sendStethFirst()
- Show 33/33/34 split: sDGNRS / VAULT / GNRUS
- Show 30-day delay, stETH-first, gameOverStatePacked finalSwept flag
- Define: O_sweep = total ETH + stETH sent in final sweep

EF-12 (Player claim): Game._claimWinningsInternal()
- Show claimableWinnings[player] deduction, claimablePool deduction, ETH transfer
- Prove: amount deducted from claimableWinnings = amount sent to player
- Define: O_claim = ETH transferred out

EF-13 (GNRUS redemption): GNRUS.burn() -> game.claimWinnings(address(this))
- Show proportional ETH+stETH calculation based on GNRUS balance
- Trace the callback to Game for ETH portion
- Define: O_gnrus_redeem = proportional ETH sent to burner

EF-15 (Affiliate claim): Game.claimAffiliateDgnrs() -> coinflip.creditFlip()
- Show DGNRS claim + flip credit path
- Define: O_affiliate = DGNRS + flip credit value

EF-18 (burnAtGameOver): StakedDegenerusStonk.burnAtGameOver()
- Show remaining pools burned at gameover
- Define: O_burn = ETH removed from circulation

EF-19 (Year sweep): DegenerusStonk.yearSweep()
- Show 50/50 GNRUS + VAULT split
- Show SweepNotReady / NothingToSweep guards
- Define: O_yearSweep = total ETH swept

**Section 4: Global Conservation Equation**
Construct the master equation:

SUM(I_x for all inflow chains) = SUM(O_x for all outflow chains) + SUM(H_x for all held pools)

Where held pools at any instant are:
H = currentPrizePool + nextPool + futurePool + claimablePool + resumeEthPool + yieldAccumulator + contract_balance_residual

Prove each term is accounted exactly once (no double-counting, no omission).

Show the equation balances by:
1. Every I_x has a corresponding pool credit (shown in Section 1)
2. Every O_x has a corresponding pool deduction (shown in Section 3)
3. Internal flows (Section 2) are zero-sum transfers between pools
4. H captures all remaining ETH at any point in time

**Section 5: Conservation Verdict**
- Per-chain verdict table covering all 20 EF chains
- Global verdict: CONSERVED or LEAK with justification
- Any INFO findings (non-vulnerability observations)
- Reference Phase 214 state corruption verdicts as supporting evidence per D-02

| Chain ID | Direction | Pool Deducted | Pool Credited / Recipient | Symbolic Equation | Verdict |
  </action>
  <verify>
    <automated>grep -c "EF-" .planning/phases/216-pool-eth-accounting/216-01-ETH-CONSERVATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains "Section 3: ETH Outflow Accounting" covering EF-04 through EF-19
    - File contains "Section 4: Global Conservation Equation" with master equation
    - File contains "Section 5: Conservation Verdict" with per-chain verdict table
    - All 20 EF chains (EF-01 through EF-20) appear in the document
    - Contains symbolic variables for every outflow (O_daily, O_baf, O_claim, etc.)
    - Contains global held-pool term H with all pool components listed
    - Contains "CONSERVED" or "LEAK" verdict for each chain
    - Contains a global verdict statement
    - Contains line number references for every outflow code path
    - Contains reference to Phase 214 state corruption verdicts per D-02
    - Does NOT reference prior pool audit artifacts (phases 183-187, 199-200) per D-01
  </acceptance_criteria>
  <done>All 20 EF chains algebraically proven, global conservation equation constructed and verified, per-chain and global verdicts rendered</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player -> purchase/bet entry | Untrusted msg.value enters pool system |
| pool storage -> claimableWinnings | Internal state transition (pool deduction -> claimable credit) |
| claimableWinnings -> ETH transfer | State deduction must match external send amount |
| two-call split boundary | ETH must be fully conserved across CALL1 -> resumeEthPool -> CALL2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-216-01 | Tampering | Pool consolidation memory batch | mitigate | Prove memFuture+memNext+memCurrent+memYield is invariant across all arithmetic in _consolidatePoolsAndRewardJackpots |
| T-216-02 | Tampering | Two-call split resumeEthPool | mitigate | Prove resumeEthPool = unprocessed ETH from CALL1, fully consumed in CALL2, zero after completion |
| T-216-03 | Information Disclosure | claimableWinnings amounts | accept | Amounts are on-chain and public by design |
| T-216-04 | Elevation of Privilege | _claimWinningsInternal claim path | mitigate | Verify claim deducts claimableWinnings[msg.sender] before sending ETH (CEI ordering -- cite 214-01) |
</threat_model>

<verification>
- 216-01-ETH-CONSERVATION.md contains algebraic proof covering all 20 EF chains
- Every chain has symbolic equation + code-level evidence
- Global conservation equation constructed with all terms accounted
- No references to prior pool audit artifacts (D-01 compliance)
- Phase 214 cited as supporting evidence where appropriate (D-02 compliance)
</verification>

<success_criteria>
An algebraic ETH conservation proof covers the consolidated pool architecture -- all ETH in = all ETH out + all ETH held -- with each equation term grounded by code-level flow traces.
</success_criteria>

<output>
After completion, create `.planning/phases/216-pool-eth-accounting/216-01-SUMMARY.md`
</output>
