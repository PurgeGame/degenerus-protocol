---
phase: 216-pool-eth-accounting
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/216-pool-eth-accounting/216-02-POOL-MUTATION-SSTORE.md
autonomous: true
requirements: [POOL-02]

must_haves:
  truths:
    - "Every SSTORE site that writes to prizePoolsPacked is catalogued with function name, line number, mutation direction, and guard conditions"
    - "Every SSTORE site that writes to currentPrizePool is catalogued with function name, line number, mutation direction, and guard conditions"
    - "Every SSTORE site that writes to claimablePool / claimableWinnings is catalogued with function name, line number, mutation direction, and guard conditions"
    - "Every SSTORE site that writes to futurePool (via prizePoolsPacked upper 128 bits) is catalogued"
    - "Every SSTORE site that writes to resumeEthPool and yieldAccumulator is catalogued"
    - "Intermediary variables (memory locals, packed fields) that hold ETH amounts during computation are identified and traced"
    - "Each SSTORE site has a verdict: SAFE (correct direction, correct guard) or VULNERABLE (incorrect mutation or missing guard)"
  artifacts:
    - path: ".planning/phases/216-pool-eth-accounting/216-02-POOL-MUTATION-SSTORE.md"
      provides: "Complete SSTORE catalogue for all ETH-touching state variables"
      contains: "POOL-02"
  key_links:
    - from: "_setPrizePools(next, future)"
      to: "prizePoolsPacked"
      via: "packed SSTORE"
      pattern: "_setPrizePools"
    - from: "_setCurrentPrizePool(val)"
      to: "currentPrizePool"
      via: "uint128 narrowing SSTORE"
      pattern: "_setCurrentPrizePool"
    - from: "_addClaimableEth() / _creditClaimable()"
      to: "claimableWinnings mapping"
      via: "mapping SSTORE"
      pattern: "claimableWinnings"
    - from: "claimablePool += / -="
      to: "claimablePool"
      via: "aggregate tracking SSTORE"
      pattern: "claimablePool"
---

<objective>
Catalogue every SSTORE site touching prize pool / claimable pool / future pool and all ETH-touching intermediaries (POOL-02).

Purpose: Produce an exhaustive catalogue of every storage write that modifies ETH-denominated state. Per D-04, this goes beyond the three named pools to include intermediary variables (memory batches, packed fields, local variables) that hold ETH amounts during computation. Each site is classified by mutation direction (increase/decrease/set) and guard conditions. Per D-01, fresh from scratch. Per D-02, Phase 214 can be cited as supporting evidence.

Output: 216-02-POOL-MUTATION-SSTORE.md -- per-contract SSTORE catalogue with verdicts for all ETH-touching state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/216-pool-eth-accounting/216-CONTEXT.md

@.planning/phases/213-delta-extraction/213-DELTA-EXTRACTION.md (EF chain definitions -- identifies which contracts touch pools)

Phase 214 can be cited as supporting evidence per D-02:
@.planning/phases/214-adversarial-audit/214-02-ACCESS-OVERFLOW.md (uint128 narrowing safety verdicts)
@.planning/phases/214-adversarial-audit/214-03-STATE-COMPOSITION.md (packed field integrity verdicts)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Catalogue all SSTORE sites for named pools and packed storage</name>
  <files>.planning/phases/216-pool-eth-accounting/216-02-POOL-MUTATION-SSTORE.md</files>
  <read_first>
    contracts/storage/DegenerusGameStorage.sol (lines 337-396 for currentPrizePool, claimablePool, claimableWinnings, prizePoolsPacked definitions; lines 362-370 for prizePoolsPacked; lines 693-821 for all _set/_get pool helpers; line 1020 for resumeEthPool)
    contracts/modules/DegenerusGameAdvanceModule.sol (full file -- 1682 lines -- this is the primary pool mutation contract; key areas: lines 620-800 _consolidatePoolsAndRewardJackpots, line 789 SSTORE batch, lines 156-600 advanceGame main flow)
    contracts/modules/DegenerusGameJackpotModule.sol (lines 310-550 payDailyJackpot + payDailyJackpotCoinAndTickets; lines 716-760 distributeYieldSurplus; lines 764-850 _addClaimableEth; lines 1182-1280 _processDailyEth; lines 1404-1500 _handleSoloBucketWinner; lines 1977-2158 runBafJackpot)
    contracts/modules/DegenerusGameDecimatorModule.sol (lines 307-400 claimDecimatorJackpot; lines 779-889 claimTerminalDecimatorJackpot -- look for _creditClaimable and claimablePool writes)
    contracts/modules/DegenerusGameDegeneretteModule.sol (lines 511-560 _collectBetFunds; lines 684-750 _distributePayout -- look for claimablePool and claimableWinnings writes)
    contracts/modules/DegenerusGameMintModule.sol (lines 903-950 _purchaseFor; lines 875-900 _purchaseCoinFor; lines 1396-1500 _purchaseBurnieLootboxFor -- look for futurePool, currentPrizePool writes)
    contracts/modules/DegenerusGameWhaleModule.sol (lines 187-250 purchaseWhaleBundle; lines 380-420 purchaseLazyPass; lines 538-580 purchaseDeityPass -- look for pool writes)
    contracts/modules/DegenerusGameGameOverModule.sol (lines 79-248 entire file -- handleGameOverDrain, handleFinalSweep, _sendToVault, _sendStethFirst)
    contracts/DegenerusGame.sol (lines 1366-1430 _claimWinningsInternal + claimAffiliateDgnrs -- claimableWinnings reads/writes)
  </read_first>
  <action>
Systematically read every contract listed in read_first. For each file, search for every write to an ETH-denominated storage variable. The target variables are:

**Named pool storage (direct SSTORE targets):**
1. prizePoolsPacked (contains nextPool:uint128 | futurePool:uint128) -- written via _setPrizePools or direct assembly
2. currentPrizePool (uint128) -- written via _setCurrentPrizePool or direct assignment
3. claimablePool (uint128, same slot as currentPrizePool) -- written via direct +=/-= assignment
4. claimableWinnings (mapping(address => uint256)) -- written via direct assignment per recipient
5. resumeEthPool (uint128) -- written for two-call split carry
6. yieldAccumulator (uint256) -- written during yield surplus distribution
7. levelPrizePool (mapping) -- per-level pool snapshots

**Intermediary variables per D-04:**
8. Memory locals in _consolidatePoolsAndRewardJackpots (memFuture, memNext, memCurrent, memYieldAcc) -- these hold ETH during computation before writeback
9. Packed fields that contain ETH amounts (any bits in prizePoolsPacked, presaleStatePacked, etc.)
10. Function return values that carry ETH amounts between functions (e.g., runBafJackpot claimableDelta)

Produce 216-02-POOL-MUTATION-SSTORE.md with these sections:

**Section 1: Storage Variable Inventory**
Table of all ETH-denominated storage variables with:
| Variable | Type | Storage Slot | Packing | Written By (contract list) |

**Section 2: Per-Contract SSTORE Catalogue**
For each contract that writes ETH-touching state, produce a sub-section:

**2.1: DegenerusGameStorage.sol -- Helper Functions**
Catalogue the _set* helper functions that mediate most pool writes:
- _setPrizePools(next, future): line numbers, what it writes, packing logic
- _setCurrentPrizePool(val): line numbers, uint128 narrowing
- _setPendingPools(next, future): line numbers
- _setNextPrizePool(val), _setFuturePrizePool(val): line numbers

**2.2: DegenerusGameAdvanceModule.sol**
For every SSTORE site in this contract:
| Line | Variable Written | Direction | Amount Source | Guard | Verdict |

Focus on _consolidatePoolsAndRewardJackpots: identify every assignment to memFuture/memNext/memCurrent/memYieldAcc, then the final _setPrizePools/_setCurrentPrizePool writeback.

**2.3: DegenerusGameJackpotModule.sol**
Same table format. Cover:
- _addClaimableEth: claimableWinnings[recipient] += amount, claimablePool += amount
- _processDailyEth: any pool deductions
- _handleSoloBucketWinner: any pool writes
- runBafJackpot: pool deduction + claimable credit
- distributeYieldSurplus: yieldAccumulator deduction, claimableWinnings credits

**2.4: DegenerusGameDecimatorModule.sol**
_creditClaimable: claimableWinnings += and claimablePool +=

**2.5: DegenerusGameDegeneretteModule.sol**
_distributePayout -> _creditClaimable, _collectBetFunds pool writes

**2.6: DegenerusGameMintModule.sol**
_purchaseFor pool allocation writes

**2.7: DegenerusGameWhaleModule.sol**
Pass purchase pool writes

**2.8: DegenerusGameGameOverModule.sol**
handleGameOverDrain pool drains, handleFinalSweep terminal writes

**2.9: DegenerusGame.sol (non-delegatecall)**
_claimWinningsInternal: claimableWinnings[player] -= amount, claimablePool -= amount

**Section 3: Intermediary Variable Tracking (per D-04)**
For every memory local or function return value that holds ETH during computation:
| Function | Variable | Type | Lifetime | Written Back To | Risk |

Focus on:
- _consolidatePoolsAndRewardJackpots memory locals (memFuture, memNext, memCurrent, memYieldAcc)
- runBafJackpot claimableDelta return value
- _processDailyEth intermediate calculations
- Any uint128-narrowed intermediate values

**Section 4: SSTORE Verdict Summary**
Master table across all contracts:
| # | Contract | Function | Line | Variable | Direction | Guard | Verdict |

Count: total SSTORE sites found, SAFE count, VULNERABLE count, INFO count.

For each SSTORE site, verdict is:
- SAFE: correct direction (increase on inflow, decrease on outflow), proper guard (access control, overflow check, invariant maintained)
- VULNERABLE: incorrect direction, missing guard, or ETH can be created/destroyed
- INFO: observation worth noting but not a vulnerability (e.g., uint128 narrowing that is provably safe)

Per D-02, cite Phase 214 verdicts (214-02 for uint128 narrowing safety, 214-03 for packed field integrity) as supporting evidence where they apply.
  </action>
  <verify>
    <automated>grep -c "SAFE\|VULNERABLE\|INFO" .planning/phases/216-pool-eth-accounting/216-02-POOL-MUTATION-SSTORE.md</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/216-pool-eth-accounting/216-02-POOL-MUTATION-SSTORE.md exists
    - Contains "Section 1: Storage Variable Inventory" with all 7+ named pool variables
    - Contains "Section 2: Per-Contract SSTORE Catalogue" with sub-sections 2.1 through 2.9
    - Contains "Section 3: Intermediary Variable Tracking" per D-04 (memory locals, return values)
    - Contains "Section 4: SSTORE Verdict Summary" with master table
    - Every SSTORE site has: line number, variable name, direction, guard, verdict
    - Contains verdicts for _setPrizePools, _setCurrentPrizePool, claimablePool, claimableWinnings writes
    - Contains intermediary tracking for _consolidatePoolsAndRewardJackpots memory locals
    - Contains total counts: SSTORE sites, SAFE, VULNERABLE, INFO
    - References Phase 214 verdicts (214-02, 214-03) as supporting evidence per D-02
    - Does NOT reference prior pool audit artifacts (phases 183-187, 199-200) per D-01
  </acceptance_criteria>
  <done>Every SSTORE site touching ETH-denominated state is catalogued with line numbers, mutation direction, guards, and verdicts; intermediary variables tracked per D-04; master summary with counts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| memory locals -> SSTORE writeback | Computation in memory must faithfully write back to storage |
| claimableWinnings += amount | Credit must match pool deduction to prevent ETH creation |
| uint256 -> uint128 narrowing | Pool values narrowed during storage must not silently truncate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-216-05 | Tampering | prizePoolsPacked packing | mitigate | Verify _setPrizePools correctly packs next (lower 128) and future (upper 128) with no bit overlap |
| T-216-06 | Tampering | currentPrizePool uint128 narrowing | mitigate | Verify uint128(val) narrowing is safe (cite 214-02 verdict: max ETH << 2^128) |
| T-216-07 | Tampering | claimablePool += without matching pool deduction | mitigate | Every _addClaimableEth / _creditClaimable must show corresponding pool deduction |
| T-216-08 | Repudiation | Intermediary variables not written back | mitigate | Verify every memory local in _consolidatePoolsAndRewardJackpots is written back to storage |
</threat_model>

<verification>
- 216-02-POOL-MUTATION-SSTORE.md catalogues every SSTORE site across all 9 contract sections
- Intermediary variables tracked (memory locals, packed fields, return values)
- Each site has line number, direction, guard, verdict
- Master summary table with total counts
- No references to prior pool audit artifacts (D-01)
</verification>

<success_criteria>
Every SSTORE site touching prize pool / claimable pool / future pool and all ETH-holding intermediaries is catalogued with mutation direction, guards, and per-site verdicts.
</success_criteria>

<output>
After completion, create `.planning/phases/216-pool-eth-accounting/216-02-SUMMARY.md`
</output>
