---
phase: 216-pool-eth-accounting
plan: 03
type: execute
wave: 2
depends_on: [216-02]
files_modified:
  - .planning/phases/216-pool-eth-accounting/216-03-CROSS-MODULE-FLOWS.md
autonomous: true
requirements: [POOL-03]

must_haves:
  truths:
    - "Jackpot payout flows (daily, solo bucket, BAF) are traced end-to-end from pool deduction through _addClaimableEth to eventual player claim, with ETH amounts verified at each handoff"
    - "Redemption flow (GNRUS.burn) is traced from burn call through proportional calculation to ETH+stETH transfer, with amounts verified"
    - "Sweep flows (handleFinalSweep 33/33/34 split, yearSweep 50/50 split) are traced from pool drain to external transfer, with amounts verified"
    - "Gameover drain (handleGameOverDrain) is traced through terminal jackpots, BAF, decimator to final _creditClaimable, with ETH amounts verified at each step"
    - "Every cross-module handoff has an ETH amount verification: amount leaving module A = amount entering module B"
    - "The SSTORE catalogue from Plan 02 is referenced to confirm each write in the flow matches the catalogue"
  artifacts:
    - path: ".planning/phases/216-pool-eth-accounting/216-03-CROSS-MODULE-FLOWS.md"
      provides: "Cross-module ETH flow verification for jackpot, redemption, and sweep paths"
      contains: "POOL-03"
  key_links:
    - from: "AdvanceModule._consolidatePoolsAndRewardJackpots()"
      to: "JackpotModule.payDailyJackpot()"
      via: "delegatecall after pool consolidation"
      pattern: "payDailyJackpot"
    - from: "JackpotModule._addClaimableEth()"
      to: "Game._claimWinningsInternal()"
      via: "claimableWinnings mapping bridge"
      pattern: "claimableWinnings"
    - from: "GNRUS.burn()"
      to: "Game.claimWinnings(address(this))"
      via: "external call from GNRUS to Game"
      pattern: "claimWinnings"
    - from: "GameOverModule.handleFinalSweep()"
      to: "_sendToVault() / _sendStethFirst()"
      via: "33/33/34 split"
      pattern: "_sendToVault|_sendStethFirst"
---

<objective>
Verify cross-module ETH flows for jackpot payouts, redemption, and sweep paths (POOL-03).

Purpose: Trace ETH amounts at every cross-module handoff to verify no ETH is created, destroyed, or misrouted during multi-contract flows. References the SSTORE catalogue from Plan 02 to confirm each storage write in the flow matches the expected mutation. Per D-01, fresh from scratch. Per D-02, Phase 214 can be cited as supporting evidence.

Output: 216-03-CROSS-MODULE-FLOWS.md -- end-to-end flow traces with ETH amount verification at each handoff for all payout, redemption, and sweep paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/216-pool-eth-accounting/216-CONTEXT.md

@.planning/phases/213-delta-extraction/213-DELTA-EXTRACTION.md (EF chain definitions)

Plan 02 deliverable (SSTORE catalogue -- dependency):
@.planning/phases/216-pool-eth-accounting/216-02-POOL-MUTATION-SSTORE.md

Phase 214 can be cited as supporting evidence per D-02:
@.planning/phases/214-adversarial-audit/214-01-REENTRANCY-CEI.md (CEI ordering for claim paths)
@.planning/phases/214-adversarial-audit/214-03-STATE-COMPOSITION.md (state corruption verdicts)
@.planning/phases/214-adversarial-audit/214-05-ATTACK-CHAINS-CALLGRAPH.md (call graphs for ETH-flow entry points)

@docs/JACKPOT-PAYOUT-REFERENCE.md (jackpot payout flow -- primary reference)
@docs/JACKPOT-EVENT-CATALOG.md (event emissions for tracing)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace jackpot payout and gameover drain flows cross-module</name>
  <files>.planning/phases/216-pool-eth-accounting/216-03-CROSS-MODULE-FLOWS.md</files>
  <read_first>
    .planning/phases/216-pool-eth-accounting/216-02-POOL-MUTATION-SSTORE.md (Plan 02 SSTORE catalogue -- needed to cross-reference each write)
    docs/JACKPOT-PAYOUT-REFERENCE.md (complete file -- jackpot payout flow reference)
    docs/JACKPOT-EVENT-CATALOG.md (complete file -- event catalog for tracing)
    contracts/modules/DegenerusGameAdvanceModule.sol (lines 156-600 advanceGame main flow; lines 620-800 _consolidatePoolsAndRewardJackpots including delegatecalls to jackpot/decimator)
    contracts/modules/DegenerusGameJackpotModule.sol (lines 310-550 payDailyJackpot + payDailyJackpotCoinAndTickets; lines 764-850 _addClaimableEth; lines 1182-1280 _processDailyEth; lines 1404-1500 _handleSoloBucketWinner; lines 1977-2158 runBafJackpot)
    contracts/modules/DegenerusGameDecimatorModule.sol (lines 307-400 claimDecimatorJackpot; lines 779-889 claimTerminalDecimatorJackpot)
    contracts/modules/DegenerusGameGameOverModule.sol (lines 79-248 entire file -- handleGameOverDrain, handleFinalSweep)
    contracts/DegenerusGame.sol (lines 1366-1430 _claimWinningsInternal + claimAffiliateDgnrs)
  </read_first>
  <action>
Read every cross-module ETH flow path from source. For each flow, trace the exact ETH amount at every module boundary crossing and verify the amount is preserved.

Produce the first half of 216-03-CROSS-MODULE-FLOWS.md with these sections:

**Section 1: Flow Methodology**
Explain the verification approach:
- Each flow is traced as a sequence of module-to-module handoffs
- At each handoff, the ETH amount is recorded from both the sending side (what leaves) and receiving side (what arrives)
- Each storage write is cross-referenced against the SSTORE catalogue from Plan 02 (216-02-POOL-MUTATION-SSTORE.md)
- Verdict per flow: VERIFIED (amounts match at every handoff) or MISMATCH (discrepancy found)

**Section 2: Daily Jackpot Flow (EF-04, EF-05)**
Trace the complete daily jackpot lifecycle:

Step 1: AdvanceModule.advanceGame() -> _consolidatePoolsAndRewardJackpots()
- Show where daily jackpot processing begins (delegatecall to JackpotModule)
- Show the two-call split: what happens in CALL1 vs CALL2
- Amount at entry: currentPrizePool slice (6-14% on days 1-4, 100% on day 5)

Step 2: JackpotModule.payDailyJackpot() -> _processDailyEth()
- Show the winner determination and ETH division among winners
- Amount at handoff: total daily ETH to distribute

Step 3: _processDailyEth() -> _addClaimableEth() (per winner)
- Show each winner receiving their share
- Amount at handoff: per-winner ETH amount
- Cross-reference: _addClaimableEth writes to claimableWinnings[winner] -- match against SSTORE catalogue

Step 4: Solo bucket flow (EF-05): _handleSoloBucketWinner() -> _addClaimableEth()
- Show when solo bucket triggers (entire bucket to one winner)
- Additional whale pass and DGNRS reward side effects
- Amount at handoff: full bucket amount to winner

Verify: sum of all _addClaimableEth credits = currentPrizePool slice deducted

**Section 3: BAF Jackpot Flow (EF-06)**
Trace the BAF jackpot lifecycle:

Step 1: AdvanceModule._consolidatePoolsAndRewardJackpots() -> JackpotModule.runBafJackpot() (delegatecall)
- Show the self-call guard
- Amount at entry: BAF pool amount

Step 2: runBafJackpot() -> _addClaimableEth() (per winner)
- Show winner selection and ETH division
- Amount at handoff: per-winner share
- Show claimableDelta return value

Step 3: Back in AdvanceModule: claimableDelta applied
- Show how the returned delta is used

Verify: claimableDelta = sum of _addClaimableEth credits = BAF pool deduction

**Section 4: Decimator Jackpot Flows (EF-07, EF-08)**
Trace both decimator claim paths:

EF-07: DecimatorModule.claimDecimatorJackpot()
- Entry: player calls claim, level validates
- Pool deduction: currentPrizePool or claimablePool source
- _creditClaimable() call: amount credited to player
- Cross-reference: claimablePool SSTORE in catalogue

EF-08: DecimatorModule.claimTerminalDecimatorJackpot()
- Entry: terminal condition (<=7 days blocked)
- Multiplier calculation: show exact formula
- _creditClaimable() call: multiplied amount credited
- Cross-reference: claimablePool SSTORE in catalogue

Verify: amount deducted from pool = amount credited via _creditClaimable

**Section 5: Gameover Drain Flow (EF-10)**
Trace the complete gameover lifecycle:

Step 1: GameOverModule.handleGameOverDrain() entry
- RNG gate defense-in-depth check
- gameOverStatePacked state read

Step 2: Terminal jackpots within drain
- Which jackpots fire (daily, BAF, decimator)
- ETH amounts for each

Step 3: _creditClaimable() for remaining pools
- Final claimable credit
- What pools are zeroed out

Step 4: State finalization
- gameOverStatePacked updates
- What ETH remains after drain

Verify: all pool balances before drain = total credited to claimable + any retained residual

For EVERY handoff include:
- Source contract + function + line number
- Destination contract + function + line number
- ETH amount (symbolic or formula)
- SSTORE catalogue reference (e.g., "matches SSTORE #17 in 216-02 catalogue")
- Verdict: VERIFIED or MISMATCH
  </action>
  <verify>
    <automated>grep -c "VERIFIED\|MISMATCH" .planning/phases/216-pool-eth-accounting/216-03-CROSS-MODULE-FLOWS.md</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/216-pool-eth-accounting/216-03-CROSS-MODULE-FLOWS.md exists
    - Contains "Section 1: Flow Methodology" explaining handoff verification approach
    - Contains "Section 2: Daily Jackpot Flow" covering EF-04 and EF-05 with multi-step trace
    - Contains "Section 3: BAF Jackpot Flow" covering EF-06 with claimableDelta return
    - Contains "Section 4: Decimator Jackpot Flows" covering EF-07 and EF-08
    - Contains "Section 5: Gameover Drain Flow" covering EF-10 with terminal jackpots
    - Every handoff has source/destination contract+function+line
    - Contains cross-references to 216-02 SSTORE catalogue
    - Contains "VERIFIED" or "MISMATCH" verdicts for each flow
    - Does NOT reference prior pool audit artifacts (phases 183-187, 199-200) per D-01
  </acceptance_criteria>
  <done>Jackpot payout flows (daily, solo, BAF), decimator claims, and gameover drain traced cross-module with ETH amounts verified at every handoff against SSTORE catalogue</done>
</task>

<task type="auto">
  <name>Task 2: Trace redemption, sweep, and claim flows; produce phase synthesis</name>
  <files>.planning/phases/216-pool-eth-accounting/216-03-CROSS-MODULE-FLOWS.md</files>
  <read_first>
    .planning/phases/216-pool-eth-accounting/216-03-CROSS-MODULE-FLOWS.md (sections 1-5 from Task 1)
    .planning/phases/216-pool-eth-accounting/216-02-POOL-MUTATION-SSTORE.md (SSTORE catalogue for cross-referencing)
    .planning/phases/216-pool-eth-accounting/216-01-ETH-CONSERVATION.md (conservation proof for synthesis)
    contracts/GNRUS.sol (lines 282-340 for burn; line 340 for burnAtGameOver)
    contracts/DegenerusGame.sol (lines 1366-1430 for _claimWinningsInternal; lines 1393-1430 for claimAffiliateDgnrs)
    contracts/modules/DegenerusGameGameOverModule.sol (lines 188-248 for handleFinalSweep, _sendToVault, _sendStethFirst)
    contracts/DegenerusStonk.sol (lines 304-360 for yearSweep)
    contracts/StakedDegenerusStonk.sol (lines 455-500 for burnAtGameOver)
    contracts/modules/DegenerusGameDegeneretteModule.sol (lines 511-560 _collectBetFunds; lines 684-750 _distributePayout)
    contracts/modules/DegenerusGameAdvanceModule.sol (lines 207-250 coinflip.creditFlip calls; line 433 creditFlip; line 776 creditFlip)
  </read_first>
  <action>
Read every remaining cross-module flow and append sections 6-10 to the existing 216-03-CROSS-MODULE-FLOWS.md.

**Section 6: GNRUS Redemption Flow (EF-13)**
Trace the complete redemption lifecycle:

Step 1: GNRUS.burn(amount) entry
- Show the proportional calculation: burner's GNRUS / total GNRUS supply
- Show how ETH share and stETH share are calculated

Step 2: GNRUS -> Game.claimWinnings(address(this)) callback
- Show the external call from GNRUS contract to Game contract
- Show how Game handles the claim for the GNRUS contract address
- Amount at handoff: claimableWinnings[GNRUS_address]

Step 3: Game._claimWinningsInternal for GNRUS
- Show claimableWinnings deduction and claimablePool deduction
- Show ETH transfer to GNRUS contract
- GNRUS forwards proportional share to burner

Verify: amount burned proportionally = ETH received from Game + stETH received directly

**Section 7: Final Sweep Flow (EF-11)**
Trace the terminal sweep:

Step 1: GameOverModule.handleFinalSweep() entry
- 30-day delay check
- gameOverStatePacked finalSwept flag check
- Remaining contract balance calculation

Step 2: _sendToVault(amount, stethBal)
- 33/33/34 split: sDGNRS address / VAULT address / GNRUS address
- stETH-first strategy via _sendStethFirst

Step 3: _sendStethFirst(to, amount, stethBal) per recipient
- Show stETH transfer first, then ETH remainder
- Amount at each recipient: exactly 33% or 34% of total

Verify: sum of all three recipient amounts = total contract balance at sweep time

**Section 8: Year Sweep Flow (EF-19)**
Step 1: DegenerusStonk.yearSweep() entry
- 1-year post-gameover guard (SweepNotReady)
- NothingToSweep guard
- Contract balance calculation

Step 2: 50/50 split to GNRUS + VAULT
- Amount to GNRUS address
- Amount to VAULT address

Verify: GNRUS_amount + VAULT_amount = contract balance at sweep time

**Section 9: Remaining Flows**

EF-09 (Degenerette winnings): DegeneretteModule._distributePayout() -> _creditClaimable()
- Trace bet resolution to payout credit
- Cross-reference SSTORE catalogue

EF-12 (Player claim): Game._claimWinningsInternal()
- Trace from claimableWinnings[player] read to ETH send
- Verify: deducted = sent (cite 214-01 CEI ordering)

EF-15 (Affiliate claim): Game.claimAffiliateDgnrs()
- Trace DGNRS claim + coinflip.creditFlip call
- Show cross-contract boundary to BurnieCoinflip

EF-17 (Degenerette bets): DegeneretteModule._collectBetFunds()
- Trace from msg.value / claimable deduction to pool credit
- Show milli-ETH packing in lootboxRngPendingEth

EF-18 (burnAtGameOver): StakedDegenerusStonk.burnAtGameOver()
- Trace remaining pool burns at gameover
- Show gameOver gate

EF-20 (BURNIE flip credit): AdvanceModule coinflip.creditFlip() during consolidation
- Trace the creditFlip call from AdvanceModule to BurnieCoinflip
- Show ETH amount passed

**Section 10: Cross-Module Flow Synthesis**

10.1: Handoff Verification Matrix
| Flow | Source Module | Dest Module | Amount | Handoff Mechanism | SSTORE Cat Ref | Verdict |
Cover all flows from sections 2-9.

10.2: Inter-Contract Call Summary
| Caller | Callee | Call Type | ETH Value | Purpose |
Show every external call or delegatecall that carries or affects ETH.

10.3: Phase 216 Overall Verdict
Synthesize across all three plans (01, 02, 03):
- Plan 01 conservation proof result
- Plan 02 SSTORE catalogue result
- Plan 03 cross-module flow result
- Overall verdict: SOUND (no ETH leaks found) or FINDINGS (issues identified)
- List any INFO observations
- Cite Phase 214 supporting evidence per D-02
  </action>
  <verify>
    <automated>grep -c "EF-" .planning/phases/216-pool-eth-accounting/216-03-CROSS-MODULE-FLOWS.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains "Section 6: GNRUS Redemption Flow" covering EF-13 with proportional calculation
    - File contains "Section 7: Final Sweep Flow" covering EF-11 with 33/33/34 split
    - File contains "Section 8: Year Sweep Flow" covering EF-19 with 50/50 split
    - File contains "Section 9: Remaining Flows" covering EF-09, EF-12, EF-15, EF-17, EF-18, EF-20
    - File contains "Section 10: Cross-Module Flow Synthesis" with handoff matrix
    - All 20 EF chains (EF-01 through EF-20) are accounted for across sections 2-9 (inflows covered in 01 conservation proof, outflows traced here)
    - Contains handoff verification matrix table
    - Contains inter-contract call summary table
    - Contains overall Phase 216 verdict synthesizing all three plans
    - Contains cross-references to 216-02 SSTORE catalogue
    - Contains cross-references to 216-01 conservation proof
    - References Phase 214 (214-01, 214-03, 214-05) as supporting evidence per D-02
    - Does NOT reference prior pool audit artifacts (phases 183-187, 199-200) per D-01
  </acceptance_criteria>
  <done>All cross-module flows traced with ETH amounts verified at every handoff; phase synthesis produced with overall verdict across all three plans</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JackpotModule -> claimableWinnings mapping | ETH credited must match pool deduction |
| GNRUS.burn() -> Game.claimWinnings() | External call crosses contract boundary with ETH |
| GameOverModule -> external transfers | Terminal ETH exits contract permanently |
| AdvanceModule -> BurnieCoinflip.creditFlip() | Cross-contract call with ETH/credit value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-216-09 | Tampering | _addClaimableEth amount vs pool deduction | mitigate | Verify amount credited = amount deducted from source pool at every call site |
| T-216-10 | Tampering | GNRUS proportional calculation | mitigate | Verify burn amount / total supply * claimableWinnings[GNRUS] = ETH transferred |
| T-216-11 | Tampering | handleFinalSweep 33/33/34 split | mitigate | Verify sum of three recipient amounts = total contract balance (no rounding loss or gain) |
| T-216-12 | Denial | handleGameOverDrain terminal jackpots | accept | Terminal jackpots failing would leave ETH in pools -- caught by final sweep as safety net |
| T-216-13 | Information Disclosure | Cross-module ETH amounts | accept | All amounts are on-chain and public by design |
</threat_model>

<verification>
- 216-03-CROSS-MODULE-FLOWS.md traces all payout, redemption, and sweep flows
- Every cross-module handoff has ETH amount verification
- SSTORE catalogue cross-references confirm write correctness
- Phase synthesis covers all three plans with overall verdict
- No references to prior pool audit artifacts (D-01)
</verification>

<success_criteria>
Jackpot payout, redemption, and sweep flows are traced cross-module with ETH amounts verified at each handoff, and the phase synthesis confirms pool ETH accounting is sound across all three audit documents.
</success_criteria>

<output>
After completion, create `.planning/phases/216-pool-eth-accounting/216-03-SUMMARY.md`
</output>
