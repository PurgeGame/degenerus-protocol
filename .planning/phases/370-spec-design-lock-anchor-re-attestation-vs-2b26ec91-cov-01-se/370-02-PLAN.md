---
phase: 370-spec-design-lock-anchor-re-attestation-vs-2b26ec91-cov-01-se
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/370-spec-design-lock-anchor-re-attestation-vs-2b26ec91-cov-01-se/370-02-COV01-ADJUDICATION.md
  - .planning/audit-v52/runs/v59/xmodel/
autonomous: true
requirements: [COV-01]
must_haves:
  truths:
    - "The area-solvency XMODEL leg ran against frozen 2b26ec91 with a SECOND independent model that actually read the frozen source (not a Plan-Mode refusal like the v58 Gemini leg)"
    - "Every claim the second model returned has a Claude verdict recorded — CONFIRMED (routed to fold into the Phase-371 IMPL diff) or REFUTED (logged with the frozen-source reason)"
    - "The COV-01 disposition is recorded for the TERMINAL audit/FINDINGS-v59.0.md, closing the v58.0 area-solvency coverage gap"
  artifacts:
    - path: ".planning/phases/370-spec-design-lock-anchor-re-attestation-vs-2b26ec91-cov-01-se/370-02-COV01-ADJUDICATION.md"
      provides: "The COV-01 second-model area-solvency re-run record + per-claim Claude adjudication + routing dispositions"
      contains: "area-solvency"
      min_lines: 40
    - path: ".planning/audit-v52/runs/v59/xmodel/"
      provides: "The raw second-model area-solvency run output (prompt + model result + council manifest) against 2b26ec91"
  key_links:
    - from: "370-02-COV01-ADJUDICATION.md"
      to: ".planning/audit-v52/runs/v59/xmodel/results/"
      via: "the raw second-model area-solvency output the adjudication verifies against frozen source"
      pattern: "area-solvency"
    - from: "any CONFIRMED COV-01 finding"
      to: "Phase 371 IMPL batched diff"
      via: "routed-to-IMPL disposition"
      pattern: "CONFIRMED|route"
---

<objective>
Close the v58.0 `area-solvency` cross-model coverage gap (the original Gemini solvency leg
REFUSED via Plan Mode — it never read the frozen tree) by re-running the `area-solvency` XMODEL
leg with a SECOND independent model against frozen `2b26ec91` through the `.planning/audit-v52/`
harness, then adjudicating every returned claim against frozen source (Claude owns the verdict,
parallel verification). Any CONFIRMED new solvency finding is routed to fold into the SAME
Phase-371 IMPL batched diff; refuted claims are logged; the disposition is recorded for the
TERMINAL `audit/FINDINGS-v59.0.md`. This runs EARLY (at SPEC) so a confirmed finding can join
IMPL before the milestone relies on the F-03/F-04 corrections.

Purpose: F-03/F-04 are solvency-identity corrections; before relying on them, the spine needs a
second independent model's read (v58 had Codex + composition + Claude on the spine, but the
Gemini solvency leg never ran). The rng-freeze Gemini gap is deliberately NOT re-run — out of
scope (Claude 364/365 + the refuted Codex C1 already deep-cover the freeze).

Output: the raw harness run under `.planning/audit-v52/runs/v59/xmodel/` + the
`370-02-COV01-ADJUDICATION.md` record. Harness + paper only — ZERO `contracts/*.sol`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/audit-v52/RUNBOOK.md
@.planning/audit-v52/LAUNCH.md
@.planning/audit-v52/RUN-CONTRACT-v58.md
@audit/FINDINGS-v58.0.md

<notes>
- The v58 `area-solvency` run already exists at `.planning/audit-v52/runs/v58/xmodel/`:
  the prompt is `prompts/area-solvency.combined.txt` (`_preamble.txt` + `solvency-focus.txt`),
  the driver is `runs/v58/xmodel/run-council.sh`, the council fan-out is
  `.planning/audit-v52/cross-model/bin/council.sh` (→ `ask-gemini.sh` + `ask-codex.sh`).
- WHY the v58 Gemini solvency leg failed (read `results/area-solvency.gemini.txt`): `ask-gemini.sh`
  runs `gemini --approval-mode plan`, and in Plan Mode Gemini CANNOT execute `git show`/`git grep`
  — but the prompt forbids reading the working tree. So Gemini returned a refusal, not a result.
  The Codex leg DID run and produced F-03. So "a SECOND independent model on the spine" means a
  model OTHER than Codex that actually reads frozen `2b26ec91`.
- The executor decides the exact second-model mechanism (this is a harness-operations decision):
  acceptable options include (a) re-run Gemini with the frozen source MATERIALIZED into the prompt
  context (extract the in-scope frozen `*.sol` via `git show 2b26ec91:<path>` into a temp context
  file referenced by the prompt, so Plan Mode reads it as a FILE with no `git` shell needed), or
  (b) drive a different second model the harness/CLIs support that can read the frozen tree. Codex
  alone does NOT satisfy "second independent model" (it was the v58 leg). PONG-smoke-test first.
- Solvency scope (from `solvency-focus.txt`): the invariant
  `bal + stETH >= claimablePool == Σ claimableWinnings + Σ afkingFunding`, the prize-pool ladder
  future→next→current→claimable, the afking debit, the salvage ETH relabel, `distributeYieldSurplus`,
  the redemption segregation. Traps: `Pool.*`/`transferFromPool` move sDGNRS TOKENS not ETH;
  `creditFlip`/BURNIE legs are token emissions not ETH.
- PACING (this is the one heavier leg in Phase 370): run it as a discrete, checkpointable, pausable
  task. Spawn the external model leg with `concurrency: 1`. Persist the raw output to disk the
  moment it returns, then checkpoint (write the partial adjudication + STATE Current Position)
  before doing the verification analysis. If a 5h usage-cap stop is imminent, stop AFTER persisting
  the raw run and BEFORE starting verification — the verification (Task 2) resumes from the on-disk
  raw output, losing nothing. Do not start the external leg if you cannot persist its result.
- The external leg runs in read-only/plan mode (`ask-gemini.sh` uses `--approval-mode plan`,
  Codex equivalently read-only). Prompts go in a FILE, never inline, to avoid the repo's
  contract-commit guard hook.
- This phase touches ZERO `contracts/*.sol`. The harness run dir + the adjudication doc are
  AGENT-committable (no contract-commit gate).
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke-test the council, then run the area-solvency leg with a second independent model against 2b26ec91</name>
  <files>.planning/audit-v52/runs/v59/xmodel/, .planning/phases/370-spec-design-lock-anchor-re-attestation-vs-2b26ec91-cov-01-se/370-02-COV01-ADJUDICATION.md</files>
  <read_first>
    - .planning/audit-v52/RUNBOOK.md ("External council" + the council.sh / ask-gemini.sh / ask-codex.sh mechanics) and LAUNCH.md (the PONG smoke-test + the run pattern) and RUN-CONTRACT-v58.md (the XMODEL second-opinion rules: prompts in a FILE, PONG first, read frozen ONLY via `git show 2b26ec91`)
    - .planning/audit-v52/runs/v58/xmodel/run-council.sh and .planning/audit-v52/cross-model/bin/council.sh + ask-gemini.sh + ask-codex.sh (the actual invocation, the `--approval-mode plan` constraint)
    - .planning/audit-v52/runs/v58/xmodel/prompts/area-solvency.combined.txt and prompts/solvency-focus.txt and prompts/_preamble.txt (the exact solvency prompt to re-use)
    - .planning/audit-v52/runs/v58/xmodel/results/area-solvency.gemini.txt (the Plan-Mode refusal — confirm WHY it failed so you pick a mechanism that won't repeat it) and results/area-solvency.codex.txt (the v58 Codex result you must NOT just re-use — Codex was the v58 leg, so the SECOND model must differ)
  </read_first>
  <action>
    Set up a v59 XMODEL run dir: `.planning/audit-v52/runs/v59/xmodel/` (mkdir `results/` and
    `prompts/`). PONG-smoke-test the council first (per LAUNCH.md §0:
    `printf 'Reply with one word: PONG' > /tmp/ping.txt; bash .planning/audit-v52/cross-model/bin/council.sh --out-dir /tmp/council-ping --label ping /tmp/ping.txt`)
    and record which models answered.
    Re-use the v58 solvency prompt content (`_preamble.txt` + `solvency-focus.txt` → the combined
    `area-solvency` prompt) — copy it into `runs/v59/xmodel/prompts/` so the run is self-contained.
    Then run the `area-solvency` leg with a SECOND independent model (NOT Codex — Codex was the v58
    leg) against frozen `2b26ec91`, choosing the mechanism that avoids the v58 Plan-Mode refusal:
    EITHER materialize the in-scope frozen `*.sol` into a context file the prompt references (so the
    plan-mode model reads frozen source as a FILE, no `git` shell needed — this is the robust path
    given the refusal cause) OR drive whatever second model the CLIs support that can read the frozen
    tree. Persist the raw model output + a council manifest under `runs/v59/xmodel/results/` the
    moment it returns (this is the pacing checkpoint — write to disk, update STATE Current Position,
    THEN proceed). If the chosen model still refuses or returns empty, record that as a coverage note
    and fall back to the alternate mechanism; do not leave the leg silently empty.
    Pacing: concurrency 1, prompts in a FILE (never inline), read-only/plan mode for the external
    model. If a usage-cap stop is imminent, stop after the raw output is on disk (Task 2 resumes
    from it).
    Begin `370-02-COV01-ADJUDICATION.md` with: the smoke-test result, the second model used + the
    mechanism chosen (and WHY — referencing the v58 Plan-Mode refusal cause), the frozen SHA
    `2b26ec91`, and a pointer to the raw `runs/v59/xmodel/results/` output. Do NOT yet adjudicate —
    that is Task 2.
  </action>
  <verify>
    <automated>test -d .planning/audit-v52/runs/v59/xmodel/results && find .planning/audit-v52/runs/v59/xmodel/results -type f -name '*solvency*' -size +1c | grep -q . && grep -q '2b26ec91' .planning/phases/370-spec-design-lock-anchor-re-attestation-vs-2b26ec91-cov-01-se/370-02-COV01-ADJUDICATION.md</automated>
  </verify>
  <acceptance_criteria>
    A non-empty `area-solvency` second-model result against `2b26ec91` exists on disk under
    `.planning/audit-v52/runs/v59/xmodel/results/` (a real read of the frozen source, NOT a Plan-Mode
    refusal), produced by a model OTHER than the v58 Codex leg; the adjudication doc records the
    smoke-test outcome, the second model + the mechanism chosen (with the v58-refusal reason) and the
    frozen SHA. The raw output is persisted to disk before any verification begins (the pacing
    checkpoint).
  </acceptance_criteria>
  <done>v59 XMODEL run dir created, PONG smoke-test recorded, area-solvency leg run with a genuine second independent model against 2b26ec91, raw output persisted, adjudication doc opened with the run record.</done>
</task>

<task type="auto">
  <name>Task 2: Adjudicate every returned claim against frozen source + record routing dispositions</name>
  <files>.planning/phases/370-spec-design-lock-anchor-re-attestation-vs-2b26ec91-cov-01-se/370-02-COV01-ADJUDICATION.md</files>
  <read_first>
    - the raw second-model output you persisted in Task 1 (`.planning/audit-v52/runs/v59/xmodel/results/area-solvency.*`)
    - the frozen source for every cited solvency path, read via `git show 2b26ec91:<path>` and `git grep -n <token> 2b26ec91 -- contracts/` — the prize-pool ladder, the afking debit, the salvage relabel, `distributeYieldSurplus`, `_queueWhalePassClaimCore` / the decimator remainder (so you can tell a NEW claim apart from F-03/F-04 already in scope)
    - audit/FINDINGS-v58.0.md (so an already-known claim — F-03/F-04 re-found, or a v58 REFUTED claim like A2/B3 — is dispositioned as already-adjudicated, not re-opened)
    - the v58 Codex solvency result `.planning/audit-v52/runs/v58/xmodel/results/area-solvency.codex.txt` (to dedup the second model's claims against what Codex already raised)
  </read_first>
  <action>
    For EACH claim the second model returned, parallel-verify it against frozen `2b26ec91` source
    (Claude owns the verdict — read the cited path, confirm or refute). Record a per-claim row in
    `370-02-COV01-ADJUDICATION.md`: claim title + the model's `file:line` cite · the AS-FOUND frozen
    code · Claude verdict (CONFIRMED / REFUTED / ALREADY-KNOWN) · the frozen-source reason · the
    disposition. Dispositions:
    - CONFIRMED + NEW (a genuine solvency-identity break not already F-03/F-04) → mark "ROUTE TO
      Phase-371 IMPL" with the one-line fix sketch, so it folds into the SAME batched diff; also flag
      it to the 370-01 design-lock so the edit-order map can absorb it.
    - CONFIRMED but = F-03 or F-04 (corroboration) → "ALREADY IN SCOPE — corroborates F-03/F-04",
      strengthens the close, no new IMPL work.
    - REFUTED → log the frozen-source reason (and note if it matches a v58 REFUTED claim like A2/B3).
    End with a disposition summary: total claims · CONFIRMED-new (routed to 371) · CONFIRMED-known
    (corroborations) · REFUTED · and the verdict line: "COV-01 area-solvency coverage gap CLOSED —
    a second independent model read frozen 2b26ec91; net new solvency findings for IMPL = K." Write a
    one-paragraph carry-forward note for the TERMINAL (374) `audit/FINDINGS-v59.0.md` recording the
    COV-01 result so AUDIT-01 can fold it in.
    If K > 0 (a confirmed new finding), the 370-01 design-lock SPEC plan must absorb it into the IMPL
    edit order — note that cross-dependency explicitly in this doc and in the SUMMARY.
  </action>
  <verify>
    <automated>grep -Eiq 'CONFIRMED|REFUTED' .planning/phases/370-spec-design-lock-anchor-re-attestation-vs-2b26ec91-cov-01-se/370-02-COV01-ADJUDICATION.md && grep -Eiq 'coverage gap|CLOSED|disposition' .planning/phases/370-spec-design-lock-anchor-re-attestation-vs-2b26ec91-cov-01-se/370-02-COV01-ADJUDICATION.md && grep -q '2b26ec91' .planning/phases/370-spec-design-lock-anchor-re-attestation-vs-2b26ec91-cov-01-se/370-02-COV01-ADJUDICATION.md</automated>
  </verify>
  <acceptance_criteria>
    Every claim the second model returned has a Claude verdict (CONFIRMED / REFUTED / ALREADY-KNOWN)
    recorded against the AS-FOUND frozen `2b26ec91` code with the reason; CONFIRMED-new claims carry a
    "ROUTE TO Phase-371 IMPL" disposition + fix sketch (and a cross-note to the 370-01 design-lock);
    the doc ends with the disposition summary + the "coverage gap CLOSED, net new findings = K" verdict
    and a carry-forward note for the TERMINAL FINDINGS-v59.0. No claim is left un-adjudicated.
  </acceptance_criteria>
  <done>Per-claim adjudication complete against frozen 2b26ec91, every claim verdicted + dispositioned, CONFIRMED-new claims routed to Phase-371, disposition summary + TERMINAL carry-forward note written; COV-01 coverage gap recorded CLOSED.</done>
</task>

</tasks>

<threat_model>
Harness + paper-only phase — no new attack surface introduced. The external model leg runs in
read-only / plan mode (`ask-gemini.sh --approval-mode plan`, Codex equivalently read-only) and
reads ONLY frozen `2b26ec91` source; prompts go in a FILE (never inline) to avoid the repo's
contract-commit guard hook. The analyzed surface is the SOLVENCY-01 identity
(`claimablePool == Σ claimableWinnings + Σ afkingFunding`) — re-attested as a DESIGN INPUT (a
second independent model's read closing the v58 coverage gap), not modified here. Any CONFIRMED
finding is routed to Phase-371, where the actual contract change (and its security review) lives.
No package installs. No contract edits in this plan.
</threat_model>

<verification>
- `.planning/audit-v52/runs/v59/xmodel/results/` holds a non-empty area-solvency second-model
  result against `2b26ec91` (a genuine source read, not a Plan-Mode refusal).
- The second model is NOT the v58 Codex leg (genuine second independent model).
- Every returned claim has a CONFIRMED / REFUTED / ALREADY-KNOWN verdict against frozen source.
- CONFIRMED-new claims carry a route-to-371 disposition.
- The disposition summary + TERMINAL carry-forward note are present.
- ZERO `contracts/*.sol` modified (`git status --porcelain contracts/` empty for this plan).
</verification>

<success_criteria>
ROADMAP Phase 370 Success Criterion 4 is satisfied: the `area-solvency` leg is re-run with a
second independent model against frozen `2b26ec91`, every claim is adjudicated against frozen
source with Claude owning the verdict, CONFIRMED-new findings are routed to fold into the
Phase-371 IMPL diff, refuted claims are logged, and the disposition is recorded for the TERMINAL
`audit/FINDINGS-v59.0.md` — closing the v58.0 area-solvency coverage gap before the milestone
relies on the F-03/F-04 corrections.
</success_criteria>

<output>
Create `.planning/phases/370-spec-design-lock-anchor-re-attestation-vs-2b26ec91-cov-01-se/370-02-SUMMARY.md` when done.
</output>
