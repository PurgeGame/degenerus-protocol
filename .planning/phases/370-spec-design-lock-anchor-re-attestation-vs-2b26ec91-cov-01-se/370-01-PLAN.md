---
phase: 370-spec-design-lock-anchor-re-attestation-vs-2b26ec91-cov-01-se
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/370-spec-design-lock-anchor-re-attestation-vs-2b26ec91-cov-01-se/370-01-SPEC.md
autonomous: true
requirements: [SPEC-01]
must_haves:
  truths:
    - "Every cited file:line anchor (SALV / AFAFF / SOLV / PRESALE + Change-A/B/C) is grep-verified against frozen 2b26ec91 with as-cited / as-found / drift columns — no 'by construction' assertion survives un-grepped"
    - "Exactly one F-03 / SOLV-01 fix variant is chosen and recorded with a written rationale citing the frozen function bodies"
    - "The producer-before-consumer batched-diff edit order is mapped across all 9 IMPL reqs (STREAK-01 before STREAK-02; SOLV-01 return-value before the BAF caller fold)"
    - "The WINDOW-01 pre-edit verification confirms exactly the 6 comparisons flip, the extension + early-renewal math is untouched, and the afking eviction boundary stays inclusive-through-validThroughLevel"
  artifacts:
    - path: ".planning/phases/370-spec-design-lock-anchor-re-attestation-vs-2b26ec91-cov-01-se/370-01-SPEC.md"
      provides: "The v59.0 design-lock SPEC: anchor re-attestation table + F-03 variant lock + edit-order map + WINDOW-01 pre-edit verification"
      contains: "F-03 variant"
      min_lines: 80
  key_links:
    - from: "370-01-SPEC.md anchor table"
      to: "frozen 2b26ec91 source"
      via: "git show / git grep at 2b26ec91"
      pattern: "2b26ec91"
    - from: "370-01-SPEC.md edit-order map"
      to: "Phase 371 IMPL"
      via: "producer-before-consumer ordering of the 9 contract reqs"
      pattern: "STREAK-01.*STREAK-02|SOLV-01.*caller"
---

<objective>
Produce the v59.0 design-lock SPEC document so Phase 371 IMPL authors ONE fully-reconciled
batched contract diff with zero "by construction" assumptions: re-attest every cited
`file:line` anchor against the frozen baseline `2b26ec91`, LOCK the one open F-03 / SOLV-01
fix variant with a written rationale, fix the producer-before-consumer edit order across all
9 IMPL reqs, and complete the WINDOW-01 pre-edit verification.

Purpose: The IMPL diff is producer-before-consumer and touches the solvency spine — it cannot
be authored safely against anchors that have drifted or against an undecided fix variant. This
plan settles those decisions in writing.

Output: `.planning/phases/370-.../370-01-SPEC.md` — a design-lock doc with the anchor
re-attestation table, the F-03 variant decision + rationale, the ordered edit sequence, and
the WINDOW-01 verification. Paper-only — ZERO `contracts/*.sol`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@audit/FINDINGS-v58.0.md
@.planning/PLAN-PASS-STAT-FRONTLOAD.md

<notes>
- FROZEN_SHA = `2b26ec91`. Read source ONLY as `git show 2b26ec91:<path>` and grep ONLY as
  `git grep -n <pattern> 2b26ec91 -- contracts/`. NEVER read the working tree for attestation —
  the working tree is docs-only on top of the frozen contract subject, but attest against the SHA.
- The cited line numbers in FINDINGS-v58.0.md / PLAN-PASS-STAT-FRONTLOAD.md are EXPECTED to drift
  by a few lines (the v58.0 council read frozen source via a context-pack; some cites are
  approximate). Spot-checks already confirmed: the SALV quote-loop `n == 0` check is ~`:174`
  (PLAN says 173-181), the AFAFF `affiliateBase` accrual missing `/ 1 ether` is ~`:879`, the SOLV
  `_queueWhalePassClaimCore` remainder credit + `claimablePool += uint128(remainder)` returning
  nothing is ~`:54-60` of `DegenerusGamePayoutUtils.sol`, the CENTURY x0 gate term is at
  `DegenerusGameWhaleModule.sol:411-416`, the decimator remainder re-credit (no `claimablePool`
  bump) is at `DegenerusGameDecimatorModule.sol:~595`. Re-grep and record the AS-FOUND line.
- Project style: lean docs — no procedural meta. The SPEC explains the fix decisions + the
  attestation result in plain English, not plan-IDs/milestone-history filler.
- This phase touches ZERO `contracts/*.sol`. If a task even appears to edit a contract it is
  mis-scoped — the contract diff is Phase 371.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-attest every cited file:line anchor against frozen 2b26ec91</name>
  <files>.planning/phases/370-spec-design-lock-anchor-re-attestation-vs-2b26ec91-cov-01-se/370-01-SPEC.md</files>
  <read_first>
    - audit/FINDINGS-v58.0.md (the Executive-summary cite column + each CONFIRMED finding's `file:line` — SALV F-01, AFAFF F-02, SOLV F-03/F-04, PRESALE F-05)
    - .planning/PLAN-PASS-STAT-FRONTLOAD.md (the Change-A 6-comparison table + `hasAnyLazyPass` docstring; the Change-B 3 STREAK activation sites + `MINT_STREAK_LAST_COMPLETED_SHIFT` prereq; the Change-C CENTURY gate `DegenerusGameWhaleModule.sol:411-416`)
    - .planning/ROADMAP.md Phase 370 Success Criterion 2 (the authoritative enumerated anchor list) and REQUIREMENTS.md SPEC-01
    - the frozen blobs themselves, read via `git show 2b26ec91:<path>` for each cited file
  </read_first>
  <action>
    Begin `370-01-SPEC.md` with an anchor re-attestation section. For EVERY anchor group below,
    grep/show the frozen blob at `2b26ec91` (`git show 2b26ec91:<path> | sed -n '<range>p'` and/or
    `git grep -n '<token>' 2b26ec91 -- <path>`) and record a table row with columns:
    anchor-as-cited (from FINDINGS/PLAN/ROADMAP) · anchor-as-found-at-2b26ec91 (the real line) ·
    drift (yes/no) · corrected-line-if-drifted · the one-line role of that line.
    Anchor groups (enumerated in ROADMAP SC2 / SPEC-01 part 2):
    (1) SALV — the salvage quote loop `if (n == 0) revert E();` + `faceWei = priceForLevel(L) * n`
        in `DegenerusGameMintStreakUtils.sol` (~173-181), the debit `uint32(quantities[i]) * 4` in
        `DegenerusGameMintModule.sol` (~1013-1015), the holdings guard `_removeFarFutureTickets`
        `if (owed < entries) revert` (~`:1059`).
    (2) AFAFF — `GameAfkingModule.sol:879` `base = (_ethToBurnie(amount, mp) * 7) / 100` (the
        accrual missing `/ 1 ether`) + the two siblings that DO divide: the buyer-bonus
        `bonusWhole = bonusBase / 1 ether` (~`:826`) and slot-0 `QUEST_SLOT0_REWARD / 1 ether`
        (~`:887`).
    (3) SOLV — `_queueWhalePassClaimCore` body `DegenerusGamePayoutUtils.sol:46-65` (the remainder
        credit + `claimablePool += uint128(remainder)` that returns nothing), the BAF caller
        `memFuture -= claimed` in `DegenerusGameAdvanceModule.sol:897-903`, the decimator
        `_creditDecJackpotClaimCore` full-`lootboxPortion` debit `DegenerusGameDecimatorModule.sol:392-399`,
        the whale-branch remainder re-credit `_creditClaimable(winner, remainder)` (~`:596`), and
        the checked-math `claimWinnings` debit (~`:1588`).
    (4) PRESALE — the `subscribe` UPSERT `FLAG_USE_TICKETS` flip with no pre-claim
        `GameAfkingModule.sol:397-398` + the presale-box-credit halving keyed on the live flag
        `:1599-1601`.
    (5) Change-A WINDOW — the 6 comparisons (`DegenerusGameMintModule.sol:291`/`:295`,
        `DegenerusGameMintStreakUtils.sol:262`/`:310`/`:56`, `DegenerusGame.sol:1678` `hasAnyLazyPass`)
        + the stale `hasAnyLazyPass` docstring (`DegenerusGame.sol:~1662-1668`).
    (6) Change-B STREAK — the 3 pass-activation sites (`_activate10LevelPass`
        `DegenerusGameStorage.sol:~1049`, `_applyWhalePassStats` `DegenerusGameStorage.sol:~1134`
        with callers `DegenerusGameWhaleModule.sol:1013` + `DegenerusGameDecimatorModule.sol:592`,
        the inline block in `_purchaseWhaleBundle` `DegenerusGameWhaleModule.sol:264-291`) + the
        `MINT_STREAK_LAST_COMPLETED_SHIFT` local-constant in `DegenerusGameMintStreakUtils.sol:19`
        and its 4 refs.
    (7) Change-C CENTURY — the lazy-pass gate x0 term `DegenerusGameWhaleModule.sol:411-416` + the
        `:409-410` comment + the `purchaseLazyPass` `@dev`/`@custom:reverts` doc.
    Every row must be backed by an actual grep/show against `2b26ec91`. Write a one-line verdict at
    the end of the section: "N anchors attested, M drifted (corrected), 0 unverified."
    Do NOT include fenced code blocks of contract bodies in the SPEC — cite the line and quote the
    single token/identifier, not the implementation.
  </action>
  <verify>
    <automated>test -f .planning/phases/370-spec-design-lock-anchor-re-attestation-vs-2b26ec91-cov-01-se/370-01-SPEC.md && grep -q '2b26ec91' .planning/phases/370-spec-design-lock-anchor-re-attestation-vs-2b26ec91-cov-01-se/370-01-SPEC.md && grep -Eiq 'drift' .planning/phases/370-spec-design-lock-anchor-re-attestation-vs-2b26ec91-cov-01-se/370-01-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    The SPEC contains an anchor re-attestation table with a row for EVERY one of the 7 anchor groups
    above, each row carrying as-cited / as-found-at-2b26ec91 / drift(yes/no) / corrected-line columns,
    and each row is grep-verified against `2b26ec91` (not asserted "by construction"). The section ends
    with the "N attested, M drifted, 0 unverified" verdict line.
  </acceptance_criteria>
  <done>Anchor re-attestation table complete in 370-01-SPEC.md, all 7 groups grep-verified against 2b26ec91, verdict line present.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the F-03 variant + map the producer-before-consumer edit order + complete WINDOW-01 verification</name>
  <files>.planning/phases/370-spec-design-lock-anchor-re-attestation-vs-2b26ec91-cov-01-se/370-01-SPEC.md</files>
  <read_first>
    - The frozen bodies that decide the F-03 / SOLV-01 variant: `git show 2b26ec91:contracts/modules/DegenerusGamePayoutUtils.sol` (the `_queueWhalePassClaimCore` body, ~46-65), `git show 2b26ec91:contracts/modules/DegenerusGameAdvanceModule.sol` (the BAF caller `memFuture -= claimed` + `memFuture` write-back, ~897-968), `git show 2b26ec91:contracts/modules/DegenerusGameDecimatorModule.sol` (the `_creditDecJackpotClaimCore` + whale-branch remainder, ~392-596), and the `_processSoloBucketWinner` push-back pattern referenced in FINDINGS (`JackpotModule.sol:1379-1381`)
    - audit/FINDINGS-v58.0.md F-03 (both fix sketches: return-and-fold-into-`claimableDelta` vs push-remainder-back-to-`futurePrizePool`) and F-04
    - .planning/PLAN-PASS-STAT-FRONTLOAD.md Change-A/B/C (the edit details + the Change-A verification checklist + the afking-eviction-inclusive note)
    - the Task-1 anchor table you just wrote (the edit order references the AS-FOUND lines)
    - re-grep for any OTHER `frozenUntilLevel` window-reader: `git grep -n 'frozenUntilLevel' 2b26ec91 -- contracts/`
  </read_first>
  <action>
    Append three sections to `370-01-SPEC.md`:
    (A) F-03 / SOLV-01 VARIANT LOCK. Evaluate the two candidate fixes against the frozen bodies you
        read: variant (a) return the BAF whale-pass `remainder` from `_queueWhalePassClaimCore` and
        fold it into the BAF caller's `claimableDelta` so `memFuture -= claimed` debits
        `futurePrizePool`; variant (b) push the `remainder` back to `futurePrizePool` via the
        `_processSoloBucketWinner` pattern. DECIDE exactly one (this is the executor's decision to
        make against frozen source — the v59 memory notes lean toward the return-and-fold variant,
        but verify it, do not assume). Record the chosen variant, the exact call sites it touches
        (function name + AS-FOUND line), and a written rationale citing the frozen function bodies
        (why the chosen variant correctly debits `futurePrizePool` without disturbing the lootbox
        leg that adds nothing to `claimableDelta`, and why the rejected variant was rejected).
        Note that SOLV-02 (F-04) is the simple `claimablePool += uint128(remainder);` add alongside
        `_creditClaimable(winner, remainder)` — no variant choice, just confirm the site.
    (B) BATCHED-DIFF EDIT ORDER MAP. List the full ordered edit sequence across all 9 IMPL reqs
        (SALV-01, AFAFF-01, SOLV-01, SOLV-02, PRESALE-01, WINDOW-01, STREAK-01, STREAK-02, CENTURY-01)
        producer-before-consumer: STREAK-01 (`MINT_STREAK_LAST_COMPLETED_SHIFT` promotion into
        `BitPackingLib` + repoint the 4 `MintStreakUtils` refs + delete the local) MUST precede
        STREAK-02 (the `_withPassStreakFrontLoad` helper that reads `BitPackingLib.MINT_STREAK_LAST_COMPLETED_SHIFT`);
        SOLV-01's `_queueWhalePassClaimCore` return-value change MUST precede the BAF caller's
        `claimableDelta` fold. For each req give: the file(s), the AS-FOUND anchor, and which other
        reqs it must precede/follow. The independent reqs (SALV-01, AFAFF-01, SOLV-02, PRESALE-01,
        WINDOW-01, CENTURY-01) may be noted as order-free.
    (C) WINDOW-01 PRE-EDIT VERIFICATION. Using the `git grep -n 'frozenUntilLevel' 2b26ec91`
        results, confirm in writing: EXACTLY the 6 comparisons from the Change-A table are the
        "is-the-pass-active / is-this-level-covered" reads to flip to inclusive; the
        `> targetFrozenLevel` extension math (`DegenerusGameStorage.sol:1070`/`:1151`,
        `DegenerusGameWhaleModule.sol:221`) and the `frozenUntilLevel > currentLevel + 7`
        early-renewal guard (`DegenerusGameWhaleModule.sol:428`) are DIFFERENT semantics and stay
        UNTOUCHED; and there is NO other `frozenUntilLevel` window-reader missed (list every hit and
        classify it flip / extension / early-renewal / unrelated). Re-confirm the afking eviction
        boundary stays inclusive-through-`validThroughLevel` (cite `processSubscriberStage`) and that
        this change makes freeze/floor MATCH that already-inclusive boundary rather than diverge.
    Keep all three sections in plain-English design prose with line cites — no fenced contract
    bodies.
  </action>
  <verify>
    <automated>grep -Eiq 'F-03|SOLV-01' .planning/phases/370-spec-design-lock-anchor-re-attestation-vs-2b26ec91-cov-01-se/370-01-SPEC.md && grep -Eiq 'edit order|producer-before-consumer' .planning/phases/370-spec-design-lock-anchor-re-attestation-vs-2b26ec91-cov-01-se/370-01-SPEC.md && grep -Eiq 'frozenUntilLevel' .planning/phases/370-spec-design-lock-anchor-re-attestation-vs-2b26ec91-cov-01-se/370-01-SPEC.md && grep -Eiq 'validThroughLevel' .planning/phases/370-spec-design-lock-anchor-re-attestation-vs-2b26ec91-cov-01-se/370-01-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    The SPEC records exactly one chosen F-03 / SOLV-01 variant with a written rationale citing the
    frozen function bodies (not "either is fine"); the edit-order map lists all 9 IMPL reqs with
    STREAK-01-before-STREAK-02 and SOLV-01-return-before-BAF-fold explicit; the WINDOW-01 section
    enumerates every `frozenUntilLevel` hit from the `2b26ec91` grep, classifies each as flip /
    extension / early-renewal / unrelated, confirms exactly 6 flips with the extension + early-renewal
    math untouched, and re-confirms the afking eviction boundary stays inclusive-through-`validThroughLevel`.
  </acceptance_criteria>
  <done>F-03 variant locked with rationale, edit-order map covers all 9 IMPL reqs producer-before-consumer, WINDOW-01 verification complete with every frozenUntilLevel hit classified.</done>
</task>

</tasks>

<threat_model>
Paper-only phase — no new attack surface introduced. The analyzed surfaces (the SOLVENCY-01
identity `claimablePool == Σ claimableWinnings + Σ afkingFunding`, the RNG-freeze, the lazy-pass
far-future-ticket queueing path) are re-attested as DESIGN INPUTS for Phase 371, not modified
here. The SPEC's only obligation toward the security floor is to LOCK the F-03/F-04
solvency-identity-restoring variant correctly so the IMPL diff restores (does not further break)
the identity — verified by grounding the variant decision in the frozen `2b26ec91` function bodies.
No package installs, no contract edits, no executable harness in this plan.
</threat_model>

<verification>
- `370-01-SPEC.md` exists and references `2b26ec91` throughout the attestation.
- Anchor table covers all 7 groups; every row grep-verified.
- Exactly one F-03 variant chosen with rationale.
- Edit-order map covers all 9 IMPL reqs producer-before-consumer.
- WINDOW-01 verification enumerates every `frozenUntilLevel` hit and confirms exactly 6 flips.
- ZERO `contracts/*.sol` modified (`git status --porcelain contracts/` empty for this plan).
</verification>

<success_criteria>
The design-lock SPEC is complete: ROADMAP Phase 370 Success Criteria 1, 2, and 3 are satisfied
in writing (variant lock + edit order; full anchor re-attestation; WINDOW-01 pre-edit
verification), so Phase 371 IMPL can author a single reconciled diff with zero un-grepped "by
construction" assumptions.
</success_criteria>

<output>
Create `.planning/phases/370-spec-design-lock-anchor-re-attestation-vs-2b26ec91-cov-01-se/370-01-SUMMARY.md` when done.
</output>
