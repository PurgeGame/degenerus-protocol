---
phase: 370-spec-design-lock-anchor-re-attestation-vs-2b26ec91-cov-01-se
plan: 01
type: SPEC
status: design-locked
frozen_sha: 2b26ec91
requirements: [SPEC-01]
inputs:
  - audit/FINDINGS-v58.0.md
  - .planning/PLAN-PASS-STAT-FRONTLOAD.md
outputs:
  - the anchor re-attestation table (7 groups, grep-verified vs 2b26ec91)
  - the F-03 / SOLV-01 fix-variant lock + rationale
  - the producer-before-consumer batched-diff edit-order map (9 IMPL reqs)
  - the WINDOW-01 pre-edit verification (every frozenUntilLevel hit classified)
---

# Phase 370 Plan 01 — v59.0 Design-Lock SPEC (anchor re-attestation + F-03 variant + edit order + WINDOW-01)

Every cited `file:line` below was grep/show-verified against the frozen baseline
`2b26ec91` (the same SHA the v58.0 council read). No anchor survives "by construction" —
each carries an as-cited / as-found / drift verdict. The IMPL diff (Phase 371) is authored
against the AS-FOUND lines in this document.

---

## Section 1 — Anchor re-attestation vs frozen `2b26ec91`

Verification method: `git show 2b26ec91:<path>` + `git grep -n '<token>' 2b26ec91 -- <path>`.
"As-cited" = the line in FINDINGS-v58.0.md / PLAN-PASS-STAT-FRONTLOAD.md / ROADMAP SC2.
"As-found" = the real line at `2b26ec91`. Drift is expected (the council read approximate
context-pack cites); the AS-FOUND line is authoritative for IMPL.

### Group (1) SALV — F-01 CRITICAL salvage uint32-truncation

| Anchor (as-cited) | As-found @ 2b26ec91 | Drift | Corrected | Role |
|---|---|---|---|---|
| `DegenerusGameMintStreakUtils.sol:173-181` quote-loop `if (n == 0) revert E();` | `:174` (`if (n == 0) revert E();`); `:175` `faceWei = PriceLookupLib.priceForLevel(L) * n;` | yes (~−1) | `:174` / `:175` | The quote values the **full `uint256` `n`** (`n = quantities[i]`, `:173`) — this is the unbounded valuation leg |
| `DegenerusGameMintModule.sol:1013-1015` debit `uint32(quantities[i]) * 4` | `:1014` (`uint32 entries = uint32(quantities[i]) * 4;`) | yes (~+1) | `:1014` | The execution debit **truncates `n` to `uint32`** — values billions, removes a handful |
| `DegenerusGameMintModule.sol:1059` holdings guard `if (owed < entries) revert` | `:1059` (`if (owed < entries) revert E();`) | no | `:1059` | `owed` is `uint32`, so the guard constrains only the **truncated** quantity — never the high bits of `n` |

SALV-01 fix target = the quote loop at `DegenerusGameMintStreakUtils.sol:174`. The repo uses
custom errors (`revert E()`), not `require` — the fix widens to
`if (n == 0 || n > type(uint32).max) revert E();` so the valued quantity equals the debited
`uint32` quantity and the `n = 2^32 + k` exploit reverts.

### Group (2) AFAFF — F-02 HIGH afking `affiliateBase` unit slip

| Anchor (as-cited) | As-found @ 2b26ec91 | Drift | Corrected | Role |
|---|---|---|---|---|
| `GameAfkingModule.sol:879` `base = (_ethToBurnie(amount, mp) * 7) / 100` | `:879` (exact) | no | `:879` | Stores **18-dec** `_ethToBurnie` output into the **whole-BURNIE** `uint32 affiliateBase` — **missing `/ 1 ether`**; clamp saturates to 100M on the first buy |
| buyer-bonus sibling `bonusWhole = bonusBase / 1 ether` (~`:826`) | `:825` (`uint256 bonusWhole = bonusBase / 1 ether;`); `:826` `if (bonusWhole != 0) {` | yes (~−1) | `:825` | Same block **DOES divide** — proves the omission at `:879` is a bug, not intent |
| slot-0 `QUEST_SLOT0_REWARD / 1 ether` (~`:887`) | `:887` (`(QUEST_SLOT0_REWARD / 1 ether);`) | no | `:887` | Second sibling that **DOES divide** into whole-BURNIE units |

AFAFF-01 fix = add the missing `/ 1 ether` at `:879`:
`base = ((_ethToBurnie(amount, mp) * 7) / 100) / 1 ether;`. The read-and-zero counterpart is
`drainAffiliateBase` (`:1614`/`:1628-1629`), and the field doc is `affiliateBase` whole-BURNIE
100M-clamp (`DegenerusGameStorage.sol:1926-1931`, per the council cite).

### Group (3) SOLV — F-03 / F-04 MEDIUM whale-pass remainder solvency

| Anchor (as-cited) | As-found @ 2b26ec91 | Drift | Corrected | Role |
|---|---|---|---|---|
| `_queueWhalePassClaimCore` body `DegenerusGamePayoutUtils.sol:46-65` | `:45` (fn decl); `:49` `remainder`; `:52` `whalePassClaims[winner] += fullHalfPasses;`; `:56` `claimableWinnings[winner] += remainder;`; `:58` `claimablePool += uint128(remainder);` | yes (~−1) | `:45-60` | Credits `remainder` to BOTH `claimableWinnings` and `claimablePool`, **returns nothing** (`internal`, no return type) |
| BAF caller `memFuture -= claimed` `DegenerusGameAdvanceModule.sol:897-903` | `:897` `runBafJackpot(...)`; `:902` `memFuture -= claimed;`; `:903` `claimableDelta += claimed;`; writeback `:968` `_setPrizePools(...)`; `:972` `claimablePool += uint128(claimableDelta);` | no (range exact) | `:897-903` (+`:968`/`:972`) | `claimed` = `runBafJackpot` return = **only the ETH-leg `claimableDelta`** — the whale-pass `remainder` is NOT in it |
| decimator full-`lootboxPortion` debit `DegenerusGameDecimatorModule.sol:392-399` | `:385` (fn decl `_creditDecJackpotClaimCore`); `:393` `lootboxPortion = amount - ethPortion;`; `:398` `claimablePool -= uint128(lootboxPortion);`; `:399` `_awardDecimatorLootbox(...)` | yes (~−7 on decl) | `:385-399` | Debits `claimablePool` by the **FULL** `lootboxPortion` before awarding |
| whale-branch remainder re-credit `_creditClaimable(winner, remainder)` (~`:596`) | `:580` (fn decl `_awardDecimatorLootbox`); `:589` `remainder = amount - (fullHalfPasses * HALF_WHALE_PASS_PRICE);`; `:595` `if (remainder != 0) {`; `:596` `_creditClaimable(winner, remainder);` | no | `:596` | Re-credits `remainder` to `claimableWinnings` with **no `claimablePool` bump** → `Σ claimableWinnings > claimablePool` |
| checked-math `claimWinnings` debit (~`:1588`) | `DegenerusGame.sol:1556` `function claimWinnings`; `:1568` `_claimWinningsInternal`; `:1588` `claimablePool -= uint128(payout); // ... (checked math)` | no | `DegenerusGame.sol:1588` | The checked-math debit that **reverts on underflow** for the tail claimant summing to `remainder` |

BAF leg chain confirmed: `DegenerusGameJackpotModule.runBafJackpot` (`:1901-1909`) returns
`claimableDelta`, accumulated ONLY from `_addClaimableEth` (`:730-740`, returns `weiAmount`,
credits via `_creditClaimable`, **no inline `claimablePool` bump**). The large-lootbox leg calls
`_queueWhalePassClaimCore(winner, lootboxPortion)` (`JackpotModule.sol:1949`) whose `remainder`
bumps `claimablePool` inline AND returns nothing → excluded from `claimableDelta` → the F-03 leak.
Second `_queueWhalePassClaimCore` caller = `JackpotModule.sol:2001` (terminal/solo path).

SOLV-02 (F-04) has **no variant choice** — confirmed site: add
`claimablePool += uint128(remainder);` alongside `_creditClaimable(winner, remainder)` at
`DegenerusGameDecimatorModule.sol:596`. The `:592` `_applyWhalePassStats(winner, startLevel)`
in the same branch is ALSO a STREAK-02 fold site (Group 6).

### Group (4) PRESALE — F-05 LOW `pendingBurnie` presale 2×

| Anchor (as-cited) | As-found @ 2b26ec91 | Drift | Corrected | Role |
|---|---|---|---|---|
| `subscribe` UPSERT `FLAG_USE_TICKETS` flip, no pre-claim `GameAfkingModule.sol:397-398` | `:397` `if (useTickets) s.flags |= FLAG_USE_TICKETS;`; `:398` `else s.flags &= ~FLAG_USE_TICKETS;` | no | `:397-398` | Flips the flag **in place** with no pre-claim; only the CANCEL branch (`:358`) auto-claims |
| presale-box-credit halving on live flag `:1599-1601` | `:1600` `if ((s.flags & FLAG_USE_TICKETS) != 0) credit /= 2;`; `:1601` `presaleBoxCredit[player] += credit;` | yes (~+1) | `:1600-1601` | Halving keys on the **live** flag (also at `:358-359` in the CANCEL/drain path) |

PRESALE-01 fix = auto-claim `pendingBurnie` (the `:358`/drain shape) BEFORE flipping
`FLAG_USE_TICKETS` in the UPSERT branch (`:397-398`) so the halving keys on the settled flag.
The doubled unit is `presaleBoxCredit` (ETH-denominated allowance, capped 50 ETH), not a payout.

### Group (5) Change-A WINDOW — the 6 inclusive-flip comparisons + `hasAnyLazyPass` docstring

| # | Anchor (as-cited) | As-found @ 2b26ec91 | Drift | Flip |
|---|---|---|---|---|
| 1 | `DegenerusGameMintModule.sol:291` `lvl < frozenUntilLevel` | `:291` `bool isFrozen = frozenUntilLevel > 0 && lvl < frozenUntilLevel;` | no | `<` → `<=` |
| 2 | `DegenerusGameMintModule.sol:295` `lvl >= frozenUntilLevel` | `:295` `if (frozenUntilLevel > 0 && lvl >= frozenUntilLevel) {` | no | `>=` → `>` |
| 3 | `DegenerusGameMintStreakUtils.sol:262` `frozenUntilLevel > currLevel` | `:262` `bool passActive = frozenUntilLevel > currLevel &&` | no | `>` → `>=` |
| 4 | `DegenerusGameMintStreakUtils.sol:310` `frozenUntilLevel > currLevel` | `:310` `} else if (frozenUntilLevel > currLevel) {` | no | `>` → `>=` |
| 5 | `DegenerusGameMintStreakUtils.sol:56` `frozenUntilLevel > level` | `:56` `if (frozenUntilLevel > level) return true;` | no | `>` → `>=` |
| 6 | `DegenerusGame.sol:1678` `hasAnyLazyPass` `frozenUntilLevel > level` | `:1670` fn decl; `:1678` `return frozenUntilLevel > level;` | no | `>` → `>=` |
| doc | stale `hasAnyLazyPass` docstring `DegenerusGame.sol:1662-1668` | `:1662-1668` (exact — says "ends AT `frozenUntilLevel` (exclusive)" + "reports 'no pass' one level before AfKing evicts. By design.") | no | rewrite to inclusive semantics |

All 6 flip sites + the docstring confirmed AS-CITED at `2b26ec91`. The WINDOW-01 pre-edit
exhaustive `frozenUntilLevel` classification is in Section 4 below.

### Group (6) Change-B STREAK — the 3 activation sites + the shift prerequisite

| Anchor (as-cited) | As-found @ 2b26ec91 | Drift | Role |
|---|---|---|---|
| `_activate10LevelPass` `DegenerusGameStorage.sol:1049` | `:1049` (fn decl); builds `data` word, single SSTORE; computes `ticketStartLevel`/`newFrozenLevel`/`deltaFreeze` (`:1056-1074`) | no | Lazy + pass-award activation; STREAK-02 fold site |
| `_applyWhalePassStats` `DegenerusGameStorage.sol:1134` | `:1134` (fn decl); same delta-based word build (`:1140-1155`) | no | Whale claim (no tickets); STREAK-02 fold site |
| caller `DegenerusGameWhaleModule.sol:1013` | `:1013` `_applyWhalePassStats(player, startLevel);` | no | Whale-claim caller |
| caller `DegenerusGameDecimatorModule.sol:592` | `:592` `_applyWhalePassStats(winner, startLevel);` | no | Decimator whale-branch caller (also the F-04 site) |
| inline block `_purchaseWhaleBundle` `DegenerusGameWhaleModule.sol:264-291` | `:188` fn decl; `:264` `uint24 newLevelCount = ...`; `:267-291` `data` word build (LEVEL_COUNT / FROZEN_UNTIL / WHALE_BUNDLE_TYPE / LAST_LEVEL); SSTORE follows | no | Whale-100 inline activation; STREAK-02 fold site |
| `MINT_STREAK_LAST_COMPLETED_SHIFT` local `DegenerusGameMintStreakUtils.sol:19` | `:19` `uint256 internal constant MINT_STREAK_LAST_COMPLETED_SHIFT = 160;` | no | The ONLY field shift with no `BitPackingLib` constant; STREAK-01 promotes it |
| its 4 refs | `:22` (mask build in `MINT_STREAK_FIELDS_MASK`), `:72` (read in `_recordMintStreakForLevel`), `:94` (write in `_recordMintStreakForLevel`), `:105` (read in `_mintStreakEffective`) | no | The 4 repoint targets for STREAK-01 |

`BitPackingLib.sol` confirmation for STREAK-01: the bit-layout doc ALREADY reserves
`[160-183] MINT_STREAK_LAST_COMPLETED - Last level credited for mint streak (24 bits)` with
`[154-159] (unused)` immediately before it; constants `WHALE_BUNDLE_TYPE_SHIFT = 152` (`:66`)
and `HAS_DEITY_PASS_SHIFT = 184` (`:71`) bracket the slot. So promoting
`MINT_STREAK_LAST_COMPLETED_SHIFT = 160` into `BitPackingLib` (with `MASK_24`) is a clean,
no-conflict, no-behavior-change move — the layout already documents the slot. The helper must
preserve the `MINT_STREAK_FIELDS_MASK` pattern (clears BOTH last-completed AND `LEVEL_STREAK`
fields, `MintStreakUtils:21-23`).

### Group (7) Change-C CENTURY — the lazy-pass gate x0 term

| Anchor (as-cited) | As-found @ 2b26ec91 | Drift | Role |
|---|---|---|---|
| lazy-pass gate x0 term `DegenerusGameWhaleModule.sol:411-416` | `:414` `(currentLevel % 10 != 0 || currentLevel % 100 == 0) &&` | yes (term at `:414` within the `:412-417` `if` block) | The x0 term that blocks every century outright |
| the `:409-410` comment | `:411-412` (`// Purchasable at levels 0-2, x9 ... or x0 ... whose 10-level window would overrun level 100`) | yes (~+2) | The gate-shape comment to update |
| `purchaseLazyPass` `@dev`/`@custom:reverts` doc | `purchaseLazyPass` decl `:377`; `_purchaseLazyPass` `:381`; early-renewal guard `:428` `if (frozenUntilLevel > currentLevel + 7) revert E();` | n/a | The entry doc to update; the early-renewal guard is SEPARATE semantics (untouched) |

CENTURY-01 edit = the x0 term at `:414`:
`(currentLevel % 10 != 0 || (currentLevel % 100 == 0 && jackpotPhaseFlag)) &&`.
`jackpotPhaseFlag` is shared delegatecall storage, in scope in `_purchaseLazyPass`.

### Verdict

**7 anchor groups attested, 7 carry drift on ≥1 line (all corrected to AS-FOUND above),
0 unverified.** Per-line: 24 distinct anchors grep/show-verified against `2b26ec91`; the
drifts are ±1–2 lines (the council read approximate context-pack cites) plus the `:1588`
"module" cite resolving to `DegenerusGame.sol` and the `:596`/`:392-399` decimator decl drift.
No "by construction" assertion survives — every IMPL target line is the AS-FOUND line. `git
status --porcelain contracts/` is empty (paper-only).
