---
phase: 350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam
plan: 02
type: execute
wave: 2
depends_on: [350-01]
files_modified:
  - .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-GAS-SKEPTIC-VERDICTS.md
autonomous: true
requirements: [GAS-01, GAS-02, GAS-03]

must_haves:
  truths:
    - "Every re-pinned candidate (SCAV-348-01..07) carries an explicit /gas-skeptic verdict — APPROVE / REJECT / ESCALATE / CONFIRMED-STRUCTURAL — with reasoning, under the feedback_security_over_gas floor"
    - "GAS-01 and GAS-02 are re-attested CONFIRMED-STRUCTURAL (no apply; the saving is the relocation, measured at 351)"
    - "GAS-03 (the claimablePool same-slot flush at GameAfkingModule.sol:710) is adjudicated with the warm-SSTORE magnitude (~100 gas x (N-1), not ~2.9k) + the off-hot-path affiliate/pool finding + the solvency-surface cost; the expected disposition is REJECT-with-reasoning, but the verdict is the executor's under the floor"
    - "The GAS-03 SAFE-WITH-CONDITIONS carve-out is carried VERBATIM: quests.handlePurchase/handleAffiliate are NEVER batched (non-linear completion logic) — any candidate batching them is REJECT"
    - "The mixed-chunk hazard (RESEARCH Open Question 1 — a ticket-mode purchaseWith claimablePool write interleaved with lootbox :710 debits) is adjudicated as decisive for any Outcome-B flush"
    - "No invariant-trading candidate is APPROVED: afkingFunding[src] per-key debit, the swap-pop tombstone (CONSENT-02 / H-CANCEL-SWAP-MISS), the no-orphan guard, freeze fields, and fail-loud-on-solvency are all floor-protected REJECTs if touched"
    - "No contracts/*.sol file is modified by this plan (read-only adjudication)"
  artifacts:
    - path: ".planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-GAS-SKEPTIC-VERDICTS.md"
      provides: "Per-candidate APPROVE/REJECT/ESCALATE disposition with reasoning + the GAS-03 adjudication + the W3 branch directive (Outcome A vs B)"
      contains: "GAS-03"
  key_links:
    - from: "350-GAS-SKEPTIC-VERDICTS.md"
      to: "350-RE-PIN-AND-CONFIRM.md"
      via: "consumes the re-pinned candidate table as the adjudication input"
      pattern: "re-pin"
    - from: "350-GAS-SKEPTIC-VERDICTS.md"
      to: "contracts/modules/GameAfkingModule.sol:710"
      via: "the GAS-03 candidate site under adjudication"
      pattern: "GameAfkingModule.sol:710"
---

<objective>
Run the `/gas-skeptic` validation discipline over each re-pinned candidate from plan 350-01, UNDER the `feedback_security_over_gas` floor, and record an APPROVE / REJECT / ESCALATE verdict with explicit reasoning for each. This is where GAS-03 (the sole residual code-change candidate) is adjudicated.

Purpose: The SPEC gas inventory is intentionally aggressive (the gas-scavenger persona is reckless by design). The `/gas-skeptic` gate is the ONLY validation gate — its job is to DISPROVE the wrong candidates. GAS-01/GAS-02 are re-attested confirmed-structural (no apply). GAS-03 is the one candidate that could be a net-new contract change; the live evidence (warm `claimablePool` SSTORE ~100 gas/iter, NOT ~2.9k; the affiliate/quest/pool calls off the STAGE hot path) points it toward REJECT-with-reasoning — but the executor renders the verdict against the live evidence, not a pre-ordained answer. The output directs plan 350-03's branch (Outcome A = no diff, expected; Outcome B = author the held flush diff).

Output: `350-GAS-SKEPTIC-VERDICTS.md`. NO contract edits.

NOTE on the skeptic skill: the `/gas-skeptic` skill MD is NOT vendored in this repo (`.claude/skills/` absent). Apply the skeptic discipline INLINE — the discipline is fully specified by the RESEARCH (warm-write re-derivation, off-hot-path affiliate finding, the floor-check, the penny-exact obligations) and the v49 REJECT-with-reasoning precedent. If the `/gas-skeptic` skill is in fact available to the executor, invoke it; otherwise run the discipline inline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-RESEARCH.md
@.planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-RE-PIN-AND-CONFIRM.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-GAS-INVENTORY.md
@.planning/REQUIREMENTS.md

# 350-RE-PIN-AND-CONFIRM.md (plan 350-01 output) is the candidate input.
# 350-RESEARCH.md §"GAS-03 Linearity Map", §"Penny-Exact Solvency Check", §"Realistic Outcome",
# and §"Common Pitfalls" supply the full skeptic evidence. 348-GAS-INVENTORY.md §4 is the
# SAFE-WITH-CONDITIONS carve-out (carry VERBATIM); §5 is the security-over-gas floor.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adjudicate every candidate (APPROVE/REJECT/ESCALATE) under the security-over-gas floor; render the GAS-03 verdict</name>
  <files>.planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-GAS-SKEPTIC-VERDICTS.md</files>
  <read_first>
    - .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-RE-PIN-AND-CONFIRM.md (the re-pinned candidate table from plan 350-01)
    - .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-RESEARCH.md (§"GAS-03 Linearity Map" — every per-sub write classified linear/batchable; §"Penny-Exact Solvency Check" — the 4 proof obligations; §"Realistic Outcome" — the warm-write magnitude + the no-net-change branch; §"Open Questions" #1 — the mixed-chunk purchaseWith interleave hazard; §"Common Pitfalls" 1-4)
    - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-GAS-INVENTORY.md (§4 SAFE-WITH-CONDITIONS — carry VERBATIM; §5 the security floor)
    - contracts/modules/GameAfkingModule.sol (the STAGE loop :539-767 — confirm grep-empty for payAffiliate/quests.*/prizePoolsPacked; the claimablePool debit :710; the afkingFunding[src] per-key debit :709; the swap-pop set-mutations :588/:622/:676; the no-orphan guard :570; the ticket-mode purchaseWith :718-730)
    - contracts/storage/DegenerusGameStorage.sol (claimablePool :365 packed slot 1 with currentPrizePool; the SOLVENCY invariant :358)
  </read_first>
  <action>
    Author `350-GAS-SKEPTIC-VERDICTS.md`. For EACH re-pinned candidate (SCAV-348-01..07), record a verdict row: ID | live site | disposition {APPROVE / REJECT / ESCALATE / CONFIRMED-STRUCTURAL} | reasoning (live-evidence-grounded, under the floor). Apply the v49 precedent: REJECT non-real wins WITH reasoning; do not re-litigate.

    Expected dispositions (render each against the live evidence — these are the expected outcomes, not a script to copy):
    - SCAV-348-01 (box-ledger→stamp = GAS-01): CONFIRMED-STRUCTURAL — delivered `77c3d9ef`, no apply, measured at 351 TST-06.
    - SCAV-348-02 (staticcall→SLOAD = GAS-02): CONFIRMED-STRUCTURAL — delivered `77c3d9ef`, no apply; the `afkingSnapshot`/`afkingFundingOf` view-helpers serve `DegenerusVault.sol:518` and are NOT removal targets.
    - SCAV-348-04 / SCAV-348-05 / SCAV-348-06 / SCAV-348-07: DISSOLVED / CONFIRMED-STRUCTURAL (relocation by-products — bytecode-retire / dead-scaffold / layout / two-path; nothing to apply at 350 runtime gas).
    - SCAV-348-03 (same-slot flush = GAS-03): the load-bearing adjudication. Record ALL of: (a) the only batchable shared slot on the STAGE loop is `claimablePool` (`:710`), a WARM SSTORE after iter 1 (packed slot 1 with `currentPrizePool`, Storage `:365`) → the batch saving is ~100 gas x (N−1), NOT the inventory's ~2.9k x (N−1) headline (the over-count the inventory's own Skeptic note flagged); (b) the affiliate/quest/pool calls (`payAffiliate`/`quests.*`) are grep-CONFIRMED absent from `GameAfkingModule` — they live inside the per-sub `purchaseWith` delegatecall (`:718-730`, ticket mode only), un-collapsible without batching `quests.*` (= REJECT per §4); (c) `prizePoolsPacked` is grep-empty in `GameAfkingModule` (only the AdvanceModule jackpot-settle writes it, already once-per-advance) → NOT on the afking path; (d) the mixed-chunk hazard (RESEARCH Open Question 1): a chunk mixing ticket + lootbox subs could interleave a `purchaseWith`-internal `claimablePool` write with the lootbox `:710` debits — the "mid-loop read/write of the batched slot" that breaks the accumulate-and-flush identity — which strengthens REJECT; (e) the cost/benefit under the floor: a ~0.04%-of-chunk warm-write saving against net-new audit surface on the SOLVENCY-01 spine at TERMINAL (352). Render the GAS-03 verdict (expected REJECT; if the executor finds a genuine, floor-safe, mixed-chunk-safe win worth the surface, ESCALATE or APPROVE with the penny-exact obligations attached — do NOT silently APPROVE a marginal win).

    Carry the §4 carve-out VERBATIM into the doc: `quests.handlePurchase`/`handleAffiliate` are NON-LINEAR completion logic and MUST NOT be batched; each sub's quest calls run per-sub, in order; any candidate that batches them is pre-marked REJECT. State the floor-protected REJECT list explicitly: the `afkingFunding[src]` per-key debit (`:709`, the per-account underflow guard / SOLVENCY-01 precondition), the swap-pop tombstone (CONSENT-02 / H-CANCEL-SWAP-MISS, `:588/:622/:676`), the no-orphan guard (`:570`), the freeze fields (scorePlus1/amount/lastAutoBoughtDay stamp), and fail-loud-on-solvency (the checked `claimablePool -=`).

    End with a W3 BRANCH DIRECTIVE: state which Outcome plan 350-03 must execute — Outcome A (no net contract change, record the verdict) if GAS-03 is REJECTED and all else CONFIRMED-STRUCTURAL/DISSOLVED (the expected path); Outcome B (author the held `claimablePool` flush diff) ONLY if a GAS-03 win is APPROVED with the penny-exact obligations. NO fenced code blocks of implementations — identifiers/anchors/slot names only. READ contracts; WRITE the markdown doc; assert `git diff --name-only -- contracts/` is EMPTY.
  </action>
  <verify>
    <automated>test -f .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-GAS-SKEPTIC-VERDICTS.md && for id in SCAV-348-01 SCAV-348-02 SCAV-348-03 SCAV-348-04 SCAV-348-05 SCAV-348-06 SCAV-348-07; do grep -q "$id" .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-GAS-SKEPTIC-VERDICTS.md || { echo "MISSING $id"; exit 1; }; done && grep -qiE "REJECT|APPROVE|ESCALATE|CONFIRMED-STRUCTURAL" .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-GAS-SKEPTIC-VERDICTS.md && grep -q "GameAfkingModule.sol:710" .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-GAS-SKEPTIC-VERDICTS.md && grep -qiE "handleAffiliate|handlePurchase" .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-GAS-SKEPTIC-VERDICTS.md && grep -qiE "Outcome A|Outcome B" .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-GAS-SKEPTIC-VERDICTS.md && [ -z "$(git diff --name-only -- contracts/)" ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `350-GAS-SKEPTIC-VERDICTS.md` exists and records a disposition {APPROVE / REJECT / ESCALATE / CONFIRMED-STRUCTURAL} with reasoning for ALL seven SCAV-348-01..07 candidates.
    - GAS-01 (SCAV-348-01) and GAS-02 (SCAV-348-02) are recorded CONFIRMED-STRUCTURAL (no apply; measured at 351).
    - The GAS-03 (SCAV-348-03) row cites the live site `GameAfkingModule.sol:710`, records the warm-SSTORE magnitude (~100 gas x (N−1), NOT ~2.9k), the off-hot-path affiliate/pool/quests finding (grep-confirmed absent from `GameAfkingModule`), the `prizePoolsPacked`-not-on-path finding, the mixed-chunk `purchaseWith` interleave hazard, and the solvency-surface cost — and renders an explicit verdict.
    - The §4 SAFE-WITH-CONDITIONS carve-out (no `quests.handlePurchase`/`handleAffiliate` batching — non-linear completion logic) is carried verbatim; any quests-batching candidate is marked REJECT.
    - The floor-protected REJECT list is stated explicitly (afkingFunding[src] per-key debit, swap-pop tombstone CONSENT-02, no-orphan guard, freeze fields, fail-loud `claimablePool -=`).
    - The doc ends with a W3 branch directive naming Outcome A (no diff — expected) or Outcome B (held flush diff) for plan 350-03.
    - No GAS-03 win is APPROVED without the penny-exact obligations attached (net-delta identity + no-mid-loop-read + fail-loud-preserved + byte-identical 351 oracle).
    - `git diff --name-only -- contracts/` is EMPTY; no fenced code block of a contract implementation.
  </acceptance_criteria>
  <done>
    Every candidate carries a floor-grounded verdict, GAS-03 is adjudicated on the live warm-write + off-hot-path evidence (expected REJECT, executor-rendered), the §4 carve-out + floor-protected REJECT list are recorded verbatim, and the doc directs plan 350-03's Outcome A/B branch — with zero contract edits.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Read-only adjudication + documentation. No new code, no new input. The adjudication GOVERNS the existing SOLVENCY-01 boundary (`claimablePool` tandem moves) by deciding whether a flush diff may touch it — it does not itself cross any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-350-03 | Tampering | A marginal warm-write win silently APPROVED, adding net audit surface on the SOLVENCY-01 spine for ~0.04% gas | mitigate | The floor (`feedback_security_over_gas`) governs; the GAS-03 verdict must weigh the warm-write magnitude + the solvency surface; no APPROVE without the penny-exact obligations attached. |
| T-350-04 | Tampering | A candidate that batches `quests.*` (non-linear completion) slipping through as a "gas win" | mitigate | The §4 carve-out is carried verbatim; quests-batching candidates are pre-marked REJECT; quest calls stay per-sub, in order. |
| T-350-05 | Tampering | A candidate that touches the swap-pop tombstone / no-orphan guard / afkingFunding[src] per-key debit for gas (regression of CONSENT-02 / SOLVENCY-01) | mitigate | The floor-protected REJECT list is stated explicitly; any candidate touching set-mutation, the no-orphan guard, the per-account debit, or the freeze fields is REJECT. |
| T-350-SC | Tampering | npm/pip/cargo installs | accept | No package installs — read-only grep + Markdown write only. |
</threat_model>

<verification>
- `350-GAS-SKEPTIC-VERDICTS.md` exists with a verdict for all seven candidates.
- GAS-01/GAS-02 = CONFIRMED-STRUCTURAL; GAS-03 adjudicated with the full warm-write + off-hot-path + mixed-chunk + solvency-surface reasoning.
- The §4 carve-out + floor-protected REJECT list carried verbatim.
- W3 branch directive (Outcome A / B) present.
- `git diff --name-only -- contracts/` is EMPTY.
</verification>

<success_criteria>
- ROADMAP SC3: the GAS-03 same-slot flush is adjudicated SAFE-WITH-CONDITIONS under the floor (expected REJECT-with-reasoning; quests.* never batched), v49 precedent applied (REJECT with reasoning, no re-litigation).
- GAS-01/GAS-02 re-attested confirmed-structural.
- Plan 350-03's branch is directed (Outcome A expected; Outcome B only on an APPROVED, penny-exact-obligated win).
- Zero contract files modified.
</success_criteria>

<output>
Create `.planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-02-SUMMARY.md` when done.
</output>
