---
phase: 214-adversarial-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/214-adversarial-audit/214-02-ACCESS-OVERFLOW.md
autonomous: true
requirements: [ADV-01]
must_haves:
  truths:
    - "Every changed/new function has an access control verdict (SAFE / VULNERABLE / INFO)"
    - "Every changed/new function has an integer overflow verdict (SAFE / VULNERABLE / INFO)"
    - "Every modifier change (onlyGame replacing multiple modifiers, vault.isVaultOwner replacing _contractOwner, expanded onlyFlipCreditors) is explicitly verified"
    - "Every uint48->uint32 narrowing is verified safe against truncation"
    - "Every uint128 narrowing (claimablePool, currentPrizePool) is verified safe against overflow"
  artifacts:
    - path: ".planning/phases/214-adversarial-audit/214-02-ACCESS-OVERFLOW.md"
      provides: "Access control + integer overflow audit document"
      contains: "## Per-Function Verdicts"
  key_links:
    - from: "213-DELTA-EXTRACTION.md scope definition"
      to: "214-02-ACCESS-OVERFLOW.md per-function verdicts"
      via: "Every function in Phase 214 scope has verdict rows for both access control and overflow"
      pattern: "SAFE|VULNERABLE|INFO"
---

<objective>
Access control and integer overflow audit pass across all changed/new functions in the v5.0-to-HEAD delta.

Purpose: Identify any function where access control was weakened (modifier removal/change) or where integer narrowing (uint48->uint32, uint256->uint128) creates overflow/truncation risk. Second of three vulnerability-class passes per D-01.
Output: 214-02-ACCESS-OVERFLOW.md with per-function verdicts for both vulnerability classes
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/213-delta-extraction/213-DELTA-EXTRACTION.md
@.planning/phases/213-delta-extraction/213-01-DELTA-MODULES.md
@.planning/phases/213-delta-extraction/213-02-DELTA-CORE.md
@.planning/phases/214-adversarial-audit/214-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Access control audit of all changed/new functions</name>
  <files>.planning/phases/214-adversarial-audit/214-02-ACCESS-OVERFLOW.md</files>
  <read_first>
    - .planning/phases/213-delta-extraction/213-DELTA-EXTRACTION.md (scope definition -- Phase 214 scope section)
    - .planning/phases/213-delta-extraction/213-01-DELTA-MODULES.md (module function changelog -- modifier changes)
    - .planning/phases/213-delta-extraction/213-02-DELTA-CORE.md (core function changelog -- modifier changes)
    - .planning/phases/214-adversarial-audit/214-CONTEXT.md (D-01, D-02, D-03)
    - contracts/storage/DegenerusGameStorage.sol (shared modifiers, _queueTickets rngBypass parameter, new event/constant centralizations)
    - contracts/DegenerusGame.sol (entry contract -- all external/public function access gates, delegatecall routing, claimWinningsStethFirst restricted to VAULT only)
    - contracts/modules/DegenerusGameAdvanceModule.sol (rngGate, _enforceDailyMintGate changes)
    - contracts/modules/DegenerusGameJackpotModule.sol (distributeYieldSurplus made external -- who can call?)
    - contracts/modules/DegenerusGameMintModule.sol (recordMintQuestStreak access changed COIN->GAME)
    - contracts/modules/DegenerusGameWhaleModule.sol (claimWhalePass moved -- access check preserved?)
    - contracts/modules/DegenerusGameDecimatorModule.sol (terminal burn blocked <=7 days -- access to recordTerminalDecBurn)
    - contracts/modules/DegenerusGameGameOverModule.sol (RNG gate defense-in-depth -- who triggers handleGameOverDrain?)
    - contracts/BurnieCoin.sol (CRITICAL: onlyGame replaced onlyDegenerusGameContract/onlyTrustedContracts/onlyFlipCreditors/onlyAdmin -- verify no privilege escalation)
    - contracts/BurnieCoinflip.sol (onlyFlipCreditors expanded to GAME+QUESTS+AFFILIATE+ADMIN -- verify each creditor is legitimate)
    - contracts/DegenerusAdmin.sol (proposeFeedSwap/voteFeedSwap -- new governance access, sDGNRS.votingSupply replacing circulatingSupply)
    - contracts/DegenerusAffiliate.sol (registerAffiliateCode rejects address-range codes -- access control for registration)
    - contracts/DegenerusQuests.sol (handle* functions -- who can call each?)
    - contracts/DegenerusStonk.sol (unwrapTo access CREATOR->vault.isVaultOwner, claimVested access)
    - contracts/StakedDegenerusStonk.sol (burnAtGameOver access, resolveRedemptionPeriod access)
    - contracts/DegenerusVault.sol (sdgnrsBurn/sdgnrsClaimRedemption -- vault owner only?)
    - contracts/DegenerusDeityPass.sol (onlyOwner changed from _contractOwner to vault.isVaultOwner)
    - contracts/GNRUS.sol (NEW -- propose/vote/pickCharity/burn/burnAtGameOver access controls)
    - contracts/WrappedWrappedXRP.sol (unwrap/donate removed -- verify no residual access paths)
    - contracts/libraries/BitPackingLib.sol (new shift constants -- verify no overlap with existing shifts)
    - contracts/libraries/GameTimeLib.sol (uint48->uint32 narrowing)
  </read_first>
  <action>
Per D-01 (vulnerability class split) and D-02 (fresh from scratch), audit every changed/new function for access control correctness and integer overflow/truncation safety. Do NOT reference any v5.0 adversarial audit artifacts.

Create 214-02-ACCESS-OVERFLOW.md with the following structure:

```markdown
# Adversarial Audit: Access Control + Integer Overflow Pass

**Scope:** All changed/new functions in v5.0-to-HEAD delta (per 213-DELTA-EXTRACTION.md Phase 214 scope)
**Method:** Fresh audit (per D-02) -- no prior audit artifacts referenced
**Chains in scope:** SM-01 through SM-56, EF-01 through EF-20, RNG-01 through RNG-11, RO-01 through RO-12 (per D-03)

## Methodology

### Access Control
For each function:
1. Identify the access modifier/guard (onlyGame, onlyVault, msg.sender == X, etc.)
2. Compare against the delta changelog: was the modifier CHANGED, ADDED, or REMOVED?
3. If changed: verify the new guard is at least as restrictive as needed (no privilege escalation)
4. If removed: verify the function no longer needs access control (or flag as VULNERABLE)
5. For delegatecall-targeted functions: verify the caller gate at the DegenerusGame entry point

### Integer Safety
For each function:
1. Identify all type narrowings (uint48->uint32, uint256->uint128, etc.)
2. For each narrowing: compute the maximum possible value and verify it fits in the target type
3. For uint32 day indexes: verify 2^32 days (11.7M years) exceeds any possible game lifetime
4. For uint128 pool values: verify maximum possible ETH (10^18 * amount) fits in uint128 (3.4 * 10^38)
5. For packed bitfield operations: verify shift constants do not overlap (BitPackingLib additions)
6. Check for unchecked arithmetic blocks and verify each cannot overflow

## Findings

[If any VULNERABLE verdicts exist, list them here with F-ACCESS-NN or F-OVERFLOW-NN IDs]

## Access Control Modifier Change Matrix

| Old Modifier/Guard | New Modifier/Guard | Contracts Affected | Verdict |
|--------------------|--------------------|--------------------|---------|
| onlyDegenerusGameContract | onlyGame (msg.sender == GAME) | BurnieCoin | [equivalent/weakened/strengthened] |
| onlyTrustedContracts | onlyGame | BurnieCoin | [equivalent/weakened/strengthened] |
| onlyFlipCreditors (GAME+BURNIE) | onlyFlipCreditors (GAME+QUESTS+AFFILIATE+ADMIN) | BurnieCoinflip | [widened -- verify each new creditor] |
| _contractOwner | vault.isVaultOwner(msg.sender) | DegenerusDeityPass | [equivalent/weakened/strengthened] |
| CREATOR | vault.isVaultOwner | DegenerusStonk.unwrapTo | [equivalent/weakened/strengthened] |
| ADMIN | vault.isVaultOwner | DegenerusGame.adminStakeEthForStEth, setLootboxRngThreshold | [equivalent/weakened/strengthened] |
| COIN | GAME (self from delegatecall) | DegenerusGame.recordMintQuestStreak | [equivalent/weakened/strengthened] |
| SDGNRS+VAULT | VAULT only | DegenerusGame.claimWinningsStethFirst | [strengthened -- verify sDGNRS no longer needs access] |

## Integer Type Narrowing Matrix

| Type Change | Where Used | Max Possible Value | Fits Target | Verdict |
|-------------|------------|-------------------|-------------|---------|
| uint48->uint32 (day index) | GameTimeLib, all modules, BurnieCoinflip, DegenerusQuests, StakedDegenerusStonk | ~11.7M years of days | Yes (uint32 max = 4.29B) | [SAFE/VULNERABLE] |
| uint256->uint128 (claimablePool) | DegenerusGameStorage, DecimatorModule, DegeneretteModule, GameOverModule, DegenerusGame | max ETH in pool | [compute max] | [SAFE/VULNERABLE] |
| uint256->uint128 (currentPrizePool) | DegenerusGameStorage (slot 1 repack) | max ETH in pool | [compute max] | [SAFE/VULNERABLE] |
| New bitfield shifts | BitPackingLib: HAS_DEITY_PASS_SHIFT(184), AFFILIATE_BONUS_LEVEL_SHIFT(185), AFFILIATE_BONUS_POINTS_SHIFT(209) | bit positions | [verify no overlap] | [SAFE/VULNERABLE] |

## Per-Function Verdicts

### Module Contracts

#### DegenerusGameAdvanceModule.sol

| Function | Visibility | Access Gate | Access Verdict | Integer Patterns | Overflow Verdict | Notes |
|----------|-----------|-------------|----------------|------------------|------------------|-------|
| advanceGame | external | [gate] | SAFE/VULNERABLE/INFO | [narrowings] | SAFE/VULNERABLE/INFO | [analysis] |
| ... | ... | ... | ... | ... | ... | ... |

[Repeat table for every module: JackpotModule, MintModule, WhaleModule, DecimatorModule, DegeneretteModule, LootboxModule, BoonModule, GameOverModule, PayoutUtils, MintStreakUtils, Storage]

### Core Contracts

[Same format for: DegenerusGame, DegenerusAdmin, DegenerusAffiliate, DegenerusQuests, BurnieCoin, BurnieCoinflip, DegenerusStonk, StakedDegenerusStonk, DegenerusVault, DegenerusDeityPass, GNRUS, WrappedWrappedXRP]

### Libraries

[BitPackingLib, GameTimeLib, JackpotBucketLib shift/type analysis]

## GNRUS Access Control (New Contract)

[Dedicated section for GNRUS since it is entirely new:]
| Function | Expected Access | Actual Guard | Verdict |
|----------|----------------|--------------|---------|
| burn | any holder | balance check | [SAFE/VULNERABLE] |
| burnAtGameOver | Game only | msg.sender == game | [SAFE/VULNERABLE] |
| propose | sDGNRS holder (0.5% threshold) or vault owner (up to 5x) | [actual guard] | [SAFE/VULNERABLE] |
| vote | sDGNRS holder | [actual guard] | [SAFE/VULNERABLE] |
| pickCharity | Game only | msg.sender == game | [SAFE/VULNERABLE] |
| receive | anyone | no guard (payable) | [SAFE/INFO] |

## Critical Access Control Changes

### BurnieCoin Modifier Collapse
[Detailed analysis: BurnieCoin had 5+ different modifiers, now has onlyGame/onlyVault only. For each removed function (creditFlip, creditFlipBatch, rollDailyQuest, notifyQuest*, etc.), verify the function was actually removed from BurnieCoin and the caller now goes to the correct contract (BurnieCoinflip, DegenerusQuests). Verify no path exists to call a removed function.]

### BurnieCoinflip Expanded Creditors
[Detailed analysis: onlyFlipCreditors went from GAME+BURNIE to GAME+QUESTS+AFFILIATE+ADMIN. For each new creditor, verify it legitimately needs to credit flips and cannot be exploited to inflate flip balances.]

### Vault-Based Ownership Migration
[Analysis of _contractOwner -> vault.isVaultOwner migration in DegenerusDeityPass and DegenerusStonk. Verify vault.isVaultOwner cannot return true for an attacker. Verify the DegenerusVault.isVaultOwner implementation.]
```

Read every contract source file listed in read_first to determine actual access modifiers and integer types. Do not assume from the changelog alone -- verify against the source.
  </action>
  <verify>
    <automated>grep -c "SAFE\|VULNERABLE\|INFO" .planning/phases/214-adversarial-audit/214-02-ACCESS-OVERFLOW.md</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/214-adversarial-audit/214-02-ACCESS-OVERFLOW.md exists
    - Contains "## Access Control Modifier Change Matrix" with all modifier changes from the delta
    - Contains "## Integer Type Narrowing Matrix" with uint48->uint32, uint256->uint128, and bitfield shift analysis
    - Contains per-function verdict tables for EVERY module and core contract in Phase 214 scope
    - Each function has BOTH an Access Verdict AND an Overflow Verdict
    - Contains "## GNRUS Access Control" with verdicts for all 6+ GNRUS functions
    - Contains "## Critical Access Control Changes" subsections for BurnieCoin modifier collapse, BurnieCoinflip expanded creditors, and vault-based ownership
    - Every verdict is one of: SAFE, VULNERABLE, INFO
    - No references to v5.0 adversarial audit artifacts (per D-02)
    - grep -c "SAFE\|VULNERABLE\|INFO" returns at least 200 (two verdicts per function minimum)
  </acceptance_criteria>
  <done>Every changed/new function has both an access control verdict and an integer overflow verdict. All modifier changes are catalogued and verified. All type narrowings are proven safe or flagged. GNRUS access control is fully audited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan IS the security audit -- it produces access control and overflow findings.

| Boundary | Description |
|----------|-------------|
| player -> Game | Untrusted caller can invoke any external function |
| module -> external contract | Module (as Game) calls creditFlip -- expanded creditor list must be verified |
| vault owner -> admin functions | vault.isVaultOwner replaces _contractOwner -- trust boundary shifted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-214-04 | E (Elevation of Privilege) | BurnieCoin modifier collapse | mitigate | This audit verifies no privilege escalation from 5+ modifiers -> onlyGame/onlyVault |
| T-214-05 | E (Elevation of Privilege) | BurnieCoinflip expanded creditors | mitigate | This audit verifies each new creditor (QUESTS, AFFILIATE, ADMIN) legitimately needs creditFlip access |
| T-214-06 | T (Tampering) | uint48->uint32 truncation | mitigate | This audit proves uint32 day index cannot overflow within game lifetime |
| T-214-07 | T (Tampering) | uint256->uint128 pool narrowing | mitigate | This audit proves maximum pool ETH fits in uint128 |
| T-214-08 | T (Tampering) | BitPackingLib shift overlap | mitigate | This audit verifies new shifts (184, 185, 209) do not collide with existing packed fields |
</threat_model>

<verification>
- 214-02-ACCESS-OVERFLOW.md exists with per-function verdict tables for all contracts in scope
- Every function has both access control AND overflow verdicts
- Modifier change matrix covers all changes from delta
- Type narrowing matrix covers all uint48->uint32 and uint256->uint128 changes
- No references to prior v5.0 audit artifacts
</verification>

<success_criteria>
Every changed/new function has dual verdicts (access + overflow). Zero modifier changes are uncatalogued. All type narrowings are analyzed with maximum-value proofs. GNRUS access control is fully verified.
</success_criteria>

<output>
After completion, create `.planning/phases/214-adversarial-audit/214-02-SUMMARY.md`
</output>
