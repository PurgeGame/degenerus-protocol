---
phase: 214-adversarial-audit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/214-adversarial-audit/214-03-STATE-COMPOSITION.md
autonomous: true
requirements: [ADV-01]
must_haves:
  truths:
    - "Every changed/new function has a state corruption verdict (SAFE / VULNERABLE / INFO)"
    - "Every changed/new function has a composition attack verdict (SAFE / VULNERABLE / INFO)"
    - "The pool consolidation write-batch pattern (_consolidatePoolsAndRewardJackpots) is verified for memory-to-storage consistency"
    - "All packed bitfield operations (presaleStatePacked, gameOverStatePacked, dailyJackpotTraitsPacked, mintPacked_, lootboxRng packed) are verified for read-modify-write atomicity"
    - "EndgameModule function redistribution is verified: no state is lost, duplicated, or orphaned across AdvanceModule/JackpotModule/WhaleModule"
  artifacts:
    - path: ".planning/phases/214-adversarial-audit/214-03-STATE-COMPOSITION.md"
      provides: "State corruption + composition attack audit document"
      contains: "## Per-Function Verdicts"
  key_links:
    - from: "213-DELTA-EXTRACTION.md scope definition"
      to: "214-03-STATE-COMPOSITION.md per-function verdicts"
      via: "Every function in Phase 214 scope has verdict rows for both state corruption and composition"
      pattern: "SAFE|VULNERABLE|INFO"
---

<objective>
State corruption and composition attack audit pass across all changed/new functions in the v5.0-to-HEAD delta.

Purpose: Identify any function where state can become inconsistent (packed field corruption, partial write, memory/storage mismatch) or where combining multiple functions creates an attack not visible in isolation. Third of three vulnerability-class passes per D-01.
Output: 214-03-STATE-COMPOSITION.md with per-function verdicts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/213-delta-extraction/213-DELTA-EXTRACTION.md
@.planning/phases/213-delta-extraction/213-01-DELTA-MODULES.md
@.planning/phases/213-delta-extraction/213-02-DELTA-CORE.md
@.planning/phases/214-adversarial-audit/214-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: State corruption and composition attack audit of all changed/new functions</name>
  <files>.planning/phases/214-adversarial-audit/214-03-STATE-COMPOSITION.md</files>
  <read_first>
    - .planning/phases/213-delta-extraction/213-DELTA-EXTRACTION.md (scope definition -- Phase 214 scope, all 99 cross-module chains, architectural change impact summary)
    - .planning/phases/213-delta-extraction/213-01-DELTA-MODULES.md (module function changelog)
    - .planning/phases/213-delta-extraction/213-02-DELTA-CORE.md (core function changelog)
    - .planning/phases/214-adversarial-audit/214-CONTEXT.md (D-01, D-02, D-03)
    - contracts/storage/DegenerusGameStorage.sol (ALL packed fields: slot 0 repack, slot 1 repack, presaleStatePacked, gameOverStatePacked, dailyJackpotTraitsPacked, _psRead/_psWrite, _goRead/_goWrite, _djtRead/_djtWrite, _lrRead/_lrWrite, _getCurrentPrizePool/_setCurrentPrizePool)
    - contracts/modules/DegenerusGameAdvanceModule.sol (_consolidatePoolsAndRewardJackpots -- entire pool math in memory then single SSTORE batch; _evaluateGameOverAndTarget -- sets/clears gameOverPossible; rngGate -- returns tuple with gapDays adjusting purchaseStartDay)
    - contracts/modules/DegenerusGameJackpotModule.sol (_processDailyEth with splitMode -- SPLIT_NONE/CALL1/CALL2 and resumeEthPool state; _addClaimableEth 3-return tuple; distributeYieldSurplus -- 23/23/23/23/8 split; runBafJackpot via self-call -- returns claimableDelta)
    - contracts/modules/DegenerusGameMintModule.sol (processTicketBatch moved from JackpotModule -- entropy source, cursor tracking; _raritySymbolBatch LCG PRNG assembly)
    - contracts/modules/DegenerusGameWhaleModule.sol (claimWhalePass moved from EndgameModule -- _queueTicketRange with rngBypass; purchaseDeityPass -- mintPacked_ HAS_DEITY_PASS_SHIFT)
    - contracts/modules/DegenerusGameDecimatorModule.sol (_terminalDecMultiplierBps redesigned -- day-index arithmetic; _awardDecimatorLootbox -- inline whale pass calculation)
    - contracts/modules/DegenerusGameDegeneretteModule.sol (_collectBetFunds -- packed milli-ETH/whole-BURNIE encoding; _placeDegeneretteBetCore -- activity score via _playerActivityScore)
    - contracts/modules/DegenerusGameLootboxModule.sol (multi-boon per player -- _activeBoonCategory/_boonCategory removed; _applyBoon upgrade semantics; gameOverPossible flag for endgame redirect)
    - contracts/modules/DegenerusGameBoonModule.sol (BoonConsumed events -- state clear then emit pattern)
    - contracts/modules/DegenerusGameGameOverModule.sol (gameOverStatePacked -- multiple packed flags; _sendToVault 33/33/34 split with GNRUS; handleGameOverDrain defense-in-depth)
    - contracts/modules/DegenerusGamePayoutUtils.sol (_creditClaimable -- claimablePool uint128; _queueWhalePassClaimCore)
    - contracts/modules/DegenerusGameMintStreakUtils.sol (_playerActivityScore 5-component scoring)
    - contracts/DegenerusGame.sol (delegatecall routing for moved functions; reverseFlip inline with compounding nudge; _processMintPayment claimablePool uint128; _claimWinningsInternal _goRead packed)
    - contracts/DegenerusAdmin.sol (Proposal struct repacked 3 slots from 7; _applyVote shared helpers; FeedProposal new struct)
    - contracts/DegenerusAffiliate.sol (75/20/5 roll replacing 3-recipient; leaderboard tracking moved post-taper; _resolveCodeOwner for default codes)
    - contracts/DegenerusQuests.sol (handlePurchase unifies mint+lootbox; rollLevelQuest new; all handle* gain mintPrice param)
    - contracts/BurnieCoin.sol (mintForGame expanded -- dual caller check composition)
    - contracts/BurnieCoinflip.sol (creditFlipBatch dynamic arrays; depositCoinflip auto-rebuy fix reads claimableStored)
    - contracts/DegenerusStonk.sol (claimVested -- vesting arithmetic; yearSweep -- 50/50 split, burns remaining)
    - contracts/StakedDegenerusStonk.sol (poolTransfer self-win burn instead of no-op; resolveRedemptionPeriod void return -- BURNIE paid on claim)
    - contracts/DegenerusVault.sol (gameDegeneretteBet consolidated with currency param; sdgnrsBurn/sdgnrsClaimRedemption new)
    - contracts/GNRUS.sol (entirely new -- soulbound enforcement, proportional burn math, governance proposal/vote/pickCharity, 2% distribution per level)
    - contracts/libraries/BitPackingLib.sol (new shift constants, MASK_6/MASK_2/MASK_1)
  </read_first>
  <action>
Per D-01 (vulnerability class split) and D-02 (fresh from scratch), audit every changed/new function for state corruption and composition attack vulnerabilities. Do NOT reference any v5.0 adversarial audit artifacts.

Create 214-03-STATE-COMPOSITION.md with the following structure:

```markdown
# Adversarial Audit: State Corruption + Composition Attack Pass

**Scope:** All changed/new functions in v5.0-to-HEAD delta (per 213-DELTA-EXTRACTION.md Phase 214 scope)
**Method:** Fresh audit (per D-02) -- no prior audit artifacts referenced
**Chains in scope:** SM-01 through SM-56, EF-01 through EF-20, RNG-01 through RNG-11, RO-01 through RO-12 (per D-03)

## Methodology

### State Corruption
For each function:
1. Identify all storage writes (SSTORE sites, packed field writes via helpers)
2. For packed bitfield operations: verify read-modify-write atomicity (no concurrent mutation from re-entry or parallel paths)
3. For memory-then-store patterns: verify ALL memory computations are flushed to storage before any external call or return
4. For struct repacking: verify no field overlap, no uninitialized bits interpreted as data
5. For moved functions: verify the function writes to exactly the same storage slots in the new location

### Composition Attacks
For each function pair/chain:
1. Can function A leave state that function B misinterprets?
2. Can calling A then B produce a state that neither A nor B can produce alone?
3. For the 99 cross-module chains: does the chain's state mutation sequence remain consistent across all possible ordering?
4. For the EndgameModule redistribution: does calling the redistributed function produce identical state effects?

## Findings

[If any VULNERABLE verdicts exist, list them here with F-STATE-NN or F-COMP-NN IDs]

## Packed State Field Audit

### Slot 0 Repack
[Full slot 0 layout with bit positions. Verify all reads/writes use correct shifts and masks. Verify 32/32 fill leaves no uninitialized gap.]

| Field | Bits | Shift | Mask | Read Helper | Write Helper | Verdict |
|-------|------|-------|------|-------------|--------------|---------|
| purchaseStartDay | 32 | [shift] | [mask] | [helper] | [helper] | SAFE/VULNERABLE |
| dailyIdx | 32 | [shift] | [mask] | [helper] | [helper] | SAFE/VULNERABLE |
| ... | ... | ... | ... | ... | ... | ... |

### Slot 1 Repack (currentPrizePool uint128 + claimablePool uint128)
[Verify the two uint128 values share a slot correctly. Verify no read of one corrupts the other.]

### presaleStatePacked
[Layout, read/write helpers _psRead/_psWrite, all consumers]

### gameOverStatePacked
[Layout, read/write helpers _goRead/_goWrite, all consumers]

### dailyJackpotTraitsPacked
[Layout, read/write helpers _djtRead/_djtWrite, all consumers]

### mintPacked_ (per-player)
[Layout with BitPackingLib shifts: HAS_DEITY_PASS_SHIFT(184), AFFILIATE_BONUS_LEVEL_SHIFT(185), AFFILIATE_BONUS_POINTS_SHIFT(209). Verify no overlap with existing fields.]

### Lootbox RNG Packed (_lrRead/_lrWrite)
[Layout: lootboxRngIndex, pending ETH (milli-ETH encoding), pending BURNIE (whole-BURNIE encoding). Verify encoding/decoding roundtrip.]

## EndgameModule Redistribution Verification

| Original Function (EndgameModule) | New Location | State Equivalence | Verdict |
|-----------------------------------|-------------|-------------------|---------|
| rewardTopAffiliate | AdvanceModule._rewardTopAffiliate (private, inlined) | [same state writes?] | SAFE/VULNERABLE |
| runRewardJackpots | AdvanceModule._consolidatePoolsAndRewardJackpots (inlined) | [same state writes?] | SAFE/VULNERABLE |
| _addClaimableEth | JackpotModule._addClaimableEth (modified return) | [same state writes?] | SAFE/VULNERABLE |
| _runBafJackpot | JackpotModule.runBafJackpot (external, self-call) | [same state writes?] | SAFE/VULNERABLE |
| claimWhalePass | WhaleModule.claimWhalePass | [same state writes?] | SAFE/VULNERABLE |

## Pool Consolidation Write-Batch Integrity

[Detailed analysis of _consolidatePoolsAndRewardJackpots:]
1. Which pool values are loaded into memory at the start?
2. Which computations modify the in-memory values?
3. At what point is the single SSTORE batch executed?
4. Is there ANY code path where an external call occurs between memory load and storage write?
5. Can the self-call to Game.runBafJackpot() cause a storage write that conflicts with the pending memory batch?
6. Verdict: SAFE (all pool math is memory-isolated) or VULNERABLE (external call between load and store)

## Two-Call Split State Consistency

[Analysis of the CALL1/CALL2 pattern in _processDailyEth:]
1. What state is written after CALL1 to resumeEthPool?
2. What state does CALL2 (_resumeDailyEth) read to reconstruct?
3. Can any function be called between CALL1 and CALL2 that modifies the stored state?
4. What happens if CALL2 reverts but CALL1 succeeded?
5. Verdict: SAFE or VULNERABLE with specific scenario

## Per-Function Verdicts

### Module Contracts

#### DegenerusGameAdvanceModule.sol

| Function | Visibility | State Writes | State Corruption Verdict | Composition Risk | Composition Verdict | Notes |
|----------|-----------|--------------|--------------------------|------------------|---------------------|-------|
| advanceGame | external | [list SSTORE targets] | SAFE/VULNERABLE/INFO | [composition risk] | SAFE/VULNERABLE/INFO | [analysis] |
| ... | ... | ... | ... | ... | ... | ... |

[Repeat table for every module and core contract in scope]

## Cross-Module Composition Analysis

### State-Mutation Chains (SM-01 through SM-56)

For each chain, assess whether the multi-step state mutation can leave inconsistent state:

| Chain | Caller -> Callee | Composition Risk | Verdict | Notes |
|-------|-----------------|------------------|---------|-------|
| SM-01 | Game.advanceGame -> AdvanceModule | [risk assessment] | SAFE/VULNERABLE/INFO | [analysis] |
| SM-02 | AdvanceModule._consolidate... -> JackpotModule.distributeYieldSurplus | [risk assessment] | SAFE/VULNERABLE/INFO | [analysis] |
| ... | ... | ... | ... | ... |

[Cover all 56 SM chains + 20 EF chains + 11 RNG chains + 12 RO chains]

## GNRUS State Integrity (New Contract)

[Dedicated analysis for the entirely new GNRUS contract:]
1. Soulbound enforcement: Can transfer/transferFrom/approve be bypassed?
2. Proportional burn math: Can rounding errors accumulate to drain more ETH than allocated?
3. Governance state: Can a proposal be double-counted or a vote be replayed?
4. pickCharity distribution: Can the 2% of unallocated be manipulated by timing pickCharity calls?
5. burnAtGameOver: What happens to pending governance proposals when all unallocated is burned?
```

Read every contract source file listed in read_first to determine actual storage operations and state transitions. Do not assume from the changelog alone -- verify against the source.

Key areas requiring deep analysis:
- _consolidatePoolsAndRewardJackpots memory batch: must trace every line from memory load to SSTORE
- Packed field helpers (_psRead/_psWrite, _goRead/_goWrite, etc.): must verify bit operations
- EndgameModule redistribution: must compare old and new state writes for equivalence
- GNRUS proportional math: must verify burn amount calculation cannot extract more than proportional share
  </action>
  <verify>
    <automated>grep -c "SAFE\|VULNERABLE\|INFO" .planning/phases/214-adversarial-audit/214-03-STATE-COMPOSITION.md</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/214-adversarial-audit/214-03-STATE-COMPOSITION.md exists
    - Contains "## Packed State Field Audit" with subsections for: Slot 0, Slot 1, presaleStatePacked, gameOverStatePacked, dailyJackpotTraitsPacked, mintPacked_, Lootbox RNG Packed
    - Contains "## EndgameModule Redistribution Verification" with all 5 moved functions verified
    - Contains "## Pool Consolidation Write-Batch Integrity" with the 6-point analysis
    - Contains "## Two-Call Split State Consistency" with the 5-point analysis
    - Contains per-function verdict tables for EVERY module and core contract in Phase 214 scope
    - Each function has BOTH a State Corruption Verdict AND a Composition Verdict
    - Contains "## Cross-Module Composition Analysis" covering all 99 chains (SM-01 to SM-56, EF-01 to EF-20, RNG-01 to RNG-11, RO-01 to RO-12)
    - Contains "## GNRUS State Integrity" with the 5-point analysis
    - Every verdict is one of: SAFE, VULNERABLE, INFO
    - No references to v5.0 adversarial audit artifacts (per D-02)
    - grep -c "SAFE\|VULNERABLE\|INFO" returns at least 200 (two verdicts per function + chain verdicts)
  </acceptance_criteria>
  <done>Every changed/new function has both a state corruption verdict and a composition attack verdict. All packed fields are bit-verified. EndgameModule redistribution is proven state-equivalent or flagged. Pool consolidation write-batch is proven memory-isolated or flagged. All 99 cross-module chains are assessed for composition risk. GNRUS state integrity is fully analyzed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan IS the security audit -- it produces state corruption and composition findings.

| Boundary | Description |
|----------|-------------|
| memory -> storage | Pool consolidation computes in memory, writes in batch -- window between them is the trust boundary |
| CALL1 -> CALL2 | Two-call split leaves mid-execution state in storage -- inter-call window is the trust boundary |
| EndgameModule -> redistributed | Functions moved across modules must preserve state write equivalence |
| packed fields | Bit-level operations must be atomic -- concurrent read-modify-write is the trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-214-09 | T (Tampering) | Pool consolidation memory batch | mitigate | This audit verifies no external call between memory load and SSTORE |
| T-214-10 | T (Tampering) | Two-call split mid-state | mitigate | This audit verifies CALL1/CALL2 state cannot be corrupted between calls |
| T-214-11 | T (Tampering) | Packed bitfield overlaps | mitigate | This audit verifies all packed field bit positions are non-overlapping |
| T-214-12 | I (Information Disclosure) | GNRUS proportional burn | mitigate | This audit verifies burn math cannot extract disproportionate value |
| T-214-13 | T (Tampering) | EndgameModule redistribution | mitigate | This audit verifies state equivalence of moved functions |
</threat_model>

<verification>
- 214-03-STATE-COMPOSITION.md exists with per-function verdict tables for all contracts in scope
- Every function has both state corruption AND composition verdicts
- Packed field audit covers all 7 packed state groups
- EndgameModule redistribution verified for all 5 moved functions
- Pool consolidation write-batch integrity analyzed
- Two-call split consistency analyzed
- All 99 chains assessed for composition risk
- No references to prior v5.0 audit artifacts
</verification>

<success_criteria>
Every changed/new function has dual verdicts (state corruption + composition). All packed fields are bit-verified. All architectural changes (pool consolidation, two-call split, EndgameModule redistribution) are explicitly analyzed for state consistency. GNRUS state integrity is fully verified.
</success_criteria>

<output>
After completion, create `.planning/phases/214-adversarial-audit/214-03-SUMMARY.md`
</output>
