---
phase: 214-adversarial-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/214-adversarial-audit/214-01-REENTRANCY-CEI.md
autonomous: true
requirements: [ADV-01]
must_haves:
  truths:
    - "Every changed/new function has a reentrancy verdict (SAFE / VULNERABLE / INFO)"
    - "Every external call site is documented with CEI compliance status"
    - "The AdvanceModule self-call pattern (Game -> AdvanceModule delegatecall -> Game.runBafJackpot self-call -> JackpotModule delegatecall) is explicitly analyzed for reentrancy"
    - "All 99 cross-module chains from Phase 213 are checked for reentrancy risk through their external call sites"
  artifacts:
    - path: ".planning/phases/214-adversarial-audit/214-01-REENTRANCY-CEI.md"
      provides: "Reentrancy + CEI compliance audit document"
      contains: "## Per-Function Verdicts"
  key_links:
    - from: "213-DELTA-EXTRACTION.md scope definition"
      to: "214-01-REENTRANCY-CEI.md per-function verdicts"
      via: "Every function in Phase 214 scope has a verdict row"
      pattern: "SAFE|VULNERABLE|INFO"
---

<objective>
Reentrancy and CEI compliance audit pass across all changed/new functions in the v5.0-to-HEAD delta.

Purpose: Identify any function where an external call is made before state is finalized, creating a reentrancy window. This is the first of three vulnerability-class passes per D-01.
Output: 214-01-REENTRANCY-CEI.md with per-function verdicts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/213-delta-extraction/213-DELTA-EXTRACTION.md
@.planning/phases/213-delta-extraction/213-01-DELTA-MODULES.md
@.planning/phases/213-delta-extraction/213-02-DELTA-CORE.md
@.planning/phases/214-adversarial-audit/214-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reentrancy and CEI compliance audit of all changed/new functions</name>
  <files>.planning/phases/214-adversarial-audit/214-01-REENTRANCY-CEI.md</files>
  <read_first>
    - .planning/phases/213-delta-extraction/213-DELTA-EXTRACTION.md (scope definition -- Phase 214 scope section lists every function to audit)
    - .planning/phases/213-delta-extraction/213-01-DELTA-MODULES.md (module function changelog)
    - .planning/phases/213-delta-extraction/213-02-DELTA-CORE.md (core function changelog)
    - .planning/phases/214-adversarial-audit/214-CONTEXT.md (D-01: split by vuln class, D-02: fresh from scratch)
    - contracts/modules/DegenerusGameAdvanceModule.sol (largest reentrancy surface -- self-call pattern, external calls to DGNRS/Quests/GNRUS/sDGNRS)
    - contracts/modules/DegenerusGameJackpotModule.sol (largest module -- 2157 lines changed, external calls to coinflip.creditFlip, two-call split state)
    - contracts/modules/DegenerusGameMintModule.sol (external calls to affiliate/quests/coinflip)
    - contracts/modules/DegenerusGameWhaleModule.sol (external calls to DGNRS, moved claimWhalePass)
    - contracts/modules/DegenerusGameDecimatorModule.sol (claimDecimatorJackpot _creditClaimable, terminal decimator)
    - contracts/modules/DegenerusGameDegeneretteModule.sol (external calls to quests/sDGNRS)
    - contracts/modules/DegenerusGameLootboxModule.sol (external calls to coinflip)
    - contracts/modules/DegenerusGameBoonModule.sol (BoonConsumed events -- check no external calls before state clear)
    - contracts/modules/DegenerusGameGameOverModule.sol (multiple external calls -- burnAtGameOver for GNRUS/DGNRS, _sendStethFirst)
    - contracts/modules/DegenerusGamePayoutUtils.sol (_creditClaimable, _calcAutoRebuy)
    - contracts/modules/DegenerusGameMintStreakUtils.sol (_playerActivityScore)
    - contracts/storage/DegenerusGameStorage.sol (shared storage, _queueTickets, events)
    - contracts/DegenerusGame.sol (entry contract -- delegatecall routing, reverseFlip inline, claimWinningsStethFirst)
    - contracts/DegenerusAdmin.sol (governance -- proposeFeedSwap/voteFeedSwap, onTokenTransfer external calls)
    - contracts/DegenerusAffiliate.sol (payAffiliate -- external calls to quests/coinflip)
    - contracts/DegenerusQuests.sol (handle* functions -- internal coinflip.creditFlip calls)
    - contracts/BurnieCoin.sol (mintForGame expanded access, decimatorBurn)
    - contracts/BurnieCoinflip.sol (depositCoinflip, _claimInternal with mintForGame, creditFlip/creditFlipBatch)
    - contracts/DegenerusStonk.sol (claimVested, yearSweep -- ETH transfers)
    - contracts/StakedDegenerusStonk.sol (poolTransfer self-win burn, resolveRedemptionPeriod void return)
    - contracts/DegenerusVault.sol (gameDegeneretteBet consolidated, sdgnrsBurn/sdgnrsClaimRedemption)
    - contracts/DegenerusDeityPass.sol (vault-based ownership)
    - contracts/GNRUS.sol (entirely new -- burn with ETH+stETH payout, governance, pickCharity)
    - contracts/WrappedWrappedXRP.sol (backing stripped)
  </read_first>
  <action>
Per D-01 (vulnerability class split) and D-02 (fresh from scratch), audit every changed/new function listed in 213-DELTA-EXTRACTION.md Phase 214 Scope for reentrancy and CEI compliance. Do NOT reference any v5.0 adversarial audit artifacts.

Create 214-01-REENTRANCY-CEI.md with the following structure:

```markdown
# Adversarial Audit: Reentrancy + CEI Compliance Pass

**Scope:** All changed/new functions in v5.0-to-HEAD delta (per 213-DELTA-EXTRACTION.md Phase 214 scope)
**Method:** Fresh audit (per D-02) -- no prior audit artifacts referenced
**Chains in scope:** SM-01 through SM-56, EF-01 through EF-20, RNG-01 through RNG-11, RO-01 through RO-12 (per D-03)

## Methodology

For each function:
1. Identify all external calls (call, delegatecall, staticcall, transfer, send)
2. For each external call, verify state is finalized BEFORE the call (CEI pattern)
3. If state mutation occurs after an external call, classify as VULNERABLE or INFO (with justification)
4. For delegatecall chains, trace through to the actual external calls made by the delegatee
5. For the self-call pattern (Game -> AdvanceModule -> Game.runBafJackpot -> JackpotModule), verify no reentrancy window exists between the self-call dispatch and the delegatecall resolution

## Findings

[If any VULNERABLE verdicts exist, list them here with F-REENT-NN IDs]

## Per-Function Verdicts

### Module Contracts (via delegatecall from DegenerusGame)

#### DegenerusGameAdvanceModule.sol

| Function | Visibility | External Calls | CEI Compliant | Verdict | Notes |
|----------|-----------|----------------|---------------|---------|-------|
| advanceGame | external | [list each: target.method()] | Yes/No | SAFE/VULNERABLE/INFO | [analysis] |
| ... | ... | ... | ... | ... | ... |

[Repeat table for every module: JackpotModule, MintModule, WhaleModule, DecimatorModule, DegeneretteModule, LootboxModule, BoonModule, GameOverModule, PayoutUtils, MintStreakUtils, Storage]

### Core Contracts

#### DegenerusGame.sol
[Same table format]

[Repeat for: DegenerusAdmin, DegenerusAffiliate, DegenerusQuests, BurnieCoin, BurnieCoinflip, DegenerusStonk, StakedDegenerusStonk, DegenerusVault, DegenerusDeityPass, GNRUS, WrappedWrappedXRP]

## High-Risk Patterns Analyzed

### Self-Call Reentrancy (AdvanceModule -> Game.runBafJackpot -> JackpotModule)
[Detailed analysis of whether the self-call from AdvanceModule._consolidatePoolsAndRewardJackpots() through Game.runBafJackpot() to JackpotModule.runBafJackpot() creates a reentrancy window. Specifically: is pool state in memory vs storage during the self-call? Can runBafJackpot's state writes conflict with the caller's pending memory-to-storage batch?]

### Two-Call Split Mid-State (JackpotModule CALL1/CALL2)
[Analysis of whether the mid-execution state between CALL1 and CALL2 of _processDailyEth can be exploited through reentrancy. What state is written after CALL1? Can an external call made during CALL1 re-enter before CALL2?]

### GNRUS Burn Reentrancy (GNRUS.burn -> game.claimWinnings)
[Analysis of whether GNRUS.burn() calling game.claimWinnings(address(this)) creates a reentrancy path back into GNRUS or game state]

### GameOver Drain Multi-Call (handleGameOverDrain -> burnAtGameOver x2 -> _creditClaimable)
[Analysis of sequential external calls to charityGameOver.burnAtGameOver() and dgnrs.burnAtGameOver() during drain]

### Cross-Module Chain Reentrancy Summary
[For each of the 99 chains (SM/EF/RNG/RO), a one-line assessment: does the chain involve an external call that could re-enter the caller? Group by risk level.]
```

For each function, read the actual Solidity source to determine:
- The exact external call targets and methods
- Whether state (storage writes) is finalized before or after external calls
- Whether the function uses nonReentrant or rngLocked guards
- Whether the delegatecall context means the "external call" is actually internal to Game's storage context

IMPORTANT: Functions that only make delegatecall within the Game's own module system are NOT external calls for reentrancy purposes (they execute in Game's storage context). Only calls to OTHER contracts (DegenerusStonk, BurnieCoinflip, DegenerusQuests, GNRUS, StakedDegenerusStonk, etc.) count as external calls.

The verdict for each function must be one of:
- SAFE: No reentrancy risk (either no external calls, or full CEI compliance, or adequate guards)
- VULNERABLE: Reentrancy risk identified (state mutation after external call without guard)
- INFO: Notable pattern but not exploitable (e.g., read-only external call, or post-call state mutation that cannot be exploited due to value constraints)
  </action>
  <verify>
    <automated>grep -c "SAFE\|VULNERABLE\|INFO" .planning/phases/214-adversarial-audit/214-01-REENTRANCY-CEI.md</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/214-adversarial-audit/214-01-REENTRANCY-CEI.md exists
    - Contains "## Per-Function Verdicts" section
    - Contains a verdict table for EVERY module contract: AdvanceModule, JackpotModule, MintModule, WhaleModule, DecimatorModule, DegeneretteModule, LootboxModule, BoonModule, GameOverModule
    - Contains a verdict table for EVERY core contract: DegenerusGame, DegenerusAdmin, DegenerusAffiliate, DegenerusQuests, BurnieCoin, BurnieCoinflip, DegenerusStonk, StakedDegenerusStonk, DegenerusVault, DegenerusDeityPass, GNRUS, WrappedWrappedXRP
    - Every changed/new function from 213-DELTA-EXTRACTION.md Phase 214 scope appears in the verdicts
    - Contains "## High-Risk Patterns Analyzed" with subsections for: Self-Call Reentrancy, Two-Call Split Mid-State, GNRUS Burn Reentrancy, GameOver Drain Multi-Call
    - Contains "## Cross-Module Chain Reentrancy Summary" covering all 99 chains
    - Every verdict is one of: SAFE, VULNERABLE, INFO
    - No references to v5.0 adversarial audit artifacts (per D-02)
    - grep -c "SAFE\|VULNERABLE\|INFO" returns at least 100 (one verdict per function minimum)
  </acceptance_criteria>
  <done>Every changed/new function in the Phase 214 scope has a reentrancy/CEI verdict. All high-risk patterns (self-call, two-call split, GNRUS burn, gameover drain) are explicitly analyzed. All 99 cross-module chains assessed for reentrancy risk.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan IS the security audit -- it produces reentrancy findings rather than consuming them.

| Boundary | Description |
|----------|-------------|
| player -> Game entry points | Untrusted calldata enters via purchase/bet/claim/advance functions |
| Game delegatecall -> external contracts | Module code (running as Game) makes external calls to Stonk/Coinflip/Quests/GNRUS/sDGNRS/Affiliate |
| Game self-call | AdvanceModule (via delegatecall) calls back into Game.runBafJackpot which delegates to JackpotModule |
| GNRUS -> Game | GNRUS.burn calls game.claimWinnings -- cross-contract callback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-214-01 | T (Tampering) | AdvanceModule self-call chain | mitigate | This audit explicitly analyzes the self-call pattern for state corruption via reentrancy |
| T-214-02 | T (Tampering) | Two-call split mid-state | mitigate | This audit explicitly analyzes the CALL1/CALL2 window for exploitable reentrancy |
| T-214-03 | T (Tampering) | GNRUS.burn -> game.claimWinnings callback | mitigate | This audit traces the callback path for reentrancy into GNRUS or game state |
</threat_model>

<verification>
- 214-01-REENTRANCY-CEI.md exists with per-function verdict tables for all contracts in scope
- Every function from 213-DELTA-EXTRACTION.md Phase 214 scope has a verdict
- High-risk patterns section covers self-call, two-call split, GNRUS burn, gameover drain
- Cross-module chain summary covers all 99 chains
- No references to prior v5.0 audit artifacts
</verification>

<success_criteria>
Every changed/new function has a reentrancy/CEI verdict. Zero functions are missing from the audit. All identified high-risk reentrancy patterns are explicitly analyzed with a conclusion.
</success_criteria>

<output>
After completion, create `.planning/phases/214-adversarial-audit/214-01-SUMMARY.md`
</output>
