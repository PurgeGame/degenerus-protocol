---
phase: 214-adversarial-audit
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/214-adversarial-audit/214-04-STORAGE-LAYOUT.md
autonomous: true
requirements: [ADV-02]
must_haves:
  truths:
    - "Storage layout is verified identical across ALL DegenerusGameStorage inheritors"
    - "forge inspect output is captured and compared for every inheritor"
    - "Any layout discrepancy is flagged as a finding"
  artifacts:
    - path: ".planning/phases/214-adversarial-audit/214-04-STORAGE-LAYOUT.md"
      provides: "Storage layout verification document with forge inspect evidence"
      contains: "## Layout Comparison"
  key_links:
    - from: "DegenerusGameStorage.sol"
      to: "All module contracts"
      via: "Solidity inheritance (delegatecall safety requires identical layout)"
      pattern: "identical|mismatch"
---

<objective>
Verify storage layout is identical across all DegenerusGameStorage inheritors using forge inspect output.

Purpose: All module contracts execute via delegatecall from DegenerusGame, meaning they share DegenerusGame's storage context. If any inheritor has a different storage layout, delegatecall would read/write wrong slots -- catastrophic state corruption. This is requirement ADV-02.
Output: 214-04-STORAGE-LAYOUT.md with forge inspect evidence and comparison
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/214-adversarial-audit/214-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Storage layout verification via forge inspect</name>
  <files>.planning/phases/214-adversarial-audit/214-04-STORAGE-LAYOUT.md</files>
  <read_first>
    - .planning/phases/214-adversarial-audit/214-CONTEXT.md (ADV-02 requirement)
    - contracts/storage/DegenerusGameStorage.sol (the base storage contract -- understand the full layout)
    - contracts/modules/DegenerusGameAdvanceModule.sol (first ~20 lines for inheritance declaration)
    - contracts/modules/DegenerusGameJackpotModule.sol (first ~20 lines for inheritance declaration)
    - contracts/modules/DegenerusGameMintModule.sol (first ~20 lines for inheritance declaration -- also inherits MintStreakUtils)
    - contracts/modules/DegenerusGameWhaleModule.sol (first ~20 lines for inheritance declaration)
    - contracts/modules/DegenerusGameDecimatorModule.sol (first ~20 lines for inheritance declaration)
    - contracts/modules/DegenerusGameDegeneretteModule.sol (first ~20 lines for inheritance declaration)
    - contracts/modules/DegenerusGameLootboxModule.sol (first ~20 lines for inheritance declaration)
    - contracts/modules/DegenerusGameBoonModule.sol (first ~20 lines for inheritance declaration)
    - contracts/modules/DegenerusGameGameOverModule.sol (first ~20 lines for inheritance declaration)
    - contracts/modules/DegenerusGamePayoutUtils.sol (first ~20 lines for inheritance declaration)
    - contracts/modules/DegenerusGameMintStreakUtils.sol (first ~20 lines for inheritance declaration)
    - contracts/DegenerusGame.sol (first ~30 lines for inheritance declaration -- this is the delegatecall dispatcher)
  </read_first>
  <action>
Run `forge inspect` for DegenerusGameStorage and every contract that inherits from it (all module contracts + DegenerusGame itself). Compare the storage layout output to verify all layouts are identical.

Step 1: Identify all DegenerusGameStorage inheritors by reading each contract's inheritance declaration. The expected list is:
- DegenerusGameStorage (base)
- DegenerusGamePayoutUtils (inherits Storage)
- DegenerusGameMintStreakUtils (inherits Storage or PayoutUtils)
- DegenerusGameAdvanceModule
- DegenerusGameJackpotModule
- DegenerusGameMintModule (inherits MintStreakUtils)
- DegenerusGameWhaleModule
- DegenerusGameDecimatorModule
- DegenerusGameDegeneretteModule
- DegenerusGameLootboxModule
- DegenerusGameBoonModule
- DegenerusGameGameOverModule
- DegenerusGame (the dispatcher)

Step 2: For each contract, run:
```bash
forge inspect <ContractName> storageLayout 2>/dev/null
```

Capture the output. The storage layout includes slot numbers, offsets, types, and variable names for every storage variable.

Step 3: Compare all layouts. The storage variables from DegenerusGameStorage must appear at identical slots and offsets in every inheritor. Inheritors may have ADDITIONAL variables (appended after Storage's variables), but the Storage portion must be byte-for-byte identical.

Step 4: Check for any inheritor that adds its own storage variables BEFORE or BETWEEN DegenerusGameStorage variables (this would shift slots and break delegatecall).

Create 214-04-STORAGE-LAYOUT.md:

```markdown
# Adversarial Audit: Storage Layout Verification (ADV-02)

**Method:** forge inspect storageLayout comparison across all DegenerusGameStorage inheritors
**Date:** [execution date]

## Inheritance Tree

```
DegenerusGameStorage
  +-- DegenerusGamePayoutUtils
  |     +-- DegenerusGameMintStreakUtils
  |           +-- DegenerusGameMintModule
  +-- DegenerusGameAdvanceModule
  +-- DegenerusGameJackpotModule
  +-- DegenerusGameWhaleModule
  +-- DegenerusGameDecimatorModule
  +-- DegenerusGameDegeneretteModule
  +-- DegenerusGameLootboxModule
  +-- DegenerusGameBoonModule
  +-- DegenerusGameGameOverModule
  +-- DegenerusGame
```
[Correct this tree based on actual inheritance found in source]

## DegenerusGameStorage Base Layout

[forge inspect output for DegenerusGameStorage -- full slot table]

| Slot | Offset | Type | Variable | Size |
|------|--------|------|----------|------|
| 0 | 0 | [type] | [name] | [bytes] |
| ... | ... | ... | ... | ... |

## Layout Comparison

### Methodology
For each inheritor, the Storage-derived slots must match exactly:
- Same slot number
- Same byte offset within slot
- Same type
- Same variable name

### Results

| Contract | Storage Slots Match | Extra Slots | Verdict |
|----------|--------------------:|-------------|---------|
| DegenerusGameStorage (base) | -- | -- | REFERENCE |
| DegenerusGamePayoutUtils | [Yes/No] | [list any extra] | IDENTICAL/MISMATCH |
| DegenerusGameMintStreakUtils | [Yes/No] | [list any extra] | IDENTICAL/MISMATCH |
| DegenerusGameAdvanceModule | [Yes/No] | [list any extra] | IDENTICAL/MISMATCH |
| DegenerusGameJackpotModule | [Yes/No] | [list any extra] | IDENTICAL/MISMATCH |
| DegenerusGameMintModule | [Yes/No] | [list any extra] | IDENTICAL/MISMATCH |
| DegenerusGameWhaleModule | [Yes/No] | [list any extra] | IDENTICAL/MISMATCH |
| DegenerusGameDecimatorModule | [Yes/No] | [list any extra] | IDENTICAL/MISMATCH |
| DegenerusGameDegeneretteModule | [Yes/No] | [list any extra] | IDENTICAL/MISMATCH |
| DegenerusGameLootboxModule | [Yes/No] | [list any extra] | IDENTICAL/MISMATCH |
| DegenerusGameBoonModule | [Yes/No] | [list any extra] | IDENTICAL/MISMATCH |
| DegenerusGameGameOverModule | [Yes/No] | [list any extra] | IDENTICAL/MISMATCH |
| DegenerusGame | [Yes/No] | [list any extra] | IDENTICAL/MISMATCH |

## Findings

[Any MISMATCH entries get F-STORAGE-NN finding IDs]

## Slot 0 Repack Verification

[Special attention to the slot 0 repack (levelStartTime->purchaseStartDay uint32, dailyIdx uint32, etc.):]
- Total bits used in slot 0: [X]/256
- Field boundaries: [list each field with bit range]
- Verification: [all forge inspect outputs show identical slot 0 layout]

## Slot 1 Repack Verification

[currentPrizePool uint128 + claimablePool uint128:]
- Offset 0: currentPrizePool (16 bytes)
- Offset 16: claimablePool (16 bytes)
- Verification: [all forge inspect outputs show identical slot 1 layout]

## Conclusion

Storage layout is [IDENTICAL/NOT IDENTICAL] across all DegenerusGameStorage inheritors. Delegatecall safety is [CONFIRMED/AT RISK].
```

IMPORTANT: If forge inspect produces JSON output, parse it to extract the storage layout table. If it produces a tabular format, capture it directly. The key fields to compare are: slot number, offset, type, and label (variable name).
  </action>
  <verify>
    <automated>grep -c "IDENTICAL\|MISMATCH" .planning/phases/214-adversarial-audit/214-04-STORAGE-LAYOUT.md</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/214-adversarial-audit/214-04-STORAGE-LAYOUT.md exists
    - Contains "## Inheritance Tree" showing the actual inheritance hierarchy
    - Contains "## DegenerusGameStorage Base Layout" with forge inspect output
    - Contains "## Layout Comparison" with a verdict row for every inheritor (at least 12 contracts)
    - Every inheritor has a verdict of IDENTICAL or MISMATCH
    - Contains "## Slot 0 Repack Verification" with bit-level field boundaries
    - Contains "## Slot 1 Repack Verification" with uint128+uint128 layout
    - Contains "## Conclusion" with overall delegatecall safety assessment
    - No references to v5.0 adversarial audit artifacts (per D-02)
  </acceptance_criteria>
  <done>Storage layout verified identical (or mismatches flagged) across all DegenerusGameStorage inheritors. forge inspect evidence captured. Slot 0 and Slot 1 repacks verified at bit level. Delegatecall safety confirmed or findings raised.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DegenerusGame storage -> module delegatecall | All modules execute in Game's storage context via delegatecall -- layout mismatch = catastrophic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-214-14 | T (Tampering) | Storage layout mismatch | mitigate | forge inspect comparison across all inheritors |
| T-214-15 | T (Tampering) | Slot 0/1 repack correctness | mitigate | Bit-level verification of packed slot fields |
</threat_model>

<verification>
- 214-04-STORAGE-LAYOUT.md exists with forge inspect evidence
- All inheritors compared
- Slot 0 and Slot 1 repacks bit-verified
- Overall delegatecall safety conclusion stated
</verification>

<success_criteria>
forge inspect output captured for all DegenerusGameStorage inheritors. Layout comparison shows IDENTICAL for all or findings raised for any MISMATCH. Slot repacks verified at bit level.
</success_criteria>

<output>
After completion, create `.planning/phases/214-adversarial-audit/214-04-SUMMARY.md`
</output>
