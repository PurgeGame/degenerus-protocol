---
phase: 39-passes-and-vault
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ../simulator/src/mechanics/passPricing.ts
  - ../simulator/src/mechanics/passPricing.test.ts
  - ../simulator/src/mechanics/passCapitalInjection.ts
  - ../simulator/src/mechanics/passCapitalInjection.test.ts
autonomous: true
requirements: [PASS-01, PASS-02, PASS-03, PASS-07]

must_haves:
  truths:
    - "Whale bundle prices 2.4 ETH for levels 0-3, 4 ETH for level 4+ with 10/25/50% boon discounts"
    - "Lazy pass prices 0.24 ETH flat for levels 0-2 with bonus tickets from excess, sum-of-10-level-prices for level 3+"
    - "Deity pass uses triangular pricing 24 + T(n) ETH where T(n) = n*(n+1)/2, 32-pass cap, with tier-based discounts"
    - "Pass capital injection splits match contract: Whale/Deity 30/70 (lvl 0), 5/95 (lvl 1+). Lazy 90/10 all levels"
  artifacts:
    - path: "../simulator/src/mechanics/passPricing.ts"
      provides: "Pass pricing functions for all 3 pass types"
      exports: ["calculateWhaleBundlePrice", "calculateLazyPassPrice", "calculateDeityPassPrice", "WHALE_EARLY_PRICE", "WHALE_STANDARD_PRICE", "DEITY_BASE_PRICE", "LAZY_FLAT_PRICE", "DEITY_MAX_PASSES"]
    - path: "../simulator/src/mechanics/passCapitalInjection.ts"
      provides: "Pool split routing for pass purchases"
      exports: ["routePassCapital", "PassCapitalResult"]
  key_links:
    - from: "../simulator/src/mechanics/passPricing.ts"
      to: "../simulator/src/mechanics/priceLookup.ts"
      via: "priceForLevel used in lazy pass sum-of-10 calculation"
      pattern: "priceForLevel"
---

<objective>
Implement pass pricing and capital injection modules for all three pass types (whale bundle, lazy pass, deity pass).

Purpose: These pure-function modules provide the pricing math and pool routing that downstream deity bonus, boon, and engine integration tasks depend on.
Output: Two tested mechanics modules with full pricing and capital split logic.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@../simulator/src/mechanics/priceLookup.ts
@../simulator/src/mechanics/activityScore.ts
@../simulator/src/types/state.ts
@../simulator/src/types/config.ts

<interfaces>
<!-- Existing priceLookup module used by lazy pass sum-of-10 -->
From ../simulator/src/mechanics/priceLookup.ts:
```typescript
export function priceForLevel(level: number): bigint;
```

From ../simulator/src/types/state.ts:
```typescript
export interface PoolState {
  nextPool: bigint;
  currentPool: bigint;
  futurePool: bigint;
  claimablePool: bigint;
}
```

<!-- Contract constants extracted from DegenerusGameWhaleModule.sol -->
Whale bundle constants:
- WHALE_BUNDLE_EARLY_PRICE = 2.4 ETH (levels 0-3)
- WHALE_BUNDLE_STANDARD_PRICE = 4 ETH (level 4+ / x49/x99)
- Boon discounts: tier 1 = 10% (1000 BPS), tier 2 = 25% (2500 BPS), tier 3 = 50% (5000 BPS)
- qty 1-100, covers 100 levels
- Tickets: 40/level for bonus levels (passLevel through level 10), 2/level for levels 11+

Lazy pass constants:
- LAZY_FLAT_PRICE = 0.24 ETH (levels 0-2, excess over actual cost -> bonus tickets)
- Sum-of-10-level-prices for level 3+ (sum of priceForLevel for 10 consecutive levels)
- Purchase windows: levels 0-2, then only x9 levels (9,19,29...) or with valid boon
- Provides 4 tickets/level for 10 consecutive levels
- Blocked if player holds deity pass or has 7+ levels remaining on freeze

Deity pass constants:
- DEITY_PASS_BASE = 24 ETH
- Price = 24 + T(k) where T(k) = k*(k+1)/2, k = passes already sold (0-indexed)
- 32-pass cap (one per symbol)
- Boon tiers: tier 1 = 10% off, tier 2 = 25% off, tier 3 = 50% off
- Transfer cost: 5 ETH worth of BURNIE burned
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pass pricing module with all 3 pass types</name>
  <files>../simulator/src/mechanics/passPricing.ts, ../simulator/src/mechanics/passPricing.test.ts</files>
  <behavior>
    - Whale bundle: calculateWhaleBundlePrice(level, qty, boonTier) returns correct price
      - level 0, qty 1, no boon -> 2.4 ETH
      - level 3, qty 5, no boon -> 12 ETH (2.4 * 5)
      - level 4, qty 1, no boon -> 4 ETH
      - level 50, qty 2, boonTier 1 -> 7.2 ETH (4 * 0.9 * 2)
      - level 50, qty 1, boonTier 2 -> 3 ETH (4 * 0.75)
      - level 50, qty 1, boonTier 3 -> 2 ETH (4 * 0.5)
    - Lazy pass: calculateLazyPassPrice(currentLevel, hasBoon, boonDiscountBps)
      - level 0, no boon -> 0.24 ETH
      - level 2, no boon -> 0.24 ETH
      - level 3, no boon -> sum of priceForLevel(4..13) = 10 * 0.04 ETH = 0.4 ETH
      - level 9, no boon -> sum of priceForLevel(10..19) = 0.04*20 + 0.04*10 = check actual prices
      - level 29, no boon -> sum of priceForLevel(30..39) = 10 * 0.08 ETH = 0.8 ETH
      - level 0, boon 1000 BPS -> 0.24 * 0.9 = 0.216 ETH
    - Lazy pass bonus tickets: calculateLazyPassBonusTickets(currentLevel) returns ticket count from excess
      - level 0: (0.24 ETH - sum(priceForLevel(1..10))) / priceForLevel(1) * 4 tickets
    - Deity pass: calculateDeityPassPrice(passesSold, boonTier) returns correct triangular price
      - 0 sold, no boon -> 24 ETH
      - 1 sold, no boon -> 25 ETH (24 + 1)
      - 5 sold, no boon -> 39 ETH (24 + 15)
      - 31 sold, no boon -> 520 ETH (24 + 496)
      - 10 sold, boonTier 2 -> (24 + 55) * 0.75 = 59.25 ETH
    - Lazy pass window validation: isLazyPassWindowValid(level) returns true for 0-2 and x9 levels
  </behavior>
  <action>
    Create `../simulator/src/mechanics/passPricing.ts` with:

    1. Export constants: WHALE_EARLY_PRICE (2_400_000_000_000_000_000n), WHALE_STANDARD_PRICE (4n * 10n**18n), DEITY_BASE_PRICE (24n * 10n**18n), LAZY_FLAT_PRICE (240_000_000_000_000_000n), DEITY_MAX_PASSES (32), LAZY_PASS_LEVELS (10), LAZY_PASS_TICKETS_PER_LEVEL (4).

    2. `calculateWhaleBundlePrice(level: number, qty: number, boonTier: 0 | 1 | 2 | 3): bigint`
       - level 0-3: WHALE_EARLY_PRICE * BigInt(qty)
       - level 4+: WHALE_STANDARD_PRICE * BigInt(qty), apply boon discount if tier > 0
       - Boon discount: unitPrice = WHALE_STANDARD_PRICE * (10000n - discountBps) / 10000n
       - discountBps: tier 1 = 1000, tier 2 = 2500, tier 3 = 5000

    3. `calculateLazyPassPrice(currentLevel: number, hasBoon: boolean, boonDiscountBps: number): bigint`
       - levels 0-2: benefitValue = LAZY_FLAT_PRICE. If hasBoon, apply discount. Otherwise totalPrice = LAZY_FLAT_PRICE
       - level 3+: benefitValue = sum of priceForLevel(startLevel + i) for i=0..9 where startLevel = currentLevel + 1 (or 1 if level 0). If hasBoon, apply discount. Otherwise totalPrice = benefitValue
       - Import priceForLevel from './priceLookup.js'

    4. `calculateLazyPassBonusTickets(currentLevel: number): number`
       - Only applicable at levels 0-2 where flat price exceeds sum-of-10
       - startLevel = currentLevel == 0 ? 1 : currentLevel + 1
       - baseCost = sum of priceForLevel for 10 levels starting at startLevel
       - balance = LAZY_FLAT_PRICE - baseCost (if positive)
       - bonusTickets = Number((balance * 4n) / priceForLevel(startLevel))

    5. `calculateDeityPassPrice(passesSold: number, boonTier: 0 | 1 | 2 | 3): bigint`
       - k = passesSold (0-indexed, 0..31)
       - basePrice = DEITY_BASE_PRICE + (BigInt(k) * BigInt(k + 1) * ETH / 2n) where ETH = 10n**18n
       - Apply boon discount same as whale tiers
       - Cap at DEITY_MAX_PASSES

    6. `isLazyPassWindowValid(level: number): boolean`
       - Returns true if level <= 2 OR level % 10 === 9

    Write tests in passPricing.test.ts covering all behaviors listed above. Tests use vitest with `import { describe, it, expect } from 'vitest'`.
  </action>
  <verify>
    <automated>cd ../simulator && npx vitest run src/mechanics/passPricing.test.ts</automated>
  </verify>
  <done>All pass pricing functions return values matching contract constants. Test suite passes with at least 15 test cases covering whale/lazy/deity pricing, boon discounts, lazy bonus tickets, and window validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pass capital injection module</name>
  <files>../simulator/src/mechanics/passCapitalInjection.ts, ../simulator/src/mechanics/passCapitalInjection.test.ts</files>
  <behavior>
    - Whale/Deity at level 0: 30% next / 70% future
    - Whale/Deity at level 1+: 5% next / 95% future
    - Lazy at level 0: 90% next / 10% future
    - Lazy at level 3+: 90% next / 10% future (same split all levels)
    - routePassCapital returns { nextPoolDelta, futurePoolDelta } as bigints summing to totalPrice
    - Whale at level 0, 2.4 ETH: next = 720000000000000000n, future = 1680000000000000000n
    - Deity at level 5, 39 ETH: next = 1.95 ETH, future = 37.05 ETH
    - Lazy at level 9, 0.4 ETH: next = 0.36 ETH, future = 0.04 ETH
  </behavior>
  <action>
    Create `../simulator/src/mechanics/passCapitalInjection.ts` with:

    1. Type: `PassType = 'whale' | 'lazy' | 'deity'`

    2. Interface: `PassCapitalResult = { nextPoolDelta: bigint; futurePoolDelta: bigint }`

    3. `routePassCapital(passType: PassType, totalPrice: bigint, currentLevel: number): PassCapitalResult`
       - Whale/Deity splits (from contract DegenerusGameWhaleModule.sol):
         - level 0: nextShare = totalPrice * 3000n / 10000n (30%), rest to future
         - level 1+: nextShare = totalPrice * 500n / 10000n (5%), rest to future
       - Lazy splits (from contract, LAZY_PASS_TO_FUTURE_BPS = 1000):
         - All levels: futureShare = totalPrice * 1000n / 10000n (10%), nextShare = totalPrice - futureShare (90%)
       - Return { nextPoolDelta: nextShare, futurePoolDelta: totalPrice - nextShare } for whale/deity
       - Return { nextPoolDelta: nextShare, futurePoolDelta: futureShare } for lazy

    Write tests covering all pass types at level 0 and level 5+, verifying exact BPS math with known values.
  </action>
  <verify>
    <automated>cd ../simulator && npx vitest run src/mechanics/passCapitalInjection.test.ts</automated>
  </verify>
  <done>Capital injection splits match contract BPS constants for all pass types at level 0 and level 1+. Tests verify exact bigint values for at least 6 cases.</done>
</task>

</tasks>

<verification>
- All tests pass: `cd ../simulator && npx vitest run src/mechanics/passPricing.test.ts src/mechanics/passCapitalInjection.test.ts`
- Whale bundle at level 0 qty 1 = 2.4 ETH, level 50 qty 1 = 4 ETH
- Deity pass first = 24 ETH, last (31st sold) = 520 ETH
- Lazy pass level 0 = 0.24 ETH, level 29 = 0.8 ETH (sum of 10 prices at 0.08)
- Pool splits: whale level 0 = 30/70, whale level 5 = 5/95, lazy level anything = 90/10
</verification>

<success_criteria>
Pass pricing and capital injection modules are complete, tested, and ready for consumption by the engine integration plan.
</success_criteria>

<output>
After completion, create `.planning/phases/39-passes-and-vault/39-01-SUMMARY.md`
</output>
