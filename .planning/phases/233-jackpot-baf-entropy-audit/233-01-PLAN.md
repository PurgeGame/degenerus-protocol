---
phase: 233-jackpot-baf-entropy-audit
plan: 233-01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/233-jackpot-baf-entropy-audit/233-01-AUDIT.md
autonomous: true
requirements: [JKP-01]
must_haves:
  truths:
    - "Every function modified by commit 104b5d42 in 230-01-DELTA-MAP.md §1.2 (runBafJackpot body + four emit call-sites) has at least one verdict row in the per-function verdict table"
    - "Every attack vector in CONTEXT.md D-04 (domain-collision sweep: every on-chain reader of event `traitId` field and every on-chain read of `winningTraitsPacked` / `bonusTraitsPacked` storage either narrows to uint8 before indexing a trait table, treats the sentinel as an opaque tag, or does not exist) appears as at least one verdict row"
    - "Every verdict row cites the 7-char commit SHA `104b5d42` (per D-02)"
    - "Every verdict row cites a File:Line anchor pointing into contracts/modules/DegenerusGameJackpotModule.sol at HEAD (per D-02)"
    - "Verdict vocabulary used is exactly `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (per D-02)"
    - "Every verdict row carries a `Finding Candidate: Y/N` column value (per D-02 extended column set, matching the 232-NN-AUDIT.md precedent)"
    - "No F-29-NN finding IDs appear anywhere in 233-01-AUDIT.md (per D-11)"
    - "The four emit sites inside `runBafJackpot` at DegenerusGameJackpotModule.sol:2002 (JackpotEthWin large-winner), :2014 (JackpotTicketWin large-winner), :2034 (JackpotEthWin small-winner even-idx), :2038 (JackpotTicketWin small-winner odd-idx) each have a dedicated verdict row confirming the 420 sentinel is passed, no payout-math change, no CEI shift"
    - "A domain-collision sweep enumerates EVERY on-chain reader of event `traitId` (JackpotEthWin, JackpotTicketWin) inside contracts/ — for each reader either (a) narrows to uint8 before indexing a trait table, (b) treats the value as an opaque tag, or (c) does not exist (off-chain indexer territory) — per D-04"
    - "A domain-collision sweep enumerates EVERY on-chain read of storage `winningTraitsPacked` / `bonusTraitsPacked` inside contracts/ — for each reader confirm it cannot produce a value == 420 because the unpacking is done via JackpotBucketLib.unpackWinningTraits which returns uint8[4] — per D-04"
    - "The new file-scope constant `uint16 private constant BAF_TRAIT_SENTINEL = 420;` at DegenerusGameJackpotModule.sol:136 has a verdict row confirming (a) type is uint16 (wider than uint8 real-trait space), (b) value 420 is outside the 0-255 real-trait domain by construction, (c) visibility `private` prevents external misuse as a trait ID"
    - "Event-signature widening (`uint8 traitId -> uint16 traitId` on JackpotEthWin at line 69-78 and JackpotTicketWin at line 80-85) is recorded as a SAFE-INFO Finding Candidate: Y row — off-chain ABI regeneration required; not an on-chain finding but a Phase 236 candidate note for indexer team"
    - "Findings-Candidate Block, Scope-guard Deferrals, and Downstream Hand-offs subsections present; Phase 236 FIND-01 hand-off explicitly named for ID assignment"
    - "Zero contracts/ and zero test/ writes occur during plan execution (READ-only per v29.0 milestone rule)"
  artifacts:
    - path: ".planning/phases/233-jackpot-baf-entropy-audit/233-01-AUDIT.md"
      provides: "JKP-01 BAF traitId=420 sentinel adversarial audit"
      contains: "## Per-Function Verdict Table"
  key_links:
    - from: "230-01-DELTA-MAP.md §1.2 (runBafJackpot MODIFIED + BAF_TRAIT_SENTINEL constant + event widenings) + §2.3 IM-14 + IM-15 + §3.3.c ID-84 + §4 JKP-01 row"
      to: "233-01-AUDIT.md per-function verdict table rows"
      via: "Every 104b5d42-owned emit site and constant declaration has a verdict"
      pattern: "SAFE|SAFE-INFO|VULNERABLE|DEFERRED"
    - from: "233-01-AUDIT.md domain-collision sweep"
      to: "Phase 236 FIND-01 finding-candidate pool"
      via: "Findings-Candidate Block (no F-29-NN IDs, Finding Candidate: Y/N column)"
      pattern: "## Findings-Candidate Block"
    - from: "233-01-AUDIT.md event-widening row (SAFE-INFO Finding Candidate: Y)"
      to: "Phase 236 FIND-01 / FIND-02 indexer-compat note"
      via: "Downstream Hand-offs subsection (off-chain ABI regeneration)"
      pattern: "Phase 236 FIND"
---

<objective>
JKP-01 adversarial audit of the BAF `traitId=420` sentinel introduced by commit `104b5d42` ("feat(jackpot): tag BAF wins with traitId=420 sentinel"). Produces `233-01-AUDIT.md`: a per-function verdict table covering the four `emit` sites inside `runBafJackpot` (two `JackpotEthWin`, two `JackpotTicketWin`), the new file-scope constant `uint16 private constant BAF_TRAIT_SENTINEL = 420;`, and a domain-collision sweep enumerating every on-chain reader of event `traitId` fields and every on-chain read of `winningTraitsPacked` / `bonusTraitsPacked` storage to confirm no downstream branch treats 420 as a real trait ID. Plus Findings-Candidate / Scope-guard Deferrals / Downstream Hand-offs subsections.

Purpose: The BAF payout paths in `runBafJackpot` now tag four `emit` sites with the file-scope constant `BAF_TRAIT_SENTINEL = 420` instead of a literal `0`. The event `traitId` field was widened `uint8 -> uint16` to fit the sentinel above the real 0-255 trait space. A C4A warden would probe: (a) whether 420 can collide with a real trait ID anywhere in the on-chain path (it cannot by construction — real traits are stored/read as `uint8` in every non-event code path, so 420 is out-of-domain by the type system), (b) whether any downstream on-chain branch treats `traitId == 420` as if it were a real trait (event consumer tolerance), (c) whether the emit payloads in `runBafJackpot` correctly carry the sentinel at all four sites, (d) whether the event-widening `uint8 -> uint16` introduces any on-chain side effect (it does not — topic encoding is unchanged, only the ABI signature hash changes, which is an off-chain / indexer concern). This plan surfaces adversarial verdicts per D-04 (domain-collision sweep enumerating every on-chain reader of event `traitId` + every on-chain read of `winningTraitsPacked` / `bonusTraitsPacked` storage). Phase 236 FIND-01 owns canonical `F-29-NN` ID assignment per D-11. No cross-path bonus-trait consistency check is performed here — that is JKP-03's scope (per D-01 three-plan split).

Output: `233-01-AUDIT.md` with per-function verdict table + domain-collision sweep table + Findings-Candidate Block + Scope-guard Deferrals + Downstream Hand-offs subsections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/233-jackpot-baf-entropy-audit/233-CONTEXT.md
@.planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md
@.planning/phases/230-delta-extraction-scope-map/230-02-DELTA-ADDENDUM.md
@.planning/phases/230-delta-extraction-scope-map/230-01-SUMMARY.md
@.planning/phases/231-earlybird-jackpot-audit/231-02-PLAN.md
@.planning/phases/232-decimator-audit/232-01-PLAN.md
@.planning/phases/232-decimator-audit/232-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract 104b5d42 diff + read JackpotModule at HEAD for every JKP-01 target; run domain-collision sweep; write 233-01-AUDIT.md</name>
  <files>.planning/phases/233-jackpot-baf-entropy-audit/233-01-AUDIT.md</files>
  <read_first>
    - .planning/phases/233-jackpot-baf-entropy-audit/233-CONTEXT.md (D-01 through D-12 — specifically: D-01 three-plan split with Plan 233-01 owning JKP-01 only; D-02 locked column schema `Function | Commit SHA | File:Line | Verdict | Evidence | Notes` plus `Finding Candidate: Y/N` extension per 232-NN-AUDIT.md precedent; D-03 scope-guard deferral rule (record in SUMMARY, do NOT edit 230-01-DELTA-MAP.md); D-04 JKP-01 methodology — domain-collision sweep, narrowing-to-uint8 / opaque-tag / non-existence classification; D-10 ID-103 interface-drift already PASS'd in Phase 230 §3.3.f (not re-verified here); D-11 no F-29-NN IDs emitted (Phase 236 FIND-01 owns ID assignment); D-12 MUST read 230-02-DELTA-ADDENDUM.md — but NOTE: 104b5d42 commit is NOT in the addendum's commit set (`314443af` + `c2e5e0a9`), so addendum impact on JKP-01 is limited to confirming the BAF sentinel surface was not additionally touched by the two post-Phase-230 commits)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.2 (lines 91-105 — `_runEarlyBirdLootboxJackpot` MODIFIED by 20a951df is out of scope for JKP-01; `runBafJackpot` MODIFIED by 104b5d42 IS the JKP-01 body-level target; the Non-function declarations block at lines 100-103 lists the two widened events `JackpotEthWin` / `JackpotTicketWin` + the new file-scope constant `BAF_TRAIT_SENTINEL = 420` — all in scope)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §2.3 IM-14 (line 263 — self-call `_consolidatePoolsAndRewardJackpots -> DegenerusGame.runBafJackpot` via `IDegenerusGame(address(this))`; pre-existing call site around source line 746 unchanged; included because callee is MODIFIED) and IM-15 (line 264 — delegatecall `DegenerusGame.runBafJackpot -> DegenerusGameJackpotModule.runBafJackpot` via `IDegenerusGameJackpotModule.runBafJackpot.selector`; selector unchanged; callee body modified to emit widened events with 420 sentinel)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §3.3.c ID-84 (line 417 — `runBafJackpot(uint256 poolWei, uint24 lvl, uint256 rngWord) external returns (uint256 claimableDelta)` — interface `IDegenerusGameJackpotModule` vs implementer `DegenerusGameJackpotModule.runBafJackpot` at line 1966 — PASS at HEAD; function body MODIFIED by 104b5d42 but external signature unchanged; the BAF_TRAIT_SENTINEL tagging is internal to the function body)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §4 Consumer Index JKP-01 row (line 555 — authoritative scope anchor per D-04: "§1.2 (runBafJackpot MODIFIED by 104b5d42) + event declarations (JackpotEthWin, JackpotTicketWin widened to uint16) + constant BAF_TRAIT_SENTINEL = 420, §2.3 (IM-14, IM-15), commit 104b5d42 rows")
    - .planning/phases/230-delta-extraction-scope-map/230-02-DELTA-ADDENDUM.md — the `c2e5e0a9` commit modifies 16 sites in DegenerusGameJackpotModule.sol per the addendum's file table; lines 277, 443, 508, 522, 544, 594, 596, 607-609, 874, 937, 1134, 1238, 1345, 1681-1683, 1741, 1798-1800, 1808. None of the four `runBafJackpot` emit sites (lines 2002 / 2014 / 2034 / 2038) are in that list, and the new `BAF_TRAIT_SENTINEL` constant at line 136 is also not touched. Record this in the audit as "104b5d42 surface not additionally touched by 230-02-DELTA-ADDENDUM.md commits" so JKP-01 doesn't need to re-verify addendum rows for the BAF emit paths.
    - Output of `git show 104b5d42 --stat` and `git show 104b5d42 -- contracts/modules/DegenerusGameJackpotModule.sol` — the delta IS the audit surface. Pre-fix source (`104b5d42^`) shows the four emit sites with literal `0` as the `traitId` argument + the narrower `uint8 traitId` in event decls; HEAD shows `BAF_TRAIT_SENTINEL` + `uint16 traitId`. The diff should be a compact, purely semantic change (no CEI or payout-math modification).
    - contracts/modules/DegenerusGameJackpotModule.sol at HEAD — enumerate the following structural elements and record exact File:Line for each:
        * File-scope event decl `JackpotEthWin(address indexed winner, uint24 indexed level, uint16 indexed traitId, uint256 amount, uint256 ticketIndex, uint24 rebuyLevel, uint32 rebuyTickets)` — currently at line 69 (grep-verify). Third indexed field `traitId` is `uint16` post-fix; pre-fix it was `uint8`.
        * File-scope event decl `JackpotTicketWin(address indexed winner, uint24 indexed ticketLevel, uint16 indexed traitId, uint32 ticketCount, uint24 sourceLevel, uint256 ticketIndex)` — currently at line 80 (grep-verify). Same `uint8 -> uint16` widening.
        * File-scope constant `uint16 private constant BAF_TRAIT_SENTINEL = 420;` — currently at line 136 (grep-verify). NEW by 104b5d42.
        * `runBafJackpot(uint256 poolWei, uint24 lvl, uint256 rngWord) external returns (uint256 claimableDelta)` — currently at line 1966 (grep-verify). The four emit sites inside the body:
          - Line 2002: `emit JackpotEthWin(winner, lvl, BAF_TRAIT_SENTINEL, ethPortion, 0, rl, rt);` — large-winner ETH half (>=5% of pool)
          - Line 2014: `emit JackpotTicketWin(winner, lvl, BAF_TRAIT_SENTINEL, 0, lvl, 0);` — large-winner lootbox half (<= LOOTBOX_CLAIM_THRESHOLD; immediate award)
          - Line 2034: `emit JackpotEthWin(winner, lvl, BAF_TRAIT_SENTINEL, amount, 0, rl, rt);` — small-winner even-index 100% ETH
          - Line 2038: `emit JackpotTicketWin(winner, lvl, BAF_TRAIT_SENTINEL, 0, lvl, 0);` — small-winner odd-index 100% lootbox
        * Verify there is NO third emit of any event carrying a `traitId` from `runBafJackpot` (grep for `emit.*Jackpot.*Win` inside the function body — should yield exactly 4 matches). Also verify `emit JackpotWhalePassWin(...)` at line 2018 does NOT carry a `traitId` field (whale-pass deferral path — non-trait emit; confirm by reading the event decl).
    - contracts/modules/DegenerusGameJackpotModule.sol at HEAD — domain-collision sweep (D-04 core task): grep ALL reads of event `traitId` field + storage `winningTraitsPacked` / `bonusTraitsPacked` inside the module. Expected hits based on preliminary grep (verify against HEAD):
        * `_rollWinningTraits(randWord, false/true) -> uint32 packed` — defined at line 1865. Returns a uint32 packed format (4 × 8 bits). The sentinel 420 (> 255) CANNOT appear in any packed slot because packing is `uint8[4] traits` via `JackpotBucketLib.packWinningTraits`. Record this structural impossibility.
        * `JackpotBucketLib.unpackWinningTraits(uint32 packed) -> uint8[4]` — called at lines 278, 445, 676, 893, 1082, 1139, 1722 (verify via grep). Every caller of unpack narrows to `uint8[4]` which cannot represent 420. Record each call site.
        * Direct reads of storage `winningTraitsPacked` (the `uint32 winningTraitsPacked` local/struct field) at lines 253, 276, 334, 343, 445, 518, 545, 560, 857, 872, 885, 893, 1082 (from preliminary grep, verify against HEAD). Each assignment is from `_rollWinningTraits` (uint32 packed format) — no path injects a literal 420 into this field.
        * Direct reads of storage `bonusTraitsPacked` (the `uint32 bonusTraitsPacked` local/parameter) at lines 102, 507, 521, 593, 614, 643, 1679, 1690, 1707 (from preliminary grep, verify against HEAD). Each assignment is from `_rollWinningTraits(*, true)` (uint32 packed format) — same structural impossibility.
        * `emit JackpotEthWin(..., traitId, ...)` and `emit JackpotTicketWin(..., traitId, ...)` — grep all emits across the module and categorize each by whether the `traitId` argument is (a) the BAF sentinel `BAF_TRAIT_SENTINEL`, (b) a `uint8` narrowing from `JackpotBucketLib.unpackWinningTraits(...)`, or (c) another path. Record each emit site with its traitId argument expression.
    - Module-wide domain-collision sweep (D-04 core task, beyond JackpotModule): grep `traitId` across ALL of contracts/ to find on-chain readers (expected: most are off-chain indexer territory — event fields never appear as input to on-chain logic; confirm by absence of any inbound-event-reading code path in the contracts). Files to grep: contracts/modules/DegenerusGameJackpotModule.sol (already covered), contracts/modules/DegenerusGameLootboxModule.sol, contracts/modules/DegenerusGameDecimatorModule.sol, contracts/modules/DegenerusGameMintModule.sol, contracts/DegenerusGame.sol, contracts/DegenerusJackpots.sol, contracts/DegenerusVault.sol, contracts/BurnieCoinflip.sol, contracts/libraries/JackpotBucketLib.sol, contracts/libraries/TraitLookupLib.sol (or equivalent). For each `traitId` reference found outside the JackpotModule, classify: (a) uint8 parameter / return (real trait), (b) event field in an emit site (off-chain only), (c) event field in a declaration without a consumer in contracts/, (d) other. Record results in the domain-collision sweep table of the AUDIT.
    - Module-wide sweep for reads of `winningTraitsPacked` / `bonusTraitsPacked` across ALL of contracts/: grep both identifiers across contracts/; confirm all consumers are inside `DegenerusGameJackpotModule.sol` (no cross-module read). If any external read is found, record as a finding-candidate row.
    - contracts/DegenerusJackpots.sol — read the `runBafJackpot(poolWei, lvl, rngWord)` implementation called at DegenerusGameJackpotModule.sol:1973 (`jackpots.runBafJackpot(poolWei, lvl, rngWord)`). Verify the return tuple `(address[] memory winnersArr, uint256[] memory amountsArr, ...)` — specifically the third ignored return field — does NOT contain trait IDs (the winners/amounts selection upstream uses its own entropy path; the BAF sentinel is appended downstream in the module). Record the call-site and the return-tuple shape.
    - .planning/phases/231-earlybird-jackpot-audit/231-02-PLAN.md — precedent plan-of-record structural template (the 20a951df trait-alignment audit; cite as methodology precedent for domain-collision sweep output shape) + acceptance_criteria format to mirror. Do NOT carry forward 231-02 verdicts — audit every function body fresh from HEAD per D-03.
    - .planning/phases/232-decimator-audit/232-01-PLAN.md and 232-02-PLAN.md — precedent audit-phase PLAN.md shape (frontmatter must_haves block, objective, execution_context, tasks with read_first / action / verify / acceptance_criteria / done structure). Mirror this structure for 233-01.
  </read_first>
  <action>
Execute a fresh-from-source adversarial audit of the `104b5d42` BAF `traitId=420` sentinel per CONTEXT.md D-01 through D-12. Verdict vocabulary per D-02 is strictly `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`. Column schema per D-02 (matching 232-NN-AUDIT.md precedent) is strictly `Function | Commit SHA | File:Line | Verdict | Evidence | Notes` with an additional `Finding Candidate: Y/N` column per the 232-precedent extension.

**Step 1 — Build the scope list from `230-01-DELTA-MAP.md` §4 Consumer Index JKP-01 row (D-04).** The target element set is:
  - **`DegenerusGameJackpotModule.runBafJackpot`** body (MODIFIED by `104b5d42`) — specifically the four `emit` sites inside the function body (lines 2002 / 2014 / 2034 / 2038 at HEAD — verify via grep):
    - Large-winner ETH half: `emit JackpotEthWin(winner, lvl, BAF_TRAIT_SENTINEL, ethPortion, 0, rl, rt);` at line 2002
    - Large-winner lootbox half (immediate path): `emit JackpotTicketWin(winner, lvl, BAF_TRAIT_SENTINEL, 0, lvl, 0);` at line 2014
    - Small-winner even-index 100% ETH: `emit JackpotEthWin(winner, lvl, BAF_TRAIT_SENTINEL, amount, 0, rl, rt);` at line 2034
    - Small-winner odd-index 100% lootbox: `emit JackpotTicketWin(winner, lvl, BAF_TRAIT_SENTINEL, 0, lvl, 0);` at line 2038
  - **File-scope event decl `JackpotEthWin(... uint16 indexed traitId ...)`** at line 69 — MODIFIED by `104b5d42` (widened `uint8 -> uint16`)
  - **File-scope event decl `JackpotTicketWin(... uint16 indexed traitId ...)`** at line 80 — MODIFIED by `104b5d42` (widened `uint8 -> uint16`)
  - **File-scope constant `uint16 private constant BAF_TRAIT_SENTINEL = 420;`** at line 136 — NEW by `104b5d42`
  - **IM-14** (self-call `_consolidatePoolsAndRewardJackpots -> DegenerusGame.runBafJackpot`) — call site pre-existing, caller chain unchanged; included as consumer-of-modified-callee
  - **IM-15** (delegatecall `DegenerusGame.runBafJackpot -> DegenerusGameJackpotModule.runBafJackpot`) — selector unchanged; callee body MODIFIED
  - **Domain-collision sweep target set** (per D-04): every on-chain reader of event `traitId` field + every on-chain read of storage `winningTraitsPacked` / `bonusTraitsPacked` in contracts/

**Step 2 — For each target element, run the attack vectors from CONTEXT.md D-04:**
  - **(a) Sentinel constant properties (D-04):** Verify the new file-scope constant `uint16 private constant BAF_TRAIT_SENTINEL = 420;` at line 136 satisfies: (i) type is `uint16` which is strictly wider than `uint8` (the real-trait domain), (ii) value `420` is strictly greater than `type(uint8).max = 255` and therefore outside the real-trait domain by construction, (iii) visibility is `private` — no external ABI exposure; readers outside the module cannot reference the symbol (they would have to hard-code `420`). Record SAFE with File:Line of the constant declaration.
  - **(b) Four emit sites correctly pass the sentinel (D-04):** For each of the four emit sites at lines 2002 / 2014 / 2034 / 2038, verify: (i) the `traitId` argument position is literally the symbol `BAF_TRAIT_SENTINEL` (not a numeric literal `420`, not a variable that could be mutated), (ii) no surrounding control flow could cause the emit site to fire with a different `traitId` value (the sites are straight-line after the branch guard; no conditional trait-id assignment), (iii) the emit arguments match the event decl order / types — `winner (address)`, `lvl (uint24)`, `BAF_TRAIT_SENTINEL (uint16)`, and the remaining fields. Record one verdict row per emit site with File:Line.
  - **(c) Payout math + CEI unchanged (D-04 + auto-rule):** Use `git show 104b5d42 -- contracts/modules/DegenerusGameJackpotModule.sol` to confirm the diff at each emit site is purely a `0 -> BAF_TRAIT_SENTINEL` substitution in the third argument position; no surrounding state mutation was added or removed; no ordering change of the emit relative to prior state mutations (`_addClaimableEth`, `_awardJackpotTickets`, `_queueWhalePassClaimCore`, `claimableDelta += cd`). Record as SAFE with evidence excerpt.
  - **(d) Domain-collision sweep — event `traitId` readers (D-04):** Enumerate EVERY on-chain reader of `traitId` across all files in `contracts/` (grep pattern: `traitId` excluding comments/NatSpec). For each hit classify as: (i) uint8 parameter or return (real trait — cannot carry 420 because type narrower than uint16; if 420 were assigned, compiler would reject), (ii) event field inside an `emit` call (output only — never read back on-chain), (iii) event field inside a declaration (no consumer), (iv) any other path. If any on-chain branch does `if (traitId == 420)` or `traitId + 1` / `traitId * X` arithmetic or `arr[traitId]` indexing in a way that would misbehave for 420, record as VULNERABLE with File:Line. Expected outcome: all hits fall in categories (i) through (iii); no on-chain branch treats `traitId` as numerically meaningful in a way that could collide with 420. Record each hit row in a dedicated domain-collision sweep table.
  - **(e) Domain-collision sweep — storage `winningTraitsPacked` / `bonusTraitsPacked` readers (D-04):** Enumerate EVERY read of these two identifiers across `contracts/`. For each hit, verify: (i) the reader's narrowing path goes through `JackpotBucketLib.unpackWinningTraits` which returns `uint8[4]` — 420 is structurally impossible because packing was `uint8[4] traits` in the originating `_rollWinningTraits(*, false/true)` at line 1865-1875 via `JackpotBucketLib.packWinningTraits(traits)`, or (ii) the reader uses the packed `uint32` opaquely (e.g. passed through an event field) without extracting a trait ID that could be 420, or (iii) the reader does not exist. If any reader extracts a value and branches on `value == 420` or treats >=256 as a valid trait, record as VULNERABLE. Record each hit row in the domain-collision sweep table.
  - **(f) Event-signature widening off-chain side-effect (D-04 + event-surface note in 233-CONTEXT.md code_context):** The `uint8 traitId -> uint16 traitId` widening on `JackpotEthWin` and `JackpotTicketWin` changes the Solidity event-signature HASH (the keccak over the human-readable event signature string, which is what Solidity ABI encoders use for ABI decoding; note that EVM topic encoding itself is 32 bytes regardless of declared width, so on-chain behavior of `indexed` topic slot is unchanged). Consequence: off-chain ABI consumers (indexers, UI, The Graph subgraphs) must regenerate their ABI. This is NOT an on-chain finding — contract behavior is identical — but it IS a finding-candidate for the indexer team (v28.0 Phase 227 indexer parallel). Record as a single SAFE-INFO verdict row with Finding Candidate: Y, Notes explicitly stating "Off-chain ABI regeneration required; routes to Phase 236 FIND-01 / FIND-02 as indexer-compat OBSERVATION, NOT an on-chain contract finding". Cross-reference: 232-02-AUDIT.md precedent for indexer-compat OBSERVATION rows (DCM-02 Finding Candidate: Y row for DecimatorClaimed / TerminalDecimatorClaimed event-registration gap).
  - **(g) IM-14 / IM-15 chain correctness (D-04 + §2.3 anchors):** For IM-14 (self-call caller side at AdvanceModule:417-423 `_consolidatePoolsAndRewardJackpots -> IDegenerusGame(address(this)).runBafJackpot(...)`), verify the call arguments `(poolWei, lvl, rngWord)` match the expected ABI and the caller did not change its argument derivation. For IM-15 (delegatecall from Game wrapper to module), verify via `make check-delegatecall` output (cite the 44/44 PASS line from Phase 230 §3.5 as corroboration per D-10) that the delegatecall target alignment is preserved. The selector itself is unchanged (`IDegenerusGameJackpotModule.runBafJackpot.selector`) because the function SIGNATURE at line 1966 is unchanged; only the body was modified. Record as SAFE with File:Line for each chain row + cite §2.3 IM-14 / IM-15 rows as evidence.
  - **(h) 230-02-DELTA-ADDENDUM.md cross-check (D-12):** Confirm that none of the BAF-sentinel surface (lines 69, 80, 136, 2002, 2014, 2034, 2038) overlaps with the 16 `c2e5e0a9` sites listed in the addendum. Per addendum page 1 the modified JackpotModule.sol lines are 277, 443, 508, 522, 544, 594, 596, 607-609, 874, 937, 1134, 1238, 1345, 1681-1683, 1741, 1798-1800, 1808 — NO overlap with BAF emit sites. Record as a single SAFE verdict row noting "230-02-DELTA-ADDENDUM.md c2e5e0a9 touches 16 JackpotModule sites; none overlap the 104b5d42 BAF emit surface (lines 69, 80, 136, 2002, 2014, 2034, 2038). No addendum-driven re-verification required for JKP-01; JKP-02 owns the post-c2e5e0a9 entropy-formulation equivalence audit per D-12."

**Step 3 — Write `233-01-AUDIT.md` mirroring the 232-NN-AUDIT.md precedent shape.** Required structure, in this exact order:

```markdown
# JKP-01 Adversarial Audit: BAF traitId=420 Sentinel

**Scope:** Commit `104b5d42` ("feat(jackpot): tag BAF wins with traitId=420 sentinel") — `runBafJackpot` body (four emit sites) + event decls `JackpotEthWin` / `JackpotTicketWin` (uint8 -> uint16 widening) + new file-scope constant `BAF_TRAIT_SENTINEL = 420` (all in `230-01-DELTA-MAP.md` §1.2)
**Method:** Fresh read from HEAD source — no reuse of v25.0 Phase 214 or v26.0 Phase 218/219 or v28.0 Phase 227 verdicts as pre-approved; every on-chain reader of event `traitId` and every storage read of `winningTraitsPacked` / `bonusTraitsPacked` enumerated by grep against `contracts/` at HEAD
**Scope source:** `230-01-DELTA-MAP.md` §4 Consumer Index row JKP-01 (per D-04)
**Verdict vocabulary:** `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (per D-02)
**Finding-ID policy:** No `F-29-NN` IDs emitted (per D-11) — `Finding Candidate: Y/N` column only; Phase 236 FIND-01 owns canonical ID assignment
**230-02-DELTA-ADDENDUM.md integration (per D-12):** Confirmed zero overlap between the 104b5d42 BAF surface and the 16 c2e5e0a9 JackpotModule sites. JKP-01 surface unaffected by post-Phase-230 keccak hardening.

## Methodology

[One paragraph: attack vectors per CONTEXT.md D-04 (domain-collision sweep enumerating every on-chain reader of event `traitId` field + every read of `winningTraitsPacked` / `bonusTraitsPacked` storage; narrowing-to-uint8 / opaque-tag / non-existence classification; BAF sentinel correctly passed at all four emit sites; event-widening off-chain ABI regeneration characterised as indexer-compat OBSERVATION not on-chain finding). Locked verdict vocabulary. Locked columns per D-02 plus Finding Candidate column per 232-precedent.]

## Findings-Candidate Block

[Prose free-form (no F-29-NN IDs). One block per VULNERABLE or DEFERRED verdict identified in the table below, plus any SAFE-INFO row marked Finding Candidate: Y. Each block: title, verdict, File:Line, owning SHA `104b5d42`, attack vector, evidence excerpt, recommended Phase 236 severity-classification note. Expected: one SAFE-INFO Finding Candidate: Y for the event-widening off-chain ABI regeneration note. If zero VULNERABLE / zero DEFERRED and no on-chain finding candidates, state "No on-chain candidate findings — all on-chain verdicts SAFE against 104b5d42 surface. One SAFE-INFO OBSERVATION for off-chain ABI regeneration routes to Phase 236 FIND-01 / FIND-02."]

## Per-Function Verdict Table

| Function | Commit SHA | File:Line | Verdict | Evidence | Notes | Finding Candidate |
|---|---|---|---|---|---|---|
| `DegenerusGameJackpotModule.runBafJackpot` emit site 1 (large-winner ETH) | 104b5d42 | contracts/modules/DegenerusGameJackpotModule.sol:2002 | SAFE | `emit JackpotEthWin(winner, lvl, BAF_TRAIT_SENTINEL, ethPortion, 0, rl, rt);` — 3rd arg is the symbol, not literal 420; straight-line after large-winner branch guard at line 1990 | No payout-math or CEI change vs pre-fix | N |
| `DegenerusGameJackpotModule.runBafJackpot` emit site 2 (large-winner lootbox immediate) | 104b5d42 | contracts/modules/DegenerusGameJackpotModule.sol:2014 | SAFE | `emit JackpotTicketWin(winner, lvl, BAF_TRAIT_SENTINEL, 0, lvl, 0);` — 3rd arg is the symbol; fires after `_awardJackpotTickets` returns new rngWord | No payout-math or CEI change vs pre-fix | N |
| `DegenerusGameJackpotModule.runBafJackpot` emit site 3 (small-winner even-idx 100% ETH) | 104b5d42 | contracts/modules/DegenerusGameJackpotModule.sol:2034 | SAFE | `emit JackpotEthWin(winner, lvl, BAF_TRAIT_SENTINEL, amount, 0, rl, rt);` — 3rd arg is the symbol; fires after `_addClaimableEth` | No payout-math or CEI change vs pre-fix | N |
| `DegenerusGameJackpotModule.runBafJackpot` emit site 4 (small-winner odd-idx 100% lootbox) | 104b5d42 | contracts/modules/DegenerusGameJackpotModule.sol:2038 | SAFE | `emit JackpotTicketWin(winner, lvl, BAF_TRAIT_SENTINEL, 0, lvl, 0);` — 3rd arg is the symbol; fires after `_awardJackpotTickets` returns new rngWord | No payout-math or CEI change vs pre-fix | N |
| `DegenerusGameJackpotModule.BAF_TRAIT_SENTINEL` constant decl | 104b5d42 | contracts/modules/DegenerusGameJackpotModule.sol:136 | SAFE | `uint16 private constant BAF_TRAIT_SENTINEL = 420;` — uint16 wider than uint8 real-trait domain; 420 > 255 = out-of-domain by construction; private visibility | Type-system guarantees 420 cannot collide with any uint8-narrowed real trait | N |
| `DegenerusGameJackpotModule.JackpotEthWin` event decl widening | 104b5d42 | contracts/modules/DegenerusGameJackpotModule.sol:69 | SAFE-INFO | `uint16 indexed traitId` — widened `uint8 -> uint16`; topic encoding 32 bytes regardless; event signature hash changes | Off-chain ABI regeneration required; routes to Phase 236 FIND-01 / FIND-02 as indexer-compat OBSERVATION; NOT an on-chain finding | Y |
| `DegenerusGameJackpotModule.JackpotTicketWin` event decl widening | 104b5d42 | contracts/modules/DegenerusGameJackpotModule.sol:80 | SAFE-INFO | Same widening as JackpotEthWin; same consequences | Same indexer-compat OBSERVATION consolidated or listed independently at audit's discretion | Y |
| IM-14 self-call chain (_consolidatePoolsAndRewardJackpots -> DegenerusGame.runBafJackpot) | 104b5d42 | contracts/modules/DegenerusGameAdvanceModule.sol:<line range for self-call site inside _consolidatePoolsAndRewardJackpots>; callee at DegenerusGame.sol:<line for runBafJackpot wrapper> | SAFE | Call args `(poolWei, lvl, rngWord)` unchanged pre-fix vs HEAD; self-call pattern via `IDegenerusGame(address(this))` unchanged | 230-01-DELTA-MAP.md §2.3 IM-14 | N |
| IM-15 delegatecall chain (DegenerusGame.runBafJackpot -> module) | 104b5d42 | contracts/DegenerusGame.sol:<line for wrapper delegatecall>; callee at contracts/modules/DegenerusGameJackpotModule.sol:1966 | SAFE | Selector unchanged (signature byte-identical); delegatecall target alignment corroborated by `make check-delegatecall` 44/44 PASS at HEAD per Phase 230 §3.5 | 230-01-DELTA-MAP.md §2.3 IM-15 + §3.3.c ID-84 | N |
| 230-02-DELTA-ADDENDUM.md overlap check (D-12) | 104b5d42 | (cross-file — addendum `c2e5e0a9` touches JackpotModule lines 277, 443, 508, 522, 544, 594, 596, 607-609, 874, 937, 1134, 1238, 1345, 1681-1683, 1741, 1798-1800, 1808 per 230-02 per-file table) | SAFE | Zero overlap between 104b5d42 BAF surface (lines 69, 80, 136, 2002, 2014, 2034, 2038) and c2e5e0a9 keccak-hardening sites | JKP-01 surface unaffected by post-Phase-230 entropy hardening; JKP-02 owns the post-c2e5e0a9 entropy-formulation equivalence per D-12 | N |

[Rows above are the MINIMUM. Replace `<line>` placeholders with real File:Line values extracted during Step 1. Additional rows allowed per (element × distinct attack vector).]

## Domain-Collision Sweep Table

Enumerates every on-chain reader of event `traitId` field + every on-chain read of storage `winningTraitsPacked` / `bonusTraitsPacked` across all `contracts/` files at HEAD.

| Reader | File:Line | Kind | Narrowing / Disposition | Collision Risk | Verdict |
|---|---|---|---|---|---|
| `_rollWinningTraits(*, false)` consumer via `JackpotBucketLib.unpackWinningTraits` | contracts/modules/DegenerusGameJackpotModule.sol:278 (or current location) | `unpack(uint32) -> uint8[4]` narrowing | Narrows to uint8 — 420 structurally impossible | None | SAFE |
| `_rollWinningTraits(*, true)` consumer via `JackpotBucketLib.unpackWinningTraits` | contracts/modules/DegenerusGameJackpotModule.sol:676 (earlybird path) | Same uint8 narrowing | None | None | SAFE |
| [Additional rows for every unpack call site — lines 445, 893, 1082, 1139, 1722 or equivalent per HEAD grep; each classified as uint8-narrowing SAFE] | ... | ... | ... | ... | ... |
| Direct reads of `winningTraitsPacked` as uint32 opaque in struct fields / locals | contracts/modules/DegenerusGameJackpotModule.sol:<each hit line> | uint32 opaque passthrough | Not extracted as individual trait at this site | None | SAFE |
| Direct reads of `bonusTraitsPacked` as uint32 opaque in struct fields / locals | contracts/modules/DegenerusGameJackpotModule.sol:<each hit line> | uint32 opaque passthrough | Not extracted as individual trait at this site | None | SAFE |
| `emit Jackpot*.Win(..., traitId, ...)` sites across module | contracts/modules/DegenerusGameJackpotModule.sol:<each emit line> | `emit` output only — no on-chain reader | 420 never read back on-chain | None | SAFE |
| Module-wide `traitId` references outside JackpotModule | contracts/<file>:<line> for each cross-file hit | Classify: (a) uint8 param / return — real trait, (b) emit field — off-chain only, (c) event decl w/o consumer, (d) other | Each must fall in (a)-(c) to remain SAFE; any arithmetic or indexing by `traitId` that could misbehave for 420 is VULNERABLE | None expected | SAFE |

[Replace `<line>` placeholders with real File:Line values from HEAD grep. If the cross-file sweep finds zero consumers outside JackpotModule, record a single row stating "Module-wide sweep of `traitId` references outside JackpotModule — zero on-chain reader branches; all hits are uint8 parameters, emit fields, or event decls". If the sweep finds a consumer branch that could misbehave for 420, that row becomes the VULNERABLE / Finding Candidate: Y anchor routed to Phase 236 FIND-01.]

## High-Risk Patterns Analyzed

### Sentinel Type-Domain Separation
[Prose analysis: `uint16 BAF_TRAIT_SENTINEL = 420` vs `uint8` real-trait space. `type(uint8).max = 255 < 420 <= type(uint16).max = 65535` — 420 cannot be representable as a valid uint8 trait ID; any attempt to narrow via implicit or explicit cast would truncate or revert. The event field was widened to `uint16` to carry 420 as a valid indexed topic; on-chain readers that still treat the `traitId` value as a uint8 array index would reject 420 either at compile time (explicit cast) or at runtime (bounds check on trait-id-to-index mapping). Cite the constant decl line + two event decl lines verbatim.]

### Emit Argument Correctness at All Four BAF Sites
[Prose analysis: each of the four emit sites at lines 2002 / 2014 / 2034 / 2038 passes `BAF_TRAIT_SENTINEL` (the symbol, not a numeric literal 420) as the third argument. The symbol resolves to `uint16 420` at compile time; no intermediate variable mutation occurs between the constant definition and the emit site. Cite each emit line verbatim and note the straight-line control flow after each branch guard.]

### Domain-Collision Sweep — winningTraitsPacked / bonusTraitsPacked
[Prose analysis: the two storage / local identifiers are ALWAYS populated from `_rollWinningTraits(randWord, bool)` which returns a `uint32 packed` format = 4 × 8 bits of individual uint8 trait IDs via `JackpotBucketLib.packWinningTraits(uint8[4])`. No code path injects a uint16 or wider value into these identifiers. Downstream unpacking via `JackpotBucketLib.unpackWinningTraits(uint32) -> uint8[4]` narrows back to uint8 at every consumer. Conclusion: the BAF sentinel 420 CANNOT appear in either identifier by type construction. Cite each grep hit File:Line.]

### Domain-Collision Sweep — On-Chain Readers of Event traitId Field
[Prose analysis: `emit` statements produce log topics / data that are never read back by EVM execution within the same transaction or any subsequent transaction — events are off-chain only. Therefore the 420 sentinel in `JackpotEthWin.traitId` / `JackpotTicketWin.traitId` is observationally invisible to every on-chain branch. Confirm by grepping all contracts for patterns that could inadvertently read an event field (should yield zero matches).]

### Event-Signature Hash Change (Off-Chain Only)
[Prose analysis: Solidity ABI event-signature hash is computed over the human-readable signature string (name + canonical param types). Widening `uint8 traitId -> uint16 traitId` changes the canonical type string from `uint8` to `uint16`, which changes the signature hash (the value of `topic0` for non-anonymous events). Off-chain ABI consumers (indexers, UI, subgraphs) must regenerate their ABI to decode post-fix events. EVM topic encoding itself is 32-byte left-padded regardless of declared width, so on-chain behavior is unchanged. Cite v28.0 Phase 227 precedent for indexer-compat OBSERVATION (DCM-02 DecimatorClaimed / TerminalDecimatorClaimed).]

## Scope-guard Deferrals

[Any JKP-01-adjacent concern surfaced during this audit that is NOT anchored in `230-01-DELTA-MAP.md` §4 JKP-01 rows is recorded here per D-03. Phase 230 catalog is READ-only — record deferrals in this plan's SUMMARY rather than editing the catalog. Each entry: concern, why not in JKP-01 scope, recommended receiving phase. If none, state "None surfaced during this audit."]

## Downstream Hand-offs

[Concerns overlapping downstream-phase territory (per D-11 and general scope boundaries). Expected entries:
- **Phase 233 JKP-03 hand-off (cross-path bonus-trait consistency):** The BAF sentinel 420 is NEVER derived from `bonusTraitsPacked` or `winningTraitsPacked` — it is a literal file-scope constant. Therefore JKP-03's cross-path identity proof (three paths producing identical 4-trait sets for same VRF word) is ORTHOGONAL to JKP-01's sentinel-injection. No cross-plan dependency from JKP-01 to JKP-03 beyond citation.
- **Phase 233 JKP-02 hand-off (entropy passthrough):** The `rngWord` argument to `runBafJackpot` is entropy-consumed downstream via `_addClaimableEth`, `_awardJackpotTickets`, `_queueWhalePassClaimCore`. JKP-02 owns the entropy-passthrough equivalence proof for `processFutureTicketBatch`; the `rngWord` lineage into `runBafJackpot` is NOT newly-threaded by 104b5d42 (pre-existing caller chain) so JKP-02 does not need to audit this boundary beyond the existing IM-14 row.
- **Phase 235 RNG-01 hand-off (backward trace):** `runBafJackpot` is a pre-existing RNG consumer; 104b5d42 did not change the entropy lineage (only the emit-field sentinel). Phase 235 RNG-01 backward-trace applies to NEW RNG consumers in the v29.0 delta; the BAF emit-sentinel change does not introduce a new consumer. Record as N/A — 104b5d42 modifies output labeling only, not entropy derivation.
- **Phase 235 CONS-01 hand-off (pool conservation):** No SSTORE delta introduced by 104b5d42 — emit statements are pure event output, no storage mutation. Phase 235 CONS-01 does not need to re-cover the BAF emit surface.
- **Phase 236 FIND-01 hand-off:** Every VULNERABLE / DEFERRED row + every SAFE-INFO row with `Finding Candidate: Y` becomes a finding-candidate anchor for Phase 236 severity classification. Expected: 2 SAFE-INFO Finding Candidate: Y rows (JackpotEthWin + JackpotTicketWin event-widening off-chain ABI regeneration); 0 VULNERABLE; 0 DEFERRED.
- **Phase 236 FIND-02 hand-off (KNOWN-ISSUES routing):** Event-widening off-chain ABI regeneration is a design decision, not a contract-side vulnerability — Phase 236 FIND-02 may route to KNOWN-ISSUES.md rather than audit/FINDINGS-v29.0.md as an informational design decision. Plan 233-01 records the candidate; Phase 236 decides routing.
- **Future `database/` milestone indexer-compat:** Post-fix event signatures require indexer ABI regeneration. This is v28.0 Phase 227 / future milestone scope — NOT a contract-side finding. Flag in hand-offs; do NOT add a Phase 236 FIND row for it.
Any additional hand-offs surfaced during audit recorded here.]
```

Every verdict row MUST:
- Use exactly the locked column set per D-02 + Finding Candidate column per 232-NN-AUDIT.md precedent: `Function | Commit SHA | File:Line | Verdict | Evidence | Notes | Finding Candidate`
- Cite the 7-char SHA `104b5d42`
- Use a real File:Line anchor from HEAD source (never a placeholder like `<line>`)
- Use verdict vocabulary strictly from `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (per D-02)
- Never reference F-29-NN IDs (per D-11)
- Carry a `Y` or `N` in Finding Candidate column

Read contract sources ONLY from `contracts/` directory at HEAD (per user memory `feedback_contract_locations.md` — never read stale copies). Use `git show 104b5d42^:contracts/...` for pre-fix comparisons (git-tree read of the same repo at a prior SHA — NOT a stale copy).

READ-only constraint (v29.0 milestone rule): Zero writes to `contracts/` or `test/` permitted. Only writes allowed during task execution are `.planning/phases/233-jackpot-baf-entropy-audit/233-01-AUDIT.md` itself. `git status --porcelain contracts/ test/` MUST be empty before and after task execution.
  </action>
  <verify>
    <automated>test -f .planning/phases/233-jackpot-baf-entropy-audit/233-01-AUDIT.md && grep -c "104b5d42" .planning/phases/233-jackpot-baf-entropy-audit/233-01-AUDIT.md && test -z "$(git status --porcelain contracts/ test/)"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/233-jackpot-baf-entropy-audit/233-01-AUDIT.md` exists
    - File contains the literal header string `## Per-Function Verdict Table`
    - File contains the literal header string `## Domain-Collision Sweep Table`
    - File contains the literal header string `## Findings-Candidate Block`
    - File contains the literal header string `## Scope-guard Deferrals`
    - File contains the literal header string `## Downstream Hand-offs`
    - Verdict table header row is exactly `| Function | Commit SHA | File:Line | Verdict | Evidence | Notes | Finding Candidate |`
    - Every row in the Per-Function Verdict Table cites the 7-char SHA `104b5d42`
    - Every row cites a File:Line anchor starting with `contracts/modules/DegenerusGameJackpotModule.sol:` or `contracts/modules/DegenerusGameAdvanceModule.sol:` or `contracts/DegenerusGame.sol:`
    - Every row's verdict is exactly one of the strings `SAFE`, `SAFE-INFO`, `VULNERABLE`, or `DEFERRED` (per D-02)
    - Every row's Finding Candidate cell is exactly `Y` or `N`
    - At least one row per structural target: four emit sites (line 2002, 2014, 2034, 2038), BAF_TRAIT_SENTINEL constant (line 136), JackpotEthWin event decl (line 69), JackpotTicketWin event decl (line 80), IM-14 self-call, IM-15 delegatecall, 230-02-DELTA-ADDENDUM overlap check (10 distinct verdict rows minimum)
    - `grep -cE "SAFE|SAFE-INFO|VULNERABLE|DEFERRED" .planning/phases/233-jackpot-baf-entropy-audit/233-01-AUDIT.md` returns at least 10
    - `grep -c "104b5d42" .planning/phases/233-jackpot-baf-entropy-audit/233-01-AUDIT.md` returns at least 10 (one per verdict row minimum)
    - The string `F-29-NN` does NOT appear in the file (per D-11)
    - The string `F-29-` does NOT appear in any form in the file (Phase 236 owns ID assignment per D-11)
    - At least two rows carry `SAFE-INFO` verdict with Finding Candidate: Y for the two event-widening decls (JackpotEthWin + JackpotTicketWin off-chain ABI regeneration)
    - Domain-Collision Sweep Table exists with at least 6 rows (four unpackWinningTraits call sites minimum + module-wide traitId reader classification + emit-field reader classification)
    - Every Domain-Collision Sweep Table row classifies the reader as either `uint8-narrowing` / `uint32-opaque-passthrough` / `emit-output-only` / `non-existent` and assigns a verdict of SAFE (any VULNERABLE classification is acceptable if genuine but would route to Phase 236 FIND-01 Finding Candidate: Y)
    - High-Risk Patterns Analyzed section contains at least five subsections: `Sentinel Type-Domain Separation`, `Emit Argument Correctness at All Four BAF Sites`, `Domain-Collision Sweep — winningTraitsPacked / bonusTraitsPacked`, `Domain-Collision Sweep — On-Chain Readers of Event traitId Field`, `Event-Signature Hash Change (Off-Chain Only)`
    - Downstream Hand-offs subsection explicitly names `Phase 236 FIND-01` (per D-11 finding-ID assignment) and references the JKP-02 / JKP-03 / RNG-01 / CONS-01 non-overlap posture
    - 230-02-DELTA-ADDENDUM.md overlap check row explicitly lists "zero overlap" between 104b5d42 BAF surface and the 16 c2e5e0a9 JackpotModule sites (per D-12)
    - No placeholder line numbers like `:<line>` or `<line range>` remain in the final file — every File:Line is a real integer or integer range
    - No contract source content read from outside `contracts/` directory (per user memory `feedback_contract_locations.md`)
    - `git status --porcelain contracts/ test/` returns empty output both before and after task execution (READ-only v29.0 milestone constraint; zero writes to production code)
  </acceptance_criteria>
  <deliverable>
`233-01-AUDIT.md` with per-function verdict table covering the four `runBafJackpot` emit sites at DegenerusGameJackpotModule.sol:2002 / 2014 / 2034 / 2038, the new `BAF_TRAIT_SENTINEL = 420` constant at line 136, the two widened event decls at lines 69 and 80, IM-14 and IM-15 chain rows, and a 230-02-DELTA-ADDENDUM.md overlap row. Plus a dedicated Domain-Collision Sweep Table enumerating every on-chain reader of event `traitId` and every on-chain read of `winningTraitsPacked` / `bonusTraitsPacked` storage across contracts/, each classified as uint8-narrowing / uint32-opaque-passthrough / emit-output-only / non-existent. Findings-Candidate Block records two SAFE-INFO Finding Candidate: Y rows for the event-widening off-chain ABI regeneration. Scope-guard Deferrals / Downstream Hand-offs subsections present with explicit Phase 236 FIND-01 / FIND-02 routing. Zero F-29-NN emissions. Zero contracts/ or test/ writes.
  </deliverable>
  <done>`233-01-AUDIT.md` exists with the full structure above; every row cites `104b5d42` and a real File:Line in `contracts/`; verdicts use `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` vocabulary; D-04 domain-collision sweep covers every on-chain reader of event `traitId` + every read of `winningTraitsPacked` / `bonusTraitsPacked`; Findings-Candidate Block lists at least the two event-widening SAFE-INFO Finding Candidate: Y rows; Scope-guard Deferrals + Downstream Hand-offs subsections explicitly name Phase 236 FIND-01 / FIND-02; zero F-29-NN IDs emitted; zero contracts/ or test/ writes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-review checkpoint — present 233-01-AUDIT.md to orchestrator before commit</name>
  <what-built>`233-01-AUDIT.md` with the full per-function verdict table + domain-collision sweep + subsections described in Task 1. No contracts/ or test/ modifications.</what-built>
  <how-to-verify>
    1. `cat .planning/phases/233-jackpot-baf-entropy-audit/233-01-AUDIT.md` — confirm full artifact structure: Methodology, Findings-Candidate Block, Per-Function Verdict Table, Domain-Collision Sweep Table, High-Risk Patterns Analyzed, Scope-guard Deferrals, Downstream Hand-offs
    2. Verify every verdict row cites `104b5d42` (7-char) and a real File:Line in `contracts/`
    3. Verify verdicts use only `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`
    4. Verify at least two SAFE-INFO rows with Finding Candidate: Y for the event-widening (JackpotEthWin + JackpotTicketWin off-chain ABI regeneration)
    5. Verify domain-collision sweep table enumerates every on-chain reader of `traitId` + every read of `winningTraitsPacked` / `bonusTraitsPacked`
    6. Verify 230-02-DELTA-ADDENDUM.md overlap check row is present (per D-12)
    7. Verify zero F-29-NN or F-29- strings anywhere in the file (per D-11)
    8. `git status --porcelain contracts/ test/` — must be empty
    9. Verify `.planning/phases/233-jackpot-baf-entropy-audit/233-01-AUDIT.md` is the ONLY new file
  </how-to-verify>
  <resume-signal>Type "approved" (then Task 3 commits the AUDIT) or describe issues / required revisions.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit approved 233-01-AUDIT.md</name>
  <files>.planning/phases/233-jackpot-baf-entropy-audit/233-01-AUDIT.md</files>
  <read_first>
    - .planning/phases/233-jackpot-baf-entropy-audit/233-01-AUDIT.md (human-reviewed and approved in Task 2)
  </read_first>
  <action>
Stage and commit the approved `233-01-AUDIT.md` only. Commit message body explains it's a READ-only audit artifact per v29.0 milestone rule; no `contracts/` or `test/` changes in this commit.

```
git add .planning/phases/233-jackpot-baf-entropy-audit/233-01-AUDIT.md
git commit -m "docs(233-01): JKP-01 BAF traitId=420 sentinel adversarial audit

Per-function verdict table + domain-collision sweep for commit 104b5d42:
- Four emit sites inside runBafJackpot (lines 2002/2014/2034/2038) verified
  to pass BAF_TRAIT_SENTINEL = 420 symbol (not literal); no payout-math or CEI
  change vs pre-fix.
- Sentinel constant decl (line 136) SAFE: uint16 > uint8 real-trait space, 420
  > 255 out-of-domain by construction, private visibility.
- Domain-collision sweep across contracts/ confirms no on-chain branch treats
  traitId == 420 as a real trait; every reader either narrows to uint8 via
  JackpotBucketLib.unpackWinningTraits, treats uint32 packed as opaque, or is
  emit-output-only (never read back on-chain).
- Event decl widening uint8 -> uint16 on JackpotEthWin (line 69) + JackpotTicketWin
  (line 80) recorded as SAFE-INFO Finding Candidate: Y rows for off-chain ABI
  regeneration (indexer-compat OBSERVATION routing to Phase 236 FIND-01 / FIND-02,
  NOT an on-chain contract finding).
- 230-02-DELTA-ADDENDUM.md overlap check: zero overlap between 104b5d42 BAF
  surface and 16 c2e5e0a9 JackpotModule entropy-keccak hardening sites.

Zero F-29-NN emissions (Phase 236 FIND-01 owns ID assignment per CONTEXT D-11).
Zero contracts/ or test/ writes (READ-only v29.0 milestone rule)."
```

Do NOT run `git push`. Verify `git log -1` shows the new commit and `git status --porcelain contracts/ test/` is empty.
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -qE "233-01.*JKP-01.*BAF" && test -z "$(git status --porcelain contracts/ test/)"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one new commit added via `git log -1`
    - Commit message subject starts with `docs(233-01):` and contains `JKP-01` + `BAF` + `sentinel`
    - Commit touches exactly `.planning/phases/233-jackpot-baf-entropy-audit/233-01-AUDIT.md` (no contracts/ or test/ files)
    - `git status --porcelain contracts/ test/` returns empty
    - Commit not pushed — branch state local to repository
  </acceptance_criteria>
  <deliverable>
Approved `233-01-AUDIT.md` committed with a `docs(233-01): JKP-01 ...` message; no code modifications; no push.
  </deliverable>
  <done>Commit exists with correct message format; repository `contracts/` and `test/` remain unmodified; commit is local only (not pushed).</done>
</task>

</tasks>

<verification>
- `233-01-AUDIT.md` exists with per-function verdict table covering every element in 230-01-DELTA-MAP.md §1.2 owned by `104b5d42` (four emit sites + BAF_TRAIT_SENTINEL constant + two widened event decls) + IM-14 + IM-15 + 230-02-DELTA-ADDENDUM overlap row
- Dedicated Domain-Collision Sweep Table enumerating every on-chain reader of event `traitId` + every read of `winningTraitsPacked` / `bonusTraitsPacked` across contracts/
- Locked column set per D-02 (`Function | Commit SHA | File:Line | Verdict | Evidence | Notes`) + `Finding Candidate` column per 232-NN-AUDIT.md precedent
- Every verdict row cites `104b5d42`
- Every File:Line anchor resolves into `contracts/` at HEAD
- Verdicts exclusively use `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (per D-02)
- Every row carries `Y` or `N` in Finding Candidate column
- D-04 attack vectors present: four emit sites verified + constant properties verified + domain-collision sweep (winningTraitsPacked / bonusTraitsPacked / traitId event-field readers classified)
- Event-widening off-chain ABI regeneration recorded as SAFE-INFO Finding Candidate: Y for both event decls (indexer-compat OBSERVATION)
- 230-02-DELTA-ADDENDUM.md overlap check row present (per D-12)
- Findings-Candidate / Scope-guard Deferrals / Downstream Hand-offs subsections present
- Hand-offs explicitly name Phase 236 FIND-01 (per D-11) and address JKP-02 / JKP-03 / RNG-01 / CONS-01 non-overlap
- Zero `F-29-NN` references anywhere in the file (per D-11)
- Zero writes to `contracts/` or `test/` during plan execution (READ-only v29.0 milestone rule)
- Approved AUDIT.md committed with message `docs(233-01): JKP-01 BAF traitId=420 sentinel adversarial audit`
</verification>

<success_criteria>
Every element touched by commit `104b5d42` across the JKP-01 surface (four emit sites, BAF_TRAIT_SENTINEL constant, two widened event decls) has a verdict against the D-04 attack vectors. Every on-chain reader of event `traitId` field + every on-chain read of storage `winningTraitsPacked` / `bonusTraitsPacked` in contracts/ is enumerated in the Domain-Collision Sweep Table with a narrowing / opaque-tag / non-existence classification. Every verdict is traceable via SHA + File:Line. Event-widening is characterised as SAFE-INFO indexer-compat OBSERVATION routing to Phase 236 FIND-01 / FIND-02; NOT an on-chain finding. 230-02-DELTA-ADDENDUM.md overlap check confirms zero overlap with JKP-01 surface. Phase 230 delta-map is not edited; any out-of-scope discovery is recorded in Scope-guard Deferrals per D-03. READ-only milestone rule honored: zero writes to `contracts/` or `test/`.
</success_criteria>

<output>
After completion, create `.planning/phases/233-jackpot-baf-entropy-audit/233-01-SUMMARY.md`
</output>
</content>
