---
phase: 233-jackpot-baf-entropy-audit
plan: 233-03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/233-jackpot-baf-entropy-audit/233-03-AUDIT.md
autonomous: true
requirements: [JKP-03]
must_haves:
  truths:
    - "Every caller site that reads `bonusTraitsPacked` via `_rollWinningTraits(rngWord, true)` across the three in-scope paths (purchase-phase / jackpot-phase / earlybird) has at least one verdict row in the per-path cross-path derivation table"
    - "The three paths are: (a) purchase-phase `payDailyJackpot` consumer at DegenerusGameJackpotModule.sol:507 (`bonusTraitsPacked = _rollWinningTraits(randWord, true)` — consumer surfaced by 20a951df rewrite context though primary path is coin-jackpot); (b) jackpot-phase `payDailyJackpotCoinAndTickets` consumer at DegenerusGameJackpotModule.sol:593 (`bonusTraitsPacked = _rollWinningTraits(randWord, true)`); (c) earlybird `_runEarlyBirdLootboxJackpot` consumer at DegenerusGameJackpotModule.sol:677 (`_rollWinningTraits(rngWord, true)`) — the rewritten path owned by 20a951df"
    - "The shared helper `_rollWinningTraits` at DegenerusGameJackpotModule.sol:1865-1875 is verdict-row verified to produce identical output for identical `(rngWord, bonus=true)` arguments across all callers. The BONUS_TRAITS_TAG domain-separation is cryptographically invariant."
    - "The shared consumer `JackpotBucketLib.unpackWinningTraits(uint32 packed) -> uint8[4]` is verdict-row verified to produce identical 4-trait output for identical packed input across all callers"
    - "Every verdict row cites the 7-char commit SHA(s) `104b5d42` (BAF emit path context) AND `20a951df` (earlybird rewrite) per D-08 shared-SHA directive (per D-09 Phase 231 EBD-02 owns different aspects; Phase 233 JKP-03 explicitly documents non-overlap)"
    - "Every verdict row cites a File:Line anchor pointing into contracts/modules/DegenerusGameJackpotModule.sol at HEAD (per D-02)"
    - "Verdict vocabulary used is exactly `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (per D-02)"
    - "Every verdict row carries a `Finding Candidate: Y/N` column value"
    - "No F-29-NN finding IDs appear anywhere in 233-03-AUDIT.md (per D-11)"
    - "D-08 cross-path property is explicitly tested: for the SAME VRF word, the three paths MUST derive an IDENTICAL 4-trait set. The audit proves this via (a) each path uses the SAME helper `_rollWinningTraits` with the SAME arguments `(rngWord, true)`, (b) the helper is deterministic-pure given its inputs (grep for state reads — `_applyHeroOverride` at line 1873 reads hero state; confirm this is pre-committed state outside the VRF window)"
    - "D-09 Non-overlap with Phase 231 EBD-02 paragraph is EXPLICITLY present: Phase 231 EBD-02 audits the `20a951df` rewrite's INTERNAL correctness (bonus-trait parity with coin jackpot, salt-space isolation, lvl+1 queue fix, futurePool -> nextPool conservation). Phase 233 JKP-03 audits the CROSS-PATH AGREEMENT (three paths, same VRF word -> same 4-trait set). Different aspects, same SHA, non-overlapping."
    - "D-12 230-02-DELTA-ADDENDUM.md integration: plan MUST confirm the cross-path agreement holds POST-HARDENING. The 16 JackpotModule c2e5e0a9 sites (277, 443, 508, 522, 544, 594, 596, 607-609, 874, 937, 1134, 1238, 1345, 1681-1683, 1741, 1798-1800, 1808) include sites at 508 / 522 / 1681 that use `keccak256(abi.encode(randWord, lvl, COIN_JACKPOT_TAG))` for the coin-jackpot main-trait decorrelation; confirm the bonusTraitsPacked readers at 507 / 593 / 677 continue to use `_rollWinningTraits(rngWord, true)` which applies its own `BONUS_TRAITS_TAG` domain separator at line 1870 — the two keccak formulations sit at DIFFERENT levels (rollWinningTraits with bonus flag vs coin-jackpot main-trait sub-selection) and DO NOT interfere with cross-path bonus-trait agreement."
    - "Findings-Candidate Block, Scope-guard Deferrals, and Downstream Hand-offs subsections present; Phase 236 FIND-01 hand-off explicitly named"
    - "Zero contracts/ and zero test/ writes occur during plan execution (READ-only per v29.0 milestone rule)"
  artifacts:
    - path: ".planning/phases/233-jackpot-baf-entropy-audit/233-03-AUDIT.md"
      provides: "JKP-03 cross-path bonusTraitsPacked consistency adversarial audit"
      contains: "## Cross-Path Derivation Table"
  key_links:
    - from: "230-01-DELTA-MAP.md §1.2 (_runEarlyBirdLootboxJackpot MODIFIED by 20a951df + coin-jackpot readers of bonusTraitsPacked) + §2.3 IM-14 / IM-15 / IM-16 + §4 JKP-03 row"
      to: "233-03-AUDIT.md cross-path derivation table rows"
      via: "Every bonusTraitsPacked reader has a row proving same helper + same args across paths"
      pattern: "SAFE|SAFE-INFO|VULNERABLE|DEFERRED"
    - from: "233-03-AUDIT.md cross-path consistency verdict"
      to: "Phase 231 EBD-02 earlybird-rewrite-internal verdicts (SUMMARY cross-reference)"
      via: "D-09 non-overlap paragraph: Phase 231 audits the rewrite INTERNAL correctness; Phase 233 JKP-03 audits the CROSS-PATH agreement"
      pattern: "Phase 231 EBD-02"
    - from: "233-03-AUDIT.md post-hardening cross-path agreement row"
      to: "230-02-DELTA-ADDENDUM.md c2e5e0a9 JackpotModule verdicts"
      via: "D-12 integration — cross-path agreement holds post-c2e5e0a9 because the _rollWinningTraits helper keccak formulation sits at a different level than the coin-jackpot main-trait decorrelation"
      pattern: "BONUS_TRAITS_TAG|COIN_JACKPOT_TAG"
    - from: "233-03-AUDIT.md VULNERABLE/DEFERRED verdicts"
      to: "Phase 236 FIND-01 finding-candidate pool"
      via: "Findings-Candidate Block (no F-29-NN IDs, Finding Candidate: Y/N column)"
      pattern: "## Findings-Candidate Block"
---

<objective>
JKP-03 adversarial audit of cross-path `bonusTraitsPacked` consistency — proving that every jackpot caller site using `bonusTraitsPacked` derives an IDENTICAL 4-trait set for the SAME VRF word across the purchase-phase path, the jackpot-phase path, and today's earlybird rewrite. Commit ownership is shared between `104b5d42` (BAF sentinel emission paths — provides event-surface context) and `20a951df` (earlybird trait-alignment rewrite — introduces the third consumer site). Produces `233-03-AUDIT.md`: a per-path cross-path derivation table listing the three bonus-trait consumer sites with columns `Path | File:Line | Helper | Salt Arguments | Flag | Verdict`, plus a per-helper verdict table for `_rollWinningTraits` + `JackpotBucketLib.unpackWinningTraits`, plus a D-09 non-overlap paragraph explicitly disclosing that Phase 231 EBD-02 audits the rewrite's INTERNAL correctness while Phase 233 JKP-03 audits the CROSS-PATH AGREEMENT (different aspects, same SHA). Plus Findings-Candidate / Scope-guard Deferrals / Downstream Hand-offs subsections.

Purpose: The v26.0 Bonus Jackpot Split introduced the `bonus` boolean flag on `_rollWinningTraits` to derive an INDEPENDENT 4-trait set (via `keccak256(abi.encodePacked(randWord, BONUS_TRAITS_TAG))` salt) from the same VRF word as the main-trait roll. The 20a951df commit rewrote `_runEarlyBirdLootboxJackpot` to consume this bonus set via the SAME helper call pattern (`_rollWinningTraits(rngWord, true)` at line 677, then unpack via `JackpotBucketLib.unpackWinningTraits` at line 676) — intentionally aligning with the coin-jackpot bonus consumers at lines 507 + 593 (both per DegenerusGameJackpotModule.sol grep). A C4A warden would probe: (a) do all three consumers supply IDENTICAL `(rngWord, true)` arguments to `_rollWinningTraits`? — any divergence (different salt, different flag, different ordering) breaks cross-path agreement; (b) does `_rollWinningTraits` produce byte-identical output for byte-identical inputs? — the helper at line 1865-1875 is state-reading via `_applyHeroOverride` at line 1873, so its purity depends on hero-state commitment timing; verify hero state is read from pre-committed pre-VRF-window storage; (c) does `JackpotBucketLib.unpackWinningTraits` produce byte-identical `uint8[4]` output for byte-identical `uint32 packed` input? — this is a pure library function; (d) post-`c2e5e0a9` keccak hardening (D-12 integration): do the three bonus-trait consumers remain cross-path-consistent after the addendum's 16 JackpotModule keccak replacements? — verify none of the hardened sites introduce per-call divergence for the bonus-trait path. Phase 231 EBD-02 separately verifies the earlybird rewrite's INTERNAL correctness (bonus-trait parity with coin jackpot, salt-space isolation, lvl+1 queue fix, futurePool -> nextPool conservation); Phase 233 JKP-03 verifies the CROSS-PATH AGREEMENT. The two phases share SHA `20a951df` for non-overlapping aspects per D-09.

Output: `233-03-AUDIT.md` with cross-path derivation table + per-helper verdict table + D-09 non-overlap paragraph + D-12 post-hardening consistency proof + Findings-Candidate Block + Scope-guard Deferrals + Downstream Hand-offs subsections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/233-jackpot-baf-entropy-audit/233-CONTEXT.md
@.planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md
@.planning/phases/230-delta-extraction-scope-map/230-02-DELTA-ADDENDUM.md
@.planning/phases/230-delta-extraction-scope-map/230-01-SUMMARY.md
@.planning/phases/231-earlybird-jackpot-audit/231-02-PLAN.md
@.planning/phases/231-earlybird-jackpot-audit/231-02-AUDIT.md
@.planning/phases/232-decimator-audit/232-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract 20a951df + 104b5d42 diffs + read JackpotModule at HEAD; enumerate three bonusTraitsPacked consumer sites; prove cross-path agreement; write 233-03-AUDIT.md</name>
  <files>.planning/phases/233-jackpot-baf-entropy-audit/233-03-AUDIT.md</files>
  <read_first>
    - .planning/phases/233-jackpot-baf-entropy-audit/233-CONTEXT.md (D-01 through D-12 — specifically: D-01 three-plan split with Plan 233-03 owning JKP-03 only; D-02 locked column schema + Finding Candidate extension per 232-NN-AUDIT.md precedent; D-03 scope-guard deferral rule; D-08 cross-path property — "For the same VRF word, the three call sites that read `bonusTraitsPacked` must derive the identical 4-trait set. Plan 233-03 enumerates the three sites from `230-01-DELTA-MAP.md §2.3` (IM-14 coin-jackpot bonus roll path via `runBafJackpot` caller chain, IM-15 the BAF emit path, IM-16 `_runEarlyBirdLootboxJackpot` -> `_rollWinningTraits(rngWord, true)`) and the purchase-phase reader path surfaced by the 20a951df rewrite. For each, the trait-derivation is traced back to a shared helper (`JackpotBucketLib.unpackWinningTraits` / `_rollWinningTraits`) called with the same `(rngWord, true)` flag. Any divergence — different salt, different flag, different bit unpacking — is a finding candidate."; D-09 overlap with Phase 231 EBD-02 — "Commit `20a951df` is PRIMARILY owned by Phase 231 EBD-02 (the trait-alignment rewrite itself: parity, salt isolation, fixed-level queueing, futurePool->nextPool conservation). Phase 233 JKP-03 cites `20a951df` for a DIFFERENT aspect: cross-path consistency (three paths, same VRF word -> same 4-trait set). SHA-sharing is expected and correct per ROADMAP; the aspects are non-overlapping. Plan 233-03 does not re-audit EBD-02's aspects and does not block on Phase 231."; D-11 no F-29-NN IDs; D-12 MUST read 230-02-DELTA-ADDENDUM.md and confirm cross-path agreement holds post-hardening)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.2 (lines 91-105 — `_runEarlyBirdLootboxJackpot(uint24 lvl, uint256 rngWord)` MODIFIED by 20a951df at line 97: "Rewritten to draw winners from the same 4 bonus traits rolled for the coin jackpot via `_rollWinningTraits(rngWord, true)` + `JackpotBucketLib.unpackWinningTraits`. 4 × 25 winners per trait (100 total) pulled by 4 `_randTraitTicket` calls (was 100 per-winner calls with per-winner `entropy = EntropyLib.entropyStep(entropy)`). All winners queued at `lvl` (= outer caller level + 1) instead of spreading across `baseLevel..baseLevel+4`. Budget source and `_setNextPrizePool(... + totalBudget)` residual flow unchanged." — this is the THIRD consumer surfaced by 20a951df; the first two are pre-existing in the coin-jackpot paths)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §2.3 IM-14 (line 263 — `_consolidatePoolsAndRewardJackpots -> DegenerusGame.runBafJackpot` self-call pre-existing; callee MODIFIED by 104b5d42 BAF sentinel emit; context for JKP-03 because the `_consolidatePoolsAndRewardJackpots` caller path is the jackpot-phase entry into the bonus-trait consumers), IM-15 (line 264 — `DegenerusGame.runBafJackpot -> DegenerusGameJackpotModule.runBafJackpot` delegatecall; body MODIFIED by 104b5d42 BAF sentinel emit), and IM-16 (line 265 — `_runEarlyBirdLootboxJackpot -> _rollWinningTraits(rngWord, true)` NEW by 20a951df — "primary entropy-parity invariant for EBD-02 / JKP-03"). The IM-14 / IM-15 chain exists for context but the BAF path itself DOES NOT READ `bonusTraitsPacked` (runBafJackpot does not invoke `_rollWinningTraits` — it consumes winners from `jackpots.runBafJackpot(poolWei, lvl, rngWord)` external call at line 1973). Clarify in the AUDIT: IM-14 / IM-15 are cited for cross-phase traceability, but the THREE actual `bonusTraitsPacked` consumer sites are (a) `payDailyJackpot` at line 507 purchase-phase path, (b) `payDailyJackpotCoinAndTickets` at line 593 jackpot-phase path, (c) `_runEarlyBirdLootboxJackpot` at line 677 earlybird path. Each calls `_rollWinningTraits(rngWord, true)` independently — the cross-path agreement is that all three produce identical 4-trait sets for the same rngWord because the helper is deterministic given inputs.
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §4 Consumer Index JKP-03 row (line 557 — authoritative scope anchor per D-04: "§1.2 (`_runEarlyBirdLootboxJackpot`) + all jackpot-side `bonusTraitsPacked` readers/writers, §2.3 (IM-14..IM-16 — all bonusTrait chains)")
    - .planning/phases/230-delta-extraction-scope-map/230-02-DELTA-ADDENDUM.md (D-12 integration): specifically the c2e5e0a9 JackpotModule verdict rows for lines 508 / 522 / 1681 group using `keccak256(abi.encode(randWord, lvl, COIN_JACKPOT_TAG))` — these are MAIN-trait decorrelation, NOT bonus-trait formulation; the bonus-trait formulation remains `_rollWinningTraits(rngWord, true)` -> `keccak256(abi.encodePacked(randWord, BONUS_TRAITS_TAG))` at line 1869-1871 — a DIFFERENT keccak formulation at a DIFFERENT tag. Verify cross-path bonus-trait agreement holds post-hardening by confirming the 3 consumer sites (507, 593, 677) still call `_rollWinningTraits(rngWord, true)` with identical arguments (neither 104b5d42 nor c2e5e0a9 nor 314443af modifies `_rollWinningTraits` body itself).
    - .planning/phases/231-earlybird-jackpot-audit/231-02-AUDIT.md (CRITICAL — per D-09 non-overlap disclosure): Phase 231 EBD-02 audit shipped with 6 verdict rows across `_runEarlyBirdLootboxJackpot` + `_rollWinningTraits` covering all 4 EBD-02 attack vectors (bonus-trait parity with coin jackpot, salt-space isolation, lvl+1 queue fix, futurePool -> nextPool CEI). Per STATE.md Phase 231-02 bullet: "`_rollWinningTraits(rngWord, true)` call at `DegenerusGameJackpotModule:677` is byte-identical in arg order and salt flag to bonus consumers at lines 1679 and 1705" + "BONUS_TRAITS_TAG = keccak256('BONUS_TRAITS')" compile-time constant at line 171 is the cryptographic domain separator. JKP-03 DOES NOT re-audit these aspects — they are EBD-02 territory. JKP-03 cites 231-02-AUDIT.md as prior-art evidence for the domain-separation + call-shape invariant, then extends to cross-path agreement across the THREE actual consumer sites (NOT to re-prove salt isolation).
    - Output of `git show 20a951df --stat` and `git show 20a951df -- contracts/modules/DegenerusGameJackpotModule.sol` — the `_runEarlyBirdLootboxJackpot` rewrite adds the third bonus-trait consumer at line 677. Pre-fix (`20a951df^`) used `_randTraitTicket` with per-winner entropy-stepped picks; HEAD uses 4 × 25 winners via `_rollWinningTraits(rngWord, true)` + `JackpotBucketLib.unpackWinningTraits`.
    - Output of `git show 104b5d42 --stat` and `git show 104b5d42 -- contracts/modules/DegenerusGameJackpotModule.sol` — the BAF sentinel commit does NOT touch `_rollWinningTraits` or the three bonus-trait consumer sites; confirm via diff grep. It only modifies the BAF emit sites (lines 2002, 2014, 2034, 2038) + event decls + adds the BAF_TRAIT_SENTINEL constant. No cross-path bonus-trait impact.
    - contracts/modules/DegenerusGameJackpotModule.sol at HEAD — enumerate EVERY `_rollWinningTraits(*, true)` call site and EVERY `bonusTraitsPacked` read / write. Verify via grep the following preliminary-grep anchor set (update against HEAD):
        * Line 102: `uint32 bonusTraitsPacked` — struct field or event argument in a declaration; NOT a consumer call. Record context.
        * Line 507: `uint32 bonusTraitsPacked = _rollWinningTraits(randWord, true);` — consumer #1 inside `payDailyJackpot` (purchase-phase path). Record full surrounding context (lines 500-530 approx): what path inside `payDailyJackpot` invokes this? Is it gated by purchase-phase-only conditions?
        * Line 518-525: `winningTraitsPacked = _rollWinningTraits(randWord, false);` at 518, then `uint32 bonusTraitsPacked = _rollWinningTraits(randWord, true);` at 521 inside `payDailyJackpot` — additional consumer #1 variant inside `payDailyJackpot`. Record path.
        * Line 545: `winningTraitsPacked: winningTraitsPacked,` — field assignment in a struct literal; not a consumer. Context only.
        * Line 560: `winningTraitsPacked,` — field reference in a function call; context only.
        * Line 592: `uint32 mainTraitsPacked = _rollWinningTraits(randWord, false);` — main-trait consumer inside `payDailyJackpotCoinAndTickets`; context for the bonus consumer at line 593.
        * Line 593: `uint32 bonusTraitsPacked = _rollWinningTraits(randWord, true);` — consumer #2 inside `payDailyJackpotCoinAndTickets` (jackpot-phase path). Record full surrounding context.
        * Line 614: `bonusTraitsPacked,` — field reference (likely in an emit or function call) inside `payDailyJackpotCoinAndTickets`; context.
        * Line 643: `bonusTraitsPacked,` — same as 614 reference.
        * Line 677: `_rollWinningTraits(rngWord, true)` nested inside `JackpotBucketLib.unpackWinningTraits(...)` — consumer #3 inside `_runEarlyBirdLootboxJackpot` (earlybird path; NEW by 20a951df). Record line 676-678 block. NOTE: here the intermediate is NOT stored into a `bonusTraitsPacked` local — the result is directly unpacked into `uint8[4] memory bonusTraits`. So consumer #3 invokes `_rollWinningTraits(rngWord, true)` in the same pattern as consumers #1 and #2 but skips the intermediate named local.
        * Line 1679: `uint32 bonusTraitsPacked = _rollWinningTraits(randWord, true);` — ADDITIONAL consumer inside `_awardDailyCoinJackpotInRange` per Phase 231-02 audit note ("bonus consumers at lines 1679 and 1705"). Record context — is this the SAME path as consumer #2 or a parallel path?
        * Line 1705-1707: `uint32 mainTraitsPacked = _rollWinningTraits(randWord, true);` at 1705 (NOTE: arg `true` but variable named `mainTraitsPacked` — might be a naming quirk or a separate non-bonus usage; verify semantic intent from surrounding context) + `uint32 bonusTraitsPacked = _rollWinningTraits(saltedRng, true);` at 1707 (NOTE: uses `saltedRng` not `randWord` — DIFFERENT VRF INPUT; record this as EITHER a cross-path divergence candidate OR evidence that this is a DIFFERENT consumer with a DIFFERENT VRF source). VERIFY the context at lines 1700-1720 — if `saltedRng = keccak256(abi.encode(randWord, <something>))` or similar, this might be a per-level bonus carryover path that is expected to diverge from the main-tx bonus consumers. CRITICAL: JKP-03 cross-path property says "for the same VRF word". Line 1707's `saltedRng` is NOT the same VRF word as the main `randWord` used at 1705 — so line 1707 is OUT OF SCOPE for the cross-path property (different VRF input by construction) but MUST be documented to prevent confusion with the three primary consumers.
        * `_rollWinningTraits(uint256 randWord, bool isBonus) private view returns (uint32 packed)` at line 1865-1875 — the shared helper. Record its body verbatim:
          - Line 1869-1871: `uint256 r = isBonus ? uint256(keccak256(abi.encodePacked(randWord, BONUS_TRAITS_TAG))) : randWord;`
          - Line 1872: `uint8[4] memory traits = JackpotBucketLib.getRandomTraits(r);`
          - Line 1873: `_applyHeroOverride(traits, r);`
          - Line 1874: `packed = JackpotBucketLib.packWinningTraits(traits);`
        * Grep `BONUS_TRAITS_TAG` to locate the compile-time constant declaration — per Phase 231-02 STATE summary it's at line 171 (`BONUS_TRAITS_TAG = keccak256("BONUS_TRAITS")`). Verify against HEAD.
        * Grep `_applyHeroOverride` to locate its definition + understand its state reads. This is CRUCIAL: if `_applyHeroOverride` reads mutable state that can change between consumer invocations, then different paths could produce different outputs for the same `r`. Verify `_applyHeroOverride` is state-pure given `(traits, r)` or only reads state that is pre-committed at VRF request time (e.g. `hero symbol` ranking for the current day — which is set during purchase-phase before VRF fulfillment).
        * Grep `JackpotBucketLib.getRandomTraits` + `JackpotBucketLib.packWinningTraits` + `JackpotBucketLib.unpackWinningTraits` — all should be pure library functions.
    - Determine the SCOPE of "three consumer sites" for JKP-03 per D-08. CONTEXT.md D-08 explicitly says "three call sites": purchase-phase path / jackpot-phase path / earlybird path. Per the grep above, the actual code has MORE than three `_rollWinningTraits(*, true)` call sites (at 507, 521, 593, 677, 1679, 1707). The three CONCEPTUAL paths map to:
        * Purchase-phase path: `payDailyJackpot` at line 507 (+ variant at 521). Note `payDailyJackpot` has the signature `(bool isJackpotPhase, uint24 lvl, uint256 randWord)` — so the same function entry point handles both phases; the 507 call site is inside the purchase-phase branch. Verify from HEAD source.
        * Jackpot-phase path: `payDailyJackpotCoinAndTickets` at line 593 (+ possibly `_awardDailyCoinJackpotInRange` at line 1679 as carryover sub-path).
        * Earlybird path: `_runEarlyBirdLootboxJackpot` at line 677.
      Record the exact mapping in the AUDIT — the "three" label aligns with the three CONCEPTUAL paths, but the per-function verdict table may have more than three rows because of multiple call sites within each conceptual path.
    - contracts/libraries/JackpotBucketLib.sol at HEAD — locate `unpackWinningTraits(uint32 packed) returns (uint8[4] memory)` + `packWinningTraits(uint8[4] memory) returns (uint32)` + `getRandomTraits(uint256 r) returns (uint8[4])`. Verify all three are pure functions (no state reads, no external calls). This proves the deterministic property: same inputs -> same outputs.
    - contracts/modules/DegenerusGameJackpotModule.sol at HEAD — locate `_applyHeroOverride(uint8[4] memory traits, uint256 r)` — this is a potential impurity concern for JKP-03. Read its body to confirm:
        * Either (a) it is pure modulo its inputs (does not read storage), OR
        * (b) it reads pre-committed storage (e.g. `hero symbol for level` set at day-start before the VRF request), OR
        * (c) it reads storage that CAN change between consumer invocations — which would break cross-path agreement.
      If (c), record as VULNERABLE Finding Candidate: Y. Expected: (a) or (b). Cross-reference with Phase 231-02 audit (which also handled `_rollWinningTraits` re-verification per D-03).
    - .planning/phases/231-earlybird-jackpot-audit/231-02-PLAN.md (precedent plan-of-record structural template for JKP-03 methodology citing) — mirror the structure for 233-03 but with cross-path-table shape instead of per-function-table shape for the primary deliverable.
    - .planning/phases/232-decimator-audit/232-01-PLAN.md — precedent audit-phase PLAN.md shape (frontmatter must_haves block, objective, execution_context, tasks with read_first / action / verify / acceptance_criteria / done structure).
  </read_first>
  <action>
Execute a fresh-from-source adversarial audit of the cross-path `bonusTraitsPacked` consistency property per CONTEXT.md D-01 through D-12, with primary focus on D-08 (cross-path property) and D-09 (non-overlap with Phase 231 EBD-02). Verdict vocabulary per D-02 is strictly `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`. Column schema per D-02 is strictly `Function | Commit SHA | File:Line | Verdict | Evidence | Notes` with an additional `Finding Candidate: Y/N` column per 232-NN-AUDIT.md precedent. The PRIMARY deliverable table uses D-08's specified columns `Path | File:Line | Helper | Salt Arguments | Flag | Verdict` for the cross-path derivation table; a secondary per-function verdict table uses the standard D-02 schema.

**Step 1 — Build the scope list from `230-01-DELTA-MAP.md` §4 Consumer Index JKP-03 row (D-04) + D-08 three-path definition.** The target element set is:
  - **Consumer site #1 (purchase-phase path):** `DegenerusGameJackpotModule.payDailyJackpot` bonus-trait consumer at line 507 (+ variant at 521 if grep confirms two calls inside the same function body) — owned by the jackpot-phase/purchase-phase dispatcher; verify which branch (isJackpotPhase == false) feeds into this consumer
  - **Consumer site #2 (jackpot-phase path):** `DegenerusGameJackpotModule.payDailyJackpotCoinAndTickets` bonus-trait consumer at line 593 (main-trait at 592) + possible parallel path at `_awardDailyCoinJackpotInRange` at line 1679
  - **Consumer site #3 (earlybird path):** `DegenerusGameJackpotModule._runEarlyBirdLootboxJackpot` bonus-trait consumer at line 677 (directly nested inside `JackpotBucketLib.unpackWinningTraits(_rollWinningTraits(rngWord, true))` at lines 676-678) — NEW by 20a951df
  - **Shared helper:** `DegenerusGameJackpotModule._rollWinningTraits(uint256 randWord, bool isBonus) private view returns (uint32 packed)` at line 1865-1875 — the SINGLE point of truth for bonus-trait derivation
  - **Shared helper sub-routine:** `DegenerusGameJackpotModule._applyHeroOverride(uint8[4] memory traits, uint256 r)` — impurity candidate; verify state-reading behavior
  - **Shared library functions:** `JackpotBucketLib.unpackWinningTraits`, `JackpotBucketLib.getRandomTraits`, `JackpotBucketLib.packWinningTraits` in `contracts/libraries/JackpotBucketLib.sol`
  - **Domain separator constant:** `BONUS_TRAITS_TAG = keccak256("BONUS_TRAITS")` at line 171

**Step 2 — For each consumer site and shared helper, run the D-08 cross-path attack vectors:**

**(A) D-08 cross-path helper-invocation identity:**
  - For each of the three consumer sites (507 / 593 / 677), extract the exact argument list passed to `_rollWinningTraits`. Verify:
    - Argument 1 (`randWord` or `rngWord`): What variable is passed? Is it sourced from the SAME VRF word that the other consumers receive for the same logical transaction? (Note: the three paths may run in DIFFERENT transactions — purchase-phase is a daily advanceGame call, jackpot-phase is a daily advanceGame call, earlybird is a specific jackpot-phase day; but the cross-path property is "for the SAME VRF word" — i.e. if consumer #1 were given the same word as consumer #2, they'd produce the same output. The property is about helper purity, not call-timing alignment.)
    - Argument 2: literal `true` passed in all three cases. Verify via grep.
    - Return value consumption pattern: consumer #1 at 507 assigns to `uint32 bonusTraitsPacked` local; consumer #2 at 593 assigns to `uint32 bonusTraitsPacked` local; consumer #3 at 677 passes directly to `JackpotBucketLib.unpackWinningTraits(...)` without a named local. All three ultimately feed into `JackpotBucketLib.unpackWinningTraits` which returns `uint8[4]`. Record whether any consumer applies an intermediate transformation to the packed value before unpacking (e.g. bit manipulation, XOR with another value) — expected: no transformation; direct pack -> unpack.
  - Record a verdict row per consumer site confirming IDENTICAL invocation pattern (`_rollWinningTraits(<vrf_word_variable>, true)` with no intermediate transformation before `unpackWinningTraits`).

**(B) D-08 shared-helper determinism:**
  - Read `_rollWinningTraits` body at line 1865-1875 verbatim. The helper receives `(uint256 randWord, bool isBonus)` and returns `uint32 packed`. The function is declared `private view` — VIEW means it reads state but does not modify it. View-ness alone does NOT guarantee cross-invocation determinism: if the state it reads CAN CHANGE between invocations, two invocations with the same arguments produce DIFFERENT outputs.
  - Identify every state read inside `_rollWinningTraits`:
    - Line 1870: `keccak256(abi.encodePacked(randWord, BONUS_TRAITS_TAG))` — BONUS_TRAITS_TAG is a COMPILE-TIME CONSTANT (verify visibility at line 171), so this keccak depends only on `randWord`.
    - Line 1872: `JackpotBucketLib.getRandomTraits(r)` — library function; verify pure.
    - Line 1873: `_applyHeroOverride(traits, r)` — POTENTIAL STATE READ. Must inspect its body.
    - Line 1874: `JackpotBucketLib.packWinningTraits(traits)` — library function; verify pure.
  - Inspect `_applyHeroOverride`. If it reads state that CAN change between invocations of `_rollWinningTraits` with the same inputs, then the three consumer sites may produce different outputs despite identical helper invocations — breaking cross-path agreement. Classify the state reads:
    - If `_applyHeroOverride` reads hero-symbol ranking data that is SET during purchase-phase activity and FROZEN from VRF-request onward: cross-path agreement holds because all three consumers read the same frozen state.
    - If `_applyHeroOverride` reads state that is mutated between VRF fulfillment and any of the three consumer invocations: record as VULNERABLE Finding Candidate: Y for Phase 236 FIND-01.
    - Expected: frozen-from-VRF-request-onward state (Phase 231-02 audit presumably verified this as part of the salt-space isolation audit — cite 231-02-AUDIT.md as prior art if so).

**(C) D-08 library-function purity:**
  - Verify `JackpotBucketLib.unpackWinningTraits`, `JackpotBucketLib.getRandomTraits`, `JackpotBucketLib.packWinningTraits` are pure functions (no state, no external calls, deterministic in inputs). Read the library body at `contracts/libraries/JackpotBucketLib.sol`. Record SAFE with evidence if pure.

**(D) D-08 per-consumer argument provenance:**
  - For consumer #1 at line 507: where does `randWord` come from? Trace upward from `payDailyJackpot(bool isJackpotPhase, uint24 lvl, uint256 randWord)` at line 328 to its caller. The caller is `DegenerusGame.payDailyJackpot` wrapper (grep to locate) which delegates to this module. The `randWord` is passed from AdvanceModule (daily RNG word). Verify the call chain and confirm `randWord` == daily VRF-fulfilled word.
  - For consumer #2 at line 593: `randWord` from `payDailyJackpotCoinAndTickets(uint256 randWord)` at line 579. Same chain as #1. Verify.
  - For consumer #3 at line 677: `rngWord` from `_runEarlyBirdLootboxJackpot(uint24 lvl, uint256 rngWord)` at line 664. Caller at `payDailyJackpot(true, lvl, randWord)` inside the jackpot-phase branch, which invokes `_runEarlyBirdLootboxJackpot(lvl + 1, randWord)` at line 379 (grep to verify). Same VRF source. Verify.
  - Record a verdict row per consumer site noting the VRF-word source + caller chain.

**(E) D-09 non-overlap disclosure with Phase 231 EBD-02:**
  - Include an EXPLICIT paragraph in the AUDIT (not merely a hand-off hint) stating:
    "Per CONTEXT.md D-09, commit `20a951df` is PRIMARILY owned by Phase 231 EBD-02 which audits the rewrite's INTERNAL correctness: (i) bonus-trait parity with coin jackpot, (ii) salt-space isolation between `bonus=true` and `bonus=false` branches, (iii) fixed-level queueing at `lvl+1`, (iv) futurePool -> nextPool conservation. Phase 233 JKP-03 cites `20a951df` for a DIFFERENT aspect: cross-path CONSISTENCY across the three bonus-trait consumer sites. The two phases' aspects are non-overlapping — Phase 231 audits the REWRITE itself in isolation; Phase 233 audits the AGREEMENT between the rewrite and the two pre-existing consumer sites. SHA-sharing is expected and correct per ROADMAP. This plan does NOT re-audit EBD-02's aspects and does NOT block on Phase 231 completion (which completed 2026-04-17). Evidence from 231-02-AUDIT.md (specifically the BONUS_TRAITS_TAG salt-isolation verdict) is cited as prior-art input to JKP-03's cross-path proof but not re-derived."

**(F) D-12 230-02-DELTA-ADDENDUM.md post-hardening cross-path agreement:**
  - Verify none of the 16 c2e5e0a9 JackpotModule keccak-replacement sites touches `_rollWinningTraits` (line 1865-1875) or `_applyHeroOverride` or the three bonus-trait consumer sites (507 / 593 / 677). Per addendum page 1 per-file table the 16 sites are at 277, 443, 508, 522, 544, 594, 596, 607-609, 874, 937, 1134, 1238, 1345, 1681-1683, 1741, 1798-1800, 1808. NOTE: lines 508 and 522 are ADJACENT to consumer #1's site at 507; line 594 is ADJACENT to consumer #2's site at 593; line 1681 is ADJACENT to consumer at 1679. Verify these adjacent hardening sites modify SURROUNDING logic (main-trait decorrelation, carryover decorrelation, etc.) but do NOT modify the `_rollWinningTraits(rngWord, true)` bonus-trait call itself. Record as SAFE — cross-path bonus-trait agreement is preserved post-hardening because the hardening addresses MAIN-trait + carryover + per-bucket decorrelation, NOT the bonus-trait formulation.
  - Verify `_rollWinningTraits`'s internal keccak at line 1870 (`keccak256(abi.encodePacked(randWord, BONUS_TRAITS_TAG))`) is IDENTICAL pre-hardening and post-hardening — neither 104b5d42, 20a951df, nor c2e5e0a9 touches this line. Record as SAFE.

**(G) 104b5d42 non-overlap with JKP-03 bonus-trait scope:**
  - Verify `104b5d42` modifies ONLY the BAF emit sites + event decls + BAF_TRAIT_SENTINEL constant (per JKP-01 scope). It does NOT modify `_rollWinningTraits`, `_applyHeroOverride`, or any of the three bonus-trait consumer sites. Record as SAFE noting the 104b5d42 surface is ORTHOGONAL to JKP-03's bonus-trait cross-path scope.

**Step 3 — Write `233-03-AUDIT.md` mirroring the 232-NN-AUDIT.md precedent shape + D-08 specified cross-path table shape.** Required structure, in this exact order:

```markdown
# JKP-03 Adversarial Audit: Cross-Path bonusTraitsPacked Consistency

**Scope:** Commits `104b5d42` (BAF sentinel emission context — does NOT modify the bonus-trait consumer sites; cited for §2.3 IM-14 / IM-15 chain traceability) AND `20a951df` (earlybird trait-alignment rewrite — introduces consumer #3 at line 677) per `230-01-DELTA-MAP.md` §1.2 + §2.3 IM-14 / IM-15 / IM-16 + §4 JKP-03 row
**Method:** Fresh read from HEAD source — no reuse of v26.0 Phase 218/219 or Phase 231 EBD-02 verdicts as pre-approved (Phase 231 verdicts cited as prior art for DIFFERENT aspects per D-09 non-overlap)
**Scope source:** `230-01-DELTA-MAP.md` §4 Consumer Index row JKP-03 (per D-04)
**Verdict vocabulary:** `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (per D-02)
**Finding-ID policy:** No `F-29-NN` IDs emitted (per D-11) — `Finding Candidate: Y/N` column only; Phase 236 FIND-01 owns canonical ID assignment
**D-09 non-overlap disclosure:** Phase 231 EBD-02 owns the `20a951df` rewrite's INTERNAL correctness (parity, salt isolation, lvl+1 queue, pool conservation); Phase 233 JKP-03 owns the CROSS-PATH AGREEMENT (three paths -> same 4-trait set for same VRF word). Non-overlapping aspects; shared SHA is expected per ROADMAP.
**D-12 integration:** 230-02-DELTA-ADDENDUM.md c2e5e0a9 16-site JackpotModule hardening adjacent to consumer sites (lines 508/522 near 507, 594 near 593, 1681 near 1679) modifies MAIN-trait + carryover decorrelation only; does NOT modify `_rollWinningTraits` or the bonus-trait consumer calls. Cross-path agreement preserved post-hardening.

## Methodology

[One paragraph: D-08 cross-path property (three consumer sites derive identical 4-trait set for identical VRF word via shared deterministic helper `_rollWinningTraits(randWord, true)` + `JackpotBucketLib.unpackWinningTraits`). D-09 non-overlap with Phase 231 EBD-02 (different aspects, same SHA). D-12 post-hardening consistency (c2e5e0a9 adjacent sites do not alter bonus-trait formulation). Locked verdict vocabulary. Primary deliverable table uses D-08-specified columns `Path | File:Line | Helper | Salt Arguments | Flag | Verdict`; secondary per-function verdict table uses standard D-02 schema.]

## D-09 Non-Overlap Disclosure with Phase 231 EBD-02

[EXPLICIT prose paragraph per D-09: Phase 231 EBD-02 owns the rewrite INTERNAL correctness; Phase 233 JKP-03 owns the cross-path AGREEMENT. Shared SHA 20a951df for different aspects. Cite 231-02-AUDIT.md as prior-art evidence for BONUS_TRAITS_TAG salt isolation + byte-identical call-shape invariant across bonus consumers at lines 1679 + 1705 (which Phase 231-02 noted). This plan does NOT re-audit those aspects.]

## Findings-Candidate Block

[Prose free-form (no F-29-NN IDs). One block per VULNERABLE or DEFERRED verdict identified in the tables below, plus any SAFE-INFO row marked Finding Candidate: Y. Each block: title, verdict, File:Line, owning SHA, attack vector, evidence excerpt, recommended Phase 236 severity-classification note. Expected: zero VULNERABLE / zero DEFERRED; possibly zero candidate findings if `_applyHeroOverride` is confirmed pure-given-inputs or frozen-state-at-VRF-request. If zero candidate findings, state "No candidate findings — all verdicts SAFE against cross-path bonus-trait consistency property."]

## Cross-Path Derivation Table (Primary — Per D-08 Column Schema)

| Path | File:Line | Helper | Salt Arguments | Flag | Verdict |
|---|---|---|---|---|---|
| Purchase-phase path (`payDailyJackpot` bonus-trait roll) | contracts/modules/DegenerusGameJackpotModule.sol:507 | `_rollWinningTraits` | `(randWord, BONUS_TRAITS_TAG)` via line 1869-1871 | `isBonus = true` | SAFE |
| Jackpot-phase path (`payDailyJackpotCoinAndTickets` bonus-trait roll) | contracts/modules/DegenerusGameJackpotModule.sol:593 | `_rollWinningTraits` | Same as above — `(randWord, BONUS_TRAITS_TAG)` | `isBonus = true` | SAFE |
| Earlybird path (`_runEarlyBirdLootboxJackpot` bonus-trait roll) — NEW by 20a951df | contracts/modules/DegenerusGameJackpotModule.sol:677 | `_rollWinningTraits` | Same as above — `(rngWord, BONUS_TRAITS_TAG)` | `isBonus = true` | SAFE |
| [Additional rows for parallel sub-paths if grep surfaces them — e.g. 521 purchase-phase variant, 1679 carryover sub-path] | contracts/modules/DegenerusGameJackpotModule.sol:<line> | `_rollWinningTraits` | Same | `isBonus = true` | <verdict> |

[Replace `<line>` with real File:Line values. The three CONCEPTUAL paths are minimum; additional rows per distinct call site within each path are allowed. Verdict requires: (a) same VRF-word source by construction, (b) same `isBonus=true` flag literal, (c) no intermediate transformation of the packed value before unpacking.]

## Per-Function Verdict Table (Secondary — Per D-02 Standard Schema)

| Function | Commit SHA | File:Line | Verdict | Evidence | Notes | Finding Candidate |
|---|---|---|---|---|---|---|
| `DegenerusGameJackpotModule._rollWinningTraits` (shared helper) | 20a951df (re-verification per D-03; salt flag pre-existed from v26.0) | contracts/modules/DegenerusGameJackpotModule.sol:1865-1875 | SAFE | `keccak256(abi.encodePacked(randWord, BONUS_TRAITS_TAG))` at line 1870 — BONUS_TRAITS_TAG is compile-time constant at line 171; keccak domain-separation gives preimage-resistant isolation; helper is deterministic given (randWord, isBonus) | Cross-path agreement holds iff `_applyHeroOverride` is pure-given-inputs or frozen-state-at-VRF-request | N |
| `DegenerusGameJackpotModule._applyHeroOverride` (shared sub-routine) | (pre-existing; re-verified at HEAD per D-03) | contracts/modules/DegenerusGameJackpotModule.sol:<line> | <verdict — depends on state-reading behavior> | [evidence: state reads inside body; classification as pure / frozen-at-VRF-request / mutable-during-window] | If mutable-during-window, cross-path agreement breaks | <Y/N — Y if mutable> |
| `JackpotBucketLib.unpackWinningTraits` | (pre-existing library; re-verified at HEAD) | contracts/libraries/JackpotBucketLib.sol:<line> | SAFE | Pure function; `uint32 packed -> uint8[4]` bit unpacking; deterministic | N/A | N |
| `JackpotBucketLib.getRandomTraits` | (pre-existing library; re-verified at HEAD) | contracts/libraries/JackpotBucketLib.sol:<line> | SAFE | Pure function; `uint256 r -> uint8[4]` deterministic bucket sampling | N/A | N |
| `JackpotBucketLib.packWinningTraits` | (pre-existing library; re-verified at HEAD) | contracts/libraries/JackpotBucketLib.sol:<line> | SAFE | Pure function; `uint8[4] -> uint32` bit packing; deterministic | N/A | N |
| `BONUS_TRAITS_TAG` constant | (pre-existing from v26.0 Phase 218) | contracts/modules/DegenerusGameJackpotModule.sol:171 | SAFE | `bytes32 private constant BONUS_TRAITS_TAG = keccak256("BONUS_TRAITS");` — compile-time constant; identical at every `_rollWinningTraits` invocation | Domain-separation tag | N |
| Consumer #1 at `payDailyJackpot:507` (bonus consumer purchase-phase) | (pre-existing; path context from 104b5d42 / 20a951df commits' broader module edits) | contracts/modules/DegenerusGameJackpotModule.sol:507 | SAFE | Assigns `_rollWinningTraits(randWord, true)` result to `uint32 bonusTraitsPacked` local; unpacks via subsequent `JackpotBucketLib.unpackWinningTraits` read (locate exact line) | Cross-path aspect of JKP-03 | N |
| Consumer #2 at `payDailyJackpotCoinAndTickets:593` (bonus consumer jackpot-phase) | (pre-existing) | contracts/modules/DegenerusGameJackpotModule.sol:593 | SAFE | Same pattern as #1; different caller function but same helper call shape | Cross-path aspect of JKP-03 | N |
| Consumer #3 at `_runEarlyBirdLootboxJackpot:677` (bonus consumer earlybird) — NEW by 20a951df | 20a951df | contracts/modules/DegenerusGameJackpotModule.sol:676-678 | SAFE | Directly nests `_rollWinningTraits(rngWord, true)` inside `JackpotBucketLib.unpackWinningTraits(...)` without intermediate named local; same helper, same args | Cross-path aspect; Phase 231 EBD-02 covers the rewrite's internal correctness separately per D-09 | N |
| IM-14 / IM-15 / IM-16 cross-phase traceability | 104b5d42 + 20a951df | (cross-file; see §2.3 rows) | SAFE | Self-call + delegatecall chains unchanged semantically by 104b5d42 (BAF sentinel is emit-side only; no helper invocation change); IM-16 is the NEW earlybird -> _rollWinningTraits chain | Cross-phase citation only; IM-14 / IM-15 do not invoke `_rollWinningTraits` | N |
| D-12 post-hardening cross-path preservation | c2e5e0a9 | (cross-file; see 230-02-DELTA-ADDENDUM.md c2e5e0a9 row table) | SAFE | 16 JackpotModule hardening sites (277, 443, 508, 522, 544, 594, 596, 607-609, 874, 937, 1134, 1238, 1345, 1681-1683, 1741, 1798-1800, 1808) modify MAIN-trait + carryover + per-bucket decorrelation; none touches `_rollWinningTraits` body or the three bonus-trait consumer calls at 507/593/677; adjacent sites at 508/522 (next to 507), 594 (next to 593), 1681 (next to 1679) modify DIFFERENT logic at DIFFERENT keccak levels | Cross-path bonus-trait agreement preserved | N |
| 104b5d42 non-overlap | 104b5d42 | contracts/modules/DegenerusGameJackpotModule.sol:2002/2014/2034/2038 (BAF emit sites — NOT in JKP-03 scope) | SAFE | 104b5d42 surface is ORTHOGONAL to bonus-trait cross-path scope; only modifies BAF emit sites + event decls + BAF_TRAIT_SENTINEL constant (all in JKP-01 scope) | Scope clarification row | N |

[Rows above are the MINIMUM set. Replace `<line>` placeholders with real File:Line values. Additional rows for parallel sub-paths or additional attack vectors allowed.]

## High-Risk Patterns Analyzed

### Shared-Helper Determinism (`_rollWinningTraits`)
[Prose analysis: `_rollWinningTraits(uint256 randWord, bool isBonus) private view returns (uint32 packed)` at line 1865-1875. View function — reads state but does not modify. Body: (1) compute `r = isBonus ? keccak256(abi.encodePacked(randWord, BONUS_TRAITS_TAG)) : randWord` at line 1869-1871 — BONUS_TRAITS_TAG is compile-time constant at line 171 (`keccak256("BONUS_TRAITS")`); (2) `uint8[4] memory traits = JackpotBucketLib.getRandomTraits(r)` at line 1872 — pure library; (3) `_applyHeroOverride(traits, r)` at line 1873 — IMPURITY CANDIDATE; (4) `packed = JackpotBucketLib.packWinningTraits(traits)` at line 1874 — pure library. Cross-path agreement requires step (3) to be deterministic given `(traits, r)` OR to read only frozen-at-VRF-request state. Inspect `_applyHeroOverride` body and classify.]

### Bonus vs Main Salt-Space Separation (Prior-Art Citation)
[Prose: Phase 231 EBD-02 audit (231-02-AUDIT.md) previously established that `BONUS_TRAITS_TAG = keccak256("BONUS_TRAITS")` salt gives preimage-resistant isolation between `isBonus=true` and `isBonus=false` output spaces for the same `randWord`. JKP-03 cites this as prior art per D-09 non-overlap. JKP-03's own focus is cross-path agreement, not salt-space separation.]

### Cross-Path Invocation-Shape Agreement
[Prose: all three consumer sites invoke `_rollWinningTraits(<vrf_word_variable>, true)` with identical second argument (`true`) and source the first argument from the daily VRF word (or the function-scope rngWord parameter that itself traces to the daily VRF word). No intermediate transformation. No consumer applies XOR / bit-rotation / addition to the packed return value before `JackpotBucketLib.unpackWinningTraits`. Record each consumer's call pattern verbatim.]

### `_applyHeroOverride` State-Reading Classification
[Prose analysis: identify every state read inside `_applyHeroOverride`. Classify as (a) pure-given-inputs (no state read), (b) frozen-at-VRF-request (hero-symbol ranking set during daily activity before rngGate fires), (c) mutable-during-window (would break cross-path agreement). Expected classification: (a) or (b). Phase 231-02 presumably established this as part of the salt-space isolation audit per D-09 — cite 231-02-AUDIT.md section on `_rollWinningTraits` re-verification if applicable.]

### Post-Hardening Cross-Path Preservation (D-12)
[Prose: 230-02-DELTA-ADDENDUM.md c2e5e0a9 16-site JackpotModule hardening at lines 277 / 443 / 508 / 522 / 544 / 594 / 596 / 607-609 / 874 / 937 / 1134 / 1238 / 1345 / 1681-1683 / 1741 / 1798-1800 / 1808. Adjacent to consumer sites: lines 508/522 next to 507; line 594 next to 593; line 1681 next to 1679. Verify each hardening site modifies a DIFFERENT keccak-mixing operation (main-trait decorrelation via `keccak256(abi.encode(randWord, lvl, COIN_JACKPOT_TAG))`, per-bucket iteration, carryover decorrelation, etc.) and does NOT modify the `_rollWinningTraits(rngWord, true)` bonus-trait call. Cross-path bonus-trait agreement is preserved.]

## Scope-guard Deferrals

[Any JKP-03-adjacent concern surfaced during this audit that is NOT anchored in `230-01-DELTA-MAP.md` §4 JKP-03 rows is recorded here per D-03. Phase 230 catalog is READ-only. Each entry: concern, why not in JKP-03 scope, recommended receiving phase. Potential deferrals:
- Line 1707 `_rollWinningTraits(saltedRng, true)` where `saltedRng != randWord` — NOT a cross-path divergence because the input VRF word itself differs; document as out-of-scope for the cross-path-same-VRF-word property but record the semantic context for Phase 236 REG-01 regression check.
- `_awardFarFutureCoinJackpot` and other non-daily-jackpot consumers — out of JKP-03 three-path scope.
- Trait-generation entropy sufficiency (separate from cross-path agreement) — Phase 235 RNG-01 / RNG-02 territory.
If none surface, state "None surfaced during this audit."]

## Downstream Hand-offs

[Concerns overlapping downstream-phase territory (per D-11 and general scope boundaries). Expected entries:
- **Phase 231 EBD-02 hand-off (COMPLETED 2026-04-17):** The `20a951df` rewrite's INTERNAL correctness (parity, salt isolation, lvl+1 queue, pool conservation) is Phase 231 EBD-02 scope per D-09. Phase 231-02-AUDIT.md verdicts (6 PASS) are prior-art evidence for JKP-03's cross-path agreement; specifically the BONUS_TRAITS_TAG salt-isolation + byte-identical call-shape across bonus consumers at lines 1679 + 1705.
- **Phase 233 JKP-01 hand-off:** 104b5d42 BAF sentinel surface is orthogonal; only a scope-clarification row in this plan's per-function verdict table.
- **Phase 233 JKP-02 hand-off:** The rngWord arrives at all three consumer sites via the entropy-passthrough boundary audited by JKP-02. JKP-03 assumes rngWord arrives unchanged (per JKP-02's D-05 forward-trace proof). No cross-plan dependency beyond citation.
- **Phase 235 RNG-01 hand-off:** Backward-trace from `_rollWinningTraits(rngWord, true)` consumer back to VRF fulfillment — Phase 235 RNG-01 owns. JKP-03 proves helper determinism + cross-path agreement; Phase 235 RNG-01 proves VRF word was unknown at input commitment time for the bonus-trait consumer.
- **Phase 235 RNG-02 hand-off:** Commitment-window enumeration for bonus-trait consumer state reads (e.g. `_applyHeroOverride` state classification) — Phase 235 RNG-02 owns milestone-wide enumeration. JKP-03 records the passthrough-specific classification of `_applyHeroOverride` state reads; Phase 235 RNG-02 extends to milestone-wide.
- **Phase 235 CONS-01 hand-off:** Pool-conservation impact of the earlybird rewrite — Phase 231-02-AUDIT.md already routes to Phase 235 CONS-01 per its Downstream Hand-offs; JKP-03 does NOT duplicate.
- **Phase 236 FIND-01 hand-off:** Every VULNERABLE / DEFERRED row + every SAFE-INFO Finding Candidate: Y row becomes a finding-candidate anchor. Expected: zero or minimal candidate findings given cross-path determinism is satisfied by construction.
- **Phase 236 REG-01 hand-off:** v26.0 Phase 218 bonus-trait salt-isolation conclusions regression-check during Phase 236 REG-01.
Any additional hand-offs surfaced during audit recorded here.]
```

Every verdict row MUST:
- Use exactly the locked column set per D-02 (secondary table) + Finding Candidate column per 232-NN-AUDIT.md precedent: `Function | Commit SHA | File:Line | Verdict | Evidence | Notes | Finding Candidate`
- Primary cross-path derivation table uses D-08-specified schema: `Path | File:Line | Helper | Salt Arguments | Flag | Verdict`
- Cite the 7-char SHA(s) (`104b5d42` AND/OR `20a951df` per D-08 shared-SHA directive, `c2e5e0a9` for D-12 addendum rows)
- Use a real File:Line anchor from HEAD source (never a placeholder like `<line>`)
- Use verdict vocabulary strictly from `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (per D-02)
- Never reference F-29-NN IDs (per D-11)
- Carry a `Y` or `N` in Finding Candidate column

Read contract sources ONLY from `contracts/` directory at HEAD (per user memory `feedback_contract_locations.md` — never read stale copies). Use `git show 20a951df^:contracts/...` / `git show 104b5d42^:contracts/...` for pre-fix comparisons (git-tree reads of the same repo at prior SHAs — NOT stale copies).

READ-only constraint (v29.0 milestone rule): Zero writes to `contracts/` or `test/` permitted. Only writes allowed during task execution are `.planning/phases/233-jackpot-baf-entropy-audit/233-03-AUDIT.md` itself. `git status --porcelain contracts/ test/` MUST be empty before and after task execution.
  </action>
  <verify>
    <automated>test -f .planning/phases/233-jackpot-baf-entropy-audit/233-03-AUDIT.md && grep -c "20a951df" .planning/phases/233-jackpot-baf-entropy-audit/233-03-AUDIT.md && grep -c "104b5d42" .planning/phases/233-jackpot-baf-entropy-audit/233-03-AUDIT.md && test -z "$(git status --porcelain contracts/ test/)"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/233-jackpot-baf-entropy-audit/233-03-AUDIT.md` exists
    - File contains the literal header strings: `## Cross-Path Derivation Table (Primary — Per D-08 Column Schema)`, `## Per-Function Verdict Table (Secondary — Per D-02 Standard Schema)`, `## D-09 Non-Overlap Disclosure with Phase 231 EBD-02`, `## Findings-Candidate Block`, `## Scope-guard Deferrals`, `## Downstream Hand-offs`
    - Cross-Path Derivation Table header row is exactly `| Path | File:Line | Helper | Salt Arguments | Flag | Verdict |` (per D-08)
    - Per-Function Verdict Table header row is exactly `| Function | Commit SHA | File:Line | Verdict | Evidence | Notes | Finding Candidate |` (per D-02 + 232-precedent)
    - Cross-Path Derivation Table has at least 3 rows (one per conceptual path: purchase-phase, jackpot-phase, earlybird) + allowance for additional sub-path rows
    - Per-Function Verdict Table has at least 11 rows (minimum from template above: _rollWinningTraits, _applyHeroOverride, unpackWinningTraits, getRandomTraits, packWinningTraits, BONUS_TRAITS_TAG constant, 3 consumer sites at 507/593/677, IM-14/15/16 row, D-12 post-hardening row, 104b5d42 non-overlap row)
    - Every row in BOTH tables cites at least one 7-char SHA from {`104b5d42`, `20a951df`, `c2e5e0a9`} with at least one row citing each (minimum coverage: at least 1 row for 104b5d42, at least 3 rows for 20a951df — consumer #3 + IM-16 + non-overlap — and at least 1 row for c2e5e0a9 D-12 integration)
    - Every row cites a File:Line anchor starting with `contracts/modules/DegenerusGameJackpotModule.sol:` or `contracts/libraries/JackpotBucketLib.sol:` (or flagged as "cross-file" for IM chain rows)
    - Every row's verdict is exactly one of the strings `SAFE`, `SAFE-INFO`, `VULNERABLE`, or `DEFERRED` (per D-02)
    - Every row's Finding Candidate cell is exactly `Y` or `N`
    - `grep -cE "SAFE|SAFE-INFO|VULNERABLE|DEFERRED" .planning/phases/233-jackpot-baf-entropy-audit/233-03-AUDIT.md` returns at least 14 (3 Cross-Path + 11 Per-Function minimum)
    - `grep -c "20a951df" .planning/phases/233-jackpot-baf-entropy-audit/233-03-AUDIT.md` returns at least 3
    - `grep -c "104b5d42" .planning/phases/233-jackpot-baf-entropy-audit/233-03-AUDIT.md` returns at least 1
    - `grep -c "c2e5e0a9" .planning/phases/233-jackpot-baf-entropy-audit/233-03-AUDIT.md` returns at least 1
    - The string `F-29-NN` does NOT appear in the file (per D-11)
    - The string `F-29-` does NOT appear in any form in the file (Phase 236 owns ID assignment per D-11)
    - D-09 Non-Overlap Disclosure paragraph is explicitly present (not just a hand-off entry) — literal string "Phase 231 EBD-02" appears at least twice in the paragraph + in Downstream Hand-offs
    - D-12 230-02-DELTA-ADDENDUM.md integration row is present in the Per-Function Verdict Table (confirming cross-path agreement holds post-hardening; c2e5e0a9 adjacent sites at 508/522/594/1681 do NOT touch bonus-trait formulation)
    - High-Risk Patterns Analyzed section contains at least five subsections: `Shared-Helper Determinism (_rollWinningTraits)`, `Bonus vs Main Salt-Space Separation (Prior-Art Citation)`, `Cross-Path Invocation-Shape Agreement`, `_applyHeroOverride State-Reading Classification`, `Post-Hardening Cross-Path Preservation (D-12)`
    - Downstream Hand-offs subsection explicitly names `Phase 231 EBD-02` (prior-art citation) and `Phase 236 FIND-01` (per D-11)
    - `_applyHeroOverride` is explicitly inspected and its state-reading behavior classified as (a) pure-given-inputs, (b) frozen-at-VRF-request, or (c) mutable-during-window (with VULNERABLE Finding Candidate: Y if (c))
    - No placeholder line numbers like `:<line>` remain in the final file — every File:Line is a real integer or integer range (exception allowed for "cross-file" annotations on IM-chain rows where no single File:Line applies)
    - No contract source content read from outside `contracts/` directory (per user memory `feedback_contract_locations.md`)
    - `git status --porcelain contracts/ test/` returns empty output both before and after task execution
  </acceptance_criteria>
  <deliverable>
`233-03-AUDIT.md` with Cross-Path Derivation Table (primary, D-08 schema) listing the three bonus-trait consumer sites (purchase-phase at 507, jackpot-phase at 593, earlybird at 677) proving identical helper + args + flag. Per-Function Verdict Table (secondary, D-02 schema) covering `_rollWinningTraits`, `_applyHeroOverride`, three JackpotBucketLib library functions, BONUS_TRAITS_TAG constant, three consumer sites, IM-14/15/16 cross-phase traceability row, D-12 post-hardening preservation row, 104b5d42 non-overlap row (minimum 11 rows). D-09 Non-Overlap Disclosure paragraph explicitly disclosing Phase 231 EBD-02 vs Phase 233 JKP-03 different aspects same SHA. D-12 230-02-DELTA-ADDENDUM.md integration row confirming cross-path agreement holds post-hardening. `_applyHeroOverride` state-reading behavior explicitly classified. Findings-Candidate / Scope-guard Deferrals / Downstream Hand-offs subsections with Phase 231 EBD-02 + Phase 236 FIND-01 explicit routing. Zero F-29-NN emissions. Zero contracts/ or test/ writes.
  </deliverable>
  <done>`233-03-AUDIT.md` exists with the full structure above; Cross-Path Derivation Table uses D-08 schema; Per-Function Verdict Table uses D-02 + 232-precedent schema; every row cites a 7-char SHA and a real File:Line in `contracts/`; verdicts use `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` vocabulary; D-08 cross-path property explicitly tested (same helper, same args, same flag, no intermediate transformation across three consumer sites); D-09 Non-Overlap Disclosure paragraph explicitly present; D-12 post-hardening cross-path preservation verified; `_applyHeroOverride` state-reading classified; Scope-guard Deferrals + Downstream Hand-offs subsections explicitly name Phase 231 EBD-02 + Phase 236 FIND-01; zero F-29-NN IDs emitted; zero contracts/ or test/ writes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-review checkpoint — present 233-03-AUDIT.md to orchestrator before commit</name>
  <what-built>`233-03-AUDIT.md` with Cross-Path Derivation Table (D-08 schema) + Per-Function Verdict Table (D-02 schema) + D-09 Non-Overlap Disclosure paragraph + D-12 post-hardening integration + subsections described in Task 1. No contracts/ or test/ modifications.</what-built>
  <how-to-verify>
    1. `cat .planning/phases/233-jackpot-baf-entropy-audit/233-03-AUDIT.md` — confirm full artifact structure
    2. Verify Cross-Path Derivation Table uses D-08 columns `Path | File:Line | Helper | Salt Arguments | Flag | Verdict` with at least 3 rows for the three conceptual paths
    3. Verify Per-Function Verdict Table uses `Function | Commit SHA | File:Line | Verdict | Evidence | Notes | Finding Candidate` with at least 11 rows
    4. Verify every verdict row cites at least one of `104b5d42` / `20a951df` / `c2e5e0a9` (7-char SHA) and a real File:Line in `contracts/`
    5. Verify verdicts use only `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`
    6. Verify D-09 Non-Overlap Disclosure paragraph is explicitly present (literal "Phase 231 EBD-02" string at least twice in the paragraph)
    7. Verify D-12 230-02-DELTA-ADDENDUM.md integration row confirms cross-path agreement holds post-hardening (c2e5e0a9 adjacent sites at 508/522/594/1681 do NOT touch bonus-trait formulation)
    8. Verify `_applyHeroOverride` state-reading behavior is explicitly classified (pure-given-inputs / frozen-at-VRF-request / mutable-during-window)
    9. Verify zero F-29-NN / F-29- strings anywhere in the file (per D-11)
    10. `git status --porcelain contracts/ test/` — must be empty
    11. Verify `.planning/phases/233-jackpot-baf-entropy-audit/233-03-AUDIT.md` is the ONLY new file
  </how-to-verify>
  <resume-signal>Type "approved" (then Task 3 commits the AUDIT) or describe issues / required revisions.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit approved 233-03-AUDIT.md</name>
  <files>.planning/phases/233-jackpot-baf-entropy-audit/233-03-AUDIT.md</files>
  <read_first>
    - .planning/phases/233-jackpot-baf-entropy-audit/233-03-AUDIT.md (human-reviewed and approved in Task 2)
  </read_first>
  <action>
Stage and commit the approved `233-03-AUDIT.md` only. Commit message body explains it's a READ-only audit artifact per v29.0 milestone rule; no `contracts/` or `test/` changes in this commit.

```
git add .planning/phases/233-jackpot-baf-entropy-audit/233-03-AUDIT.md
git commit -m "docs(233-03): JKP-03 cross-path bonusTraitsPacked consistency adversarial audit

Cross-path derivation table (D-08 schema) + per-function verdict table
(D-02 schema) proving identical 4-trait set derivation for same VRF word
across three consumer paths:

- Purchase-phase: payDailyJackpot bonus-trait roll at line 507
- Jackpot-phase: payDailyJackpotCoinAndTickets bonus-trait roll at line 593
- Earlybird: _runEarlyBirdLootboxJackpot bonus-trait roll at line 677 (NEW
  by 20a951df)

All three invoke _rollWinningTraits(rngWord, true) with identical helper +
args + flag. BONUS_TRAITS_TAG = keccak256('BONUS_TRAITS') at line 171 gives
cryptographic domain separation. Shared helper at line 1865-1875 is
deterministic-pure modulo _applyHeroOverride state reads; state-reading
behavior classified (expected: frozen-at-VRF-request per Phase 231-02
prior art).

D-09 non-overlap disclosure: commit 20a951df is PRIMARILY owned by Phase 231
EBD-02 which audits rewrite INTERNAL correctness (parity, salt isolation,
lvl+1 queue, pool conservation). Phase 233 JKP-03 audits CROSS-PATH
AGREEMENT. Different aspects, shared SHA, non-overlapping per ROADMAP.
Phase 231 EBD-02 completed 2026-04-17 with 6 PASS verdicts; JKP-03 cites
as prior art for salt-isolation + byte-identical call-shape invariant at
lines 1679/1705.

D-12 230-02-DELTA-ADDENDUM.md integration: c2e5e0a9 16 JackpotModule
hardening sites (adjacent to consumers at lines 508/522/594/1681 near
507/593/1679) modify MAIN-trait + carryover + per-bucket decorrelation;
NONE touches _rollWinningTraits body or the three bonus-trait consumer
calls. Cross-path agreement preserved post-hardening.

104b5d42 non-overlap: BAF sentinel surface (lines 2002/2014/2034/2038 +
event decls + BAF_TRAIT_SENTINEL constant) is orthogonal to bonus-trait
cross-path scope.

Zero F-29-NN emissions (Phase 236 FIND-01 owns ID assignment per CONTEXT
D-11). Zero contracts/ or test/ writes (READ-only v29.0 milestone rule)."
```

Do NOT run `git push`. Verify `git log -1` shows the new commit and `git status --porcelain contracts/ test/` is empty.
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -qE "233-03.*JKP-03.*cross-path" && test -z "$(git status --porcelain contracts/ test/)"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one new commit added via `git log -1`
    - Commit message subject starts with `docs(233-03):` and contains `JKP-03` + `cross-path` + `bonusTraitsPacked` + `consistency`
    - Commit touches exactly `.planning/phases/233-jackpot-baf-entropy-audit/233-03-AUDIT.md` (no contracts/ or test/ files)
    - `git status --porcelain contracts/ test/` returns empty
    - Commit not pushed — branch state local to repository
  </acceptance_criteria>
  <deliverable>
Approved `233-03-AUDIT.md` committed with a `docs(233-03): JKP-03 ...` message; no code modifications; no push.
  </deliverable>
  <done>Commit exists with correct message format; repository `contracts/` and `test/` remain unmodified; commit is local only (not pushed).</done>
</task>

</tasks>

<verification>
- `233-03-AUDIT.md` exists with Cross-Path Derivation Table (primary, D-08 schema `Path | File:Line | Helper | Salt Arguments | Flag | Verdict`) + Per-Function Verdict Table (secondary, D-02 schema with Finding Candidate column)
- Cross-Path Derivation Table: at least 3 rows for the three conceptual paths (purchase-phase at 507, jackpot-phase at 593, earlybird at 677)
- Per-Function Verdict Table: at least 11 rows covering `_rollWinningTraits`, `_applyHeroOverride`, three JackpotBucketLib library functions, BONUS_TRAITS_TAG constant, three consumer sites, IM-14/15/16 row, D-12 post-hardening row, 104b5d42 non-overlap row
- Every verdict row cites at least one 7-char SHA from {104b5d42, 20a951df, c2e5e0a9}
- Every File:Line anchor resolves into `contracts/` at HEAD
- Verdicts exclusively use `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (per D-02)
- Every row carries `Y` or `N` in Finding Candidate column
- D-09 Non-Overlap Disclosure with Phase 231 EBD-02 paragraph EXPLICITLY present (literal "Phase 231 EBD-02" at least twice)
- D-12 230-02-DELTA-ADDENDUM.md integration row confirming cross-path agreement holds post-hardening
- `_applyHeroOverride` state-reading behavior explicitly classified
- Findings-Candidate / Scope-guard Deferrals / Downstream Hand-offs subsections present
- Hand-offs explicitly name Phase 231 EBD-02 + Phase 236 FIND-01
- Zero `F-29-NN` references anywhere in the file (per D-11)
- Zero writes to `contracts/` or `test/` during plan execution
- Approved AUDIT.md committed with message `docs(233-03): JKP-03 cross-path bonusTraitsPacked consistency adversarial audit`
</verification>

<success_criteria>
Every bonus-trait consumer site in the three conceptual paths (purchase-phase at line 507, jackpot-phase at line 593, earlybird at line 677) has a verdict in the Cross-Path Derivation Table against the D-08 cross-path property (identical helper + args + flag). The shared helper `_rollWinningTraits` at line 1865-1875 + its sub-routine `_applyHeroOverride` + the JackpotBucketLib library functions have verdicts in the Per-Function Verdict Table against determinism. D-09 non-overlap with Phase 231 EBD-02 is explicitly disclosed (different aspects, same SHA). D-12 post-hardening cross-path preservation is verified (c2e5e0a9 adjacent sites do not touch bonus-trait formulation). Every verdict cites a 7-char SHA + File:Line into `contracts/`. Phase 230 delta-map and addendum are not edited; any out-of-scope discovery is recorded in Scope-guard Deferrals per D-03. READ-only milestone rule honored: zero writes to `contracts/` or `test/`.
</success_criteria>

<output>
After completion, create `.planning/phases/233-jackpot-baf-entropy-audit/233-03-SUMMARY.md`
</output>
</content>
