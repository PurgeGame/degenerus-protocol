---
phase: 84-prize-pool-flow-currentprizepool-deep-dive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v4.0-prize-pool-flow.md
autonomous: true
requirements: [PPF-01, PPF-02, PPF-03, PPF-04, PPF-05, PPF-06]

must_haves:
  truths:
    - "Every write site for currentPrizePool is enumerated with file:line"
    - "prizePoolsPacked bit layout (lower 128 = next, upper 128 = future) is documented with accessor functions"
    - "All 13 prizePoolFrozen check sites are classified as redirect or revert"
    - "consolidatePrizePools 5-step flow is documented with file:line for each step"
    - "VRF-dependent readers of currentPrizePool are identified and safety verdict confirmed"
    - "All discrepancies between current code and v3.5/v3.8 audit prose are tagged"
  artifacts:
    - path: "audit/v4.0-prize-pool-flow.md"
      provides: "Comprehensive prize pool flow audit document"
      contains: "## Section 1"
  key_links:
    - from: "contracts/modules/DegenerusGameJackpotModule.sol"
      to: "currentPrizePool"
      via: "consolidatePrizePools writes (JM:889, JM:900) and payDailyJackpot writes (JM:403, JM:522)"
      pattern: "currentPrizePool"
    - from: "contracts/storage/DegenerusGameStorage.sol"
      to: "prizePoolFrozen"
      via: "_swapAndFreeze sets (GS:722), _unfreezePool clears (GS:735)"
      pattern: "prizePoolFrozen"
    - from: "contracts/modules/DegenerusGameAdvanceModule.sol"
      to: "consolidatePrizePools"
      via: "delegatecall wrapper _consolidatePrizePools (AM:541)"
      pattern: "_consolidatePrizePools"
---

<objective>
Produce an exhaustive audit document tracing the complete prize pool flow: all readers and writers of currentPrizePool with file:line citations, the packed prizePoolsPacked storage layout and accessor functions, the full prizePoolFrozen freeze/unfreeze lifecycle with all 13 check sites classified, the 5-step consolidatePrizePools mechanics, VRF-dependent readers with safety verdicts, and all discrepancies between current code and prior audit documentation (v3.5, v3.8).

Purpose: Independent re-audit of prize pool mechanics as part of the v4.0 Ticket Lifecycle & RNG-Dependent Variable Re-Audit milestone, treating all prior audit prose as unverified.
Output: audit/v4.0-prize-pool-flow.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-prize-pool-flow-currentprizepool-deep-dive/84-RESEARCH.md

<interfaces>
<!-- Key contract locations for prize pool tracing. All line numbers from research -- executor MUST verify against actual code. -->

Storage declarations (DegenerusGameStorage.sol):
- GS:339 prizePoolFrozen (bool) -- EVM Slot 1, byte [26:27]
- GS:348 currentPrizePool (uint256) -- EVM Slot 2 (full word)
- GS:356 prizePoolsPacked (uint256) -- EVM Slot 3 (full word)
  - [0:128] nextPrizePool (uint128)
  - [128:256] futurePrizePool (uint128)
- GS:449 prizePoolPendingPacked (uint256) -- slot TBD (verify with forge inspect)
  - [0:128] nextPrizePoolPending (uint128)
  - [128:256] futurePrizePoolPending (uint128)

Packed pool helpers (GS:660-758):
- _setPrizePools(uint128 next, uint128 future) at GS:660
- _getPrizePools() returns (uint128 next, uint128 future) at GS:664
- _setPendingPools(uint128 next, uint128 future) at GS:670
- _getPendingPools() returns (uint128 next, uint128 future) at GS:674
- _getNextPrizePool() at GS:743
- _setNextPrizePool(uint128) at GS:749
- _getFuturePrizePool() at GS:755

Freeze/unfreeze (GS:719-736):
- _swapAndFreeze(uint24) at GS:719 -- sets prizePoolFrozen, zeros pending
- _unfreezePool() at GS:729 -- merges pending into live, clears frozen

currentPrizePool write sites (6 total):
- JM:889 currentPrizePool += _getNextPrizePool() (consolidation merge)
- JM:900 currentPrizePool += moveWei (consolidation x00 future->current)
- JM:403 currentPrizePool -= dailyLootboxBudget (payDailyJackpot lootbox deduction)
- JM:522 currentPrizePool -= paidDailyEth (payDailyJackpot ETH payout deduction)
- GM:118 currentPrizePool = 0 (gameOver no funds)
- GM:130 currentPrizePool = 0 (gameOver after final jackpot)

currentPrizePool read sites (5 total):
- JM:365 poolSnapshot = currentPrizePool (daily budget, VRF-dependent)
- JM:905 _creditDgnrsCoinflip(currentPrizePool) (consolidation, VRF-dependent)
- JM:915 obligations = currentPrizePool + ... (yield surplus, VRF-dependent)
- DG:2141 currentPrizePoolView() (view only)
- DG:2168 obligations = currentPrizePool + ... (yieldPoolView, view only)

prizePoolFrozen check sites (13):
- DG:396, DG:1750, DG:2840 (redirect)
- MM:779 (redirect)
- WM:298, WM:434, WM:551 (redirect)
- DegeneretteM:558 (redirect), DegeneretteM:685 (REVERT)
- DM:321, DM:834 (REVERT)
- GS:721 (set), GS:730 (clear)

consolidatePrizePools: JM:879-908 (5 steps)
_consolidatePrizePools wrapper: AM:541-551 (delegatecall)
_applyTimeBasedFutureTake: AM:1029-1101 (pre-consolidation)
_drawDownFuturePrizePool: AM:1106-1118 (post-consolidation)
_unfreezePool calls: AM:246, AM:293, AM:369

Prior audit docs to cross-reference:
- audit/v3.8-commitment-window-inventory.md (Sections 1.10, 1.11, Section 4)
- audit/v3.5-comment-findings-54-05-game-modules.md (CMT-V35 line 181, 176)
- Known line drift: 3-line shift in AdvanceModule (Phase 81 finding)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate currentPrizePool Storage Slot, All Writers, and All Readers (PPF-01)</name>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol (GS:60-100 layout comment, GS:348 declaration)
    - contracts/modules/DegenerusGameJackpotModule.sol (JM:879-908 consolidatePrizePools, JM:362-522 payDailyJackpot)
    - contracts/modules/DegenerusGameGameOverModule.sol (GM:100-140 terminal zeroing)
    - contracts/DegenerusGame.sol (DG:2130-2175 view functions)
  </read_first>
  <files>audit/v4.0-prize-pool-flow.md</files>
  <action>
Create audit/v4.0-prize-pool-flow.md with the document header and Section 1.

**Document header:**
- Title: "v4.0 Prize Pool Flow & currentPrizePool Deep Dive"
- Date, milestone, phase, methodology (same format as audit/v4.0-ticket-creation-queue-mechanics.md)
- Executive Summary placeholder (to be filled in Task 3)
- Finding Counts table placeholder (to be filled in Task 3)

**Section 1: currentPrizePool Storage and Access (PPF-01)**

1a. Storage slot confirmation:
- Read GS:348 to confirm `currentPrizePool` declaration
- Run `forge inspect DegenerusGame storage-layout` and grep for `currentPrizePool` to confirm EVM slot number (expected: slot 2)
- Also confirm `prizePoolsPacked` slot (expected: 3) and `prizePoolPendingPacked` slot (unknown -- use forge inspect result)
- Document the EVM slot numbers with both source declaration line and forge inspect confirmation

1b. Write site enumeration (expect 6):
- JM:889: `currentPrizePool += _getNextPrizePool()` -- consolidation merge next into current
- JM:900: `currentPrizePool += moveWei` -- consolidation x00 future-to-current transfer
- JM:403: `currentPrizePool -= dailyLootboxBudget` -- payDailyJackpot lootbox ticket budget deduction
- JM:522: `currentPrizePool -= paidDailyEth` -- payDailyJackpot ETH payout deduction
- GM:118: `currentPrizePool = 0` -- gameOver when no available funds
- GM:130: `currentPrizePool = 0` -- gameOver after final jackpot paid

For each write site: read the actual Solidity at that line, document the exact statement, the enclosing function name, the calling context (who calls this function and how -- direct call vs delegatecall), and whether it runs during VRF commitment window.

Verify completeness: grep `currentPrizePool` across all contracts/ files with `=` or `+=` or `-=` operators. If any write site is NOT in the list of 6, document it as a [NEW FINDING].

1c. Read site enumeration (expect 5):
- JM:365: `poolSnapshot = currentPrizePool` in payDailyJackpot
- JM:905: `_creditDgnrsCoinflip(currentPrizePool)` in consolidatePrizePools
- JM:915: `obligations = currentPrizePool + ...` in _distributeYieldSurplus
- DG:2141: `currentPrizePoolView()` external view
- DG:2168: `obligations = currentPrizePool + ...` in yieldPoolView

For each read site: document the exact Solidity, enclosing function, whether VRF-dependent (YES if runs during advanceGame jackpot phase, NO if view-only or post-VRF).

Verify completeness: grep `currentPrizePool` across all contracts/ files, filtering out write sites. If any read site is NOT in the list of 5, document it.

Output: Section 1 with tables for write sites and read sites, each row having columns: #, Location (file:line), Exact Statement, Function, Context, VRF-Dependent?.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -rn "currentPrizePool" contracts/ | grep -v "//" | grep -c "[+\-]=" && echo "--- write sites above ---" && forge inspect DegenerusGame storage-layout 2>/dev/null | grep -i "prizePool" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - audit/v4.0-prize-pool-flow.md exists and contains "## Section 1"
    - Section 1 contains a "Write Sites" table with exactly 6 rows (JM:889, JM:900, JM:403, JM:522, GM:118, GM:130) or documents additional sites as [NEW FINDING]
    - Section 1 contains a "Read Sites" table with at least 5 rows
    - Each table row contains a file:line citation, exact Solidity statement, and VRF-Dependent column
    - Document contains forge inspect output confirming currentPrizePool slot number
  </acceptance_criteria>
  <done>currentPrizePool storage slot confirmed by forge inspect, all 6 writers and all readers enumerated with file:line citations and VRF-dependency classification</done>
</task>

<task type="auto">
  <name>Task 2: Document prizePoolsPacked Layout, Freeze Lifecycle, and Consolidation Mechanics (PPF-02, PPF-03, PPF-04)</name>
  <read_first>
    - audit/v4.0-prize-pool-flow.md (current state from Task 1)
    - contracts/storage/DegenerusGameStorage.sol (GS:356 declaration, GS:660-678 packed helpers, GS:719-736 freeze/unfreeze, GS:743-758 single-component accessors)
    - contracts/modules/DegenerusGameJackpotModule.sol (JM:879-908 consolidatePrizePools, JM:1281-1296 _futureKeepBps, JM:2346 _creditDgnrsCoinflip, JM:910-944 _distributeYieldSurplus, JM:2656-2669 _dailyCurrentPoolBps)
    - contracts/modules/DegenerusGameAdvanceModule.sol (AM:233 _swapAndFreeze call, AM:246/293/369 _unfreezePool calls, AM:313-338 consolidation call chain, AM:1029-1101 _applyTimeBasedFutureTake, AM:1106-1118 _drawDownFuturePrizePool, AM:541-551 _consolidatePrizePools wrapper)
    - contracts/DegenerusGame.sol (DG:186 PURCHASE_TO_FUTURE_BPS, DG:392-408 purchase revenue routing)
    - contracts/modules/DegenerusGameMintModule.sol (MM:779 freeze check)
    - contracts/modules/DegenerusGameWhaleModule.sol (WM:293-298 whale split, WM:434 lazy freeze, WM:547-551 deity split)
    - contracts/modules/DegenerusGameDegeneretteModule.sol (DegeneretteM:558 bet redirect, DegeneretteM:685 payout revert)
    - contracts/modules/DegenerusGameDecimatorModule.sol (DM:321 claim revert, DM:834 terminal revert)
  </read_first>
  <files>audit/v4.0-prize-pool-flow.md</files>
  <action>
Append Sections 2, 3, and 4 to audit/v4.0-prize-pool-flow.md.

**Section 2: prizePoolsPacked Storage Layout (PPF-02)**

2a. Storage declaration and slot:
- Document prizePoolsPacked at GS:356, EVM slot (from forge inspect in Task 1)
- Document prizePoolPendingPacked at GS:449, EVM slot (from forge inspect in Task 1)
- Both are uint256 with identical packing: lower 128 bits = next component, upper 128 bits = future component

2b. Accessor functions with exact Solidity:
Read and document each accessor function body from GS:660-678 and GS:743-758:
- `_setPrizePools(uint128 next, uint128 future)` at GS:660 -- packs as `uint256(future) << 128 | uint256(next)`
- `_getPrizePools()` at GS:664 -- unpacks with `uint128(packed)` and `uint128(packed >> 128)`
- `_setPendingPools(uint128 next, uint128 future)` at GS:670
- `_getPendingPools()` at GS:674
- `_getNextPrizePool()` at GS:743 -- `uint128(prizePoolsPacked)`
- `_setNextPrizePool(uint128 v)` at GS:749
- `_getFuturePrizePool()` at GS:755 -- `uint128(prizePoolsPacked >> 128)`

2c. BPS allocation table:
- PURCHASE_TO_FUTURE_BPS = 1000 (10% to future) at DG:186
- Whale/deity level 0: 30% next / 70% future (WM:293, WM:547)
- Whale/deity level 1+: 5% next / 95% future (WM:295, WM:549)
- NEXT_TO_FUTURE_BPS_FAST = 3000 (AM:99), NEXT_TO_FUTURE_BPS_MIN = 1300 (AM:100), NEXT_TO_FUTURE_BPS_X9_BONUS = 200 (AM:102), NEXT_TO_FUTURE_BPS_MAX = 8000 (AM:109)
- DAILY_CURRENT_BPS_MIN = 600 (JM:146), DAILY_CURRENT_BPS_MAX = 1400 (JM:147), DAILY_REWARD_JACKPOT_LOOTBOX_BPS = 5000 (JM:176)

Read the actual constant declarations at each cited line and confirm values. If any value differs from the above, document as [DISCREPANCY].

**Section 3: prizePoolFrozen Freeze/Unfreeze Lifecycle (PPF-03)**

3a. Freeze trigger:
- `_swapAndFreeze(purchaseLevel)` at GS:719-725
- Called at AM:233 during daily RNG request (advanceGame flow)
- Sets `prizePoolFrozen = true` at GS:722 and zeros `prizePoolPendingPacked` at GS:723
- No-op if already frozen (multi-day jackpot sequence)

3b. Unfreeze trigger:
- `_unfreezePool()` at GS:729-736
- Three call sites: AM:246 (phase transition complete), AM:293 (purchase phase daily), AM:369 (jackpot phase ended)
- Merges pending into live: `_setPrizePools(next + pNext, future + pFuture)` at GS:733
- Zeros pending: `prizePoolPendingPacked = 0` at GS:734
- Clears frozen: `prizePoolFrozen = false` at GS:735

Read the actual Solidity at each call site (AM:246, AM:293, AM:369) and document the enclosing function name and trigger condition.

3c. All 13 check sites classified:
For each of the 13 prizePoolFrozen check sites, read the actual code and classify as REDIRECT (routes ETH to pending accumulators) or REVERT (blocks the operation entirely):

REDIRECT sites (7):
1. DG:396 -- purchase revenue routing: if frozen, write to pending via _setPendingPools
2. DG:1750 -- sDGNRS lootbox: if frozen, redirect to pending future
3. DG:2840 -- receive() fallback: if frozen, redirect to pending future
4. MM:779 -- mint lootbox: if frozen, redirect split to pending
5. WM:298 -- whale bundle: if frozen, redirect split to pending
6. WM:434 -- lazy pass: if frozen, redirect split to pending
7. WM:551 -- deity pass: if frozen, redirect split to pending

REVERT sites (3):
8. DegeneretteM:685 -- degenerette payout: revert if frozen (reads futurePrizePool directly)
9. DM:321 -- decimator jackpot claim: revert if frozen
10. DM:834 -- terminal decimator claim: revert if frozen

SET/CLEAR sites (3):
11. GS:721 -- _swapAndFreeze: check before setting
12. GS:730 -- _unfreezePool: early return if not frozen
13. (verify if there is a 13th check site -- research says "13+" but only 12 numbered above)

Read EVERY check site and confirm the classification. If any site differs from the above classification, document as [DISCREPANCY]. If any prizePoolFrozen reference exists that is NOT in this list, document as [NEW FINDING].

Verify completeness: grep `prizePoolFrozen` across all contracts/ files.

**Section 4: Prize Pool Consolidation Mechanics (PPF-04)**

4a. Pre-consolidation steps (AdvanceModule):
- AM:314: `levelPrizePool[purchaseLevel] = _getNextPrizePool()` -- record level target
- AM:315: `_applyTimeBasedFutureTake(ts, purchaseLevel, rngWord)` -- time-based next-to-future skim
  - Read AM:1029-1101 and document: reads levelStartTime (slot 0), prizePoolsPacked; computes skim BPS based on elapsed time and level pattern; modifies prizePoolsPacked (next decreases, future increases); does NOT touch currentPrizePool
- AM:316: `_consolidatePrizePools(purchaseLevel, rngWord)` -- delegatecall to JM:879

4b. consolidatePrizePools (JM:879-908) -- document all 5 steps:
1. x00 yield dump (JM:881-886): if lvl % 100 == 0, move 50% of yieldAccumulator to futurePrizePool
2. Merge (JM:889-890): currentPrizePool += _getNextPrizePool(), then _setNextPrizePool(0)
3. x00 keep roll (JM:892-902): if lvl % 100 == 0, roll _futureKeepBps(rngWord) for 30-65% retention, move remainder from future to current
4. Credit coinflip (JM:905): _creditDgnrsCoinflip(currentPrizePool) -- mints BURNIE to sDGNRS coinflip
5. Yield surplus (JM:907): _distributeYieldSurplus(rngWord) -- 23% each to DGNRS/Vault claimable, 46% to yieldAccumulator

Read each line range and confirm the exact Solidity matches the described behavior. For step 3, also read _futureKeepBps at JM:1281-1296 and document the 5-dice roll mechanism (30-65% range).

4c. Post-consolidation (AM:327-338):
- jackpotPhaseFlag = true
- _drawDownFuturePrizePool(lvl) at AM:1106-1118: moves 15% of future to next on non-x00 levels
- Confirm drawdown runs in SAME transaction as consolidation
- Confirm drawdown does NOT modify currentPrizePool

4d. ETH flow diagram (ASCII art showing the four-pool architecture with all transition arrows):
futurePrizePool -> (consolidation x00) -> currentPrizePool
nextPrizePool -> (consolidation merge) -> currentPrizePool
currentPrizePool -> (payDailyJackpot) -> claimablePool
Purchase revenue -> (10/90 split) -> futurePrizePool + nextPrizePool
(when frozen) -> prizePoolPendingPacked -> (unfreeze) -> futurePrizePool + nextPrizePool
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -c "## Section [234]" audit/v4.0-prize-pool-flow.md && grep -c "prizePoolFrozen" audit/v4.0-prize-pool-flow.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v4.0-prize-pool-flow.md contains "## Section 2", "## Section 3", and "## Section 4"
    - Section 2 contains a table of accessor functions with file:line citations (at least 7 functions)
    - Section 2 contains a BPS allocation table with at least 8 constants, each with a file:line citation
    - Section 3 contains all prizePoolFrozen check sites classified as REDIRECT, REVERT, or SET/CLEAR
    - Section 3 contains the three _unfreezePool call sites (AM:246, AM:293, AM:369) with enclosing function names
    - Section 4 contains the 5-step consolidatePrizePools breakdown with file:line for each step
    - Section 4 contains _applyTimeBasedFutureTake documentation confirming it does NOT touch currentPrizePool
    - Section 4 contains _drawDownFuturePrizePool documentation confirming it does NOT modify currentPrizePool
  </acceptance_criteria>
  <done>prizePoolsPacked layout fully documented with accessors; all 13+ prizePoolFrozen check sites classified; consolidation 5-step flow documented with pre- and post-consolidation steps</done>
</task>

<task type="auto">
  <name>Task 3: VRF-Dependent Readers, Discrepancy Scan, and Final Document Assembly (PPF-05, PPF-06)</name>
  <read_first>
    - audit/v4.0-prize-pool-flow.md (current state from Tasks 1-2)
    - contracts/modules/DegenerusGameJackpotModule.sol (JM:362-376 daily budget, JM:879-908 consolidation, JM:2346 _creditDgnrsCoinflip)
    - contracts/DegenerusGame.sol (DG:2141 view, DG:2168 view)
    - audit/v3.8-commitment-window-inventory.md (Sections 1.10, 1.11, Section 4 verdicts)
    - audit/v3.5-comment-findings-54-05-game-modules.md (CMT-V35 lines 176, 181)
    - audit/v4.0-ticket-creation-queue-mechanics.md (for cross-reference format and methodology)
  </read_first>
  <files>audit/v4.0-prize-pool-flow.md</files>
  <action>
Append Sections 5 and 6, fill in the Executive Summary, and add the requirement verdict table to audit/v4.0-prize-pool-flow.md.

**Section 5: VRF-Dependent Readers of currentPrizePool (PPF-05)**

5a. Identify all readers from Section 1 that are VRF-dependent:
For each read site classified as VRF-Dependent=YES in Section 1, perform a backward trace:

Reader 1: JM:365 `poolSnapshot = currentPrizePool` in payDailyJackpot
- Who calls payDailyJackpot? Trace the call chain from advanceGame -> jackpot phase processing
- Does it run during VRF commitment window? No -- it runs AFTER rawFulfillRandomWords delivers the RNG word. The RNG word determines dailyBps via _dailyCurrentPoolBps.
- Can currentPrizePool be mutated between VRF request and fulfillment? No -- prizePoolFrozen is set at _swapAndFreeze (AM:233) BEFORE VRF request, and all purchase-time writers redirect to pending accumulators.
- Verdict: SAFE (freeze-gated; pool is immutable during commitment window)

Reader 2: JM:905 `_creditDgnrsCoinflip(currentPrizePool)` in consolidatePrizePools
- Runs during consolidation, which is AFTER VRF fulfillment (inside advanceGame after RNG word is delivered)
- currentPrizePool was just written at JM:889 (merge) in the same function
- Not readable during commitment window
- Verdict: SAFE (post-VRF; only accessible via advanceGame)

Reader 3: JM:915 `obligations = currentPrizePool + ...` in _distributeYieldSurplus
- Called from consolidatePrizePools step 5 (JM:907)
- Same timing as Reader 2 -- post-VRF, inside advanceGame
- Verdict: SAFE (post-VRF; only accessible via advanceGame)

Reader 4: DG:2141 currentPrizePoolView() -- view function, no on-chain effect
Reader 5: DG:2168 yieldPoolView() -- view function, no on-chain effect

5b. Backward trace from rawFulfillRandomWords:
- Read rawFulfillRandomWords in the VRF callback path and confirm it does NOT read currentPrizePool
- The VRF callback stores the random word and triggers advanceGame continuation, which then reads currentPrizePool
- The key safety property: the VRF callback itself does not use currentPrizePool to determine any outcome

5c. Summary table:
| Reader | Location | Timing | Mutable During Window? | Verdict |
for all 5 readers.

5d. Overall VRF Safety Verdict:
Confirm or refute v3.8 Section 4 verdict: "SAFE -- prizePoolFrozen = true. All purchase revenue is redirected to prizePoolPendingPacked. currentPrizePool is immutable during daily window."

**Section 6: Discrepancies and New Findings (PPF-06)**

6a. Cross-reference v3.8 commitment window inventory:
Read audit/v3.8-commitment-window-inventory.md Sections 1.10, 1.11, and Section 4. For each claim:

- Section 1.10 claim: consolidatePrizePools reads/writes yieldAccumulator (slot 100 R/W), currentPrizePool (slot 2 R/W), prizePoolsPacked (slot 3 R/W), claimableWinnings (W), claimablePool (R/W), autoRebuyState (R)
  -> Verify each slot number against forge inspect output from Task 1
  -> Verify each R/W classification against actual code

- Section 1.11 claim: _applyTimeBasedFutureTake reads levelStartTime (slot 0 offset 0), prizePoolsPacked (slot 3 R/W), levelPrizePool[lvl-1], yieldAccumulator
  -> Verify against actual code at AM:1029-1101

- Section 4 currentPrizePool verdict: "SAFE -- prizePoolFrozen = true..."
  -> Confirm or refute based on Section 5 findings

- Section 4 prizePoolsPacked verdict: "SAFE -- Freeze-gated..."
  -> Confirm or refute

- Section 4 prizePoolPendingPacked verdict: "SAFE -- not read by any VRF-dependent outcome"
  -> Confirm or refute

For each claim: document as CONFIRMED (with current code evidence) or [DISCREPANCY] (with what's different).

6b. Cross-reference v3.5 comment findings:
Read audit/v3.5-comment-findings-54-05-game-modules.md for:
- CMT-V35 line 181: "consolidatePrizePools NatSpec accurately describes pool merge and x00 keep roll"
  -> Read the NatSpec at JM:866-878 and confirm it still matches the 5-step implementation
- CMT-V35 line 176: "FUND ACCOUNTING description matches actual pool flow"
  -> Read the fund accounting description and confirm

6c. Known line drift:
Check all v3.8 AdvanceModule line references against current code. Known: 3-line shift from Phase 81 findings.
- v3.8: AM:230 (_swapAndFreeze call) -> current: AM:233
- Apply same shift to all AM references in v3.8 and verify

6d. Any NEW FINDINGs discovered during Tasks 1-2 (not in prior audits) should be collected here with severity assessment.

**Executive Summary (fill in at top of document):**
- Total finding counts by severity (HIGH/MEDIUM/LOW/INFO)
- Key conclusions (3-5 bullets)
- Format: same as audit/v4.0-ticket-creation-queue-mechanics.md

**Requirement Verdict Table (append at bottom):**
| Requirement | Description | Verdict | Evidence |
| PPF-01 | currentPrizePool storage slot + writers | VERIFIED/FAILED | Section 1 |
| PPF-02 | prizePoolsPacked layout | VERIFIED/FAILED | Section 2 |
| PPF-03 | prizePoolFrozen lifecycle | VERIFIED/FAILED | Section 3 |
| PPF-04 | Consolidation mechanics | VERIFIED/FAILED | Section 4 |
| PPF-05 | VRF-dependent readers | VERIFIED/FAILED | Section 5 |
| PPF-06 | Discrepancies tagged | VERIFIED/FAILED | Section 6 |
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -c "## Section [56]" audit/v4.0-prize-pool-flow.md && grep -c "\[DISCREPANCY\]\|\[NEW FINDING\]\|CONFIRMED" audit/v4.0-prize-pool-flow.md && grep -c "PPF-0[1-6]" audit/v4.0-prize-pool-flow.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v4.0-prize-pool-flow.md contains "## Section 5" and "## Section 6"
    - Section 5 contains a VRF safety table with verdict for each of the 5 reader sites
    - Section 5 contains backward trace from rawFulfillRandomWords confirming no currentPrizePool read
    - Section 6 contains cross-reference results for v3.8 Sections 1.10, 1.11, and Section 4 (at least 5 CONFIRMED or DISCREPANCY tags)
    - Section 6 contains cross-reference results for v3.5 CMT-V35 (at least 2 CONFIRMED or DISCREPANCY tags)
    - Document contains Executive Summary with finding counts table at the top
    - Document contains requirement verdict table at the bottom with all 6 PPF-XX requirements
    - Every PPF-XX requirement appears at least once with a VERIFIED or FAILED verdict
  </acceptance_criteria>
  <done>VRF-dependent readers documented with safety verdicts; all v3.5 and v3.8 cross-references checked and tagged as CONFIRMED or DISCREPANCY; executive summary and requirement verdicts complete; audit document is a single self-contained deliverable</done>
</task>

</tasks>

<verification>
- All 6 currentPrizePool write sites documented with file:line citations and exact Solidity
- All 5+ currentPrizePool read sites documented with VRF-dependency classification
- prizePoolsPacked bit layout documented with all 7+ accessor functions
- All 13+ prizePoolFrozen check sites classified as REDIRECT, REVERT, or SET/CLEAR
- consolidatePrizePools 5-step flow documented with pre- and post-consolidation steps
- _applyTimeBasedFutureTake confirmed as NOT touching currentPrizePool
- VRF safety verdict for currentPrizePool confirmed with backward trace
- All v3.5 and v3.8 prior audit claims cross-referenced
- All discrepancies tagged with [DISCREPANCY] or [NEW FINDING]
- All 6 requirements (PPF-01 through PPF-06) have VERIFIED or FAILED verdict
- forge inspect output used to confirm storage slot numbers
</verification>

<success_criteria>
1. Single audit document at audit/v4.0-prize-pool-flow.md with 6 sections covering all requirements
2. All 6 requirements (PPF-01 through PPF-06) verified with code evidence and file:line citations
3. All prior audit claims (v3.5, v3.8) independently verified and cross-referenced
4. Finding counts table populated with severity breakdown
5. No MEDIUM or higher findings overlooked (if any exist, they are tagged)
</success_criteria>

<output>
After completion, create `.planning/phases/84-prize-pool-flow-currentprizepool-deep-dive/84-01-SUMMARY.md`
</output>
