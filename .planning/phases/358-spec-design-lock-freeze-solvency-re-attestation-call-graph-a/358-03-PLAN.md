---
phase: 358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a
plan: 03
type: execute
wave: 3
depends_on: ["358-01", "358-02"]
files_modified:
  - .planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md
autonomous: true
requirements: [WWXRP-02, TDEC-02, TDEC-03, BURNIE-03, SALVAGE-02, CANCEL-02]
must_haves:
  truths:
    - "358-SPEC.md contains a '## Cross-Cutting RNG-Freeze Re-Attestation (paper)' section asserting RNG-freeze-intact across all 8 items (BATCH-01/02 BURNIE-accounting-only · WWXRP s==9-committed-counter · TDEC weight+bucket-precede-the-draw · BURNIE purchaseCoin-reads-no-rngWord · SALVAGE settled-prior-day-word-no-new-VRF · CANCEL no-rngWord · the UDVT abi.encodePacked uint32-cast byte-image preservation as the load-bearing item) each with anchor + the TST-361 SEC-01 proof obligation"
    - "358-SPEC.md contains a '## Cross-Cutting SOLVENCY Re-Attestation (paper)' section: 6 of 8 items OFF the ETH/claimablePool path (debit byte-unchanged), the TWO functional exceptions flagged (BURNIE RESTORES ticket claims = posture-widening but no unbacked obligation; SALVAGE draws LESS ETH + sDGNRS-owned-BURNIE transfer = solvency-positive no new emission); CANCEL + SALVAGE-BURNIE are sDGNRS-owned/BURNIE-emission no new ETH-pool draw; claimablePool<=balance holds on every path; each with the SEC-02 TST-361 proof obligation"
    - "358-SPEC.md contains a '## UDVT Width/Byte-Preservation Discipline (design feed)' section locking the per-site matrix (D-19): the 3 abi.encodePacked(…day…) sites cast Day->uint32 (AdvanceModule:1405/:1828, DegenerusGame:1011); packed Sub/struct day fields stay uint24-backed (no cold-slot spill); standalone day slots + indexed event topics stay raw uint32; rngWordByDay key layout unchanged; operator overloads <,<=,==,%,+,- with solc 0.8.34 UDVT support confirmed; the test-file handling D-20"
    - "358-SPEC.md contains a '## Full Call-Graph Grep-Attestation (vs 1e7a646d)' table with one row per CONTEXT 'Source anchors' file:line, each grep-confirmed Y/N with drift corrected — INCLUDING the noted drifts: HYG-02 DegeneretteModule:809 names EndgameModule/runRewardJackpots (not _runRewardJackpots-only); the GameOverModule rngWordByDay read :106 is inside a preRefundAvailable != 0 guard; the auto-evict accrual-forfeit needs explicit-delete vs the current persist-out-of-set; the 3 encodePacked sites present at exactly :1405/:1828/:1011; the _purchaseCoinFor :887-907 discards-returns confirmed; the BATCH-01 :947-949 + :1220/:1355 fold confirmed"
    - "358-SPEC.md contains a '## SPEC Lock' section flipping the status to LOCKED, listing all 6 owned req-IDs (WWXRP-02, TDEC-02, TDEC-03, BURNIE-03, SALVAGE-02, CANCEL-02) as design-locked, mapping each to its IMPL/TST owner, and asserting all 8 ROADMAP Phase-358 Success Criteria are satisfied"
  artifacts:
    - path: ".planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md"
      provides: "Appends the cross-cutting RNG-freeze + SOLVENCY re-attestation + the UDVT discipline design feed + the full call-graph grep-attestation table + the SPEC Lock; completes the design-lock document"
      contains: "Full Call-Graph Grep-Attestation"
      min_lines: 420
  key_links:
    - from: "358-SPEC.md UDVT Width/Byte-Preservation Discipline section"
      to: "DegenerusGameAdvanceModule.sol:1405 + :1828 + DegenerusGame.sol:1011 (the 3 abi.encodePacked day sites)"
      via: "cast Day->uint32 to preserve the keccak preimage byte-image; rngWordByDay key layout unchanged"
      pattern: "abi.encodePacked|uint32|byte-?image"
    - from: "358-SPEC.md Full Call-Graph Grep-Attestation table"
      to: "every CONTEXT Source-anchors file:line vs 1e7a646d (incl. HYG-02 :809, GameOverModule :106 guard, auto-evict explicit-delete)"
      via: "grep each anchor, record confirmed/drift, correct the 3 noted drifts"
      pattern: "1e7a646d|grep-?attest|drift"
---

<objective>
Complete `358-SPEC.md` with the cross-cutting work that spans all 8 milestone items: (a) the RNG-freeze-intact re-attestation on paper (all 8 items, the UDVT byte-image being load-bearing); (b) the SOLVENCY framing (6 of 8 off the ETH path; BURNIE + SALVAGE the two functional exceptions, both flagged + solvency-positive-or-neutral); (c) the UDVT width/byte-preservation discipline design feed (the per-site cast/keep/widen matrix D-19/D-20); (d) the FULL call-graph grep-attestation of every CONTEXT.md "Source anchors" file:line against `1e7a646d`, correcting the known drifts; and (e) the SPEC Lock flipping the document to LOCKED and asserting all 8 ROADMAP Phase-358 Success Criteria.

Purpose: close the design gate. After this plan the SPEC has (1) every owned design-lock IMPL-ready (TDEC/WWXRP/BURNIE/SALVAGE/CANCEL from plans 01-02), (2) the freeze/solvency design floor re-attested, (3) the UDVT discipline that the IMPL must follow byte-for-byte, and (4) every cited anchor proven against the frozen subject — so Phase 359 authors the batched diff with zero un-checked assumptions.

Output: `358-SPEC.md` extended with `## Cross-Cutting RNG-Freeze Re-Attestation (paper)`, `## Cross-Cutting SOLVENCY Re-Attestation (paper)`, `## UDVT Width/Byte-Preservation Discipline (design feed)`, `## Full Call-Graph Grep-Attestation (vs 1e7a646d)`, and `## SPEC Lock`. PAPER-ONLY — ZERO `contracts/*.sol` mutation. Depends on plans 01 + 02 (the TDEC/WWXRP/BURNIE/SALVAGE/CANCEL sections must exist; this plan appends to + locks the same file).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-CONTEXT.md
@.planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md

<frozen_subject>
Baseline = v56.0 closure HEAD, frozen contract subject `1e7a646d`. Working-tree `contracts/` is byte-identical to `1e7a646d` — confirm `git diff --quiet 1e7a646d HEAD -- contracts/` (clean). The grep-attestation MUST grep each anchor against the working tree (= `1e7a646d` under the guard) and record confirmed/drift; anchors also readable via `git show 1e7a646d:<path>`.
</frozen_subject>

<key_anchors>
The "8 items" (for the cross-cutting re-attestation): (1) BATCH-01/02 handlePurchase BURNIE-flip batching, (2) WWXRP whale-halfpass, (3) terminal-decimator boost + bucket promotion, (4) test/comment hygiene (HYG), (5) the repo-wide UDVT, (6) the BURNIE coin-buy ticket-queue fix, (7) the SALVAGE combo payout, (8) the CANCEL auto-claim. (RNG-freeze + SOLVENCY span all 8; the OWNED 358 req-IDs are the 6 in the frontmatter.)

UDVT (D-19/D-20 — design feed, NOT an owned 358 req-ID; UDVT-01/02/03 built at IMPL 359):
- The 3 `abi.encodePacked(…day…)` RNG-entropy sites (verified present at exactly these lines per CONTEXT): `contracts/modules/DegenerusGameAdvanceModule.sol:1405` (combined, currentDay, block.prevrandao), `:1828` (vrfWord, gapDay), `contracts/DegenerusGame.sol:1011` (day, address(this)) — cast Day→uint32 to preserve the keccak preimage byte-image.
- `rngWordByDay` mapping KEY layout unchanged (uint24/uint32 zero-pad to the same slot); `contracts/storage/DegenerusGameStorage.sol:454` (rngWordByDay).
- Packed `Sub`/struct day fields become `Day` (uint24-backed) → same slots, the v56 packed-Sub gas win intact. Standalone day slots + indexed day event topics stay raw uint32. Operator overloads `<,<=,==,%,+,-`; solc 0.8.34 supports UDVT + global operator overloads (confirm). ~649 day-bearing lines / 27 contracts (SPEC produces the exact per-site count — Claude's discretion within the locked shapes). Test-file handling D-20: contract-side UDVT in the ONE 359 diff; ~143 test-file updates land as separate agent-committable commits.

Full call-graph grep-attestation — the CONTEXT.md "Source anchors" lists are the canonical input. The KNOWN DRIFTS to verify + record (per the phase brief):
- HYG-02: `contracts/modules/DegenerusGameAdvanceModule.sol:1191` (`_runRewardJackpots`) confirmed; the SECOND cited site `contracts/modules/DegenerusGameDegeneretteModule.sol:809` did NOT match a `_runRewardJackpots` grep — the requirement also names `EndgameModule`/`runRewardJackpots` (verified at planning: `:809` is a comment region referencing `advanceGame / runRewardJackpots`). Re-attest the exact text/line.
- The GameOverModule `rngWordByDay` read (verified at planning): at `1e7a646d` `contracts/modules/DegenerusGameGameOverModule.sol:106` the `rngWord = rngWordByDay[day]` read is INSIDE an `if (preRefundAvailable != 0)` guard — reconcile (already handled in TDEC-03; record it in the attestation table too).
- The auto-evict accrual-forfeit: today the pass/funding evict paths `_removeFromSet` but leave `_subOf` claimable out-of-set (a latent inconsistency) → the forfeit intent needs an EXPLICIT delete (already handled in CANCEL-02 D-32; record it in the table).
- The `_purchaseCoinFor:887-907` discards-returns (verified at planning), the BATCH-01 `:947-949` inline + `:1220`/`:1355` fold (verified), the SALVAGE `:975-977` relabel (verified), the manual-cancel `:345-362` + `:348-351` FALSE comment (verified) — confirm Y in the table.
- HYG-01 stale `gameSetAutoRebuy` test refs (`test/unit/GovernanceGating.test.js:247`, `test/unit/DegenerusVault.test.js:385/456`/`:453/456`, `test/fuzz/CoverageGap222.t.sol:1055/1060/1084/1085`) + the rename target `coinSetAutoRebuy(true, 0)` — grep-attest (these are TST-owned at 361 but cited in CONTEXT, so attest presence).
</key_anchors>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the cross-cutting RNG-freeze + SOLVENCY re-attestation + the UDVT width/byte-preservation discipline design feed</name>
  <files>.planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md</files>
  <read_first>
    - .planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md (the TDEC/WWXRP/BURNIE/SALVAGE/CANCEL sections from plans 01-02 — append to it; cross-reference their freeze/solvency sub-points)
    - .planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-CONTEXT.md (D-18 WWXRP freeze; D-24 BURNIE freeze/solvency; D-28/D-29 SALVAGE freeze/solvency; D-33 CANCEL freeze/solvency; D-02/D-03 TDEC freeze; D-19/D-20 UDVT; the code_context "Integration Points" UDVT RNG boundary)
    - contracts/modules/DegenerusGameAdvanceModule.sol (:1405 the abi.encodePacked combined/currentDay/prevrandao site; :1828 the vrfWord/gapDay site — confirm both are abi.encodePacked with a day argument)
    - contracts/DegenerusGame.sol (:1011 the abi.encodePacked day/address(this) site — confirm)
    - contracts/storage/DegenerusGameStorage.sol (:454 rngWordByDay mapping — confirm the key type / slot)
    - REQUIREMENTS.md (SEC-01 + SEC-02 wording — to phrase the proof obligations exactly)
  </read_first>
  <action>
    Append three sections to `358-SPEC.md`.

    `## Cross-Cutting RNG-Freeze Re-Attestation (paper) (SEC-01 design feed — OWNED at TST 361)`: one labelled row per the 8 items asserting RNG-freeze-intact, each with its source anchor + the TST-361 SEC-01 proof obligation:
    - (1) BATCH-01/02: BURNIE-accounting only (return-instead-of-credit at `DegenerusQuests.sol:947-949` → caller fold `MintModule:1220/:1355`); reads/derives no RNG; determinism unchanged.
    - (2) WWXRP: writes only the RNG-insensitive `whalePassClaims` counter + `wwxrpJackpotWhalePassBracketAwarded[level/10]` flag, gated by the already-committed `s==9` (deterministic from the committed daily `rngWord`); pre-liveness only. Cross-ref WWXRP-02 D-18.
    - (3) terminal-decimator boost + bucket promotion: all weight + bucket mutation precedes the draw (the future-day-word lemma); deterministic from the fixed effective-streak factor × burn, committed before the RNG word can exist. Cross-ref TDEC-03.
    - (4) HYG: comment/test-only, no contract logic → no RNG surface.
    - (5) UDVT (LOAD-BEARING): the 3 `abi.encodePacked(…day…)` entropy sites cast `Day→uint32` so the keccak preimage byte-image is preserved bit-for-bit (`AdvanceModule:1405/:1828`, `DegenerusGame:1011`); the `rngWordByDay` KEY layout is unchanged (uint24/uint32 zero-pad to the same slot, `Storage:454`); flag this as the per-site RNG-freeze byte-diff gate the TST 361 SEC-01 enforces empirically.
    - (6) BURNIE coin-buy fix: `purchaseCoin` reads no `rngWord` (gated by `_livenessTriggered`/`gameOverPossible`). Cross-ref BURNIE-03 D-24.
    - (7) SALVAGE: reads only the SETTLED prior-day word under the `rngLockedFlag` gate; the new ETH/BURNIE split is one more derived slice of the SAME seed → no new VRF, no new manipulable freeze surface; non-exploitability rests on the cap, not unpredictability. Cross-ref SALVAGE-02 D-28.
    - (8) CANCEL: reads no `rngWord` (`creditFlip`/`drainAffiliateBase` only); `rngLock`-gated at the `subscribe` entry. Cross-ref CANCEL-02 D-33.
    State that SEC-01 is OWNED at TST 361; this section is the design gate.

    `## Cross-Cutting SOLVENCY Re-Attestation (paper) (SEC-02 design feed — OWNED at TST 361)`: state the framing — SIX of the eight items are BURNIE flip-credit / weight-only / pure-annotation OFF the ETH/`claimablePool` path with the ETH/pool DEBIT code BYTE-UNCHANGED (BATCH, WWXRP, TDEC, HYG, UDVT, CANCEL). The TWO functional exceptions, both FLAGGED + both solvency-positive-or-neutral:
    - BURNIE coin-buy fix: the ETH/`claimablePool` DEBIT stays byte-unchanged BUT this RESTORES queued ticket claims on the ETH prize pools (intended pre-160 design) — a posture-widening, FLAGGED; no unbacked obligation (ticket wins stay pro-rata from the available pool; BURNIE pays no ETH into pools → `claimablePool <= balance` holds). Cross-ref BURNIE-03 D-24.
    - SALVAGE combo payout: draws LESS ETH from `claimableWinnings[SDGNRS]` (only the ETH part `cashWei − actualBurnie`, not the full `cashWei`) → ETH liability DROPS (solvency-POSITIVE); the BURNIE part is a TRANSFER of sDGNRS-OWNED BURNIE (token balance + claimable coinflip stake) — NO new emission; the source-availability check + ETH fallback prevent over-draw. Cross-ref SALVAGE-02 D-29.
    Explicitly note CANCEL + the SALVAGE-BURNIE leg are sDGNRS-owned / BURNIE-emission, NO new ETH-pool draw. Assert `claimablePool <= balance` holds on EVERY path. State SEC-02 is OWNED at TST 361; this is the design gate. One proof-obligation line per item.

    `## UDVT Width/Byte-Preservation Discipline (design feed — UDVT-01/02/03 built at IMPL 359)`: lock the per-site decision matrix D-19, one labelled sub-point each:
    - (1) the 3 RNG `abi.encodePacked(…day…)` sites cast `Day→uint32` to preserve the exact keccak preimage byte-image: `AdvanceModule:1405` (combined, currentDay, block.prevrandao), `:1828` (vrfWord, gapDay), `DegenerusGame:1011` (day, address(this)). State the "pure annotation" premise is FALSE at these sites (an unwrapped uint24-backed `Day` would shorten the preimage).
    - (2) packed `Sub`/struct day fields become `Day` (uint24-backed) → same slots, the v56 packed-Sub gas win intact (no cold-slot spill).
    - (3) standalone day slots + `indexed` day event topics stay raw `uint32` (preserve the "uint24 packed / uint32 transient" convention; cast at `Day` boundaries).
    - (4) `rngWordByDay` mapping KEY layout unchanged (uint24/uint32 zero-pad to the same slot, `Storage:454`).
    - (5) operator overloads `<, <=, ==, %, +, -` (final set grepped from actual day comparisons — the exact set is Claude's-discretion at IMPL within this list; confirm solc 0.8.34 supports UDVT + global operator overloads).
    - (6) repo-wide ~649 day-bearing lines / 27 contracts (the exact per-site count is produced at IMPL — Claude's discretion within the locked shapes).
    - D-20 test-file handling: the contract-side UDVT is part of the ONE batched USER-approved 359 diff (held for hand-review); the ~143 test-file updates land as separate AGENT-committable commits (only `contracts/*.sol` commits need approval).
    Flag "UDVT-01/02/03 owned at IMPL 359 — their byte-preservation discipline is fixed here; the GAS-neutrality measured at 360, the regression proven at 361".

    No contract code authored; cite anchors + behavior only; no fenced code.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && git diff --quiet 1e7a646d HEAD -- contracts/ && f=.planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md && grep -q "Cross-Cutting RNG-Freeze Re-Attestation" "$f" && grep -q "Cross-Cutting SOLVENCY Re-Attestation" "$f" && grep -q "UDVT Width/Byte-Preservation Discipline" "$f" && grep -Eq "1405" "$f" && grep -Eq "1828" "$f" && grep -Eq "1011" "$f" && grep -Eqi "byte-?image|byte image" "$f" && grep -Eqi "claimablePool <= balance|claimablePool <=balance|claimablePool<=balance" "$f" && grep -Eqi "solvency-?positive" "$f" && grep -Eq "D-19" "$f" && grep -Eq "D-20" "$f" && grep -Eqi "0.8.34" "$f" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `## Cross-Cutting RNG-Freeze Re-Attestation (paper)` asserts freeze-intact for ALL 8 items, one labelled row each with anchor + TST-361 SEC-01 obligation; the UDVT `abi.encodePacked` uint32-cast byte-image is flagged LOAD-BEARING (cites `:1405`/`:1828`/`:1011`).
    - `## Cross-Cutting SOLVENCY Re-Attestation (paper)` states 6 of 8 off the ETH path (debit byte-unchanged), flags the two functional exceptions (BURNIE RESTORES claims = posture-widening no unbacked obligation; SALVAGE draws LESS ETH + sDGNRS-owned-BURNIE transfer = solvency-positive no new emission), notes CANCEL + SALVAGE-BURNIE are sDGNRS-owned/BURNIE-emission with no new ETH-pool draw, and asserts `claimablePool <= balance` on every path, each with the SEC-02 obligation.
    - `## UDVT Width/Byte-Preservation Discipline (design feed)` locks the full per-site matrix (D-19 items 1-6 + D-20): 3 encodePacked sites cast uint32, packed Sub fields stay uint24, standalone/indexed stay uint32, rngWordByDay key unchanged, operator-overload set + solc 0.8.34 confirmed, test-file handling.
    - ZERO `contracts/*.sol` files modified; no fenced implementation code.
  </acceptance_criteria>
  <done>The cross-cutting RNG-freeze + SOLVENCY re-attestation + the UDVT byte-preservation discipline are authored in `358-SPEC.md`.</done>
</task>

<task type="auto">
  <name>Task 2: Author the full call-graph grep-attestation table (vs 1e7a646d) + flip the SPEC Lock asserting all 8 Success Criteria</name>
  <files>.planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md</files>
  <read_first>
    - .planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md (the full draft from plans 01-02 + Task 1 — append the attestation table + the Lock)
    - .planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-CONTEXT.md (the ENTIRE canonical_refs "Source anchors" lists — every file:line is a table row; the 3 noted drifts)
    - .planning/ROADMAP.md (the "Phase 358 ... Success Criteria" 1-8 — to map each to the SPEC section that satisfies it in the Lock)
    - contracts/modules/DegenerusGameAdvanceModule.sol (:1191 _runRewardJackpots HYG-02 first site)
    - contracts/modules/DegenerusGameDegeneretteModule.sol (:809 the HYG-02 SECOND site — confirm the exact comment text: does it name EndgameModule / runRewardJackpots?)
    - contracts/modules/DegenerusGameGameOverModule.sol (:106 — confirm the rngWordByDay[day] read is inside if (preRefundAvailable != 0))
    - contracts/modules/GameAfkingModule.sol (:1144 / :1175 / :1226 the evict paths — confirm _removeFromSet leaves _subOf out-of-set, i.e. the explicit-delete drift)
    - (spot-grep the remaining CONTEXT anchors as needed; HYG-01 test refs: test/unit/GovernanceGating.test.js:247, test/unit/DegenerusVault.test.js:385/456, test/fuzz/CoverageGap222.t.sol:1055/1060/1084/1085)
  </read_first>
  <action>
    Append two sections to `358-SPEC.md`.

    `## Full Call-Graph Grep-Attestation (vs 1e7a646d)`: confirm the frozen-subject guard once (`git diff --quiet 1e7a646d HEAD -- contracts/` clean) then build a table with one row per CONTEXT.md "Source anchors" `file:line`, grouped by the 6 anchor blocks (Degenerette/WWXRP · Decimator/TDEC · Advance/GameOver/RNG · Quests/streak · BURNIE-path · SALVAGE · CANCEL · HYG). For EACH anchor: grep the working tree (= `1e7a646d` under the guard) to confirm the cited symbol/text is at the cited line, and record a row: `Anchor (symbol) | Cited path:line | Confirmed (Y/N) | Drift note`.
    RECORD the three known drifts explicitly with their reconciliation:
    - HYG-02 second site: `DegeneretteModule:809` — record the EXACT comment text grepped at `1e7a646d` (it references `advanceGame / runRewardJackpots`, NOT a `_runRewardJackpots` symbol; the HYG-02 fix target is `_consolidatePoolsAndRewardJackpots`); note this is a comment-only HYG-02 item owned at TST 361.
    - GameOverModule `:106`: the `rngWord = rngWordByDay[day]` read is INSIDE `if (preRefundAvailable != 0)` — record + cross-ref the TDEC-03 reconciliation (the future-day-word property holds for the key regardless of the read-guard).
    - auto-evict explicit-delete: the pass/funding evict paths (`:1175`/`:1226`) + tombstone-reclaim (`:1144`) `_removeFromSet` but leave `_subOf` claimable out-of-set — record the latent inconsistency + cross-ref CANCEL-02 D-32 (the forfeit intent requires an EXPLICIT `delete _subOf`).
    Confirm Y for the verified-at-planning anchors (`_purchaseCoinFor:887-907` discards returns; BATCH-01 `:947-949` inline + `:1220`/`:1355` fold; SALVAGE `:975-977` relabel; manual-cancel `:345-362` + `:348-351` FALSE comment; `_resolveFullTicketBet` s>=7 block `:710-715`; TerminalDecEntry `:1585-1591` 232/256; `_livenessTriggered:1231-1240`; `getPlayerQuestView:1088`; the 3 encodePacked sites `:1405`/`:1828`/`:1011`). Attest the HYG-01 stale `gameSetAutoRebuy` test refs present (TST-owned at 361 but cited in CONTEXT). State explicitly: "no 'by construction' / 'single fn reaches all paths' claim survives un-checked".

    `## SPEC Lock (LOCKED)`: flip the document status from DRAFT to LOCKED. List the 6 OWNED req-IDs (WWXRP-02, TDEC-02, TDEC-03, BURNIE-03, SALVAGE-02, CANCEL-02) as DESIGN-LOCKED, each mapped to its IMPL/TST owner (WWXRP-01@359, TDEC-01@359, BURNIE-01/02@359, SALVAGE-01@359 + SALVAGE-03@361, CANCEL-01@359 + CANCEL-03@361; UDVT-01/02/03@359 design-fed). Then author a `### ROADMAP Phase-358 Success Criteria` checklist mapping all EIGHT ROADMAP SCs to the SPEC section that satisfies each (SC1→WWXRP-02; SC2→TDEC-02; SC3→TDEC-03; SC4→UDVT discipline; SC5→Cross-Cutting RNG-Freeze + SOLVENCY + the Full Call-Graph Grep-Attestation; SC6→BURNIE-03; SC7→SALVAGE-02; SC8→CANCEL-02) — each marked SATISFIED with the section name. Record the final paper-only attestation: ZERO `contracts/*.sol` touched in this phase.

    No contract code authored; cite anchors + behavior only; no fenced code.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && git diff --quiet 1e7a646d HEAD -- contracts/ && f=.planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-SPEC.md && grep -q "Full Call-Graph Grep-Attestation" "$f" && grep -q "SPEC Lock" "$f" && grep -Eqi "preRefundAvailable" "$f" && grep -Eqi ":809|DegeneretteModule.*809" "$f" && grep -Eqi "explicit.?delete|delete _subOf|out-of-set|out of set" "$f" && for r in WWXRP-02 TDEC-02 TDEC-03 BURNIE-03 SALVAGE-02 CANCEL-02; do grep -q "$r" "$f" || { echo "MISSING $r"; exit 1; }; done && grep -Eqi "by construction" "$f" && grep -Eqi "Success Criteria" "$f" && grep -Eqi "SC8|SC-8" "$f" && grep -Eqi "LOCKED" "$f" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `## Full Call-Graph Grep-Attestation (vs 1e7a646d)` contains one table row per CONTEXT "Source anchors" file:line with a Confirmed (Y/N) + Drift column, grouped by the anchor blocks; the frozen-subject guard is asserted once.
    - All THREE known drifts are explicitly recorded with reconciliation: HYG-02 `DegeneretteModule:809` exact comment text (names runRewardJackpots/EndgameModule, fix target `_consolidatePoolsAndRewardJackpots`); GameOverModule `:106` `preRefundAvailable != 0` guard (cross-ref TDEC-03); auto-evict `_removeFromSet`-leaves-`_subOf`-out-of-set explicit-delete (cross-ref CANCEL-02 D-32).
    - The verified-at-planning anchors are marked Y; the "no 'by construction' survives un-checked" statement is present.
    - `## SPEC Lock (LOCKED)` flips the status, lists all 6 owned req-IDs mapped to IMPL/TST owners, and a `### ROADMAP Phase-358 Success Criteria` checklist maps all 8 SCs to satisfying sections (each SATISFIED).
    - ZERO `contracts/*.sol` files modified; no fenced implementation code.
  </acceptance_criteria>
  <done>The full call-graph grep-attestation table (with the 3 drifts reconciled) + the SPEC Lock asserting all 8 ROADMAP Success Criteria are authored, completing `358-SPEC.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (DESIGN-LEVEL — paper-only SPEC phase; zero new code-attack-surface since zero contracts change)

| Boundary | Description |
|----------|-------------|
| RNG-entropy `abi.encodePacked(…day…)` boundary (the freeze spine) | The UDVT must preserve the exact keccak preimage byte-image at the 3 entropy sites; an unwrapped uint24-backed `Day` would shorten the preimage and change the derived word — the single freeze-sensitive integration of the wide refactor. |
| ETH/`claimablePool` solvency path (must hold `claimablePool <= balance`) | Six items stay off it (debit byte-unchanged); BURNIE restores claims (no unbacked obligation); SALVAGE draws less ETH + sDGNRS-owned-BURNIE transfer (solvency-positive). |
| cited-anchor ↔ frozen subject `1e7a646d` | Every design assertion rests on a `file:line` that MUST be grep-confirmed against the frozen subject; un-checked anchors are the "by construction" failure mode the grep-attestation closes. |

## STRIDE Threat Register (design assertions the SPEC LOCKS; PROVEN at TST 361 / adversarially at TERMINAL 362)

| Threat ID | Category | Component | Disposition | Mitigation Plan (design assertion) |
|-----------|----------|-----------|-------------|------------------------------------|
| T-358-15 | Tampering (RNG/freeze) | UDVT widening shortens the keccak preimage → changes the derived word | mitigate | The 3 `abi.encodePacked(…day…)` sites explicitly cast `Day→uint32` (`AdvanceModule:1405/:1828`, `DegenerusGame:1011`); the `rngWordByDay` KEY layout unchanged; enforced by the per-site byte-diff gate at TST 361 SEC-01. SPEC UDVT discipline + Cross-Cutting RNG-Freeze. |
| T-358-16 | Tampering (gas/layout) | UDVT spills a packed Sub day field to a cold slot → kills the v56 gas win | mitigate | Packed `Sub`/struct day fields stay uint24-backed (same slots); standalone/indexed widen to uint32 only. SPEC UDVT discipline D-19(2/3); proven GAS-NEUTRAL at 360. |
| T-358-17 | Information (un-checked anchor) | a "by construction" claim rests on a drifted/absent anchor | mitigate | The Full Call-Graph Grep-Attestation greps EVERY cited `file:line` vs `1e7a646d`, records Y/N + drift; the 3 known drifts (HYG-02 :809, GameOverModule :106 guard, auto-evict explicit-delete) are reconciled. SPEC Full Call-Graph Grep-Attestation. |
| T-358-18 | Tampering (solvency) | a flagged functional exception (BURNIE/SALVAGE) introduces an unbacked obligation | mitigate | Both exceptions are solvency-positive-or-neutral; `claimablePool <= balance` re-attested on every path; the no-arb cap (SALVAGE) + pro-rata ticket wins (BURNIE) bound the liability. SPEC Cross-Cutting SOLVENCY; TST 361 SEC-02. |

**Block-on-HIGH note:** Paper-only design phase — no package installs, no contract mutation. The block-on-HIGH gate is the DESIGN gate. The UDVT byte-image preservation (T-358-15) is the load-bearing freeze item of the whole milestone and is locked per-site at the 3 entropy boundaries; the grep-attestation (T-358-17) closes the "by construction" hole across all anchors. No HIGH design hole remains open at the SPEC Lock.
</threat_model>

<verification>
- `git diff --quiet 1e7a646d HEAD -- contracts/` is clean throughout (ZERO contract mutation — paper-only).
- `358-SPEC.md` extended with `## Cross-Cutting RNG-Freeze Re-Attestation (paper)`, `## Cross-Cutting SOLVENCY Re-Attestation (paper)`, `## UDVT Width/Byte-Preservation Discipline (design feed)`, `## Full Call-Graph Grep-Attestation (vs 1e7a646d)`, `## SPEC Lock (LOCKED)`.
- The full grep-attestation table covers every CONTEXT "Source anchors" file:line with the 3 known drifts reconciled; the SPEC Lock asserts all 8 ROADMAP Phase-358 Success Criteria SATISFIED.
- All 6 owned req-IDs (WWXRP-02, TDEC-02, TDEC-03, BURNIE-03, SALVAGE-02, CANCEL-02) appear in this plan's `requirements` (the closure/coverage plan) and are listed as design-locked in the Lock.
</verification>

<success_criteria>
- `358-SPEC.md` is a COMPLETE, LOCKED design-lock document: every owned design-lock IMPL-ready, the RNG-freeze + SOLVENCY design floor re-attested, the UDVT byte-preservation discipline fixed, every cited anchor grep-proven vs `1e7a646d` (drifts reconciled), and all 8 ROADMAP Success Criteria asserted satisfied.
- No "by construction" assertion survives un-checked.
- ZERO `contracts/*.sol` mutation.
</success_criteria>

<output>
Create `.planning/phases/358-spec-design-lock-freeze-solvency-re-attestation-call-graph-a/358-03-SUMMARY.md` when done.
</output>
