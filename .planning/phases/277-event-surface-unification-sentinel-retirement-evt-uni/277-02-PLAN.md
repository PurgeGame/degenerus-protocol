---
phase: 277-event-surface-unification-sentinel-retirement-evt-uni
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - test/unit/EventSurfaceUnification.test.js
  - test/edge/LootboxAutoResolveRegression.test.js
  - test/unit/LootboxWholeTicket.test.js
  - test/unit/JackpotTicketRollSilentColdBust.test.js
  - package.json
autonomous: false
requirements:
  - TST-EVT-UNI-01
  - TST-EVT-UNI-02
  - TST-EVT-UNI-03
  - TST-EVT-UNI-04
  - TST-EVT-UNI-05
  - TST-EVT-UNI-06

must_haves:
  truths:
    - "A test asserts the new `LootBoxOpened` / `BurnieLootOpen` / `JackpotTicketWin` signatures and that the old `LootboxTicketRoll` topic hash has zero emit sites."
    - "A test asserts `LootboxTicketRoll` is absent from both `DegenerusGameLootboxModule.sol` and `IDegenerusGameModules.sol`."
    - "A test asserts `_resolveLootboxCommon` no longer branches on `index != type(uint48).max` and that auto-resolve callers pass `0` + `emitLootboxEvent=false`."
    - "A test derives `whole = (futureTickets / TICKET_SCALE) + (roundedUp ? 1 : 0)` from the already-emitted `futureTickets`/`tickets` fields and proves field-consistency for manual `LootBoxOpened` / `BurnieLootOpen`."
    - "A test asserts auto-resolve emits no `LootBoxOpened` and the manual-only cold-bust consolation is gated by `emitLootboxEvent`."
    - "A test asserts `JackpotTicketWin.roundedUp` mirrors the `_jackpotTicketRoll` Bernoulli outcome."
  artifacts:
    - path: "test/unit/EventSurfaceUnification.test.js"
      provides: "Topic-hash change tests, LootboxTicketRoll removal sweep, sentinel-retirement structural proof, field-consistency derivations, JackpotTicketWin.roundedUp proof"
      min_lines: 80
    - path: "test/edge/LootboxAutoResolveRegression.test.js"
      provides: "Updated TST-REG-03 block — LootboxTicketRoll count 1→0, consolation moves to emitLootboxEvent gate, auto-resolve emitLootboxEvent now false"
    - path: "test/unit/LootboxWholeTicket.test.js"
      provides: "Updated drift-grep patterns for the post-retirement source structure"
    - path: "test/unit/JackpotTicketRollSilentColdBust.test.js"
      provides: "Updated JackpotTicketWin assertion for the new roundedUp field"
  key_links:
    - from: "test/unit/EventSurfaceUnification.test.js"
      to: "compiled contract ABI / source"
      via: "ethers Interface topic-hash computation + fs source grep"
      pattern: "getEvent\\(|keccak256|readFileSync"
    - from: "package.json scripts"
      to: "new + updated test files"
      via: "test script wiring"
      pattern: "EventSurfaceUnification"
---

<objective>
Land the Phase 277 test wave in one batched, user-approved commit: a new `test/unit/EventSurfaceUnification.test.js` covering all six TST-EVT-UNI requirements, plus targeted updates to three existing precedent test files whose assertions Phase 277 invalidates (the `LootboxTicketRoll` count, the consolation gating location, the auto-resolve `emitLootboxEvent` value, the `JackpotTicketWin` signature), plus `package.json` script wiring.

Purpose: Prove the event-surface unification + sentinel retirement landed correctly and that no precedent test silently passes against stale assertions.
Output: One new test file, three updated test files, `package.json` wiring, one batched diff presented to the user, one user-approved commit `test(277): event topic-hash changes + LootboxTicketRoll removal + sentinel retirement + field consistency [TST-EVT-UNI-01..06]`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/277-event-surface-unification-sentinel-retirement-evt-uni/277-CONTEXT.md
@.planning/phases/277-event-surface-unification-sentinel-retirement-evt-uni/277-PATTERNS.md
@.planning/phases/277-event-surface-unification-sentinel-retirement-evt-uni/277-01-PLAN.md

<authority_note>
`277-CONTEXT.md` `<decisions>` is authoritative. The ROADMAP.md TST-EVT-UNI-04 / TST-EVT-UNI-05 success-criteria text references a stale `preRollTickets` field — that field is NOT added (D-277-NO-PREROLL-01). Plan the tests against the CONTEXT.md shape: derive `whole = (futureTickets / TICKET_SCALE) + (roundedUp ? 1 : 0)` from the already-emitted `LootBoxOpened.futureTickets` / `BurnieLootOpen.tickets` fields, NOT from a `preRollTickets` field. TST-EVT-UNI-05 must test EVT-UNI-06 in its resolved form (D-277-AR-SILENT-01: auto-resolve emits nothing) — assert the SILENCE, not an auto-resolve `LootBoxOpened` emit.
</authority_note>

<test_precedent>
Established on-disk test layout: `test/unit/`, `test/edge/`, `test/stat/` — there is no `test/regression/` dir (REQUIREMENTS.md §TST-EVT-UNI's `test/lootbox/` + `test/jackpot/` + `test/regression/` suggestion is NOT the on-disk pattern; regression tests live in `test/unit/` and `test/edge/`). New file goes in `test/unit/EventSurfaceUnification.test.js`.

Precedent test idiom (Phase 274/275/276): **source-structural assertions + tester-direct EV proofs** — for "event X no longer emitted / no longer declared" properties, read the contract source with `fs.readFileSync` and assert grep-match counts; for signature properties, use the ethers `Interface` / `getEvent` to compute and compare topic hashes against the compiled ABI. There is a documented fixture-coverage gap for full end-to-end resolution-path integration (LBX-02, RE-DEFERRED) — do NOT attempt a full end-to-end fixture; follow the source-structural + tester-direct precedent.

Existing files this plan UPDATES (their current assertions become stale after Wave 1):
- `test/edge/LootboxAutoResolveRegression.test.js` — TST-REG-03 block (currently around :224-262) asserts `emit LootboxTicketRoll` count `== 1` and that the auto-resolve branch never emits `LootboxTicketRoll` / never calls `wwxrp.mintPrize`. After Wave 1: `LootboxTicketRoll` count is `0`; the sentinel branch is gone; the consolation moved under `if (emitLootboxEvent)`; auto-resolve callers now pass `emitLootboxEvent = false` (they previously passed `true`).
- `test/unit/LootboxWholeTicket.test.js` — contains a source-grep drift-detection test asserting the production contract contains an exact instruction sequence around the manual-branch Bernoulli math. The Bernoulli math is unchanged but the branch structure around it (sentinel removal) changed — the drift-grep patterns need updating.
- `test/unit/JackpotTicketRollSilentColdBust.test.js` — its header already states "Phase 277 EVT-UNI-04 adds the `roundedUp` field; the Phase 277 test wave updates that assertion." Update the `JackpotTicketWin` signature/field assertion for the new `roundedUp` field; the silent-cold-bust queue-surface scope is unchanged.
</test_precedent>

<discipline>
Per project CLAUDE.md + feedback memory:
- Only read/edit contracts from `contracts/` — stale copies exist elsewhere (test files reference `contracts/` paths).
- `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md` — test changes are batched into ONE diff, presented to the user, committed only after explicit user approval. Test changes are NEVER pre-approved.
- This plan touches ONLY test files + `package.json` — no `contracts/` edits.
</discipline>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write test/unit/EventSurfaceUnification.test.js covering all six TST-EVT-UNI requirements</name>
  <files>test/unit/EventSurfaceUnification.test.js, package.json</files>
  <read_first>
    - test/edge/LootboxAutoResolveRegression.test.js (full file — the source-structural assertion idiom: `fs.readFileSync` + regex match counts; the existing TST-REG-03 structure to mirror)
    - test/unit/LootboxWholeTicket.test.js (the tester-direct + drift-grep idiom)
    - test/unit/JackpotTicketRollSilentColdBust.test.js (the JackpotBernoulliTester idiom + its Phase 277 pickup note)
    - contracts/modules/DegenerusGameLootboxModule.sol (post-Wave-1 state — the restructured `LootBoxOpened` / `BurnieLootOpen` defs, the retired sentinel, the unified `_queueTickets` path, auto-resolve callers passing `0` + `false`)
    - contracts/modules/DegenerusGameJackpotModule.sol (post-Wave-1 state — `JackpotTicketWin` with `roundedUp`, `_jackpotTicketRoll` capture)
    - contracts/interfaces/IDegenerusGameModules.sol (post-Wave-1 state — `LootboxTicketRoll` deleted from `IDegenerusGameLootboxModule`)
    - package.json (test script section — the wiring pattern Phase 274/275/276 used to register new test files)
    - .planning/phases/277-event-surface-unification-sentinel-retirement-evt-uni/277-CONTEXT.md (D-277-NO-PREROLL-01 off-chain derivation formula, D-277-AR-SILENT-01, D-277-CONSOLATION-GATE-01)
  </read_first>
  <action>
    Create `test/unit/EventSurfaceUnification.test.js` with six describe/it blocks, one per requirement, following the source-structural + tester-direct precedent (no full end-to-end resolution fixture — that gap is RE-DEFERRED). Use `fs.readFileSync` against `contracts/` paths for structural sweeps and the ethers `Interface`/`getEvent` against the compiled ABI for topic-hash work.

    TST-EVT-UNI-01 — Topic-hash change tests. Load the compiled `DegenerusGameLootboxModule` and `DegenerusGameJackpotModule` ABIs (or build an `ethers.Interface` from the contract artifacts). Assert `LootBoxOpened`, `BurnieLootOpen`, `JackpotTicketWin` resolve to event fragments whose field lists match the new Wave-1 signatures: `LootBoxOpened(address indexed player, uint48 indexed lootboxIndex, uint32 day, uint256 amount, uint24 futureLevel, uint32 futureTickets, uint256 burnie, uint256 bonusBurnie, bool roundedUp)`; `BurnieLootOpen(...)` ending in `bool roundedUp`; `JackpotTicketWin(...)` ending in non-indexed `bool roundedUp` with exactly 3 indexed params. Compute each new topic-0 hash via the Interface and assert it is well-formed (non-zero 32-byte hash). Assert the OLD `LootboxTicketRoll` topic hash (`keccak256("LootboxTicketRoll(address,uint48,uint32,bool)")`) has zero emit sites by grepping `contracts/` for `emit LootboxTicketRoll` and asserting count `0`.

    TST-EVT-UNI-02 — `LootboxTicketRoll` removal regression. `fs.readFileSync` both `contracts/modules/DegenerusGameLootboxModule.sol` and `contracts/interfaces/IDegenerusGameModules.sol`; assert the regex `/LootboxTicketRoll/` matches zero times in EACH file (event def deleted from both, emit site deleted). Also assert across all of `contracts/` recursively (or the known module set) that `emit LootboxTicketRoll(` count is `0`.

    TST-EVT-UNI-03 — Sentinel-retirement regression. `fs.readFileSync` `contracts/modules/DegenerusGameLootboxModule.sol`; assert `/index != type\(uint48\)\.max/` matches zero times and `/type\(uint48\)\.max/` matches zero times. Assert the auto-resolve caller bodies pass the corrected args: grep the `resolveLootboxDirect` and `resolveRedemptionLootbox` function bodies and assert each `_resolveLootboxCommon` call passes `0` as the `index` arg (3rd positional) and `false` as the `emitLootboxEvent` arg (11th positional). Assert the `if (futureTickets != 0)` block calls `_queueTickets(player, targetLevel, whole, false)` exactly once and NOT inside any `index`-conditional branch.

    TST-EVT-UNI-04 — Manual-path `LootBoxOpened` / `BurnieLootOpen` field-consistency. Per D-277-NO-PREROLL-01 there is NO `preRollTickets` field — derive from the already-emitted scaled fields. Using a tester-direct mirror of the Bernoulli arithmetic (reuse `LootboxBernoulliTester` from the `LootboxWholeTicket.test.js` precedent if it is callable, or assert the formula structurally): assert that for the manual path, `whole = (LootBoxOpened.futureTickets / TICKET_SCALE) + (LootBoxOpened.roundedUp ? 1 : 0)` is the derivation a consumer applies, and that `BurnieLootOpen.tickets` carries the same scaled pre-Bernoulli value. Assert the WWXRP-consolation case is inferable as `whole == 0 && futureTickets > 0` corroborated by a same-tx `LootBoxWwxrpReward`. Assert `LootBoxOpened.lootboxIndex` is the `index` arg passed to `openLootBox` / `openBurnieLootBox` (structural: the emit passes the `index` param into the `lootboxIndex` slot, `day` into the `day` slot). If a tester-direct EV/derivation proof is the established idiom, follow it; otherwise assert the derivation formula and the emit-arg wiring structurally against the source.

    TST-EVT-UNI-05 — Auto-resolve event field-consistency, EVT-UNI-06 resolved form. Per D-277-AR-SILENT-01 auto-resolve emits NOTHING. Assert SILENCE: `fs.readFileSync` the module; assert that within the `resolveLootboxDirect` / `resolveRedemptionLootbox` reachable path there is no `emit LootBoxOpened` gated on auto-resolve, i.e. the only `LootBoxOpened` emit is inside `if (emitLootboxEvent)` and auto-resolve callers pass `emitLootboxEvent = false`. Assert the manual cold-bust consolation (`wwxrp.mintPrize` + `emit LootBoxWwxrpReward` with `LOOTBOX_WWXRP_CONSOLATION`) appears exactly once and is inside the `if (emitLootboxEvent)` block (so it is manual-only). Assert auto-resolve ticket awards remain observable via `_queueTickets` → `TicketsQueued` (structural: the unified path calls `_queueTickets`).

    TST-EVT-UNI-06 — `JackpotTicketWin.roundedUp` field-consistency. Assert the `JackpotTicketWin` ABI fragment has a non-indexed final `bool roundedUp`. Using the `JackpotBernoulliTester` mirror (precedent from `JackpotTicketRollSilentColdBust.test.js`): assert that the `roundedUp` value the contract emits from `_jackpotTicketRoll` mirrors the Bernoulli predicate outcome (`frac != 0 && (uint16(entropy >> 200) % uint16(TICKET_SCALE)) < uint16(frac)`), and that the two trait-matched emit sites emit `roundedUp = false`. Structural assertion against the source: `_jackpotTicketRoll` declares `bool roundedUp = false;` before the predicate and sets `roundedUp = true;` inside it; all 3 `emit JackpotTicketWin` sites pass a 7th arg.

    Wire the new file into `package.json`: add it to whichever test script Phase 274/275/276 used for new unit tests (mirror the existing wiring pattern — do not invent a new script name).
  </action>
  <verify>
    <automated>npx hardhat test test/unit/EventSurfaceUnification.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test/unit/EventSurfaceUnification.test.js` exists with six describe blocks, one labeled per `TST-EVT-UNI-01` through `TST-EVT-UNI-06`.
    - `npx hardhat test test/unit/EventSurfaceUnification.test.js` runs and all its tests pass.
    - The TST-EVT-UNI-01 block asserts the new `LootBoxOpened` / `BurnieLootOpen` / `JackpotTicketWin` signatures via the compiled ABI and asserts zero `emit LootboxTicketRoll` sites.
    - The TST-EVT-UNI-02 block asserts `/LootboxTicketRoll/` matches zero times in both `DegenerusGameLootboxModule.sol` and `IDegenerusGameModules.sol`.
    - The TST-EVT-UNI-03 block asserts `/type\(uint48\)\.max/` matches zero times in `DegenerusGameLootboxModule.sol` and asserts auto-resolve callers pass `0` + `false`.
    - The TST-EVT-UNI-04 block derives `whole` from `futureTickets`/`tickets` + `roundedUp` (NO `preRollTickets` field referenced) and asserts the `lootboxIndex`/`day` emit wiring.
    - The TST-EVT-UNI-05 block asserts auto-resolve emits no `LootBoxOpened` and the consolation is `emitLootboxEvent`-gated (manual-only).
    - The TST-EVT-UNI-06 block asserts `JackpotTicketWin.roundedUp` is non-indexed and mirrors the `_jackpotTicketRoll` Bernoulli outcome.
    - `package.json` registers the new test file in the existing test-script wiring pattern.
    - No test references a `preRollTickets` event field.
  </acceptance_criteria>
  <done>
    `test/unit/EventSurfaceUnification.test.js` exists, covers all six TST-EVT-UNI requirements via the source-structural + tester-direct precedent, all its tests pass, and it is wired into `package.json`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update three precedent test files whose assertions Wave 1 invalidated</name>
  <files>test/edge/LootboxAutoResolveRegression.test.js, test/unit/LootboxWholeTicket.test.js, test/unit/JackpotTicketRollSilentColdBust.test.js</files>
  <read_first>
    - test/edge/LootboxAutoResolveRegression.test.js (full file — the TST-REG-03 block around :224-262 asserting `LootboxTicketRoll` count == 1 and auto-resolve branch behavior)
    - test/unit/LootboxWholeTicket.test.js (full file — the source-grep drift-detection test asserting the production instruction sequence)
    - test/unit/JackpotTicketRollSilentColdBust.test.js (full file — the header Phase 277 pickup note + the `JackpotTicketWin` assertion)
    - contracts/modules/DegenerusGameLootboxModule.sol (post-Wave-1 state)
    - contracts/modules/DegenerusGameJackpotModule.sol (post-Wave-1 state)
  </read_first>
  <action>
    Update the three precedent test files so their assertions reflect the post-Wave-1 source. Make minimal, targeted edits — do NOT rewrite the files.

    (a) `test/edge/LootboxAutoResolveRegression.test.js` — TST-REG-03 block (around :224-262): change the `emit LootboxTicketRoll` count assertion from `== 1` to `== 0` (or delete the count-based sub-test and replace it with a `== 0` assertion — match whichever shape the surrounding block uses). The sub-test asserting "auto-resolve branch NEVER emits `LootboxTicketRoll` or calls `wwxrp.mintPrize`" must be rewritten: the sentinel branch no longer exists, so re-target it to assert (1) `LootboxTicketRoll` appears zero times anywhere in the module, (2) the `wwxrp.mintPrize(player, LOOTBOX_WWXRP_CONSOLATION)` cold-bust consolation appears exactly once and is inside the `if (emitLootboxEvent)` block (manual-only), and (3) both auto-resolve callers pass `emitLootboxEvent = false`. Update the file's header comment block to describe the post-Phase-277 invariants (describe what IS — the sentinel is retired, consolation gated by `emitLootboxEvent`, auto-resolve callers pass `false`); do NOT leave stale "under the sentinel gate" / "pass `type(uint48).max`" prose.

    (b) `test/unit/LootboxWholeTicket.test.js` — the source-grep drift-detection test: the Bernoulli arithmetic itself is unchanged, but the branch structure around it changed (sentinel `if (index != type(uint48).max)` removed, `_queueTickets` now unconditional, consolation moved under `if (emitLootboxEvent)`). Update the drift-grep regex patterns so they match the post-retirement source structure — the patterns should still pin the unchanged Bernoulli instruction sequence (`scaledPre`/`whole`/`frac`/`roundedUp`, `uint16(seed >> 152)`) but must no longer expect the sentinel branch. Remove any pattern that pins `index != type(uint48).max` or the `emit LootboxTicketRoll` line.

    (c) `test/unit/JackpotTicketRollSilentColdBust.test.js` — update the `JackpotTicketWin` signature/field assertion to expect the new non-indexed trailing `bool roundedUp` field. If the file uses `JackpotBernoulliTester` and asserts the emit args, add the `roundedUp` expectation (mirrors the Bernoulli outcome). The silent-cold-bust queue-surface scope is unchanged — do not alter those assertions. Update the header's Phase 277 pickup note to reflect that the update is now DONE (describe what IS).
  </action>
  <verify>
    <automated>npx hardhat test test/edge/LootboxAutoResolveRegression.test.js test/unit/LootboxWholeTicket.test.js test/unit/JackpotTicketRollSilentColdBust.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx hardhat test` on all three updated files passes.
    - `test/edge/LootboxAutoResolveRegression.test.js` asserts `emit LootboxTicketRoll` count `== 0` (not `== 1`); its rewritten sub-test asserts the consolation is `emitLootboxEvent`-gated and auto-resolve callers pass `false`; its header has no stale sentinel/`type(uint48).max` prose.
    - `test/unit/LootboxWholeTicket.test.js` drift-grep patterns match the post-retirement source and contain no pattern pinning `index != type(uint48).max` or `emit LootboxTicketRoll`.
    - `test/unit/JackpotTicketRollSilentColdBust.test.js` asserts the `JackpotTicketWin` event has a trailing non-indexed `bool roundedUp`; its header pickup note describes the update as done.
    - No updated file contains change-history language describing the test edit itself (comments describe what the test asserts now).
  </acceptance_criteria>
  <done>
    All three precedent test files reflect the post-Wave-1 source, their assertions pass, and no stale sentinel / `LootboxTicketRoll` / `preRollTickets` assertions remain.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run full affected suite, present batched test diff, await user approval, commit</name>
  <read_first>
    - feedback_no_contract_commits.md
    - feedback_batch_contract_approval.md
    - feedback_never_preapprove_contracts.md
    - feedback_manual_review_before_push.md
  </read_first>
  <what-built>
    The Phase 277 test wave: a new `test/unit/EventSurfaceUnification.test.js` covering all six TST-EVT-UNI requirements, targeted updates to `test/edge/LootboxAutoResolveRegression.test.js` / `test/unit/LootboxWholeTicket.test.js` / `test/unit/JackpotTicketRollSilentColdBust.test.js`, and `package.json` script wiring.

    Before presenting the diff, run the full set of affected tests and confirm they pass: `npx hardhat test test/unit/EventSurfaceUnification.test.js test/edge/LootboxAutoResolveRegression.test.js test/unit/LootboxWholeTicket.test.js test/unit/JackpotTicketRollSilentColdBust.test.js`. Also run any other existing lootbox/jackpot test files that reference the changed event signatures (`test/unit/LootboxConsolation.test.js`, `test/unit/LootboxAutoResolveSilentColdBust.test.js`, `test/gas/LootboxOpenGas.test.js` — run them and report whether they pass or need follow-up; if any fails because it asserts a stale signature, surface it to the user rather than silently fixing scope creep).
  </what-built>
  <how-to-verify>
    1. Run `git --no-pager diff --stat test/ package.json` and confirm only the four test files + `package.json` changed, plus the one new file `test/unit/EventSurfaceUnification.test.js` shows as added.
    2. Run `git --no-pager diff test/ package.json` and review the full batched diff.
    3. Confirm: the new file covers all six TST-EVT-UNI ids; no test references `preRollTickets`; the three precedent files no longer assert the stale `LootboxTicketRoll` count / sentinel / auto-resolve-`emitLootboxEvent=true` behavior; no stale sentinel prose in headers.
    4. Review the executor's report on the broader lootbox/jackpot suite (any unexpected failures surfaced).
    5. Approve the diff explicitly, or describe required changes.
  </how-to-verify>
  <action>
    After explicit user approval ONLY: stage exactly the four test files + `package.json` and commit with:
    `test(277): event topic-hash changes + LootboxTicketRoll removal + sentinel retirement + field consistency [TST-EVT-UNI-01..06]`
    Do NOT push. Do NOT stage `contracts/` files or anything outside the four test files + `package.json`. If the user requests changes, apply them and re-present the diff — do NOT commit until approved.
  </action>
  <resume-signal>Type "approved" to commit the batched test diff, or describe the changes you want.</resume-signal>
  <acceptance_criteria>
    - `git diff --stat test/ package.json` shows exactly `test/unit/EventSurfaceUnification.test.js` (new), `test/edge/LootboxAutoResolveRegression.test.js`, `test/unit/LootboxWholeTicket.test.js`, `test/unit/JackpotTicketRollSilentColdBust.test.js`, `package.json` — and no other files.
    - The full affected-suite run was executed and its result reported before the diff was presented.
    - The commit is created ONLY after explicit user approval, with message `test(277): event topic-hash changes + LootboxTicketRoll removal + sentinel retirement + field consistency [TST-EVT-UNI-01..06]`.
    - Nothing is pushed; no `contracts/` files staged.
  </acceptance_criteria>
  <done>
    The user has reviewed and approved the batched test diff; one commit `test(277): ... [TST-EVT-UNI-01..06]` exists; the affected suite passes; nothing pushed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test source → CI gate | Tests are the verification gate for the Wave 1 contract change; a test asserting a stale invariant would let a regression pass silently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-277T-01 | Tampering | precedent test files asserting stale invariants (`LootboxTicketRoll` count == 1, sentinel branch, auto-resolve `emitLootboxEvent=true`) | mitigate | Task 2 explicitly re-targets every stale assertion in the three precedent files; Task 3's verify step greps the diff for residual `preRollTickets` / sentinel prose and runs the broader lootbox/jackpot suite to catch any other file asserting a stale signature. |
| T-277T-02 | Repudiation | a test passing against the OLD topic hash | mitigate | TST-EVT-UNI-01 computes topic hashes from the freshly compiled ABI (Wave 2 depends on Wave 1, so the ABI is post-change) and asserts the new signatures; TST-EVT-UNI-02 asserts zero `emit LootboxTicketRoll` sites. |
| T-277T-03 | Elevation of Privilege | test wave touching `contracts/` | accept | This plan touches ONLY `test/` + `package.json`; Task 3 acceptance criteria explicitly forbid staging `contracts/` files. No contract surface is modified or re-modified here. |
</threat_model>

<verification>
- `npx hardhat test test/unit/EventSurfaceUnification.test.js` passes; six TST-EVT-UNI describe blocks present.
- `npx hardhat test` on the three updated precedent files passes.
- No test file references a `preRollTickets` event field.
- `git diff --stat` shows exactly the four test files + `package.json`; one user-approved commit; nothing pushed.
</verification>

<success_criteria>
- TST-EVT-UNI-01: new `LootBoxOpened` / `BurnieLootOpen` / `JackpotTicketWin` signatures asserted via compiled ABI; old `LootboxTicketRoll` topic hash has zero emit sites.
- TST-EVT-UNI-02: `LootboxTicketRoll` absent from `DegenerusGameLootboxModule.sol` AND `IDegenerusGameModules.sol`; zero emission sites.
- TST-EVT-UNI-03: `_resolveLootboxCommon` has no `index != type(uint48).max`; auto-resolve callers pass `0` + `emitLootboxEvent=false`.
- TST-EVT-UNI-04: manual-path field-consistency derived from `futureTickets`/`tickets` + `roundedUp` (no `preRollTickets`); `lootboxIndex`/`day` emit wiring asserted.
- TST-EVT-UNI-05: auto-resolve emits no `LootBoxOpened`; manual cold-bust consolation `emitLootboxEvent`-gated.
- TST-EVT-UNI-06: `JackpotTicketWin.roundedUp` non-indexed, mirrors the `_jackpotTicketRoll` Bernoulli outcome.
- Three precedent test files updated off their stale assertions; affected suite passes; one batched user-approved commit; nothing pushed.
</success_criteria>

<output>
After completion, create `.planning/phases/277-event-surface-unification-sentinel-retirement-evt-uni/277-02-SUMMARY.md`
</output>
