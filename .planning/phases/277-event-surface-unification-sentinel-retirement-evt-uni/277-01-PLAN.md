---
phase: 277-event-surface-unification-sentinel-retirement-evt-uni
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/modules/DegenerusGameLootboxModule.sol
  - contracts/modules/DegenerusGameJackpotModule.sol
  - contracts/interfaces/IDegenerusGameModules.sol
autonomous: false
requirements:
  - EVT-UNI-01
  - EVT-UNI-02
  - EVT-UNI-03
  - EVT-UNI-04
  - EVT-UNI-05
  - EVT-UNI-06
  - EVT-UNI-07
  - EVT-UNI-08

must_haves:
  truths:
    - "The codebase has zero `LootboxTicketRoll` event declarations and zero `emit LootboxTicketRoll` sites."
    - "`LootBoxOpened` emits a real per-lootbox `lootboxIndex` distinct from `day`, plus a `roundedUp` bool; its amount/burnie/bonusBurnie fields stay `uint256` wei (no narrowing, no `/ 1 ether`)."
    - "`BurnieLootOpen` and `JackpotTicketWin` each carry a `roundedUp` bool; no `preRollTickets` field is added to any event."
    - "`_resolveLootboxCommon` has no `index != type(uint48).max` branch; the ticket-award path is a single unified flow."
    - "Auto-resolve callers (`resolveLootboxDirect`, `resolveRedemptionLootbox`) emit no `LootBoxOpened` and the manual cold-bust WWXRP consolation only fires for manual callers."
    - "Storage layout is byte-identical to v39 baseline `6a7455d1`; no new state, modifiers, or entry points."
  artifacts:
    - path: "contracts/modules/DegenerusGameLootboxModule.sol"
      provides: "Restructured LootBoxOpened + BurnieLootOpen events, LootboxTicketRoll deleted, sentinel retired, auto-resolve callers silent"
      contains: "event LootBoxOpened"
    - path: "contracts/modules/DegenerusGameJackpotModule.sol"
      provides: "JackpotTicketWin gains roundedUp; _jackpotTicketRoll captures roundedUp; 3 emit sites supply it"
      contains: "event JackpotTicketWin"
    - path: "contracts/interfaces/IDegenerusGameModules.sol"
      provides: "LootboxTicketRoll deleted from IDegenerusGameLootboxModule interface block"
      contains: "interface IDegenerusGameLootboxModule"
  key_links:
    - from: "_resolveLootboxCommon ticket-award path"
      to: "_queueTickets / manual cold-bust consolation"
      via: "unconditional _queueTickets(player, targetLevel, whole, false) + consolation under if (emitLootboxEvent)"
      pattern: "_queueTickets\\(player, targetLevel, whole, false\\)"
    - from: "resolveLootboxDirect / resolveRedemptionLootbox"
      to: "_resolveLootboxCommon"
      via: "index arg 0, emitLootboxEvent arg false"
      pattern: "emitLootboxEvent"
    - from: "_jackpotTicketRoll roundedUp local"
      to: "JackpotTicketWin emit"
      via: "captured at the whole += 1 site, threaded to the emit"
      pattern: "emit JackpotTicketWin"
---

<objective>
Land the full Phase 277 contract surface change in one batched, user-approved commit: delete the `LootboxTicketRoll` event (interface + contract), restructure `LootBoxOpened` (fix the `index`/`day` mislabel, add `roundedUp`, keep fields wide), add `roundedUp` to `BurnieLootOpen` and `JackpotTicketWin`, retire the `index != type(uint48).max` sentinel in `_resolveLootboxCommon`, and make auto-resolve callers emit nothing.

Purpose: Consolidate the v39.0-additive `LootboxTicketRoll` event into the existing per-action events and remove the now-purposeless behavior-gating sentinel, with zero gas added to the advanceGame chain and zero truncation risk.
Output: Three modified contract files, one batched diff presented to the user, one user-approved commit `feat(277): event surface unification + sentinel retirement [EVT-UNI-01..08]` carrying a bytecode + gas delta in the message.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/277-event-surface-unification-sentinel-retirement-evt-uni/277-CONTEXT.md
@.planning/phases/277-event-surface-unification-sentinel-retirement-evt-uni/277-PATTERNS.md

<authority_note>
`277-CONTEXT.md` `<decisions>` is the ONLY authoritative source for implementation decisions. ROADMAP.md Phase 277 and REQUIREMENTS.md §EVT-UNI texts are STALE for EVT-UNI-02/-03/-06 — they describe a superseded approach (`uint32 burnie`/`uint16 bonus` narrowing, `preRollTickets` field, auto-resolve `LootBoxOpened` emission). Where roadmap/requirements text conflicts with CONTEXT.md, CONTEXT.md wins. Correcting the roadmap/requirements text is a deferred docs-only follow-up, NOT part of this plan.
</authority_note>

<code_state_correction>
PATTERNS.md and CONTEXT.md (D-277-CONSOLATION-GATE-01) both assert that the two auto-resolve callers currently pass `emitLootboxEvent = false`. **This is FACTUALLY WRONG against the current code.** Verified at `DegenerusGameLootboxModule.sol`: `_resolveLootboxCommon` param order is `(player, day, index, amount, targetLevel, currentLevel, seed, presale, allowWhalePass, allowLazyPass, emitLootboxEvent, allowBoons, distressEth, totalPackedEth)` — `emitLootboxEvent` is the 11th arg. `resolveLootboxDirect` (call at :714) and `resolveRedemptionLootbox` (call at :750) both currently pass the 11th arg as `true`, so they DO emit `LootBoxOpened` today; only `LootboxTicketRoll` and the cold-bust consolation are gated off for them by the `index != type(uint48).max` sentinel.

The CONTEXT.md *decision intent* is unambiguous and unchanged: D-277-AR-SILENT-01 = auto-resolve emits NO `LootBoxOpened`; D-277-CONSOLATION-GATE-01 = the manual cold-bust consolation stays manual-only via `emitLootboxEvent`. Task 1 therefore explicitly FLIPS the 11th arg of both auto-resolve calls from `true` to `false` (in addition to changing the `index` arg from `type(uint48).max` to `0`). This is the correct way to honor the decision intent — after the flip, `emitLootboxEvent` IS `true` for manual / `false` for auto-resolve exactly as D-277-CONSOLATION-GATE-01 describes, and D-277-CONSOLATION-GATE-01's gate logic becomes correct rather than relying on a false premise.
</code_state_correction>

<interfaces>
<!-- Current contract state extracted from the codebase. Executor uses these directly. -->

`_resolveLootboxCommon` signature (DegenerusGameLootboxModule.sol):
`function _resolveLootboxCommon(address player, uint32 day, uint48 index, uint256 amount, uint24 targetLevel, uint24 currentLevel, uint256 seed, bool presale, bool allowWhalePass, bool allowLazyPass, bool emitLootboxEvent, bool allowBoons, uint256 distressEth, uint256 totalPackedEth) private returns (uint32 futureTickets, uint256 burnieAmount, uint256 bonusBurnie)`

Current `LootBoxOpened` (DegenerusGameLootboxModule.sol, NatSpec + def):
`event LootBoxOpened(address indexed player, uint32 indexed index, uint256 amount, uint24 futureLevel, uint32 futureTickets, uint256 burnie, uint256 bonusBurnie)` — the emit passes `day` into the `index`-named slot (the mislabel).

Current `BurnieLootOpen`:
`event BurnieLootOpen(address indexed player, uint32 indexed index, uint256 burnieAmount, uint24 ticketLevel, uint32 tickets, uint256 burnieReward)`

Current `LootboxTicketRoll` (to delete from BOTH the contract event block AND the `IDegenerusGameLootboxModule` interface in `IDegenerusGameModules.sol`):
`event LootboxTicketRoll(address indexed player, uint48 indexed lootboxIndex, uint32 preRollTickets, bool roundedUp)`

Current `JackpotTicketWin` (DegenerusGameJackpotModule.sol):
`event JackpotTicketWin(address indexed winner, uint24 indexed ticketLevel, uint16 indexed traitId, uint32 ticketCount, uint24 sourceLevel, uint256 ticketIndex)` — `traitId` is already the 3rd indexed topic, so `roundedUp` MUST be non-indexed.

Hoisted Bernoulli locals in `_resolveLootboxCommon` (already outside the sentinel gate per D-275-HOIST-01): `uint32 scaledPre`, `uint32 whole`, `uint32 frac`, `bool roundedUp` — `roundedUp` is set `true` inside `if (frac != 0 && (uint16(seed >> 152) % uint16(TICKET_SCALE)) < uint16(frac))`.

`_queueTickets(buyer, targetLevel, quantity, rngBypass)` at `DegenerusGameStorage.sol:562` — early-returns on `quantity == 0`, emits `TicketsQueued` unconditionally otherwise.
</interfaces>

<discipline>
Per project CLAUDE.md + feedback memory:
- Only read/edit contracts from `contracts/` — stale copies exist elsewhere.
- `feedback_no_history_in_comments.md` — updated NatSpec/comments describe what IS, never what changed.
- `feedback_no_dead_guards.md` — the sentinel retirement must leave ZERO unreachable branches.
- `feedback_gas_worst_case.md` — the gas-delta task derives the theoretical worst case FIRST, then benchmarks.
- `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md` — all contract edits batched into ONE diff, presented to the user, committed only after explicit user approval. Contract changes are NEVER pre-approved.
</discipline>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete LootboxTicketRoll, restructure LootBoxOpened/BurnieLootOpen, retire the sentinel, silence auto-resolve callers</name>
  <files>contracts/modules/DegenerusGameLootboxModule.sol, contracts/interfaces/IDegenerusGameModules.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameLootboxModule.sol (full file — event defs at :58-90 + :134-159, _resolveLootboxCommon NatSpec/sig at :865-934, sentinel branch + emit at :1019-1095, callers openLootBox :600-632 / openBurnieLootBox :660-698 / resolveLootboxDirect :703-730 / resolveRedemptionLootbox :739-766)
    - contracts/interfaces/IDegenerusGameModules.sol:260-285 (IDegenerusGameLootboxModule interface block with the LootboxTicketRoll def)
    - contracts/storage/DegenerusGameStorage.sol:560-590 (_queueTickets — confirm it early-returns on quantity == 0 and emits TicketsQueued unconditionally)
    - .planning/phases/277-event-surface-unification-sentinel-retirement-evt-uni/277-CONTEXT.md (D-277-EVT-WIDE-01, D-277-NO-PREROLL-01, D-277-ROUNDEDUP-01, D-277-AR-SILENT-01, D-277-CONSOLATION-GATE-01, D-277-AR-INDEX-01)
    - .planning/phases/277-event-surface-unification-sentinel-retirement-evt-uni/277-PATTERNS.md (Changes 1-6 and Change 10)
  </read_first>
  <action>
    Apply all of the following to `DegenerusGameLootboxModule.sol` and `IDegenerusGameModules.sol` (EVT-UNI-01, EVT-UNI-02, EVT-UNI-03, EVT-UNI-05, EVT-UNI-06, EVT-UNI-08):

    (a) EVT-UNI-01 — DELETE the `LootboxTicketRoll` event: remove its NatSpec block + `event LootboxTicketRoll(...)` def from `DegenerusGameLootboxModule.sol` (currently at :134-159) AND from the `IDegenerusGameLootboxModule` interface block in `IDegenerusGameModules.sol` (currently :267-281 — the NatSpec block + def). Grep-confirm afterward that `LootboxTicketRoll` appears nowhere in `contracts/`.

    (b) EVT-UNI-02 + D-277-EVT-WIDE-01 + D-277-ROUNDEDUP-01 — RESTRUCTURE `LootBoxOpened`. Fix the mislabel: the current 2nd field is named `index` but the emit passes `day`. Replace it with TWO distinct fields — a real `lootboxIndex` carrying the `uint48 index` param value, and a separate `day` field. Add ONE new field `bool roundedUp` (non-indexed). Do NOT add `preRollTickets` (D-277-NO-PREROLL-01). Keep `amount`, `burnie`, `bonusBurnie` as `uint256` wei — the EVT-UNI-02 `uint32 burnie`/`uint16 bonus` narrowing is REJECTED (D-277-EVT-WIDE-01: event data words are 32 bytes regardless, narrowing saves zero gas and risks truncation). Keep `futureLevel` as `uint24` and `futureTickets` as `uint32`. Final field layout (planner decision): `event LootBoxOpened(address indexed player, uint48 indexed lootboxIndex, uint32 day, uint256 amount, uint24 futureLevel, uint32 futureTickets, uint256 burnie, uint256 bonusBurnie, bool roundedUp)`. Planner decision on indexed topics: `player` + `lootboxIndex` are the two indexed topics (keeps topic count at 2, identical to today — the mislabeled `index` topic is replaced by the real `lootboxIndex`); `day` becomes a non-indexed data field (manual `openLootBox` is not on the advanceGame chain, but adding a 3rd indexed topic is +375 gas/emit for filterability the existing consumers did not have, so keep it flat). Update the `LootBoxOpened` NatSpec `@param` list to match the new field set, describing what IS (no change-history language). At the emit site (currently :1080-1090, inside `if (emitLootboxEvent)`): pass `index` (the `uint48 index` param, now purely an event identifier) into `lootboxIndex`, `day` into `day`, and the live `roundedUp` local (from the hoisted Bernoulli block) as the last arg. Feed `amount`, `burnieAmount`, `bonusBurnie` wide as `uint256` wei — NO `/ 1 ether` divisions.

    (c) EVT-UNI-03 + D-277-NO-PREROLL-01 + D-277-ROUNDEDUP-01 — Add ONLY `bool roundedUp` (non-indexed) to `BurnieLootOpen`; do NOT add `preRollTickets`. New def: `event BurnieLootOpen(address indexed player, uint32 indexed index, uint256 burnieAmount, uint24 ticketLevel, uint32 tickets, uint256 burnieReward, bool roundedUp)`. `_resolveLootboxCommon` must surface the `roundedUp` local to the `openBurnieLootBox` caller — extend the return tuple from `(uint32 futureTickets, uint256 burnieAmount, uint256 bonusBurnie)` to `(uint32 futureTickets, uint256 burnieAmount, uint256 bonusBurnie, bool roundedUp)` and update the `return` statement + the function's `@return` NatSpec accordingly. Update both manual destructure sites: `openBurnieLootBox` currently destructures `(uint32 tickets, uint256 burnieReward, )` at the call — change to `(uint32 tickets, uint256 burnieReward, , bool roundedUp)` and pass `roundedUp` to the `BurnieLootOpen` emit (currently :688-695). The `openLootBox` call site does not currently destructure the tuple — leave it not destructuring (the trailing `bool` is simply ignored there). Update the `BurnieLootOpen` NatSpec `@param` list for the new field.

    (d) EVT-UNI-05 + D-277-CONSOLATION-GATE-01 + D-277-AR-SILENT-01 — RETIRE the `if (index != type(uint48).max)` branch at :1046 inside the `if (futureTickets != 0)` block. The Phase-275-hoisted shared Bernoulli locals (`scaledPre`/`whole`/`frac`/`roundedUp` at :1038-1044) STAY exactly as-is — they already sit outside the sentinel gate. Replace the entire `if (index != type(uint48).max) { ... } else { ... }` construct with: an UNCONDITIONAL `_queueTickets(player, targetLevel, whole, false);` (it early-returns on `whole == 0`, giving the silent auto-resolve cold-bust for free), followed by MOVING the manual cold-bust consolation under the existing `if (emitLootboxEvent)` gate that already wraps the `LootBoxOpened` emit. Concretely: inside the `if (emitLootboxEvent)` block (currently at :1080), before/around the `LootBoxOpened` emit, add `if (whole == 0) { wwxrp.mintPrize(player, LOOTBOX_WWXRP_CONSOLATION); emit LootBoxWwxrpReward(player, day, amount, LOOTBOX_WWXRP_CONSOLATION); }`. Delete the now-removed `emit LootboxTicketRoll(...)` line (was :1060). Result: zero `index != type(uint48).max` branch, zero leftover unreachable branch (`feedback_no_dead_guards.md`), the `uint48 index` param keeps only its event-identifier role on the manual `LootBoxOpened` emit.

    (e) EVT-UNI-05 + EVT-UNI-06 + D-277-AR-INDEX-01 + D-277-AR-SILENT-01 — Update the two auto-resolve callers. In `resolveLootboxDirect` (call at :714) and `resolveRedemptionLootbox` (call at :750): change the 3rd arg (`index`) from `type(uint48).max` to `0` (D-277-AR-INDEX-01: clean default; the value gates nothing and is emitted nowhere now). CRITICAL — also change the 11th arg (`emitLootboxEvent`) from `true` to `false`. NOTE: the current code passes the 11th arg as `true` for both auto-resolve callers (verified — see `<code_state_correction>` in `<context>`); CONTEXT.md/PATTERNS.md incorrectly assumed it was already `false`. After this flip, auto-resolve emits NO `LootBoxOpened` (D-277-AR-SILENT-01) and the manual cold-bust consolation moved in step (d) correctly fires for manual callers only. Manual callers `openLootBox` (call at :616) and `openBurnieLootBox` (call at :671) already pass a real `index` and `emitLootboxEvent = true` — UNCHANGED.

    (f) NatSpec / comment cleanup (`feedback_no_history_in_comments.md` — describe what IS): Update the `_resolveLootboxCommon` `@param index` NatSpec (currently :867-875) — drop the sentinel/dual-purpose description; `index` is the per-lootbox event identifier on the manual `LootBoxOpened` emit only. Update the `@param emitLootboxEvent` NatSpec (currently :898) — it now gates the `LootBoxOpened` emit AND the manual cold-bust WWXRP consolation. Delete/rewrite the inline comments that reference the sentinel and `LootboxTicketRoll` (the comment block at :1030-1052 and the auto-resolve-path comment at :1061-1064). The bit-allocation NatSpec at :880-892 (`bits[152..167]` consumed on both paths) is already accurate per D-275-NATSPEC-01 — verify it still reads correctly post-edit and leave it unchanged if so.
  </action>
  <verify>
    <automated>grep -rn "LootboxTicketRoll" contracts/ ; test -z "$(grep -rn 'LootboxTicketRoll' contracts/)" && echo NO_LOOTBOXTICKETROLL ; grep -cn "index != type(uint48).max" contracts/modules/DegenerusGameLootboxModule.sol ; npx hardhat compile 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "LootboxTicketRoll" contracts/` returns zero lines (event def deleted from both `DegenerusGameLootboxModule.sol` and `IDegenerusGameModules.sol`; emit site deleted).
    - `grep -c "index != type(uint48).max" contracts/modules/DegenerusGameLootboxModule.sol` returns `0`.
    - `grep -c "type(uint48).max" contracts/modules/DegenerusGameLootboxModule.sol` returns `0` (auto-resolve callers no longer pass the sentinel).
    - `LootBoxOpened` def contains a `lootboxIndex` field and a separate `day` field and a `bool roundedUp` field; it still contains `uint256 amount`, `uint256 burnie`, `uint256 bonusBurnie` (NOT narrowed); it does NOT contain `preRollTickets`.
    - `BurnieLootOpen` def contains `bool roundedUp` and does NOT contain `preRollTickets`; all pre-existing fields unchanged.
    - `_resolveLootboxCommon` return tuple has 4 elements ending in `bool roundedUp`; the `return` statement returns 4 values.
    - Both `resolveLootboxDirect` and `resolveRedemptionLootbox` calls to `_resolveLootboxCommon` pass `0` as the 3rd arg and `false` as the 11th arg.
    - The manual cold-bust consolation (`wwxrp.mintPrize(player, LOOTBOX_WWXRP_CONSOLATION)` + `emit LootBoxWwxrpReward`) appears exactly once and is inside the `if (emitLootboxEvent)` block.
    - `_queueTickets(player, targetLevel, whole, false)` is called unconditionally in the `if (futureTickets != 0)` block (not inside any `index`-conditional branch).
    - `npx hardhat compile` succeeds with no errors.
    - No comment in the edited regions contains change-history language ("was", "previously", "used to", "changed from", "no longer" describing a code change).
  </acceptance_criteria>
  <done>
    `LootboxTicketRoll` is gone from contracts and interface; `LootBoxOpened`/`BurnieLootOpen` are restructured per D-277-EVT-WIDE-01/D-277-NO-PREROLL-01/D-277-ROUNDEDUP-01; the `index != type(uint48).max` sentinel is retired with no dead branch; auto-resolve callers pass `index=0` + `emitLootboxEvent=false` and emit nothing; the contract compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add roundedUp to JackpotTicketWin and thread it through all three emit sites</name>
  <files>contracts/modules/DegenerusGameJackpotModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameJackpotModule.sol (JackpotTicketWin def + NatSpec at :79-95, trait-matched emit at :705-712, near/far-future coin emit at :1008-1015, _jackpotTicketRoll Bernoulli predicate + emit at :2225-2255)
    - contracts/modules/DegenerusGameLootboxModule.sol:1038-1044 (the Bernoulli roundedUp capture pattern to mirror — `bool roundedUp = false;` then `roundedUp = true;` inside the predicate `if`)
    - .planning/phases/277-event-surface-unification-sentinel-retirement-evt-uni/277-CONTEXT.md (D-277-ROUNDEDUP-01, "JackpotTicketWin.roundedUp capture mechanics" discretion item)
    - .planning/phases/277-event-surface-unification-sentinel-retirement-evt-uni/277-PATTERNS.md (Changes 7-9 + Shared Pattern "Bernoulli roundedUp capture")
  </read_first>
  <action>
    Apply EVT-UNI-04 + D-277-ROUNDEDUP-01 to `DegenerusGameJackpotModule.sol`:

    (a) Add `bool roundedUp` as the LAST, NON-INDEXED field of `JackpotTicketWin`. New def: `event JackpotTicketWin(address indexed winner, uint24 indexed ticketLevel, uint16 indexed traitId, uint32 ticketCount, uint24 sourceLevel, uint256 ticketIndex, bool roundedUp)`. `roundedUp` MUST be non-indexed — `traitId` already occupies the 3rd (and final allowed) indexed topic slot. Update the `JackpotTicketWin` NatSpec to document `roundedUp` (describe what IS: true iff the BAF `_jackpotTicketRoll` Bernoulli round-up incremented the whole-ticket count; false on the trait-matched paths which have a zero fractional part by construction).

    (b) In `_jackpotTicketRoll` (Bernoulli predicate at ~:2229-2239): the predicate currently does `if (frac != 0 && (uint16(entropy >> 200) % uint16(TICKET_SCALE)) < uint16(frac)) { unchecked { whole += 1; } }` with NO `roundedUp` bool. Mirror the LootboxModule capture pattern exactly: declare `bool roundedUp = false;` immediately before the predicate, and set `roundedUp = true;` inside the `if` block alongside the `whole += 1`. Do NOT change the Bernoulli math (`entropy >> 200`, `% uint16(TICKET_SCALE)`, `< uint16(frac)`) — only add the bool capture.

    (c) Thread `roundedUp` to the `JackpotTicketWin` emit inside `_jackpotTicketRoll` (currently at ~:2246-2253) as the new last arg.

    (d) The two trait-matched emit sites — the bonus-trait path (currently :705-712) and the near/far-future coin path (currently :1008-1015) — pass `false` as the new last `roundedUp` arg. These paths award integer ticket counts then emit a `× TICKET_SCALE` scaled value, so the fractional part is zero by construction; `false` is correct.
  </action>
  <verify>
    <automated>grep -c "emit JackpotTicketWin" contracts/modules/DegenerusGameJackpotModule.sol ; grep -n "bool roundedUp" contracts/modules/DegenerusGameJackpotModule.sol ; npx hardhat compile 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `JackpotTicketWin` def's final field is `bool roundedUp` and it is NOT marked `indexed`.
    - `JackpotTicketWin` still has exactly 3 `indexed` params (`winner`, `ticketLevel`, `traitId`).
    - `_jackpotTicketRoll` declares `bool roundedUp = false;` before the Bernoulli predicate and sets `roundedUp = true;` inside the `if` that does `whole += 1`.
    - The Bernoulli predicate expression in `_jackpotTicketRoll` still reads `entropy >> 200` and `% uint16(TICKET_SCALE)` and `< uint16(frac)` — unchanged math.
    - `grep -c "emit JackpotTicketWin" contracts/modules/DegenerusGameJackpotModule.sol` returns `3`; all three emits pass a 7th arg; the `_jackpotTicketRoll` emit passes the captured `roundedUp` local, the other two pass literal `false`.
    - `npx hardhat compile` succeeds with no errors.
    - No comment in the edited regions contains change-history language.
  </acceptance_criteria>
  <done>
    `JackpotTicketWin` carries a non-indexed `roundedUp` bool; `_jackpotTicketRoll` captures it with the LootboxModule-mirrored pattern; all 3 emit sites supply it (real value in `_jackpotTicketRoll`, `false` on the two trait-matched paths); the contract compiles.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Derive gas worst-case, present batched diff, await user approval, commit</name>
  <read_first>
    - feedback_gas_worst_case.md
    - feedback_no_contract_commits.md
    - feedback_batch_contract_approval.md
    - feedback_never_preapprove_contracts.md
    - feedback_manual_review_before_push.md
  </read_first>
  <what-built>
    All Phase 277 contract changes from Tasks 1-2: `LootboxTicketRoll` deleted (interface + contract), `LootBoxOpened` restructured (mislabel fixed, `roundedUp` added, fields kept wide), `roundedUp` added to `BurnieLootOpen` and `JackpotTicketWin`, the `index != type(uint48).max` sentinel retired in `_resolveLootboxCommon`, auto-resolve callers (`resolveLootboxDirect`, `resolveRedemptionLootbox`) made silent (`index=0`, `emitLootboxEvent=false`).

    Before presenting the diff, derive the THEORETICAL GAS WORST CASE FIRST, then benchmark to confirm direction (`feedback_gas_worst_case.md`):
    - Manual `openLootBox` path: deletes one `LootboxTicketRoll` LOG3 (375 + 2·375 + payload), adds only one `roundedUp` data word (+32 bytes = +256 gas) to `LootBoxOpened`, plus one `day` word that was already emitted into the mislabeled slot (net-neutral on payload count, the field just gets a correct name). Expected: net gas-NEGATIVE.
    - advanceGame-chain auto-resolve path (`resolveLootboxDirect` reachable via `processCoinflipPayouts`): `emitLootboxEvent` flips `true`→`false`, so a full `LootBoxOpened` LOG3 is REMOVED from this path; sentinel branch collapses to a straight-line `_queueTickets`. Expected: net gas-NEGATIVE for the lootbox portion on the block-gas-sensitive chain.
    - `JackpotTicketWin` on the advanceGame chain (`_jackpotTicketRoll`): +1 non-indexed data word (+32 bytes ≈ +256 gas). Genuinely new information, unavoidable, minimal.
    Run `npx hardhat compile` and capture the bytecode size delta for `DegenerusGameLootboxModule` and `DegenerusGameJackpotModule` vs the current HEAD. Capture a gas benchmark for the manual `openLootBox` path if an existing gas test covers it (`test/gas/LootboxOpenGas.test.js`). Record the worst-case derivation + measured deltas — they go in the commit message.
  </what-built>
  <how-to-verify>
    1. Run `git --no-pager diff --stat contracts/` and confirm exactly 3 files changed: `contracts/modules/DegenerusGameLootboxModule.sol`, `contracts/modules/DegenerusGameJackpotModule.sol`, `contracts/interfaces/IDegenerusGameModules.sol`.
    2. Run `git --no-pager diff contracts/` and review the full batched diff.
    3. Confirm: no `LootboxTicketRoll` anywhere; `LootBoxOpened`/`BurnieLootOpen`/`JackpotTicketWin` have `roundedUp`; no `index != type(uint48).max`; auto-resolve callers pass `0` + `false`; no change-history language in comments; no narrowed event fields.
    4. Review the derived gas worst-case + measured bytecode/gas deltas reported by the executor.
    5. Approve the diff explicitly, or describe required changes.
  </how-to-verify>
  <action>
    After explicit user approval ONLY: stage exactly the three contract files and commit with:
    `feat(277): event surface unification + sentinel retirement [EVT-UNI-01..08]`
    The commit body MUST include: the per-path gas worst-case derivation, the measured bytecode size delta for both modules, the measured `openLootBox` gas delta (or a note that no gas test covers it), and a one-line note that breaking event topic-hashes are accepted per D-40N-EVT-BREAK-01 (EVT-UNI-08). Do NOT push. Do NOT stage test files or any other file. If the user requests changes, apply them and re-present the diff — do NOT commit until approved.
  </action>
  <resume-signal>Type "approved" to commit the batched contract diff, or describe the changes you want.</resume-signal>
  <acceptance_criteria>
    - `git diff --stat contracts/` shows exactly the three named files and no others.
    - The gas worst-case derivation was produced BEFORE benchmarking and is recorded.
    - The commit is created ONLY after explicit user approval, with message `feat(277): event surface unification + sentinel retirement [EVT-UNI-01..08]` and a body containing the gas/bytecode deltas + the D-40N-EVT-BREAK-01 note.
    - Nothing is pushed; no non-contract files are staged.
  </acceptance_criteria>
  <done>
    The user has reviewed and approved the batched 3-file contract diff; one commit `feat(277): event surface unification + sentinel retirement [EVT-UNI-01..08]` exists carrying the gas + bytecode delta report; nothing pushed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| off-chain indexer → event log topic-hashes | Indexers subscribe to `LootBoxOpened` / `BurnieLootOpen` / `JackpotTicketWin` by topic hash; changing the event signature changes the topic-0 hash. |
| external caller → `resolveLootboxDirect` / `resolveRedemptionLootbox` | Auto-resolve entry points reachable on the advanceGame chain (block-gas-limit-sensitive) and via decimator-claim / sDGNRS-redemption. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-277-01 | Tampering | `LootBoxOpened` field restructure (mislabel fix + `roundedUp`) | accept | Breaking topic-hash + ABI change is intentional and accepted per D-40N-EVT-BREAK-01 / EVT-UNI-08; pre-launch, indexer rebuild expected, no live indexer impact. Fields kept `uint256` wide (D-277-EVT-WIDE-01) specifically to AVOID introducing truncation of `burnie`/`bonusBurnie` wei values. |
| T-277-02 | Information Disclosure | `_jackpotTicketRoll` `roundedUp` capture | mitigate | `roundedUp` is derived purely from the existing in-function Bernoulli predicate result; no new state is read or written, no new entropy bits consumed. The capture mirrors the already-audited `DegenerusGameLootboxModule.sol:1038-1044` pattern byte-for-byte except the bool assignment. |
| T-277-03 | Denial of Service | sentinel retirement → unconditional `_queueTickets` on auto-resolve path | mitigate | `_queueTickets` (`DegenerusGameStorage.sol:562`) already early-returns on `quantity == 0`; the unconditional call adds no new revert path and no unbounded loop. Auto-resolve `emitLootboxEvent` flips to `false`, REMOVING a `LootBoxOpened` LOG3 from the advanceGame chain — net gas reduction, not addition, on the block-gas-sensitive path. |
| T-277-04 | Elevation of Privilege | new entry points / state / modifiers | accept | None added. Storage layout byte-identical to v39 baseline `6a7455d1`; event signature changes do not affect storage layout. No new admin/external mutation entry points. |
| T-277-05 | Repudiation | manual cold-bust consolation moves from sentinel gate to `emitLootboxEvent` gate | mitigate | After Task 1 flips auto-resolve `emitLootboxEvent` to `false`, the `if (emitLootboxEvent)` gate is `true` for exactly the two manual callers and `false` for exactly the two auto-resolve callers — 1:1 with the prior sentinel split, so the manual-keeps / auto-resolve-silent consolation asymmetry (D-40N-SILENT-01) is preserved with no behavior change to who receives the consolation. |
</threat_model>

<verification>
- `npx hardhat compile` succeeds.
- `grep -rn "LootboxTicketRoll" contracts/` returns nothing.
- `grep -c "index != type(uint48).max" contracts/modules/DegenerusGameLootboxModule.sol` returns 0.
- `grep -c "type(uint48).max" contracts/modules/DegenerusGameLootboxModule.sol` returns 0.
- `LootBoxOpened`, `BurnieLootOpen`, `JackpotTicketWin` each contain `roundedUp`; none contain `preRollTickets`.
- Exactly 3 contract files changed; one user-approved commit created; nothing pushed.
</verification>

<success_criteria>
- EVT-UNI-01: `LootboxTicketRoll` deleted from both the contract event block and the `IDegenerusGameLootboxModule` interface; zero emission sites.
- EVT-UNI-02: `LootBoxOpened` mislabel fixed (real `lootboxIndex` + separate `day`), `roundedUp` added, fields kept `uint256` wide (no narrowing, no `preRollTickets`).
- EVT-UNI-03: `BurnieLootOpen` gains only `roundedUp`.
- EVT-UNI-04: `JackpotTicketWin` gains non-indexed `roundedUp`; all 3 emit sites supply it; `_jackpotTicketRoll` captures it.
- EVT-UNI-05: `index != type(uint48).max` sentinel retired with no dead branch; auto-resolve callers pass `index=0`.
- EVT-UNI-06: auto-resolve stays silent — `emitLootboxEvent` flipped to `false` for both auto-resolve callers; no `LootBoxOpened` on auto-resolve.
- EVT-UNI-07: gas worst-case derived first, then benchmarked; bytecode + gas delta recorded in the commit message.
- EVT-UNI-08: breaking topic-hashes accepted; noted in the commit message.
- Contract compiles; exactly one batched user-approved commit; nothing pushed.
</success_criteria>

<output>
After completion, create `.planning/phases/277-event-surface-unification-sentinel-retirement-evt-uni/277-01-SUMMARY.md`
</output>
