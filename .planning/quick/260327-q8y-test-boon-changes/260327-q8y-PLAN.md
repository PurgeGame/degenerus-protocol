---
phase: quick
plan: 260327-q8y
type: execute
wave: 1
depends_on: []
files_modified:
  - test/edge/MultiBoon.test.js
autonomous: true
requirements: [BOON-MULTI]

must_haves:
  truths:
    - "A player can hold boons from two different categories simultaneously via deity issuance"
    - "A second deity boon in a different category does not clear the first category"
    - "Upgrade semantics still work within a single category (higher tier replaces lower)"
    - "checkAndClearExpiredBoon correctly reports hasAnyBoon when multiple categories active"
  artifacts:
    - path: "test/edge/MultiBoon.test.js"
      provides: "Multi-category boon coexistence tests"
      min_lines: 80
  key_links:
    - from: "test/edge/MultiBoon.test.js"
      to: "contracts/modules/DegenerusGameLootboxModule.sol"
      via: "issueDeityBoon + consumeCoinflipBoon/consumePurchaseBoost"
      pattern: "issueDeityBoon.*consumeCoinflipBoon|consumePurchaseBoost"
---

<objective>
Write tests for the boon system change in commit 004a9065 that removed single-category
exclusivity, allowing players to hold one boon per category simultaneously.

Purpose: The exclusivity gate (_activeBoonCategory check + early return in _rollLootboxBoons)
was removed, but no tests verify the new multi-category behavior. Without tests, a future
refactor could re-introduce exclusivity or break the upgrade semantics.

Output: test/edge/MultiBoon.test.js with tests covering multi-category boon coexistence
via deity boon issuance (the controllable path for testing).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@contracts/modules/DegenerusGameLootboxModule.sol (lines 1020-1090 — _rollLootboxBoons without exclusivity gate)
@contracts/modules/DegenerusGameBoonModule.sol (checkAndClearExpiredBoon, consumeCoinflipBoon, consumePurchaseBoost, consumeDecimatorBoost)
@contracts/DeityBoonViewer.sol (slot type lookup for deity boons)
@test/edge/WhaleBundle.test.js (reference for deity boon issuance pattern, settleRngDay, lockRngWithFulfilledWord helpers)
@test/helpers/deployFixture.js (deployFullProtocol fixture)
@test/helpers/testUtils.js (advanceToNextDay, getLastVRFRequestId, eth, getEvents)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create multi-category boon coexistence test suite</name>
  <files>test/edge/MultiBoon.test.js</files>
  <action>
Create a new test file test/edge/MultiBoon.test.js that verifies multiple boon categories
can coexist after the exclusivity removal (commit 004a9065).

Use the deity boon issuance path (issueDeityBoon) since it gives deterministic control over
which boon type is applied. Follow the pattern from test/edge/WhaleBundle.test.js for VRF
settlement and deity boon slot scanning.

Structure:
1. Import deployFullProtocol, restoreAddresses, eth, advanceToNextDay, advanceTime,
   getLastVRFRequestId, getEvents from existing helpers.

2. Replicate the settleRngDay and lockRngWithFulfilledWord helpers from WhaleBundle.test.js.

3. Create a helper function `issueSpecificBoon(game, viewer, deity, recipient, deployer, mockVRF, targetCategory)`
   that scans deity boon slots across days to find and issue a boon of a specific category
   (coinflip, purchase, whale, etc.). Use DeityBoonViewer for slot lookup (NOT lootboxModule
   directly — lootboxModule reads its own empty storage via delegatecall architecture).

   Boon type ranges for category detection:
   - Coinflip: types 1-3
   - Lootbox: types 5, 6, 22
   - Purchase: types 7, 8, 9
   - Decimator: types 13, 14, 15
   - Whale: types 16, 23, 24
   - Activity: types 17, 18, 19

4. Tests (describe "Multi-category boon coexistence"):

   a. "player can hold coinflip + purchase boons simultaneously"
      - Alice buys deity pass (24 ETH)
      - Settle RNG, issue coinflip boon to Bob via deity
      - Issue purchase boon to Bob via deity (same or later day)
      - Verify consumeCoinflipBoon returns non-zero BPS for Bob
      - After consume, verify consumePurchaseBoost still returns non-zero BPS
      (This is the core test: old code would have blocked the second boon)

   b. "player can hold coinflip + decimator boons simultaneously"
      - Same setup, issue coinflip + decimator boons
      - Verify both consumeCoinflipBoon and consumeDecimatorBoost return non-zero

   c. "upgrade semantics within category: higher tier replaces lower"
      - Issue a coinflip T1 boon (type 1, 5% = 500 BPS)
      - Issue a coinflip T2 boon (type 2, 10% = 1000 BPS)
      - consumeCoinflipBoon should return 1000 (higher tier kept)

   d. "checkAndClearExpiredBoon returns true when multiple categories active"
      - Issue two boons in different categories
      - Call checkAndClearExpiredBoon (via game proxy delegatecall — trigger
        via a lootbox purchase that invokes _rollLootboxBoons, or just verify
        that the consume functions return non-zero which proves the boons survived
        the cleanup call)

   e. "expiry of one category does not affect another"
      - Issue coinflip boon (2-day expiry) + purchase boon (4-day expiry) on same day
      - Advance 3 days (past coinflip expiry, within purchase expiry)
      - consumeCoinflipBoon returns 0 (expired)
      - consumePurchaseBoost returns non-zero (still active)

Important implementation notes:
- Deity boons are same-day only (deityDay != currentDay clears them), so for tests
  that issue multiple boons, issue them all on the SAME day by calling
  lockRngWithFulfilledWord once, then issuing multiple boons before advancing.
- For the expiry test (e), use lootbox-sourced boons via _rollLootboxBoons (deity
  boons expire on day change). Alternative: issue deity boons and test within same
  day for coexistence, then use a separate approach for expiry. Simplest: just verify
  within same day for tests a-d, and for test e, trigger two lootbox resolutions
  that happen to award different categories (harder to control). If lootbox path is
  too non-deterministic, skip test e and document it as a TODO — the core value is
  tests a-d proving coexistence.
- The consume functions are on the game contract (delegatecall to BoonModule).
  Call them via game.consumeCoinflipBoon(player), game.consumePurchaseBoost(player), etc.
  These are NOT access-controlled to caller (they check the `player` arg state).
  Wait — check access: the DegenerusGame.test.js shows consumeCoinflipBoon reverts
  for non-authorized callers. These are likely restricted to specific caller contracts
  (coinflip, purchase module). If so, test coexistence by verifying boon state via
  DeityBoonViewer or by reading boonPacked directly. Alternative: issue boons and verify
  via the actual game flow (purchase + VRF resolution + check events). Simplest: just
  check that issuing the second boon succeeds and read boon state via a view function.
  Check if there's a view function for boon state on the game contract.

- If consume functions are access-restricted, verify coexistence by:
  1. Issue boon A (category X) to Bob
  2. Issue boon B (category Y) to Bob
  3. Use DeityBoonViewer or a game view to confirm both are stored
  4. Or: trigger a coinflip for Bob (which calls consumeCoinflipBoon internally)
     and verify BoonConsumed event, then trigger a purchase for Bob and verify
     purchase boost was applied via price reduction

Set timeout to 120_000 on the describe block (deity boon scanning loops through days).
  </action>
  <verify>
    <automated>npx hardhat test test/edge/MultiBoon.test.js --no-compile 2>&1 | tail -20</automated>
  </verify>
  <done>
All tests pass. At minimum: a test proving two different boon categories can be held
simultaneously by one player (the behavior that was impossible before the exclusivity
removal).
  </done>
</task>

</tasks>

<verification>
npx hardhat test test/edge/MultiBoon.test.js
All tests pass with no reverts on valid multi-boon scenarios.
</verification>

<success_criteria>
- test/edge/MultiBoon.test.js exists and passes
- At least one test demonstrates a player holding boons from 2+ categories simultaneously
- Tests use deity boon issuance for deterministic boon type control
- No changes to contract code
</success_criteria>

<output>
After completion, create `.planning/quick/260327-q8y-test-boon-changes/260327-q8y-SUMMARY.md`
</output>
