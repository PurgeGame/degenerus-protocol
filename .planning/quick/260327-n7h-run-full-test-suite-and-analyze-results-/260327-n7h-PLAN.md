---
phase: quick
plan: 260327-n7h
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
must_haves:
  truths:
    - "Full test suite (35 test files across 7 categories) runs to completion"
    - "All passing/failing/pending counts are captured and reported"
    - "Any failures are analyzed with root cause and recommended fix"
  artifacts: []
  key_links: []
---

<objective>
Run the complete Hardhat test suite and analyze results after recent commits (price feed governance, ERC-20 Approval fix, test updates). Three test files have unstaged modifications -- these must be included in the run.

Purpose: Verify the codebase is green after the recent wave of changes (419b134, 7f4c4d3, 1ee764b) and surface any regressions before the pre-audit hardening milestone continues.
Output: Pass/fail summary with root-cause analysis of any failures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
Working tree has 3 unstaged test file modifications:
- test/deploy/DeployScript.test.js
- test/unit/DGNRSLiquid.test.js
- test/unit/DegenerusDeityPass.test.js

Test execution order (from hardhat.config.js): access -> deploy -> unit -> integration -> edge -> validation -> gas

The `npm test` script covers: unit, integration, deploy, access, edge, gas, adversarial.
The hardhat config default ordering covers: access, deploy, unit, integration, edge, validation, gas.
Use `npx hardhat test` (no args) to run ALL categories via the config ordering, which is the most complete.

STATE.md blocker note: ContractAddresses.sol may have unstaged changes with different deploy addresses. This should not affect test runs since tests use local hardhat network, but be aware.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and capture results</name>
  <files></files>
  <action>
Run the full Hardhat test suite using `npx hardhat test` (uses the config-defined ordering across all 7 test categories: access, deploy, unit, integration, edge, validation, gas -- 35 test files total). The unstaged test file modifications will be picked up automatically.

Capture the full output. If the run takes longer than 5 minutes, that is normal for a full suite with compilation.

After the run completes, produce a structured analysis:

1. SUMMARY: total passing / failing / pending counts
2. BY CATEGORY: break down pass/fail counts per test directory (access, deploy, unit, integration, edge, validation, gas)
3. FAILURES (if any): for each failure, provide:
   - Test name and file
   - Error message
   - Root cause analysis (check if related to the 3 recent commits: price feed governance, ERC-20 Approval fix, test suite updates)
   - Recommended fix (specific, actionable)
4. REGRESSION CHECK: explicitly state whether the recent commits (419b134 price feed governance tests, 7f4c4d3 price feed governance feat, 1ee764b5 ERC-20 Approval fix) introduced any regressions
  </action>
  <verify>
    <automated>npx hardhat test 2>&1 | tail -5</automated>
  </verify>
  <done>Full test suite has been executed, all results captured, and any failures analyzed with root causes and fixes identified. A clear pass/fail verdict is delivered.</done>
</task>

</tasks>

<verification>
- Test suite ran to completion (did not crash/hang)
- Every test category executed (access, deploy, unit, integration, edge, validation, gas)
- Pass/fail/pending counts reported
- Any failures have root cause and fix recommendation
</verification>

<success_criteria>
Full test suite results are captured and analyzed. User has a clear picture of codebase health after recent commits and knows exactly what (if anything) needs fixing.
</success_criteria>

<output>
After completion, create `.planning/quick/260327-n7h-run-full-test-suite-and-analyze-results-/260327-n7h-SUMMARY.md`
</output>
