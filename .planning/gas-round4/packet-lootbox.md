# Gas round-4 packet — lootbox

Line numbers reference the AUDIT-TIME source (three gas rounds ago) — locate by code content, NOT line number.


## LOOTBOX-02 [unused_function / cold] L1024-L1039 — file: modules/DegenerusGameLootboxModule.sol
**Description:** External view `deityBoonSlots(address)` has ZERO reachable callers on the deployed system. DegenerusGame.sol has no passthrough for this selector and no catch-all module-forwarding fallback (only receive() at Game L2988); the off-chain viewer DeityBoonViewer.sol L95-119 REIMPLEMENTS the slot computation against Game.deityBoonData(deity) (Game L980) instead of calling the module. Calling the module address directly would read the module's own empty storage (test/edge/WhaleBundle.test.js L544 documents exactly this: 'deityBoonSlots on the lootbox module cannot be called'). The interface decl at IDegenerusGameModules.sol:399 is the only remaining reference.
**Change:** Delete deityBoonSlots (L1024-1039) from the module and the matching declaration at contracts/interfaces/IDegenerusGameModules.sol:399; update the module header comment at L37. Keep _deityBoonForSlot and _boonFromRoll (still used by issueDeityBoon).
**Excerpt (audit-time):**
```solidity
function deityBoonSlots(
    address deity
) external view returns (uint8[3] memory slots, uint8 usedMask, uint24 day) {
```
**Risk notes:** Verify no JS/forge harness attaches the module ABI to the GAME address and relies on this selector reaching the module via some route (repo grep found only viewer-based calls). Module-side bytecode only; DegenerusGame size unchanged.
**Skeptic reasoning:** Verified zero reachable callers: DegenerusGame has no fallback (only receive() at Game L2988) and its only msg.data-forwarding stubs are fixed-selector forwards to OTHER modules (claimDecimatorJackpotMany L1457, creditRedemptionDirect L2005), so no route delivers the deityBoonSlots selector to the module in the Game's storage context. DeityBoonViewer.sol L95-121 reimplements the slot computation against Game.deityBoonData (Game L980) — all JS tests call the viewer, and WhaleBundle.test.js L544 documents the module function is uncallable. Only other reference is the interface decl at IDegenerusGameModules.sol:399, removable in the same change. No scripts reference it.
**Implementation notes:** Delete module L1024-1039, the IDegenerusGameModules.sol:399 declaration, and update the module header comment L37. Keep _deityBoonForSlot and _boonFromRoll (used by issueDeityBoon L1075). Combine with LOOTBOX-15 since deityBoonSlots' 4x word reads disappear with it.
**Invariant impact:** none | **Risk:** low

## LOOTBOX-05 [redundant_check / warm] L1247-L1248, L729-L730, L1926-L1928, L2018 — file: modules/DegenerusGameLootboxModule.sol
**Description:** PriceLookupLib.priceForLevel (libraries/PriceLookupLib.sol L21-46) returns a non-zero constant on EVERY branch for EVERY uint24 input. Therefore: (a) L1248 `if (targetPrice == 0) revert E();` in _settleLootboxRoll is a dead revert on the per-roll path; (b) L730 `if (priceWei != 0)` in _resolvePresaleBox is always-true; (c) the `priceWei == 0` half of L1926 in _lootboxTicketCount is dead (and budgetWei != 0 is guaranteed by the L1246 rollAmount != 0 gate, since ticketBudget = amount*16100/10000 >= 1 for amount >= 1); (d) L2018 `if (passLevel == 0) return 0;` in _lazyPassPriceForLevel is dead — its only caller passes lazyPassLevel >= 1 (L1331: ternary forces >= 1; in fact currentLevel = level+1 >= 1 makes the `currentLevel == 0` arm itself dead).
**Change:** Remove the targetPrice==0 revert at L1248; flatten the L730 conditional; reduce L1926 to `if (budgetWei == 0)` or delete entirely; delete L2018 and simplify L1331 to `uint24 lazyPassLevel = currentLevel + 1;`.
**Excerpt (audit-time):**
```solidity
uint256 targetPrice = PriceLookupLib.priceForLevel(rollLevel);
if (targetPrice == 0) revert E();
```
**Risk notes:** Dominating fact is the library itself: every return statement in priceForLevel is a non-zero ether constant (verified L23-44). If the price table is ever edited to introduce a 0, these guards matter — but contracts are frozen at deploy.
**Skeptic reasoning:** Dominating fact verified: PriceLookupLib.priceForLevel (L21-46) returns a non-zero ether constant on EVERY branch for EVERY uint24 (0.01-0.24 ether; the level-0/100+/cycle branches all covered). (a) L1248 dead — also no downstream div-by-zero risk (L1907, L1961 divisors safe). (b) L730 always-true. (c) Both halves of L1926 dead at the SINGLE call site (L1864): amount >= 1 enforced at L1857, LOOTBOX_TICKET_ROLL_BPS = 16_100 (L241) so ticketBudget = amount*16100/10000 >= 1. (d) L2018 dead — sole caller L1332 passes lazyPassLevel >= 1; the L1331 simplification is identity-equivalent even in the impossible currentLevel==0 arm (ternary yields 1 = 0+1), so it is bit-exact.
**Implementation notes:** All four sub-changes safe. If deleting L1926 entirely, keep in mind _lootboxTicketCount is private with exactly one caller (grep-verified); document the >=1-wei precondition in its natspec.
**Invariant impact:** none | **Risk:** low

## LOOTBOX-06 [redundant_check / cold] L701-L702 — file: modules/DegenerusGameLootboxModule.sol
**Description:** _resolvePresaleBox's `if (amount == 0) return;` is dominated. Sole caller _openBoxBoth gates on `stored != 0` (L597), and the only writer of presaleBoxEth (DegenerusGameMintModule.sol L1597-1600) always packs a non-zero amount in bits [0:95]: applied = min(boxAmount, remaining) with boxAmount >= PRESALE_BOX_MIN (L1549) and remaining > 0 (L1558). A record with zero amount but non-zero flag/soldBefore bits is not constructible.
**Change:** Delete L702 (keep the L701 mask extraction).
**Excerpt (audit-time):**
```solidity
uint256 amount = stored & PRESALE_BOX_AMOUNT_MASK;
if (amount == 0) return;
```
**Risk notes:** Domination spans two modules (writer in MintModule L1561/1597, reader here) — Skeptic should re-verify no other presaleBoxEth writer exists (repo grep found only MintModule:1597).
**Skeptic reasoning:** Cross-module domination re-verified: the ONLY writers of presaleBoxEth in all of contracts/ are MintModule L1597-1600 (packs `applied` in bits [0:95], with applied >= min(PRESALE_BOX_MIN, remaining) >= 1 wei via the L1549 floor and L1558 remaining>0 gate; applied <= 50 ether so uint96 cast cannot truncate to zero) and the zeroing dequeue at LootboxModule L600. Therefore stored != 0 (the L597 caller gate) implies amount != 0 — a record with flag/soldBefore bits but zero amount is not constructible.
**Implementation notes:** Delete L702 only; keep the L701 mask extraction. Value is marginal — fold into a batch with the other approved removals rather than as a standalone edit.
**Invariant impact:** none | **Risk:** low

## LOOTBOX-07 [redundant_sload / warm] L1329, L1399, L1336/L1481, L1799 — file: modules/DegenerusGameLootboxModule.sol
**Description:** Redundant warm SLOADs of slot-0 `level` and of deityPassOwners.length inside one box resolution: (1) _resolveLootboxCommon already holds `currentLevel` as a param, yet _rollLootboxBoons re-reads `level + 1` at L1329; (2) _boonPoolStats re-reads `level` again at L1399 for priceForLevel(level); (3) deityPassOwners.length is SLOAD'd at L1336 and AGAIN at L1481 inside _boonPoolStats when deityEligible; (4) the whale-pass branch of _applyBoon re-reads `level + 1` at L1799 for the event. Each is a warm SLOAD (~100 gas) replaceable by parameter threading of values already on the stack.
**Change:** Add a `uint24 currentLevel` param to _rollLootboxBoons (passed from _resolveLootboxCommon L1193) and to _boonPoolStats (use currentLevel-1 where L1399 uses `level`); pass `deityPassOwners.length` (loaded at L1336) into _boonPoolStats and reuse at L1481. Optionally thread currentLevel into _applyBoon for the rare L1799 emit.
**Excerpt (audit-time):**
```solidity
uint24 currentLevel = level + 1;   // L1329 — caller already has it
...
uint256 priceWei = PriceLookupLib.priceForLevel(level);  // L1399 — third read
```
**Risk notes:** Pure value-threading within one tx; `level` cannot change between the reads (no external call mutates it mid-resolution). No RNG-window semantics change — the same values are consumed.
**Skeptic reasoning:** All four redundant warm reads verified (L1329 level, L1399 level via priceForLevel(level), L1336+L1481 deityPassOwners.length, L1799 level). Staleness check: between the caller's `level` read and the re-reads, the only calls are delegatecalls to the protocol boon module (writes boonPacked only — verified checkAndClearExpiredBoon/consumeActivityBoon bodies) and internal logic; nothing writes `level` or deityPassOwners mid-resolution, and no callee hands control to third-party code (creditFlip→_addDailyFlip, wwxrp.mintPrize→_mint, dgnrs.transferFromPool all verified internal-accounting-only). The currentLevel-1 substitution at L1399 is exact (currentLevel >= 1). Identical values consumed — no RNG-window semantics change.
**Implementation notes:** Thread `uint24 currentLevel` from _resolveLootboxCommon (L1193) into _rollLootboxBoons and _boonPoolStats; pass the L1336-loaded deityPassOwners.length into _boonPoolStats. SKIP the optional _applyBoon threading for L1799: _applyBoon's other caller issueDeityBoon has no currentLevel in hand, the emit is rare (whale-pass weight 8/1498), and the extra param costs more complexity than it saves.
**Invariant impact:** none | **Risk:** low

## LOOTBOX-08 [redundant_sload / warm] L642-L678 (skip-check L661-663) vs L505, L510, L596, L598 — file: modules/DegenerusGameLootboxModule.sol
**Description:** In the openHumanBoxes sweep, each opened entry pays duplicate warm SLOADs: the skip-check reads `lootboxEth[idx][player]` (L661) and `presaleBoxEth[idx][player]` (L662), then _openBoxBoth -> _openLootBoxLeg re-reads lootboxEth (L505) and presaleBoxEth (L596); `lootboxRngWordByIndex[idx]` is read per-index at L647, then re-read per-box at L510 and (presale leg) L598. Per opened box that is ~300 gas of warm re-reads while presale is outstanding, ~200 after.
**Change:** Refactor to internal variants that accept pre-loaded values: `_openLootBoxLegWith(player, index, packedWord, rngWord)` and a presale leg taking (stored, rngWord). The sweep loads `packed`, `stored`, and the per-index `word` once and passes them down; the manual openBox shell keeps loading them itself (no change to its cost).
**Excerpt (audit-time):**
```solidity
if (
    (lootboxEth[idx][player] & LB_AMOUNT_MASK) == 0 &&
    (!checkPresale || presaleBoxEth[idx][player] == 0)
) continue;
_openBoxBoth(player, idx, checkPresale);  // re-SLOADs all three
```
**Risk notes:** The pre-loaded values cannot go stale between the check and the open (same tx, no intervening external call before _openLootBoxLeg's read; note _openBoxBoth's lootbox leg CAN run external calls before the presale leg reads presaleBoxEth — so thread the presale `stored` only if re-verified that the lootbox-leg externals (coinflip/wwxrp/dgnrs/QUESTS) cannot reenter a presaleBoxEth write; with the rngLockedFlag entry-gate and the buy path gated on un-worded indices (MintModule L1588) a same-index presale write mid-open is impossible, but Skeptic should confirm.
**Skeptic reasoning:** Duplicate reads verified exactly as claimed (skip-check L661-662 + per-index L647 vs re-reads L505/L596/L510/L598). Cache safety proven on every threaded value: (1) lootboxEth packed word and rngWord — no external call between the sweep reads and their uses; (2) the presale `stored` read does sit after the lootbox leg's external calls, but is safe on TWO independent grounds: (a) every callee on that path (coinflip.creditFlip→_addDailyFlip, wwxrp.mintPrize→_mint with onlyMinter, dgnrs.transferFromPool onlyGame pure-accounting, quests.awardQuestStreakBonus onlyGame, boon-module delegatecalls) performs internal accounting with no control handoff to player code; (b) even under a hypothetical reentry, MintModule L1588 reverts any presaleBoxEth write at an index whose word is set, and the sweep only opens worded indices (L647 gate) — so the cached value cannot go stale; a reentrant double-resolve is additionally blocked because L600 zeroes before resolution and that ordering must be preserved.
**Implementation notes:** Hard constraints: (1) presale leg must keep read-then-zero-then-resolve order (current L596/L600/L601); (2) manual openBox shell keeps its own loads — only the sweep uses the pre-loaded variants; (3) module-only edit, but it restructures the guaranteed-non-reverting sweep body — re-run the openBoxes orphan-index and budget-boundary tests.
**Invariant impact:** none | **Risk:** low

## VIEWER-02 [unused_function / cold] L1024-L1041 — file: modules/DegenerusGameLootboxModule.sol
**Description:** Discovered while verifying the viewer's reachability: the LootboxModule's external view deityBoonSlots(address) has NO production dispatch path. DegenerusGame.sol contains no deityBoonSlots wrapper and no fallback dispatcher (grep-verified), so the module function is only callable directly on the module address — where it executes against the module's own EMPTY storage and returns meaningless data. The live read path is DegenerusGame.deityBoonData (DegenerusGame.sol L980) + the standalone DeityBoonViewer, which exists precisely so the Game (at the EIP-170 ceiling) does not need a wrapper. The module's internal _deityBoonForSlot/_boonFromRoll MUST stay (used by issueDeityBoon, warm path); only the external aggregator function is production-dead.
**Change:** Remove the external deityBoonSlots function from DegenerusGameLootboxModule.sol and its declaration from interfaces/IDegenerusGameModules.sol L399. Keep _deityBoonForSlot, _boonFromRoll, and issueDeityBoon untouched.
**Excerpt (audit-time):**
```solidity
function deityBoonSlots(
    address deity
) external view returns (uint8[3] memory slots, uint8 usedMask, uint24 day) {
```
**Risk notes:** Outside the three assigned files (flagging because the viewer analysis surfaced it). Verify no test harness delegatecalls this selector through a Game fixture before removal (test/ may exercise it via vm-level delegatecall); also confirm no off-chain tooling does eth_call with state-override against the module address. Does not touch RNG/solvency: the on-chain boon issuance path (issueDeityBoon → _deityBoonForSlot) is unchanged.
**Skeptic reasoning:** All reachability claims verified against source: (1) DegenerusGame.sol contains no deityBoonSlots wrapper and no fallback dispatcher (only receive() at L2988), so the module's external deityBoonSlots (LootboxModule L1024-1039) has no production dispatch path through the Game; (2) called directly on the module it reads the module's own empty storage (the repo's own test comments confirm this exact failure mode — WhaleBundle.test.js L62-65 and L544-547: 'deityBoonSlots on the lootbox module cannot be called directly'); (3) the live read path is Game.deityBoonData (L980) + the standalone DeityBoonViewer; (4) full-repo grep including contracts/test/ and contracts/mocks/ shows zero additional references — only the interface declaration (IDegenerusGameModules.sol L399), the module header comment (L37), and the viewer's own same-named function; (5) JS tests call viewer.deityBoonSlots exclusively, never the module's; (6) the warm-path internals stay: _deityBoonForSlot is still consumed by issueDeityBoon (L1075). Boon issuance and RNG derivation are untouched.
**Implementation notes:** Remove the module function (L1024-1039), the interface declaration (IDegenerusGameModules.sol L399 block), and the stale mention in the module header comment (L37). Keep _deityBoonForSlot, _boonFromRoll, issueDeityBoon, and Game.deityBoonData untouched. Run the JS suites touching boons (MultiBoon, WhaleBundle) post-removal as a smoke check — they use the viewer, so they should be unaffected.
**Invariant impact:** none | **Risk:** low

## RT-CLAIMS-02 [redundant_check / warm] L529-L531, L1175-L1177, L1216 — file: modules/DegenerusGameLootboxModule.sol
**Description:** Three `targetLevel < currentLevel` down-clamps are unreachable. _rollTargetLevel (L1094-L1108) returns `baseLevel + offset` where offset >= 0 under 0.8 checked uint24 arithmetic (an overflow reverts, never wraps), so its result is always >= baseLevel. Every producer of `targetLevel` passes the SAME `currentLevel` as baseLevel: _openLootBoxLeg L527, resolveLootboxDirect L839, resolveRedemptionLootbox L874, resolveAfkingBox L989, and the split-roll level2 at L1215. The clamp at L529-531 is additionally dominated by the identical clamp at L1175-1177 inside _resolveLootboxCommon.
**Change:** Delete the clamp blocks at L529-531 and L1216; the L1175-1177 clamp inside _resolveLootboxCommon may also be deleted since all four callers derive targetLevel from _rollTargetLevel(currentLevel, ...) with the same currentLevel they pass in (dominating guarantee: _rollTargetLevel L1094-L1108 construction). If a single belt is preferred, keep only L1175-1177 and delete the other two.
**Excerpt (audit-time):**
```solidity
if (targetLevel < currentLevel) {
    targetLevel = currentLevel;
}
```
**Risk notes:** Defensive clamp on level math, but redundant by construction on every reachable path; consistent with the frozen-contracts/no-future-proofing rule. No RNG/solvency interaction (targetLevel only selects the ticket-queue level, and _queueTickets has its own far-future/RngLocked gates).
**Skeptic reasoning:** Verified by construction: _rollTargetLevel (L1094-1108) returns baseLevel + offset on both branches with checked uint24 arithmetic (overflow reverts, never wraps), so the result is always >= baseLevel. All four producers (L527 _openLootBoxLeg, L839 resolveLootboxDirect, L874 resolveRedemptionLootbox, L989 resolveAfkingBox) pass the identical currentLevel they hand to _resolveLootboxCommon (call sites L550/844/881/1000 — the only four, grep-confirmed), and the split-roll level2 at L1215 rolls from the same currentLevel param. All three clamps (L529-531, L1175-1177, L1216) are therefore unreachable on every path. No edge case at level 0 (currentLevel = 1, roll >= 1) or high levels (checked add reverts rather than wrapping below base).
**Implementation notes:** All three may be deleted. If a single defensive belt is preferred, keep L1175-1177 (the chokepoint inside _resolveLootboxCommon) and delete the other two — behavior is identical either way.
**Invariant impact:** none | **Risk:** low

## RT-CLAIMS-03 [redundant_sload / warm] L1329 (_rollLootboxBoons), L1399 + L1481 (_boonPoolStats) — file: modules/DegenerusGameLootboxModule.sol
**Description:** Every lootbox resolution re-SLOADs `level` twice after the caller already holds it: _resolveLootboxCommon receives `currentLevel` (= level+1) as a parameter, but _rollLootboxBoons re-derives `currentLevel = level + 1` (L1329) and _boonPoolStats re-reads `level` for priceForLevel(level) (L1399). Additionally `deityPassOwners.length` is SLOADed twice per resolution (L1336 for deityEligible and L1481 inside _boonPoolStats when eligible). All are warm SLOADs (~100 gas each) plus helper overhead, on EVERY box open (manual, auto-sweep, afking, degenerette-share, redemption chunk).
**Change:** Add a `uint24 currentLevel` parameter to _rollLootboxBoons (passed from _resolveLootboxCommon), use it at L1329/L1331; in _boonPoolStats use `priceForLevel(currentLevel - 1)` (identical value since currentLevel == level+1 at that point) via a threaded param; read deityPassOwners.length once in _rollLootboxBoons and pass the count into _boonPoolStats for the deity price curve.
**Risk notes:** Value-identical threading; `level` cannot change between _resolveLootboxCommon entry and the boon roll (no external call to a level-mutating path in between - the only delegatecalls are BoonModule expiry/consume which never write level). No RNG input changes: the seed never includes level.
**Skeptic reasoning:** Verified value-identical threading: the storage var level is written ONLY by DegenerusGameAdvanceModule.sol L1741 (advance chain), unreachable from any lootbox resolution path; the GAME_BOON_MODULE checkAndClearExpiredBoon delegatecall (L1322, the only external interaction before the L1329 read) does not write level. _rollLootboxBoons has exactly one caller (_resolveLootboxCommon L1193, which holds currentLevel as a param) and _boonPoolStats has exactly one caller (L1338). priceForLevel(currentLevel - 1) == priceForLevel(level) since currentLevel == level + 1 with no underflow (currentLevel >= 1 always). deityPassOwners.length double-read confirmed (L1336 + L1481) with no external call between. Seeds never include level, so RNG inputs are untouched.
**Implementation notes:** Thread uint24 currentLevel into _rollLootboxBoons and onward into _boonPoolStats (use priceForLevel(currentLevel - 1)); read deityPassOwners.length once at L1336 and pass the count into _boonPoolStats for the L1482 price curve.
**Invariant impact:** none | **Risk:** low

## RT-CLAIMS-05 [redundant_check / cold] L1061-L1070 (issueDeityBoon) — file: modules/DegenerusGameLootboxModule.sol
**Description:** On the day-rollover branch, issueDeityBoon writes `deityBoonUsedMask[deity] = 0` (L1063), then re-reads the just-zeroed slot (L1067, warm SLOAD), then writes it again with `mask | slotMask` (L1070) - two SSTOREs to the same slot in one tx (~2900 for the dirty nonzero->zero write that the final write immediately overwrites). All readers of deityBoonUsedMask gate on `deityBoonDay[deity] == day` (grep-verified: DegenerusGame.sol L994 and deityBoonSlots L1029-1030), so a stale mask under a stale day is never observable.
**Change:** Restructure to a single terminal write: `uint8 mask; if (deityBoonDay[deity] != day) { deityBoonDay[deity] = day; } else { mask = deityBoonUsedMask[deity]; }` then keep the existing slot checks and the single `deityBoonUsedMask[deity] = mask | slotMask;` write - dropping the L1063 zero-write and the L1067 unconditional re-read.
**Risk notes:** Leaves a stale nonzero mask in storage across days (refund-profile difference only); both on-chain readers and the view gate on deityBoonDay == day, so observable behavior is identical. No RNG interaction (slot contents are not seed inputs).
**Skeptic reasoning:** Behavior claim verified: all three readers of deityBoonUsedMask gate on deityBoonDay[deity] == day (DegenerusGame.sol L994, deityBoonSlots L1029-1030, and issueDeityBoon's own L1067 which the restructure replaces), so a stale mask under a stale day is never observable. The restructure (skip the L1063 zero-write, read the mask only on the same-day branch) is state-machine-identical. HOWEVER the savings estimate is wrong by ~15x: under EIP-3529, the current X->0 write (2900, +4800 refund) followed by the dirty 0->Y write (100, -4800 refund) nets out against the restructured direct X->Y write (2900) — the real saving is the avoided dirty SSTORE + warm SLOAD, ~200 gas, on a cold path (first boon issuance per deity per day, <=32 deities).
**Implementation notes:** Restructure exactly as proposed; the refund-profile difference (stale non-zero mask persisting across days) is unobservable through every reader.
**Invariant impact:** none | **Risk:** low

## RT-CLAIMS-12 [redundant_check / warm] L1247-L1248, L729-L730, L1371, L1926 — file: modules/DegenerusGameLootboxModule.sol
**Description:** Four zero-guards on PriceLookupLib.priceForLevel output are unreachable: priceForLevel (PriceLookupLib.sol L23-L48) is a total pure function returning one of {0.01, 0.02, 0.04, 0.08, 0.12, 0.16, 0.24} ether on EVERY branch for EVERY uint24 input - it can never return 0. Dead guards: `if (targetPrice == 0) revert E();` (L1248, runs per settle roll), `if (priceWei != 0)` wrapper (L730, presale box), and the `priceWei == 0` arms at L1371 (_burnieToEthValue) and L1926 (_lootboxTicketCount) when fed from priceForLevel.
**Change:** Delete the L1248 revert and the L730 wrapper condition (keep the body). For L1371/L1926, only narrow the disjunction if all callers feed priceForLevel output - L1371 is also fed `priceWei` from _boonPoolStats's priceForLevel(level) (same guarantee), and L1926's budget guard must stay; verify each caller before trimming the price arm. Dominating guarantee: PriceLookupLib.sol L23-L48 (every return is a nonzero constant).
**Risk notes:** Pure-function totality argument; if a future price table could return 0 these guards would matter, but contracts are frozen at deploy. The L1248 revert also serves as a belt against a corrupted rollLevel - removal is behavior-identical today but consider keeping just this one if a single belt is wanted.
**Skeptic reasoning:** Totality verified: PriceLookupLib.priceForLevel (L21-46) returns a nonzero constant on every branch for every uint24 input — no zero return exists. All four sites' producers confirmed: L1248's targetPrice comes from priceForLevel(rollLevel) at L1247; L729-730's priceWei from priceForLevel(level + 1); _burnieToEthValue's six call sites (L1402/1406/1410/1448/1452/1456, grep-confirmed complete) all pass _boonPoolStats's priceForLevel(level); _lootboxTicketCount's sole caller (L1864) passes the L1247 targetPrice through _resolveLootboxRoll. The zero-amount arms (burnieAmount == 0 at L1371, budgetWei == 0 at L1926) are reachable and MUST stay — only the price arms are dead. Consistent with the frozen-at-deploy/no-future-proofing rule.
**Implementation notes:** Delete L1248's revert; unwrap L730 keeping the body; narrow L1371 to `if (burnieAmount == 0) return 0;` and L1926 to `if (budgetWei == 0)`. Do not touch the zero-amount arms.
**Invariant impact:** none | **Risk:** low