# Gas round-4 packet — decimator

Line numbers reference the AUDIT-TIME source (three gas rounds ago) — locate by code content, NOT line number.


## QUESTS-04 [redundant_external_call / warm] L863-L868 (caller); DegenerusQuests.sol L1083-L1116 (the two views) — file: modules/DegenerusGameDecimatorModule.sol
**Description:** DecimatorModule's terminal-dec boost calls IDegenerusQuests.getPlayerQuestView(player) and consumes ONLY .baseStreak. getPlayerQuestView materializes both quest slots, per-slot _questViewData with _questRequirements, which makes an external questGame.mintPrice() callback for every ETH-family slot (slot 0 is always MINT_ETH, so at least one call; slot 1 often a second), and ABI-encodes the full nested PlayerQuestView struct across the contract boundary. effectiveBaseStreak (DegenerusQuests.sol L1111) returns the byte-identical value via the same _effectiveBaseStreak single source of truth (L1089 vs L1112) without any of that — the function exists exactly for this read.
**Change:** In DegenerusGameDecimatorModule.sol L863-868, replace `IDegenerusQuests(ContractAddresses.QUESTS).getPlayerQuestView(player).baseStreak` with `IDegenerusQuests(ContractAddresses.QUESTS).effectiveBaseStreak(player)`.
**Excerpt (audit-time):**
```solidity
uint256 effectiveStreak = uint256(IDegenerusQuests(ContractAddresses.QUESTS).getPlayerQuestView(player).baseStreak);
```
**Risk notes:** Value equivalence is provable: getPlayerQuestView.baseStreak = _effectiveBaseStreak(state, currentDay) (L1089) and effectiveBaseStreak = the same expression on the same inputs (L1112-1115). Change lives in DecimatorModule, not the assigned file — cross-contract validation needed.
**Skeptic reasoning:** Verified exactly as claimed. DecimatorModule L864-868 calls getPlayerQuestView(player) and consumes only .baseStreak. Value equivalence is provable from source: getPlayerQuestView sets baseStreak = _effectiveBaseStreak(state, currentDay) (L1089) where currentDay = _currentQuestDay(_materializeActiveQuestsForView()); effectiveBaseStreak (L1111-1116) computes the identical expression on the identical inputs. effectiveBaseStreak is already declared in IDegenerusQuests (L215) and the identical swap pattern is already used by DegeneretteModule L514 and DegenerusGame L2681 — this brings the last laggard call site in line. getPlayerQuestView's per-slot _questRequirements makes ≥1 questGame.mintPrice() callback (slot 0 is always MINT_ETH) plus full nested-struct encode/decode, all avoided. RNG lens: boostTerminalDecimator's placement commits pre-liveness from a view-read streak; the swap returns a byte-identical value, so nothing about the commitment window changes.
**Implementation notes:** One-line swap at DegenerusGameDecimatorModule.sol L864-868 to IDegenerusQuests(ContractAddresses.QUESTS).effectiveBaseStreak(player); returns uint32, existing uint256 cast keeps working.
**Invariant impact:** none | **Risk:** low

## DECIMATOR-02 [redundant_sload / warm] L341-L399 (claimDecimatorJackpotMany + _claimDecimatorJackpotFor + _consumeDecClaim) — file: modules/DegenerusGameDecimatorModule.sol
**Description:** Eligibility is computed TWICE per claimed player in the batch path: the loop calls _decClaimableFromEntry (L356) to filter, then _claimDecimatorJackpotFor -> _consumeDecClaim recomputes everything from scratch: re-derives decClaimRounds[lvl] (L283, keccak), re-derives decBurn[lvl][player] for the third time (L353, L374, L286 - two keccaks each), re-reads decBucketOffsetPacked[lvl] (L290, keccak+SLOAD), re-reads round.poolWei/round.totalBurn (warm SLOADs), and reruns the full pro-rata mul/div (L292). The single-claim path duplicates too: L374 reads decBurn[lvl][player].bucket then L375 re-derives the same mapping slot.
**Change:** Refactor so eligibility is computed once: have _claimDecimatorJackpotFor take (address player, uint24 lvl, DecEntry storage e, uint256 poolWei, uint256 totalBurn, uint64 packedOffsets, uint256 amountWei) computed by the caller; inside, set e.claimed = 1 at the exact same point in the ordering (before any credit), then run the gameOver/credit logic. Single-claim path computes amountWei once via the same shared core. With DECIMATOR-01 landed, _consumeDecClaim folds away entirely.
**Excerpt (audit-time):**
```solidity
if (_decClaimableFromEntry(round.poolWei, totalBurn, e, packedOffsets) == 0) continue; _claimDecimatorJackpotFor(players[i], lvl); // -> _consumeDecClaim recomputes the identical amount
```
**Risk notes:** MUST preserve: (1) e.claimed = 1 written before _creditClaimable/futurePrizePool effects (current ordering), (2) DecNotWinner revert in the single path vs skip in the batch path, (3) prizePoolFrozen gate stays in both external entries. Pure restructure - no accounting amounts change.
**Skeptic reasoning:** Double computation confirmed exactly as described: the batch loop filters via _decClaimableFromEntry (L355-362), then _claimDecimatorJackpotFor reads decBurn[lvl][player].bucket (L374, third derivation of that mapping slot) and _consumeDecClaim (L283-298) recomputes everything: decClaimRounds[lvl] keccak + poolWei warm SLOAD, decBurn[lvl][player] two-keccak re-derivation, decBucketOffsetPacked[lvl] keccak+SLOAD, totalBurn warm re-read, full pro-rata mul/div. One correction to the SLOAD accounting: DecEntry packs burn/bucket/subBucket/claimed into ONE slot (216 bits, Storage L1641-1650), so the per-entry re-reads are one warm SLOAD, not several — savings revise slightly downward. The restructure is behavior-identical if the stated constraints hold; nothing between the caller's eligibility computation and the core's claimed-write makes an external call, so passing precomputed values is safe.
**Implementation notes:** MUST preserve bit-identically: (1) e.claimed = 1 written before _creditClaimable/claimablePool/futurePrizePool effects; (2) single path reverts DecClaimInactive/DecAlreadyClaimed/DecNotWinner while batch path skips already-claimed/non-winner but still reverts DecClaimInactive; (3) prizePoolFrozen gate in both external entries; (4) winBucket captured from the entry before claim consumption (same value either way since _consumeDecClaim never writes bucket, but keep the read-before pattern per the in-code comment). Combine with DECIMATOR-01 so _consumeDecClaim folds away.
**Invariant impact:** none | **Risk:** medium

## DECIMATOR-03 [loop / warm] L347-L365, L377 — file: modules/DegenerusGameDecimatorModule.sol
**Description:** Loop-invariant reads inside the claimDecimatorJackpotMany loop: round.poolWei is SLOADed from storage every iteration (passed at L357 from the storage ref); the outer mapping derivation decBurn[lvl] (keccak over lvl) is recomputed for every per-player access (3x per claimed player until DECIMATOR-02 lands); gameOver (L377) is SLOADed once per claimed player inside _claimDecimatorJackpotFor.
**Change:** Before the loop: `uint256 poolWeiCached = round.poolWei;`, `mapping(address => DecEntry) storage levelEntries = decBurn[lvl];` (use levelEntries[players[i]]), and pass/hoist a `bool over = gameOver` through the claim core (combine with DECIMATOR-02 refactor).
**Excerpt (audit-time):**
```solidity
for (uint256 i; i < players.length; ++i) { DecEntry storage e = decBurn[lvl][players[i]]; ... _decClaimableFromEntry(round.poolWei, totalBurn, e, packedOffsets) ... }
```
**Risk notes:** poolWei/gameOver cannot change mid-loop (no external calls leave the storage context except the lootbox delegatecall which operates in the same context - verify resolveLootboxDirect never mutates gameOver/decClaimRounds; it does not, it only credits lootbox state). via_ir may already CSE some of this at runs=50 - verify with forge snapshot.
**Skeptic reasoning:** Caching is provably safe across the loop. (1) decClaimRounds[lvl].poolWei/totalBurn are written ONLY by runDecimatorJackpot (GAME self-call gated), which is idempotent per level via the L221 `poolWei != 0` guard — once nonzero the slot can never be rewritten, so no mid-loop mutation is possible even under hypothetical reentry into the advance chain. (2) gameOver is written only in game-over resolution. (3) The only external calls inside the loop are to fixed trusted protocol contracts: coinflip.creditFlip (LootboxModule L1283) and boon-module delegatecalls (same storage context, write boon state only); wwxrp.mintPrize is NOT reachable on this path because resolveLootboxDirect passes payColdBustConsolation=false (L851, gate L1277). No player-controlled callback surface exists, so no reentrancy can flip gameOver or poolWei mid-loop. (4) Verified _resolveLootboxCommon/_settleLootboxRoll never write gameOver or decClaimRounds. Note gameOver/prizePoolFrozen share the packed slot-0 region, so per-player gameOver reads are warm (~100 each), matching the estimate.
**Implementation notes:** Hoist before the loop: poolWei, totalBurn (already hoisted), packedOffsets (already hoisted), `mapping(address => DecEntry) storage levelEntries = decBurn[lvl]`, and thread `bool over = gameOver` through the DECIMATOR-02 refactored core. Do both findings as one change.
**Invariant impact:** none | **Risk:** low

## DECIMATOR-06 [redundant_check / hot] L576, L591, L608, L504, L154 — file: modules/DegenerusGameDecimatorModule.sol
**Description:** Four guards are dead on every reachable path because bucket/denom is always in [2,12]: the SOLE writer of bucket values is BurnieCoin.decimatorBurn (BurnieCoin.sol:641) passing _adjustDecimatorBucket output (BurnieCoin.sol:694-713: base 12, floored at minBucket 2 or 5, never below 2), and terminal buckets come from _terminalDecBucket (module L1100-1111: clamped to [2,12]). Specifically: (a) L576 `_decUpdateSubbucket` guard `delta == 0 || denom == 0` - both call sites dominate it (L160 `prevBurn != 0` and L183 `delta != 0`; denom = m.bucket which is only ever assigned coin-validated values >= 2); (b) L591 `_decRemoveSubbucket` `denom == 0` half (sole call site L156 is in the branch where m.bucket != 0; KEEP the delta==0 half - prevBurn can be 0); (c) L608 `_decSubbucketFor` `bucket == 0` (all 4 call sites L153/L158/L794/L896 pass values >= 2); (d) L504 `_unpackDecWinningSubbucket` `denom < 2` (callers gate e.bucket != 0 at L528, and at L1038/L1068 weightedBurn != 0 implies a burn set bucket in [2,12]). Additionally L154's `bucket != 0 &&` term is always true (coin floor 2) - the condition reduces to `bucket < m.bucket`.
**Change:** Delete the guard line at L576; reduce L591 to `if (delta == 0) return;`; delete L608 `if (bucket == 0) return 0;`; delete L504 `if (denom < 2) return 0;`; reduce L154 to `else if (bucket < m.bucket)`.
**Excerpt (audit-time):**
```solidity
if (delta == 0 || denom == 0) return;  /  if (bucket == 0) return 0;  /  if (denom < 2) return 0;
```
**Risk notes:** These are defense-in-depth guards. Soundness rests on BurnieCoin (immutable, sole caller via the COIN-gated entry) never passing bucket outside [2,12] - proven for the deployed pair. Per user policy frozen contracts don't keep future-proofing redundancy, but The Skeptic should weigh: a wrong removal here corrupts subbucket aggregates (solvency-adjacent). Modulo-by-zero in _decSubbucketFor would revert (panic), not corrupt, if the impossible input ever occurred.
**Skeptic reasoning:** Domination verified end-to-end across both frozen contracts. Bucket sources: (1) BurnieCoin._adjustDecimatorBucket (BurnieCoin.sol:695-713) returns base 12, reduction <= range, floored at minBucket (5, or 2 on x100) -> always [2,12]; sole recordDecBurn call site BurnieCoin.sol:641; module entry COIN-gated (L144). (2) _terminalDecBucket (module L1100-1111) clamps to [2,12]. Per-guard: (a) L576 — both call sites gate delta != 0 (L160 prevBurn!=0, L183 delta!=0) and pass m.bucket/bucketUsed in [2,12]; (b) L591 denom-half dead (sole site L156 in the m.bucket!=0 branch), delta-half MUST stay (prevBurn can be 0) — the finding's change spec correctly keeps it; (c) L608 — all 4 sites (L153/L158/L794/L896) pass >= 2; (d) L504 — L531 gated by e.bucket!=0 at L528 + writer invariant, L1038 gated at L1030, L1068 holds because weightedBurn!=0 implies bucket was assigned in [2,12] (verified: weightedBurn only written at L818 after L792-794, and boost requires bucket!=0 at L859); (e) L154 `bucket != 0 &&` always true (coin floor 2). Failure modes on the impossible violated domain are checked-arithmetic panics (underflow/mod-zero) or writes to a denom-0 aggregate that resolution never reads (loops denoms 2-12 only) — no silent winning-data corruption. Both contracts frozen at deploy so the invariant cannot drift. Repo policy explicitly drops future-proofing redundancy on frozen contracts.
**Implementation notes:** Implement exactly as specified: delete L576 guard; reduce L591 to `if (delta == 0) return;` (do NOT drop the delta half); delete L608 and L504 guards; reduce L154 to `else if (bucket < m.bucket)`. Cite BurnieCoin._adjustDecimatorBucket and _terminalDecBucket as the dominating writers in a one-line comment if any guard removal needs justification in-code.
**Invariant impact:** none | **Risk:** medium

## DECIMATOR-08 [redundant_check / warm] L521 — file: modules/DegenerusGameDecimatorModule.sol
**Description:** `if (totalBurn == 0) return 0;` in _decClaimableFromEntry is dominated at all three call sites: (a) _consumeDecClaim L284 gates round.poolWei != 0, and the snapshot invariant (L251-L262: the round is only written when totalBurn != 0) guarantees totalBurn != 0 whenever poolWei != 0 (uint232 truncation to zero needs ~2^40 max-saturated players - unreachable); (b) claimDecimatorJackpotMany L348 same gate/invariant; (c) _decClaimable explicitly checks totalBurn == 0 at L550 before calling.
**Change:** Remove L521. (If DECIMATOR-05 narrows totalBurn to uint128, re-verify the non-truncation argument in the same pass.)
**Excerpt (audit-time):**
```solidity
if (totalBurn == 0) return 0;
```
**Risk notes:** This is the div-by-zero backstop for the pro-rata division at L535. If removed and the invariant were ever violated, the result is a panic revert (no fund loss). Skeptic may prefer keeping a 6-gas backstop in front of a division.
**Skeptic reasoning:** All three call sites of _decClaimableFromEntry verified dominated (grep confirms exactly three: L292, L356, L556): (a) L292 behind round.poolWei != 0 (L284) where the snapshot writer (L251-262) only stores when totalBurn != 0 — uint232 truncation to exactly zero requires totalBurn to be a multiple of 2^232, unreachable; (b) L356 behind the same gate at L348; (c) L556 behind the explicit totalBurn == 0 check at L550. Failure mode if the invariant were ever violated is a div-by-zero panic at L535 — a revert, not corruption or fund loss. Marginal saving but provably safe.
**Implementation notes:** If DECIMATOR-05 lands with a narrowed totalBurn width, re-verify the non-zero-truncation argument in the same pass exactly as the finding says. Reasonable to bundle with DECIMATOR-02's restructure rather than as a standalone edit.
**Invariant impact:** none | **Risk:** low

## DECIMATOR-09 [idiom / hot] L177-L179, L783-L789, L821 — file: modules/DegenerusGameDecimatorModule.sol
**Description:** Multiple separate member-writes to single packed storage slots, relying on the IR optimizer to coalesce: (a) recordDecBurn L177-179 writes e.burn, e.bucket, e.subBucket as three read-modify-writes of one slot, and on the common repeat-burn path bucket/subBucket are rewritten with UNCHANGED values; (b) recordTerminalDecBurn lazy reset L783-789 issues six member-writes to one slot; (c) L821 re-SLOADs e.bucket/e.subBucket for the bucketKey when locals are derivable.
**Change:** (a) Move `e.bucket = m.bucket; e.subBucket = m.subBucket;` inside the two branches that actually mutate them (first-burn L151-153 and migration L154-163); common path keeps only `e.burn = newBurn;`. (b) Replace the six zero-writes with one struct assignment `terminalDecEntries[player] = TerminalDecEntry(0, 0, 0, 0, uint48(lvl), false);` (then re-take the storage ref). (c) Cache `uint8 b = e.bucket; uint8 sb = e.subBucket;` after the set block and use in bucketKey + event.
**Excerpt (audit-time):**
```solidity
e.burn = newBurn; e.bucket = m.bucket; e.subBucket = m.subBucket;
```
**Risk notes:** Behavior-identical reordering of writes within one function; e.claimed is untouched in both forms. Low risk, but the saving is speculative until measured.
**Skeptic reasoning:** All three sub-items verified behavior-identical. (a) On the common repeat-burn path m.bucket/m.subBucket equal the loaded storage values, so relocating those writes into the two mutating branches (first-burn L151-153, migration L154-163) leaves the final slot content identical; no intermediate read depends on the storage copies (helpers take params; _decEffectiveAmount uses prevBurn only); e.claimed is preserved by the packed read-modify-write either way. (b) TerminalDecEntry field order (totalBurn, weightedBurn, bucket, subBucket, burnLevel, boosted — Storage L1746-1753, single 240-bit slot) matches the proposed literal (0,0,0,0,uint48(lvl),false) exactly, including boosted=false matching the L788 reset. (c) Caching bucket/subBucket locals for the L821 key + event is a plain warm-SLOAD elimination. The savings are genuinely speculative — via_ir at runs=50 may already coalesce same-slot member writes — and the finding honestly flags this.
**Implementation notes:** Measure first (snapshot before/after) per the finding's own caveat; only commit if the diff is nonzero. For (b), after the struct assignment the existing storage ref e remains valid (same slot) — no re-take strictly needed, but harmless.
**Invariant impact:** none | **Risk:** low

## DECIMATOR-11 [idiom / warm] L628-L629, L635-L642 — file: modules/DegenerusGameDecimatorModule.sol
**Description:** (a) L629 computes the remainder as `amount - (fullHalfPasses * HALF_WHALE_PASS_PRICE)` (mul+sub) where `amount % HALF_WHALE_PASS_PRICE` (single mod) is equivalent. (b) The L640 recursive self-call for the remainder always lands in the direct-resolve branch (remainder < 2.25 ether < LOOTBOX_CLAIM_THRESHOLD 5 ether), so the recursion re-executes the L626 zero-checks and the L627 threshold comparison that are statically false, plus an internal call frame.
**Change:** (a) `uint256 remainder = amount % HALF_WHALE_PASS_PRICE;`. (b) Restructure to a fall-through: set `amount = remainder` and let execution continue into the direct-resolve tail when remainder >= 0.01 ether (or extract the delegatecall tail into a tiny private fn called from both places), removing the recursive frame and dead re-checks.
**Excerpt (audit-time):**
```solidity
uint256 remainder = amount - (fullHalfPasses * HALF_WHALE_PASS_PRICE); ... if (remainder >= 0.01 ether) { _awardDecimatorLootbox(winner, remainder, rngWord, evScore); }
```
**Risk notes:** Same arithmetic results; the dust-stays-in-futurePrizePool behavior for remainder < 0.01 ether must be preserved exactly (no credit, no event).
**Skeptic reasoning:** (a) `amount % HALF_WHALE_PASS_PRICE` is arithmetically identical to `amount - (amount/H)*H` for the nonzero compile-time constant — verified. (b) The recursion is provably single-depth: first frame requires amount > 5 ether, remainder < 2.25 ether < LOOTBOX_CLAIM_THRESHOLD, so the recursive frame always falls through the statically-false L626/L627 checks into the direct-resolve delegatecall with identical args (winner, remainder, rngWord, evScore). A fall-through restructure that runs the delegatecall tail when remainder >= 0.01 ether, and does nothing below it, is behavior-identical including the dust-stays-in-futurePrizePool path (no credit, no event). _awardDecimatorLootbox is private with two callers (L439 and the self-recursion), so no external surface changes. Note the same mul/sub idiom also exists in PayoutUtils._queueWhalePassClaimCore L38-39 (out of this batch's scope).
**Implementation notes:** Preserve exactly: remainder >= 0.01 ether gate, identical delegatecall args, and the silent-dust behavior below 0.01. Bundle with DECIMATOR-07's wrapper removal.
**Invariant impact:** none | **Risk:** low

## DECIMATOR-13 [redundant_sload / cold] L1007-L1016, L894-L911 — file: modules/DegenerusGameDecimatorModule.sol
**Description:** Post-GAMEOVER cold-path micro-redundancies: (a) claimTerminalDecimatorJackpot re-reads lastTerminalDecClaimRound.lvl (L1013) for the event after _consumeTerminalDecClaim already loaded it (L1055). (b) boostTerminalDecimator: when no bucket promotion occurs (the common case), oldKey == newKey by construction yet the code computes two abi.encode+keccak key derivations and performs two separate read-modify-writes on the same mapping slot (L908-911).
**Change:** (a) Have _consumeTerminalDecClaim return (amountWei, lvl) and emit from the returned value. (b) Branch on the already-known promotion condition: if liveBucket was not adopted, do `terminalDecBucketBurnTotal[oldKey] += newWeighted - oldWeighted;` (newWeighted >= oldWeighted since factorBps >= 10000) and skip the second key derivation; keep the two-key form only for the promotion branch.
**Excerpt (audit-time):**
```solidity
terminalDecBucketBurnTotal[oldKey] -= oldWeighted; terminalDecBucketBurnTotal[newKey] += newWeighted;
```
**Risk notes:** Weight conservation must hold bit-identically in both branches (the aggregate feeds runTerminalDecimatorJackpot's totalWinnerBurn - solvency-adjacent). The += (new - old) form is exactly equal to -=old; +=new on the same key; saturation at uint88 max happens before either form.
**Skeptic reasoning:** (a) Verified: claimTerminalDecimatorJackpot re-reads lastTerminalDecClaimRound.lvl (L1013) for the event after _consumeTerminalDecClaim loaded the same single packed slot (L1055); returning (amountWei, lvl) is behavior-identical and saves one warm SLOAD. (b) Verified: in the non-promotion case (liveBucket >= oldBucket) newBucket==oldBucket and newSub==oldSub by construction (L892-899), so oldKey==newKey, and `+= (newWeighted - oldWeighted)` is exactly equal to the -=/+= pair on one key. newWeighted >= oldWeighted always: effectiveStreak >= 1 (zero reverts at L869) gives factorBps >= 13000 > 10000, and the uint88 saturation clamp is >= oldWeighted (itself uint88). Weight conservation feeding runTerminalDecimatorJackpot's totalWinnerBurn is preserved bit-identically; checked subtraction cannot underflow. Both are post-GAMEOVER/final-day cold paths.
**Implementation notes:** For (b), branch on the existing promotion condition (liveBucket < oldBucket); keep the two-key form in the promotion branch unchanged. Lowest-priority of the approved set — bundle with other module edits rather than landing alone.
**Invariant impact:** none | **Risk:** low

## DECIMATOR-14 [dead_code / warm] L803 (+ L452, L727) — file: modules/DegenerusGameDecimatorModule.sol
**Description:** `if (effectiveAmount == 0) revert TerminalDecCapped();` is unreachable. recordTerminalDecBurn's only caller is BurnieCoin.terminalDecimatorBurn (BurnieCoin.sol:665-687, COIN-gated at module L763) which enforces `amount >= DECIMATOR_MIN = 1000 ether-units` (BurnieCoin.sol:676, 145) before passing it as baseAmount. _decEffectiveAmount with baseAmount > 0 provably never returns 0 on any branch: early-returns yield baseAmount; the fullEffective branch yields >= baseAmount; the cap-split branch yields multiplied + (baseAmount - maxMultBase) where baseAmount > maxMultBase (else-branch inequality), so >= 1. The misnamed 'capped' revert can never fire - at cap the extra is counted at 1x, not rejected. Same coin-side gate (BurnieCoin.sol:601 for the regular path) makes the L452 `baseAmount == 0` early-return dead at both call sites (L167, L798).
**Change:** Remove L803 and the now-unused error TerminalDecCapped (L727). Optionally remove the L452 `if (baseAmount == 0) return 0;` early-return (same domination).
**Excerpt (audit-time):**
```solidity
if (effectiveAmount == 0) revert TerminalDecCapped();
```
**Risk notes:** Unreachability is a math proof over _decEffectiveAmount plus the immutable coin-side minimum; both contracts are frozen at deploy. If removed, an (impossible) zero-effective burn would silently record zero weight instead of reverting - acceptable only because the input domain excludes it. L452 removal additionally relies on every future call site passing nonzero baseAmount - there are exactly two call sites, both dominated.
**Skeptic reasoning:** Unreachability proven. recordTerminalDecBurn's only caller is BurnieCoin.terminalDecimatorBurn (sole call site BurnieCoin.sol:684; module COIN-gated at L763) which enforces amount >= DECIMATOR_MIN = 1000 ether (BurnieCoin.sol:676, constant L145). _decEffectiveAmount with baseAmount > 0 never returns 0 on any branch: early branches return baseAmount; fullEffective branch returns >= baseAmount... > 0; cap-split branch: fullEffective > remaining implies baseAmount > maxMultBase (floor inequality), so effectiveAmount = multiplied + (baseAmount - maxMultBase) >= 1 — including the maxMultBase == 0 edge (returns baseAmount). The revert is also semantically misnamed: at the cap, extra burn counts at 1x rather than being rejected, so 'Capped' can never fire. TerminalDecCapped has exactly two references (declaration L727 + L803) — grep confirmed, removable. The L452 early-return removal is even safer than claimed: with baseAmount == 0, every branch of _decEffectiveAmount returns 0 anyway (mult<=1e4 returns 0; prev>=cap returns 0; fullEffective = 0 <= remaining returns 0), so removal is behavior-identical UNCONDITIONALLY, independent of the coin-side gates. Post-removal failure mode on the impossible zero input is a silent zero-weight record (delta 0, no aggregate change) — no corruption.
**Implementation notes:** Remove L803 and the TerminalDecCapped declaration (L727). L452 removal is safe unconditionally (not merely by coin-gate domination) — include it.
**Invariant impact:** none | **Risk:** low

## RT-QUESTS-AFFILIATE-04 [redundant_external_call / cold] L864-L869 — file: modules/DegenerusGameDecimatorModule.sol
**Description:** boostTerminalDecimator fetches the effective streak via the heavyweight `getPlayerQuestView(player).baseStreak`, which materializes both quest slots, per-slot QuestRequirements (incl. a questGame.mintPrice() staticcall back into the Game for the always-present MINT_ETH slot-0), progress conversions, and ABI-encodes/decodes the full PlayerQuestView struct — when DegenerusQuests already exposes the exact same value through the cheap `effectiveBaseStreak(player)` accessor (DegenerusQuests L1104-L1116 documents `_effectiveBaseStreak` as the single source of truth for both, and L1089 sets viewData.baseStreak from the same helper; both return uint32). Additionally the subsequent playerActivityScore self-call (L884) re-fetches effectiveBaseStreak a second time in the same tx.
**Change:** Replace `IDegenerusQuests(ContractAddresses.QUESTS).getPlayerQuestView(player).baseStreak` with `IDegenerusQuests(ContractAddresses.QUESTS).effectiveBaseStreak(player)` at DecimatorModule L865-L868.
**Excerpt (audit-time):**
```solidity
uint256 effectiveStreak = uint256(
    IDegenerusQuests(ContractAddresses.QUESTS)
        .getPlayerQuestView(player)
        .baseStreak
);
```
**Risk notes:** Value-equivalence is asserted by the Quests source itself (same private helper, same inputs). Interface IDegenerusQuests already declares effectiveBaseStreak (L215). Verify the second streak fetch inside playerActivityScore is left as-is (it is part of a separate self-call and uses its own consumer cap).
**Skeptic reasoning:** Verified at DegenerusGameDecimatorModule.sol L864-868: boostTerminalDecimator fetches IDegenerusQuests(QUESTS).getPlayerQuestView(player).baseStreak. Value equivalence is exact: getPlayerQuestView L1089 sets viewData.baseStreak = _effectiveBaseStreak(state, currentDay) and effectiveBaseStreak L1111-1116 returns the same _effectiveBaseStreak(questPlayerState[player], _currentQuestDay(...)) — same private helper, same inputs, both uint32 (PlayerQuestView.baseStreak is uint32 per IDegenerusQuests L35). getPlayerQuestView additionally materializes per-slot QuestRequirements including at least one questGame.mintPrice() staticcall (slot 0 is always MINT_ETH, L1192) and ABI-encodes the full nested struct — all dropped. Interface already declares effectiveBaseStreak (IDegenerusQuests L215) and the module already imports that interface, so the swap is a one-line change in the module. The second streak fetch via the playerActivityScore self-call at L884 is a separate consumer with its own cap and is correctly left untouched. Both are view staticcalls — no state interaction.
**Implementation notes:** Cross-contract check done: no other consumer of getPlayerQuestView exists on-chain; the interface function is already declared.
**Invariant impact:** none | **Risk:** low