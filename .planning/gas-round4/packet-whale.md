# Gas round-4 packet — whale

Line numbers reference the AUDIT-TIME source (three gas rounds ago) — locate by code content, NOT line number.


## GAME-12 [redundant_external_call / warm] L876 (consumer of Game L2668-L2683) — file: modules/DegenerusGameWhaleModule.sol
**Description:** DegenerusGameWhaleModule:876 calls IDegenerusGame(address(this)).playerActivityScore(buyer) via external self-call, but WhaleModule inherits DegenerusGameMintStreakUtils, which already contains _playerActivityScore — the Game's external body is just `_playerActivityScore(player, quests.effectiveBaseStreak(player))`. The self-CALL frame + dispatcher is pure overhead; the quests STATICCALL is paid either way. (DegenerusGameDecimatorModule:769/:884 make the same self-call but Decimator does NOT inherit MintStreakUtils — converting it would add ~1-2KB to that module, a tradeoff to measure separately.) The Game-side external view MUST remain — BurnieCoin:620 and StakedDegenerusStonk:987 call it cross-contract.
**Change:** In DegenerusGameWhaleModule.sol:876 replace the self-call with `_playerActivityScore(buyer, quests.effectiveBaseStreak(buyer))` (inherited internal + the same quests read the Game body performs).
**Excerpt (audit-time):**
```solidity
WhaleModule:876: uint256 activityScore = IDegenerusGame(address(this)).playerActivityScore(buyer);
```
**Risk notes:** Byte-equivalent score: the Game view passes quests.effectiveBaseStreak as questStreak and _activeTicketLevel() as base — replicate exactly. No state writes involved (view).
**Skeptic reasoning:** Verified: WhaleModule:876 self-calls playerActivityScore whose Game body (L2674-2683) is exactly `_playerActivityScore(player, quests.effectiveBaseStreak(player))`. WhaleModule inherits DegenerusGameMintStreakUtils (declaration L21), which holds both the 2-arg _playerActivityScore (:335-339, internally appending _activeTicketLevel()) and access to the `quests` constant (DegenerusGameStorage:142) — so the replacement is byte-equivalent: same quests STATICCALL (msg.sender seen by quests is GAME in both designs since delegatecall preserves address(this)), same internal scoring. Pure view, no state writes. The exclusions are correct: DecimatorModule (verified `is DegenerusGamePayoutUtils`, no MintStreakUtils) would grow by the helper bodies — out of scope here. The Game-side view MUST stay: BurnieCoin:620 and StakedDegenerusStonk:987 call it cross-contract (verified).
**Implementation notes:** Replace WhaleModule:876 with `uint256 activityScore = _playerActivityScore(buyer, quests.effectiveBaseStreak(buyer));`. Do NOT touch the Game-side view or the DecimatorModule call sites.
**Invariant impact:** none | **Risk:** low

## WHALEBOON-01 [loop / warm] L323-L330 — file: modules/DegenerusGameWhaleModule.sol
**Description:** _purchaseWhaleBundle queues 100 levels of tickets by calling _queueTickets once per level. Each call re-runs _livenessTriggered() (~3-4 warm SLOADs: lastPurchaseDay/jackpotPhaseFlag/level/purchaseStartDay/rngRequestTime), re-reads `level` and rngLockedFlag, and emits a separate TicketsQueued event (2 topics + 64B data ~1640 gas). The storage base already provides _queueTicketRange (DegenerusGameStorage.sol:680-712) which hoists the liveness check and the `level` SLOAD outside the loop and emits ONE TicketsQueuedRange event; it preserves the identical per-level far-future/rngLockedFlag gate, so the RNG freeze behavior is byte-equivalent. claimWhalePass (L1011) and _activate10LevelPass already use the range helper — this loop is a re-implementation.
**Change:** Replace the loop at L323-330 with: uint24 bonusCount = passLevel <= WHALE_BONUS_END_LEVEL ? (WHALE_BONUS_END_LEVEL - passLevel + 1) : 0; _queueTicketRange(buyer, ticketStartLevel, bonusCount, bonusTickets, false); _queueTicketRange(buyer, ticketStartLevel + bonusCount, 100 - bonusCount, standardTickets, false); (skip the first call when bonusCount == 0; bonusCount <= 10 < 100 always since passLevel >= 1).
**Excerpt (audit-time):**
```solidity
for (uint24 i = 0; i < 100; ) { uint24 lvl = ticketStartLevel + i; bool isBonus = (lvl >= passLevel && lvl <= WHALE_BONUS_END_LEVEL); _queueTickets(buyer, lvl, isBonus ? bonusTickets : standardTickets, false); unchecked { ++i; } }
```
**Risk notes:** Event API shape changes: 100x TicketsQueued becomes <=2x TicketsQueuedRange. TicketsQueuedRange is already emitted by claimWhalePass/_activate10LevelPass/DecimatorModule, so off-chain consumers must already handle it. Per-level rngLockedFlag far-future gating and revert points are identical (both revert the whole tx at the same first far-future level when locked); within one tx _livenessTriggered is deterministic so 1 check == 100 checks. No advanceGame-chain impact (purchase path only, gas strictly reduced).
**Skeptic reasoning:** Claim verified against live source. Loop at WhaleModule L323-330 calls _queueTickets per level; _queueTicketRange (DegenerusGameStorage.sol:680-712) is behaviorally equivalent: it hoists _livenessTriggered (deterministic within one tx, so 1 check == 100 checks) and caches `level`, while preserving the identical per-level far-future gate `lvl > level + 5 && rngLockedFlag -> revert RngLocked` (storage L693-694) — both versions revert the entire tx at the first far-future level when locked, so RNG-freeze behavior is unchanged. Same ticketsOwedPacked/ticketQueue writes per level. Bonus-window math checks out: ticketStartLevel = passLevel (L222), so bonus levels are exactly passLevel..10; bonusCount = WHALE_BONUS_END_LEVEL - passLevel + 1 (max 10) when passLevel <= 10, else 0; second range covers the remaining 100 - bonusCount >= 90 levels. ticketsPerLevel is always nonzero (40*qty / 2*qty, qty >= 1) so _queueTicketRange's missing zero-quantity early-return is moot. TicketsQueuedRange is already emitted by claimWhalePass (WhaleModule L1011), _activate10LevelPass (storage L1285), and DecimatorModule L633, so off-chain consumers must already decode it. Event-shape change (100x TicketsQueued -> <=2x TicketsQueuedRange) is off-chain-only.
**Implementation notes:** Must skip the first _queueTicketRange call when bonusCount == 0 — _queueTicketRange with numLevels = 0 still runs the liveness check and emits an empty-range event. WhaleModule has ~11.4KB EIP-170 headroom (13,108 bytes deployed), so no size concern.
**Invariant impact:** none | **Risk:** low

## WHALEBOON-02 [loop / cold] L644-L659 — file: modules/DegenerusGameWhaleModule.sol
**Description:** Identical pattern in _purchaseDeityPass: 100 per-level _queueTickets calls for the affiliate's whale pass, each paying the per-call liveness check + level/rngLocked SLOADs + a separate TicketsQueued event, where two _queueTicketRange calls are equivalent.
**Change:** Replace the L645-659 loop with: uint24 bonusCount = passLevel <= WHALE_BONUS_END_LEVEL ? (WHALE_BONUS_END_LEVEL - passLevel + 1) : 0; _queueTicketRange(affiliateAddr, ticketStartLevel, bonusCount, WHALE_BONUS_TICKETS_PER_LEVEL, false); _queueTicketRange(affiliateAddr, ticketStartLevel + bonusCount, 100 - bonusCount, WHALE_STANDARD_TICKETS_PER_LEVEL, false);
**Excerpt (audit-time):**
```solidity
for (uint24 i = 0; i < 100; ) { uint24 lvl = ticketStartLevel + i; bool isBonus = (lvl >= passLevel && lvl <= WHALE_BONUS_END_LEVEL); _queueTickets(affiliateAddr, lvl, isBonus ? WHALE_BONUS_TICKETS_PER_LEVEL : WHALE_STANDARD_TICKETS_PER_LEVEL, false); ... }
```
**Risk notes:** Same event-shape and equivalence notes as WHALEBOON-01. Deity passes are capped at 32 lifetime, so runtime value is bounded; the bytecode dedup still applies.
**Skeptic reasoning:** Identical pattern verified at L644-659 (_purchaseDeityPass, tickets to affiliateAddr). Same equivalence proof as WHALEBOON-01; additionally _purchaseDeityPass rejects rngLockedFlag at entry (L558), so the per-level rng gate can never fire differently post-refactor. ticketStartLevel = passLevel (L644 via L601), tickets per level are nonzero constants (40/2). affiliateAddr is documented always non-zero (getReferrer defaults to VAULT). Deity passes are capped at 32 lifetime so runtime value is bounded; bytecode dedup still applies.
**Implementation notes:** Same bonusCount==0 skip guard as WHALEBOON-01. Use the WHALE_BONUS_TICKETS_PER_LEVEL / WHALE_STANDARD_TICKETS_PER_LEVEL constants directly as proposed.
**Invariant impact:** none | **Risk:** low

## WHALEBOON-08 [redundant_sload / warm] L861, L880-L885, L904-L910, L922 — file: modules/DegenerusGameWhaleModule.sol
**Description:** _recordLootboxEntry touches the single packed slot lootboxRngPacked three times: L861 _lrRead(LR_INDEX...) (SLOAD 1), L922 _lrRead(LR_PENDING_ETH...) (SLOAD 2) and _lrWrite (SLOAD 3 inside DegenerusGameStorage.sol:1515-1517, then SSTORE). Nothing between L861 and L922 writes that slot: the self-call enqueueBoxForAutoOpen only pushes to boxPlayers (DegenerusGame.sol:1717-1720), playerActivityScore is a view, and _applyLootboxBoostOnPurchase writes only boonPacked. Additionally `level` is SLOADed and `lootboxEvBenefitUsedByLevel[buyer][level + 1]` is hashed twice in each branch (L880/L885 and L904/L910); the via_ir optimizer cannot prove cacheability across the L876/L894 external calls.
**Change:** Load `uint256 lr = lootboxRngPacked;` once at the top; derive index from it; at the end write the updated pending-eth field back with one masked SSTORE built from `lr`. Also hoist `uint24 capLevel = level + 1;` once before the branches and use it for both lootboxEvBenefitUsedByLevel accesses in each branch.
**Excerpt (audit-time):**
```solidity
uint48 index = uint48(_lrRead(LR_INDEX_SHIFT, LR_INDEX_MASK)); ... _lrWrite(LR_PENDING_ETH_SHIFT, LR_PENDING_ETH_MASK, _lrRead(LR_PENDING_ETH_SHIFT, LR_PENDING_ETH_MASK) + _packEthToMilliEth(lootboxAmount));
```
**Risk notes:** Safe only because the two intervening self-calls cannot touch lootboxRngPacked (verified: boxPlayers push + view). If WHALEBOON-10 is adopted (direct push) the reasoning becomes local and trivial. Do not cache across any call that can reach LootboxModule.
**Skeptic reasoning:** Write-census of the L861-L922 window verified clean for lootboxRngPacked: _recordLootboxMintDay writes only mintPacked_ (MintStreakUtils L385-398); IDegenerusGame.playerActivityScore is declared view (IDegenerusGame.sol:65) so the self-call compiles to STATICCALL — no mutation possible; enqueueBoxForAutoOpen is a guarded self-call whose entire body is boxPlayers[index].push(player) (DegenerusGame.sol:1717-1720); _applyLootboxBoostOnPurchase writes only boonPacked[buyer].slot0 (L945-985); lootboxEvBenefitUsedByLevel and lootboxEth are separate mappings; _isDistressMode is view. Nothing in the window writes `level` either, so hoisting `capLevel = level + 1` is safe. Current code performs 3 SLOADs of lootboxRngPacked (L861, L922 _lrRead, plus the read inside _lrWrite at storage L1515-1517) + 1 SSTORE; the cache reduces this to 1 SLOAD + 1 SSTORE.
**Implementation notes:** The hand-rolled final write must replicate _lrWrite's field masking exactly ((value & mask) << shift) so pending-eth overflow semantics are unchanged. Do not extend the cache across any call that can reach LootboxModule. If WHALEBOON-10 lands first, the window becomes fully local and the safety argument is trivial.
**Invariant impact:** none | **Risk:** low

## WHALEBOON-09 [redundant_external_call / warm] L876 — file: modules/DegenerusGameWhaleModule.sol
**Description:** First-deposit path does an external self-call `IDegenerusGame(address(this)).playerActivityScore(buyer)` — a full CALL back into the Game plus its dispatcher — but the Game's implementation (DegenerusGame.sol:2674-2683) is just `_playerActivityScore(player, quests.effectiveBaseStreak(player))`, and the internal `_playerActivityScore` lives in DegenerusGameMintStreakUtils (L241/L335) which WhaleModule ALREADY inherits (L21). The module can compute the identical value with one fewer external hop (the quests staticcall remains either way).
**Change:** Replace L876 with `uint256 activityScore = _playerActivityScore(buyer, quests.effectiveBaseStreak(buyer));` (quests is a compile-time constant in the shared storage base, DegenerusGameStorage.sol:142).
**Excerpt (audit-time):**
```solidity
uint256 activityScore = IDegenerusGame(address(this)).playerActivityScore(buyer);
```
**Risk notes:** Semantics byte-identical to the Game wrapper (verified at DegenerusGame.sol:2681-2682, including the decay-aware effectiveBaseStreak). Check WhaleModule's own EIP-170 headroom before adopting (modules are separate deployments, normally far from the ceiling). Score snapshot timing unchanged — same point in the call sequence.
**Skeptic reasoning:** Semantics verified byte-identical: DegenerusGame.playerActivityScore (L2674-2683) is exactly `_playerActivityScore(player, quests.effectiveBaseStreak(player))` using the 2-arg wrapper (MintStreakUtils L335-340, streakBaseLevel = _activeTicketLevel()), and WhaleModule already inherits DegenerusGameMintStreakUtils (L21) with `quests` available on the shared storage base (used by BoonModule L325). Under delegatecall the internal version reads identical storage; the quests staticcall is unchanged; score snapshot timing is the same call-sequence point. The module-size concern is resolved by measurement: WhaleModule deployed bytecode is 13,108 bytes (~11.4KB EIP-170 headroom), so pulling in _playerActivityScore/_mintStreakEffective (~1-1.5KB) is harmless. Game bytecode unchanged (endpoint remains for other callers).
**Implementation notes:** Use the 2-arg overload `_playerActivityScore(buyer, quests.effectiveBaseStreak(buyer))` to match the Game wrapper exactly. Re-verify module size in the build after the change (artifact figures are from the current head's forge profile; the hardhat runs=50 deployment build will differ slightly but headroom is ample).
**Invariant impact:** none | **Risk:** low

## WHALEBOON-10 [redundant_external_call / warm] L894 — file: modules/DegenerusGameWhaleModule.sol
**Description:** External self-call `IDegenerusGame(address(this)).enqueueBoxForAutoOpen(index, buyer)` round-trips through the Game's dispatcher to execute one line: `boxPlayers[index].push(player)` behind a msg.sender==address(this) guard (DegenerusGame.sol:1717-1720). boxPlayers is declared in the SHARED storage base (DegenerusGameStorage.sol:2211), so the delegatecalled module can perform the identical push directly — the guard is vacuous for an in-module write.
**Change:** Replace L894 with `boxPlayers[index].push(buyer);` (keep the explanatory comment about the auto-open enqueue).
**Excerpt (audit-time):**
```solidity
IDegenerusGame(address(this)).enqueueBoxForAutoOpen(index, buyer);
```
**Risk notes:** Behavior identical: same producer-only push, same consumer gate on lootboxRngWordByIndex != 0 (per the in-code comment). The self-call pattern may exist as deliberate single-writer discipline — confirm design intent before flattening; DegenerusGameMintModule has the same pattern (out of this file's scope) and the Game endpoint must remain for it.
**Skeptic reasoning:** Verified: boxPlayers is declared in the shared storage base (DegenerusGameStorage.sol:2211), so the delegatecalled module writes the identical slot with a direct push; the Game endpoint's entire body is the msg.sender==address(this) guard plus boxPlayers[index].push(player) (DegenerusGame.sol:1717-1720) — the guard only enforces self-call and is vacuous for an in-module write. Consumer gating (lootboxRngWordByIndex != 0) is untouched; producer-only semantics preserved; same push at the same point in the sequence. The endpoint must and does remain for MintModule (L1258, L1602) and GameAfkingModule (L983). The self-call pattern reads as code-reuse convenience, not a security boundary — there is no single-writer invariant it could enforce, since all writers already execute in the Game's storage context.
**Implementation notes:** Keep the explanatory comment about why the enqueue exists (it pins a prior auto-open hardening fix). Do not touch the Game endpoint or the MintModule/AfkingModule call sites.
**Invariant impact:** none | **Risk:** low

## WHALEBOON-11 [redundant_sload / cold] L562-L564, L601, L611-L616, L664 — file: modules/DegenerusGameWhaleModule.sol
**Description:** _purchaseDeityPass reads mintPacked_[buyer] at L563 (deity-bit check) and again inside L611-612 (read-modify-write). No code between them touches mintPacked_ (boon reads, _creditAfkingValue/_settleShortfall write only balancesPacked/claimablePool, deityPassPricePaid and presaleBoxCredit are separate mappings) — one warm SLOAD + mapping keccak is wasted. Likewise `level` is SLOADed at L601 (passLevel) and again at L664 for the pool split; no intervening call can advance level mid-purchase, so `nextShare` can branch on `passLevel == 1` instead. The third mintPacked_ read at L688 MUST remain fresh: when buyer == VAULT, getReferrer(VAULT) returns VAULT, so _applyWhalePassStats(affiliateAddr) at L660 mutates mintPacked_[buyer] (DegenerusVault.sol:587 really does buy as itself).
**Change:** Cache `uint256 mp = mintPacked_[buyer];` at L562 and reuse for the bit check; at L611 do `mp = BitPackingLib.setPacked(mp, BitPackingLib.HAS_DEITY_PASS_SHIFT, 1, 1); mintPacked_[buyer] = mp;`. Replace L664's `level == 0` with `passLevel == 1`. Leave L688's fresh read untouched.
**Excerpt (audit-time):**
```solidity
if (mintPacked_[buyer] >> BitPackingLib.HAS_DEITY_PASS_SHIFT & 1 != 0) revert E(); ... mintPacked_[buyer] = BitPackingLib.setPacked(mintPacked_[buyer], BitPackingLib.HAS_DEITY_PASS_SHIFT, 1, 1);
```
**Risk notes:** Do NOT extend the cache past the L621 external mint or the dgnrs transfers (VAULT self-affiliate case mutates mintPacked_[buyer] at L660). passLevel == 1 <=> level == 0 by construction (L601, level cannot move within the tx).
**Skeptic reasoning:** Both legs verified. (1) mintPacked_ cache L563->L611: the window contains only deityPassOwners.length, boonPacked reads/writes, _simulatedDayIndex (view), _creditAfkingValue and _settleShortfall — both verified to write only balancesPacked/claimablePool with zero external calls (storage L867-891, L949-954) — plus deityPassPricePaid/presaleBoxCredit (separate mappings). No mintPacked_ write, no external call, cache is safe. (2) passLevel==1 substitution at L664: between L601 and L664 the externals are DegenerusDeityPass.mint — verified a PLAIN mint with no onERC721Received callback (DegenerusDeityPass.sol:380-389), affiliate.getReferrer (declared view, IDegenerusAffiliate.sol:71), dgnrs.poolBalance (view) and dgnrs.transferFromPool (onlyGame, pure internal accounting, no callback — StakedDegenerusStonk.sol:493-516); no reentry vector exists, so `level` is frozen for the whole purchase and passLevel==1 <=> level==0. The recommendation correctly preserves the fresh L688 read (VAULT self-affiliate: _applyWhalePassStats at L660 mutates mintPacked_[buyer] when buyer == VAULT).
**Implementation notes:** Strictly cold-path value; bundle with WHALEBOON-02/-04 edits to the same function rather than as a standalone change. Do NOT extend the mp cache past L611.
**Invariant impact:** none | **Risk:** low

## WHALEBOON-13 [dead_code / warm] L242-L243, L418-L422, L464-L467, L476-L479 — file: modules/DegenerusGameWhaleModule.sol
**Description:** All 'legacy/stale boon' fallback branches are unreachable on the deployed system. The ONLY writer of the whale/lazy boon fields (grep-verified across contracts/) is DegenerusGameLootboxModule:1709-1731 / 1805-1826, which always sets tier (>=1, via _whaleBpsToTier/_lazyPassBpsToTier on 1000/2500/5000 bps) and the stamp day TOGETHER, and every clear mask (BP_WHALE_CLEAR = bits 200-255, BP_LAZY_PASS_CLEAR, checkAndClearExpiredBoon) wipes tier+day+deityDay together. Hence: (a) whale day != 0 with tier == 0 is impossible, so `if (discountBps == 0) discountBps = 1000; // legacy` (L243) is dead; (b) lazy day != 0 with tier == 0 is impossible, so both `boonDiscountBps == 0 -> default` blocks (L465-466, L477-478) are dead; (c) lazy tier != 0 with day == 0 is impossible, so the stale-tier clearing branch L418-422 is dead. Contracts are deploy-frozen — no pre-existing 'legacy' storage state exists.
**Change:** Delete L243 (keep _whaleTierToBps result as-is), delete the two `if (boonDiscountBps == 0) boonDiscountBps = LAZY_PASS_BOON_DEFAULT_DISCOUNT_BPS;` blocks (L465-466, L477-478) plus the now-unused LAZY_PASS_BOON_DEFAULT_DISCOUNT_BPS constant (L126), and delete the `else if (lazyTier != 0)` branch (L418-422).
**Excerpt (audit-time):**
```solidity
if (discountBps == 0) discountBps = 1000; // Default 10% for legacy boons
```
**Risk notes:** Rests on the writer census being complete: BP_WHALE_TIER/DAY and BP_LAZY_PASS_TIER/DAY writers grepped across all of contracts/ — only LootboxModule grants (tier>=1 + day co-set, upgrade-gated) and the all-field clears. Skeptic should re-run the census including any deity-direct grant path; if any writer can set day without tier (or tier without day) the branches are live.
**Skeptic reasoning:** Writer census independently re-run and confirmed COMPLETE, including the deity-direct path the recommendation flagged for re-check: the only writers of whale/lazy tier+day fields in all of contracts/ are LootboxModule._applyBoon whale (L1709-1731) and lazy (L1805-1826) blocks, both gated on `newTier > existingTier` with newTier in {1,2,3} (_whaleBpsToTier/_lazyPassBpsToTier return 0-3 only) and co-writing day. Both call sites supply nonzero days: the lootbox roll path passes currentDay = _simulatedDayIndex() >= 1 (LootboxModule L1363 via _rollLootboxBoons), and issueDeityBoon passes day = _simulatedDayIndex() >= 1 (L1059, L1076). Every clear mask wipes tier+day+deityDay together (BP_WHALE_CLEAR bits 200-255, BP_LAZY_PASS_CLEAR bits 128-183 — storage L1878, L1887-1888). Decode of tier 1-3 is nonzero (1000/2000/3500 whale; 1000/2500/5000 lazy — storage L1927-1940). Hence: (a) L243 whale legacy-default dead; (b) L465-466 and L477-478 lazy defaults dead (and LAZY_PASS_BOON_DEFAULT_DISCOUNT_BPS becomes unused); (c) L418-422 stale-tier branch dead. Contracts deploy fresh — no pre-existing legacy storage. Failure mode even under a hypothetically missed writer is a player-side discount loss, not a solvency or RNG issue.
**Implementation notes:** Delete L243, L418-422, L465-467, L477-479 and the L126 constant. Add a one-line regression asserting a granted lazy/whale boon always reads a nonzero tier-derived discount, pinning the census invariant.
**Invariant impact:** none | **Risk:** low

## RT-AFKING-WHALE-11 [redundant_sload / warm] L485-L489 (_purchaseLazyPass) — file: modules/DegenerusGameWhaleModule.sol
**Description:** On the hasValidBoon==true path, slot1 is re-read (`s1 = bpLazy.slot1;`) before the consume-clear write. But every branch that wrote slot1 earlier in the function (L408, L414, L420) also leaves hasValidBoon == false, so on this path slot1 is untouched since the L400 load — the re-read always returns the same word.
**Change:** Delete the re-read at L487 and write `bpLazy.slot1 = s1 & BP_LAZY_PASS_CLEAR;` using the existing local s1 (also drop the stale comment — the deity-cleared case cannot reach this branch).
**Excerpt (audit-time):**
```solidity
if (hasValidBoon) {
    // Re-read slot1 in case deity path already cleared, then clear lazy pass fields
    s1 = bpLazy.slot1;
    bpLazy.slot1 = s1 & BP_LAZY_PASS_CLEAR;
}
```
**Risk notes:** Verified: no write to bpLazy.slot1 is reachable between L400 and L487 with hasValidBoon == true.
**Skeptic reasoning:** Verified at L395-489: hasValidBoon becomes true ONLY at L412, a path that performs no slot1 write; every earlier slot1 write (L408 deity-day mismatch, L414 expired, L420 stale tier) forces hasValidBoon == false / unreachable-true. Between L400 (load) and L487 (re-read) with hasValidBoon == true there are only reads, pure price math (PriceLookupLib), and reverts — no external calls, no boonPacked writes (confirmed _creditAfkingValue/_settleShortfall sit AFTER L488). The re-read therefore always returns the L400 value, and the comment's deity-cleared scenario cannot reach this branch. Deleting the re-read and clearing from the existing local s1 is exactly equivalent.
**Implementation notes:** 
**Invariant impact:** none | **Risk:** low

## RT-AFKING-WHALE-12 [redundant_external_call / warm] L876 (_recordLootboxEntry) — file: modules/DegenerusGameWhaleModule.sol
**Description:** The first-deposit branch obtains the activity score via an external self-CALL: IDegenerusGame(address(this)).playerActivityScore(buyer). The Game's implementation (DegenerusGame.sol L2674-L2683) is exactly `quests.effectiveBaseStreak(player)` + the inherited 2-arg _playerActivityScore — both of which this module already has (it inherits DegenerusGameMintStreakUtils and the `quests` compile-time constant). The self-call adds a full CALL round-trip + ABI encode/decode + the Game's large selector dispatcher on every pass-bundled box (whale/lazy/deity).
**Change:** Replace with the in-context equivalent: `uint256 activityScore = _playerActivityScore(buyer, quests.effectiveBaseStreak(buyer));` — same external quests STATICCALL, no self-call hop. Keep the Game-side external function (other off-chain/peer consumers may use it).
**Excerpt (audit-time):**
```solidity
uint256 activityScore = IDegenerusGame(address(this)).playerActivityScore(buyer);
```
**Risk notes:** Equivalence verified against DegenerusGame.sol L2674-L2683 in this repo (no extra logic in the Game wrapper). Re-verify if the Game-side function ever gains logic.
**Skeptic reasoning:** Equivalence verified against live source: DegenerusGame.playerActivityScore (L2674-2683) is exactly `quests.effectiveBaseStreak(player)` fed to the inherited 2-arg _playerActivityScore (MintStreakUtils L335-340, which applies _activeTicketLevel) — no additional logic in the wrapper. DegenerusGameWhaleModule is DegenerusGameMintStreakUtils (L21) and inherits the `quests` compile-time constant (Storage L142), and under delegatecall it executes in the Game's storage context, so the in-context call reads identical state and returns the identical value (uint16 truncation at L877 unaffected; max score ~30,500 < 65,535). The replacement removes a self-STATICCALL + the Game's selector dispatch on every pass-bundled box first deposit. Keeping the Game-side external function is correct (off-chain/peer surface).
**Implementation notes:** Re-verify trivially at review time that the Game wrapper still contains no extra logic (it currently does not).
**Invariant impact:** none | **Risk:** low