# Gas round-4 packet — mint

Line numbers reference the AUDIT-TIME source (three gas rounds ago) — locate by code content, NOT line number.


## MINT-04 [redundant_external_call / hot] L1703-1709 (with DegenerusGame.sol:489-523, MintModule recordMintData L179-374) — file: modules/DegenerusGameMintModule.sol
**Description:** The ETH-purchase chain bounces through the mint module twice: MintModule._callTicketPurchase self-calls Game.recordMint{value} (required for payment/msg.value semantics — keep), and recordMint's LAST statement (Game:522) is _recordMintDataModule, which DELEGATECALLs back into MintModule.recordMintData. Game.recordMint has exactly one caller (MintModule:1703), recordMintData's only reachable chain is this one, so the trailing delegatecall-back can be eliminated by making the mint-data recording an internal call at the return point of the self-call — execution order is byte-identical (recordMintData runs immediately after recordMint's payment processing either way).
**Change:** Convert recordMintData's body to an internal function in MintModule; call it directly after the recordMint self-call returns at L1709 (inside the !payInCoin branch only, preserving the coin-path skip); delete the external recordMintData entry point, Game._recordMintDataModule (DegenerusGame.sol:1162+) and the call at Game:522. recordMint keeps its payment logic and self-call-auth unchanged.
**Excerpt (audit-time):**
```solidity
IDegenerusGame(address(this)).recordMint{value: value}(
    buyer,
    targetLevel,
    costWei,
    mintUnits,
    payKind
);
```
**Risk notes:** Touches the tail of the payment-bearing recordMint chain — recording only, not payment math; ordering preserved because the relocated call sits at the exact return point of recordMint and reverts still bubble. Skeptic should verify recordMint has no other internal Game callers of _recordMintDataModule and that mintUnits/lvl args match (Game currently forwards the same values the module passed in).
**Skeptic reasoning:** Verified end to end. Game.recordMint (489-523) has exactly one caller (MintModule:1703, the !payInCoin branch); its last statement (522) is _recordMintDataModule (1162-1178), whose only caller is Game:522; MintModule.recordMintData (179-374) is reached only via that selector (Game:1171). The recordMintData body references neither msg.value nor msg.sender (read in full: pure mintPacked_ unpack/repack + one external view affiliate.affiliateBonusPointsBest at L351, which executes from address(this)==GAME in both the old delegatecall frame and the new internal frame). Moving the call to the exact return point of the self-call (before the freshEth computation at L1715) preserves ordering byte-for-byte; recordMintData touches no claimable/pool state so the freshEth math is unaffected either way; reverts still bubble (internal call). The coin path never reaches recordMint (L1689 branch), so placing the internal call in the !payInCoin branch preserves the skip. Payment math in recordMint is untouched.
**Implementation notes:** Convert recordMintData's body to internal in MintModule; call it immediately after L1709 inside the !payInCoin branch with (buyer, targetLevel, mintUnits) — the same values currently forwarded (Game forwards its params unmodified at 522). Delete the external entry, IDegenerusGameModules.sol:224, Game._recordMintDataModule and the call at Game:522. Diff the recording behavior with a before/after state-equivalence test on same-level/new-level/<4-unit/frozen-whale paths.
**Invariant impact:** none | **Risk:** low

## MINT-05 [loop / warm] L540-557 (allocation), callers L473, L800 — file: modules/DegenerusGameMintModule.sol
**Description:** _raritySymbolBatch allocates uint32[256] memory counts + uint8[256] memory touchedTraits = 512 fresh memory words (16,384 bytes, memory arrays never pack) on EVERY call, and it is called once per queue entry inside the budget loops of processTicketBatch and processFutureTicketBatch. Solidity never reuses memory, so a batch processing N entries grows memory by 16KB*N and pays quadratic expansion: with the 550-unit writes budget and small entries (~7 units each), up to ~78 calls/batch -> ~1.28MB -> roughly 3*40000 + 40000^2/512 = ~3.1M gas of pure memory expansion plus ~120k of zero-init, entirely unmodeled by the writes-budget gas math (which assumes SSTOREs dominate). Even a modest 20-entry batch wastes ~235k gas.
**Change:** Allocate the two arrays ONCE before the while-loop in processTicketBatch and processFutureTicketBatch and pass them by memory reference into _raritySymbolBatch (and through _processOneTicketEntry); at the end of _raritySymbolBatch's existing write-back loop over touchedTraits, also zero counts[traitId] so the buffer is clean for the next entry (touchedLen is already a per-call local). Behavior and storage writes are unchanged; gas strictly decreases.
**Excerpt (audit-time):**
```solidity
uint32[256] memory counts;
uint8[256] memory touchedTraits;
uint16 touchedLen;
```
**Risk notes:** Strictly tightens the advance-chain gas ceiling margin (the 16.7M hard bound) — the worst-case many-small-players shape currently stacks ~3M of unbudgeted memory cost on top of the SSTORE budget. Reset logic must clear exactly the touched entries (3 gas each) or stale counts would corrupt trait emission — needs a focused test.
**Skeptic reasoning:** Claim verified with one location correction: the two batch loops are processFutureTicketBatch (L387-529, inlined body, _raritySymbolBatch call at L473) and processTicketBatch (L665-728 via _processOneTicketEntry, call at L800) — only those two callers exist. _raritySymbolBatch (L540) allocates uint32[256] + uint8[256] = 512 memory words (16,384 bytes) per call (L548-549); Solidity/via_ir never reuses memory, the while-loops run in a single delegatecall frame, so memory grows ~16.4KB per processed entry. Math checks out: 20 entries -> w=10,256 words -> 3w + w^2/512 ≈ 236k; ~78 small entries (550 budget / ~7 units per 1-ticket entry: baseOv 4 + 2*take + 1 finish) -> ~3.2M of pure memory expansion uncounted by the writes budget. This is a live margin liability on the daily advance chain — the fix strictly REDUCES the worst-case chunk, helping the <10M target / 16.7M ceiling. The proposed reset (zero counts[traitId] for exactly the touched entries in the existing write-back loop) is sound: only touched entries become nonzero, touchedLen is a fresh local per call, trait derivation (keccak/LCG) is untouched so RNG output is bit-identical.
**Implementation notes:** Hoist both arrays before the while-loop in BOTH processTicketBatch and processFutureTicketBatch; thread through _processOneTicketEntry by memory reference. Reset counts[touchedTraits[u]] = 0 inside the existing assembly write-back loop (or right after it). REQUIRED: a differential test asserting identical traitBurnTicket contents and TraitsGenerated events old-vs-new across randomized multi-entry batches (incl. remainder rolls, take<owed splits, 256+ takes), plus a re-run of the worst-case advance gas suite. The failure mode (stale counts) corrupts trait emission, so do not ship without the differential oracle.
**Invariant impact:** none | **Risk:** medium

## MINT-08 [redundant_check / hot] L1111-1146 and L1156-1171, also L1515 — file: modules/DegenerusGameMintModule.sol
**Description:** The single-tx purchase path computes the mint cost twice: _purchaseFor calls _mintCost (L1121) which reads jackpotPhaseFlag + level (slot 0) and runs PriceLookupLib.priceForLevel, then _purchaseForWith (L1158-1161) re-reads jackpotPhaseFlag + level and re-runs priceForLevel + the same ticketCost division (L1168). The _creditAfkingValue storage write at L1125 sits between them, blocking via_ir CSE. buyLootboxAndPresaleBox (L1515) has the same shape.
**Change:** Have _purchaseFor / buyLootboxAndPresaleBox compute (cachedJpFlag, cachedLevel, priceWei, ticketCost) once and pass them into a variant of _purchaseForWith (the afking purchaseWith entry computes them itself). No semantic change — both reads happen pre-external-call in the same frame.
**Risk notes:** level/jackpotPhaseFlag cannot change between the two reads (no external call between), so caching is equivalence-preserving.
**Skeptic reasoning:** Verified both sites. _purchaseFor:1121 calls _mintCost (1138-1146: reads jackpotPhaseFlag+level, priceForLevel, the /(4*TICKET_SCALE) division), then _purchaseForWith re-reads jackpotPhaseFlag+level at 1158-1159 and recomputes priceForLevel (1161) + ticketCost (1168); the conditional _creditAfkingValue write sits between (1125). buyLootboxAndPresaleBox has the identical shape (_mintCost at 1515 -> _purchaseForWith at 1520, nothing external between). No external call between the duplicate reads in either path, so level/jackpotPhaseFlag provably cannot change — caching is equivalence-preserving. purchaseWith (866) keeps computing its own values via the wrapper, as proposed.
**Implementation notes:** Pass (cachedJpFlag, cachedLevel, priceWei, ticketCost) into a private variant of _purchaseForWith; keep the existing signature as a thin wrapper for purchaseWith. Assert in tests that Claimable payKind (fresh=0) and the overpay->afking credit path are unchanged.
**Invariant impact:** none | **Risk:** low

## MINT-09 [redundant_sload / hot] L1244-1277 — file: modules/DegenerusGameMintModule.sol
**Description:** The lootbox branch of _purchaseForWith reads the lootboxRngPacked slot three times — _lrRead(LR_INDEX) at L1244, then _lrWrite(PENDING, _lrRead(PENDING)+x) at L1267 (both helpers SLOAD lootboxRngPacked per call, storage:1510-1517) — and the enqueueBoxForAutoOpen external self-call at L1258 sits between them, preventing CSE. Similarly presaleStatePacked is SLOADed at L1245 (_psRead) and again at L1270.
**Change:** Cache uint256 lrWord = lootboxRngPacked once at L1244, derive lbIndex and the pending-eth field from it locally, and write the updated word back with one masked SSTORE; cache uint256 psWord = presaleStatePacked at L1245 and reuse it in the presale block (the enqueue push touches only boxPlayers, never these slots, so manual caching is safe).
**Risk notes:** Manual caching across the enqueue call is correct only because enqueueBoxForAutoOpen (boxPlayers push) cannot alias lootboxRngPacked/presaleStatePacked — document that assumption at the cache site; re-verify if MINT-02 changes the call shape (after MINT-02 the push is local and the reasoning is simpler).
**Skeptic reasoning:** Verified. lootboxRngPacked is SLOADed 3x in the branch: L1244 (_lrRead index), L1267 _lrRead(PENDING) plus _lrWrite's internal read-modify-write (storage:1510-1516). presaleStatePacked is SLOADed at L1245 (_psRead, storage:1040-1041) and again directly at L1270. Between the reads sit only: lootboxEth SLOAD (1247), _unpackLootbox, the enqueue self-call (1258) and _applyLootboxBoostOnPurchase (1265, writes boonPacked only). The self-call executes the known Game.enqueueBoxForAutoOpen body (1717-1720) which writes ONLY boxPlayers (keccak-derived mapping slots — cannot alias the fixed lootboxRngPacked/presaleStatePacked slots); no untrusted code runs, so caching across it is provably safe. The presale write-back at 1277 happens after the cached read with no intervening writer.
**Implementation notes:** Apply after MINT-02 so the push is local and the non-aliasing argument disappears. Cache lrWord once, derive lbIndex and pending-eth locally, write back one masked word; cache psWord at entry of the lootbox block and feed the L1269-1277 presale block from it. Add a one-line comment documenting that nothing between the cache and the write-back touches these slots.
**Invariant impact:** none | **Risk:** low

## MINT-11 [redundant_sload / warm] L508-527 and L719-726 — file: modules/DegenerusGameMintModule.sol
**Description:** Both batch functions unconditionally SSTORE ticketCursor = uint32(idx) (L510 / L719) and then, when the queue finished, immediately overwrite the same packed slot (ticketCursor uint32 + ticketLevel uint24 share one slot, storage:499-502) with zeros (L520-526 / L723-724). On every level-finishing batch call the first write is wasted (~100 gas dirty-slot SSTORE), and the far-future promotion branch (L516-519) writes the slot a third time.
**Change:** Restructure the epilogues to compute the final (cursor, level) pair in locals and emit a single packed-slot write: only assign ticketCursor = uint32(idx) in the not-finished path; in the finished path write the promotion or zero values directly.
**Risk notes:** via_ir may already coalesce the adjacent zero-writes (L521-522) into one SSTORE but cannot remove the L510/L719 write that precedes the branch. Equivalence is trivially checkable.
**Skeptic reasoning:** Verified. ticketCursor (uint32, storage:499) and ticketLevel (uint24, storage:502) are adjacent value declarations sharing one slot (preceded by mappings which occupy whole slots). processTicketBatch writes ticketCursor = uint32(idx) at 719 then immediately overwrites cursor+level to 0 at 723-724 on the finishing path; processFutureTicketBatch same at 510 then 517-518 (promotion) or 521-522/525-526 (zero). On every queue-finishing call the first dirty-slot SSTORE (~100) is wasted; via_ir cannot elide it across the intervening keccak-slot delete of ticketQueue[rk]. Restructuring to compute the final (cursor, level) pair in locals and emit one write is trivially equivalence-checkable — same final storage state on every path.
**Implementation notes:** Only assign the slot once per epilogue path: not-finished -> (uint32(idx), unchanged level); finished+promotion -> (0, lvl|TICKET_FAR_FUTURE_BIT); finished -> (0, 0). Re-run the batch-resumption tests (cursor handoff across calls).
**Invariant impact:** none | **Risk:** low

## MINT-16 [redundant_sload / hot] L1648-1663 and L1722-1734 — file: modules/DegenerusGameMintModule.sol
**Description:** _callTicketPurchase reads compressedJackpotFlag and jackpotCounter unconditionally at L1648-1649 (slot 0, ~1 warm SLOAD after CSE of the adjacent pair), but both values are consumed ONLY under cachedJpFlag-true branches (L1653 and L1722). During the purchase phase — the dominant phase for ticket buys — the read is wasted. Additionally the jackpot step-size expression is duplicated verbatim at L1654-1660 and L1724-1728.
**Change:** Move the two reads (and a single hoisted step computation) inside an if (cachedJpFlag) block, defaulting cachedComp/cachedCnt to 0 otherwise (the L1722 guard cachedJpFlag && cachedComp != 2 keeps semantics with defaults).
**Risk notes:** Defaults must preserve the exact branch outcomes for jackpot-phase buys; trivially testable against both phases.
**Skeptic reasoning:** Verified. compressedJackpotFlag and jackpotCounter (slot-0 fields per the storage layout map, storage:50-59) are read unconditionally at MintModule:1648-1649 but consumed only under cachedJpFlag-true guards (1653 and 1722) — confirmed by reading the whole of _callTicketPurchase (1628-1802): no other consumer. During the dominant purchase phase the read is wasted (~100 warm). Moving the reads into a single if (cachedJpFlag) block AT THE SAME POSITION (before the recordMint self-call) preserves read-ordering for jackpot-phase buys exactly; defaults of 0 are never consumed when cachedJpFlag is false (both uses are short-circuit-guarded on cachedJpFlag). The step-expression dedup is real but the two sites are NOT verbatim-identical (1654-1660 includes the comp==2 -> JACKPOT_LEVEL_CAP arm; 1724-1728 runs under cachedComp != 2) — share only the inner (comp==1 && cnt window) ? 2 : 1 term.
**Implementation notes:** Keep the conditional read BEFORE the recordMint self-call so jackpot-phase values are snapshotted at the same point as today. Test both phases plus the rngLockedFlag last-day routing branch (1653-1663) and the freshBurnie inflation branch (1722-1734).
**Invariant impact:** none | **Risk:** low

## RT-MINT-04 [redundant_external_call / warm] L1343 (with DegenerusGame.sol L528-532) — file: modules/DegenerusGameMintModule.sol
**Description:** On a completed 1x-price ETH quest, the module self-CALLs Game.recordMintQuestStreak, whose body is just _activeTicketLevel() + _recordMintStreakForLevel — both inherited internal functions of DegenerusGameMintStreakUtils, which the MintModule itself already inherits. The module can run the identical logic with an internal call; purchaseLevel (== _activeTicketLevel(), computed at L1160 from the same unchanged jackpotPhaseFlag/level) is already on the stack, saving the wrapper's two slot-0 SLOADs as well. The second call site (L1885) is inside dead code (_questMint, see RT-MINT-10).
**Change:** Replace `IDegenerusGame(address(this)).recordMintQuestStreak(buyer);` at L1343 with `_recordMintStreakForLevel(buyer, purchaseLevel);` (purchaseLevel from L1160 — verified no intervening write to jackpotPhaseFlag/level, so it equals _activeTicketLevel() at this point). Then delete Game.recordMintQuestStreak (DegenerusGame.sol L528-532) and the IDegenerusGame entry — grep-verified the only live caller is L1343. Deleting it also removes the Game's SOLE internal reference to _recordMintStreakForLevel, dropping that inherited helper body (~200-300 bytes) from the Game's bytecode — the known MintStreakUtils relocation lever for the EIP-170 ceiling.
**Excerpt (audit-time):**
```solidity
if (ethMintSpendWei > 0 && questType == 1) {
                    IDegenerusGame(address(this)).recordMintQuestStreak(buyer);
                }
```
**Risk notes:** Behavior-identical: same internal function, same level argument, no events involved. mintPacked_ write ordering relative to the later _playerActivityScore read is unchanged (streak update still lands before the score read at L1355, preserving the post-action score semantics).
**Skeptic reasoning:** Verified: Game.recordMintQuestStreak (DegenerusGame.sol L528-532) is gated msg.sender==ContractAddresses.GAME (self-call only, externally unreachable) and its body is exactly _activeTicketLevel() + _recordMintStreakForLevel — both internal members of DegenerusGameMintStreakUtils, which DegenerusGameMintModule inherits (L72-75). _activeTicketLevel() == jackpotPhaseFlag ? level : level+1 (MintStreakUtils L121-123) == purchaseLevel computed at L1160 from the L1158-1159 reads; grep confirms NOTHING in MintModule writes level/jackpotPhaseFlag, and the intervening external calls (consumePurchaseBoost -> boonPacked writes; recordMint -> balances/pools/mintPacked_; affiliate.payAffiliate -> Affiliate/Quests/Coinflip own storage; quests.handlePurchase -> Quests storage) touch neither flag, so the substitution is value-identical including on the level-0, jackpot-phase, and rng-locked branches. The streak write stays at the L1343 position, before _clearCurse (L1351) and the score read (L1355) — post-action score semantics preserved. Grep confirms L531 is the Game's SOLE reference to _recordMintStreakForLevel (dropping its body from Game bytecode — the known MintStreakUtils relocation lever), while _activeTicketLevel stays (Game refs at L2546/2618/2648/2723). ORDERING DEPENDENCY: the second call site L1885 sits inside _questMint, verified dead under RT-MINT-10 — delete _questMint FIRST or the IDegenerusGame.recordMintQuestStreak interface-entry removal breaks compilation.
**Implementation notes:** Replace L1343 with _recordMintStreakForLevel(buyer, purchaseLevel); delete Game L528-532 and interfaces/IDegenerusGame.sol L251 only after RT-MINT-10 removes _questMint (L1871-1890).
**Invariant impact:** none | **Risk:** low

## RT-MINT-08 [redundant_sload / hot] L1244-1278 — file: modules/DegenerusGameMintModule.sol
**Description:** The lootbox leg touches the packed lootboxRngPacked slot three times per purchase — L1244 _lrRead(LR_INDEX), then L1267 _lrRead(LR_PENDING_ETH) nested inside _lrWrite which itself re-SLOADs the slot before the SSTORE — and presaleStatePacked twice (L1245 _psRead(PS_ACTIVE), then a fresh full read at L1270 while presale is active). The compiler cannot coalesce these because the external enqueue self-call at L1258 sits between the reads (and after RT-MINT-05 lands, an explicit single-read pattern is still clearer and guaranteed).
**Change:** Load `uint256 lr = lootboxRngPacked;` once near L1244; extract lbIndex and pendingEth from the local; compute the updated word and do a single `lootboxRngPacked = newLr;` at the L1267 position (the intervening enqueue/push provably never writes this slot). Similarly read presaleStatePacked once into a local at L1245 and reuse it for the L1270 mint-ETH update.
**Excerpt (audit-time):**
```solidity
lbIndex = uint48(_lrRead(LR_INDEX_SHIFT, LR_INDEX_MASK));
            bool presale = _psRead(PS_ACTIVE_SHIFT, PS_ACTIVE_MASK) != 0;
            ...
            _lrWrite(LR_PENDING_ETH_SHIFT, LR_PENDING_ETH_MASK, _lrRead(LR_PENDING_ETH_SHIFT, LR_PENDING_ETH_MASK) + _packEthToMilliEth(lootBoxAmount));
```
**Risk notes:** Single-writer reasoning required: confirmed nothing between L1244 and L1278 writes lootboxRngPacked or presaleStatePacked (the enqueue push touches only boxPlayers). RNG-freeze unaffected — LR_INDEX is read-only here and the pending-ETH accumulator feeds only the later VRF-request threshold, with identical final value.
**Skeptic reasoning:** Verified: _lrRead/_lrWrite (storage/DegenerusGameStorage.sol L1510-1517) each perform a full lootboxRngPacked SLOAD and _lrWrite adds another internally, so the L1244 read + L1267 _lrWrite(... _lrRead(...)) pattern is 3 SLOADs + 1 SSTORE on one slot per lootbox-leg purchase; presaleStatePacked is touched at L1245 (_psRead) and re-read fully at L1270. Single-writer reasoning confirmed line-by-line: between L1244 and L1278 the only writes are mintPacked_ (_recordLootboxMintDay L1243), boxPlayers push (the L1258 self-call, whose entire body is the gate + push — a closed self-call with no untrusted reentry surface), boonPacked (_applyLootboxBoostOnPurchase L1265, writes bp.slot0 only), and lootboxEth — none touch lootboxRngPacked or presaleStatePacked. RNG-freeze unaffected: LR_INDEX is read-only here and the LR_PENDING_ETH accumulator lands at the identical final value (it only feeds the later request threshold). Caching across the L1258 external call is provably safe because the callee is the gated push-only Game function (and becomes a plain internal push if RT-MINT-05 lands).
**Implementation notes:** Compute the new packed word from the single local and assign once at the L1267 position; reuse the L1245 local for the L1270 mint-ETH update. Keep the PS_ACTIVE-clear logic (L1274-1276) operating on the same local before the single presaleStatePacked store.
**Invariant impact:** none | **Risk:** low

## RT-MINT-09 [redundant_sload / hot] L1111-1146 with L1156-1171, L1337, L1648-1649 — file: modules/DegenerusGameMintModule.sol
**Description:** Three same-call recomputations of values already derivable from the stack: (1) _purchaseFor calls _mintCost, which reads jackpotPhaseFlag + level and runs priceForLevel + the ticket-cost mul/div; _purchaseForWith immediately re-reads both flags (L1158-1159) and recomputes priceWei/ticketCost/totalCost (L1160-1171) — the same applies on the buyLootboxAndPresaleBox path (L1514). (2) L1337 recomputes PriceLookupLib.priceForLevel(cachedLevel + 1) for quests.handlePurchase even though it equals the already-computed priceWei whenever !cachedJpFlag (the purchase phase, i.e. most days). (3) _callTicketPurchase loads compressedJackpotFlag and jackpotCounter unconditionally (L1648-1649) though both are consumed only under cachedJpFlag branches (L1653, L1722).
**Change:** (1) Have _purchaseFor read jackpotPhaseFlag/level once, compute priceWei/ticketCost/totalCost, and pass them into a refactored _purchaseForWith (private signature change only; purchaseWith computes its own). (2) Replace L1337's argument with `cachedJpFlag ? PriceLookupLib.priceForLevel(cachedLevel + 1) : priceWei`. (3) Wrap the L1648-1649 loads in `if (cachedJpFlag) { ... }` with zero-initialized locals.
**Excerpt (audit-time):**
```solidity
uint256 cost = _mintCost(ticketQuantity, lootBoxAmount); // reads jackpotPhaseFlag, level, priceForLevel
        ...
        bool cachedJpFlag = jackpotPhaseFlag;   // re-read
        uint24 cachedLevel = level;             // re-read
```
**Risk notes:** Mechanical value threading; no check is moved or removed. Item 3 must keep the locals defined (zero) for the !cachedJpFlag path used at L1722's short-circuit.
**Skeptic reasoning:** All three items verified mechanically. (1) _mintCost (MintModule L1138-1146) reads jackpotPhaseFlag+level+priceForLevel and _purchaseForWith re-reads/recomputes the identical values at L1158-1171; nothing between the two reads writes either flag (_creditAfkingValue is a balancesPacked credit), and the same double-compute exists on the buyLootboxAndPresaleBox path (L1515 then L1520). Threading priceWei/ticketCost/totalCost through the private signature is value-identical; purchaseWith (L866) computing its own keeps that path unchanged. (2) At L1337, when !cachedJpFlag, purchaseLevel==cachedLevel+1 so priceWei==priceForLevel(cachedLevel+1) exactly; the proposed cachedJpFlag ? priceForLevel(cachedLevel+1) : priceWei preserves the jackpot-phase value. (3) L1648-1649 load compressedJackpotFlag/jackpotCounter unconditionally but every consumer sits behind cachedJpFlag (L1653 branch and the L1722 condition, where && short-circuits before cachedComp on purchase days); zero-initialized locals keep the !cachedJpFlag path well-defined. No check is moved or removed; no external-call boundary is crossed by any cached value.
**Implementation notes:** Item 1 touches three call sites of _purchaseForWith (private fn, no ABI change). Item 3: declare uint8 cachedComp; uint8 cachedCnt; and populate only under cachedJpFlag.
**Invariant impact:** none | **Risk:** low

## RT-IDIOMS-04 [redundant_external_call / warm] L1343, L1885 (stub at DegenerusGame.sol L528-L532) — file: modules/DegenerusGameMintModule.sol
**Description:** On quest completion the mint module makes an external self-call `IDegenerusGame(address(this)).recordMintQuestStreak(buyer)`. The Game-side body is just `_recordMintStreakForLevel(player, _activeTicketLevel())` — both helpers are inherited by the mint module itself via DegenerusGameMintStreakUtils (L66, L121). The round trip (CALL frame + dispatch + GAME-gate) buys nothing; the module can execute the same two internal functions in-context. The Game stub then has zero callers (grep-verified: only these 2 module sites) and is removable, along with the IDegenerusGame.sol:251 declaration.
**Change:** At MintModule L1343 and L1885 replace `IDegenerusGame(address(this)).recordMintQuestStreak(x);` with `_recordMintStreakForLevel(x, _activeTicketLevel());`. Delete DegenerusGame.recordMintQuestStreak (L528-532) and the interface entry IDegenerusGame.sol:251.
**Excerpt (audit-time):**
```solidity
if (ethMintSpendWei > 0 && questType == 1) {
    IDegenerusGame(address(this)).recordMintQuestStreak(buyer);
}
```
**Risk notes:** Identical storage writes in the identical context. The msg.sender != GAME gate exists only to protect the external surface, which disappears. Confirm no off-chain tooling calls recordMintQuestStreak (it is a state-mutator with a GAME-only gate, so no third party can call it today anyway).
**Skeptic reasoning:** Verified. Game.recordMintQuestStreak (DegenerusGame.sol:528-532) is gated msg.sender == ContractAddresses.GAME (only self-calls can pass) and its body is exactly `_recordMintStreakForLevel(player, _activeTicketLevel())`. Both helpers are inherited by the mint module via DegenerusGameMintStreakUtils (MintModule declares it at L72-75; helper bodies at MintStreakUtils L66-97 and L121-123). _recordMintStreakForLevel is pure mintPacked_ storage manipulation — no msg.sender/msg.value reads, no events — so executing it in the module's delegatecall frame instead of a fresh self-call frame is byte-identical in storage effect (the only frame differences, msg.sender/msg.value, are never read). Caller set is grep-complete: MintModule L1343 and L1885 only; interface entry IDegenerusGame.sol:251; no peer contract or off-chain-tooling concern (the GAME-only gate already makes third-party calls revert today). No delegatecall is introduced, so no callvalue hazard. Bonus: L531 is the Game's SOLE referencer of _recordMintStreakForLevel, so deleting the stub also drops that helper body from the Game's bytecode — the known de-dup lever at the EIP-170 ceiling.
**Implementation notes:** Replace both sites with `_recordMintStreakForLevel(buyer, _activeTicketLevel());`, delete Game L528-532 and IDegenerusGame.sol:251. The module's bytecode grows by the helper body (its own budget, not at the ceiling).
**Invariant impact:** none | **Risk:** low