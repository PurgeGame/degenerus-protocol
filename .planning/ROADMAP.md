# Roadmap: Degenerus Protocol — Audit Repository

**Core value:** Every finding a C4A warden could submit is identified and either fixed or documented as known before the audit begins.

## Milestones

- ✅ **v44.0 sStonk Per-Day Redemption Refactor** — Phases 304-308 (shipped 2026-05-20)
- ✅ **v45.0 VRF-Rotation Liveness Fix** — Phases 309-314 (shipped 2026-05-23, minimal close)
- ✅ **v46.0 Do-Work Crank + AfKing Subscription** — Phases 316-320 (shipped 2026-05-24)
- ✅ **v47.0 Rake-Free Presale + Lootbox-Boon Unification** — Phases 321-324 (shipped 2026-05-25)
- ✅ **v48.0 sDGNRS Salvage Swap + v47 Deferred Fixes + Keeper/Pool/Tombstone/Hero** — Phases 325-328 (shipped 2026-05-26)
- ✅ **v49.0 Unified Keeper Router + Bounty Recalibration + AfKing Keeper Sweep** — Phases 329-333 (shipped 2026-05-27)
- ✅ **v50.0 Whale-Pass O(1) Refactor + AfKing Pass-Gated Subs + MintModule Advance-Divergence + External RNG-Audit Protocol** — Phases 334-338 (shipped 2026-05-28, minimal close)
- ✅ **v51.0 claimBingo — Color-Completion Claim** — Phases 339-342 (closed 2026-05-28 at IMPL HEAD `c3e9d907`; 341 TST + 342 TERMINAL folded → v52 audit)
- ⊘ **v54.0 Game-Side Keeper-Funding Ledger + AfKing De-Custody + Dead-Code/Gas Sweep** — Phases 343-347 (CLOSED-as-superseded 2026-05-30 — 343 SPEC + 344 IMPL shipped `20ca1f79`; 345/346/347 dropped → folded into v55; no `MILESTONE_V54_AT_HEAD` signal)
- ✅ **v55.0 AfKing-in-Game Redesign** — Phases 348-352 (shipped 2026-06-01; closure signal `MILESTONE_V55_AT_HEAD_ca3bbd3220de763298ef2e742111f6e6ef90d583`; subject frozen `453f8073`; baseline `20ca1f79`; 7 phases incl. 349.1/349.2; 0 NEW_FINDINGS)
- ▶ **v56.0 AfKing Everyday-Gas Minimization** — Phases 353-357 (ACTIVE 2026-06-01; baseline = v55 HEAD frozen `453f8073` / `MILESTONE_V55_AT_HEAD_ca3bbd3220de763298ef2e742111f6e6ef90d583`)

---

## ▶ v56.0 AfKing Everyday-Gas Minimization (ACTIVE 2026-06-01)

**Milestone:** v56.0 (started 2026-06-01)
**Defined:** 2026-06-01
**Audit baseline → subject:** v55.0 closure HEAD — frozen contract subject `453f8073`, closure signal `MILESTONE_V55_AT_HEAD_ca3bbd3220de763298ef2e742111f6e6ef90d583` → v56.0 closure HEAD. Subject = the single carefully-sequenced batched `contracts/*.sol` diff for the mode-agnostic aggregator + the ticket-mode minimal-write primitive + the open-end optimizations + the shared-`DegenerusQuests`-core batched-settle entrypoint. Every cited `file:line` MUST be re-attested against the v55.0-closure HEAD `453f8073` before any patch.
**Scope source:** `.planning/REQUIREMENTS.md` (24 v56.0 REQ-IDs across 8 categories: AGG 5 · TKT 2 · AFF 2 · QST 5 · OPEN 2 · GAS 4 · SEC 2 · XMODEL 1 · AUDIT 1) + the milestone init (2026-06-01) + the design-lock input `.planning/PLAN-V56-AFKING-BATCHING-GAS.md` + the `[[v56-batch-afking-affiliate-quest-seed]]` memory. **No research** — a fully-specced internal contract gas+batching refactor with a USER-converged mechanism (the mode-agnostic ~10-day aggregator); the design explorations (affiliate-distribution non-gameability, ticket-mode parity) are resolved in the plan doc.

> **Scope (USER-locked):** the AFKING SYSTEM specifically — **BOTH ENDS: the BUYING (the per-day process STAGE / accrual / settle) AND the OPENING (the box open-pass / materialize).** Make the whole afking path maximally gas-efficient while remaining COMPLETELY secure and UNMANIPULABLE (no economic edge from any "fuckery" — with PARTICULAR focus on gaining an edge by subscribing/unsubscribing strategically). NOT a whole-ecosystem sweep — afking buy + open only (shared-code touches handled with care).

> **Posture — NOT a behavior-identical pass.** Slight semantic simplifications are acceptable if they cut gas and stay unmanipulable; the hard floor is unmanipulable + secure (enforced by a mandatory 3-skill adversarial economic review PLUS a baked-in cross-model Codex + Gemini review — XMODEL-01). The unifying design (USER-converged 2026-06-01) is a MODE-AGNOSTIC accrue-cheap-per-buy aggregator that settles all affiliate + quest ("the extraneous stuff") for BOTH ticket and lootbox subs: accrue cheap per-buy into a per-sub accumulator (NO cross-contract calls), then settle per-leg — the AFFILIATE leg is a flat-7% deterministic-split PULL (`claim`/`withdraw`, NO scheduled flush, NO roll) and the QUEST leg settles AUTOMATICALLY by RIDING THE DAILY BUY STAGE on the global settle day (`currentDay % settlePeriod == 0`, ~10-day cadence — the slot-0 `questProgress` delivered-day counter mints `× QUEST_SLOT0_REWARD` **+ the accrued `buyerOwedBurnie` 10%/20% ticket buyer-bonus** in ONE `creditFlip` to the sub) + a permissionless `claimQuest(subs[])` keeper-liveness fallback (+ quest-settle-on-unsub) — _amended 2026-06-01 — quest settle RIDES THE BUY STAGE (the separate `mintBurnie` "settlement-due" router leg is REJECTED as a redundant cold-SLOAD pass; `SUB_STAGE_BATCH` SHRUNK so the settle-day chunk fits 16.7M, number deferred to 355); the afking ticket buyer-bonus 10%/20% is accrued + settled with the quest (matches live `:1655`); first-sub-only +daysToNextSettle streak, no provisional: SUPERSEDES the prior "scheduled-roll/player-flush-replays-the-roll" mechanism; the roll is removed_. Each mode's per-day work collapses to a minimal primitive — lootbox = the existing box-stamp; ticket = a NEW custom "write the ticket entries to the queue" fn that mirrors the box-stamp (dropping the per-day `MintModule.purchaseWith` ~262k heavyweight, **but ACCRUING the 10%/20% ticket buyer-bonus `:1655-1659` into `buyerOwedBurnie` at live parity — closing the v55-style dropped-bonus regression**). Plus a STAGE gas sweep (accumulator in the `Sub` slot's spare bits, `SUB_STAGE_BATCH` re-tune, mode/SLOAD collapse) + fold-in O1 (the pre-existing `DegenerusQuests` lootbox-quest BURNIE double-credit, surfaced at the v55 close).

> **Why it is unexploitable (the load-bearing non-exploitability fact, carried into SEC/XMODEL):** the afking affiliate distribution is a **flat-7% deterministic-split PULL** with NO roll and NO seed (_amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic: SUPERSEDES the prior daily-seeded roll_) — per buy accrue a flat `_ethToBurnie(eth)×7/100` into the running `affiliateBase`, settle by PULL via `claim` with the fixed 75/20/5 split + `withdraw` (CEI). The **buyer never receives the base** (`A ≠ sub` guaranteed + the rare U1/U2==sub cycle skip, `DegenerusAffiliate.sol:579`) → no settle-timing profit. Because there is exactly ONE deterministic distribution path (no roll, no seed, no scheduled-vs-player-flush divergence), there is **no favorable seed to select AND no two-distribution free option to arbitrage** — the XMODEL C1/C2 free-option finding is MOOT (the roll is removed). The taper is afking-N/A (flat 7%; the anti-concentration taper applies to manual buys only). No double-settle: self-marking running balances (`affiliateBase` zeroed at `claim`; the slot-0 `questProgress` delivered-day counter drained at the automatic `mintBurnie` settlement-due flush — or the permissionless `claimQuest(subs[])` fallback) — the per-sub `windowStartDay`/`lastSettledDay` markers are DROPPED. Streak-farming is gated by the active-pass check + the `lastCompletedDay`/`afkCoveredThroughDay` double-credit guards + the per-window streak advancing only on debit-DELIVERED days; the first-sub-only `+daysToNextSettle` head-start is bounded +0..+9 once/account (USER-ACCEPTED-BY-DESIGN — the bound replaces the prior confirmed-vs-provisional escrow; sybil farming is pass-cost-gated — _amended 2026-06-01 — automatic slot-0 BURNIE via mintBurnie chain + claimQuest fallback; first-sub-only +daysToNextSettle streak, no provisional_). Adversarially re-cleared (economic-analyst + zero-day-hunter, no Medium+).

> **Cross-cutting rule (every requirement):** every cited `file:line` MUST be re-attested against the **v55.0 closure HEAD `453f8073`** before any patch (no "by construction" / "single fn reaches all paths" claim survives un-checked — the `DegenerusGame` mint/jackpot inline-duplication precedent; `feedback_verify_call_graph_against_source`). The cited anchors (the per-buy cross-contract storm `GameAfkingModule.sol:760-831` / the lootbox stamp branch `:735-749` / the ticket-mode `purchaseWith` route `:713-731`; the affiliate daily-seeded roll `DegenerusAffiliate.sol:558`/`:579` + the leaderboard `affiliateCoinEarned[lvl]` + `_updateTopAffiliate`; the `DegenerusQuests.handlePurchase` double-credit `:890` + `awardQuestStreakBonus`; the afking open path `_openAfkingBox`→`resolveAfkingBox` + `mintBurnie`/`autoOpen`/`OPEN_BATCH`; the `Sub` 4-field stamp `(scorePlus1, amount, lastAutoBoughtDay)` + `lastOpenedDay` in `DegenerusGameStorage`; the `_applyEvMultiplierWithCap`/`lootboxEvBenefitUsedByLevel[player][level+1]` EV-cap; the STAGE placement in `DegenerusGameAdvanceModule`) are confirmed at SPEC. **Security / freeze / solvency floor over gas** (`feedback_security_over_gas`): SEC-01 (unmanipulable, esp. strategic sub/unsub) + SEC-02 (SOLVENCY-01 untouched — affiliate/quest stay BURNIE flip-credit OFF the ETH/`claimablePool` path; RNG-freeze intact) are the hard gate. Pre-launch redeploy-fresh (storage-layout break fine, no migration; `feedback_frozen_contracts_no_future_proofing`).

> **Posture:** the contract changes ship as ONE carefully-sequenced batched USER-APPROVED `contracts/*.sol` diff with a **HARD STOP at the contract-commit boundary** (applied + locally compiled/tested, NEVER committed without explicit user hand-review — `feedback_batch_contract_approval` + `feedback_never_preapprove_contracts` + `feedback_manual_review_before_push` + `feedback_no_contract_commits`; `ContractAddresses.sol` freely modifiable per `feedback_contractaddresses_policy`). The IMPL (354) is the ONE contract gate — the aggregator (accrue + scheduled/alteration settle), the ticket-mode custom write primitive, the `DegenerusQuests` batched-settle entrypoint, the accumulator, and the open-end optimizations land as a single producer-before-consumer diff. Sequential-on-main (worktrees unsafe — submodule + node_modules; `use_worktrees: false`, `no_worktree_paths: [contracts]`). Tests + planning + docs AGENT-committable.

> **Audit posture — FULL CLOSE WITH ITS OWN INTERNAL SWEEP (NOT deferred).** Like v54.0 + v55.0 (and unlike v50.0 + v51.0, which deferred their internal sweep → the v52 consolidated audit), **v56.0 runs its own internal 3-skill genuine-PARALLEL adversarial sweep + delta-audit + `audit/FINDINGS-v56.0.md` at TERMINAL (357, AUDIT-01)** — the 3-skill pass (`/contract-auditor` + `/economic-analyst` + `/zero-day-hunter`; `/degen-skeptic` = the dual-gate filter per `D-271-ADVERSARIAL-02`) AUGMENTED by the cross-model close (Codex + Gemini) + the delta-audit + `audit/FINDINGS-v56.0.md` (chmod 444) + the atomic 5-doc closure flip all execute in-milestone, focused on the **strategic sub/unsub edge + the shared-quest-core non-perturbation + the two-path open**. Rationale: this touches the **shared `DegenerusQuests` core + the everyday `advanceGame` STAGE** — the unmanipulability + non-perturbation are the load-bearing concerns and must be adversarially probed in-milestone, not deferred. (The separate v52 consolidated cross-model audit still folds the v56 surface into its cumulative sweep — an additional track, not a substitute for v56's own close.)

> **Cross-model review (XMODEL-01) is baked in at TWO touchpoints.** It runs at **SPEC (353)** as a DESIGN INPUT — Codex + Gemini are fed crafted prompts on the buy+open design (long-run gas suggestions + adversarial "unmanipulable" verification, PARTICULARLY the strategic sub/unsub edge); the suggestions + exploit findings are folded into the design-lock BEFORE IMPL (the gating deliverable — this is XMODEL-01's home/primary deliverable). The same cross-model models then AUGMENT the **TERMINAL (357)** adversarial close (Claude 3-skill + Codex + Gemini). Both touchpoints are reflected in the respective phases' success criteria.

> **Phase numbering** continues from the previous milestone — v55.0 ran 348–352, so **v56.0 starts at Phase 353.** Not reset to 1. (Prior milestones' phase dirs are archived under `.planning/milestones/vXX.0-phases/`.)

> **Milestone shape** is the established v54.0 + v55.0 audit pattern (with a dedicated GAS phase): **353 SPEC design-lock (the mode-agnostic aggregator + the ticket-mode minimal-write primitive + the afking-OPEN-end review + the per-sub accumulator storage layout + the shared-`DegenerusQuests`-core non-perturbation approach + the freeze/solvency/unmanipulable re-attestation + the XMODEL-01 design-input cross-model pass folded in + grep-attest every `file:line` vs `453f8073`) → 354 IMPL the ONE carefully-sequenced batched contract diff (the aggregator [accrue + scheduled/alteration settle] + the ticket-mode custom write primitive + the `DegenerusQuests` batched-settle entrypoint + the accumulator + the open-end optimizations; producer-before-consumer) → 355 GAS (measure + tune — per-buy + per-open + per-settle marginals under the 16.7M ceiling, accumulator packing, `SUB_STAGE_BATCH` re-tune, mode/SLOAD collapse) → 356 TST (the proofs — unmanipulable [esp. strategic sub/unsub] + non-perturbation of the shared quest core + two-path-open coexistence + gas marginals + NON-WIDENING regression) → 357 TERMINAL FULL close (delta-audit + 3-skill genuine-PARALLEL adversarial sweep AUGMENTED by the XMODEL cross-model close + `audit/FINDINGS-v56.0.md` + atomic 5-doc closure flip).** The contract-boundary HARD STOP lives at ONE phase (354 IMPL).

### Phases

- [x] **Phase 353: SPEC — Design-Lock (mode-agnostic aggregator + ticket-mode primitive + open-end review + accumulator layout + shared-quest non-perturbation + XMODEL design-input) + Unmanipulable/Solvency Re-Attestation + Call-Graph Attestation** - Design-lock the mode-agnostic ~10-day aggregator (accrue-cheap-per-buy → settle-once: the affiliate accrue/settle split [AFF-01 the **flat-7% deterministic-split PULL** — accrue flat `_ethToBurnie(eth)×7/100` per buy into the running `affiliateBase`, settle by PULL via `claim`/`withdraw` with the fixed 75/20/5 split (buyer-never-wins, `DegenerusAffiliate.sol:579`); NO roll, NO seed, NO scheduled/mutation affiliate flush — _amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic: SUPERSEDES the XMODEL roll-unification — the roll itself is REMOVED, so the C1/C2 free-option finding is MOOT; adversarially re-cleared (economic-analyst + zero-day-hunter, no Medium+)_; AFF-02 taper afking-N/A (manual-only) + leaderboard claim-time] + the per-sub accumulator storage layout [the SHRUNK in-slot `affiliateBase`+`questProgress` running balances + the off-slot `pendingClaim` mapping; `settledThroughDay`/`windowStartDay`/`lastSettledDay` DROPPED] + the QUEST-only self-marking settle) + the ticket-mode minimal-write primitive shape (the custom "write the ticket entries to the queue" fn mirroring the box-stamp + the century/x00 quantity-bonus parity keep-or-drop decision) + the afking-OPEN-end review (the `_openAfkingBox`→`resolveAfkingBox` + `mintBurnie`/`autoOpen`/`OPEN_BATCH` materialization + the unmanipulable re-verification) + the shared-`DegenerusQuests`-core batched-settle entrypoint shape + the non-perturbation approach (PROVE non-perturbing to manual/bingo/degenerette/boon callers) + the QST design (automatic slot-0 BURNIE via the `mintBurnie` settlement-due chain + a `claimQuest` fallback; the first-sub-only `+daysToNextSettle` streak head-start [bounded +0..+9, no provisional — _amended 2026-06-01 — automatic slot-0 BURNIE via mintBurnie chain + claimQuest fallback; first-sub-only +daysToNextSettle streak, no provisional_]) + the unmanipulable (esp. strategic sub/unsub) + SOLVENCY-01-untouched + RNG-freeze-intact re-attestation; run the XMODEL-01 cross-model design-input pass (Codex + Gemini crafted prompts on buy+open → fold gas suggestions + exploit findings into the design-lock BEFORE IMPL); grep-attest every cited `file:line` vs `453f8073` — paper-only, ZERO `contracts/*.sol`. (completed 2026-06-01)
- [x] **Phase 354: IMPL — The ONE Carefully-Sequenced Batched Contract Diff (aggregator + ticket-mode primitive + DegenerusQuests batched-settle + accumulator + open-end)** - Land the single reconciled `contracts/*.sol` diff producer-before-consumer: the per-sub accumulator (`DegenerusGameStorage` — packed per the SPEC layout) + the mode-agnostic aggregator in the STAGE (amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic) (AGG-01 accrue flat-7% affiliate-base + quest-progress running balances (+ the `buyerOwedBurnie` 10%/20% ticket buyer-bonus on ticket subs) with NO cross-contract calls; the AFFILIATE leg is PULL — `claim`/`withdraw` with the fixed 75/20/5 split, NO scheduled/mutation affiliate flush; AGG-02 the QUEST-only automatic settle RIDING THE DAILY BUY STAGE on the global settle day [amended 2026-06-01 — the separate `mintBurnie` "settlement-due" router leg is REJECTED as a redundant cold-SLOAD pass; the inline `_settleQuest` mints `questProgress × QUEST_SLOT0_REWARD` + the accrued `buyerOwedBurnie` ticket bonus in ONE `creditFlip`; `SUB_STAGE_BATCH` SHRUNK so the settle-day chunk fits 16.7M, number deferred to 355]; AGG-03 the QUEST-settle-on-unsub BEFORE applying the change [the affiliate base is NOT flushed — it persists for the uplines to PULL]; AGG-04 mode-agnostic uniform QUEST-settle for BOTH ticket + lootbox subs [the affiliate base likewise pulled uniformly via `claim`]; AGG-05 the self-marking running balances [`windowStartDay`/`lastSettledDay` double-settle markers DROPPED]) + the ticket-mode minimal-write primitive (TKT-01 the custom "write the ticket entries to the queue" fn replacing the per-day `purchaseWith` heavyweight + the inline affiliate/quest deferred to the aggregator + the 10%/20% ticket buyer-bonus `:1655-1659` ACCRUED into `buyerOwedBurnie` [closing the v55-style dropped-bonus regression]; TKT-02 resolution-equivalent ticket entries + the century/x00 parity decision applied + the 10%/20% buyer-bonus at live parity, minted with the quest at the STAGE settle — amended 2026-06-01 — the afking ticket primitive accrues the 10%/20% buyer-bonus [matches live `:1655`, minus kickback], settled with the quest) + the `DegenerusQuests` batched-settle entrypoint + the streak machinery (amended 2026-06-01 — automatic slot-0 BURNIE via mintBurnie chain + claimQuest fallback; first-sub-only +daysToNextSettle streak, no provisional) (QST-01 the first-sub-only `+daysToNextSettle` head-start [bounded +0..+9, no provisional] on the ±10-per-window model + slot-0 = a `questProgress` delivered-day counter → settle-mint `× QUEST_SLOT0_REWARD` [+ the accrued `buyerOwedBurnie` ticket bonus] via the STAGE-riding settle on the global settle day / `claimQuest` fallback + slot-1 manual; QST-02 the bounded-accepted first-sub head-start REFRAME [USER-ACCEPTED-BY-DESIGN, the bound replaces the prior escrow], the activity-score reads the actual streak, the per-window streak advances only on debit-DELIVERED days; QST-03 the `lastCompletedDay`/`afkCoveredThroughDay` double-credit guard + active-pass anti-reset, slot rewards never suppressed; QST-04 the entrypoint proven non-perturbing to manual/bingo/degenerette/boon callers; QST-05 the O1 lootbox-quest double-credit confirmed-intended-or-fixed) + the open-end optimizations (OPEN-01 the `_openAfkingBox`→`resolveAfkingBox` + `mintBurnie`/`autoOpen`/`OPEN_BATCH` path optimized for max gas, cheapest viable shared materialization with the human path, no cold ledger; OPEN-02 the open stays COMPLETELY unmanipulable — live-level parity with human `openLootBox`, no double-open via `lastOpenedDay` monotone, no EV-cap double-draw on the shared `(player,level)` budget, no shared-mutable-state hazard — RE-VERIFIED after the accrual/settle refactor) — authored producer-before-consumer, applied to `contracts/` and locally compiling (`forge build` clean), then HELD at the contract-commit boundary for explicit user hand-review. (completed 2026-06-01)
- [ ] **Phase 355: GAS — Measure + Tune (per-buy / per-open / per-settle marginals under 16.7M + accumulator packing + SUB_STAGE_BATCH re-tune + mode/SLOAD collapse)** - Measure + tune the everyday cost under the security-over-gas floor (gas-scavenger surfaces, gas-skeptic validates — any simplification must stay unmanipulable; not-real or invariant-trading wins REJECTED with reasoning): the per-buy marginal measurably reduced (GAS-01 target lootbox ~206k → ~130–140k; ticket off the ~262k `purchaseWith` heavyweight; measured per-buy + per-settle marginal via a TST-06-style harness under the 16.7M HARD per-tx ceiling); the per-sub accumulator packed into the `Sub` slot's spare bits where feasible (GAS-02 no new cold per-buy SSTORE); `SUB_STAGE_BATCH` re-tuned for the lower per-sub cost (GAS-03 throughput/headroom; the per-day STAGE stays under the 16.7M ceiling at the `SUBSCRIBER_CAP`); and redundant payment-mode branches / repeated SLOADs in the STAGE collapsed where a slight simplification is cheaper (GAS-04, allowed under the scope latitude). Any net contract change beyond the IMPL diff rides the same batched USER-APPROVED diff held at the contract-commit boundary; if the savings are structural to the IMPL refactor (Outcome-A precedent, v55 350), that is recorded as the outcome.
- [x] **Phase 356: TST — Unmanipulable (strategic sub/unsub) + Quest-Core Non-Perturbation + Two-Path-Open Coexistence + Gas Marginals + Non-Widening** ✅ COMPLETE 2026-06-02 (7/7 plans; 4 new v56 green proofs [V56SecUnmanipulable 11 / V56FreezeSolvency 7 / V56QuestNonPerturb 7 / V56AfkingGasMarginal 15] + the D-10 10-file offset migration + the BY-NAME NON-WIDENING ledger `REGRESSION-BASELINE-v56.md` [live v56 624/134/30 byte-identical to the empirical 453f8073 baseline union BY NAME; the 14 migration-unmasked v56-behavior reds DROPPED-by-name; SOLVENCY-01 leg-1 byte-diff anchor; Hardhat compile-sanity EXIT 0]; drainAffiliateBase dispatch-stub reachability carried to 357; ZERO contracts/*.sol mutation) - Prove the milestone behaviorally correct + the hard security floor empirically against the v55-baseline tree (the SEC gate): the afking system (buy + open) is unmanipulable — no positive-EV vector from settle-timing (no seed — _amended 2026-06-01: the roll is REMOVED under the flat-7% deterministic-split pull_), **strategic sub/unsub churn (the USER-flagged PRIMARY concern — churn to re-claim the affiliate [forfeit-nothing-gain-nothing] / dodge a streak penalty / harvest or duplicate a settlement)**, re-rate-on-alteration, pre-credit-EV inflation, double-credit, open-timing, or settle-griefing (SEC-01 — proven empirically + adversarially); SOLVENCY-01 untouched (SEC-02 — the affiliate/quest rewards remain BURNIE flip-credit OFF the ETH/`claimablePool` path, the ETH/pool debit byte-unchanged, RNG-freeze intact under the new accrual/settle); the shared `DegenerusQuests` core batched-settle entrypoint is non-perturbing to the manual/bingo/degenerette/boon callers; the two open routes (afking-stamp vs human `lootboxEth`/`boxPlayers`) coexist with no shared-state hazard; the per-buy + per-open + per-settle marginal gas is measured under the 16.7M HARD per-tx ceiling (the GAS-01..04 wins proven same-results); and the suite is NON-WIDENING vs the v55 baseline `453f8073` (every pre-existing red enumerated BY NAME, `REGRESSION-BASELINE-v56.md`).
- [ ] **Phase 357: TERMINAL — Delta Audit + 3-Skill Genuine-PARALLEL Adversarial Sweep (AUGMENTED by XMODEL Codex + Gemini close) + FINDINGS-v56.0 + Closure Flip** - Close the v56.0 audit subject (the carefully-sequenced batched diff — the aggregator + the ticket-mode primitive + the open-end optimizations + the `DegenerusQuests` batched-settle, FROZEN at the IMPL+GAS HEAD) via a FULL close that runs its own internal sweep IN-MILESTONE (NOT deferred to v52): a delta-audit (every changed surface NON-WIDENING vs the v55 baseline `453f8073` with zero orphan hunks; SOLVENCY-01 byte-unchanged + RNG-freeze intact + the affiliate deterministic-split-PULL non-gameability (NO roll/seed — _amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic_) + the open-end two-path/no-double-open + the shared-quest-core non-perturbation re-attested) + the mandatory 3-skill genuine-PARALLEL adversarial economic review (`/contract-auditor` + `/economic-analyst` + `/zero-day-hunter`; `/degen-skeptic` = the dual-gate filter per `D-271-ADVERSARIAL-02`) focused on the strategic sub/unsub edge + the settle-timing non-exploitability (no seed — _amended 2026-06-01: the roll/seed is REMOVED under the flat-7% deterministic-split pull_) + the pre-credit-EV inflation + the two-path open, AUGMENTED by the XMODEL-01 cross-model close (Codex + Gemini fed crafted prompts on the FULL afking system buy+open, augmenting the Claude sweep) + `audit/FINDINGS-v56.0.md` (chmod 444) + the atomic 5-doc closure flip with the `MILESTONE_V56_AT_HEAD_<sha>` signal — the internal sweep + the cross-model close run IN-MILESTONE (NOT deferred to v52).

---

## Phase Details

### Phase 353: SPEC — Design-Lock + Unmanipulable/Solvency Re-Attestation + XMODEL Design-Input + Call-Graph Attestation

**Goal**: The v56.0 mechanism is settled in writing so the IMPL phase authors a fully reconciled diff with zero "by construction" assumptions, and the load-bearing non-exploitability + non-perturbation are designed BEFORE any code is written: the mode-agnostic ~10-day aggregator (the affiliate accrue/settle split + the per-sub accumulator storage layout + the double-settle markers), the ticket-mode minimal-write primitive shape (+ the century/x00 parity decision), the afking-OPEN-end review (the materialization + the unmanipulable re-verification), the shared-`DegenerusQuests`-core batched-settle entrypoint shape + the non-perturbation approach, and the QST design (automatic slot-0 BURNIE via the `mintBurnie` settlement-due chain + a `claimQuest` fallback; the first-sub-only `+daysToNextSettle` streak head-start [bounded +0..+9, no provisional] — _amended 2026-06-01 — automatic slot-0 BURNIE via mintBurnie chain + claimQuest fallback; first-sub-only +daysToNextSettle streak, no provisional_) are LOCKED; the unmanipulable (esp. strategic sub/unsub) + SOLVENCY-01-untouched + RNG-freeze-intact properties are re-attested on paper; the XMODEL-01 cross-model design-input pass (Codex + Gemini crafted prompts) is run and its gas suggestions + exploit findings folded into the design-lock BEFORE IMPL; and every cited `file:line` is grep-verified against the v55.0 HEAD `453f8073` — paper-only, zero `contracts/*.sol`.
**Type**: SPEC
**Depends on**: Nothing (first v56.0 phase; consumes the v55.0 closure HEAD `453f8073` as the frozen audit baseline, plus the design-lock input `.planning/PLAN-V56-AFKING-BATCHING-GAS.md` + the `[[v56-batch-afking-affiliate-quest-seed]]` memory)
**Requirements**: AFF-01, AFF-02, XMODEL-01
**Success Criteria** (what must be TRUE):

  1. The affiliate distribution is design-locked + proven non-gameable (AFF-01 / AFF-02) — the afking affiliate distribution is a **flat-7% deterministic-split PULL**: accrue `_ethToBurnie(ethSpent)×7/100` (flat, no taper, no kickback) per buy into the sub's running `affiliateBase`; settle by PULL via `claim(address[] subs)` (same-affiliate batch, ONE `sumB`, the fixed 75/20/5 split computed once → `pendingClaim[A/U1/U2]`, buyer-never-wins via `A ≠ sub` guaranteed + the rare U1/U2==sub cycle skip); `withdraw()` = the only cross-contract `creditFlip` (CEI). There is NO roll, NO seed, NO scheduled/mutation affiliate flush — so settle-timing can NEVER select a favorable seed AND no two-distribution free option can exist (exactly ONE deterministic path) (amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic: SUPERSEDES the XMODEL roll-unification — the roll itself is REMOVED, so the C1/C2 free-option finding is MOOT; adversarially re-cleared, economic-analyst + zero-day-hunter, no Medium+) (the load-bearing non-exploitability fact: the split is EV-neutral + intra-upline-chain-only-redistributive + buyer-never-wins, `DegenerusAffiliate.sol:579`, re-attested) (AFF-01); the activity taper is afking-N/A (flat 7%; `_applyLootboxTaper` applies to MANUAL buys only), and the affiliate leaderboard credits at `claim` time to the direct affiliate `A` (USER-accepted claim-time distortion — there is no scheduled affiliate settle under the pull) (AFF-02).

  2. The mode-agnostic aggregator + the per-sub accumulator storage layout + the self-marking settle are design-locked (AFF-01/AFF-02 inputs; AGG/GAS-02 design feed) — the affiliate leg is PULL (no scheduled flush) while the QUEST leg settles uniformly for BOTH ticket and lootbox subs (the single mode-agnostic quest-settle path); the per-sub accumulator layout is decided (the SHRUNK in-slot `affiliateBase uint32 (whole-BURNIE, 100M clamp)` + `questProgress` running balances re-packed into the same Sub slot — attested at 232/256, NOT the stale "176/256" premise — plus the off-slot `pendingClaim` mapping for the affiliate recipients); and double-settle is impossible via **self-marking running balances** — the affiliate `claim` zeroes `affiliateBase[sub]` and the quest flush drains `questProgress` (both a no-op on a double-fire), so the per-sub `windowStartDay`/`lastSettledDay`/`settledThroughDay` markers are DROPPED — recorded for the IMPL to BUILD (amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic) (AGG-01..05 / GAS-02 are owned at IMPL/GAS; their design is fixed here).

  3. The ticket-mode minimal-write primitive + the afking-OPEN-end review are design-locked (TKT/OPEN design feed) — the custom "write the ticket entries to the queue" fn shape is locked (a cheap `_queueTicketsScaled`-equivalent primitive mirroring the lootbox box-stamp, producing resolution-equivalent placement/trait/quantity, the affiliate + quest deferred to the aggregator), and the afking-ticket century/x00 quantity-bonus parity (`MintModule:1243`) keep-or-drop decision is made (allowed to drop for simplicity under the scope latitude); and the afking-OPEN-end (`_openAfkingBox`→`resolveAfkingBox` + `mintBurnie`/`autoOpen`/`OPEN_BATCH`) is reviewed for the cheapest viable materialization shared with the human path AND re-verified unmanipulable (live-level parity, no double-open, no EV-cap double-draw, no shared-state hazard) — recorded for the IMPL (TKT-01/02 + OPEN-01/02 are owned at IMPL; their design is fixed here).

  4. The shared-`DegenerusQuests`-core non-perturbation approach + the QST design (automatic slot-0 BURNIE + first-sub-only streak head-start) are design-locked + the O1 disposition is decided (QST design feed; amended 2026-06-01 — automatic slot-0 BURNIE via mintBurnie chain + claimQuest fallback; first-sub-only +daysToNextSettle streak, no provisional) — the batched-settle entrypoint added to the shared `DegenerusQuests` core has a locked shape with a non-perturbation approach (PROVE non-perturbing to the manual / bingo / degenerette / boon callers — `awardQuestStreakBonus` etc.); the QST settle/streak design is derived — slot-0 completion accrues a delivered-day `questProgress` COUNTER → settled = mint `× QUEST_SLOT0_REWARD` via the `mintBurnie` "settlement-due" router chain when due (~10-day cadence) + a permissionless `claimQuest(subs[])` keeper-liveness fallback (always credits the sub, never the caller) + a quest-settle-on-unsub (slot-1 stays MANUAL; the ±10 streak is the activity-score multiplier, NOT direct BURNIE), the streak uses a SIMPLIFIED first-sub-only `+daysToNextSettle` head-start (`hasEverSubscribed` 1-bit; bounded +0..+9 over the manual baseline, no provisional/vesting) on the ±10-per-window model (−10 on unsub) with the per-window streak advancing only on debit-DELIVERED days, the `lastCompletedDay`/`afkCoveredThroughDay` double-credit guard + active-pass anti-reset (slot rewards NEVER suppressed); the bounded +0..+9 first-sub head-start is USER-ACCEPTED-BY-DESIGN (the bound REPLACES the prior confirmed-vs-provisional escrow for the afking grant — a deliberate accepted tradeoff, NOT a missed control; the activity-score still reads the actual `state.streak`); and the pre-existing O1 lootbox-quest BURNIE double-credit (`DegenerusQuests.handlePurchase` internal `creditFlip` + the returned-and-re-credited value, `:890`) is adjudicated intended-vs-bug with the fix designed if a genuine over-mint — recorded for the IMPL (QST-01..05 are owned at IMPL; their design is fixed here).

  5. The unmanipulable/solvency/freeze re-attestation + the XMODEL design-input pass land + every cited `file:line` is grep-attested vs `453f8073` (XMODEL-01; SEC design feed) — the unmanipulable property (esp. the strategic sub/unsub edge), SOLVENCY-01-untouched (the affiliate/quest stay BURNIE flip-credit OFF the ETH/`claimablePool` path; this is a BURNIE-emission-timing change only), and RNG-freeze-intact-under-the-new-accrual/settle are re-attested on paper as the SPEC's design gate (SEC-01/02 are owned at TST; their design is fixed here); the XMODEL-01 cross-model DESIGN-INPUT pass is run — Codex + Gemini are fed crafted prompts covering the FULL afking system (the BUYING STAGE/accrual/settle AND the OPENING open-pass/materialize) for (a) long-run gas-optimization suggestions and (b) adversarial verification that the design is completely unmanipulable (PARTICULAR focus on the strategic sub/unsub edge), and the suggestions + exploit findings are folded into the design-lock BEFORE IMPL (this is XMODEL-01's primary deliverable / home; its TERMINAL close-augmentation half is reflected in Phase 357's success criteria); and every cited anchor across the milestone scope (the per-buy cross-contract storm + the lootbox stamp branch + the ticket `purchaseWith` route in `GameAfkingModule`, the affiliate roll + leaderboard sites, the `DegenerusQuests.handlePurchase` double-credit + `awardQuestStreakBonus`, the afking open path, the `Sub` stamp + the EV-cap map, the STAGE placement in `DegenerusGameAdvanceModule`) is grep-verified against `453f8073` with any drift corrected in the SPEC (no "by construction" survives un-checked).

**Plans**: 2 plans (Wave 1: 353-01 SPEC core authoring [AFF-01/AFF-02 + AGG/TKT/QST/OPEN design feeds + corrected accumulator slot + anchor attestation + threat-model re-attestation]; Wave 2: 353-02 XMODEL-01 cross-model design-input pass [codex + gemini × 5 concerns] + disposition fold-in + SPEC lock)

Plans:

- [x] 353-01-PLAN.md — Author the SPEC core: lock AFF-01/AFF-02 + fold the AGG/TKT/QST/OPEN design feeds + the corrected new-dedicated accumulator slot + the anchor attestation table + the unmanipulable/solvency/freeze re-attestation
- [x] 353-02-PLAN.md — Run the XMODEL-01 cross-model design-input pass (codex + gemini, 5 bespoke per-concern prompts), fold the disposition table into the SPEC, and flip the SPEC lock

**UI hint**: no

### Phase 354: IMPL — The ONE Carefully-Sequenced Batched Contract Diff (aggregator + ticket-mode primitive + DegenerusQuests batched-settle + accumulator + open-end)

**Goal**: The v56.0 mechanism lands as a single reconciled `contracts/*.sol` diff under the SPEC's settled shapes, authored producer-before-consumer (accumulator storage → the aggregator accrue/settle in the STAGE → the `DegenerusQuests` batched-settle entrypoint → the ticket-mode minimal-write primitive → the open-end optimizations → the interface/wiring): the per-sub accumulator appended/packed into `DegenerusGameStorage`; the mode-agnostic aggregator (accrue cheap per-buy with NO cross-contract calls — flat-7% `affiliateBase` + `questProgress` running balances + the `buyerOwedBurnie` 10%/20% ticket buyer-bonus on ticket subs + the AFFILIATE leg settled by PULL via `claim(address[] subs)`/`withdraw` with the fixed 75/20/5 split [no roll, no seed, no scheduled/mutation affiliate flush] + the QUEST leg settled by RIDING THE DAILY BUY STAGE on the global settle day [amended 2026-06-01 — the separate `mintBurnie` "settlement-due" router leg REJECTED as a redundant cold-SLOAD pass; the inline `_settleQuest` mints `questProgress × QUEST_SLOT0_REWARD` + `buyerOwedBurnie` in ONE `creditFlip`; `SUB_STAGE_BATCH` SHRUNK so the settle-day chunk fits 16.7M, deferred to 355] + a quest-settle-on-unsub + the uniform mode-agnostic quest-settle for BOTH ticket + lootbox subs + the self-marking running balances [`windowStartDay`/`lastSettledDay` double-settle markers DROPPED]); the ticket-mode minimal-write primitive (the custom "write the ticket entries to the queue" fn replacing the per-day `purchaseWith` heavyweight, the inline affiliate/quest deferred to the aggregator, producing resolution-equivalent ticket entries with the century/x00 parity decision applied + ACCRUING the 10%/20% ticket buyer-bonus `:1655-1659` into `buyerOwedBurnie` at live parity [closing the v55-style dropped-bonus regression]); the `DegenerusQuests` batched-settle entrypoint + the streak machinery (amended 2026-06-01 — quest settle rides the buy STAGE; slot-0 BURNIE + the 10%/20% ticket buyer-bonus mint together; first-sub-only +daysToNextSettle streak, no provisional) (the slot-0 `questProgress` delivered-day counter → settle-mint `× QUEST_SLOT0_REWARD` [+ the accrued `buyerOwedBurnie` ticket bonus] via the STAGE-riding settle on the global settle day / `claimQuest` fallback, the first-sub-only `+daysToNextSettle` head-start [bounded +0..+9, no provisional] with the activity-score reading the actual `state.streak` and the per-window streak advancing only on debit-DELIVERED days, the `lastCompletedDay`/`afkCoveredThroughDay` double-credit guard + active-pass anti-reset with slot rewards never suppressed, proven non-perturbing to the manual/bingo/degenerette/boon callers, and the O1 lootbox-quest double-credit confirmed-intended-or-fixed); and the open-end optimizations (the `_openAfkingBox`→`resolveAfkingBox` + `mintBurnie`/`autoOpen`/`OPEN_BATCH` path optimized for max gas with the cheapest viable shared materialization + no cold ledger, the open kept COMPLETELY unmanipulable — live-level parity, no double-open via `lastOpenedDay` monotone, no EV-cap double-draw on the shared `(player,level)` budget, no shared-mutable-state hazard with the human route) — applied to `contracts/` and locally compiling (`forge build` clean), then HELD at the contract-commit boundary for explicit user hand-review.
**Type**: IMPL (CONTRACT BOUNDARY — the ONE carefully-sequenced batched USER-APPROVED `contracts/*.sol` diff; `autonomous: false` at the commit gate; never auto-commit contracts)
**Depends on**: Phase 353 (the SPEC must lock the aggregator + the accumulator layout + the affiliate accrue/settle split + the ticket-mode primitive shape + the `DegenerusQuests` batched-settle entrypoint + the non-perturbation approach + the ±10-streak derivation + the open-end review + the unmanipulable/solvency re-attestation, fold in the XMODEL design-input suggestions, and grep-attest the edit-order first)
**Requirements**: AGG-01, AGG-02, AGG-03, AGG-04, AGG-05, TKT-01, TKT-02, QST-01, QST-02, QST-03, QST-04, QST-05, OPEN-01, OPEN-02
**Success Criteria** (what must be TRUE):

  1. The mode-agnostic aggregator accrues cheap + settles per-leg (AGG-01 / AGG-02 / AGG-03 / AGG-04 / AGG-05 — amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic) — per buy the STAGE accrues the flat-7% affiliate base + quest progress (+ the `buyerOwedBurnie` 10%/20% ticket buyer-bonus on ticket subs) into the per-sub accumulator running balances with NO cross-contract calls (the cheap hot path, replacing the per-buy `handlePurchase`/`payAffiliate`/`creditFlip` storm) (AGG-01); the AFFILIATE leg is PULL (no scheduled flush) — the accrued `affiliateBase` persists for the uplines to settle via `claim`/`withdraw` (the fixed 75/20/5 split, no roll, no seed) — while AGG-02 SURVIVES carrying the QUEST leg only: the quest settle RIDES THE DAILY BUY STAGE on the global settle day (`currentDay % settlePeriod == 0`, ~10-day cadence — amended 2026-06-01: the separate `mintBurnie` "settlement-due" router leg is REJECTED as a redundant cold-SLOAD pass; the inline `_settleQuest` mints the sub's accrued quest BURNIE `questProgress × QUEST_SLOT0_REWARD` + the accrued `buyerOwedBurnie` ticket bonus in ONE `creditFlip` + applies the ±10 streak, draining both counters once per epoch; `SUB_STAGE_BATCH` SHRUNK so the settle-day chunk fits 16.7M, number deferred to 355) (quests stay AUTOMATIC — the sub's own reward) (AGG-02); any player-triggered sub mutation that ends the sub (unsub) triggers a lightweight QUEST-settle draining `questProgress` BEFORE applying the change — the affiliate base is NOT flushed on mutation, it persists for the uplines to PULL (an unsub does not forfeit the uplines' accrued affiliate) (AGG-03); the QUEST-settle path settles uniformly for BOTH ticket + lootbox subs (mode-agnostic — `questProgress` mode-independent; the affiliate base is likewise mode-agnostic and pulled uniformly via `claim`) (AGG-04); and double-settle is impossible via self-marking running balances — the affiliate `claim` zeroes `affiliateBase[sub]` and the quest flush drains `questProgress` (both a no-op on a double-fire), so the per-sub `windowStartDay`/`lastSettledDay` double-settle markers are DROPPED (AGG-05).

  2. The ticket-mode minimal-write primitive lands at parity (TKT-01 / TKT-02 — amended 2026-06-01 — the primitive accrues the 10%/20% ticket buyer-bonus, settled with the quest) — afking ticket subs use a custom minimal function that writes the ticket entries to the queue (mirrors the lootbox box-stamp) AND ACCRUES the 10%/20% ticket buyer-bonus (`DegenerusGameMintModule.sol:1655-1659` — flat 10% of the BURNIE-equivalent ticket spend, doubling to 20% for ≥10-ticket buys) into the per-sub `buyerOwedBurnie` field, and the per-day `MintModule.purchaseWith` heavyweight + its inline affiliate/quest are removed from the per-buy path (deferred to the AGG aggregator) — closing the v55-style regression where the bypass-`_callTicketPurchase` primitive silently DROPPED the buyer-bonus (TKT-01); the custom ticket-write produces the same queued ticket entries as `purchaseWith`'s ticket leg (resolution-equivalent placement/trait/quantity), the afking-ticket century / x00 quantity-bonus parity (`MintModule:1243`) keep-or-drop decision from the SPEC is applied, and the 10%/20% buyer-bonus is at PARITY with live (minus the affiliate-kickback leg), minted to the sub TOGETHER WITH the slot-0 quest reward at the quest STAGE settle [one `creditFlip`], ticket-mode-specific (lootbox subs use the boon) and riding the quest PUSH not the affiliate PULL (TKT-02).

  3. The quest batching lands non-perturbing with no pre-credit-EV inflation + no double-credit (QST-01 / QST-02 / QST-03 / QST-04 / QST-05 — amended 2026-06-01 — automatic slot-0 BURNIE via mintBurnie chain + claimQuest fallback; first-sub-only +daysToNextSettle streak, no provisional) — the afking quest streak uses a SIMPLIFIED first-sub-only `+daysToNextSettle` head-start (`hasEverSubscribed` 1-bit; bounded +0..+9 over the manual baseline, no provisional/vesting) on the ±10-per-window activity model (−10 on unsub), the slot-0 reward accrues as a delivered-day `questProgress` COUNTER → settled = mint `× QUEST_SLOT0_REWARD` (the ONLY direct quest BURNIE) via the `mintBurnie` settlement-due chain + the `claimQuest(subs[])` fallback + quest-settle-on-unsub, slot-1 remains the player's own manual quest, the ±10 streak is the activity-score multiplier NOT direct BURNIE (QST-01); the first-sub-only `+daysToNextSettle` head-start is a bounded (+0..+9, once/account) DIRECT streak grant that is USER-ACCEPTED-BY-DESIGN — the prior "read confirmed-delivered, never the +10 pre-credit" escrow guard is SIMPLIFIED AWAY for the afking grant (the bound REPLACES the escrow; a deliberate accepted tradeoff, NOT a missed control), the activity-score still reads the actual `state.streak`, and the per-window streak still advances only on debit-DELIVERED days (the C3-a streak-dodge fix stays in force) (QST-02); the afk+manual double-credit guard (`lastCompletedDay`/`afkCoveredThroughDay`) prevents double streak credit on afk-covered days, the gap-reset is suppressed via an active-pass check (anti-reset without daily writes), and slot rewards are NEVER suppressed (only the duplicate streak credit) (QST-03); the batched-settle entrypoint added to the shared `DegenerusQuests` core is non-perturbing to the manual / bingo / degenerette / boon callers (`awardQuestStreakBonus` etc.) (QST-04); and the pre-existing O1 lootbox-quest BURNIE double-credit (`handlePurchase` internal `creditFlip` + the returned-and-re-credited value) is confirmed intended or fixed (QST-05).

  4. The afking open-end is optimized + stays completely unmanipulable (OPEN-01 / OPEN-02) — the afking open path (`_openAfkingBox` → `resolveAfkingBox` + the `mintBurnie` open leg / the `autoOpen` cursor + `OPEN_BATCH`) is optimized for maximum gas efficiency (the per-open marginal + the batch cost), reading no cold ledger and sharing the cheapest viable materialization with the human path (OPEN-01); and the afking open stays COMPLETELY unmanipulable under the v56 changes — the live-level open = parity with human `openLootBox` (open is permissionless + bounty-driven, never player-timed → no tier-timing edge), no double-open (`lastOpenedDay` monotone), no EV-cap double-draw (the shared per-`(player,level)` budget), no shared-mutable-state hazard with the human route — ALL re-verified after the accrual/settle refactor (OPEN-02).

  5. The diff is producer-before-consumer + `forge build` is clean + HELD at the boundary (all 14 IMPL reqs) — the whole diff is authored producer-before-consumer per the SPEC edit-order map (accumulator storage → aggregator accrue/settle → `DegenerusQuests` batched-settle → ticket-mode primitive → open-end → interfaces/wiring), applied to `contracts/` and locally compiling (`forge build` clean; `ContractAddresses.sol` freely modifiable), but NOT committed without explicit user hand-review of the single batched diff (the SOLVENCY-01 ETH/`claimablePool` path stays byte-unchanged — affiliate/quest are BURNIE flip-credit only — and RNG-freeze stays intact, both carried as IMPL invariants and re-proven at TST 356).

**Plans**: 6 plans in 4 waves (producer-before-consumer)

Plans:
**Wave 1**

- [x] 354-01-PLAN.md — Wave 1: re-pack the Sub struct + the in-slot accumulator (affiliateBase/questProgress/buyerOwedBurnie + hasEverSubscribed; drop window/settled markers; amount->milli-ETH) (AGG-05)
- [x] 354-02-PLAN.md — Wave 1: DegenerusQuests core — the onlyGame batched-settle entrypoint (non-perturbing) + O1 line-890 double-credit fix + dead handleLootBox removal (QST-01..05)

**Wave 2** *(blocked on Wave 1 completion)*

- [x] 354-03-PLAN.md — Wave 2: GameAfkingModule mode-agnostic accrue + inline _settleQuest (rides the STAGE) + claimQuest fallback + unsub-settle + first-sub head-start; SOLVENCY-01 byte-unchanged (AGG-01..05, QST-01/02/03)

**Wave 3** *(blocked on Wave 2 completion)*

- [x] 354-04-PLAN.md — Wave 2: DegenerusAffiliate flat-7% deterministic-split PULL — claim(subs[]) 75/20/5 + CEI withdraw() + pendingClaim; no roll/seed (AGG-01/04/05)

**Wave 4** *(blocked on Wave 3 completion)*

- [x] 354-05-PLAN.md — Wave 3: ticket minimal-write primitive + buyerOwedBurnie 10%/20% accrual + century parity (boons-OFF) + the open-end re-verification + wiring (TKT-01/02, OPEN-01/02)

**Wave 5** *(blocked on Wave 4 completion)*

- [x] 354-06-PLAN.md — Wave 4: forge-build-clean + the per-requirement diff-review + the autonomous:false USER hand-review-and-commit gate (the ONE v56 contract boundary)

**UI hint**: no

### Phase 355: GAS — Measure + Tune (per-buy / per-open / per-settle marginals + accumulator packing + SUB_STAGE_BATCH re-tune + mode/SLOAD collapse)

**Goal**: Beyond the structural reduction the IMPL refactor already delivers (the per-buy cross-contract storm collapsed into the cheap accrue + the deferred settle), the everyday cost gets measured + tuned under the security-over-gas floor (gas-scavenger surfaces, gas-skeptic validates — each tune must stay unmanipulable; invariant-trading or not-real wins REJECTED with reasoning, not re-litigated): the per-buy marginal is measurably reduced (lootbox ~206k → ~130–140k; ticket off the ~262k `purchaseWith` heavyweight) and measured per-buy + per-settle under the 16.7M HARD per-tx ceiling (GAS-01); the per-sub accumulator packs into the `Sub` slot's spare bits where feasible with no new cold per-buy SSTORE (GAS-02); `SUB_STAGE_BATCH` is re-tuned for the lower per-sub cost (throughput/headroom, the per-day STAGE staying under the 16.7M ceiling at the `SUBSCRIBER_CAP`) (GAS-03); and redundant payment-mode branches / repeated SLOADs in the STAGE are collapsed where a slight simplification is cheaper (GAS-04, allowed under the scope latitude). Any net contract change rides the batched USER-APPROVED diff held at the contract-commit boundary; if the savings are structural to the IMPL refactor (the v55 Outcome-A precedent), that is recorded as the outcome.

> **RE-SCOPED 2026-06-01 (USER override, mid-355-execute — three directives; see [[v56-deferred-quest-payout-two-batch-redesign]] / `355-CONTEXT.md`).** Phase 355 is no longer a pure constant-tune (Outcome-A is no longer the likely outcome) — it now ALSO lands a SMALL IMPL change in the same batched USER-APPROVED diff at the 355-03 boundary: **(a) DEFERRED PAYOUT (GAS-05)** — `_settleQuest` keeps advancing the `settleAfkingQuest` streak on the ~10-day cadence and draining the counters, but the per-sub quest+buyer-bonus `creditFlip` is REPLACED by a `pendingBurnie` accumulator paid via a NEW permissionless claim entrypoint (the keeper `mintBurnie` bounty stays an immediate push; auto-run only; off the solvency path). **(b) TWO STAGE BATCH SIZES (GAS-03 amended)** — split `SUB_STAGE_BATCH` into `SUB_STAGE_BATCH_NORMAL` (large, the cheap ~9/10 days) + `SUB_STAGE_BATCH_SETTLE` (smaller, the settle day with per-sub `settleAfkingQuest`), the STAGE chunker branching on `currentDay % SETTLE_PERIOD == 0`; each sized from its day's measured marginal to <10M target / ≤16.7M ceiling; `OPEN_BATCH` stays single. The harness measures the `creditFlip` component cost so the post-swap settle marginal + both batch sizes are derivable, landing as ONE batched diff (swap + claim + two-batch branch + computed constants). The committed 355-01 Task-1 harness `3b9df3fb` is the pre-change before-baseline. **Plan list below is SUPERSEDED — re-planned 2026-06-01.**

**Type**: GAS (CONTRACT BOUNDARY — any net `contracts/*.sol` change rides the batched USER-APPROVED diff; `autonomous: false` at the commit gate if a diff is produced)
**Depends on**: Phase 354 (the gas pass operates on the post-IMPL surface — the accumulator + the aggregator accrue/settle loop + the ticket-mode primitive + the open-end the IMPL established; the marginals are measured on the applied tree, not SPEC placeholders)
**Requirements**: GAS-01, GAS-02, GAS-03, GAS-04, GAS-05
**Success Criteria** (what must be TRUE):

  1. The per-buy + per-settle marginals are measured + reduced (GAS-01) — the per-buy marginal is measurably reduced (target: lootbox ~206k → ~130–140k; ticket off the ~262k `purchaseWith` heavyweight), measured per-buy + per-settle marginal via a TST-06-style harness under the 16.7M HARD per-tx ceiling, proven behavior-identical-where-claimed (the accrue + deferred-settle shape produces the same eventual affiliate/quest credit as the per-buy storm did, modulo the accepted settle-timing simplification).

  2. The accumulator packs with no new cold SSTORE (GAS-02 — amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic) — the SHRUNK per-sub accumulator (the flat-7% `affiliateBase uint32` whole-BURNIE/100M-clamp + `questProgress`, both self-marking running balances; `windowStartDay`/`lastSettledDay` DROPPED) packs into the re-packed `Sub` slot's spare bits (per the SPEC layout), avoiding a new cold per-buy SSTORE; the off-slot `pendingClaim` mapping (affiliate recipients) is touched at `claim`/`withdraw`, NEVER per-buy; if a field genuinely cannot pack, that is recorded with reasoning (not forced at the cost of an invariant).

  3. SUB_STAGE_BATCH is re-tuned + the STAGE stays under the ceiling (GAS-03) — `SUB_STAGE_BATCH` is re-tuned for the lower per-sub cost (raise the batch for more subs/advance under 16.7M, or keep it with large headroom), and the per-day STAGE is proven to stay under the 16.7M HARD per-tx ceiling at the `SUBSCRIBER_CAP` (the binding ticket-mode case included).

  4. The mode/SLOAD collapse lands + any net diff is held at the boundary (GAS-04 / GAS-01..03) — redundant payment-mode branches / repeated SLOADs in the STAGE are collapsed where a slight simplification is cheaper (allowed under the scope latitude, proven unmanipulable); and any `contracts/*.sol` change from this phase rides the batched USER-APPROVED diff, applied + locally compiling (`forge build` clean), HELD at the contract-commit boundary for explicit user hand-review (never auto-committed) — or, if the savings are structural to the IMPL refactor and no net tune lands (the v55 Outcome-A precedent), that is recorded as the outcome and no separate diff is gated.

  5. The deferred payout + two-batch split land unmanipulable (GAS-05 / GAS-03 — USER override 2026-06-01) — `_settleQuest` still advances the `settleAfkingQuest` streak on the ~10-day cadence + drains `questProgress`/`buyerOwedBurnie`, but accrues the owed quest+buyer-bonus BURNIE into `pendingBurnie` (the already-warm Sub slot) instead of an inline `creditFlip`; a permissionless claim entrypoint does a final settle then pays `pendingBurnie` in ONE `creditFlip` and zeroes it (always credits the sub, idempotent, off the solvency path — SOLVENCY-01 byte-unchanged, RNG-freeze intact); the keeper `mintBurnie` bounty stays an immediate push; the STAGE chunker uses `SUB_STAGE_BATCH_NORMAL` vs `SUB_STAGE_BATCH_SETTLE` (branch on `currentDay % SETTLE_PERIOD == 0`), each proven <10M target / ≤16.7M ceiling at the cap; the deferral is proven non-gameable (no settle-timing/claim-timing EV — `pendingBurnie` is the sub's own accrued reward, drained atomically).

**Plans**: 3 plans in 3 waves (measure -> scavenge/skeptic-validate -> set SUB_STAGE_BATCH/tune-or-Outcome-A; sequential, each wave consumes the prior's measurements)

Plans:

**Wave 1**

- [ ] 355-01-PLAN.md — Build the v56 gas-marginal harness + measure per-buy (lootbox + ticket) / per-open / per-settle marginals + the worst-case settle-day STAGE chunk at the cap, all under 16.7M; prove GAS-02 no-new-cold-SSTORE (GAS-01/02; autonomous, test-only)

**Wave 2** *(blocked on Wave 1 completion)*

- [ ] 355-02-PLAN.md — gas-scavenger over the STAGE/accrue/settle/open + gas-skeptic verdicts -> the APPROVED/REJECTED candidate ledger (GAS-04 mode/SLOAD collapse + GAS-02 packing finalization) feeding 355-03 (GAS-02/04; autonomous, doc-only)

**Wave 3** *(blocked on Wave 2 completion)*

- [ ] 355-03-PLAN.md — Set SUB_STAGE_BATCH against the measured settle-day chunk + land the skeptic-APPROVED GAS-04 changes (or record Outcome-A), re-prove the STAGE under 16.7M at the cap, HOLD at the contract-commit boundary (GAS-03/04; autonomous:false — the ONE Phase-355 contract gate)

**UI hint**: no

### Phase 356: TST — Unmanipulable (strategic sub/unsub) + Quest-Core Non-Perturbation + Two-Path-Open Coexistence + Gas Marginals + Non-Widening

**Goal**: The milestone is proven behaviorally correct + the hard security floor (the SEC gate) is proven empirically against the v55-baseline tree: the afking system (buy + open) is unmanipulable — no positive-EV vector from settle-timing (no seed — _amended 2026-06-01: the roll is REMOVED under the flat-7% deterministic-split pull_), strategic sub/unsub churn (the USER-flagged PRIMARY concern), re-rate-on-alteration, pre-credit-EV inflation, double-credit, open-timing, or settle-griefing; SOLVENCY-01 is untouched (affiliate/quest remain BURNIE flip-credit OFF the ETH/`claimablePool` path, the ETH/pool debit byte-unchanged, RNG-freeze intact under the new accrual/settle); the shared `DegenerusQuests` core batched-settle entrypoint is non-perturbing to the manual/bingo/degenerette/boon callers; the two open routes coexist with no shared-state hazard; the per-buy + per-open + per-settle marginal gas is measured under the 16.7M HARD per-tx ceiling (the GAS-01..04 wins proven same-results); and the suite is NON-WIDENING vs the v55 baseline `453f8073` (every pre-existing red enumerated BY NAME) — restoring a clean v56.0 regression baseline.
**Type**: TST
**Depends on**: Phase 355 (tests exercise the FINAL applied surface — the live aggregator + ticket-mode primitive + open-end from 354 plus the 355 tune AND the USER-directed liveness adds: the `openBoxes` valve `86a2d6c8` + the gap/jackpot decouple `3d969621` — not SPEC placeholders or an intermediate pre-gas state)
**Requirements**: SEC-01, SEC-02, LIVE-01, GAS-06 (LIVE-01 + GAS-06 added 2026-06-02 — the 355 liveness adds; see SC 6-7)
**Success Criteria** (what must be TRUE):

  1. The afking system is proven unmanipulable — strategic sub/unsub the PRIMARY probe (SEC-01) — empirical + adversarial proof that there is NO positive-EV vector from settle-timing (no seed — _amended 2026-06-01: the roll is REMOVED under the flat-7% deterministic-split pull_), **strategic sub/unsub churn (churn to re-claim the affiliate [forfeit-nothing-gain-nothing] / dodge a streak penalty / harvest or duplicate a settlement — the USER-flagged primary concern)**, re-rate-on-alteration, pre-credit-EV inflation, double-credit, open-timing, or settle-griefing: a churn loop (subscribe → accrue → unsub → re-subscribe) cannot extract more affiliate/quest value than a steady sub (amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic: the accrued `affiliateBase` is the uplines' money that PERSISTS on unsub — churn forfeits nothing AND gains nothing; there is NO roll to re-roll, NO seed to grind, NO two-distribution free option to arbitrage [exactly ONE deterministic split path], the buyer never receives the affiliate base [`A ≠ sub` guaranteed + the rare U1/U2==sub cycle skip], the flat 7% accrues the same either way), and the self-marking running balances make double-settle impossible (a re-`claim` sees `affiliateBase == 0`; a double quest flush finds `questProgress == 0`). (The 3-skill adversarial economic review + the XMODEL cross-model review at TERMINAL 357 are the final gate; this phase proves the property empirically.)

  2. SOLVENCY-01 untouched + RNG-freeze intact (SEC-02) — the affiliate/quest rewards remain BURNIE flip-credit off the ETH/`claimablePool` path (proven via the ETH/pool debit being byte-unchanged vs `453f8073` — a BURNIE-emission-timing change only), and RNG-freeze stays intact under the new accrual/settle (the accrue/settle touches no frozen RNG-window slot; the open-end materialization keeps the live-level parity + the stamped-seed determinism the v55 close proved).

  3. The shared quest-core is proven non-perturbing + the two-path open coexists (SEC-01 corollary; QST-04 / OPEN-02 re-proven) — the `DegenerusQuests` batched-settle entrypoint is proven non-perturbing to the manual / bingo / degenerette / boon callers (`awardQuestStreakBonus` etc. produce byte-identical results with the entrypoint present), and the two open routes (afking-stamp vs human `lootboxEth`/`boxPlayers`) coexist with no shared-state hazard + no double-open (the afking open re-verified equivalent to human `openLootBox` at the live level, `lastOpenedDay` monotone, the EV-cap budget shared exactly-once).

  4. The gas marginals are measured under the ceiling (GAS-01..04 re-proven same-results) — the per-buy + per-open + per-settle marginal gas is measured under the 16.7M HARD per-tx ceiling (a TST-06-style harness), with the GAS-01..04 wins (the per-buy reduction, the accumulator packing, the `SUB_STAGE_BATCH` re-tune, the mode/SLOAD collapse) proven same-results (or same-eventual-credit where the accepted settle-timing simplification applies).

  5. The NON-WIDENING regression is proven (SEC corollary) — the reconceived afking/quest/affiliate suite compiles + passes against the v56 surface with net-zero new regression vs the v55 baseline `453f8073` (every pre-existing red enumerated BY NAME, `REGRESSION-BASELINE-v56.md`), absorbing any test renames / oracle migrations from the aggregator + ticket-mode refactor; ZERO contract mutation during the TST phase (the subject stays frozen at the IMPL+GAS HEAD). INCLUDES the 355-deferred stale-Sub-offset fuzz-suite migration (`OFF_LASTBOUGHT=21→11`/uint24, ~10 `test/fuzz/` files — the pre-existing `6555125 != 3774873600` reds, baseline-confirmed NOT from the v56 contract changes) as the NON-WIDENING precondition.

  6. The `openBoxes` liveness valve is proven (LIVE-01, NEW from 355 — commit `86a2d6c8`) — the unified permissionless `openBoxes(maxCount)` (AFKING boxes first, then human, uncapped, unrewarded) clears any backlog of either box type in caller-sized chunks proven < 16.7M per tx (each box uniform O(1)); both persistent cursors advance so repeated bounded calls drain any state; the individual `openLootBox(player,index)` + the rewarded `mintBurnie` open path are byte-unchanged; `drainAfkingBoxes` reachable only via the `openBoxes` delegatecall (no autoOpen selector collision).

  7. The gap-backfill / daily-jackpot decouple is proven (GAS-06, NEW from 355 — commit `3d969621`; closes the Codex-found protocol-forced 16.7M composition breach) — after a multi-day VRF-stall gap backfill, `advanceGame` breaks at `STAGE_GAP_BACKFILLED` (the day's jackpot is NOT paid that tx) and the NEXT advance pays it with the same frozen word (idempotent resume: `rngGate` returns `gapDays==0`, `dailyIdx` not advanced so `advanceDue()` stays true, `purchaseStartDay` bumped once). PROVE EACH `advanceGame` call in the gap-resume is < 16.7M (the existing test bounds only the 25M total resume — write a per-advance harness) + a `gap→defer→next-pays` regression test.

**Plans**: 7 plans in 3 waves (Wave 0: fixture-sanity gate + the D-10 offset migration [the NON-WIDENING precondition]; Wave 1: the SEC-01/SEC-02/QST-04 fuzz+repro proofs + the gas/LIVE/GAS-06 EXTEND; Wave 2: the BY-NAME NON-WIDENING ledger)

- [x] 356-01-PLAN.md — Wave-0 fixture-sanity gate + migrate the 7 read-only keeper/afking fuzz files (OFF_LASTBOUGHT 21->11/uint24) [SEC-01]
- [x] 356-02-PLAN.md — Wave-0 migrate the 3 v55-proof files (offsets + _setStamp/_setScorePlus1 write-masks in lockstep; the SEC ADAPT-source templates) [SEC-01, SEC-02]
- [x] 356-03-PLAN.md — Wave-1 SEC-01 V56SecUnmanipulable: churn-fuzz invariants + the 4 named repros (affiliate re-claim churn / streak decay-gap dodge / pendingBurnie double-claim CEI / 4 finalize hooks before delete) [SEC-01] ✅ 2026-06-02 (11/11 green incl. 1000-run churn fuzz; affiliateBase persists byte-identical across unsub+re-sub; decay-on-read + funding-kill guard zero the streak; CEI pays once; 4 finalize hooks all finalize-before-delete; no-orphan holds; ZERO contract mutation; commits cad9c48f + e8b68c89; threat_flag: drainAffiliateBase has no DegenerusGame dispatch stub → 357)
- [x] 356-04-PLAN.md — Wave-1 SEC-02 V56FreezeSolvency: solvency invariant fuzz + RNG-freeze determinism + the SOLVENCY-01 debit byte-diff anchor [SEC-02]
- [x] 356-05-PLAN.md — Wave-1 QST-04 V56QuestNonPerturb: slot-1 streak-neutral during afking + cross-caller (awardQuestStreakBonus/bingo/degenerette/boon) byte-identity [SEC-01] ✅ 2026-06-02 (7/7 green; afkingActive gates the streak bump — slot-0/slot-1 completion streak-neutral during afking yet slot-1 fully accessible paying its 200-BURNIE reward, NON-afking advances +1 [C3-a dodge stays closed]; two-world snapshot/revert byte-identity on the full PlayerQuestState word with afking siblings present vs absent [incl. a mid-finalize sibling]; O1 day-4 LOOTBOX single-credit holds [reward returned once, NOT internally creditFlipped]; drove the quest core directly via GAME/COIN access gates + slot-2 vm.load reads; ZERO contract mutation; commits b25c7e33 + ad3fbbcd)
- [x] 356-06-PLAN.md — Wave-1 LIVE-01 + GAS-06 EXTEND V56AfkingGasMarginal: SUBSCRIBER_CAP fix 500->1000 + per-tx gap-resume <16.7M ceiling + the decouple idempotent-resume + the openBoxes valve + the 4 residuals + the GAS-01..04 regression locks [LIVE-01, GAS-06] ✅ 2026-06-02 (15/15 green; gap-backfill advance N ~6.85M + deferred-jackpot N+1 SEPARATE tx; mixed-day OPEN_BATCH chunk ~10.35M; ZERO contract mutation; commits c6b087dc + f3cda660)
- [x] 356-07-PLAN.md — Wave-2 REGRESSION-BASELINE-v56.md: the BY-NAME NON-WIDENING ledger (empirical 453f8073 checkout + the subset gate live-union==empty + the D-10 narrowing) [SEC-01, SEC-02, LIVE-01, GAS-06] ✅ 2026-06-02 (435-line ledger, commit `97fac47b`; baseline established EMPIRICALLY via the byte-identical-contracts commit `83a6a9ca` [the raw 453f8073 corpus is UNCOMPILABLE — AfKing.sol deleted but DeployProtocol+5 files still deploy it at a load-bearing nonce; `git diff 453f8073 83a6a9ca -- contracts/` EMPTY] → reproduced the v55 TST-HEAD 603/134/16 = the 453f8073 red union; live v56 HEAD 624/134/30, the live 134 BYTE-IDENTICAL to the baseline 134 BY NAME — `live − union == ∅` AND `union − live == ∅`; the EXPANDED MANDATE's 14 migration-unmasked v56-behavior reds RESOLVED via vm.skip-with-reason DROP, commit `f23b010e` [milli-ETH packing / 0.01-ETH subscribe min-buy / `E()`-vs-Panic(0x11) / unified openBoxes valve / dropped autoOpen — each a stale v55 assertion the audited v56 diff superseded, each re-proven GREEN by V56SecUnmanipulable/V56FreezeSolvency/V56AfkingGasMarginal]; D-10 offset-migration red→green NARROWING + the SOLVENCY-01 leg-1 byte-diff anchor [GameAfkingModule 709-710↔663-664 byte-identical] recorded; the unseeded DegeneretteBet.inv ⊆ relaxation carried; Hardhat compile-sanity arm EXIT 0; drainAffiliateBase dispatch-stub reachability SURFACED as a carried 357 finding; ZERO contracts/*.sol mutation)

**UI hint**: no

### Phase 357: TERMINAL — Delta Audit + 3-Skill Genuine-PARALLEL Adversarial Sweep (AUGMENTED by XMODEL Codex + Gemini close) + FINDINGS-v56.0 + Closure Flip

**Goal**: The v56.0 audit subject (the carefully-sequenced batched diff — the aggregator + the ticket-mode primitive + the open-end optimizations + the `DegenerusQuests` batched-settle, FROZEN at the IMPL+GAS HEAD) is closed via a FULL close that runs its own internal sweep IN-MILESTONE (NOT deferred to v52, like v54.0/v55.0 and unlike v50.0/v51.0): a delta-audit confirms every v56 surface is NON-WIDENING vs the v55 HEAD `453f8073` with SOLVENCY-01 byte-unchanged + RNG-freeze intact + the affiliate deterministic-split-PULL non-gameability (NO roll/seed — _amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic_) + the open-end two-path/no-double-open + the shared-quest-core non-perturbation re-attested; the 3-skill genuine-PARALLEL adversarial economic review (`/contract-auditor` + `/economic-analyst` + `/zero-day-hunter`; `/degen-skeptic` = the dual-gate filter) probes the strategic sub/unsub edge + the settle-timing non-exploitability (no seed — _amended 2026-06-01: the roll/seed is REMOVED under the flat-7% deterministic-split pull_) + the pre-credit-EV inflation + the two-path open, AUGMENTED by the XMODEL-01 cross-model close (Codex + Gemini fed crafted prompts on the FULL afking system buy+open); `audit/FINDINGS-v56.0.md` (chmod 444) is authored; and the atomic 5-doc closure flip (ROADMAP + STATE + MILESTONES + PROJECT + REQUIREMENTS) is applied with the `MILESTONE_V56_AT_HEAD_<sha>` closure signal.
**Type**: TERMINAL (FULL close — delta-audit + internal 3-skill adversarial sweep AUGMENTED by the XMODEL cross-model close + FINDINGS + closure flip; NOT deferred)
**Depends on**: Phase 356 (the subject must be implemented + gas-tuned + test-proven — incl. the unmanipulable + non-perturbation + two-path + NON-WIDENING proofs — before the requirements are re-attested at closure and the adversarial sweep runs on the proven surface)
**Requirements**: AUDIT-01
**Success Criteria** (what must be TRUE):

  1. The delta-audit + the internal 3-skill adversarial sweep (AUGMENTED by XMODEL) are run IN-MILESTONE (AUDIT-01 + XMODEL-01 close half) — the delta-audit confirms every v56 surface (the accumulator append + the aggregator accrue/settle + the ticket-mode primitive + the `DegenerusQuests` batched-settle entrypoint + the open-end edits) is NON-WIDENING vs the v55 HEAD `453f8073` with zero orphan hunks, SOLVENCY-01 byte-unchanged (the ETH/`claimablePool` debit untouched) + RNG-freeze intact + the affiliate deterministic-split-PULL non-gameability (the flat-7% accrue + the fixed 75/20/5 split at `claim`/`withdraw`, NO roll/seed/flush, buyer-never-wins — _amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic: SUPERSEDES the XMODEL roll-unification, the roll is REMOVED so the C1/C2 free-option finding is MOOT_) + the open-end two-path/no-double-open + the shared-`DegenerusQuests`-core non-perturbation re-attested; the 3-skill genuine-PARALLEL adversarial economic review (`/contract-auditor` + `/economic-analyst` + `/zero-day-hunter` run as concurrent background Task spawns from the orchestrator; `/degen-skeptic` = the dual-gate filter per `D-271-ADVERSARIAL-02`) probes the strategic sub/unsub edge (churn to re-claim / forfeit-nothing-gain-nothing / dodge a streak penalty / harvest or duplicate a settlement), the settle-timing non-exploitability (trivial — no seed), the pre-credit-EV inflation, and the two-path open, with each charged probe dispositioned NEGATIVE-VERIFIED / SAFE_BY_DESIGN / FINDING_CANDIDATE; and the XMODEL-01 cross-model close (Codex + Gemini fed crafted prompts on the FULL afking system buy+open) AUGMENTS the Claude sweep (its disposition folded into the adversarial log).

  2. The findings deliverable is authored + all 24 v56.0 requirements re-attested (AUDIT-01) — `audit/FINDINGS-v56.0.md` (the full multi-section report, chmod 444) is authored capturing the delta-audit + the adversarial disposition (Claude 3-skill + the XMODEL cross-model augmentation) + any findings, and all 24 v56.0 requirements (AGG-01..05 · TKT-01/02 · AFF-01/02 · QST-01..05 · OPEN-01/02 · GAS-01..04 · SEC-01/02 · XMODEL-01 · AUDIT-01) are confirmed satisfied against the frozen v56.0 closure HEAD.

  3. The closure flip is applied (AUDIT-01) — the `MILESTONE_V56_AT_HEAD_<sha>` closure signal is emitted and propagated verbatim, and the atomic 5-doc closure flip (ROADMAP + STATE + MILESTONES + PROJECT + REQUIREMENTS) is applied; the closure plan is a single blocking USER closure-verdict + signal-format approval gate (`autonomous: false`) — the auto-advance is HELD at the closure boundary per `feedback_pause_at_contract_phase_boundaries`. (The separate v52 consolidated cross-model audit still folds the v56 surface into its cumulative sweep — an additional track, recorded in the v52 charge, NOT a substitute for this in-milestone close.)

**Plans**: 6 plans in 4 waves (Wave 1: the leading contract gate + the D-14 reconciliation; Wave 2: the delta-audit ∥ the 3-skill genuine-PARALLEL sweep [+ best-effort XMODEL], both READ-ONLY @ HEAD'; Wave 3: FINDINGS-v56.0; Wave 4: the closure flip + the close-then-prompt-to-push offer). TWO `autonomous: false` gates (357-00 contract gate + 357-04 closure gate) — unlike every prior TERMINAL's single closure gate.

Plans:
**Wave 1**

- [x] 357-00-PLAN.md — Wave 1 (autonomous:false CONTRACT GATE): the THREE bundled contract changes — F-356-01 drainAffiliateBase dispatch stub + D-11 pass-required subscribe + D-12 purchase-grounded subscribe (D-13 VAULT/SDGNRS exempt) — forge build clean, HELD for USER hand-review, committed as the SOLE .sol commit (HEAD') [AUDIT-01]
- [x] 357-00b-PLAN.md — Wave 1 (autonomous): the D-14 NON-WIDENING reconciliation — re-run forge test, drop the D-11/D-12 supersession reds (vm.skip + re-prove via the new V56SubHardening positive proofs), record the F-356-01 narrowing, re-freeze the subject [AUDIT-01] ✅ COMPLETE 2026-06-03 (**EXTENDED to the SECOND contract gate HEAD'' = `61315ecd`** — the advance-incentive redesign: `advanceGame` PURE LIVENESS [`MustMintToday` removed], the must-mint ladder → SOFT pay-gate `_bountyEligible` / `bountyEligible(addr)` view, `mintBurnie` pays the advance bounty only when `mult>0 && eligible`, Vault+sDGNRS→`mintBurnie`). 3 commits `056e78c8`(GovernanceGating GATE-01..04 rewritten to the soft pay-gate model, 6/6 GREEN — `MustMintToday` was a Hardhat-only consumer)+`1d5fd872`(V56SubHardening advance-soft-gate proofs 11→17 GREEN: advanceGame liveness + bountyEligible truth table + mintBurnie eligible/ineligible pay + Vault/sDGNRS routing)+`48fab561`(ledger §9: 567/133/99, `live − union == ∅` HOLDS — 133 ⊆ the §2 134-name 453f8073 union, empty set-diff; the 134→133 narrowing = run-variance in the non-deterministic Bucket A/F + vm.assume-exhaustion cluster; SOLVENCY-01 leg-1 byte-anchor held). The HEAD'-only 357-00b commits `ae7dfa26`+`9d32abec` are superseded by the HEAD'' §9. ZERO contracts/*.sol mutation; `git diff 61315ecd HEAD -- contracts/` EMPTY — subject re-frozen at **HEAD'' = `61315ecd`** (downstream waves now re-freeze against HEAD'', not HEAD').

**Wave 2** *(blocked on Wave 1 completion)*

- [ ] 357-01-PLAN.md — Wave 2 (autonomous, READ-ONLY @ HEAD', ∥ 357-02): the delta-audit — every v56 surface NON-WIDENING vs 453f8073 (zero orphan hunks; SOLVENCY-01 byte-unchanged + RNG-freeze + affiliate flat-7% deterministic-split-PULL non-gameability + open two-path + LIVE-01 + GAS-06 + shared-quest non-perturbation + the 5cb707f2 + the 357-00 hardening attributed) + the regression live − union == ∅ BY NAME [AUDIT-01]
- [ ] 357-02-PLAN.md — Wave 2 (autonomous, READ-ONLY @ HEAD', ∥ 357-01): the 3-skill genuine-PARALLEL adversarial sweep (run INLINE from the orchestrator context per D-06) + best-effort XMODEL augmentation — the strategic sub/unsub edge (PRIMARY) + settle-timing + pre-credit-EV + two-path open + the D-11/D-12 hardened gates + the now-sound 5cb707f2 bypass + the drainAffiliateBase stub + the affiliate claim CEI; /degen-skeptic dual-gate filter [AUDIT-01]

**Wave 3** *(blocked on Wave 2 completion)*

- [ ] 357-03-PLAN.md — Wave 3 (autonomous): author audit/FINDINGS-v56.0.md (the 9-section report, chmod 444 deferred to closure) folding the delta-audit + the adversarial disposition; F-356-01 RESOLVED-AT-357; re-attest the CURRENT v56.0 requirement set [AUDIT-01]

**Wave 4** *(blocked on Wave 3 completion)*

- [ ] 357-04-PLAN.md — Wave 4 (autonomous:false CLOSURE GATE): the atomic 5-doc closure flip + the MILESTONE_V56_AT_HEAD_<sha> signal (reconcile the stale 356 progress row; chmod 444 the findings; no .sol in the closure commit) + the close-then-prompt-to-push offer (D-02 — wait for explicit USER GO) [AUDIT-01]

**UI hint**: no

---

## Progress

**Execution Order:** Phases execute in numeric order: 353 → 354 → 355 → 356 → 357

| Phase | Milestone | Plans Complete | Status | Completed |
|-------|-----------|----------------|--------|-----------|
| 353. SPEC — Design-Lock + Unmanipulable/Solvency Re-Attestation + XMODEL Design-Input + Call-Graph Attestation | v56.0 | 2/2 | Complete    | 2026-06-01 |
| 354. IMPL — The ONE Carefully-Sequenced Batched Contract Diff (aggregator + ticket-mode primitive + DegenerusQuests batched-settle + accumulator + open-end) | v56.0 | 6/6 | Complete   | 2026-06-01 |
| 355. GAS — Measure + Tune (+ GAS-05 deferred payout + liveness adds: openBoxes valve + gap/jackpot decouple) | v56.0 | 3/3 | Complete + PUSHED | 2026-06-02 |
| 356. TST — Unmanipulable (strategic sub/unsub) + Quest-Core Non-Perturbation + Two-Path-Open + Liveness Valve + Gap-Decouple + Gas Marginals + Non-Widening | v56.0 | 5/7 | In Progress|  |
| 357. TERMINAL — Delta Audit + 3-Skill Adversarial Sweep (AUGMENTED by XMODEL) + FINDINGS-v56.0 + Closure | v56.0 | 1/6 | In Progress|  |

> **🔒 v56.0 CONTRACT-BOUNDARY HARD STOP (ONE gate — 354 IMPL · 355 GAS rides the same boundary if it produces a net diff).** Phase 354 IMPL is THE contract phase — the carefully-sequenced batched diff (the per-sub accumulator + the mode-agnostic aggregator accrue/settle + the ticket-mode minimal-write primitive + the `DegenerusQuests` batched-settle entrypoint + the open-end optimizations, authored producer-before-consumer) is applied to `contracts/` and locally compiled (`forge build` clean) but HELD at the contract-commit boundary, NEVER committed without explicit user hand-review (`feedback_batch_contract_approval` + `feedback_never_preapprove_contracts` + `feedback_manual_review_before_push` + `feedback_no_contract_commits`). Phase 355 GAS is a tuning pass — any net behavior-preserving gas change rides the same batched USER-APPROVED diff at the same boundary (or records Outcome-A no-diff per the v55 350 precedent). `ContractAddresses.sol` freely modifiable per `feedback_contractaddresses_policy`; tests + planning + docs AGENT-committable.

> **🔓 v56.0 AUDIT POSTURE — FULL CLOSE (internal sweep + XMODEL cross-model close NOT deferred).** Like v54.0 + v55.0 (and unlike v50.0 + v51.0, whose internal sweeps were deferred → the v52 consolidated audit), **v56.0 runs its own internal 3-skill genuine-PARALLEL adversarial sweep + delta-audit + `audit/FINDINGS-v56.0.md` at TERMINAL (357, AUDIT-01), AUGMENTED by the XMODEL Codex + Gemini cross-model close** — because the milestone touches the shared `DegenerusQuests` core + the everyday `advanceGame` STAGE and the unmanipulability (esp. strategic sub/unsub) + the non-perturbation must be adversarially probed in-milestone. The separate v52 consolidated cross-model audit folds the v56 surface into its cumulative sweep as an additional track (recorded in the v52 charge), not a substitute.

---

## Coverage (v56.0)

**24/24 v56.0 requirements mapped to exactly one phase — 0 orphaned, 0 duplicated.**

| Phase | Requirements | Count |
|-------|--------------|-------|
| 353 SPEC | AFF-01, AFF-02, XMODEL-01 | 3 |
| 354 IMPL | AGG-01, AGG-02, AGG-03, AGG-04, AGG-05, TKT-01, TKT-02, QST-01, QST-02, QST-03, QST-04, QST-05, OPEN-01, OPEN-02 | 14 |
| 355 GAS | GAS-01, GAS-02, GAS-03, GAS-04 | 4 |
| 356 TST | SEC-01, SEC-02 | 2 |
| 357 TERMINAL | AUDIT-01 | 1 |
| **Total** | | **24** |

**Per-category split (verification):**

| Category | Total | SPEC (353) | IMPL (354) | GAS (355) | TST (356) | TERMINAL (357) |
|----------|-------|------------|------------|-----------|-----------|----------------|
| AGG | 5 | — | 5 (01-05) | — | — | — |
| TKT | 2 | — | 2 (01,02) | — | — | — |
| AFF | 2 | 2 (01,02) | — | — | — | — |
| QST | 5 | — | 5 (01-05) | — | — | — |
| OPEN | 2 | — | 2 (01,02) | — | — | — |
| GAS | 4 | — | — | 4 (01-04) | — | — |
| SEC | 2 | — | — | — | 2 (01,02) | — |
| XMODEL | 1 | 1 (01) | — | — | — | — |
| AUDIT | 1 | — | — | — | — | 1 (01) |
| **Total** | **24** | **3** | **14** | **4** | **2** | **1** |

**Center-of-gravity rationale (where a requirement spans design + impl + test + close):**

- **AFF-01 / AFF-02** (the affiliate distribution — the flat-7% deterministic-split PULL: accrue flat 7% per buy into the running `affiliateBase`, settle by PULL via `claim`/`withdraw` with the fixed 75/20/5 split [no roll, no seed, no scheduled/mutation affiliate flush]; taper afking-N/A + leaderboard-at-claim [_amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic: SUPERSEDES the XMODEL roll-unification, the roll is removed_]) → SPEC (353), where they are DESIGN-LOCKED as the load-bearing non-gameability decisions BEFORE any code (the affiliate distribution mechanism IS a design choice, not a built behavior — once the flat-7%-accrue / deterministic-split-PULL shape is locked, IMPL builds it inside the aggregator). The aggregator + the affiliate PULL that USE them (AGG-01..05 + the `claim`/`withdraw` plumbing) are owned at IMPL; AFF-01/02 are the distribution-mechanism design that the affiliate accrue + the QUEST-only AGG-02/03 settle consume — no double-counting (AGG = the quest accrue/settle plumbing + the affiliate accrue; AFF = the distribution rule the `claim` PULL applies).
- **XMODEL-01** (the cross-model Codex + Gemini review) → SPEC (353) as its HOME, because its PRIMARY deliverable is the DESIGN-INPUT pass that must be folded into the design-lock BEFORE IMPL (the gating half — it shapes the mechanism). Its TERMINAL close-augmentation half (Codex + Gemini augmenting the Claude 3-skill sweep) is a touchpoint REFLECTED in Phase 357's success criteria (AUDIT-01's sweep), NOT a separate requirement count — the instruction's "assign it to the phase where its primary deliverable lands … but reflect both touchpoints" resolved to SPEC-as-home.
- **AGG-01..05 · TKT-01/02 · QST-01..05 · OPEN-01/02** (the built behaviors) → IMPL (354) as part of the single batched diff. The SPEC concerns those decisions feed (the accumulator layout, the ticket-primitive shape, the `DegenerusQuests` batched-settle entrypoint shape + non-perturbation approach, the ±10-streak derivation, the open-end review) are folded into the SPEC's success criteria (SC2/SC3/SC4) — they are NOT double-counted at SPEC; only the requirement's HOME (where it must be BUILT) is counted at IMPL.
- **GAS-01 / GAS-02 / GAS-03 / GAS-04** (the measured everyday-cost reduction — per-buy/per-settle marginals, accumulator packing, `SUB_STAGE_BATCH` re-tune, mode/SLOAD collapse) → GAS (355). Note: much of GAS-01/02 is structural to the IMPL refactor (the cheap accrue + the deferred settle + the packed accumulator ARE the saving); the GAS phase MEASURES the savings (proven same-results in TST), lands the residual `SUB_STAGE_BATCH` re-tune + the mode/SLOAD collapse, and gates any net contract change under the same USER-approved diff (or records Outcome-A no-diff per the v55 350 precedent). The SPEC produces the accumulator-layout decision (folded into SC2), but the wins LAND/are measured at the GAS phase.
- **SEC-01 / SEC-02** (the hard security floor — unmanipulable esp. strategic sub/unsub; SOLVENCY-01 untouched + RNG-freeze intact) → TST (356), where they are PROVEN empirically + adversarially as the hard gate (per the explicit "TST Owns SEC-01/02" directive — mirrors v55, where FREEZE design lived at SPEC but the empirical proofs were a distinct phase). The SPEC "SEC design" (the unmanipulable/solvency/freeze re-attestation on paper) is the design gate folded into 353's SC5 — NOT a separate count; the TERMINAL adversarial review (AUDIT-01) is the FINAL gate that re-confirms SEC, but SEC-01/02's center-of-gravity (where the property is first PROVEN, incl. the strategic-sub/unsub proof) is TST. No double-counting.
- **AUDIT-01** (the TERMINAL FULL close) → TERMINAL (357); it re-attests all 24 v56.0 requirements + runs the in-milestone delta-audit + the 3-skill adversarial sweep AUGMENTED by the XMODEL cross-model close + `audit/FINDINGS-v56.0.md` + the atomic 5-doc closure flip with the `MILESTONE_V56_AT_HEAD_<sha>` signal.

---

## ✅ v55.0 AfKing-in-Game Redesign (SHIPPED 2026-06-01)

**Milestone:** v55.0 (started 2026-05-30; SHIPPED 2026-06-01) — Phases 348-352 (7 phases incl. the inserted 349.1 box redesign + 349.2 quest/affiliate regression fix).
**Closure signal:** `MILESTONE_V55_AT_HEAD_ca3bbd3220de763298ef2e742111f6e6ef90d583` (subject frozen `453f8073`; baseline `20ca1f79`).
**Shape:** SPEC (348) → IMPL (349, shipped via the inserted 349.1 `77c3d9ef` + 349.2 `453f8073`) → GAS (350, Outcome-A no-diff) → TST (351, 603/134/16 NON-WIDENING) → TERMINAL (352, FULL close, 0 NEW_FINDINGS).
**Goal:** Fold AfKing's subscriber state + logic into DegenerusGame (subscriber set + cursors + a per-sub box-stamp + the v54 `afkingFunding` ledger into `DegenerusGameStorage`; a new `GameAfkingModule` delegatecall owns subscribe/setters + the process-pass + the open-pass + the router; AfKing proper collapses to thin dispatch stubs). Boxes materialize at open from a per-sub stamp under the freeze / solvency / liveness spine.
**Closure verdict (EMITTED, Phase 352):** `AFKING_IN_GAME_FOLD SHIPPED (AfKing.sol dissolved → GameAfkingModule delegatecall + DegenerusGameStorage append, AF_KING removed 1→0; ARCH-01..04); BOX_REDESIGN SHIPPED (boons-OFF amount=spend, the COMMITTED 4-field stamp scorePlus1+amount+lastAutoBoughtDay+lastOpenedDay [349.1 SUPERSEDED the 348 5-field design], DAY-keyed seed rngWordByDay[lastAutoBoughtDay], live-level open byte-identical to openLootBox, lastOpenedDay no-double-open; BOX-01..05); FREEZE_SPINE INTACT (FREEZE-01/02/03 — live-level collapse NARROWS the frozen set to the SEED, strictly safe); REVERT_FREE_CHAIN DISCHARGED (REVERT-01 + REVERT-02 no-valve); EVCAP_AT_OPEN (EVCAP-01 RMW exactly-once, buy-time write bypassed); OPEN-E_4-PROTECTION RE-ATTESTED HOLD (CONSENT-01/02); SOLVENCY-01 HELD NET (afkingFunding rides inside claimablePool, no new aggregate); GAS Outcome-A; RNG_FREEZE_INTACT; NON-WIDENING 603/134/16; 0 NEW_FINDINGS; KNOWN_ISSUES_UNMODIFIED; + 1 OUT-OF-SCOPE informational advisory O1 [the pre-existing symmetric DegenerusQuests lootbox-quest double-credit, NOT in the v55 delta — folded into v56.0 as QST-05]`. Full report: `audit/FINDINGS-v55.0.md` (chmod 444).
**Coverage:** 29/29 v55.0 requirements mapped (348 SPEC: 5 · 349 IMPL: 14 · 350 GAS: 3 · 351 TST: 6 · 352 TERMINAL: 1); 0 orphaned, 0 duplicated. 2 USER-APPROVED contract commits (`77c3d9ef` 349.1 + `453f8073` 349.2); 350 GAS Outcome-A no-diff. **NOTHING pushed.**

> **Full v55.0 phase-detail (the per-phase goals + success criteria + plan checklists for Phases 348-352, incl. the inserted 349.1/349.2) is archived in `.planning/MILESTONES.md` and the per-phase `.planning/phases/348-*..352-*/` directories.** The 349.1/349.2 box-redesign + quest/affiliate-restore supersession history is captured in the STATE.md "Last Shipped Milestone (v55.0)" block + the `[[v55-afking-revert-free-proof]]` / `[[v55-afking-lootbox-quest-affiliate-regression-fix]]` memory topic files.

---

## Roadmap Overview

| Phase | Goal | Requirements (count) | Depends on |
|-------|------|----------------------|------------|
| 353 — SPEC — Design-Lock + Unmanipulable/Solvency Re-Attestation + XMODEL Design-Input + Call-Graph Attestation | Design-lock the mode-agnostic ~10-day aggregator (the affiliate flat-7% deterministic-split PULL [`claim`/`withdraw`, no roll/seed/flush — _amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic: SUPERSEDES the XMODEL roll-unification_] + the QUEST-only automatic settle + the per-sub accumulator layout [the SHRUNK in-slot `affiliateBase`+`questProgress` + off-slot `pendingClaim`; double-settle markers DROPPED — self-marking running balances]), the ticket-mode minimal-write primitive shape (+ century/x00 parity decision), the afking-OPEN-end review (materialization + unmanipulable re-verification), the shared-`DegenerusQuests`-core batched-settle entrypoint + non-perturbation approach, and the QST design (automatic slot-0 BURNIE via the `mintBurnie` settlement-due chain + a `claimQuest` fallback; the first-sub-only `+daysToNextSettle` streak head-start [bounded +0..+9, no provisional] — _amended 2026-06-01 — automatic slot-0 BURNIE via mintBurnie chain + claimQuest fallback; first-sub-only +daysToNextSettle streak, no provisional_); re-attest unmanipulable (esp. strategic sub/unsub) + SOLVENCY-01-untouched + RNG-freeze-intact on paper; run the XMODEL-01 cross-model design-input pass (Codex + Gemini) and fold its gas suggestions + exploit findings into the design-lock; grep-attest every cited file:line vs `453f8073`. Zero contract mutation. | AFF-01 · AFF-02 · XMODEL-01 (3) | Nothing (baseline v55.0 closure HEAD `453f8073` / `MILESTONE_V55_AT_HEAD_ca3bbd3220de763298ef2e742111f6e6ef90d583`) |
| 354 — IMPL — The ONE Carefully-Sequenced Batched Contract Diff (aggregator + ticket-mode primitive + DegenerusQuests batched-settle + accumulator + open-end) | Apply the single reconciled diff producer-before-consumer (accumulator storage → aggregator accrue/settle → DegenerusQuests batched-settle → ticket-mode minimal-write primitive → open-end optimizations → interfaces): the mode-agnostic aggregator (accrue-cheap-no-cross-contract + the AFFILIATE leg as a flat-7% deterministic-split PULL [`claim`/`withdraw`, no roll/seed/flush] + the QUEST leg settled by RIDING THE DAILY BUY STAGE on the global settle day [the separate `mintBurnie` "settlement-due" router leg REJECTED as a redundant cold-SLOAD pass; the slot-0 quest BURNIE + the `buyerOwedBurnie` ticket bonus mint TOGETHER in one `creditFlip`; `SUB_STAGE_BATCH` SHRUNK, number deferred to 355] + a quest-settle-on-unsub + uniform mode-agnostic quest-settle + self-marking running balances [double-settle markers DROPPED] — _amended 2026-06-01 — quest settle rides the buy STAGE; afking ticket buyer-bonus 10%/20% accrued+settled with the quest (matches live `:1655`)_), the ticket-mode minimal-write primitive (at resolution parity, century/x00 decision applied, the 10%/20% ticket buyer-bonus `:1655-1659` accrued into `buyerOwedBurnie` at live parity), the DegenerusQuests batched-settle entrypoint + the streak machinery (the slot-0 `questProgress` delivered-day counter → settle-mint `× QUEST_SLOT0_REWARD` [+ the accrued `buyerOwedBurnie` ticket bonus] via the STAGE-riding settle on the global settle day / `claimQuest` fallback, the first-sub-only `+daysToNextSettle` head-start [bounded +0..+9, no provisional] reading the actual `state.streak`, double-credit guard + active-pass anti-reset, non-perturbing, O1 confirmed/fixed — _amended 2026-06-01 — quest settle rides the buy STAGE; slot-0 BURNIE + the 10%/20% ticket buyer-bonus mint together; first-sub-only +daysToNextSettle streak, no provisional_), and the open-end optimizations (max gas, cheapest shared materialization, COMPLETELY unmanipulable — live-level parity / no double-open / no EV-cap double-draw / no shared-state hazard); HARD STOP at the contract-commit boundary (applied + locally compiled, never committed without explicit user hand-review). | AGG-01..05 · TKT-01/02 · QST-01..05 · OPEN-01/02 (14) | Phase 353 |
| 355 — GAS — Measure + Tune (marginals + accumulator packing + SUB_STAGE_BATCH re-tune + mode/SLOAD collapse) | Measure + tune the everyday cost under the security-over-gas floor (gas-scavenger → gas-skeptic, each tune unmanipulable): the per-buy + per-settle marginals (lootbox ~206k → ~130–140k; ticket off ~262k) under the 16.7M ceiling; the accumulator packed into the Sub spare bits (no new cold SSTORE); `SUB_STAGE_BATCH` re-tuned with the per-day STAGE under 16.7M at the cap; redundant mode-branches / repeated SLOADs collapsed where cheaper. Any net change rides the same USER-approved diff at the contract boundary (or records Outcome-A no-diff). | GAS-01 · GAS-02 · GAS-03 · GAS-04 (4) | Phase 354 |
| 356 — TST — Unmanipulable (strategic sub/unsub) + Quest-Core Non-Perturbation + Two-Path-Open + Gas Marginals + Non-Widening | Prove the hard security floor empirically: the afking system (buy + open) is unmanipulable — no positive-EV from settle-timing (no seed) / strategic sub/unsub churn / re-rate-on-alteration / pre-credit-EV inflation / double-credit / open-timing / settle-griefing (amended 2026-06-01: the roll is REMOVED under the flat-7% deterministic-split pull); SOLVENCY-01 untouched (BURNIE flip-credit OFF the ETH/pool path, debit byte-unchanged, RNG-freeze intact); the shared DegenerusQuests core non-perturbing; the two open routes coexist hazard-free; the gas marginals measured under 16.7M (GAS wins same-results); NON-WIDENING vs `453f8073` (every red BY NAME). | SEC-01 · SEC-02 (2) | Phase 355 |
| 357 — TERMINAL — Delta Audit + 3-Skill Adversarial Sweep (AUGMENTED by XMODEL Codex + Gemini close) + FINDINGS-v56.0 + Closure | NON-WIDENING delta-audit vs the v55 baseline `453f8073` (SOLVENCY-01 byte-unchanged + RNG-freeze + affiliate deterministic-split-PULL non-gameability [NO roll/seed — _amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic_] + open-end two-path/no-double-open + shared-quest non-perturbation re-attested), 3-skill genuine-PARALLEL adversarial sweep over the strategic sub/unsub edge + settle-timing non-exploitability (no seed) + pre-credit-EV + two-path open (skeptic-filtered) AUGMENTED by the XMODEL cross-model close (Codex + Gemini), author `audit/FINDINGS-v56.0.md` (9-section, chmod 444), emit `MILESTONE_V56_AT_HEAD_<sha>` + atomic 5-doc closure flip; re-attest all 24 requirements. | AUDIT-01 (1) | Phase 356 |

**Coverage:** 24/24 v56.0 requirements mapped (AGG 5 + TKT 2 + AFF 2 + QST 5 + OPEN 2 + GAS 4 + SEC 2 + XMODEL 1 + AUDIT 1); zero orphan requirements; zero duplicate mappings.
