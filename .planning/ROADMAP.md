# Roadmap: Degenerus Protocol Audit

## Milestones

- ✅ **v1.0 Initial RNG Security Audit** — Phases 1-5 (shipped 2026-03-14)
- ✅ **v2.0 Adversarial Audit** — Phases 6-18 (shipped 2026-03-17)
- ✅ **v3.0-v24.1** — Phases 19-212 (shipped 2026-04-10)
- ✅ **v25.0 Full Audit (Post-v5.0 Delta + Fresh RNG)** — Phases 213-217 (shipped 2026-04-11)
- ✅ **v26.0 Bonus Jackpot Split** — Phases 218-219 (shipped 2026-04-12)
- ✅ **v27.0 Call-Site Integrity Audit** — Phases 220-223 (shipped 2026-04-13)
- ✅ **v28.0 Database & API Intent Alignment Audit** — Phases 224-229 (shipped 2026-04-15) — see [milestones/v28.0-ROADMAP.md](milestones/v28.0-ROADMAP.md)
- ✅ **v29.0 Post-v27 Contract Delta Audit** — Phases 230-236 (shipped 2026-04-18) — see [milestones/v29.0-ROADMAP.md](milestones/v29.0-ROADMAP.md)
- ✅ **v30.0 Full Fresh-Eyes VRF Consumer Determinism Audit** — Phases 237-242 (shipped 2026-04-20) — see [milestones/v30.0-ROADMAP.md](milestones/v30.0-ROADMAP.md)
- ✅ **v31.0 Post-v30 Delta Audit + Gameover Edge-Case Re-Audit** — Phases 243-246 (shipped 2026-04-24) — see [milestones/v31.0-ROADMAP.md](milestones/v31.0-ROADMAP.md)
- ✅ **v32.0 Backfill Idempotency + purchaseLevel Underflow Audit** — Phases 247-253 (shipped 2026-05-02) — see [milestones/v32.0-ROADMAP.md](milestones/v32.0-ROADMAP.md)
- ✅ **v33.0 Charity Allowlist Governance** — Phases 254-258 (shipped 2026-05-07; closure signal `MILESTONE_V33_AT_HEAD_4ce3703d740d3707c88a1af595618120a8168399` supersedes `MILESTONE_V33_AT_HEAD_dcb70941`) — see [milestones/v33.0-ROADMAP.md](milestones/v33.0-ROADMAP.md)
- ✅ **v34.0 Trait Rarity Rework + Gold Solo Priority** — Phases 259-262 (shipped 2026-05-09; closure signal `MILESTONE_V34_AT_HEAD_6b63f6d4daf346a53a1d463790f637308ea8d555`) — see [milestones/v34.0-ROADMAP.md](milestones/v34.0-ROADMAP.md)
- ✅ **v35.0 BURNIE Near-Future Per-Pull Level Resample** — Phases 263-265 (shipped 2026-05-09; closure signal `MILESTONE_V35_AT_HEAD_5db8682bd7b811437f0c1cf47e832619d1478ac6`)
- ✅ **v36.0 Lootbox-Path Entropy Refactor** — Phase 266 (shipped 2026-05-10; closure signal `MILESTONE_V36_AT_HEAD_1c0f09132d7439af9881c56fe197f81757f8164a`) — see [milestones/v36.0-ROADMAP.md](milestones/v36.0-ROADMAP.md)
- ✅ **v37.0 Degenerette Recalibration + Maintenance Bundle** — Phases 267-271 (shipped 2026-05-11; closure signal `MILESTONE_V37_AT_HEAD_2654fcc2`) — see [milestones/v37.0-ROADMAP.md](milestones/v37.0-ROADMAP.md)
- ✅ **v38.0 Always-Hero Simplification + Maximal Dead-Code Cleanup** — Phase 272 (shipped 2026-05-11; closure signal `MILESTONE_V38_AT_HEAD_06623edb`) — see [milestones/v38.0-ROADMAP.md](milestones/v38.0-ROADMAP.md)
- ✅ **v39.0 Lootbox Whole-Ticket Rounding + WWXRP Consolation** — Phase 274 (shipped 2026-05-13; closure signal `MILESTONE_V39_AT_HEAD_6a7455d1`) — see [milestones/v39.0-ROADMAP.md](milestones/v39.0-ROADMAP.md)
- 🚧 **v40.0 Unified Whole-Ticket Award Protocol + Whole-BURNIE Floor** — Phases 275-280 (planning 2026-05-13; closure signal target `MILESTONE_V40_AT_HEAD_<sha>`)

## Phases

- [x] **Phase 275: Auto-Resolve LootboxModule Bernoulli (LBX-AR)** — Contract + test waves extending v39.0 Phase 274 manual-path Bernoulli to the 2 auto-resolve callers (`resolveLootboxDirect` decimator-claim + `resolveRedemptionLootbox` sDGNRS-redemption). The auto-resolve branch of `_resolveLootboxCommon` applies Bernoulli round-up using the SAME `bits[152..167]` 16-bit slice the v39.0 manual-path consumes (per-resolution seed-uniqueness verified safe across all 4 upstream callers per PROJECT.md v40.0 trace). Replaces `_queueTicketsScaled(...)` at `DegenerusGameLootboxModule.sol:1068` with `_queueTickets(player, level, whole, false)`. SILENT on cold-bust per D-40N-SILENT-01 (no WWXRP consolation; auto-resolve cold-bust is a silent zero-award outcome). Net gas-NEUTRAL after factoring eliminated `_rollRemainder` consumption at trait-assignment time. Mint-boost path at `DegenerusGameMintModule.sol:1142` UNCHANGED per D-40N-MINTBOOST-OUT-01. One USER-APPROVED batched contract commit + one USER-APPROVED batched test commit per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md`.
- [ ] **Phase 276: JackpotModule:2216 BAF Bernoulli (JPT-BR)** — Contract + test waves applying Bernoulli round-up to the JackpotModule small-lootbox ticket-roll path. `_jackpotTicketRoll` (at `JackpotModule.sol:2186`) reads `bits[200..215]` of the existing `entropy` chain (16-bit slice for <0.10% relative bias; 180+ bits separated from existing bits[0..12] consumers for path/bucket selection). Replaces `_queueLootboxTickets(player, level, scaledTickets)` wrapper at `JackpotModule.sol:2216` with direct `_queueTickets(player, level, whole, false)`. SILENT on cold-bust per D-40N-SILENT-01 — roll outcome surfaces via the new `JackpotTicketWin.roundedUp` field (added in Phase 277 EVT-UNI-04). Bit-allocation NatSpec documents the new sub-roll. Net gas-NEGATIVE (wrapper retirement + smaller storage write + eliminated `_rollRemainder` consumption). One USER-APPROVED batched contract commit + one USER-APPROVED batched test commit.
- [ ] **Phase 277: Event Surface Unification + Sentinel Retirement (EVT-UNI)** — Contract + test waves consolidating the v39.0-additive `LootboxTicketRoll` event into the existing per-action events. Drops `LootboxTicketRoll` from `IDegenerusGameLootboxModule.sol` + the contract event block (zero remaining emission sites). Adds `(uint32 preRollTickets, bool roundedUp)` fields to `LootBoxOpened` + `BurnieLootOpen`; adds `bool roundedUp` to `JackpotTicketWin`. Retires the `_resolveLootboxCommon` `index != type(uint48).max` behavior-gating sentinel — manual + auto-resolve converge on the same `_queueTickets(whole)` model so the gate no longer serves a purpose; the `uint48 index` parameter retains its event-emission identifier role. Auto-resolve callers (`resolveLootboxDirect` + `resolveRedemptionLootbox`) update to pass real `index` instead of `type(uint48).max`. Auto-resolve `LootBoxOpened` emission shape decision (EVT-UNI-06 plan-phase) per D-40N-AR-EMIT-01. Breaking topic-hashes accepted per D-40N-EVT-BREAK-01 — pre-launch supersession of v39.0 D-274-NO-EVT-BREAK-01 non-breaking stance; indexer rebuild expected. Saves ~1,350 gas per manual lootbox open. One USER-APPROVED batched contract commit + one USER-APPROVED batched test commit.
- [ ] **Phase 278: JackpotModule Cleanup + ENT-05 BAF Xorshift Refactor + Wrapper Retirement (JPT-CLEAN)** — Contract + test waves consolidating remaining JackpotModule maintenance. Cosmetic `xTICKET_SCALE` cleanup at `JackpotModule.sol:702/835/1005` (deferred per D-274-JACKPOT-OUT-01); ENT-05 BAF xorshift refactor (deferred since v36.0 P266; carry per D-274-JACKPOT-OUT-01 + EXC-04 NARROWS-retained scope) — refactor BAF jackpot xorshift entropy path to align with `EntropyLib.entropyStep` keccak primary-chunk convention OR explicit derivation discipline; EXC-04 KI envelope may demote from `NARROWS` to `NEGATIVE` if the refactor lands; final refactor shape TBD by plan-phase per `feedback_design_intent_before_deletion.md`. Retires `_queueLootboxTickets` wrapper function (unused after Phase 276 JPT-BR-02 lands — `JackpotModule.sol:2216` was its only caller) per `feedback_no_dead_guards.md`. Adds cross-surface mixing regression test (TST-CROSS-01) covering same-player ticket-award mixing across all 3 RNG-driven surfaces (manual lootbox + auto-resolve lootbox + jackpot) at the same target future level, plus mint-boost mixed in once to confirm `_rollRemainder` still fires for mint-boost queues per D-40N-MINTBOOST-OUT-01. One USER-APPROVED batched contract commit + one USER-APPROVED batched test commit.
- [ ] **Phase 279: Whole-BURNIE Floor (BUR)** — Contract + test waves applying A1 floor-per-winner mechanic to all 3 RNG-influenced BURNIE-award sites per D-40N-BUR-FLOOR-01. Floor `burnieAmount` at `DegenerusGameLootboxModule.sol:1080` upstream of `coinflip.creditFlip(player, burnieAmount)` — `burnieAmount = (burnieAmount / 1 ether) * 1 ether` (variance-roll-derived BURNIE becomes whole-BURNIE only; per-spin per-player dust loss bounded < 1 BURNIE). Floor `baseAmount = coinBudget / cap` at `DegenerusGameJackpotModule.sol:1785` to whole-BURNIE multiples; the existing `extra` cursor-rotation +1-wei distribution retires per A1 mechanic; daily-budget evaporation accepted on low-pool days when `baseAmount < 1 ether`. Floor `perWinner = farBudget / found` at `DegenerusGameJackpotModule.sol:1900` (in `_awardFarFutureCoinJackpot`) to whole-BURNIE multiples; existing `if (perWinner == 0) return` early-bail at `:1901` handles the post-floor zero case naturally; 25% far-future budget evaporates when `perWinner < 1 ether`. Sub-1-BURNIE residues evaporate per D-40N-BUR-DUST-01 (user disposition 2026-05-13: "sub 1 burnie amounts are economically negligible"). NO consolation, NO replacement event, NO cursor-rotation residue redistribution per D-40N-BUR-SILENT-01. Storage byte-identical; zero new admin / events / emit sites — pure-amount transformation upstream of existing emit sites (event-field values reflect post-floor amounts). Bytecode delta NET-NEGATIVE expected (integer-division floor adds ~5-15 gas per site; eliminates fractional-wei storage in `coinflip.creditFlip`). Mint-boost flip-credit at `MintModule:1199` + daily-coinflip claim at `BurnieCoinflip:409/770/789` + advance bounty + quests + affiliate DGNRS deity bonus all explicitly EXCLUDED per D-40N-BUR-MINTBOOST-OUT-01 (deterministic on user-altered or system-deterministic inputs; not RNG-amount). One USER-APPROVED batched contract commit + one USER-APPROVED batched test commit.
- [ ] **Phase 280: Delta Audit + Findings Consolidation (Terminal)** — Single `audit/FINDINGS-v40.0.md` 9-section deliverable per D-40N-FILES-01 carry; FINAL READ-only at v40.0 closure HEAD (`chmod 444`). §3.A row coverage for the 5 v40.0 phase contract+test commits (Phases 275-279); §4 adversarial surfaces enumerated (11 surfaces per AUDIT-03 a..k); 3-skill PARALLEL adversarial pass `/contract-auditor` + `/zero-day-hunter` + `/economic-analyst` per D-40N-ADVERSARIAL-01 (`/degen-skeptic` OUT OF SCOPE per D-271-ADVERSARIAL-02 carry); KI walkthrough EXC-01..04 RE_VERIFIED at v40 HEAD with EXC-04 disposition per JPT-CLEAN-04 outcome; LEAN regression REG-01 (v39.0 closure NON-WIDENING for v39-touched surfaces NOT in v40 scope; manual lootbox + bits[152..167] sharing with auto-resolve verified via per-resolution seed-uniqueness; LootboxModule:1080 lootbox spin BURNIE site newly v40-scoped per BUR-01 — excluded from non-widening proof) + REG-02 (v34.0 closure NON-WIDENING) + REG-03 (KI envelope re-verifications) + REG-04 (prior-finding spot-check sweep across audit/FINDINGS-v25.0..v39.0 including the 3 BUR sites). Closure signal `MILESTONE_V40_AT_HEAD_<sha>` emitted in §9c; ROADMAP/STATE/MILESTONES/PROJECT closure-flips land atomically post-§9c attestation per D-40N-CLOSURE-01 carry. Forward-cite zero-emission at terminal phase per D-40N-FCITE-01.

(Prior milestone phases archived in the collapsible blocks below.)

## Phase Details

### Phase 275: Auto-Resolve LootboxModule Bernoulli (LBX-AR)

**Goal:** Extend the v39.0 Phase 274 manual-path Bernoulli round-up to the 2 auto-resolve callers (`resolveLootboxDirect` decimator-claim + `resolveRedemptionLootbox` sDGNRS-redemption) so the auto-resolve branch of `_resolveLootboxCommon` (the branch reached at `DegenerusGameLootboxModule.sol:1068`) Bernoulli-collapses scaled `futureTickets` to whole tickets at queue time, replacing the `_queueTicketsScaled(...)` call with `_queueTickets(player, level, whole, false)`. The Bernoulli predicate consumes `bits[152..167]` of the per-resolution seed — the SAME 16-bit slice the v39.0 manual-path uses per D-274-BIT-SLICE-01 (v39 supersession to 16-bit form for <0.10% relative bias). Seed-uniqueness across all 4 upstream auto-resolve callers (DecimatorModule:594 + DegeneretteModule:786 + StakedDegenerusStonk:672 + DegenerusGame:1721 5-ETH-chunk redemption loop) is verified safe per PROJECT.md v40.0 trace — the redemption-loop wrapper evolves `rngWord` per iteration via `keccak256(abi.encode(rngWord))` at L1769 so each chunk's seed is unique. Auto-resolve cold-bust is SILENT per D-40N-SILENT-01 — when `whole == 0` post-Bernoulli on the auto-resolve branch, the function returns without `wwxrp.mintPrize(...)` and without `LootBoxWwxrpReward` emission (asymmetry with v39 manual-path consolation pattern intentional + documented; the auto-resolve paths resolve without explicit player intent at the moment of resolution). The `_queueTicketsScaled` helper itself may be deleted from the LootboxModule if no caller remains after LBX-AR-02 lands AND the mint-boost call path doesn't share the helper (final disposition by plan-phase via call-site grep). The `_rollRemainder` invocation count from auto-resolve lootbox paths drops to zero post-v40.0; mint-boost continues to invoke `_rollRemainder` per D-40N-MINTBOOST-OUT-01. Storage layout byte-identical at v40 phase-close HEAD vs v39 baseline `6a7455d1`; zero new admin entry points; zero new external mutation entry points; zero new modifiers. Wave shape: 1 USER-APPROVED batched contract commit `feat(275): auto-resolve lootbox Bernoulli whole-ticket [LBX-AR-01..06]` + 1 USER-APPROVED batched test commit `test(275): auto-resolve lootbox whole-ticket + silent cold-bust + seed-uniqueness regression + mint-boost regression [TST-LBX-AR-01..06]` per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md`. Bytecode delta + gas delta reported in commit messages.
**Depends on:** Nothing (independent surface from JPT-BR per CONTEXT.md dependency hints; baseline v39.0 closure HEAD `MILESTONE_V39_AT_HEAD_6a7455d1`)
**Requirements:** LBX-AR-01, LBX-AR-02, LBX-AR-03, LBX-AR-04, LBX-AR-05, LBX-AR-06, TST-LBX-AR-01, TST-LBX-AR-02, TST-LBX-AR-03, TST-LBX-AR-04, TST-LBX-AR-05, TST-LBX-AR-06
**Success Criteria** (what must be TRUE):
  1. The auto-resolve branch of `_resolveLootboxCommon` (reached from `resolveLootboxDirect` + `resolveRedemptionLootbox`) Bernoulli-collapses scaled `futureTickets` to whole tickets via `bits[152..167]` of the per-resolution seed: `frac != 0 && (uint16(seed >> 152) % uint16(TICKET_SCALE)) < uint16(frac)` → `roundedUp = true`; assigns `futureTickets = whole + (roundedUp ? 1 : 0)`. EV-neutrality holds: `E[whole_post] == scaledPre / TICKET_SCALE` exactly. Call at `DegenerusGameLootboxModule.sol:1068` swaps from `_queueTicketsScaled(player, targetLevel, futureTickets, false)` to `_queueTickets(player, targetLevel, whole, false)`; emits `TicketsQueued(buyer, level, qty)` (qty = whole), NOT `TicketsQueuedScaled` (LBX-AR-01, LBX-AR-02).
  2. Auto-resolve cold-bust is SILENT per D-40N-SILENT-01 — when `whole == 0` post-Bernoulli on the auto-resolve branch, the function returns without invoking `wwxrp.mintPrize(...)` and without emitting `LootBoxWwxrpReward`. Auto-resolve cold-bust is a silent zero-award outcome; the v39.0 manual-path's `LOOTBOX_WWXRP_CONSOLATION = 1 ether` consolation pattern does NOT extend to this surface (asymmetry intentional + documented at the manual-branch comment block) (LBX-AR-03).
  3. Seed-uniqueness preservation verified across all 4 upstream auto-resolve callers — (a) DecimatorModule:594 `claimDecimatorJackpot(lvl)` single-shot per call (unique rngWord from per-level storage); (b) DegeneretteModule:786 single-shot per payout call; (c) StakedDegenerusStonk:672 single-shot per redemption with entropy = `keccak(rngWord, player)`; (d) DegenerusGame:1721 redemption-loop wrapper evolves rngWord per 5-ETH-chunk iteration via `rngWord = keccak256(abi.encode(rngWord))` at L1769 so each chunk's seed is unique. v40.0 introduces no new seed-collision risk (LBX-AR-04).
  4. Storage layout byte-identical at v40 phase-close HEAD vs v39 baseline `6a7455d1` for `DegenerusGameLootboxModule.sol` (storage-slot grep proof); zero new admin entry points; zero new external mutation entry points; zero new modifiers. The `_queueTicketsScaled` helper may be deleted from the module if no caller remains after LBX-AR-02 lands AND mint-boost call paths don't share the helper (final disposition by plan-phase call-site grep — `_queueTicketsScaled` is module-private to LootboxModule; mint-boost caller is `DegenerusGameMintModule.sol:1142` so if helpers are module-private the LootboxModule copy retires). `_rollRemainder` invocation count from auto-resolve lootbox paths drops to zero post-v40.0; mint-boost path at `DegenerusGameMintModule.sol:1142` continues to invoke `_rollRemainder` per D-40N-MINTBOOST-OUT-01 (LBX-AR-05, LBX-AR-06).
  5. Test coverage: TST-LBX-AR-01 EV-neutrality property (≥10K seeded `claimDecimatorJackpot` + `resolveRedemptionLootbox` invocations across scaled value span 47/99/100/147/250/1000/9999; `mean(whole_post) × TICKET_SCALE` within ±0.5% of pre-Bernoulli scaled value); TST-LBX-AR-02 boundary tests (scaled 0/1/99/100/101/199/200); TST-LBX-AR-03 silent-cold-bust regression (force seed where `whole==0` from non-zero pre; assert zero `TicketsQueued` emit, zero `LootBoxWwxrpReward` emit, zero `wwxrp.mintPrize` invocation, `wwxrp.balanceOf(player)` unchanged); TST-LBX-AR-04 seed-uniqueness chi-square across all 4 upstream callers; TST-LBX-AR-05 `_rollRemainder` zero-invocation regression on auto-resolve queues; TST-LBX-AR-06 mint-boost regression confirming `_rollRemainder` STILL fires for mint-boost queues per D-40N-MINTBOOST-OUT-01.
  6. Wave shape: 1 USER-APPROVED batched contract commit per `feedback_batch_contract_approval.md` + `feedback_no_contract_commits.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md` (bytecode delta + gas delta reported in commit message; net gas-NEUTRAL expected after factoring eliminated `_rollRemainder` consumption at trait-assignment time) + 1 USER-APPROVED batched test commit. Both commits land before Phase 277 EVT-UNI begins (Phase 277 depends on this phase for sentinel retirement convergence).

**Plans:**
- [x] 275-A-PLAN.md — Wave 1: contract edits (Bernoulli hoist + auto-resolve _queueTickets swap + NatSpec update + worst-case gas benchmark + storage-layout proof) [LBX-AR-01..06]
- [x] 275-B-PLAN.md — Wave 2: test additions (TST-LBX-AR-01 EV-neutrality + TST-LBX-AR-02 boundaries + TST-LBX-AR-03 silent cold-bust + TST-LBX-AR-04 seed-uniqueness chi-square + TST-LBX-AR-05 _rollRemainder zero-invocation + TST-LBX-AR-06 mint-boost regression) [TST-LBX-AR-01..06]

### Phase 276: JackpotModule:2216 BAF Bernoulli (JPT-BR)

**Goal:** Apply Bernoulli round-up to the JackpotModule small-lootbox ticket-roll path. `_jackpotTicketRoll` (at `JackpotModule.sol:2186`) reads `bits[200..215]` of the existing `entropy` chain — a 16-bit slice for <0.10% relative bias; 180+ bits separated from the existing bits[0..12] consumers (path/bucket selection). Predicate: `frac != 0 && (uint16(entropy >> 200) % uint16(TICKET_SCALE)) < uint16(frac)` → `roundedUp = true`; assigns `whole = (scaledTickets / TICKET_SCALE) + (roundedUp ? 1 : 0)`. The `_queueLootboxTickets(player, level, scaledTickets)` wrapper call at `JackpotModule.sol:2216` swaps to direct `_queueTickets(player, level, whole, false)`. The wrapper retires in Phase 278 (JPT-CLEAN-05) since `:2216` is its only caller. Per-roll uniqueness verified: `EntropyLib.entropyStep` evolves the entropy chain between the 2-roll pattern at `_awardJackpotTickets` L2157/L2166 so each ticket-roll's entropy seed is distinct; v40.0 introduces no new entropy-collision risk. SILENT on cold-bust per D-40N-SILENT-01 — when `whole == 0` post-Bernoulli, the function returns without `wwxrp.mintPrize(...)`. Roll outcome surfaces via the new `JackpotTicketWin.roundedUp` field (added in Phase 277 EVT-UNI-04). Net gas-NEGATIVE — wrapper retirement removes one internal call frame; `_queueTickets(whole)` writes smaller value than `_queueTicketsScaled(scaled)`; eliminated `_rollRemainder` consumption at trait-assignment time. Bit-allocation NatSpec on `_awardJackpotTickets` / `_jackpotTicketRoll` updated to document the new `bits[200..215] jackpotTicketRoundUp % 100` sub-roll with clear 180+ bit separation from existing bits[0..12] consumers. Wave shape: 1 USER-APPROVED batched contract commit `feat(276): jackpot ticket-roll Bernoulli whole-ticket [JPT-BR-01..06]` + 1 USER-APPROVED batched test commit `test(276): jackpot ticket-roll Bernoulli + silent cold-bust + bit-slice independence + 2-roll uniqueness [TST-JPT-BR-01..04]` per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md`. Bytecode delta + gas delta reported in commit messages.
**Depends on:** Nothing (independent surface from LBX-AR per CONTEXT.md dependency hints; baseline v39.0 closure HEAD `MILESTONE_V39_AT_HEAD_6a7455d1`)
**Requirements:** JPT-BR-01, JPT-BR-02, JPT-BR-03, JPT-BR-04, JPT-BR-05, JPT-BR-06, TST-JPT-BR-01, TST-JPT-BR-02, TST-JPT-BR-03, TST-JPT-BR-04
**Success Criteria** (what must be TRUE):
  1. `_jackpotTicketRoll` (at `JackpotModule.sol:2186`) applies Bernoulli round-up using `bits[200..215]` of the existing `entropy` chain: `frac != 0 && (uint16(entropy >> 200) % uint16(TICKET_SCALE)) < uint16(frac)` → `roundedUp = true`; assigns `whole = (scaledTickets / TICKET_SCALE) + (roundedUp ? 1 : 0)`. The `_queueLootboxTickets(player, level, scaledTickets)` wrapper call at `JackpotModule.sol:2216` swaps to direct `_queueTickets(player, level, whole, false)` (the wrapper function itself retires in Phase 278 JPT-CLEAN-05) (JPT-BR-01, JPT-BR-02).
  2. Per-roll uniqueness verified — `EntropyLib.entropyStep` evolves the entropy chain between the 2-roll pattern at `_awardJackpotTickets` L2157/L2166 so each ticket-roll's entropy seed is distinct. The bits[200..215] slices of the 2 rolls within a single `_awardJackpotTickets` invocation are uncorrelated. v40.0 introduces no new entropy-collision risk (JPT-BR-03).
  3. Jackpot cold-bust is SILENT per D-40N-SILENT-01 — when `whole == 0` post-Bernoulli, the function returns without invoking `wwxrp.mintPrize(...)` and without emitting `LootBoxWwxrpReward`. Roll outcome surfaces via the new `JackpotTicketWin.roundedUp` field (added in Phase 277 EVT-UNI-04 — until Phase 277 lands, the test surface for JPT-BR uses the pre-EVT-UNI event shape and asserts the existing fields; Phase 277 test wave updates the assertion to cover the new field) (JPT-BR-04).
  4. Net gas-NEGATIVE — `_queueLootboxTickets` wrapper retirement removes one internal call frame (deferred to Phase 278); `_queueTickets(whole)` writes a smaller value to storage than `_queueTicketsScaled(scaled)`; eliminated `_rollRemainder` consumption at trait-assignment time. Bit-allocation NatSpec on `_awardJackpotTickets` / `_jackpotTicketRoll` updated to document `bits[200..215] jackpotTicketRoundUp % 100` with clear 180+ bit separation from bits[0..12] consumers (JPT-BR-05, JPT-BR-06).
  5. Test coverage: TST-JPT-BR-01 EV-neutrality property (≥10K seeded `_awardJackpotTickets` invocations across representative pre-Bernoulli scaled values; `mean(whole_post) × TICKET_SCALE` within ±0.5%); TST-JPT-BR-02 silent-cold-bust regression (force seed where `scaled > 0` and `< TICKET_SCALE` AND Bernoulli loses; assert zero `TicketsQueued` emit, zero `LootBoxWwxrpReward` emit, `wwxrp.balanceOf(player)` unchanged; `JackpotTicketWin` event emits with pre-EVT-UNI fields at this phase, post-EVT-UNI field assertions added in Phase 277); TST-JPT-BR-03 bit-slice independence chi-square (≥10K seeds; verify bits[200..215] uncorrelated with bits[0..12] consumers); TST-JPT-BR-04 2-roll pattern uniqueness within single `_awardJackpotTickets` invocation.
  6. Wave shape: 1 USER-APPROVED batched contract commit + 1 USER-APPROVED batched test commit per the same approval discipline as Phase 275. Both commits land before Phase 278 JPT-CLEAN begins (Phase 278 depends on this phase for wrapper retirement) AND before Phase 277 EVT-UNI begins (Phase 277 EVT-UNI-04 adds `roundedUp` field to `JackpotTicketWin` which is the event surface for this phase's roll outcomes).

**Plans:** TBD

### Phase 277: Event Surface Unification + Sentinel Retirement (EVT-UNI)

**Goal:** Consolidate the v39.0-additive `LootboxTicketRoll` event into the existing per-action events, retire the `_resolveLootboxCommon` `index != type(uint48).max` behavior-gating sentinel, and update auto-resolve callers to pass real `index` instead of the sentinel. DROP the v39.0-additive `LootboxTicketRoll` event entirely from `IDegenerusGameLootboxModule.sol` AND the contract-level event block of `DegenerusGameLootboxModule.sol` — zero remaining emission sites in the codebase. Add `(uint32 preRollTickets, bool roundedUp)` fields to `LootBoxOpened` (final field order TBD by plan-phase for optimal slot packing); `preRollTickets` = pre-Bernoulli scaled value (matches `LootboxTicketRoll.preRollTickets` semantics from v39); `roundedUp` = Bernoulli outcome (matches `LootboxTicketRoll.roundedUp` semantics). Emitted on BOTH manual + auto-resolve paths (auto-resolve emission shape decided by plan-phase per D-40N-AR-EMIT-01 EVT-UNI-06 — option (a) add `LootBoxOpened` emission to both auto-resolve callers for emission-model uniformity, OR option (b) keep auto-resolve silent on `LootBoxOpened` and surface `(preRollTickets, roundedUp)` via a different signal e.g. `TicketsQueued` extension; option (a) is the default; option (b) reduces gas on the high-volume decimator-claim path). Add `(uint32 preRollTickets, bool roundedUp)` to `BurnieLootOpen` with same semantics; emitted from `openBurnieLootBox` paths (manual only — auto-resolve doesn't open BURNIE lootboxes). Add `bool roundedUp` to `JackpotTicketWin`; emitted from `_awardJackpotTickets` L2157/L2166 ticket-roll outcomes (the existing event surface already emits scaled values; the existing `preRollTickets`-equivalent semantics live in current event fields per plan-phase grep). RETIRE the `index != type(uint48).max` sentinel behavior-gating branch on `_resolveLootboxCommon` — manual + auto-resolve converge on the same `_queueTickets(whole)` model post-Phase-275, so the behavior gate no longer serves a purpose; both branches now Bernoulli-roll and call `_queueTickets(whole)`. The `uint48 index` parameter retains its event-emission identifier role per EVT-UNI-02. Auto-resolve callers (`resolveLootboxDirect` + `resolveRedemptionLootbox`) update to pass real `index` instead of `type(uint48).max`. Breaking topic-hashes accepted per D-40N-EVT-BREAK-01 — pre-launch supersession of v39.0 D-274-NO-EVT-BREAK-01 non-breaking stance; indexer rebuild expected; no live indexer impact. Bytecode delta — saves ~1,350 gas per manual lootbox open (no separate LOG3 emit for `LootboxTicketRoll`). Wave shape: 1 USER-APPROVED batched contract commit `feat(277): event surface unification + sentinel retirement [EVT-UNI-01..08]` + 1 USER-APPROVED batched test commit `test(277): event topic-hash changes + LootboxTicketRoll removal + sentinel retirement + field consistency [TST-EVT-UNI-01..06]` per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md`. Gas delta reported in commit messages — expected NET-NEGATIVE across all ticket-award paths after factoring `LogN` topic count + payload-size deltas.
**Depends on:** Phase 275 (sentinel retirement requires both manual + auto-resolve to converge on `_queueTickets(whole)` — auto-resolve convergence ships in Phase 275 LBX-AR-02); Phase 276 (EVT-UNI-04 adds `roundedUp` field to `JackpotTicketWin` which is the event surface for JPT-BR roll outcomes — JPT-BR-04 silent-cold-bust requirement references the post-EVT-UNI field shape)
**Requirements:** EVT-UNI-01, EVT-UNI-02, EVT-UNI-03, EVT-UNI-04, EVT-UNI-05, EVT-UNI-06, EVT-UNI-07, EVT-UNI-08, TST-EVT-UNI-01, TST-EVT-UNI-02, TST-EVT-UNI-03, TST-EVT-UNI-04, TST-EVT-UNI-05, TST-EVT-UNI-06
**Success Criteria** (what must be TRUE):
  1. The v39.0-additive `LootboxTicketRoll` event is DROPPED entirely from `IDegenerusGameLootboxModule.sol` interface signature + `DegenerusGameLootboxModule.sol` contract event block; zero remaining emission sites in the codebase (verified via static-analysis grep + dynamic test sweep TST-EVT-UNI-02). `LootBoxOpened` gains `(uint32 preRollTickets, bool roundedUp)` fields with v39 `LootboxTicketRoll` semantics (preRollTickets = pre-Bernoulli scaled; roundedUp = Bernoulli outcome); `BurnieLootOpen` gains the same 2 fields with same semantics (manual paths only); `JackpotTicketWin` gains `bool roundedUp` field mirroring `_jackpotTicketRoll` Bernoulli outcome; final field order TBD by plan-phase for optimal slot packing (EVT-UNI-01, EVT-UNI-02, EVT-UNI-03, EVT-UNI-04).
  2. The `_resolveLootboxCommon` `index != type(uint48).max` behavior-gating sentinel is RETIRED — both branches now Bernoulli-roll and call `_queueTickets(whole)`. The `uint48 index` parameter retains its event-emission identifier role. Auto-resolve callers (`resolveLootboxDirect` + `resolveRedemptionLootbox`) update to pass real `index` instead of `type(uint48).max`. Static-analysis or coverage trace confirms the conditional disassembles to a single unified code path (EVT-UNI-05, TST-EVT-UNI-03).
  3. Auto-resolve emission shape decision (EVT-UNI-06 plan-phase decision per D-40N-AR-EMIT-01): option (a) ADD `LootBoxOpened` emission to both `resolveLootboxDirect` + `resolveRedemptionLootbox` (matches unified event-emission model; field-consistent with manual paths; DEFAULT), OR option (b) keep auto-resolve silent on `LootBoxOpened` and surface `(preRollTickets, roundedUp)` via alternative signal (e.g., `TicketsQueued` extension; reduces gas on high-volume decimator-claim path). Decision recorded in plan-phase artifact; test wave TST-EVT-UNI-05 asserts the chosen shape (EVT-UNI-06).
  4. Breaking event topic-hashes accepted per D-40N-EVT-BREAK-01 — pre-launch supersession of v39.0 D-274-NO-EVT-BREAK-01 non-breaking stance. Indexer rebuild expected; no live indexer impact. TST-EVT-UNI-01 computes new topic hashes; asserts event emissions match new signatures; asserts old topic hashes have zero remaining emit sites (EVT-UNI-08, TST-EVT-UNI-01).
  5. Test coverage: TST-EVT-UNI-01 topic-hash change tests on `LootBoxOpened` + `BurnieLootOpen` + `JackpotTicketWin`; TST-EVT-UNI-02 `LootboxTicketRoll` removal regression (zero remaining emission sites verified via static-analysis grep + dynamic test sweep; interface signature deleted from `IDegenerusGameLootboxModule.sol`); TST-EVT-UNI-03 sentinel-retirement regression (auto-resolve callers pass real `index`, NOT `type(uint48).max`); TST-EVT-UNI-04 manual-path event field-consistency on `LootBoxOpened` (derived `whole = (preRollTickets / 100) + (roundedUp ? 1 : 0)` equals queued ticket count; `preRollTickets` matches scaled-pre value; `lootboxIndex` matches `index` arg; consolation correlation preserved per v39.0 invariants); TST-EVT-UNI-05 auto-resolve event field-consistency (per EVT-UNI-06 decision); TST-EVT-UNI-06 jackpot event field-consistency (`JackpotTicketWin.roundedUp` present and mirrors `_jackpotTicketRoll` Bernoulli outcome).
  6. Wave shape: 1 USER-APPROVED batched contract commit + 1 USER-APPROVED batched test commit per the same approval discipline as Phases 275-276. Both commits land before Phase 279 terminal audit begins. Net per-op gas delta reported in commit message (expected NET-NEGATIVE: ~1,350 gas saved per manual lootbox open from `LogN` topic count + payload-size deltas).

**Plans:** TBD

### Phase 278: JackpotModule Cleanup + ENT-05 BAF Xorshift Refactor + Wrapper Retirement (JPT-CLEAN)

**Goal:** Consolidate remaining JackpotModule maintenance and ship the cross-surface mixing regression test. Cosmetic `xTICKET_SCALE` cleanup at `JackpotModule.sol:702/835/1005` — simplify `× TICKET_SCALE` expressions where the scaled-vs-whole distinction is moot post-Phase-276 JPT-BR-02 landing; final shape TBD by plan-phase grep + design-intent trace per `feedback_design_intent_before_deletion.md`. ENT-05 BAF xorshift refactor (deferred since v36.0 P266; carry per D-274-JACKPOT-OUT-01 + EXC-04 NARROWS-retained scope) — refactor the BAF jackpot xorshift entropy path to align with `EntropyLib.entropyStep` keccak primary-chunk convention OR explicit derivation discipline; specific refactor shape TBD by plan-phase (e.g., replace xorshift with keccak-derived sub-roll, OR document xorshift as locally-required for legacy reasons). EXC-04 KI envelope narrowing scope reduces post-refactor — if the refactor lands, EXC-04 RE_VERIFIED at v40 close may demote from `NARROWS` to `NEGATIVE`. RETIRE the `_queueLootboxTickets` wrapper function — unused after Phase 276 JPT-BR-02 lands (`JackpotModule.sol:2216` was its only caller); replacing with direct `_queueTickets(whole)` makes the wrapper dead code; delete per `feedback_no_dead_guards.md`. Storage layout byte-identical at v40 phase-close HEAD vs v39 baseline `6a7455d1` for `JackpotModule.sol` (storage-slot grep proof). Bytecode delta reported in commit message — expected NET-NEGATIVE (cleanup + refactor + wrapper retirement all shrink bytecode). Cross-surface mixing regression test TST-CROSS-01 lives in this phase's test wave since by Phase 278 all 3 RNG-driven surfaces (manual lootbox + auto-resolve lootbox + jackpot ticket-roll) are in their final state — exercises same player through all 3 surface families at the same target future level (5 manual lootbox opens + 3 auto-resolve lootbox opens + 2 jackpot ticket-roll awards), asserts all 3 families Bernoulli-roll independently (no shared `rem` byte residue accumulation across surfaces; per-resolution whole-ticket commits), and mixes mint-boost in once at the same future level to confirm `_rollRemainder` continues to accumulate via `rem` byte per D-40N-MINTBOOST-OUT-01. Wave shape: 1 USER-APPROVED batched contract commit `feat(278): jackpot cleanup + ENT-05 BAF xorshift refactor + _queueLootboxTickets retirement [JPT-CLEAN-01..06]` + 1 USER-APPROVED batched test commit `test(278): jackpot cleanup byte-equivalence + ENT-05 refactor stat regression + cross-surface mixing [TST-CLEAN-01..03, TST-CROSS-01]` per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md`.
**Depends on:** Phase 276 (`_queueLootboxTickets` wrapper retirement requires JPT-BR-02 to land first — `JackpotModule.sol:2216` is the wrapper's only caller; replacing it with direct `_queueTickets(whole)` is what makes the wrapper dead code). Phase 275 + Phase 277 not strictly required for JPT-CLEAN-01..06 but the cross-surface regression TST-CROSS-01 requires all 3 RNG-driven surfaces in their final state — Phase 275 (auto-resolve Bernoulli), Phase 276 (jackpot Bernoulli), Phase 277 (event surface) — so this phase sequences AFTER Phases 275-277.
**Requirements:** JPT-CLEAN-01, JPT-CLEAN-02, JPT-CLEAN-03, JPT-CLEAN-04, JPT-CLEAN-05, JPT-CLEAN-06, TST-CLEAN-01, TST-CLEAN-02, TST-CLEAN-03, TST-CROSS-01
**Success Criteria** (what must be TRUE):
  1. Cosmetic `xTICKET_SCALE` cleanup lands at `JackpotModule.sol:702/835/1005` — `× TICKET_SCALE` expressions simplified where the scaled-vs-whole distinction is moot post-Phase-276 JPT-BR-02 landing; final shape decided by plan-phase grep + design-intent trace per `feedback_design_intent_before_deletion.md`. Pre/post-cleanup function bytecode at L702 + L835 + L1005 matches semantically (no behavior change); storage layout byte-identical (JPT-CLEAN-01, JPT-CLEAN-02, JPT-CLEAN-03, TST-CLEAN-03).
  2. ENT-05 BAF xorshift refactor lands — BAF jackpot xorshift entropy path refactored to align with `EntropyLib.entropyStep` keccak primary-chunk convention OR explicit derivation discipline; specific refactor shape decided by plan-phase. Pre/post-refactor BAF jackpot outputs match across N seeded `_awardJackpotTickets` invocations (byte-equivalence) OR test asserts the NEW invariant (post-refactor entropy chi-square + seed-uniqueness) if the refactor changes entropy semantics intentionally. EXC-04 KI envelope re-verified at Phase 279 §6b — may demote from `NARROWS` to `NEGATIVE` per Phase 279 §6 walkthrough (JPT-CLEAN-04, TST-CLEAN-01).
  3. `_queueLootboxTickets` wrapper retired — unused after Phase 276 JPT-BR-02 lands (`JackpotModule.sol:2216` was its only caller); wrapper function deleted from `JackpotModule.sol`; zero remaining `_queueLootboxTickets` invocation sites in the codebase post-v40.0 (static-analysis grep) (JPT-CLEAN-05, TST-CLEAN-02).
  4. Storage layout byte-identical at v40 phase-close HEAD vs v39 baseline `6a7455d1` for `JackpotModule.sol` (storage-slot grep proof). Bytecode delta reported in commit message — expected NET-NEGATIVE (cleanup + refactor + wrapper retirement all shrink bytecode) (JPT-CLEAN-06).
  5. Cross-surface mixing regression TST-CROSS-01: exercise same player through all 3 RNG-driven ticket-award surfaces at the same target future level — 5 manual lootbox opens (`openLootBox` + `openBurnieLootBox`) + 3 auto-resolve lootbox opens (`resolveLootboxDirect` + `resolveRedemptionLootbox`) + 2 jackpot ticket-roll awards. Assert: all 3 surface families Bernoulli-roll independently (no shared `rem` byte residue accumulation across surfaces; per-resolution whole-ticket commits). Mix mint-boost in once at the same future level — confirm only mint-boost contributions go through `_rollRemainder` at activation time per D-40N-MINTBOOST-OUT-01 (TST-CROSS-01).
  6. Wave shape: 1 USER-APPROVED batched contract commit + 1 USER-APPROVED batched test commit per the same approval discipline as Phases 275-277. Both commits land before Phase 279 terminal audit begins.

**Plans:** TBD

### Phase 279: Whole-BURNIE Floor (BUR)

**Goal:** Apply A1 floor-per-winner mechanic to all 3 RNG-influenced BURNIE-award sites per D-40N-BUR-FLOOR-01 — `LootboxModule.sol:1080` (lootbox spin BURNIE) + `JackpotModule.sol:1785/1842` (near-future coin jackpot baseAmount) + `JackpotModule.sol:1900/1922` (far-future coin jackpot perWinner). Floor `burnieAmount` at `DegenerusGameLootboxModule.sol:1080` upstream of `coinflip.creditFlip(player, burnieAmount)` (the variance-roll-derived BURNIE flows through the `burnieNoMultiplier + burniePresale + bonusBurnie` accumulator at lines ~1072-1077; floor applied to the final sum). Per-spin per-player dust loss bounded < 1 BURNIE; sub-1-BURNIE residues evaporate per D-40N-BUR-DUST-01. Floor `baseAmount = coinBudget / cap` at `DegenerusGameJackpotModule.sol:1785` to whole-BURNIE multiples; the existing `extra = coinBudget % cap` cursor-rotation `if (extra != 0 && cursor < extra) amount += 1` distribution at `:1832` retires per A1 mechanic (NOT A2 budget-floor-redistribute); when `baseAmount < 1 ether`, the full daily near-future BURNIE jackpot budget evaporates that day. Floor `perWinner = farBudget / found` at `_awardFarFutureCoinJackpot` (`:1900`) to whole-BURNIE multiples; the existing `if (perWinner == 0) return` early-bail at `:1901` extends to the post-floor zero case naturally; when `perWinner < 1 ether`, the 25% daily BURNIE budget allocated to far-future evaporates. Budget evaporation on low-pool days accepted per D-40N-BUR-DUST-01 (user disposition 2026-05-13: "sub 1 burnie amounts are economically negligible so just don't worry about it"). NO consolation, NO replacement event, NO cursor-rotation residue redistribution at any of the 3 sites per D-40N-BUR-SILENT-01. Storage layout byte-identical at v40 phase-close HEAD vs v39 baseline `6a7455d1` for both `DegenerusGameLootboxModule.sol` AND `DegenerusGameJackpotModule.sol`; zero new state variables; zero new admin entry points; zero new external mutation entry points; zero new modifiers; zero new events; zero new emit sites. Whole-BURNIE floor is a pure-amount transformation upstream of existing `coinflip.creditFlip(...)` + `coinflip.creditFlipBatch(...)` emit sites + their existing `JackpotBurnieWin` / `LootBoxOpened.burnie` / `FarFutureCoinJackpotWinner` event emissions (event-field values reflect the post-floor amount). Bytecode delta NET-NEGATIVE expected — single integer-division floor adds ~5-15 gas per site at the BURNIE-amount compute point, but eliminates fractional-wei storage in `coinflip.creditFlip` (mint amount stored at integer-1-ether boundaries; smaller calldata size on `creditFlipBatch`). Mint-boost flip-credit at `DegenerusGameMintModule.sol:1199` `coinflip.creditFlip(buyer, lootboxFlipCredit)` + daily-coinflip claim/mint at `BurnieCoinflip.sol:409/770/789` + advance bounty at `DegenerusGameAdvanceModule.sol:191/227/477/886` + quest rewards at `DegenerusQuests.sol:514/629/739/887/890/954/1885` + affiliate DGNRS deity bonus at `DegenerusGame.sol:1463` + `DegenerusAffiliate.sol:777` all explicitly EXCLUDED per D-40N-BUR-MINTBOOST-OUT-01 (deterministic on user-altered or system-deterministic inputs; not RNG-amount; out of v40.0 "RNG-driven BURNIE awards" framing). Wave shape: 1 USER-APPROVED batched contract commit `feat(279): whole-BURNIE floor at lootbox spin + near/far-future coin jackpot [BUR-01..05]` + 1 USER-APPROVED batched test commit `test(279): whole-BURNIE floor + daily-budget evaporation regression + whole-BURNIE invariant sweep [TST-BUR-01..04]` per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md`. Gas delta reported in commit messages.
**Depends on:** Nothing content-wise (independent of Phases 275-278 — touches different sites inside LootboxModule + JackpotModule). Sequences after Phase 278 for clean linear contract-mutation history (per-commit USER-APPROVAL gates serialize wall-clock execution).
**Requirements:** BUR-01, BUR-02, BUR-03, BUR-04, BUR-05, TST-BUR-01, TST-BUR-02, TST-BUR-03, TST-BUR-04
**Success Criteria** (what must be TRUE):
  1. `burnieAmount` at `DegenerusGameLootboxModule.sol:1080` floored to whole-BURNIE multiples upstream of `coinflip.creditFlip(player, burnieAmount)` invocation: `burnieAmount = (burnieAmount / 1 ether) * 1 ether`. The variance-roll-derived BURNIE amount (flowing through the `burnieNoMultiplier + burniePresale + bonusBurnie` accumulator at lines ~1072-1077) becomes whole-BURNIE only at the credit-emission point. Boundary cases verified: 0.99 BURNIE → 0; 1.99 BURNIE → 1; 2.00 BURNIE → 2; 0 BURNIE → 0 (existing `if (burnieAmount != 0)` guard preserves no-op). `LootBoxOpened.burnie` event field equals the floored amount (BUR-01, TST-BUR-01).
  2. `baseAmount = coinBudget / cap` at `DegenerusGameJackpotModule.sol:1785` floored to whole-BURNIE multiples — `baseAmount = ((coinBudget / cap) / 1 ether) * 1 ether`. The existing `extra = coinBudget % cap` cursor-rotation +1-wei distribution at `:1832` retires per A1 mechanic. When `baseAmount < 1 ether` (e.g., 50 BURNIE / 100 winners = 0.5 BURNIE/winner), all near-future-coin-jackpot winners that day receive 0 BURNIE; full daily near-future BURNIE jackpot budget evaporates. Daily-budget evaporation accepted per D-40N-BUR-DUST-01 (BUR-02, TST-BUR-02).
  3. `perWinner = farBudget / found` at `_awardFarFutureCoinJackpot` (`DegenerusGameJackpotModule.sol:1900`) floored to whole-BURNIE multiples — `perWinner = ((farBudget / found) / 1 ether) * 1 ether`. The existing `if (perWinner == 0) return` early-bail at `:1901` extends to the post-floor zero case naturally. When `perWinner < 1 ether` (e.g., 5 BURNIE / 10 winners = 0.5 BURNIE/winner), the 25% daily BURNIE budget allocated to far-future evaporates (BUR-03, TST-BUR-03).
  4. Storage layout byte-identical at v40 phase-close HEAD vs v39 baseline `6a7455d1` for both `DegenerusGameLootboxModule.sol` AND `DegenerusGameJackpotModule.sol` (storage-slot grep proof). Zero new state variables; zero new admin entry points; zero new external mutation entry points; zero new modifiers; zero new events; zero new emit sites. Existing `LootBoxOpened.burnie` + `JackpotBurnieWin.amount` + `FarFutureCoinJackpotWinner.perWinner` event fields emit post-floor amounts (BUR-04).
  5. Bytecode delta NET-NEGATIVE — single integer-division floor adds ~5-15 gas per site at the BURNIE-amount compute point; eliminates fractional-wei storage in `coinflip.creditFlip` (mint amount stored at integer-1-ether boundaries; smaller calldata on `creditFlipBatch`). Gas delta reported in commit message (BUR-05).
  6. Whole-BURNIE invariant sweep — across N representative budgets/spins on all 3 sites, every observable BURNIE-mint amount via `coinflip.creditFlip(...)` + `coinflip.creditFlipBatch(...)` from these 3 sites is a multiple of 1 ether (`mod(amount, 1 ether) == 0`). Negative cross-site assertion: mint-boost flip-credit at `DegenerusGameMintModule.sol:1199` continues emitting fractional-wei per D-40N-BUR-MINTBOOST-OUT-01 — confirms BUR scope correctly narrows to the 3 RNG-influenced sites (TST-BUR-04).
  7. Wave shape: 1 USER-APPROVED batched contract commit + 1 USER-APPROVED batched test commit per the same approval discipline as Phases 275-278. Both commits land before Phase 280 terminal audit begins.

**Plans:** TBD

### Phase 280: Delta Audit + Findings Consolidation (Terminal)

**Goal:** Publish `audit/FINDINGS-v40.0.md` as the single canonical milestone-closure deliverable per D-40N-FILES-01 carry — 9-section shape consistent with v32.0..v39.0 precedent; FINAL READ-only at v40.0 closure HEAD (`chmod 444` post-closure-flip); 5-Bucket Severity Rubric carry from v38.0 / D-08. §3.A delta-surface table covers all source-tree changes v39.0 audit-subject HEAD `6a7455d1` → v40.0 closure HEAD with hunk-level evidence + {NEW, MODIFIED_LOGIC, REFACTOR_ONLY, DELETED} classification: Phase 275 auto-resolve LootboxModule Bernoulli contract + test commits; Phase 276 JackpotModule:2216 BAF Bernoulli contract + test commits; Phase 277 Event surface unification contract + test commits; Phase 278 JackpotModule cleanup + ENT-05 xorshift refactor + `_queueLootboxTickets` retirement + cross-surface regression contract + test commits; Phase 279 Whole-BURNIE floor contract + test commits; each commit gets §3.A coverage proportional to surface change. §4 adversarial surfaces enumerated for v40.0 changes (11 surfaces per AUDIT-03 a..k): (a) EV-neutrality of Bernoulli collapse on auto-resolve paths vs cross-lootbox accumulation; (b) EV-neutrality of Bernoulli collapse on jackpot ticket-roll path; (c) Bit-slice `[152..167]` reuse on auto-resolve (independent of manual-path bits[152..167] consumption due to per-resolution-distinct seed); (d) Bit-slice `[200..215]` independence on jackpot vs existing bits[0..12] consumers; (e) Silent cold-bust gating predicate on auto-resolve + jackpot (no consolation crossover from v39 manual-path); (f) Event topic-hash change correctness (LootBoxOpened + BurnieLootOpen + JackpotTicketWin signatures + emission sites); (g) Index-sentinel retirement byte-equivalence (no behavior crossover between manual + auto-resolve post-retirement); (h) `_queueLootboxTickets` wrapper retirement + ENT-05 BAF xorshift refactor structural integrity; (i) Mint-boost path byte-equivalent at v40 HEAD (status-quo preservation per D-40N-MINTBOOST-OUT-01); (j) Lootbox spin BURNIE floor at `LootboxModule:1080` — RNG-amount-rounding invariant (per-spin floor preserves variance-roll EV-floor; no consolation; LootBoxOpened.burnie field consistency); (k) JackpotModule near-future coin jackpot (`:1842`) + far-future coin jackpot (`:1900`) BURNIE floor — daily-budget-evaporation ledger when per-winner amount < 1 BURNIE; cursor-rotation residue retirement at near-future; perWinner==0 early-bail at far-future; mint-boost flip-credit non-floor (`MintModule:1199`) status-quo preservation per D-40N-BUR-MINTBOOST-OUT-01. 3-skill PARALLEL adversarial pass on finished §4 draft per D-40N-ADVERSARIAL-01 carry: `/contract-auditor` + `/zero-day-hunter` + `/economic-analyst` spawn; `/degen-skeptic` OUT OF SCOPE per D-271-ADVERSARIAL-02 carry. Adversarial-log at `phases/280-NN/280-NN-ADVERSARIAL-LOG.md`; dispositions: zero residual FINDING_CANDIDATE OR explicit RESOLVED_AT_V40 with amendment commit reference. KI walkthrough EXC-01..04 RE_VERIFIED at v40 HEAD; EXC-04 may demote from `NARROWS` to `NEGATIVE` if Phase 278 JPT-CLEAN-04 ENT-05 BAF xorshift refactor lands and removes the narrows scope; default zero-promotion path per D-40N-KI-01 carry; closure verdict in §6b: `N of N KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_<UNMODIFIED|MODIFIED>`. LEAN regression: REG-01 (v39.0 closure signal `MILESTONE_V39_AT_HEAD_6a7455d1` re-verified NON-WIDENING at v40 HEAD; surface set: manual lootbox path (Bernoulli + consolation + LootboxTicketRoll-replaced-by-LootBoxOpened-fields); bits[152..167] manual-path slice (now shared with auto-resolve in v40 — verify per-resolution seed-uniqueness prevents observable manual-path drift); lootbox spin BURNIE site at `LootboxModule:1080` newly v40-scoped per BUR-01 — explicitly EXCLUDED from non-widening proof (in-scope mutation); byte-identical for surfaces NOT in v40 scope: Degenerette + BURNIE coinflip + mint-boost ticket queue + mint-boost flip-credit `MintModule:1199` + WWXRP consolation predicate on manual-path + advance bounty + affiliate DGNRS deity bonus + quest rewards); REG-02 (v34.0 closure signal `MILESTONE_V34_AT_HEAD_6b63f6d4daf346a53a1d463790f637308ea8d555` re-verified NON-WIDENING; TraitUtils + `_pickSoloQuadrant` + JackpotBucketLib byte-identical); REG-03 (KI envelope re-verifications at v40 HEAD — EXC-01..03 NEGATIVE-scope; EXC-04 disposition per JPT-CLEAN-04 outcome NARROWS retained OR demoted to NEGATIVE); REG-04 (prior-finding spot-check sweep across `audit/FINDINGS-v25.0..v39.0.md` for v40-touched function/surface set including the 3 BUR sites). Closure signal `MILESTONE_V40_AT_HEAD_<sha>` emitted in §9c with verbatim presence in 5 FINDINGS locations + 3 cross-document propagation targets per v39.0 P274 Task 3.10 precedent. ROADMAP + STATE + MILESTONES + PROJECT closure-flips land atomically post-§9c attestation per D-40N-CLOSURE-01 carry. §9.NN three-subsection commit-readiness register populated (i USER-APPROVED contracts: Phase 275 + 276 + 277 + 278 + 279 contract commits; ii USER-APPROVED tests: Phase 275 + 276 + 277 + 278 + 279 test commits; iii AGENT-COMMITTED audit + planning artifacts: `audit/FINDINGS-v40.0.md` + ROADMAP/STATE/MILESTONES/PROJECT/REQUIREMENTS flips). Forward-cite zero-emission at terminal phase per D-40N-FCITE-01 (carries D-NN-FCITE-01).
**Depends on:** Phase 275, Phase 276, Phase 277, Phase 278, Phase 279 (audit baseline is the post-Phase-279 HEAD — all 5 surface phases must land their contract + test commits before terminal audit begins)
**Requirements:** AUDIT-01, AUDIT-02, AUDIT-03, AUDIT-04, AUDIT-05, AUDIT-06, REG-01, REG-02, REG-03, REG-04
**Success Criteria** (what must be TRUE):
  1. `audit/FINDINGS-v40.0.md` published FINAL READ-only at v40.0 closure HEAD (`chmod 444` post-closure-flip) with 9 sections (executive summary, per-phase summaries §3a-c, F-40-NN finding blocks under D-08 5-bucket severity rubric, §3.A delta-surface table covering Phase 275 + 276 + 277 + 278 + 279 contract + test commits with hunk-level evidence + {NEW, MODIFIED_LOGIC, REFACTOR_ONLY, DELETED} classification, §3.B zero-new-state scan, §3.C conservation re-proof, regression appendix §5, KI gating walk §6, prior-artifact cross-cites §7, forward-cite zero-emission proof §8, closure attestation §9 with §9.NN three-subsection commit-readiness register); §3.A row-count covers every changed declaration grep-reproducibly (AUDIT-01, AUDIT-02).
  2. §4 adversarial sweep verdicts every identified surface SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE / FINDING_CANDIDATE with row-level evidence covering the 11 surfaces per AUDIT-03 (a EV-neutrality auto-resolve Bernoulli vs cross-lootbox accumulation + b EV-neutrality jackpot Bernoulli + c bits[152..167] reuse on auto-resolve independence + d bits[200..215] independence vs bits[0..12] on jackpot + e silent cold-bust gating predicate auto-resolve + jackpot + f event topic-hash change correctness + g index-sentinel retirement byte-equivalence + h `_queueLootboxTickets` wrapper retirement + ENT-05 BAF xorshift refactor structural integrity + i mint-boost path byte-equivalent at v40 HEAD + j lootbox spin BURNIE floor at LootboxModule:1080 RNG-amount-rounding invariant + k JackpotModule near+far-future coin jackpot BURNIE floor daily-budget-evaporation ledger) (AUDIT-03).
  3. 3-skill PARALLEL adversarial pass on finished §4 draft per D-40N-ADVERSARIAL-01 carry — `/contract-auditor` + `/zero-day-hunter` + `/economic-analyst` spawn; `/degen-skeptic` OUT OF SCOPE per D-271-ADVERSARIAL-02 carry. Adversarial-log at `phases/279-NN/279-NN-ADVERSARIAL-LOG.md` 3 H2 sections + Disposition; zero residual FINDING_CANDIDATE OR explicit RESOLVED_AT_V40 with amendment commit reference (AUDIT-04).
  4. KI walkthrough EXC-01..04 RE_VERIFIED at v40 HEAD; EXC-04 may demote from `NARROWS` to `NEGATIVE` if Phase 278 JPT-CLEAN-04 ENT-05 BAF xorshift refactor lands and removes the narrows scope; default zero-promotion path per D-40N-KI-01 carry; closure verdict in §6b: `N of N KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_<UNMODIFIED|MODIFIED>` (AUDIT-05).
  5. Closure signal `MILESTONE_V40_AT_HEAD_<sha>` emitted in §9c with verbatim presence in 5 FINDINGS locations + 3 cross-document propagation targets (ROADMAP + STATE + MILESTONES + PROJECT closure-flips atomic post-§9c attestation per D-40N-CLOSURE-01); §9.NN three-subsection commit-readiness register populated (i USER-APPROVED contracts Phase 275/276/277/278/279 + ii USER-APPROVED tests Phase 275/276/277/278/279 + iii AGENT-COMMITTED audit + planning artifacts); §8 forward-cite zero-emission grep recipe verifies zero `v41.0+` references emitted from this terminal phase per D-40N-FCITE-01 (carries D-NN-FCITE-01) (AUDIT-06).
  6. §5 regression appendix: REG-01 PASS for v39.0 closure signal `MILESTONE_V39_AT_HEAD_6a7455d1` non-widening at v40 HEAD (surface set: manual lootbox path Bernoulli + consolation + LootboxTicketRoll-replaced-by-LootBoxOpened-fields; bits[152..167] manual-path slice shared with auto-resolve in v40 with per-resolution seed-uniqueness proof; byte-identical for surfaces NOT in v40 scope — Degenerette + BURNIE coinflip + mint-boost + WWXRP consolation predicate on manual-path); REG-02 PASS for v34.0 closure signal `MILESTONE_V34_AT_HEAD_6b63f6d4daf346a53a1d463790f637308ea8d555` non-widening (TraitUtils + `_pickSoloQuadrant` + JackpotBucketLib byte-identical); REG-03 KI envelope re-verifications (EXC-01..03 NEGATIVE-scope; EXC-04 disposition per JPT-CLEAN-04 outcome); REG-04 prior-finding spot-check sweep across `audit/FINDINGS-v25.0..v39.0.md` for v40-touched function/surface set — focus: `_resolveLootboxCommon` auto-resolve branch additions + manual-path index-sentinel retirement; `_queueTickets` new auto-resolve + jackpot callsites; `_jackpotTicketRoll` Bernoulli addition; `LootBoxOpened` / `BurnieLootOpen` / `JackpotTicketWin` event signature additions; `_queueLootboxTickets` retirement; `xTICKET_SCALE` cleanup sites; ENT-05 BAF xorshift refactor (REG-01, REG-02, REG-03, REG-04).

**Plans:** TBD


<details>
<summary>✅ v39.0 Lootbox Whole-Ticket Rounding + WWXRP Consolation (Phase 274) — SHIPPED 2026-05-13</summary>

- [x] Phase 274: Lootbox Whole-Ticket Rounding + WWXRP Consolation (Terminal) (1/1 plans) — completed 2026-05-13 (closure signal `MILESTONE_V39_AT_HEAD_6a7455d1` emitted in audit/FINDINGS-v39.0.md §9c)

**Audit baseline:** v38.0 closure HEAD `MILESTONE_V38_AT_HEAD_06623edb` → v39.0 audit-subject HEAD `6a7455d1`. Single-phase milestone-closure patch mirroring v36.0 Phase 266 + v38.0 Phase 272 precedent. MANUAL LOOTBOX OPENS ONLY (`openLootBox` + `openBurnieLootBox`) — auto-resolve callers (`resolveLootboxDirect` decimator-claim + `resolveRedemptionLootbox` sDGNRS-redemption) explicitly UNCHANGED per D-274-MANUAL-ONLY-01. Wave 1 USER-APPROVED batched contract commit `c21f833a` ships `_resolveLootboxCommon` `uint48 index` parameter + manual-branch Bernoulli-collapse via `bits[152..167]` + new private constant `LOOTBOX_WWXRP_CONSOLATION = 1 ether` + new event `LootboxTicketRoll(player, lootboxIndex, preRollTickets, roundedUp)` declared on interface + contract event block + bit-allocation NatSpec update 152 → 168. D-274-BIT-SLICE-01 superseded intra-Wave-1 from 8-bit to 16-bit slice on bias quantification. Wave 2 USER-APPROVED batched test commit `f8e55cfe` ships TST-WT-01..07 + TST-WX-01..03 + TST-REG-01..04 (+1,422 LOC across 4 new test files; 74 tests; all passing). Wave 3 audit-deliverable atomic chain `386e797d` → `6a7455d1` + closure-flip commits. Phase 273 included-since-baseline (`ff929948` + `e9807891` + `e04d3333` + `1eb1ecb5`) folded into v39.0 audit baseline per D-274-BAF273-INCLUDE-01. 39/39 in-scope requirements satisfied (5 LBX-WT + 4 LBX-WX + 6 LBX-EVT + 7 TST-WT + 3 TST-WX + 4 TST-REG + 6 AUDIT + 4 REG). Result: 8 of 8 §4 adversarial surfaces SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE / SAFE_BY_DESIGN_PHASE_273; zero F-39-NN finding blocks emitted; 12 novel-vector hypotheses (i)..(t) returned 10 NEGATIVE_RESULT_ONLY + 2 ACCEPTED_DESIGN dispositions. LEAN regression: 1 PASS REG-01 (v38.0 closure NON-WIDENING for v38-touched surfaces NOT in v39 manual-lootbox scope) + 1 PASS REG-02 (v34.0 closure NON-WIDENING) + REG-03 KI envelope re-verifications + REG-04 prior-finding spot-check sweep across audit/FINDINGS-v25..v38.0. KI envelopes EXC-01..03 RE_VERIFIED-NEGATIVE-scope at v39; EXC-04 RE_VERIFIED with NARROWS retained (BAF-jackpot-only scope; EntropyLib byte-identical at v39 HEAD). KNOWN-ISSUES.md UNMODIFIED per D-274-KI-01 default zero-promotion path. Closure verdict `0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_UNMODIFIED`. Adversarial pass via 3-skill PARALLEL spawn `/contract-auditor` + `/zero-day-hunter` + `/economic-analyst` per D-274-ADVERSARIAL-01 carry. Deliverable: `audit/FINDINGS-v39.0.md` (FINAL READ-only at HEAD `6a7455d1`, 9 sections; chmod 444). See [milestones/v39.0-ROADMAP.md](milestones/v39.0-ROADMAP.md).

</details>


<details>
<summary>✅ v38.0 Always-Hero Simplification + Maximal Dead-Code Cleanup (Phase 272) — SHIPPED 2026-05-11</summary>

- [x] Phase 272: Always-Hero Simplification + Maximal Dead-Code Cleanup (Terminal) (1/1 plans) — completed 2026-05-11 (3 USER-APPROVED commits: Wave 1 `527e3adc` + Wave 1.5 `4760459f` + Wave 2 `e3fcb95c`; closure signal `MILESTONE_V38_AT_HEAD_06623edb` emitted in audit/FINDINGS-v38.0.md §9c)

**Audit baseline:** v37.0 closure HEAD `MILESTONE_V37_AT_HEAD_2654fcc2` → v38.0 audit-subject HEAD `06623edb`. 2 contract-tree commits since baseline (`527e3adc` Phase 272 Wave 1 silent-normalize HERO-01..05 + CLEAN-01..05 + `4760459f` Phase 272 Wave 1.5 input-validation revision per D-272-INPUT-VALIDATION-01; bytecode delta −57 bytes 8955 → 8898; storage layout byte-identical; public ABI signature byte-identical with semantics shift accept+normalize → revert+InvalidBet) + 1 batched test-tree commit (`e3fcb95c` Phase 272 Wave 2; +238/−36 LOC across 6 files in `test/stat/`, `test/gas/`, `package.json` — STAT-01..02 hero-always-on re-validation + SURF-01..03 cross-surface preservation with SURF-03 rebase to `PHASE_269_CLOSE_BASELINE = "8fd5c2e1..."` + LBX-02 path-of-investigation prose + GASPIN-02 (a-alt) `test:gas` script split + STAT-03-v35-carry ACCEPTED-DESIGN documentation). Single-phase milestone shape per v36.0 Phase 266 precedent. 29/30 in-scope requirements satisfied (5 HERO + 6 CLEAN + 2 STAT + 3 SURF + 2 GASPIN + 1 STAT-03-v35-carry + 6 AUDIT + 4 REG) + 1 RE-DEFERRED-V39+ (LBX-02 — fixture-coverage gap persists; analytical worst-case load-bearing per Phase 266 GAS-01 precedent + `feedback_gas_worst_case.md`; path-of-investigation prose at `audit/FINDINGS-v38.0.md` §9.NN.iv). Result: 7 of 7 §4 adversarial surfaces SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE / SAFE_BY_DEFENSIVE_VALIDATION (a EV-neutrality preserved across (M, N) under always-on hero + b quadrant 0 default does NOT create payout-bias under defensive boundary validation + c cleanup-removal invariants preserved per `feedback_no_dead_guards.md` + d storage layout byte-identical + e public ABI byte-identical with semantics shift + f variance-impact bound on risk-averse subset + g clean test:stat at v38 close); zero F-38-NN finding blocks emitted; 1 INFO-severity docs-vs-behavior drift identified at Hypothesis (i) (`dailyHeroWagers` post-v38 ledger-tracking on `0xFF` input) at Wave 3 3-skill PARALLEL adversarial pass — RESOLVED_AT_V38 via Wave 1.5 commit `4760459f` defensive boundary validation per D-272-INPUT-VALIDATION-01 (status pivoted from KEEP_AS_NEGATIVE_FINDING to RESOLVED_AT_V38). LEAN regression: 1 PASS REG-01 (v37.0 closure NON-WIDENING) + 1 PASS REG-02 (v34.0 closure NON-WIDENING) + REG-03 KI envelope re-verifications + REG-04 prior-finding spot-check sweep across audit/FINDINGS-v25..v37.0. KI envelopes EXC-01..03 RE_VERIFIED-NEGATIVE-scope at v38; EXC-04 RE_VERIFIED with NARROWS retained (BAF-jackpot-only scope; EntropyLib byte-identical at v38 HEAD). KNOWN-ISSUES.md UNMODIFIED per D-272-KI-01 default zero-promotion path. Closure verdict `0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_UNMODIFIED`. Adversarial pass via `/contract-auditor` + `/zero-day-hunter` + `/economic-analyst` PARALLEL spawn per D-271-ADVERSARIAL-01 carry (`/degen-skeptic` OUT OF SCOPE per D-271-ADVERSARIAL-02 carry) returned ZERO disagreements; ZERO FINDING_CANDIDATE; ZERO 8th-surface NEW_VECTOR; ZERO KI promotion candidate per `272-01-ADVERSARIAL-LOG.md` Disposition (with Wave 1.5 disposition update appended at commit `1249a6fd`). Deliverable: `audit/FINDINGS-v38.0.md` (FINAL READ-only at HEAD `MILESTONE_V38_AT_HEAD_06623edb`, 9 sections, chmod 444). See [milestones/v38.0-ROADMAP.md](milestones/v38.0-ROADMAP.md) and [milestones/v38.0-REQUIREMENTS.md](milestones/v38.0-REQUIREMENTS.md) and [milestones/v38.0-MILESTONE-AUDIT.md](milestones/v38.0-MILESTONE-AUDIT.md).

</details>


<details>
<summary>✅ v37.0 Degenerette Recalibration + Maintenance Bundle (Phases 267-271) — SHIPPED 2026-05-11</summary>

- [x] Phase 267: Degenerette Producer + 5-Table Payout Rewrite (1/1 plans) — completed 2026-05-10 (USER-APPROVED contract commit `e1136071` — `feat(267): degenerette producer + 5-table payout rewrite + 3-tier ETH split [DGN-01..15, PAY-SPLIT-01..03]`)
- [x] Phase 268: Degenerette Statistical Validation + Cross-Surface Preservation (1/1 plans) — completed 2026-05-11 (USER-APPROVED test commit `4b277aaf` — `test(268): degenerette stat suite + cross-surface preservation v37.0 + worst-case gas regression [STAT-01..07, SURF-01..06]`)
- [x] Phase 269: Lootbox Dead-Branch Cleanup + SURF-05 Gas-Pin Re-Pinning (1/1 plans) — completed 2026-05-11 (USER-APPROVED contract commit `8fd5c2e1` — `feat(269): delete unreachable BURNIE-conversion branch in _resolveLootboxRoll [LBX-01]`; PARTIAL ship — 4 deferred to v38+)
- [x] Phase 270: Post-v32.0 Deferred-Commit Adversarial Sub-Audit (1/1 plans) — completed 2026-05-11 (AGENT-COMMITTED working-file `4017b9ec` + phase-close `5cd4f2bc`)
- [x] Phase 271: Delta Audit + Findings Consolidation (Terminal) (1/1 plans) — completed 2026-05-11 (closure signal `MILESTONE_V37_AT_HEAD_2654fcc2` emitted in audit/FINDINGS-v37.0.md §9c)

**Audit baseline:** v36.0 closure HEAD `1c0f09132d7439af9881c56fe197f81757f8164a` → v37.0 audit-subject HEAD `2654fcc2` (post-Task-12 §9 attestation commit). 2 contract-tree commits since baseline (`e1136071` Phase 267 Degenerette producer + 5-table payout rewrite + 3-tier ETH split + `8fd5c2e1` Phase 269 LBX-01 dead-branch deletion; bytecode shrink 177 bytes 18,330 → 18,153) + 1 batched test-tree commit (`4b277aaf` Phase 268; +2,277/−1 LOC across 6 files). 5-phase milestone shape: Phases 267 (contracts) + 268 (tests) + 269 (LBX cleanup PARTIAL ship) + 270 (post-v32.0 sub-audit; zero source-tree mutations) + 271 (delta audit terminal). 48/48 in-scope requirements satisfied (15 DGN + 3 PAY-SPLIT + 7 STAT + 6 SURF + LBX-01 + LBX-03 + GASPIN-01 + 4 DELTA + 6 AUDIT + 4 REG) + 3 DEFERRED-V38+ per D-271-DEFERRED-02 (LBX-02 + GASPIN-02 + GASPIN-03). Result: 8 of 8 §4 adversarial surfaces SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE (a per-N table dispatch + b symbol-only hero match + c `_countGoldQuadrants` boundary + d producer byte-layout + e WWXRP × hero composition + f lootbox dead-branch byte-equivalence + g hero × per-N skill-expression carry + h ETH PAY-SPLIT 3-tier boundary-gaming v37-NEW); zero F-37-NN finding blocks emitted; 1 PASS REG-01 + 1 PASS REG-02 + 5 PASS + 1 SUPERSEDED REG-04; `0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_UNMODIFIED` per D-271-PAYSPLIT-01 + D-271-KI-01 default zero-promotion path (PAY-SPLIT 3.0× bet boundary discontinuity documented via §4 (h) prose-only attestation per /economic-analyst mechanism-design assessment). 4 RE_VERIFIED KI envelope rows (EXC-01..03 NEGATIVE-scope at v37; EXC-04 NARROWS retained — BAF-jackpot-only scope). Adversarial pass via `/contract-auditor` + `/zero-day-hunter` + `/economic-analyst` PARALLEL spawn per D-271-ADVERSARIAL-01 (`/degen-skeptic` OUT OF SCOPE per D-271-ADVERSARIAL-02) returned ZERO disagreements; zero FINDING_CANDIDATE / zero 9th-surface NEW_VECTOR / zero KI promotion candidate per `271-01-ADVERSARIAL-LOG.md` Disposition. Deliverable: `audit/FINDINGS-v37.0.md` (FINAL READ-only at HEAD `MILESTONE_V37_AT_HEAD_2654fcc2`, 9 sections, chmod 444). See [milestones/v37.0-ROADMAP.md](milestones/v37.0-ROADMAP.md) and [milestones/v37.0-REQUIREMENTS.md](milestones/v37.0-REQUIREMENTS.md).

</details>

## Progress

| Phase | Plans Complete | Status | Completed |
|-------|----------------|--------|-----------|
| 275. Auto-Resolve LootboxModule Bernoulli (LBX-AR) | 2/2 | Complete   | 2026-05-13 |
| 276. JackpotModule:2216 BAF Bernoulli (JPT-BR) | 0/0 | Not started | - |
| 277. Event Surface Unification + Sentinel Retirement (EVT-UNI) | 0/0 | Not started | - |
| 278. JackpotModule Cleanup + ENT-05 BAF Xorshift Refactor + Wrapper Retirement (JPT-CLEAN) | 0/0 | Not started | - |
| 279. Whole-BURNIE Floor (BUR) | 0/0 | Not started | - |
| 280. Delta Audit + Findings Consolidation (Terminal) | 0/0 | Not started | - |
