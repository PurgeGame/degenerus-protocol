# Roadmap: Degenerus Protocol Audit

## Milestones

- ✅ **v1.0 Initial RNG Security Audit** — Phases 1-5 (shipped 2026-03-14)
- ✅ **v2.0 Adversarial Audit** — Phases 6-18 (shipped 2026-03-17)
- ✅ **v3.0-v24.1** — Phases 19-212 (shipped 2026-04-10)
- ✅ **v25.0 Full Audit (Post-v5.0 Delta + Fresh RNG)** — Phases 213-217 (shipped 2026-04-11)
- ✅ **v26.0 Bonus Jackpot Split** — Phases 218-219 (shipped 2026-04-12)
- ✅ **v27.0 Call-Site Integrity Audit** — Phases 220-223 (shipped 2026-04-13)
- ✅ **v28.0 Database & API Intent Alignment Audit** — Phases 224-229 (shipped 2026-04-15) — see [milestones/v28.0-ROADMAP.md](milestones/v28.0-ROADMAP.md)
- ✅ **v29.0 Post-v27 Contract Delta Audit** — Phases 230-236 (shipped 2026-04-18) — see [milestones/v29.0-ROADMAP.md](milestones/v29.0-ROADMAP.md)
- ✅ **v30.0 Full Fresh-Eyes VRF Consumer Determinism Audit** — Phases 237-242 (shipped 2026-04-20) — see [milestones/v30.0-ROADMAP.md](milestones/v30.0-ROADMAP.md)
- ✅ **v31.0 Post-v30 Delta Audit + Gameover Edge-Case Re-Audit** — Phases 243-246 (shipped 2026-04-24) — see [milestones/v31.0-ROADMAP.md](milestones/v31.0-ROADMAP.md)
- ✅ **v32.0 Backfill Idempotency + purchaseLevel Underflow Audit** — Phases 247-253 (shipped 2026-05-02) — see [milestones/v32.0-ROADMAP.md](milestones/v32.0-ROADMAP.md)
- ✅ **v33.0 Charity Allowlist Governance** — Phases 254-258 (shipped 2026-05-07; closure signal `MILESTONE_V33_AT_HEAD_4ce3703d740d3707c88a1af595618120a8168399` supersedes `MILESTONE_V33_AT_HEAD_dcb70941`) — see [milestones/v33.0-ROADMAP.md](milestones/v33.0-ROADMAP.md)
- ✅ **v34.0 Trait Rarity Rework + Gold Solo Priority** — Phases 259-262 (shipped 2026-05-09; closure signal `MILESTONE_V34_AT_HEAD_6b63f6d4daf346a53a1d463790f637308ea8d555`) — see [milestones/v34.0-ROADMAP.md](milestones/v34.0-ROADMAP.md)
- ✅ **v35.0 BURNIE Near-Future Per-Pull Level Resample** — Phases 263-265 (shipped 2026-05-09; closure signal `MILESTONE_V35_AT_HEAD_5db8682bd7b811437f0c1cf47e832619d1478ac6`)
- ✅ **v36.0 Lootbox-Path Entropy Refactor** — Phase 266 (shipped 2026-05-10; closure signal `MILESTONE_V36_AT_HEAD_1c0f09132d7439af9881c56fe197f81757f8164a`) — see [milestones/v36.0-ROADMAP.md](milestones/v36.0-ROADMAP.md)
- ✅ **v37.0 Degenerette Recalibration + Maintenance Bundle** — Phases 267-271 (shipped 2026-05-11; closure signal `MILESTONE_V37_AT_HEAD_2654fcc2`) — see [milestones/v37.0-ROADMAP.md](milestones/v37.0-ROADMAP.md)
- ✅ **v38.0 Always-Hero Simplification + Maximal Dead-Code Cleanup** — Phase 272 (shipped 2026-05-11; closure signal `MILESTONE_V38_AT_HEAD_06623edb`) — see [milestones/v38.0-ROADMAP.md](milestones/v38.0-ROADMAP.md)
- ✅ **v39.0 Lootbox Whole-Ticket Rounding + WWXRP Consolation** — Phase 274 (shipped 2026-05-13; closure signal `MILESTONE_V39_AT_HEAD_6a7455d1`) — see [milestones/v39.0-ROADMAP.md](milestones/v39.0-ROADMAP.md)
- ✅ **v40.0 Unified Whole-Ticket Award Protocol + Whole-BURNIE Floor** — Phases 275-280 (shipped 2026-05-14; closure signal `MILESTONE_V40_AT_HEAD_cd549499`) — see [milestones/v40.0-ROADMAP.md](milestones/v40.0-ROADMAP.md)
- ⏳ **v41.0 Mint-Batch Cross-Call Determinism Fix** — Phases 281-284 (active; audit baseline `MILESTONE_V40_AT_HEAD_cd549499`)

## Phases

- [ ] **Phase 281: Mint-Batch Determinism Fix (FIX)** — Contract-side fix-shape decision (`D-41N-FIX-SHAPE-01` choosing one of (a) persist `processed` in storage per `(rk, player)` — widen `ticketsOwedPacked` packed-form OR sibling map; (b) store `initialOwed` at queue-time; derive `processed = initialOwed - currentOwed`; (c) mix per-call nonce into `baseKey` — block number, cursor distance) recorded BEFORE the contract patch lands per `feedback_design_intent_before_deletion.md` + FIX-05. Patches `contracts/modules/DegenerusGameMintModule.sol` `processFutureTicketBatch` (L385-L532) so the local `uint32 processed` (L419) is no longer load-bearing for cross-call seed-distinctness — within-player progress persists across `writesBudget`-bounded yields so multi-call drains produce trait sequences byte-identical to a hypothetical single-call drain. `processFutureTicketBatch(uint24, uint256)` public ABI selector + parameter types + return shape byte-identical post-fix; `TicketsCredited` + `TraitsGenerated` + `TicketsQueued` event topic hashes preserved; `TraitsGenerated.startIndex` semantic shift documented (the field's MEANING changes from `0-on-resumption` to within-player monotonic cursor). Shape (a)/(b) may add/widen a storage slot — the ONLY allowed storage-layout impact, attested via storage-slot grep proof in AUDIT-04. Shape (c) consumes a NEW entropy slice and must attest non-collision with existing consumers (bits[0..12] jackpot path-select, bits[152..167] manual+auto-resolve Bernoulli, bits[200..215] jackpot Bernoulli) — AUDIT-03 §3.C conservation re-proof. `_raritySymbolBatch` LCG + keccak-per-group-of-16 algorithm byte-equivalent post-fix; only its `startIndex` input changes. Day-boundary behavior (cross-day VRF rotation) verified preserved (not changed). One USER-APPROVED batched contract commit per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md` (decision-anchor doc + patch land in the same approval round; if shape choice changes mid-implementation the decision anchor updates intra-phase before commit). Bytecode delta + storage-slot grep proof + worst-case gas benchmark reported in commit message per `feedback_gas_worst_case.md`.
- [ ] **Phase 282: Multi-Call Drain Regression Fixture (TST-FIX)** — Test wave covering the cross-call-equivalence-to-single-call invariant the fix establishes. Multi-call drain trait-byte-identity (TST-FIX-01); `TraitsGenerated.startIndex` cross-call monotonicity (TST-FIX-02); distinct keccak seeds per call (TST-FIX-03); single-call drain byte-identity preserved against v40-baseline (TST-FIX-04); shape-dependent storage layout regression (TST-FIX-05 — shape (a) re-validates `ticketsOwedPacked` 8/24/8+ bit split; shape (b) attests new `initialOwed[rk][player]` slot writes at all queue-time callsites; shape (c) attests zero storage delta and gas regression instead); **on-chain anchor replay regression (TST-FIX-06) — fixture replays the PRODUCTION_REPLAYABLE scenario: `player @ queueIdx=6` with `~5840 owed` (= 19×292 + 119+); daily entropy `2f02…`; multi-call drain spanning ~20 calls; pre-fix reproduces the 20 identical 292-trait sequences witnessed at blocks 10862393..10862412; post-fix produces 20 distinct trait sequences whose concatenation matches a single-call drain.** TST-FIX-06 is the load-bearing pre-deploy detection artifact that AUDIT-02 cites in the F-41-01 finding block. One USER-APPROVED batched test commit per the same approval discipline as Phase 281. Tests added under `test/edge/` or `test/mint/` (final directory decided at plan-phase). Test count + LOC delta + runtime delta reported in commit message.
- [ ] **Phase 283: Cross-Surface Batched-Loop Sweep (SWEEP)** — Analytical pass enumerating all batched / cursor-based loops in `contracts/modules/` and attesting cross-call seed-distinctness per surface. Candidate surfaces (non-exhaustive — plan-phase enumerates the full list and writes the SWEEP-LOG.md artifact): `DegenerusGameMintModule.processFutureTicketBatch` (CONFIRMED DEFECT — already patched in Phase 281; included for completeness in the SWEEP-LOG attestation row); `DegenerusGameLootboxModule._resolveLootboxCommon` + auto-resolve callers (`resolveLootboxDirect` + `resolveRedemptionLootbox`); `JackpotModule._awardDailyCoinToTraitWinners` + `_awardFarFutureCoinJackpot` + `_jackpotTicketRoll` 2-roll pattern + `_awardJackpotTickets`; `BurnieCoinflip` claim path + daily resolution loops; `DegenerusGameAdvanceModule` advance-game bounty loops; `DegenerusQuests` quest-reward batched loops; `DegenerusGameDegeneretteModule.advance` if present. Each loop catalogued with a 3-question attestation (SWEEP-02): (i) does a within-iteration counter analogous to `processed` ever exit on the stack without storage persistence? (ii) does a per-call resumption regenerate identical RNG inputs (`baseKey`, `entropyWord`, `groupIdx`-equivalent)? (iii) is there a `writesBudget`-equivalent cooperative-yield mechanism that could split a single conceptual operation across calls? Three-no answer = SAFE-BY-STRUCTURE. Specific re-attestations: SWEEP-03 lootbox queue-advance cross-call seed-distinctness (`bits[152..167]` per-resolution seed via `keccak(rngWord, player)` chain at v40 close); SWEEP-04 jackpot ticket-award batched-loop cross-call distinctness (`bits[200..215]` per-roll entropy via `EntropyLib.entropyStep` between the 2-roll pattern at L2157/L2166; cross-`advanceGame` cooperative-yield Q answered yes/no at sweep execution). Default outcome: 0 additional F-41-NN findings (mint-batch is the only known defect); placeholder TST-SWEEP-01 covers any new sweep-derived finding's cross-call seed-distinct regression (count expands or zero-outs at plan-phase per SWEEP-05). SWEEP outcome recorded in `audit/FINDINGS-v41.0.md` §4 (any new F-41-NN finding) and §5 (sweep methodology + negative-result attestations). If SWEEP-05 surfaces any F-41-NN beyond F-41-01, contract+test commits for the remediation REQUIRE USER APPROVAL per the same batched-approval discipline as Phase 281 (no pre-approval per `feedback_never_preapprove_contracts.md`). Default zero-mutation outcome ships zero new contract commits.
- [ ] **Phase 285: Day-Index Read Audit + Hero-Override Fix (HOFIX)** — Audit ALL day-keyed storage reads in `contracts/modules/` + `contracts/` to identify surfaces where a read on day D returns current-day-keyed data when design intent says it should return previous-day's data (intent/implementation gap analogous to F-41-02 hero-override defect surfaced by Phase 284 3-skill PARALLEL adversarial pass — all 3 skills converged on HIGH severity; CRITICAL elevation on `isFinalPhysicalDay_` where `dailyBps=10_000`). Per identified surface, lock a fix-shape decision (`D-285-FIX-SHAPE-NN` — read-side `-N` offset OR write-side `+N` offset) BEFORE patching per `feedback_design_intent_before_deletion.md`. Default scope: F-41-02 hero-override fix at one of two callsites — `_topHeroSymbol(_simulatedDayIndex())` at `JackpotModule:1595` (Approach A read-side `-1`) OR `day = _simulatedDayIndex()` at `DegenerusGameDegeneretteModule:486` (Approach B write-side `+1`). Expanded scope: any analogous surface surfaced by HOFIX-AUDIT (FIX-HOFIX-SWEEP-NN requirements added at plan-phase per audit results). F-41-02 root cause: `_applyHeroOverride` (called from `_rollWinningTraits`) reads `dailyHeroWagers[_simulatedDayIndex()]` LIVE on each invocation; any bet placed on day D before the jackpot fires AND any bet placed between CALL 1 and CALL 2 of the daily-jackpot 2-call ETH split mutates the slot the jackpot reads. Same-day pre-jackpot exploit: bet on day D before today's jackpot biases today's hero override (contradicts user design intent that bets affect NEXT day's jackpot, not current). 2-call split exploit: attacker bets between CALL 1 and CALL 2 (bet gate `lootboxRngWordByIndex[currentIndex] == 0` OPEN during the window per `AdvanceModule:1112-1223` + `DegenerusGameDegeneretteModule:452` trace); divergent `_topHeroSymbol(D)` → divergent `traitIds[heroQuadrant]` → divergent `_pickSoloQuadrant` → divergent `effectiveEntropy` → divergent `bucketCounts` → divergent `order` → divergent `call1Bucket` mask → disjoint-bucket-subset structural invariant (Phase 283 SWEEP-04) BREAKS → bucket double-pay or skipped. Fix (either approach): jackpot reads `dailyHeroWagers[D-1]` (frozen historical slot) regardless of intra-day bet activity. Audit scope (HOFIX-AUDIT-01..05) covers (i) every `mapping(uint32 day => ...)` storage map in contract surface (`dailyHeroWagers`, `rngWordByDay`, `coinflipBalance`, `coinflipDayResult`, `coinflipTopByDay`, etc.); (ii) every `_simulatedDayIndex()` callsite (~18 enumerated by grep across BoonModule + DecimatorModule + WhaleModule + MintModule + LootboxModule + JackpotModule + AdvanceModule + GameStorage); (iii) per surface, design-intent question "does day D's read expect day D's or day D-N's data?"; (iv) per surface, implementation-vs-intent verdict (MATCH / GAP / TBD); (v) per GAP, fix-shape decision anchor. Wave shape (default 1-surface outcome): 1 USER-APPROVED batched contract commit `feat(285): hero-override day-index fix [FIX-HOFIX-01..NN]` + 1 AGENT-COMMITTED HOFIX-AUDIT.md artifact (planner-local). Multi-surface expanded outcome: N USER-APPROVED batched contract commits per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md`. Storage byte-identical (both fix shapes are arithmetic-offset only; zero new storage, zero new SSTORE, zero new SLOAD). Public ABI byte-identical. Event topic hashes preserved. Pre-existing-bug attribution: both ingredients (`resumeEthPool` 2-call split + `_applyHeroOverride`) have been in code since Phase 195-200; bug went undetected through v32-v40 audits because prior scopes were narrow per-surface and cross-call cooperative-yield invariants weren't a systematic audit rubric until Phase 283 SWEEP introduced 3-Q methodology. Pre-launch posture: zero realized capital loss; fix lands before mainnet activation per audit chain design.
- [ ] **Phase 286: Hero-Override Day-Index Regression Fixture (TST-HOFIX)** — Test wave covering the post-fix invariants from Phase 285. TST-HOFIX-01: bets placed on day D do NOT affect day D's jackpot (game-mechanic correctness — slot[D-1] is frozen). TST-HOFIX-02: bets placed on day D DO affect day D+1's jackpot (user design intent: "top symbol bet in degenerette in the previous day is guaranteed to win the next day's jackpot"). TST-HOFIX-03: 2-call ETH split read consistency under arbitrary inter-call bet interleaving — fixture executes CALL 1 → places `placeDegeneretteBet` from independent EOA → executes CALL 2; asserts `_topHeroSymbol(D-1)` returns identical result in both calls; asserts `call1Bucket` mask identical; asserts disjoint-subset invariant holds (no bucket double-pay or skip). TST-HOFIX-04: F-41-02 anchor-replay regression — fixture simulates production attack flow (3-tx atomic sequence: advanceGame → placeDegeneretteBet → advanceGame); pre-fix produces divergent bucket mask; post-fix produces identical mask. TST-HOFIX-05: per Phase 285 HOFIX-AUDIT, regression test per additional FIX-HOFIX-SWEEP-NN surface if any (count expands at plan-phase per HOFIX-AUDIT verdict). One USER-APPROVED batched test commit per same approval discipline as Phase 282. Tests under `test/edge/` (matches Phase 282 `MintBatchDeterminism.test.js` convention). Test count + LOC delta + runtime delta reported in commit message.
- [ ] **Phase 284: Delta Audit + Findings Consolidation (Terminal)** — Single `audit/FINDINGS-v41.0.md` 9-section deliverable per D-NN-FILES-01 carry → D-41N-FILES-01; FINAL READ-only at v41.0 closure HEAD (`chmod 444`); 5-Bucket Severity Rubric carry from D-08. **First multi-finding milestone in v25..v41 audit history — §4 contains TWO non-zero finding blocks: F-41-01 (mint-batch determinism defect; severity HIGH per D-281-SEVERITY-01; status PRODUCTION_REPLAYABLE evidence-class ALGORITHM_VERIFIED per Phase 282 reduced-scope; disposition RESOLVED_AT_V41 via Phase 281 owed-salt fix + Phase 282 TST-FIX coverage) AND F-41-02 (hero-override day-index defect surfaced by Phase 284 3-skill PARALLEL adversarial pass on Phase 283 SWEEP-04 carry-forward observation; severity HIGH; status PRODUCTION_OPEN at original audit pass → RESOLVED_AT_V41 after Phase 285 fix + Phase 286 tests land; citation chain `D-285-FIX-SHAPE-01` + Phase 285 chosen-shape contract commit + Phase 286 TST-HOFIX coverage).** §3.A delta-surface table covers all v40→v41 audit-subject commits (Phase 281 contract + Phase 282 tests + any Phase 283 sweep-derived remediations). §3.B zero-new-state grep-proof (shape (a)/(b) adds a storage slot — attested; shape (c) adds zero storage; ALL shapes attest zero new public/external mutation entry points + zero new admin + zero new modifiers + zero new upgrade hooks). §3.C conservation re-proof (total-traits-credited invariant + bit-slice independence + storage byte-identity for non-mutated slots). §4 adversarial surfaces enumerated; 3-skill PARALLEL adversarial pass `/contract-auditor` + `/zero-day-hunter` + `/economic-analyst` per D-271-ADVERSARIAL-01 + D-271-ADVERSARIAL-03 carry; `/degen-skeptic` OUT OF SCOPE per D-271-ADVERSARIAL-02 carry. Required adversarial hypothesis surface: (i) does the fix re-introduce a different determinism break? (ii) does shape (c) per-call-nonce open MEV / griefing on the trait-distribution? (iii) does shape (a) wider packed form change `_rollRemainder` interaction semantics for mint-boost paths? (iv) does shape (b) `initialOwed` storage open any griefing where a queue-time write could be manipulated? KI walkthrough EXC-01..03 RE_VERIFIED-NEGATIVE-scope at v41 (the v41 audit subject has zero affiliate-roll / AdvanceModule / gameover-RNG-substitution interaction); EXC-04 stays STRUCTURALLY ELIMINATED (no resurrection at v41 — `EntropyLib.entropyStep` was deleted at v40.0 Phase 278 `8a81a87c` and is not reintroduced). KNOWN-ISSUES.md disposition recorded as `D-41N-KI-01` (default: record realized v41-historical miscount as a HISTORICAL closed entry citing blocks 10862393..10862412 + RESOLVED_AT_V41 disposition + `D-41N-FIX-SHAPE-01`; alternative: defer to launch posture review and record only in §9 closure prose — decision locked at plan-phase). LEAN regression: REG-01 (v40.0 closure signal `MILESTONE_V40_AT_HEAD_cd549499` NON-WIDENING for v40-touched surfaces NOT in v41 scope — LootboxModule Bernoulli + WWXRP consolation + JackpotModule:2216 Bernoulli + `_jackpotTicketRoll` keccak self-mix + `LootBoxOpened`/`BurnieLootOpen`/`JackpotTicketWin` event shapes + whole-BURNIE floor at the 3 RNG-amount sites byte-identical at v41 close HEAD) + REG-02 (v34.0 closure NON-WIDENING — TraitUtils + `_pickSoloQuadrant` + JackpotBucketLib byte-identical) + REG-03 (KI envelope re-verifications) + REG-04 (prior-finding spot-check sweep across `audit/FINDINGS-v25..v40.0.md` for v41-touched surface set — `DegenerusGameMintModule.processFutureTicketBatch` + `_raritySymbolBatch` + any sweep-derived surfaces). §9 closure-verdict math exercises non-zero F-NN path for the first time in audit history: `1 of 1 F-41-NN RESOLVED_AT_V41; 0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_<MODIFIED|UNMODIFIED>` (KI status per AUDIT-08). Closure signal `MILESTONE_V41_AT_HEAD_<sha>` emitted in §9c with verbatim presence in 5 FINDINGS locations + 3 cross-document propagation targets. Forward-cite zero-emission at terminal phase per D-NN-FCITE-01 carry → D-41N-FCITE-01 (no `v42.0+` references emitted from this terminal phase). SOURCE-TREE FROZEN at terminal — zero contracts/ + zero test/ mutations during this phase; audit deliverable + KNOWN-ISSUES.md edit (if D-41N-KI-01 records the historical entry) + closure-flip docs AGENT-COMMITTED.

(Prior milestone phases archived in the collapsible blocks below.)

## Phase Details

### Phase 281: Mint-Batch Determinism Fix (FIX)

**Goal:** Eliminate the within-player determinism defect in `contracts/modules/DegenerusGameMintModule.sol` `processFutureTicketBatch` (L385-L532) by recording a fix-shape decision (`D-41N-FIX-SHAPE-01` — one of (a) persist `processed` in storage per `(rk, player)`; (b) store `initialOwed` at queue-time and derive `processed = initialOwed - currentOwed`; (c) mix per-call nonce into `baseKey`) BEFORE the contract patch lands, then landing the patch so within-player progress persists across `writesBudget`-bounded yields. The local `uint32 processed` at L419 lives only on the stack today, so when a single player's `owed` count exceeds `writesBudget`, subsequent calls regenerate identical `keccak256(baseKey, entropyWord, groupIdx)` seeds and emit identical trait sequences (production replay: blocks 10862393..10862412, 20 identical 292-trait `TraitsGenerated` events). Post-fix, multi-call drains produce trait sequences byte-identical to a hypothetical single-call drain. Fix-shape decision-intent trace per FIX-05 + `feedback_design_intent_before_deletion.md` covers: (i) original 40-bit `ticketsOwedPacked` packing rationale; (ii) why `processed` was placed on stack vs storage; (iii) game-theory implications of each shape (does shape (c)'s per-call nonce open MEV / griefing on trait-distribution where a transaction-ordering attacker could force a specific trait by selecting block number? does shape (a)'s wider packed form change `_rollRemainder` semantics for mint-boost? does shape (b)'s `initialOwed` storage open griefing where a queue-time write could be manipulated?); (iv) gas-cost comparison at queue-time + per-call. Decision rationale committed to plan artifact. Public ABI byte-identical: `processFutureTicketBatch(uint24, uint256) → (bool, bool, uint32)` selector + parameter types + return shape unchanged. Event topic hashes preserved for `TicketsCredited` + `TraitsGenerated` + `TicketsQueued`. `TraitsGenerated.startIndex` field's MEANING shifts from `0-on-resumption` to within-player monotonic cursor (resets to 0 only on the `remainingOwed == 0` → next-player transition); ABI shape unchanged. Storage-layout impact bounded to the chosen shape: (a) widens `ticketsOwedPacked[rk][player]` beyond 40 bits OR adds a sibling map (preserves low 8 bits `rem` + next 24 bits `owed`; adds high 8+ bits `processed`); (b) adds an `initialOwed[rk][player]` slot at queue-time on the existing 4-5 queue callsites; (c) adds zero storage. Bit-slice non-collision (shape (c) only) attested: per-call nonce mixes into `baseKey` with no overlap into bits[0..12] jackpot path-select / bits[152..167] manual+auto-resolve Bernoulli / bits[200..215] jackpot Bernoulli. `_raritySymbolBatch` LCG + keccak-per-group-of-16 algorithm byte-equivalent post-fix; only its `startIndex` input changes. Day-boundary behavior verified preserved — daily VRF (`entropy`) rotates per day so cross-day multi-call drains already get fresh entropy (which would IN-SCOPE de-collide the bug); the fix targets the within-day multi-call case while leaving cross-day behavior byte-identical. Mint-boost fractional path (`_queueTicketsScaled` + `_rollRemainder` + `rem` byte) STAYS per `D-40N-MINTBOOST-OUT-01` carry — v41.0 fix is to `processFutureTicketBatch`, not upstream queuing. Wave shape: 1 USER-APPROVED batched contract commit `feat(281): mint-batch processed persistence + fix-shape decision anchor [FIX-01..05]` per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md`. Bytecode delta + storage-slot grep proof (byte-identical for non-mutated slots vs v40.0 closure HEAD `cd549499`; only the chosen-shape slot delta visible) + worst-case gas benchmark per `feedback_gas_worst_case.md` reported in commit message.
**Depends on:** Nothing (first v41.0 phase; baseline v40.0 closure HEAD `MILESTONE_V40_AT_HEAD_cd549499`)
**Requirements:** FIX-01, FIX-02, FIX-03, FIX-04, FIX-05
**Success Criteria** (what must be TRUE):

  1. `D-41N-FIX-SHAPE-01` decision anchor (one of (a)/(b)/(c)) recorded in the plan artifact BEFORE the contract patch commits, with design-intent trace covering original packing rationale + stack-vs-storage rationale + per-shape game-theory implications + per-shape gas comparison per FIX-05 + `feedback_design_intent_before_deletion.md` (FIX-05).
  2. `processFutureTicketBatch` (L385-L532) produces a trait sequence byte-identical to a hypothetical single-call drain when a single player's `owed` count requires N ≥ 2 calls to fully drain — for any `(rk, player)` pair where `owed > writesBudget / 2` at queue-time, the multiset of traits credited across N consecutive invocations equals the multiset a single-call drain (hypothetical infinite `writesBudget`) would have produced. Within-player progress persists across calls (FIX-01).
  3. `TraitsGenerated.startIndex` reflects within-player progress across calls — the emitted `startIndex` sequence is monotonically non-decreasing within a player's batch run and resets to 0 only on the `remainingOwed == 0` → next-player transition. Post-fix, an indexer can reconstruct the exact trait-sequence position from `(level, queueIdx, startIndex, count)` without ambiguity (FIX-04).
  4. Public ABI signature byte-identical — `processFutureTicketBatch(uint24, uint256) → (bool, bool, uint32)` selector + parameter types + return shape unchanged. `TicketsCredited` + `TraitsGenerated` + `TicketsQueued` event topic hashes preserved (the `TraitsGenerated.startIndex` field's MEANING changes post-fix, but its ABI shape does not) (FIX-03).
  5. Storage layout regression — non-mutated slots byte-identical to v40.0 closure HEAD `cd549499` (storage-slot grep proof committed to AUDIT artifacts). Chosen-shape impact bounded: (a) widens `ticketsOwedPacked[rk][player]` beyond 40 bits OR adds a sibling map; (b) adds an `initialOwed[rk][player]` slot at queue-time; (c) adds zero storage. ALL shapes attest zero new public/external mutation entry points + zero new admin entry points + zero new modifiers + zero new upgrade hooks (FIX-02).
  6. Day-boundary behavior verified preserved — cross-day multi-call drains continue to get fresh daily `entropy` (which would already de-collide the bug); the fix narrowly addresses the within-day case while leaving cross-day behavior byte-identical.
  7. Wave shape: 1 USER-APPROVED batched contract commit per `feedback_batch_contract_approval.md` + `feedback_no_contract_commits.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md`. Bytecode delta + storage-slot grep proof + worst-case gas benchmark reported in commit message per `feedback_gas_worst_case.md`. Commit lands before Phase 282 begins (Phase 282 fixture asserts against the chosen-shape behavior).

**Plans:** 1 plan

Plans:
- [x] 281-01-PLAN.md — owed-salt 4th keccak input in `_raritySymbolBatch`; `processFutureTicketBatch` passes `owed` at L469 + as 4th positional arg to `TraitsGenerated` emit; design-intent trace + measurement artifacts land in the same USER-APPROVED batched commit covering FIX-01..05 (D-281-FIX-SHAPE-01 + D-281-STARTINDEX-SEMANTICS-01 + D-281-FIX01-REFRAME-01 + D-281-SEVERITY-01 + D-281-KI-01)

### Phase 282: Multi-Call Drain Regression Fixture (TST-FIX)

**Goal:** Regression test suite covering the cross-call-equivalence-to-single-call invariant Phase 281 establishes, including the load-bearing PRODUCTION_REPLAYABLE anchor-replay fixture (TST-FIX-06) that would have caught the bug pre-deploy and is the evidence artifact cited by AUDIT-02 in the F-41-01 finding block. Tests cover: trait-byte-identity across N ≥ 2 calls (TST-FIX-01); `TraitsGenerated.startIndex` cross-call monotonicity + reset-only-on-next-player (TST-FIX-02); distinct keccak seeds per call witnessed via `groupIdx` advancement OR synthetic test-only assertion on cumulative trait-counts not collapsing to a 20×-duplicate distribution OR on-chain trace inspection of `_raritySymbolBatch` call args (TST-FIX-03); single-call drain byte-identity preserved when `owed ≤ writesBudget` (no behavior change in the single-call case; baseline via recorded v40 trace OR deterministic seed run against a v40-tagged fixture) (TST-FIX-04); shape-dependent storage regression (TST-FIX-05 — shape (a) re-validates `ticketsOwedPacked` 8-bit `rem` + 24-bit `owed` + 8+-bit `processed` packed-form including max-owed range; shape (b) attests new `initialOwed[rk][player]` slot written at all 4-5 queue-time callsites via storage-slot grep; shape (c) attests zero storage delta + gas regression instead; the unchosen shapes' tests are inapplicable). **TST-FIX-06 on-chain anchor replay** — fixture replays the production anchor: `player @ queueIdx=6` with `~5840 owed` (= 19×292 + 119+); daily entropy `2f02…`; multi-call drain spanning ~20 calls. Pre-fix branch: produces 20 identical 292-trait sequences matching the on-chain emission witnessed at blocks 10862393..10862412 (this is the regression artifact that would have caught the bug pre-deploy). Post-fix branch: produces 20 distinct trait sequences whose concatenation matches a single-call drain. TST-FIX-06 is the load-bearing PRODUCTION_REPLAYABLE evidence for AUDIT-02 §4 F-41-01. Test directory (`test/edge/` or `test/mint/`) decided at plan-phase. Wave shape: 1 USER-APPROVED batched test commit `test(282): multi-call drain trait-byte-identity + startIndex monotonicity + seed-distinct + single-call preservation + storage regression + production anchor replay [TST-FIX-01..06]` per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md`. Test count + LOC delta + runtime delta reported in commit message.
**Depends on:** Phase 281 (TST-FIX-05 fixture is shape-dependent and TST-FIX-01..04 + TST-FIX-06 assert against the post-fix behavior; the chosen shape's contract patch must land before this phase begins)
**Requirements:** TST-FIX-01, TST-FIX-02, TST-FIX-03, TST-FIX-04, TST-FIX-05, TST-FIX-06
**Success Criteria** (what must be TRUE):

  1. Multi-call drain fixture mints player with `owed > writesBudget / 2` (forcing N ≥ 2 calls within a single VRF day); advances `processFutureTicketBatch` N times with the same daily `entropy`; asserts the concatenated trait multiset matches a single-call (`writesBudget = ∞`) drain trait-by-trait (TST-FIX-01).
  2. `TraitsGenerated.startIndex` sequence is monotonically non-decreasing within a player's batch run — call 1 emits `startIndex=0, count=X1`; call 2 emits `startIndex=X1, count=X2`; …; final call emits `startIndex=X1+X2+…, count=X_remaining`; then transitions to the next player at `startIndex=0` (TST-FIX-02).
  3. Distinct keccak seeds per call verified — `groupIdx` value differs across the N calls covering a single player (since `groupIdx = startIndex >> 4` and `startIndex` advances per FIX-04, `groupIdx` advances by at least 1 per call covering a player) OR equivalent assertion (cumulative trait-counts not collapsing to a 20×-duplicate distribution OR on-chain trace inspection of `_raritySymbolBatch` call args) proves the fix eliminates the v40 defect at the algorithmic level (TST-FIX-03).
  4. Single-call drain byte-identity preserved — for any `(rk, player)` pair where `owed ≤ writesBudget` (single-call drain), the trait multiset post-fix matches a v40-baseline trait multiset. The fix MUST NOT alter behavior when the whole player fits in one call. Baseline captured via recorded v40 trace OR deterministic seed run against a v40-tagged fixture (TST-FIX-04).
  5. Storage layout regression — shape-dependent. Shape (a): re-validates `ticketsOwedPacked` packed-form (low 8 bits `rem` preserved; next 24 bits `owed` preserved + still holds max-owed; high 8+ bits `processed` NEW). Shape (b): attests new `initialOwed[rk][player]` slot written at queue-time on the existing 4-5 callsites via storage-slot grep. Shape (c): attests zero storage delta + gas regression. The unchosen shapes' tests are inapplicable (TST-FIX-05).
  6. **On-chain anchor replay regression** — fixture replays `player @ queueIdx=6` with `~5840 owed` and daily entropy `2f02…` across ~20 calls. Pre-fix branch reproduces the 20 byte-identical `TraitsGenerated(player, lvl=1, queueIdx=6, startIndex=0, count=292, entropy=2f02…)` sequences witnessed at blocks 10862393..10862412 (regression-artifact pre-deploy detection). Post-fix branch produces 20 distinct trait sequences whose concatenation matches a single-call drain. TST-FIX-06 is the load-bearing PRODUCTION_REPLAYABLE evidence cited by AUDIT-02 in the §4 F-41-01 finding block (TST-FIX-06).
  7. Wave shape: 1 USER-APPROVED batched test commit per the same approval discipline as Phase 281. Test count + LOC delta + runtime delta reported in commit message. Commit lands before Phase 284 terminal audit begins.

**Plans:** TBD (created by `/gsd:plan-phase 282`)

### Phase 283: Cross-Surface Batched-Loop Sweep (SWEEP)

**Goal:** Enumerate all batched / cursor-based loops in `contracts/modules/` and attest cross-call seed-distinctness per surface, ruling out (or surfacing) analogous local-var-on-stack resumption defects beyond the confirmed mint-batch defect Phase 281 fixes. Default outcome: 0 additional F-41-NN findings (the audit baseline assumption is that mint-batch is the only such defect; sweep validates or refutes). The phase produces a SWEEP-LOG.md artifact catalouging every loop with a 3-question structural attestation; any new finding gets a remediation requirement (`FIX-SWEEP-NN`) + regression test (`TST-SWEEP-NN`) that EXPANDS this phase's contract+test commit footprint (with USER APPROVAL per the same batched-approval discipline as Phase 281 — no pre-approval per `feedback_never_preapprove_contracts.md`). SWEEP-01 enumerates all batched/cursor loops; candidate surfaces (non-exhaustive — plan-phase locks the full list): `DegenerusGameMintModule.processFutureTicketBatch` (CONFIRMED DEFECT — already patched in Phase 281; included in SWEEP-LOG for completeness as the reference defect row); `DegenerusGameLootboxModule._resolveLootboxCommon` + auto-resolve callers (`resolveLootboxDirect` decimator-claim + `resolveRedemptionLootbox` sDGNRS-redemption); `JackpotModule._awardDailyCoinToTraitWinners` + `_awardFarFutureCoinJackpot` + `_jackpotTicketRoll` 2-roll pattern + `_awardJackpotTickets`; `BurnieCoinflip` claim path + daily resolution loops; `DegenerusGameAdvanceModule` advance-game bounty loops; `DegenerusQuests` quest-reward batched loops; `DegenerusGameDegeneretteModule.advance` if present. SWEEP-02 per-loop attestation answers (i) within-iteration counter on stack without storage persistence? (ii) per-call resumption regenerates identical RNG inputs? (iii) `writesBudget`-equivalent cooperative-yield mechanism that could split a single conceptual operation across calls? Three-no = SAFE-BY-STRUCTURE. SWEEP-03 lootbox queue-advance attestation: per-resolution seed comes from per-VRF-call `keccak(rngWord, player)` chain (v40 close trace), each resolution is single-shot (no cooperative yield), Bernoulli predicate consumes `bits[152..167]` of per-resolution seed; cross-call attestation confirms no analogous defect. SWEEP-04 jackpot ticket-award attestation: Bernoulli at `JackpotModule:2216` consumes `bits[200..215]` of per-roll entropy; per-roll entropy evolves via `EntropyLib.entropyStep` between rolls (post-v40.0 Phase 278 keccak self-mix replaces xorshift); each `_jackpotTicketRoll` is single-shot within a single `advanceGame` call — cross-`advanceGame` cooperative-yield question answered yes/no at sweep execution (if YES, do the jackpot batched loops regenerate identical entropy on resumption?). SWEEP-05 records any new F-41-NN finding in `audit/FINDINGS-v41.0.md` §4 (any new finding) and §5 (sweep methodology + negative-result attestations); count of `FIX-SWEEP-NN` + `TST-SWEEP-NN` requirements expands at plan-phase from the placeholder TST-SWEEP-01 baseline. Wave shape (default zero-mutation outcome): 0 USER-APPROVED contract commits + 1 AGENT-COMMITTED SWEEP-LOG.md artifact summarizing the analytical pass. Non-default outcome (sweep surfaces ≥ 1 new finding): N USER-APPROVED batched contract commits + N USER-APPROVED batched test commits per the same approval discipline as Phase 281, each tagged `feat(283): sweep-derived <surface> fix [FIX-SWEEP-NN]` and `test(283): sweep-derived <surface> regression [TST-SWEEP-NN]`.
**Depends on:** Nothing strictly (sweep is analytical and doesn't require Phase 281's patch to land; can in principle sequence in parallel). Pragmatic sequencing places this AFTER Phase 281 + Phase 282 to (i) keep contract-mutation history linear (per-commit USER-APPROVAL gates serialize wall-clock execution) and (ii) avoid a SWEEP-derived patch landing alongside the FIX patch with mixed approval scope. Plan-phase confirms sequencing.
**Requirements:** SWEEP-01, SWEEP-02, SWEEP-03, SWEEP-04, SWEEP-05, TST-SWEEP-01
**Success Criteria** (what must be TRUE):

  1. SWEEP-LOG.md artifact enumerates every batched / cursor-based loop in `contracts/modules/` — each surface gets a row with the 3-question structural attestation (within-iteration counter on stack? per-call resumption regenerates identical RNG inputs? cooperative-yield mechanism present?). Three-no answer rows tagged SAFE-BY-STRUCTURE. Candidate surface list from SWEEP-01 covered at minimum: `DegenerusGameMintModule.processFutureTicketBatch` (reference defect); `_resolveLootboxCommon` + 2 auto-resolve callers; `JackpotModule` 4 entry points + 2-roll pattern; `BurnieCoinflip` claim + daily resolution; `DegenerusGameAdvanceModule` advance-game bounty; `DegenerusQuests` quest-reward batched; `DegenerusGameDegeneretteModule.advance` if present. Plan-phase locks the full enumerated list (SWEEP-01, SWEEP-02).
  2. Lootbox queue-advance attestation: per-resolution seed comes from per-VRF-call `keccak(rngWord, player)` chain (v40 close trace cited); each resolution is single-shot (no cooperative yield); Bernoulli consumes `bits[152..167]` of per-resolution seed; cross-call attestation confirms no analogous defect at the lootbox surfaces (SWEEP-03).
  3. Jackpot ticket-award attestation: Bernoulli at `JackpotModule:2216` consumes `bits[200..215]` of per-roll entropy; per-roll entropy evolves via `EntropyLib.entropyStep` between rolls (post-v40.0 Phase 278 keccak self-mix); each `_jackpotTicketRoll` is single-shot within a single `advanceGame` call. Cross-`advanceGame` cooperative-yield question answered explicitly (yes/no). If YES — do the jackpot batched loops regenerate identical entropy on resumption? If NO — SAFE-BY-STRUCTURE (SWEEP-04).
  4. Default outcome: zero new F-41-NN findings beyond F-41-01 (mint-batch); SWEEP-05 records the negative-result attestation set in `audit/FINDINGS-v41.0.md` §5. Non-default outcome: each new F-41-NN finding gets (i) a `FIX-SWEEP-NN` remediation requirement; (ii) a `TST-SWEEP-NN` regression test covering cross-call seed-distinct property at the affected surface; (iii) USER-APPROVED batched contract + test commits per the same approval discipline as Phase 281 (SWEEP-05, TST-SWEEP-01).
  5. SWEEP outcome recorded in `audit/FINDINGS-v41.0.md` §4 (any new F-41-NN finding) and §5 (sweep methodology + negative-result attestations). The SWEEP-LOG.md artifact lives under `.planning/phases/283-<slug>/` or equivalent plan-phase location.
  6. Wave shape: default 0 USER-APPROVED contract commits + 1 AGENT-COMMITTED SWEEP-LOG.md artifact. Non-default: N USER-APPROVED batched contract + N USER-APPROVED batched test commits, each requiring explicit USER APPROVAL per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md`. Commit(s) land before Phase 284 terminal audit begins.

**Plans:** TBD (created by `/gsd:plan-phase 283`)

### Phase 284: Delta Audit + Findings Consolidation (Terminal)

**Goal:** Publish `audit/FINDINGS-v41.0.md` as the single canonical milestone-closure deliverable per D-NN-FILES-01 carry → D-41N-FILES-01 — 9-section shape consistent with v32.0..v40.0 precedent; FINAL READ-only at v41.0 closure HEAD (`chmod 444` post-closure-flip); 5-Bucket Severity Rubric carry from D-08. **First non-zero finding milestone in v25..v41 audit history — §4 contains a non-zero F-41-01 finding block for the mint-batch cross-call determinism defect.** F-41-01 severity HIGH or CRITICAL per AUDIT-02 (justification based on launch-posture impact decided at plan-phase); status PRODUCTION_REPLAYABLE (cites on-chain evidence at blocks 10862393..10862412 — 20 byte-identical `TraitsGenerated(player, lvl=1, queueIdx=6, startIndex=0, count=292, entropy=2f02…)` events); disposition RESOLVED_AT_V41 (post-Phase-281 fix-shape patch); citation chain: `D-41N-FIX-SHAPE-01` decision anchor + the chosen-shape contract commit + Phase 282 TST-FIX-06 anchor-replay regression as the load-bearing pre-deploy detection artifact. §3.A delta-surface table covers all source-tree changes v40→v41 (Phase 281 fix commit + Phase 282 test commit + any Phase 283 sweep-derived contract+test commits) with hunk-level evidence + {NEW, MODIFIED_LOGIC, REFACTOR_ONLY, DELETED} classification; phase-row groups match the final phase split. §3.B zero-new-state grep-proof attestation: fix-shape (a)/(b) adds a storage slot (attested via grep); fix-shape (c) adds zero storage. ALL shapes attest zero new public/external mutation entry points + zero new admin entry points + zero new modifiers + zero new upgrade hooks. 5-row roll-up matches v36..v40 audit-attestation pattern. §3.C conservation re-proof: (i) total-traits-credited invariant — sum over all `(rk, player)` of `_raritySymbolBatch.count` = sum of all queued ticket awards (`owed` consumed across all calls); (ii) bit-slice independence — shape (a)/(b) introduce no new entropy-slice consumption; shape (c) consumes a NEW entropy slice and attests non-collision with existing consumers (bits[0..12] jackpot path-select, bits[152..167] manual+auto-resolve Bernoulli, bits[200..215] jackpot Bernoulli); (iii) storage byte-identity for non-mutated slots. §4 adversarial surfaces enumerated and attested SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE except F-41-01 RESOLVED_AT_V41; 3-skill PARALLEL adversarial pass per D-271-ADVERSARIAL-01 + D-271-ADVERSARIAL-03 carry: `/contract-auditor` + `/zero-day-hunter` + `/economic-analyst` spawn; `/degen-skeptic` OUT OF SCOPE per D-271-ADVERSARIAL-02 carry. Required adversarial hypothesis surface (charged specifically with red-teaming the chosen fix shape): (i) does the fix re-introduce a different determinism break? (ii) does shape (c) per-call-nonce open MEV / griefing on the trait-distribution (e.g., can a transaction-ordering attacker force a specific trait by selecting block number)? (iii) does shape (a) wider packed form change `_rollRemainder` interaction semantics for mint-boost paths? (iv) does shape (b) `initialOwed` storage open any griefing where a queue-time write could be manipulated? Adversarial pass disposition committed to `<phase>-01-ADVERSARIAL-LOG.md` (3 H2 sections + Disposition; zero residual FINDING_CANDIDATE OR explicit RESOLVED_AT_V41 with amendment reference). KI walkthrough — EXC-01..03 RE_VERIFIED-NEGATIVE-scope at v41 (the v41 audit subject has zero affiliate-roll / AdvanceModule / gameover-RNG-substitution interaction). EXC-04 stays STRUCTURALLY ELIMINATED (no resurrection at v41 — `EntropyLib.entropyStep` was deleted at v40.0 Phase 278 `8a81a87c` and is not reintroduced; static-analysis grep confirms `entropyStep` does not reappear in `contracts/` at v41 close HEAD). KI envelope re-verifications committed to AUDIT §6. KNOWN-ISSUES.md disposition decision recorded as `D-41N-KI-01` — default: record realized v41-historical miscount as a HISTORICAL closed entry (cites blocks 10862393..10862412 + RESOLVED_AT_V41 disposition + `D-41N-FIX-SHAPE-01`); alternative: defer disposition to launch posture review (record only in §9 closure prose, not in KNOWN-ISSUES.md). Decision locked at plan-phase. LEAN regression appendix §5: REG-01 PASS for v40.0 closure signal `MILESTONE_V40_AT_HEAD_cd549499` non-widening at v41 close HEAD on v40-touched surfaces NOT in v41 scope — `DegenerusGameLootboxModule` Bernoulli + WWXRP consolation; `JackpotModule._jackpotTicketRoll` Bernoulli + keccak self-mix (post-ENT-05 refactor); `LootBoxOpened`/`BurnieLootOpen`/`JackpotTicketWin` event shapes; whole-BURNIE floor at LootboxModule:1080 + JackpotModule:1842/1922 — all byte-identical between v40 close HEAD and v41 close HEAD on non-Mint surfaces. REG-02 PASS for v34.0 closure signal `MILESTONE_V34_AT_HEAD_6b63f6d4daf346a53a1d463790f637308ea8d555` non-widening — TraitUtils 3 functions + `_pickSoloQuadrant` + JackpotBucketLib byte-identical at v41 close HEAD. REG-03 KI envelope re-verifications (EXC-01..03 NEGATIVE-scope; EXC-04 STRUCTURALLY ELIMINATED preserved — `EntropyLib.entropyStep` does not reappear). REG-04 prior-finding spot-check sweep PASS across `audit/FINDINGS-v25..v40.0.md` for v41-touched function/surface set (`DegenerusGameMintModule.processFutureTicketBatch` + `_raritySymbolBatch` + any sweep-derived surfaces); each prior finding on these surfaces re-verified RESOLVED or NEGATIVE-scope at v41 close HEAD. §9 closure-verdict math exercises non-zero F-NN path for the first time in audit history: `1 of 1 F-41-NN RESOLVED_AT_V41; 0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_<MODIFIED|UNMODIFIED>` (KI status per AUDIT-08 D-41N-KI-01 outcome). Closure signal `MILESTONE_V41_AT_HEAD_<sha>` emitted in §9c with verbatim presence in 5 FINDINGS locations + 3 cross-document propagation targets per v39.0 P274 Task 3.10 precedent. ROADMAP + STATE + MILESTONES + PROJECT + REQUIREMENTS closure-flips land atomically post-§9c attestation per D-NN-CLOSURE-01 carry → D-41N-CLOSURE-01. §9.NN three-subsection commit-readiness register populated (i USER-APPROVED contracts: Phase 281 fix commit + any Phase 283 sweep-derived; ii USER-APPROVED tests: Phase 282 fixture commit + any Phase 283 sweep-derived; iii AGENT-COMMITTED audit + planning artifacts: `audit/FINDINGS-v41.0.md` + KNOWN-ISSUES.md edit (if D-41N-KI-01 records) + ROADMAP/STATE/MILESTONES/PROJECT/REQUIREMENTS flips). Forward-cite zero-emission at terminal phase per D-NN-FCITE-01 carry → D-41N-FCITE-01 (no `v42.0+` references emitted from this terminal phase; planner-handoff register in §9 "Deferred to Future Milestones" uses locked-decision IDs + descriptive labels). SOURCE-TREE FROZEN — zero contracts/ + zero test/ mutations during this phase.
**Depends on:** Phase 281 (audit baseline is the post-Phase-281 HEAD — fix commit must land), Phase 282 (TST-FIX-06 anchor-replay is the load-bearing pre-deploy detection artifact cited in F-41-01), Phase 283 (sweep outcome shapes §3.A row count + §4 surface enumeration + §5 sweep methodology subsection — terminal phase needs the sweep results to consolidate findings)
**Requirements:** AUDIT-01, AUDIT-02, AUDIT-03, AUDIT-04, AUDIT-05, AUDIT-06, AUDIT-07, AUDIT-08, REG-01, REG-02, REG-03, REG-04
**Success Criteria** (what must be TRUE):

  1. `audit/FINDINGS-v41.0.md` published FINAL READ-only at v41.0 closure HEAD (`chmod 444` post-closure-flip) with 9 sections (executive summary, per-phase summaries §3a-c, F-41-NN finding blocks under D-08 5-bucket severity rubric, §3.A delta-surface table covering Phase 281 + 282 + any 283 sweep-derived commits with hunk-level evidence + classification, §3.B zero-new-state grep-proof attestation, §3.C conservation re-proof, regression appendix §5, KI gating walk §6, prior-artifact cross-cites §7, forward-cite zero-emission proof §8, closure attestation §9 with §9.NN three-subsection commit-readiness register); §3.A row-count covers every changed declaration grep-reproducibly. §3.A phase-row groups match the final phase split (AUDIT-01).
  2. **§4 includes a non-zero F-41-01 finding block for the mint-batch determinism defect** — severity HIGH or CRITICAL per D-08 5-Bucket Severity Rubric (justification recorded at plan-phase based on launch-posture impact); status PRODUCTION_REPLAYABLE (cites on-chain evidence at blocks 10862393..10862412 — 20 byte-identical `TraitsGenerated` events); disposition RESOLVED_AT_V41; citation chain: `D-41N-FIX-SHAPE-01` decision anchor + the chosen-shape contract commit (Phase 281) + Phase 282 TST-FIX-06 anchor-replay regression as the load-bearing pre-deploy detection artifact (AUDIT-02).
  3. §3.B zero-new-state grep-proof: fix-shape (a)/(b) adds a storage slot (attested via grep); fix-shape (c) adds zero storage. ALL shapes attest zero new public/external mutation entry points + zero new admin entry points + zero new modifiers + zero new upgrade hooks. 5-row roll-up matches v36..v40 audit-attestation pattern (AUDIT-04).
  4. §3.C conservation re-proof: (i) total-traits-credited invariant — sum over all `(rk, player)` of `_raritySymbolBatch.count` = sum of all queued ticket awards (`owed` consumed); (ii) bit-slice independence — shape (a)/(b) introduce no new entropy-slice consumption; shape (c) consumes a NEW entropy slice and attests non-collision with bits[0..12] + bits[152..167] + bits[200..215] existing consumers; (iii) storage byte-identity for non-mutated slots (AUDIT-03).
  5. §4 adversarial surfaces enumerated and attested SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE except F-41-01 RESOLVED_AT_V41 (+ any sweep-derived F-41-NN per Phase 283 outcome); 3-skill PARALLEL adversarial pass `/contract-auditor` + `/zero-day-hunter` + `/economic-analyst` per D-271-ADVERSARIAL-01 + D-271-ADVERSARIAL-03 carry (`/degen-skeptic` OUT OF SCOPE per D-271-ADVERSARIAL-02 carry); required hypothesis surface (i)..(iv) covers fix-shape re-introducing different determinism break + shape (c) per-call-nonce MEV/griefing + shape (a) `_rollRemainder` mint-boost interaction + shape (b) `initialOwed` queue-time griefing; `<phase>-01-ADVERSARIAL-LOG.md` 3 H2 sections + Disposition; zero residual FINDING_CANDIDATE OR explicit RESOLVED_AT_V41 with amendment reference (AUDIT-05).
  6. KI walkthrough EXC-01..03 RE_VERIFIED-NEGATIVE-scope at v41 (no affiliate-roll / AdvanceModule / gameover-RNG-substitution interaction in v41 audit subject); EXC-04 STRUCTURALLY ELIMINATED preserved (no resurrection at v41 — `EntropyLib.entropyStep` does not reappear in `contracts/` at v41 close HEAD; static-analysis grep proof). KI envelope re-verifications committed to AUDIT §6 (AUDIT-06).
  7. §9 closure-verdict structure exercises non-zero F-NN path for the first time in audit history: `1 of 1 F-41-NN RESOLVED_AT_V41; 0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_<MODIFIED|UNMODIFIED>` (KI status per AUDIT-08 D-41N-KI-01 outcome). Closure signal `MILESTONE_V41_AT_HEAD_<sha>` emitted in §9c with verbatim presence in 5 FINDINGS locations + 3 cross-document propagation targets (ROADMAP + STATE + MILESTONES + PROJECT + REQUIREMENTS closure-flips atomic post-§9c attestation per D-41N-CLOSURE-01); §9.NN three-subsection commit-readiness register populated (AUDIT-07).
  8. KNOWN-ISSUES.md disposition decision `D-41N-KI-01` locked at plan-phase. Default: record realized v41-historical miscount as a HISTORICAL closed entry (cites blocks 10862393..10862412 + RESOLVED_AT_V41 disposition + `D-41N-FIX-SHAPE-01`). Alternative: defer to launch posture review (record only in §9 closure prose, not in KNOWN-ISSUES.md). Outcome reflected in closure verdict KNOWN_ISSUES_<MODIFIED|UNMODIFIED> token (AUDIT-08).
  9. §5 regression appendix: REG-01 PASS for v40.0 closure signal `MILESTONE_V40_AT_HEAD_cd549499` non-widening at v41 close HEAD on v40-touched surfaces NOT in v41 scope (LootboxModule Bernoulli + WWXRP consolation + JackpotModule:2216 Bernoulli + `_jackpotTicketRoll` keccak self-mix + event shapes + whole-BURNIE floor at LootboxModule:1080 + JackpotModule:1842/1922 byte-identical); REG-02 PASS for v34.0 closure signal `MILESTONE_V34_AT_HEAD_6b63f6d4daf346a53a1d463790f637308ea8d555` non-widening (TraitUtils + `_pickSoloQuadrant` + JackpotBucketLib byte-identical); REG-03 KI envelope re-verifications (EXC-01..03 NEGATIVE-scope; EXC-04 STRUCTURALLY ELIMINATED preserved); REG-04 prior-finding spot-check sweep PASS across `audit/FINDINGS-v25..v40.0.md` for v41-touched surface set — `DegenerusGameMintModule.processFutureTicketBatch` + `_raritySymbolBatch` + any sweep-derived surfaces — each prior finding re-verified RESOLVED or NEGATIVE-scope at v41 close HEAD (REG-01, REG-02, REG-03, REG-04).
  10. Forward-cite zero-emission at terminal phase per D-41N-FCITE-01 — no `v42.0+` references emitted from this terminal phase across scoped artifacts; planner-handoff carry-forward register in §9 "Deferred to Future Milestones" uses locked-decision IDs + descriptive labels. SOURCE-TREE FROZEN — zero contracts/ + zero test/ mutations during this phase; audit deliverable + KNOWN-ISSUES.md edit (if D-41N-KI-01 records the historical entry) + closure-flip docs AGENT-COMMITTED.

**Plans:** TBD (created by `/gsd:plan-phase 284`)

<details>
<summary>✅ v40.0 Unified Whole-Ticket Award Protocol + Whole-BURNIE Floor (Phases 275-280) — SHIPPED 2026-05-14</summary>

- [x] Phase 275: Auto-Resolve LootboxModule Bernoulli (LBX-AR) (2/2 plans) — completed 2026-05-13
- [x] Phase 276: JackpotModule:2216 BAF Bernoulli (JPT-BR) (2/2 plans) — completed 2026-05-14
- [x] Phase 277: Event Surface Unification + Sentinel Retirement (EVT-UNI) (2/2 plans) — completed 2026-05-14
- [x] Phase 278: JackpotModule Cleanup + ENT-05 BAF Xorshift Refactor + Wrapper Retirement (JPT-CLEAN) (2/2 plans) — completed 2026-05-14
- [x] Phase 279: Whole-BURNIE Floor (BUR) (2/2 plans) — completed 2026-05-14
- [x] Phase 280: Delta Audit + Findings Consolidation (Terminal) (1/1 plans) — completed 2026-05-14 (closure signal `MILESTONE_V40_AT_HEAD_cd549499` emitted in audit/FINDINGS-v40.0.md §9c)

**Audit baseline:** v39.0 closure HEAD `MILESTONE_V39_AT_HEAD_6a7455d1` → v40.0 closure HEAD `cd549499`. 6-phase multi-phase milestone shape (5 surface-split phases 275-279 + 1 terminal audit phase 280) per v33/v34/v35/v37 precedent — NOT the v36/v38/v39 single-phase pattern. The 12-commit v40.0 audit subject: Phase 275 (`b6ed8fce` + `bb1b1abd`) auto-resolve LootboxModule Bernoulli — the v39 manual-path Bernoulli extended to `resolveLootboxDirect` + `resolveRedemptionLootbox`, predicate hoisted to shared scope, auto-resolve branch swapped to `_queueTickets(whole)`, silent cold-bust; Phase 276 (`c473867e` + `1568fd5c`) JackpotModule:2216 BAF Bernoulli — inline Bernoulli in `_jackpotTicketRoll` reading `bits[200..215]`, `:2216` swapped to direct `_queueTickets(whole, true)`; Phase 277 (`02fb7085` + `6fbee850` + `f7a6fccd`) event surface unification + sentinel retirement — `LootboxTicketRoll` deleted, `roundedUp` field added to `LootBoxOpened`/`BurnieLootOpen`/`JackpotTicketWin`, `index != type(uint48).max` sentinel retired, `f7a6fccd` CR-01 gap-closure added the dedicated `payColdBustConsolation` parameter + trimmed the event surface; Phase 278 (`8a81a87c` + `c3baf694` + `a91dac85`) JackpotModule cleanup + ENT-05 keccak refactor + wrapper retirement — `_jackpotTicketRoll` swapped xorshift `EntropyLib.entropyStep` for `EntropyLib.hash2` keccak self-mix, `entropyStep` + the zero-caller `_queueLootboxTickets` wrapper deleted, 3 `JackpotTicketWin` emits unified onto whole-ticket counts; Phase 279 (`8ef4a010` + `37207743`) whole-BURNIE floor — A1 floor-per-winner at the 3 RNG-amount sites (LootboxModule:1080 + JackpotModule:1842 + JackpotModule:1922). 5 USER-APPROVED batched contract commits + 5 USER-APPROVED batched test commits + 2 remediation commits. Phase 280 terminal phase SOURCE-TREE FROZEN — zero contracts/ + zero test/ mutations. 65/65 requirements satisfied. Result: 11 of 11 §4 adversarial surfaces SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE; zero F-40-NN finding blocks emitted; 10 novel-vector hypotheses + edge cases across the 3-skill PARALLEL adversarial pass (`/contract-auditor` + `/zero-day-hunter` + `/economic-analyst`; `/degen-skeptic` OUT OF SCOPE) all NEGATIVE_RESULT_ONLY / ACCEPTED_DESIGN. LEAN regression: 1 PASS REG-01 (v39.0 closure NON-WIDENING for v39-touched surfaces NOT in v40 scope) + 1 PASS REG-02 (v34.0 closure NON-WIDENING) + REG-03 KI envelope re-verifications + REG-04 prior-finding spot-check sweep across audit/FINDINGS-v25..v39.0; aggregate 12 PASS / 0 REGRESSED / 0 SUPERSEDED-as-verdict. KI envelopes EXC-01..03 RE_VERIFIED-NEGATIVE-scope at v40; EXC-04 STRUCTURALLY ELIMINATED at v40.0 (Phase 278 `8a81a87c` deleted `EntropyLib.entropyStep`). KNOWN-ISSUES.md MODIFIED per D-280-EXC04-01 — the line-31 EXC-04 entry REMOVED OUTRIGHT. Closure verdict `4 of 4 KI_ELIGIBLE addressed; KNOWN_ISSUES_MODIFIED`. Deliverable: `audit/FINDINGS-v40.0.md` (FINAL READ-only at HEAD `cd549499`, 9 sections, ~907 lines; chmod 444). Closure signal: `MILESTONE_V40_AT_HEAD_cd549499`.

</details>

<details>
<summary>✅ v39.0 Lootbox Whole-Ticket Rounding + WWXRP Consolation (Phase 274) — SHIPPED 2026-05-13</summary>

- [x] Phase 274: Lootbox Whole-Ticket Rounding + WWXRP Consolation (Terminal) (1/1 plans) — completed 2026-05-13 (closure signal `MILESTONE_V39_AT_HEAD_6a7455d1` emitted in audit/FINDINGS-v39.0.md §9c)

**Audit baseline:** v38.0 closure HEAD `MILESTONE_V38_AT_HEAD_06623edb` → v39.0 audit-subject HEAD `6a7455d1`. Single-phase milestone-closure patch mirroring v36.0 Phase 266 + v38.0 Phase 272 precedent. MANUAL LOOTBOX OPENS ONLY (`openLootBox` + `openBurnieLootBox`) — auto-resolve callers (`resolveLootboxDirect` decimator-claim + `resolveRedemptionLootbox` sDGNRS-redemption) explicitly UNCHANGED per D-274-MANUAL-ONLY-01. Wave 1 USER-APPROVED batched contract commit `c21f833a` ships `_resolveLootboxCommon` `uint48 index` parameter + manual-branch Bernoulli-collapse via `bits[152..167]` + new private constant `LOOTBOX_WWXRP_CONSOLATION = 1 ether` + new event `LootboxTicketRoll(player, lootboxIndex, preRollTickets, roundedUp)` declared on interface + contract event block + bit-allocation NatSpec update 152 → 168. D-274-BIT-SLICE-01 superseded intra-Wave-1 from 8-bit to 16-bit slice on bias quantification. Wave 2 USER-APPROVED batched test commit `f8e55cfe` ships TST-WT-01..07 + TST-WX-01..03 + TST-REG-01..04 (+1,422 LOC across 4 new test files; 74 tests; all passing). Wave 3 audit-deliverable atomic chain `386e797d` → `6a7455d1` + closure-flip commits. Phase 273 included-since-baseline (`ff929948` + `e9807891` + `e04d3333` + `1eb1ecb5`) folded into v39.0 audit baseline per D-274-BAF273-INCLUDE-01. 39/39 in-scope requirements satisfied (5 LBX-WT + 4 LBX-WX + 6 LBX-EVT + 7 TST-WT + 3 TST-WX + 4 TST-REG + 6 AUDIT + 4 REG). Result: 8 of 8 §4 adversarial surfaces SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE / SAFE_BY_DESIGN_PHASE_273; zero F-39-NN finding blocks emitted; 12 novel-vector hypotheses (i)..(t) returned 10 NEGATIVE_RESULT_ONLY + 2 ACCEPTED_DESIGN dispositions. LEAN regression: 1 PASS REG-01 (v38.0 closure NON-WIDENING for v38-touched surfaces NOT in v39 manual-lootbox scope) + 1 PASS REG-02 (v34.0 closure NON-WIDENING) + REG-03 KI envelope re-verifications + REG-04 prior-finding spot-check sweep across audit/FINDINGS-v25..v38.0. KI envelopes EXC-01..03 RE_VERIFIED-NEGATIVE-scope at v39; EXC-04 RE_VERIFIED with NARROWS retained (BAF-jackpot-only scope; EntropyLib byte-identical at v39 HEAD). KNOWN-ISSUES.md UNMODIFIED per D-274-KI-01 default zero-promotion path. Closure verdict `0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_UNMODIFIED`. Adversarial pass via 3-skill PARALLEL spawn `/contract-auditor` + `/zero-day-hunter` + `/economic-analyst` per D-274-ADVERSARIAL-01 carry. Deliverable: `audit/FINDINGS-v39.0.md` (FINAL READ-only at HEAD `6a7455d1`, 9 sections; chmod 444). See [milestones/v39.0-ROADMAP.md](milestones/v39.0-ROADMAP.md).

</details>


<details>
<summary>✅ v38.0 Always-Hero Simplification + Maximal Dead-Code Cleanup (Phase 272) — SHIPPED 2026-05-11</summary>

- [x] Phase 272: Always-Hero Simplification + Maximal Dead-Code Cleanup (Terminal) (1/1 plans) — completed 2026-05-11 (3 USER-APPROVED commits: Wave 1 `527e3adc` + Wave 1.5 `4760459f` + Wave 2 `e3fcb95c`; closure signal `MILESTONE_V38_AT_HEAD_06623edb` emitted in audit/FINDINGS-v38.0.md §9c)

**Audit baseline:** v37.0 closure HEAD `MILESTONE_V37_AT_HEAD_2654fcc2` → v38.0 audit-subject HEAD `06623edb`. 2 contract-tree commits since baseline (`527e3adc` Phase 272 Wave 1 silent-normalize HERO-01..05 + CLEAN-01..05 + `4760459f` Phase 272 Wave 1.5 input-validation revision per D-272-INPUT-VALIDATION-01; bytecode delta −57 bytes 8955 → 8898; storage layout byte-identical; public ABI signature byte-identical with semantics shift accept+normalize → revert+InvalidBet) + 1 batched test-tree commit (`e3fcb95c` Phase 272 Wave 2; +238/−36 LOC across 6 files in `test/stat/`, `test/gas/`, `package.json` — STAT-01..02 hero-always-on re-validation + SURF-01..03 cross-surface preservation with SURF-03 rebase to `PHASE_269_CLOSE_BASELINE = "8fd5c2e1..."` + LBX-02 path-of-investigation prose + GASPIN-02 (a-alt) `test:gas` script split + STAT-03-v35-carry ACCEPTED-DESIGN documentation). Single-phase milestone shape per v36.0 Phase 266 precedent. 29/30 in-scope requirements satisfied (5 HERO + 6 CLEAN + 2 STAT + 3 SURF + 2 GASPIN + 1 STAT-03-v35-carry + 6 AUDIT + 4 REG) + 1 RE-DEFERRED-V39+ (LBX-02 — fixture-coverage gap persists; analytical worst-case load-bearing per Phase 266 GAS-01 precedent + `feedback_gas_worst_case.md`; path-of-investigation prose at `audit/FINDINGS-v38.0.md` §9.NN.iv). Result: 7 of 7 §4 adversarial surfaces SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE / SAFE_BY_DEFENSIVE_VALIDATION (a EV-neutrality preserved across (M, N) under always-on hero + b quadrant 0 default does NOT create payout-bias under defensive boundary validation + c cleanup-removal invariants preserved per `feedback_no_dead_guards.md` + d storage layout byte-identical + e public ABI byte-identical with semantics shift + f variance-impact bound on risk-averse subset + g clean test:stat at v38 close); zero F-38-NN finding blocks emitted; 1 INFO-severity docs-vs-behavior drift identified at Hypothesis (i) (`dailyHeroWagers` post-v38 ledger-tracking on `0xFF` input) at Wave 3 3-skill PARALLEL adversarial pass — RESOLVED_AT_V38 via Wave 1.5 commit `4760459f` defensive boundary validation per D-272-INPUT-VALIDATION-01 (status pivoted from KEEP_AS_NEGATIVE_FINDING to RESOLVED_AT_V38). LEAN regression: 1 PASS REG-01 (v37.0 closure NON-WIDENING) + 1 PASS REG-02 (v34.0 closure NON-WIDENING) + REG-03 KI envelope re-verifications + REG-04 prior-finding spot-check sweep across audit/FINDINGS-v25..v37.0. KI envelopes EXC-01..03 RE_VERIFIED-NEGATIVE-scope at v38; EXC-04 RE_VERIFIED with NARROWS retained (BAF-jackpot-only scope; EntropyLib byte-identical at v38 HEAD). KNOWN-ISSUES.md UNMODIFIED per D-272-KI-01 default zero-promotion path. Closure verdict `0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_UNMODIFIED`. Adversarial pass via `/contract-auditor` + `/zero-day-hunter` + `/economic-analyst` PARALLEL spawn per D-271-ADVERSARIAL-01 carry (`/degen-skeptic` OUT OF SCOPE per D-271-ADVERSARIAL-02 carry) returned ZERO disagreements; ZERO FINDING_CANDIDATE; ZERO 8th-surface NEW_VECTOR; ZERO KI promotion candidate per `272-01-ADVERSARIAL-LOG.md` Disposition (with Wave 1.5 disposition update appended at commit `1249a6fd`). Deliverable: `audit/FINDINGS-v38.0.md` (FINAL READ-only at HEAD `MILESTONE_V38_AT_HEAD_06623edb`, 9 sections, chmod 444). See [milestones/v38.0-ROADMAP.md](milestones/v38.0-ROADMAP.md) and [milestones/v38.0-REQUIREMENTS.md](milestones/v38.0-REQUIREMENTS.md) and [milestones/v38.0-MILESTONE-AUDIT.md](milestones/v38.0-MILESTONE-AUDIT.md).

</details>


<details>
<summary>✅ v37.0 Degenerette Recalibration + Maintenance Bundle (Phases 267-271) — SHIPPED 2026-05-11</summary>

- [x] Phase 267: Degenerette Producer + 5-Table Payout Rewrite (1/1 plans) — completed 2026-05-10 (USER-APPROVED contract commit `e1136071` — `feat(267): degenerette producer + 5-table payout rewrite + 3-tier ETH split [DGN-01..15, PAY-SPLIT-01..03]`)
- [x] Phase 268: Degenerette Statistical Validation + Cross-Surface Preservation (1/1 plans) — completed 2026-05-11 (USER-APPROVED test commit `4b277aaf` — `test(268): degenerette stat suite + cross-surface preservation v37.0 + worst-case gas regression [STAT-01..07, SURF-01..06]`)
- [x] Phase 269: Lootbox Dead-Branch Cleanup + SURF-05 Gas-Pin Re-Pinning (1/1 plans) — completed 2026-05-11 (USER-APPROVED contract commit `8fd5c2e1` — `feat(269): delete unreachable BURNIE-conversion branch in _resolveLootboxRoll [LBX-01]`; PARTIAL ship — 4 deferred to v38+)
- [x] Phase 270: Post-v32.0 Deferred-Commit Adversarial Sub-Audit (1/1 plans) — completed 2026-05-11 (AGENT-COMMITTED working-file `4017b9ec` + phase-close `5cd4f2bc`)
- [x] Phase 271: Delta Audit + Findings Consolidation (Terminal) (1/1 plans) — completed 2026-05-11 (closure signal `MILESTONE_V37_AT_HEAD_2654fcc2` emitted in audit/FINDINGS-v37.0.md §9c)

**Audit baseline:** v36.0 closure HEAD `1c0f09132d7439af9881c56fe197f81757f8164a` → v37.0 audit-subject HEAD `2654fcc2` (post-Task-12 §9 attestation commit). 2 contract-tree commits since baseline (`e1136071` Phase 267 Degenerette producer + 5-table payout rewrite + 3-tier ETH split + `8fd5c2e1` Phase 269 LBX-01 dead-branch deletion; bytecode shrink 177 bytes 18,330 → 18,153) + 1 batched test-tree commit (`4b277aaf` Phase 268; +2,277/−1 LOC across 6 files). 5-phase milestone shape: Phases 267 (contracts) + 268 (tests) + 269 (LBX cleanup PARTIAL ship) + 270 (post-v32.0 sub-audit; zero source-tree mutations) + 271 (delta audit terminal). 48/48 in-scope requirements satisfied (15 DGN + 3 PAY-SPLIT + 7 STAT + 6 SURF + LBX-01 + LBX-03 + GASPIN-01 + 4 DELTA + 6 AUDIT + 4 REG) + 3 DEFERRED-V38+ per D-271-DEFERRED-02 (LBX-02 + GASPIN-02 + GASPIN-03). Result: 8 of 8 §4 adversarial surfaces SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE (a per-N table dispatch + b symbol-only hero match + c `_countGoldQuadrants` boundary + d producer byte-layout + e WWXRP × hero composition + f lootbox dead-branch byte-equivalence + g hero × per-N skill-expression carry + h ETH PAY-SPLIT 3-tier boundary-gaming v37-NEW); zero F-37-NN finding blocks emitted; 1 PASS REG-01 + 1 PASS REG-02 + 5 PASS + 1 SUPERSEDED REG-04; `0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_UNMODIFIED` per D-271-PAYSPLIT-01 + D-271-KI-01 default zero-promotion path (PAY-SPLIT 3.0× bet boundary discontinuity documented via §4 (h) prose-only attestation per /economic-analyst mechanism-design assessment). 4 RE_VERIFIED KI envelope rows (EXC-01..03 NEGATIVE-scope at v37; EXC-04 NARROWS retained — BAF-jackpot-only scope). Adversarial pass via `/contract-auditor` + `/zero-day-hunter` + `/economic-analyst` PARALLEL spawn per D-271-ADVERSARIAL-01 (`/degen-skeptic` OUT OF SCOPE per D-271-ADVERSARIAL-02) returned ZERO disagreements; zero FINDING_CANDIDATE / zero 9th-surface NEW_VECTOR / zero KI promotion candidate per `271-01-ADVERSARIAL-LOG.md` Disposition. Deliverable: `audit/FINDINGS-v37.0.md` (FINAL READ-only at HEAD `MILESTONE_V37_AT_HEAD_2654fcc2`, 9 sections, chmod 444). See [milestones/v37.0-ROADMAP.md](milestones/v37.0-ROADMAP.md) and [milestones/v37.0-REQUIREMENTS.md](milestones/v37.0-REQUIREMENTS.md).

</details>

## Progress

| Phase | Plans Complete | Status | Completed |
|-------|----------------|--------|-----------|
| 281. Mint-Batch Determinism Fix (FIX) | 1/1 | Complete    | 2026-05-16 |
| 282. Multi-Call Drain Regression Fixture (TST-FIX) | 1/1 | Complete    | 2026-05-16 |
| 283. Cross-Surface Batched-Loop Sweep (SWEEP) | 0/1 | Not started | — |
| 285. Day-Index Read Audit + Hero-Override Fix (HOFIX) | 0/1 | Not started | F-41-02 remediation; surfaced by Phase 284 adversarial pass |
| 286. Hero-Override Day-Index Regression Fixture (TST-HOFIX) | 0/1 | Not started | Depends on Phase 285 fix |
| 284. Delta Audit + Findings Consolidation (Terminal) | 0/1 | Not started | Depends on 281, 282, 283, 285, 286 |
