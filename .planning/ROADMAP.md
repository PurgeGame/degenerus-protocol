# Roadmap: Degenerus Protocol Audit

## Milestones

- ✅ **v1.0 Initial RNG Security Audit** — Phases 1-5 (shipped 2026-03-14)
- ✅ **v2.0 Adversarial Audit** — Phases 6-18 (shipped 2026-03-17)
- ✅ **v3.0-v24.1** — Phases 19-212 (shipped 2026-04-10)
- ✅ **v25.0 Full Audit (Post-v5.0 Delta + Fresh RNG)** — Phases 213-217 (shipped 2026-04-11)
- ✅ **v26.0 Bonus Jackpot Split** — Phases 218-219 (shipped 2026-04-12)
- ✅ **v27.0 Call-Site Integrity Audit** — Phases 220-223 (shipped 2026-04-13)
- ✅ **v28.0 Database & API Intent Alignment Audit** — Phases 224-229 (shipped 2026-04-15) — see [milestones/v28.0-ROADMAP.md](milestones/v28.0-ROADMAP.md)
- ✅ **v29.0 Post-v27 Contract Delta Audit** — Phases 230-236 (shipped 2026-04-18) — see [milestones/v29.0-ROADMAP.md](milestones/v29.0-ROADMAP.md)
- ✅ **v30.0 Full Fresh-Eyes VRF Consumer Determinism Audit** — Phases 237-242 (shipped 2026-04-20) — see [milestones/v30.0-ROADMAP.md](milestones/v30.0-ROADMAP.md)
- ✅ **v31.0 Post-v30 Delta Audit + Gameover Edge-Case Re-Audit** — Phases 243-246 (shipped 2026-04-24) — see [milestones/v31.0-ROADMAP.md](milestones/v31.0-ROADMAP.md)
- ✅ **v32.0 Backfill Idempotency + purchaseLevel Underflow Audit** — Phases 247-253 (shipped 2026-05-02) — see [milestones/v32.0-ROADMAP.md](milestones/v32.0-ROADMAP.md)
- ✅ **v33.0 Charity Allowlist Governance** — Phases 254-258 (shipped 2026-05-07; closure signal `MILESTONE_V33_AT_HEAD_4ce3703d740d3707c88a1af595618120a8168399` supersedes `MILESTONE_V33_AT_HEAD_dcb70941`) — see [milestones/v33.0-ROADMAP.md](milestones/v33.0-ROADMAP.md)
- ✅ **v34.0 Trait Rarity Rework + Gold Solo Priority** — Phases 259-262 (shipped 2026-05-09; closure signal `MILESTONE_V34_AT_HEAD_6b63f6d4daf346a53a1d463790f637308ea8d555`) — see [milestones/v34.0-ROADMAP.md](milestones/v34.0-ROADMAP.md)
- ✅ **v35.0 BURNIE Near-Future Per-Pull Level Resample** — Phases 263-265 (shipped 2026-05-09; closure signal `MILESTONE_V35_AT_HEAD_5db8682bd7b811437f0c1cf47e832619d1478ac6`)
- ✅ **v36.0 Lootbox-Path Entropy Refactor** — Phase 266 (shipped 2026-05-10; closure signal `MILESTONE_V36_AT_HEAD_1c0f09132d7439af9881c56fe197f81757f8164a`) — see [milestones/v36.0-ROADMAP.md](milestones/v36.0-ROADMAP.md)
- ✅ **v37.0 Degenerette Recalibration + Maintenance Bundle** — Phases 267-271 (shipped 2026-05-11; closure signal `MILESTONE_V37_AT_HEAD_2654fcc2`) — see [milestones/v37.0-ROADMAP.md](milestones/v37.0-ROADMAP.md)
- ✅ **v38.0 Always-Hero Simplification + Maximal Dead-Code Cleanup** — Phase 272 (shipped 2026-05-11; closure signal `MILESTONE_V38_AT_HEAD_06623edb`) — see [milestones/v38.0-ROADMAP.md](milestones/v38.0-ROADMAP.md)
- ✅ **v39.0 Lootbox Whole-Ticket Rounding + WWXRP Consolation** — Phase 274 (shipped 2026-05-13; closure signal `MILESTONE_V39_AT_HEAD_6a7455d1`) — see [milestones/v39.0-ROADMAP.md](milestones/v39.0-ROADMAP.md)
- ✅ **v40.0 Unified Whole-Ticket Award Protocol + Whole-BURNIE Floor** — Phases 275-280 (shipped 2026-05-14; closure signal `MILESTONE_V40_AT_HEAD_cd549499`) — see [milestones/v40.0-ROADMAP.md](milestones/v40.0-ROADMAP.md)
- ✅ **v41.0 Cross-Call Determinism Fix (mint-batch + hero-override)** — Phases 281-289 + 284 (shipped 2026-05-17; closure signal `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4`; 3 of 3 F-41-NN RESOLVED_AT_V41) — see [milestones/v41.0-ROADMAP.md](milestones/v41.0-ROADMAP.md)
- ✅ **v42.0 Mint-Batch Event/Sig Cleanup + Hero-Override Weighted Roll + Deity-Pass Gold Nerf + Lootbox RNG Retry** — Phases 290-297 (shipped 2026-05-18; closure signal `MILESTONE_V42_AT_HEAD_81d7c94bc924edb3429f6dc16ee33280fc11c7c2`; 0 of 0 F-42-NN; 1 Tier-1 ACCEPT_AS_DOCUMENTED on (xiv) retryLootboxRng; KNOWN_ISSUES_UNMODIFIED) — see [milestones/v42.0-ROADMAP.md](milestones/v42.0-ROADMAP.md)
- ✅ **v43.0 Total rngLock Determinism Audit — Every VRF Input Frozen at Commitment** — Phases 298-303 (shipped 2026-05-19; closure signal `MILESTONE_V43_AT_HEAD_8111cfc5189f628b64b500c881f9995c3edf0ed2`; 0 of 0 F-43-NN; 111 of 111 CATALOG_VIOLATIONS DEFERRED_TO_V44; 142-anchor v44.0 handoff register; KNOWN_ISSUES_UNMODIFIED)
- ✅ **v44.0 sStonk Per-Day Redemption Refactor + Accounting Invariant Proof** — Phases 304-308 (shipped 2026-05-20; closure signal `MILESTONE_V44_AT_HEAD_6f0ba2963a10654ba554a8c333c5ee80c54a8349`; 7 of 7 SSTONK_VIOLATIONS RESOLVED_AT_V44; 13 of 13 INVARIANTS PROVEN; 20 of 20 EDGE_CASES TESTED; 0 NEW_FINDINGS; KNOWN_ISSUES_UNMODIFIED; 135-anchor v45.0+ handoff register) — see `audit/FINDINGS-v44.0.md`
- ✅ **v45.0 VRF-Rotation Liveness Fix + Consolidate-Forward Delta Audit** — Phases 309-315 (shipped 2026-05-23, user-approved minimal close; closure signal `MILESTONE_V45_AT_HEAD_62fb514bfcc8ad042a45cef960e5ff0ff6fbb801`; VRF_ROTATION_ORPHAN RESOLVED_AT_V45; ROTATION_LIVENESS PRESERVED; FREEZE_INVARIANT INTACT_UNDER_ROTATION; 10 of 10 VRF_CLUSTER_ANCHORS RESOLVED; CONSOLIDATE_FORWARD_DELTA AUDITED; 0 NEW_FINDINGS; KNOWN_ISSUES_UNMODIFIED; AUDIT-01 formal FINDINGS-v45.0.md WAIVED per user)
- 🚧 **v46.0 Do-Work Crank + AfKing Auto-Rebuy Subscription + Legacy AFKing/ETH-Auto-Rebuy Removal** — Phases 316-320 (started 2026-05-23; combined ADD+REMOVE in one batched diff / one test pass / one audit; ADD = permissionless do-work crank + `AfKing` auto-rebuy subscription (`StreakKeeperV2` moved in-tree) wired via PROTO-01..05; REMOVE = legacy in-game AFKing mode + free ETH auto-rebuy (BURNIE flip recycle KEPT @ flat 75bps) + the folded-in JGAS jackpot two-call ETH-split removal enabled by the ETH-auto-rebuy removal (single-call @305-winner ceiling, no EV change); audit baseline → subject `MILESTONE_V45_AT_HEAD_62fb514bfcc8ad042a45cef960e5ff0ff6fbb801` → v46.0 closure HEAD)

## Phases

**Active milestone: v46.0 Do-Work Crank + AfKing Auto-Rebuy Subscription + Legacy AFKing/ETH-Auto-Rebuy Removal** — Phases 316-320 (started 2026-05-23). Phase directories live under `.planning/phases/` for the active milestone; archived milestones under `.planning/milestones/v{X.Y}-phases/`. Prior-milestone phases archived in the collapsible blocks below.

**Goal:** Ship the permissionless do-work crank and the AfKing auto-rebuy subscription (`StreakKeeperV2` moved in-tree as `AfKing`, wired via PROTO-01..05), and in the SAME batched diff remove the legacy in-game AFKing mode + free ETH auto-rebuy it succeeds — one source-tree change, one test pass, one adversarial audit, one `MILESTONE_V46_AT_HEAD_<sha>` closure. The removal is a **prerequisite** for the subscription's reinvest mode (the old free auto-rebuy intercepts winnings before they reach `claimable`; the subscription reads *from* claimable — combining avoids a coexistence window where both act on winnings). The freed daily-ETH-path gas from that ETH-auto-rebuy removal also funds the **JGAS** folded-in deletion of the jackpot two-call ETH split (`STAGE_JACKPOT_ETH_RESUME` + `resumeEthPool` across `DegenerusGameJackpotModule` + `DegenerusGameAdvanceModule`) at the SAME 305-winner ceiling — pure gas simplification, no winner-count/EV change, gated on a worst-case-first gas check at SPEC. Non-negotiable closure verdict (target): `CRANK_DO_WORK SHIPPED; AFKING_SUBSCRIPTION SHIPPED; LEGACY_AFKING_MODE + FREE_ETH_AUTOREBUY REMOVED; BURNIE_FLIP_AUTOREBUY KEPT@75BPS; FAUCET_BOUNDED; SWEEP NON-BRICK + CONCURRENT-SAFE; FUNDING_WATERFALL + TWO-TIER_SKIP-KILL CORRECT; RNG_FREEZE_INTACT (+ obligations RETIRED by removal); JACKPOT_ETH_SPLIT REMOVED (single-call fits @305-ceiling); WWXRP_ZERO_REWARD; 0 NEW_FINDINGS; KNOWN_ISSUES_UNMODIFIED`.

**Audit baseline → subject:** v45.0 closure HEAD `MILESTONE_V45_AT_HEAD_62fb514bfcc8ad042a45cef960e5ff0ff6fbb801` → v46.0 closure HEAD. Subject = the batched ADD+REMOVE diff across `DegenerusGame` + modules + `BurnieCoin`/`BurnieCoinflip` + `DegenerusVault` + `StakedDegenerusStonk` + `ContractAddresses` + the in-tree `AfKing` keeper (paired `degenerus-utilities` rework). Design locked in `.planning/PLAN-CRANK-DO-WORK-INCENTIVE.md` (ADD half) + `.planning/PLAN-V47-REMOVE-AFKING-ETH-AUTOREBUY.md` (REMOVE half, grep-verified deletion footprint).

**Scope & posture:** Single batched USER-APPROVED contract diff at the IMPL phase per `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_no_contract_commits.md` + `feedback_manual_review_before_push.md` — present ONE diff at phase end, get ONE approval; never pre-approve. ContractAddresses.sol is the lone freely-modifiable mainnet contract per `feedback_contractaddresses_policy.md`; every other `contracts/*.sol` edit is in the one batched approval. AGENT-COMMITTED test/planning/docs per `feedback_no_contract_commits.md` (`.planning/` is gitignored — force-add). Pre-launch redeploy-fresh per `feedback_frozen_contracts_no_future_proofing.md` (storage-layout break fine, no migration). Confirm direction after the sensitive contract phase per `feedback_pause_at_contract_phase_boundaries.md`. All "by construction" / "single fn reaches all paths" call-graph claims grep-verified against source pre-patch per `feedback_verify_call_graph_against_source.md`. Security/RNG-non-manipulability is the hard floor per `feedback_security_over_gas.md`. **Out of scope** (per `.planning/REQUIREMENTS.md`): system-chore cranks (advanceGame/jackpot); degenerette payout-EV / placement changes; bet/box ledger storage re-key (OPEN-D bet-cursor deferred); liquid-BURNIE rewards; off-chain indexer/webpage; deity-pass utilities outside the BURNIE recycle bonus; OPEN-E shared-funding-source (promote at SPEC only if wanted); deployed-state migration (none — pre-launch).

### Phase 316: SPEC — Crank + Subscription + Legacy-Removal Design Lock (SPEC)

**Goal:** Lock the FULL add+remove design BEFORE any contract change and grep-verify every call-graph claim against contract HEAD per `feedback_verify_call_graph_against_source.md`. Produce `.planning/phases/316-*/316-SPEC.md` covering BOTH halves so the milestone is one batched diff / one test pass / one audit. **ADD design:** the do-work entry signatures + work-type encoding (grouped `(player, ids)` off-chain-discovered lists; bets = caller-list with `BatchAlreadyTaken` collision short-circuit per CRANK-02; boxes = parameterless cursor per OPEN-D); the reward formula `gasUnits(workType)·0.5 gwei → BURNIE` via `_ethToBurnieValue` + per-work-type gas-peg constants reserved (numbers calibrated at Phase 319, OPEN-A) + price-unavailable edge → reward 0 never revert (OPEN-B); coinflip-credit deferred-mint reward batched to one `creditFlip`/cranker/tx; `batchPurchase` shape + per-player try/catch/slice-refund + batch-level `rngLocked`/game-over pre-check + reentrancy disposition (CEI proof vs guard, OPEN-C); the cursor sweep design (`sweep(maxCount)` + daily `sweepCursor` self-partition + tombstone-on-cancel + in-sweep swap-pop reclaim + `windowPaid` bit + stall-escalating bounty); authorization (subscribe-time self-or-operator consent, never at sweep) + pass-OR-pay gate via `hasAnyLazyPass`; the **quantity model** (flat `dailyQuantity` uint8 min-1 COEXIST with optional `reinvestPct` uint8, effective = `max(flat, floor(claimable×pct/price))`, one flags byte + `reinvestPct`, no new slot); the **funding waterfall** (claimable→pool→`InsufficientPool` skip via the existing `drainGameCreditFirst=true` model; claimable-only = empty `_poolOf`, no new flag); the **two-tier skip-kill** (NORMAL subs cancelled on a funding skip via swap-pop, Vault/sDGNRS EXEMPT keyed on un-spoofable pinned address identity); the protocol-owned subs at init (sDGNRS claimable-only flat-1 + 2% reinvest + BURNIE auto-rebuy takeProfit=0; Vault claimable-only flat-1); the 5 PROTO additions with exact signatures + gating on the pinned `AF_KING` constant. **REMOVE design + reconciliation:** lock PROTO-01/RM-04 as "KEEP+EXPOSE `_hasAnyLazyPass`, delete the rest of afKing"; the RM-01..06 deletion footprint (afKing mode surface, `AutoRebuyState` storage, jackpot `_processAutoRebuy`/`_calcAutoRebuy` + entropy-param drop, BURNIE recycle collapse to flat 75bps dropping the deity tier, Vault `gameSet*` wrappers + sStonk `setAfKingMode` init replaced by the keeper self-subscribe, interface decls); the storage-layout slot-constant re-derivation plan; the VRF-freeze-obligation retirement (the ETH auto-rebuy was a VRF consumer + 3 player-mutable in-window inputs). **JGAS jackpot-split-removal gate (JGAS-01):** the RM-02 removal of the per-winner `autoRebuyState` SLOAD + `_processAutoRebuy` branch from `_addClaimableEth` frees gas on the daily-ETH path — derive the theoretical worst-case single-call daily-ETH-jackpot gas after that removal (max scale `DAILY_JACKPOT_SCALE_MAX_BPS=63_600` → `DAILY_ETH_MAX_WINNERS=305` winners 159/95/50/1, all 4 buckets, ONE call) worst-case-FIRST per `feedback_gas_worst_case.md`; trace the two-call split's design intent before deletion per `feedback_design_intent_before_deletion.md`; DECISION GATE — fits-with-margin → lock removal of the split spanning `DegenerusGameJackpotModule` (`SPLIT_*`/`resumeEthPool`/`_resumeDailyEth`/`call1Bucket`/`splitMode`/the `JACKPOT_MAX_WINNERS` threshold branch) AND `DegenerusGameAdvanceModule` (`STAGE_JACKPOT_ETH_RESUME` + resume-check), else retain+document; the 305 winner ceiling is PRESERVED (split mechanism only, no winner-count/EV change). **Resolve the SPEC-open items:** claimable-only confirm, "1 price lootbox" denomination, whale-pass-expiry renewal funding for protocol subs, OPEN-D box-cursor / bet-caller-list dispositions, the two-tier skip-kill identity. Zero `contracts/` + zero `test/` mutations. **Requirements:** locks all 42 (SPEC-OPEN resolution owner per the design-lock primary mapping below; downstream IMPL/TST/GAS phases consume the locks).

**Depends on:** Nothing new (v45.0 closure artifacts + `.planning/PLAN-CRANK-DO-WORK-INCENTIVE.md` + `.planning/PLAN-V47-REMOVE-AFKING-ETH-AUTOREBUY.md` + `.planning/REQUIREMENTS.md` + the `degenerus-utilities` `StreakKeeperV2` source + current contract HEAD are read-only inputs at baseline `MILESTONE_V45_AT_HEAD_62fb514bfcc8ad042a45cef960e5ff0ff6fbb801`)

**Requirements (primary owner — design lock):** SUB-09, RM-04, PROTO-01, JGAS-01 (the cross-half reconciliation + protocol-sub init + the jackpot-split removal decision gate are SPEC-decided here; every other requirement's design is also locked at this phase but its primary verification owner is the downstream phase below)

**Success Criteria** (what must be TRUE):

  1. `316-SPEC.md` exists with the ADD design fully locked — do-work entry signatures + work-type encoding (caller-list bets w/ `BatchAlreadyTaken`, parameterless box cursor per OPEN-D), reward formula + reserved gas-peg constants + price-unavailable→0 edge, `batchPurchase` try/catch/slice-refund shape + reentrancy disposition, cursor sweep (self-partition + tombstone-on-cancel + swap-pop + `windowPaid` + escalating bounty), authorization + pass-OR-pay gate, the 5 PROTO signatures gating on the pinned `AF_KING` constant.
  2. The subscription quantity + funding model is locked — flat `dailyQuantity` (min 1) COEXIST with `reinvestPct` via `max(flat, floor(claimable×pct/price))` in one flags byte + `reinvestPct` (no new slot); the claimable→pool→`InsufficientPool`-skip waterfall (claimable-only = empty `_poolOf`, no new flag); the two-tier skip-kill (NORMAL cancel via swap-pop, Vault/sDGNRS EXEMPT by un-spoofable pinned address identity, never a settable flag).
  3. The protocol-owned subs at init are specified (SUB-09) — sDGNRS claimable-only flat-1 + 2% reinvest + BURNIE auto-rebuy `takeProfit=0`, Vault claimable-only flat-1 no-reinvest; both free-renew via their Whale pass with whale-pass-expiry renewal funding confirmed; "1 price lootbox" denomination resolved.
  4. The REMOVE design + reconciliation is locked — PROTO-01/RM-04 = KEEP+EXPOSE `_hasAnyLazyPass` & delete the rest of afKing (overrides the dead-code instinct); the RM-01..06 deletion footprint enumerated (afKing mode, `AutoRebuyState`, jackpot `_processAutoRebuy`/`_calcAutoRebuy` + entropy-param drop, BURNIE recycle → flat 75bps dropping deity tier, Vault/sStonk cascade + interface decls); storage-layout slot-constant re-derivation plan; VRF-freeze-obligation retirement noted. **The JGAS-01 jackpot-split-removal decision is locked** — the theoretical worst-case single-call 305-winner daily-ETH-jackpot gas derived FIRST, the gate resolved (remove if it fits with margin, else retain+document), and the deletion footprint enumerated + grep-verified across BOTH `DegenerusGameJackpotModule` and `DegenerusGameAdvanceModule` (`STAGE_JACKPOT_ETH_RESUME`); 305 ceiling preserved.
  5. Every cited file:line across `DegenerusGame`/modules/`BurnieCoin`/`BurnieCoinflip`/`DegenerusVault`/`StakedDegenerusStonk`/`ContractAddresses` + the `StreakKeeperV2` keeper grep-verified against source HEAD per `feedback_verify_call_graph_against_source.md`; zero "by construction" / "single fn reaches all paths" claims; the SPEC confirms the keeper does NOT depend on anything RM-* deletes (esp. the lazy-pass sync); zero `contracts/` + zero `test/` mutations.

**Plans:** 5 plans

Plans:
**Wave 1**

- [ ] 316-01-PLAN.md — Lock the ADD-half design: do-work crank entry signatures + work-type encoding, reward/charge model, batchPurchase shape, subscription cursor sweep + authorization + pass-gate, and the 5 PROTO signatures (PROTO-01).

**Wave 2** *(blocked on Wave 1 completion)*

- [ ] 316-02-PLAN.md — Lock the REMOVE footprint + reconciliation: PROTO-01/RM-04 KEEP+EXPOSE _hasAnyLazyPass, the verified RM-01..06 deletion footprint, the **COMPOUNDED RM-02+JGAS storage slot-shift re-derivation plan** (`autoRebuyState`@19 + `resumeEthPool`@33 → slot-≥34 region shifts −2; one combined `forge inspect` pass), and the VRF-freeze-obligation retirement (RM-04).

**Wave 3** *(blocked on Wave 2 completion)*

- [ ] 316-05-PLAN.md — *(NEW — JGAS fold)* Lock the **JGAS-01 jackpot two-call-split-removal decision gate**: the split's design-intent trace (pure gas-ceiling workaround, no EV/fairness load), the theoretical worst-case single-call 305-winner gas derived FIRST (~9-12M vs ~30M, ~2.5-3.3× margin), the locked decision "REMOVE pending JGAS-04 empirical confirmation, RETAIN-fallback documented" (305 ceiling preserved), the grep-verified deletion footprint across both `DegenerusGameJackpotModule` + `DegenerusGameAdvanceModule`, and the J5 VRF/freeze-SAFE verdict (JGAS-01). Cross-references 316-02 for the −2 slot consequence; depends on 316-02.

**Wave 4** *(blocked on Wave 3 completion)*

- [ ] 316-03-PLAN.md — Resolve the open items: quantity model + funding waterfall + two-tier skip-kill by pinned identity, the SUB-09 protocol-owned subs at init, OPEN-B/OPEN-C/denomination, and the user-ratified whale-pass-expiry renewal funding (SUB-09).

**Wave 5** *(blocked on Wave 4 completion)*

- [ ] 316-04-PLAN.md — Author the call-graph attestation table (SC#5, now incl. the JGAS J1 footprint + the two +1 resume-check drifts + the J5 VRF verdict) + the **42-requirement** design-coverage map (FOUR SPEC-owned: PROTO-01/SUB-09/RM-04/JGAS-01) and assemble the final 316-SPEC.md (PROTO-01, RM-04, JGAS-01).

> ✅ **JGAS fold RESOLVED (2026-05-23).** Phase 316 re-planned in scope-addition mode: **316-05-PLAN.md** (Wave 3) added to lock JGAS-01's design (decision gate + design-intent + worst-case-first gas + two-module footprint + VRF verdict); 316-02's storage slot-shift section deepened to the COMPOUNDED `autoRebuyState`@19 + `resumeEthPool`@33 −2 shift (one combined `forge inspect`, no duplicated footprint); 316-04's coverage map extended 38→42 reqs (JGAS-01..04 mapped to phases 316/317/318/319) with the JGAS attestation embedded and JGAS-01 added to the SPEC-owned set. Existing 316-01/316-03 content preserved (JGAS is removal-side). Assembly plan 316-04 remains LAST (Wave 5, depends on all design plans incl. 316-05).

### Phase 317: IMPL — Batched ADD+REMOVE Contract Diff + Paired Keeper Rework (IMPL)

**Goal:** Single batched USER-APPROVED contract diff per `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md` + `feedback_manual_review_before_push.md` — present ONE diff at phase end, get ONE approval; no partial commits; no pre-approval; never told "pre-approved" per `feedback_never_preapprove_contracts.md`. Apply the Phase 316 SPEC as ONE batched `degenerus-audit/contracts/` diff PLUS the paired `degenerus-utilities` `AfKing` keeper rework. **Protocol side (degenerus-audit):** **PROTO-01** expose `hasAnyLazyPass` (KEEP `_hasAnyLazyPass`, delete the rest of afKing — RM-04 reconciliation); **PROTO-02** `BurnieCoin.burnForKeeper` all-or-nothing `onlyAfKing`; **PROTO-03** authorize the keeper in `BurnieCoinflip.onlyFlipCreditors`; **PROTO-04** `DegenerusGame.batchPurchase(players[],amounts[],modes[])` keeper-gated, per-player try/catch + slice-refund, one batch value transfer, batch-level `rngLocked`/game-over pre-checked once; **PROTO-05** pin the `AF_KING` keeper address constant in `BurnieCoin`/`BurnieCoinflip`/`ContractAddresses.sol`; **the crank entry** (CRANK-01..04 + REW-01..04) — permissionless mass do-work over off-chain-discovered `(player,ids)` lists, per-item `onlySelf` self-call + try/catch (skip stale/reverting, reward only successes), bets caller-list w/ `BatchAlreadyTaken` short-circuit, boxes parameterless cursor (OPEN-D), WWXRP `currency==3` zero reward, gas-pegged coinflip-credit batched to one `creditFlip`/tx, fixed `gasUnits` constants never `gasleft()`/`tx.gasprice`, no caller restriction; **the protocol-owned sub init wiring** (SUB-09) — sDGNRS + Vault self-subscribe at their own init; **RM-01..06 removals** — afKing mode surface + `AutoRebuyState` storage + jackpot `_processAutoRebuy`/`_calcAutoRebuy` (+ entropy-param drop so the jackpot credit path no longer consumes a VRF word) + BURNIE recycle collapsed to flat 75bps (deity tier dropped) + Vault `gameSet*` wrappers + sStonk `setAfKingMode` init replaced + `IDegenerusGame`/`IBurnieCoinflip` decls + `settleFlipModeChange`, all grep-clean for `afKing`/`AFKING_` (excl. `contracts/test`+`mocks`); storage-layout slot constants re-derived after the `AutoRebuyState` deletion; ETH winnings always credit to claimable. **JGAS-02 daily-ETH split removal** (if JGAS-01 locked removal) — delete the two-call split in the SAME diff across `DegenerusGameJackpotModule` (`SPLIT_NONE/CALL1/CALL2` constants, `splitMode` param, `call1Bucket` mask routing, `_resumeDailyEth`, the `resumeEthPool` storage slot, the `JACKPOT_MAX_WINNERS` split-threshold branch + the resume-check) AND `DegenerusGameAdvanceModule` (`STAGE_JACKPOT_ETH_RESUME` + its resume-check + stage handler); daily ETH jackpot now completes in ONE advanceGame stage / one call at the 305 ceiling; slot constants re-derived after the `resumeEthPool` deletion (compounds with the `AutoRebuyState` re-derivation); no winner-count/scaling/EV change. **Keeper side (degenerus-utilities):** the `AfKing` rework — cursor sweep (`sweep(maxCount)` + `sweepCursor` + tombstone-on-cancel + swap-pop reclaim + `windowPaid` bit + stall-escalating bounty), `reinvestPct` + `max`-compute added to the sweep, two-tier skip-kill (Vault/sDGNRS EXEMPT by pinned identity), switch the per-player purchase to the game's `batchPurchase`, `subscribe(player,…)` resolve-gate, doc-comment sync + unit-test selector updates + deploy-script pinned-address constants. All cited file:line re-grep-verified against source pre-patch per `feedback_verify_call_graph_against_source.md`. Storage-layout BREAK acceptable (pre-launch redeploy-fresh per `feedback_frozen_contracts_no_future_proofing.md`). Pause to confirm direction at this sensitive-contract boundary per `feedback_pause_at_contract_phase_boundaries.md`. Requirements: PROTO-01..05 + CRANK-01..04 + REW-01..04 + SUB-01..09 + RM-01..06. Wave shape: 1 USER-APPROVED batched `contracts/` commit (final wave `autonomous: false` — USER-APPROVAL checkpoint gates the single diff) + AGENT-COMMITTED keeper/planning/docs.

**Depends on:** Phase 316 SPEC (locked add+remove design + reconciliation + storage-slot re-derivation plan are load-bearing)

**Requirements (primary owner):** PROTO-02, PROTO-03, PROTO-04, PROTO-05, CRANK-01, CRANK-02, CRANK-03, CRANK-04, REW-01, REW-02, REW-03, REW-04, SUB-01, SUB-02, SUB-03, SUB-04, SUB-05, SUB-06, SUB-07, SUB-08, RM-01, RM-02, RM-03, RM-05, RM-06, JGAS-02

**Success Criteria** (what must be TRUE):

  1. `forge build` PASS against the patched contract tree (degenerus-audit) AND the paired keeper compiles (degenerus-utilities); the keeper now self-subscribes sDGNRS + Vault at init (SUB-09 wiring) and calls the game's `batchPurchase`.
  2. The 5 PROTO additions are present and gate on the pinned `AF_KING` constant (PROTO-05) — `hasAnyLazyPass` external view exposed (PROTO-01), `burnForKeeper` all-or-nothing `onlyAfKing` (PROTO-02), keeper authorized in `onlyFlipCreditors` (PROTO-03), keeper-gated `batchPurchase` with per-player try/catch + slice-refund + one batch value transfer + batch-level `rngLocked`/game-over pre-check (PROTO-04).
  3. The crank entry is permissionless + faucet-bounded by construction (CRANK-01..04 + REW-01..04) — per-item `onlySelf` + try/catch isolation, bets caller-list w/ `BatchAlreadyTaken`, boxes parameterless cursor, WWXRP `currency==3` earns zero, reward = `gasUnits·0.5gwei → BURNIE` via `_ethToBurnieValue` paid as one batched `creditFlip`/tx with fixed `gasUnits` constants (never measured gas) and no caller restriction.
  4. RM-01..06 applied — grep for `afKing`/`AFKING_`/`setAutoRebuy`/`autoRebuyState`/`AutoRebuyState`/`_processAutoRebuy`/`_calcAutoRebuy`/`settleFlipModeChange`/`_afKingRecyclingBonus`/`_afKingDeityBonusHalfBpsWithLevel`/`deactivateAfKingFromCoin`/`syncAfKingLazyPassFromCoin` returns ZERO matches outside `contracts/test`+`mocks`; the jackpot ETH credit path no longer consumes a VRF word (entropy param dropped); BURNIE flip auto-rebuy still works end-to-end at flat 75bps with the deity tier gone; `_hasAnyLazyPass` KEPT + exposed (RM-04); storage-layout slot constants re-derived after the `AutoRebuyState` deletion. **JGAS-02** — if JGAS-01 locked removal, grep for `resumeEthPool`/`SPLIT_CALL1`/`SPLIT_CALL2`/`_resumeDailyEth`/`STAGE_JACKPOT_ETH_RESUME`/`call1Bucket` returns ZERO matches outside `contracts/test`+`mocks`, the daily ETH jackpot distributes all 305 winners in ONE call (no resume stage), and slots are re-derived after the `resumeEthPool` deletion.
  5. USER-APPROVED diff committed exactly once per `feedback_batch_contract_approval.md` (never pre-approved per `feedback_never_preapprove_contracts.md`; not pushed before user review of the diff per `feedback_manual_review_before_push.md`); every cited file:line re-grep-verified pre-patch per `feedback_verify_call_graph_against_source.md`; keeper/planning/docs AGENT-COMMITTED.

**Plans:** TBD
**UI hint:** yes

### Phase 318: TST — Subscription + Crank Correctness + Removal Proofs (TST)

**Goal:** Ship Foundry coverage proving BOTH halves against the Phase 317 IMPL. AGENT-COMMITTED test commit(s) per `feedback_no_contract_commits.md` (test/ + planning/ commit autonomously). **ADD coverage:** **SAFE-01** faucet bounded by the three caller-independent locks (purchase-gate + gas-peg + coinflip-credit illiquidity) — self-crank/Sybil round-trip ≤ 0, WWXRP 0 reward, one-reward-per-item via existing delete/zero, batch marginal-gas no base-amortization margin, no resolve before the RNG word lands; **SAFE-02** non-brick (BOTH cranks AND `batchPurchase`) — one reverting/stale/not-ready player skipped via `onlySelf`+try/catch, batch completes + reward only successes, cancel un-brickable, no double-buy reentrancy (in-context sub-call rolls back on revert); **SAFE-03** concurrency — two same-block sweeps self-partition via the advancing cursor + `lastSweptDay`, no double-buy; **SAFE-04** RNG-freeze intact — resolution stays post-unlock (`RngNotReady` guard), placement guard untouched; plus the testable acceptance of subscription correctness (pass-OR-pay + `burnForKeeper` all-or-nothing + coinflip bounty; SUB-*), the crank reward model (REW-*), the protocol-sub funding waterfall + two-tier skip-kill + every-entry-every-day cursor coverage + tombstone-on-cancel no-miss. **REMOVE proofs:** the old afKing/ETH-auto-rebuy paths are gone (grep-clean assertion + behavioral — ETH jackpot winnings always land in claimable, no ticket-conversion path), BURNIE flip recycle = flat 75bps applied unconditionally (no under/over-credit, no deity scaling), storage-layout slot constants re-derived + suite recompiles green with no slot-index drift, `KNOWN_ISSUES` + the BURNIE win/loss RNG path (`processCoinflipPayouts`, `rngWord & 1`) UNMODIFIED. **JGAS-03 jackpot single-call correctness** — prove the daily ETH jackpot pays all 305 winners (buckets 159/95/50/1) correctly in ONE call after the split removal: every bucket paid, exact per-winner amounts, none missed/double-paid, total credited = pool, gas under the block limit; the old split path (`resumeEthPool`/`SPLIT_CALL1/2`/`STAGE_JACKPOT_ETH_RESUME`) grep-clean + behaviorally gone (no resume stage entered). Gas analysis derives the theoretical worst case FIRST per `feedback_gas_worst_case.md` (full numbers calibrated at Phase 319). Requirements: SAFE-01..04 + JGAS-03 + the testable acceptance of SUB-*/CRANK-*/REW-*/RM-* (SAFE-*/JGAS-03 are the primary owners here).

**Depends on:** Phase 317 IMPL (tests target the patched add+remove surface + the reworked keeper)

**Requirements (primary owner):** SAFE-01, SAFE-02, SAFE-03, SAFE-04, JGAS-03

**Success Criteria** (what must be TRUE):

  1. Faucet-resistance PASSES (SAFE-01) — self-crank/Sybil round-trip ≤ 0; WWXRP earns 0; one-reward-per-item enforced by the existing delete/zero; batch marginal-gas carries no base-amortization margin; no resolve succeeds before the RNG word lands.
  2. Non-brick PASSES (SAFE-02) — a reverting/stale/not-ready player is skipped via `onlySelf`+try/catch and the batch completes rewarding only successes (BOTH cranks AND `batchPurchase`); cancel is un-brickable; a reentrant `sweep`/`cancel` cannot double-buy (in-context sub-call rolls back on revert).
  3. Concurrency + every-entry-every-day PASS (SAFE-03) — two same-block sweeps self-partition via the cursor + `lastSweptDay` with no double-buy and no missed entry; tombstone-on-cancel leaves no dead-slot buildup; subscription correctness (pass-OR-pay, `burnForKeeper` all-or-nothing, gas-pegged coinflip bounty, funding waterfall, two-tier skip-kill incl. Vault/sDGNRS exemption) proven.
  4. RNG-freeze intact (SAFE-04) — resolution stays post-unlock behind the `RngNotReady` guard, the placement guard is untouched, and the removal proofs hold: old afKing/ETH-auto-rebuy paths grep-clean + ETH jackpot winnings always credit to claimable, BURNIE recycle = flat 75bps unconditional (no deity scaling, no under/over-credit). **JGAS-03** — the daily ETH jackpot pays all 305 winners correctly in ONE call (every bucket, exact amounts, none missed/double-paid, gas under limit) and the split path is behaviorally gone (no `STAGE_JACKPOT_ETH_RESUME` entered).
  5. `forge build` + the full suite PASS — storage-layout slot constants re-derived with no slot-index drift; `KNOWN_ISSUES` + the BURNIE win/loss RNG path (`processCoinflipPayouts`, `rngWord & 1`) byte-UNMODIFIED; AGENT-COMMITTED test commit(s) per `feedback_no_contract_commits.md`.

**Plans:** TBD

### Phase 319: GAS — Worst-Case-First Gas Pass + 0.5 gwei Peg Calibration (GAS)

**Goal:** Maximal gas-efficiency within the security floor per `feedback_security_over_gas.md` + `feedback_maximal_variable_packing.md`. **GAS-01** worst-case-FIRST measurement per work-type (derive the theoretical worst case — one degenerette bet × `MAX_SPINS_PER_BET=10` × all-match → 10× lootbox materialization; per-player sweep — THEN measure it, per `feedback_gas_worst_case.md`); **GAS-02** one `creditFlip`/cranker/tx + one batch value transfer + `level`/`mintPrice` read once per batch verified; **GAS-03** calldata grouped by player + homogeneous per-work-type fns; **GAS-04** maximal storage packing (`Sub` + any new state to tightest cap-bounded widths) with NO new per-bet/box storage on the hot placement path; **GAS-05** Scavenger + Skeptic pass — every removal/packing validated against the security-floor guardrails (the `RngNotReady` resolve guard + placement twin, the delete/zero one-reward-per-item + double-crank guard, `batchPurchase` per-player try/catch, `burnForKeeper` all-or-nothing, keeper/address gating — never optimized away); verify swap-pop/no-`++i` integrity + bounded tombstone overhead; **GAS-06** regression bounds (placement hot-path +0%) + the measured worst-cases calibrate the `gasUnits`/`*_ETH_TARGET` 0.5 gwei peg constants (closes OPEN-A; the SPEC reserved them); **JGAS-04** empirically measure the worst-case single-call 305-winner daily-ETH-jackpot gas on the patched tree, confirm the JGAS-01 theoretical derivation + the margin under the block gas limit, and attribute the enabling delta to the removed per-winner `autoRebuyState` SLOAD + `_processAutoRebuy` branch (folds into the GAS-01 worst-case-first pass). Requirements: GAS-01..06 + JGAS-04.

**Depends on:** Phase 317 IMPL + Phase 318 TST (measure the patched surface + its coverage; calibrate the reserved peg constants)

**Requirements (primary owner):** GAS-01, GAS-02, GAS-03, GAS-04, GAS-05, GAS-06, JGAS-04

**Success Criteria** (what must be TRUE):

  1. Theoretical worst case derived FIRST then measured per work-type (resolve-bet, open-box, sweep-per-player) per `feedback_gas_worst_case.md` (GAS-01); the worst-case bet item (`MAX_SPINS_PER_BET=10`, all-match → 10× lootbox materialization) is the measured cost center. **JGAS-04** — the worst-case single-call 305-winner daily-ETH-jackpot gas is measured on the patched tree, confirms JGAS-01's theoretical derivation with margin under the block gas limit, and the enabling delta is attributed to the removed per-winner `autoRebuyState` SLOAD + branch.
  2. The batched-reward levers hold (GAS-02 + GAS-03) — one `creditFlip`/cranker/tx, one batch value transfer, `level`/`mintPrice` read once per batch, calldata grouped by player with homogeneous per-work-type fns.
  3. Maximal storage packing applied (GAS-04) — `Sub` + any new state packed to the tightest cap-bounded widths per `feedback_maximal_variable_packing.md`; NO new per-bet/box storage on the hot placement path.
  4. Scavenger + Skeptic pass complete (GAS-05) — every removal/packing validated against the security floor; the resolve/placement freeze guards, one-reward delete/zero, `batchPurchase` try/catch, `burnForKeeper` all-or-nothing, and keeper/address gating are all preserved (never optimized away per `feedback_security_over_gas.md`); swap-pop/no-`++i` integrity + bounded tombstone overhead verified.
  5. Regression bounds met (GAS-06) — placement hot-path +0%; the measured worst-cases calibrate the `gasUnits`/`*_ETH_TARGET` 0.5 gwei peg constants (OPEN-A closed) and those values land in the contract (folded into the Phase 317 diff or a follow-up USER-APPROVED constant-tune if numbers shifted).

**Plans:** TBD

### Phase 320: AUDIT — Adversarial Sweep + Add/Remove Delta Audit + Closure (TERMINAL)

**Goal:** SOURCE-TREE FROZEN — zero `contracts/` + zero `test/` mutations during Phase 320 (unless a Tier-1 user-approved or Tier-2 auto-elevated FINDING_CANDIDATE triggers a RE-PASS per `D-284-ADVERSARIAL-RE-PASS-01`). **Adversarial sweep** — `/economic-analyst` + `/zero-day-hunter` (and `/contract-auditor` per the established 3-skill HYBRID pattern; `/degen-skeptic` OUT OF SCOPE per `D-271-ADVERSARIAL-02`) charged with: composition + subscription-trigger griefing + coinflip-credit recycle + BURNIE-supply interaction + the `burnForKeeper`/`creditFlip`/`batchPurchase` authority surface + the two-tier skip-kill identity (can a NORMAL sub spoof the Vault/sDGNRS exemption? can a funding-margin griefer force-cancel a fragile claimable-only sub?) + faucet round-trip + the REMOVE surface (can removing the ETH auto-rebuy branch strand winnings? can the BURNIE 75bps collapse under/over-credit? can removing the two-call jackpot split strand a payout via the dropped `resumeEthPool` carry, or under/over-pay a bucket at the 305-winner single-call ceiling?). Skeptic-reviewer filter per `feedback_skeptic_pass_before_catastrophe.md` (structural-protection check + 3-condition EV lens) BEFORE any user-pause; two-tier consensus (Tier-1 any-skill FINDING_CANDIDATE → AskUserQuestion PAUSE; Tier-2 3-of-3 → auto-elevation + RE-PASS). **Delta-audit** every v45→v46 `contracts/` change — the batched ADD+REMOVE diff together — confirming the add surfaces (PROTO/CRANK/REW/SUB) and the remove surfaces (RM + the JGAS jackpot two-call-split removal) compose cleanly, no orphaned winnings, no double-credit; the daily ETH jackpot pays all 305 winners in one call with no payout stranded by the dropped `resumeEthPool` carry. **Regression** — RNG-freeze intact under permissionless resolve + the freeze obligations RETIRED by the ETH-auto-rebuy removal (one fewer VRF consumer + three fewer player-mutable in-window inputs); faucet bounded; v44.0/v45.0 closure surfaces NON-WIDENING; `KNOWN_ISSUES` + the BURNIE win/loss RNG path UNMODIFIED. Emit `MILESTONE_V46_AT_HEAD_<sha>` + atomic 5-doc closure flip (ROADMAP + STATE + MILESTONES + PROJECT + REQUIREMENTS) per `D-NN-CLOSURE-01` carry. Closure verdict (target): `CRANK_DO_WORK SHIPPED; AFKING_SUBSCRIPTION SHIPPED; LEGACY_AFKING_MODE + FREE_ETH_AUTOREBUY REMOVED; BURNIE_FLIP_AUTOREBUY KEPT@75BPS; FAUCET_BOUNDED; SWEEP NON-BRICK + CONCURRENT-SAFE; FUNDING_WATERFALL + TWO-TIER_SKIP-KILL CORRECT; RNG_FREEZE_INTACT (+ obligations RETIRED by removal); JACKPOT_ETH_SPLIT REMOVED (single-call fits @305-ceiling); WWXRP_ZERO_REWARD; 0 NEW_FINDINGS; KNOWN_ISSUES_UNMODIFIED`. Requirements: the cross-cutting acceptance / closure verdict over all 42 (this phase attests the milestone; SAFE-04's freeze-retirement attestation + the JGAS jackpot-split-removal delta + the full add/remove composition land here as the terminal verdict — no requirement is primary-owned here, every requirement is re-attested at closure).

**Depends on:** All prior v46.0 phases (316 SPEC + 317 IMPL + 318 TST + 319 GAS)

**Requirements (primary owner):** none — TERMINAL re-attests all 42 at closure (cross-cutting acceptance / closure verdict)

**Success Criteria** (what must be TRUE):

  1. Adversarial-charge authored + dispositioned — `/economic-analyst` + `/zero-day-hunter` (+ `/contract-auditor` per the 3-skill HYBRID pattern, PARALLEL_SUBAGENT or HYBRID-fallback per executor Task-tool availability; `/degen-skeptic` OUT per `D-271-ADVERSARIAL-02`) cover composition, subscription-trigger griefing, coinflip-credit recycle, BURNIE-supply interaction, the `burnForKeeper`/`creditFlip`/`batchPurchase` authority surface, the two-tier skip-kill identity, faucet round-trip, and the REMOVE surface; skeptic-reviewer filter per `feedback_skeptic_pass_before_catastrophe.md` applied BEFORE any Tier-1 user-pause; two-tier consensus honored.
  2. The add+remove delta-audit is complete — every v45→v46 `contracts/` change audited together; the add surfaces (PROTO/CRANK/REW/SUB) and remove surfaces (RM + the JGAS jackpot two-call-split removal across both modules) compose cleanly with no orphaned/double-credited winnings; the daily ETH jackpot pays all 305 winners in one call with nothing stranded by the dropped `resumeEthPool` carry.
  3. Regression PASS — RNG-freeze intact under permissionless resolve AND the freeze obligations RETIRED by the ETH-auto-rebuy removal (one fewer VRF consumer + three fewer player-mutable in-window inputs); faucet bounded; v44.0 + v45.0 closure surfaces NON-WIDENING; `KNOWN_ISSUES` + the BURNIE win/loss RNG path UNMODIFIED.
  4. §9 closure verdict reads `CRANK_DO_WORK SHIPPED; AFKING_SUBSCRIPTION SHIPPED; LEGACY_AFKING_MODE + FREE_ETH_AUTOREBUY REMOVED; BURNIE_FLIP_AUTOREBUY KEPT@75BPS; FAUCET_BOUNDED; SWEEP NON-BRICK + CONCURRENT-SAFE; FUNDING_WATERFALL + TWO-TIER_SKIP-KILL CORRECT; RNG_FREEZE_INTACT (+ obligations RETIRED by removal); JACKPOT_ETH_SPLIT REMOVED (single-call fits @305-ceiling); WWXRP_ZERO_REWARD; 0 NEW_FINDINGS; KNOWN_ISSUES_UNMODIFIED`.
  5. `MILESTONE_V46_AT_HEAD_<sha>` emitted + atomic 5-doc closure flip (ROADMAP + STATE + MILESTONES + PROJECT + REQUIREMENTS); SOURCE-TREE FROZEN attestation (zero `contracts/` + zero `test/` mutations during Phase 320 absent a RE-PASS) verifiable via `git diff` returning no in-phase source-tree change.

**Plans:** TBD

<details>
<summary>✅ v45.0 VRF-Rotation Liveness Fix + Consolidate-Forward Delta Audit (Phases 309-315) — SHIPPED 2026-05-23 (user-approved minimal close); closure signal `MILESTONE_V45_AT_HEAD_62fb514bfcc8ad042a45cef960e5ff0ff6fbb801`</summary>

**Goal:** Close the CATASTROPHE-class VRF-rotation orphan-index liveness defect in `updateVrfCoordinatorAndSub` (and the governance-VRF freeze-violation cluster it overlaps), audit the already-landed degenerette refactor, and consolidate-audit every contract change since v44.0 into one closure signal at current HEAD. Consolidate-forward (redefined 2026-05-22): phases 309 (SPEC) + 310 (IMPL `9bcd582d`) retained as completed V-081 lootbox EV-cap groundwork; active work = the VRF-rotation orphan-index liveness fix + §9d governance-VRF cluster HANDOFF-78/85/86/87/88/89/90/91 + ADMA-01/02, the degenerette-refactor audit, and a single delta-audit of every contract change since v44.0.

**Audit baseline → subject:** v44.0 closure HEAD `MILESTONE_V44_AT_HEAD_6f0ba2963a10654ba554a8c333c5ee80c54a8349` → v45.0 closure HEAD `62fb514bfcc8ad042a45cef960e5ff0ff6fbb801`. Subject = every `contracts/` commit in the delta: V-081 `9bcd582d` (+ EV-freeze `e5928fb8`), jackpot pending-pool `6e5acd7e` + regression `f3e21064`, degenerette `92b110bf`, VRF-rotation fix `a303ae18`.

- [x] **Phase 309: SPEC — V-081 Locked Layout + Bonus-Only Cap + Shared-Cap Disposition (SPEC)** — COMPLETE (groundwork; 2/2 plans; `309-SPEC.md`). Locked the V-081 packed-slot layout (SPEC-01), bonus-only cap semantics (SPEC-02), allocation-time tally + open-time application (SPEC-03), shared-cap disposition backward-trace (SPEC-04). Zero `contracts/` + `test/` mutations.
- [x] **Phase 310: IMPL — V-081 Single Batched USER-APPROVED Diff (IMPL)** — COMPLETE (groundwork; 3/3 plans; USER-APPROVED contract commit `9bcd582d`). Bonus-only cap in `_applyEvMultiplierWithCap` + packed `uint256` snapshot + purchase-time cap tally + frozen open-time application across `DegenerusGameStorage.sol` + `DegenerusGameLootboxModule.sol` + `DegenerusGameMintModule.sol` + `DegenerusGameWhaleModule.sol`. IMPL-01..05.
- [x] **Phase 311: SPEC — VRF-Rotation Liveness Fix (SPEC)** — COMPLETE (2/2 plans; `311-SPEC.md`). Design-intent backward-trace of `updateVrfCoordinatorAndSub` + `wireVrf` across Scenario A (same-day → entropy-0 traits) + Scenario B (next-day → freeze + forced game-over); LOCKED re-issue-in-flight fix shape (VRF-01/02/03 closing HANDOFF-78/85/87/89/91) + `wireVrf` one-shot lock (VRF-04 closing HANDOFF-86/88/90 + ADMA-01) + vault-routed reach (VRF-05 / ADMA-02). Validator-influenceable entropy backfill REJECTED per `feedback_security_over_gas.md`. Zero `contracts/` + `test/` mutations.
- [x] **Phase 312: IMPL — VRF-Rotation Fix (Single Batched USER-APPROVED Diff) (IMPL)** — COMPLETE (1/1 plan; USER-APPROVED contract commit `a303ae18` to `DegenerusGameAdvanceModule.sol`). `_requestVrfWord` + `_setVrfConfig` helpers + `wireVrf` routed through `_setVrfConfig` + the `updateVrfCoordinatorAndSub` 3-case preserve+re-issue rework (VRF-01/02/03/05). VRF-04 `wireVrf` init-only lock OMITTED — user-approved deviation: `wireVrf` is one-shot by construction (reachable only from the DegenerusAdmin constructor).
- [x] **Phase 313: TST — VRF Regression + Freeze-Invariant Fuzz Under Rotation (TST)** — COMPLETE (7 plans across 3 waves; AGENT-COMMITTED; zero `contracts/` mutation). VTST-01 orphan-index reproduction + VTST-02 liveness-after-rotation + VTST-03 freeze-invariant fuzz under rotation (extends the v43 `RngLockDeterminism.t.sol` harness) + VTST-04 `wireVrf` one-shot lock (access-guard form per the no-init-lock deviation). VTST-01..04 prove VRF-01..05. Review-driven cleanup (313-07) corrected vacuous slot reads + a fuzz reachability test bug. SC-5 proven empirically (0 NEW failures vs pre-fix `41546f16`).
- [x] **Phase 314: SWEEP — 3-Skill Adversarial + Degenerette Audit (SWEEP)** — COMPLETE (1/1 plan; AGENT-COMMITTED `314-01-ADVERSARIAL-LOG.md`). 3-skill — `/contract-auditor` SEQUENTIAL_MAIN_CONTEXT + `/zero-day-hunter` ‖ `/economic-analyst` GENUINE PARALLEL_SUBAGENT; `/degen-skeptic` OUT per `D-271-ADVERSARIAL-02`. **unanimous-NEGATIVE** — 33 disposition rows (26 NEGATIVE-VERIFIED + 7 SAFE_BY_DESIGN); 0 FINDING_CANDIDATE; Task 6 RE-PASS gate SKIPPED; 0 skeptic-filter discards. Degenerette audit (DGAUD-01..04) folded into the `/contract-auditor` LOG per D-05: recompile-clean + dangling-ref grep ZERO, `dailyHeroWagers` behavioral identity, `BetPlaced` off-chain reconstruction viable, HANDOFF-01/02/03/18/81/82 carry-forward.
- [x] **Phase 315: TERMINAL — Consolidate-Forward Delta Audit + Closure (TERMINAL)** — **CLOSED VIA MINIMAL CLOSE** (2026-05-23, user-approved; NOT run as a full TERMINAL phase). Closure substance discharged inline: REG-01 PASS (non-widening — every `contracts/`+`test/` diff vs `MILESTONE_V44_AT_HEAD_6f0ba296…` attributable to a v45-scope commit) + the closure flip (signal `MILESTONE_V45_AT_HEAD_62fb514bfcc8ad042a45cef960e5ff0ff6fbb801`; ROADMAP/STATE/MILESTONES/PROJECT/REQUIREMENTS updated). **AUDIT-01 (the formal `audit/FINDINGS-v45.0.md` 9-section deliverable + `chmod 444`) WAIVED per user** — adversarial disposition lives in `.planning/phases/314-*/314-01-ADVERSARIAL-LOG.md` (unanimous-NEGATIVE); VRF attestation in 312/313 VERIFICATION + VTST-01..04. To produce the publishable findings report later, re-open this phase.

**Result:** 30/31 requirements satisfied; 1 WAIVED (AUDIT-01). Closure verdict `VRF_ROTATION_ORPHAN RESOLVED_AT_V45; ROTATION_LIVENESS PRESERVED; FREEZE_INVARIANT INTACT_UNDER_ROTATION; 10 of 10 VRF_CLUSTER_ANCHORS RESOLVED; CONSOLIDATE_FORWARD_DELTA AUDITED (V-081 + jackpot-pending-pool + degenerette); 0 NEW_FINDINGS; KNOWN_ISSUES_UNMODIFIED; CLOSED_VIA_MINIMAL_CLOSE`. 1 USER-APPROVED contract commit this milestone (`a303ae18` VRF fix; the V-081/jackpot/degenerette changes already landed user-committed). See `.planning/MILESTONES.md` for the full v45.0 archive entry.

</details>

<details>
<summary>✅ v44.0 sStonk Per-Day Redemption Refactor + Accounting Invariant Proof (Phases 304-308) — SHIPPED 2026-05-20; closure signal `MILESTONE_V44_AT_HEAD_6f0ba2963a10654ba554a8c333c5ee80c54a8349`</summary>

### v44.0 Phases (shipped 2026-05-20)

**Goal:** FIX MILESTONE. Eliminate the V-184 sStonk cross-day re-roll exploit + 6 subsumed catalog rows (V-186/V-188/V-190/V-191/V-192/V-193 — FIXREC §0.6 fan-out, HANDOFF-111..117) by structurally redesigning sStonk gambling-burn redemption storage. Replace `redemptionPeriodIndex` + single-pool `pendingRedemptionEthBase` / `pendingRedemptionBurnieBase` with per-day keyed `pendingByDay[uint32]` mapping matching the existing lootbox/coinflip per-id commitment pattern. **Prove via 12 formal accounting invariants + 18 exhaustive edge-case tests that the resulting contract is 100% non-manipulable by any non-EXEMPT actor under all timing combinations.** Non-negotiable closure verdict: `7 of 7 SSTONK_VIOLATIONS RESOLVED_AT_V44; 12 of 12 INVARIANTS PROVEN; 18 of 18 EDGE_CASES TESTED; 0 NEW_FINDINGS; KNOWN_ISSUES_UNMODIFIED`.

**Audit baseline:** v43.0 closure HEAD `MILESTONE_V43_AT_HEAD_8111cfc5189f628b64b500c881f9995c3edf0ed2`.

**Narrow scope:** v44.0 closes ONLY the sStonk cluster (7 anchors: HANDOFF-111..117). Remaining 135 v43 FIXREC + ADMA + ERRATUM anchors defer to v45.0+ per `audit/FINDINGS-v43.0.md` §9d handoff register. USER-APPROVED single batched contract commit at Phase 305 IMPL per `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md`; AGENT-COMMITTED test/planning commits per `D-43N-TEST-COMMITS-AUTO-01` lineage. Pre-launch posture preserved per `feedback_frozen_contracts_no_future_proofing.md` — storage layout breaks acceptable; no migration concerns.

### Phase 304: SPEC + Invariant Model (SPEC) — COMPLETE (shipped 2026-05-19; 5/5 plans; 304-SPEC.md 960 lines)

**Goal:** Formally document the per-day redemption refactor design BEFORE any contract change. Produce `.planning/phases/304-*/304-SPEC.md` covering: (1) INV-01..12 each stated as a precise formal accounting property with the storage variables involved + the state transitions across which the property must hold; (2) SPEC-01..05 locked design decisions — `pendingByDay[uint32]` struct shape (`{uint256 ethBase; uint256 burnieBase; uint128 supplySnapshot; uint128 burned}`), composite-key `pendingRedemptions[player][day]` mapping, explicit `dayToResolve` arg on `resolveRedemptionPeriod`, gameOver-mid-pending semantics, supply-snapshot lazy-init timing, storage-refund (`delete`) sites; (3) EDGE-01..18 exhaustive scenario enumeration (one row per case with positive + negative assertions stated narratively); (4) design-intent backward-trace + actor game-theory walk across all timing/state combos before locking any deletion per `feedback_design_intent_before_deletion.md` (the 5 storage slots being removed — `redemptionPeriodIndex`, `redemptionPeriodSupplySnapshot`, `redemptionPeriodBurned`, `pendingRedemptionEthBase`, `pendingRedemptionBurnieBase` — each get traced + game-theory walked); (5) every cited `contracts/StakedDegenerusStonk.sol` file:line + `contracts/modules/DegenerusGameAdvanceModule.sol` call-site grep-verified against source per `feedback_verify_call_graph_against_source.md` (no "by construction" claims). Zero contract changes; zero test changes. Output is the load-bearing input for Phase 305 IMPL. Requirements: INV-01..12 (12) + SPEC-01..05 (5) + EDGE-01..18 (18) — 35 requirements covered at SPEC phase (INV + EDGE coverage continues at Phase 306 + Phase 308 §3.F).

**Depends on:** Nothing (v43.0 closure artifacts — FINDINGS-v43.0.md §9d + RNGLOCK-FIXREC.md §103 + ADMIN-AUDIT.md — are read-only inputs at baseline `MILESTONE_V43_AT_HEAD_8111cfc5189f628b64b500c881f9995c3edf0ed2`)

**Success Criteria** (what must be TRUE):

  1. `.planning/phases/304-*/304-SPEC.md` exists with §1 INV-01..12 each stated as a precise formal property + storage-variable + state-transition enumeration.
  2. §2 SPEC-01..05 design decisions LOCKED with rationale (struct shape, composite-key mapping, `dayToResolve` arg shape, gameOver semantics, snapshot timing, storage-refund sites).
  3. §3 EDGE-01..18 scenarios each have a positive assertion + a negative (revert-or-no-exploit) assertion stated narratively, ready for Phase 306 to mechanically turn into Foundry fuzz functions.
  4. §4 design-intent + actor game-theory walk per `feedback_design_intent_before_deletion.md` covers all 5 storage-slot deletions (`redemptionPeriodIndex`, `redemptionPeriodSupplySnapshot`, `redemptionPeriodBurned`, `pendingRedemptionEthBase`, `pendingRedemptionBurnieBase`) + the `UnresolvedClaim` revert deletion + the `redemptionPeriodIndex` reset block deletion.
  5. §5 every cited file:line in `contracts/StakedDegenerusStonk.sol` + `contracts/modules/DegenerusGameAdvanceModule.sol` grep-verified against source HEAD per `feedback_verify_call_graph_against_source.md`; zero "by construction" or "covered by single fn" claims.

**Plans:** 5 plans

- [x] 304-01-PLAN.md — skeleton + §0 header + §1 INV-01..12 formal invariant model (shipped 2026-05-19; commits `5a5e1034` + `46b16273`; SUMMARY `.planning/phases/304-spec-invariant-model-spec/304-01-SUMMARY.md`)
- [x] 304-02-PLAN.md — §2 SPEC-01..05 locked design decisions (struct shape + composite-key + dayToResolve arg + gameOver/zero-rounded/refund-timing sub-locks + 50% supply cap snapshot timing) (shipped 2026-05-19; commit `6edc3967`; SUMMARY `.planning/phases/304-spec-invariant-model-spec/304-02-SUMMARY.md`)
- [x] 304-03-PLAN.md — §3 EDGE-01..18 exhaustive scenario enumeration (positive + negative assertions; EDGE-07 V-184 attack reproduction with structural-closure byte-identity assertion) (shipped 2026-05-19; commits `315280b0` + `971688ba`; SUMMARY `.planning/phases/304-spec-invariant-model-spec/304-03-SUMMARY.md`)
- [x] 304-04-PLAN.md — §4 design-intent backward-trace + actor game-theory walk for 7 deletions (5 storage slots + UnresolvedClaim revert + redemptionPeriodIndex reset block) (shipped 2026-05-19; commits `20f3d439` + `d3c2aea5`; SUMMARY `.planning/phases/304-spec-invariant-model-spec/304-04-SUMMARY.md`)
- [x] 304-05-PLAN.md — §5 source-verified citation manifest (grep-verify every file:line vs StakedDegenerusStonk.sol + DegenerusGameAdvanceModule.sol HEAD; document all 3 sStonk resolve call sites) (shipped 2026-05-19; commit `20ec70cc`; SUMMARY `.planning/phases/304-spec-invariant-model-spec/304-05-SUMMARY.md`; 50 sStonk + 11 AdvanceModule citations grep-verified — 61 VERIFIED / 0 CORRECTED / 0 ABSENT; all THREE inline-duplicated resolveRedemptionPeriod call sites attested at :1230 + :1293 + :1323; 6 forbidden-lexicon claims reframed in §1-§4; FOOTER LINE appended; Phase 304 COMPLETE)

### Phase 305: Implementation (IMPL)

**Goal:** Single batched USER-APPROVED contract diff per `feedback_batch_contract_approval.md` + `feedback_never_preapprove_contracts.md`. Refactor `contracts/StakedDegenerusStonk.sol` per Phase 304 SPEC: remove 5 storage slots (`redemptionPeriodIndex`, `redemptionPeriodSupplySnapshot`, `redemptionPeriodBurned`, `pendingRedemptionEthBase`, `pendingRedemptionBurnieBase`); add `mapping(uint32 => DayPending) internal pendingByDay` with the locked struct shape; change `pendingRedemptions` to composite key `mapping(address => mapping(uint32 => PendingRedemption))`; drop the `redemptionPeriodIndex` reset block at `:758-762`; drop the `UnresolvedClaim` revert at `:796-797`; update `_submitGamblingClaimFrom` to write `pendingByDay[currentDayView()]` slots with lazy-init supply snapshot; update `resolveRedemptionPeriod` to accept `uint32 dayToResolve` arg + read `pendingByDay[dayToResolve]` + write `redemptionPeriods[dayToResolve]` + `delete pendingByDay[dayToResolve]` for storage refund; update `claimRedemption(uint32 day)` to read composite-keyed mapping + `delete pendingRedemptions[msg.sender][day]` after payout. Update `contracts/modules/DegenerusGameAdvanceModule.sol` call site (around `:1230` per FIXREC §103.A.4) to pass `dayToResolve = currentDayView - 1` (or locked equivalent per SPEC). Update `IDegenerusGamePlayer` interface only if minimum required by SPEC. Storage layout BREAKS acceptable (pre-launch redeploy-fresh posture per `feedback_frozen_contracts_no_future_proofing.md`). All cited file:line grep-verified against source pre-patch per `feedback_verify_call_graph_against_source.md`. Requirements IMPL-01..04 (4). Wave shape: 1 USER-APPROVED batched contract commit (`contracts/StakedDegenerusStonk.sol` + `contracts/modules/DegenerusGameAdvanceModule.sol` + optional `IDegenerusGamePlayer` interface delta; single diff presented at end of phase per `feedback_batch_contract_approval.md`).

**Depends on:** Phase 304 SPEC (locked design decisions are load-bearing)

**Success Criteria** (what must be TRUE):

  1. `forge build` PASS against the patched contract tree.
  2. Storage layout diff vs v43.0 baseline shows the 5 slots removed + `pendingByDay` mapping added + `pendingRedemptions` re-keyed; storage breaks ACCEPTED per pre-launch posture (no migration scaffolding).
  3. `_submitGamblingClaimFrom` writes burns to `pendingByDay[currentDayView()]` (per-day pool); cumulative `pendingRedemptionEthValue` global continues to track the cross-day sum unchanged.
  4. `resolveRedemptionPeriod(uint16 roll, uint32 flipDay, uint32 dayToResolve)` accepts the day arg + writes `redemptionPeriods[dayToResolve]` + deletes `pendingByDay[dayToResolve]`; `AdvanceModule` call site passes `dayToResolve = currentDayView - 1` (or SPEC-locked equivalent).
  5. `claimRedemption(uint32 day)` reads composite-keyed `pendingRedemptions[msg.sender][day]` + `redemptionPeriods[day]`; `UnresolvedClaim` revert at `:796-797` REMOVED; `delete pendingRedemptions[msg.sender][day]` after payout for storage refund. USER-APPROVED diff committed exactly once per `feedback_batch_contract_approval.md`.

**Plans:** 1 plan

Plans:

- [x] 305-01-PLAN.md — Single batched USER-APPROVED contract diff per Phase 304 SPEC (pre-patch grep re-verification + 14 source mutations across 5 files: contracts/StakedDegenerusStonk.sol + contracts/modules/DegenerusGameAdvanceModule.sol + contracts/interfaces/IStakedDegenerusStonk.sol + test/fuzz/RedemptionGas.t.sol + test/fuzz/CoverageGap222.t.sol)

### Phase 306: Test (TST)

**Goal:** Ship Foundry coverage proving every INV-01..12 + EDGE-01..18 against the Phase 305 IMPL refactor. 3 new test files + 1 modified existing harness: (a) `test/fuzz/StakedStonkRedemption.t.sol` — per-function fuzz suite covering burn/advance/claim flows at 10k runs per case under `FOUNDRY_PROFILE=deep`; (b) `test/invariant/RedemptionAccounting.t.sol` — Foundry invariant harness with stateful handler emitting random action sequences from {burn, advance, claim, gameOver, transfer, approve, admin-action}, with one `invariant_*` function per INV-01..12 asserted after every handler call; (c) `test/fuzz/RedemptionEdgeCases.t.sol` — one fuzz function per EDGE-NN scenario with positive + negative assertions per the Phase 304 SPEC enumeration; (d) modify `test/fuzz/RngLockDeterminism.t.sol` HANDOFF-111..117 `vm.skip(true)` blocks → remove the skip + assert strict byte-identity (the 7 previously-skipped cases that demonstrate the V-184 mechanic at v43.0 HEAD must PASS at v44.0 close). TST-04 EDGE-07 V-184 attack reproduction is the headline negative test (player A burns day D → day-D+1 advance resolves with R_{D+1} → attacker burns 1 wei post-resolve → day-D+2 advance fires → ASSERT `redemptionPeriods[D].roll` byte-identical to first resolution; no overwrite). Gas regression: burn-path gas ≤ +5% of v43.0 baseline; claim-path gas ≤ +0% (per-day keying makes claim simpler). AGENT-COMMITTED test commit per `D-43N-TEST-COMMITS-AUTO-01` (only mainnet `contracts/*.sol` require explicit approval per `feedback_no_contract_commits.md` clarified policy; test/ + .planning/ + docs commit autonomously). Requirements: TST-01..07 (7) + EDGE-01..18 (18) — INV-01..12 are concurrently proven via TST-02 invariant harness.

**Depends on:** Phase 305 IMPL (tests target the refactored contract surface)

**Success Criteria** (what must be TRUE):

  1. `test/fuzz/StakedStonkRedemption.t.sol` PASSES at 10k runs per case under `FOUNDRY_PROFILE=deep` (`testFuzz_BurnLandsInCurrentDayPool` + `testFuzz_ResolveWritesCorrectDay` + `testFuzz_ClaimReadsCorrectDay` + `testFuzz_MultipleSameDayBurnsAggregate` + `testFuzz_SupplyCapEnforced` + `testFuzz_EvCapEnforced`).
  2. `test/invariant/RedemptionAccounting.t.sol` PASSES with 12 `invariant_*` functions (one per INV-NN) asserted after every handler call across {burn, advance, claim, gameOver, transfer, approve, admin-action} random action sequences at default invariant depth + runs.
  3. `test/fuzz/RedemptionEdgeCases.t.sol` PASSES with one fuzz function per EDGE-01..18 scenario; positive paths assert correct outcome; negative paths assert revert OR no-exploit. EDGE-07 (V-184 attack reproduction) explicitly asserts `redemptionPeriods[D].roll` byte-identical across post-resolution write attempts.
  4. `test/fuzz/RngLockDeterminism.t.sol` HANDOFF-111..117 7 `vm.skip(true)` blocks REMOVED + strict byte-identity assertion in place; all 7 previously-skipped fuzz cases PASS at 10k runs per case under `FOUNDRY_PROFILE=deep` (the v43.0 vm.skip → v44.0 strict-assertion flip is the load-bearing closure assertion for HANDOFF-111..117).
  5. `forge build` PASS + `FOUNDRY_PROFILE=deep forge test --match-path "test/{fuzz,invariant}/**"` PASS + gas regression assertions PASS (burn ≤ +5% v43, claim ≤ +0% v43). AGENT-COMMITTED batched test commit per `D-43N-TEST-COMMITS-AUTO-01`.

**Plans:** 5 plans

Plans:

- [x] 306-01-PLAN.md — Foundry invariant harness — RedemptionAccounting (13 invariant_INV_NN_* fns + RedemptionHandler v44 refresh; INV-01..13 + TST-02 + TST-07) — COMPLETE (commit `de75f620`; PROVEN at FOUNDRY_PROFILE=deep × 1000 runs × 256 depth = 256000 calls per invariant; zero failures)
- [x] 306-02-PLAN.md — Edge fuzz coverage — RedemptionEdgeCases (20 testFuzz_EDGE_NN_* fns incl V-184 reproduction + multi-day stall + dust floor; TST-03 + TST-04 + EDGE-01..20)
- [x] 306-03-PLAN.md — Per-function fuzz — StakedStonkRedemption (6 ROADMAP-canonical + 2 ACL/sentinel testFuzz_* fns; TST-01)
- [x] 306-04-PLAN.md — RngLockDeterminism vm.skip flip (HANDOFF-111 strict-assertion + 6 subsumed catalog rows close structurally per FIXREC §0.6; TST-05 + REG-01)
- [x] 306-05-PLAN.md — Gas regression bench — RedemptionGas (theoretical worst case first + v43 baseline capture + assertion limits; TST-06)

### Phase 307: Adversarial Sweep (SWEEP)

**Goal:** 3-skill HYBRID adversarial pass per `D-302-INVOKE-01` precedent: `/contract-auditor` SEQUENTIAL_MAIN_CONTEXT + `/zero-day-hunter` + `/economic-analyst` PARALLEL_SUBAGENT. Pre-authorized per `D-44N-SWEEP-PREAUTH-01` (locked at Phase 304 SPEC signoff). `/degen-skeptic` OUT OF SCOPE per `D-271-ADVERSARIAL-02` carry. `/economic-analyst` IN SCOPE per `D-271-ADVERSARIAL-03` carry. Charged with: SWP-01 `/contract-auditor` finds any state transition violating INV-01..12; any (burn, advance, claim, gameOver) interleaving producing exploitable outcome; any storage-collision or packing bug in the new layout. SWP-02 `/zero-day-hunter` finds novel attack surfaces — composition with lootbox/coinflip flows; ERC20 callback-induced re-entry on transfer paths; cross-module read/write races between sStonk and DegenerusGame storage. SWP-03 `/economic-analyst` finds game-theoretic write-induced effects under the per-day model; coordinated-burn scenarios; timing arbitrage between gap burns vs post-advance burns; MEV surfaces on the new state machine. SWP-04 two-tier consensus per `D-302-CONSENSUS-01`: Tier 1 any-skill FINDING_CANDIDATE → AskUserQuestion PAUSE; Tier 2 3-of-3 consensus → automatic elevation + RE-PASS per `D-284-ADVERSARIAL-RE-PASS-01` against any FIXREC-augment diff. SWP-05 disposition table per skill with NEGATIVE-VERIFIED / FINDING_CANDIDATE / SAFE_BY_DESIGN classification — **skeptic-reviewer filter per `feedback_skeptic_pass_before_catastrophe.md` (structural-protection check + 3-condition EV lens) MUST be applied BEFORE any user-pause is invoked**. Requirements SWP-01..05 (5). Wave shape: 1 AGENT-COMMITTED `.planning/phases/307-*/307-01-ADVERSARIAL-LOG.md` artifact bundle (CHARGE + 3 per-skill MDs + integrated LOG + Disposition section).

**Depends on:** Phase 305 IMPL + Phase 306 TST (adversaries probe the implementation AND the test coverage simultaneously)

**Success Criteria** (what must be TRUE):

  1. `.planning/phases/307-*/307-ADVERSARIAL-CHARGE.md` authored with SWP-01..05 verbatim charges + any augments; `/contract-auditor` SEQUENTIAL_MAIN_CONTEXT executed against the charge with disposition table.
  2. `/zero-day-hunter` disposition table complete (PARALLEL_SUBAGENT spawn if executor has Task tool; HYBRID-fallback to SEQUENTIAL_MAIN_CONTEXT per v42 P296 / v43 P302 precedent acceptable; persona fidelity preserved via dedicated per-skill MD file with verbatim CHARGE).
  3. `/economic-analyst` disposition table complete with charged + beyond-charge hypothesis rows; game-theoretic + MEV surface enumeration documented.
  4. Skeptic-reviewer filter applied per `feedback_skeptic_pass_before_catastrophe.md` BEFORE any Tier-1 user-pause: structural-protection check + 3-condition EV lens; only items surviving the filter route to AskUserQuestion. Two-tier consensus rule per `D-302-CONSENSUS-01` honored.
  5. Integrated `.planning/phases/307-*/307-01-ADVERSARIAL-LOG.md` artifact bundle AGENT-COMMITTED; zero `contracts/` + zero `test/` mutations during Phase 307 unless a Tier-1 user-approved or Tier-2 auto-elevated FINDING_CANDIDATE triggers a FIXREC-augment commit (RE-PASS discipline per `D-284-ADVERSARIAL-RE-PASS-01` if it lands).

**Plans:** Plan 01 of 1 — Phase 307 SWEEP COMPLETE (unanimous-NEGATIVE; 72/72 disposition rows; 0 FINDING_CANDIDATE; 3 SAFE_BY_DESIGN; Task 6 elevation gate SKIPPED per D-307-ELEVATION-ROUTING-01 precondition fail; HYBRID-fallback to SEQUENTIAL_MAIN_CONTEXT for hunter+economist per Task tool unavailability — v43 P302 + v42 P296 precedent extended).

### Phase 308: Delta Audit + Findings Consolidation (TERMINAL)

**Goal:** SOURCE-TREE FROZEN — zero `contracts/` + zero `test/` mutations during Phase 308. Single-file deliverable `audit/FINDINGS-v44.0.md` 9-section TERMINAL: §1 + §2 + §3 (a-f) + §3.A delta-surface table enumerating the Phase 305 USER-APPROVED contract commit + every AGENT-COMMITTED test/audit/planning commit + §3.B per-exempt-entry-point attestation matrix (3 exempt entry points × per-participating-slot row including the sStonk-specific row for `redemptionPeriods[day].roll` exempt writer = `resolveRedemptionPeriod`) + §3.C conservation re-proof (every INV-01..12 each attested as proven by a specific TST-NN / EDGE-NN test ID) + §3.D V-184 disposition (explicit RESOLVED-AT-V44 attestation; HANDOFF-111..117 all closed; cross-reference to TST-04 + TST-05 + EDGE-07) + §3.E remaining v43 backlog reference (135 anchors deferred to v45.0+ via v43.0 §9d handoff register; v44.0 does not consume them) + **§3.F formal invariant attestation matrix NEW (v44.0-specific) with (INV-NN, test_id, status) rows × 12 invariants; status = PROVEN / WAIVED-with-rationale / FAILING-blocks-closure** + §4 adversarial-pass disposition (every SWP-01..03 hypothesis with verdict; skeptic-reviewer filter results) + §5 LEAN regression REG-01 (v43.0 closure non-widening — every v43.0 audit-subject surface byte-identical at v44.0 close EXCEPT the Phase 301 `vm.skip(HANDOFF-111..117)` lines flipped to strict assertions, intended diff attested in §3.A) + §6 KI walkthrough (EXC-01..04 RE_VERIFIED-NEGATIVE-scope at v44 close; v44 audit subject is sStonk redemption refactor with zero affiliate-roll / AdvanceModule game-over-RNG-substitution interaction beyond sStonk-internal; KNOWN-ISSUES.md UNMODIFIED per `D-44N-KI-01`) + §7 prior-artifact cross-cites + §8 forward-cite closure (zero post-milestone references per `D-NN-FCITE-01` carry → `D-44N-FCITE-01`; pickup-pointers via locked-decision IDs only) + §9 closure attestation `7 of 7 SSTONK_VIOLATIONS RESOLVED_AT_V44; 12 of 12 INVARIANTS PROVEN; 18 of 18 EDGE_CASES TESTED; 0 NEW_FINDINGS; KNOWN_ISSUES_UNMODIFIED` + v45.0+ handoff register (135 remaining v43 anchors per `D-43N-V44-HANDOFF-NN` excluding the 7 closed by v44.0 + 22 ADMA + 1 ADMA-ERRATUM). 2-commit AGENT-COMMITTED sequential SHA orchestration per `D-44N-CLOSURE-01` (precedent `D-303-CLOSURE-01` + `D-297-CLOSURE-01` + `D-284-CLOSURE-01`): Commit 1 ships audit deliverable with `<commit-1-sha>` placeholder + planner-private bundle; Commit 2 resolves placeholder + propagates verbatim to 5 FINDINGS verbatim locations + 3 cross-doc targets + `chmod 444 audit/FINDINGS-v44.0.md` + atomic 5-doc closure flip (ROADMAP/STATE/MILESTONES/PROJECT/REQUIREMENTS). Closure signal `MILESTONE_V44_AT_HEAD_<commit-1-sha>` propagated atomically. Pre-authorized per `D-44N-CLOSURE-PREAUTH-01` (user grants closure-flip authorization at Phase 304 SPEC signoff; eliminates Tier-1 ping at TERMINAL commit-2). Requirements AUDIT-01..09 (9) + REG-01 (1) + CLS-01..02 (2).

**Depends on:** All prior v44.0 phases (304 + 305 + 306 + 307)

**Success Criteria** (what must be TRUE):

  1. `audit/FINDINGS-v44.0.md` 9-section deliverable shipped with §3.A delta-surface complete + §3.B per-exempt-entry-point attestation complete + §3.C INV-01..12 attested as proven by specific test IDs + §3.D V-184 RESOLVED-AT-V44 disposition explicit + §3.E v43 backlog reference + §3.F formal invariant attestation matrix (12 rows × `(INV-NN, test_id, status)`) all PROVEN.
  2. §4 Phase 307 adversarial-pass disposition table complete (skeptic-filter results documented); §5 REG-01 v43.0 non-widening PASS (Phase 301 vm.skip flip is the only intended diff, attested in §3.A); §6 KI walkthrough complete; EXC-01..04 RE_VERIFIED-NEGATIVE-scope at v44.
  3. §9 closure attestation reads `7 of 7 SSTONK_VIOLATIONS RESOLVED_AT_V44; 12 of 12 INVARIANTS PROVEN; 18 of 18 EDGE_CASES TESTED; 0 NEW_FINDINGS; KNOWN_ISSUES_UNMODIFIED`; v45.0+ handoff register lists the 135 remaining v43 anchors verbatim.
  4. 2-commit AGENT-COMMITTED sequential SHA orchestration per `D-44N-CLOSURE-01` executed: Commit 1 ships deliverable with `<commit-1-sha>` placeholder; Commit 2 resolves placeholder + propagates verbatim to 5 FINDINGS verbatim locations + 3 cross-doc propagation targets + `chmod 444 audit/FINDINGS-v44.0.md` + atomic 5-doc closure flip.
  5. Closure signal `MILESTONE_V44_AT_HEAD_<commit-1-sha>` propagated atomically across ROADMAP + STATE + MILESTONES + PROJECT + REQUIREMENTS; KNOWN-ISSUES.md UNMODIFIED per `D-44N-KI-01`; SOURCE-TREE FROZEN attestation (zero `contracts/` + zero `test/` mutations during Phase 308) per `git diff HEAD~2 HEAD -- contracts/ test/` returning no output.

**Plans:** 1 plan

Plans:

- [x] 308-01-PLAN.md — Ship `audit/FINDINGS-v44.0.md` 9-section TERMINAL deliverable per `D-308-TASK-SPLIT-01` (13 tasks; 2 AGENT-COMMITTED commits per `D-44N-CLOSURE-01`) — SHIPPED 2026-05-20; Commit 1 `6f0ba296` (deliverable) + Commit 2 closure flip; closure signal `MILESTONE_V44_AT_HEAD_6f0ba2963a10654ba554a8c333c5ee80c54a8349`; verdict 7/7 + 13/13 + 20/20 + 0 NEW + KNOWN_ISSUES_UNMODIFIED per `D-308-INV-COUNT-01`; SOURCE-TREE FROZEN (zero contracts/ + zero test/)

</details>

<details>
<summary>✅ v43.0 Total rngLock Determinism Audit — Every VRF Input Frozen at Commitment (Phases 298-303) — SHIPPED 2026-05-19; closure signal `MILESTONE_V43_AT_HEAD_8111cfc5189f628b64b500c881f9995c3edf0ed2`</summary>

**Goal:** AUDIT MILESTONE. Catalog every storage slot that participates in deriving any VRF-influenced output at `rngLockedFlag = true`; enumerate every external/public/admin writer of every participating slot; classify (slot × writer × callsite) verdicts; recommend per-VIOLATION remediation tactics; prove the freeze invariant empirically via a Foundry fuzz harness; 3-skill HYBRID adversarial sweep across the catalog. **Three explicit exempt entry points:** (1) `advanceGame()` and reachable resolution flow; (2) VRF coordinator callback delivering `randomness`; (3) `retryLootboxRng()` failsafe (`D-42N-RETRY-RNG-DOMAIN-SEP-01` Option A accepted). **No SAFE_BY_DESIGN escape hatch for participating slots** — "could possibly affect" = theoretical reachability; eliminate even if economic likelihood is LOW. **Audit-only posture (`D-43N-AUDIT-ONLY-01` user-authorization 2026-05-18):** zero `contracts/` mutations across the entire milestone; `test/` mutations permitted only at Phase 301 FUZZ harness with `vm.skip`-on-known-failing strategy (CI green; v44 flips skips). Actual contract remediations deferred to **v44.0 FIX-MILESTONE** which consumes Phase 298 CATALOG + Phase 299 FIXREC + Phase 300 ADMA artifacts as load-bearing input.

**Audit baseline:** v42.0 closure HEAD `MILESTONE_V42_AT_HEAD_81d7c94bc924edb3429f6dc16ee33280fc11c7c2`.

- [x] **Phase 298: VRF Read-Graph Catalog (CATALOG)** — COMPLETE 2026-05-18. Catalog phase backward-traces from every VRF consumer site per `feedback_rng_backward_trace.md`. For each consumer, walks every reachable SLOAD inside the resolution code path; explicit enumeration with file:line citation per `feedback_verify_call_graph_against_source.md`. Produces per-(slot × writer × callsite) verdict table with EXEMPT-ADVANCEGAME / EXEMPT-VRFCALLBACK / EXEMPT-RETRYLOOTBOXRNG / VIOLATION classification per tuple. 13 parallel sub-agents per consumer per D-298-EXEC-SHAPE-01. Output: `.planning/RNGLOCK-CATALOG.md` artifact + entry in FINDINGS-v43.0.md §3.A. Requirements: CAT-01..06 (6). Wave shape: 1 AGENT-COMMITTED catalog artifact bundle. **Zero `contracts/` + `test/` mutations.** 14 plans (13 Wave-1 per-consumer sub-agent dispatches + 1 Wave-2 aggregation per D-298-EXEC-SHAPE-01).

- [x] **Phase 299: Fix Recommendation Document (FIXREC)** — COMPLETE 2026-05-18. Audit-only repurpose per `D-43N-AUDIT-ONLY-01`. For each VIOLATION tuple in Phase 298 catalog, produces a per-VIOLATION analysis entry covering: design-intent backward-trace + actor game-theory walk + recommended remediation tactic (a/b/c/d menu) with full rationale + bytecode/storage-layout/public-ABI impact estimate + v44.0 FIX-MILESTONE handoff anchor. Output: `.planning/RNGLOCK-FIXREC.md` artifact + FINDINGS-v43.0.md §3.D entry. Requirements: FIXREC-01..05 (5). Wave shape: 1 AGENT-COMMITTED FIXREC artifact bundle. **Zero `contracts/` + `test/` mutations.** 11 plans (10 Wave-1 parallel cluster contributions by participating-slot family + 1 Wave-2 aggregation). **Depends on Phase 298.**

- [x] **Phase 300: Admin Path Enumeration Audit (ADMA)** — COMPLETE 2026-05-18. Audit-only repurpose per `D-43N-AUDIT-ONLY-01`. Enumerates every `onlyOwner` / `onlyAdmin` / role-gated external function across all modules in `contracts/`. Cross-references with Phase 298 CAT-03 writer table; produces per-admin-function recommendation entry. Output: `.planning/ADMIN-AUDIT.md` artifact + FINDINGS-v43.0.md §3.E entry. Requirements: ADMA-01..04 (4). Wave shape: 1 AGENT-COMMITTED ADMA artifact bundle. **Zero `contracts/` + `test/` mutations.** 1 plan (single Wave-1 main-context end-to-end AGENT-COMMITTED ADMA artifact bundle). **Depends on Phase 298.**

- [x] **Phase 301: State-Shuffle Determinism Fuzz Harness (FUZZ)** — COMPLETE 2026-05-18. Foundry harness ships `test/fuzz/RngLockDeterminism.t.sol` (1,778 lines, 18 fuzz functions) fuzzing randomized action sequences mid-rngLock window. `vm.skip` strategy per `D-43N-FUZZ-VMSKIP-01` — cases that demonstrably reproduce a CATALOG VIOLATION at current contract state are `vm.skip`-gated so CI passes green at v43.0 closure; v44.0 FIX-MILESTONE flips skips to assertions. Requirements: FUZZ-01..05 (5). Wave shape: 1 AGENT-COMMITTED batched test commit (test-tree only; no `contracts/` mutations) per `D-43N-TEST-COMMITS-AUTO-01`. 6 plans across 2 waves. **Depends on Phases 298 + 299 + 300.**

- [x] **Phase 302: Cross-Surface Adversarial Sweep (SWEEP)** — COMPLETE 2026-05-19. ZERO_FINDING_ELEVATION (9 charged hypotheses + 7 beyond-charge; skeptic-reviewer filter → 5 ALREADY-DOCUMENTED REAL_EXPLOITs + 2 documentation-fix items + 1 coverage-gap; user fast-path 2026-05-19 5/5 accept-as-documented; documentation-class items routed to Phase 303 §6 catalog hygiene; FUZZ-harness 3 missing edge-cases deferred to v44.0 FIX-MILESTONE). HYBRID-fallback to SEQUENTIAL_MAIN_CONTEXT for all 3 skills per v42 P296 precedent. Zero `contracts/` + zero `test/` mutations preserved; KNOWN-ISSUES.md UNMODIFIED per `D-302-KI-01`. Requirements: SWP-01..05 (5). Wave shape: 1 AGENT-COMMITTED adversarial-log artifact. 1 plan. **Depends on Phases 298 + 299 + 300 + 301.**

- [x] **Phase 303: Delta Audit + Findings Consolidation (TERMINAL)** — COMPLETE 2026-05-19. SOURCE-TREE FROZEN preserved. 2-commit sequential SHA orchestration per `D-303-CLOSURE-01`. Closure signal `MILESTONE_V43_AT_HEAD_8111cfc5189f628b64b500c881f9995c3edf0ed2` propagated to 5 FINDINGS verbatim locations + 3 cross-document propagation targets + chmod 444 applied. 9-section deliverable shipped with §9d 142-anchor v44.0 FIX-MILESTONE handoff register (119 D-43N-V44-HANDOFF-NN + 22 D-43N-V44-ADMA-NN + 1 D-43N-V44-ADMA-ERRATUM-01). Verdict: `111 of 111 CATALOG_VIOLATIONS DEFERRED_TO_V44; 0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_UNMODIFIED` per `D-303-VERDICT-01`. Pre-authorized AGENT-COMMITTED autonomous closure per `D-43N-CLOSURE-PREAUTH-01`. Requirements: AUDIT-01..09 (9) + REG-01..04 (4) + CLS-01..02 (2). Wave shape: 2 AGENT-COMMITTED commits. 1 plan. **Depends on all prior v43.0 phases.**

**Phase count:** 6 phases (298-303) FIXED per `D-43N-AUDIT-ONLY-01` audit-only pivot.

</details>

<details>
<summary>✅ v42.0 Mint-Batch Event/Sig Cleanup + Hero-Override Weighted Roll + Deity-Pass Gold Nerf + Lootbox RNG Retry (Phases 290-297) — SHIPPED 2026-05-18</summary>

- [x] Phase 290: Mint-Batch Event/Sig Cleanup (MINTCLN) (2/2 plans) — completed 2026-05-17 (USER-APPROVED contract commit `e5665117` — MINTCLN-01..10 cleanup; 3-input keccak + owed-in-baseKey collapse; BREAKING `TraitsGenerated` topic-hash per MINTCLN-04 / D-42N-EVT-BREAK-01)
- [x] Phase 291: MINTCLN Regression Fixture (TST-MINTCLN) (2/2 plans) — completed 2026-05-17 (USER-APPROVED test commit `a1404efd` — TST-MINTCLN-01..05)
- [x] Phase 292: Hero-Override Weighted Roll (HRROLL) (2/2 plans) — completed 2026-05-17 (USER-APPROVED contract commit `a0218952` — weighted-roll with ×1.5 leader bonus + no floor + bit-slice non-collision per D-42N-COLOR-ENTROPY-01)
- [x] Phase 293: HRROLL Regression Fixture (TST-HRROLL) (2/2 plans) — completed 2026-05-17 (USER-APPROVED test commit `0cd01a9c` — TST-HRROLL-01..06 + JS-replay oracle; 1 RELAX override on TST-HRROLL-06 user-accepted)
- [x] Phase 294: Deity-Pass Gold Nerf (DPNERF) (2/2 plans) — completed 2026-05-18 (USER-APPROVED initial commit `47936e0c` + BURNIE gap-closure amendment `38319463` per D-294-CALLER-UNIFORM-01; gold-tier flat-1 virtualCount on `color == 7` at both ETH + BURNIE paths)
- [x] Phase 295: DPNERF Regression Fixture (TST-DPNERF) (1/1 plans) — completed 2026-05-18 (USER-APPROVED test commit `8027b16c` — TST-DPNERF-01..05; 1 RELAX override on TST-DPNERF-03 user-accepted)
- [x] Phase 296: Cross-Surface Adversarial Sweep (SWEEP) (1/1 plans) — completed 2026-05-18 (USER-APPROVED mid-sweep feature commit `123f2dac` retryLootboxRng + AGENT-COMMITTED LOG bundle `f2bf0767`; 3-skill HYBRID adversarial pass; ZERO_FINDING + 1 Tier-1 ACCEPT_AS_DOCUMENTED on (xiv))
- [x] Phase 297: Delta Audit + Findings Consolidation (Terminal) (1/1 plans) — completed 2026-05-18 (AGENT-COMMITTED Commit 1 `81d7c94b` audit deliverable + Commit 2 `27f828cb` closure flip; `audit/FINDINGS-v42.0.md` FINAL READ-only chmod 444; closure signal `MILESTONE_V42_AT_HEAD_81d7c94bc924edb3429f6dc16ee33280fc11c7c2` propagated to 5 FINDINGS locations + 3 cross-doc targets)

**Audit baseline:** v41.0 closure HEAD `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` → v42.0 closure HEAD `81d7c94bc924edb3429f6dc16ee33280fc11c7c2` resolved at Phase 297 Commit 1 per D-297-CLOSURE-01. 8-phase surface-pair + sweep + terminal shape (3 contract surfaces MINTCLN/HRROLL/DPNERF + 1 mid-sweep contract surface RETRY_LOOTBOX_RNG; 3 paired test phases + 1 SWEEP + 1 terminal audit). 5 USER-APPROVED contract commits + 4 USER-APPROVED test commits + 1 AGENT-COMMITTED Phase 296 LOG bundle + 2 AGENT-COMMITTED Phase 297 terminal commits (Commit 1 deliverable + Commit 2 closure flip). 60/60 requirements satisfied (10 MINTCLN + 5 TST-MINTCLN + 10 HRROLL + 6 TST-HRROLL + 6 DPNERF + 5 TST-DPNERF + 5 SWEEP + 9 AUDIT + 4 REG). **Result:** 0 of 0 F-42-NN findings (zero F-NN milestone; clean closure verdict math per D-297-VERDICT-01); 1 Tier-1 ACCEPTED_AS_DOCUMENTED on Phase 296 hypothesis (xiv) retryLootboxRng entropy-correlation under daily-flow-takeover composition (user disposition 2026-05-18; intended design; documented at §4.2 + §3.C 4th conservation invariant + §9.NN `ADVERSARIAL_TIER_1_RESOLVED` register entry). 4 audit-subject surfaces: MINTCLN + HRROLL + DPNERF + RETRY_LOOTBOX_RNG. Adversarial pass: 3-skill HYBRID (/contract-auditor SEQUENTIAL_MAIN_CONTEXT + /zero-day-hunter + /economic-analyst PARALLEL_SUBAGENT per user authorization 2026-05-18); 14 charged hypotheses + 8 beyond-charge entries; 13 of 14 CLEAR first-tier; Tier-2 (3-of-3 consensus) did NOT trigger; RE-PASS not triggered per D-296-REPASS-SCOPE-01; /degen-skeptic OUT OF SCOPE per D-271-ADVERSARIAL-02 carry. LEAN regression: REG-01 (v41.0 NON-WIDENING) + REG-02 (v40.0 NON-WIDENING) + REG-03 (v34.0 NON-WIDENING) + REG-04 (prior-finding spot-check) all PASS. KI envelopes: EXC-01..03 RE_VERIFIED-NEGATIVE-scope at v42; EXC-04 STRUCTURALLY ELIMINATED preserved (no `entropyStep` resurrection at v42 close HEAD). **KNOWN-ISSUES.md UNMODIFIED** per D-297-KI-01 (carry from v41 D-281-KI-01). Closure verdict `0 of 0 F-42-NN RESOLVED_AT_V42; 0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_UNMODIFIED`. Deliverable: `audit/FINDINGS-v42.0.md` (FINAL READ-only at HEAD `81d7c94b`, 9 sections, chmod 444). See [milestones/v42.0-ROADMAP.md](milestones/v42.0-ROADMAP.md) and [milestones/v42.0-REQUIREMENTS.md](milestones/v42.0-REQUIREMENTS.md) and [milestones/v42.0-MILESTONE-AUDIT.md](milestones/v42.0-MILESTONE-AUDIT.md). Phase directories archived to [milestones/v42.0-phases/](milestones/v42.0-phases/).

</details>

<details>
<summary>✅ v41.0 Cross-Call Determinism Fix (mint-batch + hero-override) (Phases 281-289 + 284) — SHIPPED 2026-05-17</summary>

- [x] Phase 281: Mint-Batch Determinism Fix (FIX) (1/1 plans) — completed 2026-05-16 (USER-APPROVED contract commit `221afcf7` — owed-salt 4th keccak input; B2 symmetric scope)
- [x] Phase 282: Multi-Call Drain Regression Fixture (TST-FIX) (1/1 plans) — completed 2026-05-16 (USER-APPROVED test commit `a1212b00` — TST-FIX-01..04; REDUCED SCOPE 4 of 6 per 2026-05-16 user authorization; F-41-01 evidence class ALGORITHM_VERIFIED)
- [x] Phase 283: Cross-Surface Batched-Loop Sweep (SWEEP) (1/1 plans) — completed 2026-05-16 (default zero-mutation outcome; 6 surfaces SAFE-BY-STRUCTURE; `_applyHeroOverride` carry-forward observation surfaced F-41-02)
- [x] Phase 285: Day-Index Read Audit + Hero-Override Fix (HOFIX) (1/1 plans) — completed 2026-05-16 (USER-APPROVED contract commit `c4d62564` — write-side `+1` offset; **SUPERSEDED-AT-PHASE-288**)
- [x] Phase 286: Hero-Override Day-Index Regression Fixture (TST-HOFIX) (1/1 plans) — completed 2026-05-16 (USER-APPROVED test commit `cef9a972` — TST-HOFIX-01..04; **REVISED-AT-PHASE-289**)
- [x] Phase 287: Jackpot-Influence Surface Closure (JPSURF) (1/1 plans) — completed 2026-05-17 (FLAG-ONLY posture per user instruction; 0 contract commits; 27 SLOAD READ-SET catalog; 0 VIOLATIONs; 3 residuals (F-41-03 candidate + N-5 + N-9) flagged → all CLOSED collaterally by Phase 288)
- [x] Phase 288: F-41-03 Cross-Day Determinism Fix (FIX-JPSURF) (1/1 plans) — completed 2026-05-17 (USER-APPROVED contract commit `4837fa5c` — `dailyIdx` structural anchor; supersedes Phase 285; closes F-41-02 + F-41-03 collaterally; net −36 bytes; 0-byte storage delta)
- [x] Phase 289: F-41-03 Cross-Day Snapshot Regression Fixture (TST-JPSURF) (1/1 plans) — completed 2026-05-17 (USER-APPROVED test commit `ab76e990` — TST-JPSURF-01..04 + TST-HOFIX-01..04 adjustments; 9 tests PASS)
- [x] Phase 284: Delta Audit + Findings Consolidation (Terminal) (1/1 plans) — completed 2026-05-17 (closure signal `MILESTONE_V41_AT_HEAD_315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` emitted in audit/FINDINGS-v41.0.md §9c; closure-flip commits `315978a0` + `c6997520`)

**Audit baseline:** v40.0 closure HEAD `MILESTONE_V40_AT_HEAD_cd549499` → v41.0 closure HEAD `315978a0c18294e0d7fa5cd4cdfe7f8e5b9a95c4` resolved at Phase 284 terminal closure-flip per D-284-CLOSURE-01. 9-phase shape (281, 282, 283, 285, 286, 287, 288, 289, 284) with Phase 285→288 supersede chain. 6 USER-APPROVED batched commits (3 contract + 3 test) + 1 AGENT-COMMITTED closure-flip. 43/43 requirements satisfied. **Result:** 3 of 3 F-41-NN findings RESOLVED_AT_V41 (first multi-finding milestone in v25..v41 audit history). **F-41-01** (mint-batch determinism HIGH; production-replayed at blocks 10862393..10862412 in pre-launch indexer; RESOLVED via Phase 281 owed-salt + Phase 282 ALGORITHM_VERIFIED tests). **F-41-02** (hero-override day-index within-day HIGH with CRITICAL elevation note on `isFinalPhysicalDay_`; RESOLVED via Phase 288 dailyIdx structural fix + Phase 289 regression coverage; Phase 285 superseded). **F-41-03** (hero-override day-index cross-day MEDIUM-catastrophy-tier; RESOLVED collaterally via Phase 288 + Phase 289 TST-JPSURF-04). 3-skill PARALLEL adversarial pass `/contract-auditor` + `/zero-day-hunter` + `/economic-analyst` run TWICE per D-271-ADVERSARIAL-01 + D-271-ADVERSARIAL-03 carry + new D-284-ADVERSARIAL-RE-PASS-01; RE-PASS on Phase 288 fix: 0 FINDING_CANDIDATEs across all 3 skills. Phase 287 JPSURF audit: 27 SLOAD slot READ-SET catalog + complete MUTATOR-SET enumeration; 0 VIOLATIONs. LEAN regression: 1 PASS REG-01 (v40.0 closure NON-WIDENING) + 1 PASS REG-02 (v34.0 closure NON-WIDENING) + REG-03 KI envelope re-verifications + REG-04 prior-finding spot-check sweep PASS. KNOWN-ISSUES.md UNMODIFIED per D-281-KI-01. Closure verdict `3 of 3 F-41-NN RESOLVED_AT_V41; 0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_UNMODIFIED`. Deliverable: `audit/FINDINGS-v41.0.md` (FINAL READ-only at v41.0 closure HEAD, 9 sections; chmod 444). See [milestones/v41.0-ROADMAP.md](milestones/v41.0-ROADMAP.md) and [milestones/v41.0-REQUIREMENTS.md](milestones/v41.0-REQUIREMENTS.md). Phase directories archived to [milestones/v41.0-phases/](milestones/v41.0-phases/).

</details>

<details>
<summary>✅ v40.0 Unified Whole-Ticket Award Protocol + Whole-BURNIE Floor (Phases 275-280) — SHIPPED 2026-05-14</summary>

- [x] Phase 275: Auto-Resolve LootboxModule Bernoulli (LBX-AR) (2/2 plans) — completed 2026-05-13
- [x] Phase 276: JackpotModule:2216 BAF Bernoulli (JPT-BR) (2/2 plans) — completed 2026-05-14
- [x] Phase 277: Event Surface Unification + Sentinel Retirement (EVT-UNI) (2/2 plans) — completed 2026-05-14
- [x] Phase 278: JackpotModule Cleanup + ENT-05 BAF Xorshift Refactor + Wrapper Retirement (JPT-CLEAN) (2/2 plans) — completed 2026-05-14
- [x] Phase 279: Whole-BURNIE Floor (BUR) (2/2 plans) — completed 2026-05-14
- [x] Phase 280: Delta Audit + Findings Consolidation (Terminal) (1/1 plans) — completed 2026-05-14 (closure signal `MILESTONE_V40_AT_HEAD_cd549499` emitted in audit/FINDINGS-v40.0.md §9c)

**Audit baseline:** v39.0 closure HEAD `MILESTONE_V39_AT_HEAD_6a7455d1` → v40.0 closure HEAD `cd549499`. 6-phase multi-phase milestone shape (5 surface-split phases 275-279 + 1 terminal audit phase 280) per v33/v34/v35/v37 precedent — NOT the v36/v38/v39 single-phase pattern. The 12-commit v40.0 audit subject: Phase 275 (`b6ed8fce` + `bb1b1abd`) auto-resolve LootboxModule Bernoulli — the v39 manual-path Bernoulli extended to `resolveLootboxDirect` + `resolveRedemptionLootbox`, predicate hoisted to shared scope, auto-resolve branch swapped to `_queueTickets(whole)`, silent cold-bust; Phase 276 (`c473867e` + `1568fd5c`) JackpotModule:2216 BAF Bernoulli — inline Bernoulli in `_jackpotTicketRoll` reading `bits[200..215]`, `:2216` swapped to direct `_queueTickets(whole, true)`; Phase 277 (`02fb7085` + `6fbee850` + `f7a6fccd`) event surface unification + sentinel retirement — `LootboxTicketRoll` deleted, `roundedUp` field added to `LootBoxOpened`/`BurnieLootOpen`/`JackpotTicketWin`, `index != type(uint48).max` sentinel retired, `f7a6fccd` CR-01 gap-closure added the dedicated `payColdBustConsolation` parameter + trimmed the event surface; Phase 278 (`8a81a87c` + `c3baf694` + `a91dac85`) JackpotModule cleanup + ENT-05 keccak refactor + wrapper retirement — `_jackpotTicketRoll` swapped xorshift `EntropyLib.entropyStep` for `EntropyLib.hash2` keccak self-mix, `entropyStep` + the zero-caller `_queueLootboxTickets` wrapper deleted, 3 `JackpotTicketWin` emits unified onto whole-ticket counts; Phase 279 (`8ef4a010` + `37207743`) whole-BURNIE floor — A1 floor-per-winner at the 3 RNG-amount sites (LootboxModule:1080 + JackpotModule:1842 + JackpotModule:1922). 5 USER-APPROVED batched contract commits + 5 USER-APPROVED batched test commits + 2 remediation commits. Phase 280 terminal phase SOURCE-TREE FROZEN — zero contracts/ + zero test/ mutations. 65/65 requirements satisfied. Result: 11 of 11 §4 adversarial surfaces SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE; zero F-40-NN finding blocks emitted; 10 novel-vector hypotheses + edge cases across the 3-skill PARALLEL adversarial pass (`/contract-auditor` + `/zero-day-hunter` + `/economic-analyst`; `/degen-skeptic` OUT OF SCOPE) all NEGATIVE_RESULT_ONLY / ACCEPTED_DESIGN. LEAN regression: 1 PASS REG-01 (v39.0 closure NON-WIDENING for v39-touched surfaces NOT in v40 scope) + 1 PASS REG-02 (v34.0 closure NON-WIDENING) + REG-03 KI envelope re-verifications + REG-04 prior-finding spot-check sweep across audit/FINDINGS-v25..v39.0; aggregate 12 PASS / 0 REGRESSED / 0 SUPERSEDED-as-verdict. KI envelopes EXC-01..03 RE_VERIFIED-NEGATIVE-scope at v40; EXC-04 STRUCTURALLY ELIMINATED at v40.0 (Phase 278 `8a81a87c` deleted `EntropyLib.entropyStep`). KNOWN-ISSUES.md MODIFIED per D-280-EXC04-01 — the line-31 EXC-04 entry REMOVED OUTRIGHT. Closure verdict `4 of 4 KI_ELIGIBLE addressed; KNOWN_ISSUES_MODIFIED`. Deliverable: `audit/FINDINGS-v40.0.md` (FINAL READ-only at HEAD `cd549499`, 9 sections, ~907 lines; chmod 444). Closure signal: `MILESTONE_V40_AT_HEAD_cd549499`.

</details>

<details>
<summary>✅ v39.0 Lootbox Whole-Ticket Rounding + WWXRP Consolation (Phase 274) — SHIPPED 2026-05-13</summary>

- [x] Phase 274: Lootbox Whole-Ticket Rounding + WWXRP Consolation (Terminal) (1/1 plans) — completed 2026-05-13 (closure signal `MILESTONE_V39_AT_HEAD_6a7455d1` emitted in audit/FINDINGS-v39.0.md §9c)

**Audit baseline:** v38.0 closure HEAD `MILESTONE_V38_AT_HEAD_06623edb` → v39.0 audit-subject HEAD `6a7455d1`. Single-phase milestone-closure patch mirroring v36.0 Phase 266 + v38.0 Phase 272 precedent. MANUAL LOOTBOX OPENS ONLY (`openLootBox` + `openBurnieLootBox`) — auto-resolve callers (`resolveLootboxDirect` decimator-claim + `resolveRedemptionLootbox` sDGNRS-redemption) explicitly UNCHANGED per D-274-MANUAL-ONLY-01. Wave 1 USER-APPROVED batched contract commit `c21f833a` ships `_resolveLootboxCommon` `uint48 index` parameter + manual-branch Bernoulli-collapse via `bits[152..167]` + new private constant `LOOTBOX_WWXRP_CONSOLATION = 1 ether` + new event `LootboxTicketRoll(player, lootboxIndex, preRollTickets, roundedUp)` declared on interface + contract event block + bit-allocation NatSpec update 152 → 168. D-274-BIT-SLICE-01 superseded intra-Wave-1 from 8-bit to 16-bit slice on bias quantification. Wave 2 USER-APPROVED batched test commit `f8e55cfe` ships TST-WT-01..07 + TST-WX-01..03 + TST-REG-01..04 (+1,422 LOC across 4 new test files; 74 tests; all passing). Wave 3 audit-deliverable atomic chain `386e797d` → `6a7455d1` + closure-flip commits. Phase 273 included-since-baseline (`ff929948` + `e9807891` + `e04d3333` + `1eb1ecb5`) folded into v39.0 audit baseline per D-274-BAF273-INCLUDE-01. 39/39 in-scope requirements satisfied (5 LBX-WT + 4 LBX-WX + 6 LBX-EVT + 7 TST-WT + 3 TST-WX + 4 TST-REG + 6 AUDIT + 4 REG). Result: 8 of 8 §4 adversarial surfaces SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE / SAFE_BY_DESIGN_PHASE_273; zero F-39-NN finding blocks emitted; 12 novel-vector hypotheses (i)..(t) returned 10 NEGATIVE_RESULT_ONLY + 2 ACCEPTED_DESIGN dispositions. LEAN regression: 1 PASS REG-01 (v38.0 closure NON-WIDENING for v38-touched surfaces NOT in v39 manual-lootbox scope) + 1 PASS REG-02 (v34.0 closure NON-WIDENING) + REG-03 KI envelope re-verifications + REG-04 prior-finding spot-check sweep across audit/FINDINGS-v25..v38.0. KI envelopes EXC-01..03 RE_VERIFIED-NEGATIVE-scope at v39; EXC-04 RE_VERIFIED with NARROWS retained (BAF-jackpot-only scope; EntropyLib byte-identical at v39 HEAD). KNOWN-ISSUES.md UNMODIFIED per D-274-KI-01 default zero-promotion path. Closure verdict `0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_UNMODIFIED`. Adversarial pass via 3-skill PARALLEL spawn `/contract-auditor` + `/zero-day-hunter` + `/economic-analyst` per D-274-ADVERSARIAL-01 carry. Deliverable: `audit/FINDINGS-v39.0.md` (FINAL READ-only at HEAD `6a7455d1`, 9 sections; chmod 444). See [milestones/v39.0-ROADMAP.md](milestones/v39.0-ROADMAP.md).

</details>

<details>
<summary>✅ v38.0 Always-Hero Simplification + Maximal Dead-Code Cleanup (Phase 272) — SHIPPED 2026-05-11</summary>

- [x] Phase 272: Always-Hero Simplification + Maximal Dead-Code Cleanup (Terminal) (1/1 plans) — completed 2026-05-11 (3 USER-APPROVED commits: Wave 1 `527e3adc` + Wave 1.5 `4760459f` + Wave 2 `e3fcb95c`; closure signal `MILESTONE_V38_AT_HEAD_06623edb` emitted in audit/FINDINGS-v38.0.md §9c)

**Audit baseline:** v37.0 closure HEAD `MILESTONE_V37_AT_HEAD_2654fcc2` → v38.0 audit-subject HEAD `06623edb`. 2 contract-tree commits since baseline (`527e3adc` Phase 272 Wave 1 silent-normalize HERO-01..05 + CLEAN-01..05 + `4760459f` Phase 272 Wave 1.5 input-validation revision per D-272-INPUT-VALIDATION-01; bytecode delta −57 bytes 8955 → 8898; storage layout byte-identical; public ABI signature byte-identical with semantics shift accept+normalize → revert+InvalidBet) + 1 batched test-tree commit (`e3fcb95c` Phase 272 Wave 2; +238/−36 LOC across 6 files in `test/stat/`, `test/gas/`, `package.json` — STAT-01..02 hero-always-on re-validation + SURF-01..03 cross-surface preservation with SURF-03 rebase to `PHASE_269_CLOSE_BASELINE = "8fd5c2e1..."` + LBX-02 path-of-investigation prose + GASPIN-02 (a-alt) `test:gas` script split + STAT-03-v35-carry ACCEPTED-DESIGN documentation). Single-phase milestone shape per v36.0 Phase 266 precedent. 29/30 in-scope requirements satisfied (5 HERO + 6 CLEAN + 2 STAT + 3 SURF + 2 GASPIN + 1 STAT-03-v35-carry + 6 AUDIT + 4 REG) + 1 RE-DEFERRED-V39+ (LBX-02 — fixture-coverage gap persists; analytical worst-case load-bearing per Phase 266 GAS-01 precedent + `feedback_gas_worst_case.md`; path-of-investigation prose at `audit/FINDINGS-v38.0.md` §9.NN.iv). Result: 7 of 7 §4 adversarial surfaces SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE / SAFE_BY_DEFENSIVE_VALIDATION (a EV-neutrality preserved across (M, N) under always-on hero + b quadrant 0 default does NOT create payout-bias under defensive boundary validation + c cleanup-removal invariants preserved per `feedback_no_dead_guards.md` + d storage layout byte-identical + e public ABI byte-identical with semantics shift + f variance-impact bound on risk-averse subset + g clean test:stat at v38 close); zero F-38-NN finding blocks emitted; 1 INFO-severity docs-vs-behavior drift identified at Hypothesis (i) (`dailyHeroWagers` post-v38 ledger-tracking on `0xFF` input) at Wave 3 3-skill PARALLEL adversarial pass — RESOLVED_AT_V38 via Wave 1.5 commit `4760459f` defensive boundary validation per D-272-INPUT-VALIDATION-01 (status pivoted from KEEP_AS_NEGATIVE_FINDING to RESOLVED_AT_V38). LEAN regression: 1 PASS REG-01 (v37.0 closure NON-WIDENING) + 1 PASS REG-02 (v34.0 closure NON-WIDENING) + REG-03 KI envelope re-verifications + REG-04 prior-finding spot-check sweep across audit/FINDINGS-v25..v37.0. KI envelopes EXC-01..03 RE_VERIFIED-NEGATIVE-scope at v38; EXC-04 RE_VERIFIED with NARROWS retained (BAF-jackpot-only scope; EntropyLib byte-identical at v38 HEAD). KNOWN-ISSUES.md UNMODIFIED per D-272-KI-01 default zero-promotion path. Closure verdict `0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_UNMODIFIED`. Adversarial pass via `/contract-auditor` + `/zero-day-hunter` + `/economic-analyst` PARALLEL spawn per D-271-ADVERSARIAL-01 carry (`/degen-skeptic` OUT OF SCOPE per D-271-ADVERSARIAL-02 carry) returned ZERO disagreements; ZERO FINDING_CANDIDATE; ZERO 8th-surface NEW_VECTOR; ZERO KI promotion candidate per `272-01-ADVERSARIAL-LOG.md` Disposition (with Wave 1.5 disposition update appended at commit `1249a6fd`). Deliverable: `audit/FINDINGS-v38.0.md` (FINAL READ-only at HEAD `MILESTONE_V38_AT_HEAD_06623edb`, 9 sections, chmod 444). See [milestones/v38.0-ROADMAP.md](milestones/v38.0-ROADMAP.md) and [milestones/v38.0-REQUIREMENTS.md](milestones/v38.0-REQUIREMENTS.md) and [milestones/v38.0-MILESTONE-AUDIT.md](milestones/v38.0-MILESTONE-AUDIT.md).

</details>

<details>
<summary>✅ v37.0 Degenerette Recalibration + Maintenance Bundle (Phases 267-271) — SHIPPED 2026-05-11</summary>

- [x] Phase 267: Degenerette Producer + 5-Table Payout Rewrite (1/1 plans) — completed 2026-05-10 (USER-APPROVED contract commit `e1136071` — `feat(267): degenerette producer + 5-table payout rewrite + 3-tier ETH split [DGN-01..15, PAY-SPLIT-01..03]`)
- [x] Phase 268: Degenerette Statistical Validation + Cross-Surface Preservation (1/1 plans) — completed 2026-05-11 (USER-APPROVED test commit `4b277aaf` — `test(268): degenerette stat suite + cross-surface preservation v37.0 + worst-case gas regression [STAT-01..07, SURF-01..06]`)
- [x] Phase 269: Lootbox Dead-Branch Cleanup + SURF-05 Gas-Pin Re-Pinning (1/1 plans) — completed 2026-05-11 (USER-APPROVED contract commit `8fd5c2e1` — `feat(269): delete unreachable BURNIE-conversion branch in _resolveLootboxRoll [LBX-01]`; PARTIAL ship — 4 deferred to v38+)
- [x] Phase 270: Post-v32.0 Deferred-Commit Adversarial Sub-Audit (1/1 plans) — completed 2026-05-11 (AGENT-COMMITTED working-file `4017b9ec` + phase-close `5cd4f2bc`)
- [x] Phase 271: Delta Audit + Findings Consolidation (Terminal) (1/1 plans) — completed 2026-05-11 (closure signal `MILESTONE_V37_AT_HEAD_2654fcc2` emitted in audit/FINDINGS-v37.0.md §9c)

**Audit baseline:** v36.0 closure HEAD `1c0f09132d7439af9881c56fe197f81757f8164a` → v37.0 audit-subject HEAD `2654fcc2` (post-Task-12 §9 attestation commit). 2 contract-tree commits since baseline (`e1136071` Phase 267 Degenerette producer + 5-table payout rewrite + 3-tier ETH split + `8fd5c2e1` Phase 269 LBX-01 dead-branch deletion; bytecode shrink 177 bytes 18,330 → 18,153) + 1 batched test-tree commit (`4b277aaf` Phase 268; +2,277/−1 LOC across 6 files). 5-phase milestone shape: Phases 267 (contracts) + 268 (tests) + 269 (LBX cleanup PARTIAL ship) + 270 (post-v32.0 sub-audit; zero source-tree mutations) + 271 (delta audit terminal). 48/48 in-scope requirements satisfied (15 DGN + 3 PAY-SPLIT + 7 STAT + 6 SURF + LBX-01 + LBX-03 + GASPIN-01 + 4 DELTA + 6 AUDIT + 4 REG) + 3 DEFERRED-V38+ per D-271-DEFERRED-02 (LBX-02 + GASPIN-02 + GASPIN-03). Result: 8 of 8 §4 adversarial surfaces SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE (a per-N table dispatch + b symbol-only hero match + c `_countGoldQuadrants` boundary + d producer byte-layout + e WWXRP × hero composition + f lootbox dead-branch byte-equivalence + g hero × per-N skill-expression carry + h ETH PAY-SPLIT 3-tier boundary-gaming v37-NEW); zero F-37-NN finding blocks emitted; 1 PASS REG-01 + 1 PASS REG-02 + 5 PASS + 1 SUPERSEDED REG-04; `0 of 0 KI_ELIGIBLE_PROMOTED; KNOWN_ISSUES_UNMODIFIED` per D-271-PAYSPLIT-01 + D-271-KI-01 default zero-promotion path (PAY-SPLIT 3.0× bet boundary discontinuity documented via §4 (h) prose-only attestation per /economic-analyst mechanism-design assessment). 4 RE_VERIFIED KI envelope rows (EXC-01..03 NEGATIVE-scope at v37; EXC-04 NARROWS retained — BAF-jackpot-only scope). Adversarial pass via `/contract-auditor` + `/zero-day-hunter` + `/economic-analyst` PARALLEL spawn per D-271-ADVERSARIAL-01 (`/degen-skeptic` OUT OF SCOPE per D-271-ADVERSARIAL-02) returned ZERO disagreements; zero FINDING_CANDIDATE / zero 9th-surface NEW_VECTOR / zero KI promotion candidate per `271-01-ADVERSARIAL-LOG.md` Disposition. Deliverable: `audit/FINDINGS-v37.0.md` (FINAL READ-only at HEAD `MILESTONE_V37_AT_HEAD_2654fcc2`, 9 sections, chmod 444). See [milestones/v37.0-ROADMAP.md](milestones/v37.0-ROADMAP.md) and [milestones/v37.0-REQUIREMENTS.md](milestones/v37.0-REQUIREMENTS.md).

</details>
