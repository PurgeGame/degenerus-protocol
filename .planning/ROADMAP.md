# Roadmap: Degenerus Protocol — Audit Repository

**Core value:** Every finding a C4A warden could submit is identified and either fixed or documented as known before the audit begins.

## Milestones

- ✅ **v44.0 sStonk Per-Day Redemption Refactor** — Phases 304-308 (shipped 2026-05-20)
- ✅ **v45.0 VRF-Rotation Liveness Fix** — Phases 309-314 (shipped 2026-05-23, minimal close)
- ✅ **v46.0 Do-Work Crank + AfKing Subscription** — Phases 316-320 (shipped 2026-05-24)
- ✅ **v47.0 Rake-Free Presale + Lootbox-Boon Unification** — Phases 321-324 (shipped 2026-05-25)
- ✅ **v48.0 sDGNRS Salvage Swap + v47 Deferred Fixes + Keeper/Pool/Tombstone/Hero** — Phases 325-328 (shipped 2026-05-26)
- ✅ **v49.0 Unified Keeper Router + Bounty Recalibration + AfKing Keeper Sweep** — Phases 329-333 (shipped 2026-05-27)
- ✅ **v50.0 Whale-Pass O(1) Refactor + AfKing Pass-Gated Subs + MintModule Advance-Divergence + External RNG-Audit Protocol** — Phases 334-338 (shipped 2026-05-28, minimal close)
- ✅ **v51.0 claimBingo — Color-Completion Claim** — Phases 339-342 (closed 2026-05-28 at IMPL HEAD `c3e9d907`; 341 TST + 342 TERMINAL folded → v52 audit)
- ⊘ **v54.0 Game-Side Keeper-Funding Ledger + AfKing De-Custody + Dead-Code/Gas Sweep** — Phases 343-347 (CLOSED-as-superseded 2026-05-30 — 343 SPEC + 344 IMPL shipped `20ca1f79`; 345/346/347 dropped → folded into v55; no `MILESTONE_V54_AT_HEAD` signal)
- ✅ **v55.0 AfKing-in-Game Redesign** — Phases 348-352 (shipped 2026-06-01; closure signal `MILESTONE_V55_AT_HEAD_ca3bbd3220de763298ef2e742111f6e6ef90d583`; subject frozen `453f8073`; baseline `20ca1f79`; 7 phases incl. 349.1/349.2; 0 NEW_FINDINGS)
- ✅ **v56.0 AfKing Everyday-Gas Minimization** — Phases 353-357 (shipped 2026-06-04; closure signal `MILESTONE_V56_AT_HEAD_1e7a646d44da4ee26375edd0b006274821fef73e`; subject frozen `1e7a646d`; baseline = v55 HEAD frozen `453f8073` / `MILESTONE_V55_AT_HEAD_ca3bbd3220de763298ef2e742111f6e6ef90d583`; 0 NEW_FINDINGS)
- ✅ **v58.0 RNG-Safety, ETH-Flow & Gas-Ceiling Documentation Audit** (document-only; autonomous + resumable; reshaped from the drafted "v52 idea" `.planning/PLAN-V58-CONSOLIDATED-AUDIT.md`) — Phases 363-369 (SHIPPED 2026-06-04; closure signal `MILESTONE_V58_AT_HEAD_2b26ec91810a733e15666a4c23e8f365a4f04f51`; baseline = v57.0 closure HEAD `b7237db4` / contract subject frozen `2b26ec91`; FIVE artifacts as ONE cohesive dark-themed static site over the full cumulative v50→v57 surface; **7 CONFIRMED findings [1 CRIT · 1 HIGH · 2 MED · 2 LOW · 1 INFO] + 5 REFUTED** — the 5 Claude analysis phases found 0 on their own surfaces, the XMODEL Gemini+Codex council surfaced 7 on adjacent surfaces [salvage arithmetic, AfKing affiliate units, BAF/decimator whale-pass remainders], all verified vs frozen source + routed to a LATER remediation milestone [document-only]; NO contract-commit gate; 22 reqs / 10 categories; NOT pushed)
- ✅ **v57.0 Small-Feature Bundle + Day-Width `uint24` Normalization** (the `type Day` UDVT was dropped for plain uint24; RNG re-based by design, pre-launch) — Phases 358-362 (shipped 2026-06-04; closure signal `MILESTONE_V57_AT_HEAD_2b26ec91810a733e15666a4c23e8f365a4f04f51`; subject frozen `2b26ec91`; 1 LOW found+RESOLVED at 362, 0 MEDIUM+; SOLVENCY-01 held; NOT pushed; baseline = v56.0 closure HEAD frozen `1e7a646d` / `MILESTONE_V56_AT_HEAD_1e7a646d44da4ee26375edd0b006274821fef73e`; 7 small wins [handlePurchase BURNIE-batching · WWXRP jackpot whale-halfpass · terminal-decimator final-day streak boost · test/comment hygiene · **BURNIE coin-buy ticket-queue Critical fix** · **sDGNRS salvage combo ETH/BURNIE pawn-shop payout** · **manual-cancel auto-claim + auto-evict forfeit** (last three added mid-358)] + the repo-wide `type Day is uint24` UDVT [the heavy item]; ONE batched diff; SPEC→IMPL→GAS→TST→TERMINAL)
- ✅ **v60.0 Council-Findings Remediation + Pass-Stat Front-Load (v59 IMPL) + Maximal Cross-Model Pre-C4A Audit** — Phases 370-374 (folded) + the off-phase v60 audit (SHIPPED 2026-06-06 — single combined close; closure signal `MILESTONE_V60_AT_HEAD_2bee6d6faa2f66a9231d4b9bd01a53d09f40ff5e`; baseline `2b26ec91`, v59 IMPL `4577cfb6` → v60 fix HEAD `2bee6d6f`; the v59 batched diff [5 council fixes SALV/AFAFF/SOLV-01/SOLV-02/PRESALE + pass-stat WINDOW/STREAK/CENTURY] PLUS a full external-led maximal audit [round 1: 6 findings → 5 fixed + PRESALE wontfix; round 2: every-surface rotation — RNGRETRY/DECSTREAK/RNGREUSE/coinflip/GASCEIL 17.54M→6.37M/WHALE-01]; RNG-freeze + SOLVENCY-01 + the 16.7M ceiling all re-attested; 7 commits NOT pushed)
- 🚧 **v61.0 AfKing-as-Payment-Source + Cashout-Curse + Deity-Smite** — Phases 375-379 (ACTIVE 2026-06-06; baseline = v60.0 closure HEAD `2bee6d6f`; ONE batched contract diff — afking-as-payment waterfall `msg.value → claimable → afking` + claimable/afking slot-packing [feature-first] + cashout-curse counter/cure/`decurse` + deity-smite on the same counter; SPEC→IMPL→GAS→TST→TERMINAL; 27 reqs / 8 categories; hard floor RNG-freeze + SOLVENCY-01; FULL in-milestone close at TERMINAL; NOT pushed)

---

## 🚧 v61.0 AfKing-as-Payment-Source + Cashout-Curse + Deity-Smite (ACTIVE 2026-06-06 — baseline = v60.0 closure HEAD `2bee6d6f`; ONE batched contract diff; phases 375-379)

> **Scope (USER-locked 2026-06-06):** ONE milestone, ONE batched contract diff — **(1) AfKing-as-payment** (`PLAN-V61-AFKING-AS-PAYMENT-SOURCE.md`): `afkingFunding` becomes a fresh-ETH-equivalent fallback tier (waterfall `msg.value → claimable → afking`) across every non-mintBurnie spend path (tickets / lootbox / presale box / whale ×3 / degenerette ETH bet); the afking portion is treated identically to `msg.value` (fresh-rate affiliate incl. lootbox activity score + presale credit) with NO rebuy bonus; implicit (no opt-in flag) + `AfkingSpent` event — **PLUS** the `claimableWinnings`+`afkingFunding` slot-packing into one `uint256` (`[afking:high128 | claimable:low128]`) via an accessor layer, folded **feature-first**. **(2) Cashout-curse** (`PLAN-CASHOUT-CURSE.md`): a `uint8` curse counter (`BitPackingLib` bit 215) +2 pts/200 bps per ghost-cashout (cash out while ≥5 days idle), −100 bps/pt off every activity-score consumer (floored 0), stacks; cured by any ≥1-ticket buy (funding-agnostic) OR the permissionless `decurse(target)` = 100 BURNIE; active afker exempt. **(3) Deity-smite** (`PLAN-V61-DEITY-SMITE.md`): `smite(deityId, smitee)` burns `PRICE_COIN_UNIT/5` = 200 BURNIE → +1 stack on the SAME curse counter; active afker = sole immunity, can't smite ≥5 stacks, 1/tx, `Smited` event. All 3 + the packing IN scope; the AfKing-as-payment sub-concept ancestor `PLAN-UNIVERSAL-CLAIMABLE-PAY.md` is shipped (whale/presale already pull claimable — this adds the THIRD tier).

> **Posture — hard floor on EVERY change = RNG-freeze intact + SOLVENCY-01 re-attested.** All three items read no `rngWord` (ledger / activity-score / PvP). AFPAY + PACK keep `claimablePool == Σ(claimableWinnings + afkingFunding)` — afking already rides inside `claimablePool`; each afking debit pairs `claimablePool -=` (pool-neutral, contract balance unchanged); the PACK uint128 halves are width-safe (ETH supply ≪ 2^128, no overflow `require`). CURSE/SMITE only ever LOWER an activity-score-derived multiplier (payouts down, never up; freeze-safe, score path view-only). Pre-launch redeploy-fresh (the PACK storage break needs no migration).

> **Posture — ONE contract-boundary HARD STOP at IMPL (376).** The whole AFPAY + PACK + CURSE + SMITE change ships as ONE batched USER-APPROVED `contracts/*.sol` diff with a HARD STOP at the contract-commit boundary (applied + locally compiled/tested, NEVER committed without explicit user hand-review — only contract commits need approval per `[[only-contract-commits-need-approval]]`; docs/tests/SPEC run hands-off). **Producer-before-consumer sequencing** is load-bearing: the CURSE counter + cure + APPLY hook MUST precede SMITE (shared `curseCount` + cap + cure); the PACK accessor layer MUST precede the repack (feature-first — the AFPAY waterfall lands on today's two mappings, then the accessor-routed repack folds in). Sequential-on-main (`use_worktrees: false`, `no_worktree_paths: [contracts]`). `ContractAddresses.sol` freely modifiable; tests + planning + docs AGENT-committable.

> **Audit posture — FULL CLOSE WITH ITS OWN INTERNAL SWEEP (NOT deferred).** Like v54/v55/v56/v57/v59 (and unlike v50/v51), **v61.0 runs its own internal 3-skill genuine-PARALLEL adversarial sweep + delta-audit + `audit/FINDINGS-v61.0.md` IN-MILESTONE at TERMINAL (379, AUDIT-01)** — because #1 reworks the claimable/afking solvency accounting (+ the storage repack) and #2/#3 ride the shared activity-score path, all of which must be adversarially probed in-milestone.

> **Phase numbering** continues from the previous milestone — v60.0 closed at Phase 374, so **v61.0 starts at Phase 375.** Not reset to 1.

> **Milestone shape** is the established SPEC → IMPL → GAS → TST → TERMINAL audit pattern: **375 SPEC** (design-lock the open knobs [packing sequencing · `AfkingSpent` breadth · `purchaseWith`-dead · curse cap rec 20 + staleness day-basis · smite protocol-skip/self-smite] + re-attest every anchor vs `2bee6d6f` + map the producer-before-consumer edit order; ZERO `contracts/*.sol`) → **376 IMPL** the ONE batched diff (AFPAY-01..07 + PACK-01/02 + CURSE-01..07 + SMITE-01, producer-before-consumer; HELD for USER hand-review) → **377 GAS** (the gas-neutrality gate — CURSE-02 APPLY stays zero-new-SLOAD on the hot activity-score read, PACK adds no cold slot, AFPAY/SMITE add no `advanceGame`-ceiling regression; owns NO REQ-ID; Outcome-A no-diff in the expected gas-neutral case, else rides the same boundary) → **378 TST** (the proofs — one proving test per surface [TST-01..06] + the SEC hard floor [SEC-01 RNG-freeze + SEC-02 SOLVENCY-01]) → **379 TERMINAL** FULL close (delta-audit + 3-skill sweep + `audit/FINDINGS-v61.0.md` + the atomic closure flip with the `MILESTONE_V61_AT_HEAD_<sha>` signal). The contract-boundary HARD STOP lives at ONE phase (376 IMPL).

### Phases

- [x] **Phase 375: SPEC — Design-Lock (open knobs) + Anchor Re-Attestation vs `2bee6d6f` + Edit-Order Map** - Settle the open decisions in writing so IMPL authors a fully reconciled diff: AFPAY feature-first vs accessor-first packing sequencing + `AfkingSpent` emission breadth + confirm `purchaseWith` is dead; CURSE counter cap (rec 20 pts) + the staleness day-basis; SMITE protocol-addr skip (rec keep VAULT/SDGNRS/GNRUS) + self-smite sanity; re-attest every cited `file:line` anchor against the frozen baseline `2bee6d6f`; and map the producer-before-consumer edit order (CURSE infra before SMITE; PACK accessor before repack; the SOLVENCY accessor-invariant location). Paper-only, ZERO `contracts/*.sol`.
- [ ] **Phase 376: IMPL — The ONE Batched Contract Diff (AFPAY + PACK + CURSE + SMITE)** - Land the single reconciled `contracts/*.sol` diff producer-before-consumer: the afking-as-payment waterfall (AFPAY-01..07) on today's two mappings, then the accessor-layer claimable/afking slot-packing (PACK-01/02) folded feature-first; the cashout-curse counter + APPLY + SET + cure + bounty-stamp + `decurse` (CURSE-01..07); and deity-smite on the shared counter (SMITE-01). Applied to `contracts/` and locally compiling (`forge build` clean), then HELD at the contract-commit boundary for explicit user hand-review. CONTRACT-BOUNDARY HARD STOP (`autonomous:false` at the commit gate).
- [ ] **Phase 377: GAS — Gas-Neutrality Gate (curse APPLY zero-SLOAD · packing no cold slot · no advance-ceiling regression)** - Confirm the bundle adds no regression under the security-over-gas floor: CURSE-02 APPLY rides the `packed` slot already loaded at `_playerActivityScore:248` (zero new SLOAD on the hot read); the PACK repack adds no cold slot (one slot/player — a winner prepaying afking saves a ~20k cold SSTORE); AFPAY's shared-helper afking draw + SMITE's burn add no `advanceGame`-ceiling regression. Expected outcome = gas-neutral/positive-by-construction → recorded as Outcome-A no-diff per the v55 350 / v56 / v57 / v59 precedent; any net behavior-preserving tune rides the same batched USER-APPROVED diff. (Owns no REQ-ID — the gas-neutrality measurement gate.)
- [ ] **Phase 378: TST — One Proving Test per Surface (TST-01..06) + RNG-Freeze Intact + SOLVENCY-01 Re-Attested (SEC-01/02)** - Prove the milestone behaviorally correct + the hard security floor empirically against the frozen `2bee6d6f` baseline: TST-01 AFPAY waterfall (DirectEth-skips-claimable-taps-afking, fresh-rate affiliate, no rebuy bonus, no auto-buy double-draw) · TST-02 PACK (packed round-trip, `claimablePool == Σ`, gameOver afking-half preserved, no cross-half carry) · TST-03 CURSE SET (+2 stale-only, all exemptions, stacking, saturation) · TST-04 CURE+bounty+`decurse` · TST-05 SMITE (gate/immunity/ceiling/burn/shared-counter/cure) · TST-06 NON-WIDENING vs `2bee6d6f`; plus SEC-01 (RNG-freeze intact) + SEC-02 (SOLVENCY-01 re-attested). Tests AGENT-committable.
- [ ] **Phase 379: TERMINAL — Delta Audit + 3-Skill Adversarial Sweep + FINDINGS-v61.0 + Closure Flip** - Close the v61.0 subject (the batched diff, FROZEN at the IMPL+GAS HEAD) via a FULL in-milestone close: a delta-audit (every changed surface NON-WIDENING vs `2bee6d6f`, RNG-freeze + SOLVENCY-01 re-attested with grep/diff anchors) + the mandatory 3-skill genuine-PARALLEL adversarial sweep (`/contract-auditor` + `/economic-analyst` + `/zero-day-hunter`; `/degen-skeptic` = the dual-gate filter) focused on the afking-payment solvency accounting + the PACK repack + the curse/smite activity-score path + `audit/FINDINGS-v61.0.md` (chmod 444) + the atomic closure flip with the `MILESTONE_V61_AT_HEAD_<sha>` signal (AUDIT-01).

### Phase Details (v61.0)

### Phase 375: SPEC — Design-Lock (open knobs) + Anchor Re-Attestation vs `2bee6d6f` + Edit-Order Map

**Goal**: The open design decisions are settled so IMPL authors a fully reconciled diff with zero "by construction" assumptions.
**Type**: SPEC
**Depends on**: Nothing (first v61.0 phase; consumes the v60.0-closed frozen subject `2bee6d6f` + the 3 design-lock plan docs)
**Requirements**: SPEC-01
**Success Criteria** (what must be TRUE):

  1. The open knobs are LOCKED in writing (packing sequencing · `AfkingSpent` breadth · `purchaseWith`-dead · curse cap + staleness basis · smite protocol-skip/self-smite).
  2. Every cited anchor is re-attested against `2bee6d6f` with any drift corrected.
  3. The producer-before-consumer edit order is mapped (CURSE before SMITE; PACK accessor before repack).

**Plans**: 2 plans

  - [x] 375-01-PLAN.md — Re-attest every cited anchor vs `2bee6d6f` (drift corrected) + resolve the `purchaseWith`-dead / self-smite / SOLVENCY-accessor verification items → `375-ANCHOR-REATTESTATION.md`
  - [x] 375-02-PLAN.md — Author `SPEC-V61-DESIGN-LOCK.md`: lock the knobs D-01..D-05, fold the re-attested anchor table, map the producer-before-consumer edit order (Track A PACK→AFPAY / Track B CURSE→SMITE)

**UI hint**: no

### Phase 376: IMPL — The ONE Batched Contract Diff (AFPAY + PACK + CURSE + SMITE)

**Goal**: The v61.0 bundle lands as a single reconciled `contracts/*.sol` diff under the SPEC's settled knobs + edit order, with RNG-freeze intact + SOLVENCY-01 held.
**Type**: IMPL (CONTRACT BOUNDARY; `autonomous:false` at the commit gate)
**Depends on**: Phase 375
**Requirements**: AFPAY-01..07, PACK-01/02, CURSE-01..07, SMITE-01
**Success Criteria** (what must be TRUE):

  1. The AFPAY waterfall lands on all spend paths with the afking portion treated as fresh ETH (no rebuy bonus).
  2. The PACK accessor layer + repack land feature-first, `claimablePool == Σ` in one place.
  3. The CURSE counter/APPLY/SET/cure/bounty-stamp/`decurse` land (zero new SLOAD on the score read).
  4. SMITE lands on the shared counter (afker-immune, ≥5-stack ceiling).
  5. `forge build` clean, HELD at the boundary for hand-review.

**Plans**: 3 plans (sequential-on-main, all `autonomous:false` — ONE batched diff held at ONE boundary)
Plans:
**Wave 1**

- [ ] 376-01-PLAN.md — Track A: PACK accessor + repack (PACK-01/02) then the AFPAY waterfall + AfkingSpent (AFPAY-01..07)

**Wave 2** *(blocked on Wave 1 completion)*

- [ ] 376-02-PLAN.md — Track B: cashout-curse counter/APPLY/SET/CURE/stamp/decurse/view (CURSE-01..07) + deity-smite (SMITE-01)

**Wave 3** *(blocked on Wave 2 completion)*

- [ ] 376-03-PLAN.md — Build-cleanliness (SettleClaimableShortfallTester + test sweep) + EIP-170 guardrail + contract-commit boundary HOLD

**UI hint**: no

### Phase 377: GAS — Gas-Neutrality Gate (curse APPLY zero-SLOAD · packing no cold slot · no advance-ceiling regression)

**Goal**: Confirm gas-neutrality (CURSE APPLY zero-new-SLOAD, PACK no cold slot, AFPAY/SMITE no advance-ceiling regression).
**Type**: GAS (CONTRACT BOUNDARY — net diff rides the same boundary, else Outcome-A)
**Depends on**: Phase 376
**Requirements**: none
**Success Criteria** (what must be TRUE):

  1. The lazy/score read path + the spend paths stay within budget; outcome recorded (Outcome-A no-diff expected).

**Plans**: TBD
**UI hint**: no

### Phase 378: TST — One Proving Test per Surface (TST-01..06) + RNG-Freeze Intact + SOLVENCY-01 Re-Attested (SEC-01/02)

**Goal**: The milestone proven behaviorally correct + the hard floor proven empirically vs `2bee6d6f`.
**Type**: TST
**Depends on**: Phase 377
**Requirements**: SEC-01, SEC-02, TST-01..06
**Success Criteria** (what must be TRUE):

  1. One proving test per surface passes.
  2. RNG-freeze intact across all changes.
  3. SOLVENCY-01 re-attested (`claimablePool == Σ`, never exceeds `bal + stETH`).
  4. Suite NON-WIDENING by name vs `2bee6d6f`.

**Plans**: 6 plans (6 sequential waves — one plan per wave; all share one Foundry build env so no parallelism; all `autonomous: true`, test-side only, ZERO contract edits)
Plans:
**Wave 1**

- [x] 378-01-PLAN.md — Foundation: storage-layout recalibration key (forge inspect; delta is region-dependent, NOT uniform −1; balancesPacked root UNMOVED at slot 7) + StorageFoundation/redemption Game-resident slot fixes (semantic low-128) + frozen-baseline `2bee6d6f` red set BY NAME captured (533/183/103, 172 names) (TST-06) ✅ `8da54ed5`/`bad1889e`/`5de3ccb8`

**Wave 2** *(blocked on Wave 1)*

- [x] 378-02-PLAN.md — Gas-harness slot recalibration (the 6 slot-hardcoded harnesses → authoritative v61 slots; NoPass/panic slot-stale class resolved, V56AfkingGasMarginal 16/16) + STAGE_2 subscriber all-evict live re-measure on v61 = 13.61M / 3.09M headroom < 16.7M (PACK 1-slot-neutral on the evict path) (TST-06) ✅ `3aadcf49`/`09870c57`

**Wave 3** *(blocked on Wave 2)*

- [x] 378-03-PLAN.md — Behavior-update triage (a/b/c) across the ~32-file tail (+1 interim-surfaced file): slot-stale recalibrated to authoritative v61 layout → full suite 177→66 failed (111 red→green); interim non-widening check PASSED (63 HEAD reds = 60 carried in-union + 3 documented class-(c) accepted-staleness; `live − (union ∪ documented) == ∅`); NO class-(b) rewrites needed (v61 behavior proven positively by 378-04/05); `InvalidBet()` preserved (24-bucket was 100% slot-stale); deliverable `378-03-CANDIDATE-FINDINGS.md`; zero .sol edits (TST-06) ✅ `3c04615c`/`631b5c85`/`ff4b0e9b`

**Wave 4** *(blocked on Wave 3)*

- [ ] 378-04-PLAN.md — Proving tests TST-01 (AFPAY waterfall) + TST-02 (PACK) + TST-03 (CURSE SET)

**Wave 5** *(blocked on Wave 4)*

- [ ] 378-05-PLAN.md — Proving tests TST-04 (CURE+bounty+decurse) + TST-05 (SMITE) + TST-06 (final NON-WIDENING by-name comparison vs the 378-01 baseline)

**Wave 6** *(blocked on Wave 5)*

- [ ] 378-06-PLAN.md — SEC-01 (RNG-freeze empirical determinism) + SEC-02 (SOLVENCY-01 invariant across the afking spend paths)
**UI hint**: no

### Phase 379: TERMINAL — Delta Audit + 3-Skill Adversarial Sweep + FINDINGS-v61.0 + Closure Flip

**Goal**: Close the v61.0 subject via a FULL in-milestone sweep.
**Type**: TERMINAL
**Depends on**: Phase 378
**Requirements**: AUDIT-01
**Success Criteria** (what must be TRUE):

  1. Delta-audit clean (NON-WIDENING, RNG-freeze + SOLVENCY-01 anchored).
  2. The 3-skill adversarial sweep runs as genuine concurrent spawns.
  3. `audit/FINDINGS-v61.0.md` authored + the atomic closure flip emits `MILESTONE_V61_AT_HEAD_<sha>`; all 27 reqs re-attested.

**Plans**: TBD
**UI hint**: no

### Progress (v61.0)

**Execution Order:** 375 → 376 → 377 → 378 → 379

| Phase | Milestone | Plans Complete | Status | Completed |
|-------|-----------|----------------|--------|-----------|
| 375. SPEC — Design-Lock + Anchor Re-Attestation vs `2bee6d6f` + Edit-Order Map | v61.0 | 2/2 | Complete    | 2026-06-06 |
| 376. IMPL — The ONE Batched Contract Diff (AFPAY + PACK + CURSE + SMITE) | v61.0 | 0/3 | Planned     | 2026-06-06 |
| 377. GAS — Gas-Neutrality Gate (curse APPLY zero-SLOAD · packing no cold slot) | v61.0 | 0/TBD | Not started | - |
| 378. TST — One Proving Test per Surface (TST-01..06) + RNG-Freeze + SOLVENCY-01 | v61.0 | 2/6 | In Progress | 2026-06-07 |
| 379. TERMINAL — Delta Audit + 3-Skill Sweep + FINDINGS-v61.0 + Closure Flip | v61.0 | 0/TBD | Not started | - |

> **🔒 v61.0 CONTRACT-BOUNDARY HARD STOP (ONE gate — 376 IMPL · 377 GAS rides the same boundary if it produces a net diff).** Phase 376 IMPL is THE contract phase — the batched diff (AFPAY + PACK + CURSE + SMITE, producer-before-consumer: CURSE infra before SMITE, PACK accessor before repack) is applied to `contracts/` and locally compiled (`forge build` clean) but HELD at the contract-commit boundary, NEVER committed without explicit user hand-review. `ContractAddresses.sol` freely modifiable; tests + planning + docs AGENT-committable.

> **🔓 v61.0 AUDIT POSTURE — FULL CLOSE (internal sweep NOT deferred).** Like v54/v55/v56/v57/v59 (and unlike v50/v51), v61.0 runs its own internal 3-skill genuine-PARALLEL adversarial sweep + delta-audit + `audit/FINDINGS-v61.0.md` at TERMINAL (379, AUDIT-01) — #1 reworks the claimable/afking solvency accounting (+ the repack) and #2/#3 ride the shared activity-score path.

### Coverage (v61.0)

**27/27 v61.0 requirements mapped to exactly one phase — 0 orphaned, 0 duplicated.**

| Phase | Requirements | Count |
|-------|--------------|-------|
| 375 SPEC | SPEC-01 | 1 |
| 376 IMPL | AFPAY-01..07, PACK-01/02, CURSE-01..07, SMITE-01 | 17 |
| 377 GAS | (none — the gas-neutrality measurement gate) | 0 |
| 378 TST | SEC-01, SEC-02, TST-01..06 | 8 |
| 379 TERMINAL | AUDIT-01 | 1 |
| **Total** | | **27** |

Per-category: AFPAY 7 (IMPL) · PACK 2 (IMPL) · CURSE 7 (IMPL) · SMITE 1 (IMPL) · SPEC 1 (SPEC) · SEC 2 (TST) · TST 6 (TST) · AUDIT 1 (TERMINAL). The 17 contract reqs land producer-before-consumer in the ONE batched diff at IMPL (376); SEC-01/02 + TST-01..06 are PROVEN empirically at TST (378); the internal sweep + delta-audit + `audit/FINDINGS-v61.0.md` run IN-MILESTONE at 379 (NOT deferred). Full per-requirement mapping in `.planning/REQUIREMENTS.md` Traceability.

---

## ✅ v60.0 Council-Findings Remediation + Pass-Stat Front-Load (v59 IMPL) + Maximal Cross-Model Pre-C4A Audit (SHIPPED 2026-06-06 — single combined close; closure signal `MILESTONE_V60_AT_HEAD_2bee6d6faa2f66a9231d4b9bd01a53d09f40ff5e`; baseline = the v58.0-audited frozen tree `2b26ec91`; v59 IMPL `4577cfb6` → v60 fix HEAD `2bee6d6f`; phases 370-374 folded + the off-phase maximal audit; full archive `.planning/milestones/v60.0-ROADMAP.md`)

> **✅ SHIPPED as a SINGLE COMBINED CLOSE (USER 2026-06-06).** Two intertwined bodies of work shipped as one unit: **(1) v59.0** — the council-findings + pass-stat batched contract diff (`4577cfb6`, 9 reqs, USER hand-reviewed); phases **370 SPEC ✅ · 371 IMPL ✅ · 372 GAS ⊘ dropped · 373 TST ↪ folded into v60 · 374 TERMINAL ↪ folded into v60** (v59 lean-closed 2026-06-05, no standalone `MILESTONE_V59_AT_HEAD` signal — the v60 audit IS its verification). **(2) v60.0** — a maximal cross-model pre-C4A audit (off the formal phase structure): round 1 = 17 raw → 6 findings (SOLV-02 `c0565ee4`; FLOWBURNIE/LIFECYCLE/FLOWENTROPY/STREAK `4cb9ccbf`; PRESALE wontfix); round 2 = an external-led rotation run to completion across every surface (RNGRETRY `305fa1c1` + RETRYLOCK `a8e7d939` · DECSTREAK `6bd94bb5`/`3603822f` · RNGREUSE `5847258a` · coinflip G-1/C-1/C-2 `0f4e2a54` · quest-boon `07e930d5` · GASCEIL `6d2c8d0c` [game-over composition 17.54M→6.37M < 16.7M] · WHALE-01 `2bee6d6f`). RNG-freeze + SOLVENCY-01 re-attested; `forge build` clean; baseline-diff zero regressions; **7 commits ahead of origin/main, NOT pushed.** Canonical deliverable `audit/FINDINGS-v60.0.md` (chmod 444). The detailed v59 phase plan is retained below as the as-planned record; the Progress table reflects the as-shipped status.

> **Scope (USER-locked 2026-06-04):** ONE milestone, ONE carefully-sequenced batched contract diff — (1) the 5 fixable v58.0 council findings: **F-01 CRIT** salvage `uint32`-truncation (drains sDGNRS) · **F-02 HIGH** afking `affiliateBase` unit slip (unbacked BURNIE emission) · **F-03/F-04 MED** whale-pass *remainder* solvency-identity corrections (BAF + decimator) · **F-05 LOW** `pendingBurnie` presale 2×-credit — PLUS (2) the deferred pass-stat seed: **Change A** inclusive 1–10 pass-coverage window (Option B) · **Change B** front-load the LEVEL mint streak on pass purchase · **Change C** allow a lazy pass during a century's PURCHASE phase (all centuries) — PLUS (3) a second-model `area-solvency` XMODEL re-run (COV-01) closing the v58.0 coverage gap before relying on the F-03/F-04 corrections. **F-06 LOW = WONTFIX / working-as-intended** + **F-07 INFO = no fix** → Out of Scope. Design-lock inputs: `audit/FINDINGS-v58.0.md` (the 7 confirmed council findings + fix sketches, chmod 444) + `.planning/PLAN-PASS-STAT-FRONTLOAD.md` (the deferred seed, `[[pass-stat-frontload-window-seed]]`).

> **Posture — hard floor on EVERY change = RNG-freeze intact + SOLVENCY-01 re-attested.** F-03/F-04 are the solvency-identity *corrections* (they RESTORE the documented `bal + stETH >= claimablePool == Σ claimableWinnings + Σ afkingFunding`); F-01 closes an sDGNRS drain (solvency-positive); F-02 stops unbacked BURNIE emission (BURNIE supply, ETH/pool untouched); F-05 is a capped ETH-denominated presale ALLOWANCE (not a payout); WINDOW/STREAK/CENTURY are off the ETH/`claimablePool` path (stat/streak/count/gate-only). The 5 findings fixes read no `rngWord`; the pass-stat changes touch only RNG-insensitive stat/streak/count slots; Change C tightens (does not loosen) the lazy-pass gate — the jackpot-phase half it now blocks would revert `RngLocked` on its far-future tickets anyway, and the purchase-phase window x01–x10 queues far-future tickets identically to the already-allowed entries. Pre-launch redeploy-fresh (storage break fine, no migration).

> **Posture — ONE contract-boundary HARD STOP at IMPL (371).** The whole Stream-1+Stream-2 contract change ships as ONE carefully-sequenced batched USER-APPROVED `contracts/*.sol` diff with a **HARD STOP at the contract-commit boundary** (applied + locally compiled/tested, NEVER committed without explicit user hand-review — only contract commits need approval; docs/tests/SPEC run hands-off per `[[only-contract-commits-need-approval]]`). The IMPL (371) is the ONE contract gate. **Producer-before-consumer sequencing** is load-bearing: STREAK-01 (`BitPackingLib` shift promotion) MUST precede STREAK-02 (the helper that reads the promoted constant); SOLV-01's return-value plumbing MUST precede the BAF caller's `claimableDelta` fold. Sequential-on-main (worktrees unsafe — submodule + node_modules; `use_worktrees: false`, `no_worktree_paths: [contracts]`). `ContractAddresses.sol` freely modifiable. Tests + planning + docs AGENT-committable.

> **Audit posture — FULL CLOSE WITH ITS OWN INTERNAL SWEEP (NOT deferred).** Like v54.0 + v55.0 + v56.0 + v57.0 (and unlike v50.0 + v51.0, which deferred their sweep → the v52 consolidated audit), **v59.0 runs its own internal 3-skill genuine-PARALLEL adversarial sweep + delta-audit + `audit/FINDINGS-v59.0.md` IN-MILESTONE at TERMINAL (374, AUDIT-01)** — because F-03/F-04 touch the solvency spine and Change A/B/C interact with the lazy-pass far-future-ticket queueing path, both of which must be adversarially probed in-milestone. The second-model `area-solvency` re-run (COV-01) runs EARLY at SPEC so any CONFIRMED new solvency finding can join the SAME IMPL batched diff; its final disposition is folded into the TERMINAL close. (The separate v52 consolidated cross-model audit + the v50/v51 FINDINGS backfill stay a SEPARATE future track — the v59 surface folds into it as an additional track, not a substitute for v59's own close.)

> **Phase numbering** continues from the previous milestone — v58.0 ran 363–369, so **v59.0 starts at Phase 370.** Not reset to 1. (Prior milestones' phase dirs are archived under `.planning/milestones/vXX.0-phases/`.)

> **Milestone shape** is the established v54.0 + v55.0 + v56.0 + v57.0 audit pattern (with a dedicated GAS phase): **370 SPEC** (design-lock the one open fix variant [F-03 return-and-fold-into-`claimableDelta` vs push-remainder-back-to-`futurePrizePool`] + the batched-diff edit order + re-attest every cited `file:line` anchor against the frozen `2b26ec91` + run the COV-01 second-model `area-solvency` re-run EARLY so any confirmed finding joins IMPL; ZERO `contracts/*.sol`) → **371 IMPL** the ONE carefully-sequenced batched contract diff (the 5 findings fixes + Changes A/B/C, producer-before-consumer; HELD for USER hand-review) → **372 GAS** (the gas-neutrality measurement gate — confirm Change A/B/C's lazy-pass path + the F-04 decimator path add no `advanceGame`-ceiling regression; the binding all-evict STAGE has ~19% headroom at 13.60M cold; no structural loop is added [F-06's structural gap-resolution change is WONTFIX]; owns NO REQ-ID; rides the same boundary if a net tune, else Outcome-A no-diff per the v55 350 / v56 / v57 precedent) → **373 TST** (the proofs — one proving regression test per fix [TST-01..07] + the SEC hard floor [SEC-01 RNG-freeze intact across all 8 changes + SEC-02 SOLVENCY-01 re-attested]) → **374 TERMINAL** FULL close (delta-audit + 3-skill genuine-PARALLEL adversarial sweep + the COV-01 adjudication folded in + `audit/FINDINGS-v59.0.md` + the atomic closure flip with the `MILESTONE_V59_AT_HEAD_<sha>` signal). The contract-boundary HARD STOP lives at ONE phase (371 IMPL).

### Phases

- [x] **Phase 370: SPEC — Design-Lock (F-03 fix-variant + batched-diff edit order) + Anchor Re-Attestation vs `2b26ec91` + COV-01 Second-Model Solvency Re-Run** - Settle the one open fix variant (F-03 / SOLV-01: return-and-fold-the-`remainder`-into-`claimableDelta` vs push-the-`remainder`-back-to-`futurePrizePool` via the `_processSoloBucketWinner` pattern) and the batched-diff edit order (producer-before-consumer: STREAK-01 `BitPackingLib` shift promotion before STREAK-02's helper; SOLV-01 return-value before the BAF caller fold); re-attest every cited `file:line` anchor (SALV / AFAFF / SOLV / PRESALE + Changes A/B/C) against the frozen baseline `2b26ec91` (the v58.0 council read this same SHA — confirm no drift), re-grep for any OTHER `frozenUntilLevel` window-reader before the WINDOW-01 edit (the seed's verification checklist), and re-confirm the afking eviction boundary stays inclusive-through-`validThroughLevel` (this change makes freeze/floor MATCH it); and run the COV-01 second-model `area-solvency` XMODEL leg EARLY against `2b26ec91` (via the `.planning/audit-v52/` harness) so any CONFIRMED new solvency finding folds into the SAME IMPL batched diff. Paper + audit-harness only, ZERO `contracts/*.sol`. (completed 2026-06-04)
- [ ] **Phase 371: IMPL — The ONE Carefully-Sequenced Batched Contract Diff (5 council-findings fixes + pass-stat Changes A/B/C)** - Land the single reconciled `contracts/*.sol` diff producer-before-consumer: **SALV-01** (F-01 — widen the quote-loop zero-check `if (n == 0) revert E();` → `if (n == 0 || n > type(uint32).max) revert E();` so the valued quantity equals the debited `uint32` quantity; the `n = 2^32 + k` exploit reverts); **AFAFF-01** (F-02 — add the missing `/ 1 ether` at `GameAfkingModule.sol:879` so `affiliateBase` is stored in whole-BURNIE units, no clamp saturation, no unbacked emission); **SOLV-01** (F-03 — return the BAF whale-pass `remainder` from `_queueWhalePassClaimCore` and fold it into `claimableDelta` per the SPEC-locked variant so `memFuture -= claimed` debits `futurePrizePool`); **SOLV-02** (F-04 — add `claimablePool += uint128(remainder);` alongside the `_creditClaimable(winner, remainder)` at `DegenerusGameDecimatorModule.sol:596` so `claimablePool` covers the re-credited decimator remainder); **PRESALE-01** (F-05 — auto-claim `pendingBurnie` BEFORE flipping `FLAG_USE_TICKETS` in the `subscribe` UPSERT branch so the presale-box halving keys on the settled flag); **WINDOW-01** (Change A — flip the 6 pass-coverage comparisons to inclusive + update the stale `hasAnyLazyPass` docstring, fixing the latent level-10 `levelCount` over-count, leaving the extension/early-renewal math untouched); **STREAK-01** (Change B prereq — promote `MINT_STREAK_LAST_COMPLETED_SHIFT (=160)` into `BitPackingLib` + repoint the 4 `MintStreakUtils` refs, no behavior change); **STREAK-02** (Change B — add the pure `_withPassStreakFrontLoad` delta-based helper folded into `data` before the SSTORE at the 3 pass-activation sites, keeping the active-pass 50-floor on top); **CENTURY-01** (Change C — edit the x0 term of the lazy-pass gate to `(currentLevel % 100 == 0 && jackpotPhaseFlag)` so a lazy pass is buyable at `level==x00 && !jackpotPhaseFlag` across all centuries, still blocked during the jackpot phase). Authored producer-before-consumer (STREAK-01 before STREAK-02; SOLV-01 return-value before the BAF caller fold), applied to `contracts/` and locally compiling (`forge build` clean), then HELD at the contract-commit boundary for explicit user hand-review.
- [ ] **Phase 372: GAS — Gas-Neutrality Gate (Change A/C lazy-pass path + F-04 decimator path add no advanceGame-ceiling regression)** - Confirm the bundle adds no `advanceGame`-ceiling regression under the security-over-gas floor: Change A's inclusive-window flips + Change C's century purchase-phase gate edit are comparison/gate-only (no new storage, no new loop), the STREAK-01/02 front-load is a delta-based fold into the existing single SSTORE at the activation sites (no extra cold slot), and the F-04 `claimablePool += uint128(remainder)` is a single warm add alongside an existing write — so the lazy-pass far-future-ticket path + the decimator remainder path stay within budget; the binding all-evict STAGE retains ~19% headroom at 13.60M cold; NO structural loop is added (F-06's structural gap-resolution change is WONTFIX, explicitly out of scope). Any net behavior-preserving tune rides the same batched USER-APPROVED diff held at the contract boundary; the expected outcome is gas-neutral-by-construction → recorded as the Outcome-A no-diff outcome per the v55 350 / v56 / v57 precedent. (Owns no REQ-ID — the gas-neutrality measurement gate.)
- [ ] **Phase 373: TST — One Proving Regression Test per Fix (TST-01..07) + RNG-Freeze Intact + SOLVENCY-01 Re-Attested (SEC-01/02)** - Prove the milestone behaviorally correct + the hard security floor empirically against the frozen `2b26ec91` baseline (the SEC gate): one proving test per fix — **TST-01** (SALV — the `n = 2^32 + k` salvage swap REVERTS; a positive small-`n` swap still values exactly the entries it debits); **TST-02** (AFAFF — an afking buy accrues `affiliateBase` in whole-BURNIE units, `claim` mints ~70 whole BURNIE not ~100M); **TST-03** (SOLV — after a BAF whale-pass remainder AND a non-gameover decimator whale-pass remainder, the `claimablePool == Σ claimableWinnings + Σ afkingFunding` identity holds, `futurePrizePool` is debited by the BAF remainder, the tail claimant withdraws with no underflow revert); **TST-04** (PRESALE — re-`subscribe(useTickets=false)` then `claimAfkingBurnie` halves the presale-box credit exactly once); **TST-05** (WINDOW+STREAK — fresh L1 lazy pass → `levelCount += 10`, `frozenUntilLevel=10`, `streak=10`, `lastCompleted=10`; freeze covers manual mints 1–10 with no double-count, first un-frozen mint at L11 increments; floor + whale bonus active through L10 inclusive; renewal = delta-only; the whale 100-bundle mirrors lazy); **TST-06** (CENTURY — buyable at `level==x00 && !jackpotPhaseFlag`, reverts at `jackpotPhaseFlag`, x90/x9/x0 unchanged, x01–x10 window queues fine across multiple centuries); **TST-07** (the forge + Hardhat suite is NON-WIDENING vs `2b26ec91`, every pre-existing red enumerated BY NAME, the carried v57 pre-launch harness layout/RNG artifacts stay characterized); plus the hard floor — **SEC-01** (RNG-freeze intact across all 8 changes, no new player-manipulable VRF-derived read/write — proven empirically + carried to the adversarial close) + **SEC-02** (SOLVENCY-01 re-attested — `claimablePool` never exceeds `bal + stETH` after a BAF-remainder, a decimator-remainder, and a salvage-swap event — proven empirically + carried adversarial). Tests AGENT-committable (no contract-commit gate).
- [ ] **Phase 374: TERMINAL — Delta Audit + 3-Skill Genuine-PARALLEL Adversarial Sweep + COV-01 Adjudication + FINDINGS-v59.0 + Closure Flip** - Close the v59.0 audit subject (the carefully-sequenced batched diff — the 5 council-findings fixes + Changes A/B/C, FROZEN at the IMPL+GAS HEAD) via a FULL close that runs its own internal sweep IN-MILESTONE (NOT deferred to v52): a delta-audit (every changed surface NON-WIDENING vs the baseline `2b26ec91`, RNG-freeze + SOLVENCY-01 re-attested with grep/diff anchors) + the mandatory 3-skill genuine-PARALLEL adversarial review (`/contract-auditor` + `/economic-analyst` + `/zero-day-hunter`; `/degen-skeptic` = the dual-gate filter per `D-271-ADVERSARIAL-02`) focused on the F-03/F-04 solvency-identity restoration + the Change A/B/C interaction with the lazy-pass far-future-ticket path + the COV-01 second-model solvency re-run adjudication folded into the close + `audit/FINDINGS-v59.0.md` (chmod 444, documenting all 5 findings RESOLVED-AT-V59, the F-06 WONTFIX + F-07 INFO dispositions, the pass-stat seed, and any COV-01 result) + the atomic closure flip with the `MILESTONE_V59_AT_HEAD_<sha>` signal — re-attesting all v59.0 requirements against the frozen closure HEAD (AUDIT-01).

---

## Phase Details (v59.0)

### Phase 370: SPEC — Design-Lock + Anchor Re-Attestation vs `2b26ec91` + COV-01 Second-Model Solvency Re-Run

**Goal**: The v59.0 open design decisions are settled in writing so the IMPL phase authors a fully reconciled diff with zero "by construction" assumptions, and the second-model solvency coverage gap is closed BEFORE relying on the F-03/F-04 corrections: the one open fix variant (F-03 / SOLV-01) is LOCKED, the batched-diff edit order is fixed, every cited anchor is re-attested against the frozen `2b26ec91`, and the COV-01 `area-solvency` re-run is adjudicated EARLY so any confirmed finding joins IMPL. Paper + audit-harness only, ZERO `contracts/*.sol`.
**Type**: SPEC
**Depends on**: Nothing (first v59.0 phase; consumes the v58.0-audited frozen contract subject `2b26ec91` as the baseline, plus the design-lock inputs `audit/FINDINGS-v58.0.md` + `.planning/PLAN-PASS-STAT-FRONTLOAD.md`)
**Requirements**: COV-01, SPEC-01
**Success Criteria** (what must be TRUE):

  1. The one open fix variant is LOCKED + the batched-diff edit order is fixed (SPEC-01 part 1) — F-03 / SOLV-01's choice is decided (return-the-`remainder`-from-`_queueWhalePassClaimCore`-and-fold-into-`claimableDelta` vs push-the-`remainder`-back-to-`futurePrizePool` via the `_processSoloBucketWinner` pattern), with the rationale recorded; and the producer-before-consumer edit order is mapped (STREAK-01 `BitPackingLib` shift promotion BEFORE STREAK-02's helper; SOLV-01 return-value BEFORE the BAF caller's `memFuture -= claimed` fold) so the IMPL authors a single reconciled diff.
  2. Every cited `file:line` anchor is re-attested against the frozen baseline `2b26ec91` (SPEC-01 part 2) — the SALV (`DegenerusGameMintStreakUtils.sol:173-181`/`DegenerusGameMintModule.sol:1013-1015`/`:1059`), AFAFF (`GameAfkingModule.sol:879` + the `:826`/`:887` siblings), SOLV (`DegenerusGamePayoutUtils.sol:46-65`/`DegenerusGameAdvanceModule.sol:897-903`/`DegenerusGameDecimatorModule.sol:392-399`/`:596`/`:1588`), PRESALE (`GameAfkingModule.sol:397-398`/`:1599-1601`), and Change-A/B/C anchors (the 6 WINDOW comparisons + `hasAnyLazyPass` docstring, the 3 STREAK activation sites + the `MINT_STREAK_LAST_COMPLETED_SHIFT` local, the CENTURY gate `DegenerusGameWhaleModule.sol:411-416`) are grep-verified against `2b26ec91` (the same SHA the v58.0 council read) with any drift corrected in the SPEC (no "by construction" survives un-checked).
  3. The WINDOW-01 pre-edit verification is complete (SPEC-01 part 3) — a re-grep for any OTHER `frozenUntilLevel` window-reader (the seed's verification checklist) confirms exactly the 6 comparisons to flip + the `> targetFrozenLevel` extension math + the `frozenUntilLevel > currentLevel + 7` early-renewal guard are left UNTOUCHED (different semantics), and the afking eviction boundary is re-confirmed inclusive-through-`validThroughLevel` (this change makes freeze/floor MATCH the already-inclusive eviction boundary, not diverge from it).
  4. The COV-01 second-model `area-solvency` XMODEL re-run is adjudicated EARLY (COV-01) — the `area-solvency` leg is re-run with a second independent model against the frozen `2b26ec91` (via the `.planning/audit-v52/` harness, after a council smoke-test), Claude owns the verdict against frozen source (parallel verification), any CONFIRMED new solvency claim is routed to fold into the SAME IMPL batched diff, refuted claims are logged, and the disposition is recorded for the TERMINAL `audit/FINDINGS-v59.0.md` — closing the v58.0 coverage gap (the original Gemini solvency leg refused via Plan-Mode) BEFORE the milestone relies on the F-03/F-04 corrections. (The rng-freeze Gemini gap is deliberately NOT re-run — Claude 364/365 RNGSAFE/RNGVAR + the refuted Codex C1 already deep-cover the freeze.)

**Plans**: 2 plans (2 waves collapse to 1 — both autonomous, no file overlap, run in parallel)

- [x] 370-01-PLAN.md — Design-lock SPEC: re-attest every anchor vs frozen `2b26ec91`, lock the F-03/SOLV-01 fix variant + the producer-before-consumer edit order, complete the WINDOW-01 pre-edit verification (SPEC-01)
- [x] 370-02-PLAN.md — COV-01 second-model `area-solvency` XMODEL re-run vs `2b26ec91` + per-claim adjudication against frozen source, routing any confirmed finding into the Phase-371 IMPL diff (COV-01)

**UI hint**: no

### Phase 371: IMPL — The ONE Carefully-Sequenced Batched Contract Diff (5 council-findings fixes + pass-stat Changes A/B/C)

**Goal**: The v59.0 bundle lands as a single reconciled `contracts/*.sol` diff under the SPEC's settled fix-variant + edit order, authored producer-before-consumer: the 5 council-findings fixes (SALV-01 / AFAFF-01 / SOLV-01 / SOLV-02 / PRESALE-01) + the deferred pass-stat seed (WINDOW-01 / STREAK-01 / STREAK-02 / CENTURY-01) — with RNG-freeze intact + the SOLVENCY-01 identity restored (F-03/F-04) / off-path (the rest). Applied to `contracts/` and locally compiling (`forge build` clean), then HELD at the contract-commit boundary for explicit user hand-review.
**Type**: IMPL (CONTRACT BOUNDARY — the ONE carefully-sequenced batched USER-APPROVED `contracts/*.sol` diff; `autonomous: false` at the commit gate; never auto-commit contracts)
**Depends on**: Phase 370 (the SPEC must lock the F-03 fix variant + the producer-before-consumer edit order + grep-attest the anchors first; any COV-01-confirmed solvency finding folds into this same diff)
**Requirements**: SALV-01, AFAFF-01, SOLV-01, SOLV-02, PRESALE-01, WINDOW-01, STREAK-01, STREAK-02, CENTURY-01
**Success Criteria** (what must be TRUE):

  1. The 3 sDGNRS/BURNIE-supply fixes land solvency-positive (SALV-01 / AFAFF-01 / PRESALE-01) — the salvage quote-loop zero-check is widened to `if (n == 0 || n > type(uint32).max) revert E();` so every reachable swap values exactly the `uint32` entries it removes and the `n = 2^32 + k` exploit reverts (SALV-01); the afking `affiliateBase` accrual gains the missing `/ 1 ether` at `GameAfkingModule.sol:879` so it stores whole-BURNIE units with no clamp saturation and no unbacked `recordAmount=0` emission, mirroring the `:826`/`:887` siblings (AFAFF-01); and the `subscribe` UPSERT branch auto-claims `pendingBurnie` BEFORE flipping `FLAG_USE_TICKETS` so the presale-box halving keys on the settled flag and the allowance is halved exactly once (PRESALE-01).
  2. The 2 whale-pass remainder solvency-identity corrections land (SOLV-01 / SOLV-02) — `_queueWhalePassClaimCore` returns the BAF whale-pass `remainder` and the BAF caller folds it into `claimableDelta` (per the SPEC-locked variant) so `memFuture -= claimed` debits `futurePrizePool` and the same wei no longer backs two obligations (SOLV-01, F-03); and the non-gameover `_creditDecJackpotClaimCore` path bumps `claimablePool += uint128(remainder);` alongside the `_creditClaimable(winner, remainder)` at `DegenerusGameDecimatorModule.sol:596` so `Σ claimableWinnings` never exceeds `claimablePool` and the tail claimant no longer reverts on checked-math underflow at `claimWinnings` (SOLV-02, F-04; the gameOver + terminal claim legs are already correct and unaffected). Both RESTORE `claimablePool == Σ claimableWinnings + Σ afkingFunding`.
  3. Change A (inclusive 1–10 pass-coverage window) lands correct (WINDOW-01) — the 6 "is this level covered / is the pass active" comparisons are flipped to inclusive (`DegenerusGameMintModule.sol:291`/`:295`, `DegenerusGameMintStreakUtils.sol:262`/`:310`/`:56`, `DegenerusGame.sol:1678` `hasAnyLazyPass`) AND its stale docstring is updated to the inclusive semantics; post-flip, freeze clears at `lvl > frozenUntilLevel` (level 11) so a fresh L1 pass freezes 1–10 and `+10 levelCount` covers exactly 10 levels (no double-count, the latent level-10 over-count fixed); the `> targetFrozenLevel` extension math + the `frozenUntilLevel > currentLevel + 7` early-renewal guard are LEFT UNTOUCHED (different semantics).
  4. Change B (front-load the LEVEL mint streak) lands producer-before-consumer (STREAK-01 / STREAK-02) — `MINT_STREAK_LAST_COMPLETED_SHIFT (=160)` is promoted into `BitPackingLib` (between `WHALE_BUNDLE_TYPE_SHIFT` and `HAS_DEITY_PASS_SHIFT`) with the 4 `MintStreakUtils` refs repointed and the local deleted, no behavior change (STREAK-01); and the pure delta-based contiguity-aware `_withPassStreakFrontLoad` helper (`streak += levelsToAdd` if the prior run reaches the pass start with no gap else reset to the span, `MASK_24`-saturating, `lastCompleted` never regresses) is folded into `data` before the existing single SSTORE at the 3 pass-activation sites (`_activate10LevelPass`, `_applyWhalePassStats`, the inline `_purchaseWhaleBundle` block), keeping the active-pass 50-floor on top — so a fresh L1 lazy buyer enters L11 with `lastCompleted=10, streak=10` (continuable) instead of cliffing to 0, and a whale 100-bundle → streak 100 (STREAK-02). STREAK-01 is authored BEFORE STREAK-02.
  5. Change C (lazy pass during a century's purchase phase) lands gate-only (CENTURY-01) — the x0 term of the lazy-pass gate (`DegenerusGameWhaleModule.sol:411-416`) is edited `(currentLevel % 10 != 0 || currentLevel % 100 == 0)` → `(currentLevel % 10 != 0 || (currentLevel % 100 == 0 && jackpotPhaseFlag))` so a lazy pass is buyable at `level==x00 && !jackpotPhaseFlag` (ALL centuries 100/200/300/…) and still blocked during the jackpot phase, with the `:409-410` comment + the `purchaseLazyPass` `@dev`/`@custom:reverts` doc updated; the change is gate-only (independent of Changes A/B), the window x01–x10 contains no century level (no century-bonus-in-window exposure, no overrun), and the jackpot-phase half it still blocks would revert `RngLocked` on its far-future tickets anyway — making the blanket exclusion precise, not looser.
  6. The diff is producer-before-consumer + `forge build` is clean + HELD at the boundary (all 9 IMPL reqs) — the whole diff is authored producer-before-consumer per the SPEC edit-order map (STREAK-01 before STREAK-02; SOLV-01 return-value before the BAF caller fold), applied to `contracts/` and locally compiling (`forge build` clean; `ContractAddresses.sol` freely modifiable), but NOT committed without explicit user hand-review of the single batched diff (RNG-freeze stays intact — no new player-manipulable VRF-derived read/write — and SOLVENCY-01 is restored by F-03/F-04 / off-path for the rest, both carried as IMPL invariants and re-proven at TST 373).

**Plans**: TBD
**UI hint**: no

### Phase 372: GAS — Gas-Neutrality Gate (Change A/C lazy-pass path + F-04 decimator path)

**Goal**: The cost impact of the bundle is confirmed under the security-over-gas floor: Change A/C's lazy-pass path + the F-04 decimator path add no `advanceGame`-ceiling regression (the binding all-evict STAGE retains ~19% headroom at 13.60M cold; no structural loop is added — F-06's structural gap-resolution change is WONTFIX). Any net change rides the same batched USER-APPROVED diff at the contract boundary; the expected outcome is gas-neutral-by-construction → Outcome-A no-diff per the v55 350 / v56 / v57 precedent.
**Type**: GAS (CONTRACT BOUNDARY — rides the same boundary if a net tune; else Outcome-A no-diff)
**Depends on**: Phase 371 (the IMPL diff must be applied + locally compiling before the gas marginals can be measured)
**Requirements**: (none — the gas-neutrality measurement gate)
**Success Criteria** (what must be TRUE):

  1. The lazy-pass path stays within budget (Change A/B/C) — Change A's inclusive-window comparison flips + Change C's century-gate edit are comparison/gate-only (no new storage, no new loop), and the STREAK-01/02 front-load is a delta-based fold into the existing single SSTORE at the activation sites (no extra cold slot) → the lazy-pass far-future-ticket path adds no `advanceGame`-ceiling regression, measured against the frozen baseline `2b26ec91`.
  2. The F-04 decimator remainder path stays within budget (SOLV-02) — the added `claimablePool += uint128(remainder);` is a single warm add alongside the existing `_creditClaimable` write, adding no measurable cost to the decimator remainder path; the binding all-evict STAGE retains ~19% headroom at 13.60M cold; NO structural loop is added (F-06's structural gap-resolution change is explicitly WONTFIX / out of scope).
  3. The outcome is recorded — if the bundle is gas-neutral-by-construction (the expected outcome — the fixes are arithmetic/accounting/guard edits + the pass-stat changes are comparison/delta-fold/gate edits with no packed-field storage-layout growth), that is recorded as the Outcome-A no-diff outcome (per the v55 350 / v56 / v57 precedent); any net behavior-preserving change rides the same batched USER-APPROVED diff held at the contract-commit boundary.

**Plans**: TBD
**UI hint**: no

### Phase 373: TST — One Proving Regression Test per Fix (TST-01..07) + RNG-Freeze Intact + SOLVENCY-01 Re-Attested

**Goal**: The milestone is proven behaviorally correct + the hard security floor proven empirically against the frozen `2b26ec91` baseline (the SEC gate): one proving regression test per fix (TST-01..06 — the gap that let each finding hide) + a NON-WIDENING regression (TST-07), with RNG-freeze intact across all 8 changes (SEC-01) and SOLVENCY-01 re-attested (SEC-02 — `claimablePool` never exceeds `bal + stETH` after a BAF-remainder, a decimator-remainder, and a salvage-swap event).
**Type**: TST
**Depends on**: Phase 372 (the IMPL+GAS subject must be settled — the per-fix + freeze + solvency + non-widening proofs run against the frozen contract subject)
**Requirements**: SEC-01, SEC-02, TST-01, TST-02, TST-03, TST-04, TST-05, TST-06, TST-07
**Success Criteria** (what must be TRUE):

  1. The 5 council-findings fixes each have a proving regression test (TST-01..04 + the SOLV pair in TST-03) — the `n = 2^32 + k` salvage swap REVERTS (`E()`) while a positive small-`n` swap still values exactly the entries it debits (TST-01, the missing bound that let F-01 hide); an afking buy accrues `affiliateBase` in whole-BURNIE units with no 100M clamp saturation and `DegenerusAffiliate.claim` mints ~70 whole BURNIE to the A/U1/U2 tree not ~100M (TST-02); after a BAF whale-pass remainder AND a non-gameover decimator whale-pass remainder (`lootboxPortion > 5 ETH`, non-exact-2.25-multiple) the `claimablePool == Σ claimableWinnings + Σ afkingFunding` identity holds, `futurePrizePool` is debited by the BAF remainder, and the tail claimant withdraws with no underflow revert (TST-03); and a ticket-sub re-`subscribe(useTickets=false)` then `claimAfkingBurnie` halves the presale-box credit exactly once with the pre-claim firing in the UPSERT branch (TST-04).
  2. The pass-stat Changes A/B/C each have a proving regression test (TST-05 / TST-06) — a fresh lazy pass @ L1 yields `levelCount += 10`, `frozenUntilLevel=10`, `streak=10`, `lastCompleted=10`; freeze covers manual mints 1–10 (no `levelCount` double-count), the first un-frozen mint at L11 increments `levelCount`, the activity-score floor + whale bonus are active through L10 inclusive and off at L11, renewal/overlap is delta-only (no double-credit) with `lastCompleted` advancing to the new horizon, and the whale 100-bundle path mirrors lazy (TST-05); and a lazy pass is buyable at `level==x00 && !jackpotPhaseFlag`, reverts at `level==x00 && jackpotPhaseFlag`, x90/x9/x0 entries are unchanged, and the x01–x10 window queues fine across multiple centuries (100/200/…) (TST-06).
  3. The suite is NON-WIDENING vs the frozen baseline `2b26ec91` (TST-07) — the forge + Hardhat suite is enumerated BY NAME (every pre-existing red), the v59 fixes add green, and the carried v57 pre-launch harness layout/RNG artifacts stay characterized (no new contract regression introduced by the bundle).
  4. RNG-freeze is intact across all 8 changes (SEC-01) — no new player-manipulable VRF-derived read/write: the 5 findings fixes are arithmetic/accounting/guard (read no `rngWord`), WINDOW + STREAK touch only RNG-insensitive stat/streak/count slots, and CENTURY tightens the lazy-pass gate (the jackpot-phase half it now blocks would revert `RngLocked` on its far-future tickets anyway; the purchase-phase window x01–x10 queues far-future tickets identically to the already-allowed x90/x9/x0 entries) — proven empirically (determinism) and carried to the adversarial close (374).
  5. SOLVENCY-01 is re-attested (SEC-02) — F-03/F-04 RESTORE the `claimablePool == Σ claimableWinnings + Σ afkingFunding` identity (the corrections), F-01 closes an sDGNRS drain (solvency-positive), F-02 stops unbacked BURNIE emission (BURNIE supply, ETH/pool untouched), F-05 is a capped ETH-denominated presale ALLOWANCE (not a payout), and WINDOW/STREAK/CENTURY are off the ETH/`claimablePool` path; the solvency review explicitly proves `claimablePool` never exceeds `bal + stETH` after a BAF-remainder, a decimator-remainder, and a salvage-swap event — proven empirically (solvency-invariant) and carried to the adversarial close (374).

**Plans**: TBD
**UI hint**: no

### Phase 374: TERMINAL — Delta Audit + 3-Skill Genuine-PARALLEL Adversarial Sweep + COV-01 Adjudication + FINDINGS-v59.0 + Closure Flip

**Goal**: Close the v59.0 audit subject (the carefully-sequenced batched diff — the 5 council-findings fixes + Changes A/B/C, FROZEN at the IMPL+GAS HEAD) via a FULL close that runs its own internal sweep IN-MILESTONE (NOT deferred to v52): a delta-audit (every changed surface NON-WIDENING vs the baseline `2b26ec91` with the RNG-freeze + SOLVENCY-01 anchors re-attested) + the mandatory 3-skill genuine-PARALLEL adversarial economic review + the COV-01 second-model solvency re-run adjudication folded in + `audit/FINDINGS-v59.0.md` (chmod 444) + the atomic closure flip with the `MILESTONE_V59_AT_HEAD_<sha>` signal — re-attesting all v59.0 requirements against the frozen closure HEAD.
**Type**: TERMINAL (FULL close — sweep IN-MILESTONE)
**Depends on**: Phase 373 (the IMPL+GAS+TST subject must be settled + the SEC floor proven before the delta-audit + adversarial sweep + closure flip run against the frozen contract subject)
**Requirements**: AUDIT-01
**Success Criteria** (what must be TRUE):

  1. The delta-audit is clean (AUDIT-01 part 1) — every changed surface across the 9 contract reqs (SALV / AFAFF / SOLV / PRESALE / WINDOW / STREAK / CENTURY) is NON-WIDENING vs the baseline `2b26ec91` with grep/diff anchors and zero orphan hunks; the RNG-freeze (no new player-manipulable VRF-derived read/write) + the SOLVENCY-01 identity (`claimablePool == Σ claimableWinnings + Σ afkingFunding`, restored by F-03/F-04) are re-attested with anchors.
  2. The 3-skill genuine-PARALLEL adversarial sweep + the COV-01 adjudication land (AUDIT-01 part 2) — the mandatory `/contract-auditor` + `/economic-analyst` + `/zero-day-hunter` review runs as genuine concurrent spawns (`/degen-skeptic` = the dual-gate filter per `D-271-ADVERSARIAL-02`), focused on the F-03/F-04 solvency-identity restoration + the Change A/B/C interaction with the lazy-pass far-future-ticket path; and the COV-01 second-model `area-solvency` re-run adjudication (any CONFIRMED finding already folded into the IMPL diff; refuted claims logged) is folded into the close with its final disposition recorded.
  3. FINDINGS-v59.0 is authored + the closure flip is atomic (AUDIT-01 part 3) — `audit/FINDINGS-v59.0.md` (chmod 444) records all 5 findings RESOLVED-AT-V59, the F-06 WONTFIX + F-07 INFO dispositions, the pass-stat seed, and any COV-01 result; the atomic closure flip emits the `MILESTONE_V59_AT_HEAD_<sha>` signal; all v59.0 requirements are re-attested against the frozen closure HEAD; KNOWN-ISSUES.md is byte-unmodified unless a genuine new finding is recorded.

**Plans**: TBD
**UI hint**: no

---

## Progress (v59.0)

**Execution Order:** Phases execute in numeric order: 370 → 371 → 372 → 373 → 374

| Phase | Milestone | Plans Complete | Status | Completed |
|-------|-----------|----------------|--------|-----------|
| 370. SPEC — Design-Lock (F-03 fix-variant + edit order) + Anchor Re-Attestation vs `2b26ec91` + COV-01 Second-Model Solvency Re-Run | v59.0 | 2/2 | Complete    | 2026-06-04 |
| 371. IMPL — The ONE Carefully-Sequenced Batched Contract Diff (5 council-findings fixes + pass-stat Changes A/B/C) | v60.0 | 1/1 | ✅ Shipped (`4577cfb6`; SOLV-02 corrected `c0565ee4`) | 2026-06-04 |
| 372. GAS — Gas-Neutrality Gate (Change A/C lazy-pass path + F-04 decimator path) | v60.0 | — | ⊘ Dropped (folded into the v60 gasceil hunt → `6d2c8d0c`) | - |
| 373. TST — One Proving Regression Test per Fix (TST-01..07) + RNG-Freeze Intact + SOLVENCY-01 Re-Attested | v60.0 | — | ↪ Folded into the v60 maximal audit | 2026-06-06 |
| 374. TERMINAL — Delta Audit + 3-Skill Adversarial Sweep + COV-01 Adjudication + FINDINGS + Closure Flip | v60.0 | — | ↪ Folded into the v60 audit → `audit/FINDINGS-v60.0.md` + `MILESTONE_V60_AT_HEAD_2bee6d6f` | 2026-06-06 |

> **🔒 v59.0 CONTRACT-BOUNDARY HARD STOP (ONE gate — 371 IMPL · 372 GAS rides the same boundary if it produces a net diff).** Phase 371 IMPL is THE contract phase — the carefully-sequenced batched diff (the 5 council-findings fixes + Changes A/B/C, authored producer-before-consumer: STREAK-01 before STREAK-02, SOLV-01 return-value before the BAF caller fold) is applied to `contracts/` and locally compiled (`forge build` clean) but HELD at the contract-commit boundary, NEVER committed without explicit user hand-review (only contract commits need approval per `[[only-contract-commits-need-approval]]`). Phase 372 GAS is the gas-neutrality measurement gate — any net behavior-preserving change rides the same batched USER-APPROVED diff at the same boundary (or records Outcome-A no-diff per the v55 350 / v56 / v57 precedent). `ContractAddresses.sol` freely modifiable; tests + planning + docs AGENT-committable.

> **🔓 v59.0 AUDIT POSTURE — FULL CLOSE (internal sweep NOT deferred).** Like v54.0 + v55.0 + v56.0 + v57.0 (and unlike v50.0 + v51.0, whose internal sweeps were deferred → the v52 consolidated audit), **v59.0 runs its own internal 3-skill genuine-PARALLEL adversarial sweep + delta-audit + `audit/FINDINGS-v59.0.md` at TERMINAL (374, AUDIT-01)** — because F-03/F-04 touch the solvency spine and Change A/B/C interact with the lazy-pass far-future-ticket queueing path, both of which must be adversarially probed in-milestone. The second-model `area-solvency` re-run (COV-01) runs EARLY at SPEC (370) so any confirmed finding joins the same IMPL diff; its disposition folds into the TERMINAL close. The separate v52 consolidated cross-model audit folds the v59 surface into its cumulative sweep as an additional track, not a substitute.

---

## Coverage (v59.0)

**21/21 v59.0 requirements mapped to exactly one phase — 0 orphaned, 0 duplicated.**

| Phase | Requirements | Count |
|-------|--------------|-------|
| 370 SPEC | COV-01, SPEC-01 | 2 |
| 371 IMPL | SALV-01, AFAFF-01, SOLV-01, SOLV-02, PRESALE-01, WINDOW-01, STREAK-01, STREAK-02, CENTURY-01 | 9 |
| 372 GAS | (none — the gas-neutrality measurement gate) | 0 |
| 373 TST | SEC-01, SEC-02, TST-01, TST-02, TST-03, TST-04, TST-05, TST-06, TST-07 | 9 |
| 374 TERMINAL | AUDIT-01 | 1 |
| **Total** | | **21** |

**Per-category split (verification):**

| Category | Total | SPEC (370) | IMPL (371) | GAS (372) | TST (373) | TERMINAL (374) |
|----------|-------|------------|------------|-----------|-----------|----------------|
| SALV | 1 | — | 1 (01) | — | — | — |
| AFAFF | 1 | — | 1 (01) | — | — | — |
| SOLV | 2 | — | 2 (01,02) | — | — | — |
| PRESALE | 1 | — | 1 (01) | — | — | — |
| WINDOW | 1 | — | 1 (01) | — | — | — |
| STREAK | 2 | — | 2 (01,02) | — | — | — |
| CENTURY | 1 | — | 1 (01) | — | — | — |
| COV | 1 | 1 (01) | — | — | — | — |
| SPEC | 1 | 1 (01) | — | — | — | — |
| SEC | 2 | — | — | — | 2 (01,02) | — |
| TST | 7 | — | — | — | 7 (01..07) | — |
| AUDIT | 1 | — | — | — | — | 1 (01) |
| **Total** | **21** | **2** | **9** | **0** | **9** | **1** |

**Center-of-gravity rationale:**

- **The 9 contract reqs (SALV-01 · AFAFF-01 · SOLV-01 · SOLV-02 · PRESALE-01 · WINDOW-01 · STREAK-01 · STREAK-02 · CENTURY-01)** → IMPL (371) as the single batched diff. Producer-before-consumer ordering is owned here (STREAK-01 `BitPackingLib` shift promotion BEFORE STREAK-02's helper; SOLV-01 return-value BEFORE the BAF caller fold) — the SPEC (370) only LOCKS the order + the F-03 variant + the anchors; it builds nothing.
- **COV-01** (the second-model `area-solvency` re-run) → SPEC (370), where it runs EARLY so any CONFIRMED new solvency finding can fold into the SAME IMPL diff (and its final disposition folds into the TERMINAL close). **SPEC-01** (design-lock + anchor re-attestation) → SPEC (370) as the design gate; no double-counting — SPEC-01 = the lock, the IMPL reqs = the built behaviors.
- **GAS phase (372) owns NO REQ-ID** — it is the gas-neutrality measurement gate (confirm Change A/C's lazy-pass path + the F-04 decimator path add no `advanceGame`-ceiling regression; the binding all-evict STAGE has ~19% headroom at 13.60M cold; no structural loop added). Outcome-A no-diff in the expected gas-neutral-by-construction case, per the v55 350 / v56 / v57 precedent (v59 has no GAS-* requirements).
- **SEC-01 / SEC-02 + TST-01..07** → TST (373). SEC-01/02 (RNG-freeze intact + SOLVENCY-01 re-attested) are PROVEN empirically as the hard gate (the SPEC re-attestation at 370 is the design gate, NOT a separate count); the TERMINAL adversarial review (AUDIT-01) re-confirms SEC, but SEC-01/02's center-of-gravity (where the property is first PROVEN) is TST. TST-01..07 are the one-proving-test-per-fix regressions (the gap that let each finding hide) + the NON-WIDENING ledger — test-file edits, AGENT-committable, off the contract-commit gate.
- **AUDIT-01** (the TERMINAL FULL close) → TERMINAL (374); it re-attests all 21 v59.0 requirements + runs the in-milestone delta-audit + the 3-skill adversarial sweep + the COV-01 adjudication + `audit/FINDINGS-v59.0.md` + the atomic closure flip with the `MILESTONE_V59_AT_HEAD_<sha>` signal.

---

## ✅ v58.0 RNG-Safety, ETH-Flow & Gas-Ceiling Documentation Audit (SHIPPED 2026-06-04 — document-only; closure signal `MILESTONE_V58_AT_HEAD_2b26ec91810a733e15666a4c23e8f365a4f04f51`; 7 CONFIRMED findings [1 CRIT · 1 HIGH · 2 MED · 2 LOW · 1 INFO] + 5 REFUTED, all routed to a later remediation milestone; baseline = v57.0 closure HEAD `b7237db4`, contract subject frozen `2b26ec91`; phases 363-369)

**Milestone:** v58.0 (started 2026-06-04)
**Defined:** 2026-06-04
**Audit subject:** the full cumulative v50→v57 contract surface at the frozen v57-closure HEAD (contract subject `2b26ec91`; `b7237db4` closure + `0d5d2afe` archive on top are docs-only → identical contracts). 363 PREP-01 pins `FROZEN_SHA`. Every cited `file:line` MUST be re-attested against the frozen HEAD before use (the `audit-v52/` context-pack refs predate the AfKing.sol→GameAfkingModule dissolution).
**Scope source:** `.planning/REQUIREMENTS.md` (22 v58.0 REQ-IDs across 10 categories: PREP 3 · RNGSAFE 3 · RNGVAR 3 · ETHFLOW 3 · GASCEIL 3 · FORENSIC 2 · XMODEL 1 · SITE 1 · AUDIT 1 · OPS 2) + the milestone init (2026-06-04) + the drafted plan `.planning/PLAN-V58-CONSOLIDATED-AUDIT.md` (reshaped). **No research** — pure in-repo code analysis.

> **Scope (USER-locked 2026-06-04):** a DOCUMENT-ONLY, autonomous, resumable audit producing FIVE artifacts packaged as ONE cohesive dark-themed static site (`audit/site/`): (1) a **Player-Function RNG-Safety** page — every player-facing function, what it does + WHY it cannot influence any RNG-dependent variable, + a sanity/comment check that flags behavior↔name/comment mismatches as findings (e.g. a BURNIE-burning buy that queues zero tickets); (2) an **RNG-Variable Freeze** table — every RNG-interacting variable + why it cannot change between RNG request and resolution; (3) a full **ETH-Flow chart** — inflow → pool cycling → outflow; (4) an **advanceGame Gas-Ceiling** table — theoretical max gas per stage vs the 16,777,216 (EIP-7825) per-tx cap; (5) a side-track **BURNIE ticket-mint forensic**. Coverage basis = the full cumulative v50→v57 surface. The drafted plan's TOP track (the AfKing reward-leak hunt) FOLDS into the RNG-safety + ETH-flow + solvency tracks rather than leading.

> **Posture — DOCUMENT-ONLY, NO CONTRACT GATE.** This milestone changes NO `contracts/*.sol` — it produces analysis + HTML + markdown only → there is NO contract-commit HARD STOP and the whole run is hands-off. Any fix the audit surfaces routes to a LATER remediation milestone. Deliverables persist under `audit/` (and `audit/site/`); planning is gitignored-local. The v50/v51 `FINDINGS` backfill is DROPPED from this run (carried forward). External Gemini + Codex are a ~5-prompt targeted SECOND OPINION only ($20-tier), NOT the per-cell council.

> **Execution posture — AUTONOMOUS + RESUMABLE + BUDGET-PACED (USER-locked 2026-06-04, OPS-01/02).** Runs autonomously with no mid-run user interaction; RESUMES GRACEFULLY after any usage-limit hit or error (each phase checkpoints its partial artifact + commits per-phase so a restart continues from the last checkpoint). PACED to ~45% of a weekly usage allotment over 24h, NEVER slamming a 5h cap mid-task → `concurrency: 1`, small batches with frequent persist, spine-weighted depth (RNG-freeze + solvency + the AfKing surface first), pausable between work windows. The `audit-v52/` harness (`coordinator.sh`, `cross-model/`, `context-pack/`) is the run scaffold.

> **Phase numbering** continues from the previous milestone — v57.0 ran 358-362, so **v58.0 starts at Phase 363.** Not reset to 1.

> **Milestone shape** — a documentation/analysis audit (NOT the SPEC→IMPL→GAS→TST→TERMINAL contract shape, since nothing is built): **363 PREP** (pin `FROZEN_SHA` + rebuild/re-attest the context-pack + enumerate the raw inventory for all four web sections + scaffold the cohesive site + lock the resumable run scaffold) → **364 RNGSAFE** (the Player-Function RNG-Safety page + sanity/comment check) → **365 RNGVAR** (the RNG-Variable Freeze table) → **366 ETHFLOW** (the ETH-Flow chart) → **367 GASCEIL** (the advanceGame Gas-Ceiling table) → **368 FORENSIC** (the BURNIE ticket-mint regression forensic — the side track) → **369 TERMINAL** (targeted external corroboration + cohesive-site assembly/polish + consolidate `audit/FINDINGS-v58.0.md` + the closure flip with the `MILESTONE_V58_AT_HEAD_<sha>` signal). Phases 364-367 each ANALYZE + RENDER their own site section into the 363 scaffold; 369 assembles + polishes the whole. 368 FORENSIC is parallelizable / lower-priority (the "on the side" track).

### Phases

- [x] **Phase 363: PREP — Scope Freeze, Inventory & Run Scaffold** ✅ (2026-06-04 — FROZEN_SHA `2b26ec91` pinned; scope.txt rebuilt; 4 inventories enumerated [164 player fns · 44 RNG vars/47 entropy sites · 46 ETH edges · 13 advanceGame stages] seeding `audit/site/data/*.js`; cohesive static site scaffolded; run contract locked) - Pin `FROZEN_SHA` to the v57.0 contract-frozen HEAD (`2b26ec91`); rebuild + re-attest the `audit-v52/` context-pack against it (`scope.txt` — drop `AfKing.sol`, add `GameAfkingModule.sol` + `DegenerusGameBingoModule.sol`; `targets.md`; `hot-surfaces.md`; every cited `file:line` verified vs the frozen tree); enumerate the raw inventory for the four web deliverables (every player-facing function · every RNG-interacting variable · every ETH inflow/pool/outflow site · the ordered `advanceGame` stages) as the source-of-truth list each section builds from, gaps flagged explicitly; scaffold the cohesive dark-themed static site under `audit/site/` (vanilla HTML/CSS/JS, shared nav/tabs, searchable table component, no build step / no framework / no CDN); and lock the resumable run scaffold (the harness config + the checkpoint/persist convention + the autonomous + budget-pacing rules OPS-01/02). PREP-01/02/03 + OPS-01/02.
- [x] **Phase 364: ✅ RNGSAFE — Player-Function RNG-Safety Page + Sanity/Comment Check** (164 fns: 129 safe/31 gate/4 lock/0 finding; RNGSAFE-03 clean) - For every player-facing (external/public, user-callable, non-admin) function: a site row with contract + signature + plain-English "what it does" + access/gating, COMPLETE against the 363 enumeration (RNGSAFE-01); an RNG-safety verdict + reasoning per row (WHY it cannot influence any RNG-dependent variable between request and resolution — writes no participating slot / reverts under `rngLockedFlag` / reads only post-resolution state, cited to `file:line`; any window-touching writer escalated to a finding) (RNGSAFE-02); and a sanity + comment check flagging any function whose behavior diverges from its name/NatSpec/comment/intent (e.g. a BURNIE-burning buy that queues zero tickets) → recorded as a finding with severity + fix sketch (RNGSAFE-03). Renders into the site's RNG-Safety section. RNGSAFE-01/02/03.
- [x] **Phase 365: ✅ RNGVAR — RNG-Variable Freeze Invariant Table** (45 vars: 13 lock/15 gate/17 safe/0 finding; 6 edge-cases resolved) - Enumerate every state variable that participates in an RNG-derived output (entropy inputs to the `abi.encode(Packed)` sites, ticket-selection inputs, weights/buckets, day keys, `rngWordByDay`, the lock flags) with its declaring contract + slot/struct location + the RNG outputs it feeds (RNGVAR-01); prove the freeze per variable — the writer set enumerated, each shown to revert under the lock or be unreachable in the request→resolution window, cited to `file:line`; any window-mutating writer escalated to a finding (RNGVAR-02); render as the site's RNG-Variable Freeze section (searchable table: variable → outputs → writers → freeze-gate) (RNGVAR-03). RNGVAR-01/02/03.
- [x] **Phase 366: ✅ ETHFLOW — ETH Flow Chart** (46 edges: 21 safe/20 gate/5 warn/0 finding; solvency identity HOLDS; SVG diagram built) - Trace every ETH INFLOW (presale, ticket buys, cover-buys, redemptions-in, any `payable`/`receive` entry) with entry `file:line` + destination pool/ledger (ETHFLOW-01); trace the internal pool cycling + every OUTFLOW — the `claimablePool` / `_poolOf` / `Pool.*` buckets, the consolidation path, and every exit (claims, payouts, redemptions-out, withdrawals, the salvage ETH leg), with the conservation/solvency invariant noted at each hop (ETHFLOW-02); render a full ETH-flow DIAGRAM (nodes = sources/pools/sinks, edges = labeled transfers) in the site with a companion table backing each edge to a `file:line` (ETHFLOW-03). ETHFLOW-01/02/03.
- [x] **Phase 367: ✅ GASCEIL — advanceGame Gas-Ceiling Table** (13 stages; max ~15.08M @ 305-winner jackpot, ~10% headroom vs 16.7M; OPEN_BATCH+degenerette excluded; 2 LOW/info notes) - Decompose `advanceGame` into its ordered stages (subscriber STAGE, resolution, jackpot/decimator, pool consolidation, etc.), each with its bounding loop + the protocol cap on its iteration count (GASCEIL-01); derive the THEORETICAL WORST-CASE (max) gas per stage = measured per-item marginal × the proven iteration ceiling, bounding assumptions stated explicitly (worst-case branch, NOT typical seed) (GASCEIL-02); render a gas-ceiling table (stage → bound → marginal → worst-case total) with a per-stage verdict against the 16,777,216 (EIP-7825) per-tx cap; any stage that could approach/exceed the cap escalated to a finding (GASCEIL-03). GASCEIL-01/02/03.
- [x] **Phase 368: ✅ FORENSIC — BURNIE Ticket-Mint Regression Forensic (side track)** (root cause `24f0898b` phase-160 2026-04-01; live ~63d pre-launch; fixed `980865e8`; regression-test rec given) - Root-cause the regression — the commit SHA + era/refactor where BURNIE ticket buying (`purchaseCoin` → `_purchaseCoinFor`) stopped queuing tickets while still burning BURNIE (the BURNIE-01 coin-burned-zero-tickets sink, fixed in `980865e8`), via git history (candidate: the phase-160 `_queueTicketsScaled` relocation, or the v47 BURNIE-lootbox-removal rework) (FORENSIC-01); explain HOW it went unnoticed (no test asserted post-buy ticket-queue growth on the coin path) + recommend the regression test that would have caught it, delivered as a short root-cause note (FORENSIC-02). Parallelizable / lower-priority. FORENSIC-01/02.
- [x] **Phase 369: TERMINAL — External Corroboration + Site Assembly + FINDINGS-v58.0 + Closure** ✅ (2026-06-04 — XMODEL council adjudicated [7 confirmed / 5 refuted]; `audit/FINDINGS-v58.0.md` consolidated [chmod 444]; site Findings tab populated; closure flip + `MILESTONE_V58_AT_HEAD_2b26ec91810a733e15666a4c23e8f365a4f04f51` signal; 22 reqs re-attested) - Run the targeted external second opinion (~5 area prompts × 2 models [Gemini + Codex, $20-tier] on RNG-freeze invariant / solvency-conservation / AfKing-value-leak / v57 money paths / composition, via the `cross-model/` area-result pattern after a council PONG smoke-test; Claude adjudicates each external claim and folds corroboration/divergence in — XMODEL-01); assemble the four analysis sections into ONE cohesive navigable site under `audit/site/` (shared nav/tabs, dark game aesthetic, searchable tables, embedded ETH-flow diagram, opens from a local file with no server/build — SITE-01); consolidate every finding (RNGSAFE-03 / RNGVAR-02 / ETHFLOW-02 / GASCEIL-03 / FORENSIC / XMODEL) into `audit/FINDINGS-v58.0.md` (severity-classified, each fix sketch routed to a later milestone — document-only) and close the milestone via the atomic closure flip + the `MILESTONE_V58_AT_HEAD_<sha>` signal, re-attesting all 22 requirements against the frozen HEAD (AUDIT-01). XMODEL-01 · SITE-01 · AUDIT-01.

**Coverage:** 22/22 v58.0 requirements mapped to exactly one phase (363: 5 · 364: 3 · 365: 3 · 366: 3 · 367: 3 · 368: 2 · 369: 3); 0 orphaned, 0 duplicated. Per-category: PREP 3 + OPS 2 (PREP/scaffold) · RNGSAFE 3 (364) · RNGVAR 3 (365) · ETHFLOW 3 (366) · GASCEIL 3 (367) · FORENSIC 2 (368) · XMODEL 1 + SITE 1 + AUDIT 1 (369 TERMINAL). Document-only — NO contract-commit gate; the whole run is hands-off + resumable. Full per-requirement mapping in `.planning/REQUIREMENTS.md` Traceability.

---

### Phase Details (v58.0)

#### Phase 363: PREP — Scope Freeze, Inventory & Run Scaffold

**Goal**: The audit subject is frozen and the run is set up so phases 364-369 can execute hands-off and resumably with zero re-attestation drift: `FROZEN_SHA` is pinned, the context-pack is rebuilt + every `file:line` re-attested against it, the complete source-of-truth inventory for all four web sections is enumerated, the cohesive static-site shell exists under `audit/site/`, and the autonomous + budget-paced + checkpoint/resume run contract is locked.
**Requirements**: PREP-01, PREP-02, PREP-03, OPS-01, OPS-02
**Success criteria**:

1. `FROZEN_SHA` pinned to the v57 contract-frozen HEAD; `audit-v52/` `scope.txt`/`targets.md`/`hot-surfaces.md` rebuilt with `AfKing.sol`→`GameAfkingModule.sol` + `DegenerusGameBingoModule.sol` reconciled and 0 stale `file:line` refs.
2. The four enumerations (player-facing functions · RNG-interacting variables · ETH inflow/pool/outflow sites · `advanceGame` stages) exist as explicit lists, with any coverage gap logged (no silent truncation).
3. `audit/site/` opens from a local file with a working shared nav/tabs shell + a searchable-table component, no build step / framework / CDN.
4. The resumable run contract is written down: the harness config (`concurrency: 1`, small batches), the per-phase checkpoint/persist + commit convention, and the ~45%-weekly / 24h / no-5h-cap pacing rule.

#### Phase 364: RNGSAFE — Player-Function RNG-Safety Page + Sanity/Comment Check

**Goal**: A reader can see every player-facing function, understand what it does, and read a cited reason WHY it cannot influence any RNG-dependent variable between request and resolution — and every behavior↔name/comment mismatch (a real-bug class, e.g. a BURNIE-burning buy that queues zero tickets) is surfaced as a finding.
**Requirements**: RNGSAFE-01, RNGSAFE-02, RNGSAFE-03
**Success criteria**:

1. Every player-facing function from the 363 enumeration has a row (contract · signature · what-it-does · gating); 0 unlisted, deliberate exclusions noted.
2. Each row carries an RNG-safety verdict + cited reasoning; any function able to touch a participating slot in the freeze window is escalated to a finding.
3. The sanity/comment check is applied to every row; each behavior↔comment mismatch is recorded as a severity-classified finding with a fix sketch.
4. The RNG-Safety section renders in the site as a searchable/filterable table.

#### Phase 365: RNGVAR — RNG-Variable Freeze Invariant Table

**Goal**: A reader can see every variable that feeds RNG and a cited proof of why it cannot be modified between RNG request and resolution.
**Requirements**: RNGVAR-01, RNGVAR-02, RNGVAR-03
**Success criteria**:

1. Every RNG-participating variable is enumerated with its location + the RNG outputs it feeds.
2. Each variable's writer set is enumerated and each writer shown lock-gated or window-unreachable, cited to `file:line`; any window-mutating writer escalated to a finding.
3. The RNG-Variable Freeze table renders in the site (variable → outputs → writers → freeze-gate), searchable.

#### Phase 366: ETHFLOW — ETH Flow Chart

**Goal**: A reader can follow every ETH wei from where it enters, through the pools, to where it leaves — and the conservation/solvency invariant is visible at each hop.
**Requirements**: ETHFLOW-01, ETHFLOW-02, ETHFLOW-03
**Success criteria**:

1. Every ETH inflow is traced to its destination pool/ledger with an entry `file:line`.
2. Pool cycling + every outflow is traced with the conservation/solvency invariant noted per hop; any leak/mismatch escalated to a finding.
3. A readable ETH-flow diagram (sources/pools/sinks + labeled edges) renders in the site, each edge backed by a `file:line`.

#### Phase 367: GASCEIL — advanceGame Gas-Ceiling Table

**Goal**: A reader can see the theoretical worst-case gas of each `advanceGame` stage and whether it stays under the 16.7M per-tx cap.
**Requirements**: GASCEIL-01, GASCEIL-02, GASCEIL-03
**Success criteria**:

1. `advanceGame` is decomposed into ordered stages, each with its bounding loop + iteration cap.
2. Worst-case max gas per stage is derived (marginal × ceiling) with explicit bounding assumptions (worst-case branch, not typical seed).
3. The gas-ceiling table renders with a per-stage verdict vs the 16,777,216 cap; any stage that could approach/exceed it is escalated to a finding.

#### Phase 368: FORENSIC — BURNIE Ticket-Mint Regression Forensic (side track)

**Goal**: The "BURNIE burned but no tickets queued" regression is root-caused to a commit and explained, with a regression-test recommendation so it can't recur silently.
**Requirements**: FORENSIC-01, FORENSIC-02
**Success criteria**:

1. The introducing commit SHA + era/refactor is identified via git history.
2. A short root-cause note explains why no test caught it + recommends the specific regression test (post-buy ticket-queue-growth assertion on the coin path).

#### Phase 369: TERMINAL — External Corroboration + Site Assembly + FINDINGS-v58.0 + Closure

**Goal**: The artifacts are corroborated by a targeted external second opinion, assembled into one cohesive shippable site, and every finding is consolidated into `audit/FINDINGS-v58.0.md`; the milestone closes with a signal.
**Requirements**: XMODEL-01, SITE-01, AUDIT-01
**Success criteria**:

1. ~5 area prompts × 2 models run (after a council PONG smoke-test); each external claim adjudicated by Claude and folded into the artifacts/FINDINGS.
2. The four sections assemble into ONE cohesive navigable site under `audit/site/` (shared nav, dark aesthetic, searchable tables, embedded ETH-flow diagram, opens with no server/build).
3. `audit/FINDINGS-v58.0.md` consolidates all findings (severity-classified, fix sketches routed to a later milestone); the atomic closure flip lands with a `MILESTONE_V58_AT_HEAD_<sha>` signal and all 22 requirements re-attested.

---

## ✅ v57.0 Small-Feature Bundle + Day-Width `uint24` Normalization (SHIPPED 2026-06-04 — closure signal `MILESTONE_V57_AT_HEAD_2b26ec91810a733e15666a4c23e8f365a4f04f51`; subject frozen `2b26ec91`; baseline = v56.0 closure HEAD frozen `1e7a646d` / `MILESTONE_V56_AT_HEAD_1e7a646d44da4ee26375edd0b006274821fef73e`; 1 LOW found+RESOLVED at 362, 0 MEDIUM+; SOLVENCY-01 held; RNG re-based by design [pre-launch]; NOT pushed; phases 358-362)

> **⚡ RE-CHARTER (2026-06-04 session 2) — the heavy item changed; v57 SHIPPED.** Mid-359-IMPL the USER CANCELLED the `type Day` UDVT as overcomplicated; it shipped as a plain `uint24` day-width normalization that **INTENTIONALLY RE-BASES the RNG** (the `abi.encodePacked(…day…)` sites encode the uint24 day directly — pre-launch, the preserved image belonged to a never-deployed build; the deityBoonData site was deleted). So **UDVT-01/02/03 + the "RNG byte-image preserved / NON-WIDENING vs `1e7a646d`" success criteria are SUPERSEDED** by: *day-comparison BEHAVIOR preserved + RNG re-based by design + SOLVENCY-01 the surviving hard floor.* **+3 in-session fixes folded in** (not original REQ-IDs): the century-bonus per-player packed stamp (fixes a real cross-player staleness bug — global `centuryBonusLevel` dropped), the dailyHeroWagers 1e12→1e14 coarsening (off-path lottery weights), the deityBoonData honest-0-preview (dropped a fabricated placeholder seed). **359 shipped as 3 USER-approved commits on main (NOT pushed): `c3e84b79` feat + `aed10dab` test + `38389c07` fix.** Phase status: 358 ✅ · 359 ✅ (re-chartered) · 360 GAS = **Outcome-A** (gas-neutral by construction; the century fix reclaims a slot; the gas harness needs a layout refresh) · 361 TST ✅ (`test/REGRESSION-BASELINE-v57.md`: 529 pass/179 fail = 134 carried baseline + ≤44 NEW, **ALL harness layout/RNG artifacts**, ZERO solvency reds, hard floor intact) · 362 TERMINAL ✅ (delta-audit clean + 3-skill genuine-PARALLEL sweep: 1 LOW found+RESOLVED [TDEC sticky-`boosted`], 0 MEDIUM+ → `audit/FINDINGS-v57.0.md` chmod 444; atomic 5-doc closure flip + `MILESTONE_V57_AT_HEAD_2b26ec91` signal). Deferred pre-launch plumbing: the forge-harness layout/RNG refresh + the ~143 Hardhat JS recompile.

**Milestone:** v57.0 (started 2026-06-04)
**Defined:** 2026-06-04
**Audit baseline → subject:** v56.0 closure HEAD — frozen contract subject `1e7a646d`, closure signal `MILESTONE_V56_AT_HEAD_1e7a646d44da4ee26375edd0b006274821fef73e` → v57.0 closure HEAD. Subject = the single carefully-sequenced batched `contracts/*.sol` diff bundling the 7 small wins + the repo-wide `type Day is uint24` UDVT. Every cited `file:line` MUST be re-attested against the v56.0-closure HEAD `1e7a646d` before any patch.
**Scope source:** `.planning/REQUIREMENTS.md` (25 v57.0 REQ-IDs across 10 categories: BATCH 2 · BURNIE 3 · SALVAGE 3 · CANCEL 3 · WWXRP 2 · TDEC 3 · UDVT 3 · HYG 3 · SEC 2 · AUDIT 1) + the milestone init (2026-06-04) + the design-lock inputs `.planning/PLAN-WWXRP-JACKPOT-WHALEPASS.md` + `.planning/PLAN-TERMINAL-DECIMATOR-STREAK-BOOST.md` + the `type-day-udvt-post-v56-seed` + `handlepurchase-burnie-flip-batching-post-v56-seed` memories. **No research** — a fully-specced internal refactor/feature bundle (the original 5 items were PRE-VALIDATED READ-ONLY against the live `1e7a646d` tree on 2026-06-04; the 6th — the BURNIE coin-buy ticket-queue Critical fix — was found+VERIFIED against `1e7a646d` mid-358, verdicts + corrections folded into REQUIREMENTS).

> **Scope (USER-locked 2026-06-04):** bundle ALL of — (1) `handlePurchase` BURNIE-flip batching (manual path), (2) the WWXRP jackpot whale-halfpass (one per 10-level bracket), (3) the terminal-decimator final-day streak boost (with bucket promotion), (4) test/comment hygiene, (5) the repo-wide `type Day is uint24` UDVT, (6) **the BURNIE coin-buy ticket-queue Critical fix** (ADDED 2026-06-04 mid-358 — `_purchaseCoinFor` burns BURNIE but queues zero tickets; restore queueing + MINT_BURNIE burn-rebate; BURNIE-01/02/03), (7) **the sDGNRS salvage-swap combo ETH/BURNIE pawn-shop payout** (ADDED 2026-06-04 mid-358 — variable knowable-in-advance offer, cap-bounded; SALVAGE-01/02/03), (8) **manual sub-cancel auto-claim (self + affiliate tree) + auto-evict pure-forfeit** (ADDED 2026-06-04 mid-358 — fixes a latent loss bug; CANCEL-01/02/03) — into ONE milestone, ONE carefully-sequenced batched contract diff. The UDVT is the HEAVY item and dominates IMPL/TST effort; the BURNIE fix is the highest-severity item.

> **Posture — hard floor on EVERY item.** Each of the 5 changes is BURNIE-credit / weight-only / pure-annotation, OFF the ETH/`claimablePool` path → the hard floor every item = **RNG-freeze intact + SOLVENCY-01 byte-untouched**. The UDVT is the load-bearing item for the freeze floor: the `rngWordByDay` mapping KEY is byte-preserved (uint24/uint32 zero-pad to the same slot), BUT the `abi.encodePacked(…day…)` RNG-entropy sites (`DegenerusGameAdvanceModule.sol:1828` + `:1405`, `DegenerusGame.sol:1011`) MUST be explicitly cast to `uint32` to preserve the exact keccak preimage byte-image (an unwrapped uint24-backed `Day` would shorten the preimage and change the derived word — the seed's "pure annotation" premise is FALSE at these sites); the packed `Sub`/struct day fields MUST stay uint24-backed (else they spill a cold slot and kill the v56 gas win); standalone uint32 slots + indexed uint32 event topics widen explicitly. Enforced by a per-site RNG-freeze byte-diff gate + a NON-WIDENING regression.

> **Cross-cutting rule (every requirement):** every cited `file:line` MUST be re-attested against the **v56.0 closure HEAD `1e7a646d`** before any patch (no "by construction" / "single fn reaches all paths" claim survives un-checked; `feedback_verify_call_graph_against_source`). The cited anchors (`DegenerusQuests.sol:947-949` inline `creditFlip` + `DegenerusGameMintModule.sol:1220`/`:1355` fold; the `DegeneretteModule._resolveFullTicketBet` ~:713 s≥7 sDGNRS-award hook + `whalePassClaims`/`claimWhalePass`; the `DegenerusGameDecimatorModule` `recordTerminalDecBurn`/`runTerminalDecimatorJackpot`/`_terminalDecMultiplierBps` + the `keccak256(abi.encode(lvl,bucket,subBucket))` key + the packed `TerminalDecEntry` + `getPlayerQuestView`; the 3 `abi.encodePacked(…day…)` sites + the day-bearing surface ~649 lines/27 contracts; the stale `gameSetAutoRebuy`/`_runRewardJackpots` references) are confirmed at SPEC. **Security / freeze / solvency floor over correctness-cosmetics** (`feedback_security_over_gas`): SEC-01 (RNG-freeze intact across all 8 items, incl. the UDVT per-site byte-diff gate) + SEC-02 (SOLVENCY-01 byte-untouched) are the hard gate. Pre-launch redeploy-fresh (storage-layout break fine, no migration; `feedback_frozen_contracts_no_future_proofing`).

> **Posture:** the contract changes ship as ONE carefully-sequenced batched USER-APPROVED `contracts/*.sol` diff with a **HARD STOP at the contract-commit boundary** (applied + locally compiled/tested, NEVER committed without explicit user hand-review — `feedback_batch_contract_approval` + `feedback_never_preapprove_contracts` + `feedback_manual_review_before_push` + `feedback_no_contract_commits`; `ContractAddresses.sol` freely modifiable per `feedback_contractaddresses_policy`). The IMPL (359) is the ONE contract gate — the 7 small wins + the wide UDVT refactor land as a single producer-before-consumer diff. Sequential-on-main (worktrees unsafe — submodule + node_modules; `use_worktrees: false`, `no_worktree_paths: [contracts]`). Tests + planning + docs AGENT-committable.

> **Audit posture — FULL CLOSE WITH ITS OWN INTERNAL SWEEP (NOT deferred).** Like v54.0 + v55.0 + v56.0 (and unlike v50.0 + v51.0, which deferred their internal sweep → the v52 consolidated audit), **v57.0 runs its own internal 3-skill genuine-PARALLEL adversarial sweep + delta-audit + `audit/FINDINGS-v57.0.md` at TERMINAL (362, AUDIT-01)** — the 3-skill pass (`/contract-auditor` + `/economic-analyst` + `/zero-day-hunter`; `/degen-skeptic` = the dual-gate filter per `D-271-ADVERSARIAL-02`) + the delta-audit + `audit/FINDINGS-v57.0.md` (chmod 444) + the atomic 5-doc closure flip all execute in-milestone, focused on the **UDVT byte-image preservation + the WWXRP/terminal-decimator weight-only freeze-safety + the bucket-promotion freeze re-proof**. Rationale: the UDVT touches the RNG-entropy `abi.encodePacked` sites (the freeze spine) and the terminal-decimator bucket-promotion is a NEW weight-determining write before the draw — both must be adversarially probed in-milestone, not deferred. (The separate v52 consolidated cross-model audit still folds the v57 surface into its cumulative sweep — an additional track, not a substitute for v57's own close.)

> **Phase numbering** continues from the previous milestone — v56.0 ran 353–357, so **v57.0 starts at Phase 358.** Not reset to 1. (Prior milestones' phase dirs are archived under `.planning/milestones/vXX.0-phases/`.)

> **Milestone shape** is the established v54.0 + v55.0 + v56.0 audit pattern (with a dedicated GAS phase): **358 SPEC design-lock (the WWXRP per-bracket rationing key + operator-recipient policy [WWXRP-02 design half] + the terminal-decimator design decisions [TDEC-02 SPEC half: effective-streak source / bucket-improvement + promotion rule + keep-vs-re-derive subBucket / uint88 overflow policy / double-count-vs-burn-time multBps / shields consume-vs-read; TDEC-03 freeze-safety re-proof under the bucket-promotion allowance] + the UDVT width/byte-preservation discipline [which sites cast to uint32, which stay uint24] + the BURNIE coin-buy ticket-queue fix design-lock [BURNIE-03] + the salvage combo ETH/BURNIE pawn-shop design-lock + no-arb re-proof [SALVAGE-02] + the manual-cancel auto-claim / auto-evict-forfeit design-lock [CANCEL-02] + the freeze/solvency re-attestation on paper + grep-attest every cited `file:line` vs `1e7a646d`; ZERO `contracts/*.sol`) → 359 IMPL the ONE carefully-sequenced batched contract diff (BATCH-01/02 + WWXRP-01 + TDEC-01 + UDVT-01/02/03 + BURNIE-01/02 + SALVAGE-01 + CANCEL-01 — the wide UDVT refactor dominates; producer-before-consumer; HELD for USER hand-review) → 360 GAS (measure + tune — the UDVT must be GAS-NEUTRAL [packed Sub day fields stay uint24, no cold-slot spill] + the small features measured; rides the same contract boundary if a net diff, else records Outcome-A no-diff per the v55 350 / v56 precedent — owns no REQ-ID, it is the gas-neutrality measurement gate) → 361 TST (the proofs — SEC-01 RNG-freeze intact across all 8 items [incl. the UDVT per-site byte-diff gate] + SEC-02 SOLVENCY-01 byte-untouched + NON-WIDENING regression + HYG-01/HYG-02/HYG-03 test/comment hygiene + SALVAGE-03 combo-payout/no-arb-cap proof + CANCEL-03 cancel-auto-claim/evict-forfeit proof) → 362 TERMINAL FULL close (delta-audit + 3-skill genuine-PARALLEL adversarial sweep + `audit/FINDINGS-v57.0.md` + atomic 5-doc closure flip).** The contract-boundary HARD STOP lives at ONE phase (359 IMPL).

### Phases

- [x] **Phase 358: SPEC — Design-Lock (WWXRP per-bracket rationing + operator-recipient policy · terminal-decimator design decisions + bucket-promotion freeze re-proof · UDVT width/byte-preservation discipline) + Freeze/Solvency Re-Attestation + Call-Graph Attestation** - Design-lock the open decisions for the WWXRP whale-halfpass (the per-10-level-bracket rationing key `level/10` + the operator-placed-bet recipient policy [pass to `player` = bet owner vs operator; whether one player may win multiple brackets — default allow, naturally rationed] — WWXRP-02 design half) and the terminal-decimator final-day streak boost (the effective-streak source via `getPlayerQuestView`, the bucket "improvement"/promotion rule + the keep-or-re-derive-`subBucket` decision on a promotion, the `uint88 weightedBurn` overflow policy [base × time-mult ≤20× × boost ≤20× ≈ 400× base — saturate or prove headroom], the double-count policy vs the burn-time `multBps` streak lever [keep both levers vs strip streak from burn-time], the shields consume-vs-read decision — TDEC-02 SPEC half; and the freeze-safety RE-PROOF under the bucket-promotion allowance [since TDEC-01 now allows promotion, the validator's original `subBucket`-fixed simplification no longer holds — re-prove "all weight/bucket mutation precedes the draw" — TDEC-03 SPEC half) and the UDVT width/byte-preservation discipline (the per-site decision matrix — which sites cast to `uint32` at the `abi.encodePacked` RNG-entropy boundary [`DegenerusGameAdvanceModule.sol:1828` + `:1405`, `DegenerusGame.sol:1011`], which packed `Sub`/struct day fields stay uint24-backed [no cold-slot spill — the v56 gas win], which standalone slots + indexed event topics widen to uint32 explicitly); re-attest the RNG-freeze-intact + SOLVENCY-01-byte-untouched properties on paper as the design gate (5 of the 6 items are BURNIE-credit / weight-only / pure-annotation OFF the ETH/`claimablePool` path; the BURNIE coin-buy fix keeps the ETH/pool DEBIT byte-unchanged but restores ticket claims — the posture widening is flagged); and grep-attest every cited `file:line` against the v56.0 HEAD `1e7a646d` — paper-only, ZERO `contracts/*.sol`. (completed 2026-06-04)
- [x] **Phase 359: IMPL — The ONE Carefully-Sequenced Batched Contract Diff (handlePurchase batching + WWXRP whale-halfpass + terminal-decimator boost + the wide UDVT refactor)** - Land the single reconciled `contracts/*.sol` diff producer-before-consumer: the `handlePurchase` BURNIE-flip batching (BATCH-01 — drop the inline `coinflip.creditFlip(player, burnieMintReward)` at `DegenerusQuests.sol:947-949`, return `burnieMintReward` in `totalReturned`; the sole caller `DegenerusGameMintModule.sol:1220` already folds the full return into `lootboxFlipCredit` [single credit `:1355`] = behavior-equivalent, saving one cross-contract `creditFlip` per MINT_BURNIE-quest-completing buy; afking path unaffected — never calls `handlePurchase`; BATCH-02 — correct the misleading `ethMintReward`/"ETH mint reward" naming to reflect quest-TYPE semantics [MINT_ETH/LOOTBOX/MINT_BURNIE are quest TYPE constants; all rewards pay BURNIE flip stake]); the WWXRP jackpot whale-halfpass (WWXRP-01 — at `DegeneretteModule._resolveFullTicketBet` ~:713, after the s≥7 sDGNRS award: on `s == 9` + `currency == CURRENCY_WWXRP` (=3) + `amountPerTicket >= MIN_BET_WWXRP`, award `whalePassClaims[player] += 1` rationed to one award per 10-level bracket via a per-bracket award flag keyed by `level/10` [NOT a global 0→5 lifetime cap], the new `wwxrpJackpotWhalePass*` per-bracket state appended to `DegenerusGameStorage`, the recipient + multi-bracket policy from SPEC applied, short-circuiting to zero added cost on non-jackpot spins); the terminal-decimator final-day streak boost (TDEC-01 — a new `boostTerminalDecimator()` that validates the LIVE EFFECTIVE quest streak via `getPlayerQuestView` [gap-reset + shields applied — NOT raw `playerQuestStates.streak`] → scales `weightedBurn` ×factor [streak 100→20×, 10→4×] folded into `terminalDecBucketBurnTotal[key]` via the existing `keccak256(abi.encode(lvl,bucket,subBucket))` key, with a `boosted` bit added to the packed `TerminalDecEntry` for idempotence, AND PROMOTES the player's bucket if the boosted weight qualifies for an improved bucket [per the SPEC improvement + keep-vs-re-derive-subBucket rule], overflow-handled per the SPEC `uint88` policy, gated by `require(!gameOver)`, landing same-tx before `gameOver` flips and before subbuckets are drawn); and the repo-wide `type Day is uint24` UDVT (UDVT-01 — the `type Day is uint24` value type + global operator overloads [`<`, `<=`, `==`, `%`, `+`, `-`] replacing the raw day-counter integers across the ~649-line/27-contract day-bearing surface [heaviest DegenerusQuests/GameAfkingModule/DegenerusGameStorage/AdvanceModule/BoonModule]; UDVT-02 — the RNG byte-image preserved bit-for-bit: every `abi.encodePacked(…day…)` RNG-entropy site explicitly casts the day to `uint32` [`DegenerusGameAdvanceModule.sol:1828`/`:1405`, `DegenerusGame.sol:1011`], the `rngWordByDay` mapping KEY layout unchanged; UDVT-03 — storage layout + gas preserved: packed `Sub`/struct day fields stay uint24-backed [no cold-slot spill], standalone day slots + indexed day event topics widen to `uint32` explicitly) — authored producer-before-consumer, applied to `contracts/` and locally compiling (`forge build` clean), then HELD at the contract-commit boundary for explicit user hand-review.
- [x] **Phase 360: GAS — Measure + Tune (the UDVT gas-neutrality gate: packed Sub day fields stay uint24 / no cold-slot spill + the small features measured)** - Measure + tune the cost impact of the bundle under the security-over-gas floor (gas-scavenger surfaces, gas-skeptic validates — any simplification must stay freeze-safe + solvency-neutral): the UDVT is proven GAS-NEUTRAL — the packed `Sub`/struct day fields stay uint24-backed with NO cold-slot spill (the v56 gas win intact; UDVT-03's gas half measured here), and the explicit uint32 widening of standalone slots + indexed event topics is the only intentional storage delta; the `handlePurchase` batching saves one cross-contract `creditFlip` per qualifying buy (BATCH-01's ~5–25k win measured); the WWXRP grant + the terminal-decimator boost short-circuit to ~zero added cost on the non-triggering path. Any net contract change beyond the IMPL diff rides the same batched USER-APPROVED diff held at the contract-commit boundary; if the bundle is gas-neutral-by-construction (the expected outcome — the UDVT is a pure-annotation/explicit-cast pass with no storage-layout change to the packed fields), that is recorded as the Outcome-A no-diff outcome per the v55 350 / v56 precedent. (Owns no REQ-ID — the gas-neutrality measurement gate; UDVT-03 is OWNED at IMPL, its gas-half measured here.)
- [x] **Phase 361: TST — RNG-Freeze Intact (incl. the UDVT per-site byte-diff gate) + SOLVENCY-01 Byte-Untouched + Non-Widening + Test/Comment Hygiene** - Prove the milestone behaviorally correct + the hard security floor empirically against the v56-baseline tree (the SEC gate): RNG-freeze is intact across all 8 items (SEC-01 — the UDVT `abi.encodePacked` sites preserve the EXACT byte-image [the per-site RNG-freeze byte-diff gate — UDVT-02 enforced empirically: the derived `rngWord` is byte-identical at every entropy site after the uint32 casts], the WWXRP grant + the terminal-decimator boost touch only RNG-insensitive counters/weights gated by already-committed outcomes [`s==9` deterministic from the committed word; the boost + bucket-promotion deterministic from the fixed effective-streak factor × burn, committed before the draw], and `handlePurchase` batching is BURNIE-accounting only — proven empirically [byte-diff + determinism]); SOLVENCY-01 is untouched (SEC-02 — every change is BURNIE flip-credit / weight-only / pure-annotation OFF the ETH/`claimablePool` path, the ETH/pool debit byte-unchanged — proven empirically [solvency-invariant]); the suite is NON-WIDENING vs the v56 baseline `1e7a646d` (UDVT-03's regression half — every pre-existing red enumerated BY NAME, `REGRESSION-BASELINE-v57.md`); AND the test/comment hygiene lands (HYG-01 — fix the stale `gameSetAutoRebuy` → `coinSetAutoRebuy(true, 0)` references [`test/unit/GovernanceGating.test.js:247`, `test/unit/DegenerusVault.test.js:385/456`, `test/fuzz/CoverageGap222.t.sol:1055/1060/1084/1085`], restoring the intended `onlyVaultOwner`-rejects-non-majority assertions; HYG-02 — correct the two stale `_runRewardJackpots`/`EndgameModule` comments → `_consolidatePoolsAndRewardJackpots` [`DegenerusGameAdvanceModule.sol:1191`, `DegenerusGameDegeneretteModule.sol:809`], comment-only).
- [x] **Phase 362: TERMINAL — Delta Audit + 3-Skill Genuine-PARALLEL Adversarial Sweep + FINDINGS-v57.0 + Closure Flip** - Close the v57.0 audit subject (the carefully-sequenced batched diff — the 7 small wins + the wide UDVT refactor, FROZEN at the IMPL+GAS HEAD) via a FULL close that runs its own internal sweep IN-MILESTONE (NOT deferred to v52): a delta-audit (every changed surface NON-WIDENING vs the v56 baseline `1e7a646d` with zero orphan hunks; the RNG-freeze byte-diff [the per-site `abi.encodePacked` uint32-cast byte-image] + the SOLVENCY-01 byte-anchor re-attested; the WWXRP per-bracket grant + the terminal-decimator weight-only boost + bucket-promotion freeze-safety + the `handlePurchase` batching equivalence re-attested) + the mandatory 3-skill genuine-PARALLEL adversarial economic review (`/contract-auditor` + `/economic-analyst` + `/zero-day-hunter`; `/degen-skeptic` = the dual-gate filter per `D-271-ADVERSARIAL-02`) focused on the UDVT byte-image preservation + the terminal-decimator bucket-promotion freeze-safety (the NEW weight-determining write before the draw) + the WWXRP rationing-key correctness + the operator-recipient policy + `audit/FINDINGS-v57.0.md` (chmod 444) + the atomic 5-doc closure flip with the `MILESTONE_V57_AT_HEAD_<sha>` signal — the internal sweep runs IN-MILESTONE (NOT deferred to v52); re-attests all 25 v57.0 requirements against the frozen closure HEAD.

---

## Phase Details (v57.0)

### Phase 358: SPEC — Design-Lock + Freeze/Solvency Re-Attestation + Call-Graph Attestation

**Goal**: The v57.0 open design decisions are settled in writing so the IMPL phase authors a fully reconciled diff with zero "by construction" assumptions, and the load-bearing freeze-safety + byte-preservation are designed BEFORE any code: the WWXRP per-bracket rationing key + operator-recipient policy (WWXRP-02 design half), the terminal-decimator design decisions (TDEC-02 SPEC half: effective-streak source, the bucket-improvement/promotion rule + keep-vs-re-derive-`subBucket`, the `uint88` overflow policy, double-count-vs-burn-time-`multBps`, shields consume-vs-read) + the freeze-safety re-proof under the bucket-promotion allowance (TDEC-03 SPEC half), and the UDVT width/byte-preservation discipline (the per-site cast/keep/widen decision matrix) are LOCKED; the BURNIE coin-buy ticket-queue Critical fix is design-locked (BURNIE-03 — the `_purchaseCoinFor` queue-on-return algorithm + the MINT_BURNIE burn-rebate mechanic [full-cost gate, deferred net burn] + the BATCH-01 reconciliation + the freeze/solvency framing); the RNG-freeze-intact + SOLVENCY-01-byte-untouched properties are re-attested on paper; and every cited `file:line` is grep-verified against the v56.0 HEAD `1e7a646d` — paper-only, zero `contracts/*.sol`.
**Type**: SPEC
**Depends on**: Nothing (first v57.0 phase; consumes the v56.0 closure HEAD `1e7a646d` as the frozen audit baseline, plus the design-lock inputs `.planning/PLAN-WWXRP-JACKPOT-WHALEPASS.md` + `.planning/PLAN-TERMINAL-DECIMATOR-STREAK-BOOST.md` + the `type-day-udvt-post-v56-seed` + `handlepurchase-burnie-flip-batching-post-v56-seed` memories; the BURNIE fix was found+verified mid-358 against `1e7a646d`)
**Requirements**: WWXRP-02, TDEC-02, TDEC-03, BURNIE-03, SALVAGE-02, CANCEL-02
**Success Criteria** (what must be TRUE):

  1. The WWXRP whale-halfpass design is locked (WWXRP-02 design half) — the per-10-level-bracket rationing key is settled (`level/10`, a per-bracket award flag, NOT a global 0→5 lifetime cap), the operator-placed-bet recipient policy is decided (pass to `player` = bet owner vs operator) and whether one player may win multiple brackets across the game is settled (default allow — naturally rationed), the new `wwxrpJackpotWhalePass*` per-bracket storage shape is fixed, and the freeze-safe/cheap properties (writes only an RNG-insensitive counter gated by the already-committed `s==9`, reuses `claimWhalePass`, short-circuits on non-jackpot spins) are re-attested on paper — recorded for the IMPL (WWXRP-01 is owned at IMPL; its design is fixed here).
  2. The terminal-decimator design decisions are locked (TDEC-02 SPEC half) — the effective-streak source is `getPlayerQuestView` (gap-reset + shields applied, NOT raw `playerQuestStates.streak`), the bucket "improvement"/promotion rule is defined (what makes a bucket better) along with the keep-or-re-derive-`subBucket` decision on a promotion, the `uint88 weightedBurn` overflow policy is decided (base × time-mult ≤20× × boost ≤20× ≈ 400× base — saturate or prove headroom), the double-count policy vs the burn-time `multBps` streak lever is resolved (keep both levers vs strip streak from burn-time), and the shields consume-vs-read decision is made — recorded for the IMPL (TDEC-01 is owned at IMPL; its design is fixed here).
  3. The terminal-decimator freeze-safety is RE-PROVEN under the bucket-promotion allowance (TDEC-03 SPEC half) — since TDEC-01 now allows a bucket PROMOTION (not only a within-bucket share increase), the validator's original `subBucket`-fixed simplification no longer holds, so SPEC re-proves freeze-safety on the basis "all weight + bucket mutation precedes the draw" (the boost + any promotion are DETERMINISTIC from the player's fixed effective-streak factor × their burn, committed in the same tx BEFORE `gameOver` flips and BEFORE subbuckets are drawn from `rngWord` → the player cannot use draw knowledge to manipulate placement), the `require(!gameOver)` gate + the pool-finalized-in-resolution + the shares-sum-to-pool invariants are re-attested — recorded for the IMPL (TDEC-01 builds it; the freeze-safety proof is the SPEC's design gate).
  4. The UDVT width/byte-preservation discipline is locked (UDVT-01/02/03 design feed) — the per-site decision matrix is fixed: which `abi.encodePacked(…day…)` RNG-entropy sites cast to `uint32` (`DegenerusGameAdvanceModule.sol:1828`/`:1405`, `DegenerusGame.sol:1011` — to preserve the exact keccak preimage byte-image), which packed `Sub`/struct day fields stay uint24-backed (no cold-slot spill — the v56 gas win), which standalone day slots + indexed day event topics widen to `uint32` explicitly, and the `rngWordByDay` mapping KEY is confirmed layout-unchanged (uint24/uint32 zero-pad to the same slot); the operator-overload set (`<`, `<=`, `==`, `%`, `+`, `-`) + solc 0.8.34 UDVT support is confirmed — recorded for the IMPL (UDVT-01/02/03 are owned at IMPL; their byte-preservation discipline is fixed here).
  5. The freeze/solvency re-attestation lands + every cited `file:line` is grep-attested vs `1e7a646d` (SEC design feed) — the RNG-freeze-intact (across all 8 items, the UDVT byte-image being load-bearing) + SOLVENCY-byte properties are re-attested on paper as the SPEC's design gate (5 of 6 items are BURNIE-credit / weight-only / pure-annotation OFF the ETH/`claimablePool` path; the BURNIE fix keeps the ETH/pool DEBIT byte-unchanged but RESTORES ticket claims on the pools — the posture widening is flagged) (SEC-01/02 are owned at TST; their design is fixed here); and every cited anchor across the milestone scope (the `handlePurchase`/`MintModule` fold sites, the `_resolveFullTicketBet` hook + `whalePassClaims`/`claimWhalePass`, the `DegenerusGameDecimatorModule` machinery + the `TerminalDecEntry` packing + `getPlayerQuestView`, the 3 `abi.encodePacked` sites + the day-bearing surface, the BURNIE-path `_purchaseCoinFor`/`_callTicketPurchase`/`_queueTicketsScaled`/`gamePurchaseTicketsBurnie` anchors, the stale `gameSetAutoRebuy`/`_runRewardJackpots`/3-arg-`purchaseCoin` references) is grep-verified against `1e7a646d` with any drift corrected in the SPEC (no "by construction" survives un-checked).
  6. The BURNIE coin-buy ticket-queue Critical fix is design-locked (BURNIE-03 — the SPEC design half) — the verified bug (`_purchaseCoinFor` `DegenerusGameMintModule.sol:887-907` burns the coin via `_callTicketPurchase(payInCoin=true)` but DISCARDS the returns → ZERO tickets queued; phase-160 `24f0898b` orphaned the coin path) is documented with all 4 decisive anchors, and the fix is locked: (a) BURNIE-01 — capture the returns + `_queueTicketsScaled(buyer, targetLevel, adjustedQty, false)`; (b) BURNIE-02 — the MINT_BURNIE-leg-only `handlePurchase` routing (skip score/affiliate/non-mint-quests) + the burn-REBATE mechanic (require the FULL ticket cost upfront, defer the burn, burn net = full − MINT_BURNIE reward floored at 0) co-designed with BATCH-01's return-instead-of-credit; (c) the freeze framing (`purchaseCoin` reads no `rngWord`) + the SOLVENCY posture-widening (ETH debit byte-unchanged, ticket claims restored, no unbacked obligation) — recorded for the IMPL (BURNIE-01/02 owned at 359; their design is fixed here).
  7. The sDGNRS salvage-swap combo ETH/BURNIE pawn-shop payout is design-locked (SALVAGE-02 — the SPEC design half) — the verified current structure (`sellFarFutureTickets` `DegenerusGame.sol:2074`→`MintModule.sol:929`; quote `MintStreakUtils.sol:145-190` with the prior-day `seed`, `jitterMult` 70–110%, `ticketShareBps` 40–80% → cash ≤60%; ETH relabel `MintModule:975-977`; `_ethToBurnieValue` `:1657`; `creditFlip`; `rngLockedFlag`-gated) is documented, and the change is locked: split the cash leg `cashWei` into `ethWei` (full-range `[0..ethCap]` from a NEW slice of the prior-day seed; `ethCap` = the existing eth-% cap) + a BURNIE part paid by TRANSFERRING sDGNRS-OWNED BURNIE (token balance + claimable coinflip stake), valued via `_ethToBurnieValue` @ current eth-equivalent — **NOT a `creditFlip` mint** (USER): `actualBurnie = min(target, sDGNRS-available)`, the remainder + the zero-available case paid as ETH ("do it more like now"); ticket leg unchanged. **The "pawn-shop" safety model is the SPEC's load-bearing proof:** offers are VARIABLE + KNOWABLE-IN-ADVANCE (no hidden VRF — reuses the settled prior-day word), and the SOLE non-exploitability property is the **total payout cap (the existing no-arb ceiling) + the eth-% cap** — every reachable offer ≤ the cap → no extraction above it regardless of optimization (suboptimal players forfeit EV to the protocol/pools, intended); SOLVENCY-positive (only the ETH part leaves SDGNRS; the BURNIE is sDGNRS-owned, TRANSFERRED — NO new emission; source-availability check prevents over-draw). The no-arb re-proof obligation (extend `SWAP08_NoArbAtCeiling` to the new variability + the fallback) is owned here, verified at SALVAGE-03 (TST). Grep-attest anchors vs `1e7a646d`. Recorded for the IMPL (SALVAGE-01 owned at 359).
  8. The manual-sub-cancel auto-claim + auto-evict pure-forfeit is design-locked (CANCEL-02 — the SPEC design half) — the verified latent loss bug is documented (manual cancel `GameAfkingModule:345-362` tombstones + leaves `pendingBurnie`/`affiliateBase` in the slot, but the next `processSubscriberStage` reclaim `delete _subOf[player]` `:1144` wipes both → loss in the race), and the fix is locked: manual cancel auto-claims BEFORE clearing — pay the canceller's `pendingBurnie` via `creditFlip` (+ presale-box parity, mirror `claimAfkingBurnie:1560`) + drain `affiliateBase` and settle the tree A/U1/U2 75/20/5 (mirror `DegenerusAffiliate.claim:629`/`drainAffiliateBase:1605`), then `_finalizeAfking`+clear; AUTO-EVICT (pass `:1175` / funding `:1226` / reclaim `:1144`) stays a pure FORFEIT (delete both accumulators, no payout — USER). Freeze/solvency re-attested: BURNIE-emission only (`creditFlip`), no `rngWord` read, no ETH/`claimablePool` touch, `rngLock`-gated at the `subscribe` entry, CEI (zero before credit). Grep-attest anchors vs `1e7a646d`. Recorded for the IMPL (CANCEL-01 owned at 359).

**Plans**: 3 plans

Plans:

- [x] 358-01-PLAN.md — SPEC header + frozen-subject guard + terminal-decimator mechanics design-lock (TDEC-02 / D-04..D-13) + the load-bearing TDEC-03 freeze-safety proof (future-day-word lemma, D-01/D-02/D-03)
- [x] 358-02-PLAN.md — the four small-feature design-locks: WWXRP-02 (D-14..D-18) + BURNIE-03 (D-21..D-24) + SALVAGE-02 (D-25..D-29) + CANCEL-02 (D-30..D-33)
- [x] 358-03-PLAN.md — cross-cutting RNG-freeze + SOLVENCY re-attestation + UDVT byte-preservation discipline (D-19/D-20) + the full call-graph grep-attestation vs 1e7a646d (3 drifts reconciled) + the SPEC Lock asserting all 8 Success Criteria

**UI hint**: no

### Phase 359: IMPL — The ONE Carefully-Sequenced Batched Contract Diff (handlePurchase batching + WWXRP whale-halfpass + terminal-decimator boost + the wide UDVT refactor)

**Goal**: The v57.0 bundle lands as a single reconciled `contracts/*.sol` diff under the SPEC's settled shapes, authored producer-before-consumer (the small-feature edits → the wide UDVT refactor → interfaces/wiring): the `handlePurchase` BURNIE-flip batching (BATCH-01 return-instead-of-credit + BATCH-02 naming correction), the WWXRP jackpot whale-halfpass (WWXRP-01 per-bracket grant at `_resolveFullTicketBet`), the terminal-decimator final-day streak boost (TDEC-01 `boostTerminalDecimator()` with bucket promotion), the BURNIE coin-buy ticket-queue Critical fix (BURNIE-01 `_purchaseCoinFor` queue-on-return + BURNIE-02 MINT_BURNIE burn-rebate, co-designed with BATCH-01), and the repo-wide `type Day is uint24` UDVT (UDVT-01 the type + overloads, UDVT-02 the `abi.encodePacked` uint32 casts, UDVT-03 the storage-layout/gas preservation) — the wide UDVT refactor DOMINATES this phase. Applied to `contracts/` and locally compiling (`forge build` clean), then HELD at the contract-commit boundary for explicit user hand-review.
**Type**: IMPL (CONTRACT BOUNDARY — the ONE carefully-sequenced batched USER-APPROVED `contracts/*.sol` diff; `autonomous: false` at the commit gate; never auto-commit contracts)
**Depends on**: Phase 358 (the SPEC must lock the WWXRP rationing key + recipient policy, the terminal-decimator design decisions + the bucket-promotion freeze re-proof, the UDVT per-site cast/keep/widen discipline, the BURNIE queue-on-return + burn-rebate design, and grep-attest the edit-order first)
**Requirements**: BATCH-01, BATCH-02, WWXRP-01, TDEC-01, UDVT-01, UDVT-02, UDVT-03, BURNIE-01, BURNIE-02, SALVAGE-01, CANCEL-01
**Success Criteria** (what must be TRUE):

  1. The `handlePurchase` BURNIE-flip batching lands behavior-equivalent (BATCH-01 / BATCH-02) — `DegenerusQuests.handlePurchase` returns `burnieMintReward` in `totalReturned` instead of crediting it inline (the `coinflip.creditFlip(player, burnieMintReward)` at `:947-949` dropped); the sole caller `DegenerusGameMintModule.sol:1220` already folds the full return into `lootboxFlipCredit` (single credit `:1355`) → same recipient + same amount + additive accumulator = behavior-equivalent, saving one cross-contract `creditFlip` per MINT_BURNIE-quest-completing buy (the afking path is unaffected — it never calls `handlePurchase`) (BATCH-01); and the misleading `ethMintReward`/"ETH mint reward" naming + comments are corrected to reflect quest-TYPE semantics — MINT_ETH/LOOTBOX/MINT_BURNIE are quest TYPE constants and ALL quest rewards pay BURNIE flip stake; no payout is ETH (BATCH-02).
  2. The WWXRP jackpot whale-halfpass lands rationed per bracket (WWXRP-01) — a player who hits the Degenerette jackpot (`s == 9`) on a `currency == CURRENCY_WWXRP` (=3) bet with `amountPerTicket >= MIN_BET_WWXRP` is awarded a Whale halfpass via `whalePassClaims[player] += 1`, RATIONED to one award per 10-level bracket (a per-bracket award flag keyed by `level/10` — NOT a global 0→5 lifetime cap), hooked at `DegeneretteModule._resolveFullTicketBet` (~:713, after the s≥7 sDGNRS award), with the new `wwxrpJackpotWhalePass*` per-bracket state in `DegenerusGameStorage`, the recipient + multi-bracket policy from SPEC applied, reusing the `claimWhalePass` future-ticket deferral (no ETH/`claimablePool` touch), and short-circuiting to zero added cost on non-jackpot spins.
  3. The terminal-decimator final-day streak boost lands with bucket promotion (TDEC-01) — a new `boostTerminalDecimator()` lets a player on the final day multiply their `weightedBurn` by their effective quest-streak factor (validated via `getPlayerQuestView` — the EFFECTIVE streak with gap-reset + shields, NOT raw `playerQuestStates.streak`; streak 100→20×, 10→4×) folded into `terminalDecBucketBurnTotal[key]` via the existing `keccak256(abi.encode(lvl,bucket,subBucket))` key, with a `boosted` bit added to the packed `TerminalDecEntry` for idempotence, AND PROMOTES the player's bucket if the boosted weight qualifies for an improved bucket (per the SPEC improvement + keep-vs-re-derive-subBucket rule), overflow-handled per the SPEC `uint88` policy, reusing the live `DegenerusGameDecimatorModule` machinery (`recordTerminalDecBurn`/`runTerminalDecimatorJackpot`/`_terminalDecMultiplierBps`), gated by `require(!gameOver)`, landing same-tx before `gameOver` flips and before subbuckets are drawn (the freeze-safety carried as an IMPL invariant, re-proven at TST).
  4. The repo-wide `type Day is uint24` UDVT lands byte-preserving + layout-preserving (UDVT-01 / UDVT-02 / UDVT-03 — the heavy item) — the `type Day is uint24` value type + global operator overloads (`<`, `<=`, `==`, `%`, `+`, `-`) replace the raw day-counter integers across the ~649-line/27-contract day-bearing surface with zero behavior change (UDVT-01); the RNG byte-image is preserved bit-for-bit — every `abi.encodePacked(…day…)` RNG-entropy site explicitly casts the day to `uint32` (`DegenerusGameAdvanceModule.sol:1828`/`:1405`, `DegenerusGame.sol:1011` — an unwrapped uint24-backed `Day` would shorten the keccak preimage and change the derived word), the `rngWordByDay` mapping KEY layout unchanged (UDVT-02); and storage layout + gas are preserved — packed `Sub`/struct day fields stay uint24-backed (no cold-slot spill → the v56 gas win intact), standalone day slots + indexed day event topics widen to `uint32` explicitly (UDVT-03).
  5. The diff is producer-before-consumer + `forge build` is clean + HELD at the boundary (all 9 IMPL reqs) — the whole diff is authored producer-before-consumer per the SPEC edit-order map (small-feature edits → the BURNIE coin-buy fix → the wide UDVT refactor → interfaces/wiring), applied to `contracts/` and locally compiling (`forge build` clean; `ContractAddresses.sol` freely modifiable), but NOT committed without explicit user hand-review of the single batched diff (the SOLVENCY ETH/`claimablePool` DEBIT path stays byte-unchanged across all items — the BURNIE fix restores ticket claims without an unbacked obligation — and RNG-freeze stays intact via the UDVT uint32 casts, both carried as IMPL invariants and re-proven at TST 361).
  6. The BURNIE coin-buy ticket-queue Critical fix lands (BURNIE-01 / BURNIE-02) — `_purchaseCoinFor` captures `_callTicketPurchase`'s returns and `if (adjustedQty != 0) _queueTicketsScaled(buyer, targetLevel, adjustedQty, false)` so a BURNIE buy QUEUES tickets (was a pure token sink; `DegenerusVault.gamePurchaseTicketsBurnie` is a live consumer) (BURNIE-01); and the MINT_BURNIE quest credit is restored on the coin path via `quests.handlePurchase`'s MINT_BURNIE leg only (`ethMintSpendWei=0`, `lootBoxAmount=0`; skip score/`recordMint`/affiliate/non-mint-quests) applied as a BURN REBATE — the tx requires the FULL ticket cost upfront, the burn is deferred until `handlePurchase` returns, then burns net = full − MINT_BURNIE reward (floored at 0), co-designed with BATCH-01's return-instead-of-credit (BURNIE-02). RNG-freeze unaffected (`purchaseCoin` reads no `rngWord`); ETH/pool DEBIT byte-unchanged (restored ticket claims create no unbacked obligation), both re-proven at TST 361.
  7. The sDGNRS salvage-swap combo ETH/BURNIE pawn-shop payout lands (SALVAGE-01) — `sellFarFutureTickets`'s cash leg is split into `ethWei` + `burnieValue = cashWei − ethWei`, the ETH share RANDOMIZED full-range `[0..ethCap]` from a new slice of the existing prior-day `seed` (no new VRF), `ethCap` = the existing eth-% cap (BURNIE excluded); the BURNIE leg paid by TRANSFERRING sDGNRS-OWNED BURNIE (token balance + claimable coinflip stake), valued via `_ethToBurnieValue` @ current eth-equivalent — NOT a `creditFlip` mint; `actualBurnie = min(target, sDGNRS-available)`, the remainder + the zero-available case paid as ETH ("like now"); ticket leg unchanged; both the execute path (`MintModule:929/975`) and the preview/quote (`_quoteFarFutureSwap`, reflecting source-availability) updated so the offer stays previewable. Only the ETH part is relabeled out of `claimableWinnings[SDGNRS]` (NO new BURNIE emission) → solvency-positive. Every reachable offer stays ≤ the no-arb ceiling + the eth-% cap (the pawn-shop safety invariant, re-proven at TST 361 / SALVAGE-03).
  8. The manual-sub-cancel auto-claim + auto-evict pure-forfeit lands (CANCEL-01) — manual cancel (`subscribe(…, dailyQuantity=0)`) auto-claims BEFORE clearing: pay the canceller's `pendingBurnie` to themselves via `coinflip.creditFlip` (+ presale-box-credit parity, zero the field/CEI) + drain `affiliateBase` and settle the affiliate tree A/U1/U2 75/20/5 (via `drainAffiliateBase`→`creditFlip`), then `_finalizeAfking`+clear — closing the loss-on-reclaim race; AUTO-EVICT (pass/funding/reclaim) stays a pure FORFEIT (delete the Sub record incl. both accumulators, no payout to self or uplines). BURNIE-emission only (no `rngWord`, no ETH/`claimablePool` touch), `rngLock`-gated at the subscribe entry; SOLVENCY-untouched + RNG-freeze-clean, re-proven at TST 361 / CANCEL-03.

**Plans**: 4 plans

Plans:
**Wave 1**

- [x] 359-01-PLAN.md — BATCH-01/02 producer: handlePurchase returns burnieMintReward (drop inline credit) + quest-TYPE comment fix (wave 1, autonomous:false)

**Wave 2** *(blocked on Wave 1 completion)*

- [x] 359-02-PLAN.md — BURNIE-01/02 (queue-on-return + MINT_BURNIE deferred net-burn rebate) + SALVAGE-01 (combo ETH/BURNIE pawn-shop cash-leg split), same MintModule file (wave 2, autonomous:false)

**Wave 3** *(blocked on Wave 2 completion)*

- [x] 359-03-PLAN.md — WWXRP-01 (per-bracket jackpot whale-halfpass) + TDEC-01 (boostTerminalDecimator + bucket promotion) + CANCEL-01 (manual-cancel auto-claim / auto-evict forfeit) + forge-build CHECKPOINT 1 (wave 3, autonomous:false) — ✅ executed 2026-06-04; CHECKPOINT 1 forge build GREEN; 7 contracts uncommitted

**Wave 4** *(blocked on Wave 3 completion)*

- [ ] 359-04-PLAN.md — UDVT-01/02/03 the wide type Day is uint24 sweep (3 encodePacked uint32 casts, packed uint24 / standalone+key uint32) + forge .t.sol churn + forge-build CHECKPOINT 2 + the USER hand-review contract-commit HARD STOP (wave 4, autonomous:false)

**UI hint**: no

### Phase 360: GAS — Measure + Tune (the UDVT gas-neutrality gate)

**Goal**: The cost impact of the bundle is measured + tuned under the security-over-gas floor: the UDVT is proven GAS-NEUTRAL (packed `Sub`/struct day fields stay uint24-backed with no cold-slot spill → the v56 gas win intact; the explicit uint32 widening of standalone slots + indexed event topics is the only intentional storage delta), the `handlePurchase` batching's saved `creditFlip` is measured (BATCH-01's ~5–25k win), and the WWXRP grant + terminal-decimator boost are confirmed ~zero added cost on the non-triggering path. Any net change rides the same batched USER-APPROVED diff at the contract boundary; the expected outcome is gas-neutral-by-construction → Outcome-A no-diff per the v55 350 / v56 precedent.
**Type**: GAS (CONTRACT BOUNDARY — rides the same boundary if a net tune; else Outcome-A no-diff)
**Depends on**: Phase 359 (the IMPL diff must be applied + locally compiling before the gas marginals can be measured)
**Requirements**: (none — the gas-neutrality measurement gate; UDVT-03 is OWNED at IMPL, its gas-half measured here)
**Success Criteria** (what must be TRUE):

  1. The UDVT is proven GAS-NEUTRAL — the packed `Sub`/struct day fields stay uint24-backed with NO cold-slot spill (the v56 gas win intact, measured), and the explicit uint32 widening of standalone day slots + indexed day event topics is the only intentional storage delta (no unexpected slot growth).
  2. The small-feature gas wins are measured — the `handlePurchase` batching saves exactly one cross-contract `creditFlip` per MINT_BURNIE-quest-completing buy (BATCH-01's ~5–25k win confirmed same-results), and the WWXRP grant + the terminal-decimator boost short-circuit to ~zero added cost on the non-triggering (non-jackpot / non-final-day) path.
  3. The outcome is recorded — if the bundle is gas-neutral-by-construction (the expected outcome — the UDVT is a pure-annotation/explicit-cast pass with no packed-field storage-layout change), that is recorded as the Outcome-A no-diff outcome (per the v55 350 / v56 precedent); any net behavior-preserving change rides the same batched USER-APPROVED diff held at the contract-commit boundary.

**Plans**: TBD
**UI hint**: no

### Phase 361: TST — RNG-Freeze Intact + SOLVENCY-01 Byte-Untouched + Non-Widening + Test/Comment Hygiene

**Goal**: The milestone is proven behaviorally correct + the hard security floor proven empirically against the v56-baseline tree (the SEC gate): RNG-freeze is intact across all 8 items (SEC-01 — incl. the UDVT per-site byte-diff gate that proves the derived `rngWord` is byte-identical at every `abi.encodePacked` entropy site after the uint32 casts), SOLVENCY-01 is untouched (SEC-02 — every change is BURNIE flip-credit / weight-only / pure-annotation OFF the ETH/`claimablePool` path, the ETH/pool debit byte-unchanged), the suite is NON-WIDENING vs the v56 baseline `1e7a646d` (every pre-existing red enumerated BY NAME), and the test/comment hygiene lands (HYG-01 the stale `gameSetAutoRebuy` references fixed; HYG-02 the two stale `_runRewardJackpots`/`EndgameModule` comments corrected).
**Type**: TST
**Depends on**: Phase 360 (the IMPL+GAS subject must be settled — the byte-diff + solvency + non-widening proofs run against the frozen contract subject)
**Requirements**: SEC-01, SEC-02, HYG-01, HYG-02, HYG-03, SALVAGE-03, CANCEL-03
**Success Criteria** (what must be TRUE):

  1. RNG-freeze is intact across all 8 items (SEC-01) — the UDVT `abi.encodePacked` sites preserve the EXACT byte-image (the per-site RNG-freeze byte-diff gate: the derived `rngWord` is byte-identical at `DegenerusGameAdvanceModule.sol:1828`/`:1405` + `DegenerusGame.sol:1011` after the uint32 casts — UDVT-02 enforced empirically), the WWXRP grant + the terminal-decimator boost + bucket-promotion touch only RNG-insensitive counters/weights gated by already-committed outcomes (`s==9` deterministic from the committed word; the boost + promotion deterministic from the fixed effective-streak factor × burn, committed before the draw via `!_livenessTriggered()`), `handlePurchase` batching is BURNIE-accounting only, and the BURNIE ticket-queue fix reads no `rngWord` (`purchaseCoin` gated by `_livenessTriggered`/`gameOverPossible`) — proven empirically (byte-diff + determinism) as the hard gate.
  2. SOLVENCY is preserved (SEC-02) — 5 of 6 items are BURNIE flip-credit / weight-only / pure-annotation OFF the ETH/`claimablePool` path with the ETH/pool DEBIT byte-unchanged; the BURNIE fix restores queued ticket claims on the pools (intended pre-160 design) without creating an unbacked obligation (claimablePool never exceeds balance; ticket wins stay pro-rata) — proven empirically (the solvency-invariant harness against the v56 baseline, incl. a BURNIE-buy-then-claim path).
  3. The suite is NON-WIDENING vs the v56 baseline `1e7a646d` (UDVT-03's regression half) — every pre-existing red is enumerated BY NAME (`REGRESSION-BASELINE-v57.md`), `live − union = empty`, no new contract regression introduced by the bundle (the wide UDVT refactor + the small features incl. the BURNIE coin-buy fix + the salvage combo payout).
  4. The test/comment hygiene lands (HYG-01 / HYG-02 / HYG-03) — the stale `gameSetAutoRebuy` references are fixed to `coinSetAutoRebuy(true, 0)` (`test/unit/GovernanceGating.test.js:247`, `test/unit/DegenerusVault.test.js:385/456`, `test/fuzz/CoverageGap222.t.sol:1055/1060/1084/1085`), restoring the intended `onlyVaultOwner`-rejects-non-majority assertions (HYG-01); the two stale `_runRewardJackpots`/`EndgameModule` comment references are corrected to `_consolidatePoolsAndRewardJackpots` (`DegenerusGameAdvanceModule.sol:1191`, `DegenerusGameDegeneretteModule.sol:809`) — comment-only, the resolution is already inlined there (HYG-02); and the BURNIE-finding doc/test drift is fixed (HYG-03) — a NEW positive test asserts `purchaseCoin` queues a near ticket at `targetLevel` + applies the MINT_BURNIE burn rebate (the missing coverage that let BURNIE-01 hide; existing tests only assert negatives), the stale 3-arg `purchaseCoin(address,uint256,uint256)` call in `CoverageGap222.t.sol` is corrected to the live 2-arg signature, and the unenforced "blocked when RNG locked" `purchaseCoin` docstrings (`DegenerusGame.sol` + `DegenerusGameMintModule.sol`) are corrected.
  5. The salvage-swap combo payout + caps are proven (SALVAGE-03) — a test drives `sellFarFutureTickets` and asserts: the cash leg pays a variable ETH+BURNIE combo when sDGNRS holds BURNIE (some paths transfer sDGNRS-owned BURNIE — token balance and/or claimable coinflip stake — some pay ETH; the split varies), zero sDGNRS BURNIE → all-ETH fallback ("like now"), the BURNIE leg never over-draws sDGNRS (`actualBurnie ≤ sDGNRS-available`), ETH out ≤ the eth-% cap on every path, the total payout value ≤ the no-arb ceiling across the new variability + the fallback (EXTEND `test_SWAP08_NoArbAtCeiling_SweepAllDistances` so the max reachable offer still sits below the far-ticket acquisition floor), and SOLVENCY holds after a salvage (claimablePool never exceeds balance; only the ETH part leaves `claimableWinnings[SDGNRS]`, NO new BURNIE minted).
  6. The manual-cancel auto-claim + auto-evict forfeit are proven (CANCEL-03) — a test asserts: manual cancel pays the canceller's `pendingBurnie` + settles the tree A/U1/U2 even when an advance/`processSubscriberStage` runs immediately after (NO loss — the loss-on-reclaim race is closed); auto-evict (pass-expiry / funding-out) forfeits BOTH accumulators (self + uplines get nothing, the accruals are wiped + unclaimable post-evict); idempotent re-cancel / re-claim finds 0; CEI ordering (zero before credit) holds; all BURNIE-emission off the ETH path (claimablePool byte-unchanged).

**Plans**: TBD
**UI hint**: no

### Phase 362: TERMINAL — Delta Audit + 3-Skill Genuine-PARALLEL Adversarial Sweep + FINDINGS-v57.0 + Closure Flip

**Goal**: Close the v57.0 audit subject (the carefully-sequenced batched diff — the 7 small wins + the wide UDVT refactor, FROZEN at the IMPL+GAS HEAD) via a FULL close that runs its own internal sweep IN-MILESTONE (NOT deferred to v52): a delta-audit (every changed surface NON-WIDENING vs the v56 baseline `1e7a646d` with the RNG-freeze byte-diff + the SOLVENCY-01 byte-anchor re-attested) + the mandatory 3-skill genuine-PARALLEL adversarial economic review + `audit/FINDINGS-v57.0.md` (chmod 444) + the atomic 5-doc closure flip with the `MILESTONE_V57_AT_HEAD_<sha>` signal — re-attesting all 25 v57.0 requirements against the frozen closure HEAD.
**Type**: TERMINAL (FULL close — sweep IN-MILESTONE)
**Depends on**: Phase 361 (the TST proofs + the NON-WIDENING ledger must be established before the delta-audit + adversarial sweep run against the frozen subject)
**Requirements**: AUDIT-01
**Success Criteria** (what must be TRUE):

  1. The delta-audit lands NON-WIDENING vs the v56 baseline (AUDIT-01 part 1) — every changed surface (the `handlePurchase`/`MintModule` fold, the `_resolveFullTicketBet` WWXRP grant, the `boostTerminalDecimator()` + the `DegenerusGameDecimatorModule` machinery, the wide UDVT refactor across the day-bearing surface) is NON-WIDENING with grep/diff anchors @ `1e7a646d`, zero orphan hunks; the RNG-freeze byte-diff (the per-site `abi.encodePacked` uint32-cast byte-image) + the SOLVENCY-01 byte-anchor + the WWXRP per-bracket-grant correctness + the terminal-decimator weight-only-boost + bucket-promotion freeze-safety + the `handlePurchase` batching equivalence are re-attested.
  2. The 3-skill genuine-PARALLEL adversarial sweep runs + is skeptic-filtered (AUDIT-01 part 2) — `/contract-auditor` + `/economic-analyst` + `/zero-day-hunter` run as concurrent background Task spawns (`/degen-skeptic` = the dual-gate filter per `D-271-ADVERSARIAL-02`), focused on the UDVT byte-image preservation + the terminal-decimator bucket-promotion freeze-safety (the NEW weight-determining write before the draw) + the WWXRP rationing-key correctness + the operator-recipient policy; every charged probe is dispositioned (NEGATIVE-VERIFIED / SAFE_BY_DESIGN / FINDING_CANDIDATE) in the adversarial log.
  3. FINDINGS-v57.0 is authored + the closure flip is atomic (AUDIT-01 part 3) — `audit/FINDINGS-v57.0.md` (9-section, chmod 444) records the verdict + the dispositions; the atomic 5-doc closure flip emits the `MILESTONE_V57_AT_HEAD_<sha>` signal; all 25 v57.0 requirements are re-attested against the frozen closure HEAD; KNOWN-ISSUES.md is byte-unmodified unless a genuine new finding is recorded.

**Plans**: TBD
**UI hint**: no

---

## Progress (v57.0)

**Execution Order:** Phases execute in numeric order: 358 → 359 → 360 → 361 → 362

| Phase | Milestone | Plans Complete | Status | Completed |
|-------|-----------|----------------|--------|-----------|
| 358. SPEC — Design-Lock (WWXRP rationing + terminal-decimator decisions + UDVT discipline) + Freeze/Solvency Re-Attestation + Call-Graph Attestation | v57.0 | 3/3 | Complete   | 2026-06-04 |
| 359. IMPL — The ONE Carefully-Sequenced Batched Contract Diff (handlePurchase batching + WWXRP whale-halfpass + terminal-decimator boost + the wide UDVT refactor) | v57.0 | 4/4 | Complete | 2026-06-04 |
| 360. GAS — Measure + Tune (the UDVT gas-neutrality gate) | v57.0 | Outcome-A | Complete | 2026-06-04 |
| 361. TST — RNG-Freeze Intact + SOLVENCY-01 Byte-Untouched + Non-Widening + Test/Comment Hygiene | v57.0 | — (inline) | Complete | 2026-06-04 |
| 362. TERMINAL — Delta Audit + 3-Skill Adversarial Sweep + FINDINGS-v57.0 + Closure | v57.0 | — (inline) | Complete | 2026-06-04 |

> **🔒 v57.0 CONTRACT-BOUNDARY HARD STOP (ONE gate — 359 IMPL · 360 GAS rides the same boundary if it produces a net diff).** Phase 359 IMPL is THE contract phase — the carefully-sequenced batched diff (the 7 small wins + the wide UDVT refactor, authored producer-before-consumer) is applied to `contracts/` and locally compiled (`forge build` clean) but HELD at the contract-commit boundary, NEVER committed without explicit user hand-review (`feedback_batch_contract_approval` + `feedback_never_preapprove_contracts` + `feedback_manual_review_before_push` + `feedback_no_contract_commits`). Phase 360 GAS is the gas-neutrality measurement gate — any net behavior-preserving change rides the same batched USER-APPROVED diff at the same boundary (or records Outcome-A no-diff per the v55 350 precedent). `ContractAddresses.sol` freely modifiable per `feedback_contractaddresses_policy`; tests + planning + docs AGENT-committable.

> **🔓 v57.0 AUDIT POSTURE — FULL CLOSE (internal sweep NOT deferred).** Like v54.0 + v55.0 + v56.0 (and unlike v50.0 + v51.0, whose internal sweeps were deferred → the v52 consolidated audit), **v57.0 runs its own internal 3-skill genuine-PARALLEL adversarial sweep + delta-audit + `audit/FINDINGS-v57.0.md` at TERMINAL (362, AUDIT-01)** — because the UDVT touches the RNG-entropy `abi.encodePacked` sites (the freeze spine) and the terminal-decimator bucket-promotion is a NEW weight-determining write before the draw, both of which must be adversarially probed in-milestone. The separate v52 consolidated cross-model audit folds the v57 surface into its cumulative sweep as an additional track, not a substitute.

---

## Coverage (v57.0)

**25/25 v57.0 requirements mapped to exactly one phase — 0 orphaned, 0 duplicated.** (BURNIE + SALVAGE + CANCEL categories + HYG-03 ADDED 2026-06-04 mid-358 — the coin-buy ticket-queue Critical fix + the salvage-swap combo ETH/BURNIE pawn-shop payout + the manual-cancel auto-claim / auto-evict forfeit.)

| Phase | Requirements | Count |
|-------|--------------|-------|
| 358 SPEC | WWXRP-02, TDEC-02, TDEC-03, BURNIE-03, SALVAGE-02, CANCEL-02 | 6 |
| 359 IMPL | BATCH-01, BATCH-02, WWXRP-01, TDEC-01, UDVT-01, UDVT-02, UDVT-03, BURNIE-01, BURNIE-02, SALVAGE-01, CANCEL-01 | 11 |
| 360 GAS | (none — the gas-neutrality measurement gate; UDVT-03 gas-half measured here, owned at IMPL) | 0 |
| 361 TST | SEC-01, SEC-02, HYG-01, HYG-02, HYG-03, SALVAGE-03, CANCEL-03 | 7 |
| 362 TERMINAL | AUDIT-01 | 1 |
| **Total** | | **25** |

**Per-category split (verification):**

| Category | Total | SPEC (358) | IMPL (359) | GAS (360) | TST (361) | TERMINAL (362) |
|----------|-------|------------|------------|-----------|-----------|----------------|
| BATCH | 2 | — | 2 (01,02) | — | — | — |
| WWXRP | 2 | 1 (02) | 1 (01) | — | — | — |
| TDEC | 3 | 2 (02,03) | 1 (01) | — | — | — |
| UDVT | 3 | — | 3 (01,02,03) | — | — | — |
| HYG | 2 | — | — | — | 2 (01,02) | — |
| SEC | 2 | — | — | — | 2 (01,02) | — |
| AUDIT | 1 | — | — | — | — | 1 (01) |
| **Total** | **15** | **3** | **7** | **0** | **4** | **1** |

**Center-of-gravity rationale (where a requirement spans design + impl + test + close):**

- **WWXRP-02** (the design half — the per-bracket rationing key + the operator-recipient policy + the multi-bracket-allow decision) → SPEC (358), where the rationing-key + recipient decisions are LOCKED BEFORE any code (they are design choices, not built behaviors). **WWXRP-01** (the BUILT per-bracket grant at `_resolveFullTicketBet`) → IMPL (359). No double-counting: WWXRP-02 = the design rule the IMPL applies; WWXRP-01 = the grant the IMPL builds.
- **TDEC-02 / TDEC-03** (the design half — the effective-streak source / bucket-improvement + promotion rule / `uint88` overflow / double-count / shields decisions [TDEC-02], and the freeze-safety RE-PROOF under the bucket-promotion allowance [TDEC-03]) → SPEC (358), where they are settled on paper BEFORE the code (the bucket-promotion freeze re-proof is load-bearing — TDEC-01 now allows promotion, so the validator's original `subBucket`-fixed simplification no longer holds). **TDEC-01** (the BUILT `boostTerminalDecimator()` + bucket promotion) → IMPL (359). No double-counting.
- **BATCH-01/02 · UDVT-01/02/03** (the built behaviors) → IMPL (359) as part of the single batched diff. The UDVT byte-preservation discipline (the per-site cast/keep/widen matrix — the SPEC's SC4) is the design input folded into 358, NOT double-counted; only the requirement's HOME (where it is BUILT) is counted at IMPL.
- **GAS phase (360) owns NO REQ-ID** — it is the gas-neutrality measurement gate. UDVT-03 ("storage layout + gas preserved + NON-WIDENING") is OWNED at IMPL (where the uint24-keep/uint32-widen discipline is BUILT); its gas-neutrality half is MEASURED at GAS (the packed Sub day fields stay uint24 / no cold-slot spill), and its NON-WIDENING-regression half is PROVEN at TST. The GAS phase exists because the UDVT must be PROVEN gas-neutral (the v56 packed-Sub gas win is load-bearing) — a measurement gate, not a requirement owner. (Unlike v55/v56, whose GAS phases owned GAS-* requirements, v57 has no GAS-* requirements — the bundle is gas-neutral-by-construction, so the GAS phase records Outcome-A no-diff in the expected case.)
- **SEC-01 / SEC-02 · HYG-01 / HYG-02** → TST (361). SEC-01/02 (RNG-freeze intact incl. the UDVT per-site byte-diff gate + SOLVENCY-01 byte-untouched) are PROVEN empirically as the hard gate (the SPEC re-attestation at 358's SC5 is the design gate, NOT a separate count); the TERMINAL adversarial review (AUDIT-01) re-confirms SEC, but SEC-01/02's center-of-gravity (where the property is first PROVEN) is TST. HYG-01/02 (the test/comment hygiene) land here because they are test-file + comment edits — no contract logic change, so they do NOT belong in the IMPL contract diff; they ride the TST phase (AGENT-committable, no contract-commit gate).
- **AUDIT-01** (the TERMINAL FULL close) → TERMINAL (362); it re-attests all 25 v57.0 requirements + runs the in-milestone delta-audit + the 3-skill adversarial sweep + `audit/FINDINGS-v57.0.md` + the atomic 5-doc closure flip with the `MILESTONE_V57_AT_HEAD_<sha>` signal.

---

## ✅ v56.0 AfKing Everyday-Gas Minimization (SHIPPED 2026-06-04 — closure signal `MILESTONE_V56_AT_HEAD_1e7a646d44da4ee26375edd0b006274821fef73e`; subject frozen `1e7a646d`; baseline `453f8073`; 0 NEW_FINDINGS; THE FIRST TERMINAL to mutate contracts/ across SIX gates)

**Milestone:** v56.0 (started 2026-06-01)
**Defined:** 2026-06-01
**Audit baseline → subject:** v55.0 closure HEAD — frozen contract subject `453f8073`, closure signal `MILESTONE_V55_AT_HEAD_ca3bbd3220de763298ef2e742111f6e6ef90d583` → v56.0 closure HEAD. Subject = the single carefully-sequenced batched `contracts/*.sol` diff for the mode-agnostic aggregator + the ticket-mode minimal-write primitive + the open-end optimizations + the shared-`DegenerusQuests`-core batched-settle entrypoint. Every cited `file:line` MUST be re-attested against the v55.0-closure HEAD `453f8073` before any patch.
**Scope source:** `.planning/REQUIREMENTS.md` (24 v56.0 REQ-IDs across 8 categories: AGG 5 · TKT 2 · AFF 2 · QST 5 · OPEN 2 · GAS 4 · SEC 2 · XMODEL 1 · AUDIT 1) + the milestone init (2026-06-01) + the design-lock input `.planning/PLAN-V56-AFKING-BATCHING-GAS.md` + the `[[v56-batch-afking-affiliate-quest-seed]]` memory. **No research** — a fully-specced internal contract gas+batching refactor with a USER-converged mechanism (the mode-agnostic ~10-day aggregator); the design explorations (affiliate-distribution non-gameability, ticket-mode parity) are resolved in the plan doc.

> **Scope (USER-locked):** the AFKING SYSTEM specifically — **BOTH ENDS: the BUYING (the per-day process STAGE / accrual / settle) AND the OPENING (the box open-pass / materialize).** Make the whole afking path maximally gas-efficient while remaining COMPLETELY secure and UNMANIPULABLE (no economic edge from any "fuckery" — with PARTICULAR focus on gaining an edge by subscribing/unsubscribing strategically). NOT a whole-ecosystem sweep — afking buy + open only (shared-code touches handled with care).

> **Posture — NOT a behavior-identical pass.** Slight semantic simplifications are acceptable if they cut gas and stay unmanipulable; the hard floor is unmanipulable + secure (enforced by a mandatory 3-skill adversarial economic review PLUS a baked-in cross-model Codex + Gemini review — XMODEL-01). The unifying design (USER-converged 2026-06-01) is a MODE-AGNOSTIC accrue-cheap-per-buy aggregator that settles all affiliate + quest ("the extraneous stuff") for BOTH ticket and lootbox subs: accrue cheap per-buy into a per-sub accumulator (NO cross-contract calls), then settle per-leg — the AFFILIATE leg is a flat-7% deterministic-split PULL (`claim`/`withdraw`, NO scheduled flush, NO roll) and the QUEST leg settles AUTOMATICALLY by RIDING THE DAILY BUY STAGE on the global settle day (`currentDay % settlePeriod == 0`, ~10-day cadence — the slot-0 `questProgress` delivered-day counter mints `× QUEST_SLOT0_REWARD` **+ the accrued `buyerOwedBurnie` 10%/20% ticket buyer-bonus** in ONE `creditFlip` to the sub) + a permissionless `claimQuest(subs[])` keeper-liveness fallback (+ quest-settle-on-unsub) — _amended 2026-06-01 — quest settle RIDES THE BUY STAGE (the separate `mintBurnie` "settlement-due" router leg is REJECTED as a redundant cold-SLOAD pass; `SUB_STAGE_BATCH` SHRUNK so the settle-day chunk fits 16.7M, number deferred to 355); the afking ticket buyer-bonus 10%/20% is accrued + settled with the quest (matches live `:1655`); first-sub-only +daysToNextSettle streak, no provisional: SUPERSEDES the prior "scheduled-roll/player-flush-replays-the-roll" mechanism; the roll is removed_. Each mode's per-day work collapses to a minimal primitive — lootbox = the existing box-stamp; ticket = a NEW custom "write the ticket entries to the queue" fn that mirrors the box-stamp (dropping the per-day `MintModule.purchaseWith` ~262k heavyweight, **but ACCRUING the 10%/20% ticket buyer-bonus `:1655-1659` into `buyerOwedBurnie` at live parity — closing the v55-style dropped-bonus regression**). Plus a STAGE gas sweep (accumulator in the `Sub` slot's spare bits, `SUB_STAGE_BATCH` re-tune, mode/SLOAD collapse) + fold-in O1 (the pre-existing `DegenerusQuests` lootbox-quest BURNIE double-credit, surfaced at the v55 close).

> **Why it is unexploitable (the load-bearing non-exploitability fact, carried into SEC/XMODEL):** the afking affiliate distribution is a **flat-7% deterministic-split PULL** with NO roll and NO seed (_amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic: SUPERSEDES the prior daily-seeded roll_) — per buy accrue a flat `_ethToBurnie(eth)×7/100` into the running `affiliateBase`, settle by PULL via `claim` with the fixed 75/20/5 split + `withdraw` (CEI). The **buyer never receives the base** (`A ≠ sub` guaranteed + the rare U1/U2==sub cycle skip, `DegenerusAffiliate.sol:579`) → no settle-timing profit. Because there is exactly ONE deterministic distribution path (no roll, no seed, no scheduled-vs-player-flush divergence), there is **no favorable seed to select AND no two-distribution free option to arbitrage** — the XMODEL C1/C2 free-option finding is MOOT (the roll is removed). The taper is afking-N/A (flat 7%; the anti-concentration taper applies to manual buys only). No double-settle: self-marking running balances (`affiliateBase` zeroed at `claim`; the slot-0 `questProgress` delivered-day counter drained at the automatic `mintBurnie` settlement-due flush — or the permissionless `claimQuest(subs[])` fallback) — the per-sub `windowStartDay`/`lastSettledDay` markers are DROPPED. Streak-farming is gated by the active-pass check + the `lastCompletedDay`/`afkCoveredThroughDay` double-credit guards + the per-window streak advancing only on debit-DELIVERED days; the first-sub-only `+daysToNextSettle` head-start is bounded +0..+9 once/account (USER-ACCEPTED-BY-DESIGN — the bound replaces the prior confirmed-vs-provisional escrow; sybil farming is pass-cost-gated — _amended 2026-06-01 — automatic slot-0 BURNIE via mintBurnie chain + claimQuest fallback; first-sub-only +daysToNextSettle streak, no provisional_). Adversarially re-cleared (economic-analyst + zero-day-hunter, no Medium+).

> **Cross-cutting rule (every requirement):** every cited `file:line` MUST be re-attested against the **v55.0 closure HEAD `453f8073`** before any patch (no "by construction" / "single fn reaches all paths" claim survives un-checked — the `DegenerusGame` mint/jackpot inline-duplication precedent; `feedback_verify_call_graph_against_source`). The cited anchors (the per-buy cross-contract storm `GameAfkingModule.sol:760-831` / the lootbox stamp branch `:735-749` / the ticket-mode `purchaseWith` route `:713-731`; the affiliate daily-seeded roll `DegenerusAffiliate.sol:558`/`:579` + the leaderboard `affiliateCoinEarned[lvl]` + `_updateTopAffiliate`; the `DegenerusQuests.handlePurchase` double-credit `:890` + `awardQuestStreakBonus`; the afking open path `_openAfkingBox`→`resolveAfkingBox` + `mintBurnie`/`autoOpen`/`OPEN_BATCH`; the `Sub` 4-field stamp `(scorePlus1, amount, lastAutoBoughtDay)` + `lastOpenedDay` in `DegenerusGameStorage`; the `_applyEvMultiplierWithCap`/`lootboxEvBenefitUsedByLevel[player][level+1]` EV-cap; the STAGE placement in `DegenerusGameAdvanceModule`) are confirmed at SPEC. **Security / freeze / solvency floor over gas** (`feedback_security_over_gas`): SEC-01 (unmanipulable, esp. strategic sub/unsub) + SEC-02 (SOLVENCY-01 untouched — affiliate/quest stay BURNIE flip-credit OFF the ETH/`claimablePool` path; RNG-freeze intact) are the hard gate. Pre-launch redeploy-fresh (storage-layout break fine, no migration; `feedback_frozen_contracts_no_future_proofing`).

> **Posture:** the contract changes ship as ONE carefully-sequenced batched USER-APPROVED `contracts/*.sol` diff with a **HARD STOP at the contract-commit boundary** (applied + locally compiled/tested, NEVER committed without explicit user hand-review — `feedback_batch_contract_approval` + `feedback_never_preapprove_contracts` + `feedback_manual_review_before_push` + `feedback_no_contract_commits`; `ContractAddresses.sol` freely modifiable per `feedback_contractaddresses_policy`). The IMPL (354) is the ONE contract gate — the aggregator (accrue + scheduled/alteration settle), the ticket-mode custom write primitive, the `DegenerusQuests` batched-settle entrypoint, the accumulator, and the open-end optimizations land as a single producer-before-consumer diff. Sequential-on-main (worktrees unsafe — submodule + node_modules; `use_worktrees: false`, `no_worktree_paths: [contracts]`). Tests + planning + docs AGENT-committable.

> **Audit posture — FULL CLOSE WITH ITS OWN INTERNAL SWEEP (NOT deferred).** Like v54.0 + v55.0 (and unlike v50.0 + v51.0, which deferred their internal sweep → the v52 consolidated audit), **v56.0 runs its own internal 3-skill genuine-PARALLEL adversarial sweep + delta-audit + `audit/FINDINGS-v56.0.md` at TERMINAL (357, AUDIT-01)** — the 3-skill pass (`/contract-auditor` + `/economic-analyst` + `/zero-day-hunter`; `/degen-skeptic` = the dual-gate filter per `D-271-ADVERSARIAL-02`) AUGMENTED by the cross-model close (Codex + Gemini) + the delta-audit + `audit/FINDINGS-v56.0.md` (chmod 444) + the atomic 5-doc closure flip all execute in-milestone, focused on the **strategic sub/unsub edge + the shared-quest-core non-perturbation + the two-path open**. Rationale: this touches the **shared `DegenerusQuests` core + the everyday `advanceGame` STAGE** — the unmanipulability + non-perturbation are the load-bearing concerns and must be adversarially probed in-milestone, not deferred. (The separate v52 consolidated cross-model audit still folds the v56 surface into its cumulative sweep — an additional track, not a substitute for v56's own close.)

> **Cross-model review (XMODEL-01) is baked in at TWO touchpoints.** It runs at **SPEC (353)** as a DESIGN INPUT — Codex + Gemini are fed crafted prompts on the buy+open design (long-run gas suggestions + adversarial "unmanipulable" verification, PARTICULARLY the strategic sub/unsub edge); the suggestions + exploit findings are folded into the design-lock BEFORE IMPL (the gating deliverable — this is XMODEL-01's home/primary deliverable). The same cross-model models then AUGMENT the **TERMINAL (357)** adversarial close (Claude 3-skill + Codex + Gemini). Both touchpoints are reflected in the respective phases' success criteria.

> **Phase numbering** continues from the previous milestone — v55.0 ran 348–352, so **v56.0 starts at Phase 353.** Not reset to 1. (Prior milestones' phase dirs are archived under `.planning/milestones/vXX.0-phases/`.)

> **Milestone shape** is the established v54.0 + v55.0 audit pattern (with a dedicated GAS phase): **353 SPEC design-lock (the mode-agnostic aggregator + the ticket-mode minimal-write primitive + the afking-OPEN-end review + the per-sub accumulator storage layout + the shared-`DegenerusQuests`-core non-perturbation approach + the freeze/solvency/unmanipulable re-attestation + the XMODEL-01 design-input cross-model pass folded in + grep-attest every `file:line` vs `453f8073`) → 354 IMPL the ONE carefully-sequenced batched contract diff (the aggregator [accrue + scheduled/alteration settle] + the ticket-mode custom write primitive + the `DegenerusQuests` batched-settle entrypoint + the accumulator + the open-end optimizations; producer-before-consumer) → 355 GAS (measure + tune — per-buy + per-open + per-settle marginals under the 16.7M ceiling, accumulator packing, `SUB_STAGE_BATCH` re-tune, mode/SLOAD collapse) → 356 TST (the proofs — unmanipulable [esp. strategic sub/unsub] + non-perturbation of the shared quest core + two-path-open coexistence + gas marginals + NON-WIDENING regression) → 357 TERMINAL FULL close (delta-audit + 3-skill genuine-PARALLEL adversarial sweep AUGMENTED by the XMODEL cross-model close + `audit/FINDINGS-v56.0.md` + atomic 5-doc closure flip).** The contract-boundary HARD STOP lives at ONE phase (354 IMPL).

### Phases

- [x] **Phase 353: SPEC — Design-Lock (mode-agnostic aggregator + ticket-mode primitive + open-end review + accumulator layout + shared-quest non-perturbation + XMODEL design-input) + Unmanipulable/Solvency Re-Attestation + Call-Graph Attestation** - Design-lock the mode-agnostic ~10-day aggregator (accrue-cheap-per-buy → settle-once: the affiliate accrue/settle split [AFF-01 the **flat-7% deterministic-split PULL** — accrue flat `_ethToBurnie(eth)×7/100` per buy into the running `affiliateBase`, settle by PULL via `claim`/`withdraw` with the fixed 75/20/5 split (buyer-never-wins, `DegenerusAffiliate.sol:579`); NO roll, NO seed, NO scheduled/mutation affiliate flush — _amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic: SUPERSEDES the XMODEL roll-unification — the roll itself is REMOVED, so the C1/C2 free-option finding is MOOT; adversarially re-cleared (economic-analyst + zero-day-hunter, no Medium+)_; AFF-02 taper afking-N/A (manual-only) + leaderboard claim-time] + the per-sub accumulator storage layout [the SHRUNK in-slot `affiliateBase`+`questProgress` running balances + the off-slot `pendingClaim` mapping; `settledThroughDay`/`windowStartDay`/`lastSettledDay` DROPPED] + the QUEST-only self-marking settle) + the ticket-mode minimal-write primitive shape (the custom "write the ticket entries to the queue" fn mirroring the box-stamp + the century/x00 quantity-bonus parity keep-or-drop decision) + the afking-OPEN-end review (the `_openAfkingBox`→`resolveAfkingBox` + `mintBurnie`/`autoOpen`/`OPEN_BATCH` materialization + the unmanipulable re-verification) + the shared-`DegenerusQuests`-core batched-settle entrypoint shape + the non-perturbation approach (PROVE non-perturbing to manual/bingo/degenerette/boon callers) + the QST design (automatic slot-0 BURNIE via the `mintBurnie` settlement-due chain + a `claimQuest` fallback; the first-sub-only `+daysToNextSettle` streak head-start [bounded +0..+9, no provisional — _amended 2026-06-01 — automatic slot-0 BURNIE via mintBurnie chain + claimQuest fallback; first-sub-only +daysToNextSettle streak, no provisional_]) + the unmanipulable (esp. strategic sub/unsub) + SOLVENCY-01-untouched + RNG-freeze-intact re-attestation; run the XMODEL-01 cross-model design-input pass (Codex + Gemini crafted prompts on buy+open → fold gas suggestions + exploit findings into the design-lock BEFORE IMPL); grep-attest every cited `file:line` vs `453f8073` — paper-only, ZERO `contracts/*.sol`. (completed 2026-06-01)
- [x] **Phase 354: IMPL — The ONE Carefully-Sequenced Batched Contract Diff (aggregator + ticket-mode primitive + DegenerusQuests batched-settle + accumulator + open-end)** - Land the single reconciled `contracts/*.sol` diff producer-before-consumer: the per-sub accumulator (`DegenerusGameStorage` — packed per the SPEC layout) + the mode-agnostic aggregator in the STAGE (amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic) (AGG-01 accrue flat-7% affiliate-base + quest-progress running balances (+ the `buyerOwedBurnie` 10%/20% ticket buyer-bonus on ticket subs) with NO cross-contract calls; the AFFILIATE leg is PULL — `claim`/`withdraw` with the fixed 75/20/5 split, NO scheduled/mutation affiliate flush; AGG-02 the QUEST-only automatic settle RIDING THE DAILY BUY STAGE on the global settle day [amended 2026-06-01 — the separate `mintBurnie` "settlement-due" router leg is REJECTED as a redundant cold-SLOAD pass; the inline `_settleQuest` mints `questProgress × QUEST_SLOT0_REWARD` + the accrued `buyerOwedBurnie` ticket bonus in ONE `creditFlip`; `SUB_STAGE_BATCH` SHRUNK so the settle-day chunk fits 16.7M, number deferred to 355]; AGG-03 the QUEST-settle-on-unsub BEFORE applying the change [the affiliate base is NOT flushed — it persists for the uplines to PULL]; AGG-04 mode-agnostic uniform QUEST-settle for BOTH ticket + lootbox subs [the affiliate base likewise pulled uniformly via `claim`]; AGG-05 the self-marking running balances [`windowStartDay`/`lastSettledDay` double-settle markers DROPPED]) + the ticket-mode minimal-write primitive (TKT-01 the custom "write the ticket entries to the queue" fn replacing the per-day `purchaseWith` heavyweight + the inline affiliate/quest deferred to the aggregator + the 10%/20% ticket buyer-bonus `:1655-1659` ACCRUED into `buyerOwedBurnie` [closing the v55-style dropped-bonus regression]; TKT-02 resolution-equivalent ticket entries + the century/x00 parity decision applied + the 10%/20% buyer-bonus at live parity, minted with the quest at the STAGE settle — amended 2026-06-01 — the afking ticket primitive accrues the 10%/20% buyer-bonus [matches live `:1655`, minus kickback], settled with the quest) + the `DegenerusQuests` batched-settle entrypoint + the streak machinery (amended 2026-06-01 — automatic slot-0 BURNIE via mintBurnie chain + claimQuest fallback; first-sub-only +daysToNextSettle streak, no provisional) (QST-01 the first-sub-only `+daysToNextSettle` head-start [bounded +0..+9, no provisional] on the ±10-per-window model + slot-0 = a `questProgress` delivered-day counter → settle-mint `× QUEST_SLOT0_REWARD` [+ the accrued `buyerOwedBurnie` ticket bonus] via the STAGE-riding settle on the global settle day / `claimQuest` fallback + slot-1 manual; QST-02 the bounded-accepted first-sub head-start REFRAME [USER-ACCEPTED-BY-DESIGN, the bound replaces the prior escrow], the activity-score reads the actual streak, the per-window streak advances only on debit-DELIVERED days; QST-03 the `lastCompletedDay`/`afkCoveredThroughDay` double-credit guard + active-pass anti-reset, slot rewards never suppressed; QST-04 the entrypoint proven non-perturbing to manual/bingo/degenerette/boon callers; QST-05 the O1 lootbox-quest double-credit confirmed-intended-or-fixed) + the open-end optimizations (OPEN-01 the `_openAfkingBox`→`resolveAfkingBox` + `mintBurnie`/`autoOpen`/`OPEN_BATCH` path optimized for max gas, cheapest viable shared materialization with the human path, no cold ledger; OPEN-02 the open stays COMPLETELY unmanipulable — live-level parity with human `openLootBox`, no double-open via `lastOpenedDay` monotone, no EV-cap double-draw on the shared `(player,level)` budget, no shared-mutable-state hazard — RE-VERIFIED after the accrual/settle refactor) — authored producer-before-consumer, applied to `contracts/` and locally compiling (`forge build` clean), then HELD at the contract-commit boundary for explicit user hand-review. (completed 2026-06-01)
- [ ] **Phase 355: GAS — Measure + Tune (per-buy / per-open / per-settle marginals under 16.7M + accumulator packing + SUB_STAGE_BATCH re-tune + mode/SLOAD collapse)** - Measure + tune the everyday cost under the security-over-gas floor (gas-scavenger surfaces, gas-skeptic validates — any simplification must stay unmanipulable; not-real or invariant-trading wins REJECTED with reasoning): the per-buy marginal measurably reduced (GAS-01 target lootbox ~206k → ~130–140k; ticket off the ~262k `purchaseWith` heavyweight; measured per-buy + per-settle marginal via a TST-06-style harness under the 16.7M HARD per-tx ceiling); the per-sub accumulator packed into the `Sub` slot's spare bits where feasible (GAS-02 no new cold per-buy SSTORE); `SUB_STAGE_BATCH` re-tuned for the lower per-sub cost (GAS-03 throughput/headroom; the per-day STAGE stays under the 16.7M ceiling at the `SUBSCRIBER_CAP`); and redundant payment-mode branches / repeated SLOADs in the STAGE collapsed where a slight simplification is cheaper (GAS-04, allowed under the scope latitude). Any net contract change beyond the IMPL diff rides the same batched USER-APPROVED diff held at the contract-commit boundary; if the savings are structural to the IMPL refactor (Outcome-A precedent, v55 350), that is recorded as the outcome.
- [x] **Phase 356: TST — Unmanipulable (strategic sub/unsub) + Quest-Core Non-Perturbation + Two-Path-Open Coexistence + Gas Marginals + Non-Widening** ✅ COMPLETE 2026-06-02 (7/7 plans; 4 new v56 green proofs [V56SecUnmanipulable 11 / V56FreezeSolvency 7 / V56QuestNonPerturb 7 / V56AfkingGasMarginal 15] + the D-10 10-file offset migration + the BY-NAME NON-WIDENING ledger `REGRESSION-BASELINE-v56.md` [live v56 624/134/30 byte-identical to the empirical 453f8073 baseline union BY NAME; the 14 migration-unmasked v56-behavior reds DROPPED-by-name; SOLVENCY-01 leg-1 byte-diff anchor; Hardhat compile-sanity EXIT 0]; drainAffiliateBase dispatch-stub reachability carried to 357; ZERO contracts/*.sol mutation) - Prove the milestone behaviorally correct + the hard security floor empirically against the v55-baseline tree (the SEC gate): the afking system (buy + open) is unmanipulable — no positive-EV vector from settle-timing (no seed — _amended 2026-06-01: the roll is REMOVED under the flat-7% deterministic-split pull_), **strategic sub/unsub churn (the USER-flagged PRIMARY concern — churn to re-claim the affiliate [forfeit-nothing-gain-nothing] / dodge a streak penalty / harvest or duplicate a settlement)**, re-rate-on-alteration, pre-credit-EV inflation, double-credit, open-timing, or settle-griefing (SEC-01 — proven empirically + adversarially); SOLVENCY-01 untouched (SEC-02 — the affiliate/quest rewards remain BURNIE flip-credit OFF the ETH/`claimablePool` path, the ETH/pool debit byte-unchanged, RNG-freeze intact under the new accrual/settle); the shared `DegenerusQuests` core batched-settle entrypoint is non-perturbing to the manual/bingo/degenerette/boon callers; the two open routes (afking-stamp vs human `lootboxEth`/`boxPlayers`) coexist with no shared-state hazard; the per-buy + per-open + per-settle marginal gas is measured under the 16.7M HARD per-tx ceiling (the GAS-01..04 wins proven same-results); and the suite is NON-WIDENING vs the v55 baseline `453f8073` (every pre-existing red enumerated BY NAME, `REGRESSION-BASELINE-v56.md`).
- [x] **Phase 357: ✅ COMPLETE 2026-06-04 — TERMINAL — Delta Audit + 3-Skill Genuine-PARALLEL Adversarial Sweep (AUGMENTED by XMODEL Codex + Gemini close) + FINDINGS-v56.0 + Closure Flip** - Close the v56.0 audit subject (the carefully-sequenced batched diff — the aggregator + the ticket-mode primitive + the open-end optimizations + the `DegenerusQuests` batched-settle, FROZEN at the IMPL+GAS HEAD) via a FULL close that runs its own internal sweep IN-MILESTONE (NOT deferred to v52): a delta-audit (every changed surface NON-WIDENING vs the v55 baseline `453f8073` with zero orphan hunks; SOLVENCY-01 byte-unchanged + RNG-freeze intact + the affiliate deterministic-split-PULL non-gameability (NO roll/seed — _amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic_) + the open-end two-path/no-double-open + the shared-quest-core non-perturbation re-attested) + the mandatory 3-skill genuine-PARALLEL adversarial economic review (`/contract-auditor` + `/economic-analyst` + `/zero-day-hunter`; `/degen-skeptic` = the dual-gate filter per `D-271-ADVERSARIAL-02`) focused on the strategic sub/unsub edge + the settle-timing non-exploitability (no seed — _amended 2026-06-01: the roll/seed is REMOVED under the flat-7% deterministic-split pull_) + the pre-credit-EV inflation + the two-path open, AUGMENTED by the XMODEL-01 cross-model close (Codex + Gemini fed crafted prompts on the FULL afking system buy+open, augmenting the Claude sweep) + `audit/FINDINGS-v56.0.md` (chmod 444) + the atomic 5-doc closure flip with the `MILESTONE_V56_AT_HEAD_<sha>` signal — the internal sweep + the cross-model close run IN-MILESTONE (NOT deferred to v52).

---

## Phase Details

### Phase 353: SPEC — Design-Lock + Unmanipulable/Solvency Re-Attestation + XMODEL Design-Input + Call-Graph Attestation

**Goal**: The v56.0 mechanism is settled in writing so the IMPL phase authors a fully reconciled diff with zero "by construction" assumptions, and the load-bearing non-exploitability + non-perturbation are designed BEFORE any code is written: the mode-agnostic ~10-day aggregator (the affiliate accrue/settle split + the per-sub accumulator storage layout + the double-settle markers), the ticket-mode minimal-write primitive shape (+ the century/x00 parity decision), the afking-OPEN-end review (the materialization + the unmanipulable re-verification), the shared-`DegenerusQuests`-core batched-settle entrypoint shape + the non-perturbation approach, and the QST design (automatic slot-0 BURNIE via the `mintBurnie` settlement-due chain + a `claimQuest` fallback; the first-sub-only `+daysToNextSettle` streak head-start [bounded +0..+9, no provisional] — _amended 2026-06-01 — automatic slot-0 BURNIE via mintBurnie chain + claimQuest fallback; first-sub-only +daysToNextSettle streak, no provisional_) are LOCKED; the unmanipulable (esp. strategic sub/unsub) + SOLVENCY-01-untouched + RNG-freeze-intact properties are re-attested on paper; the XMODEL-01 cross-model design-input pass (Codex + Gemini crafted prompts) is run and its gas suggestions + exploit findings folded into the design-lock BEFORE IMPL; and every cited `file:line` is grep-verified against the v55.0 HEAD `453f8073` — paper-only, zero `contracts/*.sol`.
**Type**: SPEC
**Depends on**: Nothing (first v56.0 phase; consumes the v55.0 closure HEAD `453f8073` as the frozen audit baseline, plus the design-lock input `.planning/PLAN-V56-AFKING-BATCHING-GAS.md` + the `[[v56-batch-afking-affiliate-quest-seed]]` memory)
**Requirements**: AFF-01, AFF-02, XMODEL-01
**Success Criteria** (what must be TRUE):

  1. The affiliate distribution is design-locked + proven non-gameable (AFF-01 / AFF-02) — the afking affiliate distribution is a **flat-7% deterministic-split PULL**: accrue `_ethToBurnie(ethSpent)×7/100` (flat, no taper, no kickback) per buy into the sub's running `affiliateBase`; settle by PULL via `claim(address[] subs)` (same-affiliate batch, ONE `sumB`, the fixed 75/20/5 split computed once → `pendingClaim[A/U1/U2]`, buyer-never-wins via `A ≠ sub` guaranteed + the rare U1/U2==sub cycle skip); `withdraw()` = the only cross-contract `creditFlip` (CEI). There is NO roll, NO seed, NO scheduled/mutation affiliate flush — so settle-timing can NEVER select a favorable seed AND no two-distribution free option can exist (exactly ONE deterministic path) (amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic: SUPERSEDES the XMODEL roll-unification — the roll itself is REMOVED, so the C1/C2 free-option finding is MOOT; adversarially re-cleared, economic-analyst + zero-day-hunter, no Medium+) (the load-bearing non-exploitability fact: the split is EV-neutral + intra-upline-chain-only-redistributive + buyer-never-wins, `DegenerusAffiliate.sol:579`, re-attested) (AFF-01); the activity taper is afking-N/A (flat 7%; `_applyLootboxTaper` applies to MANUAL buys only), and the affiliate leaderboard credits at `claim` time to the direct affiliate `A` (USER-accepted claim-time distortion — there is no scheduled affiliate settle under the pull) (AFF-02).

  2. The mode-agnostic aggregator + the per-sub accumulator storage layout + the self-marking settle are design-locked (AFF-01/AFF-02 inputs; AGG/GAS-02 design feed) — the affiliate leg is PULL (no scheduled flush) while the QUEST leg settles uniformly for BOTH ticket and lootbox subs (the single mode-agnostic quest-settle path); the per-sub accumulator layout is decided (the SHRUNK in-slot `affiliateBase uint32 (whole-BURNIE, 100M clamp)` + `questProgress` running balances re-packed into the same Sub slot — attested at 232/256, NOT the stale "176/256" premise — plus the off-slot `pendingClaim` mapping for the affiliate recipients); and double-settle is impossible via **self-marking running balances** — the affiliate `claim` zeroes `affiliateBase[sub]` and the quest flush drains `questProgress` (both a no-op on a double-fire), so the per-sub `windowStartDay`/`lastSettledDay`/`settledThroughDay` markers are DROPPED — recorded for the IMPL to BUILD (amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic) (AGG-01..05 / GAS-02 are owned at IMPL/GAS; their design is fixed here).

  3. The ticket-mode minimal-write primitive + the afking-OPEN-end review are design-locked (TKT/OPEN design feed) — the custom "write the ticket entries to the queue" fn shape is locked (a cheap `_queueTicketsScaled`-equivalent primitive mirroring the lootbox box-stamp, producing resolution-equivalent placement/trait/quantity, the affiliate + quest deferred to the aggregator), and the afking-ticket century/x00 quantity-bonus parity (`MintModule:1243`) keep-or-drop decision is made (allowed to drop for simplicity under the scope latitude); and the afking-OPEN-end (`_openAfkingBox`→`resolveAfkingBox` + `mintBurnie`/`autoOpen`/`OPEN_BATCH`) is reviewed for the cheapest viable materialization shared with the human path AND re-verified unmanipulable (live-level parity, no double-open, no EV-cap double-draw, no shared-state hazard) — recorded for the IMPL (TKT-01/02 + OPEN-01/02 are owned at IMPL; their design is fixed here).

  4. The shared-`DegenerusQuests`-core non-perturbation approach + the QST design (automatic slot-0 BURNIE + first-sub-only streak head-start) are design-locked + the O1 disposition is decided (QST design feed; amended 2026-06-01 — automatic slot-0 BURNIE via mintBurnie chain + claimQuest fallback; first-sub-only +daysToNextSettle streak, no provisional) — the batched-settle entrypoint added to the shared `DegenerusQuests` core has a locked shape with a non-perturbation approach (PROVE non-perturbing to the manual / bingo / degenerette / boon callers — `awardQuestStreakBonus` etc.); the QST settle/streak design is derived — slot-0 completion accrues a delivered-day `questProgress` COUNTER → settled = mint `× QUEST_SLOT0_REWARD` via the `mintBurnie` "settlement-due" router chain when due (~10-day cadence) + a permissionless `claimQuest(subs[])` keeper-liveness fallback (always credits the sub, never the caller) + a quest-settle-on-unsub (slot-1 stays MANUAL; the ±10 streak is the activity-score multiplier, NOT direct BURNIE), the streak uses a SIMPLIFIED first-sub-only `+daysToNextSettle` head-start (`hasEverSubscribed` 1-bit; bounded +0..+9 over the manual baseline, no provisional/vesting) on the ±10-per-window model (−10 on unsub) with the per-window streak advancing only on debit-DELIVERED days, the `lastCompletedDay`/`afkCoveredThroughDay` double-credit guard + active-pass anti-reset (slot rewards NEVER suppressed); the bounded +0..+9 first-sub head-start is USER-ACCEPTED-BY-DESIGN (the bound REPLACES the prior confirmed-vs-provisional escrow for the afking grant — a deliberate accepted tradeoff, NOT a missed control; the activity-score still reads the actual `state.streak`); and the pre-existing O1 lootbox-quest BURNIE double-credit (`DegenerusQuests.handlePurchase` internal `creditFlip` + the returned-and-re-credited value, `:890`) is adjudicated intended-vs-bug with the fix designed if a genuine over-mint — recorded for the IMPL (QST-01..05 are owned at IMPL; their design is fixed here).

  5. The unmanipulable/solvency/freeze re-attestation + the XMODEL design-input pass land + every cited `file:line` is grep-attested vs `453f8073` (XMODEL-01; SEC design feed) — the unmanipulable property (esp. the strategic sub/unsub edge), SOLVENCY-01-untouched (the affiliate/quest stay BURNIE flip-credit OFF the ETH/`claimablePool` path; this is a BURNIE-emission-timing change only), and RNG-freeze-intact-under-the-new-accrual/settle are re-attested on paper as the SPEC's design gate (SEC-01/02 are owned at TST; their design is fixed here); the XMODEL-01 cross-model DESIGN-INPUT pass is run — Codex + Gemini are fed crafted prompts covering the FULL afking system (the BUYING STAGE/accrual/settle AND the OPENING open-pass/materialize) for (a) long-run gas-optimization suggestions and (b) adversarial verification that the design is completely unmanipulable (PARTICULAR focus on the strategic sub/unsub edge), and the suggestions + exploit findings are folded into the design-lock BEFORE IMPL (this is XMODEL-01's primary deliverable / home; its TERMINAL close-augmentation half is reflected in Phase 357's success criteria); and every cited anchor across the milestone scope (the per-buy cross-contract storm + the lootbox stamp branch + the ticket `purchaseWith` route in `GameAfkingModule`, the affiliate roll + leaderboard sites, the `DegenerusQuests.handlePurchase` double-credit + `awardQuestStreakBonus`, the afking open path, the `Sub` stamp + the EV-cap map, the STAGE placement in `DegenerusGameAdvanceModule`) is grep-verified against `453f8073` with any drift corrected in the SPEC (no "by construction" survives un-checked).

**Plans**: 2 plans (Wave 1: 353-01 SPEC core authoring [AFF-01/AFF-02 + AGG/TKT/QST/OPEN design feeds + corrected accumulator slot + anchor attestation + threat-model re-attestation]; Wave 2: 353-02 XMODEL-01 cross-model design-input pass [codex + gemini × 5 concerns] + disposition fold-in + SPEC lock)

Plans:

- [x] 353-01-PLAN.md — Author the SPEC core: lock AFF-01/AFF-02 + fold the AGG/TKT/QST/OPEN design feeds + the corrected new-dedicated accumulator slot + the anchor attestation table + the unmanipulable/solvency/freeze re-attestation
- [x] 353-02-PLAN.md — Run the XMODEL-01 cross-model design-input pass (codex + gemini, 5 bespoke per-concern prompts), fold the disposition table into the SPEC, and flip the SPEC lock

**UI hint**: no

### Phase 354: IMPL — The ONE Carefully-Sequenced Batched Contract Diff (aggregator + ticket-mode primitive + DegenerusQuests batched-settle + accumulator + open-end)

**Goal**: The v56.0 mechanism lands as a single reconciled `contracts/*.sol` diff under the SPEC's settled shapes, authored producer-before-consumer (accumulator storage → the aggregator accrue/settle in the STAGE → the `DegenerusQuests` batched-settle entrypoint → the ticket-mode minimal-write primitive → the open-end optimizations → the interface/wiring): the per-sub accumulator appended/packed into `DegenerusGameStorage`; the mode-agnostic aggregator (accrue cheap per-buy with NO cross-contract calls — flat-7% `affiliateBase` + `questProgress` running balances + the `buyerOwedBurnie` 10%/20% ticket buyer-bonus on ticket subs + the AFFILIATE leg settled by PULL via `claim(address[] subs)`/`withdraw` with the fixed 75/20/5 split [no roll, no seed, no scheduled/mutation affiliate flush] + the QUEST leg settled by RIDING THE DAILY BUY STAGE on the global settle day [amended 2026-06-01 — the separate `mintBurnie` "settlement-due" router leg REJECTED as a redundant cold-SLOAD pass; the inline `_settleQuest` mints `questProgress × QUEST_SLOT0_REWARD` + `buyerOwedBurnie` in ONE `creditFlip`; `SUB_STAGE_BATCH` SHRUNK so the settle-day chunk fits 16.7M, deferred to 355] + a quest-settle-on-unsub + the uniform mode-agnostic quest-settle for BOTH ticket + lootbox subs + the self-marking running balances [`windowStartDay`/`lastSettledDay` double-settle markers DROPPED]); the ticket-mode minimal-write primitive (the custom "write the ticket entries to the queue" fn replacing the per-day `purchaseWith` heavyweight, the inline affiliate/quest deferred to the aggregator, producing resolution-equivalent ticket entries with the century/x00 parity decision applied + ACCRUING the 10%/20% ticket buyer-bonus `:1655-1659` into `buyerOwedBurnie` at live parity [closing the v55-style dropped-bonus regression]); the `DegenerusQuests` batched-settle entrypoint + the streak machinery (amended 2026-06-01 — quest settle rides the buy STAGE; slot-0 BURNIE + the 10%/20% ticket buyer-bonus mint together; first-sub-only +daysToNextSettle streak, no provisional) (the slot-0 `questProgress` delivered-day counter → settle-mint `× QUEST_SLOT0_REWARD` [+ the accrued `buyerOwedBurnie` ticket bonus] via the STAGE-riding settle on the global settle day / `claimQuest` fallback, the first-sub-only `+daysToNextSettle` head-start [bounded +0..+9, no provisional] with the activity-score reading the actual `state.streak` and the per-window streak advancing only on debit-DELIVERED days, the `lastCompletedDay`/`afkCoveredThroughDay` double-credit guard + active-pass anti-reset with slot rewards never suppressed, proven non-perturbing to the manual/bingo/degenerette/boon callers, and the O1 lootbox-quest double-credit confirmed-intended-or-fixed); and the open-end optimizations (the `_openAfkingBox`→`resolveAfkingBox` + `mintBurnie`/`autoOpen`/`OPEN_BATCH` path optimized for max gas with the cheapest viable shared materialization + no cold ledger, the open kept COMPLETELY unmanipulable — live-level parity, no double-open via `lastOpenedDay` monotone, no EV-cap double-draw on the shared `(player,level)` budget, no shared-mutable-state hazard with the human route) — applied to `contracts/` and locally compiling (`forge build` clean), then HELD at the contract-commit boundary for explicit user hand-review.
**Type**: IMPL (CONTRACT BOUNDARY — the ONE carefully-sequenced batched USER-APPROVED `contracts/*.sol` diff; `autonomous: false` at the commit gate; never auto-commit contracts)
**Depends on**: Phase 353 (the SPEC must lock the aggregator + the accumulator layout + the affiliate accrue/settle split + the ticket-mode primitive shape + the `DegenerusQuests` batched-settle entrypoint + the non-perturbation approach + the ±10-streak derivation + the open-end review + the unmanipulable/solvency re-attestation, fold in the XMODEL design-input suggestions, and grep-attest the edit-order first)
**Requirements**: AGG-01, AGG-02, AGG-03, AGG-04, AGG-05, TKT-01, TKT-02, QST-01, QST-02, QST-03, QST-04, QST-05, OPEN-01, OPEN-02
**Success Criteria** (what must be TRUE):

  1. The mode-agnostic aggregator accrues cheap + settles per-leg (AGG-01 / AGG-02 / AGG-03 / AGG-04 / AGG-05 — amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic) — per buy the STAGE accrues the flat-7% affiliate base + quest progress (+ the `buyerOwedBurnie` 10%/20% ticket buyer-bonus on ticket subs) into the per-sub accumulator running balances with NO cross-contract calls (the cheap hot path, replacing the per-buy `handlePurchase`/`payAffiliate`/`creditFlip` storm) (AGG-01); the AFFILIATE leg is PULL (no scheduled flush) — the accrued `affiliateBase` persists for the uplines to settle via `claim`/`withdraw` (the fixed 75/20/5 split, no roll, no seed) — while AGG-02 SURVIVES carrying the QUEST leg only: the quest settle RIDES THE DAILY BUY STAGE on the global settle day (`currentDay % settlePeriod == 0`, ~10-day cadence — amended 2026-06-01: the separate `mintBurnie` "settlement-due" router leg is REJECTED as a redundant cold-SLOAD pass; the inline `_settleQuest` mints the sub's accrued quest BURNIE `questProgress × QUEST_SLOT0_REWARD` + the accrued `buyerOwedBurnie` ticket bonus in ONE `creditFlip` + applies the ±10 streak, draining both counters once per epoch; `SUB_STAGE_BATCH` SHRUNK so the settle-day chunk fits 16.7M, number deferred to 355) (quests stay AUTOMATIC — the sub's own reward) (AGG-02); any player-triggered sub mutation that ends the sub (unsub) triggers a lightweight QUEST-settle draining `questProgress` BEFORE applying the change — the affiliate base is NOT flushed on mutation, it persists for the uplines to PULL (an unsub does not forfeit the uplines' accrued affiliate) (AGG-03); the QUEST-settle path settles uniformly for BOTH ticket + lootbox subs (mode-agnostic — `questProgress` mode-independent; the affiliate base is likewise mode-agnostic and pulled uniformly via `claim`) (AGG-04); and double-settle is impossible via self-marking running balances — the affiliate `claim` zeroes `affiliateBase[sub]` and the quest flush drains `questProgress` (both a no-op on a double-fire), so the per-sub `windowStartDay`/`lastSettledDay` double-settle markers are DROPPED (AGG-05).

  2. The ticket-mode minimal-write primitive lands at parity (TKT-01 / TKT-02 — amended 2026-06-01 — the primitive accrues the 10%/20% ticket buyer-bonus, settled with the quest) — afking ticket subs use a custom minimal function that writes the ticket entries to the queue (mirrors the lootbox box-stamp) AND ACCRUES the 10%/20% ticket buyer-bonus (`DegenerusGameMintModule.sol:1655-1659` — flat 10% of the BURNIE-equivalent ticket spend, doubling to 20% for ≥10-ticket buys) into the per-sub `buyerOwedBurnie` field, and the per-day `MintModule.purchaseWith` heavyweight + its inline affiliate/quest are removed from the per-buy path (deferred to the AGG aggregator) — closing the v55-style regression where the bypass-`_callTicketPurchase` primitive silently DROPPED the buyer-bonus (TKT-01); the custom ticket-write produces the same queued ticket entries as `purchaseWith`'s ticket leg (resolution-equivalent placement/trait/quantity), the afking-ticket century / x00 quantity-bonus parity (`MintModule:1243`) keep-or-drop decision from the SPEC is applied, and the 10%/20% buyer-bonus is at PARITY with live (minus the affiliate-kickback leg), minted to the sub TOGETHER WITH the slot-0 quest reward at the quest STAGE settle [one `creditFlip`], ticket-mode-specific (lootbox subs use the boon) and riding the quest PUSH not the affiliate PULL (TKT-02).

  3. The quest batching lands non-perturbing with no pre-credit-EV inflation + no double-credit (QST-01 / QST-02 / QST-03 / QST-04 / QST-05 — amended 2026-06-01 — automatic slot-0 BURNIE via mintBurnie chain + claimQuest fallback; first-sub-only +daysToNextSettle streak, no provisional) — the afking quest streak uses a SIMPLIFIED first-sub-only `+daysToNextSettle` head-start (`hasEverSubscribed` 1-bit; bounded +0..+9 over the manual baseline, no provisional/vesting) on the ±10-per-window activity model (−10 on unsub), the slot-0 reward accrues as a delivered-day `questProgress` COUNTER → settled = mint `× QUEST_SLOT0_REWARD` (the ONLY direct quest BURNIE) via the `mintBurnie` settlement-due chain + the `claimQuest(subs[])` fallback + quest-settle-on-unsub, slot-1 remains the player's own manual quest, the ±10 streak is the activity-score multiplier NOT direct BURNIE (QST-01); the first-sub-only `+daysToNextSettle` head-start is a bounded (+0..+9, once/account) DIRECT streak grant that is USER-ACCEPTED-BY-DESIGN — the prior "read confirmed-delivered, never the +10 pre-credit" escrow guard is SIMPLIFIED AWAY for the afking grant (the bound REPLACES the escrow; a deliberate accepted tradeoff, NOT a missed control), the activity-score still reads the actual `state.streak`, and the per-window streak still advances only on debit-DELIVERED days (the C3-a streak-dodge fix stays in force) (QST-02); the afk+manual double-credit guard (`lastCompletedDay`/`afkCoveredThroughDay`) prevents double streak credit on afk-covered days, the gap-reset is suppressed via an active-pass check (anti-reset without daily writes), and slot rewards are NEVER suppressed (only the duplicate streak credit) (QST-03); the batched-settle entrypoint added to the shared `DegenerusQuests` core is non-perturbing to the manual / bingo / degenerette / boon callers (`awardQuestStreakBonus` etc.) (QST-04); and the pre-existing O1 lootbox-quest BURNIE double-credit (`handlePurchase` internal `creditFlip` + the returned-and-re-credited value) is confirmed intended or fixed (QST-05).

  4. The afking open-end is optimized + stays completely unmanipulable (OPEN-01 / OPEN-02) — the afking open path (`_openAfkingBox` → `resolveAfkingBox` + the `mintBurnie` open leg / the `autoOpen` cursor + `OPEN_BATCH`) is optimized for maximum gas efficiency (the per-open marginal + the batch cost), reading no cold ledger and sharing the cheapest viable materialization with the human path (OPEN-01); and the afking open stays COMPLETELY unmanipulable under the v56 changes — the live-level open = parity with human `openLootBox` (open is permissionless + bounty-driven, never player-timed → no tier-timing edge), no double-open (`lastOpenedDay` monotone), no EV-cap double-draw (the shared per-`(player,level)` budget), no shared-mutable-state hazard with the human route — ALL re-verified after the accrual/settle refactor (OPEN-02).

  5. The diff is producer-before-consumer + `forge build` is clean + HELD at the boundary (all 14 IMPL reqs) — the whole diff is authored producer-before-consumer per the SPEC edit-order map (accumulator storage → aggregator accrue/settle → `DegenerusQuests` batched-settle → ticket-mode primitive → open-end → interfaces/wiring), applied to `contracts/` and locally compiling (`forge build` clean; `ContractAddresses.sol` freely modifiable), but NOT committed without explicit user hand-review of the single batched diff (the SOLVENCY-01 ETH/`claimablePool` path stays byte-unchanged — affiliate/quest are BURNIE flip-credit only — and RNG-freeze stays intact, both carried as IMPL invariants and re-proven at TST 356).

**Plans**: 6 plans in 4 waves (producer-before-consumer)

Plans:
**Wave 1**

- [x] 354-01-PLAN.md — Wave 1: re-pack the Sub struct + the in-slot accumulator (affiliateBase/questProgress/buyerOwedBurnie + hasEverSubscribed; drop window/settled markers; amount->milli-ETH) (AGG-05)
- [x] 354-02-PLAN.md — Wave 1: DegenerusQuests core — the onlyGame batched-settle entrypoint (non-perturbing) + O1 line-890 double-credit fix + dead handleLootBox removal (QST-01..05)

**Wave 2** *(blocked on Wave 1 completion)*

- [x] 354-03-PLAN.md — Wave 2: GameAfkingModule mode-agnostic accrue + inline _settleQuest (rides the STAGE) + claimQuest fallback + unsub-settle + first-sub head-start; SOLVENCY-01 byte-unchanged (AGG-01..05, QST-01/02/03)

**Wave 3** *(blocked on Wave 2 completion)*

- [x] 354-04-PLAN.md — Wave 2: DegenerusAffiliate flat-7% deterministic-split PULL — claim(subs[]) 75/20/5 + CEI withdraw() + pendingClaim; no roll/seed (AGG-01/04/05)

**Wave 4** *(blocked on Wave 3 completion)*

- [x] 354-05-PLAN.md — Wave 3: ticket minimal-write primitive + buyerOwedBurnie 10%/20% accrual + century parity (boons-OFF) + the open-end re-verification + wiring (TKT-01/02, OPEN-01/02)

**Wave 5** *(blocked on Wave 4 completion)*

- [x] 354-06-PLAN.md — Wave 4: forge-build-clean + the per-requirement diff-review + the autonomous:false USER hand-review-and-commit gate (the ONE v56 contract boundary)

**UI hint**: no

### Phase 355: GAS — Measure + Tune (per-buy / per-open / per-settle marginals + accumulator packing + SUB_STAGE_BATCH re-tune + mode/SLOAD collapse)

**Goal**: Beyond the structural reduction the IMPL refactor already delivers (the per-buy cross-contract storm collapsed into the cheap accrue + the deferred settle), the everyday cost gets measured + tuned under the security-over-gas floor (gas-scavenger surfaces, gas-skeptic validates — each tune must stay unmanipulable; invariant-trading or not-real wins REJECTED with reasoning, not re-litigated): the per-buy marginal is measurably reduced (lootbox ~206k → ~130–140k; ticket off the ~262k `purchaseWith` heavyweight) and measured per-buy + per-settle under the 16.7M HARD per-tx ceiling (GAS-01); the per-sub accumulator packs into the `Sub` slot's spare bits where feasible with no new cold per-buy SSTORE (GAS-02); `SUB_STAGE_BATCH` is re-tuned for the lower per-sub cost (throughput/headroom, the per-day STAGE staying under the 16.7M ceiling at the `SUBSCRIBER_CAP`) (GAS-03); and redundant payment-mode branches / repeated SLOADs in the STAGE are collapsed where a slight simplification is cheaper (GAS-04, allowed under the scope latitude). Any net contract change rides the batched USER-APPROVED diff held at the contract-commit boundary; if the savings are structural to the IMPL refactor (the v55 Outcome-A precedent), that is recorded as the outcome.

> **RE-SCOPED 2026-06-01 (USER override, mid-355-execute — three directives; see [[v56-deferred-quest-payout-two-batch-redesign]] / `355-CONTEXT.md`).** Phase 355 is no longer a pure constant-tune (Outcome-A is no longer the likely outcome) — it now ALSO lands a SMALL IMPL change in the same batched USER-APPROVED diff at the 355-03 boundary: **(a) DEFERRED PAYOUT (GAS-05)** — `_settleQuest` keeps advancing the `settleAfkingQuest` streak on the ~10-day cadence and draining the counters, but the per-sub quest+buyer-bonus `creditFlip` is REPLACED by a `pendingBurnie` accumulator paid via a NEW permissionless claim entrypoint (the keeper `mintBurnie` bounty stays an immediate push; auto-run only; off the solvency path). **(b) TWO STAGE BATCH SIZES (GAS-03 amended)** — split `SUB_STAGE_BATCH` into `SUB_STAGE_BATCH_NORMAL` (large, the cheap ~9/10 days) + `SUB_STAGE_BATCH_SETTLE` (smaller, the settle day with per-sub `settleAfkingQuest`), the STAGE chunker branching on `currentDay % SETTLE_PERIOD == 0`; each sized from its day's measured marginal to <10M target / ≤16.7M ceiling; `OPEN_BATCH` stays single. The harness measures the `creditFlip` component cost so the post-swap settle marginal + both batch sizes are derivable, landing as ONE batched diff (swap + claim + two-batch branch + computed constants). The committed 355-01 Task-1 harness `3b9df3fb` is the pre-change before-baseline. **Plan list below is SUPERSEDED — re-planned 2026-06-01.**

**Type**: GAS (CONTRACT BOUNDARY — any net `contracts/*.sol` change rides the batched USER-APPROVED diff; `autonomous: false` at the commit gate if a diff is produced)
**Depends on**: Phase 354 (the gas pass operates on the post-IMPL surface — the accumulator + the aggregator accrue/settle loop + the ticket-mode primitive + the open-end the IMPL established; the marginals are measured on the applied tree, not SPEC placeholders)
**Requirements**: GAS-01, GAS-02, GAS-03, GAS-04, GAS-05
**Success Criteria** (what must be TRUE):

  1. The per-buy + per-settle marginals are measured + reduced (GAS-01) — the per-buy marginal is measurably reduced (target: lootbox ~206k → ~130–140k; ticket off the ~262k `purchaseWith` heavyweight), measured per-buy + per-settle marginal via a TST-06-style harness under the 16.7M HARD per-tx ceiling, proven behavior-identical-where-claimed (the accrue + deferred-settle shape produces the same eventual affiliate/quest credit as the per-buy storm did, modulo the accepted settle-timing simplification).

  2. The accumulator packs with no new cold SSTORE (GAS-02 — amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic) — the SHRUNK per-sub accumulator (the flat-7% `affiliateBase uint32` whole-BURNIE/100M-clamp + `questProgress`, both self-marking running balances; `windowStartDay`/`lastSettledDay` DROPPED) packs into the re-packed `Sub` slot's spare bits (per the SPEC layout), avoiding a new cold per-buy SSTORE; the off-slot `pendingClaim` mapping (affiliate recipients) is touched at `claim`/`withdraw`, NEVER per-buy; if a field genuinely cannot pack, that is recorded with reasoning (not forced at the cost of an invariant).

  3. SUB_STAGE_BATCH is re-tuned + the STAGE stays under the ceiling (GAS-03) — `SUB_STAGE_BATCH` is re-tuned for the lower per-sub cost (raise the batch for more subs/advance under 16.7M, or keep it with large headroom), and the per-day STAGE is proven to stay under the 16.7M HARD per-tx ceiling at the `SUBSCRIBER_CAP` (the binding ticket-mode case included).

  4. The mode/SLOAD collapse lands + any net diff is held at the boundary (GAS-04 / GAS-01..03) — redundant payment-mode branches / repeated SLOADs in the STAGE are collapsed where a slight simplification is cheaper (allowed under the scope latitude, proven unmanipulable); and any `contracts/*.sol` change from this phase rides the batched USER-APPROVED diff, applied + locally compiling (`forge build` clean), HELD at the contract-commit boundary for explicit user hand-review (never auto-committed) — or, if the savings are structural to the IMPL refactor and no net tune lands (the v55 Outcome-A precedent), that is recorded as the outcome and no separate diff is gated.

  5. The deferred payout + two-batch split land unmanipulable (GAS-05 / GAS-03 — USER override 2026-06-01) — `_settleQuest` still advances the `settleAfkingQuest` streak on the ~10-day cadence + drains `questProgress`/`buyerOwedBurnie`, but accrues the owed quest+buyer-bonus BURNIE into `pendingBurnie` (the already-warm Sub slot) instead of an inline `creditFlip`; a permissionless claim entrypoint does a final settle then pays `pendingBurnie` in ONE `creditFlip` and zeroes it (always credits the sub, idempotent, off the solvency path — SOLVENCY-01 byte-unchanged, RNG-freeze intact); the keeper `mintBurnie` bounty stays an immediate push; the STAGE chunker uses `SUB_STAGE_BATCH_NORMAL` vs `SUB_STAGE_BATCH_SETTLE` (branch on `currentDay % SETTLE_PERIOD == 0`), each proven <10M target / ≤16.7M ceiling at the cap; the deferral is proven non-gameable (no settle-timing/claim-timing EV — `pendingBurnie` is the sub's own accrued reward, drained atomically).

**Plans**: 3 plans in 3 waves (measure -> scavenge/skeptic-validate -> set SUB_STAGE_BATCH/tune-or-Outcome-A; sequential, each wave consumes the prior's measurements)

Plans:

**Wave 1**

- [ ] 355-01-PLAN.md — Build the v56 gas-marginal harness + measure per-buy (lootbox + ticket) / per-open / per-settle marginals + the worst-case settle-day STAGE chunk at the cap, all under 16.7M; prove GAS-02 no-new-cold-SSTORE (GAS-01/02; autonomous, test-only)

**Wave 2** *(blocked on Wave 1 completion)*

- [ ] 355-02-PLAN.md — gas-scavenger over the STAGE/accrue/settle/open + gas-skeptic verdicts -> the APPROVED/REJECTED candidate ledger (GAS-04 mode/SLOAD collapse + GAS-02 packing finalization) feeding 355-03 (GAS-02/04; autonomous, doc-only)

**Wave 3** *(blocked on Wave 2 completion)*

- [ ] 355-03-PLAN.md — Set SUB_STAGE_BATCH against the measured settle-day chunk + land the skeptic-APPROVED GAS-04 changes (or record Outcome-A), re-prove the STAGE under 16.7M at the cap, HOLD at the contract-commit boundary (GAS-03/04; autonomous:false — the ONE Phase-355 contract gate)

**UI hint**: no

### Phase 356: TST — Unmanipulable (strategic sub/unsub) + Quest-Core Non-Perturbation + Two-Path-Open Coexistence + Gas Marginals + Non-Widening

**Goal**: The milestone is proven behaviorally correct + the hard security floor (the SEC gate) is proven empirically against the v55-baseline tree: the afking system (buy + open) is unmanipulable — no positive-EV vector from settle-timing (no seed — _amended 2026-06-01: the roll is REMOVED under the flat-7% deterministic-split pull_), strategic sub/unsub churn (the USER-flagged PRIMARY concern), re-rate-on-alteration, pre-credit-EV inflation, double-credit, open-timing, or settle-griefing; SOLVENCY-01 is untouched (affiliate/quest remain BURNIE flip-credit OFF the ETH/`claimablePool` path, the ETH/pool debit byte-unchanged, RNG-freeze intact under the new accrual/settle); the shared `DegenerusQuests` core batched-settle entrypoint is non-perturbing to the manual/bingo/degenerette/boon callers; the two open routes coexist with no shared-state hazard; the per-buy + per-open + per-settle marginal gas is measured under the 16.7M HARD per-tx ceiling (the GAS-01..04 wins proven same-results); and the suite is NON-WIDENING vs the v55 baseline `453f8073` (every pre-existing red enumerated BY NAME) — restoring a clean v56.0 regression baseline.
**Type**: TST
**Depends on**: Phase 355 (tests exercise the FINAL applied surface — the live aggregator + ticket-mode primitive + open-end from 354 plus the 355 tune AND the USER-directed liveness adds: the `openBoxes` valve `86a2d6c8` + the gap/jackpot decouple `3d969621` — not SPEC placeholders or an intermediate pre-gas state)
**Requirements**: SEC-01, SEC-02, LIVE-01, GAS-06 (LIVE-01 + GAS-06 added 2026-06-02 — the 355 liveness adds; see SC 6-7)
**Success Criteria** (what must be TRUE):

  1. The afking system is proven unmanipulable — strategic sub/unsub the PRIMARY probe (SEC-01) — empirical + adversarial proof that there is NO positive-EV vector from settle-timing (no seed — _amended 2026-06-01: the roll is REMOVED under the flat-7% deterministic-split pull_), **strategic sub/unsub churn (churn to re-claim the affiliate [forfeit-nothing-gain-nothing] / dodge a streak penalty / harvest or duplicate a settlement — the USER-flagged primary concern)**, re-rate-on-alteration, pre-credit-EV inflation, double-credit, open-timing, or settle-griefing: a churn loop (subscribe → accrue → unsub → re-subscribe) cannot extract more affiliate/quest value than a steady sub (amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic: the accrued `affiliateBase` is the uplines' money that PERSISTS on unsub — churn forfeits nothing AND gains nothing; there is NO roll to re-roll, NO seed to grind, NO two-distribution free option to arbitrage [exactly ONE deterministic split path], the buyer never receives the affiliate base [`A ≠ sub` guaranteed + the rare U1/U2==sub cycle skip], the flat 7% accrues the same either way), and the self-marking running balances make double-settle impossible (a re-`claim` sees `affiliateBase == 0`; a double quest flush finds `questProgress == 0`). (The 3-skill adversarial economic review + the XMODEL cross-model review at TERMINAL 357 are the final gate; this phase proves the property empirically.)

  2. SOLVENCY-01 untouched + RNG-freeze intact (SEC-02) — the affiliate/quest rewards remain BURNIE flip-credit off the ETH/`claimablePool` path (proven via the ETH/pool debit being byte-unchanged vs `453f8073` — a BURNIE-emission-timing change only), and RNG-freeze stays intact under the new accrual/settle (the accrue/settle touches no frozen RNG-window slot; the open-end materialization keeps the live-level parity + the stamped-seed determinism the v55 close proved).

  3. The shared quest-core is proven non-perturbing + the two-path open coexists (SEC-01 corollary; QST-04 / OPEN-02 re-proven) — the `DegenerusQuests` batched-settle entrypoint is proven non-perturbing to the manual / bingo / degenerette / boon callers (`awardQuestStreakBonus` etc. produce byte-identical results with the entrypoint present), and the two open routes (afking-stamp vs human `lootboxEth`/`boxPlayers`) coexist with no shared-state hazard + no double-open (the afking open re-verified equivalent to human `openLootBox` at the live level, `lastOpenedDay` monotone, the EV-cap budget shared exactly-once).

  4. The gas marginals are measured under the ceiling (GAS-01..04 re-proven same-results) — the per-buy + per-open + per-settle marginal gas is measured under the 16.7M HARD per-tx ceiling (a TST-06-style harness), with the GAS-01..04 wins (the per-buy reduction, the accumulator packing, the `SUB_STAGE_BATCH` re-tune, the mode/SLOAD collapse) proven same-results (or same-eventual-credit where the accepted settle-timing simplification applies).

  5. The NON-WIDENING regression is proven (SEC corollary) — the reconceived afking/quest/affiliate suite compiles + passes against the v56 surface with net-zero new regression vs the v55 baseline `453f8073` (every pre-existing red enumerated BY NAME, `REGRESSION-BASELINE-v56.md`), absorbing any test renames / oracle migrations from the aggregator + ticket-mode refactor; ZERO contract mutation during the TST phase (the subject stays frozen at the IMPL+GAS HEAD). INCLUDES the 355-deferred stale-Sub-offset fuzz-suite migration (`OFF_LASTBOUGHT=21→11`/uint24, ~10 `test/fuzz/` files — the pre-existing `6555125 != 3774873600` reds, baseline-confirmed NOT from the v56 contract changes) as the NON-WIDENING precondition.

  6. The `openBoxes` liveness valve is proven (LIVE-01, NEW from 355 — commit `86a2d6c8`) — the unified permissionless `openBoxes(maxCount)` (AFKING boxes first, then human, uncapped, unrewarded) clears any backlog of either box type in caller-sized chunks proven < 16.7M per tx (each box uniform O(1)); both persistent cursors advance so repeated bounded calls drain any state; the individual `openLootBox(player,index)` + the rewarded `mintBurnie` open path are byte-unchanged; `drainAfkingBoxes` reachable only via the `openBoxes` delegatecall (no autoOpen selector collision).

  7. The gap-backfill / daily-jackpot decouple is proven (GAS-06, NEW from 355 — commit `3d969621`; closes the Codex-found protocol-forced 16.7M composition breach) — after a multi-day VRF-stall gap backfill, `advanceGame` breaks at `STAGE_GAP_BACKFILLED` (the day's jackpot is NOT paid that tx) and the NEXT advance pays it with the same frozen word (idempotent resume: `rngGate` returns `gapDays==0`, `dailyIdx` not advanced so `advanceDue()` stays true, `purchaseStartDay` bumped once). PROVE EACH `advanceGame` call in the gap-resume is < 16.7M (the existing test bounds only the 25M total resume — write a per-advance harness) + a `gap→defer→next-pays` regression test.

**Plans**: 7 plans in 3 waves (Wave 0: fixture-sanity gate + the D-10 offset migration [the NON-WIDENING precondition]; Wave 1: the SEC-01/SEC-02/QST-04 fuzz+repro proofs + the gas/LIVE/GAS-06 EXTEND; Wave 2: the BY-NAME NON-WIDENING ledger)

- [x] 356-01-PLAN.md — Wave-0 fixture-sanity gate + migrate the 7 read-only keeper/afking fuzz files (OFF_LASTBOUGHT 21->11/uint24) [SEC-01]
- [x] 356-02-PLAN.md — Wave-0 migrate the 3 v55-proof files (offsets + _setStamp/_setScorePlus1 write-masks in lockstep; the SEC ADAPT-source templates) [SEC-01, SEC-02]
- [x] 356-03-PLAN.md — Wave-1 SEC-01 V56SecUnmanipulable: churn-fuzz invariants + the 4 named repros (affiliate re-claim churn / streak decay-gap dodge / pendingBurnie double-claim CEI / 4 finalize hooks before delete) [SEC-01] ✅ 2026-06-02 (11/11 green incl. 1000-run churn fuzz; affiliateBase persists byte-identical across unsub+re-sub; decay-on-read + funding-kill guard zero the streak; CEI pays once; 4 finalize hooks all finalize-before-delete; no-orphan holds; ZERO contract mutation; commits cad9c48f + e8b68c89; threat_flag: drainAffiliateBase has no DegenerusGame dispatch stub → 357)
- [x] 356-04-PLAN.md — Wave-1 SEC-02 V56FreezeSolvency: solvency invariant fuzz + RNG-freeze determinism + the SOLVENCY-01 debit byte-diff anchor [SEC-02]
- [x] 356-05-PLAN.md — Wave-1 QST-04 V56QuestNonPerturb: slot-1 streak-neutral during afking + cross-caller (awardQuestStreakBonus/bingo/degenerette/boon) byte-identity [SEC-01] ✅ 2026-06-02 (7/7 green; afkingActive gates the streak bump — slot-0/slot-1 completion streak-neutral during afking yet slot-1 fully accessible paying its 200-BURNIE reward, NON-afking advances +1 [C3-a dodge stays closed]; two-world snapshot/revert byte-identity on the full PlayerQuestState word with afking siblings present vs absent [incl. a mid-finalize sibling]; O1 day-4 LOOTBOX single-credit holds [reward returned once, NOT internally creditFlipped]; drove the quest core directly via GAME/COIN access gates + slot-2 vm.load reads; ZERO contract mutation; commits b25c7e33 + ad3fbbcd)
- [x] 356-06-PLAN.md — Wave-1 LIVE-01 + GAS-06 EXTEND V56AfkingGasMarginal: SUBSCRIBER_CAP fix 500->1000 + per-tx gap-resume <16.7M ceiling + the decouple idempotent-resume + the openBoxes valve + the 4 residuals + the GAS-01..04 regression locks [LIVE-01, GAS-06] ✅ 2026-06-02 (15/15 green; gap-backfill advance N ~6.85M + deferred-jackpot N+1 SEPARATE tx; mixed-day OPEN_BATCH chunk ~10.35M; ZERO contract mutation; commits c6b087dc + f3cda660)
- [x] 356-07-PLAN.md — Wave-2 REGRESSION-BASELINE-v56.md: the BY-NAME NON-WIDENING ledger (empirical 453f8073 checkout + the subset gate live-union==empty + the D-10 narrowing) [SEC-01, SEC-02, LIVE-01, GAS-06] ✅ 2026-06-02 (435-line ledger, commit `97fac47b`; baseline established EMPIRICALLY via the byte-identical-contracts commit `83a6a9ca` [the raw 453f8073 corpus is UNCOMPILABLE — AfKing.sol deleted but DeployProtocol+5 files still deploy it at a load-bearing nonce; `git diff 453f8073 83a6a9ca -- contracts/` EMPTY] → reproduced the v55 TST-HEAD 603/134/16 = the 453f8073 red union; live v56 HEAD 624/134/30, the live 134 BYTE-IDENTICAL to the baseline 134 BY NAME — `live − union == ∅` AND `union − live == ∅`; the EXPANDED MANDATE's 14 migration-unmasked v56-behavior reds RESOLVED via vm.skip-with-reason DROP, commit `f23b010e` [milli-ETH packing / 0.01-ETH subscribe min-buy / `E()`-vs-Panic(0x11) / unified openBoxes valve / dropped autoOpen — each a stale v55 assertion the audited v56 diff superseded, each re-proven GREEN by V56SecUnmanipulable/V56FreezeSolvency/V56AfkingGasMarginal]; D-10 offset-migration red→green NARROWING + the SOLVENCY-01 leg-1 byte-diff anchor [GameAfkingModule 709-710↔663-664 byte-identical] recorded; the unseeded DegeneretteBet.inv ⊆ relaxation carried; Hardhat compile-sanity arm EXIT 0; drainAffiliateBase dispatch-stub reachability SURFACED as a carried 357 finding; ZERO contracts/*.sol mutation)

**UI hint**: no

### Phase 357: TERMINAL — Delta Audit + 3-Skill Genuine-PARALLEL Adversarial Sweep (AUGMENTED by XMODEL Codex + Gemini close) + FINDINGS-v56.0 + Closure Flip

**Goal**: The v56.0 audit subject (the carefully-sequenced batched diff — the aggregator + the ticket-mode primitive + the open-end optimizations + the `DegenerusQuests` batched-settle, FROZEN at the IMPL+GAS HEAD) is closed via a FULL close that runs its own internal sweep IN-MILESTONE (NOT deferred to v52, like v54.0/v55.0 and unlike v50.0/v51.0): a delta-audit confirms every v56 surface is NON-WIDENING vs the v55 HEAD `453f8073` with SOLVENCY-01 byte-unchanged + RNG-freeze intact + the affiliate deterministic-split-PULL non-gameability (NO roll/seed — _amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic_) + the open-end two-path/no-double-open + the shared-quest-core non-perturbation re-attested; the 3-skill genuine-PARALLEL adversarial economic review (`/contract-auditor` + `/economic-analyst` + `/zero-day-hunter`; `/degen-skeptic` = the dual-gate filter) probes the strategic sub/unsub edge + the settle-timing non-exploitability (no seed — _amended 2026-06-01: the roll/seed is REMOVED under the flat-7% deterministic-split pull_) + the pre-credit-EV inflation + the two-path open, AUGMENTED by the XMODEL-01 cross-model close (Codex + Gemini fed crafted prompts on the FULL afking system buy+open); `audit/FINDINGS-v56.0.md` (chmod 444) is authored; and the atomic 5-doc closure flip (ROADMAP + STATE + MILESTONES + PROJECT + REQUIREMENTS) is applied with the `MILESTONE_V56_AT_HEAD_<sha>` closure signal.
**Type**: TERMINAL (FULL close — delta-audit + internal 3-skill adversarial sweep AUGMENTED by the XMODEL cross-model close + FINDINGS + closure flip; NOT deferred)
**Depends on**: Phase 356 (the subject must be implemented + gas-tuned + test-proven — incl. the unmanipulable + non-perturbation + two-path + NON-WIDENING proofs — before the requirements are re-attested at closure and the adversarial sweep runs on the proven surface)
**Requirements**: AUDIT-01
**Success Criteria** (what must be TRUE):

  1. The delta-audit + the internal 3-skill adversarial sweep (AUGMENTED by XMODEL) are run IN-MILESTONE (AUDIT-01 + XMODEL-01 close half) — the delta-audit confirms every v56 surface (the accumulator append + the aggregator accrue/settle + the ticket-mode primitive + the `DegenerusQuests` batched-settle entrypoint + the open-end edits) is NON-WIDENING vs the v55 HEAD `453f8073` with zero orphan hunks, SOLVENCY-01 byte-unchanged (the ETH/`claimablePool` debit untouched) + RNG-freeze intact + the affiliate deterministic-split-PULL non-gameability (the flat-7% accrue + the fixed 75/20/5 split at `claim`/`withdraw`, NO roll/seed/flush, buyer-never-wins — _amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic: SUPERSEDES the XMODEL roll-unification, the roll is REMOVED so the C1/C2 free-option finding is MOOT_) + the open-end two-path/no-double-open + the shared-`DegenerusQuests`-core non-perturbation re-attested; the 3-skill genuine-PARALLEL adversarial economic review (`/contract-auditor` + `/economic-analyst` + `/zero-day-hunter` run as concurrent background Task spawns from the orchestrator; `/degen-skeptic` = the dual-gate filter per `D-271-ADVERSARIAL-02`) probes the strategic sub/unsub edge (churn to re-claim / forfeit-nothing-gain-nothing / dodge a streak penalty / harvest or duplicate a settlement), the settle-timing non-exploitability (trivial — no seed), the pre-credit-EV inflation, and the two-path open, with each charged probe dispositioned NEGATIVE-VERIFIED / SAFE_BY_DESIGN / FINDING_CANDIDATE; and the XMODEL-01 cross-model close (Codex + Gemini fed crafted prompts on the FULL afking system buy+open) AUGMENTS the Claude sweep (its disposition folded into the adversarial log).

  2. The findings deliverable is authored + all 24 v56.0 requirements re-attested (AUDIT-01) — `audit/FINDINGS-v56.0.md` (the full multi-section report, chmod 444) is authored capturing the delta-audit + the adversarial disposition (Claude 3-skill + the XMODEL cross-model augmentation) + any findings, and all 24 v56.0 requirements (AGG-01..05 · TKT-01/02 · AFF-01/02 · QST-01..05 · OPEN-01/02 · GAS-01..04 · SEC-01/02 · XMODEL-01 · AUDIT-01) are confirmed satisfied against the frozen v56.0 closure HEAD.

  3. The closure flip is applied (AUDIT-01) — the `MILESTONE_V56_AT_HEAD_<sha>` closure signal is emitted and propagated verbatim, and the atomic 5-doc closure flip (ROADMAP + STATE + MILESTONES + PROJECT + REQUIREMENTS) is applied; the closure plan is a single blocking USER closure-verdict + signal-format approval gate (`autonomous: false`) — the auto-advance is HELD at the closure boundary per `feedback_pause_at_contract_phase_boundaries`. (The separate v52 consolidated cross-model audit still folds the v56 surface into its cumulative sweep — an additional track, recorded in the v52 charge, NOT a substitute for this in-milestone close.)

**Plans**: 6 plans in 4 waves (Wave 1: the leading contract gate + the D-14 reconciliation; Wave 2: the delta-audit ∥ the 3-skill genuine-PARALLEL sweep [+ best-effort XMODEL], both READ-ONLY @ HEAD'; Wave 3: FINDINGS-v56.0; Wave 4: the closure flip + the close-then-prompt-to-push offer). TWO `autonomous: false` gates (357-00 contract gate + 357-04 closure gate) — unlike every prior TERMINAL's single closure gate.

Plans:
**Wave 1**

- [x] 357-00-PLAN.md — Wave 1 (autonomous:false CONTRACT GATE): the THREE bundled contract changes — F-356-01 drainAffiliateBase dispatch stub + D-11 pass-required subscribe + D-12 purchase-grounded subscribe (D-13 VAULT/SDGNRS exempt) — forge build clean, HELD for USER hand-review, committed as the SOLE .sol commit (HEAD') [AUDIT-01]
- [x] 357-00b-PLAN.md — Wave 1 (autonomous): the D-14 NON-WIDENING reconciliation — re-run forge test, drop the D-11/D-12 supersession reds (vm.skip + re-prove via the new V56SubHardening positive proofs), record the F-356-01 narrowing, re-freeze the subject [AUDIT-01] ✅ COMPLETE 2026-06-03 (**EXTENDED to the SECOND contract gate HEAD'' = `61315ecd`** — the advance-incentive redesign: `advanceGame` PURE LIVENESS [`MustMintToday` removed], the must-mint ladder → SOFT pay-gate `_bountyEligible` / `bountyEligible(addr)` view, `mintBurnie` pays the advance bounty only when `mult>0 && eligible`, Vault+sDGNRS→`mintBurnie`). 3 commits `056e78c8`(GovernanceGating GATE-01..04 rewritten to the soft pay-gate model, 6/6 GREEN — `MustMintToday` was a Hardhat-only consumer)+`1d5fd872`(V56SubHardening advance-soft-gate proofs 11→17 GREEN: advanceGame liveness + bountyEligible truth table + mintBurnie eligible/ineligible pay + Vault/sDGNRS routing)+`48fab561`(ledger §9: 567/133/99, `live − union == ∅` HOLDS — 133 ⊆ the §2 134-name 453f8073 union, empty set-diff; the 134→133 narrowing = run-variance in the non-deterministic Bucket A/F + vm.assume-exhaustion cluster; SOLVENCY-01 leg-1 byte-anchor held). The HEAD'-only 357-00b commits `ae7dfa26`+`9d32abec` are superseded by the HEAD'' §9. ZERO contracts/*.sol mutation; `git diff 61315ecd HEAD -- contracts/` EMPTY — subject re-frozen at **HEAD'' = `61315ecd`** (downstream waves now re-freeze against HEAD'', not HEAD').

**Wave 2** *(blocked on Wave 1 completion)*

- [x] 357-01-PLAN.md — Wave 2 (autonomous, READ-ONLY @ **HEAD'' `61315ecd`**, ∥ 357-02): the delta-audit — every v56 surface NON-WIDENING vs 453f8073 (zero orphan hunks; SOLVENCY-01 byte-unchanged + RNG-freeze + affiliate flat-7% deterministic-split-PULL non-gameability + open two-path + LIVE-01 + GAS-06 + shared-quest non-perturbation + the 357-00 hardening attributed) + the regression live − union == ∅ BY NAME [AUDIT-01] ✅ COMPLETE 2026-06-03 (`357-01-DELTA-AUDIT.md`, commit `f7d0ca52`). **Enumerated the 453f8073→HEAD'' delta = 15 files / +1565−803** (re-derived; the redesign added `DegenerusGameMintStreakUtils.sol` + reshaped AdvanceModule/Game). **The obsolete `5cb707f2` bypass framing is SUPERSEDED by the advance-incentive redesign attestation** — the gate it bypassed (`_enforceDailyMintGate` + `MustMintToday`) was DELETED ENTIRELY (grep-ZERO @ HEAD''); `advanceGame` is pure liveness; the must-mint ladder → the non-reverting SOFT pay-gate `_bountyEligible` (MONOTONE, BURNIE-bounty-only, off the ETH path). SOLVENCY-01 leg-1 debit byte-identical (453f8073:709-710 ↔ HEAD'':690-691); RNG-freeze intact (premature-advance touches no frozen window slot). `git diff 61315ecd HEAD -- contracts/` EMPTY.
- [x] 357-02-PLAN.md — Wave 2 (autonomous, READ-ONLY @ **HEAD'' `61315ecd`**, ∥ 357-01): the 3-skill genuine-PARALLEL adversarial sweep (run INLINE from the orchestrator context per D-06 — recorded execution path **PARALLEL_SUBAGENT**) + best-effort XMODEL augmentation — the strategic sub/unsub edge (PRIMARY) + settle-timing + pre-credit-EV + two-path open + the D-11/D-12 hardened gates + the advance-incentive redesign (the obsolete `5cb707f2` bypass framing SUPERSEDED — the gate was DELETED) + the drainAffiliateBase stub + the affiliate claim CEI; /degen-skeptic dual-gate filter [AUDIT-01] ✅ COMPLETE 2026-06-03 (`357-02-ADVERSARIAL-LOG.md`, commit `851409a4`). **0 FINDING_CANDIDATE** across all 3 Claude skills (`/contract-auditor` 12 + `/economic-analyst` 10 + `/zero-day-hunter` 10 = 32 charged probes: 18 NEGATIVE-VERIFIED + 13 SAFE_BY_DESIGN + 1 ADVISORY) + the Codex XMODEL close (4-area NO ISSUE corroboration; Gemini attempted-partial per D-03). The ONLY new finding = **F-356-01 RESOLVED-AT-357** (fixed @ HEAD' `ac5f1e03`, re-verified @ HEAD'' — not a live candidate) → clean v55-style close. **1 ADVISORY** (ZDH-7: the NEW-run subscribe cover-buy double-accrues the flat 100-BURNIE slot-0 reward — `GameAfkingModule:426-485` guards only `done[0]`, missing the `lastAutoBoughtDay>=today` skip the STAGE `:954` + active-sub re-sub `:395` carry; **EV-NEGATIVE** dual-gate trace [~0.001 ETH non-extractable BURNIE vs ~400k+ gas + a funded ≥0.01-ETH buy/cycle; 7% affiliateBase routes to the upline]) — explicitly NOT a finding, surfaced to 357-03/357-04 with the optional one-line `if (s.lastAutoBoughtDay == uint24(today)) skip` hardening. Premature-advance-INERT HOLDS (VRF timing-independent, separate callback tx, rngLockedFlag fences reactive actions, freeze atomic with the request); `_bountyEligible` = sound pay-predicate NOT a security boundary; the passless cap-occupancy + unfunded free-rider vectors CONFIRMED CLOSED (SEC-01 strengthened); the drainAffiliateBase stub guard-less-but-correct (delegatecall preserves msg.sender; module AFFILIATE-only `:1333`) + claim() CEI clean. `git diff 61315ecd HEAD -- contracts/` EMPTY (DIFF_LINES:0)

- [x] 357-00d (subscribe-hardening reconciliation to HEAD'''') — Wave 2.5 (autonomous): the TWO newest 357 contract gates layered on HEAD'' — **HEAD''' `7b0b2a0b`** (the NEW-run slot-0 per-day idempotency guard `GameAfkingModule.sol:451` — closes the ZDH-7 churn advisory) + **HEAD'''' `77d8bc88`** (the USER-caught D-11 LEVEL-0 zero-horizon rejection `:372` — closes a level-0 passless gap the 357-02 sweep MISSED, having run D-11 only at level ≥ 1) [AUDIT-01] ✅ COMPLETE 2026-06-03. 3 commits `30ea4b89`(V56SubHardening 17→22 GREEN: churn-idempotency [slot-0 accrued ONCE across same-day churn; a NEXT-day subscribe does a fresh funded buy] + the level-0 pass-gate proofs [passless-at-L0 reverts NoPass; real-pass/deity/VAULT-sDGNRS subscribe OK at L0])+`519f6e00`(ledger §10 reconciled to HEAD'''': **573/134/103**; `live − union == ∅` AND `union − live == ∅` HOLD — the live 134 == the §2 134-name 453f8073 union BY NAME; the ONE revert-reason flip the D-11 level-0 fix introduced [4 `AfKingSubscription` passless-at-level-0 fixtures, 8/8 GREEN @ HEAD'' → `NoPass()` @ HEAD''''] `vm.skip`-dropped per the §3b/§8c discipline, re-proven GREEN by the new V56SubHardening proofs; SOLVENCY-01 leg-1 byte-anchor held — debit relocated `:690-691`→`:702-703` only)+`b541c445`(357-01-DELTA-AUDIT §3.8 attests both gates NON-WIDENING control-flow-only/revert-only + 357-02-ADVERSARIAL-LOG probe-7 RESOLVED-AT-357 [HEAD'''] + probe-11 §B.3-addendum: the D-11 level-0 gap USER-caught/sweep-missed RESOLVED-AT-357 [HEAD'''']). **Clean close: THREE resolved-in-phase items (F-356-01 + slot-0 advisory + level-0 D-11 gap), 0 unresolved FINDING_CANDIDATE.** ZERO contracts/*.sol mutation; `git diff 77d8bc88 HEAD -- contracts/` EMPTY — subject re-frozen at **HEAD'''' = `77d8bc88`** (downstream 357-03/04 now re-freeze against HEAD'''', not HEAD'').

**Wave 3** *(blocked on Wave 2 completion)*

- [x] 357-03-PLAN.md — Wave 3 (autonomous): author audit/FINDINGS-v56.0.md (the 9-section report, chmod 444 deferred to closure) folding the delta-audit + the adversarial disposition; **THREE resolved-in-phase items (F-356-01 @ HEAD' + slot-0 churn advisory @ HEAD''' + D-11 level-0 gap [USER-caught, sweep-missed] @ HEAD''''), all RESOLVED-AT-357**; re-attest the CURRENT v56.0 requirement set against **HEAD'''' `77d8bc88`** [AUDIT-01] ✅ COMPLETE 2026-06-03 (`audit/FINDINGS-v56.0.md`, 843 lines, all 9 sections mirroring FINDINGS-v55.0.md; 0 UNRESOLVED FINDING_CANDIDATE / 3 RESOLVED-AT-357; the CURRENT 27-row REQUIREMENTS.md set re-attested [AUDIT-01 the only Pending]; MILESTONE_V56_AT_HEAD_<sha> carried verbatim; chmod 444 deferred to 357-04; DOC-ONLY zero source edits). **⚠ 357-04 ACTION:** a FIFTH source commit `c9b5d20d` ("refactor(passes): flat 10% pass lootbox") landed AFTER the HEAD'''' freeze (mutates DegenerusGame.sol + WhaleModule) — OUTSIDE the audited subject `77d8bc88`; the deliverable is authored against the planned subject `77d8bc88`; 357-04 must decide in-scope (re-freeze + 357-00d-style reconcile) vs out-of-scope (pin the signal to `77d8bc88`). Logged to deferred-items.md.

**Wave 4** *(blocked on Wave 3 completion)*

- [ ] 357-04-PLAN.md — Wave 4 (autonomous:false CLOSURE GATE): the atomic 5-doc closure flip + the MILESTONE_V56_AT_HEAD_<sha> signal (reconcile the stale 356 progress row; chmod 444 the findings; no .sol in the closure commit) + the close-then-prompt-to-push offer (D-02 — wait for explicit USER GO) [AUDIT-01]

**UI hint**: no

---

## Progress

**Execution Order:** Phases execute in numeric order: 353 → 354 → 355 → 356 → 357

| Phase | Milestone | Plans Complete | Status | Completed |
|-------|-----------|----------------|--------|-----------|
| 353. SPEC — Design-Lock + Unmanipulable/Solvency Re-Attestation + XMODEL Design-Input + Call-Graph Attestation | v56.0 | 2/2 | Complete    | 2026-06-01 |
| 354. IMPL — The ONE Carefully-Sequenced Batched Contract Diff (aggregator + ticket-mode primitive + DegenerusQuests batched-settle + accumulator + open-end) | v56.0 | 6/6 | Complete   | 2026-06-01 |
| 355. GAS — Measure + Tune (+ GAS-05 deferred payout + liveness adds: openBoxes valve + gap/jackpot decouple) | v56.0 | 3/3 | Complete + PUSHED | 2026-06-02 |
| 356. TST — Unmanipulable (strategic sub/unsub) + Quest-Core Non-Perturbation + Two-Path-Open + Liveness Valve + Gap-Decouple + Gas Marginals + Non-Widening | v56.0 | 7/7 | Complete | 2026-06-02 |
| 357. TERMINAL — Delta Audit + 3-Skill Adversarial Sweep (AUGMENTED by XMODEL) + FINDINGS-v56.0 + Closure | v56.0 | 6/6 (+357-00d HEAD'''' reconcile) | Complete | 2026-06-04 |

> **🔒 v56.0 CONTRACT-BOUNDARY HARD STOP (ONE gate — 354 IMPL · 355 GAS rides the same boundary if it produces a net diff).** Phase 354 IMPL is THE contract phase — the carefully-sequenced batched diff (the per-sub accumulator + the mode-agnostic aggregator accrue/settle + the ticket-mode minimal-write primitive + the `DegenerusQuests` batched-settle entrypoint + the open-end optimizations, authored producer-before-consumer) is applied to `contracts/` and locally compiled (`forge build` clean) but HELD at the contract-commit boundary, NEVER committed without explicit user hand-review (`feedback_batch_contract_approval` + `feedback_never_preapprove_contracts` + `feedback_manual_review_before_push` + `feedback_no_contract_commits`). Phase 355 GAS is a tuning pass — any net behavior-preserving gas change rides the same batched USER-APPROVED diff at the same boundary (or records Outcome-A no-diff per the v55 350 precedent). `ContractAddresses.sol` freely modifiable per `feedback_contractaddresses_policy`; tests + planning + docs AGENT-committable.

> **🔓 v56.0 AUDIT POSTURE — FULL CLOSE (internal sweep + XMODEL cross-model close NOT deferred).** Like v54.0 + v55.0 (and unlike v50.0 + v51.0, whose internal sweeps were deferred → the v52 consolidated audit), **v56.0 runs its own internal 3-skill genuine-PARALLEL adversarial sweep + delta-audit + `audit/FINDINGS-v56.0.md` at TERMINAL (357, AUDIT-01), AUGMENTED by the XMODEL Codex + Gemini cross-model close** — because the milestone touches the shared `DegenerusQuests` core + the everyday `advanceGame` STAGE and the unmanipulability (esp. strategic sub/unsub) + the non-perturbation must be adversarially probed in-milestone. The separate v52 consolidated cross-model audit folds the v56 surface into its cumulative sweep as an additional track (recorded in the v52 charge), not a substitute.

---

## Coverage (v56.0)

**24/24 v56.0 requirements mapped to exactly one phase — 0 orphaned, 0 duplicated.**

| Phase | Requirements | Count |
|-------|--------------|-------|
| 353 SPEC | AFF-01, AFF-02, XMODEL-01 | 3 |
| 354 IMPL | AGG-01, AGG-02, AGG-03, AGG-04, AGG-05, TKT-01, TKT-02, QST-01, QST-02, QST-03, QST-04, QST-05, OPEN-01, OPEN-02 | 14 |
| 355 GAS | GAS-01, GAS-02, GAS-03, GAS-04 | 4 |
| 356 TST | SEC-01, SEC-02 | 2 |
| 357 TERMINAL | AUDIT-01 | 1 |
| **Total** | | **24** |

**Per-category split (verification):**

| Category | Total | SPEC (353) | IMPL (354) | GAS (355) | TST (356) | TERMINAL (357) |
|----------|-------|------------|------------|-----------|-----------|----------------|
| AGG | 5 | — | 5 (01-05) | — | — | — |
| TKT | 2 | — | 2 (01,02) | — | — | — |
| AFF | 2 | 2 (01,02) | — | — | — | — |
| QST | 5 | — | 5 (01-05) | — | — | — |
| OPEN | 2 | — | 2 (01,02) | — | — | — |
| GAS | 4 | — | — | 4 (01-04) | — | — |
| SEC | 2 | — | — | — | 2 (01,02) | — |
| XMODEL | 1 | 1 (01) | — | — | — | — |
| AUDIT | 1 | — | — | — | — | 1 (01) |
| **Total** | **24** | **3** | **14** | **4** | **2** | **1** |

**Center-of-gravity rationale (where a requirement spans design + impl + test + close):**

- **AFF-01 / AFF-02** (the affiliate distribution — the flat-7% deterministic-split PULL: accrue flat 7% per buy into the running `affiliateBase`, settle by PULL via `claim`/`withdraw` with the fixed 75/20/5 split [no roll, no seed, no scheduled/mutation affiliate flush]; taper afking-N/A + leaderboard-at-claim [_amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic: SUPERSEDES the XMODEL roll-unification, the roll is removed_]) → SPEC (353), where they are DESIGN-LOCKED as the load-bearing non-gameability decisions BEFORE any code (the affiliate distribution mechanism IS a design choice, not a built behavior — once the flat-7%-accrue / deterministic-split-PULL shape is locked, IMPL builds it inside the aggregator). The aggregator + the affiliate PULL that USE them (AGG-01..05 + the `claim`/`withdraw` plumbing) are owned at IMPL; AFF-01/02 are the distribution-mechanism design that the affiliate accrue + the QUEST-only AGG-02/03 settle consume — no double-counting (AGG = the quest accrue/settle plumbing + the affiliate accrue; AFF = the distribution rule the `claim` PULL applies).
- **XMODEL-01** (the cross-model Codex + Gemini review) → SPEC (353) as its HOME, because its PRIMARY deliverable is the DESIGN-INPUT pass that must be folded into the design-lock BEFORE IMPL (the gating half — it shapes the mechanism). Its TERMINAL close-augmentation half (Codex + Gemini augmenting the Claude 3-skill sweep) is a touchpoint REFLECTED in Phase 357's success criteria (AUDIT-01's sweep), NOT a separate requirement count — the instruction's "assign it to the phase where its primary deliverable lands … but reflect both touchpoints" resolved to SPEC-as-home.
- **AGG-01..05 · TKT-01/02 · QST-01..05 · OPEN-01/02** (the built behaviors) → IMPL (354) as part of the single batched diff. The SPEC concerns those decisions feed (the accumulator layout, the ticket-primitive shape, the `DegenerusQuests` batched-settle entrypoint shape + non-perturbation approach, the ±10-streak derivation, the open-end review) are folded into the SPEC's success criteria (SC2/SC3/SC4) — they are NOT double-counted at SPEC; only the requirement's HOME (where it must be BUILT) is counted at IMPL.
- **GAS-01 / GAS-02 / GAS-03 / GAS-04** (the measured everyday-cost reduction — per-buy/per-settle marginals, accumulator packing, `SUB_STAGE_BATCH` re-tune, mode/SLOAD collapse) → GAS (355). Note: much of GAS-01/02 is structural to the IMPL refactor (the cheap accrue + the deferred settle + the packed accumulator ARE the saving); the GAS phase MEASURES the savings (proven same-results in TST), lands the residual `SUB_STAGE_BATCH` re-tune + the mode/SLOAD collapse, and gates any net contract change under the same USER-approved diff (or records Outcome-A no-diff per the v55 350 precedent). The SPEC produces the accumulator-layout decision (folded into SC2), but the wins LAND/are measured at the GAS phase.
- **SEC-01 / SEC-02** (the hard security floor — unmanipulable esp. strategic sub/unsub; SOLVENCY-01 untouched + RNG-freeze intact) → TST (356), where they are PROVEN empirically + adversarially as the hard gate (per the explicit "TST Owns SEC-01/02" directive — mirrors v55, where FREEZE design lived at SPEC but the empirical proofs were a distinct phase). The SPEC "SEC design" (the unmanipulable/solvency/freeze re-attestation on paper) is the design gate folded into 353's SC5 — NOT a separate count; the TERMINAL adversarial review (AUDIT-01) is the FINAL gate that re-confirms SEC, but SEC-01/02's center-of-gravity (where the property is first PROVEN, incl. the strategic-sub/unsub proof) is TST. No double-counting.
- **AUDIT-01** (the TERMINAL FULL close) → TERMINAL (357); it re-attests all 24 v56.0 requirements + runs the in-milestone delta-audit + the 3-skill adversarial sweep AUGMENTED by the XMODEL cross-model close + `audit/FINDINGS-v56.0.md` + the atomic 5-doc closure flip with the `MILESTONE_V56_AT_HEAD_<sha>` signal.

---

## ✅ v55.0 AfKing-in-Game Redesign (SHIPPED 2026-06-01)

**Milestone:** v55.0 (started 2026-05-30; SHIPPED 2026-06-01) — Phases 348-352 (7 phases incl. the inserted 349.1 box redesign + 349.2 quest/affiliate regression fix).
**Closure signal:** `MILESTONE_V55_AT_HEAD_ca3bbd3220de763298ef2e742111f6e6ef90d583` (subject frozen `453f8073`; baseline `20ca1f79`).
**Shape:** SPEC (348) → IMPL (349, shipped via the inserted 349.1 `77c3d9ef` + 349.2 `453f8073`) → GAS (350, Outcome-A no-diff) → TST (351, 603/134/16 NON-WIDENING) → TERMINAL (352, FULL close, 0 NEW_FINDINGS).
**Goal:** Fold AfKing's subscriber state + logic into DegenerusGame (subscriber set + cursors + a per-sub box-stamp + the v54 `afkingFunding` ledger into `DegenerusGameStorage`; a new `GameAfkingModule` delegatecall owns subscribe/setters + the process-pass + the open-pass + the router; AfKing proper collapses to thin dispatch stubs). Boxes materialize at open from a per-sub stamp under the freeze / solvency / liveness spine.
**Closure verdict (EMITTED, Phase 352):** `AFKING_IN_GAME_FOLD SHIPPED (AfKing.sol dissolved → GameAfkingModule delegatecall + DegenerusGameStorage append, AF_KING removed 1→0; ARCH-01..04); BOX_REDESIGN SHIPPED (boons-OFF amount=spend, the COMMITTED 4-field stamp scorePlus1+amount+lastAutoBoughtDay+lastOpenedDay [349.1 SUPERSEDED the 348 5-field design], DAY-keyed seed rngWordByDay[lastAutoBoughtDay], live-level open byte-identical to openLootBox, lastOpenedDay no-double-open; BOX-01..05); FREEZE_SPINE INTACT (FREEZE-01/02/03 — live-level collapse NARROWS the frozen set to the SEED, strictly safe); REVERT_FREE_CHAIN DISCHARGED (REVERT-01 + REVERT-02 no-valve); EVCAP_AT_OPEN (EVCAP-01 RMW exactly-once, buy-time write bypassed); OPEN-E_4-PROTECTION RE-ATTESTED HOLD (CONSENT-01/02); SOLVENCY-01 HELD NET (afkingFunding rides inside claimablePool, no new aggregate); GAS Outcome-A; RNG_FREEZE_INTACT; NON-WIDENING 603/134/16; 0 NEW_FINDINGS; KNOWN_ISSUES_UNMODIFIED; + 1 OUT-OF-SCOPE informational advisory O1 [the pre-existing symmetric DegenerusQuests lootbox-quest double-credit, NOT in the v55 delta — folded into v56.0 as QST-05]`. Full report: `audit/FINDINGS-v55.0.md` (chmod 444).
**Coverage:** 29/29 v55.0 requirements mapped (348 SPEC: 5 · 349 IMPL: 14 · 350 GAS: 3 · 351 TST: 6 · 352 TERMINAL: 1); 0 orphaned, 0 duplicated. 2 USER-APPROVED contract commits (`77c3d9ef` 349.1 + `453f8073` 349.2); 350 GAS Outcome-A no-diff. **NOTHING pushed.**

> **Full v55.0 phase-detail (the per-phase goals + success criteria + plan checklists for Phases 348-352, incl. the inserted 349.1/349.2) is archived in `.planning/MILESTONES.md` and the per-phase `.planning/phases/348-*..352-*/` directories.** The 349.1/349.2 box-redesign + quest/affiliate-restore supersession history is captured in the STATE.md "Last Shipped Milestone (v55.0)" block + the `[[v55-afking-revert-free-proof]]` / `[[v55-afking-lootbox-quest-affiliate-regression-fix]]` memory topic files.

---

## Roadmap Overview

| Phase | Goal | Requirements (count) | Depends on |
|-------|------|----------------------|------------|
| 353 — SPEC — Design-Lock + Unmanipulable/Solvency Re-Attestation + XMODEL Design-Input + Call-Graph Attestation | Design-lock the mode-agnostic ~10-day aggregator (the affiliate flat-7% deterministic-split PULL [`claim`/`withdraw`, no roll/seed/flush — _amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic: SUPERSEDES the XMODEL roll-unification_] + the QUEST-only automatic settle + the per-sub accumulator layout [the SHRUNK in-slot `affiliateBase`+`questProgress` + off-slot `pendingClaim`; double-settle markers DROPPED — self-marking running balances]), the ticket-mode minimal-write primitive shape (+ century/x00 parity decision), the afking-OPEN-end review (materialization + unmanipulable re-verification), the shared-`DegenerusQuests`-core batched-settle entrypoint + non-perturbation approach, and the QST design (automatic slot-0 BURNIE via the `mintBurnie` settlement-due chain + a `claimQuest` fallback; the first-sub-only `+daysToNextSettle` streak head-start [bounded +0..+9, no provisional] — _amended 2026-06-01 — automatic slot-0 BURNIE via mintBurnie chain + claimQuest fallback; first-sub-only +daysToNextSettle streak, no provisional_); re-attest unmanipulable (esp. strategic sub/unsub) + SOLVENCY-01-untouched + RNG-freeze-intact on paper; run the XMODEL-01 cross-model design-input pass (Codex + Gemini) and fold its gas suggestions + exploit findings into the design-lock; grep-attest every cited file:line vs `453f8073`. Zero contract mutation. | AFF-01 · AFF-02 · XMODEL-01 (3) | Nothing (baseline v55.0 closure HEAD `453f8073` / `MILESTONE_V55_AT_HEAD_ca3bbd3220de763298ef2e742111f6e6ef90d583`) |
| 354 — IMPL — The ONE Carefully-Sequenced Batched Contract Diff (aggregator + ticket-mode primitive + DegenerusQuests batched-settle + accumulator + open-end) | Apply the single reconciled diff producer-before-consumer (accumulator storage → aggregator accrue/settle → DegenerusQuests batched-settle → ticket-mode minimal-write primitive → open-end optimizations → interfaces): the mode-agnostic aggregator (accrue-cheap-no-cross-contract + the AFFILIATE leg as a flat-7% deterministic-split PULL [`claim`/`withdraw`, no roll/seed/flush] + the QUEST leg settled by RIDING THE DAILY BUY STAGE on the global settle day [the separate `mintBurnie` "settlement-due" router leg REJECTED as a redundant cold-SLOAD pass; the slot-0 quest BURNIE + the `buyerOwedBurnie` ticket bonus mint TOGETHER in one `creditFlip`; `SUB_STAGE_BATCH` SHRUNK, number deferred to 355] + a quest-settle-on-unsub + uniform mode-agnostic quest-settle + self-marking running balances [double-settle markers DROPPED] — _amended 2026-06-01 — quest settle rides the buy STAGE; afking ticket buyer-bonus 10%/20% accrued+settled with the quest (matches live `:1655`)_), the ticket-mode minimal-write primitive (at resolution parity, century/x00 decision applied, the 10%/20% ticket buyer-bonus `:1655-1659` accrued into `buyerOwedBurnie` at live parity), the DegenerusQuests batched-settle entrypoint + the streak machinery (the slot-0 `questProgress` delivered-day counter → settle-mint `× QUEST_SLOT0_REWARD` [+ the accrued `buyerOwedBurnie` ticket bonus] via the STAGE-riding settle on the global settle day / `claimQuest` fallback, the first-sub-only `+daysToNextSettle` head-start [bounded +0..+9, no provisional] reading the actual `state.streak`, double-credit guard + active-pass anti-reset, non-perturbing, O1 confirmed/fixed — _amended 2026-06-01 — quest settle rides the buy STAGE; slot-0 BURNIE + the 10%/20% ticket buyer-bonus mint together; first-sub-only +daysToNextSettle streak, no provisional_), and the open-end optimizations (max gas, cheapest shared materialization, COMPLETELY unmanipulable — live-level parity / no double-open / no EV-cap double-draw / no shared-state hazard); HARD STOP at the contract-commit boundary (applied + locally compiled, never committed without explicit user hand-review). | AGG-01..05 · TKT-01/02 · QST-01..05 · OPEN-01/02 (14) | Phase 353 |
| 355 — GAS — Measure + Tune (marginals + accumulator packing + SUB_STAGE_BATCH re-tune + mode/SLOAD collapse) | Measure + tune the everyday cost under the security-over-gas floor (gas-scavenger → gas-skeptic, each tune unmanipulable): the per-buy + per-settle marginals (lootbox ~206k → ~130–140k; ticket off ~262k) under the 16.7M ceiling; the accumulator packed into the Sub spare bits (no new cold SSTORE); `SUB_STAGE_BATCH` re-tuned with the per-day STAGE under 16.7M at the cap; redundant mode-branches / repeated SLOADs collapsed where cheaper. Any net change rides the same USER-approved diff at the contract boundary (or records Outcome-A no-diff). | GAS-01 · GAS-02 · GAS-03 · GAS-04 (4) | Phase 354 |
| 356 — TST — Unmanipulable (strategic sub/unsub) + Quest-Core Non-Perturbation + Two-Path-Open + Gas Marginals + Non-Widening | Prove the hard security floor empirically: the afking system (buy + open) is unmanipulable — no positive-EV from settle-timing (no seed) / strategic sub/unsub churn / re-rate-on-alteration / pre-credit-EV inflation / double-credit / open-timing / settle-griefing (amended 2026-06-01: the roll is REMOVED under the flat-7% deterministic-split pull); SOLVENCY-01 untouched (BURNIE flip-credit OFF the ETH/pool path, debit byte-unchanged, RNG-freeze intact); the shared DegenerusQuests core non-perturbing; the two open routes coexist hazard-free; the gas marginals measured under 16.7M (GAS wins same-results); NON-WIDENING vs `453f8073` (every red BY NAME). | SEC-01 · SEC-02 (2) | Phase 355 |
| 357 — TERMINAL — Delta Audit + 3-Skill Adversarial Sweep (AUGMENTED by XMODEL Codex + Gemini close) + FINDINGS-v56.0 + Closure | NON-WIDENING delta-audit vs the v55 baseline `453f8073` (SOLVENCY-01 byte-unchanged + RNG-freeze + affiliate deterministic-split-PULL non-gameability [NO roll/seed — _amended 2026-06-01 — flat-7% deterministic-split pull; quests stay automatic_] + open-end two-path/no-double-open + shared-quest non-perturbation re-attested), 3-skill genuine-PARALLEL adversarial sweep over the strategic sub/unsub edge + settle-timing non-exploitability (no seed) + pre-credit-EV + two-path open (skeptic-filtered) AUGMENTED by the XMODEL cross-model close (Codex + Gemini), author `audit/FINDINGS-v56.0.md` (9-section, chmod 444), emit `MILESTONE_V56_AT_HEAD_<sha>` + atomic 5-doc closure flip; re-attest all 24 requirements. | AUDIT-01 (1) | Phase 356 |

**Coverage:** 24/24 v56.0 requirements mapped (AGG 5 + TKT 2 + AFF 2 + QST 5 + OPEN 2 + GAS 4 + SEC 2 + XMODEL 1 + AUDIT 1); zero orphan requirements; zero duplicate mappings.
