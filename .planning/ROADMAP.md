# Roadmap: Degenerus Protocol Audit

## Milestones

- ✅ **v1.0 Initial RNG Security Audit** — Phases 1-5 (shipped 2026-03-14)
- ✅ **v2.0 Adversarial Audit** — Phases 6-18 (shipped 2026-03-17)
- ✅ **v3.0-v24.1** — Phases 19-212 (shipped 2026-04-10)
- ✅ **v25.0 Full Audit (Post-v5.0 Delta + Fresh RNG)** — Phases 213-217 (shipped 2026-04-11)
- ✅ **v26.0 Bonus Jackpot Split** — Phases 218-219 (shipped 2026-04-12)
- ✅ **v27.0 Call-Site Integrity Audit** — Phases 220-223 (shipped 2026-04-13)
- ✅ **v28.0 Database & API Intent Alignment Audit** — Phases 224-229 (shipped 2026-04-15) — see [milestones/v28.0-ROADMAP.md](milestones/v28.0-ROADMAP.md)
- ✅ **v29.0 Post-v27 Contract Delta Audit** — Phases 230-236 (shipped 2026-04-18) — see [milestones/v29.0-ROADMAP.md](milestones/v29.0-ROADMAP.md)
- ✅ **v30.0 Full Fresh-Eyes VRF Consumer Determinism Audit** — Phases 237-242 (shipped 2026-04-20) — see [milestones/v30.0-ROADMAP.md](milestones/v30.0-ROADMAP.md)
- ✅ **v31.0 Post-v30 Delta Audit + Gameover Edge-Case Re-Audit** — Phases 243-246 (shipped 2026-04-24) — see [milestones/v31.0-ROADMAP.md](milestones/v31.0-ROADMAP.md)
- ✅ **v32.0 Backfill Idempotency + purchaseLevel Underflow Audit** — Phases 247-253 (shipped 2026-05-02) — see [milestones/v32.0-ROADMAP.md](milestones/v32.0-ROADMAP.md)
- ✅ **v33.0 Charity Allowlist Governance** — Phases 254-258 (shipped 2026-05-07; closure signal `MILESTONE_V33_AT_HEAD_4ce3703d740d3707c88a1af595618120a8168399` supersedes `MILESTONE_V33_AT_HEAD_dcb70941`) — see [milestones/v33.0-ROADMAP.md](milestones/v33.0-ROADMAP.md)
- ✅ **v34.0 Trait Rarity Rework + Gold Solo Priority** — Phases 259-262 (shipped 2026-05-09; closure signal `MILESTONE_V34_AT_HEAD_6b63f6d4daf346a53a1d463790f637308ea8d555`) — see [milestones/v34.0-ROADMAP.md](milestones/v34.0-ROADMAP.md)
- ✅ **v35.0 BURNIE Near-Future Per-Pull Level Resample** — Phases 263-265 (shipped 2026-05-09; closure signal `MILESTONE_V35_AT_HEAD_5db8682bd7b811437f0c1cf47e832619d1478ac6`)
- ✅ **v36.0 Lootbox-Path Entropy Refactor** — Phase 266 (shipped 2026-05-10; closure signal `MILESTONE_V36_AT_HEAD_1c0f09132d7439af9881c56fe197f81757f8164a`) — see [milestones/v36.0-ROADMAP.md](milestones/v36.0-ROADMAP.md)
- 🚧 **v37.0 Degenerette Recalibration + Maintenance Bundle** — Phases 267-271 (planning; baseline `MILESTONE_V36_AT_HEAD_1c0f09132d7439af9881c56fe197f81757f8164a`)

## Phases

- [x] **Phase 267: Degenerette Producer + 5-Table Payout Rewrite** — Add `packedTraitsDegenerette` producer + 5 per-N payout/hero/WWXRP table dispatch in `DegenerusGameDegeneretteModule.sol`; delete `_evNormalizationRatio`; rewrite `_distributePayout` ETH branch with 3-tier split rule (≤3× bet → 100% ETH; 3-10× bet → 2.5× bet ETH floor + remainder lootbox; >10× bet → existing 25/75 split); single batched USER-APPROVED contract commit. (completed 2026-05-10)
- [x] **Phase 268: Degenerette Statistical Validation + Cross-Surface Preservation** — 3 new `test/stat/` files (per-N EV exactness + producer chi² + bonus EV) + `SurfaceRegression.test.js` v37.0 extension proving Mint/Jackpot/Lootbox/EntropyLib byte-identity. (completed 2026-05-11)
- [ ] **Phase 269: Lootbox Dead-Branch Cleanup + SURF-05 Gas-Pin Re-Pinning** — Delete unreachable BURNIE-conversion branch in `_resolveLootboxRoll` L1568-1581 (caller-clamp invariant); diagnose + fix Phase 261/264 SURF-05 ~120K gas-pin drift under `npm run test:stat`.
- [ ] **Phase 270: Post-v32.0 Deferred-Commit Adversarial Sub-Audit** — Audit-only adversarial sweep of commits `002bde55` (presale auto-deactivate) + `2713ce61` (setDecimatorAutoRebuy removal); read-only delta-classification + KI envelope check; FINDING_CANDIDATE rows escalate to Phase 271.
- [ ] **Phase 271: Delta Audit + Findings Consolidation (Terminal)** — Single `audit/FINDINGS-v37.0.md` 9-section deliverable; closure signal `MILESTONE_V37_AT_HEAD_<sha>` emitted in §9c; KNOWN-ISSUES.md walkthrough; ROADMAP/STATE/MILESTONES flips.

## Phase Details

### Phase 267: Degenerette Producer + 5-Table Payout Rewrite

**Goal:** `contracts/DegenerusTraitUtils.sol` ships a new `packedTraitsDegenerette(uint256) internal pure returns (uint32)` library helper using the per-quadrant near-uniform color distribution `[16,16,16,16,16,16,16,8]/120` (commons 13.33%, gold 6.67%) and uniform 1/8 symbol; the existing `weightedColorBucket` / `traitFromWord` / `packedTraitsFromSeed` bodies are byte-identical (Mint + Jackpot + v34.0 gold-solo paths UNTOUCHED). `contracts/modules/DegenerusGameDegeneretteModule.sol` is rewritten to dispatch payout/hero/WWXRP across 5 per-N tables indexed by `_countGoldQuadrants(playerTicket) ∈ {0..4}`: `_evNormalizationRatio` (L808-851) is DELETED with its single call site (L965-969); `_getBasePayoutBps` rewritten for per-N dispatch; `_applyHeroMultiplier` rewritten symbol-only with per-N dispatch; `_wwxrpBonusRoiForBucket` rewritten for per-N dispatch; `_distributePayout` ETH branch rewritten with 3-tier split rule (PAY-SPLIT-01..03: ≤3× bet → 100% ETH; 3-10× bet → 2.5× bet ETH floor + remainder lootbox; >10× bet → existing 25/75 split; pool-cap takes precedence on top); `betAmount` threaded into `_distributePayout` from the L656 call site; producer call site at L607 swapped to `packedTraitsDegenerette`; 4 stale comments (L239 RTP, L262 weight=10, L287-298 normalizer prose, L316 HERO_BOOST) rewritten to per-N reality. Net constant count: 11 → 24 (5 payout packed + 5 jackpot M=8 + 5 hero packed + 5 WWXRP packed, less 4 single-table + 2 normalizer constants); two threshold/floor multipliers added inline to `_distributePayout` (3× and 2.5×). Single batched USER-APPROVED contract commit per `feedback_batch_contract_approval.md`. Zero new storage; zero new admin entry points; `packedTraitsDegenerette` is `internal pure` library helper inlined into the consumer module — does NOT widen the public ABI.
**Depends on:** Nothing (first impl phase; baseline v36.0 closure HEAD `1c0f09132d7439af9881c56fe197f81757f8164a`)
**Requirements:** DGN-01, DGN-02, DGN-03, DGN-04, DGN-05, DGN-06, DGN-07, DGN-08, DGN-09, DGN-10, DGN-11, DGN-12, DGN-13, DGN-14, DGN-15, PAY-SPLIT-01, PAY-SPLIT-02, PAY-SPLIT-03
**Success Criteria** (what must be TRUE):
  1. `contracts/DegenerusTraitUtils.sol` ships new `packedTraitsDegenerette(uint256) internal pure returns (uint32)` library helper with per-quadrant color via base-15 scaling (`scaled == 14 ? 7 : scaled >> 1`) and symbol uniform `1/8`; byte layout `[QQ][CCC][SSS]` preserved; library `internal pure` is inlined into the consumer at compile time — does NOT widen the public ABI; existing `weightedColorBucket` / `traitFromWord` / `packedTraitsFromSeed` bodies grep-cite byte-identical to v36.0 closure HEAD `1c0f0913` (DGN-01, DGN-14).
  2. `contracts/modules/DegenerusGameDegeneretteModule.sol` deletes `_evNormalizationRatio` (L808-851) + its single call site (L965-969); adds `_countGoldQuadrants(uint32) private pure returns (uint8)` returning N ∈ {0..4} from color==7 quadrant count; `_getBasePayoutBps`, `_applyHeroMultiplier` (symbol-only via `((playerTicket >> heroQuadrant*8) & 7) == ((resultTicket >> heroQuadrant*8) & 7)`), and `_wwxrpBonusRoiForBucket` REWRITTEN to dispatch across 5 per-N tables; producer callsite at L607 swapped from `packedTraitsFromSeed` to `packedTraitsDegenerette` (DGN-02, DGN-03, DGN-04, DGN-07, DGN-09, DGN-12).
  3. 25 packed constants present and grep-citable: `QUICK_PLAY_PAYOUTS_N{0..4}_PACKED` (5) + `QUICK_PLAY_PAYOUT_N{0..4}_M8` (5) + `HERO_BOOST_N{0..4}_PACKED` (5) + `WWXRP_FACTORS_N{0..4}_PACKED` (5); 4 old single-table constants (`QUICK_PLAY_BASE_PAYOUTS_PACKED`, `QUICK_PLAY_BASE_PAYOUT_8_MATCHES`, `WWXRP_BONUS_FACTOR_BUCKET5..8` block, `HERO_BOOST_PACKED`) + 2 normalizer constants DELETED; net constant count 11 → 24; values byte-identical to `derive_5_tables.py` Fraction-exact output (DGN-05, DGN-06, DGN-08, DGN-10, DGN-11).
  4. 4 stale comment blocks rewritten with no prose drift between code and comments: L239 ("99.99% RTP" claim → "basePayoutEV = 100 centi-x exact per N; RTP = roiBps/10000"), L262 ("derived from … all weights=10" → "per-N factors derived from each N's basePayout schedule + binomial-convolution P_N(M) + 10/30/30/30 split"), L287-298 ("EXACT EV NORMALIZATION" prose → per-N table calibration prose with no runtime correction), L316 (`HERO_BOOST_PACKED` reference → per-N HERO_BOOST_N{0..4} reference) (DGN-13).
  5. Storage layout byte-identical at v37.0 phase-close HEAD vs v36.0 baseline `1c0f0913` (storage-slot grep proof); zero new public/external mutation entry points; zero new external pure entry points (`packedTraitsDegenerette` is `internal pure` library helper, inlined into consumer — does NOT widen the public ABI); zero new admin functions; zero new modifiers; existing `packedTraitsFromSeed` consumer paths in MintModule + JackpotModule UNCHANGED at the byte level — single batched USER-APPROVED contract commit lands per `feedback_batch_contract_approval.md` + `feedback_no_contract_commits.md` + `feedback_never_preapprove_contracts.md` (DGN-15).
  6. `_distributePayout` ETH-currency branch (`DegenerusGameDegeneretteModule.sol` L684-740) rewritten with 3-tier split rule: (a) `payout <= 3 * betAmount` → 100% ETH, no lootbox conversion (PAY-SPLIT-01); (b) `3 * betAmount < payout <= 10 * betAmount` → `ethShare = 2.5 * betAmount`, `lootboxShare = payout - ethShare` (PAY-SPLIT-02 floor); (c) `payout > 10 * betAmount` → existing `ethShare = payout / 4`, `lootboxShare = payout - ethShare` (PAY-SPLIT-02 standard); (d) pool-cap (`ETH_WIN_CAP_BPS = 1_000`, 10% of futurePool) applied AFTER the split rule and takes precedence: if `ethShare > 10% * pool`, excess flips to lootbox per existing L716-723 logic (PAY-SPLIT-03). `betAmount` (uint128) threaded into `_distributePayout` as a 5th argument from the L656 call site (`amountPerTicket` already in scope). `CURRENCY_BURNIE` + `CURRENCY_WWXRP` branches UNCHANGED. NatSpec on `_distributePayout` documents the 3-tier rule + pool-cap precedence in language of CURRENT design only (per `feedback_no_history_in_comments.md`). Phase 268 STAT-07 empirically verifies the rule across per-N payout distributions; Phase 271 §4 surface (h) audits boundary-gaming + composition correctness (PAY-SPLIT-01, PAY-SPLIT-02, PAY-SPLIT-03).

**Plans:** 1/1 plans complete

Plans:
- [x] 267-01-PLAN.md — Degenerette producer + 5-table payout rewrite + 3-tier ETH split (1 plan, 4 tasks: docs upstream fixes | constants verification | USER-APPROVED contract commit | phase-close summary; covers DGN-01..15 + PAY-SPLIT-01..03)

### Phase 268: Degenerette Statistical Validation + Cross-Surface Preservation

**Goal:** `test/stat/DegenerettePerNEvExactness.test.js` (NEW) drives ≥1M-draw Monte Carlo per N ∈ {0..4} asserting measured `basePayoutEV = 100.00 ± 0.50 centi-x` against the per-N table dispatch (equal-EV invariant satisfied across all 16,384 player-pick configurations within statistical tolerance). `test/stat/DegeneretteProducerChi2.test.js` (NEW) drives ≥1M-sample chi² uniformity for `packedTraitsDegenerette` color distribution `[16,16,16,16,16,16,16,8]/120` (Wilson-Hilferty Z<1.645 / `CHI2_CRIT_05[7]=14.067`) and symbol uniform `1/8` (`CHI2_CRIT_05[7]=14.067`) at α=0.05. `test/stat/DegeneretteBonusEv.test.js` (NEW) drives ≥100K-draw per-N hero EV + WWXRP factor EV measurement within ±1% per-N target. `test/stat/SurfaceRegression.test.js` extended with v37.0 describe asserting byte-identity (codehash or selector enumeration) of: `DegenerusTraitUtils` Mint/Jackpot consumer bodies (`packedTraitsFromSeed`, `weightedColorBucket`, `traitFromWord`) UNCHANGED; `DegenerusGameJackpotModule` v34.0 gold-solo `_pickSoloQuadrant` + 4 ETH-distribution injection sites + `JackpotBucketLib` UNCHANGED; `DegenerusGameLootboxModule` v36.0 entropy refactor surfaces (`_rollTargetLevel`, `_lootboxTicketCount`, `_resolveLootboxRoll` post-Phase-269 cleanup) hash2/bit-slice patterns preserved; `EntropyLib` `hash2` + `entropyStep` bodies UNCHANGED (carries v36.0 ENT-04). Per-N analytical match-count histogram cross-cited against measured frequencies within ±0.5% bin tolerance (proves the 5-table calibration assumption set). `advanceGame` per-day gas envelope unchanged within ±2K vs v36.0 baseline; Degenerette path is OFF the `advanceGame` hot path so impact is bounded to `quickPlay` + related entry-point gas regressions. Test infrastructure REUSE-ONLY of Phase 261/264/266 chi² tooling (`makeRng`, `CHI2_CRIT_05`, `wilsonHilfertyZ`); no new statistical primitives introduced.
**Depends on:** Phase 267 (the per-N dispatch + producer + payout-split rule is the unit under empirical test)
**Requirements:** STAT-01, STAT-02, STAT-03, STAT-04, STAT-05, STAT-06, STAT-07, SURF-01, SURF-02, SURF-03, SURF-04, SURF-05, SURF-06
**Success Criteria** (what must be TRUE):
  1. `test/stat/DegenerettePerNEvExactness.test.js` exists and PASSES: for each N ∈ {0..4}, ≥1M draws against the per-N table dispatch yield `basePayoutEV = 100.00 ± 0.50 centi-x`; cross-pick parity asserted across the 16,384 player-pick configuration space within statistical tolerance; per-N analytical match-count histogram matches measured frequencies within ±0.5% bin (STAT-01, STAT-05).
  2. `test/stat/DegeneretteProducerChi2.test.js` exists and PASSES: ≥1M `packedTraitsDegenerette` samples; per-quadrant color frequencies match `[16,16,16,16,16,16,16,8]/120` within Wilson-Hilferty Z<1.645 / `CHI2_CRIT_05[7]=14.067` at α=0.05; per-quadrant symbol frequencies match uniform `1/8` within `CHI2_CRIT_05[7]=14.067` at α=0.05; tooling REUSE-ONLY of Phase 261/264/266 chi² primitives (no new helpers introduced) (STAT-02, STAT-06).
  3. `test/stat/DegeneretteBonusEv.test.js` exists and PASSES: for each N ∈ {0..4}, ≥100K hero-active draws yield measured hero-boost EV matching per-N target within ±1%; ≥100K WWXRP-active draws yield measured WWXRP factor EV matching per-N target within ±1% (10/30/30/30 bucket split EV = exactly 5.000% ETH bonus per N analytically, ±1% empirically) (STAT-03, STAT-04).
  4. `test/stat/SurfaceRegression.test.js` v37.0 describe extension asserts byte-identity of all SURF-01..04 surfaces: (a) `DegenerusTraitUtils.packedTraitsFromSeed` + `weightedColorBucket` + `traitFromWord` bodies UNCHANGED vs v36.0 baseline `1c0f0913` (only additive change: `packedTraitsDegenerette` helper); (b) `DegenerusGameJackpotModule` v34.0 `_pickSoloQuadrant` + 4 ETH-distribution injection sites + `JackpotBucketLib` body UNCHANGED; (c) `DegenerusGameLootboxModule` v36.0 hash2/bit-slice patterns in `_rollTargetLevel`, `_lootboxTicketCount`, `_resolveLootboxRoll` (post-Phase-269 dead-branch cleanup) preserved; (d) `EntropyLib.sol` `hash2` + `entropyStep` bodies byte-identical (carries v36.0 ENT-04). Codehash or selector-enumeration evidence grep-citable (SURF-01, SURF-02, SURF-03, SURF-04, SURF-05).
  5. `advanceGame` per-day gas envelope delta within ±2K vs v36.0 baseline (Degenerette path is OFF the `advanceGame` hot path); `quickPlay` + related Degenerette entry-point per-call gas regression tested with theoretical worst case derived FIRST per `feedback_gas_worst_case.md`; net gas ≈ wash expected (normalizer math removed; gold-counter 4-iter loop + 5-case dispatch added); 1.99× margin from v34/v35/v36 SURF-05 baseline preserved. Test commits batched USER-APPROVED per `feedback_batch_contract_approval.md` (test-tree treated identically to contract-tree under `feedback_no_contract_commits.md` discipline) (SURF-06).

**Plans:** 1/1 plans complete

Plans:
- [x] 268-01-PLAN.md — Degenerette stat suite + cross-surface preservation v37.0 + worst-case gas regression (1 plan, 3 tasks: chore inventory | USER-APPROVED batched test commit | phase-close summary; covers STAT-01..07 + SURF-01..06)

### Phase 269: Lootbox Dead-Branch Cleanup + SURF-05 Gas-Pin Re-Pinning

**Goal:** `contracts/modules/DegenerusGameLootboxModule.sol` `_resolveLootboxRoll` (~L1568-1581 at v36.0 HEAD) deletes the unreachable `if (targetLevel < currentLevel) { burnieOut = ... }` branch, replacing the surrounding `if (ticketsScaled != 0)` block with a direct `ticketsOut = ticketsScaled;` assignment. The branch is structurally dead because the only caller `_resolveLootboxCommon` at L882-884 clamps `targetLevel = max(targetLevel, currentLevel)` before invocation; verified by `grep -rn "_resolveLootboxRoll" contracts/` returning only the definition + the single caller. `test/gas/LootboxOpenGas.test.js` (extended) confirms the ~50 g/open savings under the 55%-tickets-path probability profile + bytecode shrink. Audit-trail row added to `audit/FINDINGS-v37.0.md` §3.A delta-surface in Phase 271 confirming the v36.0 ENT-02 callsite numbering stays consistent post-deletion (line numbers may shift; bit-slice budget UNAFFECTED since only the inner conditional was dead). The `SURF-05` gas-pin drift (~120K under `npm run test:stat` ordering vs standalone-stable in Phase 261/264) is root-caused: hypotheses to investigate include (a) state pollution from prior test files, (b) gas-meter snapshot ordering, (c) fixture-loader caching, (d) hardhat node restart timing. Stabilization path chosen from {re-pin gas snapshots to combined-suite-stable values + document offset rationale; OR fix ordering dependency via `before(() => vm.reset())` injection / test-file isolation; OR split affected describes across separate test files} — least-invasive option preferred; decision documented inline at the test fixture. Single batched USER-APPROVED contract commit (LBX cleanup) + USER-APPROVED test/chore commit(s) (GASPIN fix + LootboxOpenGas extension) per `feedback_no_contract_commits.md` + `feedback_batch_contract_approval.md`. Behavior change posture: ZERO behavioral change at LBX (caller-clamp invariant proves byte-equivalence at runtime); ZERO behavioral change at GASPIN (test infrastructure only).
**Depends on:** Nothing (mixed-maintenance phase; baseline can run in parallel with Phase 267-268 from a content perspective, but sequenced after Phase 267-268 batched-commit gates for delta-surface accounting cleanliness)
**Requirements:** LBX-01, LBX-02, LBX-03, GASPIN-01, GASPIN-02, GASPIN-03
**Success Criteria** (what must be TRUE):
  1. `_resolveLootboxRoll` (~L1568-1581 at v36.0 HEAD) dead `if (targetLevel < currentLevel) { burnieOut = (uint256(ticketsScaled) * PRICE_COIN_UNIT) / TICKET_SCALE; }` branch DELETED; replaced by direct `ticketsOut = ticketsScaled;` assignment inside the surviving `if (ticketsScaled != 0)` guard; `grep -rn "_resolveLootboxRoll" contracts/` returns only the definition + the single `_resolveLootboxCommon` caller (no widening of the call graph) (LBX-01).
  2. `test/gas/LootboxOpenGas.test.js` extended with v37.0 describe asserting per-tickets-path-open gas savings ≥30 g and ≤80 g (55% probability-weighted ~27 g amortized across all paths) vs v36.0 closure HEAD baseline; bytecode shrink confirmed via deployment-byte-count delta; per `feedback_no_history_in_comments.md`, no trace comment added at the deletion site unless caller-clamp invariant is non-obvious from local reading (LBX-02).
  3. `audit/FINDINGS-v37.0.md` §3.A delta-surface row (authored in Phase 271) confirms v36.0 ENT-02 callsite numbering remains consistent post-deletion: `_resolveLootboxRoll` 4 hash2/bit-slice callsites at L1548 / L1569 / L1585 / L1599 (v36.0 line numbers) survive byte-identical at the structural level; bit-slice budget UNAFFECTED; line numbers may shift downward by a few lines after the dead-branch removal but the post-Phase-269 grep recipe stays valid (LBX-03).
  4. Phase 261/264 SURF-05 gas-pin drift root-caused: investigation report cites which hypothesis path (state pollution, gas-meter snapshot ordering, fixture-loader caching, hardhat node restart timing) was confirmed; decision recorded inline at the test fixture or in the Phase 269 plan (GASPIN-01).
  5. `npm run test:stat` runs cleanly start-to-finish in CI-equivalent fresh-checkout: zero flaky failures across 3 consecutive runs; both standalone and combined-suite gas pins agree at the new pinned values; chosen stabilization path (re-pin / ordering-fix / split-files) is the least-invasive option per the recorded decision; test commits batched USER-APPROVED per `feedback_batch_contract_approval.md` (GASPIN-02, GASPIN-03).

**Plans:** TBD

### Phase 270: Post-v32.0 Deferred-Commit Adversarial Sub-Audit

**Goal:** Audit-only adversarial sweep of two long-deferred post-v32.0 contract-tree commits whose adversarial coverage has been carry-forward-deferred from v33.0 → v34.0 → v35.0 → v36.0 close: commit `002bde55` (presale auto-deactivate) and commit `2713ce61` (`setDecimatorAutoRebuy` admin-entry-point removal). For each commit: read full diff at the source-tree level; classify per the `audit/FINDINGS-v33.0..v36.0.md` delta-surface taxonomy {NEW, MODIFIED_LOGIC, REFACTOR_ONLY, DELETED}; sweep adversarial surfaces with grep-cited evidence — for `002bde55`: state-machine ordering implications, presale-flag timing across the auto-deactivate threshold, downstream consumer assumptions in `MintModule` interaction, presale → post-presale transition coherence; for `2713ce61`: admin-entry-point-removal blast radius, downstream gating assumptions in BURNIE auto-rebuy code path, Decimator state-machine implications, residual `setDecimatorAutoRebuy` callsite proof of zero (grep-recipe). Per-surface verdict assigned: SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE / FINDING_CANDIDATE. FINDING_CANDIDATE rows escalate to §3.A finding blocks in Phase 271 deliverable. KI envelope check: confirm neither `002bde55` nor `2713ce61` widens EXC-01..04 nor introduces a new accepted-design entry warranting KI promotion. Phase 270 emits ZERO source-tree mutations (audit-only); deliverable is a working-file appendix `.planning/phases/270-post-v32-deferred-commit-adversarial-subaudit/270-01-DELTA-SURFACE.md` (or equivalent) feeding directly into Phase 271 §3.A authoring.
**Depends on:** Nothing (audit-only; can run in parallel with Phase 267-269; sequenced before Phase 271 to feed delta-surface inputs)
**Requirements:** DELTA-01, DELTA-02, DELTA-03, DELTA-04
**Success Criteria** (what must be TRUE):
  1. Commit `002bde55` (presale auto-deactivate) full-diff read; per-declaration classification {NEW, MODIFIED_LOGIC, REFACTOR_ONLY, DELETED} per `audit/FINDINGS-v33.0..v36.0.md` taxonomy; adversarial sweep covers state-machine ordering, presale-flag timing across auto-deactivate threshold, downstream `MintModule` consumer assumptions, presale → post-presale transition coherence; each surface row carries grep-cited evidence (file + line range + commit-hash anchor) (DELTA-01).
  2. Commit `2713ce61` (`setDecimatorAutoRebuy` removal) full-diff read; per-declaration classification per same taxonomy; adversarial sweep covers admin-entry-point-removal blast radius, downstream gating assumptions in the BURNIE auto-rebuy code path, Decimator state-machine implications, residual `setDecimatorAutoRebuy` callsite proof-of-zero via grep recipe; row-level evidence file + line + commit-hash anchored (DELTA-02).
  3. Per-surface verdict SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE / FINDING_CANDIDATE assigned to every enumerated row; FINDING_CANDIDATE rows (if any) flagged with finding-block stub content (severity-rubric tier candidate, surface description, exploit/defensive argument) for Phase 271 §3.A escalation; default expectation: zero FINDING_CANDIDATE rows given the surfaces are mature post-v32.0 commits with no known regressions; deviation requires explicit Phase 271 plan disposition (DELTA-03).
  4. KI envelope check: row-level evidence confirms neither `002bde55` nor `2713ce61` widens EXC-01 (affiliate roll), EXC-02 (prevrandao fallback), EXC-03 (F-29-04 mid-cycle substitution), or EXC-04 (EntropyLib XOR-shift; v36.0 NARROWS scope to BAF-only); no new accepted-design entries warranting KI promotion; result feeds the Phase 271 §6 KI gating walk as RE_VERIFIED-NEGATIVE-scope rows (DELTA-04).
  5. Phase 270 emits ZERO source-tree mutations; working-file appendix `.planning/phases/270-post-v32-deferred-commit-adversarial-subaudit/270-01-DELTA-SURFACE.md` (or equivalent canonical filename) AGENT-COMMITTED at phase close; appendix is grep-citable from Phase 271 `audit/FINDINGS-v37.0.md` §3.A (DELTA-03 grep recipe).

**Plans:** TBD

### Phase 271: Delta Audit + Findings Consolidation (Terminal)

**Goal:** Publish `audit/FINDINGS-v37.0.md` as the single canonical milestone-closure deliverable — 9-section shape consistent with v32.0 / v33.0 / v34.0 / v35.0 / v36.0. AUDIT-01 §3.A delta-surface table covers all source-tree changes v36.0 audit-subject HEAD `1c0f09132d7439af9881c56fe197f81757f8164a` → v37.0 closure HEAD with hunk-level evidence + {NEW, MODIFIED_LOGIC, REFACTOR_ONLY, DELETED} classification: Phase 267 Degenerette contract changes (TraitUtils new helper + DegeneretteModule normalizer-deletion + 5-table dispatch + producer-callsite swap + 25 packed constants delta + `_distributePayout` 3-tier ETH split rewrite + `betAmount` threading) + Phase 269 LootboxModule dead-branch deletion (LBX-03 callsite-numbering carry note) + Phase 270 post-v32.0 carry-forward declarations (`002bde55` + `2713ce61` rows). AUDIT-02 §4 adversarial sweep verdicts every surface SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE / FINDING_CANDIDATE with grep-cited evidence; minimum surface list: (a) per-N table dispatch correctness vs match-count distribution; (b) symbol-only hero match preserves uniformity with no color-channel info leak; (c) `_countGoldQuadrants` boundary (color==7 only, not >=7) — overflow / off-by-one immune; (d) producer `[16×7, 8]/120` byte-layout consistency with downstream consumers; (e) WWXRP factor table-dispatch composition with hero boost — no double-counting; (f) lootbox dead-branch removal byte-equivalence (caller-clamp invariant); (g) hero × per-N composition skill-expression channel preserved (v34.0 surface (f) carry); (h) ETH payout split-rule monotonicity + boundary-gaming check — verify 3.0× threshold (PAY-SPLIT-01), 2.5× ETH floor (PAY-SPLIT-02), and 10%-pool-cap precedence (PAY-SPLIT-03) compose correctly across all per-N basePayout × roiBps × hero × WWXRP-bonus combinations; assert no payout-multiple gaming exploits the boundary discontinuity at 3.0× bet; verify thin-pool cap-flip path preserves total payout amount (ethShare + lootboxShare = payout invariant). AUDIT-03 §3 conservation re-proof: Degenerette payout flow preserves coin/BURNIE conservation invariants; per-N table calibration math algebraically verified to hold `basePayoutEV = 100 centi-x ± rounding`; no new mint sites; solvency invariant unchanged. AUDIT-04 §3 zero-new-state scan attests zero new storage slots; zero new public/external mutation entry points; zero new external pure entry points (`packedTraitsDegenerette` is `internal pure` library helper, inlined into the consumer at compile time — does NOT widen the public ABI); zero new admin functions; zero new modifiers. Adversarial pass via `/contract-auditor` + `/zero-day-hunter` SEQUENTIAL after full §4 draft per D-NN-ADVERSARIAL-02 carry; real captured output logged in `271-NN-ADVERSARIAL-LOG.md`; `/economic-analyst` + `/degen-skeptic` inclusion deferred to Phase 271 discuss-phase decision per CONTEXT.md. KI envelopes EXC-01..04 RE_VERIFIED (EXC-01..03 NEGATIVE-scope at v37 since Degenerette + lootbox-cleanup + post-v32 commits have zero affiliate-roll / AdvanceModule / gameover-RNG-substitution interaction; EXC-04 RE_VERIFIED with NARROWS retained from v36.0). REG-01 v36.0 closure-signal non-widening (EntropyLib byte-identical; lootbox v36.0 entropy bodies byte-identical EXCEPT for LBX-01 dead-branch deletion which is no-behavior-change cleanup). REG-02 v34.0 closure-signal non-widening (TraitUtils existing functions + JackpotModule gold-solo + JackpotBucketLib byte-identical; Degenerette path uses NEW `packedTraitsDegenerette`; gold-solo Mint/Jackpot path uses unchanged `packedTraitsFromSeed`; surfaces strictly disjoint). REG-04 prior-finding spot-check sweep across `audit/FINDINGS-v25..v36.0.md` for findings referencing the v37-touched function set. KNOWN-ISSUES.md walkthrough: assess whether Degenerette payout-recalibration + lootbox cleanup + post-v32 commits warrant any new KI entries (default zero-promotion path per D-262-KI-01 carry; deviation requires user disposition). Closure signal `MILESTONE_V37_AT_HEAD_<sha>` emitted in §9c. ROADMAP/STATE/MILESTONES flips documented in §9.NN three-subsection commit-readiness register (i USER-APPROVED contracts + ii USER-APPROVED tests + iii AGENT-COMMITTED audit + planning artifacts; NO awaiting-approval subsection).
**Depends on:** Phase 267, Phase 268, Phase 269, Phase 270 (audit baseline is the post-impl + post-stats + post-maintenance + post-sub-audit HEAD)
**Requirements:** AUDIT-01, AUDIT-02, AUDIT-03, AUDIT-04, AUDIT-05, AUDIT-06, REG-01, REG-02, REG-03, REG-04
**Success Criteria** (what must be TRUE):
  1. `audit/FINDINGS-v37.0.md` published FINAL READ-only at v37.0 closure HEAD with 9 sections (executive summary, per-phase summaries §3a-c, F-37-NN finding blocks under D-08 5-bucket severity rubric, §3.A delta-surface table covering Phase 267 Degenerette changes + Phase 269 LBX dead-branch deletion + Phase 270 post-v32 carry-forward declarations with hunk-level evidence and {NEW, MODIFIED_LOGIC, REFACTOR_ONLY, DELETED} classification, §3 conservation re-proof, §3 zero-new-state scan, regression appendix §5, KI gating walk §6, prior-artifact cross-cites §7, forward-cite zero-emission proof §8, closure attestation §9 with §9.NN three-subsection commit-readiness register); §3.A row-count covers every changed declaration grep-reproducibly (AUDIT-01).
  2. §4 adversarial sweep verdicts every identified surface SAFE / SAFE_BY_DESIGN / SAFE_BY_STRUCTURAL_CLOSURE / FINDING_CANDIDATE with explicit row-level evidence covering at minimum: (a) per-N table dispatch correctness vs match-count distribution P_N(M); (b) symbol-only hero match preserves uniformity, no color-channel info leak; (c) `_countGoldQuadrants` boundary `color == 7` strict (not `>= 7`); (d) producer `[16,16,16,16,16,16,16,8]/120` byte-layout consistency with downstream consumers; (e) WWXRP factor table-dispatch composition with hero boost — no double-counting in `_fullTicketPayout`; (f) lootbox dead-branch removal byte-equivalence via caller-clamp invariant at L882-884; (g) hero × per-N composition skill-expression channel preserved (v34.0 surface (f) carry-forward); (h) ETH payout split-rule monotonicity + boundary-gaming check — `_distributePayout` 3-tier rule (PAY-SPLIT-01 ≤3× all-ETH + PAY-SPLIT-02 2.5× floor on 3-10× band + PAY-SPLIT-03 pool-cap precedence) composes correctly across all per-N × roiBps × hero × WWXRP-bonus combinations; boundary discontinuity at exactly 3.0× bet documented as accepted (3.0× → 2.5× ETH drop at 3.01× under min-2.5× floor); thin-pool cap-flip path preserves `ethShare + lootboxShare = payout` invariant. Adversarial pass via `/contract-auditor` + `/zero-day-hunter` SEQUENTIAL after full §4 draft per D-NN-ADVERSARIAL-02 carry; real captured output logged in `271-NN-ADVERSARIAL-LOG.md` with zero-disagreement default; `/economic-analyst` + `/degen-skeptic` inclusion explicitly recorded (in scope or deferred per Phase 271 discuss-phase user disposition). Conservation re-proof §3 algebraically verifies `basePayoutEV = 100 centi-x ± rounding` per N; ETH bonus EV = exactly 5.000% per N; per-N hero EV-neutrality holds within `P(hero|M,N) × boost(M,N) + (1-P) × HERO_PENALTY ≈ HERO_SCALE` 0.05% tolerance; no new mint sites; solvency invariant unchanged (AUDIT-02, AUDIT-03, AUDIT-06).
  3. §3 zero-new-state scan attests zero new storage slots, zero new public/external mutation entry points, zero new external pure entry points (`packedTraitsDegenerette` is `internal pure` library helper, inlined into the consumer at compile time — does NOT widen the public ABI), zero new admin functions, zero new modifiers, zero new upgrade hooks; storage layout byte-identical at v37.0 closure HEAD vs v36.0 baseline `1c0f0913` (slot-by-slot grep proof) (AUDIT-04).
  4. §5 regression appendix: REG-01 PASS for v36.0 closure signal `MILESTONE_V36_AT_HEAD_1c0f09132d7439af9881c56fe197f81757f8164a` non-widening (EntropyLib `hash2` + `entropyStep` byte-identical; lootbox v36.0 entropy refactor bodies in `_rollTargetLevel` / `_resolveLootboxRoll` / `_lootboxTicketCount` byte-identical EXCEPT for LBX-01 dead-branch deletion which §3.A documents as no-behavior-change cleanup with caller-clamp invariant audit-trail row); REG-02 PASS for v34.0 closure signal `MILESTONE_V34_AT_HEAD_6b63f6d4daf346a53a1d463790f637308ea8d555` non-widening (`_pickSoloQuadrant` + 4 ETH-distribution injection sites + `JackpotBucketLib` + `weightedColorBucket` byte-identical; Degenerette path uses NEW `packedTraitsDegenerette` while gold-solo Mint/Jackpot path uses UNCHANGED `packedTraitsFromSeed` — surfaces strictly disjoint, no widening); REG-03 KI envelopes EXC-01..04 RE_VERIFIED with EXC-01..03 NEGATIVE-scope (Degenerette + lootbox-cleanup + post-v32 commits have zero affiliate-roll / AdvanceModule / gameover-RNG-substitution interaction) and EXC-04 RE_VERIFIED with NARROWS retained from v36.0 (BAF-jackpot-only scope unchanged); REG-04 prior-finding spot-check sweep across `audit/FINDINGS-v25..v36.0.md` for findings referencing `DegenerusGameDegeneretteModule` (`_evNormalizationRatio`, `_getBasePayoutBps`, `_applyHeroMultiplier`, `_wwxrpBonusRoiForBucket`), `DegenerusTraitUtils.packedTraitsFromSeed`, `DegenerusGameLootboxModule._resolveLootboxRoll`, presale-flag handling, `setDecimatorAutoRebuy` — default expectation ALL rows PASS; SUPERSEDED rows allowed with explicit successor cite (e.g., F-25-NN normalization-related rows replaced by 5-table design) (REG-01, REG-02, REG-03, REG-04).
  5. §6 KI gating walk records the v37.0 KNOWN-ISSUES.md disposition (default zero-promotion path per D-262-KI-01 carry; explicit user disposition required for any deviation); §9c emits closure signal `MILESTONE_V37_AT_HEAD_<sha>` exactly once and grep-citable from `.planning/MILESTONES.md` v37.0 row + the v37.0 SUMMARY frontmatter; §9.NN three-subsection commit-readiness register populated (i USER-APPROVED contracts: Phase 267 + Phase 269 LBX cleanup; ii USER-APPROVED tests: Phase 268 stat suite + Phase 269 GASPIN re-pin + LootboxOpenGas extension; iii AGENT-COMMITTED audit + planning artifacts: `audit/FINDINGS-v37.0.md` + Phase 270 working-file appendix + ROADMAP/STATE/MILESTONES flips); §8 forward-cite zero-emission grep recipe verifies zero `v38.0+` references emitted from this terminal phase (D-NN-FCITE-01 carry); KNOWN-ISSUES.md walkthrough records the disposition explicitly (UNMODIFIED expected unless adversarial pass surfaces a D-09-gate-passing candidate) (AUDIT-05).

**Plans:** TBD

## Progress

| Phase | Plans Complete | Status | Completed |
|-------|----------------|--------|-----------|
| 267. Degenerette Producer + 5-Table Payout Rewrite | 1/1 | Complete   | 2026-05-10 |
| 268. Degenerette Statistical Validation + Cross-Surface Preservation | 1/1 | Complete   | 2026-05-11 |
| 269. Lootbox Dead-Branch Cleanup + SURF-05 Gas-Pin Re-Pinning | 0/0 | Not started | - |
| 270. Post-v32.0 Deferred-Commit Adversarial Sub-Audit | 0/0 | Not started | - |
| 271. Delta Audit + Findings Consolidation (Terminal) | 0/0 | Not started | - |
