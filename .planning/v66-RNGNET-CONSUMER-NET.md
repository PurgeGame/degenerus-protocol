# v66 RNGNET — Re-Derived VRF-Consumer Net (current HEAD)

> **Supersedes** the stale `.planning/RNGLOCK-CATALOG.md` (v42/v43, pre-rename) and the v30
> state-machine / freeze-proof as the CURRENT-HEAD VRF-consumer enumeration. Subject = byte-frozen
> `contracts/` tree `0dd445a6` (commit `42c8e9c6`). Produced by the Phase 411 RNGNET workflow
> (`wf_59121131-0b4`, 4 cluster agents re-deriving FROM HEAD, independent of the catalog).

**67 consumers** re-derived (the stale catalog enumerated 13).

- Freeze verdicts: {'FROZEN-AT-COMMIT': 36, 'CROSS-CONTRACT-SEAM': 21, 'NEEDS-PROOF': 9, 'MUTABLE-INPUT': 1}
- In catalog: {'NO': 26, 'MISCLASSIFIED': 14, 'YES': 27}
- Missing from catalog: 27 · Misclassified: 19 · Open-freeze (NEEDS-PROOF/SEAM) routed to 412/413: 31

## RNGNET-02 — Diff summary

Re-derived 67 VRF-derived-value consumer entries across 4 clusters at HEAD tree 0dd445a6 (commit 42c8e9c6) and diffed against the v43.0 RNGLOCK-CATALOG's 13-consumer list (D-298-CONSUMER-LIST-01, §1-§13) plus the v30 state-machine and freeze-proof anchors. MISSING from the 13-list: 28 consumers — the entire BAF winner-select surface (sampleTraitTicketsAtLevel, sampleFarFutureTickets in DegenerusGame; runBafJackpot slot/scatter/far-future slices in DegenerusJackpots; Game-side runBafJackpot dispatch — catalog has NO BAF section), the whole afking subsystem (afking-box open read, cover-box bind, resolveAfkingBox), the salvage _farFutureSeed + its two quote consumers, the sDGNRS-side redemption seed mix (hash2 over rngWordForDay(day+1)) and submit gate, the Degenerette FLIP survival flip and all box-spin paths, plus advanceGame producer/keep-roll/backfill sites. All 5 panel-flagged suspected-missing seeds CONFIRMED present and uncatalogued. 20 consumers MISCLASSIFIED — chiefly because the catalog's enumerated call sites are stale (off-by-N lines, removed flipDay struct, the BurnieCoinflip/StakedDegenerusStonk/DegenerusStonk contract names that no longer exist) or the consumer is named only as a writer (issueDeityBoon) rather than a VRF-derivation site. FREEZE STILL OPEN (route to Phase 412/413): 31 consumers are NEEDS-PROOF or CROSS-CONTRACT-SEAM, dominated by (a) the BAF/jackpot winner-selects sampling player-mutable traitBurnTicket/ticketQueue pools, (b) the dailyHeroWagers freshness-window across cross-cycle dailyIdx mid-update, (c) the sDGNRS day+1 redemption seam, (d) the salvage prior-day-word seed, (e) the deity-pick-after-word MUTABLE-INPUT at _deityBoonForSlot, (f) the _awardDegeneretteDgnrs / _rollLootboxBoons co-resident mutable reads, and (g) the gameover prevrandao fallback. KEY STALE ANCHORS: the catalog's headline #1 §12 cross-day re-roll exploit is fully REMEDIATED by the redesigned day-keyed redemption model (redemptionPeriodIndex/pendingRedemptionEthBase removed; _pendingResolveDay sentinel + BurnsBlockedBeforeDailyRng gate); the v30 state-machine's THIRD clear-site C-02 (updateVrfCoordinatorAndSub rngLockedFlag=false @ :1635) is REMOVED — rotation now KEEPS the lock and re-issues, leaving _unlockRng @ :1799 as the SOLE clear; currentDayView() cross-calls were replaced by local GameTimeLib computation (the view fn survives); and all §1-§13 line anchors + slot/contract names are pre-rename and off by tens-to-hundreds of lines. The v30 docs are anchored to the 7ab515fe baseline and represent a strict subset of the current net — historical, not current truth.

## Full re-derived net (67 consumers)

| # | Consumer | Site | Source | Player inputs | Freeze | Catalog |
|---|----------|------|--------|---------------|--------|---------|
| 1 | midday-lootbox-readiness-gate | contracts/modules/DegenerusGameAdvanceModule.sol:advanceGame:226-231 | lootboxRngWordByIndex[ (LR_INDEX-1) ] | index = lootboxRngPacked LR_INDEX field (advanced by VRF request, not player-selectable per resolution); tickets in the read queue were frozen by the write->read swap at request time | FROZEN-AT-COMMIT | NO |
| 2 | midday-lootbox-word-assign (rngWordCurrent + totalFlipReversals) | contracts/modules/DegenerusGameAdvanceModule.sol:advanceGame:281-290 | rngWordCurrent (raw fulfilled daily word) -> written into lootboxRngWordByIndex[preIdx] | totalFlipReversals = sum of player FLIP-burn nudges (reverseFlip), but reverseFlip reverts while rngLockedFlag set, so nudges are frozen before the word is knowable; preIdx is request-advanced | FROZEN-AT-COMMIT | NO |
| 3 | salted-coin-jackpot seed (level-1 second draw) | contracts/modules/DegenerusGameAdvanceModule.sol:advanceGame:474-479 | rngWord (rngWordByDay[day] via rngGate return) | none at this site (fixed tag salt); downstream winner-select samples trait pools in JackpotModule | FROZEN-AT-COMMIT | MISCLASSIFIED |
| 4 | future-take additive-random + triangular variance | contracts/modules/DegenerusGameAdvanceModule.sol:_consolidatePoolsAndRewardJackpots:869,883-884 | rngWord (rngWordByDay[day]) | none — operates on global pool magnitudes (memNext/memFuture) and elapsed days; no player-selectable preimage | FROZEN-AT-COMMIT | MISCLASSIFIED |
| 5 | BAF fire gate (bit-0 daily flip) | contracts/modules/DegenerusGameAdvanceModule.sol:_consolidatePoolsAndRewardJackpots:922 | rngWord (rngWordByDay[day]), bit 0 | none at the gate; the fired BAF then calls the player-pool samplers (separate consumers below) | FROZEN-AT-COMMIT | YES |
| 6 | x00 keep-roll (5d4 dice via hash2) | contracts/modules/DegenerusGameAdvanceModule.sol:_consolidatePoolsAndRewardJackpots:958-973 | rngWord (rngWordByDay[day]) via EntropyLib.hash2 | none — pool-magnitude only, fixed FUTURE_KEEP_TAG salt | FROZEN-AT-COMMIT | NO |
| 7 | redemption roll (rngGate normal path) | contracts/modules/DegenerusGameAdvanceModule.sol:rngGate:1255-1258 | currentWord (the day's applied rngWordByDay[day]), bits 8+ | toResolve = sdgnrs.pendingResolveDay() (sentinel, one unresolved day); the redemption magnitude pendingRedemptionEthBase is a CROSS-CONTRACT player-mutable pool re-armable by a 1-wei burn on a future wall-day | CROSS-CONTRACT-SEAM | YES |
| 8 | redemption roll (gameover normal path) | contracts/modules/DegenerusGameAdvanceModule.sol:_gameOverEntropy:1316-1320 | currentWord (applied rngWordByDay[day]), bits 8+ | toResolve = sentinel pendingResolveDay; magnitude is cross-contract sDGNRS redemption state | CROSS-CONTRACT-SEAM | MISCLASSIFIED |
| 9 | redemption roll (gameover prevrandao-fallback path) | contracts/modules/DegenerusGameAdvanceModule.sol:_gameOverEntropy:1351-1355 | fallbackWord = _getHistoricalRngFallback(day) (historical VRF words + block.prevrandao), bits 8+ | block.prevrandao (1-bit validator propose/skip bias on a gameover-only path); sentinel resolve day; cross-contract redemption magnitude | NEEDS-PROOF | MISCLASSIFIED |
| 10 | historical-RNG prevrandao fallback word | contracts/modules/DegenerusGameAdvanceModule.sol:_getHistoricalRngFallback:1394-1411 | rngWordByDay[searchDay] (up to 5 early committed words) + block.prevrandao | block.prevrandao (validator can propose-or-skip = 1-bit bias); currentDay; historical words are immutable on-chain | NEEDS-PROOF | NO |
| 11 | _finalizeLootboxRng (daily-word -> current lootbox index) | contracts/modules/DegenerusGameAdvanceModule.sol:_finalizeLootboxRng:1281-1285 | rngWord (the applied daily/gameover/fallback word) -> lootboxRngWordByIndex[index] | index is request-advanced (not player-selectable at resolution); boxes at the index were enqueued with frozen per-index commitment slots | FROZEN-AT-COMMIT | NO |
| 12 | rawFulfillRandomWords mid-day direct lootbox finalize | contracts/modules/DegenerusGameAdvanceModule.sol:rawFulfillRandomWords:1843-1845 | randomWords[0] (raw VRF word) -> lootboxRngWordByIndex[index] | index reserved at the mid-day request (requestLootboxRng advances LR_INDEX + sets LR_MID_DAY); tickets swap-frozen at request | FROZEN-AT-COMMIT | MISCLASSIFIED |
| 13 | _backfillGapDays derived per-day words | contracts/modules/DegenerusGameAdvanceModule.sol:_backfillGapDays:1869-1877 | vrfWord (first post-gap rngWordCurrent) -> rngWordByDay[gapDay] | gapDay index only (calendar-derived); coinflip bets for those days were committed in real time during the stall before the post-gap word existed | FROZEN-AT-COMMIT | NO |
| 14 | _backfillOrphanedLootboxIndices derived words | contracts/modules/DegenerusGameAdvanceModule.sol:_backfillOrphanedLootboxIndices:1889-1901 | vrfWord (fresh post-gap word) -> lootboxRngWordByIndex[i] | index i only; the per-index box commitment slots (lootboxEth etc.) were written at purchase, before this fresh vrfWord existed | FROZEN-AT-COMMIT | NO |
| 15 | afking-box open word read (rngWordByDay[stampDay]) | contracts/modules/GameAfkingModule.sol:_autoOpen:1537 + _openAfkingBox:1463-1486 | rngWordByDay[sub.lastAutoBoughtDay] (the frozen stamp day's daily word) | stampDay (lastAutoBoughtDay) is set by the pre-RNG process STAGE BEFORE rngGate writes that day's word, so the seed day is committed while the word is still uncommitted; score/amount frozen at stamp; level + EV-cap read LIVE at open in resolveAfkingBox | FROZEN-AT-COMMIT | NO |
| 16 | afking cover-box bind to lootboxRngWordByIndex | contracts/modules/GameAfkingModule.sol:_recordAfkingCoverBox:979-1043 | lootboxRngWordByIndex[index] (future word) via lootboxRngPacked LR_INDEX | subscribe is caller-timed and runs AFTER the day's word is public, so the cover-buy deliberately binds to the NEXT index's future word (not rngWordByDay[stampDay]); index advances at the next request so the buyer cannot select a revealed index | FROZEN-AT-COMMIT | NO |
| 17 | deityBoonData daily boon seed | contracts/DegenerusGame.sol:deityBoonData:882-890 | rngWordByDay[day] (today's daily word) | none in this view (day is wall-clock); the actual boon issuance (issueDeityBoon -> LootboxModule) reads the same word and applies per-recipient derivation downstream | FROZEN-AT-COMMIT | NO |
| 18 | rngWordForDay public getter (cross-contract seam) | contracts/DegenerusGame.sol:rngWordForDay:2089-2091 | rngWordByDay[day] | day argument is caller-supplied; the read is a past committed word (or 0). Manipulability lives in the CALLER's choice of which frozen day to read, resolved in the cross-contract consumer | CROSS-CONTRACT-SEAM | MISCLASSIFIED |
| 19 | boxesPending frontier readiness probe | contracts/DegenerusGame.sol:boxesPending:1412-1421 | lootboxRngWordByIndex[idx] (frontier open index) | none — equality-to-zero probe, no outcome derived; idx is the monotonic boxCursorIndex frontier | FROZEN-AT-COMMIT | NO |
| 20 | BAF trait-ticket sampler (sampleTraitTicketsAtLevel) | contracts/DegenerusGame.sol:sampleTraitTicketsAtLevel:2314-2333 | entropy param = EntropyLib.hash2(daily rngWord, salt) chain from DegenerusJackpots BAF | traitBurnTicket[targetLvl][traitSel] MEMBERSHIP and ordering — players control which trait buckets they hold tickets in via burns; targetLvl chosen by caller (DegenerusJackpots) from the frozen word | CROSS-CONTRACT-SEAM | NO |
| 21 | BAF far-future ticket sampler (sampleFarFutureTickets) | contracts/DegenerusGame.sol:sampleFarFutureTickets:2341-2374 | entropy param = EntropyLib.hash2(daily rngWord, salt) chain from DegenerusJackpots BAF, plus internal keccak re-roll | ticketQueue[_tqFarFutureKey(candidate)] MEMBERSHIP — players control far-future queue membership by purchasing far tickets; the level walk + index select off the keccak-chained word; currentLvl read live | CROSS-CONTRACT-SEAM | NO |
| 22 | openHumanBoxes/_openLootBoxLeg → _openLootBoxLegWith box seed | contracts/modules/DegenerusGameLootboxModule.sol:_openLootBoxLegWith:560 (also _openLootBoxLeg:525, openHumanBoxes:681/705) | lootboxRngWordByIndex[index] (per-index committed VRF word) | player (msg.sender-derived, fixed at deposit), amount (frozen in packed lootboxEth word at deposit); index is the per-index anchor, not selectable post-word; LIVE level read at open but auto-open removes timing control | FROZEN-AT-COMMIT | YES |
| 23 | _resolvePresaleBox coin-presale-box roll | contracts/modules/DegenerusGameLootboxModule.sol:_resolvePresaleBox:751 (reached openBox:633 / openHumanBoxes:708) | lootboxRngWordByIndex[index] (committed daily word for the index, frozen at buy) | player + amount (immutable buy data); soldBefore (frozen buy-time cumulative). No new mutable SLOAD enters the roll; live level read only for FLIP pricing (priceForLevel(level+1)) | FROZEN-AT-COMMIT | MISCLASSIFIED |
| 24 | resolveLootboxDirect (decimator/degenerette/ETH-spin-recirc) seed | contracts/modules/DegenerusGameLootboxModule.sol:resolveLootboxDirect:883 | rngWord caller param (Degenerette passes hash2(lootboxRngWordByIndex[index], betId); decimator passes its per-level word; ETH-spin recirc passes hash2(seed, BOX_RECIRC_TAG)) | player; caller domain-separates per resolution (betId / per-level word). No live post-reveal input; _applyEvMultiplierWithCap is the only live read (monotonic down-clamp on frozen per-level EV budget) | FROZEN-AT-COMMIT | YES |
| 25 | resolveRedemptionLootbox → _resolveRedemptionChunk seed (sDGNRS gambling-burn claim) | contracts/modules/DegenerusGameLootboxModule.sol:_resolveRedemptionChunk:970 (chunk loop resolveRedemptionLootbox:951; rngWord re-hashed per chunk via EntropyLib.hash1:955) | rngWord caller param from sDGNRS (the redemption period roll); per-chunk re-hash hash1(rngWord) | player, amount, snapshotted activityScore (frozen at burn submit). No live day in seed | CROSS-CONTRACT-SEAM | YES |
| 26 | resolveAfkingBox seed (afking-subscription box, live-level twin) | contracts/modules/DegenerusGameLootboxModule.sol:resolveAfkingBox:1084 | rngWord caller param = rngWordByDay[day] (frozen stamp-day word, passed by GameAfkingModule open-leg) | player, amount (stamped spend), day (boundary-pinned FROZEN process day). Live level read but auto-open removes timing control; day MUST stay frozen or self-keepering grinds the seed | FROZEN-AT-COMMIT | NO |
| 27 | _rollLootboxBoons boon roll | contracts/modules/DegenerusGameLootboxModule.sol:_rollLootboxBoons:1456 (roll = uint32(seed>>120)%BOON_PPM_SCALE) | Per-resolution seed (derived from the box's committed rngWord, bits[120..151]) | Reads LIVE game-state into the weight space: decWindowOpen, deityPassOwners.length, mintPacked_[player] (deity-pass bit), lazyPassValue (price), currentDay. These reshape the boon-type mapping, not the seed | NEEDS-PROOF | YES |
| 28 | issueDeityBoon → _deityBoonForSlot deity boon roll | contracts/modules/DegenerusGameLootboxModule.sol:_deityBoonForSlot:2319 (rngWord = rngWordByDay[day] read at issueDeityBoon:1139) | rngWordByDay[day] (the day's VRF word) | deity (msg.sender deity-pass holder), day, slot (0-2, caller-chosen). decimatorAllowed (_isDecimatorWindow) and deityPassAvailable (deityPassOwners.length) reshape the weight total → boon-type mapping | MUTABLE-INPUT | MISCLASSIFIED |
| 29 | _resolveFullTicketBet per-spin result-ticket draws (Degenerette spins) | contracts/modules/DegenerusGameDegeneretteModule.sol:_resolveFullTicketBet:676 (resultSeed = keccak(rngWord,index,[spinIdx],QUICK_PLAY_SALT)) | lootboxRngWordByIndex[index] (read at :652; index frozen in the packed bet at placement) | index (frozen at bet placement, gated: lootboxRngWordByIndex[index]!=0 reverts RngNotReady at place time); customTicket/heroQuadrant only score AGAINST the result, do not steer the seed; bet committed before word lands | FROZEN-AT-COMMIT | YES |
| 30 | _resolveFullTicketBet FLIP survival flip | contracts/modules/DegenerusGameDegeneretteModule.sol:_resolveFullTicketBet:773 (EntropyLib.hash2(rngWord, betId) & 1) | lootboxRngWordByIndex[index] (the bet's committed word) + betId | betId (the player's own bet nonce, fixed at placement) + rngWord (frozen). Both committed before the VRF word lands; a losing bet pays zero whether resolved or abandoned | FROZEN-AT-COMMIT | NO |
| 31 | resolveWwxrpSpinFromBox spin reels + S==9 jackpot | contracts/modules/DegenerusGameDegeneretteModule.sol:resolveWwxrpSpinFromBox:1305 | seed param = EntropyLib.hash2(boxSeed, BOX_WWXRP_SPIN_TAG) (tagged off the box's committed-word seed) | none on the seed (player + box fixed); S==9 award reads live level bracket + the per-bracket flag, but that gates only a deduped one-per-bracket halfpass, not the spin outcome | FROZEN-AT-COMMIT | NO |
| 32 | resolveFlipSpinsFromBox 3 spins + survival flip | contracts/modules/DegenerusGameDegeneretteModule.sol:resolveFlipSpinsFromBox:1362 (loop ss=hash2(seed,i); survival hash2(seed,BOX_SURVIVAL_TAG)&1) | seed param = hash2(boxSeed, BOX_FLIP_SPIN_TAG) | none — seed and box committed; mint-only, no pool/ETH/recirc | FROZEN-AT-COMMIT | NO |
| 33 | resolveEthSpinFromBox spin + 3-tier split + recirc | contracts/modules/DegenerusGameDegeneretteModule.sol:resolveEthSpinFromBox:1414 | seed param = hash2(boxSeed, BOX_ETH_SPIN_TAG); recirc box re-hashes hash2(seed, BOX_RECIRC_TAG) | none on the seed; _distributePayout reads LIVE futurePrizePool for the 10% cap (flushed before recirc so recirc sees fresh state) — a pool read, not a seed input | FROZEN-AT-COMMIT | NO |
| 34 | _awardDegeneretteDgnrs reward (S>=7 ETH bets, also box ETH-spin) | contracts/modules/DegenerusGameDegeneretteModule.sol:_awardDegeneretteDgnrs:1210 (called _resolveFullTicketBet:738, resolveEthSpinFromBox:1437) | Indirect: gated by s (VRF-derived score). Reward magnitude reads sdgnrs.poolBalance(Reward), NOT the VRF word | betWei (frozen); poolBalance(Reward) is a live cross-contract SLOAD (read fresh per call) | CROSS-CONTRACT-SEAM | MISCLASSIFIED |
| 35 | runTerminalJackpot trait roll + entropy + bucket counts | contracts/modules/DegenerusGameJackpotModule.sol:runTerminalJackpot:239 (_rollWinningTraits(rngWord,false); _soloAdjustedEntropy(traitIds, hash2(rngWord,targetLvl))) | rngWord param (terminal jackpot VRF seed from GameOverModule) | Winner pool = traitBurnTicket[targetLvl] membership (players hold tickets at targetLvl); gameOver latched TRUE before call | CROSS-CONTRACT-SEAM | YES |
| 36 | payDailyJackpot trait pair + entropy + carryover source + daily-bps | contracts/modules/DegenerusGameJackpotModule.sol:payDailyJackpot:298 (_rollWinningTraitsPair; _dailyCurrentPoolBps:331; DAILY_CARRYOVER_SOURCE keccak:370; _soloAdjustedEntropy) | randWord param (daily VRF word, = rngWordCurrent) | Winner pool = traitBurnTicket[lvl] membership; _rollWinningTraitsPair reads dailyHeroWagers[dailyIdx] (settled prior-day wager pool) for hero override | NEEDS-PROOF | YES |
| 37 | payDailyJackpot purchase-phase path (ETH + lootbox/ticket bonuses) | contracts/modules/DegenerusGameJackpotModule.sol:payDailyJackpot:468-541 (traitIds + _soloAdjustedEntropy(hash2(randWord,lvl)); _distributeLootboxAndTickets) | randWord param (daily VRF word) | traitBurnTicket[lvl] membership; same dailyHeroWagers[dailyIdx] hero override | NEEDS-PROOF | YES |
| 38 | _runEarlyBirdLootboxJackpot bonus-trait winner draw | contracts/modules/DegenerusGameJackpotModule.sol:_runEarlyBirdLootboxJackpot:621 (called payDailyJackpot:342) | rngWord param (daily VRF word) | traitBurnTicket[lvl] membership; winner idx = keccak(rngWord,trait,salt,i)%effectiveLen | CROSS-CONTRACT-SEAM | YES |
| 39 | payDailyJackpotCoinAndTickets (Phase 2: coin jackpot + ticket distribution) | contracts/modules/DegenerusGameJackpotModule.sol:payDailyJackpotCoinAndTickets:555 (_rollWinningTraitsPair; _runFlipJackpot; _distributeTicketJackpot entropy=hash2(randWord,lvl/sourceLevel)) | randWord param (must match rngWordCurrent from Phase 1) | traitBurnTicket[lvl]/[sourceLevel] membership; dailyHeroWagers[dailyIdx] hero override | NEEDS-PROOF | YES |
| 40 | _runFlipJackpot / _awardDailyCoinToTraitWinners near-future FLIP draw | contracts/modules/DegenerusGameJackpotModule.sol:_awardDailyCoinToTraitWinners:1629 (lvlPrime = minLevel + keccak(randomWord,FLIP_LEVEL_TAG,i)%range; idx = keccak(randomWord,trait_i,lvlPrime,i)%effectiveLen) | randWord (daily VRF word) via _runFlipJackpot | traitBurnTicket[lvlPrime][trait_i] membership; deityBySymbol virtual entries | CROSS-CONTRACT-SEAM | YES |
| 41 | _awardFarFutureCoinJackpot far-future ticketQueue draw | contracts/modules/DegenerusGameJackpotModule.sol:_awardFarFutureCoinJackpot:1692 (entropy=keccak(rngWord,lvl,FAR_FUTURE_FLIP_TAG); candidate=lvl+5+entropy%95; winner=queue[(entropy>>32)%len]) | rngWord (daily VRF word) via _runFlipJackpot | ticketQueue[far-future key] membership — players hold far-future tickets at sampled levels | CROSS-CONTRACT-SEAM | YES |
| 42 | payDailyFlipJackpot (purchaseLevel==1 path) FLIP jackpot | contracts/modules/DegenerusGameJackpotModule.sol:payDailyFlipJackpot:1524 (bonusTraits=_rollWinningTraits(randWord,true); _runFlipJackpot) | randWord param (daily VRF word) | traitBurnTicket + far-future ticketQueue membership | CROSS-CONTRACT-SEAM | YES |
| 43 | _rollHeroSymbol weighted hero (quadrant,symbol) roll | contracts/modules/DegenerusGameJackpotModule.sol:_rollHeroSymbol:1396 (pick = keccak(entropy,day)%effectiveTotal over dailyHeroWagers[day]) | VRF entropy (randWord / heroEntropy) passed from the daily/terminal roll callers | dailyHeroWagers[day] — the SETTLED wager pool, written by placeDegeneretteBet (ETH bets) keyed by _simulatedDayIndex; day = frozen dailyIdx snapshot | NEEDS-PROOF | YES |
| 44 | _applyHeroResult hero-color sampling | contracts/modules/DegenerusGameJackpotModule.sol:_applyHeroResult:1316 (heroColor = (randomWord >> {0,3,6,9}) & 7) | randomWord (per-roll VRF word: randWord for main, hash2(randWord,BONUS_TRAITS_TAG) for bonus) | none — pure bit-slice of the VRF word; heroQuadrant/heroSymbol come from the (frozen-on-entropy) _rollHeroSymbol result | FROZEN-AT-COMMIT | YES |
| 45 | _processDailyEth / _processBucket / _randTraitTicket ETH winner selection | contracts/modules/DegenerusGameJackpotModule.sol:_processBucket:1147 → _randTraitTicket:1500 (idx = keccak(randomWord,trait,salt,i)%effectiveLen) | effectiveEntropy / entropyState (VRF-derived, chained via keccak(entropyState,traitIdx,share)) | traitBurnTicket[lvl][trait] membership + deityBySymbol[fullSymId] virtual entries | CROSS-CONTRACT-SEAM | YES |
| 46 | _dailyCurrentPoolBps daily pool percentage | contracts/modules/DegenerusGameJackpotModule.sol:_dailyCurrentPoolBps:1839 (seed = keccak(randWord,DAILY_CURRENT_BPS_TAG,counter)) | randWord (daily VRF word) | none — counter is jackpotCounter (advance-driven), pure derivation | FROZEN-AT-COMMIT | YES |
| 47 | _emitDailyWinningTraits / emitDailyWinningTraits bonus-target-level + trait rolls | contracts/modules/DegenerusGameJackpotModule.sol:_emitDailyWinningTraits:1806 (bonusTargetLevel=lvl+1+keccak(randWord,lvl,FLIP_JACKPOT_TAG)%4); emitDailyWinningTraits:1568 | randWord (daily VRF word) | none (event-only derivation) | FROZEN-AT-COMMIT | YES |
| 48 | DegenerusGame.runBafJackpot dispatch + 50/50 split + ticket recirc | contracts/modules/DegenerusGameJackpotModule.sol:runBafJackpot:1911 → _awardJackpotTickets:2010 → _jackpotTicketRoll:2077 (entropy=hash2(entropy,entropy); roundUp uint16(entropy>>200)%TICKET_SCALE) | rngWord param (BAF VRF seed); the Jackpots contract returns winners, then the ticket-recirc legs re-derive entropy | Winner addresses come from the Jackpots contract (BAF leaderboard / ticket samples); the recirc ticket roll has no player input on the seed | CROSS-CONTRACT-SEAM | NO |
| 49 | DegenerusJackpots.runBafJackpot 3rd/4th-slot pick + scatter level targeting | contracts/DegenerusJackpots.sol:runBafJackpot:300 (pick=2+uint8(entropy&1)); :382-386 (targetLvl = lvl±entropy%{3,4,99}) | rngWord param (BAF VRF seed from the Game) | Candidate pool = bafTop leaderboard (PlayerScore[4]) + sampled trait/far-future tickets; players control their BAF coinflip stake (bafPlayer.total) which ranks them on bafTop and as _bafScore in the slices | CROSS-CONTRACT-SEAM | NO |
| 50 | DegenerusJackpots.runBafJackpot far-future BAF draws (slices D/D2) via sampleFarFutureTickets | contracts/DegenerusJackpots.sol:runBafJackpot:318 (farTickets = degenerusGame.sampleFarFutureTickets(entropy)) then best/second by _bafScore | rngWord-derived entropy (hash2 chain) passed to the Game's sampleFarFutureTickets sampler | Candidate pool = far-future ticketQueue membership (Game-side); ranking by _bafScore = player's BAF coinflip stake at this bracket epoch | CROSS-CONTRACT-SEAM | NO |
| 51 | DegenerusJackpots.runBafJackpot coinflip-leaderboard slice (top-bettor 24h) | contracts/DegenerusJackpots.sol:runBafJackpot:284 (coin.coinflipTopLastDay()) | NOT a VRF read — deterministic external view of BurnieCoinflip's 24h coinflip leaderboard (coinflipTopByDay) | coinflip leaderboard membership (player's coinflip stake over the last-day window) | CROSS-CONTRACT-SEAM | MISCLASSIFIED |
| 52 | redemption lootbox seed (hash2 over rngWordForDay(day+1)) | contracts/sDGNRS.sol:_claimRedemptionFor:916-920 (reads at :916, derives at :917, consumes via game.resolveRedemptionLootbox at :920) | game.rngWordForDay(day + 1) — the Game's per-wall-clock-day VRF word for the resolving day (day+1) | player address is folded into the seed preimage (hash2(rngWord, uint160(player))) — but the address is fixed (the (player,day) claim slot was written at submit) and cannot be chosen at claim time; lootboxEth derives from claim.ethValueOwed*roll (frozen at submit + resolve); the day+1 word is the resolving word. | CROSS-CONTRACT-SEAM | NO |
| 53 | FLIP-escrow leg day-result read (getCoinflipDayResult(day+1)) | contracts/sDGNRS.sol:_claimRedemptionFor:888-893 (read at :888, payout at :892-893) | coinflip.getCoinflipDayResult(day + 1) → Coinflip._dayResult → coinflipDayResultPacked[(day+1)>>5] lane — the stored 3-state byte derived from the day+1 VRF word inside processCoinflipPayouts | claim.flipEscrow (whole-token slice) and the (player,day) slot are frozen at submit; the consumed day-result is the resolving day+1 coinflip — same word every non-redeeming holder's backing rides. No claim-time player-selectable input steers rewardPercent/flipWon. | CROSS-CONTRACT-SEAM | MISCLASSIFIED |
| 54 | submit-window VRF-word presence gate (rngWordForDay(currentPeriod)==0) | contracts/sDGNRS.sol:_submitGamblingClaimFrom:1036 | game.rngWordForDay(currentPeriod) — the current wall-clock day's VRF word (read for non-zeroness only) | none — the word VALUE is not consumed into any derived outcome; only its presence (drawn vs not) gates admission. currentPeriod is a pure block.timestamp function (GameTimeLib). | FROZEN-AT-COMMIT | NO |
| 55 | coinflip day-result derivation (win bit + reward-percent roll) | contracts/Coinflip.sol:processCoinflipPayouts:859-889 (seedWord :859, reward roll :864-883, win bit :886, store :889) | rngWord parameter — the Game's daily VRF word forwarded from the advanceGame/VRF-callback stack (onlyDegenerusGameContract); epoch is the day index being resolved | none player-selectable in the derivation: rngWord is the VRF word, epoch the resolved day, bonus is caller-precomputed from frozen protocol state (level / first-jackpot-day, not a player-flippable flag). The bounty credit at :902-909 pays bountyOwedTo, but that owner was latched earlier under the !rngLocked gate. | FROZEN-AT-COMMIT | YES |
| 56 | win/loss claim consumer (_dayResult per cursor day) | contracts/Coinflip.sol:_processClaimDays:522-578 (reads _dayResult at :522, payout/carry/winningBafCredit :542-578) | _dayResult(cursor) → coinflipDayResultPacked[cursor>>5] lane — the stored VRF-derived result for each resolved day in the claim window | player address (whose claim) and per-day stake (_flipStake[cursor][player], set at deposit on a future day before the word was drawn) — stake is frozen for the resolved day; the consumed result byte was written by processCoinflipPayouts for a past day. No claim-time input steers win/rewardPercent. | FROZEN-AT-COMMIT | YES |
| 57 | getCoinflipDayResult external view (cross-contract day-result accessor) | contracts/Coinflip.sol:getCoinflipDayResult:367-369 → _dayResult:1193-1197 | coinflipDayResultPacked[day>>5] lane — the stored VRF-derived 3-state byte (0 unresolved / 1 loss / 50..156 win-reward%) | day argument is caller-supplied but only indexes into already-resolved frozen results; no value is re-derived from a live word here. | FROZEN-AT-COMMIT | MISCLASSIFIED |
| 58 | coinflip leaderboard read (coinflipTopByDay via coinflipTopLastDay) | contracts/Coinflip.sol:coinflipTopLastDay:1103-1112 (reads coinflipTopByDay[flipsClaimableDay] at :1110); written by _updateTopDayBettor:1284-1295 | coinflipTopByDay[day] PlayerScore (highest stakeScore for the day) — NOT itself a VRF-derived value, but the leaderboard membership consumed by a downstream VRF-seeded BAF/jackpot winner-select | player address + stake size determine leaderboard membership (a player can steer who is top.player by out-staking). flipsClaimableDay is set from the VRF-resolved epoch. The winner-select that consumes this (samples top bettor against a VRF word) is external (DegenerusJackpots), so player-controllable membership is the freeze-relevant input. | CROSS-CONTRACT-SEAM | MISCLASSIFIED |
| 59 | processTicketBatch entropy SLOAD (current-level trait gen) | contracts/modules/DegenerusGameMintModule.sol:processTicketBatch:870 | lootboxRngWordByIndex[index-1] (index from lootboxRngPacked LR_INDEX field; the prior settled per-index VRF word) | lvl (function param, but the only external callers are AdvanceModule advanceGame-stack delegatecalls); per-entry queue[idx] player address and ticketsOwedPacked owed value enter the keccak baseKey downstream — not selectable at consumption time | FROZEN-AT-COMMIT | YES |
| 60 | _raritySymbolBatch 3-input keccak (trait roll) | contracts/modules/DegenerusGameMintModule.sol:_raritySymbolBatch:745 | entropyWord (== rngWordByDay[day] for the future path, or lootboxRngWordByIndex[index-1] for the current path) via the two outer loops | player address, lvl, queueIdx, owed are all baked into baseKey — but all are loop-locals fixed at the start of the advanceGame resolution; none is selectable after the word is knowable | FROZEN-AT-COMMIT | YES |
| 61 | _rollRemainder fractional-ticket roll | contracts/modules/DegenerusGameMintModule.sol:_rollRemainder:831 | entropy (same VRF word threaded from the two outer loops) plus rollSalt = baseKey (carries player address) | player address (in rollSalt/baseKey bits 191-32) and `rem` (fractional remainder from ticketsOwedPacked) — both fixed queue state, not chosen after the word | FROZEN-AT-COMMIT | YES |
| 62 | processFutureTicketBatch entropy param (future/far-future trait gen) | contracts/modules/DegenerusGameMintModule.sol:processFutureTicketBatch:540 (param), :609 / :656 (_rollRemainder), :640 (_raritySymbolBatch) | rngWordByDay[day] — supplied by caller AdvanceModule._processFutureTicketBatch (rngGate-returned rngWord) | none selectable at consumption — lvl param + per-entry queue membership are advanceGame-controlled; far-future queue writes from purchase paths are rngLockedFlag-gated (Storage:572) during the window | FROZEN-AT-COMMIT | YES |
| 63 | _farFutureSeed salvage seed derivation | contracts/modules/DegenerusGameMintStreakUtils.sol:_farFutureSeed:252-255 | rngWordByDay[_simulatedDayIndex()-1] — the SETTLED prior wall-clock-day VRF word (_simulatedDayIndex = GameTimeLib.currentDayIndex, NOT dailyIdx) | player address (preimage) and call-timing (which wall-clock day → which prior-day word). The prior-day word is public/settled and thus knowable BEFORE the player commits the sell — the player can read their exact seed first. NO level/quantity steers the seed (those are face-value only). | NEEDS-PROOF | NO |
| 64 | _quoteFarFutureSwap jitter + ticket-share derivation | contracts/modules/DegenerusGameMintStreakUtils.sol:_quoteFarFutureSwap:183-184,203 | seed (== _farFutureSeed output, derived from rngWordByDay[currentDay-1]) | levels[]/quantities[] choose WHICH lines to value, but they only scale face value linearly — they do NOT enter the seed, so they cannot steer jitter/share. The only seed inputs are player address + prior-day word (knowable at call). | NEEDS-PROOF | NO |
| 65 | _quoteFarFutureFlipSplit ETH/FLIP target slice | contracts/modules/DegenerusGameMintStreakUtils.sol:_quoteFarFutureFlipSplit:233 | seed (== _farFutureSeed output, derived from rngWordByDay[currentDay-1]) | buyer FLIP inventory (coin.balanceOfSpendableForSalvage(buyer)) — buyer is sDGNRS or vault, resolved by _resolveSalvageBuyer, not the seller; cashWei from upstream quote. Player (seller) address is in the seed but not chosen post-word. | CROSS-CONTRACT-SEAM | NO |
| 66 | handleGameOverDrain rngWordByDay substitution read | contracts/modules/DegenerusGameGameOverModule.sol:handleGameOverDrain:93 | rngWordByDay[day] (day param passed by AdvanceModule._handleGameOverPath after _gameOverEntropy/_applyDailyRng wrote it) | day param is advanceGame-controlled; the word is written by _applyDailyRng/_getHistoricalRngFallback BEFORE this delegatecall and is single-shot/immutable for the body. No player-selectable preimage; deity refund FIFO + pool reads are not VRF-derived. | FROZEN-AT-COMMIT | YES |
| 67 | lootbox enqueue not-yet-fulfilled producer guard | contracts/modules/DegenerusGameMintModule.sol:_buyPresaleBox:1951 (also producer index reads at :1584/:1949 and WhaleModule:850/:887) | lootboxRngWordByIndex[index] (read only to assert == 0) | index = current lootboxRngPacked LR_INDEX; buyer enqueues into boxPlayers[index]. This is producer-side: blocks committing a box once the word is knowable. | FROZEN-AT-COMMIT | MISCLASSIFIED |

## RNGNET-03 — Stale-anchor reconciliation (the trusted docs are a pre-rename historical subset)

- **[.planning/RNGLOCK-CATALOG.md (S-54..S-62, §11, §12 headers)]** Consumers/slots owned by 'BurnieCoinflip' (currentBounty S-54, bountyOwedTo S-55, coinflipDayResult[flipDay] S-62) and 'StakedDegenerusStonk' (S-14/15/17/56/57/58/59/60/61) and 'DegenerusStonk'
  - **now:** No contract named BurnieCoinflip / StakedDegenerusStonk / DegenerusStonk / BurnieCoin exists at HEAD (grep returns zero in contracts/). The coinflip contract is Coinflip.sol; staked equity is sDGNRS.sol; equity is DGNRS.sol; the renamed FLIP token is FLIP.sol. Every §11/§12 slot owner name and the S-62/§16 'BurnieCoinflip' attributions are dead identifiers.
- **[.planning/RNGLOCK-CATALOG.md S-62 + §12 + §16 C-9]** S-62 coinflipDayResult[flipDay] is a 'mapping(uint32 => CoinflipDayResult) internal' STRUCT mapping; consumer enumerated at the stored flipDay struct field, with the redemption FLIP-escrow read keyed via redemptionPeriods[claim.periodIndex].flipDay (sStonk:649)
  - **now:** Current code stores results in coinflipDayResultPacked (mapping(uint24=>uint256), 32 days/slot, 8-bit lanes) at Coinflip.sol:176; read via _dayResult:1193 (b=uint8(packed[day>>5]>>((day&31)*8))). There is NO stored flipDay struct field. The FLIP-escrow leg reads getCoinflipDayResult(day+1) at sDGNRS.sol:888 (day-keyed, not periodIndex.flipDay). The §16/C-9 enumerated call site sStonk:649 is stale; the live consumer is un-netted.
- **[.planning/RNGLOCK-CATALOG.md §12 headline #1 hazard + S-56/S-57/S-58/S-59/S-60/S-61]** Cross-day re-roll exploit: redemptionPeriodIndex (S-56) not advanced inside resolveRedemptionPeriod; attacker burns 1 wei on a future day to re-arm pendingRedemptionEthBase (S-57) and force overwrite redemptionPeriods[period].roll — ~19% positive EV per iteration. resolveRedemptionPeriod at sStonk:585.
  - **now:** The entire period-index model is redesigned and the exploit is remediated. redemptionPeriodIndex / pendingRedemptionEthBase / pendingRedemptionBurnieBase no longer exist. Current: resolveRedemptionPeriod(uint16 roll, uint24 dayToResolve) at sDGNRS.sol:742 writes redemptionPeriods[dayToResolve]=roll (plain uint16 keyed by DAY) then delete pendingByDay[dayToResolve] — no re-resolution. Re-arm is blocked by the _pendingResolveDay single-pool sentinel (PriorDayUnresolved at sDGNRS:1042) + BurnsBlockedBeforeDailyRng submit gate (sDGNRS:1036). S-56..S-61 cross-contract slots are stale.
- **[audit/v30-RNGLOCK-STATE-MACHINE.md (Clear-Site RNGLOCK-239-C-02 / Path RNGLOCK-239-P-008)]** updateVrfCoordinatorAndSub is a clear-site that executes 'rngLockedFlag = false' at AdvanceModule:1635 — one of THREE clear-sites; exactly TWO 'rngLockedFlag = false' SSTOREs at :1635 and :1676.
  - **now:** updateVrfCoordinatorAndSub (now at AdvanceModule.sol:1755) DELIBERATELY KEEPS rngLockedFlag=true on the daily-in-flight branch (':1774 else if (rngLockedFlag) { // Daily in flight: KEEP rngLockedFlag=true }') and contains NO 'rngLockedFlag = false' SSTORE. The clear-site C-02 is REMOVED. The sole clear is now _unlockRng at AdvanceModule.sol:1799 (rngLockedFlag = false). The set is at :1696. The v30 3-clear-site / 9-path state-machine structure and the :1635/:1676 line anchors are stale; rotation now re-issues on the new coordinator preserving the lock.
- **[.planning/RNGLOCK-CATALOG.md §12 + headline #1 + audit/v30-* (AdvanceModule:1187 short-circuit)]** Same-day re-resolution blocked by the rngWordByDay[day] short-circuit at AdvanceModule:1187.
  - **now:** The rngGate rngWordByDay[day]==0 short-circuit is at AdvanceModule.sol:638 at HEAD (with related gap checks at :185/:352/:392). Line :1187 is stale (off by ~550 lines).
- **[.planning/RNGLOCK-CATALOG.md §16 C-9 / §12 mention of currentDayView()]** Catalog redemption/coinflip seam reasoning relies on game.currentDayView() cross-calls from sDGNRS/Coinflip/Jackpots.
  - **now:** currentDayView() the view function STILL EXISTS at DegenerusGame.sol:522. But the cross-CALLs were eliminated: DegenerusJackpots:483/498, Coinflip:1250, sDGNRS:1026-1027 now compute the wall-clock day LOCALLY via GameTimeLib.currentDayIndex() (comments state 'identical to game.currentDayView() without the CALL'). Any catalog reasoning that assumes a live cross-call to currentDayView() at these seam sites is stale (the value is now local-pure).
- **[.planning/RNGLOCK-CATALOG.md §1-§13 line anchors (D-298-CONSUMER-LIST-01)]** §1 payDailyJackpot @ :339; §2 payDailyJackpotCoinAndTickets @ :596; §3 runTerminalJackpot @ :278; §4 runTerminalDecimatorJackpot @ :755; §5 GameOverModule rngWordByDay substitution @ :100; §6 resolveRedemptionLootbox @ :707; §7 _resolveLootboxCommon/_resolveLootboxRoll @ :960/:1623; §8 _resolveLootboxDirect @ :797/:594; §10 _raritySymbolBatch @ :563-565 + processFutureTicketBatch @ :385 + processTicketBatch @ :686.
  - **now:** All §1-§13 line anchors are off by tens-to-hundreds of lines at HEAD: payDailyJackpot:298, payDailyJackpotCoinAndTickets:555, runTerminalJackpot:239, handleGameOverDrain rngWord SLOAD:93, resolveRedemptionLootbox/_resolveRedemptionChunk:951/970, _resolveLootboxCommon via _openLootBoxLegWith:560, _resolveLootboxDirect:883 + inline _resolveFullTicketBet:676, _raritySymbolBatch:745, processFutureTicketBatch:540, processTicketBatch:870. The catalog cites itself as 'stale :686' etc. — every consumer line anchor needs recalibration.
- **[.planning/RNGLOCK-CATALOG.md §16 V-179 bit-allocation map + audit/v30-FREEZE-PROOF.md INV-237 row labels]** Future-take variance attributed to _takeFuturePrizePoolSlice; lootbox consumers named openLootBox / openBurnieLootBox; 13-list and INV-237 rows reference 'Burnie' lootbox legs and EntropyLib XOR-shift PRNG roll names.
  - **now:** _takeFuturePrizePoolSlice does not exist; the live future-take variance is in _consolidatePoolsAndRewardJackpots:869,883-884. The 'Burnie' lootbox leg is now the FLIP leg (_resolvePresaleBox:751, _openLootBoxLegWith:560); 'openBurnieLootBox' is gone (openHumanBoxes:681/705). All 'Burnie'-prefixed consumer/slot labels in §7/§16 and INV-237-127/128/137 are renamed-away.
- **[audit/v30-RNGLOCK-STATE-MACHINE.md + audit/v30-FREEZE-PROOF.md (audit baseline)]** Both docs are anchored to HEAD 7ab515fe (v30.0 / v29.0 1646d5af contract tree) and assert the 146-row INV-237 universe + 13-row rngLock state machine are current.
  - **now:** Subject is byte-frozen at contracts/ tree 0dd445a6 / commit 42c8e9c6 — many milestones beyond v30. The afking subsystem (GameAfkingModule, resolveAfkingBox), box-spin Degenerette features, FLIP survival flip, presale box, salvage _farFutureSeed, packed coinflipDayResultPacked, and the day-keyed redemption model are all post-v30. The v30 docs' consumer universe is a strict SUBSET of the current net and their slot/contract names are pre-rename; treat both as historical baselines, not current truth.

## Open-freeze consumers → Phase 412 (seams) / 413 (input-selection + fallback)

These 31 are NEEDS-PROOF or CROSS-CONTRACT-SEAM — the freeze is not provable from a single file and
is the hunt material for 412/413. **1 is already MUTABLE-INPUT (a candidate finding):** the deity boon.

- historical-RNG prevrandao fallback word producer @ DegenerusGameAdvanceModule.sol:_getHistoricalRngFallback:1394-1411 (NEEDS-PROOF — validator 1-bit prevrandao bias, gameover-only)
- redemption roll (gameover prevrandao-fallback path) @ DegenerusGameAdvanceModule.sol:_gameOverEntropy:1351-1355 (NEEDS-PROOF — prevrandao + cross-contract redemption magnitude)
- redemption roll (rngGate normal path) @ DegenerusGameAdvanceModule.sol:rngGate:1255-1258 (CROSS-CONTRACT-SEAM — sDGNRS redemption magnitude/sentinel)
- redemption roll (gameover normal path) @ DegenerusGameAdvanceModule.sol:_gameOverEntropy:1316-1320 (CROSS-CONTRACT-SEAM)
- rngWordForDay public getter seam @ DegenerusGame.sol:rngWordForDay:2089-2091 (CROSS-CONTRACT-SEAM — caller-chosen frozen day; resolved in sDGNRS/Coinflip)
- BAF trait-ticket sampler @ DegenerusGame.sol:sampleTraitTicketsAtLevel:2314-2333 (CROSS-CONTRACT-SEAM — player-mutable traitBurnTicket pool; reached from DegenerusJackpots.runBafJackpot)
- BAF far-future ticket sampler @ DegenerusGame.sol:sampleFarFutureTickets:2341-2374 (CROSS-CONTRACT-SEAM — player-mutable ticketQueue pool)
- _rollLootboxBoons boon roll @ DegenerusGameLootboxModule.sol:_rollLootboxBoons:1456 (NEEDS-PROOF — decWindowOpen/deityPassOwners.length/mintPacked_/lazyPassValue reshape roll->boonType mapping co-resident with the VRF read)
- issueDeityBoon -> _deityBoonForSlot deity boon roll @ DegenerusGameLootboxModule.sol:_deityBoonForSlot:2319 (MUTABLE-INPUT — deity picks recipient/slot AFTER reading the public day word; route to Phase 412/413)
- resolveRedemptionLootbox -> _resolveRedemptionChunk seed @ DegenerusGameLootboxModule.sol:_resolveRedemptionChunk:970 (CROSS-CONTRACT-SEAM — sDGNRS must have frozen the redemption-period roll at submit)
- runTerminalJackpot trait roll @ DegenerusGameJackpotModule.sol:runTerminalJackpot:239 (CROSS-CONTRACT-SEAM — multi-tx gameover window where rngWordByDay[day]!=0 while gameOver==false; depends on GameOverModule drain ordering)
- payDailyJackpot trait pair + carryover @ DegenerusGameJackpotModule.sol:payDailyJackpot:298 (NEEDS-PROOF — dailyHeroWagers[dailyIdx] written by placeDegeneretteBet without rngLockedFlag gate; cross-cycle dailyIdx mid-update)
- payDailyJackpot purchase-phase path @ DegenerusGameJackpotModule.sol:payDailyJackpot:468-541 (NEEDS-PROOF — same dailyHeroWagers freshness window)
- _runEarlyBirdLootboxJackpot bonus-trait winner draw @ DegenerusGameJackpotModule.sol:_runEarlyBirdLootboxJackpot:621 (CROSS-CONTRACT-SEAM — traitBurnTicket membership)
- payDailyJackpotCoinAndTickets @ DegenerusGameJackpotModule.sol:payDailyJackpotCoinAndTickets:555 (NEEDS-PROOF — dailyHeroWagers reader/writer day-index race)
- _runFlipJackpot near-future FLIP draw @ DegenerusGameJackpotModule.sol:_awardDailyCoinToTraitWinners:1629 (CROSS-CONTRACT-SEAM — traitBurnTicket[lvlPrime] membership)
- _awardFarFutureCoinJackpot far-future ticketQueue draw @ DegenerusGameJackpotModule.sol:_awardFarFutureCoinJackpot:1692 (CROSS-CONTRACT-SEAM — far-future ticketQueue NOT double-buffered; rngLockedFlag runtime revert is the de-facto mitigation)
- payDailyFlipJackpot @ DegenerusGameJackpotModule.sol:payDailyFlipJackpot:1524 (CROSS-CONTRACT-SEAM — far/near coin draws over ticket pools)
- _rollHeroSymbol weighted hero roll @ DegenerusGameJackpotModule.sol:_rollHeroSymbol:1396 (NEEDS-PROOF — weights read player-controllable dailyHeroWagers[day] written without rngLockedFlag gate)
- _processDailyEth/_randTraitTicket ETH winner selection @ DegenerusGameJackpotModule.sol:_randTraitTicket:1500 (CROSS-CONTRACT-SEAM — traitBurnTicket membership)
- runBafJackpot dispatch + ticket recirc @ DegenerusGameJackpotModule.sol:runBafJackpot:1911 (CROSS-CONTRACT-SEAM — winner SET computed in DegenerusJackpots)
- DegenerusJackpots.runBafJackpot slot pick + scatter @ DegenerusJackpots.sol:runBafJackpot:300,382-386 (CROSS-CONTRACT-SEAM — bafTop leaderboard + sampled ticket pools; SUSPECTED-MISSING CONFIRMED)
- DegenerusJackpots.runBafJackpot far-future draws @ DegenerusJackpots.sol:runBafJackpot:318 (CROSS-CONTRACT-SEAM — Game ticketQueue under onlyGame advance-stack call)
- DegenerusJackpots.runBafJackpot coinflip-leaderboard slice @ DegenerusJackpots.sol:runBafJackpot:284 (CROSS-CONTRACT-SEAM — deterministic top-bettor read-timing seam)
- redemption lootbox seed hash2 over rngWordForDay(day+1) @ sDGNRS.sol:_claimRedemptionFor:916-920 (CROSS-CONTRACT-SEAM — freeze rests on Game rngWordForDay(day+1) undrawn at submit)
- FLIP-escrow leg day-result read getCoinflipDayResult(day+1) @ sDGNRS.sol:_claimRedemptionFor:888-893 (CROSS-CONTRACT-SEAM — un-netted re-shaped consumer; catalog points at stale sStonk:649)
- coinflip leaderboard read coinflipTopByDay @ Coinflip.sol:coinflipTopLastDay:1103-1112 (CROSS-CONTRACT-SEAM — membership is preimage to external BAF winner-select)
- _farFutureSeed salvage seed @ DegenerusGameMintStreakUtils.sol:_farFutureSeed:252-255 (NEEDS-PROOF — knowable-at-commit prior-day word + address-in-preimage; PANEL-FLAGGED)
- _quoteFarFutureSwap jitter + ticket-share @ DegenerusGameMintStreakUtils.sol:_quoteFarFutureSwap:183-184,203 (NEEDS-PROOF — transitive salvage seed)
- _quoteFarFutureFlipSplit ETH/FLIP split @ DegenerusGameMintStreakUtils.sol:_quoteFarFutureFlipSplit:233 (CROSS-CONTRACT-SEAM — buyer-owned FLIP balance via coin.balanceOfSpendableForSalvage)
- _awardDegeneretteDgnrs reward @ DegenerusGameDegeneretteModule.sol:_awardDegeneretteDgnrs:1210 (CROSS-CONTRACT-SEAM — s-gate frozen but reward reads drainable sdgnrs.poolBalance(Reward); §8 D-28 VIOLATION still live)

---

## Council layer (RNGNET-02 — the cross-model primary-finder net)

Two independent council legs re-derived the net read-only against HEAD (tree `0dd445a6` verified intact after).

### Gemini-flash leg
- **Completeness:** independently enumerated ~95 candidate sites; **0 consumers missing** from the 67 — net judged comprehensive.
- **Freeze:** concurs with every open-freeze classification (deity-boon MUTABLE-INPUT, dailyHeroWagers un-gated-write window, BAF/jackpot mutable pools, sDGNRS day+1 seam, `_farFutureSeed`, drainable reward pool, prevrandao fallback).

### Codex leg — FOUND 5 CONSUMERS THE CLAUDE NET + GEMINI MISSED
The Claude workflow's 4 clusters never assigned `DegenerusGameDecimatorModule.sol`, `DegenerusQuests.sol`, or
`DeityBoonViewer.sol`. Codex re-derived independently and surfaced their consumers — net expands **67 → 72**:

| # | Consumer (codex-found) | Site | Source | Freeze (routing) |
|---|---|---|---|---|
| 68 | Normal decimator jackpot subbucket derivation | `DegenerusGameDecimatorModule.sol:224,245,410,579` | `rngWord` -> winning subbuckets; claims read `decBucketOffsetPacked`/`round.rngWord` | **NEEDS-PROOF** -> 412 (verify bucket membership value-sealed-at-bucketing BEFORE the word) |
| 69 | Terminal decimator jackpot subbucket derivation | `GameOverModule.sol:177` -> `DecimatorModule.sol:978,994,1100` | `rngWord` -> terminal subbuckets | **NEEDS-PROOF** -> 412 |
| 70 | Daily quest roll (slot-1 quest type) | `AdvanceModule.sol:1239` -> `DegenerusQuests.sol:381,395,1719` | `currentWord` -> rollDailyQuest type select | **NEEDS-PROOF** -> 412 (verify quest type not player-biasable for gain; runs in advanceGame) |
| 71 | Level quest roll | `AdvanceModule.sol:537` -> `DegenerusQuests.sol:2014` | `rngWord` -> rollLevelQuest type select | **NEEDS-PROOF** -> 412 |
| 72 | DeityBoonViewer preview re-derive | `DeityBoonViewer.sol:99,118` | re-derives slot boons from `deityBoonData` dailySeed (view) | mirror of the deity-boon **MUTABLE-INPUT** (issuance side `_deityBoonForSlot`) |

Codex also flagged presence/control-flow gates (requestLootboxRng day-word gate Advance:1090, isRngFulfilled
Game:2102, advance gap-clamp Advance:185, Degenerette unworded-index guard Degenerette:533) — readiness probes,
not value draws; freeze-enforcing, not consumers.

### Council DIVERGENCES → priority adjudication in Phase 412
- **`dailyHeroWagers` freshness window:** gemini = NEEDS-PROOF (placeDegeneretteBet writes with no rngLockedFlag gate); codex = "protected by the day offset" (bets affect a FUTURE resolution, not the now-resolving word). **412 RNGSEAM/hero adjudicates the this-cycle-vs-next-cycle offset.**
- **sDGNRS day+1 redemption seam:** gemini = open seam (timing/knowledge of day+1 word); codex = pinned by the BurnsBlockedBeforeDailyRng submit gate + day+1 undrawn at submit. **412 RNGSEAM-01/02 adjudicates the claimRedemption(player,day) argument-selection.**

### Net verdict (RNGNET)
- **72 council-verified VRF consumers** (catalog enumerated 13). 0 missing after both legs (codex closed the 5-file gap).
- **Triple-confirmed candidate finding:** `_deityBoonForSlot` MUTABLE-INPUT (deity picks recipient/slot AFTER the public day word -> seed at `Lootbox:1138/1149/2319`). Highest-priority 412/413 adversarial-verify target.
- **~35 open-freeze consumers** route to 412 (seams) / 413 (input-selection + fallback), including the 2 council divergences as priority adjudications.
- **9 stale anchors** reconciled (the catalog + v30 docs are a pre-rename historical SUBSET; treat as historical, not current truth).
