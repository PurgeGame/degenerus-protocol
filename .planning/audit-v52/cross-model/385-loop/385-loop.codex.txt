Read only `git show c4d48008:...`; ignored working tree.

**Findings**
1. PROPERTY: mid-day lootbox RNG is public before manual open reads live player/game state.
Reachable sequence: player buys lootbox at index `i`; `requestLootboxRng()` reserves `i` and increments `LR_INDEX` without setting `rngLockedFlag` (`contracts/modules/DegenerusGameAdvanceModule.sol:1064`, `:1132`, `:1140`); callback writes `lootboxRngWordByIndex[i]` (`:1814`); player then changes live pass/boon/level state and opens manually. `openLootBox` reads live `level + 1` (`contracts/modules/DegenerusGameLootboxModule.sol:512`) and `_rollLootboxBoons` reads live `boonPacked`, `level`, `mintPacked_`, and `deityPassOwners.length` (`:1222`, `:1230`, `:1237`, `:1383`). Example mutation: `purchaseDeityPass` is only blocked by `rngLockedFlag` (`contracts/modules/DegenerusGameWhaleModule.sol:558`), which is false for mid-day RNG, and changes both `mintPacked_` and `deityPassOwners` (`:611`, `:617`). Freshness failure: open-level / boon eligibility are not snapshotted at lootbox commitment. This is distinct from the already-found auto-open off-by-one; the manual resolver itself consumes live SLOADs. SEVERITY: HIGH.

2. PROPERTY: daily coinflip payout modifier reads mutable presale state after the daily VRF word is public.
Reachable sequence: daily VRF callback stores `rngWordCurrent` while lock remains active (`contracts/modules/DegenerusGameAdvanceModule.sol:1810`); before the next `advanceGame` consumes it, a player can call ETH `purchase` because the public wrapper has no actual lock guard (`contracts/DegenerusGame.sol:640`) and `_purchaseForWith` only checks liveness (`contracts/modules/DegenerusGameMintModule.sol:1156`). A lootbox purchase crossing `LOOTBOX_PRESALE_ETH_CAP` clears `PS_ACTIVE` (`:1271`, `:1276`). `rngGate` then calls `coinflip.processCoinflipPayouts` (`contracts/modules/DegenerusGameAdvanceModule.sol:1241`), which reads `game.lootboxPresaleActiveFlag()` (`contracts/BurnieCoinflip.sol:790`; view at `contracts/DegenerusGame.sol:2523`). Freshness failure: the +6pp presale bonus flag is not frozen at request time, and the word is already visible before consumption. New stake cannot be added to the resolving day because `_targetFlipDay()` is wall-day + 1 (`contracts/BurnieCoinflip.sol:1060`), so this is a payout-parameter manipulation, not bet-after-randomness. SEVERITY: MEDIUM.

3. PROPERTY: FC1 confirmed, pending mid-day ticket RNG can stall the next `advanceGame`.
Reachable sequence: `requestLootboxRng()` with a nonempty write ticket queue swaps write to read and sets `LR_MID_DAY` (`contracts/modules/DegenerusGameAdvanceModule.sol:1109`, `:1115`, `:1116`). If the callback has not landed by the next day, `advanceGame` hits the new-day ticket drain, sees `lootboxRngWordByIndex[LR_INDEX - 1] == 0`, sees `rngWordCurrent == 0`, and reverts before `rngGate` (`:270`, `:277`, `:279`). The retry path exists after `MIDDAY_RNG_RETRY_TIMEOUT` (`:1157`, `:1159`) and does not increment the index, so this is a temporary liveness stall, not a permanent gas brick. SEVERITY: MEDIUM/LOW.

**Bounded Loops**
No new `advanceGame`-chain unbounded loop found.

Verified bounded:
- `_backfillOrphanedLootboxIndices`: syntactically backward, but structurally at most one unfilled reserved index under the frozen code: mid-day requests require `rngRequestTime == 0` (`contracts/modules/DegenerusGameAdvanceModule.sol:1077`), retries do not increment (`:1177`), coordinator rotation preserves index (`:1756`), and fulfillment fills `LR_INDEX - 1` (`:1815`).
- `_backfillGapDays`: hard cap 120 (`contracts/modules/DegenerusGameAdvanceModule.sol:1842`).
- Deity refunds: loop over `deityPassOwners.length` (`contracts/modules/DegenerusGameGameOverModule.sol:106`), capped by 32 unique symbols in `purchaseDeityPass` (`contracts/modules/DegenerusGameWhaleModule.sol:560`, `:617`).
- Subscriber stage: active set cap `SUBSCRIBER_CAP = 1000` (`contracts/modules/GameAfkingModule.sol:188`, enforced at `:618`), per-call weight budget 500 (`contracts/modules/DegenerusGameAdvanceModule.sol:158`, `:774`), and every branch advances cursor/weight before persisting (`contracts/modules/GameAfkingModule.sol:1144`, `:1328`).
- Ticket batches are write-budgeted (`contracts/modules/DegenerusGameMintModule.sol:424`, `:693`); redemption lootbox chunk loop is bounded by the 160 ETH daily base cap and 175% roll, max 28 chunks (`contracts/StakedDegenerusStonk.sol:926`, `:713`; loop at `contracts/DegenerusGame.sol:2040`).

**Other Checks**
- FC5: no finding. `TraitsGenerated` is slimmed, but binding is still on-chain: `_raritySymbolBatch` derives traits from `baseKey`, `entropyWord`, and `groupIdx`; current-ticket entropy comes from `lootboxRngWordByIndex[LR_INDEX - 1]` (`contracts/modules/DegenerusGameMintModule.sol:690`, `:800`).
- FC6: no finding. Coordinator swap with mid-day pending reissues the mid-day request while preserving `LR_INDEX`/`LR_MID_DAY` (`contracts/modules/DegenerusGameAdvanceModule.sol:1756`); fulfillment writes the same reserved index (`:1815`). Zero gap days is correct when no full daily gap exists.
- Daily RNG nudges are frozen: `reverseFlip` is blocked during daily lock (`contracts/DegenerusGame.sol:2336`), and `_applyDailyRng` consumes then clears `totalFlipReversals` (`contracts/modules/DegenerusGameAdvanceModule.sol:1887`, `:1893`).
- Degenerette indexed RNG is frozen: placement requires the index word be zero and snapshots index/activity (`contracts/modules/DegenerusGameDegeneretteModule.sol:502`, `:525`); resolution reads that stored index (`:650`, `:654`).
- Jackpot hero wagers are frozen through `dailyIdx`: jackpot reads `dailyHeroWagers[dailyIdx]`, and `dailyIdx` only updates at unlock; same-day bets write the wall-day slot (`contracts/modules/DegenerusGameJackpotModule.sol:1393`; `contracts/modules/DegenerusGameDegeneretteModule.sol:542`).
