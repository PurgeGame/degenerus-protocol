I found two concrete issues at frozen `c4d48008`.

**Finding 1**
PROPERTY: `advanceGame()` must not compose two heavy stages in one tx.

Reachable pre-state / call sequence: jackpot phase or `lastPurchaseDay` daily VRF request stalls for 121+ days; `rngWordCurrent` is later fulfilled; `dailyIdx + 1` has no `rngWordByDay`; 1000 normal AfKing subscribers existed before the lock, their boxes are already opened, and 1000 self-funded subscribers withdraw funding during the lock via `withdrawAfkingFunding()` (`contracts/DegenerusGame.sol:1635`). First `advanceGame()` evicts 500 and breaks. Second `advanceGame()` evicts the remaining 500, reaches set end, then falls through into `rngGate()` and performs the 120-day gap backfill.

Heavy stage: final subscriber chunk falls through at `contracts/modules/DegenerusGameAdvanceModule.sol:321-337`; gap backfill runs at `contracts/modules/DegenerusGameAdvanceModule.sol:1216-1237` and is only broken after the work at `:363-365`. The backfill loop is capped at 120 at `:1834-1855`. Subscriber evict/funding-kill path is `contracts/modules/GameAfkingModule.sol:1267-1295`.

Gas mechanism: the stage rollup already measures/records the binding all-evict subscriber chunk at about `13.6M` and the 120-day gap-backfill advance at about `7.3M`; composed, this is about `20.9M`, well above `16,777,216`. The existing gap decouple prevents `gapBackfill + jackpot`, but not `finalSubscriberChunk + gapBackfill`.

SEVERITY: HIGH, gas-DoS of `advanceGame()`.

**Finding 2**
PROPERTY: due, not-rng-locked `advanceGame()` should not permanently revert.

Reachable pre-state / call sequence: in `lastPurchaseDay` or jackpot phase, new day is due, no request is in flight (`rngRequestTime == 0`, `rngLockedFlag == false`), and VRF request submission reverts because coordinator/subscription/keyHash/funding is broken. `advanceGame()` reaches `rngGate()` and calls `_requestRng()`.

Heavy stage / line: `rngGate()` requests fresh RNG at `contracts/modules/DegenerusGameAdvanceModule.sol:1275-1277`; `_requestRng()` hard-bubbles coordinator failure at `:1611-1623`. The fallback liveness path is disabled because `_livenessTriggered()` returns false for `lastPurchaseDay || jackpotPhaseFlag` at `contracts/storage/DegenerusGameStorage.sol:1377-1385`, and `_handleGameOverPath()` is skipped for those phases at `contracts/modules/DegenerusGameAdvanceModule.sol:207`.

Revert mechanism: `_finalizeRngRequest()` is never reached, so no lock/fallback timer is started. Repeated calls hit the same hard revert until external VRF repair.

SEVERITY: HIGH if VRF request failure is in scope; otherwise an explicitly acknowledged operational dependency.

**Stage Graph Notes**
Game-over ticket drain is fixed: every successful batch, including a finishing slot-swap batch, returns `STAGE_TICKETS_WORKING` before terminal drain (`DegenerusGameAdvanceModule.sol:620-659`). Terminal drain then runs separately at `:664-672`.

Ticket/future-ticket stages are bounded: any real work breaks before downstream stages (`:270-290`, `:386-392`, `:406-426`, `:471-481`). No-work fallthrough is cheap.

Jackpot stages are split: fresh jackpot starts at `:529-531`; coin/tickets complete at `:517-526`; phase end breaks before transition. Daily jackpot caps are 305 ETH winners and 50 coin winners in `DegenerusGameJackpotModule.sol`.

No additional source-local permanent revert was found under normal pinned-contract and solvency invariants. The remaining reverts are expected wait states (`RngNotReady`, `NotTimeYet`) or bubbled pinned external/module failures.
