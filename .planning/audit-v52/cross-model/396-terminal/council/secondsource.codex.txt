All anchors are at `a8b702a7`.

AREA A: DEFECT  
Mechanism / structural fact: ACTIONABLE: `burnieOwed` only uses held BURNIE plus `previewClaimCoinflips(address(this))` (`contracts/StakedDegenerusStonk.sol:1029-1031`), while `previewClaimCoinflips` returns only `_viewClaimableCoin + claimableStored` (`contracts/BurnieCoinflip.sol:971-974`). It excludes `autoRebuyCarry`, which is a separate state field (`contracts/BurnieCoinflip.sol:149-154`) and is where sDGNRS post-seed 0-take-profit wins roll (`contracts/BurnieCoinflip.sol:877-890`, `contracts/BurnieCoinflip.sol:493-506`). `redeemBurnieShare` also burns held balance then consumes claimable/stored coinflips only (`contracts/BurnieCoinflip.sol:944-959`), so carry is not liquidated for redeemers. This is conservative: it under-credits redeemers, not over-credits them, and BURNIE settlement is isolated from ETH/`claimablePool` because it is burn/consume plus equal flip credit (`contracts/BurnieCoinflip.sol:962-963`, `contracts/StakedDegenerusStonk.sol:1068-1077`).

AREA B: DEFECT  
Mechanism / structural fact: ACTIONABLE: the constructor seeds days 1-20 to both VAULT and sDGNRS (`contracts/BurnieCoinflip.sol:183-197`), but only sDGNRS is automatically claimed/armed during daily payout processing (`contracts/BurnieCoinflip.sol:870-892`). VAULT access is manual through `onlyVaultOwner` claim/auto-rebuy calls (`contracts/DegenerusVault.sol:630-646`), and `onlyVaultOwner` requires >50.1% DGVE (`contracts/DegenerusVault.sol:419-431`). On first claim, `lastClaim == 0` selects the 30-day first window (`contracts/BurnieCoinflip.sol:423`); if `flipsClaimableDay == 51`, `minClaimableDay = 21`, `start` is clamped to 21, and iteration begins at 22 (`contracts/BurnieCoinflip.sol:431-444`). Thus days 1-20 are never processed and become operationally stranded/forfeited. This is not a player extraction path; it is a protocol-owned VAULT operations failure mode.

AREA C: SOUND  
Mechanism / structural fact: The bounties are real but economically negative at the user’s 5-50 gwei range after the flip-credit haircut. Decimator bounty target is `15e12` wei and documented against ~30k gas at 0.5 gwei (`contracts/modules/DegenerusGameDecimatorModule.sol:112-117`); realized value at 0.30x is ~`4.5e12` wei, equivalent to ~0.15 gwei for 30k gas. Redemption bounty target is `24e12` wei and documented against ~48k gas at 0.5 gwei (`contracts/StakedDegenerusStonk.sol:343-348`); realized value is ~`7.2e12` wei, also ~0.15 gwei for 48k gas. Both are paid only as `creditFlip` stakes (`contracts/modules/DegenerusGameDecimatorModule.sol:359-369`, `contracts/StakedDegenerusStonk.sol:803-813`, `contracts/BurnieCoinflip.sol:903-908`). They are not freely manufacturable: decimator entries require BurnieCoin’s decimator burn/consume path before `recordDecBurn` (`contracts/BurnieCoin.sol:591-600`, `contracts/BurnieCoin.sol:631-637`), and bounty count increments only for nonzero claimable entries later marked claimed (`contracts/modules/DegenerusGameDecimatorModule.sol:343-356`, `contracts/modules/DegenerusGameDecimatorModule.sol:398-399`). Redemption batches skip empty slots via `claim.ethValueOwed == 0` (`contracts/StakedDegenerusStonk.sol:821-823`), and entries are created by burning at least `1e18` sDGNRS (`contracts/StakedDegenerusStonk.sol:976-1044`).

AREA D: SOUND  
Mechanism / structural fact: The conservation identity holds for successful live claims. Submit stores gwei-snapped base and increments the MAX reservation telescopically (`contracts/StakedDegenerusStonk.sol:1058-1066`); resolve changes the reservation from MAX to rolled value once for the period (`contracts/StakedDegenerusStonk.sol:740-745`). Each claim computes `totalRolledEth`, splits it into direct/lootbox or direct/forfeit legs, then releases exactly `totalRolledEth` (`contracts/StakedDegenerusStonk.sol:826-854`). Each live leg recomputes fresh ETH balance and forwards `min(balance, legAmount)` (`contracts/StakedDegenerusStonk.sol:880-890`, `contracts/StakedDegenerusStonk.sol:897-900`); the Game pulls `amount - msg.value` as stETH and reverts on shortfall (`contracts/modules/DegenerusGameLootboxModule.sol:929-936`, `contracts/modules/DegenerusGameLootboxModule.sol:1004-1014`). Earlier ETH depletion changes the ETH/stETH mix for later claims, not the amount; failure is fail-closed.

AREA E: SOUND  
Mechanism / structural fact: `claimBingo` reads `traitBurnTicket` only, validates all 8 color slots against `msg.sender`, then sets per-player and first-claim bits before external interactions (`contracts/modules/DegenerusGameBingoModule.sol:130-151`, `contracts/modules/DegenerusGameBingoModule.sol:153-198`). Tier precedence is correct: quadrant-first is checked first, sets both quadrant and symbol bits, and suppresses the symbol bonus (`contracts/modules/DegenerusGameBingoModule.sol:153-178`). The population is structurally frozen against live steering: ticket processing reads the read queue, while purchases write the opposite queue key (`contracts/storage/DegenerusGameStorage.sol:755-764`), and the read slot must be drained before swap (`contracts/storage/DegenerusGameStorage.sol:778-784`). Pool split also conserves: BPS constants sum to `10_000` (`contracts/StakedDegenerusStonk.sol:301-312`), constructor allocation uses that split (`contracts/StakedDegenerusStonk.sol:384-409`), and `transferFromPool` clamps every pool draw to available balance (`contracts/StakedDegenerusStonk.sol:548-570`). There are live `Pool.Reward` consumers for bingo and coinflip bounty (`contracts/modules/DegenerusGameBingoModule.sol:188-193`, `contracts/DegenerusGame.sol:465-475`), but no live jackpot final-day `Pool.Reward` path: the final-day solo branch calls `_processSoloBucketWinner` (`contracts/modules/DegenerusGameJackpotModule.sol:1159-1166`), whose body only credits ETH/whale-pass value (`contracts/modules/DegenerusGameJackpotModule.sol:1265-1280`).
