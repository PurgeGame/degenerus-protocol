CANDIDATE 1: HOLDS  
Mechanism / structural fact: I cannot construct a closed liquid ETH pump. The recycle kicker only adds `lootboxFlipCredit`, then calls `coinflip.creditFlip`; that path writes future-day BURNIE flip stake, not Game claimable ETH (`DegenerusGameMintModule.sol:1740-1747`, `BurnieCoinflip.sol:903-908`, `BurnieCoinflip.sol:610-617`). Presale credit is spend-gated, and box proceeds credit VAULT/sDGNRS, while the buyer’s presale box resolves only BURNIE/DGNRS/WWXRP (`DegenerusGameMintModule.sol:1722-1727`, `DegenerusGameMintModule.sol:1844-1861`, `DegenerusGamePayoutUtils.sol:19-24`, `DegenerusGameLootboxModule.sol:731-787`). ETH-spin recirculation is depth-limited: direct recirc calls pass `allowEthSpin=false`, so roll 19 becomes tickets, not another ETH spin (`DegenerusGameLootboxModule.sol:889-906`, `DegenerusGameLootboxModule.sol:2027-2049`).

CANDIDATE 2: HOLDS  
Mechanism / structural fact: I cannot make the same slot increment twice in one day. Completion is masked per slot and checked before increment (`DegenerusQuests.sol:1706-1711`), then persisted before any afking callback (`DegenerusQuests.sol:1720-1734`). While afking, slot 0 is streak-neutral; only slot 1 calls `recordAfkingSecondary` (`DegenerusQuests.sol:1745-1754`). Level quest completion has its own completed bit and also routes to manual-or-afking, not both (`DegenerusQuests.sol:2037-2038`, `DegenerusQuests.sol:2063-2067`). Downstream score reads manual OR live afking streak, never a sum (`DegenerusGameStorage.sol:2289-2292`), and caps remain fixed (`DegenerusGameStorage.sol:139-143`, `DegenerusGameStorage.sol:1629-1630`, `DegenerusGameDegeneretteModule.sol:1145-1149`, `DegenerusGameDecimatorModule.sol:951-963`).

CANDIDATE 3: HOLDS  
Mechanism / structural fact: `whalePassCost` is counted exactly once as paid value. In the solo path, `ethAmount = perWinner - whalePassCost` is credited to the winner and returned as `claimableDelta`/`ethPaid`; the pass cost is added once to `futurePrizePool` and returned separately as `whalePassSpent` (`DegenerusGameJackpotModule.sol:1261-1275`). `_handleSoloBucketWinner` adds both `paid` and `wpSpent` into `paidDelta` (`DegenerusGameJackpotModule.sol:1202-1215`), `_processDailyEth` adds only `claimDelta` to `claimablePool` (`DegenerusGameJackpotModule.sol:1100-1119`), and `payDailyJackpot` subtracts `paidDailyEth` from `currentPrizePool` or excludes it from final-day unpaid rollover (`DegenerusGameJackpotModule.sol:442-451`).

CANDIDATE 4: BREAKS  
ACTIONABLE: The “no player-controllable input into either word” premise is false. `reverseFlip()` is explicitly a paid +1 nudge to the next RNG word while RNG is unlocked (`DegenerusGame.sol:1811-1827`), and `_applyDailyRng` adds `totalFlipReversals` into `finalWord` before storing `rngWordByDay[day]` (`DegenerusGameAdvanceModule.sol:1877-1893`). That finalized word is passed to `runDecimatorJackpot` (`DegenerusGameAdvanceModule.sol:958-960`) and narrowed for claim-time lootbox seed as `uint32(rngWord)` (`DegenerusGameDecimatorModule.sol:275-277`). I do not find after-reveal steering: `reverseFlip` reverts once `rngLockedFlag` is true (`DegenerusGame.sol:1817-1819`), and the VRF callback is coordinator/request gated (`DegenerusGameAdvanceModule.sol:1795-1808`).
