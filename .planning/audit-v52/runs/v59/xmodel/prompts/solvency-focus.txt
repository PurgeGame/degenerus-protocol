ETH SOLVENCY & VALUE CONSERVATION. The core invariant: `address(this).balance + stETH_held >= claimablePool`, and `claimablePool == Σ claimableWinnings[addr] + Σ afkingFunding[addr]`. ETH lives in ledger/pool vars: `claimablePool` (uint128), `claimableWinnings[addr]`, `afkingFunding[addr]`, `currentPrizePool`, `prizePoolsPacked` (→ next | future), `prizePoolPendingPacked` (the freeze buffer), `yieldAccumulator`, and sDGNRS-side `pendingRedemptionEthValue`.

TRAPS (do not waste time tripping on these): the `Pool.*` enum + `transferFromPool`/`transferBetweenPools`/`poolBalances` move sDGNRS *tokens*, NOT ETH. `creditFlip`/BURNIE legs are token emissions, NOT ETH. stETH rides WITH ETH as one reserve (most outflows are stETH-first or ETH-fallback).

Your job: trace every ETH inflow (purchase/bundle/presale-box/lazy-pass/deity-pass/whale-bundle/degenerette-bet/afking-funding/subscribe/redemption-lootbox/admin stETH swap/`receive()`), every pool→pool cycle (the prize-pool ladder future→next→current→claimable, redemption segregation, the afking debit, the salvage ETH relabel), and every outflow (claimWinnings/withdrawAfkingFunding/redemption/game-over sweep/refund). Find ANY path that: credits `claimableWinnings`/`claimablePool` ETH that is NOT backed by a real inflow (double-credit, missing debit, rounding/dust leak), strands or confiscates prepaid ETH, or breaks the solvency identity. Pay special attention to `distributeYieldSurplus` (it mints NEW claimable liability from stETH yield — verify the backing exists before the credit) and the v48/v57 sDGNRS SALVAGE swap ETH relabel out of `claimableWinnings[SDGNRS]`. Cite each concern.
