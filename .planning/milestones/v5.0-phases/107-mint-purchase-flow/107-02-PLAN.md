---
phase: 107-mint-purchase-flow
plan: 02
type: execute
wave: 2
depends_on: [107-01]
files_modified:
  - audit/unit-05/ATTACK-REPORT.md
autonomous: true
requirements: [ATK-01, ATK-02, ATK-03, ATK-04, ATK-05]

must_haves:
  truths:
    - "Every Category B function has a fully-expanded recursive call tree with line numbers for every call site"
    - "Every Category B function has a complete storage-write map listing every variable written by any function in its transitive call graph"
    - "Every Category B function has an explicit cached-local-vs-storage check identifying all (ancestor_local, descendant_write) pairs"
    - "Every Category B function is attacked from all 10 applicable angles with VULNERABLE / INVESTIGATE / SAFE verdicts"
    - "Every [MULTI-PARENT] Category C function has standalone analysis covering all calling contexts"
    - "The inline Yul assembly in _raritySymbolBatch is verified for storage slot correctness and array length accounting"
    - "The ticket queue WRITE path is traced end-to-end from purchase to _queueTicketsScaled"
    - "The self-call re-entry pattern (recordMint) is analyzed for state coherence"
    - "Every VULNERABLE or INVESTIGATE finding includes exact line numbers, a concrete attack scenario, and proof-of-concept steps"
  artifacts:
    - path: "audit/unit-05/ATTACK-REPORT.md"
      provides: "Complete function-by-function attack analysis"
      contains: "Call Tree"
    - path: "audit/unit-05/ATTACK-REPORT.md"
      provides: "Storage write maps for all functions"
      contains: "Storage Writes"
    - path: "audit/unit-05/ATTACK-REPORT.md"
      provides: "Cached-local-vs-storage checks"
      contains: "Cached-Local-vs-Storage"
  key_links:
    - from: "audit/unit-05/ATTACK-REPORT.md"
      to: "contracts/modules/DegenerusGameMintModule.sol"
      via: "Per-function analysis with line numbers"
      pattern: "line \\d+"
    - from: "audit/unit-05/ATTACK-REPORT.md"
      to: "audit/unit-05/COVERAGE-CHECKLIST.md"
      via: "Every checklist function has a corresponding analysis section"
      pattern: "MintModule::"
---

<objective>
Mad Genius Agent: Attack every state-changing function in DegenerusGameMintModule.sol and DegenerusGameMintStreakUtils.sol with full call-tree expansion, storage-write mapping, cached-local-vs-storage checks, and multi-angle attack analysis.

Purpose: This is the core attack phase for the primary ETH inflow module. The purchase flow handles real money (ETH), lootbox pool splits, affiliate payments, and ticket queue writes. The inline Yul assembly in _raritySymbolBatch and the self-call re-entry pattern through recordMint require meticulous tracing. The ticket queue WRITE paths complement Phase 104's READ/drain analysis.

Output: ATTACK-REPORT.md with one section per function, covering every function on the Taskmaster's checklist.
</objective>

<execution_context>
@.planning/ULTIMATE-AUDIT-DESIGN.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/107-mint-purchase-flow/107-CONTEXT.md
@.planning/phases/107-mint-purchase-flow/107-RESEARCH.md
@audit/KNOWN-ISSUES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attack all Category B functions and [MULTI-PARENT] Category C functions</name>
  <files>audit/unit-05/ATTACK-REPORT.md</files>

  <read_first>
    - .planning/ULTIMATE-AUDIT-DESIGN.md (Agent 1: Mad Genius section -- adopt this persona COMPLETELY)
    - audit/unit-05/COVERAGE-CHECKLIST.md (the Taskmaster's checklist -- cover EVERY function marked for full analysis)
    - contracts/modules/DegenerusGameMintModule.sol (~1,167 lines -- read the ENTIRE file, line by line)
    - contracts/modules/DegenerusGameMintStreakUtils.sol (62 lines -- read fully)
    - contracts/storage/DegenerusGameStorage.sol (storage layout -- needed for storage-write maps and cache checks, plus inherited helpers _queueTicketsScaled, _awardEarlybirdDgnrs, prize pool helpers)
    - contracts/interfaces/IDegenerusGame.sol (MintPaymentKind enum, recordMint, consumePurchaseBoost, playerActivityScore)
    - .planning/phases/107-mint-purchase-flow/107-RESEARCH.md (key pitfalls, ticket lifecycle analysis -- use as STARTING POINTS, not conclusions)
    - audit/KNOWN-ISSUES.md (do NOT re-report known issues)
  </read_first>

  <action>
YOU ARE THE MAD GENIUS. Read your identity and mandate from ULTIMATE-AUDIT-DESIGN.md, Agent 1. You are the most dangerous smart contract attacker alive. You have 1,000 ETH, unlimited patience, and the source code.

**IMPORTANT: Read EVERY LINE of DegenerusGameMintModule.sol and DegenerusGameMintStreakUtils.sol. Not skim. Not scan. READ. Line by line.**

**PRIORITY ORDER:**

**Tier 1 -- CRITICAL (analyze FIRST and DEEPEST):**

1. **B3 `purchase()` -> C1 `_purchaseFor()`** (L560-829) -- Primary ETH inflow. Caches `priceWei = price` (L637), `purchaseLevel = level + 1` (L636), `initialClaimable = claimableWinnings[buyer]` (L650). Then calls _callTicketPurchase which self-calls recordMint and calls affiliate.payAffiliate. Does any subordinate write to `price`, `level`, or `claimableWinnings[buyer]`? The lootbox pool split writes to `prizePoolsPacked`/`prizePoolPendingPacked` -- does any parent cache these? Trace the boost application, century bonus, earlybird DGNRS, and affiliate payment chains.

2. **B2 `processFutureTicketBatch()`** (L295-434) -- Batch state machine with inline Yul assembly. Verify: cursor saves correctly across calls, tickets cannot be skipped or double-processed, `_raritySymbolBatch` storage slot calculation matches Solidity layout, LCG has full period, trait distribution is unbiased, write budget accounting prevents gas limit attacks.

**Tier 2:**

3. **B1 `recordMintData()`** (L175-284) -- Bit-packed storage. Verify: whale bundle frozen state transition, level units counting, unchecked arithmetic safety, early-exit paths.

4. **B4 `purchaseCoin()` -> C2 `_purchaseCoinFor()`** (L581-626) -- BURNIE purchase. Verify: coin purchase cutoff timing, BURNIE burn before queue write ordering.

**Tier 3:**

5. **B5 `purchaseBurnieLootbox()`** (L595-598) -- Thin wrapper. Verify: zero-address check, delegation to C5.

**MULTI-PARENT standalone analysis:**

- **C3 `_callTicketPurchase()`** -- Called from C1 (ETH) and C2 (BURNIE) with different payInCoin values and value params. Analyze both calling contexts. Ticket level routing logic (L842-851) is critical.
- **C5 `_purchaseBurnieLootboxFor()`** -- Called from B5 and C2. Analyze both contexts.
- **C6 `_maybeRequestLootboxRng()`** -- Called from C1 and C5. Verify accumulator consistency.

**ASSEMBLY verification for C8 `_raritySymbolBatch()`:**

Verify inline Yul assembly at lines 502-536:
- `levelSlot := keccak256(lvl, traitBurnTicket.slot)` -- does this match Solidity's storage layout for `mapping(uint24 => address[256])`?
- `elem := add(levelSlot, traitId)` -- correct array element slot?
- `len := sload(elem)` -- correct length location?
- `data := keccak256(elem)` -- correct data start?
- `sstore(dst, player)` with `dst := add(data, len + k)` -- no overwrite of adjacent storage?

**For EACH function, produce:**

```
## MintModule::functionName() (lines X-Y) [B#/C#]

### Call Tree
[Full recursive expansion with line numbers]

### Storage Writes (Full Tree)
[Every storage variable written by any function in the call tree]

### Cached-Local-vs-Storage Check
[Every (ancestor_local, descendant_write) pair]

### Attack Analysis
[Each of the 10 angles with VERDICT]
```
  </action>

  <verify>
    <automated>test -f audit/unit-05/ATTACK-REPORT.md && grep -c "VULNERABLE\|INVESTIGATE\|SAFE" audit/unit-05/ATTACK-REPORT.md</automated>
  </verify>

  <acceptance_criteria>
    - File audit/unit-05/ATTACK-REPORT.md exists
    - Every Category B function (B1-B5) has its own section with Call Tree, Storage Writes, Cache Check, Attack Analysis
    - Every [MULTI-PARENT] helper (C3, C5, C6) has standalone cross-context analysis
    - _raritySymbolBatch assembly has dedicated verification section
    - Self-call re-entry pattern (recordMint) analyzed for state coherence
    - Ticket queue write path traced end-to-end
    - All verdicts are VULNERABLE, INVESTIGATE, or SAFE with justification
    - Line numbers reference actual source code
    - No known issues re-reported
  </acceptance_criteria>

  <done>Complete attack report with every function analyzed, all call trees expanded, all storage writes mapped, cached-local-vs-storage checked, assembly verified, self-call pattern traced, and ticket queue write paths validated</done>
</task>

</tasks>

<verification>
- ATTACK-REPORT.md covers every function on the COVERAGE-CHECKLIST
- All call trees fully expanded with line numbers
- All storage writes mapped for complete transitive call graphs
- Cached-local-vs-storage check present for every function
- Assembly verification section present and complete
- Self-call re-entry analysis present
</verification>

<success_criteria>
- Mad Genius has attacked every state-changing function in both contracts
- The inline Yul assembly in _raritySymbolBatch has been verified
- The self-call pattern through recordMint has been analyzed for state coherence
- Every VULNERABLE and INVESTIGATE finding has a concrete attack scenario with line numbers
</success_criteria>

<output>
After completion, create `.planning/phases/107-mint-purchase-flow/107-02-SUMMARY.md`
</output>
