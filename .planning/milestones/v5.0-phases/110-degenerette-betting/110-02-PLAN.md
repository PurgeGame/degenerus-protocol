---
phase: 110-degenerette-betting
plan: 02
type: execute
wave: 2
depends_on: [110-01]
files_modified:
  - audit/unit-08/ATTACK-REPORT.md
autonomous: true
requirements: [ATK-01, ATK-02, ATK-03, ATK-04, ATK-05]

must_haves:
  truths:
    - "Every Category B function has a fully-expanded recursive call tree with line numbers for every call site"
    - "Every Category B function has a complete storage-write map listing every variable written by any function in its transitive call graph"
    - "Every Category B function has an explicit cached-local-vs-storage check identifying all (ancestor_local, descendant_write) pairs"
    - "Every Category B function is attacked from all 10 applicable angles with VULNERABLE / INVESTIGATE / SAFE verdicts"
    - "Every [MULTI-PARENT] Category C function has standalone analysis covering all calling contexts"
    - "The delegatecall to LootboxModule via _resolveLootboxDirect is traced for state coherence (no stale cache after delegatecall return)"
    - "The ETH claimable pull logic at L552 is specifically analyzed for off-by-one"
    - "The prizePoolFrozen check gap between bet placement and resolution is analyzed"
    - "Every VULNERABLE or INVESTIGATE finding includes exact line numbers, a concrete attack scenario, and proof-of-concept steps"
  artifacts:
    - path: "audit/unit-08/ATTACK-REPORT.md"
      provides: "Complete function-by-function attack analysis"
      contains: "Call Tree"
    - path: "audit/unit-08/ATTACK-REPORT.md"
      provides: "Storage write maps for all functions"
      contains: "Storage Writes"
    - path: "audit/unit-08/ATTACK-REPORT.md"
      provides: "Cached-local-vs-storage checks"
      contains: "Cached-Local-vs-Storage"
  key_links:
    - from: "audit/unit-08/ATTACK-REPORT.md"
      to: "contracts/modules/DegenerusGameDegeneretteModule.sol"
      via: "Per-function analysis with line numbers"
      pattern: "line \\d+"
    - from: "audit/unit-08/ATTACK-REPORT.md"
      to: "audit/unit-08/COVERAGE-CHECKLIST.md"
      via: "Every checklist function has a corresponding analysis section"
      pattern: "DegeneretteModule::"
---

<objective>
Mad Genius Agent: Attack every state-changing function in DegenerusGameDegeneretteModule.sol with full call-tree expansion, storage-write mapping, cached-local-vs-storage checks, and multi-angle attack analysis.

Purpose: This is the core attack phase for the Degenerette betting module. The module has complex multi-currency payout paths (ETH with pool cap + lootbox delegatecall, BURNIE mint, WWXRP mint), activity score computation with external view calls, EV normalization math, and hero quadrant multipliers. The resolution loop processes multiple spins per bet with per-spin state changes. The ETH distribution path is the highest risk: it deducts from futurePrizePool, credits claimable, then delegatecalls to LootboxModule -- state coherence across this chain is critical.

Output: ATTACK-REPORT.md with one section per function, covering every function on the Taskmaster's checklist.
</objective>

<execution_context>
@.planning/ULTIMATE-AUDIT-DESIGN.md
</execution_context>

<context>
@.planning/phases/110-degenerette-betting/110-CONTEXT.md
@.planning/phases/110-degenerette-betting/110-RESEARCH.md
@audit/unit-08/COVERAGE-CHECKLIST.md
@audit/KNOWN-ISSUES.md
</context>
