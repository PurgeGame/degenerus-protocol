---
phase: 105-jackpot-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/unit-03/COVERAGE-CHECKLIST.md
autonomous: true
requirements: [COV-01, COV-02, COV-03]

must_haves:
  truths:
    - "Every state-changing function (external, public, internal, private) in DegenerusGameJackpotModule.sol and DegenerusGamePayoutUtils.sol has a checklist entry"
    - "Each function is categorized as B (external state-changing), C (internal/private state-changing), or D (view/pure) per D-01"
    - "BAF-critical functions (_addClaimableEth, _processAutoRebuy, _creditClaimable) are flagged with [BAF-CRITICAL] markers"
    - "Multi-parent helpers (C3, C6, C12, C22, C23, C24) are flagged with [MULTI-PARENT] markers"
    - "Risk tiers (1/2/3) are assigned to all Category B functions matching the research risk tier assignments"
    - "The inline Yul assembly function _raritySymbolBatch is flagged with [ASSEMBLY] marker"
  artifacts:
    - path: "audit/unit-03/COVERAGE-CHECKLIST.md"
      provides: "Complete function checklist for Taskmaster coverage enforcement"
      contains: "Function Checklist"
  key_links:
    - from: "audit/unit-03/COVERAGE-CHECKLIST.md"
      to: "contracts/modules/DegenerusGameJackpotModule.sol"
      via: "Every state-changing function listed with line numbers"
      pattern: "line \\d+"
    - from: "audit/unit-03/COVERAGE-CHECKLIST.md"
      to: "contracts/modules/DegenerusGamePayoutUtils.sol"
      via: "All 3 PayoutUtils functions listed with line numbers"
      pattern: "PayoutUtils"
---

<objective>
Taskmaster Agent: Build the coverage checklist for Unit 3 (DegenerusGameJackpotModule + DegenerusGamePayoutUtils).

Purpose: The Taskmaster ensures no function is skipped during the Mad Genius attack phase. With 55 functions across two contracts and deep call chains (6+ levels in the ticket subsystem), completeness is critical. The BAF-critical functions (_addClaimableEth, _processAutoRebuy) and inline Yul assembly (_raritySymbolBatch) need explicit flagging.

Output: COVERAGE-CHECKLIST.md (function-by-function audit tracking table covering both contracts as a single unit per D-04/D-05)
</objective>

<execution_context>
@.planning/ULTIMATE-AUDIT-DESIGN.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-jackpot-distribution/105-CONTEXT.md
@.planning/phases/105-jackpot-distribution/105-RESEARCH.md
@audit/KNOWN-ISSUES.md

<interfaces>
<!-- Prior unit checklists for FORMAT REFERENCE ONLY (do not copy findings). -->
From audit/unit-01/COVERAGE-CHECKLIST.md:
- Category A/B/C/D table structure with Analyzed?, Call Tree?, Storage Map?, Cache Check? columns
- Risk tier assignments (Tier 1/2/3) for Category B

From audit/unit-02/COVERAGE-CHECKLIST.md:
- Same table structure adapted for module (no Category A dispatchers)
- Sequential C-numbering scheme (C1-C26) used in Phase 104
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build complete function coverage checklist for JackpotModule + PayoutUtils</name>
  <files>audit/unit-03/COVERAGE-CHECKLIST.md</files>

  <read_first>
    - .planning/ULTIMATE-AUDIT-DESIGN.md (Agent 3: Taskmaster section -- adopt this persona)
    - .planning/phases/105-jackpot-distribution/105-RESEARCH.md (complete function inventory with categories, risk tiers, subsystem map, BAF-critical chain analysis)
    - .planning/phases/105-jackpot-distribution/105-CONTEXT.md (decisions D-01 through D-11)
    - contracts/modules/DegenerusGameJackpotModule.sol (2,715 lines -- read the ENTIRE file, line by line)
    - contracts/modules/DegenerusGamePayoutUtils.sol (92 lines -- read the ENTIRE file, line by line)
    - contracts/storage/DegenerusGameStorage.sol (storage layout reference -- needed for storage-write column)
    - contracts/interfaces/IDegenerusGameModules.sol (module interface -- verify function signatures)
    - audit/unit-02/COVERAGE-CHECKLIST.md (FORMAT REFERENCE ONLY -- see how Phase 104 structured the checklist for a module contract)
    - audit/KNOWN-ISSUES.md (context for known issues -- do not include in checklist findings)
  </read_first>

  <action>
YOU ARE THE TASKMASTER. Read your identity and mandate from ULTIMATE-AUDIT-DESIGN.md, Agent 3.

Your job: Build the COVERAGE CHECKLIST that the Mad Genius must check off function-by-function. If a function is missing from your checklist, it will NOT be audited. Completeness is everything.

**IMPORTANT: You must read EVERY LINE of DegenerusGameJackpotModule.sol (2,715 lines) and DegenerusGamePayoutUtils.sol (92 lines). Not skim. Not scan. READ. Line by line. The research inventory is a starting point. You must independently verify every function exists and is correctly categorized. If you find a function the research missed, ADD IT.**

**Step 1: Categorize every function into B/C/D per D-01.**

Per D-01: No Category A for modules. External/public state-changing -> Category B. Internal/private state-changing helpers -> Category C. View/pure -> Category D.

Per D-04/D-05: Both contracts are covered in a single checklist. PayoutUtils functions (_creditClaimable, _calcAutoRebuy, _queueWhalePassClaimCore) get Category C treatment within JackpotModule's call trees.

**Category B: External State-Changing (expected 7 functions per research)**

For each, record:
- Function name and line range (from actual source, not research estimates)
- Access control (exact modifier or require statement)
- Primary storage variables written
- External calls made (to BurnieCoin, StakedDegenerusStonk, Lido, etc.)
- Risk tier: Tier 1 (BAF-path, complex multi-path, 200+ lines), Tier 2 (significant ETH/coin flows), Tier 3 (simpler)
- Subsystem tag: ETH-DIST, COIN-JACKPOT, TICKET-DIST, POOL-MGMT

Expected Category B functions (verify and correct line numbers):
- B1: runTerminalJackpot() -- Tier 1, ETH-DIST
- B2: payDailyJackpot() -- Tier 1 (CRITICAL), ETH-DIST
- B3: payDailyJackpotCoinAndTickets() -- Tier 2, COIN-JACKPOT + TICKET-DIST
- B4: awardFinalDayDgnrsReward() -- Tier 2, COIN-JACKPOT
- B5: consolidatePrizePools() -- Tier 1, POOL-MGMT
- B6: processTicketBatch() -- Tier 1, TICKET-DIST
- B7: payDailyCoinJackpot() -- Tier 2, COIN-JACKPOT

**Category C: Internal/Private State-Changing Helpers (expected ~35 functions per research)**

Per D-03: Traced as part of parent call trees. Multi-parent helpers get standalone analysis. For each, record:
- Sequential number (C1-C35) and function name
- Line range (from actual source)
- Which contract defines it (JackpotModule or PayoutUtils)
- Called by which parent(s)
- Key storage writes
- Flags: [BAF-CRITICAL], [MULTI-PARENT], [ASSEMBLY], [BAF-PATH] as applicable

Per D-06: _addClaimableEth (C3) and _processAutoRebuy (C4) are Tier 1 BAF-CRITICAL. Flag prominently.
Per D-07: _processAutoRebuy chain is re-audited from scratch as if the v4.4 fix does not exist.
Per D-09: If a subordinate call writes to storage that the parent cached locally, that IS a finding regardless of which module owns the subordinate.

**Note on C30-C35 from research:** Several of these are view/pure but listed as C because they are called exclusively within state-changing chains. During your independent verification, reclassify any that are truly view/pure to Category D. Document the reclassification.

**Category D: View/Pure (expected ~13-15 functions per research)**

For each, record:
- Function name and line range
- What it reads/computes
- Security note (RNG derivation, entropy usage, modulo bias, etc.)
- Subsystem tag

**Step 2: Build the cross-reference table for BAF-critical call chains.**

After the main checklist, add a dedicated section documenting every call chain that reaches _addClaimableEth (C3):

```
## BAF-Critical Call Chains

Every path from a Category B entry point to _addClaimableEth:

B1 -> C12 -> C13 -> C14 -> C15 -> C3 (runTerminalJackpot ETH distribution)
B2 -> C11 -> C3 (payDailyJackpot -> _processDailyEth -> _addClaimableEth)
B2 -> C9 -> C10 -> C12 -> C13 -> C14 -> C15 -> C3 (early-burn path)
B2 -> C1 -> ... (earlybird lootbox path -- trace to verify if it reaches C3)
B5 -> C2 -> C3 (consolidatePrizePools -> _distributeYieldSurplus -> _addClaimableEth)

For each chain, the Mad Genius must verify: does any ancestor cache futurePrizePool, nextPrizePool, currentPrizePool, or claimablePool in a local variable?
```

**Step 3: Build the cross-module external call table.**

List every external call JackpotModule makes (not delegatecall -- actual cross-contract calls):

| Call | From Functions | Target Contract | State Impact |
|------|---------------|----------------|-------------|
| coin.creditFlip | C15, C29, B2 | BurnieCoin | BURNIE minting |
| coin.creditFlipBatch | C22, C23 | BurnieCoin | Batch BURNIE minting |
| coin.rollDailyQuest | B2, B3 | BurnieCoin | Quest state |
| dgnrs.transferFromPool | B4 | StakedDegenerusStonk | sDGNRS transfer |
| dgnrs.poolBalance | B4 | StakedDegenerusStonk | View only |
| steth.balanceOf | C2 | Lido stETH | View only |

Per D-08: These are traced for state coherence only. Full internal audit is in their unit phases.

**Step 4: Produce COVERAGE-CHECKLIST.md with this exact structure:**

```
# Unit 3: Jackpot Distribution -- Coverage Checklist

## Contracts Under Audit
- contracts/modules/DegenerusGameJackpotModule.sol (2,715 lines)
- contracts/modules/DegenerusGamePayoutUtils.sol (92 lines)

## Methodology
- Per D-01: Categories B/C/D only (no Category A -- these are modules, not router)
- Per D-04/D-05: Both contracts audited as single unit
- Per D-06: BAF-critical functions flagged [BAF-CRITICAL] and prioritized Tier 1
- Per D-07: _processAutoRebuy re-audited from scratch (v4.4 fix NOT trusted)
- Per D-10: Fresh RNG analysis (Phase 104 findings NOT trusted)

## Checklist Summary
| Category | Count | Analysis Depth |
|----------|-------|---------------|
| B: External State-Changing | 7 | Full Mad Genius (per D-02) |
| C: Internal Helpers | ~35 | Via caller call tree; standalone for [MULTI-PARENT] (per D-03) |
| D: View/Pure | ~13 | Minimal; RNG derivation functions get extra scrutiny |
| **TOTAL** | **~55** | |

## Category B: External State-Changing Functions
[Table with columns: #, Function, Lines, Access Control, Storage Writes, External Calls, Risk Tier, Subsystem, Analyzed?, Call Tree?, Storage Map?, Cache Check?]

## Category C: Internal Helpers (State-Changing)
[Table with columns: #, Function, Lines, Contract, Called By, Storage Writes, Flags, Analyzed?, Call Tree?, Storage Map?, Cache Check?]

## Category D: View/Pure Functions
[Table with columns: #, Function, Lines, Reads/Computes, Security Note, Subsystem, Reviewed?]

## BAF-Critical Call Chains
[Every path from B entry point to _addClaimableEth, as described above]

## Cross-Module External Calls
[Table as described above]

## RNG/Entropy Usage Map
[Summary of every RNG usage point and what it derives from, per D-10]
```

All "Analyzed?" columns start as "pending" -- the Mad Genius fills these in during Wave 2.

**CRITICAL CHECKS:**
1. Verify the exact function count. The research says 55 total (7B + 35C + 13D). If your independent read finds a different count, document the discrepancy and explain.
2. For every [MULTI-PARENT] helper, list ALL parents. If you find a parent the research missed, add it.
3. For C30-C35 (view/pure helpers listed as C in research), reclassify to D if they truly have no state changes. Document each reclassification.
4. Verify that _calcAutoRebuy (PayoutUtils line 38-72) is truly pure (no storage writes). The research says yes but you must confirm.
  </action>

  <verify>
    <automated>test -f audit/unit-03/COVERAGE-CHECKLIST.md && grep -c "pending" audit/unit-03/COVERAGE-CHECKLIST.md</automated>
  </verify>

  <acceptance_criteria>
    - File audit/unit-03/COVERAGE-CHECKLIST.md exists
    - Contains Category B table with exactly 7 external state-changing functions (or documents why count differs)
    - Contains Category C table with all ~35 internal helpers including PayoutUtils functions
    - Contains Category D table with all ~13 view/pure functions
    - Every entry has line numbers from the actual source code (not research estimates)
    - _addClaimableEth is flagged [BAF-CRITICAL] and [MULTI-PARENT]
    - _processAutoRebuy is flagged [BAF-CRITICAL]
    - _raritySymbolBatch is flagged [ASSEMBLY]
    - Multi-parent helpers (C3, C6, C12, C22, C23, C24 at minimum) are flagged [MULTI-PARENT] with all parents listed
    - Risk tiers assigned: B2 and B5 and B6 are Tier 1, B1 is Tier 2, B3/B4/B7 are Tier 2
    - BAF-Critical Call Chains section traces every path from Category B to _addClaimableEth
    - Cross-Module External Calls table lists all 6 external call types
    - RNG/Entropy Usage Map section present
    - All "Analyzed?" columns show "pending"
    - No function from either contract's source code is missing from the checklist
  </acceptance_criteria>

  <done>Complete coverage checklist with every state-changing function in DegenerusGameJackpotModule.sol and DegenerusGamePayoutUtils.sol listed with line numbers, categorized B/C/D, BAF-critical paths flagged, multi-parent helpers identified, and ready for Mad Genius sign-off</done>
</task>

</tasks>

<verification>
- COVERAGE-CHECKLIST.md contains every state-changing function from both contracts
- All checklist entries have line numbers from actual source
- BAF-critical paths are explicitly documented
- All multi-parent helpers are identified with complete parent lists
- Risk tiers match the research analysis (Tier 1 for BAF/complex, Tier 2 for significant flows, Tier 3 for standard)
</verification>

<success_criteria>
- Taskmaster has built a complete function checklist that the Mad Genius can systematically work through
- BAF-critical call chains are documented so the Mad Genius knows exactly which paths need the deepest cached-local-vs-storage analysis
- No state-changing function in either contract is missing from the checklist
</success_criteria>

<output>
After completion, create `.planning/phases/105-jackpot-distribution/105-01-SUMMARY.md`
</output>
