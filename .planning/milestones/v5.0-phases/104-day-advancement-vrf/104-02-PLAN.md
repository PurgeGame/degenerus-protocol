---
phase: 104-day-advancement-vrf
plan: 02
type: execute
wave: 2
depends_on: [104-01]
files_modified:
  - audit/unit-02/ATTACK-REPORT.md
autonomous: true
requirements: [ATK-01, ATK-02, ATK-03, ATK-04, ATK-05]

must_haves:
  truths:
    - "Every Category B function has a fully-expanded recursive call tree with line numbers for every call site"
    - "Every Category B function has a complete storage-write map listing every variable written by any function in its transitive call graph"
    - "Every Category B function has an explicit cached-local-vs-storage check identifying all (ancestor_local, descendant_write) pairs"
    - "Every Category B function is attacked from all 10 applicable angles with VULNERABLE / INVESTIGATE / SAFE verdicts"
    - "Every VULNERABLE and INVESTIGATE finding includes exact line numbers, a concrete attack scenario, and proof-of-concept steps"
    - "The ticket queue drain investigation has a dedicated section with a standalone verdict: CONFIRMED BUG or PROVEN SAFE with full trace evidence"
    - "Cross-module delegatecall storage writes are traced far enough to verify parent state coherence per D-08 and D-09"
  artifacts:
    - path: "audit/unit-02/ATTACK-REPORT.md"
      provides: "Complete function-by-function attack analysis for Unit 2"
      contains: "Call Tree"
    - path: "audit/unit-02/ATTACK-REPORT.md"
      provides: "Storage write maps for all functions"
      contains: "Storage Writes"
    - path: "audit/unit-02/ATTACK-REPORT.md"
      provides: "Cached-local-vs-storage checks for all functions"
      contains: "Cached-Local-vs-Storage"
    - path: "audit/unit-02/ATTACK-REPORT.md"
      provides: "Ticket queue drain investigation verdict"
      contains: "Ticket Queue Drain Investigation"
  key_links:
    - from: "audit/unit-02/ATTACK-REPORT.md"
      to: "contracts/modules/DegenerusGameAdvanceModule.sol"
      via: "Per-function analysis with line numbers"
      pattern: "line \\d+"
    - from: "audit/unit-02/ATTACK-REPORT.md"
      to: "audit/unit-02/COVERAGE-CHECKLIST.md"
      via: "Every checklist function has a corresponding analysis section"
      pattern: "AdvanceModule::"
---

<objective>
Mad Genius Agent: Attack every state-changing function in DegenerusGameAdvanceModule.sol with full call-tree expansion, storage-write mapping, cached-local-vs-storage checks, and multi-angle attack analysis. Produce a standalone verdict on the ticket queue drain bug.

Purpose: This is the core attack phase for Unit 2. The AdvanceModule is the heartbeat of the game -- advanceGame() alone is a 270-line FSM with 11 stage paths, multiple delegatecall chains into 4 modules, and a double-buffered ticket queue system. BAF-class risk is elevated because advanceGame() caches level, jackpotPhaseFlag, lastPurchaseDay, and other state in locals, then delegates to modules that write to shared storage.

Output: ATTACK-REPORT.md with one section per function, covering every function on the Taskmaster's checklist, plus a dedicated Ticket Queue Drain Investigation section.
</objective>

<execution_context>
@.planning/ULTIMATE-AUDIT-DESIGN.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-day-advancement-vrf/104-CONTEXT.md
@.planning/phases/104-day-advancement-vrf/104-RESEARCH.md
@.planning/phases/104-day-advancement-vrf/104-01-SUMMARY.md
@audit/KNOWN-ISSUES.md

<!-- Format reference from Phase 103 Mad Genius output (methodology only) -->
@audit/unit-01/ATTACK-REPORT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attack all state-changing functions and produce ticket queue drain investigation</name>
  <files>audit/unit-02/ATTACK-REPORT.md</files>

  <read_first>
    - .planning/ULTIMATE-AUDIT-DESIGN.md (Agent 1: Mad Genius section -- adopt this persona COMPLETELY. You are the most dangerous smart contract attacker alive.)
    - .planning/phases/104-day-advancement-vrf/104-CONTEXT.md (locked decisions D-01 through D-10, priority investigation D-04/D-05)
    - .planning/phases/104-day-advancement-vrf/104-RESEARCH.md (function inventory, cached-local-vs-storage pairs, cross-module delegatecall map, ticket queue architecture, pitfalls)
    - audit/unit-02/COVERAGE-CHECKLIST.md (the Taskmaster's checklist -- you must cover EVERY function)
    - contracts/modules/DegenerusGameAdvanceModule.sol (1,570 lines -- read the ENTIRE file, line by line. No skimming. No "this looks standard.")
    - contracts/storage/DegenerusGameStorage.sol (1,613 lines -- read fully for storage variable declarations, ticket queue helpers, prize pool helpers)
    - contracts/interfaces/IDegenerusGameModules.sol (module interface -- needed for delegatecall target verification)
    - audit/KNOWN-ISSUES.md (do NOT re-report known issues)
    - test/foundry/TicketLifecycle.t.sol (the 3 failing tests -- read for ticket queue drain investigation per D-04/D-05)
    - audit/unit-01/ATTACK-REPORT.md (format reference ONLY -- replicate the per-function analysis structure)
  </read_first>

  <action>
YOU ARE THE MAD GENIUS. Read your identity and mandate from ULTIMATE-AUDIT-DESIGN.md, Agent 1. You are the most dangerous smart contract attacker alive. You have 1,000 ETH, unlimited patience, and the source code.

**IMPORTANT: You must read EVERY LINE of DegenerusGameAdvanceModule.sol (1,570 lines). Not skim. Not scan. READ. Line by line. The BAF bug hid in a rare path inside a helper function. You will not find it by skimming.**

The COVERAGE-CHECKLIST.md from Wave 1 tells you exactly which functions to analyze. Work through them systematically.

**Part 1: Category B Functions -- Full Attack Analysis (6 functions, per D-02)**

For EACH of the 6 Category B functions, produce a complete analysis section:

```
## DegenerusGameAdvanceModule::functionName() (lines X-Y) [BN]

### Call Tree
[Full recursive expansion with line numbers. For every internal/external/delegatecall
this function makes, recursively expand it. For cross-module delegatecalls (per D-08),
trace the target function far enough to list all storage writes, but do NOT
deep-dive into the module's internal sub-helpers -- those are audited in their own
unit phases (105-117). If a subordinate call writes to storage that the parent
has cached locally, that IS a finding per D-09.]

### Storage Writes (Full Tree)
[Every storage variable written by ANY function in the call tree.
Format: variable_name (slot N) -- written by functionName() at line X
For cross-module delegatecall targets: list the top-level storage writes of the
delegated function (per D-08). Deep internal writes are deferred to the module's
own unit phase.]

### Cached-Local-vs-Storage Check
[THE BAF PATTERN CHECK -- #1 PRIORITY]
[Does any ancestor cache a storage value in a local variable?
Does any descendant in the call tree write to that same storage variable?
If yes: the ancestor will write back the stale cache, overwriting the descendant's update.]
[List EVERY (ancestor_local, descendant_write) pair and verdict: VULNERABLE / SAFE]
[If SAFE, explain exactly why -- which guard prevents it, or why the local is not written back.]

CRITICAL PAIRS TO CHECK (from research):
- lvl = level (L131) vs _finalizeRngRequest writes level (L1352): HIGH risk
- inJackpot = jackpotPhaseFlag (L130) vs advanceGame self-writes jackpotPhaseFlag (L263, L341): MEDIUM risk
- lastPurchase derived from lastPurchaseDay (L143) vs advanceGame writes lastPurchaseDay (L139, L302): MEDIUM risk
- purchaseLevel (L145) vs _finalizeRngRequest writes level which changes what purchaseLevel should be: HIGH risk
- advanceBounty (L127) uses price which _finalizeRngRequest can update (L1356-1379): MEDIUM risk

### Attack Analysis
[For each applicable attack angle:]

**State Coherence:** [analysis of all cached-local pairs above] VERDICT: VULNERABLE / INVESTIGATE / SAFE
**Access Control:** [who can call, what guard, verify the actual check] VERDICT: ...
**RNG Manipulation:** [Is the RNG word unknown at input commitment time? Can player-controllable state change between VRF request and fulfillment? Per memory: trace BACKWARD from each consumer to verify word was unknown at input commitment time. Check what player-controllable state can change between VRF request and fulfillment.] VERDICT: ...
**Cross-Contract State Desync:** [delegatecall to 4 modules -- does any assume state that another module could change?] VERDICT: ...
**Edge Cases:** [zero values, max values, first-ever call, game-over state, day boundaries, level transitions, empty ticket queues] VERDICT: ...
**Conditional Paths:** [advanceGame has 11 stage paths -- EVERY branch must be analyzed. The BAF bug was on a RARE PATH.] VERDICT: ...
**Economic/MEV:** [can attacker profit from timing or ordering of advanceGame calls? Front-running the VRF callback?] VERDICT: ...
**Griefing:** [can attacker stall the game by preventing advanceGame from completing? Denial of service?] VERDICT: ...
**Ordering/Sequencing:** [unexpected call order? What if advanceGame is called before VRF fulfills? What if VRF fulfills twice?] VERDICT: ...
**Silent Failures:** [does any path silently succeed when it should revert?] VERDICT: ...
```

**Priority order for Category B functions (analyze in this order):**

Tier 1 -- CRITICAL (analyze FIRST and DEEPEST):
1. `advanceGame()` (L125-397) -- 270-line FSM, 11 stage paths, caches 5+ locals from storage, delegates to 4 modules. This is the most complex function in the module. SPEND THE MOST TIME HERE.
2. `rawFulfillRandomWords()` (L1455-1476) -- VRF callback entry point, branches on rngLockedFlag for daily vs mid-day path. Per D-07, full treatment, no reduced scrutiny.
3. `requestLootboxRng()` (L689-759) -- Mid-day VRF request, ticket buffer swap, LINK balance check, pending accumulator reset.

Tier 2 -- MEDIUM:
4. `updateVrfCoordinatorAndSub()` (L1390-1419) -- Emergency VRF rotation, resets 6 state flags.

Tier 3 -- LOW:
5. `wireVrf()` (L412-425) -- Deploy-only VRF config.
6. `reverseFlip()` (L1438-1446) -- Burns BURNIE, increments counter.

**Part 2: Category C Functions -- Via Caller Call Trees (per D-03)**

Category C functions are analyzed as part of their parent's call tree in Part 1. They appear inside the Part 1 call trees. However, per D-03, if a Category C function is MULTI-PARENT (called from multiple parents with different cached-local states), give it additional scrutiny in the parent analysis that uses the riskiest cached-local context.

MULTI-PARENT functions requiring explicit cross-parent comparison:
- C7: payDailyJackpot() -- purchase path vs jackpot path
- C11: rngGate() -- called from advanceGame with cached lvl/inJackpot
- C17: _processFutureTicketBatch() -- called from _prepareFutureTickets and advanceGame
- C19: _runProcessTicketBatch() -- 4 calling paths with different state contexts
- C25: _unlockRng() -- called from advanceGame and rngGate
- C28: _applyDailyRng() -- called from rngGate and _gameOverEntropy

For each MULTI-PARENT function, explicitly document which parent passes which cached-local context and whether the function behaves correctly in all cases.

**Part 3: PRIORITY INVESTIGATION -- Ticket Queue Drain (D-04, D-05)**

THIS IS MANDATORY. Produce a dedicated section:

```
## PRIORITY INVESTIGATION: Ticket Queue Drain

### Context
3 TicketLifecycle Foundry tests fail with `Read queue not drained for level 1: 2 != 0`:
- testFiveLevelIntegration
- testMultiLevelZeroStranding
- testZeroStrandingSweepAfterTransitions

### Full Lifecycle Trace
[Trace the COMPLETE ticket lifecycle per D-05:]
1. Queue write: Where and when are tickets queued for level 1? (constructor, purchases, lootbox)
2. Swap: When does _swapAndFreeze() / _swapTicketSlot() toggle the write/read buffers?
3. Processing: When does _runProcessTicketBatch(1) drain the read queue for level 1?
4. Assertion: What does the test assert and when?

### Double-Buffer Architecture Analysis
[Explain the ticketWriteSlot toggle mechanism:]
- Slot 0 vs Slot 1, write key vs read key
- Far-future (FF) key space
- How _swapAndFreeze changes which slot is "read" vs "write"
- What happens after an even number of swaps vs odd number

### Constructor Ticket Trace
[Trace the constructor-seeded tickets (SDGNRS + VAULT, 16 each for levels 1-100):]
- Which slot do they land in at deploy time? (ticketWriteSlot=0 at construction)
- When do they become the "read" queue? (after first _swapAndFreeze)
- Are they processed by _runProcessTicketBatch(1)?
- What is the state of ticketWriteSlot when the test asserts?

### Test Assertion Analysis
[Read test/foundry/TicketLifecycle.t.sol and trace:]
- How does the test compute _readKeyForLevel(1)?
- Does it use the CURRENT ticketWriteSlot (which may have toggled multiple times)?
- If ticketWriteSlot has toggled an even number of times since level 1 was processed, the "read key" points to the WRONG buffer

### Verdict: CONFIRMED BUG / PROVEN SAFE
[With full evidence. If CONFIRMED BUG: what exactly is broken and where. If PROVEN SAFE: why the test assertion is incorrect and what the correct assertion should be.]
```

**Part 4: Cross-Module Delegatecall State Coherence Check (D-08, D-09)**

For each delegatecall in advanceGame() (and subordinate functions), produce:

```
### Cross-Module: [MODULE_NAME].[functionName]

Storage writes by this delegatecall:
- [variable] at line X

Parent's cached locals at time of delegatecall:
- [local] = [storage_var] cached at line Y

Conflict check: Does the delegatecall write to any variable the parent cached?
[YES (finding!) / NO (safe)]
```

Modules to check (from research cross-module delegatecall map):
- GAME_JACKPOT_MODULE: payDailyJackpot, payDailyJackpotCoinAndTickets, payDailyCoinJackpot, consolidatePrizePools, awardFinalDayDgnrsReward, processTicketBatch
- GAME_ENDGAME_MODULE: rewardTopAffiliate, runRewardJackpots
- GAME_MINT_MODULE: processFutureTicketBatch
- GAME_GAMEOVER_MODULE: handleGameOverDrain, handleFinalSweep

**ANTI-SHORTCUTS DOCTRINE (from ULTIMATE-AUDIT-DESIGN.md):**
1. No "similar to above" -- every function gets its own full analysis
2. No skipping private helpers -- they get full scrutiny as part of call trees
3. No assuming guards work -- read the modifier/require, trace what it checks
4. No skipping the rare path -- if advanceGame has 11 stage paths, analyze ALL 11
5. No trusting previous audits (per D-06) -- start from zero, do NOT reference v3.7/v3.8 findings
6. No batch dismissals
7. Call trees are MANDATORY
8. Storage write maps are MANDATORY

**BIAS: When in doubt, flag it. A false positive wastes 5 minutes of Skeptic review time. A missed bug costs the protocol.**

**What is NOT a valid attack (from ULTIMATE-AUDIT-DESIGN.md):**
- Arithmetic overflow/underflow (Solidity 0.8.34, audited in v3.0-v4.2)
- Classic reentrancy (no raw .call patterns, audited in v3.0-v3.3)
- Admin key compromise (owner trust assumption)
- VRF coordinator compromise (Chainlink trust assumption)
- Known issues listed in audit/KNOWN-ISSUES.md

**OUTPUT FORMAT:**
Start the ATTACK-REPORT.md with a summary table of all findings, then per-function analysis sections, then the Ticket Queue Drain Investigation section.

```
# Unit 2: Day Advancement + VRF -- Attack Report

## Findings Summary
| ID | Function | Verdict | Severity | Title |
|----|----------|---------|----------|-------|
| F-01 | funcName | VULNERABLE/INVESTIGATE | SEVERITY | [title] |

## Detailed Analysis
[Per-function sections for all Category B functions]

## Cross-Module Delegatecall State Coherence
[Per-delegatecall conflict checks]

## PRIORITY INVESTIGATION: Ticket Queue Drain
[Dedicated investigation section with CONFIRMED BUG / PROVEN SAFE verdict]
```
  </action>

  <verify>
    <automated>test -f audit/unit-02/ATTACK-REPORT.md && echo "exists" && grep -c "## DegenerusGameAdvanceModule::" audit/unit-02/ATTACK-REPORT.md && grep -c "VERDICT:" audit/unit-02/ATTACK-REPORT.md && grep -c "Ticket Queue Drain" audit/unit-02/ATTACK-REPORT.md</automated>
  </verify>

  <acceptance_criteria>
    - File audit/unit-02/ATTACK-REPORT.md exists
    - Contains a Findings Summary table at the top
    - Has a dedicated analysis section for EVERY Category B function (6 external/public state-changing)
    - Every Category B section contains: Call Tree, Storage Writes (Full Tree), Cached-Local-vs-Storage Check, Attack Analysis with verdicts on all 10 angles
    - advanceGame() analysis covers all 11 stage paths (stages 0-11) with no paths skipped
    - The critical cached-local pairs (lvl, inJackpot, lastPurchase, purchaseLevel, advanceBounty) are explicitly checked
    - Cross-module delegatecall state coherence section covers all 4 target modules (JACKPOT, ENDGAME, MINT, GAMEOVER) and all delegatecall entry points
    - PRIORITY INVESTIGATION section exists with: full lifecycle trace, double-buffer analysis, constructor ticket trace, test assertion analysis, and CONFIRMED BUG / PROVEN SAFE verdict
    - All line numbers reference actual source code lines (not research estimates)
    - Every VULNERABLE/INVESTIGATE finding includes exact line numbers, concrete attack scenario, and proof-of-concept steps
    - No function uses "similar to above" or "standard pattern" as justification for skipping analysis
    - MULTI-PARENT functions have explicit cross-parent cached-local comparison
    - Known issues from audit/KNOWN-ISSUES.md are NOT re-reported
    - No references to or trust of prior v3.7/v3.8 audit findings (per D-06)
  </acceptance_criteria>

  <done>Complete attack report with per-function analysis for all 6 Category B functions with call trees, storage-write maps, cache checks, and 10-angle attack verdicts; cross-module delegatecall coherence checks for all 4 modules; and ticket queue drain investigation with standalone CONFIRMED BUG or PROVEN SAFE verdict</done>
</task>

</tasks>

<verification>
- ATTACK-REPORT.md covers every function on the COVERAGE-CHECKLIST.md
- Every Category B function has all four required sections (Call Tree, Storage Writes, Cache Check, Attack Analysis)
- Cross-module delegatecall state coherence checked for all module targets
- Ticket queue drain investigation complete with verdict
- All findings have verdicts (VULNERABLE / INVESTIGATE / SAFE)
- All VULNERABLE/INVESTIGATE findings have line numbers, attack scenarios, and PoC steps
</verification>

<success_criteria>
- The Mad Genius has systematically attacked every state-changing function in DegenerusGameAdvanceModule.sol
- No function was skipped or dismissed without analysis
- The BAF-class cache-overwrite pattern was explicitly checked for every Category B function, with special focus on advanceGame()'s 5+ cached locals
- Cross-module delegatecall storage writes verified for state coherence
- Ticket queue drain investigation has a definitive verdict with full trace evidence
- Report is ready for Skeptic review in Wave 3
</success_criteria>

<output>
After completion, create `.planning/phases/104-day-advancement-vrf/104-02-SUMMARY.md`
</output>
