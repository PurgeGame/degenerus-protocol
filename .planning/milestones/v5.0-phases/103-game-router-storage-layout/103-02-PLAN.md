---
phase: 103-game-router-storage-layout
plan: 02
type: execute
wave: 2
depends_on: [103-01]
files_modified:
  - audit/unit-01/ATTACK-REPORT.md
autonomous: true
requirements: [ATK-01, ATK-02, ATK-03, ATK-04, ATK-05]

must_haves:
  truths:
    - "Every direct state-changing function has a fully-expanded recursive call tree with line numbers"
    - "Every direct state-changing function has a complete storage-write map listing every variable written by any function in its transitive call graph"
    - "Every direct state-changing function has an explicit cached-local-vs-storage check identifying all (ancestor_local, descendant_write) pairs"
    - "Every direct state-changing function is attacked from all applicable angles with VULNERABLE / INVESTIGATE / SAFE verdicts"
    - "Every delegatecall dispatcher has dispatch correctness verified: module address, selector, parameter forwarding, return decoding, access control ownership"
    - "Every VULNERABLE and INVESTIGATE finding includes exact line numbers, concrete attack scenario, and proof-of-concept steps"
  artifacts:
    - path: "audit/unit-01/ATTACK-REPORT.md"
      provides: "Complete function-by-function attack analysis"
      contains: "Call Tree"
    - path: "audit/unit-01/ATTACK-REPORT.md"
      provides: "Storage write maps for all functions"
      contains: "Storage Writes"
    - path: "audit/unit-01/ATTACK-REPORT.md"
      provides: "Cached-local-vs-storage checks"
      contains: "Cached-Local-vs-Storage"
  key_links:
    - from: "audit/unit-01/ATTACK-REPORT.md"
      to: "contracts/DegenerusGame.sol"
      via: "Per-function analysis with line numbers"
      pattern: "line \\d+"
    - from: "audit/unit-01/ATTACK-REPORT.md"
      to: "audit/unit-01/COVERAGE-CHECKLIST.md"
      via: "Every checklist function has a corresponding analysis section"
      pattern: "DegenerusGame::"
---

<objective>
Mad Genius Agent: Attack every state-changing function in DegenerusGame.sol with full call-tree expansion, storage-write mapping, cached-local-vs-storage checks, and multi-angle attack analysis. Verify every delegatecall dispatcher for correctness.

Purpose: This is the core attack phase. The Mad Genius's job is to find every exploitable vulnerability — especially BAF-class bugs where a parent function caches a storage value locally, a descendant writes to that same slot, and the parent writes back the stale cache.

Output: ATTACK-REPORT.md with one section per function, covering every function on the Taskmaster's checklist.
</objective>

<execution_context>
@.planning/ULTIMATE-AUDIT-DESIGN.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-game-router-storage-layout/103-CONTEXT.md
@.planning/phases/103-game-router-storage-layout/103-RESEARCH.md
@.planning/phases/103-game-router-storage-layout/103-01-SUMMARY.md
@audit/KNOWN-ISSUES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attack all direct state-changing functions (Category B) and internal helpers (Category C)</name>
  <files>audit/unit-01/ATTACK-REPORT.md</files>

  <read_first>
    - .planning/ULTIMATE-AUDIT-DESIGN.md (Agent 1: Mad Genius section — adopt this persona COMPLETELY)
    - audit/unit-01/COVERAGE-CHECKLIST.md (the Taskmaster's checklist — you must cover EVERY function marked for full analysis)
    - audit/unit-01/STORAGE-LAYOUT-VERIFICATION.md (understand the storage slot layout)
    - contracts/DegenerusGame.sol (2,848 lines — read the ENTIRE file, line by line. No skimming. No "this looks standard.")
    - contracts/storage/DegenerusGameStorage.sol (1,613 lines — read the ENTIRE file, every storage variable declaration)
    - contracts/modules/DegenerusGameMintStreakUtils.sol (read fully — inherited by DegenerusGame)
    - contracts/interfaces/IDegenerusGameModules.sol (module interface — needed for selector verification)
    - contracts/interfaces/IDegenerusGame.sol (game interface — needed for external function verification)
    - contracts/ContractAddresses.sol (module addresses for dispatch verification)
    - audit/KNOWN-ISSUES.md (do NOT re-report known issues)
  </read_first>

  <action>
YOU ARE THE MAD GENIUS. Read your identity and mandate from ULTIMATE-AUDIT-DESIGN.md, Agent 1. You are the most dangerous smart contract attacker alive. You have 1,000 ETH, unlimited patience, and the source code.

**IMPORTANT: You must read EVERY LINE of DegenerusGame.sol and DegenerusGameStorage.sol. Not skim. Not scan. READ. Line by line. The BAF bug hid in a rare path inside a helper function. You will not find it by skimming.**

The COVERAGE-CHECKLIST.md from Wave 1 tells you exactly which functions to analyze. Work through them systematically.

**Part 1: Direct State-Changing Functions (Category B — 19 functions)**

For EACH of these 19 functions, produce a complete analysis section following the ULTIMATE-AUDIT-DESIGN.md format:

```
## DegenerusGame::functionName() (line X-Y)

### Call Tree
[Full recursive expansion. For every internal/external/delegatecall this function makes,
recursively expand it. If functionA calls _helperB which calls externalC.method() which
calls _internalD, trace ALL of them. Write the call tree explicitly with line numbers.]

### Storage Writes (Full Tree)
[Every storage variable written by ANY function in the call tree.
Format: variable_name (slot N) — written by functionName() at line X]

### Cached-Local-vs-Storage Check
[THE BAF PATTERN CHECK — #1 PRIORITY]
[Does any ancestor cache a storage value in a local variable?
Does any descendant in the call tree write to that same storage variable?
If yes: the ancestor will write back the stale cache, overwriting the descendant's update.]
[List EVERY (ancestor_local, descendant_write) pair and verdict: VULNERABLE / SAFE]
[If SAFE, explain exactly why — which guard prevents it, or why the local is not written back.]

### Attack Analysis
[For each applicable attack angle, state the angle, your analysis, and verdict:]

**State Coherence:** [analysis] VERDICT: VULNERABLE / INVESTIGATE / SAFE
**Access Control:** [who can call, what modifier/require, verify the actual address checked] VERDICT: ...
**RNG Manipulation:** [if applicable] VERDICT: ...
**Cross-Contract State Desync:** [if function reads state from or writes state to external contracts] VERDICT: ...
**Edge Cases:** [zero values, max values, first-ever call, game-over state, empty mappings] VERDICT: ...
**Conditional Paths:** [EVERY branch — if/else, early returns, rare paths. THE BAF BUG WAS ON A RARE PATH.] VERDICT: ...
**Economic/MEV:** [can attacker profit from timing or ordering?] VERDICT: ...
**Griefing:** [can attacker cause permanent damage without profiting?] VERDICT: ...
**Ordering/Sequencing:** [unexpected call order?] VERDICT: ...
**Silent Failures:** [does any path silently succeed when it should revert?] VERDICT: ...
```

**Priority order for Category B functions (analyze in this order):**

Tier 1 — Highest Risk (analyze FIRST and DEEPEST):
1. `recordMint()` (lines ~374-419) — Multiple payment paths, delegatecall to _recordMintDataModule, earlybird DGNRS award. BAF-class risk: caches local prize contribution then calls delegatecall.
2. `resolveRedemptionLootbox()` (lines ~1729-1779) — HYBRID. Unchecked arithmetic on line ~1744. Prize pool writes before delegatecall loop.
3. `_claimWinningsInternal()` (lines ~1361-1377) — CEI for ETH payouts. Sentinel pattern (claimableWinnings = 1). stETH fallback.
4. `claimAffiliateDgnrs()` (lines ~1388-1431) — Multi-step pro-rata calculation. External calls to dgnrs and coin contracts. Deity pass bonus.
5. `_setAfKingMode()` (lines ~1566-1605) — Cross-contract state sync with coinflip. Multiple autoRebuyState writes.

Tier 2 — Medium Risk:
6. `receive()` (lines ~2838-2847) — uint128 truncation risk. Prize pool freeze state routing.
7. `adminStakeEthForStEth()` (lines ~1833-1853) — Reserve calculation. Edge case when claimablePool < stethSettleable.
8. `adminSwapEthForStEth()` (lines ~1813-1824) — stETH transfer rounding risk.
9. `constructor()` — Initial state setup. Verify all state variables initialized correctly.
10-19. Remaining direct functions: `recordMintQuestStreak`, `payCoinflipBountyDgnrs`, `setOperatorApproval`, `claimWinnings`, `claimWinningsStethFirst`, `setAutoRebuy`, `setDecimatorAutoRebuy`, `setAutoRebuyTakeProfit`, `setAfKingMode`, `deactivateAfKingFromCoin`, `syncAfKingLazyPassFromCoin`, `setLootboxRngThreshold`.

**Part 2: Internal Helpers (Category C)**

Internal helpers are analyzed AS PART of their caller's call tree (they appear in Part 1 call trees). However, if any internal helper has complex logic that deserves standalone analysis, give it its own section. At minimum, the following Storage.sol helpers warrant attention if they appear in any call tree:
- `_queueTickets`, `_queueTicketsScaled`, `_queueTicketRange` — ticket queue manipulation
- `_setPrizePools`, `_setPendingPools` — prize pool writes (freeze-state routing)
- `_setNextPrizePool`, `_setFuturePrizePool` — individual pool setters
- `_awardEarlybirdDgnrs` — external call to DGNRS contract
- `_recordMintStreakForLevel` (MintStreakUtils) — mintPacked_ writes

**Part 3: Delegatecall Dispatch Verification (Category A — 30 functions)**

For EACH of the 30 delegatecall dispatchers, produce a shorter verification section:

```
## DegenerusGame::functionName() [DISPATCH] (line X-Y)

### Dispatch Verification
- Module address: [constant name] = [address from ContractAddresses.sol]
- Selector encoded: [interface.function.selector]
- Parameters forwarded: [all params listed, verified against interface signature]
- Return value decoded: [yes/no, type if yes]
- Pre/post-delegatecall code: [NONE / describe any code outside standard dispatch pattern]
- Access control owner: ROUTER / MODULE / BOTH [which layer checks msg.sender?]

### Verdict: CORRECT / INCORRECT / INVESTIGATE
[If INCORRECT or INVESTIGATE: exact details of what's wrong]
```

**SPECIFIC ANOMALIES TO INVESTIGATE:**
1. `consumeDecimatorBoon()` dispatches to `consumeDecimatorBoost.selector` — verify this name mismatch is intentional and correctly wired
2. `resolveRedemptionLootbox()` — this is a HYBRID, already analyzed in Part 1. In Part 3, verify the dispatch portion specifically.
3. Functions where module address comes from ContractAddresses — verify the address constant matches the deployed module

**ANTI-SHORTCUTS DOCTRINE (from ULTIMATE-AUDIT-DESIGN.md):**
1. No "similar to above" — every function gets its own full analysis
2. No skipping private helpers — they get full scrutiny as part of call trees
3. No assuming guards work — read the modifier/require, trace what it checks
4. No skipping the rare path — if a function has an if(autoRebuy) branch, analyze it FULLY
5. No trusting previous audits — start from zero
6. No batch dismissals — "all setters are standard" is NOT acceptable
7. Call trees are MANDATORY
8. Storage write maps are MANDATORY

**BIAS: When in doubt, flag it. A false positive wastes 5 minutes of Skeptic review time. A missed bug costs the protocol.**

**What is NOT a valid attack (from ULTIMATE-AUDIT-DESIGN.md):**
- Arithmetic overflow/underflow (Solidity 0.8.34 built-in protection, audited in v3.0-v4.2)
- Classic reentrancy (no raw .call patterns, audited in v3.0-v3.3)
- Admin key compromise (owner trust assumption)
- VRF coordinator compromise (Chainlink trust assumption)
- Known issues listed in audit/KNOWN-ISSUES.md

**OUTPUT FORMAT:**
Start the ATTACK-REPORT.md with a summary table of all findings, then the per-function analysis sections.

```
# Unit 1: Game Router + Storage Layout — Attack Report

## Findings Summary
| ID | Function | Verdict | Severity | Title |
|----|----------|---------|----------|-------|
| F-01 | funcName | VULNERABLE | HIGH | [title] |
| F-02 | funcName | INVESTIGATE | MEDIUM | [title] |

## Detailed Analysis
[Per-function sections as described above]
```
  </action>

  <verify>
    <automated>test -f audit/unit-01/ATTACK-REPORT.md && echo "exists" && grep -c "## DegenerusGame::" audit/unit-01/ATTACK-REPORT.md && grep -c "VERDICT:" audit/unit-01/ATTACK-REPORT.md</automated>
  </verify>

  <acceptance_criteria>
    - File audit/unit-01/ATTACK-REPORT.md exists
    - Contains a Findings Summary table at the top
    - Has a dedicated analysis section for EVERY Category B function (19 direct state-changing)
    - Has a dispatch verification section for EVERY Category A function (30 dispatchers)
    - Every Category B section contains: Call Tree, Storage Writes (Full Tree), Cached-Local-vs-Storage Check, Attack Analysis with verdicts
    - Every Category A section contains: Dispatch Verification with module address, selector, params, return, access control, verdict
    - All line numbers reference actual source code lines (not estimates)
    - Every finding classified as VULNERABLE or INVESTIGATE includes exact line numbers, concrete attack scenario, and proof-of-concept steps
    - No function uses "similar to above" or "standard pattern" as justification for skipping analysis
    - The consumeDecimatorBoon/consumeDecimatorBoost mismatch is explicitly investigated
    - resolveRedemptionLootbox appears in BOTH Part 1 (full analysis) and Part 3 (dispatch verification)
    - Known issues from audit/KNOWN-ISSUES.md are NOT re-reported
  </acceptance_criteria>

  <done>Complete attack report with per-function analysis for all 49 state-changing functions (19 direct + 30 dispatch), with call trees, storage-write maps, cache checks, and multi-angle attack verdicts for every direct function</done>
</task>

</tasks>

<verification>
- ATTACK-REPORT.md covers every function on the COVERAGE-CHECKLIST.md
- Every direct state-changing function has all four required sections (Call Tree, Storage Writes, Cache Check, Attack Analysis)
- Every delegatecall dispatcher has dispatch verification
- All findings have verdicts (VULNERABLE / INVESTIGATE / SAFE)
- All VULNERABLE/INVESTIGATE findings have line numbers, attack scenarios, and PoC steps
</verification>

<success_criteria>
- The Mad Genius has systematically attacked every state-changing function in DegenerusGame.sol
- No function was skipped or dismissed without analysis
- The BAF-class cache-overwrite pattern was explicitly checked for every direct function
- All delegatecall dispatchers verified for correctness
- Report is ready for Skeptic review in Wave 3
</success_criteria>

<output>
After completion, create `.planning/phases/103-game-router-storage-layout/103-02-SUMMARY.md`
</output>
