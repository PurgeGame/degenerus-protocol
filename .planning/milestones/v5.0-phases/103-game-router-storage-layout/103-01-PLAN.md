---
phase: 103-game-router-storage-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/unit-01/COVERAGE-CHECKLIST.md
  - audit/unit-01/STORAGE-LAYOUT-VERIFICATION.md
autonomous: true
requirements: [COV-01, COV-02, COV-03]

must_haves:
  truths:
    - "Every state-changing function (external, public, internal, private) in DegenerusGame.sol and DegenerusGameStorage.sol has a checklist entry"
    - "Storage layout from forge inspect matches the manual slot comments in DegenerusGameStorage.sol"
    - "No module contract adds non-constant state variables that could collide with DegenerusGameStorage slots"
    - "The checklist categorizes functions by type: delegatecall dispatchers (30), direct state-changing (19), internal helpers (27), view/pure (60+)"
  artifacts:
    - path: "audit/unit-01/COVERAGE-CHECKLIST.md"
      provides: "Complete function checklist for Taskmaster coverage enforcement"
      contains: "Function Checklist"
    - path: "audit/unit-01/STORAGE-LAYOUT-VERIFICATION.md"
      provides: "Storage layout cross-reference and module alignment proof"
      contains: "forge inspect"
  key_links:
    - from: "audit/unit-01/COVERAGE-CHECKLIST.md"
      to: "contracts/DegenerusGame.sol"
      via: "Every state-changing function listed with line numbers"
      pattern: "line \\d+"
    - from: "audit/unit-01/STORAGE-LAYOUT-VERIFICATION.md"
      to: "contracts/storage/DegenerusGameStorage.sol"
      via: "Slot-by-slot cross-reference"
      pattern: "slot \\d+"
---

<objective>
Taskmaster Agent: Build the coverage checklist and verify storage layout for Unit 1 (DegenerusGame + DegenerusGameStorage).

Purpose: The Taskmaster ensures no function is skipped during the Mad Genius attack phase. The storage layout verification proves the delegatecall module system shares a coherent storage layout — a prerequisite for all subsequent module audits.

Output: COVERAGE-CHECKLIST.md (function-by-function audit tracking table) and STORAGE-LAYOUT-VERIFICATION.md (forge-inspect cross-reference + module alignment proof)
</objective>

<execution_context>
@.planning/ULTIMATE-AUDIT-DESIGN.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-game-router-storage-layout/103-CONTEXT.md
@.planning/phases/103-game-router-storage-layout/103-RESEARCH.md
@audit/KNOWN-ISSUES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build complete function coverage checklist</name>
  <files>audit/unit-01/COVERAGE-CHECKLIST.md</files>

  <read_first>
    - .planning/ULTIMATE-AUDIT-DESIGN.md (Agent 3: Taskmaster section — adopt this persona)
    - .planning/phases/103-game-router-storage-layout/103-RESEARCH.md (complete function inventory)
    - contracts/DegenerusGame.sol (2,848 lines — read the ENTIRE file, line by line)
    - contracts/storage/DegenerusGameStorage.sol (1,613 lines — read the ENTIRE file, line by line)
    - contracts/modules/DegenerusGameMintStreakUtils.sol (read fully — inherited by DegenerusGame)
  </read_first>

  <action>
YOU ARE THE TASKMASTER. Read your identity and mandate from ULTIMATE-AUDIT-DESIGN.md, Agent 3.

Your job: Build the COVERAGE CHECKLIST that the Mad Genius must check off function-by-function. If a function is missing from your checklist, it will NOT be audited. Completeness is everything.

**Step 1: Read every line of DegenerusGame.sol (2,848 lines) and DegenerusGameStorage.sol (1,613 lines).**
Do NOT rely solely on the research inventory. The research is a starting point. You must independently verify every function exists and is correctly categorized. If you find a function the research missed, ADD IT.

**Step 2: Categorize every function into one of four categories:**

Category A — Delegatecall Dispatchers (expected ~30):
Functions that encode a selector, delegatecall to a module, and bubble up revert/return. For each, record:
- Function name and line range
- Module target (from ContractAddresses)
- Selector being encoded
- Whether any code exists before/after the delegatecall (if yes, flag as HYBRID — needs full analysis, not just dispatch verification)
- Access control: who can call this function?

Category B — Direct State-Changing Functions (expected ~19):
Functions that execute logic directly in DegenerusGame.sol. For each, record:
- Function name and line range
- Access control (exact modifier or require statement)
- Primary storage variables written
- External calls made
- Risk tier: Tier 1 (complex, multiple code paths, BAF-class risk), Tier 2 (moderate, fewer paths), Tier 3 (simple setter)

Category C — Private/Internal Helpers (expected ~27 across Game + Storage + MintStreakUtils):
State-changing helpers called by Category A or B functions. For each, record:
- Function name and line range
- Which contract defines it (Game, Storage, or MintStreakUtils)
- Called by which parent function(s)
- Storage variables written

Category D — View/Pure Functions (expected ~60+):
Read-only functions. For each, record:
- Function name
- Quick note: what it reads
- Flag any that expose internal state in a potentially dangerous way (e.g., revealing RNG words before they should be visible)

**Step 3: Produce COVERAGE-CHECKLIST.md with this exact structure:**

```
# Unit 1: Game Router + Storage Layout — Coverage Checklist

## Contracts Under Audit
- contracts/DegenerusGame.sol (X lines)
- contracts/storage/DegenerusGameStorage.sol (X lines)
- contracts/modules/DegenerusGameMintStreakUtils.sol (X lines)

## Checklist Summary
| Category | Count | Analysis Depth |
|----------|-------|---------------|
| A: Delegatecall Dispatchers | NN | Dispatch verification |
| B: Direct State-Changing | NN | Full Mad Genius |
| C: Internal Helpers | NN | Via caller's call tree |
| D: View/Pure | NN | Minimal |
| **TOTAL** | **NN** | |

## Category A: Delegatecall Dispatchers

| # | Function | Lines | Module | Selector | Hybrid? | Access Control | Analyzed? | Dispatch Correct? |
|---|----------|-------|--------|----------|---------|---------------|-----------|-------------------|
| 1 | funcName() | X-Y | MODULE_NAME | selector | NO/YES | who can call | pending | pending |

## Category B: Direct State-Changing Functions

| # | Function | Lines | Access Control | Storage Writes | External Calls | Risk Tier | Analyzed? | Call Tree? | Storage Map? | Cache Check? |
|---|----------|-------|---------------|----------------|----------------|-----------|-----------|------------|--------------|-------------|
| 1 | funcName() | X-Y | guard | vars | calls | 1/2/3 | pending | pending | pending | pending |

## Category C: Internal Helpers (State-Changing)

| # | Function | Lines | Contract | Called By | Storage Writes | Analyzed? | Call Tree? | Storage Map? | Cache Check? |
|---|----------|-------|----------|----------|----------------|-----------|------------|--------------|-------------|
| 1 | _funcName() | X-Y | Contract | parents | vars | pending | pending | pending | pending |

## Category D: View/Pure Functions

| # | Function | Lines | Reads | Security Note | Reviewed? |
|---|----------|-------|-------|---------------|-----------|
| 1 | funcName() | X-Y | what | note | pending |
```

All "Analyzed?" columns start as "pending" — the Mad Genius fills these in during Wave 2.

**CRITICAL: resolveRedemptionLootbox() is a HYBRID.** It performs direct state changes (claimableWinnings debit, claimablePool decrement, prize pool credit) BEFORE the delegatecall loop. It MUST appear in BOTH Category A (dispatch verification) AND Category B (full analysis). Flag this explicitly.

**CRITICAL: consumeDecimatorBoon() dispatches to consumeDecimatorBoost.selector.** The function name does not match the selector's source. Flag this name/selector mismatch explicitly for the Mad Genius to verify.

Per D-01: Delegatecall dispatchers get dispatch-correctness verification only. Direct state-changing functions (per D-02) get full Mad Genius treatment.
  </action>

  <verify>
    <automated>test -f audit/unit-01/COVERAGE-CHECKLIST.md && grep -c "pending" audit/unit-01/COVERAGE-CHECKLIST.md</automated>
  </verify>

  <acceptance_criteria>
    - File audit/unit-01/COVERAGE-CHECKLIST.md exists
    - Contains all four category tables (A, B, C, D)
    - Category A has exactly 30 delegatecall dispatchers (or documents why count differs from research)
    - Category B has exactly 19 direct state-changing functions (or documents why count differs)
    - Category C has all internal helpers from DegenerusGame.sol (27), DegenerusGameStorage.sol (15 state-changing), and MintStreakUtils.sol (2 state-changing)
    - Category D lists all view/pure functions (~60+)
    - Every entry has line numbers from the actual source (not estimates)
    - resolveRedemptionLootbox is flagged as HYBRID in Category A
    - consumeDecimatorBoon/consumeDecimatorBoost selector mismatch is flagged
    - All "Analyzed?" columns show "pending" (Mad Genius has not started yet)
    - No function from the source code is missing from the checklist
  </acceptance_criteria>

  <done>Complete coverage checklist with every state-changing function in DegenerusGame.sol, DegenerusGameStorage.sol, and MintStreakUtils.sol listed with line numbers, categorized, and ready for Mad Genius sign-off</done>
</task>

<task type="auto">
  <name>Task 2: Verify storage layout alignment across all modules</name>
  <files>audit/unit-01/STORAGE-LAYOUT-VERIFICATION.md</files>

  <read_first>
    - .planning/ULTIMATE-AUDIT-DESIGN.md (Agent 3: Taskmaster section)
    - .planning/phases/103-game-router-storage-layout/103-RESEARCH.md (storage layout structure, module inheritance)
    - .planning/phases/103-game-router-storage-layout/103-CONTEXT.md (decisions D-03, D-04, D-06)
    - contracts/storage/DegenerusGameStorage.sol (1,613 lines — read fully for manual slot comments)
    - contracts/DegenerusGame.sol (check inheritance chain)
    - contracts/ContractAddresses.sol (module addresses)
  </read_first>

  <action>
YOU ARE THE TASKMASTER. This task verifies the foundational assumption of the entire module system: that all delegatecall modules share an identical storage layout.

**Step 1: Run forge inspect on DegenerusGame to get authoritative slot assignments (per D-03).**

```bash
forge inspect DegenerusGame storage-layout --pretty
```

Save the full output. This is the AUTHORITATIVE storage map.

**Step 2: Cross-reference forge output against manual slot comments in DegenerusGameStorage.sol.**

Read the manual slot comments in DegenerusGameStorage.sol (the packed slot documentation near the top of the file). For every manually-documented slot, verify the forge output agrees. Document:
- Slot number (forge vs source comment)
- Variable name
- Type
- Offset within slot
- Size in bytes
- MATCH or MISMATCH

If any MISMATCH is found, this is a CRITICAL finding — flag it prominently.

**Step 3: Run forge inspect on ALL 10 module contracts (per D-04).**

```bash
forge inspect DegenerusGameAdvanceModule storage-layout --pretty
forge inspect DegenerusGameMintModule storage-layout --pretty
forge inspect DegenerusGameWhaleModule storage-layout --pretty
forge inspect DegenerusGameJackpotModule storage-layout --pretty
forge inspect DegenerusGameDecimatorModule storage-layout --pretty
forge inspect DegenerusGameEndgameModule storage-layout --pretty
forge inspect DegenerusGameGameOverModule storage-layout --pretty
forge inspect DegenerusGameLootboxModule storage-layout --pretty
forge inspect DegenerusGameBoonModule storage-layout --pretty
forge inspect DegenerusGameDegeneretteModule storage-layout --pretty
```

For each module, compare its storage layout against DegenerusGame's layout. Every slot must match EXACTLY (same variable, same type, same slot number, same offset). Document:
- Module name
- Total storage slots
- Match with DegenerusGame layout: EXACT MATCH or MISMATCH (with details)

**Step 4: Verify the DegeneretteModule diamond inheritance.**
DegenerusGameDegeneretteModule inherits from both DegenerusGamePayoutUtils and DegenerusGameMintStreakUtils. Solidity C3 linearization should resolve this cleanly since both paths lead to DegenerusGameStorage, but verify via forge inspect that no duplicate slots exist.

**Step 5: Verify no module adds non-constant state variables.**
The research pre-verified this via grep, but independently verify by checking if any module's forge inspect output contains variables NOT present in DegenerusGameStorage.sol. Constant and immutable declarations do not occupy storage slots and are expected.

**Step 6: Produce STORAGE-LAYOUT-VERIFICATION.md with this structure:**

```
# Unit 1: Storage Layout Verification

## Methodology
- Tool: forge inspect (Foundry 1.5.1-stable)
- Authoritative source: DegenerusGame storage layout
- Cross-reference: Manual slot comments in DegenerusGameStorage.sol
- Module alignment: All 10 delegatecall modules

## DegenerusGame Authoritative Layout
[Full forge inspect output, formatted as table]

## Manual Comment Cross-Reference
| Slot | Variable | Forge Says | Source Comment Says | Status |
|------|----------|------------|---------------------|--------|
[Every manually-documented slot]

## Module Alignment Matrix
| Module | Inherits Via | Slot Count | Layout Match | Notes |
|--------|-------------|------------|--------------|-------|
| DegenerusGameAdvanceModule | Direct | NN | EXACT/MISMATCH | |
[All 10 modules]

## Diamond Inheritance Check (DegeneretteModule)
[C3 linearization verification]

## Rogue Variable Check
[Confirm no module adds non-constant state variables]

## Verdict
[PASS: All layouts aligned / FAIL: Mismatches found (list)]
```

Per D-06: This phase proves the storage layout is correct. Per-module alignment verification at the function level is deferred to Phase 118.
  </action>

  <verify>
    <automated>test -f audit/unit-01/STORAGE-LAYOUT-VERIFICATION.md && grep -c "EXACT\|MATCH\|MISMATCH\|PASS\|FAIL" audit/unit-01/STORAGE-LAYOUT-VERIFICATION.md</automated>
  </verify>

  <acceptance_criteria>
    - File audit/unit-01/STORAGE-LAYOUT-VERIFICATION.md exists
    - Contains full forge inspect output for DegenerusGame
    - Contains slot-by-slot cross-reference against manual comments in DegenerusGameStorage.sol
    - Contains forge inspect comparison for all 10 module contracts
    - DegeneretteModule diamond inheritance explicitly verified
    - No-rogue-variable check completed
    - Clear PASS or FAIL verdict with evidence
    - Any MISMATCH is documented with exact slot numbers and variable names
  </acceptance_criteria>

  <done>Storage layout verified as aligned across DegenerusGame and all 10 delegatecall modules, with forge-inspect cross-reference against manual comments, and PASS/FAIL verdict issued</done>
</task>

</tasks>

<verification>
- COVERAGE-CHECKLIST.md contains every state-changing function from DegenerusGame.sol (119 functions), DegenerusGameStorage.sol (38 functions), and MintStreakUtils.sol (2 functions)
- STORAGE-LAYOUT-VERIFICATION.md has forge inspect output for DegenerusGame + all 10 modules
- All checklist entries have line numbers from actual source
- Storage layout verdict is clearly PASS or FAIL
</verification>

<success_criteria>
- Taskmaster has built a complete function checklist that the Mad Genius can systematically work through
- Storage layout is proven correct (or mismatches are documented as findings)
- No state-changing function in the audit scope is missing from the checklist
</success_criteria>

<output>
After completion, create `.planning/phases/103-game-router-storage-layout/103-01-SUMMARY.md`
</output>
