---
phase: 132-event-correctness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/event-correctness-nongame.md
autonomous: true
requirements: [EVT-01, EVT-02, EVT-03]

must_haves:
  truths:
    - "Every external/public state-changing function in all non-game production contracts is audited for event emission"
    - "Every emitted event's parameter values are verified against actual post-state"
    - "Indexer-critical transitions (token transfers, governance actions, vault operations) all verified for sufficient data"
    - "Libraries confirmed to have no event emissions (or documented if they do)"
  artifacts:
    - path: "audit/event-correctness-nongame.md"
      provides: "Partial event audit for all non-game contracts"
      contains: "BurnieCoin"
  key_links:
    - from: "contracts/*.sol"
      to: "audit/event-correctness-nongame.md"
      via: "function-by-function event audit"
      pattern: "emit"
---

<objective>
Audit event correctness across all non-game production contracts: 6 token contracts, 5 admin/governance contracts, 4 periphery contracts, and 5 libraries. Three verification passes per external/public state-changing function.

Purpose: These contracts handle token transfers, governance actions, vault operations, and affiliate/quest systems. Many of the 107 bot-race findings (NC-9/10/11/17/33) target these contracts' event patterns.

Output: Partial report `audit/event-correctness-nongame.md`. Will be merged into final `audit/event-correctness.md` by Plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-event-correctness/132-CONTEXT.md
@audit/bot-race/4naly3er-triage.md (lines 419-424: NC-9/10/11/17/33 routed to Phase 132)
@audit/bot-race/slither-triage.md (DOC-02: events-maths)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit token contracts + DegenerusVault</name>
  <files>audit/event-correctness-nongame.md</files>
  <read_first>
    contracts/BurnieCoin.sol
    contracts/BurnieCoinflip.sol
    contracts/DegenerusStonk.sol
    contracts/StakedDegenerusStonk.sol
    contracts/GNRUS.sol
    contracts/WrappedWrappedXRP.sol
    contracts/DegenerusVault.sol
    audit/bot-race/4naly3er-triage.md
    audit/bot-race/slither-triage.md
  </read_first>
  <action>
Create `audit/event-correctness-nongame.md` with partial report. Methodology per D-02: three passes per function.

**Contracts to audit (7 files, ~5,300 lines, ~92 emits):**

1. **BurnieCoin.sol** (1075 lines, 17 emits) — ERC-20 token with burn mechanics, coinflip claim integration
2. **BurnieCoinflip.sol** (1129 lines, 17 emits) — Coinflip game with VRF, claim/resolve paths
3. **DegenerusStonk.sol** (305 lines, 8 emits) — DGNRS ERC-20 token
4. **StakedDegenerusStonk.sol** (839 lines, 14 emits) — sDGNRS soulbound governance token
5. **GNRUS.sol** (539 lines, 12 emits) — GNRUS soulbound token with burn redemption
6. **WrappedWrappedXRP.sol** (389 lines, 8 emits) — WWXRP wrapper token
7. **DegenerusVault.sol** (1050 lines, 10 emits) — Vault with staking, ETH distribution, skim

**For each contract, produce:**

```markdown
## ContractName (filename.sol)

### Event Inventory
| Event | Parameters | Indexed Fields | Inherited (OZ) |
|-------|-----------|----------------|-----------------|

### Function-by-Function Audit
| Function | Visibility | State Changes | Event Emitted | Params Match Post-State | Indexer-Critical | Finding |
|----------|-----------|--------------|---------------|------------------------|-----------------|---------|

### Findings
- [Finding ID]: [Description] — Disposition: DOCUMENT
```

**Indexer-critical events for these contracts:**
- Token transfers (Transfer events on all 6 tokens) — must have from/to/amount
- Approval changes — must have owner/spender/amount
- Vault staking/unstaking — must emit with actual staked amounts
- Coinflip bet/resolve — must emit with bet amounts and outcomes
- GNRUS burn redemption — must emit with redeemed amounts
- Governance actions (sDGNRS) — vote weight changes

**Special attention:**
- BurnieCoin._claimCoinflipShortfall may mint tokens during transfer — verify the Transfer event reflects actual minted amount
- BurnieCoin transfer to VAULT burns tokens — verify Transfer event has correct to/amount
- DegenerusVault._payEth — Slither DOC-02 flagged missing claimablePool event; verify and document
- StakedDegenerusStonk soulbound restrictions — verify Transfer events only fire for mint/burn, never for blocked transfers
- WrappedWrappedXRP wrap/unwrap — verify events match actual wrapped/unwrapped amounts
- OpenZeppelin inherited events (Transfer, Approval) — note as inherited, verify they fire correctly

**Per D-03:** Default disposition is DOCUMENT, not fix. No contract code changes.
**Per D-04:** Only evaluate indexed fields on indexer-critical events.
  </action>
  <verify>
    <automated>grep -c "BurnieCoin\|BurnieCoinflip\|DegenerusStonk\|StakedDegenerusStonk\|GNRUS\|WrappedWrappedXRP\|DegenerusVault" audit/event-correctness-nongame.md | xargs test 5 -le</automated>
  </verify>
  <acceptance_criteria>
    - audit/event-correctness-nongame.md exists
    - Contains sections for all 7 contracts: BurnieCoin, BurnieCoinflip, DegenerusStonk, StakedDegenerusStonk, GNRUS, WrappedWrappedXRP, DegenerusVault
    - Each section has event inventory and function audit content
    - Slither DOC-02 (claimablePool) is explicitly addressed in DegenerusVault or DegenerusGame section
    - OpenZeppelin inherited events noted where applicable
    - Findings have DOCUMENT disposition per D-03
  </acceptance_criteria>
  <done>All 7 token/vault contracts audited for event correctness. Partial report written with event inventories, function-by-function audit, and findings.</done>
</task>

<task type="auto">
  <name>Task 2: Audit admin, governance, periphery contracts + libraries</name>
  <files>audit/event-correctness-nongame.md</files>
  <read_first>
    audit/event-correctness-nongame.md
    contracts/DegenerusAdmin.sol
    contracts/DegenerusAffiliate.sol
    contracts/DegenerusQuests.sol
    contracts/DegenerusJackpots.sol
    contracts/DegenerusDeityPass.sol
    contracts/ContractAddresses.sol
    contracts/Icons32Data.sol
    contracts/DegenerusTraitUtils.sol
    contracts/DeityBoonViewer.sol
    contracts/libraries/BitPackingLib.sol
    contracts/libraries/EntropyLib.sol
    contracts/libraries/GameTimeLib.sol
    contracts/libraries/JackpotBucketLib.sol
    contracts/libraries/PriceLookupLib.sol
  </read_first>
  <action>
Append to `audit/event-correctness-nongame.md` with sections for admin/governance, periphery, and library contracts.

**Admin/governance contracts (5 files, ~3,970 lines, ~46 emits):**
1. **DegenerusAdmin.sol** (803 lines, 17 emits) — Admin setter functions, governance configuration
2. **DegenerusAffiliate.sol** (880 lines, 14 emits) — Affiliate registration, referral tracking
3. **DegenerusQuests.sol** (1598 lines, 11 emits) — Quest system, reward distribution
4. **DegenerusJackpots.sol** (650 lines, 1 emit) — Jackpot pool management
5. **DegenerusDeityPass.sol** (392 lines, 5 emits) — NFT pass with deity powers

**Periphery contracts (4 files, ~620 lines, 0 emits expected):**
6. **ContractAddresses.sol** (39 lines) — Address registry, likely no events
7. **Icons32Data.sol** (228 lines) — Static data, likely no events
8. **DegenerusTraitUtils.sol** (183 lines) — Trait utilities, likely no events
9. **DeityBoonViewer.sol** (171 lines) — View-only contract, no events expected

**Libraries (5 files, ~560 lines, 0 emits expected):**
10. **BitPackingLib.sol** (88 lines) — Bit manipulation, no events
11. **EntropyLib.sol** (24 lines) — RNG utility, no events
12. **GameTimeLib.sol** (35 lines) — Time utility, no events
13. **JackpotBucketLib.sol** (307 lines) — Jackpot math, no events
14. **PriceLookupLib.sol** (47 lines) — Price lookup, no events

**For admin/governance contracts, same full audit format as Task 1.**

**Indexer-critical events in these contracts:**
- DegenerusAdmin: All parameter change events (per NC-17, 27 instances flagged for missing events on critical parameter changes). Governance actions.
- DegenerusAffiliate: Affiliate registration, referral payouts
- DegenerusQuests: Quest completion, reward distribution
- DegenerusJackpots: Jackpot pool deposits/withdrawals
- DegenerusDeityPass: NFT mint/transfer events

**For periphery contracts and libraries:**
- Verify they emit NO events (confirming no hidden state changes)
- If any do emit events, audit them with the standard three-pass methodology
- A brief "No state-changing functions / No events" note is sufficient for pure view/data contracts

**Special attention for NC-17 (Missing event for critical parameter change):**
DegenerusAdmin.sol is the primary target for NC-17 findings. For every admin setter function:
- Does it emit an event for the parameter change?
- If yes, does the event contain the new value? Per NC-11, should it contain old+new?
- If no event: is this a critical parameter that off-chain monitoring should track?

**Per D-03:** Default disposition is DOCUMENT, not fix.
**Per D-04:** Only evaluate indexed fields on indexer-critical events.
  </action>
  <verify>
    <automated>grep -c "DegenerusAdmin\|DegenerusAffiliate\|DegenerusQuests\|DegenerusJackpots\|DegenerusDeityPass\|Libraries" audit/event-correctness-nongame.md | xargs test 5 -le</automated>
  </verify>
  <acceptance_criteria>
    - audit/event-correctness-nongame.md contains sections for DegenerusAdmin, DegenerusAffiliate, DegenerusQuests, DegenerusJackpots, DegenerusDeityPass
    - audit/event-correctness-nongame.md contains a section for periphery contracts (ContractAddresses, Icons32Data, DegenerusTraitUtils, DeityBoonViewer)
    - audit/event-correctness-nongame.md contains a section for libraries (5 files)
    - NC-17 findings explicitly assessed for DegenerusAdmin setter functions
    - Libraries confirmed as no-event contracts
    - All findings have DOCUMENT disposition per D-03
  </acceptance_criteria>
  <done>All non-game contracts audited. audit/event-correctness-nongame.md is a complete partial report covering tokens, admin/governance, periphery, and libraries.</done>
</task>

</tasks>

<verification>
- audit/event-correctness-nongame.md exists with sections for all non-game contracts
- Token contracts audited with attention to inherited OZ events
- Admin contracts audited with attention to NC-17 (missing parameter change events)
- Libraries confirmed as no-event contracts
- All findings have DOCUMENT disposition per D-03
</verification>

<success_criteria>
- Complete event audit of all non-game production contracts (~12.5K lines)
- Every state-changing function assessed for event emission
- Parameter correctness verified
- Indexer-critical transitions verified
- Slither DOC-02 addressed
- Partial report ready for assembly in Plan 03
</success_criteria>

<output>
After completion, create `.planning/phases/132-event-correctness/132-02-SUMMARY.md`
</output>
