---
phase: 133-comment-re-scan
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/modules/DegenerusGameLootboxModule.sol
  - contracts/modules/DegenerusGameMintModule.sol
  - contracts/modules/DegenerusGameDecimatorModule.sol
  - contracts/modules/DegenerusGameDegeneretteModule.sol
  - contracts/modules/DegenerusGameWhaleModule.sol
  - contracts/modules/DegenerusGameEndgameModule.sol
  - contracts/modules/DegenerusGameGameOverModule.sol
  - contracts/modules/DegenerusGameBoonModule.sol
  - contracts/modules/DegenerusGameMintStreakUtils.sol
  - contracts/modules/DegenerusGamePayoutUtils.sol
autonomous: true
requirements: [CMT-01, CMT-02]

must_haves:
  truths:
    - "Every @param/@return in all 10 game modules matches current function signatures"
    - "Inline comments in v6.0-modified functions (BoonModule charity hooks, GameOverModule drain) describe actual behavior"
    - "No NatSpec references deleted entities (e.g., removed lastLootboxRngWord, old jackpot chunk logic)"
  artifacts:
    - path: "contracts/modules/DegenerusGameLootboxModule.sol"
      provides: "Fixed NatSpec and inline comments"
    - path: "contracts/modules/DegenerusGameMintModule.sol"
      provides: "Fixed NatSpec and inline comments"
    - path: "contracts/modules/DegenerusGameDecimatorModule.sol"
      provides: "Fixed NatSpec and inline comments"
    - path: "contracts/modules/DegenerusGameDegeneretteModule.sol"
      provides: "Fixed NatSpec and inline comments"
    - path: "contracts/modules/DegenerusGameWhaleModule.sol"
      provides: "Fixed NatSpec and inline comments"
    - path: "contracts/modules/DegenerusGameEndgameModule.sol"
      provides: "Fixed NatSpec and inline comments"
    - path: "contracts/modules/DegenerusGameGameOverModule.sol"
      provides: "Fixed NatSpec and inline comments"
    - path: "contracts/modules/DegenerusGameBoonModule.sol"
      provides: "Fixed NatSpec and inline comments"
    - path: "contracts/modules/DegenerusGameMintStreakUtils.sol"
      provides: "Fixed NatSpec and inline comments"
    - path: "contracts/modules/DegenerusGamePayoutUtils.sol"
      provides: "Fixed NatSpec and inline comments"
  key_links: []
---

<objective>
Fix NatSpec and inline comment issues in all remaining game modules (10 files, ~7500 lines combined).

Purpose: Complete the game module comment sweep. These modules were modified in v6.0 (charity hooks, degenerette freeze fix) and earlier milestones.
Output: Comment-only fixes committed to the 10 module files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-comment-re-scan/133-CONTEXT.md
@audit/bot-race/4naly3er-report.md (NC-18/19/20 sections only)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix NatSpec/comments in LootboxModule + MintModule + DecimatorModule + DegeneretteModule + WhaleModule</name>
  <files>contracts/modules/DegenerusGameLootboxModule.sol, contracts/modules/DegenerusGameMintModule.sol, contracts/modules/DegenerusGameDecimatorModule.sol, contracts/modules/DegenerusGameDegeneretteModule.sol, contracts/modules/DegenerusGameWhaleModule.sol</files>
  <read_first>contracts/modules/DegenerusGameLootboxModule.sol, contracts/modules/DegenerusGameMintModule.sol, contracts/modules/DegenerusGameDecimatorModule.sol, contracts/modules/DegenerusGameDegeneretteModule.sol, contracts/modules/DegenerusGameWhaleModule.sol</read_first>
  <action>
    Scan every function in these 5 modules (~6000 lines combined). For each function:

    1. **NatSpec accuracy (CMT-01, per D-01/D-02):**
       - Verify every @param name matches the actual parameter name
       - Verify every @return describes what is actually returned
       - Verify @notice/@dev accurately describe current behavior
       - Add missing NatSpec on public/external functions (NC-18 bot findings)
       - Add missing @param/@return tags on documented functions (NC-19/NC-20)

    2. **Inline comment accuracy (CMT-02, per D-01):**
       - LootboxModule: verify comments reflect removal of lastLootboxRngWord (v6.0 change)
       - DecimatorModule: verify decimator jackpot comments match current 30% pool logic
       - DegeneretteModule: verify comments reflect degenerette freeze fix (v6.0 Phase 122)
       - MintModule: verify mint streak comments match current behavior
       - WhaleModule: verify whale pass comments are current

    CRITICAL CONSTRAINTS:
    - ONLY modify comments. NEVER modify code logic.
    - Commit with message like: "docs(133-02): fix NatSpec/comments in Lootbox+Mint+Decimator+Degenerette+Whale modules"
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Every public/external function in all 5 modules has @notice
    - Every @param matches its actual parameter name
    - No inline comment references lastLootboxRngWord or other removed storage
    - DegeneretteModule comments reflect frozen-context ETH routing through pending pools
    - forge build succeeds
  </acceptance_criteria>
  <done>All NatSpec and inline comments in the 5 larger game modules match current code. No stale references.</done>
</task>

<task type="auto">
  <name>Task 2: Fix NatSpec/comments in EndgameModule + GameOverModule + BoonModule + MintStreakUtils + PayoutUtils</name>
  <files>contracts/modules/DegenerusGameEndgameModule.sol, contracts/modules/DegenerusGameGameOverModule.sol, contracts/modules/DegenerusGameBoonModule.sol, contracts/modules/DegenerusGameMintStreakUtils.sol, contracts/modules/DegenerusGamePayoutUtils.sol</files>
  <read_first>contracts/modules/DegenerusGameEndgameModule.sol, contracts/modules/DegenerusGameGameOverModule.sol, contracts/modules/DegenerusGameBoonModule.sol, contracts/modules/DegenerusGameMintStreakUtils.sol, contracts/modules/DegenerusGamePayoutUtils.sol</read_first>
  <action>
    Scan every function in these 5 smaller modules (~1280 lines combined). For each function:

    1. **NatSpec accuracy (CMT-01):**
       - Verify every @param/@return matches actual signatures
       - Add missing NatSpec on public/external functions (NC-18)
       - Add missing @param/@return tags (NC-19/NC-20)

    2. **Inline comment accuracy (CMT-02):**
       - BoonModule: verify charity hook comments (v6.0 Phase 124 addition) are accurate
       - GameOverModule: verify handleGameOver drain comments match current flow
       - EndgameModule: verify endgame condition comments are current
       - PayoutUtils: verify payout calculation comments match current logic
       - MintStreakUtils: verify streak reset/increment comments

    CRITICAL CONSTRAINTS:
    - ONLY modify comments. NEVER modify code logic.
    - Commit with message like: "docs(133-02): fix NatSpec/comments in Endgame+GameOver+Boon+MintStreak+Payout"
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Every public/external function in all 5 modules has @notice
    - BoonModule charity hook NatSpec matches actual GNRUS integration
    - GameOverModule drain comments match current flow
    - forge build succeeds
  </acceptance_criteria>
  <done>All NatSpec and inline comments in the 5 smaller game modules match current code. No stale references.</done>
</task>

</tasks>

<verification>
- `forge build` succeeds after all changes
- `git diff --stat` shows only .sol files in contracts/modules/
- All 10 module files have been scanned and fixed
</verification>

<success_criteria>
- All 10 game module files have accurate NatSpec on every public/external function
- No stale inline comments referencing removed/renamed entities
- Compilation passes
- Changes committed
</success_criteria>

<output>
After completion, create `.planning/phases/133-comment-re-scan/133-02-SUMMARY.md`
</output>
