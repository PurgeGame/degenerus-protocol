---
phase: 133-comment-re-scan
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/DegenerusGame.sol
  - contracts/storage/DegenerusGameStorage.sol
  - contracts/modules/DegenerusGameAdvanceModule.sol
  - contracts/modules/DegenerusGameJackpotModule.sol
autonomous: true
requirements: [CMT-01, CMT-02]

must_haves:
  truths:
    - "Every @param/@return in DegenerusGame matches current function signatures"
    - "Every @param/@return in DegenerusGameStorage matches current struct/mapping declarations"
    - "Every @param/@return in AdvanceModule and JackpotModule matches current function signatures"
    - "Inline comments in v6.0/v7.0-modified functions describe actual current behavior"
    - "No NatSpec references deleted/renamed entities in these 4 files"
  artifacts:
    - path: "contracts/DegenerusGame.sol"
      provides: "Fixed NatSpec and inline comments"
    - path: "contracts/storage/DegenerusGameStorage.sol"
      provides: "Fixed NatSpec and inline comments"
    - path: "contracts/modules/DegenerusGameAdvanceModule.sol"
      provides: "Fixed NatSpec and inline comments"
    - path: "contracts/modules/DegenerusGameJackpotModule.sol"
      provides: "Fixed NatSpec and inline comments"
  key_links: []
---

<objective>
Fix NatSpec and inline comment issues in the game core contracts: DegenerusGame, DegenerusGameStorage, DegenerusGameAdvanceModule, and DegenerusGameJackpotModule.

Purpose: These are the largest and most complex contracts (~8700 lines combined). Accurate comments prevent wardens from filing NC findings and help auditors understand the code.
Output: Comment-only fixes committed to the 4 contract files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-comment-re-scan/133-CONTEXT.md
@audit/bot-race/4naly3er-triage.md
@audit/bot-race/4naly3er-report.md (NC-18/19/20/34 sections only)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix NatSpec and inline comments in DegenerusGame + DegenerusGameStorage</name>
  <files>contracts/DegenerusGame.sol, contracts/storage/DegenerusGameStorage.sol</files>
  <read_first>contracts/DegenerusGame.sol, contracts/storage/DegenerusGameStorage.sol, audit/bot-race/4naly3er-report.md (NC-18/19/20 sections for these files)</read_first>
  <action>
    Scan every function in DegenerusGame.sol and DegenerusGameStorage.sol. For each function:

    1. **NatSpec accuracy (CMT-01, per D-01/D-02):**
       - Verify every @param name matches the actual parameter name in the signature
       - Verify every @return describes what is actually returned
       - Verify @notice/@dev accurately describe what the function does
       - Fix any stale references to renamed/removed variables, functions, or constants
       - Add missing NatSpec on public/external functions that lack it (per NC-18 bot findings)
       - Add missing @param tags on documented functions (per NC-19 bot findings)
       - Add missing @return tags on documented functions (per NC-20 bot findings)

    2. **Inline comment accuracy (CMT-02, per D-01):**
       - Read each inline comment and verify it describes what the code actually does NOW
       - Fix any comments describing removed/changed logic (especially v6.0/v7.0 delta changes)
       - Fix stale references to deleted variables/constants/functions

    Known NC-18 instances in DegenerusGame.sol from 4naly3er: line ~1119 recordTerminalDecBurn (missing @notice), line ~1139 runTerminalDecimatorJackpot (missing @notice).
    Known NC-19 instances in DegenerusGame.sol: payCoinflipBountyDgnrs (missing @param for winningBet and bountyPool), recordTerminalDecBurn (missing @param tags), runTerminalDecimatorJackpot (missing @param tags).
    Known NC-20 instance in DegenerusGame.sol: runTerminalDecimatorJackpot (missing @return).

    CRITICAL CONSTRAINTS:
    - ONLY modify comments (NatSpec and inline). NEVER modify code logic, even whitespace in code.
    - NEVER touch ContractAddresses.sol.
    - Commit with message like: "docs(133-01): fix NatSpec/comments in DegenerusGame + GameStorage"
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Every public/external function in DegenerusGame.sol has @notice
    - Every @param in DegenerusGame.sol matches its actual parameter name
    - Every @return in DegenerusGame.sol matches the actual return value
    - No inline comment references a removed/renamed variable or function
    - DegenerusGameStorage.sol NatSpec matches current storage layout
    - forge build succeeds (comment-only changes cannot break compilation)
  </acceptance_criteria>
  <done>All NatSpec tags in DegenerusGame.sol and DegenerusGameStorage.sol match current function signatures and behavior. No stale comments remain.</done>
</task>

<task type="auto">
  <name>Task 2: Fix NatSpec and inline comments in AdvanceModule + JackpotModule</name>
  <files>contracts/modules/DegenerusGameAdvanceModule.sol, contracts/modules/DegenerusGameJackpotModule.sol</files>
  <read_first>contracts/modules/DegenerusGameAdvanceModule.sol, contracts/modules/DegenerusGameJackpotModule.sol, audit/bot-race/4naly3er-report.md (NC-18/19/20 sections for these files)</read_first>
  <action>
    Scan every function in DegenerusGameAdvanceModule.sol (~1582 lines) and DegenerusGameJackpotModule.sol (~2721 lines). For each function:

    1. **NatSpec accuracy (CMT-01):**
       - Verify every @param name matches the actual parameter name
       - Verify every @return describes what is actually returned
       - Verify @notice/@dev accurately describe current behavior
       - Add missing NatSpec on public/external functions (NC-18)
       - Add missing @param/@return tags (NC-19/NC-20)

    2. **Inline comment accuracy (CMT-02):**
       - Verify inline comments describe what the code actually does NOW
       - Fix any stale descriptions from pre-v6.0/v7.0 logic
       - Pay special attention to jackpot distribution comments (changed in v4.2 daily jackpot chunk removal)

    CRITICAL CONSTRAINTS:
    - ONLY modify comments. NEVER modify code logic.
    - Commit with message like: "docs(133-01): fix NatSpec/comments in AdvanceModule + JackpotModule"
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Every public/external function in both modules has @notice
    - Every @param matches its actual parameter name
    - Every @return matches the actual return value
    - No inline comment references removed jackpot chunk logic or other deleted features
    - forge build succeeds
  </acceptance_criteria>
  <done>All NatSpec and inline comments in AdvanceModule and JackpotModule match current code behavior. No stale references.</done>
</task>

</tasks>

<verification>
- `forge build` succeeds after all changes
- `git diff --stat` shows only .sol files in contracts/ and contracts/modules/ and contracts/storage/
- `git diff` shows only comment changes (lines starting with `///`, `//`, `/*`, or `*`)
</verification>

<success_criteria>
- All 4 contract files have accurate NatSpec on every public/external function
- No stale inline comments referencing removed/renamed entities
- Compilation passes
- Changes committed
</success_criteria>

<output>
After completion, create `.planning/phases/133-comment-re-scan/133-01-SUMMARY.md`
</output>
