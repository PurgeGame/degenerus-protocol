---
phase: 127-degeneruscharity-full-adversarial-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/unit-charity/01-TOKEN-OPS-AUDIT.md
autonomous: true
requirements: [CHAR-01, CHAR-02]

must_haves:
  truths:
    - "Every token operation function (transfer, transferFrom, approve, burn, _mint, totalSupply, balanceOf, constructor, receive) has Mad Genius attack analysis with explicit VULNERABLE/INVESTIGATE/SAFE verdict"
    - "Soulbound enforcement is proven correct — transfer, transferFrom, approve all unconditionally revert"
    - "Proportional burn-for-redemption math is proven correct including last-holder sweep, dust handling, zero-supply edge, and rounding"
    - "Supply invariant holds: totalSupply == sum(balanceOf[all addresses]) at all times"
    - "BAF-class cache-overwrite check completed on burn() — the only function that reads then writes storage"
    - "Skeptic has validated every Mad Genius finding with CONFIRMED/REFUTED/DOWNGRADED verdict"
  artifacts:
    - path: "audit/unit-charity/01-TOKEN-OPS-AUDIT.md"
      provides: "Complete three-agent adversarial audit of all token operation functions"
      contains: "VERDICT:"
  key_links:
    - from: "audit/unit-charity/01-TOKEN-OPS-AUDIT.md"
      to: "contracts/DegenerusCharity.sol"
      via: "Line-by-line function analysis with code citations"
      pattern: "line \\d+"
---

<objective>
Three-agent adversarial audit of DegenerusCharity.sol token operations: soulbound enforcement (transfer/transferFrom/approve), burn-for-redemption, _mint, totalSupply, balanceOf, constructor, and receive().

Purpose: Prove GNRUS token economics are correct and safe — soulbound enforcement cannot be bypassed, proportional redemption math is sound, and supply invariants hold. This is the highest-value attack surface in the contract because burn() handles ETH and stETH outflows.

Output: audit/unit-charity/01-TOKEN-OPS-AUDIT.md with per-function Mad Genius analysis, Skeptic validation, and Taskmaster coverage sign-off.
</objective>

<execution_context>
@.planning/ULTIMATE-AUDIT-DESIGN.md
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-degeneruscharity-full-adversarial-audit/127-CONTEXT.md
@.planning/phases/126-delta-extraction-plan-reconciliation/FUNCTION-CATALOG.md
@contracts/DegenerusCharity.sol
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mad Genius attack analysis of all token operation functions</name>
  <files>audit/unit-charity/01-TOKEN-OPS-AUDIT.md</files>

  <read_first>
    - .planning/ULTIMATE-AUDIT-DESIGN.md (Agent 1: Mad Genius — adopt this persona completely)
    - contracts/DegenerusCharity.sol (538 lines — read the ENTIRE file line by line)
    - .planning/phases/126-delta-extraction-plan-reconciliation/FUNCTION-CATALOG.md (Section 1 — DegenerusCharity function table)
    - .planning/phases/127-degeneruscharity-full-adversarial-audit/127-CONTEXT.md (decisions D-05, D-06)
  </read_first>

  <action>
YOU ARE THE MAD GENIUS. Read your identity and mandate from ULTIMATE-AUDIT-DESIGN.md, Agent 1.

Read contracts/DegenerusCharity.sol in its entirety (538 lines). Do NOT skim. Read every line.

Analyze these 9 functions/elements in order, using the full Mad Genius methodology (call tree, storage writes, attack from every angle, cached-local-vs-storage check):

**1. constructor() (line 245-247)**
- Verify _mint(address(this), INITIAL_SUPPLY) creates exactly 1T GNRUS
- Check: can constructor be called again? (No — constructor, but document why)
- Check: is INITIAL_SUPPLY correct (1_000_000_000_000 * 1e18)?

**2. _mint(address, uint256) (line 530-537)**
- Verify mint is private (cannot be called externally)
- Check unchecked block: totalSupply + amount overflow? (1T * 1e18 = 1e30, uint256 max = 1.15e77 — safe)
- Check: called only from constructor (grep for _mint usage)

**3. totalSupply() — public state variable (line 134)**
- View function. Document that it decreases on burn, never increases after constructor.
- Fast-track: SAFE (view, no attack surface)

**4. balanceOf(address) — public mapping (line 137)**
- View function. Document behavior.
- Fast-track: SAFE (view, no attack surface)

**5. transfer(address, uint256) (line 254)**
- Soulbound enforcement: verify it ALWAYS reverts with TransferDisabled()
- Check: is it `pure`? (Yes — cannot be overridden by storage manipulation)
- Per D-05: code-cite that `revert TransferDisabled()` is unconditional

**6. transferFrom(address, address, uint256) (line 257)**
- Same as transfer — verify unconditional revert
- Per D-05: code-cite the `pure` keyword and `revert TransferDisabled()`

**7. approve(address, uint256) (line 260)**
- Same as transfer — verify unconditional revert
- No allowance mapping exists in the contract (verify this — grep for "allowance")

**8. burn(uint256) (line 273-320) — PRIMARY ATTACK SURFACE**
This is the most complex function. Full depth analysis required:

Call tree expansion:
- burn reads: totalSupply, balanceOf[burner], balanceOf[address(this)], address(this).balance, steth.balanceOf(address(this)), game.claimableWinningsOf(address(this))
- burn calls: game.claimWinnings(address(this)) [conditional], steth.transfer(burner, stethOut), burner.call{value: ethOut}("")
- burn writes: balanceOf[burner], totalSupply

Storage writes map:
- balanceOf[burner] -= amount (line 306)
- totalSupply = supply - amount (line 307, unchecked)

Attack angles for burn():

a) **Reentrancy via ETH transfer (line 317-318):** burner.call{value: ethOut}("") is a raw call. Can the burner reenter burn()? Trace: state updates happen on lines 306-307 BEFORE external calls on 313-318. CEI pattern holds. But verify: does game.claimWinnings (line 297) have reentrancy risk? It's called BEFORE state updates — if game.claimWinnings triggers a callback to DegenerusCharity, what state does it see?

b) **Reentrancy via stETH.transfer (line 314):** stETH is Lido's contract — is it a standard ERC20 transfer or does it have hooks? stETH is a rebasing token. Its transfer does NOT have ERC-777 hooks. Document this assumption.

c) **Last-holder sweep logic (line 282-283):** Two conditions:
   - `burnerBal == amount` — simple case, burning everything
   - `(supply - balanceOf[address(this)]) == amount` — burning all non-contract supply
   In the second case, `amount` gets replaced with `burnerBal` which could be LARGER than original amount. Is this safe? Check: can a user pass amount = (supply - balanceOf[address(this)]) when their balance is much larger, getting their full balance burned and a proportional share based on that larger amount? YES — but they're burning MORE tokens for MORE payout, which is proportional. The sweep just prevents dust from being stranded. VERIFY this is not exploitable.

d) **Claimable winnings accounting (line 289-290):** `if (claimable > 1) { claimable -= 1; }` — why -1? This is likely stETH 1-wei rounding protection. Document.

e) **Division precision (line 292):** `owed = ((ethBal + stethBal + claimable) * amount) / supply` — check for division by zero. Can supply be 0? Only if all tokens are burned, but amount >= MIN_BURN (1e18) and balanceOf[burner] -= amount would underflow if burner has 0. Safe.

f) **BAF-class cache-overwrite check:** burn() caches `supply = totalSupply` and `burnerBal = balanceOf[burner]` locally. Does any called function write to totalSupply or balanceOf before burn() writes them back? game.claimWinnings is an EXTERNAL call — it cannot modify DegenerusCharity storage directly. steth.transfer and burner.call also external. NO BAF RISK — state writes happen after local caching and before external calls.

g) **ethOut / stethOut calculation (lines 302-303):** `ethOut = owed <= ethBal ? owed : ethBal` and `stethOut = owed - ethOut`. If owed > ethBal, then ethOut = ethBal and stethOut = owed - ethBal. Can stethOut exceed steth balance? Only if claimable was included in owed but claimWinnings wasn't called. But lines 296-300 ensure claimWinnings is called when owed > onHand. After claimWinnings, ethBal and stethBal are re-read. So the math should be consistent. BUT: what if claimWinnings deposits ETH but the contract also receives stETH from another source between reads? (Not possible in single transaction). SAFE.

h) **Unchecked totalSupply decrement (line 307):** `unchecked { totalSupply = supply - amount; }`. Is supply >= amount guaranteed? Yes — supply was read from totalSupply, and balanceOf[burner] -= amount on line 306 already succeeded (Solidity 0.8 underflow check). Since balanceOf[burner] <= totalSupply (invariant), supply >= amount. SAFE.

**9. receive() (line 506)**
- Accepts ETH from anyone. No logic. SAFE.

Per D-05/D-06 (analytical proof with code-cite):
After the per-function analysis, add a section proving these invariants:
- **Soulbound invariant:** No code path transfers GNRUS between external addresses. transfer/transferFrom/approve are pure reverts. _mint is private, only called in constructor. burn reduces supply. resolveLevel moves from address(this) to recipient. No other balance mutation exists.
- **Supply invariant:** totalSupply == sum(all balanceOf). _mint increments both. burn decrements both. resolveLevel's unchecked block preserves the sum (decrements address(this), increments recipient by same amount). handleGameOver decrements both by same amount.
- **Proportional redemption correctness:** burn(amount) pays amount/totalSupply of total backing. The last-holder sweep is the only non-proportional path, and it's MORE favorable to the last holder (they get slightly more than proportional due to dust).
- **Edge cases per D-06:** (a) Last burner: sweep gives full remaining balance. (b) Zero supply: unreachable because MIN_BURN check prevents burning to zero (burn requires amount >= 1e18, and balanceOf must cover it). Wait — can supply reach exactly 0? If the last GNRUS holder burns their entire balance, totalSupply becomes 0. But the next burn would fail because no one has tokens. SAFE. (c) Rounding: integer division truncates, so burner gets slightly less than proportional. Dust accumulates in the contract. Last-holder sweep handles this.

Format the output as specified in ULTIMATE-AUDIT-DESIGN.md (call tree, storage writes, attack analysis with verdicts, cached-local-vs-storage check).

Add a Skeptic Validation section at the end where you switch to the Skeptic persona (Agent 2 from ULTIMATE-AUDIT-DESIGN.md) and validate every finding. For each Mad Genius finding rated VULNERABLE or INVESTIGATE, the Skeptic must: read the cited code, trace the execution path, check preconditions, and issue CONFIRMED/REFUTED/DOWNGRADED verdict.

Add a Taskmaster Coverage section confirming all 9 token-domain functions have been analyzed (per D-02).
  </action>

  <verify>
    <automated>test -f audit/unit-charity/01-TOKEN-OPS-AUDIT.md && grep -c "VERDICT:" audit/unit-charity/01-TOKEN-OPS-AUDIT.md</automated>
  </verify>

  <acceptance_criteria>
    - File audit/unit-charity/01-TOKEN-OPS-AUDIT.md exists
    - Contains analysis for all 9 functions: constructor, _mint, totalSupply, balanceOf, transfer, transferFrom, approve, burn, receive
    - burn() analysis includes full call tree with line numbers from DegenerusCharity.sol
    - burn() analysis includes storage writes map listing balanceOf[burner] and totalSupply
    - burn() analysis includes BAF-class cached-local-vs-storage check
    - burn() analysis addresses reentrancy via ETH transfer (line 317-318) with explicit verdict
    - burn() analysis addresses last-holder sweep logic (line 282-283) with explicit verdict
    - Soulbound enforcement proof cites `pure` keyword on transfer/transferFrom/approve
    - Supply invariant proof covers _mint, burn, resolveLevel unchecked block, handleGameOver
    - Proportional redemption proof addresses last-burner dust and rounding
    - Skeptic validation section exists with CONFIRMED/REFUTED/DOWNGRADED for every VULNERABLE/INVESTIGATE finding
    - Taskmaster coverage section confirms 9/9 token functions analyzed
    - Every function has explicit VULNERABLE/INVESTIGATE/SAFE verdict
    - grep -c "VERDICT:" returns at least 9 (one per function)
  </acceptance_criteria>

  <done>All 9 token operation functions have Mad Genius attack analysis with verdicts, Skeptic validation of all non-SAFE findings, supply invariant proof, soulbound enforcement proof, proportional redemption correctness proof, and Taskmaster coverage confirmation showing 9/9 complete</done>
</task>

</tasks>

<verification>
- audit/unit-charity/01-TOKEN-OPS-AUDIT.md has per-function analysis for all 9 token operations
- Every function has an explicit VERDICT line
- burn() has full call tree, storage write map, and BAF check
- Skeptic has validated all non-SAFE findings
- Taskmaster confirms 9/9 coverage
- Soulbound, supply, and redemption invariant proofs are present with code citations
</verification>

<success_criteria>
- Every token operation function has a VULNERABLE/INVESTIGATE/SAFE verdict
- Soulbound enforcement is proven unconditionally correct (pure reverts)
- Proportional burn-for-redemption math is proven correct including edge cases
- Supply invariant is proven to hold across all state-changing paths
- No unvalidated VULNERABLE or INVESTIGATE findings remain
</success_criteria>

<output>
After completion, create `.planning/phases/127-degeneruscharity-full-adversarial-audit/127-01-SUMMARY.md`
</output>
