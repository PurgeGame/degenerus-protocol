---
phase: 128-changed-contract-adversarial-audit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/delta-v6/03-GAME-INTEGRATION-AUDIT.md
autonomous: true
requirements: [AUDIT-01, AUDIT-02, AUDIT-04]

must_haves:
  truths:
    - "Every Phase 124 game integration function (10 entries across GameOverModule, Stonk, Game, AdvanceModule, JackpotModule, LootboxModule) has Mad Genius attack analysis with VULNERABLE/INVESTIGATE/SAFE verdict"
    - "handleGameOverDrain 33/33/34 fund split is proven correct — no ETH rounding loss, all three recipients receive exact amounts"
    - "Path A handleGameOver removal (behavioral drift from Phase 126) is verified safe — handleGameOver only needed on Path B"
    - "yearSweep timing is verified: only callable 1 year after gameOverTimestamp, sweeps correct amount"
    - "claimWinningsStethFirst access control narrowing (VAULT+SDGNRS -> VAULT-only) verified: SDGNRS path still works correctly"
    - "gameOverTimestamp() view returns correct value from storage"
    - "BAF-class cache-overwrite check completed on every function that reads then writes storage"
    - "Skeptic has validated every non-SAFE Mad Genius finding"
  artifacts:
    - path: "audit/delta-v6/03-GAME-INTEGRATION-AUDIT.md"
      provides: "Three-agent adversarial audit of all Phase 124 game integration changes"
      contains: "VERDICT:"
  key_links:
    - from: "audit/delta-v6/03-GAME-INTEGRATION-AUDIT.md"
      to: "contracts/modules/DegenerusGameGameOverModule.sol"
      via: "handleGameOverDrain, handleFinalSweep, _sendStethFirst, _sendToVault analysis"
      pattern: "line \\d+"
    - from: "audit/delta-v6/03-GAME-INTEGRATION-AUDIT.md"
      to: "contracts/DegenerusStonk.sol"
      via: "yearSweep, gameOverTimestamp analysis"
      pattern: "line \\d+"
    - from: "audit/delta-v6/03-GAME-INTEGRATION-AUDIT.md"
      to: "contracts/DegenerusGame.sol"
      via: "gameOverTimestamp, claimWinningsStethFirst analysis"
      pattern: "line \\d+"
---

<objective>
Three-agent adversarial audit of all Phase 124 (game integration) changes: handleGameOverDrain fund split, yearSweep, gameOverTimestamp, claimWinningsStethFirst access control, and charity hook wiring in AdvanceModule/JackpotModule/LootboxModule.

Purpose: Phase 124 introduced the charity integration — ETH fund split changed from 50/50 to 33/33/34, yearSweep added as a 1-year post-gameover safety valve, and access control was narrowed on claimWinningsStethFirst. These are high-value ETH flow changes that must be proven correct. The Path A handleGameOver removal (behavioral drift) needs explicit safety analysis.

Output: audit/delta-v6/03-GAME-INTEGRATION-AUDIT.md with per-function analysis, Skeptic validation, and coverage sign-off.
</objective>

<execution_context>
@.planning/ULTIMATE-AUDIT-DESIGN.md
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-changed-contract-adversarial-audit/128-CONTEXT.md
@.planning/phases/126-delta-extraction-plan-reconciliation/FUNCTION-CATALOG.md
@.planning/phases/126-delta-extraction-plan-reconciliation/PLAN-RECONCILIATION.md
@.planning/phases/124-game-integration/124-CONTEXT.md
@audit/unit-charity/03-GAME-HOOKS-STORAGE-AUDIT.md
@audit/KNOWN-ISSUES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mad Genius + Skeptic + Taskmaster audit of Phase 124 game integration (10 functions)</name>
  <files>audit/delta-v6/03-GAME-INTEGRATION-AUDIT.md</files>

  <read_first>
    - .planning/ULTIMATE-AUDIT-DESIGN.md (all three agent personas)
    - .planning/phases/128-changed-contract-adversarial-audit/128-CONTEXT.md (decisions D-01, D-02, D-07)
    - .planning/phases/126-delta-extraction-plan-reconciliation/FUNCTION-CATALOG.md (Sections 4, 5, 6, 7, 8, 9 — Phase 124 functions)
    - .planning/phases/126-delta-extraction-plan-reconciliation/PLAN-RECONCILIATION.md (Path A handleGameOver removal drift analysis)
    - .planning/phases/124-game-integration/124-CONTEXT.md (design intent for charity game hooks)
    - contracts/modules/DegenerusGameGameOverModule.sol (read ENTIRE file — handleGameOverDrain, handleFinalSweep, _sendStethFirst, _sendToVault)
    - contracts/DegenerusStonk.sol (read yearSweep function and gameOverTimestamp — search for these specific functions)
    - contracts/DegenerusGame.sol (read gameOverTimestamp and claimWinningsStethFirst functions)
    - contracts/modules/DegenerusGameAdvanceModule.sol (read _finalizeRngRequest — the Phase 124 charity resolveLevel call)
    - contracts/modules/DegenerusGameJackpotModule.sol (read _distributeYieldSurplus — the Phase 124 charity share)
    - audit/unit-charity/03-GAME-HOOKS-STORAGE-AUDIT.md (Phase 127 findings on handleGameOver/resolveLevel call paths from the DegenerusCharity side — cross-reference)
  </read_first>

  <action>
Run the full three-agent adversarial audit cycle on the 10 Phase 124 game integration function catalog entries.

**Phase 124 Function Catalog (from FUNCTION-CATALOG.md):**

| # | Function | Contract | Change |
|---|----------|----------|--------|
| 1 | `_sendStethFirst(address, uint256, uint256)` | GameOverModule | new — extracted helper for stETH-first sending |
| 2 | `handleGameOverDrain()` | GameOverModule | modified — fund split changed from 50/50 to 33/33/34 (DGNRS/vault/GNRUS) |
| 3 | `handleFinalSweep()` | GameOverModule | modified — now sends to 3 recipients |
| 4 | `_sendToVault(uint256, uint256)` | GameOverModule | modified — parameter changes for new split |
| 5 | `yearSweep()` | DegenerusStonk | new — 1-year post-gameover sweep to vault |
| 6 | `gameOverTimestamp()` | DegenerusStonk | new — view exposing gameOverTime from Game |
| 7 | `gameOverTimestamp()` | DegenerusGame | new — view exposing gameOverTime storage |
| 8 | `claimWinningsStethFirst()` | DegenerusGame | modified — access restricted from VAULT+SDGNRS to VAULT-only |
| 9 | `_finalizeRngRequest(...)` | AdvanceModule | modified — added charityResolve.resolveLevel(lvl-1) call at level transition (audit the 124 portion here; 121 portions cross-ref Plan 1 per D-02) |
| 10 | `_distributeYieldSurplus(uint256)` | JackpotModule | modified — 46% accumulator split into 23% charity + 23% accumulator (audit the 124 charity routing here; arithmetic cross-ref Plan 1 per D-02) |

**STEP 1 — MAD GENIUS (Attacker)**

For each function, apply the full Mad Genius methodology:

1. Read the function line-by-line in the actual contract source
2. Build complete call tree for the changed portion
3. Map every storage write in the changed code path
4. Attack from every angle per ULTIMATE-AUDIT-DESIGN.md
5. Explicit BAF-class cached-local-vs-storage check (AUDIT-04)

**Special focus areas:**

**handleGameOverDrain (#2) — HIGHEST PRIORITY:**
- The fund split changed from `amount / 2` (50/50) to 33/33/34 with three recipients
- Verify: `dgnrsShare = total / 3`, `vaultShare = total / 3`, `charityShare = total - dgnrsShare - vaultShare` (or however it's implemented)
- Verify: no ETH is lost to rounding — all three shares sum to exactly the total
- Verify: Path A no longer calls handleGameOver on DegenerusCharity — is this safe? What if gameOver state on DegenerusCharity is never set when Path A fires? Cross-reference Phase 126 PLAN-RECONCILIATION.md for the drift analysis.
- Verify: Path B still calls handleGameOver correctly

**yearSweep (#5) — HIGH PRIORITY:**
- Verify: only callable after `block.timestamp >= gameOverTimestamp + 365 days` (or similar)
- Verify: sweeps the correct amount (unburned GNRUS-held ETH/stETH)
- Verify: can only be called once (or is idempotent)
- Verify: access control — who can call this?

**claimWinningsStethFirst (#8) — HIGH PRIORITY:**
- Verify: the access control change from `require(msg.sender == VAULT || msg.sender == SDGNRS)` to `require(msg.sender == VAULT)` is intentional
- Verify: SDGNRS can still claim winnings through the normal (non-stETH-first) path
- Verify: no ETH/stETH is stranded by this change

**_finalizeRngRequest (#9) and _distributeYieldSurplus (#10):**
- These have overlap with Plan 1 functions — audit only the Phase 124 charity-specific changes here
- For #9: verify resolveLevel(lvl-1) call is correct level argument, handles revert gracefully
- For #10: verify 23%/23% split is correct arithmetic, charity receiver address is correct

6. Verdict per function: VULNERABLE / INVESTIGATE / SAFE

**STEP 2 — SKEPTIC (Validator)**

For every VULNERABLE or INVESTIGATE finding:
1. Read the cited code yourself
2. Trace execution path — especially the three-way fund split arithmetic
3. Check preconditions
4. Classify: CONFIRMED / FALSE POSITIVE / DOWNGRADE TO INFO

**STEP 3 — TASKMASTER (Coverage Enforcer)**

Build coverage matrix for all 10 functions. Verify complete analysis, sign off PASS or FAIL.

Write to `audit/delta-v6/03-GAME-INTEGRATION-AUDIT.md` with sections:
1. Executive Summary
2. Per-function Mad Genius Analysis
3. Phase 126 Drift Analysis (Path A handleGameOver removal)
4. Skeptic Validation
5. Taskmaster Coverage Matrix
6. Final Verdict
  </action>

  <verify>
    <automated>grep -c "VERDICT:" audit/delta-v6/03-GAME-INTEGRATION-AUDIT.md</automated>
  </verify>

  <acceptance_criteria>
    - audit/delta-v6/03-GAME-INTEGRATION-AUDIT.md exists and contains "VERDICT:" for at least 10 functions
    - File contains "handleGameOverDrain" with 33/33/34 or three-way split analysis
    - File contains "Path A" analysis addressing handleGameOver removal drift
    - File contains "yearSweep" with timing/access control analysis
    - File contains "claimWinningsStethFirst" with access control narrowing analysis
    - File contains "BAF" or "cache-overwrite" for each state-changing function
    - File contains "## Taskmaster" or "Coverage Matrix" with 10 entries
    - File contains cross-reference to Plan 1 for shared functions (advanceGame, _distributeYieldSurplus)
  </acceptance_criteria>

  <done>All 10 Phase 124 game integration functions audited with three-agent methodology; handleGameOverDrain fund split proven correct; Path A drift verified safe; yearSweep timing verified; claimWinningsStethFirst access control change verified; BAF-class checks explicit; Taskmaster signs off 100% coverage</done>
</task>

</tasks>

<verification>
- `grep -c "VERDICT:" audit/delta-v6/03-GAME-INTEGRATION-AUDIT.md` returns >= 10
- `grep "handleGameOverDrain\|Path A" audit/delta-v6/03-GAME-INTEGRATION-AUDIT.md` shows drift analysis
- `grep "yearSweep" audit/delta-v6/03-GAME-INTEGRATION-AUDIT.md` shows timing analysis
- `grep "Taskmaster" audit/delta-v6/03-GAME-INTEGRATION-AUDIT.md` shows coverage sign-off
</verification>

<success_criteria>
Every Phase 124 game integration function has a three-agent adversarial verdict. Fund split arithmetic proven correct. Path A drift verified safe. Access control changes verified. BAF-class checks explicit. Taskmaster signs off 100% coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/128-changed-contract-adversarial-audit/128-03-SUMMARY.md`
</output>
