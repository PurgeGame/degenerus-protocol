---
phase: 128-changed-contract-adversarial-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/delta-v6/01-STORAGE-GAS-FIXES-AUDIT.md
autonomous: true
requirements: [AUDIT-01, AUDIT-02, AUDIT-04, STOR-03]

must_haves:
  truths:
    - "Every Phase 121 changed function (11 entries across AdvanceModule, JackpotModule, LootboxModule, EndgameModule, GameStorage, BitPackingLib) has Mad Genius attack analysis with explicit VULNERABLE/INVESTIGATE/SAFE verdict"
    - "lastLootboxRngWord deletion verified: zero stale references remain in codebase (grep confirms)"
    - "advanceBounty rewrite verified: no precision or overflow change from upfront to inline computation"
    - "Double-SLOAD caching in payDailyJackpot and _runEarlyBirdLootboxJackpot verified correct"
    - "Deity boon downgrade prevention in _applyBoon verified: only-if-higher-tier semantics correct for all 8 boon types"
    - "BAF-class cache-overwrite check completed on every function that reads then writes storage"
    - "Skeptic has validated every non-SAFE Mad Genius finding"
    - "runRewardJackpots event emission fix verified: rebuyDelta hoisting emits correct post-reconciliation value"
  artifacts:
    - path: "audit/delta-v6/01-STORAGE-GAS-FIXES-AUDIT.md"
      provides: "Three-agent adversarial audit of all Phase 121 storage/gas fix changes"
      contains: "VERDICT:"
  key_links:
    - from: "audit/delta-v6/01-STORAGE-GAS-FIXES-AUDIT.md"
      to: "contracts/modules/DegenerusGameAdvanceModule.sol"
      via: "advanceGame/advanceBounty rewrite analysis, _finalizeLootboxRng, _backfillOrphanedLootboxIndices"
      pattern: "line \\d+"
    - from: "audit/delta-v6/01-STORAGE-GAS-FIXES-AUDIT.md"
      to: "contracts/modules/DegenerusGameJackpotModule.sol"
      via: "payDailyJackpot, _runEarlyBirdLootboxJackpot, _distributeYieldSurplus, processTicketBatch"
      pattern: "line \\d+"
    - from: "audit/delta-v6/01-STORAGE-GAS-FIXES-AUDIT.md"
      to: "contracts/modules/DegenerusGameLootboxModule.sol"
      via: "_boonCategory, _applyBoon deity downgrade prevention"
      pattern: "line \\d+"
---

<objective>
Three-agent adversarial audit of all Phase 121 (storage/gas fixes) changes: advanceBounty rewrite, lastLootboxRngWord deletion, double-SLOAD caching, event emission fix, deity boon downgrade prevention, and BitPackingLib natspec correction.

Purpose: Verify every Phase 121 fix is correct — no precision changes in advanceBounty, no stale references to deleted storage, caching preserves correctness, event emission now accurate, and boon upgrade semantics are sound. STOR-03 (lastLootboxRngWord zero stale refs) is specifically addressed here.

Output: audit/delta-v6/01-STORAGE-GAS-FIXES-AUDIT.md with per-function Mad Genius analysis, Skeptic validation, BAF-class checks, and coverage sign-off.
</objective>

<execution_context>
@.planning/ULTIMATE-AUDIT-DESIGN.md
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-changed-contract-adversarial-audit/128-CONTEXT.md
@.planning/phases/126-delta-extraction-plan-reconciliation/FUNCTION-CATALOG.md
@.planning/phases/126-delta-extraction-plan-reconciliation/DELTA-INVENTORY.md
@audit/KNOWN-ISSUES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mad Genius + Skeptic + Taskmaster audit of Phase 121 storage/gas fixes (11 functions)</name>
  <files>audit/delta-v6/01-STORAGE-GAS-FIXES-AUDIT.md</files>

  <read_first>
    - .planning/ULTIMATE-AUDIT-DESIGN.md (all three agent personas — you will play all three sequentially)
    - .planning/phases/128-changed-contract-adversarial-audit/128-CONTEXT.md (decisions D-01, D-02, D-08, D-09, D-10)
    - .planning/phases/126-delta-extraction-plan-reconciliation/FUNCTION-CATALOG.md (Sections 6, 7, 8, 10, 11, 12 — Phase 121 functions)
    - contracts/modules/DegenerusGameAdvanceModule.sol (read ENTIRE file — contains advanceGame, _finalizeLootboxRng, _finalizeRngRequest, _backfillOrphanedLootboxIndices)
    - contracts/modules/DegenerusGameJackpotModule.sol (read ENTIRE file — contains payDailyJackpot, _runEarlyBirdLootboxJackpot, _distributeYieldSurplus, processTicketBatch)
    - contracts/modules/DegenerusGameLootboxModule.sol (read ENTIRE file — contains _boonCategory, _applyBoon)
    - contracts/modules/DegenerusGameEndgameModule.sol (read ENTIRE file — contains runRewardJackpots)
    - contracts/storage/DegenerusGameStorage.sol (read ENTIRE file — verify lastLootboxRngWord is deleted, check for lootboxRngWordByIndex)
    - contracts/libraries/BitPackingLib.sol (read WHALE_BUNDLE_TYPE_SHIFT constant and its NatSpec)
  </read_first>

  <action>
Run the full three-agent adversarial audit cycle on the 11 Phase 121 function catalog entries. You will play Mad Genius first, then Skeptic, then Taskmaster.

**Phase 121 Function Catalog (from FUNCTION-CATALOG.md):**

| # | Function | Contract | Change |
|---|----------|----------|--------|
| 1 | `advanceGame()` | AdvanceModule | advanceBounty moved from upfront to inline payout-time (FIX-07), removed lastLootboxRngWord assignment (FIX-01), added charityResolve.resolveLevel (Phase 124 — audit the 121 parts here, cross-ref Plan 3 for 124 parts per D-02) |
| 2 | `_finalizeLootboxRng(...)` | AdvanceModule | Modified (Phase 121) |
| 3 | `_backfillOrphanedLootboxIndices(...)` | AdvanceModule | Modified (Phase 121) |
| 4 | `payDailyJackpot(...)` | JackpotModule | Cache _getFuturePrizePool() to avoid double SLOAD (FIX-02) |
| 5 | `_runEarlyBirdLootboxJackpot(...)` | JackpotModule | Cache _getFuturePrizePool() to avoid double SLOAD (FIX-02) |
| 6 | `_distributeYieldSurplus(uint256)` | JackpotModule | 46% accumulator split into 23% charity + 23% accumulator (Phase 121/124 — audit the arithmetic here, cross-ref Plan 3 for charity routing) |
| 7 | `processTicketBatch(...)` | JackpotModule | Replaced lastLootboxRngWord with lootboxRngWordByIndex[lootboxRngIndex - 1] (FIX-01) |
| 8 | `_boonCategory(...)` | LootboxModule | Modified (Phase 121) |
| 9 | `_applyBoon(...)` | LootboxModule | Removed `isDeity ||` override from 8 boon branches — uniform upgrade semantics |
| 10 | `runRewardJackpots(...)` | EndgameModule | rebuyDelta hoisted to emit correct post-reconciliation value (FIX-03) |
| 11 | `lastLootboxRngWord` (variable) | GameStorage | Deleted — verify zero stale references |
| 12 | `WHALE_BUNDLE_TYPE_SHIFT` (constant) | BitPackingLib | NatSpec-only correction "bits 152-154" -> "bits 152-153" — confirm no logic change |

**STEP 1 — MAD GENIUS (Attacker)**

For each function (#1-#10), apply the full Mad Genius methodology:

1. Read the function line-by-line in the actual contract source
2. Build the complete call tree for the CHANGED portion (not the entire unchanged call tree — focus on what the diff touches)
3. Map every storage write in the changed code path
4. Attack from every angle per ULTIMATE-AUDIT-DESIGN.md
5. Explicit BAF-class cached-local-vs-storage check (AUDIT-04): does any ancestor cache a value that the changed descendant now writes differently?
6. For advanceGame (#1): verify the inline bounty formula `(ADVANCE_BOUNTY_ETH * PRICE_COIN_UNIT * bountyMultiplier) / price` produces identical results to the old upfront computation — check for precision loss from reordering
7. For processTicketBatch (#7): verify `lootboxRngWordByIndex[lootboxRngIndex - 1]` is equivalent to the deleted `lastLootboxRngWord` in all cases — what if lootboxRngIndex is 0?
8. For _applyBoon (#9): verify all 8 boon branches (coinflip, lootbox, purchase, decimator, whale, activity, deity pass, lazy pass) use only-if-higher semantics correctly — no accidental downgrade
9. Verdict per function: VULNERABLE / INVESTIGATE / SAFE

**For #11 (lastLootboxRngWord deletion — STOR-03):**

Run `grep -rn "lastLootboxRngWord" contracts/` to verify zero remaining references. Document the grep output. If any reference remains, flag as VULNERABLE.

**For #12 (BitPackingLib natspec — per discretion):**

Confirm the change is NatSpec-only by diffing the constant declaration. If any logic differs, escalate. Otherwise mark SAFE (no analysis needed).

**STEP 2 — SKEPTIC (Validator)**

For every VULNERABLE or INVESTIGATE finding from Step 1:
1. Read the cited code yourself
2. Trace the execution path
3. Check preconditions
4. Classify: CONFIRMED / FALSE POSITIVE / DOWNGRADE TO INFO
5. Cite specific line numbers that prove your verdict

**STEP 3 — TASKMASTER (Coverage Enforcer)**

Build the coverage matrix:

| # | Function | Mad Genius Analyzed? | Call Tree Complete? | Storage Writes Listed? | BAF Cache Check? | Verdict |
|---|----------|---------------------|--------------------|-----------------------|-----------------|---------|

Verify:
- All 12 entries have analysis (including deletion and natspec)
- No function was skipped or given "similar to above" treatment
- Every cache-overwrite check is explicit
- Sign off: PASS or FAIL with specific gaps

Write the complete audit to `audit/delta-v6/01-STORAGE-GAS-FIXES-AUDIT.md` with sections:
1. Executive Summary (function count, verdicts)
2. Per-function Mad Genius Analysis (full methodology)
3. Skeptic Validation (if any non-SAFE findings)
4. STOR-03 Verification (lastLootboxRngWord grep results)
5. Taskmaster Coverage Matrix
6. Final Verdict
  </action>

  <verify>
    <automated>grep -c "VERDICT:" audit/delta-v6/01-STORAGE-GAS-FIXES-AUDIT.md</automated>
  </verify>

  <acceptance_criteria>
    - audit/delta-v6/01-STORAGE-GAS-FIXES-AUDIT.md exists and contains "VERDICT:" for at least 10 functions
    - File contains "## Taskmaster Coverage" or "Coverage Matrix" section
    - File contains "lastLootboxRngWord" grep verification with "zero" or "0 references" result
    - File contains "BAF" or "cache-overwrite" for each analyzed function
    - File contains "advanceBounty" precision analysis
    - File contains "_applyBoon" with all 8 boon types listed
    - File contains "STOR-03" reference
  </acceptance_criteria>

  <done>All 11 Phase 121 changed functions + 1 deleted variable + 1 natspec-only change are audited with three-agent methodology; STOR-03 verified; BAF-class checks explicit on every read-then-write function; Taskmaster signs off 100% coverage</done>
</task>

</tasks>

<verification>
- `grep -c "VERDICT:" audit/delta-v6/01-STORAGE-GAS-FIXES-AUDIT.md` returns >= 10
- `grep -c "SAFE\|VULNERABLE\|INVESTIGATE" audit/delta-v6/01-STORAGE-GAS-FIXES-AUDIT.md` returns >= 10
- `grep "lastLootboxRngWord" audit/delta-v6/01-STORAGE-GAS-FIXES-AUDIT.md` shows STOR-03 verification
- `grep "Taskmaster" audit/delta-v6/01-STORAGE-GAS-FIXES-AUDIT.md` shows coverage sign-off
</verification>

<success_criteria>
Every Phase 121 storage/gas fix function has a three-agent adversarial verdict. lastLootboxRngWord deletion has zero stale references confirmed. advanceBounty precision is proven equivalent. BAF-class checks are explicit. Taskmaster signs off 100% coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/128-changed-contract-adversarial-audit/128-01-SUMMARY.md`
</output>
