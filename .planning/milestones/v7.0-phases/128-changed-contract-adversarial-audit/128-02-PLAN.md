---
phase: 128-changed-contract-adversarial-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/delta-v6/02-DEGENERETTE-FREEZE-FIX-AUDIT.md
autonomous: true
requirements: [AUDIT-01, AUDIT-02, AUDIT-04]

must_haves:
  truths:
    - "All 18 DegeneretteModule functions are triaged into 'logic change' or 'formatting-only' per D-04"
    - "Every logic-change function has full Mad Genius attack analysis with VULNERABLE/INVESTIGATE/SAFE verdict per D-05"
    - "Every formatting-only function has fast-track verification confirming no logic change, marked SAFE per D-06"
    - "The frozen-context ETH routing through pending pool side-channel is proven correct and safe"
    - "BAF-class cache-overwrite check completed on every function that reads then writes storage"
    - "Skeptic has validated every non-SAFE Mad Genius finding"
    - "_addClaimableEth and _distributePayout ETH flow changes are verified — no ETH can be lost or double-counted during prizePoolFrozen"
  artifacts:
    - path: "audit/delta-v6/02-DEGENERETTE-FREEZE-FIX-AUDIT.md"
      provides: "Three-agent adversarial audit of all Phase 122 degenerette freeze fix changes"
      contains: "VERDICT:"
  key_links:
    - from: "audit/delta-v6/02-DEGENERETTE-FREEZE-FIX-AUDIT.md"
      to: "contracts/modules/DegenerusGameDegeneretteModule.sol"
      via: "Per-function triage and attack analysis"
      pattern: "line \\d+"
---

<objective>
Three-agent adversarial audit of all 18 Phase 122 (degenerette freeze fix) changes in DegenerusGameDegeneretteModule.sol, with triage classification before deep analysis.

Purpose: The freeze fix allows ETH resolution during prizePoolFrozen by routing through a pending pool side-channel. This is a significant ETH flow change — must verify no ETH is lost, double-counted, or exploitable during frozen state. Many of the 18 functions may be formatting-only changes that need fast-track rather than full analysis (per D-04/D-05/D-06).

Output: audit/delta-v6/02-DEGENERETTE-FREEZE-FIX-AUDIT.md with triage classification, per-function analysis, Skeptic validation, and coverage sign-off.
</objective>

<execution_context>
@.planning/ULTIMATE-AUDIT-DESIGN.md
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-changed-contract-adversarial-audit/128-CONTEXT.md
@.planning/phases/126-delta-extraction-plan-reconciliation/FUNCTION-CATALOG.md
@.planning/phases/122-degenerette-freeze-fix/122-CONTEXT.md
@audit/KNOWN-ISSUES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triage + Mad Genius + Skeptic + Taskmaster audit of Phase 122 degenerette freeze fix (18 functions)</name>
  <files>audit/delta-v6/02-DEGENERETTE-FREEZE-FIX-AUDIT.md</files>

  <read_first>
    - .planning/ULTIMATE-AUDIT-DESIGN.md (all three agent personas)
    - .planning/phases/128-changed-contract-adversarial-audit/128-CONTEXT.md (decisions D-04, D-05, D-06)
    - .planning/phases/126-delta-extraction-plan-reconciliation/FUNCTION-CATALOG.md (Section 2 — DegeneretteModule 18 functions)
    - .planning/phases/122-degenerette-freeze-fix/122-CONTEXT.md (design intent for the freeze fix)
    - contracts/modules/DegenerusGameDegeneretteModule.sol (read ENTIRE file — all 18 functions are here)
    - contracts/storage/DegenerusGameStorage.sol (read relevant storage variables: prizePoolFrozen, pendingPrizePool, claimable mappings)
  </read_first>

  <action>
Run the full three-agent adversarial audit cycle on the 18 Phase 122 DegeneretteModule functions. You play all three agents sequentially.

**Phase 122 Function Catalog (from FUNCTION-CATALOG.md Section 2):**

| # | Function | Change |
|---|----------|--------|
| 1 | `_resolvePlayer(address)` | modified |
| 2 | `placeFullTicketBets(...)` | modified |
| 3 | `resolveBets(address, uint64[])` | modified |
| 4 | `_placeFullTicketBets(...)` | modified |
| 5 | `_placeFullTicketBetsCore(...)` | modified |
| 6 | `_collectBetFunds(...)` | modified |
| 7 | `_resolveBet(address, uint64)` | modified |
| 8 | `_resolveFullTicketBet(address, uint64, uint256)` | modified |
| 9 | `_distributePayout(address, uint8, uint256, uint256)` | modified |
| 10 | `_maybeAwardConsolation(address, uint8, uint128)` | modified |
| 11 | `_packFullTicketBet(...)` | modified |
| 12 | `_countMatches(...)` | modified |
| 13 | `_fullTicketPayout(...)` | modified |
| 14 | `_applyHeroMultiplier(...)` | modified |
| 15 | `_roiBpsFromScore(...)` | modified |
| 16 | `_wwxrpHighValueRoi(uint256)` | modified |
| 17 | `_addClaimableEth(address, uint256)` | modified |
| 18 | `_awardDegeneretteDgnrs(address, uint256, uint8)` | modified |

**STEP 0 — TRIAGE (per D-04)**

Before any deep analysis, classify each of the 18 functions by examining the actual `git diff` between the v5.0 version and current version:

Run: `git diff v5.0..HEAD -- contracts/modules/DegenerusGameDegeneretteModule.sol`

For each function, classify:
- **LOGIC CHANGE**: The diff modifies control flow, arithmetic, storage reads/writes, external calls, or conditional branches
- **FORMATTING-ONLY**: The diff only changes whitespace, line wrapping, comment text, or variable naming with no semantic change

Create a triage table at the top of the audit document:

| # | Function | Classification | Reason |
|---|----------|---------------|--------|

**STEP 1 — MAD GENIUS (Attacker)**

**For LOGIC CHANGE functions (per D-05):** Apply the full Mad Genius methodology:
1. Read the function line-by-line in the contract
2. Build complete call tree for the changed portion
3. Map every storage write in the changed code path
4. Attack from every angle per ULTIMATE-AUDIT-DESIGN.md
5. Explicit BAF-class cached-local-vs-storage check (AUDIT-04)
6. Special focus areas:
   - `_addClaimableEth`: How does ETH routing change when prizePoolFrozen is true? Does the pending pool side-channel correctly accumulate and later drain?
   - `_distributePayout`: Does the frozen-context payout path produce identical final state as the non-frozen path after unfreeze?
   - `_resolvePlayer`: Does player resolution handle the frozen/unfrozen transition correctly?
   - `resolveBets` / `_resolveBet` / `_resolveFullTicketBet`: Can a bet be resolved in a way that exploits the frozen state?
7. Verdict per function: VULNERABLE / INVESTIGATE / SAFE

**For FORMATTING-ONLY functions (per D-06):** Fast-track verification:
1. Confirm no logic change by citing the specific diff lines
2. Mark SAFE
3. Taskmaster counts as covered

**STEP 2 — SKEPTIC (Validator)**

For every VULNERABLE or INVESTIGATE finding from Step 1:
1. Read the cited code yourself
2. Trace the execution path with freeze state considerations
3. Check preconditions — can the attacker actually trigger the frozen path?
4. Classify: CONFIRMED / FALSE POSITIVE / DOWNGRADE TO INFO

**STEP 3 — TASKMASTER (Coverage Enforcer)**

Build the coverage matrix:

| # | Function | Triage | Mad Genius Analyzed? | Call Tree? | Storage Writes? | BAF Check? | Verdict |
|---|----------|--------|---------------------|-----------|----------------|-----------|---------|

Verify all 18 functions covered (logic-change = full analysis, formatting-only = fast-track with explicit diff citation). Sign off: PASS or FAIL.

Write to `audit/delta-v6/02-DEGENERETTE-FREEZE-FIX-AUDIT.md` with sections:
1. Executive Summary
2. Triage Classification Table
3. Per-function Analysis (full for logic-change, fast-track for formatting-only)
4. Skeptic Validation (if any non-SAFE findings)
5. Taskmaster Coverage Matrix
6. Final Verdict
  </action>

  <verify>
    <automated>grep -c "VERDICT:" audit/delta-v6/02-DEGENERETTE-FREEZE-FIX-AUDIT.md</automated>
  </verify>

  <acceptance_criteria>
    - audit/delta-v6/02-DEGENERETTE-FREEZE-FIX-AUDIT.md exists and contains "VERDICT:" for at least 18 functions
    - File contains "Triage" or "Classification" table with all 18 functions
    - File contains "LOGIC CHANGE" and/or "FORMATTING-ONLY" classifications
    - File contains "_addClaimableEth" analysis with frozen/unfrozen ETH routing
    - File contains "_distributePayout" analysis
    - File contains "BAF" or "cache-overwrite" for each logic-change function
    - File contains "## Taskmaster" or "Coverage Matrix" with 18 entries
    - File contains "PASS" or "FAIL" coverage verdict
  </acceptance_criteria>

  <done>All 18 DegeneretteModule functions triaged, logic-change functions get full three-agent audit, formatting-only get fast-track verification; BAF-class checks explicit on every read-then-write logic-change function; Taskmaster signs off 100% coverage of all 18 entries</done>
</task>

</tasks>

<verification>
- `grep -c "VERDICT:" audit/delta-v6/02-DEGENERETTE-FREEZE-FIX-AUDIT.md` returns >= 18
- `grep "Triage\|Classification" audit/delta-v6/02-DEGENERETTE-FREEZE-FIX-AUDIT.md` confirms triage section exists
- `grep "Taskmaster" audit/delta-v6/02-DEGENERETTE-FREEZE-FIX-AUDIT.md` shows coverage sign-off
</verification>

<success_criteria>
All 18 Phase 122 degenerette freeze fix functions are triaged and audited per D-04/D-05/D-06. ETH routing through pending pool side-channel is proven correct. BAF-class checks are explicit. Taskmaster signs off 100% coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/128-changed-contract-adversarial-audit/128-02-SUMMARY.md`
</output>
