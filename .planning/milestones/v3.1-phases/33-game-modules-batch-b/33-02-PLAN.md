---
phase: 33-game-modules-batch-b
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v3.1-findings-33-game-modules-batch-b.md
autonomous: true
requirements: [CMT-03, DRIFT-03]

must_haves:
  truths:
    - "Every NatSpec tag in DegenerusGameDecimatorModule.sol is verified against current code behavior"
    - "Every inline comment in DegenerusGameDecimatorModule.sol is verified against current logic"
    - "Post-Phase-29 change (30e193ff: burn deadline shift from day 0 to day 1, curve shift) is specifically reviewed for stale comments"
    - "recordTerminalDecBurn NatSpec verified: 'Burns blocked when <= 1 day remains (24h cooldown before termination)'"
    - "_terminalDecMultiplierBps NatSpec verified: 'linear 2x (day 10) to 1x (day 2), burns blocked at day 1'"
    - "BPS scale annotations verified throughout DecimatorModule (highest NatSpec density: 161 tags in 1,027 lines)"
    - "No references to old 'daysRemaining == 0' burn rule remain unflagged"
    - "Any vestigial guards, unnecessary restrictions, or intent drift in DecimatorModule is flagged"
  artifacts:
    - path: "audit/v3.1-findings-33-game-modules-batch-b.md"
      provides: "DecimatorModule section appended to Phase 33 findings file"
      contains: "## DegenerusGameDecimatorModule.sol"
  key_links:
    - from: "audit/v3.1-findings-33-game-modules-batch-b.md"
      to: "contracts/modules/DegenerusGameDecimatorModule.sol"
      via: "File:Line citations for every finding"
      pattern: "DegenerusGameDecimatorModule\\.sol:\\d+"
---

<objective>
Comment audit and intent drift review for DegenerusGameDecimatorModule.sol (1,027 lines, 161 NatSpec tags, ~287 comment lines, 30 functions).

Purpose: DecimatorModule has the highest NatSpec density in Batch B (161 tags in 1,027 lines -- more tags than JackpotModule despite being 1/3 the size). It received 1 post-Phase-29 commit (30e193ff) that changed the burn block condition from `daysRemaining == 0` to `daysRemaining <= 1` (24h cooldown before termination) and shifted the time multiplier curve in `_terminalDecMultiplierBps` from `10000 + ((daysRemaining - 1) * 10000) / 9` to `10000 + ((daysRemaining - 2) * 10000) / 8`. The NatSpec was updated by this commit but needs independent verification. The terminal decimator mechanics (burn tracking, bucket/subbucket, time multiplier curve) are complex and BPS-heavy.

Output: `## DegenerusGameDecimatorModule.sol` section appended to `audit/v3.1-findings-33-game-modules-batch-b.md`.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-game-modules-batch-b/33-RESEARCH.md
@audit/v3.1-findings-32-game-modules-batch-a.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comment audit and intent drift review for DegenerusGameDecimatorModule.sol</name>
  <files>audit/v3.1-findings-33-game-modules-batch-b.md</files>
  <read_first>
    contracts/modules/DegenerusGameDecimatorModule.sol
    audit/v3.1-findings-33-game-modules-batch-b.md
    .planning/phases/33-game-modules-batch-b/33-RESEARCH.md
  </read_first>
  <action>
Perform the full DegenerusGameDecimatorModule.sol comment audit and intent drift review (1,027 lines, 161 NatSpec tags, ~287 comment lines, 30 functions). This contract has the highest NatSpec density in Batch B.

**Step 1: Verify post-Phase-29 commit 30e193ff (burn deadline shift + curve change)**

Run `git diff 30e193ff~1..30e193ff -- contracts/modules/DegenerusGameDecimatorModule.sol` to see exact changes. Verify:
- `recordTerminalDecBurn` burn block condition changed from `daysRemaining == 0` to `daysRemaining <= 1`
- NatSpec on `recordTerminalDecBurn` (line ~800) updated to "Burns blocked when <= 1 day remains (24h cooldown before termination)"
- `_terminalDecMultiplierBps` formula changed from `10000 + ((daysRemaining - 1) * 10000) / 9` to `10000 + ((daysRemaining - 2) * 10000) / 8`
- NatSpec on `_terminalDecMultiplierBps` (lines ~997-998) updated to "linear 2x (day 10) to 1x (day 2), burns blocked at day 1"
- Inline comment (line ~1004) updated to "Linear: 2x at day 10, 1x at day 2 (day 1 blocked by caller)"
- Verify the math: Day 10: 10000+(8*10000)/8 = 20000 (2x), Day 2: 10000+(0*10000)/8 = 10000 (1x), Day 1: blocked
- Search for any remaining references to old `daysRemaining == 0` rule or old formula `(daysRemaining - 1) * 10000 / 9`

**Step 2: Review all 161 NatSpec tags**

For each @notice, @dev, @param, @return, @custom tag:
1. Read the tag text
2. Cross-reference against actual function behavior in current HEAD
3. Pay special attention to:
   - BPS scale annotations (DecimatorModule uses BPS extensively for burn multipliers, pool percentages)
   - Terminal decimator mechanics: burn tracking (bucket/subbucket system), time multiplier curve, decimation threshold
   - Burn window descriptions: active vs terminal decimator differences
   - Cross-module references: DecimatorModule inherits PayoutUtils and is called via delegatecall from AdvanceModule and from DegeneretteModule (for lootbox resolution)
   - Constants with @dev descriptions: verify every percentage annotation matches the constant's actual value
4. Focus on warden-readability

**Step 3: Review all inline comments (~287 comment lines)**

Read every `//` and `/* */` comment. Check for:
- References to old burn deadline (daysRemaining == 0 vs daysRemaining <= 1)
- Stale references to old multiplier curve formula
- BPS confusion (DecimatorModule uses BPS for multipliers)
- Cross-module references (calls to PayoutUtils._creditClaimable, _calcAutoRebuy)
- Bucket/subbucket system descriptions

**Step 4: Intent drift review for DecimatorModule**

Scan for:
- Vestigial guards from the old `daysRemaining == 0` rule
- Any inconsistent references to the old "remaining=0" rule
- Logic whose behavior changed but comments still describe old behavior
- Orphaned NatSpec from removed functions (pattern from CMT-010/011/020)
- Cross-module consistency: do descriptions of how AdvanceModule calls DecimatorModule match current behavior?

**Step 5: Write DecimatorModule findings section**

Append a `## DegenerusGameDecimatorModule.sol` section to the findings file. Continue CMT/DRIFT numbering from where JackpotModule section left off (or from CMT-025/DRIFT-003 if this plan executes before Plan 01 finishes).

NOTE: This plan runs in parallel with Plan 01. If the findings file does not yet exist when this task runs, create it with the header template first (same template as Plan 01 Task 1). If it already exists, append to it.

Use this format for each finding:

```markdown
### CMT-0NN: [Brief title]
- **What:** [The specific comment inaccuracy]
- **Where:** DegenerusGameDecimatorModule.sol:LINE
- **Why:** [Why a warden would be misled]
- **Suggestion:** [Recommended fix]
- **Category:** comment-inaccuracy | intent-drift
- **Severity:** INFO | LOW
```

Update the Summary table DecimatorModule row with actual counts.

Add a review summary block:

```markdown
---

**DegenerusGameDecimatorModule.sol review complete.** 161 NatSpec tags verified, ~287 comment lines reviewed, all inline comments cross-referenced against current code behavior. [N] findings total. Post-Phase-29 commit 30e193ff (burn deadline shift + curve change) independently verified.
```
  </action>
  <verify>
    <automated>grep '## DegenerusGameDecimatorModule.sol' audit/v3.1-findings-33-game-modules-batch-b.md; grep 'DecimatorModule.sol review complete' audit/v3.1-findings-33-game-modules-batch-b.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.1-findings-33-game-modules-batch-b.md contains `## DegenerusGameDecimatorModule.sol`
    - File contains "DecimatorModule.sol review complete"
    - Summary table row for DecimatorModule has actual integer counts (not X/Y/Z placeholders)
    - Every finding has What, Where, Why, Suggestion, Category, and Severity fields
    - CMT numbering continues sequentially from Phase 32 (CMT-025 or higher)
    - grep for `daysRemaining == 0` in DecimatorModule returns empty (confirming old rule is gone) or any remaining references are flagged
    - No .sol files modified (flag-only)
  </acceptance_criteria>
  <done>DegenerusGameDecimatorModule.sol (1,027 lines, 161 NatSpec tags, ~287 comment lines, 30 functions) fully reviewed for comment accuracy and intent drift. Post-Phase-29 commit 30e193ff independently verified. Terminal decimator multiplier curve and burn deadline NatSpec verified. All BPS scale annotations verified. DecimatorModule findings section appended to batch deliverable.</done>
</task>

</tasks>

<verification>
- audit/v3.1-findings-33-game-modules-batch-b.md contains DecimatorModule section
- grep 'DecimatorModule.sol review complete' returns results
- Every finding has what/where/why/suggestion/category/severity format
- Post-Phase-29 NatSpec updates independently verified against code
- No .sol files in contracts/ directory were modified
</verification>

<success_criteria>
- DecimatorModule fully reviewed for comment accuracy and intent drift
- Post-Phase-29 code change (30e193ff) specifically reviewed for stale NatSpec
- Terminal multiplier curve math independently verified (2x at day 10, 1x at day 2, blocked at day 1)
- All 161 NatSpec tags verified
- Every finding has what/why/suggestion per item
- No code changes made to any contract files
</success_criteria>

<output>
After completion, create `.planning/phases/33-game-modules-batch-b/33-02-SUMMARY.md`
</output>
