---
phase: 33-game-modules-batch-b
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - audit/v3.1-findings-33-game-modules-batch-b.md
autonomous: true
requirements: [CMT-03, DRIFT-03]

must_haves:
  truths:
    - "Every NatSpec tag in DegenerusGameEndgameModule.sol is verified against current code behavior"
    - "Every inline comment in DegenerusGameEndgameModule.sol is verified against current logic"
    - "Every NatSpec tag in DegenerusGameGameOverModule.sol is verified against current code behavior"
    - "Every inline comment in DegenerusGameGameOverModule.sol is verified against current logic"
    - "GameOverModule _sendToVault hard-revert risk NatSpec absence is checked (Phase 29 GO-05-F01)"
    - "GameOverModule post-Phase-29 level-0 guard simplification (df1e9f78) NatSpec verified"
    - "Every NatSpec tag in DegenerusGameAdvanceModule.sol is verified against current code behavior"
    - "Every inline comment in DegenerusGameAdvanceModule.sol is verified against current logic"
    - "AdvanceModule delegatecall wrapper NatSpec matches CURRENT behavior of target modules (not Phase 29 behavior)"
    - "AdvanceModule does NOT reference old future dump (1-in-1e15), old keep-roll range (0-100%), or old burn deadline (daysRemaining == 0)"
    - "Any vestigial guards, unnecessary restrictions, or intent drift in these 3 contracts is flagged"
    - "The consolidated findings file has complete Summary counts for all 5 contracts"
    - "Total finding count in Summary table matches actual finding heading count in file"
  artifacts:
    - path: "audit/v3.1-findings-33-game-modules-batch-b.md"
      provides: "Complete per-batch findings file with all 5 contract sections and final summary counts"
      contains: "## DegenerusGameAdvanceModule.sol"
  key_links:
    - from: "audit/v3.1-findings-33-game-modules-batch-b.md"
      to: "contracts/modules/DegenerusGameEndgameModule.sol"
      via: "File:Line citations for every finding"
      pattern: "DegenerusGameEndgameModule\\.sol:\\d+"
    - from: "audit/v3.1-findings-33-game-modules-batch-b.md"
      to: "contracts/modules/DegenerusGameGameOverModule.sol"
      via: "File:Line citations for every finding"
      pattern: "DegenerusGameGameOverModule\\.sol:\\d+"
    - from: "audit/v3.1-findings-33-game-modules-batch-b.md"
      to: "contracts/modules/DegenerusGameAdvanceModule.sol"
      via: "File:Line citations for every finding"
      pattern: "DegenerusGameAdvanceModule\\.sol:\\d+"
---

<objective>
Comment audit and intent drift review for EndgameModule (540 lines), GameOverModule (232 lines), and AdvanceModule (1,383 lines). Finalize the Phase 33 findings file.

Purpose: These three remaining contracts complete the Batch B audit. EndgameModule and AdvanceModule have NOT been modified since Phase 29, but AdvanceModule is the orchestrator that delegatecalls into JackpotModule, EndgameModule, and GameOverModule -- its NatSpec descriptions of those modules must match their CURRENT behavior (including post-Phase-29 changes). GameOverModule had 1 post-Phase-29 commit (df1e9f78: simplified level-0 guard) and Phase 29 flagged GO-05-F01 (_sendToVault hard-revert risk absent from NatSpec). This plan also finalizes the consolidated findings file with complete Summary table counts.

Output: `## DegenerusGameEndgameModule.sol`, `## DegenerusGameGameOverModule.sol`, and `## DegenerusGameAdvanceModule.sol` sections appended to findings file, plus finalized Summary table.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-game-modules-batch-b/33-RESEARCH.md
@.planning/phases/33-game-modules-batch-b/33-01-SUMMARY.md
@.planning/phases/33-game-modules-batch-b/33-02-SUMMARY.md
@audit/v3.1-findings-32-game-modules-batch-a.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comment audit and intent drift review for EndgameModule, GameOverModule, and AdvanceModule</name>
  <files>audit/v3.1-findings-33-game-modules-batch-b.md</files>
  <read_first>
    contracts/modules/DegenerusGameEndgameModule.sol
    contracts/modules/DegenerusGameGameOverModule.sol
    contracts/modules/DegenerusGameAdvanceModule.sol
    audit/v3.1-findings-33-game-modules-batch-b.md
    .planning/phases/33-game-modules-batch-b/33-RESEARCH.md
  </read_first>
  <action>
Perform comment audit and intent drift review for the 3 remaining contracts (2,155 lines combined, 115 NatSpec tags, ~425 comment lines, 51 functions).

**PART A: EndgameModule (540 lines, 23 NatSpec tags, ~134 comment lines, 7 functions)**

No post-Phase-29 changes. Handles BAF (Burn And Freeze) dispatch and affiliate rewards.

Step A1: Review all 23 NatSpec tags
- Verify @notice/@dev/@param/@return for each of the 7 functions
- BAF/Decimator schedule descriptions -- verify trigger logic matches descriptions
- Affiliate reward calculation NatSpec -- verify percentages and BPS
- Cross-module references (EndgameModule inherits PayoutUtils, calls _creditClaimable)

Step A2: Review all inline comments (~134 comment lines)
- BAF activation conditions
- Decimator window management
- Affiliate reward distribution logic
- Pool interaction descriptions

Step A3: Intent drift review for EndgameModule
- Vestigial guards from removed features
- Stale cross-module references
- Orphaned NatSpec

Step A4: Write EndgameModule section
Append `## DegenerusGameEndgameModule.sol` section with findings (or 0-findings summary if clean).

**PART B: GameOverModule (232 lines, 21 NatSpec tags, ~57 comment lines, 7 functions)**

1 post-Phase-29 change (df1e9f78: simplified level-0 guard). Phase 29 flagged GO-05-F01.

Step B1: Verify post-Phase-29 commit df1e9f78 (level-0 guard simplification)
Run `git diff df1e9f78~1..df1e9f78 -- contracts/modules/DegenerusGameGameOverModule.sol` to see exact changes:
- Old: `uint24 currentLevel = level; uint24 lvl = currentLevel == 0 ? 1 : currentLevel;` and `if (currentLevel < 10)`
- New: `uint24 lvl = level;` and `if (lvl < 10)`
- Verify no NatSpec described the old level-0 guard that would now be stale
- Verify the function's overall NatSpec is accurate with the simplified logic

Step B2: Check GO-05-F01 _sendToVault NatSpec status
Phase 29 Plan 03 confirmed: `_sendToVault` hard-revert risk is absent from GameOverModule NatSpec. The `@custom:reverts E` tag exists (lines 67, 169, 193) but does not specifically warn about the permanent game-blocking risk if stETH transfer reverts during the gameover drain.
- If the NatSpec STILL lacks this warning, flag as a finding (DRIFT category -- design intent to warn wardens about this risk is not reflected in NatSpec)
- If the NatSpec was updated since Phase 29, verify the update is accurate

Step B3: Review all 21 NatSpec tags
- Verify @notice/@dev/@param/@return/@custom for each of the 7 functions
- handleGameOverDrain function NatSpec -- verify game-over flow description
- _sendToVault NatSpec -- verify revert conditions
- Pool draining descriptions
- Cross-module references (GameOverModule inherits Storage directly, NOT PayoutUtils)

Step B4: Review all inline comments (~57 comment lines)
- Game-over trigger conditions
- Pool draining order
- ETH distribution during game-over

Step B5: Intent drift review for GameOverModule
- Stale level-0 guard references
- GO-05-F01 revert risk documentation
- Vestigial guards

Step B6: Write GameOverModule section
Append `## DegenerusGameGameOverModule.sol` section with findings.

**PART C: AdvanceModule (1,383 lines, 71 NatSpec tags, ~234 comment lines, 37 functions)**

No post-Phase-29 changes, BUT this is the orchestrator that delegatecalls into modules that DID change.

Step C1: Review delegatecall section header (lines ~480-492)
This block comment lists module purposes and is prime warden reading material. Verify each module description matches CURRENT behavior:
- JackpotModule description: must NOT reference old "0-100% keep" or "1-in-1e15 dump"
- DecimatorModule description: must NOT reference old "daysRemaining == 0" burn rule
- GameOverModule description: should reflect simplified level-0 handling
- EndgameModule description: verify accuracy

Step C2: Review delegatecall wrapper function NatSpec
For each private delegatecall wrapper (e.g., `_consolidatePrizePools` lines ~531-544, `payDailyJackpot` lines ~564-585):
- Verify NatSpec description matches the CURRENT behavior of the target module function
- This is the key area where stale cross-module NatSpec would appear
- Focus on external/public function NatSpec and the delegatecall section header block comment
- Flag missing NatSpec on wrappers only if the wrapper adds behavior beyond pure dispatch

Step C3: Review advanceGame function NatSpec
The main orchestration function -- verify the flow description matches current behavior:
- RNG gating description
- VRF interaction description
- Level advancement logic
- Module dispatch order

Step C4: Review remaining NatSpec tags and inline comments
Cover all 71 NatSpec tags and ~234 comment lines:
- RNG/VRF function NatSpec
- View functions
- Helper functions
- All inline comments

Step C5: Run stale cross-module reference checks
```bash
grep -in '0-100%\|avg 50%\|future.*dump\|1e15\|quadrillion\|daysRemaining == 0\|remaining.*0.*block' contracts/modules/DegenerusGameAdvanceModule.sol
```
Any matches indicate stale references to pre-Phase-29 behavior and must be flagged.

Step C6: Intent drift review for AdvanceModule
- Stale cross-module NatSpec (most likely issue)
- Vestigial delegatecall paths
- Orphaned NatSpec from removed functions
- Guards that reference old module behavior

Step C7: Write AdvanceModule section
Append `## DegenerusGameAdvanceModule.sol` section with findings.
  </action>
  <verify>
    <automated>grep -c '## DegenerusGame.*Module.sol\|## DegenerusGameGameOverModule.sol' audit/v3.1-findings-33-game-modules-batch-b.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.1-findings-33-game-modules-batch-b.md contains `## DegenerusGameEndgameModule.sol`
    - audit/v3.1-findings-33-game-modules-batch-b.md contains `## DegenerusGameGameOverModule.sol`
    - audit/v3.1-findings-33-game-modules-batch-b.md contains `## DegenerusGameAdvanceModule.sol`
    - GO-05-F01 _sendToVault revert risk has been explicitly checked and either flagged or confirmed addressed
    - GameOverModule post-Phase-29 level-0 simplification has been verified
    - AdvanceModule delegatecall wrapper NatSpec has been verified against current module behavior
    - grep for stale cross-module references in AdvanceModule returns empty or matches are flagged
    - Every finding has What, Where, Why, Suggestion, Category, and Severity fields
    - No .sol files modified (flag-only)
  </acceptance_criteria>
  <done>EndgameModule (540 lines), GameOverModule (232 lines), and AdvanceModule (1,383 lines) fully reviewed for comment accuracy and intent drift. GameOverModule post-Phase-29 change verified. GO-05-F01 status checked. AdvanceModule cross-module NatSpec verified against current module behavior. All 3 contract sections appended to findings file.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize Phase 33 findings file</name>
  <files>audit/v3.1-findings-33-game-modules-batch-b.md</files>
  <read_first>
    audit/v3.1-findings-33-game-modules-batch-b.md
  </read_first>
  <action>
Finalize the consolidated Phase 33 findings file by updating the Summary table and performing a completeness check.

**Step 1: Update Summary table with final counts**

Read the entire findings file. Count the actual number of `### CMT-` and `### DRIFT-` headings for each contract section. Update the Summary table at the top:

```markdown
| Contract | CMT findings | DRIFT findings | Total |
|----------|-------------|----------------|-------|
| DegenerusGameJackpotModule.sol | [actual] | [actual] | [actual] |
| DegenerusGameDecimatorModule.sol | [actual] | [actual] | [actual] |
| DegenerusGameEndgameModule.sol | [actual] | [actual] | [actual] |
| DegenerusGameGameOverModule.sol | [actual] | [actual] | [actual] |
| DegenerusGameAdvanceModule.sol | [actual] | [actual] | [actual] |
| **Total** | **[sum]** | **[sum]** | **[sum]** |
```

Replace all X/Y/Z placeholders with actual integer counts.

**Step 2: Verify all 5 contracts are covered**

Confirm the file contains sections for ALL 5 contracts:
1. `## DegenerusGameJackpotModule.sol`
2. `## DegenerusGameDecimatorModule.sol`
3. `## DegenerusGameEndgameModule.sol`
4. `## DegenerusGameGameOverModule.sol`
5. `## DegenerusGameAdvanceModule.sol`

If any section is missing, flag it.

**Step 3: Cross-check finding numbering**

Verify CMT numbering is sequential starting from CMT-025 (continuing Phase 32 which ended at CMT-024). Verify DRIFT numbering is sequential starting from DRIFT-003 (continuing Phase 32 which ended at DRIFT-002). No gaps or duplicates in the numbering sequence.

**Step 4: Verify GO-05-F01 is addressed**

Confirm that the GameOverModule section explicitly addresses the Phase 29 GO-05-F01 finding about _sendToVault hard-revert risk absent from NatSpec. This must be either flagged as a finding or confirmed as addressed.

**Step 5: Remove placeholder text**

Delete the `*Summary counts updated after all contracts reviewed.*` placeholder line if it still exists.

**Step 6: Verify Summary table matches actual content**

Count `### CMT-` headings in file and verify total matches the CMT total in Summary table. Count `### DRIFT-` headings and verify total matches DRIFT total. Verify grand total equals CMT + DRIFT counts.
  </action>
  <verify>
    <automated>grep -c '### CMT-\|### DRIFT-' audit/v3.1-findings-33-game-modules-batch-b.md</automated>
  </verify>
  <acceptance_criteria>
    - Summary table at top has actual integer counts (no X, Y, or Z placeholders) for all 5 contracts and totals
    - grep for all 5 `## DegenerusGame*.sol` section headers returns 5 matches
    - Total finding count in Summary table matches actual count of `### CMT-` and `### DRIFT-` headings
    - CMT numbering starts at CMT-025 (no collision with Phase 32 CMT-011 through CMT-024)
    - DRIFT numbering starts at DRIFT-003 (no collision with Phase 32 DRIFT-001 through DRIFT-002)
    - No duplicate finding numbers
    - GO-05-F01 explicitly addressed (flagged or confirmed resolved)
    - Every finding has What, Where, Why, Suggestion, Category, and Severity fields
    - No .sol files modified (flag-only)
    - No `*Summary counts updated*` placeholder text remains
  </acceptance_criteria>
  <done>Phase 33 findings file is complete. All 5 contracts have sections. Summary table has accurate counts. Finding numbering is sequential and collision-free with Phase 32. GO-05-F01 explicitly addressed. File is ready for Phase 36 consolidation.</done>
</task>

</tasks>

<verification>
- audit/v3.1-findings-33-game-modules-batch-b.md exists with all 5 contract sections
- Summary table has real counts (not placeholders) totaling correctly
- CMT numbering starts at CMT-025, DRIFT starts at DRIFT-003
- GO-05-F01 _sendToVault revert risk explicitly addressed
- AdvanceModule cross-module NatSpec verified against current module behavior
- Every finding has what/where/why/suggestion/category/severity format
- No .sol files in contracts/ directory were modified
- Finding count in Summary table equals actual `### CMT-|### DRIFT-` heading count
</verification>

<success_criteria>
- EndgameModule, GameOverModule, and AdvanceModule fully reviewed for comment accuracy and intent drift
- GameOverModule post-Phase-29 change (df1e9f78) verified
- GO-05-F01 _sendToVault NatSpec explicitly checked and addressed
- AdvanceModule cross-module NatSpec verified against CURRENT module behavior (not Phase 29)
- Consolidated findings file complete with all 5 contracts and accurate summary counts
- Finding numbering sequential and collision-free with Phase 32
- No code changes made to any contract files
- Phase 33 per-batch deliverable ready for Phase 36 consolidation
</success_criteria>

<output>
After completion, create `.planning/phases/33-game-modules-batch-b/33-03-SUMMARY.md`
</output>
