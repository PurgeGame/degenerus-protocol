---
phase: 33-game-modules-batch-b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v3.1-findings-33-game-modules-batch-b.md
autonomous: true
requirements: [CMT-03, DRIFT-03]

must_haves:
  truths:
    - "Every NatSpec tag in DegenerusGameJackpotModule.sol is verified against current code behavior"
    - "Every inline comment in DegenerusGameJackpotModule.sol is verified against current logic"
    - "Post-Phase-29 changes (a2093fd6 keep-roll tightening, 4cefca59 future dump removal) are specifically reviewed for stale comments"
    - "No references to removed future dump feature (1-in-1e15, FUTURE_DUMP_TAG, FUTURE_DUMP_ODDS, _shouldFutureDump) remain unflagged"
    - "Keep-roll range NatSpec (30-65%, avg ~47.5%) verified against _futureKeepBps formula"
    - "BPS vs raw percentage scale annotations verified throughout JackpotModule"
    - "Any vestigial guards, unnecessary restrictions, or intent drift in JackpotModule is flagged"
    - "A per-batch findings file exists with what/why/suggestion for every finding"
  artifacts:
    - path: "audit/v3.1-findings-33-game-modules-batch-b.md"
      provides: "Per-batch findings file for Phase 33 with JackpotModule section"
      contains: "## DegenerusGameJackpotModule.sol"
  key_links:
    - from: "audit/v3.1-findings-33-game-modules-batch-b.md"
      to: "contracts/modules/DegenerusGameJackpotModule.sol"
      via: "File:Line citations for every finding"
      pattern: "DegenerusGameJackpotModule\\.sol:\\d+"
---

<objective>
Create the Phase 33 findings file and perform comment audit + intent drift review for DegenerusGameJackpotModule.sol (2,795 lines, 147 NatSpec tags, ~453 comment lines, 56 functions).

Purpose: JackpotModule is the largest module in Batch B with the most complex logic (prize pool splits, daily jackpots, ticket processing, auto-rebuy). It received 2 post-Phase-29 commits: a2093fd6 (tightened x00 future pool keep-roll range from 0-100% to 30-65%) and 4cefca59 (removed the rare 1-in-1e15 future pool dump on non-x00 levels, deleting FUTURE_DUMP_TAG, FUTURE_DUMP_ODDS, and _shouldFutureDump). These NatSpec updates need independent verification, and any remaining references to the removed feature must be found. Phase 31 already flagged stale jackpot section headers in DegenerusGame.sol (CMT-006/007/008) -- this plan verifies the module-side NatSpec is clean.

Output: `audit/v3.1-findings-33-game-modules-batch-b.md` with file header, summary table template, and JackpotModule findings section. CMT numbering starts at CMT-025, DRIFT numbering starts at DRIFT-003 (continuing from Phase 32).
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-game-modules-batch-b/33-RESEARCH.md
@audit/v3.1-findings-32-game-modules-batch-a.md
@audit/v3.1-findings-31-core-game-contracts.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comment audit and intent drift review for DegenerusGameJackpotModule.sol (header, constants, prize pool, daily jackpot)</name>
  <files>audit/v3.1-findings-33-game-modules-batch-b.md</files>
  <read_first>
    contracts/modules/DegenerusGameJackpotModule.sol
    .planning/phases/33-game-modules-batch-b/33-RESEARCH.md
    audit/v3.1-findings-32-game-modules-batch-a.md
    audit/v3.1-findings-31-core-game-contracts.md
  </read_first>
  <action>
Create the Phase 33 findings file and perform the first half of the DegenerusGameJackpotModule.sol review, covering the contract header (architecture block comments lines 16-43), constants/events, consolidatePrizePools, _futureKeepBps, and daily jackpot functions (payDailyJackpot, payDailyJackpotCoinAndTickets, _payDailyCoinJackpot, _payDailyTicketJackpot).

**Step 1: Create findings file with header**

Create `audit/v3.1-findings-33-game-modules-batch-b.md` with this exact template:

```markdown
# Phase 33 Findings: Game Modules Batch B

**Date:** 2026-03-18
**Scope:** JackpotModule, DecimatorModule, EndgameModule, GameOverModule, AdvanceModule
**Pass:** v3.1 second independent review (v3.0 Phase 29 was first pass)
**Mode:** Flag-only -- no code changes

## Summary

| Contract | CMT findings | DRIFT findings | Total |
|----------|-------------|----------------|-------|
| DegenerusGameJackpotModule.sol | X | Y | Z |
| DegenerusGameDecimatorModule.sol | X | Y | Z |
| DegenerusGameEndgameModule.sol | X | Y | Z |
| DegenerusGameGameOverModule.sol | X | Y | Z |
| DegenerusGameAdvanceModule.sol | X | Y | Z |
| **Total** | **X** | **Y** | **Z** |

*Summary counts updated after all contracts reviewed.*
```

**Step 2: Verify post-Phase-29 commit a2093fd6 (keep-roll tightening)**

Run `git diff a2093fd6~1..a2093fd6 -- contracts/modules/DegenerusGameJackpotModule.sol` to see exact changes. Verify:
- `_futureKeepBps` formula changed from `(total * 10_000) / 15` to `3000 + (total * 3500) / 15`
- NatSpec on `consolidatePrizePools` (line ~871) updated to "30-65%, avg ~47.5%"
- NatSpec on `_futureKeepBps` (line ~1281) updated to "30-65% keep (avg ~47.5%)"
- Verify the math: min = 3000 (30%), max = 3000+3500 = 6500 (65%), avg with total mean 7.5 = 3000+1750 = 4750 (47.5%)
- Search for any remaining references to old "0-100%" or "avg 50%" range

**Step 3: Verify post-Phase-29 commit 4cefca59 (future dump removal)**

Run `git diff 4cefca59~1..4cefca59 -- contracts/modules/DegenerusGameJackpotModule.sol` to see exact changes. Verify:
- `FUTURE_DUMP_TAG`, `FUTURE_DUMP_ODDS`, and `_shouldFutureDump` function are deleted
- The else-if branch in `consolidatePrizePools` that called `_shouldFutureDump` is removed
- NatSpec on `consolidatePrizePools` no longer mentions "1-in-1e15 chance to dump 90% of future into current"
- Run `grep -in 'future.*dump\|FUTURE_DUMP\|shouldFutureDump\|1e15\|quadrillion' contracts/modules/DegenerusGameJackpotModule.sol` to confirm no stale references remain
- If any stale references found, flag as CMT finding

**Step 4: Review architecture block comments (lines 16-43)**

These are extensive architecture comments describing jackpot flow, fund accounting, and randomness patterns. Verify each statement against current code behavior. Key areas:
- Jackpot fund flow description
- Prize pool split percentages
- Daily jackpot distribution percentages
- Auto-rebuy mechanics
- VRF/RNG interaction description
- Flag any descriptions that reference the removed future dump feature

**Step 5: Review all constants and events NatSpec**

For each constant and event @dev/@notice/@param:
1. Check the constant value against its description
2. Verify BPS annotations: DAILY_CURRENT_BPS_MIN=600 -> 6%, DAILY_CURRENT_BPS_MAX=1400 -> 14%. These are NOT standard BPS (where 10000=100%) -- they use 10000 as the roll range and the value directly as percentage*100 (so 600=6%, 1400=14%). Verify the NatSpec correctly describes this.
3. Verify pool split descriptions (1/10 for decimator, etc.)
4. Check for any constants referencing removed future dump feature

**Step 6: Review consolidatePrizePools function NatSpec and inline comments**

This is the most critical function for post-Phase-29 verification:
- Verify every NatSpec tag describes current behavior
- Verify all inline comments about keep-roll range match the new 30-65% range
- Verify no references to the removed future dump branch remain
- Verify pool split percentages in comments match actual code

**Step 7: Review daily jackpot functions NatSpec**

Functions: payDailyJackpot, payDailyJackpotCoinAndTickets, _payDailyCoinJackpot, _payDailyTicketJackpot
- Verify @notice/@dev/@param/@return for each
- Verify percentage annotations (6-14% daily jackpot range)
- Verify auto-rebuy descriptions
- Verify cross-module delegatecall descriptions (these functions are called via delegatecall from AdvanceModule)

**Step 8: Write JackpotModule findings section (first half)**

Add a `## DegenerusGameJackpotModule.sol` section header to the findings file. Write findings for any issues found in the header, constants, prize pool, and daily jackpot sections. Use this format:

```markdown
### CMT-0NN: [Brief title]
- **What:** [The specific comment inaccuracy]
- **Where:** DegenerusGameJackpotModule.sol:LINE
- **Why:** [Why a warden would be misled]
- **Suggestion:** [Recommended fix]
- **Category:** comment-inaccuracy | intent-drift
- **Severity:** INFO | LOW
```

Start CMT numbering at CMT-025, DRIFT numbering at DRIFT-003 (continuing from Phase 32 which ended at CMT-024, DRIFT-002).
  </action>
  <verify>
    <automated>grep -c '### CMT-\|### DRIFT-' audit/v3.1-findings-33-game-modules-batch-b.md; grep 'DegenerusGameJackpotModule' audit/v3.1-findings-33-game-modules-batch-b.md | head -1</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.1-findings-33-game-modules-batch-b.md exists
    - File contains `# Phase 33 Findings: Game Modules Batch B`
    - File contains `## DegenerusGameJackpotModule.sol`
    - CMT numbering starts at CMT-025 or higher (continuing from Phase 32)
    - Every finding has What, Where, Why, Suggestion, Category, and Severity fields
    - No finding has Category other than `comment-inaccuracy` or `intent-drift`
    - No finding has Severity other than `INFO` or `LOW`
    - grep for `future.*dump\|FUTURE_DUMP\|1e15` in JackpotModule returns empty (confirming removal is clean) or any remaining references are flagged as findings
    - No .sol files modified (flag-only)
  </acceptance_criteria>
  <done>Phase 33 findings file created. JackpotModule header, constants, events, prize pool consolidation, and daily jackpot functions reviewed. Post-Phase-29 commits a2093fd6 and 4cefca59 independently verified. Keep-roll range and future dump removal NatSpec verified.</done>
</task>

<task type="auto">
  <name>Task 2: Comment audit and intent drift review for DegenerusGameJackpotModule.sol (ticket processing, auto-rebuy, helpers)</name>
  <files>audit/v3.1-findings-33-game-modules-batch-b.md</files>
  <read_first>
    contracts/modules/DegenerusGameJackpotModule.sol
    audit/v3.1-findings-33-game-modules-batch-b.md
    .planning/phases/33-game-modules-batch-b/33-RESEARCH.md
  </read_first>
  <action>
Complete the second half of the DegenerusGameJackpotModule.sol review, covering ticket processing functions, auto-rebuy logic, helper/view functions, and the remaining NatSpec tags and inline comments.

**Step 1: Review ticket processing functions NatSpec**

Functions in this area include ticket evaluation, level jackpot processing, and player prize distribution. For each:
- Verify @notice/@dev/@param/@return match current behavior
- Verify BPS scale annotations for prize calculations
- Verify ticket count, level bonus, and prize pool interaction descriptions
- Pay special attention to any descriptions of how tickets interact with whale passes (cross-reference with WhaleModule behavior audited in Phase 32)

**Step 2: Review auto-rebuy logic NatSpec**

Auto-rebuy is a complex system where jackpot winnings can be automatically reinvested into new purchases:
- Verify the auto-rebuy flow description matches actual implementation
- Verify percentage/BPS annotations for auto-rebuy splits
- Verify cross-module references (auto-rebuy calls into MintModule functions)
- Check that the auto-rebuy threshold descriptions match actual constants

**Step 3: Review all remaining helper/view functions NatSpec**

Cover internal helpers (_futureKeepBps already verified in Task 1, skip), view functions (getJackpotInfo, etc.), and any utility functions:
- Verify each function's @notice/@dev/@param/@return
- Verify constants used by helper functions have accurate descriptions
- Check for any orphaned NatSpec (pattern from CMT-010/011/020 in prior phases)

**Step 4: Review all remaining inline comments**

Read every remaining `//` and `/* */` comment not covered in Task 1. Check for:
- Stale references to removed future dump feature
- BPS vs raw percentage confusion
- Cross-module references that may be misleading after post-Phase-29 changes
- Arithmetic descriptions that don't match actual formulas

**Step 5: Intent drift review for entire JackpotModule**

Scan the entire contract for:
- Vestigial references to removed features (future dump, old keep-roll range)
- Guards or conditions that may have become unnecessary after the code changes
- Logic whose behavior changed but comments still describe old behavior
- Any code paths that reference FUTURE_DUMP_TAG, FUTURE_DUMP_ODDS, or _shouldFutureDump
- Orphaned NatSpec from removed functions

**Step 6: Write remaining JackpotModule findings**

Append any additional findings to the JackpotModule section. Continue CMT/DRIFT numbering sequentially.

Update the Summary table JackpotModule row with actual counts for the entire contract.

Add a review summary block after all findings:

```markdown
---

**DegenerusGameJackpotModule.sol review complete.** 147 NatSpec tags verified, ~453 comment lines reviewed, all inline comments cross-referenced against current code behavior. [N] findings total. Post-Phase-29 commits a2093fd6 (keep-roll tightening) and 4cefca59 (future dump removal) independently verified.
```
  </action>
  <verify>
    <automated>grep -c '### CMT-\|### DRIFT-' audit/v3.1-findings-33-game-modules-batch-b.md; grep 'JackpotModule.sol review complete' audit/v3.1-findings-33-game-modules-batch-b.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.1-findings-33-game-modules-batch-b.md contains "JackpotModule.sol review complete"
    - Summary table row for JackpotModule has actual integer counts (not X/Y/Z placeholders)
    - Every finding has What, Where, Why, Suggestion, Category, and Severity fields
    - All 147 NatSpec tags in JackpotModule are accounted for (either verified clean or flagged)
    - No .sol files modified (flag-only)
  </acceptance_criteria>
  <done>DegenerusGameJackpotModule.sol (2,795 lines, 147 NatSpec tags, ~453 comment lines, 56 functions) fully reviewed for comment accuracy and intent drift. Post-Phase-29 changes independently verified. All remaining ticket processing, auto-rebuy, and helper functions reviewed. Review summary written. Summary table updated.</done>
</task>

</tasks>

<verification>
- audit/v3.1-findings-33-game-modules-batch-b.md exists with header and summary table
- grep 'DegenerusGameJackpotModule.sol' returns results
- grep 'JackpotModule.sol review complete' returns results
- CMT numbering starts at CMT-025 (no collision with Phase 32 CMT-011 through CMT-024)
- Every finding has what/where/why/suggestion/category/severity format
- No .sol files in contracts/ directory were modified
- No references to removed future dump feature remain unflagged
</verification>

<success_criteria>
- JackpotModule fully reviewed for comment accuracy and intent drift
- Post-Phase-29 code changes (a2093fd6, 4cefca59) specifically reviewed for stale NatSpec
- Keep-roll range (30-65%) and future dump removal independently verified
- Per-batch findings file created with proper format continuing Phase 32 numbering
- Every finding has what/why/suggestion per item
- No code changes made to any contract files
</success_criteria>

<output>
After completion, create `.planning/phases/33-game-modules-batch-b/33-01-SUMMARY.md`
</output>
