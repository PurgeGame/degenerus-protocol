---
phase: 32-game-modules-batch-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v3.1-findings-32-game-modules-batch-a.md
autonomous: true
requirements: [CMT-02, DRIFT-02]

must_haves:
  truths:
    - "Every NatSpec tag in DegenerusGameMintModule.sol is verified against current code behavior"
    - "Every inline comment in DegenerusGameMintModule.sol is verified against current logic"
    - "Every NatSpec tag in DegenerusGameWhaleModule.sol is verified against current code behavior"
    - "Every inline comment in DegenerusGameWhaleModule.sol is verified against current logic"
    - "Post-Phase-29 changes to MintModule (93708354) and WhaleModule (3542e227, 9aff84b2) are specifically reviewed for stale comments"
    - "Orphaned NatSpec at MintModule lines 1140-1146 is flagged"
    - "Missing NatSpec on MintModule processFutureTicketBatch is flagged"
    - "Misleading x1 NatSpec at WhaleModule line 166 is flagged"
    - "Any vestigial guards, unnecessary restrictions, or intent drift in these 2 contracts is flagged"
    - "A per-batch findings file exists with what/why/suggestion for every finding"
  artifacts:
    - path: "audit/v3.1-findings-32-game-modules-batch-a.md"
      provides: "Per-batch findings file for Phase 32 with MintModule and WhaleModule sections"
      contains: "## DegenerusGameMintModule.sol"
  key_links:
    - from: "audit/v3.1-findings-32-game-modules-batch-a.md"
      to: "contracts/modules/DegenerusGameMintModule.sol"
      via: "File:Line citations for every finding"
      pattern: "DegenerusGameMintModule\\.sol:\\d+"
    - from: "audit/v3.1-findings-32-game-modules-batch-a.md"
      to: "contracts/modules/DegenerusGameWhaleModule.sol"
      via: "File:Line citations for every finding"
      pattern: "DegenerusGameWhaleModule\\.sol:\\d+"
---

<objective>
Create the Phase 32 findings file and perform comment audit + intent drift review for DegenerusGameMintModule.sol (1,193 lines) and DegenerusGameWhaleModule.sol (840 lines) -- the two contracts modified after Phase 29.

Purpose: MintModule and WhaleModule are the only two contracts in this batch with post-Phase-29 code changes. MintModule had commit 93708354 (removing last-purchase-day lootbox block) and has orphaned NatSpec at lines 1140-1146 plus a completely undocumented external function (processFutureTicketBatch at line 294). WhaleModule had commits 3542e227 and 9aff84b2 (blocking lazy pass at x99, limiting boon discount to first bundle, gating x99 single buys) and has a misleading "Tickets always start at x1" NatSpec at line 166. Research pre-identified 4 of the 5 known issues in these two contracts.

Output: `audit/v3.1-findings-32-game-modules-batch-a.md` with file header, summary table template, and MintModule + WhaleModule findings sections. CMT numbering starts at CMT-011, DRIFT numbering starts at DRIFT-003 (continuing from Phase 31).
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-game-modules-batch-a/32-RESEARCH.md
@.planning/phases/31-core-game-contracts/31-01-SUMMARY.md
@audit/v3.1-findings-31-core-game-contracts.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comment audit and intent drift review for DegenerusGameMintModule.sol</name>
  <files>audit/v3.1-findings-32-game-modules-batch-a.md</files>
  <read_first>
    contracts/modules/DegenerusGameMintModule.sol
    .planning/phases/32-game-modules-batch-a/32-RESEARCH.md
    audit/v3.1-findings-31-core-game-contracts.md
  </read_first>
  <action>
Create the Phase 32 findings file and perform the full DegenerusGameMintModule.sol review (1,193 lines, 47 NatSpec tags, ~208 comment lines, 16 functions).

**Step 1: Create findings file with header**

Create `audit/v3.1-findings-32-game-modules-batch-a.md` with this exact template:

```markdown
# Phase 32 Findings: Game Modules Batch A

**Date:** 2026-03-18
**Scope:** MintModule, DegeneretteModule, WhaleModule, BoonModule, LootboxModule, PayoutUtils, MintStreakUtils
**Pass:** v3.1 second independent review (v3.0 Phase 29 was first pass)
**Mode:** Flag-only -- no code changes

## Summary

| Contract | CMT findings | DRIFT findings | Total |
|----------|-------------|----------------|-------|
| DegenerusGameMintModule.sol | X | Y | Z |
| DegenerusGameDegeneretteModule.sol | X | Y | Z |
| DegenerusGameWhaleModule.sol | X | Y | Z |
| DegenerusGameBoonModule.sol | X | Y | Z |
| DegenerusGameLootboxModule.sol | X | Y | Z |
| DegenerusGamePayoutUtils.sol | X | Y | Z |
| DegenerusGameMintStreakUtils.sol | X | Y | Z |
| **Total** | **X** | **Y** | **Z** |

*Summary counts updated after all contracts reviewed.*
```

**Step 2: Review MintModule post-Phase-29 changes**

MintModule had commit 93708354 (removing last-purchase-day lootbox block: two lines removed). The research says "No stale comment left behind from this change." However, verify independently -- read the area around the removed lines to confirm no orphaned references remain.

**Step 3: Review orphaned NatSpec at end of file (lines 1140-1193)**

CONFIRMED PRE-IDENTIFIED ISSUE: Lines 1140-1146 contain orphaned NatSpec (`@notice Resolve a lootbox directly (decimator claims)...` with 6 NatSpec lines) but no function body. Lines 1147-1192 are empty. The function was removed but the NatSpec was left behind. Flag this as CMT finding following the CMT-010 orphaned NatSpec pattern from Phase 31.

**Step 4: Review missing NatSpec on processFutureTicketBatch (line 294)**

CONFIRMED PRE-IDENTIFIED ISSUE: `processFutureTicketBatch` is a significant external function with no NatSpec whatsoever (no @notice, @dev, @param, or @return). This function processes the ticket activation queue and is callable externally. Flag this as CMT finding (missing NatSpec on external function).

**Step 5: Review all 47 NatSpec tags in MintModule**

For each @notice, @dev, @param, @return, @custom tag:
1. Read the tag text
2. Cross-reference against the actual function behavior in current HEAD
3. Focus on warden-readability: "Would a C4A warden reading this be misled?"
4. Pay special attention to:
   - BPS scale annotations (verify percentages match denominators)
   - Cross-module delegatecall references (this module calls LootboxModule, BoonModule)
   - Lootbox-related NatSpec (lootbox block was removed -- any stale references?)
   - Constants duplicated from other modules (LOOTBOX_BOOST_5_BONUS_BPS etc.)
5. Flag any mismatch as CMT finding

**Step 6: Review all inline comments (~208 comment lines)**

Read every `//` and `/* */` comment. Check for:
- References to the removed last-purchase-day lootbox block
- Stale references to removed features
- Scale/unit confusions (BPS vs absolute)
- Cross-module references that may be misleading

**Step 7: Intent drift review for MintModule**

Scan for:
- Vestigial guards from removed features
- The orphaned NatSpec at lines 1140-1146 is also an intent drift issue (code removed but documentation left)
- Unnecessary restrictions that may have accumulated
- Logic whose behavior changed but comments still describe old behavior

**Step 8: Write MintModule findings section**

Add a `## DegenerusGameMintModule.sol` section to the findings file. Use this format for each finding:

```markdown
### CMT-0NN: [Brief title]
- **What:** [The specific comment inaccuracy]
- **Where:** DegenerusGameMintModule.sol:LINE
- **Why:** [Why a warden would be misled]
- **Suggestion:** [Recommended fix]
- **Category:** comment-inaccuracy | intent-drift
- **Severity:** INFO | LOW
```

Start CMT numbering at CMT-011, DRIFT numbering at DRIFT-003 (continuing from Phase 31 which ended at CMT-010, DRIFT-002).

The file MUST include at minimum the 2 pre-identified MintModule issues: orphaned NatSpec at lines 1140-1146, missing NatSpec on processFutureTicketBatch at line 294. If the audit finds fewer than 2 MintModule findings, something is wrong.

Update the Summary table MintModule row with actual counts.
  </action>
  <verify>
    <automated>grep -c '### CMT-\|### DRIFT-' audit/v3.1-findings-32-game-modules-batch-a.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.1-findings-32-game-modules-batch-a.md exists
    - File contains `# Phase 32 Findings: Game Modules Batch A`
    - File contains `## DegenerusGameMintModule.sol`
    - File contains at least 2 findings entries for MintModule
    - File contains a finding referencing lines 1140-1146 (orphaned NatSpec)
    - File contains a finding referencing line 294 or processFutureTicketBatch (missing NatSpec)
    - Every finding has What, Where, Why, Suggestion, Category, and Severity fields
    - CMT numbering starts at CMT-011 or higher (continuing from Phase 31)
    - No finding has Category other than `comment-inaccuracy` or `intent-drift`
    - No finding has Severity other than `INFO` or `LOW`
    - No .sol files modified (flag-only)
  </acceptance_criteria>
  <done>DegenerusGameMintModule.sol (1,193 lines, 47 NatSpec tags, ~208 comment lines, 16 functions) fully reviewed for comment accuracy and intent drift. All pre-identified issues flagged. Findings file created with MintModule section.</done>
</task>

<task type="auto">
  <name>Task 2: Comment audit and intent drift review for DegenerusGameWhaleModule.sol</name>
  <files>audit/v3.1-findings-32-game-modules-batch-a.md</files>
  <read_first>
    contracts/modules/DegenerusGameWhaleModule.sol
    audit/v3.1-findings-32-game-modules-batch-a.md
    .planning/phases/32-game-modules-batch-a/32-RESEARCH.md
  </read_first>
  <action>
Perform the full DegenerusGameWhaleModule.sol comment audit and intent drift review (840 lines, 85 NatSpec tags, ~179 comment lines, 14 functions). WhaleModule received 2 post-Phase-29 commits that changed both code and NatSpec, making this a high-priority review target.

**Step 1: Review post-Phase-29 changes in WhaleModule**

Two commits changed this contract after Phase 29:
- **3542e227:** Blocked lazy pass at x99 levels, updated NatSpec for `purchaseLazyPass` (added x99 exclusion to `@custom:reverts`), added `currentLevel % 100 == 99` guard
- **9aff84b2:** Limited boon discount to first bundle only, gated x99 single buys, updated pricing comment ("boon discount applies to first bundle only"), updated `@dev` for `WHALE_BUNDLE_STANDARD_PRICE` from "x49/x99 levels" to "levels 4+"

For each change, verify: (a) the NatSpec update is accurate and complete, (b) no nearby NatSpec was left stale by the change, (c) inline comments around the changed code are accurate.

**Step 2: Review misleading "x1" NatSpec at line 166**

CONFIRMED PRE-IDENTIFIED ISSUE: NatSpec says `Tickets always start at x1` but `ticketStartLevel = passLevel` (line 211) where `passLevel = level + 1` (line 192). Tickets start at whatever level+1 is, not necessarily at a level ending in 1. The "x1" notation means "level ending in 1" (e.g., x1, x11, x21) but the code starts tickets at level+1 which could be any value. Flag this as CMT finding.

**Step 3: Review all 85 NatSpec tags in WhaleModule**

For each @notice, @dev, @param, @return, @custom tag:
1. Read the tag text
2. Cross-reference against actual function behavior
3. Pay special attention to:
   - BPS vs PPM scale annotations (WhaleModule uses BOTH for different calculations: BPS for prices, PPM for DGNRS pool calculations)
   - `@dev` blocks on purchase functions (extensive documentation that is prime warden reading material)
   - Constants like WHALE_BUNDLE_STANDARD_PRICE -- verify `@dev` matches current pricing logic
   - `purchaseWhaleBundle` NatSpec (the most complex function with boon discount, bundle pricing, x99 guards)
   - `purchaseLazyPass` NatSpec (recently updated for x99 exclusion -- verify completeness)
4. Focus on warden-readability

**Step 4: Review all inline comments (~179 comment lines)**

Read every `//` comment. Check for:
- References to old pricing (pre-boon-discount-limit, pre-x99-gate)
- Stale references to "x49/x99 levels" in pricing contexts (should now be "levels 4+")
- BPS vs PPM confusion in pool calculation comments
- Cross-module references (WhaleModule inherits from MintStreakUtils -> PayoutUtils)

**Step 5: Intent drift review for WhaleModule**

Scan for:
- Vestigial guards from pre-x99-gate era
- Any pricing logic that references old pricing tiers
- Logic that may have become unnecessary after the boon discount was limited to first bundle
- Guards that were left in place when the x99 gate was added (redundant checks)

**Step 6: Write WhaleModule findings section**

Append a `## DegenerusGameWhaleModule.sol` section to the findings file. Continue CMT/DRIFT numbering from where MintModule left off. Use the same finding format.

The file MUST include at minimum the pre-identified misleading "x1" NatSpec at line 166. If the audit finds 0 findings in WhaleModule, something is almost certainly wrong.

Update the Summary table WhaleModule row with actual counts.
  </action>
  <verify>
    <automated>grep '## DegenerusGameWhaleModule.sol' audit/v3.1-findings-32-game-modules-batch-a.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.1-findings-32-game-modules-batch-a.md contains `## DegenerusGameWhaleModule.sol`
    - File contains a finding referencing line 166 or "x1" (misleading ticket start level)
    - WhaleModule section has at least 1 finding
    - Every finding has What, Where, Why, Suggestion, Category, and Severity fields
    - Summary table row for WhaleModule has been updated with actual counts
    - No code changes to contracts/modules/DegenerusGameWhaleModule.sol (flag-only)
  </acceptance_criteria>
  <done>DegenerusGameWhaleModule.sol (840 lines, 85 NatSpec tags, ~179 comment lines, 14 functions) fully reviewed for comment accuracy and intent drift. Post-Phase-29 changes specifically reviewed for completeness. Pre-identified misleading "x1" NatSpec flagged. WhaleModule findings section appended to batch deliverable.</done>
</task>

</tasks>

<verification>
- audit/v3.1-findings-32-game-modules-batch-a.md exists with header and summary table
- grep 'DegenerusGameMintModule.sol' returns results
- grep 'DegenerusGameWhaleModule.sol' returns results
- grep -c '### CMT-\|### DRIFT-' returns at least 3 (2 MintModule pre-identified + 1 WhaleModule pre-identified)
- CMT numbering starts at 011 or higher (no collision with Phase 31 CMT-001 through CMT-010)
- No .sol files in contracts/ directory were modified
</verification>

<success_criteria>
- MintModule and WhaleModule fully reviewed for comment accuracy and intent drift
- All 4 pre-identified issues in these contracts are flagged (MintModule orphaned NatSpec, MintModule missing NatSpec, WhaleModule misleading x1)
- Post-Phase-29 code changes specifically reviewed for stale NatSpec
- Per-batch findings file created with proper format continuing Phase 31 numbering
- Every finding has what/why/suggestion per item
- No code changes made to any contract files
</success_criteria>

<output>
After completion, create `.planning/phases/32-game-modules-batch-a/32-01-SUMMARY.md`
</output>
