---
phase: 32-game-modules-batch-a
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - audit/v3.1-findings-32-game-modules-batch-a.md
autonomous: true
requirements: [CMT-02, DRIFT-02]

must_haves:
  truths:
    - "Every NatSpec tag in DegenerusGameDegeneretteModule.sol is verified against current code behavior"
    - "Every inline comment in DegenerusGameDegeneretteModule.sol is verified against current logic"
    - "Every NatSpec tag in DegenerusGameBoonModule.sol is verified against current code behavior"
    - "Every inline comment in DegenerusGameBoonModule.sol is verified against current logic"
    - "Every NatSpec tag in DegenerusGamePayoutUtils.sol is verified against current code behavior"
    - "Every NatSpec tag in DegenerusGameMintStreakUtils.sol is verified against current code behavior"
    - "Orphaned NatSpec at DegeneretteModule line 406 is flagged"
    - "Packed bet layout documentation in DegeneretteModule (lines 312-341) is verified against actual packing logic"
    - "Any vestigial guards, unnecessary restrictions, or intent drift in these 4 contracts is flagged"
  artifacts:
    - path: "audit/v3.1-findings-32-game-modules-batch-a.md"
      provides: "Findings file with DegeneretteModule, BoonModule, PayoutUtils, and MintStreakUtils sections added"
      contains: "## DegenerusGameDegeneretteModule.sol"
  key_links:
    - from: "audit/v3.1-findings-32-game-modules-batch-a.md"
      to: "contracts/modules/DegenerusGameDegeneretteModule.sol"
      via: "File:Line citations for every finding"
      pattern: "DegenerusGameDegeneretteModule\\.sol:\\d+"
    - from: "audit/v3.1-findings-32-game-modules-batch-a.md"
      to: "contracts/modules/DegenerusGameBoonModule.sol"
      via: "File:Line citations for every finding"
      pattern: "DegenerusGameBoonModule\\.sol:\\d+"
---

<objective>
Comment audit and intent drift review for DegenerusGameDegeneretteModule.sol (1,179 lines), DegenerusGameBoonModule.sol (359 lines), DegenerusGamePayoutUtils.sol (94 lines), and DegenerusGameMintStreakUtils.sol (62 lines).

Purpose: These 4 contracts form the inheritance chain (PayoutUtils -> MintStreakUtils -> DegeneretteModule) plus the standalone BoonModule. None have been modified since Phase 29, but v3.1 provides a fresh warden-readability lens. DegeneretteModule has a confirmed orphaned NatSpec at line 406 (identical to the CMT-010 pattern from Phase 31) and extensive packed bet layout documentation (lines 312-341) that is prime warden reading material. BoonModule has 4 near-identical consume functions and a large repetitive boon-clearing function requiring pattern-check methodology.

Output: DegeneretteModule, BoonModule, PayoutUtils, and MintStreakUtils findings sections appended to `audit/v3.1-findings-32-game-modules-batch-a.md`.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-game-modules-batch-a/32-RESEARCH.md
@.planning/phases/32-game-modules-batch-a/32-01-SUMMARY.md
@audit/v3.1-findings-31-core-game-contracts.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comment audit and intent drift review for PayoutUtils, MintStreakUtils, and BoonModule</name>
  <files>audit/v3.1-findings-32-game-modules-batch-a.md</files>
  <read_first>
    contracts/modules/DegenerusGamePayoutUtils.sol
    contracts/modules/DegenerusGameMintStreakUtils.sol
    contracts/modules/DegenerusGameBoonModule.sol
    audit/v3.1-findings-32-game-modules-batch-a.md
    .planning/phases/32-game-modules-batch-a/32-RESEARCH.md
  </read_first>
  <action>
Review the 3 smaller contracts: PayoutUtils (94 lines, 7 NatSpec tags, 3 functions), MintStreakUtils (62 lines, 5 NatSpec tags, 2 functions), and BoonModule (359 lines, 18 NatSpec tags, 5 functions). None have changed since Phase 29.

**Step 1: Review DegenerusGamePayoutUtils.sol (94 lines)**

This is an abstract utility contract with 3 functions and 7 NatSpec tags. It provides payout helpers used by DegeneretteModule and WhaleModule via inheritance.

For each NatSpec tag:
1. Read the tag text
2. Verify it accurately describes the helper function's behavior
3. Check that parameter descriptions match actual parameter names and types
4. Verify any BPS/percentage annotations match the actual scale used in calculations
5. Check inline comments for accuracy

Since this is only 94 lines, perform a thorough line-by-line review.

Intent drift: Check for any payout calculation logic that may have become vestigial or unnecessary after game mechanic changes.

**Step 2: Review DegenerusGameMintStreakUtils.sol (62 lines)**

This is an abstract utility contract with 2 functions and 5 NatSpec tags. It provides mint streak helpers used by DegeneretteModule and WhaleModule via inheritance.

For each NatSpec tag:
1. Read the tag text
2. Verify it accurately describes the mint streak function's behavior
3. Cross-reference against how DegeneretteModule and WhaleModule actually call these functions
4. Check streak calculation constants against NatSpec descriptions

Since this is only 62 lines, perform a thorough line-by-line review.

Intent drift: Check for mint streak logic that may reference old streak definitions or thresholds that have changed.

**Step 3: Review DegenerusGameBoonModule.sol (359 lines)**

BoonModule has 5 functions and 18 NatSpec tags. It consists of 4 near-identical `consumeBoonXxx` functions and one large `checkAndClearExpiredBoon` function with 11 repetitive boon-clearing blocks.

Review approach per research recommendation:
1. Review the first `consumeBoon` function's NatSpec in full detail -- verify every tag against code behavior
2. Pattern-check the remaining 3 `consumeBoon` functions -- verify they follow the same structure and their NatSpec is consistent
3. Flag any `consumeBoon` function whose NatSpec deviates from the established pattern
4. Review `checkAndClearExpiredBoon` NatSpec tags -- verify the function-level description
5. Verify the first boon-clearing block in full, then pattern-check the remaining 10 blocks for consistent structure
6. Flag any block that deviates from the pattern

Focus areas for BoonModule:
- BPS vs PPM: BoonModule uses BOON_PPM_SCALE (1e6) for probability rolls -- verify percentage comments match the PPM denominator
- Cross-module consistency: BoonModule is called via nested delegatecall from LootboxModule and MintModule -- verify NatSpec accurately describes the delegatecall context
- Boon expiration mechanics: verify NatSpec describes the actual expiration logic

**Step 4: Write findings sections for all 3 contracts**

Append sections to the findings file in this order:
1. `## DegenerusGamePayoutUtils.sol` -- findings or explicit "0 findings" note
2. `## DegenerusGameMintStreakUtils.sol` -- findings or explicit "0 findings" note
3. `## DegenerusGameBoonModule.sol` -- findings or explicit "0 findings" note

Continue CMT/DRIFT numbering from where Plan 01 left off. Update Summary table rows for all 3 contracts.

For zero-finding contracts, document: "**Review scope:** X lines, Y NatSpec tags, Z functions. **Post-Phase-29 changes:** None. All NatSpec verified accurate. All inline comments verified. No intent drift detected."
  </action>
  <verify>
    <automated>grep -c '## DegenerusGamePayoutUtils.sol\|## DegenerusGameMintStreakUtils.sol\|## DegenerusGameBoonModule.sol' audit/v3.1-findings-32-game-modules-batch-a.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.1-findings-32-game-modules-batch-a.md contains `## DegenerusGamePayoutUtils.sol`
    - audit/v3.1-findings-32-game-modules-batch-a.md contains `## DegenerusGameMintStreakUtils.sol`
    - audit/v3.1-findings-32-game-modules-batch-a.md contains `## DegenerusGameBoonModule.sol`
    - Each contract section has either findings entries OR an explicit review-complete note
    - Every finding (if any) has What, Where, Why, Suggestion, Category, and Severity fields
    - Summary table rows for PayoutUtils, MintStreakUtils, and BoonModule have actual counts (not X/Y/Z)
    - No code changes to any .sol files (flag-only)
  </acceptance_criteria>
  <done>PayoutUtils (94 lines), MintStreakUtils (62 lines), and BoonModule (359 lines) fully reviewed for comment accuracy and intent drift. All NatSpec tags verified. Boon repetitive blocks pattern-checked for consistency. Findings sections appended to batch deliverable.</done>
</task>

<task type="auto">
  <name>Task 2: Comment audit and intent drift review for DegenerusGameDegeneretteModule.sol</name>
  <files>audit/v3.1-findings-32-game-modules-batch-a.md</files>
  <read_first>
    contracts/modules/DegenerusGameDegeneretteModule.sol
    audit/v3.1-findings-32-game-modules-batch-a.md
    .planning/phases/32-game-modules-batch-a/32-RESEARCH.md
  </read_first>
  <action>
Perform the full DegenerusGameDegeneretteModule.sol comment audit and intent drift review (1,179 lines, 156 NatSpec tags, ~333 comment lines, 31 functions). This contract has NOT been modified since Phase 29.

**Step 1: Review orphaned NatSpec at line 406**

CONFIRMED PRE-IDENTIFIED ISSUE: `@notice Places Full Ticket bets using pending affiliate Degenerette credit.` appears immediately before the unrelated `resolveBets` function. This NatSpec belongs to a removed function (affiliate credit betting) but was left behind. This is identical to the CMT-010 pattern from Phase 31 (orphaned NatSpec from removed function). Flag as CMT finding.

**Step 2: Review packed bet layout documentation (lines 312-341)**

These lines contain extensive documentation describing how bet data is packed into uint256 slots. This is prime warden reading material. For each bit field described:
1. Read the bit range and field name from the comment
2. Verify it matches the actual packing/unpacking logic in the code
3. Check that field widths and positions are accurate
4. Verify mask and shift values in the code match the documented layout

This is a critical verification because incorrect packed layout documentation would directly mislead wardens analyzing the bet resolution logic.

**Step 3: Review all 156 NatSpec tags in DegeneretteModule**

For each @notice, @dev, @param, @return, @custom tag:
1. Read the tag text
2. Cross-reference against actual function behavior
3. Pay special attention to:
   - BPS scale annotations for ROI calculations (DegeneretteModule uses BPS for ROI)
   - Bet resolution NatSpec (complex logic with multiple outcomes)
   - Functions inherited from PayoutUtils and MintStreakUtils -- verify NatSpec describes the inherited behavior correctly
   - Cross-module delegatecall references (DegeneretteModule calls LootboxModule for lootbox rewards)
   - Constants with `@dev` annotations -- verify each percentage/value matches
4. Focus on warden-readability

**Step 4: Review all inline comments (~333 comment lines)**

Read every `//` and `/* */` comment. Check for:
- References to removed affiliate credit betting function (the orphaned NatSpec at line 406 suggests there may be other stale references)
- Stale references to old bet mechanics or pricing
- BPS/percentage accuracy in calculation comments
- Cross-module references that may be misleading

**Step 5: Intent drift review for DegeneretteModule**

Scan for:
- Vestigial guards from the removed affiliate credit betting function
- Any affiliate-related code that may have been partially removed
- Unnecessary restrictions in bet placement or resolution
- Logic whose behavior has changed but surrounding comments still describe old behavior
- Inheritance-related drift: PayoutUtils/MintStreakUtils methods used in ways that differ from their NatSpec

**Step 6: Write DegeneretteModule findings section**

Append a `## DegenerusGameDegeneretteModule.sol` section to the findings file. Continue CMT/DRIFT numbering from previous findings.

The file MUST include at minimum the pre-identified orphaned NatSpec at line 406. If the audit finds 0 findings in DegeneretteModule, something is almost certainly wrong.

Update the Summary table DegeneretteModule row with actual counts.
  </action>
  <verify>
    <automated>grep '## DegenerusGameDegeneretteModule.sol' audit/v3.1-findings-32-game-modules-batch-a.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.1-findings-32-game-modules-batch-a.md contains `## DegenerusGameDegeneretteModule.sol`
    - File contains a finding referencing line 406 or "orphaned NatSpec" for DegeneretteModule
    - DegeneretteModule section has at least 1 finding
    - Packed bet layout documentation (lines 312-341) is explicitly mentioned as verified
    - Every finding has What, Where, Why, Suggestion, Category, and Severity fields
    - Summary table row for DegeneretteModule has actual counts
    - No code changes to contracts/modules/DegenerusGameDegeneretteModule.sol (flag-only)
  </acceptance_criteria>
  <done>DegenerusGameDegeneretteModule.sol (1,179 lines, 156 NatSpec tags, ~333 comment lines, 31 functions) fully reviewed for comment accuracy and intent drift. Orphaned NatSpec at line 406 flagged. Packed bet layout documentation verified. All NatSpec tags verified. Findings section appended to batch deliverable.</done>
</task>

</tasks>

<verification>
- grep 'DegenerusGameDegeneretteModule.sol' audit/v3.1-findings-32-game-modules-batch-a.md returns results
- grep 'DegenerusGameBoonModule.sol' audit/v3.1-findings-32-game-modules-batch-a.md returns results
- grep 'DegenerusGamePayoutUtils.sol' audit/v3.1-findings-32-game-modules-batch-a.md returns results
- grep 'DegenerusGameMintStreakUtils.sol' audit/v3.1-findings-32-game-modules-batch-a.md returns results
- At least 1 finding references DegeneretteModule line 406 (orphaned NatSpec)
- No .sol files in contracts/ directory were modified
</verification>

<success_criteria>
- All 4 contracts fully reviewed for comment accuracy and intent drift
- Pre-identified orphaned NatSpec at DegeneretteModule line 406 is flagged
- Packed bet layout documentation verified for accuracy
- BoonModule repetitive pattern-check methodology applied
- PayoutUtils and MintStreakUtils reviewed thoroughly (small enough for line-by-line)
- Every finding has what/why/suggestion per item
- No code changes made to any contract files
</success_criteria>

<output>
After completion, create `.planning/phases/32-game-modules-batch-a/32-02-SUMMARY.md`
</output>
