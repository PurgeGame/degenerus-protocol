---
phase: 32-game-modules-batch-a
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - audit/v3.1-findings-32-game-modules-batch-a.md
autonomous: true
requirements: [CMT-02, DRIFT-02]

must_haves:
  truths:
    - "Every NatSpec tag in DegenerusGameLootboxModule.sol is verified against current code behavior"
    - "Every inline comment in DegenerusGameLootboxModule.sol is verified against current logic"
    - "The 260% vs 255% discrepancy at LootboxModule line 328 is flagged"
    - "BPS vs PPM scale annotations are verified against actual denominators throughout LootboxModule"
    - "Boon probability math NatSpec is verified against actual calculation logic"
    - "Any vestigial guards, unnecessary restrictions, or intent drift in LootboxModule is flagged"
    - "The consolidated findings file has complete Summary counts for all 7 contracts"
    - "Total finding count in Summary table matches actual finding heading count in file"
  artifacts:
    - path: "audit/v3.1-findings-32-game-modules-batch-a.md"
      provides: "Complete per-batch findings file with all 7 contract sections and final summary counts"
      contains: "## DegenerusGameLootboxModule.sol"
  key_links:
    - from: "audit/v3.1-findings-32-game-modules-batch-a.md"
      to: "contracts/modules/DegenerusGameLootboxModule.sol"
      via: "File:Line citations for every finding"
      pattern: "DegenerusGameLootboxModule\\.sol:\\d+"
---

<objective>
Comment audit and intent drift review for DegenerusGameLootboxModule.sol (1,778 lines, 308 NatSpec tags, ~408 comment lines, 27 functions). Finalize the Phase 32 findings file.

Purpose: LootboxModule is the largest contract in Batch A and has the most NatSpec tags (308). It uses BPS extensively but also BOON_PPM_SCALE (1e6) for boon probability rolls, making scale verification critical. A confirmed 260% vs 255% discrepancy exists at line 328 (should be 255% per ACTIVITY_SCORE_MAX_BPS = 25_500). The contract has NOT been modified since Phase 29, but its complexity (reward distribution, boon probability, activity score calculations, lootbox boost mechanics) demands focused attention. This plan also finalizes the consolidated findings file with complete Summary table counts.

Output: `## DegenerusGameLootboxModule.sol` section appended to `audit/v3.1-findings-32-game-modules-batch-a.md`, plus finalized Summary table with actual counts for all 7 contracts.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-game-modules-batch-a/32-RESEARCH.md
@.planning/phases/32-game-modules-batch-a/32-01-SUMMARY.md
@.planning/phases/32-game-modules-batch-a/32-02-SUMMARY.md
@audit/v3.1-findings-31-core-game-contracts.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comment audit and intent drift review for DegenerusGameLootboxModule.sol</name>
  <files>audit/v3.1-findings-32-game-modules-batch-a.md</files>
  <read_first>
    contracts/modules/DegenerusGameLootboxModule.sol
    audit/v3.1-findings-32-game-modules-batch-a.md
    .planning/phases/32-game-modules-batch-a/32-RESEARCH.md
  </read_first>
  <action>
Perform the full DegenerusGameLootboxModule.sol comment audit and intent drift review (1,778 lines, 308 NatSpec tags, ~408 comment lines, 27 functions). This contract has NOT been modified since Phase 29.

**Step 1: Review 260% vs 255% discrepancy at line 328**

CONFIRMED PRE-IDENTIFIED ISSUE: Comment says `Maximum EV at 260%+ activity (135%)` but `ACTIVITY_SCORE_MAX_BPS = 25_500` which is 255%, not 260%. Line 322 correctly says "255%+". The same function's NatSpec at line 467 correctly says "255%+ activity". Only line 328 is wrong. Flag as CMT finding.

**Step 2: Review all BPS and PPM scale annotations**

LootboxModule uses multiple scale systems:
- BPS (basis points, /10_000) for most calculations
- BOON_PPM_SCALE (1e6) for boon probability rolls
- Activity score BPS (where 100% = 10_000)

For EVERY percentage or scale annotation in a NatSpec or inline comment:
1. Identify the constant being referenced
2. Check the constant's actual value
3. Calculate the real percentage: value / denominator * 100
4. Compare against the comment's stated percentage
5. Flag any mismatch

Key constants to verify:
- ACTIVITY_SCORE_MAX_BPS = 25_500 (should be described as 255%)
- All LOOTBOX_BOOST_*_BONUS_BPS constants -- verify percentage descriptions
- BOON_PPM_SCALE and boon probability constants -- verify PPM descriptions
- Any "EV" (expected value) percentage annotations -- verify against actual multiplier math

**Step 3: Review all 308 NatSpec tags in LootboxModule**

This is the most NatSpec-dense contract in the batch. For each @notice, @dev, @param, @return, @custom tag:
1. Read the tag text
2. Cross-reference against actual function behavior
3. Pay special attention to:
   - Reward distribution functions -- complex branching logic where NatSpec must accurately describe all outcomes
   - Boon probability calculation NatSpec -- verify the math descriptions match the code
   - Activity score calculation NatSpec -- verify thresholds and scaling descriptions
   - Lootbox boost mechanics NatSpec -- verify boost tier descriptions match constants
   - Cross-module delegatecall references (LootboxModule calls BoonModule for boon consumption)
   - Constants with duplicated names across modules (LOOTBOX_BOOST_5_BONUS_BPS appears in MintModule, WhaleModule, and LootboxModule) -- verify consistent NatSpec descriptions
4. Focus on warden-readability: complex math functions need clear NatSpec

**Step 4: Review all inline comments (~408 comment lines)**

Read every `//` and `/* */` comment. Check for:
- Scale/unit confusions (BPS vs PPM)
- Percentage annotations that may not match actual constants
- References to old reward mechanics or pricing
- Activity score threshold descriptions
- Boon interaction descriptions (LootboxModule -> BoonModule delegatecall)
- Lootbox boost tier descriptions

**Step 5: Intent drift review for LootboxModule**

Scan for:
- Vestigial guards from removed features
- Activity score thresholds or calculations that may have changed without NatSpec updates
- Boon probability logic that may have been adjusted without corresponding comment updates
- Reward distribution branches that may reference old reward tiers or amounts
- Cross-module consistency: do LootboxModule's references to BoonModule behavior match BoonModule's actual behavior?

**Step 6: Write LootboxModule findings section**

Append a `## DegenerusGameLootboxModule.sol` section to the findings file. Continue CMT/DRIFT numbering from where Plan 02 left off.

The file MUST include at minimum the pre-identified 260% vs 255% discrepancy at line 328. If the audit finds 0 findings in LootboxModule, something is almost certainly wrong -- 308 NatSpec tags across 1,778 lines with complex math is very likely to have at least one inaccuracy.

Update the Summary table LootboxModule row with actual counts.
  </action>
  <verify>
    <automated>grep '## DegenerusGameLootboxModule.sol' audit/v3.1-findings-32-game-modules-batch-a.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.1-findings-32-game-modules-batch-a.md contains `## DegenerusGameLootboxModule.sol`
    - File contains a finding referencing line 328 or "260%" (activity score percentage discrepancy)
    - LootboxModule section has at least 1 finding
    - Every finding has What, Where, Why, Suggestion, Category, and Severity fields
    - Summary table row for LootboxModule has actual counts
    - No code changes to contracts/modules/DegenerusGameLootboxModule.sol (flag-only)
  </acceptance_criteria>
  <done>DegenerusGameLootboxModule.sol (1,778 lines, 308 NatSpec tags, ~408 comment lines, 27 functions) fully reviewed for comment accuracy and intent drift. Pre-identified 260% vs 255% discrepancy flagged. All BPS/PPM scale annotations verified. All NatSpec tags verified. LootboxModule findings section appended to batch deliverable.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize Phase 32 findings file</name>
  <files>audit/v3.1-findings-32-game-modules-batch-a.md</files>
  <read_first>
    audit/v3.1-findings-32-game-modules-batch-a.md
  </read_first>
  <action>
Finalize the consolidated Phase 32 findings file by updating the Summary table and performing a completeness check.

**Step 1: Update Summary table with final counts**

Read the entire findings file. Count the actual number of `### CMT-` and `### DRIFT-` headings for each contract section. Update the Summary table at the top:

```markdown
| Contract | CMT findings | DRIFT findings | Total |
|----------|-------------|----------------|-------|
| DegenerusGameMintModule.sol | [actual] | [actual] | [actual] |
| DegenerusGameDegeneretteModule.sol | [actual] | [actual] | [actual] |
| DegenerusGameWhaleModule.sol | [actual] | [actual] | [actual] |
| DegenerusGameBoonModule.sol | [actual] | [actual] | [actual] |
| DegenerusGameLootboxModule.sol | [actual] | [actual] | [actual] |
| DegenerusGamePayoutUtils.sol | [actual] | [actual] | [actual] |
| DegenerusGameMintStreakUtils.sol | [actual] | [actual] | [actual] |
| **Total** | **[sum]** | **[sum]** | **[sum]** |
```

Replace all X/Y/Z placeholders with actual integer counts.

**Step 2: Verify all 7 contracts are covered**

Confirm the file contains sections for ALL 7 contracts:
1. `## DegenerusGameMintModule.sol`
2. `## DegenerusGameDegeneretteModule.sol`
3. `## DegenerusGameWhaleModule.sol`
4. `## DegenerusGameBoonModule.sol`
5. `## DegenerusGameLootboxModule.sol`
6. `## DegenerusGamePayoutUtils.sol`
7. `## DegenerusGameMintStreakUtils.sol`

If any section is missing, flag it -- do not create placeholder findings.

**Step 3: Cross-check finding numbering**

Verify CMT numbering is sequential starting from CMT-011 (continuing Phase 31 which ended at CMT-010). Verify DRIFT numbering is sequential starting from DRIFT-003 (continuing Phase 31 which ended at DRIFT-002). No gaps or duplicates in the numbering sequence.

**Step 4: Verify all pre-identified issues are present**

Confirm these 5 pre-identified issues from research are all present as findings:
1. MintModule lines 1140-1146: orphaned NatSpec
2. DegeneretteModule line 406: orphaned NatSpec
3. LootboxModule line 328: 260% vs 255%
4. WhaleModule line 166: misleading "x1"
5. MintModule line 294: missing NatSpec on processFutureTicketBatch

If any are missing, add them now using the standard finding format.

**Step 5: Remove placeholder text**

Delete the `*Summary counts updated after all contracts reviewed.*` placeholder line if it still exists.

**Step 6: Verify Summary table matches actual content**

Count `### CMT-` headings in file and verify total matches the CMT total in Summary table. Count `### DRIFT-` headings and verify total matches DRIFT total. Verify grand total equals CMT + DRIFT counts.
  </action>
  <verify>
    <automated>grep -c '### CMT-\|### DRIFT-' audit/v3.1-findings-32-game-modules-batch-a.md</automated>
  </verify>
  <acceptance_criteria>
    - Summary table at top has actual integer counts (no X, Y, or Z placeholders) for all 7 contracts and totals
    - grep for all 7 `## DegenerusGame*.sol` section headers returns 7 matches
    - Total finding count in Summary table matches actual count of `### CMT-` and `### DRIFT-` headings
    - CMT numbering starts at CMT-011 (no collision with Phase 31 CMT-001 through CMT-010)
    - DRIFT numbering starts at DRIFT-003 (no collision with Phase 31 DRIFT-001 through DRIFT-002)
    - No duplicate finding numbers
    - All 5 pre-identified issues from research are present as findings
    - Every finding has What, Where, Why, Suggestion, Category, and Severity fields
    - No .sol files modified (flag-only)
    - No `*Summary counts updated*` placeholder text remains
  </acceptance_criteria>
  <done>Phase 32 findings file is complete. All 7 contracts have sections. Summary table has accurate counts. All 5 pre-identified issues confirmed present. Finding numbering is sequential and collision-free with Phase 31. File is ready for Phase 36 consolidation.</done>
</task>

</tasks>

<verification>
- audit/v3.1-findings-32-game-modules-batch-a.md exists with all 7 contract sections
- Summary table has real counts (not placeholders) totaling correctly
- At least 5 pre-identified issues are flagged (MintModule orphaned NatSpec, MintModule missing NatSpec, DegeneretteModule orphaned NatSpec, LootboxModule 260/255, WhaleModule x1)
- CMT numbering starts at CMT-011, DRIFT starts at DRIFT-003
- Every finding has what/where/why/suggestion/category/severity format
- No .sol files in contracts/ directory were modified
- Finding count in Summary table equals actual `### CMT-|### DRIFT-` heading count
</verification>

<success_criteria>
- LootboxModule fully reviewed for comment accuracy and intent drift
- Pre-identified 260% vs 255% discrepancy flagged
- All BPS/PPM scale annotations verified across all 27 functions
- Consolidated findings file complete with all 7 contracts and accurate summary counts
- All 5 pre-identified issues from research confirmed present
- Finding numbering sequential and collision-free with Phase 31
- No code changes made to any contract files
- Phase 32 per-batch deliverable ready for Phase 36 consolidation
</success_criteria>

<output>
After completion, create `.planning/phases/32-game-modules-batch-a/32-03-SUMMARY.md`
</output>
