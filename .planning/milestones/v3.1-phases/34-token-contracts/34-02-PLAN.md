---
phase: 34-token-contracts
plan: 02
type: execute
wave: 1
depends_on: [34-01]
files_modified:
  - audit/v3.1-findings-34-token-contracts.md
autonomous: true
requirements: [CMT-04, DRIFT-04]

must_haves:
  truths:
    - "Every NatSpec tag in DegenerusStonk.sol is verified against current code behavior"
    - "Every NatSpec tag in StakedDegenerusStonk.sol is verified against current code behavior"
    - "Every NatSpec tag in WrappedWrappedXRP.sol is verified against current code behavior"
    - "Every inline comment in all 3 contracts is verified against current logic"
    - "Post-Phase-29 change in DegenerusStonk.sol (fd9dbad1: VRF stall 20h->5h) independently verified clean"
    - "sDGNRS/DGNRS naming inconsistency in StakedDegenerusStonk.sol pool NatSpec (lines 300, 304, 327) is flagged"
    - "DegenerusStonk.sol _transfer self-transfer block without NatSpec is evaluated"
    - "Any vestigial logic or intent drift in all 3 contracts is flagged"
    - "Summary table has actual integer counts for all 4 contracts"
    - "Total finding count is correct across all rows"
  artifacts:
    - path: "audit/v3.1-findings-34-token-contracts.md"
      provides: "Complete per-batch findings file for Phase 34 with all 4 contract sections and finalized summary"
      contains: "## WrappedWrappedXRP.sol"
  key_links:
    - from: "audit/v3.1-findings-34-token-contracts.md"
      to: "contracts/StakedDegenerusStonk.sol"
      via: "File:Line citations for every finding"
      pattern: "StakedDegenerusStonk\\.sol:\\d+"
    - from: "audit/v3.1-findings-34-token-contracts.md"
      to: "contracts/DegenerusStonk.sol"
      via: "File:Line citations for every finding"
      pattern: "DegenerusStonk\\.sol:\\d+"
    - from: "audit/v3.1-findings-34-token-contracts.md"
      to: "contracts/WrappedWrappedXRP.sol"
      via: "File:Line citations for every finding"
      pattern: "WrappedWrappedXRP\\.sol:\\d+"
---

<objective>
Perform comment audit + intent drift review for DegenerusStonk.sol (223 lines), StakedDegenerusStonk.sol (514 lines), and WrappedWrappedXRP.sol (389 lines), then finalize the Phase 34 findings file with summary counts.

Purpose: These 3 contracts total 1,126 lines and 53 functions. DegenerusStonk is the DGNRS wrapper around sDGNRS with one post-Phase-29 change (fd9dbad1: VRF stall threshold 20h to 5h). StakedDegenerusStonk has a pre-identified sDGNRS/DGNRS naming inconsistency in pool function NatSpec. WrappedWrappedXRP is a joke ERC20 with unbacked mintPrize and first-come-first-served unwrap. The DegenerusStonk/StakedDegenerusStonk pair should be reviewed together since they cross-reference each other.

Output: Complete `audit/v3.1-findings-34-token-contracts.md` with all 4 contract sections and finalized summary table.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-token-contracts/34-RESEARCH.md
@audit/v3.1-findings-34-token-contracts.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comment audit and intent drift review for DegenerusStonk.sol + StakedDegenerusStonk.sol</name>
  <files>audit/v3.1-findings-34-token-contracts.md</files>
  <read_first>
    contracts/DegenerusStonk.sol
    contracts/StakedDegenerusStonk.sol
    audit/v3.1-findings-34-token-contracts.md
    .planning/phases/34-token-contracts/34-RESEARCH.md
  </read_first>
  <action>
Review both the DGNRS/sDGNRS pair together since they cross-reference each other. Total: DegenerusStonk.sol (223 lines, 41 NatSpec tags, ~80 comment lines, 14 functions) + StakedDegenerusStonk.sol (514 lines, 107 NatSpec tags, ~168 comment lines, 23 functions) = 737 lines, 148 NatSpec tags, 37 functions.

**Part A: DegenerusStonk.sol (223 lines)**

**Step A1: Verify post-Phase-29 change (fd9dbad1)**

Run `git diff 9238faf2..HEAD -- contracts/DegenerusStonk.sol` to see exact changes. Verify:
- `unwrapTo` NatSpec at line ~146 now says "(>5h)" instead of "(>20h)"
- Inline comment at line ~150 also updated to reference 5 hours
- Code at line ~151: `> 5 hours` matches NatSpec
- Run `grep -in '20.*hour\|20h' contracts/DegenerusStonk.sol` to confirm no stale "20 hour" references remain

**Step A2: Audit contract-level header**

Read the contract-level @dev block. Verify:
- Description of DGNRS as "transferable ERC20 wrapper around sDGNRS" is accurate
- Description of wrap/unwrap mechanics matches actual functions
- Cross-reference to StakedDegenerusStonk contract is accurate

**Step A3: Audit all function NatSpec**

For each of the 14 functions, verify @notice/@dev/@param/@return:
- `wrap` / `unwrap` / `unwrapTo` -- verify wrap/unwrap mechanics, VRF stall guard description on unwrapTo
- `_transfer` -- verify the self-transfer block (`address(this)`) has NatSpec explaining why; if missing, flag
- `burn` / `burnThrough` -- verify burn-through to sDGNRS delegation is described
- View functions (name, symbol, decimals, totalSupply, balanceOf) -- verify override descriptions
- Any other functions

**Step A4: Audit all inline comments**

Read every `//` comment. Check for:
- Stale references to old VRF stall threshold ("20 hours")
- Cross-contract references to sDGNRS that may be inaccurate
- Arithmetic descriptions that don't match actual formulas

**Step A5: Intent drift review for DegenerusStonk.sol**

- Check if `_transfer` blocking transfers to `address(this)` has NatSpec explaining why (likely prevents accidental token lock)
- Check for any unnecessary restrictions or vestigial guards
- Check for references to sDGNRS behavior that may have changed

**Step A6: Write DegenerusStonk.sol findings section**

Add `## DegenerusStonk.sol` section after the BurnieCoin section. Include:

```markdown
**Review scope:** 223 lines, 41 NatSpec tags, ~80 comment lines, 14 functions.
**Post-Phase-29 changes:** 1 commit (fd9dbad1) -- changed VRF stall threshold from 20h to 5h for `unwrapTo` blocking. NatSpec updated in same commit.
```

Write findings with CMT/DRIFT numbering continuing sequentially from BurnieCoin section. Every finding needs What/Where/Why/Suggestion/Category/Severity.

Add review completion marker:

```markdown
---

**DegenerusStonk.sol review complete.** 41 NatSpec tags verified, ~80 comment lines reviewed. [N] findings total. Post-Phase-29 commit fd9dbad1 (VRF stall 20h->5h) independently verified [clean/with issues].
```

**Part B: StakedDegenerusStonk.sol (514 lines)**

**Step B1: Verify no post-Phase-29 changes**

Run `git diff 9238faf2..HEAD -- contracts/StakedDegenerusStonk.sol` to confirm empty diff.

**Step B2: Audit sDGNRS/DGNRS naming in pool functions (pre-identified issue)**

Verify the naming inconsistency:
- Run `grep -n 'DGNRS' contracts/StakedDegenerusStonk.sol` to list all DGNRS references
- `transferFromPool` NatSpec at line ~300: confirm it says "Transfer DGNRS" -- should be "Transfer sDGNRS"
- `@param amount` at line ~304: confirm it says "DGNRS" -- should be "sDGNRS"
- `transferBetweenPools` at line ~327: confirm it says "Transfer DGNRS between two reward pools" -- should be "sDGNRS"
- Flag all 3 instances (or group as one finding if they share the same root cause)

**Step B3: Audit contract-level header**

Read the contract-level @dev block. Verify:
- Description of sDGNRS as "soulbound token backed by ETH + stETH + BURNIE reserves" is accurate
- Pre-minted pool mechanics description matches actual implementation
- Cross-references to DegenerusStonk (DGNRS), BurnieCoin (COIN), and other contracts are accurate

**Step B4: Audit all function NatSpec**

For each of the 23 functions, verify @notice/@dev/@param/@return:
- Pool management: `transferFromPool`, `transferBetweenPools`, pool view functions
- Burn/payout: `burn` with proportional payout description, ETH-preferred payout order
- Minting: verify mint access control descriptions
- Coinflip claim on burn: verify description of auto-claim pattern
- View functions: verify return value descriptions

**Step B5: Audit all inline comments**

Read every `//` comment. Check for:
- sDGNRS/DGNRS naming confusion beyond the 3 pre-identified instances
- Cross-contract references to DGNRS, BURNIE, stETH that may be inaccurate
- Arithmetic descriptions for proportional burn calculations
- Storage layout annotations

**Step B6: Intent drift review for StakedDegenerusStonk.sol**

- Soulbound enforcement: verify NatSpec correctly describes that sDGNRS cannot be transferred (only burned)
- Pre-minted pools: verify NatSpec describes the pool allocation pattern accurately
- Any vestigial references to pre-split DGNRS behavior
- Any guards or conditions that may be unnecessary

**Step B7: Write StakedDegenerusStonk.sol findings section**

Add `## StakedDegenerusStonk.sol` section. Include:

```markdown
**Review scope:** 514 lines, 107 NatSpec tags, ~168 comment lines, 23 functions.
**Post-Phase-29 changes:** None -- contract unchanged since Phase 29 (commit 9238faf2).
```

Write findings continuing CMT/DRIFT numbering. Every finding needs What/Where/Why/Suggestion/Category/Severity.

Add review completion marker:

```markdown
---

**StakedDegenerusStonk.sol review complete.** 107 NatSpec tags verified, ~168 comment lines reviewed. [N] findings total. sDGNRS/DGNRS naming inconsistency in pool function NatSpec confirmed and flagged.
```
  </action>
  <verify>
    <automated>grep 'DegenerusStonk.sol review complete' audit/v3.1-findings-34-token-contracts.md; grep 'StakedDegenerusStonk.sol review complete' audit/v3.1-findings-34-token-contracts.md; grep -c '### CMT-\|### DRIFT-' audit/v3.1-findings-34-token-contracts.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.1-findings-34-token-contracts.md contains `## DegenerusStonk.sol`
    - audit/v3.1-findings-34-token-contracts.md contains `## StakedDegenerusStonk.sol`
    - audit/v3.1-findings-34-token-contracts.md contains "DegenerusStonk.sol review complete"
    - audit/v3.1-findings-34-token-contracts.md contains "StakedDegenerusStonk.sol review complete"
    - `grep -in '20.*hour\|20h' contracts/DegenerusStonk.sol` returns empty (confirming fd9dbad1 NatSpec update clean) or any stale references are flagged
    - sDGNRS/DGNRS naming inconsistency in StakedDegenerusStonk.sol pool NatSpec is flagged (lines ~300, ~304, ~327)
    - Every finding has What, Where, Why, Suggestion, Category, and Severity fields
    - CMT/DRIFT numbering is sequential with no gaps or collisions from BurnieCoin section
    - No .sol files modified (flag-only)
  </acceptance_criteria>
  <done>DegenerusStonk.sol (223 lines) and StakedDegenerusStonk.sol (514 lines) fully reviewed for comment accuracy and intent drift. Post-Phase-29 VRF stall threshold change independently verified. sDGNRS/DGNRS naming inconsistency in pool NatSpec flagged. Cross-references between DGNRS/sDGNRS pair verified.</done>
</task>

<task type="auto">
  <name>Task 2: Comment audit and intent drift review for WrappedWrappedXRP.sol + finalize Phase 34 findings</name>
  <files>audit/v3.1-findings-34-token-contracts.md</files>
  <read_first>
    contracts/WrappedWrappedXRP.sol
    audit/v3.1-findings-34-token-contracts.md
    .planning/phases/34-token-contracts/34-RESEARCH.md
  </read_first>
  <action>
Review WrappedWrappedXRP.sol (389 lines, 111 NatSpec tags, ~141 comment lines, 16 functions) and finalize the Phase 34 findings file.

**Step 1: Verify no post-Phase-29 changes**

Run `git diff 9238faf2..HEAD -- contracts/WrappedWrappedXRP.sol` to confirm empty diff.

**Step 2: Audit contract-level header**

Read the contract-level @dev block. Verify:
- Description of WWXRP as a "joke ERC20 that may or may not be backed by wXRP" is accurate
- Any cross-references to WXRP, GAME, COIN, COINFLIP, VAULT are accurate
- Unbacked mintPrize and first-come-first-served unwrap patterns are described

**Step 3: Audit all function NatSpec**

For each of the 16 functions, verify @notice/@dev/@param/@return:
- `mintPrize` -- verify NatSpec describes unbacked minting pattern
- `unwrap` -- verify first-come-first-served description, any supply constraints
- `wrap` -- verify wrapping from WXRP is described
- Vault integration: verify vault mint allowance pattern matches BurnieCoin pattern
- Coinflip integration: verify any coinflip-related functions
- View functions: verify return value descriptions
- ERC20 overrides: verify override descriptions

**Step 4: Audit all inline comments**

Read every `//` and `/* */` comment. Check for:
- Cross-contract references to VAULT, GAME, COINFLIP that may be inaccurate
- Arithmetic descriptions for wrap/unwrap calculations
- Any humor/joke comments that may mask actual technical details

**Step 5: Intent drift review for WrappedWrappedXRP.sol**

- Check for any vestigial references to features or patterns that were changed
- Check for unnecessary restrictions or guards
- Verify the unbacked mint pattern is accurately documented (wardens need to understand this is BY DESIGN)
- Check that the first-come-first-served unwrap pattern is clearly documented as a design choice

**Step 6: Write WrappedWrappedXRP.sol findings section**

Add `## WrappedWrappedXRP.sol` section. Include:

```markdown
**Review scope:** 389 lines, 111 NatSpec tags, ~141 comment lines, 16 functions.
**Post-Phase-29 changes:** None -- contract unchanged since Phase 29 (commit 9238faf2).
```

Write findings continuing CMT/DRIFT numbering. Every finding needs What/Where/Why/Suggestion/Category/Severity.

Add review completion marker:

```markdown
---

**WrappedWrappedXRP.sol review complete.** 111 NatSpec tags verified, ~141 comment lines reviewed. [N] findings total.
```

**Step 7: Finalize summary table**

Update the Summary table at the top of the findings file with actual integer counts for ALL 4 contracts:

```markdown
| Contract | CMT findings | DRIFT findings | Total |
|----------|-------------|----------------|-------|
| BurnieCoin.sol | [actual count] | [actual count] | [actual total] |
| StakedDegenerusStonk.sol | [actual count] | [actual count] | [actual total] |
| DegenerusStonk.sol | [actual count] | [actual count] | [actual total] |
| WrappedWrappedXRP.sol | [actual count] | [actual count] | [actual total] |
| **Total** | **[sum]** | **[sum]** | **[sum]** |
```

Remove the `*Summary counts updated after all contracts reviewed.*` placeholder.

**Step 8: Cross-check**

Verify:
- Total CMT count = count of `### CMT-` headings in the file
- Total DRIFT count = count of `### DRIFT-` headings in the file
- Every contract section has a "review complete" marker
- CMT numbering is sequential with no gaps: CMT-041, CMT-042, ... CMT-NNN
- DRIFT numbering is sequential: DRIFT-004, DRIFT-005, ... (if any)
- No finding has a Where line referencing a contract not in Phase 34 scope

Run: `grep -c '### CMT-' audit/v3.1-findings-34-token-contracts.md` and `grep -c '### DRIFT-' audit/v3.1-findings-34-token-contracts.md` and verify counts match summary table.
  </action>
  <verify>
    <automated>grep 'WrappedWrappedXRP.sol review complete' audit/v3.1-findings-34-token-contracts.md; grep -c '### CMT-\|### DRIFT-' audit/v3.1-findings-34-token-contracts.md; grep '**Total**' audit/v3.1-findings-34-token-contracts.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.1-findings-34-token-contracts.md contains `## WrappedWrappedXRP.sol`
    - audit/v3.1-findings-34-token-contracts.md contains "WrappedWrappedXRP.sol review complete"
    - Summary table has actual integer counts for all 4 contracts (no X/Y/Z placeholders)
    - Total row sums match individual row sums
    - `grep -c '### CMT-'` count matches the CMT total in the summary table
    - `grep -c '### DRIFT-'` count matches the DRIFT total in the summary table
    - CMT numbering is sequential starting from CMT-041 with no gaps
    - DRIFT numbering is sequential starting from DRIFT-004 (if any DRIFT findings exist)
    - Every finding has What, Where, Why, Suggestion, Category, and Severity fields
    - All 4 contract sections have "review complete" markers
    - No .sol files modified (flag-only)
  </acceptance_criteria>
  <done>WrappedWrappedXRP.sol (389 lines, 111 NatSpec tags, ~141 comment lines, 16 functions) fully reviewed for comment accuracy and intent drift. Phase 34 findings file finalized: all 4 contracts reviewed, summary table updated with actual counts, cross-checked for sequential numbering and accurate totals. Phase 34 complete.</done>
</task>

</tasks>

<verification>
- audit/v3.1-findings-34-token-contracts.md has sections for all 4 contracts: BurnieCoin.sol, DegenerusStonk.sol, StakedDegenerusStonk.sol, WrappedWrappedXRP.sol
- All 4 sections have "review complete" markers
- Summary table has actual integer counts with correct totals
- CMT numbering starts at CMT-041 and is sequential with no gaps
- DRIFT numbering starts at DRIFT-004 (if any) and is sequential
- Every finding has what/where/why/suggestion/category/severity format
- Pre-identified issues are all flagged: orphaned DATA TYPES, orphaned BOUNTY STATE, OnlyGame reuse, burnCoin redundancy, sDGNRS/DGNRS naming
- Post-Phase-29 DegenerusStonk.sol change independently verified
- No .sol files in contracts/ directory were modified
</verification>

<success_criteria>
- DegenerusStonk.sol, StakedDegenerusStonk.sol, and WrappedWrappedXRP.sol fully reviewed for comment accuracy and intent drift
- All 259 NatSpec tags (148+111) and ~389 comment lines (248+141) verified across 3 contracts
- Post-Phase-29 VRF stall threshold change independently verified
- sDGNRS/DGNRS naming inconsistency formally flagged
- Phase 34 findings file complete with finalized summary
- Every finding has what/why/suggestion per item
- No code changes made to any contract files
</success_criteria>

<output>
After completion, create `.planning/phases/34-token-contracts/34-02-SUMMARY.md`
</output>
