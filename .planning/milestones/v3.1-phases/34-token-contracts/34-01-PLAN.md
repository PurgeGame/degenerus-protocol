---
phase: 34-token-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v3.1-findings-34-token-contracts.md
autonomous: true
requirements: [CMT-04, DRIFT-04]

must_haves:
  truths:
    - "Every NatSpec tag in BurnieCoin.sol is verified against current code behavior"
    - "Every inline comment in BurnieCoin.sol is verified against current logic"
    - "Orphaned DATA TYPES section (lines 213-227) documenting structs that live in BurnieCoinflip is flagged"
    - "Orphaned BOUNTY STATE section (lines 333-361) documenting variables that live in BurnieCoinflip is flagged"
    - "OnlyGame() error reuse in onlyAdmin modifier (line 663) is flagged as undocumented"
    - "burnCoin @dev redundant 'DegenerusGame, game, or affiliate' wording is flagged"
    - "Any vestigial references from coinflip split are flagged"
    - "A per-batch findings file exists with what/why/suggestion for every finding"
  artifacts:
    - path: "audit/v3.1-findings-34-token-contracts.md"
      provides: "Per-batch findings file for Phase 34 with BurnieCoin section"
      contains: "## BurnieCoin.sol"
  key_links:
    - from: "audit/v3.1-findings-34-token-contracts.md"
      to: "contracts/BurnieCoin.sol"
      via: "File:Line citations for every finding"
      pattern: "BurnieCoin\\.sol:\\d+"
---

<objective>
Create the Phase 34 findings file and perform comment audit + intent drift review for BurnieCoin.sol (1,065 lines, 215 NatSpec tags, ~270 comment lines, 44 functions).

Purpose: BurnieCoin is the largest token contract with the most complex review surface -- decimator integration, quest routing, vault escrow, coinflip proxy, and notably orphaned NatSpec blocks from the coinflip split. Phase 29 verified all 44 functions as MATCH at function-level NatSpec, but did NOT audit block comments, section headers, storage layout documentation, or orphaned NatSpec patterns. The v3.1 pass applies deeper scrutiny. The research pre-identified 4 issues that need formal verification and there may be additional findings.

Output: `audit/v3.1-findings-34-token-contracts.md` with file header, summary table template, and BurnieCoin.sol findings section. CMT numbering starts at CMT-041, DRIFT numbering starts at DRIFT-004 (continuing from Phase 33 which ended at CMT-040, DRIFT-003).
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-token-contracts/34-RESEARCH.md
@audit/v3.1-findings-33-game-modules-batch-b.md (first 60 lines -- format reference and last finding numbers)
@audit/v3.1-findings-32-game-modules-batch-a.md (format reference only)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comment audit and intent drift review for BurnieCoin.sol (header, state, modifiers, ERC20, vault, coinflip proxy)</name>
  <files>audit/v3.1-findings-34-token-contracts.md</files>
  <read_first>
    contracts/BurnieCoin.sol
    contracts/BurnieCoinflip.sol
    .planning/phases/34-token-contracts/34-RESEARCH.md
    audit/v3.1-findings-33-game-modules-batch-b.md
    audit/v3.1-findings-32-game-modules-batch-a.md
  </read_first>
  <action>
Create the Phase 34 findings file and perform the first half of the BurnieCoin.sol review, covering: contract-level block comments, DATA TYPES section, ERC20 STATE section, BOUNTY STATE section, CONSTANTS, ERRORS/EVENTS, MODIFIERS, ERC20 overrides (transfer, transferFrom, balanceOf, totalSupply), vault escrow functions (mintForVault, burnFromVault, vaultMintAllowance), and coinflip proxy functions (burnForCoinflip, coinflipAutoRebuyInfo, setCoinflipAutoRebuy, _claimCoinflipShortfall, _consumeCoinflipShortfall).

**Step 1: Create findings file with header**

Create `audit/v3.1-findings-34-token-contracts.md` with this exact template:

```markdown
# Phase 34 Findings: Token Contracts

**Date:** 2026-03-19
**Scope:** BurnieCoin, StakedDegenerusStonk, DegenerusStonk, WrappedWrappedXRP
**Pass:** v3.1 second independent review (v3.0 Phase 29 was first pass)
**Mode:** Flag-only -- no code changes

## Summary

| Contract | CMT findings | DRIFT findings | Total |
|----------|-------------|----------------|-------|
| BurnieCoin.sol | X | Y | Z |
| StakedDegenerusStonk.sol | X | Y | Z |
| DegenerusStonk.sol | X | Y | Z |
| WrappedWrappedXRP.sol | X | Y | Z |
| **Total** | **X** | **Y** | **Z** |

*Summary counts updated after all contracts reviewed.*
```

Then add `---` separator and `## BurnieCoin.sol` section header with:

```markdown
**Review scope:** 1,065 lines, 215 NatSpec tags, ~270 comment lines, 44 functions.
**Post-Phase-29 changes:** None -- contract unchanged since Phase 29 (commit 9238faf2).
```

**Step 2: Verify BurnieCoin.sol has no post-Phase-29 changes**

Run `git diff 9238faf2..HEAD -- contracts/BurnieCoin.sol` to confirm empty diff.

**Step 3: Audit orphaned DATA TYPES section (lines 213-227)**

This is a pre-identified issue. Verify:
- Run `grep -n 'struct\b' contracts/BurnieCoin.sol` -- should return only `struct Supply` at line ~196
- The DATA TYPES section header (lines ~213-227) documents a "Leaderboard entry" struct (lines 220-222) and an "Outcome record" struct (lines 224-226) but NO struct declarations follow
- Verify these structs exist in BurnieCoinflip.sol: `grep -n 'struct\b' contracts/BurnieCoinflip.sol`
- Flag as CMT-041 with suggestion to remove section (BurnieCoinflip.sol has its own documentation)

**Step 4: Audit orphaned BOUNTY STATE section (lines 333-361)**

Pre-identified issue. Verify:
- Run `grep -n 'currentBounty\|biggestFlipEver\|bountyOwedTo' contracts/BurnieCoin.sol` -- should return only comment references, no variable declarations
- Verify these variables exist in BurnieCoinflip.sol: `grep -n 'currentBounty\|biggestFlipEver\|bountyOwedTo' contracts/BurnieCoinflip.sol` -- should show declarations at lines ~161-163
- The STORAGE LAYOUT table references slots 17-18 which are incorrect for BurnieCoin's storage
- Flag as CMT-042 with suggestion to remove entire section

**Step 5: Audit onlyAdmin modifier OnlyGame() error reuse (line 663)**

Pre-identified issue. Verify:
- Read the `onlyAdmin` modifier at line ~663 -- confirm it reverts with `OnlyGame()` not `OnlyAdmin()` or similar
- Read the `burnForCoinflip` guard at line ~519 -- confirm it has inline comment "Reusing error for simplicity"
- The `onlyAdmin` modifier has NO such acknowledgment comment
- Flag as CMT-043 with suggestion to add inline comment acknowledging the deliberate error reuse (matching the coinflip pattern at line 519)

**Step 6: Audit burnCoin @dev redundancy (line 854)**

Pre-identified issue. Verify:
- Read the `burnCoin` @dev NatSpec at line ~854 -- confirm it says "DegenerusGame, game, or affiliate"
- Verify what `onlyTrustedContracts` modifier checks (should be GAME address and AFFILIATE address)
- "DegenerusGame" and "game" are the same thing -- flag as CMT-044 with suggestion to change to "DegenerusGame or affiliate"

**Step 7: Audit contract-level header block comments**

Read the contract-level @dev block and any architecture comments at the top of the file. Verify:
- Token name, symbol, decimals descriptions match actual values
- Any description of the coinflip split or integration with BurnieCoinflip matches current architecture
- Cross-contract references (GAME, QUESTS, COINFLIP, VAULT, AFFILIATE, ADMIN) are accurate

**Step 8: Audit ERC20 STATE section and Supply struct**

Verify:
- The `Supply` struct NatSpec at line ~196 matches the struct fields
- ERC20 state variable NatSpec (totalMinted, totalBurned, etc.) matches actual usage
- Storage slot annotations (if any) are correct

**Step 9: Audit CONSTANTS, ERRORS, EVENTS NatSpec**

For each constant:
- Check value matches description
- Check BPS/percentage annotations are correct

For each error/event:
- Check @param descriptions match actual parameters
- Check descriptions match actual usage in the contract

**Step 10: Audit MODIFIERS**

Review all modifier NatSpec:
- `onlyGame` -- verify description and access control
- `onlyAdmin` -- verify description (already flagging OnlyGame() reuse in Step 5)
- `onlyTrustedContracts` -- verify which addresses are considered "trusted"
- Any other modifiers

**Step 11: Audit ERC20 override functions**

Review transfer, transferFrom, balanceOf, totalSupply, and any other ERC20 overrides:
- Verify NatSpec describes the override behavior (e.g., vault virtual balance, coinflip shortfall auto-claim)
- Verify inline comments explaining why standard ERC20 behavior is modified
- Verify cross-references to vault escrow and coinflip shortfall patterns

**Step 12: Audit vault escrow functions**

Review mintForVault, burnFromVault, vaultMintAllowance:
- Verify NatSpec describes the virtual mint allowance pattern
- Verify cross-references to DegenerusVault contract

**Step 13: Audit coinflip proxy functions**

Review burnForCoinflip, coinflipAutoRebuyInfo, setCoinflipAutoRebuy, _claimCoinflipShortfall, _consumeCoinflipShortfall:
- Verify NatSpec matches actual behavior
- Note that _claimCoinflipShortfall (line ~580) and _consumeCoinflipShortfall (line ~593) are private helpers with NO NatSpec per research -- flag if behavior is non-obvious and wardens would benefit from documentation
- Verify the coinflipAutoRebuyInfo return value description (returns 3 values but underlying IBurnieCoinflip returns 4, discarding startDay)

**Step 14: Write findings to file**

Write all findings found in Steps 3-13 to the BurnieCoin.sol section. Every finding must have:
- `### CMT-0NN:` or `### DRIFT-0NN:` header with brief title
- `**What:**` the specific comment inaccuracy or intent drift
- `**Where:**` BurnieCoin.sol:LINE
- `**Why:**` why a C4A warden would be misled
- `**Suggestion:**` recommended fix
- `**Category:**` comment-inaccuracy or intent-drift
- `**Severity:**` INFO or LOW

CMT numbering starts at CMT-041. DRIFT numbering starts at DRIFT-004.
  </action>
  <verify>
    <automated>grep -c '### CMT-\|### DRIFT-' audit/v3.1-findings-34-token-contracts.md; grep 'BurnieCoin.sol' audit/v3.1-findings-34-token-contracts.md | head -3</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.1-findings-34-token-contracts.md exists
    - File contains `# Phase 34 Findings: Token Contracts`
    - File contains `## BurnieCoin.sol`
    - File contains `**Review scope:** 1,065 lines`
    - CMT numbering starts at CMT-041 (continuing from Phase 33 CMT-040)
    - At minimum CMT-041 (orphaned DATA TYPES), CMT-042 (orphaned BOUNTY STATE), CMT-043 (OnlyGame reuse), CMT-044 (burnCoin redundancy) are present as pre-identified issues
    - Every finding has What, Where, Why, Suggestion, Category, and Severity fields
    - No finding has Category other than `comment-inaccuracy` or `intent-drift`
    - No finding has Severity other than `INFO` or `LOW`
    - `grep -n 'struct\b' contracts/BurnieCoin.sol` confirms only Supply struct (validates orphaned DATA TYPES finding)
    - `grep -n 'currentBounty\|biggestFlipEver\|bountyOwedTo' contracts/BurnieCoin.sol` returns only comment references (validates orphaned BOUNTY STATE finding)
    - No .sol files modified (flag-only)
  </acceptance_criteria>
  <done>Phase 34 findings file created with header and summary table. BurnieCoin.sol first half reviewed: header block comments, DATA TYPES orphaned section, ERC20 STATE, BOUNTY STATE orphaned section, CONSTANTS, ERRORS/EVENTS, MODIFIERS, ERC20 overrides, vault escrow, and coinflip proxy functions. Pre-identified issues (orphaned NatSpec blocks, OnlyGame reuse, burnCoin redundancy) formally verified and flagged.</done>
</task>

<task type="auto">
  <name>Task 2: Comment audit and intent drift review for BurnieCoin.sol (game functions, quest routing, decimator, views, intent drift) + finalize BurnieCoin section</name>
  <files>audit/v3.1-findings-34-token-contracts.md</files>
  <read_first>
    contracts/BurnieCoin.sol
    audit/v3.1-findings-34-token-contracts.md
    .planning/phases/34-token-contracts/34-RESEARCH.md
  </read_first>
  <action>
Complete the second half of the BurnieCoin.sol review, covering: game-integration functions (mintCoin, burnCoin, decimatorBurn), quest routing hub functions (processQuestReward, routeQuestBurn), affiliate functions, view/getter functions, and full intent drift scan.

**Step 1: Audit game-integration functions**

Review mintCoin, burnCoin, decimatorBurn:
- Verify @notice/@dev/@param/@return for each function
- burnCoin: already flagged in Task 1 for "@dev says DegenerusGame, game, or affiliate" redundancy; now also verify the full function NatSpec for any other issues
- decimatorBurn: verify the burn mechanics description matches actual behavior (decimator calls this to burn tokens)
- mintCoin: verify minting access control description and any mint cap references

**Step 2: Audit quest routing hub functions**

BurnieCoin serves as a routing hub for quest rewards. Review processQuestReward, routeQuestBurn, and any related functions:
- Verify NatSpec describes the routing pattern accurately
- Verify cross-references to DegenerusQuests contract
- Verify reward type handling descriptions

**Step 3: Audit affiliate functions**

Review any affiliate-related functions:
- Verify NatSpec for affiliate reward/burn functions
- Verify cross-references to DegenerusAffiliate contract

**Step 4: Audit all view/getter functions**

Review every view function:
- Verify @return descriptions match actual return values
- Verify any computed values are described correctly
- Check for stale descriptions of removed state (coinflip split leftovers)

**Step 5: Audit all remaining inline comments**

Read every remaining `//` and `/* */` comment not covered in Task 1. Check for:
- Stale references to BurnieCoinflip state or functions that were split out
- Arithmetic descriptions that don't match actual formulas
- Cross-contract references that may be misleading
- Section header accuracy (verify each section actually contains what the header claims)

**Step 6: Full intent drift review**

Scan the entire BurnieCoin.sol for:
- Vestigial references from the coinflip split (structs, variables, functions that moved to BurnieCoinflip.sol)
- Guards or conditions that may have become unnecessary after the split
- Access control patterns that don't match their documentation
- The `onlyAdmin` modifier using `OnlyGame()` error (already flagged in Task 1) -- but scan for any OTHER instances of error reuse
- Storage slot annotations that reference incorrect slot numbers
- Any function that delegates to BurnieCoinflip but whose NatSpec doesn't explain the delegation

**Step 7: Write remaining BurnieCoin findings and finalize section**

Append any additional findings to the BurnieCoin.sol section. Continue CMT/DRIFT numbering sequentially from Task 1's last number.

Update the Summary table BurnieCoin.sol row with actual counts.

Add review completion marker:

```markdown
---

**BurnieCoin.sol review complete.** 215 NatSpec tags verified, ~270 comment lines reviewed, all inline comments cross-referenced against current code behavior. [N] findings total ([X] CMT, [Y] DRIFT). Contract unchanged since Phase 29 -- orphaned NatSpec from coinflip split is the primary finding pattern.
```
  </action>
  <verify>
    <automated>grep 'BurnieCoin.sol review complete' audit/v3.1-findings-34-token-contracts.md; grep -c '### CMT-\|### DRIFT-' audit/v3.1-findings-34-token-contracts.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.1-findings-34-token-contracts.md contains "BurnieCoin.sol review complete"
    - Summary table row for BurnieCoin.sol has actual integer counts (not X/Y/Z placeholders)
    - All 215 NatSpec tags in BurnieCoin.sol are accounted for (either verified clean or flagged)
    - Every finding has What, Where, Why, Suggestion, Category, and Severity fields
    - Intent drift scan completed -- any vestigial coinflip split references flagged
    - No .sol files modified (flag-only)
  </acceptance_criteria>
  <done>BurnieCoin.sol (1,065 lines, 215 NatSpec tags, ~270 comment lines, 44 functions) fully reviewed for comment accuracy and intent drift. Game-integration, quest routing, affiliate, and view functions reviewed. Full intent drift scan complete. Review summary written. Summary table updated with actual counts.</done>
</task>

</tasks>

<verification>
- audit/v3.1-findings-34-token-contracts.md exists with header and summary table
- grep 'BurnieCoin.sol' returns results
- grep 'BurnieCoin.sol review complete' returns results
- CMT numbering starts at CMT-041 (continuing from Phase 33 CMT-040)
- DRIFT numbering starts at DRIFT-004 (continuing from Phase 33 DRIFT-003)
- Every finding has what/where/why/suggestion/category/severity format
- BurnieCoin.sol summary row has actual integer counts
- No .sol files in contracts/ directory were modified
- Pre-identified issues (orphaned DATA TYPES, orphaned BOUNTY STATE, OnlyGame reuse, burnCoin redundancy) are all flagged
</verification>

<success_criteria>
- BurnieCoin.sol fully reviewed for comment accuracy and intent drift
- All 215 NatSpec tags and ~270 comment lines verified
- Orphaned NatSpec blocks from coinflip split formally flagged
- Per-batch findings file created with proper format continuing Phase 33 numbering
- Every finding has what/why/suggestion per item
- No code changes made to any contract files
</success_criteria>

<output>
After completion, create `.planning/phases/34-token-contracts/34-01-SUMMARY.md`
</output>
