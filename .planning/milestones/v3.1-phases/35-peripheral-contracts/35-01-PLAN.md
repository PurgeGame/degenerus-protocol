---
phase: 35-peripheral-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v3.1-findings-35-peripheral-contracts.md
autonomous: true
requirements: [CMT-05, DRIFT-05]

must_haves:
  truths:
    - "Every NatSpec tag in BurnieCoinflip.sol is verified against current code behavior"
    - "Every inline comment in BurnieCoinflip.sol is verified against current logic"
    - "OnlyBurnieCoin() error reuse in _resolvePlayer (line 1142) is flagged"
    - "depositCoinflip sparse NatSpec (line 225) is evaluated and flagged if warranted"
    - "Any vestigial references from the BurnieCoin/BurnieCoinflip split are flagged"
    - "A per-batch findings file exists with header, summary table, and BurnieCoinflip section"
  artifacts:
    - path: "audit/v3.1-findings-35-peripheral-contracts.md"
      provides: "Per-batch findings file for Phase 35 with BurnieCoinflip section"
      contains: "## BurnieCoinflip.sol"
  key_links:
    - from: "audit/v3.1-findings-35-peripheral-contracts.md"
      to: "contracts/BurnieCoinflip.sol"
      via: "File:Line citations for every finding"
      pattern: "BurnieCoinflip\\.sol:\\d+"
---

<objective>
Create the Phase 35 findings file and perform comment audit + intent drift review for BurnieCoinflip.sol (1,154 lines, 62 NatSpec tags, ~152 comment lines, 37 functions).

Purpose: BurnieCoinflip is the largest peripheral contract with the coinflip split history from BurnieCoin.sol. Phase 34 found 5 orphaned NatSpec findings in BurnieCoin tracing to the split -- BurnieCoinflip needs checking for the inverse (any stale references to BurnieCoin state). The research pre-identified the OnlyBurnieCoin() error reuse pattern at line 1142 and sparse NatSpec on depositCoinflip. None of the 10 peripheral contracts were modified after Phase 29 (commit 9238faf2).

Output: `audit/v3.1-findings-35-peripheral-contracts.md` with file header, summary table template, and BurnieCoinflip.sol findings section. CMT numbering starts at CMT-059, DRIFT numbering starts at DRIFT-004 (continuing from Phase 34 which ended at CMT-058, DRIFT-003).
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-peripheral-contracts/35-RESEARCH.md
@audit/v3.1-findings-34-token-contracts.md (first 60 lines -- format reference and last finding numbers)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comment audit and intent drift review for BurnieCoinflip.sol (first half: header through modifiers and core coinflip functions, lines 1-600)</name>
  <files>audit/v3.1-findings-35-peripheral-contracts.md</files>
  <read_first>
    contracts/BurnieCoinflip.sol
    .planning/phases/35-peripheral-contracts/35-RESEARCH.md
    audit/v3.1-findings-34-token-contracts.md
  </read_first>
  <action>
Create the Phase 35 findings file and perform the first half of the BurnieCoinflip.sol review, covering lines 1-600: contract-level block comments (lines 4-20), EVENTS section (lines 37-91), CUSTOM ERRORS (lines 92-106), STORAGE VARIABLES section (lines 108-173 including immutables, constants, structs CoinflipDayResult/PlayerCoinflipState/PlayerScore, mappings, bounty system, RNG state, leaderboard), CONSTRUCTOR (lines 175-184), MODIFIERS (lines 186-206: onlyDegenerusGameContract, onlyFlipCreditors, onlyBurnieCoin), and CORE COINFLIP FUNCTIONS section (lines 208-600: depositCoinflip, claimCoinflips, claimCoinflipsTakeProfit, previewClaimCoinflips, setCoinflipAutoRebuy, setCoinflipAutoRebuyTakeProfit, and related internal helpers).

**Step 1: Create findings file with header**

Create `audit/v3.1-findings-35-peripheral-contracts.md` with this exact template:

```markdown
# Phase 35 Findings: Peripheral Contracts

**Date:** 2026-03-19
**Scope:** BurnieCoinflip, DegenerusAffiliate, DegenerusDeityPass, DegenerusQuests, DegenerusJackpots, DegenerusVault, DegenerusTraitUtils, DeityBoonViewer, ContractAddresses, Icons32Data
**Pass:** v3.1 second independent review (v3.0 Phase 29 was first pass)
**Mode:** Flag-only -- no code changes

## Summary

| Contract | CMT findings | DRIFT findings | Total |
|----------|-------------|----------------|-------|
| BurnieCoinflip.sol | X | Y | Z |
| DegenerusQuests.sol | X | Y | Z |
| DegenerusJackpots.sol | X | Y | Z |
| DegenerusAffiliate.sol | X | Y | Z |
| DegenerusVault.sol | X | Y | Z |
| DegenerusDeityPass.sol | X | Y | Z |
| DegenerusTraitUtils.sol | X | Y | Z |
| DeityBoonViewer.sol | X | Y | Z |
| ContractAddresses.sol | X | Y | Z |
| Icons32Data.sol | X | Y | Z |
| **Total** | **X** | **Y** | **Z** |

*Summary counts updated after all contracts reviewed.*
```

Then add `---` separator and `## BurnieCoinflip.sol` section header with:

```markdown
**Review scope:** 1,154 lines, 62 NatSpec tags, ~152 comment lines, 37 functions.
**Post-Phase-29 changes:** None -- contract unchanged since Phase 29 (commit 9238faf2).
```

**Step 2: Verify BurnieCoinflip.sol has no post-Phase-29 changes**

Run `git diff 9238faf2..HEAD -- contracts/BurnieCoinflip.sol` to confirm empty diff.

**Step 3: Audit contract-level header block (lines 4-20)**

Read the @title, @author, @notice, @dev ARCHITECTURE, @dev INTERACTIONS block. Verify:
- "Extracted from BurnieCoin to reduce contract size" is accurate
- "Manages daily coinflip system with optional afKing mode bonuses" matches actual functionality
- "Burns BURNIE from players on deposit (via BurnieCoin.burnForCoinflip)" -- verify BurnieCoinflip calls `burnie.burnForCoinflip()` in depositCoinflip
- "Mints BURNIE to players on claim (via BurnieCoin.mintForCoinflip)" -- verify calls `burnie.mintForCoinflip()` in claim functions
- "Receives quest flip credits from game contract" -- verify onlyFlipCreditors allows GAME
- "Processes RNG results for payout calculations" -- verify processCoinflipPayouts exists

**Step 4: Audit EVENTS section (lines 37-91)**

For each event:
- CoinflipDeposit, CoinflipAutoRebuyToggled, CoinflipAutoRebuyStopSet (lines 41-43): verify no NatSpec (only event declarations) -- flag if complex events lack NatSpec
- CoinflipStakeUpdated (lines 50-60): verify @notice/@param against actual emit sites
- CoinflipDayResolved (lines 61-75): verify @notice/@param against actual emit sites, especially bountyPaid/bountyRecipient
- CoinflipTopUpdated (lines 76-84): verify @notice/@param
- BiggestFlipUpdated (lines 85-88): verify @notice/@param -- "raw, before bonuses" is accurate
- BountyOwed, BountyPaid (lines 89-90): verify NatSpec presence and accuracy

**Step 5: Audit CUSTOM ERRORS (lines 92-106)**

Verify each error is used where its name suggests. Look for:
- AmountLTMin, CoinflipLocked, OnlyFlipCreditors, OnlyBurnieCoin, OnlyDegenerusGame, AutoRebuyNotEnabled, AutoRebuyAlreadyEnabled, TakeProfitZero, RngLocked, Insufficient, NotApproved
- Specifically: `OnlyBurnieCoin` is defined as an access control error for the `onlyBurnieCoin` modifier (line 203-206) but is ALSO reused in `_resolvePlayer` (line 1142) for operator approval failure -- this error reuse is the pre-identified issue

**Step 6: Audit STORAGE VARIABLES (lines 108-173)**

Review immutable references (burnie, degenerusGame, jackpots, wwxrp), constants (MIN, COINFLIP_LOSS_WWXRP_REWARD, etc.), struct NatSpec (CoinflipDayResult, PlayerCoinflipState, PlayerScore), mapping comments, bounty system variables (currentBounty, biggestFlipEver, bountyOwedTo), RNG state, and leaderboard.

Pay attention to:
- Are constant value descriptions accurate (e.g., MIN = 100 ether, AFKING_RECYCLE_BONUS_BPS = 160)?
- Does PlayerCoinflipState struct documentation match actual field usage?
- Are inline comments on struct fields accurate?

**Step 7: Audit MODIFIERS (lines 186-206)**

Review onlyDegenerusGameContract, onlyFlipCreditors, onlyBurnieCoin:
- Verify each modifier's NatSpec (if any) matches what addresses it allows
- onlyFlipCreditors allows `degenerusGame` AND `burnie` -- verify this matches any NatSpec
- Check for any missing "Used for:" annotations (matching Phase 34 pattern)

**Step 8: Audit depositCoinflip and core coinflip functions (lines 208-600)**

Read depositCoinflip (line ~225) -- the research notes it has only a @notice with no @dev/@param/@return. Verify:
- Is the function complex enough to warrant more NatSpec? (it has complex access control: self-deposit vs operator-approved deposit)
- Verify all inline comments in this function match actual behavior
- Audit all other core coinflip functions in this range: claimCoinflips, claimCoinflipsTakeProfit, previewClaimCoinflips, setCoinflipAutoRebuy, setCoinflipAutoRebuyTakeProfit
- For each function: verify @notice/@dev/@param/@return, check inline comments, verify cross-contract references

**Step 9: Write findings to file**

Write all findings found in Steps 3-8 to the BurnieCoinflip.sol section. Every finding must have:
- `### CMT-0NN:` or `### DRIFT-0NN:` header with brief title
- `**What:**` the specific comment inaccuracy or intent drift
- `**Where:**` BurnieCoinflip.sol:LINE
- `**Why:**` why a C4A warden would be misled
- `**Suggestion:**` recommended fix
- `**Category:**` comment-inaccuracy or intent-drift
- `**Severity:**` INFO or LOW

CMT numbering starts at CMT-059. DRIFT numbering starts at DRIFT-004.
  </action>
  <verify>
    <automated>grep -c '### CMT-\|### DRIFT-' audit/v3.1-findings-35-peripheral-contracts.md; grep 'BurnieCoinflip.sol' audit/v3.1-findings-35-peripheral-contracts.md | head -3</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.1-findings-35-peripheral-contracts.md exists
    - File contains `# Phase 35 Findings: Peripheral Contracts`
    - File contains `## BurnieCoinflip.sol`
    - File contains `**Review scope:** 1,154 lines`
    - CMT numbering starts at CMT-059 (continuing from Phase 34 CMT-058)
    - Every finding has What, Where, Why, Suggestion, Category, and Severity fields
    - No finding has Category other than `comment-inaccuracy` or `intent-drift`
    - No finding has Severity other than `INFO` or `LOW`
    - `git diff 9238faf2..HEAD -- contracts/BurnieCoinflip.sol` is empty (confirms no changes)
    - No .sol files modified (flag-only)
  </acceptance_criteria>
  <done>Phase 35 findings file created with header and summary table. BurnieCoinflip.sol first half reviewed: contract-level header, EVENTS, CUSTOM ERRORS, STORAGE VARIABLES, CONSTRUCTOR, MODIFIERS, and core coinflip functions (lines 1-600). Pre-identified error reuse pattern evaluated.</done>
</task>

<task type="auto">
  <name>Task 2: Comment audit and intent drift review for BurnieCoinflip.sol (second half: payout processing, bounty, quest integration, auto-rebuy, helpers, intent drift, lines 600-1154)</name>
  <files>audit/v3.1-findings-35-peripheral-contracts.md</files>
  <read_first>
    contracts/BurnieCoinflip.sol
    audit/v3.1-findings-35-peripheral-contracts.md
    .planning/phases/35-peripheral-contracts/35-RESEARCH.md
  </read_first>
  <action>
Complete the second half of the BurnieCoinflip.sol review, covering lines 600-1154: payout processing functions (processCoinflipPayouts and related helpers), bounty system functions (bounty payout, biggestFlipEver tracking, bountyOwedTo mechanics), quest integration functions (quest flip credit handling, affiliateQuestReward interaction), auto-rebuy logic (auto-rebuy deposit processing, carry tracking), view/getter functions (coinflipTopLastDay, coinflipBalance views), and all private helper functions (_resolvePlayer at line 1138, _requireApproved at line 1148, _score96 at line 1107, _updateTopDayBettor at line 1116, _bafBracketLevel at line 1130).

**Step 1: Audit payout processing (processCoinflipPayouts and helpers)**

Read processCoinflipPayouts and all related functions. Verify:
- @notice/@dev/@param/@return for each function
- Inline comments describing payout calculation logic
- Cross-contract references to BurnieCoin (mintForCoinflip), DegenerusJackpots (recordBafFlip), DegenerusQuests
- The RNG locking/unlocking mechanics documentation
- Win/loss percentage calculation descriptions

**Step 2: Audit bounty system**

Review bounty-related logic:
- currentBounty, biggestFlipEver, bountyOwedTo usage vs documentation
- Bounty payout flow NatSpec accuracy
- BountyOwed and BountyPaid event emission accuracy
- Bounty rollover mechanics description

**Step 3: Audit quest integration**

Review quest flip credit handling:
- How coinflip results trigger quest progress
- Cross-references to DegenerusQuests contract
- affiliateQuestReward interaction NatSpec

**Step 4: Audit auto-rebuy logic**

Review auto-rebuy deposit processing:
- autoRebuyCarry tracking NatSpec
- Auto-rebuy stop threshold documentation
- Edge cases in auto-rebuy flow documentation

**Step 5: Audit view/getter functions**

Review coinflipTopLastDay and all other view functions:
- @return descriptions match actual return values
- Any computed value descriptions are correct
- External view functions accessible via IDegenerusCoinJackpotView interface

**Step 6: Audit private helper functions (lines 1107-1154)**

Review _score96 (line 1107), _updateTopDayBettor (line 1116), _bafBracketLevel (line 1130), _resolvePlayer (line 1138), _requireApproved (line 1148):
- _resolvePlayer at line 1142: `revert OnlyBurnieCoin(); // Reusing error` -- this is the pre-identified error reuse pattern. Verify: OnlyBurnieCoin is defined as an access control error for the onlyBurnieCoin modifier (lines 203-206) but used here for operator approval failure. The comment says "Reusing error" which partially acknowledges the reuse but does not explain WHAT the error means in this context. Flag matching Phase 34 CMT-043 pattern.
- _requireApproved at line 1150: uses `revert NotApproved()` for the same check -- why does _resolvePlayer use OnlyBurnieCoin but _requireApproved uses NotApproved? Note this inconsistency if NatSpec does not explain it.
- _score96 and _updateTopDayBettor: verify @dev matches behavior
- _bafBracketLevel: verify bracket rounding description

**Step 7: Full intent drift review for BurnieCoinflip.sol**

Scan the entire BurnieCoinflip.sol for:
- Vestigial references to BurnieCoin state that may have been split (inverse of Phase 34 findings)
- Guards or conditions that may have become unnecessary after the split
- Access control patterns that don't match their documentation
- Any references to features that no longer exist or work differently
- The error reuse pattern (OnlyBurnieCoin in _resolvePlayer) as potential intent drift

**Step 8: Write remaining findings and finalize BurnieCoinflip section**

Append any additional findings. Continue CMT/DRIFT numbering sequentially from Task 1's last number.

Update the Summary table BurnieCoinflip.sol row with actual counts.

Add review completion marker:

```markdown
---

**BurnieCoinflip.sol review complete.** 62 NatSpec tags verified, ~152 comment lines reviewed, all inline comments cross-referenced against current code behavior. [N] findings total ([X] CMT, [Y] DRIFT). Contract unchanged since Phase 29 -- [primary finding pattern summary].
```
  </action>
  <verify>
    <automated>grep 'BurnieCoinflip.sol review complete' audit/v3.1-findings-35-peripheral-contracts.md; grep -c '### CMT-\|### DRIFT-' audit/v3.1-findings-35-peripheral-contracts.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.1-findings-35-peripheral-contracts.md contains "BurnieCoinflip.sol review complete"
    - Summary table row for BurnieCoinflip.sol has actual integer counts (not X/Y/Z placeholders)
    - All 62 NatSpec tags in BurnieCoinflip.sol are accounted for (either verified clean or flagged)
    - _resolvePlayer error reuse at line 1142 is evaluated and either flagged or documented as clean
    - Every finding has What, Where, Why, Suggestion, Category, and Severity fields
    - Intent drift scan completed
    - No .sol files modified (flag-only)
  </acceptance_criteria>
  <done>BurnieCoinflip.sol (1,154 lines, 62 NatSpec tags, ~152 comment lines, 37 functions) fully reviewed for comment accuracy and intent drift. Second half reviewed: payout processing, bounty system, quest integration, auto-rebuy logic, view functions, private helpers. Error reuse pattern formally evaluated. Full intent drift scan complete. Review summary written. Summary table updated with actual counts.</done>
</task>

</tasks>

<verification>
- audit/v3.1-findings-35-peripheral-contracts.md exists with header and summary table
- grep 'BurnieCoinflip.sol' returns results
- grep 'BurnieCoinflip.sol review complete' returns results
- CMT numbering starts at CMT-059 (continuing from Phase 34 CMT-058)
- DRIFT numbering starts at DRIFT-004 (continuing from Phase 34 DRIFT-003)
- Every finding has what/where/why/suggestion/category/severity format
- BurnieCoinflip.sol summary row has actual integer counts
- No .sol files in contracts/ directory were modified
- Pre-identified issue (OnlyBurnieCoin error reuse at line 1142) is evaluated
</verification>

<success_criteria>
- BurnieCoinflip.sol fully reviewed for comment accuracy and intent drift
- All 62 NatSpec tags and ~152 comment lines verified
- Per-batch findings file created with proper format continuing Phase 34 numbering
- Every finding has what/why/suggestion per item
- No code changes made to any contract files
</success_criteria>

<output>
After completion, create `.planning/phases/35-peripheral-contracts/35-01-SUMMARY.md`
</output>
