---
phase: 35-peripheral-contracts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v3.1-findings-35-peripheral-contracts.md
autonomous: true
requirements: [CMT-05, DRIFT-05]

must_haves:
  truths:
    - "Every NatSpec tag in DegenerusQuests.sol is verified against current code behavior"
    - "Every NatSpec tag in DegenerusJackpots.sol is verified against current code behavior"
    - "Every inline comment in both contracts is verified against current logic"
    - "QUEST_TYPE_RESERVED = 4 vestigial constant is evaluated and flagged if warranted"
    - "DegenerusJackpots COIN CONTRACT HOOKS section header accuracy verified (BurnieCoin vs BurnieCoinflip)"
    - "onlyCoin modifier naming evaluated (allows both COIN and COINFLIP)"
    - "Any vestigial logic or intent drift in both contracts is flagged"
  artifacts:
    - path: "audit/v3.1-findings-35-peripheral-contracts.md"
      provides: "DegenerusQuests.sol and DegenerusJackpots.sol sections appended to findings file"
      contains: "## DegenerusQuests.sol"
  key_links:
    - from: "audit/v3.1-findings-35-peripheral-contracts.md"
      to: "contracts/DegenerusQuests.sol"
      via: "File:Line citations for every finding"
      pattern: "DegenerusQuests\\.sol:\\d+"
    - from: "audit/v3.1-findings-35-peripheral-contracts.md"
      to: "contracts/DegenerusJackpots.sol"
      via: "File:Line citations for every finding"
      pattern: "DegenerusJackpots\\.sol:\\d+"
---

<objective>
Perform comment audit + intent drift review for DegenerusQuests.sol (1,598 lines, 249 NatSpec tags, ~558 comment lines, 35 functions) and DegenerusJackpots.sol (689 lines, 78 NatSpec tags, ~128 comment lines, 14 functions).

Purpose: DegenerusQuests.sol has the densest NatSpec in the entire batch (249 tags) and the pre-identified QUEST_TYPE_RESERVED vestigial constant. DegenerusJackpots.sol has a pre-identified stale "COIN CONTRACT HOOKS" section header that says "Called by BurnieCoin" when the actual caller is BurnieCoinflip. Both contracts interact closely with the coinflip system and are game mechanics peripherals. Neither was modified after Phase 29.

Output: DegenerusQuests.sol and DegenerusJackpots.sol sections appended to `audit/v3.1-findings-35-peripheral-contracts.md`. CMT/DRIFT numbering continues sequentially from Plan 01's last number.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-peripheral-contracts/35-RESEARCH.md
@audit/v3.1-findings-35-peripheral-contracts.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comment audit and intent drift review for DegenerusQuests.sol (1,598 lines, 249 NatSpec tags, 35 functions)</name>
  <files>audit/v3.1-findings-35-peripheral-contracts.md</files>
  <read_first>
    contracts/DegenerusQuests.sol
    audit/v3.1-findings-35-peripheral-contracts.md
    .planning/phases/35-peripheral-contracts/35-RESEARCH.md
  </read_first>
  <action>
Review DegenerusQuests.sol (1,598 lines, 249 NatSpec tags, ~558 comment lines, 35 functions) -- the densest NatSpec contract in the batch. Systematic verification: constants section, then structs, then modifiers, then each handler function.

**Step 1: Verify no post-Phase-29 changes**

Run `git diff 9238faf2..HEAD -- contracts/DegenerusQuests.sol` to confirm empty diff.

**Step 2: Audit contract-level header block (lines 8-48)**

Read the @title, @author, @notice, @dev Architecture Overview, Security Model, Quest Lifecycle, Progress Versioning, and Streak System blocks. Verify each claim:
- "Called by the Degenerus ContractAddresses.COIN contract" -- verify: is it called only by COIN or also by COINFLIP? Check the onlyCoin modifier (lines 283-288) which allows BOTH COIN and COINFLIP
- "All player-action handlers are ContractAddresses.COIN-gated via onlyCoin modifier" -- verify: onlyCoin allows COIN AND COINFLIP, so "COIN-gated" is misleading
- "Quest normalization allows ContractAddresses.COIN OR game to trigger via onlyCoinOrGame" -- verify: does an `onlyCoinOrGame` modifier actually exist? Search for it
- "No external calls to untrusted contracts -- only reads trusted questGame" -- verify: check all external calls
- "Slot 0 is always a fixed deposit new ETH quest (mint with ETH)" -- verify against rollDailyQuest implementation
- "Slot 1 is a weighted-random quest from the remaining quest types" -- verify
- "No tiers or difficulty variance; targets are fixed" -- verify
- "Missing a day resets streak to zero" -- verify

**Step 3: Audit CUSTOM ERRORS (lines 50-58)**

- OnlyCoin() @notice says "Thrown when caller is not the authorized COIN or COINFLIP contract" -- verify this matches the onlyCoin modifier (lines 283-288) which checks both COIN and COINFLIP. The error name says "OnlyCoin" but the description correctly says "COIN or COINFLIP" -- evaluate if the misleading error name should be flagged
- OnlyGame() @notice -- verify against onlyGame modifier

**Step 4: Audit EVENTS (lines 60-135)**

For each event: QuestSlotRolled, QuestProgressUpdated, QuestCompleted, QuestStreakReset, QuestShieldConsumed
- Verify @notice/@param against actual emit sites
- Check indexed parameters match descriptions

**Step 5: Audit CONSTANTS section (lines 136-189)**

For each constant, verify @dev description matches the constant value and its usage:
- QUEST_TYPE_MINT_BURNIE (0), QUEST_TYPE_MINT_ETH (1), QUEST_TYPE_FLIP (2), QUEST_TYPE_AFFILIATE (3)
- **QUEST_TYPE_RESERVED = 4**: Pre-identified issue. Run `grep -n 'QUEST_TYPE_RESERVED' contracts/DegenerusQuests.sol` to confirm the constant is ONLY declared, never used in code logic. Verify the @dev "Retired quest type id kept reserved for compatibility" -- evaluate whether this is vestigial (DRIFT) or deliberate defensive code (just note it). Flag as appropriate.
- QUEST_TYPE_DECIMATOR (5), QUEST_TYPE_LOOTBOX (6), QUEST_TYPE_DEGENERETTE_ETH (7), QUEST_TYPE_DEGENERETTE_BURNIE (8), QUEST_TYPE_COUNT (9)
- Streak constants (QUEST_STATE_STREAK_CREDITED)
- Quest targets (QUEST_MINT_TARGET, QUEST_BURNIE_TARGET, QUEST_LOOTBOX_TARGET_MULTIPLIER)
- Verify descriptions like "2x price in BURNIE" match the formula `2 * PRICE_COIN_UNIT`

**Step 6: Audit STRUCTS section**

Review Quest struct, PlayerSlotState struct, PlayerQuestState struct:
- Verify packed field descriptions match actual field types and widths
- Verify field @notice/@dev annotations

**Step 7: Audit STATE VARIABLES and MODIFIERS (lines ~260-294)**

Review mappings (quests, questPlayerState, questStreakShieldCount, questVersionCounter):
- Verify NatSpec descriptions match actual usage
- onlyCoin modifier (lines 283-288): allows both COIN and COINFLIP. Verify @dev "Restricts access to the authorized COIN or COINFLIP contract" -- this is accurate. But the modifier NAME is `onlyCoin` which is misleading. Evaluate whether to flag the naming vs NatSpec mismatch as CMT.
- onlyGame modifier (line 290-293): no NatSpec -- verify if it needs any

**Step 8: Audit QUEST ROLLING section (lines ~295-400)**

Review rollDailyQuest and _seedSlot:
- Verify NatSpec describes the two-slot rolling logic
- Verify inline comments about Slot 0 always being QUEST_TYPE_MINT_ETH
- Verify weighted random selection for Slot 1

**Step 9: Audit HANDLER FUNCTIONS section (lines ~400-1200)**

Review each handler function systematically: handleMint, handleFlip, handleAffiliate, handleDecimator, handleLootbox, handleDegenerette, and any other handlers:
- Each handler follows a pattern: early-exit, sync state, find matching slot, accumulate progress, check completion
- Verify @notice/@dev/@param for each handler
- Verify the pattern documentation matches reality
- Check inline comments about quest progress accumulation

**Step 10: Audit VIEW FUNCTIONS and remaining functions (lines ~1200-1598)**

Review all view/getter functions:
- Verify @return descriptions match actual return values
- Check for any computed values that may be inaccurately described
- Review normalizeQuestProgress and any other utility functions

**Step 11: Full intent drift review for DegenerusQuests.sol**

- QUEST_TYPE_RESERVED = 4: Is the constant truly unused? Could its presence confuse a warden about quest type enumeration?
- onlyCoin modifier name vs actual behavior: allows COINFLIP too
- Any vestigial references to removed features
- Any guards or conditions that may be unnecessary

**Step 12: Write DegenerusQuests.sol findings section**

Add `## DegenerusQuests.sol` section. Include:

```markdown
**Review scope:** 1,598 lines, 249 NatSpec tags, ~558 comment lines, 35 functions.
**Post-Phase-29 changes:** None -- contract unchanged since Phase 29 (commit 9238faf2).
```

Write all findings with CMT/DRIFT numbering continuing sequentially from Plan 01. Every finding needs What/Where/Why/Suggestion/Category/Severity.

Add review completion marker:

```markdown
---

**DegenerusQuests.sol review complete.** 249 NatSpec tags verified, ~558 comment lines reviewed. [N] findings total ([X] CMT, [Y] DRIFT). QUEST_TYPE_RESERVED evaluated. Dense NatSpec systematically verified across all 35 functions.
```
  </action>
  <verify>
    <automated>grep 'DegenerusQuests.sol review complete' audit/v3.1-findings-35-peripheral-contracts.md; grep -c '### CMT-\|### DRIFT-' audit/v3.1-findings-35-peripheral-contracts.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.1-findings-35-peripheral-contracts.md contains `## DegenerusQuests.sol`
    - audit/v3.1-findings-35-peripheral-contracts.md contains "DegenerusQuests.sol review complete"
    - Summary table row for DegenerusQuests.sol has actual integer counts (not X/Y/Z)
    - QUEST_TYPE_RESERVED evaluated -- either flagged as DRIFT or documented as deliberate
    - `grep -n 'QUEST_TYPE_RESERVED' contracts/DegenerusQuests.sol` confirms constant is only declared, not used in logic
    - Every finding has What, Where, Why, Suggestion, Category, and Severity fields
    - CMT/DRIFT numbering continues sequentially from Plan 01's last number with no gaps
    - No .sol files modified (flag-only)
  </acceptance_criteria>
  <done>DegenerusQuests.sol (1,598 lines, 249 NatSpec tags, ~558 comment lines, 35 functions) fully reviewed for comment accuracy and intent drift. Constants, structs, modifiers, quest rolling, all handler functions, and view functions verified. QUEST_TYPE_RESERVED evaluated. Dense NatSpec systematically verified.</done>
</task>

<task type="auto">
  <name>Task 2: Comment audit and intent drift review for DegenerusJackpots.sol (689 lines, 78 NatSpec tags, 14 functions)</name>
  <files>audit/v3.1-findings-35-peripheral-contracts.md</files>
  <read_first>
    contracts/DegenerusJackpots.sol
    audit/v3.1-findings-35-peripheral-contracts.md
    .planning/phases/35-peripheral-contracts/35-RESEARCH.md
  </read_first>
  <action>
Review DegenerusJackpots.sol (689 lines, 78 NatSpec tags, ~128 comment lines, 14 functions) -- BAF jackpot system with leaderboard-based distribution.

**Step 1: Verify no post-Phase-29 changes**

Run `git diff 9238faf2..HEAD -- contracts/DegenerusJackpots.sol` to confirm empty diff.

**Step 2: Audit contract-level header and interface block (lines 1-38)**

Read the file-level block comment (lines 4-9): "Standalone contract managing the BAF (Big Ass Flip) jackpot system. Decimator logic is handled in the game decimator module." Verify accuracy.

Read the IDegenerusCoinJackpotView interface (lines 19-26):
- @notice says "View interface for coin contract jackpot-related queries" -- "coin contract" could mean COIN or COINFLIP. Verify which contract actually implements this interface.
- @dev says "Used to retrieve coinflip statistics for leaderboards" -- verify

Read the contract-level NatSpec (lines 32-38):
- @dev "BurnieCoin forwards flips into this contract" -- VERIFY THIS. The `recordBafFlip` function uses `onlyCoin` modifier which allows both COIN and COINFLIP. Run `grep -n 'recordBafFlip' contracts/BurnieCoin*.sol` to determine which contract actually calls recordBafFlip. If BurnieCoinflip calls it (not BurnieCoin), this NatSpec is stale from the coinflip split.

**Step 3: Audit ERRORS and EVENTS sections (lines 40-69)**

Review error NatSpec:
- OnlyCoin: "restricted to the coin contract" -- but onlyCoin allows COIN AND COINFLIP
- OnlyGame: verify

Review event NatSpec:
- BafFlipRecorded: verify @param descriptions match actual emit at line 188

**Step 4: Audit STRUCTS section (lines 71-84)**

Review PlayerScore struct:
- @dev "Packed into single slot: address (160) + score (96) = 256 bits" -- verify this is correct

**Step 5: Audit CONSTANT STATE section (lines 86-97)**

Review `coin` variable (line 93):
- Variable name is `coin` but it is set to `ContractAddresses.COINFLIP` -- the variable name is misleading
- @notice says "Coinflip contract for coinflip stats queries (constant)" -- this NatSpec IS correct despite the confusing variable name
- Evaluate whether the misleading variable name warrants a CMT finding in the NatSpec context

Review `degenerusGame` constant (line 96).

**Step 6: Audit CONSTANTS section (lines 99-115)**

Verify BAF_SCATTER_MASK_OFFSET, BAF_SCATTER_TICKET_WINNERS, BAF_SCATTER_ROUNDS:
- @dev descriptions match actual values and usage

**Step 7: Audit BAF STATE STORAGE section (lines 117-139)**

Verify all mapping and state variable NatSpec:
- bafTotals, bafTop, bafTopLen, bafEpoch, bafPlayerEpoch, lastBafResolvedDay
- Check descriptions match actual usage in recordBafFlip and resolveBafJackpot

**Step 8: Audit MODIFIERS & ACCESS CONTROL section (lines 141-159)**

Review onlyCoin modifier (lines 147-152):
- @dev "Restricts function to coin or coinflip contract" -- verify against code which checks `ContractAddresses.COIN` AND `ContractAddresses.COINFLIP`
- The modifier NAME is `onlyCoin` but allows COINFLIP too -- same pattern as DegenerusQuests.sol

Review onlyGame modifier.

**Step 9: Audit COIN CONTRACT HOOKS section (lines 161-189) -- PRE-IDENTIFIED ISSUE**

The section header (lines 161-166) says:
```
COIN CONTRACT HOOKS
Called by BurnieCoin to record coinflip activity.
These hooks build state used by jackpot resolution.
```

Verify which contract actually calls `recordBafFlip`:
- Run `grep -n 'recordBafFlip\|jackpots\.' contracts/BurnieCoin.sol contracts/BurnieCoinflip.sol`
- If BurnieCoinflip (COINFLIP) calls recordBafFlip but BurnieCoin does NOT, the section header is stale from the coinflip split
- Flag as CMT with suggestion to change "Called by BurnieCoin" to "Called by BurnieCoinflip"

Review recordBafFlip function NatSpec (lines 168-188):
- @dev "Called by coin contract on every manual coinflip" -- if actually called by COINFLIP, "coin contract" is ambiguous/stale
- Verify @param and @custom:access descriptions

**Step 10: Audit BAF JACKPOT RESOLUTION section (lines 191-689)**

Review resolveBafJackpot and all resolution-related functions:
- Prize distribution table in block comments -- verify percentages match code
- Winner selection logic NatSpec
- Scatter round documentation
- ETH payout flow descriptions
- Internal helper functions (_updateBafTop, _sortedInsert, etc.)

**Step 11: Audit all view functions**

Review any view/getter functions for @return accuracy.

**Step 12: Full intent drift review for DegenerusJackpots.sol**

- "COIN CONTRACT HOOKS" section header referencing BurnieCoin when caller is BurnieCoinflip
- `coin` variable name pointing to COINFLIP address
- onlyCoin modifier name allowing COINFLIP
- Any other vestigial references from before the coinflip split

**Step 13: Write DegenerusJackpots.sol findings section**

Add `## DegenerusJackpots.sol` section. Include:

```markdown
**Review scope:** 689 lines, 78 NatSpec tags, ~128 comment lines, 14 functions.
**Post-Phase-29 changes:** None -- contract unchanged since Phase 29 (commit 9238faf2).
```

Write all findings. Add review completion marker:

```markdown
---

**DegenerusJackpots.sol review complete.** 78 NatSpec tags verified, ~128 comment lines reviewed. [N] findings total ([X] CMT, [Y] DRIFT). COIN CONTRACT HOOKS section header evaluated. `coin` variable naming evaluated.
```

Update summary table rows for both DegenerusQuests.sol and DegenerusJackpots.sol with actual counts.
  </action>
  <verify>
    <automated>grep 'DegenerusJackpots.sol review complete' audit/v3.1-findings-35-peripheral-contracts.md; grep -c '### CMT-\|### DRIFT-' audit/v3.1-findings-35-peripheral-contracts.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.1-findings-35-peripheral-contracts.md contains `## DegenerusJackpots.sol`
    - audit/v3.1-findings-35-peripheral-contracts.md contains "DegenerusJackpots.sol review complete"
    - Summary table rows for DegenerusQuests.sol and DegenerusJackpots.sol have actual integer counts
    - "COIN CONTRACT HOOKS" section header evaluated -- flagged if BurnieCoinflip is the actual caller
    - `grep -n 'recordBafFlip\|jackpots\.' contracts/BurnieCoin*.sol` output used to determine actual caller
    - Every finding has What, Where, Why, Suggestion, Category, and Severity fields
    - CMT/DRIFT numbering continues sequentially with no gaps
    - No .sol files modified (flag-only)
  </acceptance_criteria>
  <done>DegenerusJackpots.sol (689 lines, 78 NatSpec tags, ~128 comment lines, 14 functions) fully reviewed for comment accuracy and intent drift. COIN CONTRACT HOOKS section header evaluated. `coin` variable naming evaluated. BAF resolution logic NatSpec verified. Both DegenerusQuests.sol and DegenerusJackpots.sol sections complete with actual finding counts in summary table.</done>
</task>

</tasks>

<verification>
- audit/v3.1-findings-35-peripheral-contracts.md has sections for DegenerusQuests.sol and DegenerusJackpots.sol
- Both sections have "review complete" markers
- Summary table rows updated with actual counts
- QUEST_TYPE_RESERVED evaluated in DegenerusQuests.sol
- COIN CONTRACT HOOKS section header evaluated in DegenerusJackpots.sol
- onlyCoin modifier naming pattern evaluated in both contracts
- Every finding has what/where/why/suggestion/category/severity format
- No .sol files in contracts/ directory were modified
</verification>

<success_criteria>
- DegenerusQuests.sol (1,598 lines) and DegenerusJackpots.sol (689 lines) fully reviewed
- All 327 NatSpec tags (249+78) and ~686 comment lines (558+128) verified
- Pre-identified issues (QUEST_TYPE_RESERVED, COIN CONTRACT HOOKS header, coin variable naming) formally evaluated
- Every finding has what/why/suggestion per item
- No code changes made to any contract files
</success_criteria>

<output>
After completion, create `.planning/phases/35-peripheral-contracts/35-02-SUMMARY.md`
</output>
