---
phase: 31-core-game-contracts
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - audit/v3.1-findings-31-core-game-contracts.md
autonomous: true
requirements: [CMT-01, DRIFT-01]

must_haves:
  truths:
    - "Every NatSpec tag in DegenerusGame.sol is verified against current code behavior with warden-readability focus"
    - "Every inline comment in DegenerusGame.sol is verified against current logic"
    - "Block comment headers in DegenerusGame.sol are verified for staleness"
    - "The known stale 18h VRF timeout comment at line 287 is flagged"
    - "Any vestigial guards, unnecessary restrictions, or intent drift in DegenerusGame.sol is flagged"
    - "The consolidated findings file has complete Summary counts for all 3 contracts"
  artifacts:
    - path: "audit/v3.1-findings-31-core-game-contracts.md"
      provides: "Complete per-batch findings file with all 3 contract sections and summary counts"
      contains: "## DegenerusGame.sol"
  key_links:
    - from: "audit/v3.1-findings-31-core-game-contracts.md"
      to: "contracts/DegenerusGame.sol"
      via: "File:Line citations for every finding"
      pattern: "DegenerusGame\\.sol:\\d+"
---

<objective>
Comment audit and intent drift review for DegenerusGame.sol (2,856 lines, 68 external/public functions, 507 NatSpec tags, ~664 comment lines).

Purpose: DegenerusGame.sol is the largest core contract and the primary entry point for game interactions. Phase 29 Plan 01 already verified 108 functions (105 MATCH, 1 DISCREPANCY) and found 3 inline issues. The code has NOT changed since Phase 29. However, v3.1 focuses on warden-readability and intent drift, which Phase 29 did not specifically address. The known stale "18h timeout" comment at line 287 (should be 12h per KEY DECISIONS) is a confirmed pre-identified finding. This plan also finalizes the consolidated findings file by updating the Summary table with complete counts.

Output: `## DegenerusGame.sol` section appended to `audit/v3.1-findings-31-core-game-contracts.md`, plus updated Summary table with final counts for all 3 contracts.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-core-game-contracts/31-RESEARCH.md
@.planning/phases/31-core-game-contracts/31-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Block comment and architecture header review for DegenerusGame.sol</name>
  <files>audit/v3.1-findings-31-core-game-contracts.md</files>
  <read_first>
    contracts/DegenerusGame.sol
    audit/v3.1-findings-31-core-game-contracts.md
    .planning/phases/31-core-game-contracts/31-RESEARCH.md
    .planning/phases/29-comment-documentation-correctness/29-01-SUMMARY.md
  </read_first>
  <action>
Read DegenerusGame.sol in full and perform the warden-readability comment audit focusing on block comments, architecture headers, and the first half of function NatSpec.

**Step 1: Review block comment headers**

Read and verify these specific block comment sections identified in RESEARCH.md:
- Lines 4-28: Contract-level header and architecture description
- Lines 85-93: Module delegation architecture description
- Lines 279-292: VRF/RNG section header

For each block:
- Does it accurately describe the current contract architecture?
- Are there references to removed features, old parameter values, or prior architecture?
- Would a C4A warden reading this form correct assumptions about the protocol?

Known stale item: Line 287 says `RNG must be ready (not locked) or recently stale (18h timeout)` but actual timeout is 12h (changed per KEY DECISIONS in PROJECT.md, VRF retry timeout 18h -> 12h). This MUST be flagged.

**Step 2: Review function NatSpec for warden-readability**

For the functions in the first ~1,400 lines (roughly the first half of the contract):
1. Read each @notice, @dev, @param, @return tag
2. Verify it describes actual behavior, not historical behavior
3. Focus on warden-readability: "Would a C4A warden reading this NatSpec understand the function correctly?"
4. Pay special attention to:
   - Functions that delegatecall to modules -- does NatSpec clarify execution context?
   - Functions with complex game mechanics -- are edge cases mentioned?
   - Functions that interact with VRF -- are timing/locking semantics clear?
5. Cross-reference against Phase 29 findings: Phase 29 found 1 NatSpec DISCREPANCY (futurePrizePoolTotalView "aggregate" description) and 3 inline issues. Verify these are still present or note if fixed.

**Step 3: Review inline comments in first half**

For every `//` and `/* */` comment in lines 1-1400:
- Verify accuracy against current code
- Check for stale references to old constants, removed features, or prior architecture
- Check the Phase 29 findings: jackpot section header comments at lines ~1795-1796 were flagged as stale -- these are in the second half and will be checked in Task 2

**Step 4: Begin intent drift review for first half**

Scan lines 1-1400 for:
- Vestigial guards from removed features
- Unnecessary restrictions
- Logic whose behavior has changed but comments still describe old behavior
- delegatecall dispatch logic where comment context might be misleading

**Step 5: Write findings for first half**

Append a `## DegenerusGame.sol` section to the findings file. Write all findings from the first-half review. Use the same CMT-NNN / DRIFT-NNN format. The file MUST include the pre-identified stale item at line 287 (18h timeout).

Note: Task 2 will continue with the second half and finalize.
  </action>
  <verify>
    <automated>grep '## DegenerusGame.sol' audit/v3.1-findings-31-core-game-contracts.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.1-findings-31-core-game-contracts.md contains `## DegenerusGame.sol`
    - File contains a finding referencing DegenerusGame.sol line 287 or nearby (stale 18h VRF timeout)
    - Block comments at lines 4-28, 85-93, 279-292 are explicitly reviewed (mentioned in findings or noted as verified)
    - Every finding has What, Where, Why, Suggestion, Category, and Severity fields
    - No code changes to contracts/DegenerusGame.sol (flag-only)
  </acceptance_criteria>
  <done>Block comments, architecture headers, and first-half NatSpec/inline comments in DegenerusGame.sol reviewed with warden-readability and intent drift lens. DegenerusGame.sol section started in findings file with all first-half findings documented.</done>
</task>

<task type="auto">
  <name>Task 2: Complete DegenerusGame.sol review and finalize batch findings</name>
  <files>audit/v3.1-findings-31-core-game-contracts.md</files>
  <read_first>
    contracts/DegenerusGame.sol
    audit/v3.1-findings-31-core-game-contracts.md
  </read_first>
  <action>
Complete the DegenerusGame.sol review (second half) and finalize the consolidated findings file.

**Step 1: Review function NatSpec for second half (lines ~1400-2856)**

Continue the function-by-function NatSpec review from where Task 1 left off. This section includes:
- Jackpot-related functions (Phase 29 flagged stale headers at ~lines 1795-1796 regarding "day 5" and "decimator scope")
- Decimator functions
- Endgame functions
- GameOver functions
- Admin/utility functions

For each function:
1. Read @notice, @dev, @param, @return tags
2. Verify against current behavior
3. Check warden-readability
4. Check delegatecall context documentation

**Step 2: Review inline comments in second half (lines ~1400-2856)**

- Verify every `//` comment
- Check the Phase 29 stale items: jackpot section header comments were flagged -- verify if they are still stale
- Check for any references to old constants, removed features, or changed mechanics

**Step 3: Complete intent drift review for DegenerusGame.sol**

Complete the intent drift scan for the second half:
- Vestigial guards from removed features
- Unnecessary restrictions accumulated through iterative hardening
- Logic whose behavior drifted from comments/docs
- delegatecall patterns where module behavior may differ from what NatSpec describes

**Step 4: Append remaining findings**

Add all second-half findings to the DegenerusGame.sol section of the findings file. Continue the CMT-NNN / DRIFT-NNN numbering from Task 1.

**Step 5: Finalize the consolidated findings file**

Update the Summary table at the top of the file with actual counts for ALL 3 contracts:
- DegenerusAdmin.sol: X CMT, Y DRIFT, Z total (from Plan 01 Task 1)
- DegenerusGameStorage.sol: X CMT, Y DRIFT, Z total (from Plan 01 Task 2)
- DegenerusGame.sol: X CMT, Y DRIFT, Z total (from this plan's Tasks 1+2)
- Total: sum of all

Verify the file has all required sections:
- Header with date, scope, pass, mode
- Summary table with actual counts
- DegenerusAdmin.sol section (from Plan 01)
- DegenerusGameStorage.sol section (from Plan 01)
- DegenerusGame.sol section (from this plan)
  </action>
  <verify>
    <automated>grep -c '### CMT-\|### DRIFT-' audit/v3.1-findings-31-core-game-contracts.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.1-findings-31-core-game-contracts.md contains findings from all 3 contracts
    - Summary table at top has actual integer counts (no X or Y placeholders) for all 3 contracts and totals
    - grep for '## DegenerusAdmin.sol' returns a match
    - grep for '## DegenerusGameStorage.sol' returns a match
    - grep for '## DegenerusGame.sol' returns a match
    - Total finding count in Summary table matches actual count of `### CMT-` and `### DRIFT-` headings in file
    - Every finding has What, Where, Why, Suggestion, Category, and Severity fields
    - No code changes to any .sol files (flag-only)
  </acceptance_criteria>
  <done>DegenerusGame.sol (2,856 lines, 507 NatSpec tags, ~664 comment lines) fully reviewed with warden-readability and intent drift lens. Consolidated findings file complete with all 3 contracts, accurate Summary table, and every finding having what/why/suggestion. Phase 31 per-batch deliverable is ready for Phase 36 consolidation.</done>
</task>

</tasks>

<verification>
- audit/v3.1-findings-31-core-game-contracts.md exists and contains all 3 contract sections
- Summary table has real counts (not placeholders)
- At least 3 pre-identified stale items are flagged (Admin line 38, Admin line 41, Game line 287)
- Every finding has what/where/why/suggestion/category/severity format
- No .sol files in contracts/ directory were modified (git diff --name-only should show only audit/ file)
- Finding count in Summary table equals actual `### CMT-|### DRIFT-` heading count
</verification>

<success_criteria>
- DegenerusGame.sol fully reviewed for comment accuracy and intent drift
- Pre-identified stale "18h timeout" comment at line 287 is flagged
- Phase 29 findings (futurePrizePoolTotalView discrepancy, jackpot header staleness) are cross-referenced
- Consolidated findings file is complete with all 3 contracts and accurate summary counts
- No code changes made to any contract files
</success_criteria>

<output>
After completion, create `.planning/phases/31-core-game-contracts/31-02-SUMMARY.md`
</output>
