---
phase: 31-core-game-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v3.1-findings-31-core-game-contracts.md
autonomous: true
requirements: [CMT-01, DRIFT-01]

must_haves:
  truths:
    - "Every NatSpec tag in DegenerusAdmin.sol is verified against current code behavior"
    - "Every inline comment in DegenerusAdmin.sol is verified against current logic"
    - "Every NatSpec tag in DegenerusGameStorage.sol is verified against current code behavior"
    - "Every inline comment in DegenerusGameStorage.sol is verified against current logic"
    - "Post-Phase-29 changes to DegenerusAdmin.sol are specifically reviewed for stale comments"
    - "Any vestigial guards, unnecessary restrictions, or intent drift in Admin and Storage contracts is flagged"
    - "A per-batch findings file exists with what/why/suggestion for every finding"
  artifacts:
    - path: "audit/v3.1-findings-31-core-game-contracts.md"
      provides: "Per-batch findings file for Phase 31 with DegenerusAdmin and DegenerusGameStorage sections"
      contains: "## DegenerusAdmin.sol"
  key_links:
    - from: "audit/v3.1-findings-31-core-game-contracts.md"
      to: "contracts/DegenerusAdmin.sol"
      via: "File:Line citations for every finding"
      pattern: "DegenerusAdmin\\.sol:\\d+"
    - from: "audit/v3.1-findings-31-core-game-contracts.md"
      to: "contracts/storage/DegenerusGameStorage.sol"
      via: "File:Line citations for every finding"
      pattern: "DegenerusGameStorage\\.sol:\\d+"
---

<objective>
Comment audit and intent drift review for DegenerusAdmin.sol (801 lines) and DegenerusGameStorage.sol (1,631 lines).

Purpose: DegenerusAdmin.sol received two code-changing commits AFTER Phase 29 completed (df1e9f78 adding 1-per-address proposal limit + voidedUpTo watermark, and fd9dbad1 lowering initial threshold from 60% to 50%). These post-Phase-29 changes introduced at least 2 demonstrably stale header comments. DegenerusGameStorage.sol is unchanged since Phase 29 but has extensive inline documentation that needs warden-readability review. This plan creates the batch deliverable file and populates it with findings for these two contracts.

Output: `audit/v3.1-findings-31-core-game-contracts.md` with DegenerusAdmin and DegenerusGameStorage findings sections. DegenerusGame.sol section will be added by Plan 02.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-core-game-contracts/31-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comment audit and intent drift review for DegenerusAdmin.sol</name>
  <files>audit/v3.1-findings-31-core-game-contracts.md</files>
  <read_first>
    contracts/DegenerusAdmin.sol
    .planning/phases/31-core-game-contracts/31-RESEARCH.md
    .planning/phases/29-comment-documentation-correctness/29-04-SUMMARY.md
  </read_first>
  <action>
Create the findings file `audit/v3.1-findings-31-core-game-contracts.md` with the header/template structure, then perform the full DegenerusAdmin.sol review.

**Step 1: Create findings file with header**

Use this exact template for the file header:

```markdown
# Phase 31 Findings: Core Game Contracts

**Date:** 2026-03-18
**Scope:** DegenerusAdmin.sol, DegenerusGameStorage.sol, DegenerusGame.sol
**Pass:** v3.1 second independent review (v3.0 Phase 29 was first pass)
**Mode:** Flag-only -- no code changes

## Summary

| Contract | CMT findings | DRIFT findings | Total |
|----------|-------------|----------------|-------|
| DegenerusAdmin.sol | X | Y | Z |
| DegenerusGameStorage.sol | X | Y | Z |
| DegenerusGame.sol | X | Y | Z |
| **Total** | **X** | **Y** | **Z** |

*Summary counts updated after all contracts reviewed.*
```

**Step 2: Review DegenerusAdmin.sol block comments (lines 6-42)**

Read the entire header block. Known stale items to verify:
- Line 38: `Approval voting with decaying threshold (60% -> 5% over 7 days)` -- STALE. Commit fd9dbad1 changed starting threshold to 50%. The code at line 539 returns 5000 (50%), not 6000.
- Line 41: `Death clock pauses while any proposal is active` -- STALE. Commit 73c50cb3 removed death clock pause entirely. No anyProposalActive() function exists.
- Check ALL other lines 6-42 for any additional stale references to removed features (activeProposalCount, anyProposalActive, 60%, death clock pause).

**Step 3: Review all NatSpec tags in DegenerusAdmin.sol**

For each @notice, @dev, @param, @return, @custom tag across all 57 NatSpec tags:
1. Read the tag text
2. Cross-reference against the actual function behavior in current code
3. Pay special attention to NatSpec added by post-Phase-29 commits (activeProposalId at line 274, voidedUpTo at lines 277-278)
4. Check for any references to old threshold values, removed features, or prior architecture
5. Focus on warden-readability: "Would a C4A warden reading this be misled?"

**Step 4: Review all inline comments in DegenerusAdmin.sol**

Read every `//` comment in the contract. Check for:
- References to 60% threshold (should be 50%)
- References to death clock pause (removed)
- References to activeProposalCount (removed)
- References to anyProposalActive() (removed)
- Any other stale references to pre-hardening architecture

**Step 5: Intent drift review for DegenerusAdmin.sol**

Scan for:
- Vestigial guards from removed features (any code that was left behind when death clock pause or activeProposalCount was removed)
- Unnecessary restrictions that may have accumulated through iterative hardening
- Logic whose behavior changed (threshold now starts at 50% not 60%) but whose surrounding code/comments still describe old behavior
- The AlreadyHasActiveProposal error and 1-per-address proposal guard from df1e9f78 -- verify NatSpec describes this correctly

**Step 6: Write DegenerusAdmin.sol findings section**

Add a `## DegenerusAdmin.sol` section to the findings file. For each finding, use this exact format:

```markdown
### CMT-NNN: [Brief title]
- **What:** [The specific comment inaccuracy]
- **Where:** DegenerusAdmin.sol:LINE
- **Why:** [Why a warden would be misled, or why it matters]
- **Suggestion:** [Recommended fix text]
- **Category:** comment-inaccuracy
- **Severity:** INFO | LOW
```

For intent drift findings, use `DRIFT-NNN` prefix and `Category: intent-drift`.

The file MUST include at minimum the 2 pre-identified stale items (line 38 threshold, line 41 death clock). If the audit finds 0 findings total in DegenerusAdmin, something is almost certainly wrong -- the research confirmed at least 2 stale items.
  </action>
  <verify>
    <automated>grep -c '### CMT-\|### DRIFT-' audit/v3.1-findings-31-core-game-contracts.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.1-findings-31-core-game-contracts.md exists
    - File contains `# Phase 31 Findings: Core Game Contracts`
    - File contains `## DegenerusAdmin.sol`
    - File contains at least 2 findings entries (CMT-NNN or DRIFT-NNN) for DegenerusAdmin
    - File contains a finding referencing DegenerusAdmin.sol:38 (stale 60% threshold)
    - File contains a finding referencing DegenerusAdmin.sol:41 (stale death clock pause)
    - Every finding has What, Where, Why, Suggestion, Category, and Severity fields
    - No finding has Category other than `comment-inaccuracy` or `intent-drift`
    - No finding has Severity other than `INFO` or `LOW`
  </acceptance_criteria>
  <done>All NatSpec tags, inline comments, and block comments in DegenerusAdmin.sol (801 lines, 57 NatSpec tags, ~140 comment lines) are verified against current code. Every stale or misleading comment is flagged. Intent drift items are identified. Findings section written with what/why/suggestion per item.</done>
</task>

<task type="auto">
  <name>Task 2: Comment audit and intent drift review for DegenerusGameStorage.sol</name>
  <files>audit/v3.1-findings-31-core-game-contracts.md</files>
  <read_first>
    contracts/storage/DegenerusGameStorage.sol
    audit/v3.1-findings-31-core-game-contracts.md
    .planning/phases/29-comment-documentation-correctness/29-05-SUMMARY.md
  </read_first>
  <action>
Perform the full DegenerusGameStorage.sol comment audit and intent drift review. DegenerusGameStorage.sol has NOT been modified since Phase 29. Phase 29 Plan 05 verified the storage layout diagram byte-accurate and found 0 discrepancies in DOC-03/DOC-04 scope. However, v3.1 focuses on warden-readability and intent drift, which Phase 29 did not specifically address.

**Step 1: Review block comments / storage layout diagram (lines 10-116)**

Read the entire header and storage layout documentation. This is the primary warden-facing documentation for the storage architecture. Check:
- Does the storage layout diagram accurately describe the current slot packing?
- Are all variable names and byte ranges still correct?
- Are any referenced features, mechanisms, or design rationale stale?
- Would a warden reading this diagram understand the storage layout without confusion?
- Check for any references to removed features (activeProposalCount was a uint8 in the storage -- was it in the storage diagram? The removal commit should have updated this)

**Step 2: Review all NatSpec tags in DegenerusGameStorage.sol (218 tags)**

For each @notice, @dev, @param, @return tag:
1. Read the tag text
2. Verify it describes the actual variable, event, or function correctly
3. Pay attention to variables that were involved in removed features or changed mechanics
4. Focus on: scale annotations (BPS, half-BPS, PPM), pool descriptions, game mechanic descriptions
5. Warden-readability: "Would a C4A warden reading this NatSpec understand the variable's purpose and constraints?"

**Step 3: Review all inline comments (~644 comment lines)**

Read through all inline comments. Check for:
- References to removed features or old mechanics
- Stale descriptions of pool behavior, game phases, or token mechanics
- Any comments that describe "how" something works when the "how" has changed
- Scale/unit confusions (BPS vs absolute, ETH vs wei)
- References to constants that have been removed or renamed

**Step 4: Intent drift review for DegenerusGameStorage.sol**

Since this is a storage contract, intent drift here means:
- Storage variables that exist but are no longer written to or read by any active code path
- Variables whose NatSpec describes a purpose that no longer matches how they are actually used
- Storage layout decisions that made sense historically but may confuse wardens (e.g., variables packed for a feature that was removed)
- Any activeProposalCount-related storage that should have been cleaned up but was not

**Step 5: Append DegenerusGameStorage.sol findings section**

Add a `## DegenerusGameStorage.sol` section to the findings file. Use the same CMT-NNN / DRIFT-NNN format as Task 1.

If the review finds zero findings, document that explicitly with a brief note explaining that Phase 29 DOC-03/DOC-04 already verified accuracy and v3.1 found no additional warden-readability or intent drift concerns.

Update the Summary table counts for DegenerusGameStorage.sol row.
  </action>
  <verify>
    <automated>grep '## DegenerusGameStorage.sol' audit/v3.1-findings-31-core-game-contracts.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.1-findings-31-core-game-contracts.md contains `## DegenerusGameStorage.sol`
    - DegenerusGameStorage section has either findings entries OR an explicit "0 findings" note with justification
    - Every finding (if any) has What, Where, Why, Suggestion, Category, and Severity fields
    - Summary table row for DegenerusGameStorage.sol has been updated with actual counts
    - No code changes to contracts/storage/DegenerusGameStorage.sol (flag-only)
  </acceptance_criteria>
  <done>All NatSpec tags (218), inline comments (~644 lines), and block comments in DegenerusGameStorage.sol (1,631 lines) are verified against current code with warden-readability lens. Intent drift items are identified. Findings section appended to the batch deliverable.</done>
</task>

</tasks>

<verification>
- grep 'DegenerusAdmin.sol' audit/v3.1-findings-31-core-game-contracts.md returns results
- grep 'DegenerusGameStorage.sol' audit/v3.1-findings-31-core-game-contracts.md returns results
- grep -c '### CMT-\|### DRIFT-' returns count of findings (at least 2 from known stale items)
- No .sol files in contracts/ directory were modified (git diff --name-only should show only audit/ file)
</verification>

<success_criteria>
- DegenerusAdmin.sol and DegenerusGameStorage.sol fully reviewed for comment accuracy and intent drift
- At least 2 pre-identified stale comments in DegenerusAdmin.sol are flagged (line 38 threshold, line 41 death clock)
- Per-batch findings file created at audit/v3.1-findings-31-core-game-contracts.md with proper format
- Every finding has what/why/suggestion per item
- No code changes made to any contract files
</success_criteria>

<output>
After completion, create `.planning/phases/31-core-game-contracts/31-01-SUMMARY.md`
</output>
