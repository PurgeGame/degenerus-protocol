---
phase: 146-execute-removals
plan: 04
type: execute
wave: 3
depends_on: ["146-01", "146-02", "146-03"]
files_modified:
  - test/unit/DegenerusGame.test.js
  - test/unit/DegenerusAdmin.test.js
  - test/unit/BurnieCoin.test.js
  - test/unit/SecurityEconHardening.test.js
  - test/edge/RngStall.test.js
  - test/integration/GameLifecycle.test.js
  - test/access/AccessControl.test.js
  - test/unit/BurnieCoinflip.test.js
  - test/unit/DegenerusAffiliate.test.js
  - test/unit/EthInvariant.test.js
  - test/unit/DistressLootbox.test.js
  - test/fuzz/VRFPathCoverage.t.sol
  - test/fuzz/VRFStallEdgeCases.t.sol
  - test/fuzz/StallResilience.t.sol
  - test/fuzz/VRFCore.t.sol
  - test/fuzz/TicketLifecycle.t.sol
  - test/fuzz/LootboxRngLifecycle.t.sol
  - test/fuzz/handlers/VRFPathHandler.sol
autonomous: true
requirements: [CLN-03, CLN-04]

must_haves:
  truths:
    - "Full Hardhat test suite passes with 0 failures"
    - "Full Foundry test suite passes with 0 failures"
    - "NatSpec on all rewired callers accurately describes call targets"
  artifacts:
    - path: "test/unit/DegenerusGame.test.js"
      provides: "Updated tests without references to removed view functions"
    - path: "test/fuzz/VRFPathCoverage.t.sol"
      provides: "Foundry tests using storage reads instead of removed views"
  key_links:
    - from: "test/"
      to: "contracts/DegenerusGame.sol"
      via: "function calls in test assertions"
      pattern: "game\\."
---

<objective>
Fix all test failures caused by the 25 function removals across Plans 01-03, update Foundry tests to use alternative accessors, and verify full test suite passes.

Purpose: Ensure the codebase is green after all removals — CLN-03 requires 0 new test failures.
Output: All tests pass; NatSpec current.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/145-candidate-review/145-REVIEW-DECISIONS.md
@.planning/phases/146-execute-removals/146-01-SUMMARY.md
@.planning/phases/146-execute-removals/146-02-SUMMARY.md
@.planning/phases/146-execute-removals/146-03-SUMMARY.md

<interfaces>
<!-- Test files that reference removed functions (from grep analysis): -->

<!-- HARDHAT TESTS referencing removed VIEWS: -->
<!--
  test/unit/DegenerusGame.test.js:
    - rngStalledForThreeDays (lines 80-81, 566-568)
    - autoRebuyEnabledFor (lines 381, 388, 612-614)
    - decimatorAutoRebuyEnabledFor (lines 420, 422, 617-619)
    - lootboxRngIndexView (line 577-579)
    - lootboxRngThresholdView (line 582-584)
    - ethMintLevelCount (line 592-594)
    - ethMintStreakCount (line 597-599)
    - hasActiveLazyPass (line 602-604)
    - lootboxRngWord (line 637-639)

  test/unit/SecurityEconHardening.test.js:
    - rewardPoolView (lines 166, 168, 617)
    - deityPassPurchasedCountFor (lines 183, 193, 211, 214)

  test/edge/RngStall.test.js:
    - rngStalledForThreeDays (lines 449-503, 636-664)

  test/integration/GameLifecycle.test.js:
    - ethMintLastLevel (lines 495-502)
    - ethMintStreakCount (lines 507-514)
    - ethMintLevelCount (lines 518-523)

  test/unit/DegenerusAdmin.test.js:
    - lootboxRngThresholdView (lines 534, 537)
    - References to stakeGameEthToStEth/setLootboxRngThreshold via Admin
-->

<!-- HARDHAT TESTS referencing removed WRAPPERS: -->
<!--
  test/unit/BurnieCoin.test.js: creditFlip, creditFlipBatch, creditLinkReward, previewClaimCoinflips, coinflipAmount, claimableCoin, coinflipAutoRebuyInfo references
  test/access/AccessControl.test.js: creditFlip/creditFlipBatch access tests
  test/unit/DegenerusAffiliate.test.js: may reference coin.creditFlip
  test/unit/EthInvariant.test.js: creditFlip reference
  test/unit/BurnieCoinflip.test.js: may need updated access control tests
-->

<!-- FOUNDRY TESTS referencing removed VIEWS: -->
<!--
  test/fuzz/VRFPathCoverage.t.sol: lootboxRngIndexView (lines 231, 251, 311, 315, 326, 338, 345), lootboxRngWord (line 355)
  test/fuzz/VRFStallEdgeCases.t.sol: lootboxRngIndexView (lines 348, 361, 476, 493, 517), lootboxRngWord (lines 477, 486, 494, 502, 520)
  test/fuzz/StallResilience.t.sol: lootboxRngIndexView (line 178), lootboxRngWord (lines 197, 203, 209)
  test/fuzz/VRFCore.t.sol: lootboxRngIndexView (lines 290, 296, 311, 320, 350, 355, 368, 495, 508, 573, 578, 589)
  test/fuzz/TicketLifecycle.t.sol: lootboxRngWord (lines 687, 807, 1198, 1328, 1334, 1352, 2119), lootboxRngIndexView (line 2098)
  test/fuzz/LootboxRngLifecycle.t.sol: lootboxRngIndexView (line 104), lootboxRngWord (line 109)
  test/fuzz/handlers/VRFPathHandler.sol: lootboxRngIndexView (lines 67, 111, 118, 173, 179, 196, 202), lootboxRngWord (line 184)
-->

<!-- REPLACEMENT STRATEGY for removed views in Foundry tests: -->
<!-- lootboxRngIndexView -> read storage slot directly via vm.load(), or add a test-only helper -->
<!-- lootboxRngWord(uint48) -> read from lootboxRngWords mapping via vm.load() -->
<!-- These are Foundry tests, so vm.load is available for reading storage -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Hardhat test files — remove/rewrite tests for removed functions</name>
  <files>
    test/unit/DegenerusGame.test.js
    test/unit/DegenerusAdmin.test.js
    test/unit/BurnieCoin.test.js
    test/unit/SecurityEconHardening.test.js
    test/edge/RngStall.test.js
    test/integration/GameLifecycle.test.js
    test/access/AccessControl.test.js
    test/unit/BurnieCoinflip.test.js
    test/unit/DegenerusAffiliate.test.js
    test/unit/EthInvariant.test.js
    test/unit/DistressLootbox.test.js
  </files>
  <read_first>
    test/unit/DegenerusGame.test.js
    test/unit/DegenerusAdmin.test.js
    test/unit/BurnieCoin.test.js
    test/unit/SecurityEconHardening.test.js
    test/edge/RngStall.test.js
    test/integration/GameLifecycle.test.js
    test/access/AccessControl.test.js
    test/unit/BurnieCoinflip.test.js
    test/unit/DegenerusAffiliate.test.js
    test/unit/EthInvariant.test.js
    test/unit/DistressLootbox.test.js
  </read_first>
  <action>
For each test file, apply one of these strategies:

**Strategy A: DELETE test** — when the test solely exercises a removed function (e.g., "rngStalledForThreeDays returns false at deployment" — the function is gone, test has no purpose).

**Strategy B: REWRITE test** — when the test exercises kept behavior through a removed function. Replace the removed function call with the equivalent:
- `rewardPoolView()` -> `futurePrizePoolView()` (they returned the same value)
- `ethMintLastLevel(addr)` / `ethMintLevelCount(addr)` / `ethMintStreakCount(addr)` -> `ethMintStats(addr)` (returns a struct with all three)
- `deityPassPurchasedCountFor(addr)` -> read from deityPassPurchased mapping or relevant accessor
- `lootboxRngThresholdView()` -> this is only used in Admin test; rewrite Admin tests to call Game.setLootboxRngThreshold directly as vault owner
- `autoRebuyEnabledFor(addr)` / `decimatorAutoRebuyEnabledFor(addr)` -> check via coinflip contract or relevant state
- `hasActiveLazyPass(addr)` -> delete test or use alternative
- `lootboxRngIndexView()` / `lootboxRngWord(idx)` -> for Hardhat tests (DistressLootbox.test.js), these may need to read from storage or use events

**Strategy C: REWIRE test** — when the test calls a removed forwarding wrapper (e.g., `burnieCoin.creditFlip` -> `burnieCoinflip.creditFlip`):
- Update to call BurnieCoinflip directly
- Update access control tests to verify BurnieCoinflip.onlyFlipCreditors accepts AFFILIATE and ADMIN
- Remove tests that specifically test the forwarding behavior (e.g., "BurnieCoin.creditFlip forwards to BurnieCoinflip")

**For test/unit/DegenerusAdmin.test.js:**
- Tests for stakeGameEthToStEth via Admin: rewrite to call Game.adminStakeEthForStEth directly as vault owner
- Tests for setLootboxRngThreshold via Admin: rewrite to call Game.setLootboxRngThreshold directly as vault owner
- Access control tests: update to verify vault owner can call, non-owner reverts

**For test/access/AccessControl.test.js:**
- Remove BurnieCoin.creditFlip/creditFlipBatch access tests
- Add BurnieCoinflip access tests for AFFILIATE and ADMIN if not already present
- Update any Admin access tests for removed functions

**IMPORTANT:** Run `npx hardhat test` after each batch of file changes to catch compilation errors early. Do NOT change test logic for tests that don't reference removed functions.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && npx hardhat test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx hardhat test` passes with 0 failures
    - No test references any of the 25 removed function names via contract calls
    - Tests that verified forwarding behavior are deleted (not just commented out)
    - Tests that verified underlying behavior are rewritten to use direct calls
    - Access control tests updated for new BurnieCoinflip creditors and vault-owner Game functions
  </acceptance_criteria>
  <done>All Hardhat tests pass with 0 failures after all 25 removals</done>
</task>

<task type="auto">
  <name>Task 2: Fix Foundry test files — replace removed view calls with storage reads</name>
  <files>
    test/fuzz/VRFPathCoverage.t.sol
    test/fuzz/VRFStallEdgeCases.t.sol
    test/fuzz/StallResilience.t.sol
    test/fuzz/VRFCore.t.sol
    test/fuzz/TicketLifecycle.t.sol
    test/fuzz/LootboxRngLifecycle.t.sol
    test/fuzz/handlers/VRFPathHandler.sol
  </files>
  <read_first>
    test/fuzz/VRFPathCoverage.t.sol
    test/fuzz/VRFStallEdgeCases.t.sol
    test/fuzz/StallResilience.t.sol
    test/fuzz/VRFCore.t.sol
    test/fuzz/TicketLifecycle.t.sol
    test/fuzz/LootboxRngLifecycle.t.sol
    test/fuzz/handlers/VRFPathHandler.sol
    contracts/storage/DegenerusGameStorage.sol
  </read_first>
  <action>
The Foundry tests heavily use `game.lootboxRngIndexView()` and `game.lootboxRngWord(idx)` which are now removed. These tests need the actual values for assertions.

**Approach: Create a test-only helper contract** that reads storage directly, OR use `vm.load` to read slots. Determine which is cleaner:

1. Check `contracts/storage/DegenerusGameStorage.sol` to find the storage slot for `lootboxRngIndex` (a uint48) and `lootboxRngWords` (a mapping uint48 => uint256).

2. **Option A — test helper interface:** Create a minimal Solidity helper in `test/helpers/` that wraps vm.load calls. This is reusable across all fuzz tests.

3. **Option B — inline vm.load:** In each test, compute the slot and call `vm.load(address(game), slot)`. For mappings: `keccak256(abi.encode(key, baseSlot))`.

Choose the option that produces the smallest diff. If the storage layout is straightforward and the slot numbers are documented in DegenerusGameStorage.sol, Option B is fine. If not, create a helper.

**For each Foundry test file:**

Replace every `game.lootboxRngIndexView()` with the storage-read equivalent.
Replace every `game.lootboxRngWord(idx)` with the mapping storage-read equivalent.

**For `rngStalledForThreeDays` references in Foundry tests:** grep first — if none exist in Foundry files, skip.

After all changes, run `forge test` to verify. If any test fails for reasons UNRELATED to our changes (pre-existing), note it but do not fix unrelated tests.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge test --no-match-contract "Halmos" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `forge test` passes with 0 new failures
    - No Foundry test file references lootboxRngIndexView or lootboxRngWord as function calls on the game contract
    - Storage reads correctly retrieve the same values the removed views returned
    - All VRF path, stall resilience, and ticket lifecycle assertions preserved
  </acceptance_criteria>
  <done>All Foundry tests pass with 0 failures after view removals</done>
</task>

</tasks>

<verification>
```bash
cd /home/zak/Dev/PurgeGame/degenerus-audit

# Full Hardhat suite
npx hardhat test

# Full Foundry suite (exclude Halmos)
forge test --no-match-contract "Halmos"

# Verify no stale references to removed functions in test files
grep -rn "\.creditFlip\b\|\.creditFlipBatch\b\|\.creditLinkReward\b\|\.claimableCoin()\|\.coinflipAutoRebuyInfo\b" test/ --include="*.js" --include="*.ts" | grep -v "coinflip\." | grep -v "//" && echo "STALE JS REFS" || echo "Clean JS"

grep -rn "\.lootboxRngIndexView()\|\.lootboxRngWord(" test/ --include="*.sol" | grep -v "//" && echo "STALE SOL REFS" || echo "Clean Sol"
```
</verification>

<success_criteria>
- Hardhat test suite: 0 failures
- Foundry test suite: 0 new failures
- No test file calls any of the 25 removed functions on the contract
- NatSpec on all rewired callers is accurate
</success_criteria>

<output>
After completion, create `.planning/phases/146-execute-removals/146-04-SUMMARY.md`
</output>
