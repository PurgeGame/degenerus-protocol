---
phase: 146-execute-removals
plan: 03
type: execute
wave: 2
depends_on: ["146-01", "146-02"]
files_modified:
  - contracts/DegenerusGame.sol
  - contracts/interfaces/IDegenerusGame.sol
  - contracts/interfaces/IDegenerusGameModules.sol
autonomous: true
requirements: [CLN-02, CLN-04]

must_haves:
  truths:
    - "DegenerusGame no longer exposes any of the 16 approved unused view functions"
    - "IDegenerusGame.sol interface matches DegenerusGame.sol — no stale signatures"
  artifacts:
    - path: "contracts/DegenerusGame.sol"
      provides: "Game contract without 16 unused views"
    - path: "contracts/interfaces/IDegenerusGame.sol"
      provides: "Interface without 16 unused view signatures"
  key_links: []
---

<objective>
Remove all 16 approved unused view/pure functions from DegenerusGame.sol and their interface declarations.

Purpose: Eliminate 16 dead view functions from the Game ABI — reduces contract size, removes misleading API surface.
Output: DegenerusGame and IDegenerusGame cleaned of all approved unused views.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/145-candidate-review/145-REVIEW-DECISIONS.md

<interfaces>
<!-- The 16 functions to remove from DegenerusGame.sol: -->
<!-- Each is a standalone view/pure with zero on-chain callers. -->
<!--
  1.  futurePrizePoolTotalView()   — duplicate of futurePrizePoolView
  2.  rewardPoolView()             — duplicate of futurePrizePoolView, confusing name
  3.  lastRngWord()                — dead
  4.  rngStalledForThreeDays()     — dead
  5.  hasActiveLazyPass(address)   — dead
  6.  autoRebuyEnabledFor(address) — dead
  7.  decimatorAutoRebuyEnabledFor(address) — dead
  8.  ethMintLastLevel(address)    — redundant with ethMintStats
  9.  ethMintLevelCount(address)   — redundant with ethMintStats
  10. ethMintStreakCount(address)   — redundant with ethMintStats
  11. deityPassPurchasedCountFor(address) — dead
  12. deityPassTotalIssuedCount()   — dead
  13. lootboxRngIndexView()        — dead
  14. lootboxRngWord(uint48)       — dead
  15. lootboxRngThresholdView()    — dead
  16. lootboxRngMinLinkBalanceView() — dead
-->

<!-- IDegenerusGame.sol contains these (found via grep): -->
<!--
  function lootboxRngWord(uint48 lootboxIndex) external view — line 65
  function ethMintLevelCount(address player) external view — line 70
  function ethMintStreakCount(address player) external view — line 76
  function ethMintLastLevel(address player) external view — line 81
  function futurePrizePoolTotalView() external view — line 153
  function lootboxRngIndexView() external view — line 345
  function lootboxRngThresholdView() external view — line 349
  function lootboxRngMinLinkBalanceView() external view — line 353
  function deityPassPurchasedCountFor(address) external view — line 400
  function deityPassTotalIssuedCount() external view — line 404
-->
<!-- Some may NOT be in IDegenerusGame.sol (rewardPoolView, lastRngWord, rngStalledForThreeDays, etc.) — check both. -->

<!-- IDegenerusGameModules.sol may also contain some — check it. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove 16 unused views from DegenerusGame.sol</name>
  <files>
    contracts/DegenerusGame.sol
  </files>
  <read_first>
    contracts/DegenerusGame.sol
  </read_first>
  <action>
Delete each of these 16 functions along with their NatSpec doc comments. Search for each by name and remove the complete function block (NatSpec + signature + body + closing brace):

1. `futurePrizePoolTotalView()` — search for "function futurePrizePoolTotalView"
2. `rewardPoolView()` — search for "function rewardPoolView"
3. `lastRngWord()` — search for "function lastRngWord"
4. `rngStalledForThreeDays()` — search for "function rngStalledForThreeDays"
5. `hasActiveLazyPass(address)` — search for "function hasActiveLazyPass"
6. `autoRebuyEnabledFor(address)` — search for "function autoRebuyEnabledFor"
7. `decimatorAutoRebuyEnabledFor(address)` — search for "function decimatorAutoRebuyEnabledFor"
8. `ethMintLastLevel(address)` — search for "function ethMintLastLevel"
9. `ethMintLevelCount(address)` — search for "function ethMintLevelCount"
10. `ethMintStreakCount(address)` — search for "function ethMintStreakCount"
11. `deityPassPurchasedCountFor(address)` — search for "function deityPassPurchasedCountFor"
12. `deityPassTotalIssuedCount()` — search for "function deityPassTotalIssuedCount"
13. `lootboxRngIndexView()` — search for "function lootboxRngIndexView"
14. `lootboxRngWord(uint48)` — search for "function lootboxRngWord"
15. `lootboxRngThresholdView()` — search for "function lootboxRngThresholdView"
16. `lootboxRngMinLinkBalanceView()` — search for "function lootboxRngMinLinkBalanceView"

Be careful not to delete:
- `futurePrizePoolView()` (KEPT — different from futurePrizePoolTotalView)
- Storage variables or internal functions that these views read from (those are still used by other code)
- Any section header comments that may sit between functions — leave section structure intact

After deletion, check for orphaned blank lines (3+ consecutive) and collapse to 2 max.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && for fn in futurePrizePoolTotalView rewardPoolView lastRngWord rngStalledForThreeDays hasActiveLazyPass autoRebuyEnabledFor decimatorAutoRebuyEnabledFor ethMintLastLevel ethMintLevelCount ethMintStreakCount deityPassPurchasedCountFor deityPassTotalIssuedCount lootboxRngIndexView lootboxRngWord lootboxRngThresholdView lootboxRngMinLinkBalanceView; do grep -q "function $fn" contracts/DegenerusGame.sol && echo "FAIL: $fn still present"; done; echo "CHECK COMPLETE"</automated>
  </verify>
  <acceptance_criteria>
    - Zero of the 16 function signatures appear in DegenerusGame.sol
    - futurePrizePoolView() (the keeper) is still present
    - Storage variables and internal helpers are untouched
    - No orphaned section headers or excessive blank lines
  </acceptance_criteria>
  <done>All 16 unused view functions removed from DegenerusGame.sol</done>
</task>

<task type="auto">
  <name>Task 2: Remove corresponding interface declarations from IDegenerusGame.sol and IDegenerusGameModules.sol</name>
  <files>
    contracts/interfaces/IDegenerusGame.sol
    contracts/interfaces/IDegenerusGameModules.sol
  </files>
  <read_first>
    contracts/interfaces/IDegenerusGame.sol
    contracts/interfaces/IDegenerusGameModules.sol
  </read_first>
  <action>
**IDegenerusGame.sol — remove these function declarations and their NatSpec:**

Search for each and delete the complete declaration (NatSpec + function signature + semicolon):

1. `futurePrizePoolTotalView` (line ~153)
2. `lootboxRngWord` (line ~65)
3. `ethMintLevelCount` (line ~70)
4. `ethMintStreakCount` (line ~76)
5. `ethMintLastLevel` (line ~81)
6. `lootboxRngIndexView` (line ~345)
7. `lootboxRngThresholdView` (line ~349) — NOTE: setLootboxRngThreshold was already removed in Plan 02; this is the view getter
8. `lootboxRngMinLinkBalanceView` (line ~353)
9. `deityPassPurchasedCountFor` (line ~400)
10. `deityPassTotalIssuedCount` (line ~404)

Also check for and remove (if present):
- `rewardPoolView`
- `lastRngWord`
- `rngStalledForThreeDays`
- `hasActiveLazyPass`
- `autoRebuyEnabledFor`
- `decimatorAutoRebuyEnabledFor`

**IDegenerusGameModules.sol — check and remove any of the 16 if present.**

After cleanup, ensure no duplicate blank lines pile up. Verify the interface still compiles.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && for fn in futurePrizePoolTotalView rewardPoolView lastRngWord rngStalledForThreeDays hasActiveLazyPass autoRebuyEnabledFor decimatorAutoRebuyEnabledFor ethMintLastLevel ethMintLevelCount ethMintStreakCount deityPassPurchasedCountFor deityPassTotalIssuedCount lootboxRngIndexView lootboxRngWord lootboxRngThresholdView lootboxRngMinLinkBalanceView; do grep -q "$fn" contracts/interfaces/IDegenerusGame.sol && echo "FAIL: $fn still in interface"; done; echo "CHECK COMPLETE"</automated>
  </verify>
  <acceptance_criteria>
    - None of the 16 removed function names appear in IDegenerusGame.sol
    - IDegenerusGameModules.sol has no stale references to removed functions
    - Contracts compile: `npx hardhat compile` exits 0
  </acceptance_criteria>
  <done>Interface files cleaned of all 16 removed view function declarations; contracts compile</done>
</task>

</tasks>

<verification>
```bash
cd /home/zak/Dev/PurgeGame/degenerus-audit
# Count removed functions — should be 0 in both files
grep -c "futurePrizePoolTotalView\|rewardPoolView\|lastRngWord\|rngStalledForThreeDays\|hasActiveLazyPass\|autoRebuyEnabledFor\|decimatorAutoRebuyEnabledFor\|ethMintLastLevel\|ethMintLevelCount\|ethMintStreakCount\|deityPassPurchasedCountFor\|deityPassTotalIssuedCount\|lootboxRngIndexView\|lootboxRngWord\|lootboxRngThresholdView\|lootboxRngMinLinkBalanceView" contracts/DegenerusGame.sol contracts/interfaces/IDegenerusGame.sol

# Compile
npx hardhat compile
```
</verification>

<success_criteria>
- All 16 view functions removed from DegenerusGame.sol
- All corresponding interface declarations removed from IDegenerusGame.sol
- IDegenerusGameModules.sol cleaned
- Contracts compile without errors
</success_criteria>

<output>
After completion, create `.planning/phases/146-execute-removals/146-03-SUMMARY.md`
</output>
