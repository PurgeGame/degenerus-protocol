---
phase: 313-tst-vrf-regression-freeze-invariant-fuzz-under-rotation-tst
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fuzz/VrfRotationLiveness.t.sol
autonomous: true
requirements: [VTST-02]
tags: [solidity, foundry, chainlink-vrf, vrf-rotation, liveness, rng-lock, degenerus]

must_haves:
  truths:
    - "`forge test --match-contract VrfRotationLiveness` PASSES (exit 0): after a real emergency rotation, the daily-drain advance gate, requestLootboxRng, and retryLootboxRng all stay reachable — no permanent revert / forced game-over"
    - "The mid-day rotation branch is exercised: after a mid-flight mid-day rotation + fulfilment on the new coordinator, advanceGame drains without RngNotReady() — the re-issued word fills lootboxRngWordByIndex[N] so the :269 drain gate is satisfied"
    - "The daily rotation branch is exercised: after a daily-in-flight rotation (rngWordCurrent==0) + fulfilment on the new coordinator, advanceGame completes the day without RngNotReady() — the re-issued word fills rngWordCurrent so the :271 drain gate is satisfied"
    - "The retryLootboxRng failsafe is exercised as the documented recovery for a NEW coordinator that also stalls (RESEARCH §5 Open Risk 1): a stalled re-issued request is rescued by retryLootboxRng after MIDDAY_RNG_RETRY_TIMEOUT, with no double-index-advance"
  artifacts:
    - path: "test/fuzz/VrfRotationLiveness.t.sol"
      provides: "VTST-02 liveness-after-rotation: mid-day branch, daily branch, daily rngWordCurrent!=0 short-circuit, nothing-in-flight no-op, and retryLootboxRng failsafe all stay live post-rotation"
      contains: "contract VrfRotationLiveness is DeployProtocol"
  key_links:
    - from: "post-rotation advanceGame drain gate"
      to: "no RngNotReady() revert"
      via: "the re-issued request fulfilled on the new coordinator fills [N] (mid-day) or rngWordCurrent (daily), un-blocking the :269/:271 drain gate"
      pattern: "game\\.advanceGame\\(\\)"
---

<objective>
Author `test/fuzz/VrfRotationLiveness.t.sol` proving **VTST-02** (which proves **VRF-02**): after an emergency coordinator/subscription rotation, the protocol stays live. ZERO contracts/ mutation (audit-only per D-43N-AUDIT-ONLY-01); test-tree only.

Cover all three rotation branches of the Phase 312 fix plus the failsafe (per 312-RESEARCH §2/§5 and 311-SPEC §2.2):
1. **Mid-day in flight** (LR_MID_DAY==1): rotation re-issues for the reserved index; fulfilling the re-issued request on the new coordinator fills lootboxRngWordByIndex[N], so the same-day/next-day drain gate (`:269`) no longer reverts RngNotReady() and requestLootboxRng/advance stay reachable.
2. **Daily in flight, rngWordCurrent==0**: rotation re-issues the daily word; fulfilling it on the new coordinator fills rngWordCurrent so the new-day drain gate (`:271`) no longer reverts and the day completes.
3. **Daily already delivered (rngWordCurrent!=0) short-circuit / nothing-in-flight no-op**: rotation does NOT re-issue; the delivered word is preserved and the advance proceeds normally (no orphaned re-issue, no revert).
4. **retryLootboxRng failsafe** (RESEARCH §5 Open Risk 1): if the NEW coordinator also stalls the re-issued mid-day request, retryLootboxRng (after MIDDAY_RNG_RETRY_TIMEOUT) re-fires and rescues it — recoverable, never a permanent freeze. Exercise this explicitly.

Purpose: prove no permanent revert / ~120-day freeze / forced premature game-over after rotation. Output: a passing Foundry test contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/312-impl-vrf-rotation-fix-single-batched-user-approved-diff-impl/312-01-SUMMARY.md
@.planning/phases/312-impl-vrf-rotation-fix-single-batched-user-approved-diff-impl/312-RESEARCH.md
@.planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md

<interfaces>
<!-- Live at HEAD 08c0f2aa. -->

DegenerusGame views: currentDayView() :471, rngWordForDay(uint32) :2183, rngLocked() :2190, lastVrfProcessed() :2202.

AdvanceModule constants (contracts/modules/DegenerusGameAdvanceModule.sol):
  VRF_REQUEST_CONFIRMATIONS = 10 (:122), VRF_MIDDAY_CONFIRMATIONS = 4 (:123)
  MIN_LINK_FOR_LOOTBOX_RNG = 40 ether (:140), MIDDAY_RNG_RETRY_TIMEOUT = 6 hours (:141)
  requestLootboxRng() (:1044), retryLootboxRng() (:1133, reverts if LR_MID_DAY==0; LINK precheck :1138-1141; re-fires :1143; sets fresh rngRequestTime),
  updateVrfCoordinatorAndSub(...) (:1688, admin-only), rawFulfillRandomWords(...) (:1756; :1761 guard; :1768 daily / :1772 mid-day write).
  Drain gate reverts: same-day :213 RngNotReady(); :238 NotTimeYet(); new-day :271 RngNotReady() (after :269 lootboxRngWordByIndex[preIdx]==0).

Storage: rngWordCurrent slot 3, vrfRequestId slot 4, lootboxRngPacked slot 37 (LR_INDEX low bits; LR_MID_DAY bit 224 mask 0xFF), lootboxRngWordByIndex slot 38.

MockVRFCoordinator: createSubscription, addConsumer(subId, game), fundSubscription(subId, amount), requestRandomWords (accepts unconditionally), fulfillRandomWords(reqId, word), lastRequestId(), pendingRequests(reqId)->(subId, consumer, fulfilled), resetFulfilled(reqId).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mid-day + daily rotation-branch liveness — advanceGame drains without RngNotReady() after rotation+fulfilment</name>
  <files>test/fuzz/VrfRotationLiveness.t.sol</files>
  <read_first>
    - test/fuzz/VrfRotationLiveness.t.sol — the file being created (does not exist yet).
    - contracts/modules/DegenerusGameAdvanceModule.sol — updateVrfCoordinatorAndSub (~:1688, the 3-case branch), rawFulfillRandomWords (~:1756, :1768 daily / :1772 mid-day writes), and the drain gate reverts (~:205-238 same-day, ~:263-275 new-day :269/:271). DO NOT modify.
    - test/fuzz/LootboxRngLifecycle.t.sol — `_setupForMidDayRng()`, `_doCoordinatorSwap()`, `_completeDay()`, `_readLootboxRngIndex()`, `_readLootboxWord()`, `_readRngWordCurrent()`. Model directly.
    - test/fuzz/StallResilience.t.sol — `_resumeAfterSwap(newVRF, vrfWord)` (lines ~58-66): advanceGame -> fulfill on newVRF -> loop until unlocked. NOTE: post-fix this helper must fulfil the ALREADY-re-issued request on newVRF, not trigger a fresh one. Read it to understand the post-fix resume shape (the request already exists on newVRF after the rotation).
    - test/fuzz/helpers/DeployProtocol.sol — base contract.
  </read_first>
  <behavior>
    - test_midDayRotation_liveness(uint256 vrfWord): after _setupForMidDayRng() + requestLootboxRng() + a mid-flight rotation to a 2nd MockVRFCoordinator + fulfilling the re-issued request on the new coordinator with vrfWord, advanceGame()/the daily flow proceeds without reverting RngNotReady(); lootboxRngWordByIndex[reservedIndex] == vrfWord; rngLocked() eventually false (drain completes).
    - test_dailyRotation_liveness(uint256 vrfWord): after a daily VRF request is in flight (rngLocked()==true, rngWordCurrent==0) and a rotation re-issues the daily word, fulfilling it on the new coordinator fills rngWordCurrent and the day completes (rngWordForDay(currentDay) != 0); no RngNotReady() revert.
    - test_dailyAlreadyDelivered_shortCircuit(uint256 vrfWord): if a daily word was delivered (rngWordCurrent != 0) BEFORE the rotation, the rotation does NOT re-issue (no new request id on the new coordinator) and the delivered word is preserved; advance proceeds.
    - test_nothingInFlight_noOp(): a rotation with nothing in flight (rngLocked()==false, LR_MID_DAY==0) is a pure config repoint — no new request on the new coordinator, advance proceeds.
  </behavior>
  <action>
    Create `test/fuzz/VrfRotationLiveness.t.sol`: SPDX AGPL-3.0-only, pragma ^0.8.26, `contract VrfRotationLiveness is DeployProtocol`. Imports mirror LootboxRngLifecycle.t.sol (DeployProtocol, MockVRFCoordinator, MintPaymentKind). setUp(): `_deployProtocol(); vm.warp(block.timestamp + 1 days);`.

    Port the helper shapes from LootboxRngLifecycle.t.sol (copy file-local helpers): `_completeDay`, `_setupForMidDayRng`, `_readLootboxRngIndex`, `_readLootboxWord`, `_readRngWordCurrent`. Add a `_rotateTo()` helper returning a freshly-deployed + funded 2nd MockVRFCoordinator after `vm.prank(address(admin)); game.updateVrfCoordinatorAndSub(...)`. Add a `_drainUntilUnlocked()` loop (advanceGame up to 50x while rngLocked()).

    Implement the four tests per the behavior block:
    - test_midDayRotation_liveness: drive mid-day in flight; capture reservedIndex; rotate; assert the re-issued request exists on the new coordinator (lastRequestId()!=0); fulfill it on the new coordinator with vrfWord; assert lootboxRngWordByIndex[reservedIndex]==vrfWord; then advance/drain and assert it does NOT revert RngNotReady() and rngLocked() reaches false. Use vm.assume(vrfWord != 0).
    - test_dailyRotation_liveness: warp to a new day, advanceGame to fire a daily request (rngLocked()==true, _readRngWordCurrent()==0); rotate while daily-in-flight; assert the re-issued daily request exists on the new coordinator; fulfill on the new coordinator; drain; assert rngWordForDay(currentDayView()) != 0 and no RngNotReady().
    - test_dailyAlreadyDelivered_shortCircuit: fire a daily request, fulfill it on the OLD coordinator so rngWordCurrent != 0, THEN rotate; assert the new coordinator's lastRequestId()==0 (no re-issue) and the delivered word is preserved (rngWordCurrent unchanged); advance proceeds.
    - test_nothingInFlight_noOp: from an unlocked steady state (rngLocked()==false, LR_MID_DAY==0), rotate; assert the new coordinator's lastRequestId()==0 and advance proceeds without revert.

    To assert "no RngNotReady() revert" use a positive shape (the drain loop completes and rngLocked() becomes false / the day word is set) rather than vm.expectRevert; the failure mode under the OLD bug is a revert, which would fail the test naturally. Comments describe what IS. ZERO contracts/ mutation.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge test --match-contract VrfRotationLiveness --match-test "test_midDayRotation_liveness|test_dailyRotation_liveness|test_dailyAlreadyDelivered_shortCircuit|test_nothingInFlight_noOp" 2>&1 | grep -E "PASS|FAIL|Suite result"; echo EXIT $?</automated>
  </verify>
  <acceptance_criteria>
    - All four tests PASS in the invocation above (exit 0).
    - test_midDayRotation_liveness asserts lootboxRngWordByIndex[reservedIndex]==vrfWord after fulfilment on the new coordinator AND that the post-rotation drain reaches rngLocked()==false (no permanent revert).
    - test_dailyRotation_liveness asserts rngWordForDay(currentDayView()) != 0 after a daily-in-flight rotation + fulfilment on the new coordinator.
    - test_dailyAlreadyDelivered_shortCircuit asserts the new coordinator's lastRequestId()==0 (no re-issue when rngWordCurrent != 0).
    - test_nothingInFlight_noOp asserts the new coordinator's lastRequestId()==0 (pure config repoint).
    - `git diff --name-only` shows ONLY test/fuzz/VrfRotationLiveness.t.sol.
  </acceptance_criteria>
  <done>All three rotation branches (mid-day, daily-not-delivered, daily-delivered/nothing-in-flight) stay live post-rotation; no RngNotReady() permanent revert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: retryLootboxRng failsafe + requestLootboxRng reachability after rotation</name>
  <files>test/fuzz/VrfRotationLiveness.t.sol</files>
  <read_first>
    - test/fuzz/VrfRotationLiveness.t.sol — extend the file from Task 1.
    - contracts/modules/DegenerusGameAdvanceModule.sol — retryLootboxRng (~:1133): reverts if LR_MID_DAY==0 (:1134); LINK precheck (:1138-1141); re-fires (:1143); sets fresh rngRequestTime; the >= MIDDAY_RNG_RETRY_TIMEOUT (6h) gate. And requestLootboxRng (:1044) + its gates (:1045 rngLockedFlag, :1048 LR_MID_DAY!=0, :1056 rngWordByDay[currentDay]==0). DO NOT modify.
    - test/fuzz/VRFStallEdgeCases.t.sol — `test_retryLootboxRngRescuesStalledMidDay` (lines ~481-545): the canonical retry-rescue pattern (mid-day request, retry reverts before 6h, succeeds at 6h, re-fires without double-advancing the index, the new word resolves the stall). Model the failsafe test on this (it PASSES post-fix — confirmed).
    - .planning/phases/312-impl-vrf-rotation-fix-single-batched-user-approved-diff-impl/312-RESEARCH.md section 5 Open Risk 1 (real coordinator stalling an unfunded re-issued request; retryLootboxRng is the documented failsafe).
  </read_first>
  <behavior>
    - test_retryRescuesStalledReissueAfterRotation(uint256 vrfWord): a mid-day request is in flight; rotate to a NEW coordinator (re-issue fires); the NEW coordinator does NOT fulfil (simulating the new coordinator also stalling). Before MIDDAY_RNG_RETRY_TIMEOUT, retryLootboxRng() reverts; at/after the timeout, retryLootboxRng() succeeds and re-fires a NEW request on the new coordinator WITHOUT advancing lootboxRngIndex; fulfilling that retry request lands a real word in the reserved index; the daily flow then proceeds.
    - test_requestLootboxRngReachableAfterRotation(uint256 vrfWord): after a completed rotation (daily branch, fulfilled, drained to unlocked), a fresh mid-day requestLootboxRng() on the new coordinator succeeds (advances the index, fires a request) — proving the request path is reachable post-rotation.
  </behavior>
  <action>
    Add the two failsafe/reachability tests per the behavior block.

    test_retryRescuesStalledReissueAfterRotation: _setupForMidDayRng(); game.requestLootboxRng(); capture reservedIndex + stalledRequestTime (read rngRequestTime from packed slot 0 bytes [8:14], i.e. uint48(packed >> 64) — mirror LootboxRngLifecycle._readRngRequestTime); rotate to newVRF (fund it >= MIN_LINK_FOR_LOOTBOX_RNG = 40 ether) but DO NOT fulfil; assert retryLootboxRng() reverts before timeout (vm.expectRevert(); game.retryLootboxRng();); warp to >= 6h after the re-issue's rngRequestTime; game.retryLootboxRng() succeeds; assert lootboxRngIndex unchanged by retry (no double-advance) and a new request id appeared on newVRF; fulfill the retry request on newVRF with vrfWord; assert lootboxRngWordByIndex[reservedIndex]==vrfWord; drain; assert rngLocked() reaches false. vm.assume(vrfWord != 0).

    test_requestLootboxRngReachableAfterRotation: complete a daily-branch rotation (re-issue + fulfil + drain to unlocked) so the game is past the rotation; set up a fresh mid-day condition (purchase to create pending lootbox ETH, fund the new coordinator); capture index; game.requestLootboxRng() succeeds; assert lootboxRngIndex incremented by 1 and a request fired on the new coordinator.

    Use the VRFStallEdgeCases retry pattern for timing (LINK funding >= 40 ether so the :1138-1141 precheck in retryLootboxRng passes; the precheck does NOT apply to the rotation re-issue per VER-01, but it DOES apply to retryLootboxRng). Comments describe what IS. ZERO contracts/ mutation.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge test --match-contract VrfRotationLiveness 2>&1 | grep -E "PASS|FAIL|Suite result"; echo EXIT $?</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-contract VrfRotationLiveness` exits 0 with ALL tests PASS (the 4 from Task 1 + these 2).
    - test_retryRescuesStalledReissueAfterRotation asserts retryLootboxRng() reverts before MIDDAY_RNG_RETRY_TIMEOUT and succeeds after, re-fires without advancing lootboxRngIndex, and the retry word lands in the reserved index.
    - test_requestLootboxRngReachableAfterRotation asserts requestLootboxRng() succeeds and advances the index after a completed rotation.
    - `git diff --name-only` shows ONLY test/fuzz/VrfRotationLiveness.t.sol.
  </acceptance_criteria>
  <done>The retryLootboxRng failsafe rescues a stalled re-issue after rotation, and requestLootboxRng stays reachable post-rotation — VTST-02 / VRF-02 fully exercised.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (TEST-INTEGRITY — zero mainnet-contract mutation; no production attack surface)

| Boundary | Description |
|----------|-------------|
| Test harness to AdvanceModule rotation/drain paths | The test must prove liveness via a POSITIVE outcome (drain completes, word set), since the defect mode is a revert |
| New MockVRFCoordinator to rawFulfillRandomWords | The re-issued request id must come from the rotation (contract path), not be fabricated |

## STRIDE Threat Register (test-integrity)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-313-02-01 | Information Disclosure (false assurance) | "no RngNotReady()" asserted weakly | mitigate | Liveness is proven by a POSITIVE outcome — the drain loop reaching rngLocked()==false and the day word / index word being set — so a revert (the OLD-bug failure mode) fails the test naturally rather than passing silently. |
| T-313-02-02 | Tampering (re-issue not actually exercised) | rotation re-issue asserted | mitigate | Each branch asserts the new coordinator's lastRequestId() state (non-zero where a re-issue is expected, zero for short-circuit/no-op), proving the rotation drove the re-issue, not the test. |
| T-313-02-03 | Denial of Service (flaky failsafe timing) | retry timeout boundary | mitigate | Retry timing is anchored to the contract's MIDDAY_RNG_RETRY_TIMEOUT (6h) measured from the re-issue's rngRequestTime read from storage; the boundary (before/after) is asserted deterministically per the VRFStallEdgeCases precedent. |
| T-313-02-SC | Tampering | npm/pip/cargo installs | n/a | NO package installs. Pure Solidity test. Slopcheck not applicable. |

**No production attack surface introduced — zero contracts/ mutation (D-43N-AUDIT-ONLY-01).**
</threat_model>

<verification>
- `forge build` exits 0.
- `forge test --match-contract VrfRotationLiveness` exits 0 — all six tests PASS.
- `git diff --name-only` shows ONLY test/fuzz/VrfRotationLiveness.t.sol; ZERO contracts/ change.
</verification>

<success_criteria>
ROADMAP Phase 313 Success Criterion 2: liveness-after-rotation PASSES — requestLootboxRng/retryLootboxRng/daily-drain advance all succeed post-rotation, no permanent revert (VRF-02 by VTST-02).
</success_criteria>

<output>
Create `.planning/phases/313-tst-vrf-regression-freeze-invariant-fuzz-under-rotation-tst/313-02-SUMMARY.md` when done.
</output>
