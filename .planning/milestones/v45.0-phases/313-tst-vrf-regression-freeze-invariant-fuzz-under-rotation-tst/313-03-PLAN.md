---
phase: 313-tst-vrf-regression-freeze-invariant-fuzz-under-rotation-tst
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fuzz/RngLockRotationDeterminism.t.sol
autonomous: true
requirements: [VTST-03]
tags: [solidity, foundry, chainlink-vrf, vrf-rotation, rng-lock, freeze-invariant, fuzz, determinism, degenerus]

must_haves:
  truths:
    - "forge test --match-contract RngLockRotationDeterminism PASSES (exit 0): a coordinator/sub rotation perturbed between a VRF request and its fulfilment produces a VRF-derived output byte-identical to the no-rotation baseline given the SAME delivered VRF word"
    - "The harness extends the v43 RngLockDeterminism.t.sol pattern — it reuses the snapshot/revert + byte-identity-digest comparison shape (_snapshotPreLock / _revertToPreLock / _assertVrfOutputByteIdentity) and the perturbation discipline, applied to the rotation perturbation specifically"
    - "The rotation is a NON-vm.skip live test (it asserts, not skips): the perturbed run rotates the coordinator mid-window via updateVrfCoordinatorAndSub then delivers the SAME word on the new coordinator; the baseline run delivers that same word on the original coordinator with no rotation; the two VRF-derived digests are byte-identical"
    - "Admin rotation is treated as EXEMPT-class vs PLAYERS per v45-vrf-freeze-invariant — the abandoned old word never enters any digest; the v43 RngLockDeterminism.t.sol file is NOT modified (additive new file only)"
  artifacts:
    - path: "test/fuzz/RngLockRotationDeterminism.t.sol"
      provides: "VTST-03 freeze-invariant fuzz under rotation: perturbed-rotation vs no-rotation byte-identical VRF-derived output, modeled on the v43 RngLockDeterminism harness"
      contains: "contract RngLockRotationDeterminism is DeployProtocol"
  key_links:
    - from: "perturbed-rotation run digest"
      to: "no-rotation baseline digest (byte-identical)"
      via: "updateVrfCoordinatorAndSub mid-window then fulfill SAME word on new coordinator; assertEq(perturbedDigest, baselineDigest)"
      pattern: "assertEq"
---

<objective>
Author `test/fuzz/RngLockRotationDeterminism.t.sol` proving **VTST-03** (which proves **VRF-03**): an emergency coordinator/sub rotation between a VRF request and its fulfilment cannot break the rngLock freeze invariant — every VRF-derived output is byte-identical to the no-rotation baseline. ZERO contracts/ mutation (audit-only per D-43N-AUDIT-ONLY-01); test-tree only.

This EXTENDS the v43 canonical freeze-invariant harness `test/fuzz/RngLockDeterminism.t.sol` (Phase 301). That harness asserts byte-identical VRF-derived outputs under mid-rngLock-window state perturbations via a snapshot/revert + digest-comparison pattern; its `testFuzz_RngLockDeterminism_RetryLootboxRng` is the closest LIVE (non-vm.skip) analog — it perturbs during the retry path and compares digests. This plan does the same for the ROTATION perturbation: rotate the coordinator between request and fulfilment, deliver the SAME VRF word on the new coordinator, and assert the resulting VRF-derived digest is byte-identical to a no-rotation run that delivers that same word on the original coordinator.

The freeze-invariant lens (v45-vrf-freeze-invariant, 311-SPEC §3): the old in-flight word is abandoned by the rawFulfillRandomWords:1761 requestId guard and never consumed; the re-issued word is unpredictable and is the only word consumed this cycle. Admin rotation is EXEMPT-class vs PLAYERS. The byte-identity assertion proves rotation introduces no change to the consumed VRF-derived output beyond the legitimate VRF word substitution itself — given the SAME delivered word, rotation vs no-rotation yields identical outputs.

This is an ADDITIVE new file. It MUST NOT modify `test/fuzz/RngLockDeterminism.t.sol` (the v43 harness, which plan 313-06 confirms still PASSES).

Purpose: prove rotation is freeze-safe (no VRF-participating slot mutated mid-window changes an in-flight VRF-derived output). Output: a passing Foundry fuzz test contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md
@.planning/phases/312-impl-vrf-rotation-fix-single-batched-user-approved-diff-impl/312-RESEARCH.md

<interfaces>
<!-- Live at HEAD 08c0f2aa. -->

RngLockDeterminism.t.sol reusable patterns (test/fuzz/RngLockDeterminism.t.sol — the harness this extends):
  _completeDay(uint256 vrfWord)                          // :73
  _readRngWordCurrent / _readVrfRequestId / _readLootboxRngIndex  // :86 / :90 / :94
  _lootboxRngWord(uint48 index) view returns (uint256)  // :98 (keccak256(abi.encode(index, 38)))
  _advanceToVrfRequestBoundary() returns (uint256 reqId)  // :105 (warp +1d, advanceGame, require rngLocked + pending)
  _deliverMockVrf(uint256 reqId, uint256 word)           // :115 (fulfill if not already, drain loop)
  _snapshotPreLock() returns (uint256)                   // :130 (vm.snapshot)
  _revertToPreLock(uint256 snapshotId)                   // :134 (vm.revertTo)
  _assertVrfOutputByteIdentity(bytes32 perturbed, bytes32 baseline, string label)  // :138 (assertEq)
  testFuzz_RngLockDeterminism_RetryLootboxRng (:1401, NOT skipped) — the snapshot/perturb/deliver/capture-digest/revert/baseline/assert template to mirror.

DegenerusGame views: currentDayView() :471, rngWordForDay(uint32) :2183, rngLocked() :2190.

AdvanceModule (contracts/modules/DegenerusGameAdvanceModule.sol):
  updateVrfCoordinatorAndSub(address,uint256,bytes32) (:1688, admin-only) — the rotation perturbation.
  rawFulfillRandomWords (:1756): :1761 guard rejects a stale requestId or a non-zero rngWordCurrent (abandons old word); :1768 daily write / :1772 mid-day write.

Storage: rngWordCurrent slot 3, vrfRequestId slot 4, lootboxRngPacked slot 37, lootboxRngWordByIndex slot 38.

MockVRFCoordinator: createSubscription, addConsumer(subId, game), fundSubscription(subId, amount), requestRandomWords (unconditional), fulfillRandomWords(reqId, word), lastRequestId(), pendingRequests(reqId) returns (subId, consumer, fulfilled).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the rotation-perturbation byte-identity fuzz harness (daily branch)</name>
  <files>test/fuzz/RngLockRotationDeterminism.t.sol</files>
  <read_first>
    - test/fuzz/RngLockRotationDeterminism.t.sol — the file being created (does not exist yet).
    - test/fuzz/RngLockDeterminism.t.sol — read the harness header + slot constants (lines ~36-67), the shared helpers (_completeDay, _readRngWordCurrent, _advanceToVrfRequestBoundary, _deliverMockVrf, _snapshotPreLock, _revertToPreLock, _assertVrfOutputByteIdentity — lines ~62-144), and the FULL testFuzz_RngLockDeterminism_RetryLootboxRng (lines ~1401-1544) as the template (snapshot then request then perturb then deliver word then drain then capture digest then revert then baseline path then capture digest then assert byte-identity).
    - contracts/modules/DegenerusGameAdvanceModule.sol — updateVrfCoordinatorAndSub (~:1688) + rawFulfillRandomWords (~:1756, :1761 guard, :1768 daily write). DO NOT modify.
    - .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md section 3 (freeze-invariant disposition: old word abandoned, new word unpredictable, consumed-this-cycle is the fresh word, admin EXEMPT).
  </read_first>
  <behavior>
    - testFuzz_RotationFreezeInvariant_Daily(uint256 vrfWord, uint256 rotSeed): with vrfWord bounded non-zero, two runs from a common pre-request snapshot must produce a byte-identical VRF-derived digest. Run A (perturbed): advance to a daily VRF-request boundary; rotate the coordinator mid-window via updateVrfCoordinatorAndSub to a 2nd MockVRFCoordinator; deliver vrfWord on the NEW coordinator; drain; capture a digest of the VRF-derived daily outputs. Run B (baseline, no rotation): revert to the snapshot; advance to the same boundary; deliver vrfWord on the ORIGINAL coordinator; drain; capture the same digest. assertEq(digestA, digestB).
    - rotSeed fuzzes the rotation newKeyHash/newSubId (which MUST NOT influence the VRF-derived output — the freeze invariant); varying them must leave the digest identical.
  </behavior>
  <action>
    Create `test/fuzz/RngLockRotationDeterminism.t.sol`: SPDX AGPL-3.0-only, pragma ^0.8.26, `contract RngLockRotationDeterminism is DeployProtocol`. Imports + setUp() mirror RngLockDeterminism.t.sol (DeployProtocol, VRFHandler, MockVRFCoordinator, MintPaymentKind; setUp deploys protocol, warps +1 day, funds subscription 1). Copy the needed file-local helpers from RngLockDeterminism.t.sol VERBATIM into this new file (do NOT import them — they are file-local there): the slot constants, _completeDay, _readRngWordCurrent, _readVrfRequestId, _readLootboxRngIndex, _lootboxRngWord, _advanceToVrfRequestBoundary, _deliverMockVrf, _snapshotPreLock, _revertToPreLock, _assertVrfOutputByteIdentity.

    Add a _rotateMidWindow(uint256 seed) returns (MockVRFCoordinator) helper that deploys + funds a 2nd MockVRFCoordinator and admin-pranks updateVrfCoordinatorAndSub(newCoord, newSub, bytes32(seed)). Add _captureDailyVrfDigest() returns (bytes32) that hashes the post-drain VRF-derived daily state (currentDayView, rngWordForDay(currentDayView), rngWordCurrent) into one bytes32.

    Implement testFuzz_RotationFreezeInvariant_Daily per the behavior block, mirroring testFuzz_RngLockDeterminism_RetryLootboxRng: snapshot before the request; Run A perturbs with the rotation, delivers vrfWord on the NEW coordinator (use newVRF.lastRequestId() for the re-issued request, NOT the abandoned old request), drains, captures digestA; revert; Run B delivers vrfWord on the ORIGINAL coordinator with no rotation, drains, captures digestB; _assertVrfOutputByteIdentity(digestA, digestB, "Rotation daily byte-identity VTST-03 VRF-03"). Use vm.assume(vrfWord != 0). Use vm.assume(false) to filter unreachable iterations (mirror the harness filters) but ensure the common case is reachable so the fuzz runs count is non-zero.

    Deliver the SAME vrfWord in both runs so the only difference is the rotation. Comments describe what IS. ZERO contracts/ mutation; do NOT edit RngLockDeterminism.t.sol.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge test --match-test testFuzz_RotationFreezeInvariant_Daily 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - forge test --match-test testFuzz_RotationFreezeInvariant_Daily exits 0 with the fuzz test PASS and a non-zero runs count (the vm.assume filters must not starve all iterations).
    - The test compares two digests (rotation-perturbed vs no-rotation) computed over VRF-derived daily state and asserts byte-identity via assertEq / _assertVrfOutputByteIdentity.
    - The perturbed run delivers the word on the NEW coordinator (newVRF.lastRequestId()), the baseline on the ORIGINAL coordinator; the SAME vrfWord is delivered in both runs.
    - git diff --name-only shows ONLY test/fuzz/RngLockRotationDeterminism.t.sol; RngLockDeterminism.t.sol is NOT modified.
  </acceptance_criteria>
  <done>The daily-branch rotation perturbation produces byte-identical VRF-derived output vs the no-rotation baseline; the harness mirrors the v43 snapshot/digest pattern; the v43 file is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mid-day rotation byte-identity (LR_INDEX frozen across the window)</name>
  <files>test/fuzz/RngLockRotationDeterminism.t.sol</files>
  <read_first>
    - test/fuzz/RngLockRotationDeterminism.t.sol — extend the file from Task 1.
    - test/fuzz/RngLockDeterminism.t.sol — re-read the RetryLootboxRng test (~:1401-1544) for the mid-day lootbox request + per-index word capture shape, and confirm the existing harness vm.skip discipline (most tests are vm.skip(true); the live ones are RetryLootboxRng + StakedStonkRedemption).
    - test/fuzz/LootboxRngLifecycle.t.sol — _setupForMidDayRng() for the mid-day-eligible state setup (complete 2 days, lootbox purchase, fund VRF).
    - contracts/modules/DegenerusGameAdvanceModule.sol — rawFulfillRandomWords :1772 mid-day write (lootboxRngWordByIndex[index]=word). DO NOT modify.
    - .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md section 3.2 (no freeze-breaking mid-window mutation; LR_INDEX frozen across the window).
  </read_first>
  <behavior>
    - testFuzz_RotationFreezeInvariant_MidDay(uint256 vrfWord, uint256 rotSeed): with vrfWord bounded non-zero, two runs from a common pre-mid-day-request snapshot must produce a byte-identical lootbox-index-word digest. Run A fires requestLootboxRng, rotates mid-flight, delivers vrfWord on the new coordinator (re-issued mid-day word lands in the preserved index N); Run B fires requestLootboxRng, delivers vrfWord on the original coordinator with NO rotation. The reserved index N is identical across runs (LR_INDEX frozen by rotation), and lootboxRngWordByIndex[N] is byte-identical (== vrfWord) across runs.
  </behavior>
  <action>
    Add testFuzz_RotationFreezeInvariant_MidDay per the behavior block, mirroring Task 1 snapshot/revert/digest structure but for the mid-day path. Port _setupForMidDayRng from LootboxRngLifecycle.t.sol (complete 2 days, purchase lootbox ETH, fund VRF) and run it BEFORE the snapshot. Run A: requestLootboxRng; capture N = lootboxRngIndex - 1; rotate mid-flight via _rotateMidWindow(rotSeed); deliver vrfWord on the new coordinator; assert lootboxRngWordByIndex[N] == vrfWord; capture digestA from keccak(N, lootboxRngWordByIndex[N]). Revert. Run B: requestLootboxRng; capture N2 = lootboxRngIndex - 1; deliver vrfWord on the original coordinator (no rotation); capture digestB from keccak(N2, lootboxRngWordByIndex[N2]). Assert N == N2 (LR_INDEX frozen) and _assertVrfOutputByteIdentity(digestA, digestB, "Rotation mid-day byte-identity VTST-03 VRF-03"). vm.assume(vrfWord != 0); vm.assume filters for unreachable mid-day setup (mirror RetryLootboxRng try/catch + vm.assume(false) filters).

    Comments describe what IS. ZERO contracts/ mutation; do NOT edit test/fuzz/RngLockDeterminism.t.sol (it stays additive — plan 313-06 verifies the v43 harness still PASSES).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge test --match-contract RngLockRotationDeterminism 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - forge test --match-contract RngLockRotationDeterminism exits 0 with BOTH fuzz tests (testFuzz_RotationFreezeInvariant_Daily + testFuzz_RotationFreezeInvariant_MidDay) PASS, each with a non-zero runs count.
    - testFuzz_RotationFreezeInvariant_MidDay asserts the reserved index N is identical across the rotation-perturbed and no-rotation runs (LR_INDEX frozen) AND lootboxRngWordByIndex[N] is byte-identical (== vrfWord) across runs.
    - The new file does NOT modify test/fuzz/RngLockDeterminism.t.sol (git diff --name-only shows ONLY test/fuzz/RngLockRotationDeterminism.t.sol).
    - ZERO contracts/ change.
  </acceptance_criteria>
  <done>The mid-day-branch rotation perturbation produces byte-identical VRF-derived output + identical reserved index vs the no-rotation baseline; the v43 harness file is untouched — VTST-03 / VRF-03 proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (TEST-INTEGRITY — zero mainnet-contract mutation; no production attack surface)

| Boundary | Description |
|----------|-------------|
| Test harness to AdvanceModule rotation + fulfilment | Both runs must deliver the SAME word; the perturbed run delivers on the NEW coordinator (the re-issued request), the baseline on the original |
| New MockVRFCoordinator to rawFulfillRandomWords | The abandoned old word must never enter the digest (the :1761 guard ensures this; the test never delivers the old request in Run A) |

## STRIDE Threat Register (test-integrity)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-313-03-01 | Tampering (digest captures the wrong word) | byte-identity comparison | mitigate | Both runs deliver the SAME vrfWord; the perturbed run delivers it via the re-issued request on the new coordinator (newVRF.lastRequestId()), so the digest reflects the consumed-this-cycle VRF-derived output, not the abandoned old word (rejected by :1761). |
| T-313-03-02 | Information Disclosure (false-pass via starved fuzz) | vm.assume filters | mitigate | The acceptance criterion requires a non-zero runs count; the common-case setup must be reachable so the byte-identity assertion actually executes (mirrors the RetryLootboxRng harness filters which keep runs non-zero). |
| T-313-03-03 | Denial of Service (flaky fuzz) | non-deterministic seed making byte-identity intermittent | mitigate | Determinism is enforced by delivering the SAME vrfWord in both runs and using vm.snapshot/vm.revertTo to share identical pre-request state; rotSeed only varies the rotation config (which the invariant requires NOT to affect the output), so any drift is a real freeze-invariant violation, not flakiness. |
| T-313-03-SC | Tampering | npm/pip/cargo installs | n/a | NO package installs. Pure Solidity test. Slopcheck not applicable. |

**No production attack surface introduced — zero contracts/ mutation (D-43N-AUDIT-ONLY-01).**
</threat_model>

<verification>
- forge build exits 0.
- forge test --match-contract RngLockRotationDeterminism exits 0 — both fuzz tests PASS with non-zero runs.
- git diff --name-only shows ONLY test/fuzz/RngLockRotationDeterminism.t.sol; the v43 RngLockDeterminism.t.sol is NOT modified; ZERO contracts/ change.
</verification>

<success_criteria>
ROADMAP Phase 313 Success Criterion 3: freeze-invariant fuzz under rotation PASSES — every VRF-derived output byte-identical to the no-rotation baseline across perturbed rotations between request and fulfilment (VRF-03 by VTST-03).
</success_criteria>

<output>
Create `.planning/phases/313-tst-vrf-regression-freeze-invariant-fuzz-under-rotation-tst/313-03-SUMMARY.md` when done.
</output>
