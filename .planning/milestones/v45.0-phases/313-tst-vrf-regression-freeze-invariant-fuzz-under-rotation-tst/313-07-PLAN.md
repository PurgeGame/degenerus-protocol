---
phase: 313-tst-vrf-regression-freeze-invariant-fuzz-under-rotation-tst
plan: 07
type: execute
wave: 3
depends_on: ['313-01', '313-02', '313-03', '313-04', '313-05', '313-06']
files_modified:
  - test/fuzz/VRFCore.t.sol
  - test/fuzz/VRFPathCoverage.t.sol
  - test/fuzz/VRFStallEdgeCases.t.sol
  - test/fuzz/VrfRotationOrphanIndex.t.sol
  - test/fuzz/VrfWireOneShot.t.sol
  - foundry.toml
autonomous: true
requirements: [VTST-01]
tags: [solidity, foundry, chainlink-vrf, rng-lock, test-integrity, review-fix, degenerus]

must_haves:
  truths:
    - "CR-01/CR-02 closed: VRFCore and VRFPathCoverage read lootboxRngIndex from slot 37 (low 48 bits of lootboxRngPacked) and lootboxRngWordByIndex from the keccak(index,38) mapping slot — no LR_INDEX/word assertion in those files remains vacuous (assertEq(0,0)); a fix that double-incremented LR_INDEX would now be caught"
    - "After the slot corrections, any affected test that newly fails is correctly classified: a fix-induced regression is migrated to preserve+re-issue (313-05 pattern); a genuine behavioral mismatch HALTS for report (never masked, never made to pass by weakening an assertion)"
    - "WR-02 closed: the VTST-01 pre-fix arm reproduces the orphan defect non-tautologically — it stores a NONZERO sentinel into the reserved index to simulate a fillable slot, exercises the path, and asserts the orphan/entropy-0 consequence in a way that would NOT pass if the slot were genuinely populated by a real word"
    - "WR-03 closed: the VTST-04 structural one-shot test actually verifies exactly one wireVrf call site — foundry.toml grants read fs_permissions for ./contracts so vm.readFile succeeds, and the test no longer relies on a vacuous catch branch that merely re-asserts lastVrfProcessed()!=0"
    - "WR-01 closed: the VRFStallEdgeCases _readVrfRequestId comment states slot 4 (matching SLOT_VRF_REQUEST_ID = 4), describing what IS"
    - "SC-5 still holds with the honest assertions: re-running the corrected HEAD test tree against the pre-fix contract 41546f16 shows the HEAD failing-function set is still a subset of the pre-fix baseline (0 NEW failures); forge build exit 0; the v43 RngLockDeterminism.t.sol harness and the 4 new VTST contracts still PASS"
    - "ZERO contracts/ mainnet mutation across this plan (foundry.toml and test/** only)"
  artifacts:
    - path: "test/fuzz/VRFCore.t.sol"
      provides: "_lootboxRngIndex() reads slot 37; previously-vacuous LR_INDEX preservation assertions now bind to real state"
      contains: "37"
    - path: "test/fuzz/VRFPathCoverage.t.sol"
      provides: "_lootboxRngIndex() slot 37 + _lootboxRngWord() keccak(index,38) mapping read; index-lifecycle/gap-backfill assertions non-vacuous"
      contains: "keccak256"
    - path: "test/fuzz/VrfRotationOrphanIndex.t.sol"
      provides: "non-tautological pre-fix arm (nonzero sentinel reproduction)"
      contains: "sentinel"
    - path: "test/fuzz/VrfWireOneShot.t.sol"
      provides: "structural one-shot test that actually greps the source for a single wireVrf call site"
      contains: "wireVrf"
    - path: "foundry.toml"
      provides: "fs_permissions read access to ./contracts so vm.readFile works for the structural attestation"
      contains: "fs_permissions"
  key_links:
    - from: "corrected slot-37 read in VRFCore/VRFPathCoverage"
      to: "LR_INDEX preservation assertions bind to lootboxRngPacked, not the always-zero mapping root"
      via: "vm.load(game, bytes32(uint256(37))) low 48 bits"
      pattern: "uint256(37)"
    - from: "foundry.toml fs_permissions read ./contracts"
      to: "VTST-04 structural test greps DegenerusAdmin source for exactly one gameAdmin.wireVrf( call site"
      via: "vm.readFile no longer reverts → the try-branch (real grep) executes"
      pattern: "fs_permissions"
tags_note: "Review-driven cleanup pass closing 313-REVIEW.md findings CR-01, CR-02, WR-01, WR-02, WR-03 (all advisory). Source of truth for the fixes: ${phase_dir}/313-REVIEW.md."
---

<objective>
Close all five findings in `313-REVIEW.md` (CR-01, CR-02, WR-01, WR-02, WR-03) so the Phase 313 regression suite is honest — no vacuous `assertEq(0,0)` preservation assertions, a non-tautological VTST-01 pre-fix arm, and a VTST-04 structural attestation that actually runs. AUDIT-ONLY: ZERO `contracts/` mainnet mutation; only `test/**` and `foundry.toml`. AGENT-COMMITTED.

The new VTST files already prove VRF-01..05 soundly (verifier passed 5/5); this pass removes false assurance in the migrated/auxiliary tests that 313-05 left inconsistent (it applied the identical slot fix to StallResilience but not to VRFCore/VRFPathCoverage). The authoritative storage layout (from `forge inspect DegenerusGame storage-layout`): lootboxRngPacked = slot 37 (low 48 bits = LR_INDEX), lootboxRngWordByIndex = slot 38 (mapping), lootboxDay = slot 39 (mapping), rngWordCurrent = slot 3, vrfRequestId = slot 4.
</objective>

<tasks>
<task type="auto">
<name>CR-01 + CR-02: correct vacuous storage-slot reads; reclassify any newly-failing affected tests</name>
<read_first>
- .planning/phases/313-tst-vrf-regression-freeze-invariant-fuzz-under-rotation-tst/313-REVIEW.md (CR-01 §50-66, CR-02 §70-97 — exact fixes)
- .planning/phases/313-tst-vrf-regression-freeze-invariant-fuzz-under-rotation-tst/313-05-SUMMARY.md (the preserve+re-issue migration pattern + the pre-fix baseline classification methodology)
- test/fuzz/VRFCore.t.sol (the _lootboxRngIndex helper near :53; the retryDetection/timeout tests that use it)
- test/fuzz/VRFPathCoverage.t.sol (the _lootboxRngIndex/_lootboxRngWord helpers near :55-63; index-lifecycle/gap-backfill tests)
- test/fuzz/StallResilience.t.sol (the already-correct slot-37/38 helper shape to mirror)
- contracts/modules/DegenerusGameAdvanceModule.sol (READ-ONLY — confirm LR_INDEX lives in lootboxRngPacked; grep, do not modify)
</read_first>
<action>
In test/fuzz/VRFCore.t.sol change `_lootboxRngIndex()` to read storage slot 37 (was 38) and return `uint48(uint256(vm.load(address(game), bytes32(uint256(37)))))`. In test/fuzz/VRFPathCoverage.t.sol change `_lootboxRngIndex()` to slot 37 (was 38) AND `_lootboxRngWord(index)` to read `vm.load(address(game), keccak256(abi.encode(uint256(index), uint256(38))))` (was the slot-39 form). Mirror the exact helper shape already correct in StallResilience.t.sol. Comments must describe what IS (the slot), not the history.
Then run the affected tests. For each test that NEWLY fails after the slot correction, classify with the 313-05 methodology (compare against the pre-fix contract 41546f16): if it failed pre-fix because it encoded OLD blanket-reset behavior → migrate it to preserve+re-issue exactly like 313-05; if it reveals a genuine behavioral mismatch unrelated to the fix → HALT and report (do NOT weaken the assertion to force a pass, do NOT mask). Expected outcome: the previously-vacuous preservation assertions now bind to real state and PASS because the fix is correct (the new VTST tests already confirm index preservation with slot 37/38).
</action>
<verify>
<automated>forge build && forge test --match-contract VRFCore -vv && forge test --match-contract VRFPathCoverage -vv && grep -n "uint256(37)" test/fuzz/VRFCore.t.sol test/fuzz/VRFPathCoverage.t.sol && ! grep -nE "vm.load\(address\(game\), bytes32\(uint256\(38\)\)\)" test/fuzz/VRFCore.t.sol</automated>
</verify>
<acceptance_criteria>
- `grep -n "uint256(37)"` matches in both VRFCore.t.sol and VRFPathCoverage.t.sol `_lootboxRngIndex()`.
- No `_lootboxRngIndex()` in either file still reads slot 38; no `_lootboxRngWord()` in VRFPathCoverage still reads slot 39.
- `forge build` exits 0.
- Every affected test either PASSES with the corrected slot, or is migrated (313-05 preserve+re-issue pattern) and then PASSES; any genuine mismatch is reported, not masked.
- ZERO contracts/ mutation (`git diff --name-only -- contracts/` empty).
</acceptance_criteria>
</task>

<task type="auto">
<name>WR-01/WR-02/WR-03: comment fix, non-tautological pre-fix arm, real structural attestation; then full re-verify</name>
<read_first>
- .planning/phases/313-tst-vrf-regression-freeze-invariant-fuzz-under-rotation-tst/313-REVIEW.md (WR-01 §103-114, WR-02 §118-125, WR-03 §129-139)
- test/fuzz/VRFStallEdgeCases.t.sol (the _readVrfRequestId comment near :94 and SLOT_VRF_REQUEST_ID near :16)
- test/fuzz/VrfRotationOrphanIndex.t.sol (the pre-fix arm test_preFix_orphanedZeroIndex_yieldsEntropyZero near :185-199; the sound post-fix arm to contrast against)
- test/fuzz/VrfWireOneShot.t.sol (test_structuralOneShot_wireVrfOnlyFromConstructor near :113-131; the try/catch on vm.readFile)
- foundry.toml (no fs_permissions key currently)
- contracts/DegenerusAdmin.sol (READ-ONLY — confirm exactly one `gameAdmin.wireVrf(` call site in the constructor)
</read_first>
<action>
WR-01: in test/fuzz/VRFStallEdgeCases.t.sol fix the `_readVrfRequestId` doc comment to say "storage slot 4" (matching SLOT_VRF_REQUEST_ID = 4); code already correct.
WR-02: rewrite the VTST-01 pre-fix arm in VrfRotationOrphanIndex.t.sol so it is NOT tautological. Instead of `vm.store(0)` into an already-zero slot, store a NONZERO sentinel word into the reserved `lootboxRngWordByIndex[reservedIndex]` (keccak(index,38)) to simulate a slot that WAS fillable, then reproduce the pre-fix orphaning consequence and assert the entropy-0 / deterministic-trait outcome in a form that would FAIL if a real VRF word were present — i.e. the assertion must distinguish the orphaned state from a genuinely-fulfilled state. Keep the sound post-fix arm unchanged. Document the reproduction shape in a comment (what IS).
WR-03: add `fs_permissions = [{ access = "read", path = "./contracts" }]` to the `[profile.default]` (or appropriate profile) section of foundry.toml, then make test_structuralOneShot_wireVrfOnlyFromConstructor actually assert exactly ONE `gameAdmin.wireVrf(` call site from the vm.readFile grep (the try-branch). The catch branch must NOT silently pass as an equivalent fallback — if vm.readFile still reverts, the test must `fail()` (forcing the read to be enabled), per REVIEW.md WR-03 option 3, so the structural invariant is genuinely proven.
Then RE-VERIFY the whole phase honestly: forge build; the 4 new VTST contracts pass; the v43 RngLockDeterminism.t.sol harness passes and is byte-identical; and re-run the empirical SC-5 check (corrected HEAD test tree vs the pre-fix contract 41546f16, restored byte-identically afterward, never committed) to confirm the HEAD failing-function set is still a subset of the pre-fix baseline (0 NEW failures) now that the assertions are honest.
</action>
<verify>
<automated>forge build && grep -n "fs_permissions" foundry.toml && forge test --match-contract VrfWireOneShot -vv && forge test --match-contract VrfRotationOrphanIndex -vv && forge test --match-path test/fuzz/RngLockDeterminism.t.sol -vv && test -z "$(git diff --name-only -- contracts/)"</automated>
</verify>
<acceptance_criteria>
- `grep -n "fs_permissions" foundry.toml` matches; `vm.readFile` of a contracts/ path no longer reverts.
- VRFStallEdgeCases `_readVrfRequestId` comment reads "slot 4".
- The VTST-01 pre-fix arm uses a nonzero sentinel and its assertion would fail if a real VRF word were present (non-tautological); the post-fix arm is unchanged and still passes.
- VrfWireOneShot structural test executes the real source grep (try-branch) and asserts exactly one `gameAdmin.wireVrf(` call site; the catch branch calls `fail()` rather than silently passing.
- `forge build` exit 0; the 4 new VTST contracts and the v43 RngLockDeterminism.t.sol harness all pass; the harness file is byte-identical (unmodified).
- Empirical SC-5 re-check: corrected HEAD failing-function set is a subset of the pre-fix 41546f16 baseline (0 NEW failures); the pre-fix contract is restored byte-identically and never committed.
- ZERO contracts/ mainnet mutation (only test/** and foundry.toml changed).
</acceptance_criteria>
</task>
</tasks>

<threat_model>
ASVS L1 — test-integrity threats (this plan mutates zero mainnet contracts; only test/** and foundry.toml).

- **T-313-07-01 — Masking instead of fixing.** Risk: when the slot correction turns a vacuous test real and it fails, an executor "fixes" it by weakening/deleting the assertion, re-introducing false assurance. Mitigation: must_haves require classify-then-migrate (313-05 preserve+re-issue pattern) or HALT-and-report; weakening an assertion to force a pass is prohibited.
- **T-313-07-02 — fs_permissions over-grant.** Risk: granting broad filesystem access in foundry.toml widens the test sandbox. Mitigation: read-only access scoped to ./contracts; no write access, no broader path.
- **T-313-07-03 — Sentinel pre-fix arm still tautological.** Risk: the nonzero-sentinel rewrite asserts something trivially true. Mitigation: acceptance requires the assertion distinguish the orphaned state from a genuinely-fulfilled state (would fail if a real word were present).
- **T-313-07-04 — SC-5 drift.** Risk: honest assertions introduce a NEW failure vs the pre-fix baseline that gets silently accepted. Mitigation: re-run the empirical pre-fix-vs-HEAD subset check after the fixes; any net-new failure HALTS.

No production attack surface introduced (audit-only; zero contracts/ mainnet mutation).
</threat_model>
