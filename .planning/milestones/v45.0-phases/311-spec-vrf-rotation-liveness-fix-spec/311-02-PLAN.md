---
phase: 311-spec-vrf-rotation-liveness-fix-spec
plan: 02
type: execute
wave: 2
depends_on: ["311-01"]
files_modified:
  - .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md
autonomous: true
requirements: [VRF-01, VRF-02, VRF-03, VRF-04, VRF-05]

must_haves:
  truths:
    - "§1 contains a design-intent backward-trace of updateVrfCoordinatorAndSub + wireVrf across Scenario A (same-day → entropy-0 traits) and Scenario B (next-day → freeze + forced game-over)"
    - "§2 locks the re-issue-in-flight fix shape (D-01) with surgical preserve+re-issue for BOTH daily and mid-day paths (D-02), chosen WITH rationale vs the rejected pendingVrfRotationPacked queue+apply split — closing VRF-01 and VRF-02"
    - "§3 locks the freeze-invariant disposition (VRF-03): no VRF-participating slot mutated mid-window in a freeze-breaking way; validator-influenceable entropy backfill (timestamp/newKeyHash) EXPLICITLY REJECTED"
    - "§4 specifies the wireVrf one-shot init lock (D-03, VRF-04), the _setVrfConfig shared-internal dedup (D-04), and the vault-routed reach disposition (VRF-05)"
    - "§5 explicitly traces and CONFIRMS re-issue restores reachability of the existing :1208 backfill (D-05), states the LR_MID_DAY ≤1-orphan bound, and includes the escalation gate"
    - "§6 records the two rejected options (queue+apply pendingVrfRotationPacked; belt-and-suspenders gate-independent backfill) each with rationale"
    - "totalFlipReversals carry-over (:1711-1714) is preserved by the design, not designed away"
    - "Every design assertion cites a §0 manifest row; zero by-construction claims; zero contracts/ + zero test/ mutations"
  artifacts:
    - path: ".planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md"
      provides: "Complete VRF-rotation fix design: §1 backward-trace, §2 locked fix shape, §3 freeze disposition, §4 wireVrf lock + dedup + vault reach, §5 orphan-recovery breadth, §6 rejected options"
      contains: "LOCKED Fix Shape"
  key_links:
    - from: "311-SPEC.md §2 fix shape"
      to: "VRF-01 + VRF-02 closure"
      via: "re-issue lands real word in lootboxRngWordByIndex[N] + preserves liveness"
      pattern: "re-issue|lootboxRngWordByIndex"
    - from: "311-SPEC.md §5 reachability trace"
      to: "the :1208 _backfillOrphanedLootboxIndices call + :269/:238 drain gate"
      via: "re-issue un-blocks the gate → backfill reachable, with escalation clause"
      pattern: "_backfillOrphanedLootboxIndices|escalat"
---

<objective>
Author §1–§6 of `311-SPEC.md` — the complete VRF-rotation fix design — consuming the §0 grep-verified manifest authored in Plan 01. This locks the design that drives Phase 312 IMPL: the design-intent backward-trace across both orphan scenarios, the re-issue-in-flight fix shape (D-01/D-02) with rationale vs the rejected queue+apply split, the freeze-invariant disposition (VRF-03) with validator-influenceable backfill rejected, the wireVrf one-shot lock + `_setVrfConfig` dedup + vault-routed reach (D-03/D-04/VRF-04/VRF-05), the D-05 orphan-recovery reachability confirmation with escalation gate, and the rejected-options record.

Purpose: Produce the single load-bearing SPEC input for Phase 312 IMPL. D-01..D-05 are LOCKED inputs — the SPEC refines mechanics (exact re-issue call, wired-detection mechanism, `_setVrfConfig` signature/visibility) but MUST NOT silently reverse a locked decision. The ONLY sanctioned reversal is D-05's escalation clause (if the §5 reachability trace finds a residual orphan path that re-issue + the :1208 backfill do NOT cover, escalate to belt-and-suspenders WITH explicit rationale).
Output: `311-SPEC.md` with §1–§6 fully authored, every assertion cited to a §0 manifest row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-CONTEXT.md

# The §0 manifest authored by Plan 01 — every §1–§6 assertion cites a row here
@.planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md

# Sibling SPEC format precedent (design-section prose style)
@.planning/phases/309-spec-locked-layout-bonus-only-cap-shared-cap-disposition-spe/309-SPEC.md

# D-04 grounding — the Phase 310 single-source-of-truth (consolidate shared logic) precedent
@.planning/phases/310-implementation-single-batched-user-approved-contract-diff-im/310-CONTEXT.md

<locked_decisions>
<!-- From 311-CONTEXT.md <decisions>. These are LOCKED INPUTS, not open questions. -->
D-01: re-issue the in-flight request on the NEW coordinator for the same index — fresh vrfRequestId/rngRequestTime, preserve in-flight flag — existing rawFulfillRandomWords fills the slot when the new coordinator delivers. retryLootboxRng (≥6h cooldown) stays as failsafe. Rejected alt: pendingVrfRotationPacked queue+apply (cleaner "zero mid-window mutation" but adds recovery latency — old coordinator dead, word never resolves, wait full timeout).
D-02: drop the unconditional force-unlock; preserve+re-issue BOTH paths. Daily (rngLockedFlag==true): keep =true, re-request daily word → rngWordCurrent. Mid-day (LR_MID_DAY==1): keep =1, re-request reserved index → lootboxRngWordByIndex[index]. Nothing in flight: re-point config only, no re-issue, no flag change.
D-03: wireVrf init-only lock — reverts once VRF is wired; updateVrfCoordinatorAndSub becomes SOLE post-init mutator. Detection mechanism (vrfWired bool vs address(vrfCoordinator)!=address(0)) is SPEC discretion — prefer no-new-slot check unless it conflicts with a legitimate re-wire-before-first-request init flow.
D-04: dedup into shared internal _setVrfConfig(coord, sub, key) — both wireVrf and updateVrfCoordinatorAndSub route the 3-field write through it (single-source-of-truth, Phase 310 D-01/D-02 precedent).
D-05: NARROW fix; SPEC verifies gate-reachability is restored. No new backfill wiring. Re-issue fills the single in-flight index AND un-blocks the drain gate (:269/:238), restoring reachability of the EXISTING _backfillOrphanedLootboxIndices (:1208). LR_MID_DAY single-in-flight gate (:1048) → ≤1 orphan per rotation. SPEC OBLIGATION: explicitly trace + confirm re-issue restores the :1208 path. If a residual orphan path is found, ESCALATE to belt-and-suspenders WITH rationale.
Discretion: exact requestRandomWords mechanics on the new coordinator; rngRequestTime=block.timestamp for liveness/timeout; daily-vs-mid-day branch structure inside updateVrfCoordinatorAndSub; the wired-detection mechanism; _setVrfConfig signature/visibility.
Preserve: totalFlipReversals NOT reset on rotation (:1711-1714 carry-over) — re-issue MUST keep this.
</locked_decisions>

<applied_lenses>
<!-- Methodology lenses the design MUST apply (from 311-CONTEXT.md <canonical_refs> + memory). -->
v45-vrf-freeze-invariant: every variable interacting with a VRF word frozen [rng request→unlock] vs PLAYERS; admin rotation is EXEMPT-class (not a player-discretionary write); verify consumed-this-cycle not buffered-for-next.
project_rnglock_audit_disposition: §9d rows are a maximalist catalog, NOT live player vectors — do NOT over-fix governance rows beyond the real liveness defect.
feedback_security_over_gas: validator-influenceable entropy backfill (timestamp / newKeyHash) is REJECTED; the only acceptable entropy source is keccak-of-a-real-VRF-word (the :1817 backfill pattern).
feedback_design_intent_before_deletion: trace original design intent + actor game-theory across timing/state combos BEFORE locking the change to the force-unlock block.
</applied_lenses>
</context>

<tasks>

<task type="auto">
  <name>Task 1: §1 backward-trace + §2 locked fix shape + §3 freeze-invariant disposition</name>
  <files>.planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md</files>
  <read_first>
    - .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md (the §0 manifest from Plan 01 — cite its rows; this task fills §1–§3 placeholders)
    - .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-CONTEXT.md (D-01/D-02 fix shape, the freeze-invariant lens, the Scenario A/B root-cause statement in <domain>)
    - contracts/modules/DegenerusGameAdvanceModule.sol (updateVrfCoordinatorAndSub :1688 force-unlock block, rawFulfillRandomWords :1756 both branches, requestLootboxRng/retryLootboxRng — read from contracts/ ONLY per feedback_contract_locations)
    - contracts/modules/DegenerusGameMintModule.sol (the :686 entropy = lootboxRngWordByIndex[...-1] consumer with no zero-guard — Scenario A)
  </read_first>
  <action>
    Author §1, §2, §3 of `311-SPEC.md`, replacing the placeholders. Every call-path assertion MUST cite a §0 manifest row (no "by construction").

    §1 — Design-Intent Backward-Trace (closes the design-intent obligation per feedback_design_intent_before_deletion). Trace `updateVrfCoordinatorAndSub` (:1688) + `wireVrf` (:498) original intent, then walk the two orphan scenarios across timing/state combos:
      - Scenario A (same-day advance after rotation while a mid-day request was in flight): the force-unlock block (:1701-1704) + LR_MID_DAY=0 (:1709) clears the reservation but never backfills lootboxRngWordByIndex[N]; `MintModule:686` reads that index with NO zero-guard → entropy=0 → deterministic/entropy-0 traits (HIGH, EV-positive grind).
      - Scenario B (next-day): the drain gate at :269 (`lootboxRngWordByIndex[preIdx]==0`) reverts via :238 (`NotTimeYet`) before reaching the existing `_backfillOrphanedLootboxIndices` call at :1208 → ~120-day freeze until forced game-over.
      Include an actor game-theory note: admin rotation is an EXEMPT-class operation (not player-discretionary) per the v45-vrf-freeze-invariant lens; the §9d rows are a maximalist catalog not live player vectors per project_rnglock_audit_disposition.

    §2 — LOCKED Fix Shape (closes VRF-01 + VRF-02). State the re-issue-in-flight design (D-01) with the surgical preserve+re-issue for BOTH paths (D-02):
      - Daily (rngLockedFlag==true): keep rngLockedFlag=true, re-request the daily word on the new coordinator → flows through the rawFulfillRandomWords daily branch → rngWordCurrent.
      - Mid-day (LR_MID_DAY==1): keep LR_MID_DAY=1, re-request for the reserved index → flows through the mid-day branch → lootboxRngWordByIndex[index].
      - Nothing in flight: re-point config only — no re-issue, no flag change.
      Refine the SPEC-discretion mechanics: the exact `requestRandomWords` call on the new coordinator (cite the §0 requestRandomWords call-site rows as the existing pattern to mirror), `rngRequestTime = block.timestamp` for the liveness/timeout machinery, and the daily-vs-mid-day branch structure inside the rotation function. State precisely how this closes VRF-01 (a real VRF word lands in lootboxRngWordByIndex[N] via the unchanged :1769-area mid-day fulfillment branch — the old word is abandoned by the :1761 requestId guard) and VRF-02 (requestLootboxRng :1044 / retryLootboxRng :1133 / the daily-drain advance gate :209-238 all stay reachable; retryLootboxRng remains the ≥6h failsafe). Choose this shape WITH explicit rationale vs the rejected pendingVrfRotationPacked queue+apply split (queue+apply adds recovery latency — the old coordinator is dead so the in-flight word never resolves; you wait the full timeout). State that totalFlipReversals is NOT reset (preserve the :1711-1714 carry-over) and nudges apply to the first post-rotation daily word.

    §3 — Freeze-Invariant Disposition (closes VRF-03, HANDOFF-78/85/87/89/91). Apply the v45-vrf-freeze-invariant lens: enumerate the VRF-participating slots (vrfCoordinator/vrfSubscriptionId/vrfKeyHash/rngRequestTime/LR_MID_DAY, cite the §0 storage rows) and show that under the §2 design no slot is mutated mid-window in a way that changes an in-flight VRF-derived output consumed THIS cycle (the old word is abandoned; the new word is unpredictable; admin rotation is EXEMPT-class vs PLAYERS, not player-discretionary). EXPLICITLY REJECT validator/timestamp/newKeyHash-influenceable entropy backfill per feedback_security_over_gas, and state that the only sanctioned entropy source is keccak-of-a-real-VRF-word (the :1817 backfill pattern — cite the §0 row). Note that MintModule:686's absent zero-guard is mitigated STRUCTURALLY (the index is always filled by re-issue), not by adding a guard.

    Per feedback_contract_locations: contract reads from `contracts/` only. Do NOT modify any `.sol` or `test/` file.
  </action>
  <verify>
    <automated>grep -q "Scenario A" .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md && grep -q "Scenario B" .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md && grep -Eiq "re-issue" .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md && grep -Eiq "reject" .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md && grep -q "totalFlipReversals" .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md && git diff --quiet -- contracts/ test/ && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - §1 contains a backward-trace of updateVrfCoordinatorAndSub + wireVrf across Scenario A (entropy-0 traits via MintModule:686) and Scenario B (drain-gate revert at :269/:238 before the :1208 backfill → freeze + forced game-over), each citing §0 rows.
    - §2 states the re-issue-in-flight fix (D-01) with preserve+re-issue for BOTH daily (→rngWordCurrent) and mid-day (→lootboxRngWordByIndex[index]) paths plus the nothing-in-flight re-point-only case (D-02), and explains how it closes VRF-01 and VRF-02, with explicit rationale vs the rejected queue+apply split.
    - §2 records that totalFlipReversals is NOT reset (preserves the :1711-1714 carry-over).
    - §3 enumerates the VRF-participating slots (citing §0 storage rows), shows no freeze-breaking mid-window mutation under the design (VRF-03), explicitly rejects validator-influenceable entropy backfill per feedback_security_over_gas, and cites the :1817 keccak-of-VRF-word entropy source.
    - Every call-path assertion in §1–§3 cites a §0 manifest row; no "by construction" claims.
    - `git diff --quiet -- contracts/ test/` returns clean.
  </acceptance_criteria>
  <done>§1–§3 are authored: the backward-trace covers both scenarios, the fix shape closes VRF-01/VRF-02 with rationale, the freeze disposition closes VRF-03 with validator-influenceable backfill rejected; totalFlipReversals preserved; no contract/test files touched.</done>
</task>

<task type="auto">
  <name>Task 2: §4 wireVrf lock + _setVrfConfig dedup + vault reach, §5 orphan-recovery breadth + escalation gate, §6 rejected options</name>
  <files>.planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md</files>
  <read_first>
    - .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md (§0 manifest + §0.Y vault-reach trace from Plan 01, and §1–§3 from this plan's Task 1 — cite them; this task fills §4–§6 placeholders)
    - .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-CONTEXT.md (D-03/D-04/D-05, the escalation clause, the deferred rejected options in <deferred>)
    - .planning/phases/310-implementation-single-batched-user-approved-contract-diff-im/310-CONTEXT.md (the Phase 310 D-01/D-02 single-source-of-truth precedent grounding the D-04 _setVrfConfig dedup)
    - contracts/modules/DegenerusGameAdvanceModule.sol (wireVrf :498 + updateVrfCoordinatorAndSub :1688 — the two inline 3-field config writes D-04 collapses; :1208 backfill call + :1817 definition + :209-238/:269 drain gate for D-05 — read from contracts/ ONLY)
    - contracts/DegenerusAdmin.sol + contracts/DegenerusGame.sol (the vault/admin-routed dispatch sites recorded in §0.Y — for the VRF-05 disposition)
  </read_first>
  <action>
    Author §4, §5, §6 of `311-SPEC.md`, replacing the placeholders. Every assertion cites a §0 manifest row.

    §4 — wireVrf One-Shot Lock + _setVrfConfig Dedup + Vault Reach (closes VRF-04 = HANDOFF-86/88/90 + ADMA-01, and VRF-05 = ADMA-02).
      - D-03 wireVrf init-only lock: specify that `wireVrf` (:498) reverts once VRF is wired, making `updateVrfCoordinatorAndSub` (:1688) the SOLE post-init VRF-config mutator (now safe via §2). Choose the wired-detection mechanism (SPEC discretion): prefer the no-new-slot check `address(vrfCoordinator) != address(0)` UNLESS a legitimate re-wire-before-first-request init flow requires a dedicated `vrfWired` bool — state which is chosen and the rationale, including whether any init flow re-wires before the first request.
      - D-04 _setVrfConfig dedup: specify the shared `internal` helper `_setVrfConfig(coord, sub, key)` that both `wireVrf` and `updateVrfCoordinatorAndSub` route their 3-field write through (cite the two §0 inline-write rows that confirm the near-duplication). State the chosen signature/visibility, grounded in the Phase 310 D-01/D-02 single-source-of-truth precedent (consolidate shared logic, avoid inline-duplication drift per feedback_verify_call_graph_against_source).
      - VRF-05 vault reach: using the §0.Y trace, confirm the D-03 lock + D-01/D-02 safe-rotation cover the vault/admin-routed dispatch path to both functions (the actual `DegenerusAdmin.sol` / `DegenerusGame.sol` sites recorded in §0.Y, not the CONTEXT-named "DegenerusVault"); state the disposition.

    §5 — Orphan-Recovery Breadth (D-05, supports VRF-01/VRF-02 closure completeness). EXPLICITLY trace and CONFIRM that re-issue (§2) un-blocks the drain gate at :269 (so :238 NotTimeYet no longer reverts) and thereby RESTORES reachability of the EXISTING `_backfillOrphanedLootboxIndices` call at :1208 (VRF-derived keccak(word,i) per the :1817 definition, not front-runnable) for any residual orphans — the helper is NOT left stranded. State the LR_MID_DAY single-in-flight bound (:1048 gate → ≤1 orphaned index per rotation; repeated rotations re-cover the same index). Include the ESCALATION GATE: if this reachability trace finds a residual orphan path that re-issue + the :1208 backfill do NOT cover, escalate to the rejected belt-and-suspenders option (gate-independent backfill / dedicated recovery entry point) WITH explicit rationale — and record the trace's CONCLUSION (confirmed-covered OR escalated). This is the ONLY sanctioned reversal of a locked decision.

    §6 — Rejected Options. Record the two documented alternatives, each with its rationale: (1) queue+apply rotation via a new `pendingVrfRotationPacked` slot (the §9d HANDOFF-78 tactic) — rejected because the old coordinator is dead so the in-flight word never resolves; adds recovery latency (full timeout wait) and a two-step UX, sacrificing the liveness that is the whole point of the emergency procedure; (2) belt-and-suspenders gate-independent orphan backfill / dedicated recovery entry point — deferred in favor of D-05's narrow fix; revisit ONLY if §5's reachability trace escalates.

    Finally append a `## §7 SPEC Self-Check` block asserting: every cited anchor is grep-verified against HEAD (§0); D-05 reachability explicitly confirmed-or-escalated (§5); validator-influenceable backfill explicitly rejected (§3); D-01..D-05 all carried forward without silent reversal; VRF-01..05 each mapped to a closing section; zero contracts/ + zero test/ mutations.

    Per feedback_contract_locations: contract reads from `contracts/` only. Do NOT modify any `.sol` or `test/` file.
  </action>
  <verify>
    <automated>grep -q "_setVrfConfig" .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md && grep -q "_backfillOrphanedLootboxIndices" .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md && grep -Eiq "escalat" .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md && grep -Eq "pendingVrfRotationPacked" .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md && grep -q "§7 SPEC Self-Check" .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md && grep -Eq "VRF-01" .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md && grep -Eq "VRF-05" .planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-SPEC.md && git diff --quiet -- contracts/ test/ && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - §4 specifies the D-03 wireVrf one-shot lock with a CHOSEN wired-detection mechanism + rationale, the D-04 `_setVrfConfig` shared-internal dedup with chosen signature/visibility (citing the two §0 inline-write rows + the Phase 310 precedent), and the VRF-05 vault-reach disposition referencing the §0.Y actual dispatch sites.
    - §5 explicitly traces re-issue → drain-gate un-block (:269/:238) → restored :1208 backfill reachability, states the LR_MID_DAY ≤1-orphan bound (:1048), includes the escalation gate, and records a CONCLUSION (confirmed-covered or escalated-with-rationale).
    - §6 records both rejected options (queue+apply pendingVrfRotationPacked; belt-and-suspenders gate-independent backfill) each with rationale.
    - §7 self-check asserts all six self-check items including VRF-01..05 each mapped to a closing section and zero contract/test mutations.
    - Every assertion in §4–§6 cites a §0 manifest row; no "by construction" claims; D-01..D-05 carried forward without silent reversal.
    - `git diff --quiet -- contracts/ test/` returns clean.
  </acceptance_criteria>
  <done>§4–§7 are authored: the wireVrf lock + dedup + vault reach close VRF-04/VRF-05, §5 confirms-or-escalates D-05 reachability, §6 records both rejected options, §7 self-check passes; the SPEC is complete with all 5 requirements closed and D-01..D-05 honored; no contract/test files touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Design (SPEC) phase — the trust boundary is **design-correctness**: a wrong or incomplete locked design re-opens the CATASTROPHE-class orphan-index / freeze defect at Phase 312 IMPL. The design-correctness threats below are the ones the SPEC sections must each neutralize.

| Boundary | Description |
|----------|-------------|
| locked design → Phase 312 IMPL | the SPEC is the sole input; an uncovered orphan path or a freeze-breaking mutation in the design propagates to a deployed defect |
| admin rotation → PLAYERS (freeze invariant) | rotation is EXEMPT-class, but a slot mutation that changes a consumed-this-cycle VRF output would break the invariant vs players |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-311-04 | Denial of Service (residual freeze) | a residual orphan-index path re-issue + the :1208 backfill do NOT cover | mitigate | §5 explicitly traces + CONFIRMS re-issue restores the :1208 reachability; D-05 escalation clause forces belt-and-suspenders WITH rationale if the trace finds an uncovered path (the only sanctioned locked-decision reversal) |
| T-311-05 | Tampering (freeze-invariant break) | a VRF-participating slot mutated mid-window in a freeze-breaking way slipping into §2's design | mitigate | §3 freeze-invariant disposition enumerates all VRF-participating slots and shows no consumed-this-cycle output changes; old word abandoned by :1761 guard, new word unpredictable; admin rotation EXEMPT-class per v45-vrf-freeze-invariant |
| T-311-06 | Tampering (predictable entropy) | validator/timestamp/newKeyHash-influenceable entropy creeping into any backfill path | mitigate | §3 EXPLICITLY REJECTS validator-influenceable backfill per feedback_security_over_gas; only sanctioned source is keccak-of-a-real-VRF-word (:1817), grep-verified in §0 |
| T-311-07 | Elevation of privilege (lock bypass) | the wireVrf lock leaving a vault/admin-routed (ADMA-02) bypass that re-opens post-init VRF-config mutation | mitigate | §4 VRF-05 backward-traces the actual dispatch sites (§0.Y) and confirms the D-03 lock + D-01/D-02 safe-rotation cover the routed path |
| T-311-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan (doc-only); nothing to verify |
</threat_model>

<verification>
- §1–§7 of `311-SPEC.md` authored; every design assertion cites a §0 manifest row.
- The four design-correctness threats (T-311-04..07) are each neutralized by the corresponding section (§5, §3, §3, §4).
- D-01..D-05 carried forward without silent reversal; the only reversal path is D-05's escalation, recorded with rationale if triggered.
- `git diff --quiet -- contracts/ test/` is clean.
</verification>

<success_criteria>
- All 5 ROADMAP Success Criteria are satisfied: §1 backward-trace + locked fix shape (SC1); VRF-01/VRF-02 precisely specified (SC2); freeze-invariant disposition + validator-influenceable backfill rejected (SC3); wireVrf one-shot lock + vault reach (SC4); every cited line grep-verified + §9d anchors mapped + zero by-construction + zero contract/test mutations (SC5).
- VRF-01..05 each closed by a named section; D-01..D-05 honored.
- Zero contracts/ + zero test/ mutations.
</success_criteria>

<output>
Create `.planning/phases/311-spec-vrf-rotation-liveness-fix-spec/311-02-SUMMARY.md` when done.
</output>
