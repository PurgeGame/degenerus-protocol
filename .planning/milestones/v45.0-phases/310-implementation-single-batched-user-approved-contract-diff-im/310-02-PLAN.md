---
phase: 310-implementation-single-batched-user-approved-contract-diff-im
plan: 02
type: execute
wave: 2
depends_on: ["310-01"]
files_modified:
  - contracts/modules/DegenerusGameLootboxModule.sol
autonomous: true
requirements: [IMPL-01, IMPL-04, IMPL-05]
must_haves:
  truths:
    - "A sub-neutral (penalty) or neutral box applies amount * mult / 10_000 and draws ZERO from the cap"
    - "Only a bonus box (mult > NEUTRAL) draws the shared cap at resolution in the two resolvers"
    - "openLootBox applies the frozen allocation read from the packed word with NO cap SLOAD/SSTORE"
    - "openLootBox clears the whole packed word in a single SSTORE"
    - "The open-path roll seed keccak256(abi.encode(rngWord, player, day, amount)) is byte-identical to HEAD; only reward scaling uses adjustedPortion"
  artifacts:
    - path: "contracts/modules/DegenerusGameLootboxModule.sol"
      provides: "Bonus-only cap (IMPL-01) + openLootBox frozen-apply (IMPL-04) + seed preservation (IMPL-05 open side)"
      contains: "if (evMultiplierBps <= LOOTBOX_EV_NEUTRAL_BPS)"
  key_links:
    - from: "openLootBox"
      to: "DegenerusGameStorage.lootboxPurchasePacked + _unpackLootboxPurchase + _lootboxEvMultiplierFromScore"
      via: "single SLOAD + unpack + frozen-apply formula"
      pattern: "lootboxPurchasePacked|_unpackLootboxPurchase"
    - from: "_applyEvMultiplierWithCap"
      to: "lootboxEvBenefitUsedByLevel[player][lvl]"
      via: "cap draw only on mult > NEUTRAL (resolvers only)"
      pattern: "evMultiplierBps <= LOOTBOX_EV_NEUTRAL_BPS"
---

<objective>
Apply the v45.0 LootboxModule edits: the bonus-only cap rewrite (IMPL-01, SPEC-02), the
`openLootBox` frozen-apply with no cap SLOAD/SSTORE and a whole-word zero-at-open (IMPL-04,
SPEC-03 §3.4), and the open-path seed/roll byte-identity preservation (IMPL-05, SPEC §3.5).

After Plan 01 moved `_lootboxEvMultiplierFromScore` + the EV constants + the pack/unpack helpers
into the shared base (Storage.sol), this plan rewires LootboxModule to consume those inherited
symbols and to read the new packed `lootboxPurchasePacked` word.

Purpose: This is the consumer side of the open path — `openLootBox` stops drawing the cap at open
(the V-081 surface) and instead applies the frozen `adjustedPortion` allocated at deposit time
(Plan 03). The two resolvers keep drawing the shared cap at resolution (SPEC-04 ACCEPT) but now
through the bonus-only `<=` rule.

Output: A modified `DegenerusGameLootboxModule.sol` (uncommitted — committed at the END of Plan 03
after the USER-APPROVAL gate). NO commit in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

CONTRACT-COMMIT POLICY (binding for this entire phase):
- `contracts/*.sol` changes MUST NOT be committed or pushed without explicit USER approval of
  the full diff. Contract changes are NOT pre-approved.
- This plan (02) leaves its edit UNCOMMITTED in the working tree. The single batched contract
  commit covering all 4 files happens ONLY in Plan 03 after the USER-APPROVAL checkpoint. Do NOT
  run `git commit` for any `contracts/*.sol` file here.
</execution_context>

<context>
@.planning/phases/310-implementation-single-batched-user-approved-contract-diff-im/310-CONTEXT.md
@.planning/phases/309-spec-locked-layout-bonus-only-cap-shared-cap-disposition-spe/309-SPEC.md
@.planning/REQUIREMENTS.md
@.planning/phases/310-implementation-single-batched-user-approved-contract-diff-im/310-01-SUMMARY.md

<interfaces>
<!-- Symbols this plan CONSUMES from the shared base (created in Plan 01). Use them directly; -->
<!-- they are inherited internal members of DegenerusGameStorage. Do NOT re-declare them. -->

From DegenerusGameStorage.sol (post-Plan-01):
```solidity
mapping(uint48 => mapping(address => uint256)) internal lootboxPurchasePacked;
function _unpackLootboxPurchase(uint256 word)
    internal pure returns (uint16 scorePlus1, uint64 adj, uint24 baseLevelPlus1);
function _lootboxEvMultiplierFromScore(uint256 score) internal pure returns (uint256);
// internal constants now in Storage: LOOTBOX_EV_NEUTRAL_BPS (=10_000), LOOTBOX_EV_BENEFIT_CAP (=10 ether), etc.
```

SPEC §3.4 frozen-apply formula (LOCKED):
```
scaled = mult <= NEUTRAL ? amount * mult / 1e4
                         : adj * mult / 1e4 + (amount - adj)
```
where `mult = _lootboxEvMultiplierFromScore(score)` (score from the unpacked word, score+1 - 1),
`adj = adjustedPortion` (unpacked from the word).
</interfaces>

<existing_anchors>
<!-- Re-grep-verified against live HEAD bfe25567. Locate by MATCHED SUBSTRING, not line number. -->

DegenerusGameLootboxModule.sol:
- `function _applyEvMultiplierWithCap(`                                    (~475, def — stays private)
- `if (evMultiplierBps == LOOTBOX_EV_NEUTRAL_BPS) {` / `return amount;`     (~482-484, SPEC-02 Change-1 target)
- `function openLootBox(address player, uint48 index) external {`          (~517)
- `uint24 baseLevelPacked = lootboxBaseLevelPacked[index][player];`         (~541, open baseLevel read — rewire to packed word)
- `uint256 seed = uint256(keccak256(abi.encode(rngWord, player, day, amount)));` (~545, open seed — MUST stay byte-identical)
- `uint16 evScorePacked = lootboxEvScorePacked[index][player];`            (~557, open score read — rewire to packed word)
- `uint256 evMultiplierBps = _lootboxEvMultiplierFromScore(uint256(evScorePacked - 1));` (~558, mult source)
- `uint256 scaledAmount = _applyEvMultiplierWithCap(`                       (~559, OPEN call site — REPLACE with frozen-apply)
- `lootboxBaseLevelPacked[index][player] = 0;`                             (~570, clear — merge into whole-word zero)
- `lootboxEvScorePacked[index][player] = 0;`                               (~571, clear — merge into whole-word zero)
- `uint256 seed = uint256(keccak256(abi.encode(rngWord, player, day, amountEth)));` (~621, BURNIE open seed — DO NOT TOUCH, uses amountEth)
- `... = _applyEvMultiplierWithCap(player, currentLevel, amount, evMultiplierBps);` (~675, resolveLootboxDirect call — KEEP)
- `... = _applyEvMultiplierWithCap(player, currentLevel, amount, evMultiplierBps);` (~711, resolveRedemptionLootbox call — KEEP)
</existing_anchors>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bonus-only cap rewrite in _applyEvMultiplierWithCap (IMPL-01 / SPEC-02)</name>
  <files>contracts/modules/DegenerusGameLootboxModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameLootboxModule.sol (read `_applyEvMultiplierWithCap` body ~475-509)
    - 309-SPEC.md §2 (SPEC-02 rule + the exact `<=` rewrite), §2.2 (applies to all three callers)
  </read_first>
  <action>
    In `_applyEvMultiplierWithCap` (locate by matched substring `function _applyEvMultiplierWithCap(`),
    replace the existing neutral early-return (matched substring
    `if (evMultiplierBps == LOOTBOX_EV_NEUTRAL_BPS) {` returning `amount`) with:
    `if (evMultiplierBps <= LOOTBOX_EV_NEUTRAL_BPS) { return (amount * evMultiplierBps) / 10_000; }`.
    This makes penalty (`< NEUTRAL`) and neutral (`== NEUTRAL`) boxes apply the multiplier on the
    FULL amount and draw ZERO from the cap; only `> NEUTRAL` falls through to the cap-draw branch
    (the `lootboxEvBenefitUsedByLevel` SLOAD/SSTORE). The `/ 10_000` divisor matches the existing
    scaling literal. Keep `_applyEvMultiplierWithCap` `private` in LootboxModule (do NOT relocate
    it — only the two resolvers call it post-IMPL). `LOOTBOX_EV_NEUTRAL_BPS` now resolves to the
    inherited Storage constant (relocated in Plan 01) — do NOT re-declare it.

    Comments describe what IS (bonus-only cap semantics), never what changed.
  </action>
  <verify>
    <automated>grep -q "if (evMultiplierBps <= LOOTBOX_EV_NEUTRAL_BPS)" contracts/modules/DegenerusGameLootboxModule.sol && grep -q "return (amount \* evMultiplierBps) / 10_000;" contracts/modules/DegenerusGameLootboxModule.sol && ! grep -q "if (evMultiplierBps == LOOTBOX_EV_NEUTRAL_BPS)" contracts/modules/DegenerusGameLootboxModule.sol && echo PASS</automated>
  </verify>
  <done>
    `_applyEvMultiplierWithCap` returns `(amount * evMultiplierBps) / 10_000` for
    `evMultiplierBps <= LOOTBOX_EV_NEUTRAL_BPS` and never touches `lootboxEvBenefitUsedByLevel` on
    that branch; the old `== NEUTRAL` early return is gone; the fn is still `private` in
    LootboxModule; both resolver call sites (~675/~711) are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: openLootBox frozen-apply + whole-word zero + seed preservation (IMPL-04 / IMPL-05)</name>
  <files>contracts/modules/DegenerusGameLootboxModule.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameLootboxModule.sol (read openLootBox in full ~517-591)
    - 309-SPEC.md §3.4 (frozen-apply formula, no cap SLOAD/SSTORE), §1.8 (whole-word zero-at-open), §3.5 + §0.F (seed byte-identity), §4.B.1 (post-IMPL the cap SLOAD row 10 is REMOVED from the open path)
    - 310-01-SUMMARY.md (the as-implemented packed-word layout + helper signatures)
  </read_first>
  <action>
    Rewire `openLootBox` (locate by matched substring `function openLootBox(address player, uint48 index) external {`):

    1. SLOAD the packed word once: `lootboxPurchasePacked[index][player]`, then
       `_unpackLootboxPurchase(word)` -> `(scorePlus1, adj, baseLevelPlus1)`. This single read
       replaces BOTH the baseLevel read (matched substring
       `uint24 baseLevelPacked = lootboxBaseLevelPacked[index][player];`) and the score read
       (matched substring `uint16 evScorePacked = lootboxEvScorePacked[index][player];`). Preserve
       the existing baseLevel/grace logic that consumes `baseLevelPacked` (the
       `graceLevel = baseLevelPacked == 0 ? currentLevel : baseLevelPacked - 1;` decision) by
       feeding it `baseLevelPlus1` from the unpacked word — the value semantics are identical
       (it was `lootboxBaseLevelPacked[index][player]` before).
    2. Compute `mult = _lootboxEvMultiplierFromScore(uint256(scorePlus1 - 1))` (inherited Storage
       fn; preserves the existing `evScorePacked - 1` decode). Do NOT re-order the score read
       relative to the seed build.
    3. REPLACE the `_applyEvMultiplierWithCap(...)` call (matched substring
       `uint256 scaledAmount = _applyEvMultiplierWithCap(`) with the SPEC §3.4 frozen-apply, NO
       cap SLOAD/SSTORE: `scaledAmount = mult <= LOOTBOX_EV_NEUTRAL_BPS ? (amount * mult) / 10_000
       : (uint256(adj) * mult) / 10_000 + (amount - uint256(adj));`. `openLootBox` must NOT read
       or write `lootboxEvBenefitUsedByLevel` anywhere (the cap was drawn at deposit time in
       Plan 03).
    4. Replace the two separate clears (matched substrings
       `lootboxBaseLevelPacked[index][player] = 0;` and
       `lootboxEvScorePacked[index][player] = 0;`) with a SINGLE whole-word zero:
       `lootboxPurchasePacked[index][player] = 0;` (clears score+1, adjustedPortion, baseLevel+1
       in one SSTORE per SPEC §1.8).
    5. PRESERVE the open-path roll seed BYTE-IDENTICAL: the matched substring
       `uint256 seed = uint256(keccak256(abi.encode(rngWord, player, day, amount)));` (~545) is
       NOT edited — it still binds the RAW `amount` (the value read from `lootboxEth`), never
       `adj`. `targetLevel`/`baseLevel`/`day` derivation is unchanged. Do NOT touch the BURNIE
       open path seed (~621, uses `amountEth`).

    Comments describe what IS (frozen-allocation apply), never what changed.
  </action>
  <verify>
    <automated>grep -q "_unpackLootboxPurchase(" contracts/modules/DegenerusGameLootboxModule.sol && grep -q "lootboxPurchasePacked\[index\]\[player\] = 0;" contracts/modules/DegenerusGameLootboxModule.sol && ! grep -q "lootboxBaseLevelPacked\|lootboxEvScorePacked" contracts/modules/DegenerusGameLootboxModule.sol && [ "$(grep -c 'keccak256(abi.encode(rngWord, player, day, amount))' contracts/modules/DegenerusGameLootboxModule.sol)" -ge 3 ] && grep -q "keccak256(abi.encode(rngWord, player, day, amountEth))" contracts/modules/DegenerusGameLootboxModule.sol && awk '/function openLootBox\(address player, uint48 index\) external \{/{f=1} f&&/_applyEvMultiplierWithCap/{print "FAIL_OPEN_STILL_CALLS_CAP"; bad=1} /^    \}/{if(f)f=0} END{if(!bad)print "PASS"}' contracts/modules/DegenerusGameLootboxModule.sol | grep -q PASS && echo PASS</automated>
  </verify>
  <done>
    `openLootBox` SLOADs `lootboxPurchasePacked` once, unpacks via `_unpackLootboxPurchase`,
    applies the SPEC §3.4 frozen formula, performs NO `lootboxEvBenefitUsedByLevel` read/write,
    and zeroes the whole packed word in one SSTORE. The open-path seed line
    `keccak256(abi.encode(rngWord, player, day, amount))` (~545) is byte-identical to HEAD; the
    BURNIE seed (~621, `amountEth`) is untouched; `lootboxBaseLevelPacked`/`lootboxEvScorePacked`
    appear NOWHERE in LootboxModule. Both resolver `_applyEvMultiplierWithCap` calls remain.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player -> openLootBox | player triggers open at a chosen time; the per-box allocation must already be frozen so open order cannot steer it |
| VRF word -> openLootBox resolution | `lootboxRngWordByIndex[index]` gates open; every other in-window read must be frozen pre-word |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-310-02 | Tampering | open-order cap steering (the V-081 surface) | mitigate | `openLootBox` performs NO `lootboxEvBenefitUsedByLevel` SLOAD/SSTORE — it applies the `adjustedPortion` frozen at deposit time (Plan 03). The awk gate asserts no `_applyEvMultiplierWithCap` call survives inside `openLootBox`. Removes the greedy open-time cap draw -> open order can no longer change total payout. Ties to INV-01 (order-independence) + INV-02 (penalty non-dodge). |
| T-310-03 | Tampering | reward scaling vs roll seed conflation | mitigate | The frozen-apply uses `adj`/`amount` ONLY for `scaledAmount`; the roll seed at ~545 keeps the RAW `amount` byte-identical (grep gate: >=3 raw-`amount` seeds + 1 `amountEth` BURNIE seed present). The rolled `targetLevel`/index/word are unchanged. Ties to INV-04 (seed/roll unchanged) / IMPL-05. |
| T-310-04 | Tampering | bonus-only cap rule (`<=`) reachability | mitigate | The `<=` rewrite at the single `_applyEvMultiplierWithCap` definition reaches both resolver call sites (SPEC §2.2); a sub-neutral box can no longer consume cap headroom reserved for bonus boxes. Ties to INV-02/INV-03/INV-05. |
| T-310-06 | Information disclosure / freeze | open-path in-window read set must not grow | mitigate | The packed-word SLOAD replaces TWO prior per-(index,player) SLOADs with ONE; no NEW slot is read in-window; the removed cap SLOAD strictly reduces the word-adjacent shared-state surface to zero for purchased boxes (SPEC §4.B.1 post-IMPL note). Ties to INV-06. |
| T-310-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs; pure Solidity edits to an existing module. No new dependency surface. |
</threat_model>

<verification>
- `grep`/`awk` gates in each task PASS.
- `forge build` is NOT gated here (Mint/Whale still reference the old mappings until Plan 03; the
  build gate runs in Plan 03 once all consumers are wired). The executor MAY run `forge build`
  informationally; expect unresolved references from not-yet-edited Mint/Whale.
</verification>

<success_criteria>
- IMPL-01: `_applyEvMultiplierWithCap` uses the bonus-only `<=` rule and never draws the cap on a
  sub-neutral/neutral box; it stays `private` in LootboxModule.
- IMPL-04: `openLootBox` reads `lootboxPurchasePacked` once, applies the SPEC §3.4 frozen formula
  with NO cap SLOAD/SSTORE, and zeroes the whole word in a single SSTORE.
- IMPL-05 (open side): the open-path seed `keccak256(abi.encode(rngWord, player, day, amount))`
  is byte-identical; the BURNIE seed (`amountEth`) untouched; rolled index/word unchanged.
- Both resolver call sites still call `_applyEvMultiplierWithCap` (SPEC-04 ACCEPT, with Change-1).
- NO contract commit in this plan (deferred to Plan 03 USER-APPROVAL gate).
</success_criteria>

<output>
Create `.planning/phases/310-implementation-single-batched-user-approved-contract-diff-im/310-02-SUMMARY.md` when done.
Record: the final `openLootBox` allocation flow, an explicit confirmation the open seed line is
byte-identical to HEAD, and a note that `DegenerusGameLootboxModule.sol` is left UNCOMMITTED for
the Plan 03 batched diff.
</output>
