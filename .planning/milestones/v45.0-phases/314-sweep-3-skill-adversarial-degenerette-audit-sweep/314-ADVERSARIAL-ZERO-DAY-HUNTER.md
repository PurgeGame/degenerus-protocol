# Phase 314 `/zero-day-hunter` Adversarial Pass — v45.0 VRF-Rotation Fix + Consolidated Delta (Novel-Surface)

```yaml
[invocation]
skill: /zero-day-hunter
mode: PARALLEL_SUBAGENT
dispatch_timestamp: "2026-05-23T00:00:00Z"
runner: task-subagent
fallback_reason: null
charge_anchor: ".planning/phases/314-sweep-3-skill-adversarial-degenerette-audit-sweep/314-ADVERSARIAL-CHARGE.md"
```

```yaml
[skeptic-filter]
arm: per-skill self-filter
protocol: D-314-SKEPTIC-FILTER-01
discarded: []
note: "No (a)-only hard discards at the per-skill self-filter arm. Every novel/composition/timing hypothesis probed against current source HEAD resolved to a structural-protection-cited NEGATIVE-VERIFIED verdict or a SAFE_BY_DESIGN intentional-design citation. The headline hand-off hypothesis (totalFlipReversals nudge-grind across a rotation boundary, SWP-01.H.1) is structurally closed by the rngLockedFlag gate on reverseFlip (DegenerusGame.sol:1929), which stays asserted through the ENTIRE daily window including any mid-window rotation. No FINDING_CANDIDATE produced; the (b)+(c) severity-downgrade arm is therefore inapplicable. Orchestrator integration-time re-application at Task 5 re-verifies against the union of all 3 skills."
```

---

## §0 Charge-frame re-anchor

This pass executes the **novel-attack-surface** arms of **SWP-01** + **SWP-02**, verbatim per `314-ADVERSARIAL-CHARGE.md`, probing composition / timing / sequence / precision surfaces the `/contract-auditor` structural pass would not have surfaced (and which it explicitly handed off in its §4):

> **SWP-01**: Red-team the VRF-rotation fix — rotation-spam / stuck-pending / double-request griefing, a new liveness-DoS, a new freeze violation, or a `wireVrf`-lock that breaks a legitimate ops path. RE-PASS per `D-284-ADVERSARIAL-RE-PASS-01` if a candidate materialises.

> **SWP-02**: Composition pass across the consolidated delta surfaces — V-081 allocation/packing, jackpot pending-pool obligations, degenerette removal — any cross-surface composition attack or differential behaviour an attacker can game.

**Hunter divergence note:** the `/contract-auditor` SEQUENTIAL_MAIN_CONTEXT pass already discharged the *structural* SWP-01.A..F + SWP-02.* rows (0 FINDING_CANDIDATE). Per the charge, this hunter does NOT re-run those; it probes the genuinely-novel surfaces named in the auditor's §4 hand-off: (1) `totalFlipReversals` nudge-grind across the rotation timing window (`feedback_rng_commitment_window`), (2) cross-module read/write race on `lootboxRngWordByIndex` / `lootboxPurchasePacked`, (3) backfill `keccak256(vrfWord, i)` foreknowledge/collision, plus the full SLOAD-in-window enumeration (`feedback_rng_window_storage_read_freshness`) and cross-surface composition across V-081 / jackpot / degenerette.

**Subject HEAD:** `40db9e94` (audit branch), contract subject post-`a303ae18` (VRF-rotation fix) + `9bcd582d` (V-081) + `6e5acd7e`/`f3e21064` (jackpot pending-pool) + `92b110bf` (degenerette). All file:line anchors grep-verified against source HEAD 2026-05-23.

---

## §1 Per-hypothesis disposition table

| Hypothesis-ID | Verdict | Severity tag | Evidence anchors | Reasoning summary |
| --- | --- | --- | --- | --- |
| **SWP-01.H.1** — `totalFlipReversals` nudge-grind across the rotation boundary: can a player observe a delivered-but-not-yet-consumed daily word (or exploit the rotation window where the word is re-issued) and THEN call `reverseFlip()` to `+N` the word toward a profitable outcome? (`feedback_rng_commitment_window` headline) | NEGATIVE-VERIFIED | N-A | `DegenerusGame.sol:1928-1936` (`reverseFlip`), `:1929` (`if (rngLockedFlag) revert RngLocked()`); `AdvanceModule.sol:1669` (`rngLockedFlag = true` at request), `:1732` (rotation KEEPS `rngLockedFlag=true`), `:1774` (only `_unlockRng` clears it), `:1798-1800` (callback sets `rngWordCurrent` but leaves `rngLockedFlag` true), `:1875-1882` (`_applyDailyRng` consumes+zeroes nudges); `feedback_rng_commitment_window`, `v45-vrf-freeze-invariant`. | The nudge counter is player-writable ONLY via `reverseFlip()`, which reverts whenever `rngLockedFlag` is set (`:1929`). The daily VRF lifecycle asserts `rngLockedFlag = true` at request (`AdvanceModule.sol:1669`) and clears it ONLY at `_unlockRng` (`:1774`) AFTER the word has been consumed by `_applyDailyRng`. The fulfillment callback `rawFulfillRandomWords` does NOT clear the flag — it sets `rngWordCurrent = word` while `rngLockedFlag` stays true (`:1798-1800`). A VRF coordinator rotation KEEPS `rngLockedFlag = true` (`:1731-1740`; the `:1743-1746` comment confirms the carried-over nudges were purchased BEFORE the lock). Therefore the ENTIRE window `[daily-request → word-delivered → consumed → unlock]`, including any mid-window rotation, has `rngLockedFlag == true`, so `reverseFlip` is unreachable across the whole window. Nudges are committed before any word is requested (unknown at commitment), `_applyDailyRng` zeroes them on consume (`:1881`) so they cannot grind a second word. The carry-over-on-rotation is value-preservation (burned BURNIE applies to the fresh re-issued word, still unknown), not a foreknowledge primitive. The +N is an ADD to an unpredictable base word — influence, not prediction. NEGATIVE-VERIFIED. |
| **SWP-01.H.2** — mid-day rotation window (`rngLockedFlag == false`): a player CAN reach `reverseFlip` during a mid-day VRF window — does a nudge queued there manipulate the in-flight mid-day word, or leak/double-apply? | NEGATIVE-VERIFIED | N-A | `AdvanceModule.sol:1043` (`requestLootboxRng` requires `!rngLockedFlag`), `:1726-1730` (mid-day rotation re-issue, no `rngLockedFlag` set), `:1801-1808` (mid-day fulfill stores `lootboxRngWordByIndex[index] = word` RAW — no nudge add), `:1875-1882` (`_applyDailyRng` — the ONLY nudge consumer is the DAILY word path), `:273` (daily-advance pre-gate nudge add); `DegenerusGame.sol:1929`. | During a mid-day window `rngLockedFlag == false`, so `reverseFlip` is reachable. BUT the mid-day word is stored RAW (`:1804` `lootboxRngWordByIndex[index] = word`; no `+ totalFlipReversals`). `totalFlipReversals` feeds ONLY the daily word — `_applyDailyRng:1879` (`finalWord += nudges`) and the daily-advance pre-gate `:273` (`cw += totalFlipReversals` → `_finalizeLootboxRng`). A nudge queued during a mid-day window is therefore buffered for the NEXT daily word, whose VRF entropy is not yet requested (the next daily request requires the mid-day to be cleared first, `:209-225`, and is unknown at nudge time). This is the canonical buffered-for-next not-a-violation pattern (`v45-vrf-freeze-invariant` discriminator). The mid-day word cannot be nudged; no this-cycle manipulation. NEGATIVE-VERIFIED. |
| **SWP-01.H.3** — nudge double-apply / value-leak via the dual consumer (pre-gate `:273` reads `totalFlipReversals` without zeroing; `_applyDailyRng:1881` zeroes): can a player split the consumption across advances to apply the same nudge to BOTH the lootbox word and the daily word for different days, or leave it un-zeroed for a free re-grind? | NEGATIVE-VERIFIED | N-A | `AdvanceModule.sol:269-275` (pre-gate: `if (lootboxRngWordByIndex[preIdx]==0){ cw = rngWordCurrent + totalFlipReversals; _finalizeLootboxRng(cw); }`), `:1256-1261` (`_finalizeLootboxRng` idempotent: `if (lootboxRngWordByIndex[index] != 0) return;`), `:1215` (`_applyDailyRng` consume), `:1237` (`_finalizeLootboxRng(currentWord)` re-stores same post-nudge word), `:1875-1882`. | Traced the dual-consumer ordering. The pre-gate `:273` adds nudges to the lootbox finalization (`_finalizeLootboxRng`) but does NOT zero the counter; `_applyDailyRng` (reached later via `rngGate:1215`) adds the SAME nudges to the daily word and zeroes (`:1881`). The lootbox word (`rngWordCurrent + nudges`) and the daily word (`rawWord + nudges`) are CONSISTENT — same `rngWordCurrent`/`rawWord` is the delivered word, same nudge applied to both representations of the SAME cycle's word, then zeroed exactly once. `_finalizeLootboxRng` is idempotent (`:1258` early-return if already set), so the `:1237` re-store on the same index is a no-op and cannot double-add. There is no path where `:273` runs and `_applyDailyRng` does NOT eventually run for the same cycle: once `:275` sets the index, the `:269` `== 0` guard is false on subsequent advances (no re-add), and the cycle only `_unlockRng`s after `_applyDailyRng` zeroed the counter. No leak, no cross-day re-grind, no double-apply. NEGATIVE-VERIFIED. |
| **SWP-01.H.4** — stuck-pending / double-request liveness-DoS on the re-issue branch + `retryLootboxRng` failsafe: can an attacker wedge the state machine (two live requestIds, a permanently-pending word, a callback that consumes the wrong word)? | NEGATIVE-VERIFIED | N-A | `AdvanceModule.sol:1788-1809` (`rawFulfillRandomWords`), `:1792` (coordinator-only), `:1793` (`requestId != vrfRequestId \|\| rngWordCurrent != 0 → return`), `:1729`/`:1735` (rotation sets fresh single `vrfRequestId`), `:1131-1154` (`retryLootboxRng` timeout-gated), `:1242-1248` (daily 12h timeout retry), `:1650-1668` (`_finalizeRngRequest` retry-vs-fresh index handling). | `vrfRequestId` is a single scalar; each re-issue (rotation `:1729`/`:1735`, daily retry `:1245`, mid-day retry `:1152`) OVERWRITES it. The callback `:1793` accepts ONLY the request matching the current `vrfRequestId` AND only when `rngWordCurrent == 0` (not yet delivered). A stale OLD-coordinator callback carries the old requestId → `requestId != vrfRequestId` → inert `return`. A late/duplicate callback after delivery → `rngWordCurrent != 0` → inert `return`. So at most one word is ever consumed per cycle, and it is always the freshest re-issued request — no double-request consumption, no two-live-word race. Liveness: a never-fulfilled daily word is recoverable by the 12h timeout re-request (`:1242-1248`); a never-fulfilled mid-day word by the permissionless `retryLootboxRng` (`:1131`, timeout+LINK-gated). No permanent wedge. NEGATIVE-VERIFIED. |
| **SWP-01.H.5** — backfill `keccak256(vrfWord, i)` foreknowledge / collision: can a player foreknow or grind the fallback word for an orphaned index `i`, or force two indices to collide on the same word? | NEGATIVE-VERIFIED | N-A | `AdvanceModule.sol:1849-1868` (`_backfillOrphanedLootboxIndices`), `:1850` (`idx = LR_INDEX`), `:1854-1855` (backward scan, break on first filled), `:1857-1861` (`fallbackWord = keccak256(vrfWord, i)`), `:1206` (called with the FRESH post-gap `currentWord`); `:1822-1843` (`_backfillGapDays` uses `keccak256(vrfWord, gapDay)` symmetrically). | `vrfWord` is the fresh post-gap daily VRF word (`currentWord` at `:1206`), unknown at any lootbox deposit/commitment time — a player cannot foreknow the fallback word. The index `i` is protocol-assigned and monotonic (`LR_INDEX`, advanced by `_finalizeRngRequest:1655` / `requestLootboxRng:1112`), not player-chosen. Collision: distinct `i` produce distinct `keccak256(vrfWord, i)` preimages (the `abi.encodePacked(vrfWord, i)` second operand differs), so two indices cannot be steered to the same word without a keccak preimage break. Backfill fills ONLY indices with `lootboxRngWordByIndex[i] == 0` (genuinely orphaned), and stops at the first filled index, so it never overwrites a delivered word. No foreknowledge, no controllable collision, no 0-entropy fallback. NEGATIVE-VERIFIED. |
| **SWP-01.H.6** — full SLOAD-in-window freshness enumeration (`feedback_rng_window_storage_read_freshness`): is there ANY non-VRF-derived storage slot consumed alongside the daily word inside the rng-lock window that has a player-controllable writer firing in `[request, unlock]`? | NEGATIVE-VERIFIED | N-A | `AdvanceModule.sol:1156-1173` (VRF bit-allocation map — all consumers), `:1875` (`totalFlipReversals` — gated by `:1929`), `:1227-1230` (`pendingResolveDay()` redemption roll — sentinel, advances only at resolve), `:289` (`bonusFlip` from `inJackpot`/`jackpotCounter`/`lvl` — protocol FSM, not player-writable mid-window); `JackpotModule` hero-override `dailyHeroWagers` (day-keyed, buffered); `v45-vrf-freeze-invariant` (verified-dispositions list). | Walked every SLOAD reachable from the daily-word resolution path. The only player-influenceable input combined with the word is `totalFlipReversals` (SWP-01.H.1: gated by `rngLockedFlag`, unreachable in-window). All other word-combined inputs are protocol-FSM-derived (`bonusFlip`, `lvl`, `jackpotCounter`), VRF-derived (the word itself, keccak-mixed for coinflip/jackpot/trait consumers `:1163-1169`), or buffered/segregated per the `v45-vrf-freeze-invariant` verified-dispositions list (`dailyHeroWagers` day-keyed → `_simulatedDayIndex` write vs `dailyIdx` read disjoint; `prizePoolsPacked` → pending buffer during freeze; `ticketQueue` double-buffered; lootbox per-index frozen by LR_INDEX rotation). No fresh player-controllable SLOAD enters a VRF-derived output this cycle. NEGATIVE-VERIFIED. |
| **SWP-02.H.1** — cross-module read/write race on `lootboxRngWordByIndex`: can `LootboxModule.openLootBox` (reader `:484`) interleave with an AdvanceModule writer (`_finalizeLootboxRng:1259`, mid-day fulfill `:1804`, backfill `:1861`) to read a half-written / pre-nudge / wrong-index word? | NEGATIVE-VERIFIED | N-A | `LootboxModule.sol:477-485` (`openLootBox`, `:484` read, `:485` `if (rngWord == 0) revert RngNotReady()`); `AdvanceModule.sol:1259`/`:1804`/`:1861` (the 3 writers, all VRF-derived); `:509` (`seed = keccak256(rngWord, player, day, amount)` consumer). | The three writers of `lootboxRngWordByIndex` all live in AdvanceModule and all source entropy from a VRF word unknown at deposit. `openLootBox` is a separate transaction; there is no mid-write interleave within a single advance (writes are atomic SSTOREs, no reentrancy mid-backfill since `_backfillOrphanedLootboxIndices` is private and makes no untrusted external call between SSTOREs). The reader gates on `rngWord == 0 → revert RngNotReady` (`:485`), so an as-yet-unwritten index cannot be opened against a zero/partial word. Once written, the index is the finalized post-nudge daily word (or raw mid-day word, or fresh-entropy backfill word), all unpredictable at deposit. The open consumes it via `keccak256(rngWord, player, day, amount)` (`:509`) — additional per-player keccak mixing. No race produces a player-favourable read. NEGATIVE-VERIFIED. |
| **SWP-02.H.2** — V-081 packing-collision + frozen-snapshot ordering: can writing one packed field of `lootboxPurchasePacked` corrupt an adjacent field, or can deposit/open ordering extract a bonus beyond the frozen `adj`? | NEGATIVE-VERIFIED | N-A | `Storage.sol:1387-1392` (`_packLootboxPurchase`: `scorePlus1 & 0xFFFF \| (adj & 0xFFFF…F)<<16 \| (baseLevelPlus1 & 0xFFFFFF)<<80`), `:1396-1401` (`_unpackLootboxPurchase` exact inverse), `:1442` (`lootboxPurchasePacked`); `LootboxModule.sol:447-462` (deposit-time cap draw + `lootboxEvBenefitUsedByLevel[player][lvl] = usedBenefit + adjustedPortion`), `:504-505` (frozen SLOAD), `:520-526` (frozen apply, NO cap re-draw), `:533` (whole-word zero-at-open). | Packing: each field is `&`-masked to its width THEN `<<`-shifted to a disjoint range ([0:16], [16:80], [80:104]); an over-wide argument is masked off before shift (`:1385-1386` NatSpec confirms intent), so no field can alias an adjacent one. `_unpackLootboxPurchase` is the exact inverse. Compiler-managed, no manual asm. Ordering: the per-(player,level) EV-benefit cap (`lootboxEvBenefitUsedByLevel`, 10 ETH) is drawn ONCE at deposit (`:462`, monotonic accumulation) and the eligible `adjustedPortion` frozen into `adj`; `openLootBox` reads the frozen `adj` and applies the multiplier WITHOUT re-drawing the cap (`:524-526`, "the cap was drawn at deposit"). Open order is irrelevant (frozen apply); deposit order cannot exceed the 10-ETH cap (capped monotonic). Penalty/neutral boxes early-return on the full amount (`:442`, never touch the cap). No EV-positive ordering, no packing corruption. NEGATIVE-VERIFIED (re-confirms the auditor's SWP-02.V081 from the timing/composition lens). |
| **SWP-02.H.3** — cross-surface composition across V-081 EV-cap × jackpot pending-pool × degenerette removal × the VRF re-issue: any differential behaviour where a state mutation on one surface changes an outcome on another in an attacker-favourable way? | NEGATIVE-VERIFIED | N-A | `JackpotModule.sol:732-751` (`distributeYieldSurplus` obligations now include `_getPendingPools()` `:746-747`); `LootboxModule.sol:447-462` (EV-cap, per-(player,level) storage); `DegeneretteModule.sol:479-498` (`degeneretteBets` + `dailyHeroWagers`, day-keyed); `AdvanceModule.sol:423` (`_distributeYieldSurplus(rngWord)` in the level-transition flow). | The three delta surfaces touch DISJOINT storage and disjoint code paths: V-081 reads/writes `lootboxEvBenefitUsedByLevel` + `lootboxPurchasePacked` (per-player lootbox state); the jackpot pending-pool fix reads `prizePoolPendingPacked` into `distributeYieldSurplus` obligations (`:746-747`, freeze-window liability — `_getPendingPools()` reads 0 when not frozen, no double-count after `_unfreezePool`); degenerette removal deleted `playerDegeneretteEthWagered`/`topDegeneretteByLevel` (dangling-ref grep ZERO) and left `dailyHeroWagers` (day-keyed, buffered) + `degeneretteBets` + `BetPlaced` untouched. The only shared touchpoint is the daily `rngWord` flowing into `_distributeYieldSurplus(rngWord)` at level transition (`:423`) — but the pending-pool obligation math is a balance-minus-liabilities subtraction (`:749 totalBal <= obligations → return`; `:751 yieldPool = totalBal - obligations`) that does not consume player-controllable mid-window state. No surface lets an attacker mutate one to gain on another. NEGATIVE-VERIFIED. |

**SWP summary:** 9 disposition rows — 9 NEGATIVE-VERIFIED + 0 SAFE_BY_DESIGN + 0 FINDING_CANDIDATE + 0 self-discards.

---

## §2 Skeptic-Filter Self-Discarded subsection

**No self-discards.** Every novel/composition/timing hypothesis probed against current source HEAD produced a concrete NEGATIVE-VERIFIED verdict with a structural-protection citation. The skeptic-filter (a)-only hard-discard arm therefore had no `FINDING_CANDIDATE` inputs; the (b)+(c) severity-downgrade arm is inapplicable. In particular, the headline hand-off hypothesis (SWP-01.H.1, `totalFlipReversals` nudge-grind across rotation) did NOT survive to candidate status — it is structurally physically unreachable in-window because `reverseFlip` reverts on `rngLockedFlag` (`DegenerusGame.sol:1929`), which stays asserted through the entire daily window including any rotation (strict structural arm). Orchestrator integration-time re-application at Task 5 re-verifies against the union of all 3 skills.

| Hypothesis-ID | Source skill | Structural-protection citation (file:line) | EV-lens failed condition | Note |
| --- | --- | --- | --- | --- |
| (none) | /zero-day-hunter | n/a | n/a | No FINDING_CANDIDATE produced; nothing to discard. |

---

## §3 Cross-skill hand-off notes

Observations to anchor the `/economic-analyst` game-theoretic / MEV lens (keeps coverage divergent; the auditor's §4 already routed the structural arms here):

- **Nudge economics (`totalFlipReversals`) — game-theory, not structural.** The hunter proved the nudge is structurally non-manipulable on a known word (gated by `rngLockedFlag`). The ECONOMIST should probe the *intended-mechanic* economics: nudge cost compounds +50% per queued nudge from a 100-BURNIE base (`DegenerusGame.sol:1942-1952`, `_currentNudgeCost`). The carry-over-on-rotation (`AdvanceModule.sol:1743-1746`) means BURNIE burned for nudges during a multi-day stall applies to the first post-swap word — confirm there is no coordinated "burn-to-bias" cartel EV given the +N is added to a uniformly-random ~uint256 word (a single +1..+K shift on a full-width modular word has negligible distributional effect on any consumer at `:1156-1173`). Likely an intended influence-not-predict sink; flag whether the EV is ever positive for a coordinated burn.
- **Mid-day window nudge timing.** SWP-01.H.2 found a nudge queued during a mid-day window (where `rngLockedFlag == false`) is buffered to the next daily word. The economist should confirm there is no profitable timing where a player front-loads cheap nudges during a long mid-day stall to bias the next daily word at a discount (the +50% compounding and the full-width-word dilution should make this EV-neutral, but it is a timing surface).
- **V-081 frozen activity-score snapshot timing.** The hunter confirmed `scorePlus1`/`adj` are frozen at deposit (`LootboxModule.sol:504-505`, order-independent apply). The economist should re-confirm (per the auditor's hand-off) whether timing a deposit at a high-activity-score moment for a frozen-in bonus is an INTENDED engagement mechanic vs a gameable edge — purely an EV/incentive question, structurally closed.
- **Jackpot pending-pool surplus split.** Obligations now include the freeze-window pending buffer (`JackpotModule.sol:746-747`). The economist should probe whether a coordinated actor can time freeze-window revenue to influence the `yieldPool` surplus (the `_getPendingPools` 0-when-not-frozen read should close it — confirm from the distribution-share game-theory angle).
- **VRF-rotation MEV.** Rotation is ADMIN-only + VRF-derived (no mempool-visible roll); the `:1793` stale-callback guard bounds any backrun. The economist should confirm no extractable backrun/sandwich on a rotation tx (the hunter sees none structurally).

---

## §4 Summary

| Bucket | Count |
| --- | --- |
| Hypotheses charged (novel/composition/timing) | 9 (SWP-01.H.1..6 + SWP-02.H.1..3) |
| Total disposition rows | 9 |
| NEGATIVE-VERIFIED | 9 |
| SAFE_BY_DESIGN | 0 |
| FINDING_CANDIDATE | 0 |
| Skeptic-filter self-discards | 0 |
| Severity downgrades | 0 (no findings to downgrade) |

**Verdict:** `/zero-day-hunter` PARALLEL_SUBAGENT novel-surface pass produces **0 FINDING_CANDIDATE** rows. The headline hand-off hypothesis — a `totalFlipReversals` nudge-grind across the VRF-rotation boundary (the `feedback_rng_commitment_window` concern) — is structurally closed: `reverseFlip` reverts on `rngLockedFlag` (`DegenerusGame.sol:1929`), and `rngLockedFlag` is asserted from daily-request (`AdvanceModule.sol:1669`) through to `_unlockRng` (`:1774`), INCLUDING the rotation re-issue which keeps it true (`:1731-1740`) and the fulfillment callback which leaves it true (`:1798-1800`). The nudge counter cannot be written while ANY daily word is in flight, the mid-day word never consumes nudges (stored raw `:1804`), and the dual-consumer (`:273` lootbox / `:1881` daily) applies the same committed nudge to the same cycle's word exactly once with no leak or cross-day re-grind. The cross-module `lootboxRngWordByIndex` reader/writer set (3 writers, all VRF-derived; reader gated by `RngNotReady`) admits no exploitable interleave; the backfill `keccak256(vrfWord, i)` is foreknowledge- and collision-resistant (fresh VRF entropy, monotonic protocol-assigned index); V-081 packing is mask-then-shift disjoint with a frozen order-independent apply; and the three consolidated deltas touch disjoint storage with no cross-surface differential. Game-theoretic / MEV residuals routed to `/economic-analyst` in §3.

---

*Phase 314 / Plan 01 / Task 3 / `/zero-day-hunter` PARALLEL_SUBAGENT / 2026-05-23.*
