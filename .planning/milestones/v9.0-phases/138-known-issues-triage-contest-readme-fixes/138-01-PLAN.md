---
phase: 138-known-issues-triage-contest-readme-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - KNOWN-ISSUES.md
autonomous: false
requirements:
  - KI-01
  - KI-02
  - KI-03
  - KI-04
  - KI-05

must_haves:
  truths:
    - "Every KNOWN-ISSUES.md entry is classified as KNOWN-ISSUE or DESIGN-DOC with rationale"
    - "Bootstrap assumption says DGNRS (not sDGNRS), vests over 30 levels, with 50B initial + 5B/level"
    - "Fuzzy claims have worst-case quantified bounds"
    - "Creator vesting (claimVested) is documented in KNOWN-ISSUES.md"
    - "unwrapTo guard change (5h timestamp to rngLocked boolean) is documented in KNOWN-ISSUES.md"
    - "DESIGN-DOC entries that are not warden filing risks remain in KNOWN-ISSUES.md but clearly marked"
  artifacts:
    - path: "KNOWN-ISSUES.md"
      provides: "Triaged known issues with accurate admin resistance, quantified claims, new entries"
      contains: "claimVested"
  key_links:
    - from: "KNOWN-ISSUES.md"
      to: "contracts/DegenerusStonk.sol"
      via: "Vesting constants and rngLocked guard documented accurately"
      pattern: "50B initial.*5B/level|rngLocked"
---

<objective>
Triage every entry in KNOWN-ISSUES.md: classify each as KNOWN-ISSUE or DESIGN-DOC, fix factual errors (bootstrap assumption says sDGNRS but should say DGNRS), quantify fuzzy claims with worst-case bounds, and add two new entries for creator DGNRS vesting and the rngLocked guard change.

Purpose: Wardens and judges work from accurate, precise, defensible documentation. Imprecise claims leave adjacent attack surface gameable.
Output: Updated KNOWN-ISSUES.md with all entries triaged and corrected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-known-issues-triage-contest-readme-fixes/138-CONTEXT.md
@KNOWN-ISSUES.md
@contracts/DegenerusStonk.sol
@contracts/StakedDegenerusStonk.sol
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triage all entries and produce classification table</name>
  <files>KNOWN-ISSUES.md, contracts/DegenerusStonk.sol, contracts/StakedDegenerusStonk.sol, contracts/BurnieCoinflip.sol, contracts/DegenerusGame.sol</files>
  <read_first>
    - KNOWN-ISSUES.md (current state of every entry)
    - contracts/DegenerusStonk.sol (vesting constants, rngLocked guard, claimVested function)
    - contracts/StakedDegenerusStonk.sol (CREATOR_BPS, pool allocation constants)
    - contracts/BurnieCoinflip.sol (recycling bonus constants for quantification)
    - contracts/DegenerusGame.sol (BPS constants for rounding quantification)
  </read_first>
  <action>
Read every entry in KNOWN-ISSUES.md and classify each as either:
- KNOWN-ISSUE: A warden could file this as a finding. It describes a real deviation, tradeoff, or risk. Needs to stay in KNOWN-ISSUES.md so judges reject duplicate filings.
- DESIGN-DOC: Explains how the code works, not a vulnerability or tradeoff. Not a filing risk. Per D-02, these should ideally move to NatSpec but per D-03 user reviews first.

For each entry, produce a classification line in a markdown table with columns: Entry Title | Classification | Rationale (one sentence).

Classification guidance per D-01:
- ERC-20 deviations = KNOWN-ISSUE (wardens absolutely will file these)
- Governance scenarios (VRF swap, price feed swap) = KNOWN-ISSUE (admin resistance concerns)
- Automated tool findings = KNOWN-ISSUE (wardens run the same tools)
- "Boon coexistence" = DESIGN-DOC (explains storage layout, no filing risk)
- "Recycling bonus uses total claimable" = DESIGN-DOC (explains a parameter change, no filing risk)
- "Feed governance uses live circulating supply" = DESIGN-DOC (explains an asymmetry, borderline but lean DESIGN-DOC)
- Event design decisions = KNOWN-ISSUE (wardens file missing-event findings)

Additionally, identify:
1. The bootstrap assumption error: currently says "admin holds majority of sDGNRS" -- must be corrected to say admin holds DGNRS (per D-07)
2. Fuzzy claims needing quantification: "All rounding favors solvency" needs worst-case wei bound; affiliate timing extraction needs ETH bound
3. Missing entries: creator DGNRS vesting (D-11) and rngLocked guard change (D-12)

Output the classification table to stdout for the checkpoint task. Do NOT modify KNOWN-ISSUES.md yet -- per D-03, user reviews first.
  </action>
  <verify>
    <automated>grep -c "KNOWN-ISSUE\|DESIGN-DOC" /dev/stdin <<< "$(cat /tmp/ki-triage-table.md 2>/dev/null)" || echo "Table should be printed to stdout for checkpoint review"</automated>
  </verify>
  <done>Classification table produced covering every KNOWN-ISSUES.md entry with KNOWN-ISSUE or DESIGN-DOC label and one-sentence rationale. Bootstrap error identified. Fuzzy claims identified. Two new entries drafted.</done>
  <acceptance_criteria>
    - Executor prints a markdown table with columns: Entry Title | Classification | Rationale
    - Every bold-header entry in KNOWN-ISSUES.md appears in the table (minimum 20 entries)
    - "Multi-category boon coexistence" classified as DESIGN-DOC
    - "Recycling bonus uses total claimable" classified as DESIGN-DOC
    - VRF swap governance classified as KNOWN-ISSUE
    - All ERC-20 deviation entries classified as KNOWN-ISSUE
    - Bootstrap assumption error flagged (sDGNRS -> DGNRS)
    - "All rounding favors solvency" flagged as fuzzy claim needing quantification
    - Draft text for creator vesting entry included
    - Draft text for rngLocked guard change entry included
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User reviews classification table</name>
  <files>KNOWN-ISSUES.md</files>
  <action>Present the classification table from Task 1 to the user for review. Per D-03, user approves or overrides individual KNOWN-ISSUE vs DESIGN-DOC classifications before any changes are applied to KNOWN-ISSUES.md. Also present the bootstrap assumption correction, fuzzy claim quantification approach, and the two new entry drafts (creator vesting, rngLocked guard change) for approval.</action>
  <what-built>Classification of every KNOWN-ISSUES.md entry as KNOWN-ISSUE or DESIGN-DOC, plus identification of factual errors, fuzzy claims, and two new entries to add. Per D-03, user approves or overrides individual classifications before changes are applied.</what-built>
  <how-to-verify>
    1. Review the classification table printed by Task 1
    2. Check that each entry's KNOWN-ISSUE vs DESIGN-DOC label matches your intent
    3. Review the bootstrap assumption correction (sDGNRS -> DGNRS + vesting schedule)
    4. Review the fuzzy claim quantification approach (worst-case bounds)
    5. Review the two new entry drafts (creator vesting, rngLocked guard change)
    6. Override any classifications you disagree with
  </how-to-verify>
  <verify>User provides "approved" or specific overrides</verify>
  <done>User has approved or overridden all classifications, and any overrides are recorded for Task 3</done>
  <resume-signal>Type "approved" to proceed with all classifications as-is, or list specific overrides (e.g., "change X to KNOWN-ISSUE because Y")</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply triage changes to KNOWN-ISSUES.md</name>
  <files>KNOWN-ISSUES.md</files>
  <read_first>
    - KNOWN-ISSUES.md (current full text to modify)
    - contracts/DegenerusStonk.sol (exact vesting constants for accurate documentation)
    - contracts/StakedDegenerusStonk.sol (CREATOR_BPS value for accurate documentation)
  </read_first>
  <action>
Apply the user-approved classifications to KNOWN-ISSUES.md. Specific changes:

**Structure change:** Keep the existing section headers (Design Decisions, Automated Tool Findings, ERC-20 Deviations, Event Design Decisions). DESIGN-DOC entries stay in the file but are clearly under "Design Decisions" header. KNOWN-ISSUE entries stay in their respective sections.

**KI-02 — Fix bootstrap assumption (both VRF and price feed governance entries):**
Replace "admin holds a majority of sDGNRS" with accurate text per D-05 and D-07:
- Creator receives DGNRS (not sDGNRS) that vests over 30 levels (50B initial allocation + 5B per level to vault owner via claimVested())
- To vote, creator must unwrap DGNRS to sDGNRS via unwrapTo -- but unwrapTo is blocked when rngLocked() is true (during active VRF request/fulfillment)
- Admin cannot dominate governance once community sDGNRS share exceeds the creator's unwrapped allocation
- Per D-06: VRF coordinator swap and price feed swap require Chainlink death clock to trigger first (20h+ VRF stall or 2d+/7d+ feed stall respectively), making exploitation require: compromised admin + Chainlink failure + community inattention

**KI-03 — Quantify fuzzy claims:**
For "All rounding favors solvency": Per D-04, if the worst case is dust, give a quick estimate. Read BPS constants from contracts. For a 10,000 BPS split, max rounding loss per operation is 1 wei per recipient. Over a max-length game (~500 levels, ~50 operations per level), worst-case cumulative rounding retention is ~25,000 wei (~0.000000000025 ETH). State this bound explicitly.

For affiliate timing extraction: the affiliate winner is chosen by deterministic seed (block.timestamp, totalPurchaseCount, etc). Worst case: player times a purchase to direct affiliate credit to a preferred affiliate. The credit is a share of the purchase BPS allocation to affiliates. No protocol ETH extraction -- credit goes to a valid affiliate either way.

**KI-04 — Admin resistance bootstrap per D-05, D-07:**
Already covered by the bootstrap assumption fix above. Ensure the framing emphasizes the security property: "admin governance power is bounded by vesting schedule and decays as community accumulates sDGNRS."

**KI-05 — Add two new entries under Design Decisions:**

New entry 1 - Creator DGNRS Vesting (per D-11):
"**Creator DGNRS vesting.** The vault owner receives DGNRS tokens on a vesting schedule: 50B initial allocation plus 5B per game level, claimable via `claimVested()`. Fully vested at level 30 (200B total). DGNRS can be unwrapped to sDGNRS via `unwrapTo` for governance participation, but unwrapping is blocked during active VRF windows (`rngLocked()` guard). This vesting schedule determines the admin's maximum governance weight at each stage of the game."

New entry 2 - rngLocked Guard (per D-12):
"**unwrapTo uses rngLocked guard.** The `unwrapTo` function (DGNRS to sDGNRS conversion) is blocked when `rngLocked()` returns true, which indicates an active VRF request awaiting fulfillment. This replaced the previous 5-hour `lastVrfProcessed` timestamp check. The new guard is tighter: it only blocks during the actual VRF request/fulfillment window (typically seconds to minutes), not for a fixed 5-hour cooldown. This prevents governance vote manipulation via just-in-time sDGNRS minting during VRF-dependent game state transitions."
  </action>
  <verify>
    <automated>grep -c "claimVested\|rngLocked\|50B initial\|DGNRS (not sDGNRS)\|DGNRS.*vests\|worst-case" /home/zak/Dev/PurgeGame/degenerus-audit/KNOWN-ISSUES.md</automated>
  </verify>
  <done>
    - Bootstrap assumption corrected from sDGNRS to DGNRS with vesting schedule in both governance entries
    - Fuzzy claims have explicit worst-case bounds
    - Creator vesting entry added
    - rngLocked guard change entry added
    - DESIGN-DOC entries remain under Design Decisions section
  </done>
  <acceptance_criteria>
    - KNOWN-ISSUES.md contains "claimVested" (new vesting entry)
    - KNOWN-ISSUES.md contains "rngLocked" in a new entry about the guard change
    - KNOWN-ISSUES.md contains "50B initial" (vesting constants)
    - VRF swap governance entry no longer says "admin holds a majority of sDGNRS"
    - VRF swap governance entry mentions "DGNRS" and "vests" or "vesting"
    - "All rounding favors solvency" entry contains a wei-denominated worst-case bound
    - grep -c "DGNRS.*vesting\|claimVested\|rngLocked.*guard\|worst-case.*wei" KNOWN-ISSUES.md returns at least 3
  </acceptance_criteria>
</task>

</tasks>

<verification>
After all tasks complete:
1. Every entry in KNOWN-ISSUES.md has been reviewed and classified
2. No entry says "sDGNRS" where it should say "DGNRS" in bootstrap assumptions
3. Fuzzy claims have quantified bounds
4. Two new entries exist (vesting, rngLocked)
5. File is well-structured with clear section headers
</verification>

<success_criteria>
- KNOWN-ISSUES.md accurately reflects current contract state
- Bootstrap assumption corrected (DGNRS not sDGNRS, vesting schedule)
- All fuzzy claims quantified with worst-case bounds
- Vesting (claimVested) documented
- rngLocked guard change documented
- User approved all classifications before changes were applied
</success_criteria>

<output>
After completion, create `.planning/phases/138-known-issues-triage-contest-readme-fixes/138-01-SUMMARY.md`
</output>
