---
phase: 330-impl-the-one-batched-contract-diff-router-advance-rework-mic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/modules/DegenerusGameAdvanceModule.sol
autonomous: true
requirements: [ADV-01, ADV-02, ADV-03, ADV-05]
must_haves:
  truths:
    - "Standalone advanceGame() pays NO bounty (the 3 in-callee creditFlip sites are gone)"
    - "advanceGame() returns (uint8 mult, bool rewardable) carrying the canonical day-epoch stall multiplier"
    - "The new-day path returns mult = the stall ladder (1/2/4/6); mid-day partial-drain and gameover partial-drain return mult = 1 (no escalation)"
    - "The ticket-drain / day-advance / RNG logic is byte-unchanged — only the bounty payment is removed"
  artifacts:
    - path: "contracts/modules/DegenerusGameAdvanceModule.sol"
      provides: "advanceGame producing (uint8 mult, bool rewardable); zero advance creditFlip to the caller"
      contains: "returns (uint8 mult, bool rewardable)"
  key_links:
    - from: "DegenerusGameAdvanceModule.advanceGame"
      to: "the (mult, rewardable) tuple"
      via: "ABI return decoded by the DegenerusGame.advanceGame wrapper (330-02)"
      pattern: "returns \\(uint8 mult, bool rewardable\\)"
---

<objective>
AdvanceModule is the ROOT PRODUCER of the redesigned advance surface (329-SPEC §1 R1, §3 edit-order step 1). Delete the 3 in-callee advance-bounty `creditFlip(caller, …)` sites (ADV-01) and instead RETURN `(uint8 mult, bool rewardable)` (ADV-02) so the unified router (`AfKing.doWork`, 330-04) pays the re-homed bounty from the multiplier's canonical day-epoch home — never recomputing it in a money path. Standalone `advanceGame()` stays fully functional and UNREWARDED (ADV-03). The mid-day partial-drain stays router-rewardable advance-leg work but returns `mult = 1` (ADV-05/D-07 — NO mid-day escalation).

This is the FIRST edit applied in the single BATCH-02 batched diff so no intermediate state ships where advancing is unrewarded (T-329-R1).

Purpose: re-home the advance bounty into the router and surface the stall multiplier as a typed return so the consumer pays once.
Output: `contracts/modules/DegenerusGameAdvanceModule.sol` with the 3 caller-creditFlips removed and an `(uint8 mult, bool rewardable)` return on `advanceGame()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md

<interfaces>
<!-- Attested @ 0cc5d10f (329-ATTEST-ROUTER-ADVANCE.md §D). Re-grep at edit time — the held-330 tree is DIRTY; greps are baseline-anchored. -->

The 3 advance creditFlip sites to DELETE (all pay `caller`, the advance bounty), classified by 329-ATTEST §D.2 U1/U2/U3:
- U1 — `AdvanceModule:189` (math `:191`, NO bountyMultiplier): inside `_handleGameOverPath`'s `goStage == STAGE_TICKETS_WORKING` branch (the gameover partial-drain). → mult = 1.
- U2 — `AdvanceModule:225` (math `:227`, NO bountyMultiplier): inside the MID-DAY path (`if (day == dailyIdx)`) when `_runProcessTicketBatch` did work (`ticketWorked || !ticketsFinished`). → mult = 1 (ADV-05/D-07 mid-day no-escalation).
- U3 — `AdvanceModule:468` (math `:470`, APPLIES `bountyMultiplier`): the NEW-DAY advance leg, after the KEPT GAME-day stall epoch block (`:241-253`, epoch `:243-246`, bands `:248` 2h→6 / `:250` 1h→4 / `:252` 20m→2). → mult = the stall ladder (1/2/4/6).

The reward arithmetic being deleted (all three): `(ADVANCE_BOUNTY_ETH * PRICE_COIN_UNIT [* bountyMultiplier]) / PriceLookupLib.priceForLevel(lvl)`.

The KEPT, non-caller creditFlip that STAYS (329-ATTEST §D.2 U6): `AdvanceModule:876` `coinflip.creditFlip(ContractAddresses.SDGNRS, …)` — payee is `ContractAddresses.SDGNRS`, NOT the caller; it is a game-economic SDGNRS credit on the gameover-RNG path, NOT a keeper bounty. DO NOT remove it.

`ADVANCE_BOUNTY_ETH` constant: `AdvanceModule:147` (`uint256 private constant ADVANCE_BOUNTY_ETH = 0.005 ether;`). Becomes dead once all 3 caller-creditFlips are removed — delete it (it is NOT a GAS-331 peg placeholder; the re-pegged advance reward is computed in the router from `mult`).

The KEPT GAME-day stall epoch (329-ATTEST §D.1 D3, `:241-253`): the `uint256 bountyMultiplier = 1; { uint256 dayStart = (uint256(day - 1) + ContractAddresses.DEPLOY_DAY_BOUNDARY) * 1 days + 82_620; … }` block computing 1/2/4/6 from elapsed. This is the SOLE surviving stall epoch (GAS-03 satisfied-by-deletion; AfKing's absolute-day epoch is deleted in 330-04). The `mult` returned on the new-day path IS this `bountyMultiplier`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete the 3 advance caller-bounty creditFlip sites + the dead ADVANCE_BOUNTY_ETH constant (ADV-01)</name>
  <read_first>
    - contracts/modules/DegenerusGameAdvanceModule.sol (the file being modified — read the whole advanceGame body :155-475, the _handleGameOverPath branch, the mid-day path, the new-day path, and the constant decl :147)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md (§D.2 U1/U2/U3/U6 classification — the authoritative anchor list)
    - Baseline anchors are vs 0cc5d10f; re-grep at edit time: `git show 0cc5d10f:contracts/modules/DegenerusGameAdvanceModule.sol | grep -n "creditFlip\|ADVANCE_BOUNTY_ETH"`
  </read_first>
  <action>
    Remove the three caller-paying `coinflip.creditFlip(caller, …)` statements at the U1 (~`:189`), U2 (~`:225`), and U3 (~`:468`) sites — the gameover partial-drain bounty, the mid-day partial-drain bounty, and the new-day bounty respectively. In each case remove ONLY the `creditFlip` call and its reward-arithmetic expression; PRESERVE every surrounding control-flow statement byte-faithfully: the `emit Advance(STAGE_TICKETS_WORKING, lvl)` / `emit Advance(stage, lvl)` emits, the `ticketsFullyProcessed`/`_lrWrite` writes, the early `return` statements, the `_unlockRng`/`payDailyJackpot` calls, and the `do { … } while(false)` structure. The ticket-drain, day-advance, gameover, and RNG-consume logic MUST be byte-identical — only the bounty payment leaves.

    DELETE the now-dead `ADVANCE_BOUNTY_ETH` constant decl (~`:147`) after confirming no remaining consumer (grep `ADVANCE_BOUNTY_ETH` returns 0 references after the 3 deletions).

    DO NOT touch the U6 site `coinflip.creditFlip(ContractAddresses.SDGNRS, …)` (~`:876`) — its payee is SDGNRS, not the caller; it is a game-economic credit, not a keeper bounty, and STAYS verbatim (per 329-ATTEST §D.2 U6 / R4 "1 stays").

    Leave the KEPT GAME-day stall epoch block (~`:241-253`) computing `bountyMultiplier` (1/2/4/6) IN PLACE — Task 2 consumes its value as the returned `mult`. Do NOT delete `coinflip` imports/handles (still used by U6).
  </action>
  <verify>
    <automated>git show 0cc5d10f:contracts/modules/DegenerusGameAdvanceModule.sol > /tmp/adv_base.sol; grep -v '^[[:space:]]*//' contracts/modules/DegenerusGameAdvanceModule.sol | grep -c "creditFlip(\s*caller\|creditFlip(caller" | grep -qx 0 && grep -v '^[[:space:]]*//' contracts/modules/DegenerusGameAdvanceModule.sol | grep -c "ADVANCE_BOUNTY_ETH" | grep -qx 0 && grep -c "creditFlip(ContractAddresses.SDGNRS\|creditFlip(\s*ContractAddresses.SDGNRS" contracts/modules/DegenerusGameAdvanceModule.sol | grep -qx 1 && echo PASS</automated>
  </verify>
  <done>Zero caller-paying creditFlip statements remain (3 removed); `ADVANCE_BOUNTY_ETH` is gone with no dangling reference; the single SDGNRS creditFlip (U6) survives; all surrounding emits/returns/RNG logic byte-preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add the (uint8 mult, bool rewardable) return to advanceGame() (ADV-02/03/05)</name>
  <read_first>
    - contracts/modules/DegenerusGameAdvanceModule.sol (the just-edited file — the 3 return points + the stall-epoch block)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md (§1 R1 + §2 ADV-02/ADV-05 + §0 design-1 verdict — mid-day/gameover mult=1, new-day mult=stall ladder, rewardable = distinct gameover-only-false bool)
  </read_first>
  <action>
    Change the `advanceGame()` signature (module entry ~`:155`) to `function advanceGame() external returns (uint8 mult, bool rewardable)`. Wire each exit to produce the tuple per 329-SPEC §1 R1 / the design-1 verdict:
    - The gameover partial-drain exit (U1 path, `goStage == STAGE_TICKETS_WORKING`): set `mult = 1`, `rewardable = true` and `return (mult, rewardable)`.
    - The gameover NON-rewardable exits (the other `_handleGameOverPath` `goReturn` branches that today just `return` without any caller bounty): `rewardable = false` (the distinct gameover-only-false path), `mult = 1`.
    - The mid-day partial-drain exit (U2 path, after the `emit Advance(STAGE_TICKETS_WORKING, lvl)`): `mult = 1` (NO escalation, ADV-05/D-07), `rewardable = true`.
    - The mid-day `revert NotTimeYet()` path: unchanged (a revert, not a return — no tuple).
    - The new-day exit (U3 path, after the stall-epoch block + `emit Advance(stage, lvl)`): `mult = bountyMultiplier` (the 1/2/4/6 from the KEPT epoch block), `rewardable = true`.

    Use named returns or explicit `return (…, …)` consistently; default the named returns to `(1, false)` so any path that falls through without a rewarded action yields `rewardable = false` (advance ran but earned nothing — e.g. a gameover non-rewardable advance). The standalone `advanceGame()` remains fully functional: callers that ignore the return value (e.g. `DegenerusVault.gameAdvance()` / `StakedDegenerusStonk.gameAdvance()`, which call it without decoding) compile and behave identically (Solidity allows discarding return values on an external call). Do NOT add any new SLOAD of player-controllable state into the advance-consume window (ADV-04 — the `mult` comes from the already-present epoch block, not a fresh read).
  </action>
  <verify>
    <automated>grep -c "function advanceGame() external returns (uint8 mult, bool rewardable)" contracts/modules/DegenerusGameAdvanceModule.sol | grep -qx 1 && echo PASS</automated>
  </verify>
  <done>`advanceGame()` declares `returns (uint8 mult, bool rewardable)`; the new-day path returns `mult = bountyMultiplier` (1/2/4/6); mid-day & gameover partial-drains return `mult = 1, rewardable = true`; gameover non-rewardable paths return `rewardable = false`; no new in-window SLOAD introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keeper (any EOA) → AfKing.doWork → DegenerusGame.advanceGame wrapper → this module (delegatecall) | the advance-consume runs here; the returned `(mult, rewardable)` becomes a bounty in the router |
| advance-consume ↔ VRF freeze window | `totalFlipReversals` / `rngWordCurrent` are read into the consumed word inside `advanceGame` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-330-01-01 | Tampering | the `mult` return on the new-day path | mitigate | `mult` is read from the EXISTING KEPT epoch block (`:241-253`), a `block.timestamp`-derived stall ladder — NOT a fresh player-controllable SLOAD. No new in-window read introduced (ADV-04 / invariant b preserved). |
| T-330-01-02 | Elevation of Privilege | standalone advanceGame() now returns a tuple | accept | The bounty is no longer paid here; a direct (non-router) `advanceGame()` caller earns nothing (ADV-03). Re-homing removes NO structural fallback caller (Vault/sStonk `gameAdvance()` + 30-min bypass + death-clock all untouched, 329-ATTEST §G). |
| T-330-01-03 | Denial of Service | removing the U1/U2/U3 creditFlips mid-edit | mitigate | This is the FIRST edit in the single BATCH-02 diff; the bounty is re-homed in `doWork` (330-04) within the same diff. No intermediate state ships where advancing is unrewarded (T-329-R1; producer-before-consumer). |
| T-330-01-SC | Tampering | npm/forge installs | mitigate | No package installs in this plan (source edit only). |
</threat_model>

<verification>
- `git show 0cc5d10f:contracts/modules/DegenerusGameAdvanceModule.sol | grep -n "creditFlip"` (baseline) shows 4 sites (U1/U2/U3 caller + U6 SDGNRS); after this plan only the U6 SDGNRS site remains.
- The file compiles as part of the BATCH-02 `forge build` (330-07) — it will not compile standalone until the wrapper (330-02) and interface (330-03) consume the new return; that is expected and gated by BATCH-02.
- No new mutable in-window SLOAD added to the advance-consume (ADV-04).
</verification>

<success_criteria>
- ADV-01: the 3 caller-paying advance creditFlip sites removed; standalone advanceGame() pays no bounty.
- ADV-02: advanceGame() returns `(uint8 mult, bool rewardable)`; new-day mult = stall ladder, mid-day/gameover mult = 1.
- ADV-03: standalone advanceGame() stays fully functional + unrewarded.
- ADV-05: mid-day partial-drain returns rewardable = true with mult = 1 (router-rewardable, no escalation).
</success_criteria>

<output>
Create `.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-01-SUMMARY.md` when done.
</output>
