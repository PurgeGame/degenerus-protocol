---
phase: 330-impl-the-one-batched-contract-diff-router-advance-rework-mic
plan: 04
type: execute
wave: 4
depends_on: ["330-01", "330-02"]
files_modified:
  - contracts/interfaces/IDegenerusGameModules.sol
autonomous: true
requirements: [ADV-02]
must_haves:
  truths:
    - "IDegenerusGameAdvanceModule.advanceGame() declares returns (uint8 mult, bool rewardable) so the wrapper's abi.decode of the delegatecall return type-matches"
    - "No degeneretteResolve interface row is added (the symbols are defined directly on DegenerusGame, ABSENT from all interface files)"
  artifacts:
    - path: "contracts/interfaces/IDegenerusGameModules.sol"
      provides: "IDegenerusGameAdvanceModule.advanceGame() tuple signature"
      contains: "function advanceGame() external returns (uint8 mult, bool rewardable)"
  key_links:
    - from: "DegenerusGame.advanceGame wrapper"
      to: "IDegenerusGameAdvanceModule.advanceGame.selector + abi.decode return shape"
      via: "the interface return signature must match the module + the wrapper decode"
      pattern: "advanceGame\\(\\) external returns \\(uint8 mult, bool rewardable\\)"
---

<objective>
The interfaces edit (329-SPEC §3 step 3). The ONLY global-interface change the redesign requires is the `IDegenerusGameAdvanceModule.advanceGame()` return signature: the `DegenerusGame.advanceGame` wrapper (330-02 Task 1) delegatecalls via `IDegenerusGameAdvanceModule.advanceGame.selector` and `abi.decode`s the return — the interface's declared return type MUST match the module's new `(uint8 mult, bool rewardable)` (330-01 Task 2).

Confirmed scope-limiters (329-ATTEST):
- The global `IDegenerusGame.sol` declares NONE of the router functions (`autoOpen`/`autoBuy`/`advanceGame`/`advanceDue`/`boxesPending`/`batchPurchase`/`keeperSnapshot`) — the keeper consumes those through AfKing's OWN local `IGame` interface (defined in AfKing.sol, edited in 330-06), NOT the global interfaces file. So no `IDegenerusGame.sol` edit is needed for the router surface.
- `degeneretteResolve`/`_degeneretteResolveBet` are ABSENT from every `contracts/interfaces/` file (329-ATTEST-DEGENERETTE-RESOLVE §A.2) — NO interface row to rename.

Purpose: keep the advance-module interface in lock-step with the new return type so the wrapper decode compiles.
Output: `contracts/interfaces/IDegenerusGameModules.sol` with the advance tuple signature.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md

<interfaces>
<!-- Attested @ 0cc5d10f. Re-grep at edit time. -->

`contracts/interfaces/IDegenerusGameModules.sol`:
- `interface IDegenerusGameAdvanceModule { … function advanceGame() external; … }` at `:8`/`:10`. → change to `function advanceGame() external returns (uint8 mult, bool rewardable);`.

The wrapper (330-02) calls `ContractAddresses.GAME_ADVANCE_MODULE.delegatecall(abi.encodeWithSelector(IDegenerusGameAdvanceModule.advanceGame.selector))` and `abi.decode(data, (uint8, bool))`. The selector is computed from the signature — `advanceGame()` has the SAME 4-byte selector with or without a `returns` clause (the selector excludes return types), so the delegatecall dispatch is unchanged; the `returns` clause only documents the decode shape. Adding it keeps the interface honest and lets any future typed caller decode the tuple.

The global `IDegenerusGame.sol` is NOT edited (the router views/keeperSnapshot/autoOpen live in AfKing's local IGame, 330-06). Do NOT add `degeneretteResolve` anywhere in `contracts/interfaces/` (ABSENT — §A.2).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add the (uint8 mult, bool rewardable) return to IDegenerusGameAdvanceModule.advanceGame() (ADV-02)</name>
  <read_first>
    - contracts/interfaces/IDegenerusGameModules.sol (the IDegenerusGameAdvanceModule interface, advanceGame decl :10)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md (§1 R1 producer→consumer chain: module → wrapper → interface → AfKing)
  </read_first>
  <action>
    In `interface IDegenerusGameAdvanceModule`, change `function advanceGame() external;` (`:10`) to `function advanceGame() external returns (uint8 mult, bool rewardable);` to match the module's new return (330-01) and the wrapper's `abi.decode` (330-02). Do NOT touch the other rows in this interface or any other interface in the file. Do NOT add a `degeneretteResolve` row anywhere (ABSENT, §A.2). Do NOT edit `contracts/interfaces/IDegenerusGame.sol` (the router surface the keeper uses lives in AfKing's local `IGame`, 330-06).
  </action>
  <verify>
    <automated>grep -c "function advanceGame() external returns (uint8 mult, bool rewardable);" contracts/interfaces/IDegenerusGameModules.sol | grep -qx 1 && grep -c "degeneretteResolve\|autoResolve" contracts/interfaces/IDegenerusGameModules.sol | grep -qx 0 && git show 0cc5d10f:contracts/interfaces/IDegenerusGame.sol > /tmp/idg_base.sol && diff -q /tmp/idg_base.sol contracts/interfaces/IDegenerusGame.sol && echo PASS</automated>
  </verify>
  <done>`IDegenerusGameAdvanceModule.advanceGame()` declares `returns (uint8 mult, bool rewardable)`; no `degeneretteResolve`/`autoResolve` row in any interface; the global `IDegenerusGame.sol` is byte-unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DegenerusGame wrapper → IDegenerusGameAdvanceModule (delegatecall) | the interface declares the ABI return shape the wrapper decodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-330-04-01 | Tampering | the delegatecall selector changing under the new return | accept | The 4-byte selector excludes return types — `advanceGame()` keeps the same selector with or without `returns`, so the delegatecall dispatch is unchanged; the `returns` clause only documents the decode shape (the wrapper decodes the raw `data` regardless). No dispatch risk. |
| T-330-04-02 | Tampering | accidental edit to the global IDegenerusGame.sol | mitigate | The verify diff asserts `contracts/interfaces/IDegenerusGame.sol` is byte-identical to the baseline — the router surface deliberately lives in AfKing's local IGame, not here. |
| T-330-04-SC | Tampering | npm/forge installs | mitigate | No package installs (source edit only). |
</threat_model>

<verification>
- The interface return type matches the module (330-01) and the wrapper decode (330-02) — proven by the BATCH-02 `forge build` (330-08).
- The global `IDegenerusGame.sol` stays byte-identical to baseline (the router surface is AfKing-local).
</verification>

<success_criteria>
- ADV-02: `IDegenerusGameAdvanceModule.advanceGame()` declares the `(uint8 mult, bool rewardable)` return so the wrapper decode type-matches; no spurious interface rows added.
</success_criteria>

<output>
Create `.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-04-SUMMARY.md` when done.
</output>
