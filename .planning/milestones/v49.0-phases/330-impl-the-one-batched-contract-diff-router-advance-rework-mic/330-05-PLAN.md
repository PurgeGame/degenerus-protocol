---
phase: 330-impl-the-one-batched-contract-diff-router-advance-rework-mic
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/modules/DegenerusGameMintModule.sol
autonomous: true
requirements: [GASOPT-01]
must_haves:
  truths:
    - "Both per-ticket loops read/write through a hoisted storage pointer to ticketsOwedPacked[rk] instead of re-indexing [rk] every access"
    - "Behavior is byte-identical — the same packed values are read and written; gas-only change"
  artifacts:
    - path: "contracts/modules/DegenerusGameMintModule.sol"
      provides: "hoisted ticketsOwedPacked[rk] storage pointer in processFutureTicketBatch + processTicketBatch"
      contains: "storage"
  key_links:
    - from: "processTicketBatch / processFutureTicketBatch"
      to: "ticketsOwedPacked[rk]"
      via: "loop-invariant storage-pointer hoist (rk is fixed before the player loop)"
      pattern: "= ticketsOwedPacked\\[rk\\];"
---

<objective>
GASOPT-01 (329-SPEC §3 step 5, a SURVIVOR carry — independent, lands anywhere in the batched diff). Hoist the loop-invariant `ticketsOwedPacked[rk]` storage-pointer in `DegenerusGameMintModule.sol`'s two per-ticket loops: `processFutureTicketBatch` (rk computed at `:398`) and `processTicketBatch` (rk computed at `:671`). `rk` is computed ONCE before the player loop in each function, so re-indexing `ticketsOwedPacked[rk][player]` on every access recomputes the outer mapping slot each time; hoisting `mapping(address => uint40) storage owedMap = ticketsOwedPacked[rk];` (confirm the exact value type against source) collapses that to one slot computation per access on the inner key only. Behavior-identical, gas-only.

Purpose: a real, no-cost gas win on the mint hot path (also helps normal players).
Output: `contracts/modules/DegenerusGameMintModule.sol` with the hoisted pointer in both functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-SPEC.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md

<interfaces>
<!-- Attested @ 0cc5d10f (329-ATTEST-ROUTER-ADVANCE.md §C.3 C6/C7). Re-grep at edit time. -->

processFutureTicketBatch (`:393`): `rk` computed once at `:398` (`uint24 rk = inFarFuture ? _tqFarFutureKey(lvl) : _tqReadKey(lvl);`). The `ticketsOwedPacked[rk][player]` accesses in THIS function's player loop: `:431` (read), `:441`/`:453`/`:463`/`:498` (writes). These are the hoist targets.

processTicketBatch (`:670`): `rk` computed once at `:671` (`uint24 rk = _tqReadKey(lvl);`). The `ticketsOwedPacked[rk][player]` accesses: `:741`/`:748`/`:754` (writes), `:769` (read), `:822` (write). These are the hoist targets.

OUT OF SCOPE: the `ticketsOwedPacked[ffk][player]` accesses at `:978`/`:989`/`:991` (a DIFFERENT key `ffk`, far-future, inside a separate helper). GASOPT-01's named scope is `[rk]` in the two named functions only — do NOT touch `[ffk]`.

The storage value type of `ticketsOwedPacked` is `mapping(... => mapping(address => uint40))` (the inner value is `uint40 packed`). Confirm the exact declared mapping shape in source before declaring the hoisted pointer type — declare the hoisted pointer to match it exactly (`mapping(address => uint40) storage owedMap = ticketsOwedPacked[rk];`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Hoist ticketsOwedPacked[rk] in processFutureTicketBatch + processTicketBatch (GASOPT-01)</name>
  <read_first>
    - contracts/modules/DegenerusGameMintModule.sol (the file being modified — read both functions in full: processFutureTicketBatch :393-510 and processTicketBatch :670-830, plus the ticketsOwedPacked storage declaration to confirm the exact mapping value type)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md (§C.3 C6/C7 — the hoist sites and the loop-invariant proof)
    - Re-grep: `git show 0cc5d10f:contracts/modules/DegenerusGameMintModule.sol | grep -n "ticketsOwedPacked"`
  </read_first>
  <action>
    In `processFutureTicketBatch`, immediately after `rk` is assigned (`:398`), declare `mapping(address => uint40) storage owedMap = ticketsOwedPacked[rk];` (match the exact value type from the source declaration). Replace every `ticketsOwedPacked[rk][player]` access within this function's player loop (`:431` read, `:441`/`:453`/`:463`/`:498` writes) with `owedMap[player]`. Do NOT touch the `[ffk][player]` accesses (different key).

    In `processTicketBatch`, immediately after `rk` is assigned (`:671`), declare the same hoisted pointer `mapping(address => uint40) storage owedMap = ticketsOwedPacked[rk];`. Replace every `ticketsOwedPacked[rk][player]` access (`:741`/`:748`/`:754`/`:822` writes, `:769` read) with `owedMap[player]`.

    The change is purely a storage-pointer alias — the read/written values and control flow are unchanged. Verify `rk` is NOT reassigned anywhere between the hoist point and the last use in each function (it is loop-invariant per the attestation); if it is, do not hoist that function and flag it.
  </action>
  <verify>
    <automated>grep -c "= ticketsOwedPacked\[rk\];" contracts/modules/DegenerusGameMintModule.sol | grep -qx 2 && grep -c "ticketsOwedPacked\[rk\]\[player\]" contracts/modules/DegenerusGameMintModule.sol | grep -qx 0 && echo PASS</automated>
  </verify>
  <done>Both functions declare `storage owedMap = ticketsOwedPacked[rk];` after the `rk` assignment; zero `ticketsOwedPacked[rk][player]` accesses remain (all routed through `owedMap[player]`); the `[ffk]` accesses are untouched; behavior byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mint path → ticketsOwedPacked storage | the per-player owed-ticket ledger; gas-only refactor, no trust edge crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-330-05-01 | Tampering | the hoisted storage pointer | mitigate | A `storage` reference is an alias to the same slot — reads/writes are identical. The verify grep proves zero `[rk][player]` accesses remain (all hoisted) so no half-converted access can read a stale slot. Same-results proven empirically at TST-03 (Phase 332). |
| T-330-05-02 | Tampering | `rk` reassigned between hoist and use | mitigate | The action requires confirming `rk` is loop-invariant (attested C6/C7) before hoisting; if it is reassigned the function is left untouched and flagged. |
| T-330-05-SC | Tampering | npm/forge installs | mitigate | No package installs (source edit only). |
</threat_model>

<verification>
- Behavior-identical: the same packed values are read/written (proven same-results at TST-03, Phase 332).
- Compiles as part of the BATCH-02 `forge build` (330-07).
</verification>

<success_criteria>
- GASOPT-01: both `processTicketBatch` and `processFutureTicketBatch` hoist the loop-invariant `ticketsOwedPacked[rk]` storage pointer; behavior-identical, gas-only.
</success_criteria>

<output>
Create `.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-05-SUMMARY.md` when done.
</output>
