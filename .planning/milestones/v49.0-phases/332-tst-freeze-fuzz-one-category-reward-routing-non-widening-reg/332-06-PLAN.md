---
phase: 332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg
plan: 06
type: execute
wave: 3
depends_on: ["332-01", "332-02", "332-03", "332-04", "332-05"]
files_modified:
  - test/REGRESSION-BASELINE-v49.md
autonomous: true
requirements: [TST-04]
must_haves:
  truths:
    - "test/REGRESSION-BASELINE-v49.md exists, mirroring the v48 ledger §1-§6 shape"
    - "The binding headline is recorded: the forge failing set == the 42 v48-baseline reds by NAME (net-zero new regression), NOT a bare count"
    - "The 42-red carried-forward union is recorded by NAME (verbatim from v48 §2 buckets A/B/C — unchanged)"
    - "The 17 deletions are recorded with per-test re-homing justification, classified reward-shape vs oracle-migration"
    - "The 5 Crank*->Keeper* renames are recorded so file-path churn is attributable"
    - "The new green proof files (TST-01/02/03/05 + the two 331-added green files) are recorded under 'new green proof files'"
  artifacts:
    - path: "test/REGRESSION-BASELINE-v49.md"
      provides: "the v49 NON-WIDENING regression ledger (42-union by name + 17 deletions + renames + new green + net-zero proof)"
      contains: "the 42 v48-baseline reds by NAME"
  key_links:
    - from: "test/REGRESSION-BASELINE-v49.md §2"
      to: "test/REGRESSION-BASELINE-v48.md §2 (42-name union)"
      via: "carry forward the bucket A/B/C names verbatim, unchanged"
      pattern: "42 v48 union by NAME"
    - from: "test/REGRESSION-BASELINE-v49.md §6"
      to: "forge test --json whole-tree run at the TST HEAD"
      via: "per-suite last-touching-commit membership table proving net-zero"
      pattern: "last-touching commit per failing suite"
---

<objective>
Execute TST-04 part B: author `test/REGRESSION-BASELINE-v49.md`, the plain-markdown NON-WIDENING
regression ledger (NOT a `.sol` test), mirroring `test/REGRESSION-BASELINE-v48.md`. It records the
authoritative full-tree `forge test` run AT THE TST HEAD (after all v49 proofs + the deletions +
renames have landed), the 42-red carried-forward union BY NAME, the 17 deletions with per-test
re-homing justification, the 5 Crank->Keeper renames, the new green proof files, and the net-zero
proof. The binding invariant is "the forge failing set == the 42 v48 reds by NAME" — NOT a bare
count. This restores a clean v49.0 regression baseline against the GAS-calibrated constants.

Purpose: the ledger is the authoritative NON-WIDENING gate the Phase-333 TERMINAL delta-audit
consumes. It must record names, not counts.
Output: `test/REGRESSION-BASELINE-v49.md`.

POSTURE (binding): `test/` + `.planning/` ONLY. ZERO `contracts/*.sol` (mainnet) mutation — FROZEN
subject. The ledger lives in `test/` (NOT gitignored), commits normally. Use the Write tool, never
heredoc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg/332-RESEARCH.md
@.planning/phases/332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg/332-PATTERNS.md
@test/REGRESSION-BASELINE-v48.md
@.planning/phases/332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg/332-01-SUMMARY.md
@.planning/phases/332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg/332-02-SUMMARY.md
@.planning/phases/332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg/332-03-SUMMARY.md
@.planning/phases/332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg/332-04-SUMMARY.md
@.planning/phases/332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg/332-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the authoritative whole-tree forge test + reconcile the net-zero red-set</name>

  <files>test/REGRESSION-BASELINE-v49.md</files>
  <read_first>
    - test/REGRESSION-BASELINE-v48.md (§1 arithmetic table, §2 the 42-name buckets A/B/C, §4 the
      net-zero proof + last-touching-commit membership table, §5 scope attestation — the exact
      template)
    - .planning/phases/332-.../332-RESEARCH.md (the TST-04 live-gate command block; the two NEW green
      331 files `RouterWorstCaseGas.t.sol` + `KeeperBatchAffiliateDeltaAudit.t.sol`; Pitfall 1
      gate-on-the-42-name-union)
    - .planning/phases/332-.../332-05-SUMMARY.md (the deletion + rename outcome — the post-delete
      failing set)
    - the 332-01/02/03/04 SUMMARYs (the NEW green proof files + their passing test counts)
  </read_first>
  <action>
    Run `forge test --json > /tmp/forge332-final.json` on the WHOLE tree (NOT `--match-path`). Parse
    the failing set and CONFIRM it equals exactly the 42 v48-baseline reds by NAME (the §2
    enumeration). If any failing name is NOT in the 42 union, STOP and emit `## STOP — NEW REGRESSION
    OUTSIDE BASELINE` naming the offending test (do NOT write a passing ledger over a real
    regression). Record the actual passed/failed/skipped totals. Compute the arithmetic:
    `passed_at_tst_head − (the deleted-greens, if any) + N fresh green proofs` and
    `failed == 42 by name`. Capture the per-suite last-touching-commit (`git log -1 --format=%h <file>`
    per failing suite) for the §6 membership table. Capture the actual passing-test counts of the new
    green files (TST-01 added functions in RngLockDeterminism, the TST-02/03/05 new files, and the
    two 331 green files `RouterWorstCaseGas.t.sol` + `KeeperBatchAffiliateDeltaAudit.t.sol` —
    re-count via `grep -c "function test" <file>` / the JSON, do not assume 13/3).
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('/tmp/forge332-final.json')); f=sorted(set(t.split('(')[0] for s in d.values() for t,r in s.get('test_results',{}).items() if r.get('status')=='Failure')); print('failing count:', len(f)); print('exactly 42:', len(f)==42); [print(' ',x) for x in f]"</automated>
  </verify>
  <done>The whole-tree run is captured; the failing set is exactly the 42 v48 union by NAME (or a
  STOP block is emitted for a genuine new regression); the new-green counts and the per-suite
  last-touching commits are recorded for the ledger.</done>
</task>

<task type="auto">
  <name>Task 2: Author test/REGRESSION-BASELINE-v49.md mirroring the v48 ledger</name>

  <files>test/REGRESSION-BASELINE-v49.md</files>
  <read_first>
    - test/REGRESSION-BASELINE-v48.md (the full §1-§5 structure to mirror — copy the bucket A/B/C
      names verbatim into the v49 §2)
    - .planning/phases/332-.../332-PATTERNS.md (the "test/REGRESSION-BASELINE-v49.md — NEW markdown
      ledger" section, lines 197-207, listing §1-§6)
    - .planning/phases/332-.../332-RESEARCH.md (the 17-red enumeration table with provenance commits
      `3afbf676`/`795e679d`/`dfba3ac1`/`47b9d031`/`b9bc5206`; the reward-shape vs oracle-migration
      classification)
    - the §1-task captured run totals + new-green counts + membership commits
  </read_first>
  <action>
    Write `test/REGRESSION-BASELINE-v49.md` (use the Write tool, NOT heredoc) mirroring the v48 ledger:
    §1 the arithmetic table (`passed at TST HEAD − deleted + N fresh green`; `failed == 42` = the v48
    union) using the ACTUAL captured totals; §2 the AUTHORITATIVE 42-red expected union BY NAME (carry
    forward the v48 §2 buckets A/B/C verbatim — UNCHANGED); §3 (NEW vs v48) the 17 deletions with the
    per-test re-homing justification from the RESEARCH/PATTERNS table, classified reward-shape
    (premise retired) vs oracle-migration (RD-2 guard-drop / no-double-buy), with the v46 provenance
    commit per row; §4 (NEW vs v48) the 5 `Crank*`->`Keeper*` renames (so file-path churn is
    attributable — NON-WIDENING is about the red-set/behavior, not file names); §5 the new green proof
    files (TST-01 RngLockDeterminism router functions + TST-02 KeeperRouterOneCategory + TST-03
    KeeperRewardRoutingSameResults + TST-05 DegeneretteResolveRepeg + the two 331-added green files
    RouterWorstCaseGas / KeeperBatchAffiliateDeltaAudit) with their ACTUAL passing counts; §6 (mirror
    v48 §4) the net-zero PROOF + the per-suite last-touching-commit membership table proving the 42
    reds predate v49 + the false-confidence guards (gate on NAME-set not count; the 17 deletions and
    the renames are attributable; the WHOLE tree was run not `--match-path`). Record the binding
    headline prose: "the forge failing set == the 42 v48-baseline reds by NAME — net-zero new
    regression". State the subject is FROZEN at the committed v49 source (`63bc16ca` + the 331 GAS
    constants) with zero `contracts/*.sol` edits this phase. Note the Hardhat Degenerette stat
    secondary gate result recorded at 332-04 (precedent-locked v48 parity).
  </action>
  <verify>
    <automated>test -f test/REGRESSION-BASELINE-v49.md && grep -c "Bucket A\|Bucket B\|Bucket C" test/REGRESSION-BASELINE-v49.md && grep -ic "42 v48\|42 .*baseline\|net-zero" test/REGRESSION-BASELINE-v49.md</automated>
  </verify>
  <done>`test/REGRESSION-BASELINE-v49.md` exists with §1-§6; §2 carries the 42 v48 names verbatim; §3
  records the 17 deletions with re-homing + provenance; §4 records the 5 renames; §5 records the new
  green files with actual counts; §6 has the net-zero proof + membership table; the binding "==42 by
  NAME" headline is present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the ledger | the authoritative NON-WIDENING gate the TERMINAL delta-audit consumes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-332-06-COUNT | Tampering (audit integrity) | the non-widening headline | mitigate | the ledger's binding invariant is "failing set == the 42 v48 names by NAME", not a bare count — §2 enumerates the union by name and §6 proves every red predates v49 via the last-touching-commit table; a count-only gate would mask a real new regression |
| T-332-06-FALSE | Repudiation | a passing ledger over a real regression | mitigate | the §1 task STOPS with `## STOP — NEW REGRESSION OUTSIDE BASELINE` if any failing name is outside the 42 union — the ledger is never written green over a genuine regression |
| T-332-06-ATTR | Information Disclosure | unattributable churn | mitigate | §3 (17 deletions + provenance commits) + §4 (5 renames) make every test-tree diff vs v48 attributable; the WHOLE tree was run (not `--match-path`, the v48 §4 FC4 guard) |
| T-332-06-SC | Tampering | test toolchain | accept | no package installs (doc-authoring + a forge run only) |
</threat_model>

<verification>
- `test/REGRESSION-BASELINE-v49.md` exists with §1-§6 mirroring the v48 ledger.
- §2 carries the 42 v48 reds by NAME verbatim; the headline gate is by NAME, not count.
- §3/§4 make the 17 deletions + 5 renames attributable; §5 lists the new green files; §6 proves
  net-zero via the per-suite last-touching-commit membership table.
- Zero `contracts/*.sol` modifications: `git diff --name-only contracts/` returns nothing.
</verification>

<success_criteria>
TST-04 complete: the v49 NON-WIDENING regression ledger is authored — the forge failing set equals
the 42 v48-baseline reds by name (net-zero new regression), with the 17 deletions, 5 renames, and new
green proof files all recorded and attributable.
</success_criteria>

<output>
Create `.planning/phases/332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg/332-06-SUMMARY.md` when done
</output>
