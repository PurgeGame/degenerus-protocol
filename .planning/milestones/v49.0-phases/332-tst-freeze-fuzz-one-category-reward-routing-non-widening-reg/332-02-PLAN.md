---
phase: 332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fuzz/KeeperRouterOneCategory.t.sol
autonomous: true
requirements: [TST-02]
must_haves:
  truths:
    - "Exactly ONE COINFLIP.creditFlip fires per doWork() tx across all three category branches (buy / advance / open) — no bounty-stacking"
    - "The bountyEarned==0 SKIP path runs the category but credits ZERO (no creditFlip) and does NOT revert"
    - "doWork with all 3 O(1) predicates empty reverts NoWork() and credits nothing"
    - "Reentrancy double-pay is structurally impossible — proven by grep-attestation (no untrusted external call in any leg + single creditFlip CEI-last), NO attacker harness"
    - "Parameterless doWork() does its fixed per-leg default batch (BUY_BATCH=50 / OPEN_BATCH=100) and does NOT OOG; a larger backlog leaves a remainder for the next call"
    - "Standalone autoBuy(count)/autoOpen(count) escapes run the leg and credit NOTHING (unrewarded)"
  artifacts:
    - path: "test/fuzz/KeeperRouterOneCategory.t.sol"
      provides: "one-category/no-stacking creditFlip-count proofs + structural reentrancy grep-attest + default-batch/remainder + unrewarded escapes"
      contains: "_countCoinflipStakeUpdatedFor"
  key_links:
    - from: "test/fuzz/KeeperRouterOneCategory.t.sol"
      to: "contracts/AfKing.sol::doWork (:883-919) single creditFlip CEI-last (:916-918)"
      via: "vm.recordLogs(); afKing.doWork(); assertEq(count, 1)"
      pattern: "creditFlip|doWork"
    - from: "test/fuzz/KeeperRouterOneCategory.t.sol (grep-attest)"
      to: "contracts/AfKing.sol source (comment-stripped)"
      via: "vm.readFile + _stripComments + _countOccurrences"
      pattern: "creditFlip\\(msg.sender, bountyEarned\\)"
---

<objective>
Prove the one-rewarded-category-per-tx invariant (no bounty-stacking) and the router→game→
`creditFlip` double-pay disposition EMPIRICALLY + STRUCTURALLY, plus the parameterless-`doWork()`
default-batch/remainder behavior and the standalone UNREWARDED escapes. Per D-02, no-stacking is
proven by COUNTING `creditFlip` calls (exactly ONE per `doWork()` tx across all branches, ZERO on
the `bountyEarned==0` skip), NOT by exact-amount assertions. Per D-01, reentrancy is a STRUCTURAL
grep-attestation (no untrusted external call in any leg + the single `creditFlip` CEI-last after the
one-category early-return) — NO synthetic attacker harness is built.

Purpose: TST-02 is the second load-bearing SECURITY proof (with TST-01). The one-category
structural early-return is the mitigation for bounty-stacking; the single-CEI-last `creditFlip` is
the mitigation for composed reentrant double-pay. Security is the HARD FLOOR.
Output: a NEW `test/fuzz/KeeperRouterOneCategory.t.sol` proof file.

POSTURE (binding): `test/` + `.planning/` ONLY. ZERO `contracts/*.sol` (mainnet) mutation — FROZEN
subject. Contract `file:line` are READ-ONLY anchors. Surface any contract defect, do not patch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg/332-RESEARCH.md
@.planning/phases/332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg/332-PATTERNS.md

<interfaces>
<!-- READ-ONLY contract anchors (FROZEN subject). -->

contracts/AfKing.sol — the doWork structure the proof keys on:
  :883  function doWork() external
  :885    uint256 unit = (BOUNTY_ETH_TARGET * PRICE_COIN_UNIT) / mp;
  :890    if (_autoBuyDay != _currentDay() || _autoBuyCursor < _subscribers.length) {  // BUY leg
  :891       uint256 bought = _autoBuy(BUY_BATCH);
  :893       if (bought > 0) bountyEarned = (unit * BUY_RATIO_NUM) / BUY_RATIO_DEN;     // flat, NOT count-scaled
  :896    } else if (...advanceDue()) { uint8 mult = ...advanceGame(); if (mult>0) bountyEarned = unit*ADVANCE_RATIO_NUM*mult; }
  :902    } else if (...boxesPending()) { uint256 opened = ...autoOpen(OPEN_BATCH); ... bountyEarned = (unit*k)/OPEN_KNEE; }
  :910    } else { revert NoWork(); }
  :916    if (bountyEarned > 0) { ICoinflip(COINFLIP).creditFlip(msg.sender, bountyEarned); }  // SINGLE creditFlip CEI-last
  :850  uint256 internal constant BUY_BATCH = 50;
  :856  uint256 internal constant OPEN_BATCH = 100;
  :869  uint256 internal constant OPEN_KNEE = 5;
  :923  function autoBuy(uint256 count) external   // standalone UNREWARDED escape
  :929  function autoOpen(uint256 count) external  // standalone UNREWARDED escape

reuse from test/gas/CrankLeversAndPacking.t.sol (copy verbatim into the new file or via a shared base):
  :75   COINFLIP_STAKE_UPDATED_SIG = keccak256("CoinflipStakeUpdated(address,uint32,uint256,uint256)")
  :523  function _countCoinflipStakeUpdated() internal returns (uint256)
  :538  function _countCoinflipStakeUpdatedFor(address who) internal returns (uint256)   // recipient-isolated
  :587  function _stripComments(string memory src) private pure
  :555  function _countOccurrences(string memory haystack, string memory needle)
  :92   AFKING_SRC = "contracts/AfKing.sol"
  :257  _countOccurrences(afking, "creditFlip(msg.sender, bountyEarned)")  // the existing single-CEI grep gate

reuse from test/fuzz/AfKingConcurrency.t.sol:
  :100  _setupHealthyBuyingSubs(N, prefix)
  :103  afKing.autoBuyProgress()   // cursor reads for default-batch/remainder
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One-category creditFlip-count oracle + bountyEarned==0 skip + NoWork()</name>

  <files>test/fuzz/KeeperRouterOneCategory.t.sol</files>
  <read_first>
    - test/gas/CrankLeversAndPacking.t.sol (the count-oracle: `COINFLIP_STAKE_UPDATED_SIG` :75,
      `_countCoinflipStakeUpdated` :523, `_countCoinflipStakeUpdatedFor` :538, and the usage idiom
      `vm.recordLogs(); vm.prank(keeper); afKing.doWork(); assertEq(_countCoinflipStakeUpdatedFor(keeper), 1, ...)`
      :184-195 — copy the call shape)
    - test/fuzz/AfKingConcurrency.t.sol (`_setupHealthyBuyingSubs` :100, the DeployProtocol base)
    - test/fuzz/helpers/DeployProtocol.sol (the `game`/`coinflip`/`afKing` members + MockVRF drain)
    - contracts/AfKing.sol:883-919 (the else-if chain + the `bountyEarned>0` block) — READ-ONLY
    - .planning/phases/332-.../332-PATTERNS.md (the TST-02 section + the creditFlip-count oracle
      shared-pattern block)
  </read_first>
  <behavior>
    - Test (BUY branch): drive a fresh-day backlog so the buy leg runs; `vm.recordLogs(); doWork();`
      → `_countCoinflipStakeUpdatedFor(keeper) == 1`.
    - Test (ADVANCE branch): drive a buys-empty + advance-due state; `doWork()` →
      `_countCoinflipStakeUpdatedFor(keeper) == 1` (mult>0).
    - Test (OPEN branch): drive buys-empty + advance-not-due + boxesPending; `doWork()` →
      `_countCoinflipStakeUpdatedFor(keeper) == 1`.
    - Test (bountyEarned==0 SKIP): drive a buy chunk that walks only already-bought subs (bought==0)
      → the buy category runs, `_countCoinflipStakeUpdatedFor(keeper) == 0`, and `doWork()` does NOT
      revert.
    - Test (NoWork): all three O(1) predicates empty → `doWork()` reverts `NoWork()`,
      `_countCoinflipStakeUpdated() == 0`.
  </behavior>
  <action>
    Create `test/fuzz/KeeperRouterOneCategory.t.sol` as `contract KeeperRouterOneCategory is
    DeployProtocol`. Port the `COINFLIP_STAKE_UPDATED_SIG` constant + `_countCoinflipStakeUpdated`/
    `_countCoinflipStakeUpdatedFor` log-count helpers from `CrankLeversAndPacking.t.sol` (copy the
    bodies; they are not in a shared base). Author the five D-02 count assertions above
    (`testBuyBranchCreditsExactlyOnce`, `testAdvanceBranchCreditsExactlyOnce`,
    `testOpenBranchCreditsExactlyOnce`, `testBountyEarnedZeroSkipCreditsNothing`,
    `testNoWorkRevertsAndCreditsNothing`). Use `_countCoinflipStakeUpdatedFor(keeper)` (recipient-
    isolated, topic[1] == keeper) to separate the router credit from a box-owner's winnings credit
    (`LootboxModule:1036`). Assert COUNT (==1 / ==0), NEVER exact amounts and NEVER the retired
    per-item *summed* reward. The proof that the else-if chain cannot credit two categories in one tx
    IS the "exactly one across all three branches + zero on skip" set.
  </action>
  <verify>
    <automated>forge test --match-contract KeeperRouterOneCategory --match-test "Branch|Skip|NoWork" 2>&1 | tail -20</automated>
  </verify>
  <done>All five count assertions pass; each branch credits exactly one, the skip path zero, NoWork
  zero — no exact-amount or summed-reward assertion appears in the file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Structural reentrancy grep-attestation + default-batch/remainder + unrewarded escapes</name>

  <files>test/fuzz/KeeperRouterOneCategory.t.sol</files>
  <read_first>
    - test/gas/CrankLeversAndPacking.t.sol (`_stripComments` :587, `_countOccurrences` :555,
      `vm.readFile(AFKING_SRC)` :221, the existing single-CEI grep gate
      `_countOccurrences(afking, "creditFlip(msg.sender, bountyEarned)")` :257)
    - foundry.toml (`fs_permissions = [{access="read", path="./contracts"}]` — enables vm.readFile)
    - contracts/AfKing.sol:883-919 (doWork legs), :561 (`_autoBuy`), :923/:929 (escapes) — READ-ONLY
    - test/fuzz/AfKingConcurrency.t.sol (`_setupHealthyBuyingSubs` :100, `afKing.autoBuyProgress()`
      :103 cursor reads)
    - .planning/phases/332-.../332-CONTEXT.md (D-01 structural reentrancy: NO attacker harness; D-03
      default-batch/escapes = planner territory)
  </read_first>
  <behavior>
    - Test (structural reentrancy attest, grep): comment-strip `contracts/AfKing.sol`; assert (a)
      exactly ONE `creditFlip(msg.sender, bountyEarned)` occurrence in `doWork` (the existing
      `:257`-style gate), and (b) the legs (`_autoBuy` / the `advanceGame` call / the `autoOpen`
      call) target only pinned `ContractAddresses.GAME` / `ContractAddresses.COINFLIP` — no untrusted
      address literal. NO attacker harness, NO reentrant mock.
    - Test (default-batch/remainder, no-OOG): with a backlog LARGER than `BUY_BATCH` (>50 subs), one
      `doWork()` processes a fixed default batch and does NOT OOG; `autoBuyProgress()` shows a
      remainder (cursor < length); a second `doWork()` advances further — proving the parameterless
      `doWork()` chunks (D-07).
    - Test (unrewarded escapes): standalone `autoBuy(count)` and `autoOpen(count)` run the leg
      (state changes — e.g. subs bought / boxes opened) and credit NOTHING
      (`_countCoinflipStakeUpdated() == 0`).
  </behavior>
  <action>
    Add to `test/fuzz/KeeperRouterOneCategory.t.sol`: (1)
    `testDoWorkReentrancyStructurallySafeSourceAttest()` — port `_stripComments`/`_countOccurrences`
    + `AFKING_SRC`, assert the single `creditFlip(msg.sender, bountyEarned)` occurrence (==1) and
    that no untrusted-address external call appears in the legs (grep for the pinned
    `ContractAddresses.GAME`/`.COINFLIP` targets being the only external call sites; assert ZERO
    occurrences of a `.call`/`.transfer`/`.send` to a non-pinned address in `doWork`/`_autoBuy`).
    (2) `testParameterlessDoWorkDefaultBatchLeavesRemainder()` — set up >`BUY_BATCH` healthy subs via
    `_setupHealthyBuyingSubs`, fire one `doWork()`, assert no OOG (it returns) and a remainder via
    `autoBuyProgress()`, then a second `doWork()` advances. (3)
    `testStandaloneAutoBuyEscapeUnrewarded()` + `testStandaloneAutoOpenEscapeUnrewarded()` — call the
    parametered escapes directly, assert work happened but `_countCoinflipStakeUpdated()==0`. Do NOT
    build a synthetic reentrant attacker (D-01 forbids it).
  </action>
  <verify>
    <automated>forge test --match-contract KeeperRouterOneCategory 2>&1 | tail -25</automated>
  </verify>
  <done>The grep-attest passes (single CEI-last creditFlip + no untrusted external call); the
  default-batch test proves no-OOG + remainder; both escapes run the leg and credit zero. No attacker
  harness exists in the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keeper → AfKing.doWork | permissionless caller; the bounty payee is `msg.sender` |
| AfKing → COINFLIP.creditFlip | the only money edge — mints FLIP CREDIT, NO ETH push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-332-02-STACK | Elevation of Privilege (reward) | doWork else-if chain | mitigate | one-category STRUCTURAL early-return proven by `creditFlip`-COUNT == 1 across all 3 branches + 0 on skip/NoWork (D-02); never exact-amount, so the proof can't pass by coincidence |
| T-332-02-REENT | Tampering | router→game→creditFlip | mitigate | STRUCTURAL grep-attest (D-01): single `creditFlip(msg.sender, bountyEarned)` CEI-last + no untrusted external call in any leg → no reentrancy hook. NO attacker harness (would be a false-soundness theatre — there is no untrusted call to re-enter through) |
| T-332-02-OOG | Denial of Service | parameterless doWork batch | mitigate | proven: fixed default batch (BUY_BATCH=50/OPEN_BATCH=100) does NOT OOG; backlog leaves a remainder for the next call (no unbounded loop) |
| T-332-02-VAC | Tampering | the count oracle | mitigate | recipient-isolated `_countCoinflipStakeUpdatedFor(keeper)` separates the router bounty from a box-owner winnings credit — the count can't be inflated/masked by an unrelated credit |
| T-332-02-SC | Tampering | test toolchain | accept | no package installs (test-authoring only) |
</threat_model>

<verification>
- `forge test --match-contract KeeperRouterOneCategory` green.
- The structural reentrancy attest is a grep over COMMENT-STRIPPED source (NatSpec cannot
  self-satisfy the gate); the single `creditFlip(msg.sender, bountyEarned)` count == 1.
- No synthetic reentrant attacker / reentrant mock exists in the file (D-01).
- Zero `contracts/*.sol` modifications: `git diff --name-only contracts/` returns nothing.
</verification>

<success_criteria>
TST-02 proven: exactly one bounty credit per `doWork()` tx (no stacking), structural reentrancy
safety attested without an attacker harness, parameterless `doWork()` chunks without OOG, and the
standalone escapes are unrewarded.
</success_criteria>

<output>
Create `.planning/phases/332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg/332-02-SUMMARY.md` when done
</output>
