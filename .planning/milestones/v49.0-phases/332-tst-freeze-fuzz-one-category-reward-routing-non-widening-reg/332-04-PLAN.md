---
phase: 332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fuzz/DegeneretteResolveRepeg.t.sol
autonomous: true
requirements: [TST-05]
must_haves:
  truths:
    - "degeneretteResolve pays exactly ONE flat ~1 BURNIE (RESOLVE_FLAT_BURNIE=1e18) per tx — NOT a per-item sum — when ≥3 non-WWXRP bets resolve"
    - "1-2 non-WWXRP resolved → committed UNPAID, no revert (the tail is never stranded)"
    - "0 resolved → revert NoWork()"
    - "WWXRP (currency==3) is excluded from BOTH the ≥3 gate count AND the reward: 3 WWXRP-only → resolved, UNPAID, no revert"
    - "The per-item resolution RESULTS (BURNIE/WWXRP/DGNRS mints, claimable/pool deltas, RNG draws) are byte-identical regardless of whether the ≥3 reward fired — the rename touched only names + the bounty wrapper"
    - "The Hardhat Degenerette stat tests stay GREEN (chi²/EV-exactness unchanged) after the rename"
  artifacts:
    - path: "test/fuzz/DegeneretteResolveRepeg.t.sol"
      provides: "flat ~1 BURNIE / ≥3-gate / NoWork / WWXRP-excluded / RESULTS-equality proofs for degeneretteResolve"
      contains: "RESOLVE_FLAT_BURNIE"
  key_links:
    - from: "test/fuzz/DegeneretteResolveRepeg.t.sol"
      to: "contracts/DegenerusGame.sol::degeneretteResolve (:1595-1631), successCount/≥3 gate (:1619/:1630), NoWork (:1629)"
      via: "_countCoinflipStakeUpdatedFor(keeper) ∈ {0,1} per case"
      pattern: "degeneretteResolve|RESOLVE_FLAT_BURNIE|successCount"
    - from: "test/fuzz/DegeneretteResolveRepeg.t.sol (RESULTS-equality)"
      to: "contracts/DegenerusGame.sol::_degeneretteResolveBet → delegatecall resolveBets (:1741)"
      via: "capture BURNIE/WWXRP/claimable/pool deltas, assert value-invariant to the bounty wrapper"
      pattern: "_degeneretteResolveBet|resolveBets"
---

<objective>
Prove the `autoResolve`→`degeneretteResolve` rename + flat ~1-BURNIE "lose" re-peg (GAS-06 / TST-05):
the bounty is a flat literal ~1 BURNIE (`RESOLVE_FLAT_BURNIE = 1e18`) per tx — NOT per-item — gated
at ≥3 successfully-resolved NON-WWXRP bets; `revert NoWork()` on zero resolved; 1-2 resolved →
committed UNPAID (no revert, the tail is never stranded); WWXRP (currency==3) excluded from BOTH the
gate count AND the reward; and the per-item resolution RESULTS are byte-identical vs the per-item
path (rename + bounty-shape change only, no payout/RNG change). Per Open Question 1, RESULTS-equality
is proven by VALUE-INVARIANCE: the per-item math is produced by the unchanged
`_degeneretteResolveBet → delegatecall GAME_DEGENERETTE_MODULE.resolveBets`, so RESULTS are
independent of the bounty wrapper — no resurrection of the deleted `autoResolve` source.

Purpose: TST-05 closes the SC5 rename/re-peg bar. The re-peg must change ONLY the bounty shape, never
the resolution payouts. The Hardhat Degenerette stat tree (chi²/EV) re-confirms distribution
invariance as the secondary gate.
Output: a NEW `test/fuzz/DegeneretteResolveRepeg.t.sol` Foundry proof + a Hardhat stat re-confirm.

POSTURE (binding): `test/` + `.planning/` ONLY. ZERO `contracts/*.sol` (mainnet) mutation — FROZEN
subject. Contract `file:line` are READ-ONLY anchors. Surface any contract defect, do not patch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg/332-RESEARCH.md
@.planning/phases/332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg/332-PATTERNS.md

<interfaces>
<!-- READ-ONLY contract anchors (FROZEN subject). -->

contracts/DegenerusGame.sol — the degeneretteResolve structure:
  :1544  uint256 private constant RESOLVE_FLAT_BURNIE = 1e18;
  :1595  function degeneretteResolve(address[] calldata players, uint64[] calldata betIds) ...
  :1600  if (len == 0 || betIds.length != len) revert E();              // input-validate
  :1604  if (degeneretteBets[players[0]][betIds[0]] == 0) revert BatchAlreadyTaken(); // AUTO-02 probe
  :1606  uint256 successCount;
  :1612  uint8 currency = uint8((betPacked >> 42) & 0x3);               // currency bits (WWXRP == 3)
  :1614  try this._degeneretteResolveBet(players[i], betIds[i]) {
  :1618-1619  ++totalResolved; if (currency != 3) ++successCount;       // WWXRP excluded from the count
  :1629  if (totalResolved == 0) revert NoWork();                       // revert-on-no-work (any currency)
  :1630  if (successCount >= 3) coinflip.creditFlip(msg.sender, RESOLVE_FLAT_BURNIE); // flat ≥3 gate
  :1741  function _degeneretteResolveBet(address player, uint64 betId) external  // self-call gated; delegatecall resolveBets

reuse: DegeneretteFreezeResolution capture/replay (`_replayPerSpinBaseline` :406, delta asserts
:411-423, claimablePool slot-1-byte-16 read :63); DegeneretteHeroScore placement helpers;
CrankLeversAndPacking `_countCoinflipStakeUpdatedFor` (:538, the flat-ONE count oracle).

Hardhat stat secondary gate (re-confirm GREEN, NO edit):
  test/stat/DegenerettePerNEvExactness.test.js / DegeneretteBonusEv.test.js / DegeneretteProducerChi2.test.js
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The 5 re-peg / gate / WWXRP-exclusion cases (flat ONE credit, never the sum)</name>

  <files>test/fuzz/DegeneretteResolveRepeg.t.sol</files>
  <read_first>
    - contracts/DegenerusGame.sol:1595-1631 (the degeneretteResolve structure: input-validate :1600,
      AUTO-02 probe :1604, currency bits :1612, per-item try :1614, successCount excludes WWXRP
      :1618-1619, NoWork :1629, flat ≥3 gate :1630), :1544 (`RESOLVE_FLAT_BURNIE = 1e18`) — READ-ONLY
    - test/fuzz/DegeneretteFreezeResolution.t.sol (the resolve/capture scaffold + placement helpers)
    - test/fuzz/DegeneretteHeroScore.t.sol (Degenerette bet placement helpers)
    - test/gas/CrankLeversAndPacking.t.sol (`_countCoinflipStakeUpdatedFor` :538 — count oracle; port it)
    - .planning/phases/332-.../332-RESEARCH.md (Pitfall 6: WWXRP excluded from BOTH gate + reward;
      the 5 cases a-e)
    - .planning/phases/332-.../332-PATTERNS.md (the TST-05 re-peg/gate assertions block, lines 129-140)
  </read_first>
  <behavior>
    - Case (a) ≥3 non-WWXRP resolved → exactly ONE `RESOLVE_FLAT_BURNIE` creditFlip
      (`_countCoinflipStakeUpdatedFor(keeper) == 1`), and the credited amount == 1e18 (the FLAT
      literal, NOT a per-item sum).
    - Case (b) 1-2 non-WWXRP resolved → committed (totalResolved > 0) but UNPAID
      (`_countCoinflipStakeUpdatedFor(keeper) == 0`), NO revert — the trailing tail is not stranded.
    - Case (c) 0 resolved → `degeneretteResolve` reverts `NoWork()`.
    - Case (d) 3 WWXRP-only resolved → `totalResolved == 3` (no revert), `successCount == 0` → UNPAID
      (zero creditFlip).
    - Case (e) mixed 2 WWXRP + 3 non-WWXRP → PAID (3 non-WWXRP ≥ gate → exactly one flat creditFlip).
  </behavior>
  <action>
    Create `test/fuzz/DegeneretteResolveRepeg.t.sol` as `contract DegeneretteResolveRepeg is
    DeployProtocol`. Port `_countCoinflipStakeUpdatedFor` (+ `COINFLIP_STAKE_UPDATED_SIG`) from
    `CrankLeversAndPacking.t.sol`. Use the `DegeneretteFreezeResolution`/`DegeneretteHeroScore`
    placement helpers to set up bets across currencies (ETH/BURNIE/DGNRS non-WWXRP vs WWXRP
    currency==3). Author the five cases `testGteThreeNonWwxrpPaysExactlyOneFlat` (assert count==1 AND
    credited == 1e18, never `3 * peg`), `testOneOrTwoNonWwxrpCommittedUnpaidNoRevert`,
    `testZeroResolvedRevertsNoWork`, `testThreeWwxrpOnlyResolvedUnpaidNoRevert`, and
    `testMixedWwxrpAndNonWwxrpPaysAtGate`. The reward count is the flat-ONE oracle — NEVER the retired
    per-item *summed* premise (`3 * CRANK_RESOLVE_BET_GAS_UNITS * ...`).
  </action>
  <verify>
    <automated>forge test --match-contract DegeneretteResolveRepeg --match-test "NonWwxrp|NoWork|Wwxrp|Mixed|Flat" 2>&1 | tail -20</automated>
  </verify>
  <done>All five cases pass: ≥3 non-WWXRP → one flat 1e18 credit; 1-2 → unpaid no revert; 0 →
  NoWork(); 3 WWXRP-only → unpaid no revert; mixed → paid at the gate. No summed-reward assertion
  appears.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RESULTS-equality (value-invariant to the bounty wrapper) + Hardhat stat re-confirm</name>

  <files>test/fuzz/DegeneretteResolveRepeg.t.sol</files>
  <read_first>
    - contracts/DegenerusGame.sol:1741 (`_degeneretteResolveBet` self-call gated, delegatecalls the
      unchanged `GAME_DEGENERETTE_MODULE.resolveBets`) — READ-ONLY
    - test/fuzz/DegeneretteFreezeResolution.t.sol:351-433 (the `_replayPerSpinBaseline` capture +
      delta `assertEq` for BURNIE/WWXRP/claimable/claimablePool; claimablePool slot-1-byte-16 read
      :63; non-vacuity `assertGt(expectedBurnie, 0)` :426-428)
    - .planning/phases/332-.../332-RESEARCH.md (Open Question 1: route (b) value-invariance — do NOT
      resurrect deleted autoResolve source; A4 the delegatecall is unchanged)
    - .planning/phases/332-.../332-PATTERNS.md (the TST-05 capture+replay RESULTS-equality block,
      lines 119-127; the Hardhat secondary gate, line 142)
    - test/stat/DegenerettePerNEvExactness.test.js / DegeneretteBonusEv.test.js /
      DegeneretteProducerChi2.test.js (READ to confirm they reference no renamed symbol)
  </read_first>
  <behavior>
    - Test (RESULTS value-invariance, paid path): resolve ≥3 non-WWXRP bets via `degeneretteResolve`;
      capture BURNIE/WWXRP/DGNRS mint deltas + claimable + claimablePool deltas + RNG draws via the
      `_replayPerSpinBaseline` delta-capture; assert they match the per-spin-derived expected sums
      (the resolution math is byte-identical) — with a non-vacuity `assertGt(expectedBurnie, 0)`.
    - Test (RESULTS independent of whether reward fired): resolve a set that does NOT trip the ≥3
      gate (e.g. 2 non-WWXRP) via snapshot→resolve→capture; revert; resolve a set that DOES trip the
      gate (≥3 non-WWXRP) over the same bets; assert the per-bet resolution deltas for the shared
      bets are identical — the bounty wrapper does not touch the resolution math.
    - Test (Hardhat stat secondary gate): the chi²/EV-exactness stat tests stay GREEN after the
      rename (distribution invariance).
  </behavior>
  <action>
    Add to `test/fuzz/DegeneretteResolveRepeg.t.sol`: `testResultsEqualityValueInvariant` (reuse the
    `DegeneretteFreezeResolution._replayPerSpinBaseline` capture+delta `assertEq` for BURNIE/WWXRP/
    DGNRS/claimable/claimablePool; assert the resolution deltas match the per-spin-derived expected
    sums, with the non-vacuity `assertGt`) and `testResolutionDeltasIndependentOfRewardGate` (prove a
    per-bet resolution delta is identical whether or not the ≥3 reward fired, via snapshot/revert).
    Prove value-invariance directly — do NOT check out or mock the deleted `autoResolve` source
    (Open Question 1 route b). Then RUN the Hardhat Degenerette stat secondary gate
    (`npx hardhat test test/stat/DegenerettePerNEvExactness.test.js test/stat/DegeneretteBonusEv.test.js
    test/stat/DegeneretteProducerChi2.test.js`) once to confirm GREEN (A2); record the result in the
    SUMMARY. If a stat test is RED for an unrelated pre-existing reason, record it as the v48-parity
    baseline (do NOT "fix" it). If the rename surfaces a CONTRACT defect, STOP and surface it.
  </action>
  <verify>
    <automated>forge test --match-contract DegeneretteResolveRepeg 2>&1 | tail -25</automated>
    <human-check>Hardhat stat gate run during execution: `npx hardhat test test/stat/DegenerettePerNEvExactness.test.js test/stat/DegeneretteBonusEv.test.js test/stat/DegeneretteProducerChi2.test.js` — record GREEN/parity-baseline in the SUMMARY</human-check>
  </verify>
  <done>The Foundry RESULTS-equality proves the resolution deltas are byte-identical and independent
  of the bounty gate (value-invariant), with a non-vacuity guard; the Hardhat stat gate result is
  recorded (GREEN at v48 parity, or the unrelated pre-existing red documented). No deleted source is
  resurrected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keeper → degeneretteResolve | permissionless caller supplies `(players[], betIds[])`; pays a flat bounty only at ≥3 non-WWXRP |
| degeneretteResolve → _degeneretteResolveBet (self-call) → resolveBets (delegatecall) | the per-item resolution math; unchanged by the rename |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-332-04-SUM | Elevation of Privilege (reward) | re-peg shape | mitigate | proven: exactly ONE flat 1e18 credit at ≥3, NEVER a per-item sum (the retired premise) — asserted as count==1 AND amount==RESOLVE_FLAT_BURNIE |
| T-332-04-WWXRP | Elevation of Privilege (reward) | WWXRP gate exclusion | mitigate | proven: WWXRP (currency==3) excluded from BOTH the ≥3 count AND the reward (cases d/e) — a faucet via WWXRP spam is impossible |
| T-332-04-TAIL | Denial of Service | trailing tail | mitigate | proven: 1-2 resolved → committed UNPAID, no revert (lean = do-not-strand the tail); only 0-resolved reverts NoWork() |
| T-332-04-RESULTS | Tampering (payout) | RESULTS-equality | mitigate | proven by VALUE-INVARIANCE: the resolution deltas are byte-identical and independent of the bounty wrapper (the delegatecall to resolveBets is unchanged) — no payout/RNG change rode the rename |
| T-332-04-VAC | Tampering | the equality proof | mitigate | non-vacuity `assertGt(expectedBurnie, 0)` — the RESULTS-equality cannot pass against an empty/zero baseline |
| T-332-04-SC | Tampering | test toolchain | accept | no package installs (test-authoring only) |
</threat_model>

<verification>
- `forge test --match-contract DegeneretteResolveRepeg` green.
- The reward count oracle asserts a FLAT one credit (amount == 1e18), never a per-item sum.
- WWXRP excluded from both the gate and the reward (cases d/e green).
- RESULTS-equality is value-invariant + non-vacuous; no deleted `autoResolve` source resurrected.
- Hardhat stat gate result recorded in the SUMMARY (GREEN at v48 parity).
- Zero `contracts/*.sol` modifications: `git diff --name-only contracts/` returns nothing.
</verification>

<success_criteria>
TST-05 proven: flat ~1 BURNIE/tx at ≥3 non-WWXRP, revert-on-no-work, 1-2 unpaid-no-strand, WWXRP
excluded from gate+reward, and byte-identical resolution RESULTS (value-invariant to the bounty
wrapper); the Hardhat stat tree stays green.
</success_criteria>

<output>
Create `.planning/phases/332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg/332-04-SUMMARY.md` when done
</output>
