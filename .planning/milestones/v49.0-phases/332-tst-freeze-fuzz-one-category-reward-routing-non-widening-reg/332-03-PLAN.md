---
phase: 332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fuzz/KeeperRewardRoutingSameResults.t.sol
autonomous: true
requirements: [TST-03]
must_haves:
  truths:
    - "Standalone game.advanceGame() pays NO bounty (the 3 in-callee creditFlip removed at ADV-01) — zero creditFlip to the caller"
    - "advanceGame driven via doWork IS rewarded: the new-day stall multiplier (1/2/4/6) is honored and the mid-day partial-drain leg (mult=1) is rewarded"
    - "The gameover path (mult=0) is UNREWARDED via doWork — zero creditFlip"
    - "GASOPT-01 (MintModule owedMap pointer hoist) is same-results — ticket processing byte-identical"
    - "GASOPT-03 (keeperSnapshot batched read) returns the SAME (mintPrice, rngLocked, claimables[]) as N individual claimableWinningsOf calls, and an autoBuy through it produces identical buy outcomes"
  artifacts:
    - path: "test/fuzz/KeeperRewardRoutingSameResults.t.sol"
      provides: "advance unrewarded-standalone vs rewarded-via-doWork (mult honored) + GASOPT-01/03 behavioral-equality"
      contains: "keeperSnapshot"
  key_links:
    - from: "test/fuzz/KeeperRewardRoutingSameResults.t.sol"
      to: "contracts/AfKing.sol::doWork advance leg (unit*ADVANCE_RATIO_NUM*mult, :899)"
      via: "_countCoinflipStakeUpdatedFor(keeper) == 1 with mult honored"
      pattern: "advanceGame|ADVANCE_RATIO_NUM"
    - from: "test/fuzz/KeeperRewardRoutingSameResults.t.sol"
      to: "contracts/DegenerusGame.sol::keeperSnapshot (:2628)"
      via: "assertEq(keeperSnapshot[i], claimableWinningsOf(player))"
      pattern: "keeperSnapshot|claimableWinningsOf"
---

<objective>
Prove the advanceGame reward-routing rework (ADV-01..05) and the two GASOPT micro-opts EMPIRICALLY:
`advanceGame` is UNREWARDED when called standalone via `game.advanceGame()` (the 3 in-callee
`creditFlip` sites were removed at ADV-01) and REWARDED when driven via `doWork` (the stall
multiplier honored on the new-day path, the mid-day partial-drain `mult=1` leg rewarded, gameover
`mult=0` unrewarded); and the two GASOPT micro-opts produce same-results (gas-only, byte-identical
behavior) — GASOPT-01 the MintModule `owedMap` pointer hoist and GASOPT-03 the batched
`keeperSnapshot` read (which SUBSUMES the original GASOPT-02 per-iteration `claimableWinningsOf`
hoist — there is NO separate AfKing claimable-hoist site).

Purpose: TST-03 closes the SC3 reward-routing + same-results bar. The re-homing must demonstrably
move the bounty from the standalone path to the router without changing the advance behavior.
Output: a NEW `test/fuzz/KeeperRewardRoutingSameResults.t.sol` proof file.

POSTURE (binding): `test/` + `.planning/` ONLY. ZERO `contracts/*.sol` (mainnet) mutation — FROZEN
subject. Contract `file:line` are READ-ONLY anchors. Surface any contract defect, do not patch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg/332-RESEARCH.md
@.planning/phases/332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg/332-PATTERNS.md

<interfaces>
<!-- READ-ONLY contract anchors (FROZEN subject). -->

contracts/modules/DegenerusGameAdvanceModule.sol — the advanceGame return shape:
  :154  function advanceGame() external returns (uint8 mult)   // NO creditFlip — returns only mult
  :156  mult = 1;                                              // default
  :185  // gameover → return mult = 0 so doWork pays nothing
  :217-218  // mid-day partial-drain: mult = 1 (ADV-05/D-07 — no escalation); return mult
  :236/:238/:240  mult = 6 / 4 / 2;                            // new-day stall ladder (1/2/4/6)

contracts/AfKing.sol — the router advance leg:
  :896  } else if (IGame(GAME).advanceDue()) {
  :897     uint8 mult = IGame(GAME).advanceGame();
  :899     if (mult > 0) bountyEarned = unit * ADVANCE_RATIO_NUM * mult;   // mult==0 pays nothing
  :860  uint256 internal constant ADVANCE_RATIO_NUM = 2;
  :807  // AfKing consumes keeperSnapshot here (GASOPT-03)

contracts/DegenerusGame.sol — GASOPT-03 batched read:
  :2628  function keeperSnapshot(address[] calldata players) external view
         returns (uint256 mintPriceWei, bool rngLocked_, uint256[] memory claimables)

contracts/modules/DegenerusGameMintModule.sol — GASOPT-01 owedMap pointer hoist:
  :399  mapping(address => uint40) storage owedMap = ticketsOwedPacked[rk];  // processFutureTicketBatch
  :673  mapping(address => uint40) storage owedMap = ticketsOwedPacked[rk];  // processTicketBatch

reuse: CrankLeversAndPacking `_countCoinflipStakeUpdatedFor` (:538); AfKingConcurrency
`_setupHealthyBuyingSubs` (:100); AdvanceGameRewrite `AdvanceHarness` (test/fuzz/AdvanceGameRewrite.t.sol:8);
DegeneretteFreezeResolution snapshot/replay delta-capture (:351-433); RngLockDeterminism
`_snapshotPreLock`/`_revertToPreLock` (:130/:134).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: advanceGame unrewarded-standalone vs rewarded-via-doWork (multiplier honored)</name>

  <files>test/fuzz/KeeperRewardRoutingSameResults.t.sol</files>
  <read_first>
    - test/fuzz/AdvanceGameRewrite.t.sol (the `AdvanceHarness` drain-gate exposure :8-102 — the
      advance-driving scaffold)
    - test/gas/CrankLeversAndPacking.t.sol (`_countCoinflipStakeUpdatedFor` :538 — the recipient-
      isolated count oracle; port it or via a shared base)
    - contracts/modules/DegenerusGameAdvanceModule.sol:154 (returns only mult), :185 (gameover
      mult=0), :217-218 (mid-day mult=1), :236/:238/:240 (new-day ladder 1/2/4/6) — READ-ONLY
    - contracts/AfKing.sol:896-899 (the router advance leg `unit*ADVANCE_RATIO_NUM*mult`) — READ-ONLY
    - .planning/phases/332-.../332-PATTERNS.md (the TST-03 advanceGame section, lines 101-104)
  </read_first>
  <behavior>
    - Test (standalone UNREWARDED): drive an advance-due state; call `game.advanceGame()` directly
      with `vm.recordLogs()`; assert `_countCoinflipStakeUpdatedFor(caller) == 0` (no bounty) AND the
      day actually advanced (the tick happened — advance is still fully functional standalone).
    - Test (rewarded via doWork, new-day mult honored): drive a new-day advance-due state with a
      stall so `mult > 1`; fire `doWork()`; assert `_countCoinflipStakeUpdatedFor(keeper) == 1` and
      the credited amount is consistent with `mult` being honored (assert the credit at a higher
      stall is strictly greater than at mult=1 — proving the multiplier flows through, WITHOUT
      hard-coding the exact peg).
    - Test (mid-day partial-drain rewarded, mult=1): drive `day == dailyIdx` with tickets not fully
      processed; fire `doWork()`; assert exactly one creditFlip to the keeper (the partial-drain leg
      IS rewardable advance-leg work).
    - Test (gameover UNREWARDED): drive the gameover path (mult=0) reachable via `doWork`; assert
      ZERO creditFlip.
  </behavior>
  <action>
    Create `test/fuzz/KeeperRewardRoutingSameResults.t.sol` as `contract
    KeeperRewardRoutingSameResults is DeployProtocol`. Port the `_countCoinflipStakeUpdatedFor`
    log-count helper (+ `COINFLIP_STAKE_UPDATED_SIG`) from `CrankLeversAndPacking.t.sol`. Author
    `testAdvanceStandaloneUnrewarded` (zero credit + day advanced), `testAdvanceViaDoWorkRewarded`
    (one credit, mult honored — assert higher-stall credit > mult=1 credit, not the exact constant),
    `testMidDayPartialDrainRewardedViaDoWork` (one credit), and `testGameoverAdvanceUnrewarded` (zero
    credit). Use the `AdvanceHarness` / day-advancing helpers to reach each mult state. Assert the
    multiplier is HONORED by comparing relative credit magnitudes across stall levels, never by
    asserting the GAS-calibrated constant value (which is owned by 331 and not the proof's subject).
  </action>
  <verify>
    <automated>forge test --match-contract KeeperRewardRoutingSameResults --match-test "Advance|MidDay|Gameover" 2>&1 | tail -20</automated>
  </verify>
  <done>Standalone advance credits zero (but ticks the day); doWork-driven advance credits exactly
  one with the multiplier honored (higher stall → strictly larger credit); mid-day partial-drain
  rewarded; gameover unrewarded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GASOPT-01 + GASOPT-03 behavioral same-results equality</name>

  <files>test/fuzz/KeeperRewardRoutingSameResults.t.sol</files>
  <read_first>
    - contracts/DegenerusGame.sol:2628 (`keeperSnapshot(address[])` returns
      `(mintPriceWei, rngLocked_, claimables[])`) — READ-ONLY
    - contracts/modules/DegenerusGameMintModule.sol:399 + :673 (the `owedMap` pointer-hoist sites in
      `processFutureTicketBatch` / `processTicketBatch`) — READ-ONLY
    - test/fuzz/DegeneretteFreezeResolution.t.sol:351-433 (the snapshot→run-A→revert→run-B→assertEq
      delta-capture idiom)
    - test/fuzz/RngLockDeterminism.t.sol:130/:134 (`_snapshotPreLock`/`_revertToPreLock`)
    - test/fuzz/AfKingConcurrency.t.sol:100 (`_setupHealthyBuyingSubs`), :807-ref keeperSnapshot
      consumption
    - .planning/phases/332-.../332-RESEARCH.md (Pitfall 5: GASOPT-02 SUBSUMED into GASOPT-03 — do NOT
      search for an AfKing per-iter claimableWinningsOf hoist; count is 0)
    - .planning/phases/332-.../332-PATTERNS.md (the TST-03 GASOPT same-results block, lines 106-111)
  </read_first>
  <behavior>
    - Test (GASOPT-03 batched == individual): with N players holding varied claimable balances,
      assert `keeperSnapshot(players)` returns `mintPriceWei == game.mintPrice()`,
      `rngLocked_ == game.rngLocked()`, and `claimables[i] == game.claimableWinningsOf(players[i])`
      for every i — the batched read is value-identical to N individual reads.
    - Test (GASOPT-03 drives identical autoBuy outcome): run an autoBuy driven through the
      `keeperSnapshot` path and assert the buy outcomes (subs bought, pool deltas) are identical to
      the reference per-player computation.
    - Test (GASOPT-01 same-results): exercise `processTicketBatch` / `processFutureTicketBatch` over a
      multi-player ticket backlog and assert the ticket-processing RESULTS (owed deltas, traits/
      indices, claimable changes) are byte-identical to the expected per-player accounting — the
      `owedMap` pointer hoist is `rk`-loop-invariant and behavior-identical.
  </behavior>
  <action>
    Add to `test/fuzz/KeeperRewardRoutingSameResults.t.sol`: `testKeeperSnapshotEqualsIndividualReads`
    (assert the 3-tuple equality element-by-element across N players),
    `testKeeperSnapshotDrivenAutoBuyIdenticalOutcome` (snapshot/revert delta-capture: the autoBuy
    outcome through the batched read == the reference), and `testGasopt01OwedMapHoistSameResults`
    (process a multi-player ticket backlog, assert owed/trait/claimable results match the expected
    per-player accounting). Use the `DegeneretteFreezeResolution`-style snapshot→run→revert→assertEq
    delta-capture; never a bytecode diff and never resurrected old source. Do NOT search `AfKing.sol`
    for a per-iteration `claimableWinningsOf` STATICCALL — GASOPT-02 was SUBSUMED into the batched
    `keeperSnapshot` (count 0); the "two GASOPT micro-opts" are GASOPT-01 (owedMap) + GASOPT-03
    (keeperSnapshot).
  </action>
  <verify>
    <automated>forge test --match-contract KeeperRewardRoutingSameResults 2>&1 | tail -25</automated>
  </verify>
  <done>`keeperSnapshot` is value-identical to N individual reads (all three tuple fields); a
  keeperSnapshot-driven autoBuy matches the reference outcome; GASOPT-01 ticket-processing results
  are byte-identical. No per-iter `claimableWinningsOf` hoist is searched for or asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| standalone caller → game.advanceGame | the unrewarded liveness fallback path |
| keeper → doWork advance leg | the re-homed bounty path (pays from the multiplier's canonical home) |
| keeper → keeperSnapshot (view) | a batched read; no state mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-332-03-DBL | Elevation of Privilege (reward) | advance bounty re-home | mitigate | proven: standalone advance credits ZERO (the 3 in-callee creditFlip removed); only doWork credits — no double-home, no recompute in a money path |
| T-332-03-MULT | Tampering (reward) | stall multiplier flow | mitigate | proven: the multiplier is honored (higher stall → strictly larger credit) and gameover mult=0 pays nothing — asserted by relative magnitude, not the exact peg (peg is owned by 331) |
| T-332-03-EQUIV | Tampering | GASOPT same-results | mitigate | behavioral-equality: keeperSnapshot == N individual reads (value-identical); owedMap hoist byte-identical results — a same-results assertion that compares CONCRETE state deltas, not nothing |
| T-332-03-SUBSUME | Information Disclosure | GASOPT-02 phantom site | accept | GASOPT-02 SUBSUMED into GASOPT-03 (count 0) — the proof does not chase a non-existent per-iter hoist (Pitfall 5) |
| T-332-03-SC | Tampering | test toolchain | accept | no package installs (test-authoring only) |
</threat_model>

<verification>
- `forge test --match-contract KeeperRewardRoutingSameResults` green.
- The same-results assertions compare concrete state (tuple fields / owed deltas / buy outcomes) —
  not a vacuous comparison.
- The reward-routing assertions use relative magnitude for the multiplier, never the 331 peg constant.
- Zero `contracts/*.sol` modifications: `git diff --name-only contracts/` returns nothing.
</verification>

<success_criteria>
TST-03 proven: advance is unrewarded standalone and rewarded via the router with the multiplier
honored (mid-day rewarded, gameover unrewarded); GASOPT-01 and GASOPT-03 are same-results.
</success_criteria>

<output>
Create `.planning/phases/332-tst-freeze-fuzz-one-category-reward-routing-non-widening-reg/332-03-SUMMARY.md` when done
</output>
