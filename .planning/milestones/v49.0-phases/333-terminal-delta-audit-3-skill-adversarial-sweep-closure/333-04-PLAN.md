---
phase: 333-terminal-delta-audit-3-skill-adversarial-sweep-closure
plan: 04
type: execute
wave: 3
depends_on: ["333-03"]
files_modified:
  - audit/FINDINGS-v49.0.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/MILESTONES.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [BATCH-03]

must_haves:
  truths:
    - "A single blocking USER checkpoint approves the closure verdict + the MILESTONE_V49_AT_HEAD_<sha> signal + the GASOPT-05 OPEN-E 4-protection re-attestation outcome + any new_findings disposition BEFORE the signal is emitted"
    - "MILESTONE_V49_AT_HEAD_<sha> is resolved to the concrete closure-HEAD SHA (the closure commit's own SHA, self-referential) and propagated verbatim across all targets"
    - "The atomic 5-doc closure flip (ROADMAP + STATE + MILESTONES + PROJECT + REQUIREMENTS) is applied with all 36 v49.0 requirements re-attested at closure"
    - "audit/FINDINGS-v49.0.md is chmod 444 at the resolved closure HEAD"
    - "A final grep confirms ZERO unresolved MILESTONE_V49_AT_HEAD_<sha> placeholders remain"
    - "git diff 4c9f9d9b HEAD -- contracts/ is empty (closure is doc-only; subject frozen at 4c9f9d9b); nothing is pushed"
  artifacts:
    - path: "audit/FINDINGS-v49.0.md"
      provides: "The locked (chmod 444) v49.0 findings deliverable with the resolved closure-signal SHA"
      contains: "MILESTONE_V49_AT_HEAD_"
    - path: ".planning/REQUIREMENTS.md"
      provides: "All 36 v49.0 REQ rows flipped to a closure attestation"
      contains: "BATCH-03"
  key_links:
    - from: ".planning/STATE.md"
      to: "audit/FINDINGS-v49.0.md"
      via: "verbatim closure-signal propagation"
      pattern: "MILESTONE_V49_AT_HEAD_"
    - from: ".planning/REQUIREMENTS.md"
      to: "the 36 v49.0 REQ-IDs"
      via: "closure re-attestation"
      pattern: "Complete|RESOLVED|NEGATIVE-VERIFIED"
---

<objective>
Author the BATCH-03 CLOSURE FLIP: gate on a single USER approval of the closure verdict + the `MILESTONE_V49_AT_HEAD_<sha>` signal + the GASOPT-05 OPEN-E 4-protection re-attestation + any new_findings disposition, then resolve the placeholder SHA, apply the atomic 5-doc closure flip (ROADMAP + STATE + MILESTONES + PROJECT + REQUIREMENTS) with all 36 requirements re-attested, and chmod 444 the findings deliverable. Closure is LAST and is `autonomous: false` (the single blocking USER gate, CONTEXT D-10). Doc-only — ZERO `contracts/*.sol` edits (the subject stays frozen at `4c9f9d9b`). NOTHING is pushed.

Purpose: BATCH-03 of the terminal phase — the milestone close. This is `autonomous: false` per the v45.0/v47.0/v48.0 "USER-APPROVED closure" precedent + the project's review-before-closure discipline ([[feedback_wait_for_approval]] / feedback_pause_at_contract_phase_boundaries): the USER must approve the closure verdict + the signal string before it is emitted/propagated. Do NOT auto-advance even if auto_advance is on.
Output: the resolved `MILESTONE_V49_AT_HEAD_<sha>` signal propagated verbatim; the 5-doc flip applied; audit/FINDINGS-v49.0.md locked chmod 444; nothing pushed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MILESTONES.md
@.planning/REQUIREMENTS.md
@audit/FINDINGS-v49.0.md
@audit/FINDINGS-v48.0.md
@.planning/phases/333-terminal-delta-audit-3-skill-adversarial-sweep-closure/333-CONTEXT.md

<interfaces>
<!-- The closure-flip mechanics (from the v48 Phase 328 terminal precedent). Executor follows exactly. -->
- The 5 atomic flip targets: (1) ROADMAP.md — flip Phase 333 to Complete + the v49.0 milestone to shipped + the Progress table row 333; (2) STATE.md — move v49.0 from the active "Current Milestone" position to "Last Shipped Milestone" with the closure-signal + verdict, update progress (5/5 phases, percent 100), and clear the stale "Next: Phase 333" notes; (3) MILESTONES.md — prepend the v49.0 archive entry (mirror the v48.0 archive block at the top of the file); (4) PROJECT.md — flip the Current Milestone section to v49.0 shipped; (5) REQUIREMENTS.md — flip all 36 v49.0 REQ rows in the Traceability table to a closure attestation (the 14 already-Complete rows [329 SPEC 4 + 331 GAS 5 + 332 TST 5] stay; the 18 IMPL rows [330, currently Pending] + the 4 TERMINAL rows [SWEEP-01/02/03 + BATCH-03] flip to attested-at-closure).
- The closure signal: `MILESTONE_V49_AT_HEAD_<sha>` where `<sha>` is the v49.0 closure HEAD. The placeholder `<sha>` resolves to the closure commit's OWN SHA (self-referential). RESEARCH Pitfall 1 / State-of-the-Art note: v48 (the immediate precedent) used a SINGLE-commit self-referential orchestration (`57a796d1`: emit + flip + chmod 444 in one commit; the signal = the commit's own SHA); CONTEXT D-10's "2-commit" wording is carried from the older v44/v46/v47 precedent. BOTH models yield the same self-referential SHA — RECOMMEND mirroring the v48 single-commit actual (simpler, the immediate precedent), but this is a low-stakes executor's-discretion detail. The signal propagates VERBATIM to: audit/FINDINGS-v49.0.md frontmatter (closure_signal + audit_subject_head) + §1 + §9b/§9c; ROADMAP.md; STATE.md (Last Shipped Milestone); MILESTONES.md (archive); PROJECT.md; REQUIREMENTS.md. Replace every `MILESTONE_V49_AT_HEAD_<sha>` placeholder string with the resolved value in one pass.
- The audit subject is FROZEN at `4c9f9d9b`; the closure is a DOC-ONLY flip — no contracts/*.sol edits. `.planning/` is gitignored, so force-add planning docs at commit time ([[feedback_contract_commit_guard_hook]]); audit/FINDINGS-v49.0.md is tracked. The commit-guard hook does NOT block (no .sol in the diff).
- chmod 444: applied to audit/FINDINGS-v49.0.md at closure (the v44/v46/v47/v48 precedent — FINAL READ-only at the closure HEAD).
- The USER gate (autonomous: false): present the closure verdict (from FINDINGS-v49.0 §9a) + the proposed `MILESTONE_V49_AT_HEAD_<sha>` signal + the GASOPT-05 OPEN-E 4-protection re-attestation outcome (the HARD BLOCKING condition from 333-01 — if it FAILED, the GASOPT-05 :676-drop is REVERTED before ship, which HALTS closure into a new contract phase) + the new_findings disposition (from 333-02) and WAIT for explicit USER approval BEFORE resolving the SHA / propagating / flipping. This is the single review-before-closure gate. Do NOT auto-advance even if auto_advance is on.
- new_findings: if 333-02 surfaced a FINDING_CANDIDATE, the USER adjudicates its disposition (DEFER->v50 [default leaning, fix design locked] / FIX-as-new-phase / ACCEPT_AS_DOCUMENTED) at THIS gate, exactly as v47 F-47-01/F-47-02 were DEFER-adjudicated; the verdict's `0 NEW_FINDINGS` clause is amended to match. If a candidate is FIX-adjudicated OR the OPEN-E re-attestation FAILED, closure HALTS and a new contract phase is planned (the subject is frozen at `4c9f9d9b`; any fix needs USER approval). If zero candidates + OPEN-E holds, the `0 NEW_FINDINGS` clause holds verbatim.
- Commit discipline: planning docs + audit/FINDINGS-v49.0.md are AGENT-committable (no contracts/*.sol in the diff -> the commit-guard hook does not block; verify `git diff 4c9f9d9b HEAD -- contracts/` empty before committing). End the commit message with the Co-Authored-By trailer per the global commit convention. **NOTHING is pushed** ([[feedback_manual_review_before_push]] + the CONTEXT posture).
</interfaces>
</context>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan introduces NO new code or attack surface — it is the doc-only milestone closure flip. The audit subject stays frozen at `4c9f9d9b`. The only "boundary" is the human review gate before the closure signal is emitted. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-333-04-PREMATURE | Elevation | emitting the closure signal before review | mitigate | The plan is autonomous: false — a single blocking USER checkpoint approves the verdict + signal + the OPEN-E re-attestation outcome + any new_findings disposition BEFORE resolution/propagation (the v45.0/v47.0/v48.0 USER-APPROVED-CLOSE precedent; does not auto-advance even with auto_advance on). |
| T-333-04-SHADRIFT | Tampering | closure-signal SHA propagation | mitigate | The `MILESTONE_V49_AT_HEAD_<sha>` placeholder is resolved + propagated VERBATIM in one pass across all targets; a final grep confirms ZERO unresolved `<sha>` placeholders remain (the literal `<sha>` token must be gone). |
| T-333-04-CONTRACTS | Tampering | frozen subject | mitigate | `git diff 4c9f9d9b HEAD -- contracts/` verified empty before the closure commit; closure is doc-only; the commit-guard hook does not block (no .sol in the diff); nothing is pushed. |
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: USER closure-verdict approval gate (before emitting MILESTONE_V49_AT_HEAD)</name>
  <files>(none — this is a blocking human-verify gate; the doc flips happen in Task 2 after approval)</files>
  <what-built>
    The full v49.0 audit substance is complete and consolidated: 333-01 delta-audit (every contract surface NON-WIDENING vs v48 across the 5-file blast radius, each delta hunk mapped to one v49 work item, the 4 structural invariants intact, the OPEN-E 4-protection BLOCKING re-attestation, VRF-freeze intact, regression 666/42/17 NON-WIDENING by NAME, the v48 SWAP advisory carried-forward), 333-02 adversarial sweep (the 3-skill disposition + skeptic dual-gate; the composed-reentrancy SAFE_BY_DESIGN row), and audit/FINDINGS-v49.0.md (the 9-section deliverable). The closure verdict + the proposed closure signal are ready but NOT yet emitted.
  </what-built>
  <how-to-verify>
    1. Read audit/FINDINGS-v49.0.md §9a (the closure verdict — locked-target + actual) and §4.1 (the adversarial sweep outcome: row counts + any FINDING_CANDIDATE).
    2. Confirm the delta-audit attests all 5 surfaces NON-WIDENING + the 4 structural invariants intact + the OPEN-E 4-protection re-attestation HOLDING (333-01-DELTA-AUDIT.md §3.B) + VRF-freeze intact, the regression baseline is 666/42/17 NON-WIDENING by NAME, and the v48 SWAP advisory is carried-forward-unmodified.
    3. Approve (a) the closure verdict text, (b) the `MILESTONE_V49_AT_HEAD_<sha>` signal format, (c) the GASOPT-05 OPEN-E 4-protection re-attestation outcome (if it FAILED, the :676-drop is REVERTED → closure HALTS into a new contract phase), and (d) the disposition of any FINDING_CANDIDATE surfaced by 333-02 (DEFER->v50 [default leaning] / FIX-as-new-phase / ACCEPT_AS_DOCUMENTED — the v47 F-47-01/F-47-02 DEFER precedent). If a candidate is FIX-adjudicated, closure HALTS and a new contract phase is planned (the subject is frozen at `4c9f9d9b`; any fix needs USER approval).
  </how-to-verify>
  <read_first>
    - audit/FINDINGS-v49.0.md (§9a verdict + §4 adversarial disposition + §3 delta-surface + §3.B the OPEN-E re-attestation)
    - .planning/phases/333-terminal-delta-audit-3-skill-adversarial-sweep-closure/333-01-DELTA-AUDIT.md
    - .planning/phases/333-terminal-delta-audit-3-skill-adversarial-sweep-closure/333-02-ADVERSARIAL-LOG.md
    - .planning/ROADMAP.md (the Phase 333 goal — the locked-target verdict source)
  </read_first>
  <action>
    Present the closure verdict (FINDINGS-v49.0 §9a), the adversarial sweep outcome (§4.1 row counts + any FINDING_CANDIDATE), the delta-audit NON-WIDENING attestation, the GASOPT-05 OPEN-E 4-protection re-attestation outcome (the HARD BLOCKING condition), and the carried-forward v48 SWAP advisory to the USER, then BLOCK for explicit approval of the verdict text + the MILESTONE_V49_AT_HEAD signal format + the OPEN-E outcome + any FINDING_CANDIDATE disposition. Resolve / propagate / flip nothing until approval is given. This gate must NOT auto-advance even with auto_advance on (per feedback_pause_at_contract_phase_boundaries — confirm direction at the closure boundary).
  </action>
  <verify>
    <human-check>The USER reviews FINDINGS-v49.0 §9a + §4.1 + §3.B (the OPEN-E re-attestation) + the 333-01/333-02 logs and explicitly approves the verdict + the signal + the OPEN-E outcome + any FINDING_CANDIDATE disposition.</human-check>
  </verify>
  <acceptance_criteria>
    - The closure verdict (FINDINGS §9a), the sweep outcome (§4.1), the delta-audit NON-WIDENING + the 4 invariants + the OPEN-E 4-protection re-attestation outcome, and any FINDING_CANDIDATE disposition are presented to the USER.
    - Execution BLOCKS until the USER explicitly approves the verdict + the signal format + the OPEN-E outcome + the FINDING_CANDIDATE disposition.
    - No closure-signal SHA is resolved or propagated, and no doc is flipped, before approval.
    - This gate does NOT auto-advance even if auto_advance is on.
  </acceptance_criteria>
  <done>The USER has explicitly approved the closure verdict + the MILESTONE_V49_AT_HEAD signal + the OPEN-E 4-protection re-attestation outcome + any FINDING_CANDIDATE disposition; execution is cleared to proceed to Task 2.</done>
  <resume-signal>Type "approved" to emit MILESTONE_V49_AT_HEAD and apply the 5-doc flip, or describe required changes / the FINDING_CANDIDATE disposition / an OPEN-E revert.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Resolve the closure-signal SHA, apply the atomic 5-doc flip, chmod 444, commit (nothing pushed)</name>
  <files>audit/FINDINGS-v49.0.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/MILESTONES.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - audit/FINDINGS-v48.0.md §9 (the single-commit self-referential SHA closure orchestration + the verbatim propagation-target list to mirror)
    - audit/FINDINGS-v49.0.md (the placeholder targets to resolve: frontmatter + §1 + §9b/§9c)
    - .planning/ROADMAP.md (Phase 333 + the v49.0 milestone flip points + the Progress table)
    - .planning/STATE.md (the Current Milestone -> Last Shipped Milestone move + the stale "Next: Phase 333" notes to clear)
    - .planning/MILESTONES.md (the v48.0 archive block at the top — the format to mirror for the v49.0 entry, prepended)
    - .planning/PROJECT.md (the Current Milestone section)
    - .planning/REQUIREMENTS.md (the Traceability table — all 36 v49.0 REQ rows)
  </read_first>
  <action>
    After USER approval (Task 1), execute the closure:
    1. Verify `git diff 4c9f9d9b HEAD -- contracts/` is empty (the subject is frozen; closure is doc-only; the T-CONTRACTS guard).
    2. Resolve the `MILESTONE_V49_AT_HEAD_<sha>` placeholder to the closure-HEAD SHA (the closure commit's own SHA, self-referential; mirror the v48 single-commit actual unless splitting — executor's discretion per RESEARCH Pitfall 1). Replace EVERY `MILESTONE_V49_AT_HEAD_<sha>` placeholder string in audit/FINDINGS-v49.0.md (frontmatter closure_signal + audit_subject_head, §1, §9b, §9c) with the resolved value.
    3. Apply the atomic 5-doc flip, propagating the resolved signal VERBATIM:
       - ROADMAP.md: Phase 333 -> Complete; the v49.0 milestone -> shipped (closure signal + verdict); the Progress table row 333 -> Complete.
       - STATE.md: move v49.0 to "Last Shipped Milestone" (closure signal + the 5-phase summary + verdict + the new_findings disposition); update progress (5/5 phases, percent 100); CLEAR the stale "Next: Phase 333 TERMINAL HELD for USER" notes (it is DONE).
       - MILESTONES.md: prepend the v49.0 archive entry mirroring the v48.0 block (subject `4c9f9d9b`, baseline `0cc5d10f`, the 5-phase shape 329-333, closure signal, the unified router + bounty re-peg + degeneretteResolve + GASOPT shipped, the 4 invariants + OPEN-E re-attested + RNG-freeze intact, regression 666/42/17 NON-WIDENING by NAME, any FINDING_CANDIDATE disposition).
       - PROJECT.md: flip the Current Milestone section (v49.0 shipped).
       - REQUIREMENTS.md: flip all 36 v49.0 REQ rows in the Traceability table to a closure attestation (the 18 Pending IMPL rows [330] + the 4 TERMINAL rows [SWEEP-01/02/03 + BATCH-03] -> Complete/attested-at-closure; the 4 SPEC rows + 5 GAS rows + 5 TST rows already Complete stay; GASOPT-02 stays SUBSUMED).
    4. chmod 444 audit/FINDINGS-v49.0.md (FINAL READ-only at the closure HEAD, the v44/v46/v47/v48 precedent).
    5. Commit the closure flip (planning docs force-added since .planning/ is gitignored; audit/FINDINGS-v49.0.md tracked; no contracts/*.sol in the diff so the commit-guard hook does not block). End the commit message with the Co-Authored-By trailer. **DO NOT push.**
    Confirm a final grep finds ZERO unresolved `MILESTONE_V49_AT_HEAD_<sha>` placeholders remaining (the literal `<sha>` token must be gone). Doc-only — no contracts/ edits.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && test -z "$(git diff 4c9f9d9b HEAD -- contracts/)" && test "$(stat -c '%a' audit/FINDINGS-v49.0.md)" = "444" && ! grep -q "MILESTONE_V49_AT_HEAD_<sha>" audit/FINDINGS-v49.0.md</automated>
  </verify>
  <acceptance_criteria>
    - `git diff 4c9f9d9b HEAD -- contracts/` is empty (closure is doc-only; subject frozen).
    - The `MILESTONE_V49_AT_HEAD_<sha>` placeholder is resolved to the concrete closure SHA and propagated verbatim to all FINDINGS targets (frontmatter + §1 + §9b/§9c); ZERO unresolved `<sha>` placeholders remain.
    - The atomic 5-doc flip is applied: ROADMAP (Phase 333 Complete + milestone shipped + Progress row), STATE (Last Shipped Milestone + signal + verdict + stale "Next:" notes cleared + 5/5 phases 100%), MILESTONES (v49.0 archive entry prepended), PROJECT (Current Milestone flipped), REQUIREMENTS (all 36 v49.0 rows attested at closure).
    - audit/FINDINGS-v49.0.md is chmod 444.
    - The closure flip is committed with the Co-Authored-By trailer; the commit-guard hook did not block (no contracts/*.sol in the diff); NOTHING is pushed.
  </acceptance_criteria>
  <done>Closure signal resolved + propagated verbatim; the atomic 5-doc flip applied with all 36 reqs re-attested; FINDINGS-v49.0.md chmod 444; closure committed (not pushed); contracts/ byte-identical to 4c9f9d9b; zero unresolved placeholders.</done>
</task>

</tasks>

<verification>
- `git diff 4c9f9d9b HEAD -- contracts/` empty (closure is doc-only; subject frozen throughout).
- audit/FINDINGS-v49.0.md is chmod 444 with the resolved closure SHA (no `<sha>` placeholder remaining).
- The 5 docs (ROADMAP/STATE/MILESTONES/PROJECT/REQUIREMENTS) are flipped atomically; all 36 v49.0 REQ rows attested at closure.
- The USER approved the verdict + signal + the OPEN-E re-attestation outcome at the Task 1 blocking gate before any propagation; nothing pushed.
</verification>

<success_criteria>
BATCH-03 satisfied: the USER approves the closure verdict + the MILESTONE_V49_AT_HEAD signal + the OPEN-E 4-protection re-attestation outcome at a single blocking gate; the signal is resolved (self-referential closure-commit SHA) + propagated verbatim; the atomic 5-doc closure flip is applied with all 36 requirements re-attested; FINDINGS-v49.0.md is locked chmod 444; zero unresolved placeholders; nothing pushed. contracts/ byte-identical to `4c9f9d9b`.
</success_criteria>

<output>
Create `.planning/phases/333-terminal-delta-audit-3-skill-adversarial-sweep-closure/333-04-SUMMARY.md` when done
</output>
