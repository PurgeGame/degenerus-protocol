---
phase: 329-spec-design-lock-call-graph-attestation-4-structural-invaria
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md
autonomous: true
requirements: [BATCH-01, ROUTER-07, ADV-04, GAS-03]
must_haves:
  truths:
    - "Every cited file:line on the REDESIGNED router/advance surface is grep-attested against the FROZEN v48.0-closure HEAD 0cc5d10f via `git show 0cc5d10f:contracts/...` (NOT the live working tree — the tree carries the held-330 diff and is dirty), with a MATCH / SHIFTED(±N) / ABSENT verdict and the actual 0cc5d10f line; held-tree/redesign-doc line drift (e.g. rngLock guard held :717 → 0cc5d10f :568; isOperatorApproved held :838 → 0cc5d10f :676; claimableWinningsOf held :854/:888 → 0cc5d10f :691/:722; AutoBought emit held :954 → 0cc5d10f :785, decl held :186 → :171; batchPurchase pre-checks held :1693/:1694 → 0cc5d10f :1737/:1738; autoOpen try/catch held :1701/1707/1719 → 0cc5d10f :1664/:1672/:1676) is recorded per anchor"
    - "RD-2 (autoBuy = normal buy / drop rngLock guards) is attested: the AfKing _autoBuy-precursor rngLock guard (autoBuy entry `if (rngLocked()) revert AutoBuyAborted(...,1)` AfKing.sol:568) AND the game-side batchPurchase rngLock pre-check (DegenerusGame.sol:1737) are both grep-located; the gameOver pre-check (:1738) is confirmed KEEP; and the Q5 dependent grep is PERFORMED — every batchPurchase caller is enumerated and the verdict recorded on whether removing the :1737 game-side rngLock pre-check affects any caller besides the keeper (it gates ALL batchPurchase callers, not just AfKing)"
    - "RD-3/RD-5 (block autoOpen during rngLock + drop try/catch + entry-gate) is attested: autoOpen's ++cursor-before-try/catch ordering (DegenerusGame.sol:1659 ++cursor / :1664 try this._autoOpenBox / :1672 catch{}) is grep-shown (the stranded-tail hazard if try/catch is dropped without an entry-gate); _autoOpenBox is confirmed external onlySelf (:1705); the EXACTLY-TWO open-path revert sources are grep-attested — rngLock (DegenerusGame.sol:1737/rngLocked :2413) and the deliberate terminal-jackpot control `_queueTickets → if(_livenessTriggered()) revert E()` at contracts/storage/DegenerusGameStorage.sol:571 (reached via _autoOpenBox→_openLootBoxFor DegenerusGame.sol:729→_queueTickets DegenerusGameStorage:560); _livenessTriggered defined :1213 — confirming the entry-gate `if (rngLocked() || _livenessTriggered()) return` replicates both"
    - "RD-4 (unify the bounty into doWork) is attested: the THREE in-callee creditFlip sites that the unified model pulls out are grep-located and classified — the advance trio (AdvanceModule.sol:189 new-day / :225 mid-day partial-drain ADV-05 / :468) + the autoOpen in-callee creditFlip (DegenerusGame.sol:1676) + the autoBuy in-callee creditFlip (AfKing.sol:846); the gameover-RNG-path creditFlip (AdvanceModule.sol:876) is classified NOT-an-advance-leg-reward; the verdict that after the rework there is exactly ONE creditFlip site (in doWork) fed by legs that return raw counts/mult and never self-credit is recorded"
    - "D-07 (flat per-tx model) deletion/keep surface is attested: the AfKing autoBuy stall ladder + its absolute-day epoch (AfKing.sol:823-846, dayStart=today*1days+82620 :829) is grep-located + classified DEAD-after-redesign (D-07: only advance escalates → autoBuy stall multiplier removed); the AdvanceModule new-day stall ladder + game-day epoch (:241-253, (day-1+DEPLOY_DAY_BOUNDARY)*1days+82620 :244-246) is KEPT (advance is the SOLE stall epoch); the autoOpen gas-units bounty machinery (AUTO_OPEN_BOX_GAS_UNITS DegenerusGame.sol:1546 + the open-leg _ethToBurnieValue conversion :1666) is grep-located + classified DEAD-after-unification (open folds onto the shared per-tx base); GAS-03 verdict recorded = SATISFIED BY DELETION (advance is the sole stall epoch; no two epochs to collapse — D-03 dissolved by D-07)"
    - "ADV-04 (invariant b): the totalFlipReversals nudge is grep-attested at 0cc5d10f — read into the consumed word at AdvanceModule.sol:1838 (`nudges = totalFlipReversals`) inside _applyDailyRng (:1834) and reset (:1844); the additional in-window read at :270 (`cw += totalFlipReversals`) is enumerated per feedback_rng_window_storage_read_freshness; recorded fact = the protected freeze window is rng-request→consume and the REDESIGNED router (autoBuy→advance→autoOpen, with autoBuy now UNGATED during day-open) introduces NO new mutable in-window SLOAD into the advance-consume (autoBuy runs at day-open BEFORE the word is requested; advance consumes via the design-1 return and recomputes nothing); the empirical proof is TST-01's job"
    - "ROUTER-07 (D-01/D-01a, re-grounded on the unified single creditFlip): the no-untrusted-ETH-send claim is grep-attested PER LEG (advance / autoOpen / _autoBuy) at 0cc5d10f AND for the single unified doWork creditFlip — each leg routes player value through claimableWinnings (pull ledger) and sends ETH only to ContractAddresses.* pinned contracts; the unified model has exactly ONE creditFlip (CEI-last) fed by non-self-crediting legs; the formal no-guard basis is recorded verbatim = keeper-never-a-payee + no untrusted ETH send + one-category structural early-return + single-creditFlip-last CEI ordering (AfKing CEI :100)"
    - "Invariant (c) (D-04, amended): the existing structural free-fallback advanceGame() caller paths are grep-attested — the 30-min universal bypass (AdvanceModule.sol:1012 `if (elapsed >= 30 minutes) return`), DegenerusVault.gameAdvance() :527, StakedDegenerusStonk.gameAdvance() :421, and the 120-day death-clock (DEPLOY_IDLE_TIMEOUT_DAYS :109 + extend ~:1233/:1296) — with the D-04a amendment recorded: under autoBuy-HIGHEST-priority (RD-1), while subscriber buys pend the router's rewarded advance leg is blocked, so first-30-min advance during a buy backlog relies on these bypass tiers, NOT the router bounty; re-homing the bounty adds NO single-point liveness risk"
    - "The Claude's-Discretion attestations are resolved by grep at 0cc5d10f: the advanceGame return-encoding decode site (DegenerusGame.sol:275 wrapper, currently returns nothing → the (uint8 mult, bool rewardable) tuple decode is the IMPL producer edit); the discovery-view predicate forms (advanceDue() = currentDayView():462 != dailyIdx OR mid-day partial-drain; boxesPending() rngLock-aware per RD-3 covering mid-day-resolved rounds; buys-pending true even during rngLock per RD-2 via AfKing-local cursor); the v48 KEEP-04 bytes32(\"DGNRS\") two-tier affiliate passthrough surviving the _autoBuy refactor (the autoBuy → IGame.batchPurchase call site, ROUTER-05 pre-condition); the GASOPT-01 hoist sites (MintModule processFutureTicketBatch :393 + processTicketBatch :670, ticketsOwedPacked[rk][player] loop-invariant)"
    - "D-08 (the 3 new keeper-gas DOs → GASOPT-03/04/05) baseline is attested at 0cc5d10f: GASOPT-03 (batched keeper read collapsing per-player claimableWinningsOf STATICCALLs :691/:722 — NEW batchPurchaseForKeeper/keeperSnapshot, SUBSUMES GASOPT-02); GASOPT-04 (the per-player AutoBought event decl :171 + emit :785, with the test-oracle migration noted — keccak256(\"AutoBought(address,uint32,uint256)\") is the no-double-buy oracle across AfKingConcurrency/Subscription/FundingWaterfall + SweepPerPlayerWorstCaseGas; lastAutoBoughtDay storage stamp :81/:784 is the replacement); GASOPT-05 (the per-iteration isOperatorApproved(player, AfKing) check :676 to REMOVE, with the subscribe-time isOperatorApproved(fundingSource, subscriber) :401 confirmed KEEP — the BLOCKING-CONDITION that 333 SWEEP re-attests the 4 OPEN-E protections without :676 recorded)"
  artifacts:
    - path: ".planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md"
      provides: "Per-anchor grep tables (vs 0cc5d10f) for the REDESIGNED router + advance surface: the RD-1..RD-5 deletion/keep/rework surface, the D-07 dead-epoch/dead-gas-units deletion surface, the RD-4 single-creditFlip unification (the 3 pull-out sites classified), the D-08 GASOPT-03/04/05 baselines, the per-leg + single-creditFlip no-untrusted-ETH-send attestation (D-01a), the GAS-03 satisfied-by-deletion verdict (D-03/D-07), the invariant-(c) D-04a fallback-caller re-attestation, the totalFlipReversals freeze (ADV-04), and the GASOPT-01 hoist sites"
      contains: "Verdict legend"
  key_links:
    - from: "the held-330/redesign-doc cited file:line anchors for the router/advance surface"
      to: "FROZEN baseline contracts/ source at HEAD 0cc5d10f"
      via: "git show 0cc5d10f:contracts/<path> | grep -n (NOT the dirty working tree)"
      pattern: "MATCH|SHIFTED|ABSENT"
    - from: "each doWork leg (advance / autoOpen / _autoBuy) + the single unified doWork creditFlip"
      to: "claimableWinnings pull ledger + ContractAddresses.* pinned sends only"
      via: "per-leg + single-creditFlip no-untrusted-ETH-send grep attestation (D-01a, re-grounded on the unified model)"
      pattern: "claimableWinnings|ContractAddresses\\.|creditFlip"
---

<objective>
Grep-attest every cited `file:line` anchor on the v49.0 **REDESIGNED router + advance-bounty** surface
against the FROZEN v48.0-closure HEAD `0cc5d10fbc1232a6d2e7b0464fe21541b9812029` (`0cc5d10f`),
recording per-anchor MATCH / SHIFTED(±N) / ABSENT verdicts in `329-ATTEST-ROUTER-ADVANCE.md`. This is
the keeper-router-REDESIGN re-attestation (the original 329-01 attested the SUPERSEDED
advance→open→buy / `doWork(maxCount)` / dual-epoch-single-source design). Resolve the redesign's
locked changes + dispositions on this surface:

- **RD-1 / RD-2 / RD-3 / RD-4 / RD-5** — the 5 locked router changes: autoBuy-first order, the dropped
  rngLock guards (incl. the Q5 `batchPurchase:1737` dependent grep), the autoOpen rngLock-block +
  try/catch-drop + entry-gate (the `_queueTickets` liveness basis), and the unified-bounty-into-doWork
  pull-out (the 3 in-callee `creditFlip` sites).
- **D-07** — the flat per-tx model's DELETION surface: the dead AfKing autoBuy stall ladder + absolute
  epoch and the dead autoOpen gas-units machinery.
- **GAS-03 / D-03** — now SATISFIED BY DELETION (advance is the sole stall epoch; no dual epoch to
  collapse).
- **ROUTER-07 / D-01a** — the per-leg + single-unified-`creditFlip` "no untrusted ETH send" no-guard
  basis (re-grounded on RD-4's single `creditFlip`).
- **ADV-04 (invariant b)** — the `totalFlipReversals` VRF-window nudge frozen request→consume; the
  redesigned (autoBuy-ungated-at-day-open) router adds no new in-window SLOAD.
- **Invariant (c) / D-04a** — the existing structural free-fallback `advanceGame()` callers under
  autoBuy-highest-priority.
- **D-08** — the GASOPT-03/04/05 baselines (batched keeper read / dropped `AutoBought` event +
  test-oracle migration / dropped per-iteration `isOperatorApproved`).

**CRITICAL — attest against the FROZEN HEAD, not the dirty tree.** The working tree's `contracts/*.sol`
is DIRTY (it carries the superseded held-330 diff). All verdicts MUST be resolved via
`git show 0cc5d10f:contracts/<path> | grep -n`. The held-tree / redesign-doc line numbers are drift to
RECORD, not the source of truth (`feedback_contract_locations`, `feedback_verify_call_graph_against_source`).

Purpose: produce the load-bearing call-graph evidence so Plan 03's reconciliation + Phase 330's
re-IMPL diff carry zero un-grepped "by construction" assumptions. This is the BATCH-01 attestation half
for the core REDESIGNED router/advance surface.

Output: one `329-ATTEST-ROUTER-ADVANCE.md` under the phase directory. ZERO `contracts/*.sol` mutation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-CONTEXT.md

# The binding re-SPEC input — the 5 locked changes + Q1/Q2/Q5 resolutions + the autoBuy-gas analysis (GASOPT-03/04/05 source):
@.planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-ROUTER-REDESIGN-INTENT.md

# The locked milestone design (no research sub-phase):
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/PLAN-CRANK-DO-WORK-INCENTIVE.md
@.planning/PLAN-V48-KEEPER-RENAME-AND-VAULT-CODE.md

# The v48 ATTEST format to mirror (verdict legend + per-anchor table + exact-body excerpt + Roll-up):
@.planning/milestones/v48.0-phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-KEEP-POOL.md

<attestation_method>
**The working tree's `contracts/*.sol` is DIRTY — it carries the superseded held-330 diff** (verified
during planning: `git status --porcelain -- 'contracts/*.sol'` lists AfKing.sol, DegenerusGame.sol,
both interfaces, AdvanceModule.sol, MintModule.sol as modified). The COMMITTED HEAD tree is
byte-identical to the baseline `0cc5d10f` (`git diff --name-only 0cc5d10f HEAD -- 'contracts/*.sol'`
is EMPTY). Therefore **every grep MUST be against the frozen blob**: `git show
0cc5d10f:contracts/<path> | grep -n`. Do NOT `grep` the live working tree for line numbers — it would
report held-330 lines, not baseline lines. Read source from `contracts/` ONLY (ignore stale copies
elsewhere — `feedback_contract_locations`). The doc header MUST state this dirty-tree /
grep-against-0cc5d10f fact so the verdicts are baseline-anchored.

Verdict legend (mirror 325-ATTEST): `MATCH` (anchor lands on the claimed line at 0cc5d10f) ·
`SHIFTED(±N)` (content present, N lines off — record the actual 0cc5d10f line) · `ABSENT` (content not
found / materially diverged — flag as an IMPL blocker in the Roll-up). For held-tree/redesign-doc
anchors, record BOTH the held line (the cited drift) AND the 0cc5d10f line (the truth).
</attestation_method>

<interfaces>
<!-- Grep-confirmed anchors at the FROZEN baseline 0cc5d10f (verified during planning via `git show 0cc5d10f:...`). The executor RE-VERIFIES + extends; these are the truth, the held-tree lines are drift. -->
AfKing.sol @ 0cc5d10f (the router's home; NO doWork/_autoBuy/sweep exist yet — the redesign authors them):
  :100      CEI invariant comment ("No reentrancy guard — strict CEI everywhere; the keeper is never a payee in any contract it calls")
  :171      `event AutoBought(address indexed player, uint32 day, uint256 cost);` (GASOPT-04 DROP target; decl) ; emit at :785 ; held-tree decl :186 / emit :954
  :81       `uint32 lastAutoBoughtDay;` (Sub field — the GASOPT-04 no-double-buy oracle replacement) ; day-stamp write :784
  :263      `uint256 public immutable BOUNTY_ETH_TARGET;` (peg target; ctor :279) — re-pegged at GAS, placeholder at SPEC
  :401      subscribe-time `if (!IGame(GAME).isOperatorApproved(fundingSource, subscriber))` (GASOPT-05 KEEP this one)
  :567      `function autoBuy(uint256 maxCount) external returns (uint256 bountyEarned)` (refactored to internal `_autoBuy` at IMPL); entry rngLock guard `:568` `if (IGame(GAME).rngLocked()) revert AutoBuyAborted(msg.sender, 1);` (RD-2 REMOVE — held-tree :717)
  :676      per-iteration `if (!IGame(GAME).isOperatorApproved(player, address(this)))` (GASOPT-05 REMOVE — held-tree :838)
  :691 + :722  two `IGame(GAME).claimableWinningsOf(player)` calls inside the buy loop (GASOPT-02 hoist sites / GASOPT-03 batched-read targets — held-tree :854/:888)
  :823-846  the autoBuy stall ladder + ABSOLUTE-day epoch (`dayStart = uint256(today) * 1 days + 82_620` :829, bands 2h→6x/1h→4x/20m→2x :831-836) — D-07 DEAD-after-redesign (only advance escalates)
  :845      `bountyEarned = batchLen * ((BOUNTY_ETH_TARGET * PRICE_COIN_UNIT * bountyMultiplier) / mp);` (the per-tx bounty — RD-4 moves the creditFlip into doWork; D-07 flattens)
  :846      `ICoinflip(COINFLIP).creditFlip(msg.sender, bountyEarned);` (the autoBuy in-callee creditFlip — RD-4 PULL OUT into doWork)
  :887      `function _currentDay()` `uint32((block.timestamp - 82620) / 1 days)`
  errors: `AutoBuyAborted` / `NoSubscribersAutoBought()` :146 (the ROUTER-06 NoWork() revert-idiom precedent)
  affiliate: the keeper buy fires `IGame(GAME).batchPurchase{value:...}(players, amounts, modes)` (AfKing batchPurchase has NO affiliate arg; the bytes32("DGNRS") code is wired game-side at DegenerusGame.sol:1778 per v48 KEEP-04 — confirm it survives the _autoBuy refactor, ROUTER-05)

DegenerusGameAdvanceModule.sol @ 0cc5d10f (the advance multiplier single-source-of-truth — KEPT as the SOLE stall epoch):
  :109      `DEPLOY_IDLE_TIMEOUT_DAYS = 365` (L0); comment "level 1+ uses hardcoded 120 days" (tertiary death-clock, invariant c)
  :147      `uint256 private constant ADVANCE_BOUNTY_ETH = 0.005 ether;` (deleted at IMPL per ADV-01)
  :155      `function advanceGame() external` (gains design-1 return `(uint8 mult, bool rewardable)` at IMPL)
  :189      creditFlip site #1 (new-day advance bounty) — `(ADVANCE_BOUNTY_ETH * PRICE_COIN_UNIT) / ...` :191 (RD-4 pull out + ADV-01 delete)
  :225      creditFlip site #2 (MID-DAY partial-drain bounty — ADV-05 rewardable advance-leg, mid-day mult=1 per D-07) — :227
  :241-253  the KEPT stall multiplier block — `(uint256(day-1) + DEPLOY_DAY_BOUNDARY) * 1 days + 82_620` (:244-246, GAME-DAY epoch), bands 2h→6x/1h→4x/20m→2x :248-253 — NEW-DAY path only
  :468      creditFlip site #3 — `(ADVANCE_BOUNTY_ETH * PRICE_COIN_UNIT * bountyMultiplier) / ...` :470 (RD-4 pull out + ADV-01 delete)
  :876      creditFlip (gameover-RNG-fallback path) — classify NOT-an-advance-leg-reward (rewardable=true gameover path? confirm)
  :270      `cw += totalFlipReversals;` (an in-window read of the nudge — enumerate per rng_window_storage_read_freshness)
  :1012     `if (elapsed >= 30 minutes) return;` (the UNCONDITIONAL 30-min universal bypass — invariant c; inside _enforceDailyMintGate :989)
  :1233 / :1296  death-clock / gameover-RNG-fallback stall extends (invariant c tertiary)
  :1834     `function _applyDailyRng(` ; :1838 `uint256 nudges = totalFlipReversals;` ; :1844 `totalFlipReversals = 0;` (the ADV-04 player-controllable VRF-window nudge)

DegenerusGame.sol @ 0cc5d10f:
  :275      `function advanceGame() external` wrapper — delegatecalls GAME_ADVANCE_MODULE, `if (!ok) _revertDelegate(data);` (returns NOTHING today; gains the design-1 (uint8,bool) decode at IMPL)
  :462      `function currentDayView() external view returns (uint32)` (advanceDue() new-day predicate basis)
  :1539     `AUTO_GAS_PRICE_REF = 0.5 gwei` ; :1545 `AUTO_RESOLVE_BET_GAS_UNITS = 66_528` ; :1546 `AUTO_OPEN_BOX_GAS_UNITS = 71_203` (open-leg gas-units — D-07 DEAD-after-unification; the open conversion :1666)
  :1587     `function autoResolve(...)` (degeneretteResolve rename — D-05, attested in 329-02; here only as a co-located anchor)
  :1636     `function autoOpen(uint256 maxCount) external` (the open leg) ; :1651 `cursor = boxCursor` ; :1656 `while (cursor < qlen && opened < maxCount)` ; :1659 `++cursor` (BEFORE the try/catch) ; :1664 `try this._autoOpenBox(index, player)` ; :1672 `catch {}` ; :1675 `boxCursor = uint48(cursor)` ; :1676 in-callee `creditFlip(msg.sender, reward)` (RD-4 pull out) — held-tree :1701/1707/1719
  :1705     `function _autoOpenBox(uint48 index, address player) external` — onlySelf (`if (msg.sender != address(this)) revert E();`) → `_openLootBoxFor(player, index)` (RD-5 make INTERNAL, drop this./try-catch) ; _openLootBoxFor :729
  :1731     `function batchPurchase(...)` ; :1737 `if (rngLockedFlag) revert RngLocked();` (RD-2 REMOVE — held-tree :1693) ; :1738 `if (gameOver) revert E();` (RD-2 KEEP — held-tree :1694)
  :2413     `function rngLocked() external view returns (bool)` (the freeze gate)
  boxesPending discovery basis: grep `boxPlayers` / `boxCursor` / `lootboxRngWordByIndex[activeIndex]` (the O(1) predicate; RD-3 makes it rngLock-aware AND keeps mid-day-resolved rounds openable)

Open-path liveness revert (RD-5 entry-gate basis):
  contracts/storage/DegenerusGameStorage.sol:560 `function _queueTickets(...)` ; :571 `if (_livenessTriggered()) revert E();` (the DELIBERATE terminal-jackpot control — KEEP for direct openLootBox) ; _livenessTriggered :1213. NOTE the file is `contracts/storage/DegenerusGameStorage.sol` (CONTEXT/redesign wrote bare "DegenerusGameStorage:571" — :571 is CORRECT, the path prefix is `storage/`).

Invariant-(c) fallback wrappers (read-only — confirm, do NOT modify):
  DegenerusVault.sol:527 `function gameAdvance() external onlyVaultOwner`
  StakedDegenerusStonk.sol:421 `function gameAdvance() external` (permissionless)

DegenerusGameMintModule.sol (GASOPT-01 hoist sites):
  :393      `function processFutureTicketBatch(` — repeated `ticketsOwedPacked[rk][player]` reads (:431/:441/:453/:463/:498) — `rk` loop-invariant (CONTEXT held cited :398)
  :670      `function processTicketBatch(uint24 lvl) external returns (bool finished)` — repeated `ticketsOwedPacked[rk][player]` (:741/:748/:754) — `rk` loop-invariant (CONTEXT held cited :671)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attest the AfKing router surface under the redesign (RD-1/RD-2 + D-07 dead-epoch + GASOPT-03/04/05 baselines + GASOPT-01 + the single-creditFlip no-untrusted-ETH-send) → 329-ATTEST-ROUTER-ADVANCE.md sections A/B/C</name>
  <read_first>
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-01-PLAN.md (this plan — the `<interfaces>` block + attestation_method)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-CONTEXT.md (RD-1/RD-2; D-07 deletion surface; D-08 GASOPT-03/04/05; D-01/D-01a re-grounded on the unified creditFlip)
    - .planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-ROUTER-REDESIGN-INTENT.md ("Locked design changes" 1/2 + the "autoBuy gas" analysis = the GASOPT-03/04/05 source + Q1/Q5 resolutions)
    - .planning/milestones/v48.0-phases/325-spec-design-lock-call-graph-attestation-shared-surface-recon/325-ATTEST-KEEP-POOL.md (the table + exact-body-excerpt + Roll-up format to mirror)
    - .planning/research/PITFALLS.md (Pitfall 6 reentrancy / OPEN-C; Pitfall 1 faucet locks; Pitfall 5 O(1) discovery)
    - .planning/PLAN-V48-KEEPER-RENAME-AND-VAULT-CODE.md (the v48 KEEP-04 bytes32("DGNRS") two-tier 75/20/5 affiliate wiring — the ROUTER-05 passthrough survival pre-condition)
    - `git show 0cc5d10f:contracts/AfKing.sol` (CEI :100; AutoBought :171/:785; lastAutoBoughtDay :81/:784; BOUNTY_ETH_TARGET :263; subscribe-time isOperatorApproved :401; autoBuy :567 + entry rngLock guard :568; per-iteration isOperatorApproved :676; claimableWinningsOf :691/:722; stall ladder + absolute epoch :823-846; bounty :845 + creditFlip :846; _currentDay :887; the batchPurchase keeper-buy call site + the absence of an affiliate arg)
    - `git show 0cc5d10f:contracts/DegenerusGame.sol` (batchPurchase :1731 + rngLock pre-check :1737 + gameOver :1738 — the RD-2 game-side guard for Q5; the bytes32("DGNRS") affiliate wiring at :1778)
    - `git show 0cc5d10f:contracts/modules/DegenerusGameMintModule.sol` (GASOPT-01: processFutureTicketBatch :393 + processTicketBatch :670 + the repeated ticketsOwedPacked[rk][player] reads)
  </read_first>
  <action>Create 329-ATTEST-ROUTER-ADVANCE.md mirroring 325-ATTEST-KEEP-POOL.md: a `## Scope` + `## Sources of truth` header naming the exact `contracts/` files + the DIRTY-tree / grep-against-`git show 0cc5d10f` attestation-method note, a `## Verdict legend`, then the following sections (each a `## Anchor reconciliation table` with columns `# | Anchor (held-tree/redesign-doc claim) | ACTUAL @ 0cc5d10f (git show 0cc5d10f:contracts/<file>) | Verdict`).
Section A — AfKing router anchors + RD-2 (drop rngLock guard) + D-07 (dead epoch): grep-verify each AfKing anchor in the `<interfaces>` block against `git show 0cc5d10f:contracts/AfKing.sol` (CEI :100, AutoBought decl :171/emit :785, lastAutoBoughtDay :81/:784, BOUNTY_ETH_TARGET :263, subscribe-time isOperatorApproved :401 KEEP, autoBuy :567 + the entry rngLock guard :568 RD-2-REMOVE, per-iteration isOperatorApproved :676 GASOPT-05-REMOVE, claimableWinningsOf :691/:722, the stall ladder + absolute-day epoch :823-846 D-07-DEAD, bounty :845 + creditFlip :846 RD-4-PULL-OUT, _currentDay :887). Record the held-tree line as the "claim" and the 0cc5d10f line as the ACTUAL with an explicit drift note for every shifted anchor (held :717→:568, :838→:676, :854/:888→:691/:722, :954→:785, :186→:171). Add a KEEP-04 affiliate-passthrough row: grep-confirm the autoBuy keeper buy fires IGame.batchPurchase (no affiliate arg) and the bytes32("DGNRS") code is wired game-side at DegenerusGame.sol:1778 — give an explicit survives-the-_autoBuy-refactor verdict (ROUTER-05).
Section B — RD-2 game-side guard + Q5 dependent grep + ROUTER-07 / D-01a per-leg + single-creditFlip no-untrusted-ETH-send: (1) grep-locate the batchPurchase rngLock pre-check (DegenerusGame.sol:1737, RD-2 REMOVE) and the gameOver pre-check (:1738, KEEP); (2) PERFORM the Q5 dependent grep — enumerate every batchPurchase caller (`git show 0cc5d10f:contracts/... | grep -rn "batchPurchase"` across contracts + interfaces) and record the verdict on whether removing the game-side :1737 rngLock pre-check affects any caller besides the keeper (it gates ALL batchPurchase callers); (3) a table with ONE row per doWork leg (advance / autoOpen / _autoBuy) PLUS one row for the single unified doWork creditFlip, each grep-shown to send ETH only to ContractAddresses.* pinned contracts + route player value through claimableWinnings (NOT a push to msg.sender / untrusted address), plus a row confirming the bounty pays as creditFlip flip-credit to the player (keeper-never-a-payee) and the unified model fires exactly ONE creditFlip LAST (CEI). State the formal no-guard basis verbatim: *keeper-never-a-payee + no untrusted ETH send + one-category structural early-return + single-creditFlip-last CEI ordering*. If ANY leg pushes ETH to an untrusted address, flag a ROUTER-07 BLOCKER.
Section C — D-07 deletion surface + GAS-03 + GASOPT-03/04/05 baselines + GASOPT-01: (1) a D-07 dead-surface table — the AfKing autoBuy stall ladder + absolute epoch (:823-846, :829) classified DEAD-after-redesign, with the GAS-03 verdict recorded = SATISFIED BY DELETION (only advance escalates → no second stall epoch → no two epochs to collapse; D-03 dissolved by D-07); (2) the GASOPT-03 baseline row (claimableWinningsOf STATICCALLs :691/:722 → NEW batched batchPurchaseForKeeper/keeperSnapshot; SUBSUMES GASOPT-02 — note); (3) the GASOPT-04 baseline row (AutoBought decl :171 + emit :785, the test-oracle migration → lastAutoBoughtDay :81/:784, the test files keyed on keccak256("AutoBought(address,uint32,uint256)") enumerated: AfKingConcurrency.t.sol / AfKingSubscription.t.sol / AfKingFundingWaterfall.t.sol / SweepPerPlayerWorstCaseGas.t.sol, and the no-double-buy `_countAutoBoughtFor(sub)==1` invariant noted as the migration's hardest case); (4) the GASOPT-05 baseline row (per-iteration isOperatorApproved(player, AfKing) :676 REMOVE + subscribe-time isOperatorApproved(fundingSource, subscriber) :401 KEEP + the 333-SWEEP-re-attests-the-4-OPEN-E-protections BLOCKING-CONDITION); (5) two GASOPT-01 rows — processFutureTicketBatch (:393) + processTicketBatch (:670), `rk` loop-invariant, behavior-identical gas-only, CONTEXT held-line drift noted. NO contracts/*.sol edit.</action>
  <verify>
    <automated>bash -c 'F=.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md; test -f "$F" && grep -q "Verdict legend" "$F" && grep -Eqi "MATCH|SHIFTED|ABSENT" "$F" && grep -q "0cc5d10f" "$F" && grep -q "creditFlip" "$F" && grep -q "claimableWinnings" "$F" && grep -q "batchPurchase" "$F" && grep -Eq "1737" "$F" && grep -q "AutoBought" "$F" && grep -q "isOperatorApproved" "$F" && grep -q "ticketsOwedPacked" "$F" && grep -Eqi "satisfied by deletion|sole stall epoch" "$F"'</automated>
  </verify>
  <acceptance_criteria>
    - 329-ATTEST-ROUTER-ADVANCE.md exists with a Verdict legend (MATCH/SHIFTED/ABSENT) and a header note that the working tree is DIRTY (held-330 diff) so every grep is against `git show 0cc5d10f`.
    - Section A has a grep-verified verdict row (with held-tree-vs-0cc5d10f drift recorded) for every AfKing anchor in the interfaces block, PLUS the KEEP-04 affiliate-passthrough-survives-the-_autoBuy-refactor verdict.
    - Section B locates the RD-2 game-side guard (:1737 remove / :1738 keep), PERFORMS the Q5 dependent batchPurchase-caller grep with a verdict, and has one no-untrusted-ETH-send row PER doWork leg (advance/autoOpen/_autoBuy) + one for the single unified doWork creditFlip; the formal no-guard basis sentence is recorded verbatim; any untrusted-push leg is flagged a ROUTER-07 blocker.
    - Section C records the D-07 dead-epoch deletion surface (:823-846), the GAS-03 SATISFIED-BY-DELETION verdict, and the GASOPT-03/04/05 baselines (with the GASOPT-04 test-oracle file list + the no-double-buy invariant note, and the GASOPT-05 333-SWEEP blocking-condition) + the two GASOPT-01 hoist rows.
    - No contracts/*.sol modified.
  </acceptance_criteria>
  <done>329-ATTEST-ROUTER-ADVANCE.md sections A/B/C committed: the AfKing redesign surface (RD-2 dropped guards, D-07 dead epoch, GASOPT-03/04/05 baselines, GASOPT-01) grep-attested vs 0cc5d10f, the Q5 dependent grep performed, the single-creditFlip no-guard basis (D-01a) attested, GAS-03 SATISFIED-BY-DELETION; zero .sol mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Attest the advance + autoOpen redesign (RD-1 order / RD-3 rngLock-block + RD-5 try/catch-drop + entry-gate / RD-4 unified-bounty 3-site pull-out / ADV-04 freeze / invariant-(c) D-04a) → 329-ATTEST-ROUTER-ADVANCE.md sections D/E/F/G + Roll-up</name>
  <read_first>
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-01-PLAN.md (this plan — the `<interfaces>` block)
    - .planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-CONTEXT.md (RD-1/RD-3/RD-4/RD-5; ADV-04; D-04/D-04a amended; the advanceGame return-encoding + discovery-view Discretion items)
    - .planning/phases/330-impl-the-one-batched-contract-diff-router-advance-rework-mic/330-ROUTER-REDESIGN-INTENT.md ("Locked design changes" 1/3/4 + the Q2-RE-RESOLVED try/catch-drop + entry-gate trace)
    - .planning/research/PITFALLS.md (Pitfall 3 advance-timing/VRF-freeze + totalFlipReversals; Pitfall 8 unrewarded-advance liveness; Pitfall 5 O(1) discovery)
    - `git show 0cc5d10f:contracts/modules/DegenerusGameAdvanceModule.sol` (advanceGame :155; ADVANCE_BOUNTY_ETH :147; the 3 creditFlip sites :189 new-day / :225 mid-day / :468 + bounty math :191/:227/:470; the gameover-RNG creditFlip :876; stall+game-day epoch :241-253; _enforceDailyMintGate :989 + 30-min bypass :1012; death-clock :109 + extends :1233/:1296; _applyDailyRng :1834 + totalFlipReversals :1838/:1844 + the :270 in-window read — backward-trace per feedback_rng_backward_trace)
    - `git show 0cc5d10f:contracts/DegenerusGame.sol` (advanceGame wrapper :275 + _revertDelegate; currentDayView :462; rngLocked :2413; autoOpen :1636 + ++cursor :1659 + try this._autoOpenBox :1664 + catch :1672 + boxCursor :1675 + in-callee creditFlip :1676; _autoOpenBox :1705 onlySelf + _openLootBoxFor :729; the boxPlayers/boxCursor/lootboxRngWordByIndex discovery state)
    - `git show 0cc5d10f:contracts/storage/DegenerusGameStorage.sol` (_queueTickets :560 + the liveness revert :571 + _livenessTriggered :1213 — the RD-5 entry-gate basis)
    - `git show 0cc5d10f:contracts/DegenerusVault.sol` (gameAdvance :527 onlyVaultOwner — read-only) ; `git show 0cc5d10f:contracts/StakedDegenerusStonk.sol` (gameAdvance :421 permissionless — read-only)
  </read_first>
  <action>Append sections D, E, F, G to 329-ATTEST-ROUTER-ADVANCE.md (same legend + table format, all grepped via `git show 0cc5d10f`).
Section D — advance anchors + RD-4 unified-bounty 3-site pull-out + the design-1 return: grep-verify advanceGame :155, ADVANCE_BOUNTY_ETH :147, the KEPT stall+game-day epoch block :241-253, and the DegenerusGame advanceGame wrapper :275 (the delegatecall + `if (!ok) _revertDelegate(data)` — note it returns NOTHING today; the design-1 (uint8 mult, bool rewardable) return + the decode of `data` is the IMPL producer edit; record the exact wrapper decode site). Then the RD-4 unification table — ONE row per creditFlip site the unified model pulls out: AdvanceModule :189 (new-day advance — REWARDABLE advance-leg), :225 (MID-DAY partial-drain — REWARDABLE per ADV-05, mult=1 per D-07), :468 (classify the firing condition — REWARDABLE vs not), DegenerusGame autoOpen :1676 (the open-leg in-callee creditFlip — pulled into doWork), AfKing autoBuy :846 (the buy-leg in-callee creditFlip — pulled into doWork), plus AdvanceModule :876 (gameover-RNG-fallback — classify NOT-an-advance-leg-reward). Record the unified verdict: after the rework there is exactly ONE creditFlip site (in doWork), legs return raw counts/mult and never self-credit (this is the re-grounding that strengthens D-01). Confirm deleting/pulling the advance trio leaves advanceGame functional + unrewarded standalone (ADV-01/ADV-03). Give the explicit (uint8 mult, bool rewardable) distinct-bool tuple verdict + the mid-day mult=1 rule (D-07: no escalation for mid-day partial-drain).
Section E — RD-3/RD-5 autoOpen rngLock-block + try/catch-drop + entry-gate: grep-show the autoOpen current shape (++cursor :1659 BEFORE try this._autoOpenBox :1664 / catch :1672 / boxCursor :1675 — the stranded-tail hazard if try/catch drops without an entry-gate) and that _autoOpenBox :1705 is external onlySelf (`if (msg.sender != address(this)) revert E();` → _openLootBoxFor :729 → ... → _queueTickets DegenerusGameStorage:560). Attest the EXACTLY-TWO open-path revert sources: (1) rngLock (DegenerusGame.sol:1737 game-side + rngLocked :2413) and (2) the DELIBERATE terminal-jackpot control `_queueTickets → if(_livenessTriggered()) revert E()` at contracts/storage/DegenerusGameStorage.sol:571 (_livenessTriggered :1213) — note the file path is `storage/DegenerusGameStorage.sol` (CONTEXT wrote the bare name; :571 is CORRECT). Record the verdict that the redesign's entry-gate `if (rngLocked() || _livenessTriggered()) return; ... _autoOpenBox(index, player) /* INTERNAL */` replicates both excluded sources pre-loop (brick-proof, no marooning) and that the terminal-jackpot guard stays intact for DIRECT openLootBox callers; record the USER-accepted trade (the entry-gate trusts these are the ONLY revert sources forever — frozen contract). Add a boxesPending() rngLock-awareness row (RD-3): the predicate returns false during rngLock but STILL opens mid-day-resolved boxes when a round's word has landed and we are not locked.
Section F — ADV-04 (invariant b) totalFlipReversals freeze + ROUTER-04 O(1) discovery views: (1) grep-show _applyDailyRng :1834 reads totalFlipReversals (:1838) into the consumed word + resets (:1844), and enumerate the additional in-window read :270 (`cw += totalFlipReversals`) per feedback_rng_window_storage_read_freshness — record that the protected freeze window is rng-request→consume, and (the redesign-relevant fact) under RD-1 autoBuy runs at day-open BEFORE the advance leg requests the word (rngLock false), so autoBuy is structurally pre-entropy and the REDESIGNED router introduces NO new mutable in-window SLOAD into the advance-consume (advance consumes via the design-1 return, recomputes nothing); the empirical freeze proof is TST-01's job. (2) A discovery-view table — advanceDue() (currentDayView():462 != dailyIdx OR mid-day partial-drain), boxesPending() (rngLock-aware per RD-3, covering mid-day-resolved rounds: `boxPlayers[activeIndex].length > boxCursor AND lootboxRngWordByIndex[activeIndex] != 0 AND !rngLocked`), buys-pending (AfKing-local cursor — TRUE even during rngLock per RD-2, since autoBuy no longer aborts on rngLock) — each O(1), no unbounded scan (Pitfall 5); settle WHERE each lives (advance/boxes on DegenerusGame, buys on AfKing-local).
Section G — invariant (c) / D-04a free-fallback callers: a table with one row per structurally-guaranteed unrewarded advanceGame() caller path — the 30-min universal bypass (AdvanceModule :1012), DegenerusVault.gameAdvance() :527, StakedDegenerusStonk.gameAdvance() :421, the 120-day death-clock (:109 + extend :1233/:1296) — with the D-04a amendment recorded: under autoBuy-HIGHEST-priority (RD-1) the router's REWARDED advance leg is blocked while subscriber buys pend (buys drain monotonically, finite set, cursor advances each call), so first-30-min advance during a buy backlog relies on these bypass tiers NOT the router bounty; re-homing the bounty removes NO structural caller, only moves the payment. End with a `## Roll-up` line: the total IMPL-blocker count across all sections, the Q5-no-other-dependent verdict, the ROUTER-07 no-guard-basis-holds verdict, the GAS-03 satisfied-by-deletion verdict, the ADV-04 no-new-in-window-read verdict, the RD-5 entry-gate-replicates-both-revert-sources verdict, and the invariant-(c) D-04a fallback-callers-intact verdict. NO contracts/*.sol edit.</action>
  <verify>
    <automated>bash -c 'F=.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-ATTEST-ROUTER-ADVANCE.md; test -f "$F" && grep -q "totalFlipReversals" "$F" && grep -q ":189" "$F" && grep -q ":225" "$F" && grep -q ":468" "$F" && grep -q "1676" "$F" && grep -Eqi "DegenerusGameStorage.*571|:571" "$F" && grep -Eqi "_livenessTriggered|liveness" "$F" && grep -Eqi "advanceDue|boxesPending|buys.pending" "$F" && grep -q "gameAdvance" "$F" && grep -Eqi "30 minutes|death.clock|120" "$F" && grep -Eqi "rewardable" "$F" && grep -q "entry-gate" "$F" && grep -q "Roll-up" "$F"'</automated>
  </verify>
  <acceptance_criteria>
    - Section D attests advanceGame :155, ADVANCE_BOUNTY_ETH :147, the KEPT stall+game-day epoch :241-253, the DegenerusGame :275 wrapper decode site, AND the RD-4 unification table classifying ALL pull-out creditFlip sites (advance :189/:225/:468 + autoOpen :1676 + autoBuy :846 + the gameover-RNG :876) with the single-creditFlip-in-doWork verdict and the (uint8 mult, bool rewardable) distinct-bool + mid-day-mult=1 verdict.
    - Section E grep-shows the autoOpen ++cursor-before-try/catch hazard (:1659/:1664/:1672/:1675 + in-callee creditFlip :1676), that _autoOpenBox :1705 is onlySelf, and attests the EXACTLY-TWO revert sources (rngLock :1737/:2413 + the _queueTickets liveness revert at contracts/storage/DegenerusGameStorage.sol:571 / _livenessTriggered :1213) with the entry-gate-replicates-both verdict + the boxesPending rngLock-aware (RD-3) row + the USER-accepted-trade note.
    - Section F grep-shows totalFlipReversals read :1838 + reset :1844 inside _applyDailyRng :1834 + enumerates the :270 in-window read, records the request→consume window + the autoBuy-runs-pre-entropy-at-day-open / no-new-in-window-SLOAD verdict (TST-01 handoff), and has an O(1) row per discovery view (advanceDue / boxesPending rngLock-aware / buys-pending TRUE-during-rngLock).
    - Section G has a verdict row per free-fallback caller (30-min :1012 / Vault :527 / sStonk :421 / 120-day death-clock :109/:1233/:1296) with the D-04a autoBuy-first amendment (rewarded advance blocked while buys pend → bypass tiers, not the bounty, cover first-30-min).
    - The final Roll-up states the blocker count + the Q5 / ROUTER-07 / GAS-03 / ADV-04 / RD-5-entry-gate / invariant-(c)-D-04a verdicts.
    - No contracts/*.sol modified.
  </acceptance_criteria>
  <done>329-ATTEST-ROUTER-ADVANCE.md sections D/E/F/G + Roll-up committed: the advance/autoOpen redesign (RD-1/3/4/5), the 3-site→1 creditFlip unification, the design-1 return, the RD-5 entry-gate basis (_queueTickets liveness :571), ADV-04 freeze under the new order, the rngLock-aware discovery views, and the D-04a invariant-(c) fallbacks all grep-attested vs 0cc5d10f; zero .sol mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a paper-only attestation plan — ZERO code mutation this phase; the mitigations below are LOCKED
in the ATTEST evidence for the re-IMPL (Phase 330) diff to apply, and proven empirically at TST (332) /
TERMINAL (333). The "trust boundary" here is between the redesign docs' *claimed* `file:line` anchors +
design assumptions and the *actual* FROZEN baseline source — an under-attested anchor, a grep against
the DIRTY held-330 tree instead of `0cc5d10f`, or an un-grepped "by construction" claim lets a stale
reference or a false security disposition reach Phase 330's re-IMPL diff.

| Boundary | Description |
|----------|-------------|
| redesign-doc / held-tree anchor → frozen contracts/ source @ 0cc5d10f | The held tree is DIRTY; grepping it reports held-330 lines. Every verdict must come from `git show 0cc5d10f` |
| each doWork leg + the single unified creditFlip → ETH send target (pinned protocol vs untrusted) | The ROUTER-07 no-guard disposition (D-01) rests on "no untrusted ETH send" + "exactly one creditFlip" being checked facts |
| the dropped rngLock guards (RD-2) → orphan-index / freeze safety | Removing the AfKing :568 + game-side :1737 guards must be proven freeze-safe by construction (buy queues pre-entropy) and the Q5 grep must confirm no other batchPurchase caller relies on :1737 |
| the dropped autoOpen try/catch (RD-5) → the stranded-tail brick | Dropping try/catch without the entry-gate bricks the tail; the EXACTLY-TWO-revert-sources attestation is the entry-gate's correctness proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-329-01 | Tampering | A cited router/advance anchor is grepped against the DIRTY working tree (held-330 lines) instead of 0cc5d10f → the attestation rubber-stamps the wrong baseline | mitigate | The header note mandates `git show 0cc5d10f:contracts/...`; every verdict records BOTH the held line (drift) and the 0cc5d10f line (truth); ABSENT anchors flagged as IMPL blockers in the Roll-up |
| T-329-02 | Spoofing / Elevation (false security disposition) | ROUTER-07 no-guard (D-01) recorded without the per-leg + single-creditFlip grep proof, masking an untrusted ETH-send leg that would make the unified composition reentrant (Pitfall 6) | mitigate | Section B emits one no-untrusted-ETH-send row PER leg + one for the unified doWork creditFlip; any untrusted-push leg is flagged a ROUTER-07 blocker — the no-guard disposition rests on a checked fact + the empirical TST-02 double-pay regression (D-01b) |
| T-329-03 | Tampering (RNG / liveness) | RD-2 drops the rngLock guards but the Q5 grep is skipped → a non-keeper batchPurchase caller silently loses its freeze protection; OR the orphan-index safety claim survives un-grepped | mitigate | Section B PERFORMS the Q5 dependent batchPurchase-caller grep + records the orphan-defended-on-the-resolution-side basis; Section F confirms autoBuy runs pre-entropy at day-open so no new in-window read is introduced (ADV-04) |
| T-329-04 | Denial of service (brick) | RD-5 drops the autoOpen try/catch and the EXACTLY-TWO-revert-sources claim survives un-grepped → a third revert source bricks the tail | mitigate | Section E grep-attests the two revert sources (rngLock + the _queueTickets liveness control at storage/DegenerusGameStorage.sol:571) and records the entry-gate-replicates-both verdict + the USER-accepted frozen-contract trade |
| T-329-05 | Information disclosure (false-negative) | A "single fn reaches all paths" / "by construction" claim survives un-grepped (the DegenerusGame mint/jackpot inline-duplication precedent) | mitigate | Every design claim is decomposed to concrete 0cc5d10f anchors + grepped; the 6-site creditFlip unification + the GAS-03 satisfied-by-deletion + the invariant-(c) D-04a enumeration are explicit verdict rows |
| T-329-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this paper-only plan; no install task exists. RESEARCH Package Legitimacy Gate N/A |
</threat_model>

<verification>
- 329-ATTEST-ROUTER-ADVANCE.md exists with a Verdict legend and the DIRTY-tree / grep-against-0cc5d10f header note.
- Every router/advance anchor cited in the interfaces block has a verdict row (MATCH/SHIFTED(±N)/ABSENT) recording held-tree-vs-0cc5d10f drift; ABSENT rows surfaced as blockers in the Roll-up.
- The 5 locked changes are attested: RD-1 (order, via the discovery-view priority), RD-2 (dropped rngLock guards :568/:1737 + the Q5 caller grep), RD-3 (boxesPending rngLock-aware), RD-4 (the 6-site→1 creditFlip unification), RD-5 (try/catch-drop + the entry-gate basis at storage/DegenerusGameStorage.sol:571).
- ROUTER-07 (D-01a per-leg + single-creditFlip no-untrusted-ETH-send), GAS-03 (D-03/D-07 satisfied-by-deletion), ADV-04 (totalFlipReversals freeze + no new in-window read under the new order), invariant (c) (D-04a free-fallback callers under autoBuy-first), and the D-08 GASOPT-03/04/05 baselines are each resolved with explicit grep-derived verdicts.
- The Claude's-Discretion grep facts (design-1 return decode site, discovery views, KEEP-04 affiliate survival, GASOPT-01 hoist sites) are resolved.
- Zero contracts/*.sol files modified: `git status --porcelain -- 'contracts/*.sol'` is UNCHANGED from planning (the held-330 diff is pre-existing; this plan adds no new .sol delta).
</verification>

<success_criteria>
- The REDESIGNED router + advance surface anchor attestation is complete with grep-derived verdicts vs 0cc5d10f (BATCH-01 attestation half).
- RD-1..RD-5, D-07, GAS-03/D-03 (satisfied-by-deletion), ROUTER-07/D-01a, ADV-04, invariant-(c)/D-04a, and the D-08 GASOPT-03/04/05 baselines are resolved in writing.
- No "by construction" claim left un-grepped for this surface; the Q5 dependent grep is performed.
- ZERO contracts/*.sol mutation (paper-only phase; the pre-existing held-330 diff is untouched).
</success_criteria>

<output>
Create `.planning/phases/329-spec-design-lock-call-graph-attestation-4-structural-invaria/329-01-SUMMARY.md` when done, listing the ATTEST doc, the aggregate blocker count, the Q5 verdict, the ROUTER-07 / GAS-03(satisfied-by-deletion) / ADV-04 / invariant-(c)-D-04a verdicts, the RD-5 entry-gate-basis verdict, and the design-1-return / discovery-view / KEEP-04 / GASOPT-01 / GASOPT-03/04/05-baseline resolutions for Plan 03 to consume.
</output>
