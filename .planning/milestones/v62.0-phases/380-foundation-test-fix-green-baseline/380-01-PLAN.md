---
phase: 380-foundation-test-fix-green-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fuzz/StorageFoundation.t.sol
  - test/fuzz/VRFCore.t.sol
  - test/fuzz/VRFStallEdgeCases.t.sol
  - test/fuzz/VrfRotationLiveness.t.sol
  - test/fuzz/KeeperNonBrick.t.sol
  - test/fuzz/V56SubHardening.t.sol
  - test/fuzz/QueueDoubleBuffer.t.sol
  - test/fuzz/FarFutureIntegration.t.sol
  - test/fuzz/LootboxRngLifecycle.t.sol
  - test/fuzz/FarFutureSalvageSwap.t.sol
autonomous: true
requirements: [FOUND-01]

must_haves:
  truths:
    - "An authoritative c4d48008 storage-layout key exists, captured from forge inspect (not assumed)"
    - "Every named slot-hardcoded forge harness reads via a slot derived from the authoritative layout or a named getter — no stale slot literal remains"
    - "The named VRF/Keeper/Sub/Queue/FarFuture/LootboxRng suites pass (exit 0) at subject c4d48008"
  artifacts:
    - path: ".planning/phases/380-foundation-test-fix-green-baseline/380-01-LAYOUT-KEY.md"
      provides: "Authoritative c4d48008 DegenerusGame storage layout + per-harness recalibration ledger"
      min_lines: 30
    - path: "test/fuzz/StorageFoundation.t.sol"
      provides: "Slot-0 bit-offset + packed-pool slot assertions matching c4d48008"
  key_links:
    - from: "test harness vm.store/vm.load slot constants"
      to: "forge inspect DegenerusGame storageLayout (c4d48008)"
      via: "slot constant re-derivation"
      pattern: "forge inspect DegenerusGame storageLayout"
---

<objective>
Repair the stale-fixture / storage-layout-drift forge failures (FOUND-01): garbage-slot reads
(signatures `188753807911851` / `171`), `runs:0` setup reverts, and `Panic(0x11)` underflows
caused by harnesses that hard-code DegenerusGame storage slots pinned at an old layout. The v61
PACK fold + the c4d48008 forgiving-funding change (+15 lines in DegenerusGameStorage.sol) shifted
some regions; the shift is REGION-DEPENDENT, not a uniform −1.

Purpose: these ~31 targeted (~176 full-suite) reds are the bulk of the broken regression net.
Until they are green the council sweeps (382+) have no oracle. This is the heaviest repair item.

Output: an authoritative layout key for subject c4d48008 + the named slot-hardcoded forge suites
turned green by re-deriving every slot from `forge inspect storageLayout` (or a named getter).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

# HIGH-VALUE REFERENCE — the Phase 378 recalibration key (built against b97a7a2e, the v61 HEAD).
# c4d48008 = b97a7a2e + the forgiving-funding change. Spot-checks confirm balancesPacked@7,
# prizePoolPendingPacked@11, ticketWriteSlot@slot0-off26, deityPassPricePaid@29 are UNCHANGED at
# c4d48008 — so most of this key still applies. RE-VERIFY against c4d48008 before trusting any row.
@.planning/milestones/v61.0-phases/378-tst-proving-tests-rng-freeze-solvency/378-01-RECALIBRATION-KEY.md
@.planning/milestones/v61.0-phases/378-tst-proving-tests-rng-freeze-solvency/378-PLAN-TRIAGE.md

# The carried-red ceiling these reds live under (the by-name non-widening ledger being superseded).
@test/REGRESSION-BASELINE-v61.md
</context>

<landmine>
⚠ `hardhat compile --force` (or `npx hardhat compile`) REGENERATES `contracts/ContractAddresses.sol`
and breaks the forge fixture. Do NOT run it in this plan. Use `forge build` / `forge inspect` only.
If any step regenerates ContractAddresses.sol, restore it: `git checkout HEAD -- contracts/ContractAddresses.sol`
and re-verify `git status --porcelain contracts/` is empty before finishing.
</landmine>

<tasks>

<task type="auto">
  <name>Task 1: Capture the authoritative c4d48008 storage layout + recalibration ledger</name>
  <files>.planning/phases/380-foundation-test-fix-green-baseline/380-01-LAYOUT-KEY.md</files>
  <read_first>
    - .planning/milestones/v61.0-phases/378-tst-proving-tests-rng-freeze-solvency/378-01-RECALIBRATION-KEY.md (the v61-HEAD layout + the per-harness ledger — the template to update)
    - contracts/storage/DegenerusGameStorage.sol (the c4d48008 storage declarations — the source of truth the layout reflects)
    - Run `forge clean && forge build` then `forge inspect DegenerusGame storageLayout` (THE authority — emits the layout regardless of profile)
  </read_first>
  <action>
    Run `forge clean && forge build` (must be clean — pre-existing lint warnings OK), then
    `forge inspect DegenerusGame storageLayout`. Capture the AUTHORITATIVE slot+offset for every
    symbol the named harnesses poke: ticketWriteSlot / prizePoolFrozen / ticketsFullyProcessed /
    presaleOver / subsFullyProcessed (slot-0 bit offsets), claimablePool, prizePoolsPacked,
    balancesPacked, mintPacked_, rngWordByDay, prizePoolPendingPacked, ticketQueue, _subOf,
    _subscribers, _subscriberIndex, _subCursor/_subOpenCursor/_afkingResetDay, lootboxEth,
    lootboxEthBase, lootboxRngPacked, lootboxRngWordByIndex, lootboxBurnie, degeneretteBets,
    degeneretteBetNonce. Write `380-01-LAYOUT-KEY.md` recording each VERBATIM from forge inspect,
    plus a delta column vs the 378 key (most rows expected unchanged — call out any that moved due
    to the c4d48008 +15-line storage add). Include a per-harness ledger row for each file in this
    plan's files_modified marking Game-resident pokes (recalibrate) vs sDGNRS-resident pokes
    (leave byte-identical — they live on address(sdgnrs), the Game fold cannot move them). Do NOT
    run hardhat (see landmine). Verify `git status --porcelain contracts/` empty when done.
  </action>
  <verify>
    <automated>test -f .planning/phases/380-foundation-test-fix-green-baseline/380-01-LAYOUT-KEY.md &amp;&amp; grep -q "balancesPacked" .planning/phases/380-foundation-test-fix-green-baseline/380-01-LAYOUT-KEY.md &amp;&amp; grep -q "prizePoolPendingPacked" .planning/phases/380-foundation-test-fix-green-baseline/380-01-LAYOUT-KEY.md &amp;&amp; [ -z "$(git status --porcelain contracts/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - 380-01-LAYOUT-KEY.md exists and records balancesPacked, prizePoolPendingPacked, _subOf,
      mintPacked_, rngWordByDay, lootboxRngPacked, degeneretteBets slots taken verbatim from
      `forge inspect DegenerusGame storageLayout` at c4d48008.
    - Each row notes whether it changed vs the 378 key.
    - `git status --porcelain contracts/` is empty (ContractAddresses.sol not regenerated).
  </acceptance_criteria>
  <done>The authoritative c4d48008 layout + per-harness recalibration ledger is recorded; contracts/ untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Recalibrate StorageFoundation + the VRF / VrfRotation suites</name>
  <files>test/fuzz/StorageFoundation.t.sol, test/fuzz/VRFCore.t.sol, test/fuzz/VRFStallEdgeCases.t.sol, test/fuzz/VrfRotationLiveness.t.sol, test/fuzz/LootboxRngLifecycle.t.sol</files>
  <read_first>
    - .planning/phases/380-foundation-test-fix-green-baseline/380-01-LAYOUT-KEY.md (Task 1 output — the authoritative slots)
    - test/fuzz/StorageFoundation.t.sol (asserts slot-0 bit offsets + packed-pool slots directly — the canary)
    - test/fuzz/VRFCore.t.sol, test/fuzz/VRFStallEdgeCases.t.sol, test/fuzz/VrfRotationLiveness.t.sol, test/fuzz/LootboxRngLifecycle.t.sol (the VRF/RNG-window suites with vm.store/vm.load pokes — grep each for `uint256(` slot literals + `keccak256(abi.encode`)
  </read_first>
  <action>
    For each file: first run the suite (`forge test --match-contract &lt;Contract&gt; -vv`) to see the
    ACTUAL failures at c4d48008 (do not assume — some slots may already be correct, e.g.
    V56SubHardening already carries 62/65/9). For each failing slot-hardcoded read, replace the
    stale slot literal with the authoritative value from 380-01-LAYOUT-KEY.md, OR better, route the
    read through the contract's named getter / accessor where one exists (prefer getters over raw
    vm.load). For StorageFoundation, recalibrate the slot-0 bit offsets to the c4d48008 values
    (ticketWriteSlot/prizePoolFrozen/ticketsFullyProcessed shifted down by the two added bools) and
    confirm the packed-pool slot asserts (slot 2 / slot 11) match. For the VRF suites, recalibrate
    rngWordByDay / lootboxRngWordByIndex / lootboxRngPacked reads and any orphan-index/stall slot
    pokes. Leave any sDGNRS-resident (address(sdgnrs)) pokes byte-identical. Do NOT run hardhat.
    NOTE: VRFPath stateful-fuzz invariants may be pre-existing bucket-A non-deterministic reds
    (documented in REGRESSION-BASELINE-v61.md §7) — if a `*.inv.t.sol` invariant reproduces red at
    the baseline too, it is carried, NOT this task's target; record it for the Plan 04 ledger and
    move on rather than forcing it green.
  </action>
  <verify>
    <automated>forge test --match-contract "StorageFoundation|VRFCore|VRFStallEdgeCases|VrfRotationLiveness|LootboxRngLifecycle" 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-contract StorageFoundation` exits 0 (all its test* names pass).
    - `forge test --match-contract VRFCore` / `VRFStallEdgeCases` / `VrfRotationLiveness` /
      `LootboxRngLifecycle` show 0 failing deterministic `test*` names (any carried bucket-A
      `invariant_*` non-determinism is documented, not a regression).
    - No bare stale slot literal remains in the touched files: re-derived slot constants cite the
      authoritative layout, or reads go through named getters. Verify by grep that the touched files
      no longer reference the pre-378 stale literals (e.g. no `uint256(65)` _subOf / `uint256(11)`
      rngWordByDay on address(game)).
  </acceptance_criteria>
  <done>StorageFoundation + the VRF/VrfRotation/LootboxRng suites pass at c4d48008 with slots derived from the authoritative layout.</done>
</task>

<task type="auto">
  <name>Task 3: Recalibrate the Keeper / V56Sub / Queue / FarFuture suites</name>
  <files>test/fuzz/KeeperNonBrick.t.sol, test/fuzz/V56SubHardening.t.sol, test/fuzz/QueueDoubleBuffer.t.sol, test/fuzz/FarFutureIntegration.t.sol, test/fuzz/FarFutureSalvageSwap.t.sol</files>
  <read_first>
    - .planning/phases/380-foundation-test-fix-green-baseline/380-01-LAYOUT-KEY.md (Task 1 output — the authoritative slots)
    - test/fuzz/KeeperNonBrick.t.sol, test/fuzz/V56SubHardening.t.sol, test/fuzz/QueueDoubleBuffer.t.sol, test/fuzz/FarFutureIntegration.t.sol, test/fuzz/FarFutureSalvageSwap.t.sol (grep each for slot literals + `keccak256(abi.encode` + setUp reverts)
  </read_first>
  <action>
    For each file: run the suite first (`forge test --match-contract &lt;Contract&gt; -vv`) to see the
    ACTUAL c4d48008 failures. V56SubHardening already carries the recalibrated 62/65/9 constants —
    re-run it and only touch what still fails (it may already be green; if so, record that and make
    no edit). For the rest, recalibrate every Game-resident slot poke (_subOf / _subscribers /
    _subscriberIndex / _subCursor for the Sub region; ticketQueue / ticketWriteSlot for the queue;
    mintPacked_ / rngWordByDay for far-future ticket routing) to the authoritative 380-01-LAYOUT-KEY
    values, preferring named getters where available. Fix any `runs:0` setUp revert (a setUp that
    reverts before any test runs — usually a stale-slot write that now corrupts an enum/bool and
    underflows): trace the offending vm.store to its symbol and repoint it. Leave sDGNRS-resident
    pokes byte-identical. FarFutureSalvageSwap touches the salvage-swap path — confirm its slot
    pokes hit DegenerusGame (recalibrate) vs StakedDegenerusStonk (leave). Do NOT run hardhat.
  </action>
  <verify>
    <automated>forge test --match-contract "KeeperNonBrick|V56SubHardening|QueueDoubleBuffer|FarFutureIntegration|FarFutureSalvageSwap" 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-contract KeeperNonBrick` / `V56SubHardening` / `QueueDoubleBuffer` /
      `FarFutureIntegration` / `FarFutureSalvageSwap` each show 0 failing deterministic `test*`
      names and 0 `runs:0` setUp reverts at c4d48008.
    - Every Game-resident slot read in the touched files derives from the authoritative layout (no
      pre-378 stale literal remains); sDGNRS-resident pokes unchanged.
  </acceptance_criteria>
  <done>The Keeper/Sub/Queue/FarFuture suites pass at c4d48008 with no stale Game-resident slot literals and no setUp reverts.</done>
</task>

</tasks>

<threat_model>
N/A — test-only changes; no `contracts/*.sol` modification, no new attack surface, no production
behavior change. (Subject c4d48008 is read-only here; only test/ files are edited.)
</threat_model>

<verification>
- `forge test --match-contract "StorageFoundation|VRFCore|VRFStallEdgeCases|VrfRotationLiveness|LootboxRngLifecycle|KeeperNonBrick|V56SubHardening|QueueDoubleBuffer|FarFutureIntegration|FarFutureSalvageSwap"`
  shows 0 failing deterministic test* names.
- `git status --porcelain contracts/` empty (ContractAddresses.sol not regenerated).
- 380-01-LAYOUT-KEY.md records the authoritative c4d48008 layout.
</verification>

<success_criteria>
The named slot-hardcoded forge harnesses pass at subject c4d48008 with every Game-resident slot
re-derived from `forge inspect DegenerusGame storageLayout` (or a named getter); the authoritative
layout key is recorded; contracts/ is byte-untouched.
</success_criteria>

<output>
Create `.planning/phases/380-foundation-test-fix-green-baseline/380-01-SUMMARY.md` when done.
</output>
