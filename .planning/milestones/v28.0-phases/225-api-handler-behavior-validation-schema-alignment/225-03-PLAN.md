---
phase: 225-api-handler-behavior-validation-schema-alignment
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/225-api-handler-behavior-validation-schema-alignment/225-03-REQUEST-SCHEMA-AUDIT.md
autonomous: true
requirements: [API-05]
must_haves:
  truths:
    - "225-03-REQUEST-SCHEMA-AUDIT.md exists with a row for every exported Zod schema in /home/zak/Dev/PurgeGame/database/src/api/schemas/*.ts (6 files: common.ts, game.ts, history.ts, leaderboard.ts, player.ts, tokens.ts)"
    - "Each exported Zod schema row records: schema name, source file:line, where-used (list of route file:line registrations that import it as schema.querystring/params/body), and the corresponding openapi.yaml parameter / requestBody / $ref anchor(s)"
    - "Each Zod schema has a PASS/FAIL verdict on name, type, required/optional, enum, regex/format, min/max alignment with the openapi.yaml parameter definitions it maps to"
    - "Every Zod↔openapi request-side mismatch is emitted as an F-28-225-NN stub with Direction: code->docs (openapi lags per D-225-02), Severity INFO default (promoted LOW for caller-breaking per 225-CONTEXT.md: required request parameter documented optional in openapi.yaml), and cross-repo file:line citations"
    - "Coverage summary reports: {N exported Zod schemas} / {M that are referenced by at least one route registration} / {P that map to an openapi.yaml parameter or requestBody definition} / {Q mismatches found}"
    - "Orphan Zod schemas (exported but not imported by any route registration) listed as INFO stubs with Direction: code-only (not code->docs) — note that these are NOT API-05 failures per the requirement wording but ARE context worth recording"
    - "Orphan openapi.yaml parameter definitions (declared in openapi.yaml but with no Zod counterpart enforced at runtime) listed as F-28-225-NN stubs with Direction: docs->code (exception to D-225-02 default because the docs claim a parameter that Zod doesn't validate — caller-facing promise is unchecked)"
    - "Summary footer reconfirms D-225-02 direction logic with the one explicit exception (orphan openapi params = docs->code)"
  artifacts:
    - path: ".planning/phases/225-api-handler-behavior-validation-schema-alignment/225-03-REQUEST-SCHEMA-AUDIT.md"
      provides: "API-05 request-schema audit: every exported Zod schema in src/api/schemas/*.ts vs the openapi.yaml parameter / requestBody definitions, with PASS/FAIL verdict rows and F-28-225-NN finding stubs"
      contains: "## Per-Zod-schema verdicts"
      min_lines: 60
  key_links:
    - from: "exported Zod schemas in /home/zak/Dev/PurgeGame/database/src/api/schemas/*.ts"
      to: "/home/zak/Dev/PurgeGame/database/src/api/routes/*.ts schema.querystring OR schema.params OR schema.body"
      via: "named import + usage pattern: grep -nE 'schema\\.(querystring|params|body)' or walking import lines"
      pattern: "from '\\.\\./schemas/[^']+'"
    - from: "each Zod-referenced route registration"
      to: "openapi.yaml paths:<path>:(get|post|...):parameters: OR requestBody:"
      via: "path-normalized match ({name}≡:name per 224 rule) then field-by-field compare"
      pattern: "parameters:|requestBody:"
    - from: "each Zod↔openapi mismatch"
      to: "Phase 229 finding rollup pool"
      via: "F-28-225-NN stub with Direction: code->docs (default) OR docs->code (orphan-openapi-param exception)"
      pattern: "^F-28-225-\\d+$"
---

<objective>
Audit every exported Zod schema in `/home/zak/Dev/PurgeGame/database/src/api/schemas/*.ts` (6 files) against the corresponding parameter / requestBody definitions in `/home/zak/Dev/PurgeGame/database/docs/openapi.yaml`. For each schema: identify the route registrations that use it (as `schema.querystring` / `schema.params` / `schema.body`), map it to the openapi.yaml parameter or requestBody definition for the same endpoint + field, and compare field names, types, required/optional status, enum values, and regex/format constraints. Produce `225-03-REQUEST-SCHEMA-AUDIT.md` with per-schema verdict rows and F-28-225-NN finding stubs.

Per D-225-02, mismatches default to `Direction: code->docs` (openapi.yaml patched to match Zod). One explicit exception: when openapi.yaml declares a parameter that has NO Zod counterpart enforced at runtime, the direction is `docs->code` (the docs promise a parameter the code never validates) — this is a real contract gap.

Purpose: Satisfy requirement API-05 for all 6 Zod schema files.
Output: `.planning/phases/225-api-handler-behavior-validation-schema-alignment/225-03-REQUEST-SCHEMA-AUDIT.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/225-api-handler-behavior-validation-schema-alignment/225-CONTEXT.md
@.planning/phases/224-api-route-openapi-alignment/224-01-API-ROUTE-MAP.md
@/home/zak/Dev/PurgeGame/degenerus-audit/audit/FINDINGS-v27.0.md

<interfaces>
The 6 Zod schema files this plan audits (confirmed by directory listing):

| # | File (in /home/zak/Dev/PurgeGame/database/src/api/schemas/) | Size (bytes) | Known exports (partial; confirmed from scouting) |
| --- | --- | --- | --- |
| 1 | `common.ts`      | 884  | `addressParamSchema` (regex `^0x[a-fA-F0-9]{40}$`, transform toLowerCase), `errorResponseSchema`, `paginationQuerySchema` (cursor: string optional; limit: coerce.number int 1-100 default 20), `encodeCursor` (function — not a schema), `decodeCursor` (function — not a schema) |
| 2 | `game.ts`        | 3086 | walk file for exports |
| 3 | `history.ts`     | 1337 | walk file for exports |
| 4 | `leaderboard.ts` | 736  | confirmed: `leaderboardEntrySchema` (internal), `coinflipLeaderboardResponseSchema`, `affiliateLeaderboardResponseSchema`, `bafBracketResponseSchema`, `coinflipQuerySchema` (day: coerce.number int optional), `levelQuerySchema` (level: coerce.number int optional) |
| 5 | `player.ts`      | 2674 | walk file for exports |
| 6 | `tokens.ts`      | 458  | walk file for exports |

**Request-side vs response-side distinction**: API-05 covers REQUEST-side schemas only (`schema.querystring`, `schema.params`, `schema.body`). Response schemas (`schema.response[200]`) are Plan 225-02's scope. Some schema files mix both — executor must classify each exported schema as request-side or response-side by walking where it's imported in `src/api/routes/*.ts`. Example from confirmed scouting:
- `leaderboard.ts` exports BOTH request-side (`coinflipQuerySchema`, `levelQuerySchema`) AND response-side (`coinflipLeaderboardResponseSchema`, etc.) schemas. Only the request-side ones are in Plan 225-03's scope; the response-side ones belong to Plan 225-02 (already sampled if leaderboards.ts was one of the 8 samples).

**openapi.yaml parameter location**: per 224-01-API-ROUTE-MAP.md Pass 1, every endpoint's `parameters:` block (when it has one) lives in the 30-50 lines following the path key. Path keys and their starting line numbers are enumerated in 224-01-API-ROUTE-MAP.md Pass 1 (rows 1-27). The `requestBody:` block is uniformly absent across openapi.yaml (confirmed by 224 Pass 1: "no `requestBody:` declarations exist anywhere"). So:
- All 27 endpoints are GET or otherwise body-less.
- Every request-side Zod schema that goes into `schema.body` is automatically orphan-on-docs (but, per body-shape-detection scan at 224-01-API-ROUTE-MAP.md line 166, zero registrations declare `schema.body` either — so this case is empty).
- Real API-05 comparison is between Zod query/param schemas and openapi.yaml `parameters:` entries.

**Path normalization rule** (carried from Phase 224 D-224 Pass 1/2 methodology): openapi uses `{name}`, code uses `:name`. These are equivalent for matching. 224-01-API-ROUTE-MAP.md already locked the 27 path pairings.

**Zod-to-openapi type mapping** (same as Plan 225-02, reused):

| Zod construct | openapi.yaml counterpart |
| --- | --- |
| `z.string()` with `.regex(/pattern/)` | `schema: { type: string, pattern: "pattern" }` |
| `z.string().transform(...)` | `schema: { type: string }` (transform is code-only; not in docs) |
| `z.coerce.number()` | `schema: { type: number }` (in query params, openapi doesn't distinguish coerce) |
| `z.coerce.number().int()` | `schema: { type: integer }` |
| `z.coerce.number().min(N).max(M)` | `schema: { type: integer, minimum: N, maximum: M }` |
| `z.coerce.number().default(D)` | `schema: { type: integer, default: D }` |
| `z.optional(X)` on query/param | `required: false` (or omit `required: true`) |
| (non-`.optional()` on query/param) | `required: true` |
| `z.enum([...])` | `schema: { type: string, enum: [...] }` |

**Direction rule per D-225-02**: Default is `code->docs`. Exception: openapi.yaml parameter with NO Zod enforcement = `docs->code` (a real contract gap — the docs promise validation the code does not perform).

**Severity rules per 225-CONTEXT.md Severity scheme**:
- Default: INFO
- Promote to LOW when caller-breaking:
  - **Required request parameter documented optional in openapi.yaml** (callers omit it, get 400) — the canonical API-05 LOW case
  - Parameter declared optional in Zod but openapi.yaml lists it as `required: true` (breaks compliant consumers who skip it)
  - Parameter documented in openapi.yaml that the Zod schema does not validate at all (orphan-openapi-param, docs->code direction)

**Orphan classification**:
- **Orphan-in-Zod**: exported Zod schema with no route-file import. NOT an API-05 failure (the requirement is about Zod-openapi alignment at referenced call sites). Record as INFO-context with Direction `code-only` and resolution `INFO-ACCEPTED`.
- **Orphan-in-openapi**: openapi.yaml parameter declared on an endpoint where the Zod `schema.querystring`/`schema.params` does NOT include that parameter. Direction `docs->code`, severity promoted to LOW by the third rule above.

**Finding-ID format per D-225-06**: `F-28-225-NN` — continue the numeric pool from Plans 225-01 and 225-02. At execution time, read `225-01-SUMMARY.md` and `225-02-SUMMARY.md` (if they exist) to find the next free ID; otherwise start at the next available number in the sequence.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate exported Zod schemas in all 6 files, map to route usage, and compare against openapi.yaml parameter definitions</name>
  <files>.planning/phases/225-api-handler-behavior-validation-schema-alignment/225-03-REQUEST-SCHEMA-AUDIT.md</files>
  <read_first>
    - .planning/phases/225-api-handler-behavior-validation-schema-alignment/225-CONTEXT.md (D-225-02 direction rule, D-225-05 three-catalog convention, D-225-06 finding ID format, D-225-07 cross-repo path convention; Severity scheme section for LOW promotion rules)
    - .planning/phases/224-api-route-openapi-alignment/224-01-API-ROUTE-MAP.md (Pass 1 table lines 65-93 — 27 openapi paths with parameter names per endpoint; Pass 2 table lines 128-156 — 27 route registrations with declared params referenced via Zod schema names)
    - /home/zak/Dev/PurgeGame/database/src/api/schemas/common.ts (full file — confirmed 29 lines; exports: addressParamSchema, errorResponseSchema, paginationQuerySchema, encodeCursor, decodeCursor)
    - /home/zak/Dev/PurgeGame/database/src/api/schemas/game.ts (full file — walk all top-level `export const <name> = z.` declarations)
    - /home/zak/Dev/PurgeGame/database/src/api/schemas/history.ts (full file — walk all top-level `export const <name> = z.` declarations)
    - /home/zak/Dev/PurgeGame/database/src/api/schemas/leaderboard.ts (full file — 28 lines confirmed; exports: leaderboardEntrySchema [internal], coinflipLeaderboardResponseSchema, affiliateLeaderboardResponseSchema, bafBracketResponseSchema, coinflipQuerySchema, levelQuerySchema)
    - /home/zak/Dev/PurgeGame/database/src/api/schemas/player.ts (full file — walk all top-level `export const <name> = z.` declarations)
    - /home/zak/Dev/PurgeGame/database/src/api/schemas/tokens.ts (full file — walk all top-level `export const <name> = z.` declarations)
    - /home/zak/Dev/PurgeGame/database/src/api/routes/game.ts (grep: `^import.*from '\\.\\./schemas/' ` and `schema\\.(querystring|params|body)` — record every usage)
    - /home/zak/Dev/PurgeGame/database/src/api/routes/health.ts (confirmed: 0 Zod imports from schemas/, inline `healthResponseSchema`, no querystring/params/body)
    - /home/zak/Dev/PurgeGame/database/src/api/routes/history.ts (grep as above)
    - /home/zak/Dev/PurgeGame/database/src/api/routes/leaderboards.ts (confirmed: imports from schemas/leaderboard.ts — coinflipLeaderboardResponseSchema, affiliateLeaderboardResponseSchema, bafBracketResponseSchema, coinflipQuerySchema, levelQuerySchema)
    - /home/zak/Dev/PurgeGame/database/src/api/routes/player.ts (grep as above)
    - /home/zak/Dev/PurgeGame/database/src/api/routes/replay.ts (grep as above)
    - /home/zak/Dev/PurgeGame/database/src/api/routes/tokens.ts (confirmed: imports tokenAnalyticsResponseSchema from schemas/tokens.ts; no querystring/params/body)
    - /home/zak/Dev/PurgeGame/database/src/api/routes/viewer.ts (grep as above)
    - /home/zak/Dev/PurgeGame/database/docs/openapi.yaml (for each endpoint that uses a Zod request schema, read the 30-50 lines following the path key to find the `parameters:` block; path key line numbers in 224-01-API-ROUTE-MAP.md Pass 1)
    - .planning/phases/225-api-handler-behavior-validation-schema-alignment/225-01-SUMMARY.md (if it exists — continue F-28-225-NN numbering)
    - .planning/phases/225-api-handler-behavior-validation-schema-alignment/225-02-SUMMARY.md (if it exists — continue F-28-225-NN numbering)
  </read_first>
  <action>
Create `.planning/phases/225-api-handler-behavior-validation-schema-alignment/225-03-REQUEST-SCHEMA-AUDIT.md` with the following exact structure:

1. **Preamble**: Phase 225, Plan 03. Requirement satisfied: API-05. Direction rule: D-225-02 with one exception (orphan-openapi-param = docs->code). Severity rule: 225-CONTEXT.md (with the "required-documented-optional" canonical LOW case). Finding-ID format: F-28-225-NN shared pool per D-225-06. Scope: REQUEST-side Zod schemas only (`schema.querystring`, `schema.params`, `schema.body`); response-side schemas belong to Plan 225-02.

2. **Section `## Zod schema enumeration`**: a table with one row per top-level `export const <name> = z.<expr>` declaration across all 6 schema files. Columns:

   | # | schema file:line | exported name | z-type (outermost) | side (request / response / mixed / internal) | where-used (route file:line list) | orphan? |

   "side" classification:
   - `request`: imported by a route as `schema.querystring`, `schema.params`, or `schema.body`
   - `response`: imported by a route as `schema.response[<code>]`
   - `mixed`: imported both ways (rare — likely error)
   - `internal`: used only within the same schemas/*.ts file (e.g., `leaderboardEntrySchema` in leaderboard.ts)
   - `orphan`: exported but not imported anywhere

   For each request-side row, the "where-used" column lists all route registration file:line pairs that reference it as a request schema.

3. **Section `## Per-Zod-schema verdicts`**: for every schema row whose `side` is `request`, emit a subsection:

   `### {schema name} — {schema file:line}`

   - **Used by**: one bullet per route file:line that imports this schema as a request schema, each bullet including the full URL path (composed per 224-01-API-ROUTE-MAP.md prefix rules) and the usage slot (querystring / params / body).
   - **Field-by-field comparison table** — for each field declared in the Zod schema, one row with columns:
     | Zod field | Zod type | Zod required? | Zod default / min / max / enum / regex | openapi.yaml parameter (path + slot + name) | openapi.yaml type | openapi.yaml required? | openapi.yaml default / min / max / enum / pattern | verdict |
     Verdict values: `PASS`, `FAIL`, `ORPHAN-IN-OPENAPI` (openapi declares a parameter Zod doesn't validate; raised in a separate orphan section below).
   - **Per-schema verdict**: `PASS` (all fields match across all endpoints that use this schema) or `FAIL` (≥1 mismatch).
   - If `FAIL`, enumerate finding stubs per FAIL row (schema below).

4. **Section `## Orphan openapi.yaml parameters`**: for each openapi.yaml `parameters:` entry on a route that has no Zod-validated counterpart, emit a subsection with:

   `### openapi.yaml:<line> — <endpoint path> — parameter <name>`

   - openapi.yaml declaration block (quoted)
   - Route registration file:line (if the endpoint has one)
   - Zod schema imports on that route (confirmed not to validate this parameter)
   - F-28-225-NN stub with Direction `docs->code` (this is the one exception to D-225-02), severity LOW (third LOW-promotion rule in 225-CONTEXT.md: "documented in openapi.yaml that the handler never emits/validates"), resolution RESOLVED-DOC (remove from openapi.yaml) OR RESOLVED-CODE (add Zod validation) — recommend RESOLVED-DOC by default unless the parameter is security-relevant.

5. **Section `## Orphan Zod schemas`**: for each exported Zod schema with `side=orphan` (no route imports at all), a list entry with schema name, file:line, and a one-sentence INFO-ACCEPTED disposition (not a finding, just context). These are NOT API-05 failures; they are dead exports worth noting.

6. **Finding-stub schema** (emitted per FAIL row OR orphan-openapi parameter):

   ```markdown
   #### F-28-225-NN — {short title: schema name + field + mismatch}

   - **Severity:** {INFO | LOW}
   - **Direction:** {code->docs (default) | docs->code (orphan-openapi-param exception)}
   - **Phase:** 225 (API-05 request-schema audit)
   - **Code side:** `/home/zak/Dev/PurgeGame/database/src/api/schemas/<file>.ts:<line>` — {Zod declaration}
   - **Code usage:** `/home/zak/Dev/PurgeGame/database/src/api/routes/<file>.ts:<line>` — <slot: querystring/params/body> on <full path>
   - **Docs side:** `/home/zak/Dev/PurgeGame/database/docs/openapi.yaml:<line>` — {openapi parameter / requestBody declaration, or "(parameter absent)" for orphan-in-Zod side, or "(Zod validation absent)" for orphan-openapi-param side}
   - **Mismatch:** {one sentence}
   - **Caller impact:** {one sentence — compliance break OR harmless doc lag}
   - **Suggested resolution:** {RESOLVED-DOC default for code->docs | RESOLVED-DOC or RESOLVED-CODE for docs->code with rationale}
   ```

7. **Section `## Coverage summary`**:

   - **Total exported Zod schemas across 6 files:** {N}
   - **Request-side (used as querystring/params/body by ≥1 route):** {M}
   - **Response-side (out of scope for this plan; in Plan 225-02):** {R}
   - **Internal (used only within schemas/):** {I}
   - **Orphan (exported but never imported):** {O}
   - **Total openapi.yaml `parameters:` entries across 27 endpoints:** {sum — computable from 224-01-API-ROUTE-MAP.md Pass 1 "parameters (path/query)" column; reconfirm by reading openapi.yaml}
   - **openapi parameters covered by Zod (has a matching Zod field):** {P}
   - **Orphan-in-openapi (parameter declared but no Zod validation):** {Q}
   - **Coverage %:** `100 * P / (P + Q)` — the percentage of openapi-documented parameters that Zod actually validates at runtime

8. **Section `## Summary`**:

   - **Schemas audited:** 6 files / {N} exported declarations / {M} request-side
   - **Endpoints covered by this audit:** {all 27 - 1 for /health which has no querystring/params/body} = 26 (confirm)
   - **Findings by severity:** INFO = {x}, LOW = {y}
   - **F-28-225-NN IDs consumed by this plan:** {range}
   - **Direction reconfirmation:** "All request-side Zod↔openapi mismatches default Direction: code->docs per D-225-02. Exception: orphan-openapi parameters (docs declare a parameter Zod does not validate) use Direction: docs->code per 225-03-PLAN interfaces block."
   - **Scope exclusion reconfirmation:** "Response-side schemas audited in Plan 225-02 (not this plan). Indexer schemas in src/db/schema/*.ts audited in Phase 226 (not this phase)."

**Methodology notes for the executor:**

- When a route uses an inline Zod declaration rather than importing from `schemas/`, treat it as if it were in an anonymous "inline schemas" row (e.g., `dayParamSchema` declared inside `game.ts` per 224-01-API-ROUTE-MAP.md row 5). Enumerate these too — API-05 covers ALL Fastify request-validation schemas, not just those in `src/api/schemas/`. Add a row to the Zod schema enumeration table with `schema file:line` pointing at the inline declaration.
- For schemas that are referenced by multiple routes (e.g., `addressParamSchema` used by player.ts rows 17, 225; history.ts row 129; viewer.ts rows 167, 318 — 5 usages), the per-schema verdict subsection must audit the schema against the openapi `parameters:` block for EACH endpoint that uses it. If ANY endpoint's openapi declaration disagrees with the shared Zod schema, that endpoint's row gets a FAIL and a finding stub.
- For `paginationQuerySchema` (common.ts: cursor optional string, limit coerce.number int 1-100 default 20) used by multiple history/pagination endpoints, similarly audit against each endpoint's openapi `parameters:` block.
- Inline `errorResponseSchema` is response-side — exclude from API-05 audit (note in enumeration table with side=response, out of scope for this plan).
- `encodeCursor` and `decodeCursor` in common.ts are FUNCTIONS, not schemas — mark as `not a schema` in the enumeration table and exclude from per-schema verdicts.

**Important — no database/ writes.** This task writes ONLY to `.planning/phases/225-api-handler-behavior-validation-schema-alignment/225-03-REQUEST-SCHEMA-AUDIT.md`. No files under `/home/zak/Dev/PurgeGame/database/` are touched.
  </action>
  <verify>
    <automated>test -f .planning/phases/225-api-handler-behavior-validation-schema-alignment/225-03-REQUEST-SCHEMA-AUDIT.md && grep -c '^## ' .planning/phases/225-api-handler-behavior-validation-schema-alignment/225-03-REQUEST-SCHEMA-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `.planning/phases/225-api-handler-behavior-validation-schema-alignment/225-03-REQUEST-SCHEMA-AUDIT.md`
    - File contains these exact section headings (grep-verifiable): `## Zod schema enumeration`, `## Per-Zod-schema verdicts`, `## Orphan openapi.yaml parameters`, `## Orphan Zod schemas`, `## Coverage summary`, `## Summary`
    - Zod schema enumeration table has at least one row per file for each of the 6 files: grep `common.ts:` ≥1, `game.ts:` ≥1, `history.ts:` ≥1, `leaderboard.ts:` ≥1, `player.ts:` ≥1, `tokens.ts:` ≥1
    - Every `### ` subsection under `## Per-Zod-schema verdicts` names a request-side Zod schema (not response or internal); each has a "Used by:" bullet list and a field-by-field comparison table
    - Every finding stub matches regex `/^#### F-28-225-\d+ —/`
    - Every finding stub has a `**Direction:**` line reading either `code->docs` (default) or `docs->code` (orphan-openapi-param exception); no other directions
    - No finding stub has Direction `comment->code` (that's Plan 225-01's domain)
    - Every finding stub cites a file path under `/home/zak/Dev/PurgeGame/database/` (schemas/, routes/, or docs/openapi.yaml)
    - Coverage summary section includes lines for "Total exported Zod schemas", "Request-side", "Response-side", "Internal", "Orphan", "Total openapi.yaml parameters:", "openapi parameters covered by Zod", "Orphan-in-openapi", "Coverage %"
    - Summary direction reconfirmation line reads exactly: `All request-side Zod↔openapi mismatches default Direction: code->docs per D-225-02.` with the orphan exception noted
    - Summary scope-exclusion reconfirmation mentions "Response-side schemas audited in Plan 225-02" and "schema/*.ts audited in Phase 226"
    - For schemas reused across multiple endpoints (e.g., addressParamSchema), the per-schema verdict subsection lists ALL using endpoints in the Used-by bullet list
    - errorResponseSchema is classified as side=response in the enumeration table (out of scope for this plan)
    - encodeCursor / decodeCursor are marked "not a schema" in the enumeration table
  </acceptance_criteria>
  <done>
    `225-03-REQUEST-SCHEMA-AUDIT.md` exists with: (a) a full Zod schema enumeration across all 6 schema files classifying every exported declaration by side (request/response/internal/orphan); (b) per-Zod-schema verdict subsections for every request-side schema, with field-by-field comparison tables against every endpoint's openapi.yaml `parameters:` block; (c) orphan-openapi-parameter subsections with F-28-225-NN stubs at Direction `docs->code`; (d) orphan Zod schemas listed as INFO-context (not findings); (e) coverage summary with total counts and Coverage %; (f) Summary reconfirming D-225-02 direction logic and the one exception. All cross-repo citations point into `/home/zak/Dev/PurgeGame/database/`. No writes outside `.planning/phases/225-*/`.
  </done>
</task>

</tasks>

<threat_model>
N/A — read-only audit phase; produces audit documentation only, modifies no production code.
</threat_model>

<verification>
- All 6 Zod schema files audited; every exported declaration enumerated with side classification
- Request-side schemas compared field-by-field against openapi.yaml parameter definitions at every endpoint that uses them
- Inline Zod declarations in route files (not in src/api/schemas/) also enumerated and audited — API-05 covers all Fastify request-validation schemas
- Response-side schemas classified and excluded (scope of Plan 225-02)
- Orphan-in-openapi parameters flagged with Direction: docs->code (the one exception to D-225-02)
- Orphan-in-Zod schemas listed as INFO-context, not findings
- Coverage % reported (openapi-parameters-covered-by-Zod / total-openapi-parameters)
- All file:line citations point into /home/zak/Dev/PurgeGame/database/ per D-225-07
- F-28-225-NN IDs allocated sequentially, continuing from Plans 225-01 and 225-02
</verification>

<success_criteria>
API-05 is satisfied when every request-side Zod schema (across 6 schema files plus inline declarations in route files) is compared field-by-field against its openapi.yaml counterpart at every endpoint where it is used; every mismatch is enumerated as an F-28-225-NN stub with Direction `code->docs` (default) or `docs->code` (orphan-openapi-param); orphan Zod schemas are listed as context; and a Coverage % summary confirms the openapi-to-Zod coverage ratio.
</success_criteria>

<output>
After completion, create `.planning/phases/225-api-handler-behavior-validation-schema-alignment/225-03-SUMMARY.md` summarizing: schemas audited, request-side vs response-side vs internal vs orphan counts, openapi-parameter coverage %, finding counts by severity and by direction, F-28-225-NN range consumed.
</output>
</content>
</invoke>