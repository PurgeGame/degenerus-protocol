---
phase: 226-schema-migration-orphan-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/226-schema-migration-orphan-audit/226-01-SCHEMA-MIGRATION-DIFF.md
autonomous: true
requirements: [SCHEMA-01]
requirements_addressed: [SCHEMA-01]
must_haves:
  truths:
    - "226-01-SCHEMA-MIGRATION-DIFF.md exists with at least one row for every *.ts schema file in /home/zak/Dev/PurgeGame/database/src/db/schema/ (30 files including index.ts, indexes.ts, views.ts)"
    - "Every pgTable('<pg_name>', ...) declaration and every pgMaterializedView('<mv_name>', ...) declaration has a dedicated section comparing TS-declared state against cumulative .sql ground truth"
    - "Every column in every table has a row with columns: name, TS-type, SQL-type, NOT NULL match, DEFAULT match, PK match, verdict (PASS/FAIL)"
    - "Every index (index / uniqueIndex) and every FK and every composite PK on each table has a row with TS side, SQL side, verdict"
    - "Each FAIL row produces an F-28-226-NN finding stub with Severity, Direction: schema<->migration, Phase: 226, File:line citation into /home/zak/Dev/PurgeGame/database/, Resolution"
    - "Summary footer reports: total tables, total columns compared, total indexes compared, FAIL count per category, F-28-226-NN range consumed"
  artifacts:
    - path: ".planning/phases/226-schema-migration-orphan-audit/226-01-SCHEMA-MIGRATION-DIFF.md"
      provides: "SCHEMA-01 per-table per-column/index/FK diff catalog between cumulative .sql and schema/*.ts"
      contains: "## Per-table verdicts"
      min_lines: 100
  key_links:
    - from: "pgTable('<pg>', ...) in src/db/schema/*.ts"
      to: "CREATE TABLE / ALTER TABLE statements in cumulative drizzle/*.sql application"
      via: "statement-breakpoint split + regex classify + per-table accumulator"
      pattern: "pgTable\\('([a-z_0-9]+)'"
    - from: "each FAIL row"
      to: "Phase 229 finding rollup pool"
      via: "F-28-226-NN stub at catalog tail with Direction: schema<->migration"
      pattern: "^F-28-226-\\d+"
---

<objective>
Build the SCHEMA-01 per-column / per-index / per-FK reconciliation catalog between the cumulative applied state of all 7 `/home/zak/Dev/PurgeGame/database/drizzle/*.sql` migrations and the TS-declared state in all 30 `/home/zak/Dev/PurgeGame/database/src/db/schema/*.ts` files. Every drift produces an `F-28-226-NN` finding stub with direction `schema<->migration` per D-226-05.

Purpose: Satisfy requirement SCHEMA-01.
Output: `.planning/phases/226-schema-migration-orphan-audit/226-01-SCHEMA-MIGRATION-DIFF.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/226-schema-migration-orphan-audit/226-CONTEXT.md
@.planning/phases/226-schema-migration-orphan-audit/226-RESEARCH.md
@.planning/phases/225-api-handler-behavior-validation-schema-alignment/225-01-PLAN.md

<interfaces>
## Field-level mapping table (verbatim from 226-RESEARCH.md §"drizzle/meta/*.json Snapshot Shape")

This is the diff contract. Copy into 226-01 as the verdict rubric.

| Concept | `.sql` DDL | `schema/*.ts` | `meta/*.json` (path) |
|---------|------------|----------------|-----------------------|
| Table exists | `CREATE TABLE "<t>"` | `pgTable('<t>', ...)` | `tables["public.<t>"]` |
| Column name | quoted ident inside `CREATE TABLE` / `ALTER ... ADD COLUMN` | key of first-arg object (or `text('snake_name')` override) | `tables[...].columns.<key>.name` |
| Column type | `text`, `integer`, `bigint`, `smallint`, `timestamp`, `boolean`, `jsonb`, identity spec | `text()` / `integer()` / `bigint({mode})` / `smallint()` / `boolean()` / timestamp modifier | `.type` string |
| NOT NULL | `NOT NULL` | `.notNull()` | `.notNull: true` |
| DEFAULT | `DEFAULT <expr>` | `.default(<lit>)` / `.default(sql\`...\`)` | `.default` string |
| PK (single col) | `PRIMARY KEY` on col | `.primaryKey()` | `.primaryKey: true` on column |
| PK (composite) | `CONSTRAINT "<n>" PRIMARY KEY("a","b")` | `primaryKey({columns:[t.a,t.b]})` in 2nd-arg array | `compositePrimaryKeys.<name>` |
| Identity / serial | `GENERATED {ALWAYS\|BY DEFAULT} AS IDENTITY (SEQUENCE ...)` | `.generatedAlwaysAsIdentity()` / `.generatedByDefaultAsIdentity()` | `.identity` object |
| Unique index | `CREATE UNIQUE INDEX "<n>" ON "<t>" USING btree ("<c>")` | `uniqueIndex('<n>').on(t.c)` | `.indexes.<n>.isUnique: true` |
| Plain index | `CREATE INDEX "<n>" ...` | `index('<n>').on(t.c)` | `.indexes.<n>.isUnique: false` |
| Foreign key | `CONSTRAINT "<n>" FOREIGN KEY (...) REFERENCES ...` | `foreignKey({columns, foreignColumns, name})` or col-level `.references(...)` | `.foreignKeys.<n>` |
| Enum | `CREATE TYPE "<n>" AS ENUM ('a','b')` | `pgEnum('<n>', ['a','b'])` | `enums.<n>` |
| Materialized view | `CREATE MATERIALIZED VIEW "<n>" AS ...` | `pgMaterializedView('<n>').as((qb) => ...)` | `views.<n>` (if generated) |

## Gotchas (do NOT log as findings) — 226-RESEARCH.md §"Known drizzle-kit Gotchas"

1. Identity-column `SEQUENCE NAME / MINVALUE / MAXVALUE / START WITH / CACHE` defaults = drizzle-kit boilerplate, not drift.
2. `timestamp({ mode: 'date'|'string' })` — `mode` is JS-runtime only; SQL side identical.
3. `bigint({ mode: 'bigint'|'number' })` — same.
4. DEFAULT literal representation — normalize before comparing (`.default(0)` == `DEFAULT 0`).
5. Composite PK auto-names — TS `primaryKey({columns:[t.a,t.b]})` with no name → SQL `<table>_<col>_<col>_pk`. Name derivation, not drift.
6. Camel vs snake column names — `text('snake_name')` override is the snake-case signal; absence → PG col is camelCase.
7. `indexes.ts` is NOT in `drizzle.config.ts`. Its `ADDITIONAL_INDEXES` array raw SQL appears in PG but NOT in `.sql` migrations or `meta/*.json`. Record as context note; do NOT emit per-index SCHEMA-01 findings for these.
8. `0005_red_doctor_octopus.sql` is 0 bytes — verify `meta/0005_snapshot.json` vs `0004_snapshot.json` for meta-only legitimacy.
9. `0007_trait_burn_tickets.sql` without `0007_snapshot.json` — pre-assigned F-28-226-01 to Plan 226-02; do NOT re-emit here. Context note only.
10. `CREATE MATERIALIZED VIEW` absent from all `.sql` files despite `views.ts` defining four — flag as INVESTIGATE in Views subsection (see Open Questions Q1 in 226-RESEARCH.md).

## Parsing strategy (226-RESEARCH.md §"Parsing Strategy")

1. For each of 7 `.sql` files, split on `--> statement-breakpoint`.
2. Classify each fragment by leading token: `CREATE TABLE`, `ALTER TABLE`, `CREATE (UNIQUE )?INDEX`, `CREATE MATERIALIZED VIEW`, `DROP ...`, `ADD CONSTRAINT`, `CREATE TYPE`.
3. Maintain an accumulator: after each migration, the authoritative cumulative state.
4. Final cumulative state = per-(table, column) last-row. This is the SQL side of the diff.

## Grep patterns (verbatim — drop into audit)

```bash
# All DDL statements grouped by leading keyword
rg -n --no-heading '^(CREATE (UNIQUE |MATERIALIZED )?(TABLE|INDEX|VIEW)|ALTER TABLE|DROP (TABLE|INDEX|COLUMN|CONSTRAINT)|ADD CONSTRAINT|CREATE TYPE)' \
  /home/zak/Dev/PurgeGame/database/drizzle/*.sql

# Per-migration statement-count sanity check
for f in /home/zak/Dev/PurgeGame/database/drizzle/*.sql; do
  echo "$f $(grep -c -F 'statement-breakpoint' "$f")"
done

# Every TS table (pg_name)
rg -n --no-heading "= pgTable\('([a-z_0-9]+)'" \
  /home/zak/Dev/PurgeGame/database/src/db/schema/ --replace '$1'

# Every TS index/uniqueIndex
rg -n --no-heading "(unique)?[Ii]ndex\('([a-z_0-9]+)'\)\.on\(" \
  /home/zak/Dev/PurgeGame/database/src/db/schema/

# Every TS foreign key (expected zero per scout; flag if any)
rg -n --no-heading 'foreignKey\(' /home/zak/Dev/PurgeGame/database/src/db/schema/

# Every TS materialized view
rg -n --no-heading "pgMaterializedView\('([a-z_0-9]+)'" \
  /home/zak/Dev/PurgeGame/database/src/db/schema/views.ts
```

## Finding-stub format (verbatim)

```markdown
#### F-28-226-NN: {short title}

- **Severity:** {INFO | LOW}
- **Direction:** schema<->migration
- **Phase:** 226 (SCHEMA-01 cumulative-SQL vs schema/*.ts diff)
- **File:** `/home/zak/Dev/PurgeGame/database/<path>:<line>`
- **Resolution:** {RESOLVED-CODE (patch schema) | RESOLVED-CODE (next migration) | INFO-ACCEPTED}
- **TS side:** {quoted TS declaration, file:line}
- **SQL side:** {quoted SQL fragment, file:line}
- **Mismatch:** {one-sentence description of the drift}
```

## Severity rules (D-226-05 + Claude discretion)

- Default: **INFO**.
- Promote to **LOW** only when the drift would cause silent data corruption OR a query to return wrong results (e.g. NOT NULL present in SQL but missing in TS → TS validation may reject nullable inserts silently; index in SQL but not in TS → drizzle-kit next-generate would DROP the index).
- Do NOT promote beyond LOW.

## Finding-ID allocation

Start at `F-28-226-02` (F-28-226-01 is pre-allocated to Plan 226-02 per D-226-06). Increment only when emitting a stub. Plan 226-02 will continue the sequence from where 226-01 ends.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parse cumulative .sql state and enumerate all TS tables/views/indexes</name>
  <files>
    .planning/phases/226-schema-migration-orphan-audit/226-01-SCHEMA-MIGRATION-DIFF.md
  </files>
  <read_first>
    - .planning/phases/226-schema-migration-orphan-audit/226-CONTEXT.md (full file — all D-226-NN decisions)
    - .planning/phases/226-schema-migration-orphan-audit/226-RESEARCH.md (full file — §Parsing Strategy, §Drizzle TS Extraction, §Meta JSON Shape, §views.ts, §Gotchas)
    - /home/zak/Dev/PurgeGame/database/drizzle.config.ts (29 registered schema files)
    - /home/zak/Dev/PurgeGame/database/drizzle/0000_brown_justin_hammer.sql
    - /home/zak/Dev/PurgeGame/database/drizzle/0001_*.sql through /home/zak/Dev/PurgeGame/database/drizzle/0007_trait_burn_tickets.sql (all 7 migrations)
    - /home/zak/Dev/PurgeGame/database/drizzle/meta/0006_snapshot.json (secondary cross-check per D-226-01)
    - Every *.ts file in /home/zak/Dev/PurgeGame/database/src/db/schema/ (30 files — use `ls` then `Read` each)
    - /home/zak/Dev/PurgeGame/degenerus-audit/audit/FINDINGS-v27.0.md (finding-stub format reference)
  </read_first>
  <action>
Create `.planning/phases/226-schema-migration-orphan-audit/226-01-SCHEMA-MIGRATION-DIFF.md` with the structure below.

**Step 1 — Build SQL cumulative state.** For each of the 7 `.sql` files in order (0000..0007), split on `--> statement-breakpoint` and classify each fragment per the grep patterns in `<interfaces>`. Maintain an internal accumulator mapping `pg_table_name -> { columns: [...], indexes: [...], pks: [...], fks: [...] }`. After processing 0007, the accumulator is the SQL ground truth.

**Step 2 — Enumerate TS side.** `ls` the schema directory to get the 30-file list. For each `*.ts` file (including `indexes.ts`, `views.ts`, `index.ts` barrel), read the file and extract:
  - Every `pgTable('<pg_name>', { ... }, (t) => [ ... ])` declaration — record pg_name, line, column list, constraint array.
  - Every `pgMaterializedView('<mv_name>', ...)` declaration.
  - Every `pgEnum('<name>', [...])`.
Record `indexes.ts` per Gotcha #7 — context-only, not diffed against SQL.
Record `index.ts` if it is a pure barrel (only re-exports) — context-only, not diffed.

**Step 3 — Write the catalog markdown.** Structure (verbatim headings):

1. `# Phase 226 Plan 01 — SCHEMA-01 Schema ↔ Migration Diff`

2. `## Preamble` — cite Phase 226, Plan 01, requirement SCHEMA-01, D-226-01 (hybrid SQL+meta), D-226-05 (direction `schema<->migration`), D-226-08 (cross-repo READ-only).

3. `## Input universe` — bullet list:
   - SQL side: 7 migrations (list the 7 paths, lines-of-DDL for each).
   - TS side: 30 schema files (list all 30 paths). Note which 29 are in `drizzle.config.ts` and note `indexes.ts` absence per Gotcha #7.
   - Meta side: 7 snapshots (note `0007_snapshot.json` missing → context pointer to Plan 226-02 which owns F-28-226-01).

4. `## Gotchas carried forward` — copy the 10-item list from `<interfaces>` verbatim so executor does not re-flag known noise.

5. `## Per-table verdicts` — for EACH pgTable in the TS side, one subsection:
   ```markdown
   ### Table `<pg_name>` — TS: src/db/schema/<file>.ts:<line>
   
   **Columns**
   | TS col | SQL col | TS type | SQL type | NOT NULL (TS/SQL) | DEFAULT (TS/SQL) | PK (TS/SQL) | Verdict |
   |---|---|---|---|---|---|---|---|
   | ... | ... | ... | ... | ✓/✓ | - / - | ✓/✓ | PASS |
   
   **Indexes / unique indexes**
   | Name | TS declaration | SQL DDL (file:line) | Verdict |
   |---|---|---|---|
   
   **Composite PKs / FKs**
   | Kind | Name | TS | SQL | Verdict |
   
   **Verdict summary:** {PASS | FAIL — F-28-226-NN}
   ```
   For each SQL-side table with no matching TS pgTable, emit a row with TS side = `MISSING` and emit a FAIL finding.
   For each TS pgTable with no matching SQL table, emit same (SQL side MISSING).

6. `## Views subsection` — enumerate every `pgMaterializedView(...)` in `views.ts`. For each, check:
   - Is there a `CREATE MATERIALIZED VIEW "<name>"` in any of the 7 `.sql` files? (Per 226-RESEARCH.md §views.ts, the answer is expected "no for all four".)
   - Is `views.<name>` present in `drizzle/meta/0006_snapshot.json`?
   - Record as INVESTIGATE row with context note: "No CREATE MATERIALIZED VIEW in migrations; creation path presumed runtime (view-refresh.ts, Phase 228 scope). Per 226-RESEARCH.md Q1 recommendation, log as INFO context note, not a finding, until 228 dispositions."

7. `## Enums subsection` — every `pgEnum(...)` in TS ↔ every `CREATE TYPE ... AS ENUM` in SQL ↔ `enums.*` in `0006_snapshot.json`. Table rows: name, TS loc, SQL loc, meta loc, verdict.

8. `## indexes.ts context note` — per Gotcha #7, list the raw-SQL index statements in `indexes.ts`'s `ADDITIONAL_INDEXES` array as CONTEXT ONLY. Not diffed. Explicit note: "Applied outside drizzle-kit lifecycle per D-226-03 / Gotcha #7; orphan scan (Plan 226-04) confirms these indexes as legitimate references."

9. `## Secondary cross-check vs 0006_snapshot.json` — for each table compared above, add a one-line note whether `.sql` cumulative state agrees with `tables["public.<t>"]` in `0006_snapshot.json`. Disagreements are context pointers to Plan 226-02 (do NOT re-emit findings here; 226-02 owns migration-trace findings).

10. `## Finding stubs` — one stub block per FAIL verdict above, format per `<interfaces>` Finding-stub format. Allocate IDs starting at `F-28-226-02` (F-28-226-01 reserved for Plan 226-02 per D-226-06).

11. `## Summary` — subheadings:
    - `### Table coverage`: total pgTables compared (expected ~30 minus barrel/indexes.ts/enum files).
    - `### Column comparison totals`: count of column rows, PASS count, FAIL count.
    - `### Index / unique-index totals`: same.
    - `### Composite PK / FK totals`: same (FK count expected 0 per 226-RESEARCH.md assumption A2; if any found, list here).
    - `### Enum totals`: same.
    - `### Views`: count of mat-views, INVESTIGATE count.
    - `### Finding IDs allocated`: `F-28-226-02` through `F-28-226-<last>`. State the number handed forward to Plan 226-02.
    - `### Severity distribution`: INFO vs LOW counts with LOW justification per finding.

**No writes outside `.planning/phases/226-schema-migration-orphan-audit/`.** Zero writes to `/home/zak/Dev/PurgeGame/database/` per D-226-08.
  </action>
  <verify>
    <automated>test -f .planning/phases/226-schema-migration-orphan-audit/226-01-SCHEMA-MIGRATION-DIFF.md && grep -c "^### Table \`" .planning/phases/226-schema-migration-orphan-audit/226-01-SCHEMA-MIGRATION-DIFF.md && grep -c "^## " .planning/phases/226-schema-migration-orphan-audit/226-01-SCHEMA-MIGRATION-DIFF.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `.planning/phases/226-schema-migration-orphan-audit/226-01-SCHEMA-MIGRATION-DIFF.md`
    - Contains exactly these top-level section headings (grep `^## `): `## Preamble`, `## Input universe`, `## Gotchas carried forward`, `## Per-table verdicts`, `## Views subsection`, `## Enums subsection`, `## indexes.ts context note`, `## Secondary cross-check vs 0006_snapshot.json`, `## Finding stubs`, `## Summary`
    - At least one `### Table \`` subsection exists for EVERY pg_name returned by `rg -n --no-heading "= pgTable\('([a-z_0-9]+)'" /home/zak/Dev/PurgeGame/database/src/db/schema/ --replace '$1' | sort -u` — verified by counting unique pg_names and matching row count
    - Every *.ts file under `/home/zak/Dev/PurgeGame/database/src/db/schema/` has at least one row in the catalog (via Table subsection, Views subsection, Enums subsection, or indexes.ts context note) — verified by `rg -l '^export const' /home/zak/Dev/PurgeGame/database/src/db/schema/*.ts` file-count ↔ catalog file-reference count
    - Every finding stub matches regex `^#### F-28-226-\d+:` and contains the five required lines: `- **Severity:**`, `- **Direction:** schema<->migration`, `- **Phase:** 226`, `- **File:** \`/home/zak/Dev/PurgeGame/database/`, `- **Resolution:**`
    - No finding stub uses ID `F-28-226-01` (reserved for Plan 226-02 per D-226-06)
    - Every `**File:**` citation starts with the absolute path `/home/zak/Dev/PurgeGame/database/`
    - `## Summary` contains all 8 subheadings listed in the action step 11
    - Zero writes occurred to `/home/zak/Dev/PurgeGame/database/` (verifiable via `git -C /home/zak/Dev/PurgeGame/database status --porcelain | wc -l` == 0 or unchanged from pre-task state)
  </acceptance_criteria>
  <done>
    `226-01-SCHEMA-MIGRATION-DIFF.md` exists with per-table verdicts covering every pgTable in `src/db/schema/`, every column / index / FK / composite PK diffed against cumulative `.sql` state, views + enums subsections present, `indexes.ts` handled as context-note per Gotcha #7, secondary cross-check against `0006_snapshot.json` recorded, every FAIL emits an `F-28-226-NN` stub (NN ≥ 02) with direction `schema<->migration`, and Summary footer reports all totals + finding-ID range handed forward to Plan 226-02. Zero writes to `/home/zak/Dev/PurgeGame/database/`.
  </done>
</task>

</tasks>

<threat_model>
N/A — read-only catalog audit; produces audit documentation only, modifies no production code. Per D-226-10 no runtime gate is shipped.
</threat_model>

<verification>
- Every pg_name in `rg "pgTable\('([a-z_0-9]+)'" /home/zak/Dev/PurgeGame/database/src/db/schema/` has a `### Table \`<pg_name>\`` subsection in the catalog.
- Every .sql CREATE TABLE has a matching catalog subsection OR a FAIL finding for missing TS side.
- Every FAIL verdict has a corresponding `F-28-226-NN` stub (NN ≥ 02).
- Every finding cites a File:line under `/home/zak/Dev/PurgeGame/database/`.
- Gotchas (identity sequence defaults, timestamp/bigint modes, composite-PK auto-names, DEFAULT literal normalization, indexes.ts raw-SQL, empty 0005, 0007 anomaly, absent mat-view DDL) are NOT logged as findings — they appear in `## Gotchas carried forward` and `## Views subsection` / `## indexes.ts context note` instead.
- Severity promotions to LOW justified inline with silent-data-corruption or wrong-query-result rationale.
- Finding-ID `F-28-226-01` is NOT used by this plan (reserved for 226-02).
</verification>

<success_criteria>
SCHEMA-01 satisfied when the catalog enumerates every pgTable / pgMaterializedView / pgEnum in the schema tree, diffs each column / index / FK / composite PK against cumulative `.sql` state, records every drift as an `F-28-226-NN` stub with direction `schema<->migration`, and the secondary `0006_snapshot.json` cross-check is recorded for every table.
</success_criteria>

<output>
After completion, create `.planning/phases/226-schema-migration-orphan-audit/226-01-SUMMARY.md` summarizing: tables audited, columns compared, indexes compared, FAIL counts per category, F-28-226-NN range consumed, and the next available finding ID for Plan 226-02.
</output>
</content>
