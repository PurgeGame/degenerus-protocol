---
phase: 228
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-02-VIEW-REFRESH-AUDIT.md
  - .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-02-SUMMARY.md
autonomous: true
requirements: [IDX-05]
requirements_addressed: [IDX-05]
finding_id_block: F-28-228-101+
absorbs_227_deferrals: [3]
severity_taxonomy: [INFO, LOW, MEDIUM]
tags: [IDX-05, view-refresh, materialized-views, state-machine, catalog-audit, read-only]
must_haves:
  truths:
    - "228-02-VIEW-REFRESH-AUDIT.md enumerates every refresh trigger in view-refresh.ts and cross-references against (1) in-source comments in indexer/*.ts AND (2) schema view definitions in views.ts per D-228-08"
    - "All 4 M-matrix rows assigned to 228-02 (M9, M10, M11, M12) appear verbatim as audit rows with PASS/FAIL verdicts + rationale"
    - "Phase 227 deferral #3 (view-refresh.ts:5 refresh-failure alerting policy) appears as an explicit labelled row with '227-deferral-3' annotation"
    - "All 4 pgMaterializedViews in views.ts (mv_player_summary, mv_coinflip_top10, mv_affiliate_leaderboard, mv_baf_top4) have an explicit trigger-to-view mapping row"
    - "Every FAIL row emits an F-28-228-NN finding stub starting at F-28-228-101 (no overlap with 228-01's F-28-228-01+ block)"
    - "Every finding File:line cites an absolute path under /home/zak/Dev/PurgeGame/database/"
    - "Zero writes to /home/zak/Dev/PurgeGame/database/"
    - "SC-4 file-touch evidence: view-refresh.ts + views.ts cited; contribution to the 9-indexer-file coverage goal documented"
  artifacts:
    - path: ".planning/phases/228-cursor-reorg-view-refresh-state-machines/228-02-VIEW-REFRESH-AUDIT.md"
      provides: "Per-trigger staleness-model verdict table cross-referenced against indexer comments + views.ts; F-28-228-NN finding stubs in F-28-228-101+ block"
      contains: "| Trigger site | View | Staleness claim | Verdict |"
    - path: ".planning/phases/228-cursor-reorg-view-refresh-state-machines/228-02-SUMMARY.md"
      provides: "Verdict counts, findings consumed, next available F-28-228-NN, Phase 229 handoff"
  key_links:
    - from: "228-02-VIEW-REFRESH-AUDIT.md trigger-to-view mapping"
      to: "/home/zak/Dev/PurgeGame/database/src/db/schema/views.ts (4 pgMaterializedView entries) + /home/zak/Dev/PurgeGame/database/src/indexer/view-refresh.ts"
      via: "ALL_VIEWS iteration at view-refresh.ts:26-34 mapped to views.ts:16-88 mv definitions"
      pattern: "pgMaterializedView|ALL_VIEWS|refreshMaterializedView"
    - from: "M-matrix rows M9..M12"
      to: "228-02-VIEW-REFRESH-AUDIT.md row IDs"
      via: "Verbatim row copy from 228-RESEARCH.md §Documented-Invariant vs Code-Behavior Matrix"
      pattern: "M(9|10|11|12)"
---

<objective>
IDX-05 — Produce `228-02-VIEW-REFRESH-AUDIT.md`, enumerating every refresh trigger in `/home/zak/Dev/PurgeGame/database/src/indexer/view-refresh.ts` and cross-referencing each trigger against TWO staleness-model sources per D-228-08: (1) in-source comments inside `view-refresh.ts` and referring `indexer/*.ts`; (2) Drizzle schema view definitions in `/home/zak/Dev/PurgeGame/database/src/db/schema/views.ts`. Absorbs Phase 227 deferral #3 (view-refresh.ts:5 refresh-failure alerting policy) per D-228-09.

Purpose: close the IDX-05 behavioral audit surface; feed Phase 229 with F-28-228-NN finding stubs in the reserved `F-28-228-101+` block.
Output: `228-02-VIEW-REFRESH-AUDIT.md` + `228-02-SUMMARY.md`.

Per D-228-01: cross-repo READ-only. Zero writes to `/home/zak/Dev/PurgeGame/database/`.
Per D-228-02: catalog-only — no runtime gate, no CI.
Per D-228-10: finding IDs start at `F-28-228-101` (disjoint from 228-01's `F-28-228-01+` block for parallel-Wave-1 safety).
Per D-228-11: severity ∈ {INFO, LOW, MEDIUM}; HIGH/CRITICAL reserved for Phase 229 promotion.
Per D-228-08: downstream API consumers OUT OF SCOPE — if an obvious consumer mismatch surfaces it may be noted as INFO context, not a standalone finding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/228-cursor-reorg-view-refresh-state-machines/228-CONTEXT.md
@.planning/phases/228-cursor-reorg-view-refresh-state-machines/228-RESEARCH.md
@.planning/phases/228-cursor-reorg-view-refresh-state-machines/228-VALIDATION.md
@.planning/phases/227-indexer-event-processing-correctness/227-03-SUMMARY.md
@.planning/phases/226-schema-migration-orphan-audit/226-01-SCHEMA-MIGRATION-DIFF.md

<interfaces>
M-matrix rows pre-assigned to 228-02 (copied verbatim from 228-RESEARCH.md §Documented-Invariant vs Code-Behavior Matrix). Every row below MUST appear as an explicit audit row in `228-02-VIEW-REFRESH-AUDIT.md` with final verdict + rationale. Expected verdicts are priors — executor re-verifies.

| # | File:line | Claim (comment or docstring) | Code behavior | Expected verdict | Plan |
|---|-----------|------------------------------|---------------|------------------|------|
| M9 | view-refresh.ts:4-5 "Refresh is non-fatal — a stale view for one block is acceptable" | try/catch per view; no metric/alert/retry | LOW — staleness bound is not "one block" under sustained failure | 228-02 |
| M10 | views.ts:9-10 "UNIQUE index to enable REFRESH CONCURRENTLY" | VIEW_UNIQUE_INDEXES × 4; all CREATE UNIQUE INDEX; view-refresh.ts calls .concurrently() | PASS | 228-02 |
| M11 | view-refresh.ts:1-8 "Called after each block batch commit (outside the transaction)" | Call at main.ts:214 is after `await db.transaction(...)` closes at :201 | PASS | 228-02 |
| M12 | (undocumented) Per-view trigger granularity | All 4 views refreshed together, every trigger | INFO — document as design choice, no finding | 228-02 |

Phase 227 deferral absorbed into 228-02 (from 227-03-SUMMARY.md §Phase 228 Handoff):

| # | File:line | Deferred concern | 228-02 row label |
|---|-----------|------------------|------------------|
| 227-deferral-3 | `/home/zak/Dev/PurgeGame/database/src/indexer/view-refresh.ts:5` | try/catch swallows refresh failures — only feedback is log.error. Staleness policy: metric/alerting for sustained refresh failures beyond one block? | Row overlaps M9 — annotate row with "227-deferral-3"; emit a LOW finding in F-28-228-101+ block with resolution RESOLVED-CODE-FUTURE (deferred-ideas per 228-CONTEXT.md) |

Trigger map (from 228-RESEARCH.md §View-Refresh State Machine §Trigger map, verified 2026-04-15):

| Trigger site | Condition | Views refreshed | Error handling | Debounce / rate-limit |
|--------------|-----------|-----------------|----------------|-----------------------|
| main.ts:214 `refreshMaterializedViews(db)` | `lag = Number(tip - batchEnd); lag <= config.batchSize` (main.ts:212-213) — follow-mode gate | ALL 4 via `ALL_VIEWS` iteration | per-view try/catch; log.error + continue | NONE — refresh fires every batch in follow mode |
| main.ts:112 `ensureViewIndexes(db)` | Startup once (index bootstrap, NOT a refresh trigger) | N/A | Fatal on error | N/A |

4 materialized views in views.ts (from 228-RESEARCH.md and 226-01):

| View | Defined at | Has trigger? | On-demand? |
|------|-----------|--------------|------------|
| mv_player_summary | /home/zak/Dev/PurgeGame/database/src/db/schema/views.ts:16-40 | ✓ via V1 loop | No |
| mv_coinflip_top10 | /home/zak/Dev/PurgeGame/database/src/db/schema/views.ts:45-56 | ✓ via V1 loop | No |
| mv_affiliate_leaderboard | /home/zak/Dev/PurgeGame/database/src/db/schema/views.ts:61-72 | ✓ via V1 loop | No |
| mv_baf_top4 | /home/zak/Dev/PurgeGame/database/src/db/schema/views.ts:77-88 | ✓ via V1 loop | No |

Observability inventory for 227-deferral-3 (from 228-RESEARCH.md §View-Refresh State Machine):

| Channel | Present? | Notes |
|---------|----------|-------|
| log.error | ✓ | Pino-structured; `{view, err}` |
| Prometheus / metric counter | ✗ | None in view-refresh.ts or post-refresh main.ts |
| Alert / PagerDuty / webhook | ✗ | None |
| Retry / backoff | ✗ | Only next follow-mode batch re-triggers |
| Per-view staleness timestamp in DB | ✗ | No view_refresh_state table |
| Health check / /healthz surface | ✗ | Not exposed |

INFO context (from 228-RESEARCH.md §Views.ts ↔ view-refresh.ts cross-reference): `views.ts` declares 4 pgMaterializedView entries but no `CREATE MATERIALIZED VIEW` appears in any `.sql` migration [CITED: 226-01-SCHEMA-MIGRATION-DIFF.md:708-717]. Runtime path is drizzle-kit push/snapshot — unverified. Record as single INFO row with `Resolution: INFO-ACCEPTED` or `RESOLVED-CODE-FUTURE`. Not a standalone 228-02 finding.

Severity thresholds (D-228-11):
- INFO: documented behavior matches; minor comment drift
- LOW: behavior diverges from comment but no data-integrity risk
- MEDIUM: silent data corruption or missed refresh-recovery risk
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed 228-02-VIEW-REFRESH-AUDIT.md with the 4 M-rows, trigger-to-view mapping, and absorbed 227-deferral-3 row</name>
  <files>.planning/phases/228-cursor-reorg-view-refresh-state-machines/228-02-VIEW-REFRESH-AUDIT.md</files>
  <read_first>
    - /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/228-cursor-reorg-view-refresh-state-machines/228-RESEARCH.md (FULL file — §View-Refresh State Machine, §Documented-Invariant vs Code-Behavior Matrix rows M9..M12, §Views.ts ↔ view-refresh.ts cross-reference)
    - /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/228-cursor-reorg-view-refresh-state-machines/228-CONTEXT.md (D-228-08 two-source cross-ref, D-228-09 deferral absorption)
    - /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/227-indexer-event-processing-correctness/227-03-SUMMARY.md (§Phase 228 Handoff — deferral #3)
    - /home/zak/Dev/PurgeGame/database/src/indexer/view-refresh.ts (full file)
    - /home/zak/Dev/PurgeGame/database/src/db/schema/views.ts (full file — 4 pgMaterializedView definitions)
  </read_first>
  <action>
Create `228-02-VIEW-REFRESH-AUDIT.md` with this top-matter and scaffold. Copy every M-row in `<interfaces>` into the Audit Rows table VERBATIM (File:line, Claim, Code behavior, Expected verdict) — do NOT paraphrase. Final Verdict + Rationale left blank for Task 2.

```markdown
# 228-02 View-Refresh Audit (IDX-05)

**Phase:** 228 | **Plan:** 02 | **Requirement:** IDX-05
**Deliverable for:** D-228-06 (228-02 deliverable)
**Finding block:** F-28-228-101+ (per D-228-10; disjoint from 228-01's F-28-228-01+)
**Severity taxonomy:** INFO / LOW / MEDIUM (per D-228-11)
**Cross-ref sources (D-228-08):** (1) in-source comments in view-refresh.ts + indexer/*.ts; (2) /home/zak/Dev/PurgeGame/database/src/db/schema/views.ts
**Out of scope:** downstream API consumers (per D-228-08 — Phase 224 already audited API routes)
**Scope:** cross-repo READ-only per D-228-01; catalog-only per D-228-02.
**Audit surface:** /home/zak/Dev/PurgeGame/database/src/indexer/view-refresh.ts + /home/zak/Dev/PurgeGame/database/src/db/schema/views.ts + /home/zak/Dev/PurgeGame/database/src/indexer/main.ts (trigger wiring at :112, :211-215)

## Absorbed Phase 227 Deferral (D-228-09)

| # | 227 deferral | File:line | Handled by row |
|---|--------------|-----------|----------------|
| 3 | view-refresh.ts:5 try/catch swallows refresh failures — only feedback is log.error | /home/zak/Dev/PurgeGame/database/src/indexer/view-refresh.ts:5 | M9 (annotated `227-deferral-3`); observability inventory table reproduced below |

## Trigger Map (exhaustive — from 228-RESEARCH.md §View-Refresh State Machine)

| Trigger site (File:line) | Condition | Views refreshed | Error handling | Debounce / rate-limit |
|--------------------------|-----------|-----------------|----------------|-----------------------|
| /home/zak/Dev/PurgeGame/database/src/indexer/main.ts:214 | `lag = Number(tip - batchEnd); lag <= config.batchSize` (follow-mode gate at main.ts:212-213) | ALL 4 via ALL_VIEWS loop at /home/zak/Dev/PurgeGame/database/src/indexer/view-refresh.ts:26-34 | per-view try/catch; log.error + continue | NONE |
| /home/zak/Dev/PurgeGame/database/src/indexer/main.ts:112 | Startup once (index bootstrap only; NOT a refresh trigger) | N/A — ensureViewIndexes | Fatal on error (throw) | N/A |

## Trigger-to-View Mapping (all 4 pgMaterializedViews per D-228-08 source 2)

| View name | views.ts definition | Triggered by | Per-view on-demand trigger? | Verdict |
|-----------|---------------------|--------------|------------------------------|---------|
| mv_player_summary | /home/zak/Dev/PurgeGame/database/src/db/schema/views.ts:16-40 | main.ts:214 via ALL_VIEWS loop | No | TBD-Task2 |
| mv_coinflip_top10 | /home/zak/Dev/PurgeGame/database/src/db/schema/views.ts:45-56 | main.ts:214 via ALL_VIEWS loop | No | TBD-Task2 |
| mv_affiliate_leaderboard | /home/zak/Dev/PurgeGame/database/src/db/schema/views.ts:61-72 | main.ts:214 via ALL_VIEWS loop | No | TBD-Task2 |
| mv_baf_top4 | /home/zak/Dev/PurgeGame/database/src/db/schema/views.ts:77-88 | main.ts:214 via ALL_VIEWS loop | No | TBD-Task2 |

## Observability Inventory (227-deferral-3 source)

| Channel | Present? | Notes |
|---------|----------|-------|
| log.error | ✓ | Pino-structured; `{view, err}` at view-refresh.ts:31 |
| Prometheus / metric counter | ✗ | None |
| Alert / PagerDuty / webhook | ✗ | None |
| Retry / backoff | ✗ | Only next follow-mode batch re-triggers |
| Per-view staleness timestamp in DB | ✗ | No view_refresh_state table |
| Health check / /healthz surface | ✗ | Not exposed |

## Audit Rows (M-matrix — verbatim from 228-RESEARCH.md)

| Row ID | Annotations | File:line | Claim | Code behavior | Expected verdict | Final verdict | Rationale | Finding ID (if FAIL/LOW) |
|--------|-------------|-----------|-------|---------------|------------------|---------------|-----------|---------------------------|
| M9 | 227-deferral-3 | /home/zak/Dev/PurgeGame/database/src/indexer/view-refresh.ts:4-5 | "Refresh is non-fatal — a stale view for one block is acceptable" | try/catch per view; no metric/alert/retry | LOW — staleness bound is not "one block" under sustained failure | TBD-Task2 | TBD | TBD |
| M10 | — | /home/zak/Dev/PurgeGame/database/src/db/schema/views.ts:9-10 | "UNIQUE index to enable REFRESH CONCURRENTLY" | VIEW_UNIQUE_INDEXES × 4; all CREATE UNIQUE INDEX; view-refresh.ts calls .concurrently() | PASS | TBD | TBD | TBD |
| M11 | — | /home/zak/Dev/PurgeGame/database/src/indexer/view-refresh.ts:1-8 | "Called after each block batch commit (outside the transaction)" | Call at main.ts:214 is after `await db.transaction(...)` closes at :201 | PASS | TBD | TBD | TBD |
| M12 | — | (undocumented) | Per-view trigger granularity | All 4 views refreshed together, every trigger | INFO — design choice, no finding | TBD | TBD | TBD |
| V1 | info-context | /home/zak/Dev/PurgeGame/database/src/db/schema/views.ts:16-88 vs /home/zak/Dev/PurgeGame/database/drizzle/*.sql | views.ts declares 4 pgMaterializedView entries; no `CREATE MATERIALIZED VIEW` in any migration SQL (cited 226-01-SCHEMA-MIGRATION-DIFF.md:708-717) | Runtime materialization path unclear — drizzle-kit push/snapshot assumed | INFO — record context only; INFO-ACCEPTED or RESOLVED-CODE-FUTURE | TBD | TBD | TBD |

## SC-4 File-Touch Evidence

| Indexer file | Touched by 228-02? | Where |
|--------------|--------------------|-------|
| view-refresh.ts | ✓ | M9, M11, Trigger Map, Observability Inventory |
| main.ts | ✓ | Trigger Map (main.ts:112, :214), M11 cross-ref |
| views.ts (db/schema) | ✓ | M10, Trigger-to-View Mapping (4 rows), V1 |
```

Do NOT fill in Final Verdict / Rationale / Finding ID columns — Task 2 does that. Task 1 produces the SCAFFOLD with verbatim M-rows + trigger-to-view mapping so Task 2 has a fixed target.
  </action>
  <verify>
    <automated>test -f .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-02-VIEW-REFRESH-AUDIT.md && grep -cE '^\| M(9|10|11|12) ' .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-02-VIEW-REFRESH-AUDIT.md | awk '{ if ($1 < 4) exit 1 }' && grep -q 'mv_player_summary' .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-02-VIEW-REFRESH-AUDIT.md && grep -q 'mv_coinflip_top10' .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-02-VIEW-REFRESH-AUDIT.md && grep -q 'mv_affiliate_leaderboard' .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-02-VIEW-REFRESH-AUDIT.md && grep -q 'mv_baf_top4' .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-02-VIEW-REFRESH-AUDIT.md && grep -q '227-deferral-3' .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-02-VIEW-REFRESH-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `228-02-VIEW-REFRESH-AUDIT.md` exists.
    - All 4 M-rows assigned to 228-02 (M9, M10, M11, M12) appear in the Audit Rows table with File:line, Claim, Code behavior, Expected verdict copied VERBATIM from 228-RESEARCH.md.
    - Row V1 (views.ts materialization-path INFO context from 226-01) appears.
    - Trigger-to-View Mapping table contains 4 rows — one per pgMaterializedView (mv_player_summary, mv_coinflip_top10, mv_affiliate_leaderboard, mv_baf_top4) — each citing its views.ts line range.
    - 227-deferral-3 annotation appears on M9.
    - Observability Inventory table present with 6 channel rows.
    - Trigger Map table has both trigger sites (main.ts:214 refresh + main.ts:112 ensure-indexes bootstrap).
    - SC-4 File-Touch Evidence table present with 3 rows (view-refresh.ts, main.ts, views.ts).
    - Every File:line citation is an absolute path rooted at `/home/zak/Dev/PurgeGame/database/`.
    - Zero writes outside `.planning/phases/228-cursor-reorg-view-refresh-state-machines/` (verify: `git -C /home/zak/Dev/PurgeGame/database status` clean).
  </acceptance_criteria>
  <done>Scaffold committed with 4 M-rows, V1 info-context row, trigger-to-view mapping for all 4 views, observability inventory, and 227-deferral-3 annotation.</done>
</task>

<task type="auto">
  <name>Task 2: Fill every row's Final Verdict + Rationale, emit F-28-228-NN stubs in the F-28-228-101+ block, write SUMMARY</name>
  <files>
    .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-02-VIEW-REFRESH-AUDIT.md
    .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-02-SUMMARY.md
  </files>
  <read_first>
    - The 228-02-VIEW-REFRESH-AUDIT.md scaffold produced by Task 1
    - /home/zak/Dev/PurgeGame/database/src/indexer/view-refresh.ts (full file — re-read for verdict verification)
    - /home/zak/Dev/PurgeGame/database/src/db/schema/views.ts (full file — verify VIEW_UNIQUE_INDEXES and ALL_VIEWS exports)
    - /home/zak/Dev/PurgeGame/database/src/indexer/main.ts (lines 111-115 for ensureViewIndexes; lines 188-215 for tx-close + refresh-gate + refresh-call)
    - /home/zak/Dev/PurgeGame/degenerus-audit/.planning/phases/226-schema-migration-orphan-audit/226-01-SCHEMA-MIGRATION-DIFF.md (lines 708-778, §Views — for V1 INFO context)
    - /home/zak/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
Step A — For every row (M9, M10, M11, M12, V1) fill:
- **Final verdict** — PASS / PASS-with-INFO / PASS-with-LOW / LOW / MEDIUM.
- **Rationale** — ≤3 sentences citing File:line evidence. Reference the enclosing function body, not just the claim line.
- **Finding ID** — only for rows warranting a stub (typically LOW, MEDIUM, and select INFO rows per D-228-11). Assign sequentially from `F-28-228-101`.

Row-by-row guidance (priors from 228-RESEARCH.md — executor verifies):
- **M9** (227-deferral-3): PASS-with-LOW. Emit `F-28-228-101` — comment "stale view for one block is acceptable" overstates the bound; under sustained refresh failure the staleness is indefinite with only log.error as signal. Direction `comment→code`; resolution `RESOLVED-CODE-FUTURE` (per 228-CONTEXT.md Deferred Ideas — do NOT design alerting in this phase). Rationale cites the 6-row observability inventory.
- **M10**: PASS. Re-verify by re-reading views.ts exports: ALL_VIEWS array, VIEW_UNIQUE_INDEXES array, and ADDITIONAL_INDEXES. Confirm all 4 entries have `CREATE UNIQUE INDEX IF NOT EXISTS ...` and view-refresh.ts:29 calls `.concurrently()`.
- **M11**: PASS. Re-verify by re-reading main.ts:188-214: `await db.transaction(async (tx) => { ... })` closes at :201; `if (lag <= config.batchSize)` at :213; `await refreshMaterializedViews(db)` at :214 (uses `db`, not `tx`) — confirms "outside the transaction" claim.
- **M12**: PASS-with-INFO. Per-view trigger granularity is coarse (all 4 views refreshed together every trigger); this is a design choice not a defect. Emit `F-28-228-NN` INFO stub only if the executor judges it worth documenting for Phase 229 consolidation — otherwise record as INFO in the row, no finding.
- **V1**: INFO. Views.ts has 4 pgMaterializedView entries with no matching `CREATE MATERIALIZED VIEW` in any `.sql` migration (per 226-01:708-717). Record as single INFO row with resolution `INFO-ACCEPTED` or `RESOLVED-CODE-FUTURE`. Per D-228-08 context note ("runtime creation path unclear — out of phase scope per 226-01 handoff"), do NOT emit a finding — record in the row and cite 226-01 as source.

Step B — Cross-reference validation per D-228-08 two sources:
- Source 1 (in-source comments): re-grep `rg -n 'stale|non-fatal|concurrently|refresh' /home/zak/Dev/PurgeGame/database/src/indexer/view-refresh.ts` and compare each hit against the Audit Rows.
- Source 2 (schema views.ts): re-grep `rg -n 'pgMaterializedView' /home/zak/Dev/PurgeGame/database/src/db/schema/views.ts` — confirm exactly 4 hits, each matching a row in the Trigger-to-View Mapping.
Append a `## Cross-Reference Validation` section documenting both greps' output and the match.

Step C — Finding stub format (append section `## Findings` at the bottom of 228-02-VIEW-REFRESH-AUDIT.md):

```markdown
#### F-28-228-NN: <short title>

- **Severity:** INFO | LOW | MEDIUM
- **Direction:** comment→code | docs→code | schema↔view
- **Phase:** 228
- **Requirement:** IDX-05
- **Origin row:** M9 | M10 | M11 | M12 | V1
- **File:** /home/zak/Dev/PurgeGame/database/src/indexer/view-refresh.ts:<line> (or views.ts:<line>)
- **Resolution (proposed):** RESOLVED-DOC | RESOLVED-CODE | RESOLVED-CODE-FUTURE | INFO-ACCEPTED
- **Evidence:** <1-3 line explanation citing File:line>
```

Finding IDs run contiguously from `F-28-228-101`. Do NOT emit any ID < 101 (that block is reserved for 228-01).

Step D — SC-4 file-touch evidence. Update the SC-4 table if any additional indexer files were incidentally cited; do not expand scope just for coverage.

Step E — Spot-recheck log (per 228-VALIDATION.md §Sampling Rate for 228-02: 2 of {M9, M10, M11, M12}). For each picked row, re-read the enclosing function body (≥15 lines around the cited line) and re-verify. Append a `## Spot-Recheck Log` section mirroring 227-03-SUMMARY.md Re-check 1 / Re-check 2 format.

Step F — Create `228-02-SUMMARY.md` with:
1. Verdict counts (PASS / PASS-with-INFO / PASS-with-LOW / LOW / MEDIUM).
2. Findings consumed (list F-28-228-NN IDs and their severity; all must be ≥ 101).
3. Next available finding ID (first unused in the `F-28-228-101+` block).
4. Handoff to Phase 229: list every finding stub with severity + direction + resolution for consolidation.
5. SC-4 coverage confirmation: indexer files touched by 228-02 (view-refresh.ts; main.ts incidentally via trigger wiring). Combined 228-01 + 228-02 + 227 coverage covers the 9-file requirement.
6. Confirm all 4 pgMaterializedViews have a trigger (no orphan views).
7. Confirm 227-deferral-3 resolved as a LOW finding with RESOLVED-CODE-FUTURE.
8. Scope-boundary reminder: per D-228-01/02 no writes to `database/`; per D-228-08 downstream API consumers out of scope; per D-228-11 HIGH/CRITICAL reserved for Phase 229.
  </action>
  <verify>
    <automated>test -f .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-02-SUMMARY.md && grep -qE 'F-28-228-10[1-9]' .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-02-VIEW-REFRESH-AUDIT.md && ! grep -qE 'F-28-228-0[1-9](?!0)' .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-02-VIEW-REFRESH-AUDIT.md && ! grep -qE '^\|.*\| TBD \|' .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-02-VIEW-REFRESH-AUDIT.md && grep -q 'Spot-Recheck' .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-02-VIEW-REFRESH-AUDIT.md && grep -q 'Cross-Reference Validation' .planning/phases/228-cursor-reorg-view-refresh-state-machines/228-02-VIEW-REFRESH-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - Every row (M9, M10, M11, M12, V1) and each of the 4 Trigger-to-View Mapping rows has a non-TBD Final Verdict and Rationale.
    - At least one `F-28-228-1NN` finding stub exists (expected per priors: ≥1 for M9/227-deferral-3 LOW).
    - All finding IDs are in the `F-28-228-101+` block (no `F-28-228-0NN` emitted — that block belongs to 228-01).
    - Every finding cites an absolute File:line under `/home/zak/Dev/PurgeGame/database/`.
    - Every finding has exactly one severity ∈ {INFO, LOW, MEDIUM}.
    - Cross-Reference Validation section documents both D-228-08 sources' grep output.
    - Spot-Recheck Log has ≥2 re-verifications.
    - `228-02-SUMMARY.md` exists with bucket counts, finding handoff, next-available ID in the 101+ block, and SC-4 coverage confirmation.
    - 227-deferral-3 resolved with a finding stub whose resolution is RESOLVED-CODE-FUTURE (per 228-CONTEXT.md Deferred Ideas — do NOT design alerting).
    - Zero writes outside `.planning/phases/228-cursor-reorg-view-refresh-state-machines/` (verify: `git -C /home/zak/Dev/PurgeGame/database status` clean).
  </acceptance_criteria>
  <done>All rows verdicted; findings emitted in F-28-228-101+ block; SUMMARY committed; Phase 229 handoff explicit; 227-deferral-3 closed.</done>
</task>

</tasks>

<threat_model>
N/A — catalog-only, no runtime gate per D-228-02. This plan produces markdown audit artifacts only; no code executed, no network surface changed, no trust boundary crossed. STRIDE register not applicable. Observability gaps around swallowed refresh failures are captured as a LOW finding under the normal D-228-11 severity taxonomy with resolution `RESOLVED-CODE-FUTURE` per 228-CONTEXT.md Deferred Ideas.
</threat_model>

<verification>
- `228-02-VIEW-REFRESH-AUDIT.md` and `228-02-SUMMARY.md` exist.
- All 4 M-rows (M9, M10, M11, M12) + V1 INFO context row have verdicts + rationale.
- 227-deferral-3 annotated on M9 and resolved via a LOW finding in F-28-228-101+ block with RESOLVED-CODE-FUTURE.
- All 4 pgMaterializedViews (mv_player_summary, mv_coinflip_top10, mv_affiliate_leaderboard, mv_baf_top4) have a trigger-to-view mapping row.
- Finding IDs consumed in `F-28-228-101+` block only (no collision with 228-01's F-28-228-01+).
- Cross-Reference Validation section documents both D-228-08 sources.
- Zero writes to `/home/zak/Dev/PurgeGame/database/` (git status clean there).
- Spot-recheck log has ≥2 re-verifications per 228-VALIDATION.md.
</verification>

<success_criteria>
- IDX-05 satisfied: view-refresh trigger staleness model catalogued against both sources (comments + views.ts) with PASS/FAIL verdicts.
- 227-deferral-3 resolved as an explicit audit row with LOW finding + RESOLVED-CODE-FUTURE.
- Finding stubs handed to Phase 229 for consolidation.
- All 4 pgMaterializedViews confirmed trigger-covered (no orphans).
- SC-4 indexer-file coverage advanced (view-refresh.ts audit-touched by 228-02; combined with 228-01's 5 files + 227's coverage, all 9 indexer files are audit-touched across 227+228).
</success_criteria>

<output>
After completion, `.planning/phases/228-cursor-reorg-view-refresh-state-machines/228-02-SUMMARY.md` exists with verdict counts, finding handoff, next available finding ID (in F-28-228-101+ block), and SC-4 coverage confirmation that together with 228-01 satisfies the 9-indexer-file requirement.
</output>
</content>
</invoke>