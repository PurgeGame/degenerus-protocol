---
phase: 167-integration-test-baseline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/167-integration-test-baseline/167-02-TEST-BASELINE.md
autonomous: true
requirements: [INTEG-02]

must_haves:
  truths:
    - "All Hardhat test suites pass with no new failures"
    - "All Foundry fuzz tests pass with no new failures"
    - "Test results are documented with pass/fail counts"
  artifacts:
    - path: ".planning/phases/167-integration-test-baseline/167-02-TEST-BASELINE.md"
      provides: "Test baseline verification report"
      contains: "TEST RESULTS"
  key_links:
    - from: "test/unit/*.test.js"
      to: "contracts/"
      via: "Hardhat test runner"
      pattern: "passing.*failing"
    - from: "test/fuzz/*.t.sol"
      to: "contracts/"
      via: "forge test"
      pattern: "test result"
---

<objective>
Run the full Hardhat and Foundry test suites to verify no new failures after v11.0-v14.0 changes.

Purpose: The v11.0-v14.0 delta touched 19 contract files with 1337 insertions and 1542 deletions. Tests must still pass to confirm the changes are integration-safe. This is the final gate before the v15.0 Delta Audit milestone is complete.

Output: `167-02-TEST-BASELINE.md` documenting all test results with pass/fail counts per suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Hardhat test suite and document results</name>
  <files>.planning/phases/167-integration-test-baseline/167-02-TEST-BASELINE.md</files>
  <read_first>
    - hardhat.config.ts (test directory ordering, compiler config)
    - package.json (test script definitions)
  </read_first>
  <action>
Run the full Hardhat test suite and capture results. The Hardhat suite covers: access, deploy, unit, integration, edge, validation, and gas test directories.

**Pre-flight check:**
1. Verify `npx hardhat compile` succeeds (should already be confirmed from Plan 01, but re-verify independently since plans run in parallel).
2. Check that ContractAddresses.sol has no unstaged diffs that could affect test deployment. If it does, note this but do NOT modify the file (per project rules). The stash@{0} labeled "phase-160-all-changes" contains contract changes but those are already committed to HEAD -- the current worktree is clean for contracts/.

**Test execution:**
Run `npm test` which executes: `hardhat test test/unit/*.test.js test/integration/*.test.js test/deploy/*.test.js test/access/*.test.js test/edge/*.test.js test/gas/*.test.js`

Set a generous timeout (10 minutes). The test suite runs against a local Hardhat node with forked state.

**If tests fail:**
- For each failure, record: test file, test name, error message, and whether the failure is EXPECTED (related to v11.0-v14.0 changes) or UNEXPECTED (pre-existing or unrelated).
- A failure in a test that calls a removed function (e.g., `notifyQuestMint`) would be EXPECTED -- the test needs updating, not the contract.
- A failure in a test that exercises unchanged functionality would be UNEXPECTED and a potential regression.

**Documentation:**
Write results to `167-02-TEST-BASELINE.md` with:
- Compilation status
- Per-directory breakdown: access (N passing, M failing), deploy, unit, integration, edge, validation, gas
- Total: N passing, M failing, K pending
- For any failures: table with test name, file, error, classification (EXPECTED/UNEXPECTED)

IMPORTANT: Do NOT modify any test files or contract files. This is verification-only.
  </action>
  <verify>
    <automated>test -f .planning/phases/167-integration-test-baseline/167-02-TEST-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - 167-02-TEST-BASELINE.md exists with Hardhat test results section
    - Pass/fail counts are recorded per test directory
    - Any failures are classified as EXPECTED or UNEXPECTED with reasoning
    - No contract or test files were modified
  </acceptance_criteria>
  <done>Hardhat test results documented with per-directory breakdown. Any failures classified as expected (test needs update for v11.0-v14.0 API changes) or unexpected (potential regression).</done>
</task>

<task type="auto">
  <name>Task 2: Run Foundry fuzz test suite and document results</name>
  <files>.planning/phases/167-integration-test-baseline/167-02-TEST-BASELINE.md</files>
  <read_first>
    - foundry.toml (test config, fuzz runs, solc version)
    - .planning/phases/167-integration-test-baseline/167-02-TEST-BASELINE.md (append to existing)
  </read_first>
  <action>
Run the Foundry fuzz test suite and capture results. The Foundry suite covers: fuzz tests (unit fuzz + invariant fuzz) under test/fuzz/.

**Test execution:**
Run `npm run fuzz` which executes: `forge test --match-path 'test/fuzz/*' -vv`

Set a generous timeout (10 minutes). Foundry fuzz tests run 1000 iterations per test function by default (per foundry.toml).

**If tests fail:**
- For each failure, record: test contract, test function, error message, counterexample (if fuzz), and classification.
- Fuzz failures are more significant than unit test failures since they indicate property violations under random inputs.

**Documentation:**
Append to `167-02-TEST-BASELINE.md` under a "FOUNDRY FUZZ TESTS" section:
- Compilation status (forge build)
- Test results: N passing, M failing
- Per-file breakdown for any failures
- Fuzz-specific: any counterexamples found
- For any failures: table with test name, file, error, classification

**Final summary section:**
Add a "COMBINED BASELINE" section at the bottom:
- Total Hardhat: N passing / M failing
- Total Foundry: N passing / M failing
- Overall verdict: PASS (zero unexpected failures) or FAIL (with details)
- Comparison note: since this is the first baseline for v11.0-v14.0, note that pre-v11.0 baseline counts are not available in this repo -- the success criterion is "no new failures" meaning zero unexpected failures in the current test suite

IMPORTANT: Do NOT modify any test files or contract files. This is verification-only.
  </action>
  <verify>
    <automated>grep -c "COMBINED BASELINE" .planning/phases/167-integration-test-baseline/167-02-TEST-BASELINE.md | xargs test 1 -eq</automated>
  </verify>
  <acceptance_criteria>
    - 167-02-TEST-BASELINE.md contains both Hardhat and Foundry results
    - Foundry fuzz test section has pass/fail counts
    - Combined baseline section with overall verdict
    - No contract or test files were modified
  </acceptance_criteria>
  <done>Full test baseline documented: Hardhat per-directory results + Foundry fuzz results + combined verdict. Zero unexpected failures = PASS, any unexpected failures documented with regression analysis.</done>
</task>

</tasks>

<verification>
- `167-02-TEST-BASELINE.md` exists with Hardhat + Foundry sections + combined baseline
- Zero unexpected test failures (expected failures from removed API calls are acceptable and documented)
- No source files modified
</verification>

<success_criteria>
All Hardhat tests pass with no new failures. All Foundry tests pass with no new failures. Results documented with pass/fail counts and any failures classified.
</success_criteria>

<output>
After completion, create `.planning/phases/167-integration-test-baseline/167-02-SUMMARY.md`
</output>
