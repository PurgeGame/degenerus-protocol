---
phase: 167-integration-test-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/167-integration-test-baseline/167-01-CALL-GRAPH-AUDIT.md
autonomous: true
requirements: [INTEG-01]

must_haves:
  truths:
    - "No contract references a function that was removed in v11.0-v14.0"
    - "No contract references an old function signature that was changed in v11.0-v14.0"
    - "No interface file declares a function that no longer exists in its implementation"
    - "No contract imports an interface that was deleted (DegenerusGameModuleInterfaces.sol)"
  artifacts:
    - path: ".planning/phases/167-integration-test-baseline/167-01-CALL-GRAPH-AUDIT.md"
      provides: "Cross-contract call graph verification report"
      contains: "STALE REFERENCE CHECK"
  key_links:
    - from: "DegenerusQuests.sol"
      to: "IDegenerusQuests.sol"
      via: "interface match"
      pattern: "function signatures match"
    - from: "BurnieCoin.sol"
      to: "IDegenerusCoin.sol"
      via: "interface match"
      pattern: "removed functions not in interface"
    - from: "DegenerusGame.sol"
      to: "IDegenerusGame.sol"
      via: "interface match"
      pattern: "decWindow returns bool only"
---

<objective>
Verify the cross-contract call graph has no broken interfaces or stale references after v11.0-v14.0 changes.

Purpose: The v11.0-v14.0 delta removed 30+ functions, renamed modifiers, changed function signatures, deleted an entire interface file, and moved constants to centralized storage. Any stale reference to a removed/renamed symbol would cause a compile error or (worse) silently call the wrong function. This plan systematically verifies every removal/rename from the 162-CHANGELOG has no remaining consumers.

Output: `167-01-CALL-GRAPH-AUDIT.md` with pass/fail for every removed/renamed/re-signed function.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-changelog-extraction/162-CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify no stale references to removed functions</name>
  <files>.planning/phases/167-integration-test-baseline/167-01-CALL-GRAPH-AUDIT.md</files>
  <read_first>
    - .planning/phases/162-changelog-extraction/162-CHANGELOG.md (full removed functions list)
    - contracts/BurnieCoin.sol
    - contracts/BurnieCoinflip.sol
    - contracts/DegenerusAffiliate.sol
    - contracts/DegenerusGame.sol
    - contracts/DegenerusQuests.sol
    - contracts/interfaces/IDegenerusCoin.sol
    - contracts/interfaces/IDegenerusGame.sol
    - contracts/interfaces/IDegenerusQuests.sol
    - contracts/storage/DegenerusGameStorage.sol
    - contracts/modules/DegenerusGameAdvanceModule.sol
    - contracts/modules/DegenerusGameMintModule.sol
    - contracts/modules/DegenerusGameJackpotModule.sol
    - contracts/modules/DegenerusGameLootboxModule.sol
    - contracts/modules/DegenerusGameDegeneretteModule.sol
    - contracts/modules/DegenerusGameWhaleModule.sol
    - contracts/modules/DegenerusGameEndgameModule.sol
    - contracts/modules/DegenerusGameGameOverModule.sol
    - contracts/modules/DegenerusGameBoonModule.sol
    - contracts/modules/DegenerusGameMintStreakUtils.sol
  </read_first>
  <action>
Systematically grep the ENTIRE contracts/ directory for every removed/renamed/re-signed symbol from 162-CHANGELOG. The search must cover ALL .sol files under contracts/ (not just the ones listed above -- mocks, test contracts, and unlisted files can also have stale refs).

**Category A: Removed functions (must have ZERO references as call targets)**

For each of these, grep contracts/ recursively. References in comments/NatSpec are acceptable. References as function calls, interface declarations, or selector constants are NOT acceptable.

1. `affiliateQuestReward` -- removed from BurnieCoin.sol
2. `rollDailyQuest` as called on BurnieCoin (coin.rollDailyQuest) -- moved to DegenerusQuests
3. `notifyQuestMint` -- removed from BurnieCoin/IDegenerusCoin
4. `notifyQuestLootBox` -- removed from BurnieCoin/IDegenerusCoin
5. `notifyQuestDegenerette` -- removed from BurnieCoin/IDegenerusCoin
6. `_questApplyReward` -- private helper eliminated
7. `decWindowOpenFlag` -- merged into decWindow()
8. `_isGameoverImminent` -- replaced by gameOverPossible flag
9. `_activeTicketLevel` as a function in DegenerusGame.sol (moved to MintStreakUtils)
10. `_threeDayRngGap` -- removed entirely
11. `_playerActivityScoreInternal` -- moved to MintStreakUtils as _playerActivityScore
12. `deityPassCountFor` -- replaced by hasDeityPass
13. `_rollWeightedAffiliateWinner` -- replaced by inline mod-20
14. `IDegenerusCoinModule` -- entire interface file deleted
15. `_clearDailyEthState` -- eliminated from JackpotModule
16. `DailyCarryoverStarted` event -- removed
17. `DailyQuestRolled` event on BurnieCoin -- moved to DegenerusQuests
18. `QuestCompleted` event on BurnieCoin -- moved to DegenerusQuests
19. `OnlyAffiliate` error -- removed from BurnieCoin
20. `OnlyTrustedContracts` error -- removed from BurnieCoin
21. `COIN_PURCHASE_CUTOFF` / `COIN_PURCHASE_CUTOFF_LVL0` -- replaced by gameOverPossible
22. `BURNIE_LOOT_CUTOFF` / `BURNIE_LOOT_CUTOFF_LVL0` -- replaced by gameOverPossible
23. `_maybeAwardConsolation` / `ConsolationPrize` event -- removed from DegeneretteModule
24. `CONSOLATION_MIN_ETH` / `CONSOLATION_MIN_BURNIE` / `CONSOLATION_MIN_WWXRP` / `CONSOLATION_PRIZE_WWXRP` -- removed
25. `_rollDailyQuest` (private, was in DegenerusQuests) -- inlined into rollDailyQuest
26. `STAGE_JACKPOT_ETH_RESUME` -- constant eliminated
27. `DAILY_REWARD_JACKPOT_LOOTBOX_BPS` -- removed
28. `DAILY_CARRYOVER_MIN_WINNERS` -- removed

**Category B: Renamed symbols (old name must have ZERO references)**

29. `onlyDegenerusGameContract` modifier -- renamed to `onlyGame`
30. `onlyTrustedContracts` modifier -- renamed to `onlyGame`
31. `IDegenerusCoinAffiliate` interface -- replaced by `IDegenerusQuestsAffiliate`
32. `deityPassCount` mapping reads/writes -- replaced by mintPacked_ bit operations

**Category C: Changed signatures (callers must use new signature)**

33. `decWindow()` -- old signature returned `(bool, uint24)`, new returns `(bool)` only. Grep for any caller destructuring two return values from decWindow.
34. `handleMint` / `handleLootBox` / `handleDegenerette` on DegenerusQuests -- added mintPrice parameter. Verify all callers pass the new parameter count.
35. `rollDailyQuest` -- return type removed (was `(bool,uint8[2],bool)`, now void). Verify no caller captures return values.

**Category D: Moved constants (old locations must not re-declare)**

36. `coin`, `coinflip`, `affiliate`, `dgnrs`, `questView` constants -- moved to DegenerusGameStorage. Verify NO module still declares its own copy (they should inherit from Storage).

For each item, record: symbol name, grep command used, result (CLEAN or STALE with file:line), and verdict.

**Method:** Use `grep -rn "PATTERN" contracts/ --include="*.sol"` for each symbol. Exclude the definition site (the file where it SHOULD still exist, if any). Exclude comment-only matches (lines starting with `//` or `*`).

Write results to `167-01-CALL-GRAPH-AUDIT.md` with a master table of all 36 checks plus detailed grep output for any non-CLEAN results.

IMPORTANT: Do NOT modify any contract files. This is audit-only. Also do NOT touch ContractAddresses.sol per project rules.
  </action>
  <verify>
    <automated>grep -c "STALE" .planning/phases/167-integration-test-baseline/167-01-CALL-GRAPH-AUDIT.md | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - 167-01-CALL-GRAPH-AUDIT.md exists and contains a master table with all 36 checks
    - Every check has a verdict of CLEAN or STALE
    - If any STALE results exist, they are documented with exact file:line references
    - The Hardhat compilation already passed (61 files compiled successfully) which confirms no broken imports at compile time -- this task goes deeper to catch runtime-only stale references (e.g., ABI-encoded selectors, string comparisons, comment references that suggest stale mental models)
  </acceptance_criteria>
  <done>All 36 removed/renamed/re-signed symbols verified with zero STALE references in production code, OR any STALE references documented with severity assessment</done>
</task>

<task type="auto">
  <name>Task 2: Verify interface-implementation consistency</name>
  <files>.planning/phases/167-integration-test-baseline/167-01-CALL-GRAPH-AUDIT.md</files>
  <read_first>
    - contracts/interfaces/IDegenerusCoin.sol
    - contracts/interfaces/IDegenerusGame.sol
    - contracts/interfaces/IDegenerusQuests.sol
    - contracts/interfaces/IDegenerusGameModules.sol
    - contracts/interfaces/IBurnieCoinflip.sol
    - contracts/interfaces/IDegenerusAffiliate.sol
    - contracts/BurnieCoin.sol
    - contracts/DegenerusGame.sol
    - contracts/DegenerusQuests.sol
    - contracts/BurnieCoinflip.sol
    - contracts/DegenerusAffiliate.sol
  </read_first>
  <action>
For each interface file that changed in v11.0-v14.0, verify its function declarations match the implementation:

1. **IDegenerusCoin.sol**: Verify `notifyQuestMint`, `notifyQuestLootBox`, `notifyQuestDegenerette` are NOT declared. Verify `IDegenerusCoinModule` is NOT imported. Verify `vaultEscrow` IS declared inline.

2. **IDegenerusGame.sol**: Verify `decWindowOpenFlag()` NOT declared. Verify `deityPassCountFor(address)` NOT declared. Verify `hasDeityPass(address) returns (bool)` IS declared. Verify `mintPackedFor(address)` IS declared. Verify `decWindow()` returns `(bool)` only (not `(bool, uint24)`).

3. **IDegenerusQuests.sol**: Verify `handlePurchase(address,uint32,uint32,uint256,uint256,uint256)` IS declared. Verify `rollLevelQuest(uint256)` IS declared. Verify `clearLevelQuest()` IS declared. Verify `handleMint` has 4 parameters including mintPrice. Verify `handleLootBox` has 3 parameters including mintPrice. Verify `handleDegenerette` has 4 parameters including mintPrice.

4. **IDegenerusGameModules.sol**: Verify this file still exists (it should -- it declares MintModule, LootboxModule, etc. interfaces). Verify `DegenerusGameModuleInterfaces.sol` does NOT exist (deleted in v14.0).

5. **ContractAddresses.sol**: Verify `QUESTS` address constant exists (needed for onlyFlipCreditors modifier change). Do NOT modify this file.

Append results to the same `167-01-CALL-GRAPH-AUDIT.md` under a new "INTERFACE CONSISTENCY" section.

Also verify that `forge build` and `npx hardhat compile` both succeed (confirming no broken references at the Solidity compiler level). Record compiler output.
  </action>
  <verify>
    <automated>npx hardhat compile 2>&1 | grep -c "Compiled.*successfully" | xargs test 1 -eq</automated>
  </verify>
  <acceptance_criteria>
    - Interface consistency section added to 167-01-CALL-GRAPH-AUDIT.md
    - All 5 interface checks documented with PASS/FAIL
    - Both Hardhat and Foundry compilation succeed
    - Compilation output recorded in the audit document
  </acceptance_criteria>
  <done>All interface files verified consistent with their implementations, both compilers confirm zero errors</done>
</task>

</tasks>

<verification>
- `167-01-CALL-GRAPH-AUDIT.md` contains 36 stale-reference checks + 5 interface consistency checks
- Zero STALE verdicts in production code (comment-only matches acceptable)
- Both `npx hardhat compile` and `forge build` succeed
</verification>

<success_criteria>
Cross-contract call graph is verified: no function calls to removed or renamed functions, no stale interface references, no broken imports. All checks documented in audit report.
</success_criteria>

<output>
After completion, create `.planning/phases/167-integration-test-baseline/167-01-SUMMARY.md`
</output>
