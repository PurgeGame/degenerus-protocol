---
phase: 165-per-function-adversarial-audit
plan: 04
type: execute
wave: 2
depends_on: [165-01, 165-02, 165-03]
files_modified:
  - .planning/phases/165-per-function-adversarial-audit/165-04-FINDINGS.md
autonomous: true
requirements: [AUD-02, AUD-03]

must_haves:
  truths:
    - "Every modified function in JackpotModule not already covered by Phase 164 has a SAFE/VULNERABLE verdict"
    - "Every modified function in WhaleModule has a SAFE/VULNERABLE verdict"
    - "Storage layout verified via forge inspect for DegenerusGameStorage (Slot 0 and Slot 1 changes)"
    - "Storage layout verified via forge inspect for DegenerusQuests (new levelQuest storage)"
    - "Zero unexpected slot shifts across all contracts with storage changes"
    - "Zero open HIGH or MEDIUM findings across all 4 plans"
  artifacts:
    - path: ".planning/phases/165-per-function-adversarial-audit/165-04-FINDINGS.md"
      provides: "JackpotModule + WhaleModule audit verdicts + storage layout verification + consolidated findings"
      contains: "forge inspect"
  key_links:
    - from: "DegenerusGameStorage Slot 0"
      to: "All modules (delegatecall)"
      via: "shared storage layout"
      pattern: "Slot.*0"
    - from: "DegenerusGameStorage Slot 1"
      to: "price removal, ticketWriteSlot move"
      via: "storage packing"
      pattern: "Slot.*1"
---

<objective>
Audit remaining modified functions (JackpotModule non-carryover changes + WhaleModule), verify storage layouts via forge inspect for all contracts with storage changes, and produce consolidated findings across all 4 plans.

Purpose: This plan closes the audit by covering the remaining function audits and performing the critical storage layout verification (AUD-03). Storage slot shifts in delegatecall-based architectures can silently corrupt data across all modules.
Output: 165-04-FINDINGS.md with remaining verdicts, forge inspect results, and consolidated findings table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-changelog-extraction/162-CHANGELOG.md
@.planning/phases/164-jackpot-carryover-audit/164-CARRYOVER-AUDIT.md
@.planning/phases/165-per-function-adversarial-audit/165-01-FINDINGS.md
@.planning/phases/165-per-function-adversarial-audit/165-02-FINDINGS.md
@.planning/phases/165-per-function-adversarial-audit/165-03-FINDINGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit JackpotModule (non-carryover) + WhaleModule + Storage layout verification</name>
  <files>.planning/phases/165-per-function-adversarial-audit/165-04-FINDINGS.md</files>
  <read_first>
    - contracts/modules/DegenerusGameJackpotModule.sol (JackpotModule -- focus on non-carryover changes)
    - contracts/modules/DegenerusGameWhaleModule.sol (WhaleModule changes)
    - contracts/storage/DegenerusGameStorage.sol (storage layout -- Slot 0, Slot 1 changes)
    - contracts/DegenerusQuests.sol (storage: levelQuestType, levelQuestVersion, levelQuestPlayerState)
    - contracts/libraries/BitPackingLib.sol (HAS_DEITY_PASS_SHIFT at bit 184)
    - contracts/libraries/PriceLookupLib.sol (priceForLevel)
    - .planning/phases/162-changelog-extraction/162-CHANGELOG.md (storage changes list)
    - .planning/phases/164-jackpot-carryover-audit/164-CARRYOVER-AUDIT.md (already-audited carryover functions)
    - .planning/phases/165-per-function-adversarial-audit/165-01-FINDINGS.md (Plans 01-03 findings for consolidation)
    - .planning/phases/165-per-function-adversarial-audit/165-02-FINDINGS.md
    - .planning/phases/165-per-function-adversarial-audit/165-03-FINDINGS.md
  </read_first>
  <action>
Create 165-04-FINDINGS.md with three sections:

## Section 1: JackpotModule Non-Carryover Functions

Audit these JackpotModule functions NOT already covered by Phase 164:

1. `_distributeTicketJackpot()` (line ~944, v14.0): Added `queueLvl` parameter separating source (winner selection) from queue (ticket destination). Check: (a) old behavior was `lvl + 1` for both source and queue -- new behavior passes them separately; (b) all callers pass correct queueLvl; (c) internal delegation to _distributeTicketsToBuckets correctly forwards queueLvl.

2. `_distributeTicketsToBuckets()` (line ~994, v14.0): Added `queueLvl` passthrough. Check: correctly forwards to _distributeTicketsToBucket.

3. `_distributeTicketsToBucket()` (line ~1033, v14.0): Reads from `traitBurnTicket[sourceLvl]`, queues to `queueLvl`. Check: (a) source and queue are correctly separated -- winners drawn from sourceLvl trait holders, tickets queued at queueLvl; (b) no path where source and queue are accidentally swapped; (c) `_queueTickets(winner, queueLvl, units)` call is correct.

4. `_creditDgnrsCoinflip()` (line ~2148, v14.0): `price` replaced by `PriceLookupLib.priceForLevel(level)`. Check: this function credits DGNRS coinflip at a rate derived from price. Verify `level` is the correct argument (this is called during jackpot phase where `level` is the current level -- during jackpot, price should be `priceForLevel(level)` since jackpot-phase purchases target the current level).

5. `_calcDailyCoinBudget()` (line ~2450, v14.0): `price` replaced by `PriceLookupLib.priceForLevel(level)`. Same analysis as _creditDgnrsCoinflip -- verify level argument during jackpot phase.

6. `payDailyJackpot()` non-carryover path (line ~293, v14.0): The carryover path is covered by Phase 164. Audit only: (a) removal of `coin.rollDailyQuest` call (now in AdvanceModule -- verify no duplicate call); (b) removal of STAGE_JACKPOT_ETH_RESUME and `dailyEthPhase` state machine -- verify no orphaned references; (c) future ticket prep guard simplification.

7. `payDailyJackpotCoinAndTickets()` daily ticket path (line ~533, v14.0): The carryover path is covered by Phase 164. Audit only: (a) daily ticket `queueLvl = lvl + 1` -- verify this matches old behavior where daily tickets always target level+1; (b) removal of `coin.rollDailyQuest` call.

**WhaleModule (3):**

8. `purchaseDeityPass()` (line ~532, v14.0): Deity pass check and write changed to mintPacked_ bit shift. Check: (a) duplicate purchase check: `mintPacked_ >> HAS_DEITY_PASS_SHIFT & 1 != 0` reverts if already has pass -- verify this is a revert (not a silent return); (b) write via `BitPackingLib.setPacked(mintPacked_[buyer], HAS_DEITY_PASS_SHIFT, 1, 1)` -- verify only bit 184 is written, no adjacent bit corruption.

9. `purchaseLazyPass()` (line ~354, v14.0): Deity pass check changed to bit shift. Same check as purchaseDeityPass -- verify the check direction is correct (checking IF the buyer has a deity pass as a prerequisite for lazy pass purchase? Or checking they DON'T have one?). Trace the old `deityPassCount[buyer] != 0` logic.

10. `consolidatePrizePools()` (line ~235, v14.0): Formatting only. Confirm no behavioral change by reading the function.

## Section 2: Storage Layout Verification

Run `forge inspect` for every contract with storage changes per the 162-CHANGELOG.md:

**DegenerusGameStorage.sol (6 storage changes):**
```bash
forge inspect DegenerusGameStorage storage-layout --pretty
```

Verify:
- Slot 0: `dailyEthPhase` REMOVED -- confirm byte 30 is now free (compressedJackpotFlag moved from byte 31 to byte 30)
- Slot 0: total bytes used = 31 (was 32)
- Slot 1: `price` REMOVED -- confirm `ticketWriteSlot` moved from byte 22 to byte 6
- Slot 1: total bytes used = 9 (was 25)
- `gameOverPossible` (new, v11.0): verify slot/offset -- should be in the bool cluster near jackpotPhaseFlag
- `deityPassCount` mapping REMOVED -- verify no slot conflict with remaining mappings
- `dailyEthPoolBudget`, `dailyCarryoverEthPool`, `dailyCarryoverWinnerCap` REMOVED -- verify no orphaned slots

**DegenerusQuests.sol (5 storage changes):**
```bash
forge inspect DegenerusQuests storage-layout --pretty
```

Verify:
- `levelQuestType` (uint8, new) -- packs with questVersionCounter and levelQuestVersion in same slot
- `levelQuestVersion` (uint8, new) -- in same slot as above
- `levelQuestPlayerState` (mapping, new) -- occupies its own slot
- `QUEST_TYPE_MINT_BURNIE` constant changed from 0 to 9 -- this is a constant, not storage, but verify no storage references use the old value

**BitPackingLib.sol:**
Not a storage contract, but verify `HAS_DEITY_PASS_SHIFT = 184` does not conflict with existing mintPacked_ bit allocations. Read the bit layout header comment in BitPackingLib.sol and confirm:
- Bits 0-183: used by existing fields
- Bit 184: HAS_DEITY_PASS_SHIFT (new)
- Bits 185-227: unused
- Bits 228+: used by existing fields (or unused)

**DegenerusGameAdvanceModule.sol:**
```bash
forge inspect DegenerusGameAdvanceModule storage-layout --pretty
```
Verify `DECAY_RATE` constant is not storage (it's a constant, should NOT appear in storage layout).

## Section 3: Consolidated Phase 165 Findings

Collect all findings from Plans 01-03 and this plan:

1. Read 165-01-FINDINGS.md, 165-02-FINDINGS.md, 165-03-FINDINGS.md
2. Compile master table:

| # | Contract | Function | Plan | Verdict | Notes |
|---|----------|----------|------|---------|-------|

3. Count: total audited, SAFE, VULNERABLE (by severity)
4. Phase 164 coverage: list functions covered by carryover audit (reference only, not re-audited)
5. Final statement: "Zero open HIGH or MEDIUM findings" or list open findings requiring action

**Audit scope verification:** Cross-reference against 162-CHANGELOG.md "High-Risk Changes" table (items 1-20). Confirm every high-risk item has a verdict in Plans 01-04 or Phase 164. If any high-risk item is missing, audit it in this task.
  </action>
  <verify>
    <automated>grep -c "Verdict:" .planning/phases/165-per-function-adversarial-audit/165-04-FINDINGS.md && grep "forge inspect" .planning/phases/165-per-function-adversarial-audit/165-04-FINDINGS.md | head -4</automated>
  </verify>
  <acceptance_criteria>
    - 165-04-FINDINGS.md contains at least 10 verdict sections (JackpotModule + WhaleModule functions)
    - Section 2 contains forge inspect output for DegenerusGameStorage and DegenerusQuests
    - Slot 0 verification confirms dailyEthPhase removed, 31 bytes used
    - Slot 1 verification confirms price removed, ticketWriteSlot at new offset, 9 bytes used
    - DegenerusQuests storage shows levelQuestType, levelQuestVersion, levelQuestPlayerState
    - BitPackingLib bit 184 confirmed non-conflicting with existing allocations
    - Section 3 master table includes every verdict from Plans 01-04 plus Phase 164 references
    - Every high-risk item (1-20) from 162-CHANGELOG.md has coverage in a plan or Phase 164
    - Final statement confirms zero open HIGH/MEDIUM findings (or lists them)
  </acceptance_criteria>
  <done>All remaining functions audited, storage layouts verified via forge inspect with zero unexpected slot shifts, consolidated findings confirm complete coverage of all v11.0-v14.0 changes</done>
</task>

</tasks>

<verification>
- Storage layout commands complete without error:
  - `forge inspect DegenerusGameStorage storage-layout --pretty`
  - `forge inspect DegenerusQuests storage-layout --pretty`
- All 20 high-risk changelog items have audit coverage
- Consolidated table accounts for every new (17) and modified (37) function from 162-CHANGELOG.md
- Zero unexpected storage slot shifts
</verification>

<success_criteria>
All remaining functions audited. Storage layouts verified with zero slot shifts. Consolidated findings table covers the entire audit scope. Phase 165 success criteria are fully met: (1) every new function has SAFE/VULNERABLE verdict, (2) every modified function has behavioral equivalence or correct-new-behavior verdict, (3) storage layouts verified, (4) zero open HIGH or MEDIUM findings.
</success_criteria>

<output>
After completion, create `.planning/phases/165-per-function-adversarial-audit/165-04-SUMMARY.md`
</output>
