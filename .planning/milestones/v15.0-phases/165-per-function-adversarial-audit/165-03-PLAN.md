---
phase: 165-per-function-adversarial-audit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/165-per-function-adversarial-audit/165-03-FINDINGS.md
autonomous: true
requirements: [AUD-01, AUD-02]

must_haves:
  truths:
    - "Every new and modified function in DegenerusQuests (handlePurchase, rollLevelQuest, clearLevelQuest, _isLevelQuestEligible, _levelQuestTargetValue, _handleLevelQuestProgress, getPlayerLevelQuestView, rollDailyQuest, handleMint, handleFlip, handleDecimator, handleAffiliate, handleLootBox, handleDegenerette, _questHandleProgressSlot, _canRollDecimatorQuest, _bonusQuestType, onlyCoin) has a SAFE/VULNERABLE verdict"
    - "Every modified function in BurnieCoin (burnCoin, burnDecimator, onlyGame) has a SAFE/VULNERABLE verdict"
    - "Every modified function in BurnieCoinflip (onlyFlipCreditors, _resolveRecycleRebet, _resolveRecycleBatch) and DegenerusAffiliate (payAffiliate) and DegeneretteModule (_placeBet, _createCustomTickets, _resolvePayout) has a SAFE/VULNERABLE verdict"
    - "All access control changes are proven to not open unauthorized call paths"
    - "The handlePurchase combined handler returns correct values matching what old separate calls provided"
  artifacts:
    - path: ".planning/phases/165-per-function-adversarial-audit/165-03-FINDINGS.md"
      provides: "Adversarial audit verdicts for quest system + access control + external contract changes"
      contains: "Verdict"
  key_links:
    - from: "DegenerusQuests.handlePurchase"
      to: "BurnieCoinflip.creditFlip"
      via: "internal BURNIE/lootbox reward crediting"
      pattern: "coinflip\\.creditFlip"
    - from: "DegenerusQuests._handleLevelQuestProgress"
      to: "BurnieCoinflip.creditFlip"
      via: "800 BURNIE level quest reward"
      pattern: "creditFlip"
    - from: "DegenerusAffiliate.payAffiliate"
      to: "DegenerusQuests.handleAffiliate"
      via: "quest progress notification (was coin.affiliateQuestReward)"
      pattern: "quests\\.handleAffiliate"
---

<objective>
Adversarial audit of the quest system (DegenerusQuests: 7 new + 11 modified functions), access control changes across BurnieCoin (3 modified), BurnieCoinflip (3 modified), DegenerusAffiliate (1 major rewrite), and DegeneretteModule (3 modified).

Purpose: The quest system is entirely new in v13.0/v14.0 and handles BURNIE rewards via coinflip credits. Access control was extensively refactored (BurnieCoin stripped of quest middleman role, DegenerusQuests became the central quest hub). Any access control gap could allow unauthorized reward minting.
Output: 165-03-FINDINGS.md with per-function SAFE/VULNERABLE verdicts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-changelog-extraction/162-CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit DegenerusQuests new + modified functions (18 functions)</name>
  <files>.planning/phases/165-per-function-adversarial-audit/165-03-FINDINGS.md</files>
  <read_first>
    - contracts/DegenerusQuests.sol (primary contract under audit -- read fully)
    - contracts/interfaces/IDegenerusQuests.sol (interface declarations)
    - contracts/interfaces/IDegenerusGame.sol (game interface consumed by quests)
    - contracts/storage/DegenerusGameStorage.sol (shared constants, IDegenerusQuestView)
    - contracts/BurnieCoinflip.sol (creditFlip interface -- quests credits flips for rewards)
    - .planning/phases/162-changelog-extraction/162-CHANGELOG.md (function list, risk tags)
  </read_first>
  <action>
Create 165-03-FINDINGS.md. Audit 18 DegenerusQuests functions:

**New functions (7):**

1. `handlePurchase(address,uint32,uint32,uint256,uint256,uint256)` (line ~798, v14.0): Combined purchase-path handler. Check: (a) all 6 parameters match caller's intent (player, ethMintUnits, burnieMintUnits, mintPrice, lootboxTickets, randWord); (b) no double-counting -- each quest type handled exactly once; (c) creditFlip calls for BURNIE/lootbox rewards are correct amounts; (d) ETH mint reward returned (not creditFlipped) -- verify caller uses the return value; (e) questStreak returned for compute-once forwarding; (f) reentrancy: coinflip.creditFlip is external -- verify state is finalized before call or reentrancy guard exists.

2. `rollLevelQuest(uint256)` (line ~1766, v13.0): Selects quest type via _bonusQuestType, bumps levelQuestVersion. Check: (a) only called from advanceGame at level transition (access control); (b) version bump invalidates old player progress (verify _handleLevelQuestProgress checks version); (c) entropy argument is domain-separated from daily quest entropy.

3. `clearLevelQuest()` (line ~1773, v13.0): Zeros levelQuestType. Check: (a) called at level transition before new RNG arrives -- verify ordering in advanceGame; (b) access control (onlyGame).

4. `_isLevelQuestEligible(address)` (line ~1779, v13.0): Loyalty gate (levelStreak >= 5 OR active pass) AND activity gate (4+ units minted). Check: (a) levelStreak source -- how is it read? From mintPacked_ or from quest state? (b) active pass check reads from game.mintPackedFor(player) -- verify bit positions match; (c) mint count threshold (4 units) -- where is mintCount read from?

5. `_levelQuestTargetValue(uint8,uint256)` (line ~1804, v13.0): Returns 10x target for quest type. Check: (a) all 9 quest types (1-9) have cases -- no fallthrough to wrong value; (b) MINT_ETH target uses `mintPrice * 10` -- verify mintPrice parameter source; (c) LOOTBOX/DEGEN_ETH uses `mintPrice * 20` -- verify 20x multiplier is intentional; (d) BURNIE types use fixed 20000 -- verify units (is this 20000 BURNIE or 20000 * 10^18?).

6. `_handleLevelQuestProgress(address,uint8,uint256,uint256)` (line ~1834, v13.0): Shared handler called by all 6 daily handlers. Check: (a) version mismatch short-circuits (stale progress ignored); (b) type mismatch short-circuits (only matching quest type progresses); (c) completion threshold comparison is `>=` not `>` (or whichever is correct); (d) 800 BURNIE reward via coinflip.creditFlip -- verify amount and that credit is only given once per level quest (completed flag at bit 136); (e) event emission after state update.

7. `getPlayerLevelQuestView(address)` (line ~1893, v13.0): View function. Check: read-only (no state mutation), correct unpacking of levelQuestPlayerState.

**Modified functions (11):**

8. `rollDailyQuest(uint48,uint256)` (line ~322, v13.0/v14.0): Now `external onlyGame` (was onlyCoin). Check: (a) access control change is correct -- called from AdvanceModule (delegatecall context = GAME); (b) idempotent per day -- verify day-based dedup prevents double-rolling; (c) events emitted directly (no longer via BurnieCoin relay).

9. `handleMint(address,uint32,bool,uint256)` (line ~415, v13.0/v14.0): Added mintPrice param + level quest handling. Check: (a) mintPrice is passed through to _levelQuestTargetValue (not re-fetched from storage); (b) levelQuestHandled flag prevents double-counting across mint slots; (c) BURNIE mint rewards now creditFlipped internally -- verify old return-to-BurnieCoin path is fully removed.

10. `handleFlip(address,uint256)` (line ~540, v13.0): Added _handleLevelQuestProgress. Check: quest type for flip is correct constant, delta value is the flip amount.

11. `handleDecimator(address,uint256)` (line ~599, v13.0/v14.0): Added level quest + internal creditFlip. Check: reward amount is correct, creditFlip replaces old return-to-BurnieCoin.

12. `handleAffiliate(address,uint256)` (line ~654, v13.0): Added _handleLevelQuestProgress. Check: quest type matches AFFILIATE constant, delta is affiliate reward amount.

13. `handleLootBox(address,uint256,uint256)` (line ~695, v13.0/v14.0): Added mintPrice param + level quest + internal creditFlip. Same checks as handleMint.

14. `handleDegenerette(address,uint256,bool,uint256)` (line ~909, v13.0/v14.0): Added mintPrice + level quest + internal creditFlip. Removed `questGame.mintPrice()` call. Check: mintPrice argument source is correct.

15. `_questHandleProgressSlot(...)` (line ~1225, v13.0/v14.0): 3 new params: handlerQuestType, levelDelta, levelQuestPrice. Calls _handleLevelQuestProgress internally. Check: parameters correctly forwarded.

16. `_canRollDecimatorQuest()` (line ~1157, v14.0): `game_.decWindowOpenFlag()` changed to `game_.decWindow()`. Check: new simplified decWindow returns only bool -- verify this is what _canRollDecimatorQuest needs (yes, it only needs the bool).

17. `_bonusQuestType()` loop (line ~1476, v13.0): Added skip for sentinel 0 (unrolled marker). Check: the skip prevents 0 from being selected as a quest type (since 0 means "no quest"), correct alongside existing QUEST_TYPE_RESERVED skip.

18. `modifier onlyCoin()` (line ~289, v13.0): Expanded from COIN+COINFLIP to COIN+COINFLIP+GAME+AFFILIATE. Check: (a) each new allowed address is correct ContractAddresses constant; (b) no unauthorized address added; (c) the OR chain is complete (no missing address that should be allowed).

**Verdict format same as Plan 01.**
  </action>
  <verify>
    <automated>grep -c "Verdict:" .planning/phases/165-per-function-adversarial-audit/165-03-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - 165-03-FINDINGS.md contains exactly 18 verdict sections for DegenerusQuests
    - handlePurchase verdict covers all 6 parameters and reentrancy via creditFlip
    - _handleLevelQuestProgress verdict confirms single-completion guard (bit 136) and version check
    - onlyCoin modifier verdict lists all allowed addresses and confirms no unauthorized addition
    - rollDailyQuest verdict confirms access control change from onlyCoin to onlyGame is correct
  </acceptance_criteria>
  <done>18 DegenerusQuests functions audited with SAFE/VULNERABLE verdicts</done>
</task>

<task type="auto">
  <name>Task 2: Audit BurnieCoin + BurnieCoinflip + DegenerusAffiliate + DegeneretteModule (10 functions)</name>
  <files>.planning/phases/165-per-function-adversarial-audit/165-03-FINDINGS.md</files>
  <read_first>
    - contracts/BurnieCoin.sol (burnCoin, burnDecimator, onlyGame modifier)
    - contracts/BurnieCoinflip.sol (onlyFlipCreditors, _resolveRecycleRebet, _resolveRecycleBatch)
    - contracts/DegenerusAffiliate.sol (payAffiliate -- major rewrite)
    - contracts/modules/DegenerusGameDegeneretteModule.sol (_placeBet, _createCustomTickets, _resolvePayout)
    - contracts/DegenerusQuests.sol (handleAffiliate, handleDegenerette -- called by these contracts)
    - contracts/interfaces/IDegenerusGame.sol (hasDeityPass interface)
  </read_first>
  <action>
Append to 165-03-FINDINGS.md. Audit 10 functions:

**BurnieCoin (3):**

1. `burnCoin(address,uint256)` (line ~565, v13.0): Modifier changed from `onlyTrustedContracts` to `onlyGame`. Check: (a) old `onlyTrustedContracts` allowed GAME+AFFILIATE. New `onlyGame` allows only GAME. Verify: no caller from AFFILIATE path still tries to call burnCoin. If DegenerusAffiliate no longer calls burnCoin (it was removed from affiliate quest flow), this restriction is correct.

2. `burnDecimator()` (line ~596, v13.0): Multiple changes: `decWindow()` return simplified, quest reward routing simplified, activity score bonus. Check: (a) `decWindow()` now returns only `(bool)` -- verify burnDecimator only uses the bool (not the removed lvl); (b) reward returned directly (was via _questApplyReward) -- verify reward amount calculation is equivalent; (c) activity score bonus applied to `baseAmount` only when quest completed -- verify the conditional is correct (quest completed = non-zero reward).

3. `modifier onlyGame()` (line ~555, v13.0): Renamed from `onlyDegenerusGameContract`, replaces `onlyTrustedContracts`. Check: (a) the modifier checks `msg.sender == ContractAddresses.GAME`; (b) all functions using old modifiers now use `onlyGame`; (c) no function that previously needed AFFILIATE access is now blocked (verify AFFILIATE no longer calls any BurnieCoin function).

**BurnieCoinflip (3):**

4. `modifier onlyFlipCreditors()` (line ~190, v13.0): COIN replaced by QUESTS. Check: (a) `ContractAddresses.QUESTS` is the correct address for DegenerusQuests; (b) COIN removed because quest rewards are now routed through QUESTS, not COIN -- verify no path still has COIN calling creditFlip; (c) other allowed addresses unchanged.

5. `_resolveRecycleRebet()` (line ~299, v14.0): `deityPassCountFor(caller)` changed to `hasDeityPass(caller)`. Check: semantic equivalence -- old: `count != 0` (has any pass), new: `hasDeityPass` returns `mintPacked_ >> 184 & 1 != 0` (has pass bit set). These are equivalent for the current system where deity pass count is always 0 or 1.

6. `_resolveRecycleBatch()` (line ~432, v14.0): Same change as _resolveRecycleRebet. Same analysis.

**DegenerusAffiliate (1):**

7. `payAffiliate()` (line ~360, v13.0/v14.0): MAJOR rewrite. Audit:
   - (a) Leaderboard tracking moved after lootbox taper -- verify the taper calculation is applied before leaderboard records the amount (so leaderboard tracks post-taper, not pre-taper).
   - (b) 3-tier distribution replaced by winner-takes-all 75/20/5 weighted roll using `mod 20`. Check: `rand % 20 < 15` = 75% (slot 1), `rand % 20 < 19` = 20% (slot 2), else = 5% (slot 3). Verify: 15/20 = 75%, (19-15)/20 = 20%, (20-19)/20 = 5%. Correct.
   - (c) Quest call changed from `coin.affiliateQuestReward` to `quests.handleAffiliate`. Verify: handleAffiliate receives the correct player and reward amount. Verify no ETH is sent with the call.
   - (d) No-referrer path: affiliateShareBase flushes to VAULT/DGNRS coin flip. Check: the flush amount is correct, coinflip credit is properly formatted.
   - (e) Reentrancy: external calls to quests.handleAffiliate and coinflip.creditFlip. Verify state is finalized before external calls.
   - (f) RNG: mod-20 roll uses existing entropy. Verify entropy source is the same `randWord` used for other affiliate operations, domain-separated appropriately.

**DegeneretteModule (3):**

8. `_placeBet()` (line ~402, v13.0): Quest notification changed from `coin.notifyQuestDegenerette` to `quests.handleDegenerette` with mintPrice passthrough. Check: (a) all parameters match (player, betAmount, isWin, mintPrice); (b) mintPrice source is correct (should be current purchase price); (c) no ETH sent with the call.

9. `_createCustomTickets()` (line ~431, v14.0): Activity score source changed from `_playerActivityScoreInternal` to `_playerActivityScore(player, questStreak, level + 1)`. Check: (a) questStreak is fetched from `questView.playerQuestStates` -- verify the fetch and unpack is correct; (b) `level + 1` matches old behavior (DegeneretteModule always uses purchase-phase level for score, which is level+1).

10. `_resolvePayout()` (line ~672, v13.0): Consolation prize block removed (no more WWXRP mint on total loss). Check: (a) removal is clean -- no orphaned state, no path that expects consolation to exist; (b) the total loss case now simply returns without extra logic -- verify no compensation is missing that was critical to game balance (this is a design decision, not a security concern -- just confirm no ETH accounting gap).

**Add a Findings Summary table at bottom of 165-03-FINDINGS.md with all 28 verdicts (18 DegenerusQuests + 10 external).**
  </action>
  <verify>
    <automated>grep -c "Verdict:" .planning/phases/165-per-function-adversarial-audit/165-03-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - 165-03-FINDINGS.md contains exactly 28 verdict sections total (18 quests + 10 external)
    - payAffiliate verdict covers all 6 audit points (a through f) including mod-20 probability math
    - BurnieCoin onlyGame verdict confirms AFFILIATE no longer calls any BurnieCoin function
    - BurnieCoinflip onlyFlipCreditors verdict confirms COIN no longer calls creditFlip
    - DegeneretteModule _createCustomTickets verdict confirms level+1 for activity score matches old behavior
    - Findings Summary table at bottom with all 28 verdicts
  </acceptance_criteria>
  <done>28 total verdicts covering DegenerusQuests, BurnieCoin, BurnieCoinflip, DegenerusAffiliate, and DegeneretteModule with all access control changes proven correct</done>
</task>

</tasks>

<verification>
- `grep -c "Verdict:" .planning/phases/165-per-function-adversarial-audit/165-03-FINDINGS.md` returns 28
- Every access control change (5 modifiers: onlyGame, onlyFlipCreditors, onlyCoin expanded, rollDailyQuest onlyGame, recordMintQuestStreak GAME) has explicit verdict
- Zero paths exist where an unauthorized address can call a restricted function
</verification>

<success_criteria>
28 functions audited. All access control refactoring proven safe. The quest reward flow (DegenerusQuests -> BurnieCoinflip.creditFlip) has no reentrancy or double-credit vulnerability. Any VULNERABLE findings include severity and fix.
</success_criteria>

<output>
After completion, create `.planning/phases/165-per-function-adversarial-audit/165-03-SUMMARY.md`
</output>
