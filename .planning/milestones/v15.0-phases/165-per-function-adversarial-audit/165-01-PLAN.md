---
phase: 165-per-function-adversarial-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/165-per-function-adversarial-audit/165-01-FINDINGS.md
autonomous: true
requirements: [AUD-01, AUD-02]

must_haves:
  truths:
    - "Every new and modified function in AdvanceModule (gameOverPossible, drip projection, advanceGame loop changes) has a SAFE/VULNERABLE verdict"
    - "Every new and modified function in DegenerusGame.sol (hasDeityPass, mintPackedFor, decWindow, recordMintQuestStreak, playerActivityScore, claimAffiliateDgnrs) has a SAFE/VULNERABLE verdict"
    - "Access control changes on recordMintQuestStreak (COIN->GAME) and decWindow (simplified) are proven correct"
    - "gameOverPossible evaluation logic is proven correct for all three call sites (FLAG-01, FLAG-02, FLAG-03)"
  artifacts:
    - path: ".planning/phases/165-per-function-adversarial-audit/165-01-FINDINGS.md"
      provides: "Adversarial audit verdicts for AdvanceModule + DegenerusGame functions"
      contains: "Verdict"
  key_links:
    - from: "AdvanceModule._evaluateGameOverPossible"
      to: "MintModule._purchaseBurnTickets (GameOverPossible revert)"
      via: "gameOverPossible storage flag"
      pattern: "gameOverPossible"
    - from: "AdvanceModule.advanceGame"
      to: "DegenerusQuests.rollDailyQuest / rollLevelQuest / clearLevelQuest"
      via: "direct cross-contract calls"
      pattern: "quests\\.(rollDailyQuest|rollLevelQuest|clearLevelQuest)"
---

<objective>
Adversarial audit of AdvanceModule (3 new + 4 modified functions) and DegenerusGame.sol (2 new + 8 modified functions) covering the endgame flag lifecycle, drip projection math, advanceGame loop changes (quest rolling, level quest lifecycle, carryover resume removal, PriceLookupLib substitution), and all DegenerusGame view/accessor changes.

Purpose: These are the core state-management contracts -- AdvanceModule drives all phase transitions and level advancement, DegenerusGame exposes the external API. Security regressions here cascade to every downstream module.
Output: 165-01-FINDINGS.md with per-function SAFE/VULNERABLE verdicts and reasoning.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-changelog-extraction/162-CHANGELOG.md
@.planning/phases/163-level-system-documentation/163-LEVEL-SYSTEM.md
@.planning/phases/164-jackpot-carryover-audit/164-CARRYOVER-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit AdvanceModule new + modified functions (7 functions)</name>
  <files>.planning/phases/165-per-function-adversarial-audit/165-01-FINDINGS.md</files>
  <read_first>
    - contracts/modules/DegenerusGameAdvanceModule.sol (the contract under audit)
    - contracts/storage/DegenerusGameStorage.sol (storage layout, gameOverPossible declaration)
    - contracts/libraries/PriceLookupLib.sol (price derivation -- called by AdvanceModule)
    - contracts/DegenerusQuests.sol (quest interface -- called by AdvanceModule)
    - .planning/phases/162-changelog-extraction/162-CHANGELOG.md (function list and risk tags)
    - .planning/phases/164-jackpot-carryover-audit/164-CARRYOVER-AUDIT.md (carryover functions already audited -- do not re-audit)
  </read_first>
  <action>
For each function below, perform adversarial audit checking: reentrancy (external calls before state updates), access control (who can call, modifier correctness), overflow/underflow (unchecked blocks, shift operations, arithmetic), and state corruption (storage writes to wrong slots, race conditions, missing guards).

**New functions (3) -- need SAFE/VULNERABLE verdict with full reasoning:**

1. `_wadPow(uint256,uint256)` (line ~1571, v11.0): WAD-scale exponentiation via repeated squaring. Check: overflow in intermediate `result * base / WAD` multiplication, max iterations for exp<=120, edge cases (exp=0 returns WAD, base=0 returns 0).

2. `_projectedDrip(uint256,uint256)` (line ~1588, v11.0): Closed-form geometric series `futurePool * (1 - 0.9925^n)`. Check: underflow in `WAD - decayN` when decayN could exceed WAD (impossible since DECAY_RATE < WAD, but verify), overflow in `futurePool * complement / WAD`, zero-day edge case.

3. `_evaluateGameOverPossible(uint24,uint24)` (line ~1599, v11.0): Sets/clears gameOverPossible flag. Verify all THREE call sites:
   - FLAG-01: purchase-phase entry (line ~290) -- verify lvl/purchaseLevel args correct
   - FLAG-02: daily re-check after jackpot (line ~413 area) -- verify called with current state
   - FLAG-03: target-met clear (line ~327 area) -- verify `gameOverPossible = false`
   Check: L10+ threshold (levels 0-9 skip), deficit calculation (nextPool vs levelPrizePool), projected drip vs deficit comparison.

**Modified functions (4) -- need behavioral equivalence OR correct new behavior verdict:**

4. `advanceGame()` main loop (line ~148, v11.0/v13.0/v14.0): Multiple changes across milestones:
   - `quests.rollDailyQuest(day, rngWord)` called directly (was via BurnieCoin) -- verify entropy arg matches old path
   - `quests.clearLevelQuest()` at RNG request -- verify call site timing (before level increment)
   - `quests.rollLevelQuest(questEntropy)` at level transition -- verify entropy derivation is domain-separated
   - All `price` references replaced by `PriceLookupLib.priceForLevel(lvl)` -- verify every substitution uses correct level argument
   - Carryover ETH resume block removed (STAGE_JACKPOT_ETH_RESUME eliminated) -- verify no orphaned references
   - Future ticket prep guard simplified -- verify no valid path is now skipped

5. `_processPhaseTransition()` (line ~1379, v14.0): Removed price-setting if-else chain. Verify no side effects lost (the old chain set `price` storage variable which is now removed -- confirm no other consumer of `price` storage remains).

6. `_applyMintGate()` (line ~682, v14.0): Deity pass check changed from `deityPassCount[caller] != 0` to `mintData >> HAS_DEITY_PASS_SHIFT & 1 != 0`. Verify: bit position 184 is correct per BitPackingLib, shift result is equivalent to old boolean check, no off-by-one in mask.

7. `_coinflipRngGate()` (line ~738, v14.0): `price` replaced by `PriceLookupLib.priceForLevel(level)`. Verify: the level argument is `level` (not `purchaseLevel` or `level+1`) -- confirm this matches the old `price` semantic (price was always set to the level being purchased, which during purchase phase is level+1, so check if `priceForLevel(level)` here should actually be `priceForLevel(level+1)` or if the function context means `level` is already the purchase-phase level).

**Format each verdict as:**
```
### [Function Name] (Contract, line ~N)
**Milestone:** vXX.0
**Change type:** New / Modified
**Verdict:** SAFE / VULNERABLE

**Analysis:**
[Specific reasoning covering reentrancy, access control, overflow, state corruption as applicable]

**Edge cases checked:**
- [case 1]
- [case 2]
```

**IMPORTANT:** Do NOT re-audit functions already covered by Phase 164 (carryover audit). Reference 164-CARRYOVER-AUDIT.md verdicts for: `payDailyJackpot` carryover path, `_selectCarryoverSourceOffset`, `_highestCarryoverSourceOffset`, `_budgetToTicketUnits`, `_packDailyTicketBudgets`, `_unpackDailyTicketBudgets`, `payDailyJackpotCoinAndTickets` carryover path, final-day detection, final-day routing, `lastPurchaseDay` lifecycle, level increment timing.
  </action>
  <verify>
    <automated>grep -c "Verdict:" .planning/phases/165-per-function-adversarial-audit/165-01-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - 165-01-FINDINGS.md contains exactly 7 verdict sections (one per function audited)
    - Every verdict is either "SAFE" or "VULNERABLE" (grep for "Verdict:" returns 7 lines)
    - _wadPow verdict includes overflow analysis for intermediate multiplication
    - _evaluateGameOverPossible verdict references all three call sites (FLAG-01, FLAG-02, FLAG-03)
    - advanceGame verdict covers quest rolling entropy, PriceLookupLib substitution correctness, and carryover resume removal
    - _coinflipRngGate verdict resolves whether `level` vs `level+1` is correct for price lookup context
    - No function from Phase 164 carryover audit is re-audited
  </acceptance_criteria>
  <done>7 functions have SAFE/VULNERABLE verdicts with adversarial reasoning covering reentrancy, access control, overflow, and state corruption</done>
</task>

<task type="auto">
  <name>Task 2: Audit DegenerusGame.sol new + modified functions (10 functions)</name>
  <files>.planning/phases/165-per-function-adversarial-audit/165-01-FINDINGS.md</files>
  <read_first>
    - contracts/DegenerusGame.sol (the contract under audit)
    - contracts/storage/DegenerusGameStorage.sol (mintPacked_ layout, shared constants)
    - contracts/libraries/BitPackingLib.sol (HAS_DEITY_PASS_SHIFT = 184)
    - contracts/libraries/PriceLookupLib.sol (priceForLevel)
    - contracts/modules/DegenerusGameMintStreakUtils.sol (_playerActivityScore implementation)
    - contracts/interfaces/IDegenerusGame.sol (interface declarations for hasDeityPass, mintPackedFor)
  </read_first>
  <action>
Append to 165-01-FINDINGS.md. For each function below, perform adversarial audit checking: reentrancy, access control, overflow, state corruption.

**New functions (2):**

1. `hasDeityPass(address)` (line ~2251, v14.0): View returning bool from `mintPacked_` bit 184. Check: shift operation correctness (`mintPacked_[player] >> 184 & 1 != 0`), cannot be manipulated by caller (view function), matches constructor initialization path.

2. `mintPackedFor(address)` (interface exposure, v14.0): Exposes raw `mintPacked_` for external callers (DegenerusQuests eligibility checks). Check: read-only (no state mutation), information disclosure risk (all packed data becomes public -- but this is on-chain storage already publicly readable, so no new risk).

**Modified functions (8):**

3. `constructor` (line ~204, v14.0): Deity pass init changed from `deityPassCount[addr] = 1` to `BitPackingLib.setPacked(mintPacked_[addr], HAS_DEITY_PASS_SHIFT, 1, 1)`. Verify: setPacked writes exactly 1 bit at position 184, does not corrupt adjacent bits (check BitPackingLib.setPacked implementation -- mask generation via `(1 << width) - 1`).

4. `recordMintQuestStreak(address)` (line ~376, v13.0): Access changed from COIN to GAME. Verify: called via MintModule delegatecall (which executes in DegenerusGame context, so msg.sender == buyer's tx origin, but the access check is `msg.sender == ContractAddresses.GAME`). Since delegatecall preserves msg.sender context... wait -- delegatecall preserves the CALLER's msg.sender. So if MintModule is called via delegatecall FROM DegenerusGame, msg.sender in the delegated context is the original external caller. Verify exactly how this access control works: is the modifier checking `msg.sender == GAME` or `address(this) == GAME`? Trace the actual modifier implementation.

5. `processPayment()` (line ~338, v14.0): Return type formatting only. Confirm NO behavioral change -- read the actual diff context.

6. `claimAffiliateDgnrs()` (line ~1358, v14.0): Two changes: (a) deity pass check via bit shift instead of `deityPassCount`, (b) `price` replaced by `PriceLookupLib.priceForLevel(level)`. For (a): verify bit shift equivalence. For (b): verify `level` is the correct argument -- `claimAffiliateDgnrs` uses price to calculate affiliate DGNRS reward amount. The old `price` was the current purchase-phase price. `priceForLevel(level)` returns the price for the current level, but purchase-phase price should be `priceForLevel(level + 1)`. Check if this is a bug or if the function's semantic context means `level` is correct.

7. `_hasAnyLazyPass()` (line ~1551, v14.0): Reads `mintPacked_` once into local `packed`; deity pass check via bit shift. Verify: single SLOAD optimization is correct (packed variable read once, both pass checks derived from same read), bit positions don't overlap.

8. `mintPrice()` (line ~2079, v14.0): Returns `PriceLookupLib.priceForLevel(level)`. Verify: `mintPrice()` is a public view -- callers expect the current purchase price. During purchase phase, purchase price is `priceForLevel(level + 1)`. During jackpot phase, purchase price is `priceForLevel(level)`. Check if this function correctly reflects the phase-dependent price or if it always returns `priceForLevel(level)` regardless of phase. If always `level`, check all external callers to see if any depend on phase-correct pricing.

9. `decWindow()` (line ~2115, v11.0/v14.0): Signature changed from `(bool on, uint24 lvl)` to `(bool)`. Returns only `decWindowOpen`. Verify: no external callers depended on the removed `lvl` return value. Check DegenerusQuests._canRollDecimatorQuest which was updated to use simplified interface. Verify no other callers still expect two return values.

10. `playerActivityScore(address)` (line ~2210, v14.0): Body replaced -- now fetches questStreak from `questView.playerQuestStates` then delegates to `_playerActivityScore(player, streak)` in MintStreakUtils. Verify: the questStreak extraction from packed return value is correct (check bit positions), the MintStreakUtils function produces equivalent results to the old inline computation.

**Same verdict format as Task 1.**
  </action>
  <verify>
    <automated>grep -c "Verdict:" .planning/phases/165-per-function-adversarial-audit/165-01-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - 165-01-FINDINGS.md now contains exactly 17 verdict sections total (7 from Task 1 + 10 from Task 2)
    - grep for "Verdict:" returns 17 lines
    - recordMintQuestStreak verdict traces the actual modifier implementation to resolve delegatecall access control
    - claimAffiliateDgnrs verdict explicitly resolves whether priceForLevel(level) vs priceForLevel(level+1) is correct
    - mintPrice() verdict identifies all external callers and confirms phase-dependent pricing correctness
    - decWindow() verdict confirms no callers still expect two return values
    - Findings section at bottom lists any VULNERABLE verdicts with severity (or states "No findings")
  </acceptance_criteria>
  <done>17 total verdicts in 165-01-FINDINGS.md covering all AdvanceModule + DegenerusGame new/modified functions, with zero unresolved questions about price level arguments or access control paths</done>
</task>

</tasks>

<verification>
- `grep -c "Verdict:" .planning/phases/165-per-function-adversarial-audit/165-01-FINDINGS.md` returns 17
- `grep "VULNERABLE" .planning/phases/165-per-function-adversarial-audit/165-01-FINDINGS.md` returns empty (all SAFE) or lists specific findings with severity
- All 3 new AdvanceModule functions and all 4 modified AdvanceModule functions have verdicts
- All 2 new DegenerusGame functions and all 8 modified DegenerusGame functions have verdicts
</verification>

<success_criteria>
17 functions audited with SAFE/VULNERABLE verdicts. Zero unresolved access control or price-level-argument questions. Any VULNERABLE findings include severity (HIGH/MEDIUM/LOW/INFO) and recommended fix.
</success_criteria>

<output>
After completion, create `.planning/phases/165-per-function-adversarial-audit/165-01-SUMMARY.md`
</output>
