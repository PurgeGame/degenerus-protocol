---
phase: 164-jackpot-carryover-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/164-jackpot-carryover-audit/164-CARRYOVER-AUDIT.md
autonomous: true
requirements: [JACK-01, JACK-02]

must_haves:
  truths:
    - "Single-pass carryover distribution takes 0.5% of futurePrizePool, not more, not less"
    - "Source level selection range is exactly [lvl+1, lvl+4] with random offset within eligible levels"
    - "Carryover tickets queue at current level during normal days"
    - "Carryover tickets queue at lvl+1 on the final jackpot day"
    - "No edge case can strand tickets (zero budget, zero sources, boundary levels)"
    - "Final-day detection uses jackpotCounter + counterStep >= JACKPOT_LEVEL_CAP consistently"
  artifacts:
    - path: ".planning/phases/164-jackpot-carryover-audit/164-CARRYOVER-AUDIT.md"
      provides: "Complete audit report with per-function verdicts and edge case analysis"
      contains: "SAFE"
  key_links:
    - from: "payDailyJackpot (Phase 1)"
      to: "payDailyJackpotCoinAndTickets (Phase 2)"
      via: "dailyTicketBudgetsPacked storage"
      pattern: "pack/unpack round-trip preserves all four fields"
    - from: "_selectCarryoverSourceOffset"
      to: "payDailyJackpot carryoverSourceLevel"
      via: "offset -> lvl + offset"
      pattern: "offset=0 means no carryover, offset in [1..4] means lvl+offset"
    - from: "payDailyJackpotCoinAndTickets isFinalDay"
      to: "_distributeTicketJackpot queueLvl"
      via: "ternary: isFinalDay ? lvl+1 : lvl"
      pattern: "final day routes tickets to next level"
---

<objective>
Audit the jackpot carryover ticket distribution and final-day behavior to prove correctness or identify vulnerabilities.

Purpose: The carryover system was redesigned in v13.0 (Phase 158.1) and subsequently fixed in commit 6805969e. This audit traces every code path end-to-end to verify the redesigned logic is correct, checking budget calculation, source level selection, ticket queue level, final-day routing, and edge cases.

Output: `164-CARRYOVER-AUDIT.md` with SAFE/VULNERABLE verdicts per function, edge case analysis, and any findings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/164-jackpot-carryover-audit/164-CONTEXT.md
@.planning/phases/162-changelog-extraction/162-CHANGELOG.md
@.planning/phases/163-level-system-documentation/163-LEVEL-SYSTEM.md

<interfaces>
<!-- Key function signatures the executor must trace. -->

From DegenerusGameJackpotModule.sol:

payDailyJackpot(bool isDaily, uint24 lvl, uint256 randWord)
  -- Phase 1: computes carryover budget (0.5% futurePrizePool), selects source offset,
     converts budget to ticketUnits at current level, packs into dailyTicketBudgetsPacked

payDailyJackpotCoinAndTickets(uint256 randWord)
  -- Phase 2: unpacks dailyTicketBudgetsPacked, reconstructs carryoverSourceLevel,
     checks isFinalDay, calls _distributeTicketJackpot with queueLvl = isFinalDay ? lvl+1 : lvl

_selectCarryoverSourceOffset(uint24 lvl, uint32 winningTraitsPacked, uint256 randWord, uint8 counter)
  -- Returns offset in [1..4] or 0 if no eligible levels

_highestCarryoverSourceOffset(uint24 lvl, uint32 winningTraitsPacked)
  -- Scans offsets 4..1, returns first with actual trait ticket holders

_budgetToTicketUnits(uint256 budget, uint24 lvl)
  -- Converts ETH budget to ticket units: (budget << 2) / priceForLevel(lvl)

_distributeTicketJackpot(uint24 sourceLvl, uint24 queueLvl, uint32 winningTraitsPacked, uint256 ticketUnits, uint256 entropy, uint16 maxWinners, uint8 saltBase)
  -- Winners selected from sourceLvl trait holders, tickets queued at queueLvl

_packDailyTicketBudgets / _unpackDailyTicketBudgets
  -- Packing: [counterStep:8][dailyTicketUnits:64@8][carryoverTicketUnits:64@72][carryoverSourceOffset:8@136]

Constants:
  DAILY_CARRYOVER_MAX_OFFSET = 4
  DAILY_CARRYOVER_SOURCE_TAG = keccak256("daily-carryover-source")
  JACKPOT_LEVEL_CAP = 5

From DegenerusGameAdvanceModule.sol:

advanceGame()
  -- Sets lastPurchaseDay = true when nextPrizePool >= levelPrizePool target
  -- Increments level at RNG request time when lastPurchaseDay = true
  -- Resets lastPurchaseDay = false at jackpot-to-purchase transition
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace carryover ticket distribution end-to-end (JACK-01)</name>
  <read_first>
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 265-410 for Phase 1, lines 525-611 for Phase 2, lines 914-993 for _budgetToTicketUnits and _distributeTicketJackpot, lines 2493-2588 for source selection and packing)
    - .planning/phases/163-level-system-documentation/163-LEVEL-SYSTEM.md (Section 6: Carryover Tickets)
    - .planning/phases/162-changelog-extraction/162-CHANGELOG.md (JackpotModule section)
  </read_first>
  <files>.planning/phases/164-jackpot-carryover-audit/164-CARRYOVER-AUDIT.md</files>
  <action>
    Trace the carryover ticket distribution through both phases and produce the first sections of 164-CARRYOVER-AUDIT.md. Per D-01 and D-02, verify each claim against the code:

    **1. Budget calculation (payDailyJackpot, lines 373-380):**
    - Verify reserveSlice = futurePrizePool / 200 (exactly 0.5%)
    - Verify upfront deduction from futurePrizePool
    - Verify reserveSlice flows to nextPool (line 384)
    - Edge case: futurePrizePool = 0 -> reserveSlice = 0 -> no deduction, no tickets
    - Edge case: futurePrizePool = 1 wei -> reserveSlice = 0 (integer division) -> no deduction

    **2. Source level selection (_selectCarryoverSourceOffset, lines 2513-2556):**
    - Verify _highestCarryoverSourceOffset scans [4..1] correctly (lines 2495-2508)
    - Verify random offset selection in [1..highestEligible] (line 2526-2536)
    - Verify wrap-around probe covers all eligible offsets (lines 2539-2554)
    - Edge case: no eligible offsets -> returns 0 -> no carryover
    - Edge case: only offset 1 eligible -> returns 1 (hardcoded shortcut at line 2524)
    - Edge case: all 4 eligible -> random start, guaranteed to find one

    **3. Ticket unit conversion (_budgetToTicketUnits, lines 915-922):**
    - Verify tickets priced at current level: _budgetToTicketUnits(reserveSlice, lvl) at line 393-396
    - Verify formula: (budget << 2) / priceForLevel(lvl) matches 4 tickets per price unit
    - Edge case: priceForLevel returns 0 at level 0 -> function returns 0 (line 921)

    **4. Pack/unpack round-trip (_packDailyTicketBudgets / _unpackDailyTicketBudgets, lines 2558-2587):**
    - Verify packing layout: [counterStep:8][dailyTicketUnits:64@8][carryoverTicketUnits:64@72][carryoverSourceOffset:8@136]
    - Verify unpack masks: uint8(packed), uint64(packed >> 8), uint64(packed >> 72), uint8(packed >> 136)
    - Check: do any fields overlap? 8+64=72, 72+64=136, 136+8=144. Total 144 bits of 256. No overlap.
    - Edge case: max carryoverTicketUnits (uint64 max) -> does it bleed into carryoverSourceOffset? uint64 cast truncates at unpack.

    **5. Phase 2 distribution (payDailyJackpotCoinAndTickets, lines 525-611):**
    - Verify carryoverSourceLevel reconstruction matches Phase 1 logic (lines 540-543)
    - Verify entropy derivation uses carryoverSourceLevel (line 544)
    - Verify _distributeTicketJackpot call: sourceLvl=carryoverSourceLevel, queueLvl=lvl (line 592-598)
    - Verify guard: carryoverTicketUnits != 0 && carryoverSourceOffset != 0 (line 590)

    **6. Early-bird day skip (payDailyJackpot, lines 332-334, 359, 375):**
    - Verify isEarlyBirdDay (counter == 0) skips carryover source selection (line 359)
    - Verify isEarlyBirdDay skips reserveSlice calculation (line 375)
    - Verify early-bird runs _runEarlyBirdLootboxJackpot instead (line 333)

    **Edge cases to check explicitly:**
    - Level 1 (lvl=1): source range [2..5], does _hasActualTraitTickets return false for all? If so, offset=0, no carryover. SAFE.
    - Max level: does priceForLevel handle arbitrarily high levels?
    - Compressed jackpot (counterStep=2): carryover still runs on non-early-bird days? Yes, counter>0 for these days.
    - Turbo mode (counterStep=5): counter starts at 0, isEarlyBirdDay=true, so no carryover on the single turbo day. Verify this is correct.

    Write SAFE/VULNERABLE verdict for each function: _selectCarryoverSourceOffset, _highestCarryoverSourceOffset, _budgetToTicketUnits, _packDailyTicketBudgets, _unpackDailyTicketBudgets, payDailyJackpot (carryover path), payDailyJackpotCoinAndTickets (carryover path).
  </action>
  <verify>
    <automated>grep -c "SAFE\|VULNERABLE" .planning/phases/164-jackpot-carryover-audit/164-CARRYOVER-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - Every function listed has a SAFE or VULNERABLE verdict with line-number evidence
    - Budget calculation verified as exactly 0.5% with upfront deduction
    - Source range verified as [1..4] (DAILY_CARRYOVER_MAX_OFFSET=4)
    - Pack/unpack round-trip verified lossless (144 bits, no overlap)
    - All edge cases (zero budget, zero sources, level 1, turbo mode) analyzed
    - Phase 1 to Phase 2 data flow verified (dailyTicketBudgetsPacked preserves all fields)
  </acceptance_criteria>
  <done>Carryover distribution audit section complete with per-function verdicts and edge case analysis covering JACK-01</done>
</task>

<task type="auto">
  <name>Task 2: Verify final-day ticket routing to level+1 (JACK-02)</name>
  <read_first>
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 588-601 for final-day routing, lines 314-316 for isFinalPhysicalDay)
    - contracts/modules/DegenerusGameAdvanceModule.sol (lines 134-160 for lastPurchaseDay detection, lines 317-389 for purchase-to-jackpot transition, lines 1370-1382 for level increment at RNG request)
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 287-410 for payDailyJackpot full flow to verify counterStep computation)
  </read_first>
  <files>.planning/phases/164-jackpot-carryover-audit/164-CARRYOVER-AUDIT.md</files>
  <action>
    Append the final-day audit section to 164-CARRYOVER-AUDIT.md. Per D-01 and D-02, trace the final-day detection and routing:

    **1. Final-day detection in Phase 2 (payDailyJackpotCoinAndTickets, line 591):**
    - Verify: isFinalDay = jackpotCounter + counterStep >= JACKPOT_LEVEL_CAP
    - Verify JACKPOT_LEVEL_CAP = 5 (check constant declaration)
    - Trace counterStep values: normal=1, compressed=2, turbo=5
    - Normal path: counter=4, step=1, 4+1>=5 -> true (final day). Counter=3, step=1, 3+1<5 -> false.
    - Compressed path: counter=1, step=2, 1+2<5 -> false. Counter=3, step=2, 3+2>=5 -> true.
    - Turbo path: counter=0, step=5, 0+5>=5 -> true. But turbo is isEarlyBirdDay, so carryover is skipped entirely (line 375). Verify this does not create a gap -- turbo has exactly 1 physical day with no carryover, which is the early-bird day. SAFE because early-bird runs lootbox instead.

    **2. Ticket routing on final day (payDailyJackpotCoinAndTickets, lines 592-599):**
    - Verify: queueLvl = isFinalDay ? lvl + 1 : lvl
    - On final day, tickets go to lvl+1 key space
    - On non-final days, tickets go to lvl key space
    - Confirm _distributeTicketJackpot receives queueLvl as second argument (line 594)
    - Confirm sourceLvl (first argument) is always carryoverSourceLevel regardless of final day (line 593)

    **3. Why lvl+1 on final day is correct:**
    - After the final jackpot day, AdvanceModule transitions to purchase phase for next level
    - Level is incremented at RNG request time (AdvanceModule line 1374: level = lvl)
    - Tickets at the old level's key space would be stranded (old level is complete)
    - Tickets at lvl+1 are in the correct key space for the next purchase phase
    - Verify this matches the MintModule final-day override (Section 6 of 163-LEVEL-SYSTEM.md)

    **4. lastPurchaseDay detection in AdvanceModule:**
    - Normal trigger: nextPrizePool >= levelPrizePool target (lines 327-329)
    - Early trigger (turbo): target met within first 2 days (lines 144-152), sets compressedJackpotFlag=2
    - Verify lastPurchaseDay is reset to false at jackpot-to-purchase transition (line 377)
    - Verify level increment happens at _requestRng when lastPurchaseDay=true (line 1374)

    **5. Edge cases for final-day routing:**
    - Final day with zero carryover budget: guard at line 590 (carryoverTicketUnits != 0) prevents _distributeTicketJackpot call. No tickets stranded. SAFE.
    - Final day with zero eligible source levels: carryoverSourceOffset = 0, guard at line 590 (carryoverSourceOffset != 0) prevents call. SAFE.
    - Final day + compressed mode: counter=3, step=2, isFinalDay=true, lvl+1 routing applies. Verify carryover was not skipped for this day (counter>0, not early-bird). SAFE.
    - Consecutive final days (impossible? verify): jackpotCounter resets at level transition. Each level has its own independent counter.

    **6. Cross-check: daily ticket distribution final-day behavior (lines 576-586):**
    - Daily tickets (non-carryover) use queueLvl = lvl+1 always (line 579). These go to next level regardless.
    - Carryover tickets conditionally use lvl+1 only on final day.
    - Verify these two distributions do not conflict in key space.

    Write SAFE/VULNERABLE verdicts for: final-day detection logic, final-day carryover routing, lastPurchaseDay lifecycle, level increment timing.

    **7. Overall summary section:**
    Add a summary table at the end of 164-CARRYOVER-AUDIT.md:
    | Function | Verdict | Notes |
    With all functions audited across both tasks.

    Add a "Findings" section listing any issues found (or "No findings" if clean).

    Per D-03, the complete report goes to `.planning/phases/164-jackpot-carryover-audit/164-CARRYOVER-AUDIT.md`.
  </action>
  <verify>
    <automated>grep -c "SAFE\|VULNERABLE" .planning/phases/164-jackpot-carryover-audit/164-CARRYOVER-AUDIT.md | xargs test 7 -le</automated>
  </verify>
  <acceptance_criteria>
    - Final-day detection logic verified with all counterStep variants (normal/compressed/turbo)
    - Ticket routing to lvl+1 on final day traced through _distributeTicketJackpot
    - lastPurchaseDay lifecycle traced through AdvanceModule (set, consumed, reset)
    - Level increment timing verified (at RNG request, before next purchase phase)
    - All edge cases analyzed (zero budget final day, compressed final day, turbo skip)
    - Summary table covers all audited functions with verdicts
    - Report is complete at 164-CARRYOVER-AUDIT.md
  </acceptance_criteria>
  <done>Final-day audit complete, full report written with per-function verdicts, edge case analysis, and summary table covering both JACK-01 and JACK-02</done>
</task>

</tasks>

<verification>
- 164-CARRYOVER-AUDIT.md exists with both carryover distribution and final-day sections
- Every audited function has a SAFE or VULNERABLE verdict
- Edge cases enumerated and analyzed (zero budget, zero sources, level 1, turbo, compressed, final day)
- No findings of VULNERABLE severity remain unaddressed (if any found, they are documented with severity and recommendation)
</verification>

<success_criteria>
- JACK-01: Carryover ticket distribution traced end-to-end with correct source range [1-4], budget 0.5%, current-level queueing, and no ticket-stranding edge cases
- JACK-02: Final-day routing to level+1 verified with consistent isFinalDay detection across all compression modes
- Complete audit report at 164-CARRYOVER-AUDIT.md with per-function verdicts
</success_criteria>

<output>
After completion, create `.planning/phases/164-jackpot-carryover-audit/164-01-SUMMARY.md`
</output>
