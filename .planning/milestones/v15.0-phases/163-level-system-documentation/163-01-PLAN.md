---
phase: 163-level-system-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/163-level-system-documentation/163-LEVEL-SYSTEM.md
autonomous: true
requirements:
  - DOC-01

must_haves:
  truths:
    - "Level advancement trigger and state transitions are documented with function names and line ranges"
    - "PriceLookupLib price tier table is documented with exact thresholds and prices"
    - "purchaseLevel semantics (jackpot vs purchase phase) are documented with the ternary logic"
    - "Quest target calculation is documented for both daily and level quests with multipliers"
    - "Lootbox baseline (always level+1) is documented with storage references"
    - "Jackpot ticket routing including carryover and final-day behavior is documented"
  artifacts:
    - path: ".planning/phases/163-level-system-documentation/163-LEVEL-SYSTEM.md"
      provides: "Complete level system reference document"
      min_lines: 150
      contains: "priceForLevel"
  key_links:
    - from: "163-LEVEL-SYSTEM.md section 3 (purchaseLevel)"
      to: "163-LEVEL-SYSTEM.md section 2 (price derivation)"
      via: "purchaseLevel feeds into PriceLookupLib.priceForLevel"
      pattern: "priceForLevel.*purchaseLevel"
    - from: "163-LEVEL-SYSTEM.md section 4 (quest targets)"
      to: "163-LEVEL-SYSTEM.md section 3 (purchaseLevel)"
      via: "daily quest targets use mintPrice from purchaseLevel, level quest targets use level+1 price"
      pattern: "mintPrice|levelQuestPrice"
---

<objective>
Produce a complete reference document for the level system covering all 6 required sections: level advancement, price derivation, purchaseLevel semantics, quest target calculation, lootbox baseline, and jackpot ticket routing.

Purpose: Phase 165 auditors need to trace any level-dependent behavior from input to output without reading contract source. This document is the single reference for how `level` flows through the entire system.

Output: `.planning/phases/163-level-system-documentation/163-LEVEL-SYSTEM.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/163-level-system-documentation/163-CONTEXT.md
@.planning/phases/162-changelog-extraction/162-CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read contracts and write level system reference document</name>
  <files>.planning/phases/163-level-system-documentation/163-LEVEL-SYSTEM.md</files>
  <read_first>
    contracts/libraries/PriceLookupLib.sol (47 lines — read in full)
    contracts/storage/DegenerusGameStorage.sol (focus on Slot 0 layout lines ~35-95 for level/jackpotPhaseFlag declarations, and level variable at line ~256)
    contracts/modules/DegenerusGameAdvanceModule.sol (focus on advanceGame function at line ~136, level assignment at line ~1374, purchaseLevel at line ~755)
    contracts/modules/DegenerusGameMintModule.sol (focus on _purchaseFor at line ~616 for purchaseLevel ternary at line ~627, lootboxBaseLevelPacked at line ~697, lootboxEth packing at line ~712)
    contracts/DegenerusQuests.sol (focus on handlePurchase at line ~760, _questTargetValue at line ~1412, _levelQuestTargetValue at line ~1822, _handleLevelQuestProgress at line ~1847)
    contracts/modules/DegenerusGameJackpotModule.sol (focus on carryover at lines ~331-405, _distributeTicketJackpot at line ~958, DAILY_CARRYOVER_MAX_OFFSET at line ~131)
  </read_first>
  <action>
Read all 6 contracts listed in read_first. For each, focus on the indicated line ranges but read surrounding context to understand the full picture.

Write `.planning/phases/163-level-system-documentation/163-LEVEL-SYSTEM.md` with these 6 sections (per D-02):

**Section 1: Level Advancement** (per D-02 item 1)
- Where `level` is stored (DegenerusGameStorage Slot 0, uint24 at bits 18-21)
- What triggers level increment (advanceGame in AdvanceModule)
- Who can call it and when
- State transitions: what else changes when level increments (jackpotPhaseFlag, levelStartTime, etc.)
- Include function name, contract path, and line number for every claim (per D-04)

**Section 2: Price Derivation** (per D-02 item 2)
- PriceLookupLib.priceForLevel pure function — full tier table
- Intro pricing (levels 0-4: 0.01 ETH, levels 5-9: 0.02 ETH)
- Standard tiers (x01-x29: 0.04, x30-x59: 0.08, x60-x89: 0.12, x90-x99: 0.16)
- Milestone levels (x00: 0.24 ETH for levels 100+)
- How it replaced the old price storage variable (removed in Phase 160.1)

**Section 3: purchaseLevel Semantics** (per D-02 item 3)
- The ternary: `jackpotPhaseFlag ? level : level + 1`
- Why: during jackpot phase, tickets still route to current level; during purchase phase, tickets route to next level
- Where computed (_purchaseFor in MintModule, line ~627)
- How purchaseLevel feeds into priceForLevel to determine ticket price
- How purchaseLevel affects ticket key space routing

**Section 4: Quest Target Calculation** (per D-02 item 4)
- Daily quest targets: _questTargetValue uses mintPrice (derived from purchaseLevel)
- Multipliers per quest type (document each QUEST_TYPE_* multiplier)
- Level quest targets: _levelQuestTargetValue uses levelQuestPrice (derived from level+1)
- The split in handlePurchase: mintPrice for daily targets, levelQuestPrice for level quest targets
- Show the 10x multiplier source for level quests

**Section 5: Lootbox Baseline** (per D-02 item 5)
- Always level+1 regardless of jackpot phase
- lootboxBaseLevelPacked: written as uint24(cachedLevel + 1) at line ~697
- lootboxEth packing: uint256(cachedLevel + 1) << 232 at line ~712
- Why lootbox always uses level+1 (lootbox resolves later, must target upcoming level)

**Section 6: Jackpot Ticket Routing** (per D-02 item 6)
- Normal routing vs jackpot phase routing
- Carryover tickets: source range 1-4 (DAILY_CARRYOVER_MAX_OFFSET), 0.5% of futurePrizePool budget
- Last-day override: final purchase day routes to level+1
- Far-future key space (bit 23 of level key)
- _distributeTicketJackpot, _distributeTicketsToBuckets, _distributeTicketsToBucket function chain

Include cross-references between sections (e.g., "see Section 2 for the price tier table").

Read current contract code directly (per D-03) — do NOT rely on git history or phase summaries for accuracy.

Include specific function names, line ranges, and contract file paths for every claim (per D-04).
  </action>
  <verify>
    <automated>grep -c "priceForLevel\|purchaseLevel\|advanceGame\|handlePurchase\|lootboxBaseLevelPacked\|carryover" .planning/phases/163-level-system-documentation/163-LEVEL-SYSTEM.md | xargs test 6 -le</automated>
  </verify>
  <acceptance_criteria>
    - Document exists with all 6 sections
    - Each section references specific function names, line numbers, and contract paths
    - PriceLookupLib tier table is fully reproduced
    - purchaseLevel ternary is documented with both branches explained
    - Quest target section distinguishes daily vs level quest pricing
    - Lootbox section explains why level+1 is used regardless of phase
    - Jackpot routing section covers carryover source range, budget, and final-day behavior
  </acceptance_criteria>
  <done>163-LEVEL-SYSTEM.md exists with all 6 sections, each containing contract paths, function names, and line references that an auditor can verify against source</done>
</task>

<task type="auto">
  <name>Task 2: Verify document completeness against contract code</name>
  <files>.planning/phases/163-level-system-documentation/163-LEVEL-SYSTEM.md</files>
  <read_first>
    .planning/phases/163-level-system-documentation/163-LEVEL-SYSTEM.md
  </read_first>
  <action>
Read the completed document. Verify completeness by cross-checking against the 5 success criteria from ROADMAP.md Phase 163:

1. Level advancement trigger and state transitions documented — check advanceGame coverage
2. Price derivation via PriceLookupLib documented — check tier table completeness (all 7 tiers: 0.01, 0.02, 0.04, 0.08, 0.12, 0.16, 0.24)
3. purchaseLevel semantics documented — check both jackpot and purchase phase branches
4. Quest target calculation documented — check daily targets AND level quest targets with multipliers
5. Lootbox baseline and jackpot routing documented — check level+1 baseline, source range 1-4, carryover budget

For each criterion, verify the document contains:
- The specific function name(s)
- The contract file path
- Line number or range
- A clear explanation an auditor could follow

If any gaps found: fix them by reading the relevant contract section and adding the missing content.

Also verify cross-references between sections are consistent (e.g., Section 4 references Section 2 for price tiers, Section 3 references Section 1 for when level changes).
  </action>
  <verify>
    <automated>wc -l .planning/phases/163-level-system-documentation/163-LEVEL-SYSTEM.md | awk '{if ($1 >= 150) print "PASS: "$1" lines"; else print "FAIL: only "$1" lines"}'</automated>
  </verify>
  <acceptance_criteria>
    - All 5 ROADMAP success criteria are satisfied
    - Every section has at least one contract path + function name + line reference
    - No section is a stub or placeholder
    - Cross-references between sections are consistent
    - Document is self-contained — auditor needs no other reference
  </acceptance_criteria>
  <done>Document passes all 5 ROADMAP success criteria checks, contains 150+ lines, and every section has verifiable code references</done>
</task>

</tasks>

<verification>
1. `163-LEVEL-SYSTEM.md` exists in the phase directory
2. Document covers all 6 required sections per D-02
3. Each section includes contract paths, function names, and line references per D-04
4. Document reads from current code per D-03 (not git history)
5. All 5 ROADMAP Phase 163 success criteria are satisfied
</verification>

<success_criteria>
- Single reference document at `.planning/phases/163-level-system-documentation/163-LEVEL-SYSTEM.md`
- Auditor can trace any level-dependent behavior from input to output using only this document
- Every claim in the document can be verified against contract source via the provided function names and line references
</success_criteria>

<output>
After completion, create `.planning/phases/163-level-system-documentation/163-01-SUMMARY.md`
</output>
