---
phase: 166-rng-gas-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/166-rng-gas-verification/166-02-GAS-CEILING-AUDIT.md
autonomous: true
requirements: [GAS-01]
must_haves:
  truths:
    - "Score calculation (_playerActivityScore) gas cost is profiled under worst-case inputs with SLOAD counts"
    - "Quest roll (rollLevelQuest/clearLevelQuest) gas is re-verified against Phase 155 baseline"
    - "Drip projection (_evaluateGameOverPossible, _wadPow, _projectedDrip) gas cost is profiled"
    - "PriceLookupLib.priceForLevel gas cost is profiled and compared to removed storage variable"
    - "handlePurchase consolidated handler gas cost is profiled with SLOAD/external-call counts"
    - "advanceGame gas ceiling maintains safety margin against 14M block limit"
  artifacts:
    - path: ".planning/phases/166-rng-gas-verification/166-02-GAS-CEILING-AUDIT.md"
      provides: "Gas ceiling audit report for all new computation paths"
      contains: "GAS-01 SATISFIED"
  key_links:
    - from: "Phase 155 baseline (7,018,430 worst-case)"
      to: "Updated advanceGame gas ceiling"
      via: "Delta addition from new v14.0 paths"
      pattern: "safety margin"
    - from: "_playerActivityScore"
      to: "_purchaseFor"
      via: "Called once per purchase with cached score"
      pattern: "_playerActivityScore"
---

<objective>
Profile gas costs of all new computation paths introduced in v11.0-v14.0 using static analysis (SLOAD counts, external calls, loop iterations) and verify the advanceGame gas ceiling maintains safety margin against the 14M block gas limit.

Purpose: Requirement GAS-01 demands gas ceiling verification for score calculation, quest roll, drip projection, PriceLookupLib, and handlePurchase. Per D-02, this uses static analysis only (no forge execution).
Output: `.planning/phases/166-rng-gas-verification/166-02-GAS-CEILING-AUDIT.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/166-rng-gas-verification/166-CONTEXT.md
@.planning/phases/155-economic-gas-analysis/155-01-ECONOMIC-GAS-ANALYSIS.md
@.planning/phases/165-per-function-adversarial-audit/165-01-FINDINGS.md
@.planning/phases/165-per-function-adversarial-audit/165-02-FINDINGS.md
@.planning/phases/162-changelog-extraction/162-CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static gas analysis of new computation paths and advanceGame ceiling verification</name>
  <files>.planning/phases/166-rng-gas-verification/166-02-GAS-CEILING-AUDIT.md</files>
  <read_first>
    - contracts/modules/DegenerusGameMintStreakUtils.sol (full file -- _playerActivityScore at line ~81, _mintStreakEffective, _activeTicketLevel)
    - contracts/storage/DegenerusGameStorage.sol (lines 1618-1640 for _mintCountBonusPoints)
    - contracts/modules/DegenerusGameAdvanceModule.sol (lines 1576-1624 for _wadPow, _projectedDrip, _evaluateGameOverPossible; lines 96-108 for VRF constants)
    - contracts/libraries/PriceLookupLib.sol (full file -- priceForLevel pure function)
    - contracts/DegenerusQuests.sol (lines 322-350 for rollDailyQuest; lines 790-840 for handlePurchase; lines 1766-1800 for rollLevelQuest/clearLevelQuest)
    - contracts/modules/DegenerusGameMintModule.sol (lines 616-810 for _purchaseFor with handlePurchase call at ~771 and score computation at ~782)
    - .planning/phases/155-economic-gas-analysis/155-01-ECONOMIC-GAS-ANALYSIS.md (Phase 155 gas baseline: 7,018,430 worst-case, 1.99x margin)
    - .planning/phases/166-rng-gas-verification/166-CONTEXT.md (user decisions D-02 static analysis method)
  </read_first>
  <action>
Create `.planning/phases/166-rng-gas-verification/166-02-GAS-CEILING-AUDIT.md` with the following structure and content.

**Header:** Phase 166, Plan 02, Gas Ceiling Audit. List requirement GAS-01 and methodology (per D-02: static analysis counting SLOADs, MSTOREs, external calls, loop iterations from source code).

**Section 1: Gas Cost Reference**
Document the EIP-2929 gas costs used throughout:
- Cold SLOAD: 2,100 gas
- Warm SLOAD: 100 gas
- Cold SSTORE (zero to non-zero): 22,100 gas
- Warm SSTORE (non-zero to non-zero): 5,000 gas (dirty) or 2,900 gas (clean)
- STATICCALL overhead: ~700 gas + callee costs
- keccak256: 30 gas + 6 gas per 32-byte word
- Comparison/arithmetic: 3-5 gas each

**Section 2: _playerActivityScore (v14.0 NEW -- MintStreakUtils line ~81)**

Profile the worst-case path through _playerActivityScore(player, questStreak, streakBaseLevel):

| Operation | Type | Count | Gas (cold) | Gas (warm) | Notes |
|-----------|------|-------|------------|------------|-------|
| `mintPacked_[player]` | SLOAD | 1 | 2,100 | 100 | Mapping read -- usually warm from prior mint handler |
| Bit shifts + masks (HAS_DEITY_PASS_SHIFT, LEVEL_COUNT_SHIFT, FROZEN_UNTIL_LEVEL_SHIFT, WHALE_BUNDLE_TYPE_SHIFT) | Arithmetic | 5 | 25 | 25 | 3-5 gas each |
| `_mintStreakEffective(player, streakBaseLevel)` | SLOAD | 1 | 2,100 | 100 | Reads mintPacked_[player] again -- WARM (same slot as above) |
| `level` | SLOAD | 1 | 2,100 | 100 | Storage Slot 0 -- usually warm in purchase context |
| `_mintCountBonusPoints(levelCount, currLevel)` | Arithmetic | 1 | 15 | 15 | Pure: (mintCount * 25) / currLevel, cap at 25 |
| `affiliate.affiliateBonusPointsBest(currLevel, player)` | STATICCALL | 1 | ~700 + 2,200 | ~700 + 200 | External view call to DegenerusAffiliate, reads affiliateTopByLevel mapping |
| Capping/branching (streakPoints, mintCountPoints, questStreakCapped) | Arithmetic | ~8 | 40 | 40 | Comparisons and conditional assignments |
| **TOTAL** | | | **~9,280 gas** | **~1,280 gas** | |

Worst-case: 9,280 gas (all cold). Realistic hot-path: ~1,280 gas (mintPacked_ and level warm from prior reads in _purchaseFor).

Note: Called ONCE per purchase via _purchaseFor (line 782: `_playerActivityScore(buyer, questStreak)`). This is the "compute-once" pattern from v14.0 -- previously, score was computed multiple times per purchase.

**Section 3: handlePurchase (v14.0 NEW -- DegenerusQuests line ~798)**

Profile handlePurchase(player, ethMintUnits, burnieMintUnits, lootBoxAmount, priceWei, mintPrice):

| Operation | Type | Count | Gas (cold) | Gas (warm) | Notes |
|-----------|------|-------|------------|------------|-------|
| Function entry + parameter decoding | Misc | 1 | ~50 | ~50 | |
| `quests` storage reads (activeQuests[0], activeQuests[1]) | SLOAD | 2 | 4,200 | 200 | Quest slot data |
| `levelQuestType` packed read | SLOAD | 1 | 2,100 | 100 | Level quest type + version |
| `levelQuestPlayerState[player]` | SLOAD | 1 | 2,100 | 100 | Per-player level quest state |
| `questPlayerState[player]` packed read | SLOAD | 1 | 2,100 | 100 | Per-player daily quest state |
| Quest progress arithmetic (target calc, delta calc per slot) | Arithmetic | ~20 | 100 | 100 | Target = questTarget * mintPrice / PRICE_COIN_UNIT |
| Loop over 2 daily quest slots | Loop | 2 iter | -- | -- | Fixed 2 iterations, not variable |
| `_handleLevelQuestProgress` internal call | Arithmetic | 1 | ~200 | ~200 | Eligibility check + progress update |
| Progress SSTORE (questPlayerState) | SSTORE | 0-1 | 5,000 | 2,900 | Only if progress changed (dirty slot) |
| Level quest SSTORE (levelQuestPlayerState) | SSTORE | 0-1 | 5,000 | 2,900 | Only if level quest progress changed |
| `coinflip.creditFlip` on quest completion | CALL | 0-2 | ~30,000 each | ~30,000 each | Only on quest completion (rare per-call) |
| Return value encoding (questReward, questType, streak, questCompleted) | Misc | 1 | ~50 | ~50 | |
| **TOTAL (no completion)** | | | **~16,900 gas** | **~3,800 gas** | Common case |
| **TOTAL (with 1 completion + creditFlip)** | | | **~46,900 gas** | **~33,800 gas** | Rare case |

Note: Called once per purchase from _purchaseFor (line ~771). Replaces 3 separate handler calls (handleMint, handleFlip, notifyQuestMint) that existed pre-v14.0. The consolidated call SAVES gas vs the old pattern by batching 3 cross-contract calls into 1.

**Section 4: rollLevelQuest / clearLevelQuest (v13.0 NEW -- in advanceGame)**

Re-verify Phase 155 baseline (22,430 gas for rollLevelQuest):

| Operation | Type | Gas | Notes |
|-----------|------|-----|-------|
| rngWordByDay[day] read | SLOAD | 0 | Already warm from rngGate in advanceGame |
| keccak256(rngWordByDay[day], "LEVEL_QUEST") | Computation | ~36 | 30 + 6 gas |
| entropy % totalWeight + weight scan | Computation | ~100 | 9 iterations max |
| Decimator eligibility check | Computation | ~50 | Warm Slot 0 reads |
| levelQuestType SSTORE | SSTORE | 22,100 | Cold slot, new non-zero value |
| Function call overhead | Misc | ~100 | |
| **Total rollLevelQuest** | | **~22,386 gas** | Rounded: ~22,430 |

clearLevelQuest: ~5,100 gas (SSTORE zero to non-zero or non-zero to zero on levelQuestType, already warm from prior read).

Phase 155 established: +22,430 gas to advanceGame worst-case. This analysis CONFIRMS the Phase 155 baseline -- no additional gas has been added to rollLevelQuest or clearLevelQuest in v14.0.

**Section 5: _evaluateGameOverPossible / _wadPow / _projectedDrip (v11.0 NEW -- in advanceGame)**

Profile _evaluateGameOverPossible(lvl, purchaseLevel):

| Operation | Type | Gas | Notes |
|-----------|------|-----|-------|
| `lvl < 10` check | Arithmetic | 3 | Short-circuit for early levels |
| `_getNextPrizePool()` | SLOAD | 2,100 (cold) / 100 (warm) | Reads nextPool storage |
| `levelPrizePool[purchaseLevel - 1]` | SLOAD | 2,100 (cold) / 100 (warm) | Mapping read |
| `nextPool >= target` check | Arithmetic | 3 | Short-circuit if target already met |
| `levelStartTime` read | SLOAD | 100 | Slot 0, always warm in advanceGame |
| `block.timestamp` | Opcode | 2 | |
| Days remaining arithmetic | Arithmetic | ~15 | Division by 1 days |
| `_getFuturePrizePool()` | SLOAD | 2,100 (cold) / 100 (warm) | Reads futurePool storage |
| `_projectedDrip(futurePool, daysRemaining)` | Pure | ~500 | See below |
| `gameOverPossible` SSTORE | SSTORE | 5,000 / 100 | Depends on current value |
| **Total (worst-case, cold)** | | **~14,023 gas** | |
| **Total (warm, typical in advanceGame)** | | **~923 gas** | Most reads warm from prior advanceGame ops |

_wadPow(DECAY_RATE, daysRemaining):
- Max iterations: 7 (for daysRemaining <= 120, since 2^7 = 128 > 120)
- Per iteration: 2 MUL + 1 DIV + 1 AND + 1 SHR = ~25 gas
- Total: ~175 gas (7 * 25)
- _projectedDrip wraps _wadPow with 1 MUL + 1 SUB + 1 DIV = ~15 gas
- Total _projectedDrip: ~190 gas

Called in advanceGame:
- Once at purchase-phase entry (line ~290: `_evaluateGameOverPossible(lvl, purchaseLevel)`)
- Once per daily iteration when gameOverPossible is true (line ~323-324: conditional re-check)
- Worst case: called on every daily iteration (up to ~120 days). But each call is ~923 gas (warm). Over 120 days: 120 * 923 = ~110,760 gas. This is within the existing advanceGame budget.

Note: _evaluateGameOverPossible was introduced in v11.0 and was ALREADY included in the Phase 152 baseline (6,996,000 gas). It does not add new gas beyond what was already measured. Confirming no regression.

**Section 6: PriceLookupLib.priceForLevel (v14.0 NEW -- pure library)**

Profile priceForLevel(targetLevel):

| Operation | Type | Gas | Notes |
|-----------|------|-----|-------|
| Up to 6 comparisons (if chains) | Arithmetic | 18-30 | 3-5 gas per comparison |
| 1 modulo (targetLevel % 100) | Arithmetic | 5 | Only for levels >= 100 |
| Return value | Misc | 3 | |
| **Total** | | **~21-38 gas** | Pure function, zero SLOADs |

Comparison to removed `price` storage variable:
- Old: 1 SLOAD = 2,100 gas (cold) / 100 gas (warm)
- New: ~30 gas (pure computation)
- Savings: 70-2,070 gas per call

PriceLookupLib is called from multiple sites (per 162-CHANGELOG.md):
- `_coinflipRngGate` (line ~738): 1 call
- `_callTicketPurchase` (line ~864): 1 call
- `_purchaseBurnieLootbox` (line ~1040): 1 call
- `_boonPoolStats` (line ~1115): 1 call
- `claimAffiliateDgnrs` (line ~1358): 1 call
- `mintPrice()` view (line ~2079): 1 call
- `purchaseState()` view (line ~2153): 1 call
- handlePurchase (line ~771 in _purchaseFor): 1 call (passed as parameter)

Each call saves 70-2,070 gas vs the old storage read. Net effect: PriceLookupLib REDUCES gas across all call sites.

**Section 7: advanceGame Gas Ceiling Update**

Starting from Phase 155 baseline (which includes Phase 152 base + quest roll delta):

| Component | Gas | Source |
|-----------|-----|--------|
| Phase 152 base worst-case | 6,996,000 | Phase 152 gas analysis |
| + rollLevelQuest (v13.0) | +22,430 | Phase 155 analysis, confirmed in Section 4 |
| + clearLevelQuest (v13.0) | +5,100 | Section 4 above |
| = Phase 155 subtotal | 7,023,530 | (Phase 155 report rounded to 7,018,430 -- difference is clearLevelQuest not counted separately there) |
| + _evaluateGameOverPossible (v11.0) | 0 | Already in Phase 152 baseline |
| + PriceLookupLib substitution (v14.0) | -70 to -2,070 per site | NET SAVINGS (see Section 6) |
| + _playerActivityScore (v14.0) | 0 in advanceGame | Called in _purchaseFor, NOT in advanceGame loop |
| + handlePurchase (v14.0) | 0 in advanceGame | Called in _purchaseFor, NOT in advanceGame loop |
| **Updated worst-case advanceGame** | **~7,023,530 gas** | Conservative (ignoring PriceLookupLib savings) |
| **Block gas limit** | **14,000,000** | |
| **Safety margin** | **1.99x** | 14,000,000 / 7,023,530 = 1.993x |

CRITICAL NOTE: _playerActivityScore and handlePurchase are called from _purchaseFor (MintModule), which is a user-initiated purchase transaction -- NOT from the advanceGame loop. They do not affect the advanceGame gas ceiling. The advanceGame loop only calls rollLevelQuest, clearLevelQuest, and _evaluateGameOverPossible (which is already in the baseline).

**Section 8: Per-Purchase Gas Impact**

While not part of advanceGame, document the per-purchase gas impact of new v14.0 paths:

| New Path | Worst-Case Gas | Typical Gas | Context |
|----------|---------------|-------------|---------|
| _playerActivityScore | ~9,280 (cold) | ~1,280 (warm) | Once per purchase |
| handlePurchase | ~16,900 (no completion) | ~3,800 (warm) | Once per purchase, replaces 3 old calls |
| PriceLookupLib (per call) | ~30 | ~30 | Replaces 100-2,100 gas SLOAD |

Net per-purchase: handlePurchase consolidation SAVES gas vs old 3-call pattern. PriceLookupLib saves gas at every call site. _playerActivityScore compute-once pattern saves gas vs old multiple-computation pattern.

**Section 9: Conclusion**

State: GAS-01 SATISFIED.
- Score calculation: ~1,280 gas (warm), called per-purchase not in advanceGame
- Quest roll: +22,430 gas to advanceGame (confirmed Phase 155 baseline)
- Drip projection: already in Phase 152 baseline, no regression
- PriceLookupLib: net gas savings vs removed storage variable
- handlePurchase: net gas savings vs old 3-call pattern
- advanceGame worst-case: ~7,023,530 gas, safety margin 1.99x against 14M block limit
- No regression from Phase 155 baseline
  </action>
  <verify>
    <automated>grep -c "GAS-01 SATISFIED\|SLOAD\|Safety margin\|worst-case" .planning/phases/166-rng-gas-verification/166-02-GAS-CEILING-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/166-rng-gas-verification/166-02-GAS-CEILING-AUDIT.md`
    - grep "GAS-01 SATISFIED" returns a match
    - grep "_playerActivityScore" returns at least 3 matches (Section 2, Section 7, Section 8)
    - grep "handlePurchase" returns at least 3 matches (Section 3, Section 7, Section 8)
    - grep "PriceLookupLib" returns at least 3 matches (Section 6, Section 7, Section 8)
    - grep "_evaluateGameOverPossible" returns at least 2 matches (Section 5, Section 7)
    - grep "_wadPow" returns at least 1 match (Section 5)
    - grep "rollLevelQuest" returns at least 2 matches (Section 4, Section 7)
    - grep "1.99x\|1.993x" returns at least 1 match (safety margin)
    - grep "14,000,000\|14M" returns at least 1 match (block gas limit)
    - grep "7,023,530\|7,018,430" returns at least 1 match (worst-case ceiling)
    - grep "Phase 152\|Phase 155" returns at least 2 matches (baseline citations)
    - grep "SLOAD" returns at least 5 matches (static analysis tables)
    - No occurrences of "TODO" or "TBD" in the file
  </acceptance_criteria>
  <done>Complete gas ceiling audit report with static analysis profiles for all 6 new computation paths, advanceGame ceiling confirmed at 1.99x safety margin against 14M block limit, no regression from Phase 155 baseline. GAS-01 requirement satisfied.</done>
</task>

</tasks>

<verification>
- GAS-01 requirement addressed: all new computation paths profiled with SLOAD/SSTORE/call counts
- Static analysis method per D-02 (no forge execution)
- advanceGame ceiling verified against 14M block gas limit per ROADMAP success criteria #3
- Phase 155 baseline cited and confirmed (no regression)
- Per-purchase paths documented separately from advanceGame paths
</verification>

<success_criteria>
- `.planning/phases/166-rng-gas-verification/166-02-GAS-CEILING-AUDIT.md` exists with all 6 computation path profiles
- advanceGame safety margin documented as 1.99x
- Every gas table uses SLOAD/SSTORE/STATICCALL counts (static analysis per D-02)
- Phase 152 and Phase 155 baselines cited
- Zero TODO/TBD items in the report
</success_criteria>

<output>
After completion, create `.planning/phases/166-rng-gas-verification/166-02-SUMMARY.md`
</output>
