# Requirements — v65.0 Token Rename (BURNIE→FLIP + Stonk→DGNRS/sDGNRS) — ARCHIVED

> **✅ SHIPPED + PUSHED 2026-06-16** — rename commit `44642aba`, `origin/main` `4adcd5f4`. All 21 requirements satisfied. Outcome: full FUNCTIONAL PARITY (only names/selectors/metadata strings changed); forge **889/0/110 identical to baseline**, hardhat baseline-parity, EIP-170 + gas + 16.7M-ceiling all hold. Engine `scripts/v65_flip_rename.py`; surface map `.planning/v65-FLIP-RENAME-SURFACE-MAP.md`.
>
> Scope = this repo only (contracts + forge/JS tests + deploy scripts). BROAD rebrand. Two renames: currency `BURNIE`→`FLIP` + equity `DegenerusStonk`→`DGNRS` / `StakedDegenerusStonk`→`sDGNRS`. Plus the USER-spotted follow-ons folded in: `DGVB`→`DGVF`, `flipWindowOpen`→`ticketRedemptionOpen`, and `optimizer_runs` 200/50→1000 (resolved a surfaced via_ir stack-too-deep).

## Contract & file renames (CTR)
- [x] **CTR-01**: `BurnieCoin` → `FLIP` (`FLIP.sol`). ✓
- [x] **CTR-02**: `BurnieCoinflip` → `Coinflip` (`Coinflip.sol`). ✓
- [x] **CTR-03**: `IBurnieCoinflip` (+ Player/LinkReward/Affiliate) → `ICoinflip*` (`interfaces/ICoinflip.sol`). ✓
- [x] **CTR-04**: `IBurnieCoin` → `IFLIP`; `IBurnieTombstone` → `IFlipTombstone`. ✓

## On-chain metadata strings (STR)
- [x] **STR-01**: `name "Burnies"` → `"Degenerus Gambling Token"`. ✓
- [x] **STR-02**: `symbol "BURNIE"` → `"FLIP"`. ✓

## Identifier renames — BROAD (IDT)
- [x] **IDT-01**: all `burnie*`/`Burnie*`/`BURNIE*` token identifiers → `flip*`/`Flip*`/`FLIP*`. ✓
- [x] **IDT-02**: the 11 `coin`-as-the-token identifiers → `flip*`. ✓
- [x] **IDT-03**: comments/docstrings/string-literals naming the token → `FLIP`. ✓

## Stonk → DGNRS/sDGNRS rebrand (STK)
- [x] **STK-01**: `DegenerusStonk` → `DGNRS` (`DGNRS.sol`); `StakedDegenerusStonk` → `sDGNRS` (`sDGNRS.sol`). ✓
- [x] **STK-02**: display names → `"Degenerus Protocol Equity Token"` / `"…(staked)"`. (Symbols already `DGNRS`/`sDGNRS`.) ✓
- [x] **STK-03**: `IStakedDegenerusStonk`→`IsDGNRS` (collision fix Admin `IsDGNRS`→`IsDGNRSVotes`), `IStakedDegenerusStonkBurn`→`IsDGNRSBurn`, `IDegenerusStonkWrapper`→`IDGNRS`, `OnlyStakedDegenerusStonk`→`OnlysDGNRS`, `stonk`→`staked`. KEEP `ISDGNRSSnapshot`, address keys. ✓

## Carve-outs / parity (KEEP)
- [x] **KEEP-01**: creator persona untouched — `@author Burnie Degenerus` (19 files), `burnie@degener.us`. ✓ (grep-verified)
- [x] **KEEP-02**: the `burn` verb untouched. ✓
- [x] **KEEP-03**: the `coinflip`/`flip` action + generic `coin` untouched (incl. `consumeCoinflipsForBurn`, `IDegenerusCoin`, `IVaultCoin`, `PRICE_COIN_UNIT`). ✓

## External-impact manifest (EXT)
- [x] **EXT-01**: selector / event-topic / artifact-name change manifest delivered (surface-map §6/§10/§11). ✓ — indexer/ABI consumers must re-vendor (carried as a downstream follow-up; a standalone manifest can be cut on request).

## Tests & scripts (TST)
- [x] **TST-01**: 9 test files renamed; all test files' contract/identifier refs + name/symbol assertions updated. ✓
- [x] **TST-02**: artifact-name deps updated (`predictAddresses.js` `KEY_TO_CONTRACT`, `deployFixture`, `package.json`). ✓
- [x] **TST-03**: forge 889/0/110 identical; hardhat non-widening (the 2 byte-identical-to-HEAD guards self-resolved post-commit). ✓

## Verify & re-attest (VER)
- [x] **VER-01**: functional-parity proven — 31-file contract diff is 1037+/1037− balanced (name-only); forge identical. ✓
- [x] **VER-02**: EIP-170 — all contracts under 24,576 at runs=1000 (Game margin 4,149; MintModule 23,156/1,420). ✓
- [x] **VER-03**: renamed subject re-frozen at `44642aba`; closure recorded (this archive + MILESTONES.md). ✓

## Future / Out of scope (carried forward)
- Website + papers repo BURNIE→FLIP (separate repo; papers-first pilot per the original plan).
- The 3 post-v64 contract commits (`eb8e91b2`/`ab1214c3`/`ff09d19b`) remain UNAUDITED — future audit-milestone candidate.
- Coin art / logo unchanged (per USER).

## Traceability — final
| Phase | Requirements | Status |
|---|---|---|
| 406 FOUNDATION | KEEP-01/02/03, EXT-01(draft) | ✅ |
| 407 CONTRACT REFACTOR | CTR-01..04, STR-01/02, IDT-01/02/03, STK-01/02/03, KEEP-01/02/03 | ✅ |
| 408 TESTS & SCRIPTS | TST-01/02/03 | ✅ |
| 409 VERIFY & RE-ATTEST | VER-01/02/03, EXT-01(final) | ✅ |

**21/21 requirements complete. 100% coverage.**
