---
phase: 348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p
plan: 05
type: execute
wave: 3
depends_on: ["348-01", "348-02", "348-03", "348-04"]
files_modified:
  - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-IMPL-EDIT-ORDER-MAP.md
autonomous: true
requirements: [ARCH-04]

must_haves:
  truths:
    - "The producer-before-consumer edit-order for the single 349 batched diff is fixed: code-size reclaim FIRST → DegenerusGameStorage append → GameAfkingModule → AdvanceModule STAGE insertion → interfaces → AfKing thin stubs (no downstream file ships an intermediate broken state)"
    - "The code-size reclaim is sequenced AHEAD of the stub additions per 348-CODE-SIZE-PLAN.md, with the running-total < 24,576 carried in so the Game never breaches the ceiling mid-flight"
    - "The DegenerusGameStorage append is laid out: the subscriber set (_subOf/_subscribers/_subscriberIndex) + the process/open cursors + the per-sub (index, amount, day) stamp + lastAutoBoughtDay/lastOpenedIndex + the v54 afkingFunding ledger — layout-safe append"
    - "The GameAfkingModule contents are enumerated: subscribe/setters + the required-path process STAGE + the open-pass + the router (its bytecode is its OWN budget, not the Game's)"
    - "The AdvanceModule STAGE insertion is specified: before rngGate (~:273), the subsFullyProcessed/_subCursor guard + the requestLootboxRng no-interleave guard (per 348-PLACEMENT-DECISION.md + 348-FREEZE-PROOF.md FREEZE-02)"
    - "The two-path open reconciliation is recorded: the afking-stamp open route + the human lootboxEth/boxPlayers route share no mutable-state hazard (producer-before-consumer)"
    - "The carried corrections are threaded into the edit-order: the box-seed abi.encode re-pin (348-01), the no-try/catch REVERT-02 correction (348-03 INVARIANT-CARRY), the required-path override (348-04), and the EV-cap-at-open with the buy-time write bypassed"
    - "A re-pin-before-authoring caution is present: every grep is a point-in-time snapshot of 20ca1f79; if the subject HEAD moves before 349, re-run the attestation"
  artifacts:
    - path: ".planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-IMPL-EDIT-ORDER-MAP.md"
      provides: "The producer-before-consumer edit-order map for the single 349 batched diff — reconciling the GameAfkingModule + storage append + two-path open, code-size-safe, with the carried corrections threaded in"
      contains: "producer-before-consumer"
  key_links:
    - from: "348-IMPL-EDIT-ORDER-MAP.md"
      to: "348-CODE-SIZE-PLAN.md (the running-total reclaim sequence)"
      via: "reclaim-FIRST ordering carried into the edit-order so the Game stays < 24,576 mid-flight"
      pattern: "24576|reclaim"
    - from: "348-IMPL-EDIT-ORDER-MAP.md"
      to: "348-FREEZE-PROOF.md FREEZE-02 + 348-PLACEMENT-DECISION.md"
      via: "the AdvanceModule STAGE insertion with the subsFullyProcessed no-interleave guard"
      pattern: "subsFullyProcessed"
---

<objective>
Author `348-IMPL-EDIT-ORDER-MAP.md` — the DOWNSTREAM CONSUMER deliverable that reconciles every upstream 348 doc into a single producer-before-consumer edit-order map for the 349 IMPL diff. This is the analog of `343-IMPL-EDIT-ORDER-MAP.md` (343-04): it consumes the re-pinned anchors (348-01), the code-size running-total (348-02), the freeze proof + the corrected invariant set (348-03), and the placement decision (348-04), and fixes the SINGLE 349 diff sequence so no downstream file ships an intermediate broken state and the Game never breaches 24,576 mid-flight.

The producer-before-consumer ordering (per CONTEXT.md <downstream_consumer> + <code_context> Integration Points): code-size reclaim FIRST → `DegenerusGameStorage` append → `GameAfkingModule` (subscribe/setters + process STAGE + open-pass + router) → `AdvanceModule` STAGE insertion (before `rngGate`, the guards) → interfaces → `AfKing.sol` thin dispatch stubs.

Purpose: 349 IMPL authors ONE fully-reconciled, code-size-safe diff against THIS map with zero "by construction" assumptions and zero intermediate broken state — the GameAfkingModule + storage-append + two-path-open shapes are pre-reconciled here.
Output: One markdown artifact in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-CONTEXT.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-GREP-ATTESTATION.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-CODE-SIZE-PLAN.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-FREEZE-PROOF.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-INVARIANT-CARRY.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-PLACEMENT-DECISION.md

<interfaces>
<!-- CONSUMES all upstream 348 docs. The 343-IMPL-EDIT-ORDER-MAP.md is the format precedent (FINAL signatures + storage shape + producer-before-consumer edit order + the carried corrections + a re-pin-before-authoring caution). -->
<!-- Producer-before-consumer chain (CONTEXT.md <code_context> Integration Points): -->
<!--   1. code-size reclaim FIRST (claimAffiliateDgnrs→BingoModule + read-aggregators) — per 348-CODE-SIZE-PLAN.md running-total -->
<!--   2. DegenerusGameStorage append (subscriber set + process/open cursors + per-sub (index,amount,day) stamp + lastAutoBoughtDay/lastOpenedIndex + the v54 afkingFunding ledger) -->
<!--   3. GameAfkingModule (subscribe/setters + the required-path process STAGE + the open-pass + the router) -->
<!--   4. AdvanceModule STAGE insertion (before rngGate ~:273; subsFullyProcessed/_subCursor guard + the requestLootboxRng no-interleave guard) -->
<!--   5. interfaces -->
<!--   6. AfKing.sol thin dispatch stubs -->
<!-- Carried corrections to thread in: box-seed abi.encode (348-01); no-try/catch REVERT-02 (348-03); required-path override (348-04); EV-cap-at-open buy-time-write-bypassed (348-03). -->
<!-- NOTE: 349-owned design (BOX/REVERT/EVCAP/CONSENT/PLACE-02) is the BUILD; this map sequences it but does NOT re-decide it. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 348-IMPL-EDIT-ORDER-MAP.md — the producer-before-consumer edit-order reconciling all upstream 348 docs for the 349 diff</name>
  <files>.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-IMPL-EDIT-ORDER-MAP.md</files>
  <read_first>
    - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-GREP-ATTESTATION.md (the re-pinned anchors for every edit site)
    - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-CODE-SIZE-PLAN.md (the running-total reclaim sequence — carry reclaim-FIRST into the edit-order)
    - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-FREEZE-PROOF.md (FREEZE-02 the no-interleave guard the STAGE insertion must honor; FREEZE-03 the stamped-day open)
    - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-INVARIANT-CARRY.md (the no-try/catch REVERT-02 correction; the EV-cap-at-open buy-time-write-bypassed; the slice-builder invariants to preserve verbatim)
    - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-PLACEMENT-DECISION.md (the required-path STAGE mechanism + the inherited mint-gate standing)
    - .planning/PLAN-V55-AFKING-IN-GAME-REDESIGN.md (§1 the architecture [state → DegenerusGameStorage; logic → GameAfkingModule; Game → thin stubs]; §2 the box redesign producer/open shapes; §8 the Sub-record-width open item; §10 the canonical design)
    - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-IMPL-EDIT-ORDER-MAP.md (the format precedent: FINAL signatures + storage shape + producer-before-consumer edit order + the carried corrections + the re-pin-before-authoring caution)
    - contracts/storage/DegenerusGameStorage.sol (re-read the append point — confirm the layout-safe append location for the subscriber set + cursors + stamp + afkingFunding)
    - contracts/AfKing.sol (re-read the subscribe/setters/process/open surface that folds into GameAfkingModule + collapses to thin stubs)
  </read_first>
  <action>
    Author `348-IMPL-EDIT-ORDER-MAP.md` as the producer-before-consumer edit-order map for the SINGLE 349 batched diff, mirroring the 343-IMPL-EDIT-ORDER-MAP.md format. Structure: (1) a "Final reconciled shapes" section — the `DegenerusGameStorage` append layout (the subscriber set `_subOf`/`_subscribers`/`_subscriberIndex` + the process/open cursors + the per-sub `(index, amount, day)` stamp + `lastAutoBoughtDay`/`lastOpenedIndex` + the v54 `afkingFunding` ledger, laid out as a layout-safe append; note the §8 Sub-record-width finding from 348-03 INVARIANT-CARRY [likely 2 slots]); the `GameAfkingModule` contents (subscribe/setters + the required-path process STAGE + the open-pass + the router — its bytecode is its OWN budget); the two open routes (afking-stamp + human `lootboxEth`/`boxPlayers`) with the shared-no-mutable-state-hazard note; the `AfKing.sol` thin-dispatch-stub collapse. (2) The EDIT-ORDER MAP as an ordered table (Step | File | Edit | Depends-on-prior | Code-size running-total [from 348-CODE-SIZE-PLAN.md] | Notes): code-size reclaim FIRST (claimAffiliateDgnrs→BingoModule + read-aggregators drop-view/→lens, carrying the running-total < 24,576) → `DegenerusGameStorage` append → `GameAfkingModule` (subscribe/setters + process STAGE + open-pass + router) → `AdvanceModule` STAGE insertion (before `rngGate` ~:273; the `subsFullyProcessed`/`_subCursor` guard + the `requestLootboxRng` no-interleave guard per 348-FREEZE-PROOF FREEZE-02 + 348-PLACEMENT-DECISION) → interfaces → `AfKing.sol` thin stubs — proving no downstream file ships an intermediate broken state (each step's producers precede its consumers) and the Game never breaches 24,576 mid-flight. (3) A "Carried corrections threaded into the edit-order" section explicitly listing where each upstream correction lands: the box-seed `abi.encode` re-pin (348-01) → the open-pass materialization step; the no-try/catch REVERT-02 correction (348-03) → the process/open STAGE (no valve; fail-loud-on-solvency; terminal-routing-unblocked); the required-path override (348-04) → the AdvanceModule STAGE step; the EV-cap-at-open with the buy-time write BYPASSED (348-03) → the open-pass step. (4) A "Re-pin before authoring" caution: every grep is a point-in-time snapshot of `20ca1f79`; if the subject HEAD moves before 349, re-run `348-GREP-ATTESTATION.md` (the 343 hand-off precedent). State that 349-owned design (BOX/REVERT/EVCAP/CONSENT/PLACE-02) is the BUILD — this map SEQUENCES it, it does not re-decide it. Do NOT paste contract bodies — cite by re-pinned `file:line`.
  </action>
  <acceptance_criteria>
    - 348-IMPL-EDIT-ORDER-MAP.md exists with an ordered edit-order map table (the string "producer-before-consumer" appears)
    - The edit-order is: code-size reclaim FIRST → DegenerusGameStorage append → GameAfkingModule → AdvanceModule STAGE insertion → interfaces → AfKing thin stubs (all six stages present in order)
    - The code-size running-total is carried in from 348-CODE-SIZE-PLAN.md, shown < 24,576 across the reclaim-then-add sequence (the string "24576"/"24,576" appears)
    - The AdvanceModule STAGE insertion step cites the subsFullyProcessed no-interleave guard (the string "subsFullyProcessed" appears)
    - The "Carried corrections threaded into the edit-order" section names the box-seed abi.encode re-pin, the no-try/catch correction, the required-path override, and the EV-cap-buy-time-write-bypassed
    - A re-pin-before-authoring caution is present
    - No contracts/*.sol modified: `git diff --name-only -- contracts/` is empty
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-IMPL-EDIT-ORDER-MAP.md; test -f "$F" && grep -qi "producer-before-consumer" "$F" && grep -qE "24576|24,576" "$F" && grep -q "DegenerusGameStorage" "$F" && grep -q "GameAfkingModule" "$F" && grep -q "subsFullyProcessed" "$F" && grep -qiE "abi.encode" "$F" && grep -qiE "re-pin|re-grep|re-run" "$F" && [ -z "$(git diff --name-only -- contracts/)" ]</automated>
  </verify>
  <done>348-IMPL-EDIT-ORDER-MAP.md fixes the producer-before-consumer edit-order for the 349 diff (reclaim FIRST → storage append → GameAfkingModule → AdvanceModule STAGE → interfaces → AfKing stubs), carries the code-size running-total < 24,576, threads in the four carried corrections, and includes the re-pin-before-authoring caution. Zero contracts/ edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Paper-only SPEC phase; no code ships. The threat surface is the integrity of the edit-order that 349 authors against — a wrong ordering ships an intermediate broken state (a consumer edited before its producer) or breaches 24,576 mid-flight, and a lost carried-correction (the box-seed re-pin / the no-try/catch / the required-path override / the EV-cap bypass) means 349 builds the wrong shape. The documentation-integrity threats: an edit-order that breaches the ceiling mid-flight, a producer-after-consumer hazard, or an un-threaded correction surviving into IMPL.

| Boundary | Description |
|----------|-------------|
| edit-order producers → consumers | If a consumer (e.g., AfKing thin stubs, or the AdvanceModule STAGE) is edited before its producer (the GameAfkingModule / the storage append), 349 ships an intermediate broken state. The producer-before-consumer chain is the boundary. |
| code-size running-total → the ceiling | If the reclaim is not sequenced AHEAD of the stub additions, the Game breaches 24,576 mid-flight and won't deploy. The running-total carried from 348-CODE-SIZE-PLAN.md is the boundary. |
| upstream corrections → the 349 build | The box-seed re-pin, the no-try/catch correction, the required-path override, and the EV-cap bypass must each be threaded to a specific edit step, not lost. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-348-10 | Tampering | the producer-before-consumer edit-order | mitigate | The edit-order is an ordered table with an explicit Depends-on-prior column proving each step's producers precede its consumers (storage append → GameAfkingModule → AdvanceModule STAGE → interfaces → AfKing stubs); no intermediate broken state. |
| T-348-11 | Tampering | the code-size ceiling mid-flight | mitigate | The running-total from 348-CODE-SIZE-PLAN.md is carried into the edit-order with reclaim sequenced FIRST, every row shown < 24,576 — the Game never breaches the ceiling mid-flight (the 349 build is the final verification). |
| T-348-12 | Information disclosure | a lost carried correction | mitigate | A dedicated "Carried corrections threaded into the edit-order" section binds each upstream correction (box-seed abi.encode, no-try/catch REVERT-02, required-path override, EV-cap buy-time-write-bypassed) to a specific edit step. |
| T-348-SC | Tampering | npm/pip/cargo installs | accept | No package installs — paper-only Markdown authoring; the only CLI is `git diff` + `grep`/read (read-only). No Package Legitimacy Audit needed; zero installs. |
</threat_model>

<verification>
- 348-IMPL-EDIT-ORDER-MAP.md exists in the phase directory.
- The producer-before-consumer edit-order is fixed (reclaim FIRST → storage append → GameAfkingModule → AdvanceModule STAGE → interfaces → AfKing stubs) with the running-total < 24,576 carried in.
- The four carried corrections are threaded to edit steps; the re-pin-before-authoring caution is present.
- No contracts/*.sol modified: `git diff --name-only -- contracts/` is empty.
</verification>

<success_criteria>
- 348-IMPL-EDIT-ORDER-MAP.md satisfies all Task 1 acceptance criteria (ROADMAP Phase 348 Success Criterion 5, the producer-before-consumer reconciliation slice + the ARCH-04 sequencing).
- The GameAfkingModule + storage-append + two-path-open shapes are reconciled producer-before-consumer for 349.
- Zero contracts/*.sol edits.
</success_criteria>

<output>
Create `.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-05-SUMMARY.md` when done
</output>
