---
phase: 348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p
plan: 03
type: execute
wave: 2
depends_on: ["348-01"]
files_modified:
  - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-FREEZE-PROOF.md
  - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-INVARIANT-CARRY.md
autonomous: false
requirements: [FREEZE-01, FREEZE-02, FREEZE-03]

must_haves:
  truths:
    - "FREEZE-02 PROVEN against source: the process-pass reads LR_INDEX once at pass start and binds the stamp to the pre-RNG index; the no-interleave guard is SPECIFIED (block requestLootboxRng while !subsFullyProcessed, OR order the STAGE strictly before any index advance) — verified against AdvanceModule:1016 + the index advance :1089/:1629"
    - "FREEZE-03 PROVEN: the box seed keccak256(abi.encode(rngWord, player, day, amount)) (per the 348-01 re-pin at :534) seeds with the STAMPED buy-day, never the open-time _simulatedDayIndex(); no block.timestamp/number/prevrandao/coinbase/blockhash in the draw"
    - "FREEZE-01 SPLIT: the stamped fields (index, amount, day) are genuinely frozen + proven; the live-read fields (score/baseLevel/EV-cap) are filed as a WRITTEN accepted-by-design KNOWN ISSUE (D-348-05, precedent 339-01 D-03), with the −EV/no-credible-actor rationale, NOT proven-airtight and NOT red-teamed by /economic-analyst"
    - "The accepted-by-design known issue is marked to carry into audit/FINDINGS-v55.0.md + the v52 cumulative sweep as dispositioned/known"
    - "The early-slot post-RNG window-closure idea is recorded as DROPPED (the window is accepted, not closed; the VRF-timing must-verify is dropped)"
    - "INVARIANT-CARRY restates the proof's §5 obligations AS AMENDED by D-348-04: obligation 4 (try/catch valve) is DROPPED; the new form is no-valve = revert-free-by-construction (obl 1) + fail-loud-on-solvency (class B) + terminal-routing-unblocked (class C)"
    - "The REVERT-02 correction is recorded explicitly (parallel to how 343's D-01 corrected AUTOBUY-02): REQUIREMENTS.md REVERT-02 + proof §5 obl-4 both say 'try/catch valve' → corrected to no-valve"
    - "The §10 process-rule (2) becomes a PRE-EMPTIVE skip (the slice builder declines to build an unbuildable slice; under obligation 1 there are none in a healthy game — proven effectively unreachable); the §10 per-cycle eviction cap is re-evaluated (lost its revert-driven rationale)"
    - "The 3 §7 SPEC follow-ups are DISCHARGED: cost-unit reconciliation (mp·effectiveQty vs priceForLevel·ticketQuantity/(4·TICKET_SCALE)); the (index, amount, day) stamp field widths; the double-draw guard (process-pass never routes through _callTicketPurchase's lootbox EV tally)"
    - "EVCAP carry: the afking open routes through _applyEvMultiplierWithCap(player, level+1, amount, mult) keyed [player][level+1], exactly once, hard-clamped ≤10 ETH (no revert), with the buy-time EV write BYPASSED — proven equivalent to the v54 per-(sub,level) accumulator"
    - "A LIGHT /contract-auditor pass scoped to obligation-1 has run (D-348-06): the slice-builder invariants (ev=cost−claimableUse + enum payKind; the 1-wei claimable sentinel; the LOOTBOX_MIN transient skip; quantity ≥ 1) are stated correctly for the fold; result recorded with per-invariant disposition"
  artifacts:
    - path: ".planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-FREEZE-PROOF.md"
      provides: "FREEZE-01/02/03 — the freeze spine proven on paper (index-binding + stamped-day determinism) + the live-read accepted-by-design known issue"
      contains: "FREEZE-02"
    - path: ".planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-INVARIANT-CARRY.md"
      provides: "The discharged REVERT-FREE-CHAIN + EV-cap invariants carried as locked SPEC invariants with the D-348-04 try/catch correction + the 3 §7 follow-ups discharged + the light /contract-auditor obligation-1 pass"
      contains: "D-348-04"
  key_links:
    - from: "348-FREEZE-PROOF.md FREEZE-02"
      to: "AdvanceModule requestLootboxRng :1016 + the index advance :1089/:1629"
      via: "the no-interleave guard specification (block while !subsFullyProcessed / order STAGE before advance) + LR_INDEX-read-once proof"
      pattern: "subsFullyProcessed|LR_INDEX"
    - from: "348-INVARIANT-CARRY.md"
      to: "REQUIREMENTS.md REVERT-02 (349-owned) + proof §5 obligation 4"
      via: "the D-348-04 correction — drop the try/catch valve; record the rewrite (343 D-01 precedent)"
      pattern: "REVERT-02|try/catch"
---

<objective>
Author the FREEZE-spine proof + the discharged-invariant carry — the SECURITY SPINE of Phase 348. Two discrete docs in one plan (the analog of 343-02's SOLVENCY-PROOF + SOLVENCY-REDTEAM):

1. `348-FREEZE-PROOF.md` (FREEZE-01 / FREEZE-02 / FREEZE-03) — PROVE the freeze spine on paper against the re-pinned anchors: freeze-completeness (FREEZE-01, with the live-read window filed as a written accepted-by-design KNOWN ISSUE per D-348-05), pre-RNG index-binding (FREEZE-02, with the no-interleave guard SPECIFIED against source — the load-bearing D-348-02 proof obligation), stamped-day determinism (FREEZE-03).

2. `348-INVARIANT-CARRY.md` — carry the discharged REVERT-FREE-CHAIN + EV-cap invariants as locked SPEC invariants, AS AMENDED by D-348-04 (the try/catch valve is DROPPED), discharge the 3 §7 follow-ups, and run the LIGHT `/contract-auditor` pass scoped to obligation-1 (D-348-06 — the slice-builder fidelity that is now the SOLE no-brick guarantor under required-path).

The FREEZE proof and the invariant carry share a substrate: obligation-1 (slice-builder fidelity) is BOTH the no-brick guarantor (carry) AND the substrate the freeze rests on, and the light `/contract-auditor` pass is their shared gate — hence one plan, `autonomous: false` (the auditor pass is the gate, mirroring 343-02's red-team gate).

Purpose: 349 IMPL inherits a PROVEN freeze spine + the corrected (no-try/catch) invariant set + a verified obligation-1, so it can build the box-stamp + the process-pass without re-opening the proof. The live-read window is documented as known so the 352 sweep treats it as dispositioned.
Output: Two markdown artifacts in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-CONTEXT.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-GREP-ATTESTATION.md

<interfaces>
<!-- CONSUMES the re-pinned anchors from 348-GREP-ATTESTATION.md (348-01) — cite the re-grepped lines, never the upstream doc lines. -->
<!-- FREEZE-02 (D-348-02 PROOF OBLIGATION, load-bearing): verify against AdvanceModule source whether requestLootboxRng (:1016 → index advance :1089/:1629) is reachable mid-STAGE; SPECIFY the guard. -->
<!-- FREEZE-03: seed = keccak256(abi.encode(rngWord, player, day, amount)) at LB:534 (per 348-01 re-pin); STAMPED day not _simulatedDayIndex(). -->
<!-- D-348-05: live-read window (score/baseLevel/EV-cap read LIVE at open) = accepted-by-design known issue, precedent 339-01 D-03; NO /economic-analyst red-team. -->
<!-- proof §5 obligations (PLAN-V55-REVERT-FREE-CHAIN-PROOF.md §5), AS AMENDED: (1) preserve _resolveBuy invariants verbatim; (2) EV-cap at open via _applyEvMultiplierWithCap keyed [player][level+1], buy-time write bypassed; (3) stamp (index, amount, day) + seed open with stamped day; (4) DROPPED (D-348-04). -->
<!-- proof §7 follow-ups: cost-units (mp·effectiveQty vs priceForLevel·ticketQuantity/(4·TICKET_SCALE)); stamp field widths; double-draw guard. -->
<!-- D-348-06: LIGHT /contract-auditor scoped to obligation-1 only (slice-builder invariants stated correctly for the fold). ALL other adversarial probing deferred to 352. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 348-FREEZE-PROOF.md — FREEZE-01/02/03 proven on paper (index-binding + stamped-day) + the live-read known issue</name>
  <files>.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-FREEZE-PROOF.md</files>
  <read_first>
    - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-GREP-ATTESTATION.md (the re-pinned anchors — the box-seed :534 abi.encode resolution; lootboxDay :514; _simulatedDayIndex sites; requestLootboxRng :1016; the index advance :1089/:1629; rngGate :1152)
    - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-CONTEXT.md (D-348-02 the FREEZE-02 proof obligation + the uniform-index-epoch no-interleave requirement; D-348-05 the live-read accepted-by-design known issue + the FREEZE-01 split + the early-slot DROPPED note)
    - .planning/PLAN-V55-AFKING-IN-GAME-REDESIGN.md (§3 "The freeze / security spine" invariants 1-7; §10 "boons OFF → box amount = spend; score/base-level/EV-cap read LIVE at open"; the determinism note in §10)
    - .planning/PLAN-V55-REVERT-FREE-CHAIN-PROOF.md (§4 "Open determinism" — the stamped-day structural closure; the "no block.* in the draw" finding; the monotonicity necessary-but-not-sufficient argument)
    - contracts/modules/DegenerusGameAdvanceModule.sol (re-read around requestLootboxRng :1016, the index advance :1089 and :1629, rngGate :1152, the STAGE insertion point ~:273 — determine whether requestLootboxRng is reachable while the proposed STAGE runs, to SPECIFY the no-interleave guard)
    - contracts/modules/DegenerusGameLootboxModule.sol (re-read the box-seed at :534 + the lootboxDay[index][player] frozen-day read :514 + the _simulatedDayIndex() open-time sites — to prove FREEZE-03 the afking open seeds from the stamped day)
    - .planning/milestones/v50.0-phases/334-design-lock-whale-pass-o-1-claim-afking-pass-gated-subs-mintmodul/334-01-FREEZE-PROOF.md (the freeze-proof precedent shape — if the exact filename differs, read whatever 334-*-FREEZE-PROOF.md exists)
  </read_first>
  <action>
    Author `348-FREEZE-PROOF.md` with three proof sections + the known-issue section. FREEZE-02 (index-binding — the load-bearing D-348-02 obligation): walk the AdvanceModule source and PROVE (a) the process-pass reads `LR_INDEX` once at pass start (a single sub's processing does not straddle within one sub), and (b) the stamp binds to the pre-RNG `LR_INDEX` so a box can never attach to an index whose VRF word already exists — then SPECIFY the no-interleave guard: state explicitly whether `requestLootboxRng` (`:1016`, advancing the index at `:1089`/`:1629`) is reachable while `!subsFullyProcessed`, and require EITHER blocking it while `!subsFullyProcessed` OR ordering the STAGE strictly before any index advance; cite the re-pinned lines. FREEZE-03 (determinism): prove the box draw is a pure function of `(frozen word, frozen stamp)` — the seed `keccak256(abi.encode(rngWord, player, day, amount))` at the re-pinned `:534` carries NO `block.timestamp/number/prevrandao/coinbase/blockhash`, and the afking open MUST seed with the STAMPED buy-day (mirroring the frozen `lootboxDay[index][player]` at `:514`), never the open-time `_simulatedDayIndex()`; include the monotonicity-is-necessary-but-not-sufficient argument (a keeper-who-is-also-the-player could otherwise choose same-day vs later-day for two outcomes) and the boundary-pinned-stamp closure. FREEZE-01 (freeze-completeness): SPLIT it — record that under §10 (boons OFF) the stamped fields `(index, amount, day)` are genuinely frozen + proven (by FREEZE-02 + FREEZE-03), and file the live-read fields (score/baseLevel/EV-cap read LIVE at open) as a WRITTEN ACCEPTED-BY-DESIGN KNOWN ISSUE per D-348-05: state the −EV / no-credible-actor rationale (committed pass-holders via `validThroughLevel`; no on-demand score lever in the ~5-min process→open window; the 80→135% multiplier is streak-built over many levels; a legit full-price deity-pass purchase on a good pending box is fine/−EV), cite the precedent (339-01 D-03 whale-frontrunning-on-VRF-resolution non-finding), mark it to CARRY into `audit/FINDINGS-v55.0.md` + the v52 cumulative sweep as dispositioned/known, and state explicitly it is NOT proven-airtight and NOT red-teamed by `/economic-analyst` (USER established no credible vector). Add a short "Dropped defenses" note: the early-slot post-RNG window-closure idea is DROPPED (the window is accepted, not closed → the afking open stays a normal post-RNG leg → the VRF-timing must-verify is dropped). Do NOT paste contract bodies — quote only the load-bearing lines.
  </action>
  <acceptance_criteria>
    - 348-FREEZE-PROOF.md exists with distinct FREEZE-01, FREEZE-02, FREEZE-03 sections (the strings "FREEZE-01", "FREEZE-02", "FREEZE-03" all present)
    - FREEZE-02 cites the re-pinned requestLootboxRng :1016 + the index advance site(s), states whether requestLootboxRng is reachable mid-STAGE, and SPECIFIES the no-interleave guard (block while !subsFullyProcessed OR order STAGE before the index advance) — the string "subsFullyProcessed" appears
    - FREEZE-03 states the seed is keccak256(abi.encode(rngWord, player, day, amount)), seeds from the STAMPED day (not _simulatedDayIndex()), and carries no block.* entropy — the strings "stamped" (case-insensitive) and "_simulatedDayIndex" both appear
    - FREEZE-01 is split: the stamped (index, amount, day) fields are proven-frozen, and the live-read window is filed as an accepted-by-design known issue (the string "known issue" or "accepted-by-design" appears, the 339-01 precedent is cited, and it is marked to carry into FINDINGS-v55.0.md)
    - The early-slot-dropped note is present
    - No contracts/*.sol modified: `git diff --name-only -- contracts/` is empty
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-FREEZE-PROOF.md; test -f "$F" && grep -q "FREEZE-01" "$F" && grep -q "FREEZE-02" "$F" && grep -q "FREEZE-03" "$F" && grep -q "subsFullyProcessed" "$F" && grep -q "_simulatedDayIndex" "$F" && grep -qiE "stamped" "$F" && grep -qiE "known issue|accepted-by-design|accepted by design" "$F" && grep -qiE "339-01|FINDINGS-v55" "$F" && [ -z "$(git diff --name-only -- contracts/)" ]</automated>
  </verify>
  <done>348-FREEZE-PROOF.md proves FREEZE-02 (index-binding + the no-interleave guard specified against source) + FREEZE-03 (stamped-day determinism, no block.* entropy) and splits FREEZE-01 (stamped fields proven; live-read window filed as an accepted-by-design known issue carried to FINDINGS-v55.0). Zero contracts/ edits.</done>
</task>

<task type="auto">
  <name>Task 2: Author 348-INVARIANT-CARRY.md + run the light /contract-auditor obligation-1 pass (the try/catch correction + the 3 §7 follow-ups)</name>
  <files>.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-INVARIANT-CARRY.md</files>
  <read_first>
    - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-GREP-ATTESTATION.md (the re-pinned _resolveBuy :727 + the :781-782 named-revert comment + the LOOTBOX_MIN skip :772 + cost=mp*effectiveQty :761; _applyEvMultiplierWithCap :459; lootboxEvBenefitUsedByLevel :1469; LOOTBOX_EV_BENEFIT_CAP :1326)
    - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-CONTEXT.md (D-348-04 the try/catch DROP + the REVERT-02 correction + the §10 rule-(2) pre-emptive-skip + the eviction-cap re-evaluation; D-348-06 the light /contract-auditor obligation-1 pass scope; the §7 follow-ups in the "Carried-forward LOCKED design" block)
    - .planning/PLAN-V55-REVERT-FREE-CHAIN-PROOF.md (§5 the 4 LOCKED obligations [obl 4 to be dropped]; §3 the slice-builder discharge table — the exact invariants ev=cost−claimableUse / 1-wei sentinel / LOOTBOX_MIN skip / quantity≥1; §4 the EV-cap-at-open derivation + the double-draw CAUTION; §6 the two residual classes B/C; §7 the 3 follow-ups)
    - .planning/REQUIREMENTS.md (the current REVERT-02 text "A thin per-sub try/catch skip valve…" — the line this doc corrects, parallel to 343's D-01 AUTOBUY-02 correction)
    - contracts/AfKing.sol (re-read _resolveBuy :727-795 — the slice-builder validation the light /contract-auditor pass verifies survives the fold: the :781-782 comment, the LOOTBOX_MIN skip :772, cost=mp*effectiveQty :761, effectiveQty :756/759)
    - contracts/modules/DegenerusGameLootboxModule.sol (re-read _applyEvMultiplierWithCap :459-495 — the read+write-at-open EV-cap shape; confirm the ≤10-ETH hard clamp is no-revert)
  </read_first>
  <action>
    Author `348-INVARIANT-CARRY.md` restating the proof's §5 obligations as the v55 locked invariant set, AS AMENDED. Record obligations (1) preserve `_resolveBuy`'s validation invariants VERBATIM (ev=cost−claimableUse + enum payKind; the 1-wei claimable sentinel; the LOOTBOX_MIN transient skip; quantity ≥ 1) — the migration-fidelity obligation, now the SOLE no-brick guarantor; (2) EV-cap at open via `_applyEvMultiplierWithCap(player, level+1, amount, mult)` keyed `[player][level+1]`, exactly once per open, hard-clamped ≤10 ETH (no revert), with the buy-time EV write BYPASSED for afking boxes (no double-draw), proven equivalent to the v54 per-(sub,level) accumulator; (3) stamp `(index, amount, day)` + seed the open with the STAMPED buy-day. Then record the D-348-04 CORRECTION prominently in its own labeled section "Correction to REVERT-02 + proof §5 obligation 4 — DROP the try/catch valve" (parallel to how 343's D-01 corrected AUTOBUY-02→b.funder): note that REQUIREMENTS.md REVERT-02 (349-owned) AND proof §5 obligation 4 both currently say "thin per-sub try/catch skip valve," and rewrite to the new form: "NO valve: revert-free-by-construction (obligation 1) for the healthy path; fail-loud-on-solvency (class B — swallowing masks a SOLVENCY-01 violation); terminal-routing-unblocked (class C — verify the afking STAGE cannot block game-over routing)." State the consequence: the proof burden CONCENTRATES on obligation 1. Reconcile the two §10 knock-ons: (a) the §10 process-rule (2) "mint slice fails → SKIP" becomes a PRE-EMPTIVE skip (the slice builder declines to build an unbuildable slice; under obligation 1 there are none in a healthy game — PROVE rule (2) is effectively unreachable; note rule-(1) normal eviction of an UNFUNDED sub is unaffected); (b) the §10 "optional per-cycle eviction cap" loses its revert-driven-mass-eviction rationale — RE-EVALUATE whether it stays and record the recommendation. DISCHARGE the 3 §7 follow-ups in a labeled section: (i) reconcile AfKing's `mp · effectiveQty` cost units vs the Game's `priceForLevel · ticketQuantity / (4 · TICKET_SCALE)` (equivalence so the folded process-pass computes `cost` identically); (ii) confirm the `(index, amount, day)` stamp field widths hold `amount` (full wei) + `index` + `day` (likely 2 slots, the §8 Sub-record-width item); (iii) confirm the process-pass never routes through `_callTicketPurchase`'s lootbox EV tally (the obligation-2 double-draw guard). FINALLY run the LIGHT `/contract-auditor` pass scoped to obligation-1 ONLY (D-348-06): instruct the auditor to verify the slice-builder invariants are stated correctly for the fold (not a full re-audit — all other probing is deferred to 352); record the result inline in this doc as a per-invariant disposition table (Invariant | Stated-correctly? | Auditor note), and a one-line verdict. Do NOT paste contract bodies — cite by re-pinned `file:line`.
  </action>
  <acceptance_criteria>
    - 348-INVARIANT-CARRY.md exists and restates obligations 1-3 as the v55 locked invariant set (the strings "ev = cost − claimableUse" or "ev=cost−claimableUse", "1-wei", "LOOTBOX_MIN" all present)
    - The D-348-04 correction section is present: it records that REVERT-02 + proof §5 obligation 4 say "try/catch valve" and rewrites to the no-valve form (the strings "REVERT-02", "try/catch", "fail-loud" or "fail loud", "class B", "class C" all present)
    - The §10 rule-(2) pre-emptive-skip + the eviction-cap re-evaluation are recorded
    - The 3 §7 follow-ups are discharged (cost-units mp·effectiveQty; stamp field widths; the double-draw guard — the strings "effectiveQty" and "double-draw" present)
    - EVCAP carry recorded: _applyEvMultiplierWithCap keyed [player][level+1], buy-time write bypassed, ≤10 ETH no-revert (the strings "_applyEvMultiplierWithCap" and "level+1" present)
    - The light /contract-auditor obligation-1 pass result is recorded as a per-invariant disposition with a verdict (the string "contract-auditor" present)
    - No contracts/*.sol modified: `git diff --name-only -- contracts/` is empty
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-INVARIANT-CARRY.md; test -f "$F" && grep -q "D-348-04" "$F" && grep -q "REVERT-02" "$F" && grep -qi "try/catch" "$F" && grep -qiE "fail.loud" "$F" && grep -qi "class B" "$F" && grep -qi "class C" "$F" && grep -q "LOOTBOX_MIN" "$F" && grep -q "_applyEvMultiplierWithCap" "$F" && grep -qi "level+1" "$F" && grep -qi "double-draw" "$F" && grep -q "contract-auditor" "$F" && [ -z "$(git diff --name-only -- contracts/)" ]</automated>
  </verify>
  <done>348-INVARIANT-CARRY.md carries obligations 1-3 as the v55 locked invariant set, records the D-348-04 try/catch DROP correction (no-valve: revert-free-by-construction + fail-loud-on-solvency + terminal-routing-unblocked), discharges the 3 §7 follow-ups, and records the light /contract-auditor obligation-1 pass disposition. Zero contracts/ edits.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Design-gating checkpoint — lock the FREEZE spine + the no-valve invariant set for 349</name>
  <files>.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-FREEZE-PROOF.md, .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-INVARIANT-CARRY.md</files>
  <action>This is a CHECKPOINT task — no new authoring; the detail is in `<what-built>`/`<how-to-verify>` below and the run PAUSES for USER adjudication after Tasks 1-2 land the two docs. In short: present the FREEZE spine proof (348-FREEZE-PROOF.md) + the discharged-invariant carry with the D-348-04 try/catch correction (348-INVARIANT-CARRY.md) and HOLD for the USER to confirm the freeze spine holds + the no-valve invariant set is correct for 349 to build against — a mis-stated obligation-1 slice-builder invariant or an under-specified FREEZE-02 no-interleave guard is the design-gating block-on item. Do NOT edit any `contracts/*.sol`.</action>
  <what-built>The FREEZE spine proof (348-FREEZE-PROOF.md: FREEZE-01/02/03 — index-binding + stamped-day determinism proven on paper, the live-read window filed as an accepted-by-design known issue) + the discharged-invariant carry (348-INVARIANT-CARRY.md: the proof's §5 obligations carried AS AMENDED by D-348-04 [try/catch valve DROPPED → no-valve: revert-free-by-construction + fail-loud-on-solvency + terminal-routing-unblocked], the 3 §7 follow-ups discharged, the light /contract-auditor obligation-1 pass disposition recorded). This is the security spine of the SPEC — the no-brick guarantee now rests SOLELY on obligation-1 (slice-builder fidelity) under the no-try/catch required-path placement.</what-built>
  <how-to-verify>
    1. Open `.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-FREEZE-PROOF.md`. Confirm FREEZE-02 SPECIFIES the no-interleave guard against the actual AdvanceModule source (block requestLootboxRng while !subsFullyProcessed, OR order the STAGE strictly before the index advance) and that the LR_INDEX-read-once claim is proven, not asserted. Confirm FREEZE-03 seeds from the STAMPED day. Confirm the live-read window is filed as an accepted-by-design KNOWN ISSUE (not silently proven-airtight) and is marked to carry into FINDINGS-v55.0.md.
    2. Open `348-INVARIANT-CARRY.md`. Confirm the D-348-04 try/catch DROP is recorded as an explicit correction to REVERT-02 + proof §5 obligation 4 (the no-valve form), and that the consequence — obligation-1 is now the SOLE no-brick guarantor — is stated. Confirm the light /contract-auditor obligation-1 disposition says the slice-builder invariants are stated correctly for the fold (or flags any that are not).
    3. Decide: does the freeze spine hold and is the no-valve invariant set correct for 349 to build against? If the /contract-auditor pass flagged any obligation-1 invariant as mis-stated, that is a design-gating issue to resolve here before IMPL.
  </how-to-verify>
  <resume-signal>Type "approved" to lock the freeze spine + the corrected invariant set for 349, or describe the issue (e.g., a mis-stated slice-builder invariant, an under-specified no-interleave guard, a freeze gap) to revise.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Paper-only SPEC phase; no code ships. This plan IS the genuine adversarial-security content of the phase: the FREEZE red-team disposition (D-348-05) + the light /contract-auditor obligation-1 pass (D-348-06). The documentation-integrity threats are: a freeze invariant asserted-but-not-proven (esp. the FREEZE-02 no-interleave guard surviving un-specified into IMPL), and an un-verified "by construction" obligation-1 claim (the slice-builder fidelity that is now the SOLE no-brick guarantor under no-try/catch) surviving into the 349 hand-off.

| Boundary | Description |
|----------|-------------|
| process-STAGE ↔ requestLootboxRng index advance | If `requestLootboxRng` (`:1016` → index advance `:1089`/`:1629`) can interleave while `!subsFullyProcessed`, a stamp could bind to an index whose word already exists → freeze broken. The no-interleave guard is the boundary; FREEZE-02 must SPECIFY it against source. |
| slice builder → Game-side reverts (obligation 1) | Under D-348-04 (no try/catch) the funded buy must be revert-free BY CONSTRUCTION; if the slice-builder invariants are mis-stated for the fold, a single sub bricks the day. The light /contract-auditor pass is the boundary check. |
| live-read window (score/baseLevel/EV-cap at open) | An accepted-by-design known issue, NOT a closed boundary — documented with the −EV/no-credible-actor rationale and carried to FINDINGS-v55.0; the 352 sweep treats it as dispositioned, not re-litigated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-348-05 | Tampering | the FREEZE-02 index-binding guard | mitigate | FREEZE-02 walks the AdvanceModule source, proves LR_INDEX is read once at pass start, and SPECIFIES the no-interleave guard (block `requestLootboxRng` while `!subsFullyProcessed` OR order the STAGE strictly before the index advance) — citing the re-pinned `:1016`/`:1089`/`:1629`; the guard does not survive un-specified into 349. |
| T-348-06 | Tampering | manipulate score/boon/EV-cap in the reveal→open window | accept | D-348-05 accepted-by-design known issue: committed pass-holders, no on-demand score lever in the ~5-min window, the multiplier is streak-built, legit-purchase-is-−EV; documented with the 339-01 precedent + carried into FINDINGS-v55.0 + the v52 sweep as dispositioned. No credible actor (USER-established); no /economic-analyst red-team. |
| T-348-07 | Denial of service | a single sub bricking the day (no try/catch, required-path) | mitigate | The light /contract-auditor obligation-1 pass (D-348-06) verifies the slice-builder invariants (the 1-wei sentinel, LOOTBOX_MIN skip, ev=cost−claimableUse, quantity≥1, enum payKind) are stated correctly for the fold — obligation 1 is the SOLE no-brick guarantor; class B fails loud (never mask SOLVENCY-01), class C is terminal (STAGE can't block game-over routing). |
| T-348-SC | Tampering | npm/pip/cargo installs | accept | No package installs — paper-only Markdown authoring; the only agent invoked is `/contract-auditor` (analysis, no install). No Package Legitimacy Audit needed; zero installs. |
</threat_model>

<verification>
- 348-FREEZE-PROOF.md + 348-INVARIANT-CARRY.md exist in the phase directory.
- FREEZE-02 specifies the no-interleave guard against source; FREEZE-03 seeds from the stamped day; FREEZE-01's live-read window is an accepted-by-design known issue carried to FINDINGS-v55.0.
- The D-348-04 try/catch DROP is recorded as a REVERT-02 correction; the light /contract-auditor obligation-1 disposition is recorded.
- No contracts/*.sol modified: `git diff --name-only -- contracts/` is empty.
</verification>

<success_criteria>
- Both docs satisfy their Task acceptance criteria (ROADMAP Phase 348 Success Criteria 1 [FREEZE spine] + 2 [the carried invariants]).
- The human-verify checkpoint is approved (the freeze spine holds + the no-valve invariant set is correct for 349).
- Zero contracts/*.sol edits.
</success_criteria>

<output>
Create `.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-03-SUMMARY.md` when done
</output>
