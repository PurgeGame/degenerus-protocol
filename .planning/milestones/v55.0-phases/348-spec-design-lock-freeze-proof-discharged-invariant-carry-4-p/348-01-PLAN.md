---
phase: 348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-GREP-ATTESTATION.md
autonomous: true
requirements: [FREEZE-02, FREEZE-03, ARCH-04]

must_haves:
  truths:
    - "git diff --numstat 20ca1f79 HEAD -- contracts/ is recorded as EMPTY (the working tree IS the v55 baseline; grep on the live tree IS grep on 20ca1f79) — no SHA checkout needed"
    - "Every cited file:line across the v55 milestone scope is re-pinned to its ACTUAL current line with the matched source text quoted; drift corrected"
    - "The box-seed drift is RESOLVED: the doc's grep-pattern claim (keccak256(abi.encodePacked) at :534) is corrected — the openLootBox seed at :534 is keccak256(abi.encode(rngWord, player, day, amount)) (NOT abi.encodePacked); abi.encodePacked is the PRESALE box at :644 only"
    - "FREEZE-03 anchors confirmed: the frozen buy-day seed lootboxDay[index][player] :514; the open-time _simulatedDayIndex() sites :513/:766/:799/:836/:868 (the day the afking open must NOT copy); no block.timestamp/number/prevrandao/coinbase/blockhash in the draw"
    - "FREEZE-02 anchors confirmed: requestLootboxRng :1016; the RNG-request index advance _lrRead(LR_INDEX...)+1 at :1089 AND :1629; rngGate :1152; LR_INDEX read sites enumerated"
    - "EV-cap anchors confirmed: _applyEvMultiplierWithCap :459; lootboxEvBenefitUsedByLevel :1469; LOOTBOX_EV_BENEFIT_CAP=10 ether :1326"
    - "slice-builder anchors confirmed: _resolveBuy :727 (body to :795); the named-revert comment :781-782; the LOOTBOX_MIN transient skip :772; LOOTBOX_MIN decl :269; cost = mp*effectiveQty :761"
    - "OPEN-E / set-mutation anchors confirmed: isOperatorApproved interface :43; the subscribe-time OPEN-E gate (re-pinned, doc cites :400-409); validThroughLevel :103; fundingSource :106; src/funder resolution (re-pinned, doc cites :682)"
    - "ARCH-04 reclaim-target sites confirmed: claimAffiliateDgnrs DegenerusGame.sol:1553; previewSellFarFutureTickets :2113; playerActivityScore :2676"
    - "No 'by construction' / 'single fn reaches all paths' claim survives un-checked (feedback_verify_call_graph_against_source floor attested)"
  artifacts:
    - path: ".planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-GREP-ATTESTATION.md"
      provides: "The call-graph attestation + per-file Drift-Correction Table re-pinning every v55 milestone-scope anchor vs the v54 HEAD 20ca1f79, with the box-seed drift resolved"
      contains: "20ca1f79"
  key_links:
    - from: "348-GREP-ATTESTATION.md"
      to: "the v54 HEAD 20ca1f79 (contracts/ byte-identical at working HEAD)"
      via: "git diff --numstat empty + per-anchor file:line confirmation with matched text"
      pattern: "20ca1f79"
    - from: "348-GREP-ATTESTATION.md"
      to: "contracts/modules/DegenerusGameLootboxModule.sol:534 (openLootBox box-seed — the abi.encode vs abi.encodePacked drift)"
      via: "grep re-pin of the doc-cited seed site to the actual keccak256(abi.encode(...)) construction"
      pattern: "abi.encode"
---

<objective>
Author `348-GREP-ATTESTATION.md` — the call-graph attestation + Drift-Correction Table that re-pins EVERY cited `file:line` across the v55.0 milestone scope to its ACTUAL current line in the live tree (byte-identical to the v54 HEAD `20ca1f79`), with the matched source text quoted. This is the load-bearing UPSTREAM PRODUCER deliverable that every other 348 doc cites — the FREEZE-PROOF, the INVARIANT-CARRY, the PLACEMENT-DECISION, the CODE-SIZE-PLAN, and the IMPL-EDIT-ORDER-MAP all build on the actual lines this plan pins.

This is the directly-analogous deliverable to `343-GREP-ATTESTATION.md` (343-01). It enforces the audit-discipline maxim — "no 'by construction' / 'single fn reaches all paths' claim survives un-checked" ([[feedback_verify_call_graph_against_source]]). CONTEXT.md flags ONE confirmed drift to resolve: the box-seed the v55 PLAN doc cites as `LootboxModule:534` did NOT grep as `keccak256(abi.encodePacked` — the attestation MUST re-pin the actual seed-construction site so the FREEZE-03 proof depends on a true anchor.

Purpose: The downstream 348 proof/decision/inventory docs + the 349 IMPL diff author against ACTUAL lines, not the drifted doc lines. Every "by construction" assertion in the v55 PLAN docs is grep-verified here BEFORE it propagates into the proof or the IMPL.
Output: One markdown artifact, `348-GREP-ATTESTATION.md`, in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-CONTEXT.md

<interfaces>
<!-- The canonical anchor list is in 348-CONTEXT.md <canonical_refs> "Contract anchors". -->
<!-- git diff --numstat 20ca1f79 HEAD -- contracts/ is EMPTY (4 docs-only commits since) — working tree IS the v55 baseline. -->
<!-- The box-seed drift (CONTEXT.md): the doc cites LootboxModule:534 as the seed BUT the grep-pattern claim was keccak256(abi.encodePacked — that pattern does NOT match :534. Resolve below. -->
<!-- Key anchors to re-pin (doc-cited lines from CONTEXT.md; verify EACH against source, do not copy): -->
- DegenerusGameLootboxModule.sol — _applyEvMultiplierWithCap :459 (doc :459-495); the frozen buy-day read lootboxDay[index][player] :514; the open-time _simulatedDayIndex() :513/:766/:799/:836/:868 (FREEZE-03: the day the afking open must NOT copy); the openLootBox box-seed :534 = keccak256(abi.encode(rngWord, player, day, amount)) [RE-PIN: NOT abi.encodePacked; abi.encodePacked is the PRESALE box at :644]; lootboxRngWordByIndex[index] read :510
- DegenerusGameStorage.sol — lootboxEvBenefitUsedByLevel :1469 (doc :1468-1469); LOOTBOX_EV_BENEFIT_CAP = 10 ether :1326
- AfKing.sol — _resolveBuy :727 (body to :795); the named-revert comment :781-782 (the 1-wei sentinel / claimable-shortfall avoidance); the LOOTBOX_MIN transient skip :772; LOOTBOX_MIN decl :269; cost = mp*effectiveQty :761; effectiveQty :756/759; isOperatorApproved interface :43; validThroughLevel :103; fundingSource :106; the subscribe-time OPEN-E gate (doc cites :400-409 — re-pin: subscribe def ~:330, the non-zero-non-self fundingSource gate ~:343-349); the src/funder resolution (doc cites :682 — re-pin)
- DegenerusGameAdvanceModule.sol — _enforceDailyMintGate :973 (called :191); requestLootboxRng :1016; the RNG-request index advance _lrRead(LR_INDEX...)+1 :1089 + :1629 (doc :1086-1090 / :1626-1630); rngGate :1152; the STAGE insertion point (~:273, immediately before the rngGate(...) call)
- ARCH-04 reclaim targets (sizes re-measured at 348-02, this plan only PINS the sites): claimAffiliateDgnrs DegenerusGame.sol:1553; previewSellFarFutureTickets DegenerusGame.sol:2113; playerActivityScore DegenerusGame.sol:2676
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the empty-diff baseline identity + author the per-file Drift-Correction Table vs 20ca1f79</name>
  <files>.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-GREP-ATTESTATION.md</files>
  <read_first>
    - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-CONTEXT.md (<canonical_refs> "Contract anchors" — the exact anchor list with doc-cited lines + the box-seed :534 drift flag; <code_context> "Verified: git diff --numstat 20ca1f79 HEAD -- contracts/ → EMPTY")
    - .planning/PLAN-V55-AFKING-IN-GAME-REDESIGN.md (the §0 Correction-1 box-seed citation :534/:530-551; the §10 anchors; the §7 code-size reclaim targets) — to know WHICH doc lines are being attested
    - .planning/PLAN-V55-REVERT-FREE-CHAIN-PROOF.md (§3 slice-builder discharge table with AfKing.sol:766-767/781-782; §4 EV-cap + open-determinism anchors LB:459-495/534/514; the LB:549-551 cap-consume) — the second source of cited anchors
    - contracts/modules/DegenerusGameLootboxModule.sol (re-confirm the box-seed at :534 is keccak256(abi.encode(...)) NOT abi.encodePacked; _applyEvMultiplierWithCap :459; lootboxDay[index][player] :514; every _simulatedDayIndex() site; lootboxRngWordByIndex :510 — do NOT trust doc-cited lines; grep/read each)
    - contracts/storage/DegenerusGameStorage.sol (lootboxEvBenefitUsedByLevel :1469; LOOTBOX_EV_BENEFIT_CAP :1326)
    - contracts/AfKing.sol (_resolveBuy :727 + body to :795; the :781-782 named-revert comment; LOOTBOX_MIN skip :772 + decl :269; cost = mp*effectiveQty :761; effectiveQty :756/759; the subscribe def + the OPEN-E gate the doc cites :400-409; validThroughLevel :103; fundingSource :106; the src/funder resolution the doc cites :682; isOperatorApproved :43)
    - contracts/modules/DegenerusGameAdvanceModule.sol (_enforceDailyMintGate :973 + called :191; requestLootboxRng :1016; the index advance _lrRead(LR_INDEX...)+1 at :1089 and :1629; rngGate :1152; the STAGE insertion point ~:273 before the rngGate call)
    - contracts/DegenerusGame.sol (the three ARCH-04 reclaim-target function defs: claimAffiliateDgnrs :1553; previewSellFarFutureTickets :2113; playerActivityScore :2676)
  </read_first>
  <action>
    First run `git diff --numstat 20ca1f79 HEAD -- contracts/` and confirm it returns empty — record at the top of the doc that the working tree's `contracts/` is byte-identical to the v54 HEAD `20ca1f79` (4 docs-only commits since), so grep against the working tree IS a valid attestation against `20ca1f79` (no checkout needed). Then author `348-GREP-ATTESTATION.md` as the call-graph attestation deliverable, structured as per-file Drift-Correction Tables with columns: Symbol | Doc-cited line | Actual line | Matched source text (quoted) | Status (MATCH / DRIFT(±N) / PATTERN-DRIFT / FOUND). Cover every file in the interfaces block: DegenerusGameLootboxModule.sol, DegenerusGameStorage.sol, AfKing.sol, DegenerusGameAdvanceModule.sol, DegenerusGame.sol. For EACH anchor, grep/read the live tree and quote the matched line; do not transcribe doc-cited lines blindly. Devote a prominently-labeled subsection "Drift corrections (beyond simple line numbers)" to the box-seed resolution: the v55 PLAN §0-Correction-1 cites the box-seed at `LootboxModule:534`, and CONTEXT.md flags the grep-pattern claim `keccak256(abi.encodePacked` did NOT match — RESOLVE this by recording that the `openLootBox` seed at `:534` is `keccak256(abi.encode(rngWord, player, day, amount))` (the `abi.encode` form, NOT `abi.encodePacked`), and that `abi.encodePacked` appears only in the PRESALE box seed at `:644` — so FREEZE-03's "the seed = keccak256(rngWord, player, day, amount)" claim is TRUE at `:534` but the cited grep-pattern was wrong; the FREEZE-03 proof must cite `abi.encode`. Confirm there is NO `block.timestamp/number/prevrandao/coinbase/blockhash` in the draw at `:534` (grep the seed neighborhood). Re-pin the OPEN-E subscribe-time gate and the `src`/`funder` resolution to their actual lines (the doc-cited `:400-409` / `:682` may drift). Confirm the three ARCH-04 reclaim-target function defs at their actual lines (this doc PINS the sites; 348-02 MEASURES their bytes). End with a one-line attestation that NO "by construction" / "single fn reaches all paths" claim survives un-checked (the `feedback_verify_call_graph_against_source` floor) and a "Valid until" note (re-run if the subject HEAD moves before 349). Do NOT paste contract function bodies wholesale — quote only the single matched line per anchor.
  </action>
  <acceptance_criteria>
    - 348-GREP-ATTESTATION.md exists and records that `git diff --numstat 20ca1f79 HEAD -- contracts/` is empty (working tree = v54 baseline); contains the string "20ca1f79"
    - The doc contains a "Drift corrections" subsection that RESOLVES the box-seed: it records that the openLootBox seed at LootboxModule:534 is `keccak256(abi.encode(rngWord, player, day, amount))` (the abi.encode form, NOT abi.encodePacked), and that abi.encodePacked is the PRESALE box at :644 — the doc contains the literal string "abi.encode" and references both :534 and the PRESALE/:644 distinction
    - FREEZE-03 anchors present: lootboxDay[index][player] re-pinned to :514; at least the open-time _simulatedDayIndex() sites enumerated; the doc states no block.timestamp/number/prevrandao/coinbase/blockhash in the :534 draw
    - FREEZE-02 anchors present: requestLootboxRng :1016, the index advance at :1089 AND :1629, rngGate :1152
    - EV-cap anchors present: _applyEvMultiplierWithCap :459, lootboxEvBenefitUsedByLevel :1469, LOOTBOX_EV_BENEFIT_CAP :1326
    - slice-builder anchors present: _resolveBuy :727, the :781-782 named-revert comment, the LOOTBOX_MIN skip :772
    - ARCH-04 reclaim sites present: claimAffiliateDgnrs :1553, previewSellFarFutureTickets :2113, playerActivityScore :2676
    - The doc ends with the no-"by-construction"-survives-unchecked attestation
    - No contracts/*.sol modified: `git diff --name-only -- contracts/` is empty
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-GREP-ATTESTATION.md; test -f "$F" && grep -q "20ca1f79" "$F" && grep -q "abi.encode" "$F" && grep -qE ":534|534" "$F" && grep -qE ":644|644|PRESALE" "$F" && grep -qE "_applyEvMultiplierWithCap|:459" "$F" && grep -qE "_resolveBuy|:727" "$F" && grep -qE "requestLootboxRng|:1016" "$F" && grep -qE "claimAffiliateDgnrs|:1553" "$F" && [ -z "$(git diff --name-only -- contracts/)" ]</automated>
  </verify>
  <done>348-GREP-ATTESTATION.md records the empty-diff baseline identity vs 20ca1f79, re-pins every v55 milestone-scope anchor to its actual line with matched text, RESOLVES the box-seed drift (abi.encode at :534, abi.encodePacked the PRESALE box at :644), confirms the FREEZE-02/03 + EV-cap + slice-builder + ARCH-04 anchors, and ends with the no-"by-construction" attestation. Zero contracts/ edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Paper-only SPEC phase; no code ships and no runtime surface is added. The threat surface is the correctness + completeness of the anchor attestation that the rest of 348 (the FREEZE proof, the invariant carry, the placement decision, the code-size plan, the gas inventory, the edit-order map) and the 349 IMPL diff cite. The documentation-integrity risk this plan closes is "a stale/drifted/wrong-pattern anchor lets a downstream doc assert a freeze property against the wrong line, or the 349 edit lands on the wrong surface."

| Boundary | Description |
|----------|-------------|
| Doc-cited lines → live source | The v55 PLAN doc + the proof doc cite `file:line` anchors that may drift; the box-seed `:534` grep-pattern is already a confirmed wrong-pattern instance — an un-re-pinned anchor sends the FREEZE-03 proof (or the 349 edit) to the wrong place or a wrong claim. |
| SPEC attestation → downstream 348 docs → IMPL 349 | An un-attested "by construction" claim survives into the FREEZE proof / the edit-order map / IMPL and a wrong-surface assertion or diff ships. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-348-01 | Tampering | grep-attestation Drift-Correction Table | mitigate | Every cited `file:line` re-confirmed by grep/read against the live `contracts/` tree at the empty-diff baseline (`20ca1f79`); drift corrected; the box-seed `abi.encode`-vs-`abi.encodePacked` pattern-drift RESOLVED explicitly; no "by construction" survives un-checked (`feedback_verify_call_graph_against_source`). |
| T-348-02 | Information disclosure | the FREEZE-03 seed claim citing a wrong pattern | mitigate | The seed at `:534` is pinned as `keccak256(abi.encode(...))` and confirmed to carry no `block.*` entropy — so the FREEZE-PROOF (348-03) inherits a true anchor for the determinism proof, not the wrong `abi.encodePacked` pattern. |
| T-348-SC | Tampering | npm/pip/cargo installs | accept | No package installs — paper-only Markdown authoring; the only CLI is `git diff` + `grep`/read (read-only). No Package Legitimacy Audit needed; zero installs. |
</threat_model>

<verification>
- 348-GREP-ATTESTATION.md exists in the phase directory.
- Empty-diff baseline identity vs 20ca1f79 recorded; the box-seed drift RESOLVED (abi.encode at :534, abi.encodePacked the PRESALE box at :644).
- The FREEZE-02/03 + EV-cap + slice-builder + OPEN-E + ARCH-04 anchors all re-pinned with matched source text.
- No contracts/*.sol modified: `git diff --name-only -- contracts/` is empty.
</verification>

<success_criteria>
- 348-GREP-ATTESTATION.md satisfies all Task 1 acceptance criteria (ROADMAP Phase 348 Success Criterion 5, attestation slice).
- Every milestone-scope anchor re-pinned to its actual line with matched source text; the box-seed pattern-drift resolved.
- Zero contracts/*.sol edits.
</success_criteria>

<output>
Create `.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-01-SUMMARY.md` when done
</output>
