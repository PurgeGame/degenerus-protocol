---
phase: 348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p
plan: 06
type: execute
wave: 4
depends_on: ["348-01", "348-02", "348-03", "348-04", "348-05"]
files_modified:
  - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-SPEC-INDEX.md
autonomous: true
requirements: [FREEZE-01, FREEZE-02, FREEZE-03, PLACE-01, ARCH-04]

must_haves:
  truths:
    - "The D-08 multi-doc SPEC set is indexed: a doc table mapping each sibling doc (348-GREP-ATTESTATION, 348-CODE-SIZE-PLAN, 348-GAS-INVENTORY, 348-FREEZE-PROOF, 348-INVARIANT-CARRY, 348-PLACEMENT-DECISION, 348-IMPL-EDIT-ORDER-MAP) to its one-line purpose + requirement(s) + ROADMAP success criterion"
    - "All 5 phase requirements (FREEZE-01, FREEZE-02, FREEZE-03, PLACE-01, ARCH-04) are mapped to the doc(s) that satisfy them, each marked COVERED"
    - "All 5 ROADMAP Phase-348 success criteria are mapped to the doc(s) that satisfy them, each marked COVERED"
    - "The SPEC verdict is recorded: PASS/CONDITIONAL — the FREEZE spine PROVEN; the placement DECIDED (required-path, USER override); the discharged invariants CARRIED with the try/catch correction; the code-size reclaim sequenced < 24,576; the gas inventory produced; the OPEN-E/set-mutation carry-over confirmed; every anchor grep-attested vs 20ca1f79; ZERO contracts/*.sol mutation"
    - "The OPEN-E / AFSUB / set-mutation carry-over is CONFIRMED at the index level (the subscribe-time isOperatorApproved gate, the pass-gating validThroughLevel, the VAULT/SDGNRS exemption-on-player, the funder=src accounting, the 'no cursor advance after swap-pop' set-mutation invariant — all carry over verbatim; the OPEN-E 4-protection structure re-attested) — these are CONSENT inputs, 349-owned, confirmed here"
    - "The 349 IMPL hand-off is stated: the single authoring source (348-IMPL-EDIT-ORDER-MAP.md), the carried corrections 349 MUST honor, and the re-pin-before-authoring requirement"
    - "The paper-only assertion is recorded: git diff --name-only -- contracts/ is EMPTY across the whole phase; the first contracts/ mutation is the 349 IMPL diff"
  artifacts:
    - path: ".planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-SPEC-INDEX.md"
      provides: "The navigation + closure index for the D-08 348 SPEC set — requirement/success-criterion traceability + the SPEC verdict + the OPEN-E/set-mutation carry-over confirmation + the 349 IMPL hand-off"
      contains: "20ca1f79"
  key_links:
    - from: "348-SPEC-INDEX.md"
      to: "the six sibling 348 docs + the 5 phase requirements + the 5 ROADMAP success criteria"
      via: "a doc table + a requirement→doc table + a success-criterion→doc table, each row COVERED"
      pattern: "COVERED"
    - from: "348-SPEC-INDEX.md"
      to: "the 349 IMPL diff (the single authoring source 348-IMPL-EDIT-ORDER-MAP.md)"
      via: "the hand-off section + the carried corrections + the re-pin-before-authoring requirement"
      pattern: "IMPL-EDIT-ORDER-MAP|hand-off"
---

<objective>
Author `348-SPEC-INDEX.md` — the navigation + closure index for the Phase-348 D-08 multi-doc SPEC set (the analog of `343-SPEC-INDEX.md`, 343-05). It ties the six sibling docs together, maps every phase requirement + every ROADMAP success criterion to the doc(s) that satisfy it, CONFIRMS the OPEN-E/AFSUB/set-mutation carry-over at the index level (the CONSENT inputs feeding 349), records the SPEC verdict, and states the single 349 IMPL hand-off.

This plan carries all 5 phase REQ-IDs (FREEZE-01/02/03, PLACE-01, ARCH-04) because the index RE-ATTESTS the full phase requirement set + success-criterion coverage (the 343-05 precedent: the index plan's `requirements` field lists all phase reqs). It lands LAST (Wave 4), consuming all five upstream plans.

Purpose: 349 IMPL has a single navigation entry point + a recorded SPEC verdict + an explicit hand-off, and the OPEN-E/set-mutation carry-over (ROADMAP SC5's CONSENT slice) is confirmed before IMPL builds it.
Output: One markdown artifact in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-GREP-ATTESTATION.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-CODE-SIZE-PLAN.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-GAS-INVENTORY.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-FREEZE-PROOF.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-INVARIANT-CARRY.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-PLACEMENT-DECISION.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-IMPL-EDIT-ORDER-MAP.md

<interfaces>
<!-- CONSUMES all six sibling docs. The 343-SPEC-INDEX.md is the format precedent (phase summary + doc-set table + requirement→doc + success-criterion→doc + SPEC verdict + IMPL hand-off + paper-only assertion). -->
<!-- The 5 phase requirements (REQUIREMENTS.md): FREEZE-01/02/03 (→ 348-FREEZE-PROOF), PLACE-01 (→ 348-PLACEMENT-DECISION), ARCH-04 (→ 348-CODE-SIZE-PLAN + 348-IMPL-EDIT-ORDER-MAP). -->
<!-- The 5 ROADMAP Phase-348 success criteria: SC1 FREEZE spine; SC2 carried invariants; SC3 placement decided; SC4 code-size reclaim; SC5 OPEN-E/set-mutation carry-over + grep-attestation. -->
<!-- OPEN-E/set-mutation carry-over (ROADMAP SC5, CONSENT inputs 349-owned): the subscribe-time isOperatorApproved gate; pass-gating validThroughLevel; VAULT/SDGNRS exemption-on-player; funder=src accounting; the "no cursor advance after swap-pop" set-mutation invariant; the OPEN-E 4-protection structure re-attested. Confirm carry-over verbatim here. -->
<!-- The OPEN-E 4-protection structure: consent-gate-at-subscribe / default-self / no-escalation / trust-the-sub temporal bound ([[open-e-operator-approval-trust-boundary]]). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 348-SPEC-INDEX.md — the D-08 index + traceability + OPEN-E/set-mutation carry-over confirmation + SPEC verdict + 349 hand-off</name>
  <files>.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-SPEC-INDEX.md</files>
  <read_first>
    - all six sibling 348 docs (348-GREP-ATTESTATION.md, 348-CODE-SIZE-PLAN.md, 348-GAS-INVENTORY.md, 348-FREEZE-PROOF.md, 348-INVARIANT-CARRY.md, 348-PLACEMENT-DECISION.md, 348-IMPL-EDIT-ORDER-MAP.md — read each to write its one-line purpose + confirm its coverage)
    - .planning/phases/343-spec-design-lock-solvency-proof-dead-code-gas-inventories-ca/343-SPEC-INDEX.md (the format precedent: §1 phase summary, §2 doc-set table, §3 requirement→doc, §4 success-criterion→doc, §5 SPEC verdict, §6 IMPL hand-off, §7 paper-only assertion — and the discipline of OVERTURNING un-verified upstream claims at the index level if the attestation found drift)
    - .planning/REQUIREMENTS.md (the 5 phase requirements FREEZE-01/02/03/PLACE-01/ARCH-04 + the CONSENT-01/02 definitions for the carry-over confirmation)
    - .planning/ROADMAP.md (the Phase-348 5 success criteria — to map each to its covering doc)
    - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-CONTEXT.md (the §3 freeze/security spine + the OPEN-E/AFSUB/set-mutation carry-over list in the "Carried-forward LOCKED design" block; the Claude's-Discretion D-08 multi-doc shape)
    - contracts/AfKing.sol (re-confirm the OPEN-E/set-mutation carry-over surface at the re-pinned lines from 348-GREP-ATTESTATION: the subscribe-time isOperatorApproved gate, validThroughLevel :103, fundingSource :106, the src/funder resolution, the swap-pop set-mutation — to state the carry-over is verbatim against source)
  </read_first>
  <action>
    Author `348-SPEC-INDEX.md` mirroring the 343-SPEC-INDEX.md structure. (1) A "Phase summary" — paper-only SPEC design-lock for the v55 AfKing-in-Game redesign; record the baseline identity (`contracts/` byte-identical to `20ca1f79`, `git diff --numstat 20ca1f79 HEAD -- contracts/` EMPTY); state what the phase PROVED/DECIDED/PRODUCED. (2) A "Document set (the D-08 multi-doc SPEC)" table — one row per sibling doc (Doc | Plan | One-line purpose | Requirement(s) | ROADMAP SC), covering 348-GREP-ATTESTATION, 348-CODE-SIZE-PLAN, 348-GAS-INVENTORY, 348-FREEZE-PROOF, 348-INVARIANT-CARRY, 348-PLACEMENT-DECISION, 348-IMPL-EDIT-ORDER-MAP, + this index. (3) A "Requirement → doc traceability" table mapping each of FREEZE-01/02/03, PLACE-01, ARCH-04 to its covering doc(s), each row marked COVERED. (4) A "ROADMAP success-criterion → doc traceability" table mapping each of the 5 Phase-348 success criteria to its covering doc(s), each row COVERED. (5) A "SPEC verdict" section — PASS (or CONDITIONAL if the 348-03 human-verify checkpoint or the light /contract-auditor pass flagged anything): the FREEZE spine PROVEN (FREEZE-02 no-interleave guard specified + FREEZE-03 stamped-day determinism); the placement DECIDED (required-path, USER override, PLAN-V55 §4/§9 superseded); the discharged REVERT-FREE-CHAIN + EV-cap invariants CARRIED with the D-348-04 try/catch correction (no valve) + the 3 §7 follow-ups discharged; the code-size reclaim MEASURED + sequenced < 24,576; the gas inventory produced (advisory, 350 gates); the live-read window filed as an accepted-by-design known issue; ZERO `contracts/*.sol` mutation. If the attestation (348-01) overturned any upstream doc claim (e.g., the box-seed pattern), record that at the index level (the 343-05 precedent of surfacing the corrections). (6) A "CONSENT carry-over confirmation (OPEN-E / AFSUB / set-mutation)" section — CONFIRM, against the re-pinned source, that the subscribe-time `isOperatorApproved` (OPEN-E) gate, the pass-gating (`validThroughLevel`), the VAULT/SDGNRS exemption-on-`player`, the funder=src accounting, and the "no cursor advance after swap-pop" set-mutation invariant (the H-CANCEL-SWAP-MISS / cancel-tombstone-streak class) all carry over VERBATIM, and re-attest the OPEN-E 4-protection structure (consent-gate-at-subscribe / default-self / no-escalation / trust-the-sub temporal bound) — note these are CONSENT-01/02 inputs, 349-owned (BUILT at IMPL), confirmed here. (7) A "349 IMPL hand-off" section — the single authoring source is `348-IMPL-EDIT-ORDER-MAP.md`; list the carried corrections 349 MUST honor (the box-seed abi.encode re-pin; the no-try/catch REVERT-02 correction; the required-path override; the EV-cap-at-open buy-time-write-bypassed); and the re-pin-before-authoring requirement (re-run 348-GREP-ATTESTATION if the subject HEAD moves before 349). (8) A "Paper-only assertion + validity" section — `git diff --name-only -- contracts/` is EMPTY across the whole phase; valid until the 349 IMPL diff. Do NOT paste contract bodies.
  </action>
  <acceptance_criteria>
    - 348-SPEC-INDEX.md exists and records the baseline identity vs 20ca1f79 (the string "20ca1f79" appears)
    - The D-08 doc-set table covers all seven sibling docs (348-GREP-ATTESTATION, 348-CODE-SIZE-PLAN, 348-GAS-INVENTORY, 348-FREEZE-PROOF, 348-INVARIANT-CARRY, 348-PLACEMENT-DECISION, 348-IMPL-EDIT-ORDER-MAP)
    - All 5 phase requirements (FREEZE-01, FREEZE-02, FREEZE-03, PLACE-01, ARCH-04) appear in a requirement→doc table, each marked COVERED
    - All 5 ROADMAP success criteria appear in a success-criterion→doc table, each marked COVERED
    - A SPEC verdict is recorded (PASS or CONDITIONAL) covering the FREEZE spine, the placement decision, the carried invariants with the try/catch correction, the code-size reclaim < 24,576, and ZERO contracts mutation
    - The CONSENT carry-over section confirms the OPEN-E gate + validThroughLevel + VAULT/SDGNRS exemption + funder=src + "no cursor advance after swap-pop" carry over verbatim, and re-attests the OPEN-E 4-protection (the strings "isOperatorApproved" and "swap-pop" appear)
    - The 349 IMPL hand-off names 348-IMPL-EDIT-ORDER-MAP.md as the authoring source + the carried corrections + the re-pin requirement
    - The paper-only assertion is recorded (git diff --name-only -- contracts/ EMPTY)
    - No contracts/*.sol modified: `git diff --name-only -- contracts/` is empty
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-SPEC-INDEX.md; test -f "$F" && grep -q "20ca1f79" "$F" && grep -q "FREEZE-01" "$F" && grep -q "PLACE-01" "$F" && grep -q "ARCH-04" "$F" && grep -q "COVERED" "$F" && grep -qiE "PASS|CONDITIONAL" "$F" && grep -q "isOperatorApproved" "$F" && grep -qi "swap-pop" "$F" && grep -q "IMPL-EDIT-ORDER-MAP" "$F" && [ -z "$(git diff --name-only -- contracts/)" ]</automated>
  </verify>
  <done>348-SPEC-INDEX.md indexes the D-08 set, maps all 5 requirements + all 5 success criteria to their docs (COVERED), records the SPEC verdict, confirms the OPEN-E/AFSUB/set-mutation carry-over verbatim (the OPEN-E 4-protection re-attested), states the 349 IMPL hand-off (authoring source + carried corrections + re-pin), and asserts paper-only (zero contracts/ edits). Zero contracts/ edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Paper-only SPEC phase; no code ships. The threat surface is the closure integrity of the index — an unmapped requirement (a phase REQ-ID with no covering doc), an unconfirmed carry-over (the OPEN-E/set-mutation surface assumed rather than confirmed against source), or a hand-off that points 349 at the wrong authoring source or drops a carried correction. The documentation-integrity threats: a phase that closes with a coverage gap, or a CONSENT carry-over asserted-but-not-confirmed surviving into the 349 build.

| Boundary | Description |
|----------|-------------|
| phase requirements → covering docs | If a phase REQ-ID (FREEZE-01/02/03, PLACE-01, ARCH-04) has no covering doc, the phase closes with a coverage gap; the requirement→doc table is the boundary. |
| OPEN-E/set-mutation carry-over → 349 build | The CONSENT-01/02 carry-over (OPEN-E gate, validThroughLevel, VAULT/SDGNRS exemption, funder=src, swap-pop invariant) must be CONFIRMED against the re-pinned source here, not assumed, before 349 builds it. |
| SPEC verdict → 349 hand-off | The verdict + hand-off must reflect any 348-03 checkpoint / /contract-auditor flags and point 349 at 348-IMPL-EDIT-ORDER-MAP.md with the carried corrections intact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-348-13 | Tampering | the requirement/success-criterion coverage tables | mitigate | Every phase REQ-ID (FREEZE-01/02/03, PLACE-01, ARCH-04) + every ROADMAP SC mapped to a covering doc, each row marked COVERED; a coverage gap is surfaced, not silently closed. |
| T-348-14 | Spoofing | an unconfirmed OPEN-E/set-mutation carry-over | mitigate | The CONSENT carry-over section confirms the OPEN-E gate / validThroughLevel / VAULT-SDGNRS exemption / funder=src / "no cursor advance after swap-pop" against the re-pinned source, and re-attests the OPEN-E 4-protection structure — the 349 build inherits a confirmed carry-over, not an assumption. |
| T-348-15 | Tampering | the 349 hand-off correctness | mitigate | The hand-off names 348-IMPL-EDIT-ORDER-MAP.md as the single authoring source, lists the carried corrections 349 MUST honor, and requires re-pinning if the subject HEAD moves — reflecting any 348-03 /contract-auditor flag in the verdict. |
| T-348-SC | Tampering | npm/pip/cargo installs | accept | No package installs — paper-only Markdown authoring; the only CLI is `git diff` + `grep`/read (read-only). No Package Legitimacy Audit needed; zero installs. |
</threat_model>

<verification>
- 348-SPEC-INDEX.md exists in the phase directory.
- All 5 phase requirements + all 5 ROADMAP success criteria mapped to covering docs (COVERED); the SPEC verdict recorded.
- The OPEN-E/AFSUB/set-mutation carry-over confirmed verbatim against source; the 349 IMPL hand-off stated.
- No contracts/*.sol modified: `git diff --name-only -- contracts/` is empty.
</verification>

<success_criteria>
- 348-SPEC-INDEX.md satisfies all Task 1 acceptance criteria (it ties the D-08 set together + re-attests coverage + confirms the CONSENT carry-over, ROADMAP Phase 348 Success Criterion 5's carry-over slice + the cross-cutting closure).
- The SPEC verdict + the 349 IMPL hand-off are recorded.
- Zero contracts/*.sol edits.
</success_criteria>

<output>
Create `.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-06-SUMMARY.md` when done
</output>
