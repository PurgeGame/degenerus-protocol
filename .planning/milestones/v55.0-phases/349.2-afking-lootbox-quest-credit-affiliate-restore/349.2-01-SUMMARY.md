---
phase: 349.2-afking-lootbox-quest-credit-affiliate-restore
plan: 01
subsystem: smart-contracts
tags: [afking, lootbox, quests, affiliate, burnie, coinflip, solidity, gas]

# Dependency graph
requires:
  - phase: 349.1
    provides: the committed AfKing-in-Game fold + v55 box redesign (the stamp-only lootbox branch this fix amends; baseline 77c3d9ef)
provides:
  - "Per-buy quest-credit (handlePurchase + recordMintQuestStreak) restored for afking LOOTBOX-mode subs"
  - "Both affiliate.payAffiliate branches (fresh + claimable) restored for afking LOOTBOX-mode subs as BURNIE flip-credit"
  - "scorePlus1 stamp now sourced from the post-buy streak (handlers-before-score), matching a manual lootbox buy"
  - "Vestigial autoBuy(uint256) router stub removed from all three sites (module fn, Game forwarder, interface decl)"
  - "_ethToBurnie private valuation helper in GameAfkingModule (copy of MintModule._ethToBurnieValue)"
affects: [350-gas, 351-tst, 352-terminal, v56-batch-afking-affiliate-quest]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Mirror purchaseWith's lootbox-leg BURNIE side-effects in the afking stamp branch MINUS the cold ledger (GAS-01) and MINUS the buy-time EV-cap (EVCAP-01)"
    - "Affiliate/quest rewards are BURNIE flip-credit (valuation basis only), never an ETH cut — ETH/claimablePool path byte-unchanged (no solvency surface)"

key-files:
  created: []
  modified:
    - contracts/modules/GameAfkingModule.sol
    - contracts/DegenerusGame.sol
    - contracts/interfaces/IDegenerusGameModules.sol

key-decisions:
  - "Contract-commit gate (autonomous:false): diff applied + USER hand-reviewed + approved 2026-05-31, then committed `453f8073` on main (NOT pushed) via the documented pre-commit-guard bypass (move hook aside → commit → restore)"
  - "Affiliate score arg passed as uint16(activityScore) (plain truncating cast) to mirror purchaseWith:1275 exactly (parity over clamping)"
  - "Removed now-unused _questStreakOf (verified no other caller across contracts/) rather than leaving dead code"
  - "_ethToBurnie added as a private pure helper (vs pure inline) for readability; PRICE_COIN_UNIT already in-context (:913)"
  - "amount - ethValue == claimableUse (the _resolveBuy split) — never underflows (ethValue = cost - claimableUse, claimableUse in [0,cost], amount == cost for lootbox)"

patterns-established:
  - "Ticket-branch-as-existence-proof: the afking TICKET branch already delegatecalls purchaseWith (handlePurchase + recordMintQuestStreak + payAffiliate + creditFlip) from inside the process STAGE, so the lootbox-branch restoration adds NO new reentrancy/guard surface"

requirements-completed: []

# Metrics
duration: ~25min
completed: 2026-05-31
---

# Phase 349.2: AfKing LOOTBOX Quest-Credit + Affiliate Restore Summary

**Restored per-buy quest-credit + both affiliate.payAffiliate branches (BURNIE flip-credit) to the afking LOOTBOX stamp branch — closing the v55 box-redesign regression / TICKET-vs-LOOTBOX asymmetry — and dropped the vestigial autoBuy router stub. ETH/claimablePool byte-unchanged; DegenerusGame 22,855 B.**

## Performance

- **Duration:** ~25 min (inline sequential, single-plan contract phase)
- **Completed:** 2026-05-31
- **Tasks:** 3 auto (Task 1, 1b, 2) + 1 blocking human-verify gate (Task 3) satisfied
- **Files modified:** 3 (contracts only)

## Accomplishments
- **Quest parity restored** — afking LOOTBOX buys now call `quests.handlePurchase(player, amount, 0, amount, mp, priceForLevel(level+1))` BEFORE the score, with `IDegenerusGame(address(this)).recordMintQuestStreak(player)` on completion (`questType == 1`), exactly mirroring a manual lootbox buy.
- **Affiliate parity restored** — both `payAffiliate` branches: fresh-ETH portion (`ethValue`, `isFreshEth=true`, `uint16(activityScore)`) + claimable-used portion (`amount - ethValue`, `isFreshEth=false`, `0`), code `bytes32("DGNRS")`, valued in BURNIE via the new `_ethToBurnie`.
- **Score source switched** — `scorePlus1` now stamps from the post-buy streak returned by `handlePurchase` (was the pre-action `_questStreakOf` read); still frozen at stamp (FREEZE-03).
- **Single BURNIE flip-credit** — `coinflip.creditFlip(player, flipCredit)` once, accumulating questReward + both affiliate kickbacks (mirror MintModule:1366-1367).
- **autoBuy stub dropped** — removed from `GameAfkingModule` (fn + ROUTER comment), `DegenerusGame` (forwarder + stub-list comment), and `IDegenerusGameModules` (decl); `advanceGame`/`autoOpen`/`mintBurnie` + `IGameRouter` untouched.

## Task Commits

This `autonomous: false` contract phase was committed as ONE batched diff after the USER hand-review gate:

1. **Contracts (Task 1 + 1b)** — `453f8073` `fix(349.2): restore quest-credit + affiliate (BURNIE) for afking LOOTBOX subs; drop autoBuy stub` (3 files, +105/−45). On `main`, **NOT pushed**.

Committed via the repo's documented pre-commit-guard bypass (move `.git/hooks/pre-commit` aside → commit → restore) after the USER reviewed the diff and selected "Commit now" (the v55 contract-commit-gate pattern; cf. 349.1's batched diff at 77c3d9ef). Task 2 (contracts-only build) and Task 3 (human-verify gate) were verification/gate steps — no separate commit. Bookkeeping (this SUMMARY + STATE + ROADMAP) follows in a separate `docs(349.2)` commit.

## Files Created/Modified
- `contracts/modules/GameAfkingModule.sol` — lootbox-branch BURNIE side-effect restoration (handlePurchase→score→both payAffiliate→single creditFlip); `_questStreakOf` removed; `_ethToBurnie` added; `autoBuy` fn + ROUTER comment dropped.
- `contracts/DegenerusGame.sol` — `autoBuy` forwarder stub + stub-list comment dropped (ASCII-box alignment preserved).
- `contracts/interfaces/IDegenerusGameModules.sol` — `autoBuy` decl dropped from `IGameAfkingModule`.

## Decisions Made
- **Parity over clamping:** affiliate score arg is `uint16(activityScore)` (plain truncating cast), matching `purchaseWith:1275` — faithful replication is the parity requirement; adding a clamp purchaseWith lacks would BREAK parity.
- **Removed `_questStreakOf`** (dead after the streak-source switch; grep-confirmed no other caller) rather than leaving unused internal code.
- **No EV-cap, no cold ledger, no presale/spent-all-claimable extras** replicated — out of scope for the afking stamp branch (EV-cap stays AT OPEN per EVCAP-01; the afking claimable settle is handled at OPEN).

## Deviations from Plan
None — plan executed exactly as written. (The plan left `_questStreakOf` removal to executor discretion "if unused"; confirmed unused and removed. The plan offered inline-or-helper for `_ethToBurnie`; chose the private helper.)

## Issues Encountered
- `forge fmt --check` flags the *entire* file (including untouched code) — this repo's committed Solidity is hand-formatted and deliberately does NOT conform to `forge fmt` defaults, so fmt is not a gate. Did NOT run `forge fmt` (it would reformat the whole file into a massive unrelated diff). Added code matches the file's existing hand-maintained multi-line style. The real gate, `forge build`, is clean.

## Verification
- **Task 1 grep gates:** 9/9 PASS (handlers-before-score; 2× payAffiliate true/false; single `creditFlip(player`; IDegenerusGame import + `recordMintQuestStreak`; `claimablePool` writes = 2 unchanged; cold-ledger/EV-cap = 0; try/catch = 0; ticket branch intact).
- **Task 1b:** `function autoBuy` defs = 0, callers = 0; `autoOpen`/`mintBurnie` kept; `IGameRouter` (advanceGame/advanceDue) intact.
- **Task 2:** `forge build --sizes --skip 'test/**' --skip '*.t.sol'` exit 0 (only pre-existing lint warnings); **DegenerusGame = 22,855 B** (1,721 under the 24,576 ceiling; 72 B below the 22,927 post-349.1 baseline — the autoBuy forwarder removal shrank the Game image while the side-effect additions live in GameAfkingModule's own budget). GameAfkingModule/MintModule/LootboxModule all compile.
- `:708-711` ETH/`claimablePool` debit byte-unchanged; no new ETH/pool write (DESIGN §3, no solvency surface). `forge test` NOT run — Phase 351's charge (stale AfKing.sol imports from 349.1 are expected reds).

## Next Phase Readiness
- **350 GAS** re-confirms GAS-01 *net of* the restored side-effects on the post-349.2 surface.
- **351 TST** proves quest/affiliate/flip-credit parity empirically (lootbox afking == manual lootbox) and clears the stale 349.1 test reds.
- **352 TERMINAL** delta-audit: ETH/claimablePool byte-unchanged, freeze-safety of the buy-day-seeded affiliate roll, asymmetry closed. Weekly affiliate aggregation remains DEFERRED (DESIGN §5 → v56 seed).
- **Status:** committed `453f8073` on `main` (NOT pushed — v55 ships at 352 TERMINAL, then push per USER).

---
*Phase: 349.2-afking-lootbox-quest-credit-affiliate-restore*
*Completed: 2026-05-31 — contract-commit gate closed (`453f8073`)*
