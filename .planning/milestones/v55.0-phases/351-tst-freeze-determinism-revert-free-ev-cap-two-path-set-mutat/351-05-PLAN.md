---
phase: 351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat
plan: 05
type: execute
wave: 2
depends_on: ["351-01"]
files_modified:
  - test/fuzz/KeeperNonBrick.t.sol
  - test/fuzz/V55RevertFreeEvCap.t.sol
autonomous: true
requirements: [TST-02, TST-03]
must_haves:
  truths:
    - "A funded process/open never reverts on well-formed slices (the preserved _resolveBuy invariants, REVERT-01)"
    - "A solvency violation FAILS LOUD — a claimablePool underflow reverts, never masked (class B, no try/catch)"
    - "Game-over routing is NEVER blocked by the afking STAGE (class C — the mult==0 gameover leg still proceeds)"
    - "The per-(player,level) 10-ETH EV-benefit budget is enforced exactly once per open with NO double-draw (the buy-time EV write is bypassed for afking boxes)"
    - "KeeperNonBrick compiles + runs; the removed batchPurchase try/catch-isolation leg is dropped (D-351-02), the reentrancy/cancel properties reframe and stay"
    - "git diff 453f8073 HEAD -- contracts/ stays EMPTY"
  artifacts:
    - path: "test/fuzz/V55RevertFreeEvCap.t.sol"
      provides: "TST-02 revert-free/no-valve + TST-03 EV-cap exactly-once proofs"
      contains: "lootboxEvBenefitUsedByLevel"
    - path: "test/fuzz/KeeperNonBrick.t.sol"
      provides: "adapted revert-free / non-brick corpus (game-resident withdraw/cancel)"
  key_links:
    - from: "test/fuzz/V55RevertFreeEvCap.t.sol"
      to: "DegenerusGameLootboxModule._applyEvMultiplierWithCap"
      via: "lootboxEvBenefitUsedByLevel[player][level+1] RMW at open, exactly once"
      pattern: "_applyEvMultiplierWithCap"
---

<objective>
Adapt `KeeperNonBrick.t.sol` (the PRIMARY revert-free / no-brick scaffolding) to the game-resident path and
author the dedicated TST-02 (revert-free + no-valve no-brick) + TST-03 (EV-cap exactly-once) proofs. D-348-04
dropped the try/catch valve — so the no-brick guarantee rests on REVERT-01's revert-free-by-construction slice
(class A), fail-loud-on-solvency (class B), and terminal-routing-unblocked (class C). The `batchPurchase`
per-slice try/catch isolation leg is a D-351-02 removed surface (drop it, log it); the reentrancy-rollback +
un-brickable-cancel properties reframe and stay.

Purpose: TST-02 — a funded process/open never reverts on well-formed slices; a solvency violation fails loud
(never masked); game-over routing is never blocked. TST-03 — the EV-cap is enforced exactly once per open with
no double-draw vs the buy-time path, equivalent to v54.
Output: 1 adapted fuzz file + 1 new TST-02/03 proof file. ZERO `contracts/*.sol` mutation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-CONTEXT.md
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-PATTERNS.md
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-01-SUMMARY.md
@.planning/PLAN-V55-REVERT-FREE-CHAIN-PROOF.md

<interfaces>
<!-- The revert-free + EV-cap anchors — VERIFIED at 453f8073 (READ-ONLY; never edit sites). -->

TST-02 — the three revert classes (PLAN-V55-REVERT-FREE-CHAIN-PROOF.md §5, obl-4 try/catch DROPPED by D-348-04):
  class A (revert-free-by-construction): the funded slice preserves _resolveBuy's invariants VERBATIM —
    ev = cost − claimableUse + enum payKind, the 1-wei claimable sentinel, the LOOTBOX_MIN transient skip, quantity ≥ 1 (REVERT-01).
  class B (fail-loud-on-solvency): a claimablePool underflow MUST REVERT, never be masked — the checked uint128 -=
    at GameAfkingModule.sol:710 (SOLVENCY-01 invariant DegenerusGameStorage.sol:358).
  class C (terminal-routing-unblocked): the afking STAGE cannot block game-over routing — the advance gameover leg (mult==0) still proceeds.

TST-03 — the EV-cap (DegenerusGameLootboxModule.sol):
  _applyEvMultiplierWithCap :459 — reads lootboxEvBenefitUsedByLevel[player][lvl] :473, writes usedBenefit+adjustedPortion :488 (hard-clamped ≤10 ETH).
  Called ONCE at open from resolveAfkingBox :877 (the afking arm) keyed [player][level+1]; the buy-time EV write is
  BYPASSED for afking boxes (no double-draw). Human buy-time write uses the SAME map/key (resolveLootboxDirect :772/:805).
  LOOTBOX_EV_BENEFIT_CAP = 10 ether (DegenerusGameStorage.sol).

The KeeperNonBrick scaffolding to PORT (the harness) + the D-351-02 drop:
  box-enqueue + RNG-inject + poison-isolation helpers (:38-69 + body); the slot constants LOOTBOX_ETH_SLOT=15 / RNG_LOCKED_SHIFT=168 /
  GAME_OVER_SHIFT=184 (:48-61) are GAME storage → reusable as-is (re-confirm via forge inspect); the 0.5 gwei / 66_528 / 71_203 reward-peg mirrors (:66-69).
  DROP (D-351-02): the `batchPurchase` per-slice try/catch isolation leg (docstring :18-28, vm.prank(AF_KING) batch) — removed surface (v55 P5 dead-code AF_KING.batchPurchase/BatchBuy). Log BY NAME + reason for 351-09.
  KEEP + reframe: the reentrancy-rollback + un-brickable-cancel properties (:29-36) → the game-resident withdraw/cancel.

Δ4 autoBuy → STAGE-drive via new-day advanceGame(); _settleGame helper from KeeperRewardRoutingSameResults:111-127.
RE-DERIVE every pinned slot via `forge inspect storage DegenerusGame`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adapt KeeperNonBrick.t.sol — port the harness, drop the batchPurchase isolation leg (D-351-02), reframe reentrancy/cancel</name>
  <read_first>
    - test/fuzz/KeeperNonBrick.t.sol (FULL — 956 lines; the docstring :18-36, the box-enqueue/RNG-inject/poison helpers :38-69, the slot constants :48-61, the reward-peg mirrors :66-69, the 35 afKing. sites)
    - contracts/modules/GameAfkingModule.sol :539 (processSubscriberStage), :709-710 (afkingFunding -= / claimablePool -=), :985 (mintBurnie), :1023 (autoOpen)
    - contracts/storage/DegenerusGameStorage.sol :343 (cursors), :358 (SOLVENCY-01)
    - .planning/phases/351-PATTERNS.md §6 + §"No Analog Found" (the D-351-02 batchPurchase-isolation drop)
  </read_first>
  <behavior>
    - The box-enqueue + RNG-inject + reward-peg helpers work against game storage (re-confirm the slot constants via forge inspect).
    - A funded process/open does NOT brick the batch (revert-free by construction — class A).
    - The reentrancy-rollback + un-brickable-cancel properties hold on the game-resident withdraw/cancel.
    - The `batchPurchase` per-slice try/catch isolation leg is REMOVED (the surface is gone — no successor).
  </behavior>
  <action>
    Apply Δ4 (`afKing.autoBuy(N)`→STAGE-drive via new-day `advanceGame()`; port `_settleGame`) + Δ5 (views→slot-read)
    to the 35 `afKing.` sites. DROP the `batchPurchase` per-slice try/catch isolation leg (the docstring :18-28
    block + its `vm.prank(AF_KING)` batch test) per D-351-02 — it tests the removed AF_KING.batchPurchase/BatchBuy
    surface with no behavioral successor; record the dropped test name(s) + reason for 351-09's ledger (note it in
    the SUMMARY). KEEP + reframe the reentrancy-rollback + un-brickable-cancel properties (:29-36) onto the
    game-resident withdraw/cancel (`game.withdrawAfkingFunding` / re-subscribe(dailyQuantity=0)). PRESERVE the
    box-enqueue/RNG-inject/poison helpers, re-confirming the slot constants (LOOTBOX_ETH_SLOT/RNG_LOCKED_SHIFT/
    GAME_OVER_SHIFT) via `forge inspect storage DegenerusGame`. Do NOT edit any contracts/ file.
  </action>
  <verify>
    <automated>forge test --match-contract "KeeperNonBrick" 2>&1 | tail -8 (compiles + runs; batchPurchase-isolation leg gone; reentrancy/cancel reframed)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' test/fuzz/KeeperNonBrick.t.sol | grep -c "afKing\."` == 0 AND `grep -c "batchPurchase" == 0` (the removed-surface leg dropped).
    - The file compiles + runs under `forge test --match-contract KeeperNonBrick`.
    - The reentrancy-rollback + un-brickable-cancel tests are present, reframed onto the game-resident withdraw/cancel.
    - The dropped batchPurchase-isolation test name(s) + reason are noted in the SUMMARY (for the 351-09 D-351-02 ledger).
    - `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>KeeperNonBrick compiles + runs on the game-resident path; the batchPurchase try/catch-isolation leg is dropped (logged); the reentrancy/cancel properties reframe and stay; the harness helpers are preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author V55RevertFreeEvCap.t.sol — TST-02 revert-free + class-B fail-loud + class-C gameover-unblocked</name>
  <read_first>
    - contracts/modules/GameAfkingModule.sol :539 (processSubscriberStage — the slice builder), :708-711 (the funding debit + claimablePool -=), :993-996 (the advance leg / gameover routing)
    - contracts/modules/DegenerusGameAdvanceModule.sol :305-312 (the STAGE block), :313-317 (partial-drain)
    - contracts/storage/DegenerusGameStorage.sol :358 (SOLVENCY-01)
    - test/fuzz/KeeperNonBrick.t.sol (Task 1 output — the harness helpers + reward-peg mirrors to reuse)
    - .planning/PLAN-V55-REVERT-FREE-CHAIN-PROOF.md §3 (the _resolveBuy slice-builder discharge) + §5 (the 3 surviving classes)
    - .planning/phases/351-PATTERNS.md §6 (the TST-02 reframe)
  </read_first>
  <behavior>
    - class A: fuzz funded well-formed slice inputs (amount / claimable-mix across the 1-wei sentinel / LOOTBOX_MIN skip / quantity≥1) — the process STAGE never reverts.
    - class B: force a SOLVENCY-01 violation (a claimablePool underflow) — the checked uint128 -= at :710 REVERTS (never masked, no try/catch swallows it).
    - class C: with the game in a gameover-routing state (mult==0), the advance gameover leg still proceeds — the afking STAGE does not gate it.
  </behavior>
  <action>
    Create `test/fuzz/V55RevertFreeEvCap.t.sol` extending DeployProtocol (reuse the KeeperNonBrick harness helpers +
    the _settleGame VRF-drain). Author the TST-02 proofs:
    (a) class A revert-free — fuzz random FUNDED well-formed slice inputs (amount / claimable-mix) exercising the
    _resolveBuy invariants (ev = cost − claimableUse, the 1-wei sentinel, the LOOTBOX_MIN transient skip,
    quantity ≥ 1, REVERT-01); drive the STAGE via a new-day advanceGame() and assert it never reverts on a funded sub;
    (b) class B fail-loud — construct a forced claimablePool underflow (e.g. via vm.store to drive claimablePool
    below the debit, re-derived slot) and assert the checked `uint128 -=` at :710 REVERTS (the solvency violation is
    NOT masked — D-348-04 dropped the try/catch);
    (c) class C gameover-unblocked — put the game into the mult==0 gameover-routing state and assert the gameover
    advance leg proceeds (the afking STAGE does not block terminal routing).
    Gate non-vacuously: class A asserts the sub was actually funded + processed; class B asserts the revert is the
    solvency one (expectRevert on the underflow), not an unrelated revert. RE-DERIVE every pinned slot via
    `forge inspect storage DegenerusGame`. Do NOT edit any contracts/ file.
  </action>
  <verify>
    <automated>forge test --match-contract "V55RevertFreeEvCap" --match-test "RevertFree\|FailLoud\|Solvency\|GameOver\|ClassA\|ClassB\|ClassC" 2>&1 | tail -8 (the 3 classes pass; non-vacuous)</automated>
  </verify>
  <acceptance_criteria>
    - `test/fuzz/V55RevertFreeEvCap.t.sol` exists with named tests for class A (revert-free funded slice, fuzzed), class B (forced solvency underflow reverts), and class C (gameover routing unblocked).
    - class A fuzzes funded amount/claimable-mix and asserts no revert (non-vacuous: the sub was funded + processed).
    - class B uses `vm.expectRevert` on the forced claimablePool underflow at the checked `-=` (the violation fails loud, not masked).
    - class C asserts the gameover advance leg proceeds with an afking STAGE present.
    - `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>V55RevertFreeEvCap.t.sol proves TST-02's three classes: revert-free funded slice (A), fail-loud solvency (B), gameover-unblocked (C), all non-vacuous.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TST-03 EV-cap exactly-once — no double-draw vs the buy-time path (in V55RevertFreeEvCap.t.sol)</name>
  <read_first>
    - contracts/modules/DegenerusGameLootboxModule.sol :459-488 (_applyEvMultiplierWithCap — the RMW), :772/:805 (the buy-time call from resolveLootboxDirect), :877 (resolveAfkingBox — the open-time call, same map/key)
    - contracts/storage/DegenerusGameStorage.sol (LOOTBOX_EV_BENEFIT_CAP = 10 ether; lootboxEvBenefitUsedByLevel map)
    - .planning/phases/351-CONTEXT.md §"EV-cap clamp" (the exactly-once-at-open charge; the buy-time write is bypassed for afking boxes)
    - .planning/phases/351-PATTERNS.md §5 (the differential donor for the multi-open sequence)
  </read_first>
  <behavior>
    - The afking open increments lootboxEvBenefitUsedByLevel[player][level+1] EXACTLY ONCE per open (one RMW), with NO buy-time EV write for the same afking box (no double-draw).
    - The map/key is the SAME one MintModule's buy-time write uses → one shared per-level 10-ETH budget across afking + any residual human boxes (equivalent to the v54 per-(sub,level) accumulator).
    - The benefit is hard-clamped ≤ 10 ETH (no revert at the cap).
  </behavior>
  <action>
    Add to `test/fuzz/V55RevertFreeEvCap.t.sol` the TST-03 proofs:
    (a) exactly-once — open an afking box and assert `lootboxEvBenefitUsedByLevel[player][level+1]` increments by
    exactly the open's adjustedPortion (one RMW), and assert NO buy-time EV write occurred for that afking box
    (read the map before/after the STAGE stamp vs after the open — the buy-time path is bypassed → the increment
    happens only at open);
    (b) shared-budget equivalence — interleave an afking open and a human box open at the same (player, level+1) and
    assert they draw from the SAME per-level budget map/key (one shared ≤10-ETH budget), proving equivalence to the
    v54 per-(sub,level) accumulator;
    (c) cap-clamp — drive the per-level used-benefit near 10 ETH and assert the next open clamps (no revert, budget
    saturates at the cap). Light fuzz on a multi-open sequence (CONTEXT D-351-04). RE-DERIVE the
    lootboxEvBenefitUsedByLevel slot via `forge inspect storage DegenerusGame` (the [player][level+1] mapping slot).
    Do NOT edit any contracts/ file.
  </action>
  <verify>
    <automated>forge test --match-contract "V55RevertFreeEvCap" --match-test "EvCap\|ExactlyOnce\|DoubleDraw\|Budget\|Clamp" 2>&1 | tail -6 (pass; non-vacuous)</automated>
  </verify>
  <acceptance_criteria>
    - A named exactly-once test asserts the EV-cap map increments by exactly one open's portion AND that no buy-time EV write occurred for the afking box (no double-draw).
    - A shared-budget test asserts the afking open and a human open draw from the same `lootboxEvBenefitUsedByLevel[player][level+1]` key.
    - A cap-clamp test asserts the benefit saturates at 10 ETH with no revert.
    - The EV-cap map slot is re-derived (no hardcoded AfKing-layout literal).
    - `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>The EV-cap is proven exactly-once at open with no double-draw vs the buy-time path, drawing from the same shared per-level budget, clamped ≤10 ETH — equivalent to v54.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only. Adapts the revert-free corpus + authors the TST-02/03 proofs. ZERO contract mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-351-05-FG | Tampering | the TST-02/03 revert-free + EV-cap tests | mitigate | False-GREEN risks: (1) class B passing on an UNRELATED revert instead of the solvency underflow — mitigated by `vm.expectRevert` targeting the checked `-=` specifically; (2) the EV-cap exactly-once test reading a mis-derived map slot → vacuous — mitigated by `forge inspect storage DegenerusGame`; (3) a class-A "no revert" that never actually funded/processed a sub — mitigated by non-vacuity (assert the sub was funded + processed). ZERO `contracts/` mutation (`git diff 453f8073 HEAD -- contracts/` empty). |
</threat_model>

<verification>
- KeeperNonBrick + V55RevertFreeEvCap compile + run under `forge test --match-contract`.
- The class A/B/C + the EV-cap exactly-once/shared-budget/clamp assertions are non-vacuous.
- The batchPurchase-isolation leg is dropped and logged for the 351-09 ledger.
- `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
</verification>

<success_criteria>
TST-02 + TST-03 are empirically proven: a funded process/open never reverts on well-formed slices (class A),
a solvency violation fails loud (class B), game-over routing is never blocked (class C), and the EV-cap is
enforced exactly once per open with no double-draw vs the buy-time path (equivalent to v54). The revert-free
corpus is adapted; the removed batchPurchase-isolation surface is dropped per D-351-02. ZERO `contracts/*.sol`
mutation.
</success_criteria>

<output>
Create `.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-05-SUMMARY.md` when done.
</output>
