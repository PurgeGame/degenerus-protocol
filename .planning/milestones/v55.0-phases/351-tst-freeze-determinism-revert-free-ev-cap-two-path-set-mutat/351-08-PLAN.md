---
phase: 351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat
plan: 08
type: execute
wave: 2
depends_on: ["351-01"]
files_modified:
  - test/gas/KeeperOpenBoxWorstCaseGas.t.sol
  - test/gas/V55AfkingGasMarginal.t.sol
autonomous: true
requirements: [TST-06]
must_haves:
  truths:
    - "Per-BUY marginal: (gas for N funded lootbox subs − gas for N−1) / 1 measured via a new-day advanceGame() STAGE, reported vs the v54 cold-ledger ~120-130k oracle"
    - "Per-OPEN marginal: (gas for N opens − gas for N−1) / 1 measured via autoOpen over N vs N−1 ready stamped boxes"
    - "Both marginals stay under the 16.7M HARD per-tx ceiling at SUB_STAGE_BATCH=50"
    - "GAS-02: a no-STATICCALL trace assertion over the process/open path (no STATICCALL targets a different address)"
    - "GAS-03 Outcome A recorded: no claimablePool oracle exercised (N/A — no Outcome-B diff)"
    - "The per-buy marginal INCLUDES the 349.2-restored BURNIE side-effects (not flagged as a GAS-01 regression)"
    - "git diff 453f8073 HEAD -- contracts/ stays EMPTY"
  artifacts:
    - path: "test/gas/V55AfkingGasMarginal.t.sol"
      provides: "the TST-06 per-buy + per-open marginal harness + the no-STATICCALL trace + the 16.7M ceiling"
      contains: "perBoxMarginal"
    - path: "test/gas/KeeperOpenBoxWorstCaseGas.t.sol"
      provides: "the adapted per-open marginal donor (reframed onto _openAfkingBox)"
  key_links:
    - from: "test/gas/V55AfkingGasMarginal.t.sol"
      to: "GameAfkingModule processSubscriberStage (buy) + _openAfkingBox/resolveAfkingBox (open)"
      via: "loop-N-divide marginal under 16.7M, no-STATICCALL trace"
      pattern: "perBoxMarginal"
---

<objective>
Build the TST-06 marginal-gas harness EXACTLY from `350-TST06-MEASUREMENT-SPEC.md` (its section 6 Wave-0 gaps),
and adapt the per-open marginal donor `KeeperOpenBoxWorstCaseGas.t.sol` (which already embeds the CR-01
loop-N-divide idiom) onto the afking open. The harness measures the per-buy marginal (instrument
`processSubscriberStage` via a new-day `advanceGame()` with N vs N−1 funded LOOTBOX subs) + the per-open
marginal (instrument `_openAfkingBox`/`resolveAfkingBox` via `autoOpen` over N vs N−1 ready boxes), each under
the 16.7M HARD ceiling, plus the no-STATICCALL trace assertion (GAS-02) and the GAS-03 Outcome-A N/A record.

Purpose: TST-06 — the per-buy + per-open marginal gas measured under the 16.7M ceiling with the GAS-01/02 wins
proven same-results (GAS-03 N/A under Outcome A). The per-buy marginal legitimately includes the 349.2-restored
BURNIE quest/affiliate calls — report as-is, do NOT subtract (not a GAS-01 regression).
Output: 1 adapted donor file + 1 new TST-06 harness. ZERO `contracts/*.sol` mutation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-CONTEXT.md
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-PATTERNS.md
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-01-SUMMARY.md
@.planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-TST06-MEASUREMENT-SPEC.md

<interfaces>
<!-- The TST-06 spec instruments + the donor idiom (READ-ONLY anchors at 453f8073; never edit sites). -->

GAS-01 per-BUY marginal (350-SPEC section 1):
  Instrument processSubscriberStage (GameAfkingModule.sol:539; lootbox branch ~:735-833; stamp writes ~:793 scorePlus1 / ~:794 amount / ~:840 lastAutoBoughtDay).
  Invoked by advanceGame()'s new-day STAGE (DegenerusGameAdvanceModule.sol:305-312 → _runSubscriberStage :754 → processSubscriberStage(SUB_STAGE_BATCH) :759-761), strictly PRE-RNG.
  Exercising tx: a new-day advanceGame() with N>=2 funded LOOTBOX-mode subs (useTickets clear; funded via afkingFunding or claimable so :708-711 debits).
  Marginal: (gas for N funded lootbox subs − gas for N−1) / 1. Oracle: the v54 cold-ledger box-buy ~120-130k (~6 cold SSTOREs + boxPlayers.push + enqueueBoxForAutoOpen).
  ⚠ The marginal INCLUDES the 349.2-restored BURNIE side-effects (quests.handlePurchase :760, recordMintQuestStreak :773, affiliate.payAffiliate :806/:816, coinflip.creditFlip :831) — report as-is, do NOT subtract.

GAS-01 per-OPEN marginal (350-SPEC section 2):
  Instrument _openAfkingBox (GameAfkingModule.sol:888) → resolveAfkingBox (DegenerusGameLootboxModule.sol:877), seeded (sub.amount, day, rngWordByDay[day], sub.scorePlus1-1) :901-907; marker sub.lastOpenedDay=day :892.
  Invoked by _autoOpen(maxCount) :938, gated by _afkingBoxReady :918 (lastOpenedDay < lastAutoBoughtDay && rngWordByDay[lastAutoBoughtDay] != 0).
  Exercising tx: autoOpen(count) :1023 OR mintBurnie() open leg :1000-1009, over N ready stamped boxes after rngWordByDay[day] lands.
  Marginal: (gas for N opens − gas for N−1) / 1. Oracle: the human openLootBox (:503) which walks+zeroes the cold ledger — the afking open does NONE of that (uniform O(1) per box).

GAS-02 no-STATICCALL trace (350-SPEC section 3):
  Assert NO STATICCALL opcode targets a DIFFERENT address on the process/open path (the in-context afkingFunding SLOADs :464/:662/:709 + claimableWinnings :463 replaced the old cross-contract staticcalls).
  Carve-out (do NOT flag): same-contract DELEGATECALLs (purchaseWith ticket :718-730, resolveAfkingBox :901, the 349.2 quests/affiliate/coinflip :760/:806/:816/:831) + the off-path afkingFundingOf/afkingSnapshot Game views (DegenerusGame.sol:1579/:2645, called only by DegenerusVault.sol:518).

GAS-03 Outcome A (350-SPEC section 4): N/A — record "no Outcome-B diff produced; GAS-03 measurement not exercised". The per-slice-vs-batch oracle + forced-underflow test are N/A (350 closed Outcome A, GAS-03 REJECTED).

The 16.7M ceiling (350-SPEC section 5): SUB_STAGE_BATCH=50 (DegenerusGameAdvanceModule.sol:149); a landed buy ~262k → 50 ~13.1M. Both the STAGE-50 + the open leg under 16.7M.

The DONOR idiom to PORT (KeeperOpenBoxWorstCaseGas.t.sol):
  the loop-N-divide MARGINAL (Test D :163-217): gasBefore=gasleft(); game.autoOpen(nBoxes*64); totalGas=gasBefore-gasleft(); perBoxMarginal=totalGas/nBoxes; assertLt(perBoxMarginal, SINGLE_BOX_TOTAL_REF_GAS).
  the worst-case-precondition + non-vacuity gate (:97-112): each box queued + RNG-ready + un-opened BEFORE, zeroed/opened AFTER.
  the _buyBox real-deposit helper (:343-348) — reframe to a funded LOOTBOX-mode SUB for the per-buy leg.
  the RNG-word-inject + slot-read helpers (:357-373) — port for the open-readiness setup (the afking open gates on rngWordByDay[day]; RE-DERIVE that map's slot).

foundry.toml: ffi OFF; the trace assertion uses vm.startStateDiffRecording / vm trace facilities (NOT vm.ffi). RE-DERIVE every pinned slot via forge inspect storage DegenerusGame.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt KeeperOpenBoxWorstCaseGas.t.sol — reframe the per-open marginal donor onto _openAfkingBox</name>
  <read_first>
    - test/gas/KeeperOpenBoxWorstCaseGas.t.sol (FULL — 374 lines; Test D the loop-N-divide :163-217, the worst-case-precondition gate :97-112, the _buyBox helper :343-348, the RNG-inject/slot-read helpers :357-373)
    - contracts/modules/GameAfkingModule.sol :888-907 (_openAfkingBox + seed), :918 (_afkingBoxReady), :938 (_autoOpen), :1023 (autoOpen)
    - contracts/modules/DegenerusGameLootboxModule.sol :877 (resolveAfkingBox)
    - .planning/phases/350-TST06-MEASUREMENT-SPEC.md section 2 + .planning/phases/351-PATTERNS.md section 11
  </read_first>
  <action>
    Reframe `KeeperOpenBoxWorstCaseGas.t.sol` from the HUMAN `boxPlayers`/`autoOpen` path onto the AFKING open:
    `_openAfkingBox` (GameAfkingModule.sol:888) → `resolveAfkingBox` (:877), driven by `autoOpen(count)` (:1023) over
    N ready STAMPED boxes after `rngWordByDay[stampDay]` lands (per 350-SPEC section 2). PRESERVE the loop-N-divide
    MARGINAL idiom (Test D :163-217) VERBATIM in shape — `perBoxMarginal = totalGas / nBoxes`, assert <
    SINGLE_BOX_TOTAL_REF_GAS (CR-01). PRESERVE the worst-case-precondition + non-vacuity gate (:97-112): assert each
    box is queued + RNG-ready + un-opened BEFORE and zeroed/opened AFTER. Reframe the `_buyBox` helper to stamp a
    funded LOOTBOX-mode SUB via a new-day STAGE; port the RNG-word-inject helpers (re-derive the `rngWordByDay[day]`
    map slot — the afking open gates on it, not on a lootbox-index word). RE-DERIVE every pinned slot via
    `forge inspect storage DegenerusGame`. Do NOT edit any contracts/ file.
  </action>
  <verify>
    <automated>forge test --match-contract KeeperOpenBoxWorstCaseGas 2>&1 | tail -8 (the per-open marginal over afking boxes runs; non-vacuous; under the ref ceiling)</automated>
  </verify>
  <acceptance_criteria>
    - The per-open marginal is measured over N vs N−1 ready AFKING stamped boxes (the loop-N-divide idiom preserved).
    - The worst-case-precondition + non-vacuity gate asserts each box materialized (not a skip).
    - `grep -v '^[[:space:]]*//' test/gas/KeeperOpenBoxWorstCaseGas.t.sol | grep -c "afKing\."` == 0 (game-resident calls).
    - The file compiles + runs under `forge test --match-contract KeeperOpenBoxWorstCaseGas`.
    - `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>KeeperOpenBoxWorstCaseGas measures the per-open marginal over afking stamped boxes with the CR-01 loop-N-divide idiom + the non-vacuity gate preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Author V55AfkingGasMarginal.t.sol — the per-BUY + per-OPEN marginal under 16.7M (350-SPEC section 1+2+5)</name>
  <read_first>
    - .planning/phases/350-TST06-MEASUREMENT-SPEC.md (FULL — section 1 per-buy, section 2 per-open, section 5 the 16.7M ceiling, section 0 the marginal rule)
    - contracts/modules/GameAfkingModule.sol :539 (processSubscriberStage), :735-833 (lootbox branch), :888 (_openAfkingBox), :1023 (autoOpen)
    - contracts/modules/DegenerusGameAdvanceModule.sol :149 (SUB_STAGE_BATCH=50), :305-312 (STAGE)
    - test/gas/KeeperOpenBoxWorstCaseGas.t.sol (Task 1 output — the marginal idiom + helpers to reuse) + test/fuzz/KeeperRewardRoutingSameResults.t.sol (the _settleGame VRF-drain :111-127)
  </read_first>
  <action>
    Create `test/gas/V55AfkingGasMarginal.t.sol` extending DeployProtocol. Build BOTH marginals per 350-SPEC:
    (a) per-BUY marginal (section 1) — subscribe N vs N−1 funded LOOTBOX-mode subs (useTickets clear; fund via
    afkingFunding/claimable), drive a new-day `advanceGame()` STAGE, measure (gas for N − gas for N−1) / 1, report the
    per-sub marginal vs the v54 cold-ledger ~120-130k oracle; assert it is FAR below ~120-130k (the warm-stamp
    collapse) AND under 16.7M for the 50-chunk. INCLUDE the 349.2-restored BURNIE side-effects — report as-is, do NOT
    subtract (per the spec ⚠-note: this is the correct same-results target, not a GAS-01 regression).
    (b) per-OPEN marginal (section 2) — reuse the Task-1 idiom: `autoOpen` over N vs N−1 ready stamped boxes after
    the day's word lands, measure (gas for N − gas for N−1) / 1, assert uniform O(1) per box and under 16.7M.
    (c) the 16.7M ceiling (section 5) — assert both the STAGE-50 and the open leg stay < 16_700_000 on the worst-case
    funded-lootbox mix. Port the `_settleGame` VRF-drain helper for the clean-day setup. Gate every marginal with the
    worst-case preconditions + non-vacuity (the sub actually bought / the box actually opened). RE-DERIVE every pinned
    slot via `forge inspect storage DegenerusGame`. Do NOT edit any contracts/ file.
  </action>
  <verify>
    <automated>forge test --match-contract V55AfkingGasMarginal 2>&1 | tail -10 (both marginals + the 16.7M ceiling pass; per-buy far below ~120-130k; non-vacuous)</automated>
  </verify>
  <acceptance_criteria>
    - `test/gas/V55AfkingGasMarginal.t.sol` exists with named tests for the per-buy marginal, the per-open marginal, and the 16.7M ceiling.
    - The per-buy marginal is `(gas for N − gas for N−1) / 1` via a new-day `advanceGame()` STAGE and is asserted FAR below the v54 cold-ledger ~120-130k.
    - The per-open marginal is `(gas for N − gas for N−1) / 1` via `autoOpen` and asserted uniform O(1) under 16.7M.
    - Both legs assert `< 16_700_000`.
    - The per-buy test does NOT subtract the 349.2 BURNIE side-effects (a comment + the SUMMARY note this is intended, not a regression).
    - `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>V55AfkingGasMarginal proves the per-buy + per-open marginals (loop-N-divide) collapse to the warm-stamp band and stay under 16.7M, including the 349.2 BURNIE side-effects as-is.</done>
</task>

<task type="auto">
  <name>Task 3: GAS-02 no-STATICCALL trace assertion + the GAS-03 Outcome-A N/A record (in V55AfkingGasMarginal.t.sol)</name>
  <read_first>
    - .planning/phases/350-TST06-MEASUREMENT-SPEC.md section 3 (the no-STATICCALL assertion + the carve-out) + section 4 (GAS-03 Outcome A N/A)
    - contracts/modules/GameAfkingModule.sol :463-464 (claimableWinnings/afkingFunding SLOADs), :662/:709 (funding SLOAD/debit), :760/:806/:816/:831 (the same-contract delegatecalls — carve-out), :901 (resolveAfkingBox delegatecall — carve-out)
    - test/fuzz/AfKingSubscription.t.sol (the 336-04 no-SLOAD oracle idiom — the closest existing trace-assertion analog, per PATTERNS "NO-STATICCALL trace assertion")
  </read_first>
  <action>
    Add to `test/gas/V55AfkingGasMarginal.t.sol` the GAS-02 no-STATICCALL trace assertion (350-SPEC section 3): over
    the process STAGE (`processSubscriberStage`) AND the open leg (`_openAfkingBox`), assert NO `STATICCALL` opcode
    targets a DIFFERENT address (the in-context `afkingFunding[*]`/`claimableWinnings[*]` SLOADs replaced the old
    cross-contract `GAME.afkingSnapshot`/`afkingFundingOf` STATICCALLs). Use `vm.startStateDiffRecording` /
    `vm.stopAndReturnStateDiff` (or the equivalent foundry trace facility — NOT `vm.ffi`, which is OFF) to inspect the
    call records, filtering for `STATICCALL` to a non-self address. CARVE OUT (do NOT flag): same-contract
    DELEGATECALLs (purchaseWith ticket :718-730, resolveAfkingBox :901, the 349.2 quests/affiliate/coinflip
    :760/:806/:816/:831) + the off-path afkingFundingOf/afkingSnapshot Game views (called only by DegenerusVault). Then
    RECORD the GAS-03 Outcome-A N/A note (350-SPEC section 4): a test (or a SUMMARY note + a `console.log`/comment) stating
    "no Outcome-B diff produced; GAS-03 claimablePool same-slot-flush measurement not exercised (N/A)" — the
    per-slice-vs-batch oracle + the forced-underflow test are N/A under Outcome A. Do NOT author the Outcome-B oracle.
    Do NOT edit any contracts/ file.
  </action>
  <verify>
    <automated>forge test --match-contract V55AfkingGasMarginal --match-test "NoStaticcall\|Trace\|Gas02" 2>&1 | tail -6 (the trace assertion passes; the carve-out is honored)</automated>
  </verify>
  <acceptance_criteria>
    - A named no-STATICCALL trace test exists asserting no `STATICCALL` targets a non-self address on the process/open path.
    - The carve-out is honored (same-contract delegatecalls + the off-path Game views are NOT flagged).
    - The GAS-03 Outcome-A N/A record exists (a test note / SUMMARY entry): "no Outcome-B diff; GAS-03 not exercised".
    - No Outcome-B `claimablePool` per-slice-vs-batch oracle or forced-underflow test is authored (they are N/A).
    - `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>The GAS-02 no-STATICCALL trace assertion passes with the carve-out honored, and the GAS-03 Outcome-A N/A is recorded (no Outcome-B oracle authored).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only. Adapts the per-open donor + authors the TST-06 harness. ZERO contract mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-351-08-FG | Tampering | the TST-06 gas harness | mitigate | False-GREEN risks: (1) a single-box TOTAL reported as the marginal (the CR-01 faucet hazard) — mitigated by the loop-N-divide `(N − (N−1))/1` rule + the SINGLE_BOX_TOTAL_REF_GAS upper bound; (2) a marginal measured over boxes that never materialized (vacuous) — mitigated by the worst-case-precondition + non-vacuity gate; (3) the no-STATICCALL trace flagging a legitimate same-contract delegatecall — mitigated by the explicit carve-out; (4) subtracting the 349.2 BURNIE side-effects (mis-flagging a non-regression) — mitigated by reporting the marginal as-is per the spec. ZERO `contracts/` mutation (`git diff 453f8073 HEAD -- contracts/` empty). |
</threat_model>

<verification>
- KeeperOpenBoxWorstCaseGas + V55AfkingGasMarginal compile + run under `forge test --match-contract`.
- Both marginals use loop-N-divide, are non-vacuous, and stay under 16.7M.
- The no-STATICCALL trace passes with the carve-out; GAS-03 Outcome-A N/A is recorded.
- `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
</verification>

<success_criteria>
TST-06 is empirically proven: the per-buy + per-open marginals (loop-N-divide) collapse to the warm-stamp band
and stay under the 16.7M HARD ceiling, the no-STATICCALL trace confirms GAS-02, and GAS-03 is recorded N/A under
Outcome A. The per-buy marginal includes the 349.2-restored BURNIE side-effects as-is (not a regression). ZERO
`contracts/*.sol` mutation.
</success_criteria>

<output>
Create `.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-08-SUMMARY.md` when done.
</output>
