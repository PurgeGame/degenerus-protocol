---
phase: 351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fuzz/helpers/DeployProtocol.sol
  - scripts/lib/predictAddresses.js
autonomous: true
requirements: [TST-05]
must_haves:
  truths:
    - "forge build succeeds — the AfKing.sol import compile cascade is cleared"
    - "new GameAfkingModule() in the fixture lands at ContractAddresses.GAME_AFKING_MODULE"
    - "new DegenerusGameBingoModule() lands at ContractAddresses.GAME_BINGO_MODULE"
    - "the Vault/SDGNRS constructor self-subscribes (SUB-09) hit live game-resident subscribe code"
    - "git diff 453f8073 HEAD -- contracts/ stays EMPTY (zero contract mutation)"
  artifacts:
    - path: "test/fuzz/helpers/DeployProtocol.sol"
      provides: "game-resident afking + bingo module deploy, AfKing standalone removed"
      contains: "GameAfkingModule"
    - path: "scripts/lib/predictAddresses.js"
      provides: "DEPLOY_ORDER with GAME_AFKING_MODULE + GAME_BINGO_MODULE, AF_KING dropped"
      contains: "GAME_AFKING_MODULE"
  key_links:
    - from: "test/fuzz/helpers/DeployProtocol.sol"
      to: "ContractAddresses.GAME_AFKING_MODULE"
      via: "patchForFoundry-predicted CREATE address from DEPLOY_ORDER nonce position"
      pattern: "GameAfkingModule"
---

<objective>
Repair the shared Foundry fixture `test/fuzz/helpers/DeployProtocol.sol` so it deploys the game-resident
`GameAfkingModule` (and the new `DegenerusGameBingoModule`) instead of the deleted standalone
`contracts/AfKing.sol`, and reconcile the `scripts/lib/predictAddresses.js` `DEPLOY_ORDER` + the fixture's
deploy sequence so each `new …Module()` lands at its `ContractAddresses` constant under
`patchForFoundry.js`. This is **Wave 0 — blocking-first** (D-351-03): the fixture import of the deleted
`AfKing.sol` cascades a compile break to 64 dependent test files; nothing in this phase compiles until this
lands. Precedent: v46 Phase 318 "fixture-repair un-bricked 533→532 tests".

Purpose: Un-brick the entire test suite so every downstream plan (the corpus adaptation, the new v55 proofs,
the gas harness, the regression reconciliation) can compile and run against the game-resident model.
Output: A compiling fixture + an aligned deploy-order script. ZERO `contracts/*.sol` mutation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-CONTEXT.md
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-PATTERNS.md

<interfaces>
<!-- The deploy-address mechanism the fixture relies on (verified against the live tree at 453f8073). -->
<!-- Executor: use these directly — no exploration needed. -->

How module addresses resolve (the mechanism Wave 0 must satisfy):
- DegenerusGame's delegatecall stubs use COMPILE-TIME constants:
  `ContractAddresses.GAME_AFKING_MODULE` (DegenerusGame.sol:372,:392; DegenerusGameAdvanceModule.sol:756)
  `ContractAddresses.GAME_BINGO_MODULE` (DegenerusGame.sol:326,:1592 — claimBingo + claimAffiliateDgnrs ARCH-04 reclaim)
- For Foundry, `scripts/lib/patchForFoundry.js` → `patchContractAddresses.js` OVERWRITES ContractAddresses.sol's
  constants with CREATE-from-nonce addresses computed by `predictAddresses.js` DEPLOY_ORDER (with a .bak backup),
  BEFORE `forge build`. So: fixture deploy order == DEPLOY_ORDER == patched constants → `new X()` lands at its constant.
- The `0xaf6109…` / `0xB14609…` constants currently in ContractAddresses.sol are the PRODUCTION vanity addresses;
  at test time they are REPLACED by the patch. Wave 0 does NOT touch ContractAddresses.sol (it is `contracts/`, frozen);
  it only aligns DEPLOY_ORDER + the fixture so the patch produces a consistent mapping.

Current fixture break (DeployProtocol.sol):
  :26  import {AfKing} from "../../../contracts/AfKing.sol";   // DELETED → compile break
  :70  AfKing public afKing;                                   // state var
  :123-133  afKing = new AfKing(5_000_000_000, 885_000_000, 10_000_000_000); // N+18 = nonce 23
            // VAULT (N+19) + SDGNRS (N+20) constructors self-subscribe (SUB-09)

The 10 sibling delegatecall modules already deployed (no ctor args) — the EXACT shape to mirror, :92-101:
  mintModule = new DegenerusGameMintModule();    // N+1
  advanceModule = new DegenerusGameAdvanceModule(); // N+2
  … lootboxModule = new DegenerusGameLootboxModule(); // N+7 … degeneretteModule (N+9)

SUB-09 self-subscribe is ALREADY game-resident at 453f8073 (verified — do NOT re-wire the .sol):
  DegenerusVault.sol:481   gamePlayer.subscribe(address(this), true, false, 1, 0, address(0))
  StakedDegenerusStonk.sol:382  game.subscribe(address(this), true, false, 1, 2, address(0))
  DegenerusVault.sol:518   gamePlayer.withdrawAfkingFunding(gamePlayer.afkingFundingOf(address(this)))
So VAULT/SDGNRS need GAME + the afking module deployed BEFORE them (they pass address(0) per OPEN-E default-self).

Current predictAddresses.js DEPLOY_ORDER (scripts/lib/predictAddresses.js:21-45) — STALE:
  …"DEITY_PASS" (N+17), "AF_KING" (N+18), "VAULT" (N+19), "SDGNRS" (N+20), "DGNRS", "ADMIN", "GNRUS"
  → AF_KING must be replaced; GAME_BINGO_MODULE + GAME_AFKING_MODULE must be inserted; no Bingo/Afking entries today.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile predictAddresses.js DEPLOY_ORDER for the two new game modules + drop AF_KING</name>
  <read_first>
    - scripts/lib/predictAddresses.js (the full file — DEPLOY_ORDER :21-45 + the constructor-ordering constraints comment)
    - scripts/lib/patchContractAddresses.js (how the address map patches ContractAddresses.sol constants by key name)
    - contracts/ContractAddresses.sol :13-36 (the canonical slot NAMES the DEPLOY_ORDER keys must match — GAME_MINT_MODULE…GAME_DEGENERETTE_MODULE, GAME_BINGO_MODULE, GAME_AFKING_MODULE; note AF_KING is GONE from the live source)
    - .planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-PATTERNS.md §1 (the nonce-alignment work)
  </read_first>
  <action>
    Update `scripts/lib/predictAddresses.js` `DEPLOY_ORDER` (test infra — under scripts/, NOT frozen; freely
    modifiable, confirmed unchanged since 453f8073) so the predicted-address map keys match the live
    `ContractAddresses.sol` slot set and the constructor-cross-call ordering holds:
    (a) ADD `"GAME_BINGO_MODULE"` + `"GAME_AFKING_MODULE"` entries among the game-module block (they are
    constructor-arg-less delegatecall modules like the other 10) — place them so GAME (N+12) and both new
    modules are deployed BEFORE VAULT/SDGNRS (whose constructors call `game.subscribe(...)`/the afking surface);
    (b) REMOVE the `"AF_KING"` (old N+18) entry — that standalone contract is deleted.
    Renumber the N+ comments to stay self-consistent. Keep every existing constructor-ordering constraint in the
    header comment satisfied (COIN before VAULT; GAME+modules before SDGNRS; SDGNRS before DGNRS; GAME before ADMIN;
    GNRUS last). The KEY NAMES in DEPLOY_ORDER must be exactly the ContractAddresses constant names (patchContractAddresses
    replaces by key) — confirm GAME_BINGO_MODULE / GAME_AFKING_MODULE spellings against ContractAddresses.sol:33/:35.
    Do NOT introduce any AF_KING key (it has no live slot → patch would no-op / mismatch).
  </action>
  <verify>
    <automated>grep -c "GAME_AFKING_MODULE\|GAME_BINGO_MODULE" scripts/lib/predictAddresses.js (≥2) AND grep -c "\"AF_KING\"" scripts/lib/predictAddresses.js (== 0)</automated>
  </verify>
  <acceptance_criteria>
    - `DEPLOY_ORDER` contains the keys `"GAME_BINGO_MODULE"` and `"GAME_AFKING_MODULE"`.
    - `DEPLOY_ORDER` no longer contains `"AF_KING"`.
    - Both new module keys appear at deploy positions BEFORE `"VAULT"` and `"SDGNRS"`.
    - Every DEPLOY_ORDER key still maps to an existing ContractAddresses.sol constant name (no orphan key).
    - `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY (this task touches scripts/ only).
  </acceptance_criteria>
  <done>predictAddresses.js DEPLOY_ORDER lists GAME_BINGO_MODULE + GAME_AFKING_MODULE before VAULT/SDGNRS, AF_KING removed, all keys map to live ContractAddresses slots.</done>
</task>

<task type="auto">
  <name>Task 2: Repair DeployProtocol.sol — deploy GameAfkingModule + BingoModule, remove standalone AfKing, fix SUB-09 comments</name>
  <read_first>
    - test/fuzz/helpers/DeployProtocol.sol (the FULL file — the import block :20-31, the state-var block :51-75, the deploy block :79-143)
    - contracts/modules/GameAfkingModule.sol :234 (subscribe sig) + :539 (processSubscriberStage) — confirm it is a delegatecall module with NO constructor args
    - contracts/modules/DegenerusGameBingoModule.sol (confirm constructor-arg-less; it owns claimBingo + the ARCH-04 claimAffiliateDgnrs relocation)
    - scripts/lib/predictAddresses.js (Task 1 output — the deploy ORDER the fixture must mirror exactly)
    - .planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-PATTERNS.md §1 (the exact lines + the sibling-idiom shape)
  </read_first>
  <action>
    In `test/fuzz/helpers/DeployProtocol.sol`:
    (a) DELETE the import `import {AfKing} from "../../../contracts/AfKing.sol";` (:26) and ADD imports for
    `GameAfkingModule` (`contracts/modules/GameAfkingModule.sol`) + `DegenerusGameBingoModule`
    (`contracts/modules/DegenerusGameBingoModule.sol`).
    (b) Replace the `AfKing public afKing;` state var (:70) with `GameAfkingModule public afkingModule;` +
    `DegenerusGameBingoModule public bingoModule;`.
    (c) In `_deployProtocol()`, DELETE the standalone `afKing = new AfKing(...)` deploy (:126) and ADD
    `afkingModule = new GameAfkingModule();` + `bingoModule = new DegenerusGameBingoModule();` (no ctor args —
    mirror the sibling `new DegenerusGame*Module()` idiom at :92-101) at the EXACT deploy-order positions Task 1
    placed `GAME_BINGO_MODULE`/`GAME_AFKING_MODULE` in DEPLOY_ORDER — so the CREATE nonce sequence matches the
    predicted constants. Keep both new modules BEFORE the `vault = new DegenerusVault();` (:129) +
    `sdgnrs = new StakedDegenerusStonk();` (:133) deploys (their SUB-09 constructors self-subscribe via the
    game-resident path and must hit live module code). Update the stale SUB-09 comments at :123-133 to reference
    `game.subscribe(...)` / the game-resident afking surface (NOT `AfKing.subscribe`) — these are comment-only
    fixes; the .sol self-subscribe is ALREADY game-resident (do NOT re-wire DegenerusVault/StakedDegenerusStonk).
    Do not change the relative order of any OTHER contract (mocks, the 10 existing modules, COIN/COINFLIP/GAME,
    AFFILIATE/JACKPOTS/QUESTS/DEITY_PASS, DGNRS/ADMIN/GNRUS) unless inserting the two new modules requires shifting
    the N+ comments — keep the source-order ≡ DEPLOY_ORDER ≡ patched-constants invariant intact.
  </action>
  <verify>
    <automated>forge build --contracts test/fuzz/helpers/DeployProtocol.sol 2>&1 | grep -qi "AfKing.sol.*not found" && echo FAIL || echo PASS-import-cleared; then run: forge build 2>&1 | tail -3 (compiles)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AfKing" test/fuzz/helpers/DeployProtocol.sol` excluding comments (`grep -v '^[[:space:]]*//' | grep -c "new AfKing\|import {AfKing}\|AfKing public"`) == 0.
    - The fixture imports + deploys `GameAfkingModule` and `DegenerusGameBingoModule` (constructor-arg-less).
    - `forge build` no longer errors on `contracts/AfKing.sol not found` (the :26 break is gone).
    - The two new module deploys appear in `_deployProtocol()` BEFORE `vault =` and `sdgnrs =`.
    - `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>DeployProtocol.sol compiles, deploys GameAfkingModule + DegenerusGameBingoModule at the DEPLOY_ORDER nonce positions, standalone AfKing removed, SUB-09 comments corrected.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the deploy-order/nonce alignment — modules land at their ContractAddresses constants</name>
  <read_first>
    - test/fuzz/helpers/DeployProtocol.sol (the repaired deploy block from Task 2 + the existing :42-43 patch-dependency comment)
    - contracts/ContractAddresses.sol :13-36 (the GAME_AFKING_MODULE / GAME_BINGO_MODULE constants the deployed addresses must equal AT TEST TIME after patch)
    - contracts/DegenerusGame.sol :316-330 (claimBingo delegatecall stub) + :363-393 (subscribe/mintBurnie afking stubs) — these REVERT/mis-dispatch if the module is not at GAME_AFKING_MODULE/GAME_BINGO_MODULE
    - Any existing fixture address-assertion idiom (grep the corpus for `ContractAddresses.GAME_` assertions used as a deploy sanity check)
  </read_first>
  <action>
    Add a minimal alignment assertion so a future deploy-order drift fails LOUD rather than silently
    mis-dispatching a delegatecall. Either (preferred) add a `setUp`-time assertion in a tiny new sanity test
    OR extend an existing fixture-consumer: after `_deployProtocol()`, assert
    `address(afkingModule) == ContractAddresses.GAME_AFKING_MODULE` and
    `address(bingoModule) == ContractAddresses.GAME_BINGO_MODULE` (these hold only if the fixture deploy order
    matches the patched constants — the whole point of the reconciliation). If the repo's Foundry build does NOT
    run `patchForFoundry.js` automatically (no pretest hook — verified: package.json has none), document in the
    fixture comment the exact manual prerequisite (`node scripts/...patchForFoundry...` before `forge build`) and
    instead assert the WEAKER but always-true invariant: a `game.subscribe(...)` dispatch from the fixture
    succeeds (proving the afking module IS reachable at its constant — a mis-placed module would revert the
    delegatecall). Pick whichever the live patch-flow supports; the binding requirement is: a deploy-order
    regression must surface as a test failure, not a silent wrong-address dispatch. Do NOT edit ContractAddresses.sol.
  </action>
  <verify>
    <automated>forge test --match-path 'test/fuzz/helpers/*' 2>&1 | tail -5 ; AND a one-shot: forge test --match-test "testFuzz_RngLock" -vv 2>&1 | grep -qi "AfKing.sol" && echo STILL-BROKEN || echo COMPILES</automated>
  </verify>
  <acceptance_criteria>
    - A test/assertion exists that FAILS if `address(afkingModule) != GAME_AFKING_MODULE` (or the documented weaker dispatch-reachability assertion).
    - At least one previously-bricked fuzz test file now COMPILES (no `AfKing.sol not found`) — pick one via `forge test --match-path` and show it reaches execution (pass OR a non-compile failure is acceptable here; the compile cascade is what Wave 0 fixes).
    - `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>The fixture proves (or documents-and-guards) that GameAfkingModule/BingoModule land at their ContractAddresses constants; the compile cascade is cleared so downstream waves can build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only phase. The fixture + a Node deploy-order script. No contract mutation, no new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-351-01-FG | Tampering | DeployProtocol fixture / predictAddresses.js | mitigate | The only "threat" is a silent deploy-order drift → a module NOT at its ContractAddresses constant → delegatecall mis-dispatch / vacuous-GREEN tests. Mitigation: Task 3's `address(module) == GAME_*_MODULE` alignment assertion fails loud on drift. ZERO `contracts/` mutation (`git diff 453f8073 HEAD -- contracts/` empty). |
</threat_model>

<verification>
- `forge build` succeeds (the `AfKing.sol not found` cascade is cleared).
- `address(afkingModule) == ContractAddresses.GAME_AFKING_MODULE` (or the documented weaker dispatch-reachability guard).
- `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
</verification>

<success_criteria>
The shared fixture compiles against the game-resident `GameAfkingModule` + `DegenerusGameBingoModule`, the
standalone `AfKing` deploy is removed, the deploy-order script + fixture + patched constants are reconciled so
modules land at their `ContractAddresses` slots, and the SUB-09 self-subscribe path hits live game-resident
code. The 64-file compile cascade is un-bricked. ZERO `contracts/*.sol` mutation.
</success_criteria>

<output>
Create `.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-01-SUMMARY.md` when done.
</output>
