---
phase: 351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat
plan: 03
type: execute
wave: 2
depends_on: ["351-01"]
files_modified:
  - test/fuzz/KeeperRewardRoutingSameResults.t.sol
  - test/fuzz/KeeperRouterOneCategory.t.sol
  - test/fuzz/KeeperFaucetResistance.t.sol
autonomous: true
requirements: [TST-02, TST-05]
must_haves:
  truths:
    - "The reward-routing / one-category / faucet-resistance fuzz files compile + run against game.mintBurnie()"
    - "mintBurnie's one-category structural early-return (no advance+open bounty in one tx) is reframed + asserted"
    - "The bounty/faucet-resistance math is repointed to GameAfkingModule bounty constants"
    - "The vm.readFile AFKING_SRC repoints to GameAfkingModule.sol (no runtime throw)"
    - "git diff 453f8073 HEAD -- contracts/ stays EMPTY"
  artifacts:
    - path: "test/fuzz/KeeperRewardRoutingSameResults.t.sol"
      provides: "the differential same-results scaffolding (CoinflipStakeUpdated decode + _settleGame), adapted"
      contains: "mintBurnie"
    - path: "test/fuzz/KeeperRouterOneCategory.t.sol"
      provides: "one-category-no-stack property on mintBurnie, AFKING_SRC repointed"
  key_links:
    - from: "test/fuzz/KeeperRouterOneCategory.t.sol"
      to: "contracts/modules/GameAfkingModule.sol"
      via: "vm.readFile AFKING_SRC source-grep (repointed from deleted AfKing.sol)"
      pattern: "GameAfkingModule.sol"
---

<objective>
Adapt the three `Keeper*` reward/router/faucet fuzz files to the game-resident `mintBurnie()` router
(D-351-01). `KeeperRewardRoutingSameResults` is the PRIMARY differential same-results scaffolding (its
`CoinflipStakeUpdated` topic-decode + `_settleGame` VRF-drain are ported by the new v55 proofs in 351-04/05).
`KeeperRouterOneCategory` + `KeeperFaucetResistance` carry the heaviest `doWork` density and the two
`vm.readFile("contracts/AfKing.sol")` runtime traps.

Purpose: TST-02 (the router/faucet revert-free + bounty-bounded behavior) + TST-05 (these files must compile +
run for the non-widening reconciliation). The reward-routing differential scaffolding is the reusable donor
for the TST-01/TST-03/TST-06 box differential.
Output: 3 adapted fuzz files. ZERO `contracts/*.sol` mutation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-CONTEXT.md
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-PATTERNS.md
@.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-01-SUMMARY.md

<interfaces>
<!-- The call-site deltas + the runtime traps (READ-ONLY anchors at 453f8073; never edit sites). -->

Δ3 doWork → mintBurnie: `afKing.doWork()` → `game.mintBurnie()` (~42 sites across the two heavy files).
  mintBurnie GameAfkingModule.sol:985 (advance leg :993-996, open leg :1000-1009); stub DegenerusGame.sol:390.
Δ5 views: `afKing.subscriberCount()` → `_subscribers.length` via vm.load (re-derive slot).

The one-category-no-stack property reframes onto mintBurnie's structural early-return:
  if (advanceDue) {…advance leg…} else {…open leg…}  — GameAfkingModule.sol:993 vs :1000 (single creditFlip CEI-last).

Bounty/faucet math anchors (repoint the comment refs):
  OLD: AfKing.sol:851/854/870/878/890 (deleted)
  NEW: GameAfkingModule.sol bounty constants — BOUNTY_ETH_TARGET / ADVANCE_RATIO_NUM / OPEN_KNEE (~:987/:995/:1003).

The two vm.readFile RUNTIME TRAPS (file deleted → throws):
  KeeperRouterOneCategory.t.sol:88 + :286  AFKING_SRC = "contracts/AfKing.sol"  → repoint "contracts/modules/GameAfkingModule.sol"
  (KeeperLeversAndPacking is handled in plan 351-07.)
  After repointing, RE-DERIVE every grepped token (symbols renamed/relocated: the one-category early-return + single
  CEI-last creditFlip now live at GameAfkingModule.sol mintBurnie :985-1014).

The PRIMARY differential scaffolding to PRESERVE (KeeperRewardRoutingSameResults):
  COINFLIP_STAKE_UPDATED_SIG decode (:62-70) — topics[1]==recipient isolation; data[0:32]==amount.
  _settleGame(uint256 vrfWord) VRF-drain helper (:111-127) — the clean-day settle every v55 proof needs.
  Keep both intact; 351-04/05/08 port them.

foundry.toml: ffi OFF (vm.ffi blocked) but vm.readFile is allowed under fs_permissions read ./contracts;
  forge inspect / forge build --sizes do NOT need ffi.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adapt KeeperRewardRoutingSameResults.t.sol (the differential scaffolding) — doWork→mintBurnie, preserve _settleGame + the topic-decode</name>
  <read_first>
    - test/fuzz/KeeperRewardRoutingSameResults.t.sol (FULL — 712 lines; the COINFLIP_STAKE_UPDATED_SIG decode :62-70, _settleGame :111-127, the 11 afKing. + 5 doWork sites)
    - contracts/modules/GameAfkingModule.sol :985 (mintBurnie), :993-1009 (advance/open legs + creditFlip CEI-last)
    - .planning/phases/351-PATTERNS.md §5 (the differential same-results template)
  </read_first>
  <behavior>
    - `game.mintBurnie()` routes the reward exactly like the old `doWork()`: the recipient-isolated CoinflipStakeUpdated event still emits with the keeper recipient in topics[1] and the BURNIE amount in data[0:32].
    - The reward routing is byte-identical RESULTS to the v49 baseline expectation (the same-results property).
    - `_settleGame` still drives advanceGame + mock VRF fulfill until !advanceDue && !rngLocked (the clean-day helper preserved verbatim for downstream reuse).
  </behavior>
  <action>
    Apply Δ3 (`afKing.doWork()`→`game.mintBurnie()`, ~5-11 sites) + Δ5 (`subscriberCount()`→`_subscribers.length`
    via vm.load, re-derived slot). PRESERVE the COINFLIP_STAKE_UPDATED_SIG topic-decode (:62-70) and the
    `_settleGame` VRF-drain helper (:111-127) VERBATIM — these are the donor scaffolding 351-04/05/08 port. Reframe
    any `doWork`-reward assertion onto `mintBurnie`'s reward leg. RE-DERIVE the pinned slot via
    `forge inspect storage DegenerusGame`. Do NOT edit any contracts/ file.
  </action>
  <verify>
    <automated>forge test --match-contract "KeeperRewardRoutingSameResults" 2>&1 | tail -6 (compiles + runs; _settleGame + the topic-decode intact)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' test/fuzz/KeeperRewardRoutingSameResults.t.sol | grep -c "afKing\.\|doWork()"` == 0.
    - `_settleGame` and the COINFLIP_STAKE_UPDATED_SIG decode are still present (grep confirms — downstream plans depend on them).
    - The file compiles + runs under `forge test --match-contract KeeperRewardRoutingSameResults`.
    - `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>KeeperRewardRoutingSameResults compiles + runs on mintBurnie; the differential _settleGame + topic-decode scaffolding is preserved for reuse.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adapt KeeperRouterOneCategory.t.sol — mintBurnie one-category early-return + repoint AFKING_SRC vm.readFile</name>
  <read_first>
    - test/fuzz/KeeperRouterOneCategory.t.sol (FULL — 711 lines; AFKING_SRC :88 + :286, the 22 afKing. + 23 doWork sites, the one-category structural property)
    - contracts/modules/GameAfkingModule.sol :985-1014 (mintBurnie — the if(advanceDue){…}else{…} one-category split :993/:1000, single creditFlip CEI-last)
    - .planning/phases/351-PATTERNS.md §8 + the "vm.readFile source-grep repointing" shared pattern
  </read_first>
  <behavior>
    - `game.mintBurnie()` does NOT stack an advance bounty AND an open bounty in one tx — the structural early-return (advance leg XOR open leg) routes exactly one category per call.
    - The source-grep (vm.readFile of the module) finds the one-category early-return + the single CEI-last creditFlip at the new GameAfkingModule.sol location (no runtime throw on the deleted AfKing.sol).
  </behavior>
  <action>
    Repoint `AFKING_SRC = "contracts/AfKing.sol"` → `"contracts/modules/GameAfkingModule.sol"` at both :88 and :286
    (else `vm.readFile` THROWS at runtime — the file is deleted). RE-DERIVE every grepped token for the
    renamed/relocated symbols (the one-category early-return + the single CEI-last `creditFlip` now at mintBurnie
    :985-1014). Apply Δ3 to all ~23 `doWork` + 22 `afKing.` sites (→ `game.mintBurnie()` / game views). Reframe the
    one-category-no-stack property directly onto `mintBurnie`'s `if (advanceDue) {…} else {…}` structural
    early-return (:993/:1000). Repoint the bounty-math comment anchors (AfKing.sol:851/854/870/878/890) →
    GameAfkingModule.sol bounty constants. Do NOT edit any contracts/ file.
  </action>
  <verify>
    <automated>forge test --match-contract "KeeperRouterOneCategory" 2>&1 | tail -6 (no vm.readFile throw; one-category property runs)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "contracts/AfKing.sol" test/fuzz/KeeperRouterOneCategory.t.sol` == 0 (AFKING_SRC repointed at both sites).
    - `grep -v '^[[:space:]]*//' test/fuzz/KeeperRouterOneCategory.t.sol | grep -c "afKing\.\|doWork()"` == 0.
    - The file runs without a `vm.readFile` "file not found" revert.
    - The one-category-no-stack property test runs against `mintBurnie` (a single category per call asserted).
    - `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>KeeperRouterOneCategory runs on mintBurnie with AFKING_SRC repointed (no readFile throw), the one-category early-return reframed and asserted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Adapt KeeperFaucetResistance.t.sol — bounty-bounded faucet resistance on mintBurnie</name>
  <read_first>
    - test/fuzz/KeeperFaucetResistance.t.sol (FULL — 863 lines; the 11 afKing. + 19 doWork + 8 srcref sites, the bounty-faucet-resistance corners)
    - contracts/modules/GameAfkingModule.sol :985-1014 (mintBurnie + the bounty constants BOUNTY_ETH_TARGET/ADVANCE_RATIO_NUM/OPEN_KNEE ~:987/:995/:1003)
    - .planning/phases/351-PATTERNS.md §8 (faucet/router reframe)
  </read_first>
  <behavior>
    - Repeated `game.mintBurnie()` calls cannot farm an unbounded BURNIE faucet: the bounty stays bounded by the BOUNTY_ETH_TARGET/ratio/knee math, and a no-work call yields no reward (NoWork()-style or zero).
    - The faucet-resistance corners (self-crank, repeated-call, split-work) hold on the game-resident router.
  </behavior>
  <action>
    Apply Δ3 to all ~19 `doWork` + 11 `afKing.` sites (→ `game.mintBurnie()`). Repoint the bounty-math comment +
    source-grep anchors (AfKing.sol:851/854/870/878/890) → GameAfkingModule.sol bounty constants
    (BOUNTY_ETH_TARGET/ADVANCE_RATIO_NUM/OPEN_KNEE ~:987/:995/:1003); if any `vm.readFile` references AfKing.sol,
    repoint to GameAfkingModule.sol. Preserve the faucet-resistance corner scenarios, reframed onto the
    `mintBurnie` bounty. RE-DERIVE any pinned slot via `forge inspect storage DegenerusGame`. Do NOT edit any contracts/ file.
  </action>
  <verify>
    <automated>forge test --match-contract "KeeperFaucetResistance" 2>&1 | tail -6 (compiles + runs; bounty-bounded asserted)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' test/fuzz/KeeperFaucetResistance.t.sol | grep -c "afKing\.\|doWork()"` == 0.
    - `grep -c "contracts/AfKing.sol" test/fuzz/KeeperFaucetResistance.t.sol` == 0.
    - The file compiles + runs under `forge test --match-contract KeeperFaucetResistance`.
    - A faucet-resistance assertion (bounded bounty / no-work-no-reward) runs non-vacuously against mintBurnie.
    - `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
  </acceptance_criteria>
  <done>KeeperFaucetResistance compiles + runs on mintBurnie with the bounty math repointed and the faucet-resistance corners reframed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only. Adapts 3 fuzz files. ZERO contract mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-351-03-FG | Tampering | the adapted Keeper* router/faucet fuzz tests | mitigate | False-GREEN risks: (1) a `vm.readFile` still pointing at the deleted AfKing.sol throws at runtime → mitigated by repointing AFKING_SRC to GameAfkingModule.sol + re-deriving grepped tokens; (2) a vacuous bounty/one-category assertion → mitigated by non-vacuity gates against the live mintBurnie structural early-return. ZERO `contracts/` mutation (`git diff 453f8073 HEAD -- contracts/` empty). |
</threat_model>

<verification>
- The 3 adapted files compile + run under `forge test --match-contract`.
- No surviving `afKing.`/`doWork()` call, no `contracts/AfKing.sol` source-grep target.
- `_settleGame` + the CoinflipStakeUpdated decode preserved in KeeperRewardRoutingSameResults.
- `git diff --name-only 453f8073 HEAD -- contracts/` is EMPTY.
</verification>

<success_criteria>
The `Keeper*` reward/router/faucet corpus is adapted to the game-resident `mintBurnie()` router with the
one-category-no-stack + faucet-resistance properties reframed and asserted, the two `vm.readFile` traps
repointed, and the differential `_settleGame`/topic-decode scaffolding preserved for reuse. ZERO
`contracts/*.sol` mutation.
</success_criteria>

<output>
Create `.planning/phases/351-tst-freeze-determinism-revert-free-ev-cap-two-path-set-mutat/351-03-SUMMARY.md` when done.
</output>
