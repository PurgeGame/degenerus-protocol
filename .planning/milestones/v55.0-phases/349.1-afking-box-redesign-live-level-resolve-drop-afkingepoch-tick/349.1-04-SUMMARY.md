---
phase: 349.1-afking-box-redesign-live-level-resolve-drop-afkingepoch-tick
plan: 04
subsystem: afking
tags: [solidity, interfaces, dead-code, advance-module, funding]

requires:
  - phase: 349.1-03
    provides: the new processSubscriberStage + resolveAfkingBox implementation signatures
provides:
  - "AdvanceModule STAGE caller passes only processDay (epochIndex + LR_INDEX read dropped)"
  - "IDegenerusGameModules decls matching the new processSubscriberStage / resolveAfkingBox signatures"
  - "P5 dead-code removed: batchPurchase + BatchBuy + AF_KING constant + onlyFlipCreditors AF_KING clause"
  - "subscribe msg.value routed to the resolved funding bucket (USER decision)"
affects: [349.1-05]

tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - contracts/modules/DegenerusGameAdvanceModule.sol
    - contracts/interfaces/IDegenerusGameModules.sol
    - contracts/DegenerusGame.sol
    - contracts/BurnieCoinflip.sol
    - contracts/ContractAddresses.sol
    - contracts/modules/DegenerusGameMintModule.sol
    - contracts/storage/DegenerusGameStorage.sol

key-decisions:
  - "USER (2026-05-31): subscribe msg.value credits the RESOLVED funding bucket (non-self fundingSource for operator-funded subs, else subscriber) so the deposit funds the bucket the draws debit — gated by the OPENE-04 approval, claimablePool tandem preserved."
  - "Purged ALL batchPurchase / AF_KING comment references too (not just code) to satisfy the zero-match invariant + remove dangling references to deleted code."

patterns-established: []

requirements-completed: []

duration: ~30min
completed: 2026-05-31
---

# Phase 349.1 Plan 04: Consumers + P5 Dead-Code Summary

**The AdvanceModule STAGE caller + the `IDegenerusGameModules` decls are updated to the new no-epoch signatures, the dead `batchPurchase`/`BatchBuy`/`AF_KING` surface is fully removed (zero matches across contracts/), and — per a USER decision — `subscribe`'s `msg.value` now credits the resolved funding bucket so an operator-funded deposit reaches the bucket the draws actually debit.**

## Performance
- **Duration:** ~30 min (incl. the msg.value USER decision)
- **Completed:** 2026-05-31
- **Tasks:** 3 (+ 1 USER decision)
- **Files modified:** 7

## Accomplishments
- **Task 1 — AdvanceModule STAGE caller:** dropped the `uint48 epochIndex = LR_INDEX` read; call site is `_runSubscriberStage(day)`; helper is `_runSubscriberStage(uint32 processDay)` encoding `(processDay, SUB_STAGE_BATCH)`. FREEZE-02b comment retuned to the pre-RNG / no-epoch model; the partial-drain (`subsFullyProcessed`) chunk logic + the pre-rngGate insertion point are unchanged.
- **Task 2 — interfaces:** `resolveAfkingBox(address, uint256, uint32, uint256, uint16)` and `processSubscriberStage(uint32, uint256)` decls byte-match the implementations.
- **Task 3 — P5 dead-code:** removed `BatchBuy` struct + `batchPurchase` function (DegenerusGame), the `AF_KING` clause in `onlyFlipCreditors` (BurnieCoinflip), and the `AF_KING` constant (ContractAddresses). The live `afkingFunding`/deposit/withdraw/snapshot ledger is untouched. **Also purged every stale `batchPurchase` / `AF_KING` comment** across MintModule, the interface, Game, and Storage (the zero-match invariant + no dangling references to deleted code).
- **USER decision — subscribe msg.value routing:** now credits the resolved draw bucket (non-self `fundingSource` for operator-funded subs, else subscriber). Updated `GameAfkingModule.subscribe` + the `DegenerusGame.subscribe` stub doc-comment; DESIGN §6/§8 updated.

## Task Commits
NOT committed — held batched diff. No per-task commits.

## Files Created/Modified
- `DegenerusGameAdvanceModule.sol` — STAGE caller + helper drop epochIndex.
- `IDegenerusGameModules.sol` — both decls updated; purchaseWith doc-comment de-staled.
- `DegenerusGame.sol` — batchPurchase + BatchBuy removed; stub-block + GAMEOVER comments de-staled; subscribe-stub doc-comment updated for msg.value routing.
- `BurnieCoinflip.sol` — onlyFlipCreditors AF_KING clause removed + doc-comment.
- `ContractAddresses.sol` — AF_KING constant removed.
- `DegenerusGameMintModule.sol` — purchaseWith doc-comments de-staled.
- `DegenerusGameStorage.sol` — afkingFunding doc-comment de-staled.

## Decisions Made
- **subscribe msg.value → resolved funding bucket (USER):** see key-decisions. The OPENE-04 gate runs before the credit, so a non-self fundingSource has already operator-approved the subscriber ⇒ no forced gift to a non-consenting funder; self-funded subs are byte-unchanged.

## Deviations from Plan

### Auto-fixed / Added
**1. [USER decision] subscribe msg.value funding-bucket routing**
- **Issue:** msg.value at subscribe credited the player's bucket, but operator-funded draws debit the funder's bucket → deposit misdirected (operator-unrecoverable) for operator-funded subs.
- **Fix:** route msg.value to the resolved funding bucket (the draw bucket).
- **Files:** GameAfkingModule.sol, DegenerusGame.sol (stub doc-comment).

**2. [Invariant completeness] Purged batchPurchase/AF_KING comment references**
- **Issue:** The plan's zero-match invariant covers ALL of contracts/ (incl. comments); deleting the code left ~8 stale comment mentions across 5 files.
- **Fix:** reworded every comment reference; `grep -rn "...batchPurchase...AF_KING" contracts/` is now zero.

---
**Total deviations:** 1 USER decision + 1 invariant-completeness cleanup. No scope creep.

## Issues Encountered
None.

## Next Phase Readiness
- All consumers + dead-code done. Plan 05 lands the `doWork` → `mintBurnie` rename (interface + module + Game stub + the AdvanceModule comment) and runs the contracts-only `forge build --sizes` gate, then HOLDS for USER hand-review.

---
*Phase: 349.1-afking-box-redesign*
*Completed: 2026-05-31*
