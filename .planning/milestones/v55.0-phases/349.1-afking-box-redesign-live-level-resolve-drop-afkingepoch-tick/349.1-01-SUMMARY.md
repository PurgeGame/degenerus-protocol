---
phase: 349.1-afking-box-redesign-live-level-resolve-drop-afkingepoch-tick
plan: 01
subsystem: storage
tags: [solidity, afking, storage-layout, freeze]

requires:
  - phase: 349 (held IMPL diff)
    provides: the applied-but-uncommitted Sub struct + _afkingEpoch machinery this revises
provides:
  - "Sub.lastOpenedDay (uint32) day-keyed open marker, replacing lastOpenedIndex (uint48)"
  - "deletion of _afkingEpoch map + _packAfkingEpoch + _unpackAfkingEpoch + AFKING_EPOCH_BASE_LEVEL_SHIFT"
  - "the final storage layout every later 349.1 plan edits against"
affects: [349.1-02, 349.1-03, 349.1-04]

tech-stack:
  added: []
  patterns: ["day-keyed open marker (lastOpenedDay vs lastAutoBoughtDay) instead of monotonic index"]

key-files:
  created: []
  modified: [contracts/storage/DegenerusGameStorage.sol]

key-decisions:
  - "Kept the frozen wei `amount` (uint96) stamp — the SPEND must stay frozen for the box seed even though level went live (349.1-DESIGN §6 P1)."
  - "Sub now packs ≈232/256 bits (markers 80b→64b after the uint48→uint32 marker rename)."

patterns-established:
  - "No-epoch box model: only scorePlus1 + amount + lastAutoBoughtDay are frozen at stamp; level/word read live or day-keyed at open."

requirements-completed: []

duration: ~6min
completed: 2026-05-31
---

# Phase 349.1 Plan 01: Storage Foundation Summary

**`Sub.lastOpenedIndex (uint48)` → `lastOpenedDay (uint32)` day-keyed marker, and the entire `_afkingEpoch` per-day epoch machinery (map + pack/unpack + shift constant) deleted — the no-epoch storage foundation the box redesign keys on.**

## Performance
- **Duration:** ~6 min
- **Completed:** 2026-05-31
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- Renamed the open marker to `uint32 lastOpenedDay`; rewrote both marker doc-comments to the day-keyed `lastOpenedDay < lastAutoBoughtDay` predicate (used by the open leg AND the no-orphan guard).
- Rewrote the Sub layout doc-comment to the no-epoch model: only `scorePlus1` + `amount` frozen at stamp; level + EV-cap key read LIVE at open; word from `rngWordByDay[lastAutoBoughtDay]`.
- Deleted `_afkingEpoch` (map + doc), the "Afking per-day epoch pack/unpack" section (`AFKING_EPOCH_BASE_LEVEL_SHIFT`, `_packAfkingEpoch`, `_unpackAfkingEpoch`).
- Left `rngWordByDay`, `_subOf`, `_fundingSourceOf`, `_subscribers`, `_subscriberIndex`, `_subCursor`, `_subOpenCursor` untouched.

## Task Commits

NOT committed — this phase is ONE batched `contracts/*.sol` diff superseding-in-place the held 349 diff, applied to the working tree and HELD UNCOMMITTED at the contract-commit gate (USER hand-review at plan 05). No per-task commits.

## Files Created/Modified
- `contracts/storage/DegenerusGameStorage.sol` — Sub.lastOpenedDay marker + full _afkingEpoch deletion.

## Decisions Made
None beyond the plan — followed §1/§4/§7 as specified.

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
None.

## Next Phase Readiness
- Storage layout is final. Plan 02 (STAGE) and plan 03 (open leg) now edit against `lastOpenedDay` + no epoch.
- Expected: the tree does NOT `forge build` cleanly yet — GameAfkingModule still references `_afkingEpoch` / `lastOpenedIndex` until plans 02/03. Built once at the end (plan 05).

---
*Phase: 349.1-afking-box-redesign*
*Completed: 2026-05-31*
