---
phase: 349.1-afking-box-redesign-live-level-resolve-drop-afkingepoch-tick
plan: 05
subsystem: afking
tags: [solidity, rename, build-gate, contract-hold]

requires:
  - phase: 349.1-04
    provides: the consumer + dead-code edits; the diff now needs only the rename + build
provides:
  - "doWork → mintBurnie rename across interface + module + Game stub (selector) + AdvanceModule comment"
  - "contracts-only forge build clean; DegenerusGame 22,925 B < 24,576"
  - "the full revised 349.1 batched contracts/*.sol diff applied + HELD at the contract-commit gate"
affects: [349.1 USER review, 350, 351]

tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - contracts/interfaces/IDegenerusGameModules.sol
    - contracts/modules/GameAfkingModule.sol
    - contracts/DegenerusGame.sol
    - contracts/modules/DegenerusGameAdvanceModule.sol

key-decisions:
  - "Used replace_all doWork→mintBurnie per file (atomic across code + prose); verified zero false-positive substrings via the pre-rename grep."

patterns-established: []

requirements-completed: []

duration: ~15min
completed: 2026-05-31
---

# Phase 349.1 Plan 05: mintBurnie Rename + Build Gate + HOLD Summary

**The permissionless router is renamed `doWork` → `mintBurnie` atomically across the interface decl, the module impl, the Game dispatch stub (selector included), and the AdvanceModule comment; the contracts-only `forge build` is clean with DegenerusGame at 22,925 B (well under the 24,576 ceiling and below the held-349 baseline of 23,499); the full revised 349.1 batched diff is applied and HELD at the contract-commit gate.**

## Performance
- **Duration:** ~15 min
- **Completed:** 2026-05-31
- **Tasks:** 3 (rename, build, HOLD)
- **Files modified:** 4 (rename) — full diff spans 12 contract files + 1 new + 1 deleted

## Accomplishments
- **Task 1 — rename:** `doWork` → `mintBurnie` everywhere in contracts/ (15 occurrences across 4 files: code + all prose). `grep "doWork" contracts/` = 0; `grep "function mintBurnie()" contracts/` = 3 (interface, module, Game stub); the Game stub encodes `IGameAfkingModule.mintBurnie.selector`. `subscribe` / `autoBuy` / human-box `autoOpen(uint256)` untouched.
- **Task 2 — build gate:** `forge build --sizes --skip 'test/**' --skip '*.t.sol'` exits 0, **zero compiler errors** (only pre-existing unsafe-typecast / divide-before-multiply lint warnings). **DegenerusGame runtime = 22,925 B (margin 1,651)** — under 24,576 and below the held-349 baseline of 23,499 (the `_afkingEpoch`/pack/unpack + P5 deletions netted ~574 B back). GameAfkingModule (7,246), LootboxModule (18,686), AdvanceModule (19,168) all compile; no contract over EIP-170.
- **Task 3 — HOLD:** the full revised single batched `contracts/*.sol` diff is applied + locally compiling, HELD UNCOMMITTED at the contract-commit gate for USER hand-review. **Nothing committed.**

## Task Commits
NOT committed — `autonomous: false` USER contract-commit gate. The whole 349.1 diff (superseding-in-place the held 349 diff) is HELD UNCOMMITTED for explicit USER hand-review; the commit happens only on explicit USER instruction.

## Files Created/Modified (rename only)
- `IDegenerusGameModules.sol`, `GameAfkingModule.sol`, `DegenerusGame.sol`, `DegenerusGameAdvanceModule.sol` — doWork→mintBurnie.

## Decisions Made
- Atomic per-file `replace_all` for the rename (verified safe — the pre-rename grep showed all 15 occurrences are the router name, no substring false positives).

## Deviations from Plan
None for plan 05. (Phase-level USER additions folded earlier: the 500-sub cap [plan 02] and the subscribe msg.value → resolved funding bucket [plan 04]; both recorded in DESIGN §6/§8.)

## Issues Encountered
None — the first full compile of the batched diff was clean.

## Next Phase Readiness
- **HELD at the contract-commit gate.** Awaiting USER hand-review of `git diff -- contracts/`. On "approved", the ONE batched contract commit is made on explicit instruction.
- `forge test` is NOT run/required here (stale `AfKing.sol`-import + `_afkingEpoch`/ABI test reds expected until the Phase 351 test sweep).
- Phase 350 (GAS) + 351 (TST) + 352 (TERMINAL, incl. the 3-skill sweep re-attesting the freeze + the two USER additions) follow.

---
*Phase: 349.1-afking-box-redesign*
*Completed: 2026-05-31*
