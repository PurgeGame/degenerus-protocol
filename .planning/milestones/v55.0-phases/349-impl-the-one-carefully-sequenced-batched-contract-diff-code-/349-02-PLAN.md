---
phase: 349-impl-the-one-carefully-sequenced-batched-contract-diff-code-
plan: 02
type: execute
wave: 2
depends_on: ["349-01"]
files_modified:
  - contracts/storage/DegenerusGameStorage.sol
autonomous: false
requirements: [ARCH-01]
must_haves:
  truths:
    - "The subscriber set is appended to DegenerusGameStorage.sol — mapping(address => Sub) _subOf, address[] _subscribers, mapping(address => uint256) _subscriberIndex — layout-safe APPEND (never a reorder); every module that inherits the base shares it in-context"
    - "The Sub record carries the 5-FIELD box stamp (index uint48, amount, day uint32, scorePlus1 uint16, baseLevelPlus1 uint24) per D-348-07 — score+baseLevel are STAMPED-FROZEN, NOT the superseded 3-field (index, amount, day) shape; the 40 D-348-07 bits drop into the slot-2 word's spare bits (same single SSTORE, still 2-slot-feasible)"
    - "The Sub record carries the lastAutoBoughtDay (uint32) success-marker + lastOpenedIndex (uint48) monotonic no-double-open marker"
    - "The process/open cursors are appended — _subCursor (process-STAGE cursor) + the open-leg router-category cursor — and subsFullyProcessed (bool) is DECLARED (CONFIRMED-NEW: it does not exist in source; 349 authors it)"
    - "The v54 afkingFunding ledger (Storage:410) is REUSED, NOT re-declared — the afking total rides inside the existing claimablePool; NO new aggregate is introduced (the omittable-pool class stays structurally impossible, SOLVENCY-01)"
    - "The storage invariant comment (Storage:348) continues to name the afking component (claimablePool == sum claimableWinnings + sum afkingFunding); the claimablePool decl is UNCHANGED"
  artifacts:
    - path: "contracts/storage/DegenerusGameStorage.sol"
      provides: "the relocated subscriber set + cursors + subsFullyProcessed + the 5-field Sub stamp + markers; afkingFunding reused"
      contains: "subsFullyProcessed"
  key_links:
    - from: "GameAfkingModule + AdvanceModule STAGE (349-03/04/05)"
      to: "DegenerusGameStorage _subOf/_subscribers/_subscriberIndex/_subCursor/subsFullyProcessed + the Sub stamp"
      via: "shared inherited base (in-context SLOADs, no cross-contract staticcall)"
      pattern: "mapping\\(address => Sub\\) internal _subOf"
    - from: "the afking process-pass debit (349-03)"
      to: "afkingFunding (Storage:410, reused) inside claimablePool"
      via: "no new aggregate — the v54 ledger carried forward"
      pattern: "afkingFunding"
---

<objective>
Step 2 of the producer-before-consumer edit-order map (348-IMPL-EDIT-ORDER-MAP.md Section 3 Step 2):
land the STORAGE PRODUCER — append the relocated AfKing subscriber set, the process/open cursors, the
`subsFullyProcessed` chunk gate, the per-sub 5-field box stamp + markers into `DegenerusGameStorage.sol`,
and confirm the v54 `afkingFunding` ledger is reused (no new aggregate). This state must exist before any
logic reads/writes it: the `GameAfkingModule` (349-03/04) and the AdvanceModule STAGE (349-05) all consume it.

The 5-field stamp `(index, amount, day, scorePlus1, baseLevelPlus1)` is the D-348-07 shape — score+baseLevel
are STAMPED-FROZEN (this SUPERSEDES the earlier 3-field framing; the open reads score/baseLevel FROM the stamp,
not live). This is the producer-before-consumer state shape the whole fold rides on.

Purpose: ARCH-01 (state game-resident). Relocating the subscriber set into the shared inherited base is what
collapses v54's cross-contract `afkingSnapshot`/`afkingFundingOf` staticcalls into in-context SLOADs — the
structural saving the GAS phase (350) later confirms. Storage adds ~0 B Game runtime (slots, not bytecode).

Output: Edits applied to `contracts/storage/DegenerusGameStorage.sol` (part of the single batched 349 diff).
NO COMMIT — held at the contract-commit boundary.

⚠ CONTRACT-BOUNDARY HARD STOP. `autonomous: false`. The executor applies the edits but NEVER runs `git commit`
/ `git add contracts/` (the only action needing approval is committing `contracts/*.sol`). The orchestrator owns
all commits, deferred past the single user-approval gate (349-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-IMPL-EDIT-ORDER-MAP.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-INVARIANT-CARRY.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-FREEZE-PROOF.md
@.planning/phases/349-impl-the-one-carefully-sequenced-batched-contract-diff-code-/349-01-SUMMARY.md

<interfaces>
<!-- Current shapes. Inherit 349-01 Task 1's re-pinned anchors; SPEC-attested refs @ 20ca1f79 below. -->

The subscriber set to RELOCATE (currently in AfKing.sol — these move INTO DegenerusGameStorage):
- AfKing.sol `:100` — `struct Sub { ... }` (today carries `validThroughLevel` `:103`, `fundingSource` `:106`, `lastAutoBoughtDay` `:102`)
- AfKing.sol `:210` — `mapping(address => Sub) private _subOf;` (the TEMPLATE — becomes `internal` on the base)
- AfKing.sol `:214` — `address[] private _subscribers;`
- AfKing.sol `:218` — `mapping(address => uint256) private _subscriberIndex;`

The v54 afkingFunding ledger — REUSE, do NOT re-declare (348-IMPL-EDIT-ORDER-MAP §1.1):
- DegenerusGameStorage.sol `:410` — `mapping(address => uint256) internal afkingFunding;` (ALREADY shipped at 20ca1f79)
- DegenerusGameStorage.sol `:348` — the invariant comment (claimablePool == sum claimableWinnings + sum afkingFunding)
- DegenerusGameStorage.sol `:355` — `uint128 internal claimablePool;` (UNCHANGED)
- `afkingFundingOf` external view `DegenerusGame.sol:1540` (the Game-side accessor confirming the ledger is present)

The 5-field stamp (D-348-07; 348-IMPL-EDIT-ORDER-MAP §1.1 table + 348-INVARIANT-CARRY §3-ii):
- index uint48 — the pre-RNG LR_INDEX at process (FREEZE-02; openLootBox takes uint48 LB:503; _finalizeLootboxRng uint48 LB:1230)
- amount full wei (own slot, or packed with a purchaseLevel-style headroom field a la lootboxEth LB:505-509) — the stamped spend; boons OFF ⇒ amount = spend; feeds the abi.encode seed at full 32-byte fidelity
- day uint32 — the boundary-pinned process day (mirrors lootboxDay LB:514); seeds the open (FREEZE-03)
- scorePlus1 uint16 — FROZEN activityScore+1 (D-348-07); mirrors the human deposit-freeze scorePlus1 in lootboxPurchasePacked LB:529-530
- baseLevelPlus1 uint24 — FROZEN baseLevel+1 (D-348-07); the target-level roll floor; mirrors the human baseLevelPlus1 LB:530
- lastAutoBoughtDay uint32 — success-marker (set AFTER a successful afkingFunding debit)
- lastOpenedIndex uint48 — monotonic no-double-open guard (open only materializes indices strictly past it)

2-slot Sub append: one full slot for `amount` + one slot packing index(48)+day(32)+scorePlus1(16)+baseLevelPlus1(24)+lastAutoBoughtDay(32)+lastOpenedIndex(48) = 200 bits < 256. The afking stamp does NOT carry `adj` (the open passes full amount to _applyEvMultiplierWithCap).

subsFullyProcessed is CONFIRMED-NEW (grep returns 0; 349 authors it). Pre-launch redeploy-fresh ⇒ a storage break is fine (no migration); the exact packing is a 349 layout decision.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append the subscriber set + cursors + subsFullyProcessed to DegenerusGameStorage.sol</name>
  <files>contracts/storage/DegenerusGameStorage.sol</files>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol (the append region at the end of the declared state; the afkingFunding mapping :410 + invariant comment :348 + claimablePool :355 to confirm present/unchanged)
    - contracts/AfKing.sol (the set to relocate — _subOf :210, _subscribers :214, _subscriberIndex :218, all `private` today → become `internal` on the base)
    - 348-IMPL-EDIT-ORDER-MAP.md §1.1 (the appended members + the cursors) + §3 Step 2a (layout-safe append; REUSE afkingFunding, NO new aggregate)
    - 348-FREEZE-PROOF.md FREEZE-02 (c) (subsFullyProcessed is the no-interleave chunk gate — CONFIRMED-NEW)
  </read_first>
  <action>
    APPEND (never reorder) to `contracts/storage/DegenerusGameStorage.sol` the relocated subscriber set and the
    chunked-drain bookkeeping (mirroring the AfKing.sol declarations, but `internal` so every inheriting module
    shares them): `mapping(address => Sub) internal _subOf;`, `address[] internal _subscribers;`,
    `mapping(address => uint256) internal _subscriberIndex;` (the iterable swap-pop tombstone set — the
    H-CANCEL-SWAP-MISS class membership-to-packed-index bookkeeping); the process-STAGE cursor `uint256 internal
    _subCursor;`; the open-leg router-category cursor (its own cursor variable, name at the executor's discretion —
    the post-RNG OPEN_BATCH-style drain cursor); and `bool internal subsFullyProcessed;` (the FREEZE-02 chunk gate
    + no-interleave guard — CONFIRMED-NEW; 349 authors it). The `Sub` struct DEFINITION moves with this append
    (Task 2 extends it to 5 stamp fields). Confirm the v54 `afkingFunding` mapping (`:410`) is PRESENT and REUSE
    it — do NOT re-declare it and do NOT add any new aggregate (`afkingFundingPool` or similar): the systemwide
    afking total rides inside the existing `claimablePool` (`:355`, unchanged), per SOLVENCY-01. Leave the invariant
    comment (`:348`) naming the afking component intact. All comments describe current state only (no change-history
    prose).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && S=contracts/storage/DegenerusGameStorage.sol; grep -c "mapping(address => Sub) internal _subOf;" $S | grep -qx 1 && grep -c "address\[\] internal _subscribers;" $S | grep -qx 1 && grep -c "mapping(address => uint256) internal _subscriberIndex;" $S | grep -qx 1 && grep -c "bool internal subsFullyProcessed;" $S | grep -qx 1 && grep -c "uint256 internal _subCursor;" $S | grep -qx 1 && grep -v '^[[:space:]]*//' $S | grep -c "afkingFundingPool" | grep -qx 0 && echo "SET+CURSORS+GATE-APPENDED NO-AGGREGATE"</automated>
  </verify>
  <acceptance_criteria>
    - `_subOf` / `_subscribers` / `_subscriberIndex` declared once each as `internal` (relocated from AfKing, layout-safe append)
    - `subsFullyProcessed` (bool, internal) and `_subCursor` (uint256, internal) + the open-leg cursor declared (subsFullyProcessed CONFIRMED-NEW)
    - NO `afkingFundingPool` or any new aggregate (grep on non-comment lines returns 0); `afkingFunding` (`:410`) reused, `claimablePool` decl unchanged
    - The Sub struct definition is present on the base (Task 2 extends it); the invariant comment still names the afking component
  </acceptance_criteria>
  <done>The subscriber set, the process/open cursors, and `subsFullyProcessed` are appended to `DegenerusGameStorage.sol` as `internal`; no new aggregate is introduced; `afkingFunding` is reused and `claimablePool` is unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend the Sub record with the 5-field stamp (D-348-07) + the lastAutoBoughtDay/lastOpenedIndex markers, 2-slot-feasible</name>
  <files>contracts/storage/DegenerusGameStorage.sol</files>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol (the relocated Sub struct from Task 1)
    - contracts/AfKing.sol (today's Sub fields: validThroughLevel :103, fundingSource :106, lastAutoBoughtDay :102 — these carry over)
    - 348-IMPL-EDIT-ORDER-MAP.md §1.1 (the stamp field table + the 2-slot append note) + §4.4 (the 5-field stamp is D-348-07; the open reads score/baseLevel FROM the stamp)
    - 348-INVARIANT-CARRY.md §3-ii (stamp field widths: amount full wei; index uint48; day uint32; scorePlus1 uint16; baseLevelPlus1 uint24; NO adj; 2-slot-feasible — 40 D-348-07 bits into slot 2's spare)
    - 348-FREEZE-PROOF.md FREEZE-01 (a) (the 5 stamped fields are the frozen seed-AND-multiplier-input set; mirrors human lootboxPurchasePacked LB:529-530)
  </read_first>
  <action>
    Extend the relocated `Sub` struct (in `DegenerusGameStorage.sol`) with the D-348-07 5-FIELD box stamp + the
    two markers, preserving the carried-over `validThroughLevel` (CONSENT-01 pass-gating) and `fundingSource`
    (CONSENT-01 funder=src) fields: `uint48 index` (the pre-RNG LR_INDEX), `amount` at full wei fidelity (its own
    slot, or packed with a `purchaseLevel`-style headroom field à la `lootboxEth` `LB:505-509` — needed at 32-byte
    fidelity to feed the `abi.encode` seed), `uint32 day` (the boundary-pinned process day), `uint16 scorePlus1`
    (the FROZEN activityScore+1 — D-348-07), `uint24 baseLevelPlus1` (the FROZEN baseLevel+1 — D-348-07),
    `uint32 lastAutoBoughtDay` (the success-marker), `uint48 lastOpenedIndex` (the monotonic no-double-open marker).
    This is the 5-field shape `(index, amount, day, scorePlus1, baseLevelPlus1)` — it SUPERSEDES the earlier 3-field
    `(index, amount, day)` framing (D-348-07): score+baseLevel are stamped-frozen at process and read from the stamp
    at open, NOT live (the analog of the human deposit-time freeze of `scorePlus1`/`baseLevelPlus1` in
    `lootboxPurchasePacked`, `LB:529-530`). Do NOT carry an `adj` field (the afking open passes the full stamped
    `amount` to `_applyEvMultiplierWithCap`). Keep the layout 2-slot-feasible: one full slot for `amount` + one slot
    packing index(48)+day(32)+scorePlus1(16)+baseLevelPlus1(24)+lastAutoBoughtDay(32)+lastOpenedIndex(48) = 200 bits
    < 256 (the 40 D-348-07 bits drop into the slot-2 word's spare bits — same single SSTORE, no third slot). The
    exact packing is the executor's layout decision (pre-launch redeploy-fresh ⇒ storage break fine). Struct-field
    comments describe current state only. ⚠ Use the 5-field shape, NOT the superseded 3-field shape.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && S=contracts/storage/DegenerusGameStorage.sol; awk '/struct Sub/{f=1} f{print} f&&/}/{exit}' $S > /tmp/sub_struct.txt; grep -qE "uint16[[:space:]]+scorePlus1" /tmp/sub_struct.txt && grep -qE "uint24[[:space:]]+baseLevelPlus1" /tmp/sub_struct.txt && grep -qE "uint48[[:space:]]+index" /tmp/sub_struct.txt && grep -qE "uint48[[:space:]]+lastOpenedIndex" /tmp/sub_struct.txt && grep -qE "uint32[[:space:]]+lastAutoBoughtDay" /tmp/sub_struct.txt && grep -qiv "uint64[[:space:]]\+adj" /tmp/sub_struct.txt && echo "5-FIELD-STAMP NO-ADJ"</automated>
  </verify>
  <acceptance_criteria>
    - The Sub struct carries `scorePlus1` (uint16) AND `baseLevelPlus1` (uint24) — the D-348-07 stamped-frozen fields (5-field shape, NOT the superseded 3-field)
    - The Sub struct carries `index` (uint48), `amount` (full-wei field/packing), `day` (uint32), `lastAutoBoughtDay` (uint32), `lastOpenedIndex` (uint48)
    - NO `adj` field is added to the afking stamp
    - The carried-over `validThroughLevel` + `fundingSource` fields are preserved on the struct; the layout is 2-slot-feasible
  </acceptance_criteria>
  <done>The Sub record carries the D-348-07 5-field stamp (index, amount, day, scorePlus1, baseLevelPlus1) + the lastAutoBoughtDay/lastOpenedIndex markers, no `adj`, 2-slot-feasible, with validThroughLevel/fundingSource preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| afking process/open logic → game-resident state | The set + cursors + stamp must exist on the shared base before any module reads/writes them (producer-before-consumer). |
| afking total → claimablePool solvency | The afking ETH liability rides inside `claimablePool`; a stray new aggregate could be omitted from a reservation site → the omittable-pool / under-reservation class. |
| stamp completeness → FREEZE-01 | The 5-field stamp must capture the full seed-AND-multiplier-input set; a missing field (e.g. reverting to 3 fields) would leave score/baseLevel live, re-opening FREEZE-01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-349-02-AGG | Information Disclosure / Solvency | a stray new afking aggregate | mitigate | D-348/344 carry: NO new aggregate; the afking total rides inside `claimablePool` (every reservation site already reserves it, SOLVENCY-01). Task 1 verify asserts no `afkingFundingPool` — the omittable-pool class stays structurally impossible. |
| T-349-02-FRZ | Spoofing (outcome steer) | the Sub stamp shape | mitigate | The 5-field stamp (D-348-07) freezes score+baseLevel; Task 2 verify asserts `scorePlus1`+`baseLevelPlus1` are present (NOT the superseded 3-field shape) — the open reads them FROM the stamp, not live, so FREEZE-01 holds for those fields. |
| T-349-02-LAYOUT | Tampering (slot collision) | the layout-safe append | mitigate | Append-only (never a reorder); pre-launch redeploy-fresh ⇒ a storage break is benign. The 2-slot Sub extension is sized (200 bits < 256) so the markers + stamp share one SSTORE. |
| T-349-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs (Solidity edits only). |
</threat_model>

<verification>
- The subscriber set + cursors + `subsFullyProcessed` are appended `internal` (layout-safe; subsFullyProcessed CONFIRMED-NEW)
- The Sub record carries the D-348-07 5-field stamp + the two markers, no `adj`, 2-slot-feasible
- No new aggregate introduced; `afkingFunding` reused; `claimablePool` decl + invariant comment intact
- NO `git commit` / `git add contracts/` was run by the executor (orchestrator commits post-approval)
</verification>

<success_criteria>
- The game-resident subscriber set + cursors + `subsFullyProcessed` + the 5-field Sub stamp + markers are landed (ARCH-01)
- The v54 `afkingFunding` ledger is reused with NO new aggregate (SOLVENCY-01 preserved)
- The stamp is the D-348-07 5-field shape (score+baseLevel stamped-frozen), not the superseded 3-field shape
- Edits held at the contract-commit boundary (no executor commit)
</success_criteria>

<output>
Create `.planning/phases/349-impl-the-one-carefully-sequenced-batched-contract-diff-code-/349-02-SUMMARY.md` when done.
</output>
