---
phase: 350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam
plan: 03
type: execute
wave: 3
depends_on: [350-02]
files_modified:
  - .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-OUTCOME.md
  # CONTINGENT (Outcome B only — if 350-02 APPROVED a GAS-03 win). Not touched under Outcome A:
  - contracts/modules/GameAfkingModule.sol
autonomous: false
requirements: [GAS-03]

must_haves:
  truths:
    - "The phase outcome is recorded in 350-OUTCOME.md, driven by the W3 branch directive in 350-02's verdict doc"
    - "Outcome A (expected): GAS-01/02 confirmed-structural + GAS-03 REJECTED/NEGATIVE → NO contracts/*.sol diff, NO commit gate, recorded as a clean autonomous close (ROADMAP SC4's explicit no-diff branch)"
    - "Outcome B (contingency): if 350-02 APPROVED a GAS-03 win → the single penny-exact claimablePool same-slot-flush diff is authored in GameAfkingModule.processSubscriberStage (memory accumulator, flush once before the _subCursor persist at :765), forge build (contracts-only) clean, DegenerusGame < 24,576, HELD at the contract-commit boundary"
    - "Under Outcome B the flush preserves the IDENTICAL net claimablePool delta as the per-slice path (penny-exact); the checked uint128 subtraction is NOT wrapped in unchecked (fail-loud preserved, class B)"
    - "Under Outcome B quests.* / purchaseWith / afkingFunding[src] per-key debit / the swap-pop set-mutations / the no-orphan guard / the freeze fields are ALL UNTOUCHED"
    - "Under EITHER outcome no contract is committed without explicit USER hand-review (the ONLY action needing approval is committing contracts/*.sol)"
  artifacts:
    - path: ".planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-OUTCOME.md"
      provides: "The recorded phase outcome (A: no-diff close, or B: held diff + penny-exact proof) per the 350-02 branch directive"
      contains: "Outcome"
  key_links:
    - from: "350-OUTCOME.md"
      to: "350-GAS-SKEPTIC-VERDICTS.md"
      via: "executes the W3 branch directive (Outcome A vs B)"
      pattern: "Outcome"
    - from: "(Outcome B only) GameAfkingModule.sol flush"
      to: "claimablePool slot 1"
      via: "accumulate-and-flush once before _subCursor persist"
      pattern: "claimablePool"
---

<objective>
Execute the phase outcome dictated by plan 350-02's W3 branch directive. This plan has TWO first-class branches and does NOT assume a contract diff is produced.

- Outcome A (EXPECTED): GAS-01/GAS-02 are confirmed-structural and GAS-03 is REJECTED/NEGATIVE. Record the all-confirmed/NEGATIVE outcome in `350-OUTCOME.md`. NO `contracts/*.sol` diff, NO commit gate — a clean autonomous close satisfying ROADMAP Success Criterion 4's explicit "if the phase produces no net contract change ... that is recorded as the outcome and no diff is gated" branch.
- Outcome B (CONTINGENCY): ONLY if plan 350-02 APPROVED a GAS-03 win — author the single penny-exact `claimablePool` same-slot-flush diff in `GameAfkingModule.processSubscriberStage`, `forge build` (contracts-only) clean (DegenerusGame < 24,576), HELD at the contract-commit boundary for explicit USER hand-review.

Purpose: Phase 350 is the SECOND of v55.0's two contract-boundary hard stops, but the realistic outcome is no net change (the structural wins were already banked by 349/349.1; GAS-03 is expected REJECT on the warm-write evidence). This plan makes both outcomes first-class so the executor records the verdict-driven result without pre-committing to a diff.

Output: `350-OUTCOME.md` (always). Under Outcome B only: a held diff to `GameAfkingModule.sol` + a penny-exact proof section.

This plan is `autonomous: false` BECAUSE Outcome B can touch `contracts/*.sol`. Per the project rule, the ONLY action needing approval is committing `contracts/*.sol` — applying the diff + `forge build` + holding at the gate runs hands-off; the COMMIT is the gate. Under Outcome A there is no diff and the close is clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-GAS-SKEPTIC-VERDICTS.md
@.planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-RESEARCH.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-GAS-INVENTORY.md
@.planning/REQUIREMENTS.md

# 350-GAS-SKEPTIC-VERDICTS.md (plan 350-02 output) carries the W3 branch directive — READ it
# FIRST to determine Outcome A vs B. 350-RESEARCH.md §"Penny-Exact Solvency Check For The Flush"
# is the source-of-truth for the Outcome-B proof obligations; §"Where a once-per-batch flush
# WOULD go" describes the exact flush shape. §"Realistic Outcome" documents the SC4 no-diff branch.

<interfaces>
<!-- The Outcome-B candidate site (only relevant if 350-02 APPROVED GAS-03). Executor uses these
     directly — no codebase exploration needed. From contracts/modules/GameAfkingModule.sol (77c3d9ef). -->

processSubscriberStage(uint32 processDay, uint256 maxCount) returns (uint256 processed)
  - the STAGE loop spans :550-762; the cursor is persisted at :765 (`_subCursor = uint16(cursor)`)
  - the per-funded-buy debit is at :708-711 — two writes guarded by `if (ethValue != 0)`:
      :709  afkingFunding[src] -= ethValue          (per-key (src); NOT batchable — per-account underflow guard / SOLVENCY-01 precondition)
      :710  claimablePool      -= uint128(ethValue) (single uint128 slot 1, packed w/ currentPrizePool per Storage :365; WARM after iter 1; the ONLY GAS-03 candidate slot)
  - floor-protected (UNTOUCHED under Outcome B): the no-orphan guard :570; the cancel-tombstone
    reclaim :586-593; the pass-evict swap-pop :621-622; the funding skip-kill swap-pop :675-676;
    the stamp writes :747-748 + :756; the ticket-mode purchaseWith delegatecall :718-730
  - the only other claimablePool touches in the module are subscribe :282 (+=) and AdvanceModule
    :958 (+=) — both OUTSIDE this loop, so no mid-loop read of claimablePool exists (the identity
    that makes accumulate-and-flush byte-identical to the per-slice path)

SOLVENCY invariant (DegenerusGameStorage.sol :358):
  claimablePool == sum(claimableWinnings) + sum(afkingFunding)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read the 350-02 branch directive and record the phase outcome</name>
  <files>.planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-OUTCOME.md</files>
  <read_first>
    - .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-GAS-SKEPTIC-VERDICTS.md (the W3 branch directive + the GAS-03 verdict — READ FIRST to determine the branch)
    - .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-RE-PIN-AND-CONFIRM.md (the GAS-01/02 confirm-structural evidence + the re-pin table)
    - .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-RESEARCH.md (§"Realistic Outcome + The No Net Change Branch" — the SC4 no-diff record format)
    - .planning/ROADMAP.md (Phase 350 Success Criterion 4 — the explicit "no diff is gated" branch)
  </read_first>
  <action>
    Read the W3 branch directive in `350-GAS-SKEPTIC-VERDICTS.md`. Author `350-OUTCOME.md` recording the phase outcome.

    If the directive is OUTCOME A (the expected path — GAS-03 REJECTED, all else CONFIRMED-STRUCTURAL/DISSOLVED): record the all-confirmed/NEGATIVE result. State: GAS-01 = CONFIRMED-STRUCTURAL (box-ledger → warm Sub-stamp, delivered `77c3d9ef`, measured at 351 TST-06); GAS-02 = CONFIRMED-STRUCTURAL (staticcall → in-context SLOAD, delivered `77c3d9ef`); GAS-03 = REJECTED (the warm `claimablePool` SSTORE ~100 gas x (N−1) marginal + the off-hot-path affiliate/pool/quests finding + the mixed-chunk hazard + the SOLVENCY-01 audit-surface cost, under the security-over-gas floor; v49 REJECT-with-reasoning precedent). Conclude: NO `contracts/*.sol` diff this phase, NO commit gate, the phase closes on the documented verdict per ROADMAP Phase 350 SC4's no-diff branch. Assert `git diff --name-only -- contracts/` is EMPTY. Then STOP — Task 2 does NOT run under Outcome A; mark it skipped in the SUMMARY.

    If the directive is OUTCOME B (a GAS-03 win was APPROVED with the penny-exact obligations): record that Outcome B is in effect, restate the APPROVED win + its four penny-exact obligations from `350-02`, and DEFER the diff authoring + proof to Task 2. Do NOT touch any contract in this task.

    NO fenced code blocks of implementations. WRITE `350-OUTCOME.md`; under Outcome A this task READS contracts only (no edits).
  </action>
  <verify>
    <automated>test -f .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-OUTCOME.md && grep -qiE "Outcome A|Outcome B" .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-OUTCOME.md && grep -qE "GAS-01|GAS-02|GAS-03" .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-OUTCOME.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `350-OUTCOME.md` exists and names the executed branch (Outcome A or Outcome B), matching the W3 directive in `350-GAS-SKEPTIC-VERDICTS.md`.
    - Under Outcome A: the doc records GAS-01 = CONFIRMED-STRUCTURAL, GAS-02 = CONFIRMED-STRUCTURAL, GAS-03 = REJECTED (with the warm-write + off-hot-path + mixed-chunk + solvency-surface reasoning), states "no contracts/*.sol diff, no commit gate" citing ROADMAP SC4's no-diff branch, and `git diff --name-only -- contracts/` is EMPTY.
    - Under Outcome B: the doc records Outcome B in effect, restates the APPROVED GAS-03 win + its four penny-exact obligations, and defers the diff to Task 2 (no contract touched in Task 1).
    - No fenced code block of a contract implementation in `350-OUTCOME.md`.
  </acceptance_criteria>
  <done>
    `350-OUTCOME.md` records the verdict-driven branch — under Outcome A a clean no-diff close (SC4) with contracts untouched, or under Outcome B the Outcome-B setup deferring the diff to Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: (OUTCOME B ONLY) Author the penny-exact claimablePool same-slot-flush diff + proof; hold at the contract gate</name>
  <files>contracts/modules/GameAfkingModule.sol, .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-OUTCOME.md</files>
  <behavior>
    - Net-delta identity: after the flush, claimablePool == claimablePool_before − sum(ethValue_i) over the chunk — byte-identical to the per-slice path's running `claimablePool -= ethValue_i`.
    - Fail-loud preserved (class B): the batch flush `claimablePool -= uint128(poolDebitAcc)` reverts on underflow (Solidity 0.8 checked arithmetic; NOT wrapped in `unchecked`). A single batched underflow is equivalent to the first per-slice underflow that would have reverted.
    - No-mid-loop-read invariant: no `claimablePool` READ exists between the first per-slice debit and the batch flush (grep-proven; the only other touches subscribe :282 / AdvanceModule :958 are outside the loop).
    - Per-key debit unchanged: afkingFunding[src] -= ethValue stays per-iteration at :709 (the per-account underflow guard).
    - Penny-exact: the uint256 accumulator's single uint128 cast at flush equals the sum of the per-slice uint128 casts (no rounding/truncation).
    - forge build (contracts-only) is clean; DegenerusGame stays < 24,576 bytes.
  </behavior>
  <read_first>
    - .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-OUTCOME.md (Task 1's output — confirms the branch; run ONLY if it records Outcome B)
    - .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-GAS-SKEPTIC-VERDICTS.md (the APPROVED GAS-03 win + its penny-exact obligations)
    - .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-RESEARCH.md (§"Where a once-per-batch flush WOULD go" — the exact flush shape; §"Penny-Exact Solvency Check For The Flush" — the 4 proof obligations; §"Open Questions" #1 — the mixed-chunk hazard that must be excluded)
    - contracts/modules/GameAfkingModule.sol (processSubscriberStage :539-767 — the loop, the debit :708-711, the cursor persist :765; the floor-protected sites listed in the <interfaces> block)
    - contracts/storage/DegenerusGameStorage.sol (claimablePool :365 packed slot 1; the SOLVENCY invariant :358)
  </read_first>
  <action>
    Run ONLY if `350-OUTCOME.md` (Task 1) records Outcome B. If Outcome A, this task is SKIPPED (no-op; record "skipped — Outcome A" in the SUMMARY).

    Author the single penny-exact `claimablePool` same-slot-flush diff in `GameAfkingModule.processSubscriberStage`: introduce a full-width `uint256` memory accumulator; inside the loop, replace the `:710` `claimablePool -= uint128(ethValue)` with accumulating `ethValue` into the accumulator (keeping the `:709` `afkingFunding[src] -= ethValue` per-iteration, UNCHANGED); flush ONCE after the `while` loop and BEFORE the `_subCursor = uint16(cursor)` persist at `:765` with a checked `claimablePool -= uint128(accumulator)` (NOT wrapped in `unchecked`). If the 350-02 verdict flagged the mixed-chunk hazard as unresolved, the accumulator MUST collect ONLY the lootbox-mode `:710` debits and any `purchaseWith`-internal `claimablePool` write must remain immediate (un-batched) — OR, if that cannot be cleanly guaranteed, do NOT author the diff and escalate back to Outcome A (record the reason in `350-OUTCOME.md`). Leave `quests.*`, `purchaseWith` (:718-730), the per-key `afkingFunding[src]` debit (:709), the swap-pop set-mutations (:621-622, :675-676), the cancel-tombstone reclaim (:586-593), the no-orphan guard (:570), and the freeze fields (:747-748, :756) ALL UNTOUCHED.

    Add a penny-exact proof section to `350-OUTCOME.md` discharging the four obligations from the research: (1) net-delta identity (accumulator = sum of ethValue, associative; grep-prove NO `claimablePool` read between the first debit and the flush — cite the only other touches are subscribe :282 / AdvanceModule :958, both outside the loop); (2) fail-loud preserved (assert NO `unchecked` wraps the flush; a batched underflow reverts equivalently to the first per-slice underflow); (3) afkingFunding[src] stays per-iteration (cite :709 unchanged); (4) penny-exact (full-width uint256 accumulator, single uint128 cast at flush == sum of per-slice casts, no rounding). Note that 351 TST-06 must add the byte-identical-vs-per-slice-oracle + forced-underflow tests (per the conditional row in `350-TST06-MEASUREMENT-SPEC.md`).

    Run `forge build --sizes` (contracts-only) and confirm it exits clean with DegenerusGame < 24,576 bytes; record the byte count in `350-OUTCOME.md`. Do NOT run `forge test` (351's charge; stale reds expected). HOLD at the contract-commit boundary — do NOT commit `contracts/*.sol`; the commit is the USER hand-review gate. NO fenced code blocks of implementations in `350-OUTCOME.md` (describe the flush shape + cite anchors; the implementation lives in the .sol file).
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit; if grep -qi "Outcome B" .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-OUTCOME.md && grep -qiE "in effect|APPROVED" .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-OUTCOME.md; then forge build --sizes 2>&1 | tee /tmp/350b_build.log | grep -qi "DegenerusGame" && ! grep -qiE "Error|Compilation failed|compiler error" /tmp/350b_build.log && grep -qiE "penny-exact|net-delta|fail-loud" .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-OUTCOME.md && echo "PASS-OUTCOME-B"; else echo "SKIPPED-OUTCOME-A (Task 2 not applicable)"; fi</automated>
  </verify>
  <acceptance_criteria>
    - This task runs ONLY under Outcome B; under Outcome A it is a no-op recorded "skipped — Outcome A" in the SUMMARY.
    - (Outcome B) `GameAfkingModule.processSubscriberStage` accumulates the lootbox `:710` debits into a memory `uint256` and flushes once via a CHECKED `claimablePool -= uint128(accumulator)` before the `:765` cursor persist; the flush is NOT wrapped in `unchecked` (verified by reading the authored diff).
    - (Outcome B) the `:709` `afkingFunding[src] -= ethValue` per-key debit is UNCHANGED; `quests.*`, `purchaseWith`, the swap-pop set-mutations, the cancel-tombstone reclaim, the no-orphan guard, and the freeze fields are all UNTOUCHED (confirmable via `git diff contracts/modules/GameAfkingModule.sol` scope).
    - (Outcome B) `350-OUTCOME.md` discharges all four penny-exact obligations (net-delta identity + no-mid-loop-read grep + fail-loud-preserved + penny-exact cast) and notes the 351 TST-06 byte-identical-oracle + forced-underflow tests.
    - (Outcome B) `forge build --sizes` exits clean and DegenerusGame < 24,576 bytes (byte count recorded); `forge test` is NOT run.
    - (Outcome B) the diff is HELD at the contract-commit boundary — `contracts/*.sol` is NOT committed (commit = the USER hand-review gate).
    - The mixed-chunk hazard is addressed: the accumulator collects only the lootbox `:710` debits, OR the diff is abandoned back to Outcome A with the reason recorded.
  </acceptance_criteria>
  <done>
    Under Outcome A this task is correctly skipped. Under Outcome B the single penny-exact claimablePool flush is applied (per-key debit + quests + set-mutations + freeze fields untouched), all four solvency obligations are discharged in `350-OUTCOME.md`, `forge build` is clean with DegenerusGame < 24,576, and the diff is HELD at the contract-commit boundary for USER hand-review (never auto-committed).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| claimablePool solvency aggregate (slot 1) | Under Outcome B, the same-slot flush rewrites HOW `claimablePool` is debited across a chunk. This is the SOLVENCY-01 spine — the net delta MUST be penny-exact and underflow MUST fail loud. Under Outcome A this boundary is untouched. |
| contract-commit boundary | The diff (Outcome B) is applied + compiled but HELD; committing `contracts/*.sol` is the explicit USER hand-review gate (`autonomous: false`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-350-06 | Tampering | (Outcome B) the batch flush drifts the net `claimablePool` (penny mismatch) | mitigate | Net-delta identity proof (sum associative) + grep-proven no-mid-loop-read (only subscribe :282 / AdvanceModule :958 touch claimablePool, both outside the loop) + the 351 byte-identical-vs-per-slice oracle. |
| T-350-07 | Tampering | (Outcome B) masking a solvency underflow under the batched flush | mitigate | Checked `uint128 -=` on the flush (NO `unchecked`); the proof section + diff read confirm no `unchecked` wraps the `claimablePool -=`; 351 forced-underflow test (TST-02). |
| T-350-08 | Tampering | (Outcome B) the flush accidentally batches `afkingFunding[src]`, losing the per-account underflow guard | mitigate | The diff leaves `:709` per-iteration UNCHANGED; only `:710` is replaced by the accumulator; acceptance criterion asserts the per-key debit is unchanged. |
| T-350-09 | Tampering | (Outcome B) the mixed-chunk hazard — a ticket-mode purchaseWith claimablePool write interleaved with batched lootbox debits | mitigate | The accumulator collects ONLY the lootbox `:710` debits and any purchaseWith-internal claimablePool write stays immediate; if not cleanly guaranteeable, abandon to Outcome A with reason recorded. |
| T-350-10 | Tampering | (Either) a contract committed without USER hand-review | mitigate | `autonomous: false`; the diff is HELD at the commit boundary; commit is the explicit gate. |
| T-350-SC | Tampering | npm/pip/cargo installs | accept | No package installs — at most a `forge build` of the existing toolchain; no Package Legitimacy Gate triggered. |
</threat_model>

<verification>
- `350-OUTCOME.md` exists and names the executed branch (A or B) matching the 350-02 directive.
- Outcome A: GAS-01/02 CONFIRMED-STRUCTURAL + GAS-03 REJECTED recorded; `git diff --name-only -- contracts/` EMPTY; no commit gate.
- Outcome B: the penny-exact flush applied with the four obligations discharged; `forge build` clean (DegenerusGame < 24,576); per-key debit + quests + set-mutations + freeze fields untouched; diff HELD at the commit boundary (uncommitted).
</verification>

<success_criteria>
- ROADMAP Phase 350 SC4 satisfied in BOTH branches: Outcome A records "no net contract change → no diff gated" as the outcome; Outcome B holds the single batched diff at the contract-commit boundary for USER hand-review.
- GAS-03 outcome is recorded (REJECTED under A, or APPLIED penny-exact + held under B).
- No `contracts/*.sol` committed without explicit USER approval.
</success_criteria>

<output>
Create `.planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-03-SUMMARY.md` when done.
</output>
