---
phase: 350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-RE-PIN-AND-CONFIRM.md
  - .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-TST06-MEASUREMENT-SPEC.md
autonomous: true
requirements: [GAS-01, GAS-02]

must_haves:
  truths:
    - "Every SCAV-348-01..07 anchor is re-pinned against the live committed 77c3d9ef tree (status: ALREADY-DELIVERED-STRUCTURAL / DISSOLVED / STILL-APPLICABLE) with a live file:line, not the stale 20ca1f79 anchor"
    - "GAS-01 is documented confirmed-structural: the afking lootbox-mode buy writes ONE warm Sub-stamp (scorePlus1+amount) and NO cold ledger (no enqueueBoxForAutoOpen, no lootboxEth, no lootboxPurchasePacked, no boxPlayers.push) on the afking path"
    - "GAS-02 is documented confirmed-structural: the process/open hot path reads afkingFunding/claimableWinnings via in-context SLOAD and issues NO STATICCALL to a different address; the surviving afkingSnapshot/afkingFundingOf are Game view-helpers for the external DegenerusVault consumer only (NOT removal targets)"
    - "The 351 TST-06 measurement spec names per-buy + per-open marginal (loop-N-divide), the exact function/tx to instrument, and the 16.7M HARD per-tx ceiling with SUB_STAGE_BATCH=50"
    - "No contracts/*.sol file is modified by this plan (read-only confirm)"
  artifacts:
    - path: ".planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-RE-PIN-AND-CONFIRM.md"
      provides: "Re-pin table (SCAV-348-01..07 → live 77c3d9ef anchors + status) + GAS-01/02 confirm-structural evidence"
      contains: "GAS-01"
    - path: ".planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-TST06-MEASUREMENT-SPEC.md"
      provides: "The 351 TST-06 marginal-gas measurement spec (per-buy + per-open under 16.7M ceiling)"
      contains: "TST-06"
  key_links:
    - from: "350-RE-PIN-AND-CONFIRM.md"
      to: "contracts/modules/GameAfkingModule.sol:710,:747-748,:756,:464,:662,:709"
      via: "live grep anchors cited in the re-pin table"
      pattern: "GameAfkingModule.sol:"
    - from: "350-RE-PIN-AND-CONFIRM.md"
      to: "348-GAS-INVENTORY.md SCAV-348-01..07"
      via: "each SCAV-ID carried forward with re-pinned status"
      pattern: "SCAV-348-0"
---

<objective>
Re-pin the SPEC gas inventory (`348-GAS-INVENTORY.md` SCAV-348-01..07) against the LIVE committed `77c3d9ef` tree, CONFIRM that GAS-01 (box-ledger → warm Sub-stamp) and GAS-02 (cross-contract staticcall → in-context SLOAD) are structurally present (delivered by the 349/349.1 relocation — there is NOTHING to apply), and author the 351 TST-06 measurement spec.

Purpose: Phase 350 is a CONFIRM-and-validate phase, not an apply phase. The SPEC inventory's anchors were against the pre-fold `20ca1f79` tree and most drifted or dissolved. This plan re-grounds every candidate on the current tree so the W2 `/gas-skeptic` adjudication (plan 350-02) validates against real live code, and hands 351 a precise marginal-gas measurement spec. The dominant v55 gas wins (GAS-01/02) are STRUCTURAL — they materialized the moment subscriber state went game-resident — so this plan proves they are present, it does NOT re-implement them.

Output: `350-RE-PIN-AND-CONFIRM.md` (the re-pin table + GAS-01/02 confirm-structural evidence) and `350-TST06-MEASUREMENT-SPEC.md` (the 351 measurement spec). NO contract edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-RESEARCH.md
@.planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-GAS-INVENTORY.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

# The research's Re-Pin Table (350-RESEARCH.md §"Re-Pin Table") is the SEED for this plan's
# re-pin doc. The plan RE-VERIFIES each grep against the current tree — it does not copy
# the research's anchors blind. The §"Confirming GAS-01 + GAS-02 Are Structurally Present"
# and §"WHERE 351 must measure" sections are the source-of-truth for the two task deliverables.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-pin SCAV-348-01..07 against the live 77c3d9ef tree + confirm GAS-01/02 structural-present</name>
  <files>.planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-RE-PIN-AND-CONFIRM.md</files>
  <read_first>
    - .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-RESEARCH.md (the Re-Pin Table + the GAS-01/02 confirm sections + the Architectural Responsibility Map)
    - .planning/phases/348-spec-design-lock-freeze-proof-discharged-invariant-carry-4-p/348-GAS-INVENTORY.md (§2 the SCAV-348-01..07 list with the stale 20ca1f79 anchors; §3 the §6 scorecard; §4 the SAFE-WITH-CONDITIONS carve-out; §5 the security floor)
    - contracts/modules/GameAfkingModule.sol (the processSubscriberStage loop :539-767, _resolveBuy :440-496, the stamp writes :747-748,:756, the funding SLOADs :464,:662,:709, the claimablePool debit :710, the ticket-mode purchaseWith delegatecall :718-730)
    - contracts/modules/DegenerusGameLootboxModule.sol (resolveAfkingBox :877 — the afking open, no ledger; openLootBox :503 + the human cold-ledger reads/zeroes :505,:529,:555,:558 — unchanged, BOX-05 two-path)
    - contracts/DegenerusGame.sol (the residual view-helpers afkingFundingOf :1590, afkingSnapshot :2656)
    - contracts/DegenerusVault.sol (:518 — the external consumer of afkingFundingOf)
    - contracts/storage/DegenerusGameStorage.sol (the Sub struct :1867-1898 single-slot layout; claimablePool :365 packed slot 1; the SOLVENCY invariant :358)
  </read_first>
  <action>
    Author `350-RE-PIN-AND-CONFIRM.md`. Two sections:

    (A) RE-PIN TABLE. For EACH of SCAV-348-01..07, run a live grep against the committed `77c3d9ef` tree (use `git grep` or read the cited file region) and record: the OLD `20ca1f79` anchor (from the inventory), the NEW live `file:line` anchor(s), and a STATUS verdict from {ALREADY-DELIVERED-STRUCTURAL, DISSOLVED, STILL-APPLICABLE}. Expected outcomes (re-verify each — do NOT copy the research blind): SCAV-348-01 box-ledger→stamp = ALREADY-DELIVERED-STRUCTURAL (afking path writes only the Sub stamp `GameAfkingModule.sol:747-748,:756`, no `enqueueBoxForAutoOpen`/`lootboxEth`/`lootboxPurchasePacked`/`boxPlayers.push`; human path retains its ledger); SCAV-348-02 staticcall→SLOAD = ALREADY-DELIVERED-STRUCTURAL (`AfKing.sol` deleted, hot-path SLOADs at `:464,:662,:709`; `afkingSnapshot`/`afkingFundingOf` survive only as Game view-helpers for `DegenerusVault.sol:518`); SCAV-348-03 same-slot flush = STILL-APPLICABLE (the sole candidate — `claimablePool -=` at `GameAfkingModule.sol:710`; defer the verdict to plan 350-02); SCAV-348-04 AfKing bytecode retire = DISSOLVED (`contracts/AfKing.sol` absent, `AF_KING` constant grep-empty); SCAV-348-05 snapshot-batching scaffold = DISSOLVED (no equivalent in `GameAfkingModule`); SCAV-348-06 Sub-record packing = ALREADY-DELIVERED-STRUCTURAL (single slot `DegenerusGameStorage.sol:1867-1898`, 232/256 bits); SCAV-348-07 open-side ledger dead-path = ALREADY-DELIVERED-STRUCTURAL (afking open `resolveAfkingBox:877` walks no `boxPlayers`/zeroes no ledger; human `openLootBox:503` keeps `:505/:529/:555/:558`). If ANY live grep contradicts the research's expected status, record the discrepancy explicitly and flag it — do not silently override.

    (B) GAS-01 + GAS-02 CONFIRM-STRUCTURAL evidence. GAS-01: cite the live lootbox-mode stamp writes (`sub.scorePlus1` :747, `sub.amount` :748, `sub.lastAutoBoughtDay` :756) and assert grep-confirmed ABSENCE of `enqueueBoxForAutoOpen`/`lootboxEth[`/`lootboxPurchasePacked[`/`boxPlayers.push` on the afking path (those symbols live only in the human MintModule path + `boxPlayers` only in `DegenerusGame.sol`); note the box materializes at open from `(Sub stamp + rngWordByDay[lastAutoBoughtDay])` via `resolveAfkingBox:877`. GAS-02: cite the in-context SLOADs (`afkingFunding[player]` :464, `afkingFunding[src]` :662/:709, `claimableWinnings[player]` in `_resolveBuy`), assert NO STATICCALL to a different address on the process/open hot path, and document that the surviving `afkingSnapshot`/`afkingFundingOf` are Game view-helpers serving the external `DegenerusVault.sol:518` consumer (a real cross-contract caller — NOT a removal target). State explicitly: "GAS-01 and GAS-02 require NO apply work at 350 — they are confirmed structurally present in `77c3d9ef`; the empirical measurement is 351 TST-06."

    Use the research's evidence as the starting point but cite YOUR re-verified live anchors. NO fenced code blocks of implementations — name identifiers/file:line/slot names only. This plan READS contracts and WRITES the markdown doc only; assert `git diff --name-only -- contracts/` is EMPTY at the end.
  </action>
  <verify>
    <automated>test -f .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-RE-PIN-AND-CONFIRM.md && for id in SCAV-348-01 SCAV-348-02 SCAV-348-03 SCAV-348-04 SCAV-348-05 SCAV-348-06 SCAV-348-07; do grep -q "$id" .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-RE-PIN-AND-CONFIRM.md || { echo "MISSING $id"; exit 1; }; done && grep -q "GameAfkingModule.sol:710" .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-RE-PIN-AND-CONFIRM.md && grep -qi "GAS-01" .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-RE-PIN-AND-CONFIRM.md && grep -qi "GAS-02" .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-RE-PIN-AND-CONFIRM.md && [ -z "$(git diff --name-only -- contracts/)" ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `350-RE-PIN-AND-CONFIRM.md` exists and contains all seven SCAV-348-01..07 IDs, each with a live `77c3d9ef` anchor and a status verdict from {ALREADY-DELIVERED-STRUCTURAL, DISSOLVED, STILL-APPLICABLE}.
    - SCAV-348-03's row defers its APPROVE/REJECT verdict to plan 350-02 (it is named STILL-APPLICABLE here, not adjudicated).
    - The GAS-01 confirm section cites `GameAfkingModule.sol:747`, `:748`, `:756` AND asserts the grep-confirmed absence of `enqueueBoxForAutoOpen`/`lootboxEth`/`lootboxPurchasePacked`/`boxPlayers.push` on the afking path.
    - The GAS-02 confirm section cites in-context SLOADs at `:464`/`:662`/`:709`, asserts no STATICCALL on the hot path, and documents `afkingSnapshot`/`afkingFundingOf` as Game view-helpers for `DegenerusVault.sol:518` (NOT removal targets).
    - The doc states explicitly that GAS-01/GAS-02 require no apply work at 350 (confirmed structural; measured at 351).
    - `git diff --name-only -- contracts/` is EMPTY (no contract edits).
    - The doc contains NO fenced code block of a contract implementation (identifiers/anchors/slot names only).
  </acceptance_criteria>
  <done>
    The re-pin table grounds every SCAV-348 candidate on the live tree, GAS-01/GAS-02 are documented confirmed-structural with live anchors, SCAV-348-03 is carried to 350-02 for adjudication, and zero contract files are touched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author the 351 TST-06 marginal-gas measurement spec</name>
  <files>.planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-TST06-MEASUREMENT-SPEC.md</files>
  <read_first>
    - .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-RESEARCH.md (§"WHERE 351 must measure" — the function/tx/marginal table; §"GAS-03 Linearity Map" — the warm-write magnitude; the §Validation Architecture "Phase Requirements → Test Map")
    - .planning/REQUIREMENTS.md (TST-06 exact text — "measured per-buy + per-open marginal under the 16.7M HARD per-tx ceiling; the GAS-01/02/03 wins proven same-results")
    - contracts/modules/DegenerusGameAdvanceModule.sol (the STAGE insertion :288-326, _runSubscriberStage :754, SUB_STAGE_BATCH=50 :149, the 16.7M ceiling comment :147/:289)
    - contracts/modules/GameAfkingModule.sol (processSubscriberStage :539 — the per-buy site; _openAfkingBox :814 + _autoOpen :864 — the per-open site)
    - contracts/modules/DegenerusGameLootboxModule.sol (resolveAfkingBox :877 — the afking open; openLootBox :503 — the human baseline for the per-open comparison)
  </read_first>
  <action>
    Author `350-TST06-MEASUREMENT-SPEC.md` — the spec 351 (TST-06) consumes (350 writes the SPEC; 351 runs the harness). It MUST NOT run any test or gas harness itself. Record:

    (1) GAS-01 per-BUY marginal: instrument `GameAfkingModule.processSubscriberStage` (invoked inside `advanceGame`'s new-day STAGE block via `_runSubscriberStage` at `DegenerusGameAdvanceModule.sol:754`); the exercising tx is a new-day `advanceGame()` (or `mintBurnie()`'s advance leg) with N>=2 funded LOOTBOX-mode subs queued; the reported unit is the per-sub MARGINAL = (gas for N subs − gas for N−1 subs), compared against the v54 cold-ledger box-buy (~120-130k) to confirm the ~5k warm-stamp collapse. State the v46 Phase 319 CR-01 rule explicitly: peg/report the loop-N-divide MARGINAL, never a single-item total.

    (2) GAS-01 per-OPEN marginal: instrument `GameAfkingModule._openAfkingBox` (:814) → `resolveAfkingBox` (`LootboxModule.sol:877`); the exercising tx is `autoOpen(count)` or `mintBurnie()`'s open leg over N ready stamped boxes after their `rngWordByDay[day]` lands; report the per-open MARGINAL = (N opens − (N−1) opens), compared against the human `openLootBox:503` which walks the cold ledger + zeroes (`:505/:529/:555/:558`).

    (3) GAS-02: same STAGE call — a TRACE assertion that NO STATICCALL opcode targets a different address on the process/open path (the in-context SLOADs at `:464,:662,:709` replaced the staticcalls); this is a trace assertion, not a marginal number.

    (4) GAS-03 (CONDITIONAL — only if plan 350-03 lands an Outcome-B diff): if the `claimablePool` same-slot flush is applied, assert byte-identical `claimablePool` before/after a multi-sub batch vs the per-slice oracle AND a forced-underflow test that confirms the checked subtraction still reverts (fail-loud, class B). If Outcome A (no diff), this row is N/A — note that.

    (5) The 16.7M HARD per-tx ceiling context: `SUB_STAGE_BATCH = 50` chunks the STAGE (`AdvanceModule.sol:149`, comment budgets a landed buy ~262k → 50 ~ 13.1M, under the ceiling); 351 confirms a 50-chunk STAGE + the open leg each stay under 16.7M.

    NO fenced code blocks of test implementations — describe what to measure, where, and the comparison oracle. READ-only on contracts; WRITE the markdown doc only.
  </action>
  <verify>
    <automated>test -f .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-TST06-MEASUREMENT-SPEC.md && grep -qi "TST-06" .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-TST06-MEASUREMENT-SPEC.md && grep -qiE "marginal" .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-TST06-MEASUREMENT-SPEC.md && grep -qE "16\.7M|16_700_000|16700000" .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-TST06-MEASUREMENT-SPEC.md && grep -q "SUB_STAGE_BATCH" .planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-TST06-MEASUREMENT-SPEC.md && [ -z "$(git diff --name-only -- contracts/)" ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `350-TST06-MEASUREMENT-SPEC.md` exists and names BOTH the per-buy marginal (instrumenting `processSubscriberStage`) and the per-open marginal (instrumenting `_openAfkingBox`/`resolveAfkingBox`).
    - The spec states the loop-N-divide MARGINAL rule (v46 CR-01) — measure the marginal, never a single-item total.
    - The spec includes the GAS-02 no-STATICCALL trace assertion and the 16.7M HARD per-tx ceiling with `SUB_STAGE_BATCH=50`.
    - The GAS-03 measurement row is explicitly CONDITIONAL on plan 350-03 producing an Outcome-B diff (N/A under Outcome A).
    - The spec does NOT run any test/gas harness (it is the spec FOR 351, not the run).
    - `git diff --name-only -- contracts/` is EMPTY.
    - No fenced code block of a test implementation.
  </acceptance_criteria>
  <done>
    The 351 TST-06 measurement spec names the per-buy + per-open marginal sites, the comparison oracles, the no-STATICCALL trace assertion, the conditional GAS-03 oracle, and the 16.7M ceiling — with zero contract edits and no test run.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is read-only analysis + documentation. It introduces no new code, no new external input, and crosses no trust boundary. The only "boundary" referenced is the existing Game ↔ DegenerusVault view-call (`afkingFundingOf`), which this plan documents (not modifies). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-350-01 | Tampering | The re-pin doc stating a stale/incorrect anchor that misleads the 350-02 adjudication | mitigate | Every anchor is a LIVE grep against `77c3d9ef`; any contradiction with the research's expected status is recorded explicitly, not silently overridden. |
| T-350-02 | Repudiation | The TST-06 spec under-specifying the marginal, re-introducing the single-box over-count (v46 CR-01 faucet) | mitigate | The spec states the loop-N-divide MARGINAL rule verbatim; the per-buy + per-open units are both (N − N−1). |
| T-350-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — read-only grep + Markdown write only; no Package Legitimacy Gate triggered. |
</threat_model>

<verification>
- `350-RE-PIN-AND-CONFIRM.md` and `350-TST06-MEASUREMENT-SPEC.md` both exist in the phase dir.
- All seven SCAV-348-01..07 IDs re-pinned with live anchors + status; SCAV-348-03 carried to 350-02.
- GAS-01/GAS-02 documented confirmed-structural with live `GameAfkingModule.sol`/`DegenerusGameStorage.sol`/`DegenerusVault.sol` anchors.
- TST-06 spec names per-buy + per-open marginal + the 16.7M ceiling + the conditional GAS-03 oracle.
- `git diff --name-only -- contracts/` is EMPTY (read-only confirm; no contract mutation).
</verification>

<success_criteria>
- The SPEC gas inventory is re-grounded on the live `77c3d9ef` tree (ROADMAP SC1 + SC2 confirm half).
- GAS-01 (box-ledger → warm Sub-stamp) and GAS-02 (staticcall → SLOAD) are documented confirmed-structural — no apply work, measured at 351.
- The 351 TST-06 measurement spec is authored and ready for the TST phase to consume.
- Zero contract files modified.
</success_criteria>

<output>
Create `.planning/phases/350-gas-behavior-identical-no-cost-wins-box-ledger-warm-sub-stam/350-01-SUMMARY.md` when done.
</output>
