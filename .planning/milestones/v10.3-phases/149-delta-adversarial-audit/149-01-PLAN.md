---
phase: 149-delta-adversarial-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/149-delta-adversarial-audit/149-01-FINDINGS.md
autonomous: true
requirements: [DELTA-01, DELTA-02, DELTA-03, DELTA-04, DELTA-05]

must_haves:
  truths:
    - "Every changed function across all v10.1-modified contracts has a SAFE / VULNERABLE / INFO verdict with rationale"
    - "BurnieCoinflip.onlyFlipCreditors admits only GAME+COIN+AFFILIATE+ADMIN -- no other contract can call creditFlip or creditFlipBatch"
    - "Game.adminStakeEthForStEth and Game.setLootboxRngThreshold vault-owner check is equivalent or stricter than the old Admin-mediated path"
    - "BurnieCoin.mintForGame with COINFLIP+GAME callers produces identical mint behavior to the old separate functions"
    - "Storage layout has no slot collisions or gaps from function removals on any changed contract"
  artifacts:
    - path: ".planning/phases/149-delta-adversarial-audit/149-01-FINDINGS.md"
      provides: "Per-function verdict table, access control traces, storage layout verification"
      contains: "| Function"
  key_links:
    - from: "BurnieCoinflip.onlyFlipCreditors"
      to: "ContractAddresses.GAME, COIN, AFFILIATE, ADMIN"
      via: "modifier require check"
      pattern: "onlyFlipCreditors"
    - from: "DegenerusGame.adminStakeEthForStEth"
      to: "vault.isVaultOwner(msg.sender)"
      via: "access control check"
      pattern: "isVaultOwner"
    - from: "BurnieCoin.mintForGame"
      to: "ContractAddresses.COINFLIP, GAME"
      via: "caller check in modifier or require"
      pattern: "mintForGame"
---

<objective>
Adversarial audit of every function changed in v10.1 ABI Cleanup across all modified contracts. Produce a per-function security verdict table, trace the three key access control changes, and verify storage layout integrity.

Purpose: Prove no security regressions from v10.1 wrapper removal, caller rewiring, access control migration, or mint function merger before the C4A audit begins.
Output: 149-01-FINDINGS.md with per-function verdicts, access control traces, and storage layout verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Contracts to Audit (all v10.1-modified)

Production contracts:
@contracts/BurnieCoin.sol
@contracts/BurnieCoinflip.sol
@contracts/DegenerusAdmin.sol
@contracts/DegenerusGame.sol
@contracts/DegenerusAffiliate.sol
@contracts/DegenerusVault.sol
@contracts/modules/DegenerusGameAdvanceModule.sol
@contracts/modules/DegenerusGameJackpotModule.sol
@contracts/modules/DegenerusGameMintModule.sol
@contracts/modules/DegenerusGameLootboxModule.sol
@contracts/modules/DegenerusGameDegeneretteModule.sol
@contracts/StakedDegenerusStonk.sol

Interfaces:
@contracts/interfaces/IDegenerusGame.sol
@contracts/interfaces/IDegenerusCoin.sol
@contracts/interfaces/IDegenerusGameModules.sol

## What Changed in v10.1 (executor must verify each item)

1. **BurnieCoin.sol** -- 7 forwarding wrappers removed (creditFlip, creditFlipBatch, creditLinkReward, previewClaimCoinflips, coinflipAmount, claimableCoin, coinflipAutoRebuyInfo), creditCoin removed, onlyFlipCreditors modifier removed, mintForCoinflip merged into mintForGame, coinflipContract constant inlined, onlyAdmin modifier removed
2. **BurnieCoinflip.sol** -- onlyFlipCreditors expanded to GAME+COIN+AFFILIATE+ADMIN (was GAME+BURNIE), modifiers updated to use ContractAddresses.* constants, mintForCoinflip calls changed to mintForGame
3. **DegenerusAdmin.sol** -- stakeGameEthToStEth and setLootboxRngThreshold wrappers removed, creditLinkReward call changed to coinflipReward.creditFlip
4. **DegenerusGame.sol** -- added IDegenerusVaultOwnerGame interface + vault constant, adminStakeEthForStEth/setLootboxRngThreshold now check vault.isVaultOwner(msg.sender), coin.creditFlip changed to coinflip.creditFlip, 16 unused view functions removed
5. **DegenerusAffiliate.sol** -- coin.creditFlip changed to coinflip.creditFlip, creditCoin removed from interface
6. **DegenerusGameAdvanceModule.sol** -- coin.creditFlip changed to coinflip.creditFlip
7. **DegenerusGameJackpotModule.sol** -- coin.creditFlip/creditFlipBatch changed to coinflip.creditFlip/creditFlipBatch
8. **DegenerusGameMintModule.sol** -- coin.creditFlip changed to coinflip.creditFlip, orphaned NatSpec removed
9. **DegenerusGameLootboxModule.sol** -- coin.creditFlip changed to coinflip.creditFlip
10. **DegenerusGameDegeneretteModule.sol** -- coin.creditFlip changed to coinflip.creditFlip
11. **StakedDegenerusStonk.sol** -- comment update only
12. **Interfaces** -- IDegenerusGame.sol, IDegenerusCoin.sol, DegenerusGameModuleInterfaces.sol -- removed function declarations matching removed implementations
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delta Adversarial Audit -- All v10.1 Changed Functions</name>
  <read_first>
    - contracts/BurnieCoin.sol (focus: mintForGame, any remaining modifiers, removed function stubs)
    - contracts/BurnieCoinflip.sol (focus: onlyFlipCreditors modifier, creditFlip, creditFlipBatch, mintForGame calls)
    - contracts/DegenerusAdmin.sol (focus: creditLinkReward call change, removed wrappers)
    - contracts/DegenerusGame.sol (focus: IDegenerusVaultOwnerGame, vault constant, adminStakeEthForStEth, setLootboxRngThreshold, creditFlip call changes, verify 16 view functions are gone)
    - contracts/DegenerusAffiliate.sol (focus: creditFlip call change)
    - contracts/DegenerusVault.sol (focus: isVaultOwner function that Game now calls)
    - contracts/modules/DegenerusGameAdvanceModule.sol (focus: creditFlip call change)
    - contracts/modules/DegenerusGameJackpotModule.sol (focus: creditFlip/creditFlipBatch call changes)
    - contracts/modules/DegenerusGameMintModule.sol (focus: creditFlip call change)
    - contracts/modules/DegenerusGameLootboxModule.sol (focus: creditFlip call change)
    - contracts/modules/DegenerusGameDegeneretteModule.sol (focus: creditFlip call change)
    - contracts/StakedDegenerusStonk.sol (focus: comment-only change)
    - contracts/interfaces/IDegenerusGame.sol (focus: removed declarations)
    - contracts/interfaces/IDegenerusCoin.sol (focus: removed declarations)
    - contracts/interfaces/IDegenerusGameModules.sol (focus: removed declarations)
  </read_first>
  <files>.planning/phases/149-delta-adversarial-audit/149-01-FINDINGS.md</files>
  <action>
Perform a systematic adversarial audit of every function changed in v10.1. The output is a single findings document (149-01-FINDINGS.md) with the following sections:

**Section 1: Per-Function Verdict Table (DELTA-01)**

For every changed function across all 12 contracts + 3 interfaces, produce a row:

| Contract | Function | Change Type | Verdict | Rationale |
|----------|----------|-------------|---------|-----------|

Change types: REMOVED, REWIRED (caller changed), MODIFIED (logic changed), ADDED, COMMENT-ONLY
Verdicts: SAFE, VULNERABLE, INFO
For REMOVED functions: verify no on-chain caller remains (grep all .sol files for the function name)
For REWIRED functions: verify the new target accepts the same caller and parameters
For MODIFIED functions: verify the new logic is equivalent or stricter

**Section 2: BurnieCoinflip.onlyFlipCreditors Expansion (DELTA-02)**

Trace the onlyFlipCreditors modifier in BurnieCoinflip.sol:
1. Read the modifier -- document exactly which addresses it checks (should be ContractAddresses.GAME, ContractAddresses.COIN, ContractAddresses.AFFILIATE, ContractAddresses.ADMIN)
2. List every function that uses this modifier (creditFlip, creditFlipBatch, and any others)
3. For each authorized caller (GAME, COIN, AFFILIATE, ADMIN), trace backward: which functions in those contracts call creditFlip/creditFlipBatch? Are the call arguments correct?
4. Verify no contract outside the authorized set has a code path that reaches creditFlip or creditFlipBatch (grep for "creditFlip" and "creditFlipBatch" across all .sol files)
5. Compare old authorization (GAME+BURNIE) vs new (GAME+COIN+AFFILIATE+ADMIN) -- is the expanded set justified? BURNIE was the old name for what is now COIN, and AFFILIATE+ADMIN are new direct callers that previously went through BurnieCoin forwarding wrappers.

**Section 3: Vault-Owner Access Control (DELTA-03)**

Trace the access control change on Game.adminStakeEthForStEth and Game.setLootboxRngThreshold:
1. Read the old path: Admin.stakeGameEthToStEth called Game.adminStakeEthForStEth -- what was Admin's access control? (should have been onlyOwner or similar)
2. Read the new path: Game.adminStakeEthForStEth now checks vault.isVaultOwner(msg.sender) directly
3. Read DegenerusVault.isVaultOwner to understand what it checks
4. Prove equivalence or strictness: the set of addresses that can call the function is the same or smaller
5. Check for privilege escalation: can any address that was NOT authorized before now call these functions?
6. Same analysis for setLootboxRngThreshold

**Section 4: mintForGame Merger (DELTA-04)**

Trace the BurnieCoin.mintForGame function:
1. Read the current implementation -- what callers does it accept? (should be COINFLIP and GAME via modifier or require)
2. Read what mintForCoinflip used to do vs what mintForGame does -- is the mint logic identical?
3. Verify all callers that previously called mintForCoinflip now call mintForGame with correct parameters
4. Verify no new unauthorized caller can reach mintForGame (grep for "mintForGame" across all .sol files)
5. Check that the merged function does not introduce any path where GAME could mint with coinflip-only parameters or vice versa

**Section 5: Storage Layout Verification (DELTA-05)**

Run forge inspect on every changed contract that has storage variables:
```bash
forge inspect BurnieCoin storage-layout --pretty
forge inspect BurnieCoinflip storage-layout --pretty
forge inspect DegenerusAdmin storage-layout --pretty
forge inspect DegenerusGame storage-layout --pretty
forge inspect DegenerusAffiliate storage-layout --pretty
forge inspect DegenerusVault storage-layout --pretty
forge inspect DegenerusGameAdvanceModule storage-layout --pretty
forge inspect DegenerusGameJackpotModule storage-layout --pretty
forge inspect DegenerusGameMintModule storage-layout --pretty
forge inspect DegenerusGameLootboxModule storage-layout --pretty
forge inspect DegenerusGameDegeneretteModule storage-layout --pretty
forge inspect StakedDegenerusStonk storage-layout --pretty
```

For each contract:
- Confirm no slot collisions (no two variables at the same slot unless intentional packing)
- Confirm no gaps from function removals (function removals should NOT affect storage layout, but verify)
- Note: removing functions does not change storage, but adding the `vault` constant in DegenerusGame.sol could if it is a state variable rather than an immutable/constant. Verify `vault` is immutable or constant.

**Section 6: Summary**

- Total functions audited: N
- Verdicts: X SAFE, Y INFO, Z VULNERABLE
- Key findings (if any)
- Overall assessment: PASS or FAIL with rationale
  </action>
  <verify>
    <automated>test -f .planning/phases/149-delta-adversarial-audit/149-01-FINDINGS.md && grep -c "| SAFE\|| INFO\|| VULNERABLE" .planning/phases/149-delta-adversarial-audit/149-01-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - 149-01-FINDINGS.md exists with all 6 sections populated
    - Per-function verdict table covers every changed function across all 12 contracts + 3 interfaces (expect 30+ rows)
    - onlyFlipCreditors trace names every authorized caller and every function using the modifier
    - Vault-owner access control section proves equivalence or strictness with the old Admin path
    - mintForGame section proves no unauthorized mint path opened
    - Storage layout section includes forge inspect output for every contract with storage variables
    - No VULNERABLE verdicts (or if any exist, they are clearly documented with severity and recommendation)
  </acceptance_criteria>
  <done>
    - Every changed function has a SAFE/VULNERABLE/INFO verdict in the table
    - BurnieCoinflip.onlyFlipCreditors expansion is traced and verified
    - Vault-owner access control is proven equivalent or stricter
    - mintForGame merger is proven safe with no unauthorized mint paths
    - Storage layout verified via forge inspect on all changed contracts
    - Summary section states overall PASS/FAIL
  </done>
</task>

</tasks>

<verification>
1. 149-01-FINDINGS.md contains a per-function verdict table with 30+ entries covering all changed contracts
2. No VULNERABLE verdicts (or any VULNERABLE findings have clear remediation)
3. All three key access control traces (onlyFlipCreditors, vault-owner, mintForGame) are complete
4. forge inspect was run on all contracts with storage variables
5. Summary section provides overall assessment
</verification>

<success_criteria>
- Every v10.1-changed function has a documented security verdict
- The three key access control changes are traced end-to-end with proof of safety
- Storage layout is verified with no collisions or unexpected changes
- The findings document is complete enough that a reviewer can independently verify each conclusion
</success_criteria>

<output>
After completion, create `.planning/phases/149-delta-adversarial-audit/149-01-SUMMARY.md`
</output>
