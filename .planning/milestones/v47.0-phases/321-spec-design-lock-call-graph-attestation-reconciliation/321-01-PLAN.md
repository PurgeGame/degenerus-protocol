---
phase: 321-spec-design-lock-call-graph-attestation-reconciliation
plan: 01
subsystem: spec
status: complete
delivery: direct-design-lock-authoring
commit: 779eacc3
---

# Phase 321 — Plan 01: SPEC Design-Lock + Call-Graph Attestation + Shared-Surface Reconciliation

> **Delivery note (ledger reconciliation):** This SPEC phase was delivered by direct
> design-lock authoring (the established v44/v45/v46 audit-milestone variant), committed
> as `779eacc3` — `321-SPEC.md` + four `321-ATTEST-*.md` files — rather than through a
> multi-wave plan→execute cycle. This PLAN + its SUMMARY + the VERIFICATION reconcile the
> GSD ledger against that committed deliverable; they author no new design. A power outage
> interrupted the run after the deliverable commit but before this closure, leaving STATE.md
> at "Not started". No `contracts/` source was mutated at SPEC (confirmed §3 of the SPEC).

## Objective

Settle every shared contract surface to a single signature, grep-attest every cited
`file:line` across the 7 v47.0 work items against the plan-time contract HEAD, and re-prove
the two cross-plan invariants (claimable-balance + presale-box RNG freeze) on paper — so
Phase 322 applies one fully reconciled batched diff with zero un-checked "by construction"
assumptions.

## Requirements owned

- **BATCH-01** — shared-surface reconciliation (the cross-plan joint edits).
- **BATCH-02** — call-graph attestation (per-anchor grep verification vs HEAD `2a18d622`).

## Tasks (delivered)

1. **BATCH-02 attestation** — grep every cited anchor across all 7 items against source HEAD;
   record per-anchor verdicts in the four `321-ATTEST-*.md` files; roll up the verdict
   (0 IMPL blockers) and fold material clarifications into carried corrections C1–C9.
   → `321-ATTEST-PRESALE.md`, `321-ATTEST-LOOT-DGAS-DSPIN.md`, `321-ATTEST-REDEEM-CPAY.md`,
   `321-ATTEST-TOMB.md`; verdict + C1–C9 in `321-SPEC.md` §0.
2. **BATCH-01 reconciliation** — settle the cross-plan joint edits R1–R7:
   - R1 final `resolveRedemptionLootbox` signature + apply-order,
   - R2 `_resolveLootboxCommon` param reduction (5→2 bools),
   - R3 `claimablePool == Σ claimableWinnings` joint-check (`_creditBoxProceeds` +
     `pullRedemptionReserve` + CPAY shortfall pattern),
   - R4 presale-box RNG freeze (domain-salted committed-word reuse),
   - R5 single DegeneretteModule edit (DGAS + DSPIN),
   - R6 earlybird→presale-box subsystem swap + dead-flag removal scope,
   - R7 AfKing cancel-tombstone (isolated).
   → `321-SPEC.md` §1.
3. **Per-item IMPL blueprint** — the batched-diff checklist + file/edit-order map as the
   load-bearing input to Phase 322. → `321-SPEC.md` §2.

## Success criteria (Phase 321)

Mapped 1:1 to the ROADMAP success criteria; verified in `321-VERIFICATION.md`.

1. `resolveRedemptionLootbox` final signature settled in writing with apply-order (R1).
2. `claimablePool == Σ claimableWinnings` joint-check documented across PRESALE-06 /
   CPAY-01/02/03 / REDEEM-01/03 (R3).
3. Every cited `file:line` grep-attested vs HEAD; 0 blockers; no un-checked "by construction"
   claims; inline-dup logic re-checked (§0 + the 4 ATTEST docs).
4. Presale-box RNG model re-verified freeze-safe (R4).
5. Earlybird-removal scope grep-confirmed no surviving consumer; `Pool.Earlybird` →
   `Pool.PresaleBox` rename targets pinned (R5/R6).

## Out of scope

- Any `contracts/*.sol` mutation — deferred to Phase 322 (the single batched diff).
