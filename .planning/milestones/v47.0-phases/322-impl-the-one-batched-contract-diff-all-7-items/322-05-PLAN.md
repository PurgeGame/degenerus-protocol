---
phase: 322-impl-the-one-batched-contract-diff-all-7-items
plan: 05
type: execute
wave: 5
depends_on: ["322-04"]
files_modified:
  - contracts/modules/DegenerusGameDegeneretteModule.sol
autonomous: true
requirements: [DGAS-01, DGAS-02, DGAS-03, DGAS-04, DSPIN-01]
must_haves:
  truths:
    - "resolveBets accumulates ETH/BURNIE/WWXRP payouts CROSS-BET across the whole call and flushes ONCE per currency (one mintForGame/mintPrize, one claimableWinnings+claimablePool write, one pool write) — additive, byte-identical."
    - "The ETH cap/solvency stays per-spin but evaluates against a running-pool local (read pool once, decrement in memory per spin) — byte-identical to per-spin today."
    - "Lootbox-share is summed PER betId (one resolveLootboxDirect per bet), NEVER summed across betIds; the per-spin lootboxWord salting is dropped (use the bet rngWord); the per-spin RESULT seed is KEPT."
    - "The DGNRS award (_awardDegeneretteDgnrs, ETH 6+ matches) stays per-spin (reads poolBalance fresh per call)."
    - "RNG seed derivation, rngWord fetch, and the freeze invariant are UNTOUCHED (batch only bookkeeping after outcomes are determined)."
    - "Per-currency spin caps replace MAX_SPINS_PER_BET: ETH 25 / BURNIE 15 / WWXRP 5; MAX_SPINS_PER_BET retired; doc comments updated; ticketCount 8-bit packing unchanged."
  artifacts:
    - path: "contracts/modules/DegenerusGameDegeneretteModule.sol"
      provides: "write-batched resolveBets (cross-bet flush per currency; per-betId lootbox; per-spin DGNRS + result seed) + per-currency spin caps (ETH25/BURNIE15/WWXRP5); MAX_SPINS_PER_BET removed"
      contains: "CURRENCY_WWXRP"
  key_links:
    - from: "resolveBets cross-bet accumulation"
      to: "single per-currency flush (mint/claimable/pool)"
      via: "return-delta tuples from _resolveBet/_resolveFullTicketBet accumulated then flushed once"
      pattern: "resolveBets"
    - from: "per-currency cap"
      to: "replaces ticketCount > MAX_SPINS_PER_BET"
      via: "currency == CURRENCY_ETH ? 25 : currency == CURRENCY_BURNIE ? 15 : 5"
      pattern: "CURRENCY_WWXRP"
---

<objective>
Land the single DegeneretteModule edit (SPEC R5) covering BOTH DGAS (write-batching, same-results) AND DSPIN (per-currency spin caps). DGAS is gas-only with a HARD same-payout-results constraint; DSPIN raises the loop bound to 25 (ETH) that the batching must absorb. RNG seed derivation + the freeze invariant are UNTOUCHED.

Purpose: ROADMAP SC-3. One file, one edit (these two items share DegeneretteModule.sol and cannot be split — R5).
Output: A write-batched resolveBets at the new per-currency caps, payout-identical.

NON-NEGOTIABLE: EDIT .sol autonomously; do NOT commit. Security over gas (HARD FLOOR): the ETH cap stays per-spin (evaluated against a running-pool local — NOT a single end-of-call check); the DGNRS award stays per-spin (reads poolBalance fresh — summing off a stale balance would CHANGE payouts); the per-spin RESULT seed for match determination stays per-spin; RNG/freeze untouched. Lootbox-share is summed PER betId only, NEVER across betIds (resolution-batch-invariant). The DGAS refactor must be byte-identical in payout to today.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-CONTEXT.md
@.planning/phases/321-spec-design-lock-call-graph-attestation-reconciliation/321-SPEC.md
@.planning/phases/321-spec-design-lock-call-graph-attestation-reconciliation/321-ATTEST-LOOT-DGAS-DSPIN.md
@.planning/PLAN-DEGENERETTE-RESOLUTION-GAS.md
@.planning/PLAN-DEGENERETTE-SPINS-PER-CURRENCY.md

<grep_protocol>
Re-grep every construct; this file was not edited by 322-01..04 (only its _resolveLootboxDirect CALLEE in LootboxModule changed signature — 322-03 dropped 3 bools from _resolveLootboxCommon but the private _resolveLootboxDirect wrapper signature DegeneretteModule calls is unchanged; confirm). Source-of-truth: ATTEST-LOOT §C (full resolveBets / _resolveFullTicketBet / _distributePayout structure + the exact line anchors), §D (per-currency cap edit target + sole MAX_SPINS_PER_BET dependency), §E (WWXRP=3 not 2), §F-2/§F-3 (_applyEvMultiplierWithCap is cross-module; lootbox hand-off via private _resolveLootboxDirect wrapper :783).
</grep_protocol>

<interfaces>
ATTEST-LOOT §C.1/§C.2/§C.3 — current Degenerette structure:
- resolveBets(address player, uint64[] betIds) :389-398 loops _resolveBet per betId (today void).
- _resolveBet :553 -> _resolveFullTicketBet(player, betId, packed) (today void).
- _resolveFullTicketBet :561-678: currency :569 (one per bet, MASK_2), rngWord = lootboxRngWordByIndex[index] :577 (read once/bet), delete bet :580; loop for(uint8 spinIdx; spinIdx < ticketCount;) :593; resultSeed :596-609 (per-spin, KEEP); payout :621; if(payout!=0) :641 -> totalPayout += :642 -> per-spin lootboxWord salting :646-657 (DROP) -> _distributePayout :658; _awardDegeneretteDgnrs :662-663 (Tier-3, per-spin KEEP); FullTicketResolved emit :671-677.
- _distributePayout :705-779: ETH branch :713 (3-tier split -> ethShare/lootboxShare; frozen _setPendingPools :748 + _addClaimableEth :749; unfrozen pool read :754 + ETH_WIN_CAP_BPS cap :755-763 + _setFuturePrizePool :764 + _addClaimableEth :765; lootboxShare>0 -> _resolveLootboxDirect :772); BURNIE coin.mintForGame :775; WWXRP wwxrp.mintPrize :777.
ATTEST-LOOT §D.1/§D.2 — DSPIN edit target: if (ticketCount == 0 || ticketCount > MAX_SPINS_PER_BET) revert InvalidBet(); at :445-446 (spans 2 lines). MAX_SPINS_PER_BET (:226) sole readers: def :226, validation :445, doc comments :296 + :364. CURRENCY_ETH=0 :208, CURRENCY_BURNIE=1 :211, CURRENCY_WWXRP=3 :214 (C6).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DSPIN-01 — per-currency spin caps (ETH 25 / BURNIE 15 / WWXRP 5), retire MAX_SPINS_PER_BET</name>
  <read_first>
    - contracts/modules/DegenerusGameDegeneretteModule.sol (_placeDegeneretteBetCore :437; validation :445-446; MAX_SPINS_PER_BET decl :226; doc comments :296, :364; CURRENCY_ETH/BURNIE/WWXRP :208/:211/:214; ticketCount 8-bit packing [34..41] :296)
    - 321-ATTEST-LOOT §D.1/§D.2, §E (WWXRP=3); PLAN-DSPIN §2
  </read_first>
  <action>
    DSPIN-01: replace the validation `if (ticketCount == 0 || ticketCount > MAX_SPINS_PER_BET) revert InvalidBet();` (:445-446) with a per-currency cap: revert if ticketCount == 0 OR ticketCount exceeds the currency's cap, where the cap = `currency == CURRENCY_ETH ? 25 : currency == CURRENCY_BURNIE ? 15 : 5` (use the named constant CURRENCY_WWXRP for the WWXRP arm reasoning per C6; currency is already validated upstream to be one of ETH=0/BURNIE=1/WWXRP=3). RETIRE the MAX_SPINS_PER_BET constant (:226) and update the two doc comments (:296 "1..MAX_SPINS_PER_BET" and :364 "@param ticketCount Number of spins (1..MAX_SPINS_PER_BET)") to describe the per-currency caps. ticketCount is uint8 (field [34..41]) — 25 fits, NO packing change. Min bet per-spin unchanged. Comments describe what IS.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && test -z "$(grep -rl MAX_SPINS_PER_BET contracts/)" && grep -q "CURRENCY_WWXRP" contracts/modules/DegenerusGameDegeneretteModule.sol && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - The single validation branch is replaced by a per-currency cap (ETH 25 / BURNIE 15 / WWXRP 5).
    - MAX_SPINS_PER_BET is removed entirely (no token survives in contracts/); both doc comments updated.
    - ticketCount packing unchanged; min-bet logic unchanged.
  </acceptance_criteria>
  <done>Per-currency spin caps in place; MAX_SPINS_PER_BET retired; packing untouched.</done>
</task>

<task type="auto">
  <name>Task 2: DGAS-01..04 — cross-bet write-batching (same-results), per-betId lootbox, per-spin DGNRS + result seed kept, RNG untouched</name>
  <read_first>
    - contracts/modules/DegenerusGameDegeneretteModule.sol (resolveBets :389-398; _resolveBet :553; _resolveFullTicketBet :561-678 incl. resultSeed :596-609, lootboxWord salting :646-657, _distributePayout call :658, _awardDegeneretteDgnrs :662-663; _distributePayout :705-779 incl. ETH cap math :754-763, _resolveLootboxDirect :772/:783, mintForGame :775, mintPrize :777; _awardDegeneretteDgnrs fn :1125-1149 reads poolBalance :1135)
    - 321-ATTEST-LOOT §C.1/§C.2/§C.3, §F-2/§F-3; PLAN-DGAS §3 (Tier-1/2/3 + lootbox per-betId + same-results constraint)
  </read_first>
  <action>
    DGAS refactor — batch ONLY bookkeeping AFTER outcomes are determined; keep payouts byte-identical.
    DGAS-01 (cross-bet flush): change _resolveBet and _resolveFullTicketBet to RETURN per-currency delta tuples (ETH claimable delta, BURNIE mint total, WWXRP mint total, and the pool-write deltas) instead of writing inline. In resolveBets, accumulate these CROSS-BET across the whole betIds loop, then flush ONCE: one coin.mintForGame per (player, summed BURNIE), one wwxrp.mintPrize per (player, summed WWXRP), one _addClaimableEth + one claimablePool write for the summed ETH claimable, one pool write. Additive => byte-identical to per-spin/per-bet writes today.
    DGAS-02 (ETH cap per-spin against running-pool local): keep the ETH cap/solvency evaluation PER SPIN, but read the prize pool ONCE into a local and decrement it in memory per spin (so each spin's cap is computed against the same running balance the per-spin writes would have produced); do the single pool write at the end. This must be byte-identical to evaluating the cap per-spin against the live pool today (ATTEST §C.3 cap math :754-763). Do NOT move the cap to a single end-of-call check (that would change results).
    DGAS-03 (lootbox per-betId): sum the lootbox-share PER betId and call the private _resolveLootboxDirect wrapper (:772/:783) ONCE per bet (one box per bet). NEVER sum lootbox-share across betIds (resolution-batch-invariant — two bet-txs sharing an index => two boxes). DROP the per-spin lootboxWord salting (:646-657) — use the bet's rngWord. KEEP the per-spin RESULT seed (:596-609) for match determination.
    DGAS-04 (DGNRS per-spin): leave _awardDegeneretteDgnrs (:662-663, ETH 6+ matches) PER SPIN — it reads poolBalance fresh per call (:1135); summing off a stale balance would change payouts. Do NOT batch it.
    DGAS-05 note (RNG untouched): rngWord fetch (lootboxRngWordByIndex[index] :577), the result-seed derivation, and the freeze invariant are UNTOUCHED — only post-outcome bookkeeping is batched. Confirm no SLOAD inside the rng-window is moved.
    NOTE: 322-03 changed _resolveLootboxCommon's signature, but the private _resolveLootboxDirect wrapper (:783) that this module delegatecalls is unchanged (it forwards winner/amount/rngWord/evScore) — confirm it still compiles after 322-03.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -q "function resolveBets" contracts/modules/DegenerusGameDegeneretteModule.sol && grep -q "_resolveLootboxDirect" contracts/modules/DegenerusGameDegeneretteModule.sol && grep -q "_awardDegeneretteDgnrs" contracts/modules/DegenerusGameDegeneretteModule.sol && timeout 300 forge build 2>&1 | grep -ciE "^error|Error \(" | tr -d "\n" ; echo " errcount"</automated>
  </verify>
  <acceptance_criteria>
    - resolveBets accumulates cross-bet and flushes once per currency (one mint per currency, one claimable+pool write, one pool write).
    - The ETH cap stays per-spin against a running-pool local (NOT a single end-of-call check); byte-identical math to ATTEST §C.3.
    - Lootbox-share summed per betId only (one _resolveLootboxDirect per bet); per-spin lootboxWord salting dropped; per-spin result seed kept.
    - _awardDegeneretteDgnrs stays per-spin (fresh poolBalance read).
    - rngWord fetch + result-seed derivation + freeze invariant untouched.
    - forge build error count attributable only to downstream-owned constructs (document); this module compiles for its own scope.
  </acceptance_criteria>
  <done>Cross-bet write-batching is payout-identical; lootbox per-betId; DGNRS + result seed per-spin; RNG/freeze untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| resolveBets caller -> payout writes | gas refactor must not change WHO gets WHAT (same-results HARD constraint) |
| rng-window SLOADs | the freeze invariant requires no buffered/stale read enters the roll |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-322-05-01 | Tampering | payout drift from batching | mitigate | ETH cap stays per-spin against a running-pool local (byte-identical); DGNRS stays per-spin (fresh poolBalance); result seed per-spin; BURNIE/WWXRP/ETH-claimable are additive (sum == sequence of adds). Proven empirically at Phase 323 (DGAS-05). |
| T-322-05-02 | Tampering | lootbox summed across bets | mitigate | Lootbox-share summed PER betId only (one box per bet); never across (resolution-batch-invariant). |
| T-322-05-03 | Tampering | RNG/freeze breakage (R4) | mitigate | rngWord fetch + result-seed derivation untouched; only post-outcome bookkeeping batched; no rng-window SLOAD moved. |
| T-322-05-04 | Denial of Service | 25-spin worst case gas | accept | DSPIN raises the ETH loop bound to 25 (2.5x roll work); the DGAS batching absorbs the write cost. Worst-case gas derived-then-measured at Phase 323 (DSPIN-02). |
| T-322-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
- forge build clean for this module's scope.
- No MAX_SPINS_PER_BET token survives; per-currency caps present.
- resolveBets flushes once per currency; lootbox per-betId; DGNRS + result seed per-spin; cap per-spin against running-pool local.
- rngWord/result-seed/freeze untouched.
- No git commit performed.
</verification>

<success_criteria>
- ROADMAP SC-3 satisfied: Degenerette resolution write-batched same-results at per-currency caps (ETH 25 / BURNIE 15 / WWXRP 5); RNG/freeze untouched.
</success_criteria>

<output>
Create `.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-05-SUMMARY.md` when done. Record the byte-identical argument for the ETH cap running-pool local + the lootbox per-betId invariant. Do NOT git commit.
</output>
