---
phase: 322-impl-the-one-batched-contract-diff-all-7-items
plan: 02
type: execute
wave: 2
depends_on: ["322-01"]
files_modified:
  - contracts/modules/DegenerusGameMintModule.sol
  - contracts/modules/DegenerusGameLootboxModule.sol
  - contracts/modules/DegenerusGameAdvanceModule.sol
  - contracts/DegenerusGame.sol
  - contracts/interfaces/IDegenerusGame.sol
  - contracts/storage/DegenerusGameStorage.sol
autonomous: true
requirements: [PRESALE-01, PRESALE-02, PRESALE-03, PRESALE-04, PRESALE-05, PRESALE-07, PRESALE-08, PRESALE-09, PRESALE-12, CPAY-02]
must_haves:
  truths:
    - "The 20% presale vault skim is gone — presale lootbox ETH routes 100% to prize pools (special presale split collapsed to the normal 90/10)."
    - "The +62% presale BURNIE bonus block is removed (no LOOTBOX_PRESALE_BURNIE_BONUS_BPS application)."
    - "Mint ETH buys accrue presaleBoxCredit[buyer] += purchaseEth/4 while !presaleOver (replacing the MintModule _awardEarlybirdDgnrs call)."
    - "buyPresaleBox / buyLootboxAndPresaleBox entrypoints exist; the box is credit-gated, paid in fresh ETH or claimable (CPAY-02), consumes credit 1:1, capped at 50 ETH cumulative, MIN_BOX checked on the requested pre-clamp amount."
    - "The presale box resolves via its OWN boon-less 50/40/10 roll, NOT a _resolveLootboxCommon caller; entropy = keccak256(abi.encodePacked(rngWord,'PRESALE_BOX'))."
    - "The 50-ETH-crossing box is clamped to land cumulative box-ETH at exactly 50 (excess refunded), gets a normal roll + sweeps remaining Pool.PresaleBox DGNRS to that buyer, latches presaleOver=true and stops credit accrual."
    - "Box ETH routes 80/20 VAULT/SDGNRS via _creditBoxProceeds; the claimablePool == sum(claimableWinnings) invariant stays balanced."
  artifacts:
    - path: "contracts/modules/DegenerusGameMintModule.sol"
      provides: "20% vault skim removed (presale split to 90/10); presaleBoxCredit accrual; box buy/resolution + 80/20 routing + 50-ETH clamp-close + sweep + presaleOver latch"
      contains: "presaleBoxCredit"
    - path: "contracts/DegenerusGame.sol"
      provides: "buyPresaleBox / buyLootboxAndPresaleBox external payable entrypoints + presale views"
      contains: "buyPresaleBox"
    - path: "contracts/interfaces/IDegenerusGame.sol"
      provides: "presale-box entrypoint + view declarations"
  key_links:
    - from: "MintModule presaleBoxCredit accrual"
      to: "storage presaleBoxCredit (322-01)"
      via: "presaleBoxCredit[buyer] += purchaseEth/4"
      pattern: "presaleBoxCredit"
    - from: "box buy routing"
      to: "PayoutUtils _creditBoxProceeds (322-01)"
      via: "_creditBoxProceeds(boxEth)"
      pattern: "_creditBoxProceeds"
    - from: "box resolution entropy"
      to: "committed lootboxRngWordByIndex[index]"
      via: "keccak256(abi.encodePacked(rngWord, PRESALE_BOX))"
      pattern: "PRESALE_BOX"
---

<objective>
Land the rake-removal + the credit-gated coin-presale box (the headline item of v47.0). Remove the 20% vault skim + the +62% BURNIE bonus, swap the MintModule earlybird-emission call for presaleBoxCredit accrual, and add the boon-less presale box: buy entrypoints, the 50/40/10 roll, 80/20 routing, the exactly-50-ETH clamp-close + last-buyer DGNRS sweep + presaleOver latch, RNG-freeze-safe via the salted committed word.

Purpose: ROADMAP SC-1 ("truly rake-free") + the box product. Consumes the 322-01 storage + helper foundation.
Output: Rake-free presale lootbox ETH; the presale box buy/resolution surface; credit accrual on mint.

NON-NEGOTIABLE: EDIT .sol autonomously; do NOT commit (ONE batched diff, committed only at the 322-08 approval gate). Security-over-gas hard floor: box RNG entropy MUST be the committed index/day word + the "PRESALE_BOX" domain salt (R4) — no new mutable SLOAD in the box roll, frozen across request->unlock. The claimablePool == sum(claimableWinnings) invariant (R3) must stay balanced through the box buy (both fresh-ETH and claimable-funded halves).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-CONTEXT.md
@.planning/phases/321-spec-design-lock-call-graph-attestation-reconciliation/321-SPEC.md
@.planning/phases/321-spec-design-lock-call-graph-attestation-reconciliation/321-ATTEST-PRESALE.md
@.planning/PLAN-PRESALE-COIN-BOXES-RAKE-FREE.md
@.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-01-SUMMARY.md

<grep_protocol>
Re-grep every named construct before editing; lines drifted after 322-01. Source-of-truth: ATTEST-PRESALE §1 (split + 200-ETH auto-end + earlybird call site), §2 (+62% bonus), §3a (salt-pattern mirror at AdvanceModule:370-378), PLAN-PRESALE §3/§7 (full box design + LOCKED economic numbers), 322-01-SUMMARY (storage/helper wiring).
</grep_protocol>

<interfaces>
From 322-01 (storage / PayoutUtils):
- bool internal presaleOver; (slot-0 latch)
- uint96 internal presaleBoxEthSold; + PRESALE_BOX_ETH_CAP = 50 ether
- mapping(address => uint256) internal presaleBoxCredit;
- presale-box index->RNG-word + per-player queued-box-ETH mirrors (exact names/key widths in 322-01-SUMMARY)
- function _creditBoxProceeds(uint256 boxEth) internal — 80/20 VAULT/SDGNRS split, bumps claimablePool by boxEth

LOCKED economic numbers (PLAN-PRESALE §3/§7):
- DGNRS curve: pool = 10% of INITIAL_SUPPLY; base = poolSize/100 DGNRS-per-ETH; 5 cumulative-volume tiers of 10 ETH each at relative rates [3.0, 2.5, 2.0, 1.5, 1.0] (tier-1 = 3x base, tier-5 = 1x base); drains exactly at 50 ETH; transferFromPool(Pool.PresaleBox, player, amt).
- BURNIE: mean 400% of box ETH on the BURNIE branch (E[largeBurnieBps] = 40000); spread = mirror the existing lootbox BURNIE variance band recentered on 4x; credited via coinflip.creditFlip.
- WWXRP: 1 token via wwxrp.mintPrize.
- Probabilities: 50% BURNIE / 40% DGNRS / 10% WWXRP.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the rake (20% skim to 90/10 + the +62% BURNIE bonus) and swap mint credit accrual</name>
  <read_first>
    - contracts/modules/DegenerusGameMintModule.sol (presale split constants :112-119; 200-ETH auto-end + split + vault skim block :1021-1077; _awardEarlybirdDgnrs call site :1210)
    - contracts/modules/DegenerusGameLootboxModule.sol (+62% bonus block :972-976; constant LOOTBOX_PRESALE_BURNIE_BONUS_BPS :288; upstream burniePresale/burnieNoMultiplier split at :957-969)
    - contracts/modules/DegenerusGameAdvanceModule.sol (level-3 presale clear :429-431)
    - 321-ATTEST-PRESALE.md §1a/§1b/§1c/§2a/§2b; 322-01-SUMMARY.md (is _awardEarlybirdDgnrs body still present)
  </read_first>
  <action>
    PRESALE-01: in MintModule remove the 20% vault skim. Collapse the presale split so presale lootbox ETH routes 100% to pools at the normal 90/10 (LOOTBOX_SPLIT_FUTURE_BPS=9000 / NEXT_BPS=1000). Delete the LOOTBOX_PRESALE_SPLIT_FUTURE/NEXT/VAULT_BPS constants, delete the else-if(presale) split arm (presale now takes the normal path; the distress arm stays), and delete the if(vaultShare != 0){ VAULT.call{value:vaultShare} } skim block.
    PRESALE-02: in LootboxModule delete the if(presale && burniePresale != 0){ +62% } block (:972-976) and the LOOTBOX_PRESALE_BURNIE_BONUS_BPS = 6_200 constant (:288). If the burniePresale/burnieNoMultiplier split then collapses to a single BURNIE total with no consumer, simplify it (grep first). Do NOT remove the `presale` bool PARAM of _resolveLootboxCommon here — only its +62% body block; plan 322-03 drops the param.
    PRESALE-03 (D5): replace _awardEarlybirdDgnrs(buyer, ticketCost + lootBoxAmount); (:1210) with `if (!presaleOver) { presaleBoxCredit[buyer] += (ticketCost + lootBoxAmount) / 4; }`. Covers batchPurchase (routes through mint). Degenerette + BURNIE-funded buys stay excluded (no earlybird site — ATTEST §1d). The _awardEarlybirdDgnrs body in DegenerusGameStorage.sol is deleted by whichever of {this plan, 322-06} swaps the LAST call site (322-06 owns the 3 WhaleModule sites) — record ownership in the SUMMARY; do not leave a no-caller body undocumented.
    PRESALE-12 dead-flag sweep (R6/PRESALE-11): the 200-ETH mint auto-end (:1021-1030), presaleStatePacked (Storage:843)+_psRead/_psWrite, and the level-3 clear (AdvanceModule:429-431) are candidate-dead. Grep-confirm no surviving consumer per file, then delete and let presaleOver be the sole terminal. If any consumer survives, KEEP it and document WHY (R6). The OLD LOOTBOX_PRESALE_ETH_CAP = 200 ether (Storage:852, mint-volume cap) is distinct from the NEW PRESALE_BOX_ETH_CAP = 50 ether; delete the 200-ether cap only if its sole reader is removed.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && test -z "$(grep -rl LOOTBOX_PRESALE_BURNIE_BONUS_BPS contracts/)" && test -z "$(grep -rl LOOTBOX_PRESALE_SPLIT_VAULT_BPS contracts/)" && grep -q presaleBoxCredit contracts/modules/DegenerusGameMintModule.sol && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - No LOOTBOX_PRESALE_BURNIE_BONUS_BPS / LOOTBOX_PRESALE_SPLIT_VAULT_BPS token anywhere in contracts/.
    - No VAULT.call skim block in the presale lootbox path; presale routes 90/10 to pools.
    - MintModule accrues presaleBoxCredit[buyer] += purchaseEth/4 gated by !presaleOver at the former earlybird site.
    - Candidate-dead presale flags either deleted (no consumer) or kept-with-documented-reason.
  </acceptance_criteria>
  <done>Rake removed (skim + bonus gone); mint credit accrual swapped; dead presale flags resolved per R6.</done>
</task>

<task type="auto">
  <name>Task 2: Presale-box buy + boon-less 50/40/10 resolution + 80/20 routing + RNG-salt</name>
  <read_first>
    - contracts/modules/DegenerusGameMintModule.sol (purchaseCoin/_purchaseCoinFor :852-897 — the BURNIE-funded exclusion; the ETH lootbox queue+open machinery `lootboxEth`/`lootboxRngWordByIndex` to mirror)
    - contracts/modules/DegenerusGameLootboxModule.sol (openLootBox :477; resolution roll helpers _lootboxDgnrsReward ppm-of-pool tiers; the BURNIE variance band to mirror)
    - contracts/modules/DegenerusGameAdvanceModule.sol (salt pattern :370-378 — keccak256(abi.encodePacked(rngWord, keccak256("BONUS_TRAITS"))))
    - PLAN-PRESALE §3.3 (box contents + 80/20), §3.4/§3.4b (unified entrypoint + bundle-open + boon-less), §3.5 (clamp-close + sweep + latch), §7 (LOCKED curve/BURNIE/probabilities)
    - 322-01-SUMMARY.md (presaleBox storage names; _creditBoxProceeds signature)
  </read_first>
  <action>
    Build the presale box as its OWN resolution (PRESALE-08: boon-less; NOT a _resolveLootboxCommon caller). Place the box logic in MintModule or LootboxModule (your discretion; keep it off the boon path). Implement:
    PRESALE-04: credit-gated buy. boxAmount <= remaining presaleBoxCredit[player]; consume credit 1:1; global cumulative cap = PRESALE_BOX_ETH_CAP (50 ether) tracked via presaleBoxEthSold. MIN_BOX (= LOOTBOX_MIN, 0.01 ether) checked on the REQUESTED amount BEFORE the exactly-50 clamp (lock-prevention — a sub-floor gap to 50 must NOT lock the close). boxAmount > availableCredit reverts (PLAN §7 #6).
    PRESALE-05/PRESALE-07: queue-then-open like lootboxes. The box queues at its own index with the committed RNG word (mirror lootboxEth/lootboxRngWordByIndex). buyLootboxAndPresaleBox shares ONE index with the co-queued lootbox (R4: one committed word, two domain-separated draws at open); buyPresaleBox queues a lone box. Resolution single random roll on the salted entropy keccak256(abi.encodePacked(rngWord, "PRESALE_BOX")): 50% BURNIE (mean 400%-on-branch via E[largeBurnieBps]=40000, mirror lootbox variance band recentered on 4x, credit via coinflip.creditFlip), 40% DGNRS (5-tier %-of-pool curve [3.0,2.5,2.0,1.5,1.0] by cumulative box volume, base = poolSize/100, transferFromPool(Pool.PresaleBox, player, amt)), 10% -> 1 WWXRP (wwxrp.mintPrize). Bundle-open: lootbox half uses its existing seed derivation, box half uses the salted one; robust to lone-lootbox / lone-box / both.
    PRESALE-06 + CPAY-02 (80/20 routing): on purchase call _creditBoxProceeds(boxEth) (claimablePool += boxEth; VAULT += 80%; SDGNRS += 20%). Payable from claimable (CPAY-02, pure ledger move): boxAmount may be covered by msg.value + the caller's claimableWinnings shortfall using the canonical CPAY pattern (msg.value > boxAmount reverts; shortfall = boxAmount - msg.value; STRICT 1-wei sentinel: claimableWinnings[player] <= shortfall reverts; debit claimableWinnings[player] and claimablePool by shortfall) — THEN _creditBoxProceeds re-credits VAULT+SDGNRS + claimablePool += boxEth, so for the claimable-funded portion net pool delta = 0 and for the msg.value portion pool += that ETH. Verify the two halves compose to a balanced pool (R3).
    PRESALE-09 clamp-close: the box that would cross 50 ETH is sized down to land cumulative box-ETH at exactly 50 (refund the excess msg.value/credit); that clamped box gets a normal 50/40/10 roll on its clamped amount AND sweeps ALL remaining Pool.PresaleBox DGNRS to that buyer on top of the roll (regardless of roll outcome), latches presaleOver = true (slot 0), and stops credit accrual. Verify the clamped final tier + sweep zeroes Pool.PresaleBox.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -rq PRESALE_BOX contracts/modules/ && grep -rq _creditBoxProceeds contracts/modules/ && grep -rq presaleOver contracts/modules/ && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Box buy is credit-gated, 1:1 credit-consuming, capped at 50 ETH; MIN_BOX checked on the requested (pre-clamp) amount.
    - Box resolution rolls 50/40/10 with the LOCKED curve/BURNIE numbers, NOT via _resolveLootboxCommon (boon-less).
    - Box entropy = keccak256(abi.encodePacked(rngWord, "PRESALE_BOX")) off the committed index/day word (no new mutable SLOAD).
    - Box ETH routes 80/20 via _creditBoxProceeds; claimable-funded portion nets pool delta 0; msg.value portion bumps pool by that ETH.
    - The clamped 50-ETH-crossing box sweeps the remaining Pool.PresaleBox + latches presaleOver and stops accrual.
  </acceptance_criteria>
  <done>Presale box buy + boon-less roll + 80/20 routing + clamp-close/sweep/latch implemented; RNG freeze preserved via the salted committed word.</done>
</task>

<task type="auto">
  <name>Task 3: Wire DegenerusGame entrypoints + interface + presale views</name>
  <read_first>
    - contracts/DegenerusGame.sol (existing external payable mint/lootbox entrypoints + delegatecall dispatch pattern; the recordMint/purchase facade style)
    - contracts/interfaces/IDegenerusGame.sol (existing entrypoint + view decls to mirror)
    - 322-02 Task 2 output (the module-level buy/resolution functions to dispatch to)
  </read_first>
  <action>
    PRESALE-07: add `buyPresaleBox` and `buyLootboxAndPresaleBox` as `external payable` entrypoints on DegenerusGame.sol, delegatecall-dispatching to the module function(s) from Task 2 (mirror the existing mint/lootbox dispatch). buyLootboxAndPresaleBox optionally performs the credit-earning mint in the same call (PLAN §3.4: 4B mint + B box = 5B total with zero banked credit), then queues both at one index. Add the matching declarations to interfaces/IDegenerusGame.sol. Add the read-only views the plan calls for (e.g. presaleBoxCreditOf(address), presaleBoxEthRemaining()) + their interface decls (PLAN §5). These entrypoints are included in the CPAY-03 external-payable sweep (322-06) — keep them claimable-pay-capable per Task 2's box-buy pattern.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -q buyPresaleBox contracts/DegenerusGame.sol && grep -q buyPresaleBox contracts/interfaces/IDegenerusGame.sol && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - buyPresaleBox + buyLootboxAndPresaleBox exist as external payable on DegenerusGame.sol and are declared in IDegenerusGame.sol.
    - Presale views (credit-of, eth-remaining) exist + are declared.
    - The combined entrypoint queues both lootbox+box at one index (R4) and earns credit from the mint leg.
  </acceptance_criteria>
  <done>Presale entrypoints + interface decls + views wired; combined-buy shares one index.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| buyer -> buyPresaleBox/bundle (external payable) | untrusted ETH + boxAmount + (optional) mintParams cross here |
| committed RNG word -> box payout | entropy must be unknown at buy-commit and frozen across request->unlock (R4) |
| claimable ledger | claimablePool == sum(claimableWinnings) must hold through the 80/20 routing |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-322-02-01 | Tampering | box RNG | mitigate | Entropy = keccak256(abi.encodePacked(committedWord, "PRESALE_BOX")); buy only QUEUES, open resolves a later tx; no new mutable SLOAD enters the roll (R4). Re-verify freeze at Phase 324. |
| T-322-02-02 | Elevation of Privilege | box mints a whale pass | mitigate | Box is its OWN boon-less resolution, NOT a _resolveLootboxCommon caller (PRESALE-08) — a credit-funded box can never mint a pass. |
| T-322-02-03 | Information Disclosure / Integrity | 80/20 claimable routing | mitigate | _creditBoxProceeds bumps pool by full boxEth; claimable-funded shortfall debit nets pool delta 0; verify both halves compose balanced (R3). |
| T-322-02-04 | Denial of Service | close-liveness | mitigate | MIN_BOX checked on the REQUESTED (pre-clamp) amount so a sub-0.01-ETH gap to 50 can never lock the close (PRESALE-04/09). |
| T-322-02-05 | Spoofing/Tampering | presale snipe of the close sweep | mitigate | The closing box is clamped to exactly 50 ETH (cannot be inflated); the 3:1 curve drains the pool over 50 ETH so the swept remainder is dust, not a windfall (PLAN §3.5). |
| T-322-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
- `forge build` compiles (residual errors only if a dangling reference is owned by a not-yet-run downstream plan — document).
- No LOOTBOX_PRESALE_BURNIE_BONUS_BPS / vault-skim survives; presale routes 90/10.
- Box buy gated/capped/clamped per PRESALE-04/09; box roll boon-less + salted-entropy per PRESALE-05/08/12.
- Box 80/20 routing keeps claimablePool == sum(claimableWinnings) balanced (R3).
- No git commit performed.
</verification>

<success_criteria>
- ROADMAP SC-1 satisfied: rake-free presale + credit-gated boon-less boxes (80/20, 50-ETH clamp-close + sweep + presaleOver latch) replace earlybird.
- buyPresaleBox / buyLootboxAndPresaleBox entrypoints + views wired; combined buy shares one index with two domain-separated draws.
- All locked economic numbers (DGNRS curve, BURNIE mean, 50/40/10 split) implemented.
</success_criteria>

<output>
Create `.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-02-SUMMARY.md` when done. Record where the box resolution lives (Mint vs Lootbox module), which plan owns the final `_awardEarlybirdDgnrs` body deletion, and which candidate-dead presale flags were deleted vs kept-with-reason. Do NOT git commit.
</output>
