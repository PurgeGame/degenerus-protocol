---
phase: 324-terminal-delta-audit-3-skill-adversarial-sweep-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/324-terminal-delta-audit-3-skill-adversarial-sweep-closure/324-01-DELTA-AUDIT.md
autonomous: true
requirements: [BATCH-03]

must_haves:
  truths:
    - "Every one of the 18 contract files changed vs v46 baseline 16e9668a is enumerated and attested NON-WIDENING"
    - "All 7 v47.0 work-item surfaces (rake/presale, lootbox-boon, Degenerette gas+caps, claimable-pay, redemption, cancel-tombstone) carry a NON-WIDENING verdict backed by grep/diff evidence"
    - "The VRFLifecycle recalibration (1 v47-PRESALE-economics delta) + OBS-1 (Decimator under-reservation) handoff items are dispositioned in the economic re-verify"
    - "The v47 foundry regression baseline 598/38/16 is attested NON-WIDENING vs v46 565/45/16"
    - "git diff fabe9e94 -- contracts/ is empty (zero contract mutation in this phase)"
  artifacts:
    - path: ".planning/phases/324-terminal-delta-audit-3-skill-adversarial-sweep-closure/324-01-DELTA-AUDIT.md"
      provides: "The SC1 delta-audit log — per-surface NON-WIDENING disposition table + regression-baseline attestation + handoff dispositions"
      contains: "Delta-Surface Table"
  key_links:
    - from: "324-01-DELTA-AUDIT.md"
      to: "git diff 16e9668a fabe9e94 -- contracts/"
      via: "per-file delta enumeration"
      pattern: "NON-WIDENING"
---

<objective>
Author the SC1 DELTA AUDIT: enumerate every contract surface changed by the v47.0 batched diff relative to the v46.0 baseline (`MILESTONE_V46_AT_HEAD_16e9668a6de35cc0c809d81ce960aee137950687`), and attest each NON-WIDENING. This is a read-only analysis — it produces ONE markdown log (`324-01-DELTA-AUDIT.md`) and makes ZERO `contracts/*.sol` edits. The audit subject is FROZEN at commit `fabe9e94`.

Purpose: SC1 of the terminal phase. Mirrors the v46 §3.A/§5 delta-surface + regression structure so the findings deliverable (324-03) can fold this log into §3/§5.
Output: `324-01-DELTA-AUDIT.md` — a per-surface disposition table covering all 7 work items + the regression-baseline attestation + the two STATE.md "324 handoff" dispositions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PLAN-V47-MILESTONE-SCOPE.md
@.planning/REQUIREMENTS.md
@audit/FINDINGS-v46.0.md

<interfaces>
<!-- The frozen audit subject. Executor reads these directly — no exploration needed. -->
- Audit subject HEAD: `fabe9e94` (the USER-approved Degenerette resolveBets post-game-over insolvency guard `if (_livenessTriggered()) revert E();` at `DegeneretteModule:421`, on top of the batched IMPL diff `fb29ed51`).
- Audit baseline: v46.0 closure HEAD `16e9668a6de35cc0c809d81ce960aee137950687` (signal `MILESTONE_V46_AT_HEAD_16e9668a6de35cc0c809d81ce960aee137950687`).
- The delta-surface set = 18 contract files (`git diff 16e9668a fabe9e94 -- contracts/`): AfKing.sol, BurnieCoin.sol, BurnieCoinflip.sol, DegenerusGame.sol, DegenerusVault.sol, StakedDegenerusStonk.sol, interfaces/IDegenerusGame.sol, interfaces/IDegenerusGameModules.sol, interfaces/IStakedDegenerusStonk.sol, modules/DegenerusGameAdvanceModule.sol, modules/DegenerusGameDegeneretteModule.sol, modules/DegenerusGameGameOverModule.sol, modules/DegenerusGameLootboxModule.sol, modules/DegenerusGameMintModule.sol, modules/DegenerusGamePayoutUtils.sol, modules/DegenerusGameWhaleModule.sol, storage/DegenerusGameStorage.sol, test/SettleClaimableShortfallTester.sol (the last is a contracts/test/ helper, not a mainnet surface).
- Final foundry regression baseline (323-09): 598 pass / 38 fail / 16 skip — 32 pre-existing-v46 (byte-identical at 16e9668a) + 5 combined-run fuzz/cache noise (pass isolated) + 1 v47-behavioral PRESALE delta (`VRFLifecycle::test_vrfLifecycle_levelAdvancement`, owned by THIS phase's economic re-verify). v46 baseline was 565/45/16.
- The 7 work-item -> surface map (manifest §2):
  1. rake-removal + presale-box: MintModule (90/10 split, credit accrual, earlybird removal), LootboxModule (62% BURNIE bonus removed, presale-box boon-less resolution, salted RNG), DegenerusGame (presaleOver latch, 80/20 box-ETH ledger), StakedDegenerusStonk (Pool.Earlybird->PresaleBox), Storage (presale storage).
  2. lootbox-boon unification: LootboxModule (_resolveLootboxCommon 5->2 bools, 3-caller unification, 10% haircut fix, BURNIE-lootbox removal — terminal-paradox closure), DegenerusGame/MintModule/Vault (BURNIE-lootbox kill-set), interfaces.
  3. Degenerette gas (same-results) + per-currency caps: DegeneretteModule (cross-bet write-batching, per-betId lootbox, per-spin DGNRS, ETH 25 / BURNIE 15 / WWXRP 5 caps) + the fabe9e94 liveness guard.
  4. universal claimable-pay: WhaleModule (3 purchases), DegenerusGame (external payable entry sweep), claimablePool == Sum claimableWinnings.
  5. sDGNRS redemption: StakedDegenerusStonk (ETH hard-segregation pullRedemptionReserve, BURNIE flip-credit-at-submit redeemBurnieShare, reserve apparatus deletion), BurnieCoin/BurnieCoinflip (SDGNRS authority), GameOverModule (double-count drop), LootboxModule (resolveRedemptionLootbox payable).
  6. AfKing cancel-tombstone: AfKing.sol (in-place tombstone + in-sweep reclaim, no relocation).
- Handoff items from STATE.md "324 handoff" for the economic re-verify: (a) VRFLifecycle test recalibration — 1 v47-PRESALE-economics delta (the test's purchase-volume magic numbers were calibrated for the v46 prize-pool split; v47 rake-removal/presale-box split changed per-purchase nextPrizePool accumulation; this is the SPEC behaving as intended, NOT a contract defect); (b) OBS-1 — pre-existing Decimator under-reservation (a pre-existing observation, not a v47 delta).
</interfaces>
</context>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan introduces NO new code or attack surface — it is a read-only delta audit producing one markdown log. The threats under examination are the v47.0 contract surfaces enumerated in SC1 (rake/presale, lootbox-boon, Degenerette gas+caps, claimable-pay, redemption, cancel-tombstone), already frozen at `fabe9e94`. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-324-01-RO | Tampering | this plan's deliverable | accept | Read-only analysis; the executor never edits `contracts/*.sol` — verified by `git diff fabe9e94 HEAD -- contracts/` being empty. No new attack surface. |
| T-324-01-WIDEN | Elevation | v47 delta surfaces vs v46 | mitigate | The audit itself IS the mitigation: every changed surface is attested NON-WIDENING with grep/diff evidence before closure can proceed. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate the 18-file delta surface and attest each NON-WIDENING</name>
  <read_first>
    - audit/FINDINGS-v46.0.md (the §3.A Delta-Surface Table format + §3.B Composition Attestation Matrix to mirror)
    - .planning/PLAN-V47-MILESTONE-SCOPE.md (§2 shared-surface coordination map — the 7 work items -> file map)
    - .planning/REQUIREMENTS.md (the 45 REQ rows: PRESALE-01..13, LOOT-01..06, DGAS-01..05, CPAY-01..03, REDEEM-01..08, DSPIN-01..02, TOMB-01..05 — each maps to a surface)
    - the frozen subject files via `git show fabe9e94:contracts/<file>` and `git diff 16e9668a fabe9e94 -- contracts/<file>` for each of the 18 files
  </read_first>
  <files>.planning/phases/324-terminal-delta-audit-3-skill-adversarial-sweep-closure/324-01-DELTA-AUDIT.md</files>
  <action>
    Run `git diff 16e9668a fabe9e94 -- contracts/` and build a per-surface disposition table grouped by the 7 work items (manifest §2 map in the interfaces block). For each of the 7 surfaces, attest NON-WIDENING with concrete grep/diff evidence drawn from the frozen subject:
    - Rake/presale: grep the BURNIE-lootbox + earlybird kill-sets (openBurnieLootBox / purchaseBurnieLootbox / _purchaseBurnieLootboxFor / _awardEarlybirdDgnrs / _finalizeEarlybird / EARLYBIRD_) return ZERO in mainnet contracts/ (the only openBurnieLootBox survivor is a stale doc comment in contracts/test/LootboxBernoulliTester.sol — call this out as a contracts/test helper, NOT a mainnet surface); confirm presaleOver latch present (DegenerusGameStorage.sol + DegenerusGame.sol) and Pool.PresaleBox rename in StakedDegenerusStonk.sol; confirm the 90/10 collapse and 62% bonus removal. Use `grep -v '^//' | grep -c` style filtering so doc comments do not self-invalidate the kill-set counts.
    - Lootbox-boon unification: confirm _resolveLootboxCommon collapsed to 2 bools, 3 ETH callers all roll boons+passes, 10% haircut spent (no silent drop), terminal-paradox closed (no BURNIE->box path).
    - Degenerette gas+caps: confirm cross-bet write-batching is bookkeeping-only after outcomes determined (RNG/freeze untouched per DGAS-05), per-betId lootbox sum, per-spin DGNRS, per-currency caps ETH 25 / BURNIE 15 / WWXRP 5, and the fabe9e94 liveness guard at DegeneretteModule:421.
    - Claimable-pay: confirm the 3 WhaleModule purchases + the DegenerusGame external-payable entry sweep apply the shortfall pattern; assert claimablePool == Sum claimableWinnings stays balanced (cite the 323-09 SolvencyObligations canonical-set proof).
    - Redemption: confirm ETH hard-segregation (pullRedemptionReserve fail-closed), BURNIE flip-credit-at-submit (redeemBurnieShare net new BURNIE == 0), reserve apparatus deleted, gameOver double-count drop, resolveRedemptionLootbox payable + unchecked-debit removed.
    - Cancel-tombstone: confirm AfKing setDailyQuantity(0) is in-place (no _removeFromSet relocation), in-sweep reclaim branch present, no-cursor-advance — H-CANCEL-SWAP-MISS structurally resolved.
    Mirror the v46 §3.A table columns: Surface | Requirements | Re-grepped anchors | Disposition. Add a Composition Attestation Matrix mirroring v46 §3.B (ADD x REMOVE / claimable-balance / BURNIE-net-0 / RNG-freeze-intact). DO NOT edit any contracts/*.sol file — read-only via `git show`/`git diff`/`grep`.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && test -z "$(git diff fabe9e94 HEAD -- contracts/)" && grep -c "NON-WIDENING" .planning/phases/324-terminal-delta-audit-3-skill-adversarial-sweep-closure/324-01-DELTA-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - 324-01-DELTA-AUDIT.md enumerates all 18 changed contract files (from `git diff 16e9668a fabe9e94 -- contracts/`), grouped by the 7 work items, each surface carrying a NON-WIDENING verdict backed by a concrete grep/diff anchor.
    - The BURNIE-lootbox + earlybird kill-sets are shown grep-ZERO in mainnet contracts/ (the contracts/test/ doc-comment survivor explicitly noted as non-mainnet).
    - The presaleOver latch, Pool.PresaleBox rename, _resolveLootboxCommon 2-bool collapse, per-currency caps, redemption segregation, and AfKing in-place tombstone are each anchor-confirmed against the frozen subject.
    - A Composition Attestation Matrix (ADD x REMOVE / claimable-balance / BURNIE-net-0 / RNG-freeze-intact) mirrors v46 §3.B.
    - `git diff fabe9e94 HEAD -- contracts/` is empty (zero contract mutation).
  </acceptance_criteria>
  <done>All 7 surfaces attested NON-WIDENING with grep/diff evidence; the delta-surface table + composition matrix are written to 324-01-DELTA-AUDIT.md; contracts/ byte-identical to fabe9e94.</done>
</task>

<task type="auto">
  <name>Task 2: Regression-baseline attestation + handoff dispositions (VRFLifecycle recalibration + OBS-1)</name>
  <read_first>
    - .planning/phases/323-tst-repro-first-same-results-gas-behavior-ev-cancel-tombstone-proofs/323-01-SUMMARY.md (the 559/51/16 baseline + 12-new-vs-v46 classification)
    - .planning/phases/323-tst-repro-first-same-results-gas-behavior-ev-cancel-tombstone-proofs/323-09-SUMMARY.md (the FINAL 598/38/16 baseline + the full 38-failure classification table — 32 pre-existing-v46 / 5 combined-run noise / 1 v47-PRESALE-delta)
    - .planning/phases/323-tst-repro-first-same-results-gas-behavior-ev-cancel-tombstone-proofs/deferred-items.md (the VRFLifecycle recalibration + 0x11-cluster disposition)
    - audit/FINDINGS-v46.0.md §5 (the LEAN regression appendix format to mirror)
    - 324-01-DELTA-AUDIT.md (the file being appended to)
  </read_first>
  <files>.planning/phases/324-terminal-delta-audit-3-skill-adversarial-sweep-closure/324-01-DELTA-AUDIT.md</files>
  <action>
    Append a Regression-Baseline Attestation section to 324-01-DELTA-AUDIT.md mirroring v46 §5. Attest the v47 foundry baseline 598/38/16 NON-WIDENING vs the v46 baseline 565/45/16: cite the 323-09 classification (32 pre-existing-v46 byte-identical at 16e9668a + 5 combined-run fuzz/cache noise that pass in isolation + 1 v47-behavioral PRESALE delta). Disposition the two STATE.md "324 handoff" items for the economic re-verify:
    - VRFLifecycle recalibration: classify VRFLifecycle::test_vrfLifecycle_levelAdvancement as a v47-PRESALE-economics test-calibration delta (the purchase-volume magic numbers were calibrated for the v46 prize-pool split; v47's rake-removal + presale-box 40%-to-nextPrizePool split changed per-purchase accumulation so the same loop no longer bootstraps to 50 ETH) — the SPEC behaving as intended, NOT a contract defect. Record the recommended disposition (recalibrate the test's purchase-volume to the v47 split) as a v47-internal test note, NOT a finding.
    - OBS-1 (Decimator under-reservation): classify as a PRE-EXISTING observation (not a v47 delta — outside the v47 audit-subject surface set), carry it forward descriptively. Run the economic skeptic-filter lens on both (structural-protection + 3-condition EV) and record that neither elevates to a finding.
    Also state the REG-01-equivalent NON-WIDENING claim: every `git diff 16e9668a..fabe9e94 -- contracts/ test/` hunk is attributable to a known v47-scope commit (the batched IMPL diff fb29ed51 + the fabe9e94 liveness guard + the AGENT-committed test repairs). Read-only — no contracts/ or test/ edits.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && grep -c "598" .planning/phases/324-terminal-delta-audit-3-skill-adversarial-sweep-closure/324-01-DELTA-AUDIT.md && grep -c "VRFLifecycle" .planning/phases/324-terminal-delta-audit-3-skill-adversarial-sweep-closure/324-01-DELTA-AUDIT.md && grep -c "OBS-1" .planning/phases/324-terminal-delta-audit-3-skill-adversarial-sweep-closure/324-01-DELTA-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - The Regression-Baseline Attestation records 598/38/16 NON-WIDENING vs v46 565/45/16 with the 32/5/1 classification cited from 323-09.
    - VRFLifecycle::test_vrfLifecycle_levelAdvancement is dispositioned as a v47-PRESALE test-calibration delta (intended SPEC behavior), NOT a contract defect — with a recommended recalibration note.
    - OBS-1 is classified PRE-EXISTING (not a v47 delta) and carried forward descriptively.
    - Both handoff items pass the economic skeptic-filter (structural-protection + 3-condition EV) and neither elevates to a finding.
    - A REG-01-equivalent NON-WIDENING attestation states every contracts/+test/ hunk vs 16e9668a is attributable to a known v47-scope commit.
  </acceptance_criteria>
  <done>The regression baseline 598/38/16 is attested NON-WIDENING; VRFLifecycle + OBS-1 dispositioned with the skeptic-filter; the REG-01-equivalent claim is recorded — all appended to 324-01-DELTA-AUDIT.md.</done>
</task>

</tasks>

<verification>
- `git diff fabe9e94 HEAD -- contracts/` empty (zero contract mutation throughout).
- 324-01-DELTA-AUDIT.md contains the per-surface NON-WIDENING table (7 surfaces) + Composition Attestation Matrix + Regression-Baseline Attestation (598/38/16) + VRFLifecycle + OBS-1 dispositions.
- All 45 REQ IDs across the 8 categories appear referenced in the surface table (each surface lists its owning REQ-IDs).
</verification>

<success_criteria>
SC1 satisfied: every contract surface changed vs the v46.0 baseline is enumerated and attested NON-WIDENING; the 598/38/16 regression baseline is attested NON-WIDENING; the VRFLifecycle recalibration + OBS-1 handoff items are dispositioned in the economic re-verify; contracts/ is byte-identical to fabe9e94.
</success_criteria>

<output>
Create `.planning/phases/324-terminal-delta-audit-3-skill-adversarial-sweep-closure/324-01-SUMMARY.md` when done
</output>
