---
phase: 323-tst-repro-first-same-results-gas-behavior-ev-cancel-tombstone-proofs
plan: 02
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: []                # pure-repair precondition for the hardhat baseline; TOMB-05 artifact owned exclusively by 323-01
files_modified:
  - test/gas/LootboxOpenGas.test.js
  - test/gas/Phase268GasRegression.test.js
  - test/integration/CrossSurfaceTicketMixing.test.js
  - test/stat/DegenerettePerNEvExactness.test.js
  - test/unit/DegenerusStonk.test.js
  - test/unit/DegenerusVault.test.js
  - test/unit/EventSurfaceUnification.test.js
  - test/unit/LootboxAutoResolveSilentColdBust.test.js
  - test/unit/LootboxConsolation.test.js
  - test/unit/LootboxWholeTicket.test.js
must_haves:
  truths:
    - "hardhat compile completes clean"
    - "each repaired *.test.js file runs to completion (no 'function does not exist' / 'event fragment missing' runtime aborts caused by removed v47 symbols)"
    - "the Pool enum in JS tests reflects Pool.PresaleBox at ordinal 4 (was Earlybird)"
    - "MAX_SPINS_PER_BET literals in JS tests reflect the per-currency caps (ETH 25 / BURNIE 15 / WWXRP 5)"
    - "tests for the removed BURNIE-lootbox surface are removed (the surface is gone by design), not skipped to hide a failure"
  artifacts:
    - path: "test/unit/EventSurfaceUnification.test.js"
      provides: "Event-surface tests recompiled against the v47 ABI (BurnieLootOpen gone, 2-bool _resolveLootboxCommon)"
    - path: "test/unit/DegenerusStonk.test.js"
      provides: "Stonk pool tests against Pool.PresaleBox (was Earlybird)"
  key_links:
    - from: "test/unit/EventSurfaceUnification.test.js"
      to: "the v47 contract ABI"
      via: "iface.getEvent / contract method lookup"
      pattern: "getEvent\\(|\\.connect\\("
---

<objective>
Repair every HARDHAT (`*.test.js`) test that the v47.0 contract diff (`fb29ed51`) broke. Unlike the
foundry tree, JS has no static type check — these tests COMPILE but fail at RUNTIME when they call a
removed function (`game.openBurnieLootBox(...)` → no such method), look up a removed event
(`iface.getEvent("BurnieLootOpen")` → fragment missing), use a stale enum ordinal (`Pool.Earlybird`
now resolves to `PresaleBox`), or hard-code the retired `MAX_SPINS_PER_BET = 10`.

Purpose: restore a runnable hardhat suite so the v47 JS baseline is non-widening and the cross-surface
behavior is still exercised. This plan runs IN PARALLEL with 323-01 (disjoint files: JS vs .t.sol).
Output: a clean hardhat compile + a recorded v47 hardhat pass/fail baseline.

NO `contracts/*.sol` edits (subject FROZEN at `fb29ed51`). A repaired JS test that reveals a real
contract defect is a STOP-and-surface event, never a silent contract patch or a weakened assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/323-tst-repro-first-same-results-gas-behavior-ev-cancel-tombstone-proofs/323-CONTEXT.md
@.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-08-SUMMARY.md

<interfaces>
<!-- The v47 surface changes the JS tests must track. -->
REMOVED contract surface (calling/looking these up at runtime aborts the test):
  - game.openBurnieLootBox(address, uint48)        — removed (terminal-paradox closure)
  - game.purchaseBurnieLootbox(address, uint256)   — removed
  - vault.gamePurchaseBurnieLootbox(uint256)       — removed
  - event BurnieLootOpen(...)                       — removed from the ABI
  - BURNIE->tickets is KEPT (do not remove ticket-from-BURNIE tests)

CHANGED:
  - Pool enum: ordinal 4 was `Earlybird`, now `PresaleBox`. JS tests define a local
    `const Pool = { Whale:0, Affiliate:1, Lootbox:2, Reward:3, Earlybird:4 }` — rename the 4-key to
    `PresaleBox`. The earlybird *subsystem* (_awardEarlybirdDgnrs / _finalizeEarlybird) is gone; any
    test of earlybird *accrual mechanics* tests removed behavior and must be removed, but the POOL still
    exists (renamed) so pool-balance/transfer tests retarget to `Pool.PresaleBox`.
  - MAX_SPINS_PER_BET (=10) retired -> per-currency caps MAX_SPINS_ETH=25 / MAX_SPINS_BURNIE=15 /
    MAX_SPINS_WWXRP=5. JS tests with `const MAX_SPINS_PER_BET = 10;` must use the per-currency cap
    appropriate to the bet currency they exercise.
  - _resolveLootboxCommon is now 2-bool (was 5-bool); the 3 ETH callers roll full boons+passes; the
    10% haircut is fixed. Event-surface tests asserting the OLD return-tuple/bool shape retarget to
    the 2-bool surface or the surviving LootBoxOpened event.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retarget the removed BURNIE-lootbox surface + the Pool enum in the hardhat tests</name>
  <files>test/unit/EventSurfaceUnification.test.js, test/unit/LootboxConsolation.test.js, test/unit/LootboxWholeTicket.test.js, test/unit/LootboxAutoResolveSilentColdBust.test.js, test/gas/LootboxOpenGas.test.js, test/unit/DegenerusVault.test.js, test/integration/CrossSurfaceTicketMixing.test.js, test/unit/DegenerusStonk.test.js</files>
  <read_first>
    test/unit/EventSurfaceUnification.test.js, test/unit/LootboxConsolation.test.js,
    test/unit/LootboxWholeTicket.test.js, test/unit/LootboxAutoResolveSilentColdBust.test.js,
    test/gas/LootboxOpenGas.test.js, test/unit/DegenerusVault.test.js,
    test/integration/CrossSurfaceTicketMixing.test.js, test/unit/DegenerusStonk.test.js
    + the `interfaces` block above.
  </read_first>
  <action>
    Apply, per file (intent-preserving — remove only what tests genuinely-removed behavior; retarget
    everything else; never add `.skip` to hide a real failure):
    - `EventSurfaceUnification.test.js`: the `BurnieLootOpen` event-fragment assertions ([01b] etc.)
      now test a removed event. Remove those describe/it blocks (the event is gone by design). Keep the
      `LootBoxOpened` / `JackpotTicketWin` assertions; if they asserted a return-shape tied to the old
      5-bool `_resolveLootboxCommon`, retarget to the 2-bool surface.
    - `LootboxConsolation.test.js` / `LootboxWholeTicket.test.js` / `LootboxAutoResolveSilentColdBust.test.js`:
      remove the `openBurnieLootBox` cold-bust / scaled-ticket / consolation cases that exercised the
      removed BURNIE-lootbox path. Keep the ETH-lootbox (`openLootBox` / `resolveLootboxDirect`) cases —
      those callers now roll full boons+passes with the 10% haircut fixed; update expectations if the
      old test pinned the haircut'd value (the haircut fix is a v47 behavior change → update to the
      corrected value, document it as v47-delta in the SUMMARY).
    - `LootboxOpenGas.test.js`: remove the `openBurnieLootBox` per-open gas-envelope describe block.
      Keep the ETH-lootbox gas envelope.
    - `DegenerusVault.test.js`: remove the `gamePurchaseBurnieLootbox` revert-on-zero test (the wrapper
      is gone). Keep the rest of the vault surface tests.
    - `CrossSurfaceTicketMixing.test.js`: the comment/flow referencing `openBurnieLootBox` end-to-end —
      drop the BURNIE-lootbox leg of the cross-surface mix; keep the ETH-lootbox + BURNIE->tickets legs.
    - `DegenerusStonk.test.js`: rename the local `Pool` enum key `Earlybird: 4` → `PresaleBox: 4`;
      retarget `poolBalance(Pool.Earlybird)` / `transferBetweenPools(Pool.Earlybird, ...)` to
      `Pool.PresaleBox`. If a test asserts earlybird ACCRUAL (~11.43% award mechanics from the deleted
      `_awardEarlybirdDgnrs`), that behavior is removed — drop that specific assertion; the pool itself
      (renamed) still exists for balance/transfer.
  </action>
  <verify>
    <automated>npx hardhat compile 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    `npx hardhat compile` is clean. No JS test references `openBurnieLootBox` / `purchaseBurnieLootbox`
    / `gamePurchaseBurnieLootbox` / `BurnieLootOpen` against the live contract (only as removed-by-design
    deletions, not `.skip`). `Pool.PresaleBox` replaces `Pool.Earlybird`. BURNIE->tickets tests are KEPT.
  </acceptance_criteria>
  <done>Removed-surface references retargeted/deleted; enum updated; hardhat compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the per-currency spin-cap literals in the Degenerette/Phase268 gas + EV JS tests</name>
  <files>test/gas/Phase268GasRegression.test.js, test/stat/DegenerettePerNEvExactness.test.js</files>
  <read_first>
    test/gas/Phase268GasRegression.test.js, test/stat/DegenerettePerNEvExactness.test.js
    + the per-currency caps in the `interfaces` block.
  </read_first>
  <action>
    - `Phase268GasRegression.test.js`: the hard-coded `const MAX_SPINS_PER_BET = 10;` (and the comments
      citing `.sol L226`) reflect the retired single cap. Replace with the per-currency cap matching the
      currency the regression exercises — if it benches ETH bets use 25 (MAX_SPINS_ETH); if it is a
      generic per-spin gas regression keep the spin count it actually loops but rename the constant to
      the correct per-currency name and update the comment to cite the new caps. This is a GAS regression
      test — if the new cap changes the bench's spin count, the absolute gas number will change; record
      the new baseline number and note it is a v47-delta (more spins per bet at the raised cap); do NOT
      treat a higher-but-expected gas number as a failure.
    - `DegenerettePerNEvExactness.test.js`: same `const MAX_SPINS_PER_BET = 10;` → per-currency cap.
      This is a STATISTICAL EV-exactness test (per-N expected value). The write-batching is same-results
      by design (DGAS), so the per-N EV must be UNCHANGED — if the test fails on EV after only the cap
      rename, that is a possible real regression: STOP and surface it (do not adjust the expected EV to
      match a wrong output). The cap rename alone should not move EV; only the reachable N range widens.
  </action>
  <verify>
    <automated>grep -rnE "MAX_SPINS_PER_BET" test/gas/Phase268GasRegression.test.js test/stat/DegenerettePerNEvExactness.test.js | grep -vE "MAX_SPINS_(ETH|BURNIE|WWXRP)" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    No `MAX_SPINS_PER_BET` literal survives in either file (grep returns 0). The constants use the
    per-currency names. EV-exactness assertions are UNCHANGED in expected value (same-results); any EV
    drift is surfaced as a STOP, not adjusted away.
  </acceptance_criteria>
  <done>Spin-cap literals updated to per-currency; EV-exactness preserved or surfaced.</done>
</task>

<task type="auto">
  <name>Task 3: Record the v47 HARDHAT regression baseline (non-widening check)</name>
  <files>(no file edit — runs npx hardhat test, records classification in SUMMARY)</files>
  <read_first>323-CONTEXT.md scope_fence §5 (baseline ~565/~44-45), 322-08-SUMMARY.md.</read_first>
  <action>
    Run the hardhat suite to completion and capture pass/fail:
    `npx hardhat test 2>&1 | tee /tmp/323-hardhat-baseline.txt`. For each remaining failure, classify it
    pre-existing-v46 OR attributable-to-a-v47-delta (e.g. the 10%-haircut-fix value change, the raised
    spin caps' gas number). Any failure that should pass with no v47 explanation → STOP and surface as a
    possible real contract defect (do NOT edit contracts/, do NOT weaken the test). Record the final
    hardhat pass/fail count + per-failure classification in the SUMMARY for the 323-VERIFICATION
    non-widening attestation.
  </action>
  <verify>
    <automated>npx hardhat test >/tmp/323-hardhat-baseline.txt 2>&1; grep -E "passing|failing|pending" /tmp/323-hardhat-baseline.txt | tail -5</automated>
  </verify>
  <acceptance_criteria>
    The hardhat suite runs to completion. A pass/fail count is recorded. Every failure is classified
    (pre-existing-v46 / v47-delta / surfaced-defect). Non-widening relative to v46 (no unexplained NEW
    failure); any unexplained failure surfaced, not patched.
  </acceptance_criteria>
  <done>A clean, classified v47 hardhat pass/fail baseline is recorded in the SUMMARY.</done>
</task>

</tasks>

<verification>
- `npx hardhat compile` clean; `npx hardhat test` runs to completion; pass/fail recorded + classified.
- No live reference to a removed v47 symbol (`openBurnieLootBox` / `BurnieLootOpen` / `gamePurchaseBurnieLootbox` / `MAX_SPINS_PER_BET`) in the repaired JS files.
- `Pool.PresaleBox` replaces `Pool.Earlybird`.
- Zero `contracts/*.sol` (mainnet) modifications.
</verification>

<success_criteria>
The hardhat suite compiles and runs; removed-surface tests are deleted (not skipped); the enum + spin
caps reflect v47; a classified non-widening v47 hardhat baseline is recorded; no contract was edited
and no real defect was papered over.
</success_criteria>

<output>
Create `.planning/phases/323-tst-repro-first-same-results-gas-behavior-ev-cancel-tombstone-proofs/323-02-SUMMARY.md` when done.
Record: the final hardhat pass/fail count, the per-failure classification, the list of describe/it
blocks removed (removed-by-design) vs retargeted, and any v47-delta value changes (10%-haircut fix,
raised-cap gas numbers).
</output>
