---
phase: 323-tst-repro-first-same-results-gas-behavior-ev-cancel-tombstone-proofs
plan: 03
type: execute
wave: 2
depends_on: ["323-01"]
autonomous: true
requirements: [REDEEM-08]
files_modified:
  - test/fuzz/StakedStonkRedemption.t.sol
  - test/invariant/RedemptionAccounting.t.sol
  - test/fuzz/handlers/RedemptionHandler.sol
must_haves:
  truths:
    - "the two-claimant same-day ETH underflow repro FAILS against the pre-fix contract (fb29ed51^) and PASSES post-fix — recorded evidence"
    - "BURNIE-can't-block-ETH: a redeemer whose BURNIE payout exceeds held+stake still receives the full ETH leg at claim"
    - "no unchecked claimable subtraction survives in the redemption path (the only claimableWinnings[SDGNRS] debit is the CHECKED pullRedemptionReserve)"
    - "BURNIE net mint == 0 across submit/resolve/claim (redeemBurnieShare burns held + consumes stake to offset the deferred creditFlip)"
    - "address(this).balance >= pendingRedemptionEthValue holds at all times (ETH conservation / physical segregation)"
  artifacts:
    - path: "test/fuzz/StakedStonkRedemption.t.sol"
      provides: "REDEEM-08 repro-first tests (two-claimant underflow, BURNIE-can't-block-ETH)"
      contains: "fb29ed51"
    - path: "test/invariant/RedemptionAccounting.t.sol"
      provides: "REDEEM-08 conservation invariants (balance >= pendingRedemptionEthValue; net BURNIE == 0; no unchecked debit)"
  key_links:
    - from: "test/fuzz/StakedStonkRedemption.t.sol"
      to: "contracts/DegenerusGame.sol pullRedemptionReserve / resolveRedemptionLootbox"
      via: "submit -> segregate -> resolve -> claim flow against the v47 signatures"
      pattern: "pullRedemptionReserve|resolveRedemptionLootbox"
---

<objective>
Prove the sDGNRS redemption-accounting fix (REDEEM-08) EMPIRICALLY, repro-first. The pre-fix contract
(`fb29ed51^`) had an UNCHECKED `claimableWinnings[SDGNRS] -= amount` debit inside
`resolveRedemptionLootbox` (confirmed at `fb29ed51^:DegenerusGame.sol:1802-1804`) that a second
same-day claimant (or an AfKing SUB-09 drain) could underflow toward 2^256, plus a BURNIE leg that
could block the ETH payout. The v47 fix DELETED that unchecked debit, made `resolveRedemptionLootbox`
`external payable` (ETH physically arrives), added the CHECKED SDGNRS-gated `pullRedemptionReserve`,
and settled BURNIE at submit (`redeemBurnieShare`, net new BURNIE == 0).

Purpose: a C4A warden's headline finding for this surface is the two-claimant underflow; the repro
must demonstrably FAIL pre-fix and PASS post-fix so the audit can attest it RESOLVED.
Output: repro-first tests + conservation invariants in the redemption test homes.

REPRO-FIRST MECHANISM (REQUIRED): write the two-claimant-underflow repro FIRST and demonstrate it
FAILS against the pre-fix code, then PASSES against the current (post-fix) tree. Capture the pre-fix
failure evidence in the SUMMARY. Two acceptable mechanisms (use the first; the second is the fallback):
  (1) Run the repro test file against the pre-fix tree: `git stash` any WIP, then in a scratch worktree
      `git worktree add /tmp/prefix fb29ed51^` and copy the repro test in, OR `git checkout fb29ed51^ --
      contracts/DegenerusGame.sol` in a throwaway branch, run the repro, observe the underflow/wrap
      assertion FAIL (the pre-fix test should show the wrap), then restore `contracts/` to fb29ed51.
  (2) In a scratch checkout, temporarily re-introduce ONLY the deleted unchecked debit block
      (the `uint256 claimable = ...; unchecked { claimableWinnings[SDGNRS] = claimable - amount; }
      claimablePool -= uint128(amount);` at the old :1802-1804) into a COPY of DegenerusGame.sol,
      run the repro to show it wraps, then discard the copy.
DO NOT leave `contracts/` modified — the subject stays FROZEN at fb29ed51. Record which mechanism was
used + the pre-fix failure output in the SUMMARY.

NO permanent `contracts/*.sol` edits. If a NEW (not-yet-known) real defect surfaces, STOP and surface it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/323-tst-repro-first-same-results-gas-behavior-ev-cancel-tombstone-proofs/323-CONTEXT.md
@.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-04-SUMMARY.md

<interfaces>
<!-- v47 redemption surface (post-fix). All extracted from contracts/ at fb29ed51. -->
DegenerusGame.sol:
```solidity
function resolveRedemptionLootbox(address player, uint256 amount, uint256 rngWord, uint16 activityScore) external payable;
  // gate msg.sender == SDGNRS; reverts if msg.value != amount; NO claimable debit; credits futurePrizePool from msg.value
function pullRedemptionReserve(uint256 amount) external;
  // gate msg.sender == SDGNRS; CHECKED claimableWinnings[SDGNRS] -= amount; claimablePool -= amount; sends amount to SDGNRS (CEI)
  // THIS IS THE ONLY surviving claimableWinnings[SDGNRS] debit and it is CHECKED (0.8 reverts on shortfall)
```
StakedDegenerusStonk.sol:
```solidity
function _submitGamblingClaimFrom(address beneficiary, address burnFrom, uint256 amount) private; // pulls MAX(175%) into segregated balance; fail-closed
function resolveRedemptionPeriod(uint16 roll, uint32 dayToResolve) external;                       // MAX -> rolled, accounting only (2-arg)
function claimRedemption(uint32 day) external;                                                      // ETH-only; pays ethDirect FIRST from segregated balance
uint256 public pendingRedemptionEthValue;                                                           // total physically-segregated ETH
struct PendingRedemption { uint96 ethValueOwed; uint16 activityScore; }                             // burnieOwed DELETED
struct RedemptionPeriod  { uint16 roll; }                                                           // flipDay DELETED
event RedemptionSubmitted(address indexed player, uint256 sdgnrsAmount, uint256 ethValueOwed, uint256 burnieSettled, uint32 periodIndex);
```
BurnieCoinflip.sol:
```solidity
function redeemBurnieShare(address redeemer, uint256 base) external; // SDGNRS-gated; burns held + consumes stake to offset deferred creditFlip; NET new BURNIE == 0
```
PRE-FIX defect anchor (fb29ed51^:DegenerusGame.sol:1802-1804): the deleted unchecked block
  `uint256 claimable = claimableWinnings[SDGNRS]; unchecked { claimableWinnings[SDGNRS] = claimable - amount; } claimablePool -= uint128(amount);`
MAX_ROLL = 175 (sDGNRS), the segregation multiple.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: REDEEM-08 repro-first — two-claimant same-day ETH underflow (fail pre-fix, pass post-fix)</name>
  <files>test/fuzz/StakedStonkRedemption.t.sol</files>
  <read_first>
    test/fuzz/StakedStonkRedemption.t.sol (existing redemption harness/setup),
    322-04-SUMMARY.md (Defect A: the unchecked debit; the segregation + pullRedemptionReserve fix),
    + the `interfaces` block (pre-fix defect anchor + post-fix signatures).
  </read_first>
  <action>
    Write `testReproTwoClaimantSameDayNoUnderflow` (or extend the existing redemption harness) that
    reproduces Defect A: two redeemers submit gambling-burn claims for the SAME day; both resolve; both
    claim. Pre-fix, the second claim's `resolveRedemptionLootbox` ran the UNCHECKED
    `claimableWinnings[SDGNRS] -= amount` after the first claimant already drew claimable down, wrapping
    `claimableWinnings[SDGNRS]` toward 2^256. Assert the post-fix invariant: after BOTH claims,
    `claimableWinnings[SDGNRS]` is NOT anywhere near 2^256 (e.g. `< type(uint96).max` or a tight bound on
    the legitimate residual) AND `claimablePool == Σ claimableWinnings` stays balanced. The post-fix path
    uses the CHECKED `pullRedemptionReserve` (segregation at submit), so the wrap can never form.

    THEN run the repro-first proof: execute this exact test against the PRE-FIX contract (mechanism (1)
    or (2) from the objective). Observe it FAIL (the wrap assertion trips, or the test shows
    claimableWinnings[SDGNRS] near 2^256). Capture that failing output. Restore `contracts/` to fb29ed51
    and confirm the test PASSES. Record both the pre-fix FAIL and post-fix PASS evidence in the SUMMARY.

    Also cover the AfKing SUB-09 drain variant (C5): the segregation `pullRedemptionReserve` is CHECKED
    and fail-closed — if claimable[SDGNRS] was drained below the MAX(175%) at submit, the burn REVERTS
    (the redeemer retries later) rather than leaving a virtual reserve a later drain could underflow.
    Assert the submit reverts fail-closed when claimable is short, and succeeds once it recovers.
  </action>
  <verify>
    <automated>forge test --match-test testReproTwoClaimantSameDayNoUnderflow -vvv 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    `testReproTwoClaimantSameDayNoUnderflow` PASSES against the current (post-fix) tree. The SUMMARY
    records the pre-fix FAILURE evidence (the wrap/underflow tripping against fb29ed51^ or the
    re-introduced unchecked block) and the post-fix PASS. The fail-closed AfKing-drain variant asserts
    the submit reverts on shortfall and succeeds on recovery. `contracts/` is restored to fb29ed51
    (clean) after the pre-fix run.
  </acceptance_criteria>
  <done>The two-claimant underflow repro fails pre-fix and passes post-fix, evidence recorded; the fail-closed drain variant passes.</done>
</task>

<task type="auto">
  <name>Task 2: REDEEM-08 — BURNIE-can't-block-ETH + the _settleClaimableShortfall/2-arg/burnForCoinflip refinement coverage</name>
  <files>test/fuzz/StakedStonkRedemption.t.sol</files>
  <read_first>
    test/fuzz/StakedStonkRedemption.t.sol, 322-04-SUMMARY.md (redeemBurnieShare net-zero proof;
    BurnsBlockedDuringLiveness re-check), 322-08-SUMMARY.md (R1 burnForCoinflip; R3 _settleClaimableShortfall;
    R4 resolveRedemptionPeriod 2-arg) + the `interfaces` block.
  </read_first>
  <action>
    Write `testBurnieCannotBlockEthLeg`: construct a redemption where the redeemer's BURNIE share
    (held + coinflip stake) is LESS than the deferred creditFlip `base` would naively require if BURNIE
    blocked — i.e. a case the pre-fix BURNIE-reserve apparatus could have stalled. Assert the ETH leg of
    `claimRedemption` STILL pays in full from the segregated balance (`_payEth` pays ethDirect FIRST,
    independent of any BURNIE path). The post-fix design settles BURNIE entirely at submit via
    `redeemBurnieShare`, so by claim time there is NO BURNIE leg to block ETH — assert the claim's ETH
    payout equals the rolled segregated amount regardless of BURNIE.

    Add refinement coverage (from 322-08 R1/R3/R4):
    - `burnForCoinflip` redemption-burn (R1): assert BURNIE net mint == 0 across a full
      submit→resolve→claim — measure `BURNIE.totalSupply()` (and SDGNRS held + previewClaimCoinflips)
      before vs after; net delta == 0 (redeemBurnieShare burns `burnFromHeld` + consumes `remainder`
      stake to exactly offset the deferred mint of `base`).
    - `resolveRedemptionPeriod` 2-arg path (R4): assert the resolve call with the NEW 2-arg signature
      `(roll, dayToResolve)` resolves the period (sets `redemptionPeriods[day].roll`, lowers
      `pendingRedemptionEthValue` MAX→rolled) — i.e. the AdvanceModule-shaped 2-arg call works.
    - `_settleClaimableShortfall` invariant (R3): this helper is used by 5 callers (3 WhaleModule + 2
      MintModule, NOT the redemption path) — assert `claimablePool == Σ claimableWinnings` stays balanced
      across a claimable-pay purchase that triggers the shortfall sentinel (msg.value + claimable
      shortfall). A focused check that the paired claimable/pool debit keeps the global invariant is
      sufficient; this is a refinement-coverage assertion, not a full re-audit of the whale path.
  </action>
  <verify>
    <automated>forge test --match-test "testBurnieCannotBlockEthLeg|testRedeemBurnieNetMintZero|testResolveRedemptionPeriod2Arg|testSettleClaimableShortfallInvariant" -vv 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    `testBurnieCannotBlockEthLeg` PASSES (ETH leg pays in full irrespective of BURNIE). The R1 net-mint
    test shows `ΔBURNIE supply == 0` across submit/resolve/claim. The R4 2-arg `resolveRedemptionPeriod`
    call resolves the period. The R3 `_settleClaimableShortfall` check keeps `claimablePool == Σ
    claimableWinnings` balanced on a claimable-pay purchase.
  </acceptance_criteria>
  <done>BURNIE-can't-block-ETH + the R1/R3/R4 refinement coverage all pass.</done>
</task>

<task type="auto">
  <name>Task 3: REDEEM-08 conservation invariants — balance >= pendingRedemptionEthValue; no unchecked claimable debit</name>
  <files>test/invariant/RedemptionAccounting.t.sol, test/fuzz/handlers/RedemptionHandler.sol</files>
  <read_first>
    test/invariant/RedemptionAccounting.t.sol (existing invariant harness + handler at
    test/fuzz/handlers/RedemptionHandler.sol), 322-04-SUMMARY.md (Invariants section),
    + the `interfaces` block.
  </read_first>
  <action>
    Strengthen the redemption-accounting invariant suite to assert REDEEM-08 conservation across the
    randomized submit/resolve/claim/gameOver action sequence (the handler drives these):
    - `invariant_balanceCoversPendingRedemptionEth`: `address(sdgnrs).balance >= pendingRedemptionEthValue`
      at ALL times (the segregated ETH physically lives in sDGNRS; the over-pull MAX→rolled stays as free
      backing — telescoping = zero drift, so the balance always covers the obligation).
    - `invariant_claimablePoolEqualsSumClaimable`: `claimablePool == Σ claimableWinnings` stays balanced
      through every redemption action (pullRedemptionReserve decrements both by the same amount + moves
      real ETH out; resolveRedemptionLootbox touches no claimable).
    - `invariant_noUncheckedClaimableDebitInRedemptionPath`: a STRUCTURAL grep-style assertion (run as a
      test that reads the compiled/source surface) — confirm the only `claimableWinnings[SDGNRS]` debit
      reachable in the redemption path is the CHECKED `pullRedemptionReserve` and there is NO `unchecked`
      block wrapping a claimable subtraction. (If the invariant framework can't introspect source, assert
      the behavioral equivalent: drive the handler to a state that WOULD underflow a pre-fix unchecked
      debit and confirm the post-fix path reverts/segregates instead of wrapping.)
    Update the handler (`RedemptionHandler.sol`) if needed so its actions reach the v47 2-arg resolve +
    payable resolveRedemptionLootbox + segregation paths (the handler was repaired for compile in 323-01;
    here ensure its action set actually EXERCISES the new flow).
  </action>
  <verify>
    <automated>forge test --match-contract RedemptionAccounting 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    The RedemptionAccounting invariant suite PASSES with the three REDEEM-08 conservation invariants
    asserting: `balance >= pendingRedemptionEthValue` always, `claimablePool == Σ claimableWinnings`
    balanced, and no-unchecked-claimable-debit (structural or behavioral). The handler drives the v47
    segregation/payable/2-arg flow.
  </acceptance_criteria>
  <done>The conservation invariants hold across the randomized redemption action sequence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| redeemer (EOA) -> sDGNRS submit | untrusted amount; could try a second same-day claim to underflow |
| AfKing SUB-09 self-sub -> claimableWinnings[SDGNRS] | a daily drain that could undercut the segregation reserve |
| sDGNRS -> DegenerusGame.resolveRedemptionLootbox / pullRedemptionReserve | the only claimable[SDGNRS] debit path |

## STRIDE Threat Register (proof-logic relevance only — test phase)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-323-01 | Tampering | claimableWinnings[SDGNRS] underflow (two-claimant / drain) | mitigate | Task 1 repro proves the CHECKED pullRedemptionReserve + segregation can't wrap; fail-closed on shortfall |
| T-323-02 | Denial of Service | BURNIE leg blocking ETH payout | mitigate | Task 2 proves the ETH leg pays independent of BURNIE (settled at submit, net mint 0) |
| T-323-03 | Information Disclosure | conservation drift (balance < obligation) | mitigate | Task 3 invariant: balance >= pendingRedemptionEthValue always |
</threat_model>

<verification>
- Two-claimant underflow repro FAILS pre-fix, PASSES post-fix (evidence in SUMMARY).
- BURNIE-can't-block-ETH passes; net BURNIE mint == 0; 2-arg resolve + shortfall-helper invariant pass.
- Conservation invariants (balance >= pendingRedemptionEthValue; claimablePool == Σ claimableWinnings; no unchecked claimable debit) hold.
- `contracts/` restored to fb29ed51 (clean) after the pre-fix run.
</verification>

<success_criteria>
REDEEM-08 is proven empirically repro-first: the two-claimant underflow and BURNIE-can't-block-ETH
defects are demonstrated fixed (fail pre-fix, pass post-fix), conservation holds across the randomized
sequence, and the R1/R3/R4 refinements are covered — with the contract subject left FROZEN.
</success_criteria>

<output>
Create `.planning/phases/323-tst-repro-first-same-results-gas-behavior-ev-cancel-tombstone-proofs/323-03-SUMMARY.md` when done.
Record: the pre-fix FAILURE evidence + the repro mechanism used; the post-fix PASS; the net-BURNIE-mint
delta (== 0); the conservation-invariant results; confirmation `contracts/` is clean at fb29ed51.
</output>
