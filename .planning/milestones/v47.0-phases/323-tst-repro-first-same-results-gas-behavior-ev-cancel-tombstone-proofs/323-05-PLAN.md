---
phase: 323-tst-repro-first-same-results-gas-behavior-ev-cancel-tombstone-proofs
plan: 05
type: execute
wave: 2
depends_on: ["323-01"]
autonomous: true
requirements: [TOMB-04]
files_modified:
  - test/fuzz/AfKingConcurrency.t.sol
  - test/fuzz/CrankNonBrick.t.sol
must_haves:
  truths:
    - "cancel (setDailyQuantity(0)) relocates NO ONE — a pending tail entry is never pushed behind the sweep cursor (no missed day)"
    - "a tombstone is reclaimed by the next sweep that reaches it (in-loop swap-pop, no ++cursor; next-day reset self-heals the behind-cursor case)"
    - "a cancel with an unexpired paid window preserves _subOf through the deferred reclaim (preserve-vs-delete applied at reclaim, not at cancel)"
    - "reactivation (setDailyQuantity(q>0) or subscribe()) never double-adds set membership"
    - "the new didWork revert-fix: a reclaim-only / renewal-only chunk COMMITS (does not revert NoSubscribersSwept), so spam-cancel can't strand tombstones"
    - "the 318-04 guarantees still hold (exactly-once same-block, lastSweptDay backstop, no double-buy, no dead-slot buildup, two-tier skip-kill)"
  artifacts:
    - path: "test/fuzz/AfKingConcurrency.t.sol"
      provides: "TOMB-04 cancel-tombstone correctness tests + 318-04 re-confirmation"
      contains: "testCancelBehindCursorDoesNotStrandPendingTail"
    - path: "test/fuzz/CrankNonBrick.t.sol"
      provides: "TOMB-04 didWork revert-fix tests (reclaim/renewal-only chunk commits; spam-cancel no-strand)"
  key_links:
    - from: "test/fuzz/AfKingConcurrency.t.sol"
      to: "contracts/AfKing.sol setDailyQuantity / sweep"
      via: "cancel + chunked sweep + cursor introspection"
      pattern: "setDailyQuantity\\(0\\)|sweep\\("
---

<objective>
Prove the AfKing in-place cancel-tombstone (TOMB-04) restores SUB-07 and resolves the v46.0 MEDIUM
finding H-CANCEL-SWAP-MISS. The v47 fix made `setDailyQuantity(0)` a TRUE in-place tombstone (writes
the sentinel, relocates NO ONE — the old swap-pop is gone), with the delete-vs-preserve decision
DEFERRED to a new top-of-loop in-sweep reclaim branch (`AfKing.sol:611-633`, no `++cursor`), AND a
`didWork` revert-fix (`:594`/`:619`) so a reclaim/auto-pause/renewal-only chunk COMMITS instead of
reverting `NoSubscribersSwept` (which would re-strand tombstones for griefing).

Purpose: H-CANCEL-SWAP-MISS is the headline deferred finding for v47 — the cancel swap-pop relocated a
pending tail behind the chunked-sweep cursor → missed day → mint-streak reset (up to −50% activity).
The fix's correctness must be proven, and the 318-04 sweep guarantees re-confirmed non-regressed.
Output: the 4 TOMB-04 named tests + the new `didWork` cases + 318-04 re-confirmation.

NO `contracts/*.sol` edits (subject FROZEN at fb29ed51). If a test reveals the fix is incomplete (e.g.
a relocation still happens, or a tombstone strands), STOP and surface it — do NOT patch the contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/323-tst-repro-first-same-results-gas-behavior-ev-cancel-tombstone-proofs/323-CONTEXT.md
@.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-07-SUMMARY.md

<interfaces>
<!-- v47 AfKing cancel-tombstone surface (post-fix). From contracts/AfKing.sol at fb29ed51. -->
```solidity
struct Sub { uint8 dailyQuantity; uint32 lastSweptDay; uint32 paidThroughDay; uint8 reinvestPct; uint8 flags; address fundingSource; }
uint8 internal constant FLAG_WINDOW_PAID = 1;

// setDailyQuantity(0): TRUE in-place tombstone — sets s.dailyQuantity = 0, emits
//   SubscriptionUpdated(...,0,...), RETURNS. NO _removeFromSet (relocates no one). Deferred delete-vs-preserve.
// setDailyQuantity(q>0): reactivation — sets s.dailyQuantity = q, emits. NO set churn.
function setDailyQuantity(uint8 q) external;        // :460
function sweep(uint256 maxCount) external returns (uint256 bountyEarned);   // :567
function sweepProgress() external view returns (uint32 day, uint256 cursor); // :527

// sweep loop branch (0) — in-loop reclaim, ordered AHEAD of the lastSweptDay skip (:611-633):
//   if (sub.dailyQuantity == 0) {
//       bool preservePaidWindow = (sub.flags & FLAG_WINDOW_PAID) != 0 && sub.paidThroughDay > today;
//       if (!preservePaidWindow) delete _subOf[player];
//       _removeFromSet(player);                 // swap-pop, NO ++cursor
//       emit SubscriptionExpired(player, 2);    // reason 2 = CancelReclaim (1 = AutoPause)
//       ++processed; continue;                  // NO ++cursor -> swap-pop occupant re-read at THIS index
//   }
// didWork revert-fix (:594 decl, set true at :619/:643/:660/:670): tail reverts NoSubscribersSwept
//   ONLY when !didWork. A chunk that did a reclaim / auto-pause / renewal (but no buy, bounty 0) COMMITS.
error NoSubscribersSwept();   // :146
event SubscriptionExpired(address indexed player, uint8 reason);   // :189
```
Existing AfKingConcurrency tests to RE-CONFIRM (written for the OLD swap-pop cancel; revisit semantics):
  testCancelSwapPopOccupantStillProcessed, testNoDeadSlotBuildupAcrossCancels,
  testCancelPreservesPaidUnexpiredWindow, testCancelReclaimsUnpaidWindow, testSameBlockTwoSweepsExactlyOnce,
  testLastSweptDayBackstopBlocksRepeatBuyOnCursorRevisit, testCursorResetsPerDayAndEachSubBuysOncePerDay.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TOMB-04 — the 4 named cancel-tombstone correctness tests</name>
  <files>test/fuzz/AfKingConcurrency.t.sol</files>
  <read_first>
    test/fuzz/AfKingConcurrency.t.sol (existing chunked-sweep harness + cursor introspection +
    the existing cancel tests listed in `interfaces`), 322-07-SUMMARY.md (Edit 1 in-place tombstone,
    Edit 2 in-loop reclaim ordering, the behind-vs-ahead-of-cursor analysis) + the `interfaces` block.
  </read_first>
  <action>
    Author the 4 TOMB-04 named tests in `AfKingConcurrency.t.sol` (reuse the existing multi-sub +
    chunked `sweep(maxCount)` + `sweepProgress()` harness):
    - `testCancelBehindCursorDoesNotStrandPendingTail`: this is the H-CANCEL-SWAP-MISS repro. Set up N
      subscribers; begin a chunked sweep so the cursor is partway (e.g. swept the first chunk, cursor at
      index k); then cancel a sub that is AT or BEFORE the cursor (the case that, under the OLD swap-pop,
      relocated a still-pending TAIL entry into a behind-cursor slot, causing it to be missed for the
      day). Complete the day's sweep. Assert EVERY still-active sub got its daily buy exactly once (no
      pending tail entry was skipped) — i.e. the tail sub that the old swap-pop would have relocated still
      buys this day. Because the in-place tombstone relocates no one, no pending entry is ever pushed
      behind the cursor. (This is the direct mint-streak-preservation proof.)
    - `testCancelTombstoneReclaimedByNextSweep`: cancel a sub (in-place tombstone, still in the set);
      assert it is STILL in `_subscribers` immediately after cancel (relocates no one). Run the next sweep
      that reaches its slot; assert the reclaim branch fires (`SubscriptionExpired(player, 2)`), the sub
      is removed from the set, and the swap-pop occupant is re-processed at the same index (no skip).
      Cover BOTH sub-cases: tombstone AHEAD of cursor → reclaimed THIS sweep; tombstone BEHIND cursor
      (cancelled after cursor passed its slot this day) → reclaimed on the next-day cursor reset.
    - `testCancelPreservesPaidWindowThroughDeferredReclaim`: cancel a sub that has FLAG_WINDOW_PAID set
      AND paidThroughDay > today. Assert `_subOf` is PRESERVED through the deferred reclaim (the reclaim's
      `preservePaidWindow` branch keeps the record; only set membership is swap-popped). Confirm the paid
      window survives a cancel→(sweep reclaim) sequence. Contrast with an unpaid/expired-window cancel →
      `delete _subOf` at reclaim.
    - `testReactivateTombstonedSubNoDoubleAdd`: cancel (tombstone, still in set), then reactivate via
      `setDailyQuantity(q>0)` BEFORE any sweep reclaims it. Assert the sub flips back to active in place
      with NO duplicate `_subscribers` entry and NO double set membership (the deferred reclaim never ran,
      record intact, paid window survives the round-trip). Also cover the `subscribe()` reactivation path:
      a still-in-set tombstoned address re-subscribing → `_addToSet` is idempotent (non-zero index) → no
      double-add.
    If any test shows a relocation / a stranded tombstone / a double-add, STOP and surface it.
  </action>
  <verify>
    <automated>forge test --match-test "testCancelBehindCursorDoesNotStrandPendingTail|testCancelTombstoneReclaimedByNextSweep|testCancelPreservesPaidWindowThroughDeferredReclaim|testReactivateTombstonedSubNoDoubleAdd" -vv 2>&1 | tail -14</automated>
  </verify>
  <acceptance_criteria>
    All 4 named tests PASS: no pending tail stranded behind the cursor on cancel; tombstone reclaimed by
    the next reaching sweep (ahead-of-cursor this sweep, behind-cursor next-day reset); paid window
    preserved through deferred reclaim; reactivation (both paths) never double-adds. H-CANCEL-SWAP-MISS
    is demonstrably resolved.
  </acceptance_criteria>
  <done>The 4 TOMB-04 cancel-tombstone correctness tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: TOMB-04 — the new didWork revert-fix cases (reclaim/renewal-only chunk commits; spam-cancel no-strand)</name>
  <files>test/fuzz/CrankNonBrick.t.sol</files>
  <read_first>
    test/fuzz/CrankNonBrick.t.sol (existing non-brick / griefing harness), 322-08-SUMMARY.md (R2 the
    didWork revert-fix), 322-07-SUMMARY.md (the reclaim branch sets didWork) + the `interfaces` block.
  </read_first>
  <action>
    Author the new `didWork` revert-fix tests in `CrankNonBrick.t.sol` (the non-brick griefing home):
    - `testReclaimOnlyChunkCommitsNotReverts`: set up a sweep chunk that does ONLY a tombstone reclaim
      (no successful buy → bounty 0). Pre-fix, the tail `batchLen == 0 → revert NoSubscribersSwept` would
      roll back the reclaim, re-stranding the tombstone. Assert the sweep COMMITS (does NOT revert): the
      tombstone is removed from the set, `SubscriptionExpired(player, 2)` emitted, and the state change
      persists after the tx (verify by reading the set / `sweepProgress` after). Bounty may be 0 — assert
      it does not revert despite 0 bounty.
    - `testRenewalOrAutoPauseOnlyChunkCommits`: a chunk that does ONLY a renewal (FLAG_WINDOW_PAID flip)
      or ONLY an auto-pause (no buy) likewise COMMITS — assert the flag/state change persists, no revert.
    - `testSpamCancelCannotStrandTombstones`: drive a griefing scenario — many subs cancel (tombstone)
      in rapid succession across chunks, with chunk boundaries landing on reclaim-only work. Assert that
      over a full day's sweeps EVERY tombstone is eventually reclaimed (none permanently stranded) and no
      active sub's daily buy is missed. The combination of (a) in-place tombstone (no relocation) + (b)
      the didWork commit (reclaim-only chunks persist) guarantees no tombstone-stranding griefing vector.
    - Confirm a genuinely-empty chunk (no buy, no reclaim, no renewal, no auto-pause — `!didWork`) STILL
      reverts `NoSubscribersSwept` (the revert-fix only spares chunks that DID work; a truly-empty sweep
      still reverts as before).
    If a tombstone strands or a no-work chunk fails to revert, STOP and surface it.
  </action>
  <verify>
    <automated>forge test --match-test "testReclaimOnlyChunkCommitsNotReverts|testRenewalOrAutoPauseOnlyChunkCommits|testSpamCancelCannotStrandTombstones" -vv 2>&1 | tail -14</automated>
  </verify>
  <acceptance_criteria>
    The reclaim-only and renewal/auto-pause-only chunks COMMIT (no revert, state persists); spam-cancel
    strands NO tombstone over a full day and misses no active buy; a truly-empty (`!didWork`) chunk still
    reverts `NoSubscribersSwept`. The didWork revert-fix is proven to close the re-strand griefing vector.
  </acceptance_criteria>
  <done>The didWork revert-fix cases pass; spam-cancel cannot strand tombstones.</done>
</task>

<task type="auto">
  <name>Task 3: Re-confirm the 318-04 sweep guarantees against the new tombstone (non-regression)</name>
  <files>test/fuzz/AfKingConcurrency.t.sol</files>
  <read_first>
    test/fuzz/AfKingConcurrency.t.sol (the existing 318-04 guarantee tests + the old swap-pop cancel
    tests in `interfaces`), 322-07-SUMMARY.md (318-04 guarantees preserved section).
  </read_first>
  <action>
    The existing AfKingConcurrency tests `testCancelSwapPopOccupantStillProcessed`,
    `testNoDeadSlotBuildupAcrossCancels`, `testCancelPreservesPaidUnexpiredWindow`,
    `testCancelReclaimsUnpaidWindow` were written for the OLD immediate-swap-pop cancel. Re-confirm their
    INTENT holds under the new in-place tombstone + deferred reclaim:
    - `testCancelSwapPopOccupantStillProcessed`: the swap-pop now happens at RECLAIM (in-loop), not at
      cancel. Retarget the test so the swap-pop it asserts is the reclaim's swap-pop (occupant re-read at
      THIS index, no ++cursor). Intent unchanged: the occupant is still processed.
    - `testNoDeadSlotBuildupAcrossCancels`: assert that across many cancels, dead tombstones are always
      reclaimed (this sweep if ahead of cursor, else next-day reset) → no permanent dead-slot buildup. The
      net set effect equals the old immediate-swap-pop, just deferred.
    - `testCancelPreservesPaidUnexpiredWindow` / `testCancelReclaimsUnpaidWindow`: the preserve-vs-delete
      decision now fires at RECLAIM (Edit 2's `preservePaidWindow`), not at cancel. Retarget so the
      assertion observes the outcome AFTER the reclaiming sweep (paid-unexpired → `_subOf` preserved;
      unpaid/expired → deleted). Intent unchanged.
    Then run the broader 318-04 invariants and assert they still pass UNCHANGED: exactly-once same-block
    (`testSameBlockTwoSweepsExactlyOnce`), `lastSweptDay` backstop
    (`testLastSweptDayBackstopBlocksRepeatBuyOnCursorRevisit`), per-day cursor reset + one-buy-per-day
    (`testCursorResetsPerDayAndEachSubBuysOncePerDay`), no-double-buy. If any 318-04 guarantee regressed
    (e.g. a double-buy now possible, or the backstop bypassed), STOP and surface it.
  </action>
  <verify>
    <automated>forge test --match-contract AfKingConcurrency 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    The full `AfKingConcurrency` suite PASSES: the retargeted cancel tests assert the deferred-reclaim
    semantics (swap-pop at reclaim, preserve-vs-delete at reclaim, no dead-slot buildup), and the 318-04
    guarantees (exactly-once same-block, lastSweptDay backstop, per-day reset one-buy, no double-buy,
    two-tier skip-kill) hold UNCHANGED. Any regression surfaced, not patched.
  </acceptance_criteria>
  <done>The 318-04 guarantees are re-confirmed non-regressed against the in-place tombstone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canceller (EOA) -> setDailyQuantity(0) | a cancel that, pre-fix, could relocate a pending tail behind the cursor (H-CANCEL-SWAP-MISS) |
| spam-canceller -> chunked sweep boundaries | a griefer forcing reclaim-only chunks to revert (pre-fix) and re-strand tombstones |

## STRIDE Threat Register (proof-logic relevance only — test phase)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-323-04 | Tampering | cancel relocating a pending tail behind the sweep cursor (mint-streak reset) | mitigate | Task 1 proves in-place tombstone relocates no one; no missed day |
| T-323-05 | Denial of Service | reclaim/renewal-only chunk reverting -> tombstone re-stranding griefing | mitigate | Task 2 proves the didWork revert-fix commits work-doing chunks; spam-cancel no-strand |
</threat_model>

<verification>
- The 4 TOMB-04 named tests pass (no relocation, reclaim by next sweep, paid-window preserved, no double-add).
- The didWork revert-fix cases pass (reclaim/renewal-only commit; spam-cancel no-strand; truly-empty still reverts).
- The 318-04 guarantees re-confirmed non-regressed (full AfKingConcurrency suite green).
- No `contracts/*.sol` modification; any incompleteness surfaced, not patched.
</verification>

<success_criteria>
TOMB-04 proves the in-place cancel-tombstone resolves H-CANCEL-SWAP-MISS (no relocation, no missed day,
mint streaks preserved), the didWork revert-fix closes the re-strand griefing vector, and the 318-04
sweep guarantees remain intact — with the subject FROZEN.
</success_criteria>

<output>
Create `.planning/phases/323-tst-repro-first-same-results-gas-behavior-ev-cancel-tombstone-proofs/323-05-SUMMARY.md` when done.
Record: the 4 named-test results; the didWork revert-fix results (incl. the spam-cancel no-strand proof
and the truly-empty-still-reverts confirmation); the retargeted-318-04 results; confirmation no
relocation occurs and no tombstone permanently strands.
</output>
