---
phase: 323-tst-repro-first-same-results-gas-behavior-ev-cancel-tombstone-proofs
plan: 04
type: execute
wave: 2
depends_on: ["323-01"]
autonomous: true
requirements: [DGAS-05, DSPIN-02]
files_modified:
  - test/fuzz/DegeneretteFreezeResolution.t.sol
  - test/gas/CrankResolveBetWorstCaseGas.t.sol
must_haves:
  truths:
    - "the Degenerette write-batching produces byte-identical payouts to a per-spin baseline (Tier-1 additive: BURNIE/WWXRP mint sums + ETH claimable sum)"
    - "the ETH cap binds on the IDENTICAL spin under batching vs per-spin (Tier-2 running-pool-local equivalence)"
    - "the lootbox share sums PER betId (one box per bet, never across bets) under batching"
    - "the 25-spin ETH worst case is derived FIRST then measured, and shown absorbed (fits the block gas limit) by the single-flush write savings"
    - "DGNRS award stays per-spin (not batched)"
  artifacts:
    - path: "test/fuzz/DegeneretteFreezeResolution.t.sol"
      provides: "DGAS-05 same-results equivalence proof (batched payout == per-spin payout, byte-identical)"
    - path: "test/gas/CrankResolveBetWorstCaseGas.t.sol"
      provides: "DSPIN-02 derived-then-measured 25-spin ETH worst-case gas regression, shown absorbed"
  key_links:
    - from: "test/fuzz/DegeneretteFreezeResolution.t.sol"
      to: "contracts/modules/DegenerusGameDegeneretteModule.sol resolveBets"
      via: "resolve a multi-bet batch + assert claimable/mint totals match the per-spin expectation"
      pattern: "resolveBets"
---

<objective>
Prove the Degenerette `resolveBets` write-batching (DGAS-05) is payout-IDENTICAL to the old per-spin
behavior, and that the raised 25-spin ETH cap's worst case (DSPIN-02) is absorbed. The v47 refactor
accumulates ETH/BURNIE/WWXRP payouts cross-bet into a `ResolveAcc` memory struct and flushes ONCE per
currency (one `mintForGame`/`mintPrize`, one `claimableWinnings`+`claimablePool` write, one pool write,
one box per `betId`). The HARD constraint is "same results" — byte-identical to-the-wei.

Purpose: this is a GAS optimization on real-money payout logic; the floor is that it changes NOTHING
observable about who gets paid what. DGAS-05 proves that; DSPIN-02 proves the 2.5× roll work (25 vs old
10 spins) still fits the block gas limit, absorbed by the write savings.
Output: a same-results equivalence proof + a derived-then-measured worst-case gas regression.

DERIVE-THEN-MEASURE (REQUIRED for DSPIN-02): state the theoretical worst case in writing FIRST (one bet
at the 25-spin ETH cap, every spin paying + flipping into a lootbox + max pool write; plus a
mixed-currency multi-bet batch up to the caps), THEN measure it. Do not just run an existing benchmark.

NO `contracts/*.sol` edits (subject FROZEN at fb29ed51; RNG/freeze untouched). If the batched output
diverges from the per-spin expectation by even one wei, that is a real regression — STOP and surface it
(do NOT adjust the expected value to match the batched output).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/323-tst-repro-first-same-results-gas-behavior-ev-cancel-tombstone-proofs/323-CONTEXT.md
@.planning/phases/322-impl-the-one-batched-contract-diff-all-7-items/322-05-SUMMARY.md

<interfaces>
<!-- v47 DegeneretteModule surface (post-batching). From contracts/ at fb29ed51. -->
DegenerusGameDegeneretteModule.sol:
```solidity
uint8 private constant MAX_SPINS_ETH = 25;     // was MAX_SPINS_PER_BET = 10
uint8 private constant MAX_SPINS_BURNIE = 15;
uint8 private constant MAX_SPINS_WWXRP = 5;

function resolveBets(address player, uint64[] calldata betIds) external;   // PUBLIC SIGNATURE UNCHANGED (void)
// internal: threads a `ResolveAcc memory acc` (ethClaimable / burnieMint / wwxrpMint sums +
// running-pool locals + poolFrozen snapshot) through _resolveBet -> _resolveFullTicketBet ->
// _distributePayout; flushes ONCE per currency at the end of resolveBets.
struct ResolveAcc { uint256 ethClaimable; uint256 burnieMint; uint256 wwxrpMint; bool poolFrozen; bool poolLoaded; uint256 runningFuture; uint128 pendingNext; uint128 pendingFuture; }
```
Same-results structure (from 322-05-SUMMARY):
- Tier-1 (additive): mintForGame(player, Σpayout) == Σ mintForGame(player, payout); claimableWinnings[p]
  += Σx == sequence of += x; claimablePool += weiAmount additive; _addClaimableEth(0) is a no-op.
- Tier-2 (ETH cap vs running-pool-local): the local loads on the FIRST ETH win (== live storage value the
  per-spin path would read), each subsequent spin caps/checks against the decremented running local
  (== what the per-spin path's storage read would equal), so the cap binds on the IDENTICAL spin and
  PayoutCapped fires per-spin on the identical spin. Single end write == final per-spin write.
- DGAS-03: lootbox-share summed PER betId (one _resolveLootboxDirect per bet, never across bets).
- DGAS-04: _awardDegeneretteDgnrs stays PER SPIN (reads poolBalance fresh; not batched).
- RNG/freeze UNTOUCHED: rngWord fetch + per-spin RESULT seed (keccak(rngWord,index[,spinIdx],QUICK_PLAY_SALT))
  byte-for-byte kept; only the redundant per-spin lootboxWord salt was dropped (box now rolls once/bet).
The CrankResolveBetWorstCaseGas.t.sol was repaired in 323-01 (MAX_SPINS_PER_BET -> MAX_SPINS_ETH) and
already uses a gasleft-delta idiom + MAINNET_BLOCK_GAS_LIMIT = 30_000_000.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DGAS-05 same-results equivalence — batched payout is byte-identical to a per-spin expectation</name>
  <files>test/fuzz/DegeneretteFreezeResolution.t.sol</files>
  <read_first>
    test/fuzz/DegeneretteFreezeResolution.t.sol (existing Degenerette resolution harness + freeze setup),
    322-05-SUMMARY.md (Tier-1 additive + Tier-2 running-pool-local arguments; DGAS-03 per-betId lootbox;
    DGAS-04 per-spin DGNRS) + the `interfaces` block.
  </read_first>
  <action>
    Write a same-results equivalence proof in `DegeneretteFreezeResolution.t.sol`. Because the contract
    is frozen and the per-spin code no longer exists, the "per-spin baseline" is computed in the TEST as
    the independent expectation (not by running old code):
    - `testBatchedPayoutEqualsPerSpinExpectation_Tier1`: place a MIXED-currency multi-bet batch
      (several ETH bets + BURNIE bets + WWXRP bets, various spin counts within the per-currency caps).
      Independently compute, in the test, the expected per-currency totals as the SUM over every spin's
      payout (replicating the per-spin payout formula off-chain from the known RNG-derived results).
      Resolve via `resolveBets`. Assert the post-resolve `claimableWinnings[player]` ETH delta ==
      Σ ETH-spin shares, the BURNIE balance delta == Σ BURNIE-spin payouts, the WWXRP balance delta ==
      Σ WWXRP-spin payouts — byte-identical (==, not approx). Also assert `claimablePool` moved by exactly
      the ETH sum (additive).
    - `testEthCapBindsOnIdenticalSpin_Tier2`: construct an ETH batch where the prize pool is small enough
      that the per-spin ETH cap (ETH_WIN_CAP_BPS against the running pool) BINDS partway through. Compute,
      in the test, WHICH spin the cap binds on by replaying the running-pool decrement spin-by-spin
      (pool_after = pool_before - cappedShare each ETH win). Assert: (a) the total ETH credited equals the
      sum of per-spin capped shares against the shrinking running pool, and (b) `PayoutCapped` fires on
      the identical spin index the off-chain replay predicts. Repeat in the FROZEN-pool variant (the
      solvency check `pendingFuture < ethShare`) to prove the frozen path's revert/skip fires on the
      identical spin. (DegeneretteFreezeResolution already sets up the freeze window — reuse it.)
    - `testLootboxSummedPerBetIdNotAcrossBets`: place TWO bets that share a lootbox `index` (two bet-txs,
      same index). Resolve both in one `resolveBets`. Assert TWO lootbox resolutions occur (one per
      `betId`), NOT one summed-across box — i.e. the resolution-batch-invariant holds (lootbox-share is
      per-betId, never cross-bet).
    - `testDgnrsAwardStaysPerSpin`: assert the ETH 6+ match DGNRS award is applied per spin (its total
      across a multi-spin bet equals Σ per-spin awards read off the fresh poolBalance), confirming it was
      NOT folded into the cross-bet flush.
    If any equivalence assertion fails by any amount, STOP and surface (real same-results regression).
  </action>
  <verify>
    <automated>forge test --match-test "testBatchedPayoutEqualsPerSpinExpectation_Tier1|testEthCapBindsOnIdenticalSpin_Tier2|testLootboxSummedPerBetIdNotAcrossBets|testDgnrsAwardStaysPerSpin" -vv 2>&1 | tail -14</automated>
  </verify>
  <acceptance_criteria>
    All four DGAS-05 same-results tests PASS: Tier-1 totals byte-identical, Tier-2 cap binds on the
    identical spin (unfrozen + frozen), lootbox summed per-betId (two boxes for two same-index bets),
    DGNRS per-spin. Any divergence is surfaced as a regression, not adjusted away.
  </acceptance_criteria>
  <done>The write-batching is proven payout-identical to the per-spin expectation across Tier-1/Tier-2/lootbox/DGNRS.</done>
</task>

<task type="auto">
  <name>Task 2: DSPIN-02 derive-then-measure — 25-spin ETH worst case absorbed by the batching</name>
  <files>test/gas/CrankResolveBetWorstCaseGas.t.sol</files>
  <read_first>
    test/gas/CrankResolveBetWorstCaseGas.t.sol (repaired in 323-01; gasleft-delta idiom +
    MAINNET_BLOCK_GAS_LIMIT), 322-05-SUMMARY.md ("Worst case to measure at Phase 323") + the `interfaces`
    block (per-currency caps + single-flush savings).
  </read_first>
  <action>
    FIRST, in a NatSpec/comment block at the top of the worst-case test, DERIVE the theoretical worst
    case in writing:
    - The DSPIN-02 worst case is ONE ETH bet at `ticketCount == MAX_SPINS_ETH == 25` where EVERY spin
      pays AND flips into a lootbox (max per-spin work) — 2.5× the old 10-spin roll work. Plus a
      mixed-currency multi-bet batch packed up to the per-currency caps (25 ETH + 15 BURNIE + 5 WWXRP
      across multiple betIds) to exercise the cross-bet flush at maximum accumulation.
    - Note the offsetting savings: the single end-of-call flush (one mint/currency, one
      claimable+claimablePool write, one pool write, one box per bet) replaces N per-spin writes.
    THEN MEASURE:
    - `testWorstCaseResolveBet25SpinAllMatchFitsBlockGasLimit`: build one ETH bet at 25 spins all paying,
      resolve it via the crank's onlySelf resolve sub-call (msg.sender == game), measure the gasleft-delta
      around the external call, assert `gasUsed < MAINNET_BLOCK_GAS_LIMIT` (30_000_000) with comfortable
      margin, and `emit log_named_uint` the measured gas. Assert all 25 spins resolved.
    - `testWorstCaseMixedCurrencyBatchGas`: build the mixed-currency multi-bet batch up to caps, resolve
      in one call, measure + log the gas, assert it fits the block limit.
    - Report the measured gas delta in the SUMMARY (and whether the single-flush savings keep the 25-spin
      cost reasonable vs a naive 2.5× of the old 10-spin number — i.e. demonstrate the batching ABSORBS
      the raised cap). If the worst case does NOT fit the block gas limit, STOP and surface it (the cap is
      then unsafe — a real finding, not a test-tuning issue).
    This file does NOT calibrate the `CRANK_RESOLVE_BET_GAS_UNITS` peg (that was Phase 319's job and is
    out of scope here) — it MEASURES the worst case and proves block-limit fit.
  </action>
  <verify>
    <automated>forge test --match-test "testWorstCaseResolveBet25SpinAllMatchFitsBlockGasLimit|testWorstCaseMixedCurrencyBatchGas" -vv 2>&1 | tail -14</automated>
  </verify>
  <acceptance_criteria>
    The worst case is DERIVED in writing (top-of-test NatSpec states the 25-spin ETH + mixed-batch worst
    case) THEN MEASURED. `testWorstCaseResolveBet25SpinAllMatchFitsBlockGasLimit` PASSES (gas <
    30_000_000, all 25 spins resolved). The mixed-currency batch test PASSES. The measured gas is logged
    and reported in the SUMMARY, demonstrating absorption by the single-flush write savings. A
    block-limit overflow would be surfaced as a finding.
  </acceptance_criteria>
  <done>The 25-spin ETH worst case is derived-then-measured and shown absorbed within the block gas limit.</done>
</task>

</tasks>

<verification>
- DGAS-05: batched payout byte-identical to per-spin expectation (Tier-1 + Tier-2 + per-betId lootbox + per-spin DGNRS) — all pass.
- DSPIN-02: 25-spin ETH worst case + mixed-currency batch derived-then-measured, gas < block limit, reported.
- RNG/freeze untouched (the result-seed equivalence is implicit in the byte-identical Tier-1/Tier-2 payouts).
- No `contracts/*.sol` modification; any divergence/overflow surfaced, not tuned away.
</verification>

<success_criteria>
DGAS-05 proves the write-batching is same-results (byte-identical payouts, cap binds on the identical
spin, per-betId lootbox, per-spin DGNRS); DSPIN-02 proves the raised 25-spin ETH cap's worst case is
derived-then-measured and absorbed within the block gas limit — with the subject FROZEN.
</success_criteria>

<output>
Create `.planning/phases/323-tst-repro-first-same-results-gas-behavior-ev-cancel-tombstone-proofs/323-04-SUMMARY.md` when done.
Record: the Tier-1/Tier-2 equivalence results; the per-betId lootbox + per-spin DGNRS confirmations;
the DERIVED worst case; the MEASURED 25-spin + mixed-batch gas numbers + the margin under the block
limit; confirmation the batching absorbs the raised cap.
</output>
