---
phase: 236-regression-findings-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/FINDINGS-v29.0.md
  - KNOWN-ISSUES.md
autonomous: true
requirements:
  - FIND-01
  - FIND-02
  - FIND-03
tags: [consolidation, findings, audit, v29.0, known-issues]

must_haves:
  truths:
    - "audit/FINDINGS-v29.0.md exists at the repo (new file) mirroring the v27.0 structure: Executive Summary severity table + Overall Assessment + Findings section with per-phase subsections (231 → 232 → 232.1 → 233 → 234 → 235) + per-finding #### blocks."
    - "Four F-29-NN INFO blocks are present: F-29-01 (JackpotEthWin event-widening / BAF sentinel), F-29-02 (JackpotTicketWin event-widening), F-29-03 (QST-01 companion-test-coverage FC-234-A), F-29-04 (Gameover RNG substitution retroactively disclosed per D-06 / D-07)."
    - "Executive Summary shows 0 CRITICAL / 0 HIGH / 0 MEDIUM / 0 LOW / 4 INFO per D-04 (total = 4)."
    - "KNOWN-ISSUES.md at repo root gains exactly TWO NEW design-decision entries (BAF event-widening + BAF_TRAIT_SENTINEL per D-05; Gameover RNG substitution per D-06) and THREE existing KI entries gain appended v29.0 back-refs per D-08 (Gameover prevrandao fallback; Decimator settlement over-reserves claimablePool; Lootbox RNG uses index advance isolation)."
    - "Neither KI entry is added for F-29-03 (per D-10) nor for 232.1 RNG-index ordering invariant (per D-09). Existing KI entries outside the D-08 back-ref list are UNMODIFIED."
    - "Zero contracts/ or test/ writes; file-ownership gate holds."
  artifacts:
    - path: "audit/FINDINGS-v29.0.md"
      provides: "Canonical v29.0 consolidated findings report (Exec Summary + per-phase sections + per-finding blocks)"
      contains: "Degenerus Protocol -- Delta Findings Report (v29.0 Post-v27 Contract Delta Audit)"
    - path: "KNOWN-ISSUES.md"
      provides: "Pre-disclosure updated with 2 new design-decision entries + 3 targeted back-refs"
      contains: "BAF_TRAIT_SENTINEL"
  key_links:
    - from: "audit/FINDINGS-v29.0.md"
      to: ".planning/phases/233-jackpot-baf-entropy-audit/233-01-AUDIT.md"
      via: "F-29-01 + F-29-02 content extraction from JKP-01 Findings-Candidate Block"
      pattern: "F-29-0[12]"
    - from: "audit/FINDINGS-v29.0.md"
      to: ".planning/phases/234-quests-boons-misc-audit/234-01-AUDIT.md"
      via: "F-29-03 content extraction from QST-01 FC-234-A row"
      pattern: "F-29-03"
    - from: "audit/FINDINGS-v29.0.md"
      to: ".planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-05-AUDIT.md"
      via: "F-29-04 Gameover RNG substitution backed by TRNX-01 4-Path Walk + Buffer-Swap Site Citation"
      pattern: "F-29-04"
    - from: "KNOWN-ISSUES.md"
      to: "audit/FINDINGS-v29.0.md"
      via: "New KI entries reference F-29-01/02/04; three existing KI entries append v29.0 Phase-235 re-verification back-refs"
      pattern: "F-29-0[124]|v29\\.0 Phase 235"
---

<objective>
Author `audit/FINDINGS-v29.0.md` (new file) in v27.0 structural form, consolidating every v29.0 Finding Candidate: Y row from Phases 231-235 into four canonical F-29-NN INFO blocks. Update `KNOWN-ISSUES.md` at the repo root with two new design-decision entries (per D-05, D-06) and three targeted v29.0 back-refs on existing entries (per D-08). Do NOT produce the Regression Appendix — that is Plan 02's scope.

Purpose: Satisfies FIND-01 (severity-classified per-finding blocks), FIND-02 (KI updates with F-29-NN back-refs), and the Executive Summary portion of FIND-03 (per-phase + per-severity totals in the header table).

Output: `audit/FINDINGS-v29.0.md` with 4 INFO findings + per-phase subsections + executive summary; `KNOWN-ISSUES.md` with 2 new entries + 3 back-ref appends.

Expected final severity distribution: 0 CRITICAL / 0 HIGH / 0 MEDIUM / 0 LOW / 4 INFO (strict per D-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/236-regression-findings-consolidation/236-CONTEXT.md
@./CLAUDE.md
</context>

<read_first>
@audit/FINDINGS-v27.0.md
@audit/FINDINGS-v28.0.md
@audit/FINDINGS-v25.0.md
@KNOWN-ISSUES.md
@.planning/phases/233-jackpot-baf-entropy-audit/233-01-AUDIT.md
@.planning/phases/233-jackpot-baf-entropy-audit/233-01-SUMMARY.md
@.planning/phases/234-quests-boons-misc-audit/234-01-AUDIT.md
@.planning/phases/234-quests-boons-misc-audit/234-01-SUMMARY.md
@.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-05-AUDIT.md
@.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-05-SUMMARY.md
@.planning/phases/231-earlybird-jackpot-audit/231-01-SUMMARY.md
@.planning/phases/231-earlybird-jackpot-audit/231-02-SUMMARY.md
@.planning/phases/231-earlybird-jackpot-audit/231-03-SUMMARY.md
@.planning/phases/232-decimator-audit/232-01-SUMMARY.md
@.planning/phases/232-decimator-audit/232-02-SUMMARY.md
@.planning/phases/232-decimator-audit/232-03-SUMMARY.md
@.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-01-SUMMARY.md
@.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-02-SUMMARY.md
@.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-03-SUMMARY.md
@.planning/phases/233-jackpot-baf-entropy-audit/233-02-SUMMARY.md
@.planning/phases/233-jackpot-baf-entropy-audit/233-03-SUMMARY.md
@.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-01-SUMMARY.md
@.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-02-SUMMARY.md
@.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-03-SUMMARY.md
@.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-04-SUMMARY.md
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Create audit/FINDINGS-v29.0.md with exec summary, per-phase sections, and 4 F-29-NN INFO blocks</name>
  <files>audit/FINDINGS-v29.0.md</files>
  <read_first>
    - audit/FINDINGS-v27.0.md (PRIMARY structural template — copy the scaffolding: title/header block, Executive Summary severity table, Overall Assessment paragraph, `## Findings` root with `### Phase NNN: <name> (N findings)` subsections, `#### F-NN-MM:` per-finding blocks with `| Field | Value |` table + severity-justification paragraph)
    - audit/FINDINGS-v28.0.md (secondary precedent — same format at scale; shows flat F-28-NN numbering without per-phase infix)
    - audit/FINDINGS-v25.0.md (older precedent)
    - .planning/phases/233-jackpot-baf-entropy-audit/233-01-AUDIT.md (SOURCE for F-29-01 + F-29-02: Findings-Candidate Block lines 14-36 + Per-Function Verdict Table rows 6-7 for "JackpotEthWin event declaration widening" at contracts/modules/DegenerusGameJackpotModule.sol:69-77 field at line 72 AND "JackpotTicketWin event declaration widening" at contracts/modules/DegenerusGameJackpotModule.sol:80-87 field at line 83; BAF_TRAIT_SENTINEL decl at contracts/modules/DegenerusGameJackpotModule.sol:136; four emit sites at contracts/modules/DegenerusGameJackpotModule.sol:2002,2014,2034,2038)
    - .planning/phases/234-quests-boons-misc-audit/234-01-AUDIT.md (SOURCE for F-29-03: QST-01 Per-Target Verdict Table row "Companion test-file (D-04)" at test/fuzz/CoverageGap222.t.sol:1453-1455; SUMMARY identifies this as FC-234-A)
    - .planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-05-AUDIT.md (SOURCE for F-29-04: D-11 rngLocked Invariant section + Buffer-Swap Site Citation at contracts/modules/DegenerusGameAdvanceModule.sol:292 + 4-Path Walk Table Gameover row at AdvanceModule:519-627 + "_gameOverEntropy" at AdvanceModule:553-558 + final unlock at AdvanceModule:625; companion buffer-swap at AdvanceModule:1082 for mid-day lootbox RNG)
    - .planning/phases/236-regression-findings-consolidation/236-CONTEXT.md (D-01..D-11 locked decisions — see §decisions)
    - KNOWN-ISSUES.md (current repo-root file; confirmed by ls /home/zak/Dev/PurgeGame/degenerus-audit/audit/ — there is NO audit/KNOWN-ISSUES.md, only KNOWN-ISSUES.md at repo root)
  </read_first>
  <action>
Create `audit/FINDINGS-v29.0.md` (new file; path is repo-root-relative `audit/FINDINGS-v29.0.md`). Mirror `audit/FINDINGS-v27.0.md` structure exactly. Six sections in order:

**§1 — Header block** (copy the v27.0 title/meta shape):

- Title: `# Degenerus Protocol -- Delta Findings Report (v29.0 Post-v27 Contract Delta Audit)`
- `**Audit Date:** 2026-04-18` (today)
- `**Methodology:** Seven-phase post-v27 delta audit. Phase 230 delta extraction and scope map; Phases 231-234 adversarial audits per module-group (earlybird, decimator including URGENT-inserted 232.1 RNG-index drain-ordering enforcement, jackpot/BAF/entropy, quests/boons/misc); Phase 235 conservation + RNG commitment re-proof + phase-transition RNG lock removal audit; Phase 236 regression sweep + consolidation. Executed read-only at HEAD 1646d5af.`
- `**Scope:** Post-v27.0-baseline contract-side delta — the 10 contract-touching commits between commit 14cb45e1 (v27.0 phase 223 shipped 2026-04-13) and HEAD 1646d5af, plus 2 post-Phase-230 RNG-hardening addendum commits (314443af keccak-seed fix; c2e5e0a9 17-site XOR→keccak entropy-mixing replacement) captured by 230-02-DELTA-ADDENDUM.md. 12 in-scope files: DegenerusGameJackpotModule.sol, DegenerusGameStorage.sol, DegenerusQuests.sol, IDegenerusQuests.sol, DegenerusGameMintModule.sol, DegenerusGame.sol, IDegenerusGame.sol, DegenerusGameDecimatorModule.sol, BurnieCoin.sol, DegenerusGameAdvanceModule.sol, DegenerusGameWhaleModule.sol, IDegenerusGameModules.sol.`
- `**Contracts in scope:** <copy the v27.0 contracts-in-scope paragraph; note that v29.0 is a contracts-only audit — database/ repo was covered by v28.0 and is out of scope>`

**§2 — `---` separator then `## Executive Summary`** per D-04:

```
| Severity | Count |
|----------|-------|
| CRITICAL | 0 |
| HIGH | 0 |
| MEDIUM | 0 |
| LOW | 0 |
| INFO | 4 |
| **Total** | **4** |
```

**Overall Assessment paragraph** (strict factual per D-04 — no marketing language): `Zero exploitable vulnerabilities found across the v29.0 delta. Five audit phases produced 13 plans total (231=3, 232=3, 232.1=3, 233=3, 234=1, 235=5) with 251 aggregate verdict rows all SAFE or SAFE-INFO and zero VULNERABLE / zero DEFERRED row-level verdicts. Four Finding Candidate: Y rows surfaced as INFO observations (2 from Phase 233-01 BAF event-widening indexer-compat; 1 from Phase 234-01 QST-01 companion-test-coverage; 1 retroactively surfaced during Phase 236 consolidation review from Phase 235-05 TRNX-01's Gameover-path buffer-swap analysis). All four are design decisions or off-chain-only observations, not on-chain vulnerabilities. This report is a delta supplement to the v5.0 Master Findings Report (audit/FINDINGS.md, 29 INFO), the v25.0 Master Delta Findings Report (audit/FINDINGS-v25.0.md, 13 INFO), and the v27.0 Call-Site Integrity Audit (audit/FINDINGS-v27.0.md, 16 INFO). The v26.0 milestone was design-only (bonus jackpot split) with no formal findings document. External auditors should read all four documents together. Regression verification of all prior v25.0 + v27.0 findings + v27.0 KNOWN-ISSUES entries is provided in the Regression Appendix (see Plan 236-02).`

**§3 — `---` then `## Findings`** with six per-phase subsections in PHASE ORDER per D-02:

```
### Phase 231: Earlybird Jackpot Audit (0 findings)
### Phase 232: Decimator Audit (0 findings)
### Phase 232.1: RNG-Index Ticket Drain Ordering Enforcement (0 findings)
### Phase 233: Jackpot/BAF + Entropy Audit (2 findings)
### Phase 234: Quests / Boons / Misc Audit (1 finding)
### Phase 235: Conservation + RNG Commitment Re-Proof + Phase Transition (1 finding)
```

For zero-finding phases (231, 232, 232.1), write a one-paragraph "zero findings" subsection per D-02 citing the per-phase SUMMARY aggregate:

- **Phase 231:** `Three adversarial audits (EBD-01 f20a2b5e earlybird purchase-phase finalize refactor; EBD-02 20a951df trait-alignment rewrite; EBD-03 combined state machine). 40 PASS verdicts across 3 plans; zero FAIL; zero DEFER; zero Finding Candidate: Y rows. See .planning/phases/231-earlybird-jackpot-audit/231-0N-AUDIT.md for per-function verdict tables.`
- **Phase 232:** `Three adversarial audits (DCM-01 3ad0f8d3 decimator burn-key refactor + consolidated jackpot block; DCM-02 67031e7d event emission; DCM-03 858d83e4 terminal-claim passthrough). 44 verdict rows across 3 plans (36 SAFE + 8 SAFE-INFO); zero VULNERABLE / zero DEFERRED row-level verdicts; zero Finding Candidate: Y rows after Phase 236 consolidation review. (Note: DCM-01's two SAFE-INFO Finding Candidate: Y rows — DECIMATOR_MIN_BUCKET_100 dead-code revival and "prev"-prefixed naming vestige — were reviewed during Phase 236 and classified as internal documentation/naming observations that do not rise to an INFO finding on the reader's perspective; they remain in the 232-01-AUDIT.md record. DCM-02's Finding Candidate: Y row for v28.0 Phase 227 indexer-compat OBSERVATION is superseded here by the F-29-01 / F-29-02 event-widening INFO blocks which cover the same concern.)`
- **Phase 232.1:** `URGENT-inserted phase — three plans (lazy pre-finalize gate contract fix; forge invariant + game-over path-isolation suite; sim-replay regression + processFutureTicketBatch reachable-caller audit). Zero Finding Candidate: Y rows. Net effect: enforces the RNG-index ticket drain-ordering invariant structurally so _raritySymbolBatch can never be invoked with entropyWord == 0 under any reachable advanceGame stage sequence. The hardening is a gate-insertion strictly narrowing pre-existing surface; not promoted to KNOWN-ISSUES.md per 236-CONTEXT.md D-09 (hardening makes an implicit invariant explicit; no new architectural design decision). See .planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-0N-SUMMARY.md for fix-series revision chronology.`

For each finding-bearing phase (233, 234, 235), author the per-finding `#### F-29-NN:` blocks below.

**§4 — F-29-NN PER-FINDING BLOCKS** using the v27.0 block shape:

```
#### F-29-NN: <short title>

| Field | Value |
|-------|-------|
| **Severity** | INFO |
| **Source** | Phase NNN (<artifact path>) |
| **Contract** | `<contracts/... path>` |
| **Function** | `<function or :line>` |
| **Resolution** | <DESIGN-ACCEPTED | INFO-ACCEPTED | OFF-CHAIN-INDEXER-REGEN> |

<body paragraph: what was observed, why it matters, on-chain vs off-chain surface>

**Severity justification:** <one paragraph: why INFO and not higher; production risk (or lack thereof); fix / mitigation shape>.

---
```

Author the four F-29-NN blocks with these exact contents:

**F-29-01 block** — Title: `F-29-01: JackpotEthWin event signature widened uint8→uint16 traitId for BAF_TRAIT_SENTINEL=420 carry`.
- Severity: INFO
- Source: `Phase 233 (.planning/phases/233-jackpot-baf-entropy-audit/233-01-AUDIT.md Findings-Candidate Block Candidate 1 + Per-Function Verdict Table row "JackpotEthWin event declaration widening")`
- Contract: `contracts/modules/DegenerusGameJackpotModule.sol`
- Function: `event JackpotEthWin(address,uint24,uint16,uint256,uint256,uint24,uint32) at :69-77 (widened field at :72); related: uint16 private constant BAF_TRAIT_SENTINEL = 420 at :136; emit sites at :2002, :2034`
- Resolution: OFF-CHAIN-INDEXER-REGEN
- Body: `Commit 104b5d42 (feat(jackpot): tag BAF wins with traitId=420 sentinel) widens the JackpotEthWin event's traitId field from uint8 to uint16 so it can carry the new BAF_TRAIT_SENTINEL=420 compile-time constant. 420 is out-of-domain for real trait IDs (max uint8 = 255) and is injected at four emit sites inside runBafJackpot (DegenerusGameJackpotModule.sol:2002 large-winner ETH half, :2014 large-winner lootbox immediate half, :2034 small-winner even-idx 100% ETH, :2038 small-winner odd-idx 100% lootbox). EVM topic encoding is 32-byte left-padded regardless of the declared width, so on-chain log emission is byte-identical to what the pre-fix decl would have produced for the same argument value. The canonical event signature string changes from JackpotEthWin(address,uint24,uint8,uint256,uint256,uint24,uint32) to JackpotEthWin(address,uint24,uint16,uint256,uint256,uint24,uint32), which changes the keccak-derived topic0 hash. Off-chain ABI consumers (indexers, UI subgraphs, The Graph) must regenerate their ABI to decode post-fix events. Phase 233-01 domain-collision sweep (14 rows all SAFE) confirmed 420 cannot appear in winningTraitsPacked / bonusTraitsPacked (both populated from uint8[4] arrays via JackpotBucketLib.packWinningTraits) and cannot reach any uint8 narrowing consumer of event traitId (all narrowing consumers at MintModule:581/617 for rarity histograms; JackpotBucketLib.unpackWinningTraits output uint8[4] at :278/444/893/1082/1722). The 420 sentinel is observationally invisible to every on-chain branch — Solidity does not expose emitted event fields for on-chain re-read.`
- Severity justification: `INFO because (a) on-chain behavior is unchanged (EVM indexed-topic encoding pads to 32 bytes regardless of declared width); (b) domain separation is structurally guaranteed (uint16 wider than uint8; 420 > type(uint8).max = 255; private visibility keeps the symbol module-local); (c) no on-chain branch reads event fields for execution logic; (d) Degenerus is pre-launch per the 104b5d42 commit message — no live indexers affected. Downstream off-chain consumers regenerate ABIs as part of their normal deploy pipeline. Not a runtime / security / correctness risk on production contracts. See the new KNOWN-ISSUES.md "BAF event-widening + BAF_TRAIT_SENTINEL=420 pattern" entry (Plan 236-01 Task 2) for the design-decision disclosure.`

**F-29-02 block** — Title: `F-29-02: JackpotTicketWin event signature widened uint8→uint16 traitId for BAF_TRAIT_SENTINEL=420 carry`.
- Severity: INFO
- Source: `Phase 233 (.planning/phases/233-jackpot-baf-entropy-audit/233-01-AUDIT.md Findings-Candidate Block Candidate 2 + Per-Function Verdict Table row "JackpotTicketWin event declaration widening")`
- Contract: `contracts/modules/DegenerusGameJackpotModule.sol`
- Function: `event JackpotTicketWin(address,uint24,uint16,uint32,uint24,uint256) at :80-87 (widened field at :83); emit sites at :2014, :2038`
- Resolution: OFF-CHAIN-INDEXER-REGEN
- Body: `Same event-widening pattern as F-29-01 applied to JackpotTicketWin's traitId field. Commit 104b5d42 widens :83's traitId from uint8 to uint16 so the ticket-grant side of runBafJackpot can emit BAF_TRAIT_SENTINEL=420. Emit sites at DegenerusGameJackpotModule.sol:2014 (large-winner lootbox immediate half, inside the lootboxPortion <= LOOTBOX_CLAIM_THRESHOLD branch) and :2038 (small-winner odd-idx 100% lootbox). Canonical signature changes from JackpotTicketWin(address,uint24,uint8,uint32,uint24,uint256) to JackpotTicketWin(address,uint24,uint16,uint32,uint24,uint256); topic0 keccak hash changes; off-chain ABIs must regenerate. Note: JackpotBurnieWin at DegenerusGameJackpotModule.sol:90-96 retains its pre-existing uint8 traitId field at :93 (non-BAF emit sites continue to pass real uint8 values); no indexer-compat drift for JackpotBurnieWin.`
- Severity justification: `Same rationale as F-29-01 — INFO because on-chain behavior is unchanged, domain separation is structurally guaranteed, no on-chain branch reads event fields for execution logic, and Degenerus is pre-launch. This block and F-29-01 together constitute a single design pattern (the BAF sentinel approach) expressed across two event declarations. The paired KNOWN-ISSUES.md entry added by Plan 236-01 Task 2 consolidates the disclosure into ONE KI entry per 236-CONTEXT.md D-05.`

**F-29-03 block** — Title: `F-29-03: QST-01 d5284be5 companion test-file update contains no positive coverage for the new wei-direct mint_ETH quest-credit path`.
- Severity: INFO
- Source: `Phase 234 (.planning/phases/234-quests-boons-misc-audit/234-01-AUDIT.md §QST-01 Per-Target Verdict Table row "Companion test-file (D-04)"; tracked internally as FC-234-A)`
- Contract: `test/fuzz/CoverageGap222.t.sol`
- Function: `:1453-1455 (raw-selector ABI signature + argument-type update inside an onlyCoin-caller negative test at :1441-1461)`
- Resolution: INFO-ACCEPTED
- Body: `Commit d5284be5 (fix(quests): credit fresh ETH wei 1:1 to mint_ETH quest) migrates the mint_ETH quest handler from a lossy uint32-rounded unit-scaling pipeline (pre-fix: uint32 ethMintQty = uint32(quantity / (4 * TICKET_SCALE)); questUnits * freshEth / costWei; uint32 cast; ethMintQty * mintPrice) to a wei-direct 1:1 credit pipeline (post-fix: uint256 ethFreshWei = ticketFreshEth + lootboxFreshEth flows unmodified from DegenerusGameMintModule._purchaseFor at :1092 into DegenerusQuests.handlePurchase at :762 and becomes the MINT_ETH delta at :804-805/:819). git show d5284be5 -- test/ reports one touched file (test/fuzz/CoverageGap222.t.sol) with a 4-insertion / 2-deletion hunk at :1453-1455 that updates a raw-selector ABI signature from "handlePurchase(address,uint32,uint32,uint256,uint256,uint256)" to "handlePurchase(address,uint256,uint32,uint256,uint256,uint256)" and the first-argument type annotation from uint32(1) to uint256(1). The surrounding test (assertFalse(o6, "quests.handlePurchase rejected non-coin caller") at :1459) is an onlyCoin-caller negative test — it verifies the quest handler rejects non-coin callers, not that fresh-ETH wei is credited 1:1 to the MINT_ETH quest. The commit therefore introduces the necessary selector-alignment but no POSITIVE coverage for the new wei-direct credit semantics. Phase 234 QST-01 per-function audit (9 SAFE + 2 SAFE-INFO rows) proved the wei-direct pipeline correct against attack vectors (a) wei-credit 1:1 correctness, (b) fresh-ETH summation as sole feed point, (c) no double-credit with companion quests, (d) CEI / ordering, (e) interface lockstep. The finding here is strictly about test-suite coverage, not contract correctness.`
- Severity justification: `INFO because the contract-side wei-direct pipeline is independently proven correct by Phase 234 QST-01's 11-row verdict table (9 SAFE + 2 SAFE-INFO) and by Phase 235 CONS-01 ETH conservation re-proof at HEAD 1646d5af which covers the post-fix quest-credit path. The absence of a positive wei-direct test is a test-quality observation, not a runtime correctness defect. Consistent with prior-milestone patterns (see feedback_test_rnglock.md test-coverage reminders). NOT promoted to KNOWN-ISSUES.md per 236-CONTEXT.md D-10 — KNOWN-ISSUES is reserved for intentional architecture + accepted automated-tool findings; test-tooling observations are not design decisions. Recommended follow-up: a dedicated positive-coverage test phase may close this gap if desired, but it is not gating for v29.0 shipping.`

**F-29-04 block** — Title: `F-29-04: Gameover RNG substitution for mid-cycle write-buffer tickets (RNG-consumer-determinism invariant violation at terminal state)`.
- Severity: INFO
- Source: `Phase 235 (.planning/phases/235-conservation-rng-commitment-re-proof-phase-transition/235-05-AUDIT.md TRNX-01 4-Path Walk Table Gameover row + Buffer-Swap Site Citation section + rngLocked End-State Check §Gameover Path; retroactively surfaced during Phase 236 consolidation review per 236-CONTEXT.md D-06)`
- Contract: `contracts/modules/DegenerusGameAdvanceModule.sol`
- Function: `_swapAndFreeze(purchaseLevel) at :292 (daily RNG buffer-swap site); _swapTicketSlot(purchaseLevel_) at :1082 (mid-day lootbox RNG buffer-swap site); _gameOverEntropy at :1222-1246 (entropy substitution); _handleGameOverPath at :519-627 (terminal drain path); _unlockRng(day) at :625 (terminal unlock)`
- Resolution: DESIGN-ACCEPTED
- Body: `Degenerus enforces an "RNG-consumer determinism" invariant: every RNG consumer's entropy should be fully committed at input time (the VRF word that an RNG consumer will read must be unknown-but-bound at the moment the consumer's input parameters are committed to storage). Phase 235 RNG-01 and RNG-02 prove this invariant holds for every new RNG consumer in the v29.0 delta under normal and skip-split paths. During Phase 236 consolidation review the user surfaced a terminal-state case where the invariant is technically violated: if a mid-cycle ticket-buffer swap has occurred (daily RNG request at AdvanceModule:292 via _swapAndFreeze(purchaseLevel), OR mid-day lootbox RNG request at AdvanceModule:1082 via _swapTicketSlot(purchaseLevel_)) and the new write buffer is populated with tickets queued at the current level awaiting the expected-next VRF fulfillment, a game-over event intervening before that fulfillment causes those tickets to drain under _gameOverEntropy at AdvanceModule:1222-1246 rather than the originally-anticipated VRF word. _gameOverEntropy derives substitute entropy from (a) committed VRF words still resident in storage, and (b) block.prevrandao admixture after a 3-day VRF stall at gameover (see KNOWN-ISSUES.md "Gameover prevrandao fallback" and F-25-08 in audit/FINDINGS-v25.0.md). The _handleGameOverPath at AdvanceModule:519-627 then runs the best-effort drain rounds with an interposed _swapTicketSlot(lvl + 1) at :595 and finalizes the terminal unlock at AdvanceModule:625. The substitute entropy is VRF-derived or VRF-plus-prevrandao — NEVER attacker-timed. Acceptance rationale: (a) only reachable at gameover (terminal state — no further gameplay after the handleFinalSweep 30-day window); (b) no player-reachable exploit — gameover is triggered by 120-day liveness stall or pool deficit, neither of which an attacker can time against a specific mid-cycle write-buffer state; (c) at gameover the protocol must drain within bounded transactions and cannot wait for a deferred fulfillment that may never arrive if the VRF coordinator is the reason for the liveness stall. Phase 235 TRNX-01's 4-Path Walk Table Gameover row is SAFE on rngLocked invariant preservation (exactly one _unlockRng fires at AdvanceModule:625; no double unlock, no missed unlock, no read-side-buffer write, no far-future queue write blocked by the _livenessTriggered() revert at DegenerusGameStorage.sol:568/599/652). This entry is a disclosure supplement, not a re-classification of the TRNX-01 Gameover-row verdict, which remains SAFE / Finding Candidate: N as recorded.`
- Severity justification: `INFO / DESIGN-ACCEPTED per 236-CONTEXT.md D-07 — matches the severity of the already-disclosed F-25-08 (Gameover prevrandao fallback) which is a same-domain acceptance. The violation is bounded to terminal state; no player-reachable exploit exists because gameover triggers are structural (120-day liveness stall or pool deficit), not player-timeable; and the substitute entropy is always VRF-derived or VRF-plus-prevrandao. The "RNG-consumer determinism" invariant is codified here as a canonical protocol property for future audits to reference by name when analyzing RNG-consuming code paths. See the new KNOWN-ISSUES.md "Gameover RNG substitution for mid-cycle write-buffer tickets" entry (Plan 236-01 Task 2) for the full design-decision disclosure with player-reachability and terminal-state rationale.`

**§5 — `---` then `## Summary Statistics`** (copy v27.0 structure):

- By Severity table: 0 CRITICAL / 0 HIGH / 0 MEDIUM / 0 LOW / 4 INFO
- By Source Phase table:
  | Phase | Description | Findings |
  | 231 | Earlybird Jackpot Audit | 0 |
  | 232 | Decimator Audit | 0 |
  | 232.1 | RNG-Index Ticket Drain Ordering Enforcement | 0 |
  | 233 | Jackpot/BAF + Entropy Audit | 2 (F-29-01, F-29-02) |
  | 234 | Quests / Boons / Misc Audit | 1 (F-29-03) |
  | 235 | Conservation + RNG Commitment Re-Proof + Phase Transition | 1 (F-29-04) |
- By Contract / File table:
  | Contract / File | Findings |
  | `contracts/modules/DegenerusGameJackpotModule.sol` | 2 (F-29-01, F-29-02) |
  | `test/fuzz/CoverageGap222.t.sol` | 1 (F-29-03) |
  | `contracts/modules/DegenerusGameAdvanceModule.sol` | 1 (F-29-04 — Gameover terminal-state buffer-swap surface at :292, :1082, :625) |

**§6 — `---` then `## Audit Trail`** (copy v27.0 Audit Trail table shape):

| Phase | Scope | Plans | Findings | Verdict |
|-------|-------|-------|----------|---------|
| 230 | Delta Extraction & Scope Map (function-level changelog, cross-module interaction map, interface-drift catalog, consumer index) | 1 | 0 (catalog, not findings pass) | SCOPE MAP |
| 231 | Earlybird Jackpot Audit (EBD-01 purchase-phase finalize refactor; EBD-02 trait-alignment rewrite; EBD-03 combined state machine) | 3 | 40 PASS / 0 FAIL / 0 DEFER | SAFE |
| 232 | Decimator Audit (DCM-01 burn-key refactor; DCM-02 event emission; DCM-03 terminal-claim passthrough) | 3 | 36 SAFE + 8 SAFE-INFO / 0 VULN / 0 DEFER | SAFE |
| 232.1 | RNG-Index Ticket Drain Ordering Enforcement (URGENT-inserted: lazy pre-finalize gate contract fix; forge invariant suite; sim-replay + pftb reachable-caller audit) | 3 | 8/8 forge invariants PASS; zero L5 zero-hit trait IDs post-fix | SAFE (hardening) |
| 233 | Jackpot/BAF + Entropy Audit (JKP-01 traitId=420 sentinel; JKP-02 explicit entropy passthrough; JKP-03 cross-path bonus-trait consistency) | 3 | 75 SAFE + 2 SAFE-INFO / 0 VULN / 0 DEFER; 2 F-29-NN blocks contributed (F-29-01, F-29-02) | SAFE |
| 234 | Quests / Boons / Misc Audit (QST-01 mint_ETH wei-credit fix; QST-02 boonPacked mapping exposure; QST-03 BurnieCoin isolation) | 1 | 19 SAFE + 4 SAFE-INFO / 0 VULN / 0 DEFER; 1 F-29-NN block contributed (F-29-03) | SAFE |
| 235 | Conservation + RNG Commitment Re-Proof + Phase Transition (CONS-01 ETH; CONS-02 BURNIE; RNG-01 backward trace; RNG-02 commitment window; TRNX-01 phase transition) | 5 | 41 SSTORE rows + 10 path proofs + 10 mint + 6 burn + 28 backward-trace + 19 commitment-window + 4-path walk; all SAFE; 0 VULN / 0 DEFER / 0 Finding Candidate: Y at audit time; 1 F-29-NN block contributed retroactively (F-29-04) via Phase 236 consolidation review | SAFE |
| 236 | Regression Sweep + Findings Consolidation | 2 | (Regression Appendix in Plan 236-02; this document is Plan 236-01 output) | SAFE |
| **Total** | **7-phase post-v27 contract delta audit** | **19** | **4 INFO** | **SAFE** |

**§7 — Cross-reference note** ending the file: `Regression verification of all 16 v27.0 INFO findings (F-27-01..16) + 3 v27.0 KNOWN-ISSUES entries + 13 v25.0 INFO findings (F-25-01..13) + v26.0 design-only milestone conclusions is provided in the Regression Appendix below (to be authored by Plan 236-02).`

**Do NOT** write the Regression Appendix or the Regression Summary in this plan — those are Plan 236-02's exclusive scope. Leave the file ending at the cross-reference note so Plan 236-02 can append cleanly.

**Consolidation choice (F-29-01 vs F-29-02 per 236-CONTEXT.md D-Claude's Discretion):** Keep them as TWO SEPARATE F-29-NN blocks. Rationale: narrative clarity at the block level is preferable since each event has distinct emit-site line numbers and a distinct canonical-signature change; the CONSOLIDATION into one design pattern happens at the KNOWN-ISSUES.md entry (per D-05 which says "Consolidate the two 233-01 observations into ONE KI entry"). Document this choice in the `236-01-SUMMARY.md` Decisions-Made section.

**File-path gate:** The new file MUST be at `audit/FINDINGS-v29.0.md` (repo root audit/). Do NOT create or touch any `audit/KNOWN-ISSUES.md` or `audit/FINDINGS-v29.md` variant — ROADMAP §Phase 236 Success Criterion #4 cites `audit/KNOWN-ISSUES.md` but confirmed by `find . -name "KNOWN-ISSUES*"` only `./KNOWN-ISSUES.md` exists (Task 2's target).

**Writable-targets gate:** Task 1 writes ONLY `audit/FINDINGS-v29.0.md`. No other file.
  </action>
  <verify>
    <automated>test -f audit/FINDINGS-v29.0.md && grep -q "^# Degenerus Protocol -- Delta Findings Report (v29.0 Post-v27 Contract Delta Audit)" audit/FINDINGS-v29.0.md && grep -q "^## Executive Summary" audit/FINDINGS-v29.0.md && grep -q "^## Findings" audit/FINDINGS-v29.0.md && grep -q "^### Phase 231" audit/FINDINGS-v29.0.md && grep -q "^### Phase 232:" audit/FINDINGS-v29.0.md && grep -q "^### Phase 232\.1" audit/FINDINGS-v29.0.md && grep -q "^### Phase 233" audit/FINDINGS-v29.0.md && grep -q "^### Phase 234" audit/FINDINGS-v29.0.md && grep -q "^### Phase 235" audit/FINDINGS-v29.0.md && grep -q "^#### F-29-01:" audit/FINDINGS-v29.0.md && grep -q "^#### F-29-02:" audit/FINDINGS-v29.0.md && grep -q "^#### F-29-03:" audit/FINDINGS-v29.0.md && grep -q "^#### F-29-04:" audit/FINDINGS-v29.0.md && [ "$(grep -cE '^#### F-29-0[1-4]:' audit/FINDINGS-v29.0.md)" = "4" ] && ! grep -qE '^#### F-29-0[5-9]:' audit/FINDINGS-v29.0.md && ! grep -qE '^#### F-29-[1-9][0-9]:' audit/FINDINGS-v29.0.md && grep -q "0 CRITICAL\|CRITICAL | 0" audit/FINDINGS-v29.0.md && grep -q "INFO | 4\|INFO.*4" audit/FINDINGS-v29.0.md && grep -q "BAF_TRAIT_SENTINEL" audit/FINDINGS-v29.0.md && grep -q "RNG-consumer determinism" audit/FINDINGS-v29.0.md && grep -q "1646d5af" audit/FINDINGS-v29.0.md && grep -q "AdvanceModule.sol:292" audit/FINDINGS-v29.0.md && grep -q "DegenerusGameJackpotModule.sol:2002\|JackpotModule.sol:2002" audit/FINDINGS-v29.0.md && grep -q "CoverageGap222.t.sol:1453" audit/FINDINGS-v29.0.md && [ -z "$(git diff --name-only -- contracts/ test/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `audit/FINDINGS-v29.0.md` exists and starts with the exact title `# Degenerus Protocol -- Delta Findings Report (v29.0 Post-v27 Contract Delta Audit)`.
    - Contains `## Executive Summary` with severity rows that include `CRITICAL | 0`, `HIGH | 0`, `MEDIUM | 0`, `LOW | 0`, `INFO | 4`, `**Total** | **4**` (or equivalent table formatting — the four zero rows + one INFO=4 row + total=4 must all be present).
    - Contains all six per-phase subsection headers exactly: `### Phase 231: Earlybird Jackpot Audit (0 findings)`, `### Phase 232: Decimator Audit (0 findings)`, `### Phase 232.1: RNG-Index Ticket Drain Ordering Enforcement (0 findings)`, `### Phase 233: Jackpot/BAF + Entropy Audit (2 findings)`, `### Phase 234: Quests / Boons / Misc Audit (1 finding)`, `### Phase 235: Conservation + RNG Commitment Re-Proof + Phase Transition (1 finding)`.
    - Contains EXACTLY four `#### F-29-NN:` blocks: F-29-01, F-29-02, F-29-03, F-29-04. `grep -cE '^#### F-29-0[1-4]:' audit/FINDINGS-v29.0.md` equals 4. `grep -qE '^#### F-29-0[5-9]:'` returns no match. `grep -qE '^#### F-29-[1-9][0-9]:'` returns no match (no F-29-10 or higher).
    - F-29-01 block cites `contracts/modules/DegenerusGameJackpotModule.sol`, line `:69-77` or `:72`, and `BAF_TRAIT_SENTINEL`, `emit sites at :2002` (or the 4 emit-site lines 2002/2014/2034/2038).
    - F-29-02 block cites `contracts/modules/DegenerusGameJackpotModule.sol` line `:80-87` or `:83`, and `JackpotTicketWin`.
    - F-29-03 block cites `test/fuzz/CoverageGap222.t.sol` line `:1453-1455` and the `onlyCoin` negative-test context.
    - F-29-04 block cites `contracts/modules/DegenerusGameAdvanceModule.sol:292` (_swapAndFreeze daily RNG buffer swap), `:1082` (mid-day lootbox RNG buffer swap), `_gameOverEntropy`, and `:625` (terminal unlock). Contains the exact phrase `RNG-consumer determinism`.
    - Every F-29-NN block has `**Severity** | INFO`, a `**Source**` field citing the originating Phase 233/234/235 AUDIT.md, a `**Contract**` field with a contracts/ or test/ path, a `**Function**` field with a file:line, a `**Resolution**` field (OFF-CHAIN-INDEXER-REGEN for F-29-01 + F-29-02; INFO-ACCEPTED for F-29-03; DESIGN-ACCEPTED for F-29-04), and a `**Severity justification:**` paragraph.
    - Contains `## Summary Statistics` + `## Audit Trail` sections mirroring v27.0 shape.
    - Contains the HEAD `1646d5af` anchor somewhere in the Methodology or Scope section.
    - File does NOT yet contain `## Regression Appendix` (Plan 02 adds that).
    - `git diff --name-only -- contracts/ test/` returns empty (no contract/test writes).
    - `git diff --name-only` shows ONLY `audit/FINDINGS-v29.0.md` for this task (KNOWN-ISSUES.md change is the NEXT task).
  </acceptance_criteria>
  <done>`audit/FINDINGS-v29.0.md` exists with v27.0-style structure: title/meta header, Executive Summary (0/0/0/0/4), Findings section with 6 per-phase subsections in phase order, 4 F-29-NN INFO blocks (F-29-01 + F-29-02 for BAF event widening; F-29-03 for QST-01 test-coverage; F-29-04 for Gameover RNG substitution), Summary Statistics, and Audit Trail. Regression Appendix deliberately absent (Plan 02 scope). Zero contracts/ or test/ writes.</done>
</task>

<task type="auto">
  <name>Task 2: Update KNOWN-ISSUES.md with 2 new design-decision entries + 3 targeted v29.0 back-refs on existing entries</name>
  <files>KNOWN-ISSUES.md</files>
  <read_first>
    - KNOWN-ISSUES.md (current repo-root file — study the Design Decisions section formatting: one-sentence bold headline `**...**.` followed by 2-4 sentences of rationale, trailing `(See F-XX-NN in ...)` cite. Existing entries to leave UNMODIFIED: "All rounding favors solvency", "Daily advance assumption", "Non-VRF entropy for affiliate winner roll", "VRF swap governance", "Price feed swap governance", "Chainlink VRF V2.5 dependency", "Backfill cap at 120 gap days", "Lido stETH dependency", "EntropyLib XOR-shift PRNG for lootbox outcome rolls", "Deploy-pipeline VRF_KEY_HASH regex is single-line only", "Parallel `make -j test` mutates `ContractAddresses.sol` concurrently", "v27.0 Phase 222 VERIFICATION gap closures (in-cycle)". Existing entries that get an APPENDED back-ref: "Gameover prevrandao fallback", "Lootbox RNG uses index advance isolation instead of rngLockedFlag", "Decimator settlement temporarily over-reserves claimablePool".)
    - audit/FINDINGS-v29.0.md (just created in Task 1 — for F-29-NN cite targets)
    - .planning/phases/236-regression-findings-consolidation/236-CONTEXT.md (D-05, D-06, D-07, D-08, D-09, D-10, D-11 — locked rules for this task)
  </read_first>
  <action>
Edit `KNOWN-ISSUES.md` at repo root (NOT `audit/KNOWN-ISSUES.md` — that path does not exist; ROADMAP §Phase 236 Success Criterion #4 is a path typo and confirmed by Task 1 read). Three sub-edits:

**Sub-edit A — Add NEW KI entry per D-05: BAF event-widening + BAF_TRAIT_SENTINEL=420 pattern.**

Insert this entry into the "## Design Decisions" section, placed AFTER the existing `**Decimator settlement temporarily over-reserves claimablePool.**` entry and BEFORE the existing `**Deploy-pipeline VRF_KEY_HASH regex is single-line only.**` entry (keeps all v29.0 new entries grouped with the most recent existing entries):

```markdown
**BAF event-widening and `BAF_TRAIT_SENTINEL=420` pattern.** BAF (Big-Ass-Friday) jackpot payouts use a `uint16 private constant BAF_TRAIT_SENTINEL = 420;` (declared at `DegenerusGameJackpotModule.sol:136`) as a sentinel value in event emissions at the four `runBafJackpot` emit sites (`JackpotEthWin` at lines 2002 and 2034; `JackpotTicketWin` at lines 2014 and 2038). 420 is out-of-domain for real trait IDs (`uint8` max = 255) by construction. The `JackpotEthWin` and `JackpotTicketWin` event declarations were widened from `uint8 indexed traitId` to `uint16 indexed traitId` to carry the sentinel (at `DegenerusGameJackpotModule.sol:69-77` field at line 72 and `:80-87` field at line 83 respectively). On-chain EVM topic encoding is 32-byte left-padded regardless of declared width; the widening does NOT change on-chain log behavior. The canonical event signatures change, which changes the keccak-derived `topic0` hash — off-chain ABI consumers (indexers, UI subgraphs, The Graph) must regenerate their ABIs to consume the widened types. The sentinel approach avoids introducing a separate event type per BAF path while preserving structural domain-separation guarantees (wider declared type, out-of-domain value, `private` visibility keeps the symbol module-local). Intentional design. `JackpotBurnieWin` at line 90-96 retains pre-existing `uint8 traitId` at line 93 (non-BAF emitters continue passing real `uint8` values). (See F-29-01 and F-29-02 in `audit/FINDINGS-v29.0.md`)
```

**Sub-edit B — Add NEW KI entry per D-06 + D-07: Gameover RNG substitution for mid-cycle write-buffer tickets.**

Insert this entry into the "## Design Decisions" section, placed immediately AFTER the BAF entry added in Sub-edit A and BEFORE the existing `**Deploy-pipeline VRF_KEY_HASH regex is single-line only.**` entry:

```markdown
**Gameover RNG substitution for mid-cycle write-buffer tickets.** Degenerus enforces an "RNG-consumer determinism" invariant: every RNG consumer's entropy should be fully committed at input time — the VRF word that a consumer will ultimately read must be unknown-but-bound at the moment that consumer's input parameters are committed to storage. Phase 235 RNG-01 and RNG-02 prove this invariant holds for every new RNG consumer in the v29.0 delta under normal and skip-split paths. One terminal-state case technically violates it: if a mid-cycle ticket-buffer swap has occurred (daily RNG request via `_swapAndFreeze(purchaseLevel)` at `DegenerusGameAdvanceModule.sol:292`, OR mid-day lootbox RNG request via `_swapTicketSlot(purchaseLevel_)` at `DegenerusGameAdvanceModule.sol:1082`) and the new write buffer is populated with tickets queued at the current level awaiting the expected-next VRF fulfillment, a game-over event intervening before that fulfillment causes those tickets to drain under `_gameOverEntropy` (at `DegenerusGameAdvanceModule.sol:1222-1246`, VRF-derived with `block.prevrandao` admixture per the "Gameover prevrandao fallback" entry above / F-25-08 when VRF is dead 3+ days) rather than the originally-anticipated VRF word. Acceptance rationale: (a) only reachable at gameover — a terminal state with no further gameplay after the 30-day post-gameover window; (b) no player-reachable exploit — gameover is triggered by a 120-day liveness stall or a pool deficit, neither of which an attacker can time against a specific mid-cycle write-buffer state; (c) at gameover the protocol must drain within bounded transactions and cannot wait for a deferred fulfillment that may never arrive if the VRF coordinator itself is the reason for the liveness stall; (d) all substitute entropy is VRF-derived or VRF-plus-prevrandao. Phase 235 TRNX-01's 4-Path Walk Gameover row verdict (SAFE / Finding Candidate: N) is unchanged — this is a disclosure supplement, not a re-classification. Documented retroactively during Phase 236 consolidation review. (See F-29-04 in `audit/FINDINGS-v29.0.md`)
```

**Sub-edit C — APPEND v29.0 back-refs to three EXISTING KI entries per D-08.**

Locate the three existing design-decision entries listed below and append the specified parenthetical back-reference to the END of each entry's existing parenthetical cite (the trailing `(See F-XX-NN in ...)`). If the existing cite ends with `.)`, insert the v29.0 back-ref BEFORE the closing `)` or append as a new parenthetical adjacent to the existing one — match whichever style keeps the prose readable.

Entry 1 — `**Gameover prevrandao fallback.**` — find the trailing `(See F-25-08 in audit/FINDINGS-v25.0.md)` and append after it (or rewrite the parenthetical to include both cites):

From: `(See F-25-08 in audit/FINDINGS-v25.0.md)` → To: `(See F-25-08 in audit/FINDINGS-v25.0.md; re-verified v29.0 Phase 235 RNG-01 at HEAD 1646d5af — see audit/FINDINGS-v29.0.md regression appendix)`

Entry 2 — `**Lootbox RNG uses index advance isolation instead of rngLockedFlag.**` — find the trailing `(See F-25-07 in audit/FINDINGS-v25.0.md)` and append:

From: `(See F-25-07 in audit/FINDINGS-v25.0.md)` → To: `(See F-25-07 in audit/FINDINGS-v25.0.md; re-verified v29.0 Phase 235 RNG-01 + RNG-02 at HEAD 1646d5af)`

Entry 3 — `**Decimator settlement temporarily over-reserves claimablePool.**` — find the trailing `(See F-25-12 in audit/FINDINGS-v25.0.md)` and append:

From: `(See F-25-12 in audit/FINDINGS-v25.0.md)` → To: `(See F-25-12 in audit/FINDINGS-v25.0.md; re-verified v29.0 Phase 235 CONS-01 at HEAD 1646d5af)`

**Explicit DO-NOT-TOUCH list (per D-08 scope — "Other existing KI entries NOT touched"):**

- `**All rounding favors solvency.**`
- `**Daily advance assumption.**`
- `**Non-VRF entropy for affiliate winner roll.**`
- `**VRF swap governance.**`
- `**Price feed swap governance.**`
- `**Chainlink VRF V2.5 dependency.**`
- `**Backfill cap at 120 gap days.**`
- `**Lido stETH dependency.**`
- `**EntropyLib XOR-shift PRNG for lootbox outcome rolls.**`
- `**Deploy-pipeline VRF_KEY_HASH regex is single-line only.**`
- `**Parallel `make -j test` mutates `ContractAddresses.sol` concurrently.**`
- `**v27.0 Phase 222 VERIFICATION gap closures (in-cycle).**`

Leave all of the above entries 100% unmodified. Also leave the entire "## Automated Tool Findings (Pre-disclosed)", "## ERC-20 Deviations", and "## Event Design Decisions" sections unmodified — v29.0 did not exercise any of those surfaces.

**Explicit DO-NOT-PROMOTE list:**

- Per D-09: `232.1 RNG-index ticket drain-ordering invariant` is NOT promoted. It's hardening that made an implicit invariant explicit — not a new architectural design decision. Zero mention in KI.
- Per D-10: `F-29-03` (QST-01 companion-test-coverage observation FC-234-A) is NOT promoted. Test-tooling observations are not design decisions. Zero mention in KI.
- Per D-11: the v28.0 `D-229-10` directive suppressing KI promotion does NOT apply to v29.0 — v29.0 is a contract-side delta audit with normal v25/v27 KI promotion semantics. Sub-edits A and B proceed as specified.

**Writable-targets gate:** Task 2 writes ONLY `KNOWN-ISSUES.md`. No other file. Do NOT create an `audit/KNOWN-ISSUES.md` variant — the file lives at repo root.
  </action>
  <verify>
    <automated>grep -q "BAF_TRAIT_SENTINEL" KNOWN-ISSUES.md && grep -q "BAF event-widening" KNOWN-ISSUES.md && grep -q "Gameover RNG substitution for mid-cycle write-buffer tickets" KNOWN-ISSUES.md && grep -q "RNG-consumer determinism" KNOWN-ISSUES.md && grep -q "F-29-01 and F-29-02" KNOWN-ISSUES.md && grep -q "F-29-04" KNOWN-ISSUES.md && grep -q "re-verified v29.0 Phase 235 RNG-01" KNOWN-ISSUES.md && grep -q "re-verified v29.0 Phase 235 RNG-01 + RNG-02" KNOWN-ISSUES.md && grep -q "re-verified v29.0 Phase 235 CONS-01" KNOWN-ISSUES.md && ! grep -q "232.1 RNG-index ticket drain-ordering invariant" KNOWN-ISSUES.md && ! grep -qE "F-29-0?3[^0-9]" KNOWN-ISSUES.md && ! test -f audit/KNOWN-ISSUES.md && [ -z "$(git diff --name-only -- contracts/ test/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep "BAF_TRAIT_SENTINEL" KNOWN-ISSUES.md` returns a match (Sub-edit A KI entry added).
    - `grep "BAF event-widening" KNOWN-ISSUES.md` returns a match (Sub-edit A title phrase).
    - `grep "Gameover RNG substitution for mid-cycle write-buffer tickets" KNOWN-ISSUES.md` returns a match (Sub-edit B KI entry added).
    - `grep "RNG-consumer determinism" KNOWN-ISSUES.md` returns a match (invariant name codified per D-06).
    - `grep "F-29-01 and F-29-02" KNOWN-ISSUES.md` returns a match (Sub-edit A cite).
    - `grep "F-29-04" KNOWN-ISSUES.md` returns a match (Sub-edit B cite).
    - `grep "re-verified v29.0 Phase 235 RNG-01 at HEAD 1646d5af" KNOWN-ISSUES.md` returns a match (Sub-edit C Entry 1 — Gameover prevrandao back-ref).
    - `grep "re-verified v29.0 Phase 235 RNG-01 + RNG-02" KNOWN-ISSUES.md` returns a match (Sub-edit C Entry 2 — Lootbox index-advance back-ref).
    - `grep "re-verified v29.0 Phase 235 CONS-01" KNOWN-ISSUES.md` returns a match (Sub-edit C Entry 3 — Decimator over-reserves back-ref).
    - `grep "232.1 RNG-index ticket drain-ordering invariant" KNOWN-ISSUES.md` returns NO match (D-09 suppression).
    - `grep -qE "F-29-0?3[^0-9]" KNOWN-ISSUES.md` returns NO match (F-29-03 NOT in KI per D-10).
    - `test -f audit/KNOWN-ISSUES.md` returns false (no stray audit/KNOWN-ISSUES.md file created).
    - `grep "All rounding favors solvency" KNOWN-ISSUES.md` returns a match AND the line is unchanged from pre-edit state (existing DO-NOT-TOUCH entry intact).
    - `grep "Deploy-pipeline VRF_KEY_HASH regex is single-line only" KNOWN-ISSUES.md` returns a match (existing DO-NOT-TOUCH entry intact).
    - `grep "Parallel" KNOWN-ISSUES.md` returns a match (existing DO-NOT-TOUCH entry intact).
    - `git diff --name-only -- contracts/ test/` returns empty.
    - `git diff --name-only` shows ONLY `KNOWN-ISSUES.md` for this task.
    - `grep "## Automated Tool Findings" KNOWN-ISSUES.md` returns a match (section header preserved).
    - `grep "## ERC-20 Deviations" KNOWN-ISSUES.md` returns a match (section header preserved).
    - `grep "## Event Design Decisions" KNOWN-ISSUES.md` returns a match (section header preserved).
  </acceptance_criteria>
  <done>`KNOWN-ISSUES.md` has the two new Design Decisions entries (BAF event-widening + BAF_TRAIT_SENTINEL=420 pattern citing F-29-01/02; Gameover RNG substitution for mid-cycle write-buffer tickets citing F-29-04 and codifying the "RNG-consumer determinism" invariant), and three existing entries have appended v29.0 back-refs (Gameover prevrandao fallback → Phase 235 RNG-01; Lootbox RNG index advance isolation → Phase 235 RNG-01 + RNG-02; Decimator settlement over-reserves claimablePool → Phase 235 CONS-01). 232.1 RNG-index ordering invariant NOT promoted (D-09); F-29-03 NOT promoted (D-10); all other existing KI entries unmodified. No audit/KNOWN-ISSUES.md created (file lives at repo root). Zero contracts/ or test/ writes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation only | This plan produces audit documentation (audit/FINDINGS-v29.0.md) and updates pre-disclosure (KNOWN-ISSUES.md) — no contract code modified, no runtime surface touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-236-01 | Information Disclosure | audit/FINDINGS-v29.0.md | accept | Public audit document intended for disclosure (C4A wardens + external auditors). No secrets. The document discloses F-29-04 (Gameover RNG substitution) — acceptance rationale includes player-reachability analysis (not attacker-timeable due to structural gameover triggers). |
| T-236-02 | Information Disclosure | KNOWN-ISSUES.md | accept | Public pre-disclosure document. Discloses the "RNG-consumer determinism" invariant name and its one known violation — intentional per D-06/D-07 acceptance rationale. |
| T-236-03 | Tampering | audit/FINDINGS-v29.0.md + KNOWN-ISSUES.md | mitigate | Git-tracked files; diff review before commit per `feedback_manual_review_before_push.md`. Acceptance-criteria grep gates in Tasks 1 and 2 verify the exact strings land in the exact files. No code execution paths touched. |
| T-236-04 | Spoofing | F-29-NN finding-ID assignment | mitigate | Finding IDs are assigned contiguously F-29-01..F-29-04 per 236-CONTEXT.md D-01; Task 1 acceptance criteria enforce exactly-four-blocks-in-sequence (no gaps, no duplicates, no F-29-05+). |
</threat_model>

<verification>
1. `audit/FINDINGS-v29.0.md` exists with v27.0 structure (Executive Summary severity table + Overall Assessment + per-phase subsections in order 231→232→232.1→233→234→235 + per-finding blocks + Summary Statistics + Audit Trail).
2. Executive Summary shows 0 CRITICAL / 0 HIGH / 0 MEDIUM / 0 LOW / 4 INFO / Total 4 per D-04.
3. Four F-29-NN INFO blocks present: F-29-01 + F-29-02 (BAF event widening) + F-29-03 (QST-01 test-coverage) + F-29-04 (Gameover RNG substitution).
4. Every finding block has Severity, Source, Contract, Function, Resolution fields + severity-justification paragraph.
5. F-29-04 block references `AdvanceModule.sol:292` + `AdvanceModule.sol:1082` + `_gameOverEntropy` + the exact phrase "RNG-consumer determinism".
6. HEAD anchor `1646d5af` present in the Methodology or Scope section.
7. KNOWN-ISSUES.md gains the two new entries (BAF event-widening; Gameover RNG substitution) with correct F-29-NN cites.
8. Three existing KI entries have v29.0 Phase 235 back-refs appended.
9. 232.1 RNG-index invariant is NOT in KI (D-09). F-29-03 is NOT in KI (D-10).
10. No stray `audit/KNOWN-ISSUES.md` file created.
11. Zero contracts/ or test/ writes (per standing policy + 236-CONTEXT.md scope).
12. Regression Appendix is NOT in the file (Plan 02 scope) — Task 1 ends at the cross-reference note.
</verification>

<success_criteria>
FIND-01 satisfied: all four v29.0 Finding Candidate: Y items severity-classified in v27.0-style per-finding blocks in `audit/FINDINGS-v29.0.md` (0/0/0/0/4 per D-04).
FIND-02 satisfied: `KNOWN-ISSUES.md` has two new design-decision entries (per D-05, D-06) and three back-refs (per D-08); D-09 and D-10 suppression rules honored.
FIND-03 (partial — exec summary portion): Executive Summary per-phase counts + per-severity totals published as specified.
Writable-target gate holds: no writes outside `audit/FINDINGS-v29.0.md` + `KNOWN-ISSUES.md`; zero contracts/ or test/ writes.
Plan 02 downstream consumer: Plan 236-02 appends the Regression Appendix to the same `audit/FINDINGS-v29.0.md` file produced by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/236-regression-findings-consolidation/236-01-SUMMARY.md` recording: (1) file paths modified with one-line diff summary, (2) canonical F-29-NN range used (F-29-01..F-29-04), (3) F-29-01/02 consolidation decision (kept as two separate blocks; KI entry consolidated per D-05), (4) confirmation of `git diff --name-only -- contracts/ test/` empty, (5) confirmation no `audit/KNOWN-ISSUES.md` was created, (6) hand-off note for Plan 02 — Regression Appendix appends to `audit/FINDINGS-v29.0.md` at the cross-reference note placeholder.
</output>
