---
phase: 233-jackpot-baf-entropy-audit
plan: 233-02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/233-jackpot-baf-entropy-audit/233-02-AUDIT.md
autonomous: true
requirements: [JKP-02]
must_haves:
  truths:
    - "Every function modified by commit 52242a10 in 230-01-DELTA-MAP.md §1.1 / §1.4 / §1.11 (advanceGame caller sites + _processFutureTicketBatch + _prepareFutureTickets + _consolidatePoolsAndRewardJackpots + MintModule processFutureTicketBatch + IDegenerusGameMintModule.processFutureTicketBatch interface) has at least one verdict row in the per-function verdict table"
    - "The three AdvanceModule call sites that thread `rngWord` into `_processFutureTicketBatch` / `_prepareFutureTickets` each have a dedicated verdict row: IM-10 (advanceGame FF-promotion, line 315-318), IM-11 (advanceGame near-future prep, line 337 — actually `_prepareFutureTickets` which internally calls _processFutureTicketBatch), IM-12 (_consolidatePoolsAndRewardJackpots post-transition FF, line 407)"
    - "IM-13 delegatecall boundary (AdvanceModule._processFutureTicketBatch -> MintModule.processFutureTicketBatch via abi.encodeWithSelector with the new 2-arg selector) has a verdict row confirming selector alignment + argument ordering (lvl, entropy)"
    - "MintModule.processFutureTicketBatch body change — specifically the REMOVAL of the `uint256 entropy = rngWordCurrent;` SLOAD at the top of the function body — has a verdict row confirming entropy is now caller-provided and the SLOAD is gone"
    - "Every verdict row cites the 7-char commit SHA `52242a10` (per D-02)"
    - "Every verdict row cites a File:Line anchor pointing into contracts/modules/DegenerusGameAdvanceModule.sol, contracts/modules/DegenerusGameMintModule.sol, or contracts/interfaces/IDegenerusGameModules.sol at HEAD (per D-02)"
    - "Verdict vocabulary used is exactly `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (per D-02)"
    - "Every verdict row carries a `Finding Candidate: Y/N` column value"
    - "No F-29-NN finding IDs appear anywhere in 233-02-AUDIT.md (per D-11)"
    - "D-05 entropy-equivalence proof: for each of the three AdvanceModule call sites, the plan proves the passed `entropy` value is byte-identical to what the old code would have SLOAD'd from `rngWordCurrent` at the same logical point. Forward-trace from `rngWord` local (derived from `rngGate` return at AdvanceModule:283-289) to the delegatecall payload, cross-referenced against old `rngWordCurrent` SLOAD semantics at MintModule (pre-fix, `52242a10^`)."
    - "D-06 backward-trace (per feedback_rng_backward_trace.md): each entropy consumer is traced backward from consumption to VRF fulfillment to verify the VRF word was unknown at input commitment time. Backward-trace must cover: (a) rngWord local -> rngGate -> rngWordByDay[day] SLOAD -> rawFulfillRandomWords SSTORE -> VRF coordinator callback, (b) confirm no player-controllable input flows between VRF fulfillment and the entropy-consuming call site."
    - "D-06 commitment-window enumeration (per feedback_rng_commitment_window.md): plan EXPLICITLY ENUMERATES every player-controllable state variable that can change between VRF request and fulfillment and verifies NON-INFLUENTIAL for each new entropy consumer at IM-10 / IM-11 / IM-12 / IM-13. Per user directive, the enumeration MUST be a concrete table — not a generic 'commitment window preserved' assertion."
    - "D-07 no-re-use-bias check: when the same `rngWord` is passed to multiple consumers within the same advanceGame transaction (e.g. FF-promotion at IM-10 + near-future prep at IM-11 + post-transition FF at IM-12), a salt-space / domain-separation audit of each consumer's use of `entropy` is performed to prove no bias is introduced by the shared seed."
    - "D-10 ID-103 interface-drift PASS cited once as evidence that the pure-drift subquestion is closed (Phase 230 §3.3.f verdict); remaining analysis focuses on semantic passthrough not interface parity"
    - "D-12 230-02-DELTA-ADDENDUM.md integration: plan MUST include verdict rows for the post-c2e5e0a9 keccak formulations that downstream consumers (inside MintModule._raritySymbolBatch at line 540-571, _rollRemainder at line 647, and the 16 JackpotModule sites) use to mix `entropy` with `lvl` / `baseKey` / `traitIdx` / `player`. Each new keccak site that sits DOWNSTREAM of the entropy-passthrough boundary is verdict-table listed with cryptographic-equivalence evidence (hash2(a,b) vs keccak256(abi.encode(a,b)) / keccak256(abi.encode(a,b,c)) collision analysis per addendum page 2)"
    - "D-12 Also: verify post-c2e5e0a9 formulation consumers do NOT re-introduce domain-collision between 2-input hash2 and 3-input abi.encode forms (addendum already provides the analysis — JKP-02 cites + re-verifies from HEAD)"
    - "Findings-Candidate Block, Scope-guard Deferrals, and Downstream Hand-offs subsections present; Phase 235 RNG-01 / RNG-02 hand-offs explicitly named as the milestone-wide proof owners; Phase 236 FIND-01 hand-off explicitly named"
    - "Zero contracts/ and zero test/ writes occur during plan execution (READ-only per v29.0 milestone rule)"
  artifacts:
    - path: ".planning/phases/233-jackpot-baf-entropy-audit/233-02-AUDIT.md"
      provides: "JKP-02 explicit entropy passthrough adversarial audit"
      contains: "## Per-Function Verdict Table"
  key_links:
    - from: "230-01-DELTA-MAP.md §1.1 + §1.4 + §1.11 + §2.3 IM-10..IM-13 + §2.5 IM-22 + §3.3.f ID-103 + §4 JKP-02 row (52242a10)"
      to: "233-02-AUDIT.md per-function verdict table rows"
      via: "Every 52242a10 function in the delta map has a verdict + delegatecall boundary row"
      pattern: "SAFE|SAFE-INFO|VULNERABLE|DEFERRED"
    - from: "233-02-AUDIT.md backward-trace + commitment-window subsections"
      to: "Phase 235 RNG-01 / RNG-02 milestone-wide proof"
      via: "Downstream Hand-offs subsection (passthrough-specific slice feeds into milestone-wide enumeration)"
      pattern: "Phase 235 RNG-01|Phase 235 RNG-02"
    - from: "230-02-DELTA-ADDENDUM.md c2e5e0a9 per-file verdict table rows (17 keccak sites)"
      to: "233-02-AUDIT.md post-addendum keccak-formulation verdict rows"
      via: "D-12 integration — each downstream consumer of the entropy-passthrough boundary now threads through keccak rather than XOR+xorshift"
      pattern: "keccak256|hash2|EntropyLib"
    - from: "233-02-AUDIT.md VULNERABLE/DEFERRED verdicts"
      to: "Phase 236 FIND-01 finding-candidate pool"
      via: "Findings-Candidate Block (no F-29-NN IDs, Finding Candidate: Y/N column)"
      pattern: "## Findings-Candidate Block"
---

<objective>
JKP-02 adversarial audit of the explicit entropy passthrough to `processFutureTicketBatch` introduced by commit `52242a10` ("refactor: explicit entropy passthrough to processFutureTicketBatch"). Produces `233-02-AUDIT.md`: a per-function verdict table covering the three AdvanceModule call sites that now thread `rngWord` forward (IM-10 FF-promotion at `advanceGame:315-318`, IM-11 near-future prep at `advanceGame:337` + internals, IM-12 post-transition FF at `_consolidatePoolsAndRewardJackpots:407`), the AdvanceModule helper `_processFutureTicketBatch:1383` / `_prepareFutureTickets:1408` signature bumps, the MintModule receiver `processFutureTicketBatch:385` (SLOAD removed, entropy now caller-provided), the IDegenerusGameMintModule.processFutureTicketBatch interface signature bump, and the IM-13 delegatecall boundary. Plus a D-06 backward-trace subsection (VRF word unknown at input commitment time), a D-06 commitment-window enumeration table (every player-controllable state variable between VRF request and fulfillment with a non-influential verdict per consumer), a D-07 no-re-use-bias analysis, and a D-12 230-02-DELTA-ADDENDUM.md integration section covering the post-`c2e5e0a9` keccak formulations that now sit downstream of the entropy-passthrough boundary. Plus Findings-Candidate / Scope-guard Deferrals / Downstream Hand-offs subsections.

Purpose: `IDegenerusGameMintModule.processFutureTicketBatch` gained a second parameter `uint256 entropy`; the implementer at `DegenerusGameMintModule.sol:385` removed the `uint256 entropy = rngWordCurrent;` SLOAD at the top of the function body; three call sites in `DegenerusGameAdvanceModule.advanceGame` + `_consolidatePoolsAndRewardJackpots` now thread `rngWord` (derived from `rngGate` return at line 283) forward through `_processFutureTicketBatch` -> `abi.encodeWithSelector(IDegenerusGameMintModule.processFutureTicketBatch.selector, lvl, entropy)`. A C4A warden would probe: (a) cryptographic equivalence — is the passed `entropy` byte-identical to what the old code would have SLOAD'd from `rngWordCurrent` at the same logical point (D-05 forward-trace proof)?, (b) backward trace — was the VRF word unknown at input commitment time (D-06 per `feedback_rng_backward_trace.md`)?, (c) commitment-window integrity — is every player-controllable state variable that can change between VRF request and fulfillment explicitly enumerated and verified non-influential for each new consumer (D-06 per `feedback_rng_commitment_window.md`, per user directive an explicit enumeration table, not a generic assertion)?, (d) no re-use bias — when the same `rngWord` is passed to multiple consumers in the same advanceGame transaction, is domain-separation preserved such that no bias arises from the shared seed (D-07)?, (e) post-`c2e5e0a9` keccak formulations — do the 17 downstream keccak sites introduced by the hardening commit consume `entropy` without re-introducing the low-bit diffusion bug or cross-formulation collision (D-12 integration with addendum)? Phase 235 RNG-01 / RNG-02 owns the milestone-wide proof across ALL new RNG consumers (earlybird bonus-trait roll, BAF sentinel emission, entropy passthrough, phase-transition _unlockRng removal); this plan provides the PASSTHROUGH-SPECIFIC slice that Phase 235 will cite via §2.5 IM-22 in the 230 catalog.

Output: `233-02-AUDIT.md` with per-function verdict table + backward-trace subsection + commitment-window enumeration table + no-re-use-bias note + 230-02-DELTA-ADDENDUM.md integration section + Findings-Candidate Block + Scope-guard Deferrals + Downstream Hand-offs subsections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/233-jackpot-baf-entropy-audit/233-CONTEXT.md
@.planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md
@.planning/phases/230-delta-extraction-scope-map/230-02-DELTA-ADDENDUM.md
@.planning/phases/230-delta-extraction-scope-map/230-01-SUMMARY.md
@.planning/phases/231-earlybird-jackpot-audit/231-02-PLAN.md
@.planning/phases/232-decimator-audit/232-01-PLAN.md
@.planning/phases/232-decimator-audit/232-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract 52242a10 diff + read AdvanceModule / MintModule / IDegenerusGameModules at HEAD; apply D-05 forward-trace + D-06 backward-trace + D-06 commitment-window enumeration + D-07 no-re-use + D-12 addendum integration; write 233-02-AUDIT.md</name>
  <files>.planning/phases/233-jackpot-baf-entropy-audit/233-02-AUDIT.md</files>
  <read_first>
    - .planning/phases/233-jackpot-baf-entropy-audit/233-CONTEXT.md (D-01 through D-12 — specifically: D-01 three-plan split with Plan 233-02 owning JKP-02 only; D-02 locked column schema + Finding Candidate extension per 232-NN-AUDIT.md precedent; D-03 scope-guard deferral rule; D-05 entropy-equivalence proof method — forward-trace from rngWord local to delegatecall payload cross-referenced against old rngWordCurrent SLOAD semantics; D-06 backward-trace + commitment-window rules EXPLICITLY applied at the passthrough boundary — Phase 235 RNG-01/02 is the milestone-wide proof, JKP-02 is the passthrough-specific slice; D-07 no-re-use-bias check for shared-seed usage across multiple consumers in the same tx; D-10 ID-103 interface-drift PASS cited once, focus on semantic passthrough; D-11 no F-29-NN IDs emitted; D-12 MUST read 230-02-DELTA-ADDENDUM.md AND include verdict rows for post-c2e5e0a9 keccak formulations that sit downstream of the passthrough boundary — JKP-02 is the MOST-AFFECTED requirement by the addendum)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.1 (lines 74-89 — specifically: `advanceGame` MODIFIED by 2471f8e7 + 52242a10 at line 80 — for 52242a10 scope: "threads `rngWord` through the `_processFutureTicketBatch` and `_prepareFutureTickets` call sites (FF promotion + near-future prep)"; `_consolidatePoolsAndRewardJackpots` MODIFIED by 3ad0f8d3 at line 81 — 52242a10 impact is secondary; `_processFutureTicketBatch(uint24 lvl, uint256 entropy)` MODIFIED by 52242a10 at line 84 — "Signature gained `uint256 entropy` parameter; forwarded through the `abi.encodeWithSelector(IDegenerusGameMintModule.processFutureTicketBatch.selector, lvl, entropy)` delegatecall payload"; `_prepareFutureTickets(uint24 lvl, uint256 entropy)` MODIFIED by 52242a10 at line 85 — "Signature gained `uint256 entropy` parameter; both the in-flight-resume call and the per-target loop now forward entropy to `_processFutureTicketBatch`")
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.4 (lines 122-132 — specifically: `processFutureTicketBatch(uint24 lvl, uint256 entropy)` MODIFIED by 52242a10 at line 128 — "Signature gained `uint256 entropy` parameter; the internal `uint256 entropy = rngWordCurrent;` SLOAD was removed — entropy is now caller-provided (today's daily RNG word from `rngWordByDay[day]` threaded via `_processFutureTicketBatch` -> delegatecall payload). Removes reliance on `rngWordCurrent` staying set across downstream state transitions.")
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.11 (lines 202-210 — `IDegenerusGameMintModule.processFutureTicketBatch(uint24 lvl, uint256 entropy)` MODIFIED by 52242a10 — "Parameter list gained `uint256 entropy`. NatSpec documents 'caller passes today's daily RNG word'. This declaration is the selector reference used by `DegenerusGameAdvanceModule._processFutureTicketBatch` delegatecall payload — must stay in lockstep with the implementer (§1.4).")
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §2.3 rows IM-10, IM-11, IM-12, IM-13 (lines 259-262 — the four primary 52242a10 chain rows): IM-10 advanceGame -> _processFutureTicketBatch FF-promotion, "FF-promotion call site (around source line 298-303) gained `rngWord` as the second argument — entropy is now threaded from `rngGate` return into `_processFutureTicketBatch`"; IM-11 advanceGame -> _prepareFutureTickets, "Near-future prep call site (around source line 321-326) gained `rngWord` as the second argument"; IM-12 _consolidatePoolsAndRewardJackpots -> _processFutureTicketBatch, "Call site around source line 392 gained `rngWord` as the second argument"; IM-13 _processFutureTicketBatch -> MintModule.processFutureTicketBatch delegatecall, "`abi.encodeWithSelector(IDegenerusGameMintModule.processFutureTicketBatch.selector, lvl)` -> `..., lvl, entropy)`. New selector: the interface declaration in `IDegenerusGameModules.sol` was updated in lockstep (§1.11). This is the primary entropy-passthrough boundary."
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §2.5 IM-22 (line 281 — the replay row for Phase 235 RNG-01/02 cross-reference): "Same chain as IM-13 — replayed here so RNG-01 / RNG-02 auditors can cite the entropy-commitment boundary from their own phase. The `uint256 entropy` argument now arrives via calldata instead of being SLOAD'd from `rngWordCurrent` inside the callee — this is the commitment-window re-proof handle."
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §3.3.f ID-103 (line 451 — interface-drift PASS at HEAD for `processFutureTicketBatch(uint24 lvl, uint256 entropy) external returns (bool worked, bool finished, uint32 writesUsed)` — "Interface and implementer both gained `uint256 entropy` parameter in lockstep (52242a10, §1.4 / §1.11). Selector hash changes; the delegatecall call-site in `DegenerusGameAdvanceModule._processFutureTicketBatch` was updated in the same commit.") — per D-10, cite ID-103 once as evidence the pure-drift subquestion is closed; do NOT re-verify at file:line level; focus remaining analysis on semantic passthrough
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §4 Consumer Index JKP-02 row (line 556 — authoritative scope anchor per D-04: "§1.4 (processFutureTicketBatch MODIFIED by 52242a10), §1.1 (_processFutureTicketBatch / _prepareFutureTickets / advanceGame MODIFIED by 52242a10), §1.11 (IDegenerusGameMintModule.processFutureTicketBatch MODIFIED), §3.3.f ID-103, §2.3 (IM-10..IM-13), §2.5 IM-22, commit 52242a10 rows")
    - .planning/phases/230-delta-extraction-scope-map/230-02-DELTA-ADDENDUM.md (ENTIRE FILE — MOST AFFECTED requirement per D-12): Specifically the `c2e5e0a9` per-file table covering 17 keccak sites downstream of the entropy-passthrough boundary — DegenerusGameMintModule.sol:647 `_rollRemainder` uses `hash2(entropy, rollSalt)`; DegenerusGameJackpotModule.sol has 16 sites at 277, 443, 508, 522, 544, 594, 596, 607-609, 874, 937, 1134, 1238, 1345, 1681-1683, 1741, 1798-1800, 1808 each using `keccak256(abi.encode(entropy, <X>, [TAG]))` or `hash2(entropy, iterator)`; DegenerusGamePayoutUtils.sol:68-70 `_calcAutoRebuy` uses `keccak256(abi.encode(entropy, beneficiary, weiAmount))`; EntropyLib.sol:26-37 NEW `hash2(uint256, uint256) -> uint256` helper with `memory-safe` asm on scratch slots 0x00/0x20. The addendum's cryptographic collision analysis (page 2: `hash2(a,b)` hashes 64 bytes `[32-byte a][32-byte b]`; `keccak256(abi.encode(a, b, c))` hashes 96 bytes — different lengths -> different preimages -> keccak collision-resistance guarantees no accidental cross-formulation collisions) is the foundational evidence JKP-02 cites.
    - .planning/phases/230-delta-extraction-scope-map/230-01-SUMMARY.md Known Non-Issues (all 5 entries) — particularly #4 which references delegatecall-site count growth 43 -> 44 (tied to IM-08 858d83e4, not the 52242a10 passthrough — record as non-issue for JKP-02 scope)
    - Output of `git show 52242a10 --stat` and `git show 52242a10 -- contracts/modules/DegenerusGameAdvanceModule.sol contracts/modules/DegenerusGameMintModule.sol contracts/interfaces/IDegenerusGameModules.sol` — the delta IS the audit surface. Pre-fix source (`52242a10^`) shows the `uint256 entropy = rngWordCurrent;` SLOAD at the top of MintModule's processFutureTicketBatch body + the 1-arg selector in AdvanceModule._processFutureTicketBatch + the 1-arg interface decl; HEAD shows entropy-as-parameter + 2-arg selector + 2-arg interface decl.
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD — enumerate EVERY call site in the delta + the `rngGate` return-to-delegatecall forward-trace:
        * `advanceGame() external` at line 156 — main entry point
        * `rngGate(ts, day, purchaseLevel, lastPurchase, bonusFlip) returns (uint256 rngWord, uint32 gapDays)` call site at line 283-289 — the ORIGIN of the `rngWord` local that flows to every downstream consumer
        * `rngGate` function definition at line 1133 — internal: `uint256 currentWord = rngWordCurrent;` SLOAD at line 1143; return of nudged word; plus `rngWordCurrent = 0;` SSTORE after return
        * Phase-transition FF promotion call site (IM-10) at line 315-318: `(bool ffWorked, bool ffFinished, ) = _processFutureTicketBatch(ffLevel, rngWord);` — record the exact File:Line and the rngWord argument derivation
        * Near-future prep call site (IM-11) at line 337: `!_prepareFutureTickets(...)` — record the exact File:Line and rngWord argument derivation; note that IM-11 in §2.3 references `_prepareFutureTickets` not `_processFutureTicketBatch` directly — document the internal `_prepareFutureTickets` call flow at line 1408-1438 that forwards `entropy` to `_processFutureTicketBatch` via lines 1418-1421 (resume path) and 1426-1431 (target loop)
        * Post-transition FF call site (IM-12) at line 407: `) = _processFutureTicketBatch(nextLevel, rngWord);` — record the exact File:Line and rngWord argument derivation
        * `_processFutureTicketBatch(uint24 lvl, uint256 entropy) private returns (bool worked, bool finished, uint32 writesUsed)` helper at line 1383 — record the delegatecall selector payload at line 1390-1394: `abi.encodeWithSelector(IDegenerusGameMintModule.processFutureTicketBatch.selector, lvl, entropy)`
        * `_prepareFutureTickets(uint24 lvl, uint256 entropy) private returns (bool finished)` helper at line 1408 — record the forwarding of entropy into `_processFutureTicketBatch` at lines 1418 + 1428
        * `rngWordCurrent` state variable references across the file: grep all reads + writes. Record every SLOAD site (pre-fix rngWordCurrent readers) and every SSTORE site (`rngWordCurrent = 0;` at 1108, 1285, 1577, 1638, 1677; `rngWordCurrent = word;` at 1702; etc.). Build a timeline of when `rngWordCurrent` is set vs cleared relative to the three call sites.
        * `rawFulfillRandomWords(uint256 requestId, uint256[] calldata randomWords)` at line 1690 — the VRF callback setter `rngWordCurrent = word;` at line 1702; also the zero-guard `if (word == 0) word = 1;` at line 1698 (per Phase 232.1 Plan 03 audit — cite as precedent evidence that rngWordCurrent cannot be 0 post-fulfillment)
        * Timeline reconstruction: `advanceGame` entry -> `rngGate` -> `currentWord = rngWordCurrent;` SLOAD -> nudges applied -> return `rngWord` to caller -> `rngWordCurrent = 0;` (inside rngGate, line 1108) -> caller flows through IM-10/11/12 using the LOCAL `rngWord`, not SLOAD. The commitment-window question is: BEFORE `rngWordCurrent` becomes non-zero (VRF callback), can any player action influence the VRF coordinator request parameters?
    - contracts/modules/DegenerusGameMintModule.sol at HEAD — enumerate EVERY consumer of the new `entropy` parameter inside `processFutureTicketBatch` body:
        * `processFutureTicketBatch(uint24 lvl, uint256 entropy) external returns (bool worked, bool finished, uint32 writesUsed)` at line 385 (NEW signature; pre-fix was `processFutureTicketBatch(uint24 lvl)`). NOTE: the pre-fix SLOAD `uint256 entropy = rngWordCurrent;` at the top of the body has been REMOVED — confirm via `git show 52242a10^:contracts/modules/DegenerusGameMintModule.sol` that pre-fix shows the SLOAD and HEAD does not.
        * Every use of `entropy` inside the function body (grep pattern `entropy` inside the function scope lines 385-498):
          - line 443: `if (!_rollRemainder(entropy, baseKey, rem)) {` — consumer #1 (invokes _rollRemainder which per c2e5e0a9 uses hash2)
          - line 469: `_raritySymbolBatch(player, baseKey, processed, take, entropy);` — consumer #2 (invokes _raritySymbolBatch which per 314443af uses keccak256(abi.encode(baseKey, entropyWord, groupIdx)))
          - line 476: `entropy` in `emit TraitsGenerated(player, lvl, uint32(idx), processed, take, entropy)` — consumer #3 (event-output only — no on-chain re-read)
          - line 489: `if (_rollRemainder(entropy, baseKey, rem)) {` — consumer #4 (second _rollRemainder invocation; same as consumer #1)
        * `_raritySymbolBatch(player, baseKey, processed, take, entropyWord)` at line 540-571 — post-`314443af` uses `uint256 seed = uint256(keccak256(abi.encode(baseKey, entropyWord, groupIdx)));` at line 568. This is a consumer downstream of the entropy-passthrough boundary. Record the keccak formulation.
        * `_rollRemainder(uint256 entropy, uint256 rollSalt, uint8 rem) returns (bool win)` at line 643 — post-`c2e5e0a9` uses `uint256 rollEntropy = EntropyLib.hash2(entropy, rollSalt);` at line 652. This is a consumer downstream of the entropy-passthrough boundary. Record the hash2 formulation.
        * `_raritySymbolBatch` and `_rollRemainder` are also called from OTHER contexts inside MintModule (e.g. line 700 `entropy = lootboxRngWordByIndex[...]`) — these are OUT of JKP-02 scope (they feed from storage not from the new entropy parameter) but document them as "parallel entropy sources, non-overlapping with JKP-02 passthrough scope".
    - contracts/interfaces/IDegenerusGameModules.sol at HEAD — locate `interface IDegenerusGameMintModule` and confirm the `processFutureTicketBatch(uint24 lvl, uint256 entropy) external returns (bool worked, bool finished, uint32 writesUsed)` declaration is at HEAD. Cite §1.11 + §3.3.f ID-103 as the authoritative Phase 230 verdict (PASS at HEAD). Per D-10, do NOT re-verify at file:line level.
    - contracts/storage/DegenerusGameStorage.sol — grep for `rngWordCurrent` declaration. Record the storage slot layout (uint256 rngWordCurrent) and visibility. This is the pre-fix SLOAD source that has been removed.
    - Player-controllable state enumeration (D-06 commitment-window rule — per user directive an EXPLICIT table):
        * Grep contracts/ for every state variable that can be written by a non-privileged (non-Game, non-admin) caller — i.e. any `external` or `public` function that takes msg.sender as the actor and writes to storage. Candidates include: level (via advanceGame — but advanceGame is permissionless), lootboxRngWordByIndex (via VRF callback only — NOT player-controllable), ticketsOwedPacked (via purchase paths), currentPrizePool / nextPrizePool / futurePrizePool (via multiple paths), ticketQueue (via purchase paths), mintPacked_ (via recordMintData), dailyCoinPool, traitBurnTicket (via purchase paths), etc.
        * For EACH candidate variable, verify whether it can be modified BETWEEN VRF request (`VRFCoordinatorV2_5.requestRandomWords` call at the VRF-request site — grep AdvanceModule for the coordinator call) AND VRF fulfillment (`rawFulfillRandomWords` at line 1690).
        * VRF window reconstruction: rngLocked gate is set TRUE by requestLootboxRng / advanceGame at VRF-request time (grep for `rngLocked = true` writes); rngLocked is cleared by `_unlockRng` or `rawFulfillRandomWords` zero-guard. During `rngLocked == true`, all permissionless mutation paths that would invalidate the commitment window are blocked by the rngLocked guard (this is the pre-existing RNG commitment-window invariant from v25.0 Phase 215).
        * For the NEW consumer at IM-13 (MintModule.processFutureTicketBatch) — does the passed `entropy` (which is today's daily RNG word) arrive BEFORE or AFTER any player-controllable state could alter the entropy-consumer's behavior? The answer: entropy is committed at VRF request time; the consumer at IM-13 runs inside the SAME `advanceGame` transaction that received `entropy` from `rngGate`; no player tx can interleave between rngGate return (line 283) and the IM-10/11/12 delegatecall (the entire advanceGame call is atomic).
        * Per user directive: "MUST explicitly enumerate every player-controllable state variable that can change between VRF request and fulfillment and verify non-influential for each new entropy consumer". Output a table with columns: `Variable | File:Line (decl) | Writer Set | Can Change Between VRF Request and Fulfillment? | Influences IM-13 Consumer Behavior? | Verdict`. Expected outcome: all candidate variables are either (a) guarded by rngLocked (no-change-during-window), or (b) do not influence the entropy-consumer's output function, or (c) influence only pre-commitment state that advanceGame captured before rngGate.
    - .planning/milestones/v25.0-phases/215-rng-fresh-eyes/ references (mentioned in 233-CONTEXT.md under methodology precedent as 215-02-BACKWARD-TRACE.md and 215-03-COMMITMENT-WINDOW.md). These are the direct templates for JKP-02's D-06 backward-trace + commitment-window application.
    - .planning/phases/231-earlybird-jackpot-audit/231-02-PLAN.md — precedent plan-of-record structural template (the 20a951df trait-alignment audit); cite as methodology precedent for JKP-02 plan shape. Do NOT carry forward 231-02 verdicts — audit every function body fresh from HEAD per D-03.
    - .planning/phases/232-decimator-audit/232-01-PLAN.md and 232-03-PLAN.md — precedent audit-phase PLAN.md shape (frontmatter must_haves block, objective, execution_context, tasks with read_first / action / verify / acceptance_criteria / done structure). Mirror this structure for 233-02.
  </read_first>
  <action>
Execute a fresh-from-source adversarial audit of the `52242a10` entropy-passthrough refactor per CONTEXT.md D-01 through D-12. Verdict vocabulary per D-02 is strictly `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`. Column schema per D-02 is strictly `Function | Commit SHA | File:Line | Verdict | Evidence | Notes` with an additional `Finding Candidate: Y/N` column per 232-NN-AUDIT.md precedent.

**Step 1 — Build the scope list from `230-01-DELTA-MAP.md` §4 Consumer Index JKP-02 row (D-04).** The target element set is:
  - **`DegenerusGameAdvanceModule.advanceGame`** (MODIFIED by 52242a10 for entropy-passthrough aspect) — §1.1 — three downstream call sites thread `rngWord` forward (IM-10, IM-11, IM-12)
  - **`DegenerusGameAdvanceModule._processFutureTicketBatch`** (MODIFIED by 52242a10) — §1.1 line 1383 — signature gained `uint256 entropy`; delegatecall payload at line 1390-1394 now forwards `entropy`
  - **`DegenerusGameAdvanceModule._prepareFutureTickets`** (MODIFIED by 52242a10) — §1.1 line 1408 — signature gained `uint256 entropy`; both call sites (resume + target loop) forward entropy
  - **`DegenerusGameAdvanceModule._consolidatePoolsAndRewardJackpots`** (MODIFIED partially by 52242a10 — line 407 call site) — §1.1 line 721
  - **`DegenerusGameMintModule.processFutureTicketBatch`** (MODIFIED by 52242a10) — §1.4 line 385 — signature gained `uint256 entropy`; `uint256 entropy = rngWordCurrent;` SLOAD REMOVED from body top
  - **`IDegenerusGameMintModule.processFutureTicketBatch`** (MODIFIED by 52242a10) — §1.11 — interface decl gained `uint256 entropy` parameter
  - **IM-10 / IM-11 / IM-12 / IM-13 chain rows** — §2.3 — three caller-side rows + the delegatecall selector boundary
  - **§2.5 IM-22** — the entropy-commitment-boundary replay row for Phase 235 RNG-01/02 cross-reference
  - **§3.3.f ID-103** — interface-drift PASS (cited once per D-10; no re-verification)
  - **D-12 230-02-DELTA-ADDENDUM.md surface**: 17 keccak formulations DOWNSTREAM of the passthrough boundary — EntropyLib.hash2 at lines 26-37; MintModule._rollRemainder at line 647; MintModule._raritySymbolBatch at line 540-571 (post-`314443af` keccak formulation — add rows for THIS commit too since it also sits downstream of the new entropy parameter); 16 JackpotModule sites at 277, 443, 508, 522, 544, 594, 596, 607-609, 874, 937, 1134, 1238, 1345, 1681-1683, 1741, 1798-1800, 1808; PayoutUtils._calcAutoRebuy at line 68-70

**Step 2 — For each target element, run the attack vectors from CONTEXT.md D-05 / D-06 / D-07 / D-12:**

**(A) D-05 entropy-equivalence proof (three call sites + delegatecall boundary):**
  - For IM-10 (advanceGame FF-promotion at line 315-318): The `rngWord` argument passed to `_processFutureTicketBatch(ffLevel, rngWord)` is the local variable returned by `rngGate(ts, day, purchaseLevel, lastPurchase, bonusFlip)` at line 283-289. Inside `rngGate` (line 1133), the SLOAD `uint256 currentWord = rngWordCurrent;` at line 1143 reads the VRF-fulfilled word. This is the SAME value the pre-fix code would have SLOAD'd inside `processFutureTicketBatch` body. Byte-identical. Record SAFE with File:Line for caller + rngGate origin + pre-fix `52242a10^:contracts/modules/DegenerusGameMintModule.sol` SLOAD line excerpt.
  - For IM-11 (advanceGame near-future prep at line 337): Same `rngWord` local flows into `_prepareFutureTickets(lvl, rngWord)` which internally forwards to `_processFutureTicketBatch` at lines 1418 + 1428. Byte-identical. Record SAFE.
  - For IM-12 (_consolidatePoolsAndRewardJackpots post-transition FF at line 407): Same `rngWord` local (passed via parameter at `_consolidatePoolsAndRewardJackpots(lvl, purchaseLevel, day, rngWord, psd)` at line 417-423) flows into `_processFutureTicketBatch(nextLevel, rngWord)` at line 407 (NOTE: line 407 is inside advanceGame's own jackpot-phase entry path per the read_first grep results; verify exact location). Byte-identical. Record SAFE.
  - For IM-13 (delegatecall boundary): `_processFutureTicketBatch` at line 1383 wraps the delegatecall payload `abi.encodeWithSelector(IDegenerusGameMintModule.processFutureTicketBatch.selector, lvl, entropy)` at line 1390-1394. The selector is the NEW 2-arg form (matching §3.3.f ID-103 PASS). The MintModule receiver at line 385 extracts `entropy` from calldata — the byte-exact value passed by AdvanceModule. No transformation at the delegatecall boundary. Record SAFE with IM-13 + ID-103 evidence.
  - For `rngWordCurrent` SLOAD removal at MintModule: Per `git show 52242a10 -- contracts/modules/DegenerusGameMintModule.sol` the body-top `uint256 entropy = rngWordCurrent;` line is DELETED. Post-fix, entropy arrives as a parameter. Cryptographic equivalence holds because both (a) the pre-fix SLOAD and (b) the post-fix parameter source ultimately trace to the SAME `rngWordCurrent` storage slot at the moment VRF fulfillment completed (line 1702 `rngWordCurrent = word;`). Record SAFE. BUT also flag a SEMANTIC IMPROVEMENT: the old code depended on `rngWordCurrent` remaining non-zero across the entire `processFutureTicketBatch` call; the new code snapshots the value once at rngGate and passes it forward, immune to any mid-call `rngWordCurrent = 0;` SSTORE (e.g. the ones at lines 1108, 1285, 1577, 1638, 1677). Classify as SAFE-INFO Finding Candidate: N — a positive refactor side-effect worth documenting but not a candidate finding.
  - Semantic equivalence caveat: verify that the pre-fix code NEVER executed `processFutureTicketBatch` AFTER a `rngWordCurrent = 0` SSTORE within the same tx. If the pre-fix could have, the old code would have SLOAD'd 0 and the new code snapshots the real word, meaning the two ARE NOT byte-equivalent — they differ in the pre-fix's bug case. This would be a SEMANTIC DIVERGENCE classified as SAFE (new code is strictly correct) but worth surfacing as SAFE-INFO Finding Candidate: Y for Phase 236 to potentially document as "fix confirms prior implicit bug". Verify by reviewing the execution ordering in advanceGame: does any code path run `processFutureTicketBatch` after rngGate but after a rngWordCurrent clear? If yes, record SAFE-INFO Finding Candidate: Y; if no, record SAFE with no candidate flag.

**(B) D-06 backward-trace (per feedback_rng_backward_trace.md):**
  - Trace BACKWARD from each of the four entropy-consumer sites inside `MintModule.processFutureTicketBatch` body (lines 443, 469, 476, 489) — specifically the `entropy` parameter — to the VRF word's FIRST APPEARANCE in storage.
  - Step-by-step: (1) `entropy` parameter received via delegatecall from AdvanceModule; (2) Passed by AdvanceModule's `_processFutureTicketBatch(lvl, entropy)` caller chain from IM-10/11/12; (3) IM-10/11/12 source the local `rngWord` from `rngGate(...)` return at line 283; (4) `rngGate` at line 1143 SLOADs `rngWordCurrent`; (5) `rngWordCurrent` is written by `rawFulfillRandomWords` at line 1702 (`rngWordCurrent = word;`) — the VRF coordinator's callback with the randomly-sampled word; also at line 1785 (`rngWordCurrent = finalWord;` in the game-over path).
  - At step (5), the VRF word is unknown at the VRF-request time (coordinator commits to a block-hash-derived value that is not determinable by the tx submitter). Therefore the VRF word was unknown at input commitment time for every new consumer. Record as SAFE with File:Line chain.
  - Cross-reference Phase 232.1 Plan 03's PFTB-AUDIT conclusion: "per-caller verdict table proves non-zero entropy at all 4 reachable call sites of `_processFutureTicketBatch` via rawFulfillRandomWords L1698 `if (word == 0) word = 1;` zero-guard + rngGate L291 sentinel-1 break + Plan 01 pre-drain gate; zero code change to `processFutureTicketBatch` body per SPEC §Out of Scope." — the non-zero guarantee additionally strengthens the backward-trace: entropy CANNOT be zero at any IM-13 consumer invocation. Cite Phase 232.1 Plan 03 PFTB-AUDIT.md as prior-art evidence.

**(C) D-06 commitment-window enumeration (per feedback_rng_commitment_window.md + user directive):**
  - Enumerate EVERY player-controllable state variable that can change between VRF REQUEST (the `VRFCoordinatorV2_5.requestRandomWords(...)` call — grep AdvanceModule for the coordinator interface invocation; typically inside the VRF-request block of advanceGame or requestLootboxRng) and VRF FULFILLMENT (`rawFulfillRandomWords` at line 1690).
  - Apply the rngLocked invariant: during the VRF window `rngLocked == true` blocks every permissionless mutation path that could influence entropy-consumer behavior. Grep for every `rngLocked` read and write across AdvanceModule + MintModule + LootboxModule + DecimatorModule + WhaleModule + JackpotModule + GameOverModule to enumerate the full guarded-surface.
  - Produce an explicit table with columns: `Variable | File:Line (decl) | Writer Function(s) | rngLocked-Guarded? | Can Change During VRF Window? | Influences IM-13 Entropy-Consumer Behavior? | Verdict`.
  - Expected candidates to examine (non-exhaustive): `rngWordCurrent`, `rngLocked`, `vrfRequestId`, `level`, `jackpotPhase`, `currentPrizePool`, `nextPrizePool`, `futurePrizePool`, `claimablePool`, `ticketQueue`, `ticketsOwedPacked`, `mintPacked_`, `boonPacked`, `traitBurnTicket`, `lootboxRngWordByIndex`, `LR_INDEX` (lootbox RNG index), `lootboxPresale*`, `deityBoon*`, `burnieCoinflipPool`, `decBurns`, `decPool`, `playerQuestProgress`, `earlybirdDgnrsPoolStart`, `rngWordByDay[day]`. For each, classify as (a) rngLocked-guarded (no change during window -> non-influential), (b) not guarded but does not appear in IM-13 consumer path (non-influential by scope), (c) not guarded AND appears in IM-13 consumer path (REQUIRES individual verification — potential finding candidate).
  - The explicit enumeration must include at least 12 distinct variables. The commitment-window proof is that no player-controllable state variable that could change during the VRF window INFLUENCES the output of the IM-13 consumer (specifically the behavior of `_raritySymbolBatch` per-bit diffusion + `_rollRemainder` probability + `emit TraitsGenerated` event data).
  - Per user directive, the table MUST be concrete, NOT a generic 'commitment window preserved' assertion. Each variable row must state whether it can change and whether it influences the consumer.

**(D) D-07 no-re-use-bias check:**
  - Within a single `advanceGame` transaction, the same `rngWord` flows to IM-10 (FF-promotion) + IM-11 (near-future prep) + IM-12 (post-transition FF). Does re-use of the same seed across multiple consumers introduce bias?
  - Trace each consumer's use of `entropy` inside `MintModule.processFutureTicketBatch`:
    - `_rollRemainder(entropy, baseKey, rem)` at line 443 + 489 — post-`c2e5e0a9` uses `hash2(entropy, rollSalt)` where `rollSalt` depends on `(baseKey, rem)`. Different (player, lvl, idx) -> different baseKey -> different rollSalt -> different hash output. No bias from shared `entropy`.
    - `_raritySymbolBatch(player, baseKey, processed, take, entropy)` at line 469 — post-`314443af` uses `keccak256(abi.encode(baseKey, entropyWord, groupIdx))`. Different (player, lvl, idx, groupIdx) -> different preimage -> different hash. No bias from shared `entropy`.
    - `emit TraitsGenerated(... entropy)` at line 476 — output only; no computation consuming the bias.
  - Per-call salt-space analysis: each consumer has a domain-separating input (baseKey incorporates player+lvl+idx; groupIdx incorporates per-group index) that makes the consumer's output cryptographically independent from other consumers with the same `entropy`. Record SAFE.
  - Cross-call (IM-10 vs IM-11 vs IM-12): each call resolves a different `lvl` (FF level vs purchaseLevel+N vs nextLevel), which feeds into `baseKey = (uint256(lvl) << 224) | ...` at MintModule line 423-425. Different lvl -> different baseKey -> different hash output per consumer. No cross-call bias. Record SAFE.

**(E) D-12 230-02-DELTA-ADDENDUM.md integration:**
  - For each of the 17 `c2e5e0a9` keccak formulations DOWNSTREAM of the entropy-passthrough boundary, add a verdict row:
    - EntropyLib.sol:26-37 `hash2(uint256, uint256) -> uint256` (NEW helper) — verify memory-safe asm uses only reserved scratch 0x00-0x3F; output `uint256(keccak256(abi.encode(a, b)))` for same inputs. Cite 230-02-DELTA-ADDENDUM.md per-file verdict row. Record SAFE.
    - MintModule._rollRemainder at line 647 — verify `hash2(entropy, rollSalt)` diffuses all 256 bits; low 7 bits (for `% TICKET_SCALE`) now carry full player entropy via rollSalt's player-dependent composition. Record SAFE.
    - MintModule._raritySymbolBatch at line 540-571 — verify `keccak256(abi.encode(baseKey, entropyWord, groupIdx))` fixes the 314443af low-bit diffusion bug. Record SAFE. NOTE: 314443af is a SEPARATE commit from c2e5e0a9; both sit downstream of 52242a10's new `entropy` parameter. Per addendum page 1, 314443af is a fix-not-a-finding. Include as a verdict row with Finding Candidate: N.
    - JackpotModule 16 sites — per addendum page 2 per-file verdict table: every site is SAFE post-hardening. Re-verify from HEAD source that each formulation matches the addendum's cited composition (2-input `hash2(randWord, lvl)` vs 3-input `keccak256(abi.encode(randWord, lvl, TAG))` vs per-iteration `hash2(entropy, iterator)`). Addendum lists 5 groupings: line 277 single site; lines 443/544/594/874/1134 group (5 sites `hash2(randWord, lvl)`); line 596 single site (`hash2(randWord, sourceLevel)` for carryover decorrelation); lines 508/522/1681 group (3 sites `keccak256(abi.encode(randWord, lvl, COIN_JACKPOT_TAG))`); lines 607-609 group (3 sites matching 508/522 formulation); lines 937/1238/1345/1741 group (4 sites per-bucket `keccak256(abi.encode(entropy, traitIdx, X))`); lines 1798-1800 group (3 sites FAR_FUTURE_COIN_TAG); line 1808 single site (per-sample loop `hash2(entropy, s)`). Total: 16 sites. Add per-group verdict rows in the AUDIT (1 row per group = 7 rows covering all 16 sites).
    - PayoutUtils._calcAutoRebuy at line 68-70 — verify `keccak256(abi.encode(entropy, beneficiary, weiAmount))`. Record SAFE.
  - Cryptographic collision analysis (cite addendum page 2): `hash2(a, b)` hashes exactly 64 bytes; `keccak256(abi.encode(a, b, c))` hashes 96 bytes; different lengths -> different preimages -> keccak collision-resistance guarantees no accidental cross-formulation collisions. Domain-separation via compile-time TAG constants (`COIN_JACKPOT_TAG`, `FAR_FUTURE_COIN_TAG`, `BONUS_TRAITS_TAG`) further separates 3-input sites. Record a dedicated Collision-Analysis sub-section in the AUDIT re-verifying the addendum's claim from HEAD source (grep the TAG constants, confirm uniqueness).
  - Per D-12 requirement: verify post-c2e5e0a9 formulation consumers do NOT re-introduce domain-collision between 2-input `hash2` and 3-input `abi.encode` forms. The verification is that 2-input and 3-input preimages have DIFFERENT byte-lengths (64 vs 96), so keccak collision resistance guarantees disjoint outputs even with identical low-order inputs.

**(F) IDegenerusGameMintModule interface PASS citation (D-10):**
  - Cite §3.3.f ID-103 PASS at Phase 230 §3 as evidence the interface-drift subquestion is closed. Record a single verdict row noting "ID-103 PASS at HEAD — interface decl + implementer signature both `(uint24 lvl, uint256 entropy)` per 52242a10 lockstep". Do NOT re-audit at file:line level.

**(G) `make check-delegatecall` corroboration:**
  - The delegatecall site at AdvanceModule line 1387-1395 (`_processFutureTicketBatch` -> MintModule.processFutureTicketBatch via selector) is one of the 44 verified aligned sites per Phase 230 §3.5 `make check-delegatecall` 44/44 PASS. Cite the gate output line as corroboration that the selector hash change (due to signature bump) was correctly picked up by the automated gate.

**Step 3 — Write `233-02-AUDIT.md` mirroring the 232-NN-AUDIT.md precedent shape.** Required structure, in this exact order:

```markdown
# JKP-02 Adversarial Audit: Explicit Entropy Passthrough to processFutureTicketBatch

**Scope:** Commit `52242a10` ("refactor: explicit entropy passthrough to processFutureTicketBatch") — AdvanceModule three call sites + helpers (_processFutureTicketBatch, _prepareFutureTickets) + MintModule receiver (processFutureTicketBatch SLOAD removal) + IDegenerusGameMintModule interface (all per `230-01-DELTA-MAP.md` §1.1 / §1.4 / §1.11 / §2.3 IM-10..IM-13 / §2.5 IM-22 / §3.3.f ID-103)
**Method:** Fresh read from HEAD source — no reuse of v25.0 Phase 215 or Phase 232.1 verdicts as pre-approved; D-05 forward-trace + D-06 backward-trace + D-06 commitment-window enumeration + D-07 no-re-use-bias + D-12 230-02-DELTA-ADDENDUM.md integration all applied
**Scope source:** `230-01-DELTA-MAP.md` §4 Consumer Index row JKP-02 (per D-04) + `230-02-DELTA-ADDENDUM.md` (per D-12)
**Verdict vocabulary:** `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (per D-02)
**Finding-ID policy:** No `F-29-NN` IDs emitted (per D-11) — `Finding Candidate: Y/N` column only; Phase 236 FIND-01 owns canonical ID assignment
**Hand-off posture:** Phase 235 RNG-01 / RNG-02 owns the milestone-wide proof; this plan provides the passthrough-specific slice via §2.5 IM-22 cross-reference.

## Methodology

[One paragraph: D-05 entropy-equivalence proof (forward-trace from rngWord local to delegatecall payload cross-referenced against pre-fix rngWordCurrent SLOAD at 52242a10^:MintModule). D-06 backward-trace (VRF word unknown at input commitment time — cite rngGate L283 -> rngWordCurrent SLOAD L1143 -> rawFulfillRandomWords SSTORE L1702 chain). D-06 commitment-window enumeration (EXPLICIT player-controllable-state table per user directive — not a generic assertion). D-07 no-re-use-bias (salt-space + domain-separation analysis across IM-10/11/12). D-12 230-02-DELTA-ADDENDUM.md integration (17 keccak formulations downstream of the passthrough boundary + cryptographic collision analysis between 2-input hash2 and 3-input abi.encode forms). Locked verdict vocabulary. Locked columns per D-02 plus Finding Candidate column per 232-precedent.]

## Findings-Candidate Block

[Prose free-form (no F-29-NN IDs). One block per VULNERABLE or DEFERRED verdict identified in the table below, plus any SAFE-INFO row marked Finding Candidate: Y. Each block: title, verdict, File:Line, owning SHA (52242a10 for passthrough proper; c2e5e0a9 or 314443af for addendum rows), attack vector, evidence excerpt, recommended Phase 236 severity-classification note. Expected: zero to one SAFE-INFO Finding Candidate: Y for the pre-fix implicit-bug observation (if verification shows the pre-fix COULD have SLOAD'd 0 post-rngWordCurrent-clear); zero VULNERABLE; zero DEFERRED. If zero candidate findings, state "No on-chain candidate findings — all verdicts SAFE against 52242a10 passthrough + downstream c2e5e0a9 / 314443af keccak-hardening surface."]

## Per-Function Verdict Table

| Function | Commit SHA | File:Line | Verdict | Evidence | Notes | Finding Candidate |
|---|---|---|---|---|---|---|
| `DegenerusGameAdvanceModule.advanceGame` IM-10 FF-promotion call site | 52242a10 | contracts/modules/DegenerusGameAdvanceModule.sol:315-318 | SAFE | `rngWord` local sourced from `rngGate(...)` return at line 283-289 threads into `_processFutureTicketBatch(ffLevel, rngWord)` | D-05: byte-identical to pre-fix `rngWordCurrent` SLOAD inside MintModule.processFutureTicketBatch body (removed line at `52242a10^`) | N |
| `DegenerusGameAdvanceModule.advanceGame` IM-11 near-future prep call site | 52242a10 | contracts/modules/DegenerusGameAdvanceModule.sol:337 (+ _prepareFutureTickets internals at 1418/1428) | SAFE | Same rngWord local -> `_prepareFutureTickets(lvl, rngWord)` -> forwards to `_processFutureTicketBatch` at 1418 + 1428 | D-05: byte-identical forwarding chain | N |
| `DegenerusGameAdvanceModule._consolidatePoolsAndRewardJackpots` IM-12 post-transition FF | 52242a10 | contracts/modules/DegenerusGameAdvanceModule.sol:407 | SAFE | `rngWord` param flows into `_processFutureTicketBatch(nextLevel, rngWord)` | D-05: byte-identical to pre-fix SLOAD | N |
| `DegenerusGameAdvanceModule._processFutureTicketBatch` helper (delegatecall wrapper) | 52242a10 | contracts/modules/DegenerusGameAdvanceModule.sol:1383-1399 | SAFE | `abi.encodeWithSelector(IDegenerusGameMintModule.processFutureTicketBatch.selector, lvl, entropy)` at line 1390-1394 — new 2-arg selector | IM-13 boundary; ID-103 PASS at §3.3.f | N |
| `DegenerusGameAdvanceModule._prepareFutureTickets` helper (entropy forwarder) | 52242a10 | contracts/modules/DegenerusGameAdvanceModule.sol:1408-1438 | SAFE | Entropy passed through both resume-path call (line 1418) and target-loop call (line 1428) unchanged | Pure forwarding; no transformation | N |
| `DegenerusGameMintModule.processFutureTicketBatch` receiver (SLOAD removal) | 52242a10 | contracts/modules/DegenerusGameMintModule.sol:385-498 | SAFE | Pre-fix top-of-body `uint256 entropy = rngWordCurrent;` SLOAD REMOVED (verified via `git show 52242a10^:contracts/modules/DegenerusGameMintModule.sol`); entropy now arrives as parameter | Semantic improvement: immune to mid-call `rngWordCurrent = 0;` SSTOREs at lines 1108/1285/1577/1638/1677; downstream consumers (line 443 _rollRemainder, line 469 _raritySymbolBatch, line 476 emit, line 489 _rollRemainder) use the passed entropy | N |
| `IDegenerusGameMintModule.processFutureTicketBatch` interface decl | 52242a10 | contracts/interfaces/IDegenerusGameModules.sol:<line for decl> | SAFE | ID-103 PASS at Phase 230 §3.3.f; interface + implementer moved in lockstep | D-10: pure interface-drift subquestion closed; semantic passthrough is the JKP-02 focus | N |
| IM-13 delegatecall boundary (AdvanceModule._processFutureTicketBatch -> MintModule.processFutureTicketBatch) | 52242a10 | contracts/modules/DegenerusGameAdvanceModule.sol:1390-1394 -> contracts/modules/DegenerusGameMintModule.sol:385 | SAFE | Selector alignment corroborated by `make check-delegatecall` 44/44 PASS at HEAD per Phase 230 §3.5; byte-exact calldata `(uint24, uint256)` | D-05 + D-10 + gate output | N |
| IM-22 entropy-commitment boundary replay (Phase 235 cross-reference) | 52242a10 | (same as IM-13) | SAFE | §2.5 IM-22 is the Phase 235 RNG-01/02 citation handle | Passthrough-specific slice; milestone-wide proof is Phase 235 | N |
| No-re-use-bias check across IM-10/11/12 in same advanceGame tx | 52242a10 | (cross-site) | SAFE | Each consumer resolves different `lvl` -> different baseKey -> different keccak preimage; no bias from shared seed | D-07 | N |
| **D-12 Addendum integration: EntropyLib.hash2 helper** | c2e5e0a9 | contracts/libraries/EntropyLib.sol:26-37 | SAFE | `uint256(keccak256(abi.encode(a, b)))` via memory-safe asm scratch slots 0x00/0x20 | 230-02-DELTA-ADDENDUM.md per-file verdict table | N |
| **D-12 Addendum integration: MintModule._rollRemainder** | c2e5e0a9 | contracts/modules/DegenerusGameMintModule.sol:647 | SAFE | `hash2(entropy, rollSalt)` diffuses all 256 bits; low 7 bits for `% TICKET_SCALE` now carry full player entropy | 230-02-DELTA-ADDENDUM.md per-file verdict table | N |
| **D-12 Addendum integration: MintModule._raritySymbolBatch** | 314443af | contracts/modules/DegenerusGameMintModule.sol:540-571 | SAFE | `uint256 seed = uint256(keccak256(abi.encode(baseKey, entropyWord, groupIdx)));` at line 568 — fixes low-bit diffusion bug | 230-02-DELTA-ADDENDUM.md page 1 verdict; downstream of 52242a10 entropy parameter | N |
| **D-12 Addendum integration: JackpotModule 16 keccak sites (per-group verdicts)** | c2e5e0a9 | contracts/modules/DegenerusGameJackpotModule.sol:<7 per-group line ranges covering 16 sites: 277; 443/544/594/874/1134; 596; 508/522/1681; 607-609; 937/1238/1345/1741; 1798-1800; 1808> | SAFE | Per-group hash2 / keccak256(abi.encode) formulations with proper domain separation via COIN_JACKPOT_TAG / FAR_FUTURE_COIN_TAG constants | 230-02-DELTA-ADDENDUM.md page 2 per-file verdict table (7 rows, one per group; flatten or consolidate at plan discretion — minimum 7 rows) | N |
| **D-12 Addendum integration: PayoutUtils._calcAutoRebuy** | c2e5e0a9 | contracts/modules/DegenerusGamePayoutUtils.sol:68-70 | SAFE | `keccak256(abi.encode(entropy, beneficiary, weiAmount))` — full keccak diffusion; was marginally safe pre-hardening | 230-02-DELTA-ADDENDUM.md per-file verdict table | N |
| **D-12 Cross-formulation collision analysis: hash2 vs keccak256(abi.encode)** | c2e5e0a9 | (cross-file) | SAFE | hash2(a,b) hashes 64 bytes; keccak256(abi.encode(a,b,c)) hashes 96 bytes; different lengths -> disjoint preimages -> keccak collision-resistance guarantees disjoint outputs | 230-02-DELTA-ADDENDUM.md page 2 collision analysis | N |

[Rows above are the MINIMUM. Replace `<line>` placeholders with real File:Line values extracted during Step 1. Additional rows allowed per (element × distinct attack vector). For the 16-site JackpotModule addendum group, minimum 7 rows (one per formulation group per addendum page 2 table); maximum 16 rows (one per site).]

## D-06 Backward-Trace Sub-Section

[Prose + diagram-style table. Trace backward from each of the four entropy-consumer sites inside MintModule.processFutureTicketBatch body (lines 443, 469, 476, 489) to VRF fulfillment at AdvanceModule:1702 `rngWordCurrent = word;`. Show that the VRF word was unknown at input commitment time for every new consumer. Cite Phase 232.1 Plan 03 PFTB-AUDIT.md as corroborating evidence that entropy cannot be zero at any IM-13 invocation via the combined `rawFulfillRandomWords` L1698 zero-guard + `rngGate` L291 sentinel-1 break + Plan 01 pre-drain gate.]

## D-06 Commitment-Window Enumeration Table (Per User Directive)

| Variable | File:Line (decl) | Writer Function(s) | rngLocked-Guarded? | Can Change During VRF Window? | Influences IM-13 Entropy-Consumer Behavior? | Verdict |
|---|---|---|---|---|---|---|
| `rngWordCurrent` | contracts/storage/DegenerusGameStorage.sol:<line> | rawFulfillRandomWords (writer); rngGate (reader+clearer) | N/A (this IS the VRF word) | Transitions from 0 -> VRF word at fulfillment | It IS the entropy source | SAFE — committed value is the VRF word itself |
| `rngLocked` | contracts/storage/DegenerusGameStorage.sol:<line> | advanceGame / requestLootboxRng (set); rawFulfillRandomWords / _unlockRng (clear) | N/A (this IS the guard) | Transitions from false -> true at VRF request -> false at fulfillment | Gates other mutations | SAFE — by construction |
| `vrfRequestId` | contracts/storage/DegenerusGameStorage.sol:<line> | advanceGame (set at request); rawFulfillRandomWords (check) | N/A | Set once at VRF request; read at fulfillment | Identity check only | SAFE |
| `level` | contracts/storage/DegenerusGameStorage.sol:<line> | _finalizeRngRequest (increment at VRF request time — see Phase 232 DCM-01 SUMMARY); advanceGame post-transition | YES (all writers gated by rngLocked or inside advanceGame's atomic body) | No — level increments happen at VRF request, not during the window | `lvl` parameter to IM-13 consumer is caller-supplied (separate from level storage); level storage not read by _rollRemainder / _raritySymbolBatch | SAFE |
| `ticketQueue` | contracts/storage/DegenerusGameStorage.sol:<line> | purchase paths (MintModule) + _processFutureTicketBatch (drain) | YES (purchase paths check rngLocked before queueing; see Phase 232.1 Plan 02 binding-consistency invariant) | No — blocked by rngLocked | N/A — queue contents are the INPUT to processFutureTicketBatch, not the entropy source | SAFE |
| `ticketsOwedPacked` | contracts/storage/DegenerusGameStorage.sol:<line> | processFutureTicketBatch internal; purchase paths | YES (writers guarded) | No | Not read by entropy consumers — only by baseKey derivation at line 423-425 (player, not entropy) | SAFE |
| `currentPrizePool` / `nextPrizePool` / `futurePrizePool` / `claimablePool` | contracts/storage/DegenerusGameStorage.sol:<line> | advanceGame + jackpot paths | YES | No | Not read by _rollRemainder / _raritySymbolBatch | SAFE |
| `mintPacked_` | contracts/storage/DegenerusGameStorage.sol:<line> | recordMintData (purchase paths) | YES (guarded by rngLocked check upstream) | No | Not read by entropy consumers | SAFE |
| `lootboxRngWordByIndex` | contracts/storage/DegenerusGameStorage.sol:<line> | rawFulfillRandomWords (lootbox path) + internal | N/A (separate VRF channel) | Lootbox VRF windows are separate from daily RNG windows; each has its own rngLocked guard | Not read by daily processFutureTicketBatch consumers (they use the daily `entropy` parameter) | SAFE |
| `LR_INDEX` (lootbox RNG index) | contracts/storage/DegenerusGameStorage.sol:<line> | _swapAndFreeze / advanceGame (lootbox bucket swap) | YES per Phase 232.1 binding-consistency invariant | No | Not read by daily processFutureTicketBatch entropy consumers | SAFE |
| `deityBoonData` / `boonPacked` | contracts/storage/DegenerusGameStorage.sol:<line> | boon-granting paths; advanceGame | YES | No | Not read by entropy consumers | SAFE |
| `decBurns` / `decPool` / `decClaim` | contracts/storage/DegenerusGameStorage.sol:<line> | BurnieCoin.decimatorBurn (gated by rngLocked? verify); DecimatorModule | Verify: decimator burns during VRF window — if allowed, check whether decBurns entries influence IM-13 entropy consumers (expected: no, decimator state is a separate subsystem) | Verify | Expected: no influence on IM-13 | SAFE (expected) |
| `rngWordByDay[day]` | contracts/storage/DegenerusGameStorage.sol:<line> | advanceGame (writer on fulfillment of daily VRF via rngGate) | N/A | Written once per VRF fulfillment | IS the entropy source (when JKP-02's entropy parameter is threaded through) | SAFE — committed value is the daily VRF word |

[Replace `<line>` placeholders with real File:Line values. If any variable is found to be writable during the VRF window AND influences IM-13 consumer behavior, record VULNERABLE with Finding Candidate: Y for Phase 236 FIND-01. The table MUST enumerate at least 12 distinct variables (per must_haves). Add additional rows as audit progresses.]

## D-07 No-Re-Use-Bias Sub-Section

[Prose + per-consumer salt-space analysis. Show that within a single advanceGame tx, the same `rngWord` passed to IM-10 / IM-11 / IM-12 produces cryptographically independent outputs at each consumer due to (a) different `lvl` parameter -> different baseKey -> different keccak preimage, (b) domain-separation via baseKey's player+lvl+idx composition, (c) per-group iterator for _raritySymbolBatch. Cite the baseKey derivation at MintModule:423-425 verbatim.]

## D-12 230-02-DELTA-ADDENDUM.md Integration Sub-Section

[Prose + cross-reference to addendum's per-file verdict table. Acknowledge that JKP-02 is the MOST-AFFECTED requirement by the addendum (per addendum's downstream-phase impact table). Re-verify from HEAD:
- EntropyLib.sol:26-37 hash2 helper — cite memory-safe asm, keccak equivalence
- MintModule._rollRemainder at 647, _raritySymbolBatch at 540-571 — cite keccak formulation, low-bit diffusion fix
- JackpotModule 16 sites grouped into 7 formulation groups per addendum page 2 — cite each group's formulation + domain separation
- PayoutUtils._calcAutoRebuy at 68-70 — cite keccak formulation
Cross-formulation collision analysis: hash2 (64 bytes) vs keccak256(abi.encode) (96 bytes) — different lengths guarantee disjoint preimages -> disjoint outputs. Per addendum page 2 this analysis is already performed; JKP-02 cites + re-verifies from HEAD.]

## High-Risk Patterns Analyzed

### Entropy-Equivalence Proof (D-05 Forward-Trace)
[Prose: rngWord local = rngGate return = rngWordCurrent SLOAD at rngGate:L1143 = value written by rawFulfillRandomWords:L1702. Pre-fix SLOAD at MintModule.processFutureTicketBatch body-top (removed in 52242a10) would have read the SAME rngWordCurrent value. Byte-identical. Show `git show 52242a10^:contracts/modules/DegenerusGameMintModule.sol` excerpt of the removed SLOAD line verbatim.]

### Backward-Trace Integrity (D-06)
[Prose: trace backward from each of four consumers inside MintModule body (lines 443, 469, 476, 489) to VRF coordinator's callback. Show VRF word was unknown at input commitment time. Cite Phase 232.1 Plan 03 PFTB-AUDIT.md + Phase 232.1 Plan 02 binding-consistency test suite as prior-art evidence.]

### Commitment-Window Integrity (D-06)
[Prose summary of the enumeration table above. Show that no player-controllable state variable influences IM-13 consumer behavior during the VRF window. Key invariant: rngLocked guard blocks all permissionless mutations of variables that feed into entropy-consumer inputs.]

### No-Re-Use Bias Across Shared-Seed Consumers (D-07)
[Prose: cross-reference salt-space analysis from the D-07 sub-section above.]

### Post-Addendum Keccak-Formulation Safety (D-12)
[Prose: reconcile JKP-02 passthrough with 230-02-DELTA-ADDENDUM.md 17-site hardening. Show that the passthrough boundary does not re-introduce the pre-hardening low-bit diffusion bug because all 17 downstream consumers now use keccak-formulation instead of XOR+xorshift. Cite addendum page 2 per-site verdict table.]

## Scope-guard Deferrals

[Any JKP-02-adjacent concern surfaced during this audit that is NOT anchored in `230-01-DELTA-MAP.md` §4 JKP-02 rows OR in `230-02-DELTA-ADDENDUM.md` is recorded here per D-03. Phase 230 catalog is READ-only. Each entry: concern, why not in JKP-02 scope, recommended receiving phase. If none, state "None surfaced during this audit."]

## Downstream Hand-offs

[Concerns overlapping downstream-phase territory (per D-11 and general scope boundaries). Expected entries:
- **Phase 235 RNG-01 hand-off (backward trace milestone-wide):** JKP-02 provides the passthrough-specific backward-trace slice for IM-13; Phase 235 RNG-01 owns the milestone-wide enumeration across earlybird bonus-trait roll + BAF sentinel emission + entropy passthrough + phase-transition _unlockRng removal. Phase 235 RNG-01 cites §2.5 IM-22 in 230-01-DELTA-MAP.md to enter this plan's evidence.
- **Phase 235 RNG-02 hand-off (commitment window milestone-wide):** JKP-02 provides the passthrough-specific commitment-window enumeration for IM-13; Phase 235 RNG-02 owns the milestone-wide enumeration across all new RNG consumers. The explicit 12+ variable table in this plan is the passthrough-specific slice.
- **Phase 233 JKP-01 hand-off (BAF sentinel):** The rngWord lineage into runBafJackpot is pre-existing (not newly threaded by 52242a10) — JKP-01 audits the sentinel injection, not the entropy source. No cross-plan dependency.
- **Phase 233 JKP-03 hand-off (cross-path bonus-trait consistency):** The three paths that consume bonusTraitsPacked also consume rngWord; JKP-02 proves rngWord arrives unchanged at each consumer; JKP-03 proves the three paths compute identical 4-trait sets from that rngWord. Independent aspects.
- **Phase 234 QST-01 hand-off (mint_ETH quest):** MintModule.processFutureTicketBatch is upstream of quest-progression side effects via purchase paths. JKP-02 proves the entropy value is equivalent; Phase 234 QST-01 proves the quest math is correct. No cross-plan dependency on entropy semantics.
- **Phase 235 CONS-01 hand-off:** No SSTORE delta introduced by 52242a10 proper (rngWordCurrent SLOAD removal is a READ-side change, not a write-side change). Phase 235 CONS-01 covers pool-conservation independently.
- **Phase 236 FIND-01 hand-off:** Every VULNERABLE / DEFERRED row + every SAFE-INFO Finding Candidate: Y row becomes a finding-candidate anchor. Expected: zero to one SAFE-INFO Finding Candidate: Y (pre-fix implicit-bug characterization if verification confirms pre-fix could have SLOAD'd 0).
- **Phase 236 REG-01 hand-off:** v25.0 Phase 215 RNG audit conclusions are regression-checked in Phase 236 REG-01; JKP-02's passthrough proof is a new piece of evidence supplementing that regression sweep.
Any additional hand-offs surfaced during audit recorded here.]
```

Every verdict row MUST:
- Use exactly the locked column set per D-02 + Finding Candidate column per 232-NN-AUDIT.md precedent: `Function | Commit SHA | File:Line | Verdict | Evidence | Notes | Finding Candidate`
- Cite the 7-char SHA (`52242a10` for passthrough proper; `c2e5e0a9` or `314443af` for addendum rows)
- Use a real File:Line anchor from HEAD source (never a placeholder like `<line>`)
- Use verdict vocabulary strictly from `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (per D-02)
- Never reference F-29-NN IDs (per D-11)
- Carry a `Y` or `N` in Finding Candidate column

Read contract sources ONLY from `contracts/` directory at HEAD (per user memory `feedback_contract_locations.md` — never read stale copies). Use `git show 52242a10^:contracts/...` and `git show c2e5e0a9^:contracts/...` and `git show 314443af^:contracts/...` for pre-fix comparisons (git-tree reads of the same repo at prior SHAs — NOT stale copies).

READ-only constraint (v29.0 milestone rule): Zero writes to `contracts/` or `test/` permitted. Only writes allowed during task execution are `.planning/phases/233-jackpot-baf-entropy-audit/233-02-AUDIT.md` itself. `git status --porcelain contracts/ test/` MUST be empty before and after task execution.
  </action>
  <verify>
    <automated>test -f .planning/phases/233-jackpot-baf-entropy-audit/233-02-AUDIT.md && grep -c "52242a10" .planning/phases/233-jackpot-baf-entropy-audit/233-02-AUDIT.md && grep -c "c2e5e0a9" .planning/phases/233-jackpot-baf-entropy-audit/233-02-AUDIT.md && test -z "$(git status --porcelain contracts/ test/)"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/233-jackpot-baf-entropy-audit/233-02-AUDIT.md` exists
    - File contains the literal header strings: `## Per-Function Verdict Table`, `## D-06 Backward-Trace Sub-Section`, `## D-06 Commitment-Window Enumeration Table (Per User Directive)`, `## D-07 No-Re-Use-Bias Sub-Section`, `## D-12 230-02-DELTA-ADDENDUM.md Integration Sub-Section`, `## Findings-Candidate Block`, `## Scope-guard Deferrals`, `## Downstream Hand-offs`
    - Verdict table header row is exactly `| Function | Commit SHA | File:Line | Verdict | Evidence | Notes | Finding Candidate |`
    - Every row in the Per-Function Verdict Table cites either `52242a10` or `c2e5e0a9` or `314443af` (7-char SHAs)
    - Every row cites a File:Line anchor starting with `contracts/modules/DegenerusGameAdvanceModule.sol:`, `contracts/modules/DegenerusGameMintModule.sol:`, `contracts/modules/DegenerusGameJackpotModule.sol:`, `contracts/modules/DegenerusGamePayoutUtils.sol:`, `contracts/libraries/EntropyLib.sol:`, or `contracts/interfaces/IDegenerusGameModules.sol:`
    - Every row's verdict is exactly one of the strings `SAFE`, `SAFE-INFO`, `VULNERABLE`, or `DEFERRED` (per D-02)
    - Every row's Finding Candidate cell is exactly `Y` or `N`
    - At least one row per structural target: IM-10 (advanceGame:315-318), IM-11 (advanceGame:337 + _prepareFutureTickets:1418/1428), IM-12 (_consolidatePoolsAndRewardJackpots:407 context), _processFutureTicketBatch helper (line 1383-1399), _prepareFutureTickets helper (line 1408-1438), MintModule.processFutureTicketBatch receiver (line 385-498), IDegenerusGameMintModule interface decl, IM-13 delegatecall boundary, IM-22 replay row, no-re-use-bias row, EntropyLib.hash2, MintModule._rollRemainder, MintModule._raritySymbolBatch, JackpotModule 16-site addendum group (min 7 rows), PayoutUtils._calcAutoRebuy, cross-formulation collision analysis — minimum 16 distinct verdict rows
    - `grep -cE "SAFE|SAFE-INFO|VULNERABLE|DEFERRED" .planning/phases/233-jackpot-baf-entropy-audit/233-02-AUDIT.md` returns at least 16
    - `grep -c "52242a10" .planning/phases/233-jackpot-baf-entropy-audit/233-02-AUDIT.md` returns at least 10 (covering all passthrough-proper rows)
    - `grep -c "c2e5e0a9" .planning/phases/233-jackpot-baf-entropy-audit/233-02-AUDIT.md` returns at least 5 (covering addendum-integration rows)
    - `grep -c "314443af" .planning/phases/233-jackpot-baf-entropy-audit/233-02-AUDIT.md` returns at least 1 (covering _raritySymbolBatch addendum row)
    - The string `F-29-NN` does NOT appear in the file (per D-11)
    - The string `F-29-` does NOT appear in any form in the file (Phase 236 owns ID assignment per D-11)
    - D-06 Commitment-Window Enumeration Table enumerates at least 12 distinct player-controllable state variables with `Variable | File:Line (decl) | Writer Function(s) | rngLocked-Guarded? | Can Change During VRF Window? | Influences IM-13 Entropy-Consumer Behavior? | Verdict` columns (per user directive — explicit enumeration, not a generic assertion)
    - D-06 Backward-Trace Sub-Section traces from each of the four entropy-consumer sites (lines 443, 469, 476, 489 in MintModule) backward to VRF fulfillment at AdvanceModule:1702
    - D-07 No-Re-Use-Bias Sub-Section performs salt-space analysis across IM-10 / IM-11 / IM-12 within a single advanceGame tx
    - D-12 230-02-DELTA-ADDENDUM.md Integration Sub-Section re-verifies all 7 formulation groups from the addendum's page-2 verdict table from HEAD source
    - High-Risk Patterns Analyzed section contains at least five subsections: `Entropy-Equivalence Proof (D-05 Forward-Trace)`, `Backward-Trace Integrity (D-06)`, `Commitment-Window Integrity (D-06)`, `No-Re-Use Bias Across Shared-Seed Consumers (D-07)`, `Post-Addendum Keccak-Formulation Safety (D-12)`
    - Downstream Hand-offs subsection explicitly names `Phase 235 RNG-01` AND `Phase 235 RNG-02` (milestone-wide proof owners) AND `Phase 236 FIND-01` (per D-11)
    - Citation of Phase 230 §3.3.f ID-103 PASS is present for the interface-drift subquestion (per D-10)
    - Citation of `make check-delegatecall` 44/44 PASS at HEAD (Phase 230 §3.5) is present for the IM-13 delegatecall-alignment subquestion
    - No placeholder line numbers like `:<line>` or `<line range>` remain in the final file — every File:Line is a real integer or integer range
    - No contract source content read from outside `contracts/` directory (per user memory `feedback_contract_locations.md`)
    - `git status --porcelain contracts/ test/` returns empty output both before and after task execution
  </acceptance_criteria>
  <deliverable>
`233-02-AUDIT.md` with per-function verdict table covering 52242a10 passthrough surface (3 call sites + 2 helpers + MintModule receiver + interface decl + IM-13 boundary + IM-22 replay + no-re-use row) + D-12 addendum integration (EntropyLib.hash2 + MintModule._rollRemainder + MintModule._raritySymbolBatch + 7 JackpotModule formulation groups + PayoutUtils._calcAutoRebuy + cross-formulation collision analysis). Plus a D-06 Backward-Trace Sub-Section tracing from 4 consumer sites to VRF fulfillment. Plus an EXPLICIT D-06 Commitment-Window Enumeration Table with minimum 12 player-controllable state variables (per user directive). Plus a D-07 No-Re-Use-Bias Sub-Section with salt-space analysis. Plus a D-12 230-02-DELTA-ADDENDUM.md Integration Sub-Section. Plus Findings-Candidate / Scope-guard Deferrals / Downstream Hand-offs subsections with Phase 235 RNG-01/02 + Phase 236 FIND-01 explicit routing. Zero F-29-NN emissions. Zero contracts/ or test/ writes.
  </deliverable>
  <done>`233-02-AUDIT.md` exists with the full structure above; every row cites a 7-char SHA (`52242a10` / `c2e5e0a9` / `314443af`) and a real File:Line in `contracts/`; verdicts use `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` vocabulary; D-05 forward-trace + D-06 backward-trace + D-06 commitment-window enumeration (12+ variables per user directive) + D-07 no-re-use-bias + D-12 addendum integration all present; Scope-guard Deferrals + Downstream Hand-offs subsections explicitly name Phase 235 RNG-01 + Phase 235 RNG-02 + Phase 236 FIND-01; zero F-29-NN IDs emitted; zero contracts/ or test/ writes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-review checkpoint — present 233-02-AUDIT.md to orchestrator before commit</name>
  <what-built>`233-02-AUDIT.md` with the full per-function verdict table + D-06 backward-trace + D-06 commitment-window enumeration (12+ variables) + D-07 no-re-use + D-12 addendum integration + subsections described in Task 1. No contracts/ or test/ modifications.</what-built>
  <how-to-verify>
    1. `cat .planning/phases/233-jackpot-baf-entropy-audit/233-02-AUDIT.md` — confirm full artifact structure
    2. Verify every verdict row cites `52242a10`, `c2e5e0a9`, or `314443af` (7-char SHA) and a real File:Line in `contracts/`
    3. Verify verdicts use only `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`
    4. Verify D-06 Commitment-Window Enumeration Table enumerates at least 12 distinct player-controllable state variables with the required column schema (per user directive — NOT a generic assertion)
    5. Verify D-06 Backward-Trace Sub-Section traces from all 4 MintModule consumer sites (443, 469, 476, 489) backward to rawFulfillRandomWords:1702
    6. Verify D-07 No-Re-Use-Bias Sub-Section analyzes IM-10 / IM-11 / IM-12 salt-space within a single advanceGame tx
    7. Verify D-12 230-02-DELTA-ADDENDUM.md Integration Sub-Section re-verifies all 7 formulation groups
    8. Verify Downstream Hand-offs explicitly names Phase 235 RNG-01 + Phase 235 RNG-02 + Phase 236 FIND-01
    9. Verify zero F-29-NN / F-29- strings anywhere in the file (per D-11)
    10. `git status --porcelain contracts/ test/` — must be empty
    11. Verify `.planning/phases/233-jackpot-baf-entropy-audit/233-02-AUDIT.md` is the ONLY new file
  </how-to-verify>
  <resume-signal>Type "approved" (then Task 3 commits the AUDIT) or describe issues / required revisions.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit approved 233-02-AUDIT.md</name>
  <files>.planning/phases/233-jackpot-baf-entropy-audit/233-02-AUDIT.md</files>
  <read_first>
    - .planning/phases/233-jackpot-baf-entropy-audit/233-02-AUDIT.md (human-reviewed and approved in Task 2)
  </read_first>
  <action>
Stage and commit the approved `233-02-AUDIT.md` only. Commit message body explains it's a READ-only audit artifact per v29.0 milestone rule; no `contracts/` or `test/` changes in this commit.

```
git add .planning/phases/233-jackpot-baf-entropy-audit/233-02-AUDIT.md
git commit -m "docs(233-02): JKP-02 explicit entropy passthrough adversarial audit

Per-function verdict table + D-06 backward-trace + commitment-window
enumeration for commit 52242a10 + downstream c2e5e0a9 / 314443af keccak
hardening per 230-02-DELTA-ADDENDUM.md integration (D-12):

- Three AdvanceModule call sites (IM-10 advanceGame:315-318 FF-promotion,
  IM-11 advanceGame:337 near-future prep + _prepareFutureTickets internals,
  IM-12 _consolidatePoolsAndRewardJackpots:407 post-transition FF) verified
  to thread rngWord local forward unchanged; D-05 entropy-equivalence proven
  byte-identical to pre-fix rngWordCurrent SLOAD via forward-trace from
  rngGate:L283 return.
- MintModule.processFutureTicketBatch receiver (line 385-498): pre-fix
  top-of-body SLOAD uint256 entropy = rngWordCurrent; REMOVED; entropy now
  caller-provided. Semantic improvement: immune to mid-call rngWordCurrent=0
  SSTOREs.
- IM-13 delegatecall boundary: selector alignment corroborated by
  make check-delegatecall 44/44 PASS at HEAD (Phase 230 §3.5). ID-103 PASS
  cited once (D-10) — pure interface-drift closed.
- D-06 backward-trace: each of four entropy-consumer sites (MintModule
  body lines 443, 469, 476, 489) traced backward to VRF coordinator
  callback at rawFulfillRandomWords:1702. VRF word unknown at input
  commitment time. Cross-reference Phase 232.1 Plan 03 PFTB-AUDIT.md
  non-zero guarantee.
- D-06 commitment-window: EXPLICIT 12+ variable enumeration (per user
  directive — NOT generic assertion) showing every player-controllable
  state variable that can change during the VRF window is either
  rngLocked-guarded or non-influential on IM-13 consumer behavior.
- D-07 no-re-use bias: salt-space analysis across IM-10/11/12 within
  single advanceGame tx — each consumer resolves different lvl ->
  different baseKey -> different keccak preimage; no bias from shared
  seed.
- D-12 230-02-DELTA-ADDENDUM.md integration: verdict rows for the 17
  downstream keccak formulations (EntropyLib.hash2 helper; MintModule
  _rollRemainder + _raritySymbolBatch; 7 JackpotModule formulation groups
  covering 16 sites; PayoutUtils._calcAutoRebuy). Cross-formulation
  collision analysis: hash2(64 bytes) vs keccak256(abi.encode)(96 bytes)
  -> different lengths -> disjoint preimages -> disjoint outputs.

Zero F-29-NN emissions (Phase 236 FIND-01 owns ID assignment per CONTEXT
D-11). Phase 235 RNG-01 / RNG-02 owns milestone-wide proof; JKP-02 provides
passthrough-specific slice via §2.5 IM-22 cross-reference.
Zero contracts/ or test/ writes (READ-only v29.0 milestone rule)."
```

Do NOT run `git push`. Verify `git log -1` shows the new commit and `git status --porcelain contracts/ test/` is empty.
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -qE "233-02.*JKP-02.*entropy.*passthrough" && test -z "$(git status --porcelain contracts/ test/)"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one new commit added via `git log -1`
    - Commit message subject starts with `docs(233-02):` and contains `JKP-02` + `entropy` + `passthrough`
    - Commit touches exactly `.planning/phases/233-jackpot-baf-entropy-audit/233-02-AUDIT.md` (no contracts/ or test/ files)
    - `git status --porcelain contracts/ test/` returns empty
    - Commit not pushed — branch state local to repository
  </acceptance_criteria>
  <deliverable>
Approved `233-02-AUDIT.md` committed with a `docs(233-02): JKP-02 ...` message; no code modifications; no push.
  </deliverable>
  <done>Commit exists with correct message format; repository `contracts/` and `test/` remain unmodified; commit is local only (not pushed).</done>
</task>

</tasks>

<verification>
- `233-02-AUDIT.md` exists with per-function verdict table covering every element in 230-01-DELTA-MAP.md §1.1 + §1.4 + §1.11 owned by `52242a10` + IM-10..IM-13 + IM-22 + ID-103 citation
- D-12 230-02-DELTA-ADDENDUM.md integration: 17-site post-`c2e5e0a9` + `314443af` keccak formulation verdict rows present
- Locked column set per D-02 (`Function | Commit SHA | File:Line | Verdict | Evidence | Notes`) + `Finding Candidate` column per 232-precedent
- Every verdict row cites `52242a10` or `c2e5e0a9` or `314443af` (7-char SHA)
- Every File:Line anchor resolves into `contracts/` at HEAD
- Verdicts exclusively use `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (per D-02)
- Every row carries `Y` or `N` in Finding Candidate column
- D-05 entropy-equivalence proof: forward-trace from rngWord local to delegatecall payload cross-referenced against pre-fix rngWordCurrent SLOAD
- D-06 backward-trace: four MintModule consumer sites (443, 469, 476, 489) traced to rawFulfillRandomWords:1702
- D-06 commitment-window enumeration: EXPLICIT 12+ variable table per user directive (NOT generic assertion)
- D-07 no-re-use-bias: salt-space analysis across IM-10 / IM-11 / IM-12
- Findings-Candidate / Scope-guard Deferrals / Downstream Hand-offs subsections present
- Hand-offs explicitly name Phase 235 RNG-01, Phase 235 RNG-02, Phase 236 FIND-01
- Zero `F-29-NN` references anywhere in the file (per D-11)
- Zero writes to `contracts/` or `test/` during plan execution
- Approved AUDIT.md committed with message `docs(233-02): JKP-02 explicit entropy passthrough adversarial audit`
</verification>

<success_criteria>
Every function modified by commit `52242a10` across the JKP-02 surface has a verdict against D-05 entropy-equivalence, D-06 backward-trace, D-06 commitment-window enumeration (explicit 12+ variable table per user directive), and D-07 no-re-use-bias. Every verdict cites a 7-char SHA + File:Line into `contracts/`. D-12 230-02-DELTA-ADDENDUM.md integration covers all 17 downstream keccak sites with cryptographic-equivalence evidence. Phase 235 RNG-01 / RNG-02 explicitly named as milestone-wide proof owners; this plan provides the passthrough-specific slice. Phase 230 delta-map and addendum are not edited; any out-of-scope discovery is recorded in Scope-guard Deferrals per D-03. READ-only milestone rule honored: zero writes to `contracts/` or `test/`.
</success_criteria>

<output>
After completion, create `.planning/phases/233-jackpot-baf-entropy-audit/233-02-SUMMARY.md`
</output>
</content>
