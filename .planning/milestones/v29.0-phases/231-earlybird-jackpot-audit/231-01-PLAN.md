---
phase: 231-earlybird-jackpot-audit
plan: 231-01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/231-earlybird-jackpot-audit/231-01-AUDIT.md
autonomous: true
requirements: [EBD-01]
must_haves:
  truths:
    - "Every function listed in 230-01-DELTA-MAP.md §1.1/§1.4/§1.5/§1.6/§1.9 owned by commit f20a2b5e has a PASS/FAIL/DEFER verdict row"
    - "Every attack vector in CONTEXT.md D-08 EBD-01 (CEI, reentrancy, storage ordering, budget conservation, signature contraction, gas delta, double/zero-award regression) appears as at least one verdict row"
    - "Every verdict row cites the 7-char commit SHA f20a2b5e (or both f20a2b5e and d5284be5 for _purchaseFor / _callTicketPurchase dual-ownership rows, per D-05)"
    - "Every verdict row cites a File:Line anchor pointing into contracts/ at HEAD"
    - "No F-29-NN finding IDs appear anywhere in 231-01-AUDIT.md (per D-09)"
    - "Findings-Candidate Block, Scope-guard Deferrals, and Downstream Hand-offs subsections are present and non-empty (hand-offs may be empty but subsection header must exist)"
  artifacts:
    - path: ".planning/phases/231-earlybird-jackpot-audit/231-01-AUDIT.md"
      provides: "EBD-01 purchase-phase finalize refactor adversarial audit"
      contains: "## Per-Function Verdict Table"
  key_links:
    - from: "230-01-DELTA-MAP.md §1.1/§1.4/§1.5/§1.6/§1.9/§2.1 IM-01..IM-05 rows"
      to: "231-01-AUDIT.md per-function verdict table rows"
      via: "Every f20a2b5e function in the delta map has a verdict"
      pattern: "PASS|FAIL|DEFER"
    - from: "231-01-AUDIT.md FAIL/DEFER verdicts"
      to: "Phase 236 FIND-01 finding-candidate pool"
      via: "Findings-Candidate Block (no F-29-NN IDs)"
      pattern: "## Findings-Candidate Block"
---

<objective>
EBD-01 adversarial audit of the earlybird purchase-phase finalize refactor owned by commit `f20a2b5e` ("refactor(earlybird): finalize at level transition, unify award call per purchase"). Produces `231-01-AUDIT.md`: a per-function verdict table covering every function touched by `f20a2b5e` across the earlybird purchase surface, plus Findings-Candidate / Scope-guard Deferrals / Downstream Hand-offs subsections.

Purpose: Every purchase-side caller now issues a single unified `_awardEarlybirdDgnrs(buyer, wei)` at the bottom of the purchase flow (instead of two calls — one from `recordMint` and one inline). A NEW level-transition hook `_finalizeEarlybird` fires exactly once when `lvl == EARLYBIRD_END_LEVEL` and dumps the remaining Earlybird pool to Lootbox via the external DGNRS contract. A C4A warden would probe: (a) CEI ordering, (b) double/zero-award regression across the 4 unified call sites, (c) signature-contraction correctness (3-arg → 2-arg — is the dropped `currentLevel` / `passLevel` / `startLevel` load-bearing anywhere?), (d) one-shot idempotency of the sentinel flip, (e) budget conservation at the level-transition dump, (f) reentrancy surface on the no-longer-awarding `recordMint`. This plan surfaces adversarial verdicts. Phase 236 FIND-01 owns severity classification and `F-29-NN` ID assignment (D-09).

Output: `231-01-AUDIT.md` with per-function verdict table + Findings-Candidate Block + Scope-guard Deferrals + Downstream Hand-offs subsections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/231-earlybird-jackpot-audit/231-CONTEXT.md
@.planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md
@.planning/phases/230-delta-extraction-scope-map/230-01-SUMMARY.md
@.planning/milestones/v25.0-phases/214-adversarial-audit/214-01-REENTRANCY-CEI.md
@.planning/milestones/v25.0-phases/214-adversarial-audit/214-02-ACCESS-OVERFLOW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract f20a2b5e diff + read target source at HEAD for every purchase-side earlybird function</name>
  <files>.planning/phases/231-earlybird-jackpot-audit/231-01-AUDIT.md</files>
  <read_first>
    - .planning/phases/231-earlybird-jackpot-audit/231-CONTEXT.md (D-01 through D-09 — auto-rules, locked columns, READ-only scope guard, no F-29-NN IDs, attack vector enumeration for EBD-01 in D-08)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.1 (rows for `_finalizeRngRequest` MODIFIED by f20a2b5e, `_finalizeEarlybird` NEW by f20a2b5e) — lines 74-89
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.4 (rows for `_purchaseFor` MODIFIED by f20a2b5e+d5284be5, `_callTicketPurchase` MODIFIED by f20a2b5e+d5284be5) — lines 122-132
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.5 (rows for `_purchaseWhaleBundle`, `_purchaseLazyPass`, `_purchaseDeityPass` MODIFIED by f20a2b5e) — lines 134-144
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.6 (row for `recordMint` MODIFIED by f20a2b5e — award-block removed) — lines 146-155
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.9 (row for `_awardEarlybirdDgnrs` MODIFIED by f20a2b5e — 3-arg → 2-arg) — lines 177-188
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §2.1 IM-01 through IM-05 (earlybird cross-module chains, all f20a2b5e) — lines 236-244
    - Output of `git show f20a2b5e --stat` and `git show f20a2b5e -- contracts/modules/DegenerusGameAdvanceModule.sol contracts/modules/DegenerusGameMintModule.sol contracts/modules/DegenerusGameWhaleModule.sol contracts/DegenerusGame.sol contracts/storage/DegenerusGameStorage.sol` to inspect the full authored diff — the delta IS the audit surface
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD — locate `_finalizeRngRequest`, `_finalizeEarlybird`, EARLYBIRD_END_LEVEL constant definition, and note the call order (placement after `_rewardTopAffiliate(lvl)` and `level = lvl`, before decimator window logic per §1.1 row). Record exact line numbers for File:Line citation.
    - contracts/modules/DegenerusGameMintModule.sol at HEAD — locate `_purchaseFor` and `_callTicketPurchase`. Identify (a) the unified bottom-of-function `_awardEarlybirdDgnrs(buyer, ticketFreshEth + lootboxFreshEth)` call site per IM-01, (b) the returned `freshEth` addition to the `_callTicketPurchase` return tuple (per §1.4 row), (c) the CEI position of the unified award call relative to token transfers and storage writes. Record exact line numbers.
    - contracts/modules/DegenerusGameWhaleModule.sol at HEAD — locate all three functions and confirm each now calls 2-arg `_awardEarlybirdDgnrs(buyer, <wei>)` (IM-02/03/04). Record line numbers per call site.
    - contracts/DegenerusGame.sol at HEAD — locate `recordMint`. Confirm the prior trailing 7-line earlybird block (which called `_awardEarlybirdDgnrs(player, earlybirdEth, lvl)`) is removed. Verify function now terminates at `_recordMintDataModule(player, lvl, mintUnits)`. Record line numbers.
    - contracts/storage/DegenerusGameStorage.sol at HEAD — locate `_awardEarlybirdDgnrs`. Confirm (a) signature is 2-arg `(address buyer, uint256 purchaseWei)`, (b) body includes new `if (poolStart == type(uint256).max) return;` early-return guard, (c) body no longer contains the `if (currentLevel >= EARLYBIRD_END_LEVEL) { ... }` finalization branch (relocated to `_finalizeEarlybird` in AdvanceModule), (d) DGNRS call sites use `dgnrs.*` alias (not `IStakedDegenerusStonk(ContractAddresses.SDGNRS).*`). Record line numbers.
    - .planning/milestones/v25.0-phases/214-adversarial-audit/214-01-REENTRANCY-CEI.md (table shape, methodology preamble, per-function table columns, Findings section style — this audit file's structure is the precedent to mirror; lines 1-80 show the shape to reproduce)
  </read_first>
  <action>
Execute a fresh-from-source adversarial audit (per D-03) of the f20a2b5e purchase-phase finalize refactor. No reuse of v25.0 Phase 214 or v27.0 Phase 223 verdicts as pre-approved — audit every function as if no prior work exists.

**Step 1 — Build the scope list from `230-01-DELTA-MAP.md` §4 Consumer Index EBD-01 row (D-04).** The target function set is exactly:
  - `DegenerusGameAdvanceModule._finalizeRngRequest` (MODIFIED by f20a2b5e) — §1.1
  - `DegenerusGameAdvanceModule._finalizeEarlybird` (NEW by f20a2b5e) — §1.1
  - `DegenerusGameMintModule._purchaseFor` (MODIFIED by f20a2b5e; d5284be5 co-owns for wei fix) — §1.4
  - `DegenerusGameMintModule._callTicketPurchase` (MODIFIED by f20a2b5e; d5284be5 co-owns) — §1.4
  - `DegenerusGameWhaleModule._purchaseWhaleBundle` (MODIFIED by f20a2b5e) — §1.5
  - `DegenerusGameWhaleModule._purchaseLazyPass` (MODIFIED by f20a2b5e) — §1.5
  - `DegenerusGameWhaleModule._purchaseDeityPass` (MODIFIED by f20a2b5e) — §1.5
  - `DegenerusGame.recordMint` (MODIFIED by f20a2b5e — award-block removed) — §1.6
  - `DegenerusGameStorage._awardEarlybirdDgnrs` (MODIFIED by f20a2b5e — 3-arg → 2-arg) — §1.9

**Step 2 — For each target function, run the attack vectors from CONTEXT.md D-08 EBD-01:**
  - **(a) CEI ordering:** For `_finalizeEarlybird` + `_purchaseFor` unified award call — identify every state mutation (SSTORE to `earlybirdDgnrsPoolStart` sentinel in `_finalizeEarlybird`; pool-balance reads in `_awardEarlybirdDgnrs`) and every external call (`dgnrs.poolBalance`, `dgnrs.transferBetweenPools`, `dgnrs.transferFromPool`). Verify Checks-Effects-Interactions ordering. Record a verdict row per function with the File:Line anchor of each external call.
  - **(b) Reentrancy across `recordMint` no-longer-awards path:** The previously-present earlybird award block inside `recordMint` is gone. Verify no caller downstream of `_purchaseFor` still expects `recordMint` to award earlybird (grep-trace `recordMint` callers). Verify no external-call surface widens — `recordMint` is `external payable`; confirm it remains safe with the award block removed. Record PASS/FAIL/DEFER.
  - **(c) Storage read/write ordering for pool SLOADs before `_awardEarlybirdDgnrs`:** Inside `_purchaseFor`, confirm that `ticketFreshEth` and `lootboxFreshEth` are produced BEFORE the unified bottom-of-function award call, and that the sum matches intent (no accidental re-SLOAD of a mutated pool). Record evidence — the local variable lineage.
  - **(d) Budget conservation at level-transition dump (Earlybird → Lootbox via DGNRS):** Inside `_finalizeEarlybird`, verify the pool-dump amount is derived from `dgnrs.poolBalance(...)` (or equivalent) and that the subsequent `dgnrs.transferBetweenPools(...)` transfers exactly that amount. The DGNRS external-contract target itself is OUT OF SCOPE per §2 Out-of-scope preamble — verify only the caller-side math. Any balance drift concern that requires algebraic proof across all pool-mutating SSTOREs is a Phase 235 CONS-01 HAND-OFF (D-07) — record in Downstream Hand-offs.
  - **(e) Signature-contraction correctness (3-arg → 2-arg):** For `_awardEarlybirdDgnrs`, verify the function body no longer branches on the dropped `currentLevel` parameter AND no internal read substitutes a WRONG live-state read (e.g., `storageGetter().level()` mid-purchase, which would differ from the previously-passed `passLevel` during whale bundles where `passLevel` could exceed `level()`). Inspect the body — confirm no `level()` call path inside `_awardEarlybirdDgnrs` that depends on current game level after the sentinel guard. If the body references `level()` or equivalent, record as FAIL or DEFER with File:Line.
  - **(f) Gas delta vs v27.0 worst-case purchase path:** Qualitative — count external calls removed (the duplicate `recordMint` award call + one inline) vs. external calls added (none — the bottom-of-purchase unified call replaces prior two). Record evidence-based verdict: PASS if net external-call count reduces or stays flat. Do NOT run forge gas benchmarks (per Deferred #Gas-only benchmarking).
  - **(g) Double-award / zero-award regression:** For each of the 4 purchase entry points (`_purchaseFor`, `_purchaseWhaleBundle`, `_purchaseLazyPass`, `_purchaseDeityPass`), trace every path through the function and confirm `_awardEarlybirdDgnrs` fires EXACTLY ONCE regardless of branch (lootbox vs. ticket-only for `_purchaseFor`; pass-only vs. bundle for whale paths). For `recordMint` removal: confirm no regression path where earlybird is now missed (was previously fired from `recordMint` trailing block). Record PASS/FAIL/DEFER per function.

**Step 3 — Write `231-01-AUDIT.md` mirroring the v25.0 Phase 214 precedent shape (see `214-01-REENTRANCY-CEI.md` lines 1-80).** Required structure, in this exact order:

```markdown
# EBD-01 Adversarial Audit: Earlybird Purchase-Phase Finalize Refactor

**Scope:** Commit `f20a2b5e` ("refactor(earlybird): finalize at level transition, unify award call per purchase") — all functions touched across §1.1, §1.4, §1.5, §1.6, §1.9 of `230-01-DELTA-MAP.md`
**Method:** Fresh read from HEAD source (per D-03) — no reuse of v25.0 Phase 214 or v27.0 Phase 223 verdicts as pre-approved
**Scope source:** `230-01-DELTA-MAP.md` §4 Consumer Index row EBD-01 (per D-04)
**Finding-ID policy:** No `F-29-NN` IDs emitted (per D-09) — findings-candidate anchors only; Phase 236 FIND-01 owns ID assignment

## Methodology

[One paragraph: 7 attack vectors per CONTEXT.md D-08 EBD-01 (CEI, reentrancy, storage ordering, budget conservation at level-transition dump, signature-contraction correctness, gas delta, double/zero-award regression). Locked verdict vocabulary: PASS | FAIL | DEFER. Locked columns per auto-rule 4.]

## Findings-Candidate Block

[Prose free-form (no F-29-NN IDs). One block per FAIL or DEFER verdict identified in the table below. Each block: title, verdict, File:Line, owning SHA, attack vector, evidence excerpt, recommended Phase 236 severity-classification note. If zero FAILs and zero DEFERs, state "No candidate findings — all verdicts PASS against f20a2b5e surface."]

## Per-Function Verdict Table

| Function | File:Line | Attack Vector Considered | Verdict (PASS/FAIL/DEFER) | Evidence | Owning SHA |
|---|---|---|---|---|---|
| `DegenerusGameAdvanceModule._finalizeRngRequest` | contracts/modules/DegenerusGameAdvanceModule.sol:<line> | CEI ordering at new `_finalizeEarlybird` hook | PASS | [source excerpt + reasoning] | f20a2b5e |
| `DegenerusGameAdvanceModule._finalizeRngRequest` | contracts/modules/DegenerusGameAdvanceModule.sol:<line> | Double-call regression (hook fires exactly once) | PASS | [evidence: sentinel guarded path + single call placement] | f20a2b5e |
| `DegenerusGameAdvanceModule._finalizeEarlybird` | contracts/modules/DegenerusGameAdvanceModule.sol:<line> | One-shot idempotency (sentinel flip) | PASS | [excerpt: `poolStart == type(uint256).max` guard] | f20a2b5e |
| `DegenerusGameAdvanceModule._finalizeEarlybird` | contracts/modules/DegenerusGameAdvanceModule.sol:<line> | Budget conservation at level-transition dump | PASS | [pool-balance read → transferBetweenPools — caller-side math verified; DGNRS external contract out-of-scope per §2 preamble] | f20a2b5e |
| `DegenerusGameMintModule._purchaseFor` | contracts/modules/DegenerusGameMintModule.sol:<line> | CEI on unified award call | <verdict> | [evidence] | f20a2b5e (d5284be5 co-owns) |
| `DegenerusGameMintModule._purchaseFor` | contracts/modules/DegenerusGameMintModule.sol:<line> | Storage ordering before award (ticketFreshEth + lootboxFreshEth lineage) | <verdict> | [evidence] | f20a2b5e (d5284be5 co-owns) |
| `DegenerusGameMintModule._purchaseFor` | contracts/modules/DegenerusGameMintModule.sol:<line> | Double/zero-award regression (award fires exactly once on every path) | <verdict> | [evidence] | f20a2b5e (d5284be5 co-owns) |
| `DegenerusGameMintModule._callTicketPurchase` | contracts/modules/DegenerusGameMintModule.sol:<line> | Return-tuple restructure (freshEth surfaced to caller) | <verdict> | [evidence] | f20a2b5e (d5284be5 co-owns) |
| `DegenerusGameWhaleModule._purchaseWhaleBundle` | contracts/modules/DegenerusGameWhaleModule.sol:<line> | Signature-contraction correctness (dropped `passLevel` not load-bearing) | <verdict> | [evidence] | f20a2b5e |
| `DegenerusGameWhaleModule._purchaseLazyPass` | contracts/modules/DegenerusGameWhaleModule.sol:<line> | Signature-contraction correctness (dropped `startLevel` not load-bearing) | <verdict> | [evidence] | f20a2b5e |
| `DegenerusGameWhaleModule._purchaseDeityPass` | contracts/modules/DegenerusGameWhaleModule.sol:<line> | Signature-contraction correctness (dropped `passLevel` not load-bearing) | <verdict> | [evidence] | f20a2b5e |
| `DegenerusGame.recordMint` | contracts/DegenerusGame.sol:<line> | Reentrancy post award-block removal | <verdict> | [evidence] | f20a2b5e |
| `DegenerusGame.recordMint` | contracts/DegenerusGame.sol:<line> | Zero-award regression — no downstream caller expects recordMint to award | <verdict> | [evidence: grep-trace of `recordMint` callers] | f20a2b5e |
| `DegenerusGameStorage._awardEarlybirdDgnrs` | contracts/storage/DegenerusGameStorage.sol:<line> | Signature-contraction correctness (body doesn't read live level to substitute dropped arg) | <verdict> | [evidence: body inspection] | f20a2b5e |
| `DegenerusGameStorage._awardEarlybirdDgnrs` | contracts/storage/DegenerusGameStorage.sol:<line> | CEI ordering (sentinel guard → pool reads → external transfer) | <verdict> | [evidence] | f20a2b5e |
| `DegenerusGameStorage._awardEarlybirdDgnrs` | contracts/storage/DegenerusGameStorage.sol:<line> | Gas delta (extra SLOAD + guard vs. removed level-branch) | <verdict> | [evidence] | f20a2b5e |

[Rows above are the MINIMUM set. Additional rows allowed where an attack vector on a function warrants separate evidence — one row per (function × distinct attack vector). Replace `<line>` with actual File:Line at HEAD.]

## High-Risk Patterns Analyzed

### Unified Award Call Per Purchase (IM-01..IM-04)
[Prose analysis: all 4 purchase entry points now use 2-arg `_awardEarlybirdDgnrs`. The prior dual-call pattern (recordMint + inline lootbox) collapsed to one. Trace every branch per entry point — confirm exactly-once firing. Note any branch where early-return skips the award (e.g., free tickets, refund paths) and whether that's intentional.]

### Level-Transition Sentinel Flip (IM-05)
[Prose analysis of `_finalizeEarlybird` one-shot semantics: `earlybirdDgnrsPoolStart` flipped to `type(uint256).max` after dump; subsequent `_awardEarlybirdDgnrs` calls no-op via the new early-return guard in Storage. Confirm no reachable path where the sentinel flip is skipped at `EARLYBIRD_END_LEVEL`. Confirm the hook cannot re-fire on later level transitions.]

### Signature Contraction (3-arg → 2-arg)
[Prose analysis: the dropped `currentLevel` / `passLevel` / `startLevel` parameters — verify the Storage body no longer depends on that value for any computation. Specifically, confirm the finalization branch that previously used `currentLevel >= EARLYBIRD_END_LEVEL` is entirely removed (not replaced with a live-level read that could differ from the previously-passed `passLevel` for whale paths).]

## Scope-guard Deferrals

[Any earlybird-adjacent concern surfaced during this audit that is NOT anchored in `230-01-DELTA-MAP.md` §4 EBD-01 rows is recorded here per D-06. Phase 230 is READ-only. Each entry: concern, why not in EBD-01 scope, recommended receiving phase (Phase 236 REG-01 / FIND-01 typically). If none, state "None surfaced during this audit."]

## Downstream Hand-offs

[Concerns overlapping Phase 235 territory (RNG, CONS) recorded here per D-07 with the target requirement ID. Expected entries:
- Phase 235 CONS-01 hand-off: Algebraic sum-before = sum-after proof for every pool-mutating SSTORE in `_finalizeEarlybird` (dump to Lootbox) and in `_awardEarlybirdDgnrs` (purchase-side allocation). Phase 231 verified CEI position only.
- Phase 236 FIND-01 hand-off: Every FAIL / DEFER verdict row above becomes a finding-candidate anchor for Phase 236 severity classification.
Any additional hand-offs surfaced during audit recorded here.]
```

Every verdict row MUST:
- Use exactly the locked column set per auto-rule 4: `Function | File:Line | Attack Vector Considered | Verdict (PASS/FAIL/DEFER) | Evidence | Owning SHA`
- Cite the 7-char SHA `f20a2b5e` (co-ownership with `d5284be5` for `_purchaseFor` and `_callTicketPurchase` per §1.4 dual-SHA rows is allowed — cite both comma-separated)
- Use a real File:Line anchor from the HEAD source (never a placeholder)
- Never reference F-29-NN IDs (per D-09)

Commit contract reads ONLY from `contracts/` directory (per user feedback — never read stale copies).
  </action>
  <verify>
    <automated>test -f .planning/phases/231-earlybird-jackpot-audit/231-01-AUDIT.md && grep -c "f20a2b5e" .planning/phases/231-earlybird-jackpot-audit/231-01-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/231-earlybird-jackpot-audit/231-01-AUDIT.md` exists
    - File contains the literal header string `## Per-Function Verdict Table`
    - File contains the literal header string `## Findings-Candidate Block`
    - File contains the literal header string `## Scope-guard Deferrals`
    - File contains the literal header string `## Downstream Hand-offs`
    - Verdict table header row is exactly `| Function | File:Line | Attack Vector Considered | Verdict (PASS/FAIL/DEFER) | Evidence | Owning SHA |`
    - Every row in the Per-Function Verdict Table cites the 7-char SHA `f20a2b5e` (optionally comma-paired with `d5284be5` for `_purchaseFor` / `_callTicketPurchase`)
    - Every row cites a File:Line anchor starting with `contracts/` (specifically `contracts/modules/DegenerusGameAdvanceModule.sol:`, `contracts/modules/DegenerusGameMintModule.sol:`, `contracts/modules/DegenerusGameWhaleModule.sol:`, `contracts/DegenerusGame.sol:`, or `contracts/storage/DegenerusGameStorage.sol:`)
    - Every row's verdict is exactly one of the strings `PASS`, `FAIL`, or `DEFER`
    - At least one row per target function exists: `_finalizeRngRequest`, `_finalizeEarlybird`, `_purchaseFor`, `_callTicketPurchase`, `_purchaseWhaleBundle`, `_purchaseLazyPass`, `_purchaseDeityPass`, `recordMint`, `_awardEarlybirdDgnrs` (9 functions — minimum 9 rows, expected more where multiple attack vectors per function warrant separate rows)
    - `grep -c "PASS\|FAIL\|DEFER" .planning/phases/231-earlybird-jackpot-audit/231-01-AUDIT.md` returns at least 15 (one verdict per function × attack vector combination, target at least 15 distinct verdict rows including duplicates of enum strings in prose)
    - `grep -c "f20a2b5e" .planning/phases/231-earlybird-jackpot-audit/231-01-AUDIT.md` returns at least 9 (one per function row minimum)
    - The string `F-29-NN` does NOT appear in the file (per D-09)
    - The string `F-29-` does NOT appear in any form in the file (Phase 236 owns ID assignment)
    - High-Risk Patterns Analyzed section contains at least three subsections (Unified Award Call, Level-Transition Sentinel Flip, Signature Contraction)
    - No placeholder line numbers like `:<line>` remain in the final file — every File:Line is a real integer or integer range
    - No contract source content read from outside `contracts/` directory (per user feedback — only `contracts/*.sol` at HEAD is authoritative)
  </acceptance_criteria>
  <done>`231-01-AUDIT.md` exists with per-function verdict table covering all 9 f20a2b5e purchase-side functions, every row citing the correct SHA and a real File:Line in contracts/, all 7 EBD-01 attack vectors from CONTEXT.md D-08 covered across the verdict rows, Findings-Candidate / Scope-guard Deferrals / Downstream Hand-offs subsections present and populated, no F-29-NN IDs emitted.</done>
</task>

</tasks>

<verification>
- `231-01-AUDIT.md` exists with per-function verdict table covering every function in 230-01-DELTA-MAP.md §1.1/§1.4/§1.5/§1.6/§1.9 owned by f20a2b5e
- Locked column set used per auto-rule 4
- Every verdict row cites `f20a2b5e` (co-paired with `d5284be5` where §1.4 dual-ownership applies)
- Every File:Line anchor resolves into `contracts/` at HEAD
- Findings-Candidate / Scope-guard Deferrals / Downstream Hand-offs subsections present
- All 7 attack vectors from CONTEXT.md D-08 EBD-01 appear in at least one verdict row
- Zero `F-29-NN` references anywhere in the file
</verification>

<success_criteria>
Every purchase-side function touched by commit `f20a2b5e` has a PASS/FAIL/DEFER verdict against the 7 attack vectors locked in CONTEXT.md D-08 EBD-01. Every verdict is traceable via SHA + File:Line. FAIL/DEFER verdicts are captured as finding-candidate anchors for Phase 236 FIND-01, which owns severity classification and `F-29-NN` ID assignment. Phase 230 delta-map is not edited; any out-of-scope discovery is recorded in Scope-guard Deferrals per D-06.
</success_criteria>

<output>
After completion, create `.planning/phases/231-earlybird-jackpot-audit/231-01-SUMMARY.md`
</output>
