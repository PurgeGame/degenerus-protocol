---
phase: 231-earlybird-jackpot-audit
plan: 231-03
type: execute
wave: 2
depends_on: [231-01, 231-02]
files_modified:
  - .planning/phases/231-earlybird-jackpot-audit/231-03-AUDIT.md
autonomous: true
requirements: [EBD-03]
must_haves:
  truths:
    - "The combined earlybird state machine is traced end-to-end from purchase-phase level progression → `_finalizeEarlybird` at `EARLYBIRD_END_LEVEL` → jackpot-phase `_runEarlyBirdLootboxJackpot` → `_rollWinningTraits(rngWord, true)`"
    - "Normal / skip-split / gameover phase transitions in `DegenerusGameAdvanceModule.advanceGame` are each enumerated with a per-path verdict on the earlybird hand-off"
    - "No double-spend is demonstrated across the handoff: the pool dumped by `_finalizeEarlybird` is the same pool `_runEarlyBirdLootboxJackpot` consumes (no drift)"
    - "No orphaned reserves when `_finalizeEarlybird` fires at `EARLYBIRD_END_LEVEL` (one-shot sentinel covers all subsequent transitions)"
    - "No missed emissions — every reachable path through `advanceGame` either fires `_finalizeEarlybird` exactly once at the correct level or correctly skips (game-over path before EARLYBIRD_END_LEVEL)"
    - "Every verdict in the Per-Path Verdict Block cites at least one of `f20a2b5e` or `20a951df` (cross-commit invariant is EBD-03's defining surface, per D-05)"
    - "Every cross-reference to 231-01 or 231-02 verdicts is treated as a REGRESSION ANCHOR per D-03, not as pre-approval"
    - "No F-29-NN finding IDs appear anywhere in 231-03-AUDIT.md (per D-09)"
  artifacts:
    - path: ".planning/phases/231-earlybird-jackpot-audit/231-03-AUDIT.md"
      provides: "EBD-03 combined earlybird state-machine adversarial audit (end-to-end path walk)"
      contains: "## State-Machine Path Walk"
  key_links:
    - from: "231-01-AUDIT.md function verdicts (f20a2b5e surface)"
      to: "231-03-AUDIT.md state-machine anchors (purchase-phase finalize side)"
      via: "Cross-citation of 231-01 verdict rows as regression anchors"
      pattern: "231-01"
    - from: "231-02-AUDIT.md function verdicts (20a951df surface)"
      to: "231-03-AUDIT.md state-machine anchors (jackpot-phase run side)"
      via: "Cross-citation of 231-02 verdict rows as regression anchors"
      pattern: "231-02"
    - from: "231-03-AUDIT.md FAIL/DEFER verdicts"
      to: "Phase 236 FIND-01 finding-candidate pool"
      via: "Findings-Candidate Block (no F-29-NN IDs)"
      pattern: "## Findings-Candidate Block"
---

<objective>
EBD-03 adversarial audit of the combined earlybird state machine spanning both in-scope commits (`f20a2b5e` purchase-phase finalize + `20a951df` jackpot-phase trait-alignment). Produces `231-03-AUDIT.md`: a numbered path walk (NOT a per-function table — this is cross-function state-machine tracing per CONTEXT.md D-01 and Claude's Discretion in CONTEXT.md Decisions) with a Per-Path Verdict Block and the standard Findings-Candidate / Scope-guard Deferrals / Downstream Hand-offs subsections.

Purpose: 231-01 and 231-02 each closed out a single-commit surface. EBD-03 is the cross-commit invariant: the pool balance the purchase-phase finalize dumps into Lootbox must be exactly what the jackpot-phase trait roll operates on — no drift, no orphaned reserve, no missed emission. A C4A warden would probe: (a) double-spend across (purchase finalize) → (jackpot-phase run) handoff — can the earlybird pool be consumed twice?, (b) orphaned reserves when `_finalizeEarlybird` fires at `EARLYBIRD_END_LEVEL` — is every reachable transition path covered?, (c) missed emissions across normal / skip-split / gameover transitions in `DegenerusGameAdvanceModule.advanceGame` — are there paths where the finalize hook is skipped when it should fire or fires when it should not?, (d) cross-commit invariant: pool dumped by `_finalizeEarlybird` equals pool consumed by `_runEarlyBirdLootboxJackpot`. This plan surfaces the end-to-end state-machine verdicts. Phase 236 FIND-01 owns severity classification and `F-29-NN` ID assignment (D-09). 231-01 and 231-02 verdicts are REGRESSION ANCHORS only, never pre-approval (D-03).

Output: `231-03-AUDIT.md` with numbered state-machine path walk + Per-Path Verdict Block + Findings-Candidate Block + Scope-guard Deferrals + Downstream Hand-offs subsections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/231-earlybird-jackpot-audit/231-CONTEXT.md
@.planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md
@.planning/phases/230-delta-extraction-scope-map/230-01-SUMMARY.md
@.planning/phases/231-earlybird-jackpot-audit/231-01-AUDIT.md
@.planning/phases/231-earlybird-jackpot-audit/231-02-AUDIT.md
@.planning/milestones/v25.0-phases/214-adversarial-audit/214-03-STATE-COMPOSITION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace the combined earlybird state machine end-to-end across f20a2b5e + 20a951df</name>
  <files>.planning/phases/231-earlybird-jackpot-audit/231-03-AUDIT.md</files>
  <read_first>
    - .planning/phases/231-earlybird-jackpot-audit/231-CONTEXT.md (D-01 through D-09 — attack vector enumeration for EBD-03 in D-08, cross-commit invariant is the defining surface, state-machine depiction discretion allowed per Claude's Discretion block)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.1 (rows for `advanceGame` MODIFIED by 2471f8e7+52242a10, `_finalizeRngRequest` MODIFIED by f20a2b5e, `_finalizeEarlybird` NEW by f20a2b5e, `_consolidatePoolsAndRewardJackpots` MODIFIED by 3ad0f8d3 — for phase-transition branching context) — lines 74-89
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.2 (row for `_runEarlyBirdLootboxJackpot` MODIFIED by 20a951df) — lines 91-105
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.9 (row for `_awardEarlybirdDgnrs` MODIFIED by f20a2b5e with new sentinel early-return) — lines 177-188
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §2.1 IM-05 (the level-transition hook chain `_finalizeRngRequest → _finalizeEarlybird`) — line 244
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §2.3 IM-16 (the jackpot-phase chain `_runEarlyBirdLootboxJackpot → _rollWinningTraits`) — line 265
    - .planning/phases/231-earlybird-jackpot-audit/231-01-AUDIT.md (purchase-phase finalize verdicts — regression anchors ONLY per D-03; re-verify the state-machine hand-off independently)
    - .planning/phases/231-earlybird-jackpot-audit/231-02-AUDIT.md (jackpot-phase trait-alignment verdicts — regression anchors ONLY per D-03)
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD — specifically `advanceGame` (MUST enumerate every branch: normal level progression, skip-split transitions, gameover path, phase-transition packing from 2471f8e7) AND `_finalizeRngRequest` (location of the `_finalizeEarlybird` hook — placement after `_rewardTopAffiliate(lvl)` and `level = lvl`, before decimator window logic per §1.1 row) AND `_consolidatePoolsAndRewardJackpots` (for the jackpot-phase branch that eventually calls `payDailyJackpot` chain reaching `_runEarlyBirdLootboxJackpot`). Record File:Line for every cited branch.
    - contracts/modules/DegenerusGameJackpotModule.sol at HEAD — `_runEarlyBirdLootboxJackpot` entry point; trace its caller chain backward from 231-02-AUDIT.md evidence.
    - contracts/storage/DegenerusGameStorage.sol at HEAD — `_awardEarlybirdDgnrs` sentinel early-return (the `if (poolStart == type(uint256).max) return;` guard) — this is the downstream no-op side of the sentinel flip that prevents double-spend.
    - .planning/milestones/v25.0-phases/214-adversarial-audit/214-03-STATE-COMPOSITION.md (lines 1-80) — state-composition precedent for the v25.0 delta; structure for multi-path state-machine enumeration
  </read_first>
  <action>
Execute a fresh-from-source end-to-end state-machine audit (per D-03) of the combined earlybird flow spanning both `f20a2b5e` and `20a951df`. 231-01 and 231-02 verdicts are REGRESSION ANCHORS only — re-verify each cross-commit invariant independently.

**Step 1 — Build the state-machine scope from `230-01-DELTA-MAP.md` §4 Consumer Index EBD-03 row (D-04).** The scope is the cross-function chain:
  - Purchase-phase side (f20a2b5e): `_purchaseFor` / `_purchaseWhaleBundle` / `_purchaseLazyPass` / `_purchaseDeityPass` → `_awardEarlybirdDgnrs(buyer, wei)` (purchase-side allocations) → level progression → `_finalizeRngRequest` → `_finalizeEarlybird` (at `lvl == EARLYBIRD_END_LEVEL`, one-shot, flips sentinel, dumps pool to Lootbox via DGNRS)
  - Jackpot-phase side (20a951df): `advanceGame` jackpot-phase branches → `_consolidatePoolsAndRewardJackpots` / jackpot dispatch chain → `_runEarlyBirdLootboxJackpot(lvl, rngWord)` → `_rollWinningTraits(rngWord, true)` → ticket queue writes at `lvl` (resolution level)
  - Hand-off invariant: the pool consumed by `_runEarlyBirdLootboxJackpot` MUST be the same pool `_finalizeEarlybird` dumped into Lootbox (no drift between the two commits)

**Step 2 — Enumerate every reachable transition path through `DegenerusGameAdvanceModule.advanceGame` at HEAD** and classify each for earlybird interaction. The minimum path set per CONTEXT.md code_context (cross-commit state machine bullet) and Specific Ideas bullet 3 (normal / skip-split / gameover transitions):
  - **Normal path:** `advanceGame` → standard level progression → `_finalizeRngRequest` fires at `lvl == EARLYBIRD_END_LEVEL` once → `_finalizeEarlybird` flips sentinel + dumps pool → subsequent jackpot phase reaches `_runEarlyBirdLootboxJackpot` once with the correct `rngWord`
  - **Skip-split path:** the phase-transition branch where multiple stages pack into one call (especially relevant given 2471f8e7's `_unlockRng` removal packs housekeeping into the last jackpot physical day — this is the condition that increases the likelihood of the `_finalizeEarlybird` hook firing in the same tx as the jackpot-phase earlybird run). Trace: does the sentinel flip in `_finalizeEarlybird` correctly prevent re-entry if the same `advanceGame` tx subsequently reaches a second `_finalizeRngRequest` check? Does the pool-dump to Lootbox complete before the jackpot-phase `_runEarlyBirdLootboxJackpot` reads from the dumped pool?
  - **Gameover path:** if game-over triggers BEFORE `lvl == EARLYBIRD_END_LEVEL`, the earlybird finalize hook never fires — verify the game-over branch does NOT leak the remaining Earlybird pool (it stays in DGNRS until some other claim path drains it; confirm this is expected behavior or record as DEFER to Phase 236). If game-over triggers AFTER `lvl == EARLYBIRD_END_LEVEL`, the sentinel already flipped — confirm no second dump.

**Step 3 — For each path, check the 4 attack vectors from CONTEXT.md D-08 EBD-03:**
  - **(a) No double-spend across (purchase finalize) → (jackpot-phase run) handoff:** Verify the pool amount returned from `_finalizeEarlybird`'s `dgnrs.transferBetweenPools(...)` call lands in the Lootbox pool and that `_runEarlyBirdLootboxJackpot` consumes that exact Lootbox-side balance (or the downstream futurePool/nextPool derivative). The sentinel flip in `_finalizeEarlybird` + the early-return guard in `_awardEarlybirdDgnrs` must together prevent any post-finalize purchase path from re-allocating into the already-dumped pool.
  - **(b) No orphaned reserves when `_finalizeEarlybird` fires at `EARLYBIRD_END_LEVEL`:** Read the `_finalizeEarlybird` body — confirm the dump amount is the FULL remaining pool (via `dgnrs.poolBalance(...)` or equivalent), not a partial fraction. Any residual balance left behind by the dump would be an orphaned reserve.
  - **(c) No missed emissions across normal / skip-split / gameover:** For each enumerated path, verify whether `_finalizeEarlybird` fires when it should and does NOT fire when it should not. Specifically:
      - Normal: fires exactly once at `lvl == EARLYBIRD_END_LEVEL` (PASS expected)
      - Skip-split: if `advanceGame` packs multiple stages into one tx, `_finalizeRngRequest` is still called per RNG-request resolution — confirm the sentinel guard prevents a second fire in the same tx
      - Gameover before `EARLYBIRD_END_LEVEL`: never fires — confirm this is intended (the Earlybird pool stays unallocated in DGNRS; Phase 236 REG-01 may want to regression-check this against v25.0/v26.0/v27.0 assumptions)
      - Gameover at or after `EARLYBIRD_END_LEVEL`: sentinel already flipped; no re-fire
  - **(d) Cross-commit invariant (pool balance dumped = pool balance consumed):** This is the defining EBD-03 invariant. Verify:
      - `_finalizeEarlybird` dumps into the Lootbox pool (per §1.1 row semantics)
      - `_runEarlyBirdLootboxJackpot` reads from the jackpot-pool arithmetic chain that sources from Lootbox-side balance (trace the upstream `totalBudget` or pool-read — does it correspond to the Lootbox balance post-dump?). If not, record DEFER and HAND OFF to Phase 235 CONS-01 for algebraic closure.
      - There is no non-earlybird state-mutating call between `_finalizeEarlybird` and `_runEarlyBirdLootboxJackpot` that could drift the pool (per D-07 algebraic conservation is a Phase 235 hand-off — Phase 231 verifies absence of obvious intermediary mutation only).

**Step 4 — Write `231-03-AUDIT.md` as a numbered state-machine path walk** (per Claude's Discretion in CONTEXT.md: "ASCII state diagram vs. numbered path walk vs. table — numbered path walk chosen as most readable for cross-commit tracing"). Required structure, in this exact order:

```markdown
# EBD-03 Adversarial Audit: Combined Earlybird State Machine

**Scope:** Cross-commit end-to-end trace spanning `f20a2b5e` (purchase-phase finalize) + `20a951df` (jackpot-phase trait-alignment) — `230-01-DELTA-MAP.md` §1.1 IM-05 + §1.2 + §1.9 + §2.1 + §2.3 IM-16
**Method:** Fresh path-walk from HEAD source (per D-03) — 231-01 and 231-02 verdicts referenced as regression anchors only, never as pre-approval
**Scope source:** `230-01-DELTA-MAP.md` §4 Consumer Index row EBD-03 (per D-04)
**Finding-ID policy:** No `F-29-NN` IDs emitted (per D-09) — findings-candidate anchors only; Phase 236 FIND-01 owns ID assignment

## Methodology

[One paragraph: 4 attack vectors per CONTEXT.md D-08 EBD-03 (no double-spend, no orphaned reserves, no missed emissions, cross-commit invariant). Path enumeration over `DegenerusGameAdvanceModule.advanceGame` normal / skip-split / gameover branches. Locked verdict vocabulary for the Per-Path Verdict Block: PASS | FAIL | DEFER.]

## State-Machine Path Walk

### Path A: Normal Level Progression
**Entry:** `advanceGame` (normal branch — no skip-split, no gameover) at level `lvl = EARLYBIRD_END_LEVEL - 1`
**Hops (numbered):**
1. `advanceGame` (contracts/modules/DegenerusGameAdvanceModule.sol:<line>) — normal branch, rngGate cycle begins
2. `_finalizeRngRequest` (contracts/modules/DegenerusGameAdvanceModule.sol:<line>) — after `_rewardTopAffiliate(lvl)` and `level = lvl`, the `lvl == EARLYBIRD_END_LEVEL` check triggers
3. `_finalizeEarlybird` (contracts/modules/DegenerusGameAdvanceModule.sol:<line>) — sentinel check (first fire, poolStart != max), pool read via `dgnrs.poolBalance`, `dgnrs.transferBetweenPools(EARLYBIRD_POOL, LOOTBOX_POOL, amount)`, sentinel flip to `type(uint256).max`
4. Subsequent `_awardEarlybirdDgnrs` calls from in-flight purchases (if any) hit the early-return guard in Storage (contracts/storage/DegenerusGameStorage.sol:<line>) and no-op — no re-allocation into the now-dumped pool
5. `advanceGame` advances into jackpot phase on a later call; `_consolidatePoolsAndRewardJackpots` → jackpot chain → `_runEarlyBirdLootboxJackpot(lvl, rngWord)` (contracts/modules/DegenerusGameJackpotModule.sol:<line>) — consumes the Lootbox-pool-side balance that now includes the dumped Earlybird pool
6. `_rollWinningTraits(rngWord, true)` (contracts/modules/DegenerusGameJackpotModule.sol:<line>) → 4 × 25-winner queue at resolution level `lvl`
**Verdict:** [PASS/FAIL/DEFER with evidence]

### Path B: Skip-Split Transition
**Entry:** `advanceGame` branch where phase transition packs housekeeping into a single tx (relevant to 2471f8e7 context)
**Hops (numbered):**
[Enumerate each hop with File:Line anchor]
**Verdict:** [PASS/FAIL/DEFER with evidence — focus on the sentinel guard preventing double-fire within one tx]

### Path C: Game-Over Before `EARLYBIRD_END_LEVEL`
**Entry:** `advanceGame` branch reaching game-over at level `< EARLYBIRD_END_LEVEL`
**Hops (numbered):**
[Enumerate — confirm `_finalizeEarlybird` never fires]
**Verdict:** [PASS/FAIL/DEFER — is the unallocated Earlybird pool an orphaned reserve? Hand off to Phase 236 REG-01 if this behavior predates the delta]

### Path D: Game-Over At Or After `EARLYBIRD_END_LEVEL`
**Entry:** `advanceGame` branch reaching game-over at level `>= EARLYBIRD_END_LEVEL` (sentinel already flipped)
**Hops (numbered):**
[Enumerate — confirm no second dump, no orphaned reserves beyond what Path A already handled]
**Verdict:** [PASS/FAIL/DEFER]

## Per-Path Verdict Block

| Path | Attack Vector Considered | Verdict (PASS/FAIL/DEFER) | Evidence | Owning SHA |
|---|---|---|---|---|
| A: Normal | No double-spend across handoff | <verdict> | [evidence: sentinel flip sequence; Storage guard] | f20a2b5e, 20a951df |
| A: Normal | No orphaned reserves (full-pool dump) | <verdict> | [evidence: `dgnrs.poolBalance` → `transferBetweenPools` — full balance] | f20a2b5e |
| A: Normal | No missed emission (fires exactly once at EARLYBIRD_END_LEVEL) | <verdict> | [evidence: `_finalizeRngRequest` single call site to `_finalizeEarlybird`] | f20a2b5e |
| A: Normal | Cross-commit invariant (pool dumped == pool consumed) | <verdict> | [evidence: Lootbox pool flow into jackpot-phase `_runEarlyBirdLootboxJackpot` budget] | f20a2b5e, 20a951df |
| B: Skip-Split | No double-spend (sentinel prevents re-fire within single tx) | <verdict> | [evidence] | f20a2b5e |
| B: Skip-Split | No missed emission (finalize still reaches within packed tx) | <verdict> | [evidence] | f20a2b5e, 20a951df |
| C: Game-Over Before EBD-END | No missed emission (correctly skipped; orphaned pool is pre-existing behavior) | <verdict> | [evidence; may HAND OFF to Phase 236 REG-01] | f20a2b5e |
| D: Game-Over At/After EBD-END | No double-dump (sentinel already flipped) | <verdict> | [evidence] | f20a2b5e |

[Rows above are the MINIMUM set. Replace `<line>` with actual File:Line at HEAD. Additional rows allowed where warranted.]

## High-Risk Patterns Analyzed

### Cross-Commit Invariant (`_finalizeEarlybird` dump == `_runEarlyBirdLootboxJackpot` consumption)
[Prose analysis of the pool-balance hand-off between the two commits. Trace where the Lootbox-side balance lands in the jackpot-phase arithmetic that feeds `_runEarlyBirdLootboxJackpot`'s budget. If the trace requires algebraic closure, record as a Phase 235 CONS-01 HAND-OFF and provide the caller-side CEI verification only.]

### Sentinel One-Shot Semantics
[Prose: the `earlybirdDgnrsPoolStart == type(uint256).max` sentinel is the single cross-path guard preventing double-dump from `_finalizeEarlybird` AND double-allocation from in-flight `_awardEarlybirdDgnrs` calls. Verify the sentinel flip happens AFTER the `transferBetweenPools` succeeds (CEI) and BEFORE any subsequent purchase-side allocation can land.]

### Phase-Transition Interaction (Packed vs Unpacked advanceGame)
[Prose: the 2471f8e7 packing (separate phase — TRNX-01, OUT OF SCOPE here except for interaction) changed which `advanceGame` call hosts the housekeeping. In the packed tx, `_finalizeRngRequest` still fires at its normal CEI position. Verify no new reachable path where the finalize hook is skipped.]

## Scope-guard Deferrals

[Any combined-state-machine concern surfaced during this audit that is NOT anchored in `230-01-DELTA-MAP.md` §4 EBD-03 rows is recorded here per D-06. Phase 230 is READ-only. Each entry: concern, why not in EBD-03 scope, recommended receiving phase. If none, state "None surfaced during this audit."]

## Downstream Hand-offs

[Concerns overlapping downstream-phase territory (per D-07). Expected entries:
- Phase 235 CONS-01 hand-off: Algebraic sum-before = sum-after proof across the full (purchase finalize) → (jackpot-phase run) pool arithmetic. Phase 231 EBD-03 verified CEI position at each hop and sentinel integrity; Phase 235 owns algebraic closure.
- Phase 235 TRNX-01 hand-off: Interaction between the 2471f8e7 `_unlockRng` removal and the new `_finalizeEarlybird` hook placement — Phase 231 did not re-audit the `_unlockRng` invariant; Phase 235 owns.
- Phase 236 REG-01 hand-off: Behavior of the Earlybird pool in game-over-before-EBD-END paths — is the unallocated pool a pre-existing orphan or a new concern from the delta? Phase 236 REG-01 regression sweep may want to cross-check against v25.0 INFO findings.
- Phase 236 FIND-01 hand-off: Every FAIL / DEFER verdict row above becomes a finding-candidate anchor for Phase 236 severity classification.
Any additional hand-offs surfaced during audit recorded here.]
```

Every row in the Per-Path Verdict Block MUST:
- Use exactly the locked column set: `Path | Attack Vector Considered | Verdict (PASS/FAIL/DEFER) | Evidence | Owning SHA` (Note: the function column is replaced with Path for this plan per Claude's Discretion — 231-03 is a state-machine walk, not a per-function audit. Auto-rule 4 applies in spirit via the PASS/FAIL/DEFER + Evidence + Owning SHA requirements; the Path column substitutes for Function in the cross-function tracing context.)
- Cite at least one of `f20a2b5e` or `20a951df` (both for cross-commit invariant rows)
- Use real File:Line anchors in the hop descriptions (never placeholders)
- Never reference F-29-NN IDs (per D-09)

Commit contract reads ONLY from `contracts/` directory (per user feedback — never read stale copies). Read `231-01-AUDIT.md` and `231-02-AUDIT.md` as regression anchors only; do NOT copy their verdicts — re-verify each cross-commit invariant independently.
  </action>
  <verify>
    <automated>test -f .planning/phases/231-earlybird-jackpot-audit/231-03-AUDIT.md && grep -c "f20a2b5e\|20a951df" .planning/phases/231-earlybird-jackpot-audit/231-03-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/231-earlybird-jackpot-audit/231-03-AUDIT.md` exists
    - File contains the literal header string `## State-Machine Path Walk`
    - File contains the literal header string `## Per-Path Verdict Block`
    - File contains the literal header string `## Findings-Candidate Block`
    - File contains the literal header string `## Scope-guard Deferrals`
    - File contains the literal header string `## Downstream Hand-offs`
    - State-Machine Path Walk section contains at least four enumerated paths: `### Path A`, `### Path B`, `### Path C`, `### Path D` (or equivalent labels for Normal / Skip-Split / Game-Over Before / Game-Over At-or-After)
    - Per-Path Verdict Block header row is exactly `| Path | Attack Vector Considered | Verdict (PASS/FAIL/DEFER) | Evidence | Owning SHA |`
    - Every row in the Per-Path Verdict Block cites at least one of the 7-char SHAs `f20a2b5e` or `20a951df` (cross-commit invariant rows cite both, comma-separated)
    - Every row's verdict is exactly one of the strings `PASS`, `FAIL`, or `DEFER`
    - At least one row covers each of the 4 EBD-03 attack vectors: "double-spend", "orphaned reserves", "missed emission", "cross-commit invariant" (minimum 4 distinct rows)
    - `grep -c "PASS\|FAIL\|DEFER" .planning/phases/231-earlybird-jackpot-audit/231-03-AUDIT.md` returns at least 8 (at least one row per path × at least 2 attack vectors average)
    - `grep -c "f20a2b5e\|20a951df" .planning/phases/231-earlybird-jackpot-audit/231-03-AUDIT.md` returns at least 8 (one per verdict row minimum)
    - The string `F-29-NN` does NOT appear in the file (per D-09)
    - The string `F-29-` does NOT appear in any form in the file (Phase 236 owns ID assignment)
    - High-Risk Patterns Analyzed section contains at least three subsections (Cross-Commit Invariant, Sentinel One-Shot Semantics, Phase-Transition Interaction)
    - Downstream Hand-offs subsection explicitly names `Phase 235 CONS-01` (algebraic pool conservation hand-off)
    - Downstream Hand-offs subsection explicitly names `Phase 235 TRNX-01` (phase-transition interaction hand-off) OR explicitly states the interaction is confined to Phase 235 scope
    - Each path's hop list cites File:Line anchors starting with `contracts/modules/DegenerusGameAdvanceModule.sol:`, `contracts/modules/DegenerusGameJackpotModule.sol:`, or `contracts/storage/DegenerusGameStorage.sol:` — no external file references
    - No placeholder line numbers like `:<line>` remain in the final file — every File:Line is a real integer or integer range
    - 231-01 and 231-02 are referenced as regression anchors, not as pre-approval (look for language like "regression anchor" or cross-reference to "231-01-AUDIT.md" / "231-02-AUDIT.md" — but verdicts are independently established)
    - No contract source content read from outside `contracts/` directory (per user feedback)
  </acceptance_criteria>
  <done>`231-03-AUDIT.md` exists with numbered state-machine path walk across normal / skip-split / gameover transitions. Per-Path Verdict Block covers all 4 EBD-03 attack vectors from CONTEXT.md D-08. Every row cites at least one of `f20a2b5e` or `20a951df` and uses real File:Line anchors. 231-01 and 231-02 are treated as regression anchors per D-03. Findings-Candidate / Scope-guard Deferrals / Downstream Hand-offs subsections present — hand-offs route algebraic conservation to Phase 235 CONS-01 and phase-transition interaction to Phase 235 TRNX-01. No F-29-NN IDs emitted.</done>
</task>

</tasks>

<verification>
- `231-03-AUDIT.md` exists with numbered state-machine path walk
- At least 4 paths enumerated (Normal / Skip-Split / Game-Over Before / Game-Over At-or-After)
- Per-Path Verdict Block uses PASS/FAIL/DEFER vocabulary
- Every verdict cites at least one of `f20a2b5e` or `20a951df`
- Every File:Line anchor resolves into `contracts/modules/DegenerusGameAdvanceModule.sol`, `contracts/modules/DegenerusGameJackpotModule.sol`, or `contracts/storage/DegenerusGameStorage.sol` at HEAD
- All 4 attack vectors from CONTEXT.md D-08 EBD-03 covered
- 231-01 and 231-02 verdicts treated as regression anchors, not pre-approval (D-03)
- Findings-Candidate / Scope-guard Deferrals / Downstream Hand-offs subsections present
- Hand-offs explicitly name Phase 235 CONS-01 and Phase 235 TRNX-01
- Zero `F-29-NN` references anywhere in the file
</verification>

<success_criteria>
The combined earlybird state machine is traced end-to-end across every reachable transition path in `advanceGame` (normal / skip-split / gameover). The cross-commit invariant (pool dumped by `_finalizeEarlybird` equals pool consumed by `_runEarlyBirdLootboxJackpot`) has an explicit verdict. No double-spend, no orphaned reserve, no missed emission surfaces with actionable evidence. FAIL/DEFER verdicts captured as finding-candidate anchors for Phase 236 FIND-01. Algebraic pool conservation handed off to Phase 235 CONS-01; phase-transition interaction handed off to Phase 235 TRNX-01. 231-01 and 231-02 are regression anchors only — this plan re-verifies the cross-commit hand-off independently.
</success_criteria>

<output>
After completion, create `.planning/phases/231-earlybird-jackpot-audit/231-03-SUMMARY.md`
</output>
