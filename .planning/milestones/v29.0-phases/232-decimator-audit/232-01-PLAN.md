---
phase: 232-decimator-audit
plan: 232-01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/232-decimator-audit/232-01-AUDIT.md
autonomous: true
requirements: [DCM-01]
must_haves:
  truths:
    - "Every function listed in 230-01-DELTA-MAP.md §1.3 / §1.1 / §1.8 owned by commit 3ad0f8d3 has a verdict row in the per-function table"
    - "Every attack vector in CONTEXT.md D-06 (pro-rata share off-by-one under new lvl+1 keying; read/write key alignment across every decBurns / decBurnBuckets / decPool consumer) plus D-07 (consolidated jackpot-block mutual exclusivity, deterministic decPoolWei zero when neither x00 nor x5 condition holds, runDecimatorJackpot self-call args + CEI preserved) appears as at least one verdict row"
    - "Every verdict row cites the 7-char commit SHA `3ad0f8d3` (per D-03)"
    - "Every verdict row cites a File:Line anchor pointing into contracts/modules/DegenerusGameDecimatorModule.sol, contracts/modules/DegenerusGameAdvanceModule.sol, or contracts/BurnieCoin.sol at HEAD"
    - "Every read site of `decBurns`, `decBurnBuckets`, `decPool` inside DegenerusGameDecimatorModule.sol is enumerated and verified to use the same `lvl` key-space as the writer (BurnieCoin.decimatorBurn `lvl = degenerusGame.level() + 1`)"
    - "Verdict vocabulary used is exactly `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (per D-02)"
    - "No F-29-NN finding IDs appear anywhere in 232-01-AUDIT.md (per D-13)"
    - "BurnieCoin sum-in / sum-out conservation is NOT attempted — it is a Phase 235 CONS-02 hand-off (per D-14)"
    - "Findings-Candidate Block, Scope-guard Deferrals, and Downstream Hand-offs subsections present; Phase 235 CONS-02 hand-off explicitly named"
    - "Every verdict row carries a `Finding Candidate: Y/N` column value (per D-13)"
    - "Zero contracts/ and zero test/ writes occur during plan execution (READ-only per v29.0 milestone rule)"
  artifacts:
    - path: ".planning/phases/232-decimator-audit/232-01-AUDIT.md"
      provides: "DCM-01 burn-key-by-resolution-level refactor adversarial audit"
      contains: "## Per-Function Verdict Table"
  key_links:
    - from: "230-01-DELTA-MAP.md §1.3 / §1.1 / §1.8 / §2.2 IM-06..IM-09 rows (3ad0f8d3)"
      to: "232-01-AUDIT.md per-function verdict table rows"
      via: "Every 3ad0f8d3 function in the delta map has a verdict"
      pattern: "SAFE|SAFE-INFO|VULNERABLE|DEFERRED"
    - from: "232-01-AUDIT.md VULNERABLE/DEFERRED verdicts"
      to: "Phase 236 FIND-01 finding-candidate pool"
      via: "Findings-Candidate Block (no F-29-NN IDs, Finding Candidate: Y/N column)"
      pattern: "## Findings-Candidate Block"
    - from: "232-01-AUDIT.md BurnieCoin burn-key rows"
      to: "Phase 235 CONS-02 BURNIE sum-in/sum-out proof"
      via: "Downstream Hand-offs subsection (algebraic closure deferred)"
      pattern: "Phase 235 CONS-02"
---

<objective>
DCM-01 adversarial audit of the decimator burn-key refactor owned by commit `3ad0f8d3` ("fix(decimator): key burns by resolution level, consolidate jackpot block"). Produces `232-01-AUDIT.md`: a per-function verdict table covering every function touched by `3ad0f8d3` across the Decimator module, the AdvanceModule consolidated jackpot block, and BurnieCoin burn-key plumbing, plus Findings-Candidate / Scope-guard Deferrals / Downstream Hand-offs subsections.

Purpose: Burns previously keyed by `uint24 lvl = degenerusGame.level();` now key by `uint24 lvl = degenerusGame.level() + 1;` so a burn during in-flight window level N lands in `decBurn[N+1]`, matching the jackpot-resolution read side which runs after the N→N+1 level bump. A C4A warden would probe: (a) pro-rata share off-by-one under the new keying — does every reader inside `DegenerusGameDecimatorModule.sol` (`recordDecBurn`, `runDecimatorJackpot`, `consumeDecClaim`, `claimDecimatorJackpot`, `decClaimable`) read the same `lvl+1`-space bucket the writer wrote into?, (b) consolidated jackpot-block ordering in `_consolidatePoolsAndRewardJackpots` — the previously-separate `prevMod100 == 0` (x00) and `prevMod10 == 5 && prevMod100 != 95` (x5) branches now share a single tail; are the triggers mutually exclusive?, (c) `decPoolWei` determinism — is it guaranteed zero when neither x00 nor x5 fires, so the shared `runDecimatorJackpot` self-call is skipped?, (d) DECIMATOR_MIN_BUCKET_100 reachability — the new keying activates the `lvl % 100 == 0` check at L100 (previously dead code under `level()=99`), (e) BurnieCoin caller-side math on the returned `degenerusGame.level()` value (IM-09 — call itself unchanged per Phase 230 Known Non-Issue #3). This plan surfaces adversarial verdicts against the D-02 column schema using the D-02 verdict vocabulary. Phase 236 FIND-01 owns canonical F-29-NN ID assignment (D-13). BurnieCoin sum-in / sum-out conservation is HANDED OFF to Phase 235 CONS-02 per D-14 — this plan scopes BurnieCoin work to burn-key correctness only (right key, right level, no stuck state).

Output: `232-01-AUDIT.md` with per-function verdict table + Findings-Candidate Block + Scope-guard Deferrals + Downstream Hand-offs subsections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/232-decimator-audit/232-CONTEXT.md
@.planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md
@.planning/phases/230-delta-extraction-scope-map/230-01-SUMMARY.md
@.planning/milestones/v25.0-phases/214-adversarial-audit/214-01-REENTRANCY-CEI.md
@.planning/phases/231-earlybird-jackpot-audit/231-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract 3ad0f8d3 diff + read target source at HEAD for every DCM-01 function; write 232-01-AUDIT.md</name>
  <files>.planning/phases/232-decimator-audit/232-01-AUDIT.md</files>
  <read_first>
    - .planning/phases/232-decimator-audit/232-CONTEXT.md (D-01 through D-14 — locked column schema D-02, SHA-citation rule D-03, scope anchor D-04, scope-guard deferral rule D-05, burn-key audit focus D-06/D-07, finding-ID discipline D-13, BurnieCoin scope boundary D-14)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.3 (lines 107-120 — DegenerusGameDecimatorModule.sol surface: `claimDecimatorJackpot` ID-89 body MODIFIED by 67031e7d; `claimTerminalDecimatorJackpot` ID-93 body MODIFIED by 67031e7d; note that §1.3's Function-Level Changelog row set for DecimatorModule is the 67031e7d event surface — the DCM-01 `3ad0f8d3` surface for this module is the body-level read-site set that consumes `decBurns`/`decBurnBuckets`/`decPool`, which are all defined at the storage level and consumed in-module; plan must enumerate by grep against `contracts/modules/DegenerusGameDecimatorModule.sol` at HEAD)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.1 — row for `_consolidatePoolsAndRewardJackpots` MODIFIED by `3ad0f8d3` (line 81: "Consolidates the x00 and x5 decimator branches into a single tail body guarded by `decPoolWei != 0`. Mutually exclusive triggers preserved; no flow change, but the shared tail eliminates duplicate `runDecimatorJackpot` self-call + bookkeeping.")
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §1.8 — row for `BurnieCoin.decimatorBurn` MODIFIED by `3ad0f8d3` (lines 167-175: "Local `uint24 lvl = degenerusGame.level();` replaced with `uint24 lvl = degenerusGame.level() + 1;` so burns during window level N land in `decBurn[N+1]`, matching the jackpot-resolution read side (which runs after the N→N+1 bump). Two new comment lines document the rationale. Side effect: DECIMATOR_MIN_BUCKET_100 now activates at the L100 jackpot (the previous keying made the `lvl % 100 == 0` check dead code against `level()=99`).")
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §2.2 rows IM-06, IM-07, IM-09 (lines 248-253 — decimator-related cross-module chains owned by 3ad0f8d3; IM-06 `_consolidatePoolsAndRewardJackpots → runDecimatorJackpot` self-call; IM-07 `Game.runDecimatorJackpot → DecimatorModule.runDecimatorJackpot` delegatecall chain; IM-09 `BurnieCoin.decimatorBurn → DegenerusGame.level()` direct external call — Phase 230 Known Non-Issue #3 scopes this to caller's use of returned value)
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md §4 Consumer Index DCM-01 row (line 552 — authoritative scope anchor per D-04: "§1.3 (entire file — Decimator module surface), §1.1 (`_consolidatePoolsAndRewardJackpots` MODIFIED by 3ad0f8d3), §1.8 (`BurnieCoin.decimatorBurn` MODIFIED by 3ad0f8d3), §2.2 (IM-06, IM-07, IM-09), commit `3ad0f8d3` rows")
    - .planning/phases/230-delta-extraction-scope-map/230-01-SUMMARY.md "Known Non-Issues" — specifically #3 (IM-09 "call site itself unchanged (`degenerusGame.level()`). Caller body MODIFIED to compute `lvl = degenerusGame.level() + 1` post-call" — explicitly scoped to "inspect the caller's use of the returned value, not the call itself")
    - Output of `git show 3ad0f8d3 --stat` and `git show 3ad0f8d3 -- contracts/BurnieCoin.sol contracts/modules/DegenerusGameDecimatorModule.sol contracts/modules/DegenerusGameAdvanceModule.sol` to inspect the full authored diff — the delta IS the audit surface. Pre-fix source (`3ad0f8d3^`) shows the `degenerusGame.level()` (no `+ 1`) keying and the two separate x00 / x5 branches in AdvanceModule; HEAD shows `+ 1` and the consolidated tail.
    - contracts/BurnieCoin.sol at HEAD — locate `decimatorBurn(address player, uint256 amount)`. Record (a) the exact line containing `uint24 lvl = degenerusGame.level() + 1;`, (b) the subsequent SSTORE / write path into `decBurn` and any bucket state via the `DegenerusGame` self-call (specifically the `degenerusGame.recordDecBurn(player, lvl, bucket, baseAmount, multBps)` call — cite its File:Line), (c) the keccak / mapping-key computation so the write key is unambiguous.
    - contracts/modules/DegenerusGameDecimatorModule.sol at HEAD — enumerate EVERY function body that reads or writes `decBurns`, `decBurnBuckets`, `decBurn`, `decPool`, `decPoolWei`, `decClaimable`, or any `uint24 lvl` argument sourced from `degenerusGame.level()` arithmetic. Expected target function set (cross-check with §3.3.d rows ID-86..ID-94 which are ALL PASS at HEAD for interface drift — their bodies are the in-scope DCM-01 audit surface):
        * `recordDecBurn(address player, uint24 lvl, uint8 bucket, uint256 baseAmount, uint256 multBps) returns (uint8 bucketUsed)` at line 133 — writer (called by BurnieCoin.decimatorBurn via the Game wrapper IM-07)
        * `runDecimatorJackpot(uint256 poolWei, uint24 lvl, uint256 rngWord) returns (uint256 returnAmountWei)` at line 209 — reader (lvl is the jackpot-resolution level passed in by `_consolidatePoolsAndRewardJackpots`)
        * `consumeDecClaim(address player, uint24 lvl) returns (uint256 amountWei)` at line 306 — reader
        * `claimDecimatorJackpot(uint24 lvl)` at line 321 — reader (caller-supplied `lvl`; plan audits whether caller passes post-resolution `level()+1` or in-flight `level()`)
        * `decClaimable(address player, uint24 lvl) view returns (uint256 amountWei, bool winner)` at line 358 — reader
        * `recordTerminalDecBurn(address player, uint24 lvl, uint256 baseAmount)` at line 668 — writer (terminal path; record whether it also shifted to `+1` keying or keeps legacy keying)
        * `runTerminalDecimatorJackpot(uint256 poolWei, uint24 lvl, uint256 rngWord) returns (uint256 returnAmountWei)` at line 755 — reader (terminal path)
        * `claimTerminalDecimatorJackpot()` at line 811 — reader (arg-less, reads `lastTerminalDecClaimRound.lvl` from storage; DCM-02 owns the event side; DCM-01 audits whether the `lvl` used for pro-rata share calc matches the terminal burn-key space)
        * `terminalDecClaimable(address player) view returns (uint256 amountWei, bool winner)` at line 826 — reader (terminal path)
      For each, record the exact File:Line of every read/write of `decBurns`/`decBurnBuckets`/`decPool` / `decClaim` and whether the `lvl` used is post-resolution (matches write-side `level()+1` space) or in-flight.
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD — locate `_consolidatePoolsAndRewardJackpots`. Record: (a) exact File:Line of the x00 branch entry (`prevMod100 == 0`), (b) exact File:Line of the x5 branch entry (`prevMod10 == 5 && prevMod100 != 95`), (c) how `decPoolWei` is assigned inside each branch, (d) exact File:Line of the `if (decPoolWei != 0) { runDecimatorJackpot(...) }` tail call, (e) the `runDecimatorJackpot` self-call arg list (must be `(poolWei = decPoolWei, lvl = ???, rngWord = ???)` — record the exact expressions so reachability and arg correctness are verifiable). Verify the two branches are mutually exclusive (x00 implies prevMod100 == 0 which implies prevMod10 == 0, which excludes prevMod10 == 5; x5 explicitly excludes prevMod100 == 95 via `&& prevMod100 != 95` — but note x5 does NOT exclude prevMod100 == 0 since mod10==5 excludes mod100==0 by arithmetic; however the x5 guard `prevMod10 == 5` is what makes the branches disjoint because a value with mod10==0 cannot have mod10==5). Record the disjointness proof verbatim.
    - contracts/modules/DegenerusGameAdvanceModule.sol at HEAD — also locate any caller that invokes `_consolidatePoolsAndRewardJackpots` to cite the `prevMod100` / `prevMod10` derivation path (ultimately sourced from `(lvl - 1) % 100` / `(lvl - 1) % 10` or similar — verify the "previous" interpretation matches the keying commentary in §1.8 rationale).
    - .planning/milestones/v25.0-phases/214-adversarial-audit/214-01-REENTRANCY-CEI.md (lines 1-60 — precedent table shape and methodology preamble; per D-03 do NOT carry forward v25.0 verdicts as pre-approved; re-audit every function body as if no prior work exists)
    - .planning/phases/231-earlybird-jackpot-audit/231-01-PLAN.md (precedent PLAN shape — column schema, read_first pattern, acceptance_criteria format, output section — mirror this structure for 232-01)
  </read_first>
  <action>
Execute a fresh-from-source adversarial audit of the `3ad0f8d3` decimator burn-key refactor per CONTEXT.md D-01 through D-14. Verdict vocabulary per D-02 is strictly `SAFE | SAFE-INFO | VULNERABLE | DEFERRED`. Column schema per D-02 is strictly `Function | File:Line | Attack Vector | Verdict | Evidence | SHA` with an additional `Finding Candidate: Y/N` column per D-13.

**Step 1 — Build the scope list from `230-01-DELTA-MAP.md` §4 Consumer Index DCM-01 row (D-04).** The target function set is:
  - **`BurnieCoin.decimatorBurn`** (MODIFIED by `3ad0f8d3`) — §1.8 — the WRITE-key producer
  - **`DegenerusGameAdvanceModule._consolidatePoolsAndRewardJackpots`** (MODIFIED by `3ad0f8d3`) — §1.1 — the consolidated-block scheduler for the in-flight decimator jackpot
  - **`DegenerusGameDecimatorModule.recordDecBurn`** — writer callee (IM-07 chain from Game wrapper delegatecall); audits whether the write uses the `+1` key-space
  - **`DegenerusGameDecimatorModule.runDecimatorJackpot`** — primary reader (resolution-level pro-rata math); audits key-space match and off-by-one
  - **`DegenerusGameDecimatorModule.consumeDecClaim`** — reader (claim consumption); audits key alignment
  - **`DegenerusGameDecimatorModule.claimDecimatorJackpot`** — reader (user-facing claim); audits caller-supplied `lvl` key alignment
  - **`DegenerusGameDecimatorModule.decClaimable`** — reader (view); audits key alignment
  - **`DegenerusGameDecimatorModule.recordTerminalDecBurn`** — terminal writer; audits whether terminal path shifted to `+1` keying or intentionally kept legacy keying (scope-guard if unclear)
  - **`DegenerusGameDecimatorModule.runTerminalDecimatorJackpot`** — terminal reader; audits terminal key alignment
  - **`DegenerusGameDecimatorModule.claimTerminalDecimatorJackpot`** — terminal reader (DCM-02 owns event side; DCM-01 owns pro-rata share math side)
  - **`DegenerusGameDecimatorModule.terminalDecClaimable`** — terminal view; audits terminal key alignment

**Step 2 — For each target function, run the attack vectors from CONTEXT.md D-06 and D-07:**
  - **(a) Pro-rata share off-by-one under the new `lvl+1` keying (D-06):** For each READER (`runDecimatorJackpot`, `consumeDecClaim`, `claimDecimatorJackpot`, `decClaimable`, terminal counterparts), identify the exact `lvl` expression used to index into `decBurn` / `decBurnBuckets` / `decPool` / `decClaim`. Verify this `lvl` corresponds to the post-resolution level (matching the writer's `level() + 1` convention at §1.8). For each WRITER (`recordDecBurn`, `recordTerminalDecBurn`), verify the `lvl` parameter arrives from a caller that sourced `level() + 1`. Record verdict per function × attack-vector combination with File:Line.
  - **(b) Read/write key-space alignment across EVERY consumer of decimator state (D-06):** Grep-enumerate every access to `decBurns` / `decBurnBuckets` / `decPool` / `decClaim` / `decBurn` / `decClaimable` inside `contracts/modules/DegenerusGameDecimatorModule.sol` at HEAD. For each access, classify READ vs WRITE, extract the `lvl` key expression, and verify it is in the same key-space as the BurnieCoin writer's `level() + 1`. Any reader that uses `level()` (no `+1`) or caller-supplied `lvl` that could be pre-resolution is a finding candidate. Record.
  - **(c) Consolidated jackpot-block mutual exclusivity (D-07):** Verify the x00 branch (`prevMod100 == 0`) and x5 branch (`prevMod10 == 5 && prevMod100 != 95`) are mutually exclusive for every value of `prevMod100` ∈ [0, 99]. Proof sketch: `prevMod100 == 0 ⇒ prevMod10 == 0 ≠ 5`, so the x5 branch's `prevMod10 == 5` guard fails; conversely `prevMod10 == 5 ⇒ prevMod100 ∈ {5, 15, 25, 35, 45, 55, 65, 75, 85, 95}`, none of which equal 0. So the two branches cannot both enter. Record verdict with File:Line of each branch entry and the guard expression. Note that the `&& prevMod100 != 95` carve-out on the x5 branch is to exclude L95 (reserved for terminal decimator or L100 staging — verify which by reading comments / context).
  - **(d) `decPoolWei` determinism when neither x00 nor x5 fires (D-07):** Verify that outside both branches, `decPoolWei` holds a value that guarantees `if (decPoolWei != 0)` evaluates false so `runDecimatorJackpot` is skipped. This requires either (i) `decPoolWei` is initialized to 0 at function entry and only written inside the two branches, OR (ii) some other deterministic guard. Read the function body to confirm. Record verdict with File:Line of `decPoolWei`'s declaration / initial assignment.
  - **(e) `runDecimatorJackpot` self-call args + CEI preserved (D-07):** Per §1.1 row note "Selector, args, and ordering unchanged" — verify the self-call in the shared tail passes the same arg list (`poolWei`, `lvl`, `rngWord`) as the pre-fix branches did. Read `3ad0f8d3^:contracts/modules/DegenerusGameAdvanceModule.sol` to extract the pre-fix arg list and compare. Verify CEI: the self-call via `IDegenerusGame(address(this)).runDecimatorJackpot(...)` is a delegatecall through the Game wrapper (IM-07) — record any state mutation that happens AFTER the self-call in the shared tail.
  - **(f) DECIMATOR_MIN_BUCKET_100 reachability side-effect (§1.8 rationale):** Per §1.8 note "Side effect: DECIMATOR_MIN_BUCKET_100 now activates at the L100 jackpot (the previous keying made the `lvl % 100 == 0` check dead code against `level()=99`)." Locate the `DECIMATOR_MIN_BUCKET_100` constant and every consumer. Verify the new `+1` keying correctly enables this branch at the L100 jackpot boundary. If the new branch has never been exercised pre-fix (because it was dead), record as SAFE-INFO with evidence of the now-live path — this is a behavior expansion worth surfacing to Phase 236 FIND-01 as a Candidate: Y row.
  - **(g) BurnieCoin caller-side math on `degenerusGame.level()` return value (IM-09, Known Non-Issue #3):** Verify the BurnieCoin body computes `lvl = degenerusGame.level() + 1` correctly and uses `lvl` consistently throughout (no mixed `level()` + `lvl` references that could re-introduce pre-fix behavior). Grep for `degenerusGame.level()` inside `contracts/BurnieCoin.sol` to ensure only the intended call site exists. Per D-14, do NOT attempt BurnieCoin sum-in/sum-out accounting closure — record the burn-key correctness verdict only, and HAND OFF accounting to Phase 235 CONS-02 in the Downstream Hand-offs subsection.

**Step 3 — Write `232-01-AUDIT.md` mirroring the v25.0 Phase 214 precedent shape AND the 231-01-PLAN precedent structure.** Required structure, in this exact order:

```markdown
# DCM-01 Adversarial Audit: Decimator Burn-Key Refactor

**Scope:** Commit `3ad0f8d3` ("fix(decimator): key burns by resolution level, consolidate jackpot block") — all functions touched across §1.3 (DecimatorModule body-level read/write sites), §1.1 (`_consolidatePoolsAndRewardJackpots` consolidated block), and §1.8 (`BurnieCoin.decimatorBurn` burn-key plumbing) of `230-01-DELTA-MAP.md`
**Method:** Fresh read from HEAD source (per D-03) — no reuse of v25.0 Phase 214 or v27.0 Phase 223 verdicts as pre-approved; every read-site of decimator state enumerated by grep against `contracts/modules/DegenerusGameDecimatorModule.sol` at HEAD
**Scope source:** `230-01-DELTA-MAP.md` §4 Consumer Index row DCM-01 (per D-04)
**Verdict vocabulary:** `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (per D-02)
**Finding-ID policy:** No `F-29-NN` IDs emitted (per D-13) — `Finding Candidate: Y/N` column only; Phase 236 FIND-01 owns canonical ID assignment
**BurnieCoin scope boundary:** Burn-key correctness only (right key, right level, no stuck state). Sum-in / sum-out conservation is HANDED OFF to Phase 235 CONS-02 (per D-14).

## Methodology

[One paragraph: attack vectors per CONTEXT.md D-06 (pro-rata off-by-one; read/write key-space alignment across every consumer) and D-07 (consolidated-block mutual exclusivity; decPoolWei determinism; runDecimatorJackpot self-call args + CEI). Locked verdict vocabulary. Locked columns per D-02 plus Finding Candidate column per D-13.]

## Findings-Candidate Block

[Prose free-form (no F-29-NN IDs). One block per VULNERABLE or DEFERRED verdict identified in the table below, plus any SAFE-INFO row marked Finding Candidate: Y. Each block: title, verdict, File:Line, owning SHA, attack vector, evidence excerpt, recommended Phase 236 severity-classification note. If zero VULNERABLE/DEFERRED and zero SAFE-INFO Finding Candidates, state "No candidate findings — all verdicts SAFE against 3ad0f8d3 surface."]

## Per-Function Verdict Table

| Function | File:Line | Attack Vector | Verdict | Evidence | SHA | Finding Candidate |
|---|---|---|---|---|---|---|
| `BurnieCoin.decimatorBurn` | contracts/BurnieCoin.sol:<line> | Burn-key correctness (`lvl = degenerusGame.level() + 1` computed once, used consistently) | SAFE | [evidence: single call site + consistent use; no mixed level()/lvl references in body] | 3ad0f8d3 | N |
| `BurnieCoin.decimatorBurn` | contracts/BurnieCoin.sol:<line> | DECIMATOR_MIN_BUCKET_100 reachability side-effect (L100 boundary now live) | SAFE-INFO | [evidence: constant definition + consumer; pre-fix was dead branch at level()=99] | 3ad0f8d3 | Y |
| `DegenerusGameAdvanceModule._consolidatePoolsAndRewardJackpots` | contracts/modules/DegenerusGameAdvanceModule.sol:<line> | x00 / x5 branch mutual exclusivity (prevMod10 arithmetic proof) | SAFE | [evidence: x00 entry File:Line; x5 entry File:Line; disjointness proof: prevMod100==0 ⇒ prevMod10==0≠5] | 3ad0f8d3 | N |
| `DegenerusGameAdvanceModule._consolidatePoolsAndRewardJackpots` | contracts/modules/DegenerusGameAdvanceModule.sol:<line> | `decPoolWei` determinism outside both branches (guaranteed zero → tail skipped) | <verdict> | [evidence: `decPoolWei` init + branch-only writes] | 3ad0f8d3 | <Y/N> |
| `DegenerusGameAdvanceModule._consolidatePoolsAndRewardJackpots` | contracts/modules/DegenerusGameAdvanceModule.sol:<line> | `runDecimatorJackpot` self-call args + CEI preserved vs. pre-fix | <verdict> | [evidence: HEAD arg list vs. `3ad0f8d3^` arg list; ordering] | 3ad0f8d3 | <Y/N> |
| `DegenerusGameDecimatorModule.recordDecBurn` | contracts/modules/DegenerusGameDecimatorModule.sol:<line> | Caller-supplied `lvl` arrives from `level()+1` key-space (writer side) | <verdict> | [evidence: caller chain IM-07 from BurnieCoin → Game.recordDecBurn wrapper → module] | 3ad0f8d3 | <Y/N> |
| `DegenerusGameDecimatorModule.runDecimatorJackpot` | contracts/modules/DegenerusGameDecimatorModule.sol:<line> | Pro-rata share math uses same `lvl` key-space as writer | <verdict> | [evidence: `lvl` expression in every decBurns/decBurnBuckets/decPool read] | 3ad0f8d3 | <Y/N> |
| `DegenerusGameDecimatorModule.consumeDecClaim` | contracts/modules/DegenerusGameDecimatorModule.sol:<line> | Caller-supplied `lvl` key alignment on consumption | <verdict> | [evidence] | 3ad0f8d3 | <Y/N> |
| `DegenerusGameDecimatorModule.claimDecimatorJackpot` | contracts/modules/DegenerusGameDecimatorModule.sol:<line> | User-facing claim `lvl` arg key alignment (players pass resolution level, not in-flight) | <verdict> | [evidence: NatSpec / comment guidance + read sites] | 3ad0f8d3 | <Y/N> |
| `DegenerusGameDecimatorModule.decClaimable` | contracts/modules/DegenerusGameDecimatorModule.sol:<line> | View-function `lvl` key alignment | <verdict> | [evidence] | 3ad0f8d3 | <Y/N> |
| `DegenerusGameDecimatorModule.recordTerminalDecBurn` | contracts/modules/DegenerusGameDecimatorModule.sol:<line> | Terminal write path keying (`+1` adoption or legacy keying retained — which?) | <verdict> | [evidence: body inspection at line 668] | 3ad0f8d3 | <Y/N> |
| `DegenerusGameDecimatorModule.runTerminalDecimatorJackpot` | contracts/modules/DegenerusGameDecimatorModule.sol:<line> | Terminal reader `lvl` key alignment with terminal writer | <verdict> | [evidence] | 3ad0f8d3 | <Y/N> |
| `DegenerusGameDecimatorModule.claimTerminalDecimatorJackpot` | contracts/modules/DegenerusGameDecimatorModule.sol:<line> | Pro-rata share math for terminal claim (DCM-02 owns event emission side) | <verdict> | [evidence: `lastTerminalDecClaimRound.lvl` read vs. terminal writer key-space] | 3ad0f8d3 | <Y/N> |
| `DegenerusGameDecimatorModule.terminalDecClaimable` | contracts/modules/DegenerusGameDecimatorModule.sol:<line> | Terminal view-function key alignment | <verdict> | [evidence] | 3ad0f8d3 | <Y/N> |

[Rows above are the MINIMUM set. Replace `<line>` with actual File:Line at HEAD. Additional rows allowed per (function × distinct attack vector) — specifically when a single function has multiple read sites at distinct File:Lines and their key-space correctness must be verified separately, each gets its own row.]

## High-Risk Patterns Analyzed

### Burn-Key Space Alignment (BurnieCoin → recordDecBurn → jackpot readers)
[Prose analysis: WRITE-side key `level() + 1` in BurnieCoin.decimatorBurn (§1.8) → `recordDecBurn(lvl = level()+1, ...)` → decBurns[lvl]/decBurnBuckets[lvl] SSTORE → jackpot resolution at level `lvl` reads decBurns[lvl] in `runDecimatorJackpot`. Trace every read-side `lvl` expression to confirm same key-space. Cite File:Line at each hop.]

### Consolidated Jackpot-Block Disjointness
[Prose proof: for all prevMod100 ∈ [0, 99]: if prevMod100 == 0 then prevMod10 == 0 ≠ 5, so x5 guard fails; if prevMod10 == 5 then prevMod100 ∈ {5,15,25,35,45,55,65,75,85,95}, so prevMod100 ≠ 0 and x00 guard fails. Branches are mutually exclusive. `prevMod100 != 95` carve-out on x5 excludes L95 for reasons cited from in-source comment / context.]

### `decPoolWei` Determinism and Tail Reachability
[Prose: `decPoolWei` declared and initialized at function entry; only written inside the two branches; `if (decPoolWei != 0)` tail is entered iff at least one branch fired. Cite File:Line of declaration + every write site.]

### DECIMATOR_MIN_BUCKET_100 — Newly Live Branch
[Prose: pre-fix `lvl % 100 == 0` check against `level()=99` could never fire (dead code). Post-fix `lvl = level()+1` activates at L99 boundary → L100 jackpot. Cite the constant definition and every consumer. This is a behavior expansion (dead-code revival) that was the express intent of 3ad0f8d3 per its commit message — recorded as SAFE-INFO with Finding Candidate: Y so Phase 236 FIND-01 can consider whether to surface as an INFO-grade observation.]

## Scope-guard Deferrals

[Any decimator-adjacent concern surfaced during this audit that is NOT anchored in `230-01-DELTA-MAP.md` §4 DCM-01 rows is recorded here per D-05. Phase 230 is READ-only. Each entry: concern, why not in DCM-01 scope, recommended receiving phase. If none, state "None surfaced during this audit."]

## Downstream Hand-offs

[Concerns overlapping downstream-phase territory (per D-14 + general scope boundaries). Expected entries:
- **Phase 235 CONS-02 hand-off (D-14):** BurnieCoin sum-in / sum-out conservation proof across `decimatorBurn` + quest changes. Phase 232 DCM-01 verified burn-key correctness only (right key, right level, no stuck state); Phase 235 CONS-02 owns the algebraic mint/burn accounting closure.
- **Phase 235 RNG-01 hand-off (if applicable):** If any read-site of `decimatorBurn`-derived state feeds RNG-consuming logic, flag for Phase 235 RNG-01 backward-trace. Expected: N/A for DCM-01 — decimator burn-key plumbing does not participate in RNG consumption. Confirm and record "N/A — no RNG consumer touched by 3ad0f8d3".
- **Phase 236 FIND-01 hand-off:** Every VULNERABLE / DEFERRED row + every SAFE-INFO row with `Finding Candidate: Y` becomes a finding-candidate anchor for Phase 236 severity classification.
- **Phase 236 REG-01 hand-off:** If the new `+1` keying interacts with any v27.0 INFO finding or KNOWN-ISSUES entry, flag for regression cross-check.
Any additional hand-offs surfaced during audit recorded here.]
```

Every verdict row MUST:
- Use exactly the locked column set per D-02 + Finding Candidate column per D-13: `Function | File:Line | Attack Vector | Verdict | Evidence | SHA | Finding Candidate`
- Cite the 7-char SHA `3ad0f8d3`
- Use a real File:Line anchor from HEAD source (never a placeholder like `<line>`)
- Use verdict vocabulary strictly from `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (per D-02)
- Never reference F-29-NN IDs (per D-13)
- Carry a `Y` or `N` in Finding Candidate column (per D-13)

Read contract sources ONLY from `contracts/` directory at HEAD (per user memory `feedback_contract_locations.md` — never read stale copies). Use `git show 3ad0f8d3^:contracts/...` for pre-fix comparisons (git-tree read of the same repo at a prior SHA — NOT a stale copy).

READ-only constraint (v29.0 milestone rule): Zero writes to `contracts/` or `test/` permitted. Only writes allowed during task execution are `.planning/phases/232-decimator-audit/232-01-AUDIT.md` itself. `git status --porcelain contracts/ test/` MUST be empty before and after task execution.
  </action>
  <verify>
    <automated>test -f .planning/phases/232-decimator-audit/232-01-AUDIT.md && grep -c "3ad0f8d3" .planning/phases/232-decimator-audit/232-01-AUDIT.md && test -z "$(git status --porcelain contracts/ test/)"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/232-decimator-audit/232-01-AUDIT.md` exists
    - File contains the literal header string `## Per-Function Verdict Table`
    - File contains the literal header string `## Findings-Candidate Block`
    - File contains the literal header string `## Scope-guard Deferrals`
    - File contains the literal header string `## Downstream Hand-offs`
    - Verdict table header row is exactly `| Function | File:Line | Attack Vector | Verdict | Evidence | SHA | Finding Candidate |`
    - Every row in the Per-Function Verdict Table cites the 7-char SHA `3ad0f8d3`
    - Every row cites a File:Line anchor starting with `contracts/BurnieCoin.sol:`, `contracts/modules/DegenerusGameAdvanceModule.sol:`, or `contracts/modules/DegenerusGameDecimatorModule.sol:`
    - Every row's verdict is exactly one of the strings `SAFE`, `SAFE-INFO`, `VULNERABLE`, or `DEFERRED` (per D-02)
    - Every row's Finding Candidate cell is exactly `Y` or `N` (per D-13)
    - At least one row per target function exists: `decimatorBurn`, `_consolidatePoolsAndRewardJackpots`, `recordDecBurn`, `runDecimatorJackpot`, `consumeDecClaim`, `claimDecimatorJackpot`, `decClaimable`, `recordTerminalDecBurn`, `runTerminalDecimatorJackpot`, `claimTerminalDecimatorJackpot`, `terminalDecClaimable` (11 functions minimum — additional rows for multiple attack vectors per function expected, so at least 15 distinct verdict rows)
    - `grep -cE "SAFE|SAFE-INFO|VULNERABLE|DEFERRED" .planning/phases/232-decimator-audit/232-01-AUDIT.md` returns at least 15
    - `grep -c "3ad0f8d3" .planning/phases/232-decimator-audit/232-01-AUDIT.md` returns at least 11 (one per function minimum)
    - The string `F-29-NN` does NOT appear in the file (per D-13)
    - The string `F-29-` does NOT appear in any form in the file (Phase 236 owns ID assignment per D-13)
    - High-Risk Patterns Analyzed section contains at least four subsections: `Burn-Key Space Alignment`, `Consolidated Jackpot-Block Disjointness`, `decPoolWei` determinism subsection, `DECIMATOR_MIN_BUCKET_100` subsection
    - Downstream Hand-offs subsection explicitly names `Phase 235 CONS-02` (per D-14 BurnieCoin conservation hand-off)
    - Downstream Hand-offs subsection explicitly names `Phase 236 FIND-01` (per D-13 finding-ID assignment hand-off)
    - No placeholder line numbers like `:<line>` remain in the final file — every File:Line is a real integer or integer range
    - No contract source content read from outside `contracts/` directory (per user memory `feedback_contract_locations.md`)
    - `git status --porcelain contracts/ test/` returns empty output both before and after task execution (READ-only v29.0 milestone constraint; zero writes to production code)
    - x00 / x5 branch disjointness proof is explicit in the `Consolidated Jackpot-Block Disjointness` subsection (words `prevMod10` or `prevMod100` present with numeric reasoning)
    - At least one row carries `SAFE-INFO` verdict and `Finding Candidate: Y` for the DECIMATOR_MIN_BUCKET_100 reachability side-effect (per D-06 + §1.8 rationale)
  </acceptance_criteria>
  <done>`232-01-AUDIT.md` exists with per-function verdict table covering all `3ad0f8d3` functions across BurnieCoin + AdvanceModule consolidated block + every DecimatorModule read/write site of `decBurns`/`decBurnBuckets`/`decPool`. Every row cites SHA `3ad0f8d3` and a real File:Line in `contracts/`. Verdicts use `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` vocabulary. D-06 (pro-rata key alignment) and D-07 (consolidated-block disjointness, `decPoolWei` determinism, self-call args + CEI) attack vectors all covered. Findings-Candidate / Scope-guard Deferrals / Downstream Hand-offs subsections present; Phase 235 CONS-02 BurnieCoin conservation hand-off and Phase 236 FIND-01 ID-assignment hand-off both explicit. Zero F-29-NN emissions (per D-13). Zero contracts/ or test/ writes (READ-only milestone rule).</done>
</task>

</tasks>

<verification>
- `232-01-AUDIT.md` exists with per-function verdict table covering every function in 230-01-DELTA-MAP.md §1.3 / §1.1 / §1.8 owned by `3ad0f8d3`
- Locked column set per D-02 (`Function | File:Line | Attack Vector | Verdict | Evidence | SHA`) + `Finding Candidate` column per D-13
- Every verdict row cites `3ad0f8d3`
- Every File:Line anchor resolves into `contracts/` at HEAD
- Verdicts exclusively use `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` (per D-02)
- Every row carries `Y` or `N` in Finding Candidate column (per D-13)
- D-06 attack vectors present: pro-rata off-by-one + every decBurns/decBurnBuckets/decPool consumer key-space verified
- D-07 attack vectors present: x00/x5 mutual exclusivity + decPoolWei determinism + runDecimatorJackpot args + CEI preserved
- Findings-Candidate / Scope-guard Deferrals / Downstream Hand-offs subsections present
- Hand-offs explicitly name Phase 235 CONS-02 (BurnieCoin conservation per D-14) and Phase 236 FIND-01 (ID assignment per D-13)
- Zero `F-29-NN` references anywhere in the file (per D-13)
- Zero writes to `contracts/` or `test/` during plan execution (READ-only v29.0 milestone rule)
</verification>

<success_criteria>
Every function touched by commit `3ad0f8d3` across the DCM-01 surface has a `SAFE | SAFE-INFO | VULNERABLE | DEFERRED` verdict against the D-06 and D-07 attack vectors (pro-rata off-by-one, key-space alignment, consolidated-block mutual exclusivity, `decPoolWei` determinism, self-call args + CEI). Every verdict is traceable via SHA + File:Line. Every row carries a `Finding Candidate: Y/N` value per D-13 so Phase 236 FIND-01 can harvest candidates. BurnieCoin conservation accounting is explicitly deferred to Phase 235 CONS-02 per D-14 — this plan scopes BurnieCoin work to burn-key correctness only. Phase 230 delta-map is not edited; any out-of-scope discovery is recorded in Scope-guard Deferrals per D-05. READ-only milestone rule honored: zero writes to `contracts/` or `test/`.
</success_criteria>

<output>
After completion, create `.planning/phases/232-decimator-audit/232-01-SUMMARY.md`
</output>
</content>
</invoke>