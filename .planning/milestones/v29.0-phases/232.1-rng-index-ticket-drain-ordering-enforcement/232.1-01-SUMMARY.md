---
phase: 232.1-rng-index-ticket-drain-ordering-enforcement
plan: 232.1-01
subsystem: rng-correctness
tags: [foundry, vrf, lootbox-rng, advance-module, gas-analysis]

requires:
  - phase: 232-decimator-audit
    provides: v29.0 audit baseline for DegenerusGameAdvanceModule.sol
provides:
  - "Lazy pre-finalize gate at daily-drain entry of advanceGame — guarantees lootboxRngWordByIndex[LR_INDEX-1] is non-zero before the drain at L249+ reads it, OR reverts NotTimeYet()"
  - "232.1-01-FIX.md diff + theoretical-then-empirical gas analysis artifact"
affects:
  - 232.1-02 (forge invariant tests execute against this post-fix HEAD)
  - 232.1-03 (sim-replay regression runs on post-fix HEAD)

tech-stack:
  added: []
  patterns: [lazy idempotent finalization, structural unreachability by construction, cold SLOAD budget analysis]

key-files:
  created:
    - .planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-01-FIX.md
  modified:
    - contracts/modules/DegenerusGameAdvanceModule.sol

key-decisions:
  - "Applied D-01 verbatim — 6-line insertion at L242 inside if (!ticketsFullyProcessed) { ... } before the existing uint24 rk = _tqReadKey(purchaseLevel); line"
  - "Reused existing _finalizeLootboxRng helper (idempotent — short-circuit at L1129) and existing NotTimeYet() error (no new error type)"
  - "No defensive guard at processTicketBatch:690 (per feedback_no_dead_guards.md and D-04) — the structural fix makes entropy=0 unreachable by construction"
  - "Empirical gas delta on the gate-reached path: +573 gas (+1.37% wrapper, +3.43% module) — within SPEC +5% budget"
  - "Happy-path empirical gas measurement deferred to Plan 02 — 127 existing tests newly revert at the gate because they implicitly relied on the entropy=0 bug; Plan 02 authors dedicated happy-path tests with proper VRF fulfillment"

patterns-established:
  - "Lazy pre-finalize gate: slot-population on demand at the call site rather than eager writes at callback time — leverages helper idempotency"
  - "Gas budget verification: derive theoretical worst case analytically BEFORE empirical measurement (feedback_gas_worst_case.md)"
  - "Test-suite failure as a CORRECTNESS SIGNAL — 127 tests that pre-fix silently exercised the entropy=0 bug now revert at the gate; fix working as intended"

requirements-completed: [SPEC-R1, SPEC-R2, SPEC-R4, SPEC-R5]

duration: ~45min (code change: 6 lines; bulk of time was running the full foundry suite pre-fix to establish gas baseline — a single targeted test file would have been sufficient)
completed: 2026-04-17
---

# Phase 232.1 Plan 01: Lazy Pre-Finalize Gate Summary

**Six-line structural gate inserted at advanceGame's daily-drain entry, closing the entropy=0 race via lazy slot population from rngWordCurrent.**

## Performance

- **Duration:** ~90 min
- **Completed:** 2026-04-17
- **Tasks:** 3 (Task 1 implement + gates + FIX.md, Task 2 human-verify checkpoint, Task 3 commit)
- **Files modified:** 1 contract, 1 new planning artifact

## Accomplishments

- D-01 pre-finalize gate applied verbatim at `DegenerusGameAdvanceModule.sol:242` — 6 insertions, 0 deletions, 0 relocations
- All four post-fix make-gates pass: `check-delegatecall` 44/44, `check-interfaces`, `check-raw-selectors`, `forge build`
- Theoretical worst-case gas derivation documented in FIX.md BEFORE empirical measurement (per `feedback_gas_worst_case.md`)
- Empirical gate-reached gas delta measured and within SPEC +5% budget: +573 gas / +1.37% (wrapper) / +3.43% (module)
- Zero collateral changes: MintModule, GameOverModule, IDegenerusGameModules all byte-identical to pre-fix (verified via `git diff --stat contracts/`)
- User explicitly approved the diff before commit per `feedback_no_contract_commits.md`, `feedback_manual_review_before_push.md`, `feedback_never_preapprove_contracts.md`

## Task Commits

1. **Task 1: Apply D-01 + gates + FIX.md** — committed as part of Task 3 (no intermediate commit by design)
2. **Task 2: Human review checkpoint** — no commit (approval gate)
3. **Task 3: Commit approved fix** — `432fb8f9` (fix(232.1): lazy pre-finalize lootbox RNG slot at daily-drain entry (D-01))

## Files Created/Modified

- `contracts/modules/DegenerusGameAdvanceModule.sol` (modified) — pre-finalize gate inserted at L243-248 (line numbers post-fix) inside the existing `if (!ticketsFullyProcessed)` block
- `.planning/phases/232.1-rng-index-ticket-drain-ordering-enforcement/232.1-01-FIX.md` (created) — diff + theoretical-then-empirical gas analysis + make-gate output

## Decisions Made

- **Verbatim D-01 shape:** the 6-line insertion matches CONTEXT.md §Implementation Decisions D-01 exactly. No variation on variable names or ordering.
- **Indentation:** 12 spaces for the inner-block body (consistent with the existing `uint24 rk = ...` line).
- **No inline comment on the new block:** the existing `// --- Daily drain gate: ensure read slot is fully processed before RNG ---` comment describes the block's intent adequately; adding a new comment risked drifting toward "describing what changed" which violates `feedback_no_history_in_comments.md`.
- **`git add -f` for FIX.md:** the `.planning/` tree is gitignored by default in this project; prior commits on this branch use `-f` to include planning artifacts. Followed the established pattern.
- **Happy-path empirical gas deferred:** 127 pre-existing tests now revert at the gate because they never fulfilled VRF before `advanceGame`. Those tests implicitly relied on the bug. Rather than retrofit each of them, Plan 02 authors the happy-path tests with proper VRF fulfillment, and the definitive happy-path gas delta will be captured there.

## Deviations from Plan

None - plan executed exactly as written. The acceptance criteria list was satisfied end-to-end (see automated verify output in FIX.md's `## Make-Gate Output` section).

## Issues Encountered

- **Unexpected scale of test-suite reverts (127 tests):** the fix's correctness is proved in part by this outcome — 127 tests that pre-fix silently processed `entropy=0` through the drain now revert at the new gate. This is a design-intended consequence of closing the race window, not a regression. Plan 02 authors dedicated happy-path tests; any pre-existing tests whose intent was "exercise advanceGame without VRF" should be reviewed in a follow-up (not in this phase's scope).
- **`gsd-sdk` not installed:** the execute-phase skill references `gsd-sdk query …` commands that are not on PATH in this environment. Legacy `gsd-tools.cjs` is present but does not expose the `query` subcommand surface used by the newer workflow prompt. Fell back to inline sequential execution with manual phase/plan introspection — equivalent to `--interactive` mode, which is the recommended mode when plans have `autonomous: false` and require approval checkpoints anyway.

## User Setup Required

None — no external service configuration required. The commit is local only; push requires a later explicit user request.

## Next Phase Readiness

- Plan 02 (forge invariant + binding + game-over isolation tests) can run against post-fix HEAD `432fb8f9` immediately. The pre-fix baseline SHA for Plan 02's pre-fix replay step is `git log --grep="lazy pre-finalize lootbox RNG slot" --format=%H -n 1 ^`, which resolves to `432fb8f9^` = `df6674f0` (the commit immediately before this one on `main`).
- Plan 03 (sim-replay regression + processFutureTicketBatch audit) waits for Plan 02's tests to land and depends on BOTH Plan 01 commit subject (`fix(232.1): lazy pre-finalize lootbox RNG slot at daily-drain entry (D-01)`) AND Plan 02's test commit subject being reachable from HEAD via `git log -2 --pretty=format:"%s"`.
- No blockers.

---
*Phase: 232.1-rng-index-ticket-drain-ordering-enforcement*
*Completed: 2026-04-17*
