---
phase: 230-delta-extraction-scope-map
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md
  - .planning/phases/230-delta-extraction-scope-map/230-01-SUMMARY.md
autonomous: true
requirements:
  - DELTA-01
  - DELTA-02
  - DELTA-03
must_haves:
  truths:
    - "230-01-DELTA-MAP.md exists with four top-level sections in this order: Function-Level Changelog, Cross-Module Interaction Map, Interface Drift Catalog, Consumer Index (per D-05)"
    - "Every one of the 10 in-scope commits (2471f8e7, 52242a10, f20a2b5e, 3ad0f8d3, 104b5d42, 67031e7d, 858d83e4, d5284be5, e0a7f7bc, 20a951df) is cited at least once in the Function-Level Changelog section"
    - "Every one of the 12 in-scope files has a dedicated changelog block in the Function-Level Changelog section"
    - "Every cross-module call chain newly introduced or modified in the 10-commit range has a row in the Cross-Module Interaction Map with Caller, Callee, Call Type, Commit SHA, and What Changed columns (per D-08)"
    - "Every method signature in IDegenerusGame / IDegenerusQuests / IDegenerusGameModules has a PASS/FAIL row in the Interface Drift Catalog (per D-10)"
    - "The Consumer Index names every v29.0 requirement (DELTA-01, DELTA-02, DELTA-03, EBD-01..03, DCM-01..03, JKP-01..03, QST-01..03, CONS-01..02, RNG-01..02, TRNX-01, REG-01..02, FIND-01..03 = 25 total) and points each to specific sections / row-ranges of 230-01-DELTA-MAP.md"
    - "Comment-only and NatSpec-only diffs are classified UNCHANGED per D-03 (not MODIFIED)"
    - "Private and internal functions appearing in the delta are enumerated per D-04"
    - "`forge build` succeeds at HEAD and the three Makefile gates (check-interfaces, check-delegatecall, check-raw-selectors) all PASS — smoke test that no hidden contract drift exists"
  artifacts:
    - path: ".planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md"
      provides: "Function-level changelog + cross-module interaction map + interface drift catalog + consumer index covering the 14cb45e1..HEAD delta across 12 contract files and 10 commits"
      contains: "## 1. Function-Level Changelog"
      min_lines: 300
    - path: ".planning/phases/230-delta-extraction-scope-map/230-01-SUMMARY.md"
      provides: "Standard GSD plan summary: what was produced, counts, downstream consumer pointers"
      contains: "230-01"
  key_links:
    - from: "230-01-DELTA-MAP.md § Function-Level Changelog"
      to: "git diff 14cb45e1..HEAD -- contracts/"
      via: "per-function NEW/MODIFIED/DELETED verdict with commit SHA"
      pattern: "14cb45e1"
    - from: "230-01-DELTA-MAP.md § Consumer Index"
      to: "Phases 231-236 PLAN.md files (future)"
      via: "requirement-ID → section/row reference"
      pattern: "DELTA-0[123]|EBD-0[123]|DCM-0[123]|JKP-0[123]|QST-0[123]|CONS-0[12]|RNG-0[12]|TRNX-01|REG-0[12]|FIND-0[123]"
    - from: "230-01-DELTA-MAP.md § Interface Drift Catalog"
      to: "contracts/interfaces/IDegenerusGame.sol + IDegenerusQuests.sol + IDegenerusGameModules.sol"
      via: "per-method PASS/FAIL row vs. implementer contract"
      pattern: "IDegenerus(Game|Quests|GameModules)"
    - from: "230-01-DELTA-MAP.md § Cross-Module Interaction Map"
      to: "contracts/modules/*.sol delegatecall and self-call sites"
      via: "tabular caller->callee rows with commit SHA"
      pattern: "delegatecall|IDegenerusGame\\(address\\(this\\)\\)"
---

<objective>
Produce the single authoritative v29.0 audit-surface catalog `230-01-DELTA-MAP.md` covering the 10 contract-touching commits between v27.0 baseline `14cb45e1` (2026-04-12 21:55) and HEAD. The catalog has four sections in this fixed order per D-05:
  1. Function-Level Changelog (DELTA-01)
  2. Cross-Module Interaction Map (DELTA-02)
  3. Interface Drift Catalog (DELTA-03)
  4. Consumer Index (maps every v29.0 requirement to specific sections/rows of this doc — D-11)

Purpose: Phases 231-235 consume this catalog as their scope definition with zero additional discovery. Phase 236 uses the Consumer Index as its findings-sourcing map. This is a READ-only audit milestone per STATE.md — no `contracts/` or `test/` writes.

Output: `.planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md` plus the standard `230-01-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/230-delta-extraction-scope-map/230-CONTEXT.md

<interfaces>
<!-- Confirmed by scouting before plan write - copy these values verbatim into task execution. -->

### Diff boundary (D-01, D-02)
- Baseline SHA: `14cb45e1` (v27.0 phase execution complete, 2026-04-12 21:55)
- HEAD: current `main` tip (includes today's `20a951df` earlybird trait-alignment commit)
- Authoritative source: `git diff 14cb45e1..HEAD -- contracts/` (freshly re-run — NOT synthesis of commit messages)

### In-scope commits (10, newest→oldest as printed by `git log --oneline 14cb45e1..HEAD -- contracts/`)
```
20a951df feat(earlybird): align trait roll with coin jackpot, fix queue level
e0a7f7bc feat(boons): expose boonPacked mapping for UI reads
d5284be5 fix(quests): credit fresh ETH wei 1:1 to mint_ETH quest
858d83e4 feat(game): expose claimTerminalDecimatorJackpot passthrough
67031e7d feat(decimator): emit DecimatorClaimed and TerminalDecimatorClaimed events
104b5d42 feat(jackpot): tag BAF wins with traitId=420 sentinel
3ad0f8d3 fix(decimator): key burns by resolution level, consolidate jackpot block
f20a2b5e refactor(earlybird): finalize at level transition, unify award call per purchase
52242a10 refactor: pass entropy explicitly to processFutureTicketBatch
2471f8e7 phase transition fix
```

### In-scope files (12, confirmed via `git diff --name-status 14cb45e1..HEAD -- contracts/`)
All have status M (modified); none are A (added) or D (deleted).
```
M  contracts/BurnieCoin.sol
M  contracts/DegenerusGame.sol
M  contracts/DegenerusQuests.sol
M  contracts/interfaces/IDegenerusGame.sol
M  contracts/interfaces/IDegenerusGameModules.sol
M  contracts/interfaces/IDegenerusQuests.sol
M  contracts/modules/DegenerusGameAdvanceModule.sol
M  contracts/modules/DegenerusGameDecimatorModule.sol
M  contracts/modules/DegenerusGameJackpotModule.sol
M  contracts/modules/DegenerusGameMintModule.sol
M  contracts/modules/DegenerusGameWhaleModule.sol
M  contracts/storage/DegenerusGameStorage.sol
```

### Diff-stat (for sanity-checking changelog completeness)
```
 contracts/BurnieCoin.sol                           |   4 +-
 contracts/DegenerusGame.sol                        |  22 ++--
 contracts/DegenerusQuests.sol                      |  16 +--
 contracts/interfaces/IDegenerusGame.sol            |   4 +
 contracts/interfaces/IDegenerusGameModules.sol     |   4 +-
 contracts/interfaces/IDegenerusQuests.sol          |   4 +-
 contracts/modules/DegenerusGameAdvanceModule.sol   | 108 +++++++++++++++-----
 contracts/modules/DegenerusGameDecimatorModule.sol |  37 +++++++
 contracts/modules/DegenerusGameJackpotModule.sol   | 113 +++++++++------------
 contracts/modules/DegenerusGameMintModule.sol      |  52 ++++------
 contracts/modules/DegenerusGameWhaleModule.sol     |   6 +-
 contracts/storage/DegenerusGameStorage.sol         |  43 +++-----
 12 files changed, 238 insertions(+), 175 deletions(-)
```

### Detection commands (use these EXACTLY)

Per-file A/M/D status:
```
git diff --name-status 14cb45e1..HEAD -- contracts/
```

Per-file insertion/deletion counts:
```
git diff --stat 14cb45e1..HEAD -- contracts/
```

Per-file full diff:
```
git diff 14cb45e1..HEAD -- contracts/<path>
```

Per-commit diff (to attribute changes to owning SHAs):
```
git show <sha> -- contracts/<path>
```

Per-commit function list (for a given file):
```
git log --oneline 14cb45e1..HEAD -- contracts/<path>
```

Comment-only / NatSpec-only / whitespace-only detection:
```
git diff -w --ignore-blank-lines 14cb45e1..HEAD -- contracts/<path>
```
If `git diff -w --ignore-blank-lines` is empty but `git diff` is non-empty, the change is whitespace-only → UNCHANGED per D-03.
For NatSpec-only: if every hunk of the diff sits inside a `///` or `/** */` block, the change is comment-only → UNCHANGED per D-03.

Interface implementer scan (for DELTA-03):
```
grep -nE "function\s+\w+" contracts/interfaces/IDegenerusGame.sol
grep -nE "function\s+\w+" contracts/interfaces/IDegenerusQuests.sol
grep -nE "function\s+\w+" contracts/interfaces/IDegenerusGameModules.sol
```
Then match each signature against its implementer:
- `IDegenerusGame` → `contracts/DegenerusGame.sol`
- `IDegenerusQuests` → `contracts/DegenerusQuests.sol`
- `IDegenerusGameModules` → not inherited; used via `abi.encodeWithSelector(IDegenerusGameModules.<fn>.selector, ...)` in the 5 modules (Advance, Jackpot, Mint, Whale, Decimator). For drift, compare each module's implemented function against the selector-reference in the interface.

Cross-module call detection (for DELTA-02):
```
# delegatecall from DegenerusGame into a module
grep -nE "delegatecall\(abi\.encodeWithSelector" contracts/DegenerusGame.sol
# self-call from a module back into Game via IDegenerusGame(address(this))
grep -nrE "IDegenerusGame\(address\(this\)\)" contracts/modules/
# external quest calls
grep -nrE "IDegenerusQuests\(" contracts/modules/ contracts/DegenerusGame.sol
# external burnie calls
grep -nrE "BurnieCoin\(|IBurnieCoin" contracts/modules/ contracts/DegenerusGame.sol contracts/DegenerusQuests.sol
```
Filter the hits to only those whose caller line OR callee signature appears in the diff (i.e., introduced or modified by one of the 10 commits).

### Comment-only classification (D-03) — worked example
If `git diff 14cb45e1..HEAD -- contracts/FileX.sol` shows 20 added lines and 10 deleted lines, but `git diff -w --ignore-blank-lines 14cb45e1..HEAD -- contracts/FileX.sol` shows identical non-comment content on both sides, FileX counts as UNCHANGED. The changelog still lists the file (with an "UNCHANGED: comment/NatSpec only" note and the commit SHAs that touched it) so downstream auditors aren't surprised when they see it in `git blame` but nothing in the catalog.

### Interface ↔ implementer map (for DELTA-03 catalog)
- `contracts/interfaces/IDegenerusGame.sol` ↔ `contracts/DegenerusGame.sol`
- `contracts/interfaces/IDegenerusQuests.sol` ↔ `contracts/DegenerusQuests.sol`
- `contracts/interfaces/IDegenerusGameModules.sol` ↔ 5 module contracts:
  - `contracts/modules/DegenerusGameAdvanceModule.sol`
  - `contracts/modules/DegenerusGameJackpotModule.sol`
  - `contracts/modules/DegenerusGameMintModule.sol`
  - `contracts/modules/DegenerusGameWhaleModule.sol`
  - `contracts/modules/DegenerusGameDecimatorModule.sol`

### Makefile gates (already in repo; use as smoke tests, do NOT duplicate)
```
make check-interfaces      # interface ↔ implementation signature drift (compile-time)
make check-delegatecall    # 43 delegatecall sites verified ALIGNED (bash+awk)
make check-raw-selectors   # raw selector / calldata literal detection (5 patterns)
```

### Finding ID convention (from prior milestones)
- v25.0 finding pattern: `F-25-NN`
- v27.0 finding pattern: `F-27-NN`
- v28.0 finding pattern: `F-28-NN` (flat) and `F-28-224-NN` (per-phase stubs)
- v29.0 should use `F-29-NN` for consolidated (Phase 236) and may use `F-29-<phase>-NN` for per-phase stubs — Phase 230 produces NO findings (it's a scope catalog, not an adversarial pass). Do not invent finding IDs in 230-01.

### v29.0 requirements enumeration (for Consumer Index — all 25 IDs MUST appear)
From `.planning/REQUIREMENTS.md`:
- Delta (this phase):  DELTA-01, DELTA-02, DELTA-03
- Earlybird (Phase 231): EBD-01, EBD-02, EBD-03
- Decimator (Phase 232): DCM-01, DCM-02, DCM-03
- Jackpot/BAF/Entropy (Phase 233): JKP-01, JKP-02, JKP-03
- Quests/Boons/Misc (Phase 234): QST-01, QST-02, QST-03
- Conservation + RNG (Phase 235): CONS-01, CONS-02, RNG-01, RNG-02, TRNX-01
- Regression + Findings (Phase 236): REG-01, REG-02, FIND-01, FIND-02, FIND-03
TOTAL: 25 requirement IDs. Consumer Index MUST reference all 25.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Baseline per-file delta table + catalog scaffold</name>
  <files>.planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md</files>
  <read_first>
    - .planning/phases/230-delta-extraction-scope-map/230-CONTEXT.md (D-01..D-11 — the 11 locked decisions, especially D-05 four-section ordering, D-03 comment-only rule, D-04 private/internal inclusion)
    - .planning/REQUIREMENTS.md (DELTA-01/02/03 definitions + the complete 25-requirement list for the Consumer Index)
    - .planning/ROADMAP.md (Phase 230 success criteria — 4 items — verify all four are satisfied by end of task 6)
    - .planning/milestones/v25.0-phases/213-delta-extraction/213-03-PLAN.md (prior-milestone changelog methodology — style reference only, do NOT copy findings)
    - .planning/milestones/v28.0-phases/224-api-route-openapi-alignment/224-01-PLAN.md (prior-milestone single-file catalog pattern — style reference for section structure)
  </read_first>
  <action>
Create `.planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md` with the catalog scaffold and the per-file delta baseline table. This task locks in the structure; tasks 2-5 fill individual sections; task 6 adds the Consumer Index and summary.

**Step 1 — Write the preamble.** Include:
- Title: `# 230-01 Delta Map: v27.0 Baseline → HEAD`
- Preamble block stating:
  - Phase: 230 — Delta Extraction & Scope Map
  - Milestone: v29.0 Post-v27 Contract Delta Audit
  - Requirements satisfied: DELTA-01, DELTA-02, DELTA-03
  - Baseline commit: `14cb45e1` (v27.0 phase execution complete, 2026-04-12 21:55) — per D-01
  - HEAD at generation: record the output of `git rev-parse HEAD` literally
  - Diff command used: `git diff 14cb45e1..HEAD -- contracts/` — per D-02
  - Comment/NatSpec/whitespace-only changes classified UNCHANGED — per D-03
  - Private/internal functions included when appearing in the delta — per D-04
  - Section ordering locked: (1) Function-Level Changelog → (2) Cross-Module Interaction Map → (3) Interface Drift Catalog → (4) Consumer Index — per D-05
  - Post-commit: this file is READ-only; downstream phases record scope-guard deferrals rather than editing in-place — per D-06
- Verdict legend block:
  - Function change types: NEW / MODIFIED / DELETED / UNCHANGED (UNCHANGED applies only to comment-only diffs per D-03)
  - Interface drift verdicts: PASS / FAIL (FAIL if implementer signature doesn't match interface declaration at HEAD) — per D-10
  - Interaction call types: `direct` (Solidity `fn(...)` on an external address), `delegatecall` (via `address.delegatecall(...)`), `self-call` (`IDegenerusGame(address(this)).fn(...)`), `selector-call` (`abi.encodeWithSelector(IFACE.fn.selector, ...)` followed by raw call/delegatecall) — per D-08

**Step 2 — Write Section 0: Per-file delta baseline.** Create an `## 0. Per-File Delta Baseline` section containing a single table with these columns:
`File | Status | Insertions | Deletions | Owning Commit SHAs | Change Category`

Run and record verbatim:
```
git diff --name-status 14cb45e1..HEAD -- contracts/
git diff --stat 14cb45e1..HEAD -- contracts/
```

Populate 12 rows (one per in-scope file). For "Owning Commit SHAs", run `git log --oneline 14cb45e1..HEAD -- <path>` for each file and list the SHAs (7-char prefix) whose diff touched that file. For "Change Category" use one of:
- `entropy-passthrough` (52242a10)
- `earlybird-finalize` (f20a2b5e)
- `earlybird-trait-align` (20a951df)
- `decimator-burn-key` (3ad0f8d3)
- `decimator-events` (67031e7d)
- `terminal-decimator-passthrough` (858d83e4)
- `baf-sentinel` (104b5d42)
- `quest-weicredit` (d5284be5)
- `boon-expose` (e0a7f7bc)
- `rnglock-removal` (2471f8e7)
- `mixed` (if more than 2 of the above apply)

End this section with the literal line: `Files in scope: 12`. Then: `Commits in scope: 10 (14cb45e1..HEAD, computed via git log --oneline)`.

**Step 3 — Write the four top-level section headings in order (body content filled in tasks 2-5):**
```
## 1. Function-Level Changelog
<!-- DELTA-01 — populated in task 2 -->

## 2. Cross-Module Interaction Map
<!-- DELTA-02 — populated in task 3 -->

## 3. Interface Drift Catalog
<!-- DELTA-03 — populated in task 4 -->

## 4. Consumer Index
<!-- D-11 — populated in task 6 -->
```

DO NOT populate the section bodies yet. Task 2-5 extend them in place.

Per D-06 reminder: after this plan completes, 230-01-DELTA-MAP.md is read-only. No downstream phase edits it.
  </action>
  <verify>
    <automated>test -f .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md &amp;&amp; grep -q "^# 230-01 Delta Map" .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md &amp;&amp; grep -q "^## 0. Per-File Delta Baseline" .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md &amp;&amp; grep -q "^## 1. Function-Level Changelog" .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md &amp;&amp; grep -q "^## 2. Cross-Module Interaction Map" .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md &amp;&amp; grep -q "^## 3. Interface Drift Catalog" .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md &amp;&amp; grep -q "^## 4. Consumer Index" .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md &amp;&amp; grep -q "Files in scope: 12" .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md &amp;&amp; grep -q "Commits in scope: 10" .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md` exists.
    - File contains the literal title `# 230-01 Delta Map: v27.0 Baseline → HEAD`.
    - File contains all five required section headings in this exact order as top-level `##` headings: `0. Per-File Delta Baseline`, `1. Function-Level Changelog`, `2. Cross-Module Interaction Map`, `3. Interface Drift Catalog`, `4. Consumer Index`.
    - Section 0 contains a table with exactly 12 data rows (one per in-scope file listed in the `<interfaces>` block).
    - Every Section 0 row has all six columns populated: File, Status, Insertions, Deletions, Owning Commit SHAs (at least one 7-char SHA), Change Category (one of the 11 enumerated categories).
    - Section 0 ends with literal lines `Files in scope: 12` and `Commits in scope: 10 (14cb45e1..HEAD, computed via git log --oneline)`.
    - Preamble names baseline commit `14cb45e1` and lists all 11 decision references (D-01 through D-11).
    - Preamble verdict legend enumerates NEW / MODIFIED / DELETED / UNCHANGED, PASS / FAIL, and the four call types (direct / delegatecall / self-call / selector-call).
    - No file under `contracts/` or `test/` modified (READ-only milestone — verify via `git status` showing only `.planning/` changes).
  </acceptance_criteria>
  <done>Catalog scaffold with preamble, legend, per-file delta baseline, and four top-level section headings in place — ready for tasks 2-5 to extend in-place.</done>
</task>

<task type="auto">
  <name>Task 2: Populate Section 1 — Function-Level Changelog (DELTA-01)</name>
  <files>.planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md</files>
  <read_first>
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md (extend Section 1 in place)
    - contracts/BurnieCoin.sol (current HEAD)
    - contracts/DegenerusGame.sol (current HEAD)
    - contracts/DegenerusQuests.sol (current HEAD)
    - contracts/modules/DegenerusGameAdvanceModule.sol (current HEAD)
    - contracts/modules/DegenerusGameDecimatorModule.sol (current HEAD)
    - contracts/modules/DegenerusGameJackpotModule.sol (current HEAD)
    - contracts/modules/DegenerusGameMintModule.sol (current HEAD)
    - contracts/modules/DegenerusGameWhaleModule.sol (current HEAD)
    - contracts/storage/DegenerusGameStorage.sol (current HEAD)
    - contracts/interfaces/IDegenerusGame.sol (current HEAD)
    - contracts/interfaces/IDegenerusGameModules.sol (current HEAD)
    - contracts/interfaces/IDegenerusQuests.sol (current HEAD)
  </read_first>
  <action>
Populate `## 1. Function-Level Changelog` in-place inside 230-01-DELTA-MAP.md. Per D-07, organize by contract category (modules/ first, core contracts/ second, storage/ third, interfaces/ fourth). Per D-04, include private and internal functions that appear in the delta.

**Step 1 — Establish category subsections.** Create these exact `###` subsections in order:
```
### 1.1 modules/ — DegenerusGameAdvanceModule.sol
### 1.2 modules/ — DegenerusGameJackpotModule.sol
### 1.3 modules/ — DegenerusGameDecimatorModule.sol
### 1.4 modules/ — DegenerusGameMintModule.sol
### 1.5 modules/ — DegenerusGameWhaleModule.sol
### 1.6 core — DegenerusGame.sol
### 1.7 core — DegenerusQuests.sol
### 1.8 core — BurnieCoin.sol
### 1.9 storage — DegenerusGameStorage.sol
### 1.10 interfaces — IDegenerusGame.sol
### 1.11 interfaces — IDegenerusGameModules.sol
### 1.12 interfaces — IDegenerusQuests.sol
```

Every one of the 12 in-scope files MUST have its own subsection (12 total), even if the conclusion is "UNCHANGED: comment-only" — in that case the subsection contains a single line stating that with the commit SHA that touched the file.

**Step 2 — For each file subsection, enumerate changed functions.** Run `git diff 14cb45e1..HEAD -- contracts/<path>` and for each affected function emit one row in a per-file table with these columns (per D-07):
`Function Signature | Visibility | Verdict | Commit SHA(s) | What Changed (one-line semantic)`

- `Function Signature`: Full signature including parameter types and names, e.g., `processFutureTicketBatch(uint256 dayIdx, uint256 entropy, uint256 ticketCount)`.
- `Visibility`: One of `external`, `public`, `internal`, `private` (per D-04, all four are included when they appear in the delta).
- `Verdict`: NEW / MODIFIED / DELETED / UNCHANGED (UNCHANGED reserved for comment-only diffs per D-03; still listed if the function's body appears in the file diff at all, to prevent silent omission).
- `Commit SHA(s)`: One or more 7-char SHAs from the 10-commit in-scope list. If a function was touched by multiple commits, list all in order: earliest → latest. Use `git log --oneline 14cb45e1..HEAD -- contracts/<path>` and `git log -L :<funcname>:<path>` (or diff-by-diff inspection via `git show <sha>`) to attribute.
- `What Changed`: One sentence stating the semantic change. Examples:
  - NEW: "Added to expose packed mapping for off-chain UI reads."
  - MODIFIED: "Entropy now passed as explicit parameter instead of derived from block.prevrandao."
  - MODIFIED: "Queue level fixed from `lvl` to `lvl+1` to align with coin jackpot."
  - DELETED: "Removed; logic inlined into caller as part of finalize-at-transition refactor."
- Per D-03: If the whole function body is semantically unchanged and the only diff is comments/NatSpec/whitespace, the verdict is UNCHANGED and the "What Changed" column reads "comment/NatSpec only per D-03 — no runtime effect".

**Step 3 — Comment-only classification verification per file.** For each file, run:
```
git diff 14cb45e1..HEAD -- contracts/<path> > /tmp/full.diff
git diff -w --ignore-blank-lines 14cb45e1..HEAD -- contracts/<path> > /tmp/nowhite.diff
```
If `/tmp/nowhite.diff` is empty but `/tmp/full.diff` is non-empty → file is whitespace-only, classify the whole file UNCHANGED. If every hunk in `/tmp/nowhite.diff` sits inside a `///` or `/** */` block → file is NatSpec-only, classify UNCHANGED. Record the verification command + outcome under the file subsection in a one-line note.

**Step 4 — Per-file summary line.** After each per-file table, add a summary line:
`Functions — NEW: {n_new}, MODIFIED: {n_mod}, DELETED: {n_del}, UNCHANGED (comment/NatSpec-only): {n_unch}`

**Step 5 — Section closing totals.** At the end of Section 1, emit a rollup line:
`Changelog totals — NEW: {SUM}, MODIFIED: {SUM}, DELETED: {SUM}, UNCHANGED: {SUM}; files with at least one non-UNCHANGED function: {count ≤ 12}`

**Key pointers per commit (to guide per-file attribution; verify by inspecting each commit's diff with `git show <sha>`):**
- `2471f8e7` phase transition fix → DegenerusGameAdvanceModule.sol (removed `_unlockRng(day)` call at what was line 425 pre-change). Expect MODIFIED on `_endPhase` / housekeeping step.
- `52242a10` entropy passthrough → DegenerusGameJackpotModule.sol (processFutureTicketBatch gains explicit entropy param) + IDegenerusGameModules.sol (selector/signature bump) + DegenerusGameAdvanceModule.sol (call-site change).
- `f20a2b5e` earlybird finalize → DegenerusGameJackpotModule.sol (purchase-phase earlybird path refactor) + possibly DegenerusGameMintModule.sol (call site).
- `3ad0f8d3` decimator burn-key → DegenerusGameDecimatorModule.sol + BurnieCoin.sol (decimator-burn-key plumbing — likely a mapping-key signature change on `burnForDecimator` or similar) + DegenerusGameJackpotModule.sol (consolidated jackpot block).
- `104b5d42` BAF sentinel → DegenerusGameJackpotModule.sol (traitId=420 emission site).
- `67031e7d` decimator events → DegenerusGameDecimatorModule.sol (new events `DecimatorClaimed` + `TerminalDecimatorClaimed`) + IDegenerusGameModules.sol (if event declared on interface).
- `858d83e4` terminal decimator passthrough → DegenerusGame.sol (new `claimTerminalDecimatorJackpot` external) + IDegenerusGame.sol (new function declaration) + possibly DegenerusGameDecimatorModule.sol (target).
- `d5284be5` mint_ETH wei fix → DegenerusQuests.sol + IDegenerusQuests.sol + DegenerusGameMintModule.sol (call-site wiring).
- `e0a7f7bc` boon expose → DegenerusGameStorage.sol (accessor) + IDegenerusGame.sol (getter declaration) + DegenerusGame.sol (forwarding accessor if any).
- `20a951df` earlybird trait-align → DegenerusGameJackpotModule.sol (trait roll change, queue level fix `lvl+1`) + possibly DegenerusGameStorage.sol (storage field tweak).

Treat these pointers as hints, NOT facts. Verify each with `git show <sha> -- <path>` before asserting a function is touched by a given SHA.

Per D-09: Intra-module calls are implicit in this changelog and do NOT need to be re-catalogued in Section 2.
  </action>
  <verify>
    <automated>grep -q "^### 1.1 modules/ — DegenerusGameAdvanceModule.sol" .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md &amp;&amp; grep -q "^### 1.12 interfaces — IDegenerusQuests.sol" .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md &amp;&amp; [ "$(grep -cE '^### 1\.[0-9]+' .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md)" -eq 12 ] &amp;&amp; grep -q "Changelog totals —" .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md &amp;&amp; for sha in 2471f8e7 52242a10 f20a2b5e 3ad0f8d3 104b5d42 67031e7d 858d83e4 d5284be5 e0a7f7bc 20a951df; do grep -q "$sha" .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md || { echo "MISSING SHA: $sha"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - Section 1 contains exactly 12 subsections matching the regex `^### 1\.[0-9]+` — one for each in-scope file.
    - Every subsection has at least one data table row OR an explicit "UNCHANGED: comment/NatSpec only per D-03" note with the commit SHA that touched the file.
    - Every 10 in-scope commit SHAs (2471f8e7, 52242a10, f20a2b5e, 3ad0f8d3, 104b5d42, 67031e7d, 858d83e4, d5284be5, e0a7f7bc, 20a951df) appears somewhere in Section 1.
    - Every non-UNCHANGED row has all 5 columns populated (Function Signature, Visibility, Verdict, Commit SHA(s), What Changed).
    - Every Verdict cell is one of exactly: NEW / MODIFIED / DELETED / UNCHANGED.
    - Every Visibility cell is one of exactly: external / public / internal / private.
    - At least one row records an `internal` or `private` visibility function (per D-04 — delta touches at least one per commit pointers above; if somehow zero internals/privates are changed across the whole delta, the executor must add an explicit note saying so and confirming with grep evidence).
    - Every file subsection ends with the summary line `Functions — NEW: {n}, MODIFIED: {n}, DELETED: {n}, UNCHANGED (comment/NatSpec-only): {n}`.
    - Section ends with `Changelog totals — NEW: {SUM}, MODIFIED: {SUM}, DELETED: {SUM}, UNCHANGED: {SUM}; files with at least one non-UNCHANGED function: {count ≤ 12}`.
    - No file under `contracts/` or `test/` modified.
  </acceptance_criteria>
  <done>DELTA-01 satisfied: every changed function in the 10-commit range is listed with signature, visibility, verdict, owning commit SHA(s), and one-line semantic description; each of the 12 in-scope files has its own subsection; all 10 SHAs cited at least once.</done>
</task>

<task type="auto">
  <name>Task 3: Populate Section 2 — Cross-Module Interaction Map (DELTA-02)</name>
  <files>.planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md</files>
  <read_first>
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md (read Section 1 to enumerate changed functions; extend Section 2 in place)
    - contracts/DegenerusGame.sol (current HEAD — delegatecall dispatch site)
    - contracts/modules/DegenerusGameAdvanceModule.sol (current HEAD — self-call sites, cross-module calls)
    - contracts/modules/DegenerusGameJackpotModule.sol (current HEAD)
    - contracts/modules/DegenerusGameDecimatorModule.sol (current HEAD)
    - contracts/modules/DegenerusGameMintModule.sol (current HEAD)
    - contracts/modules/DegenerusGameWhaleModule.sol (current HEAD)
    - contracts/DegenerusQuests.sol (current HEAD)
  </read_first>
  <action>
Populate `## 2. Cross-Module Interaction Map` in-place inside 230-01-DELTA-MAP.md. Per D-08, the section is a single tabular layout. Per D-09, ONLY cross-module chains are catalogued — intra-module calls stay implicit in Section 1.

**Step 1 — Define module boundary.** For this section, "module" means one of:
- `DegenerusGame.sol` (the core dispatcher)
- Each of the 5 `contracts/modules/*.sol` files
- `DegenerusQuests.sol`
- `BurnieCoin.sol`
- `contracts/storage/DegenerusGameStorage.sol` (storage is a library, but calls from modules into its functions count as cross-module)

Calls that stay inside a single one of these files are INTRA-module → excluded. Calls that cross any boundary listed above are CROSS-module → included.

**Step 2 — Enumerate the candidate call surface.** Using the functions listed in Section 1 (only those with Verdict in {NEW, MODIFIED}; UNCHANGED and DELETED rows are excluded), grep the current HEAD source for call sites:

```
# delegatecall dispatch from Game into modules
grep -nE "delegatecall\(abi\.encodeWithSelector" contracts/DegenerusGame.sol

# self-call from modules back into Game
grep -nrE "IDegenerusGame\(address\(this\)\)\.[a-zA-Z_][a-zA-Z0-9_]*" contracts/modules/

# external interface call to Quests from Game/modules
grep -nrE "IDegenerusQuests\([^)]+\)\.[a-zA-Z_][a-zA-Z0-9_]*" contracts/DegenerusGame.sol contracts/modules/

# external interface call to BurnieCoin
grep -nrE "(BurnieCoin|IBurnieCoin)\([^)]+\)\.[a-zA-Z_][a-zA-Z0-9_]*" contracts/DegenerusGame.sol contracts/modules/ contracts/DegenerusQuests.sol

# storage library calls (cross-module)
grep -nrE "DegenerusGameStorage\.[a-zA-Z_][a-zA-Z0-9_]*" contracts/modules/ contracts/DegenerusGame.sol contracts/DegenerusQuests.sol

# selector-calls via IDegenerusGameModules (if any use raw selector literal)
grep -nrE "abi\.encodeWithSelector\(IDegenerusGameModules\." contracts/DegenerusGame.sol contracts/modules/
```

**Step 3 — Filter to NEW/MODIFIED chains.** For each grep hit, include it in the interaction map ONLY if either (a) the caller function appears in Section 1 with verdict NEW or MODIFIED, OR (b) the callee function appears in Section 1 with verdict NEW or MODIFIED. If both sides are UNCHANGED, the chain is not new/modified by this delta → exclude.

**Step 4 — Write the table.** Use EXACTLY these columns per D-08:
`Caller Function | Callee Function | Call Type | Commit SHA | What Changed`

- `Caller Function`: `<ContractName>.<functionName>` (e.g., `DegenerusGame.claimTerminalDecimatorJackpot`).
- `Callee Function`: `<ContractName>.<functionName>` (e.g., `DegenerusGameDecimatorModule.claimTerminalDecimatorJackpot`).
- `Call Type`: one of `delegatecall`, `direct`, `self-call`, `selector-call` (per Task-1 legend).
- `Commit SHA`: 7-char SHA from the 10-commit list that introduced or modified this chain. If the chain was NEW, the SHA is the commit that introduced it. If MODIFIED, the SHA is the commit that last touched either caller or callee body at that call site.
- `What Changed`: one sentence. Examples:
  - "NEW chain: Game forwards external call to module via delegatecall; introduced by 858d83e4."
  - "MODIFIED: entropy argument added to call; caller now passes explicit uint256 instead of relying on callee derivation."
  - "MODIFIED: selector signature changed — `processFutureTicketBatch(uint256,uint256)` → `processFutureTicketBatch(uint256,uint256,uint256)`."

**Step 5 — Row ID convention.** Prefix each row with an `IM-NN` ID (Interaction Map, zero-padded sequential starting at 01). This makes downstream cross-references from phases 231-236 greppable.

**Step 6 — Categorize by consumer phase.** Group rows under these sub-headings BEFORE the master table, so the map is navigable:
```
### 2.1 Earlybird-related chains (Phase 231 consumers)
### 2.2 Decimator-related chains (Phase 232 consumers)
### 2.3 Jackpot/BAF/Entropy-related chains (Phase 233 consumers)
### 2.4 Quest/Boon/Misc chains (Phase 234 consumers)
### 2.5 RNG/Phase-transition chains (Phase 235 consumers)
### 2.6 All other cross-module chains (general audit interest)
```

Each subsection has its own subset of the IM-NN rows — but the IM-NN numbering is global across subsections (no restart per subsection).

**Step 7 — Section closing totals.** At the end of Section 2, emit:
`Interaction map totals — total rows: {N}, delegatecall: {n}, direct: {n}, self-call: {n}, selector-call: {n}; chains per consumer phase: 231:{n}, 232:{n}, 233:{n}, 234:{n}, 235:{n}, other:{n}`

**Important: If a category has zero chains, emit a literal line "(no chains in this category)" under the subsection heading — do NOT silently omit the subsection. All six subsections MUST exist in the file.**

Per D-09 reminder: intra-module calls (e.g., a private helper inside DegenerusGameJackpotModule.sol called by another function in the same file) are NOT catalogued here. They are covered by Section 1.
  </action>
  <verify>
    <automated>awk '/^## 2\. Cross-Module Interaction Map/,/^## 3\. Interface Drift Catalog/' .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md > /tmp/sec2.md &amp;&amp; grep -q "^### 2.1 Earlybird-related chains" /tmp/sec2.md &amp;&amp; grep -q "^### 2.6 All other cross-module chains" /tmp/sec2.md &amp;&amp; [ "$(grep -cE '^### 2\.[1-6]' /tmp/sec2.md)" -eq 6 ] &amp;&amp; grep -qE "IM-[0-9]{2}" /tmp/sec2.md &amp;&amp; grep -qE "Caller Function.*Callee Function.*Call Type.*Commit SHA.*What Changed" /tmp/sec2.md &amp;&amp; grep -q "Interaction map totals —" /tmp/sec2.md</automated>
  </verify>
  <acceptance_criteria>
    - Section 2 contains all 6 subsections matching `^### 2\.[1-6] ` (Earlybird, Decimator, Jackpot/BAF/Entropy, Quest/Boon/Misc, RNG/Phase-transition, All other).
    - Every subsection either contains at least one IM-NN row OR a literal line `(no chains in this category)` — no silent empty subsection.
    - The master table (which may be rendered once per subsection or once globally — executor choice) has column header line containing all five tokens: `Caller Function`, `Callee Function`, `Call Type`, `Commit SHA`, `What Changed` in that order.
    - Every IM-NN row has all five columns populated.
    - Every Call Type cell is one of exactly: `delegatecall`, `direct`, `self-call`, `selector-call`.
    - Every Commit SHA cell contains a 7-char SHA matching one of the 10 in-scope commits.
    - Every row's caller OR callee function appears in Section 1 with verdict NEW or MODIFIED (spot-check 3 random rows — acceptance relies on the task 2 source of truth).
    - At least one row in each of subsections 2.1 through 2.5 cites the primary commit(s) for that consumer phase (earlybird rows must cite f20a2b5e or 20a951df; decimator rows must cite 3ad0f8d3, 67031e7d, or 858d83e4; jackpot/BAF rows must cite 104b5d42 or 52242a10; quest/boon rows must cite d5284be5 or e0a7f7bc; RNG/phase-transition rows must cite 2471f8e7 or 52242a10). Empty subsection satisfies via the `(no chains in this category)` line.
    - Section ends with the literal line starting `Interaction map totals —` containing per-call-type and per-consumer-phase counts.
    - No `contracts/` or `test/` files modified.
  </acceptance_criteria>
  <done>DELTA-02 satisfied: every cross-module call chain introduced or modified by the 10-commit delta is catalogued with Caller, Callee, Call Type, Commit SHA, and What Changed; rows grouped by downstream-consumer phase and IM-NN numbered for greppable downstream reference.</done>
</task>

<task type="auto">
  <name>Task 4: Populate Section 3 — Interface Drift Catalog (DELTA-03)</name>
  <files>.planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md</files>
  <read_first>
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md (extend Section 3 in place)
    - contracts/interfaces/IDegenerusGame.sol (current HEAD)
    - contracts/interfaces/IDegenerusQuests.sol (current HEAD)
    - contracts/interfaces/IDegenerusGameModules.sol (current HEAD)
    - contracts/DegenerusGame.sol (implementer of IDegenerusGame)
    - contracts/DegenerusQuests.sol (implementer of IDegenerusQuests)
    - contracts/modules/DegenerusGameAdvanceModule.sol (selector-impl of IDegenerusGameModules)
    - contracts/modules/DegenerusGameJackpotModule.sol (selector-impl of IDegenerusGameModules)
    - contracts/modules/DegenerusGameMintModule.sol (selector-impl of IDegenerusGameModules)
    - contracts/modules/DegenerusGameWhaleModule.sol (selector-impl of IDegenerusGameModules)
    - contracts/modules/DegenerusGameDecimatorModule.sol (selector-impl of IDegenerusGameModules)
  </read_first>
  <action>
Populate `## 3. Interface Drift Catalog` in-place inside 230-01-DELTA-MAP.md. Per D-10, this is a per-method PASS/FAIL catalog across the 3 interfaces.

**Step 1 — Enumerate interface signatures at HEAD.** For each interface, run:
```
grep -nE "^\s*function\s+" contracts/interfaces/IDegenerusGame.sol
grep -nE "^\s*function\s+" contracts/interfaces/IDegenerusQuests.sol
grep -nE "^\s*function\s+" contracts/interfaces/IDegenerusGameModules.sol
```
Record every function declaration with its full signature (name + param types + param names + mutability + return types).

**Step 2 — Match each signature to its implementer(s).** For each interface method:
- `IDegenerusGame.*` → search `contracts/DegenerusGame.sol` for `function <name>(` at the same param-type arity.
- `IDegenerusQuests.*` → search `contracts/DegenerusQuests.sol` for `function <name>(`.
- `IDegenerusGameModules.*` → search all 5 module files for `function <name>(`. A method in IDegenerusGameModules may have ONE implementer (the module whose selector Game delegatecalls to) or ZERO implementers (if the selector is only used for raw-selector encoding and no module exposes the function body) — both outcomes are valid; the catalog records reality.

**Step 3 — Emit the master catalog table.** Use EXACTLY these columns per D-10:
`Interface | Method Signature | Implementer Contract | Verdict | Notes`

- `Interface`: `IDegenerusGame`, `IDegenerusQuests`, or `IDegenerusGameModules`.
- `Method Signature`: The full signature at interface declaration (copy verbatim from the interface file, e.g., `function claimTerminalDecimatorJackpot() external`).
- `Implementer Contract`: The contract + function name where the implementation lives (e.g., `DegenerusGame.claimTerminalDecimatorJackpot`). If zero implementers, record `(none — selector-only reference)`. If multiple, list all.
- `Verdict`: PASS / FAIL.
  - PASS: implementer signature matches interface declaration at HEAD (identical function name, identical param types in order, identical mutability, identical return types).
  - FAIL: any mismatch (param type order, return type, mutability, function missing from implementer when interface declares it, or implementer has function but interface is missing the declaration). Missing-in-interface counts as FAIL only if the function is reachable via an abi.encodeWithSelector on that interface; otherwise note "not required" in Notes.
- `Notes`: For PASS rows, one-liner confirming signature parity. For FAIL rows, name the specific mismatch and cite file:line on both sides.

**Step 4 — Group the table by interface.** Create three sub-sections:
```
### 3.1 IDegenerusGame → DegenerusGame.sol
### 3.2 IDegenerusQuests → DegenerusQuests.sol
### 3.3 IDegenerusGameModules → 5 module contracts (selector map)
```
Each subsection contains its own PASS/FAIL table with all methods of that interface.

**Step 5 — Row ID convention.** Prefix each row with an `ID-NN` ID (Interface Drift, zero-padded sequential starting at 01, global across the three subsections).

**Step 6 — Smoke-test the Makefile gate.** Because `make check-interfaces` exists and runs against source at HEAD, run:
```
make check-interfaces 2>&1 | tee /tmp/check-interfaces.out
```
Record the gate's verdict (PASS / FAIL) and exit code in the catalog under a subsection `### 3.4 Automated gate corroboration`. If the gate reports PASS, note: "Automated `check-interfaces` gate PASSES at HEAD — corroborates manual per-method catalog (any FAIL rows above must explain why the gate didn't catch them, or be downgraded to catalog-only observations if the gate is correctly silent)." If the gate reports FAIL, note the failing lines and cross-reference to the ID-NN rows that document the same failure.

Also run `forge build` and record the outcome under the same subsection as a smoke test that the source tree compiles at HEAD. (Catalog work doesn't touch code; this is just a sanity check that the audit surface the catalog describes actually builds.)

**Step 7 — Section closing totals.** End Section 3 with:
`Interface drift totals — IDegenerusGame: {n_methods} methods, {n_pass} PASS, {n_fail} FAIL; IDegenerusQuests: {...}; IDegenerusGameModules: {...}; automated check-interfaces gate: {PASS|FAIL}; forge build: {PASS|FAIL}`

If any FAIL row exists, add a one-paragraph interpretation note explaining whether the FAIL is acceptable (e.g., interface declares selector-only method with no required impl) or is a genuine drift finding that downstream phases should treat as a Phase 236 finding candidate. Phase 230 does NOT emit finding IDs (see `<interfaces>` block); it describes drift and defers finding-ID assignment to Phase 236.
  </action>
  <verify>
    <automated>awk '/^## 3\. Interface Drift Catalog/,/^## 4\. Consumer Index/' .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md > /tmp/sec3.md &amp;&amp; grep -q "^### 3.1 IDegenerusGame" /tmp/sec3.md &amp;&amp; grep -q "^### 3.2 IDegenerusQuests" /tmp/sec3.md &amp;&amp; grep -q "^### 3.3 IDegenerusGameModules" /tmp/sec3.md &amp;&amp; grep -q "^### 3.4 Automated gate corroboration" /tmp/sec3.md &amp;&amp; grep -qE "Interface \| Method Signature \| Implementer Contract \| Verdict \| Notes" /tmp/sec3.md &amp;&amp; grep -qE "ID-[0-9]{2}" /tmp/sec3.md &amp;&amp; grep -qE "(PASS|FAIL)" /tmp/sec3.md &amp;&amp; grep -q "Interface drift totals —" /tmp/sec3.md</automated>
  </verify>
  <acceptance_criteria>
    - Section 3 contains 4 subsections matching `^### 3\.[1-4] ` (one per interface + one for automated gate corroboration).
    - Every subsection 3.1-3.3 has its own PASS/FAIL table with column header line containing all 5 tokens: `Interface`, `Method Signature`, `Implementer Contract`, `Verdict`, `Notes` in that order.
    - Every table row has all 5 columns populated.
    - Every Verdict cell is exactly `PASS` or `FAIL` (no other values).
    - Every method currently declared in `contracts/interfaces/IDegenerusGame.sol`, `IDegenerusQuests.sol`, and `IDegenerusGameModules.sol` at HEAD has exactly one row in the respective subsection (use `grep -cE "^\s*function" contracts/interfaces/IDegenerusGame.sol` etc. to count; each row count MUST match or be documented as why — e.g., overloaded signatures).
    - Every row has an `ID-NN` (zero-padded, global sequential) prefix.
    - Subsection 3.4 records both the `make check-interfaces` PASS/FAIL outcome and the `forge build` PASS/FAIL outcome, each with the command invocation and exit code.
    - Section ends with the literal line starting `Interface drift totals —` containing counts per interface and both automated-tool outcomes.
    - If any FAIL row exists, a one-paragraph interpretation note is present explaining whether it's acceptable drift or a finding candidate for Phase 236.
    - No `contracts/` or `test/` files modified.
  </acceptance_criteria>
  <done>DELTA-03 satisfied: every method in IDegenerusGame / IDegenerusQuests / IDegenerusGameModules has a PASS/FAIL row against its implementer at HEAD; automated `make check-interfaces` gate invoked and outcome recorded; `forge build` smoke test recorded.</done>
</task>

<task type="auto">
  <name>Task 5: Smoke-test remaining Makefile gates (check-delegatecall, check-raw-selectors)</name>
  <files>.planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md</files>
  <read_first>
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md (read Section 3.4 from task 4 — extend with two more gate outcomes OR create a parallel Section 3.5)
    - /home/zak/Dev/PurgeGame/degenerus-audit/Makefile (lines around `check-interfaces`, `check-delegatecall`, `check-raw-selectors` — confirmed via scouting that targets exist at lines 9, 20, 30 respectively)
    - /home/zak/Dev/PurgeGame/degenerus-audit/scripts/check-delegatecall-alignment.sh (read to understand what PASS means)
    - /home/zak/Dev/PurgeGame/degenerus-audit/scripts/check-raw-selectors.sh (read to understand what PASS means)
  </read_first>
  <action>
Extend Section 3.4 (or add a new `### 3.5 Additional automated gate corroboration`) inside 230-01-DELTA-MAP.md to record the outcomes of the two remaining Makefile gates that were NOT run in task 4.

**Step 1 — Run both gates.** Run each gate, capturing stdout + stderr + exit code:
```
make check-delegatecall 2>&1 | tee /tmp/check-delegatecall.out; echo "EXIT=$?" >> /tmp/check-delegatecall.out
make check-raw-selectors 2>&1 | tee /tmp/check-raw-selectors.out; echo "EXIT=$?" >> /tmp/check-raw-selectors.out
```

**Step 2 — Record outcomes.** In the catalog, under the appropriate subsection, add a short block for each gate:
- Command invocation (verbatim)
- Exit code
- PASS/FAIL summary (PASS if exit 0 AND the script's own PASS indicator present in stdout)
- If FAIL: quote the specific failing line(s) from the gate's stdout
- If PASS: one-line confirmation that the gate corroborates no call-site drift exists beyond what Section 1/2 already documents

**Step 3 — Cross-reference with interaction map.** If `check-delegatecall` passes, add one line under Section 2 (insert after the totals block): `Delegatecall corroboration — `make check-delegatecall` PASSES at HEAD: all 43 delegatecall sites align with the selector→address map per v27.0 Phase 220 methodology.` If the gate reports a new count (not 43), record the actual count. If it fails, quote the failing row and cross-reference the IM-NN row(s) in Section 2 that describe the same delegatecall site.

Per the READ-only milestone rule: these gates must not modify any file under `contracts/` or `test/`. Confirm with `git status` after running both gates — output should show only `.planning/` modifications. If any `contracts/` or `test/` file appears as modified, STOP and report — a gate has violated the milestone rule.

**Step 4 — Final scaffold totals line.** After both gates are recorded, add a one-line summary at the end of the gate-corroboration subsection:
`Automated gates at HEAD — check-interfaces: {PASS|FAIL}, check-delegatecall: {PASS|FAIL}, check-raw-selectors: {PASS|FAIL}, forge build: {PASS|FAIL}`

This line makes all four smoke tests grep-greppable for downstream verification.
  </action>
  <verify>
    <automated>grep -q "check-delegatecall" .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md &amp;&amp; grep -q "check-raw-selectors" .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md &amp;&amp; grep -qE "Automated gates at HEAD —" .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md &amp;&amp; [ -z "$(git status --porcelain contracts/ test/ 2>/dev/null)" ]</automated>
  </verify>
  <acceptance_criteria>
    - Catalog records `make check-delegatecall` invocation, exit code, PASS/FAIL outcome, and (on FAIL) the failing stdout line(s).
    - Catalog records `make check-raw-selectors` invocation, exit code, PASS/FAIL outcome, and (on FAIL) the failing stdout line(s).
    - Catalog contains a literal line starting `Automated gates at HEAD —` listing PASS/FAIL for all four smoke tests (check-interfaces, check-delegatecall, check-raw-selectors, forge build).
    - `git status --porcelain contracts/ test/` returns empty (no `contracts/` or `test/` file was modified by running the gates — READ-only milestone rule).
    - If check-delegatecall is PASS, Section 2 contains a corroboration line referencing the gate.
    - No finding IDs assigned (Phase 230 does not emit F-29-NN).
  </acceptance_criteria>
  <done>Two remaining Makefile gates run and recorded; milestone READ-only rule verified; "Automated gates at HEAD" rollup line present for fast downstream consumption; all four automated corroborations in place.</done>
</task>

<task type="auto">
  <name>Task 6: Populate Section 4 — Consumer Index (D-11) and create 230-01-SUMMARY.md</name>
  <files>.planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md, .planning/phases/230-delta-extraction-scope-map/230-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md (read Sections 0-3 to collect section/row anchors for the index)
    - .planning/REQUIREMENTS.md (enumerate all 25 requirement IDs)
    - .planning/ROADMAP.md (Phase 230 success criteria 1-4 — verify all 4 are satisfied)
    - .planning/phases/230-delta-extraction-scope-map/230-CONTEXT.md (D-11 Consumer Index scope)
    - $HOME/.claude/get-shit-done/templates/summary.md (standard GSD SUMMARY template — if missing, use the conventional format: Goal, What Was Done, Artifacts, Counts, Handoff)
  </read_first>
  <action>
**Part A — Populate Section 4: Consumer Index.**

Per D-11, Section 4 maps every v29.0 requirement to the specific sections/rows of 230-01-DELTA-MAP.md it will cite.

Create a master table with EXACTLY these columns:
`Requirement ID | Phase | Section/Row Reference | What the consumer will use |`

Populate ALL 25 requirement IDs (not just DELTA-01..03). For each:
- `Requirement ID`: DELTA-01, DELTA-02, DELTA-03, EBD-01..03, DCM-01..03, JKP-01..03, QST-01..03, CONS-01..02, RNG-01..02, TRNX-01, REG-01..02, FIND-01..03.
- `Phase`: 230, 231, 232, 233, 234, 235, or 236.
- `Section/Row Reference`: Specific section anchors and row IDs in this doc. Examples:
  - DELTA-01 → `§1 (all 12 subsections)`
  - DELTA-02 → `§2 (all 6 subsections, IM-01..IM-NN)`
  - DELTA-03 → `§3 (3.1, 3.2, 3.3, ID-01..ID-NN)`
  - EBD-01 → `§1.2 (Jackpot module earlybird fns), §1.6 (Game entry point), §2.1 (IM-NN earlybird chains), commit f20a2b5e rows`
  - EBD-02 → `§1.2, §2.1, commit 20a951df rows`
  - EBD-03 → `§1.2, §2.1 (combined state machine = earlybird chains + purchase finalize)`
  - DCM-01 → `§1.3 (Decimator module), §1.8 (BurnieCoin), §2.2, commit 3ad0f8d3 rows`
  - DCM-02 → `§1.3, §2.2, commit 67031e7d rows`
  - DCM-03 → `§1.3, §1.6 (Game claimTerminalDecimatorJackpot), §2.2, commit 858d83e4 rows`
  - JKP-01 → `§1.2 (jackpot module BAF path), §2.3, commit 104b5d42 rows`
  - JKP-02 → `§1.2, §1.11 (IDegenerusGameModules drift), §2.3, commit 52242a10 rows`
  - JKP-03 → `§1.2, §2.3 (cross-path bonusTraitsPacked chains)`
  - QST-01 → `§1.7 (DegenerusQuests mint_ETH), §1.4 (Mint module call site), §1.12 (IDegenerusQuests drift), §2.4, commit d5284be5 rows`
  - QST-02 → `§1.9 (Storage boonPacked accessor), §1.6 (Game forwarder), §1.10 (IDegenerusGame drift), §2.4, commit e0a7f7bc rows`
  - QST-03 → `§1.8 (BurnieCoin), §2.2 (decimator-burn-key chains), commit 3ad0f8d3 rows`
  - CONS-01 → `§1 (all pool-mutating functions), §2 (pool-SSTORE-producing chains), §3 (pool-related interface drift)`
  - CONS-02 → `§1.8 (BurnieCoin), §1.4, §1.7 (mint/burn sites), §2`
  - RNG-01 → `§1.2 (earlybird bonus-trait roll), §1.2 (BAF sentinel), §1.2 (processFutureTicketBatch), §2.3, §2.5`
  - RNG-02 → `§1 (player-controllable state reads inside new/modified RNG consumers), §2.5, §3.4-3.5 (gate corroborations)`
  - TRNX-01 → `§1.1 (DegenerusGameAdvanceModule _unlockRng removal), §2.5, commit 2471f8e7 rows`
  - REG-01 → `§1 (full delta inventory — regression sweep against v27.0 INFO findings + KNOWN-ISSUES)`
  - REG-02 → `§1 (full delta inventory — regression sweep against v25.0 + v26.0 findings)`
  - FIND-01 → `entire doc (finding-candidate anchors for Phase 236 consolidation)`
  - FIND-02 → `§4 (this index anchors KNOWN-ISSUES updates to specific F-29-NN IDs during Phase 236)`
  - FIND-03 → `entire doc (deliverable source for executive summary table in Phase 236)`
- `What the consumer will use`: One-sentence description of how the downstream phase will consume the referenced section (e.g., "Phase 231 adversarial audit cites §1.2 rows to enumerate every earlybird-refactor function requiring a per-function verdict").

Add a closing line: `Requirements covered: 25 / 25`. Count must match exactly. If any are missing, task fails acceptance.

**Part B — Section-4 success-criteria confirmation.**

After the master table, add a subsection `### 4.1 ROADMAP Phase 230 success-criteria confirmation` with a 4-item checklist. For each of the 4 criteria in `.planning/ROADMAP.md` Phase 230 section, cite the section(s) of 230-01-DELTA-MAP.md that satisfy it:
1. Function-level changelog across 10 commits → §1 (12 subsections, 10 SHAs cited) — confirmed
2. Cross-module interaction map within 12 files → §2 (6 subsections, IM-NN rows) — confirmed
3. Interface drift catalog for IDegenerusGame / IDegenerusQuests / IDegenerusGameModules → §3 (3 subsections, ID-NN rows, automated gate corroboration in §3.4-3.5) — confirmed
4. Downstream phases 231-235 can reference this as authoritative scope with no additional discovery → §4 Consumer Index (25/25 requirements mapped) — confirmed

**Part C — Create 230-01-SUMMARY.md.**

Create `.planning/phases/230-delta-extraction-scope-map/230-01-SUMMARY.md` per the standard GSD template. Required sections:
- `# Phase 230-01 Summary`
- `## Goal` — one paragraph restating phase goal from ROADMAP.
- `## What Was Done` — bullet list of task outcomes (tasks 1-6).
- `## Artifacts` — list of files created or modified in this plan (the delta map + this summary).
- `## Counts` — table with: files-in-scope=12, commits-in-scope=10, §1-NEW, §1-MODIFIED, §1-DELETED, §1-UNCHANGED, §2-IM-rows, §3-ID-rows-PASS, §3-ID-rows-FAIL, §4-requirements-covered=25.
- `## Automated Gate Outcomes` — restate the four Makefile/forge outcomes from §3.4-3.5.
- `## Handoff to Phases 231-236` — one paragraph directing downstream planners to the Consumer Index (§4) as their scope-entry point. Reminds them: 230-01-DELTA-MAP.md is READ-only per D-06; any gap is recorded via scope-guard deferral, not in-place edit.
- `## Known Non-Issues` — if any FAIL rows in §3 were judged acceptable-drift rather than finding candidates, list them here so Phase 236 doesn't re-open.

**Part D — Git commit.**

Do NOT modify anything under `contracts/` or `test/`. Final `git status --porcelain contracts/ test/` MUST be empty. This task does NOT commit; execute-plan handles commit orchestration.
  </action>
  <verify>
    <automated>awk '/^## 4\. Consumer Index/,EOF' .planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md > /tmp/sec4.md &amp;&amp; grep -qE "Requirement ID \| Phase \| Section/Row Reference \| What the consumer will use" /tmp/sec4.md &amp;&amp; for req in DELTA-01 DELTA-02 DELTA-03 EBD-01 EBD-02 EBD-03 DCM-01 DCM-02 DCM-03 JKP-01 JKP-02 JKP-03 QST-01 QST-02 QST-03 CONS-01 CONS-02 RNG-01 RNG-02 TRNX-01 REG-01 REG-02 FIND-01 FIND-02 FIND-03; do grep -q "$req" /tmp/sec4.md || { echo "MISSING REQUIREMENT: $req"; exit 1; }; done &amp;&amp; grep -q "Requirements covered: 25 / 25" /tmp/sec4.md &amp;&amp; grep -q "^### 4.1 ROADMAP Phase 230 success-criteria confirmation" /tmp/sec4.md &amp;&amp; test -f .planning/phases/230-delta-extraction-scope-map/230-01-SUMMARY.md &amp;&amp; grep -q "^# Phase 230-01 Summary" .planning/phases/230-delta-extraction-scope-map/230-01-SUMMARY.md &amp;&amp; grep -qi "handoff to phases 231-236" .planning/phases/230-delta-extraction-scope-map/230-01-SUMMARY.md &amp;&amp; [ -z "$(git status --porcelain contracts/ test/ 2>/dev/null)" ]</automated>
  </verify>
  <acceptance_criteria>
    - Section 4 contains a table with column header `Requirement ID | Phase | Section/Row Reference | What the consumer will use`.
    - Every one of the 25 v29.0 requirement IDs (DELTA-01..03, EBD-01..03, DCM-01..03, JKP-01..03, QST-01..03, CONS-01..02, RNG-01..02, TRNX-01, REG-01..02, FIND-01..03) appears in exactly one row of Section 4.
    - Every Section 4 row has all 4 columns populated; no empty `Section/Row Reference` cells.
    - Section 4 contains the literal line `Requirements covered: 25 / 25`.
    - Section 4 contains subsection `### 4.1 ROADMAP Phase 230 success-criteria confirmation` with a 4-item checklist, each item explicitly citing a section of 230-01-DELTA-MAP.md that satisfies the criterion.
    - File `.planning/phases/230-delta-extraction-scope-map/230-01-SUMMARY.md` exists with `# Phase 230-01 Summary` as the first heading.
    - 230-01-SUMMARY.md contains all 7 required sections: Goal, What Was Done, Artifacts, Counts, Automated Gate Outcomes, Handoff to Phases 231-236, Known Non-Issues.
    - 230-01-SUMMARY.md Counts table shows files-in-scope=12, commits-in-scope=10, §4-requirements-covered=25 (other counts pulled from actual task 2-5 outputs).
    - `git status --porcelain contracts/ test/` returns empty — no production code modified.
    - No finding IDs (`F-29-NN`) emitted in 230-01-DELTA-MAP.md — Phase 230 is a scope catalog, not a findings pass.
  </acceptance_criteria>
  <done>DELTA-01, DELTA-02, DELTA-03 all satisfied AND Consumer Index maps all 25 v29.0 requirements to specific sections of this doc. 230-01-SUMMARY.md created per GSD convention. READ-only milestone rule verified (no `contracts/` or `test/` modifications). Catalog is ready to serve as the authoritative scope document for phases 231-236 per D-06 read-only policy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 230 produces ONLY markdown documentation under `.planning/`. No Solidity code is modified, no test files are added, no deployment artifacts are touched. Per D-06 the catalog becomes read-only after commit. The only "inputs" are the git history (immutable) and the existing HEAD source (which this plan does not mutate).

Trust boundary: `git diff` output → markdown catalog. The boundary is authorial integrity — the risk is the catalog drifting from reality (silent omissions of changed functions, wrong commit attribution, missed cross-module chains). This is mitigated by the automated `grep`-based acceptance criteria on every task and by cross-corroboration via `make check-interfaces`, `make check-delegatecall`, and `make check-raw-selectors` smoke tests.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-230-01 | I (Information Disclosure) | 230-01-DELTA-MAP.md | accept | Document describes already-committed open-source code; no secrets, no unreleased code. No mitigation required. |
| T-230-02 | T (Tampering) | git diff input | accept | Baseline commit SHA `14cb45e1` is immutable; tampering would require rewriting published git history which is not in scope for this audit milestone. |
| T-230-03 | R (Repudiation) | Catalog authorial integrity | mitigate | Every entry cites a 7-char commit SHA and a specific file:line anchor. Any Phase 231-236 consumer can verify a catalog row by re-running the cited `git show <sha> -- <path>` command against the same baseline. |
| T-230-04 | I (Information Disclosure) | Silent-omission risk (missing changed function or cross-module chain) | mitigate | Task-level acceptance criteria require `git diff --stat`-derived counts to match catalog-row counts; `make check-interfaces` / `check-delegatecall` / `check-raw-selectors` gates run as automated corroboration in §3.4-3.5; Consumer Index requires exactly 25 / 25 requirement mappings. Any omission fails verify. |
| T-230-05 | T (Tampering) | Unauthorized `contracts/` or `test/` modification | mitigate | Every task's acceptance criteria include `git status --porcelain contracts/ test/` must be empty. The READ-only milestone rule is enforced at verify-time, not trust-based. |
</threat_model>

<verification>
Phase-level verification after all 6 tasks complete:

1. **Section scaffold check:** 230-01-DELTA-MAP.md contains all five top-level `##` headings in order: Section 0 (Per-File Delta Baseline), Section 1 (Function-Level Changelog), Section 2 (Cross-Module Interaction Map), Section 3 (Interface Drift Catalog), Section 4 (Consumer Index). Verify via `grep -nE "^## [0-4]\." 230-01-DELTA-MAP.md`.

2. **Commit SHA coverage check:** All 10 in-scope commit SHAs (2471f8e7, 52242a10, f20a2b5e, 3ad0f8d3, 104b5d42, 67031e7d, 858d83e4, d5284be5, e0a7f7bc, 20a951df) appear in the catalog. `for sha in 2471f8e7 52242a10 f20a2b5e 3ad0f8d3 104b5d42 67031e7d 858d83e4 d5284be5 e0a7f7bc 20a951df; do grep -q "$sha" 230-01-DELTA-MAP.md; done` — all must return exit 0.

3. **File coverage check:** All 12 in-scope files each have a dedicated `### 1.N` subsection. `grep -c "^### 1\." 230-01-DELTA-MAP.md` must equal 12.

4. **Requirement coverage check:** All 25 v29.0 requirement IDs appear in Section 4. `for req in DELTA-01 DELTA-02 DELTA-03 EBD-01 EBD-02 EBD-03 DCM-01 DCM-02 DCM-03 JKP-01 JKP-02 JKP-03 QST-01 QST-02 QST-03 CONS-01 CONS-02 RNG-01 RNG-02 TRNX-01 REG-01 REG-02 FIND-01 FIND-02 FIND-03; do grep -q "$req" 230-01-DELTA-MAP.md; done` — all must return exit 0. Also `grep -q "Requirements covered: 25 / 25" 230-01-DELTA-MAP.md`.

5. **Automated gate corroboration check:** The catalog records outcomes (PASS/FAIL + exit code) for all three Makefile gates (check-interfaces, check-delegatecall, check-raw-selectors) plus `forge build`. `grep -q "Automated gates at HEAD —" 230-01-DELTA-MAP.md`.

6. **READ-only integrity check:** `git status --porcelain contracts/ test/` returns empty — no production code was modified while creating this catalog.

7. **Summary file check:** `.planning/phases/230-delta-extraction-scope-map/230-01-SUMMARY.md` exists with the 7 required sections per GSD template.

8. **Finding-ID absence check:** No `F-29-NN` strings in 230-01-DELTA-MAP.md — Phase 230 is a scope catalog, not a findings pass; finding-ID assignment belongs to Phase 236.
</verification>

<success_criteria>
Phase 230 is complete when:
1. `.planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md` exists with all 5 top-level sections in the D-05 order.
2. All 10 in-scope commit SHAs appear in Section 1.
3. All 12 in-scope files have a dedicated subsection in Section 1.
4. Section 2 has all 6 consumer-phase subsections (Earlybird / Decimator / Jackpot-BAF-Entropy / Quest-Boon-Misc / RNG-Phase-transition / All other) — empty subsections are marked `(no chains in this category)` not silently omitted.
5. Section 3 has all 3 interface subsections (IDegenerusGame / IDegenerusQuests / IDegenerusGameModules) plus subsections 3.4-3.5 recording the 4 automated corroboration outcomes (make check-interfaces, make check-delegatecall, make check-raw-selectors, forge build).
6. Section 4 Consumer Index covers all 25 v29.0 requirement IDs with the literal closing line `Requirements covered: 25 / 25` and subsection 4.1 confirming all 4 ROADMAP Phase 230 success criteria.
7. `.planning/phases/230-delta-extraction-scope-map/230-01-SUMMARY.md` exists with the 7 standard sections.
8. `git status --porcelain contracts/ test/` returns empty (READ-only milestone rule intact).
9. No `F-29-NN` finding IDs emitted (finding assignment is Phase 236's job).
</success_criteria>

<output>
After completion, the plan produces:
- `.planning/phases/230-delta-extraction-scope-map/230-01-DELTA-MAP.md` — the authoritative v29.0 audit-surface catalog
- `.planning/phases/230-delta-extraction-scope-map/230-01-SUMMARY.md` — standard GSD plan summary

Downstream consumers (phases 231-236) enter via the Consumer Index (§4) and cite specific section/row anchors in their own PLAN.md frontmatter or discovery sections. Per D-06, 230-01-DELTA-MAP.md becomes READ-only after commit; any downstream gap is recorded as a scope-guard deferral in that phase's own SUMMARY rather than by editing this file in place.
</output>
