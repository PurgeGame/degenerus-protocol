---
phase: 168-storage-repack
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/storage/DegenerusGameStorage.sol
  - contracts/modules/DegenerusGameJackpotModule.sol
  - contracts/modules/DegenerusGameGameOverModule.sol
  - contracts/DegenerusGame.sol
autonomous: true
requirements: [STOR-01, STOR-02, STOR-03, STOR-04]

must_haves:
  truths:
    - "ticketsFullyProcessed and gameOverPossible read/write correctly from slot 0"
    - "currentPrizePool reads/writes via _getCurrentPrizePool()/_setCurrentPrizePool() helpers returning uint256"
    - "Slot 1 contains purchaseStartDay + ticketWriteSlot + prizePoolFrozen + currentPrizePool with no byte gaps"
    - "All slot header comments match actual layout"
    - "currentPrizePoolView() still returns uint256"
  artifacts:
    - path: "contracts/storage/DegenerusGameStorage.sol"
      provides: "Repacked storage layout and currentPrizePool helpers"
      contains: "_getCurrentPrizePool"
    - path: "contracts/modules/DegenerusGameJackpotModule.sol"
      provides: "currentPrizePool access via helpers"
      contains: "_getCurrentPrizePool"
    - path: "contracts/modules/DegenerusGameGameOverModule.sol"
      provides: "currentPrizePool zeroing via helper"
      contains: "_setCurrentPrizePool"
    - path: "contracts/DegenerusGame.sol"
      provides: "View function using helper"
      contains: "_getCurrentPrizePool"
  key_links:
    - from: "contracts/modules/DegenerusGameJackpotModule.sol"
      to: "contracts/storage/DegenerusGameStorage.sol"
      via: "_getCurrentPrizePool()/_setCurrentPrizePool() helpers"
      pattern: "_getCurrentPrizePool\\(\\)|_setCurrentPrizePool\\("
    - from: "contracts/modules/DegenerusGameGameOverModule.sol"
      to: "contracts/storage/DegenerusGameStorage.sol"
      via: "_setCurrentPrizePool(0)"
      pattern: "_setCurrentPrizePool\\(0\\)"
---

<objective>
Repack DegenerusGameStorage.sol slots 0-2: move ticketsFullyProcessed + gameOverPossible into slot 0 (filling 2-byte padding), downsize currentPrizePool from uint256 to uint128 packed into slot 1, eliminate old slot 2, add getter/setter helpers, update all consuming contracts, and fix all slot header comments.

Purpose: Eliminate wasted storage slot, save gas on co-reads, prepare for module consolidation.
Output: Fully repacked storage with all contracts compiling cleanly against new layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/168-storage-repack/168-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Key helper pattern to follow — from DegenerusGameStorage.sol -->

From contracts/storage/DegenerusGameStorage.sol (prizePoolsPacked helpers — use as template):
```solidity
function _setPrizePools(uint128 next, uint128 future) internal {
    prizePoolsPacked = uint256(future) << 128 | uint256(next);
}

function _getPrizePools() internal view returns (uint128 next, uint128 future) {
    uint256 packed = prizePoolsPacked;
    next = uint128(packed);
    future = uint128(packed >> 128);
}
```

currentPrizePool access sites to replace (from grep):
- JackpotModule: lines 317, 353, 433, 721, 732, 737, 747
- GameOverModule: lines 133, 145
- DegenerusGame: lines 2037, 2063
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repack storage variables + add currentPrizePool helpers + update slot comments</name>
  <files>contracts/storage/DegenerusGameStorage.sol</files>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol (full file — current state with poolConsolidationDone already removed)
    - .planning/phases/168-storage-repack/168-CONTEXT.md (D-01 through D-05 locked decisions)
    - .planning/research/PITFALLS.md (Pitfall 7: slot 0 byte overflow, Pitfall 8: slot 1 packing errors)
  </read_first>
  <action>
IMPORTANT: Before ANY changes, snapshot the current layout:
```
forge inspect DegenerusGameStorage storage-layout --json > /tmp/layout-before.json
```

Then make these changes to DegenerusGameStorage.sol:

**Step 1: Move ticketsFullyProcessed + gameOverPossible into slot 0 (per D-05)**

Move the declarations of `ticketsFullyProcessed` (bool) and `gameOverPossible` (bool) from slot 1 section to AFTER `compressedJackpotFlag` in slot 0. This fills the 2-byte padding exactly. Do NOT change the NatSpec on these variables — only move them.

After the move, slot 0 declaration order must be:
levelStartTime, dailyIdx, rngRequestTime, level, jackpotPhaseFlag, jackpotCounter, lastPurchaseDay, decWindowOpen, rngLockedFlag, phaseTransitionActive, gameOver, dailyJackpotCoinTicketsPending, compressedJackpotFlag, ticketsFullyProcessed, gameOverPossible

**Step 2: Downsize currentPrizePool from uint256 to uint128 (per D-02)**

Change the declaration in the slot 2 section:
- FROM: `uint256 internal currentPrizePool;`
- TO: `uint128 internal currentPrizePool;`

The compiler will pack it into slot 1 after prizePoolFrozen (the last remaining slot 1 variable after the two bools moved out).

After this change, slot 1 declaration order must be:
purchaseStartDay (uint48), ticketWriteSlot (uint8), prizePoolFrozen (bool), currentPrizePool (uint128)

Note: prizePoolFrozen naturally shifts from byte offset 8 to byte 7 because ticketsFullyProcessed and gameOverPossible no longer precede it. The compiler handles this automatically — no manual shift needed.

**Step 3: Add _getCurrentPrizePool() / _setCurrentPrizePool() helpers (per D-01)**

Add these helpers near the existing packed prize pool helpers section (after _setFuturePrizePool, around line 772). The helpers read/write the Solidity variable directly (NOT assembly) and return/accept uint256 for ABI compatibility:

```solidity
/// @dev Returns the current prize pool value as uint256.
///      Reads the uint128 packed variable and widens to uint256.
function _getCurrentPrizePool() internal view returns (uint256) {
    return uint256(currentPrizePool);
}

/// @dev Sets the current prize pool value.
///      Narrows from uint256 to uint128. Safe because currentPrizePool
///      can never exceed total ETH supply (~1.2e26 wei << uint128 max ~3.4e38 wei).
function _setCurrentPrizePool(uint256 val) internal {
    currentPrizePool = uint128(val);
}
```

**Step 4: Update ALL slot header comments (per STOR-03)**

Update the slot header comment block at the top of the file:

Slot 0 header — update to show 32/32 bytes:
```
 * +-----------------------------------------------------------------------------+
 * | EVM SLOT 0 (32 bytes) — Timing, FSM, Counters, Flags, ETH Phase           |
 * +-----------------------------------------------------------------------------+
 * | [0:6]   levelStartTime           uint48   Timestamp when level opened       |
 * | [6:12]  dailyIdx                 uint48   Monotonic day counter             |
 * | [12:18] rngRequestTime           uint48   When last VRF request was fired   |
 * | [18:21] level                    uint24   Current jackpot level (starts at 0) |
 * | [21:22] jackpotPhaseFlag         bool     Phase: false=PURCHASE, true=JACKPOT|
 * | [22:23] jackpotCounter           uint8    Jackpots processed this level      |
 * | [23:24] lastPurchaseDay          bool     Prize target met flag              |
 * | [24:25] decWindowOpen            bool     Decimator window latch             |
 * | [25:26] rngLockedFlag            bool     Daily RNG lock (jackpot window)    |
 * | [26:27] phaseTransitionActive    bool     Level transition in progress       |
 * | [27:28] gameOver                 bool     Terminal state flag                |
 * | [28:29] dailyJackpotCoinTicketsPending bool Split jackpot pending flag       |
 * | [29:30] compressedJackpotFlag    uint8    0=normal, 1=compressed, 2=turbo    |
 * | [30:31] ticketsFullyProcessed    bool     Read slot fully drained flag       |
 * | [31:32] gameOverPossible         bool     Drip projection endgame flag       |
 * +-----------------------------------------------------------------------------+
 *   Total: 32 bytes used (0 bytes padding — FULL)
```

Slot 1 header — update to show new layout (per D-02):
```
 * +-----------------------------------------------------------------------------+
 * | EVM SLOT 1 (32 bytes) — Double-Buffer Fields + Current Prize Pool          |
 * +-----------------------------------------------------------------------------+
 * | [0:6]   purchaseStartDay         uint48   Day index when purchase phase began|
 * | [6:7]   ticketWriteSlot          uint8    Double-buffer write index (0 or 1) |
 * | [7:8]   prizePoolFrozen          bool     Prize pool freeze active flag      |
 * | [8:24]  currentPrizePool         uint128  Active prize pool for current level|
 * | [24:32] <padding>                         8 bytes unused                     |
 * +-----------------------------------------------------------------------------+
 *   Total: 24 bytes used (8 bytes padding)
```

Remove the old slot 2 header entirely. Update the "SLOT 2+: Full-Width Balances and Pools" section header to "SLOT 2+: Full-Width Balances and Pools" (renumber from "SLOT 3+" if needed).

Also update the section comment above slot 1 variables (around line 310, currently says "25 bytes used, 7 bytes padding"):
Replace with: "Packs into EVM Slot 1: purchaseStartDay, ticketWriteSlot, prizePoolFrozen, currentPrizePool (24 bytes used, 8 bytes padding)."

Update the section comment above currentPrizePool declaration — remove the old "SLOT 2" section header since currentPrizePool now packs into slot 1. Change the NatSpec to note it's uint128 and accessed via helpers:
```solidity
/// @dev Active prize pool for the current level.
///      Accumulated from mint fees and distributed via jackpots.
///      Packed into slot 1 as uint128 (max ~3.4e20 ETH, far exceeds total supply).
///      Access through _getCurrentPrizePool()/_setCurrentPrizePool() helpers.
uint128 internal currentPrizePool;
```

Also update the architecture overview NatSpec at the top: remove "DegenerusGameEndgameModule" from the "inherited by" list and replace with the full list: DegenerusGameAdvanceModule, DegenerusGameJackpotModule, DegenerusGameMintModule, DegenerusGameLootboxModule, DegenerusGameWhaleModule, DegenerusGameBoonModule, DegenerusGameDecimatorModule, DegenerusGameDegeneretteModule, DegenerusGameGameOverModule. (Update the module list to be complete and accurate.)

**Step 5: Verify the repack**

After all changes, run:
```
forge inspect DegenerusGameStorage storage-layout --json > /tmp/layout-after.json
```

Verify:
- Slot 0 contains all 15 variables (levelStartTime through gameOverPossible), total 32 bytes
- Slot 1 contains purchaseStartDay, ticketWriteSlot, prizePoolFrozen, currentPrizePool (24 bytes)
- Old slot 2 is gone — prizePoolsPacked is now at slot 2 (was slot 3)
- Every variable from slot 3+ shifted down by exactly 1
  </action>
  <verify>
    <automated>forge inspect DegenerusGameStorage storage-layout 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `forge inspect DegenerusGameStorage storage-layout` shows ticketsFullyProcessed at slot 0 offset 30
    - `forge inspect DegenerusGameStorage storage-layout` shows gameOverPossible at slot 0 offset 31
    - `forge inspect DegenerusGameStorage storage-layout` shows currentPrizePool at slot 1 with type t_uint128
    - `forge inspect DegenerusGameStorage storage-layout` shows prizePoolsPacked at slot 2 (shifted from 3)
    - grep -c "_getCurrentPrizePool\|_setCurrentPrizePool" contracts/storage/DegenerusGameStorage.sol returns at least 4 (2 function defs + 2 in NatSpec/comments)
    - grep "Total: 32 bytes used (0 bytes padding" contracts/storage/DegenerusGameStorage.sol matches (slot 0 comment updated)
    - grep "Total: 24 bytes used (8 bytes padding" contracts/storage/DegenerusGameStorage.sol matches (slot 1 comment updated)
    - grep -c "EVM SLOT 2.*Current Prize Pool" contracts/storage/DegenerusGameStorage.sol returns 0 (old slot 2 header removed)
  </acceptance_criteria>
  <done>Storage variables repacked: slot 0 = 32/32 bytes, slot 1 = 24 bytes with uint128 currentPrizePool, old slot 2 eliminated. Getter/setter helpers exist. All slot header comments match actual layout.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all direct currentPrizePool access with helpers in consuming contracts</name>
  <files>contracts/modules/DegenerusGameJackpotModule.sol, contracts/modules/DegenerusGameGameOverModule.sol, contracts/DegenerusGame.sol</files>
  <read_first>
    - contracts/modules/DegenerusGameJackpotModule.sol (lines 310-760 — all currentPrizePool access sites)
    - contracts/modules/DegenerusGameGameOverModule.sol (lines 125-150 — currentPrizePool zeroing)
    - contracts/DegenerusGame.sol (lines 2030-2070 — view function and obligations)
    - contracts/storage/DegenerusGameStorage.sol (the new helpers from Task 1)
  </read_first>
  <action>
Replace every direct read/write of `currentPrizePool` with helper calls. The variable is now uint128 packed in slot 1, so direct access would corrupt adjacent fields in some contexts. All helpers return/accept uint256 for seamless integration.

**JackpotModule replacements (7 sites):**

Line 317: `uint256 poolSnapshot = currentPrizePool;`
-> `uint256 poolSnapshot = _getCurrentPrizePool();`

Line 353: `currentPrizePool -= dailyLootboxBudget;`
-> `_setCurrentPrizePool(_getCurrentPrizePool() - dailyLootboxBudget);`

Line 433: `currentPrizePool -= paidDailyEth;`
-> `_setCurrentPrizePool(_getCurrentPrizePool() - paidDailyEth);`

Line 721: `currentPrizePool += _getNextPrizePool();`
-> `_setCurrentPrizePool(_getCurrentPrizePool() + _getNextPrizePool());`

Line 732: `currentPrizePool += moveWei;`
-> `_setCurrentPrizePool(_getCurrentPrizePool() + moveWei);`

Line 737: `_creditDgnrsCoinflip(currentPrizePool);`
-> `_creditDgnrsCoinflip(_getCurrentPrizePool());`

Line 747: `uint256 obligations = currentPrizePool +`
-> `uint256 obligations = _getCurrentPrizePool() +`

NOTE: Line numbers are approximate — find each site by searching for `currentPrizePool` in the file. Some lines may have shifted from previous phases. Match on the code pattern, not the line number.

**GameOverModule replacements (2 sites):**

Line 133: `currentPrizePool = 0;`
-> `_setCurrentPrizePool(0);`

Line 145: `currentPrizePool = 0;`
-> `_setCurrentPrizePool(0);`

**DegenerusGame replacements (2 sites):**

Line 2037 (currentPrizePoolView): `return currentPrizePool;`
-> `return _getCurrentPrizePool();`

Line 2063 (obligations): `uint256 obligations = currentPrizePool +`
-> `uint256 obligations = _getCurrentPrizePool() +`

After all replacements, verify no direct references remain:
```
grep -n "currentPrizePool" contracts/modules/DegenerusGameJackpotModule.sol contracts/modules/DegenerusGameGameOverModule.sol contracts/DegenerusGame.sol | grep -v "_getCurrentPrizePool\|_setCurrentPrizePool\|@dev\|@notice\|///\|currentPrizePoolView\|NatSpec\|comment"
```
This should return zero lines of actual code usage (only comments/NatSpec should remain).

After all replacements, compile:
```
forge build
```
Must compile with zero errors. Warnings about unused imports are acceptable.
  </action>
  <verify>
    <automated>forge build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` succeeds with zero errors
    - grep -c "_getCurrentPrizePool()" contracts/modules/DegenerusGameJackpotModule.sol returns at least 5
    - grep -c "_setCurrentPrizePool(" contracts/modules/DegenerusGameJackpotModule.sol returns at least 4
    - grep -c "_setCurrentPrizePool(0)" contracts/modules/DegenerusGameGameOverModule.sol returns 2
    - grep -c "_getCurrentPrizePool()" contracts/DegenerusGame.sol returns at least 2
    - No direct `currentPrizePool` variable reads/writes remain in consuming contracts (only helper calls and comments)
  </acceptance_criteria>
  <done>All 11 direct currentPrizePool access sites replaced with _getCurrentPrizePool()/_setCurrentPrizePool() helpers. All contracts compile cleanly.</done>
</task>

</tasks>

<verification>
1. `forge inspect DegenerusGameStorage storage-layout` confirms slot 0 = 32 bytes, slot 1 = 24 bytes with currentPrizePool as uint128
2. `forge build` compiles all contracts with zero errors
3. No direct `currentPrizePool` reads/writes outside of DegenerusGameStorage.sol helpers
4. All slot header comments in DegenerusGameStorage.sol match `forge inspect` output
</verification>

<success_criteria>
- Storage layout repacked: slot 0 full (32/32), slot 1 has currentPrizePool packed as uint128, old slot 2 eliminated
- All consuming contracts use _getCurrentPrizePool()/_setCurrentPrizePool() helpers
- All contracts compile cleanly
- Slot header comments are accurate
</success_criteria>

<output>
After completion, create `.planning/phases/168-storage-repack/168-01-SUMMARY.md`
</output>
