---
phase: 168-storage-repack
plan: 02
type: execute
wave: 2
depends_on: [168-01]
files_modified:
  - test/fuzz/StorageFoundation.t.sol
  - test/fuzz/TicketLifecycle.t.sol
  - test/fuzz/DegeneretteFreezeResolution.t.sol
  - test/fuzz/VRFStallEdgeCases.t.sol
  - test/fuzz/VRFCore.t.sol
  - test/fuzz/LootboxRngLifecycle.t.sol
  - test/fuzz/LootboxBoonCoexistence.t.sol
  - test/fuzz/BafRebuyReconciliation.t.sol
  - test/fuzz/FarFutureIntegration.t.sol
  - test/fuzz/AffiliateDgnrsClaim.t.sol
  - test/fuzz/StallResilience.t.sol
  - test/fuzz/VRFPathCoverage.t.sol
  - test/fuzz/RedemptionGas.t.sol
  - test/fuzz/handlers/CompositionHandler.sol
  - test/fuzz/handlers/VRFPathHandler.sol
  - test/fuzz/handlers/RedemptionHandler.sol
  - test/fuzz/invariant/RedemptionInvariants.inv.t.sol
autonomous: true
requirements: [STOR-05]

must_haves:
  truths:
    - "All Foundry test slot constants match forge inspect output for the new layout"
    - "All byte-offset shift constants match new positions within repacked slots"
    - "StorageFoundation.t.sol slot 1 field offset assertions updated for new layout"
  artifacts:
    - path: "test/fuzz/StorageFoundation.t.sol"
      provides: "Updated slot 1 assertions and slot number corrections"
      contains: "prizePoolsPacked not at slot 2"
    - path: "test/fuzz/TicketLifecycle.t.sol"
      provides: "Updated slot constants for shifted layout"
      contains: "PRIZE_POOLS_PACKED_SLOT = 2"
  key_links:
    - from: "test/fuzz/StorageFoundation.t.sol"
      to: "contracts/storage/DegenerusGameStorage.sol"
      via: "vm.load/vm.store with slot numbers matching forge inspect"
      pattern: "vm\\.(load|store).*bytes32\\(uint256"
---

<objective>
Update all 17 Foundry test files with hardcoded vm.load/vm.store slot offsets to match the new storage layout after the slot 2 elimination. Every slot >= 2 (formerly >= 3) shifts down by 1. Slot 1 byte offsets change because ticketsFullyProcessed and gameOverPossible moved out and prizePoolFrozen shifted.

Purpose: Tests must pass against the repacked storage layout.
Output: All test slot constants corrected, byte offsets updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/168-storage-repack/168-CONTEXT.md
@.planning/phases/168-storage-repack/168-01-SUMMARY.md

<interfaces>
<!-- The executor MUST run forge inspect to get authoritative slot numbers before updating ANY test file. -->
<!-- Do NOT use the values below as authoritative — they are estimates for planning purposes only. -->
<!-- The executor MUST verify each value against forge inspect output. -->

Expected slot shifts (old -> new, slot 2 eliminated):
  - Slot 0: unchanged (same slot, but now 32 bytes)
  - Slot 1: same slot number, but internal byte offsets change
  - Old slot 2 (currentPrizePool uint256): ELIMINATED
  - Old slot 3 (prizePoolsPacked) -> new slot 2
  - Old slot 4 (rngWordCurrent) -> new slot 3
  - Old slot 5 (vrfRequestId) -> new slot 4
  - Old slot 6 (totalFlipReversals) -> new slot 5
  - Old slot 7 (dailyTicketBudgetsPacked) -> new slot 6
  - Every subsequent slot: old N -> new N-1

Expected slot 1 byte offset changes:
  - purchaseStartDay: offset 0 (unchanged)
  - ticketWriteSlot: offset 6 (unchanged position)
  - prizePoolFrozen: was offset 8 (with 2 bools before it), now offset 7 (bools moved out)
  - currentPrizePool: new at offset 8 (uint128, 16 bytes)
  - ticketsFullyProcessed: MOVED TO SLOT 0 (no longer in slot 1)
  - gameOverPossible: MOVED TO SLOT 0 (no longer in slot 1)

CRITICAL: Run `forge inspect DegenerusGameStorage storage-layout` FIRST and use those values.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Get authoritative slot layout and update all test slot constants</name>
  <files>
    test/fuzz/StorageFoundation.t.sol,
    test/fuzz/TicketLifecycle.t.sol,
    test/fuzz/DegeneretteFreezeResolution.t.sol,
    test/fuzz/VRFStallEdgeCases.t.sol,
    test/fuzz/VRFCore.t.sol,
    test/fuzz/LootboxRngLifecycle.t.sol,
    test/fuzz/LootboxBoonCoexistence.t.sol,
    test/fuzz/BafRebuyReconciliation.t.sol,
    test/fuzz/FarFutureIntegration.t.sol,
    test/fuzz/AffiliateDgnrsClaim.t.sol,
    test/fuzz/StallResilience.t.sol,
    test/fuzz/VRFPathCoverage.t.sol,
    test/fuzz/RedemptionGas.t.sol,
    test/fuzz/handlers/CompositionHandler.sol,
    test/fuzz/handlers/VRFPathHandler.sol,
    test/fuzz/handlers/RedemptionHandler.sol,
    test/fuzz/invariant/RedemptionInvariants.inv.t.sol
  </files>
  <read_first>
    - contracts/storage/DegenerusGameStorage.sol (post-repack from Plan 01)
    - test/fuzz/StorageFoundation.t.sol (slot 1 byte offset assertions)
    - test/fuzz/TicketLifecycle.t.sol (SLOT_0, SLOT_1, PRIZE_POOLS_PACKED_SLOT, TICKET_QUEUE_SLOT, etc.)
    - test/fuzz/DegeneretteFreezeResolution.t.sol (FROZEN_BYTE_OFFSET, PRIZE_POOLS_PACKED_SLOT, PENDING_PACKED_SLOT)
    - test/fuzz/VRFStallEdgeCases.t.sol (SLOT_RNG_WORD_CURRENT, SLOT_VRF_REQUEST_ID, SLOT_TOTAL_FLIP_REVERSALS)
    - test/fuzz/LootboxBoonCoexistence.t.sol (many SLOT_ constants)
    - test/fuzz/handlers/CompositionHandler.sol (MINT_PACKED_SLOT)
  </read_first>
  <action>
**Step 0: Get authoritative layout**

Run and capture the full output:
```
forge inspect DegenerusGameStorage storage-layout
```

This is the SOLE source of truth. Extract every slot number and byte offset. Every constant updated below must match this output exactly.

**Step 1: Update slot constants (N >= 2 shift down by 1)**

Per D-03 (mechanical one-time fix, no centralized helper), update each file's constants individually.

Files and their constants to update (old -> new, verify each against forge inspect):

**test/fuzz/TicketLifecycle.t.sol:**
- `PRIZE_POOLS_PACKED_SLOT = 3` -> `2` (was slot 3, now slot 2)
- `TICKET_QUEUE_SLOT = 15` -> verify against forge inspect (expect 14)
- `TICKETS_OWED_PACKED_SLOT = 16` -> verify (expect 15)
- `LOOTBOX_RNG_WORD_SLOT = 49` -> verify (expect 48)
- `WRITE_SLOT_SHIFT = 176` -> verify against forge inspect for ticketWriteSlot offset in slot 1 (may change if byte offset shifted)
- Inline literal `bytes32(uint256(45))` at line 2047 -> update to new lootboxRngIndex slot
- Slot 0 shift constants (LEVEL_SHIFT, JACKPOT_PHASE_SHIFT, JACKPOT_COUNTER_SHIFT, RNG_LOCKED_SHIFT, COMPRESSED_FLAG_SHIFT) -> verify against forge inspect. These should be UNCHANGED since the new bools are appended at the END of slot 0, not inserted in the middle.

**test/fuzz/DegeneretteFreezeResolution.t.sol:**
- `SLOT_1 = 1` -> unchanged
- `FROZEN_BYTE_OFFSET = 24` -> update to new prizePoolFrozen offset from forge inspect (expect ~7, but VERIFY: this is the byte offset from the right/low side, compute as forge_inspect_offset * 8 for the bit shift, then FROZEN_BYTE_OFFSET = forge_inspect_offset)
- `PRIZE_POOLS_PACKED_SLOT = 3` -> `2`
- `PENDING_PACKED_SLOT = 14` -> verify (expect 13)
- `LOOTBOX_RNG_WORD_SLOT = 49` -> verify (expect 48)

IMPORTANT for FROZEN_BYTE_OFFSET: The test uses `(slot1 >> (FROZEN_BYTE_OFFSET * 8)) & 0xFF` to extract prizePoolFrozen. After the repack, prizePoolFrozen is at a different byte offset within slot 1 because ticketsFullyProcessed and gameOverPossible no longer precede it. The value from forge inspect for `prizePoolFrozen` gives {slot: 1, offset: N}. Set FROZEN_BYTE_OFFSET = N.

**test/fuzz/VRFStallEdgeCases.t.sol:**
- `SLOT_RNG_WORD_CURRENT = 4` -> `3`
- `SLOT_VRF_REQUEST_ID = 5` -> `4`
- `SLOT_TOTAL_FLIP_REVERSALS = 6` -> `5`
- `SLOT_MID_DAY_PENDING = 55` -> verify (expect 54)
- Inline `bytes32(uint256(45))` -> update to new lootboxRngIndex slot

**test/fuzz/VRFCore.t.sol:**
- `SLOT_RNG_WORD_CURRENT = 4` -> `3`
- `SLOT_VRF_REQUEST_ID = 5` -> `4`
- Inline `bytes32(uint256(45))` -> update

**test/fuzz/LootboxRngLifecycle.t.sol:**
- `SLOT_RNG_WORD_CURRENT = 4` -> `3`
- `SLOT_VRF_REQUEST_ID = 5` -> `4`
- Inline `bytes32(uint256(45))` -> update

**test/fuzz/LootboxBoonCoexistence.t.sol:**
- `SLOT_BOON_PACKED = 77` -> `76`
- `SLOT_LOOTBOX_ETH = 20` -> `19`
- `SLOT_LOOTBOX_RNG_IDX = 45` -> `44`
- `SLOT_LOOTBOX_WORD = 49` -> `48`
- `SLOT_LOOTBOX_DAY = 50` -> `49`
- `SLOT_LOOTBOX_BASE = 28` -> `27`
- `SLOT_LOOTBOX_EV = 52` -> `51`

**test/fuzz/BafRebuyReconciliation.t.sol:**
- `PRIZE_POOLS_PACKED_SLOT = 3` -> `2`

**test/fuzz/FarFutureIntegration.t.sol:**
- `TICKET_QUEUE_SLOT = 15` -> verify (expect 14)
- `PRIZE_POOLS_PACKED_SLOT = 3` -> `2`

**test/fuzz/AffiliateDgnrsClaim.t.sol:**
- `SLOT_LEVEL = 0` -> unchanged (slot 0)
- `SLOT_LEVEL_DGNRS_ALLOCATION = 32` -> `31`
- `SLOT_LEVEL_DGNRS_CLAIMED = 33` -> `32`

**test/fuzz/StallResilience.t.sol:**
- Inline `bytes32(uint256(45))` -> update to new lootboxRngIndex slot

**test/fuzz/VRFPathCoverage.t.sol:**
- Inline `bytes32(uint256(45))` -> update

**test/fuzz/RedemptionGas.t.sol:**
- Inline `bytes32(uint256(10))` -> `9` (verify against forge inspect for the referenced variable)

**test/fuzz/handlers/CompositionHandler.sol:**
- `MINT_PACKED_SLOT = 12` -> `11`

**test/fuzz/handlers/VRFPathHandler.sol:**
- Inline `bytes32(uint256(45))` -> update

**test/fuzz/handlers/RedemptionHandler.sol:**
- `SLOT_PENDING_BURNIE = 10` -> `9`
- `SLOT_PERIOD_INDEX = 14` -> `13`
- `SLOT_PERIOD_BURNED = 15` -> `14`
- `SLOT_SUPPLY_SNAPSHOT = 13` -> `12`

**test/fuzz/invariant/RedemptionInvariants.inv.t.sol:**
- `SLOT_PENDING_BURNIE = 10` -> `9`
- `SLOT_SUPPLY_SNAPSHOT = 13` -> `12`
- `SLOT_PERIOD_INDEX = 14` -> `13`
- `SLOT_PERIOD_BURNED = 15` -> `14`

**Step 2: Update StorageFoundation.t.sol slot 1 assertions**

The `testSlot1FieldOffsets()` test must be rewritten to match the new slot 1 layout:

Old assertions (ticketWriteSlot at offset 22, ticketsFullyProcessed at offset 23, prizePoolFrozen at offset 24):
- ticketsFullyProcessed is NO LONGER IN SLOT 1 — remove its slot 1 assertion entirely
- prizePoolFrozen byte offset changes — use forge inspect value
- Update the function NatSpec and assertion messages

The new test should verify:
1. ticketWriteSlot at its new forge-inspect-confirmed offset in slot 1
2. prizePoolFrozen at its new forge-inspect-confirmed offset in slot 1
3. (Optionally) verify ticketsFullyProcessed and gameOverPossible are in slot 0

Also update `testPackedPoolSlotsUnshifted()`:
- `vm.store(address(harness), bytes32(uint256(3)), ...)` -> `bytes32(uint256(2))` (prizePoolsPacked at new slot)
- `vm.store(address(harness), bytes32(uint256(14)), ...)` -> update to new prizePoolPendingPacked slot
- Update assertion messages: "not at slot 3" -> "not at slot 2", "not at slot 14" -> "not at slot {N}"

**Step 3: Update inline comments referencing old slot numbers**

For each test file, update any comments that reference slot numbers (e.g., "// slot 3 = prizePoolsPacked" should become "// slot 2 = prizePoolsPacked"). Search for numeric slot references in comments near the constants.

**Step 4: Compile and run StorageFoundation test**

```
forge build && forge test --match-contract StorageFoundation -vv
```

This is the critical gate — StorageFoundation.t.sol exists specifically to catch layout misalignment.
  </action>
  <verify>
    <automated>forge build 2>&1 | tail -3 && forge test --match-contract StorageFoundation -vv 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` succeeds with zero errors
    - `forge test --match-contract StorageFoundation` passes all tests
    - grep "PRIZE_POOLS_PACKED_SLOT = 2" in TicketLifecycle.t.sol, DegeneretteFreezeResolution.t.sol, BafRebuyReconciliation.t.sol, FarFutureIntegration.t.sol returns matches
    - grep "SLOT_RNG_WORD_CURRENT = 3" in VRFStallEdgeCases.t.sol, VRFCore.t.sol, LootboxRngLifecycle.t.sol returns matches
    - grep "SLOT_VRF_REQUEST_ID = 4" in VRFStallEdgeCases.t.sol, VRFCore.t.sol, LootboxRngLifecycle.t.sol returns matches
    - grep "MINT_PACKED_SLOT = 11" in handlers/CompositionHandler.sol returns a match
    - No test file contains a hardcoded slot number that is off-by-one from forge inspect output
  </acceptance_criteria>
  <done>All 17 test files updated with correct slot numbers and byte offsets matching the repacked storage layout. StorageFoundation tests pass.</done>
</task>

</tasks>

<verification>
1. `forge build` compiles all test files
2. `forge test --match-contract StorageFoundation -vv` passes all assertions
3. Every slot constant in every test file matches `forge inspect DegenerusGameStorage storage-layout` output
</verification>

<success_criteria>
- All 17 test files have slot constants updated for the -1 shift
- StorageFoundation.t.sol slot 1 byte offset assertions match new layout
- All test files compile
- StorageFoundation tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/168-storage-repack/168-02-SUMMARY.md`
</output>
