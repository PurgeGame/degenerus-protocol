---
phase: 143-vault-self-win-delta-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/143-vault-self-win-delta-audit/143-01-delta-report.md
autonomous: true
requirements: [DELTA-04, DELTA-05]

must_haves:
  truths:
    - "Every new function in DegenerusVault.sol has been adversarially reviewed"
    - "transferFromPool self-win burn path is verified safe for solvency"
    - "No test regressions exist"
  artifacts:
    - path: ".planning/phases/143-vault-self-win-delta-audit/143-01-delta-report.md"
      provides: "Delta audit report for vault + self-win changes"
      contains: "## Findings"
---

<objective>
Adversarial delta audit of 3 changes: vault sdgnrsBurn(), vault sdgnrsClaimRedemption(), and transferFromPool self-win burn path.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Delta Adversarial Audit</name>
  <files>.planning/phases/143-vault-self-win-delta-audit/143-01-delta-report.md</files>
  <read_first>
    contracts/DegenerusVault.sol
    contracts/StakedDegenerusStonk.sol
  </read_first>
  <action>
Audit the 3 changes adversarially. For each, check: access control, reentrancy, state consistency, gas, correctness.

1. sdgnrsBurn() — calls sdgnrsToken.burn(amount). Check: onlyVaultOwner guard, reentrancy via burn payout, ETH/stETH received correctly flows to vault balance.
2. sdgnrsClaimRedemption() — calls sdgnrsToken.claimRedemption(). Check: onlyVaultOwner guard, msg.sender context (vault is caller).
3. transferFromPool self-win — when to == address(this), burns instead of transfer. Check: totalSupply reduction correct, pool accounting still consistent, solvency invariant maintained, no double-count.

Write report to delta-report.md.
  </action>
  <acceptance_criteria>
    - Report exists with verdicts for all 3 changes
    - Each verdict is SAFE or VULNERABLE with reasoning
    - Solvency invariant analysis included for self-win burn
  </acceptance_criteria>
</task>

</tasks>
