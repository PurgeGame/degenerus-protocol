---
phase: 30-payout-specification-document
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/PAYOUT-SPECIFICATION.html
autonomous: true
requirements:
  - SPEC-01

must_haves:
  truths:
    - "audit/PAYOUT-SPECIFICATION.html exists and opens in a browser showing a styled page"
    - "The document has a title, commit reference (3fa32f51), date, and summary statistics"
    - "A linked table of contents lists all 6 category sections"
    - "A pool architecture overview SVG diagram shows the 4 core pools and their relationships"
    - "CSS design system uses custom properties for consistent color/typography throughout"
  artifacts:
    - path: "audit/PAYOUT-SPECIFICATION.html"
      provides: "Self-contained HTML scaffold with CSS, header, TOC, pool overview, and empty category sections"
      contains: "<html"
  key_links:
    - from: "audit/PAYOUT-SPECIFICATION.html"
      to: "browser"
      via: "file:// protocol"
      pattern: "<!DOCTYPE html>"
---

<objective>
Create the HTML scaffold for the Payout Specification document with the complete CSS design system, document header, table of contents, pool architecture overview SVG, and empty section stubs for all 6 categories.

Purpose: Establish the self-contained HTML file with all styling and structural elements that subsequent plans will populate with system cards. This is the foundation all other plans append into.
Output: audit/PAYOUT-SPECIFICATION.html with header, TOC, pool overview SVG, and empty category sections.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-payout-specification-document/30-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create self-contained HTML scaffold with CSS design system</name>
  <files>audit/PAYOUT-SPECIFICATION.html</files>
  <read_first>
    - .planning/phases/30-payout-specification-document/30-RESEARCH.md (Architecture Patterns section for document structure, CSS design system, SVG visual language, system card template)
    - audit/v3.0-payout-audit-consolidated.md (Executive Summary for summary stats: 19 PASS, 0 Medium+, 23 distribution mechanisms)
    - audit/v3.0-gameover-audit-consolidated.md (Executive Summary for GAMEOVER stats: 8 PASS, 1 MEDIUM)
    - audit/KNOWN-ISSUES.md (for known issues count)
  </read_first>
  <action>
Create audit/PAYOUT-SPECIFICATION.html as a self-contained HTML file. The file must contain:

**DOCTYPE and head:**
- `<!DOCTYPE html>` with `<html lang="en">`
- `<meta charset="UTF-8">`, `<meta name="viewport" content="width=device-width, initial-scale=1.0">`
- `<title>Degenerus Protocol — Payout Specification</title>`

**Complete CSS in a single `<style>` tag:**
- CSS custom properties (all from RESEARCH.md CSS Design System section):
  ```
  --color-bg: #ffffff;
  --color-text: #1a1a1a;
  --color-heading: #0d47a1;
  --color-border: #e0e0e0;
  --color-card-bg: #fafafa;
  --color-code-bg: #f5f5f5;
  --color-eth: #627eea;
  --color-burnie: #ff6b35;
  --color-dgnrs: #4caf50;
  --color-steth: #00bcd4;
  --color-pool-bg: #e3f2fd;
  --color-warning: #fff3e0;
  --font-mono: 'Courier New', Courier, monospace;
  --font-sans: system-ui, -apple-system, sans-serif;
  ```
- Body: `max-width: 1100px`, `margin: 0 auto`, `padding: 2rem`, `font-family: var(--font-sans)`, `color: var(--color-text)`, `line-height: 1.6`
- `.system-card` styling: `background: var(--color-card-bg)`, `border: 1px solid var(--color-border)`, `border-radius: 8px`, `padding: 1.5rem`, `margin: 1.5rem 0`
- `.info-table` styling: bordered table with `th` at 140px width, left-aligned, alternating row shading
- `.formula-block` styling: `background: var(--color-code-bg)`, `padding: 1rem`, `border-radius: 4px`, `overflow-x: auto`
- `.formula-block pre code` styling: `font-family: var(--font-mono)`, `font-size: 0.85rem`
- `.file-ref` styling: `font-size: 0.8rem`, `color: #666`
- `.edge-cases` styling: `background: var(--color-warning)`, `padding: 1rem`, `border-radius: 4px`, `margin-top: 1rem`
- `.badge` styling: `display: inline-block`, `padding: 2px 8px`, `border-radius: 12px`, `font-size: 0.75rem`, `font-weight: 600`, `margin-right: 4px`
- Currency badge variants: `.badge-eth { background: #e8eaf6; color: var(--color-eth); }`, `.badge-burnie { background: #fff3e0; color: var(--color-burnie); }`, `.badge-dgnrs { background: #e8f5e9; color: var(--color-dgnrs); }`, `.badge-steth { background: #e0f7fa; color: var(--color-steth); }`, `.badge-wwxrp { background: #fce4ec; color: #c62828; }`
- `.flow-svg` styling: `width: 100%`, `max-width: 800px`, `height: auto`, `display: block`, `margin: 1rem auto`
- SVG shape classes: `.flow-svg .pool { fill: #e3f2fd; stroke: #1565c0; stroke-width: 1.5; }`, `.flow-svg .contract { fill: #f5f5f5; stroke: #616161; stroke-width: 1; }`, `.flow-svg .decision { fill: #fff9c4; stroke: #f9a825; stroke-width: 1; }`, `.flow-svg .recipient { fill: #e8f5e9; stroke: #2e7d32; stroke-width: 1.5; }`
- `.flow-svg text` styling: `font-family: system-ui, sans-serif`, `font-size: 12px`, `fill: #1a1a1a`
- `.toc` styling: `background: #f8f9fa`, `padding: 1.5rem`, `border-radius: 8px`, `margin: 2rem 0`
- `.toc a` styling: `color: var(--color-heading)`, `text-decoration: none`
- `.toc a:hover` styling: `text-decoration: underline`
- `h1` styling: `color: var(--color-heading)`, `border-bottom: 3px solid var(--color-heading)`, `padding-bottom: 0.5rem`
- `h2` (category headers) styling: `color: var(--color-heading)`, `margin-top: 3rem`, `border-bottom: 1px solid var(--color-border)`, `padding-bottom: 0.5rem`
- `h3` (system card headers) styling: `margin-top: 0`, `color: #333`
- `details summary` styling: `cursor: pointer`, `font-weight: 600`, `padding: 0.5rem 0`
- `.summary-stats` styling: `display: grid`, `grid-template-columns: repeat(auto-fit, minmax(180px, 1fr))`, `gap: 1rem`, `margin: 1.5rem 0`
- `.stat-card` styling: `text-align: center`, `padding: 1rem`, `background: var(--color-card-bg)`, `border-radius: 8px`, `border: 1px solid var(--color-border)`
- `.stat-card .stat-number` styling: `font-size: 2rem`, `font-weight: 700`, `color: var(--color-heading)`
- `.mutation-table` styling: standard bordered table with compact padding
- `@media print` block: hide `.toc`, reduce margins, avoid page breaks inside `.system-card`, black text

**Document header:**
- `<h1>Degenerus Protocol -- Payout Specification</h1>`
- Subtitle: `<p class="subtitle">Complete distribution system reference synthesized from Phases 26-29 audit findings</p>`
- Metadata row: commit `3fa32f51`, date `2026-03-18`, auditor `Claude Opus 4.6`
- Summary stats grid with 4 stat-cards:
  - "23" Distribution Systems
  - "28" Requirements Verified (9 GO + 19 PAY)
  - "1" Medium Finding (GO-05)
  - "0" Critical/High Findings

**Table of contents:**
- Section inside `<nav class="toc">` with `<h2>Table of Contents</h2>`
- Numbered list with anchor links to each category:
  1. Pool Architecture Overview (#pool-overview)
  2. Jackpot Distribution (#cat-jackpot) -- PAY-01, PAY-02, PAY-16
  3. Scatter / Decimator (#cat-scatter) -- PAY-03, PAY-04, PAY-05, PAY-06
  4. Coinflip Economy (#cat-coinflip) -- PAY-07, PAY-08, PAY-18, PAY-19
  5. Ancillary Payouts (#cat-ancillary) -- PAY-09, PAY-10, PAY-11, PAY-12, PAY-13, PAY-17
  6. Token Burns (#cat-burns) -- PAY-14, PAY-15
  7. GAMEOVER Terminal Distribution (#cat-gameover) -- GO-01, GO-02, GO-07, GO-08
  8. Cross-System: claimablePool Invariant (#cross-claimable)
  9. Cross-System: Known Issues (#cross-issues)
- Smooth scrolling via `html { scroll-behavior: smooth; }`

**Pool Architecture Overview (section id="pool-overview"):**
- `<h2>Pool Architecture Overview</h2>`
- An inline SVG diagram (viewBox="0 0 900 500") showing the 4 core ETH pools and their flow relationships:
  - `futurePrizePool` (rounded rect, pool class) at top-left
  - `nextPrizePool` (rounded rect, pool class) at top-right
  - `currentPrizePool` (rounded rect, pool class) at bottom-left
  - `claimablePool` (rounded rect, pool class) at bottom-right
  - `yieldAccumulator` (rounded rect, pool class) at center-right
  - Arrows showing transitions: futurePrizePool -> currentPrizePool (level transition), currentPrizePool -> claimablePool (jackpot draws), futurePrizePool -> claimablePool (scatter/decimator), yieldAccumulator -> futurePrizePool (x00 milestone)
  - Arrow from "stETH Yield" external source -> yieldAccumulator
  - Arrow from claimablePool -> "Players" (recipient pill)
  - Use the SVG visual language from RESEARCH.md: pools as rounded rects with pool class, players as pills with recipient class, arrows with arrowhead markers
  - Include `<defs>` with arrow marker: `<marker id="arrow" markerWidth="10" markerHeight="7" refX="10" refY="3.5" orient="auto"><polygon points="0 0, 10 3.5, 0 7" fill="#424242"/></marker>`
- Brief paragraph below SVG: "All ETH entering the protocol flows through a 4-pool lifecycle. futurePrizePool accumulates incoming ETH, transitions to currentPrizePool at each level, which is drawn down by jackpot distributions into claimablePool. Players claim from claimablePool via claimWinnings(). The yieldAccumulator collects stETH rebasing surplus and releases 50% to futurePrizePool at x00 milestones."

**Empty category section stubs (6 sections):**
Each section stub has just the `<section id="cat-XXX">` with `<h2>` header and a placeholder HTML comment `<!-- System cards populated by Plan 30-0N -->`:
1. `<section id="cat-jackpot"><h2>1. Jackpot Distribution</h2><!-- Plan 30-02 --></section>`
2. `<section id="cat-scatter"><h2>2. Scatter / Decimator</h2><!-- Plan 30-03 --></section>`
3. `<section id="cat-coinflip"><h2>3. Coinflip Economy</h2><!-- Plan 30-03 --></section>`
4. `<section id="cat-ancillary"><h2>4. Ancillary Payouts</h2><!-- Plan 30-04 --></section>`
5. `<section id="cat-burns"><h2>5. Token Burns</h2><!-- Plan 30-04 --></section>`
6. `<section id="cat-gameover"><h2>6. GAMEOVER Terminal Distribution</h2><!-- Plan 30-05 --></section>`

**Empty cross-system section stubs (2 sections):**
1. `<section id="cross-claimable"><h2>7. Cross-System: claimablePool Invariant</h2><!-- Plan 30-06 --></section>`
2. `<section id="cross-issues"><h2>8. Cross-System: Known Issues</h2><!-- Plan 30-06 --></section>`

**Footer:**
- `<footer>` with audit metadata: "Generated from Phases 26-29 audit findings. Commit: 3fa32f51. All file:line references verified against this commit."
- Copyright/disclaimer line

No external dependencies. No JavaScript required for core content. No CDN links, no web fonts, no images.
  </action>
  <verify>
    <automated>test -f audit/PAYOUT-SPECIFICATION.html && grep -c '<!DOCTYPE html>' audit/PAYOUT-SPECIFICATION.html && grep -c '<style>' audit/PAYOUT-SPECIFICATION.html && grep -c 'viewBox' audit/PAYOUT-SPECIFICATION.html && grep -c 'cat-jackpot\|cat-scatter\|cat-coinflip\|cat-ancillary\|cat-burns\|cat-gameover' audit/PAYOUT-SPECIFICATION.html</automated>
  </verify>
  <acceptance_criteria>
    - audit/PAYOUT-SPECIFICATION.html exists and contains `<!DOCTYPE html>`
    - File contains exactly one `<style>` block with CSS custom properties `--color-eth`, `--color-burnie`, `--color-dgnrs`, `--color-steth`
    - File contains `.system-card`, `.info-table`, `.formula-block`, `.edge-cases`, `.badge`, `.flow-svg` CSS classes
    - File contains `3fa32f51` commit reference in the header
    - File contains a `<nav class="toc">` element with anchor links to all 8+ sections
    - File contains one `<svg` element with `viewBox` for the pool architecture overview
    - SVG contains text nodes for `futurePrizePool`, `currentPrizePool`, `claimablePool`, `yieldAccumulator`
    - File contains section stubs with ids: `cat-jackpot`, `cat-scatter`, `cat-coinflip`, `cat-ancillary`, `cat-burns`, `cat-gameover`, `cross-claimable`, `cross-issues`
    - File contains `@media print` CSS block
    - File contains NO external URLs (no `http://` or `https://` in `<link>`, `<script src>`, or `@import`)
    - File size is under 30KB (scaffold only, before content)
  </acceptance_criteria>
  <done>Self-contained HTML file exists at audit/PAYOUT-SPECIFICATION.html with complete CSS design system, header with summary stats, TOC with anchor links to all sections, pool architecture overview SVG, and empty category section stubs ready for content population.</done>
</task>

</tasks>

<verification>
- `test -f audit/PAYOUT-SPECIFICATION.html` returns 0
- `grep -c '<!DOCTYPE html>' audit/PAYOUT-SPECIFICATION.html` returns 1
- `grep -c '<svg' audit/PAYOUT-SPECIFICATION.html` returns at least 1 (pool overview)
- `grep -c 'cat-jackpot' audit/PAYOUT-SPECIFICATION.html` returns at least 1
- `grep 'https\?://' audit/PAYOUT-SPECIFICATION.html` returns nothing (no external deps)
- Opening file in browser shows styled page with header, TOC, pool overview diagram
</verification>

<success_criteria>
audit/PAYOUT-SPECIFICATION.html is a valid self-contained HTML file that opens in any browser showing a professionally styled page with header, summary statistics, linked table of contents, pool architecture overview SVG diagram, and 8 empty section stubs ready for content.
</success_criteria>

<output>
After completion, create `.planning/phases/30-payout-specification-document/30-01-SUMMARY.md`
</output>
