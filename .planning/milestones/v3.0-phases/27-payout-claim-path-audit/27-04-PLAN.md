---
phase: 27-payout-claim-path-audit
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v3.0-payout-lootbox-quest-affiliate.md
autonomous: true
requirements:
  - PAY-09
  - PAY-10
  - PAY-11

must_haves:
  truths:
    - "Lootbox rewards have an explicit PASS or FINDING verdict with file:line references covering whale passes, lazy passes, deity passes, future tickets, and BURNIE payouts"
    - "Quest rewards and streak bonuses have an explicit PASS or FINDING verdict with file:line references"
    - "Affiliate commissions have an explicit PASS or FINDING verdict with file:line references covering 3-tier system, taper schedule, and DGNRS claim"
    - "claimablePool mutations through lootbox and affiliate paths are traced"
    - "Affiliate DGNRS mechanism verified as fixed-allocation (not sequential depletion)"
  artifacts:
    - path: "audit/v3.0-payout-lootbox-quest-affiliate.md"
      provides: "PAY-09, PAY-10, PAY-11 audit verdicts"
      contains: "PAY-09"
    - path: "audit/v3.0-payout-lootbox-quest-affiliate.md"
      provides: "PAY-11 affiliate verdict"
      contains: "PAY-11"
  key_links:
    - from: "contracts/modules/DegenerusGameLootboxModule.sol"
      to: "audit/v3.0-payout-lootbox-quest-affiliate.md"
      via: "lootbox reward code audited"
      pattern: "PAY-09.*PASS|PAY-09.*FINDING"
    - from: "contracts/DegenerusAffiliate.sol"
      to: "audit/v3.0-payout-lootbox-quest-affiliate.md"
      via: "affiliate commission code audited"
      pattern: "PAY-11.*PASS|PAY-11.*FINDING"
---

<objective>
Audit the ancillary payout systems Part A: lootbox rewards (PAY-09), quest rewards and streak bonuses (PAY-10), and affiliate commissions (PAY-11).

Purpose: These three systems represent diverse payout mechanisms -- lootbox distributes NFT passes and BURNIE via probability-weighted tiers, quests credit BURNIE via `coin.creditFlip`, and affiliates distribute both ETH commissions and DGNRS tokens. The affiliate system has a known discrepancy between the v1.1 doc (sequential depletion) and actual code (fixed allocation per level) that must be verified and documented (Research Open Question 1, CP-06).

Output: audit/v3.0-payout-lootbox-quest-affiliate.md with PASS/FINDING verdicts for PAY-09, PAY-10, and PAY-11.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-payout-claim-path-audit/27-RESEARCH.md
@audit/v1.1-quest-rewards.md
@audit/v1.1-affiliate-system.md
@audit/v1.1-ECONOMICS-PRIMER.md
@audit/v1.1-parameter-reference.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit lootbox rewards (PAY-09)</name>
  <files>audit/v3.0-payout-lootbox-quest-affiliate.md</files>
  <read_first>
    - contracts/modules/DegenerusGameLootboxModule.sol (full file -- 1778 lines, focus on reward resolution functions)
    - contracts/modules/DegenerusGameEndgameModule.sol (lines 405-488 for ticket tier definitions and lootbox allocation)
    - contracts/modules/DegenerusGamePayoutUtils.sol (lines 77-93 for _queueWhalePassClaimCore)
    - contracts/DegenerusGame.sol (search for: claimWhalePass -- dispatch function)
    - contracts/storage/DegenerusGameStorage.sol (search for: whalePassClaims, lazyPassClaims, deityPass)
    - audit/v1.1-ECONOMICS-PRIMER.md (lootbox overview)
    - audit/v1.1-parameter-reference.md (lootbox tier percentages, pass values)
  </read_first>
  <action>
Audit the lootbox reward system (PAY-09). Read LootboxModule.sol focusing on the reward resolution paths. This module is 1778 lines covering multiple reward types.

Create the file audit/v3.0-payout-lootbox-quest-affiliate.md with:
- Title: "# Payout Path Audit: Lootbox, Quest, and Affiliate (PAY-09, PAY-10, PAY-11)"
- Executive Summary section (placeholder for PAY-10, PAY-11 from Task 2)
- Severity Definitions (per C4A standards)

**PAY-09: Lootbox Rewards** (LootboxModule:1-1778)

The audit MUST cover each reward type:

1. **Whale passes**: Trace the whale pass reward path. Verify whale passes are queued via `_queueWhalePassClaimCore` (PayoutUtils:77-93). Verify the whale pass remainder correctly increments `claimablePool`. Verify `whalePassClaims[player]` accumulates without expiry (CP-07). Verify `claimWhalePass` correctly pays out and decrements `whalePassClaims`.

2. **Lazy passes**: Trace the lazy pass reward path. Verify lazy passes grant future ticket allocations. Verify the lazy pass claim mechanism and any expiry rules.

3. **Deity passes**: Trace deity pass rewards from lootbox. Verify deity passes are correctly minted or credited. Verify interaction with deity boon system (connects to PAY-19 boons).

4. **Future tickets**: Verify lootbox can award future tickets. Trace the ticket crediting mechanism. Verify tickets go into the correct pool (futurePool accounting).

5. **BURNIE payouts**: Verify lootbox BURNIE rewards are credited via `coin.creditFlip` (becomes next-day coinflip wager, not direct transfer). Verify the BURNIE amount is correctly calculated per tier.

6. **Tier system**: Verify the probability-weighted tier system from EndgameModule:405-488. Verify VRF randomness is used for tier selection. Verify the 2x over-collateralization for ticket prizes (connecting to PAY-16).

7. **claimablePool accounting**: Trace every `claimablePool` mutation through lootbox paths (primarily whale pass queueing). Verify no other lootbox reward type incorrectly mutates claimablePool.

8. **CEI ordering**: Verify all lootbox distribution and claim functions follow correct CEI pattern.

Write the PAY-09 verdict section with:
```
### PAY-09: Lootbox Rewards
**Verdict:** PASS | FINDING-[severity]
**Files:** [file:line-range for each reward type]
**Summary:** [1-2 sentences]
**Reward Types Audited:** whale pass, lazy pass, deity pass, future tickets, BURNIE
**Pool Source:** [which pool funds each reward type]
**claimablePool Impact:** [whale pass queueing analysis]
**CEI Status:** [correct/violation]
**Double-Claim Guard:** [mechanism per reward type]
**Recommendation:** [fix if FINDING, or "None" if PASS]
```
  </action>
  <verify>
    <automated>grep -c "PAY-09" audit/v3.0-payout-lootbox-quest-affiliate.md | grep -v "^0$" && grep -E "PAY-09.*(PASS|FINDING)" audit/v3.0-payout-lootbox-quest-affiliate.md && grep -c "whale" audit/v3.0-payout-lootbox-quest-affiliate.md | grep -v "^0$" && grep -c "deity" audit/v3.0-payout-lootbox-quest-affiliate.md | grep -v "^0$" && grep "claimablePool" audit/v3.0-payout-lootbox-quest-affiliate.md | head -1</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.0-payout-lootbox-quest-affiliate.md exists and contains "PAY-09" followed by "PASS" or "FINDING"
    - File contains "whale pass" or "whalePass" with claim and queueing analysis
    - File contains "lazy pass" or "lazyPass" analysis
    - File contains "deity pass" or "deityPass" analysis
    - File contains "future ticket" or "futureTicket" analysis
    - File contains "BURNIE" lootbox payout analysis with creditFlip routing
    - File contains "claimablePool" mutation trace for lootbox whale pass path
    - File contains tier system or probability analysis
    - File contains "_queueWhalePassClaimCore" reference
    - File contains "claimWhalePass" claim mechanism analysis
    - File contains CEI ordering verification
  </acceptance_criteria>
  <done>PAY-09 has an explicit PASS or FINDING verdict with file:line references for each of the 5 lootbox reward types (whale pass, lazy pass, deity pass, future tickets, BURNIE). Whale pass queueing and claimablePool accounting verified. Tier system probability-weighted selection verified. CEI ordering confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Audit quest rewards and affiliate commissions (PAY-10, PAY-11)</name>
  <files>audit/v3.0-payout-lootbox-quest-affiliate.md</files>
  <read_first>
    - contracts/DegenerusQuests.sol (full file -- 1598 lines; focus on reward distribution functions)
    - contracts/DegenerusAffiliate.sol (full file -- 847 lines; lines 557-930 for commission distribution)
    - contracts/DegenerusGame.sol (lines 1458-1479 for claimAffiliateDgnrs; search for: affiliateEthClaimed, totalAffiliateScore, levelDgnrsAllocation)
    - contracts/BurnieCoin.sol (search for: creditFlip -- used by quest rewards)
    - audit/v3.0-payout-lootbox-quest-affiliate.md (read current state from Task 1)
    - audit/v1.1-quest-rewards.md (quest reward specification)
    - audit/v1.1-affiliate-system.md (affiliate specification -- NOTE: describes old sequential depletion mechanism, code uses fixed allocation)
    - audit/v1.1-parameter-reference.md (quest BURNIE amounts, affiliate commission rates)
  </read_first>
  <action>
Audit the quest rewards (PAY-10) and affiliate commissions (PAY-11). Read DegenerusQuests.sol and DegenerusAffiliate.sol focusing on reward/commission distribution. Append PAY-10 and PAY-11 sections and update the Executive Summary.

**PAY-10: Quest Rewards and Streak Bonuses** (DegenerusQuests:1-1598)

The audit MUST cover:

1. **Quest reward amounts**: Verify slot 0 = 100 BURNIE, slot 1 = 200 BURNIE per v1.1-quest-rewards.md. Cross-reference actual constant values in the contract.

2. **Reward delivery**: Verify quest rewards are credited via `coin.creditFlip(player, amount)`. This means quest rewards become next-day coinflip wagers, NOT direct BURNIE transfers. Verify the creditFlip routing is correct.

3. **Streak mechanics**: Verify streak up to 100 days, contributing up to 10000 BPS to activity score. Verify the streak calculation, reset conditions, and bonus formula.

4. **Trigger conditions**: Verify quests are triggered by specific in-game actions. Verify each quest type has correct trigger conditions and cannot be re-triggered for the same completion.

5. **Double-claim guard**: Verify quest rewards cannot be claimed twice for the same quest completion. Verify the tracking mechanism.

**PAY-11: Affiliate Commissions** (DegenerusAffiliate:557-930, DegenerusGame:1458-1479)

The audit MUST cover:

1. **3-tier system**: Verify the 3 affiliate tiers. Verify tier thresholds and commission rates. Cross-reference against v1.1-affiliate-system.md (noting the v1.1 doc may describe the old mechanism).

2. **ETH commissions**: Verify affiliate ETH commission calculation and claim path. Verify weighted random lottery mechanism at DegenerusAffiliate:557-930. Verify 0.5 ETH cap per sender per level.

3. **Lootbox taper**: Verify affiliate lootbox commissions taper from 100% down to 25%. Verify the taper schedule matches the v1.1 spec or document differences.

4. **DGNRS claim path**: Verify `claimAffiliateDgnrs` at DegenerusGame:1458-1479. **CRITICAL**: Verify the actual code uses `levelDgnrsAllocation[currLevel]` and `totalAffiliateScore[currLevel]` as a FIXED denominator (not the sequential pool depletion described in v1.1-affiliate-system.md, per Research Open Question 1 / CP-06). Document the discrepancy between v1.1 doc and actual code as INFO (stale documentation, not a bug).

5. **Per-level claim guard**: Verify `affiliateDgnrsClaimedBy[lvl][player]` is set to true on claim. Verify one claim per level per player. Verify no bypass path.

6. **First-mover advantage analysis**: Using the actual code (fixed allocation, not sequential depletion), verify that early claimants do NOT get more DGNRS than late claimants for the same level. This should be confirmed PASS if using `totalAffiliateScore` as denominator (proportional, not first-come-first-served).

7. **claimablePool accounting**: Verify affiliate ETH commissions correctly interact with claimablePool (commissions are paid from what source pool?).

Update the Executive Summary to cover PAY-09, PAY-10, PAY-11. If the affiliate DGNRS mechanism differs from v1.1 doc, add a "Documentation Discrepancy" section.
  </action>
  <verify>
    <automated>grep -E "PAY-10.*(PASS|FINDING)" audit/v3.0-payout-lootbox-quest-affiliate.md && grep -E "PAY-11.*(PASS|FINDING)" audit/v3.0-payout-lootbox-quest-affiliate.md && grep -c "streak" audit/v3.0-payout-lootbox-quest-affiliate.md | grep -v "^0$" && grep "totalAffiliateScore\|levelDgnrsAllocation" audit/v3.0-payout-lootbox-quest-affiliate.md | head -1 && grep "Executive Summary" audit/v3.0-payout-lootbox-quest-affiliate.md && grep -c "claimAffiliateDgnrs" audit/v3.0-payout-lootbox-quest-affiliate.md | grep -v "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.0-payout-lootbox-quest-affiliate.md contains "PAY-10" followed by "PASS" or "FINDING"
    - File contains "PAY-11" followed by "PASS" or "FINDING"
    - File contains "100 BURNIE" or "slot 0" quest reward verification
    - File contains "streak" with mechanics analysis (up to 100 days, 10000 BPS)
    - File contains "creditFlip" routing for quest rewards
    - File contains "totalAffiliateScore" or "levelDgnrsAllocation" (actual code mechanism)
    - File contains "claimAffiliateDgnrs" with file:line reference to DegenerusGame
    - File contains "sequential depletion" or "fixed allocation" documenting the v1.1 doc discrepancy
    - File contains "affiliateDgnrsClaimedBy" per-level claim guard analysis
    - File contains "0.5 ETH" or "500000000000000000" cap per sender per level
    - File contains "taper" analysis for affiliate lootbox commissions
    - File contains "Executive Summary" covering PAY-09, PAY-10, PAY-11
  </acceptance_criteria>
  <done>PAY-10 and PAY-11 each have explicit PASS or FINDING verdicts with file:line references. Quest reward amounts and streak mechanics verified. Affiliate 3-tier system, DGNRS fixed-allocation mechanism, and per-level claim guard verified. v1.1 doc discrepancy documented. Executive summary covers all three requirements.</done>
</task>

</tasks>

<verification>
- audit/v3.0-payout-lootbox-quest-affiliate.md exists with PAY-09, PAY-10, PAY-11 sections
- All three verdicts are explicit PASS or FINDING-[severity] with file:line references
- All 5 lootbox reward types audited (whale pass, lazy pass, deity pass, future tickets, BURNIE)
- Quest streak mechanics verified
- Affiliate DGNRS mechanism verified as fixed-allocation (not sequential depletion)
- v1.1 documentation discrepancy documented as INFO
- claimablePool mutations traced through lootbox and affiliate paths
</verification>

<success_criteria>
- PAY-09 verdict: All 5 lootbox reward types verified with correct distribution and claim mechanisms
- PAY-10 verdict: Quest rewards verified with creditFlip routing and streak bonus mechanics
- PAY-11 verdict: Affiliate commissions verified with 3-tier system, fixed DGNRS allocation, and per-level guards
- Documentation discrepancy between v1.1 doc and actual code documented
- No double-claim or over-extraction path in any of the three systems
</success_criteria>

<output>
After completion, create `.planning/phases/27-payout-claim-path-audit/27-04-SUMMARY.md`
</output>
