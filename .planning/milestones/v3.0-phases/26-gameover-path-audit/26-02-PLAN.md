---
phase: 26-gameover-path-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/v3.0-gameover-safety-properties.md
autonomous: true
requirements:
  - GO-05
  - GO-06
  - GO-09

must_haves:
  truths:
    - "Every require/revert on the GAMEOVER path has an explicit verdict: either cannot block payouts (PASS) or can block payouts (FINDING)"
    - "CEI ordering is verified at every external call site on the GAMEOVER path with explicit state-write vs external-call ordering"
    - "No reentrancy vector exists that allows double-pay or fund stranding"
    - "VRF fallback path is verified secure and cannot permanently prevent GAMEOVER from firing"
  artifacts:
    - path: "audit/v3.0-gameover-safety-properties.md"
      provides: "GO-05, GO-06, GO-09 audit verdicts"
      contains: "GO-05"
    - path: "audit/v3.0-gameover-safety-properties.md"
      provides: "GO-06 reentrancy verdict"
      contains: "GO-06"
    - path: "audit/v3.0-gameover-safety-properties.md"
      provides: "GO-09 VRF fallback verdict"
      contains: "GO-09"
  key_links:
    - from: "contracts/modules/DegenerusGameGameOverModule.sol"
      to: "audit/v3.0-gameover-safety-properties.md"
      via: "revert and CEI analysis"
      pattern: "GO-05.*(PASS|FINDING)"
    - from: "contracts/modules/DegenerusGameAdvanceModule.sol"
      to: "audit/v3.0-gameover-safety-properties.md"
      via: "VRF fallback audit"
      pattern: "GO-09.*(PASS|FINDING)"
---

<objective>
Audit the safety properties of the GAMEOVER path: revert analysis (no require/revert can block payouts), reentrancy and CEI ordering (no double-pay or fund stranding), and VRF fallback (GAMEOVER can always eventually fire).

Purpose: These are the three highest-severity-if-violated properties. A revert blocking payouts could permanently lock funds. A reentrancy vector could drain the contract. A VRF failure could prevent GAMEOVER from ever firing, stranding all terminal distribution.

Output: audit/v3.0-gameover-safety-properties.md with PASS/FINDING verdicts for GO-05, GO-06, and GO-09.
</objective>

<execution_context>
@/home/zak/.claude/get-shit-done/workflows/execute-plan.md
@/home/zak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-gameover-path-audit/26-RESEARCH.md
@audit/v1.1-endgame-and-activity.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit reentrancy/CEI ordering and revert safety (GO-06, GO-05)</name>
  <files>audit/v3.0-gameover-safety-properties.md</files>
  <read_first>
    - contracts/modules/DegenerusGameGameOverModule.sol (full file -- 233 lines)
    - contracts/modules/DegenerusGameDecimatorModule.sol (lines 749-1027 -- terminal decimator; also search for _addClaimableEth, _creditClaimable, _processAutoRebuy)
    - contracts/modules/DegenerusGameJackpotModule.sol (search for: runTerminalJackpot, _distributeJackpotEth -- lines ~288-324 and ~1537-1576)
    - contracts/DegenerusGame.sol (search for: claimWinnings -- lines ~1415-1447; also search for delegatecall, handleGameOverDrain, handleFinalSweep, runTerminalDecimatorJackpot, runTerminalJackpot)
    - contracts/storage/DegenerusGameStorage.sol (search for: gameOverFinalJackpotPaid, finalSwept, _sendToVault, _sendEthOrWrap)
  </read_first>
  <action>
Perform two systematic analyses across the entire GAMEOVER execution path.

**Part A: GO-06 -- Reentrancy and CEI Ordering**

Map every SSTORE (state write) and every external call on the GAMEOVER path. For each external call, verify that all relevant state writes happen BEFORE the call. Produce a table:

```
| Step | State Writes (SSTORE) | External Call | CEI Correct? |
```

Specific items to verify:

1. **gameOverFinalJackpotPaid latch**: Set at what point? Before or after delegatecalls to decimator/jackpot? Can re-entry through advanceGame -> _handleGameOverPath -> handleGameOverDrain re-enter before latch is set?

2. **gameOver = true**: Set at GameOverModule ~line 112. Verify this is before all delegatecalls to decimator and jackpot modules.

3. **finalSwept latch**: Set in handleFinalSweep. Verify it prevents double execution of final sweep.

4. **delegatecall context**: Verify that `IDegenerusGame(address(this)).runTerminalDecimatorJackpot(...)` and `IDegenerusGame(address(this)).runTerminalJackpot(...)` execute as delegatecall (check DegenerusGame dispatch). Delegatecalls execute in the caller's context, so they can modify the caller's state. This is not reentrancy but verify no unexpected state mutation ordering.

5. **claimWinnings sentinel pattern**: DegenerusGame ~lines 1437-1446. Verify `claimableWinnings[player] = 1` (sentinel) set BEFORE `claimablePool -= payout` BEFORE external ETH send. Correct CEI.

6. **_sendToVault external calls**: Verify _sendToVault makes external calls (ETH transfer to vault, stETH transfer to sDGNRS). Verify all state is finalized before these calls. Can vault contract re-enter?

7. **burnRemainingPools external call**: GameOverModule ~line 162. Can sDGNRS contract re-enter during burn?

Deliver GO-06 verdict in C4A format.

**Part B: GO-05 -- Every Require/Revert on GAMEOVER Path**

Enumerate EVERY require() and revert statement on the GAMEOVER execution path. For each one, classify:
- **Benign**: Cannot fire during normal GAMEOVER (e.g., access control checks that are always satisfied by the caller)
- **Protective**: Correctly prevents invalid state (e.g., underflow guard)
- **Dangerous**: Could block payout execution if triggered (FINDING)

Specific items to check:

1. **GameOverModule revert E()**: Research says stETH transfer failures and ETH send failures use `revert E()`. Enumerate each one. Could a recipient contract refuse ETH and permanently block distribution?

2. **_sendToVault**: Research Pitfall 7 says _sendToVault uses `revert E()` with no try/catch. If vault or sDGNRS cannot receive ETH/stETH, handleFinalSweep reverts permanently. Classify severity.

3. **AdvanceModule safety valve** (~lines 442-444): Verify it cannot incorrectly abort a valid GAMEOVER.

4. **Decimator module reverts**: Any require/revert in runTerminalDecimatorJackpot that could fail during GAMEOVER?

5. **Jackpot module reverts**: Any require/revert in runTerminalJackpot/_distributeJackpotEth that could fail?

6. **admin.shutdownVrf() try/catch**: In handleFinalSweep, verify the try/catch correctly absorbs VRF shutdown failures.

Produce a full enumeration table:
```
| File:Line | Statement | Classification | Can Block Payout? | Notes |
```

Deliver GO-05 verdict in C4A format.

Create audit/v3.0-gameover-safety-properties.md with:
- Title: "# GAMEOVER Path Audit: Safety Properties (GO-05, GO-06, GO-09)"
- Executive Summary section
- Severity Definitions
- "## GO-06: Reentrancy and CEI Ordering" section with full verdict and SSTORE/call map
- "## GO-05: Revert Safety Analysis" section with full verdict and revert enumeration table
  </action>
  <verify>
    <automated>grep -E "GO-06.*(PASS|FINDING)" audit/v3.0-gameover-safety-properties.md && grep -E "GO-05.*(PASS|FINDING)" audit/v3.0-gameover-safety-properties.md && grep -c "revert\|require" audit/v3.0-gameover-safety-properties.md | grep -v "^0$" && grep -c "SSTORE\|state write\|CEI" audit/v3.0-gameover-safety-properties.md | grep -v "^0$" && grep "gameOverFinalJackpotPaid" audit/v3.0-gameover-safety-properties.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.0-gameover-safety-properties.md exists with "GO-06" followed by "PASS" or "FINDING"
    - File contains "GO-05" followed by "PASS" or "FINDING"
    - File contains "revert" or "require" at least 5 times (enumeration of all reverts)
    - File contains "SSTORE" or "state write" or "CEI" at least 3 times (CEI ordering analysis)
    - File contains "gameOverFinalJackpotPaid" (latch analysis)
    - File contains "finalSwept" (double-sweep prevention)
    - File contains "_sendToVault" with revert classification
    - File contains "delegatecall" context analysis
    - File contains "claimWinnings" sentinel pattern verification
    - File contains enumeration table with File:Line column
  </acceptance_criteria>
  <done>GO-06 has explicit verdict with SSTORE-vs-external-call ordering map for every external call on the GAMEOVER path. GO-05 has explicit verdict with complete enumeration of every require/revert on the path, each classified as benign/protective/dangerous. Both verdicts include file:line references.</done>
</task>

<task type="auto">
  <name>Task 2: Audit no-RNG-available GAMEOVER fallback path (GO-09)</name>
  <files>audit/v3.0-gameover-safety-properties.md</files>
  <read_first>
    - contracts/modules/DegenerusGameAdvanceModule.sol (lines 797-875 -- _gameOverEntropy function; also lines 405-462 -- _handleGameOverPath; also search for: GAMEOVER_RNG_FALLBACK_DELAY, _getHistoricalRngFallback, _tryRequestRng, rngWordByDay, rngWordCurrent, rngRequestTime)
    - contracts/modules/DegenerusGameGameOverModule.sol (line 126 -- `if (rngWord == 0) return;` retry logic; also lines 68-70 -- gameOverFinalJackpotPaid guard)
    - contracts/storage/DegenerusGameStorage.sol (search for: rngWordByDay, rngWordCurrent, rngRequestTime, GAMEOVER_RNG_FALLBACK_DELAY)
    - audit/v3.0-gameover-safety-properties.md (read current state from Task 1)
    - audit/v1.1-endgame-and-activity.md (endgame reference for VRF fallback description)
  </read_first>
  <action>
Audit the no-RNG-available GAMEOVER path -- the fallback behavior when VRF is unavailable during terminal distribution. Append GO-09 section to audit/v3.0-gameover-safety-properties.md.

The audit MUST cover these specific items:

1. **_gameOverEntropy function** (AdvanceModule ~lines 797-875):
   - Trace ALL 4 branches in order:
     a. `rngWordByDay[day] != 0` -- word already exists for today. PASS through.
     b. `rngWordCurrent != 0 && rngRequestTime != 0` -- VRF fulfilled. Apply and return.
     c. `rngRequestTime != 0` and elapsed >= 3 days -- fallback timer expired. Use historical VRF fallback.
     d. `rngRequestTime == 0` or `rngRequestTime != 0` and elapsed < 3 days -- no word yet. Request VRF or wait.
   - Verify return values: 0 means "no word, retry next advanceGame"; non-zero means "word available, proceed with GAMEOVER"

2. **_getHistoricalRngFallback** (AdvanceModule -- search for this function):
   - Verify it combines up to 5 historical VRF words (committed, non-manipulable)
   - Verify it includes prevrandao component
   - Assess validator manipulation capability: only 1-bit (propose or skip block). Is this acceptable for GAMEOVER-only fallback?
   - Verify the fallback word cannot be zero (which would cause retry loop)

3. **No-latch retry on missing RNG** (GameOverModule line 126):
   - `if (rngWord == 0) return;` -- does NOT set gameOverFinalJackpotPaid
   - Verify this allows handleGameOverDrain to be called again on next advanceGame
   - Verify no state corruption from partial execution (what state changes happen BEFORE this return?)
   - Verify that `gameOver` is NOT set when rngWord == 0 (check if line 126 is before or after line 112's `gameOver = true`)

4. **Timer start and progression**:
   - When VRF request fails: `rngRequestTime = ts` starts the 3-day timer
   - On subsequent advanceGame calls: _gameOverEntropy checks elapsed time
   - After 3 days: falls back to historical VRF
   - Verify no path where timer never starts (permanently stuck)
   - Verify no path where timer resets (extending the wait indefinitely)

5. **Deterministic/default selection with fallback word**:
   - Once the fallback word is generated, verify it is used identically to a normal VRF word in handleGameOverDrain
   - Verify all downstream functions (runTerminalDecimatorJackpot, runTerminalJackpot) accept the fallback word without additional validation

6. **Fund safety during wait period**:
   - While waiting for RNG (up to 3 days), verify no fund movement can corrupt the eventual GAMEOVER
   - Can normal gameplay advance during the wait? Can players withdraw?
   - Does the liveness guard remain triggered?

Deliver GO-09 verdict in C4A format. Update the Executive Summary with all three verdicts (GO-05, GO-06, GO-09).
  </action>
  <verify>
    <automated>grep -E "GO-09.*(PASS|FINDING)" audit/v3.0-gameover-safety-properties.md && grep -c "_gameOverEntropy\|_getHistoricalRngFallback" audit/v3.0-gameover-safety-properties.md | grep -v "^0$" && grep "GAMEOVER_RNG_FALLBACK_DELAY\|3.day\|fallback" audit/v3.0-gameover-safety-properties.md && grep "Executive Summary" audit/v3.0-gameover-safety-properties.md</automated>
  </verify>
  <acceptance_criteria>
    - audit/v3.0-gameover-safety-properties.md contains "GO-09" followed by "PASS" or "FINDING"
    - File contains "_gameOverEntropy" with file:line reference (AdvanceModule.sol:797+)
    - File contains "_getHistoricalRngFallback" with analysis
    - File contains "GAMEOVER_RNG_FALLBACK_DELAY" or "3 day" or "fallback" delay analysis
    - File contains analysis of line 126 (`if (rngWord == 0) return;`) and its no-latch behavior
    - File contains "prevrandao" or "validator manipulation" security assessment
    - File contains "Executive Summary" updated with all 3 verdicts (GO-05, GO-06, GO-09)
    - File contains all 4 branches of _gameOverEntropy traced
    - File contains analysis of timer start/reset behavior
  </acceptance_criteria>
  <done>GO-09 has explicit verdict with all 4 branches of _gameOverEntropy traced, _getHistoricalRngFallback security assessed, no-latch retry behavior verified, and timer progression confirmed. Executive summary reflects all three verdicts (GO-05, GO-06, GO-09) for this plan.</done>
</task>

</tasks>

<verification>
- audit/v3.0-gameover-safety-properties.md exists with GO-05, GO-06, and GO-09 sections
- All three verdicts are explicit PASS or FINDING-[severity] with file:line references
- CEI ordering map covers every external call on the GAMEOVER path
- Revert enumeration covers every require/revert on the GAMEOVER path
- VRF fallback analysis covers all 4 branches and the 3-day timer
</verification>

<success_criteria>
- GO-05 delivered with complete revert enumeration table (benign/protective/dangerous classification)
- GO-06 delivered with SSTORE-vs-external-call ordering map
- GO-09 delivered with all 4 entropy branches traced and fallback security assessed
- No revert on the GAMEOVER path can permanently block payouts (or a FINDING is raised)
- No reentrancy vector allows double-pay or fund stranding (or a FINDING is raised)
- VRF fallback guarantees GAMEOVER can eventually fire (or a FINDING is raised)
</success_criteria>

<output>
After completion, create `.planning/phases/26-gameover-path-audit/26-02-SUMMARY.md`
</output>
