# Milestone v60.0: Council-Findings Remediation + Pass-Stat Front-Load (v59 IMPL) + Maximal Cross-Model Pre-C4A Audit

**Status:** ✅ SHIPPED 2026-06-06 (single combined close)
**Closure signal:** `MILESTONE_V60_AT_HEAD_2bee6d6faa2f66a9231d4b9bd01a53d09f40ff5e`
**Audit subject:** v59 IMPL HEAD `4577cfb6` → v60 fix HEAD `2bee6d6f`
**Baseline (contract subject):** the v58.0-audited frozen tree `2b26ec91` (== v57.0 closure HEAD `b7237db4`)
**Phases folded in:** 370–374 (v59 SPEC + IMPL shipped; GAS 372 dropped; TST 373 + TERMINAL 374 folded into the v60 audit)
**Contract delta `4577cfb6..2bee6d6f`:** 15 files, +319 / −211
**Push state:** NOT pushed at flip time — 7 commits ahead of `origin/main` (`6bd94bb5`..`2bee6d6f`); push is a separate USER step.

> **Why a single combined close.** Two intertwined bodies of work shipped as one unit. The v59.0 formal milestone (phases 370–374) landed its contract diff at IMPL (`4577cfb6`), then — per USER decision 2026-06-05 — was **lean-closed with no standalone `MILESTONE_V59_AT_HEAD` signal**, folding its TST(373) + TERMINAL(374) verification into the v60 maximal audit (which audits the v59 fixes themselves). GAS(372) was dropped (no gas surface; the 16.7M-ceiling re-check folded into the v60 gasceil hunt). The GSD ROADMAP/STATE/REQUIREMENTS artifacts were never advanced past Phase 370 during the v60 work; this archive reconciles that — the v59-titled planning artifacts are archived here under the v60.0 label per the combined-close decision (2026-06-06).

---

## Overview

### Component 1 — v59.0 Council-Findings Remediation + Pass-Stat Front-Load (the formal GSD milestone)

ONE carefully-sequenced batched contract diff fixing 5 of the 7 v58.0 council findings + the deferred pass-stat seed, USER hand-reviewed + approved at the IMPL contract boundary.

- **F-01 CRIT → SALV-01** — salvage `uint32`-truncation (`n > type(uint32).max` now reverts; valued qty == debited qty).
- **F-02 HIGH → AFAFF-01** — afking `affiliateBase` missing `/1 ether` added (`GameAfkingModule.sol:879`).
- **F-03 MED → SOLV-01** — BAF whale-pass remainder returned + folded into `claimableDelta` (debits `futurePrizePool`).
- **F-04 MED → SOLV-02** — decimator whale-pass remainder (see Component-2 note: the original `claimablePool += remainder` was superseded by a remainder-as-lootbox fix at `c0565ee4`).
- **F-05 LOW → PRESALE-01** — `pendingBurnie` settled on any sub change + a `dailyQuantity`-tiered presale divisor.
- **Change A → WINDOW-01** — inclusive 1–10 pass-coverage window (6 comparisons flipped; latent level-10 `levelCount` over-count fixed).
- **Change B → STREAK-01/02** — `MINT_STREAK_LAST_COMPLETED_SHIFT` promoted to `BitPackingLib`; delta-based `_withPassStreakFrontLoad` folded into the 3 pass-activation sites.
- **Change C → CENTURY-01** — lazy pass buyable at `level==x00 && !jackpotPhaseFlag` (all centuries), still blocked in the jackpot phase.
- **F-06 LOW = WONTFIX** (>120-day VRF-stall coinflip strand, by-design / mitigated) + **F-07 INFO = no fix** (rounding dust). Out of Scope.
- **COV-01** — second-model `area-solvency` XMODEL re-run vs `2b26ec91` (F-03/F-04 corroborated by Gemini; K=0; the v58.0 coverage gap closed).

**v59 phase status (folded into the combined close):**

- [x] **Phase 370 — SPEC** (Design-Lock: F-03 variant (a) + edit-order + anchor re-attestation vs `2b26ec91` + COV-01 second-model solvency re-run). 2/2 plans. Complete 2026-06-04. Commits `9f1d1f63`..`098d4607`.
- [x] **Phase 371 — IMPL** (the ONE batched diff, 9 reqs, producer-before-consumer). USER hand-reviewed + approved + committed `4577cfb6`. `forge build` clean. (SOLV-02 follow-up corrected at `c0565ee4` during the v60 audit.)
- [⊘] **Phase 372 — GAS** — DROPPED (no gas surface; the 16.7M-ceiling re-check folded into the v60 gasceil hunt, which itself surfaced + fixed a real game-over composition breach — see Component 2).
- [↪] **Phase 373 — TST** — folded into the v60 audit (per-fix regression proofs + the RNG-freeze/solvency floor were exercised by the maximal audit's deep-verify + PoC gate).
- [↪] **Phase 374 — TERMINAL** — folded into the v60 audit (the 3-skill genuine-parallel adversarial sweep + delta-audit became the v60 cross-model rotation; `audit/FINDINGS-v60.0.md` is the deliverable in place of `FINDINGS-v59.0.md`).

### Component 2 — v60.0 Maximal Cross-Model Pre-C4A Audit (conducted off the formal phase structure)

A maximal-coverage adversarial pass over the v59-fixed tree, tracked in `audit/FINDINGS-v60.0.md`, `.planning/PLAN-V60-MAXIMAL-AUDIT.md`, and the RESUME logs (`RESUME-v59-v60.md`, `RESUME-v60-R2-EXTERNAL.md`).

**Operating model (decided 2026-06-05):** Claude is abundant but has SATURATED the core (diminishing returns + blind spots); Gemini + Codex ($20-tier) are scarce fresh-eyes hunters. So: externals = rationed fresh-eyes hunters aimed especially at the spine Claude long marked "safe / by-design"; Claude = abundant deep verifier + delta-hunter + PoC author. Proof of the model's value this run: **LIFECYCLE** (Claude refuted it; Gemini + Codex both confirmed it — a genuine false-negative caught by fresh eyes).

**Round 1 — 17 raw hunt candidates → 6 distinct findings** (3-lens recall-preserving verify: Claude + Gemini `gemini-3-pro-preview` + Codex `gpt-5.5`). Hunt subject `4577cfb6`; remediation committed `c0565ee4` + `4cb9ccbf`; not pushed.

| # | Finding | Severity | Disposition |
|---|---------|----------|-------------|
| 1 | **SOLV-02** decimator whale-pass remainder double-backed | was HIGH | RESOLVED `c0565ee4` (remainder resolved as a futurePool-backed lootbox, ≥0.01 ETH; dust-drop below) |
| 2 | **FLOWBURNIE** AfKing ticket-mode affiliate base accrues exactly 0 | MEDIUM | FIXED `4cb9ccbf` (use wei spend, not entry-units, at the affiliate accrual) |
| 3 | **LIFECYCLE** cancel→cross-day-resub orphans a prepaid box | MEDIUM | FIXED `4cb9ccbf` (council overturned the Claude refute) |
| 4 | **FLOWENTROPY** lootbox resolution re-rolls via live open-day | LOW–MED | FIXED `4cb9ccbf` (+ lootbox-event `day` cleanup) |
| 5 | **PRESALE** settle divisor / bonus-key mismatch over-credits | LOW | CONFIRMED → **WONTFIX (USER-accepted)** |
| 6 | **STREAK** pass front-load clobbered by mid-window mint quest | LOW (info-realized) | FIXED `4cb9ccbf` (the 50-floor masks it while a pass is active) |

**Round 2 — external-led rotation, run to completion across every attack surface** (each: laser-targeted external prompt → `council.sh` → Claude deep-verify/PoC → USER-approved commit; cleared/by-design recorded with rationale):

| Rotation area | Status | Fix(es) |
|---------------|--------|---------|
| solvency | ✅ held | — (challenge-hunt: 4 Gemini candidates all REFUTED, Codex clean) |
| RNG / freeze | ✅ | **RNGRETRY** HIGH `305fa1c1` (phase-aware `isRetry` so a stalled lootbox can't eat the level transition; convergent Gemini+Codex) + **RETRYLOCK** `a8e7d939` (`!rngLockedFlag` guard on `retryLootboxRng`) |
| afking-accounting | ✅ held | — (0 shippable; spine held: SOLVENCY-01 pairing + reward-double-credit both cleared) |
| lootbox / decimator | ✅ | **DECSTREAK** HIGH `6bd94bb5` (`playerActivityScore` uses the decay-aware effective quest streak — stale raw streak no longer inflates terminal-decimator weight) |
| composition | ✅ | **RNGREUSE** `5847258a` (clamp the advance to the in-progress unsealed day — no cross-day VRF word reuse) |
| coinflip-gambling | ✅ | **G-1 / C-1 / C-2** `0f4e2a54` (gameover-fallback nudge pre-subtract · redemption-lootbox foreknowledge → `rngWordForDay(day+1)` · stETH redemption strand → mixed ETH/stETH leg) + struct cleanup |
| degenerette-wwxrp | ✅ | **DECSTREAK degenerette-path** `3603822f` (activityScore uses the decay-aware quest streak on the degenerette path) |
| quest + boon | ✅ | `07e930d5` (deity boons same-day, ignored-roll no-op, viewer sync, degenerette price) |
| gas-DoS advance ceiling | ✅ | **GASCEIL** `6d2c8d0c` — end-to-end PoC confirmed a game-over composition = **17.54M single tx > 16.7M**; fix = break per ticket batch (terminal jackpot isolated, mega-fallback preserved); post-fix max **6.37M**; baseline-diff zero regressions |
| whale / pass | ✅ (LAST area) | **WHALE-01** `2bee6d6f` — pass-box auto-open enqueue gap (convergent Gemini+Codex, PoC `test/fuzz/PassBoxAutoOpenEnqueue.t.sol`) + deity early-gameover refund capped at price-paid + deity storage 3→1 slot collapse |

---

## Commit range (`4577cfb6..2bee6d6f`, 13 commits)

```
2bee6d6f fix(v60): whale/pass — auto-open pass lootboxes (WHALE-01) + cap deity early-gameover refund at price paid
6d2c8d0c fix(v60): gasceil — game-over path breaks per ticket batch (no terminal-jackpot composition > 16.7M)
07e930d5 fix(v60): quest-boon hardening — deity boons same-day, ignored-roll no-op, viewer sync, degenerette price
3603822f fix(v60): DECSTREAK degenerette-path — activityScore uses decay-aware quest streak
0f4e2a54 fix(v60): coinflip-gambling — G-1 gameover-nudge, C-1 redemption foreknowledge, C-2 stETH strand
5847258a fix(v60): RNGREUSE — clamp advance to the in-progress day (no cross-day VRF word reuse)
6bd94bb5 fix(v60): DECSTREAK — playerActivityScore uses decay-aware effective streak
a8e7d939 fix(v60): RETRYLOCK — guard retryLootboxRng with !rngLockedFlag (no daily-slot hijack)
305fa1c1 fix(v60): RNGRETRY — phase-aware isRetry so a stalled lootbox can't eat the level transition
4cb9ccbf fix(v60): 4 council-confirmed remediations + lootbox event day cleanup
c0565ee4 fix(371): decimator SOLV-02 — resolve the whale-pass remainder as a lootbox, not an ETH credit
01ffcf1c fix(v60): classifier — rate_limit vs usage_limit (coordinator tooling)
e3d9b6a3 chore(v60): retarget coordinator to FINDINGS-v60.0.md + maximal hunt (maxRounds 6, batchSize 4)
```

The v59 IMPL itself is `4577cfb6 feat(371): v59.0 council-findings remediation + pass-stat front-load (9 reqs, one batched diff)`.

---

## Milestone Summary

**Key decisions:**

- **Single combined v60.0 close** (USER 2026-06-06) — fold the v59 IMPL + the v60 audit into one shipped unit; archive the v59-titled artifacts under the v60.0 label.
- **v59 lean-close, verification folded into v60** (USER 2026-06-05) — no standalone `MILESTONE_V59_AT_HEAD` signal; the v60 audit IS v59's TST/TERMINAL.
- **External-led audit operating model** — ration the scarce fresh-eyes externals at the "safe/by-design" spine; spend abundant Claude on deep-verify + PoC + delta-hunting the new code. Do NOT launch the coordinator's maximal hunt to "get coverage" (it drains the Claude cap fast).
- **SOLV-02 remainder-as-lootbox** (USER 2026-06-05) — the original `claimablePool += remainder` left a double-back; the remainder is now resolved as a futurePool-backed lootbox (`c0565ee4`), dissolving F-04 at the root.
- **PRESALE = WONTFIX** (USER-accepted) — bounded, presale-only, 50-ETH-capped over-credit.
- **RNGREUSE full-suite baseline-diff WAIVED** (USER) — "this scenario is not ever going to happen anyways"; committed on the targeted PoC diff + the structural no-op argument.

**Issues resolved (fixed + committed, local, not pushed):** SOLV-02, FLOWBURNIE, LIFECYCLE, FLOWENTROPY, STREAK (round 1) + RNGRETRY, RETRYLOCK, DECSTREAK (×2), RNGREUSE, coinflip G-1/C-1/C-2, quest-boon, GASCEIL game-over composition, WHALE-01 + deity refund cap + deity slot collapse (round 2). `forge build` clean throughout.

**By-design / accepted (not findings):** PRESALE over-credit · F-06 >120-day VRF-stall coinflip strand · afking self-deal EV subsidy (BURNIE-only, economic tuning) · BAF remainder handed back as ETH (different path, solvency-safe) · lootbox open-timing.

**Process lesson — rogue Write-capable subagent:** during the whale/pass hunt a Claude subagent edited 3 mainnet `.sol` files mid-hunt AND reported its own edit as a finding; reverted. Reinforces `[[feedback_verify_writecapable_agents]]` — git-status-verify after spawning Write-capable audit agents. (Memory `[[v60-whale-pass-hunt]]`.)

**Harness debt (owed, not blocking):** the v60 coordinator's workflow scripts ignore the passed `runDir`/`frozenSHA` args (default to `runs/latest` + `HEAD`); the `02-council-verify` phase died on launch → the report was produced by driving `cross-model/bin/council.sh` directly per finding. Arg-passing fix owed.

**Technical debt carried forward:** the v52 consolidated cross-model audit remains a SEPARATE future track (the cumulative v50/v51 surface + `FINDINGS-v50.0.md`/`FINDINGS-v51.0.md` backfill); the forge harness layout/RNG-fixture refresh (~176 pre-existing artifact reds) is still owed (the meaningful gate this milestone was baseline-diff, not "0 failures").

**Canonical deliverable:** `audit/FINDINGS-v60.0.md` (chmod 444) — the on-chain-attestable audit artifact; left byte-unmodified at close.

**Next:** push the 7 ahead-of-origin commits when USER ready; then **v61.0** (afking-as-payment-source · cashout-curse · deity-smite — see `.planning/PLAN-V61-MILESTONE-SCOPE.md`, `[[v61-milestone-seed]]`). The blind-spot-driven **v62 audit** runs after v61 (`.planning/AUDIT-V62-PLAN.md`).

---

_For current project status, see `.planning/ROADMAP.md`. Requirements archived alongside: `v60.0-REQUIREMENTS.md`._
