---
phase: 304-spec-invariant-model-spec
plan: 05
type: execute
wave: 5
depends_on:
  - 304-01
  - 304-02
  - 304-03
  - 304-04
files_modified:
  - .planning/phases/304-spec-invariant-model-spec/304-SPEC.md
requirements: []
autonomous: true

must_haves:
  truths:
    - "§5 contains a complete file:line citation manifest enumerating every cited file:line in §1-§4 against contracts/StakedDegenerusStonk.sol + contracts/modules/DegenerusGameAdvanceModule.sol HEAD"
    - "Every file:line citation in 304-SPEC.md §1-§4 has been grep-verified — the manifest reports actual line numbers (post-grep) alongside the §N location that cited them"
    - "Citations that were CORRECT at write time appear with status VERIFIED; citations that needed correction appear with status CORRECTED (with both the original cited line and the verified actual line shown)"
    - "Zero by-construction / single-fn-reaches-all-paths claims per feedback_verify_call_graph_against_source.md"
    - "All THREE AdvanceModule resolveRedemptionPeriod call sites are documented (not just the prompt-cited :1230) — per feedback_verify_call_graph_against_source.md (Phase 294 BURNIE gap precedent)"
    - "No 'what changed' prose leaks into §1/§2/§3/§5 (cross-section grep proves §4 is the only zone with pre-refactor narration)"
  artifacts:
    - path: ".planning/phases/304-spec-invariant-model-spec/304-SPEC.md"
      provides: "§5 source-verified citation manifest"
      contains: "VERIFIED, CORRECTED, contracts/StakedDegenerusStonk.sol, contracts/modules/DegenerusGameAdvanceModule.sol"
  key_links:
    - from: "§5 citation manifest rows"
      to: "every file:line citation in §1-§4"
      via: "grep-verification against source HEAD"
      pattern: "contracts/StakedDegenerusStonk.sol:[0-9]+|contracts/modules/DegenerusGameAdvanceModule.sol:[0-9]+"
    - from: "§5 AdvanceModule call-site enumeration"
      to: "all three sStonk.resolveRedemptionPeriod call sites (the prompt cited only :1230 — actual sites are at :1230, :1293, :1323 per grep)"
      via: "exhaustive call-graph enumeration"
      pattern: ":1230|:1293|:1323"
---

<objective>
Fill `## §5 — Source-Verified Citation Manifest` with a complete file:line citation manifest. Every file:line citation made in §1-§4 is grep-verified against `contracts/StakedDegenerusStonk.sol` + `contracts/modules/DegenerusGameAdvanceModule.sol` HEAD. The manifest reports both the §N section that cited the line AND the verified actual line number. Citations that were correct at write time appear with status VERIFIED; citations that needed correction appear with status CORRECTED.

Purpose: §5 is the audit-trail proof that the SPEC.md is source-grounded. Per `feedback_verify_call_graph_against_source.md` (Phase 294 BURNIE gap precedent), inline-duplicated business logic is recurring in `DegenerusGameAdvanceModule.sol` — the planner already noted the prompt cited only `:1230` for the resolveRedemptionPeriod call site but THREE call sites exist (`:1230`, `:1293`, `:1323`). §5 documents this exhaustively. Phase 305 IMPL reads §5 as the load-bearing input for the diff — stale citations break the IMPL phase.

Output: §5 of 304-SPEC.md fully populated with citation manifest tables; SPEC.md complete and ready for Phase 305 IMPL consumption.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/304-spec-invariant-model-spec/304-SPEC.md
@.planning/phases/304-spec-invariant-model-spec/304-01-SUMMARY.md
@.planning/phases/304-spec-invariant-model-spec/304-02-SUMMARY.md
@.planning/phases/304-spec-invariant-model-spec/304-03-SUMMARY.md
@.planning/phases/304-spec-invariant-model-spec/304-04-SUMMARY.md
@/home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_verify_call_graph_against_source.md
@/home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md
@contracts/StakedDegenerusStonk.sol
@contracts/modules/DegenerusGameAdvanceModule.sol
</context>

<interfaces>
<!-- Verified at baseline HEAD MILESTONE_V43_AT_HEAD_8111cfc5189f628b64b500c881f9995c3edf0ed2: -->

<!-- contracts/StakedDegenerusStonk.sol key citations: -->
<!-- :88-117 ERRORS block (UnresolvedClaim :108, NotResolved :114, BurnsBlockedDuringRng :100, BurnsBlockedDuringLiveness :105, NoClaim :111, ExceedsDailyRedemptionCap :117) — EDGE-NN entries cite these by name -->
<!-- :209-214 PendingRedemption struct -->
<!-- :216-219 RedemptionPeriod struct -->
<!-- :221 mapping(address => PendingRedemption) public pendingRedemptions -->
<!-- :222 mapping(uint32 => RedemptionPeriod) public redemptionPeriods -->
<!-- :224 uint256 public pendingRedemptionEthValue (UNCHANGED post-refactor) -->
<!-- :225 uint256 internal pendingRedemptionBurnie (UNCHANGED post-refactor) -->
<!-- :226 uint256 internal pendingRedemptionEthBase (DELETED) -->
<!-- :227 uint256 internal pendingRedemptionBurnieBase (DELETED) -->
<!-- :229 uint256 internal redemptionPeriodSupplySnapshot (DELETED) -->
<!-- :230 uint32 internal redemptionPeriodIndex (DELETED) -->
<!-- :231 uint256 internal redemptionPeriodBurned (DELETED) -->
<!-- :254 uint256 private constant MAX_DAILY_REDEMPTION_EV = 160 ether -->
<!-- :486-495 burn() public entry -->
<!-- :491 livenessTriggered revert -->
<!-- :492 rngLocked revert -->
<!-- :585-610 resolveRedemptionPeriod (current 2-arg signature; SPEC-03 re-signs to 3-arg) -->
<!-- :588 uint32 period = redemptionPeriodIndex (DELETED — replaced by SPEC-03 dayToResolve arg) -->
<!-- :594 pendingRedemptionEthBase = 0 (DELETED) -->
<!-- :601 pendingRedemptionBurnieBase = 0 (DELETED) -->
<!-- :604-607 redemptionPeriods[period] = RedemptionPeriod({roll: roll, flipDay: flipDay}) (UNCHANGED slot write; the period source changes) -->
<!-- :618-684 claimRedemption (current no-arg signature; SPEC-02 re-signs to claimRedemption(uint32 day)) -->
<!-- :632 totalRolledEth = (claim.ethValueOwed * roll) / 100 -->
<!-- :638-643 50/50 split + isGameOver branch -->
<!-- :657 pendingRedemptionEthValue -= totalRolledEth -->
<!-- :683 _payEth(player, ethDirect) — pay-eth-LAST CEI ordering -->
<!-- :752-814 _submitGamblingClaimFrom -->
<!-- :754 if (amount == 0 || amount > bal) revert Insufficient -->
<!-- :757-762 redemptionPeriodIndex reset block (DELETED — Deletion 7) -->
<!-- :763 50% supply cap check -->
<!-- :790 pendingRedemptionEthBase += ethValueOwed (DELETED — replaced by pendingByDay[D].ethBase) -->
<!-- :792 pendingRedemptionBurnieBase += burnieOwed (DELETED — replaced by pendingByDay[D].burnieBase) -->
<!-- :796-797 UnresolvedClaim revert (DELETED — Deletion 6) -->
<!-- :801 ExceedsDailyRedemptionCap revert (160 ETH EV cap check) -->
<!-- :842-852 _payBurnie balance + coinflip fallback -->

<!-- contracts/modules/DegenerusGameAdvanceModule.sol key citations: -->
<!-- :19 IStakedDegenerusStonk interface import -->
<!-- :1222-1230 FIRST sStonk resolve call site: sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay) at :1230 -->
<!-- :1225 if (sdgnrs.hasPendingRedemptions()) gate -->
<!-- :1285-1293 SECOND sStonk resolve call site: at :1293 -->
<!-- :1288 second hasPendingRedemptions gate -->
<!-- :1315-1323 THIRD sStonk resolve call site: at :1323 -->
<!-- :1318 third hasPendingRedemptions gate -->
<!-- The prompt cited ONLY :1230 — the planner already discovered the other two via grep -->
<!-- SPEC-03 dayToResolve arg must be passed identically at ALL THREE sites -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Grep-verify every file:line cited in §1-§4 and build the citation manifest table</name>
  <files>.planning/phases/304-spec-invariant-model-spec/304-SPEC.md</files>
  <read_first>
    - `/home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_verify_call_graph_against_source.md` (the rule — no "by construction" claims; every cited file:line grep-verified; inline-duplicated business logic flagged)
    - `.planning/phases/304-spec-invariant-model-spec/304-SPEC.md` (full read — Task 1 sweeps every file:line citation in §1, §2, §3, §4 against source HEAD)
    - `.planning/phases/304-spec-invariant-model-spec/304-01-SUMMARY.md`, `304-02-SUMMARY.md`, `304-03-SUMMARY.md`, `304-04-SUMMARY.md` (each lists the line range its plan occupies in 304-SPEC.md AND any citations it knows need verification)
    - `contracts/StakedDegenerusStonk.sol` (full read — for grep-verification)
    - `contracts/modules/DegenerusGameAdvanceModule.sol` lines 1200-1400 (the three sStonk resolve call sites + their hasPendingRedemptions gates)
  </read_first>
  <action>
    Step 1: Run a verification sweep. For each file:line citation in 304-SPEC.md §1-§4, execute the following grep verification:

    - Extract every `contracts/StakedDegenerusStonk.sol:NNN` or `StakedDegenerusStonk.sol:NNN` or bare `:NNN` citation (where context implies sStonk).
    - For each, run `sed -n 'NNN,(NNN+5)p' contracts/StakedDegenerusStonk.sol` (or `Read` with `offset=NNN` `limit=5`) and verify the cited line contains the expected token (e.g. `:763` should be the 50% supply cap check — contains `redemptionPeriodSupplySnapshot / 2` at v43.0 baseline).
    - For each `contracts/modules/DegenerusGameAdvanceModule.sol:NNN` citation, same verification.
    - Build a status table: VERIFIED (line content matches expected) | CORRECTED (line content does not match; record the actual line number with the expected content) | ABSENT (cited line is past EOF or content nowhere in file).

    Step 2: Write `## §5 — Source-Verified Citation Manifest`. Remove the placeholder. Structure:

    Introductory paragraph (3-5 sentences): cite `feedback_verify_call_graph_against_source.md` (no "by construction" claims; grep-verification mandatory). State that §5 enumerates every file:line citation in §1-§4. Note the baseline HEAD `MILESTONE_V43_AT_HEAD_8111cfc5189f628b64b500c881f9995c3edf0ed2`.

    Sub-section `### §5.1 — contracts/StakedDegenerusStonk.sol citation manifest`:

    A Markdown table with columns: `Cited at §N` | `Cited line` | `Expected content` | `Status` | `Actual line (if CORRECTED)` | `Verified content snippet`.

    Populate with every file:line citation in §1-§4 against StakedDegenerusStonk.sol. Expected entries (at minimum — sweep may find more):
    - `:88-117` (ERRORS block) — cited by §3 EDGE-NN revert names
    - `:108` UnresolvedClaim error — cited by Deletion 6
    - `:114` NotResolved error — cited by EDGE-05
    - `:100` BurnsBlockedDuringRng — cited by EDGE-11
    - `:105` BurnsBlockedDuringLiveness — cited by EDGE-12
    - `:117` ExceedsDailyRedemptionCap — cited by EDGE-15
    - `:111` NoClaim — cited by EDGE-10
    - `:221` pendingRedemptions mapping — cited by §1 INV-NN + §2 SPEC-02
    - `:222` redemptionPeriods mapping — cited by §1 + §2 + §3
    - `:224-227` pending*EthValue/Burnie/EthBase/BurnieBase — cited by §1 + §2 + §4
    - `:229-231` redemptionPeriod{SupplySnapshot,Index,Burned} — cited by §2 + §4 (Deletions 1, 2, 3)
    - `:254` MAX_DAILY_REDEMPTION_EV constant — cited by §1 INV-11
    - `:486-495` burn() entry — cited by §3 EDGE-11/12
    - `:491` livenessTriggered revert — cited by EDGE-12
    - `:492` rngLocked revert — cited by EDGE-11
    - `:585-610` resolveRedemptionPeriod — cited by §1 + §2 + §3 + §4
    - `:588` `uint32 period = redemptionPeriodIndex` — cited by §4 Deletion 1
    - `:594` `pendingRedemptionEthBase = 0` — cited by §4 Deletion 4
    - `:601` `pendingRedemptionBurnieBase = 0` — cited by §4 Deletion 5
    - `:604-607` `redemptionPeriods[period] = RedemptionPeriod(...)` — cited by §1
    - `:618-684` claimRedemption — cited by §2 SPEC-02 + §3 EDGE-05/08/09/10
    - `:632` `totalRolledEth = (claim.ethValueOwed * roll) / 100` — cited by §1 + §3 EDGE-09
    - `:638-643` 50/50 split + isGameOver branch — cited by §2 SPEC-04 (a) + §3 EDGE-08
    - `:657` `pendingRedemptionEthValue -= totalRolledEth` — cited by §1 + §3 EDGE-09
    - `:683` `_payEth(player, ethDirect)` pay-eth-LAST CEI — cited by §3 EDGE-10
    - `:752-814` _submitGamblingClaimFrom — cited by §1 + §2 + §3 + §4
    - `:754` `if (amount == 0 || amount > bal) revert Insufficient` — cited by §2 SPEC-04 (b)
    - **`:757-762` redemptionPeriodIndex reset block — CRITICAL: prompt cited `:758-762`; planner already noted the block opens at `:757` with the `uint32 currentPeriod = ...` line being part of the block. Verify the canonical range — does the deleted block include the local variable declaration at `:757` or only the `if (...)` block at `:758-762`? Both views are defensible — document the verified range and the rationale (likely the IMPL diff deletes only `:758-762` since `:757` reads `currentPeriod` which is still used downstream at `:806`).**
    - `:763` 50% supply cap check — cited by §1 INV-10 + §3 EDGE-14
    - `:790` `pendingRedemptionEthBase += ethValueOwed` — cited by §4 Deletion 4
    - `:792` `pendingRedemptionBurnieBase += burnieOwed` — cited by §4 Deletion 5
    - `:796-797` UnresolvedClaim revert — cited by §2 SPEC-02 + §4 Deletion 6
    - `:801` ExceedsDailyRedemptionCap revert (160 ETH EV cap) — cited by §1 INV-11 + §3 EDGE-15
    - `:842-852` _payBurnie — cited by §3 EDGE-18

    Sub-section `### §5.2 — contracts/modules/DegenerusGameAdvanceModule.sol citation manifest`:

    Same table format. Populate the THREE sStonk resolve call sites:
    - `:1222-1230` first sStonk resolve call site — cited at §2 SPEC-03 + §1 + §3 EDGE-NN that reference the advance
    - `:1225` first `hasPendingRedemptions()` gate
    - `:1230` first `sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay)` call (the ONLY line cited by the prompt — Plan 02 already documented all three)
    - `:1285-1293` second sStonk resolve call site
    - `:1288` second `hasPendingRedemptions()` gate
    - `:1293` second `sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay)` call
    - `:1315-1323` third sStonk resolve call site
    - `:1318` third `hasPendingRedemptions()` gate
    - `:1323` third `sdgnrs.resolveRedemptionPeriod(redemptionRoll, flipDay)` call

    State explicitly: "Per `feedback_verify_call_graph_against_source.md` (Phase 294 BURNIE gap precedent of inline-duplicated business logic in DegenerusGameAdvanceModule.sol), the resolveRedemptionPeriod call site is inline-duplicated three times. SPEC-03 dayToResolve arg MUST be passed identically at ALL THREE sites; Phase 305 IMPL diff MUST modify all three call sites (not just `:1230`). The prompt cited only `:1230` — this is INCOMPLETE; §5.2 documents the full call graph."

    Sub-section `### §5.3 — Cross-section integrity check`:

    Run these meta-checks:
    1. No "what changed" prose in §1/§2/§3/§5 (only §4 is the exception zone). Verify by scanning §1-§3 + §5 ranges for the lexicon: `previously`, `formerly`, `used to be`, `changed from`, `was X now Y`. If any found, list them as integrity violations.
    2. Every INV-01..12 referenced in §3 EDGE entries via `Tests INV-NN` cite matches an actual §1 INV-NN subsection. Verify by enumeration.
    3. Every SPEC-01..05 referenced in §3 EDGE entries via `Depends on SPEC-NN` cite matches an actual §2 SPEC-NN subsection. Verify by enumeration.
    4. Every SPEC-NN replacement cited in §4 deletion subsections matches an actual §2 SPEC-NN subsection.
    5. No "by construction" or "covered by single fn" claims anywhere in §1-§4 (per `feedback_verify_call_graph_against_source.md`). Verify by `grep -i "by construction\|covered by single\|trivially safe"` over the full 304-SPEC.md — must return zero matches.

    Sub-section `### §5.4 — Manifest integrity attestation`:

    Final paragraph (3-5 sentences). Attest:
    - Every file:line citation in §1-§4 has been grep-verified against source HEAD.
    - The CORRECTED column lists the citations that needed amendment AND the verified line number that replaces them.
    - The three sStonk resolve call sites in `contracts/modules/DegenerusGameAdvanceModule.sol` are documented exhaustively per `feedback_verify_call_graph_against_source.md`.
    - The cross-section integrity check confirms §1/§2/§3/§5 contain no pre-refactor narration (§4 is the exception zone per `feedback_no_history_in_comments.md`).
    - This SPEC.md is ready for Phase 305 IMPL consumption — every cited line in the diff has a §5 manifest row backing it.

    Length target: 200-350 lines for §5 total. Tables MUST be Markdown tables (not embedded code blocks).

    Final sweep at end of task: append (or update) the FOOTER LINE at the bottom of 304-SPEC.md: `**END OF SPEC — Phase 305 IMPL consumes this document as load-bearing input. Baseline HEAD: MILESTONE_V43_AT_HEAD_8111cfc5189f628b64b500c881f9995c3edf0ed2.**`
  </action>
  <verify>
    <automated>bash -c 'grep -q "^## §5 — Source-Verified Citation Manifest" .planning/phases/304-spec-invariant-model-spec/304-SPEC.md && grep -q "^### §5.1 — contracts/StakedDegenerusStonk.sol citation manifest" .planning/phases/304-spec-invariant-model-spec/304-SPEC.md && grep -q "^### §5.2 — contracts/modules/DegenerusGameAdvanceModule.sol citation manifest" .planning/phases/304-spec-invariant-model-spec/304-SPEC.md && grep -q ":1230" .planning/phases/304-spec-invariant-model-spec/304-SPEC.md && grep -q ":1293" .planning/phases/304-spec-invariant-model-spec/304-SPEC.md && grep -q ":1323" .planning/phases/304-spec-invariant-model-spec/304-SPEC.md && echo CITATION_MANIFEST_COMPLETE'</automated>
  </verify>
  <acceptance_criteria>
    - §5 contains sub-sections §5.1 (sStonk manifest), §5.2 (AdvanceModule manifest), §5.3 (cross-section integrity check), §5.4 (manifest integrity attestation)
    - §5.2 explicitly enumerates all THREE AdvanceModule call sites (`:1230`, `:1293`, `:1323`); verify via `grep -c ":1230\|:1293\|:1323" 304-SPEC.md` returns ≥ 3
    - §5.1 contains rows for EVERY file:line cited in §1-§4 — at minimum the citations listed in the task action's interface block (verify by enumeration; missing rows are a fail)
    - §5.1 row for `:757-762` reset block documents the exact verified line range AND the rationale for the chosen range (whether the IMPL diff deletes the `:757` `currentPeriod = ...` local variable declaration or only the `:758-762` if-block)
    - §5.3 cross-section integrity check executes all 5 checks AND lists violations (if any). Zero violations is acceptable.
    - §5.4 explicitly attests "ready for Phase 305 IMPL consumption"
    - The placeholder `_To be filled by Plan 05_` under §5 is removed
    - The closing FOOTER LINE is present at end of 304-SPEC.md: `**END OF SPEC ...**`
    - `grep -i "by construction\|covered by single\|trivially safe" 304-SPEC.md` returns zero matches (per feedback rule)
    - Cross-check: `grep -c "^### INV-" 304-SPEC.md` returns 12; `grep -c "^### SPEC-" 304-SPEC.md` returns 5; `grep -c "^### EDGE-" 304-SPEC.md` returns 18; `grep -c "^### Deletion [1-7]:" 304-SPEC.md` returns 7. Full SPEC.md is internally consistent.
  </acceptance_criteria>
  <done>304-SPEC.md §5 fully populated with grep-verified citation manifest. All file:line citations from §1-§4 documented as VERIFIED or CORRECTED. All three AdvanceModule call sites enumerated. Cross-section integrity check passed. SPEC.md ready for Phase 305 IMPL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| §5 citation manifest → Phase 305 IMPL diff author | The manifest is the load-bearing input for the IMPL phase; every cited line in the diff must trace back to a §5 row |
| §5.2 AdvanceModule enumeration → 3 inline-duplicated call sites | Per `feedback_verify_call_graph_against_source.md` Phase 294 precedent, inline duplication is a recurring bug surface; missing one call site in IMPL re-introduces a partial-fix bug |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-304-05-01 | Tampering | Citation manifest is incomplete → §1-§4 citations exist that have no §5 row → Phase 305 IMPL acts on un-verified line numbers | mitigate | Task 1 explicitly enumerates the expected citations from §1-§4 and instructs the executor to sweep for any unlisted citations. Acceptance criterion enforces row-per-citation |
| T-304-05-02 | Tampering | §5.2 omits one of the three sStonk resolve call sites → IMPL diff modifies only `:1230` → V-184 elimination is partial → cross-day re-roll still reachable via the un-fixed call sites | mitigate | Task action explicitly documents all three sites; acceptance criterion checks `:1230 + :1293 + :1323` all present; manifest attestation specifically calls out feedback_verify_call_graph_against_source.md Phase 294 precedent |
| T-304-05-03 | Information disclosure | "By construction" claim leaks into §5 manifest attestation | mitigate | Acceptance criterion runs `grep -i "by construction"` against full 304-SPEC.md — must return zero matches |
| T-304-05-04 | Repudiation | Pre-refactor narration leaks from §4 into §1/§2/§3/§5 | mitigate | §5.3 cross-section integrity check explicitly scans for the `previously\|formerly\|used to be` lexicon outside §4; violations listed as integrity failures |
| T-304-SPEC | Threat-enumeration locus | full attack-surface enumeration | transfer to §3 (Plan 03) + §4 (Plan 04) | §5 owns only the manifest-integrity threats; the canonical threat enumeration is §3 + §4 |
</threat_model>

<verification>
- §5.1 sStonk manifest table contains rows for every file:line citation in §1-§4
- §5.2 AdvanceModule manifest enumerates `:1230`, `:1293`, `:1323` (all three call sites)
- §5.3 cross-section integrity check: zero "what changed" lexicon violations in §1/§2/§3/§5; zero "by construction" claims anywhere
- §5.4 attestation states "ready for Phase 305 IMPL consumption"
- Full SPEC internally consistent: 12 INV + 5 SPEC + 18 EDGE + 7 Deletions all present
- FOOTER LINE at end of 304-SPEC.md
</verification>

<success_criteria>
- §5 of 304-SPEC.md fully populated with grep-verified citation manifest
- §5.1 sStonk manifest documents every file:line cited in §1-§4
- §5.2 AdvanceModule manifest enumerates all three sStonk resolve call sites (`:1230`, `:1293`, `:1323`)
- §5.3 cross-section integrity check passed
- §5.4 manifest integrity attestation states "ready for Phase 305 IMPL consumption"
- 304-SPEC.md is internally consistent: 12 INV + 5 SPEC + 18 EDGE + 7 Deletions; zero "by construction" claims; zero pre-refactor narration outside §4
- FOOTER LINE present at end of file
</success_criteria>

<output>
Create `.planning/phases/304-spec-invariant-model-spec/304-05-SUMMARY.md` when done. SUMMARY captures: (1) the list of citations that were CORRECTED at grep-verification time (Phase 305 IMPL must use the corrected line numbers, NOT the originals); (2) the all-three-call-site attestation for AdvanceModule (the load-bearing artifact for IMPL-03); (3) any cross-section integrity violations found (must be zero for SPEC closure); (4) final attestation that 304-SPEC.md is ready for Phase 305 IMPL.
</output>
