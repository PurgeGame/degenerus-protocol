---
phase: 304-spec-invariant-model-spec
plan: 02
type: execute
wave: 2
depends_on:
  - 304-01
files_modified:
  - .planning/phases/304-spec-invariant-model-spec/304-SPEC.md
requirements:
  - SPEC-01
  - SPEC-02
  - SPEC-03
  - SPEC-04
  - SPEC-05
autonomous: true

must_haves:
  truths:
    - "§2.0 Priority Statement is the FIRST subsection under §2 and contains 3 numbered clauses: (1) hard floor (per-day per-player keying + INV-01/06/07 are LOAD-BEARING, no optimization may collapse the (player, day) key space or alias rolls across days); (2) soft target (within the keying floor, minimize storage slots, prefer delete refunds, lazy-init on first write, pack struct fields; TST-06 +5%/+0% is target not contract); (3) conflict resolution (any gas optimization that weakens an INV or §4 design-intent guarantee is REJECTED regardless of gas savings; rejection documented in Plan 04 §4)"
    - "§2 contains exactly 5 SPEC-NN subsections, each with the LOCKED decision + brief rationale + impact on the 5 storage-slot deletions + impact on call-sites"
    - "SPEC-01 locks the DayPending struct shape verbatim: { uint256 ethBase; uint256 burnieBase; uint128 supplySnapshot; uint128 burned; }"
    - "SPEC-02 locks composite-key `pendingRedemptions[player][day]` mapping shape AND locks removal of the UnresolvedClaim revert"
    - "SPEC-03 locks `resolveRedemptionPeriod(uint16 roll, uint32 flipDay, uint32 dayToResolve)` signature AND locks the value of `dayToResolve` (either `currentDayView() - 1` or an explicit equivalent locked at this plan)"
    - "SPEC-04 locks 4 sub-decisions (a-d): gameOver mid-pending semantics, zero-rounded ethValueOwed handling, pendingByDay[D] storage-refund timing, pendingRedemptions[player][day] storage-refund at claim"
    - "SPEC-05 locks 50% supply cap snapshot timing: supplySnapshot lazy-init on first burn of day D (when slot == 0); immutable for rest of day D"
  artifacts:
    - path: ".planning/phases/304-spec-invariant-model-spec/304-SPEC.md"
      provides: "§2 SPEC-01..05 design decision locks"
      contains: "SPEC-01:, SPEC-02:, SPEC-03:, SPEC-04:, SPEC-05:"
  key_links:
    - from: "§2 SPEC-NN entries"
      to: "§1 INV-NN entries that depended on SPEC locks (esp. INV-08 reading `pendingByDay[dayToResolve = D-1]` per SPEC-03; INV-10 lazy-init snapshot per SPEC-05; INV-12 gameOver semantics per SPEC-04 (a))"
      via: "forward-reference resolution"
      pattern: "per SPEC-0[1-5]"
    - from: "§2 SPEC-NN locks"
      to: "Phase 305 IMPL diff"
      via: "load-bearing input"
      pattern: "IMPL-0[1-4]"
---

<objective>
Fill `## §2 — Locked Design Decisions (SPEC-01..05)` with a §2.0 Priority Statement (security-first / gas-efficient within) + 5 fully-locked design decisions, each with rationale + impact statement. Each lock is non-negotiable input to Phase 305 IMPL. The SPEC-04 (a-d) sub-decisions are the ones REQUIREMENTS.md flagged "to lock at SPEC phase" — this plan IS where they lock.

USER-STATED PRIORITY ORDERING (load-bearing for Phase 305 IMPL trade-off decisions): the entire system must be completely secure and non-manipulable. RNG non-manipulability (V-184 elimination via per-day per-player keying) is the HARD floor. Within that floor, gas efficiency is maximized. If any local gas optimization would weaken an INV or §4 design-intent guarantee, the optimization is REJECTED — regardless of savings. §2.0 codifies this ordering so the IMPL phase can reference it when evaluating trade-offs.

Purpose: §2 is the contract between Phase 304 SPEC and Phase 305 IMPL. The IMPL plan-phase writes the diff against these locks. Vague locks here produce contested IMPL choices later.

Output: §2 of 304-SPEC.md fully populated with §2.0 Priority Statement + SPEC-01..05 subsections; §1 forward-references resolve cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/304-spec-invariant-model-spec/304-SPEC.md
@.planning/phases/304-spec-invariant-model-spec/304-01-SUMMARY.md
@/home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md
@/home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_frozen_contracts_no_future_proofing.md
@contracts/StakedDegenerusStonk.sol
@contracts/modules/DegenerusGameAdvanceModule.sol
</context>

<interfaces>
<!-- Pre-refactor source locations Plan 02 must reference: -->
<!-- contracts/StakedDegenerusStonk.sol :209-214 PendingRedemption struct -->
<!-- :221 `mapping(address => PendingRedemption) public pendingRedemptions` (re-key target) -->
<!-- :226-227 pendingRedemptionEthBase + pendingRedemptionBurnieBase (subsumed into pendingByDay[D]) -->
<!-- :229-231 redemptionPeriodSupplySnapshot + redemptionPeriodIndex + redemptionPeriodBurned (subsumed into pendingByDay[D]) -->
<!-- :585-610 resolveRedemptionPeriod current implementation (SPEC-03 re-signs this) -->
<!-- :618-684 claimRedemption current implementation (SPEC-02 re-signs this; SPEC-04 (d) governs refund site) -->
<!-- :752-814 _submitGamblingClaimFrom current implementation (SPEC-05 lazy-init snapshot timing) -->
<!-- :796-797 UnresolvedClaim revert (SPEC-02 removes this) -->
<!-- :757-762 redemptionPeriodIndex reset block (deleted in IMPL; SPEC-02 removes via composite-key) -->
<!-- contracts/modules/DegenerusGameAdvanceModule.sol -->
<!-- :1222-1230 first sStonk resolve call site (SPEC-03 dayToResolve arg) -->
<!-- :1285-1293 second sStonk resolve call site (SAME pattern) -->
<!-- :1315-1323 third sStonk resolve call site (SAME pattern) -->
<!-- All three call sites must pass identical dayToResolve value -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fill §2 SPEC-01..05 LOCKED design decisions</name>
  <files>.planning/phases/304-spec-invariant-model-spec/304-SPEC.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 39-46 (canonical SPEC-01..05 wording)
    - `.planning/ROADMAP.md` Phase 304 success criterion #2 (line 49 — "SPEC-01..05 design decisions LOCKED with rationale")
    - `.planning/ROADMAP.md` Phase 305 IMPL goal (lines 56-58 — describes the post-refactor surface SPEC-NN locks must produce)
    - `.planning/phases/304-spec-invariant-model-spec/304-SPEC.md` (current state after Plan 01 — §1 invariants that reference SPEC locks; especially INV-08, INV-10, INV-11, INV-12 forward-references)
    - `.planning/phases/304-spec-invariant-model-spec/304-01-SUMMARY.md` (Plan 01 SUMMARY lists which INV-NN entries forward-reference SPEC locks — this plan must resolve them)
    - `contracts/StakedDegenerusStonk.sol` lines 209-232 (current storage layout — name the slots being subsumed), 585-610 (resolveRedemptionPeriod signature being re-signed), 618-684 (claimRedemption signature being re-signed and the lootbox-eth split logic at `:638-643` that SPEC-04 (a) gameOver semantics interact with), 752-814 (_submitGamblingClaimFrom — SPEC-05 lazy-init site), 796-797 (UnresolvedClaim revert removal site), 757-762 (redemptionPeriodIndex reset block — DELETED by composite-key keying)
    - `contracts/modules/DegenerusGameAdvanceModule.sol` lines 1222-1230 + 1285-1293 + 1315-1323 (THREE sStonk resolve call sites — SPEC-03's `dayToResolve` arg must work for all three)
    - `/home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md` (§2 describes POST-REFACTOR locked state; pre-refactor prose reserved for §4)
  </read_first>
  <action>
    Under `## §2 — Locked Design Decisions (SPEC-01..05)`, remove the placeholder and write §2.0 Priority Statement FIRST, then 5 SPEC-NN subsections.

    **§2.0 Priority Statement (write this BEFORE SPEC-01):**

    Heading: `### §2.0 — Priority Statement (security-first; gas-efficient within)`

    Write three numbered clauses in this exact order:

    1. **Hard floor (non-negotiable):** Complete RNG non-manipulability. Per-day per-player keyed redemption state (`pendingByDay[day]` + `pendingRedemptions[player][day]` + `redemptionPeriods[day]`) is LOAD-BEARING. INV-01 (write-once roll per day), INV-06 (no cross-player roll manipulation), INV-07 (no self-roll manipulation via timing) are the canonical security properties. No optimization, packing, or shortcut may collapse the `(player, day)` key space or alias rolls across days. The V-184 attack class (RNGLOCK-FIXREC §103) is closed structurally — not by gating, not by ordering checks, by absence of the overwrite primitive.

    2. **Soft target (within the floor):** Within the keying floor, minimize gas: (a) struct packing where types permit (SPEC-01 packs `supplySnapshot + burned` into one slot via `uint128`); (b) `delete` on resolution + claim for storage refunds (SPEC-04 (c) + (d)); (c) lazy-init on first write to avoid wasted SSTOREs (SPEC-05 snapshot timing); (d) skip `resolveRedemptionPeriod` entirely on no-pending days via `hasPendingRedemptions(day)` single-SLOAD check (SPEC-03 secondary lock). TST-06 gas regression `≤+5%` burn / `≤+0%` claim is the SOFT target — aspirational, not a hard contract. The IMPL phase chases this target without violating clause 1.

    3. **Conflict resolution:** Any gas optimization that weakens an INV-01..12 property, or that requires a §4 design-intent walk to accept a previously-rejected design, is REJECTED — regardless of gas savings. The rejection is documented in Plan 04 §4 under a "considered + rejected" subsection so Phase 305 IMPL can see the reasoning trail. Reverse direction: if an INV property requires `+X%` gas to enforce, the gas cost is ACCEPTED and TST-06 targets revise downward at Phase 306 (the targets follow correctness, not the other way around).

    Length: ~25-40 lines. No bullet lists beyond the three numbered clauses; prose under each.

    Then proceed to SPEC-01..05 subsections. Each subsection uses this exact structure:

    `### SPEC-NN: <short name>`

    `**Lock:** <the locked decision stated declaratively. No "we should" — only "the post-refactor state IS X">`

    `**Rationale:** <2-4 sentences citing the structural reason; reference the V-184 mechanic (RNGLOCK-FIXREC §103) for SPEC-01/02/03/05; reference `feedback_frozen_contracts_no_future_proofing.md` pre-launch posture for SPEC-04 where applicable>`

    `**Impact on storage layout:** <which of the 5 deleted slots this lock subsumes, OR which new slot it introduces, OR which existing slot it re-keys; cite the pre-refactor file:line being removed (verified at Plan 05)>`

    `**Impact on call sites:** <which functions in `contracts/StakedDegenerusStonk.sol` and `contracts/modules/DegenerusGameAdvanceModule.sol` change signature or storage-access pattern as a direct consequence>`

    `**Resolves §1 forward-reference:** <if any INV-NN subsection in §1 forward-referenced this SPEC-NN, name it; e.g. SPEC-04 (a) resolves INV-12 gameOver mid-pending forward-reference>`

    Per-SPEC content guidance (use these to author each lock):

    **SPEC-01 (DayPending struct shape):**
    - **Lock:** `struct DayPending { uint256 ethBase; uint256 burnieBase; uint128 supplySnapshot; uint128 burned; }` declared INSIDE the StakedDegenerusStonk contract scope (post-refactor); `mapping(uint32 => DayPending) internal pendingByDay`. Exactly 3 storage slots per active day (slot 1: ethBase; slot 2: burnieBase; slot 3: packed supplySnapshot+burned).
    - **Impact on storage:** subsumes 5 pre-refactor slots (`pendingRedemptionEthBase` `:226`, `pendingRedemptionBurnieBase` `:227`, `redemptionPeriodSupplySnapshot` `:229`, `redemptionPeriodIndex` `:230`, `redemptionPeriodBurned` `:231`) into ONE day-keyed mapping. Cumulative scalars (`pendingRedemptionEthValue` `:224`, `pendingRedemptionBurnie` `:225`) UNCHANGED.
    - **Rationale tie-in:** the V-184 exploit (RNGLOCK-FIXREC §103) is structurally eliminated because the single-pool `redemptionPeriodIndex` indirection no longer exists; each day's pool is independently keyed and cannot be overwritten across days.

    **SPEC-02 (composite-key pendingRedemptions + UnresolvedClaim removal):**
    - **Lock:** `mapping(address => mapping(uint32 => PendingRedemption)) public pendingRedemptions` (composite key per (player, day)). `claimRedemption(uint32 day)` signature — caller specifies day; no batch helper (immediate-claim UX). `UnresolvedClaim` revert at `:796-797` REMOVED — under composite keying multiple-day pending claims are safe and the revert becomes meaningless.
    - **Impact on call sites:** `_submitGamblingClaimFrom` writes `pendingRedemptions[beneficiary][currentDay]` (composite); `claimRedemption(day)` reads `pendingRedemptions[msg.sender][day]` and `redemptionPeriods[day]`; existing `NotResolved` revert (`period.roll == 0`) preserved (`:624`). No change to `_payEth` / `_payBurnie` flows.
    - **Resolves §1:** none directly; sets the storage pattern §1 INV-04/INV-05/INV-07/INV-11 invariants depend on.

    **SPEC-03 (dayToResolve arg on resolveRedemptionPeriod):**
    - **Lock:** `function resolveRedemptionPeriod(uint16 roll, uint32 flipDay, uint32 dayToResolve) external` — explicit `dayToResolve` arg supplied by the `AdvanceModule` caller. Value passed: `dayToResolve = game.currentDayView() - 1` (i.e. the just-completed day whose pool is now being resolved post-VRF). Asserting the equivalent compact form: at the moment the advance fires for wall-day D's RNG word, day D-1's pool is what gets resolved.
    - **Lock secondary:** `hasPendingRedemptions(uint32 day)` query takes a day arg and checks `pendingByDay[day].ethBase != 0 || pendingByDay[day].burnieBase != 0`. AdvanceModule's three gating sites pass the same `dayToResolve` value to both `hasPendingRedemptions(dayToResolve)` and `resolveRedemptionPeriod(roll, flipDay, dayToResolve)`.
    - **Impact on call sites:** 3 AdvanceModule call sites (`:1230`, `:1293`, `:1323`) update to pass `dayToResolve = currentDayView() - 1` (verify the exact local-variable in scope at each site at Plan 05). Plan 05 must list all three sites in the citation manifest, not just `:1230`.
    - **Resolves §1:** INV-08 (pre-advance-gap burn safety) — confirms that the advance reads `pendingByDay[D-1]` only and does not touch `pendingByDay[D]`.

    **SPEC-04 (a-d) — the 4 sub-decisions REQUIREMENTS.md flagged "to lock at SPEC phase":**

    SPEC-04 contains 4 lettered sub-locks. Each sub-lock states the decision + brief rationale.

    - **(a) gameOver-mid-pending semantics:** LOCK: `pendingByDay[D]` SURVIVES gameOver. If pre-gameOver pending exists at day D and gameOver latches before day-D+1's advance fires, the eventual day-D+1 advance still calls `resolveRedemptionPeriod(roll, flipDay, dayToResolve = D)` (the advance loop continues normally since the redemption roll is a sub-component of the advance VRF derivation, not a separate gate). Players claim via `claimRedemption(D)` reading the stored `redemptionPeriods[D].roll` post-resolve. If gameOver fires AFTER resolve but BEFORE claim, claim proceeds normally with stored roll — the existing `:638-643` 50/50 split degenerates to 100% direct in the `isGameOver` branch which is the desired behavior (no lootboxes post-gameOver). Rationale: pre-launch posture per `feedback_frozen_contracts_no_future_proofing.md`; minimum-surface-area approach; no special gameOver branch in `resolveRedemptionPeriod`. This resolves INV-12.

    - **(b) zero-rounded ethValueOwed handling:** LOCK: existing `amount == 0` revert in `_submitGamblingClaimFrom` (`:754`) PRESERVED — burns of zero sDGNRS revert. If `ethValueOwed` rounds to zero from a tiny but non-zero `amount` (1 wei sDGNRS at very low supply), the burn PROCEEDS with `ethValueOwed = 0` written to `pendingRedemptions[player][day]`. Rationale: integer-division rounding is bounded; a zero-claim has no payout and storage-refunds on first claim; matches existing economic behavior. EDGE-13 (Plan 03) tests this case explicitly.

    - **(c) pendingByDay[D] storage-refund timing:** LOCK: `delete pendingByDay[D]` fires INSIDE `resolveRedemptionPeriod` AFTER `redemptionPeriods[D]` is written and emit `RedemptionResolved` fires. Storage-refund flows to the AdvanceModule caller (i.e. the EOA calling `advanceGame`). Rationale: resolves the pool to the player-keyed per-claim storage at one transition boundary; minimizes long-lived day-keyed storage; matches the existing semantic that the pool ceases to need cross-day reference once resolved.

    - **(d) pendingRedemptions[player][day] storage-refund at claim:** LOCK: `delete pendingRedemptions[msg.sender][day]` fires INSIDE `claimRedemption(day)` AFTER both `_payEth` and `_payBurnie` (or their lootbox-eth equivalent) have executed and the `RedemptionClaimed` event has emitted. Note: the pre-refactor `:660-665` partial-claim branch (where `flipResolved == false` retains `burnieOwed` for a later claim) — under SPEC-04 (d) this partial-claim path is RETAINED — the `delete` fires only when `flipResolved == true` OR when the player calls `claimRedemption(day)` a second time after `flipResolved` becomes true. The interaction with the coinflip-result oracle is preserved verbatim from `:651-654`.

    **SPEC-05 (50% supply cap snapshot timing):**
    - **Lock:** `pendingByDay[D].supplySnapshot` lazy-initialized on the first burn of day D — at the moment when `pendingByDay[D].supplySnapshot == 0` AND `pendingByDay[D].burned == 0`. Initialized to `totalSupply` AT THAT MOMENT. Immutable for the rest of day D — subsequent same-day burns enforce the cap against the snapshot, not against the current `totalSupply` (which is decreasing as burns proceed). The cap check at `:763` becomes `if (pendingByDay[currentDay].burned + amount > pendingByDay[currentDay].supplySnapshot / 2) revert Insufficient();`. Rationale: prevents the cap from automatically tightening as a same-day series of burns reduces total supply (which would create unintended consequences for legitimate same-day burners following an early burner); the existing pre-refactor behavior at `:759` already lazy-inits on a per-period basis — SPEC-05 preserves that semantic verbatim under per-day keying.
    - **Resolves §1:** INV-10 (per-day supply cap).

    Cross-cutting note section at end of §2: list the 7 deletions Plan 04 will design-intent-trace:
    1. `redemptionPeriodIndex` storage slot (`:230`) — subsumed by per-day keying (SPEC-01)
    2. `redemptionPeriodSupplySnapshot` storage slot (`:229`) — subsumed by `pendingByDay[D].supplySnapshot` (SPEC-01 + SPEC-05)
    3. `redemptionPeriodBurned` storage slot (`:231`) — subsumed by `pendingByDay[D].burned` (SPEC-01)
    4. `pendingRedemptionEthBase` storage slot (`:226`) — subsumed by `pendingByDay[D].ethBase` (SPEC-01)
    5. `pendingRedemptionBurnieBase` storage slot (`:227`) — subsumed by `pendingByDay[D].burnieBase` (SPEC-01)
    6. `UnresolvedClaim` revert (`:796-797`) — removed by composite keying (SPEC-02)
    7. `redemptionPeriodIndex` reset block (`:757-762` — VERIFY: the prompt cited `:758-762` but the actual reset opens at `:757` with `if (redemptionPeriodIndex != currentPeriod) {`. Plan 05 will grep-verify the canonical range; this section uses `:757-762` and Plan 05 either confirms or corrects.) — deleted by composite keying + lazy-init snapshot (SPEC-01 + SPEC-02 + SPEC-05)

    Length target: 150-300 lines for §2 total.
  </action>
  <verify>
    <automated>for n in 01 02 03 04 05; do grep -q "^### SPEC-$n:" .planning/phases/304-spec-invariant-model-spec/304-SPEC.md || { echo "MISSING SPEC-$n"; exit 1; }; done && echo "ALL_5_SPEC_PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^### §2.0 — Priority Statement" 304-SPEC.md` returns exactly 1 (the Priority Statement heading is present)
    - §2.0 Priority Statement appears BEFORE the first SPEC-NN subsection (verify: `awk '/^### §2.0/{p=NR} /^### SPEC-01:/{s=NR} END{exit !(p>0 && s>p)}' 304-SPEC.md` exits 0)
    - §2.0 contains three numbered clauses with the literal labels: `1. **Hard floor`, `2. **Soft target`, `3. **Conflict resolution`
    - §2.0 names the load-bearing invariants by ID: INV-01, INV-06, INV-07 (verify: `grep -cE "INV-0[167]" 304-SPEC.md` returns ≥ 3 across §2.0)
    - §2.0 names the V-184 mechanic explicitly (verify: `grep -c "V-184" 304-SPEC.md` returns ≥ 1)
    - `grep -c "^### SPEC-" 304-SPEC.md` returns exactly 5
    - Every SPEC-NN subsection contains all five labeled sub-fields: `**Lock:**`, `**Rationale:**`, `**Impact on storage layout:**`, `**Impact on call sites:**`, `**Resolves §1 forward-reference:**` (verify: `grep -c "\\*\\*Lock:\\*\\*" 304-SPEC.md` returns 5)
    - SPEC-01 contains the literal struct declaration text `struct DayPending` with all four fields named (`ethBase`, `burnieBase`, `supplySnapshot`, `burned`)
    - SPEC-02 explicitly states the `UnresolvedClaim` revert at `:796-797` is REMOVED
    - SPEC-03 names all THREE AdvanceModule call sites (`:1230`, `:1293`, `:1323`) — verify by `grep -c ":1230\\|:1293\\|:1323" 304-SPEC.md` returns ≥ 3 (Plan 05 will grep-verify the actual line numbers; these are the prompt-given baseline)
    - SPEC-04 contains 4 lettered sub-locks `(a)`, `(b)`, `(c)`, `(d)` — verify by `grep -c "^- \\*\\*(a)\\|^- \\*\\*(b)\\|^- \\*\\*(c)\\|^- \\*\\*(d)" 304-SPEC.md` returns ≥ 4
    - SPEC-05 explicitly states `lazy-init` and `pendingByDay[D].supplySnapshot == 0` initialization condition
    - The 7-deletion cross-cutting note appears at end of §2 with all 7 items enumerated (verify by `grep -c "^[0-9]\\. .*\\(slot\\|revert\\|reset block\\)" 304-SPEC.md` returns ≥ 7)
    - The placeholder `_To be filled by Plan 02_` under §2 is removed
    - §1 forward-references (e.g. `per SPEC-04 (a)`) now resolve to extant text in §2 — no dangling references (verify by scanning the §1..§2 region for `per SPEC-` occurrences and confirming each has a target subsection)
  </acceptance_criteria>
  <done>304-SPEC.md §2 contains 5 fully-locked SPEC-NN subsections with rationale + impact + forward-reference resolution. The 7-deletion enumeration prepares Plan 04's design-intent walk. §3/§4/§5 placeholders untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| §2 SPEC locks → Phase 305 IMPL diff | The 5 locks here become the IMPL diff's required-features list; mis-locked decisions produce mis-implemented contracts |
| §2 SPEC-04 sub-locks → SPEC-04 was flagged "to lock at SPEC phase" in REQUIREMENTS.md | This plan owns the final lock — Phase 305 has no further authority to revisit these |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-304-02-01 | Tampering | SPEC-04 (a) gameOver semantics ambiguity → IMPL writes inconsistent gameOver branch | mitigate | SPEC-04 (a) explicitly states: pendingByDay survives gameOver; advance loop continues to fire resolve normally; the `isGameOver` branch at the EXISTING `:638-643` provides the 100% direct payout. No new gameOver gate added |
| T-304-02-02 | Tampering | SPEC-03 dayToResolve value unspecified → AdvanceModule call sites diverge | mitigate | SPEC-03 explicitly states `dayToResolve = game.currentDayView() - 1` and names all 3 call sites must pass identical value. Plan 05 grep-verifies all 3 sites |
| T-304-02-03 | Repudiation | §2 prose silently narrates "what changed" → violates feedback_no_history_in_comments.md | mitigate | Task action explicitly cites the rule; rationale text is forward-looking (cites the V-184 mechanic as the structural reason for the post-refactor design) — NOT "we used to do X" |
| T-304-SPEC | Threat-enumeration locus | full attack-surface enumeration | transfer to §3 EDGE-01..18 (Plan 03) + §4 design-intent walk (Plan 04) | §2 locks the POSITIVE design; §3 + §4 carry the canonical threat enumeration. SPEC-04 (a)/(b) decisions resolve specific EDGE cases (EDGE-08 gameOver mid-pending, EDGE-13 zero-rounded burn) that Plan 03 mechanizes |
</threat_model>

<verification>
- §2.0 Priority Statement is the first subsection under §2 with 3 numbered clauses (hard floor / soft target / conflict resolution); names INV-01/06/07 + V-184 explicitly
- `grep -c "^### SPEC-" 304-SPEC.md` returns exactly 5
- §1 forward-references to SPEC-NN now have resolvable targets
- 7-deletion enumeration present at end of §2 (Plan 04 consumes this)
- No "what changed" / "what it used to be" prose in §2 (pre-refactor narration reserved for §4)
</verification>

<success_criteria>
- §2.0 Priority Statement (security-first; gas-efficient within) appears first under §2; codifies the load-bearing security floor for Phase 305 IMPL trade-off decisions
- §2 of 304-SPEC.md fully populated with SPEC-01..05 subsections
- Each SPEC-NN has Lock + Rationale + Impact on storage + Impact on call sites + Resolves §1 forward-reference
- SPEC-04 sub-locks (a-d) explicitly resolve REQUIREMENTS.md "to lock at SPEC phase" obligations
- §1 forward-references resolve cleanly; no dangling `per SPEC-` references
- 7-deletion enumeration ready for Plan 04 design-intent walk
</success_criteria>

<output>
Create `.planning/phases/304-spec-invariant-model-spec/304-02-SUMMARY.md` when done. SUMMARY captures: (1) which §1 forward-references this plan resolved (proves no dangling refs); (2) the 7-deletion enumeration line range (Plan 04 uses this as its work-item list); (3) any SPEC-NN field where the lock revealed an under-specification needing user pause (in that case, the SUMMARY surfaces it; this is a non-blocking advisory).
</output>
