---
phase: 304-spec-invariant-model-spec
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/304-spec-invariant-model-spec/304-SPEC.md
requirements:
  - INV-01
  - INV-02
  - INV-03
  - INV-04
  - INV-05
  - INV-06
  - INV-07
  - INV-08
  - INV-09
  - INV-10
  - INV-11
  - INV-12
autonomous: true

must_haves:
  truths:
    - "304-SPEC.md exists with locked §0 header (milestone, baseline, posture) + §1 INV-01..12 each stated as formal property + storage-variable set + state-transition enumeration"
    - "Every INV-NN entry names the exact storage variables it constrains (drawn from the post-refactor layout: pendingByDay[uint32].{ethBase,burnieBase,supplySnapshot,burned}, pendingRedemptions[player][day].{ethValueOwed,burnieOwed,activityScore}, redemptionPeriods[day].{roll,flipDay}, pendingRedemptionEthValue, pendingRedemptionBurnie)"
    - "Every INV-NN entry names the state transitions across which the property must hold (the action set: burn, advanceGame/resolveRedemptionPeriod, claimRedemption, gameOver-latch, transfer, approve, admin-action)"
    - "Skeleton §1..§5 table of contents present (later plans fill §2..§5)"
  artifacts:
    - path: ".planning/phases/304-spec-invariant-model-spec/304-SPEC.md"
      provides: "§0 header + §1 INV-01..12 formalization + skeleton §2..§5 headings"
      contains: "INV-01:, INV-02:, INV-03:, INV-04:, INV-05:, INV-06:, INV-07:, INV-08:, INV-09:, INV-10:, INV-11:, INV-12:"
      min_lines: 200
  key_links:
    - from: "304-SPEC.md §1 INV-NN entries"
      to: ".planning/REQUIREMENTS.md INV-01..12 canonical wording"
      via: "verbatim cite + formal-property restatement"
      pattern: "INV-01:.*never mutated|INV-02:.*ETH conservation"
    - from: "304-SPEC.md §1 storage-variable enumeration"
      to: "contracts/StakedDegenerusStonk.sol post-refactor storage layout (SPEC-01 struct shape)"
      via: "named slot citation"
      pattern: "pendingByDay\\[.*\\]\\.(ethBase|burnieBase|supplySnapshot|burned)"
---

<objective>
Create `304-SPEC.md` with the locked §0 header + the full §1 Invariant Model formalization for INV-01..12, plus the skeleton §2..§5 headings that subsequent plans will fill.

Purpose: §1 is the formal-accounting backbone of the SPEC. Every later section (§2 design locks, §3 EDGE assertions, §4 design-intent walk, §5 citation manifest) references the INV-NN properties. Phase 306 TST mechanically converts each INV-NN into a Foundry `invariant_*` function in `test/invariant/RedemptionAccounting.t.sol`; if the INV formalization is vague, the test is vague.

Output: `.planning/phases/304-spec-invariant-model-spec/304-SPEC.md` with §0 header + §1 INV-01..12 each stated as (formal property | storage variables | state transitions) + skeleton headings for §2/§3/§4/§5 that later plans expand.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@/home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/MEMORY.md
@/home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md
@/home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_frozen_contracts_no_future_proofing.md
@contracts/StakedDegenerusStonk.sol
</context>

<interfaces>
<!-- Post-refactor storage layout (SPEC-01) that INV-NN entries reference. Locked authoritatively in Plan 02. -->
<!-- pre-refactor layout at HEAD (read-only baseline) -->
<!-- contracts/StakedDegenerusStonk.sol :209-214 PendingRedemption struct (uint96 ethValueOwed; uint96 burnieOwed; uint32 periodIndex; uint16 activityScore) -->
<!-- :216-219 RedemptionPeriod struct (uint16 roll; uint32 flipDay) -->
<!-- :221 mapping(address => PendingRedemption) public pendingRedemptions -->
<!-- :222 mapping(uint32 => RedemptionPeriod) public redemptionPeriods -->
<!-- :224 uint256 public pendingRedemptionEthValue (UNCHANGED post-refactor — cumulative across days) -->
<!-- :225 uint256 internal pendingRedemptionBurnie (UNCHANGED post-refactor — cumulative across days) -->
<!-- :226 uint256 internal pendingRedemptionEthBase (REMOVED post-refactor; subsumed by pendingByDay[D].ethBase) -->
<!-- :227 uint256 internal pendingRedemptionBurnieBase (REMOVED post-refactor; subsumed by pendingByDay[D].burnieBase) -->
<!-- :229 uint256 internal redemptionPeriodSupplySnapshot (REMOVED post-refactor; subsumed by pendingByDay[D].supplySnapshot) -->
<!-- :230 uint32 internal redemptionPeriodIndex (REMOVED post-refactor; per-day keying eliminates the single-pool index) -->
<!-- :231 uint256 internal redemptionPeriodBurned (REMOVED post-refactor; subsumed by pendingByDay[D].burned) -->
<!-- :254 uint256 private constant MAX_DAILY_REDEMPTION_EV = 160 ether (UNCHANGED — INV-11 cap value) -->

<!-- POST-REFACTOR (per REQUIREMENTS.md SPEC-01..05) layout that INV-NN entries reference: -->
<!-- struct DayPending { uint256 ethBase; uint256 burnieBase; uint128 supplySnapshot; uint128 burned; } -->
<!-- mapping(uint32 => DayPending) internal pendingByDay -->
<!-- mapping(address => mapping(uint32 => PendingRedemption)) public pendingRedemptions (composite key per SPEC-02) -->
<!-- mapping(uint32 => RedemptionPeriod) public redemptionPeriods (UNCHANGED key) -->
<!-- uint256 public pendingRedemptionEthValue (UNCHANGED — global cumulative scalar) -->
<!-- uint256 internal pendingRedemptionBurnie (UNCHANGED — global cumulative scalar) -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author 304-SPEC.md §0 header + skeleton §1..§5</name>
  <files>.planning/phases/304-spec-invariant-model-spec/304-SPEC.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` (lines 22-90 — the canonical INV-01..12, SPEC-01..05, EDGE-01..18 wording)
    - `.planning/ROADMAP.md` Phase 304 section (lines 41-54)
    - `/home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md` (SPEC describes what IS — never "what changed"; pre-refactor state appears ONLY in §4 design-intent walk under explicit "ORIGINAL DESIGN INTENT" label, not in §1/§2/§3 prose)
    - `/home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_frozen_contracts_no_future_proofing.md` (pre-launch posture — storage layout breaks acceptable; no migration prose in SPEC)
  </read_first>
  <action>
    Create `.planning/phases/304-spec-invariant-model-spec/304-SPEC.md`. Write the document skeleton with the following exact section structure (use Markdown ## / ### headings):

    `# Phase 304 — sStonk Per-Day Redemption Refactor: SPEC + Invariant Model`

    Then `## §0 — Header`:
    - Milestone: `v44.0 sStonk Per-Day Redemption Refactor + Accounting Invariant Proof`
    - Phase: `304 — SPEC + Invariant Model (SPEC)`
    - Baseline: `MILESTONE_V43_AT_HEAD_8111cfc5189f628b64b500c881f9995c3edf0ed2`
    - Load-bearing inputs: `audit/FINDINGS-v43.0.md §9d HANDOFF-111..117` + `.planning/RNGLOCK-FIXREC.md §103` (V-184 mechanic) + `.planning/REQUIREMENTS.md` v44.0 block
    - Downstream consumer: Phase 305 IMPL (single batched USER-APPROVED `contracts/StakedDegenerusStonk.sol` + `contracts/modules/DegenerusGameAdvanceModule.sol` diff) — every locked decision here is a load-bearing input for that diff
    - Posture: pre-launch, frozen-at-deploy per `feedback_frozen_contracts_no_future_proofing.md`. Storage layout breaks ACCEPTED; no migration prose in this SPEC. No future-extensibility speculation.
    - Comment policy: per `feedback_no_history_in_comments.md`, §1/§2/§3/§5 prose describes the POST-REFACTOR state — what IS. Pre-refactor narrative appears ONLY in §4 design-intent walk under explicit `ORIGINAL DESIGN INTENT` subheadings.

    Then create top-level headings (one line each, body empty — later plans fill):
    - `## §1 — Invariant Model (INV-01..12)` (this plan fills it in Task 2)
    - `## §2 — Locked Design Decisions (SPEC-01..05)` (Plan 02 fills)
    - `## §3 — Edge Scenario Enumeration (EDGE-01..18)` (Plan 03 fills)
    - `## §4 — Design-Intent Backward-Trace + Actor Game-Theory Walk` (Plan 04 fills — covers the 7 deletions: 5 storage slots + UnresolvedClaim revert + redemptionPeriodIndex reset block)
    - `## §5 — Source-Verified Citation Manifest` (Plan 05 fills — grep-verifies every file:line cited above against `contracts/StakedDegenerusStonk.sol` + `contracts/modules/DegenerusGameAdvanceModule.sol` HEAD)

    Add a one-line `_To be filled by Plan 0N — see PLAN.md_` placeholder under each empty heading so an inadvertent early read does not mistake the section as finished.

    Under §0 also include the requirement-traceability table: rows for INV-01..12 → §1, SPEC-01..05 → §2, EDGE-01..18 → §3. This is the at-a-glance map a Phase 306 test author uses to locate the spec text for any requirement ID.

    Do NOT inline contract source code. Do NOT speculate about Phase 305 IMPL choices not yet locked at §2.
  </action>
  <verify>
    <automated>test -f .planning/phases/304-spec-invariant-model-spec/304-SPEC.md && grep -c '^## §[0-5]' .planning/phases/304-spec-invariant-model-spec/304-SPEC.md | grep -q '^6$'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/304-spec-invariant-model-spec/304-SPEC.md` exists
    - File contains exactly 6 second-level headings matching `^## §[0-5]` (§0 header + §1 + §2 + §3 + §4 + §5)
    - `grep -c "MILESTONE_V43_AT_HEAD_8111cfc5189f628b64b500c881f9995c3edf0ed2" 304-SPEC.md` returns ≥ 1 (baseline cited)
    - `grep -c "feedback_no_history_in_comments.md" 304-SPEC.md` returns ≥ 1 (comment policy attested)
    - `grep -c "feedback_frozen_contracts_no_future_proofing.md" 304-SPEC.md` returns ≥ 1 (posture attested)
    - §0 requirement-traceability table lists all 12 INV + 5 SPEC + 18 EDGE rows (verify by `grep -c "^| INV-0[1-9]\\|^| INV-1[0-2]\\|^| SPEC-0[1-5]\\|^| EDGE-0[1-9]\\|^| EDGE-1[0-8]" 304-SPEC.md` returns ≥ 35)
    - §1..§5 each show a `_To be filled by Plan 0N_` placeholder line (so subsequent plans can detect unfilled sections)
  </acceptance_criteria>
  <done>304-SPEC.md skeleton written with §0 header + 5 section headings + traceability table; placeholders mark §2/§3/§4/§5 as later-plan responsibility.</done>
</task>

<task type="auto">
  <name>Task 2: Fill §1 — INV-01..12 formal invariant model</name>
  <files>.planning/phases/304-spec-invariant-model-spec/304-SPEC.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 22-37 (the canonical INV-01..12 wording — restate formally; do NOT paraphrase loosely)
    - `.planning/ROADMAP.md` Phase 304 success criterion #1 (lines 47-48 — "each stated as a precise formal property + storage-variable + state-transition enumeration")
    - `.planning/ROADMAP.md` Phase 305 IMPL goal (lines 56-60 — describes the post-refactor storage shape that INV statements reference)
    - `contracts/StakedDegenerusStonk.sol` (full read — for storage-variable enumeration grounded in real slot names; note the pre-refactor names at `:209-231` + the constants at `:254`)
    - `/home/zak/.claude/projects/-home-zak-Dev-PurgeGame-degenerus-audit/memory/feedback_no_history_in_comments.md` (no narration of "what changed" in §1 — describe POST-REFACTOR state only)
  </read_first>
  <action>
    Under `## §1 — Invariant Model (INV-01..12)`, remove the placeholder and write 12 subsections, one per invariant. Each subsection uses this exact structure:

    `### INV-NN: <short name>`

    `**Formal property:** <restate the REQUIREMENTS.md wording as a precise mathematical / logical assertion, naming the storage variables involved and any inequality, equality, or modulo-dust qualifier>`

    `**Storage variables involved:** <list the exact post-refactor slot names this invariant constrains — drawn from the SPEC-01 layout: pendingByDay[uint32].{ethBase,burnieBase,supplySnapshot,burned} ; pendingRedemptions[player][day].{ethValueOwed,burnieOwed,activityScore} ; redemptionPeriods[day].{roll,flipDay} ; pendingRedemptionEthValue ; pendingRedemptionBurnie>`

    `**State transitions across which the property must hold:** <enumerate the actions: burn (`burn`/`burnWrapped` → `_submitGamblingClaimFrom`) | advanceGame → `resolveRedemptionPeriod(roll, flipDay, dayToResolve)` | claimRedemption(day) | gameOver-latch transition | transfer | approve | admin-action. Specifically name which actions the invariant must hold ACROSS — e.g. INV-01 must hold across every transition once `redemptionPeriods[D].roll != 0` is reached>`

    `**Test mapping:** <name the canonical Foundry test that will prove this — most map to `invariant_INV_NN()` in `test/invariant/RedemptionAccounting.t.sol`; some (e.g. INV-07) may also have a dedicated fuzz reproduction in `test/fuzz/StakedStonkRedemption.t.sol`>`

    Specific content guidance per invariant:
    - **INV-01** (write-once roll): name `redemptionPeriods[D].roll`; state transitions = every reachable state once first write occurs at `resolveRedemptionPeriod`; observability hook = storage-write trace + invariant assertion
    - **INV-02** (ETH conservation, dust-bounded): give the exact equation `address(this).balance + steth.balanceOf(address(this)) + claimableWinnings(address(this)) == pendingRedemptionEthValue + (ETH paid out via claimRedemption() + non-gambling burns)`; modulo dust from `(ethBase * roll) / 100` integer division (per-period dust bounded by 99 wei — derive from the floor-division remainder of the formula at the existing `:592` analog)
    - **INV-03** (BURNIE conservation): give the exact equation; note reservation release at resolve (not claim) per existing semantics
    - **INV-04** (per-day base correctness, unresolved-day pre-condition): `pendingByDay[D].ethBase == sum over all (player, D) of pendingRedemptions[player][D].ethValueOwed` when `redemptionPeriods[D].roll == 0`
    - **INV-05** (per-day cumulative correctness, mixed resolved+unresolved): give the cumulative-sum formula spanning both pools
    - **INV-06** (no cross-player roll manipulation): assert `redemptionPeriods[D].roll` is a deterministic function of day-D+1's VRF word only; no non-EXEMPT actor mutates it
    - **INV-07** (no self-roll manipulation via timing): for any (P, D) where `pendingRedemptions[P][D].ethValueOwed = X` at time T, X is byte-identical at every later time until P calls `claimRedemption(D)`
    - **INV-08** (pre-advance-gap burn safety): if `currentDayView() == D` AND day-D's advance has not yet fired, burn lands in `pendingByDay[D]` — NOT `pendingByDay[D-1]`; day-D's advance reads `pendingByDay[dayToResolve = D-1]` only
    - **INV-09** (skipped-advance recovery, oldest-first ordering): if advances for D+1..D+k skipped, eventual advance fires resolve next-oldest unresolved day; no overwrite occurs
    - **INV-10** (per-day supply cap): total burned in day D never exceeds `pendingByDay[D].supplySnapshot / 2`; snapshot taken on first burn of day D, immutable for rest of day D
    - **INV-11** (per-(player, day) EV cap): `pendingRedemptions[P][D].ethValueOwed <= MAX_DAILY_REDEMPTION_EV (160 ether)`; cap resets per new day
    - **INV-12** (gameOver mid-pending safety): if `gameOver` latches while `pendingByDay[D].ethBase != 0`, the eventual resolve+claim flow either completes correctly OR reverts cleanly (no partial state, no stuck funds, no double-payment); name which behavior is locked here (cross-reference: SPEC-04 (a) locks the gameOver semantics — Plan 02 fills the final answer; this section can forward-reference SPEC-04 (a) as the binding decision)

    Forward-references to §2 SPEC locks (e.g. "per SPEC-04 (a)") are ALLOWED here; §2 will be filled by Plan 02 and the forward references resolve at that time. Do NOT inline the SPEC-04 answer — let Plan 02 lock it.

    Length target: 200-400 lines for §1 total. Each INV-NN subsection ~15-30 lines.
  </action>
  <verify>
    <automated>for n in 01 02 03 04 05 06 07 08 09 10 11 12; do grep -q "^### INV-$n:" .planning/phases/304-spec-invariant-model-spec/304-SPEC.md || { echo "MISSING INV-$n"; exit 1; }; done && echo "ALL_12_INV_PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^### INV-" 304-SPEC.md` returns exactly 12
    - Every INV-NN subsection contains all four labeled sub-fields: `**Formal property:**`, `**Storage variables involved:**`, `**State transitions across which the property must hold:**`, `**Test mapping:**` (verify: `grep -c "\\*\\*Formal property:\\*\\*" 304-SPEC.md` returns 12; same for the other three labels)
    - INV-02 contains the explicit ETH conservation equation with `address(this).balance` + `steth.balanceOf` + `claimableWinnings` terms; verify by `grep -c "address(this).balance" 304-SPEC.md` returns ≥ 1
    - INV-10 cites `pendingByDay[D].supplySnapshot / 2`; verify by grep
    - INV-11 cites `MAX_DAILY_REDEMPTION_EV` and the 160 ETH value; verify by grep
    - The placeholder line `_To be filled by Plan 01_` under §1 has been removed
    - No prose in §1 describes "what changed" or "what it used to be" (verify by `grep -c "previously\\|formerly\\|used to be\\|changed from" 304-SPEC.md §1-range` returns 0 — pre-refactor narration is reserved for §4)
  </acceptance_criteria>
  <done>304-SPEC.md §1 contains 12 fully-formalized invariant subsections, each with formal property + storage variables + state transitions + test mapping. Skeleton §2..§5 untouched (Plans 02-05 own them).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner → SPEC document | The SPEC text is the load-bearing input for Phase 305 IMPL; mis-stated invariants produce mis-implemented contracts |
| SPEC document → Phase 306 invariant test author | INV-NN wording must be mechanizable into `invariant_*` Foundry functions without interpretation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-304-01-01 | Tampering | INV-NN wording drift vs REQUIREMENTS.md canonical text | mitigate | Task 2 explicitly restates REQUIREMENTS.md wording verbatim before formalizing; §0 traceability table cross-references each INV-NN ↔ §1 subsection |
| T-304-01-02 | Information disclosure | Forward-reference to SPEC-04 (gameOver semantics) before Plan 02 locks it | accept | Forward-reference convention `per SPEC-04 (a)` is documented in Task 2 action; Plan 02 resolves at that time. Acceptable risk — no contract behavior depends on this SPEC pre-Plan-02 |
| T-304-SPEC | Threat-enumeration locus | full attack-surface enumeration for the refactor | transfer to §3 EDGE-01..18 + §4 design-intent walk | §3 (Plan 03) + §4 (Plan 04) carry the canonical threat enumeration; §1 invariants are the POSITIVE formal properties those threats would violate. Plan 04 design-intent walk + Plan 03 EDGE-07 V-184 reproduction are the load-bearing threat artifacts for v44.0 |
</threat_model>

<verification>
- `forge build` is NOT applicable (no contract changes in this plan)
- `grep -c "^### INV-" 304-SPEC.md` returns exactly 12
- `grep -c "^## §[0-5]" 304-SPEC.md` returns exactly 6
- Phase-level requirement traceability: every INV-01..12 ID appears in §0 table AND in a §1 subsection
- No Phase 305 IMPL contract changes touched
</verification>

<success_criteria>
- 304-SPEC.md exists with locked §0 header + §1 INV-01..12 each stated as (formal property + storage variables + state transitions + test mapping)
- Skeleton §2..§5 headings + placeholders present (Plans 02-05 own them)
- §0 requirement-traceability table maps INV-01..12 → §1, SPEC-01..05 → §2, EDGE-01..18 → §3
- Zero "what changed" / "what it used to be" prose in §1 (pre-refactor narration reserved for §4)
- File compiles to coherent Markdown (no orphan headings, no unclosed code fences)
</success_criteria>

<output>
Create `.planning/phases/304-spec-invariant-model-spec/304-01-SUMMARY.md` when done. SUMMARY captures: (1) which INV-NN entries forward-reference §2 SPEC locks (Plan 02 must resolve these); (2) any §1 prose that needed Phase 305 IMPL clarification (flag for SPEC-04 lock at Plan 02); (3) the exact line range §1 occupies in 304-SPEC.md (for Plan 05 citation-manifest sweep).
</output>
