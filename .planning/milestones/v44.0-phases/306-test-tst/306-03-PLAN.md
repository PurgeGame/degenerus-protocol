---
phase: 306-test-tst
plan: 03
type: execute
wave: 1
subsystem: Per-function fuzz suite — StakedStonkRedemption
tags: [TST, fuzz, per-function, sStonk, burn, resolve, claim, supply-cap, ev-cap, v44.0]
depends_on: []
requirements: [TST-01]
files_modified:
  - test/fuzz/StakedStonkRedemption.t.sol
autonomous: true
user_setup: []

must_haves:
  truths:
    - "`forge build` exits 0 against the new per-function fuzz file"
    - "`FOUNDRY_PROFILE=deep forge test --match-path 'test/fuzz/StakedStonkRedemption.t.sol'` PASSES at 10k runs per case"
    - "6 testFuzz_* functions per ROADMAP §306 Success Criterion 1 present: testFuzz_BurnLandsInCurrentDayPool, testFuzz_ResolveWritesCorrectDay, testFuzz_ClaimReadsCorrectDay, testFuzz_MultipleSameDayBurnsAggregate, testFuzz_SupplyCapEnforced, testFuzz_EvCapEnforced"
    - "Additional ACL-rejection coverage: testFuzz_ResolveRevertsForNonGame asserts only `address(game)` can call resolveRedemptionPeriod"
    - "Additional sentinel-coverage: testFuzz_BurnSetsSentinelOnFirstBurnOfDay asserts pendingResolveDay() == currentDayView() after first burn, == 0 after resolve"
    - "AGENT-COMMITTED test-tree commit per `D-43N-TEST-COMMITS-AUTO-01` lineage; zero `contracts/*.sol` mutations"
  artifacts:
    - path: "test/fuzz/StakedStonkRedemption.t.sol"
      provides: "Per-function fuzz coverage for burn / resolve / claim / supply-cap / EV-cap + ACL + sentinel"
      contains: "testFuzz_BurnLandsInCurrentDayPool"
  key_links:
    - from: "test/fuzz/StakedStonkRedemption.t.sol::testFuzz_BurnLandsInCurrentDayPool"
      to: "pendingByDay[currentDayView()]"
      via: "asserts the burn writes to the day-keyed pool matching game.currentDayView()"
      pattern: "game\\.currentDayView\\(\\)"
    - from: "test/fuzz/StakedStonkRedemption.t.sol::testFuzz_ResolveWritesCorrectDay"
      to: "redemptionPeriods[dayToResolve].roll"
      via: "asserts the resolve writes to the exact dayToResolve passed, not currentDayView() - 1 or any other derived day"
      pattern: "redemptionPeriods\\("
    - from: "test/fuzz/StakedStonkRedemption.t.sol::testFuzz_SupplyCapEnforced"
      to: "Insufficient revert at amountWhole + burned > supplySnapshot / 2"
      via: "vm.expectRevert(StakedDegenerusStonk.Insufficient.selector)"
      pattern: "Insufficient\\.selector"
---

<objective>
Ship the per-function fuzz suite at `test/fuzz/StakedStonkRedemption.t.sol` covering the 6 canonical testFuzz_* functions named verbatim in ROADMAP §306 Success Criterion 1, plus 2 additional ACL + sentinel coverage functions. Each function focuses on a SINGLE function of the post-refactor sStonk surface (`burn` / `resolveRedemptionPeriod` / `claimRedemption` / supply-cap / EV-cap / sentinel) and asserts its post-condition + edge boundary in isolation. Per-function fuzz coverage complements the cross-action invariant coverage of Plan 01 + the scenario coverage of Plan 02.

Purpose: TST-01 closure for v44.0. The 6 named functions are the EXACT canonical name list ROADMAP success criterion 1 requires; Phase 308 §3.C conservation re-proof matrix will cite testFuzz_BurnLandsInCurrentDayPool etc. by name. Plan 03 has no contract dependencies and runs in Wave 1 fully parallel with Plans 01/02/04/05 — modifies only test/fuzz/StakedStonkRedemption.t.sol (a new file).

Output: One new file `test/fuzz/StakedStonkRedemption.t.sol` (~600-800 lines: setUp + 8 testFuzz_* functions). AGENT-COMMITTED test-tree commit envelope per `D-43N-TEST-COMMITS-AUTO-01`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/agent-contracts.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/304-spec-invariant-model-spec/304-SPEC.md
@.planning/phases/305-implementation-impl/305-01-SUMMARY.md
@contracts/StakedDegenerusStonk.sol
@test/fuzz/RedemptionGas.t.sol
@test/fuzz/CoverageGap222.t.sol
@test/fuzz/helpers/DeployProtocol.sol

<interfaces>
Same surface as Plan 02 — see Plan 02 `<interfaces>` block for the full sStonk public + error + event list at v44.0. Key per-function fuzz inputs:

```solidity
// Burn:
function burn(uint256 amount) external returns (uint256 ethOut, uint256 stethOut, uint256 burnieOut);
// Constraints: amount >= MIN_BURN_AMOUNT (1e18); amount <= balanceOf[caller]; sentinel ok; amountWhole + burned <= supplySnapshot / 2; claim + ethValueOwed <= MAX_DAILY_REDEMPTION_EV; !rngLocked; !livenessTriggered; !gameOver.

// Resolve:
function resolveRedemptionPeriod(uint16 roll, uint32 flipDay, uint32 dayToResolve) external; // onlyGame
// Constraints: msg.sender == ContractAddresses.GAME; roll ∈ [25, 175] is the valid range from AdvanceModule but resolveRedemptionPeriod itself does NOT enforce — passes through. Negative test: vm.expectRevert(Unauthorized.selector) for non-game callers.

// Claim:
function claimRedemption(uint32 day) external;
// Constraints: pendingRedemptions[msg.sender][day].ethValueOwed != 0 || burnieOwed != 0; redemptionPeriods[day].roll != 0; otherwise reverts NoClaim or NotResolved.
```
</interfaces>

<patterns>
1. **DeployProtocol scaffold + game-funding** — same as Plans 01/02; precedent at `test/fuzz/RedemptionGas.t.sol` setUp.

2. **Bounded burn** — `amount = bound(amountSeed, MIN_BURN_AMOUNT, playerBal / 2)` (divide by 2 to leave room for multi-burn tests without hitting supply cap).

3. **Direct resolve via game prank** — `vm.prank(address(game)); sdgnrs.resolveRedemptionPeriod(roll, flipDay, day);` precedent at `test/fuzz/RedemptionGas.t.sol:77-78`.

4. **Mocked coinflip resolution** — precedent at `test/fuzz/RedemptionGas.t.sol:97-109`.

5. **Public auto-getter reads** — `(uint96 ethOwed, uint96 burnieOwed, uint16 score) = sdgnrs.pendingRedemptions(player, day);` — composite-key auto-getter returns the struct's 3 fields as tuple.

6. **vm.load for internal pendingByDay** — `keccak256(abi.encode(uint256(day), uint256(SLOT_PENDING_BY_DAY)))` + decode 4×uint64. SLOT_PENDING_BY_DAY comes from `forge inspect` during execution.

7. **vm.expectRevert(selector)** — `vm.expectRevert(StakedDegenerusStonk.Unauthorized.selector);` etc. precedent at `test/fuzz/CoverageGap222.t.sol`.
</patterns>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create StakedStonkRedemption.t.sol with 6 canonical + 2 ACL/sentinel testFuzz_* fns</name>
  <files>test/fuzz/StakedStonkRedemption.t.sol</files>
  <read_first>
    Read `contracts/StakedDegenerusStonk.sol` lines 615-895 (the 4 gambling functions) + `test/fuzz/RedemptionGas.t.sol` (setUp + mock patterns) + `.planning/ROADMAP.md` lines 85-86 (the 6 canonical names verbatim).
  </read_first>
  <behavior>
    Per-function fuzz behavior contracts:

    - testFuzz_BurnLandsInCurrentDayPool(uint256 actorSeed, uint256 amountSeed): warp to day D (vm.warp); seed an actor with sDGNRS; bound amount; capture `D_pre = game.currentDayView()`; capture `pendingByDay[D_pre]_pre` via vm.load; burn; assert `pendingByDay[D_pre].ethBase > 0` post-burn (gwei units > 0 given amount >= MIN_BURN_AMOUNT and totalMoney > 0); assert game.currentDayView() == D_pre still; assert `pendingResolveDay() == D_pre`; assert NO OTHER pendingByDay[D'] for D' != D_pre is touched (scan a small window e.g. D_pre - 3 to D_pre + 3, all byte-identical pre/post except D_pre).

    - testFuzz_ResolveWritesCorrectDay(uint256 dayOffsetSeed, uint16 rollSeed, uint32 flipDaySeed): seed a burn on some day D_burn; warp + advance to a later day; vm.prank(address(game)); sdgnrs.resolveRedemptionPeriod(rollBounded, flipDayBounded, D_burn); assert `(uint16 actualRoll, uint32 actualFlipDay) = sdgnrs.redemptionPeriods(D_burn);` equals (rollBounded, flipDayBounded); assert redemptionPeriods[D_burn + 1] and [D_burn - 1] byte-identical pre/post (untouched); assert `pendingByDay[D_burn]` zeroed (deleted at resolve); assert `pendingResolveDay() == 0` (sentinel cleared).

    - testFuzz_ClaimReadsCorrectDay(uint256 actorSeed, uint256 amountSeed, uint16 rollSeed): burn day D_burn; resolve D_burn with rollBounded; mock coinflip; claimRedemption(D_burn); assert claim.ethValueOwed read came from `pendingRedemptions[player][D_burn]` (not [player][D_burn ± 1]); assert payout amount == `(claim_ethValueOwed_pre * rollBounded) / 100` exactly (D-305-GWEI-SNAP-01 zero drift); assert `pendingRedemptions[player][D_burn]` zeroed (full-claim path); assert `pendingRedemptions[player][D_burn + 1]` and `[player][D_burn - 1]` byte-identical pre/post.

    - testFuzz_MultipleSameDayBurnsAggregate(uint256 actorSeed, uint256 amount1Seed, uint256 amount2Seed, uint256 amount3Seed): same actor burns 3 times on day D with amounts a1, a2, a3 each bounded [MIN_BURN_AMOUNT, balanceOf/4]; after each burn capture `claim.ethValueOwed`; after 3rd burn assert `claim.ethValueOwed == sum_of_individual_per_burn_ethValueOwed_deltas`; capture `pendingByDay[D].ethBase` (gwei) + assert `pendingByDay[D].ethBase * 1e9 == claim.ethValueOwed` (single-burner exact equality); assert `pendingByDay[D].burned * 1e18 >= a1 + a2 + a3` (ceiling-divide preserves cap accounting).

    - testFuzz_SupplyCapEnforced(uint256 firstBurnSeed, uint256 secondBurnSeed): set up day D with snapshot via first burn at `firstBurnAmount = bound(firstBurnSeed, MIN_BURN_AMOUNT, totalSupply / 4)`; capture `snapshot = pendingByDay[D].supplySnapshot * 1e18` post-first-burn; for `secondBurnAmount = bound(secondBurnSeed, snapshot/2 - firstBurnAmount + 1, totalSupply)` — i.e. an amount that would push burned over snapshot/2; vm.expectRevert(Insufficient.selector); vm.prank(player); sdgnrs.burn(secondBurnAmount); also positive: burn exactly `snapshot/2 - firstBurnAmount` (or one wei under) succeeds.

    - testFuzz_EvCapEnforced(uint256 actorSeed, uint256 nBurnsSeed): accumulate burns until `pendingRedemptions[player][D].ethValueOwed` approaches 160 ether; the next burn that would push ethValueOwed > 160 ether reverts ExceedsDailyRedemptionCap. **NOTE:** because ethValueOwed = (totalMoney * amount) / supply then gwei-snapped, hitting the cap exactly requires inverting the formula. Simplified construction: burn amounts proportional to existing claim's headroom; assert the FIRST burn whose computed ethValueOwed (predicted via previewBurn) would exceed the headroom reverts ExceedsDailyRedemptionCap.

    - testFuzz_ResolveRevertsForNonGame(address caller, uint16 roll, uint32 flipDay, uint32 day) [ACL coverage]: vm.assume(caller != address(game) && caller != address(0)); vm.expectRevert(StakedDegenerusStonk.Unauthorized.selector); vm.prank(caller); sdgnrs.resolveRedemptionPeriod(roll, flipDay, day); assert no state change (capture sStonk balance, pendingRedemptionEthValue, redemptionPeriods[day] pre/post).

    - testFuzz_BurnSetsSentinelOnFirstBurnOfDay(uint256 actorSeed, uint256 amountSeed) [INV-13 per-function coverage]: warp to fresh day; assert `pendingResolveDay() == 0` pre-burn (or any value !=  currentDayView()'s D); burn; assert `pendingResolveDay() == game.currentDayView()` post-burn; second burn same actor same day; assert sentinel unchanged (still == currentDayView()); advance + resolve; assert sentinel cleared to 0; final assertion: third burn on a NEW day succeeds and re-stamps sentinel to new day.
  </behavior>
  <action>
    Create new file `test/fuzz/StakedStonkRedemption.t.sol`. Header + setUp per RedemptionGas.t.sol precedent. setUp seeds 4 actors with 10M sDGNRS each + funds game with 100 ether claimable winnings + mocks coinflip default result.

    Implement 8 testFuzz_* functions per the `<behavior>` block above. Each has:
    - `/// forge-config: default.fuzz.runs = 10000` NatSpec preceding the function
    - NatSpec citing the function under test, the relevant 304-SPEC §1 INV-NN or §2 SPEC-NN anchor, and the assertion strategy
    - Bounded fuzz inputs via `bound(seed, lo, hi)`
    - State capture pre/post + `assertEq` strict equality for byte-identity assertions
    - `vm.expectRevert(<Error>.selector)` for negative paths

    File size target: ~600-800 lines.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -10 && FOUNDRY_PROFILE=deep forge test --match-path "test/fuzz/StakedStonkRedemption.t.sol" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `test/fuzz/StakedStonkRedemption.t.sol` exists; `forge build` exits 0
    - The 6 ROADMAP-canonical names present: `grep -cE "function (testFuzz_BurnLandsInCurrentDayPool|testFuzz_ResolveWritesCorrectDay|testFuzz_ClaimReadsCorrectDay|testFuzz_MultipleSameDayBurnsAggregate|testFuzz_SupplyCapEnforced|testFuzz_EvCapEnforced)" test/fuzz/StakedStonkRedemption.t.sol` returns 6
    - The 2 extra fns present: testFuzz_ResolveRevertsForNonGame + testFuzz_BurnSetsSentinelOnFirstBurnOfDay
    - All 8 functions PASS at FOUNDRY_PROFILE=deep (10k runs/case)
    - testFuzz_MultipleSameDayBurnsAggregate uses STRICT assertEq for the gwei-aligned per-day sum (no dust tolerance per D-305-GWEI-SNAP-01)
  </acceptance_criteria>
  <done>
    8 testFuzz_* functions present (6 ROADMAP-canonical + 2 ACL/sentinel), all PASS at 10k runs. Per-function coverage complete for burn / resolve / claim / supply-cap / EV-cap / sentinel.
  </done>
</task>

<task type="auto">
  <name>Task 2: AGENT-COMMITTED test-tree commit envelope + SUMMARY</name>
  <files>.planning/phases/306-test-tst/306-03-SUMMARY.md</files>
  <read_first>
    Read the SUMMARY template + Phase 305 SUMMARY precedent.
  </read_first>
  <action>
    Write `.planning/phases/306-test-tst/306-03-SUMMARY.md` per the SUMMARY template. Frontmatter:
    - phase: 306-test-tst
    - plan: 03
    - subsystem: Per-function fuzz suite — StakedStonkRedemption
    - tags: [TST, fuzz, per-function, sStonk, v44.0]
    - requirements-completed: [TST-01]

    Body documents the 6 canonical fn names + 2 extras + their assertion strategy + cross-link to §3.C / §3.F for Phase 308.

    Commit envelope:
    - `git add -f test/fuzz/StakedStonkRedemption.t.sol .planning/phases/306-test-tst/306-03-SUMMARY.md`
    - `gsd-sdk query commit "test(306-03): per-function fuzz suite — 6 ROADMAP-canonical + 2 ACL/sentinel testFuzz_* PROVEN against v44 sStonk per-day source" --files test/fuzz/StakedStonkRedemption.t.sol .planning/phases/306-test-tst/306-03-SUMMARY.md`

    AGENT-COMMITTED per `D-43N-TEST-COMMITS-AUTO-01`. Zero `contracts/*.sol` mutations.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && git log --oneline -1 -- test/fuzz/StakedStonkRedemption.t.sol 2>&1 | head -5 && git diff --stat HEAD~1..HEAD -- contracts/ 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Single git commit touches test/fuzz/StakedStonkRedemption.t.sol + .planning/phases/306-test-tst/306-03-SUMMARY.md
    - Zero `contracts/*.sol` mutations
    - SUMMARY.md frontmatter lists TST-01 as requirements-completed
  </acceptance_criteria>
  <done>
    AGENT-COMMITTED atomic commit lands the per-function suite + SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Per-function test ↔ sStonk | Each test calls a single sStonk external function (burn / resolve / claim); reads public state for assertions |
| Test ↔ Coinflip mock | vm.mockCall for deterministic claim path |
| Test-tree ↔ contracts/ | Zero mutations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-306-03-01 | Tampering | testFuzz_EvCapEnforced cap-edge arithmetic involves gwei snap + integer division | mitigate | Use `previewBurn` to predict ethValueOwed → invert via headroom; OR seed multiple burns whose computed sum approaches 160 ether and assert the boundary; document approach in NatSpec |
| T-306-03-02 | Spoofing | testFuzz_ResolveRevertsForNonGame caller fuzz may accidentally pick address(game) | mitigate | `vm.assume(caller != address(game) && caller != address(0))` filters; document constraint |
| T-306-03-03 | Information disclosure | Per-day pool reads require correct SLOT_PENDING_BY_DAY | mitigate | Derive via `forge inspect` ONCE in setUp helper; embed the slot index as a contract constant |
| T-306-03-04 | Denial of service | 10k runs × 8 tests = 80k iterations | accept | Within Foundry default capacity; expected ≤ 3min wall-clock |
| T-306-03-SC | Tampering | No package installs | accept | Zero npm/pip/cargo activity |
</threat_model>

<verification>
Phase-level success criteria mapped to this plan:
- ROADMAP Success Criterion 1 (StakedStonkRedemption.t.sol PASSES with 6 canonical testFuzz_* fns) — DELIVERED with 6 + 2 extras = 8 total
- ROADMAP Success Criterion 5 (forge build PASS + FOUNDRY_PROFILE=deep test PASS) — DELIVERED for this subtree

Test invocation:
```
cd /home/zak/Dev/PurgeGame/degenerus-audit
forge build
FOUNDRY_PROFILE=deep forge test --match-path "test/fuzz/StakedStonkRedemption.t.sol" -vv
```

Expected: 8 `[PASS] testFuzz_*` lines + zero failures.
</verification>

<success_criteria>
- `test/fuzz/StakedStonkRedemption.t.sol` created with the 6 ROADMAP-canonical testFuzz_* fns + 2 ACL/sentinel extras (8 total)
- All 8 PASS at FOUNDRY_PROFILE=deep (10k runs/case)
- testFuzz_MultipleSameDayBurnsAggregate uses STRICT assertEq for the per-day-sum equality (D-305-GWEI-SNAP-01)
- 306-03-SUMMARY.md written + AGENT-COMMITTED in single atomic commit
- Zero `contracts/*.sol` mutations
</success_criteria>

<output>
Write `.planning/phases/306-test-tst/306-03-SUMMARY.md` documenting the 6 + 2 testFuzz_* fns + TST-01 closure attestation. Single AGENT-COMMITTED atomic commit envelope.
</output>
