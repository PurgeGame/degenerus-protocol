---
phase: 306-test-tst
plan: 04
type: execute
wave: 1
subsystem: RngLockDeterminism vm.skip → strict-assertion flip (HANDOFF-111 / V-184)
tags: [TST, vmskip-flip, sStonk, V-184, HANDOFF-111, RngLockDeterminism, v44.0, structural-closure]
depends_on: []
requirements: [TST-05, REG-01]
files_modified:
  - test/fuzz/RngLockDeterminism.t.sol
autonomous: true
user_setup: []

must_haves:
  truths:
    - "`forge build` exits 0 against the modified harness"
    - "The 1 vm.skip(true) block at test/fuzz/RngLockDeterminism.t.sol line 1278 (function testFuzz_RngLockDeterminism_StakedStonkRedemption, marked '// SKIP: RNGLOCK-FIXREC.md sec103 -- V-184 sStonk cross-day re-roll CATASTROPHE -- v44.0 D-43N-V44-HANDOFF-111 flips this to strict assertion') is REMOVED"
    - "The flipped test PASSES at FOUNDRY_PROFILE=deep — asserts byte-identical VRF-derived sStonk redemption outputs across mid-rngLock-window state perturbation"
    - "All other vm.skip(true) blocks in the file are PRESERVED VERBATIM — only the sec103 / HANDOFF-111 block is flipped"
    - "REG-01 v43.0 NON-WIDENING attestation: file byte-diff vs v43.0 closure HEAD MILESTONE_V43_AT_HEAD_8111cfc5189f628b64b500c881f9995c3edf0ed2 consists ONLY of (a) the vm.skip(true) line removed + (b) v44 MIN_BURN_AMOUNT bound adjustments + (c) the Phase 305 cascade fix at line 376 (vault.sdgnrsClaimRedemption now 1-arg, already shipped at commit 213f9184)"
    - "AGENT-COMMITTED test-tree commit per D-43N-TEST-COMMITS-AUTO-01 lineage; zero contracts/*.sol mutations"
  artifacts:
    - path: "test/fuzz/RngLockDeterminism.t.sol"
      provides: "Strict-assertion flip of the V-184 / HANDOFF-111 sec103 block; load-bearing v44.0 closure assertion for FINDINGS-v44.0.md §3.D"
      contains: "testFuzz_RngLockDeterminism_StakedStonkRedemption"
  key_links:
    - from: "test/fuzz/RngLockDeterminism.t.sol::testFuzz_RngLockDeterminism_StakedStonkRedemption"
      to: "_captureStonkRedemptionOutputs"
      via: "captures keccak256(pendingRedemptionEthValue + balance) before + after perturbation; assertEq for byte-identity"
      pattern: "_assertVrfOutputByteIdentity"
---

<objective>
Remove the lone vm.skip(true) block at test/fuzz/RngLockDeterminism.t.sol line 1278 (function testFuzz_RngLockDeterminism_StakedStonkRedemption, marked verbatim "RNGLOCK-FIXREC.md sec103 -- V-184 sStonk cross-day re-roll CATASTROPHE -- v44.0 D-43N-V44-HANDOFF-111 flips this to strict assertion") so the test runs and asserts byte-identical VRF-derived sStonk redemption outputs across mid-rngLock-window state perturbation. The flip is the LOAD-BEARING v44.0 closure assertion for FINDINGS-v44.0.md §3.D V-184 RESOLVED-AT-V44 disposition (per ROADMAP §306 Success Criterion 4 + TST-05 + REG-01 v43.0 NON-WIDENING attestation).

**ROADMAP CLAIM RECONCILIATION (load-bearing):** ROADMAP §306 Success Criterion 4 + the planner prompt claim "HANDOFF-111..117 7 vm.skip(true) blocks REMOVED + strict byte-identity assertion in place; all 7 previously-skipped fuzz cases PASS". Reality per `grep "vm.skip(true)" test/fuzz/RngLockDeterminism.t.sol` against v43.0 closure HEAD: only ONE vm.skip(true) block in the file is anchored to the V-184 / HANDOFF-111 cluster (line 1278; explicitly tagged with "RNGLOCK-FIXREC.md sec103 -- V-184" + "D-43N-V44-HANDOFF-111"). The other 15 vm.skip blocks in the file anchor to UNRELATED HANDOFF-NN entries (HANDOFF-01 jackpot day-roll, HANDOFF-13 prizePoolsPacked, HANDOFF-43 Cluster G lootbox commitment, HANDOFF-77 lastPurchaseDay phantom, HANDOFF-99 decimator-claim, HANDOFF-110 bountyOwedTo, HANDOFF-31 claimablePool gameover) that defer to v45.0+ FIX-MILESTONES — they are NOT in v44.0's narrow sStonk-only scope per ROADMAP "narrow scope" guarantee. The 6 catalog rows V-186/V-188/V-190/V-191/V-192/V-193 subsumed by V-184 per FIXREC §0.6 fan-out do NOT have distinct vm.skip blocks — they collapse into HANDOFF-111 structurally.

**Therefore HANDOFF-111..117 7-row closure happens via:**
1. **1 vm.skip block flipped** (this plan — line 1278, sec103/HANDOFF-111) — the load-bearing strict-assertion flip
2. **6 subsumed catalog rows close STRUCTURALLY via FIXREC §0.6 subsumption + Plan 02 EDGE-07 V-184 reproduction** — no additional vm.skip blocks exist for them; the per-day storage shape alone closes the cluster

Phase 308 FINDINGS-v44.0.md §3.D RESOLVED-AT-V44 will attest the 7-row closure via: TST-05 (this plan's strict-assertion flip — 1 anchor) + TST-04 (Plan 02 EDGE-07 V-184 reproduction — structural closure of the subsumed 6) + 304-SPEC §3 EDGE-07 cross-reference to FIXREC §0.6 subsumption map.

Purpose: TST-05 + REG-01 closure for v44.0. The flip is the cleanest single-line attestation that the V-184 mechanic NO LONGER reaches an exploit at v44.0 close — the v43.0 baseline test failed (which is why it was skipped at v43.0 closure); the v44.0 source forces the test to PASS via the per-day storage keying. Plan 04 has no contract dependencies and runs in Wave 1 fully parallel with Plans 01/02/03/05 — modifies only test/fuzz/RngLockDeterminism.t.sol.

Output: 1 vm.skip(true) line removed + 2 bound/literal adjustments for v44 MIN_BURN_AMOUNT compatibility + natspec rewrite of the SKIP marker to FLIPPED. AGENT-COMMITTED test-tree commit envelope per D-43N-TEST-COMMITS-AUTO-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/agent-contracts.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/304-spec-invariant-model-spec/304-SPEC.md
@.planning/phases/305-implementation-impl/305-01-SUMMARY.md
@contracts/StakedDegenerusStonk.sol
@test/fuzz/RngLockDeterminism.t.sol

<interfaces>
The flipped test body (already present at lines 1272-1334) reads (abbreviated; FULL body unchanged except for the deletions/edits specified in Task 1):

- Line 1272-1276: function declaration `testFuzz_RngLockDeterminism_StakedStonkRedemption(uint256 vrfWord, uint256 perturbSeed, uint256 burnAmountSeed) public`
- Line 1277: natspec marker "// SKIP: RNGLOCK-FIXREC.md sec103 -- V-184 sStonk cross-day re-roll CATASTROPHE -- v44.0 D-43N-V44-HANDOFF-111 flips this to strict assertion"
- Line 1278: `vm.skip(true);` — THE LINE TO REMOVE
- Line 1279: `vm.assume(vrfWord != 0);`
- Line 1281: `_completeDay(0xDEAD0012);`
- Line 1282: `vm.warp(block.timestamp + 1 days);`
- Lines 1284-1293: mint sStonk via `game.purchase(...)` for `holder`
- Line 1295: `uint256 burnAmount = bound(burnAmountSeed, 1, 1_000);` — THE LINE TO REBOUND (1 → 1e18, 1_000 → 100e18)
- Lines 1297-1301: `try sdgnrs.burn(burnAmount) ...`
- Line 1303: `uint256 preLockSnap = _snapshotPreLock();`
- Line 1305: `game.advanceGame();`
- Line 1310: `_perturb(perturbSeed);`
- Line 1314: `try sdgnrs.burn(1) ...` — THE PERTURBATION BURN TO REBOUND (1 → 1e18)
- Line 1319: `_deliverMockVrf(reqId, vrfWord);`
- Line 1321: `bytes32 perturbedOutputs = _captureStonkRedemptionOutputs();`
- Line 1323: `_revertToPreLock(preLockSnap);`
- Line 1324: `game.advanceGame();`
- Lines 1326-1327: re-deliver VRF + capture baselineOutputs
- Lines 1329-1333: `_assertVrfOutputByteIdentity(perturbedOutputs, baselineOutputs, "StakedStonkRedemption: VRF-derived redemption outputs must be byte-identical under mid-window perturbation (RNGLOCK-CATALOG.md sec12 + RNGLOCK-FIXREC.md sec103 -- V-184 CATASTROPHE)");`

The capture helper at lines 1336-1339:

```
function _captureStonkRedemptionOutputs() internal view returns (bytes32) {
    uint256 pre = sdgnrs.pendingRedemptionEthValue();
    return keccak256(abi.encode(pre, address(sdgnrs).balance));
}
```

works against v44 source unchanged — `pendingRedemptionEthValue()` is the v44 public getter for the cumulative-scalar slot.

**v44 compatibility caveat:** the existing `bound(burnAmountSeed, 1, 1_000)` produces sub-MIN_BURN_AMOUNT (1e18) values. At v44, those burns revert `BurnTooSmall` (D-305-DUST-FLOOR-01) — so the `try ... catch { vm.assume(false); }` would filter out ALL fuzz inputs, leaving 0 passing iterations (vacuous success). The executor MUST adjust the bound to `bound(burnAmountSeed, 1e18, 100e18)` for the test to exercise the V-184 attack vector legitimately.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip the line-1278 vm.skip(true) + adjust burn-bound for v44 MIN_BURN_AMOUNT</name>
  <files>test/fuzz/RngLockDeterminism.t.sol</files>
  <read_first>
    Read `test/fuzz/RngLockDeterminism.t.sol` lines 1268-1340 (the full testFuzz_RngLockDeterminism_StakedStonkRedemption function).
  </read_first>
  <action>
    Apply THREE specific edits to `test/fuzz/RngLockDeterminism.t.sol`:

    1. **Remove the vm.skip line.** Delete the line at original position 1278 that reads `        vm.skip(true);`. Rewrite the natspec at line 1277 from `// SKIP: RNGLOCK-FIXREC.md sec103 -- V-184 sStonk cross-day re-roll CATASTROPHE -- v44.0 D-43N-V44-HANDOFF-111 flips this to strict assertion` to `// FLIPPED at v44.0: RNGLOCK-FIXREC.md sec103 -- V-184 sStonk cross-day re-roll CATASTROPHE -- D-43N-V44-HANDOFF-111 strict-assertion attestation; structural closure via per-day storage keying (304-SPEC §3 EDGE-07)`. The natspec describes the CURRENT state (FLIPPED) per `feedback_no_history_in_comments.md` — not narration of "what changed".

    2. **Adjust the initial burn bound for v44 MIN_BURN_AMOUNT compatibility.** At line 1295 which reads `uint256 burnAmount = bound(burnAmountSeed, 1, 1_000);`, change the bound to `uint256 burnAmount = bound(burnAmountSeed, 1e18, 100e18);` so the bounded amount is always >= MIN_BURN_AMOUNT (1e18). Add an inline natspec comment ABOVE the line: `// v44 MIN_BURN_AMOUNT floor: bound legal-burn range to [1e18, 100e18]`.

    3. **Adjust the perturbation re-burn for v44 MIN_BURN_AMOUNT compatibility.** At line 1314 which reads `try sdgnrs.burn(1) returns (uint256, uint256, uint256) {} catch {}`, change `sdgnrs.burn(1)` to `sdgnrs.burn(1e18)` (the minimum legal v44 burn) so the perturbation attempts to mutate state. Add inline comment ABOVE the line: `// v44 MIN_BURN_AMOUNT: minimum legal perturbation burn`.

    DO NOT TOUCH any other vm.skip(true) block in the file. DO NOT TOUCH the natspec comments anchoring those other blocks (lines 403, 474, 546, 630, 734, 823, 950, 1036, 1152, 1211, 1350, 1561, 1597, 1641, 1685, 1736). They remain as v45.0+ deferred coverage anchors per their own HANDOFF-NN IDs (which are out-of-v44.0-scope per ROADMAP narrow-scope guarantee).

    DO NOT TOUCH the Phase-305-applied compile-cascade fix at line 376 (`try vault.sdgnrsClaimRedemption(claimDay) {} catch { return; }` — already updated to 1-arg form by Phase 305 Mutation 27 at commit 213f9184).

    Verify the diff using `git diff test/fuzz/RngLockDeterminism.t.sol` BEFORE committing — the diff MUST consist of EXACTLY:
    - 1 line removed (`vm.skip(true);` at original line 1278)
    - 1 natspec rewrite (line 1277: SKIP → FLIPPED at v44.0)
    - 1 expression change (line 1295: `bound(burnAmountSeed, 1, 1_000)` → `bound(burnAmountSeed, 1e18, 100e18)`)
    - 1 inline natspec addition above line 1295
    - 1 expression change (line 1314: `sdgnrs.burn(1)` → `sdgnrs.burn(1e18)`)
    - 1 inline natspec addition above line 1314

    Total surface: 1 deletion + 4 in-place edits + 2 new natspec lines = MINIMAL DIFF SURFACE per REG-01 NON-WIDENING. Anything beyond this scope must be flagged and reverted before commit.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && forge build 2>&1 | tail -10 && FOUNDRY_PROFILE=deep forge test --match-path "test/fuzz/RngLockDeterminism.t.sol" --match-test "testFuzz_RngLockDeterminism_StakedStonkRedemption" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` exits 0
    - `testFuzz_RngLockDeterminism_StakedStonkRedemption` runs (no longer skipped) and PASSES at FOUNDRY_PROFILE=deep
    - `grep -c "vm.skip(true);" test/fuzz/RngLockDeterminism.t.sol` returns 16 (one fewer than v43.0 baseline's 17; baseline empirically verified at pre-flip HEAD per `grep -c "vm.skip(true);" test/fuzz/RngLockDeterminism.t.sol`)
    - `grep -c "FLIPPED at v44.0: RNGLOCK-FIXREC.md sec103" test/fuzz/RngLockDeterminism.t.sol` returns 1
    - `git diff test/fuzz/RngLockDeterminism.t.sol` shows only the 6 prescribed edits — no incidental whitespace changes, no other vm.skip blocks touched
  </acceptance_criteria>
  <done>
    The V-184 / HANDOFF-111 vm.skip block is flipped to strict assertion; the test PASSES against v44 source; all other vm.skip blocks preserved verbatim for v45.0+ deferral. REG-01 NON-WIDENING surface achieved.
  </done>
</task>

<task type="auto">
  <name>Task 2: AGENT-COMMITTED test-tree commit envelope + SUMMARY</name>
  <files>.planning/phases/306-test-tst/306-04-SUMMARY.md</files>
  <read_first>
    Read the SUMMARY template + Phase 305 SUMMARY precedent for AGENT-COMMITTED format.
  </read_first>
  <action>
    Write `.planning/phases/306-test-tst/306-04-SUMMARY.md` per the SUMMARY template. Frontmatter:
    - phase: 306-test-tst
    - plan: 04
    - subsystem: RngLockDeterminism vm.skip flip (HANDOFF-111 / V-184)
    - tags: [TST, vmskip-flip, V-184, HANDOFF-111, REG-01, v44.0]
    - requirements-completed: [TST-05, REG-01]

    Body must include:
    1. The single-line edit summary + before/after diff snippets
    2. **HANDOFF-111..117 7-row closure attribution table:**
       - HANDOFF-111 (V-184): closed via this plan's flipped strict assertion (TST-05) + Plan 02 EDGE-07 V-184 reproduction (TST-04)
       - HANDOFF-112 (V-186): structurally subsumed by V-184 per FIXREC §0.6; closed via per-day storage keying — no separate vm.skip block existed at v43.0
       - HANDOFF-113 (V-188): same — subsumed by V-184; structural closure
       - HANDOFF-114 (V-190): same
       - HANDOFF-115 (V-191): same
       - HANDOFF-116 (V-192): same
       - HANDOFF-117 (V-193): same
    3. REG-01 NON-WIDENING attestation: `git diff MILESTONE_V43_AT_HEAD_8111cfc5189f628b64b500c881f9995c3edf0ed2..HEAD -- test/fuzz/RngLockDeterminism.t.sol` shows ONLY (a) the line-1278 vm.skip removal + (b) the line-1277 natspec rewrite + (c) the line-1295 bound adjustment + (d) the line-1314 burn-amount adjustment + (e) the 2 new natspec comments + (f) the line-376 sdgnrsClaimRedemption(claimDay) 1-arg cascade fix shipped by Phase 305 commit 213f9184. NO OTHER LINES touched. Plan 04 contributes (a)+(b)+(c)+(d)+(e); Phase 305 contributed (f).
    4. v45.0+ deferred coverage roster: the 15 OTHER vm.skip(true) blocks remaining in the file with their HANDOFF-NN anchors (cite the FIXREC.md sec numbers from each line's natspec).

    Commit envelope:
    - `git add -f test/fuzz/RngLockDeterminism.t.sol .planning/phases/306-test-tst/306-04-SUMMARY.md`
    - Use `gsd-sdk query commit` with message `test(306-04): flip V-184 / HANDOFF-111 vm.skip to strict assertion — TST-05 + REG-01 closure attestation` and `--files test/fuzz/RngLockDeterminism.t.sol .planning/phases/306-test-tst/306-04-SUMMARY.md`

    AGENT-COMMITTED per D-43N-TEST-COMMITS-AUTO-01. Zero `contracts/*.sol` mutations.
  </action>
  <verify>
    <automated>cd /home/zak/Dev/PurgeGame/degenerus-audit && git log --oneline -1 -- test/fuzz/RngLockDeterminism.t.sol 2>&1 | head -5 && git diff --stat HEAD~1..HEAD -- contracts/ 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Single git commit touches test/fuzz/RngLockDeterminism.t.sol + .planning/phases/306-test-tst/306-04-SUMMARY.md
    - Zero `contracts/*.sol` mutations
    - SUMMARY.md includes the 7-row HANDOFF-111..117 closure attribution table
    - SUMMARY.md attests REG-01 NON-WIDENING with explicit diff-scope enumeration
  </acceptance_criteria>
  <done>
    AGENT-COMMITTED atomic commit lands the vm.skip flip + SUMMARY artifact. Phase 308 §3.D RESOLVED-AT-V44 attestation has the explicit 7-row closure attribution table to cite.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RngLockDeterminism test ↔ sStonk | Test calls external burn via prank, observes pendingRedemptionEthValue + balance for byte-identity assertion |
| Test ↔ AdvanceModule | Test triggers VRF cycle via game.advanceGame() + mockVRF.fulfillRandomWords |
| Test-tree ↔ contracts/ | Zero mutations; test asserts against post-refactor source already at HEAD |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-306-04-01 | Spoofing | bound(burnAmountSeed, 1, 1_000) at v44 would catch all fuzz inputs via BurnTooSmall, leaving vacuous-pass | mitigate | Task 1 adjusts bound to [1e18, 100e18] explicitly; verify `forge test --match-test testFuzz_RngLockDeterminism_StakedStonkRedemption -vv` shows non-zero passing iterations |
| T-306-04-02 | Tampering | REG-01 NON-WIDENING violated by incidental whitespace / formatter edits | mitigate | Task 1 explicitly enumerates the 6 edits + acceptance criteria gates on `git diff` review BEFORE commit; executor must review diff manually |
| T-306-04-03 | Repudiation | Flipped test PASSES vacuously (e.g. perturbation never reaches V-184 attack vector) | mitigate | The capture helper reads pendingRedemptionEthValue + balance — if perturbation succeeds in mutating either, perturbedOutputs differs from baselineOutputs and test FAILS. The fuzz seed space at 10k runs explores enough perturbation states to surface any state-dependent gap. The bound adjustment in Task 1 step 3 (sdgnrs.burn(1e18) replacing sdgnrs.burn(1)) ensures the perturbation actually attempts a state mutation. |
| T-306-04-04 | Tampering | HANDOFF-111..117 7-row claim is exaggerated relative to actual file content (1 vm.skip block) | mitigate | SUMMARY.md explicitly documents the 7-row attribution: 1 block flipped (HANDOFF-111) + 6 subsumed (HANDOFF-112..117) close structurally via FIXREC §0.6 + EDGE-07 in Plan 02. Phase 308 §3.D cites the table verbatim — no claim drift. |
| T-306-04-05 | Information disclosure | Other v45.0+ deferred vm.skip blocks remain documented | accept | Per ROADMAP narrow-scope guarantee, v44.0 closes ONLY the sStonk cluster; the other 15 blocks defer to v45.0+ as deliberate scoping decisions, documented in 306-04-SUMMARY.md's "v45.0+ deferred coverage roster" |
| T-306-04-SC | Tampering | No package installs | accept | Plan 04 modifies only `.sol` + `.md` files; zero npm/pip/cargo |
</threat_model>

<verification>
Phase-level success criteria mapped to this plan:
- ROADMAP Success Criterion 4 (HANDOFF-111..117 7 vm.skip blocks → strict assertion + all 7 PASS) — DELIVERED with the load-bearing scope-clarification: 1 vm.skip block flipped + 6 subsumed catalog rows close structurally via FIXREC §0.6 + Plan 02 EDGE-07
- REG-01 v43.0 NON-WIDENING — DELIVERED via minimal-diff surface attested in 306-04-SUMMARY.md

Test invocation:
```
cd /home/zak/Dev/PurgeGame/degenerus-audit
forge build
FOUNDRY_PROFILE=deep forge test --match-path "test/fuzz/RngLockDeterminism.t.sol" --match-test "testFuzz_RngLockDeterminism_StakedStonkRedemption" -vv
```

Expected: `[PASS] testFuzz_RngLockDeterminism_StakedStonkRedemption(...)` with non-zero passing iterations (no vm.skip + bound adjustments mean the test actually executes).
</verification>

<success_criteria>
- 1 vm.skip(true) line removed from test/fuzz/RngLockDeterminism.t.sol at original line 1278 (the V-184 / HANDOFF-111 block)
- 2 burn-amount expressions rebounded to v44 MIN_BURN_AMOUNT-compatible values (lines 1295 + 1314)
- Natspec at line 1277 rewritten from SKIP to FLIPPED at v44.0
- `testFuzz_RngLockDeterminism_StakedStonkRedemption` PASSES at FOUNDRY_PROFILE=deep
- 306-04-SUMMARY.md written + AGENT-COMMITTED in single atomic commit
- Zero `contracts/*.sol` mutations
- SUMMARY.md includes the HANDOFF-111..117 7-row closure attribution table (1 block flipped + 6 subsumed)
- REG-01 NON-WIDENING attested with explicit minimal-diff scope
</success_criteria>

<output>
Write `.planning/phases/306-test-tst/306-04-SUMMARY.md` documenting the single-line flip, the HANDOFF-111..117 7-row closure attribution (1 explicit + 6 subsumed per FIXREC §0.6), REG-01 NON-WIDENING attestation with diff-scope enumeration, and the v45.0+ deferred coverage roster. Single AGENT-COMMITTED atomic commit envelope.
</output>
